# Architecture Reference

The folder structure IS the wicked problem as a near-decomposable hierarchy (ICM x Rittel & Webber x Simon); value concentrates at the weak edges between subsystems.

## ICM Layers 0-4

The canonical ICM Layer 0 through Layer 4 statement lives in docs/MINDRIAN-CANON.md (Appendix B); this file does not restate it. For the five engineering layers (PROMPT, CONTEXT, HARNESS, LOOP, GRAPH), see docs/LAYER-CONTRACT.md.

For the local SQL graph's own folder contract, see lib/core/navigation/CONTEXT.md: the two write chokepoints (node vs edge), what a claim's provenance actually is, and why an edge with a missing endpoint is legal rather than a defect.

Cascade rule: detect impact -> soft-edit affected sections -> APPROVE / REJECT (with reason) / DEFER -> decision becomes graph data.

Deep dive: docs/MINDRIAN-CANON.md (Appendix B) and docs/research/LIVE_DATA_ROOM_JTBD_PAPER.md (Simon, watchmaker, full grounding).
