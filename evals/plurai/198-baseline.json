{
  "schema_version": "1.0",
  "artifact_kind": "ci-baseline",
  "runtime_loaded": true,
  "method": "two_host_invocation_parity",
  "baseline_deferred": false,
  "synthetic_only": false,
  "measured_note": "Plan 198-10 replaces the Wave 0 baseline_deferred seed with the MEASURED invocation-parity baseline. scripts/198-plurai-gate-check.cjs reconstructs the CLI-leg governed transcript through tests/capture-198-parity-leg.cjs (the same shipped MCP tool spine the daemon calls: room_bind, reach card via gate-render, chain_run with a material halt, gate_answer approve, gated write through navigation.cjs, artifact_file), normalizes it host-invariantly (typed node label-set + edge source/target/type set + logical gate sequence, dropping volatile ids/timestamps and the host-specific renderer), and asserts membership == this baseline's expected_transcript -> verdict parity. The second-host (VS Code v1.102+ or MCP Inspector) leg is confirmed at the Plan 198-10 Task 3 human-verify checkpoint: the navigator runs the identical transcript over 127.0.0.1 and tests/parity-198.sh diffs both legs to an empty node/edge diff + identical gate sequence.",
  "source_transcript": "tests/parity-198.sh",
  "capture_harness": "tests/capture-198-parity-leg.cjs",
  "diff_harness": "tests/diff-198-parity.cjs",
  "judge_prompt": "Given the scripted parity transcript (bind room, reach card via gate_render, chain_run with halt, gate_answer approve, gated write via navigation.cjs, artifact_file) executed on two hosts, classify the pair as parity (identical typed graph writes -- node/edge diff empty -- and identical gate sequence on both hosts) or drift (any node/edge diff or gate-sequence mismatch).",
  "canon_contract": "SPEC-6 (surface parity), SPEC-8 (Plurai eval gate roadmap mandate)",
  "verdict_map": {
    "0": "parity"
  },
  "expected_transcript": {
    "gate_sequence": [
      "reach_card:binding",
      "chain_halt:/mos:act",
      "gate_answer:approve"
    ],
    "nodes": [
      { "type": "memory_event", "label": "artifact_file" },
      { "type": "memory_event", "label": "gated_write" },
      { "type": "memory_event", "label": "room_bind" }
    ],
    "edges": [
      { "source": "node:parity-a", "target": "node:parity-b", "type": "INFORMS" }
    ]
  },
  "precision": 1.0,
  "recall": 1.0,
  "accuracy": 1.0,
  "f1": 1.0,
  "row_count": 1,
  "label_semantics": {
    "parity": "identical typed graph writes + identical gate sequence on both hosts",
    "drift": "any node/edge diff or gate-sequence mismatch"
  },
  "generated_at": "2026-07-10T00:00:00.000Z",
  "generator": "198-10 Task 2 measured two-host invocation-parity baseline (CLI leg reconstructed live; VS Code leg confirmed at the Task 3 checkpoint)"
}
