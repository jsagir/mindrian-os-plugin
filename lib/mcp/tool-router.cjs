/**
 * MindrianOS MCP Hierarchical Tool Router (stub)
 *
 * Task 2 will populate this with 6 router tools covering all 41 CLI commands.
 */

'use strict';

const ALL_TOOL_COMMANDS = [];

function registerRouterTools(server, roomDir, pluginRoot, larryContext) {
  // Stub — Task 2 implements the 6 router tools
}

module.exports = { registerRouterTools, ALL_TOOL_COMMANDS };
