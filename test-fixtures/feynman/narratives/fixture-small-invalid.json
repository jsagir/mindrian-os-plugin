{
  "section": "problem-definition",
  "essence": "Deliberately invalid narrative used to exercise the tier-0 fallthrough path.",
  "plain_language": "This file is hand-crafted to fail validateNarrative: key_claims has length 2 which is below the minimum of 3 required by the schema.",
  "governing_thought": "This narrative is invalid by construction; the generator must fall back to tier-0 with a schema-validation warning on stderr.",
  "mental_model": {
    "analogy": "A form with a required field left blank.",
    "mapping": "The schema demands 3 to 5 key claims; this file provides 2. Every other field is valid so the failure point is isolated and deterministic.",
    "limits": "This fixture does not test every possible schema violation, only one representative failure."
  },
  "sweet_spot": "Used exclusively by scripts/vault-section-minto-generator.integration.test.cjs to assert that invalid narratives route through runTier0 rather than throwing.",
  "key_claims": [
    "Schema validation fires before any rendering occurs.",
    "The fallback runs under the same atomic write pattern as the happy path."
  ]
}
