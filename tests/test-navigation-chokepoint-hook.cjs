'use strict';
// Phase 109 Wave 0 stub. Plan 109-00 ships this stub; Plan 109-06 replaces with real assertions.
// Requirement: NAV-109-05
// Deliverable: pre-commit hook fails commits with direct room-db.cjs imports outside allow-list
// Pattern: matches tests/test-cross-room-memory.cjs CJS direct-CJS approach (no Mocha, no Jest, zero new npm dependencies).

process.stderr.write('MISSING - Wave 2 must implement pre-commit hook fails commits with direct room-db.cjs imports outside allow-list (Plan 109-06)\n');
process.exit(1);
