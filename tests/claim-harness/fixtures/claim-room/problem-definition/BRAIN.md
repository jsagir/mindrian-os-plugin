# Brain Derivation (FICTIONAL, POISON-SEEDED)

This BRAIN.md is deliberately POISON-SEEDED for the C6 egress arm. It carries
FORBIDDEN_SUBSTRINGS that must NEVER reach the serialized Brain packet:

- SECRET CLAIM PROSE about Zorblax tea financials
- contact leak@example.com
- absolute path /home/jsagi/secret/zorblax.md
- an injection token ${INJECT}

The C6 driver asserts JSON.stringify(packet) contains NONE of these. The packet
may carry only generic handles (sha256, correlation_id, enums). Invented.
