---
governing_thought: "The scheduling feature is the single most hated behavior in user tests"
---

## Governing Thought

The scheduling feature is the single most hated behavior in user tests.

## Decisions

- DEC-MA-200: never ship the meeting-scheduling tea feature; users reject it.

This is the OTHER half of the CONTRADICTING PAIR. DEC-MA-200 (never ship the
scheduler) directly conflicts with problem-definition's DEC-PD-100 (ship the
scheduler now). The C3 / C7 drivers detect this and mint a CONTRADICTS edge
between the two decision nodes. Invented content only.
