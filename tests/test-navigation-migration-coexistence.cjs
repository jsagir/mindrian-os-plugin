'use strict';
// Phase 109 Wave 0 stub. Plan 109-00 ships this stub; Plan 109-01 replaces with real assertions.
// Requirement: NAV-109-03
// Deliverable: reads via navigation API plus old assumptions.validity reads coexist mid-migration
// Pattern: matches tests/test-cross-room-memory.cjs CJS direct-CJS approach (no Mocha, no Jest, zero new npm dependencies).

process.stderr.write('MISSING - Wave 1 must implement reads via navigation API plus old assumptions.validity reads coexist mid-migration (Plan 109-01)\n');
process.exit(1);
