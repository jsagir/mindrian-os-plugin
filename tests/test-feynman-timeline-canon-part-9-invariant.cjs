'use strict';
// Phase 124 Wave 0 stub. Plan 124-00 ships this stub; Plan 124-04 replaces it with real assertions.
// Requirement: TEMPORAL-124-10
// Deliverable: Canon Part 9 forbidden-substring sweep (renderer + runner + scripts/feynman-timeline-refresh-command.cjs source files MUST NOT require brain-client; MUST NOT contain fetch/http/https near a render path; fs-instrument allow-list catches any read outside the FEYNMAN.md being written + room.db family; adversarial seed sweep proves the renderer reads ONLY SQL; regression assertion that the renderer imports navigation.cjs)
// Pattern: mirrors lib/memory/brain-derivation.test.cjs Test 13 + Test 14 forbidden-substring sweep + tests/test-brain-packet-part8-invariant-per-job.cjs adversarial seed + tests/helpers/fs-instrument.cjs allow-list (Phase 109-10).

process.stderr.write('MISSING - Wave 3 must implement the Canon-Part-9-invariant forbidden-substring sweep (renderer + runner source: no brain-client require, no fetch/http, no fs reads outside FEYNMAN.md + room.db) (Plan 124-04)\n');
process.exit(1);
