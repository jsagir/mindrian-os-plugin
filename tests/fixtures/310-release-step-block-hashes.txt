# Phase 310 baseline -- pre-change sha256 of every `# --- Step` block in scripts/release.sh
# Generated: 2026-09-10T00:00:00Z
# Purpose: the byte-level scope tripwire (tests/run-all-310.sh leg 3) proves that only an
# authorized region changes in a given phase; every other block must stay byte-identical to
# this file.
#
# Generating command (reproducible):
#   F=scripts/release.sh
#   mapfile -t LINES < <(grep -n '^# --- Step' "$F" | cut -d: -f1)
#   N=${#LINES[@]}; TOTAL_LINES=$(wc -l < "$F")
#   for ((i=0; i<N; i++)); do
#     start=${LINES[$i]}
#     if (( i+1 < N )); then end=$(( LINES[$((i+1))] - 1 )); else end=$TOTAL_LINES; fi
#     header=$(sed -n "${start}p" "$F")
#     hash=$(sed -n "${start},${end}p" "$F" | sha256sum | cut -d' ' -f1)
#     echo "${hash}  ${header}"
#   done
#
# Block i runs from its header line to the line before the next header (last block runs to EOF).
# Format per data line: <sha256>  <header line text or pinned label>
#
# Phase 341 Plan 05 REBASELINE (2026-09-10): this fixture was regenerated wholesale from the
# post-341-edit release.sh using the exact command above (no normalization applied at
# generation time -- every hash below is the literal, current content). Phase 341's authorized
# edit regions, all now baked into this fresh baseline (leg 3's per-block special-casing for
# these regions is retired -- see tests/run-all-310.sh's own comment):
#   - "# --- Step 4: ..." header + body -- D-01 npm-source rewrite (source object rebuilt
#     wholesale as {source, package, version}, ref/url deleted, no registry key).
#   - "# --- Step 6.7: ..." header + body -- D-06 vendoring replaced by npm-shrinkwrap.json
#     generation delegated to scripts/release-lib/shrinkwrap-gate.sh.
#   - "# --- Step 9.5: ..." body -- D-06/D-08 payload gate: node_modules/ ban and the
#     docs/+release.sh blacklist alternatives removed, delegates to
#     check-release-payload-ceiling.cjs --check.
#   - "# --- Step 7.5: ..." body -- the Commit-B `git rm -r --cached node_modules` untrack
#     block deleted (nothing to untrack any more).
#   - "# --- Step 7: ..." body -- header comment updated to describe npm-source distribution
#     instead of vendored node_modules (no code-path change).
#   - the top-of-file preamble region (lines 1-79) -- the "What it does" comment block updated
#     to describe the D-01/D-06 pipeline (Step 4 npm source, Step 6.7 shrinkwrap generation).
# The Phase 310 Step 5.5 rewrite and its Step 1 dry-run-preview single-line delta remain
# baked into this same fresh baseline (both blocks are ordinary hash-matched entries now;
# no code-level normalization is needed since this whole fixture is current-state, not
# pre-310 pinned).
#
# Quick task 260917-o1y REBASELINE (2026-09-17): this task's OWN authorized region is the
# "# --- Step 9.7: ..." body (the npm-propagation-poll extraction, R1-R3 of the Step 9.7
# fix). While reproducing the "confirm EXACTLY ONE line differs" check this task's own
# context prescribed, the current release.sh was found to already carry THREE further,
# unrelated regions of drift against this fixture's 2026-09-10 Phase 341 rebaseline, none
# caused by this task and none touched by it: the top-of-file PREAMBLE hash (lines 1-79), the "# --- Step 0:
# ..." block, and the "# --- Step 1: ..." block all changed, and two new blocks
# ("# --- Step 0.6: ..." and "# --- Step 5.6: ...") were never added to this fixture. Both
# are already-merged, pre-existing history: Step 0.6 (the Theo command-registry stamp gate)
# landed in commit 886e9854d (Phase 343 Plan 07, WD-13); Step 5.6 (the Theo notify gate)
# landed in commit 2dde2ecc1 (Phase 349 Plan 04, NOTIFY-02) -- both after the 2026-09-10
# rebaseline and neither one rebaselined this fixture at the time. Leaving that drift
# unaddressed would leave leg 2 and leg 3 permanently red regardless of this task's own
# change, so this task's fixture edit (already an authorized file for this task) absorbs
# all four pre-existing regions into a fresh wholesale regeneration using the exact
# generating command above, the same mechanism and precedent the Phase 341 REBASELINE note
# above already established -- alongside this task's own Step 9.7 line. The gate-count
# baseline below is left untouched at its existing value (a floor check, still valid: the
# current non-comment `exit 1` count is 54, above the pinned 49 baseline).
#
# NOTE on the two literal marker labels used as data-line keys below (the top-of-file
# PREAMBLE hash label and the gate-count baseline label): leg 2 and leg 4 of
# tests/run-all-310.sh find their data line with a plain `grep` on the exact label text, so
# this comment block deliberately never spells either label verbatim -- doing so would give
# grep a second matching line and corrupt both legs' `awk '{print $1}'` read, exactly the
# failure this note exists to prevent recurring.
#
# Phase 353 Plan 02 Task 10 (2026-09-17, R-353-G): this task's OWN authorized
# region is exactly two blocks, re-pinned below and nowhere else: the
# "# --- Step 0: ..." block (NO_LEDGER_CHECK=0 flag var + the --no-ledger-check
# case arm + USAGE_BLOCK addition) and the "# --- Step 2.4: ..." block (the
# offline section-command-ledger --check staleness gate, gated on
# NO_LEDGER_CHECK, added after the corpus-stats gate and before the
# SHAPE-GATE-BEGIN sentinel). No other block's hash changed; the top-of-file
# preamble hash and every other Step header below are untouched from the
# quick 260917-o1y rebaseline above (this note deliberately never spells
# that label verbatim, same reason the note below states). The header count
# stays 30; no block was added or renamed.

92da4abfaa63fdabcafc4b4e2055c940b797d208095e8202bc5e0c90ce17e389  PREAMBLE(1-79)
49  EXIT1_NONCOMMENT_COUNT
1c036be81ee9272602d48a4379a72b71b760a5851adaaf890f534b67b5c4e66b  # --- Step 0: Parse bump type + flags ---
8ac50432c8f28bd9286ca28dc76e175e6063f2ca9f8da2d940a8a3c839dff4b9  # --- Step 0.5: semver preflight ---
381f0b828cfa270d4e3ad38b4c2dc6889c63c5b757905fce94f4523db0da9d58  # --- Step 0.6: Theo command-registry stamp gate (Phase 343 Plan 07, WD-13) ---
77fd77595fba5ff1fbb071331cdb8c22c7803296bcf7b58a0cc7eb1a098a90f2  # --- Step 1: Get current version + compute NEW_VERSION via semver ---
a7138c5c7a062e8183da61b88808f312c8c5749d472d1be23ab332422abab802  # --- Step 2: Run FULL verification (not just validation) ---
41828022f754a39eec6898347b5f1c2f3e27bff4ee3e9f379c1e06f4c4b454b3  # --- Step 2.4: coverage gates (Canon Part 11 R2/R9/INV-10 step 3, Phase 172-13) ---
e6690f7ddc5bb2b45dc2bdcd4ac072c317846f75ba3856fe930d0ecab3303dd4  # --- Step 2.5: doctor --acceptance --pre-flight (HARD ABORT) ---
4c2555d9f406b7b7a740ae475711c174efdcea1463e04638db88fa378d025a30  # --- Step 3: Bump plugin.json AND package.json (5-way version consistency) ---
03b1c1b59397babb7315279a0ca9b7b24b2edc485cd92829091332851c975d75  # --- Step 4: Bump marketplace.json (version + npm source pinned to the exact version) ---
b7eb8f743631c04a89a00122ba21c444ca4814efa2787dcafe09aa4d19998bbe  # --- Step 5: Validate marketplace ---
18290b2e2b310aecbfc51e999d8e8d9f0cdbc23c5e191274421fe875ed560eeb  # --- Step 5b: Reserved-marketplace-name compliance (Phase 95.6 D-11a) ---
eecf491caddd6ba0460373e26b9ca2904c71316a9e7b0a69febd3d5f1da9ad0c  # --- Step 6: Check for CHANGELOG entry (finalize [Unreleased] -> [NEW_VERSION] - <date>) ---
6942bf9f10191f947a0239e8b5f736756c6e3b761551ec10d869c392de590c1f  # --- Step 6.5: Post-bump re-verification ---
4a673d15472560a069428db62a70276bb3518d4d19fb6417603708d944b8e7d3  # --- Step 6.6: doctor --acceptance --pre-tag (HARD ABORT, no --allow) ---
af0daa242242cdf547f3693049b92f06d1fdb90718d998476f79ff45d7ffa9dc  # --- Step 6.6a: module-registration verification (Phase 139 Plan 04, S4) ---
951a39dd9f476e71dc88c6da6e2875073a25c1b4c5d02f931c11491ee048c952  # --- Step 6.6b: doctor acceptance-gate SELF-COVERAGE (Phase 126 Plan 03) ---
4a40f6afccd5d969dea769ac7785732b5fe36900a0a0469cb65fd7b82278acf7  # --- Step 6.7: Generate npm-shrinkwrap.json (dependency delivery for the npm source) ---
d249466f5de96ee2c0658e82d32465cbaa699e0d81edf5b715a40195a2327b5c  # --- Step 7: Commit A (release commit) -- finalizes vN, NEVER git add -A ---
8ca05b5251db5f818c87af1f42af16e32ff08a42337e528fff75d7b35a260cd2  # --- Step 9.5: Publish @mindrian_os/cli at NEW_VERSION (BEFORE Commit B) ---
20370c7537cc97536740af536aee61007557cac94883a1bba4087e0461e50f12  # --- Step 9.6a: Sync install minisite to NEW_VERSION (HARD lockstep, vercel-CLI deploy) ---
17ec90feebcd1dd9aee35588898e00a46c52d34c09228f51969264704a802697  # --- Step 9.6b: Sync Mindrian website FALLBACK_VERSION to NEW_VERSION ---
084b326171a9014ff86e73999bb8d4a878c6cc742d76828c90291bec6225d126  # --- Step 9.7: npx-publish self-test (Phase 126 Plan 04 + Phase 126.1 hotfix) ---
e8cf01a8843222d55618df9d132bd9c4f4ca9b82522a8bc3a79055eb8fc33530  # --- Step 7.5: Commit B (next-bump commit) -- plugin.json/package.json -> next pre-release ---
d8ebd65d1682a2653b13b4e403cc693c3be0f563002fd4f27a30fd4ecbc59e44  # --- Step 8: Dirty-repo / ahead-of-origin guard (BEFORE the push) ---
a6be87ffa237c13d06dee29a95f119b0d12e1a0e97b67c82a403afe4f9209376  # --- Step 9: Push both ---
bd5ce06ecf71b31a29c94414cad83c56683d5a043a5c59190cf5be360704ac41  # --- Step 5.5 (tag-push verification): runs AFTER Step 9 push. ---
ca7f8c12b5c3d907be81ea79fd5fe25f6eb38f63e9d09ba2455285ee8a411b0f  # --- Step 5.6: Notify Theo of the new version (repository_dispatch) ---
cfdacfc8f16ca724187da17ceb865b3e55b6b75985669b26656377efa7b957d1  # --- Step 9.8: doctor --acceptance (full, HARD ABORT, no --allow) ---
ed8f1ecb5d8064ea90719a8784429fe5336d3676e1a03ac3668738df0988bda5  # --- Step 10: Update local cache ---
e031d5a5bc7695fdc1673a6f417a5752120f037107d7b340da6e84d9bcc77891  # --- Step 11: Post-release verification ---
