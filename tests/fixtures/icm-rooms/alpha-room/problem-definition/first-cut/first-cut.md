# First cut

A Key Decision 16 nested artifact: the artifact sits in its own subfolder
(`section/name/name.md`). This folder carries no `ROOM.md` by rule -- an
artifact folder never carries an `icm_self` block, and the doctor treats one
that does as drift.

Fixture prose only, hyphens only, small.
