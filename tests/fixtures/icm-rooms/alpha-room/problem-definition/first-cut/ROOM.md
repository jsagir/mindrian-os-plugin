---
directory_type: artifact
icm_layer: 0
auto_scaffolded: true
purpose: "Key Decision 16 nested-artifact folder. Committed here ONLY to satisfy this repo's own pre-commit ROOM.md+MINTO.md invariant (Canon decision 15); room-map.cjs classifies this folder as kind: artifact by STRUCTURE (a non-.room-root, non-section, non-structural folder nested under a section), never by the presence or absence of this file. Task-level tests that need a TRUE artifact-folder-with-no-ROOM.md exercise a runtime tmpdir copy with this file removed, never this committed path."
---

<!-- Fixture for Phase 353. See tests/fixtures/icm-rooms/README.md. This file
     exists only to satisfy the repo's pre-commit Data Room invariant; it
     carries no icm_self block and none of this phase's writers may ever add
     one here (R-353-B: artifact folders are mapped, never blocked). -->

# first-cut

Fixture placeholder identity file for a Key Decision 16 nested-artifact
folder. See `first-cut.md` for the actual artifact content.
