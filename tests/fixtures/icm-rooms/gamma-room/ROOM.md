---
directory_type: root
icm_layer: 0
auto_scaffolded: true
purpose: "The gamma-room fixture root: one section, one undeclared sub-room. Committed WITH a root ROOM.md to satisfy this repo's pre-commit Data Room invariant (Canon decision 15); the distinct missing-root-ROOM.md scenario Task 3 needs (root ROOM.md created from the identity template) is exercised via a runtime tmpdir copy of this room with ROOM.md removed, never via this committed path -- see tests/fixtures/icm-rooms/README.md."
---

<!-- ICM Layer 0 identity file, root vocabulary. Fixture for Phase 353. -->

# gamma-room

The gamma-room fixture root: one section (solution-design), one undeclared
sub-room (sub-rooms/delta-sub, no job_id).
