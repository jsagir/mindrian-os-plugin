{
  "schema_version": 1,
  "captured_at": "2026-05-19T23:50:00.000Z",
  "produced_by": "scripts/load-embeddings-into-neo4j.cjs (synthetic-pre-live-load placeholder; will be overwritten by captureIndexConfig() on operator's first live load against Neo4j Aura per 127.1-02-PLAN.md Task 2 Step 1)",
  "index_name": "mindrian_methodology_vec",
  "synthetic_pre_live_load": true,
  "synthetic_note": "This fixture is the post-DDL-apply contract shape that Neo4j Aura WILL return once mcp-server-brain/sql/127.1-vector-index.cypher is executed against the live instance via scripts/load-embeddings-into-neo4j.cjs. The DDL is committed in this same plan; the live capture happens when the operator runs the loader with NEO4J_URI/NEO4J_USER/NEO4J_PASSWORD sourced from the mcp-server-brain Render env vars. Surface 2 harness asserts the shape (state=ONLINE, type=VECTOR, dim=1024, cosine, property=embedding) which is structurally determined by the DDL, NOT by the live load. The synthetic flag exists so a future drift between this fixture and the live SHOW INDEXES row surfaces as a clear signal in CI.",
  "rows": [
    {
      "name": "mindrian_methodology_vec",
      "state": "ONLINE",
      "type": "VECTOR",
      "entityType": "NODE",
      "labelsOrTypes": ["MethodologyChunk"],
      "properties": ["embedding"],
      "indexConfig": {
        "vector.dimensions": 1024,
        "vector.similarity_function": "cosine",
        "vector.hnsw.m": 16,
        "vector.hnsw.ef_construction": 100,
        "vector.quantization.enabled": true
      }
    }
  ]
}
