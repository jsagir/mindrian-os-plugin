'use strict';
// Phase 109 Wave 0 stub. Plan 109-00 ships this stub; Plan 109-08 replaces with real assertions.
// Requirement: NAV-109-07
// Deliverable: storeBrainSuggestions writes proposed-only plus Phase 108 invariant SQL returns 0 rows
// Pattern: matches tests/test-cross-room-memory.cjs CJS direct-CJS approach (no Mocha, no Jest, zero new npm dependencies).

process.stderr.write('MISSING - Wave 3 must implement storeBrainSuggestions writes proposed-only plus Phase 108 invariant SQL returns 0 rows (Plan 109-08)\n');
process.exit(1);
