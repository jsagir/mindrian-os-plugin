# ProblemType Closed-Set -- RATIFICATION-READY DRAFT (parked behind METER)

> STATUS: DRAFT EVIDENCE, NOT CANON. This mints nothing, freezes nothing, touches no .cjs, and trips no clause.
> It is the artifact Appendix D entry 32 will need the moment the METER phase returns a real two-gauge reading
> and the entry-31 self-binding clause clears. Until then it is parked: the freeze is the lock AFTER the evidence,
> not the rule before it. Drafting an allow-list is evidence-gathering; ratifying it as a closed governed set is
> the amendment, and the amendment waits.

Date: 2026-06-27. Source: live Neo4j read of all 26 `ProblemType` nodes + their `ADDRESSES_PROBLEM_TYPE` in-degree.

## Why this exists

`ProblemType` is the context vocabulary the invocation spine routes against -- the WHEN in WHEN / WHICH / SEQUENCE.
It is the ONE constitutional vocabulary the canon never froze. Part 3 froze the verbs, Part 4 froze the edges,
entry 15 froze the reach count, entry 24 froze the node taxonomy. ProblemType stayed open, and it rotted the way
open sets rot: it accreted three taxonomies and four junk instances. Freezing it (as entry 32) is the keystone of
the WHEN half of the moat. This draft is the allow-list that freeze will adopt.

## 1. CANONICAL CORE -- the proposed closed set (KEEP, freeze as the entry-32 allow-list)

Both sub-families are already canon-named: Part 2 Engine 2 classifies on TWO axes -- definition clarity (PWS) and
system complexity (Cynefin). The core IS those two axes; nothing new is invented.

### 1a. PWS definition-clarity axis + wickedness (wire counts = live in-degree)
| Context | Wired | Canon source |
|---|---|---|
| Undefined Problem (UDP) | 32 | Part 2 Engine 2 |
| Ill-Defined Problem (IDP) | 42 | Part 2 Engine 2 |
| Well-Defined Problem (WDP) | 25 | Part 2 Engine 2 |
| Wicked Problem | 20 | Rittel & Webber; Part 1 |
| Tame Problem | 0 | canonical pair to Wicked -- KEEP (starved, wire it) |

### 1b. Cynefin system-complexity domains
| Context | Wired | Canon source |
|---|---|---|
| Clear | 1 | Cynefin (Part 2 Engine 2 complexity axis) |
| Complicated | 2 | Cynefin |
| Complex | 3 | Cynefin |
| Chaotic | 2 | Cynefin |
| Disorder | 0 | Cynefin "no-domain-yet" -- KEEP (starved) |

**Core = 10 members.** Five starved (Tame 0, Disorder 0, Clear 1) are canonical-but-underwired -- keep and WIRE,
do not retire. Underwiring is a trigger-edge gap (the hygiene step), not a vocabulary defect.

## 2. RETIRE -- with reasons (zero-wired problem INSTANCES mis-filed as context CATEGORIES)

| Node | Wired | Retire reason |
|---|---|---|
| Costco Product Development Challenge | 0 | A workshop/capstone problem INSTANCE, not a context category. Nothing routes to it. |
| PPB pollutant simulation | 0 | A research INSTANCE. Category space pollution. |
| Quantum Dot Sizing | 0 | A research INSTANCE. Category space pollution. |
| The Duchess Joy Problem | 0 | A teaching-example INSTANCE. Category space pollution. |

These four are the unambiguous deletes: zero in-degree AND categorically wrong kind (instances, not categories).
Retiring them is HYGIENE (unblocked, do now) -- it is the live R2 projection-correctness work READER depends on.
The FREEZE that makes the retirement permanent-by-constitution is the entry-32 part that waits for METER.

## 3. OPEN DECISION -- navigator-LOCKED, the one thing to lock before entry 32

Two non-canonical taxonomies that ARE wired. A vocabulary you cannot explain is still a vocabulary in USE -- 34
live leadership wires means something routes there, so this is a "sort," not a "delete."

### 3a. Leadership taxonomy (~34 live wires -- strongly used)
Delegation Gap 7 | Leadership Identity 6 | Scaling Leadership 6 | Team Conflict 6 | Culture Drift 5 | Advisory Dysfunction 4

### 3b. Ops-failure taxonomy (6 live wires -- thin)
System Failure 1 | Quality Defect 1 | Safety Incident 1 | Process Breakdown 1 | Recurring Failure 1 | Operational Underperformance 1

**The decision (lock at the entry-32 checkpoint):**
- **Option A -- keep as a GOVERNED SECOND LAYER.** A domain-specific context sub-vocabulary, frozen but SEPARATE
  from the PWS x Cynefin core -- the way Part 4 carries a domain-taxonomy edge layer beside the cascade edges.
  Evidence FOR: leadership's 34 live wires. A real, used routing layer.
- **Option B -- MERGE into core.** Map each onto a PWS x Cynefin cell (e.g. Team Conflict -> Complex/IDP) and
  retire the labels. Evidence FOR: a 10-member core is the cleanest possible router; ops (6x1) is barely used.
- **Likely split:** leadership = Option A (too wired to dissolve); ops = Option B or thin-keep (barely wired).
  But this is the navigator's call -- it is the only genuine judgment in the freeze. Lock it, then entry 32 writes it.

## 4. How this lands against v1.19

- The CLEAN (retire section 2 + wire the starved core + the separate teaching-tier dedup/stub work) = hygiene,
  unblocked, do now. It IS the v4 R2 gate that must pass before READER may read the projection.
- The FREEZE (adopt sections 1+3 as a closed, gate-enforced ProblemType vocabulary, the constitutional twin of
  Part 3 verbs / Part 4 edges) = Appendix D entry 32. Blocked by entry 31's self-binding clause until METER returns
  a real two-gauge reading from a live navigator on the gate.
- Sequence: METER -> (if a gate subject exists) clean as R2 -> freeze as entry 32, the FIRST amendment the
  self-bind earns. The thing that tried to jump the meter becomes the proof the clause was right to demand.

## 5. Out of scope for THIS draft (separate hygiene, also unblocked)
- methodology_tier tagging (pws / mindrian-operation) across the projection -- entry-19 marker, currently on ~4 nodes.
- ADDRESSES_PROBLEM_TYPE trigger edges FROM operations (commands/agents/pipelines), not only knowledge nodes.
- JTBD alias dedup (collapse the duplicate cluster to one + ALIAS_OF spokes); fill-or-retire the empty Framework stubs.
- DO NOT push the operation tier into Neo4j (Phase 137 deferred on purpose); DO NOT relax plane purity (zero user data in Brain).
