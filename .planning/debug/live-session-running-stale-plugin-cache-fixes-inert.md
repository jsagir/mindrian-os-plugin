---
status: investigating
kind: rca
trigger: "live-session-running-stale-plugin-cache-fixes-inert"
issue_id: ""
severity: high
surfaces: [cli]
brain_mode: tier-0
canon_parts: [11]
created: 2026-07-06T01:50:00Z
updated: 2026-08-20T14:30:00Z
---

## Source-of-Truth Preamble

- **CODE claims read against:** BOTH deliberately compared in this RCA -- `/home/jsagi/dev/MindrianOS-Plugin` (`main` @ commit `500ed3d4`, includes today's 260705-x85 fix) AND the actually-running install cache `/home/jsagi/.claude/plugins/cache/mindrian-marketplace/mos/1.15.3-beta.10/` (pre-fix, confirmed by diff below).
- **WIRE claims probe against:** N/A, no Brain/network.
- **Date of audit:** 2026-07-06
- **Re-verification rule:** unchanged from template default.

## Current Focus
<!-- OVERWRITE on each update - reflects NOW -->

hypothesis: [2026-08-20 UPDATE] Change 2 (session-id mismatch) CONFIRMED FIXED (commit `2cf99a11`, live in released `v2.0.0-beta.1`+, confirmed byte-identical on this machine's installed cache). Headline finding (Change 1, deployment-gap process gap) remains structurally TRUE and is freshly, independently reproducing on THIS exact machine right now (installed cache pinned to `2.0.0-beta.5` while the catalog is at `beta.7` and dev-repo tip is `beta.8`-in-progress) -- the two originally-cited fixes (`62b09ee8`, `0d47ed6d`) are confirmed live here, but the general process gap is not closed, only freshly re-demonstrated. Backstop-trigger mystery: no new evidence, thread unchanged. ORIGINAL (2026-07-06) hypothesis text preserved below for history.
test: [2026-08-20] N/A this pass -- reconnaissance/status-check pass only; no new hypothesis test run against the backstop mystery (no qualifying log entries existed to test).
expecting: [2026-08-20] N/A this pass.
next_action: [2026-08-20] NAVIGATOR ACTION NEEDED -- run `claude plugin update mos@mindrian-marketplace` (or `/mos:doctor --fix`) on THIS machine to pull the current release (`beta.7`) and close today's 2-release gap, then re-check `installed_plugins.json` to confirm the version field actually advances (this doubles as a live Test-2-style verification of the headline finding, doable right now on this machine). Separately: the backstop-trigger mystery stays blocked on a live occurrence -- nothing to do until `~/.mindrian/card-fire-intercepts.log` records a fresh `ascii-box-backstop-no-card` entry. A third, unconfirmed, NOT-yet-scoped observation (substantive, non-terse-turn `reached-registry-gate-no-card` intercepts on 2026-08-19/20) is flagged for the navigator's judgment call on whether it warrants its own new debug session -- NOT folded into this RCA's two named threads.

--- ORIGINAL (2026-07-06) ---
hypothesis: CONFIRMED (headline finding): this live session is executing `check-card-fire.cjs` from the INSTALLED plugin cache (`.../mos/1.15.3-beta.10/`), which does NOT contain today's 260705-x85 fix (confirmed by `diff`, byte-identical except for the missing `gate_subject_text` plumbing). The fix was committed to the dev repo's `main` branch but no version has been cut/released, so there is no tagged version for `claude plugin update` to pull even if run -- the fix is currently unreachable by ANY live session, this one or a future one, until a release ships. SECONDARY anomaly (NOT fully explained, do not treat as confirmed root cause): re-running the installed module's own `deriveTurnSignals` + `classifyCardFire` against this session's REAL transcript and REAL session_id (`eef8823c-74ed-414f-834b-7b6beceb5eb0`, confirmed correct because it matches the `__session__:` key in `card-fire-retries.json`) reconstructs `{ intercept: false, reason: 'no-gate-signal' }` -- i.e. by this reconstruction the hook should NOT have intercepted at all for that turn. It demonstrably DID intercept live, with reason `ascii-box-backstop-no-card`. A genuinely separate anomaly was found alongside this (the `card-fire-reached.json` side-channel's PRIMARY entries are keyed under a SHORT id `0e74d0de55c2` that does not match the long session UUID used elsewhere in the same install, meaning `recordReachedGate`'s caller(s) and `check-card-fire.cjs`'s Stop-stdin `session_id` disagree on session identity) -- this may or may not be related to the backstop-trigger mystery; not yet connected with evidence, flagged as its own open thread.
test: reconstructed `classifyCardFire`'s verdict directly against the installed module + the real transcript file + the real session_id; verdict came back non-intercepting, contradicting the observed live intercept. Not yet reconciled.
expecting: PENDING. Either (a) the real Stop-hook stdin envelope carries fields this reconstruction did not supply (untested: whether Claude Code sends anything beyond `{hook_event_name, transcript_path, session_id, stop_hook_active}` that would change the outcome), or (b) the transcript tail read at live-eval-time differed from the tail available now (untested: whether anything appended to the transcript AFTER the block but read as part of "the same tail window" could explain it -- unlikely given WR-08's tail-cap logic, but not ruled out), or (c) the actually-installed 1.15.3-beta.10 cache differs from BOTH copies just diffed in some way not yet checked (e.g. a THIRD, even-older cached version, or a symlink/resolution surprise in how `${CLAUDE_PLUGIN_ROOT}` resolves at hook-invocation time vs. how this investigation resolved the path manually).
next_action: before spending more effort chasing the exact backstop trigger, resolve the HEADLINE finding first (no fix is live without a release) since it is higher-value and fully confirmed; treat the backstop-trigger mystery and the session-id-key mismatch as two SEPARATE, still-open follow-on threads, not solved by this session's work. Do not fabricate a fix for either without further live reproduction with proper instrumentation (e.g. a debug log added temporarily to the installed copy's `main()` to capture the exact stdin envelope and computed verdict at the moment of a live intercept, then removed).

UPDATE 2026-07-22: headline finding re-confirmed live, independently, 16 days later, on a THIRD stale version (`1.15.3-beta.32`), using the diagnostic log this RCA's own `next_action` called for shipping (now live, see Evidence). Neither open secondary thread advanced by this occurrence (Change 2 did not reproduce broken this time -- not confirmed fixed, just not observed; the BACKSTOP ascii-box mystery did not reproduce at all this session). next_action UNCHANGED: cut a release (or accept the documented manual-cache-copy workaround for same-session verification only) is still the only thing that closes the headline finding; Change 2 and the backstop mystery still need their own dedicated reproduction.

UPDATE 2026-07-23: FOURTH occurrence, same `1.15.3-beta.32` version, one day later, this time reported directly by an end-tester (not the navigator dog-fooding) who explicitly tried to update and could not. This occurrence sharpens the diagnosis: multiple releases (beta.34, beta.36, beta.38, beta.40, beta.42) shipped in the 21 days since this tester's install, meaning "no release has been cut yet" (the original headline framing) does NOT explain this instance -- releases existed the whole time and this tester's `claude plugin update mos@mindrian-marketplace` still reported "already at latest" against the stale beta.32 install. This squarely matches Incident A / `docs/upstream-reports/2026-04-28-claude-plugin-update-misreports-state.md`'s framing (the plugin manager's cache -> live-install transition lying about state), not just this RCA's narrower "dev fix not yet released" framing -- the two documents describe the same underlying Anthropic-side gap from two angles (ours: "the fix is inert"; theirs: "the manager lies about update state"), and should be read together going forward. See Evidence below for the new artifact this occurrence surfaced (a `/plugin marketplace update` invocation mistake) and cross-ref to the upstream report. next_action UNCHANGED (cut releases regularly; the manager-side fix is Anthropic's to make) plus a NEW recommended mitigation: point affected testers at `/mos:doctor --fix` as the first-line workaround rather than repeated `claude plugin update` retries, since retrying the same broken comparison produces the same false "already at latest" result every time.

UPDATE 2026-08-20 (reconnaissance pass, no live user trip -- status check per navigator directive after a 4-week gap and a project version-line jump 1.15.3 -> 1.16.0 -> 2.0.0): Change 2 (session-id mismatch) is now CONFIRMED FIXED -- commit `2cf99a11` (2026-07-17, an unrelated defect's fix that happens to touch the exact same call site) is a confirmed ancestor of the released `v2.0.0-beta.1` and verified byte-identical on this machine's own installed cache. The headline finding (Change 1) does NOT get to close: both originally-cited fixes (`62b09ee8`, `0d47ed6d`) are confirmed live on this machine, but the GENERAL process gap is freshly, independently reproducing on this exact machine RIGHT NOW -- the installed cache is pinned to `2.0.0-beta.5` while the marketplace catalog is already at `2.0.0-beta.7` and the dev-repo tip is `2.0.0-beta.8`-in-progress (7 files differ, including at least one live hook). The backstop-trigger mystery has ZERO new evidence (the diagnostic log has exactly 2 entries since 2026-07-23, both PRIMARY-path, neither backstop). Full detail in Evidence below.

## Meta

- Repo: /home/jsagi/dev/MindrianOS-Plugin
- Plugin version: 1.15.3-beta.10 (installed cache; dev repo `main` is ahead by uncommitted-to-a-release work including 260705-x85)
- Reported by: navigator, live session, "SEE A NOTHER BUG AGAIN" after an `ascii-box-backstop-no-card` intercept
- Date first observed: 2026-07-06 (this specific trip); the deployment-gap finding applies retroactively to every fix landed this whole session (260705-x85 and the memory-lifecycle.cjs pointer fix both share this exposure)
- Related debug sessions: `.planning/debug/resolved/stale-voice-log-session-summary-banner-frozen-after-first-stop.md` and `.planning/debug/recurring-reach-card-defeats-relevance-gate-and-hsi-clamp-garbage.md` (Finding 2) -- BOTH of those fixes are ALSO sitting in the dev repo only, subject to this exact same deployment gap. Every verification claim in both those files' Resolution sections is true of the DEV REPO code; neither fix has been observed live in a running session yet, because none can be until a release ships.

## Problem Statement

Every code fix landed in this session (`scripts/check-card-fire.cjs`'s PRIMARY relevance-gate fix, `scripts/memory-lifecycle.cjs`'s pointer-lifecycle fix) is committed to the dev repo's `main` branch but has zero effect on this or any other live Claude Code session, because Claude Code plugins execute from an installed marketplace cache pinned to a released, tagged version -- not from the dev git repo directly -- and no release has been cut.

## Symptoms
<!-- Written during gathering, then IMMUTABLE -->

expected: after committing a fix to `scripts/check-card-fire.cjs` in the dev repo, the SAME bug class should stop reproducing in this live session (or at minimum, a fresh session after a plugin update should pick it up).
actual: the identical bug CLASS (a Stop-hook card-fire intercept the navigator judges spurious) reproduced again in this same session AFTER the dev-repo fix was committed, because the running session was never executing the fixed code in the first place.
errors: `Stop hook error: ascii-box-backstop-no-card` (a DIFFERENT reason string than the `reached-registry-gate-no-card` fixed earlier today, and, per this RCA's still-open secondary anomaly, not yet reconciled with a manual reconstruction of the same verdict from the installed module).
reproduction:
  1. Land a fix in `/home/jsagi/dev/MindrianOS-Plugin` (this repo, per its own CLAUDE.md the only dev workspace).
  2. Do NOT run `scripts/release.sh` / cut a version / run `claude plugin update`.
  3. Continue the SAME live Claude Code session (or start a new one without updating).
  4. `diff` the installed cache's copy of the touched file against the dev repo's copy -- confirms the fix is absent from what is actually executing.
started: structural since the very first fix landed this session (260705-x85); only NOTICED as a distinct problem once this specific "another bug" trip prompted checking whether the fix had actually taken effect.

## Scope and Impact

- Affected surfaces: cli (this is about the plugin *distribution* mechanism, not code specific to one surface -- Desktop/Cowork installs would have the identical exposure, since they too pull from the marketplace-pinned version, not a dev checkout)
- Affected commands: none directly; this is a release/distribution-process gap, not a command defect
- Affected users: nobody yet, since no release has shipped with today's fixes -- but every user remains exposed to BOTH bugs (the relevance-gate false positive and the pointer/voice-log staleness) until a release does ship
- Version range: N/A (this is not a version-range regression; it is "the fix exists nowhere a user can get it yet")
- Severity: high (not because the code is wrong, but because two real, tested, committed fixes are currently worthless to any actual user or session until released -- silent gap between "fixed in the repo" and "fixed for anyone")
- Blast radius: every fix landed in ANY session against this repo shares this exposure until `scripts/release.sh` actually cuts and ships a version. This is a systemic property of the CLAUDE.md-documented release/update model (`docs/autopsies/2026-04-13-wrong-workspace-incident.md`, `.claude/includes/release-process.md`), not a one-off bug in either fix.

## Eliminated
<!-- APPEND only - prevents re-investigating -->

- hypothesis: The `ascii-box-backstop-no-card` trip is a regression caused by today's 260705-x85 fix itself (i.e. the fix introduced a new bug).
  evidence: `diff` confirms the installed cache does not contain 260705-x85 at all -- the code that intercepted is the PRE-fix version. The fix cannot have caused a regression in code it was never running against.
  timestamp: 2026-07-06T01:50:00Z
- hypothesis: The trip is explained by the same PRIMARY-path mechanism 260705-x85 fixed (comparing `outputText` against `precedingUserText`).
  evidence: the reported reason was `ascii-box-backstop-no-card` (the BACKSTOP path), not `reached-registry-gate-no-card` (the PRIMARY path 260705-x85 touched). Manually re-testing `ASCII_BOX_GLYPH_RE` against the exact captured `output_text` for this turn returns `false` (no match) using BOTH the dev-repo copy and the installed copy (byte-identical regex/functions in both). The mechanism is not yet understood, but it is confirmed NOT to be the same one already fixed.
  timestamp: 2026-07-06T01:50:00Z

## Evidence
<!-- APPEND only - facts discovered -->

- timestamp: 2026-08-20T14:30:00Z
  checked: git ancestry of the F.8 `emitBindingGate` -> `recordReachedGate` call site's `sessionId` source (`scripts/intent-classifier.cjs` `resolveSessionId`, lines 967-992) against the release tag history, plus a live byte-identity check of this exact machine's installed plugin cache against dev-repo HEAD for the same file.
  found: commit `2cf99a11` ("fix(reach-sensor-relevance-gap): align CLI session-binding read key with the MCP room_bind UUID (A1)", 2026-07-17) rewrote `resolveSessionId` to PREFER `STDIN_SESSION_ID` (the real Claude session UUID read directly from the hook's own stdin payload via `extractSessionId(STDIN_RAW)` -- same source/format as `check-card-fire.cjs`'s Stop-hook `session_id`) ahead of the old fallback chain (`CLAUDE_SESSION_ID` env, then `sha256(roomDir+ISO-day).slice(0,12)` -- the exact 12-hex-char short-id shape that produced the mismatched `"0e74d0de55c2"` key this RCA found on 2026-07-06). `git merge-base --is-ancestor 2cf99a11 v2.0.0-beta.1` confirms YES: this fix is an ancestor of the FIRST 2.0.0 release (cut 2026-08-10), so every release since carries it. Directly confirmed live on THIS machine: `scripts/intent-classifier.cjs` is byte-identical between the installed cache (`~/.claude/plugins/cache/mindrian-marketplace/mos/2.0.0-beta.5/`) and current dev-repo HEAD (not present in the 7-file diff enumerated below).
  implication: Change 2 (secondary anomaly -- session-id mismatch between the F.8 `recordReachedGate` call site and `check-card-fire.cjs`'s Stop-hook `session_id`) is CONFIRMED FIXED, deployed in the currently-released version line, and live on at least this machine's installed cache. This closes Required Code Changes Change 2 and Test 1. Root cause, now understood precisely: not a bug in `recordReachedGate` itself -- the CALLER's `sessionId` source (`resolveSessionId`'s fallback chain) resolved to a sha256-hash short id whenever `STDIN_SESSION_ID`/`CLAUDE_SESSION_ID` were both absent at the F.8 call site's process context at the time of the 2026-07-06 observation. The 2026-07-17 fix (landed for an unrelated but adjacent defect, "reach-sensor-relevance-gap" A1) made `STDIN_SESSION_ID` the first-choice source, which happens to be exactly what `check-card-fire.cjs`'s Stop-hook stdin also carries -- so the two producer/consumer sides now agree by construction, not by coincidence.

- timestamp: 2026-08-20T14:30:00Z
  checked: git tag history, `CHANGELOG.md`, `~/mindrian-marketplace` (marketplace catalog repo) git log + `marketplace.json`, and this machine's own `~/.claude/plugins/cache/mindrian-marketplace/mos/` + `installed_plugins.json`, per this session's reconnaissance directive.
  found: the project has shipped SIX releases since this RCA's last update (2026-07-23): `v1.15.3` (final), `v1.16.0-beta.1` through `beta.13`, then a full major bump to `v2.0.0-beta.1/3/5/7` (2026-08-10 through 2026-08-19); dev-repo `main` is now at an unreleased `v2.0.0-beta.8`-in-progress. Both fix commits this RCA originally cited (`62b09ee8`/260705-x85, `0d47ed6d`) are confirmed ancestors of `v2.0.0-beta.1` (`git merge-base --is-ancestor`, both YES) -- shipped in the very first 2.0.0 release, 2026-08-10. The marketplace catalog (`~/mindrian-marketplace/.claude-plugin/marketplace.json`, local clone last synced 2026-08-19 per its own git log) correctly points at `2.0.0-beta.7`, the current release tag. HOWEVER: this exact machine's ACTUAL installed cache (`installed_plugins.json`) records `mos@mindrian-marketplace` at version `2.0.0-beta.5`, `lastUpdated: 2026-08-10T23:59:00.040Z` -- i.e. `claude plugin update mos@mindrian-marketplace` has not been run on this machine since the day beta.5 shipped, despite beta.7 having released 9 days later (2026-08-19) and beta.8 already in progress. Diffing the installed beta.5 cache's `scripts/` directory against current dev-repo HEAD: 7 entries differ (`brain-response-sanitize-hook.cjs`, `check-dual-graph-health.cjs`, `doctor.cjs`, `part8-egress-guard-hook.cjs`, `session-start`, plus 2 stale `.pyc` caches); `check-card-fire.cjs` AND `intent-classifier.cjs` (the two files this RCA's own findings concern) are BOTH byte-identical, confirming the two ORIGINALLY-cited fixes are live here even though the machine as a whole is stale. `part8-egress-guard-hook.cjs` is a registered live hook (`hooks/hooks.json`); commits since beta.5 touching it include `d4b81626` ("fix(part8): extend the Brain-tool matcher to the canonical connector name (WS-D1)").
  implication: this is a FRESH, LIVE, CURRENT (2026-08-20) instance of the exact same headline-finding structural pattern -- a real release exists and is correctly catalogued, but this specific machine's installed cache is stale by two releases and has not been refreshed, so whatever `d4b81626` and its siblings fixed is inert HERE, right now, mirroring beta.10/12/32 exactly, just with a different file cast and a smaller (2-release, not 8-10-release) gap. The ORIGINAL two fixes this RCA opened on (`62b09ee8`, `0d47ed6d`) ARE confirmed live on this machine specifically, closing that narrow claim -- but the GENERAL headline finding (Change 1, the process gap itself) is NOT closable as "resolved": it is actively, currently reproducing on the very machine running this investigation. Per this RCA's own Test 2 criterion (live-session pickup, not just tag existence), full closure requires running `claude plugin update mos@mindrian-marketplace` (or `/mos:doctor --fix`, this RCA's own prior recommended mitigation) on this machine and re-confirming -- a navigator-facing action, not something this session should execute unilaterally (running the Claude Code CLI's own plugin-update subcommand from inside a running Claude Code agent session risks unpredictable interaction with the current session's own plugin state and was out of scope for this reconnaissance).

- timestamp: 2026-08-20T14:30:00Z
  checked: `~/.mindrian/card-fire-intercepts.log` (the diagnostic instrument this RCA's own next_action called for shipping 2026-07-11) for any entries since the last-analyzed 2026-07-23 cutoff.
  found: exactly 2 entries total in the entire file, both freshly dated (`2026-08-19T09:30:42.995Z`, `2026-08-20T09:30:33.176Z` -- today), both `reason:"reached-registry-gate-no-card"` (the PRIMARY/registry path), NEITHER `ascii-box-backstop-no-card` (the BACKSTOP path this thread concerns); `matched_glyph_span` is empty on both, as expected for a non-backstop reason. Separately, unprompted by this RCA's own two named threads: both entries' `output_text` is substantive, on-topic, non-terse assistant content (an OAuth authorization walkthrough; a multi-paragraph knowledge-graph framework analysis with a markdown table and a cypher block) -- NOT the "hi"/"yes"-shaped terse turns `0d47ed6d`'s title names, which is the specific failure mode that fix already closed (confirmed live per the entry above). This does not match either of this RCA's two open secondary anomalies and has NOT been investigated; flagging as a raw, unconfirmed observation only, per this RCA's explicit no-fabrication discipline -- it may be entirely correct/expected behavior (e.g. a genuine binding-gate reach that legitimately needed a card and only the render step degraded), or it may be a distinct, not-yet-diagnosed third failure mode.
  implication: the backstop-trigger mystery (this RCA's other open secondary anomaly) has ZERO new evidence since 2026-07-23 -- nothing to analyze, thread status UNCHANGED, still genuinely open, still requires its own live occurrence to progress (the diagnostic log is working as designed; it has simply not caught a backstop-path intercept since). Recommend a SEPARATE, dedicated debug session if the navigator judges the two fresh substantive-turn intercepts spurious; do not fold into this RCA's existing threads. Also confirmed per this session's reconnaissance step 6: the "rethinking-mindrianos: file ... research trail" commits visible in this session's initial git context (`8a65c9ee6`, `05ebc4ee3`, `d3d2578b3`, `036887d31`, `990f545f5`) are NOT commits in this repo (`git show`/`git log` on any of them returns "unknown revision" here) -- they belong to the separate `~/MindrianRooms/rethinking-mindrianos/` room repo (confirmed by finding the identical commits at the head of that repo's own `git log`), filed research notes with no code-change relationship to this RCA. Off-topic, correctly ruled out rather than assumed.

- timestamp: 2026-07-23T14:20:00Z
  checked: an end-tester's own transcript, relayed by the navigator ("look at this a user trying to update - it's not true. check the repo"), showing two commands run in sequence: (1) `/plugin marketplace update` invoked INSIDE a Bash tool call (`Bash(/plugin marketplace update 2>&1; claude plugin update mos@mindrian-marketplace 2>&1)`), which errored `(eval):1: no such file or directory: /plugin` -- a slash command shelled out as a literal binary path never resolves, so the marketplace catalog refresh never actually ran; (2) `claude plugin update mos@mindrian-marketplace` (a real CLI subcommand, ran fine as a shell invocation) reported `✔ mos is already at the latest version (1.15.3-beta.32)`. Cross-checked this session's own repo state at the time: both the local `~/mindrian-marketplace` clone AND the GitHub remote (`raw.githubusercontent.com/jsagir/mindrian-marketplace/master/.claude-plugin/marketplace.json`) correctly read `version: 1.15.3-beta.40` (the then-current release) with a valid, fetchable git tag -- ruling out "our catalog is wrong" as an explanation from our side.
  found: two independent contributing factors, not mutually exclusive: (a) a NEW tactical finding -- the tester's own agent invoked `/plugin marketplace update` incorrectly (via Bash, not as an actual slash command at the interactive prompt), so the catalog refresh step never ran on their machine; (b) this RCA's own already-documented headline finding (cache -> live-install transition lying about state) remains the more likely dominant explanation given the SIZE of the gap -- 1.15.3-beta.32 is a minimum of 8 releases behind the-then-current beta.40 (and 10 behind the beta.42 cut later this same day), a gap far larger than "just forgot to refresh once."
  implication: this is a FOURTH confirmed occurrence of the same recurring pattern (beta.10, beta.12, beta.32-on-07-22, beta.32-on-07-23), now witnessed by an actual end tester rather than the navigator's own dog-fooding, and now co-occurring with a distinct tactical mistake (slash-command-via-Bash) that this RCA had not previously logged as a contributing variable. Recommended workaround going forward for any affected tester: skip re-running `claude plugin update` (it will report the same false "already at latest" against an unrefreshed/mistranslated cache) and go straight to `/mos:doctor --fix`, which compares the live install's `plugin.json` against the highest version actually present in the marketplace cache directory and repairs the drift directly, independent of whether `claude plugin update`'s own internal state is trustworthy.

- timestamp: 2026-07-22T12:09:09Z
  checked: fresh live reproduction, independent session (`d1f799bb-84f3-4d98-9887-12e23836f082`), independent date (16 days after this RCA's last update), against a THIRD stale-cache version (`1.15.3-beta.32`, installed cache `lastUpdated: 2026-07-20` per `installed_plugins.json`) while the dev repo `main` was already at `dd8d11c6` (past the `v1.15.3-beta.34` release, containing commit `0d47ed6d` "fix: stop hook force-fires stale Decision-Gate cards on unrelated terse turns", committed 2026-07-21 -- one day after this cache's last sync). `diff` confirmed the installed copy of `scripts/check-card-fire.cjs` lacks the `gateStale`-aware `gateTopicallyRelevant(...)` calls `0d47ed6d` added. `~/.mindrian/card-fire-intercepts.log` captured 8 intercepts across one conversation: 6x `reached-registry-gate-no-card`, 2x `bounded-escape-released-after-3-retries` (same underlying trigger, retry-cap released the turn instead of a 4th block); ZERO `ascii-box-backstop-no-card` occurrences this session (`gate_signature` and `matched_glyph_span` empty on every entry -- this is the PRIMARY/registry path, not the BACKSTOP/ascii-box path this RCA's still-open secondary mystery concerns). `ran_entries: ["scripts/intent-classifier.cjs"]` on literally every turn: the F.8 `emitBindingGate` sanhedrin-room reach re-registered as "reached" every single turn regardless of turn content (turns ranged from "hi" to an unrelated `/gsd-graphify` slash command to plain "yes"/"start"), because the cold-room sensor keeps re-firing the identical candidate reach and beta.32's relevance gate has no staleness signal to suppress the force-fire on low-signal terse turns -- exactly the failure mode `0d47ed6d`'s title describes.
  found: headline finding (deployment gap) reproduces IDENTICALLY on a third version pair, 16 days after this RCA's last entry, confirming it is not a one-off and remains unclosed by any release. ADDITIONALLY: `~/.mindrian/card-fire-reached.json`'s key this session is the FULL long session UUID (`d1f799bb-84f3-4d98-9887-12e23836f082`), matching the Stop-hook's own `session_id` exactly -- no short-id mismatch observed this session, unlike the 2026-07-06 finding. `card-fire-retries.json` has zero keys (no `__session__:` entry at all) for this session, a different shape than 2026-07-06's observation, not yet explained.
  implication: (1) headline finding re-confirmed live with independent evidence, strengthening the case for cutting a release -- this is now a THIRD documented version (beta.10, beta.12, beta.32) hitting the identical deployment gap, spanning 16 days. (2) the secondary session-id-mismatch anomaly (Change 2) did NOT reproduce this session -- either fixed upstream since 2026-07-11, or this specific call path resolves correctly, or condition-dependent; NOT confirmed fixed, just not observed broken here -- Change 2 stays open pending a direct code read of the `recordReachedGate` call site. (3) the BACKSTOP ascii-box mystery (this RCA's other open secondary anomaly) did not reproduce this session either -- no new evidence for or against it. (4) this session's intercepts are fully explained by the ALREADY-DIAGNOSED headline mechanism; unlike the 2026-07-06/07-11 entries, nothing here needs the "pre-redaction thinking content" hypothesis invoked.

- timestamp: 2026-07-11T06:30:00Z
  checked: the diagnostic instrument this RCA's own next_action called for is now SHIPPED (in `scripts/check-card-fire.cjs`, via `backstop-benign-list-defeats-relevance-gate` CR-07, navigator-approved same /goal directive).
  found: a LOCAL append-only JSONL at `~/.mindrian/card-fire-intercepts.log` is now written on EVERY `classifyCardFire` intercept OR degrade, capturing `{ timestamp, session_id, reason (the ORIGINAL slug), gate_signature, ran_entries, matched_glyph_span (the ACTUAL matched ASCII_BOX_GLYPH_RE text via the new `matchedGlyphSpan` helper), output_text (truncated ~4000) }`, TTL-pruned by the same `RETRY_TTL_MS` as the retry side-file (cannot grow unbounded). Canon Part 8: local disk only, never Brain, never network.
  implication: the NEXT occurrence of this RCA's still-open "unexplained backstop trigger" secondary anomaly becomes a one-log-read diagnosis instead of a multi-hour forensic reconstruction. The record's `matched_glyph_span` + `output_text` will show EXACTLY what text the live hook scanned at intercept time -- directly testing the leading "pre-redaction thinking content" hypothesis (whether the hook fired on raw reasoning text the persisted transcript no longer retains). This RCA's HEADLINE finding (deployment gap) and the session-id-key mismatch remain OPEN; the trigger mystery is NOT fixed here, only made diagnosable. STATUS UNCHANGED (still investigating).

- timestamp: 2026-07-11T05:55:00Z
  checked: a THIRD, independent live intercept, same "unexplained backstop trigger" symptom, different session (`924af059-4997-40ca-a8a2-9ed1f4e72486`), different date, different installed version (v1.15.3-beta.12, confirmed the currently marketplace-pinned version per `~/mindrian-marketplace/.claude-plugin/marketplace.json` `ref: v1.15.3-beta.12`, NOT a stale cache this time -- ruling out the headline stale-cache finding as the explanation for THIS occurrence specifically). Reconstructed the exact transcript state at block-time (truncated the real session `.jsonl` to the exact line ending in the blocked turn's final text) and fed it through the INSTALLED module's own `deriveTurnSignals` + `classifyCardFire`.
  found: verdict = `{"intercept":false,"reason":"no-gate-signal","degrade":false}` -- byte-for-byte the same non-reproducing result this RCA already found on 2026-07-06 against a different session. The flagged turn's only assistant content was a single plain-prose text block with a normal closing question ("...Want me to check that instead?"), zero brackets, zero numbered lines; `ASCII_BOX_GLYPH_RE.test()` against the exact persisted text returns `false`, confirmed both by hand-copying the string and by the truncated-transcript reconstruction above.
  implication: this is now CONFIRMED RECURRING (not a one-off): the same "reconstruction says no-intercept, live behavior says intercept with `ascii-box-backstop-no-card`" gap has now been independently observed twice, on two different dates, two different sessions, two different plugin versions (beta.10 and beta.12), ruling out "it was just that one stale cache" as a full explanation. The leading untested hypothesis from this session: the turn's transcript record had a `type:"thinking"` content block (`thinking:""` plus an opaque `signature`, i.e. redacted/encrypted at persistence time) immediately before the final `type:"text"` block. If the REAL, un-redacted thinking content available to Claude Code at completion time (before persistence scrubs it to `thinking:""`) contained a numbered-list-shaped span -- very plausible, chain-of-thought commonly reasons "1. option A, 2. option B" -- and the hook's real runtime input included that raw text, the backstop would fire on invisible reasoning the user never saw rendered, and it would be permanently unreproducible after the fact because the persisted transcript never retains the pre-redaction text. NOT yet confirmed; this session did not have access to pre-redaction thinking content to test it directly. The diagnostic log proposed in this RCA's own `next_action` (not yet implemented as of this timestamp) is the only way to actually confirm or eliminate this on the next live occurrence -- implementing it is now in scope via `.planning/debug/backstop-benign-list-defeats-relevance-gate.md`'s updated `next_action` (2026-07-11, navigator-approved via /goal), since it is the same script and the same diagnostic gap.
  cross-ref: also confirmed this session, a SEPARATE but adjacent defect: `card-fire-block-surface.md` Finding 1's `systemMessage` fix does not achieve its intended effect live (the raw `reason` slug still reached the user as "Stop hook error: ascii-box-backstop-no-card" despite `systemMessage` being present in the v1.15.3-beta.12 code) -- this is NOT explained by a stale cache (beta.12 is the currently-pinned version and does contain the `systemMessage` line), so it is a premise-was-wrong bug in that RCA's own fix, not a deployment-gap instance. Filed as a reopen note directly in that RCA.

- timestamp: 2026-07-06T01:50:00Z
  checked: `diff /home/jsagi/.claude/plugins/cache/mindrian-marketplace/mos/1.15.3-beta.10/scripts/check-card-fire.cjs /home/jsagi/dev/MindrianOS-Plugin/scripts/check-card-fire.cjs`
  found: the installed cache is missing exactly the `gate_subject_text` plumbing added by commit `62b09ee8` (260705-x85) -- otherwise byte-identical.
  implication: this live session (and any session using this install) has been running the PRE-fix relevance gate the entire time, including during the earlier turns this session that were reported as "fixed."
- timestamp: 2026-07-06T01:50:00Z
  checked: `node scripts/check-card-fire.cjs`'s exported `readTranscriptTurn` called directly (both dev-repo and installed copies) against this session's real transcript file (`/home/jsagi/.claude/projects/-home-jsagi/eef8823c-74ed-414f-834b-7b6beceb5eb0.jsonl`)
  found: `output_text` = the exact final assistant message of the blocked turn ("Drafted with the full play-by-play..."); `preceding_user_text` = `""` (empty); `gate_signature` = `""` (empty, degenerate).
  implication: `preceding_user_text` being empty is ITSELF explained (not mysterious): the immediately-preceding `role: user` transcript record was a `tool_result` block (from the `create_draft` Gmail tool call), and `extractAssistantText` finds no `.text` property on a bare `tool_result` envelope, so it correctly (if unhelpfully) resolves to `''`. This is expected behavior of the extraction function given tool-result-heavy turns, not itself a bug -- but it does mean the Phase 210-05 relevance gate's `gateTopicallyRelevant` call always takes its CONSERVATIVE "insufficient user text -> stays relevant" branch on any turn whose last user-role record was a tool_result rather than typed text, independent of the PRIMARY/BACKSTOP distinction.
- timestamp: 2026-07-06T01:50:00Z
  checked: `ASCII_BOX_GLYPH_RE.test(output_text)` directly, both copies
  found: `false` -- the captured output_text does not match any of the four glyph alternatives.
  implication: `backstopHit` should have been `false` for this exact text, contradicting the live-observed intercept reason.
- timestamp: 2026-07-06T01:50:00Z
  checked: `~/.mindrian/card-fire-reached.json` and `~/.mindrian/card-fire-retries.json` (the two side-channel/retry files, read directly)
  found: `card-fire-reached.json` has exactly one key, a 12-hex-char short id `"0e74d0de55c2"`, holding two `{entry: "scripts/intent-classifier.cjs", shape: "F.8", ts: ...}` records. `card-fire-retries.json` has a `__session__:eef8823c-74ed-414f-834b-7b6beceb5eb0` key (the FULL session UUID, confirmed correct because it is the same id as this session's actual transcript filename).
  implication: whatever caller invokes `recordReachedGate` for the F.8 emitBindingGate path is passing a DIFFERENT session identifier (`0e74d0de55c2`) than the one `check-card-fire.cjs` receives from the real Stop-hook stdin (`eef8823c-74ed-414f-834b-7b6beceb5eb0`) -- a genuine session-id mismatch between the PRIMARY producer and consumer. Reconstructing `deriveTurnSignals` with the CORRECT (long) session_id therefore finds `ran_entries: []` (the sidechannel lookup misses under the mismatched key), so `primaryHit` is provably false for this turn by reconstruction -- yet `reached-registry-gate-no-card` was not the reported reason anyway (the reported reason was the BACKSTOP one), so this mismatch does not yet explain the observed intercept either. It is a real, separate, confirmed anomaly worth its own follow-up regardless.
- timestamp: 2026-07-06T01:50:00Z
  checked: full `classifyCardFire` reconstruction via the installed module, real transcript, real session_id, retry_count=0, session_count=0
  found: verdict = `{ intercept: false, reason: 'no-gate-signal', degrade: false }`
  implication: this reconstruction says the hook should NOT have intercepted at all for this turn. It demonstrably did, live, with reason `ascii-box-backstop-no-card`. The discrepancy is NOT YET EXPLAINED. Candidate explanations (untested): the real Stop-hook stdin envelope may carry fields not supplied in this manual reconstruction (unverified whether Claude Code ever sends direct `output_text`/`ran_entries`/`gate_signature` fields alongside `transcript_path`, which per `deriveTurnSignals`'s own precedence rule would OVERRIDE the transcript-derived values used here); or the retry/session counters read from `card-fire-retries.json` at live-eval-time differed from the 0/0 assumed here (the file shows non-zero counts under OTHER keys, meaning the real ctxHash for this exact turn was not computed/tested here and could plausibly have been in a degrade or different-reason branch this reconstruction did not model).

## Technical Root Cause

Headline finding (CONFIRMED):
- Site: N/A (not a code site -- a release/distribution-process gap)
- Cause: Claude Code plugins execute from `${CLAUDE_PLUGIN_ROOT}` (the installed marketplace cache pinned to a released, tagged version per `.claude/includes/release-process.md`), never from a dev git checkout directly. Fixes committed to `/home/jsagi/dev/MindrianOS-Plugin`'s `main` branch have no effect on any running session until `scripts/release.sh` cuts a version AND the user runs `claude plugin update mos@mindrian-marketplace`.
- Why it surfaces now: became visible only because this session happened to check `diff` against the installed cache after a second, unexplained intercept prompted suspicion that the earlier fix might not be real -- it is real, it is simply not deployed.

Secondary anomaly (NOT YET CONFIRMED as a root cause of anything specific -- documented as found, not as solved):
- Site: whatever call site invokes `sidechannel.recordReachedGate(...)` for the F.8 `emitBindingGate` path in `scripts/intent-classifier.cjs` (both the installed and dev-repo copies -- not yet diffed against each other for THIS specific call site's `sessionId` argument, since the diff run so far only showed the 260705-x85-related hunks)
- Cause: unconfirmed. The `sessionId` value passed to `recordReachedGate` at that call site resolves to something that stringifies to a short id (`0e74d0de55c2`) rather than the long UUID `check-card-fire.cjs` receives from Stop-hook stdin.
- Why it surfaces now: only visible by directly reading the side-channel JSON file and comparing its keys against the confirmed-correct session id from a sibling file (`card-fire-retries.json`'s `__session__:` key) -- nothing in the normal Stop-hook output surfaces this mismatch to a user or developer.

## Required Code Changes
<!-- Explicit, imperative, one block per change -->

- Change 1 (headline finding -- this is a PROCESS change, not a code change):
  - Location: N/A
  - Current behavior: fixes land in the dev repo and are asserted "fixed" without confirming a live session can actually observe them.
  - Required behavior: before declaring ANY Stop-hook / enforcement-script fix "resolved" in an RCA, either (a) explicitly note in the RCA's Non-Code Follow-ups that the fix is inert until the next release ships and is NOT yet observable live, or (b) if immediate live verification is wanted, copy the fixed file(s) directly into the installed cache as a manual, documented, temporary override for THIS session only (clearly log that this is a non-standard, non-persistent verification step, never a substitute for a real release).
  - Short-term patch: none code-side; this RCA itself IS the short-term fix (making the gap visible and documented).
  - Long-term fix: consider whether this repo wants a documented "dev-loop" fast path (e.g. a `scripts/dev-sync-installed-cache.sh` that copies dev-repo changes into the local installed cache for the CURRENT machine only, clearly marked as a local dev convenience, never touching the marketplace/release path) so a developer iterating on Stop-hook-class fixes can verify live behavior without a full release cut every time. Out of scope to build here without navigator sign-off (it touches the install/cache boundary this repo treats carefully per its autopsies).
- Change 2 (secondary anomaly -- RESOLVED 2026-08-20, no action needed):
  - Location: `scripts/intent-classifier.cjs`'s `emitBindingGate` (F.8) call site to `recordReachedGate`, sourced via `resolveSessionId()` (lines 967-992).
  - Original behavior (2026-07-06): `sessionId` resolved to a sha256(roomDir+day).slice(0,12) short id whenever `STDIN_SESSION_ID`/`CLAUDE_SESSION_ID` were unavailable, disagreeing with the Stop-hook's long-UUID `session_id`.
  - Confirmed fix: commit `2cf99a11` (2026-07-17, filed under the unrelated "reach-sensor-relevance-gap" A1 defect) made `STDIN_SESSION_ID` (the real hook-payload UUID, same source `check-card-fire.cjs` reads) the first-choice source ahead of the short-hash fallback. Verified an ancestor of the released `v2.0.0-beta.1` and byte-identical on this machine's live installed cache. No further action -- this thread is closed.

## Tests to Add or Update

- Test 1:
  - Type: integration
  - Location: new or extended test covering `scripts/intent-classifier.cjs`'s `emitBindingGate` -> `recordReachedGate` call
  - Given: a live-shaped Stop-hook session_id (a long UUID, matching what Stop-hook stdin actually carries)
  - When: `emitBindingGate` fires and calls `recordReachedGate`
  - Then: assert the `sessionId` argument passed to `recordReachedGate` is the SAME long UUID, not a derived/truncated/different id
  - Runner registration: nearest existing intent-classifier / 209-06 primary-sidechannel suite
- Test 2:
  - Type: process / release-checklist (not a code test)
  - Location: N/A
  - Given: any future Stop-hook-class fix
  - When: the RCA is marked resolved
  - Then: the RCA's Non-Code Follow-ups explicitly states whether the fix has been verified against a live installed session or only against the dev-repo test suite

## Non-Code Follow-ups

- CHANGELOG.md: once the secondary anomaly is confirmed and fixed, add a Fixed entry; the headline finding is a process gap, not a code fix, so no CHANGELOG entry applies to it directly.
- Release lockstep: this RCA is itself the argument FOR actually running a release soon -- both 260705-x85 and the memory-lifecycle.cjs pointer fix are sitting inert. Recommend the navigator consider cutting a release (`scripts/release.sh`) sooner rather than later specifically so these two already-tested fixes become real for actual users, not just for the dev repo's test suite.
- Canon: none identified beyond Part 11 (declared above) for the release/distribution-gap framing.
- knowledge-base.md: add a summary block on resolve (of the secondary anomaly; the headline finding is a process note, add a short knowledge-base entry for it too since it explains a pattern likely to recur).
- Docs / monitoring: consider whether `CLAUDE.md`'s GSD Workflow Enforcement section should gain a line reminding future sessions that a dev-repo fix to a hook script is NOT observable live without a release, to prevent this exact confusion recurring.

## Resolution
<!-- OVERWRITE as understanding evolves -->

root_cause: Headline finding CONFIRMED (deployment gap: dev-repo fixes are inert until a release ships AND the install actually pulls it) and RE-CONFIRMED LIVE on 2026-08-20, on this exact machine, as a fresh 2-release-behind instance (installed `2.0.0-beta.5` vs. catalog `2.0.0-beta.7` vs. dev-repo tip `2.0.0-beta.8`-in-progress) -- this is a recurring, self-perpetuating process gap, not a one-off closed by any single release cut. Secondary anomaly 1 (session-id mismatch, Change 2): CONFIRMED FIXED -- commit `2cf99a11` (2026-07-17, "align CLI session-binding read key with the MCP room_bind UUID (A1)"), confirmed an ancestor of the released `v2.0.0-beta.1` and confirmed byte-identical on this machine's live installed cache. Secondary anomaly 2 (unexplained `ascii-box-backstop-no-card` backstop trigger): UNCHANGED, still genuinely unexplained -- zero new evidence available as of this 2026-08-20 pass (diagnostic log has 2 entries since 2026-07-23, both PRIMARY-path, neither backstop).
fix: Change 2 required no NEW fix this session -- it was already fixed by prior, unrelated work (`2cf99a11`) that this RCA had not yet connected to its own open thread; this session's contribution is confirming and closing that connection with direct evidence (commit ancestry + live cache byte-identity, not inference). Change 1 (headline) still has no code fix by design (it is a release/process gap, not a code defect); the actionable step -- run `claude plugin update mos@mindrian-marketplace` (or `/mos:doctor --fix`) on this machine to close today's specific 2-release gap -- is a navigator action, not committed here. The unexplained backstop trigger has no fix proposed; not fabricating one without a fresh live occurrence to instrument.
verification: Change 2 verified via `git merge-base --is-ancestor` (ancestor of released `v2.0.0-beta.1`, confirmed) plus a live file-identity check against this machine's actual installed cache (confirmed identical) -- both direct, both pass. Change 1's general pattern re-verified live via `installed_plugins.json` + `marketplace.json` + dev-repo tag comparison, all directly observed on this machine, not inferred. Backstop mystery: not verified, not disproven, unchanged.
files_changed: []
commits: []

## Reconnaissance Pass Summary (2026-08-20)

No code changed this session -- this was a status-check / evidence-gathering pass per navigator
directive, not a fix session. Net effect: Change 2 moves from "confirmed to exist, not yet
connected" to "confirmed fixed, connection proven." Change 1 stays open but sharpened with a live,
current, on-this-machine reproduction (see Evidence above) -- proving the pattern is structural
and recurring, not something a single release cut ever fully retires. The backstop mystery
(Change/anomaly 2 in the original framing) is untouched, blocked on a live occurrence. This RCA
file stays at `status: investigating`; it should NOT move to `resolved/` yet (one of its two named
open threads is still genuinely open, and the headline finding's Test 2 live-pickup criterion is
not yet satisfied for THIS machine specifically).

NAVIGATOR DECISION (2026-08-20): on the reconnaissance pass's two open items --
(A) live-pickup verification (Test 2): navigator declined to have this session run
`claude plugin update`/`/mos:doctor --fix` unilaterally; routed the decision to the concurrent
peer session (jsagi-e7, same working tree, doing unrelated v2.1.0 GSD work) to run or defer as
it judges best given its own in-flight state. Still open as of this note -- check that session's
own debug/state notes for the outcome before assuming it's closed.
(B) the third-failure-mode watch item (2 substantive-turn `reached-registry-gate-no-card`
intercepts, 2026-08-20, neither matching the terse-turn pattern `0d47ed6d` already fixed):
navigator confirmed LOG AND WATCH, not a new debug session -- consistent with this RCA's own
discipline against concluding from 2 data points. No new session filed. Revisit only if a third
occurrence lands.

THIRD occurrence of the watch item, same session, same day (2026-08-20T11:44:53.510Z,
session_id `a2d37a80-9f1b-487d-abeb-1ef2379a09eb`): `reached-registry-gate-no-card`,
`gate_signature` empty, `matched_glyph_span` empty (PRIMARY path, not BACKSTOP), `ran_entries:
["scripts/intent-classifier.cjs"]`. This is the F.8 `emitBindingGate` room-binding gate,
observed re-arming as "reached" on literally every turn of this entire session (visible in the
UserPromptSubmit hook context every turn as "session unbound: choose which room(s)..."), never
satisfied because the content each turn correctly had zero connection to room-artifact filing
(100% dev-repo/GSD work) -- matching this RCA's own guidance to skip an irrelevant gate, which
the enforcement hook has no signal to distinguish from a gate that was simply never rendered.
Same mechanism as the 2026-07-22 evidence entry (cold-room sensor force-fires F.8 every turn,
relevance gate has no staleness/irrelevance signal to suppress it), but that entry only had
terse "hi"/"yes"-style turns as examples -- THIS occurrence intercepted a long, substantive,
fully-relevant status-update turn, proving the false-positive isn't limited to low-signal terse
text. Resolved for this session by actually firing the pending room-bind card on request
(navigator chose "dev repo / no room"), not by a code fix. This is now a THIRD data point for
the "watch item," not a second -- worth promoting off pure-watch status: the common factor
across all three is F.8/emitBindingGate specifically staying "reached" with no expiry once a
session goes unbound and never resolves it, not a general PRIMARY-path defect. Candidate root
cause for a future session to test: `recordReachedGate`'s F.8 write path may have no TTL/one-shot
guard the way other reach candidates do, so it re-qualifies as "reached" every turn until the
gate is actually answered, however many turns that takes.
