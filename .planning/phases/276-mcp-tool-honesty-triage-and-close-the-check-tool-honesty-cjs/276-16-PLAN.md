---
phase: 276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs
plan: 16
type: execute
wave: 6
depends_on: [276-15]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - CHANGELOG.md
  - .planning/debug/meeting-file-meeting-false-success.md
  - .planning/debug/knowledge-base.md
autonomous: false
requirements: [TOOLHON-07, TOOLHON-08, TOOLHON-02]
canon_parts: [6, 7, 8, 9, 11]

must_haves:
  truths:
    - "TOOLHON-01 through TOOLHON-14 are registered in REQUIREMENTS.md with measured evidence per row, matching the CHOKE and ANCHOR precedent"
    - "The ROADMAP no longer states a finding count the measurement contradicts, and its plan checkboxes reflect what shipped"
    - "Every named follow-up this phase deferred has a home and an owner, so nothing is silently dropped a second time"
    - "The meeting RCA carries a disposition that matches what actually shipped"
    - "The dev-research trail exists in both homes and both cross-links resolve"
    - "Desktop or Cowork meeting filing is verified against room.db by a human, never from a tool response"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "The TOOLHON-01 through TOOLHON-14 registration with per-row measured evidence"
      contains: "TOOLHON-01"
    - path: "CHANGELOG.md"
      provides: "The Unreleased entry for this phase"
      contains: "tool honesty"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs"
      via: "each registered row cites the plan and the measured command that proves it"
      pattern: "276-"
---

<objective>
Close the phase: register what it committed to, correct what it inherited, hand on what it deferred,
and have a human confirm the one thing no automated test on this machine can reach.

Five things land here.

**Registration.** `TOOLHON-01` through `TOOLHON-14` are phase-local working IDs minted in
`276-RESEARCH.md`. The precedent for registering them at phase close is established and consistent:
CHOKE-01 through 06 (Phase 273), PYPORT-01 through 07 (Phase 272), ANCHOR-01 through 10 (Phase
274), HOOK-01 through 12 (Phase 267.2), GUARD-01 through 10 (Phase 267.3). Each of those rows
carries MEASURED evidence, not a claim. Copy that discipline exactly.

**Correction.** TOOLHON-08: the ROADMAP still says "the 9 findings." The measured count was 10
before the D-1 fix and 24 after. The stale `Depends on: Phase 275` line was already corrected by the
navigator, so that half is verify-only. The dependency was refuted with evidence: Phase 275 is
room-schema work with zero keyword overlap against this phase's scan set, it is itself gated and not
plannable, and the line matches the known `phase.add` heading-boilerplate defect already caught once
for this same entry.

**Hand-on.** This phase exists because Phase 273's C4 and C5 were deferred and then nearly orphaned.
The one thing it must not do is repeat that pattern with its own deferrals. Every named follow-up
gets a home and an owner in the ROADMAP, not a mention in a SUMMARY nobody reads again.

**Disposition.** `.planning/debug/meeting-file-meeting-false-success.md` sits at `partial-close` and
its own Current Focus forbids moving it to `resolved/` until the Tri-Polar gap closes. Plans 276-12
and 276-14 closed two of its three gaps; the third is structurally unreachable from MCP. That is a
real disposition and it needs writing down honestly, which may mean `resolved` and may mean a
narrowed `partial-close`. Decide from what shipped, not from what was hoped.

**Human verification.** No automation harness exists for the Desktop or Cowork surface. The
Tri-Polar rule makes a three-surface check a stated default, and this phase's whole subject is that
a tool's own success claim is not evidence. So the last task is a human, on a real surface, checking
`room.db` directly. The 2026-09-03 session that produced this phase was built on exactly that
discipline: the first `file-meeting` call that session claimed success and wrote nothing.

Purpose: close the phase in a way a reader six months from now can audit.
Output: registration, corrections, follow-ups with owners, a truthful RCA status, a composited
research trail, and a human sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs/276-VALIDATION.md
@.planning/phases/276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs/276-15-SUMMARY.md

Do NOT read `.planning/STATE.md` in full (1.4MB). Grep it for a literal if needed.
Do NOT read `276-RESEARCH.md` in full (1300 lines). Read only the sections named per task.
</context>

<artifacts_this_phase_produces>

This plan (276-16) produces:

| Artifact | Kind | Path |
|---|---|---|
| `TOOLHON-01` through `TOOLHON-14` | new registered requirement IDs | `.planning/REQUIREMENTS.md` |
| A dated research entry | new directory | `~/MindrianRooms/rethinking-mindrianos/research/2026-09-03-same-disease-consolidation/` |
| The mirrored copy | new directory | `~/MindrianOS/research/` |

The full phase artifact list is in `276-01-PLAN.md` under `artifacts_this_phase_produces`.

</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: register TOOLHON-01 through TOOLHON-14, reconcile the ROADMAP, and write the CHANGELOG entry</name>

  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, CHANGELOG.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` lines 545-575, the Phase 273 CHOKE block IN FULL. It is the exact registration shape to copy: a `### Phase NNN` heading with a provenance paragraph naming where the IDs were minted and how many plans across how many waves, then one checked bullet per requirement, each ending with a `Measured:` clause carrying a real command and its real output. Then read lines 1101-1120, the Traceability section, which lists every phase-local family and when it was minted; this phase must be added there.
    - `.planning/ROADMAP.md` Phase 276 entry IN FULL, from the `### Phase 276:` heading through its `Plans:` list. Note the "9 findings" text in Layer 1 numbered item 1, the research-correction paragraph that already states 10 and 24, the `Requirements: TBD` line, the `Plans: 0 plans` line, and the placeholder plan bullet.
    - `CHANGELOG.md` top section, to find the open Unreleased block and the entry style. The current version is `2.0.0-beta.16` and the Unreleased section already carries the quick 260903-ljj entry.
    - Every `276-NN-SUMMARY.md`, for the measured evidence each registered row needs. A row without a measured command and its output does not meet the precedent.
  </read_first>

  <action>
    Registration. Add a Phase 276 block to `.planning/REQUIREMENTS.md`, placed with the other
    phase-local families, headed "Phase 276 - Same-Disease Consolidation (MCP plus local-graph
    false-success deep fixes) (minted at research time 2026-09-03)". Provenance paragraph: the IDs
    were minted in `276-RESEARCH.md`'s requirement table, scoped to Phase 276 only, 16 plans across
    7 waves.

    One bullet per ID, TOOLHON-01 through TOOLHON-14, each stating what the requirement was and
    ending with a `Measured:` clause carrying a real command and its real output drawn from the
    owning plan's SUMMARY. Mark a row complete only if it actually landed. If any did not fully
    land, mark it unchecked and say what remains and who owns it. A registration block that marks
    everything done regardless is the phase's own disease in its own paperwork.

    Add the TOOLHON family to the Traceability section alongside CHOKE, PYPORT, ANCHOR, HOOK and
    GUARD, naming where it was minted.

    ROADMAP reconciliation, TOOLHON-08. Two edits and one verification:
      - Correct the "9 findings" text in Layer 1 item 1 to the measured reality: 10 at the live
        sweep, 24 after the D-1 detector fix, and the final post-fix bucket split from
        `276-15-SUMMARY.md`. The research-correction paragraph already says the first two numbers;
        make the item itself agree so a reader does not have to reconcile two figures in one entry.
      - VERIFY the `Depends on:` line no longer names Phase 275. It was already corrected by the
        navigator; confirm rather than re-edit, and record the confirmation.
      - Update `Requirements:` from TBD to the TOOLHON range, update `Plans: 0 plans` to the real
        count, and replace the placeholder plan bullet with a wave-grouped plan list, one checked
        line per plan with a one-line description of what it actually shipped. Follow the shape of
        the Phase 267.3 and Phase 271 entries, which group by wave with a bold wave header.

    CHANGELOG. Add an entry to the existing Unreleased section, in the repo's usual terse style,
    covering: the dead switch-parser fix that took the sweep from 10 findings to 24; the corrected
    `orchestration`, `export`, `room_content`, `gate_render` and `graph_write` descriptions; the
    busy-timeout propagation; the typed `room_db_busy` and `room_db_broken` reasons; the new claim
    write MCP tool; and the meeting gate wiring. Do NOT bump any version and do NOT run
    `scripts/release.sh`. The five-place lockstep is a release action, not a phase-close action.
  </action>

  <verify>
    <automated>grep -c "TOOLHON-01" .planning/REQUIREMENTS.md && grep -c "TOOLHON-14" .planning/REQUIREMENTS.md && test "$(grep -c 'TBD (run /gsd-plan-phase 276' .planning/ROADMAP.md)" = "0" && test "$(grep -c 'tool honesty' CHANGELOG.md)" -ge "1"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "TOOLHON-01" .planning/REQUIREMENTS.md` and `grep -c "TOOLHON-14" .planning/REQUIREMENTS.md` each return at least 1, and every ID from 01 through 14 is present (check all fourteen).
    - Every registered row ends with a `Measured:` clause naming a real command and its real output. Spot-check three rows against their owning plan SUMMARY and record the three in the plan SUMMARY here.
    - Any requirement that did not fully land is marked unchecked with what remains and who owns it. If all fourteen landed, state that explicitly rather than leaving it implied.
    - The Traceability section names the TOOLHON family: `grep -c "TOOLHON" .planning/REQUIREMENTS.md` in the region after the Traceability heading is at least 1.
    - `grep -c "TBD (run /gsd-plan-phase 276" .planning/ROADMAP.md` returns 0.
    - The Phase 276 entry states the measured counts (10 and 24 and the final split) and no longer states "9 findings" as a live figure. If the phrase survives inside the historical research-correction paragraph, that is fine and must be noted, since that paragraph exists to record the correction.
    - `grep -c "Depends on:\*\* Phase 275" .planning/ROADMAP.md` returns 0, confirming the already-corrected line.
    - The Phase 276 `Plans:` count equals the number of plan files on disk: compare against `ls .planning/phases/276-*/276-*-PLAN.md | wc -l` and record both.
    - The CHANGELOG Unreleased section gained an entry naming this phase's work. No version string moved anywhere: `git diff .claude-plugin/plugin.json package.json` is empty.
    - `grep -rP '\x{2014}' .planning/REQUIREMENTS.md .planning/ROADMAP.md CHANGELOG.md` returns no match for the lines this task added. Pre-existing occurrences elsewhere in those files are out of scope; name any in the SUMMARY rather than mass-correcting.
    - `.planning/*` is gitignored, so the two planning files need `git add -f`. `git diff --cached --name-only` before the commit lists exactly the three files.
  </acceptance_criteria>

  <done>All fourteen IDs are registered with measured evidence, the ROADMAP states the measured counts and the real plan list, the dependency correction is confirmed, and the CHANGELOG carries an entry without any version moving.</done>
</task>

<task type="auto">
  <name>Task 2: register every named follow-up, dispose of the meeting RCA, and composite the dev-research trail</name>

  <files>.planning/ROADMAP.md, .planning/debug/meeting-file-meeting-false-success.md, .planning/debug/knowledge-base.md</files>

  <read_first>
    - `.planning/debug/meeting-file-meeting-false-success.md` IN FULL (199 lines). Its frontmatter (`status: partial-close`, `next_action`), its Current Focus section which explicitly forbids moving the file to `resolved/` until the Tri-Polar gap closes, and its Technical Root Cause section naming the real defect.
    - `276-12-SUMMARY.md` and `276-14-SUMMARY.md`, for exactly which of the three gaps closed and what remains.
    - `references/meeting/filing-protocol.md` lines 44-63 as rewritten by plan 276-14, which is the current, truthful gap enumeration and should agree with whatever this task writes.
    - `.planning/debug/knowledge-base.md`, for the summary-block format a resolved or narrowed RCA adds.
    - `CLAUDE.md` section "Dev-Research Compositing (Rethinking Room)" IN FULL. The rule: same finding, two homes, cross-linked. A phase artifact that only says "per the room's research" without the citation, or a room entry that reaches a verdict and never lands in a phase or seed, both count as incomplete.
    - `.planning/ROADMAP.md` Phase 271's `REGISTERED_FOLLOWUPS` treatment and Phase 267.3's "SCOPE, explicit (what this phase did NOT absorb)" section, the two shipped shapes for handing on deferred work with an owner.
    - `~/MindrianRooms/.rooms/registry.json` to check which room is active, and `scripts/write-scope-check`, because plan 267.3-08 could not land its compositing trail from a worktree-isolated context and the guard must not be bypassed. If the write is blocked, STAGE the file inside the phase directory and record the pending navigator action, exactly as 267.3-08 did.
  </read_first>

  <action>
    Follow-up registration. Add a section to the ROADMAP's Phase 276 entry titled with the Phase
    267.3 shape, naming what this phase did NOT absorb, each with a stated owner. At minimum:

      - **Route `lazygraph-ops.openGraph` through `openRoomDb`** (D-276-4's named follow-up). Would
        also close Phase 273's M12 and C2's residue. Deferred because `openRoomDb` is synchronous
        and returns a bare handle while `openGraph` is async and returns `{db, conn}` across roughly
        38 call sites, and because it runs a 7-step migration chain on every open. Owner: repo
        navigator.
      - **The Theo-side TS-AST checker port**, filed as `docs/2026-09-03-THEO-SEED-tool-honesty-
        ts-ast-port.md` by plan 276-13, to be captured as a SEED in Theo's own `.planning/seeds/`
        via that repo's `/gsd-capture`, before Theo plan `09-12` authorizes the flip. Owner: whoever
        works the Theo repo. Never executed from here (Theo D-04).
      - **The `gate_render` Theo mirror task**, the one description this phase changed on a
        Theo-absorbed tool, plus the two pre-existing divergences (`gate_answer`, `chain_run`) plan
        276-13 measured. Owner: same.
      - **The `STATE.md` resync-clobber bug** as a dedicated `/gsd-debug` session against
        `@opengsd/gsd-core` under `$HOME/.claude/gsd-core/`, not this repo. Sixty-plus documented
        occurrences. Out of repo scope by the same logic Theo's tools are. Owner: repo navigator.
      - **Hardening `check-tool-honesty` to `--strict`.** The ljj SUMMARY names this as available
        only after the list is empty. Whether the list is now empty enough is a navigator decision,
        not a planner one. Record the current bucket split from `276-15-SUMMARY.md` alongside it so
        the decision has its number. Owner: repo navigator.
      - Anything plans 276-09, 276-10 and 276-11 registered during execution: the
        `cross-room-store.cjs` fallback swallow if it survived, the `venture-shape-nudge.cjs`
        read-only-door question, the `getCurrentJTBD` and `getCurrentOperator` remainder if plan
        276-10 registered one, and the `enrichCausalEdges` zero-caller empty-versus-absent contract.
        Read each SUMMARY and carry over whatever it registered. Do not invent entries and do not
        drop any.
      - **The nine mega-tools with no connector descriptor and no `hitl_shape`** (RESEARCH constraint
        C-3, open question 5). Only `room_bind` has one today. Whether Canon Part 11 R16 covers MCP
        tools is OQ-3, still unresolved. Registered as a finding, not built. Owner: repo navigator.

    RCA disposition. Update `.planning/debug/meeting-file-meeting-false-success.md`. Rewrite its
    Current Focus to state what actually shipped: which of the three gaps closed, in which plan, and
    what remains. Update `next_action` and `updated`. Then decide the `status` from the evidence:
      - If the remaining gap is ONLY the structurally unreachable subagent fan-out, `resolved` is
        defensible, because the file's own condition was that the Tri-Polar gap close and a
        structurally unreachable capability is a declared limitation rather than an open defect.
        Say that reasoning out loud in the file.
      - If anything else remains open, keep `partial-close` and narrow the description to exactly
        what is left.
    If and only if the status becomes `resolved`, move the file to `.planning/debug/resolved/` and
    add a summary block to `.planning/debug/knowledge-base.md`, per the RCA standard.

    Dev-research compositing. Write the reasoning trail to
    `~/MindrianRooms/rethinking-mindrianos/research/2026-09-03-same-disease-consolidation/` and
    mirror it to `~/MindrianOS/research/`. Content: the one-disease-two-layers thesis; the D-1
    discovery and why a header comment claiming an unrun verification is the cautionary case for
    regex-over-AST; the propagation-gap framing carried from Phase 273's reviewer; the icm-architect
    promotion bar (the same false-success shape from five independent sources, well past the
    three-instance bar); the measured before-and-after; and the decisions D-276-1 through D-276-6
    with their rejected alternatives. Cross-link BOTH directions: the room entry cites this phase
    directory by path, and this plan adds a "Dev-Research Compositing" line to the ROADMAP Phase 276
    entry citing the room path. Verify both links resolve.

    If `scripts/write-scope-check` or the active-room registry blocks the room write, DO NOT bypass
    the guard. Stage the file at
    `.planning/phases/276-*/276-16-COMPOSITING-TRAIL-STAGED.md` and record the pending navigator
    action in the ROADMAP entry, naming the three steps (switch the active room, copy the staged
    body to the room path, mirror to `~/MindrianOS/research/`). That is exactly what plan 267.3-08
    did and it is the honest outcome, not a failure.
  </action>

  <verify>
    <automated>test -f .planning/debug/meeting-file-meeting-false-success.md -o -f .planning/debug/resolved/meeting-file-meeting-false-success.md && grep -c "lazygraph-ops" .planning/ROADMAP.md && grep -c "THEO-SEED" .planning/ROADMAP.md && grep -c "resync-clobber" .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - The ROADMAP Phase 276 entry contains a named-follow-up section with at least the seven items listed above, each with a stated owner. Count them and record the count in the plan SUMMARY.
    - Every follow-up any earlier plan registered during execution appears. Cross-check by grepping each `276-NN-SUMMARY.md` for the word "follow-up" and confirming each hit has a corresponding ROADMAP line. Record the cross-check result.
    - The RCA file's `status`, `next_action` and `updated` all changed, and its Current Focus names which gap closed in which plan. If it moved to `resolved/`, `.planning/debug/knowledge-base.md` gained a summary block; if it did not, the file states exactly what remains.
    - The RCA's status decision is justified in the file itself, not only in the plan SUMMARY.
    - The room entry exists at the named path and the mirror exists, OR the staged file exists inside the phase directory with the pending navigator action recorded in the ROADMAP. Verify with `test -d` on the room path and on the mirror path, and record which outcome occurred.
    - Both cross-links resolve: the room entry cites the phase directory by path, and the ROADMAP cites the room path. Verify each target exists with `test -e` and record both results.
    - `scripts/write-scope-check` was NOT bypassed. If it blocked, say so explicitly.
    - `grep -rP '\x{2014}'` over every file this task wrote returns no match.
    - `.planning/*` is gitignored, so `git add -f` is required. `git diff --cached --name-only` before the commit lists exactly the intended files.
  </acceptance_criteria>

  <done>Every deferral has a home and an owner, the RCA carries a status the code supports with its reasoning written in the file, and the research trail exists in both homes with both cross-links verified, or is staged with the pending action named.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Tri-Polar verification of meeting filing on a real Desktop or Cowork surface</name>

  <files>none (human verification only; evidence is pasted into the plan SUMMARY)</files>

  <read_first>
    - `276-12-SUMMARY.md` and `276-14-SUMMARY.md` IN FULL, for the exact tool name the claim write
      primitive registered under, the exact node type and property keys it writes, and the gate
      order plan 276-14 wired. The database read in step 6 below has to name real columns and real
      property keys, so adapt the query from those two SUMMARYs before handing the steps over.
    - `lib/mcp/tools/claim.cjs` as landed, for the same reason: the property names in the query must
      match what the tool actually writes, not what a template guessed.
    - `.planning/debug/meeting-file-meeting-false-success.md` Technical Root Cause section, so the
      navigator is handed the original failure shape alongside the verification steps and can
      recognise a recurrence rather than only a mismatch.
    - `~/MindrianRooms/.rooms/registry.json`, to name a scratch room that already exists rather than
      asking the navigator to create one, if a suitable one is present.
  </read_first>

  <what-built>
    Across this phase, Claude has already automated and verified everything a test on this machine
    can reach:

    - The detector's dead switch parser is fixed, taking the live sweep from 10 findings to 24, and
      every one of those findings now carries a written disposition in a checked-in ledger that a
      test diffs against the live scan in both directions. HIGH RISK is 0.
    - The `orchestration`, `export`, `room_content`, `gate_render` and `graph_write` descriptions no
      longer claim capabilities their handlers lack, and every false completion assertion
      ("Snapshot generated", "Dashboard generated", "Export complete", "Scout intelligence
      gathered") is gone.
    - The busy timeout reached every read-write opener that can genuinely contend, proven under a
      genuinely held foreign write lock with an elapsed-time floor rather than a grep count.
    - `spine-events.cjs` reports `room_db_busy` and `room_db_broken` instead of claiming there is no
      database about a database it just proved exists.
    - A new MCP claim write tool routes through the single node-write chokepoint, and meeting filing
      now reaches the governed `gate_render` and `gate_answer` path, with confirmation verified
      against `room.db` by an automated test rather than read from a tool response.

    What no test on this machine can reach: the Desktop and Cowork surfaces. There is no automation
    harness for either. The Tri-Polar rule makes a three-surface check a stated default, and this
    phase's entire subject is that a tool's own success claim is not evidence. On 2026-09-03 the
    first `file-meeting` call of that session reported success and wrote nothing; that discovery is
    why this phase exists.
  </what-built>

  <how-to-verify>
    1. Open a **Claude Desktop** session (or Cowork, whichever is available) with the MindrianOS MCP
       server connected. Note which surface you used.

    2. Bind to a scratch room, not a real one. If none exists, create one. Record its path, for
       example `~/MindrianRooms/scratch-276/`.

    3. BEFORE doing anything else, record the room database's modification time from a terminal:
       `stat -c '%y %s' ~/MindrianRooms/scratch-276/.mindrian/room.db`
       Write down the timestamp and the byte size.

    4. In the Desktop session, file a short meeting claim through the meeting surface. Use two or
       three sentences of throwaway content. Answer the decision gate when it appears.

    5. Read what the tool told you, and write it down verbatim. Do not act on it yet.

    6. Now check the truth, from a terminal, NOT from the session:
       - `stat -c '%y %s' ~/MindrianRooms/scratch-276/.mindrian/room.db` and compare against step 3.
         The timestamp must have moved.
       - Read the node directly:
         `node -e "const {DatabaseSync}=require('node:sqlite');const db=new DatabaseSync(process.env.HOME+'/MindrianRooms/scratch-276/.mindrian/room.db',{readOnly:true});console.log(db.prepare('SELECT id, type, json_extract(properties, \$.knowledge_type) AS kt, json_extract(properties, \$.status) AS st FROM nodes ORDER BY rowid DESC LIMIT 5').all())"`
         Adjust the column and property names to match what plan 276-12's SUMMARY says the tool
         writes. The point is to read the row, not to trust the response.

    7. Answer these five questions and paste the evidence:
       - Which surface did you use, Desktop or Cowork?
       - Did the room database's mtime move?
       - Does the claim node exist, and does its `knowledge_type` match what you filed?
       - Is it at `confirmed` (if you approved the gate) or at `proposed` (if you did not)?
       - Does what the tool TOLD you match what the database SHOWS? Quote both.

    8. If the tool's response and the database disagree in ANY direction, that is a live instance of
       the exact defect this phase exists to close, found on the one surface no test could reach.
       Say so plainly. Do not approve.
  </how-to-verify>

  <acceptance_criteria>
    - The surface used is named (Desktop or Cowork), not left implicit.
    - The room database mtime before and after is pasted, both values.
    - The direct database read output is pasted, showing the claim node or showing its absence.
    - The tool's own response text is quoted, and the agreement or disagreement with the database is stated explicitly.
    - If the surface could not be reached at all (no Desktop install, no Cowork access), that is recorded as a NOT VERIFIED outcome naming the reason, and the phase closes with that gap stated in the SUMMARY rather than with an assumed pass. A stated gap is an acceptable outcome; a silent one is not.
  </acceptance_criteria>

  <action>
    Pause and hand control to the navigator. Present the what-built summary and the numbered
    verification steps below, then wait. Do NOT proceed to write the phase SUMMARY until the
    navigator has replied with either pasted evidence or an explicit not-verified outcome. When the
    reply arrives, paste it verbatim into `276-16-SUMMARY.md`, including the mtime figures, the
    direct database read output and the quoted tool response, so the evidence lives in the phase
    record rather than only in the conversation.
  </action>

  <verify>
    <automated>node -e "const p=process.env.HOME+'/MindrianRooms';console.log('scratch room parent exists:',require('node:fs').existsSync(p));process.exit(0)"</automated>
  </verify>

  <done>The navigator has replied with pasted mtime figures, a direct database read and the quoted tool response, and the agreement or disagreement between them is stated. A not-verified reply naming why Desktop and Cowork were both unreachable is also a complete outcome, recorded as a stated gap.</done>

  <resume-signal>Type "approved" with the pasted evidence, or describe the disagreement you found. If Desktop and Cowork are both unreachable, type "not verified" and name why.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase paperwork -> a future reader | A registration block that marks everything done regardless is the phase's own disease in its own paperwork |
| deferred work -> the next phase | Phase 273's C4 and C5 were deferred and nearly orphaned; this phase must not repeat that with its own deferrals |
| Desktop and Cowork surfaces -> room.db | No automation harness exists; the only check is a human reading the database directly |
| room write guard -> compositing trail | `scripts/write-scope-check` must not be bypassed to land a document |
| shared working tree -> git index | A second live session commits into this same tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-276-02 | Repudiation | The requirements registration | mitigate | Every row must end with a `Measured:` clause naming a real command and its real output, and three rows are spot-checked against their owning SUMMARY. A requirement that did not fully land is marked unchecked with what remains and who owns it. |
| T-276-38 | Repudiation | Deferrals silently dropped | mitigate | Each earlier SUMMARY is grepped for registered follow-ups and each hit must have a corresponding ROADMAP line, with the cross-check result recorded. Seven named items are the floor, not the ceiling. |
| T-276-39 | Repudiation | An RCA status the code does not support | mitigate | The status decision is made from what shipped and its reasoning is written INTO the RCA file, not only into a SUMMARY. `resolved` is defensible only if the sole remaining gap is the structurally unreachable subagent fan-out; anything else keeps `partial-close` narrowed to what is left. |
| T-276-40 | Spoofing | A Desktop success claim without a write | mitigate | The checkpoint requires a mtime comparison and a direct `node:sqlite` read from a terminal, outside the session, plus a verbatim quote of what the tool said. The two are compared explicitly. This is the discipline that found the original defect. |
| T-276-41 | Elevation of privilege | Bypassing the room write guard | mitigate | If `scripts/write-scope-check` or the active-room registry blocks the compositing write, the file is STAGED inside the phase directory and the pending navigator action is recorded, exactly as plan 267.3-08 did. The guard is never bypassed. |
| T-276-42 | Tampering | An accidental release | mitigate | This plan does not run `scripts/release.sh`, does not bump any version, and asserts `git diff .claude-plugin/plugin.json package.json` is empty. The five-place lockstep is a release action, not a phase-close action. |
| T-276-09 | Tampering | git index in a shared working tree | mitigate | Two commits, each preceded by `git diff --cached --name-only`. `.planning/*` is gitignored so `git add -f` is required and its scope must be exactly the intended files. |
| T-276-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs across all sixteen plans of this phase. No legitimacy checkpoint is owed anywhere in Phase 276. |
</threat_model>

<verification>
- Every ID from TOOLHON-01 through TOOLHON-14 is present in `.planning/REQUIREMENTS.md`.
- `grep -c "TBD (run /gsd-plan-phase 276" .planning/ROADMAP.md` returns 0.
- `grep -c "Depends on:\*\* Phase 275" .planning/ROADMAP.md` returns 0.
- `git diff .claude-plugin/plugin.json package.json` is empty: no version moved.
- The named-follow-up section names at least seven items, each with an owner.
- The RCA file's status matches what shipped and its reasoning is in the file.
- The room entry and its mirror both exist, or the staged file exists with the pending action recorded.
- `bash tests/run-all-276.sh`, `bash tests/run-all-266.sh` and `bash tests/run-all-273.sh` all still exit 0 at phase close.
- `node scripts/check-tool-honesty.cjs --report | tail -3` shows zero HIGH RISK.
</verification>

<success_criteria>
- Fourteen requirement IDs registered with measured evidence.
- The ROADMAP states the measured counts, the real plan list, and every deferral with an owner.
- The meeting RCA carries a status the shipped code supports, with the reasoning in the file.
- The dev-research trail exists in both homes with verified cross-links, or is staged with the pending action named and the write guard unbypassed.
- A human has verified meeting filing against `room.db` on Desktop or Cowork, or the gap is stated rather than assumed away.
- No version moved and no release was cut.
</success_criteria>

<output>
Create `.planning/phases/276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs/276-16-SUMMARY.md` when done.
</output>
