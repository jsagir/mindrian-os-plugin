---
phase: 276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs
plan: 04
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test-276-tool-honesty-findings-closed.cjs
  - tests/test-276-allowed-unverified-contract.cjs
  - tests/test-276-theo-description-parity.cjs
autonomous: true
requirements: [TOOLHON-02, TOOLHON-06, TOOLHON-12]
canon_parts: [7, 8]

must_haves:
  truths:
    - "No finding can be closed by silence: a live scan row absent from the disposition ledger fails the suite"
    - "No ledger entry can rot: an entry whose expected final verdict no longer matches the live scan fails as stale"
    - "An ALLOWED_UNVERIFIED entry without a real reason, a triage date and a live matching row fails a test rather than passing on a comment"
    - "MEDIUM and UNKNOWN are declared never-suppressible, and the test asserts the suppression path still refuses them"
    - "The Theo parity check reports a byte diff per constant and SKIPS loudly when the Theo checkout is absent, never blocking a plugin commit"
  artifacts:
    - path: "tests/test-276-tool-honesty-findings-closed.cjs"
      provides: "TOOLHON-02 ledger diff in both directions against live scanAll output"
      min_lines: 90
      contains: "276-dispositions.json"
    - path: "tests/test-276-allowed-unverified-contract.cjs"
      provides: "TOOLHON-06 structural enforcement of the allowlist entry contract"
      min_lines: 60
      contains: "ALLOWED_UNVERIFIED"
    - path: "tests/test-276-theo-description-parity.cjs"
      provides: "TOOLHON-12 five-constant parity report, skip-when-absent, non-blocking"
      min_lines: 80
      contains: "83a1ce2"
  key_links:
    - from: "tests/test-276-tool-honesty-findings-closed.cjs"
      to: "tests/fixtures/tool-honesty/276-dispositions.json"
      via: "reads the checked-in ledger and diffs it against live scanAll rows in both directions"
      pattern: "276-dispositions"
    - from: "tests/test-276-theo-description-parity.cjs"
      to: "/home/jsagi/Theo/src/mcp/operational"
      via: "read-only extraction of the five absorbed-tool description constants"
      pattern: "operational"
---

<objective>
Author the Wave 0 tests that make "closed" mean something checkable, that make the suppression
path enforceable rather than commented, and that give the Theo coordination surface a report
instead of a guess.

D-276-2 is the binding definition: "close all findings" means every one of the 24 gets a written
disposition (real fix, detector fix, or description correction), each verified by re-running the
checker, never by assertion. MEDIUM and UNKNOWN cannot be suppressed today
(`scripts/check-tool-honesty.cjs:1162` gates `ALLOWED_UNVERIFIED` on HIGH_RISK only) and this
phase adds NO new suppression path. A proven false positive is a detector fix, not an allowlist
entry.

Today nothing enforces any of that. `ALLOWED_UNVERIFIED` ships empty with a prose rule and zero
tests: no assertion that an entry has a non-empty reason, that it names a tool and command that
actually exist, or that it is dated. A stale entry never expires, so a fixed tool keeps a
suppression that silently re-fires if the tool regresses. The sibling `check-substrate.cjs`
`ALLOWED_DIRECT_IMPORT` (lines 53-140) copied the same discipline as STRUCTURE, with a
justification comment per entry and a documented membership rule; `check-tool-honesty.cjs` copied
it as prose only.

TOOLHON-12 is a coordination signal, not a gate. It can never block a plugin commit, because the
plugin cannot fix Theo's file from this repo (Theo D-04: coordinated, not executed cross-repo).
`gate_answer` (1462 bytes on the plugin side against 1152 on Theo's) and `chain_run` (1113 against
1006) have ALREADY diverged and nobody noticed, which proves the drift channel is live rather
than theoretical.

Purpose: make the closure claim, the suppression contract and the Theo drift all machine-visible.
Output: three test files.

**WAVE 0 IS RED BY DESIGN** for the ledger test, which cannot pass until plan 276-06 freezes the
ledger. The allowlist-contract test and the Theo parity test should PASS or SKIP from the start;
if either is red, that is a real finding, not a design intent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs/276-PATTERNS.md
@.planning/phases/276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs/276-VALIDATION.md

Do NOT read `.planning/STATE.md` in full (1.4MB).
Do NOT read `276-RESEARCH.md` in full (1300 lines). Read only the sections named per task.
</context>

<artifacts_this_phase_produces>

This plan (276-04) produces:

| Artifact | Kind | Path |
|---|---|---|
| `tests/test-276-tool-honesty-findings-closed.cjs` | new test file (integration, ledger diff) | `tests/` |
| `tests/test-276-allowed-unverified-contract.cjs` | new test file (unit, config contract) | `tests/` |
| `tests/test-276-theo-description-parity.cjs` | new test file (cross-repo, skip-when-absent) | `tests/` |

This plan READS but does not create `tests/fixtures/tool-honesty/276-dispositions.json`. That
ledger is minted by plan 276-06 after the D-1 GREEN fix, and its schema is defined in
`276-01-PLAN.md` under `artifacts_this_phase_produces`. Treat the ledger and every `reason` string
value listed there as newly created by this phase.

</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: tests/test-276-tool-honesty-findings-closed.cjs, the two-directional ledger diff</name>

  <files>tests/test-276-tool-honesty-findings-closed.cjs</files>

  <read_first>
    - `276-01-PLAN.md` section `artifacts_this_phase_produces`, the disposition ledger schema block. That schema is the contract this test enforces; do not invent a different one.
    - `scripts/check-tool-honesty.cjs` lines 1077-1189 (`scanAll` and `checkTree`, neither of which ever calls `process.exit`, so both are safe inside a test) and its `module.exports` at 1252-1269.
    - `tests/test-ljj-tool-honesty.cjs` lines 37-75 (the harness scaffold) and lines 225-237 (the comment-stripping frozen-count guard: strip comments before grepping so a comment naming a number does not self-invalidate the gate).
    - `tests/test-273-substrate-baseline-honest.cjs` IN FULL. It is the shipped doc-versus-measured analog and the closest precedent for a checked-in artifact diffed against a live measurement.
    - `276-RESEARCH.md` section `## The Live Sweep` (lines 105-148) for the current 10 findings and the 14 the detector cannot yet see, and the note that `room_content.help` moves from one OK reason to a different OK reason after the fix, which proves the pre-fix reason strings were artifacts of whole-handler classification.
  </read_first>

  <action>
    Create `tests/test-276-tool-honesty-findings-closed.cjs`.

    Load the ledger from `tests/fixtures/tool-honesty/276-dispositions.json`. If the file does not
    exist, FAIL with an explicit message naming plan 276-06 as its owner. Do not skip; an absent
    ledger is the undispositioned state this test exists to forbid.

    Call `scanAll()` over the default scan set (no `files` override) to get the live rows.

    Assertion group A, no undispositioned finding. For every live row whose verdict is not `OK`,
    assert a ledger entry exists keyed on the `tool` plus `command` pair. On failure, print the
    row's tool, command, verdict and reason so a genuinely new finding is immediately readable.
    Label this as the direction that makes closure by silence impossible.

    Assertion group B, no stale ledger entry. For every ledger entry:
      - assert `disposition` is one of the closed vocabulary `real-bug-fix`, `detector-fix`,
        `description-correction`, `documented-no-action`;
      - assert `reason` is a string of at least 40 characters and contains a `file:line` shaped
        citation (a colon followed by digits), matching the discipline that a disposition recorded
        without a file:line citation is the warning sign named in Pitfall 3;
      - assert `owner_plan` matches the pattern `276-` followed by two digits;
      - assert `expected_final_verdict` is one of `OK`, `MEDIUM`, `UNKNOWN`.

    Assertion group C, the progress meter. For every ledger entry whose `expected_final_verdict`
    is `OK`, assert the corresponding live row is absent from the non-OK set. This assertion is
    RED between plan 276-06 and the end of wave 2 by design, and the test must SAY SO in its
    header: the count of still-open entries is this phase's own progress meter. Print that count
    on every run as an INFORMATION line.

    Assertion group D, honest non-OK entries. For every ledger entry whose
    `expected_final_verdict` is NOT `OK` (a MEDIUM or UNKNOWN that stays permanently visible
    because MEDIUM and UNKNOWN are never suppressible under D-276-2), assert the live row still
    exists AND still carries exactly that verdict. An entry that no longer matches is stale and
    must fail, so the ledger cannot rot.

    Assertion group E, no frozen totals. Assert the test's OWN source, with comments stripped,
    contains no hard-coded expected total for the number of findings. Neither `10` nor `24` may
    appear as a compared literal. The counts come from the live scan and from the ledger's own
    length. State in the label that a frozen count is exactly how a gate silently stops matching
    reality.

    Assertion group F, the ledger's declared sweep versus the live sweep. Compare the ledger's
    `frozen_sweep.tools` and `frozen_sweep.branches` against the live `scanAll` totals. If they
    differ, print both as an INFORMATION line and FAIL with a message saying the scan surface
    itself changed since the freeze, so the ledger needs re-freezing rather than silent tolerance.
  </action>

  <verify>
    <automated>node tests/test-276-tool-honesty-findings-closed.cjs; test $? -eq 1</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-276-tool-honesty-findings-closed.cjs` exits 1 at this plan's end and its first failure line names `tests/fixtures/tool-honesty/276-dispositions.json` as absent and plan `276-06` as its owner.
    - The test never calls `process.exit` on the checker's behalf; it uses `scanAll` directly. `grep -c "spawnSync\|execSync" tests/test-276-tool-honesty-findings-closed.cjs` returns 0 for the classification path (a shell-out for the `meeting` regression guard is not present in this file).
    - Group E holds against the test's own source: with comments stripped, `grep -cE "(^|[^0-9])(10|24)([^0-9]|$)" ` over the stripped source finds no comparison literal. Verify by running the test's own group E assertion, which must pass in the RED run.
    - `grep -c "276-dispositions.json" tests/test-276-tool-honesty-findings-closed.cjs` is at least 1.
    - `grep -P '\x{2014}' tests/test-276-tool-honesty-findings-closed.cjs` returns no match.
  </acceptance_criteria>

  <done>The ledger test exists, enforces the diff in both directions, forbids a frozen total, prints the still-open count as this phase's progress meter, and fails today because the ledger does not exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test-276-allowed-unverified-contract.cjs, the suppression contract made structural</name>

  <files>tests/test-276-allowed-unverified-contract.cjs</files>

  <read_first>
    - `scripts/check-tool-honesty.cjs` lines 76-82 (the `ALLOWED_UNVERIFIED` declaration and its prose "never pre-populate" rule), lines 1155-1175 (the consumption site, where line 1162 skips every verdict that is not `HIGH_RISK` and the suppression rewrites the row to `OK` with a reason prefix), and the `module.exports` at 1252-1269 which exports `ALLOWED_UNVERIFIED`.
    - `scripts/check-substrate.cjs` lines 53-140, `ALLOWED_DIRECT_IMPORT`. This is the sibling that implemented the same discipline as structure rather than prose: regex entries with a multi-line justification comment each, plus a documented membership rule. Copy its discipline, not its shape.
    - `276-RESEARCH.md` section `## ALLOWED_UNVERIFIED Mechanism` (lines 325-354), especially the five mechanical facts and the recommendation for TOOLHON-06.
    - `276-VALIDATION.md` row for TOOLHON-06, which binds the MEDIUM and UNKNOWN answer to D-276-2.
  </read_first>

  <action>
    Create `tests/test-276-allowed-unverified-contract.cjs`.

    Require the checker and read its exported `ALLOWED_UNVERIFIED` array.

    Assertion group A, per-entry contract. For every entry, assert it has:
      - a `tool` string and a `command` string that together resolve to a row in the live
        `scanAll()` output. A stale entry naming a tool or command that no longer exists must turn
        the suite RED rather than rot silently.
      - a `reason` string of at least 60 characters.
      - a `triaged` field parseable as an ISO date, and not in the future.
    If the array is empty (which it is today), assert that fact explicitly as an ok line reading
    that the allowlist ships empty, so the run is informative rather than vacuously green.

    Assertion group B, the never-suppressible tiers. This is D-276-2 expressed as a test. Assert
    that the suppression path still refuses MEDIUM and UNKNOWN, by constructing a synthetic row
    set and running it through the same filter shape the script uses, or by asserting on the
    script source that the guard at the consumption site still compares against `HIGH_RISK` only.
    Prefer the behavioral form. Label it: MEDIUM and UNKNOWN are never suppressible by design
    (D-276-2), and this phase adds no new suppression path. Attempting to allowlist a MEDIUM is
    mechanically a no-op that LOOKS like it worked, which is itself a false success.

    Assertion group C, no widening. Assert the checker source contains no second suppression
    mechanism: search for any other array or Set whose consumption rewrites a verdict to `OK`.
    Express this as an assertion that the literal `verdict = 'OK'` (or the equivalent assignment
    form used at the consumption site) appears exactly once in the non-comment source. Strip
    comments first.

    Assertion group D, the entry-shape documentation. Assert the checker's header or the
    declaration comment states the required entry fields (`tool`, `command`, `reason`, `triaged`)
    so a future contributor reads the contract at the declaration site rather than inferring it
    from the consumption site, which is how this research had to derive it.
  </action>

  <verify>
    <automated>node tests/test-276-allowed-unverified-contract.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-276-allowed-unverified-contract.cjs` exits 0 for groups A, B and C against the current tree (the array is empty and the HIGH_RISK-only guard is intact), and exits 1 only on group D, which requires the declaration-site documentation plan 276-06 adds.
    - The run prints an explicit ok line stating the allowlist ships empty, rather than passing group A vacuously.
    - `grep -c "HIGH_RISK" tests/test-276-allowed-unverified-contract.cjs` is at least 1.
    - `grep -c "D-276-2" tests/test-276-allowed-unverified-contract.cjs` is at least 1 (the ruling is cited in the file that enforces it).
    - `grep -P '\x{2014}' tests/test-276-allowed-unverified-contract.cjs` returns no match.
  </acceptance_criteria>

  <done>The suppression contract is enforced by a test rather than a comment, the never-suppressible ruling is encoded, and no second suppression mechanism can be added without failing group C.</done>
</task>

<task type="auto">
  <name>Task 3: tests/test-276-theo-description-parity.cjs, a non-blocking cross-repo coordination signal</name>

  <files>tests/test-276-theo-description-parity.cjs</files>

  <read_first>
    - `276-RESEARCH.md` section `### B1a: the five description constants, diffed and measured` (lines 1141-1160). It carries the measured table (three IDENTICAL, two ALREADY DIVERGED), the `gate_answer` first-divergence offset of 585, the confirming zero-count grep for `SOURCED_FROM` in Theo's `gate-answer.ts`, and the extraction caveat: the constants are multi-line string concatenations with escaped quotes, and a regex extractor reports green over a real divergence. That is `Theo 05-REVIEW CR-01`'s exact failure mode and this test must not reproduce it.
    - `/home/jsagi/Theo/src/mcp/operational/gate-render.ts` lines 85-95, READ-ONLY, for the shape of a `*_DESCRIPTION` constant. Do not write anything under `/home/jsagi/Theo` from this repo, ever (Theo D-04).
    - `lib/mcp/tools/gate.cjs` line 113 (`gate_render`) and line 170 (`gate_answer`), `lib/mcp/tool-router.cjs` line 1696 (`room_bind`), `lib/mcp/tools/graph.cjs` line 219 (`graph_write`), and `lib/mcp/tools/chain.cjs` (`chain_run`), the five plugin-side registration strings.
    - `tests/run-all-273.sh`'s missing-target accounting arm, where a missing sweep target FAILS rather than silently passing. This test INVERTS that discipline deliberately: an absent Theo checkout must SKIP loudly, because Theo is out of repo and CI has no copy. State the inversion and its reason in the file header.
  </read_first>

  <action>
    Create `tests/test-276-theo-description-parity.cjs`.

    Resolve the Theo checkout from `process.env.THEO_ROOT` with a default of `/home/jsagi/Theo`.
    If the path does not exist, print a loud SKIP block naming the env var and exit 0. Never fail
    on absence. State in the header that this is a coordination signal and never a gate, citing
    Theo D-04 (coordinated, not executed cross-repo), and that it must never block a plugin
    commit.

    Re-pin the Theo commit at run time: read the Theo checkout's current HEAD (via
    `git -C <theoRoot> rev-parse --short HEAD`) and compare it against the pinned value `83a1ce2`
    recorded in `276-RESEARCH.md`. If they differ, print an INFORMATION line naming both, and
    continue. Theo is actively developed; a moved pin is expected news, not a failure. Assumption
    A14 says exactly this.

    Extraction, and this is the load-bearing design choice. Do NOT extract the five constants with
    a bare regex over the TypeScript source. Prefer, in order:
      1. If a compiled JS build exists under the Theo checkout (for example a `dist/` output),
         require or read the compiled module and take the constant value directly.
      2. Otherwise, extract by locating the `const NAME =` assignment and then consuming the
         string-concatenation expression with a quote-and-escape aware forward scanner, reusing
         the plugin's own shipped primitive: `scripts/check-tool-honesty.cjs` exports
         `extractStringLiteralConcat`, which already handles this exact shape. Require it and use
         it. Do not hand-roll a second extractor.
    If neither path can produce a value for a constant, report that constant as `EXTRACTION_FAILED`
    with the reason, and treat it as a REPORTED problem rather than a silent IDENTICAL. A parity
    test built on a lossy extractor reporting green over a real divergence is the failure this
    file exists to avoid.

    Comparison and report. For each of the five constants (`ROOM_BIND_DESCRIPTION`,
    `GRAPH_WRITE_DESCRIPTION`, `GATE_RENDER_DESCRIPTION`, `GATE_ANSWER_DESCRIPTION`,
    `CHAIN_RUN_DESCRIPTION`), print one line reading `IDENTICAL` or
    `DIFFERS at offset N (plugin M bytes / theo K bytes)` or `EXTRACTION_FAILED: reason`. Compute
    the first divergence offset by character comparison.

    Exit posture. Exit 0 always, EXCEPT when the flag `--strict` is passed, in which case exit
    non-zero if any constant reports `DIFFERS` or `EXTRACTION_FAILED`. Nothing wires `--strict` by
    default. Mirror the `check-tool-honesty --check` advisory posture (`scripts/release.sh:360`
    calls it with `|| true`; the pre-commit hook deliberately omits a failure tail).

    Register nothing. Do not add this file to `scripts/verify-release`, `scripts/doctor.cjs` or any
    pre-commit hook. `tests/run-all-276.sh` discovers it by glob, and its exit-0-by-default posture
    means it contributes a report line without ever turning the aggregator red.
  </action>

  <verify>
    <automated>node tests/test-276-theo-description-parity.cjs && THEO_ROOT=/nonexistent-theo node tests/test-276-theo-description-parity.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-276-theo-description-parity.cjs` exits 0 and prints five report lines, one per constant.
    - Against the current checkouts the report reads IDENTICAL for `room_bind`, `graph_write` and `gate_render`, and DIFFERS for `gate_answer` and `chain_run`. The `gate_answer` line names a first divergence offset; record the measured offset in the plan SUMMARY and compare it against the research figure of 585, reporting any difference rather than assuming.
    - `THEO_ROOT=/nonexistent-theo node tests/test-276-theo-description-parity.cjs` exits 0 and prints a SKIP block naming `THEO_ROOT`.
    - `node tests/test-276-theo-description-parity.cjs --strict` exits non-zero today (because two constants differ), proving the strict arm works without anything wiring it.
    - `grep -c "extractStringLiteralConcat" tests/test-276-theo-description-parity.cjs` is at least 1 (the shipped extractor is reused, not hand-rolled).
    - `grep -c "83a1ce2" tests/test-276-theo-description-parity.cjs` is at least 1.
    - The test writes nothing under `/home/jsagi/Theo`: `grep -cE "writeFileSync|mkdirSync|appendFileSync" tests/test-276-theo-description-parity.cjs` returns 0.
    - `grep -P '\x{2014}' tests/test-276-theo-description-parity.cjs` returns no match.
    - All three test files committed in one commit whose message begins `test(276-04):`. `git diff --cached --name-only` immediately before the commit lists exactly those three files.
  </acceptance_criteria>

  <done>The Theo parity signal exists, extracts through the shipped concat-aware primitive rather than a lossy regex, reports per constant, skips loudly when Theo is absent, and cannot block a plugin commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in ledger -> live scan | The ledger is a claim about the world; the test is what makes the claim checkable in both directions |
| plugin repo -> Theo checkout | A separate repo, read-only from here. Writing to it from this repo would breach Theo D-04 |
| suppression path -> verdict table | An allowlist entry rewrites a HIGH_RISK row to OK, which is the one mechanism in the whole checker that can hide a real defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-276-02 | Repudiation | `ALLOWED_UNVERIFIED` and the disposition ledger | mitigate | Task 2 asserts every entry has a reason of at least 60 characters, a parseable triage date, and a tool plus command pair that resolves to a live row, so a stale entry turns the suite red instead of rotting. Task 1 group A fails on any live non-OK row absent from the ledger, so closure by silence is impossible. |
| T-276-13 | Tampering | A second suppression mechanism | mitigate | Task 2 group C asserts the verdict-rewrite assignment appears exactly once in the non-comment checker source, so a future second allowlist cannot be added quietly. |
| T-276-06 | Information disclosure | Theo description constants | accept | The parity test reads Theo source read-only and prints only byte lengths and offsets, never the divergent text, so a plugin-internal string cannot leak into a report that travels to the other repo. The reciprocal risk (Theo description text carrying an unredacted plugin string, Theo T-05-04) is out of this repo's scope and is cited only, never acted on from here. |
| T-276-14 | Denial of service | Theo parity as a gate | mitigate | Exit 0 always except under an explicit `--strict` flag that nothing wires. The test is registered nowhere in `verify-release`, `doctor.cjs` or any pre-commit hook. |
| T-276-09 | Tampering | git index in a shared working tree | mitigate | `git diff --cached --name-only` before the single commit, abort on any unexpected path. |
| T-276-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan and in this phase. No legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `node tests/test-276-tool-honesty-findings-closed.cjs` exits 1, naming the absent ledger and plan 276-06 (RED by design).
- `node tests/test-276-allowed-unverified-contract.cjs` exits 1 only on the declaration-site documentation assertion; groups A, B and C pass.
- `node tests/test-276-theo-description-parity.cjs` exits 0 and prints five report lines.
- `THEO_ROOT=/nonexistent-theo node tests/test-276-theo-description-parity.cjs` exits 0 with a SKIP block.
- `node tests/test-276-theo-description-parity.cjs --strict` exits non-zero.
- `git -C /home/jsagi/Theo status --porcelain` is unchanged before and after the run (nothing was written to Theo).
- `grep -rP '\x{2014}' tests/test-276-tool-honesty-findings-closed.cjs tests/test-276-allowed-unverified-contract.cjs tests/test-276-theo-description-parity.cjs` returns no match.
</verification>

<success_criteria>
- Three test files exist, committed in one commit.
- Closure is machine-checkable in both directions; a new finding and a stale ledger entry each fail the suite.
- The suppression contract is structural; MEDIUM and UNKNOWN are pinned as never-suppressible.
- The Theo drift is reported per constant with a first-divergence offset, and the measured `gate_answer` offset is recorded in the SUMMARY.
- Nothing under `/home/jsagi/Theo` is modified.
</success_criteria>

<output>
Create `.planning/phases/276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs/276-04-SUMMARY.md` when done.
</output>
