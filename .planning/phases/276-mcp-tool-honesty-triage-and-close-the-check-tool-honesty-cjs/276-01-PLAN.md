---
phase: 276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/run-all-276.sh
  - tests/fixtures/tool-honesty/switch-dispatch.cjs
  - tests/test-276-tool-honesty-switch-branches.cjs
autonomous: true
requirements: [TOOLHON-01, TOOLHON-05]
canon_parts: [7, 8]

must_haves:
  truths:
    - "A phase-276 test aggregator exists that discovers every tests/test-276-* file by glob and hard-fails on zero discovery"
    - "A synthetic switch (command) fixture exists that the checker can scan with its own four-positional-argument server.tool( reader"
    - "A test exists that proves splitBranches yields per-command branches, and it is observed FAILING against the pre-fix splitter"
    - "The RED commit contains only the test file and its fixture, nothing else"
  artifacts:
    - path: "tests/run-all-276.sh"
      provides: "Glob-discovery aggregator with a found-eq-0 guard, a Part 8 source sweep and a no-em-dash fence"
      min_lines: 90
      contains: "TEST_276_PREFIX"
    - path: "tests/fixtures/tool-honesty/switch-dispatch.cjs"
      provides: "Synthetic MCP tool using switch (command) with one writing case and one echo case"
      min_lines: 30
      contains: "switch (command)"
    - path: "tests/test-276-tool-honesty-switch-branches.cjs"
      provides: "The RED proof for TOOLHON-01 plus the TOOLHON-05 boundary-enumeration assertion"
      min_lines: 80
      contains: "splitBranches"
  key_links:
    - from: "tests/test-276-tool-honesty-switch-branches.cjs"
      to: "scripts/check-tool-honesty.cjs"
      via: "require of the checker's exported splitBranches and scanAll"
      pattern: "check-tool-honesty"
---

<objective>
Build the Wave 0 test infrastructure for the detector half of this phase, and author the RED
proof that `scripts/check-tool-honesty.cjs`'s `switch (command)` branch splitter is dead code.

This phase is the fast-follow Phase 273's own `273-CONTEXT.md` D-02 registered ("registered as a
fast-follow phase, not silently dropped"). It consolidates one disease across two layers, and
its highest-value single item is detector bug D-1: `splitBranches` has never split a single
branch, so 33 branches across `room_state` / `room_content` / `room_graph` are graded against
their tool's whole handler body. Fixing it takes the live sweep from 10 findings to 24,
including 4 hand-verified HIGH RISK defects in `room_content`.

The sequencing rule is binding: the RED test lands FIRST, in its own commit, observed failing,
and only then does the one-line GREEN fix land in plan 276-06. That is the `209b604f` (RED) /
`75278850` (GREEN) precedent under which this exact script was originally built. It matters
here beyond ceremony: D-1 exists precisely because a header comment at
`scripts/check-tool-honesty.cjs:559-565` claimed a verification that had never run.

Purpose: give every later plan in this phase a discovery-based aggregator it never has to edit,
and pin the detector defect with an executable proof before anyone touches a description string.
Output: the aggregator, the synthetic switch fixture, and the RED test.

**WAVE 0 IS RED BY DESIGN.** `node tests/test-276-tool-honesty-switch-branches.cjs` exiting 1 at
the end of this plan is the CORRECT state, not a defect.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs/276-PATTERNS.md
@.planning/phases/276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs/276-VALIDATION.md

Do NOT read `.planning/STATE.md` in full. It is 1.4MB and will consume the whole context window.
Grep it for a specific literal if you need it.

Do NOT read `276-RESEARCH.md` in full (1300 lines). Read only the sections named in each task's
`read_first`.
</context>

<artifacts_this_phase_produces>

This plan (276-01) produces:

| Artifact | Kind | Path |
|---|---|---|
| `tests/run-all-276.sh` | new test aggregator (glob discovery) | `tests/` |
| `tests/fixtures/tool-honesty/switch-dispatch.cjs` | new test fixture (synthetic MCP tool) | `tests/fixtures/tool-honesty/` |
| `tests/test-276-tool-honesty-switch-branches.cjs` | new test file (RED-first) | `tests/` |
| `TEST_276_PREFIX` | new environment-variable override in the aggregator, used to prove the found-eq-0 guard | `tests/run-all-276.sh` |

Symbols produced by LATER plans in this phase, listed once so the whole phase surface is visible
in one place. The plan-review source-grounding pass must treat every row below as newly created
by this phase, not as drift against an existing symbol:

| Symbol | Kind | Home | Plan |
|---|---|---|---|
| `tests/test-276-busy-timeout-propagation.cjs` | new test file | `tests/` | 276-02 |
| `tests/test-276-spine-events-typed-reason.cjs` | new test file | `tests/` | 276-02 |
| `tests/test-276-orchestration-scout-honesty.cjs` | new test file | `tests/` | 276-03 |
| `tests/test-276-room-content-honesty.cjs` | new test file | `tests/` | 276-03 |
| `tests/test-276-tool-honesty-findings-closed.cjs` | new test file | `tests/` | 276-04 |
| `tests/test-276-allowed-unverified-contract.cjs` | new test file | `tests/` | 276-04 |
| `tests/test-276-theo-description-parity.cjs` | new test file (cross-repo, skip-when-absent) | `tests/` | 276-04 |
| `276-DECISIONS.md` | new navigator decisions record | `.planning/phases/276-*/` | 276-05 |
| `tests/fixtures/tool-honesty/276-dispositions.json` | new disposition ledger (schema below) | `tests/fixtures/tool-honesty/` | 276-06 |
| `room_db_busy` | new `reason` string value returned by `spine-events.cjs` | `lib/core/navigation/spine-events.cjs` | 276-10 |
| `room_db_broken` | new `reason` string value returned by `spine-events.cjs` | `lib/core/navigation/spine-events.cjs` | 276-10 |
| `room_db_open_failed` | new `reason` string value for the unclassifiable open failure | `lib/core/navigation/spine-events.cjs` | 276-10 |
| `claim_write` | new MCP tool name | `lib/mcp/tools/claim.cjs` | 276-12 |
| `lib/mcp/tools/claim.cjs` | new MCP tool module (auto-discovered by `register-core-tools.cjs`) | `lib/mcp/tools/` | 276-12 |
| `tests/test-276-claim-write-primitive.cjs` | new test file | `tests/` | 276-12 |
| `docs/2026-09-03-THEO-SEED-tool-honesty-ts-ast-port.md` | new coordination document | `docs/` | 276-13 |
| `tests/test-276-meeting-gate-wiring.cjs` | new test file | `tests/` | 276-14 |

**Disposition ledger schema** (created by 276-06, read by `tests/test-276-tool-honesty-findings-closed.cjs`):

```
{
  "schema_version": 1,
  "frozen_at_commit": "<sha of the D-1 GREEN commit>",
  "frozen_sweep": { "tools": N, "branches": N, "high_risk": N, "medium": N, "low": N, "unknown": N, "ok": N },
  "dispositions": [
    { "tool": "<tool name>", "command": "<branch or (default)>", "finding_id": "F-nn",
      "verdict_at_freeze": "HIGH_RISK|MEDIUM|LOW|UNKNOWN",
      "disposition": "real-bug-fix|detector-fix|description-correction|documented-no-action",
      "expected_final_verdict": "OK|MEDIUM|UNKNOWN",
      "owner_plan": "276-NN",
      "reason": "<at least 40 characters, citing file:line>" }
  ]
}
```

</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: tests/run-all-276.sh, the glob-discovery aggregator with a provable found-eq-0 guard</name>

  <files>tests/run-all-276.sh</files>

  <read_first>
    - `tests/run-all-274.sh` IN FULL. Its runner core at lines 46-58 (`set -uo pipefail`, `ROOT=`, the `run()` function) and its tail at lines 79-83 (`PASS=$PASS FAIL=$FAIL` then `[ "$FAIL" -eq 0 ]`) are copied verbatim. Its per-arm intent header at lines 5-20 (one sentence per plan ID) is the shape to reproduce.
    - `tests/run-all-273.sh` header block. It carries the four mechanisms this aggregator needs: glob discovery, the found-eq-0 guard provable via a `TEST_273_PREFIX` override, the Part 8 source sweep, and the no-em-dash fence with a missing-target accounting arm.
    - `276-PATTERNS.md` section "### `tests/run-all-276.sh` (aggregator, batch)" for the exact code blocks to lift.
  </read_first>

  <action>
    Create `tests/run-all-276.sh`, executable (`chmod +x`), modeled on `tests/run-all-274.sh`'s
    runner core and `tests/run-all-273.sh`'s four mechanisms.

    Header block, one sentence per requirement, naming TOOLHON-01 through TOOLHON-14 and the plan
    that owns each. Include verbatim the sentence "WAVE 0 IS RED BY DESIGN" and state that a red
    run at the end of Wave 0 is the correct state, not a defect, naming
    `tests/test-276-tool-honesty-switch-branches.cjs` as the specific arm that must be red until
    plan 276-06 lands the GREEN fix.

    Mechanism 1, glob discovery: discover every `tests/test-276-*` file (both `.cjs` and `.sh`)
    and run each through the `run()` helper. Adding a `tests/test-276-*` file must require no edit
    to this runner.

    Mechanism 2, the found-eq-0 guard: read the discovery prefix from `${TEST_276_PREFIX:-test-276-}`
    so an operator can prove the guard by pointing it at a nonexistent prefix. If the discovered
    file count is 0, print a FAILED line naming the prefix and exit non-zero. Do NOT print green
    on zero discovery. State in a comment that a harness reporting green over zero discovery is
    itself the false-success disease this phase exists to close.

    Mechanism 3, Part 8 source sweep: over every discovered test file plus this phase's production
    targets (`scripts/check-tool-honesty.cjs`, `lib/mcp/tool-router.cjs`, `lib/mcp/tools/gate.cjs`,
    `lib/mcp/tools/graph.cjs`, `lib/core/navigation/spine-events.cjs`, `lib/core/lazygraph-ops.cjs`),
    assert no `brain_` call literal appears. Count a missing sweep target as a FAILURE, not a skip.

    Mechanism 4, no-em-dash fence: `grep -P '\x{2014}'` over the same target list, with the same
    missing-target accounting. Never write a literal em-dash glyph into this file; the fence is
    expressed as the escape `\x{2014}`.

    Tail: print `Phase 276: PASS=$PASS FAIL=$FAIL` between two separator lines and exit
    `[ "$FAIL" -eq 0 ]`.
  </action>

  <verify>
    <automated>bash -n tests/run-all-276.sh && TEST_276_PREFIX=test-276-nonexistent-prefix- bash tests/run-all-276.sh; test $? -ne 0</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n tests/run-all-276.sh` exits 0 (no syntax error).
    - `TEST_276_PREFIX=test-276-nonexistent-prefix- bash tests/run-all-276.sh` exits non-zero and its output contains the discovered-count-zero failure line naming the prefix.
    - `grep -c "TEST_276_PREFIX" tests/run-all-276.sh` is at least 2.
    - `grep -c "WAVE 0 IS RED BY DESIGN" tests/run-all-276.sh` equals 1.
    - `grep -P '\x{2014}' tests/run-all-276.sh` returns no match (exit 1).
    - `test -x tests/run-all-276.sh` succeeds.
    - Committed alone. `git diff --cached --name-only` immediately before the commit lists exactly `tests/run-all-276.sh` and nothing else. Abort the commit on any other path (the working tree is shared with a second live session).
  </acceptance_criteria>

  <done>The aggregator exists, is executable, passes `bash -n`, and its found-eq-0 guard is proven non-zero-exiting by the prefix override rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 2: the synthetic switch (command) fixture</name>

  <files>tests/fixtures/tool-honesty/switch-dispatch.cjs</files>

  <read_first>
    - `tests/fixtures/tool-honesty/positive.cjs` IN FULL (23 lines). It is the exact shape to copy: `'use strict'`, a header comment naming what the fixture proves and the expected verdict, `const { z } = require('zod')`, `function register(server)`, a `server.tool(` call with FOUR POSITIONAL arguments, `module.exports = { register }`.
    - `scripts/check-tool-honesty.cjs` lines 1100-1120 (`scanAll`'s four-positional-argument reader) so the fixture is written in the shape the checker can actually read. A fixture using `registerTool(server, {config}, cb)` scans as zero tools.
    - `276-PATTERNS.md` section "### `tests/fixtures/tool-honesty/switch-dispatch.cjs` (fixture)".
  </read_first>

  <action>
    Create `tests/fixtures/tool-honesty/switch-dispatch.cjs` as a single synthetic MCP tool named
    `fixture_switch` registered through four positional arguments to `server.tool(`.

    The description must make a per-command persistence claim naming BOTH command names, in the
    shape of the real mega-tool descriptions, for example naming `write-thing` as the write and
    `echo-thing` as the reference echo, so the checker's `sentenceNamesCommand` can attribute the
    claim per branch.

    The zod schema declares `command` as a `z.enum` with exactly the two values `write-thing` and
    `echo-thing`, because `extractCommandVocabulary` resolves the vocabulary from the enum.

    The handler body is a top-level `switch (command)` with:
      - `case 'write-thing':` whose body calls a resolvable write primitive so the branch resolves WRITES. Use a `fs.writeFileSync` call, which is on the checker's fixed `fs` write list, so the fixture needs no `require` of a repo module and stays hermetic.
      - `case 'echo-thing':` whose body only builds and returns a text response, no write anywhere.
      - a `default:` arm returning a text response.

    Header comment states plainly what the fixture proves: with a working splitter,
    `fixture_switch.echo-thing` must classify non-OK while `fixture_switch.write-thing`
    classifies OK, and that with the pre-fix splitter BOTH read OK because the whole handler body
    is shared. No em-dash characters anywhere in the file.
  </action>

  <verify>
    <automated>node -e "const c=require('./scripts/check-tool-honesty.cjs');const r=c.scanAll({files:[{absPath:require('path').resolve('tests/fixtures/tool-honesty/switch-dispatch.cjs'),relPath:'tests/fixtures/tool-honesty/switch-dispatch.cjs'}]});console.log(JSON.stringify(r.rows.map(x=>[x.tool,x.command,x.verdict])));process.exit(r.rows.length>0?0:1)"</automated>
  </verify>

  <acceptance_criteria>
    - The command above prints at least one row whose `tool` is `fixture_switch` and exits 0.
    - `grep -c "switch (command)" tests/fixtures/tool-honesty/switch-dispatch.cjs` equals 1.
    - `grep -c "z.enum" tests/fixtures/tool-honesty/switch-dispatch.cjs` equals 1.
    - `node -e "require('./tests/fixtures/tool-honesty/switch-dispatch.cjs')"` exits 0 (the module is loadable, which the checker itself never does but a broken fixture would mislead a later reader).
    - `grep -P '\x{2014}' tests/fixtures/tool-honesty/switch-dispatch.cjs` returns no match.
    - Do NOT commit in this task. The fixture is committed together with the RED test in Task 3, matching the `209b604f` precedent where the test and its five fixtures landed as one commit.
  </acceptance_criteria>

  <done>The fixture exists, is scannable by `scanAll` as tool `fixture_switch`, and declares a two-value `z.enum` command vocabulary over a top-level `switch (command)`.</done>
</task>

<task type="auto">
  <name>Task 3: the RED test for TOOLHON-01 and the TOOLHON-05 boundary enumeration</name>

  <files>tests/test-276-tool-honesty-switch-branches.cjs</files>

  <read_first>
    - `tests/test-ljj-tool-honesty.cjs` IN FULL (240 lines). Copy its harness scaffold verbatim from lines 37-75 (`check(label, cond, detail)` plus `finish()` with `process.exit(failed === 0 ? 0 : 1)`), its fixture-scan helper from lines 86-88 and 109-116, its em-dash assertion from lines 216-220 (the escape sequence backslash-u-2-0-1-4 inside a JS string literal, never a literal glyph), and its comment-stripping frozen-count guard from lines 225-237.
    - `scripts/check-tool-honesty.cjs` lines 512-560, the `splitBranches` body, and its `module.exports` block at lines 1252-1269 which exports both `splitBranches` and `scanAll`.
    - `276-RESEARCH.md` section `## Detector Bug D-1` (lines 233-310) for the instrumented proof and the measured before/after table, and `## Detector Boundaries` (lines 313-322) for the B-1 through B-5 identifiers this test asserts are enumerated in the script header.
    - `276-PATTERNS.md` section "### `tests/test-276-tool-honesty-switch-branches.cjs` (test, RED-first)".
  </read_first>

  <action>
    Create `tests/test-276-tool-honesty-switch-branches.cjs`.

    Header comment states, in prose: written FIRST (TDD RED), observed FAILING against the pre-fix
    `splitBranches`, naming the precedent commit pair `209b604f` (RED) and `75278850` (GREEN), and
    naming the specific defect (the `case` label regex runs over the masked text where string
    literals are blanked, so `\s+` swallows the case value and every label is rejected at
    `scripts/check-tool-honesty.cjs:529` and `:538-541`).

    Assertions, all through the exported API, never by re-reading the script source for behavior:

    1. TOOLHON-01 core. Scan the `switch-dispatch.cjs` fixture through `scanAll` and assert the
       returned rows contain a row with `command === 'echo-thing'` AND a row with
       `command === 'write-thing'`, that is, the branches are split at all. This is the assertion
       that fails today, because the pre-fix splitter returns an empty `branchMap` and the tool
       collapses to a single `(default)` row.
    2. TOOLHON-01 verdict split. Assert `fixture_switch.echo-thing` does NOT carry the reason
       string `a write primitive is reachable`, and that `fixture_switch.write-thing` does. This
       is the honest statement of the bug: today the echo branch inherits the write branch's
       reachability.
    3. TOOLHON-01 against the real tree. Call `splitBranches` directly on the `room_content`
       handler body extracted from `lib/mcp/tool-router.cjs` through the checker's own
       `scanAll`-adjacent path, and assert the recognized branch count is greater than 0. Do not
       hard-code a count; assert `> 0` so the test does not need editing when the vocabulary
       changes.
    4. TOOLHON-05 boundary enumeration. Read `scripts/check-tool-honesty.cjs` as text and assert
       its header block enumerates every known boundary by identifier: `B-1` (argument-gated
       writes), `B-2` (barrel re-exports), `B-3` (subprocess-mediated writes), `B-4` (dispatch-shape
       coverage), `B-5` (write-primitive semantics), and `B-6` (parameter `.describe()` strings are
       never scanned; `scanAll` reads only the second positional argument to `server.tool(`). B-6
       is a new boundary this phase mints, discovered while tracing `graph_write`'s `read_version`
       describe string. This assertion fails today because the header carries no B-N identifiers.
    5. Hygiene, matching `test-ljj-tool-honesty.cjs:216-220`: the checker's source contains no
       em-dash codepoint; assert via the escape sequence backslash-u-2-0-1-4 inside a JS
       string literal so THIS file stays clean under the repo fence.
    6. The false-verification comment. Assert that the comment block at
       `scripts/check-tool-honesty.cjs:559-565` no longer claims the fall-through grouping was
       "verified against real fall-through in this codebase". That verification cannot have
       happened, because the switch path never produced a label. Strip comments before any
       count-style grep so a comment naming a number cannot self-invalidate the gate, per
       `test-ljj-tool-honesty.cjs:225-237`.

    Print a `N passed, M failed` tail and `process.exit(failed === 0 ? 0 : 1)`.
  </action>

  <verify>
    <automated>node tests/test-276-tool-honesty-switch-branches.cjs; test $? -eq 1</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-276-tool-honesty-switch-branches.cjs` exits 1 BEFORE the fix (this plan's required end state) and its output names at least assertions 1, 2, 4 and 6 as FAIL.
    - The output line count of failures is recorded verbatim in the plan SUMMARY, so plan 276-06 can prove the same command flips to exit 0.
    - `grep -c "209b604f" tests/test-276-tool-honesty-switch-branches.cjs` is at least 1.
    - `grep -c "u2014" tests/test-276-tool-honesty-switch-branches.cjs` is at least 1 and `grep -P '\x{2014}' tests/test-276-tool-honesty-switch-branches.cjs` returns no match.
    - The test requires `scripts/check-tool-honesty.cjs` and uses `scanAll` / `splitBranches`; it never shells out to the CLI for a behavioral assertion (`scanAll` and `checkTree` never call `process.exit`, so they are safe inside a test).
    - RED commit hygiene: `git diff --cached --name-only` immediately before the commit lists exactly `tests/test-276-tool-honesty-switch-branches.cjs` and `tests/fixtures/tool-honesty/switch-dispatch.cjs`, and nothing else. `git show --stat HEAD` after the commit shows those two paths only, and zero changes under `scripts/`.
    - Commit message begins `test(276-01):` and states RED in the subject line.
  </acceptance_criteria>

  <done>The RED test and its fixture are committed in one commit containing no production file, and the test is observed exiting 1 against the unmodified `scripts/check-tool-honesty.cjs`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source -> checker text reader | The checker reads MCP tool files as TEXT and never `require()`s them; a fixture that changed that posture would turn a pre-commit hook into an arbitrary-code-execution surface |
| shared working tree -> git index | A second live session commits into this same tree; an unexamined `git add` can absorb a foreign edit into this phase's commits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-276-01 | Tampering | `tests/run-all-276.sh` discovery | mitigate | The found-eq-0 guard exits non-zero on zero discovery, proven at execution time via `TEST_276_PREFIX`, never asserted. A harness that reports green over zero discovery is the phase's own disease. |
| T-276-08 | Elevation of privilege | `tests/fixtures/tool-honesty/switch-dispatch.cjs` | mitigate | The fixture uses `fs.writeFileSync` as a text-level write marker only. It is never executed by the checker (`check-tool-honesty.cjs:36-41` reads scanned modules as text). Task 2's loadability check is a developer sanity check outside the checker's path. |
| T-276-09 | Tampering | git index in a shared working tree | mitigate | Every commit in this plan runs `git diff --cached --name-only` first and aborts on any unexpected path. `.planning/*` is gitignored, so planning artifacts need `git add -f`. |
| T-276-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO packages. No `package.json` dependency changes anywhere in plans 276-01 through 276-16; the checker's zero-dependency posture is load-bearing (`check-tool-honesty.cjs:36-41`) and adding acorn or espree is explicitly ruled out. No legitimacy checkpoint is owed because no install task exists. |
</threat_model>

<verification>
- `bash -n tests/run-all-276.sh` exits 0.
- `TEST_276_PREFIX=test-276-nonexistent-prefix- bash tests/run-all-276.sh` exits non-zero.
- `node tests/test-276-tool-honesty-switch-branches.cjs` exits 1 (RED by design).
- `bash tests/run-all-276.sh` exits non-zero at the end of this plan, and that is the correct state.
- `grep -rP '\x{2014}' tests/run-all-276.sh tests/test-276-tool-honesty-switch-branches.cjs tests/fixtures/tool-honesty/switch-dispatch.cjs` returns no match.
- `node tests/test-ljj-tool-honesty.cjs` still exits 0 (this plan touches no production file, so it must not move).
</verification>

<success_criteria>
- Three files exist: the aggregator, the fixture, the RED test.
- Two commits: the aggregator alone, then the test plus fixture alone.
- The RED test is observed FAILING and the failure list is recorded verbatim in the SUMMARY.
- No file under `scripts/`, `lib/` or `bin/` is modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs/276-01-SUMMARY.md` when done.
</output>
