---
phase: 276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs
plan: 13
type: execute
wave: 4
depends_on: [276-04, 276-11]
files_modified:
  - docs/2026-09-03-THEO-SEED-tool-honesty-ts-ast-port.md
autonomous: true
requirements: [TOOLHON-12, TOOLHON-13]
canon_parts: [7, 8]

must_haves:
  truths:
    - "The five absorbed-tool description constants are diffed against the plugin's live strings, with the Theo commit re-pinned at run time"
    - "The gate_render divergence created by this phase is registered as a Theo-side mirror task, coordinated and never executed from this repo"
    - "The already-live gate_answer and chain_run divergences are reported rather than left undiscovered"
    - "The out-of-repo recommendation says port the methodology, not the script, and names why a direct run would report a false success"
    - "Nothing under the Theo checkout is modified"
  artifacts:
    - path: "docs/2026-09-03-THEO-SEED-tool-honesty-ts-ast-port.md"
      provides: "The coordinated out-of-repo recommendation: the gate_render mirror task, the two live divergences, and the TS-AST port rationale"
      min_lines: 70
      contains: "registerContentTool"
  key_links:
    - from: "docs/2026-09-03-THEO-SEED-tool-honesty-ts-ast-port.md"
      to: "lib/mcp/tools/gate.cjs"
      via: "the mirror task names the plugin-side sentence that Theo's GATE_RENDER_DESCRIPTION must adopt"
      pattern: "gate.cjs"
---

<objective>
Turn the Theo coordination surface from a guess into a measured report, and register the one owed
mirror task without ever writing to the other repo.

Where the flip actually stands, re-measured for this phase: Theo Phase 08.4 (remote hosting) CLOSED
7/7 on 2026-09-02, so the "no hosting story" blocker is gone. Theo Phase 9 (Brain-Contract Cutover)
is 11 of 12 plans done, and the one open plan IS the flip: a single `MINDRIAN_BRAIN_URL` default
change at `lib/core/brain-client.cjs:24` shipped in a MindrianOS-Plugin release, then soak, then
`pws-brain-mcp` decommission. But Theo's own `09-MOS-LEARNING.md` rules the flip NOT READY on
content grounds. So "upcoming" means structurally one plan away and content-gated, not scheduled.

**What the flip does and does not touch here.** Theo absorbs exactly five operational tools
(`room_bind`, `graph_write`, `gate_render`, `gate_answer`, `chain_run`). Per Theo's own
`05-MOS-LEARNING.md`: "Only five of roughly thirty-four operational tools moved. The other
twenty-nine stay served by `mindrian-os`, and both servers run side by side." So this phase's
findings land on code that STAYS in this repo after the flip. They are not made moot by it.

**Description fixes do NOT auto-propagate.** Theo's five absorbed tools delegate to the plugin's
handlers at call time through `src/mcp/operational/delegate.ts`, so a BEHAVIOR fix flows through.
But each carries its OWN description constant on Theo's side. A description fix here leaves Theo's
catalog carrying the old text unless a mirror task is registered.

**Measured at research time and expected to reproduce:** of the 24 findings, exactly ONE lands on a
Theo-absorbed tool, `gate_render`. That is a clean, small coordination surface: one mirror task, not
five. Separately, two constants have ALREADY diverged and nobody noticed. `gate_answer` reads 1462
bytes on the plugin side against 1152 on Theo's, first divergence at character 585, because the
plugin gained a `SOURCED_FROM` and `USES_FRAMEWORK` clause in commit `2c8dfddf` this same session
and Theo's copy predates it. `chain_run` reads 1113 against 1006. **Theo's catalog currently
under-describes what `gate_answer` actually does after the flip**, which is the benign direction but
is still a description-versus-behavior gap, and it proves the drift channel is live rather than
theoretical.

**Theo already tracks this exact disease under its own name.** `delegate.ts` names "a FALSE FAILURE,
the mirror image of the false-success class this project tracks", measured live three times.
Theo's guard is its chokepoint-audit rule set, and rule 5 itself once "reported green over a live
violation of the invariant it exists to protect" (Theo `05-REVIEW CR-01`). That is a regex-over-
error-shape audit, not a description-versus-behavior scan; Theo has zero mention of
`check-tool-honesty` anywhere.

**D-276-6 and Theo D-04 bound this plan absolutely: coordinated, never executed from this repo.**
Nothing under `/home/jsagi/Theo` is written. TOOLHON-12's parity test is a non-blocking signal that
skips when the checkout is absent.

Purpose: leave the flip a smaller diff instead of a rediscovery.
Output: a measured parity report and one coordinated recommendation document.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs/276-PATTERNS.md
@.planning/phases/276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs/276-VALIDATION.md
@.planning/phases/276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs/276-11-SUMMARY.md

Do NOT read `.planning/STATE.md` in full (1.4MB).
Do NOT read `276-RESEARCH.md` in full (1300 lines). Read only the sections named per task.
</context>

<artifacts_this_phase_produces>

This plan (276-13) produces:

| Artifact | Kind | Path |
|---|---|---|
| `docs/2026-09-03-THEO-SEED-tool-honesty-ts-ast-port.md` | new coordination document | `docs/` |

It produces NOTHING under `/home/jsagi/Theo`. The SEED is filed in THIS repo as a recommendation
the navigator carries across; Theo's own `/gsd-capture` is the mechanism on that side, run from that
repo, by whoever works there.

The full phase artifact list is in `276-01-PLAN.md` under `artifacts_this_phase_produces`.

</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: run the parity check with the Theo commit re-pinned, and record the measured report</name>

  <files>none (measurement only; results go into the plan SUMMARY and feed Task 2)</files>

  <read_first>
    - `tests/test-276-theo-description-parity.cjs` IN FULL, as authored in plan 276-04. Its extraction strategy, its skip-when-absent behavior, its `--strict` arm and its report shape.
    - `276-04-SUMMARY.md` for the measured `gate_answer` first-divergence offset recorded at wave 0, so the post-`gate_render`-fix run can be compared against it rather than against a remembered number.
    - `276-11-SUMMARY.md` for the exact new `gate_render` sentence that landed, which is what Theo's `GATE_RENDER_DESCRIPTION` must adopt.
    - `/home/jsagi/Theo/src/mcp/operational/gate-render.ts` lines 85-95, READ-ONLY, to confirm the constant is still where the pin says it is.
    - `276-RESEARCH.md` sections `### B1` (lines 1119-1139) and `### B1a` (lines 1141-1160), including assumption A13 (the `chain_run` divergence offset is NOT trustworthy from a regex extractor) and A14 (re-pin at plan time).
  </read_first>

  <action>
    Re-pin first. Run `git -C /home/jsagi/Theo rev-parse --short HEAD` and compare against the
    research pin `83a1ce2`. Record both. If they differ, that is expected news (Theo is actively
    developed), and the report simply names the commit it actually measured against. Do NOT check
    out a different Theo commit; read the working checkout as it stands, read-only.

    Run `node tests/test-276-theo-description-parity.cjs` and capture the full five-line report.
    Then run `node tests/test-276-theo-description-parity.cjs --strict` and capture its exit code,
    which is expected to be non-zero while any constant differs.

    Record, in the plan SUMMARY, a table with one row per constant: name, plugin byte length, Theo
    byte length, verdict (IDENTICAL, DIFFERS with the first divergence offset, or EXTRACTION_FAILED
    with the reason), and whether this phase caused the divergence or found it pre-existing.

    Expected shape at this point in the phase, to be confirmed rather than assumed:
      - `room_bind`: IDENTICAL, untouched by this phase.
      - `graph_write`: IDENTICAL. Plan 276-11 deliberately changed only the `read_version` parameter
        describe and left the tool description byte-identical, precisely so this stays a single small
        coordination item. Confirm it.
      - `gate_render`: DIFFERS, CAUSED BY THIS PHASE (plan 276-11). This is the owed mirror task.
      - `gate_answer`: DIFFERS, PRE-EXISTING. Confirm the cause independently by running
        `grep -c "SOURCED_FROM\|USES_FRAMEWORK" /home/jsagi/Theo/src/mcp/operational/gate-answer.ts`
        and asserting it returns 0, which proves Theo's copy predates the plugin's `2c8dfddf` clause.
      - `chain_run`: DIFFERS, PRE-EXISTING. Report the byte lengths. If the offset comes from a lossy
        extraction path, say EXTRACTION_FAILED or mark the offset untrustworthy rather than printing
        a number you cannot defend. Assumption A13 exists for exactly this.

    If any constant reports EXTRACTION_FAILED, that is a finding about the TEST, not about Theo.
    Record it and state whether the extractor needs the compiled-module path. A parity test built on
    a lossy extractor reporting green over a real divergence is `Theo 05-REVIEW CR-01`'s exact
    failure and this phase must not reproduce it.

    Write nothing to `/home/jsagi/Theo`. Confirm with `git -C /home/jsagi/Theo status --porcelain`
    before and after.
  </action>

  <verify>
    <automated>node tests/test-276-theo-description-parity.cjs && git -C /home/jsagi/Theo status --porcelain | wc -l && git -C /home/jsagi/Theo rev-parse --short HEAD</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-276-theo-description-parity.cjs` exits 0 and prints five report lines. The full output is pasted verbatim into the plan SUMMARY.
    - `node tests/test-276-theo-description-parity.cjs --strict` exits non-zero, and its exit code is recorded.
    - The measured Theo HEAD short SHA is recorded alongside the research pin `83a1ce2`, with a note whether they match.
    - `grep -c "SOURCED_FROM\|USES_FRAMEWORK" /home/jsagi/Theo/src/mcp/operational/gate-answer.ts` returns 0, confirming the `gate_answer` divergence cause independently rather than inheriting it.
    - `graph_write` reports IDENTICAL. If it does not, plan 276-11 changed more than the parameter describe and that is a finding to state, not to absorb.
    - `git -C /home/jsagi/Theo status --porcelain` produces zero lines both before and after this task.
    - Every constant is classified as CAUSED BY THIS PHASE or PRE-EXISTING, with no row left unclassified.
    - Any EXTRACTION_FAILED row is recorded as a finding about the test with a stated next step.
  </acceptance_criteria>

  <done>The five-constant parity is measured against a re-pinned Theo commit, every divergence is classified by cause, the `gate_answer` cause is independently confirmed, and nothing in the Theo checkout moved.</done>
</task>

<task type="auto">
  <name>Task 2: write the coordinated out-of-repo recommendation</name>

  <files>docs/2026-09-03-THEO-SEED-tool-honesty-ts-ast-port.md</files>

  <read_first>
    - This plan's own Task 1 measurements, recorded in the SUMMARY. The document reports what was measured, not what the research predicted.
    - `276-RESEARCH.md` section `### B3: does the methodology port to Theo's registrars?` (lines 1209-1213) IN FULL. It carries the three structural mismatches and the honest recommendation: port the METHODOLOGY, not the script.
    - `scripts/check-tool-honesty.cjs` lines 478-490 (`findServerToolCalls` matching `/server\.tool\s*\(/`), lines 1106-1113 (`scanAll` reading four POSITIONAL arguments), lines 170-220 (`maskNonCode`, which knows JS strings, comments and regex literals and nothing of TypeScript), line 227 (`scanBalanced`'s pair map, which has no angle brackets), and lines 370-409 (`resolveWritePrimitives`, which `require()`s three CJS modules to read `module.exports` and cannot work against TypeScript sources).
    - `/home/jsagi/Theo/src/mcp/register-content-tool.ts` lines 98-152 (the required `description: string` config field at 106 and `inputSchema: sealed` at 151-152) and `/home/jsagi/Theo/src/mcp/register-graph-tool.ts` lines 16-56 (whose own comment calls `server.tool(...)` deprecated in its SDK version and "a zero-gate"). READ-ONLY.
    - `docs/2026-09-03-HANDOFF-constitution-ratified-tool-honesty-phase-276.md`, for the house style of a coordination document in this repo and for the cross-links a reader will expect.
  </read_first>

  <action>
    Create `docs/2026-09-03-THEO-SEED-tool-honesty-ts-ast-port.md`.

    Section 1, the owed mirror task, first because it is the actionable item. State the exact
    change: Theo's `GATE_RENDER_DESCRIPTION` at `src/mcp/operational/gate-render.ts:89-93` should
    adopt the plugin's corrected final sentence, quoted verbatim from
    `lib/mcp/tools/gate.cjs` as landed by plan 276-11. State the reason in one line: an in-memory
    gate-ledger mint is not persistence, and "minted" reads as durable to any caller. State D-276-3
    and D-276-6 as the authority. State plainly that this is COORDINATED, not executed from this
    repo (Theo D-04), and that the plugin cannot and must not push this change.

    Section 2, the two pre-existing divergences found by measurement, not by design. Report
    `gate_answer` (with its measured byte lengths, its first-divergence offset, and the independently
    confirmed cause: the plugin's `SOURCED_FROM` and `USES_FRAMEWORK` clause from `2c8dfddf`, absent
    from Theo's copy, proven by a zero-count grep) and `chain_run` (byte lengths, with the offset
    marked untrustworthy if the extraction path was lossy). Say the useful thing plainly: Theo's
    catalog currently UNDER-describes what `gate_answer` actually does after the flip. That is the
    benign direction, and it still proves the drift channel is live.

    Section 3, the methodology port, framed as a SEED for Theo's own `/gsd-capture`, navigator
    approved, to be filed before Theo's plan `09-12` authorizes the flip.

    Lead with the warning, because it is the part a reader most needs before touching anything: **a
    direct run of `scripts/check-tool-honesty.cjs` against Theo scans ZERO tools and reports OK.**
    That is a false success inside a false-success detector, and it is the single most important
    sentence in the document.

    Then the three structural mismatches, each with the plugin-side file and line:
      - Discovery. `findServerToolCalls` matches `/server\.tool\s*\(/` and `scanAll` reads four
        POSITIONAL arguments. Theo's shape is `registerContentTool(server, { name, description,
        inputSchema }, cb)` at 23 call sites plus `registerOperationalTool` at 5. Theo's real tool
        count is 28, not "roughly 27". A config-object key lookup is needed, not a path change.
      - Language. `maskNonCode` handles JS strings, comments and regex literals and knows nothing of
        TypeScript type annotations, generics such as `registerContentTool<OutputArgs, InputArgs>`,
        or `as const`. Angle brackets are not in `scanBalanced`'s pair map, so generic call sites
        would confuse the forward scanner.
      - Write-primitive vocabulary, the most consequential. `resolveWritePrimitives` derives its
        names by `require()`ing three CJS modules and reading `module.exports`, which cannot work
        against TypeScript sources. And Theo's writes go through `delegate.ts` to the plugin's
        handlers AT CALL TIME rather than through a local `navigation.cjs`, so the whole depth-1
        reachability model would need rebuilding around the delegation boundary.

    Then the recommendation: port the METHODOLOGY, not the script. Name what transfers: the
    six-stage architecture, the claim-tier vocabulary with its negation and hyphenated-token guards,
    the `ALLOWED_UNVERIFIED` discipline, and above all the fix-the-detector-never-allowlist rule
    that took the first sweep from 34 HIGH RISK to 1. Recommend `ts.createSourceFile`, the
    compiler's own AST, which removes stages A through C entirely and would make Theo's version
    SHORTER than the plugin's, not longer.

    Then the cautionary case, which is the document's strongest argument: the plugin's own detector
    shipped with a dead switch parser (D-1) for exactly this reason. A regex over source is how a
    detector silently stops detecting. Cite this phase and the measured before-and-after (10
    findings to 24) from `276-06-SUMMARY.md`.

    Section 4, cross-links. Link to this phase directory, to `276-RESEARCH.md`'s Theo cross-check
    section, to `tests/test-276-theo-description-parity.cjs` as the standing signal, and note the
    signal is non-blocking by design and skips when the Theo checkout is absent.

    No em-dashes. Hyphens only. Write nothing under `/home/jsagi/Theo`.
  </action>

  <verify>
    <automated>test -f docs/2026-09-03-THEO-SEED-tool-honesty-ts-ast-port.md && ! grep -qP '\x{2014}' docs/2026-09-03-THEO-SEED-tool-honesty-ts-ast-port.md && grep -c "registerContentTool" docs/2026-09-03-THEO-SEED-tool-honesty-ts-ast-port.md && git -C /home/jsagi/Theo status --porcelain | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - The document exists and is at least 70 lines.
    - Section 1 quotes the plugin's corrected `gate_render` sentence VERBATIM and names the exact Theo file and line to change, `src/mcp/operational/gate-render.ts:89-93`.
    - Section 1 states explicitly that the change is coordinated and never executed from this repo, citing Theo D-04, D-276-3 and D-276-6.
    - Section 2 reports the MEASURED byte lengths and offsets from Task 1, not the research figures, and marks any untrustworthy offset as such.
    - Section 3's first substantive sentence is the zero-tools-scanned warning: `grep -c "zero tools" docs/2026-09-03-THEO-SEED-tool-honesty-ts-ast-port.md` is at least 1.
    - Section 3 names all three structural mismatches with plugin-side file and line citations, and states Theo's tool count as 28 with its 23-plus-5 breakdown.
    - Section 3 recommends `ts.createSourceFile` and cites D-1 as the cautionary case with the measured 10-to-24 figure from `276-06-SUMMARY.md`.
    - `grep -P '\x{2014}' docs/2026-09-03-THEO-SEED-tool-honesty-ts-ast-port.md` returns no match.
    - `git -C /home/jsagi/Theo status --porcelain` produces zero lines.
    - `git diff --cached --name-only` before the commit lists only the one new document.
  </acceptance_criteria>

  <done>The coordination document exists, leads with the owed mirror task, reports the two pre-existing divergences from measurement rather than research, warns that a direct script run reports a false success, and recommends the AST port with D-1 as its cautionary case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin repo -> Theo checkout | Read-only from here. Theo D-04: coordinated, not executed cross-repo |
| parity report -> the other repo's contributors | A report containing plugin-internal text could carry it across a boundary it should not cross |
| shared working tree -> git index | A second live session commits into this same tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-276-06 | Information disclosure | Theo description text carrying an unredacted plugin string (Theo T-05-04) | accept, out of repo, cited only | This is Theo's own threat on Theo's own side and cannot be mitigated from this repo. It is NAMED here so a Theo-side reader adopting the mirrored sentence checks it before merging. The sentence being mirrored is a public tool description, not internal state. |
| T-276-14 | Denial of service | Parity signal turning into a gate | mitigate | The test exits 0 by default and only under an explicit `--strict` flag exits non-zero. It is registered in no release gate, no doctor gate and no pre-commit hook. Task 1 runs both arms and records both exit codes so the posture is visible rather than assumed. |
| T-276-33 | Repudiation | A lossy extractor reporting green over a real divergence | mitigate | Any constant the extractor cannot read reports EXTRACTION_FAILED and is recorded as a finding about the TEST with a stated next step, never as IDENTICAL. This is `Theo 05-REVIEW CR-01`'s exact failure and the plan refuses to reproduce it. Assumption A13 marks the `chain_run` offset untrustworthy unless the compiled-module path was used. |
| T-276-34 | Tampering | Writing to the Theo checkout | mitigate | `git -C /home/jsagi/Theo status --porcelain` is asserted to produce zero lines both before and after each task. The parity test itself contains no write call, asserted in plan 276-04. |
| T-276-09 | Tampering | git index in a shared working tree | mitigate | One commit, preceded by `git diff --cached --name-only`, aborting on any unexpected path. |
| T-276-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs. The recommended `ts.createSourceFile` is a Theo-side implementation detail in a document, not a dependency added here. No legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `node tests/test-276-theo-description-parity.cjs` exits 0 and prints five report lines.
- `node tests/test-276-theo-description-parity.cjs --strict` exits non-zero while any constant differs.
- `git -C /home/jsagi/Theo status --porcelain` produces zero lines.
- `test -f docs/2026-09-03-THEO-SEED-tool-honesty-ts-ast-port.md` succeeds.
- `grep -P '\x{2014}' docs/2026-09-03-THEO-SEED-tool-honesty-ts-ast-port.md` returns no match.
- `bash tests/run-all-276.sh` runs the parity arm without turning the aggregator red.
</verification>

<success_criteria>
- Five constants measured against a re-pinned Theo commit, every divergence classified as caused by this phase or pre-existing.
- The `gate_answer` divergence cause independently confirmed by a zero-count grep rather than inherited from research.
- One coordination document that leads with the mirror task, warns about the zero-tools-scanned false success, and recommends the AST port.
- Nothing under `/home/jsagi/Theo` modified.
</success_criteria>

<output>
Create `.planning/phases/276-mcp-tool-honesty-triage-and-close-the-check-tool-honesty-cjs/276-13-SUMMARY.md` when done.
</output>
