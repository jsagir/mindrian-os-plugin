---
phase: 173-publish-jtbd-need-selector
plan: 03
type: execute
wave: 2
depends_on: ["173-01"]
files_modified:
  - lib/core/sensors/sensor-show-share.cjs
  - lib/core/insight-sensors.cjs
  - data/dispatch-framework-map.json
  - tests/test-show-share-sensor.cjs
  - tests/test-publish-needs-flow.cjs
  - tests/run-all-173.sh
autonomous: true
requirements: [R4, R12]
canon_parts: [3, 8, 9, 10, 11]

cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["insight-sensors dispatchSensors", "navigation-engine decide", "command-resolver"]
  gate_impact: "Registers one new sensor (SENS-SHOW) in the dispatchSensors registry; reuses the existing context_block reach; no new gate check shipped."
  explanation: "Phase 173 ADDS the show/share trigger sensor to the dispatchSensors spine (R3/R4): a turn expressing show/present/share intent surfaces the context_block reach naming the /mos:show selector through the one governed path (dispatchSensors -> decide() -> command-resolver). It USES CIRS (it produces a candidate reach on the existing spine) and mints NO 7th reach (precedent: SENS-09 reuses brain_consult). Context-enriched + confidence-thresholded at the canon 0.70 gate, keyword as the fallback tier (D-03)."

must_haves:
  truths:
    - "a show/present/share-intent turn fires the context_block reach via dispatchSensors; a neutral turn does not (R4)"
    - "the sensor reuses the existing context_block reach; REACH_IDS stays length 6 (no 7th reach minted, D-03)"
    - "the trigger is context-first with keyword as the fallback tier, recorded via trigger_tier (D-03)"
    - "the end-to-end test proves R1 (4 lanes, zero command-name labels), R2/R7 (real resolves_to + connections|gaps), R3 (command-resolver resolution), R6 (persona-default lane)"
    - "the R12 cross-surface Tri-Polar contract is documented (selector identical across surfaces; results degrade to inline ui:// with an explicit note)"
  artifacts:
    - path: "lib/core/sensors/sensor-show-share.cjs"
      provides: "SENS-SHOW: the show/share intent sensor surfacing context_block naming /mos:show"
      exports: ["sensorShowShare"]
    - path: "lib/core/insight-sensors.cjs"
      provides: "sensorShowShare registered in SENSOR_REGISTRY + module.exports"
      contains: "sensorShowShare"
    - path: "tests/run-all-173.sh"
      provides: "The phase aggregator: 173-01 + 173-03 suites + carried reach/posture drift fences"
      min_lines: 10
  key_links:
    - from: "lib/core/sensors/sensor-show-share.cjs"
      to: "lib/core/insight-sensors.cjs dispatchSensors"
      via: "registered in SENSOR_REGISTRY"
      pattern: "sensorShowShare"
    - from: "sensor-show-share dispatch handle"
      to: "/mos:show selector"
      via: "WFL-01 dispatch route / command-resolver"
      pattern: "show-jtbd-selector|show"

---

<objective>
Ship the "show my work" wire (R4): a context sensor (lib/core/sensors/sensor-show-share.cjs) that detects show/present/share intent and surfaces a context_block candidate reach naming the /mos:show selector, registered in the dispatchSensors registry. The sensor is context-enriched + confidence-thresholded (the canon 0.70 gate; keyword is the fallback tier per D-03), reuses the existing context_block reach (no 7th reach minted), and surfaces a STANDING SUGGESTION at the Decision Gate (never auto-opens UI -- Canon Part 3 GUIDED default). This plan also carries the cross-surface note (R12) and the end-to-end test proving the full R1-R7 flow.

Purpose: Make the selector invokable, not typed-only -- the trigger IS the discoverability surface (Tavily research). One governed path; no second selection brain (CIRS R4).
Output: sensor-show-share.cjs + its registration + the dispatch-framework-map entry + the two test files + the phase aggregator.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-publish-jtbd-need-selector/173-SPEC.md
@.planning/phases/173-publish-jtbd-need-selector/173-CONTEXT.md
@CLAUDE.md
@lib/core/insight-sensors.cjs
@lib/core/sensors/sensor-types.cjs
@lib/core/sensors/sensor-diffusion-adoption.cjs
@data/dispatch-framework-map.json
@lib/workflow/command-resolver.cjs
@lib/core/chain-executor.cjs
@data/publish-needs.json
@lib/core/publish-needs-default-lane.cjs
@tests/test-diffusion-adoption-sensor.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author lib/core/sensors/sensor-show-share.cjs (SENS-SHOW, R4) + register it in dispatchSensors</name>
  <files>lib/core/sensors/sensor-show-share.cjs, lib/core/insight-sensors.cjs, data/dispatch-framework-map.json</files>
  <read_first>
    - lib/core/sensors/sensor-diffusion-adoption.cjs (the EXACT precedent: SENS-09 reuses a frozen reach (brain_consult) for a new intent; signal/context/keyword/marker mode precedence; classifyTriggerTier labeling; makeReach with a frozen reach_id + posture; evidence is scalars/enums only -- the show/share sensor mirrors this control flow but targets context_block)
    - lib/core/sensors/sensor-types.cjs (makeReach, REACH_IDS, POSTURE_IDS, classifyTriggerTier, isContextTier -- the context-first / keyword-fallback tier model the sensor records in evidence)
    - lib/core/insight-sensors.cjs (the SENSOR_REGISTRY array + the require block + the module.exports list -- the new sensor is appended exactly as sensorDiffusionAdoption was, lines ~86-91 require, ~413-414 registry, ~485-486 export)
    - data/dispatch-framework-map.json (the WFL-01 translation map: the sensor's dispatch handle maps to a resolvable target; the show/share dispatch resolves to the /mos:show selector front door, not a framework)
    - lib/workflow/command-resolver.cjs (commandsForFramework -- how the dispatch handle later resolves; the sensor only PRODUCES the candidate reach naming the dispatch handle)
  </read_first>
  <behavior>
    - sensorShowShare(turn, tuple, ctx) returns a candidate reach (via makeReach) or null. It FIRES on, in R3 precedence order: (1) SIGNAL MODE -- an explicit 'show_share_intent' signal kind on the turn (strongest); (2) CONTEXT MODE -- a LOCAL navigator problem-state enum present (the trigger keys on problem-state via the navigation.cjs chokepoint convention, enum/scalar only, Part 8/9); (3) KEYWORD MODE -- the LOCAL turn text contains a show/share lexicon term (the DEMOTED fallback tier per D-03: "show", "present", "share", "send a link", "pitch", "make it land", "publish", "visualize" -- OUR fixed generic vocabulary, never user content). null when no mode fires.
    - The returned reach: reach_id 'context_block' (REUSE the existing reach -- mint NO 7th reach; mirrors SENS-09 reusing brain_consult, honoring D-03); posture 'hold' (a standing SUGGESTION at the Decision Gate, never push_forward -- it must NOT auto-open UI per Canon Part 3 GUIDED default + the "no card unprompted" doctrine, D-03); dispatch 'show-jtbd-selector' (the generic handle the WFL-01 map routes to /mos:show); signal 'show_share_intent'; evidence a flat scalar/enum bag carrying { mode, trigger_tier, problem_type } ONLY -- never the user's matched text (Part 8).
    - trigger_tier is recorded via classifyTriggerTier so "context first, keyword fallback" is a legible, testable property (D-03). The confidence-threshold at 0.70 is honored by the spine downstream (decide() applies the 0.70 RECOMMENDED gate -- the sensor only surfaces the candidate; it does NOT render a RECOMMENDED marker, matching the Part 3 sub-0.70 no-marker rule).
    - Phase 144 fence: this file PRODUCES a candidate reach; it never assigns routing_source and never requires/defines decide(). Pure / sync / LOCAL-first; soft-fail to null; never throws.
    - data/dispatch-framework-map.json gains a 'show-jtbd-selector' entry. Because the resolve target is the /mos:show command (a selector front door, not a Brain framework), the map value is the selector handle the orchestrator routes to; document the entry in the _note convention so the drift test understands it is a selector route, not a framework name. (If the existing drift test tests/test-dispatch-framework-map-drift.cjs requires every value to be a framework-names.json member, route the dispatch through the command-resolver path instead and leave dispatch-framework-map untouched -- read the drift test first and pick the path that keeps it green.)
  </behavior>
  <action>Create lib/core/sensors/sensor-show-share.cjs mirroring lib/core/sensors/sensor-diffusion-adoption.cjs verbatim in structure: a SHOW_SHARE_LEXICON fixed-vocabulary array, hasShowShareSignal(turn), textMatchesLexicon(turn), a context-mode predicate over the tuple problem-state, and sensorShowShare(turn, tuple, ctx) returning makeReach({ reach_id:'context_block', posture:'hold', dispatch:'show-jtbd-selector', signal:'show_share_intent', evidence:{ mode, trigger_tier, problem_type } }) with the signal/context/keyword precedence and classifyTriggerTier labeling. Register it in lib/core/insight-sensors.cjs exactly as sensorDiffusionAdoption is wired: add the require near the other sensor requires, append sensorShowShare to the SENSOR_REGISTRY array, and add it to module.exports. Add the 'show-jtbd-selector' route per the behavior block (read tests/test-dispatch-framework-map-drift.cjs FIRST to choose the green path). Reuse context_block; mint NO 7th reach. No em-dashes.</action>
  <verify>
    <automated>node -e "const s=require('./lib/core/insight-sensors.cjs');const {REACH_IDS,POSTURE_IDS}=require('./lib/core/sensors/sensor-types.cjs');const fire=s.sensorShowShare?s.sensorShowShare({text:'help me show my work to investors',signals:[]},{problem_type:'IDP'},{}):require('./lib/core/sensors/sensor-show-share.cjs').sensorShowShare({text:'help me show my work',signals:[]},{},{});if(!fire)throw new Error('did not fire on show keyword');if(fire.reach_id!=='context_block')throw new Error('not context_block: '+fire.reach_id);if(POSTURE_IDS.indexOf(fire.posture)===-1)throw new Error('bad posture');const neutral=require('./lib/core/sensors/sensor-show-share.cjs').sensorShowShare({text:'what is the weather',signals:[]},{},{});if(neutral!==null)throw new Error('fired on neutral turn');console.log('SENS-SHOW OK: fires on show/share, null on neutral, context_block reused');"</automated>
  </verify>
  <acceptance_criteria>
    - sensorShowShare fires on a show/present/share turn returning reach_id 'context_block' with posture 'hold'; returns null on a neutral turn.
    - The sensor is in lib/core/insight-sensors.cjs SENSOR_REGISTRY and module.exports; no 7th reach is minted (REACH_IDS still length 6).
    - The dispatch route resolves /mos:show without breaking tests/test-dispatch-framework-map-drift.cjs.
  </acceptance_criteria>
  <done>The "show my work" trigger fires the context_block reach naming the selector via dispatchSensors; a neutral turn does not.</done>
</task>

<task type="auto">
  <name>Task 2: The end-to-end flow test (proves R1-R7) + the cross-surface note (R12) + the phase aggregator</name>
  <files>tests/test-show-share-sensor.cjs, tests/test-publish-needs-flow.cjs, tests/run-all-173.sh, lib/core/sensors/sensor-show-share.cjs</files>
  <read_first>
    - tests/test-diffusion-adoption-sensor.cjs (the test idiom: assert/ok helper, frozen-reach assertions, SENSOR_REGISTRY membership + export check, dispatch-map resolution check, Part-8 scalar-only evidence check)
    - tests/run-all-172.sh (the bash PASS/FAIL aggregator format to mirror for run-all-173.sh)
    - lib/core/insight-sensors.cjs dispatchSensors (the chokepoint the flow test calls: a show/share turn must produce a context_block reach in the returned array; a neutral turn must not)
    - lib/workflow/command-resolver.cjs (the flow test asserts the resolved object came from the resolver, not from memory -- D-03: resolve a publish-needs.json resolves_to through frameworksForCommand / the registry door)
    - lib/core/publish-needs-default-lane.cjs (the flow test asserts a founder-weighted role_blend opens 'get-into-world' and a researcher-weighted opens 'find-broken' -- R6)
    - lib/mcp/app-views.cjs (the 3 shipped inline ui:// views -- the cross-surface render target the R12 note documents: results route to inline ui:// where Node cannot run, with an explicit degradation note when a Node-only surface is unavailable)
  </read_first>
  <behavior>
    - tests/test-show-share-sensor.cjs (the SENS-SHOW unit suite, mirroring test-diffusion-adoption-sensor.cjs): null without a trigger; fires on signal / keyword / context with a FROZEN reach_id (context_block) + posture (hold); Part-8-clean evidence (scalars/enums, no user prose); registered in SENSOR_REGISTRY + exported from insight-sensors; the dispatch handle resolves to /mos:show (the selector route); dispatchSensors({show turn}) includes a context_block reach AND dispatchSensors({neutral turn}) does not include the show-share reach.
    - tests/test-publish-needs-flow.cjs (the R1-R7 end-to-end proof): (R1) the selector data renders 4 lanes with zero command-name labels -- assert every publish-needs.json job label has no /mos: token and the _lanes block has exactly 4 ids; (R2/R7) every job resolves_to is real and every job has a connections|gaps shows tag (call the 173-01 validator or re-assert in-test); (R3 resolution) take a job's resolves_to and resolve it via command-resolver, asserting the resolved object came from the registry (frameworksForCommand returns a non-fabricated result for a real command); (R4) a show/share-intent turn fires the context_block reach via dispatchSensors while a neutral turn does not; (R6) defaultLaneForRoleBlend({founder:0.7}) === 'get-into-world' and ({researcher:0.8}) === 'find-broken'.
    - The cross-surface note (R12): add a documented comment block in sensor-show-share.cjs (and/or a short note appended to commands/show.md by 173-02 -- here just the sensor-side note) recording the Tri-Polar contract: the AskUserQuestion selector is identical across CLI/Desktop/Cowork; rendered RESULTS degrade per the 3-layer model -- inline ui:// views (dashboard/wiki/graph, already shipped in lib/mcp/app-views.cjs) where Node cannot run, with an EXPLICIT degradation note (never a silent failure) when a Node-only result (present/export/snapshot/radar) is requested on Desktop/Cowork.
    - tests/run-all-173.sh runs test-publish-needs-map.cjs (from 173-01), test-show-share-sensor.cjs, test-publish-needs-flow.cjs, AND the carried frozen-bank drift fences (test-reach-ids-drift.cjs / test-posture-ids-drift.cjs) to prove 173 is ADDITIVE (no frozen-set move), exiting non-zero if any fail.
  </behavior>
  <action>Create tests/test-show-share-sensor.cjs mirroring tests/test-diffusion-adoption-sensor.cjs (the SENS-SHOW unit suite with the frozen-reach + registry-membership + Part-8 + dispatch-resolution assertions and the dispatchSensors fire/no-fire pair). Create tests/test-publish-needs-flow.cjs asserting the R1-R7 chain described in the behavior block (4 lanes / zero command-name labels; real resolves_to + connections|gaps; resolve via command-resolver; sensor fires on show/share + not on neutral; persona-default lane for founder/researcher). Append the R12 cross-surface Tri-Polar note as a documented comment block in lib/core/sensors/sensor-show-share.cjs (selector identical across surfaces; results degrade to inline ui:// with an explicit note, never silent). Create tests/run-all-173.sh mirroring tests/run-all-172.sh, composing the 173-01 + 173-03 suites + the carried reach/posture drift fences. No em-dashes.</action>
  <verify>
    <automated>bash tests/run-all-173.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-173.sh` exits 0: the selector data has 4 lanes with zero command-name labels (R1); every job resolves_to is real with a connections|gaps tag (R2/R7); a job resolves via command-resolver (R3); the show/share turn fires context_block via dispatchSensors and a neutral turn does not (R4); the persona-default lane follows role_blend (R6).
    - The carried reach/posture drift fences pass (REACH_IDS length 6, POSTURE_IDS length 3 -- no frozen-set move).
    - sensor-show-share.cjs carries the R12 cross-surface Tri-Polar note.
  </acceptance_criteria>
  <done>The full selector flow is proven by test: 4 lanes, command-resolver resolution, the trigger firing on intent only, the persona-default lane, and the additive frozen-bank invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator turn -> sensorShowShare | untrusted turn text crosses here; the sensor reads it LOCALLY to DECIDE firing and egresses ONLY a mode/tier/problem_type enum, never the matched text |
| sensorShowShare -> dispatchSensors -> decide() | the one governed path (CIRS R4); the sensor produces a candidate reach only, never routes itself |
| sensor -> Brain | NONE: the sensor makes zero Brain and zero network calls (Part 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-04 | Information disclosure | turn text -> evidence/Brain | mitigate | makeReach freezes evidence to scalar/enum primitives only; the lexicon is OUR fixed vocabulary; the Part-8 sensor sweep (tests/test-sensors-part8-sweep.cjs) already spans every lib/core/sensors/*.cjs |
| T-173-05 | Elevation of privilege | a second selection brain | mitigate | the sensor produces a candidate reach on the existing spine only; resolution is command-resolver -> runChain; no bespoke router (CIRS R4) |
| T-173-06 | Tampering | minting a 7th reach | mitigate | reach_id is context_block (existing); the carried reach-id drift fence in run-all-173.sh fails CI on any add |
</threat_model>

<verification>
- `bash tests/run-all-173.sh` exits 0 (the full R1-R7 flow + the additive frozen-bank fences).
- REACH_IDS stays length 6; POSTURE_IDS stays length 3 (no 7th reach; D-03 honored).
- tests/test-dispatch-framework-map-drift.cjs (if touched) stays green.
- No em-dashes; all new files are CJS / bash (no TypeScript, no build step).
</verification>

<success_criteria>
- The show/share trigger sensor (R4) fires the context_block reach naming the selector through dispatchSensors on intent turns only, context-enriched + 0.70-thresholded with keyword as fallback (D-03), reusing the existing reach (no 7th minted).
- The end-to-end test proves R1 (4 lanes, zero command-name labels), R2/R7 (real resolves_to + connections|gaps), R3 (command-resolver resolution), R4 (trigger fire/no-fire), R6 (persona-default lane).
- The R12 cross-surface Tri-Polar contract is documented (selector identical across surfaces; results degrade to inline ui:// with an explicit note, never silent).
</success_criteria>

<output>
Create `.planning/phases/173-publish-jtbd-need-selector/173-03-SUMMARY.md` when done.
</output>
