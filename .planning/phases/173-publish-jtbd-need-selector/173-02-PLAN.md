---
phase: 173-publish-jtbd-need-selector
plan: 02
type: execute
wave: 2
depends_on: ["173-01"]
files_modified:
  - commands/show.md
  - data/connector-registry.json
  - data/connector-coverage-ledger.json
  - data/harness-manifest.json
autonomous: true
requirements: [R1, R3, R5, R12]
canon_parts: [3, 7, 10, 11]

cirs_relationship:
  surfaces_added: ["/mos:show"]
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["build-connector-registry", "command-resolver", "chain-executor runChain"]
  gate_impact: "Adds one WIRED surface (/mos:show) to the connector coverage ledger; re-runs build-connector-registry --check; no new gate check shipped."
  explanation: "Phase 173 ADDS one invocable surface, /mos:show, the JTBD need-selector front door. It is born-wired (R1/R2) with a connector: block on context_block (R3, reusing the existing reach -- no 7th reach minted, mirroring SENS-09's brain_consult reuse). The selector resolves each chosen job through the one governed path (R4): command-resolver -> runChain (Phase 166). It is USED BY CIRS (its surface is wired and counted in the coverage ledger) and USES CIRS (it reaches through the resolver spine). Per R12 this block + canon_parts 11 are gate-checked by scripts/check-cirs-declaration.cjs."

must_haves:
  truths:
    - "/mos:show renders 4 JTBD lanes via one AskUserQuestion call with zero command-name labels (R1)"
    - "the selector opens on the persona-default lane from defaultLaneForRoleBlend(role_blend) (R6)"
    - "a selected job resolves through command-resolver and is handed to runChain (R3)"
    - "the Phase 172 coverage gate passes /mos:show as WIRED with a conformant cirs_relationship block (R5/R12)"
    - "/mos:publish is unchanged and is the resolve target for the give-me-a-link job (D-02)"
  artifacts:
    - path: "commands/show.md"
      provides: "The /mos:show F.1 JTBD need-selector front door with a connector: block"
      contains: "connects_to_spine: true"
    - path: "data/connector-coverage-ledger.json"
      provides: "The regenerated coverage ledger listing /mos:show as WIRED, gap=0"
      contains: "show"
  key_links:
    - from: "commands/show.md"
      to: "lib/workflow/command-resolver.cjs"
      via: "job resolves_to resolved through the registry door, never from memory"
      pattern: "command-resolver|frameworksForCommand"
    - from: "commands/show.md"
      to: "lib/core/chain-executor.cjs"
      via: "resolved chain handed to runChain post-gate"
      pattern: "runChain|chain-executor"
    - from: "commands/show.md connector"
      to: "data/connector-coverage-ledger.json"
      via: "build-connector-registry generates the WIRED entry"
      pattern: "context_block"
---

<objective>
Ship the NEW `/mos:show` command (commands/show.md): the Shape F.1 JTBD need-selector front door (R1). It reads data/publish-needs.json, opens on the persona-default lane (R6 via the Wave-1 mapper), renders 4 JTBD lanes via ONE AskUserQuestion call with zero command-name labels, always exposes a free-text "something else" option (navigator standing preference), resolves the chosen job through the Phase 122 command-resolver, and hands the resolved chain to the Phase 166 runChain (R3/resolution path). It is born-wired under CIRS (R5/R12): a connector: block + the cirs_relationship declaration, passing the Phase 172 coverage gate as WIRED.

Purpose: Change the entry path from "user must know and type 8 command names" to "user names a job; the command stays hidden and runs" (Canon Part 10). /mos:publish stays UNCHANGED (D-02) and is the resolve target for the "give me a link" job.
Output: commands/show.md + the regenerated connector-registry / coverage-ledger / harness-manifest in lockstep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-publish-jtbd-need-selector/173-SPEC.md
@.planning/phases/173-publish-jtbd-need-selector/173-CONTEXT.md
@CLAUDE.md
@docs/CIRS-RELATIONSHIP-CONTRACT.md
@docs/CONNECTOR-CONTRACT.md
@commands/think-hats.md
@commands/publish.md
@scripts/help-renderer.cjs
@lib/workflow/command-resolver.cjs
@lib/core/chain-executor.cjs
@data/publish-needs.json
@lib/core/publish-needs-default-lane.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author commands/show.md -- the F.1 JTBD selector front door (R1) with the connector + cirs_relationship frontmatter (R5/R12)</name>
  <files>commands/show.md</files>
  <read_first>
    - commands/think-hats.md (the connector: frontmatter shape to mirror EXACTLY: the "# --- Phase 143.3 connector frontmatter ---" delimiter, connects_to_spine/sensor_triggers/reach_id/sub_mode/posture/hierarchy_rank/filing/plan_gated/web_scope/surface keys)
    - commands/publish.md (the UNCHANGED resolve target -- show.md routes the "give me a link" job here; D-02 says do NOT modify publish.md or overload it)
    - docs/CONNECTOR-CONTRACT.md sections 2-5 (the 11 sub-keys, the frozen 6 reach_ids, the additive-degrade rule, the Part-8 boundary)
    - docs/CIRS-RELATIONSHIP-CONTRACT.md (the cirs_relationship block is declared in the PLAN frontmatter, already present above; the command file carries the connector: block)
    - scripts/help-renderer.cjs (the F.1 lanes-as-tabs + options-as-jobs pattern the selector mirrors -- AskUserQuestion only, host owns the keymap, SEED-020 forbids a custom TUI)
    - data/publish-needs.json (the data the command body reads to populate lanes + jobs + resolves_to)
    - lib/core/publish-needs-default-lane.cjs (defaultLaneForRoleBlend -- the command opens on this lane)
    - skills/MOSDeckEngine/SKILL.md (the make-land routing target until Phase 175)
  </read_first>
  <action>Create commands/show.md as a new command (name: show; help_jtbd: "Name the job; I'll show or share your work."; serves_jtbd e.g. ["prepare-pitch","explore"]; body_shape: F.1; disable-model-invocation NOT set so Larry can route to it). The command body instructs Larry to: (1) read data/publish-needs.json; (2) read the active room USER.md role_blend and call defaultLaneForRoleBlend(role_blend) from lib/core/publish-needs-default-lane.cjs to pick the OPENING lane (R6); (3) render ONE AskUserQuestion Shape F.1 selector whose tabs are the 4 lane labels from publish-needs.json _lanes and whose options are the job labels (the user-voice jtbd_line / job fields) for that lane -- NEVER a /mos: token or command name (R1); (4) ALWAYS include the AskUserQuestion "Other" / free-text option ("something else") per the navigator standing preference; (5) on selection, look up the chosen job's resolves_to in publish-needs.json, resolve it through lib/workflow/command-resolver.cjs (frameworksForCommand / the registry door -- NEVER name a command from memory; D-03 the resolved object MUST come from the resolver), then hand the resolved chain to runChain in lib/core/chain-executor.cjs (auto-runs the autonomous_safe prefix, halts at the first material step per Canon Part 3); (6) for the make-land lane, route to the EXISTING MOSDeckEngine skill (D-01 -- do NOT build /mos:deck here; that is Phase 175). Add the connector: frontmatter block mirroring commands/think-hats.md: connects_to_spine: true; sensor_triggers: [SENS-SHOW] (the new sensor id Plan 03 registers); reach_id: context_block (REUSE the existing reach per D-03 -- mint NO 7th reach); sub_mode: jtbd-need-selector; framework: null (additive-degrade -- this is a selector front door, not a single-framework command, mirroring the Plan-16 framework:null presentation surfaces); posture: hold; hierarchy_rank: a free integer not colliding with shipped ranks (e.g. 18); filing: memory_event_only; plan_gated: false; web_scope: null; surface: F.1. NO fenced code blocks of implementation in the body -- directive prose only. NO em-dashes.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('commands/show.md','utf8');const fm=t.match(/^---\n([\s\S]*?)\n---/)[1];if(!/name:\s*show/.test(fm))throw new Error('missing name');if(!/connects_to_spine:\s*true/.test(fm))throw new Error('not wired');if(!/reach_id:\s*context_block/.test(fm))throw new Error('wrong reach');if(/em.dash|—/.test(t))throw new Error('em-dash');if(/disable-model-invocation:\s*true/.test(fm))throw new Error('selector must be Larry-routable');console.log('show.md frontmatter OK');"</automated>
  </verify>
  <acceptance_criteria>
    - commands/show.md exists with name: show, a connector: block declaring connects_to_spine: true and reach_id: context_block (the existing reach, no 7th minted), sub_mode: jtbd-need-selector, sensor_triggers: [SENS-SHOW].
    - The body directs: read publish-needs.json, open on defaultLaneForRoleBlend(role_blend), render ONE AskUserQuestion with 4 lanes + an "Other" free-text option and zero command-name labels, resolve via command-resolver, hand to runChain; make-land routes to MOSDeckEngine.
    - No em-dash anywhere; disable-model-invocation is NOT set (Larry can route to /mos:show).
  </acceptance_criteria>
  <done>/mos:show is a born-wired F.1 selector that resolves jobs through the one governed path; /mos:publish is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the connector registry + coverage ledger + harness manifest in lockstep; prove /mos:show passes the gate as WIRED (R5)</name>
  <files>data/connector-registry.json, data/connector-coverage-ledger.json, data/harness-manifest.json</files>
  <read_first>
    - scripts/build-connector-registry.cjs (the generator + --check tripwire; it walks commands/*.md connector: frontmatter and emits data/connector-registry.json + data/connector-coverage-ledger.json; --check exits non-zero on a stale on-disk registry)
    - data/connector-coverage-ledger.json (the current baseline: 88 wired / 36 excluded / 0 gap per STATE.md -- adding /mos:show as WIRED must keep gap=0 and bump wired to 89)
    - .planning/STATE.md (the Plan-16 baseline note: the ledger counts to match against)
    - tests/run-all-172.sh (the CIRS gate aggregator; build-connector-registry --check is leg (a) -- run it to prove /mos:show is WIRED, not a gap)
  </read_first>
  <action>Run `node scripts/build-connector-registry.cjs` to regenerate data/connector-registry.json + data/connector-coverage-ledger.json (and the harness-manifest if the generator emits it) from the commands frontmatter, picking up the new /mos:show connector block. Confirm the ledger now classifies /mos:show as WIRED (counts.wired increments by 1, counts.gap stays 0 -- NO regression). Then run `node scripts/build-connector-registry.cjs --check` to confirm the committed registry is not stale (exit 0). If the generator validates the connector framework against data/framework-names.json, confirm framework: null degrades cleanly (additive-degrade rule -- a null framework is a selector front door, not a framework command, mirroring the Plan-16 framework:null surfaces). Do NOT hand-edit any generated JSON; regenerate it.</action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check && node -e "const l=require('./data/connector-coverage-ledger.json');const has=JSON.stringify(l).includes('show');if(!has)throw new Error('/mos:show not in ledger');if((l.counts&&l.counts.gap)!==0)throw new Error('gap regression: '+(l.counts&&l.counts.gap));console.log('ledger OK, /mos:show wired, gap=0');"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-connector-registry.cjs --check` exits 0 (registry not stale).
    - data/connector-coverage-ledger.json lists /mos:show in the WIRED bucket; counts.gap is 0 (no regression from the 88-wired/0-gap baseline).
  </acceptance_criteria>
  <done>The Phase 172 coverage gate passes /mos:show as WIRED; the registry/ledger/manifest move in lockstep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator turn -> /mos:show | untrusted free-text job intent crosses here; the selector renders fixed lane/job options from publish-needs.json, the "Other" option captures free text Larry interprets and routes |
| /mos:show -> command-resolver | the resolved command MUST come from the registry door, never from model memory (D-03) |
| /mos:show -> Brain | NONE: the selector and the resolver make zero Brain calls (Part 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-01 | Spoofing | resolved command identity | mitigate | resolves_to is validated by 173-01 --check against the real command-registry; the selector resolves through command-resolver only, never names a command from memory |
| T-173-02 | Tampering | the connector coverage ledger | mitigate | the ledger is GENERATED by build-connector-registry.cjs, never hand-edited; --check fails CI on drift |
| T-173-03 | Information disclosure | user content -> Brain | accept | no path: the selector body reads LOCAL publish-needs.json + USER.md role_blend (enum/scalar) only; framework: null carries no Brain handle |
</threat_model>

<verification>
- `node scripts/build-connector-registry.cjs --check` exits 0.
- commands/show.md carries a conformant connector: block (context_block reach, no 7th reach) and the PLAN frontmatter carries the cirs_relationship block + canon_parts 11 (R12 gate-checkable).
- bash tests/run-all-172.sh still green (the new WIRED surface does not regress the CIRS gate).
- No em-dashes.
</verification>

<success_criteria>
- /mos:show is the born-wired F.1 JTBD selector (R1) resolving every job through command-resolver -> runChain (R3 resolution path), reusing the existing context_block reach (R3/D-03).
- The Phase 172 coverage gate accepts /mos:show as WIRED (R5); the cirs_relationship declaration is conformant (R12).
- /mos:publish is unchanged (D-02).
</success_criteria>

<output>
Create `.planning/phases/173-publish-jtbd-need-selector/173-02-SUMMARY.md` when done.
</output>
