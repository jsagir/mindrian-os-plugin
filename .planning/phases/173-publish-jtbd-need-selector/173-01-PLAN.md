---
phase: 173-publish-jtbd-need-selector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/publish-needs.json
  - scripts/check-publish-needs.cjs
  - lib/core/publish-needs-default-lane.cjs
  - tests/test-publish-needs-map.cjs
autonomous: true
requirements: [R2, R7, R6]
canon_parts: [2a, 3, 7, 10]

must_haves:
  truths:
    - "data/publish-needs.json exists; every job row maps to exactly one real resolves_to command/skill"
    - "every job row carries a non-empty shows tag of exactly 'connections' or 'gaps'"
    - "a job whose resolves_to is not a real command/skill fails the --check validator"
    - "a job lacking a shows tag fails the --check validator"
    - "defaultLaneForRoleBlend maps a founder-weighted role_blend to 'get-into-world' and a researcher-weighted to 'find-broken'"
  artifacts:
    - path: "data/publish-needs.json"
      provides: "The JTBD need->command map (job, jtbd_line, resolves_to, lane, persona_weight, shows)"
      contains: "resolves_to"
    - path: "scripts/check-publish-needs.cjs"
      provides: "--check validator: real resolves_to + non-empty shows enum per row"
      min_lines: 40
    - path: "lib/core/publish-needs-default-lane.cjs"
      provides: "defaultLaneForRoleBlend(roleBlend) -> lane id (role_blend dominant-role read, R6)"
      exports: ["defaultLaneForRoleBlend", "LANES", "DEFAULT_LANE"]
  key_links:
    - from: "scripts/check-publish-needs.cjs"
      to: "lib/workflow/command-resolver.cjs"
      via: "frameworksForCommand / command-registry membership check"
      pattern: "command-registry|frameworksForCommand|commands\\.find"
    - from: "lib/core/publish-needs-default-lane.cjs"
      to: "data/publish-needs.json LANES"
      via: "lane id set shared with the map"
      pattern: "find-broken|get-into-world|know-stand|make-land"
---

<objective>
Ship the two NET-NEW data contracts every later 173 plan consumes: the declarative `data/publish-needs.json` need->command map (R2) with the "show the unseen" admission tag (R7), its `--check` validator, and the `defaultLaneForRoleBlend` persona->lane mapper (R6). This is the interface-first wave: the selector command (173-02) and the trigger sensor (173-03) both read these artifacts, so they land first.

Purpose: A data-driven map (mirroring data/help-groups.json + data/dispatch-framework-map.json) keeps routing out of hardcode and lets the selector render lanes/jobs from one source of truth. The "show the unseen" tag enforces the moat (a job earns a slot only if it surfaces CONNECTIONS or GAPS). The role_blend mapper makes the opening lane persona-adaptive per Canon Part 2a.
Output: data/publish-needs.json, scripts/check-publish-needs.cjs, lib/core/publish-needs-default-lane.cjs, tests/test-publish-needs-map.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/173-publish-jtbd-need-selector/173-SPEC.md
@.planning/phases/173-publish-jtbd-need-selector/173-CONTEXT.md
@CLAUDE.md
@data/help-groups.json
@data/dispatch-framework-map.json
@lib/workflow/command-resolver.cjs
@lib/core/reverse-salient-persona-suffix.cjs
@lib/core/user-md-ops.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author data/publish-needs.json (R2) with the show-the-unseen tag (R7)</name>
  <files>data/publish-needs.json</files>
  <read_first>
    - data/help-groups.json (the lane idiom: groups carry lane: start|methodology|explore|view; _lanes labels block)
    - data/dispatch-framework-map.json (the flat data-map idiom publish-needs mirrors; _note convention)
    - .planning/ROADMAP.md Phase 173 "Design" block (the 4 lanes + the example job->command rows the navigator locked)
    - commands/graph.md, commands/radar.md, commands/publish.md (confirm the resolve-target command names exist exactly as /mos:graph, /mos:radar, /mos:publish)
    - skills/MOSDeckEngine/SKILL.md (the "Make it land" routing target until Phase 175; it IS the 6-stage Feynman pipeline skill)
  </read_first>
  <behavior>
    - Map has a top-level `version`, `_note` (mirroring dispatch-framework-map.json), a `_lanes` labels block keyed by the 4 lane ids, and a `jobs` array.
    - Exactly 4 lane ids, JTBD voice: `know-stand` ("Know where I stand"), `find-broken` ("Find what's broken"), `make-land` ("Make it land"), `get-into-world` ("Get it into the world").
    - Each `jobs[]` row carries: `job` (user-voice JTBD label, e.g. "show me how my pieces connect" -- NEVER a /mos: token or command name), `jtbd_line` (the "when X, I want Y, so I can Z" outcome phrasing from the Tavily research), `resolves_to` (the EXACT command name as keyed in data/command-registry.json, e.g. "/mos:graph"; for the make-land lane until Phase 175 the value is the skill handle "MOSDeckEngine"), `lane` (one of the 4 ids), `persona_weight` (a flat enum->scalar map naming which role_blend roles favor this job; generic enum keys only, Part 8-clean), and `shows` (exactly "connections" or "gaps").
    - Seed the locked navigator example rows at minimum: "show me how my pieces connect" -> /mos:graph (lane know-stand, shows connections); "show me where I'm weakest" -> /mos:radar (lane find-broken, shows gaps); "make this simple enough for an outsider" -> MOSDeckEngine (lane make-land, shows gaps); "give me a link I can send" -> /mos:publish (lane get-into-world, shows connections). Add the remaining shipped visual surfaces (dashboard, wiki, present, export, snapshot) as job rows ONLY where the job genuinely surfaces connections or gaps (the R7 admission bar) -- a decoration-only candidate is OMITTED, not slotted.
    - Every job label is solution-agnostic user-voice: zero label contains "/mos:" or a bare command name (R1 acceptance the selector later asserts against this data).
  </behavior>
  <action>Create data/publish-needs.json mirroring the data/dispatch-framework-map.json flat-map idiom and the data/help-groups.json lane idiom. Populate the `_lanes` block with the 4 JTBD lane labels and the `jobs` array with the row schema { job, jtbd_line, resolves_to, lane, persona_weight, shows } described in the behavior block. resolves_to values MUST be the exact command names as keyed in data/command-registry.json (with the /mos: prefix) or the "MOSDeckEngine" skill handle for the make-land lane (D-01: route to the EXISTING deck skill until Phase 175). Enforce the R7 admission bar in the data: every row's shows is "connections" or "gaps"; omit any decoration-only candidate. Do NOT phrase any job label as a command name (Part 10 commands-are-internals).</action>
  <verify>
    <automated>node -e "const m=require('./data/publish-needs.json'); const ok=Array.isArray(m.jobs)&&m.jobs.length>=4&&m.jobs.every(j=>(j.shows==='connections'||j.shows==='gaps')&&!/\/mos:|command/i.test(j.job)&&typeof j.resolves_to==='string'&&['know-stand','find-broken','make-land','get-into-world'].includes(j.lane)); if(!ok){console.error('publish-needs.json shape invalid');process.exit(1);} console.log('publish-needs.json OK',m.jobs.length,'jobs');"</automated>
  </verify>
  <acceptance_criteria>
    - data/publish-needs.json parses; jobs.length >= 4; every row.shows is "connections" or "gaps"; no job label matches /\/mos:|command/i; every row.lane is one of the 4 frozen lane ids; every row.resolves_to is a non-empty string.
  </acceptance_criteria>
  <done>The map exists, is data-driven, every job surfaces connections or gaps, and no job label leaks a command name.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship the --check validator (R2/R7 enforcement) + the role_blend->lane mapper (R6)</name>
  <files>scripts/check-publish-needs.cjs, lib/core/publish-needs-default-lane.cjs, tests/test-publish-needs-map.cjs</files>
  <read_first>
    - lib/workflow/command-resolver.cjs (frameworksForCommand + the command-registry read; the validator checks resolves_to membership the same deterministic, read-only, no-Brain way)
    - data/command-registry.json (the command name keying: entries are { command: "/mos:graph", ... }; the validator matches resolves_to against these command strings)
    - lib/core/reverse-salient-persona-suffix.cjs (the EXACT dominant-role-from-role_blend precedent: canonical-key filter, positive-weight, sorted lex tie-break, default fallback -- the mapper mirrors this control flow)
    - lib/core/user-md-ops.cjs readUserMd (the canonical USER.md reader returning { role_blend } -- the mapper takes a role_blend object, NOT a path, so the consumer reads USER.md and passes role_blend in)
    - tests/test-diffusion-adoption-sensor.cjs (the node test idiom: assert/ok helper, exit-non-zero on fail)
  </read_first>
  <behavior>
    - check-publish-needs.cjs validates data/publish-needs.json: (1) every job.resolves_to that begins with "/mos:" is a real command in data/command-registry.json; a resolves_to skill handle (e.g. "MOSDeckEngine") is validated against the skills/ directory presence; (2) every job carries a shows value of exactly "connections" or "gaps"; (3) no job label contains a /mos: token. Exits 0 when clean, exits 1 with a one-line stderr reason on any violation. Mirrors the build-connector-registry.cjs --check exit-code discipline. Zero Brain, zero network (Part 8).
    - publish-needs-default-lane.cjs exports defaultLaneForRoleBlend(roleBlend) -> one of the 4 lane ids, plus LANES (the frozen 4-id list) and DEFAULT_LANE ('know-stand'). Mapping per the SPEC R6: a founder-dominant role_blend -> 'get-into-world'; researcher-dominant -> 'find-broken'; investor-dominant -> 'find-broken' (thesis/red-team is a find-what's-broken job); any other / cold-start / empty / all-zero -> DEFAULT_LANE ('know-stand'). Dominant-role selection mirrors reverse-salient-persona-suffix.suffixFor verbatim (canonical-key filter, strict-greater positive-weight walk over sorted keys, default on no winner). Never throws.
    - The test asserts: a clean map passes --check (exit 0); a mutated fixture with a fake resolves_to fails (exit 1); a mutated fixture with a missing shows fails (exit 1); defaultLaneForRoleBlend({founder:0.7,researcher:0.3}) === 'get-into-world'; defaultLaneForRoleBlend({researcher:0.8,founder:0.2}) === 'find-broken'; defaultLaneForRoleBlend({}) === 'know-stand'; defaultLaneForRoleBlend(null) === 'know-stand'.
  </behavior>
  <action>Create scripts/check-publish-needs.cjs as a read-only LOCAL validator over data/publish-needs.json, matching resolves_to /mos: values against data/command-registry.json command strings and skill handles against skills/ directory presence, enforcing the shows enum and the no-command-name-in-label rule, exiting 1 on any violation (mirror build-connector-registry.cjs --check exit discipline). Create lib/core/publish-needs-default-lane.cjs exporting defaultLaneForRoleBlend(roleBlend), LANES, and DEFAULT_LANE, with the founder->get-into-world / researcher->find-broken / investor->find-broken / fallback->know-stand mapping, reusing the reverse-salient-persona-suffix.cjs dominant-role control flow (canonical-key filter, positive-weight strict-greater walk over sorted keys, default on no winner). Create tests/test-publish-needs-map.cjs asserting the seven behaviors above, using a tmp-dir mutated fixture for the two --check failure cases.</action>
  <verify>
    <automated>node scripts/check-publish-needs.cjs && node tests/test-publish-needs-map.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-publish-needs.cjs` exits 0 on the committed map.
    - `node tests/test-publish-needs-map.cjs` passes: the two mutated fixtures each exit 1 from the validator; defaultLaneForRoleBlend returns 'get-into-world' for founder-dominant, 'find-broken' for researcher-dominant, and 'know-stand' for empty/null.
  </acceptance_criteria>
  <done>The map is gate-validated (R2/R7 enforced) and the persona->lane mapper (R6) is shipped and tested.</done>
</task>

</tasks>

<verification>
- `node scripts/check-publish-needs.cjs` exits 0.
- `node tests/test-publish-needs-map.cjs` passes all assertions.
- No em-dashes in any new file; all new files are CJS / JSON (no TypeScript, no build step).
</verification>

<success_criteria>
- data/publish-needs.json is the single data-driven source for the selector's lanes + jobs, with every job admitted only by the connections|gaps bar (R7).
- The --check validator proves every resolves_to is a real command/skill (R2).
- defaultLaneForRoleBlend implements the persona-adaptive default lane (R6) reusing the shipped dominant-role read.
</success_criteria>

<output>
Create `.planning/phases/173-publish-jtbd-need-selector/173-01-SUMMARY.md` when done.
</output>
