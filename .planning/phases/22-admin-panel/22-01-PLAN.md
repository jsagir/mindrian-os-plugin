---
phase: 22-admin-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/admin.md
  - commands/help.md
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03]
must_haves:
  truths:
    - "Running /mos:admin as admin user shows a self-teaching overview panel with current Brain key state and numbered actions"
    - "Running /mos:admin as non-admin user gets a 3-line error (not found)"
    - "/mos:help does not show /mos:admin for non-admin users; /mos:help with --all shows it in an Admin group"
    - "Every destructive subcommand (revoke) shows consequences and asks for confirmation before calling brain-admin.cjs"
    - "Every invocation re-explains what each action does -- no separate help mode"
  artifacts:
    - path: "commands/admin.md"
      provides: "Hidden admin command wrapping brain-admin.cjs"
      min_lines: 120
    - path: "commands/help.md"
      provides: "Updated help with admin visibility filtering"
      contains: "MOS_ADMIN"
  key_links:
    - from: "commands/admin.md"
      to: "mcp-server-brain/brain-admin.cjs"
      via: "Bash tool calls (node brain-admin.cjs subcommand)"
      pattern: "brain-admin\\.cjs"
    - from: "commands/help.md"
      to: "MOS_ADMIN env var"
      via: "Environment check for admin visibility"
      pattern: "MOS_ADMIN"
---

<objective>
Create the `/mos:admin` hidden command and add admin visibility filtering to `/mos:help`.

Purpose: Jonathan gets a self-teaching admin panel for Brain API key management that wraps the existing brain-admin.cjs CLI, hidden from non-admin users.
Output: `commands/admin.md` (new), `commands/help.md` (modified with admin filtering)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-admin-panel/22-CONTEXT.md

@commands/status.md
@commands/help.md
@mcp-server-brain/brain-admin.cjs
@skills/ui-system/SKILL.md
</context>

<interfaces>
<!-- Key patterns the executor needs from existing files -->

From commands/status.md (pattern for command frontmatter -- note the YAML fields):
  name: status
  description: See where your project stands
  body_shape: A (Mondrian Board)
  ui_reference: skills/ui-system/SKILL.md
  allowed-tools: [Read, Glob, Bash]

From mcp-server-brain/brain-admin.cjs (available CLI commands):
  Commands: create, revoke, extend, list, usage, requests, help
  Usage: node brain-admin.cjs <command> [--flags]

  create  --email X --name "Name" [--days N] [--plan free|pro|admin]
  revoke  --email X
  extend  --email X --days N
  list    (no flags)
  usage   [--email X]
  requests (no flags)

From skills/ui-system/SKILL.md (admin shape assignment):
  /mos:admin uses Shape A: Mondrian Board (Admin panel, hidden)

From commands/help.md (existing admin group pattern in --all mode):
  The help command already has an "Admin (owner only)" group at the bottom
  with /mos:admin listed. This needs conditional rendering.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/admin.md -- self-teaching admin command</name>
  <files>commands/admin.md</files>
  <action>
Create `commands/admin.md` following the established command file pattern (frontmatter + markdown instructions).

**Frontmatter** (per D-09, D-10): Use the same YAML frontmatter pattern as status.md but add `visibility: admin`. Fields: name (admin), description (Brain API key management -- hidden admin panel), body_shape (A Mondrian Board), ui_reference (skills/ui-system/SKILL.md), visibility (admin), allowed-tools (Read, Bash, Glob).

Note the `visibility: admin` field -- this is the machine-readable flag that help.md will check. Per D-01, this command does not appear in help for non-admin users.

**Admin Identity Check** (per D-02):
Step 1 of the command: Check environment for admin identity. Check `MOS_ADMIN=true` in environment variables. If not set, also check for Jonathan's identity markers: username contains "jsagi" or "jonathan", or home directory matches `/home/jsagi`. If neither condition is met, render the 3-line error format:
```
✗ Command not found: admin
  Why: Not an admin user
  Fix: /mos:help
```
Then STOP.

**Self-Teaching Overview** (per D-03, D-05, D-06):
When `/mos:admin` is run with NO subcommand, render the full 4-zone self-teaching panel:

Zone 1 -- Header Panel:
```
╭─ ADMIN PANEL ── Brain API Management ──────────────────╮
│                                                          │
```

Zone 2 -- Content Body (Shape A: Mondrian Board variant for admin):
First, gather live data by running these Bash commands:
```bash
node "${CLAUDE_PLUGIN_ROOT}/mcp-server-brain/brain-admin.cjs" list 2>&1
node "${CLAUDE_PLUGIN_ROOT}/mcp-server-brain/brain-admin.cjs" requests 2>&1
```

Parse the output to extract: active key count, pending request count, total requests this month (from usage data). Per D-13, this verified data is gathered BEFORE rendering -- never hallucinate counts.

Then render the cockpit overview (per D-14, structural display not narration):
```
  What This Panel Does:
  Manage who can access the MindrianOS Brain API. Create time-limited
  keys, monitor usage, review access requests, revoke access instantly.

  Current State:
  ├─ Active keys     [N]
  ├─ Pending requests [N]
  └─ Total requests   [N] this month

  Actions:
  ├─ 1. keys         List all API keys with status and expiry
  ├─ 2. approve      Create a key for someone (guided walkthrough)
  ├─ 3. revoke       Deactivate all keys for an email (destructive)
  ├─ 4. extend       Add days to an existing key's expiry
  ├─ 5. usage        Show request counts per key
  └─ 6. requests     Review pending access requests

  Each action explains itself before executing. Destructive actions
  show consequences and ask for confirmation.
```

Zone 4 -- Action Footer:
```
  ▶ /mos:admin keys              See all active keys
  ▷ /mos:admin requests          Check pending access requests
  ▷ /mos:admin usage             View usage statistics
```

**Subcommand: keys** (per D-07, D-08):
Run `node "${CLAUDE_PLUGIN_ROOT}/mcp-server-brain/brain-admin.cjs" list` via Bash.
Display the raw output inside a Zone 2 body. The brain-admin.cjs output is already well-formatted with its own table. Wrap it in 4-zone anatomy (header + output + footer).

**Subcommand: approve <email>** (per D-04, D-07):
Self-teaching walkthrough:
1. Explain: "Creating a Brain API key gives [email] access to query the MindrianOS knowledge graph. The key expires after N days (default: 30). Plan tiers: free (read-only), pro (read+write), admin (full access)."
2. If no email provided, show error: "Usage: /mos:admin approve user@example.com"
3. Run `node "${CLAUDE_PLUGIN_ROOT}/mcp-server-brain/brain-admin.cjs" create --email [email] --name "[name]" --days [days] --plan [plan]` via Bash.
4. Show the result. Remind: "Save this key -- it will not be shown again."

**Subcommand: revoke <email>** (per D-04, D-11, D-12, ADMIN-03):
Self-teaching + destructive action protection:
1. First, run `node "${CLAUDE_PLUGIN_ROOT}/mcp-server-brain/brain-admin.cjs" list` to show current keys for context.
2. Show consequence panel: "This will immediately block [email] from accessing Brain. They will get a 401 error on next request. This cannot be undone without creating a new key."
3. Ask for inline confirmation: "Revoke all keys for [email]? (yes/no)" -- use inline prompt, NOT AskUserQuestion per D-12.
4. If confirmed, run `node "${CLAUDE_PLUGIN_ROOT}/mcp-server-brain/brain-admin.cjs" revoke --email [email]` via Bash.
5. Show updated state after action.

**Subcommand: extend <email> <days>** (per D-04):
1. Explain: "Extending adds [days] days to [email]'s current expiry. If already expired, extends from today."
2. Run `node "${CLAUDE_PLUGIN_ROOT}/mcp-server-brain/brain-admin.cjs" extend --email [email] --days [days]` via Bash.
3. Show result with old and new expiry.

**Subcommand: usage** (per D-07):
Run `node "${CLAUDE_PLUGIN_ROOT}/mcp-server-brain/brain-admin.cjs" usage` via Bash. Wrap output in 4-zone anatomy.

**Subcommand: requests** (per D-07):
1. Run `node "${CLAUDE_PLUGIN_ROOT}/mcp-server-brain/brain-admin.cjs" requests` via Bash.
2. If pending requests exist, show each with name, email, message, and submitted date.
3. Footer suggests: `/mos:admin approve <email>` for the first pending request.

**Voice and formatting rules:**
- Follow ui-system SKILL.md 4-zone anatomy throughout (per D-09)
- Use standard box-drawing card style (per D-10)
- Use only the 5-color contract and 12 glyphs -- no special admin colors (per D-10)
- NO EMOJI
- Per D-05, self-teaching text is integrated into the flow, not a separate help mode
- Every subcommand starts with a brief explanation of what it does BEFORE acting (per D-04, ADMIN-02)
  </action>
  <verify>
    <automated>grep -c "visibility: admin" commands/admin.md && grep -c "brain-admin.cjs" commands/admin.md && grep -c "MOS_ADMIN" commands/admin.md</automated>
  </verify>
  <done>
- commands/admin.md exists with visibility: admin frontmatter
- Admin identity check gates access via MOS_ADMIN env var or Jonathan's identity markers
- All 6 subcommands documented (keys, approve, revoke, extend, usage, requests)
- No-subcommand invocation shows self-teaching overview with live data
- Revoke shows consequence panel and requires confirmation
- All output follows 4-zone anatomy with Shape A variant
- All 14 locked decisions (D-01 through D-14) honored
  </done>
</task>

<task type="auto">
  <name>Task 2: Update commands/help.md -- admin visibility filtering</name>
  <files>commands/help.md</files>
  <action>
Modify `commands/help.md` to add admin visibility filtering. Per D-01, the `/mos:admin` command must NOT appear in help output for non-admin users. It must not be locked, not greyed out -- it simply does not exist.

**Changes to make:**

1. **Add admin detection step** after Step 1 (Determine Venture Stage):

Add a new section "Step 1.5: Check Admin Visibility" between Step 1 and Step 2:

"Check if the current user is an admin. Check for MOS_ADMIN=true environment variable. Alternatively, check if username contains 'jsagi' or 'jonathan'. Set an internal flag is_admin for use in rendering."

2. **Modify Step 3 (Default Behavior)** -- Zone 2 tree rendering:

In the "Export + Admin" group, wrap the admin entry in a conditional:

The existing group shows Export + Admin. Change it so that **if is_admin is true**, add at the bottom of the tree an "Admin (owner only)" group with /mos:admin. **If is_admin is false**, do NOT render the Admin group at all. No trace of /mos:admin should appear.

3. **Modify Step 5 (with --all flag):**

The existing --all section already mentions the Admin group. Add the same conditional: if is_admin is true, include the Admin group. If false, omit it entirely. The --all flag shows all commands the USER has access to, not all commands that exist.

4. **Modify Step 4 (per-command help):**

Add a guard: if the user runs `/mos:help admin` and is_admin is false, render the standard unknown command error:
```
✗ Unknown command: admin
  Why: No matching /mos: command found
  Fix: /mos:help
```

This ensures the command is truly invisible -- even asking about it reveals nothing per D-01.

5. **Add frontmatter visibility scanning:**

In the rendering logic preamble, add a note about the `visibility` frontmatter field: When listing commands, check each command file's YAML frontmatter for a `visibility` field. If `visibility: admin` is set and the current user is NOT admin, skip that command entirely. This makes the filtering generic -- future hidden commands just need `visibility: admin` in frontmatter.

Do NOT change the existing flow groupings (Getting Started, Working, Reviewing, Brain + Intelligence, Export + Admin). The Admin group is simply appended when admin, omitted when not.
  </action>
  <verify>
    <automated>grep -c "is_admin" commands/help.md && grep -c "visibility" commands/help.md && grep -c "MOS_ADMIN" commands/help.md</automated>
  </verify>
  <done>
- help.md checks MOS_ADMIN or username for admin identity
- Default /mos:help hides admin commands for non-admin users
- /mos:help --all hides admin commands for non-admin users
- /mos:help admin returns "unknown command" for non-admin users
- Filtering is generic via visibility frontmatter field (not hardcoded to "admin" command name)
- No trace of /mos:admin visible to non-admin users in any help variant
  </done>
</task>

</tasks>

<verification>
1. Read commands/admin.md -- confirm frontmatter has visibility: admin, body_shape: A
2. Read commands/help.md -- confirm MOS_ADMIN check and visibility filtering logic
3. Verify admin.md references brain-admin.cjs for ALL subcommands (wrapper, not reimplementation per D-08)
4. Verify revoke subcommand has consequence text matching D-11
5. Verify no-subcommand invocation gathers live data BEFORE rendering (D-13)
6. Verify no emoji anywhere in admin.md
7. Verify 4-zone anatomy pattern in admin.md output examples
</verification>

<success_criteria>
- commands/admin.md created with all 6 subcommands wrapping brain-admin.cjs
- commands/help.md updated with generic admin visibility filtering
- Self-teaching UX: every invocation explains before acting (ADMIN-02)
- Destructive actions show consequences + require confirmation (ADMIN-03)
- Hidden from non-admin: no trace in help, unknown command error if probed (ADMIN-01)
- All 14 locked decisions (D-01 through D-14) implemented
- Follows 4-zone anatomy and Shape A from ui-system SKILL.md
</success_criteria>

<output>
After completion, create `.planning/phases/22-admin-panel/22-01-SUMMARY.md`
</output>
