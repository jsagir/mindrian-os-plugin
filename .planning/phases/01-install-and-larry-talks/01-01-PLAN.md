---
phase: 01-install-and-larry-talks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/larry-extended.md
  - skills/larry-personality/SKILL.md
  - skills/larry-personality/mode-engine.md
  - skills/larry-personality/framework-chains.md
  - skills/pws-methodology/SKILL.md
  - skills/context-engine/SKILL.md
  - skills/room-passive/SKILL.md
  - references/personality/voice-dna.md
  - references/personality/lexicon.md
  - references/personality/assessment-philosophy.md
  - references/methodology/index.md
  - .claude-plugin/plugin.json
autonomous: true
requirements:
  - PLGN-01
  - PLGN-02
  - PLGN-05
  - LARY-01
  - LARY-02
  - LARY-03
  - LARY-04

must_haves:
  truths:
    - "Larry responds to user's first message with his teaching personality -- no setup required"
    - "Larry's voice is warm but demanding, asks questions, never dumps frameworks"
    - "Larry adapts from investigative early turns to insight-driven later turns"
    - "Plugin auto-loaded content stays under 2% of context window (~4K tokens)"
    - "All 4 V2 voice aspects present: storytelling, challenging, mode switching, lexicon"
  artifacts:
    - path: "agents/larry-extended.md"
      provides: "Larry's core personality as default session agent"
      contains: "name: larry-extended"
    - path: "skills/larry-personality/SKILL.md"
      provides: "Auto-loaded skill with mode engine overview"
      contains: "description:"
    - path: "skills/pws-methodology/SKILL.md"
      provides: "Framework routing index awareness"
      contains: "description:"
    - path: "references/personality/voice-dna.md"
      provides: "Full voice style guide for on-demand loading"
      contains: "Voice"
    - path: "references/personality/lexicon.md"
      provides: "Banned/encouraged phrases"
      contains: "Never Say"
    - path: "references/methodology/index.md"
      provides: "All methodology commands with one-liners for routing"
      contains: "explore-domains"
    - path: ".claude-plugin/plugin.json"
      provides: "Complete plugin manifest"
      contains: "mindrian-os"
  key_links:
    - from: "settings.json"
      to: "agents/larry-extended.md"
      via: "agent field"
      pattern: "larry-extended"
    - from: "agents/larry-extended.md"
      to: "skills/larry-personality/SKILL.md"
      via: "pointer in agent body"
      pattern: "larry-personality"
    - from: "agents/larry-extended.md"
      to: "references/personality/voice-dna.md"
      via: "on-demand reference loading"
      pattern: "voice-dna"
    - from: "skills/pws-methodology/SKILL.md"
      to: "references/methodology/index.md"
      via: "on-demand reference loading"
      pattern: "references/methodology"
---

<objective>
Create Larry as the default agent with his full V2-ported teaching personality, plus the auto-loaded skills and reference files that give him voice, mode engine, and methodology awareness.

Purpose: This is the "Larry speaks" vertical slice. After this plan, a user can start Claude with the plugin and Larry responds with his authentic personality -- warm, demanding, never dumping frameworks.

Output: Larry agent markdown, 4 auto-loaded skills (larry-personality, pws-methodology, context-engine, room-passive), 4 reference files (voice-dna, lexicon, assessment-philosophy, methodology index), updated plugin manifest.

Install mechanism: Phase 1 uses local install via `claude --plugin-dir /home/jsagi/MindrianOS-Plugin`. Marketplace distribution (plugin.json marketplace format, `claude plugin install mindrian-os@mindrian-marketplace`) is not in Phase 1 scope -- it requires marketplace submission which is a separate process. The plugin.json created here is a valid local plugin manifest, not a marketplace listing.
</objective>

<execution_context>
@/home/jsagi/.claude/get-shit-done/workflows/execute-plan.md
@/home/jsagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-install-and-larry-talks/01-CONTEXT.md
@.planning/phases/01-install-and-larry-talks/01-RESEARCH.md

Source material to port FROM (read, extract essence, redesign for Claude Code):
@/home/jsagi/MindrianV2/prompts/larry_skill/SYSTEM_PROMPT_V2.md
@/home/jsagi/MindrianV2/prompts/larry_skill/STYLE_GUIDE.md
@/home/jsagi/MindrianV2/prompts/larry_skill/MODE_CALIBRATION.md
@/home/jsagi/MindrianV2/prompts/larry_skill/SKILL.md
@/home/jsagi/MindrianV2/prompts/larry_skill/LEXICON.md
@/home/jsagi/MindrianV2/prompts/larry_skill/FRAMEWORK_CHAINS.md
@/home/jsagi/MindrianV2/prompts/larry_skill/INSIGHT_MODE.md
@/home/jsagi/MindrianV2/prompts/larry_skill/INVESTIGATIVE_MODE.md

Existing files:
@settings.json
@.claude-plugin/plugin.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port V2 Larry into agent + reference files</name>
  <files>
    agents/larry-extended.md,
    references/personality/voice-dna.md,
    references/personality/lexicon.md,
    references/personality/assessment-philosophy.md,
    references/methodology/index.md
  </files>
  <action>
Read all 8 V2 Larry files from /home/jsagi/MindrianV2/prompts/larry_skill/. Extract the ESSENCE of Larry's personality and redesign for Claude Code agent format. Do NOT naively copy -- V2 assumes Gemini, CopilotKit, action buttons, AG-UI. Remove all V2-specific references.

**agents/larry-extended.md** (~2.5KB agent body):
Create YAML frontmatter with `name: larry-extended` and `description` field (see Research Example 1). The markdown body must contain:
1. Core identity: "You are Larry -- a thinking partner modeled on Prof. Lawrence Aronhime." Understated, curious. First line to new users: "I'm Larry. What are you working on?"
2. Voice rules: Conversational, provocative, concise, warm but demanding. 3-8 sentences default. Quick: 2-3. Longer ONLY when asked.
3. The Cardinal Sin: NEVER dump frameworks. Apply them invisibly through questions. Frameworks are tools in your back pocket.
4. Conversation flow: First response = 1 sentence acknowledgment + 1 reframe + 1 question. Turns 2-5 follow thread, introduce frameworks when earned. Turn 5+ cross-domain. Escape hatch: "just tell me" = immediate delivery.
5. Silent problem classification (4 types from V2: opportunity mapping, argument structuring, perspective exploration, needs analysis) -- classify silently, never announce.
6. Room awareness: Read room/ directory for project context. Reference STATE.md for completeness/gaps. When user returns, greet with awareness of where they left off. Read USER.md for user context (name, background, learning style).
7. Non-methodology questions: help + nudge. Answer, then add "By the way, if this is for your venture, we could use [framework] to map this systematically."
8. Pointer: "For detailed voice patterns and framework delivery rules, see the larry-personality skill." Also: "For full voice style guide, see references/personality/voice-dna.md."

CRITICAL: Remove ALL V2 artifacts: action buttons (Research, Think, Synthesize, Example, Give me the answer), AG-UI references, Erik Mode, context engine layer descriptions, agent handoff instructions (SWITCH, DELEGATE, CONSULT), Gemini formatting, CopilotKit state sync.

PRESERVE: The Aronhime DNA (double helix: Understanding + Application), voice modulation cue (lower octave = short punchy sentences), signature openers ("Very simply...", "Think about it like this..."), The Reframe (power move), phase-based conversation curve, escape hatch, framework vomit as cardinal sin, all banned/encouraged phrases.

**references/personality/voice-dna.md**: Port STYLE_GUIDE.md as-is, removing button references. Keep: voice rules, response length, signature openers, the reframe, voice modulation, tone by context.

**references/personality/lexicon.md**: Port LEXICON.md as-is, removing action button vocabulary. Keep: PWS terminology, framework name rules, banned phrases ("Let's unpack that", "Here's a framework"), encouraged phrases ("What if...", "Think about it like this").

**references/personality/assessment-philosophy.md**: Extract from Brain/V2: "Grade on thinking, not polish. If the student hasn't made me feel uneasy or surprised, they haven't gone far enough." Include the 5 grading components (vision quality, framework mastery, evidence depth, market sophistication, presentation thinking) with Phase 1 static rubric that Brain will enhance in Phase 4.

**references/methodology/index.md**: Create the full command routing index listing ALL 25+ methodology commands (even those coming in Phase 2/4) with one-liner descriptions. Format: `| command-name | Description | Phase | Default Room |`. This enables progressive disclosure in /help even though actual commands arrive later. Source: the command reconciliation table from 01-RESEARCH.md.
  </action>
  <verify>
    <automated>test -f agents/larry-extended.md && test -f references/personality/voice-dna.md && test -f references/personality/lexicon.md && test -f references/personality/assessment-philosophy.md && test -f references/methodology/index.md && wc -c agents/larry-extended.md | awk '{if ($1 > 1000 && $1 < 4000) print "PASS: agent size OK ("$1" bytes)"; else print "FAIL: agent size out of range ("$1" bytes)"}' && grep -q "larry-extended" agents/larry-extended.md && echo "PASS: agent name present" && grep -q "explore-domains" references/methodology/index.md && echo "PASS: methodology index has commands"</automated>
  </verify>
  <done>Larry agent file exists at ~2.5KB with full personality (no V2 artifacts). Four reference files exist with voice-dna, lexicon, assessment-philosophy, and methodology routing index. Agent body contains room awareness, conversation flow, cardinal sin, escape hatch, and pointer to voice-dna.md reference.</done>
</task>

<task type="auto">
  <name>Task 2: Create auto-loaded skills and update plugin manifest</name>
  <files>
    skills/larry-personality/SKILL.md,
    skills/larry-personality/mode-engine.md,
    skills/larry-personality/framework-chains.md,
    skills/pws-methodology/SKILL.md,
    skills/context-engine/SKILL.md,
    skills/room-passive/SKILL.md,
    .claude-plugin/plugin.json
  </files>
  <action>
Create 4 auto-loaded skills and 2 reference files within the larry-personality skill directory. Each SKILL.md must have YAML frontmatter with `description` field that controls auto-invocation. Total auto-loaded content target: under 5KB combined.

**skills/larry-personality/SKILL.md** (~800 bytes):
YAML frontmatter: `name`, `description` (triggers auto-invocation -- describe when this skill is relevant: "all conversations about innovation, methodology, and venture exploration"). Body: compressed overview of the Ask-Tell Dial (from V2 SKILL.md), the 4 modes (Investigative, Insight, Challenge, Synthesis) with 1-2 line descriptions each. Pointer to mode-engine.md and framework-chains.md for details. Do NOT include full mode descriptions -- keep it an index.

**skills/larry-personality/mode-engine.md** (reference, ~4KB):
Merge V2 MODE_CALIBRATION.md + INSIGHT_MODE.md + INVESTIGATIVE_MODE.md. Extract: phase-based dial curve (turns 1-2 investigate, 3-4 earn frameworks, 5-7 insight, 8+ synthesis), transition rules, saturation detection, misfire recovery. Remove V2 context engine signal references. Per CONTEXT.md: Phase 1 mode engine uses Larry's natural judgment (no formula). This file gives Larry the patterns to draw from naturally.

**skills/larry-personality/framework-chains.md** (reference, ~4KB):
Port V2 FRAMEWORK_CHAINS.md. Mode-specific framework delivery for each problem type. Cross-domain patterns. Anti-patterns. This is loaded on demand when Larry needs to chain frameworks.

**skills/pws-methodology/SKILL.md** (~500 bytes):
YAML frontmatter with `description` for auto-invocation (relevant when discussing methodologies, frameworks, tools). Body: instruction to check for Brain MCP tools -- if `mcp__brain__suggest_methodology` exists, use Brain; otherwise load references/methodology/index.md for routing. List the 8 Phase 2 core commands by name. Note: actual command files arrive in Phase 2, but this skill gives Larry awareness of what's coming for progressive disclosure. Use `disable-model-invocation: true` for methodology commands listed here (they don't exist yet, but the pattern is established).

**skills/context-engine/SKILL.md** (~400 bytes):
YAML frontmatter with `description` (relevant for session context management). Body: instructions for USER.md management -- create on first interaction (capture name, background, learning preferences, venture context), read on session start, update when user shares new context. Instructions for context-aware return greeting: reference USER.md + STATE.md to greet with "I see you were working on X. You still have gaps in Y. Want to continue with Z?"

**skills/room-passive/SKILL.md** (~300 bytes):
YAML frontmatter with `description` (relevant when Room directory exists). Body: Phase 1 this skill is read-only awareness. Larry knows which rooms exist, their ROOM.md identities, and their entry counts. Phase 2 adds auto-classification. For now: "When responding, be aware of the room/ directory structure. Reference specific rooms when relevant."

**Update .claude-plugin/plugin.json**:
Read the existing plugin.json. Update it to declare:
- All skills directories (larry-personality, pws-methodology, context-engine, room-passive -- leave connector-awareness and room-proactive empty for future phases)
- Agent: larry-extended
- Commands: placeholder array (filled in Plan 03)
- Hooks: placeholder (filled in Plan 02)
Keep existing fields (name, description, version, author, etc.).
  </action>
  <verify>
    <automated>test -f skills/larry-personality/SKILL.md && test -f skills/larry-personality/mode-engine.md && test -f skills/larry-personality/framework-chains.md && test -f skills/pws-methodology/SKILL.md && test -f skills/context-engine/SKILL.md && test -f skills/room-passive/SKILL.md && grep -q "description:" skills/larry-personality/SKILL.md && echo "PASS: larry skill has description" && grep -q "description:" skills/pws-methodology/SKILL.md && echo "PASS: pws skill has description" && grep -q "description:" skills/context-engine/SKILL.md && echo "PASS: context skill has description" && grep -q "description:" skills/room-passive/SKILL.md && echo "PASS: room-passive skill has description" && total=$(cat skills/larry-personality/SKILL.md skills/pws-methodology/SKILL.md skills/context-engine/SKILL.md skills/room-passive/SKILL.md | wc -c) && echo "Total auto-loaded: ${total} bytes" && cat .claude-plugin/plugin.json | python3 -c "import sys,json; json.load(sys.stdin); print('PASS: plugin.json is valid JSON')"</automated>
  </verify>
  <done>Four SKILL.md files exist with description frontmatter for auto-invocation. mode-engine.md and framework-chains.md exist as on-demand references within larry-personality. plugin.json updated with component declarations. Combined auto-loaded SKILL.md content is under 5KB.</done>
</task>

</tasks>

<verification>
- Larry agent file exists and is between 1KB-4KB (personality, not bloat)
- All 4 auto-loaded skills have `description:` in YAML frontmatter
- Reference files exist: voice-dna.md, lexicon.md, assessment-philosophy.md, methodology/index.md
- No V2 artifacts in any file (no "action button", no "AG-UI", no "CopilotKit", no "Gemini")
- plugin.json is valid JSON
- settings.json still points to larry-extended
</verification>

<success_criteria>
- Starting Claude with `--plugin-dir /home/jsagi/MindrianOS-Plugin` loads Larry as the default agent
- Larry's personality is recognizable: warm, demanding, asks questions, never dumps frameworks
- Auto-loaded content totals under 5KB across all SKILL.md files
- Reference files are available for on-demand loading but not auto-loaded
- Methodology index lists all 25+ commands for future progressive disclosure
</success_criteria>

<output>
After completion, create `.planning/phases/01-install-and-larry-talks/01-01-SUMMARY.md`
</output>
