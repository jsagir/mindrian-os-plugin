---
phase: 141-local-retrieval-spine-and-capability-dial
plan: 03
type: execute
wave: 1
depends_on: ["141-01"]
files_modified:
  - lib/core/navigation/room-context.cjs
  - lib/core/navigation.cjs
autonomous: true
requirements: [RETR-01, RETR-03, RETR-04]
must_haves:
  truths:
    - "getRoomContext fuses Leg A (room-home RAW) + Leg B (windowed session-history fragments) + Leg C (getNeighborhood graph-ranking), seeded by the last ~2 turns"
    - "getRoomContext output is raw prose -- never a SHA256 hash; it imports nothing from packet.cjs"
    - "Per-turn assembly completes under the 1200ms NAV budget on a populated fixture room.db"
    - "getRoomContext and getSessionHistory are re-exported through the navigation.cjs Part 9 chokepoint"
  artifacts:
    - path: "lib/core/navigation/room-context.cjs"
      provides: "The 3-leg local fusion module + conversation-derived seed resolver"
      exports: ["getRoomContext"]
      min_lines: 40
    - path: "lib/core/navigation.cjs"
      provides: "Part 9 chokepoint re-exporting getRoomContext + getSessionHistory"
      contains: "getRoomContext"
  key_links:
    - from: "lib/core/navigation/room-context.cjs"
      to: "getRoomHomeView / getSessionHistory / getNeighborhood"
      via: "require the shipped legs, never re-derive"
      pattern: "getRoomHomeView|getSessionHistory|getNeighborhood"
    - from: "lib/core/navigation.cjs"
      to: "room-context.cjs"
      via: "re-export idiom"
      pattern: "getRoomContext"
---

<objective>
Build getRoomContext() -- the 100%-local, in-process, three-leg fusion that lets Larry walk the room graph mid-conversation, seeded by the last ~2 conversation turns. It is the substrate behind the dial's "Context Block" reach. Compose the SHAPE of buildBrainPacket but call the three SHIPPED legs as-is and NEVER import the packet.cjs projection (which hashes prose). Re-export it (and getSessionHistory, its first real consumer) through the navigation.cjs Part 9 chokepoint.

Purpose: Closes the founding loop's read side -- "do you remember X" now retrieves X-relevant nodes. This is the 4x retrieval lever (Aronhime: "reach matters more than raw intelligence").
Output: lib/core/navigation/room-context.cjs + two navigation.cjs re-exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/141-local-retrieval-spine-and-capability-dial/141-RESEARCH.md
@.planning/phases/141-local-retrieval-spine-and-capability-dial/141-PATTERNS.md
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- The shipped legs the fusion composes. Extracted from the codebase; use directly, no exploration. -->

From lib/core/navigation/room-home.cjs (Leg A -- RAW prose, reuse as-is):
- getRoomHomeView(db, roomId, opts) -> 9-field object {currentThesis, confirmedFacts, riskyAssumptions, evidence, contradictions, openQuestions, recentChanges, bankedOpportunities, nextMove}
- safeShape(row) (room-home.cjs:29-43) -- the RAW truncator: returns props.summary||props.claim||props.title sliced to 120 chars. Does NOT hash. Reuse this path, never packet.cjs.

From lib/core/memory-ops.cjs (Leg B -- window it):
- getSessionHistory(db, limit=10) -> sessions DESC, each with nested fragments[] {role, content, timestamp, section_context}. NET-NEW step: take the most recent session, slice its last N fragments with a char cap.

From lib/core/navigation/neighborhood.cjs (Leg C -- reuse as-is):
- getNeighborhood(db, focusNodeId, {topK, maxDepth}) -> top-K graph neighbors by frozen score. Returns [] for an unknown focus (neighborhood.cjs:54-55 safe-empty guard).

From lib/core/navigation/focus.cjs (the seed-resolver structural analog):
- ensureNodeExists(db, nodeId) (focus.cjs:101-104) -> Boolean. Mirror this guard before calling getNeighborhood.
- computeAutoFocus (focus.cjs:106-143) seeds from VENTURE STATE; 141's resolver seeds from CONVERSATION (last ~2 fragments) instead.

ANTIPATTERN -- DO NOT IMPORT (RETR-03): lib/core/navigation/packet.cjs projectText/shortText/hashText/safeNodeProjection/resolvePrivacyMode/PRIVACY_MODES. projectText (packet.cjs:130-139) returns hashText(s) under the default local_summary_only mode -- it hashes the prose Larry needs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the getRoomContext 3-leg fusion + conversation seed resolver</name>
  <behavior>
    - getRoomContext(db, roomId, opts) returns {summary, recentMessages, relevantNodes, _meta} against the fixture db
    - relevantNodes is non-empty when the seed fragment's section_context resolves to a seeded node (Leg C fired)
    - recentMessages carries RAW fragment content (the literal seeded text), never a hash
    - With a bad/unknown seed, the function degrades gracefully (relevantNodes [] via the neighborhood safe-empty guard) and still returns the other two legs
    - The module source contains ZERO require of packet.cjs and ZERO of {projectText, hashText, shortText, sha256}
  </behavior>
  <read_first>
    - lib/core/navigation/packet.cjs:15-17 (the require-the-legs-and-fuse composition shape to copy) + :130-139 (the egress antipattern NOT to import)
    - lib/core/navigation/room-home.cjs:29-43,102-141 (Leg A + safeShape raw truncator)
    - lib/core/memory-ops.cjs:314-333 (Leg B getSessionHistory shape)
    - lib/core/navigation/neighborhood.cjs:14-46,48-79 (Leg C frozen score + safe-empty guard)
    - lib/core/navigation/focus.cjs:49-80,101-104,106-143 (the ensureNodeExists guard + computeAutoFocus resolver shape to mirror with a conversation seed)
    - lib/core/navigation/evidence-claim.cjs:10-17 (the caller-owned-handle rule: never require node:sqlite, never open room.db)
    - tests/test-get-room-context.cjs + tests/test-room-context-part8-invariant.cjs (Plan 01 -- the RED suites this turns GREEN)
  </read_first>
  <action>
    Create lib/core/navigation/room-context.cjs exporting async getRoomContext(db, roomId, opts) where opts = {seedFragments?, topK?, fragmentWindow?, maxDepth?}. Caller-owned db handle only -- never require('node:sqlite'), never open room.db (keeps it inside the navigation allow-list scripts/check-substrate.cjs regex /^lib\/core\/navigation\//).

    Leg A: call getRoomHomeView(db, roomId, opts) as-is; reuse its safeShape raw path for any extra truncation. Do not re-implement its SELECTs.

    Leg B: call getSessionHistory(db, limit) and WINDOW it -- take the most recent session, slice the last N fragments (default fragmentWindow ~6) with a per-fragment char cap. This windowing is the net-new step. Output raw role/content/timestamp.

    Leg C seed resolver (the load-bearing new wiring): take the last ~2 windowed fragments, resolve a focus node by matching the fragment's section_context to a section node id, with a cheap lexical fallback against nodes.properties. Guard with the ensureNodeExists idiom (mirror focus.cjs:101-104) before calling getNeighborhood(db, focusNodeId, {topK: default 10-20, maxDepth: default 2}). A bad seed returns [] (graceful).

    Return {summary (Leg A), recentMessages (windowed Leg B raw), relevantNodes (Leg C ranked), _meta {seedNodeId, leg timings}}. Return the object even on partial-leg failure (defensive structured return). NEVER import packet.cjs.

    Optional (Claude's Discretion): if you log a context_assembled memory_event, treat it as a Part 9 audit-node carve-out (created_by=system review_status=confirmed) -- mirror focus.cjs:61-74 verbatim. Only add an EVENT_TYPES entry if the chosen event_type is not already in the Set; do not bump speculatively.
  </action>
  <verify>
    <automated>node tests/test-get-room-context.cjs && node tests/test-room-context-part8-invariant.cjs</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/navigation/room-context.cjs exports getRoomContext and is at least 40 lines
    - `grep -E "require\\(.*packet" lib/core/navigation/room-context.cjs` returns nothing (RETR-03)
    - `grep -E "projectText|hashText|shortText|sha256" lib/core/navigation/room-context.cjs` returns nothing
    - tests/test-get-room-context.cjs exits 0 (fusion returns the 4-field object with non-empty relevantNodes + raw recentMessages)
    - tests/test-room-context-part8-invariant.cjs exits 0 (adversarial Part-8 sweep clean)
    - The module never requires node:sqlite and never opens room.db (caller-owned handle)
  </acceptance_criteria>
  <done>getRoomContext fuses all three legs from a fixture, returns raw prose, and passes the Part-8 invariant sweep.</done>
</task>

<task type="auto">
  <name>Task 2: Re-export getRoomContext + getSessionHistory through the navigation.cjs chokepoint + benchmark RETR-04</name>
  <read_first>
    - lib/core/navigation.cjs:50-53,72-74,192-196 (the thin additive re-export idiom + the doc-comment style at :180-196)
    - lib/core/memory-ops.cjs:592 (getSessionHistory is today only re-exported here; D-04a mandates promoting it into the navigation.cjs chokepoint)
    - scripts/intent-classifier.cjs:635,1196 (NAV_HARD_TIMEOUT_MS=1200 + the Promise.race envelope assembly must finish inside it)
    - tests/test-room-context-latency.cjs (Plan 01 -- the RETR-04 1200ms budget suite)
  </read_first>
  <action>
    In lib/core/navigation.cjs, add three things via the existing thin-re-export idiom (mirror :52, :73, :194): require room-context.cjs and re-export getRoomContext; require memory-ops and re-export getSessionHistory (D-04a -- promote it into the chokepoint so getRoomContext is its first real consumer through the door, rather than calling memory-ops directly). Annotate each with the doc-comment style at :180-196.

    Then run the RETR-04 latency benchmark against a populated fixture room.db: confirm getRoomContext assembly completes under NAV_HARD_TIMEOUT_MS (1200ms). Graph-rank-first; window Leg B. If and ONLY if the benchmark fails the budget, document the FTS5 contingency (D-04b) in a code comment and in the SUMMARY -- do NOT build FTS5 speculatively. House rule: no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-room-context-latency.cjs && node -e "const n=require('./lib/core/navigation.cjs'); if(typeof n.getRoomContext!=='function'||typeof n.getSessionHistory!=='function') process.exit(1); console.log('chokepoint exports present')"</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/navigation.cjs re-exports both getRoomContext and getSessionHistory (both resolve to functions)
    - tests/test-room-context-latency.cjs exits 0 (assembly under 1200ms on the populated fixture)
    - No FTS5 virtual table was added (`grep -riE "virtual table|using fts5" lib/ scripts/` returns nothing) unless the benchmark demonstrably failed and the contingency is documented
    - `grep -c $'—' lib/core/navigation.cjs lib/core/navigation/room-context.cjs` returns 0
  </acceptance_criteria>
  <done>The chokepoint exposes getRoomContext + getSessionHistory and the assembly fits the 1200ms budget.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room.db -> Larry in-process reasoning | getRoomContext output feeds the model in-process; it MUST NOT cross the wire |
| LOCAL -> BRAIN (Part 8) | The fusion is the canonical place a careless import (packet.cjs projectText) could hash/leak; the RETR-03 sweep is the structural fence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-04 | Information disclosure | room-context.cjs accidental packet.cjs import | mitigate | RETR-03 adversarial sweep asserts zero packet require + zero sha256/hashText in source |
| T-141-05 | Denial of service | unbounded fragment dump blows the 1200ms budget | mitigate | Window Leg B (fragmentWindow cap), graph-rank first, RETR-04 latency test |
| T-141-06 | Tampering | SQL injection via the fragment-derived seed | mitigate | Resolve the seed via db.prepare bound params (the pattern every leg follows); never string-concatenate the fragment text into SQL |
| T-141-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed; pure in-repo CJS over node:sqlite built-in. No package surface. |
</threat_model>

<verification>
test-get-room-context.cjs, test-room-context-part8-invariant.cjs, and test-room-context-latency.cjs all green. navigation.cjs exposes both new exports. No FTS5 added. No em-dashes.
</verification>

<success_criteria>
RETR-01 (3-leg fusion seeded by last ~2 turns), RETR-03 (raw prose, no packet.cjs egress path), RETR-04 (under 1200ms) all satisfied; getRoomContext + getSessionHistory live on the Part 9 chokepoint.
</success_criteria>

<output>
Create `.planning/phases/141-local-retrieval-spine-and-capability-dial/141-03-SUMMARY.md` when done
</output>
