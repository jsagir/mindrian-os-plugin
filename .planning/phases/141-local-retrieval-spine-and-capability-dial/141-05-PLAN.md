---
phase: 141-local-retrieval-spine-and-capability-dial
plan: 05
type: execute
wave: 1
depends_on: ["141-01"]
files_modified:
  - scripts/build-graph-from-sqlite.cjs
autonomous: true
requirements: [BUG-01]
must_haves:
  truths:
    - "build-graph-from-sqlite.cjs no longer throws a ReferenceError on the missing-room.db guard -- it reaches its graceful exit-0 path"
    - "Running the script against a directory with no .mindrian/room.db exits 0"
  artifacts:
    - path: "scripts/build-graph-from-sqlite.cjs"
      provides: "The graph-export script with the line-53 guard referencing roomDbPath (not the undeclared lazygraphPath)"
      contains: "roomDbPath"
  key_links:
    - from: "scripts/build-graph-from-sqlite.cjs"
      to: "the no-room-db graceful exit-0 path"
      via: "fs.existsSync(roomDbPath) guard"
      pattern: "fs.existsSync\\(roomDbPath\\)"
---

<objective>
Fix BUG-01: the one-token ReferenceError at scripts/build-graph-from-sqlite.cjs:53. Line 50 defines the correct roomDbPath; line 53 references an undeclared lazygraphPath, which throws an uncaught ReferenceError BEFORE the exit-0 try/catch opens (:58), so the graceful-degradation path is effectively dead and the graph is never emitted. Change the one token lazygraphPath -> roomDbPath and prove the guard now reaches its exit-0 path against a no-room-db directory.

Purpose: An independent tag-along fix that restores the graph.json Cytoscape export path. Small, isolated, no shared files with the other plans.
Output: the one-token fix + the BUG-01 regression turning green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/141-local-retrieval-spine-and-capability-dial/141-PATTERNS.md
@.planning/phases/141-local-retrieval-spine-and-capability-dial/141-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: One-token fix lazygraphPath -> roomDbPath</name>
  <read_first>
    - scripts/build-graph-from-sqlite.cjs:45-63 (the no-roomDir guard at :45-48 is the correct exit-0 idiom; :50 defines roomDbPath; :53 references the undeclared lazygraphPath; the exit-0 try/catch opens at :58 AFTER the throwing guard)
    - tests/test-build-graph-guard.cjs (Plan 01 -- the RED regression that spawns the script against a no-room-db dir and asserts exit 0)
  </read_first>
  <action>
    In scripts/build-graph-from-sqlite.cjs, change the single token on the line-53 guard from lazygraphPath to roomDbPath so the missing-room.db guard tests the path that line 50 actually defined. This is the entire code change -- one token. Do not refactor surrounding code, do not move the try/catch, do not rename roomDbPath. House rule: no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-build-graph-guard.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "lazygraphPath" scripts/build-graph-from-sqlite.cjs` returns nothing (the undeclared symbol is gone)
    - `grep -n "fs.existsSync(roomDbPath)" scripts/build-graph-from-sqlite.cjs` returns the line-53 guard
    - tests/test-build-graph-guard.cjs exits 0 (script spawned against a no-room-db dir returns exit code 0)
    - The diff touches exactly one token on one line (verify `git diff --stat` shows a 1-line change)
  </acceptance_criteria>
  <done>The graph-export guard references roomDbPath, reaches its graceful exit-0 path, and the regression is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script -> filesystem | Reads room.db, writes graph.json; purely local batch I/O, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-10 | Denial of service | the dead ReferenceError path crashing the export | mitigate | The fix restores the graceful exit-0 guard; the regression test proves it |
| T-141-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed; one-token edit to an existing script. No package surface. |
</threat_model>

<verification>
test-build-graph-guard.cjs green; no lazygraphPath remains; the change is a single token.
</verification>

<success_criteria>
BUG-01 satisfied: the line-53 guard references roomDbPath, the script exits 0 against a no-room-db dir, and the graph-export path is no longer dead.
</success_criteria>

<output>
Create `.planning/phases/141-local-retrieval-spine-and-capability-dial/141-05-SUMMARY.md` when done
</output>
