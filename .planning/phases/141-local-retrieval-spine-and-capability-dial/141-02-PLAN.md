---
phase: 141-local-retrieval-spine-and-capability-dial
plan: 02
type: execute
wave: 1
depends_on: ["141-01"]
files_modified:
  - skills/larry-personality/SKILL.md
  - CHANGELOG.md
  - .claude-plugin/plugin.json
  - package.json
autonomous: true
requirements: [LARRY-01, LARRY-02, LARRY-03, LARRY-04, DRSCH-01, DRSCH-02, DRSCH-03, DRSCH-04]
must_haves:
  truths:
    - "The Capability Dial section is in HEAD (committed), not lost from the working tree"
    - "The committed SKILL.md frontmatter declares canon_parts: [Part 2, Part 3, Part 8, Part 9]"
    - "The dial encodes exactly 5 machine-readable reach ids and the Hierarchical Navigator encodes exactly 3 posture ids"
    - "The deep-research reach is committed as doctrine (5th row + reach rule 6); no executable plumbing"
    - "Version is bumped to 1.13.1-beta.7 in CHANGELOG, plugin.json, and package.json with the dial as a release-noted change"
  artifacts:
    - path: "skills/larry-personality/SKILL.md"
      provides: "The committed Capability Dial + Hierarchical Navigator doctrine with machine-readable ids"
      contains: "Capability Dial"
    - path: "CHANGELOG.md"
      provides: "1.13.1-beta.7 entry naming the dial, reach ids, posture ids, Navigator doctrine"
      contains: "1.13.1-beta.7"
  key_links:
    - from: "skills/larry-personality/SKILL.md"
      to: "tests/test-reach-ids-drift.cjs"
      via: "5 reach-id tokens"
      pattern: "context_block|contradiction|cross_room|brain_consult|deep_research"
    - from: "skills/larry-personality/SKILL.md"
      to: "tests/test-posture-ids-drift.cjs"
      via: "3 posture-id tokens"
      pattern: "push_forward|hold|pull_back"
---

<objective>
Commit the working-tree Capability Dial section to HEAD FIRST (D-06, hard ordering -- it is ` M`, in no commit; any stash/checkout loses it), adding the net-new pieces: the canon_parts frontmatter line, the 5 machine-readable reach ids, the LARRY-04 Hierarchical Navigator doctrine section (Usher-division-led, with 3 posture ids and Reach rule 7), and the CHANGELOG + version bump to beta.7. DRSCH ships as committed doctrine only -- the 5th reach row and reach rule 6 are already present in the working tree; this commit tracks them, it does NOT build any executable deep-research plumbing.

Purpose: This is execution step 1 of the whole phase. Until the dial is committed, every other task risks losing it. It also lays the prompt-layer contract (5 reach ids, 3 posture ids) that Phase 143 keys off.
Output: A committed SKILL.md with frontmatter + ids + the Navigator section, plus the beta.7 version bump across CHANGELOG / plugin.json / package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/141-local-retrieval-spine-and-capability-dial/141-CONTEXT.md
@.planning/phases/141-local-retrieval-spine-and-capability-dial/141-RESEARCH.md
@.planning/phases/141-local-retrieval-spine-and-capability-dial/141-PATTERNS.md
@skills/larry-personality/SKILL.md
@.claude/includes/release-process.md
@~/MindrianRooms/mindrianOS/product-evolution/v1.13.0-memory-system-review/ARONHIME-EVOLUTION-EXPLANATION-scraped.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontmatter + 5 reach ids + LARRY-04 Navigator section, then COMMIT FIRST</name>
  <read_first>
    - skills/larry-personality/SKILL.md (the full working-tree file: the dial heading "When to Reach -- The Capability Dial", the 5 reach rows including the deep-research 5th row, reach rules 1-6 -- all PRESENT as prose; frontmatter is currently only name + description)
    - skills/mva-pipeline/SKILL.md:7 (the canon_parts frontmatter precedent: `canon_parts: [Part 2, Part 8, Part 10]`)
    - .planning/phases/141-local-retrieval-spine-and-capability-dial/141-RESEARCH.md (the LARRY-03 reach-id table mapping each dial row to its id; the LARRY-04 doctrine "how to write" section: Usher division, 3 posture ids + triggers, Reach rule 7, the Aronhime quotes to use)
    - ~/MindrianRooms/mindrianOS/product-evolution/v1.13.0-memory-system-review/ARONHIME-EVOLUTION-EXPLANATION-scraped.md (the source of the three verbatim quotes: "the insight belongs to you; the reach belongs to the tool", "reach matters more than raw intelligence", "restraint is the product working correctly")
  </read_first>
  <action>
    Edit skills/larry-personality/SKILL.md (do NOT rebuild the existing dial prose -- it is correct per D-07):

    1. Frontmatter (LARRY-01, D-04c): add the line `canon_parts: [Part 2, Part 3, Part 8, Part 9]` alongside the existing name + description.

    2. Reach ids (LARRY-03, D-05): add EXACTLY 5 grep-able machine-readable reach-id tokens, one per existing dial row, mapped: Context Block row -> context_block; contradiction surface row -> contradiction; cross-room reach row -> cross_room; Brain consult row -> brain_consult; framework-led deep research row -> deep_research. Encode them so a drift test can grep the set (e.g. a fenced `reach_ids:` block listing the 5, or a per-row `id: <token>` annotation). No 6th id; no fewer than 5.

    3. LARRY-04 Hierarchical Navigator section (D-11/12/13): add a NET-NEW section AFTER the reach-rules block. Lead with the Usher division (the tool owns Usher steps 1-2 = perceive + set the stage = the reach; the human owns steps 3-4 = insight + critical revision/validation; the posture is the bidirectional traversal of the Usher cycle). Quote Aronhime verbatim: "the insight belongs to you; the reach belongs to the tool", "reach matters more than raw intelligence", and "restraint is the product working correctly". Encode EXACTLY 3 posture ids {push_forward, hold, pull_back} with their triggers (push_forward = step-4 validation holds / confirmed evidence + well-defined subsystem ready to climb; hold = mid-step-2, reach pending/failed, stay quiet; pull_back = step-4 gap, unresolved contradictions / None-tier evidence near a commit / circular-stuck-regression). State the doctrine is prompt-layer only in 141; executable sensors (143) + nav engine (144) defer.

    4. Reach rule 7 (D-13 arbitration): append rule 7 onto the reach-rules block. Encode: the two dials are two dimensions of ONE decision cycle (CoALA), not two captains; reach precedes push (Capability dial evaluates first, the reach result sets the posture, the Ask-Tell dial sets intensity within it); the user is the only helm (HIC + AITL); name the anti-pattern "Reasoning-Action Disconnect" alongside "two captains, one ship"; never change posture or filing silently.

    5. Honor the HARD house rule: hyphens only, NO em-dashes anywhere in the edited text.

    Then COMMIT FIRST (D-06, before any other phase work): stage SKILL.md and commit. This protects the loseable working-tree edit before later plans run.
  </action>
  <verify>
    <automated>node tests/test-reach-ids-drift.cjs && node tests/test-posture-ids-drift.cjs && grep -c $'—' skills/larry-personality/SKILL.md | grep -qx 0 && echo "no em-dashes"</automated>
  </verify>
  <acceptance_criteria>
    - skills/larry-personality/SKILL.md frontmatter contains `canon_parts: [Part 2, Part 3, Part 8, Part 9]`
    - tests/test-reach-ids-drift.cjs exits 0 and asserts exactly 5 ids {context_block, contradiction, cross_room, brain_consult, deep_research}
    - tests/test-posture-ids-drift.cjs exits 0 and asserts exactly 3 ids {push_forward, hold, pull_back} plus the two required Aronhime quote strings
    - `grep -c $'—' skills/larry-personality/SKILL.md` returns 0 (no em-dashes)
    - The SKILL.md edit is committed (git status no longer shows ` M skills/larry-personality/SKILL.md`) BEFORE any other Plan-02 file is touched
    - The deep-research 5th reach row + reach rule 6 are still present (DRSCH doctrine preserved, not removed)
  </acceptance_criteria>
  <done>The dial + Navigator doctrine is committed to HEAD with frontmatter, 5 reach ids, and 3 posture ids; the loseable edit is safe.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG entry + version bump to beta.7</name>
  <read_first>
    - CHANGELOG.md (the top entry format: `## [X.Y.Z] - YYYY-MM-DD` with Added/Fixed/Changed sections; the most recent entry is 1.13.1-beta.6)
    - .claude-plugin/plugin.json (the `version` field, currently 1.13.1-beta.6)
    - package.json (the `version` field, currently 1.13.1-beta.6)
    - .claude/includes/release-process.md (the version consistency rule: CHANGELOG / plugin.json / package.json / tag / marketplace must all match; tag + marketplace push are human-gated for betas, do NOT push here)
  </read_first>
  <action>
    Add a new CHANGELOG.md entry at the top: `## [1.13.1-beta.7] - 2026-06-05`. Under `### Added`, name the tracked Capability Dial (5-reach GUIDED-default trigger map + reach rules), the Hierarchical Navigator doctrine (Usher division + 3 posture ids), the 5 reach ids + 3 posture ids prompt-layer contract. Leave room to extend Added/Fixed as later 141 plans land (getRoomContext, FILEVAL, BUG-01) -- those plans append to this same entry. Bump `version` to `1.13.1-beta.7` in BOTH .claude-plugin/plugin.json and package.json. Do NOT git tag or push to marketplace -- beta tag + marketplace ref are human-gated per the release process; this commit only lands the in-repo lockstep (CHANGELOG + plugin.json + package.json). House rule: no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-capability-dial-committed.cjs</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md top entry is `## [1.13.1-beta.7]` and names the Capability Dial
    - `grep '"version"' .claude-plugin/plugin.json package.json` shows both at 1.13.1-beta.7
    - tests/test-capability-dial-committed.cjs exits 0 (HEAD has the dial + canon_parts + CHANGELOG beta.7 + matching plugin/package versions + DRSCH 5th row + reach rule 6)
    - No git tag created, no marketplace push (human-gated)
  </acceptance_criteria>
  <done>The beta.7 in-repo version lockstep is consistent and the dial is a release-noted change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt-layer doctrine -> Larry | The dial text is read by the model; no code path enforces it (CLI-honored only) |
| Part 8 (in doctrine) | The Brain-consult + deep-research rows assert generic-handles-only egress; this is doctrine, the structural fence lands in Plans 03/06 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-02 | Tampering | loseable working-tree SKILL.md edit | mitigate | Commit FIRST (D-06) before any branch/stash; Task 1 ordering enforces it |
| T-141-03 | Repudiation | drift between dial ids and downstream Phase 143 consumers | mitigate | Exactly-5 + exactly-3 drift tests freeze the id banks |
| T-141-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs zero packages; doc + version edits only. No package surface. |
</threat_model>

<verification>
The two drift tests and the committed-dial test pass. `git log -1 --stat` shows SKILL.md committed before the version-bump commit. No em-dashes in any edited file.
</verification>

<success_criteria>
LARRY-01/02/03/04 + DRSCH-01..04 satisfied at the doctrine level: dial committed to HEAD with canon_parts, 5 reach ids, 3 posture ids, the Navigator section, Reach rule 7, and the beta.7 lockstep.
</success_criteria>

<output>
Create `.planning/phases/141-local-retrieval-spine-and-capability-dial/141-02-SUMMARY.md` when done
</output>
