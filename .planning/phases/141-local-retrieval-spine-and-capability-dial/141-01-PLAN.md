---
phase: 141-local-retrieval-spine-and-capability-dial
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/run-all-141.sh
  - tests/test-get-room-context.cjs
  - tests/test-retrieval-seed.cjs
  - tests/test-room-context-part8-invariant.cjs
  - tests/test-room-context-latency.cjs
  - tests/test-capability-dial-committed.cjs
  - tests/test-reach-ids-drift.cjs
  - tests/test-posture-ids-drift.cjs
  - tests/test-fileval-readback.cjs
  - tests/test-build-graph-guard.cjs
  - tests/fixtures/room-141-fixture.cjs
autonomous: true
requirements: [RETR-01, RETR-02, RETR-03, RETR-04, LARRY-01, LARRY-02, LARRY-03, LARRY-04, DRSCH-01, DRSCH-02, DRSCH-03, DRSCH-04, FILEVAL-02, BUG-01]
must_haves:
  truths:
    - "Every Phase 141 requirement has a runnable automated test that fails RED before its implementation lands"
    - "A single command (bash tests/run-all-141.sh) runs all 141 suites and exits non-zero if any fails"
    - "A populated fixture room.db (fragments + nodes + edges) exists for the fusion + FILEVAL + latency suites to read"
  artifacts:
    - path: "tests/run-all-141.sh"
      provides: "Per-phase scoped runner aggregating all 10 CJS suites"
      contains: "Phase 141"
    - path: "tests/fixtures/room-141-fixture.cjs"
      provides: "In-memory DatabaseSync builder seeding nodes/edges/sessions/fragments for the read suites"
      exports: ["buildFixtureDb"]
    - path: "tests/test-reach-ids-drift.cjs"
      provides: "LARRY-03 exactly-5 reach-id drift assertion"
    - path: "tests/test-posture-ids-drift.cjs"
      provides: "LARRY-04 exactly-3 posture-id drift assertion"
  key_links:
    - from: "tests/run-all-141.sh"
      to: "tests/test-*.cjs"
      via: "CJS_SUITES array"
      pattern: "CJS_SUITES"
---

<objective>
Stand up the Wave-0 test scaffold for Phase 141: the scoped runner, a populated fixture room.db builder, and all ten CJS test suites (one per requirement cluster) in their RED state. This is the Nyquist floor -- every later task verifies against a suite created here, so no implementation task ships without an automated check that was failing before it.

Purpose: Make the entire phase test-first. The drift tests (LARRY-03 exactly-5, LARRY-04 exactly-3), the RETR-03/D-03a Part-8 adversarial sweep, and the FILEVAL fixture-first suite (D-02a) all originate here.
Output: tests/run-all-141.sh + tests/fixtures/room-141-fixture.cjs + 9 test-*.cjs suites, all wired into the runner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/141-local-retrieval-spine-and-capability-dial/141-VALIDATION.md
@.planning/phases/141-local-retrieval-spine-and-capability-dial/141-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy the runner + build the fixture room.db</name>
  <read_first>
    - tests/run-all-126.sh (the exact runner analog to copy: set -uo pipefail at :22, SHELL_SUITES + CJS_SUITES arrays at :27-37, per-suite missing-file guard + PASS/FAIL accounting at :64-77, non-zero exit at :90-98)
    - tests/test-feynman-timeline-canon-part-9-invariant.cjs:27-33 (the node:sqlite-unavailable SKIP exit-77 idiom) + :70-75 (applySchema building the nodes table inline)
    - lib/core/memory-ops.cjs:314-333 (getSessionHistory returns sessions DESC, each with nested fragments[] carrying role/content/timestamp/section_context -- the fixture must populate the sessions + fragments tables in this shape)
    - lib/core/navigation/neighborhood.cjs:48-79 (the recursive-CTE reads nodes + edges; the fixture must seed nodes with id/type/properties/review_status/confidence/last_seen_at and edges with source_id/target_id/edge_type so Leg C returns a non-empty neighborhood)
  </read_first>
  <action>
    Create tests/run-all-141.sh by copying tests/run-all-126.sh verbatim, then: swap the header banner to "Phase 141 scoped test runner", replace the CJS_SUITES list with the 9 Phase-141 suites (test-get-room-context.cjs, test-retrieval-seed.cjs, test-room-context-part8-invariant.cjs, test-room-context-latency.cjs, test-capability-dial-committed.cjs, test-reach-ids-drift.cjs, test-posture-ids-drift.cjs, test-fileval-readback.cjs, test-build-graph-guard.cjs), and keep SHELL_SUITES empty. Preserve set -uo pipefail, the missing-file guard, PASS/FAIL accounting, and the non-zero-on-any-failure exit. House rule: no em-dashes anywhere in the file.

    Create tests/fixtures/room-141-fixture.cjs exporting buildFixtureDb() which: requires node:sqlite DatabaseSync (with the exit-77 SKIP guard if unavailable), opens an in-memory db, applies the minimal nodes + edges + sessions + fragments schema inline (mirror the applySchema idiom from the canon-invariant test), and seeds: at least one section node + two claim nodes (one with a CONTRADICTS edge between them so Leg C ranks it), one EvidenceClaim-shaped node slot, one session with ~6 fragments whose section_context points at the seeded section node. Return the live db handle (caller-owned, never self-opening a real room.db). This single fixture feeds the fusion, seed, latency, and FILEVAL suites.
  </action>
  <verify>
    <automated>bash tests/run-all-141.sh; test $? -ne 0 && echo "RED as expected (suites not yet implemented)"; node -e "require('./tests/fixtures/room-141-fixture.cjs').buildFixtureDb()" && echo "fixture builds"</automated>
  </verify>
  <acceptance_criteria>
    - tests/run-all-141.sh exists, contains the string "Phase 141", and lists all 9 CJS suites in CJS_SUITES
    - `grep -c "em dash placeholder" tests/run-all-141.sh` is irrelevant; assert ZERO em-dash bytes: `grep -c $'—' tests/run-all-141.sh` returns 0
    - tests/fixtures/room-141-fixture.cjs exports buildFixtureDb and returns a db handle with populated nodes, edges, sessions, fragments tables (or exits 77 when node:sqlite is unavailable)
    - The runner runs to completion and exits non-zero while suites are still stubs (RED)
  </acceptance_criteria>
  <done>The runner aggregates all 9 suites and the fixture builder produces a populated in-memory room.db.</done>
</task>

<task type="auto">
  <name>Task 2: Write the read-side + bugfix suites (RED)</name>
  <read_first>
    - tests/fixtures/room-141-fixture.cjs (built in Task 1 -- the db source these suites consume)
    - .planning/phases/141-local-retrieval-spine-and-capability-dial/141-VALIDATION.md (the Per-Task Verification Map: exact command names + behaviors per req)
    - lib/core/navigation/packet.cjs:130-139 (the projectText -> hashText egress antipattern; the RETR-03 sweep asserts ZERO require of packet.cjs and ZERO sha256 in room-context.cjs)
    - scripts/build-graph-from-sqlite.cjs:45-58 (the existing exit-0 guard at :45-48 vs the lazygraphPath ReferenceError at :53; BUG-01 regression runs the script against a no-room-db dir and asserts exit 0)
  </read_first>
  <action>
    Create these RED suites that will pass only after their implementation plans land. Each asserts behavior, not implementation:

    test-get-room-context.cjs (RETR-01): require navigation.getRoomContext (expect undefined now -> RED), call it against the fixture db, assert the return is {summary, recentMessages, relevantNodes, _meta} with non-empty relevantNodes (Leg C fired) and raw prose (not hashed) in recentMessages.

    test-retrieval-seed.cjs (RETR-02): assert scripts/intent-classifier.cjs no longer hard-codes `userText: null` in the turn object AND that the un-nulled value is referenced only on the LOCAL seed path (grep that no userText assignment threads toward buildBrainPacket/brain-client in the same hot-path block). RED now (userText:null still present).

    test-room-context-part8-invariant.cjs (RETR-03 + D-03a, adversarial): grep lib/core/navigation/room-context.cjs source for ZERO `require('...packet...')`, ZERO of {projectText, shortText, hashText, safeNodeProjection, resolvePrivacyMode, PRIVACY_MODES}, ZERO sha256/hashText calls. RED now (file does not exist -> assert file-present-then-clean once it lands). Mirror the test-feynman-timeline-canon-part-9-invariant.cjs forbidden-substring + forbidden-require idiom.

    test-room-context-latency.cjs (RETR-04): build the fixture, time getRoomContext over it, assert under 1200ms. RED now (function missing).

    test-build-graph-guard.cjs (BUG-01): child_process spawn scripts/build-graph-from-sqlite.cjs against a tmp dir with NO .mindrian/room.db; assert exit code 0. RED now (ReferenceError crashes non-zero).
  </action>
  <verify>
    <automated>for t in get-room-context retrieval-seed room-context-part8-invariant room-context-latency build-graph-guard; do node tests/test-$t.cjs; echo "exit=$? (RED expected)"; done; true</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist and are valid Node (no syntax error on require)
    - test-room-context-part8-invariant.cjs scans for the literal forbidden tokens {projectText, hashText, packet, sha256} and the forbidden require regex for packet.cjs
    - test-build-graph-guard.cjs spawns the real script via child_process and asserts process exit code 0
    - Each suite exits non-zero (RED) or skips (77) -- none falsely passes before implementation
  </acceptance_criteria>
  <done>The read-side and bugfix suites exist and fail RED against the current tree.</done>
</task>

<task type="auto">
  <name>Task 3: Write the doctrine + FILEVAL suites (RED)</name>
  <read_first>
    - skills/larry-personality/SKILL.md (the dial section at the "When to Reach -- The Capability Dial" heading; frontmatter is currently only name + description -- the committed-dial test asserts canon_parts is added)
    - tests/test-feynman-timeline-canon-part-9-invariant.cjs:39-66 (the FILES_TO_SCAN + forbidden/required SET shape to mirror for the exact-set drift tests)
    - lib/core/navigation/evidence-claim.cjs:48,82-89,108-114 (the closed Part-5 tier Set, the LOCKED 6-field provenance schema, review_status:'proposed' -- the FILEVAL suite asserts artifact_path is additive and review_status stays proposed)
    - tests/fixtures/room-141-fixture.cjs (Task 1 -- the db the FILEVAL suite writes to and reads back from)
    - .claude/includes/release-process.md (the 5-place version lockstep: CHANGELOG / plugin.json / package.json / tag / marketplace -- the committed-dial test checks CHANGELOG + plugin.json + package.json are at beta.7)
  </read_first>
  <action>
    Create these RED suites:

    test-capability-dial-committed.cjs (LARRY-01/02 + DRSCH-01..04): assert `git show HEAD:skills/larry-personality/SKILL.md` contains "Capability Dial" (RED now: HEAD=0); assert the committed SKILL.md frontmatter contains `canon_parts: [Part 2, Part 3, Part 8, Part 9]`; assert CHANGELOG.md top entry names the Capability Dial and version 1.13.1-beta.7; assert plugin.json and package.json both read 1.13.1-beta.7; assert the deep-research 5th reach row + reach rule 6 are present (DRSCH doctrine-only coverage).

    test-reach-ids-drift.cjs (LARRY-03, adversarial exact-set): grep the committed SKILL.md for the reach-id tokens; assert the reach-id set is EXACTLY {context_block, contradiction, cross_room, brain_consult, deep_research} -- no more, no fewer. Use the `grep -v '^#' | grep -c` hygiene so prose comments do not self-invalidate the count. RED now (no machine tokens in the dial).

    test-posture-ids-drift.cjs (LARRY-04 / D-12, adversarial exact-set): assert the SKILL.md Hierarchical Navigator section exists, that it quotes "the insight belongs to you; the reach belongs to the tool" and "restraint is the product working correctly", and that the posture-id set is EXACTLY {push_forward, hold, pull_back} -- exactly 3. RED now (section absent).

    test-fileval-readback.cjs (FILEVAL-02 / D-02a, fixture-first): require navigation.fileEvidenceWithReadback (undefined now -> RED). Once it lands, assert against the fixture db: (a) a fixture evidence node lands with full provenance + review_status 'proposed'; (b) a simulated failed write returns {ok:false, reason:'filing_did_not_land'}; (c) the INFORMS edge lands; (d) artifact_path round-trips through the properties JSON; (e) the 4 locked provenance fields (source/url/retrieved_at/evidence_tier) are preserved unchanged.
  </action>
  <verify>
    <automated>for t in capability-dial-committed reach-ids-drift posture-ids-drift fileval-readback; do node tests/test-$t.cjs; echo "exit=$? (RED expected)"; done; true</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist and require without syntax error
    - test-reach-ids-drift.cjs asserts the exact 5-element set and uses comment-stripping grep hygiene (no bare `== 0` on unfiltered files)
    - test-posture-ids-drift.cjs asserts the exact 3-element set AND presence of the two Aronhime quote strings
    - test-fileval-readback.cjs builds its evidence node from the fixture, never opens a real room.db, and asserts review_status === 'proposed'
    - Each suite exits non-zero (RED) against the current tree
  </acceptance_criteria>
  <done>The doctrine and FILEVAL suites exist and fail RED; the full run-all-141.sh now lists and exercises all 9.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> room.db | Tests build their own in-memory fixture; never read a user's real room.db |
| no network | Wave-0 suites are pure local; zero external calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-01 | Information disclosure | fixture room.db | accept | In-memory only, synthetic data, never touches a real room |
| T-141-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs zero packages; pure node:sqlite built-in. No package surface. |
</threat_model>

<verification>
`bash tests/run-all-141.sh` runs to completion (no crash) and reports all 9 suites RED or SKIP. The fixture builder runs without error.
</verification>

<success_criteria>
All 10 Wave-0 artifacts from 141-VALIDATION.md exist; every requirement has a named automated command; the runner aggregates them and exits non-zero while implementations are pending.
</success_criteria>

<output>
Create `.planning/phases/141-local-retrieval-spine-and-capability-dial/141-01-SUMMARY.md` when done
</output>
