---
phase: 141-local-retrieval-spine-and-capability-dial
plan: 06
type: execute
wave: 2
depends_on: ["141-01", "141-03"]
files_modified:
  - scripts/intent-classifier.cjs
autonomous: true
requirements: [RETR-02]
must_haves:
  truths:
    - "The per-turn hot path stops forwarding userText:null -- it forwards a conversation seed from the last ~2 turns into LOCAL retrieval"
    - "The un-nulled userText flows ONLY to the local seed lane (getRoomContext); it never reaches buildBrainPacket / brain-client (Canon Part 8)"
    - "Per-turn assembly stays inside the 1200ms NAV_HARD_TIMEOUT_MS Promise.race envelope"
  artifacts:
    - path: "scripts/intent-classifier.cjs"
      provides: "The per-turn turn object carrying a real conversation seed on the LOCAL lane only"
      contains: "userText"
  key_links:
    - from: "scripts/intent-classifier.cjs"
      to: "the LOCAL seed lane (getRoomContext)"
      via: "un-nulled userText seeds local retrieval only"
      pattern: "userText"
---

<objective>
Flip the live per-turn seed (RETR-02 / D-03): un-null userText at the intent-classifier.cjs:1081 seam so retrieval seeds from the last ~2 conversation turns instead of venture-state only. This is what closes the conversation-to-retrieval loop now that getRoomContext (Plan 03) exists to consume the seed. HARD FENCE (D-03a): the un-nulled value flows to the LOCAL seed lane ONLY; it MUST NOT reach buildBrainPacket / brain-client. The Brain continues to receive generic handles only (Part 8).

Purpose: Without this flip getRoomContext has no per-turn conversation seed and "do you remember X" stays unanswered. This is the wiring that makes the loop fire per turn.
Output: the seam edit in intent-classifier.cjs + the RETR-02 + Part-8-fence tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/141-local-retrieval-spine-and-capability-dial/141-RESEARCH.md
@.planning/phases/141-local-retrieval-spine-and-capability-dial/141-PATTERNS.md
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- The exact seam + envelope. Extracted from the codebase. -->

From scripts/intent-classifier.cjs:
- :1080-1084 -- the turn object TODAY: { userText: null, // hot path does not forward prompt content / sectionPath / sessionId }. RETR-02 un-nulls userText.
- :635 -- const NAV_HARD_TIMEOUT_MS = 1200;
- :1196 -- callDecideWithTimeout(navEngine.decide, turn, context, NAV_HARD_TIMEOUT_MS).then(...) -- assembly must finish inside this Promise.race

From lib/core/navigation.cjs (Plan 03, landed in Wave 1):
- getRoomContext(db, roomId, opts) -- the LOCAL seed lane consumer; the seed feeds here, never the Brain
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-null userText on the LOCAL seed lane only</name>
  <read_first>
    - scripts/intent-classifier.cjs:1078-1084 (the turn object with userText:null) + :635 (NAV_HARD_TIMEOUT_MS) + :1196 (the Promise.race envelope)
    - scripts/intent-classifier.cjs around the buildBrainPacket / brain-client call site (the path the seed MUST NOT thread toward -- confirm where the Brain packet is built so the seed is fenced away from it)
    - lib/core/navigation.cjs getRoomContext (Plan 03 -- the LOCAL consumer the seed feeds)
    - tests/test-retrieval-seed.cjs + tests/test-room-context-part8-invariant.cjs (Plan 01 -- the RED suites this turns GREEN, including the D-03a brain-fence assertion)
  </read_first>
  <action>
    In scripts/intent-classifier.cjs, replace the hard-coded `userText: null` in the per-turn turn object (:1081) with the actual last ~2 turns of prompt content, so getRoomContext can derive a conversation seed. Source the seed from the data already available in the hot path (the recent prompt / fragments); window it to the last ~2 turns to respect the latency budget.

    HARD FENCE (D-03a): route the un-nulled userText into the LOCAL seed lane ONLY (the getRoomContext path). It MUST NOT be added to any payload that reaches buildBrainPacket or brain-client. If the same turn object or context is also consumed by the Brain path, keep the Brain-bound payload on generic handles only -- do NOT let the raw userText thread toward the Brain. The assembly must complete inside the NAV_HARD_TIMEOUT_MS (1200ms) Promise.race envelope at :1196; if seeding adds cost, window tighter rather than widening the budget.

    House rule: no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-retrieval-seed.cjs && node tests/test-room-context-part8-invariant.cjs && bash tests/run-all-141.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "userText: null" scripts/intent-classifier.cjs` returns nothing (the null seam is gone)
    - tests/test-retrieval-seed.cjs exits 0: the turn forwards a non-null conversation seed AND no userText assignment threads toward buildBrainPacket/brain-client in the hot-path block
    - tests/test-room-context-part8-invariant.cjs exits 0 (the Brain still receives generic handles only -- D-03a fence holds)
    - The change stays inside the 1200ms Promise.race envelope (no new blocking call outside callDecideWithTimeout)
    - bash tests/run-all-141.sh runs to completion with the seed + Part-8 suites green
    - `grep -c $'—' scripts/intent-classifier.cjs` returns 0
  </acceptance_criteria>
  <done>The per-turn loop carries a real conversation seed on the LOCAL lane only; the Brain still gets generic handles; the 1200ms budget holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-turn prompt -> LOCAL retrieval | The un-nulled userText is user content; it may seed local retrieval freely (LOCAL stays LOCAL) |
| per-turn prompt -> BRAIN (Part 8) | The SAME un-nulled userText must NEVER cross to the Brain packet; this is the dominant phase concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-11 | Information disclosure | un-nulled userText leaking to the Brain packet | mitigate | D-03a LOCAL-lane fence; the Part-8 adversarial test asserts no userText threads toward buildBrainPacket/brain-client |
| T-141-12 | Denial of service | seeding blows the 1200ms per-turn budget | mitigate | Window the seed to last ~2 turns; stay inside the existing Promise.race envelope |
| T-141-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed; in-place edit to an existing script. No package surface. |
</threat_model>

<verification>
test-retrieval-seed.cjs + test-room-context-part8-invariant.cjs green; no userText:null remains; the Brain fence holds; run-all-141.sh complete.
</verification>

<success_criteria>
RETR-02 satisfied: the per-turn loop stops forwarding userText:null and seeds LOCAL retrieval from the last ~2 turns, with the Part-8 fence (D-03a) asserted and the 1200ms budget held.
</success_criteria>

<output>
Create `.planning/phases/141-local-retrieval-spine-and-capability-dial/141-06-SUMMARY.md` when done
</output>
