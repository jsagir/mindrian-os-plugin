---
phase: 141-local-retrieval-spine-and-capability-dial
plan: 04
type: execute
wave: 2
depends_on: ["141-01", "141-03"]
files_modified:
  - lib/core/navigation/file-evidence-readback.cjs
  - lib/core/navigation.cjs
autonomous: true
requirements: [FILEVAL-02]
must_haves:
  truths:
    - "A research/decision conclusion files to room.db as a typed evidence node with provenance via the SHIPPED writeEvidenceClaim, never a new parallel node"
    - "After the write, a read-back SELECT asserts the row landed with expected provenance; a filing that did not land is surfaced (ok:false), not swallowed"
    - "The evidence node carries an additive artifact_path provenance field reserving the nested-fractal-artifact path, without renaming or dropping the 4 locked provenance fields"
    - "The filed node is review_status 'proposed' -- never auto-confirmed (Part 9 role 5)"
  artifacts:
    - path: "lib/core/navigation/file-evidence-readback.cjs"
      provides: "fileEvidenceWithReadback wrapper: writeEvidenceClaim + INFORMS edge + read-back assertion, all transactional"
      exports: ["fileEvidenceWithReadback"]
      min_lines: 40
    - path: "lib/core/navigation.cjs"
      provides: "Part 9 chokepoint re-exporting fileEvidenceWithReadback"
      contains: "fileEvidenceWithReadback"
  key_links:
    - from: "lib/core/navigation/file-evidence-readback.cjs"
      to: "writeEvidenceClaim / writeEdge"
      via: "wrap the shipped writers, never rebuild"
      pattern: "writeEvidenceClaim|writeEdge"
---

<objective>
Build the FILEVAL-02 substrate as a thin read-back-validation WRAPPER over the already-shipped writeEvidenceClaim chokepoint (Part 7 reuse-before-build) -- NOT a new evidence node. After writing the typed EvidenceClaim node + its INFORMS edge in one transaction, read the row back and assert it landed with the expected provenance; surface a failed filing as a structured ok:false (the FILEVAL honesty rule), never swallow it. Reserve ONE additive artifact_path provenance field (D-10) for the future nested-fractal-artifact path, without touching the 4 locked Phase-136 provenance fields. Built test-first against a fixture (D-02a) -- 141 has no live producer; its first consumers are deferred DRSCH execution + Phase 143 FILEVAL-01, so "unused-consumer" is expected.

Purpose: Closes the founding loop's write side -- a decision becomes typed graph data, and the system never claims a filing landed when it did not.
Output: lib/core/navigation/file-evidence-readback.cjs + a navigation.cjs re-export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/141-local-retrieval-spine-and-capability-dial/141-RESEARCH.md
@.planning/phases/141-local-retrieval-spine-and-capability-dial/141-PATTERNS.md
@docs/MINDRIAN-CANON.md
@.claude/includes/decisions.md

<interfaces>
<!-- The shipped writers to wrap. Extracted from the codebase; wrap directly, never rebuild. -->

From lib/core/navigation/evidence-claim.cjs (the SHIPPED node writer to wrap):
- writeEvidenceClaim(db, params) -> {ok, node_id} | {ok:false, reason}
- LOCKED Phase-136 provenance schema (evidence-claim.cjs:82-89), props: {source, url, retrieved_at, evidence_tier, topic, summary}. D-10 adds artifact_path PURELY ADDITIVELY; NEVER rename/drop source/url/retrieved_at/evidence_tier.
- review_status:'proposed' + created_by:'system' + UPSERT keyed on EvidenceClaim:<sessionId>:<urlHash> (evidence-claim.cjs:108-114)
- evidence_tier validated against Object.freeze(new Set(['Academic','Operational','Practitioner','None'])) (evidence-claim.cjs:48)
- caller-owned db handle rule (evidence-claim.cjs:10-17): never require node:sqlite, never open room.db

From lib/core/navigation/edges.cjs (the SHIPPED edge writer):
- writeEdge(db, {source_id, target_id, edge_type, properties}) -> {ok, edge_id, type, source, target}
- edge_type validated against the frozen ALLOWED_EDGE_TYPES Set (edges.cjs:32); INFORMS is allowed

From lib/core/findings-wirer.cjs:147-208 (the node-then-INFORMS producer pattern to mirror, surfacing each failure)
From lib/core/navigation/focus.cjs:49-80 (the hand-rolled BEGIN/COMMIT/ROLLBACK transaction idiom; node:sqlite has no transaction(fn) helper)
From lib/core/memory-events.cjs:317 (research_filed already in EVENT_TYPES -- reuse it if logging an event; no bump needed)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build fileEvidenceWithReadback wrapping the shipped writers</name>
  <behavior>
    - fileEvidenceWithReadback(db, params) files a fixture evidence node and returns {ok:true, node_id} with the node landing review_status 'proposed' and full provenance
    - A simulated failed write (e.g. read-back finds no row / wrong provenance) returns {ok:false, reason:'filing_did_not_land', node_id}
    - The INFORMS edge from the evidence node to its target lands; an edge failure returns {ok:false, reason:'informs_edge_failed', node_id}
    - artifact_path round-trips through the properties JSON (write it, read it back unchanged)
    - The 4 locked provenance fields (source, url, retrieved_at, evidence_tier) are preserved unchanged -- artifact_path is purely additive
  </behavior>
  <read_first>
    - lib/core/navigation/evidence-claim.cjs:10-17,48,65-119,82-89,108-114 (the wrapped writer, the locked schema, the tier Set, the caller-owned-handle rule)
    - lib/core/findings-wirer.cjs:147-208 (node-then-INFORMS, structured failure at each step, never swallow)
    - lib/core/navigation/focus.cjs:49-80 (the hand-rolled BEGIN/COMMIT/ROLLBACK to mirror)
    - lib/core/navigation/edges.cjs:32,194 (writeEdge + ALLOWED_EDGE_TYPES; INFORMS is allowed)
    - migrations/phase-109-nodes-provenance.cjs:95-108 (the additive pattern reference) -- but note artifact_path is a new KEY in the existing properties TEXT blob, NO ALTER TABLE needed
    - tests/test-fileval-readback.cjs + tests/fixtures/room-141-fixture.cjs (Plan 01 -- the RED suite + fixture this turns GREEN)
  </read_first>
  <action>
    Create lib/core/navigation/file-evidence-readback.cjs exporting fileEvidenceWithReadback(db, params). Caller-owned db handle only (mirror evidence-claim.cjs:10-17) -- never require node:sqlite, never open room.db.

    Inside a hand-rolled BEGIN/COMMIT/ROLLBACK transaction (mirror focus.cjs:49-80): (1) call the shipped writeEvidenceClaim(db, params) -- pass through {source, url, retrieved_at, evidence_tier, topic, summary, sessionId} plus the optional artifact_path; if it returns ok:false, ROLLBACK and return its structured reason. (2) Write the INFORMS edge from the new node_id to params.targetId via the shipped writeEdge(db, {source_id, target_id, edge_type:'INFORMS', properties}); on failure ROLLBACK and return {ok:false, reason:'informs_edge_failed', node_id}. (3) COMMIT.

    Then the NET-NEW read-back honesty layer (no shipped path has this): SELECT id, type, review_status, source_path, properties FROM nodes WHERE id = node_id. Assert the row exists, review_status === 'proposed', the 4 locked provenance fields are intact, and artifact_path round-tripped. On any mismatch return {ok:false, reason:'filing_did_not_land', node_id} -- surface it, do not swallow.

    For the artifact_path field (D-10): add it as ONE optional key inside the existing properties JSON blob -- NO column migration (properties is already TEXT). It reserves the path template <section>/<research-topic-slug>/<research-topic-slug>.md (Decision 16). NEVER rename or drop source/url/retrieved_at/evidence_tier (the LOCKED Phase-136 forward-contract consumed by Phase 136 getConfirmedFacts). The schema must not preclude the Phase-143 MEMDIAL render-from-graph projection (D-09).

    Defensive structured-return throughout: {ok, node_id} | {ok:false, reason} with short stable reason strings. If logging a memory_event, reuse research_filed (already in EVENT_TYPES) -- no bump. House rule: no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-fileval-readback.cjs</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/navigation/file-evidence-readback.cjs exports fileEvidenceWithReadback and is at least 40 lines
    - It requires and calls the SHIPPED writeEvidenceClaim + writeEdge -- `grep -E "writeEvidenceClaim|writeEdge" lib/core/navigation/file-evidence-readback.cjs` returns both
    - `grep -E "INSERT INTO nodes" lib/core/navigation/file-evidence-readback.cjs` returns nothing (no parallel node writer -- it wraps, does not rebuild)
    - tests/test-fileval-readback.cjs exits 0: node lands proposed, read-back catches a simulated failed write, INFORMS edge lands, artifact_path round-trips, 4 locked fields preserved
    - The module never requires node:sqlite and never opens room.db
    - `grep -c $'—' lib/core/navigation/file-evidence-readback.cjs` returns 0
  </acceptance_criteria>
  <done>The read-back wrapper files a fixture evidence node, validates it landed, reserves artifact_path, and surfaces a failed filing.</done>
</task>

<task type="auto">
  <name>Task 2: Re-export fileEvidenceWithReadback through the navigation.cjs chokepoint</name>
  <read_first>
    - lib/core/navigation.cjs:192-196 (the writeEvidenceClaim re-export -- add fileEvidenceWithReadback next to it with the same doc-comment style)
    - lib/core/navigation/file-evidence-readback.cjs (Task 1 -- the module to re-export)
  </read_first>
  <action>
    In lib/core/navigation.cjs, require file-evidence-readback.cjs and re-export fileEvidenceWithReadback via the thin additive idiom next to writeEvidenceClaim (:194), with a doc-comment noting it is the read-back-validated filing wrapper (FILEVAL honesty rule). This plan runs in Wave 2 after Plan 03 has already landed its getRoomContext + getSessionHistory re-exports in the same file, so add only this one new export. House rule: no em-dashes.
  </action>
  <verify>
    <automated>node -e "const n=require('./lib/core/navigation.cjs'); if(typeof n.fileEvidenceWithReadback!=='function') process.exit(1); console.log('chokepoint export present')" && bash tests/run-all-141.sh</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/navigation.cjs re-exports fileEvidenceWithReadback (resolves to a function)
    - The Plan 03 re-exports (getRoomContext, getSessionHistory) are still present (no clobber)
    - bash tests/run-all-141.sh runs to completion with the FILEVAL suite green
    - `grep -c $'—' lib/core/navigation.cjs` returns 0
  </acceptance_criteria>
  <done>fileEvidenceWithReadback is reachable through the Part 9 chokepoint, ready for deferred DRSCH + Phase 143 producers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| producer -> room.db | The filing writes a truth-claim node; it must land as 'proposed', never auto-confirmed (Part 9 role 5) |
| filing -> honesty floor | A write that did not land must be surfaced, not assumed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-07 | Repudiation | a filing silently failing (fake recall) | mitigate | The read-back SELECT + assertion returns ok:false on any mismatch; the honesty rule surfaces it |
| T-141-08 | Elevation of privilege | an agent confirming a truth claim without a human | mitigate | review_status stays 'proposed'; the test asserts it; promotion to confirmed is a human Decision Gate (out of scope) |
| T-141-09 | Tampering | renaming/dropping the locked Phase-136 provenance fields | mitigate | artifact_path is purely additive; the test asserts the 4 locked fields are preserved |
| T-141-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed; pure in-repo CJS over node:sqlite built-in. No package surface. |
</threat_model>

<verification>
test-fileval-readback.cjs green; navigation.cjs exposes fileEvidenceWithReadback; no parallel INSERT INTO nodes; the 4 locked provenance fields preserved; run-all-141.sh complete with the FILEVAL suite green.
</verification>

<success_criteria>
FILEVAL-02 satisfied: a typed evidence node files with provenance through the shipped writer, a read-back validates it landed, artifact_path is reserved additively, and a failed filing is surfaced -- all fixture-first.
</success_criteria>

<output>
Create `.planning/phases/141-local-retrieval-spine-and-capability-dial/141-04-SUMMARY.md` when done
</output>
