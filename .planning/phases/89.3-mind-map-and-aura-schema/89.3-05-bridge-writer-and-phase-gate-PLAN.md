---
phase: 89.3
plan: "05"
slug: bridge-writer-and-phase-gate
type: execute
wave: 4
depends_on: ["89.3-01", "89.3-02", "89.3-03", "89.3-04"]
files_modified:
  - lib/core/bridge-writer.cjs
  - lib/memory/test-bridge-writer-enhanced.cjs
  - lib/memory/run-feynman-tests.cjs
  - .planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md
  - .planning/phases/89.3-mind-map-and-aura-schema/89.3-VERIFICATION.md
autonomous: true
canon_parts: [part-7, part-8]
milestone: v1.11.0
bundled_release: true
success_criteria_addressed: [SC-05, SC-06, SC-07, SC-08, SC-09]

must_haves:
  truths:
    - "bridge-writer.cjs renderBridgeArtifact ENHANCED in place to inject 2 new frontmatter fields (thesis + breakthrough_score) when the input pair carries them; existing pre-89.3 fields preserved BYTE-FOR-BYTE; existing pre-89.3 consumers unaffected (regression test proves this with a real v1.10.16-vintage pair input rendering byte-identically)"
    - "Enhancement is BACKWARD-COMPATIBLE: when pair.thesis OR pair.breakthrough is missing/null, the new fields are omitted entirely (no thesis: line, no breakthrough_score: line) so v1.10.16-vintage inputs render byte-identically to pre-89.3 output. Existing consumers reading pre-89.3 fields find them unchanged."
    - "5 NEW Phase 89.3 test files registered in lib/memory/run-feynman-tests.cjs TEST_FILES array (single sweep, no Wave-1/Wave-2 plan-level contention): test-rs-neo4j-writer + test-rs-sqlite-mirror + test-rs-mind-map + test-rs-expert-mapper + test-bridge-writer-enhanced. Baseline 77 (post-89.2) -> 82 (post-89.3)"
    - "Phase Gate transcript at .planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md mirrors 89.2-PHASE-GATE.md format: G1-G9 + 5 NEW E asserts (E1-E5 are 89.3-specific: E1 = lazygraph-ops EDGE_TYPES extended without forking + E2 = Tier 0/1 dispatch verified across writer pair + E3 = bridge-writer regression test green + E4 = auditQueryObject defense-in-depth on writer pair + mind-map HTML output + E5 = end-to-end smoke: writer -> mind-map -> expert-mapper chain composes)"
    - "Phase 89.3 VERIFICATION.md mirrors 89.2-VERIFICATION.md format: 9 SCs from ROADMAP with PASS/FAIL evidence per SC"
    - "All 9 ROADMAP SCs have direct evidence pointers in VERIFICATION.md"
    - "Phase Gate PASS gate: zero G/E asserts FAIL; full Feynman 82+/82+ PASS (the 3 pre-existing failures from 89.2 may persist as CONDITIONAL PASS exactly as documented in 89.2-PHASE-GATE.md G8)"
    - "NO release artifacts touched (CHANGELOG / plugin.json / package.json all byte-identical at HEAD vs Plan 89.3-01 base commit per bundled_release: true)"
  artifacts:
    - path: lib/core/bridge-writer.cjs
      provides: "renderBridgeArtifact enhanced with 2 new frontmatter fields (thesis + breakthrough_score) when input pair carries them; backward-compatible with existing v1.10.16 consumers"
      min_lines: 427
    - path: lib/memory/test-bridge-writer-enhanced.cjs
      provides: "8 scenarios: pre-89.3 input renders byte-identically to v1.10.16 baseline (regression) + 89.3 input with thesis renders new field + 89.3 input with breakthrough renders new field + 89.3 input with both renders both + missing thesis omits gracefully + missing breakthrough omits gracefully + 2 adversarial Canon Part 8 (forbidden in pair.thesis OR pair.breakthrough.dominant_dimension)"
      min_lines: 280
    - path: lib/memory/run-feynman-tests.cjs
      provides: "+5 TEST_FILES entries (one per new 89.3 test file); baseline 77 -> 82"
      min_lines: 580
    - path: .planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md
      provides: "Phase Gate transcript with 9 G + 5 E asserts; PASS/FAIL evidence per assert; mirrors 89.2-PHASE-GATE.md byte-for-byte structure"
      min_lines: 220
    - path: .planning/phases/89.3-mind-map-and-aura-schema/89.3-VERIFICATION.md
      provides: "9 SCs from ROADMAP with PASS/FAIL evidence; mirrors 89.2-VERIFICATION.md format"
      min_lines: 120
  key_links:
    - from: "lib/core/bridge-writer.cjs::renderBridgeArtifact"
      to: "Plan 89.2-07 generateThesis output (pair.thesis + pair.breakthrough.score + pair.breakthrough.dominant_dimension)"
      via: "consumes optional fields; emits new frontmatter when present; omits when absent (backward compat)"
      pattern: "thesis|breakthrough"
    - from: "lib/memory/test-bridge-writer-enhanced.cjs::regression"
      to: "lib/core/bridge-writer.cjs::renderBridgeArtifact (v1.10.16 baseline behavior)"
      via: "asserts pre-89.3 input renders byte-identically to a captured v1.10.16-vintage golden output"
      pattern: "byte-identical"
    - from: ".planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md"
      to: ".planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md"
      via: "Phase Gate template inheritance; 89.3 G asserts mirror 89.2 G1-G9; 89.3 E asserts are phase-specific"
      pattern: "G[1-9]"
    - from: ".planning/phases/89.3-mind-map-and-aura-schema/89.3-VERIFICATION.md"
      to: ".planning/ROADMAP.md (Phase 89.3 Success Criteria 1-9)"
      via: "9 SC evidence pointers"
      pattern: "SC-[0-9]+"
    - from: "lib/memory/run-feynman-tests.cjs"
      to: "lib/memory/test-rs-neo4j-writer.cjs + test-rs-sqlite-mirror.cjs + test-rs-mind-map.cjs + test-rs-expert-mapper.cjs + test-bridge-writer-enhanced.cjs (5 new files from Plans 89.3-01 through 89.3-04 + this plan)"
      via: "TEST_FILES registry"
      pattern: "test-rs-|test-bridge-writer-enhanced"
---

<objective>
Close Phase 89.3 with a single sweep that:

1. **Enhances `lib/core/bridge-writer.cjs`** in place to inject 2 new frontmatter fields (`thesis` + `breakthrough_score`) when the input pair carries them. Backward-compatible: existing v1.10.16 consumers reading pre-89.3 fields find them unchanged. The enhancement is OPTIONAL (omits when missing). Includes a regression test proving a v1.10.16-vintage input renders byte-identically.

2. **Registers all 5 new Phase 89.3 test files** in `lib/memory/run-feynman-tests.cjs` TEST_FILES array. Single-sweep approach avoids parallel-plan contention on the registry (Plans 89.3-01/02/03/04 ran in parallel-friendly waves and never touched run-feynman-tests.cjs to avoid merge conflicts on the same array). Baseline 77 (post-89.2) -> 82 (post-89.3).

3. **Files Phase 89.3 Phase Gate transcript** at `.planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md` mirroring `89.2-PHASE-GATE.md` byte-for-byte structure. 9 G asserts (BSL 1.1 + em-dashes + Canon Part 8 forbidden-field audit + chokepoint exclusivity + Feynman registration + suite green + zero release artifacts + zero new deps + ExternalEgressViolation surface map) plus 5 NEW E asserts specific to 89.3:
   - E1 = lazygraph-ops EDGE_TYPES extended without forking (Canon Part 7 verified)
   - E2 = Tier 0 / Tier 1 dispatch verified across writer pair (mocked isAuraConnected=true vs false; correct writer module invoked)
   - E3 = bridge-writer regression test green (existing consumers unaffected)
   - E4 = auditQueryObject defense-in-depth on rs-neo4j-writer + rs-sqlite-mirror + rs-mind-map HTML output + rs-expert-mapper 2-seam
   - E5 = end-to-end smoke: writer (rs-sqlite-mirror) -> mind-map (rs-mind-map reads same room.db) -> expert-mapper (rs-expert-mapper Tier 0 reads same room.db) chain composes cleanly

4. **Files Phase 89.3 VERIFICATION.md** at `.planning/phases/89.3-mind-map-and-aura-schema/89.3-VERIFICATION.md` listing all 9 ROADMAP SCs with PASS/FAIL evidence per SC. Mirror `89.2-VERIFICATION.md` format.

NO release artifacts touched per bundled_release: true. v1.11.0 ships at Phase 91.x.

Purpose: Ships SC-05 (bridge-writer enhanced; existing consumers unaffected via regression test) + SC-06 (Tier 0/1 dispatch verified end-to-end via E2 + E5 asserts) + SC-07 (lazygraph-ops EDGE_TYPES extended without forking; Canon Part 7 verified by E1 grep audit) + SC-08 (Feynman test suite stays green + 5 new test files registered; baseline 77 -> 82) + SC-09 (NO release gate -- bundled into v1.11.0 final ship at Phase 91.x; verified by zero release artifact diffs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89.3-mind-map-and-aura-schema/89.3-CONTEXT.md
@.planning/phases/89.3-mind-map-and-aura-schema/89.3-01-rs-neo4j-writer-PLAN.md
@.planning/phases/89.3-mind-map-and-aura-schema/89.3-02-rs-sqlite-mirror-PLAN.md
@.planning/phases/89.3-mind-map-and-aura-schema/89.3-03-rs-mind-map-PLAN.md
@.planning/phases/89.3-mind-map-and-aura-schema/89.3-04-rs-expert-mapper-PLAN.md
@.planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md
@.planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md
@.planning/phases/89.2-external-research-fetching/89.2-08-feynman-registration-and-phase-gate-PLAN.md
@lib/core/bridge-writer.cjs
@lib/memory/run-feynman-tests.cjs
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- bridge-writer.cjs current renderBridgeArtifact frontmatter (BYTE-FOR-BYTE preserve these 13 fields) -->

```yaml
# --- (frontmatter fence; replaced for validator compat)
brain_framework_classical: "framework:reverse-salient-analysis"
brain_framework_algorithmic: "framework:algorithmic-generation-of-reverse-salient-solutions"
brain_chain_next: ["framework:cross-domain-innovation", "framework:medici-effect"]
brain_node_version: "2026-Q2"
bridge_id: "${bridgeId}"
mode: "${mode}"
lsa_score: ${lsa.toFixed(4)}
semantic_score: ${sem.toFixed(4)}
signed_diff: ${signedDiff.toFixed(4)}
abs_diff: ${absDiff.toFixed(4)}
direction: "${direction}"
source_room: "${identity.sourceRoom}"
target_room: "${identity.targetRoom}"
external_source: "${identity.externalSource}"
external_id: "${identity.externalId}"
generated_at: "${generatedAt}"
# --- (frontmatter fence; replaced for validator compat)
```

<!-- 2 NEW Phase 89.3 frontmatter fields (append after existing fields; emit ONLY when present) -->

Backward compatibility decision (planner-locked): emit the new fields ONLY when the source data carries them. If pair.thesis is missing OR pair.breakthrough is missing, do NOT emit those YAML keys at all. This means a v1.10.16-vintage input pair (no thesis, no breakthrough) renders BYTE-IDENTICAL frontmatter to the pre-89.3 version. The regression test (Test 1) verifies this byte-for-byte.

Alternative (rejected): always emit thesis: "" and breakthrough_score: null on missing. Rejected because it forces a frontmatter shape change for ALL v1.10.16-vintage inputs, breaking byte-identity.

<!-- Phase Gate G + E assert templates (mirror 89.2-PHASE-GATE.md byte-for-byte where possible) -->

G asserts inherited from 89.2:
- G1: Zero new runtime deps in package.json across 89.3 commit range
- G2: BSL 1.1 headers on every new/modified .cjs (5 production + 5 test = 10 files; bridge-writer.cjs already has BSL 1.1 from v1.10.16; lazygraph-ops.cjs unchanged in license; run-feynman-tests.cjs unchanged in license)
- G3: Zero em-dashes on every new/modified file (10 files)
- G4: Canon Part 8 audit (forbidden field references) on production .cjs (5 files)
- G5: Chokepoint exclusivity preserved (auditQueryObject called pre-write/pre-render in writer pair + mind-map + expert-mapper; bridge-writer enhanced gets new audit seam)
- G6: New Feynman suites registered (5 entries from 89.3): grep -c for the 5 new test file basenames returns 5
- G7: Each new fixture suite green (direct invocation): node lib/memory/test-rs-neo4j-writer.cjs && node lib/memory/test-rs-sqlite-mirror.cjs && node lib/memory/test-rs-mind-map.cjs && node lib/memory/test-rs-expert-mapper.cjs && node lib/memory/test-bridge-writer-enhanced.cjs all exit 0
- G8: Full Feynman runner green (CONDITIONAL PASS: 89.2 inherited 3 pre-existing failures persist; 89.3-owned 5 new suites all green; net baseline 82)
- G9: ExternalEgressViolation surfaces only in 89.3 production modules (NOT redefined in any new file; require'd from rs-egress-violations.cjs in all 5)

E asserts NEW for 89.3:
- E1: lazygraph-ops EDGE_TYPES extended without forking. Commands: `node -e "const { EDGE_TYPES } = require('./lib/core/lazygraph-ops.cjs'); console.log(EDGE_TYPES.length, EDGE_TYPES.includes('DISCOVERED'), EDGE_TYPES.includes('DERIVED_FROM'), EDGE_TYPES.includes('AUTHORED_BY'), EDGE_TYPES.includes('AFFILIATED_WITH'));"` returns `23 true true true true`. AND no new file named 'rs-edge-types.cjs' or similar fork: `ls lib/core/ | grep -cE "(edge-types|edges-constants|rs-edge)"` returns 0.
- E2: Tier 0 / Tier 1 dispatch verified across writer pair. Inline node smoke: rs-neo4j-writer.writeDiscovery({minimal pair}, {driver: mockAura}) returns expected shape; rs-sqlite-mirror.writeDiscovery({same minimal pair}, {roomDir: tmpDir}) returns same shape (with rollback_cypher carrying SQL instead of Cypher). Both share writeDiscovery signature.
- E3: bridge-writer regression test green. Specific command: `node lib/memory/test-bridge-writer-enhanced.cjs` returns exit 0 with Test 1 (regression) PASS in output.
- E4: auditQueryObject defense-in-depth across all 5 new modules. Per-file audit-call counts: rs-neo4j-writer >= 1, rs-sqlite-mirror >= 1, rs-mind-map >= 1, rs-expert-mapper >= 2 (2-seam), bridge-writer (enhanced) >= 1. Total auditQueryObject sites in 89.3 production code >= 6.
- E5: end-to-end smoke: writer -> mind-map -> expert-mapper chain composes. Inline node script per the executor's E5 evidence collection.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enhance bridge-writer.cjs in place + RED-GREEN regression test (8 scenarios)</name>
  <files>lib/core/bridge-writer.cjs, lib/memory/test-bridge-writer-enhanced.cjs</files>
  <read_first>
    - lib/core/bridge-writer.cjs lines 137-215 (current renderBridgeArtifact function; the 13-field frontmatter contract to preserve)
    - lib/core/bridge-writer.cjs lines 419-427 (module.exports; current export surface)
    - lib/core/rs-egress-violations.cjs (REQUIRE the SHARED ExternalEgressViolation; do NOT redefine)
    - lib/core/rs-egress-prompts.cjs (REQUIRE auditQueryObject; new audit seam in renderBridgeArtifact)
    - .planning/phases/89.2-external-research-fetching/89.2-07-SUMMARY.md (composite pair shape with thesis + breakthrough fields)
    - .planning/phases/89.3-mind-map-and-aura-schema/89.3-CONTEXT.md (bridge-writer enhancement decision: standalone Wave 3 plan because regression risk is high; backward-compat REQUIRED)
    - lib/memory/test-rs-fetcher-experts.cjs (test pattern; 8 scenarios sufficient for enhancement-with-regression)
    - docs/MINDRIAN-CANON.md Part 7 + Part 8
  </read_first>
  <behavior>
    Test 1 (REGRESSION -- v1.10.16-vintage input renders byte-identically): Input pair = {lsa_score: 0.5, semantic_score: 0.3, abs_diff: 0.2, signed_diff: -0.2, direction: 'structural_transfer', source_artifact_id: 'roomA/artifact-a', target_artifact_id: 'roomB/artifact-b', source_title: 'Source A', target_title: 'Target B'}; render with enhanced renderBridgeArtifact; rendered output contains all 13 pre-89.3 frontmatter fields verbatim (brain_framework_classical, brain_framework_algorithmic, brain_chain_next, brain_node_version, bridge_id, mode, lsa_score, semantic_score, signed_diff, abs_diff, direction, source_room, target_room, external_source, external_id, generated_at); rendered output does NOT contain 'thesis:' or 'breakthrough_score:' substrings (the new fields are conditional and absent when source pair lacks them).

    Test 2 (89.3 input with thesis only): Same pair as Test 1 + pair.thesis = 'By applying X to Y, achieve Z because mechanism.'; rendered output contains exactly one line `thesis: "By applying X to Y, achieve Z because mechanism."` AND does NOT contain 'breakthrough_score:'. Verify line position: thesis: line appears AFTER generated_at: and BEFORE the closing ---.

    Test 3 (89.3 input with breakthrough only): Same pair + pair.breakthrough = {score: 5.9, breakdown: {feasibility: 2, market: 1.5, magnitude: 1, advantage: 1.4, impact: 0}, dominant_dimension: 'feasibility'}; rendered output contains exactly one line `breakthrough_score: 5.90` AND does NOT contain 'thesis:'.

    Test 4 (89.3 input with BOTH): pair has both thesis and breakthrough; rendered output contains BOTH new lines. Verify both come AFTER generated_at and BEFORE the closing ---. Verify field order is consistent: thesis: line BEFORE breakthrough_score: line (alphabetical OR by-source-of-data; planner picks; either is fine -- locked once via fixture).

    Test 5 (missing thesis omits gracefully): pair has breakthrough but pair.thesis === null OR pair.thesis === '' OR pair.thesis is missing; rendered output emits the breakthrough_score line BUT does NOT emit a thesis line. Verify by `output.includes('thesis:')` returns false.

    Test 6 (missing breakthrough omits gracefully): pair has thesis but pair.breakthrough === undefined OR pair.breakthrough.score is not a number; rendered output emits the thesis line BUT does NOT emit a breakthrough_score line.

    Test 7 (CANON PART 8 adversarial #1 -- forbidden in pair.thesis): pair.thesis = 'By applying X to Y. governing_thought: production_leak'; renderBridgeArtifact runs auditQueryObject({rendered: outputString}, 'bridge-writer-enhanced'); throws ExternalEgressViolation with meta.surface === 'bridge-writer-enhanced' AND matched_pattern matching governing_thought.

    Test 8 (CANON PART 8 adversarial #2 -- forbidden in pair.breakthrough.dominant_dimension OR breakdown values): pair.breakthrough = {score: 5, dominant_dimension: 'identity_text exposed'}; rendered output would carry the leaked dominant_dimension if the renderer chose to emit it; the audit catches; throws ExternalEgressViolation. NOTE: even though dominant_dimension is not in the new frontmatter contract (only breakthrough_score is), the audit covers the WHOLE rendered output including any field the implementation might emit. Defense-in-depth.

    Suite-end audit A1: walk every captured rendered string from passing scenarios; JSON.stringify and scan against the full FORBIDDEN_PATTERNS set; assert ZERO hits across all happy-path scenarios.
  </behavior>
  <action>
    1. Write lib/memory/test-bridge-writer-enhanced.cjs FIRST (RED commit). Mirror lib/memory/test-rs-fetcher-experts.cjs structure (8 scenarios + A1 sweep). Setup helper:
       - BSL 1.1 header (Phase 89.3 Plan 05 -- bridge-writer enhanced fixture suite + regression test)
       - 'use strict';
       - require('node:assert/strict')
       - 8 scenarios + A1 sweep per Behavior block above
       - SCENARIO_RESULTS array captures every {result OR throw}
       - Final line: console.log('=== 89.3-05 bridge-writer-enhanced suite: 8/8 passed ===')
       Run `node lib/memory/test-bridge-writer-enhanced.cjs` -> EXPECT exit 1 (the new audit + new field logic doesn't exist yet in the in-place renderBridgeArtifact). Commit RED `--no-verify`: `test(89.3-05): RED -- failing 8-scenario bridge-writer-enhanced regression + 89.3 fields suite`.

    2. Edit lib/core/bridge-writer.cjs in place (GREEN). Surgical edits ONLY (do NOT rewrite the whole file):
       - At top of file (after existing comment block, before the first function), add: `const { ExternalEgressViolation } = require('./rs-egress-violations.cjs');` and `const { auditQueryObject } = require('./rs-egress-prompts.cjs');` and `const SURFACE_BRIDGE = 'bridge-writer-enhanced';`
       - In renderBridgeArtifact, AFTER the existing template literal that builds the frontmatter (current line ~167-184), BEFORE the body section starts:
         * Detect optional fields: `const hasThesis = typeof pair.thesis === 'string' && pair.thesis.length > 0;`
         * Detect breakthrough: `const hasBreakthrough = pair.breakthrough && typeof pair.breakthrough.score === 'number';`
         * Build the optional frontmatter lines as a separate string segment that's INSERTED into the template literal between `generated_at: ...` and the closing `---`:
           - if hasThesis: append `\nthesis: "${escapeYaml(pair.thesis)}"` (escapeYaml is a small inline helper that escapes double-quotes for safe YAML)
           - if hasBreakthrough: append `\nbreakthrough_score: ${pair.breakthrough.score.toFixed(2)}`
       - At the END of renderBridgeArtifact (just before returning the rendered string), add Canon Part 8 defense-in-depth: `auditQueryObject({rendered: out}, SURFACE_BRIDGE);` where out is the assembled markdown string. This throws ExternalEgressViolation if any forbidden pattern leaks through pair.thesis OR breakthrough fields into the rendered output.
       - module.exports section (line ~419-427) is UNCHANGED (no new exports; the enhancement is internal to renderBridgeArtifact).

    3. Run `node lib/memory/test-bridge-writer-enhanced.cjs`. EXPECT exit 0 with "8/8 passed". Iterate. Commit GREEN `--no-verify`: `feat(89.3-05): GREEN -- bridge-writer enhanced with thesis + breakthrough_score frontmatter (backward-compatible; regression test green; Canon Part 8 audit on rendered output)`.

    4. The atomic test for byte-identity (Test 1) is the load-bearing assertion. If the regression fails, the enhancement broke an existing consumer. Iterate until Test 1 passes WITHOUT modifying the v1.10.16-vintage golden expectations.
  </action>
  <verify>
    <automated>node lib/memory/test-bridge-writer-enhanced.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `test -f lib/memory/test-bridge-writer-enhanced.cjs && echo PASS` outputs PASS
    - bridge-writer.cjs unchanged in module.exports surface: `git diff HEAD~2 -- lib/core/bridge-writer.cjs | grep -cE "^[+-]module.exports"` returns 0
    - bridge-writer.cjs gains auditQueryObject import: `grep -c "auditQueryObject" lib/core/bridge-writer.cjs` returns >= 1
    - bridge-writer.cjs gains ExternalEgressViolation require: `grep -c "rs-egress-violations" lib/core/bridge-writer.cjs` returns >= 1
    - bridge-writer.cjs surface tag added: `grep -c "bridge-writer-enhanced" lib/core/bridge-writer.cjs` returns >= 1
    - bridge-writer.cjs handles optional thesis: `grep -c "pair.thesis" lib/core/bridge-writer.cjs` returns >= 1
    - bridge-writer.cjs handles optional breakthrough: `grep -c "pair.breakthrough" lib/core/bridge-writer.cjs` returns >= 1
    - BSL 1.1 preserved on bridge-writer.cjs (existing): `grep -c "BSL\\|BUSL" lib/core/bridge-writer.cjs` returns >= 1
    - BSL 1.1 on test file: `grep -c "BSL 1.1" lib/memory/test-bridge-writer-enhanced.cjs` returns >= 1
    - Zero em-dashes on both: `grep -cP '[\x{2013}\x{2014}]' lib/core/bridge-writer.cjs lib/memory/test-bridge-writer-enhanced.cjs | awk -F: '{if ($2 != 0) exit 1} END {print "PASS"}'` outputs PASS
    - CJS only: `grep -cE '^(import |export )' lib/core/bridge-writer.cjs` returns 0
    - Test suite has 2 adversarial scenarios: `grep -cE "(adversarial|CANON PART 8)" lib/memory/test-bridge-writer-enhanced.cjs` returns >= 2
    - Test suite final line: `node lib/memory/test-bridge-writer-enhanced.cjs 2>&1 | tail -1` contains "8/8 passed"
    - REGRESSION test passes: `node lib/memory/test-bridge-writer-enhanced.cjs 2>&1 | grep -cE "regression|byte-identical"` returns >= 1 AND exit code is 0
    - Both atomic commits: `git log --oneline -5 | grep -cE "(89.3-05).*RED|89.3-05.*GREEN"` returns 2
    - Zero new runtime deps: `git diff HEAD~2 -- package.json | wc -l` returns 0
    - Zero release artifacts touched: `git diff HEAD~2 -- CHANGELOG.md .claude-plugin/plugin.json | wc -l` returns 0
  </acceptance_criteria>
  <done>
    bridge-writer.cjs renderBridgeArtifact enhanced in place. 2 new optional frontmatter fields (thesis + breakthrough_score) emitted ONLY when input pair carries them; absent when input is v1.10.16-vintage (regression test verified). Backward-compatible: existing 13 frontmatter fields preserved verbatim; module.exports surface unchanged. Canon Part 8 defense-in-depth: auditQueryObject scans the FINAL rendered string before return; throws ExternalEgressViolation on hit. 8 scenarios green; A1 sweep clean. Regression Test 1 is the load-bearing assertion -- proves no existing consumer is broken. 2 adversarial Canon Part 8 scenarios cover thesis + breakthrough.dominant_dimension leak vectors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register all 5 new Phase 89.3 test files in run-feynman-tests.cjs (single sweep)</name>
  <files>lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - lib/memory/run-feynman-tests.cjs lines 555-560 (end of TEST_FILES array; insertion point for the 5 new entries)
    - lib/memory/run-feynman-tests.cjs lines 472-558 (existing 89.1a + 89.1 + 89.2 entries; the registration pattern to mirror)
    - .planning/phases/89.2-external-research-fetching/89.2-08-feynman-registration-and-phase-gate-PLAN.md (single-sweep registration approach inherited)
  </read_first>
  <action>
    1. Open lib/memory/run-feynman-tests.cjs.
    2. Locate the END of the TEST_FILES array (around line 558, after the test-rs-thesis-generator entry).
    3. Insert 5 NEW entries BEFORE the closing `];` of the TEST_FILES array. Each entry mirrors the existing 89.2 entry pattern (path.join with REPO_ROOT, 'lib', 'memory', and a short comment block above):
       - entry 1 comment: `// Phase 89.3-01: rs-neo4j-writer (Aura Cypher writer; idempotent MERGE; rollback Cypher; Canon Part 8 defense)`
       - entry 1: `path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-neo4j-writer.cjs'),`
       - entry 2 comment: `// Phase 89.3-02: rs-sqlite-mirror (Tier 0 SQLite writer; INSERT OR REPLACE; rollback SQL; Canon Part 8 defense)`
       - entry 2: `path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-sqlite-mirror.cjs'),`
       - entry 3 comment: `// Phase 89.3-03: rs-mind-map (5-branch Cytoscape; Tier 0/1 read paths; Canon Part 8 audit on HTML)`
       - entry 3: `path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-mind-map.cjs'),`
       - entry 4 comment: `// Phase 89.3-04: rs-expert-mapper (Cypher MATCH+MERGE; Tier 0 graceful degradation; Canon Part 8 2-seam defense)`
       - entry 4: `path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-expert-mapper.cjs'),`
       - entry 5 comment: `// Phase 89.3-05: bridge-writer-enhanced (regression-tested; thesis + breakthrough_score frontmatter; Canon Part 8 audit on rendered output)`
       - entry 5: `path.join(REPO_ROOT, 'lib', 'memory', 'test-bridge-writer-enhanced.cjs'),`
    4. Save. Run `node lib/memory/run-feynman-tests.cjs 2>&1 | tail -3`. Expected output: `Feynman test runner: N/82 passed, 0 skipped, M failed` where N >= 79 (the 5 new suites pass; the 3 pre-existing failures from 89.2 may persist exactly as documented in 89.2-PHASE-GATE.md G8 -- those are NOT in the 89.3 commit range).
    5. Commit `--no-verify`: `test(89.3-05): register 5 new 89.3 fixture suites in Feynman runner; baseline 77 -> 82`.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const text = fs.readFileSync('lib/memory/run-feynman-tests.cjs', 'utf8'); const required = ['test-rs-neo4j-writer.cjs', 'test-rs-sqlite-mirror.cjs', 'test-rs-mind-map.cjs', 'test-rs-expert-mapper.cjs', 'test-bridge-writer-enhanced.cjs']; for (const r of required) { if (!text.includes(r)) { console.error('Missing registration: ' + r); process.exit(1); } } console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 new entries present in TEST_FILES: `grep -cE "test-rs-neo4j-writer|test-rs-sqlite-mirror|test-rs-mind-map|test-rs-expert-mapper|test-bridge-writer-enhanced" lib/memory/run-feynman-tests.cjs` returns >= 5
    - Phase 89.3 comment blocks present: `grep -cE "Phase 89.3-0[1-5]" lib/memory/run-feynman-tests.cjs` returns >= 5
    - Syntax valid: `node --check lib/memory/run-feynman-tests.cjs && echo PASS` outputs PASS
    - Each new test file invokes successfully: `node lib/memory/test-rs-neo4j-writer.cjs && node lib/memory/test-rs-sqlite-mirror.cjs && node lib/memory/test-rs-mind-map.cjs && node lib/memory/test-rs-expert-mapper.cjs && node lib/memory/test-bridge-writer-enhanced.cjs && echo ALL_PASS` outputs ALL_PASS
    - Feynman runner total entries advanced 77 -> 82: `grep -cE "REPO_ROOT, 'lib', 'memory'" lib/memory/run-feynman-tests.cjs` returns >= 20 (sanity floor; the actual list is much longer)
    - Atomic commit: `git log --oneline -3 | grep -c "89.3-05.*register"` returns >= 1
    - Zero release artifacts touched: `git diff HEAD~1 -- CHANGELOG.md .claude-plugin/plugin.json | wc -l` returns 0
  </acceptance_criteria>
  <done>
    All 5 new Phase 89.3 test files registered in lib/memory/run-feynman-tests.cjs TEST_FILES array as a single-sweep commit. Baseline advances 77 -> 82 (89.3-owned 5 new suites added). The runner can now invoke all 5 new suites in series; each PASSes when invoked directly (verified via Task 1 + the 4 prior plans). The 3 pre-existing failures from 89.2 (test-self-update-platform + 84-smart-notebook-copilot Test 15 + post-compact-reinjection Test 9) MAY persist as inherited baseline drift; they are NOT in the 89.3 commit range and are tracked separately.
  </done>
</task>

<task type="auto">
  <name>Task 3: File Phase 89.3 Phase Gate transcript + VERIFICATION.md</name>
  <files>.planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md, .planning/phases/89.3-mind-map-and-aura-schema/89.3-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md (full template; mirror byte-for-byte structure with 89.3-specific E asserts)
    - .planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md (full template; mirror SC-by-SC evidence pointer pattern)
    - .planning/ROADMAP.md lines 469-496 (Phase 89.3 entry; 9 Success Criteria)
    - All 5 89.3 SUMMARY.md files (when they exist post-execution; if not yet present, plan documents this gap)
  </read_first>
  <action>
    1. File `.planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md`. Mirror 89.2-PHASE-GATE.md byte-for-byte structure with these adjustments:
       - frontmatter: phase=89.3, slug=mind-map-and-aura-schema, plan="05", milestone=v1.11.0, bundled_release=true, canon_parts=[part-7, part-8], status=PASS|CONDITIONAL_PASS depending on G8.
       - Base commit for diff: the commit immediately before Plan 89.3-01's first commit (the EDGE_TYPES extension); discover via `git log --oneline lib/core/lazygraph-ops.cjs -3` and pick the parent of the first 89.3-01 commit.
       - HEAD at time of assert run: the commit hash AFTER this Plan 89.3-05 Task 3 commits VERIFICATION.md.
       - Gate Summary: 14 asserts (9 G + 5 E); count PASSes; status PASS or CONDITIONAL PASS depending on G8 inheritance.
       - G1 through G9 asserts per template inherited from 89.2; substitute 89.3-specific file lists (5 production .cjs + 5 test .cjs + 1 modified bridge-writer.cjs + 1 modified lazygraph-ops.cjs + 1 modified run-feynman-tests.cjs = 13 unique files in commit range).
       - E1: lazygraph-ops EDGE_TYPES extended without forking. Commands per interface block.
       - E2: Tier 0 / Tier 1 dispatch verified across writer pair. Inline node smoke per interface block.
       - E3: bridge-writer regression test green. Specific command + observed output.
       - E4: auditQueryObject defense-in-depth across all 5 new modules. Per-file count.
       - E5: end-to-end smoke writer -> mind-map -> expert-mapper. Inline node script:
         ```
         const writer = require('./lib/core/rs-sqlite-mirror.cjs');
         const mindMap = require('./lib/core/rs-mind-map.cjs');
         const expertMapper = require('./lib/core/rs-expert-mapper.cjs');
         const tmpRoom = '/tmp/89.3-e5-' + Date.now();
         await writer.writeDiscovery(minimalPair, {roomDir: tmpRoom});
         const mm = await mindMap.renderMindMap({}, {tier: 'sqlite', roomDir: tmpRoom});
         const em = await expertMapper.mapAuthorsToAura([{name: 'Test', orcid: null, institution: null, paper_count: 1, h_index_estimate: 1, public_email_or_null: null}], {tier: 'sqlite', roomDir: tmpRoom});
         // Assertions: mm.metadata.total_nodes >= 3; em.resolution_quality === 'degraded'; em.note matches /Aura/
         ```
       - Invariant Coverage section: list inherited 89.2 invariants preserved by 89.3 + new 89.3 invariants (idempotent MERGE / INSERT OR REPLACE; deterministic ids; Tier 0/1 schema parity; backward-compat bridge-writer).
       - Gate Signature section: byte-for-byte template from 89.2.

    2. File `.planning/phases/89.3-mind-map-and-aura-schema/89.3-VERIFICATION.md`. Mirror 89.2-VERIFICATION.md format. List all 9 ROADMAP SCs with evidence per SC:
       - SC-01: rs-neo4j-writer.cjs exports writeDiscovery -- evidence: file exists + grep for export + test suite green (12/12 from Plan 89.3-01).
       - SC-02: rs-sqlite-mirror.cjs exports same writeDiscovery API surface -- evidence: file exists + grep for export + test suite green (12/12 from Plan 89.3-02).
       - SC-03: rs-mind-map.cjs exports renderMindMap -- evidence: file exists + grep + test suite green (10/10 from Plan 89.3-03).
       - SC-04: rs-expert-mapper.cjs exports mapAuthorsToAura -- evidence: file exists + grep + test suite green (10/10 from Plan 89.3-04).
       - SC-05: bridge-writer.cjs enhanced -- evidence: regression test (Test 1 from Plan 89.3-05 Task 1) green; existing 13 frontmatter fields preserved.
       - SC-06: Tier 0 / Tier 1 dispatch verified -- evidence: E2 + E5 from Phase Gate.
       - SC-07: lazygraph-ops EDGE_TYPES extended without forking -- evidence: E1 from Phase Gate; commands return expected output.
       - SC-08: Feynman test suite stays green + new tests registered (baseline 77 -> 82) -- evidence: G6 + G7 + G8 from Phase Gate.
       - SC-09: NO release gate -- bundled into v1.11.0 final ship at Phase 91.x -- evidence: G1 (zero new deps) + zero CHANGELOG / plugin.json / package.json diffs across 89.3 commit range.

    3. Commit BOTH files in a single atomic commit `--no-verify`: `docs(89.3-05): file Phase 89.3 Phase Gate transcript + VERIFICATION (9/9 SCs PASS or CONDITIONAL PASS for G8 inheritance)`.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const pg = fs.readFileSync('.planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md', 'utf8'); const ver = fs.readFileSync('.planning/phases/89.3-mind-map-and-aura-schema/89.3-VERIFICATION.md', 'utf8'); const checks = ['G1','G2','G3','G4','G5','G6','G7','G8','G9','E1','E2','E3','E4','E5','SC-01','SC-02','SC-03','SC-04','SC-05','SC-06','SC-07','SC-08','SC-09']; for (const c of checks) { const inPg = pg.includes(c); const inVer = ver.includes(c); if (!inPg && !inVer) { console.error('Missing assertion: ' + c); process.exit(1); } } console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - PHASE-GATE.md exists: `test -f .planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md && echo PASS` outputs PASS
    - VERIFICATION.md exists: `test -f .planning/phases/89.3-mind-map-and-aura-schema/89.3-VERIFICATION.md && echo PASS` outputs PASS
    - PHASE-GATE.md frontmatter declares bundled_release: true: `grep -c "bundled_release: true" .planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md` returns 1
    - PHASE-GATE.md declares canon_parts: `grep -c "canon_parts:" .planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md` returns 1
    - PHASE-GATE.md has 9 G asserts: `grep -cE "^### G[1-9]" .planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md` returns 9
    - PHASE-GATE.md has 5 E asserts: `grep -cE "^### E[1-5]" .planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md` returns 5
    - VERIFICATION.md has 9 SC sections: `grep -cE "SC-0[1-9]" .planning/phases/89.3-mind-map-and-aura-schema/89.3-VERIFICATION.md` returns >= 9
    - Zero em-dashes on both: `grep -cP '[\x{2013}\x{2014}]' .planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md .planning/phases/89.3-mind-map-and-aura-schema/89.3-VERIFICATION.md | awk -F: '{if ($2 != 0) exit 1} END {print "PASS"}'` outputs PASS
    - Atomic commit: `git log --oneline -3 | grep -cE "(89.3-05).*Phase Gate|(89.3-05).*VERIFICATION"` returns >= 1
    - Zero release artifacts touched: `git diff HEAD~1 -- CHANGELOG.md .claude-plugin/plugin.json package.json | wc -l` returns 0
  </acceptance_criteria>
  <done>
    Phase 89.3 Phase Gate transcript + VERIFICATION.md filed. 14 G/E asserts documented (9 G inherited from 89.2 + 5 E new for 89.3); 9 ROADMAP SCs mapped to evidence pointers; PASS or CONDITIONAL PASS status reflects G8 inheritance from 89.2 (3 pre-existing failures persist; documented separately). Bundled milestone protocol honored: NO release artifacts touched in 89.3 commit range. Phase 89.3 closes; downstream Phase 89.4 (chain wiring) can begin reading 89.3 SUMMARY.md outputs.
  </done>
</task>

</tasks>

<verification>
- bridge-writer.cjs renderBridgeArtifact enhanced in place; backward-compat preserved (regression test Test 1)
- 2 new optional frontmatter fields (thesis + breakthrough_score) emitted only when source pair carries them
- Canon Part 8 defense-in-depth: auditQueryObject on rendered output before return
- 5 new Phase 89.3 test files registered in run-feynman-tests.cjs (baseline 77 -> 82)
- Phase Gate transcript filed with 9 G + 5 E asserts; PASS or CONDITIONAL PASS for G8 inheritance
- VERIFICATION.md filed with all 9 ROADMAP SCs + evidence pointers
- 8 fixture scenarios + A1 sweep all green for bridge-writer-enhanced
- 2 adversarial Canon Part 8 scenarios cover thesis + breakthrough.dominant_dimension vectors
- Zero new runtime deps; CJS only; BSL 1.1; zero em-dashes
- NO release artifacts touched (bundled into v1.11.0 at Phase 91.x)
</verification>

<success_criteria>
Plan 89.3-05 complete when:
- [ ] lib/core/bridge-writer.cjs enhanced in place; renderBridgeArtifact emits 2 new optional fields backward-compatibly
- [ ] lib/memory/test-bridge-writer-enhanced.cjs ships 8 scenarios + A1 sweep (1 regression + 2 adversarial)
- [ ] Test suite exits 0 with "8/8 passed"
- [ ] lib/memory/run-feynman-tests.cjs has 5 new TEST_FILES entries (baseline 77 -> 82)
- [ ] All 5 new test files invoke directly (PASS)
- [ ] .planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md filed with 9 G + 5 E asserts
- [ ] .planning/phases/89.3-mind-map-and-aura-schema/89.3-VERIFICATION.md filed with all 9 ROADMAP SCs + evidence
- [ ] All atomic commits (RED + GREEN + register + Phase Gate) in git log
- [ ] Zero release artifacts touched (CHANGELOG / plugin.json / package.json byte-identical at HEAD vs Plan 89.3-01 base commit)
- [ ] Zero new runtime deps
</success_criteria>

<output>
After completion, create `.planning/phases/89.3-mind-map-and-aura-schema/89.3-05-SUMMARY.md`
</output>
