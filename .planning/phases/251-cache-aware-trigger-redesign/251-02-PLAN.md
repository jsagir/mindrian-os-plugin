---
phase: 251-cache-aware-trigger-redesign
plan: 02
type: execute
wave: 2
depends_on: ["251-01"]
files_modified:
  - tests/test-251-block-budget.cjs
  - tests/test-251-hitrate-report.cjs
  - tests/fixtures/cache-hitrate-fixture.jsonl
  - scripts/cache-hitrate-report.cjs
  - scripts/intent-classifier.cjs
  - docs/HOOK-INJECTION-CACHE-DOCTRINE.md
autonomous: false
execution_model: sonnet
requirements: [CACHE-02, CACHE-03]
must_haves:
  truths:
    - "NAV_BLOCK_BUDGET_BYTES exists as the executable CACHE-03 budget on the rail: the fixture-rendered per-turn block fits inside it, and the constant's comment binds any future Brain-reach addition to fit inside the same envelope or shrink elsewhere - no prefix mechanism change, ever"
    - "The first-party doctrine is filed as a tracked doc: per-turn hook injection in Claude Code is cache-safe by construction (additionalContext extends the cache); its cost is append-accumulation; the levers are size, dedup, and emission discipline; cache_control tricks and prefix pinning are named non-solutions"
    - "scripts/cache-hitrate-report.cjs parses a session JSONL read-only and reports per-session hit rate, zero-cache-read request count, NAV block count, consecutive byte-identical duplicate count, and suppressed-marker count - aggregates only, no user content in its output"
    - "A live session running a build that ACTUALLY CONTAINS the 251-01 hygiene changes (verified against the running plugin root, not the dev repo - a main commit is not live until released and picked up) shows cache-read rate at or above the measured 91% floor and observed suppression on idle turns (ROADMAP success criterion 3)"
  artifacts:
    - path: "scripts/intent-classifier.cjs"
      provides: "NAV_BLOCK_BUDGET_BYTES exported constant with the CACHE-03 binding comment"
      exports: ["NAV_BLOCK_BUDGET_BYTES"]
    - path: "docs/HOOK-INJECTION-CACHE-DOCTRINE.md"
      provides: "First-party cache doctrine: mechanism, refuted hypothesis, the three levers, the budget, the Brain-reach rider rule, the do-not list, honest limits"
      contains: "append-accumulation"
    - path: "scripts/cache-hitrate-report.cjs"
      provides: "Read-only session JSONL analyzer: hit rate, zero-cache-read requests, NAV dup + suppression counts"
    - path: "tests/test-251-block-budget.cjs"
      provides: "Budget constant + fixture-block-within-budget + doctrine-doc content fence"
    - path: "tests/test-251-hitrate-report.cjs"
      provides: "Analyzer correctness against a synthetic fixture JSONL with known counts"
    - path: "tests/fixtures/cache-hitrate-fixture.jsonl"
      provides: "Synthetic transcript: usage records (requestId-deduped), NAV attachments incl. one byte-identical repeat and one suppressed marker"
  key_links:
    - from: "tests/test-251-block-budget.cjs"
      to: "scripts/intent-classifier.cjs NAV_BLOCK_BUDGET_BYTES"
      via: "fixture block byte length asserted <= the exported budget"
      pattern: "NAV_BLOCK_BUDGET_BYTES"
    - from: "docs/HOOK-INJECTION-CACHE-DOCTRINE.md"
      to: ".planning/phases/251-cache-aware-trigger-redesign/251-CACHE-MEASUREMENT.md"
      via: "doctrine cites the measurement as its evidence base (the .planning path named as local-artifact provenance)"
      pattern: "251-CACHE-MEASUREMENT"
    - from: "checkpoint live check"
      to: "scripts/cache-hitrate-report.cjs"
      via: "the human runs the analyzer against the live session's JSONL and pastes the report"
      pattern: "cache-hitrate-report"
---

<objective>
Close CACHE-03 and the doctrine leg of CACHE-02 on the hygiene-passed rail from 251-01.

CACHE-03 (rescoped by the measurement): the Brain reach rides the EXISTING rail - proven
cache-safe by CACHE-01 - so the binding instrument is NOT a mechanism change but an explicit
BLOCK-SIZE BUDGET: an exported NAV_BLOCK_BUDGET_BYTES constant plus an executable fence test
that any future Brain-reach payload must pass inside the post-CACHE-02 envelope (~300-600 B
varying per measurement section 4). Plus the live-session check ROADMAP success criterion 3
demands: cache-read rates stay at or above the measured 91-97% baseline on a real session
running the shipped hygiene - a checkpoint:human-verify, honestly named: it verifies the
post-hygiene rail TODAY and arms the enforcement fence for the Brain reach; the Brain reach
itself lands in the later loop phases, and the budget fence is what binds it then.

CACHE-02 doctrine leg (requirement text: "Design rationale filed as first-party doctrine -
the hook layer remains corpus whitespace"): a tracked docs/ file stating the verified
mechanism so no future session re-litigates the ep55 hypothesis or "solves" the non-problem
with prefix tricks.

Cross-phase gate inherited via depends_on 251-01 (itself gated on 250-01 / HONEST-01):
planning now, execution after Phase 250 lands.

Purpose: make the cache posture self-enforcing (budget fence) and self-documenting
(doctrine), and prove the baseline held on a live session.
Output: the budget constant + fence, the doctrine doc, the read-only hit-rate analyzer with
fixture-proven correctness, and the recorded live-session baseline report.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/251-cache-aware-trigger-redesign/251-CACHE-MEASUREMENT.md
@.planning/phases/251-cache-aware-trigger-redesign/251-01-SUMMARY.md
@scripts/intent-classifier.cjs
@tests/run-all-251.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Block-size budget fence + first-party cache doctrine</name>
  <files>tests/test-251-block-budget.cjs, scripts/intent-classifier.cjs, docs/HOOK-INJECTION-CACHE-DOCTRINE.md</files>
  <behavior>
    Write tests/test-251-block-budget.cjs FIRST (node:test CJS, node:assert/strict; reuse
    the engine-arm fixture harness from tests/test-251-suppress-unchanged.cjs), run it,
    RECORD the RED output (constant and doc absent). Tests:
    - Test 1 (constant): scripts/intent-classifier.cjs exports NAV_BLOCK_BUDGET_BYTES; it
      is a positive integer <= 1200 (the ceiling below the measured 1,275 B pre-hygiene
      average - raising it past the old average would make the fence meaningless; any
      raise is a visible, deliberate diff).
    - Test 2 (fixture within budget): the composed engine-arm fixture block's
      Buffer.byteLength is <= NAV_BLOCK_BUDGET_BYTES (the post-251-01 block must fit with
      headroom - the headroom IS the Brain-reach allowance).
    - Test 3 (doctrine doc content fence): docs/HOOK-INJECTION-CACHE-DOCTRINE.md exists
      and contains ALL of: 'append-accumulation', 'cache-safe by construction',
      '251-CACHE-MEASUREMENT', 'NAV_BLOCK_BUDGET_BYTES', a do-not section naming
      'cache_control' and 'prefix pinning', and the three levers 'size', 'dedup',
      'emission discipline'. Assert ZERO em-dash codepoints in the doc (match the
      grep -P codepoint escape for U+2014, never a literal character in the test source).
    - Test 4 (budget names the rider rule): the constant's adjacent comment block in the
      source names CACHE-03 and the Brain reach (source regex: /CACHE-03/ within 10 lines
      of NAV_BLOCK_BUDGET_BYTES).
  </behavior>
  <action>
    1. scripts/intent-classifier.cjs: add and export NAV_BLOCK_BUDGET_BYTES. Pick the
       value from the 251-01 SUMMARY's measured after-bytes: after-bytes of the 3-verb
       fixture block + ~300 B headroom, rounded to a clean number <= 1200 (expected
       landing zone 1000-1200; if the 251-01 after-bytes make 1200 too tight for a 300 B
       reach, flag it in the SUMMARY rather than silently raising past 1200). Comment
       states the CACHE-03 contract: the future Brain reach rides THIS rail inside THIS
       budget - additions fit or something else shrinks; the injection mechanism itself
       never changes (no cache_control, no prefix moves - measurement section 4 item 4).
    2. Write docs/HOOK-INJECTION-CACHE-DOCTRINE.md (Feynman-simplified, JTBD-oriented,
       hyphens only, no real names, no session transcript content). Sections:
       - What actually happens: UserPromptSubmit additionalContext is appended INSIDE the
         newest user turn as a system reminder; appending at the tail EXTENDS the prompt
         cache; hooks cannot touch the tools/system prefix. Cite the hooks + prompt-caching
         docs the way the measurement does.
       - The refuted hypothesis: the ep55 prefix-break framing is FALSE for Claude Code's
         request shape - measured 91-97% hit rates with the block firing on ~73% of turns;
         2-3 zero-cache-read requests per session, all session-start/compaction. Evidence
         base: .planning/phases/251-cache-aware-trigger-redesign/251-CACHE-MEASUREMENT.md
         (named as a local planning artifact - .planning is untracked, so restate the
         headline numbers here rather than depending on the link).
       - The real cost: append-accumulation (1.25x write once, 0.1x re-read per subsequent
         request) plus compaction acceleration; roughly USD 4-7/month at measured sizes.
       - The three levers: size, dedup, emission discipline - and how 251-01 implemented
         each (suppress-when-unchanged, skeleton-to-SessionStart, payload dedup).
       - The budget: NAV_BLOCK_BUDGET_BYTES, where the fence lives
         (tests/test-251-block-budget.cjs), and the CACHE-03 rider rule for the Brain
         reach.
       - Do NOT do: per-turn cache_control tricks, prefix pinning, moving the block up the
         request, re-rendering varying content into the system prompt - non-solutions to a
         non-problem under the verified mechanism.
       - Honest limits: token conversion estimated (3.5 B/token assumed), CLI-surface
         sample only (Desktop/Cowork unmeasured - Tri-Polar gap stated deliberately),
         compaction-acceleration not isolated, and the dominant FELT per-turn cost is the
         7 synchronous UserPromptSubmit hooks' latency (out of scope here; future phase
         candidate).
    3. Run to GREEN, then bash tests/run-all-251.sh (glob picks up the new tests next to
       the 251-01 suite).
  </action>
  <verify>
    <automated>node --test tests/test-251-block-budget.cjs && bash tests/run-all-251.sh</automated>
  </verify>
  <done>RED recorded, then: budget constant exported with the CACHE-03 rider comment, fixture block within budget with reach headroom, doctrine doc passes its content fence, full 251 runner green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Read-only cache hit-rate analyzer</name>
  <files>tests/test-251-hitrate-report.cjs, tests/fixtures/cache-hitrate-fixture.jsonl, scripts/cache-hitrate-report.cjs</files>
  <behavior>
    Write tests/test-251-hitrate-report.cjs and the fixture FIRST, run the test, RECORD
    the RED output (script absent). Build tests/fixtures/cache-hitrate-fixture.jsonl as a
    ~14-line synthetic transcript with KNOWN totals (never copied from a real session -
    Part 8 / no-real-content discipline):
    - 4 user turns, 3 carrying a UserPromptSubmit attachment entry (hookEvent
      'UserPromptSubmit', attachment.content = a NAV block string); attachments 1 and 2
      byte-identical (one consecutive duplicate), attachment 3 equal to the exported
      NAV_UNCHANGED_MARKER (one suppressed marker).
    - 6 assistant usage records across 5 unique requestIds (one requestId repeated to
      prove dedup), with cache_read_input_tokens / cache_creation_input_tokens /
      input_tokens chosen so hit rate = reads / (reads + writes + uncached) computes to a
      known two-decimal value, and exactly ONE record with zero cache reads.
    - 1 sidechain/tool-result line the parser must skip.
    Tests:
    - Test 1: report.hit_rate matches the hand-computed fixture value (two decimals).
    - Test 2: report.zero_cache_read_requests === 1; report.api_requests === 5 (deduped).
    - Test 3: report.nav_blocks === 3, report.consecutive_identical === 1,
      report.suppressed_markers === 1.
    - Test 4 (aggregates only): the JSON report contains NO attachment content and NO
      user text - stringify the report and assert the fixture's block string and user
      strings do not appear.
    - Test 5: nonexistent path exits non-zero with a one-line error on stderr; a directory
      argument likewise (defensive CLI, argv switch-router convention).
  </behavior>
  <action>
    1. Write scripts/cache-hitrate-report.cjs: CJS, zero dependencies, process.argv
       switch-router convention. Usage: node scripts/cache-hitrate-report.cjs
       <session.jsonl> [--json]. Parse line by line exactly the way the CACHE-01
       measurement did (its method section is the spec): (a) UserPromptSubmit attachment
       entries -> NAV block count, consecutive byte-identical count, suppressed-marker
       count (import NAV_UNCHANGED_MARKER from scripts/intent-classifier.cjs - single
       source of the literal); (b) per-request usage records deduplicated by requestId ->
       cache_read / cache_creation / uncached input totals, hit rate, zero-cache-read
       request count; (c) skip sidechains and tool_result turns. Output: a human table by
       default, a JSON object under --json. READ-ONLY: the script never writes anywhere;
       output carries aggregates only - never attachment content, never user text (this
       is what makes it safe to run against real ~/.claude/projects transcripts).
    2. A header comment names the baseline contract: the CACHE-01 measured floor is 91.3%
       (worst sampled session) with 2-3 zero-cache-read requests per session
       (start/compaction only); the checkpoint in Task 3 and any future Brain-reach
       verification runs THIS script so the numbers stay comparable.
    3. Run to GREEN, then bash tests/run-all-251.sh.
  </action>
  <verify>
    <automated>node --test tests/test-251-hitrate-report.cjs && bash tests/run-all-251.sh</automated>
  </verify>
  <done>RED recorded, then: analyzer reproduces every fixture number exactly, dedups requestIds, skips sidechains, emits aggregates only, fails loud on bad paths; runner green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-session cache baseline check (post-hygiene rail, pre-Brain-reach)</name>
  <what-built>
    251-01 shipped the three hygiene items on the UserPromptSubmit rail; Tasks 1-2 of this
    plan shipped the CACHE-03 block-size budget fence, the first-party doctrine, and the
    read-only hit-rate analyzer. This checkpoint is the live-session leg of ROADMAP
    success criterion 3. Honest scope: it verifies the HYGIENE-PASSED rail holds the
    measured 91-97% cache-read baseline and that suppression fires in the wild; the Brain
    reach itself is NOT riding yet (it lands in the later loop phases) - when it does, the
    budget fence from Task 1 binds it and THIS SAME analyzer re-verifies the baseline.
  </what-built>
  <how-to-verify>
    1. STALENESS GUARD FIRST (hard rule: a main commit is NOT live until a release ships
       AND the running session picked it up - .planning/debug/live-session-running-stale-
       plugin-cache-fixes-inert.md): confirm the plugin root your live session actually
       loads contains the hygiene code:
       grep -l "NAV DECISION unchanged" <running-plugin-root>/scripts/intent-classifier.cjs
       where <running-plugin-root> is the installed cache (~/.claude/plugins/...) after a
       release + update, OR a dev-linked install pointing at this workspace. If the grep
       finds nothing, STOP - do not measure a stale rail and call it verified.
    2. Run a normal working session in a bound room: at least 10 real user turns,
       including at least 3 consecutive low-activity turns in an unchanged room state
       (the a396e801 idle pattern that produced 7/7 identical blocks).
    3. Find that session's transcript JSONL under ~/.claude/projects/<project-dir>/ (most
       recent file matching the session start time).
    4. Run: node scripts/cache-hitrate-report.cjs <that-session>.jsonl
    5. Confirm ALL of:
       - hit_rate >= 0.91 (the measured floor; sampled sessions ran 91.3-97.3%)
       - zero-cache-read requests are 2-3 and attributable to session start / compaction,
         not injection turns
       - suppressed_markers >= 1 (suppression observed live on the idle turns)
       - consecutive_identical === 0 for FULL blocks (identical repeats now suppress;
         marker repeats are fine)
    6. Paste the analyzer report into your reply.
  </how-to-verify>
  <resume-signal>Type "approved" with the pasted report, or describe which number missed and on which session</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real session transcripts -> analyzer output | the analyzer reads private ~/.claude/projects JSONL; its output gets pasted into planning artifacts |
| budget constant -> future Brain-reach code | a number in source gates what a later phase may add to the rail |
| doctrine doc -> future sessions' decisions | a tracked doc steers future architecture away from re-litigating the mechanism |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-251-06 | Information disclosure | cache-hitrate-report.cjs output | mitigate | aggregates only - counts, rates, byte totals; never attachment content or user text (Task 2 Test 4 asserts the negative); safe to paste into tracked artifacts |
| T-251-07 | Tampering | NAV_BLOCK_BUDGET_BYTES silently raised | mitigate | fence test pins the <= 1200 ceiling below the pre-hygiene average; any raise is a visible diff that fails Test 1 first |
| T-251-08 | Repudiation | verifying a stale rail (the recurring false-success bug class) | mitigate | checkpoint step 1 staleness guard greps the RUNNING plugin root for the hygiene marker before any measurement counts as evidence |
| T-251-09 | Information disclosure (Part 8) | fixture JSONL | mitigate | fixture is fully synthetic with invented strings - never copied from a real transcript (Task 2 behavior block) |

No package installs this phase (zero-npm-deps hard convention; Package Legitimacy Audit: not applicable).
</threat_model>

<verification>
- `bash tests/run-all-251.sh` green (now discovering the 251-01 AND 251-02 test files)
- `node --test tests/test-251-block-budget.cjs tests/test-251-hitrate-report.cjs` green
- Every test file's RED run recorded in the SUMMARY before its GREEN
- The checkpoint report pasted and hit_rate >= 0.91 with suppression observed live
- docs/HOOK-INJECTION-CACHE-DOCTRINE.md has zero em-dashes; no real names; no transcript content
</verification>

<success_criteria>
- CACHE-03 complete: the Brain reach's rail is the existing one, bound by an explicit,
  test-enforced block-size budget with recorded headroom; no prefix mechanism change
  anywhere; the live-session check confirms the 91-97% baseline held post-hygiene.
- CACHE-02 doctrine leg complete: the first-party doctrine is tracked in docs/ and
  restates the headline evidence so it survives .planning's untracked status.
- The analyzer is reusable: the same script re-verifies the baseline when the Brain reach
  actually lands.
</success_criteria>

<output>
Create `.planning/phases/251-cache-aware-trigger-redesign/251-02-SUMMARY.md` when done,
including the RED outputs, the chosen NAV_BLOCK_BUDGET_BYTES value with its headroom math,
and the pasted live-session analyzer report from the checkpoint.
</output>
