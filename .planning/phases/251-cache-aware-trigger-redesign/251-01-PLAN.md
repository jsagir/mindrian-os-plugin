---
phase: 251-cache-aware-trigger-redesign
plan: 01
type: execute
wave: 1
depends_on: ["250-01"]
files_modified:
  - tests/run-all-251.sh
  - tests/test-251-suppress-unchanged.cjs
  - tests/test-251-skeleton-split.cjs
  - tests/test-251-payload-dedup.cjs
  - tests/test-209-engine-arm-contract.cjs
  - scripts/intent-classifier.cjs
  - scripts/session-start
  - scripts/post-compact
autonomous: true
execution_model: sonnet
requirements: [CACHE-02]
must_haves:
  truths:
    - "Two consecutive byte-identical NAVIGATION DECISION blocks in one session emit the full block once, then the one-line NAV_UNCHANGED_MARKER; a changed decision always re-emits in full (the VARYING parts - reach verbs, percentages - keep injecting when changed)"
    - "A suppressed turn records NO reached-gate in the card-fire sidechannel; a full-block turn with the marker records exactly one (Stop-gate machinery and SEED-021 stay consistent - the gate is only recorded when it was actually re-presented)"
    - "A corrupt, missing, or unreadable hash sidecar fails OPEN to full emission - suppression is an optimization, never a gate"
    - "The FIRE-IF-FORK imperative reaches Larry once per session via SessionStart additionalContext (matcher startup|clear|compact re-seeds it after compaction) and no longer rides every per-turn engine block; the byte-frozen marker line stays per-turn"
    - "The per-turn [AskUserQuestion payload: ...] line no longer repeats the verb labels already printed in the option rows; shape, mode, verb_count, and recommended remain; the persisted f1_closer_payload (next-turn consumer) is untouched"
    - "The first post-compact turn always re-emits the full block (post-compact deletes the hash sidecar - compaction rewrote history, the prior block may be gone from context)"
    - "Before/after byte counts for each of the three hygiene items are recorded in the SUMMARY (ROADMAP success criterion 2)"
  artifacts:
    - path: "scripts/intent-classifier.cjs"
      provides: "NAV_UNCHANGED_MARKER const, navBlockHashPath/readNavBlockHash/writeNavBlockHash helpers, suppression gate inside renderEngineDecisionWithDial (sidechannel record hoisted AFTER the gate), deduped compact payload"
      exports: ["NAV_UNCHANGED_MARKER", "navBlockHashPath", "renderEngineDecisionWithDial", "formatEngineDecisionBlock"]
    - path: "scripts/session-start"
      provides: "The NAVIGATION CARD-FIRE CONTRACT session-doctrine block (FIRE-IF-FORK rule, SEED-021 clause, marker + payload + unchanged-marker legends) appended to $context, under 900 bytes"
      contains: "FIRE-IF-FORK"
    - path: "scripts/post-compact"
      provides: "Deletion of decision-traces/*.nav-block-hash.json so the first post-compact turn re-emits in full"
    - path: "tests/test-251-suppress-unchanged.cjs"
      provides: "Identical-turn suppression, changed-turn re-emit, fail-open sidecar, sidechannel-skip proof, env kill-switch, post-compact reset source fence"
    - path: "tests/test-251-skeleton-split.cjs"
      provides: "Binding absent from per-turn engine block, doctrine present in session-start source, emitBindingGate surface untouched"
    - path: "tests/test-251-payload-dedup.cjs"
      provides: "Payload carries verb_count + recommended and no verbs array; f1_closer_payload verbs untouched; fixture byte counts printed"
    - path: "tests/run-all-251.sh"
      provides: "Glob-discovery phase runner (tests/test-251-*), found-eq-0 guard, em-dash fence (run-all-250.sh mechanism)"
  key_links:
    - from: "scripts/intent-classifier.cjs (call site ~L3397)"
      to: "the hash sidecar decision-traces/<session>.nav-block-hash.json"
      via: "readNavBlockHash before render (ctx.prevBlockHash), writeNavBlockHash after stdout emit"
      pattern: "nav-block-hash"
    - from: "renderEngineDecisionWithDial suppression gate"
      to: "lib/core/card-fire-sidechannel.cjs recordReachedGate"
      via: "sidechannel record hoisted AFTER the hash comparison - suppressed turns never record"
      pattern: "recordReachedGate"
    - from: "scripts/session-start $context assembly (~L1744)"
      to: "the per-turn marker [AskUserQuestion contract: shape=F.X verbs=N]"
      via: "session doctrine explains the marker once; the per-turn block carries only the marker"
      pattern: "FIRE-IF-FORK"
    - from: "scripts/post-compact"
      to: "decision-traces/*.nav-block-hash.json"
      via: "sidecar deletion on compaction"
      pattern: "nav-block-hash"
---

<objective>
Land the CACHE-02 hygiene pass on the proven-cache-safe UserPromptSubmit rail, exactly the
three measured items from 251-CACHE-MEASUREMENT.md section 4, in measured-payoff order:

(a) Suppress-when-unchanged: hash the rendered block per session and emit the one-line
NAV_UNCHANGED_MARKER when the decision is byte-identical to the previous turn's (session
a396e801 measured 7/7 identical blocks - 6 of 7 emissions carried zero new information).

(b) Skeleton-to-SessionStart: the FIRE-IF-FORK imperative (~330 B/turn) moves to SessionStart
additionalContext, injected once and re-seeded on compaction by the existing
startup|clear|compact matcher; the per-turn block keeps only the byte-frozen marker.

(c) Payload dedup: the [AskUserQuestion payload: ...] JSON stops repeating the verb labels
already printed in the option rows (~300 B/block of pure duplication).

This is a HYGIENE pass, NOT a re-architecture (navigator ruling 2026-08-10): no cache_control
tricks, no prefix pinning, no mechanism change - the measurement proved the ep55 prefix-break
hypothesis FALSE for Claude Code (91-97% measured hit rates; additionalContext extends the
cache). The card-fire contract (SEED-021) and Stop-gate machinery are preserved by
construction: suppressed turns skip the reached-gate record because the gate was not
re-presented that turn.

Cross-phase gate (encoded in depends_on): this plan EXECUTES only after Phase 250's 250-01
(HONEST-01) has landed - the rail must carry the honest reach, not the silent one. Planning
now, execution after.

Purpose: cut the measured ~1,275 B/turn average injection by 40-60% plus eliminate the
zero-information repeat emissions, with before/after byte counts recorded per item.
Output: the three hygiene changes in scripts/intent-classifier.cjs + scripts/session-start +
scripts/post-compact, three born-RED test files, the re-pointed engine-arm contract test,
and tests/run-all-251.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/251-cache-aware-trigger-redesign/251-CACHE-MEASUREMENT.md
@scripts/intent-classifier.cjs
@lib/hmi/selector-dispatcher.cjs
@lib/core/card-fire-sidechannel.cjs
@tests/test-209-engine-arm-contract.cjs
@tests/run-all-250.sh
@hooks/hooks.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Suppress-when-unchanged (hash sidecar + sidechannel-safe gate) + phase runner</name>
  <files>tests/test-251-suppress-unchanged.cjs, tests/run-all-251.sh, scripts/intent-classifier.cjs, scripts/post-compact</files>
  <behavior>
    Write tests/test-251-suppress-unchanged.cjs FIRST (node:test CJS, node:assert/strict;
    REUSE the fixture/harness pattern from tests/test-209-engine-arm-contract.cjs to obtain
    a deterministic engine-arm block via renderEngineDecisionWithDial with temp room dirs).
    Run it, RECORD the RED output. Before writing tests, read
    lib/core/card-fire-sidechannel.cjs to locate its storage root and the env/opts it
    honors for isolation (recordReachedGate accepts a filePath option per its header ~L230;
    the module also has a documented default root - isolate whichever the intent-classifier
    call path actually uses, via temp HOME or the module's own override).
    Tests:
    - Test 1 (full-then-marker): call renderEngineDecisionWithDial twice with IDENTICAL
      inputs; first call with ctx.prevBlockHash null returns the full block and invokes
      ctx.onBlockHash with a sha256 hex string; second call with ctx.prevBlockHash set to
      that hash returns exactly NAV_UNCHANGED_MARKER (import the exported const, do not
      inline the literal).
    - Test 2 (changed re-emits): mutate one varying field (e.g. the Why rationale or a
      reach score) between calls; second call returns the FULL block, not the marker.
    - Test 3 (sidechannel skip): after the suppressed call of Test 1, the card-fire
      sidechannel storage contains exactly ONE reached-gate record (from the first call),
      not two. Read it via the sidechannel module's own read API against the isolated root.
    - Test 4 (fail-open): readNavBlockHash on a missing sidecar returns null; on a corrupt
      sidecar (write literal garbage bytes) returns null and never throws - and a null
      prevBlockHash always yields full emission.
    - Test 5 (kill switch): with MINDRIAN_NAV_NO_SUPPRESS=1 in env, the call-site read
      helper is bypassed - assert via the exported helper contract (readNavBlockHash still
      works; the env gate lives at the call site, so assert the source contains the env
      guard: /MINDRIAN_NAV_NO_SUPPRESS/ appears in scripts/intent-classifier.cjs).
    - Test 6 (sidecar roundtrip): writeNavBlockHash then readNavBlockHash roundtrips the
      hash; the sidecar path is decision-traces/<sessionId>.nav-block-hash.json (assert via
      exported navBlockHashPath); the file contains ONLY version/sha256/at/bytes keys -
      no user content, no block text (Canon Part 8 discipline for the sidecar).
    - Test 7 (post-compact reset fence): read scripts/post-compact as text; assert it
      contains a deletion of decision-traces nav-block-hash sidecars
      (/nav-block-hash\.json/ present near an rm or unlink).
    Record the fixture block's byte count in the RED run output (this is the item-(a)
    "before" number for the SUMMARY).
  </behavior>
  <action>
    1. Create tests/run-all-251.sh mirroring tests/run-all-250.sh EXACTLY in mechanism:
       glob discovery of tests/test-251-* (.cjs via node --test, .sh via bash), the
       found-eq-0 guard, and the em-dash fence (grep -P for the U+2014 codepoint escape
       over the files this phase touches - never a literal em-dash in the runner). Header
       enumerates the mandatory 251 test files by name. bash only, set -uo pipefail.
    2. Write the test file per the behavior block; run it; record RED.
    3. scripts/intent-classifier.cjs changes (all inside the existing fail-silent
       discipline - a fault in any new code degrades to full emission, never blocks the
       turn):
       - Add const NAV_UNCHANGED_MARKER = '[NAV DECISION unchanged - prior block stands]';
         export it from the module.exports block (~L3066).
       - Add navBlockHashPath(roomDir, sessionId) following the zeroScoreGateOfferedPath
         precedent (~L2704): path.join(roomDir, '.mindrian', 'decision-traces',
         sessionId + '.nav-block-hash.json'). Add readNavBlockHash (any error -> null)
         and writeNavBlockHash (atomic tmp + rename, the persistDecisionTrace idiom
         ~L1018-1046; payload {version: 1, sha256, at, bytes} ONLY). Export
         navBlockHashPath for tests.
       - Restructure renderEngineDecisionWithDial (~L1429) so BOTH arms funnel through ONE
         suppression gate: compose finalBlock exactly as today (base on the non-engine /
         fault arms; base + rendered.text + marker + payload on the engine arm), then:
         hash = sha256(finalBlock) via the already-required crypto; call
         ctx.onBlockHash(hash, Buffer.byteLength(finalBlock)) guarded in try/catch;
         if (ctx.prevBlockHash && ctx.prevBlockHash === hash) return NAV_UNCHANGED_MARKER
         WITHOUT calling recordReachedGate; else, on the engine arm, call
         recordReachedGate exactly as today and return finalBlock. This REQUIRES hoisting
         the existing sidechannel try/catch (~L1532-1544) to after the comparison - the
         Stop-gate predicate must never be told a gate was re-presented on a turn where
         Larry saw only the one-line marker (SEED-021 / check-card-fire.cjs consistency).
         The empty-block case (finalBlock '' or falsy) bypasses the gate entirely.
       - Call site (~L3397): unless process.env.MINDRIAN_NAV_NO_SUPPRESS is set, read
         prevHash = readNavBlockHash(roomDir, sessionId) and thread ctx.prevBlockHash;
         thread ctx.onBlockHash to capture {hash, bytes} into locals AND onto the
         traceEntry as nav_block_sha256 + nav_block_bytes + nav_suppressed (boolean,
         set after render by comparing the returned block to NAV_UNCHANGED_MARKER) for
         /mos:explain-decision forensics. After the process.stdout.write, call
         writeNavBlockHash(roomDir, sessionId, hash, bytes) fire-and-forget (idempotent
         on suppressed turns). persistDecisionTrace ordering is unchanged.
       - Do NOT touch emitBindingGate (~L2603) or the unbound one-liner (~L2797): measured
         at ~0 full blocks and ~60 B respectively - out of hygiene scope by measurement.
    4. scripts/post-compact: add a small guarded block that deletes
       <activeRoomDir>/.mindrian/decision-traces/*.nav-block-hash.json using the room
       resolution the script already performs (read the script first; reuse its existing
       room-dir variable; glob delete with || true - post-compact MUST NEVER crash).
       Comment: compaction rewrites history, so the prior block may be gone from the
       summarized context; the first post-compact turn must re-emit in full.
    5. Run to GREEN. Record the after-bytes: full block (unchanged from before for a
       changed turn) vs NAV_UNCHANGED_MARKER bytes for a suppressed turn.
  </action>
  <verify>
    <automated>node --test tests/test-251-suppress-unchanged.cjs && bash tests/run-all-251.sh</automated>
  </verify>
  <done>RED recorded with the fixture before-bytes, then: identical turns suppress to the exported marker, changed turns re-emit in full, suppressed turns record zero new reached-gates, corrupt/missing sidecar fails open, kill switch present, post-compact reset in place, runner discovers and passes the suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Skeleton-to-SessionStart (FIRE-IF-FORK doctrine once per session)</name>
  <files>tests/test-251-skeleton-split.cjs, scripts/session-start, scripts/intent-classifier.cjs, tests/test-209-engine-arm-contract.cjs</files>
  <behavior>
    Write tests/test-251-skeleton-split.cjs FIRST, run it, RECORD the RED output (which
    also records the item-(b) before-bytes: the binding line's byte length in the fixture
    block). Tests:
    - Test 1 (binding gone from per-turn block): compose an engine-arm block via the
      test-209 harness pattern; assert block.indexOf('[FIRE-IF-FORK:') === -1 AND the
      byte-frozen marker /\[AskUserQuestion contract: shape=F\.\d+ verbs=\d+\]/ is STILL
      present (the marker is the per-turn gate flag; only the imperative moved).
    - Test 2 (doctrine in session-start): read scripts/session-start as text; assert ONE
      block containing all of: '[FIRE-IF-FORK', 'AskUserQuestion', 'SEED-021',
      'NAV DECISION unchanged' (the marker legend), and 'verb_count' (the payload legend,
      forward-written for Task 3). Assert the doctrine heredoc/string is under 900 bytes
      (extract between its start/end markers and measure).
    - Test 3 (dispatcher untouched): appendAskUserQuestionTrailer still mints
      askuserquestion_marker, askuserquestion_binding, and the zones.footer trailer
      (call it directly on a fixture rendered object) - other consumers (pickShape door,
      renderRoomChooserCard) keep their inline imperative; ONLY the intent-classifier
      engine-arm concatenation stopped appending it.
    - Test 4 (binding-gate surface untouched): emitBindingGate's additionalContext still
      carries the FIRE-IF-FORK line (the existing test-209 assertion at ~L208 covers
      this - assert here via source: the emitBindingGate body still references the
      binding append).
  </behavior>
  <action>
    1. scripts/session-start: locate the $context assembly (grep for
       'escape_for_json "$context"', ~L1744) and append a static doctrine block to
       $context immediately before it, unconditionally (it is paid once at 1.25x then
       read at 0.1x; ~600-900 B). Content, in Larry-rail imperative voice, no em-dashes:
       - Header: '## NAVIGATION CARD-FIRE CONTRACT (session doctrine)'
       - The marker legend: per-turn NAVIGATION DECISION blocks end with the frozen line
         [AskUserQuestion contract: shape=F.X verbs=N].
       - The FIRE-IF-FORK rule, preserving the Phase 210-05 conditional semantics and the
         SEED-021 clause verbatim in spirit: if the fork presented by the option rows
         above the marker is genuinely unanswered and relevant to the current
         conversation, call the AskUserQuestion tool in THAT response with the N options
         shown; if the navigator already answered in plain text or the gate is unrelated,
         do NOT fire it - acknowledge and proceed instead; never reproduce the block as
         text (SEED-021).
       - The payload legend: the [AskUserQuestion payload: ...] line carries shape, mode,
         verb_count, recommended; the option labels are the rows above it.
       - The unchanged-marker legend: a line reading [NAV DECISION unchanged - prior
         block stands] means the previous turn's decision still applies.
       Delivery-mechanism note in a bash comment: SessionStart additionalContext is the
       sanctioned mechanism (tier0-removal handoff Section 6: no always-on skill
       primitive exists); the hooks.json SessionStart matcher 'startup|clear|compact'
       re-seeds this doctrine after every compaction, which resolves the measurement's
       survives-compaction caveat; PostCompact cannot carry hookSpecificOutput
       (95-RESEARCH), so the compact re-seed rides SessionStart(compact), not post-compact.
    2. scripts/intent-classifier.cjs: in renderEngineDecisionWithDial's engine-arm
       concatenation, DELETE the askuserquestion_binding append (the block += '\n' +
       binding lines, ~L1509-1512). Keep the appendAskUserQuestionTrailer call itself
       untouched - the dispatcher keeps minting the binding field and zones.footer for
       every OTHER consumer; only this concatenation stops carrying it per-turn.
    3. Re-point tests/test-209-engine-arm-contract.cjs, ONLY the assertions that pin the
       imperative INSIDE the intent-classifier per-turn block (locate by pattern, not
       line number): the "block carries the FIRE-IF-FORK line" assertion (~L113) and the
       "tier_0 carries the binding" assertion (~L162) INVERT to assert absence, each with
       a message citing this plan ("251-01: imperative moved to SessionStart doctrine").
       The emitBindingGate assertion (~L208, "additionalContext carries the FIRE-IF-FORK
       line") stays EXACTLY as-is - that surface is untouched. The marker and payload
       assertions stay (payload re-pointed in Task 3).
    4. Regression sweep: run bash tests/run-all-209.sh, bash tests/run-all-210.sh, and
       node --test tests/test-148-frozen-contracts.cjs tests/test-gate-native-fire-w1.cjs.
       If any OTHER test pins the binding inside the intent-classifier per-turn block,
       re-point it with the same cited message; dispatcher-level and card-render tests
       MUST pass unmodified - if one fails, the edit went too deep (revert and re-scope
       to the engine-arm concatenation only).
    5. Record item-(b) after-bytes: fixture block bytes now vs the Task-2 RED run, plus
       the one-time session-start doctrine bytes.
  </action>
  <verify>
    <automated>node --test tests/test-251-skeleton-split.cjs tests/test-209-engine-arm-contract.cjs && bash tests/run-all-209.sh && bash tests/run-all-210.sh && bash tests/run-all-251.sh</automated>
  </verify>
  <done>RED recorded with binding before-bytes, then: per-turn block carries marker but no imperative; session-start carries the sub-900-byte doctrine incl. SEED-021 and both legends; dispatcher and binding-gate surfaces byte-untouched; 209/210 suites green with only the two cited assertion inversions; byte deltas recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Kill the verb-line duplication in the AskUserQuestion payload</name>
  <files>tests/test-251-payload-dedup.cjs, scripts/intent-classifier.cjs, tests/test-209-engine-arm-contract.cjs</files>
  <behavior>
    Write tests/test-251-payload-dedup.cjs FIRST, run it, RECORD the RED output (which
    records the item-(c) before-bytes: the payload line's byte length with the verbs
    array). Tests:
    - Test 1 (shape): compose an engine-arm block; extract the payload via
      /\[AskUserQuestion payload: (\{.*\})\]/ and JSON.parse it; assert keys are exactly
      shape, mode, verb_count, recommended; assert no 'verbs' key; assert verb_count is a
      positive integer equal to the number of option rows rendered above; assert
      recommended survives when the contract carries one.
    - Test 2 (no label duplication): none of the verb label strings rendered in the
      option rows appear inside the payload JSON substring (the ~300 B duplication is
      dead), EXCEPT the single recommended verb, which is allowed by design (it is the
      differentiator, not a duplicate list).
    - Test 3 (f1_closer_payload fence): the persisted next-turn payload path is
      UNTOUCHED - assert via source that the traceEntry.f1_closer_payload assignment
      (~L3385) and closer.renderF1 flow still carry the full verbs array (grep the source
      for f1_closer_payload and assert its guard still tests Array.isArray(f1Payload.verbs));
      the dedup applies ONLY to the injected display line, never to the persisted
      consumer contract.
    - Test 4 (bytes): print the payload line's byte length before (RED, from the fixture)
      and after (GREEN); assert the after-line is at least 150 bytes smaller than the
      recorded before-value for the 3-verb fixture.
  </behavior>
  <action>
    1. scripts/intent-classifier.cjs, renderEngineDecisionWithDial engine arm
       (~L1513-1525): the compact object becomes { shape, mode, verb_count: verbs.length,
       recommended } - drop the verbs array from the INJECTED line only. Keep the line
       prefix '[AskUserQuestion payload: ' byte-identical (greppable contract prefix).
       Add a one-line comment citing 251-CACHE-MEASUREMENT.md section 2 (the ~300 B
       intra-block duplication finding) and noting the labels live in the option rows
       above plus the SessionStart payload legend (Task 2).
    2. Consumers audit (verified at plan time): the only non-comment consumer of the
       injected payload line is tests/test-209-engine-arm-contract.cjs (the verbs-array
       assertion ~L122); lib/core/navigation-engine-offer.cjs:439 and
       lib/hmi/selector-dispatcher.cjs mentions are comments; the next-turn machine
       consumer reads the PERSISTED f1_closer_payload, not the display line. Re-point the
       test-209 assertion: verbs array absent, verb_count present and numeric, message
       cites "251-01 Task 3: payload dedup per CACHE-02(c)".
    3. Run to GREEN. Record item-(c) before/after payload-line bytes, then compose the
       full three-item byte table for the SUMMARY: per-item before/after plus the
       projected per-session saving against the measured 1,275 B average and the
       a396e801 7/7-identical case.
  </action>
  <verify>
    <automated>node --test tests/test-251-payload-dedup.cjs tests/test-209-engine-arm-contract.cjs && bash tests/run-all-251.sh</automated>
  </verify>
  <done>RED recorded with payload before-bytes, then: payload carries verb_count + recommended and no verbs array; recommended-only exception honored; f1_closer_payload fence green; test-209 re-pointed with citation; full before/after byte table recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hash sidecar -> emission decision | a file on disk gates whether Larry sees the decision block |
| suppression gate -> Stop-gate machinery | the card-fire sidechannel record must match what Larry actually saw |
| session-start doctrine -> every turn's behavior | session-level instruction replaces per-turn instruction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-251-01 | Tampering | nav-block-hash sidecar | mitigate | fail-OPEN on missing/corrupt/unreadable sidecar (Test 4); atomic tmp+rename writes; sha256 content compare so a tampered hash can only cause a redundant full emission, never a wrong suppression |
| T-251-02 | Repudiation | Stop-gate vs suppressed turns | mitigate | recordReachedGate hoisted AFTER the suppression gate (Task 1 step 3); Test 3 proves a suppressed turn records zero new reached-gates, so check-card-fire.cjs never demands a card for a block Larry did not see |
| T-251-03 | Information disclosure (Part 8) | sidecar contents | mitigate | sidecar carries only version/sha256/at/bytes - never block text or user content (Test 6 asserts the closed key set) |
| T-251-04 | Denial of service | suppression hiding a changed decision | mitigate | byte-exact sha256 comparison - ANY varying change (reach verbs, percentages, Why) re-emits in full (Test 2); post-compact sidecar reset forces full re-emission after history rewrite; MINDRIAN_NAV_NO_SUPPRESS kill switch |
| T-251-05 | Elevation of privilege | SEED-021 weakening via skeleton move | mitigate | the frozen marker stays per-turn; the imperative is re-seeded on startup, clear, AND compact by the existing matcher; dispatcher surfaces keep their inline imperative (Task 2 Tests 3-4) |

No package installs this phase (zero-npm-deps hard convention; Package Legitimacy Audit: not applicable - no new dependencies of any kind).
</threat_model>

<verification>
- `bash tests/run-all-251.sh` green (glob discovers all three 251-01 test files; found-eq-0 guard live)
- `bash tests/run-all-209.sh` and `bash tests/run-all-210.sh` green with ONLY the cited assertion re-points
- `node --test tests/test-148-frozen-contracts.cjs tests/test-gate-native-fire-w1.cjs` green unmodified
- Every test file's RED run recorded in the SUMMARY before its GREEN (a test that cannot fail is not evidence)
- The SUMMARY carries the three-item before/after byte table (ROADMAP success criterion 2)
- No em-dashes in any touched file (runner fence)
</verification>

<success_criteria>
- CACHE-02 items (a), (b), (c) all land: unchanged decisions suppress to a one-line marker
  with the Stop-gate consistent, the FIRE-IF-FORK imperative rides SessionStart once per
  session (compaction-re-seeded by matcher), and the payload duplication is dead.
- The VARYING parts keep injecting whenever they change - suppression is byte-exact, never
  semantic; fail-open everywhere; the turn is never blocked by any new code path.
- Before/after byte counts recorded per item in the SUMMARY.
- The card-fire contract (SEED-021), the byte-frozen marker, emitBindingGate, the
  dispatcher trailer surfaces, and the persisted f1_closer_payload are all untouched.
</success_criteria>

<output>
Create `.planning/phases/251-cache-aware-trigger-redesign/251-01-SUMMARY.md` when done,
including every recorded RED output and the per-item before/after byte table with the
projected per-session saving.
</output>
