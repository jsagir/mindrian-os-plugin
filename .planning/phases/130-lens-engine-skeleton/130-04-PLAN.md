---
phase: 130-lens-engine-skeleton
plan: 04
type: execute
wave: 4
depends_on: ["130-03"]
files_modified:
  - tests/test-130-lens-engine-e2e.cjs
  - tests/fixtures/phase-130/sample-room/seed.sql
  - tests/run-all-130.sh
  - lib/memory/run-feynman-tests.cjs
autonomous: true
requirements: [E2E-8-TESTS, E2E-COGNITIVE-6, E2E-CONTRACT-2, FEYNMAN-REGISTRATION, PHASE-130-AGGREGATOR]
must_haves:
  truths:
    - "8 E2E tests pass: 6 cognitive-family (serial/parallel/single/consume modes) + 2 engine-contract"
    - "The rejection-as-data E2E proves an onReject path writes a REJECTED_BECAUSE edge"
    - "The persona-aware framing E2E proves a rotation consumes Phase 115 role_blend"
    - "All 8 E2E tests drive room.db ONLY through navigation.cjs (the instrumented zero-leak gate)"
  artifacts:
    - path: "tests/test-130-lens-engine-e2e.cjs"
      provides: "the 8 instrumented E2E tests (the phase release gate)"
      min_lines: 150
    - path: "tests/fixtures/phase-130/sample-room/seed.sql"
      provides: "the minimal room fixture the E2E loop touches"
    - path: "lib/memory/run-feynman-tests.cjs"
      provides: "Phase 130 suite registration in CI"
      contains: "130"
  key_links:
    - from: "tests/test-130-lens-engine-e2e.cjs"
      to: "lib/core/lens-engine.cjs rotate"
      via: "the full rotation -> synthesis -> selector -> accept/reject flow"
      pattern: "rotate\\("
    - from: "tests/test-130-lens-engine-e2e.cjs"
      to: "navigation.writeEdge REJECTED_BECAUSE"
      via: "rejection-as-data contract assertion"
      pattern: "REJECTED_BECAUSE"
---

<objective>
Ship the 8 E2E tests that are the Phase 130 release gate: 6 covering the cognitive family (each command's serial / parallel / single / consume mode) + 2 covering the engine contract (the rejection-as-data path writes a REJECTED_BECAUSE edge; persona-aware framing consumes Phase 115 role_blend). Mirror the 109/129 instrumented pattern: direct-CJS node:assert/strict, fs-instrument zero-leak gate, a minimal seed fixture, and registration in the phase aggregator + the Feynman runner.

Purpose: This is the acceptance criterion "8 E2E tests pass" from 130-CONTEXT. It proves by instrumentation (not promise) that the lens-engine + cognitive migration drive the full rotate -> synthesize -> selector -> accept/reject flow through navigation.cjs with zero non-SQLite filesystem leaks, that rejection becomes a typed REJECTED_BECAUSE edge per Canon Part 4, and that the persona-aware framing hook consumes role_blend per Phase 115.

Output: tests/test-130-lens-engine-e2e.cjs (8 tests); the seed fixture; run-all-130.sh complete; Feynman runner carries the Phase 130 block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-lens-engine-skeleton/130-CONTEXT.md
@.planning/phases/130-lens-engine-skeleton/130-02-SUMMARY.md
@.planning/phases/130-lens-engine-skeleton/130-03-SUMMARY.md

<interfaces>
<!-- The instrumented-test pattern to mirror EXACTLY. -->

From tests/test-129-spine-acceptance.cjs (the 129 instrumented acceptance test -- the structural template):
  direct-CJS (node:assert/strict, no Mocha/Jest, zero new npm deps), setupRoom / cleanup / run,
  fs-instrument install BEFORE the flow + uninstall AFTER, the leaked-reads filtered to EXCLUDE
  allow-listed cache filenames is asserted empty (zero non-SQLite reads), a FOLLOWS_FROM/edge assertion.

From tests/test-navigation-acceptance.cjs (Phase 109 -- the original fs-instrument idiom):
  the fsInstrument that counts non-SQLite filesystem reads during the flow.

From tests/fixtures/phase-129/sample-room/seed.sql (the trimmed-seed shape to mirror):
  room + sections + a couple artifacts + a decision -- the minimum the loop touches. Phase 130's
  fixture adds: a USER.md with a role_blend (for the persona-aware framing E2E) + the anchor nodes
  the INFORMS / REJECTED_BECAUSE edges FK-reference (edges carry FOREIGN KEY to nodes(id)).

From lib/core/lens-engine.cjs: rotate(opts) ; ROTATION_MODES (serial/parallel/single).
From lib/core/navigation.cjs: writeEdge / writeLensFinding / writeHatState / readHatState / logMemoryEvent / findRecentChanges / confirmNode / resolveByUser.
From lib/memory/run-feynman-tests.cjs: the per-phase registration block idiom (Phase 129 block is the most recent example to mirror additively).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the phase-130 seed fixture + the 6 cognitive-family E2E tests</name>
  <files>tests/fixtures/phase-130/sample-room/seed.sql, tests/test-130-lens-engine-e2e.cjs</files>
  <read_first>
    - tests/fixtures/phase-129/sample-room/seed.sql (the trimmed-seed shape: room + sections + artifacts + decision; mirror it and ADD a USER.md role_blend fixture file write in setupRoom + the FK anchor nodes for INFORMS/REJECTED_BECAUSE edges)
    - tests/test-129-spine-acceptance.cjs lines 1-80 (the setupRoom / cleanup / run harness + the fs-instrument zero-leak gate; mirror it)
    - lib/core/lens-engine.cjs rotate signature + ROTATION_MODES (the modes the 6 tests exercise: serial=think-hats, parallel=persona, single=challenge-assumptions, consume=hat-briefing)
    - lib/core/hat-persistence.cjs (the room.db HatState read/write the cognitive tests assert against -- proving the migration landed)
  </read_first>
  <action>Create tests/fixtures/phase-130/sample-room/seed.sql mirroring the phase-129 trimmed seed (a room + 2 sections + 2 artifacts + 1 decision), PLUS the FK anchor nodes the INFORMS + REJECTED_BECAUSE edges reference (so the edge-write assertions do not fail on the FOREIGN KEY to nodes(id) with PRAGMA foreign_keys ON -- the recurring 129 incident). In the test's setupRoom, also write a USER.md carrying a role_blend (e.g. founder 0.6, researcher 0.4) for the persona-aware framing E2E in Task 2. Create tests/test-130-lens-engine-e2e.cjs as a direct-CJS node:assert/strict harness mirroring test-129-spine-acceptance.cjs (setupRoom / cleanup / run; fs-instrument installed BEFORE each flow, uninstalled AFTER, the leaked non-SQLite reads filtered to exclude any allow-listed cache filename asserted empty). Write the 6 cognitive-family E2E tests: (1) think-hats serial -- rotate cognitive six-hats serial produces 6 lens_finding nodes + a tension-map synthesis + a lens_synthesis_completed memory_event; (2) persona parallel -- rotate cognitive six-hats parallel runs all lenses concurrently and writes HatState nodes via navigation, asserted by readAllHatStates; (3) challenge-assumptions single -- rotate cognitive ['black-hat'] single runs exactly one lens and emits exactly one lens_finding_written; (4) hat-briefing consume -- the reader mode reads the prior lens memory_event tail + HatState nodes via navigation (findRecentChanges + readAllHatStates) and produces a briefing without rotating (zero new lens_finding nodes); (5) HatState round-trip -- a saveHatState then loadHatState (now room.db backed) returns the same structured state, proving the filesystem retirement; (6) backfill idempotency -- migrateHatsToRoomDb run twice leaves a single HatState node per color (the sentinel makes the second run a no-op). Each test asserts the instrumented zero-leak gate. NO em-dashes.</action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && node tests/test-130-lens-engine-e2e.cjs 2>&1 | tail -5 && node -e "const fs=require('fs'); if(fs.readFileSync('./tests/test-130-lens-engine-e2e.cjs','utf8').includes(String.fromCharCode(8212)))process.exit(1); console.log('no-emdash')"</automated>
  </verify>
  <done>The seed fixture + USER.md role_blend exist; the 6 cognitive-family E2E tests pass with the instrumented zero-leak gate; zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: Add the 2 engine-contract E2E tests (rejection-as-data + persona-aware framing)</name>
  <files>tests/test-130-lens-engine-e2e.cjs</files>
  <read_first>
    - lib/core/navigation/edges.cjs (writeEdge + the REJECTED_BECAUSE allowed type from Plan 01 -- the rejection-as-data assertion queries the edges table for it)
    - lib/core/user-md-ops.cjs lines 218-240 (readUserMd -> role_blend -- the persona-aware framing assertion confirms the rotation passed role_blend into the perLensFn ctx)
    - lib/core/lens-engine.cjs onAccept/onReject wiring + the persona-aware framing hook (from Plan 02)
  </read_first>
  <action>Append 2 engine-contract E2E tests to tests/test-130-lens-engine-e2e.cjs. Test 7 (rejection-as-data per Canon Part 4): run a rotation, drive a finding through the onReject path with an enum reason scalar, then query the edges table (via navigation neighborhood or a direct read in the test, which is allow-listed under tests/) and assert exactly one REJECTED_BECAUSE edge landed from the lens_finding node, carrying the enum reason in properties + a lens_finding_rejected memory_event was emitted; assert NO INFORMS edge was written for the rejected finding (rejection is strictly the reject path). Test 8 (persona-aware framing consumes Phase 115 role_blend): with the USER.md role_blend fixture from Task 1, run a rotation and assert the role_blend was read (the perLensFn ctx received the role_blend tuple -- capture it via a spy perLensFn that records its ctx) so the framing hook is proven to consume Phase 115; assert the rotation still drives room.db only through navigation (the instrumented gate). NO em-dashes.</action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && node tests/test-130-lens-engine-e2e.cjs 2>&1 | tail -3</automated>
  </verify>
  <done>All 8 E2E tests (6 cognitive + 2 contract) pass; Test 7 proves a REJECTED_BECAUSE edge lands on reject with no INFORMS; Test 8 proves role_blend is consumed; zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the aggregator + register all 130 suites in the Feynman runner + final zero-regression gate</name>
  <files>tests/run-all-130.sh, lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - tests/run-all-130.sh (the aggregator built across Plans 01-03; add the E2E suite and confirm all 130 suites are listed)
    - lib/memory/run-feynman-tests.cjs (the Phase 129 registration block -- mirror it additively for a Phase 130 block; every existing entry must stay byte-unchanged)
    - tests/run-all-129.sh (the canonical aggregator shape for parity)
  </read_first>
  <action>Finalize tests/run-all-130.sh so it runs all Phase 130 CJS suites (test-130-lens-substrate, test-130-lens-engine, test-130-cognitive-migration, test-130-lens-engine-e2e) and exits non-zero on any failure. Add an additive Phase 130 registration block to lib/memory/run-feynman-tests.cjs registering the 4 Phase 130 suites in CI, every existing entry (including the Phase 129 + 129.5 blocks) left byte-unchanged. Run the full zero-regression gate: bash run-all-130.sh GREEN, plus Phase 129 (run-all-129.sh) + Phase 109 (test-navigation-acceptance.cjs) still GREEN. NO em-dashes in any file.</action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && bash tests/run-all-130.sh && bash tests/run-all-129.sh && node tests/test-navigation-acceptance.cjs && node -e "const s=require('fs').readFileSync('./lib/memory/run-feynman-tests.cjs','utf8'); if(!s.includes('test-130'))process.exit(1); console.log('feynman-registered')" && echo ALL_GREEN</automated>
  </verify>
  <done>run-all-130.sh runs all 4 Phase 130 suites GREEN; the Feynman runner carries the additive Phase 130 block with prior blocks byte-unchanged; Phase 129 + Phase 109 still GREEN (zero regression).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> room.db | The E2E tests drive room.db only through navigation.cjs; the fs-instrument gate proves zero non-SQLite leaks |
| fixture USER.md -> role_blend | The persona-aware framing test reads a fixture role_blend; LOCAL-only, never crosses to Brain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-04-01 | Repudiation | E2E proving by promise not instrumentation | mitigate | fs-instrument zero-leak gate on every E2E flow (mirrors 109/129); the gate IS the proof |
| T-130-04-02 | Tampering | rejection path silently skipping the edge | mitigate | Test 7 queries the edges table for the REJECTED_BECAUSE row + asserts no INFORMS for the rejected finding |
| T-130-04-03 | Information disclosure | role_blend leaking off-box | accept | role_blend stays LOCAL (USER.md fixture); the engine never sends it to Brain; assertion is local-only |
| T-130-04-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies (node built-ins + bash); no install tasks |
</threat_model>

<verification>
- 8 E2E tests pass (6 cognitive: serial/parallel/single/consume + round-trip + backfill; 2 contract: rejection-as-data + persona-framing).
- Every E2E asserts the instrumented zero-leak gate.
- Test 7 proves a REJECTED_BECAUSE edge lands on reject; Test 8 proves role_blend is consumed.
- run-all-130.sh GREEN; Feynman runner carries the Phase 130 block.
- Phase 129 + Phase 109 GREEN (zero regression).
- Zero em-dashes across all files.
</verification>

<success_criteria>
- The acceptance criterion "8 E2E tests pass" from 130-CONTEXT is met and instrumented.
- The engine contract (rejection-as-data + persona-aware framing) and the cognitive migration (serial/parallel/single/consume) are proven end-to-end through navigation.cjs.
</success_criteria>

<output>
Create `.planning/phases/130-lens-engine-skeleton/130-04-SUMMARY.md` when done
</output>
