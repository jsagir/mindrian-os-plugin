---
phase: 130-lens-engine-skeleton
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/edges.cjs
  - lib/core/navigation/lens-nodes.cjs
  - lib/core/navigation.cjs
  - docs/architecture/SUBSTRATE-CONTRACT.md
  - tests/test-130-lens-substrate.cjs
autonomous: true
requirements: [LENS-EDGE-ENUM, LENS-NODE-CHOKEPOINT, REJECTED-BECAUSE, INFORMS-EDGE, HATSTATE-NODE]
must_haves:
  truths:
    - "writeEdge accepts INFORMS and REJECTED_BECAUSE edge types (the engine onAccept / onReject paths)"
    - "A typed HatState node can be written to room.db only through navigation.cjs (no filesystem)"
    - "A typed lens-finding node can be written to room.db only through navigation.cjs"
    - "REJECTED_BECAUSE carries an enum-only reason scalar (rejection-as-data per Canon Part 4)"
  artifacts:
    - path: "lib/core/navigation/edges.cjs"
      provides: "INFORMS + REJECTED_BECAUSE added to ALLOWED_EDGE_TYPES (closes review finding H2)"
      contains: "REJECTED_BECAUSE"
    - path: "lib/core/navigation/lens-nodes.cjs"
      provides: "writeHatState + writeLensFinding chokepoint node writers (allow-listed)"
      exports: ["writeHatState", "readHatState", "readAllHatStates", "writeLensFinding"]
    - path: "lib/core/navigation.cjs"
      provides: "re-exports of the lens-node writers + readers"
      contains: "writeHatState"
  key_links:
    - from: "lib/core/lens-engine.cjs (Plan 02)"
      to: "navigation.writeEdge"
      via: "onAccept INFORMS / onReject REJECTED_BECAUSE"
      pattern: "writeEdge.*(INFORMS|REJECTED_BECAUSE)"
    - from: "lib/core/hat-persistence.cjs (Plan 03)"
      to: "navigation.writeHatState"
      via: "room.db typed HatState node write"
      pattern: "writeHatState"
---

<objective>
Ship the shared graph substrate the lens-engine writes through: two net-new edge types (INFORMS, REJECTED_BECAUSE) on the closed allowlist, and a new allow-listed navigation submodule (lens-nodes.cjs) that writes typed HatState + lens-finding nodes to room.db. This is Wave 1: it lands the enum + chokepoint BEFORE the engine (Plan 02) and the command migrations (Plan 03) consume them.

Purpose: The engine's onAccept writes INFORMS and onReject writes REJECTED_BECAUSE, both through navigation.writeEdge (closing review finding H2, the edge-allowlist bypass). The cognitive-family migration RETIRES the filesystem .mindrian/hats/{color}/STATE.md writes to room.db typed HatState nodes; those writes must route through the navigation chokepoint or the live substrate guard blocks the commit. This plan provides the door.

Output: edges.cjs allowlist extended; lens-nodes.cjs created (allow-listed under lib/core/navigation/); navigation.cjs re-exports; SUBSTRATE-CONTRACT M11 amendment; RED-first behavior test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-lens-engine-skeleton/130-CONTEXT.md

<interfaces>
<!-- Extracted from codebase. The executor should use these directly. -->

From lib/core/navigation/edges.cjs (ALLOWED_EDGE_TYPES currently holds, in order):
  DEFERRED, REJECTED, DERIVED_FROM, FILED_AS_DECISION, FOLLOWS_FROM, OPERATOR_TRANSITION
  -- NOTE: INFORMS appears ONLY in a comment ("the shipped vocabulary INFORMS / ...")
     but is NOT in the Set. REJECTED_BECAUSE is absent entirely. Both must be ADDED.
  writeEdge(db, params) where params = { source_id, target_id, edge_type, properties }
  returns { ok:true, edge_id, type, source, target } | { ok:false, reason, detail? }.
  Validation order: invalid_params, invalid_source_id, invalid_target_id,
  invalid_edge_type (when edge_type not in Set). Edge UPSERT on (source,target,type).

From lib/core/navigation/ingestion.cjs (the canon-legal node-write shape; this file is allow-listed):
  INSERT INTO nodes (id, type, properties, source_path, created_by, confidence, review_status, created_at, last_seen_at)
  VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
  nodes columns: id, type, properties (JSON TEXT), source_path, created_by, confidence,
  review_status, created_at, last_seen_at, confirmed_by.

From lib/core/navigation/memory-events.cjs:
  logEvent(db, eventType, payload) -- eventType MUST be in EVENT_TYPES Set or it returns ok:false.

From lib/core/room-db.cjs:
  openRoomDb(roomDir) -> db ; closeRoomDb(db). Tests open fixtures via these.

Canon Part 9 v1.5 carve-out: a HatState node is a SYSTEM-BOOKKEEPING node (records what
the system DID about hat rotation, not a truth-claim about the venture). It MAY carry
created_by='system' review_status='confirmed' WITHOUT a human byUser. It is NOT in the
truth-claim set {claim, CausalClaim, assumption, decision, opportunity}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add INFORMS + REJECTED_BECAUSE to ALLOWED_EDGE_TYPES (close H2)</name>
  <files>lib/core/navigation/edges.cjs, tests/test-130-lens-substrate.cjs</files>
  <read_first>
    - lib/core/navigation/edges.cjs (the full ALLOWED_EDGE_TYPES Set + writeEdge + the FOLLOWS_FROM additive comment block at lines 71-88 as the idiom to mirror)
    - lib/core/navigation/memory-events.cjs lines 10-53 (the additive-Set comment idiom + FLOOR-not-exact-count test convention)
  </read_first>
  <behavior>
    - Test: writeEdge(db, {source_id, target_id, edge_type:'INFORMS', properties:{}}) returns ok:true and inserts an INFORMS edge row.
    - Test: writeEdge(db, {source_id, target_id, edge_type:'REJECTED_BECAUSE', properties:{reason:'low_evidence'}}) returns ok:true and the reason scalar round-trips in properties.
    - Test: ALLOWED_EDGE_TYPES.has('INFORMS') and .has('REJECTED_BECAUSE') are both true; a FLOOR assertion (the 6 pre-existing types still present) guards against regression, not an exact size.
    - Test: writeEdge with a bogus edge_type ('ASSOCIATION_LENS') still returns ok:false invalid_edge_type (the rejected lens-class taxonomy stays rejected per the 2026-05-16 dual-graph verdict).
  </behavior>
  <action>Add two string members to the ALLOWED_EDGE_TYPES Set in edges.cjs: 'INFORMS' and 'REJECTED_BECAUSE'. Add them with a Phase 130-01 additive comment block mirroring the FOLLOWS_FROM block verbatim in structure. State in the comment: INFORMS is the lens-engine onAccept cascade edge (a lens finding INFORMS a target node); REJECTED_BECAUSE is the lens-engine onReject rejection-as-data edge per Canon Part 4 ("why not" is graph data). Note INFORMS was named in the shipped-vocabulary comment since Phase 129-01 but never actually in the Set -- this lands it (closes review finding H2, the edge-allowlist bypass where the engine would otherwise write an edge type writeEdge rejects). Properties stay enum-only per Canon Part 8: REJECTED_BECAUSE carries a single reason scalar (a short enum string such as low_evidence / off_topic / duplicate / out_of_scope), never freeform user content. Do NOT add ASSOCIATION_LENS or TRANSITION_LENS (rejected per dual-graph verdict). For the test scaffold, mirror tests/test-129-spine-substrate.cjs: openRoomDb on an fs.mkdtempSync fixture, seed the two FK-referenced anchor nodes before each writeEdge assertion (edges carry FOREIGN KEY to nodes(id) with PRAGMA foreign_keys ON -- see the 129-01 SUMMARY incident).</action>
  <verify>
    <automated>node -e "const {ALLOWED_EDGE_TYPES}=require('./lib/core/navigation/edges.cjs'); if(!ALLOWED_EDGE_TYPES.has('INFORMS')||!ALLOWED_EDGE_TYPES.has('REJECTED_BECAUSE')){process.exit(1)} for(const t of ['DEFERRED','REJECTED','DERIVED_FROM','FILED_AS_DECISION','FOLLOWS_FROM','OPERATOR_TRANSITION']){if(!ALLOWED_EDGE_TYPES.has(t)){process.exit(2)}} console.log('ok')"</automated>
  </verify>
  <done>ALLOWED_EDGE_TYPES contains INFORMS + REJECTED_BECAUSE plus the 6 pre-existing types; ASSOCIATION_LENS/TRANSITION_LENS stay rejected; the RED test for both new edge writes passes GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create lens-nodes.cjs node-write chokepoint + navigation.cjs re-exports + SUBSTRATE-CONTRACT amendment</name>
  <files>lib/core/navigation/lens-nodes.cjs, lib/core/navigation.cjs, docs/architecture/SUBSTRATE-CONTRACT.md, tests/test-130-lens-substrate.cjs</files>
  <read_first>
    - lib/core/navigation/ingestion.cjs lines 1-75 (the INSERT INTO nodes shape with full provenance columns -- mirror it; this file is allow-listed under lib/core/navigation/)
    - lib/core/navigation/spine-events.cjs is the structural sibling: a roomDir-taking submodule that opens/closes room.db internally; mirror its open/close/no_room_db idiom (grep it for openRoomDb usage)
    - lib/core/navigation.cjs lines 105-134 (the spine-events + confirm-node re-export blocks -- the additive-re-export idiom to copy, each with a per-export justification comment)
    - docs/architecture/SUBSTRATE-CONTRACT.md lines 59-147 (the M11 export allow-list section + the spine-events.cjs amendment paragraph at ~116-130 as the template for the new amendment)
    - lib/core/hat-persistence.cjs lines 19-28 (HAT_COLORS + HAT_LABELS -- the HatState node id convention is hatstate:{color})
  </read_first>
  <behavior>
    - Test: writeHatState(db, color, state) writes a node id 'hatstate:'+color, type 'HatState', created_by 'system', review_status 'confirmed', properties JSON carrying {current_focus, top_concerns, top_opportunities, methodology_notes, session_count, last_analysis}. Returns {ok:true, node_id}.
    - Test: writeHatState rejects a non-canonical color with {ok:false, reason:'invalid_hat_color'} (the 6-color closed set).
    - Test: readHatState(db, color) round-trips a previously written HatState node back to the structured object; readAllHatStates(db) returns the map of 6 colors (defaults for unwritten ones).
    - Test: writeLensFinding(db, {lensType, lens, topic, summary, sessionId}) writes a node type 'lens_finding' created_by 'system' review_status 'proposed', returns {ok:true, node_id}; it is the node the engine onAccept INFORMS edge points FROM.
    - Test: a source-grep over lens-nodes.cjs body proves zero require of node:sqlite / better-sqlite3 (it takes a db handle owned by the caller, like edges.cjs writeEdge -- never opens room.db itself).
  </behavior>
  <action>Create lib/core/navigation/lens-nodes.cjs as an allow-listed navigation submodule. It exports writeHatState(db, color, state), readHatState(db, color), readAllHatStates(db), and writeLensFinding(db, params). Each takes a db handle (owned by the caller via openRoomDb) exactly like edges.cjs writeEdge -- the module NEVER requires node:sqlite and NEVER opens room.db itself, so it stays inside the lib/core/navigation/ allow-list with zero substrate bypass. writeHatState UPSERTs a node with id 'hatstate:'+color, type 'HatState', created_by 'system', confidence 1.0, review_status 'confirmed', created_at/last_seen_at = Date.now(), properties = JSON.stringify of the structured hat state. The created_by='system' review_status='confirmed' is canon-legal WITHOUT a human byUser because a HatState node is a system-bookkeeping node per the Canon Part 9 v1.5 audit-node carve-out (it is NOT in the truth-claim set {claim, CausalClaim, assumption, decision, opportunity}); cite the carve-out in an in-source comment. Validate color against the closed 6-color set (white/red/black/yellow/green/blue), returning invalid_hat_color otherwise. readHatState SELECTs the node by id and JSON.parses properties, returning the default-shaped object (matching hat-persistence.cjs loadHatState's default shape: current_focus 'General analysis', last_analysis 'never', empty arrays, session_count 0) when absent. writeLensFinding UPSERTs a node type 'lens_finding' review_status 'proposed' created_by 'system' with id 'lens_finding:'+lensType+':'+sessionId+':'+lens, properties carrying {lensType, lens, topic, summary}. Use the INSERT INTO nodes shape from ingestion.cjs verbatim (full provenance columns). All writes are defensive: never throw on caller input, return {ok:false, reason} on failure. Then add re-exports to lib/core/navigation.cjs (writeHatState, readHatState, readAllHatStates, writeLensFinding) with a Phase 130-01 per-export justification block mirroring the spine-events block at lines 105-120. Then add a Phase 130-01 amendment paragraph to the M11 allow-list section of docs/architecture/SUBSTRATE-CONTRACT.md naming lens-nodes.cjs, its 4 exports, its consumers (Plan 02 lens-engine.cjs; Plan 03 hat-persistence.cjs rewrite), and stating it takes a caller-owned db handle so it carries zero direct room.db open. NO em-dashes anywhere (use hyphens). NO fenced code blocks in this action; the INSERT shape is named, not inlined.</action>
  <verify>
    <automated>node -e "const m=require('./lib/core/navigation/lens-nodes.cjs'); for(const f of ['writeHatState','readHatState','readAllHatStates','writeLensFinding']){if(typeof m[f]!=='function'){console.error('missing '+f);process.exit(1)}} const n=require('./lib/core/navigation.cjs'); if(typeof n.writeHatState!=='function'){process.exit(2)} const src=require('fs').readFileSync('./lib/core/navigation/lens-nodes.cjs','utf8'); if(/require\(['\"](?:node:sqlite|better-sqlite3)['\"]\)/.test(src)){console.error('direct sqlite require');process.exit(3)} if(src.includes(String.fromCharCode(8212))){console.error('em-dash');process.exit(4)} console.log('ok')"</automated>
  </verify>
  <done>lens-nodes.cjs exports the 4 writers/readers; navigation.cjs re-exports them; SUBSTRATE-CONTRACT M11 carries the amendment; the module has zero direct sqlite require and zero em-dashes; the RED behavior test for HatState round-trip + lens_finding write passes GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the 130 substrate suite into the test aggregator + run the zero-regression gate</name>
  <files>tests/test-130-lens-substrate.cjs, tests/run-all-130.sh</files>
  <read_first>
    - tests/run-all-129.sh (the aggregator shape to mirror for run-all-130.sh)
    - tests/test-129-spine-substrate.cjs lines 1-50 (the direct-CJS node:assert/strict harness, setupRoom/cleanup/run, openRoomDb fixture idiom)
  </read_first>
  <action>Create tests/run-all-130.sh mirroring tests/run-all-129.sh: a bash aggregator that runs the phase-130 CJS suites and exits non-zero on any failure; register tests/test-130-lens-substrate.cjs as its first entry (Plans 02/03/04 add their suites in later plans). Make it executable. Confirm zero regression: tests/test-129-spine-substrate.cjs and tests/test-navigation-acceptance.cjs (Phase 109) still pass, proving the edges.cjs + navigation.cjs additive changes broke nothing.</action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && node tests/test-130-lens-substrate.cjs && node tests/test-129-spine-substrate.cjs && node tests/test-navigation-acceptance.cjs && echo ALL_GREEN</automated>
  </verify>
  <done>run-all-130.sh exists and is executable with the substrate suite registered; the 130 suite passes; Phase 129 + Phase 109 suites still pass (zero regression).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> writeEdge / lens-nodes writers | Caller supplies edge_type + node properties; the allowlist + enum-only props are the gate |
| lens-nodes.cjs -> room.db | All node writes go through this allow-listed submodule over a caller-owned db handle; never opens room.db itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-01-01 | Tampering | writeEdge edge_type | mitigate | Closed ALLOWED_EDGE_TYPES Set; ASSOCIATION_LENS/TRANSITION_LENS stay rejected; H2 closed by adding INFORMS + REJECTED_BECAUSE explicitly |
| T-130-01-02 | Information disclosure | REJECTED_BECAUSE properties | mitigate | Enum-only reason scalar per Canon Part 8; no freeform user content on the edge; never crosses to Brain |
| T-130-01-03 | Elevation of privilege | HatState review_status=confirmed | accept | System-bookkeeping carve-out (Canon Part 9 v1.5); HatState is not a truth-claim node, so created_by=system confirmed is legal without human byUser; cited in source |
| T-130-01-04 | Tampering | lens-nodes.cjs substrate bypass | mitigate | Module takes a caller-owned db handle, zero node:sqlite require; source-grep test enforces; allow-listed under lib/core/navigation/ |
| T-130-01-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies (node built-ins + bash only); no install tasks in this plan |
</threat_model>

<verification>
- ALLOWED_EDGE_TYPES has INFORMS + REJECTED_BECAUSE plus the 6 pre-existing types.
- lens-nodes.cjs writes HatState + lens_finding nodes with zero direct sqlite require.
- navigation.cjs re-exports the 4 lens-node functions.
- SUBSTRATE-CONTRACT M11 carries the Phase 130-01 amendment.
- Phase 129 + Phase 109 suites pass (zero regression).
- Zero em-dashes in every file created/modified.
</verification>

<success_criteria>
- The engine (Plan 02) and the hat-persistence rewrite (Plan 03) have a canon-legal door for every edge + node write they need: INFORMS, REJECTED_BECAUSE, HatState, lens_finding -- all through navigation.cjs.
- The live substrate guard (--diff) stays clean on the staged changes (edges.cjs + lens-nodes.cjs + navigation.cjs are all allow-listed).
</success_criteria>

<output>
Create `.planning/phases/130-lens-engine-skeleton/130-01-SUMMARY.md` when done
</output>
