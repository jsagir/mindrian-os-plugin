---
phase: 130-lens-engine-skeleton
plan: 03
type: execute
wave: 3
depends_on: ["130-02"]
files_modified:
  - lib/core/hat-persistence.cjs
  - lib/core/persona-ops.cjs
  - scripts/migrate-hats-to-roomdb.cjs
  - commands/think-hats.md
  - commands/persona.md
  - commands/hat-briefing.md
  - commands/challenge-assumptions.md
  - tests/test-130-cognitive-migration.cjs
  - tests/run-all-130.sh
autonomous: true
requirements: [HATS-STATE-MIGRATION, HATS-BACKFILL, COGNITIVE-CLIENTS-4, DEDUP-TENSION-MAP, RETIRE-FS-HATS]
must_haves:
  truths:
    - "hat-persistence.cjs reads/writes HatState via navigation.cjs room.db nodes, not .mindrian/hats/{color}/STATE.md"
    - "Existing .mindrian/hats/*/STATE.md content backfills into room.db HatState nodes one-shot on first run after upgrade"
    - "The 4 cognitive-family commands carry thin lens-engine client frontmatter (lens_set / rotation_mode / synthesizer)"
    - "The 4 duplicated tension-map implementations are deleted; only lib/core/synthesizers/tension-map.cjs remains"
    - "persona-ops.cjs rotation loop delegates to the engine's single loop"
  artifacts:
    - path: "lib/core/hat-persistence.cjs"
      provides: "room.db-backed HatState read/write via navigation.cjs (filesystem STATE.md writes RETIRED)"
      contains: "writeHatState"
    - path: "scripts/migrate-hats-to-roomdb.cjs"
      provides: "one-shot backfill of legacy .mindrian/hats/*/STATE.md into HatState nodes"
      exports: ["migrateHatsToRoomDb"]
    - path: "commands/think-hats.md"
      provides: "thin lens-engine client frontmatter"
      contains: "lens_set: six-hats"
  key_links:
    - from: "lib/core/hat-persistence.cjs"
      to: "navigation.writeHatState / navigation.readHatState"
      via: "room.db typed HatState node"
      pattern: "navigation\\.(writeHatState|readHatState|readAllHatStates)"
    - from: "scripts/migrate-hats-to-roomdb.cjs"
      to: "navigation.writeHatState"
      via: "one-shot backfill"
      pattern: "writeHatState"
---

<objective>
Migrate the cognitive-lens family off the filesystem and onto the lens-engine. hat-persistence.cjs RETIRES its .mindrian/hats/{color}/STATE.md writes and reads/writes room.db typed HatState nodes via navigation.cjs (paying down the baselined Canon Part 9 violation the Cluster 2 audit flagged). A one-shot backfill migrates existing users' STATE.md content into HatState nodes on first run, then leaves the markdown as a read-only archive (pre-resolved decision). The 4 cognitive-family commands become thin lens-engine clients. The 4 duplicated tension-map implementations collapse into the one tension-map.cjs from Plan 02.

Purpose: This is the P8 cognitive-lens migration. It proves the engine works against a real lens family and closes the latent filesystem-state Part 9 violation. It is also the Canon Part 7 reuse payoff: ~600 lines of duplicated wrapper prose collapse to ~15 lines of frontmatter per command + the shared engine.

Output: hat-persistence.cjs rewritten to room.db; backfill script; 4 thin command clients; duplicated tension-maps deleted; persona-ops rotation loop delegating to the engine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-lens-engine-skeleton/130-CONTEXT.md
@.planning/phases/130-lens-engine-skeleton/130-01-SUMMARY.md
@.planning/phases/130-lens-engine-skeleton/130-02-SUMMARY.md

<interfaces>
<!-- From Plans 01 + 02 (shipped). -->

From lib/core/navigation.cjs:
  writeHatState(db, color, state) -> {ok, node_id} (HatState node, created_by system, review_status confirmed).
  readHatState(db, color) -> structured state (default shape when absent).
  readAllHatStates(db) -> map of 6 colors.
  writeLensFinding / writeEdge / logMemoryEvent / confirmNode / resolveByUser.

From lib/core/lens-engine.cjs:
  rotate(opts) -- the single rotation for-loop; LENS_REGISTRY; ROTATION_MODES (serial/parallel/single).

From lib/core/synthesizers/tension-map.cjs:
  synthesizeTensionMap(findingNodes) -- the ONE tension-map; the 4 duplicates must point here or be deleted.

From lib/core/room-db.cjs:
  openRoomDb(roomDir) / closeRoomDb(db). hat-persistence.cjs is NOT allow-listed, so it must NOT require
  room-db.cjs directly; it must take a roomDir and route through navigation. The migrate-* script IS
  allow-listed (scripts/migrate- prefix in check-substrate.cjs), so it MAY open room.db directly if needed,
  but PREFER routing through navigation.writeHatState for consistency.

Current hat-persistence.cjs exports (the contract callers depend on -- preserve the signatures):
  HAT_COLORS, HAT_LABELS, loadHatState(roomDir,color), saveHatState(roomDir,color,state),
  logSession(roomDir,color,findings), loadAllHatStates(roomDir), getRecentLogs(roomDir,color,days).
  Callers: lib/core/persona-ops.cjs (lines 467, 490, 514-516), commands/hat-briefing.md Step 1 node -e block.

Pre-resolved decision (130-CONTEXT): backfill once + leave the markdown as a read-only archive;
going-forward writes via navigation.cjs only. Session-log markdown (.mindrian/hats/{color}/session-log/)
is NOT migrated this phase -- only STATE.md becomes a HatState node; the daily logs stay as the archive.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite hat-persistence.cjs to read/write HatState via navigation.cjs + add the one-shot backfill</name>
  <files>lib/core/hat-persistence.cjs, scripts/migrate-hats-to-roomdb.cjs, tests/test-130-cognitive-migration.cjs</files>
  <read_first>
    - lib/core/hat-persistence.cjs (the full file -- the 7 exported functions + the parseHatState/serializeHatState state shape; the filesystem STATE.md writes at saveHatState lines 203-226 + logSession lines 235-298 are what gets RETIRED)
    - lib/core/navigation.cjs lines 88-134 (writeHatState/readHatState/readAllHatStates -- the chokepoint the rewrite routes through; hat-persistence is NOT allow-listed so it must take roomDir and route through navigation, opening room.db ONLY via a navigation helper, never via a direct room-db.cjs require)
    - lib/core/navigation/spine-events.cjs (the roomDir-taking idiom: a non-allowlisted-caller-facing function that resolves the db handle through the navigation surface; mirror how it gets a db from roomDir without a direct room-db require)
    - scripts/check-substrate.cjs lines 63-90 (the ALLOWED_DIRECT_IMPORT list -- confirm hat-persistence.cjs is NOT on it and scripts/migrate-* IS, so the rewrite must route through navigation and the migrate script may open room.db)
  </read_first>
  <action>Rewrite hat-persistence.cjs so the 6 STATE.md filesystem reads/writes become room.db typed HatState node reads/writes through navigation.cjs, while PRESERVING the 7 exported function signatures (HAT_COLORS, HAT_LABELS, loadHatState, saveHatState, logSession, loadAllHatStates, getRecentLogs) so existing callers (persona-ops.cjs, hat-briefing.md) keep working. loadHatState(roomDir, color) now reads the HatState node via navigation.readHatState; saveHatState(roomDir, color, state) now writes via navigation.writeHatState; loadAllHatStates(roomDir) via navigation.readAllHatStates. The module is NOT in the substrate allow-list, so it must NOT require lib/core/room-db.cjs directly and must NOT require node:sqlite: it takes roomDir and obtains the db handle through the navigation surface exactly as the spine-events consumers do (route every room.db touch through navigation.cjs or the live substrate guard --diff blocks the commit). RETIRE the filesystem STATE.md writes: grep must prove hat-persistence.cjs no longer calls fs.writeFileSync against a .mindrian/hats/{color}/STATE.md path (the serializeHatState helper may stay ONLY if used by the read-only archive path, but the write path goes to room.db). getRecentLogs MAY still read the session-log markdown archive (the daily logs are NOT migrated this phase per the pre-resolved decision -- only STATE.md becomes a node); logSession's STATE.md side-write becomes a navigation.writeHatState call (the daily markdown log append MAY stay as the archive, but the canonical session_count + concerns/opportunities state lives in the HatState node). Then create scripts/migrate-hats-to-roomdb.cjs exporting migrateHatsToRoomDb(roomDir): a ONE-SHOT idempotent backfill that scans existing .mindrian/hats/*/STATE.md files, parses each via the existing parseHatState shape, writes each to a HatState node via navigation.writeHatState, and stamps an idempotency sentinel (a memory_event or an identity-table row, mirroring the phase-109 migration sentinel pattern) so a second run is a no-op; the legacy markdown files are LEFT IN PLACE as a read-only archive (do NOT delete them). The migrate script is allow-listed (scripts/migrate- prefix) so it may open room.db, but prefer navigation.writeHatState. NO em-dashes; NO fenced code blocks in source.</action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && node -e "const src=require('fs').readFileSync('./lib/core/hat-persistence.cjs','utf8'); if(/writeFileSync\([^)]*\.mindrian[^)]*hats[^)]*STATE\.md/i.test(src.replace(/\s+/g,' '))){console.error('still writes STATE.md');process.exit(1)} if(/require\(['\"][^'\"]*room-db\.cjs['\"]\)/.test(src)){console.error('direct room-db require');process.exit(2)} if(/require\(['\"](?:node:sqlite|better-sqlite3)['\"]\)/.test(src)){process.exit(3)} if(src.includes(String.fromCharCode(8212)))process.exit(4); const m=require('./scripts/migrate-hats-to-roomdb.cjs'); if(typeof m.migrateHatsToRoomDb!=='function')process.exit(5); const h=require('./lib/core/hat-persistence.cjs'); for(const f of ['HAT_COLORS','loadHatState','saveHatState','logSession','loadAllHatStates','getRecentLogs']){if(h[f]===undefined)process.exit(6)} console.log('ok')"</automated>
  </verify>
  <done>hat-persistence.cjs reads/writes HatState via navigation.cjs (zero STATE.md fs.writeFileSync, zero direct room-db/sqlite require); the 7 export signatures are preserved; migrate-hats-to-roomdb.cjs ships an idempotent one-shot backfill that leaves the markdown as a read-only archive; zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the 4 cognitive-family commands to thin lens-engine clients + delete the duplicated tension-maps + delegate persona-ops loop</name>
  <files>commands/think-hats.md, commands/persona.md, commands/hat-briefing.md, commands/challenge-assumptions.md, lib/core/persona-ops.cjs, tests/test-130-cognitive-migration.cjs</files>
  <read_first>
    - commands/think-hats.md (45 lines), commands/persona.md (200 lines), commands/hat-briefing.md (127 lines), commands/challenge-assumptions.md (36 lines) -- the current full prose wrappers + their Phase 122 workflow-layer frontmatter (kind/frameworks/produces/autonomous_safe must be preserved)
    - lib/core/persona-ops.cjs lines 490-524 (the analyzeAllPerspectives serial for-loop over HAT_COLORS -- delegate it to lens-engine.rotate; plus grep persona-ops.cjs for the inter-hat tension code that duplicates tension-map)
    - lib/core/lens-engine.cjs (rotate signature + LENS_REGISTRY cognitive entry from Plan 02)
    - grep the 4 tension-map duplicate implementations: search persona-ops.cjs, hat-persistence.cjs, and the find-analogies surface for "tension" computation, and replace each with a call to lib/core/synthesizers/tension-map.cjs synthesizeTensionMap
  </read_first>
  <action>Add thin lens-engine client frontmatter to each of the 4 cognitive-family commands, preserving their existing Phase 122 workflow-layer frontmatter (kind, frameworks, produces, inputs, autonomous_safe, allowed-tools). think-hats.md gains lens_set: six-hats, rotation_mode: serial, synthesizer: tension-map. persona.md gains lens_set: six-hats, rotation_mode: parallel, persistence: memory_event. hat-briefing.md gains a reader declaration (it CONSUMES the prior lens memory_event tail + HatState nodes -- a reader, not a rotator: lens_set: six-hats, rotation_mode: consume). challenge-assumptions.md gains lens_set: ['black-hat'], rotation_mode: single, synthesizer: tension-map. In each command body, replace the hand-rolled rotation prose with a short directive pointing the executor at the lens-engine (the engine owns the loop, the synthesizers, the F-selector dispatch, and the memory_event emission); keep the Larry voice + the framework-reference reads, but the rotation mechanics are now the engine's. The hat-briefing.md Step 1 node -e block that calls loadAllHatStates stays working (Task 1 preserved the signature) but now reads HatState nodes. Then in lib/core/persona-ops.cjs, replace the analyzeAllPerspectives manual for-loop (lines ~514-517) with a delegation to lens-engine.rotate using rotationMode 'serial' over the cognitive six-hats set, and replace any inline inter-hat tension computation with a call to lib/core/synthesizers/tension-map.cjs synthesizeTensionMap. Delete the duplicated tension-map implementations wherever they live (persona-ops.cjs + any sibling) so ONLY lib/core/synthesizers/tension-map.cjs remains -- a repo grep for a second tension-computation block must come back to the single synthesizer. NO em-dashes anywhere.</action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && node -e "const fs=require('fs'); const th=fs.readFileSync('./commands/think-hats.md','utf8'); if(!/lens_set:\s*six-hats/.test(th)||!/rotation_mode:\s*serial/.test(th))process.exit(1); const ca=fs.readFileSync('./commands/challenge-assumptions.md','utf8'); if(!/lens_set:\s*\[?'?black-hat'?\]?/.test(ca)||!/rotation_mode:\s*single/.test(ca))process.exit(2); const pe=fs.readFileSync('./commands/persona.md','utf8'); if(!/rotation_mode:\s*parallel/.test(pe))process.exit(3); const hb=fs.readFileSync('./commands/hat-briefing.md','utf8'); if(!/rotation_mode:\s*consume/.test(hb))process.exit(4); for(const f of ['./commands/think-hats.md','./commands/persona.md','./commands/hat-briefing.md','./commands/challenge-assumptions.md','./lib/core/persona-ops.cjs']){if(fs.readFileSync(f,'utf8').includes(String.fromCharCode(8212))){console.error('em-dash in '+f);process.exit(5)}} console.log('ok')"</automated>
  </verify>
  <done>The 4 commands carry thin lens-engine client frontmatter (lens_set / rotation_mode / synthesizer) with their Phase 122 frontmatter preserved; persona-ops.cjs delegates its rotation loop to lens-engine.rotate; the duplicated tension-maps are deleted leaving only synthesizers/tension-map.cjs; zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 3: Register the migration suite + substrate-guard the rewritten hat-persistence + zero-regression gate</name>
  <files>tests/run-all-130.sh, tests/test-130-cognitive-migration.cjs</files>
  <read_first>
    - scripts/check-substrate.cjs (the guard scanFiles must return [] for the rewritten hat-persistence.cjs -- it is NOT allow-listed, so it must carry zero direct room.db access after the rewrite)
    - tests/run-all-130.sh (append the migration suite)
    - lib/core/persona-ops.cjs callers + any existing persona-ops test to confirm zero regression after the loop delegation
  </read_first>
  <action>Register tests/test-130-cognitive-migration.cjs in tests/run-all-130.sh. Run scripts/check-substrate.cjs scanFiles over lib/core/hat-persistence.cjs + lib/core/persona-ops.cjs and assert zero violations (the rewrite routes all room.db access through navigation.cjs). Confirm zero regression across the prior 130 suites + Phase 109 + any persona-ops regression test.</action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && node -e "const g=require('./scripts/check-substrate.cjs'); const fs=require('fs'); const v=g.scanFiles(['lib/core/hat-persistence.cjs','lib/core/persona-ops.cjs'],(p)=>{try{return fs.readFileSync(p,'utf8')}catch(_){return ''}}); if(v.length){console.error(JSON.stringify(v));process.exit(1)} console.log('substrate-clean')" && node tests/test-130-cognitive-migration.cjs && node tests/test-130-lens-engine.cjs && node tests/test-130-lens-substrate.cjs && node tests/test-navigation-acceptance.cjs && echo ALL_GREEN</automated>
  </verify>
  <done>The migration suite is registered; the substrate guard returns zero violations for the rewritten hat-persistence + persona-ops; all prior 130 suites + Phase 109 pass (zero regression).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy STATE.md -> backfill -> room.db | One-shot migration reads filesystem markdown, writes HatState nodes via navigation |
| hat-persistence.cjs -> room.db | All HatState access routes through navigation.cjs after the rewrite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-03-01 | Tampering | hat-persistence.cjs substrate bypass | mitigate | Rewrite routes through navigation.cjs; substrate guard scanFiles asserted clean (Task 3); zero room-db require |
| T-130-03-02 | Repudiation | backfill double-run corrupting state | mitigate | Idempotency sentinel (identity row / memory_event) so a second migrateHatsToRoomDb run is a no-op; markdown left intact |
| T-130-03-03 | Denial of service | absent room.db during backfill | accept | migrate script degrades to no-op when room.db absent (defensive); legacy STATE.md still readable as archive |
| T-130-03-04 | Elevation of privilege | HatState confirmed without human | accept | System-bookkeeping carve-out (Canon Part 9 v1.5); HatState is not a truth-claim node -- inherited from Plan 01 |
| T-130-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies; no install tasks |
</threat_model>

<verification>
- hat-persistence.cjs reads/writes HatState via navigation.cjs (zero STATE.md fs writes, zero direct room-db/sqlite require).
- The one-shot backfill is idempotent and leaves the markdown as a read-only archive.
- The 4 commands are thin lens-engine clients with preserved Phase 122 frontmatter.
- The duplicated tension-maps are deleted; only synthesizers/tension-map.cjs remains.
- The substrate guard returns zero violations for the rewritten modules.
- All prior 130 suites + Phase 109 pass (zero regression).
- Zero em-dashes across all files.
</verification>

<success_criteria>
- The cognitive lens family runs entirely on the engine + room.db; the latent filesystem-state Canon Part 9 violation is paid down.
- ~600 lines of duplicated wrapper prose are gone; the 4 commands are thin frontmatter clients.
</success_criteria>

<output>
Create `.planning/phases/130-lens-engine-skeleton/130-03-SUMMARY.md` when done
</output>
