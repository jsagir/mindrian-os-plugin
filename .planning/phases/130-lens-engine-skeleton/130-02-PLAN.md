---
phase: 130-lens-engine-skeleton
plan: 02
type: tdd
wave: 2
depends_on: ["130-01"]
files_modified:
  - lib/core/navigation/memory-events.cjs
  - lib/core/synthesizers/tension-map.cjs
  - lib/core/synthesizers/comparison-matrix.cjs
  - lib/core/synthesizers/convergence-map.cjs
  - lib/core/lens-engine.cjs
  - tests/test-130-lens-engine.cjs
  - tests/run-all-130.sh
autonomous: true
requirements: [LENS-ENGINE-CORE, LENS-ROTATE-INTERFACE, ROTATION-MODES, SYNTHESIZERS-3, LENS-REGISTRY-5, LENS-MEMORY-EVENT]
must_haves:
  truths:
    - "lens-engine.cjs exports rotate({lensType, lensSet, input, perLensFn, synthesize, surfaceSelector, persistence, onAccept, onReject})"
    - "rotate runs serial / parallel / single rotation modes over a lens set"
    - "Every rotation emits a memory_event (mandatory per Canon Part 9)"
    - "The registry has 5 family slots; cognitive is populated, the other 4 reserved with client_count 0"
    - "Synthesizers receive the typed node IDs that were written, not raw per-lens output arrays"
    - "rotate writes via navigation.cjs only (engine carries zero direct room.db access)"
  artifacts:
    - path: "lib/core/lens-engine.cjs"
      provides: "the rotate() engine + 5-family registry + the single rotation for-loop"
      exports: ["rotate", "LENS_REGISTRY", "ROTATION_MODES"]
      min_lines: 200
    - path: "lib/core/synthesizers/tension-map.cjs"
      provides: "the ONE tension-map (collapses 4 duplicated implementations)"
      exports: ["synthesizeTensionMap"]
    - path: "lib/core/synthesizers/comparison-matrix.cjs"
      provides: "Body Shape D table generator"
      exports: ["synthesizeComparisonMatrix"]
    - path: "lib/core/synthesizers/convergence-map.cjs"
      provides: "convergence synthesis pattern"
      exports: ["synthesizeConvergenceMap"]
  key_links:
    - from: "lib/core/lens-engine.cjs"
      to: "navigation.writeEdge / navigation.writeLensFinding / navigation.logMemoryEvent"
      via: "onAccept INFORMS / onReject REJECTED_BECAUSE / mandatory memory_event"
      pattern: "navigation\\.(writeEdge|writeLensFinding|logMemoryEvent|confirmNode)"
    - from: "lib/core/lens-engine.cjs synthesize"
      to: "lib/core/synthesizers/*.cjs"
      via: "typed node IDs"
      pattern: "synthesize(TensionMap|ComparisonMatrix|ConvergenceMap)"
---

<objective>
Ship lib/core/lens-engine.cjs (the rotate() engine), the 3 synthesizers, and 5 net-new lens memory_event types. This is the architectural unifier: ONE rotation for-loop replaces the loops duplicated at persona-ops.cjs:514, the hat-persistence rotation, and find-analogies; ONE tension-map.cjs replaces the 4 duplicated tension-map implementations the Cluster 2 audit found.

Purpose: Plan 03 migrates the 4 cognitive-family commands to thin clients of this engine. The engine owns the shared infrastructure (the for-loop, the 3 synthesizers, the F-shape selector dispatch, the cascade-edge + memory_event writes via navigation.cjs, the persona-aware framing hook). What stays per-client is declared in frontmatter: the lens set, the per-lens prompt, the synthesis strategy.

Output: lens-engine.cjs with the documented rotate() interface + 5-family registry + serial/parallel/single modes; 3 synthesizers each tested independently; 5 lens memory_event types added additively to EVENT_TYPES.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-lens-engine-skeleton/130-CONTEXT.md
@.planning/phases/130-lens-engine-skeleton/130-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (now shipped). Use these directly. -->

From lib/core/navigation.cjs (the chokepoint -- the engine requires ONLY this module):
  writeEdge(db, {source_id, target_id, edge_type, properties}) -- INFORMS + REJECTED_BECAUSE now allowed.
  writeLensFinding(db, {lensType, lens, topic, summary, sessionId}) -> {ok, node_id} (review_status proposed).
  writeHatState / readHatState / readAllHatStates (db, ...) -- HatState typed nodes.
  logMemoryEvent(db, eventType, payload) -- eventType MUST be in EVENT_TYPES.
  confirmNode(db, id, byUser, reason?) -- promotes a proposed node to confirmed with human byUser.
  resolveByUser(roomDir) -> non-agent byUser string (default 'navigator').

From lib/hmi/selector-dispatcher.cjs (Phase 88.2 F-shape substrate):
  dispatchShapeFSubShape(requestedShape, args) -- requestedShape is 'F.0'|'F.1'|'F.4'; returns a rendered
  selector block with an AskUserQuestion structural-marker trailer. This is the surfaceSelector dispatch.

From lib/core/user-md-ops.cjs (Phase 115 persona-aware framing):
  readUserMd(userMdPath) -> { role_blend: {founder, researcher, ...weights}, ... } | null.
  role_blend is the weighted role tuple the persona-aware framing hook consumes.

From lib/core/navigation/memory-events.cjs:
  EVENT_TYPES is an additive frozen Set; tests assert a FLOOR + named membership + delta, never exact size.
  The Phase 129-01 block (the last 5 entries: spine_read..suggestion_surfaced) is the additive idiom to mirror.

Pre-resolved decision (130-CONTEXT): synthesizers receive the typed node IDs that were written
(the lens_finding node_ids from writeLensFinding), NOT raw per-lens output arrays -- forces graph-native composition.
Pre-resolved decision: rotation modes serial / parallel / single cover the cognitive family this phase;
DEFER the source-family weighted-by-context 4th mode to Phase 131.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 5 lens memory_event types + build the 3 synthesizers</name>
  <files>lib/core/navigation/memory-events.cjs, lib/core/synthesizers/tension-map.cjs, lib/core/synthesizers/comparison-matrix.cjs, lib/core/synthesizers/convergence-map.cjs, tests/test-130-lens-engine.cjs</files>
  <read_first>
    - lib/core/navigation/memory-events.cjs lines 240-266 (the Phase 129-01 additive EVENT_TYPES block + the FLOOR-not-exact-size test convention; mirror it)
    - lib/core/persona-ops.cjs (the existing inter-hat tension shape that the duplicated tension-map implementations produced; grep persona-ops.cjs and hat-persistence.cjs for "tension" + "concern" to see the input shape the consolidated synthesizer must accept)
    - lib/core/visual-ops.cjs (the De Stijl Body Shape D table render conventions the comparison-matrix mirrors -- grep for the table/matrix render helpers; reuse-before-build per Canon Part 7)
  </read_first>
  <behavior>
    - Test: EVENT_TYPES gains EXACTLY 5 net-new lens types: lens_rotation_started, lens_finding_written, lens_synthesis_completed, lens_finding_accepted, lens_finding_rejected. Asserted by named membership + a delta-of-5, never an absolute size.
    - Test: synthesizeTensionMap(findingNodes) takes an array of typed lens-finding node objects (id + summary + lens) and returns a structured tension map (pairs of lenses with opposing findings) -- it is PURE (no db, no fs).
    - Test: synthesizeComparisonMatrix(findingNodes) returns a Body Shape D table structure (rows=lenses, columns=dimensions).
    - Test: synthesizeConvergenceMap(findingNodes) returns convergence clusters (themes appearing across 3+ lenses).
    - Test: a source-grep over each synthesizer proves zero require of room-db / node:sqlite / fs write (synthesizers are pure functions over typed node objects).
  </behavior>
  <action>Add 5 string members to the EVENT_TYPES Set in memory-events.cjs with a Phase 130-02 additive comment block mirroring the Phase 129-01 block: lens_rotation_started (one per rotate call), lens_finding_written (per lens that produces a finding), lens_synthesis_completed (one per synthesize), lens_finding_accepted (onAccept path), lens_finding_rejected (onReject path). State the FLOOR-not-exact-size test convention in the comment. Then create the 3 synthesizers under lib/core/synthesizers/ as PURE functions (no db handle, no fs, no require of room-db) that each take an array of typed lens-finding node objects (the node IDs + their parsed properties, per the 130-CONTEXT pre-resolved decision that synthesizers receive typed node IDs, not raw per-lens arrays). tension-map.cjs::synthesizeTensionMap consolidates the 4 duplicated implementations: pair lenses whose findings oppose (e.g. a yellow-hat opportunity vs a black-hat concern on the same topic) and return the tension pairs. comparison-matrix.cjs::synthesizeComparisonMatrix returns a Body Shape D table (rows = lenses, columns = the comparison dimensions) reusing the visual-ops.cjs table conventions (Canon Part 7 reuse). convergence-map.cjs::synthesizeConvergenceMap returns clusters of themes that appear across 3+ lenses. Each synthesizer is defensive (empty input -> empty result, never throws). NO em-dashes; NO fenced code blocks in source bodies that would trip the substrate guard m4-cypher false-positive (avoid the literal token MATCH adjacent to a template ${...}).</action>
  <verify>
    <automated>node -e "const {EVENT_TYPES}=require('./lib/core/navigation/memory-events.cjs'); for(const t of ['lens_rotation_started','lens_finding_written','lens_synthesis_completed','lens_finding_accepted','lens_finding_rejected']){if(!EVENT_TYPES.has(t)){console.error('missing '+t);process.exit(1)}} const tm=require('./lib/core/synthesizers/tension-map.cjs'),cm=require('./lib/core/synthesizers/comparison-matrix.cjs'),cv=require('./lib/core/synthesizers/convergence-map.cjs'); if(typeof tm.synthesizeTensionMap!=='function'||typeof cm.synthesizeComparisonMatrix!=='function'||typeof cv.synthesizeConvergenceMap!=='function'){process.exit(2)} tm.synthesizeTensionMap([]);cm.synthesizeComparisonMatrix([]);cv.synthesizeConvergenceMap([]); console.log('ok')"</automated>
  </verify>
  <done>EVENT_TYPES carries the 5 lens types; the 3 synthesizers exist as pure functions, each tested independently with a RED-then-GREEN suite; no synthesizer touches room.db or fs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build lens-engine.cjs -- rotate() + 5-family registry + serial/parallel/single modes</name>
  <files>lib/core/lens-engine.cjs, tests/test-130-lens-engine.cjs</files>
  <read_first>
    - lib/core/persona-ops.cjs lines 500-524 (analyzeAllPerspectives -- the serial for-loop over HAT_COLORS that the engine's single loop replaces)
    - lib/core/hat-persistence.cjs lines 19-28, 300-311 (HAT_COLORS + loadAllHatStates -- the cognitive lens set + state shape)
    - lib/core/navigation.cjs lines 88-134 (writeEdge / writeLensFinding / writeHatState / logMemoryEvent / confirmNode / resolveByUser -- the ONLY module the engine requires)
    - lib/hmi/selector-dispatcher.cjs lines 448-520 (dispatchShapeFSubShape -- the F.0/F.1/F.4 surfaceSelector dispatch)
    - lib/core/user-md-ops.cjs lines 218-240 (readUserMd -> role_blend, the persona-aware framing hook input)
  </read_first>
  <behavior>
    - Test: rotate({lensType:'cognitive', lensSet:'six-hats', input:{roomDir, topic}, perLensFn, synthesize:'tension-map', surfaceSelector:'F.1', persistence:'memory_event', onAccept, onReject}) runs perLensFn once per lens in serial mode and returns {ok:true, findingIds, synthesis, selector}.
    - Test: rotationMode 'serial' calls perLensFn sequentially; 'parallel' awaits all perLensFn concurrently (Promise.all); 'single' calls perLensFn for exactly one named lens (e.g. ['black-hat']).
    - Test: every rotate call emits a lens_rotation_started memory_event at start and a lens_synthesis_completed at end (mandatory emission per Canon Part 9); each finding emits lens_finding_written.
    - Test: synthesize receives the typed node IDs (findingIds) written by writeLensFinding, NOT the raw perLensFn return arrays.
    - Test: onAccept(lens, finding) routes to navigation.writeEdge with edge_type INFORMS; onReject routes to writeEdge with REJECTED_BECAUSE + an enum reason; both emit the matching lens_finding_accepted / lens_finding_rejected memory_event.
    - Test: LENS_REGISTRY has 5 keys (domain/cognitive/source/framework/trend); cognitive.client_count > 0 and lists six-hats; the other 4 have client_count 0 (reserved for v1.14.0).
    - Test: a source-grep over lens-engine.cjs proves zero require of room-db / node:sqlite (it reaches room.db ONLY through navigation.cjs); zero em-dashes.
    - Test: the persona-aware framing hook reads role_blend via readUserMd and passes it into the perLensFn ctx (consumes Phase 115 role_blend).
  </behavior>
  <action>Create lib/core/lens-engine.cjs exporting rotate(opts), LENS_REGISTRY, and ROTATION_MODES. rotate(opts) is the single architectural for-loop. opts = {lensType, lensSet, input:{roomDir, topic, focusNodeId?}, perLensFn, synthesize, surfaceSelector, persistence, onAccept, onReject, rotationMode}. The engine requires ONLY lib/core/navigation.cjs for all room.db access (zero room-db require -- it stays outside the substrate-guard allow-list and must route through the chokepoint, so the guard scanFiles must return clean on it). Steps inside rotate: (1) open the room via navigation (resolve the db handle through the navigation surface -- do NOT require room-db.cjs directly; obtain the handle the same way spine-event consumers do, by calling a navigation helper that takes roomDir); emit lens_rotation_started via navigation.logMemoryEvent. (2) Resolve the lens set: a named set ('six-hats' -> the 6 hat colors) or a dynamicArray (['black-hat']). (3) Read persona framing: call readUserMd on input.roomDir's USER.md and pass role_blend into each perLensFn ctx (the persona-aware framing hook, consuming Phase 115). (4) Run the for-loop per ROTATION_MODES: serial (sequential await), parallel (Promise.all over all lenses), single (one named lens only). DEFER the source-family weighted-by-context 4th mode to Phase 131 (do not implement it; the registry source slot stays client_count 0). (5) For each lens that returns a finding, call navigation.writeLensFinding and collect the typed node_id; emit lens_finding_written. (6) Pass the collected findingIds (typed node IDs, NOT raw arrays) to the named synthesizer (tension-map / comparison-matrix / convergence-map from lib/core/synthesizers/); emit lens_synthesis_completed. (7) Dispatch the surfaceSelector ('F.0'|'F.1'|'F.4') via lib/hmi/selector-dispatcher.cjs dispatchShapeFSubShape, returning the rendered selector. (8) Wire onAccept(lens, finding) -> navigation.writeEdge edge_type INFORMS (finding node INFORMS the target node) + a lens_finding_accepted memory_event; onReject(lens, finding) -> navigation.writeEdge edge_type REJECTED_BECAUSE with an enum reason scalar + a lens_finding_rejected memory_event (rejection-as-data per Canon Part 4). When persistence is 'memory_event+cascade-edge' AND the finding warrants promotion, call navigation.confirmNode with resolveByUser(roomDir) so a human-attributed accept promotes the proposed lens_finding to confirmed (per Canon Part 9 v1.5). Define LENS_REGISTRY as a frozen object with 5 family keys: cognitive (client_count 4, lens_sets ['six-hats','black-hat'], synthesizers ['tension-map']), and domain / source / framework / trend each {client_count: 0, reserved_for: 'v1.14.0'}. Define ROTATION_MODES = ['serial','parallel','single'] (the source-family weighted mode is a documented Phase 131 TODO comment, not a member). The engine is defensive: invalid lensType / lensSet / rotationMode returns {ok:false, reason}. NO em-dashes; NO fenced code blocks in the source; avoid the literal MATCH token next to a ${...} template (m4-cypher guard false-positive).</action>
  <verify>
    <automated>node -e "const e=require('./lib/core/lens-engine.cjs'); if(typeof e.rotate!=='function')process.exit(1); const r=e.LENS_REGISTRY; const keys=Object.keys(r); for(const k of ['domain','cognitive','source','framework','trend']){if(!keys.includes(k))process.exit(2)} if(!(r.cognitive.client_count>0))process.exit(3); for(const k of ['domain','source','framework','trend']){if(r[k].client_count!==0)process.exit(4)} for(const m of ['serial','parallel','single']){if(!e.ROTATION_MODES.includes(m))process.exit(5)} const src=require('fs').readFileSync('./lib/core/lens-engine.cjs','utf8'); if(/require\(['\"][^'\"]*(?:room-db|node:sqlite|better-sqlite3)['\"]\)/.test(src))process.exit(6); if(src.includes(String.fromCharCode(8212)))process.exit(7); console.log('ok')"</automated>
  </verify>
  <done>lens-engine.cjs exports rotate + LENS_REGISTRY (5 families, only cognitive populated) + ROTATION_MODES (serial/parallel/single); rotate emits the mandatory memory_events, routes onAccept/onReject through navigation.writeEdge, passes typed node IDs to synthesizers, consumes role_blend; the engine has zero direct room-db/sqlite require and zero em-dashes; the RED-then-GREEN engine suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Run the substrate guard against the engine + register the engine suite + zero-regression gate</name>
  <files>tests/run-all-130.sh, tests/test-130-lens-engine.cjs</files>
  <read_first>
    - scripts/check-substrate.cjs (the live guard; its scanFiles must return [] for lens-engine.cjs and the 3 synthesizers -- they are NOT allow-listed, so they must carry zero direct room.db access; note the m4-cypher false-positive on plain "MATCH"+${...} strings per 130-CONTEXT)
    - tests/run-all-130.sh (from Plan 01; append the engine suite)
  </read_first>
  <action>Register tests/test-130-lens-engine.cjs in tests/run-all-130.sh (after the substrate suite). Run scripts/check-substrate.cjs scanFiles over lib/core/lens-engine.cjs + the 3 synthesizers and assert zero violations (they reach room.db only through navigation.cjs; they are correctly NOT in the allow-list). Confirm zero regression: tests/test-130-lens-substrate.cjs (Plan 01), tests/test-129-spine-substrate.cjs, and tests/test-navigation-acceptance.cjs all still pass.</action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && node -e "const g=require('./scripts/check-substrate.cjs'); const files=['lib/core/lens-engine.cjs','lib/core/synthesizers/tension-map.cjs','lib/core/synthesizers/comparison-matrix.cjs','lib/core/synthesizers/convergence-map.cjs']; const fs=require('fs'); const v=g.scanFiles(files,(p)=>{try{return fs.readFileSync(p,'utf8')}catch(_){return ''}}); if(v.length){console.error(JSON.stringify(v));process.exit(1)} console.log('substrate-clean')" && node tests/test-130-lens-engine.cjs && node tests/test-130-lens-substrate.cjs && node tests/test-navigation-acceptance.cjs && echo ALL_GREEN</automated>
  </verify>
  <done>The substrate guard returns zero violations for the engine + 3 synthesizers; the engine suite is registered in run-all-130.sh; Plan 01 + Phase 109 suites still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| perLensFn -> engine | Per-lens callback returns prose + structured findings; the engine writes them as proposed lens_finding nodes |
| engine -> room.db | All writes route through navigation.cjs; the engine carries zero direct room.db access |
| onReject reason -> REJECTED_BECAUSE edge | Enum-only reason scalar; rejection-as-data per Canon Part 4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-02-01 | Tampering | lens-engine.cjs substrate bypass | mitigate | Engine requires ONLY navigation.cjs; substrate guard scanFiles asserted clean (Task 3); zero room-db require |
| T-130-02-02 | Repudiation | rotation with no memory_event | mitigate | Mandatory lens_rotation_started + lens_synthesis_completed per rotate; per-finding lens_finding_written; accept/reject events |
| T-130-02-03 | Information disclosure | synthesizers reading raw user content | mitigate | Synthesizers are pure over typed node IDs (the pre-resolved decision); source-grep proves zero fs/db access |
| T-130-02-04 | Elevation of privilege | engine promoting a finding without human | mitigate | Promotion to confirmed routes through navigation.confirmNode with resolveByUser (non-agent byUser) per Canon Part 9 v1.5 |
| T-130-02-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies (node built-ins + bash); no install tasks |
</threat_model>

<verification>
- rotate() runs serial / parallel / single over a lens set and returns findingIds + synthesis + selector.
- Every rotation emits the mandatory memory_events.
- LENS_REGISTRY has 5 families; only cognitive populated.
- The 3 synthesizers are pure and receive typed node IDs.
- The substrate guard returns zero violations for the engine + synthesizers.
- Phase 109 + Plan 01 suites pass (zero regression).
- Zero em-dashes across all files.
</verification>

<success_criteria>
- The single rotation for-loop + the 3 synthesizers + the F-selector dispatch + the cascade-edge/memory_event writes are built ONCE in the engine, ready for Plan 03 to point 4 thin clients at.
- The 4 duplicated tension-map implementations have ONE replacement (tension-map.cjs); Plan 03 deletes the duplicates.
</success_criteria>

<output>
Create `.planning/phases/130-lens-engine-skeleton/130-02-SUMMARY.md` when done
</output>
