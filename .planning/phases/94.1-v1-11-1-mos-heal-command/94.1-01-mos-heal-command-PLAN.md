---
phase: 94.1-v1-11-1-mos-heal-command
plan: "01"
type: execute
wave: 1
depends_on: ["94-02", "94-03", "94-04", "94-05", "94-06"]
autonomous: true
files_modified:
  - commands/heal.md
  - scripts/heal-command.cjs
  - lib/memory/heal-command.test.cjs
  - lib/memory/run-feynman-tests.cjs
  - docs/install/HEAL.md
requirements: []
canon_parts:
  - "Part 7 (Reuse Before Build -- wraps existing scripts/migrate-lazygraph.cjs + scripts/vault-section-state-generator.cjs + scripts/vault-section-minto-generator.cjs + scripts/compute-state. Zero net-new methodology; heal IS orchestration over shipped primitives.)"
  - "Part 4 (Every Choice Is Graph Data -- heal writes .mindrian/heal-log.json capturing every section touched + every transition + every result. The cascade becomes graph-traceable data.)"
  - "Part 8 (Graph Boundary -- heal operates LOCAL-only; reads STATE.md / ROOM.md / MINTO.md, runs local graph rebuild, writes local heal-log. Zero Brain queries; brain-derivation-queue is only enqueued, never drained inside heal.)"

must_haves:
  truths:
    - "node scripts/heal-command.cjs <fixture-room> --dry-run exits 0 and prints a 10-step plan without mutating the room"
    - "node scripts/heal-command.cjs <fixture-room> exits 0 on a deliberately-degraded fixture room and writes .mindrian/heal-log.json with one entry per step"
    - "After heal: every canonical section (problem-definition, market-analysis, solution-design, business-model, competitive-analysis, team-execution, legal-ip, financial-model) has a directory present (Step 2) AND every present section has a ROOM.md (Step 5) AND every present section has a STATE.md (Step 6)"
    - "After heal: .mindrian/room.db nodes table COUNT > 0 (Step 4 rebuild fired and indexed artifacts)"
    - "After heal: root STATE.md regenerated; computed timestamp > pre-heal computed timestamp (Step 9)"
    - "Backup created at .heal-backup/<TS>/ before any mutation (Step 1) -- backup file count > 0; pre-heal STATE.md and pre-heal MINTO.md files preserved verbatim"
    - "/mos:heal slash command frontmatter declares Bash + Read + Write in allowed-tools; argument-hint optional (default behavior heals current room)"
    - "Mega-section limitation (FEYNMINTO-01 budget, recipe Section 7) is RESPECTED not bypassed: when a section's MINTO.md write rejects with 'budget exceeded', heal logs status='blocked_feynminto_01' for that section + writes tier-0 fallback MINTO + continues; deferred for v1.12 budget fix"
    - "Heal NEVER throws on a per-section failure; always completes all 10 steps with per-step status logged; exit 0 on any-step-success, exit 2 on no-step-success"
    - "Zero em-dashes (U+2014) in any modified file"
    - "BSL 1.1 license header on lib/memory/heal-command.test.cjs and scripts/heal-command.cjs"
  artifacts:
    - path: "commands/heal.md"
      provides: "Slash command markdown skill. Frontmatter: name, description, argument-hint (optional), allowed-tools (Bash, Read, Write). Body delegates to scripts/heal-command.cjs and reports the heal-log via 4-zone UI (header + body + intelligence strip when severities found + action footer)."
      min_lines: 60
    - path: "scripts/heal-command.cjs"
      provides: "Pure CJS orchestrator wrapping the 10-step recipe. Exports runHeal(roomDir, opts) -> heal-log envelope. CLI entry runs heal on cwd or first positional arg. Honors --dry-run + --skip-step-N + --section-only flags."
      min_lines: 240
    - path: "lib/memory/heal-command.test.cjs"
      provides: "T1 dry-run on degraded fixture (10-step plan, zero mutation). T2 full heal on degraded fixture (post-heal conformance asserted). T3 heal-log shape (one entry per step, all 10 keys present). T4 backup created (file count > 0; pre-heal files preserved). T5 mega-section graceful degradation (FEYNMINTO-01 budget rejection logged not thrown). T6 idempotence (heal twice on same room is a no-op on second run)."
      min_lines: 280
    - path: "docs/install/HEAL.md"
      provides: "User-facing doc for /mos:heal: when to run, what it does, what it does NOT do (Brain queue drain, FEYNMINTO budget, etc. -- explicit v1.12 deferrals), what the heal-log captures."
      min_lines: 60
    - path: "lib/memory/run-feynman-tests.cjs"
      provides: "TEST_FILES registration of heal-command.test.cjs"
      min_lines: 1
  key_links:
    - from: "scripts/heal-command.cjs Step 4"
      to: "scripts/migrate-lazygraph.cjs"
      via: "child_process.spawnSync invocation with --force flag and roomDir as cwd"
      pattern: "migrate-lazygraph"
    - from: "scripts/heal-command.cjs Step 6"
      to: "scripts/vault-section-state-generator.cjs"
      via: "spawnSync invocation with roomDir as positional"
      pattern: "vault-section-state-generator"
    - from: "scripts/heal-command.cjs Step 7"
      to: "scripts/vault-section-minto-generator.cjs"
      via: "spawnSync invocation per section with --plan and --write modes; tier-0 fallback on rejection"
      pattern: "vault-section-minto-generator"
    - from: "scripts/heal-command.cjs Step 9"
      to: "scripts/compute-state"
      via: "bash spawnSync with roomDir; stdout redirected to STATE.md"
      pattern: "compute-state"
    - from: "lib/memory/heal-command.test.cjs"
      to: "lib/memory/run-feynman-tests.cjs TEST_FILES"
      via: "registration line referencing 'heal-command'"
      pattern: "heal-command"
---

<objective>
Ship the /mos:heal slash command + scripts/heal-command.cjs orchestrator + heal-command.test.cjs fence as v1.11.1 GA.

Source of truth for the recipe: ~/MindrianRooms/mindrianOS/methodology/2026-04-29-v1-11-0-room-wiring-heal-process.md (10 numbered steps with explicit shell invocations, schema gotchas, and v1.12 candidate issues). Recipe was authored as it was executed on the dog-food mindrianOS room and is the spec.

Purpose: any user who upgrades from v1.10.x to v1.11.x can self-heal their room without manual intervention. Lawrence's QA harness on the mos-qa-quantum-v1.11.0 room would have surfaced the missing-section / missing-ROOM.md / stale-MINTO drift in 30 seconds with /mos:heal.

Output: 4-zone command output that runs all 10 steps, writes .mindrian/heal-log.json, surfaces the 10-row table from the recipe's "What this heal touched" provenance section, ends with action footer (next moves: review heal-log, regenerate dashboard, address deferred FEYNMINTO blocks).

Out of scope (deferred to v1.12 with explicit triggers in deferred-items.md):
  - FEYNMINTO-01 budget scaling for mega-sections (BUG-1 from v1.11.0 audit)
  - brain-derivation-queue auto-drain hook (BUG-2 from v1.11.0 audit)
  - Section auto-creation on plugin upgrade (recipe Open issue 3)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@~/MindrianRooms/mindrianOS/methodology/2026-04-29-v1-11-0-room-wiring-heal-process.md
@CLAUDE.md
@scripts/migrate-lazygraph.cjs
@scripts/vault-section-state-generator.cjs
@scripts/vault-section-minto-generator.cjs
@scripts/compute-state
@.planning/phases/94.1-v1-11-1-mos-heal-command/94.1-CONTEXT.md

<interfaces>
Recipe-to-orchestrator mapping (one runHeal step per recipe step):

| Recipe step | runHeal step | Existing script invoked | Idempotent? |
|-------------|--------------|-------------------------|-------------|
| 1 Backup | step_01_backup | (inline cp orchestration) | yes |
| 2 Create canonical sections | step_02_scaffold_sections | (inline mkdir) | yes |
| 3 Section seed in new sections | step_03_section_seed | (inline write) | yes |
| 4 Refresh room.db | step_04_lazygraph_rebuild | scripts/migrate-lazygraph.cjs --force | yes |
| 5 Backfill ROOM.md | step_05_backfill_room_md | (inline write per section) | yes |
| 6 Generate STATE.md per section | step_06_section_state | scripts/vault-section-state-generator.cjs | yes |
| 7 Generate MINTO.md per section | step_07_section_minto | scripts/vault-section-minto-generator.cjs --plan + --write | yes |
| 8 Brain-derivation queue | step_08_queue_check | (inline read .mindrian/brain-derivation-queue.json) | n/a (read-only in v1.11.1; drain deferred to v1.12) |
| 9 Recompute root STATE.md | step_09_root_state | bash scripts/compute-state | yes |
| 10 On-stop invariant scan | step_10_invariant_scan | (read .mindrian/invariant-report.json after next /mos: command) | n/a (passive) |

heal-log.json envelope (written to <room>/.mindrian/heal-log.json after run):
```json
{
  "schema_version": "1.0",
  "started_at": "ISO-8601",
  "ended_at": "ISO-8601",
  "room_dir": "/abs/path",
  "backup_dir": ".heal-backup/<TS>",
  "steps": [
    {
      "step": "step_01_backup",
      "status": "ok" | "skipped" | "blocked_<reason>" | "error_<reason>",
      "duration_ms": 0,
      "details": { ... step-specific ... }
    },
    ...
  ],
  "summary": {
    "ok_count": 0,
    "skipped_count": 0,
    "blocked_count": 0,
    "error_count": 0,
    "exit_code": 0 | 2
  }
}
```

scripts/heal-command.cjs CLI surface:
  node scripts/heal-command.cjs                       # heal cwd as room
  node scripts/heal-command.cjs <room-dir>            # heal specific room
  node scripts/heal-command.cjs --dry-run             # plan only, zero mutation
  node scripts/heal-command.cjs --skip-step <N>       # skip step N (e.g. for re-runs)
  node scripts/heal-command.cjs --section <name>      # restrict heal to one section

commands/heal.md frontmatter:
```yaml
---
name: heal
description: Heal a room's structural drift after plugin upgrade
argument-hint: "[room-dir]"
allowed-tools:
  - Bash
  - Read
  - Write
---
```

Test fixture (lib/memory/heal-command.test.cjs T2 acceptance):
- mkdtempSync a room
- Deliberately degrade: missing ROOM.md in 3 sections; missing legal-ip directory; STATE.md present but root only; brain-derivation-queue.json with 1 stale entry
- Run runHeal(roomDir)
- Assert: 8 canonical sections directories present; every section ROOM.md present; every section STATE.md present; .mindrian/room.db nodes COUNT > 0; root STATE.md regenerated; backup directory populated
</interfaces>

<locked_decisions>
- /mos:heal v1.11.1 scope: 10 steps from recipe, idempotent, zero net-new methodology. Wraps 4 existing scripts.
- Mega-section MINTO failure: NOT a blocker. Step 7 logs status='blocked_feynminto_01' for the offending section + writes tier-0 fallback MINTO + continues. v1.12 will fix the budget; v1.11.1 just respects the limit gracefully.
- Brain-queue drain: read-only in v1.11.1. Step 8 reports queue depth + age of oldest entry; does NOT drain. v1.12 ships the drain hook.
- Backup format: .heal-backup/<TS>/<section>/<file>. <TS> is YYYYMMDD-HHMMSS UTC. Backup is best-effort (no fsync); ok if some files missing pre-heal (heal still proceeds).
- Heal-log location: <room>/.mindrian/heal-log.json. JSON envelope per <interfaces>. Idempotent on re-run (overwrites).
- Acceptance test fixture: in-memory mkdtempSync; recipe Section 'What this heal touched on the mindrianOS room (29 Apr 2026)' as the conformance reference.
- Canon Part 8 boundary: heal is LOCAL-only. Zero Brain queries. brain-derivation-queue is read but never drained inside heal (drain is a separate v1.12 hook).
- Zero em-dashes in any new file. BSL 1.1 on heal-command.cjs and heal-command.test.cjs.
- Slash command argument: optional [room-dir]. When omitted, heal current cwd. When provided, heal that path.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- failing fixture tests for heal command on degraded room</name>
  <files>lib/memory/heal-command.test.cjs, lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - ~/MindrianRooms/mindrianOS/methodology/2026-04-29-v1-11-0-room-wiring-heal-process.md (recipe)
    - lib/memory/folder-memory.test.cjs (test pattern reference)
    - lib/memory/rs-discovery-engine.test.cjs (recent fixture pattern, 94-02 style)
    - lib/memory/run-feynman-tests.cjs (registration target)
  </read_first>
  <behavior>
    T1 dry-run on degraded fixture: runHeal(roomDir, {dryRun: true}) returns plan envelope with 10 step entries; .mindrian/ directory NOT created; no files mutated.

    T2 full heal on degraded fixture: runHeal(roomDir) on a deliberately-degraded room. Assert post-heal: 8 canonical section directories present; every section has ROOM.md + STATE.md; .mindrian/room.db nodes COUNT > 0; root STATE.md regenerated; backup directory populated.

    T3 heal-log envelope shape: heal-log.json present at <room>/.mindrian/heal-log.json; valid JSON; schema_version + started_at + ended_at + room_dir + backup_dir + steps[10] + summary keys; every step has step+status+duration_ms+details keys.

    T4 backup created: .heal-backup/<TS>/ directory present; <TS> matches YYYYMMDD-HHMMSS pattern; pre-heal STATE.md preserved verbatim in backup.

    T5 mega-section graceful: a section with 50+ artifacts; runHeal completes step 7 for that section with status='blocked_feynminto_01'; no exception propagates; tier-0 fallback MINTO written (or skip if not feasible).

    T6 idempotence: runHeal twice on the same room. Second run completes; nothing destructive. Second-run heal-log shows step statuses 'ok' or 'skipped' (no 'error_').
  </behavior>
  <action>
    Step 1: Create lib/memory/heal-command.test.cjs with BSL 1.1 header.
    Step 2: Implement helper buildDegradedFixtureRoom(tmpDir) that creates the deliberate-degradation fixture (missing ROOM.md in 3 sections; missing legal-ip directory; root STATE.md present but stale; brain-derivation-queue.json with 1 stale entry).
    Step 3: Implement T1-T6 against require(scripts/heal-command.cjs).runHeal.
    Step 4: Append registration to lib/memory/run-feynman-tests.cjs TEST_FILES.
    Step 5: Run test. T1-T6 MUST FAIL (heal-command.cjs does not exist yet).

    Commit RED: `test(94.1-01): add failing tests for /mos:heal command (RED)`
  </action>
  <verify>
    <automated>node lib/memory/heal-command.test.cjs 2>&1 | tail -10 | grep -qE "FAIL|fail|MODULE_NOT_FOUND"</automated>
    <automated>grep -c "heal-command" lib/memory/run-feynman-tests.cjs</automated>
    <automated>grep -c "BSL" lib/memory/heal-command.test.cjs</automated>
    <automated>grep -cP "[\x{2014}]" lib/memory/heal-command.test.cjs</automated>
  </verify>
  <done>6 failing tests committed. Feynman runner registered. BSL 1.1 + zero em-dashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN -- scripts/heal-command.cjs orchestrator (10 steps + heal-log envelope)</name>
  <files>scripts/heal-command.cjs</files>
  <read_first>
    - scripts/migrate-lazygraph.cjs (Step 4 invocation contract)
    - scripts/vault-section-state-generator.cjs (Step 6)
    - scripts/vault-section-minto-generator.cjs (Step 7 + schema gotchas + FEYNMINTO-01 budget)
    - scripts/compute-state (Step 9)
    - QA recipe sections 'The recipe (10 steps)' and 'Schema gotchas' verbatim
  </read_first>
  <action>
    Step 1: Create scripts/heal-command.cjs with BSL 1.1 header.
    Step 2: Export runHeal(roomDir, opts) async function. opts: {dryRun, skipStep, section, log}.
    Step 3: Implement step_01_backup through step_10_invariant_scan as 10 functions, each returning {status, duration_ms, details}.
    Step 4: Use child_process.spawnSync for the 4 script invocations (Steps 4, 6, 7, 9). Capture stdout/stderr; map exit codes to status.
    Step 5: Step 7 catches FEYNMINTO-01 budget rejection (regex on 'budget exceeded' in stderr) and writes tier-0 fallback MINTO + status='blocked_feynminto_01'.
    Step 6: Aggregate all 10 step results into heal-log envelope; write to <room>/.mindrian/heal-log.json.
    Step 7: CLI entry parses --dry-run / --skip-step / --section / positional [room-dir].
    Step 8: NEVER throw on per-step failure; always complete all 10 with status logged. Exit code 0 on any-step-success; exit 2 on no-step-success.
    Step 9: Run T1-T6. All MUST pass.

    Commit GREEN: `feat(94.1-01): scripts/heal-command.cjs orchestrator (10-step room heal)`
  </action>
  <verify>
    <automated>test -f scripts/heal-command.cjs</automated>
    <automated>grep -c "step_01_backup\|step_02_scaffold\|step_10_invariant" scripts/heal-command.cjs</automated>
    <automated>grep -c "heal-log.json" scripts/heal-command.cjs</automated>
    <automated>grep -c "blocked_feynminto_01" scripts/heal-command.cjs</automated>
    <automated>node lib/memory/heal-command.test.cjs 2>&1 | tail -5 | grep -qE "6/6|PASS|passed"</automated>
    <automated>grep -cP "[\x{2014}]" scripts/heal-command.cjs</automated>
  </verify>
  <done>6/6 tests pass. heal-log envelope validates. FEYNMINTO-01 budget gracefully degrades. Zero em-dashes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: GREEN -- commands/heal.md slash command (4-zone UI)</name>
  <files>commands/heal.md</files>
  <read_first>
    - skills/ui-system/SKILL.md (4-zone anatomy + symbol vocabulary)
    - commands/help.md (recent slash command reference for frontmatter shape)
    - commands/research.md (recent body-prose reference)
  </read_first>
  <action>
    Step 1: Create commands/heal.md with frontmatter (name, description, argument-hint, allowed-tools: [Bash, Read, Write]).
    Step 2: Body invokes scripts/heal-command.cjs with the optional [room-dir] argument; falls back to cwd.
    Step 3: 4-zone output: header (room name + heal command + venture stage from STATE.md), body (10-step result table mirroring recipe's 'What this heal touched' format), intelligence strip (conditional on blocked_feynminto_01 or queue depth > 0), action footer (3 next-command suggestions: /mos:status, /mos:dashboard, /mos:scout).
    Step 4: Body prose explicitly names what /mos:heal does NOT do (deferred items: FEYNMINTO-01 budget; queue drain; auto-section-scaffold) and points to v1.12 candidates.
  </action>
  <verify>
    <automated>test -f commands/heal.md</automated>
    <automated>grep -c "Bash" commands/heal.md</automated>
    <automated>grep -c "heal-command.cjs" commands/heal.md</automated>
    <automated>grep -c "Action Footer\|/mos:status\|Next:" commands/heal.md</automated>
    <automated>grep -cP "[\x{2014}]" commands/heal.md</automated>
  </verify>
  <done>commands/heal.md exists with 4-zone UI; allowed-tools complete; deferred-items prose present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: docs/install/HEAL.md user-facing doc</name>
  <files>docs/install/HEAL.md</files>
  <read_first>
    - docs/install/BRAIN-SETUP.md (recent install doc reference for shape + tone)
    - QA recipe sections 'When to run' and 'Open issues for the plugin'
  </read_first>
  <action>
    Step 1: Create docs/install/HEAL.md.
    Step 2: Sections: 'When to run', 'What it does', 'What it does NOT do (v1.11.1)', 'Output (heal-log.json)', 'Mega-section limitation (FEYNMINTO-01 budget)', 'v1.12 candidates'.
    Step 3: Cite the recipe artifact and the dog-fooding provenance.
  </action>
  <verify>
    <automated>test -f docs/install/HEAL.md</automated>
    <automated>grep -c "FEYNMINTO-01\|brain-derivation-queue\|v1.12" docs/install/HEAL.md</automated>
    <automated>grep -cP "[\x{2014}]" docs/install/HEAL.md</automated>
  </verify>
  <done>docs/install/HEAL.md exists; deferred items explicit; v1.12 candidates named.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: SUMMARY closure + v1.11.1 GA staging</name>
  <files>.planning/phases/94.1-v1-11-1-mos-heal-command/94.1-01-mos-heal-command-SUMMARY.md</files>
  <read_first>
    - .planning/phases/94-v1-11-2-tester-driven-fixer/94-06-room-classifier-strict-mode-SUMMARY.md (most recent multi-task SUMMARY in phase 94)
    - This plan's must_haves block
  </read_first>
  <action>
    Step 1: Run end-to-end smoke. From a tmp room (deliberately degraded): node scripts/heal-command.cjs <tmp-room>. Confirm exit 0 + heal-log.json written + 10 step entries.
    Step 2: Run full Feynman suite. Confirm 6/6 fixture tests pass and overall suite stays green at baseline+1.
    Step 3: Write 94.1-01-SUMMARY.md mirroring 94-06 frontmatter + body structure. Include:
      - Recipe provenance (cite the dog-fooding artifact path)
      - 10-step orchestrator evidence (heal-log shape; per-step status table)
      - Test count (6/6) + Feynman baseline delta
      - End-to-end smoke evidence (degraded fixture room post-heal conformance)
      - Canon Part 7 (compose 4 existing scripts) + Part 4 (heal-log is graph data) + Part 8 (LOCAL-only) traceability
      - Explicit deferred items list (FEYNMINTO-01 budget; brain-queue drain; auto-section-scaffold) with v1.12 trigger conditions
    Step 4: Commit: `docs(94.1-01): SUMMARY -- /mos:heal command + recipe orchestration`
  </action>
  <verify>
    <automated>test -f .planning/phases/94.1-v1-11-1-mos-heal-command/94.1-01-mos-heal-command-SUMMARY.md</automated>
    <automated>grep -c "heal-log\|blocked_feynminto_01\|deferred" .planning/phases/94.1-v1-11-1-mos-heal-command/94.1-01-mos-heal-command-SUMMARY.md</automated>
    <automated>grep -cP "[\x{2014}]" .planning/phases/94.1-v1-11-1-mos-heal-command/94.1-01-mos-heal-command-SUMMARY.md</automated>
  </verify>
  <done>SUMMARY filed. End-to-end smoke green. v1.11.1 GA stage ready (CHANGELOG + plugin.json + package.json bumps + tag promotion are out-of-scope for this plan; ship via /gsd:ship in a follow-up).</done>
</task>

</tasks>

<verification>
- All 5 tasks committed with conventional commits
- 6/6 fixture tests green (T1-T6)
- node lib/memory/run-feynman-tests.cjs full suite green (baseline +1)
- node scripts/heal-command.cjs <degraded-tmp-room> exits 0; heal-log.json valid envelope
- commands/heal.md + docs/install/HEAL.md exist with required sections
- Zero em-dashes in any modified file
- BSL 1.1 on scripts/heal-command.cjs + lib/memory/heal-command.test.cjs
- SUMMARY documents the deferred-items list with v1.12 triggers
</verification>

<success_criteria>
- /mos:heal slash command shippable as v1.11.1 GA addition
- Lawrence's QA harness on a fresh-install reproducer would now have a one-command heal path
- Recipe provenance preserved (dog-fooded on mindrianOS room; cited in SUMMARY)
- Out-of-scope items (FEYNMINTO-01 budget; brain-queue drain; auto-section-scaffold) explicitly deferred to v1.12 in deferred-items.md
- Canon Part 7 (reuse before build) honored: zero net-new methodology; orchestrator over 4 shipped scripts
- Canon Part 4 (every choice is graph data) honored: heal-log.json captures the cascade
- Canon Part 8 (graph boundary) honored: heal is LOCAL-only; zero Brain queries
</success_criteria>

<output>
After completion, create `.planning/phases/94.1-v1-11-1-mos-heal-command/94.1-01-mos-heal-command-SUMMARY.md`. Must include recipe provenance, 10-step orchestrator evidence, end-to-end smoke evidence (degraded fixture conformance), Canon Part 7+4+8 traceability, deferred-items list with v1.12 triggers.
</output>
