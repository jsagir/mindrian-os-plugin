---
phase: 07-team-room-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/create-speaker-profile
  - references/meeting/speaker-profile-template.md
  - references/meeting/artifact-template.md
  - commands/new-project.md
  - commands/file-meeting.md
autonomous: true
requirements: [TEAM-01, TEAM-02, TEAM-03, TEAM-04]

must_haves:
  truths:
    - "create-speaker-profile writes roles (list), primary_role, status, last_active fields in PROFILE.md"
    - "create-speaker-profile preserves backward-compat role (singular) field"
    - "new-project creates room/team/ directory with no subfolders"
    - "file-meeting uses nested attribution: block in artifact frontmatter instead of flat speaker fields"
    - "file-meeting does NOT create speaker reference copies in subfolders"
  artifacts:
    - path: "scripts/create-speaker-profile"
      provides: "Extended PROFILE.md with roles list, status lifecycle, last_active"
      contains: "roles:"
    - path: "references/meeting/speaker-profile-template.md"
      provides: "Updated template documenting new schema"
      contains: "primary_role"
    - path: "references/meeting/artifact-template.md"
      provides: "Nested attribution block template"
      contains: "attribution:"
    - path: "commands/new-project.md"
      provides: "team/ directory creation in Step 4"
      contains: "room/team"
    - path: "commands/file-meeting.md"
      provides: "Attribution block filing, no speaker subfolder copies"
      contains: "attribution:"
  key_links:
    - from: "scripts/create-speaker-profile"
      to: "references/meeting/speaker-profile-template.md"
      via: "PROFILE.md output matches template schema"
      pattern: "roles:.*primary_role:.*status:"
    - from: "commands/file-meeting.md"
      to: "references/meeting/artifact-template.md"
      via: "Step 4 uses attribution block from template"
      pattern: "attribution:"
---

<objective>
Update the profile schema, attribution format, and room creation to support Phase 7's people-aware Data Room.

Purpose: This plan establishes the foundational contracts (PROFILE.md schema, attribution frontmatter block, team/ directory) that Plans 02 and 03 depend on. Without these, meeting archives cannot cross-link to speakers, and compute-team cannot scan profiles.

Output: Updated create-speaker-profile script, updated reference templates, updated new-project and file-meeting commands.
</objective>

<execution_context>
@/home/jsagi/.claude/get-shit-done/workflows/execute-plan.md
@/home/jsagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-team-room-structure/07-RESEARCH.md

@scripts/create-speaker-profile
@references/meeting/speaker-profile-template.md
@references/meeting/artifact-template.md
@commands/new-project.md
@commands/file-meeting.md
</context>

<interfaces>
<!-- Key contracts the executor needs from existing code -->

From scripts/create-speaker-profile (current):
- Args: <room_dir> <speaker-slug> <speaker-role> <display-name>
- Writes PROFILE.md with: name, role (singular string), affiliation, first_meeting, meetings_attended, created_by, research_status, last_updated
- Creates: insights/, advice/, connections/, concerns/ subdirectories
- Role-to-plural mapping: mentor->mentors, researcher->researchers, etc.

From references/meeting/artifact-template.md (current flat fields):
```yaml
speaker: {name}
speaker_role: {role}
meeting_date: {YYYY-MM-DD}
meeting_name: {meeting identifier}
```

From commands/file-meeting.md Step 4 (current):
- "Create Speaker Reference Copy" substep creates copies in speaker subfolders
- Uses flat speaker/speaker_role fields

From commands/new-project.md Step 4 (current):
- Creates 8 sections: problem-definition/, market-analysis/, etc.
- Does NOT create team/ directory
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend create-speaker-profile and speaker-profile-template for Phase 7 schema</name>
  <files>scripts/create-speaker-profile, references/meeting/speaker-profile-template.md</files>
  <action>
Update `scripts/create-speaker-profile` bash script:

1. PROFILE.md frontmatter gains these fields (in this order after existing fields):
   - `roles: [{speaker-role}]` -- YAML list (new, Phase 7)
   - `primary_role: {speaker-role}` -- string (new, Phase 7)
   - `role: {speaker-role}` -- KEEP this for backward compat with Phase 6
   - `status: active` -- string, default active (new, Phase 7)
   - `last_active: {TODAY}` -- date (new, Phase 7)
   - Keep all existing fields: name, affiliation, first_meeting, meetings_attended, created_by, research_status, last_updated

2. PROFILE.md body adds new sections after Expertise:
   ```
   ## Contributions

   [Computed by compute-team -- do not edit manually]
   ```
   Replace the existing "## Key Contributions" section with "## Contributions" placeholder.

3. Keep creating insights/, advice/, connections/, concerns/ subdirectories (Phase 6 legacy compat -- existing files stay, but file-meeting will stop writing to them in Task 3).

Update `references/meeting/speaker-profile-template.md`:
- Update the PROFILE.md Template section to show the new fields (roles list, primary_role, status, last_active)
- Update Field Definitions table to include new fields
- Add a "## Computed Contributions Section" documenting that compute-team rebuilds the Contributions section
- Update "Profile Updates Across Meetings" section: remove the instruction to file to subfolders (step 4), instead note that contributions are computed by compute-team
- Keep Role Changes section as-is (already says "keep profile in original directory")
- Add note about backward compatibility: "Phase 6 profiles have `role:` (singular). Phase 7 adds `roles:` (list) and `primary_role:`. compute-team reads `roles:` first, falls back to `role:` as single-element list."
  </action>
  <verify>
    <automated>bash scripts/create-speaker-profile --help && echo "---" && bash -c 'TMPDIR=$(mktemp -d); mkdir -p "$TMPDIR/room"; bash scripts/create-speaker-profile "$TMPDIR/room" test-user mentor "Test User"; grep "roles:" "$TMPDIR/room/team/mentors/test-user/PROFILE.md" && grep "primary_role:" "$TMPDIR/room/team/mentors/test-user/PROFILE.md" && grep "status:" "$TMPDIR/room/team/mentors/test-user/PROFILE.md" && grep "last_active:" "$TMPDIR/room/team/mentors/test-user/PROFILE.md" && grep "role: mentor" "$TMPDIR/room/team/mentors/test-user/PROFILE.md" && grep "Contributions" "$TMPDIR/room/team/mentors/test-user/PROFILE.md" && echo "ALL CHECKS PASSED"; rm -rf "$TMPDIR"'</automated>
  </verify>
  <done>create-speaker-profile writes PROFILE.md with roles (list), primary_role, status, last_active, backward-compat role (singular), and Contributions placeholder section. speaker-profile-template.md documents the new schema.</done>
</task>

<task type="auto">
  <name>Task 2: Update artifact-template, new-project, and file-meeting for attribution block + team/ creation</name>
  <files>references/meeting/artifact-template.md, commands/new-project.md, commands/file-meeting.md</files>
  <action>
**artifact-template.md changes:**

Replace the flat speaker fields in the YAML Frontmatter Template with a nested `attribution:` block:

```yaml
attribution:
  speaker: {name}
  role: {role from 12-type taxonomy}
  profile_path: team/{role-plural}/{speaker-slug}
  meeting_date: {YYYY-MM-DD}
  meeting_id: {YYYY-MM-DD-meeting-slug}
```

Keep all other fields unchanged (methodology, created, source, segment_type, confidence, meeting_date, meeting_name, room_section, assumptions, perspective, cascade_sections, secondary_types, rejection).

Note: `meeting_date` and `meeting_name` remain at top level for backward compat AND inside attribution for provenance chain. `speaker` and `speaker_role` at top level are REPLACED by the attribution block.

Update the Field Definitions section:
- Remove `speaker` and `speaker_role` from Core Fields table
- Add an "Attribution Block" subsection documenting the nested fields
- Add a "Backward Compatibility" note: "compute-team and other scanners must handle BOTH flat `speaker:` (Phase 6 legacy) and nested `attribution.speaker:` (Phase 7+). Grep for `^speaker:` OR `^  speaker:` (indented under attribution:)."

Update ALL examples (decision, action-item, insight, advice, question, noise) to use the new `attribution:` block format instead of flat `speaker:` and `speaker_role:`.

**new-project.md changes:**

In Step 4, after the 8 section directories are created, add team/ directory creation:
```
room/
  ...existing 8 sections...
  team/
```

Add a note: "team/ is created empty. No subfolders (members/, mentors/, advisors/) are pre-created. The structure grows organically as speakers are identified through meetings or user input."

Do NOT add team/ to the Section Definitions table -- it is not a topic section.

**file-meeting.md changes:**

1. In Step 4 "Section Mapping + Confirm-Then-File", update the "Create Filed Artifacts" substep to use the new `attribution:` block format:
   ```yaml
   attribution:
     speaker: {name}
     role: {role}
     profile_path: team/{role-plural}/{speaker-slug}
     meeting_date: {meeting date}
     meeting_id: {YYYY-MM-DD-meeting-slug}
   ```
   Keep all other frontmatter fields (methodology, created, source, segment_type, confidence, meeting_name, room_section, assumptions, perspective, cascade_sections).

2. REMOVE the entire "Create Speaker Reference Copy" substep from Step 4. Delete the section that creates copies in speaker subfolders (insights/, advice/, connections/, concerns/). Replace with a brief note: "Speaker contributions are tracked via computed backlinks in PROFILE.md (run by compute-team), not by filing copies to speaker subfolders."

3. In Step 5 "Create Meeting Summary + Archive", update the "Infer Meeting Metadata" subsection to include meeting name inference and confirmation:
   - Larry proposes a meeting name from the dominant topic/purpose
   - User confirms or changes the name
   - This meeting_id (YYYY-MM-DD-{confirmed-slug}) is used in all attribution blocks and archive paths

4. Update the tracking line at the end of Step 4 to remove "reference copies created" tracking.
  </action>
  <verify>
    <automated>grep -c "attribution:" references/meeting/artifact-template.md && grep -c "room/team" commands/new-project.md && grep -c "attribution:" commands/file-meeting.md && echo "---" && ! grep -q "Create Speaker Reference Copy" commands/file-meeting.md && echo "SPEAKER_COPY_REMOVED" || echo "SPEAKER_COPY_STILL_EXISTS"</automated>
  </verify>
  <done>artifact-template.md uses nested attribution block. new-project.md creates team/ directory (empty, no subfolders). file-meeting.md uses attribution block and does NOT create speaker subfolder copies.</done>
</task>

</tasks>

<verification>
- `bash scripts/create-speaker-profile --help` runs without error
- Create a test profile: PROFILE.md contains roles, primary_role, status, last_active, role (backward compat)
- `grep "attribution:" references/meeting/artifact-template.md` finds the nested block
- `grep "room/team" commands/new-project.md` confirms team/ directory creation
- `grep "attribution:" commands/file-meeting.md` confirms attribution block usage
- `grep -c "Create Speaker Reference Copy" commands/file-meeting.md` returns 0 (removed)
</verification>

<success_criteria>
- create-speaker-profile writes extended PROFILE.md with all Phase 7 fields
- speaker-profile-template.md documents the new schema including backward compat notes
- artifact-template.md replaces flat speaker fields with nested attribution block
- new-project creates team/ alongside the 8 topic sections (no subfolders)
- file-meeting uses attribution block and stops creating speaker subfolder copies
</success_criteria>

<output>
After completion, create `.planning/phases/07-team-room-structure/07-01-SUMMARY.md`
</output>
