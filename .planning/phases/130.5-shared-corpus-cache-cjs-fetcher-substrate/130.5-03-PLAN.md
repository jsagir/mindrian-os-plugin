---
phase: 130.5-shared-corpus-cache-cjs-fetcher-substrate
plan: 03
type: execute
wave: 2
depends_on: ["130.5-01", "130.5-02"]
files_modified:
  - scripts/rs-discovery-engine.cjs
  - tests/fixtures/phase-130.5/sample-room/seed.json
  - tests/test-130.5-corpus-migration.cjs
  - tests/run-all-130.5.sh
  - lib/memory/run-feynman-tests.cjs
autonomous: true
requirements: [SUBSTRATE-CORPUS-05]
must_haves:
  truths:
    - "rs-discovery-engine fetches corpus through fetchCorpus + the shared research-cache, not through its own private fetcher path"
    - "A fixture-room discovery run is byte-identical pre/post migration (no behavior change)"
    - "A repeat run of the same fixture topic hits the shared cache (the second run issues fewer/zero external fetches than the first)"
    - "The migrated engine still passes the Canon Part 8 input audit and never reaches the Brain directly (chain-feeder remains the only Brain door)"
    - "The phase aggregator (run-all-130.5.sh) is green and registered in the Feynman runner"
  artifacts:
    - path: "scripts/rs-discovery-engine.cjs"
      provides: "Corpus fetching repointed onto fetchCorpus + research-cache"
      contains: "research-corpus"
    - path: "tests/test-130.5-corpus-migration.cjs"
      provides: "Byte-identical fixture regression + cache-hit-on-repeat assertions"
      contains: "byte-identical"
    - path: "tests/run-all-130.5.sh"
      provides: "Phase 130.5 scoped aggregator (mirrors run-all-130.sh)"
      contains: "CJS_SUITES"
  key_links:
    - from: "scripts/rs-discovery-engine.cjs"
      to: "lib/core/research-corpus.cjs"
      via: "fetchCorpus replaces the per-source fetcher invocation for the academic source"
      pattern: "research-corpus"
    - from: "scripts/rs-discovery-engine.cjs"
      to: "lib/core/research-cache.cjs"
      via: "getCached / putCached wrap the fetch so a repeat (source, query) is served from cache"
      pattern: "research-cache"
---

<objective>
Migrate `scripts/rs-discovery-engine.cjs` (the rs-discovery-engine) onto the Plan 01 `fetchCorpus` + Plan 02 `research-cache`, closing the duplicate-fetcher / duplicate-API-quota drift, with a fixture-room regression proving byte-identical results pre/post migration (no behavior change). Ship the phase aggregator + Feynman registration.

Purpose: rs-discovery-engine today calls `fetcherAcademic.fetchAcademic(flat_queries, ...)` directly (its own fetch path) and has NO shared cache. After this plan, its corpus fetch routes through the unified `fetchCorpus` + the shared cache, so a paper fetched by `/mos:research` is reused, not re-fetched. The migration MUST be behavior-preserving: the existing rs-discovery-engine fixture suite must still pass and a recorded fixture run must be byte-identical.

Output: migrated `scripts/rs-discovery-engine.cjs`, `tests/fixtures/phase-130.5/sample-room/seed.json`, `tests/test-130.5-corpus-migration.cjs`, `tests/run-all-130.5.sh`, additive Feynman-runner registration.

SCOPE GUARD: This plan migrates the CJS rs-discovery-engine ONLY. The Python `rs-engine.py` -> `rs_corpus.py` deletion is Phase 134's job (it reuses this module and deletes the .py). Do NOT delete rs_corpus.py / rs-engine.py here. Keep the migration to repointing the CJS academic fetch onto fetchCorpus + wrapping it with the shared cache, byte-compatibly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130.5-shared-corpus-cache-cjs-fetcher-substrate/130.5-CONTEXT.md
@docs/MINDRIAN-CANON.md
@.planning/phases/130.5-shared-corpus-cache-cjs-fetcher-substrate/130.5-01-SUMMARY.md
@.planning/phases/130.5-shared-corpus-cache-cjs-fetcher-substrate/130.5-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase + Plans 01/02; do not re-explore. -->

From lib/core/research-corpus.cjs (Plan 01):
fetchCorpus is async, signature fetchCorpus({ source, query, limit }); source must be in SOURCES; it throws ExternalEgressViolation on a forbidden query (pre-egress); it returns a normalized results array. SOURCES is the frozen id list including openalex / arxiv / pubmed / tavily / brain-cypher / sci-bot.

From lib/core/research-cache.cjs (Plan 02):
getCached(roomDir, source, query, opts) returns the cached results array within TTL, else null. putCached(roomDir, source, query, results, opts) writes atomically and returns the path.

From scripts/rs-discovery-engine.cjs (the CURRENT call site to repoint, around line 323-339):
flat_queries = flattenQueryMatrix(query_matrix); then academic = await m.fetcherAcademic.fetchAcademic(flat_queries, opts.fetcherOpts || {}); academic.papers is consumed by aggregateDocuments + fetcherExperts.mapExperts. The module uses an `m.<dep>` indirection object (production module references) so deps are mockable without monkey-patching require's cache -- preserve that pattern; add m.fetchCorpus + m.researchCache. Brain is reached ONLY via chain-feeder.lookupUpstream (scenario T6 asserts grep 'fetch(' === 0 in this file). Keep it so.

From lib/memory/test-rs-discovery-engine.cjs (the existing fixture suite that must STILL pass):
9 scenarios; T6 asserts grep 'fetch(' === 0 in rs-discovery-engine.cjs; T5/T8 assert the Canon Part 8 input audit + zero FORBIDDEN_PATTERNS in the bundle. The migration must not break any of these.
</interfaces>

<read_first>
- scripts/rs-discovery-engine.cjs (THE file to migrate -- the `m.fetcherAcademic.fetchAcademic` call site near line 325; preserve the `m` indirection + the chain-feeder-only Brain path)
- lib/core/research-corpus.cjs (Plan 01 -- fetchCorpus + SOURCES)
- lib/core/research-cache.cjs (Plan 02 -- getCached / putCached)
- lib/memory/test-rs-discovery-engine.cjs (the existing 9-scenario fixture suite that must remain green -- esp. T5/T6/T8 Canon Part 8 invariants)
- tests/run-all-130.sh (the phase aggregator shape to mirror for run-all-130.5.sh)
- lib/memory/run-feynman-tests.cjs (the additive phase-block registration idiom -- see the Phase 130 block near line 1659-1680)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint rs-discovery-engine corpus fetch onto fetchCorpus + shared cache</name>
  <files>scripts/rs-discovery-engine.cjs</files>
  <read_first>
    - scripts/rs-discovery-engine.cjs (the `m.fetcherAcademic.fetchAcademic(flat_queries, ...)` call site + the `m` indirection object + flattenQueryMatrix + aggregateDocuments)
    - lib/core/research-corpus.cjs (fetchCorpus signature)
    - lib/core/research-cache.cjs (getCached / putCached)
  </read_first>
  <action>
    Repoint the academic corpus fetch (currently `m.fetcherAcademic.fetchAcademic(flat_queries, opts.fetcherOpts || {})` near line 325) onto the shared substrate, behavior-preserving. Add fetchCorpus (from lib/core/research-corpus.cjs) and the research-cache module (from lib/core/research-cache.cjs) to the `m` production-module indirection object so they stay mockable. Introduce a small async helper fetchAcademicViaCorpus(flatQueries, roomDir, opts) that, for each query in flat_queries: (1) computes a cache lookup via m.researchCache.getCached(roomDir, 'openalex', query) (the default academic source) -- on hit, reuse; (2) on miss, calls await m.fetchCorpus({ source: 'openalex', query, limit }) then m.researchCache.putCached(roomDir, 'openalex', query, results). Aggregate results into the SAME { papers, telemetry } envelope shape aggregateDocuments and fetcherExperts.mapExperts already consume, then dedupe identically (reuse the academic dedupe so ordering/keys match). The resulting `academic` object must carry academic.papers in the same shape and order as before so downstream phases are byte-unchanged. CRITICAL invariants to preserve: (a) NO direct fetch( in rs-discovery-engine.cjs (the fetch lives inside fetchCorpus / rs-fetcher-academic -- keep T6 green); (b) Brain reached ONLY via chain-feeder.lookupUpstream (no new Brain path); (c) the Canon Part 8 input audit (auditQueryObject on opts) stays first. If roomDir is unavailable (some call paths pass none), the cache wrap degrades to a direct fetchCorpus call (no cache, no throw) so behavior is unchanged for cache-less callers. No em-dashes.
  </action>
  <verify>
    <automated>node lib/memory/test-rs-discovery-engine.cjs && node -e "const fs=require('fs');const s=fs.readFileSync('scripts/rs-discovery-engine.cjs','utf8');const body=s.split('\n').filter(l=>!l.trim().startsWith('//')&&!l.trim().startsWith('*')).join('\n');if(/[^.\w]fetch\s*\(/.test(body))throw new Error('direct fetch( found in rs-discovery-engine -- T6 invariant broken');if(!/research-corpus/.test(s)||!/research-cache/.test(s))throw new Error('migration not wired');console.log('OK corpus+cache wired, no direct fetch');"</automated>
  </verify>
  <acceptance_criteria>
    - The existing `node lib/memory/test-rs-discovery-engine.cjs` 9-scenario suite stays GREEN (T5/T6/T8 Canon Part 8 invariants preserved).
    - `scripts/rs-discovery-engine.cjs` requires research-corpus.cjs + research-cache.cjs; the old corpus fetch no longer issues the academic fetch directly (it flows through fetchCorpus).
    - grep proves zero direct `fetch(` in rs-discovery-engine.cjs (T6 unchanged).
    - rs_corpus.py / rs-engine.py are NOT deleted (that is Phase 134).
  </acceptance_criteria>
  <done>rs-discovery-engine's academic corpus fetch flows through the unified fetchCorpus + the shared cache, with every prior Canon Part 8 invariant intact.</done>
</task>

<task type="auto">
  <name>Task 2: Byte-identical fixture regression + cache-hit-on-repeat + aggregator + Feynman registration</name>
  <files>tests/fixtures/phase-130.5/sample-room/seed.json, tests/test-130.5-corpus-migration.cjs, tests/run-all-130.5.sh, lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - lib/memory/test-rs-discovery-engine.cjs (the mock-module + fixture idiom: the `m` indirection lets tests inject deterministic fetchers without network)
    - tests/run-all-130.sh (the aggregator shape to mirror)
    - lib/memory/run-feynman-tests.cjs (the additive phase-block registration near the Phase 130 block)
  </read_first>
  <action>
    Create the regression that proves no behavior change. tests/fixtures/phase-130.5/sample-room/seed.json is a deterministic fixed corpus payload (a small frozen set of papers per query) that a stub fetchCorpus returns -- so the fixture run is reproducible with zero network. tests/test-130.5-corpus-migration.cjs (node:assert/strict, mirrors the test-rs-discovery-engine.cjs mock-module pattern, no network) carries three assertions:
    - Assertion A (byte-identical): run the discovery pipeline TWICE against the fixture -- once simulating the OLD path (the academic fetcher returning the fixture payload directly) and once via the migrated fetchCorpus path returning the SAME fixture payload -- and assert JSON.stringify(oldBundle.fetched_results.academic.papers) === JSON.stringify(newBundle.fetched_results.academic.papers) AND the full downstream scored / classified / theses slices stringify-equal. Normalize the non-deterministic fetched_at to a fixed stamp in the fixture so the byte-compare is stable.
    - Assertion B (cache hit on repeat): point both runs at a tmp roomDir; instrument the stub fetchCorpus with a call counter; assert the SECOND run over the same query set issues fewer external fetchCorpus calls than the first (the shared cache served the repeat) -- i.e. count_run2 < count_run1, ideally 0.
    - Assertion C (Canon Part 8 preserved): a planted forbidden query in the fixture flat_queries makes the run reject pre-egress (ExternalEgressViolation) with zero fetchCorpus network calls.
    Create tests/run-all-130.5.sh mirroring run-all-130.sh: SHELL_SUITES empty, CJS_SUITES = (test-130.5-corpus-migration.cjs). Make it executable. Add an additive Phase 130.5 block to lib/memory/run-feynman-tests.cjs registering tests/test-130.5-corpus-migration.cjs, every prior block byte-unchanged. No em-dashes anywhere.
  </action>
  <verify>
    <automated>chmod +x tests/run-all-130.5.sh && bash tests/run-all-130.5.sh && node -e "const fs=require('fs');const r=fs.readFileSync('lib/memory/run-feynman-tests.cjs','utf8');if(!r.includes('test-130.5-corpus-migration.cjs'))throw new Error('130.5 not registered in Feynman runner');console.log('OK registered');"</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-130.5.sh` is green; the migration suite passes all three assertions (byte-identical + cache-hit-on-repeat + Canon Part 8 preserved).
    - The byte-identical assertion stringify-compares the full downstream bundle slices, not just the paper list.
    - The cache-hit assertion proves count_run2 < count_run1 (the shared cache served the repeat).
    - tests/run-all-130.5.sh exists, is executable, mirrors run-all-130.sh; the Phase 130.5 block is registered in run-feynman-tests.cjs with every prior block byte-unchanged.
    - `grep -c '\-\-' tests/test-130.5-corpus-migration.cjs tests/run-all-130.5.sh` (em-dash literal scan, comment-filtered) returns 0.
  </acceptance_criteria>
  <done>A reproducible, network-free fixture regression proves the migration is byte-identical and the shared cache eliminates the duplicate fetch on repeat; the phase aggregator is green and CI-registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rs-discovery-engine -> fetchCorpus -> external APIs | the engine's corpus egress now funnels through the shared chokepoint |
| rs-discovery-engine -> chain-feeder -> Brain | the ONLY Brain path; must stay the only one (no new direct Brain call) |
| caller opts / flat_queries -> the engine | untrusted query input enters; may carry user/proprietary content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130.5-03-01 | Information Disclosure | migrated corpus egress (user content in flat_queries) | mitigate | fetchCorpus runs auditQueryString per query (Plan 01); the engine's existing auditQueryObject(opts) input audit stays first; Assertion C proves a planted forbidden query rejects pre-egress with zero fetch |
| T-130.5-03-02 | Elevation / boundary creep | a new direct Brain or fetch path slipping in during migration | mitigate | grep gate asserts zero direct `fetch(` in rs-discovery-engine.cjs (T6 preserved); Brain stays chain-feeder-only; the existing 9-scenario suite re-run guards both |
| T-130.5-03-03 | Tampering | cache poisoning via the shared cache during the engine run | mitigate | reads/writes go through Plan 02 getCached/putCached (atomic rename); a corrupt entry returns null (re-fetch), never a partial; SIGNAL-only payloads |
| T-130.5-03-04 | Repudiation / silent behavior drift | the migration silently changing discovery output | mitigate | Assertion A byte-compares the full downstream bundle (papers + scored + classified + theses) old-vs-new; any drift fails the suite |
| T-130.5-03-SC | Tampering | npm/pip/cargo installs | mitigate | zero new deps; no install task in this plan; rs_corpus.py / rs-engine.py NOT deleted here (Phase 134 owns that) |
</threat_model>

<verification>
- `node lib/memory/test-rs-discovery-engine.cjs` green (the existing 9-scenario suite -- zero regression, T5/T6/T8 Canon Part 8 invariants intact).
- `bash tests/run-all-130.5.sh` green (byte-identical + cache-hit-on-repeat + Part 8-preserved).
- grep proves zero direct `fetch(` in rs-discovery-engine.cjs and the corpus + cache modules are wired in.
- `node scripts/check-substrate.cjs` clean on the migrated file (the engine still reaches Brain only via chain-feeder; corpus + cache touch no room.db).
- Feynman runner registers the Phase 130.5 suite with all prior blocks byte-unchanged.
</verification>

<success_criteria>
- rs-discovery-engine's corpus fetch flows through the unified fetchCorpus + the shared cache (private fetcher path closed).
- A fixture-room run is byte-identical pre/post migration (full downstream bundle compared).
- A repeat run hits the shared cache (fewer external fetches second time -- duplicate-quota drift closed).
- Every prior Canon Part 8 invariant (no direct fetch, chain-feeder-only Brain, input audit) preserved.
- rs_corpus.py / rs-engine.py left intact for Phase 134.
</success_criteria>

<output>
Create `.planning/phases/130.5-shared-corpus-cache-cjs-fetcher-substrate/130.5-03-SUMMARY.md` when done.
</output>
