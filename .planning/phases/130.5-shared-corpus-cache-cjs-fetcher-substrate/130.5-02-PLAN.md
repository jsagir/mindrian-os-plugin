---
phase: 130.5-shared-corpus-cache-cjs-fetcher-substrate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/research-cache.cjs
  - lib/core/research-cache.test.cjs
autonomous: true
requirements: [SUBSTRATE-CORPUS-02]
must_haves:
  truths:
    - "A repeat (source, query) lookup within TTL returns the cached result without re-fetching"
    - "The cache is shared: a write by /mos:research and a read by rs-discovery-engine resolve to the same on-disk path for the same (source, query)"
    - "An expired (past-TTL) entry is treated as a miss so the caller re-fetches"
    - "Cache writes are atomic (mktemp + rename) so a concurrent reader never sees a half-written entry"
    - "The cache stores SIGNAL data only (public paper metadata); it is Python-free and adds no new npm dependency"
  artifacts:
    - path: "lib/core/research-cache.cjs"
      provides: "Shared TTL + source-keyed on-disk cache: getCached / putCached / cacheKey / isFresh"
      contains: "function cacheKey"
      min_lines: 90
    - path: "lib/core/research-cache.test.cjs"
      provides: "Behavior suite: hit-within-TTL, miss-past-TTL, atomic write, shared-path stability"
      contains: "getCached"
  key_links:
    - from: "lib/core/research-cache.cjs"
      to: "filesystem cache dir under .mindrian"
      via: "atomic mktemp + renameSync write of a source-keyed JSON entry"
      pattern: "renameSync"
---

<objective>
Ship `lib/core/research-cache.cjs` -- the ONE shared on-disk cache (TTL + source-keyed) so a paper fetched by `/mos:research` is not re-fetched by `rs-discovery-engine`. This closes the duplicate-API-quota drift the 2026-05-15 audit flagged (same paper, two code paths, no shared cache). The cache is consumed by Plan 03 (rs-discovery-engine migration) and by Phase 131; it is the SINGLE cache path both surfaces use.

Purpose: Build-once. Today `rs_corpus.py`/`rs_cache.py` (Pinecone) and the CJS fetcher have no shared local cache, so the same (source, query) gets fetched twice. This module is the shared, Python-free, native-fs cache keyed on (source, query) with a TTL freshness check.

Output: `lib/core/research-cache.cjs`, `lib/core/research-cache.test.cjs`.

NOTE ON SCOPE: This is the LOCAL on-disk fetch-result cache only. The Pinecone semantic-dedup namespace stays a Brain-side concern via the Phase 110 packet (NOT this module) per the CONTEXT's explicit out-of-scope list. This module embeds nothing and queries no vector store; it caches fetched result JSON keyed by (source, normalized query).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130.5-shared-corpus-cache-cjs-fetcher-substrate/130.5-CONTEXT.md
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase; do not re-explore. -->

From lib/core/rs_cache.py (the Python TTL cache being replaced -- port the TTL + source-keyed semantics, NOT the Pinecone calls):
```
# TTL_DAYS = 30 ; namespace_slug(topic) -> "external:{topic-slug}" ; is_fresh(age_days, ttl_days)
# The CJS module ports: source-keyed namespacing + a TTL freshness predicate. It does NOT port Pinecone upsert/query.
```

Atomic-write idiom already proven in the repo (mktemp + renameSync on same filesystem):
```
// See lib/core/mva-state.cjs / lib/core/write-lock.cjs / lib/hmi/jtbd-state.cjs for the
// fs.mkdtempSync or temp-file + fs.renameSync(2) atomic pattern. Reuse it; do not invent a new one.
```
</interfaces>

<read_first>
- lib/core/research-cache.cjs (the file to CREATE -- does not exist yet)
- lib/core/rs_cache.py (THE analog: TTL=30d + source-keyed namespace_slug + is_fresh; port the TTL + source-keying semantics to native fs; the Pinecone half is OUT OF SCOPE per the CONTEXT)
- lib/core/rs_corpus.py (topic_slug() -- the slug normalization to mirror for stable keys across case/whitespace)
- lib/core/mva-state.cjs (the atomic mktemp + renameSync write idiom to reuse)
- lib/hmi/jtbd-state.cjs (a second atomic-write reference: temp file + rename, bounded JSON state under .mindrian)
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: research-cache.cjs -- shared TTL + source-keyed atomic cache</name>
  <files>lib/core/research-cache.cjs, lib/core/research-cache.test.cjs</files>
  <read_first>
    - lib/core/rs_cache.py (TTL_DAYS + is_fresh + namespace_slug semantics)
    - lib/core/rs_corpus.py (topic_slug normalization)
    - lib/core/mva-state.cjs (atomic temp-file + renameSync write)
    - lib/hmi/jtbd-state.cjs (bounded JSON under .mindrian, atomic write reference)
  </read_first>
  <behavior>
    - Test 1: putCached(dir, 'openalex', 'quantum biology', resultArray) then getCached(dir, 'openalex', 'quantum biology') returns the same resultArray (hit within TTL).
    - Test 2: getCached for a (source, query) never written returns null (miss).
    - Test 3: an entry whose stored fetched_at is older than TTL (inject a past timestamp; or call with a tiny ttlMs override + a clock seam) is treated as a miss -- getCached returns null so the caller re-fetches.
    - Test 4: cacheKey('OpenAlex', 'Quantum  Biology!') === cacheKey('openalex', 'quantum biology') -- key is stable across case/whitespace/punctuation and namespaced by source, so /mos:research and rs-discovery-engine resolve the SAME path for the same logical (source, query).
    - Test 5: the on-disk write is atomic -- assert no temp/partial file remains after putCached, and the final file parses as valid JSON (no half-write window).
  </behavior>
  <action>
    Create `lib/core/research-cache.cjs` (`'use strict';`, pure CJS, node:fs + node:path + node:crypto only, zero new deps). Implement:
    - `cacheKey(source, query)`: lowercases + collapses non-alphanumeric runs to a single hyphen + strips edges (port `topic_slug` from rs_corpus.py), prefixes with the source id, and returns a filesystem-safe key. Two callers with the same logical (source, query) MUST get the identical key -- this is what makes the cache shared.
    - `cachePath(roomDir, source, query)`: resolves to `<roomDir>/.mindrian/research-cache/<cacheKey>.json` (one shared directory; both /mos:research and rs-discovery-engine point here). Ensure the dir via fs.mkdirSync({recursive:true}).
    - `putCached(roomDir, source, query, results, opts)`: writes an entry `{ source, query_key, fetched_at: <ISO now>, results }` ATOMICALLY (write to a temp file in the same dir via a unique name, then fs.renameSync onto the final path -- reuse the mva-state.cjs idiom). Returns the path.
    - `getCached(roomDir, source, query, opts)`: reads the entry; if absent returns null; computes age from fetched_at and compares against TTL; if past-TTL returns null (miss -> caller re-fetches). Accept `opts.ttlMs` (default a 30-day TTL mirroring rs_cache TTL_DAYS) and `opts.now` (a clock seam for deterministic tests). Tolerate a corrupt/unparseable entry by returning null (never throw).
    - `isFresh(fetchedAtIso, opts)`: the TTL predicate (true iff age <= ttl). Port rs_cache.is_fresh semantics.
    Canon Part 8: the cache stores ONLY SIGNAL data (public paper metadata returned by fetchCorpus); a comment block states no user/room content is cached here. Export `{ cacheKey, cachePath, getCached, putCached, isFresh }`. Write `lib/core/research-cache.test.cjs` (node:assert/strict, a tmp room dir via fs.mkdtempSync, the 5 behaviors above; use opts.now/opts.ttlMs for deterministic TTL). No em-dashes.
  </action>
  <verify>
    <automated>node lib/core/research-cache.test.cjs && node -e "const c=require('./lib/core/research-cache.cjs'); if(c.cacheKey('OpenAlex','Quantum  Biology!')!==c.cacheKey('openalex','quantum biology')) throw new Error('cacheKey not stable across case/whitespace'); console.log('OK shared key:',c.cacheKey('openalex','quantum biology'));"</automated>
  </verify>
  <acceptance_criteria>
    - `node lib/core/research-cache.test.cjs` passes all 5 behaviors.
    - cacheKey is stable across case/whitespace/punctuation and namespaced by source (proves the shared-path invariant for /mos:research + rs-discovery-engine).
    - A past-TTL entry returns null from getCached (the caller re-fetches).
    - `grep -nE "node:child_process|child_process|spawnSync|execSync|python|node-fetch|axios|requests|pinecone|Pinecone" lib/core/research-cache.cjs` returns nothing (Python-free, no Pinecone, no new dep).
    - `git diff package.json package-lock.json` shows no new dependency.
    - Atomic-write test confirms no leftover temp file and a valid-JSON final entry.
  </acceptance_criteria>
  <done>One shared, source-keyed, TTL-bounded, atomically-written, Python-free on-disk cache that both research surfaces resolve to the same path for the same (source, query).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched results -> on-disk cache | SIGNAL (public paper metadata) is persisted to .mindrian; no user/room content |
| concurrent writer + reader | a half-written cache file could be read mid-write |
| cache key construction | query string normalized into a filesystem path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130.5-02-01 | Tampering | concurrent half-write read (cache poisoning) | mitigate | writes are atomic: temp file in same dir + fs.renameSync(2); a reader sees either the old entry or the complete new one, never a partial; test asserts no leftover temp + valid JSON |
| T-130.5-02-02 | Information Disclosure | what gets persisted to the cache | mitigate | only fetchCorpus SIGNAL results (public metadata) are stored; in-source comment + the (source,query) key shape carry no room/user content; Canon Part 8 LOCAL-stays-local upheld |
| T-130.5-02-03 | Spoofing / Path Traversal | cacheKey -> filesystem path | mitigate | cacheKey collapses to [a-z0-9-] only (port of topic_slug); no path separators survive normalization, so a crafted query cannot escape the research-cache/ dir |
| T-130.5-02-04 | Denial of Service | unbounded cache growth | accept | entries are TTL-bounded and source-keyed (one file per logical query); pruning is a future concern, low risk at current volume |
| T-130.5-02-SC | Tampering | npm/pip/cargo installs | mitigate | zero new deps (acceptance grep on package.json); no install task in this plan |
</threat_model>

<verification>
- `node lib/core/research-cache.test.cjs` green (5 behaviors incl. atomic write + shared-key stability + past-TTL miss).
- zero Python / subprocess / Pinecone / new-dep greps return empty.
- `node scripts/check-substrate.cjs` clean: research-cache.cjs is fetch-cache only (no room.db, no node:sqlite), so it stays outside the substrate allow-list with zero bypass.
</verification>

<success_criteria>
- One shared TTL + source-keyed on-disk cache; repeat (source, query) within TTL is a hit, past-TTL is a miss.
- Both /mos:research and rs-discovery-engine resolve the same path for the same logical (source, query).
- Atomic writes (no cache poisoning); SIGNAL-only; Python-free; zero new npm deps.
</success_criteria>

<output>
Create `.planning/phases/130.5-shared-corpus-cache-cjs-fetcher-substrate/130.5-02-SUMMARY.md` when done.
</output>
