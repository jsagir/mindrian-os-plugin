---
phase: 130.5-shared-corpus-cache-cjs-fetcher-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/research-corpus.cjs
  - lib/core/research-corpus.test.cjs
  - data/research-sources.json
autonomous: true
requirements: [SUBSTRATE-CORPUS-01, SUBSTRATE-CORPUS-03, SUBSTRATE-CORPUS-04]
must_haves:
  truths:
    - "fetchCorpus({source, query, limit}) returns a normalized paper/result list for source openalex, arxiv, pubmed, or tavily"
    - "Every outbound query string passes the Canon Part 8 pre-egress audit before any fetch; a planted user-content query is rejected and no fetch is attempted (fails closed)"
    - "research-corpus.cjs uses native Node fetch only; it spawns no Python and requires no new npm dependency"
    - "sci-bot is a registered source but ships enabled:false and no-ops unless opt-in + user token + legal-review flag are all present"
    - "brain-cypher source routes only generic handles through the Phase 110 packet contract; it never embeds user content in a Cypher payload"
  artifacts:
    - path: "lib/core/research-corpus.cjs"
      provides: "Unified fetchCorpus + per-source adapter registry + SOURCES enum"
      contains: "function fetchCorpus"
      min_lines: 120
    - path: "data/research-sources.json"
      provides: "Source enum + per-source enabled/gating flags (incl. sci-bot enabled:false)"
      contains: "sci-bot"
    - path: "lib/core/research-corpus.test.cjs"
      provides: "Behavior suite incl. planted-violation fail-closed assertion"
      contains: "fetchCorpus"
  key_links:
    - from: "lib/core/research-corpus.cjs"
      to: "lib/core/rs-egress-prompts.cjs"
      via: "auditQueryString chokepoint call before any fetch"
      pattern: "auditQueryString"
    - from: "lib/core/research-corpus.cjs"
      to: "lib/core/rs-fetcher-academic.cjs"
      via: "delegation to the existing native-fetch academic fetcher (reuse, not re-port)"
      pattern: "rs-fetcher-academic"
    - from: "lib/core/research-corpus.cjs"
      to: "lib/core/brain-client.cjs"
      via: "brain-cypher adapter routes generic handles only through brain-client.query"
      pattern: "brain-client"
---

<objective>
Ship `lib/core/research-corpus.cjs` -- the ONE CJS-native unified external-corpus fetcher behind a single `fetchCorpus({source, query, limit})` interface, with per-source adapters for OpenAlex / arXiv / PubMed / Tavily / Brain-Cypher and a registered-but-disabled Sci-Bot adapter. Native `fetch` only; zero Python; zero new npm deps. Every outbound query passes the existing Canon Part 8 pre-egress audit (`auditQueryString` from `rs-egress-prompts.cjs`) before any network call -- fail-closed on a planted violation.

Purpose: This is the substrate Phase 131 CONSUMES and Phase 134 REUSES. Built once here so 131 adds no fetcher of its own and 134 deletes `rs_corpus.py` rather than re-porting it. Closes the duplicate-fetcher drift (the Python `rs_corpus.py` path silent-fails; this CJS path replaces it).

Output: `lib/core/research-corpus.cjs`, `data/research-sources.json`, `lib/core/research-corpus.test.cjs`.

REUSE-BEFORE-BUILD (Canon Part 7): The native-fetch academic fetching ALREADY EXISTS as `lib/core/rs-fetcher-academic.cjs` (openalex/arxiv/pubmed via global fetch, the `buildAcademicQuery` Part 8 chokepoint, dedup, telemetry). `fetchCorpus` does NOT re-implement HTTP per source -- it delegates openalex/arxiv/pubmed to the per-source dispatchers already exported there (`fetchOpenAlex` / `fetchArxiv` / `fetchPubMed`). The net-new surface is: (1) the unified single-source interface + SOURCES enum, (2) the Tavily adapter, (3) the Brain-Cypher adapter via the Phase 110 packet, (4) the disabled Sci-Bot stub, (5) the single fetchCorpus-level audit chokepoint. This repoints existing code; it does not duplicate it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130.5-shared-corpus-cache-cjs-fetcher-substrate/130.5-CONTEXT.md
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase; do not re-explore. -->

From lib/core/rs-fetcher-academic.cjs (the native-fetch academic fetcher to REUSE):
```
// SOURCES = Object.freeze(['openalex','arxiv','pubmed','scopus','ieee','nature'])
// buildAcademicQuery(query, source, opts) -- THE Part 8 chokepoint; calls auditQueryString(query,'academic') before URL build; throws ExternalEgressViolation on adversarial input.
async function fetchAcademic(queries, opts)   // queries: string[]; returns { tier, source, results, papers, telemetry }
async function fetchOpenAlex(queries, opts)   // one-source delegate -> fetchAcademic
async function fetchArxiv(queries, opts)
async function fetchPubMed(queries, opts)
// Normalized paper shape: { id, title, abstract, authors[], institution, doi, source, fetched_at }
```

From lib/core/rs-egress-prompts.cjs (the Part 8 pre-egress audit -- reuse, do NOT redefine):
```
function auditQueryString(s, surface)   // throws ExternalEgressViolation on FORBIDDEN_PATTERNS hit; returns s on pass
function auditQueryObject(obj, surface) // JSON.stringify scan; throws on hit; returns obj on pass
// FORBIDDEN_PATTERNS is re-exported BYTE-FOR-BYTE from cross-room-aggregator.cjs (Canon authoritative source).
```

From lib/core/rs-egress-violations.cjs:
```
class ExternalEgressViolation extends Error  // err.meta.surface convention; instanceof-checkable
```

From lib/core/brain-client.cjs (Brain-Cypher source -- generic handles ONLY, Canon Part 8):
```
function isAvailable()                  // false when no API key / unreachable
async function query(cypher, params)    // returns { records: [...] } | { error } | null; never throws on unreachable
function sanitizeCypherInput(value)     // bind $seed through this; never interpolate raw user content
```

From lib/core/navigation/packet.cjs (Phase 110 brain-context-packet contract):
```
async function buildBrainPacket(db, job, focusNodeId, opts)  // the typed packet; generic handles + hashes + enums only
// The brain-cypher adapter passes ONLY a generic framework/topic handle; it never sends artifact bodies.
```
</interfaces>

<read_first>
- lib/core/research-corpus.cjs (the file to CREATE -- does not exist yet)
- lib/core/rs-fetcher-academic.cjs (THE analog: native-fetch openalex/arxiv/pubmed + the buildAcademicQuery Part 8 chokepoint + dedup + telemetry -- delegate to its exported fetchOpenAlex/fetchArxiv/fetchPubMed; do NOT re-port the HTTP)
- lib/core/rs-egress-prompts.cjs (auditQueryString / auditQueryObject -- the existing Part 8 audit to route every query through)
- lib/core/rs-egress-violations.cjs (ExternalEgressViolation class)
- lib/core/rs_corpus.py (the CURRENT Python fetcher being replaced -- read its tiering + invert_abstract + dedup logic for parity; this CJS module is its replacement, 134 deletes the .py)
- lib/core/brain-client.cjs (query / isAvailable / sanitizeCypherInput -- the brain-cypher source path, generic handles only)
- lib/core/navigation/packet.cjs (the Phase 110 typed packet contract the brain-cypher adapter honors)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source enum + gating data file</name>
  <files>data/research-sources.json</files>
  <read_first>
    - data/command-registry.json (the data/ JSON layout convention to mirror)
    - .planning/phases/130.5-shared-corpus-cache-cjs-fetcher-substrate/130.5-CONTEXT.md (the Sci-Bot opt-in subsection: token + queue + legal-review gates)
  </read_first>
  <action>
    Create `data/research-sources.json` as the single source-of-truth for the source enum and per-source gating. Top-level key `sources` is an array; each entry carries `id`, `enabled` (boolean), `kind` (one of `academic` / `web` / `graph`), and a `gates` array of required-precondition enum strings. Entries:
    - `openalex`, `arxiv`, `pubmed`: enabled:true, kind:academic, gates:[] (the three no-key default sources).
    - `tavily`: enabled:true, kind:web, gates:["env:TAVILY_API_KEY"] (gated only by the existing key, not opt-in).
    - `brain-cypher`: enabled:true, kind:graph, gates:["brain:isAvailable"] (generic handles only; no user content).
    - `sci-bot`: enabled:false, kind:web, gates:["opt_in:user", "token:user_supplied", "legal_review:signed_off"]. Carry a `provenance_note` string field stating the Sci-Hub provenance + commercial/academic-tester legal-review-required posture from the CONTEXT, and an `endpoint` field "https://sci-bot.ru" for documentation only (no scraper). Per the CONTEXT, Sci-Bot output, when ever enabled, is a Practitioner/None-tier lead requiring re-grounding (Canon Part 5) -- record that as a `tier_on_enable` field set to "practitioner".
    Default-source set (the four shipped-on) is openalex/arxiv/pubmed/tavily per the CONTEXT. No em-dashes anywhere in the file.
  </action>
  <verify>
    <automated>node -e "const j=require('./data/research-sources.json'); const sb=j.sources.find(s=>s.id==='sci-bot'); if(sb.enabled!==false) throw new Error('sci-bot must be enabled:false'); if(!sb.gates.includes('legal_review:signed_off')) throw new Error('sci-bot missing legal gate'); const def=j.sources.filter(s=>s.enabled&&s.gates.filter(g=>g.startsWith('opt_in')).length===0).map(s=>s.id).sort(); console.log('OK default-eligible:',def.join(','));"</automated>
  </verify>
  <acceptance_criteria>
    - `data/research-sources.json` parses; `sci-bot` is `enabled:false` and carries all three gates (opt_in:user, token:user_supplied, legal_review:signed_off).
    - openalex / arxiv / pubmed / tavily are the default-eligible enabled sources; brain-cypher is enabled but gated on brain availability.
    - `grep -c '\-\-' data/research-sources.json` (em-dash literal) returns 0.
  </acceptance_criteria>
  <done>The source enum + gating contract exists as data; the disabled Sci-Bot adapter's gates are encoded and machine-readable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: research-corpus.cjs -- fetchCorpus unified interface + adapters + fail-closed audit</name>
  <files>lib/core/research-corpus.cjs, lib/core/research-corpus.test.cjs</files>
  <read_first>
    - lib/core/rs-fetcher-academic.cjs (delegate openalex/arxiv/pubmed to fetchOpenAlex/fetchArxiv/fetchPubMed; reuse its dedup + paper shape)
    - lib/core/rs-egress-prompts.cjs (auditQueryString -- the single chokepoint call in fetchCorpus)
    - lib/core/brain-client.cjs (query / isAvailable / sanitizeCypherInput for the brain-cypher adapter)
    - lib/core/navigation/packet.cjs (Phase 110 packet contract the brain-cypher adapter honors)
    - lib/memory/test-rs-fetcher-academic.cjs (the node:assert/strict + global.fetch-stub test idiom to mirror)
  </read_first>
  <behavior>
    - Test 1: `fetchCorpus({source:'openalex', query:'quantum biology', limit:5})` with a stubbed global.fetch returns a normalized result array (paper shape: id/title/abstract/authors/doi/source/fetched_at); source field === 'openalex'.
    - Test 2: a planted user-content query (a string matching a FORBIDDEN_PATTERN, e.g. an artifact-body / proprietary-number pattern) makes `fetchCorpus` throw ExternalEgressViolation BEFORE global.fetch is called -- assert the fetch stub call-count is 0 (fails closed).
    - Test 3: `fetchCorpus({source:'sci-bot', query:'x'})` returns a disabled no-op envelope `{disabled:true, reason:'sci-bot disabled (opt-in + token + legal-review required)'}` and never calls fetch (assert call-count 0). With opt-in+token+legal flags absent it stays disabled.
    - Test 4: `fetchCorpus({source:'brain-cypher', query:'SWOT'})` with brain-client.isAvailable() stubbed false returns an empty/degraded envelope and never throws; with isAvailable() true, the only Brain call passes a sanitized generic handle (assert the cypher/param carries the handle, NOT the raw query if the query were user content -- a forbidden-pattern brain-cypher query is rejected by the same audit before the Brain call).
    - Test 5: unknown source throws TypeError; `require('node:child_process')` / python spawn appears ZERO times in the module source.
  </behavior>
  <action>
    Create `lib/core/research-corpus.cjs` (`'use strict';`, pure CJS, native fetch only, zero new deps). Load the source registry from `data/research-sources.json` and expose `SOURCES` (frozen id list). Implement `fetchCorpus({ source, query, limit })` as the single entry point and THE audit chokepoint: it first calls `auditQueryString(query, 'research-corpus')` (from rs-egress-prompts.cjs) so a forbidden query throws ExternalEgressViolation pre-egress for EVERY source uniformly -- do not rely on per-fetcher audits alone (defense-in-depth is fine, but fetchCorpus must itself fail closed). Validate `source` against SOURCES (TypeError on unknown) and `query` is a non-empty string. Then dispatch via an adapter registry keyed by source id:
    - `openalex` / `arxiv` / `pubmed`: delegate to rs-fetcher-academic.cjs `fetchOpenAlex` / `fetchArxiv` / `fetchPubMed` passing `[query]` and an opts object carrying `limit`; return the normalized `results` array.
    - `tavily`: build the Tavily POST (endpoint https://api.tavily.com/search, body { api_key: process.env.TAVILY_API_KEY, query, max_results: min(limit,20), search_depth:'advanced' }). If TAVILY_API_KEY absent, return a degraded empty envelope (do not throw). Use the SAME fetchWithTimeout/AbortController posture as rs-fetcher-academic (10s default). Normalize each hit to the shared paper shape (source:'tavily', abstract = content snippet, doi:null). Port the Python `fetch_tavily` parse logic from rs_corpus.py (snippet-only).
    - `brain-cypher`: this is the Phase 110 packet path. If `brain-client.isAvailable()` is false, return a degraded empty envelope (no throw). When available, treat `query` as a GENERIC framework/topic handle only: bind it through `brain-client.sanitizeCypherInput` (never interpolate raw), run a read-only methodology query, and return the framework handles. The fetchCorpus-level audit already rejected any user-content query before this path runs. NEVER send artifact bodies; honor the Phase 110 packet contract (generic handles + enums only).
    - `sci-bot`: gated stub. Read the gates from the registry; unless ALL of (opts.optIn === true) AND (a user token is present, e.g. process.env.SCIBOT_TOKEN) AND (opts.legalReviewSignedOff === true) are satisfied, return `{ disabled:true, reason:'sci-bot disabled (opt-in + token + legal-review required)' }` and perform NO fetch. Do NOT implement a headless-browser scraper or any HTTP path -- the enabled branch is a documented `throw new Error('sci-bot adapter not implemented: requires official token-authenticated endpoint')` placeholder so enabling without an API cannot silently scrape. Document the queue/token/legal gates from the CONTEXT in a comment block.
    Normalize the `limit` (default 20, hard-cap mirroring rs_corpus MAX). Export `{ fetchCorpus, SOURCES, _adapters }` (adapters exposed for tests). Write `lib/core/research-corpus.test.cjs` (node:assert/strict, global.fetch stub with a call-counter, mirroring test-rs-fetcher-academic.cjs) covering the 5 behaviors above. No em-dashes.
  </action>
  <verify>
    <automated>node lib/core/research-corpus.test.cjs && node -e "const m=require('./lib/core/research-corpus.cjs'); if(!Array.isArray(m.SOURCES)||!m.SOURCES.includes('brain-cypher')||!m.SOURCES.includes('sci-bot')) throw new Error('SOURCES enum incomplete'); console.log('OK SOURCES:',m.SOURCES.join(','));"</automated>
  </verify>
  <acceptance_criteria>
    - `node lib/core/research-corpus.test.cjs` passes all 5 behavior cases.
    - `grep -nE "require\(['\"]node:child_process['\"]\)|child_process|spawnSync|execSync|python" lib/core/research-corpus.cjs` returns nothing (zero Python, zero subprocess).
    - `grep -nE "node-fetch|axios|got|requests" lib/core/research-corpus.cjs` returns nothing (native fetch only).
    - `git diff package.json package-lock.json` shows no new dependency added (zero new npm deps).
    - The planted-violation test proves global.fetch call-count is 0 when a forbidden query is passed (fails closed).
    - sci-bot path returns `disabled:true` with the three-gate reason and performs zero fetch.
  </acceptance_criteria>
  <done>fetchCorpus is the single unified interface + the single fail-closed Part 8 audit chokepoint; openalex/arxiv/pubmed reuse the existing native fetcher; tavily + brain-cypher adapters work within Canon Part 8; sci-bot is a disabled gated stub with no scraper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin -> external APIs (OpenAlex/arXiv/PubMed/Tavily) | outbound query strings cross here; SIGNAL egress per Canon Part 8 |
| plugin -> Brain MCP (brain-cypher source) | generic handles only; NO user content (Canon Part 8 / Part 9 packet) |
| caller -> fetchCorpus(query) | untrusted query string enters here; may contain user content / adversarial input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130.5-01-01 | Information Disclosure | fetchCorpus query string -> any external API | mitigate | fetchCorpus calls auditQueryString(query,'research-corpus') BEFORE any dispatch; forbidden pattern throws ExternalEgressViolation pre-egress; planted-violation test asserts zero fetch (fail closed) |
| T-130.5-01-02 | Information Disclosure | brain-cypher adapter -> Brain MCP | mitigate | only a generic handle bound via sanitizeCypherInput reaches Brain; the fetchCorpus audit rejects user-content queries before the Brain path; honors Phase 110 packet contract (handles + enums only) |
| T-130.5-01-03 | Tampering / Elevation | sci-bot adapter (Sci-Hub provenance, legal flag) | mitigate | sci-bot ships enabled:false; the enabled branch is an explicit throw, NOT a scraper; requires opt_in + user token + legal_review:signed_off, none default-true; provenance/legal posture recorded in data/research-sources.json |
| T-130.5-01-04 | Spoofing / SSRF | per-source endpoint URL construction | mitigate | endpoints are a frozen constant map (no caller-controlled host); query is URL-encoded; reuse rs-fetcher-academic's fixed ENDPOINTS map -- no caller-supplied base URL |
| T-130.5-01-05 | Denial of Service | API key handling (TAVILY_API_KEY, brain) | accept | key read from process.env at call time, never logged, never echoed into results; absence degrades to empty envelope (no throw) -- low risk, env-scoped |
| T-130.5-01-SC | Tampering | npm/pip/cargo installs | mitigate | zero new deps (acceptance grep on package.json); no install task in this plan, so no [ASSUMED]/[SUS] package checkpoint required |
</threat_model>

<verification>
- `node lib/core/research-corpus.test.cjs` green (5 behaviors).
- `node data/research-sources.json` parse + sci-bot disabled gate assertion green.
- `node scripts/check-substrate.cjs` (or its --diff staged form) clean against the new files (research-corpus.cjs does not touch room.db; it is fetch+cache only, so it stays outside the substrate allow-list with zero bypass -- no node:sqlite, no openRoomDb).
- zero Python / subprocess / new-dep greps return empty.
</verification>

<success_criteria>
- One unified `fetchCorpus({source, query, limit})` covering openalex/arxiv/pubmed/tavily/brain-cypher + a disabled sci-bot.
- Native fetch only; zero Python; zero new npm deps.
- The Part 8 pre-egress audit fails closed on a planted user-content query (no fetch attempted).
- Sci-Bot registered enabled:false behind opt-in + token + legal-review gates; no scraper shipped.
- brain-cypher honors the Phase 110 packet (generic handles only).
</success_criteria>

<output>
Create `.planning/phases/130.5-shared-corpus-cache-cjs-fetcher-substrate/130.5-01-SUMMARY.md` when done.
</output>
