---
phase: 361-dominant-design-research-mode-mos-dominant-designs-gains-a-r
plan: 08
type: execute
wave: 5
depends_on: [361-07]
files_modified:
  - .planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-LIVE-EVIDENCE.md
  - data/subagent-dispatch-grants.json
  - .planning/REQUIREMENTS.md
  - docs/OPEN-HANDOFFS.md
autonomous: false
requirements: [DDR361-09, DDR361-10, DDR361-11, DDR361-12]
canon_parts: [3, 8, 11]
tags: [live-probe, theo, grant-ratification, checkpoint, close-out, dominant-design]

must_haves:
  truths:
    - "A live Theo probe through scripts/dominant-design-research.cjs theo-structure sends only {framework: 'Dominant Design'} to each of the three tools, and framework_step's response no longer carries an egress_disclosure (361-02's arm took effect live); the observed source and reasons are recorded (D-09, D-10, D-16)"
    - "The navigator decides the Task grant for commands/dominant-designs.md at a blocking checkpoint; only a navigator answer flips the row from pending to granted (D-11); no agent identity ever appears as reviewer"
    - "Every DDR361 row is closed with its measured proof or left open with a stated reason; DDR361-12 records that the registry hash moved and the next real release's Step 5.6 theo-resync carries it, and that this phase sent no notify"
    - "The carry-forwards are written where the next session will find them: the theo-resync at next release, the STALE irreversibility-ledger entry for Phase 356's rescore, the D-17 fetcher tools: quick task, the D-15 case_story recheck, and the dist bundle refresh"
  artifacts:
    - path: ".planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-LIVE-EVIDENCE.md"
      provides: "live probe record, full-suite and acceptance results, grant decision"
      contains: "framework_step"
    - path: "data/subagent-dispatch-grants.json"
      provides: "dominant-designs grant row, granted or still pending per the navigator"
      contains: "commands/dominant-designs.md"
  key_links:
    - from: "361-LIVE-EVIDENCE.md"
      to: "scripts/dominant-design-research.cjs theo-structure"
      via: "live run output (calls[].args, calls[].egress_disclosure, source, reasons)"
      pattern: "egress_disclosure"
---

<objective>
Close Phase 361: prove the Theo read and the Part 8 arms live with only the generic handle, have the navigator ratify the Task grant, run the full phase and acceptance gates, and close every DDR361 row honestly with the carry-forwards recorded.

Purpose: D-11 (grant row pending until the navigator ratifies; the checkpoint is the navigator's, not an agent's), D-16 (live Theo reality), DDR361-12 (release-time resync recorded, not fired). The live Theo probe sends only generic framework names.
Output: `361-LIVE-EVIDENCE.md`, the grant row decision applied, REQUIREMENTS rows closed, one OPEN-HANDOFFS entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-CONTEXT.md
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-02-SUMMARY.md
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-05-SUMMARY.md
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-07-SUMMARY.md
@CLAUDE.md

Parallel-session rules: identical to 361-01-PLAN.md `<context>`. Never fire the theo-resync, never run scripts/release.sh, never push. Never rebuild data/command-irreversibility-ledger.json (Phase 356's). `git add -f` for the phase-dir file and REQUIREMENTS.md.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live Theo probe (generic handle only) and full gate run</name>
  <files>.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-LIVE-EVIDENCE.md</files>
  <read_first>
    - 361-02-SUMMARY.md, 361-05-SUMMARY.md, 361-07-SUMMARY.md
    - scripts/dominant-design-research.cjs (theo-structure subcommand output shape)
    - 361-RESEARCH.md Pattern 5 table (the 2026-09-23 live baseline to compare against)
  </read_first>
  <action>
    Run `node scripts/dominant-design-research.cjs theo-structure` live (normal HOME, normal network). This sends Theo exactly three calls whose only argument is `{framework: "Dominant Design"}`; no domain, room or venture text is involved. Assert from its JSON: every `calls[].args` deep-equals `{framework: 'Dominant Design'}`; `calls[0].egress_disclosure` is false for framework_step when Theo answered (the 361-02 arm now proves the shape; before the phase it was ambiguous). If Theo is unreachable (all reasons brain_unavailable), record an ENV GAP rather than a failure and note that the arm effect is proven offline by test-361-egress-shapes. Record `source`, `reason`, and per-tool `reasons` and compare with the Pattern 5 baseline (expected today: framework_step no_steps_in_canon, framework_techniques and case_story not_served, source reference; any change means Theo 20 or 20.1 deployed, which is news worth a line).

    Then run: `bash tests/run-all-361.sh`, `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs`, `node scripts/doctor.cjs --acceptance`. Classify every non-pass per the RCA standard (WORKING, known tracked bug, ENV GAP, NEW FAILURE); a failure owned by a peer phase is recorded with its owner and not touched.

    Write `361-LIVE-EVIDENCE.md` (hyphens only): date, commit, the live probe JSON (calls, source, reasons), the comparison with the baseline, each gate's result and classification, and the registry hash pair from 361-07-SUMMARY. Commit: `git add -f <PD>/361-LIVE-EVIDENCE.md` then `git commit --only -m "docs(361-08): live Theo probe and phase gate evidence" -- <PD>/361-LIVE-EVIDENCE.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-LIVE-EVIDENCE.md && grep -q 'framework_step' .planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-LIVE-EVIDENCE.md && bash tests/run-all-361.sh</automated>
  </verify>
  <acceptance_criteria>
    - 361-LIVE-EVIDENCE.md contains the three recorded calls, each with args exactly `{"framework":"Dominant Design"}`
    - It states framework_step's egress_disclosure value (false expected) or an ENV GAP with the reason
    - `bash tests/run-all-361.sh` exits 0; doctor --acceptance result is recorded with every non-pass classified
  </acceptance_criteria>
  <done>Research mode's Theo leg is proven live with the generic handle only, and the phase gates are recorded.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Navigator ratifies the Task grant for /mos:dominant-designs</name>
  <decision>Ratify the pending Task grant in data/subagent-dispatch-grants.json for commands/dominant-designs.md, or keep it pending.</decision>
  <context>
    Research mode fans out one read-only dominant-design-researcher per approved evidence lane (at most 4) strictly after the query gate card. The grant pre-approves those spawns so the navigator is not asked for permission up to four times per deep dive. Each worker can only search (two Tavily names plus WebSearch) and Read; it cannot write or reach the Brain (host-enforced tools list). The row today: status pending, reviewed_by navigator, reviewed_date 2026-09-23, fan_bound 4 clamped by resolveFanoutCap. With the row pending, the command still works; each spawn may show a permission prompt, and tests/test-265-swarm-task-grant.cjs reports it as unratified (a strict gate run would fail).

    Before deciding, the navigator may run a live smoke on a scratch room (this spends up to 4 Tavily searches and writes to that room only after the filing confirm): in Claude Code, bind a scratch room, run /mos:dominant-designs, pick deep dive, name a generic domain (for example "lithium-ion battery cells"), approve the gate card, then check that four lane agents dispatched only after approval, that every row shown carries a source, and that the filing report lists each lane as landed. Evidence so far is in 361-LIVE-EVIDENCE.md.
  </context>
  <options>
    <option id="ratify">
      <name>Ratify (Recommended)</name>
      <pros>No per-spawn prompts; strict grant gate passes; matches the find-analogies precedent for a gated, read-only fan-out.</pros>
      <cons>Pre-approves up to four spawns per deep dive without a prompt (still behind the gate card).</cons>
    </option>
    <option id="keep-pending">
      <name>Keep pending</name>
      <pros>Every spawn still asks for permission until a later review.</pros>
      <cons>Up to four prompts per deep dive; DDR361-11 closes as open with the reason "grant pending navigator ratification"; a strict grant gate run fails.</cons>
    </option>
  </options>
  <resume-signal>Answer "ratify" or "keep-pending" (optionally with the date of your review).</resume-signal>
  <files>.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-LIVE-EVIDENCE.md</files>
  <action>
    Present this decision to the navigator with AskUserQuestion (two options, "Ratify (Recommended)" first), quoting the context above and pointing at 361-LIVE-EVIDENCE.md. Do not change data/subagent-dispatch-grants.json in this task and never answer on the navigator's behalf (workflow.auto_advance does not apply: a grant widening is the navigator's call per D-11). When the answer arrives, append one line to 361-LIVE-EVIDENCE.md: `Grant decision: <ratify|keep-pending>, answered by the navigator on <date>` plus the navigator's words verbatim if they added any, and commit it with `git add -f` + `git commit --only -m "docs(361-08): record navigator grant decision" -- <that path>`.
  </action>
  <verify>
    <automated>grep -qE '^Grant decision: (ratify|keep-pending), answered by the navigator' .planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-LIVE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - 361-LIVE-EVIDENCE.md carries exactly one `Grant decision:` line naming the navigator as the answerer
    - data/subagent-dispatch-grants.json is unchanged by this task (`git diff HEAD~1 -- data/subagent-dispatch-grants.json` empty for this task's commit)
  </acceptance_criteria>
  <done>The navigator's grant answer is recorded verbatim and nothing else changed.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the decision and close the phase ledger</name>
  <files>data/subagent-dispatch-grants.json, .planning/REQUIREMENTS.md, docs/OPEN-HANDOFFS.md</files>
  <read_first>
    - data/subagent-dispatch-grants.json (the dominant-designs row and a ratified row, for example commands/find-analogies.md, for ratified_date / ratified_in)
    - .planning/REQUIREMENTS.md (the DDR361 block)
    - docs/OPEN-HANDOFFS.md (top entries: bold dated one-paragraph entries, newest first)
    - 361-01..07 SUMMARYs (proof per requirement)
  </read_first>
  <action>
    Grant (only on the navigator's "ratify" answer from Task 2): check `git status --short -- data/subagent-dispatch-grants.json` is clean, then with a parse-and-reserialize node script that preserves formatting set the dominant-designs row's status to `granted` and add `ratified_date` (the navigator's stated date, else today's date) and `ratified_in` `361-08`; confirm `git diff` touches only that row. Run `node tests/test-265-swarm-task-grant.cjs` and `TEST_265_GRANTS_STRICT=1 node tests/test-265-swarm-task-grant.cjs` (both exit 0 after ratification). Commit with `git add` + `git commit --only -m "chore(361-08): ratify dominant-designs Task grant (navigator)" -- data/subagent-dispatch-grants.json`. On "keep-pending", change nothing and record the decision.

    REQUIREMENTS: check `git status --short -- .planning/REQUIREMENTS.md` is clean (STOP on an unowned diff), then for each DDR361 row either run `gsd-tools query requirements.mark-complete <ID>` when its proof is recorded (tests PASS in run-all-361, live evidence, SUMMARY lines), or append ` Left open: <reason>.` to the row. Expected closes: 01-10 and 13 on green tests plus live evidence; 11 only if the grant was ratified (else left open, reason "Task grant pending navigator ratification"); 12 closes on the recorded hash move with the sentence "the notify rides the next real release's Step 5.6 theo-resync; this phase sent none". If `requirements.mark-complete` mis-edits (the known resync pattern), fix the rows by hand to `- [x]`. Commit with `git add -f .planning/REQUIREMENTS.md` + `git commit --only -m "docs(361-08): close DDR361 rows with proof" -- .planning/REQUIREMENTS.md`.

    OPEN-HANDOFFS: if `git status --short -- docs/OPEN-HANDOFFS.md` is clean, prepend one bold dated entry (2026-09-23 or the actual date) in the file's style: Phase 361 shipped research mode on main (not live until released and picked up); carried forward: (1) the next real release's Step 5.6 theo-resync carries the new command-registry hash; (2) `data/command-irreversibility-ledger.json` reports STALE for /mos:dominant-designs until Phase 356's next Jev rescore; (3) D-17 quick task: add `tools:` to agents/analogy-query-fetcher.md and agents/competitor-watch-fetcher.md; (4) D-15: recheck the case_story arm when Theo 20.1's MOS-LEARNING lands (tests/test-361-theo-parity.cjs fails automatically once Theo ships a case-story input shape that differs); (5) dist/ bundles refresh with the next dist chore; (6) Theo framework_step for Dominant Design still has zero steps, so research mode reads structure from the reference until Theo canon gains steps. Commit with `git add` + `git commit --only -m "docs(361-08): open-handoffs entry for Phase 361 carry-forwards" -- docs/OPEN-HANDOFFS.md`. If the file is dirty, skip it and put the same list in the SUMMARY under "Carried forward", noting the skip.
  </action>
  <verify>
    <automated>node tests/test-265-swarm-task-grant.cjs && test "$(grep -cE '^- \[(x| )\] \*\*DDR361-' .planning/REQUIREMENTS.md)" -eq 13 && bash tests/run-all-361.sh</automated>
  </verify>
  <acceptance_criteria>
    - The dominant-designs grant row's status matches the navigator's answer (granted with ratified_date and ratified_in 361-08, or unchanged pending); reviewed_by is navigator
    - Every DDR361 row is `[x]` or carries a `Left open:` reason; `grep -c 'DDR361' .planning/REQUIREMENTS.md` did not decrease
    - OPEN-HANDOFFS has the dated Phase 361 entry, or the SUMMARY says why it was skipped and carries the same six items
    - `bash tests/run-all-361.sh` still exits 0
  </acceptance_criteria>
  <done>The grant reflects the navigator's decision, every requirement is closed or honestly open, and the next session can find every carry-forward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin -> Theo (live) | the probe sends three calls to the remote Brain |
| agent -> grant ledger | a ratification write widens standing permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-361-39 | Information disclosure | live probe | mitigate | the probe is the theo-structure subcommand, whose args are the constant `{framework: "Dominant Design"}`; the evidence file records every args object |
| T-361-40 | Elevation of privilege | grant ratification | mitigate | blocking checkpoint:decision; the row flips only on the navigator's "ratify"; reviewed_by stays navigator; strict grant test run |
| T-361-41 | Repudiation | requirement closure | mitigate | each close cites recorded proof; open rows carry a reason; DDR361-12 states the notify is release-time |
| T-361-42 | Tampering | shared files (REQUIREMENTS, OPEN-HANDOFFS, grants) | mitigate | clean-state check before each edit, single-path `git commit --only`, skip-and-record on a dirty OPEN-HANDOFFS |
</threat_model>

<verification>
`361-LIVE-EVIDENCE.md` exists with generic-handle-only calls; `bash tests/run-all-361.sh` exits 0; the grant row matches the navigator's answer; every DDR361 row is closed or open with a reason.
</verification>

<success_criteria>
Phase 361 closes with live proof of the Theo leg, a navigator-owned grant decision, and an honest requirement ledger plus visible carry-forwards.
</success_criteria>

## Artifacts this phase produces

This plan's share: `361-LIVE-EVIDENCE.md`, the grant decision in `data/subagent-dispatch-grants.json`, closed DDR361 rows in `.planning/REQUIREMENTS.md`, one `docs/OPEN-HANDOFFS.md` entry.

Whole-phase artifact list (for the verifier): `agents/dominant-design-researcher.md`; upgraded `commands/dominant-designs.md` and regenerated `skills/dominant-designs/SKILL.md`; `lib/core/dominant-design/lane-queries.cjs`, `evidence-pack.cjs`, `theo-structure.cjs`; `scripts/dominant-design-research.cjs`; three Part 8 arms in `lib/core/part8-egress-guard.cjs`; the Research mode bullet in `references/methodology/dominant-designs.md`; the grant row; regenerated registries and ledgers; `tests/run-all-361.sh`, `tests/fixtures/361-pre-phase.json` and ten `tests/test-361-*.cjs` files; at runtime, per research run, lane evidence artifacts and an analysis artifact under `<room>/competitive-analysis/dominant-designs/` plus proposed EvidenceClaim nodes in room.db.

<output>
Create `.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-08-SUMMARY.md` when done, with the navigator's grant answer quoted and the "Carried forward" list.
</output>
