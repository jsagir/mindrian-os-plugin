---
phase: 361-dominant-design-research-mode-mos-dominant-designs-gains-a-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/fixtures/361-pre-phase.json
  - tests/test-361-baseline.cjs
  - tests/run-all-361.sh
autonomous: true
requirements: [DDR361-10, DDR361-12, DDR361-13]
canon_parts: [8, 12]
tags: [requirements, baseline, aggregator, dominant-design]

must_haves:
  truths:
    - "REQUIREMENTS.md carries a `### Phase 361 - Dominant-design research mode (DDR361 family)` block with exactly thirteen `- [ ] **DDR361-NN**` rows (01 to 13), placed after the Phase 360 block and before `## Traceability`, and the Traceability section names the family"
    - "A pre-phase baseline fixture pins the commit every later 361 test measures against: the base sha, the sha256 of data/command-registry.json, the pre-phase /mos:dominant-designs registry row, the pre-phase Setup section and quick-pass paragraph of commands/dominant-designs.md, and the pre-phase find_connections arm slice of lib/core/part8-egress-guard.cjs"
    - "tests/run-all-361.sh is written once, here, and names a guarded leg for every planned test-361 file, the existing suites 361 must keep green, the generator --check gates, the CIRS plan-declaration check, and an em-dash guard over every 361 surface; a missing planned test file reports SKIPPED, never PASSED"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "DDR361-01..13 rows and the traceability line"
      contains: "DDR361-13"
    - path: "tests/fixtures/361-pre-phase.json"
      provides: "pre-361 base sha and digests"
      contains: "base_sha"
    - path: "tests/test-361-baseline.cjs"
      provides: "proves the fixture matches the base commit and that the base is an ancestor of HEAD"
      min_lines: 60
    - path: "tests/run-all-361.sh"
      provides: "phase aggregator (run / run_if / counters / em-dash guard)"
      min_lines: 80
  key_links:
    - from: "tests/test-361-baseline.cjs"
      to: "tests/fixtures/361-pre-phase.json"
      via: "recomputes every digest from `git show <base_sha>:<path>` and compares"
      pattern: "361-pre-phase.json"
    - from: "tests/run-all-361.sh"
      to: "tests/test-361-*.cjs"
      via: "run_if guard per planned file"
      pattern: "run_if"
---

<objective>
Open Phase 361 safely: mint the DDR361 requirement family, pin the pre-phase state every later 361 test measures against, and write the phase aggregator once.

Purpose: DDR361-13 (quick pass unchanged) and DDR361-12 (registry row moves) need a before-picture taken before any 361 edit lands, and DDR361-10 needs the find_connections arm's pre-phase bytes so a later test can prove no 361 commit touched Phase 355's block (D-10, D-14). The orchestrator requires the IDs minted in the first plan.
Output: 13 REQUIREMENTS rows, `tests/fixtures/361-pre-phase.json`, `tests/test-361-baseline.cjs`, `tests/run-all-361.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-CONTEXT.md
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-RESEARCH.md
@CLAUDE.md

PD = `.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r`.

Parallel-session rules (every task in every 361 plan): three other sessions commit in this repo (Phases 355, 356, 358, 359, 360). Record `PLAN_BASE=$(git rev-parse HEAD)` at plan start. Before editing any file run `git status --short -- <file>` and STOP on a diff you did not make (report it; never work around it). Explicit-path commits only: `git add <path>` (`git add -f` for anything under `.planning/`), then `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git commit -a`, `git stash`, `git checkout -- <unowned path>`, and never revert an unowned diff. Never push. Never run `scripts/release.sh`. Never write `.planning/STATE.md` (the orchestrator owns it). Never edit: Phase 355's find_connections arm in `lib/core/part8-egress-guard.cjs`, `scripts/doctor.cjs`, `scripts/eval-icm-writers.cjs`, `tests/test-353-*.cjs`, `docs/reviews/*`, `evals/*`, `data/command-irreversibility-ledger.json`, `data/jev-labels/*`, `dist/*`, `agents/analogy-query-fetcher.md`, `agents/competitor-watch-fetcher.md`. No em-dashes or en-dashes in any file this phase writes (hyphens only). Never the Fable model.

Test hygiene (every new test-361 file): plain Node `assert`, no deps; at the top replace `globalThis.fetch` with a function that writes `NETWORK_ATTEMPT_361` to stderr and throws; any spawned child gets `NODE_OPTIONS=--require <abs path of a preload written into a mkdtemp dir>` whose body installs the same thrower; tests write only to mkdtemp dirs; exit 0 PASS, 1 FAIL, 77 ENV GAP.
</context>

<requirements_rows>
Row text for Task 1, one row per ID, each written as `- [ ] **DDR361-NN**: <text>` wrapped at about 100 columns with six-space continuation indent (the BIND360 row style):

1. DDR361-01: A new read-only agent `agents/dominant-design-researcher.md` answers ONE evidence lane per invocation, runs exactly the gate-approved query string(s) and never composes, rephrases or expands one (D-03, D-04), returns structured JSON only, and is host-restricted by a `tools:` list mirrored byte-identically under `allowed-tools:` (two Tavily search names, WebSearch, Read; no Write, Edit, Bash, Task, Agent or Brain tool), with `connector.excluded: true` plus a reason and no `hitl_shape` (D-17). Plan 361-04.
2. DDR361-02: One deterministic composer (`lib/core/dominant-design/lane-queries.cjs`) produces the four lane queries from the navigator's generic domain phrase and audits every string, and every navigator edit, through `auditQueryString` before it can reach the gate card; an audit failure degrades to local-only with no send-anyway path. Plans 361-03, 361-06.
3. DDR361-03: No researcher agent is dispatched before the navigator approves the gate card; the navigator can edit or drop lanes; an unattended or chain-driven run always takes the quick pass and never researches (D-03, D-12). Plan 361-07.
4. DDR361-04: Approved lanes fan out in parallel, one agent per lane, at most 4 agents and at most 2 approved queries per lane, exactly one `tavily-search` call per query with fixed parameters (`search_depth: basic`, `topic: general`, `max_results: 10`), `WebSearch` with the identical string only as the fallback, no `tavily-extract`; Desktop and Cowork say plainly that the research pass runs in Claude Code (D-05). Plans 361-03, 361-07.
5. DDR361-05: Every returned claim row is validated to the D-06 minimum `{claim, source_url, source_title, retrieved_at, quote_or_locator}` plus a named source type (D-13); a row missing any field or carrying a non-http(s) URL is dropped and counted, never hedged; a row carrying a score, confidence, strength, probability or rank key is dropped; a lane whose echoed queries differ from the approved ones is refused as `query_mismatch`. Plans 361-03, 361-06.
6. DDR361-06: After the navigator's approval, one evidence artifact per approved lane is written under `competitive-analysis/dominant-designs/`, including an explicit empty-lane artifact whose `## Searched, not found` list names what was searched (D-07); a dropped lane writes nothing and the analysis names it as not run. Plans 361-03, 361-06, 361-07.
7. DDR361-07: Each lane's sourced rows file through `navigation.fileEvidenceWithReadback` (one EvidenceClaim per unique URL per lane, a per-lane session suffix, the evidence tier assigned in code per D-13, landing `proposed`), and each lane's readback (landed, or not landed with the reason) is shown to the navigator; an absent room.db is said plainly. Plans 361-03, 361-06, 361-07.
8. DDR361-08: Larry's six-phase analysis artifact (the reference file's template) cites an evidence row id for every factual statement, states no score or dominance number that no evidence row states, and records `structure_source` and `structure_source_reason` (D-07, D-09). Plan 361-07.
9. DDR361-09: The Dominant Design structure is read from Theo (`framework_step`, `framework_techniques`, `case_story`) with ONLY `{framework: "Dominant Design"}` and falls back to the local reference on brain unavailable, egress blocked, not served (detected by the text `Tool X not found`, never by -32602 alone), shape refused, or served with zero steps, naming the source and the reason (D-09, D-15, D-16). Plans 361-05, 361-06, 361-08.
10. DDR361-10: `_proveKnownToolShape` gains three separate arms (`framework_step`, `framework_techniques`, `case_story`) after the `recommend_chain` arm, proving exact keys and a canonical framework handle; no 361 commit changes the `find_connections` arm; Theo input-shape parity is tested read-only against the Theo checkout (D-10, D-14, D-15). Plans 361-02, 361-08.
11. DDR361-11: Frontmatter truth: `connector.web_scope: white`, `Task` pre-approved with an adjacent pre-approval comment and a reviewed grant row in `data/subagent-dispatch-grants.json` (pending until the navigator ratifies), `teaching` names the research mode, `autonomous_safe` stays true; the command registry, connector registry, coverage ledger, skill mirror and orchestration projection regenerate and every `--check` is green; `scripts/build-command-registry.cjs`'s schema is unchanged (D-01, D-11, D-12). Plans 361-04, 361-07, 361-08.
12. DDR361-12: The `/mos:dominant-designs` command-registry row changes, so `registryHash` moves, and the phase records that the next real release's Step 5.6 `theo-resync` dispatch carries it; the phase sends no notify itself (D-01, D-11). Plans 361-07, 361-08.
13. DDR361-13: The quick pass behaves exactly as today: the Setup steps and the quick-pass flow text are byte-identical to the pre-phase command, and the quick pass calls no composer, no Theo tool and no agent. Plans 361-01, 361-07.
</requirements_rows>

<tasks>

<task type="auto">
  <name>Task 1: Mint DDR361-01..13 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines around `### Phase 360 - Room-bind picker` (about 3108-3172) for the block style, and `## Traceability` (about 3173-3280) for the family enumeration, the minted-sentence paragraph and the Caveat family list
    - .planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-CONTEXT.md (D-01..D-17)
  </read_first>
  <action>
    First run `git status --short -- .planning/REQUIREMENTS.md`; if it shows any diff, STOP and report (a peer is mid-edit; committing with --only would sweep their hunks into this commit). Re-read the file from disk right before editing (peers append to it).

    Insert a new block after the last BIND360 row of the Phase 360 block and before `## Traceability`: heading `### Phase 361 - Dominant-design research mode (DDR361 family)`, then a short intro paragraph stating that DDR361-01..13 were minted in the Phase 361 plan set (2026-09-23), ratifying `361-RESEARCH.md`'s proposed family as amended by the navigator rulings D-11..D-17 in `361-CONTEXT.md`, registered at plan time as `- [ ]` rows to be closed with measured proof, or left open with a stated reason, at phase close by `361-08-PLAN.md` Task 3. Then the thirteen rows exactly as listed in `<requirements_rows>` above, in order.

    In `## Traceability`: append `plus DDR361-01..13 (Phase 361)` to the family enumeration sentence (after the BIND360 item), append one minted-sentence paragraph for DDR361 after the BIND360 paragraph in the same style, add `DDR361` to the Caveat family list, and increase the stated active-requirements count by 13 at each place it is stated (read the current number from the file at execution time; if the two occurrences disagree, change neither and record that in the SUMMARY). Do not touch any other family's text.

    Commit immediately after the edit so the window with an uncommitted shared-file diff is short: `git add -f .planning/REQUIREMENTS.md` then `git commit --only -m "docs(361-01): mint DDR361-01..13" -- .planning/REQUIREMENTS.md`.
  </action>
  <verify>
    <automated>test "$(grep -c '^- \[ \] \*\*DDR361-' .planning/REQUIREMENTS.md)" -eq 13 && grep -q '^### Phase 361 - Dominant-design research mode (DDR361 family)' .planning/REQUIREMENTS.md && grep -q 'DDR361-01..13 (Phase 361)' .planning/REQUIREMENTS.md && ! grep -n "$(printf '\xe2\x80\x94')" .planning/REQUIREMENTS.md | grep -q DDR361</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*DDR361-' .planning/REQUIREMENTS.md` prints 13, and IDs 01 through 13 each appear exactly once as a row
    - The Phase 361 heading line number is greater than the last `**BIND360-11**` line and less than the `## Traceability` line
    - The Traceability enumeration contains `DDR361-01..13 (Phase 361)` and the Caveat list contains `DDR361`
    - `git diff PLAN_BASE -- .planning/REQUIREMENTS.md` shows only added lines plus the count and list edits in Traceability
  </acceptance_criteria>
  <done>Thirteen DDR361 rows registered and committed; no other family's text changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pre-phase baseline fixture, its test, and the phase aggregator</name>
  <files>tests/fixtures/361-pre-phase.json, tests/test-361-baseline.cjs, tests/run-all-361.sh</files>
  <read_first>
    - tests/run-all-356.sh (the whole file: run / run_if / counters / em-dash guard idiom; copy the shape, not the legs)
    - commands/dominant-designs.md (the whole file, 68 lines: find the `## Setup` section and the paragraph under `## Session Flow` starting "Then follow the framework phases")
    - lib/core/part8-egress-guard.cjs lines 454-494 (`_proveKnownToolShape`; the find_connections arm starts at the line containing `toolName.indexOf('find_connections') !== -1` and ends at the first following line that is exactly two spaces then `}`)
    - data/command-registry.json (grep `"/mos:dominant-designs"` for the row)
  </read_first>
  <behavior>
    - Test 1: the fixture's `base_sha` is a 40-hex commit and `git merge-base --is-ancestor <base_sha> HEAD` succeeds
    - Test 2: sha256 of `git show <base_sha>:data/command-registry.json` equals `command_registry_sha256`
    - Test 3: the `/mos:dominant-designs` row parsed from the base registry deep-equals `dominant_designs_row`
    - Test 4: the Setup section and the quick-pass paragraph extracted from `git show <base_sha>:commands/dominant-designs.md` equal `setup_section` and `quick_pass_paragraph`, and their sha256 values equal the stored digests
    - Test 5: the find_connections arm slice extracted from `git show <base_sha>:lib/core/part8-egress-guard.cjs` equals `find_connections_slice` and its sha256 equals `find_connections_slice_sha256`
    - Test 6: no network attempt (the fetch thrower never fired)
  </behavior>
  <action>
    Set `BASE=$(git rev-parse HEAD)` after Task 1's commit (Task 1's commit touches only REQUIREMENTS.md, so every pinned file is still pre-phase). Write `tests/fixtures/361-pre-phase.json` with keys: `base_sha`, `captured_at` (ISO date), `command_registry_sha256`, `dominant_designs_row` (the full row object from data/command-registry.json), `setup_section` (text from the `## Setup` line up to but excluding the `## Session Flow` line), `setup_section_sha256`, `quick_pass_paragraph` (the exact paragraph starting "Then follow the framework phases" through "slow down."), `quick_pass_paragraph_sha256`, `when_complete_lines` (the two lines "Create the artifact using the template from the reference file." and `Ask: "File this to competitive-analysis?" before writing.`), `find_connections_slice`, `find_connections_slice_sha256`, and `note` (one sentence: pinned before any 361 edit; later 361 tests read it). Generate the values with a throwaway node one-liner run from the shell, not by hand, and export the extraction helpers from the test file so later 361 tests reuse the same slice rules: `extractSetupSection(text)`, `extractQuickPassParagraph(text)`, `extractFindConnectionsSlice(text)`, `sha256(text)`, `loadFixture()` (guard with `require.main === module` so requiring the file runs no assertions).

    Write `tests/test-361-baseline.cjs` implementing the behavior legs above using `child_process.execFileSync('git', [...])` for `git show` and `git merge-base`. It prints one PASS/FAIL line per leg and exits 0/1.

    Write `tests/run-all-361.sh` modeled on tests/run-all-356.sh, with a header comment saying it is written ONCE in 361-01 and no later plan edits it. Guarded legs (`run_if`, in wave order): test-361-baseline.cjs (361-01), test-361-egress-shapes.cjs and test-361-theo-parity.cjs (361-02), test-361-lane-queries.cjs and test-361-evidence-pack.cjs (361-03), test-361-agent-contract.cjs (361-04), test-361-theo-structure.cjs (361-05), test-361-cli.cjs and test-361-filing.cjs (361-06), test-361-command-contract.cjs (361-07). Existing-suite legs (plain `run`): `node tests/test-260906-fda-known-tool-shapes.cjs`, `node lib/core/part8-egress-guard.test.cjs`, `node tests/test-209-declared-implies-wired.cjs`, `node tests/test-250-doctrine-fence.cjs`, `node tests/test-344-surface-layer-parity.cjs`, `node tests/test-148-engine-reaches.cjs`, `node tests/test-341-registry-drift.cjs`, `node tests/test-265-threshold-fanouts.cjs`, `node tests/test-265-swarm-task-grant.cjs`, `node tests/test-265-declaration-truth.cjs`, `node tests/test-fileval-readback.cjs`, `node scripts/check-layer-declaration.cjs`, `node scripts/check-render-coverage.cjs`. Generator legs (plain `run`): `node scripts/build-command-registry.cjs --check`, `node scripts/build-connector-registry.cjs --check`, `node scripts/build-skill-mirrors.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`. CIRS leg: `node scripts/check-cirs-declaration.cjs --check` over every `$PD/361-*-PLAN.md` (expand the glob into an array first; skip the leg with SKIPPED if the array is empty). Em-dash guard over: agents/dominant-design-researcher.md, commands/dominant-designs.md, skills/dominant-designs/SKILL.md, references/methodology/dominant-designs.md, lib/core/dominant-design/*.cjs, scripts/dominant-design-research.cjs, data/subagent-dispatch-grants.json, tests/run-all-361.sh, tests/fixtures/361-*, tests/test-361-*.cjs, and `$PD/361-*.md`; a missing file is skipped, never an error. Final line `PASSED=.. FAILED=.. SKIPPED=..`, exit 1 when FAILED > 0. `chmod +x tests/run-all-361.sh`.

    Commit: `git add tests/fixtures/361-pre-phase.json tests/test-361-baseline.cjs tests/run-all-361.sh` then `git commit --only -m "test(361-01): pre-phase baseline fixture and phase aggregator" -- tests/fixtures/361-pre-phase.json tests/test-361-baseline.cjs tests/run-all-361.sh`.
  </action>
  <verify>
    <automated>node tests/test-361-baseline.cjs && bash tests/run-all-361.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-361-baseline.cjs` exits 0 with six PASS legs
    - `bash tests/run-all-361.sh` exits 0; its output shows the baseline leg PASSED and every not-yet-landed 361 leg SKIPPED (missing ...); if an existing-suite leg fails, the executor classifies it (peer diff / pre-existing / new) in the SUMMARY and does not edit that suite
    - `grep -c 'run_if' tests/run-all-361.sh` is at least 10 (the function definition plus ten guarded legs)
    - `node -e "const f=require('./tests/fixtures/361-pre-phase.json');process.exit(/^[0-9a-f]{40}$/.test(f.base_sha)?0:1)"` exits 0
    - The fixture's `find_connections_slice` starts with the line containing `toolName.indexOf('find_connections') !== -1` and ends with a two-space `}` line
  </acceptance_criteria>
  <done>Baseline pinned at a pre-361 commit, proven by its own test; the aggregator exists and is green with later legs skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer session -> shared file | REQUIREMENTS.md is edited by several concurrent sessions |
| baseline fixture -> later tests | later 361 tests trust the fixture as the pre-phase truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-361-01 | Tampering | .planning/REQUIREMENTS.md | mitigate | `git status --short` check before edit, STOP on unowned diff, immediate `git commit --only` of that single path |
| T-361-02 | Repudiation | tests/fixtures/361-pre-phase.json | mitigate | test-361-baseline.cjs recomputes every digest from `git show <base_sha>` so a hand-edited fixture fails |
| T-361-03 | Information disclosure | test files | accept | fixtures hold only repo text (command body, registry row, guard source); no room or user data |
</threat_model>

<verification>
`node tests/test-361-baseline.cjs` and `bash tests/run-all-361.sh` both exit 0; `grep -c '^- \[ \] \*\*DDR361-' .planning/REQUIREMENTS.md` is 13.
</verification>

<success_criteria>
The DDR361 family exists, every later plan can measure against a pinned pre-phase commit, and the aggregator is in place and unedited by later plans.
</success_criteria>

## Artifacts this phase produces

This plan's share: `.planning/REQUIREMENTS.md` (DDR361 block), `tests/fixtures/361-pre-phase.json`, `tests/test-361-baseline.cjs`, `tests/run-all-361.sh`.

<output>
Create `.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-01-SUMMARY.md` when done (commit it with `git add -f` and `git commit --only`).
</output>
