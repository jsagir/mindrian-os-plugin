---
phase: 361-dominant-design-research-mode-mos-dominant-designs-gains-a-r
plan: 03
type: execute
wave: 2
depends_on: [361-01]
files_modified:
  - lib/core/dominant-design/lane-queries.cjs
  - tests/test-361-lane-queries.cjs
  - lib/core/dominant-design/evidence-pack.cjs
  - tests/test-361-evidence-pack.cjs
autonomous: true
requirements: [DDR361-02, DDR361-04, DDR361-05, DDR361-06, DDR361-07]
canon_parts: [5, 8, 12]
tags: [dominant-design, composer, part8, evidence, validator, pure-cjs]

must_haves:
  truths:
    - "composeLaneQueries({domain}) returns exactly four lanes in the fixed D-05 order, each with one audited query, or an honest local-only degrade envelope that never echoes the offending string"
    - "A venture proper noun, an email or a money figure in the domain fails the audit and yields {ok:false, degrade:'local-only', reason:'egress_violation', lane}; an empty domain yields reason 'empty_domain'; there is no send-anyway path"
    - "auditEditedQuery re-audits every navigator edit before it can reach the gate card, and refuses empty, multi-line or over-length strings"
    - "validateLaneResult keeps only rows carrying all five D-06 fields with an http(s) source_url, drops and counts the rest, drops any row carrying a score / confidence / strength / probability / rank key, refuses a lane whose echoed queries differ from the approved ones, and assigns stable row ids like E-VC-1"
    - "The evidence tier is assigned in code from the row's source type per D-13 (peer_reviewed and standards_body -> Academic; company_primary, regulatory_filing and market_data -> Operational; press, blog and other -> Practitioner), never chosen by the agent"
    - "An empty lane still renders a full artifact with zero rows and a `## Searched, not found` list naming what was searched (D-07)"
    - "toEvidenceClaimParams emits one EvidenceClaim param set per unique URL within a lane, with a per-lane session suffix, so two rows from one page never overwrite each other (Pitfall 3)"
    - "Both modules are pure: no fs, no network, no clock except an injectable now()"
  artifacts:
    - path: "lib/core/dominant-design/lane-queries.cjs"
      provides: "LANES, LANE_IDS, TAVILY_PARAMS, MAX_QUERIES_PER_LANE, composeLaneQueries, auditEditedQuery, domainSlug"
      exports: ["LANES", "LANE_IDS", "TAVILY_PARAMS", "MAX_QUERIES_PER_LANE", "composeLaneQueries", "auditEditedQuery", "domainSlug"]
      min_lines: 90
    - path: "lib/core/dominant-design/evidence-pack.cjs"
      provides: "SOURCE_TYPES, FORBIDDEN_ROW_KEYS, tierFor, validateLaneResult, renderLaneArtifact, toEvidenceClaimParams, laneArtifactName"
      exports: ["SOURCE_TYPES", "FORBIDDEN_ROW_KEYS", "tierFor", "validateLaneResult", "renderLaneArtifact", "toEvidenceClaimParams", "laneArtifactName"]
      min_lines: 180
    - path: "tests/test-361-lane-queries.cjs"
      provides: "composer and edit-audit legs"
      min_lines: 80
    - path: "tests/test-361-evidence-pack.cjs"
      provides: "validator, tier, render, params legs"
      min_lines: 150
  key_links:
    - from: "lib/core/dominant-design/lane-queries.cjs"
      to: "lib/core/rs-egress-prompts.cjs auditQueryString"
      via: "require and call on every composed or edited string"
      pattern: "auditQueryString"
    - from: "lib/core/dominant-design/evidence-pack.cjs"
      to: "lib/core/navigation/evidence-claim.cjs stripInjectionSpans"
      via: "require; applied to claim, title and quote text before rendering"
      pattern: "stripInjectionSpans"
---

<objective>
Build the two pure pieces research mode stands on: the lane-query composer (the ONLY source of outbound web strings) and the evidence-pack module (the D-06 validator, the D-13 tier map, the lane artifact renderer, and the filing-parameter builder).

Purpose: D-03/D-04 need one audited source of query strings; D-06 must be enforced by code, not by agent goodwill; D-07 needs an explicit empty-lane artifact; D-13 fixes the tiers. Pure modules are cheap to test exhaustively and are reused by the CLI in 361-06.
Output: `lib/core/dominant-design/lane-queries.cjs`, `lib/core/dominant-design/evidence-pack.cjs`, and their tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-CONTEXT.md
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-RESEARCH.md
@CLAUDE.md

Parallel-session rules and test hygiene: identical to 361-01-PLAN.md `<context>`.

Interfaces (read from source; do not re-derive):
- `auditQueryString(s, surface)` in lib/core/rs-egress-prompts.cjs (line 52): throws `ExternalEgressViolation` on a Canon FORBIDDEN_PATTERNS hit, returns otherwise.
- `composePatternQueries` in lib/core/eureka/online-pattern-query.cjs (lines 94-135): the envelope and fail-closed loop to copy in shape (`opts.auditFn` injection seam; name only the family, never the string, on a violation).
- `stripInjectionSpans(s)` exported from lib/core/navigation/evidence-claim.cjs (line 71): removes instruction, exfiltrate, email and URL spans from prose.
- `fileEvidenceWithReadback(db, params)` params shape (lib/core/navigation/file-evidence-readback.cjs lines 54-72): `{ topic, source, url, retrieved_at, evidence_tier, summary, sessionId, artifact_path?, informsTargetId? }`; `evidence_tier` in Academic | Operational | Practitioner | None; the node id is `EvidenceClaim:<sessionId>:<hash(url)>` (an UPSERT).

Decisions implemented here: D-03 and D-04 (composer is the only source of strings; edits re-audited), D-05 (four lanes, fixed order), D-06 (five-field minimum, drop not hedge, no scores), D-07 (empty lane artifact with a "Searched, not found" list), D-13 (tiers from source type). Claude's discretion used here and stated: lane templates, the per-lane query cap (2), the fixed Tavily parameters, the row cap (40 per lane), field names beyond D-06.

D-13 reading: the ruling says company primary sources and market data are Operational, and "peer-reviewed and standards-body sources rank higher; press and blogs rank lower". Canon Part 5 defines Academic as "peer-reviewed, replicated, citable to a primary source", so standards_body maps to Academic here, while regulatory filings (a company's own filed statements) stay Operational with company primaries. This is stricter than the research table, which put standards at Operational; the navigator's wording rules.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lane-query composer with the Part 8 audit fence</name>
  <files>lib/core/dominant-design/lane-queries.cjs, tests/test-361-lane-queries.cjs</files>
  <read_first>
    - lib/core/eureka/online-pattern-query.cjs lines 1-140 (header style, SURFACE constant, envelope, audit loop)
    - lib/core/rs-egress-prompts.cjs lines 1-110 (auditQueryString and what it throws)
  </read_first>
  <behavior>
    - composeLaneQueries({domain:'lithium-ion battery cells'}) -> ok true, lanes.length 4, ids in order variant_census, convergence_signals, s_curve_limits, discontinuity_signals, each lane has `label`, `question` (plain-words evidence question) and `queries` of length 1, `audited` 4, and `domain_slug` 'lithium-ion-battery-cells'
    - query text per lane is the domain followed by the fixed template words (below), single spaces, no newline
    - domain '   ' or missing -> {ok:false, degrade:'local-only', reason:'empty_domain'}
    - domain containing 'Acme Robotics', 'jane@example.com' or '$2M' -> {ok:false, degrade:'local-only', reason:'egress_violation', lane:'variant_census'} and JSON.stringify of the envelope does not contain the offending substring
    - domain with a CR or LF (checked on the raw input, before whitespace collapse), or over 80 characters after whitespace collapse -> {ok:false, degrade:'local-only', reason:'bad_domain'}
    - injected auditFn that throws on the third call -> reason 'egress_violation', lane 's_curve_limits'
    - auditEditedQuery('solid-state battery standards IEC') -> {ok:true, q}; '' -> reason 'empty_query'; a string with CR or LF -> 'bad_query'; over 200 chars -> 'too_long'; 'Acme Robotics battery' -> 'egress_violation' with no echo
    - TAVILY_PARAMS deep-equals {search_depth:'basic', topic:'general', max_results:10} and is frozen; MAX_QUERIES_PER_LANE === 2; LANES and each lane object are frozen
    - requiring the module performs no fs or network call (the fetch thrower never fires; the module source contains no require of fs, http, https or net)
  </behavior>
  <action>
    Create lib/core/dominant-design/lane-queries.cjs ('use strict', CJS, header comment naming Phase 361, D-03/D-04/D-05, Canon Part 8, and the no-em-dash house rule). `SURFACE = 'dominant-design'`. `LANES` is a frozen array of four frozen objects `{ id, abbrev, label, question, template }` where abbrev is VC, CS, SL, DS; labels are "Variant census", "Convergence signals", "S-curve limits", "Discontinuity signals"; questions restate D-05 in plain words (variant census: which competing designs exist in this domain, when each first appeared, and who backs it; convergence signals: which standards, market-share shifts, exits and consolidations show the market settling, with dates; s-curve limits: the physical, market and economic ceilings of the current dominant design; discontinuity signals: new entrants, substitutes, and patent or funding bursts that could break it); template word strings are exactly `competing designs history first introduced`, `industry standard adoption market share consolidation`, `performance limits diminishing returns cost ceiling`, `new entrants substitute technology patents funding`. Export `LANE_IDS` (frozen ids array), `TAVILY_PARAMS`, `MAX_QUERIES_PER_LANE = 2`, `MAX_QUERY_CHARS = 200`, `MAX_DOMAIN_CHARS = 80`.

    `domainSlug(domain)`: lowercase, non-alphanumerics to single hyphens, trimmed of hyphens, cut to 60 chars, never empty for a valid domain. `composeLaneQueries(input, opts)`: normalize (trim, collapse whitespace), validate, then for each lane build `domain + ' ' + template`, call `auditFn(q, SURFACE)` (default auditQueryString) inside try/catch, and on the first throw return the degrade envelope naming only the lane id. Success envelope: `{ ok:true, domain_slug, lanes:[{id, abbrev, label, question, queries:[q]}], audited, tavily_params: TAVILY_PARAMS, max_queries_per_lane: 2 }`. `auditEditedQuery(q, opts)` applies the same audit to one navigator-edited string with the length and newline checks. Write the test first (RED), then the module (GREEN). Commit both with `git add` + `git commit --only -m "feat(361-03): dominant-design lane-query composer with Part 8 audit" -- lib/core/dominant-design/lane-queries.cjs tests/test-361-lane-queries.cjs`.
  </action>
  <verify>
    <automated>node tests/test-361-lane-queries.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-361-lane-queries.cjs` exits 0 with a PASS line per behavior leg
    - `node -e "const m=require('./lib/core/dominant-design/lane-queries.cjs');const r=m.composeLaneQueries({domain:'Acme Robotics arms'});process.exit(r.ok===false&&r.degrade==='local-only'&&!JSON.stringify(r).includes('Acme')?0:1)"` exits 0
    - `grep -cE "require\(['\"](node:)?(fs|http|https|net)['\"]\)" lib/core/dominant-design/lane-queries.cjs` prints 0
  </acceptance_criteria>
  <done>Four audited lane queries or an honest local-only degrade; edits re-audited; constants frozen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evidence-pack validator, tier map, lane artifact renderer, filing params</name>
  <files>lib/core/dominant-design/evidence-pack.cjs, tests/test-361-evidence-pack.cjs</files>
  <read_first>
    - lib/core/navigation/evidence-claim.cjs lines 60-190 (stripInjectionSpans, writeEvidenceClaim validation, id derivation)
    - lib/core/navigation/file-evidence-readback.cjs lines 40-110 (param shape, what is required)
    - references/methodology/dominant-designs.md lines 116-130 (artifact frontmatter conventions: methodology, room_section)
    - lib/core/dominant-design/lane-queries.cjs (from Task 1: LANES, LANE_IDS for abbrev lookup)
  </read_first>
  <behavior>
    - SOURCE_TYPES is exactly [peer_reviewed, standards_body, company_primary, regulatory_filing, market_data, press, blog, other]; tierFor maps peer_reviewed and standards_body to Academic, company_primary, regulatory_filing and market_data to Operational, press, blog and other to Practitioner; an unknown type is coerced to other (Practitioner)
    - validateLaneResult(raw, {approvedQueries:[...]}) with 3 good rows, 1 row missing quote_or_locator, 1 with source_url 'ftp://x', 1 with source_url 'javascript:alert(1)', 1 carrying `confidence: 0.9`, 1 carrying `Score: 3` -> ok true, rows.length 3, counts {received:8, kept:3, dropped_unsourced:3, dropped_scored:2, dropped_over_cap:0}
    - kept rows carry only whitelisted keys {id, claim, source_url, source_title, retrieved_at, quote_or_locator, source_type, evidence_tier, stated_date, entities}; ids are E-VC-1..E-VC-3 in input order for variant_census
    - a row whose retrieved_at does not parse as a date is dropped as unsourced; stated_date is kept only when a non-empty string, else null; entities keeps at most 12 strings of at most 80 chars
    - more than 40 valid rows -> first 40 kept, dropped_over_cap counts the rest
    - raw.queries not byte-equal to approvedQueries (order-sensitive) -> {ok:false, reason:'query_mismatch', lane}; unknown lane id -> 'unknown_lane'; non-object or missing claims array -> 'bad_shape'
    - raw with zero claims and empty searched_not_found -> ok true, rows [], searched_not_found holds one entry per approved query of the form `searched "<query>", found no sourced evidence`
    - raw.error non-null is carried through as `error` with rows validated as usual
    - renderLaneArtifact(valid, {domain_slug, date, retrieved_at}) returns markdown with YAML frontmatter keys methodology dominant-designs, artifact_kind evidence-lane, lane, queries, searched_via, retrieved_at, claim_count, dropped_unsourced_count, dropped_scored_count, room_section competitive-analysis; a table with columns Id, Claim, Source, Type, Tier, Stated date, Quote or locator; a closing `## Searched, not found` section; an empty lane renders the table header plus a line `No sourced evidence found for this lane.` and the section list
    - when valid.error is non-null the frontmatter carries `error:` and the body states `Search did not run: <error>` above the `## Searched, not found` section (a lane that could not search is never presented as a lane that found nothing)
    - rendered cells have `|` escaped and newlines collapsed; claim, title and quote text pass through stripInjectionSpans; the source cell is a markdown link only when the URL is http(s) with no whitespace, `(`, `)` or `|`, else the bare title; output contains no em-dash
    - toEvidenceClaimParams(valid, {sessionId:'s1', artifact_path:'competitive-analysis/dominant-designs/x-evidence-variant_census.md'}) groups rows by source_url: 3 rows over 2 URLs -> 2 param sets; sessionId is 's1:dd-variant_census'; topic is `Dominant design evidence: <lane label>`; summary joins the grouped claims with their row ids; evidence_tier is the highest tier among the grouped rows (Academic > Operational > Practitioner); source is the source_title; url is the source_url; retrieved_at is the row value
    - laneArtifactName(domain_slug, date, lane) returns `<slug>-<date>-evidence-<lane>.md` and throws on a slug not matching ^[a-z0-9]+(-[a-z0-9]+)*$ or a date not matching YYYY-MM-DD
  </behavior>
  <action>
    Create lib/core/dominant-design/evidence-pack.cjs ('use strict', header naming D-06, D-07, D-13, Canon Parts 5, 8 and 12, the no-em-dash rule, and that the module is pure). `FORBIDDEN_ROW_KEYS` = score, confidence, strength, probability, rank, matched case-insensitively against every own key of a row (and any key containing `score` or `confidence`, so `dominance_strength` and `relevance_score` are caught: implement as a regex `/(score|confidence|strength|probability|rank)/i` over key names). Implement the behaviors above; `validateLaneResult` also accepts `opts.maxRows` (default 40). URL check: `new URL(s)` succeeds and protocol is `http:` or `https:`. Date check: `Number.isFinite(Date.parse(s))`. Tier order constant `TIER_RANK = {Academic:3, Operational:2, Practitioner:1, None:0}`. Rendering uses stripInjectionSpans from lib/core/navigation/evidence-claim.cjs (require it; it is a pure function). No fs, no network. Write the test first (RED), then the module (GREEN). Commit with `git add` + `git commit --only -m "feat(361-03): evidence-pack validator, D-13 tiers, lane artifact renderer" -- lib/core/dominant-design/evidence-pack.cjs tests/test-361-evidence-pack.cjs`.
  </action>
  <verify>
    <automated>node tests/test-361-evidence-pack.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-361-evidence-pack.cjs` exits 0 with a PASS line per behavior leg
    - `node -e "const e=require('./lib/core/dominant-design/evidence-pack.cjs');process.exit(e.tierFor('standards_body')==='Academic'&&e.tierFor('market_data')==='Operational'&&e.tierFor('blog')==='Practitioner'&&e.tierFor('nonsense')==='Practitioner'?0:1)"` exits 0
    - `grep -cE "require\(['\"](node:)?(fs|http|https|net)['\"]\)" lib/core/dominant-design/evidence-pack.cjs` prints 0
    - `bash tests/run-all-361.sh` shows the lane-queries and evidence-pack legs PASSED
  </acceptance_criteria>
  <done>D-06 is enforced in code, tiers come from code per D-13, empty lanes render honestly, and filing params cannot collide on a shared URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator text -> web | the domain phrase and edits leave the machine as search queries after approval |
| web content -> room | agent-returned claim rows carry untrusted third-party text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-361-09 | Information disclosure | composeLaneQueries / auditEditedQuery | mitigate | every string passes auditQueryString (Canon FORBIDDEN_PATTERNS); violation degrades local-only and the envelope never echoes the string |
| T-361-10 | Tampering | validateLaneResult | mitigate | five-field minimum, http(s) URL, parseable date, key whitelist, forbidden score-like keys dropped, row cap 40 |
| T-361-11 | Tampering | agent rephrasing a query | mitigate | `query_mismatch` refusal when echoed queries differ from the approved ones |
| T-361-12 | Elevation of privilege | prompt injection in quotes rendered to markdown | mitigate | stripInjectionSpans on claim, title and quote; `|` escaped; links only for clean http(s) URLs |
| T-361-13 | Tampering | EvidenceClaim UPSERT collisions | mitigate | one param set per unique URL per lane and a per-lane session suffix |
| T-361-14 | Repudiation | tier inflation | mitigate | tiers assigned in code from source_type per D-13; agent cannot set evidence_tier (not whitelisted from input) |
</threat_model>

<verification>
`node tests/test-361-lane-queries.cjs` and `node tests/test-361-evidence-pack.cjs` exit 0; both modules contain no fs or network require.
</verification>

<success_criteria>
The composer is the one audited source of outbound strings, and the evidence pack cannot carry an unsourced, scored, or agent-tiered row.
</success_criteria>

## Artifacts this phase produces

This plan's share: `lib/core/dominant-design/lane-queries.cjs`, `lib/core/dominant-design/evidence-pack.cjs`, `tests/test-361-lane-queries.cjs`, `tests/test-361-evidence-pack.cjs`.

<output>
Create `.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-03-SUMMARY.md` when done.
</output>
