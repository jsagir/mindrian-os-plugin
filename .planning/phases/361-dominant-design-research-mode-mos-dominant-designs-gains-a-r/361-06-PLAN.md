---
phase: 361-dominant-design-research-mode-mos-dominant-designs-gains-a-r
plan: 06
type: execute
wave: 3
depends_on: [361-03, 361-05]
files_modified:
  - scripts/dominant-design-research.cjs
  - tests/test-361-cli.cjs
  - tests/test-361-filing.cjs
autonomous: true
requirements: [DDR361-02, DDR361-05, DDR361-06, DDR361-07, DDR361-09]
canon_parts: [5, 8, 9, 11, 12]
tags: [cli, filing, evidence-claim, readback, room-db, ui-4-zone, dominant-design]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed:
    - navigation.openRoomDbForCaller
    - navigation.fileEvidenceWithReadback
    - navigation.surfaceFileEvidenceResult
    - navigation.closeRoomDbForCaller
  gate_impact: "none: the CLI is a door the command calls after its own gates; file-pack is only invoked after the navigator's File this to competitive-analysis? answer (D-08)"
  explanation: "Research found no CLI door to fileEvidenceWithReadback; this script is that door for research mode, opening room.db only through the allow-listed navigation chokepoint (Canon Part 9) and writing typed EvidenceClaim nodes that land proposed. It adds no invocable surface: it is a script called by commands/dominant-designs.md."

must_haves:
  truths:
    - "One CLI, scripts/dominant-design-research.cjs, is the only way the command composes queries, re-audits edits, reads the Theo structure, validates a lane and files the pack; it routes compose-queries | audit-query | theo-structure | validate-lane | file-pack with a process.argv switch and prints JSON (file-pack prints the 4-zone report unless --json)"
    - "compose-queries and audit-query read their input from a JSON file path, never from argv text, so navigator text never passes through a shell command line"
    - "file-pack writes one lane artifact per validated lane in the pack directory under <room>/competitive-analysis/dominant-designs/, including empty lanes, then files one EvidenceClaim per unique URL per lane through navigation.fileEvidenceWithReadback, and reports per lane landed or not landed with the reason (D-07)"
    - "An absent room.db still writes the lane artifacts, files nothing, and says so plainly (reason no_room_db); a validated file whose ok is false is reported and never rendered"
    - "Every artifact path is computed from a validated slug, date and lane id and must resolve inside the room directory; anything else is refused"
    - "The file-pack report follows the 4-zone CLI ruling: Zone 1 header, Zone 2 body table, no Zone 3 (methodology session), Zone 4 footer with one primary and one or two alternative real /mos: commands"
  artifacts:
    - path: "scripts/dominant-design-research.cjs"
      provides: "CLI router plus exported fileLane, filePack, renderFilingReport for tests"
      exports: ["fileLane", "filePack", "renderFilingReport", "main"]
      min_lines: 220
    - path: "tests/test-361-cli.cjs"
      provides: "router legs: compose, audit, validate, theo-structure offline, usage errors"
      min_lines: 110
    - path: "tests/test-361-filing.cjs"
      provides: "fixture-room legs for file-pack and fileLane"
      min_lines: 130
  key_links:
    - from: "scripts/dominant-design-research.cjs file-pack"
      to: "lib/core/navigation.cjs fileEvidenceWithReadback"
      via: "openRoomDbForCaller(roomDir) then one call per toEvidenceClaimParams entry, closeRoomDbForCaller in finally"
      pattern: "fileEvidenceWithReadback"
    - from: "scripts/dominant-design-research.cjs"
      to: "lib/core/dominant-design/{lane-queries,evidence-pack,theo-structure}.cjs"
      via: "require"
      pattern: "dominant-design/"
---

<objective>
Build the one CLI door research mode uses from the command body: compose and audit queries, read the Theo structure, validate a lane, and, after the navigator approves filing, write the lane artifacts and file the evidence with readback.

Purpose: 361-RESEARCH finding 1: there is no CLI door to `fileEvidenceWithReadback` today, so D-07's filing path needs this script. Keeping every deterministic step in one CLI keeps Larry from improvising query strings, row ids, tiers or paths.
Output: `scripts/dominant-design-research.cjs`, `tests/test-361-cli.cjs`, `tests/test-361-filing.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-CONTEXT.md
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-RESEARCH.md
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-03-SUMMARY.md
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-05-SUMMARY.md
@CLAUDE.md

Parallel-session rules and test hygiene: identical to 361-01-PLAN.md `<context>`. The theo-structure CLI leg in tests runs offline: the child gets HOME set to a mkdtemp dir (no Brain key) and the NODE_OPTIONS fetch-thrower preload, so brain-client returns null and the reference fallback is exercised; that leg asserts the fallback and does not fail on the blocked fetch attempt.

Interfaces (from 361-03 and 361-05, read their SUMMARYs and source; do not redefine):
- lane-queries.cjs: `composeLaneQueries({domain}, opts)`, `auditEditedQuery(q, opts)`, `domainSlug`, `LANE_IDS`, `TAVILY_PARAMS`, `MAX_QUERIES_PER_LANE`.
- evidence-pack.cjs: `validateLaneResult(raw, {approvedQueries})`, `renderLaneArtifact(valid, {domain_slug, date, retrieved_at})`, `toEvidenceClaimParams(valid, {sessionId, artifact_path})`, `laneArtifactName(slug, date, lane)`.
- theo-structure.cjs: `readDominantDesignStructure(opts)`.
- navigation.cjs: `openRoomDbForCaller(roomDir)` (null when `<roomDir>/.mindrian/room.db` is absent; never throws), `closeRoomDbForCaller(db)`, `fileEvidenceWithReadback(db, params)` (`{ok:true, node_id}` or `{ok:false, reason}`, never throws), `surfaceFileEvidenceResult(res)` (Larry-facing message).
- 4-zone CLI ruling: skills/ui-system/SKILL.md section 1 (Zone 1 `-- Room Name -- section -- Stage --`, room name first, `-- MindrianOS -- no room --` with no room; Zone 3 omitted in methodology sessions; Zone 4 never omitted, `>` primary exactly one plus 1-2 alternatives, real commands, never the command just run). Footer commands for this report, grounded in the reference's Cross-References: primary `/mos:find-bottlenecks`, alternatives `/mos:macro-trends` and `/mos:explore-trends` (all three exist under commands/).

CLI contract (the command body in 361-07 depends on these exact names and flags):
- `compose-queries <domain.json>`: domain.json is `{"domain": "<phrase>"}`; prints the composeLaneQueries envelope.
- `audit-query <query.json>`: query.json is `{"lane": "<id>", "q": "<string>"}`; prints `{ok, lane, q}` or `{ok:false, lane, reason}` (never echoes a refused string).
- `theo-structure [--reference <path>]`: prints the readDominantDesignStructure result.
- `validate-lane <raw.json> --approved <gate.json> [--out <path>]`: gate.json is `{"domain_slug": "...", "lanes": [{"id": "...", "queries": ["..."]}]}` (the approved lanes after edits and drops); looks up the raw lane's approved queries by its lane id (a lane id not in gate.json -> `{ok:false, reason:'lane_not_approved'}`); prints the validateLaneResult output and writes it to --out when given (--out must end in `.valid.json`).
- `file-pack --pack <dir> --room <roomDir> --domain-slug <slug> --date <YYYY-MM-DD> [--session <id>] [--room-label <text>] [--stage <text>] [--json]`: processes every `*.valid.json` in the pack dir in LANE_IDS order.
- Exit codes: 0 for any well-formed run (including honest degrades and not-landed filings, which are data), 2 for usage errors (unknown subcommand, missing file, bad flag values).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI router for compose-queries, audit-query, theo-structure, validate-lane</name>
  <files>scripts/dominant-design-research.cjs, tests/test-361-cli.cjs</files>
  <read_first>
    - scripts/analogy-fitness-report.cjs (the compose-queries subcommand and its process.argv switch router, as the house CLI precedent)
    - lib/core/dominant-design/lane-queries.cjs, evidence-pack.cjs, theo-structure.cjs (exports and envelopes)
    - 361-03-SUMMARY.md and 361-05-SUMMARY.md
  </read_first>
  <behavior>
    - compose-queries on {"domain":"lithium-ion battery cells"} -> exit 0, JSON ok true, 4 lanes, domain_slug lithium-ion-battery-cells
    - compose-queries on {"domain":"Acme Robotics arms"} -> exit 0, ok false, degrade local-only, stdout does not contain "Acme"
    - compose-queries with a missing file or invalid JSON -> exit 2 with a one-line usage message on stderr
    - audit-query {"lane":"variant_census","q":"solid-state battery standards IEC"} -> ok true; {"lane":"variant_census","q":"jane@example.com battery"} -> ok false, reason egress_violation, stdout does not contain "jane"; an unknown lane id -> ok false, reason unknown_lane
    - validate-lane with a raw lane whose queries match gate.json -> ok true and the --out file exists; with a mismatched query -> ok false, reason query_mismatch; raw lane id absent from gate.json -> ok false, reason lane_not_approved; --out not ending in .valid.json -> exit 2
    - theo-structure run offline (HOME=mkdtemp, fetch thrower preload) -> exit 0, JSON source reference, six steps, calls[*].args each deep-equal {framework:'Dominant Design'}
    - unknown subcommand or no args -> exit 2 and a usage line listing the five subcommands
    - the script source contains no `execSync`, `spawn`, or `child_process` require
  </behavior>
  <action>
    Create scripts/dominant-design-research.cjs ('use strict', shebang `#!/usr/bin/env node`, header naming Phase 361, the five subcommands, Canon Parts 8 and 9, D-03/D-04/D-07/D-08, and that navigator text is only ever read from JSON files). Router: `main(argv)` with a switch on argv[0]; a tiny flag parser that accepts `--name value` pairs and boolean `--json`; each handler returns `{code, out}` and main prints `JSON.stringify(out, null, 2)` (or text for the file-pack report) then `process.exitCode = code`. Export `main` plus the Task 2 functions, and run `main(process.argv.slice(2))` only under `require.main === module`. Implement compose-queries, audit-query, theo-structure (await the reader; print its result), and validate-lane per the CLI contract. Leave a `file-pack` case that Task 2 fills (until then it returns code 2 with `not implemented in this task`, replaced in Task 2 before the plan's final commit).

    Write tests/test-361-cli.cjs first (RED), spawning the script with `spawnSync(process.execPath, [script, ...args], {env})` against JSON files in a mkdtemp dir. Then implement (GREEN). Commit with `git add` + `git commit --only -m "feat(361-06): dominant-design-research CLI router" -- scripts/dominant-design-research.cjs tests/test-361-cli.cjs`.
  </action>
  <verify>
    <automated>node tests/test-361-cli.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-361-cli.cjs` exits 0 with a PASS line per behavior leg
    - `node scripts/dominant-design-research.cjs` exits 2 and stderr names compose-queries, audit-query, theo-structure, validate-lane, file-pack
    - `grep -c "child_process" scripts/dominant-design-research.cjs` prints 0
  </acceptance_criteria>
  <done>The command has one deterministic door for composing, auditing, reading structure and validating; navigator text never rides argv.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: file-pack: lane artifacts, EvidenceClaim filing with readback, 4-zone report</name>
  <files>scripts/dominant-design-research.cjs, tests/test-361-filing.cjs</files>
  <read_first>
    - lib/core/navigation/file-evidence-readback.cjs lines 40-240 (fileEvidenceWithReadback and surfaceFileEvidenceResult)
    - lib/core/navigation/spine-events.cjs lines 445-475 (openRoomDbForCaller / closeRoomDbForCaller)
    - tests/test-129.5-confirm-node.cjs lines 35-60 (seeding a real tmp room.db with lib/core/room-db.cjs openRoomDb, allowed in tests)
    - tests/test-fileval-readback.cjs lines 1-80 (readback assertions and how EvidenceClaim rows are read back)
    - skills/ui-system/SKILL.md lines 29-50 (4-zone anatomy)
  </read_first>
  <behavior>
    - fixture room (mkdtemp with a seeded .mindrian/room.db) + pack dir holding variant_census.valid.json (3 rows over 2 URLs, one Academic and one Practitioner row sharing a URL) and s_curve_limits.valid.json (0 rows, searched_not_found populated) -> file-pack --json exit 0; two artifacts exist at <room>/competitive-analysis/dominant-designs/<slug>-<date>-evidence-variant_census.md and ...-evidence-s_curve_limits.md; the empty one contains `## Searched, not found` and `No sourced evidence found for this lane.`
    - the room.db holds exactly 2 EvidenceClaim nodes for variant_census (one per URL) with session suffix `:dd-variant_census`, review status proposed, the grouped node's tier is Academic, and each node's artifact_path is the room-relative lane artifact path; the empty lane files 0 nodes
    - the JSON result per lane reports {lane, artifact_path, rows, filed, landed, not_landed:[{url, reason}], message} and the top level reports {ok, room_db:true}
    - the same pack filed twice with the same session does not create duplicate nodes (UPSERT) and still reports landed
    - room with no .mindrian/room.db -> artifacts still written, result {ok:false, reason:'no_room_db', filed:0}, report text says the lane files were written and no evidence nodes were filed because room.db was not found
    - a *.valid.json whose ok is false -> no artifact for that lane, reported as not run with its reason
    - --domain-slug '../evil', --date '2026-9-1', a --room that is not an existing directory, or a missing --pack -> exit 2 and nothing written
    - default session when --session is absent is `dominant-designs:<slug>:<date>`
    - without --json the output is the 4-zone text: first line matches /^-- .+ -- competitive-analysis -- .+ --$/ (room label from --room-label, else the room dir basename; stage from --stage, else `unknown stage`), a body table with one line per lane (lane label, kept, dropped, not found, filed or NOT LANDED), no signals zone, and a footer whose lines start with `>` naming /mos:find-bottlenecks first then /mos:macro-trends and /mos:explore-trends; the text contains no em-dash
    - nothing is ever written outside <room>/competitive-analysis/dominant-designs/ (the test lists every file under the mkdtemp root before and after)
  </behavior>
  <action>
    Implement in scripts/dominant-design-research.cjs: `fileLane(db, valid, {roomDir, slug, date, sessionId, retrievedAt})` renders the lane artifact with renderLaneArtifact, computes the path with laneArtifactName under `path.join(roomDir, 'competitive-analysis', 'dominant-designs')`, asserts `path.resolve(target).startsWith(path.resolve(roomDir) + path.sep)`, mkdirs recursively and writes the file (UTF-8), then when db is non-null calls `navigation.fileEvidenceWithReadback(db, p)` for each entry of `toEvidenceClaimParams(valid, {sessionId, artifact_path: <room-relative path with forward slashes>})` and collects `navigation.surfaceFileEvidenceResult(res)`; returns the per-lane result object. `filePack(opts)` validates flags (slug regex `^[a-z0-9]+(-[a-z0-9]+)*$` and at most 60 chars, date `^\d{4}-\d{2}-\d{2}$`, room and pack are existing directories), reads `*.valid.json` from the pack dir, orders by LANE_IDS, opens `navigation.openRoomDbForCaller(roomDir)` once, runs fileLane per lane inside try/finally with `closeRoomDbForCaller(db)`, and returns the pack result. `renderFilingReport(packResult, {roomLabel, stage, slug, date})` returns the 4-zone text (plain ASCII apart from the documented glyphs; no ANSI color, since output may be captured). Wire the `file-pack` case. Never import lib/core/room-db.cjs in the script (Part 9: navigation is the door; only the test seeds a db with room-db.cjs).

    Write tests/test-361-filing.cjs first (RED); read the filed nodes back through navigation's read helpers or a read-only `node:sqlite` DatabaseSync handle opened in the test on the tmp db (tests only). Then implement (GREEN) and re-run tests/test-361-cli.cjs. Commit with `git add` + `git commit --only -m "feat(361-06): file-pack writes lane artifacts and files EvidenceClaims with readback" -- scripts/dominant-design-research.cjs tests/test-361-filing.cjs`.
  </action>
  <verify>
    <automated>node tests/test-361-filing.cjs && node tests/test-361-cli.cjs && node tests/test-fileval-readback.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-361-filing.cjs` exits 0 with a PASS line per behavior leg
    - `grep -c "room-db.cjs" scripts/dominant-design-research.cjs` prints 0 and `grep -c "openRoomDbForCaller" scripts/dominant-design-research.cjs` prints at least 1
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/dominant-design-research.cjs` prints 0
    - `bash tests/run-all-361.sh` shows the cli and filing legs PASSED
  </acceptance_criteria>
  <done>After approval, one command writes every lane artifact (empty lanes included), files sourced evidence through the Part 9 door with readback, and prints an honest 4-zone report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Larry (command body) -> CLI | JSON files and flags composed by the model cross into a script that writes to disk |
| validated web evidence -> room.db | third-party text becomes local EvidenceClaim nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-361-25 | Tampering | file-pack paths | mitigate | slug and date regexes, lane id from LANE_IDS, resolved-path containment check under the room dir; exit 2 and nothing written on failure |
| T-361-26 | Elevation of privilege | shell injection via navigator text | mitigate | compose-queries and audit-query read JSON files only; no child_process in the script (asserted) |
| T-361-27 | Tampering | writing before approval | mitigate | file-pack is the only writing subcommand and the command body calls it only after the D-08 confirm card (pinned in 361-07's contract test) |
| T-361-28 | Repudiation | false "filed" claims | mitigate | per-lane readback via fileEvidenceWithReadback and surfaceFileEvidenceResult; not-landed rows reported with reasons; absent room.db said plainly |
| T-361-29 | Tampering | room.db access outside the chokepoint | mitigate | openRoomDbForCaller / closeRoomDbForCaller only; no room-db.cjs import (asserted); nodes land proposed (Part 9) |
| T-361-30 | Information disclosure | refused strings echoed | mitigate | audit-query and compose-queries never echo a refused string (asserted) |
</threat_model>

<verification>
`node tests/test-361-cli.cjs`, `node tests/test-361-filing.cjs`, `node tests/test-fileval-readback.cjs` exit 0; `bash tests/run-all-361.sh` green for all landed legs.
</verification>

<success_criteria>
Every deterministic step of research mode has one tested CLI door, filing goes only through the navigation chokepoint with readback, and the report follows the 4-zone ruling.
</success_criteria>

## Artifacts this phase produces

This plan's share: `scripts/dominant-design-research.cjs`, `tests/test-361-cli.cjs`, `tests/test-361-filing.cjs`.

<output>
Create `.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-06-SUMMARY.md` when done; include the exact CLI usage lines 361-07 will quote.
</output>
