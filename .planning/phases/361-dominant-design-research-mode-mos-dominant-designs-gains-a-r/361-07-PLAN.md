---
phase: 361-dominant-design-research-mode-mos-dominant-designs-gains-a-r
plan: 07
type: execute
wave: 4
depends_on: [361-02, 361-04, 361-06]
files_modified:
  - tests/test-361-command-contract.cjs
  - commands/dominant-designs.md
  - data/subagent-dispatch-grants.json
  - skills/dominant-designs/SKILL.md
  - data/command-registry.json
  - data/connector-registry.json
  - data/connector-coverage-ledger.json
  - data/brain-orchestration-projection.json
  - data/orchestration-command-ledger.json
autonomous: true
requirements: [DDR361-03, DDR361-04, DDR361-06, DDR361-07, DDR361-08, DDR361-11, DDR361-12, DDR361-13]
canon_parts: [3, 5, 7, 8, 9, 11, 12]
tags: [command, research-mode, gate-card, fan-out, registry, cirs, dominant-design]
cirs_relationship:
  surfaces_added: []
  surfaces_modified:
    - commands/dominant-designs.md
    - skills/dominant-designs/SKILL.md
  surfaces_removed: []
  spine_consumed:
    - navigation.fileEvidenceWithReadback
  gate_impact: "The existing F.1 Decision Gate is kept; the deep dive gains a query gate card (AskUserQuestion, at most 3 options) that must be answered before any agent is dispatched, plus the existing File this to competitive-analysis? confirm before anything is written. hitl_shape and hitl_why are unchanged."
  explanation: "Upgrades the existing /mos:dominant-designs in place per D-01 (no new command, no duplicate registry row). The quick pass is byte-preserved; the deep dive gains research mode, which dispatches the excluded dominant-design-researcher agent after the gate and files evidence through the navigation chokepoint via scripts/dominant-design-research.cjs. connector.web_scope becomes white per D-11; Task is pre-approved with a pending grant row that 361-08 ratifies."

must_haves:
  truths:
    - "The quick pass is exactly today's: the Setup section and the quick-pass paragraph are byte-identical to the pre-phase fixture, and the quick-pass subsection names no composer, agent, Theo tool or web tool (DDR361-13)"
    - "An unattended or chain-driven run takes the quick pass; the research path starts only from a navigator's answer to the gate card (D-12)"
    - "No agent is dispatched and nothing is fetched before the navigator approves the gate card; an audit failure is an honest local-only degrade with no send-anyway path (D-03, D-04)"
    - "Approved lanes fan out in parallel as dominant-design-researcher agents, at most 4, at most 2 approved queries per lane, fixed Tavily parameters, WebSearch fallback with the identical string, no tavily-extract; the Theo structure read runs alongside with only the generic handle (D-05, D-09, D-10)"
    - "Every lane result is validated by the CLI before Larry uses it; Larry runs the six phases on top of the rows, cites a row id for every factual statement, states no number no row states, and records structure_source and structure_source_reason (D-06, D-07, D-09)"
    - "Nothing is written to the room until the navigator answers File this to competitive-analysis?; then file-pack writes every lane artifact (empty lanes included) and files evidence with readback, and Larry writes the analysis artifact (D-07, D-08)"
    - "Frontmatter truth: connector.web_scope white, Task pre-approved with a pre-approval comment and a pending grant row, teaching names the research mode, autonomous_safe true, hitl_shape F.1, produces unchanged; every generated artifact regenerates and every --check is green; the registry row's key set is unchanged (D-01, D-11, D-12)"
    - "The /mos:dominant-designs command-registry row changes, so registryHash moves for the next release's theo-resync; the phase sends no notify (D-11)"
  artifacts:
    - path: "commands/dominant-designs.md"
      provides: "upgraded command with research mode"
      contains: "subagent_type: dominant-design-researcher"
      min_lines: 140
    - path: "tests/test-361-command-contract.cjs"
      provides: "quick-pass preservation, gate ordering, fan-out caps, filing order, frontmatter truth, registry movement"
      min_lines: 170
    - path: "data/subagent-dispatch-grants.json"
      provides: "pending Task grant row for commands/dominant-designs.md"
      contains: "commands/dominant-designs.md"
  key_links:
    - from: "commands/dominant-designs.md"
      to: "scripts/dominant-design-research.cjs"
      via: "Bash invocations of compose-queries, audit-query, theo-structure, validate-lane, file-pack"
      pattern: "dominant-design-research.cjs"
    - from: "commands/dominant-designs.md"
      to: "agents/dominant-design-researcher.md"
      via: "Agent tool subagent_type after the gate card"
      pattern: "subagent_type: dominant-design-researcher"
    - from: "commands/dominant-designs.md allowed-tools Task"
      to: "data/subagent-dispatch-grants.json"
      via: "grant row (tests/test-265-swarm-task-grant.cjs ARM 1-4)"
      pattern: "dominant-designs.md"
---

<objective>
Upgrade `/mos:dominant-designs` in place: the quick pass stays exactly as it is, and the deep dive gains a navigator-gated research mode that fans out the lane agents, validates their evidence, runs the six Utterback-Abernathy phases on top of the rows, and files the pack only after the navigator says so.

Purpose: D-01 (upgrade, no new command), D-03/D-04 (query gate, no silent spend), D-05 (four parallel lanes), D-06/D-07/D-08 (sourced rows, evidence pack plus Larry, file only on approval), D-09/D-10 (Theo structure with generic handle, degrade), D-11 (frontmatter-only registry change, web_scope white, pending grant), D-12 (unattended runs take the quick pass).
Output: the upgraded command, its contract test, the pending grant row, and every regenerated artifact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-CONTEXT.md
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-RESEARCH.md
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-04-SUMMARY.md
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-06-SUMMARY.md
@CLAUDE.md

Parallel-session rules and test hygiene: identical to 361-01-PLAN.md `<context>`.

Pre-state gate (run before Task 2 edits anything): `node scripts/build-skill-mirrors.cjs --check`, `node scripts/build-command-registry.cjs --check`, `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check` must all print OK, and `git status --short -- commands/dominant-designs.md skills/dominant-designs/SKILL.md data/` must be empty. If any is red or dirty, a peer has drift in flight: STOP and report (a regenerator write would sweep the peer's change into this plan's commit).

Known expected side effect, not to be "fixed": `node scripts/build-command-irreversibility-ledger.cjs --check` will print `WARN: STALE /mos:dominant-designs` after the teaching line changes (its text_hash covers teaching) and still exit 0 (never a release blocker by design). That ledger is Phase 356's, scored by Jev; do NOT rebuild it. Record the WARN in the SUMMARY as carried to 356's next rescore. Likewise do not touch dist/ (bundles are regenerated by a separate dist chore) and do not touch data/jev-labels/*.

Command-body anchors the contract test pins (use these exact strings in the body):
- A1 `Ask: "Quick pass or deep dive?"` (existing line, kept once)
- A2 `When this command runs without a navigator present (inside `/mos:act`, `chain_run`, or any caller that cannot answer the gate card), take the quick pass. The research path starts only from a navigator's answer to the gate card.`
- A3 `There is no send-anyway path.`
- A4 `Do not fetch until they approve.`
- A5 `No agent is dispatched before the navigator approves the research pass.`
- A6 `[DOMINANT-DESIGN] Dispatching N lane agents` (status block, after A4 and A5)
- A7 `subagent_type: dominant-design-researcher`
- A8 `File this to competitive-analysis?` (existing line, kept; the deep-dive filing step repeats the confirm before `file-pack`)
- A9 the literal theo-structure invocation line `node "${CLAUDE_PLUGIN_ROOT}/scripts/dominant-design-research.cjs" theo-structure` with no other argument
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Command contract test (RED)</name>
  <files>tests/test-361-command-contract.cjs</files>
  <read_first>
    - commands/dominant-designs.md (whole file, pre-phase)
    - tests/fixtures/361-pre-phase.json and tests/test-361-baseline.cjs (reuse extractSetupSection, extractQuickPassParagraph, sha256, loadFixture by require)
    - tests/test-265-threshold-fanouts.cjs lines 130-155 (the indexOf ordering pin for find-analogies, the model for A4/A5 before A6)
    - tests/test-265-swarm-task-grant.cjs lines 120-170 and 247-296 (ARM 1-4 rules the grant row must satisfy)
    - 361-06-SUMMARY.md (exact CLI usage lines)
  </read_first>
  <behavior>
    - frontmatter: connector.web_scope is white; allowed-tools contains Task; the frontmatter contains the word pre-approval; autonomous_safe is true; hitl_shape is "F.1" and hitl_why equals the base text; produces equals "room/**/dominant-designs/*"; frameworks, sensor_triggers, filing, reach_id and sub_mode equal the base values; teaching differs from the base row's teaching and matches /research/i
    - DDR361-13: the Setup section equals the fixture's setup_section byte-for-byte; the fixture's quick_pass_paragraph occurs exactly once and lies between the `### Quick pass` heading and the next `### ` heading; that subsection contains none of: dominant-design-research.cjs, subagent_type, framework_step, tavily, WebSearch, Task; A1 occurs exactly once; both fixture when_complete_lines are present
    - D-12: A2 is present verbatim and its index is less than the index of the `### Deep dive` heading
    - D-03/D-04: A3, A4 and A5 are present; index(A4) < index(A6) and index(A5) < index(A6); the body names compose-queries and audit-query and says a navigator edit goes back through audit-query before the card re-renders
    - D-05 caps: A7 present; body contains `resolveFanoutCap`, `at most 4`, `at most 2 approved queries per lane`, `search_depth: "basic"`, `topic: "general"`, `max_results: 10`, `identical string`, and `tavily-extract` appears only in a sentence that forbids it (the line contains `never` or `no`)
    - D-09/D-10: A9 present exactly; no line containing `theo-structure` contains `domain`
    - D-06/D-07/D-08: body contains validate-lane, `query_mismatch`, `structure_source`, `structure_source_reason`, `evidence_pack`, `depth: deep`, a row-id citation example matching /\[E-(VC|CS|SL|DS)-\d+\]/, and a rule that no score or dominance number appears unless an evidence row states it; in the deep-dive filing section index(A8) < index of `file-pack --pack`
    - Tri-Polar: a `## Tri-Polar` section names Claude Desktop and Cowork and says the sourced research pass runs in Claude Code
    - grant: data/subagent-dispatch-grants.json has exactly one row with command commands/dominant-designs.md, token Task, status pending or granted, reviewed_by navigator, and non-empty dispatch_shape, fan_bound, reason, reviewed_date (YYYY-MM-DD), evidence
    - registry (DDR361-11, DDR361-12): the /mos:dominant-designs row in data/command-registry.json has the same key set as the fixture's dominant_designs_row, autonomous_safe true, a teaching value different from the fixture's, and the file's sha256 differs from the fixture's command_registry_sha256
    - hygiene: no em-dash or en-dash in commands/dominant-designs.md; no match for /silent fallback/i
  </behavior>
  <action>
    Write tests/test-361-command-contract.cjs implementing every behavior leg (frontmatter parsed with the same small YAML-subset reader style as tests/test-361-agent-contract.cjs; copy the helper rather than importing it so tests stay independent). Print one PASS/FAIL line per leg. Run it and confirm it FAILS on the pre-phase command (RED). Commit the RED test: `git add tests/test-361-command-contract.cjs` then `git commit --only -m "test(361-07): failing command contract for research mode" -- tests/test-361-command-contract.cjs`.
  </action>
  <verify>
    <automated>node tests/test-361-command-contract.cjs; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - The test exits 1 on the pre-phase command, and its output shows the DDR361-13 Setup leg PASS (the Setup section is not yet changed) while the research-mode legs FAIL
    - The file contains a leg for every behavior bullet (count PASS/FAIL labels in the source: at least 14 distinct leg names)
  </acceptance_criteria>
  <done>The research-mode contract is pinned before the command changes.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade the command body and frontmatter, and add the pending grant row</name>
  <files>commands/dominant-designs.md, data/subagent-dispatch-grants.json</files>
  <read_first>
    - commands/dominant-designs.md (whole file)
    - commands/find-analogies.md lines 1-70 (the Task pre-approval comment block and connector web_scope comment) and lines 180-300 (PHASE 0 / PHASE 1 / PHASE 2 structure and wording to copy in shape)
    - references/methodology/dominant-designs.md lines 53-212 (six phases, artifact template, Quick Pass vs Deep Dive incl. 361-05's Research mode bullet)
    - agents/dominant-design-researcher.md (receives/returns contract, from 361-04)
    - 361-06-SUMMARY.md (CLI usage lines, file-pack flags and report)
    - data/subagent-dispatch-grants.json (the find-analogies row as the shape)
    - skills/ui-system/SKILL.md lines 29-50 (4-zone anatomy; Zone 3 omitted in methodology sessions)
  </read_first>
  <action>
    Run the pre-state gate from the context first; STOP if it fails.

    Frontmatter (per D-11, D-12): keep every existing key and value except: (1) `teaching` becomes `When you are wondering if the market has settled on a winning design, /mos:dominant-designs runs Utterback-Abernathy to spot it. Tells you whether to ride the wave or break it. The deep dive can open with a sourced research pass: four evidence lanes, searched only after you approve the queries, every claim with its source.`; (2) add `  - Task` as the last allowed-tools entry, with a comment block directly above `allowed-tools:` in the find-analogies style stating: Phase 361 (D-11) data/subagent-dispatch-grants.json carries a reviewed "pending" row for commands/dominant-designs.md (reviewed_date 2026-09-23) and ratification to "granted" is plan 361-08's checkpoint; Task is added as a pre-approval because the deep dive's research mode fans out one dominant-design-researcher per navigator-approved evidence lane strictly after the query gate card; allowed-tools is a pre-approval list, not a restriction list, so this removes the per-spawn permission prompt rather than granting a capability the command lacked; (3) `web_scope: null` becomes `web_scope: white` with a two-line comment above it: Phase 361 (D-11, Canon Part 2: White = Tavily + arxiv for research), research mode's lane agents search the web only with navigator-approved queries. `autonomous_safe: true`, `hitl_shape`, `hitl_why`, `produces`, `frameworks`, and the rest stay byte-identical.

    Body: keep the firing block, the title, the intro line and `## Setup` byte-identical. Replace `## Session Flow` with: A1 line unchanged; then a bold rule paragraph `**Unattended runs take the quick pass.**` followed by A2 verbatim; then `### Quick pass` containing the original paragraph byte-identical ("Then follow the framework phases ... slow down."); then `### Deep dive` containing:
    (a) opening paragraph: run Phase 1 (Domain Selection) from the reference first; ask the navigator to name the domain generically (the domain, not the venture: no company names, people, figures); then offer research mode.
    (b) `#### Research mode, PHASE 0 (sequential: compose, then the gate card)`: create a scratch dir outside the room with Bash, `mkdir -p "${TMPDIR:-/tmp}/mos-dd-$(date +%Y%m%d-%H%M%S)"`, and Write `{"domain": "<phrase>"}` to `<scratch>/domain.json`; run `node "${CLAUDE_PLUGIN_ROOT}/scripts/dominant-design-research.cjs" compose-queries <scratch>/domain.json`; state that the composer is the ONLY source of outbound query strings and never to hand-write a query. On `{ok:false, degrade:'local-only'}` say plainly why (for example "that phrase names a company, so I will not send it; name the domain instead") and continue the deep dive from what the navigator knows, then A3. On `{ok:true}` fire the gate card with AskUserQuestion: the question lists the four lanes (label, the one-line evidence question, the exact query), and the options are "Run these lanes (Recommended)", "Edit or drop a lane", "Deep dive without web research". An edit or an added second query for a lane (at most 2 approved queries per lane) is written to `<scratch>/query.json` as `{"lane": "<id>", "q": "<string>"}` and goes back through `audit-query` before the card re-renders; a refused edit is reported and never sent; a dropped lane leaves the fan-out and the pack. A4. On approval Write the approved lanes to `<scratch>/gate.json` as `{"domain_slug": ..., "lanes": [{"id": ..., "queries": [...]}]}`. Then A5 as its own bold line.
    (c) `#### Research mode, PHASE 1 (parallel: one agent per approved lane, plus the Theo structure read)`: print the A6 status block (with `Approved lanes: {n}` and the lane labels), then in the same turn dispatch with the Agent tool `subagent_type: dominant-design-researcher` once per approved lane (at most 4, clamped through `lib/core/futures/orchestrator.cjs` `resolveFanoutCap`), no manual background parameter; if the Agent tool rejects the bare name, retry with the scoped name its error lists (for example `mos:dominant-design-researcher`). Each dispatch prompt carries only: the lane id, the approved query string(s) labeled LITERAL, the lane's evidence question, the fixed parameters `search_depth: "basic"`, `topic: "general"`, `max_results: 10` with exactly one `tavily-search` call per approved query and `WebSearch` with the identical string only on a Tavily error or absence, and the rule that the agent never uses tavily-extract; never room content, venture context, or STATE.md text. Worst case 8 searches per run, normal case 4. In the same turn run A9 via Bash; it sends Theo only the generic handle "Dominant Design", and its JSON says whether the structure came from Theo or the reference and why.
    (d) `#### Research mode, PHASE 2 (sequential: validate, then Larry)`: Write each agent's returned JSON to `<scratch>/<lane>.raw.json` and run `validate-lane <scratch>/<lane>.raw.json --approved <scratch>/gate.json --out <scratch>/<lane>.valid.json`; a `query_mismatch` means the agent changed the approved query, so its rows are discarded and the lane is reported as not run; a lane whose search did not run is reported as such. Show the navigator a compact evidence summary (lane, rows kept, dropped unsourced, searched but not found). Then run the six phases on top of the rows, using the structure from the theo-structure result (Theo steps when source is theo, otherwise the reference's six phases); every factual statement ends with its row id, for example `[E-VC-3]`; a statement with no row is Larry's reasoning and is phrased as a question or labeled as opinion; no score, dominance strength number or confidence appears unless an evidence row states it (Canon Part 12: sourced or absent); worked cases from Theo, when served, are teaching examples, never evidence rows.
    (e) `#### Research mode, filing (only after the navigator says yes)`: ask A8 first; on yes run `node "${CLAUDE_PLUGIN_ROOT}/scripts/dominant-design-research.cjs" file-pack --pack <scratch> --room <room dir> --domain-slug <slug> --date <YYYY-MM-DD> --room-label "<room name>" --stage "<stage>"` which writes one evidence artifact per approved lane under `<room>/competitive-analysis/dominant-designs/` (an empty lane still gets its artifact with its `## Searched, not found` list), files each lane's sourced rows through `navigation.fileEvidenceWithReadback`, and prints the 4-zone filing report; show that report as-is, including any lane marked NOT LANDED and a missing room.db. Then Write the analysis artifact to `<room>/competitive-analysis/dominant-designs/<slug>-<YYYY-MM-DD>.md` using the reference template with frontmatter additions `depth: deep`, `evidence_pack: [<lane artifact paths>]`, `structure_source: theo | reference`, `structure_source_reason: <reason>`, `lanes_not_run: [<ids>]`, and the Discontinuities table's Evidence column holding row ids. On no, nothing is written to the room (the scratch dir is outside it).
    (f) a short paragraph for "Deep dive without web research": the six phases from what the navigator knows, as before.
    Keep `## When Complete` with the two original lines byte-identical (they govern the quick pass and the no-web deep dive) and the original cross-methodology suggestion line; add one line pointing research mode to its own filing step above. Add `## Tri-Polar`: a three-row table (Claude Code CLI: full research mode; Claude Desktop: no Agent tool, so Larry runs the deep dive from what the navigator knows and says the sourced research pass runs in Claude Code; Cowork: same as Desktop for searching, and lane artifacts filed by a CLI run are shared room state). Hyphens only; never write the phrase "silent fallback".

    Grant row (D-11): append to the `grants` array of data/subagent-dispatch-grants.json, preserving the file's formatting (edit with a node script that parses, pushes, and re-serializes with the same indentation, then confirm `git diff` shows only the added object): command `commands/dominant-designs.md`, token `Task`, status `pending`, dispatch_shape `one dominant-design-researcher subagent per navigator-approved evidence lane (variant census, convergence signals, S-curve limits, discontinuity signals), spawned in one turn strictly after the query gate card`, fan_bound `4, one per evidence lane, clamped by resolveFanoutCap / FUTURES_FANOUT_CAP default 5`, reason (why a parallel grant: four independent lane searches would otherwise raise up to four permission prompts per deep dive, and each worker is read-only by its tools list), reviewed_by `navigator`, reviewed_date `2026-09-23`, evidence `361-CONTEXT.md D-05 and D-11; 361-RESEARCH.md Pitfall 7; 361-07-PLAN.md Task 2`. No ratified_date or ratified_in yet.

    Run `node tests/test-361-command-contract.cjs`: every leg except the two registry legs must pass now (the registry regenerates in Task 3). Do not commit yet; Task 3 commits the command, the grant row and the regenerated files together so the tree is never half-regenerated at a commit.
  </action>
  <verify>
    <automated>node tests/test-361-command-contract.cjs 2>&1 | grep -c '^FAIL' | grep -qE '^[0-2]$' && node tests/test-265-swarm-task-grant.cjs && node tests/test-265-declaration-truth.cjs && node tests/test-250-doctrine-fence.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-361-command-contract.cjs` fails only on the registry legs (at most 2 FAIL lines, both naming the registry)
    - `node tests/test-265-swarm-task-grant.cjs` exits 0 and prints `unratified: commands/dominant-designs.md`
    - `node tests/test-265-declaration-truth.cjs` and `node tests/test-250-doctrine-fence.cjs` exit 0
    - `git diff -- data/subagent-dispatch-grants.json | grep -c '^-[^-]'` prints 0 (pure addition)
  </acceptance_criteria>
  <done>The command carries research mode behind the gate with the quick pass untouched, and the pending grant row exists.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate every generated artifact, prove the registry moved, commit together</name>
  <files>skills/dominant-designs/SKILL.md, data/command-registry.json, data/connector-registry.json, data/connector-coverage-ledger.json, data/brain-orchestration-projection.json, data/orchestration-command-ledger.json</files>
  <read_first>
    - scripts/build-skill-mirrors.cjs lines 18-60 and 130-150 (write mode rewrites every mirror; --check first)
    - scripts/build-command-registry.cjs lines 1-40 (generated_note, --check)
    - docs/THEO-NOTIFY-CONTRACT.md (Rulings 1-3: registryHash is computed at release Step 5.6; --dry-run never sends)
  </read_first>
  <action>
    Record `PRE_HASH=$(sha256sum data/command-registry.json | cut -d' ' -f1)`. Run the generators in write mode, one at a time, checking `git status --short` after each: `node scripts/build-skill-mirrors.cjs` (expected: only skills/dominant-designs/SKILL.md changes), `node scripts/build-command-registry.cjs`, `node scripts/build-connector-registry.cjs`, `node scripts/build-orchestration-projection.cjs`. Every changed hunk must concern /mos:dominant-designs, commands/dominant-designs.md or skills/dominant-designs, or be a derived count or hash; if a hunk concerns another surface, restore only that generated file with `git checkout -- <file>` (it was clean at the pre-state gate, so this reverts only this plan's regeneration), STOP, and report the peer drift. If `node scripts/build-harness-manifest.cjs --check` or `node scripts/check-render-coverage.cjs` report drift caused by this change, regenerate with their write mode under the same rule and add the file to the commit and the SUMMARY; a listed generated file that did not change is simply not staged.

    Then run every gate: `node tests/test-361-command-contract.cjs` (all legs PASS now), `node scripts/build-skill-mirrors.cjs --check`, `node scripts/build-command-registry.cjs --check`, `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs`, `node scripts/check-layer-declaration.cjs`, `node tests/test-209-declared-implies-wired.cjs`, `node tests/test-265-swarm-task-grant.cjs`, `node tests/test-265-declaration-truth.cjs`, `node tests/test-265-threshold-fanouts.cjs`, `node tests/test-148-engine-reaches.cjs`, `node tests/test-341-registry-drift.cjs`, `node tests/test-344-surface-layer-parity.cjs`, `node scripts/check-cirs-declaration.cjs --check` over the 361 plans, and `bash tests/run-all-361.sh`. Run `node scripts/build-command-irreversibility-ledger.cjs --check` and record its expected `WARN: STALE /mos:dominant-designs` (exit 0) without rebuilding. Record `POST_HASH` and assert it differs from `PRE_HASH` (DDR361-12); write both hashes in the SUMMARY with the sentence: the registry change rides the next real release's Step 5.6 theo-resync dispatch; this phase sends no notify.

    Re-run the four generator `--check`s immediately before committing. Commit everything from Tasks 2 and 3 in one commit: `git add commands/dominant-designs.md data/subagent-dispatch-grants.json <each changed generated file>` then `git commit --only -m "feat(361-07): /mos:dominant-designs research mode behind the query gate" -- commands/dominant-designs.md data/subagent-dispatch-grants.json <each changed generated file>`.
  </action>
  <verify>
    <automated>node tests/test-361-command-contract.cjs && node scripts/build-skill-mirrors.cjs --check && node scripts/build-command-registry.cjs --check && node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check && node tests/test-265-swarm-task-grant.cjs && bash tests/run-all-361.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-361-command-contract.cjs` exits 0 with every leg PASS
    - All four generator `--check` commands print OK; `node scripts/check-render-coverage.cjs` and `node scripts/check-layer-declaration.cjs` exit 0
    - `bash tests/run-all-361.sh` exits 0 with every 361 leg PASSED (theo-parity may be SKIPPED only for a missing checkout)
    - `node -e "const r=require('./data/command-registry.json');const row=(r.commands||[]).find(c=>c.command==='/mos:dominant-designs');process.exit(row&&row.autonomous_safe===true&&/research/i.test(row.teaching)?0:1)"` exits 0
    - `grep -A3 '"/mos:dominant-designs"' data/connector-registry.json` or the registry's equivalent entry shows web_scope white (inspect the entry; record the line in the SUMMARY)
    - The SUMMARY records PRE_HASH, POST_HASH (different), and the STALE WARN from the irreversibility ledger check
  </acceptance_criteria>
  <done>The command, grant row and every generated artifact land in one commit with all gates green and the registry hash moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator -> web | the approved domain queries leave the machine (the navigator's explicit spend, D-10) |
| Larry -> subagents | dispatch prompts cross into parallel workers |
| subagents -> room | returned evidence becomes room artifacts and room.db nodes |
| plugin -> Theo | the structure read crosses to the Brain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-361-31 | Repudiation / DoS (cost) | silent web spend | mitigate | gate card before any dispatch (A4/A5 before A6, pinned by index); unattended rule A2; caps: 4 agents, 2 queries per lane, 1 call per query, no tavily-extract |
| T-361-32 | Information disclosure | venture text in queries | mitigate | compose-queries and audit-query (Canon FORBIDDEN_PATTERNS) with no send-anyway path; body asks for the domain, not the venture |
| T-361-33 | Information disclosure | room content to Theo | mitigate | A9 has no argument (pinned); theo-structure sends only the HANDLE constant; 361-02 arms classify it |
| T-361-34 | Information disclosure | room content to subagents | mitigate | dispatch prompt carries only lane id, literal queries, lane question, parameters and return shape |
| T-361-35 | Tampering | writes before approval | mitigate | scratch dir outside the room; A8 precedes file-pack (pinned by index); no-answer path writes nothing |
| T-361-36 | Tampering | unsourced or scored claims in the analysis | mitigate | validate-lane before use; row-id citation rule; no-number-unless-sourced rule (Canon Part 12) |
| T-361-37 | Elevation of privilege | Task grant widening | mitigate | reviewed pending grant row plus pre-approval comment (ARM 1-4); ratification is a navigator checkpoint in 361-08 |
| T-361-38 | Tampering | peer drift swept into regenerated files | mitigate | pre-state gate, per-generator hunk inspection, re-check before commit, STOP on foreign hunks |
</threat_model>

<verification>
`node tests/test-361-command-contract.cjs` and `bash tests/run-all-361.sh` exit 0; all generator `--check`s, the grant, declaration-truth, threshold-fanout, engine-reach, registry-drift and surface-layer tests pass; the registry hash moved.
</verification>

<success_criteria>
`/mos:dominant-designs` offers a gated, sourced research deep dive while its quick pass is byte-identical to before, and every registry and ledger agrees with its frontmatter.
</success_criteria>

## Artifacts this phase produces

This plan's share: upgraded `commands/dominant-designs.md`, `tests/test-361-command-contract.cjs`, the pending grant row in `data/subagent-dispatch-grants.json`, regenerated `skills/dominant-designs/SKILL.md`, `data/command-registry.json`, `data/connector-registry.json`, `data/connector-coverage-ledger.json`, `data/brain-orchestration-projection.json`, `data/orchestration-command-ledger.json` (each only if it changed).

<output>
Create `.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-07-SUMMARY.md` when done; include PRE_HASH, POST_HASH, the list of regenerated files, and the irreversibility-ledger STALE WARN under "Carried forward".
</output>
