---
phase: 361-dominant-design-research-mode-mos-dominant-designs-gains-a-r
plan: 05
type: execute
wave: 2
depends_on: [361-01]
files_modified:
  - lib/core/dominant-design/theo-structure.cjs
  - tests/test-361-theo-structure.cjs
  - references/methodology/dominant-designs.md
autonomous: true
requirements: [DDR361-09, DDR361-04]
canon_parts: [8, 12]
tags: [theo, degrade, part8, reference, tri-polar, dominant-design]

must_haves:
  truths:
    - "readDominantDesignStructure calls framework_step, framework_techniques and case_story with exactly {framework: 'Dominant Design'} and nothing else, whatever the caller passes (D-10)"
    - "Theo becomes the structure source only when framework_step returns at least one step; otherwise the six phases parsed from references/methodology/dominant-designs.md are the structure and the result names why (D-09, D-16)"
    - "Each call is classified: brain_unavailable (null), egress_blocked, not_served (only when the text matches `Tool <name> not found`), shape_refused (any other -32602), refused, call_threw, no_steps_in_canon, or served; -32602 alone never means not served (D-16)"
    - "The result records per call whether the response carried an egress_disclosure, so the live probe in 361-08 can prove 361-02's arms took effect"
    - "The function never throws; with no brainClient reachable it returns the reference structure and the reason"
    - "The reference file gains a Research mode note (Tri-Polar: Desktop and Cowork run the deep dive from what the navigator knows and say plainly that the sourced research pass runs in Claude Code) without changing the six `### Phase N:` headings"
  artifacts:
    - path: "lib/core/dominant-design/theo-structure.cjs"
      provides: "HANDLE, classifyCallResult, parseReferencePhases, readDominantDesignStructure"
      exports: ["HANDLE", "classifyCallResult", "parseReferencePhases", "readDominantDesignStructure"]
      min_lines: 110
    - path: "tests/test-361-theo-structure.cjs"
      provides: "injected-brainClient legs for every classification and the generic-handle-only assertion"
      min_lines: 130
    - path: "references/methodology/dominant-designs.md"
      provides: "Research mode bullet under Quick Pass vs Deep Dive"
      contains: "Research mode"
  key_links:
    - from: "lib/core/dominant-design/theo-structure.cjs"
      to: "lib/core/brain-client.cjs callTool"
      via: "the single Theo wire door (default when opts.brainClient is absent)"
      pattern: "callTool"
    - from: "lib/core/dominant-design/theo-structure.cjs"
      to: "references/methodology/dominant-designs.md"
      via: "parseReferencePhases fallback structure"
      pattern: "dominant-designs.md"
---

<objective>
Read the Dominant Design framework structure from Theo when it is genuinely served, and fall back to the local reference otherwise, always saying which source was used and why.

Purpose: D-09 (use Theo if served, degrade otherwise; ship does not wait on Theo's deploy), D-10 (generic handle only), D-15 (case_story assumed `{framework}`), D-16 (today framework_step is served with zero steps and the other two are not served; detect not-served by text, never by -32602 alone). Also lands the Tri-Polar note in the reference file so Desktop Larry, which loads the reference and not the command body, can explain research mode honestly (DDR361-04's Desktop/Cowork clause).
Output: `lib/core/dominant-design/theo-structure.cjs`, `tests/test-361-theo-structure.cjs`, one added bullet in `references/methodology/dominant-designs.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-CONTEXT.md
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-RESEARCH.md
@CLAUDE.md

Parallel-session rules and test hygiene: identical to 361-01-PLAN.md `<context>`. Per D-02 (planned in parallel with Theo Phase 20, different repo, no file overlap) no Theo-repo file is touched and ship does not wait on Theo's deploy. This plan makes NO live Theo call; tests inject a fake brainClient. The live probe is 361-08's job, after 361-02's arms land.

Interfaces (read from source):
- `callTool(toolName, args)` in lib/core/brain-client.cjs (line 595): returns `null` when no key or unreachable; `{error:'egress_blocked', tool, egress_class}` when the Part 8 belt blocks; otherwise the parsed tool result, which may carry `egress_disclosure` when the belt rated the call ambiguous.
- Live shapes measured 2026-09-23 (361-RESEARCH Pattern 5): framework_step -> `{rows:[{name:'Dominant Design', orchestrationStatus:'draft', steps:[]}], diagnostics, egress_disclosure:{verdict:'ambiguous', ...}}`; framework_techniques and case_story -> `{text:'MCP error -32602: Tool framework_techniques not found', egress_disclosure:{...}}`.
- lib/core/strategy/taxonomy-climb.cjs lines 1-120: the one-wire-door plus local-fallback module precedent (header wording, Part 8 line, injected client seam).
- Theo, read-only: /home/jsagi/Theo/src/mcp/content/framework-step.ts lines 320-429 for the step object fields to keep (id and name or title); /home/jsagi/Theo/src/mcp/content/framework-techniques.ts lines 185-260 for the technique row fields.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Theo structure reader with honest degrade</name>
  <files>lib/core/dominant-design/theo-structure.cjs, tests/test-361-theo-structure.cjs</files>
  <read_first>
    - lib/core/strategy/taxonomy-climb.cjs lines 1-120
    - lib/core/brain-client.cjs lines 595-680 (callTool contract and the Part 8 belt)
    - lib/core/brain-client.cjs lines 2470-2495 (`_looksLikeUnknownToolError`, which treats any -32602 as unknown: do NOT reuse it, per D-16)
    - references/methodology/dominant-designs.md lines 53-115 (the six `### Phase N: Name (...)` headings)
    - /home/jsagi/Theo/src/mcp/content/framework-step.ts lines 320-429 (read-only)
  </read_first>
  <behavior>
    - HANDLE === 'Dominant Design' and is the only framework value ever sent; a fake client records every call and every args object deep-equals {framework:'Dominant Design'} with exactly one key, even when the caller passes opts.domain = 'Acme Robotics secret' (that string never appears in any recorded args)
    - calls happen in the order framework_step, framework_techniques, case_story, one each
    - all three return null -> source 'reference', reason 'brain_unavailable', steps are the six reference phases [{id:'phase-1', name:'Domain Selection'}, ..., {id:'phase-6', name:'Problems Worth Solving'}]
    - framework_step returns the live draft shape with steps [] and the other two return the `Tool X not found` text -> source 'reference', reason 'no_steps_in_canon', reasons {framework_step:'no_steps_in_canon', framework_techniques:'not_served', case_story:'not_served'}
    - framework_techniques returns `{text:'MCP error -32602: Input validation error: unrecognized key'}` -> its reason is 'shape_refused', not 'not_served'
    - framework_step returns {error:'egress_blocked', ...} -> reason 'egress_blocked'; a fake that throws -> 'call_threw'; a result with a `refusals` key or a non-egress `error` string -> 'refused'
    - framework_step returns rows[0].steps of length 3 with id and name fields -> source 'theo', reason null, steps mapped to {id, name} (name falls back to title), and a served framework_techniques row list is kept as techniques with only name and description string fields
    - each entry in result.calls is {tool, args, outcome, egress_disclosure:boolean}; egress_disclosure is true exactly when the raw result had an egress_disclosure key
    - parseReferencePhases on the real reference file returns six phases; on text with no phase headings returns []; when the reference is unreadable and Theo gives no steps, source is 'reference', steps [], reason carries 'reference_unreadable'
    - the function never throws for any fake behavior above and makes no network call (fetch thrower never fires)
  </behavior>
  <action>
    Create lib/core/dominant-design/theo-structure.cjs ('use strict'; header in the taxonomy-climb style stating Phase 361, D-09, D-10, D-15, D-16, and the Part 8 line: the only value that crosses to Theo is the constant HANDLE, never the navigator's domain or any room content). Exports: `HANDLE`, `TOOLS` (frozen ['framework_step', 'framework_techniques', 'case_story']), `classifyCallResult(toolName, result)` returning one of the reason strings or 'served', `parseReferencePhases(text)` (regex `^### Phase (\d+): ([^(\n]+?)\s*(\(|$)` per line, id `phase-<n>`, trimmed name), and `async readDominantDesignStructure(opts)` where opts may carry `brainClient` (default: lazy `require('../brain-client.cjs')` inside the function so requiring this module opens nothing) and `referencePath` (default the plugin's references/methodology/dominant-designs.md resolved from `__dirname`). Build each args object fresh inside the loop as a literal `{ framework: HANDLE }`; never spread or merge opts into it. Classification order for a result: null -> brain_unavailable; object with error === 'egress_blocked' -> egress_blocked; text (result.text, or the first content[].text, else JSON.stringify(result)) matching `/\btool\s+\S+\s+not\s+found\b/i` -> not_served; text matching /-32602/ -> shape_refused; a `refusals` key or any other `error` string -> refused; framework_step with rows[0].steps an empty array or missing rows -> no_steps_in_canon; else served. Wrap each call in try/catch -> call_threw. Result: `{ source, reason, reasons, steps, techniques, cases, calls }` where `reason` is the framework_step reason when source is reference (or 'reference_unreadable' appended with a `+` when the reference also failed). Cases, when case_story is served, keep only title, summary and outcome string fields. Read the reference with fs.readFileSync inside a try/catch.

    Write the test first (RED) with a fake brainClient factory `makeFake(map)` whose callTool records `{tool, args}` and returns scripted values; then implement (GREEN). Commit with `git add` + `git commit --only -m "feat(361-05): Theo structure reader for Dominant Design with reference fallback" -- lib/core/dominant-design/theo-structure.cjs tests/test-361-theo-structure.cjs`.
  </action>
  <verify>
    <automated>node tests/test-361-theo-structure.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-361-theo-structure.cjs` exits 0 with a PASS line per behavior leg
    - `grep -c "_looksLikeUnknownToolError" lib/core/dominant-design/theo-structure.cjs` prints 0
    - `node -e "require('./lib/core/dominant-design/theo-structure.cjs')"` completes without loading brain-client (verify with `node -e "require('./lib/core/dominant-design/theo-structure.cjs');process.exit(Object.keys(require.cache).some(k=>k.endsWith('brain-client.cjs'))?1:0)"` exiting 0)
  </acceptance_criteria>
  <done>The structure source is Theo only when Theo actually has steps; every other case degrades to the reference and names the reason, and only the generic handle is ever sent.</done>
</task>

<task type="auto">
  <name>Task 2: Research mode note in the reference file (Tri-Polar)</name>
  <files>references/methodology/dominant-designs.md</files>
  <read_first>
    - references/methodology/dominant-designs.md lines 205-212 (the `## Quick Pass vs Deep Dive` section, the last section of the file)
    - 361-RESEARCH.md `## Tri-Polar` table
  </read_first>
  <action>
    Run `git status --short -- references/methodology/dominant-designs.md` (STOP on an unowned diff). Append one bullet as the last line of the `## Quick Pass vs Deep Dive` section, after the Deep bullet, in the same `- **Label**: text` style: `- **Research mode (deep dive, Claude Code)**: before the six phases, Larry proposes one search per evidence lane (variant census, convergence signals, S-curve limits, discontinuity signals) built from the domain you name. Nothing is searched until you approve or edit those queries. Every claim that comes back carries its source or is dropped, and each lane's evidence is filed to the room only when you say so. On Claude Desktop and Cowork, Larry runs the deep dive from what you know and tells you that the sourced research pass runs in Claude Code.` Hyphens only, no em-dash. Do not edit any other line; the six `### Phase N:` headings must stay byte-identical (theo-structure.cjs parses them). Re-run `node tests/test-361-theo-structure.cjs` (the six-phase leg reads this file). Commit with `git add` + `git commit --only -m "docs(361-05): research mode note in the Dominant Designs reference" -- references/methodology/dominant-designs.md`.
  </action>
  <verify>
    <automated>grep -q '^- \*\*Research mode (deep dive, Claude Code)\*\*' references/methodology/dominant-designs.md && test "$(grep -c '^### Phase [1-6]:' references/methodology/dominant-designs.md)" -eq 6 && node tests/test-361-theo-structure.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `git diff PLAN_BASE -- references/methodology/dominant-designs.md` shows exactly one added line and no removed line
    - `grep -c '^### Phase [1-6]:' references/methodology/dominant-designs.md` prints 6
    - `grep -c "$(printf '\xe2\x80\x94')" references/methodology/dominant-designs.md` prints 0
  </acceptance_criteria>
  <done>Desktop and Cowork Larry can describe research mode honestly from the reference they load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin -> Theo (Brain) | args objects cross to the remote teaching graph |
| Theo response -> plugin | remote JSON drives the structure Larry follows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-361-20 | Information disclosure | readDominantDesignStructure args | mitigate | args built as a fresh literal `{framework: HANDLE}` per call; test asserts deep equality and that a caller-supplied domain never appears; the brain-client belt plus 361-02's arms classify it |
| T-361-21 | Spoofing | treating a served-but-empty tool as authoritative | mitigate | Theo is the source only with at least one step; otherwise reference, with the reason recorded (D-16) |
| T-361-22 | Tampering | misclassifying an input-shape refusal as not served | mitigate | not_served only on the `Tool X not found` text; other -32602 is shape_refused and surfaces |
| T-361-23 | Tampering | oversized or odd Theo payloads | mitigate | only whitelisted scalar fields kept from steps, techniques and cases |
| T-361-24 | Denial of service | Theo down | accept | returns the reference structure and reason; never throws |
</threat_model>

<verification>
`node tests/test-361-theo-structure.cjs` exits 0; the reference file has the new bullet and six unchanged phase headings; `bash tests/run-all-361.sh` shows the theo-structure leg PASSED.
</verification>

<success_criteria>
Research mode can always name its structure source and reason, sends Theo nothing but the generic handle, and the reference explains research mode on every surface.
</success_criteria>

## Artifacts this phase produces

This plan's share: `lib/core/dominant-design/theo-structure.cjs`, `tests/test-361-theo-structure.cjs`, the Research mode bullet in `references/methodology/dominant-designs.md`.

<output>
Create `.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-05-SUMMARY.md` when done.
</output>
