---
phase: 361-dominant-design-research-mode-mos-dominant-designs-gains-a-r
plan: 02
type: execute
wave: 2
depends_on: [361-01]
files_modified:
  - lib/core/part8-egress-guard.cjs
  - tests/test-361-egress-shapes.cjs
  - tests/test-361-theo-parity.cjs
autonomous: true
requirements: [DDR361-10]
canon_parts: [8]
tags: [part8, egress-guard, known-tool-shape, theo]

must_haves:
  truths:
    - "classify({framework: 'Dominant Design'}, {toolName: <any name containing framework_step | framework_techniques | case_story>}) returns verdict allow with class known_tool_shape (D-10)"
    - "framework_step additionally admits an optional step_id that matches Theo's PROCESS_STEP_ID_PATTERN; any other extra key, a missing framework, a non-string, a multi-line or over-length value, or a name outside the plugin's canonical framework vocabulary is NOT allowed by these arms (it falls through to ambiguous or block, never allow)"
    - "A framework value carrying content (an email, a money figure, a venture proper noun) is blocked or ambiguous, never allowed"
    - "The three arms are separate entries placed after the recommend_chain arm and before the final `return null`; no 361 commit changes a byte of the find_connections arm (D-10, D-14)"
    - "Before adding an arm, the executor checks whether a peer (the handoff item 1a work) already landed it and adds only what is missing (D-14)"
    - "The plugin arm's key sets and framework-name pattern match Theo's framework_step / framework_techniques input shapes, read-only from the Theo checkout; the test exits 77 when the checkout is absent"
  artifacts:
    - path: "lib/core/part8-egress-guard.cjs"
      provides: "three known-shape arms plus _isKnownFrameworkHandle"
      contains: "framework_techniques canonical framework handle"
    - path: "tests/test-361-egress-shapes.cjs"
      provides: "classify legs, hook legs, find_connections non-edit legs"
      min_lines: 150
    - path: "tests/test-361-theo-parity.cjs"
      provides: "read-only parity with Theo input shapes"
      min_lines: 60
  key_links:
    - from: "lib/core/part8-egress-guard.cjs _proveKnownToolShape"
      to: "CANONICAL_PHRASES (data/framework-names.json)"
      via: "_isKnownFrameworkHandle exact-membership check"
      pattern: "_isKnownFrameworkHandle"
    - from: "tests/test-361-egress-shapes.cjs"
      to: "tests/fixtures/361-pre-phase.json"
      via: "base_sha for the per-commit find_connections slice comparison"
      pattern: "361-pre-phase"
---

<objective>
Teach the Part 8 egress guard to recognize the three Theo calls research mode makes, so a call carrying only the generic handle `"Dominant Design"` is proven safe instead of disclosed as ambiguous, without touching Phase 355's find_connections arm.

Purpose: D-10 and D-14. Live Theo today answers these calls with `egress_disclosure: ambiguous` (361-RESEARCH Pattern 5); the arms remove that for the proven shape and only that shape. The arms are stricter than find_connections on purpose: the value must be a canonical framework name, because D-10 says generic handles only.
Output: three arms and one helper in `lib/core/part8-egress-guard.cjs`; `tests/test-361-egress-shapes.cjs`; `tests/test-361-theo-parity.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-CONTEXT.md
@.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-RESEARCH.md
@CLAUDE.md

Parallel-session rules: identical to 361-01-PLAN.md `<context>` (record PLAN_BASE; `git status --short -- <file>` before every edit and STOP on an unowned diff; `git add` + `git commit --only -m ... -- <paths>`; never `-A`, `-a`, stash, revert an unowned diff, push, or run release.sh; never write STATE.md; no em-dashes). Test hygiene: identical to 361-01 (fetch thrower in process and via a NODE_OPTIONS preload for children, mkdtemp only, exit 0/1/77).

Per D-02 this phase runs in parallel with Theo Phase 20 in a different repo: nothing under /home/jsagi/Theo is ever written, committed or run; the Theo checkout is read with fs.readFileSync only. This file is shared with Phase 355, which owns the find_connections arm. `lib/core/part8-egress-guard.cjs` must be clean (`git status --short` empty) before this plan edits it; if not, STOP and report. Keep the diff to one contiguous insertion after the recommend_chain arm plus one helper block; no reformatting, no reordering, no edits to module.exports.

Interfaces the executor relies on (read from source, do not re-derive):
- `_hasExactKeys(payload, required, optional)` at about line 386; `_isSafeShortLabel(v)` at about line 373 (1-120 chars, no CR/LF, clears the content audit).
- `classify(payload, { toolName })` returns `{verdict: 'allow'|'ambiguous'|'block', class, reason}`; step 3b calls `_proveKnownToolShape(payload, toolName)` only after the default-deny content scan ran, and the terminal fallback is `{verdict: 'ambiguous', class: 'unknown'}`.
- `CANONICAL_PHRASES` (about line 595): frozen, lowercased framework_names plus curated_extras from data/framework-names.json; empty array when the file did not load. "Dominant Design" is in data/framework-names.json.
- Theo, read-only: `FRAMEWORK_NAME_PATTERN = /^[A-Za-z0-9 '(),.\/-]{1,128}$/` and `PROCESS_STEP_ID_PATTERN` in /home/jsagi/Theo/src/mcp/content/vocabulary.ts (about lines 224 and 286-287); `inputShape` blocks in framework-step.ts (about 353-365: `framework`, optional `step_id`) and framework-techniques.ts (about 192-196: `framework`).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three separate known-shape arms plus the canonical-handle helper</name>
  <files>lib/core/part8-egress-guard.cjs, tests/test-361-egress-shapes.cjs</files>
  <read_first>
    - lib/core/part8-egress-guard.cjs lines 360-500 (helpers, the _proveKnownToolShape docblock, the three existing arms) and lines 585-615 (CANONICAL_PHRASES, _TYPED_QUESTION_DATA_LOADED)
    - lib/core/part8-egress-guard.cjs lines 700-745 (classify's step order and the terminal ambiguous verdict)
    - tests/test-260906-fda-known-tool-shapes.cjs lines 1-80 and 184-265 (expectVerdict helper, the hook-leg spawn shape and its exit-code expectations)
    - docs/2026-09-23-HANDOFF-theo-phase-20-and-phase-361-m-side-work.md section 1a (lines 14-30)
    - tests/fixtures/361-pre-phase.json and tests/test-361-baseline.cjs (reuse `extractFindConnectionsSlice`, `sha256`, `loadFixture` by require)
  </read_first>
  <behavior>
    - allow + known_tool_shape: `{framework:'Dominant Design'}` under `mcp__plugin_mos_mindrian-brain__framework_step`, under a bare `framework_techniques`, and under `case_story`; `{framework:'Dominant Design', step_id:'dominantdesign::phase::s01'}` (or any value the Theo PROCESS_STEP_ID_PATTERN copy accepts) under framework_step
    - not allow: extra key `{framework:'Dominant Design', note:'x'}` on each tool; `step_id` on framework_techniques or case_story; malformed step_id; missing framework; framework as a number; framework with a newline; a 129-char name; `{framework:'Some Unlisted Framework'}` (off-vocabulary, expected ambiguous); lowercase `{framework:'dominant design'}` is allowed only if the helper's case rule says so (decide in the helper and pin it: exact membership after lowercasing both sides, so it is allowed)
    - block: `{framework:'jane@example.com'}` and `{framework:'Dominant Design $2M ARR'}` on framework_step (content scan fires first)
    - tool-name isolation: `{framework:'Dominant Design'}` under `find_connections`, `taxonomy_ladder` and `recommend_chain` names is NOT allowed by the new arms (existing arms reject it)
    - fail closed: when CANONICAL_PHRASES is empty the helper returns false (test by calling `_proveKnownToolShape` through a fresh require with data/framework-names.json unreadable is not practical; instead assert the helper source contains the explicit `length === 0` guard and export nothing new)
    - hook leg: spawning `scripts/part8-egress-guard-hook.cjs` with `{tool_name:'mcp__plugin_mos_mindrian-brain__framework_step', tool_input:{framework:'Dominant Design'}}` exits 0 with no ambiguous-disclosure text; with `tool_input:{framework:'jane@example.com'}` it exits 2
    - find_connections leg A: the arm slice in the working tree equals the slice at HEAD
    - find_connections leg B: for every commit in `<base_sha>..HEAD` that touched lib/core/part8-egress-guard.cjs and whose subject contains `361-`, the slice at `<commit>^` equals the slice at `<commit>` (so Phase 355's own commits may change it; 361's may not)
    - arm order leg: in the source, the index of `indexOf('recommend_chain')` is less than the index of each new arm's `indexOf('framework_step')`, `indexOf('framework_techniques')`, `indexOf('case_story')`, and each is less than the index of the function's final `return null;`
  </behavior>
  <action>
    D-14 pre-check first: `grep -n "framework_step\|framework_techniques\|case_story" lib/core/part8-egress-guard.cjs` and `git log --oneline -S"framework_techniques" -- lib/core/part8-egress-guard.cjs`. If a peer already added an arm for one of the three tools, do NOT edit it; add only the missing arms, adapt that tool's allow legs to assert only what D-10 requires (`{framework:'Dominant Design'}` allowed, extra keys and content not allowed), and record which arms pre-existed in the SUMMARY.

    Write tests/test-361-egress-shapes.cjs with the behavior legs (RED). Then edit lib/core/part8-egress-guard.cjs:

    (a) Directly after the closing brace of the `_proveKnownToolShape` function (NOT inside the find_connections region), add a helper block: a module-level `FRAMEWORK_NAME_RE` copied byte-for-byte from Theo's FRAMEWORK_NAME_PATTERN (no g flag), a module-level `PROCESS_STEP_ID_RE` copied byte-for-byte from Theo's PROCESS_STEP_ID_PATTERN, and `function _isKnownFrameworkHandle(v)` returning true only when `_isSafeShortLabel(v)` is true, `FRAMEWORK_NAME_RE.test(v)` is true, `CANONICAL_PHRASES.length > 0` (explicit `length === 0` guard returns false: fail closed on a vocabulary load failure, like `_TYPED_QUESTION_DATA_LOADED`), and `CANONICAL_PHRASES.indexOf(v.toLowerCase()) !== -1` (exact membership, never substring). A comment block above it states: generic handles only per D-10; stricter than find_connections on purpose; the two regexes are copies of Theo's vocabulary.ts and tests/test-361-theo-parity.cjs pins them; CANONICAL_PHRASES is read at call time (module constants initialize before any call). Function declarations hoist, so the arms may call it.

    (b) Inside `_proveKnownToolShape`, after the recommend_chain arm's closing brace and before the final `return null;`, add three separate `if (toolName.indexOf(...) !== -1) { ... }` arms in this order: framework_step (`_hasExactKeys(payload, ['framework'], ['step_id'])`, `_isKnownFrameworkHandle(payload.framework)`, and when step_id is present it must be a string matching PROCESS_STEP_ID_RE; returns `{ class: 'known_tool_shape', reason: 'framework_step canonical framework handle' }`), framework_techniques (`_hasExactKeys(payload, ['framework'], [])` plus the helper; reason `'framework_techniques canonical framework handle'`), case_story (same as framework_techniques; reason `'case_story canonical framework handle'`; a one-line comment says the `{framework}` shape is assumed per D-15 until Theo 20.1 publishes it, and a wrong guess only falls through to ambiguous, never allow). Each arm returns `null` on any failed check. Also append one line to the `_proveKnownToolShape` docblock's list noting the three 361 arms and that they sit last, after recommend_chain.

    Do not change module.exports, the find_connections arm, the taxonomy_ladder arm, the recommend_chain arm, or classify. Run the test GREEN, then `node tests/test-260906-fda-known-tool-shapes.cjs` and `node lib/core/part8-egress-guard.test.cjs` (must stay green). Commit: `git add lib/core/part8-egress-guard.cjs tests/test-361-egress-shapes.cjs` then `git commit --only -m "feat(361-02): Part 8 known-shape arms for framework_step, framework_techniques, case_story" -- lib/core/part8-egress-guard.cjs tests/test-361-egress-shapes.cjs`.
  </action>
  <verify>
    <automated>node tests/test-361-egress-shapes.cjs && node tests/test-260906-fda-known-tool-shapes.cjs && node lib/core/part8-egress-guard.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-361-egress-shapes.cjs` exits 0 and prints a PASS line for every behavior leg, including both find_connections legs and the arm-order leg
    - `node -e "const g=require('./lib/core/part8-egress-guard.cjs');const r=g.classify({framework:'Dominant Design'},{toolName:'mcp__plugin_mos_mindrian-brain__framework_step'});process.exit(r.verdict==='allow'&&r.class==='known_tool_shape'?0:1)"` exits 0
    - `git diff PLAN_BASE -- lib/core/part8-egress-guard.cjs` contains no removed lines (`^-[^-]` count 0)
    - The existing FDA known-tool-shapes suite and the guard's own test file still exit 0
  </acceptance_criteria>
  <done>The three arms prove the generic-handle shape and nothing wider; Phase 355's block is provably untouched by 361.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Read-only Theo input-shape parity test</name>
  <files>tests/test-361-theo-parity.cjs</files>
  <read_first>
    - tests/test-354-taxonomy-ladder-casing.cjs lines 85-130 (the MINDRIAN_THEO_CHECKOUT seam, ENV GAP message, exit 77 precedent)
    - /home/jsagi/Theo/src/mcp/content/framework-step.ts lines 320-380 (read-only: inputShape keys and their zod patterns)
    - /home/jsagi/Theo/src/mcp/content/framework-techniques.ts lines 185-200 (read-only)
    - /home/jsagi/Theo/src/mcp/content/vocabulary.ts lines 215-290 (read-only: the two patterns)
  </read_first>
  <behavior>
    - Leg 1: the key set inside framework-step.ts's `inputShape: { ... }` block is exactly {framework, step_id}, and the plugin arm's required/optional lists (parsed from the plugin source's `_hasExactKeys(payload, [...], [...])` call inside the framework_step arm) union to the same set
    - Leg 2: same for framework-techniques.ts ({framework}) against the framework_techniques arm
    - Leg 3: the source text of Theo's FRAMEWORK_NAME_PATTERN and PROCESS_STEP_ID_PATTERN regex literals equals the plugin's FRAMEWORK_NAME_RE and PROCESS_STEP_ID_RE literals byte-for-byte
    - Leg 4: case_story: if a Theo file whose name contains `case-story` exists under src/mcp/content, compare its inputShape keys against the case_story arm the same way and FAIL on a difference (that is the D-15 recheck trigger); if absent, print `case_story: not built in the Theo checkout; {framework} assumed per D-15` and pass
    - Checkout absent or unreadable (MINDRIAN_THEO_CHECKOUT, default /home/jsagi/Theo): print `ENV GAP: Theo checkout not found` and exit 77
    - The test only reads files (fs.readFileSync); it never runs git or writes inside the Theo checkout
  </behavior>
  <action>
    Write tests/test-361-theo-parity.cjs implementing the legs with plain string parsing: locate `inputShape:` then brace-match to the closing brace and collect top-level keys with a `/^\s*([a-z_]+)\s*:/m` scan over that block; locate the pattern literals by their `export const NAME =` lines (PROCESS_STEP_ID_PATTERN spans two lines; join until the terminating `;`). Parse the plugin side from lib/core/part8-egress-guard.cjs source text the same way (the arm block is the text from `indexOf('framework_step')` to the next `return {`). Print the Theo checkout's HEAD commit only by reading `.git/HEAD` and the ref file if present (no git spawn), for the record. Run it; it must pass against the live checkout (commit 4055f11 or later). Commit: `git add tests/test-361-theo-parity.cjs` then `git commit --only -m "test(361-02): read-only Theo input-shape parity for the new arms" -- tests/test-361-theo-parity.cjs`.
  </action>
  <verify>
    <automated>node tests/test-361-theo-parity.cjs; s=$?; [ $s -eq 0 ] || [ $s -eq 77 ]</automated>
  </verify>
  <acceptance_criteria>
    - With the checkout present, `node tests/test-361-theo-parity.cjs` exits 0 and prints PASS for legs 1-3 and the case_story line
    - `MINDRIAN_THEO_CHECKOUT=/nonexistent node tests/test-361-theo-parity.cjs` exits 77 and prints `ENV GAP`
    - `grep -c "writeFileSync\|execSync\|spawnSync" tests/test-361-theo-parity.cjs` prints 0
  </acceptance_criteria>
  <done>Plugin arms and Theo's input seals are pinned to each other; the D-15 case_story recheck fires automatically when Theo 20.1 lands a shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin -> Theo (Brain) | any argument object sent by brainClient.callTool crosses to a remote graph |
| Theo checkout -> test | the parity test reads another repo's source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-361-04 | Information disclosure | framework_step / framework_techniques / case_story arms | mitigate | exact key sets via `_hasExactKeys`, `_isSafeShortLabel`, Theo's name pattern, and exact CANONICAL_PHRASES membership; content scan still runs first; off-vocabulary falls to ambiguous, never allow |
| T-361-05 | Elevation of privilege | arm ordering / substring match | mitigate | arms placed after recommend_chain, tool names share no substring with existing arms, arm-order leg pins positions |
| T-361-06 | Tampering | Phase 355's find_connections arm | mitigate | per-commit slice comparison over `<base_sha>..HEAD` for 361 commits plus working-tree vs HEAD leg; STOP rule on an unclean file |
| T-361-07 | Information disclosure | fail-open on vocabulary load failure | mitigate | explicit `CANONICAL_PHRASES.length === 0` returns false |
| T-361-08 | Tampering | Theo checkout | accept | test is read-only (fs.readFileSync only), asserted by a grep acceptance criterion |
</threat_model>

<verification>
`node tests/test-361-egress-shapes.cjs`, `node tests/test-361-theo-parity.cjs` (0 or 77), `node tests/test-260906-fda-known-tool-shapes.cjs`, `node lib/core/part8-egress-guard.test.cjs` all pass; `bash tests/run-all-361.sh` shows the two new legs PASSED.
</verification>

<success_criteria>
A Theo call carrying only `{framework: "Dominant Design"}` classifies allow/known_tool_shape; anything wider does not; the find_connections arm is byte-identical across every 361 commit.
</success_criteria>

## Artifacts this phase produces

This plan's share: the three arms and `_isKnownFrameworkHandle` in `lib/core/part8-egress-guard.cjs`, `tests/test-361-egress-shapes.cjs`, `tests/test-361-theo-parity.cjs`.

<output>
Create `.planning/phases/361-dominant-design-research-mode-mos-dominant-designs-gains-a-r/361-02-SUMMARY.md` when done (record which arms, if any, pre-existed per D-14).
</output>
