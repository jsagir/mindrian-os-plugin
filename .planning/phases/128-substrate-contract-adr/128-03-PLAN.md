---
phase: 128-substrate-contract-adr
plan: 03
type: execute
wave: 3
depends_on: [128-01, 128-02]
files_modified:
  - scripts/install-pre-commit.sh
  - .git/hooks/pre-commit
  - docs/architecture/SUBSTRATE-BASELINE.md
autonomous: true
requirements: [SUBC-128-04, SUBC-128-07]
canon_parts:
  - Part 6 (dog-fooding -- the plugin's own pre-commit honors its substrate contract)
  - Part 7 (reuse before build -- wire the existing guard into the existing hook, retire --check-chokepoint per the ADR)
  - Part 8 (graph boundary -- hook is git-surface only, zero network)
  - Part 9 (memory locality -- the hook makes navigation.cjs the structurally-enforced only door)

must_haves:
  truths:
    - "git commit of a file with a raw INSERT INTO nodes outside navigation.cjs is rejected by the pre-commit hook"
    - "git commit of a file adding a new openGraph caller outside the allow-list is rejected by the pre-commit hook"
    - "the live .git/hooks/pre-commit invokes check-substrate.cjs --diff (and no longer relies on the never-wired --check-chokepoint)"
    - "scripts/install-pre-commit.sh installs the check-substrate.cjs --diff invocation for fresh clones"
    - "the baseline report enumerates the ~15 openGraph openers + hsi-to-graph.cjs + hat-persistence.cjs as known pre-existing violations"
  artifacts:
    - path: "scripts/install-pre-commit.sh"
      provides: "Installer template that wires check-substrate.cjs --diff into a fresh-clone pre-commit hook"
      contains: "check-substrate"
    - path: ".git/hooks/pre-commit"
      provides: "Live hook invoking check-substrate.cjs --diff on staged files"
      contains: "check-substrate"
    - path: "docs/architecture/SUBSTRATE-BASELINE.md"
      provides: "Informational baseline violation report: the pre-existing bypasses each downstream phase owns"
      min_lines: 30
      contains: "lazygraph-ops"
  key_links:
    - from: ".git/hooks/pre-commit"
      to: "scripts/check-substrate.cjs"
      via: "node ... check-substrate.cjs --diff || exit"
      pattern: "check-substrate.cjs --diff"
    - from: "docs/architecture/SUBSTRATE-BASELINE.md"
      to: "lib/core/lazygraph-ops.cjs"
      via: "baseline enumerates openGraph openers + un-provenanced INSERTs"
      pattern: "lazygraph-ops|openGraph"
---

<objective>
Wire the Plan 02 guard into the live pre-commit hook and the install-pre-commit.sh template, retiring the never-wired `--check-chokepoint` per the Plan 01 ADR decision, and produce the informational baseline violation report at `docs/architecture/SUBSTRATE-BASELINE.md`. After this plan, a git commit that adds a NET-NEW substrate violation (raw INSERT INTO nodes, a new openGraph caller, a direct sqlite require, a Cypher user-content interpolation outside navigation.cjs) is hard-rejected by pre-commit; the ~15 PRE-EXISTING bypasses are enumerated in the baseline as known debt owned by Phases 129/129.5/130.

Purpose: the guard is inert until a hook calls it. The live hook today runs only `--check-sendpacket` (CONTEXT finding H1: --check-chokepoint exists but was never wired). This plan closes that gap and makes navigation.cjs the structurally-enforced only door.

Output: edited `scripts/install-pre-commit.sh` + edited `.git/hooks/pre-commit` + new `docs/architecture/SUBSTRATE-BASELINE.md`. NO migration of the existing violations (Phase 129+ owns that).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/architecture/SUBSTRATE-CONTRACT.md
@scripts/install-pre-commit.sh
@.planning/phases/128-substrate-contract-adr/128-CONTEXT.md

<interfaces>
<!-- The live hook + installer to edit. Read both before editing. -->

From .git/hooks/pre-commit:
  :160-163 -- brain-packet-schema --check block (Phase 110-01)
  :176-178 -- the ONLY check-schema-aliases.cjs invocation today: --check-sendpacket
             (--check-chokepoint is NEVER called by the live hook -- CONTEXT finding H1)
  the hook is plugin-source-aware: Data Room ROOM.md/MINTO.md checks are scoped to .room-root
  subtrees (lines 1-20 + 180+) and never block plugin source commits.

From scripts/install-pre-commit.sh:
  installs/appends a .git/hooks/pre-commit that invokes node scripts/check-schema-aliases.cjs.
  The append-detection greps for "check-schema-aliases.cjs". The hook body is heredoc-generated.

From scripts/check-substrate.cjs (Plan 02):
  --diff       -- staged-only scan, exit 1 on violation (THE pre-commit mode to wire)
  --baseline   -- full-repo scan, exit 0, prints all violations grouped by rule (THE report source)
  --check-chokepoint -- superset alias (retirement-compatible per the ADR)

From the CONTEXT absorbed evidence -- the ~15 known openGraph openers to enumerate in the baseline:
  lib/core/graph-ops.cjs, lib/core/rs-mind-map.cjs, lib/wiki/graph-links.cjs,
  scripts/generate-export.cjs, scripts/hsi-to-graph.cjs, scripts/generate-presentation.cjs,
  scripts/discovery-cycle.cjs, scripts/cross-room-detect.cjs, scripts/whitespace-to-graph.cjs,
  scripts/build-graph-from-sqlite.cjs, scripts/build-ecosystem-graph.cjs, scripts/causal-to-graph.cjs,
  lib/core/rs-expert-mapper.cjs, lib/core/rs-sqlite-mirror.cjs, lib/chat/fabric-chat.cjs,
  lib/core/room-auto-create.cjs (+ the rs command scripts).
  PLUS lib/core/lazygraph-ops.cjs internal un-provenanced INSERTs, hsi-to-graph.cjs raw conn.prepare,
  and lib/core/hat-persistence.cjs filesystem STATE.md writes (the Cluster-2 finding).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire check-substrate.cjs --diff into the live hook and the installer template; retire --check-chokepoint</name>
  <read_first>
    - .git/hooks/pre-commit lines 155-200 (the existing node-guard blocks; you append a check-substrate block next to the --check-sendpacket block at :176)
    - scripts/install-pre-commit.sh (the append/create heredoc logic to extend)
    - docs/architecture/SUBSTRATE-CONTRACT.md (the reuse-vs-build decision: check-substrate supersedes --check-chokepoint; the hook calls check-substrate.cjs --diff, NOT --check-chokepoint)
    - scripts/check-substrate.cjs (confirm the --diff exit-1-on-violation contract from Plan 02)
  </read_first>
  <files>.git/hooks/pre-commit, scripts/install-pre-commit.sh</files>
  <action>
    Add a Phase-128 guard block to the live .git/hooks/pre-commit, modeled on the existing --check-sendpacket block (:176-178): if node is available and scripts/check-substrate.cjs exists, run `node "$REPO_ROOT/scripts/check-substrate.cjs" --diff` and exit non-zero with a clear recovery message on failure (the message names navigation.cjs as the only door and points to docs/architecture/SUBSTRATE-CONTRACT.md). Place it adjacent to the sendpacket block so all three node-guards (brain-packet-schema, sendpacket, substrate) sit together. Do NOT remove the --check-sendpacket block. Do NOT add a --check-chokepoint invocation (it was never wired and the ADR retires it; check-substrate.cjs --diff is its strict superset).

    Extend scripts/install-pre-commit.sh so a fresh-clone install wires the same `node scripts/check-substrate.cjs --diff` invocation: update the append-detection grep to also detect check-substrate.cjs, append the check-substrate --diff line in the append branch, and add it to the create-branch heredoc body alongside the existing check-schema-aliases.cjs line. Preserve the existing schema-aliases / sendpacket behavior (additive, not a rewrite). Keep set -euo pipefail and the --no-verify bypass note.

    No em-dashes (hyphens only). The hook and installer make ZERO network calls (Canon Part 8: git-surface only).
  </action>
  <verify>
    <automated>grep -q "check-substrate.cjs --diff" .git/hooks/pre-commit &amp;&amp; grep -q "check-substrate" scripts/install-pre-commit.sh &amp;&amp; ! grep -q "check-substrate.cjs --check-chokepoint" .git/hooks/pre-commit &amp;&amp; ! grep -q $'—' scripts/install-pre-commit.sh &amp;&amp; bash -n scripts/install-pre-commit.sh &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.git/hooks/pre-commit` contains `node ... check-substrate.cjs --diff` and the existing `--check-sendpacket` block is unchanged.
    - The hook does NOT invoke `--check-chokepoint` (the never-wired subcommand is superseded per the ADR).
    - `scripts/install-pre-commit.sh` references check-substrate.cjs in both its append branch and its create-branch heredoc; `bash -n scripts/install-pre-commit.sh` parses clean.
    - The hook block contains a recovery message naming navigation.cjs and docs/architecture/SUBSTRATE-CONTRACT.md.
    - Zero em-dash characters; zero network surface added.
  </acceptance_criteria>
  <done>The live hook and the installer template both wire check-substrate.cjs --diff; --check-chokepoint is retired (not called); em-dash-free, LOCAL-only, parses clean.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the baseline violation report and prove the hook hard-fails net-new violations</name>
  <read_first>
    - scripts/check-substrate.cjs (the --baseline mode; you run it to source the report)
    - .planning/phases/128-substrate-contract-adr/128-CONTEXT.md ("Absorbed evidence" -- the openGraph openers + hsi-to-graph + hat-persistence list, and which downstream phase owns each)
    - docs/architecture/SUBSTRATE-CONTRACT.md (the scope-boundary section that assigns the migrations to Phase 129/129.5/130)
  </read_first>
  <files>docs/architecture/SUBSTRATE-BASELINE.md</files>
  <action>
    Run `node scripts/check-substrate.cjs --baseline` and capture its output. Author docs/architecture/SUBSTRATE-BASELINE.md as the INFORMATIONAL baseline report: a dated table of every pre-existing violation grouped by rule (openGraph caller / un-provenanced INSERT / direct sqlite require / raw room.db fs read / filesystem-state write), each row carrying file path, the rule it breaks, and the owning downstream phase (129 spine-script rewrites, 129.5, 130 hats, or v1.14.0 backlog) per the ADR scope boundary. Include the headline finding: lib/core/lazygraph-ops.cjs is the #1 production graph writer opened by ~15 scripts via openGraph, writing the bare 3-column un-provenanced schema that diverges from the Phase-109 provenance schema on the same room.db. State explicitly that this report is NOT blocking -- it is the known-debt ledger; the hook (Task 1) blocks only NET-NEW violations introduced after this baseline.

    Then prove the hook hard-fails a net-new violation: create a throwaway fixture file under a tmp path containing a raw `INSERT INTO nodes (id, type, properties)` outside navigation.cjs, drive the guard via the MINDRIAN_HOOK_STAGED_FILES + MINDRIAN_HOOK_STAGED_CONTENT_DIR seams (or a real git add of a tmp file inside the repo), run `node scripts/check-substrate.cjs --diff`, assert it exits non-zero, then remove the fixture. Record the proof transcript (the command + the non-zero exit + the violation line) in a "Hook enforcement proof" section of the baseline report.

    No em-dashes (hyphens only). The baseline report references, but does NOT fix, any violation (migration is Phase 129+).
  </action>
  <verify>
    <automated>test -f docs/architecture/SUBSTRATE-BASELINE.md &amp;&amp; grep -qi "lazygraph-ops" docs/architecture/SUBSTRATE-BASELINE.md &amp;&amp; grep -qi "openGraph" docs/architecture/SUBSTRATE-BASELINE.md &amp;&amp; grep -qiE "not blocking|informational|known.debt|net-new" docs/architecture/SUBSTRATE-BASELINE.md &amp;&amp; grep -qE "129|130" docs/architecture/SUBSTRATE-BASELINE.md &amp;&amp; ! grep -q $'—' docs/architecture/SUBSTRATE-BASELINE.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/architecture/SUBSTRATE-BASELINE.md` exists with >= 30 lines.
    - It enumerates the openGraph openers (names lib/core/lazygraph-ops.cjs + at least 10 of the ~15 callers) grouped by rule with per-row owning phase.
    - It names lib/core/lazygraph-ops.cjs as the #1 bypass and references the bare 3-column un-provenanced schema vs the Phase-109 provenance schema.
    - It states the report is informational / not blocking and that the hook blocks only net-new violations (grep matches "not blocking" OR "informational" OR "net-new").
    - It assigns migrations to downstream phases (grep matches 129 and/or 130).
    - A "Hook enforcement proof" section records a non-zero exit from `check-substrate.cjs --diff` against a net-new raw INSERT INTO nodes fixture.
    - Zero em-dash characters.
  </acceptance_criteria>
  <done>SUBSTRATE-BASELINE.md enumerates the pre-existing bypasses with owning phases, names the lazygraph-ops #1 bypass, marks itself non-blocking, and proves the hook hard-fails a net-new violation; em-dash-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer commit -> pre-commit hook | untrusted new code crosses the gate; the hook is the structural enforcement point |
| hook -> filesystem / git index | hook reads staged source + git index only; never room.db, never network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-04 | Elevation of privilege | a net-new openGraph/raw-INSERT bypass slipping past review | mitigate | hook runs check-substrate.cjs --diff on every commit; Task 2 proves the hard-fail with a fixture |
| T-128-05 | Repudiation | a bypass landing untracked because the hook was never wired (the H1 finding) | mitigate | install-pre-commit.sh wires the guard for fresh clones; the live hook is updated in-place |
| T-128-06 | Information disclosure | hook reaching room.db or the Brain | accept | hook is git-surface only; no network surface added; verified by the em-dash + bash -n gates and the Plan 02 LOCAL-only guard |
| T-128-SC | Tampering | npm/pip/cargo installs | mitigate | NONE -- zero new packages; no install task; no legitimacy checkpoint required |
</threat_model>

<verification>
- `.git/hooks/pre-commit` and `scripts/install-pre-commit.sh` both invoke `check-substrate.cjs --diff`; neither invokes `--check-chokepoint`.
- `bash -n scripts/install-pre-commit.sh` parses clean.
- `docs/architecture/SUBSTRATE-BASELINE.md` enumerates the pre-existing bypasses with owning phases and proves the net-new hard-fail.
- Canon Part 8 LOCAL-only: hook adds zero network surface.
</verification>

<success_criteria>
- navigation.cjs is now the structurally-enforced only door: a net-new bypass is rejected at commit time.
- The H1 finding (guard existed but was never wired) is closed.
- The known pre-existing debt is ledgered and assigned to downstream phases, not silently carried.
</success_criteria>

<output>
Create `.planning/phases/128-substrate-contract-adr/128-03-SUMMARY.md` when done.
</output>
