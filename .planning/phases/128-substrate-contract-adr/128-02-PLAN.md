---
phase: 128-substrate-contract-adr
plan: 02
type: execute
wave: 2
depends_on: [128-01]
files_modified:
  - scripts/check-substrate.cjs
  - tests/test-substrate-contract.cjs
  - tests/run-all-128.sh
autonomous: true
requirements: [SUBC-128-03, SUBC-128-05]
canon_parts:
  - Part 6 (dog-fooding -- the plugin's own CI guard enforces its substrate contract)
  - Part 7 (reuse before build -- check-substrate.cjs supersedes --check-chokepoint, reuses its scan idiom)
  - Part 8 (graph boundary -- the guard is LOCAL-only, zero network)
  - Part 9 (memory locality -- the guard enforces navigation.cjs as the only door)

must_haves:
  truths:
    - "check-substrate.cjs flags every openGraph opener in its full-repo scan"
    - "check-substrate.cjs flags lib/core/lazygraph-ops.cjs as a violation (the #1 bypass)"
    - "check-substrate.cjs catches direct sqlite require, raw room.db fs reads, raw INSERT INTO nodes/edges/memory_event, and Cypher user-content interpolation outside the chokepoint"
    - "check-substrate.cjs is a strict superset of --check-chokepoint (every require-pattern it caught is still caught)"
    - "node tests/test-substrate-contract.cjs exits 0 with all 5 cases passing"
  artifacts:
    - path: "scripts/check-substrate.cjs"
      provides: "The CI guard: full-repo scan + --diff staged scan, returns violations with file:line, has a --baseline mode listing current violations"
      min_lines: 120
      contains: "openGraph"
    - path: "tests/test-substrate-contract.cjs"
      provides: "The 5 substrate-contract test cases (2 known-good, 3 known-bad)"
      min_lines: 80
    - path: "tests/run-all-128.sh"
      provides: "Phase 128 test aggregator registering the substrate-contract suite"
      min_lines: 10
  key_links:
    - from: "scripts/check-substrate.cjs"
      to: "lib/core/navigation.cjs"
      via: "navigation.cjs (and its allow-list) is the only path exempt from the bans"
      pattern: "navigation"
    - from: "tests/test-substrate-contract.cjs"
      to: "scripts/check-substrate.cjs"
      via: "require + invoke the guard against fixture content"
      pattern: "check-substrate"
---

<objective>
Build `scripts/check-substrate.cjs` -- the CI guard that structurally enforces the Substrate Contract ADR (Plan 01) -- and `tests/test-substrate-contract.cjs` with the 5 cases from CONTEXT. The guard is a strict SUPERSET of the existing `scripts/check-schema-aliases.cjs --check-chokepoint` per the Plan 01 reuse-vs-build decision: it catches every require-pattern the old guard caught PLUS the openGraph openers PLUS raw INSERT INTO nodes/edges/memory_event PLUS direct sqlite require PLUS Cypher user-content interpolation.

Purpose: Phase 109 made navigation.cjs the chokepoint; nothing made it the ONLY door. This guard is the structural gate. Plan 03 wires it into the live pre-commit hook and produces the baseline report.

Output: `scripts/check-substrate.cjs` (guard) + `tests/test-substrate-contract.cjs` (5 cases) + `tests/run-all-128.sh` (aggregator). NO hook wiring and NO committed baseline report in this plan -- those are Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/architecture/SUBSTRATE-CONTRACT.md
@.planning/phases/128-substrate-contract-adr/128-CONTEXT.md

<interfaces>
<!-- The existing guard to reuse the scan idiom from, and supersede. Read it first. -->

From scripts/check-schema-aliases.cjs (REUSE these helper shapes; do not re-invent):
  getStagedFiles()         :340 -- reads MINDRIAN_HOOK_STAGED_FILES env seam OR git diff --cached
  readStagedContent(path)  :355 -- reads MINDRIAN_HOOK_STAGED_CONTENT_DIR/<path> seam OR git show :<path>
  isAllowedPath(p)         :335 -- tests p against ALLOWED_DIRECT_IMPORT regex list
  BANNED_PATTERNS          :322 -- require() of room-db/lazygraph-ops/memory-ops
  ALLOWED_DIRECT_IMPORT    :305 -- navigation.cjs, navigation/, room-db.cjs, lazygraph-ops.cjs,
                                   memory-ops.cjs, opportunity-ops.cjs, tests/, scripts/migrate-,
                                   grandfather list, lib/core/migrations/
  checkChokepoint()        :374 -- the subcommand check-substrate.cjs SUPERSEDES
  CLI dispatch             :482 -- args.includes('--check-chokepoint') / ('--check-sendpacket')

From lib/core/lazygraph-ops.cjs (the violation target the guard MUST flag):
  openGraph                -- exported opener; ~15 production scripts call it (graph-ops.cjs,
                              rs-mind-map.cjs, wiki/graph-links.cjs, generate-export.cjs, hsi-to-graph.cjs, ...)
  INSERT INTO nodes (id, type, properties)  :357, :362 -- un-provenanced
  INSERT INTO edges (...)  :367, :392, :397 -- raw edge writes

From lib/core/navigation.cjs:
  the chokepoint -- the ONLY module (plus its navigation/ submodules + room-db.cjs self +
  the documented allow-list) exempt from the room.db-access bans.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/test-substrate-contract.cjs (5 cases) and run-all-128.sh aggregator</name>
  <read_first>
    - tests/test-navigation-acceptance.cjs lines 1-40 (the direct-CJS node:assert/strict pattern, zero npm deps)
    - tests/run-all-124.sh (the aggregator shape to mirror: CJS_SUITES array, header documenting RED-until-owning-plan-lands)
    - scripts/check-schema-aliases.cjs lines 340-372 (the MINDRIAN_HOOK_STAGED_FILES + MINDRIAN_HOOK_STAGED_CONTENT_DIR test seams the guard will honor -- the tests drive the guard through these seams, hermetically)
    - docs/architecture/SUBSTRATE-CONTRACT.md (the contract whose rules the 5 cases assert)
  </read_first>
  <files>tests/test-substrate-contract.cjs, tests/run-all-128.sh</files>
  <behavior>
    Five cases, all hermetic (no real git, no real room.db -- drive the guard via the
    MINDRIAN_HOOK_STAGED_FILES + MINDRIAN_HOOK_STAGED_CONTENT_DIR seams with fixture file
    bodies written to a tmp dir via fs.mkdtempSync):
    - Case 1 (known-good): a module body that requires lib/core/navigation.cjs and calls writeEdge / logMemoryEvent -> guard reports ZERO violations for that file.
    - Case 2 (known-bad): a module body OUTSIDE navigation.cjs containing a direct require('node:sqlite') (or better-sqlite3) -> guard reports a violation with the file path and the matched token.
    - Case 3 (known-bad): a module body containing a raw fs.readFileSync/readFile of a path matching room\.db OUTSIDE navigation.cjs -> guard reports a violation.
    - Case 4 (known-bad): a module body containing a Cypher MATCH with user-content variable interpolation (template-literal interpolation inside a MATCH string) -> guard reports a violation (Canon Part 8 breach pattern).
    - Case 5 (known-good): a module body containing an Aura read using a PARAMETERIZED binding (MATCH ... WHERE x = $param, params object) -> guard reports ZERO violations (parameterized Cypher is the allowed Aura access pattern).
    Bonus superset assertions (prove the supersede claim from Plan 01):
    - A body with a raw `INSERT INTO nodes (id, type, properties)` outside navigation.cjs -> violation (catches the lazygraph-ops bypass class).
    - A body that requires lib/core/lazygraph-ops.cjs from a non-allowlisted path -> violation (the old --check-chokepoint require-pattern still caught).
  </behavior>
  <action>
    Author tests/test-substrate-contract.cjs using node:assert/strict + node:fs + node:os + node:path, zero npm deps, mirroring tests/test-navigation-acceptance.cjs structure. Each case writes its fixture body to a tmp content dir, sets process.env.MINDRIAN_HOOK_STAGED_FILES and MINDRIAN_HOOK_STAGED_CONTENT_DIR, invokes the guard's exported scan function (require scripts/check-substrate.cjs and call its programmatic scan API -- NOT a child process, so the tests can assert on the returned violation array), and asserts the violation count + matched file for each case. Restore env between cases. The file MUST exit 0 only when all 5 cases plus both bonus assertions pass.

    Author tests/run-all-128.sh as a verbatim-shape mirror of tests/run-all-124.sh: CJS_SUITES contains tests/test-substrate-contract.cjs; header comment documents that the suite is RED-by-design until Plan 128-02 Task 2 ships the guard. Make it executable (chmod +x semantics noted in the file; the runner uses node per suite).

    No em-dashes anywhere (hyphens only). Register nothing in lib/memory/run-feynman-tests.cjs in THIS task (the guard does not exist yet); the run-all-128.sh aggregator is the Phase 128 scope-local registration surface.
  </action>
  <verify>
    <automated>test -f tests/test-substrate-contract.cjs &amp;&amp; test -f tests/run-all-128.sh &amp;&amp; grep -q "test-substrate-contract" tests/run-all-128.sh &amp;&amp; ! grep -q $'—' tests/test-substrate-contract.cjs &amp;&amp; echo "RED-baseline ok (guard not built yet)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test-substrate-contract.cjs` exists, defines exactly the 5 named cases plus the 2 superset bonus assertions, uses node:assert/strict with zero npm deps.
    - The file drives the guard hermetically via MINDRIAN_HOOK_STAGED_FILES + MINDRIAN_HOOK_STAGED_CONTENT_DIR seams (no real git, no network).
    - `tests/run-all-128.sh` registers tests/test-substrate-contract.cjs and documents the RED-until-guard-lands state.
    - Running `node tests/test-substrate-contract.cjs` BEFORE Task 2 fails (RED) because scripts/check-substrate.cjs does not yet exist -- this is the expected RED state.
    - Zero em-dash characters in both files.
  </acceptance_criteria>
  <done>The 5-case + 2-bonus test suite and the run-all-128.sh aggregator exist and are RED (guard not yet built); em-dash-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build scripts/check-substrate.cjs (the guard, strict superset of --check-chokepoint)</name>
  <read_first>
    - tests/test-substrate-contract.cjs (the contract the guard must satisfy -- written in Task 1; the guard's programmatic scan API signature is whatever Task 1 calls)
    - scripts/check-schema-aliases.cjs lines 305-401 + 480-510 (REUSE getStagedFiles / readStagedContent / isAllowedPath idiom + the BANNED_PATTERNS + ALLOWED_DIRECT_IMPORT + the CLI dispatch shape; the guard supersedes checkChokepoint, so copy its require-ban patterns forward verbatim)
    - lib/core/lazygraph-ops.cjs lines 33-37, 357-367 (the openGraph + bare INSERT INTO nodes patterns to detect)
    - docs/architecture/SUBSTRATE-CONTRACT.md (M1-M4 + the allow-list; the guard enforces exactly these mandates)
  </read_first>
  <files>scripts/check-substrate.cjs</files>
  <action>
    Build the guard. It scans lib/**/*.cjs and scripts/**/*.cjs (plus *.js / *.mjs) and detects, OUTSIDE the navigation.cjs allow-list, every one of:
    - Direct require of node:sqlite or better-sqlite3 (M3).
    - Raw fs read (readFile / readFileSync / createReadStream) of a path matching room\.db (M2).
    - Raw SQL write statement matching INSERT|UPDATE|DELETE against nodes|edges|memory_event tables (the un-provenanced-write class, including the lazygraph-ops INSERT INTO nodes (id, type, properties) shape).
    - A call to openGraph (the lazygraph-ops opener) from any file outside the allow-list -- this is the #1 bypass; the guard MUST flag every openGraph opener.
    - Cypher MATCH with user-content variable interpolation: a MATCH inside a template literal containing ${...} interpolation, OR string concatenation into a MATCH clause (M4 / Canon Part 8 breach). Parameterized Cypher ($param with a params object) is ALLOWED and must NOT flag (Case 5).
    - Forward-carry every BANNED_PATTERNS require() rule from check-schema-aliases.cjs (require of room-db/lazygraph-ops/memory-ops outside the allow-list) so the guard is a strict superset of --check-chokepoint.

    Reuse the allow-list from check-schema-aliases.cjs ALLOWED_DIRECT_IMPORT as the base, extended per the ADR's allow-list section: navigation.cjs + navigation/ + room-db.cjs self + lazygraph-ops.cjs self (it IS the bare-schema writer, but the BASELINE not the guard owns flagging its own body -- the guard flags CALLERS that openGraph; lazygraph-ops.cjs internal INSERTs are a known baseline violation Plan 03 enumerates, NOT a new-code block) + tests/ + tests/fixtures/ + scripts/migrate-* + scripts/migrations/ + lib/core/migrations/. Pin the exact allow-list to match the ADR.

    Reuse getStagedFiles + readStagedContent + isAllowedPath (honor the MINDRIAN_HOOK_STAGED_FILES + MINDRIAN_HOOK_STAGED_CONTENT_DIR seams) so the Task-1 tests drive the guard hermetically.

    Export a programmatic scan API (the function Task 1's tests call) returning an array of {file, line, rule, match} violation objects -- pure, no process.exit inside it. Add a CLI entry point (require.main === module) with three modes:
    - default / --baseline: full-repo scan (walk lib/ + scripts/), print every violation grouped by rule, exit 0 (INFORMATIONAL -- the baseline is not blocking; Plan 03 decides blocking-on-net-new).
    - --diff: scan only staged files, exit 1 on any violation (the pre-commit mode Plan 03 wires).
    - --check-chokepoint: alias that runs the superset scan in --diff mode (so any external caller of the retired subcommand keeps working per the ADR retirement plan).

    The guard makes ZERO network calls and ZERO Brain calls (Canon Part 8: LOCAL-only). No new npm dependencies (node:fs / node:path / node:child_process spawnSync for git only). No em-dashes (hyphens only).
  </action>
  <verify>
    <automated>node tests/test-substrate-contract.cjs &amp;&amp; node scripts/check-substrate.cjs --baseline | grep -qi "lazygraph-ops\|openGraph" &amp;&amp; ! grep -rEn "fetch\(|https?://|brain.mindrian|tavily" scripts/check-substrate.cjs &amp;&amp; ! grep -q $'—' scripts/check-substrate.cjs &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-substrate-contract.cjs` exits 0 (all 5 cases + 2 superset bonus assertions GREEN).
    - `node scripts/check-substrate.cjs --baseline` runs against the live repo and its output names lib/core/lazygraph-ops.cjs and/or openGraph callers (grep -i "lazygraph-ops" OR "openGraph" matches).
    - The guard exports a pure programmatic scan function returning {file,line,rule,match} objects (the Task-1 tests import and call it).
    - `--diff` mode exits 1 when a staged fixture contains a violation; `--baseline` mode exits 0 (informational).
    - `--check-chokepoint` alias runs the superset scan (verifies the ADR retirement plan: external callers of the old subcommand still work).
    - Canon Part 8: zero network/Brain surface (grep for fetch/http/brain.mindrian/tavily in the guard returns nothing).
    - Zero new npm dependencies (no new require of a node_modules package; only node: builtins + git via spawnSync).
    - Zero em-dash characters.
  </acceptance_criteria>
  <done>scripts/check-substrate.cjs is GREEN against the 5-case suite, flags the lazygraph-ops/openGraph bypass class in --baseline, is a strict superset of --check-chokepoint, LOCAL-only, zero new deps, em-dash-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged source -> guard | untrusted new code crosses the pre-commit gate; the guard is the trust check |
| guard -> filesystem | guard reads source files + git index only; never room.db, never network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | Tampering | scripts/check-substrate.cjs scan logic | mitigate | the guard scans for the documented M1-M4 patterns; the 5-case + 2-bonus test suite is the regression fence proving each rule fires |
| T-128-02 | Information disclosure | guard reading room.db or calling Brain | mitigate | guard is LOCAL-only; verify grep proves zero fetch/http/brain surface (Canon Part 8) |
| T-128-03 | Elevation of privilege | a new openGraph caller bypassing navigation.cjs | mitigate | guard flags every openGraph caller outside the allow-list (the #1 bypass class) |
| T-128-SC | Tampering | npm/pip/cargo installs | mitigate | NONE -- zero new packages added in this plan; guard uses node: builtins only; no install task exists, so no legitimacy checkpoint required |
</threat_model>

<verification>
- `node tests/test-substrate-contract.cjs` exits 0 (5 cases + 2 bonus).
- `bash tests/run-all-128.sh` reports the substrate-contract suite GREEN.
- `node scripts/check-substrate.cjs --baseline` enumerates the live bypasses (lazygraph-ops / openGraph callers).
- Guard is a strict superset of --check-chokepoint (the forwarded require-ban patterns still fire).
- Canon Part 8 LOCAL-only verified by network-surface grep.
</verification>

<success_criteria>
- The guard structurally catches all four ADR mandate classes (M1-M4) plus the openGraph bypass.
- The guard supersedes --check-chokepoint without losing any of its coverage.
- Plan 03 can wire `check-substrate.cjs --diff` into the live hook and run `--baseline` to file the report.
</success_criteria>

<output>
Create `.planning/phases/128-substrate-contract-adr/128-02-SUMMARY.md` when done.
</output>
