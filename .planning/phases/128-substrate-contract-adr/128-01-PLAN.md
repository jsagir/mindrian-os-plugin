---
phase: 128-substrate-contract-adr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/SUBSTRATE-CONTRACT.md
  - docs/architecture/ROOM.md
autonomous: true
requirements: [SUBC-128-01, SUBC-128-02, SUBC-128-06]
canon_parts:
  - Part 6 (dog-fooding -- the plugin honors its own substrate contract)
  - Part 7 (reuse before build -- the reuse-vs-build decision on --check-chokepoint)
  - Part 8 (graph boundary -- LOCAL/BRAIN/AURA/PINECONE separation)
  - Part 9 (memory locality -- navigation.cjs as the single chokepoint)

must_haves:
  truths:
    - "A reader can name the four substrates and their single access pattern from the ADR table"
    - "A reader can see, by exported function name, the closed navigation.cjs chokepoint surface (M11 export allow-list)"
    - "A reader knows whether check-substrate.cjs supersedes or extends the existing --check-chokepoint guard, and why"
    - "A reader can tell a compliant graph write from a violation by reading the worked examples"
    - "The H5 Brain-packet value-space leak is named as a known-backlogged boundary item, not silently omitted"
  artifacts:
    - path: "docs/architecture/SUBSTRATE-CONTRACT.md"
      provides: "The substrate contract ADR: 4-substrate table, M1-M4 + M11 mandates, navigation.cjs export allow-list, compliance/violation examples, reuse-vs-build decision, H5 note"
      min_lines: 130
      contains: "navigation.cjs"
    - path: "docs/architecture/ROOM.md"
      provides: "ICM Layer 0 identity file for the new docs/architecture directory (CLAUDE.md decision #15)"
      min_lines: 5
  key_links:
    - from: "docs/architecture/SUBSTRATE-CONTRACT.md"
      to: "lib/core/navigation.cjs"
      via: "export allow-list pinned by function name"
      pattern: "getActiveFocus|getNeighborhood|writeEdge|buildBrainPacket"
    - from: "docs/architecture/SUBSTRATE-CONTRACT.md"
      to: "scripts/check-schema-aliases.cjs"
      via: "reuse-vs-build decision referencing --check-chokepoint"
      pattern: "check-chokepoint|check-substrate"
---

<objective>
Author the Substrate Contract ADR at `docs/architecture/SUBSTRATE-CONTRACT.md`. This is the binding architectural decision record that Phases 129/130/131 and every future room.db-touching phase reference. It codifies the four-substrate split, pins the navigation.cjs chokepoint surface as a named export allow-list (M11), records the reuse-vs-build decision on the pre-existing `--check-chokepoint` guard (Canon Part 7), and names the H5 Brain-packet value-space leak as a known-backlogged item.

Purpose: Phase 109 shipped the chokepoint; nothing made it the ONLY door, and no document pins the door's surface by name. This ADR is the contract the Plan 02 guard enforces. The guard cannot be specified without it (allow-list scope, severity, the supersede/extend decision all live here).

Output: `docs/architecture/SUBSTRATE-CONTRACT.md` (the ADR) + `docs/architecture/ROOM.md` (ICM Layer 0 identity for the new directory). NO guard script, NO hook wiring, NO migration in this plan -- those are Plans 02 and 03 and Phase 129+ respectively.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-substrate-contract-adr/128-CONTEXT.md
@docs/MINDRIAN-CANON.md
@CLAUDE.md

<interfaces>
<!-- The actual navigation.cjs chokepoint surface to pin in the M11 export allow-list. -->
<!-- Source: lib/core/navigation.cjs module.exports (read this file before authoring the table). -->
<!-- CONTEXT names "17 exports vs documented closed 13"; the exact list lives in module.exports. -->

From lib/core/navigation.cjs (module.exports keys -- the closed surface to pin BY NAME):
  getActiveFocus, setFocus, getNeighborhood,
  findContradictions, findUnsupportedClaims, findBlockingAssumptions,
  findStaleDecisions, findOpenQuestions, findSurfaceableTensions,
  findRecentChanges, findRelevantOpportunities,
  buildBrainPacket, storeBrainSuggestions, getRoomHomeView,
  promoteNodeStatus, logMemoryEvent, firstCapturedLastTouchedBySection,
  writeEdge, detectActiveRoom, getRecentDecisionNeighborhood

  (Documented "closed 13" = the original Plan 109 surface; the additive re-exports
   logMemoryEvent / firstCapturedLastTouchedBySection / writeEdge / detectActiveRoom /
   getRecentDecisionNeighborhood were added in Phases 110/124/125/118 with per-export
   justification comments. The ADR pins the CURRENT full list as the allow-list and
   states that each future addition requires a canon amendment line.)

From scripts/check-schema-aliases.cjs (the pre-existing guard to reuse/supersede):
  checkChokepoint()  -- at lines 374-401, dispatched by --check-chokepoint at :488
  BANNED_PATTERNS    -- relative/absolute require() of room-db/lazygraph-ops/memory-ops
  ALLOWED_DIRECT_IMPORT -- the require allow-list at :305-320
  checkSendpacket()  -- the ONLY check the live hook currently runs

From lib/core/lazygraph-ops.cjs (the #1 bypass, for the violation example):
  bare 3-column schema at :33-37 (id, type, properties) -- diverges from Phase-109 provenance schema
  un-provenanced INSERT INTO nodes at :357, :362 -- no source_path / no review_status / no truth-state
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/architecture/ROOM.md identity file</name>
  <read_first>
    - CLAUDE.md (decision #15: every directory gets ROOM.md)
    - docs/architecture/ (confirm it does not yet exist; the file creates the dir)
  </read_first>
  <files>docs/architecture/ROOM.md</files>
  <action>
    Create the ICM Layer 0 identity file for the new `docs/architecture/` directory per CLAUDE.md decision #15. State the directory's purpose: it holds architectural decision records (ADRs) for the MindrianOS plugin, starting with SUBSTRATE-CONTRACT.md. Name the owning canon parts (6, 7, 8, 9). Keep it short (5-15 lines). No em-dashes anywhere (hyphens only) per the project HARD RULE.
  </action>
  <verify>
    <automated>test -f docs/architecture/ROOM.md &amp;&amp; ! grep -q $'—' docs/architecture/ROOM.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/architecture/ROOM.md` exists.
    - File names the directory purpose (ADR home) and the owning canon parts.
    - Zero em-dash characters in the file (grep for U+2014 returns nothing).
  </acceptance_criteria>
  <done>docs/architecture/ROOM.md exists, identifies the directory as the ADR home, em-dash-free.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Substrate Contract ADR</name>
  <read_first>
    - .planning/phases/128-substrate-contract-adr/128-CONTEXT.md (the authoritative scope, especially the "Absorbed evidence" section)
    - lib/core/navigation.cjs (read module.exports to copy the exact export names for the M11 allow-list)
    - lib/core/lazygraph-ops.cjs lines 33-37 and 357-367 (the bare-schema un-provenanced INSERT example)
    - scripts/check-schema-aliases.cjs lines 305-401 (the existing --check-chokepoint guard you are deciding to supersede or extend)
    - docs/MINDRIAN-CANON.md Part 8 + Part 9 (the boundary + memory-locality constitution this ADR codifies)
    - data/brain-packet-schema.json (the H5 unbounded summary/explanation strings to name as backlogged)
  </read_first>
  <files>docs/architecture/SUBSTRATE-CONTRACT.md</files>
  <action>
    Author the ADR (~150 lines) with these REQUIRED sections, all em-dash-free (hyphens only):

    1. Header: title, status Accepted, date, the canon parts it codifies (6, 7, 8, 9), and a one-line statement that navigation.cjs is the only door to room.db.

    2. The four-substrate table: Local SQLite (room.db) via lib/core/navigation.cjs chokepoint ONLY; Aura Neo4j via parameterized Cypher MATCH (read-mostly); Brain MCP via brain_ask DirectiveEnvelope (GUIDED default); Pinecone via mcp__pinecone__* . Mirror the CONTEXT table columns (Substrate / Owns / Access pattern).

    3. Cross-cutting mandates M1 through M4: M1 Substrate Contract (each datum has exactly one owning substrate); M2 Local Graph Awareness (read room.db only via navigation.cjs); M3 no direct sqlite/better-sqlite3 require outside the chokepoint; M4 no Cypher MATCH with user-content variable interpolation (Canon Part 8 breach pattern). Each mandate gets one sentence plus a one-line rationale.

    4. M11 navigation.cjs export allow-list: a named list of EVERY export key from lib/core/navigation.cjs module.exports (copy verbatim from the file -- do not paraphrase). State that this is the closed chokepoint surface, that the documented "closed 13" was relaxed additively to the current count via per-export justification comments, and that each future addition requires a canon amendment line in this ADR. This is the M11 fix: the door is now auditable by name, not by prose.

    5. Reuse-vs-build decision (Canon Part 7): a dated decision block stating that the new scripts/check-substrate.cjs SUPERSEDES the existing scripts/check-schema-aliases.cjs --check-chokepoint subcommand (check-substrate.cjs is a strict superset: it catches every --check-chokepoint require pattern PLUS the openGraph openers PLUS raw INSERT INTO nodes/edges/memory_event PLUS direct sqlite require PLUS Cypher user-content interpolation). State the retirement plan: --check-chokepoint is NOT deleted in Phase 128 (it stays as a no-op-compatible alias so any external invocation keeps working) but the live pre-commit hook stops calling it and calls check-substrate.cjs --diff instead (Plan 03 wires this). Document WHY supersede beats a second overlapping guard: two guards is surface area without integration (Moat Mandate). This decision binds Plan 02 (the guard must be a strict superset) and Plan 03 (the hook swaps the call). Use the decision-block format the executor sees in CONTEXT Open Decisions.

    6. Compliance vs violation worked examples: at least one compliant example (a module that requires lib/core/navigation.cjs and calls writeEdge / logMemoryEvent) and at least two violation examples taken from the live codebase: (a) lib/core/lazygraph-ops.cjs opening room.db via openGraph and running un-provenanced INSERT INTO nodes (id, type, properties) against the bare 3-column schema at lines 33-37 -- name it as the #1 production bypass writing a schema that diverges from the Phase-109 provenance schema on the same room.db; (b) a raw fs.readFile of a room.db path outside navigation.cjs; (c) a Cypher MATCH with user-content interpolation. Each example shows the rule it breaks.

    7. Known-backlogged boundary item (H5): a named note that the Phase 110 Brain Context Packet schema (data/brain-packet-schema.json) leaves summary and explanation as UNBOUNDED strings, a latent Part 8 value-space leak, whose fix is backlogged (not in scope for Phase 128). State that the LIVE Brain path (lib/core/brain-derivation.cjs) was confirmed clean (hashes + frozen enums + clamped floats only) -- the leak is latent in the schema, not active.

    8. Scope boundary: explicit statement that Phase 128 is the CONTRACT plus the GUARD, NOT the migration. The actual schema-unification refactor and spine-script rewrites are Phase 129 / 129.5 / 130 per the CONTEXT Open Decisions. List the ~15 openGraph openers + hsi-to-graph.cjs + hat-persistence.cjs as known violations that the Plan 03 baseline report will enumerate and that downstream phases own.

    Do NOT inline any code implementation. Reference identifiers, file paths, line numbers, function names, SQL statement shapes, and config keys in prose. No fenced code blocks containing implementations (a short illustrative SQL statement shape or a require() line as inline prose is acceptable; full functions are not).
  </action>
  <verify>
    <automated>test -f docs/architecture/SUBSTRATE-CONTRACT.md &amp;&amp; [ $(grep -vc '^#' docs/architecture/SUBSTRATE-CONTRACT.md) -ge 100 ] &amp;&amp; grep -q "navigation.cjs" docs/architecture/SUBSTRATE-CONTRACT.md &amp;&amp; grep -qi "supersede" docs/architecture/SUBSTRATE-CONTRACT.md &amp;&amp; grep -q "writeEdge" docs/architecture/SUBSTRATE-CONTRACT.md &amp;&amp; grep -qi "lazygraph-ops" docs/architecture/SUBSTRATE-CONTRACT.md &amp;&amp; grep -qi "brain-packet-schema" docs/architecture/SUBSTRATE-CONTRACT.md &amp;&amp; ! grep -q $'—' docs/architecture/SUBSTRATE-CONTRACT.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/architecture/SUBSTRATE-CONTRACT.md` exists with at least 100 non-comment lines.
    - The four-substrate table names Local SQLite via navigation.cjs, Aura Neo4j, Brain MCP, Pinecone with their access patterns.
    - M11 export allow-list lists every export key from `lib/core/navigation.cjs` module.exports by name (grep finds getActiveFocus, getNeighborhood, writeEdge, buildBrainPacket, promoteNodeStatus all present).
    - A reuse-vs-build decision block states check-substrate.cjs SUPERSEDES --check-chokepoint and gives the retirement plan (grep -i "supersede" matches).
    - At least two violation examples are present, one of which names lib/core/lazygraph-ops.cjs and its un-provenanced INSERT INTO nodes (grep -i "lazygraph-ops" matches).
    - The H5 note names data/brain-packet-schema.json unbounded summary/explanation strings as backlogged (grep -i "brain-packet-schema" matches).
    - A scope-boundary statement says Phase 128 is contract + guard, not migration (Phase 129/130 own the rewrites).
    - Zero em-dash characters (grep for U+2014 returns nothing).
    - No fenced code block contains a full function implementation.
  </acceptance_criteria>
  <done>The ADR codifies the 4-substrate table, M1-M4 + M11 mandates with the named export allow-list, the supersede decision, compliance/violation examples, the H5 backlog note, and the contract-not-migration scope boundary; em-dash-free.</done>
</task>

</tasks>

<verification>
- `docs/architecture/SUBSTRATE-CONTRACT.md` exists, >= 100 non-comment lines, em-dash-free.
- M11 export allow-list matches `grep -E "^\s+[a-zA-Z]+:" lib/core/navigation.cjs` key set by name.
- Reuse-vs-build decision is explicit (supersede, with retirement plan), not implicit.
- `docs/architecture/ROOM.md` exists (ICM Layer 0).
- Canon Part 8 LOCAL-only: the ADR is a doc; zero network surface introduced.
</verification>

<success_criteria>
- A different executor can read SUBSTRATE-CONTRACT.md and build the Plan 02 guard as a strict superset of --check-chokepoint without asking which-supersedes-which.
- The navigation.cjs door is auditable by exported function name (M11 closed).
- The H5 leak and the ~15 known violations are named, not silently dropped.
</success_criteria>

<output>
Create `.planning/phases/128-substrate-contract-adr/128-01-SUMMARY.md` when done.
</output>
