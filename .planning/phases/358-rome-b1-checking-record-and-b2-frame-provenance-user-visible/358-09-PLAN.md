---
phase: 358-rome-b1-checking-record-and-b2-frame-provenance-user-visible
plan: 09
type: execute
wave: 3
depends_on: ["358-07", "358-08"]
files_modified:
  - tests/test-358-b2-surfaces.cjs
  - tests/helpers/b2-358-child.cjs
  - tests/test-358-b2-persistence.cjs
  - lib/mcp/tools/claim-verify.cjs
  - lib/mcp/tools/question.cjs
autonomous: true
requirements: [B2-06, B2-08, B2-03]
canon_parts: [3, 8, 9, 11, 12]
tags: [b2, mcp, question-read, question-set, desktop, cowork, rome, tdd, cirs]
cirs_relationship:
  surfaces_added: ["question_read", "question_set"]
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["frameProvenance.setGoverningQuestion", "frameProvenance.readGoverningQuestion", "frameProvenance.readQuestionHistory", "frameProvenance.renderQuestionLines", "frameProvenance.renderHistoryLines", "frameProvenance.QUESTION_TURN_EXAMPLES", "navigation.FRAME_ORIGINS_ORDERED", "navigation.resolveByUser", "resolveEffectiveSessionId", "resolveSessionRoomDir", "openRoomDbForCaller", "isWritePathEnabled", "detectHostTier"]
  gate_impact: "question_read (hitl_shape none, layer harness) and question_set (hitl_shape F.1 for the question-change card, layer harness) are born wired: their connectors entries ship in lib/mcp/tools/question.cjs in the same commit, and lib/mcp/register-core-tools.cjs auto-discovers every lib/mcp/tools/*.cjs, so no router or registration edit. data/mcp-tool-connectors.json, data/connector-registry.json, data/connector-coverage-ledger.json and data/harness-manifest.json are regenerated by 358-10, never hand-edited here; from this plan's last commit until 358-10, build-connector-registry --check reports drift for exactly these two tools and the question.cjs ledger row, and test-270 toolCount and the test-276 frozen sweep are red. lib/mcp/tools/claim-verify.cjs only gains a hostBlock export."
  explanation: "Tri-Polar: officers on Claude Desktop and Cowork reach B2 only through MCP tools. A separate file keeps frames apart from claims (research Alternatives Considered), and a read tool separate from the write tool keeps reads working on hosts where writes are refused, the same reason claim_read is separate from claim_verify. Part 9: both tools call lib/core/frame-provenance.cjs, which writes only through navigation.cjs. Part 8: no Brain call. The descriptions are built from the one origin constant and the routing examples, so Larry reaches question_set when the officer states or changes the room's question and not for an ordinary question."

must_haves:
  truths:
    - "On Claude Desktop (clientInfo.name claude-ai) an officer records the room's governing question with its origin through question_set and reads it back with its origin, time and every earlier version through question_read, over the real stdio server with MINDRIAN_MCP_FIRST unset (B2-AT1, B2-AT2 on Desktop; Cowork through the same tools once its client name is probed or the documented env fallback is set; B2-06)"
    - "question_set refuses a changed question that brings neither the officer's account nor relocate:true: nothing is recorded, the waiting change is kept, and the response carries the ask 'What did the old question get wrong?', the previous and proposed question, the Decision Gate card and a next instruction that forbids the model from writing the account (B2-AT3 part a on MCP; B2-03)"
    - "question_read on any host, even one where writes are refused, shows the waiting change FIRST with its card, then the current question, then history (B2-AT3 part b on MCP)"
    - "With the account the change lands as refines and the stored account is echoed back so the officer sees it; with relocate:true it lands as relocates; both stay in history, neither ranked (B2-AT4 on MCP)"
    - "Close everything: set in one process and session, a refused change in a second, the waiting ask read in a third, refines in a fourth, relocates in a fifth, and a sixth process in a new session reads all three versions with origins, times, change kinds and the account intact; the parent reads room.db and the artifact files directly; the CLI script in a separate process shows the same history (B2-08, Tri-Polar parity)"
    - "question_read and question_set descriptions are at least 120 characters, under 2048 bytes, name all four origins from the constant, quote the routing examples, say not for an ordinary question, and scan OK in scripts/check-tool-honesty.cjs; an unknown host's question_set is refused with the host block and the MINDRIAN_MCP_FIRST hint (navigator requirement item 1, MCP half)"
  artifacts:
    - path: "lib/mcp/tools/question.cjs"
      provides: "question_read and question_set MCP tools plus both connectors entries"
      exports: ["register", "connectors"]
      contains: "question_set"
    - path: "lib/mcp/tools/claim-verify.cjs"
      provides: "hostBlock exported (one host detection path)"
      contains: "hostBlock"
    - path: "tests/test-358-b2-surfaces.cjs"
      provides: "stub-server legs and the real stdio wire leg as claude-ai for B2-06"
      min_lines: 220
    - path: "tests/helpers/b2-358-child.cjs"
      provides: "one-tool-call child process for the close-everything test"
      min_lines: 50
    - path: "tests/test-358-b2-persistence.cjs"
      provides: "B2-08 across six processes and five sessions, plus CLI parity"
      min_lines: 100
  key_links:
    - from: "lib/mcp/tools/question.cjs question_set handler"
      to: "lib/core/frame-provenance.cjs setGoverningQuestion"
      via: "after hostBlock(...).write_path_enabled and session room resolution"
      pattern: "frameProvenance\\.setGoverningQuestion\\("
    - from: "lib/mcp/tools/question.cjs question_read handler"
      to: "lib/core/frame-provenance.cjs readGoverningQuestion / readQuestionHistory"
      via: "no write gate (reads are unconditional)"
      pattern: "frameProvenance\\.readQuestionHistory\\("
    - from: "lib/mcp/register-core-tools.cjs"
      to: "lib/mcp/tools/question.cjs register"
      via: "auto-discovery of every tools/*.cjs"
      pattern: "server\\.tool\\('question_read'"
---

<objective>
Make B2 work for officers on Claude Desktop and Cowork through two MCP tools in a new lib/mcp/tools/question.cjs: question_read (read, no write gate) and question_set (write-gated exactly like claim_verify, refusal-as-ask with the Decision Gate card). Export claim-verify.cjs's hostBlock so there is one host detection path. Prove it over the real stdio server as the Claude Desktop client, and prove close-everything persistence with separate processes and sessions, plus CLI parity. Tests first.

Purpose: B2-06 and B2-08, and the MCP halves of B2-AT1..AT4, which the Rome officers drive hands-on. Also the MCP half of the navigator requirement (2026-09-23, item 1): the descriptions quote the same routing examples the spec in 358-08 tests, so Larry reaches the door when the officer states or changes the question and not otherwise.

Why this plan runs AFTER 358-08 (wave 3, not parallel with it): the moment lib/mcp/tools/question.cjs exists on disk, `node scripts/build-connector-registry.cjs --check` reports drift (it reads the working tree), and the pre-commit hook runs that check whenever commands/*.md, skills/*/SKILL.md or agents/*.md is staged. Built in parallel, 358-08's room.md commit would be blocked. The same window blocks peers staging those paths, so question.cjs is written only in this plan's LAST task, committed at once, and 358-10 regenerates immediately after. Transient drift, stated up front: after this plan, build-connector-registry --check, tests/test-270-tool-schema-budget.cjs (toolCount) and tests/test-276-tool-honesty-findings-closed.cjs (frozen sweep) stay red until 358-10. Do NOT regenerate data/*.json here. If a peer reports a commit blocked by that drift, run 358-10 Task 1 immediately.

Output: lib/mcp/tools/question.cjs, a one-line export change in lib/mcp/tools/claim-verify.cjs, three new test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-CONTEXT.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-B2-RESEARCH.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-07-SUMMARY.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-08-SUMMARY.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-04-SUMMARY.md

Interfaces this plan consumes (read lib/core/frame-provenance.cjs first and STOP if any name is missing): setGoverningQuestion, readGoverningQuestion, readQuestionHistory, renderQuestionLines, renderHistoryLines, QUESTION_ASK, QUESTION_CARD_OPTIONS, QUESTION_TURN_EXAMPLES, MAX_QUESTION_CHARS, MAX_ACCOUNT_CHARS; navigation.FRAME_ORIGINS_ORDERED, frameOriginInfo, resolveByUser, openRoomDbForCaller, closeRoomDbForCaller; lib/core/session-binding.cjs resolveEffectiveSessionId; lib/mcp/session-room.cjs resolveSessionRoomDir; lib/mcp/mcp-first-flag.cjs isWritePathEnabled; lib/mcp/surface-detect.cjs detectHostTier. A change_needs_account refusal carries {ask, previous, proposed, options, pending, card}; card carries {shape, header, options, rendered_text, askuserquestion_marker, askuserquestion_binding}.

Shape of the question-change card (flagged deviation, see 358-07 context): the navigator requirement named F.0. question_set declares and returns F.1 because the F.0 renderer is closed-vocab Approve / Reject / Defer with an ASCII border and writes the Reject reason into a graph edge property, which would put the account prose into graph metadata against locked B2-AT4. If the navigator overrides after reading the planning return, the connectors hitl_shape here and the requestedShape in renderQuestionChangeCard change together.

Facts verified at planning time: lib/mcp/register-core-tools.cjs requires every lib/mcp/tools/*.cjs (sorted) and calls its register(server, ctx); scripts/build-connector-registry.cjs collects each module's `connectors` export into data/mcp-tool-connectors.json and adds a mcp_tool_file:lib/mcp/tools/<file> row to data/connector-coverage-ledger.json; scripts/check-tool-honesty.cjs follows one hop into a called function (setGoverningQuestion carries the navigation write calls inline, per 358-07); claim-verify.cjs defines hostBlock(server, ctx) -> {client_name, host, host_tier, write_path_enabled} but does not export it; tests/test-358-b1-surfaces.cjs runWireLeg (lines 260-340) shows the hermetic stdio drive as claude-ai with MINDRIAN_ROOM, MINDRIAN_TRANSPORT stdio and MINDRIAN_MCP_FIRST deleted; test-234 requires every tool description >= 120 chars, capital first, sentence terminator last, < 2048 bytes, no em-dash.

Parallel-session rules (every task): record `PLAN_BASE=$(git rev-parse HEAD)`. Stage explicit paths only; commit with `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git add .`, `git commit -a`, `git stash`, `--no-verify`, or a gsd-tools commit that sweeps the index. Never revert or stage a file this plan does not own. Never touch: lib/mcp/tools/gate.cjs, lib/mcp/tools/sensors.cjs, lib/mcp/tools/status.cjs, lib/mcp/tools/views.cjs, lib/mcp/tool-router.cjs, lib/mcp/register-core-tools.cjs, lib/mcp/runtime-instructions.cjs, data/*.json, commands/*, skills/*, docs/reviews/*, evals/plurai/*, tests/test-353-* (run only), .planning/STATE.md, CLAUDE.md. No em-dashes. CJS only.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| Hardened `lib/core/navigation/typed-frame.cjs`; new `lib/core/frame-provenance.cjs` (the one door, readers, renders, the Decision Gate card); `lib/core/navigation.cjs` re-exports; regenerated `data/render-coverage-registry.json`; core, render and Part 8 tests; updated `tests/test-b2-frame-provenance.cjs`; `tests/run-all-358.sh` B2 legs | 07 |
| Routing spec in `lib/core/frame-provenance.cjs`; `scripts/room-question.cjs`; `/mos:room question` subcommands, routing section and tightened description; regenerated `skills/room/SKILL.md`; routing and CLI tests | 08 |
| `lib/mcp/tools/question.cjs` (question_read, question_set, connectors, descriptions from the origin constant and the routing examples, the card in every refusal); `hostBlock` export on `lib/mcp/tools/claim-verify.cjs`; `tests/test-358-b2-surfaces.cjs`, `tests/helpers/b2-358-child.cjs`, `tests/test-358-b2-persistence.cjs` | 09 (this plan) |
| `/mos:room` teaching line for Theo's command layer; regenerated `skills/room/SKILL.md`, `data/command-registry.json`, `data/mcp-tool-connectors.json`, `data/connector-registry.json`, `data/connector-coverage-ledger.json`, `data/harness-manifest.json`; test-270 AFTER re-baseline; 276 re-freeze; recorded command-registry sha256 | 10 |
| `docs/2026-10-06-ROME-B2-GO-NO-GO.md` + human checkpoint | 11 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - MCP surfaces test (stub server legs plus the real stdio wire as claude-ai)</name>
  <files>tests/test-358-b2-surfaces.cjs</files>
  <read_first>
    - tests/test-358-b1-surfaces.cjs (whole file: hermetic env, registerAndCapture with setClient, callTool, raw row reads, NEGATION_PATTERNS transcription, spawnWireClient, runWireLeg)
    - lib/mcp/tools/claim-verify.cjs (hostBlock, the write_path_disabled refusal and hint text, the response() helper, the connectors shape)
    - lib/core/frame-provenance.cjs (refusal shapes, QUESTION_TURN_EXAMPLES, QUESTION_CARD_OPTIONS)
    - scripts/check-tool-honesty.cjs (scanAll rows: tool, command, file, verdict)
  </read_first>
  <behavior>
    Hermetic env first (fresh HOME, MINDRIAN_HOME, MINDRIAN_ROOMS_HOME; delete MINDRIAN_MCP_FIRST, CLAUDE_ACTIVE_ROOM, MINDRIAN_BRAIN_KEY). Register through lib/mcp/register-core-tools.cjs with a stub server whose server.getClientVersion returns a settable client. Fixtures Q1 'Which camera solves the delay?', Q2 'Which camera works with sunglasses?', Q3 'Should the checkpoint move north?', A2 'It assumed the delay was the camera, but the officers wear sunglasses.'. Raw assertions re-open room.db through lib/core/room-db.cjs, never trusting a response's own ok.
    - A1: question_read and question_set are both registered; the registration report lists question.cjs with no failure.
    - A2 (schemas): question_set origin enum values equal navigation.FRAME_ORIGINS_ORDERED ids in order; text rejects 1001 characters; account rejects 4001 characters; relocate and cancel accept booleans only; based_on_version accepts positive integers only; question_read accepts {} and {history:false}.
    - A3 (descriptions): each at least 120 characters, starts with a capital, ends with . ! or ?, under 2048 bytes, no em-dash; question_set description names every origin id and every QUESTION_TURN_EXAMPLES.change phrase and contains 'not for an ordinary question'; question_read description names every QUESTION_TURN_EXAMPLES.ask phrase and ends with 'This tool writes nothing.'; neither matches /\b(better|improv\w*|upgrad\w*|abandon\w*|correct(ed|ion)|superseded)\b/i.
    - A4 (Desktop write): as {name:'claude-ai'} question_set {text:Q1, origin:'tasking'} returns ok true, result 'first', version 1, host.client_name 'claude-ai', host.host 'claude-desktop', host.write_path_enabled true.
    - A5 (the pause): question_set {text:Q2, origin:'chosen', based_on_version:1} returns ok false, reason change_needs_account, the tool response has NO isError flag (the pause is not a failure), ask exactly 'What did the old question get wrong?', previous.version 1, previous.text Q1, proposed.text Q2, pending true, card.shape 'F.1', card.options first three equal QUESTION_CARD_OPTIONS, card.askuserquestion_marker '[AskUserQuestion contract: shape=F.1 verbs=4]', rendered.card contains the ask, and next contains 'account', 'relocate' and 'Never write the account yourself'; the raw role row count is still 1.
    - A6 (waiting ask first): question_read as claude-ai AND as {name:'some-other-client'} returns ok true, rendered.question first line 'A question change is waiting.', pending.text Q2, pending.card.shape 'F.1', current.version 1, current.origin 'tasking', origins of length 4 with labels from frameOriginInfo.
    - A7 (refines): question_set {text:Q2, origin:'chosen', based_on_version:1, account:A2} returns ok true, result 'refines', version 2, account_text equal to A2 (echoed), rendered.question contains 'Change: refines version 1 (account on file)'; raw: a v2 frame row with change_kind refines and one REFINES edge v2 -> v1.
    - A8 (relocates): question_set {text:Q3, origin:'prompt', relocate:true} returns result 'relocates', version 3; raw: one FOLLOWS_FROM edge v3 -> v2.
    - A9 (unknown host refused): as {name:'some-other-client'} question_set {text:'Another question?', origin:'chosen', relocate:true} returns ok false, reason write_path_disabled, isError true, host.client_name 'some-other-client', hint containing 'MINDRIAN_MCP_FIRST'; raw role row count unchanged.
    - A10 (explicit choice only): account plus relocate returns ambiguous_change with isError true; after another refused change, {cancel:true} returns result 'cancelled' and question_read shows no pending.
    - A11 (Part 8 at the surface): every frame role row review_status 'proposed'; no nodes or edges property contains Q1, Q2, Q3 or A2.
    - A12 (static): lib/mcp/tools/question.cjs requires only zod, ../../core/navigation.cjs, ../../core/frame-provenance.cjs, ../../core/session-binding.cjs, ../session-room.cjs and ./claim-verify.cjs; no node:sqlite, room-db, brain or network module; non-comment lines contain no 'pickShape' and no quoted 'tasking' or 'inherited'; no em-dash; connectors: question_read hitl_shape 'none', question_set hitl_shape 'F.1', both layer 'harness', every hitl_why and layer_why non-empty; claim-verify.cjs exports a hostBlock function.
    - A13 (honesty): scanAll().rows has an OK verdict for tool question_read and for tool question_set.
    - B1 (wire): a real bin/mindrian-mcp-server.cjs child with the runWireLeg env pattern, initialized as clientInfo {name:'claude-ai'}: tools/list includes question_read and question_set; question_set Q1 tasking returns ok true; question_set Q2 chosen without account returns change_needs_account with a card; question_read returns rendered.question whose first line is 'A question change is waiting.'.
  </behavior>
  <action>
    Write tests/test-358-b2-surfaces.cjs to the behavior above, porting the harness pieces from tests/test-358-b1-surfaces.cjs (copy, never require another test file). Every expected value (origin ids, labels, card options, examples) is read from navigation.cjs or frame-provenance.cjs at test time. Run it: it must fail (question.cjs does not exist).
    Commit (RED): `git add tests/test-358-b2-surfaces.cjs` then `git commit --only -m "test(358-09): RED MCP surfaces for the governing question (question_read, question_set, wire as claude-ai)" -- tests/test-358-b2-surfaces.cjs`.
  </action>
  <verify>
    <automated>! node tests/test-358-b2-surfaces.cjs >/dev/null 2>&1 && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-358-b2-surfaces.cjs exists, has at least 220 lines, and exits non-zero now
    - `grep -c "some-other-client" tests/test-358-b2-surfaces.cjs` is at least 2 and `grep -c "claude-ai" tests/test-358-b2-surfaces.cjs` is at least 3
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-358-b2-surfaces.cjs` is 0
    - `git show --stat HEAD --format=%s` lists exactly the one test file
  </acceptance_criteria>
  <done>The Desktop / Cowork contract for the governing question is a failing test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED - close-everything persistence across processes and sessions, with CLI parity</name>
  <files>tests/helpers/b2-358-child.cjs, tests/test-358-b2-persistence.cjs</files>
  <read_first>
    - tests/helpers/b1-358-child.cjs (whole file: env handling before requires, registerAndCapture as claude-ai, callTool, emit one JSON line)
    - tests/test-358-b1-persistence.cjs (whole file: the separate-process sequence, CHILD_ENV, the parent's direct room.db read)
    - scripts/room-question.cjs (history --json output shape, from 358-08)
  </read_first>
  <behavior>
    - tests/helpers/b2-358-child.cjs: usage `node tests/helpers/b2-358-child.cjs <tool> <roomDir> <sessionId> [jsonArgs]` with tool question_set or question_read; deletes MINDRIAN_MCP_FIRST and CLAUDE_ACTIVE_ROOM before any require; registers through register-core-tools with a stub server answering as claude-ai and ctx {fallbackRoomDir: roomDir, pluginRoot, surface:'desktop'}; calls the tool with the parsed args and {sessionId}; prints exactly one JSON line {tool, ok, payload} where ok means a parseable payload came back (the parent asserts payload.ok); exit 0 when parseable, 1 otherwise. A fixture, never a test; no em-dashes.
    - tests/test-358-b2-persistence.cjs, every step a separate child process:
      P1 session S1: question_set {text:Q1, origin:'tasking'} -> payload result 'first'.
      P2 session S2: question_set {text:Q2, origin:'chosen', based_on_version:1} -> payload reason change_needs_account.
      P3 session S3: question_read -> rendered.question first line 'A question change is waiting.', pending.text Q2, current.version 1, current.origin 'tasking'.
      P4 session S3 (a new process): question_set {text:Q2, origin:'chosen', based_on_version:1, account:A2} -> result 'refines', version 2.
      P5 session S4: question_set {text:Q3, origin:'prompt', relocate:true} -> result 'relocates', version 3.
      P6 session S5: question_read -> versions of length 3 with origins [tasking, chosen, prompt], change kinds [null, refines, relocates], set_at ISO strings in non-decreasing order, version 2 account_text equal to A2, pending null, unresolved empty.
      P7 parent (after every child exited): open room.db with openRoomDb, read navigation.readGoverningQuestionVersions -> 3 rows, all review_status 'proposed'; each question_handle and the v2 refinement_handle exist on disk under <room>/.mindrian/frames and hold the expected text; closeRoomDb. Never trust a child's own success claim.
      P8 CLI parity (Tri-Polar): spawn `node scripts/room-question.cjs history --room <room> --json` -> the same three versions (version, origin, change_kind, set_at) as P6.
  </behavior>
  <action>
    Write both files to the behavior above (port shapes from the B1 helper and persistence test; the child env is shared by every step so all children resolve the same hermetic rooms home). Run the persistence test: it must fail (question.cjs does not exist).
    Commit (RED): `git add tests/helpers/b2-358-child.cjs tests/test-358-b2-persistence.cjs` then `git commit --only -m "test(358-09): RED close-everything persistence for the governing question across processes and sessions" -- tests/helpers/b2-358-child.cjs tests/test-358-b2-persistence.cjs`.
  </action>
  <verify>
    <automated>node -c tests/helpers/b2-358-child.cjs 2>/dev/null; ! node tests/test-358-b2-persistence.cjs >/dev/null 2>&1 && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; tests/test-358-b2-persistence.cjs exits non-zero now
    - `grep -c "room-question.cjs" tests/test-358-b2-persistence.cjs` is at least 1 (CLI parity leg)
    - `grep -c "readGoverningQuestionVersions" tests/test-358-b2-persistence.cjs` is at least 1 (parent reads the store directly)
    - `grep -c "$(printf '\xe2\x80\x94')" tests/helpers/b2-358-child.cjs tests/test-358-b2-persistence.cjs` prints 0 for both
    - `git show --stat HEAD --format=%s` lists exactly the two paths
  </acceptance_criteria>
  <done>"Close everything, reopen, the history is there" is a failing test across six processes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN - lib/mcp/tools/question.cjs and the hostBlock export; commit at once, then hand to 358-10</name>
  <files>lib/mcp/tools/question.cjs, lib/mcp/tools/claim-verify.cjs</files>
  <read_first>
    - lib/mcp/tools/claim-verify.cjs (whole file: response(), hostBlock, the write gate and hint, the read tool without a gate, connectors)
    - lib/core/frame-provenance.cjs (whole file)
    - tests/test-358-b2-surfaces.cjs and tests/test-358-b2-persistence.cjs (Tasks 1 and 2)
    - tests/test-234-tool-description-floor.cjs (lines 280-345: the prose-shape checks every tool receives)
  </read_first>
  <behavior>
    - tests/test-358-b2-surfaces.cjs and tests/test-358-b2-persistence.cjs exit 0.
    - tests/test-358-b1-surfaces.cjs, tests/test-234-tool-description-floor.cjs and tests/test-358-b2-part8.cjs (now 'part8 static: 4 of 4 B2 files scanned') exit 0.
  </behavior>
  <action>
    Precondition: `git status --short -- lib/mcp/` prints nothing.

    In lib/mcp/tools/claim-verify.cjs change ONLY the export line to `module.exports = { register, connectors, hostBlock };` and add one comment line above it: hostBlock is shared with lib/mcp/tools/question.cjs so there is one host detection path (358-09). Nothing else changes.

    Create lib/mcp/tools/question.cjs (CJS, 'use strict', header comment: Phase 358-09 (Rome B2), the Desktop / Cowork half of the governing question; question_read reads, question_set writes through lib/core/frame-provenance.cjs, the one door; neither ever confirms or ranks anything; Canon Part 9 writes only through navigation.cjs via the door; Canon Part 8 no Brain or network call; the card is built by the door, never here; no em-dashes). Requires: zod, ../../core/navigation.cjs, ../../core/frame-provenance.cjs, ../../core/session-binding.cjs (resolveEffectiveSessionId), ../session-room.cjs (resolveSessionRoomDir), ./claim-verify.cjs (hostBlock). A local response(payload, error) identical to claim-verify's.
    Build every list from the constants, never a literal: ORIGIN_IDS = navigation.FRAME_ORIGINS_ORDERED ids; ORIGIN_PHRASE = the ids joined as 'chosen, tasking, prompt or inherited' (comma list with ' or ' before the last); ORIGIN_DESCRIPTION = 'Where the question came from: ' + each 'id = label' joined by '; ' + '.'; CHANGE_EXAMPLES and ASK_EXAMPLES = frameProvenance.QUESTION_TURN_EXAMPLES.change / .ask each wrapped in double quotes and joined by ', '.
    QUESTION_READ_DESCRIPTION = "Show this room's governing question: where it came from (" + ORIGIN_PHRASE + "), when it was set, and every earlier version with its origin, time and whether each change refines or relocates. A waiting question change and its ask come first. Use when the officer asks about the room's question, for example " + ASK_EXAMPLES + '. This tool writes nothing.'
    QUESTION_SET_DESCRIPTION = "Record or change this room's governing question with its origin (" + ORIGIN_PHRASE + "). Use when the officer states or changes the room's question, for example " + CHANGE_EXAMPLES + ", not for an ordinary question asked in conversation. A changed question is recorded only with the officer's own words for what the old question got wrong (account, filed as refines) or the officer's explicit relocate (a new question, filed as relocates); without either, no version is recorded, the change waits as an ask and its card is returned. Both questions stay on the record and neither is ranked."
    NEXT_TEXT = "Fire the card, or on a host without AskUserQuestion ask the officer in plain words. Then call question_set again with the same text and origin plus account (the officer's own words, filed as refines), or relocate: true (the officer says it is a new question, filed as relocates), or cancel: true. Never write the account yourself."
    register(server, ctx):
    - question_read with schema {history: z.boolean().optional().describe('Set false to return only the current question and any waiting change. Defaults to true.')}. Handler: host = hostBlock(server, ctx); sessionId = resolveEffectiveSessionId(undefined, extra); roomDir = resolveSessionRoomDir(sessionId, ctx); db = navigation.openRoomDbForCaller(roomDir); no db -> response({ok:false, reason:'no_room_db', room_dir, host}, true); try: view = frameProvenance.readGoverningQuestion(db, roomDir); history = args.history === false ? null : frameProvenance.readQuestionHistory(db, roomDir); return response({ok:true, room_dir, pending: view.pending, current: view.current, total_versions: view.total_versions, versions: history ? history.versions : undefined, unresolved: view.unresolved, origins: FRAME_ORIGINS_ORDERED mapped to {id, label, position}, rendered: {question: renderQuestionLines(view).join('\n'), history: history ? renderHistoryLines(history).join('\n') : undefined}, note: 'Both questions stay on the record; neither is ranked.', host}); finally closeRoomDbForCaller. No write gate (reads are unconditional, like claim_read).
    - question_set with schema: text z.string().min(1).max(frameProvenance.MAX_QUESTION_CHARS).optional() ('The question exactly as the officer states it. Required unless cancel is true.'); origin z.enum(ORIGIN_IDS).optional() (ORIGIN_DESCRIPTION + ' Required unless cancel is true.'); account z.string().min(1).max(frameProvenance.MAX_ACCOUNT_CHARS).optional() ("The officer's own words for what the old question got wrong, exactly as said; files the change as refines. Never write this for the officer."); relocate z.boolean().optional() ('Set true only when the officer says it is a new question; files the change as relocates. Never set it by default.'); cancel z.boolean().optional() ('Set true to drop a waiting question change.'); based_on_version z.number().int().min(1).optional() ('The version the officer was looking at; if the question changed meanwhile, nothing is recorded.'). Handler: host = hostBlock(server, ctx); if !host.write_path_enabled return the claim_verify write_path_disabled refusal shape verbatim (ok false, reason, host, the same MINDRIAN_MCP_FIRST hint text) with isError; resolve session room and open the db as above; params = {text, origin, account, relocate, cancel, based_on_version, set_by_id: navigation.resolveByUser(roomDir)}; result = frameProvenance.setGoverningQuestion(db, roomDir, params). If result.reason === 'change_needs_account': return response({ok:false, reason, ask, previous, proposed, options, pending:true, card:{shape, header, options, askuserquestion_marker, askuserquestion_binding}, rendered:{card: result.card.rendered_text, question: renderQuestionLines(readGoverningQuestion(db, roomDir)).join('\n')}, next: NEXT_TEXT, room_dir, host}) WITHOUT isError (the pause is the normal flow, not a failure). Any other ok false: response(Object.assign({room_dir, host}, result), true). ok true: view = readGoverningQuestion; return response({ok:true, room_dir, result: result.result, version: result.version, account_text: result.account_text, edge: result.edge, question: view, rendered:{question: renderQuestionLines(view).join('\n')}, note: 'Both questions stay on the record; neither is ranked.', host}). finally closeRoomDbForCaller.
    connectors (born wired, Part 11): [{tool:'question_read', surface:'question_read', connector:'mcp-tool', hitl_shape:'none', hitl_why:"Pure read of the room's governing question, its origin and its history; no fork and no write.", layer:'harness', layer_why:'Local room read through the governing-question door and the navigation chokepoint.'}, {tool:'question_set', surface:'question_set', connector:'mcp-tool', hitl_shape:'F.1', hitl_why:'A changed question is one Decision Gate card: the officer writes what the old question got wrong (refines), says it is a new question (relocates), or cancels.', layer:'harness', layer_why:'Local room write through the one governing-question door and the navigation chokepoint.'}]. Add a comment above: data/*.json are regenerated by 358-10, never hand-edited here; F.1 not F.0 for the reason in the 358-07 plan. module.exports = { register, connectors }.

    Run: `node tests/test-358-b2-surfaces.cjs`, `node tests/test-358-b2-persistence.cjs`, `node tests/test-358-b1-surfaces.cjs`, `node tests/test-358-b1-persistence.cjs`, `node tests/test-234-tool-description-floor.cjs`, `node tests/test-358-b2-part8.cjs`, `node scripts/check-tool-honesty.cjs --report | grep -E "^\[[A-Z ]+\] question_(read|set)\."`. If either honesty row is not OK, fix the code shape or description (never add a disposition). If the description floor fails, fix the description.

    Commit AT ONCE (the drift window starts when question.cjs is on disk): `git add lib/mcp/tools/question.cjs lib/mcp/tools/claim-verify.cjs` then `git commit --only -m "feat(358-09): question_read and question_set MCP tools for the room's governing question" -- lib/mcp/tools/question.cjs lib/mcp/tools/claim-verify.cjs`. Then record in the SUMMARY the output of `node scripts/build-connector-registry.cjs --check` (expected: drift naming only mcp:question_read, mcp:question_set and the question.cjs ledger row) and of `bash tests/run-all-358.sh; echo "exit=$?"` (expected: exit 1 with exactly these red legs: connector registry --check, test-270 tool schema budget, test-276 tool honesty findings closed, and the connector coverage leg if it compares against the data files; every B2 leg PASSED). Hand off to 358-10 immediately.
  </action>
  <verify>
    <automated>node tests/test-358-b2-surfaces.cjs >/dev/null && node tests/test-358-b2-persistence.cjs >/dev/null && node tests/test-358-b1-surfaces.cjs >/dev/null && node tests/test-234-tool-description-floor.cjs >/dev/null && node tests/test-358-b2-part8.cjs | grep -q "part8 static: 4 of 4 B2 files scanned" && test "$(node scripts/check-tool-honesty.cjs --report 2>/dev/null | grep -cE '^\[OK\] question_(read|set)\.')" = 2 && echo GREEN-OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-358-b2-surfaces.cjs, tests/test-358-b2-persistence.cjs, tests/test-358-b1-surfaces.cjs, tests/test-358-b1-persistence.cjs and tests/test-234-tool-description-floor.cjs each exit 0
    - `node tests/test-358-b2-part8.cjs | grep -c "part8 static: 4 of 4 B2 files scanned"` is 1
    - `node scripts/check-tool-honesty.cjs --report 2>/dev/null | grep -cE '^\[OK\] question_(read|set)\.'` is 2
    - `git diff "$PLAN_BASE" -- lib/mcp/tools/claim-verify.cjs | grep '^[-+]' | grep -v '^[-+][-+]' | wc -l` is at most 3 (the export line and one comment)
    - `grep -c "hitl_shape: 'F.1'" lib/mcp/tools/question.cjs` is 1 and `grep -c "hitl_shape: 'none'" lib/mcp/tools/question.cjs` is 1
    - `git status --short -- data/` prints nothing (no registry regenerated here)
    - `git show --stat HEAD --format=%s` lists exactly lib/mcp/tools/question.cjs and lib/mcp/tools/claim-verify.cjs
    - The SUMMARY records the connector-registry --check drift lines and the runner's red legs, each attributed to the pending 358-10 regenerate
    - `grep -c "$(printf '\xe2\x80\x94')" lib/mcp/tools/question.cjs lib/mcp/tools/claim-verify.cjs` prints 0 for both
  </acceptance_criteria>
  <done>Officers on Desktop and Cowork can read and record the room's question, a change waits for their own account or an explicit relocate, and the history survives closing everything.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host (Desktop / Cowork / CLI MCP) -> question_set | the model passes officer text; the host identity decides whether writes are allowed |
| question_set -> frame-provenance.cjs | validated values cross into the one door |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-44 | Elevation of privilege | an unrecognized host writing the room's question | mitigate | question_set checks hostBlock(...).write_path_enabled first (the same isWritePathEnabled gate as claim_verify); A9 proves an unknown host is refused and nothing is written |
| T-358-45 | Repudiation | the model fabricates the officer's account | mitigate | account schema and NEXT_TEXT say the officer's own words, never written by the model; the stored account is echoed back so the officer sees it; residual (MCP cannot prove who typed) accepted and stated in the 358-11 runbook |
| T-358-46 | Denial of service | oversized inputs | mitigate | zod caps text at 1000 and account at 4000 characters; the door re-validates |
| T-358-47 | Information disclosure | host block leaking more than a client name | accept | client_name is truncated to 64 characters and is already exposed by claim_read; no room content in it |
| T-358-48 | Tampering | a silent relocate chosen by the model | mitigate | relocate must be an explicit true; its description says never by default; account plus relocate is refused |
</threat_model>

<verification>
- `node tests/test-358-b2-surfaces.cjs && node tests/test-358-b2-persistence.cjs`
- Regression: `node tests/test-358-b1-surfaces.cjs && node tests/test-358-b1-persistence.cjs && node tests/test-234-tool-description-floor.cjs`
- `node scripts/check-tool-honesty.cjs --report` shows OK for question_read and question_set
- Expected red until 358-10 (recorded, not fixed here): build-connector-registry --check, test-270 budget, test-276 frozen sweep
</verification>

<success_criteria>
- B2-06: question_read and question_set born wired with declared shapes, write-gated like claim_verify, Desktop claude-ai enabled
- B2-08: close-everything persistence across six processes and five sessions, parent-verified, CLI parity
- B2-03 on MCP: the refusal is the ask with the card, and question_read shows it first on every host
- Navigator requirement item 1 (MCP half): descriptions quote the routing examples and exclude ordinary questions
</success_criteria>

<output>
Create `.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-09-SUMMARY.md` when done (stage it with `git add -f` and commit it with `git commit --only`).
</output>
