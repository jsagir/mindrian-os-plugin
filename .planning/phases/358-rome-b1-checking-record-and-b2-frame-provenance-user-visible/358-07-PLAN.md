---
phase: 358-rome-b1-checking-record-and-b2-frame-provenance-user-visible
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-358-b2-core.cjs
  - tests/test-358-b2-render.cjs
  - tests/test-358-b2-part8.cjs
  - tests/run-all-358.sh
  - lib/core/navigation/typed-frame.cjs
  - lib/core/navigation.cjs
  - tests/test-b2-frame-provenance.cjs
  - lib/core/frame-provenance.cjs
  - data/render-coverage-registry.json
autonomous: true
requirements: [B2-01, B2-02, B2-03, B2-04, B2-07, B2-09, B2-10]
canon_parts: [3, 8, 9, 12]
tags: [b2, frame-provenance, governing-question, core, card, rome, tdd]

must_haves:
  truths:
    - "A room has ONE governing question record, separate from the MINTO governing_thought (ruling 2): every version is a `frame` node with frame_role governing_question, an integer version, a REQUIRED origin from the one FRAME_ORIGINS_ORDERED constant marked TODO(358) (ruling 3), the sha256 of the normalized question, a question artifact handle under <room>/.mindrian/frames/ (ruling 5), and its set time from the created_at column (B2-01)"
    - "setGoverningQuestion in lib/core/frame-provenance.cjs is the only writer of that record (ruling 2, one door): the same normalized text writes nothing (unchanged); a changed question with neither account nor relocate is REFUSED with ask 'What did the old question get wrong?', the previous and proposed question, a pending marker file, and the question-change card; with the officer's account it lands as refines (account filed as an artifact, only its handle and hash on the node); with relocate:true it lands as relocates; account plus relocate together is refused as ambiguous_change; a missing choice never defaults to relocates (B2-02, B2-03)"
    - "The question-change ask is ONE Decision Gate card rendered through the SEED-020 door (lib/hmi/selector-dispatcher.cjs pickShape, never a hand-built widget and never an ASCII box): header 'What did the old question get wrong?', options 'Write what it got wrong (files as refines)', 'It is a new question (files as relocates)', 'Cancel the change', the renderer's Free-Text row last, no option marked recommended; its single pickShape call site lives in lib/core/frame-provenance.cjs and is COVERED in data/render-coverage-registry.json (navigator requirement 2026-09-23, item 1)"
    - "A written version never changes: re-writing an existing version id is refused (version_exists) and leaves the row byte-identical; no frame is ever marked superseded; the only edges are REFINES new -> prior and FOLLOWS_FROM new -> prior with {change_kind:'relocates'}, never SUPERSEDES (B2-07)"
    - "readGoverningQuestion and readQuestionHistory return the waiting change FIRST (with its card), then the current question with origin, origin label and time, then every version oldest first with its change kind and, for refines, the account text; renderQuestionLines / renderHistoryLines give every version the same line layout, never use ranking words, never say governing thought, and report an out-of-band edit or missing file as Unresolved (B2-04)"
    - "No question or account text appears in any node or edge property; no B2 file directly requires a Brain module or a network module; a full set / refuse / refines / relocates / read cycle, card included, makes zero network calls; a question change leaves the B1 room portrait counts unchanged (B2-09)"
    - "FUSION's readOpenFrames never returns a governing-question version, so tests/test-205-frame-node.cjs, tests/test-205-fusion-router.cjs and tests/test-348-one-supersession-door.cjs stay green"
    - "tests/run-all-358.sh is re-opened ONCE on purpose: it names every B2 test file up front (core, render, part8, routing, cli, surfaces, persistence), runs the frame and supersession regression tests, widens the CIRS glob to 358-[0-9][0-9]-PLAN.md and the em-dash glob to test-358-b[12]-*.cjs, and lists every B2 non-test file in PHASE_358_SURFACES (B2-10)"
  artifacts:
    - path: "lib/core/frame-provenance.cjs"
      provides: "the one door: setGoverningQuestion, readGoverningQuestion, readQuestionHistory, renderQuestionLines, renderHistoryLines, renderQuestionChangeCard, questionHash, normalizeQuestion, QUESTION_ASK, QUESTION_CARD_OPTIONS, FRAMES_DIR_REL"
      exports: ["setGoverningQuestion", "readGoverningQuestion", "readQuestionHistory", "renderQuestionLines", "renderHistoryLines", "renderQuestionChangeCard", "questionHash", "normalizeQuestion", "QUESTION_ASK", "QUESTION_CARD_OPTIONS", "FRAMES_DIR_REL"]
      min_lines: 250
    - path: "lib/core/navigation/typed-frame.cjs"
      provides: "hardened frame substrate: FRAME_ORIGINS_ORDERED (TODO(358)), frameOriginInfo, role governing_question write rules, readGoverningQuestionVersions, ordered readFrameProvenance, readOpenFrames exclusion"
      contains: "TODO(358)"
    - path: "lib/core/navigation.cjs"
      provides: "additive re-exports FRAME_ORIGINS_ORDERED, frameOriginInfo, GOVERNING_QUESTION_ROLE, GOVERNING_QUESTION_NODE_ID, readGoverningQuestionVersions"
      contains: "readGoverningQuestionVersions: typedFrame.readGoverningQuestionVersions"
    - path: "data/render-coverage-registry.json"
      provides: "regenerated render registry carrying the frame-provenance pickShape entry as card-emission"
      contains: "lib/core/frame-provenance.cjs"
    - path: "tests/test-358-b2-core.cjs"
      provides: "substrate (S) and door (D) legs for B2-01, B2-02, B2-03, B2-07"
      min_lines: 200
    - path: "tests/test-358-b2-render.cjs"
      provides: "reader, render and card legs for B2-04 and the Decision Gate card"
      min_lines: 120
    - path: "tests/test-358-b2-part8.cjs"
      provides: "Part 8 prose scan, static require scan, one-door scan, zero-network cycle, portrait-unchanged legs for B2-09"
      min_lines: 100
    - path: "tests/run-all-358.sh"
      provides: "phase runner with B2 legs"
      contains: "test-358-b2-core.cjs"
  key_links:
    - from: "lib/core/frame-provenance.cjs setGoverningQuestion"
      to: "navigation.writeFrameNode"
      via: "inside a BEGIN IMMEDIATE window owned only when db.isTransaction !== true"
      pattern: "navigation\\.writeFrameNode\\("
    - from: "lib/core/frame-provenance.cjs setGoverningQuestion"
      to: "navigation.writeEdge (REFINES or FOLLOWS_FROM)"
      via: "new version -> prior version, enum-only props"
      pattern: "navigation\\.writeEdge\\("
    - from: "lib/core/frame-provenance.cjs renderQuestionChangeCard"
      to: "lib/hmi/selector-dispatcher.cjs pickShape"
      via: "the SEED-020 single card-emission door, shape F.1, tier 2, recommendedVerb null"
      pattern: "pickShape\\("
    - from: "lib/core/frame-provenance.cjs"
      to: "lib/core/room-path-containment.cjs writeFileContained"
      via: "question, account and pending artifacts under .mindrian/frames/"
      pattern: "writeFileContained\\("
    - from: "lib/core/fusion-router.cjs readOpenFrames"
      to: "lib/core/navigation/typed-frame.cjs readOpenFrames"
      via: "SQL filter excluding frame_role governing_question"
      pattern: "governing_question"
---

<objective>
Build the B2 core: ONE explicit, room-level governing question record with ONE write door (navigator ruling 2), over a hardened frame substrate, whose pause is a real Decision Gate card. Harden lib/core/navigation/typed-frame.cjs (required origin from one TODO(358) constant, immutable versions, ordered readers with times, FUSION exclusion), add lib/core/frame-provenance.cjs (set / read / history / pending / render / the question-change card through the SEED-020 pickShape door), re-export the new substrate names through lib/core/navigation.cjs, regenerate the render-coverage registry so the card is covered, and re-open tests/run-all-358.sh once with every B2 leg named up front. Tests first.

Purpose: this is the whole data contract for slide A1. B2-AT1 (origin shown), B2-AT2 (history), B2-AT3 (the door refuses a changed question until the officer answers what the old question got wrong, the ask waits across sessions, and it is fired as one card) and B2-AT4 (refines with an account artifact, relocates without one, both kept, neither ranked) are all decided here. The CLI (358-08) and MCP (358-09) plans are thin callers of this module.

Output: two library files hardened or created, one navigation re-export block extended, the render-coverage registry regenerated, three new test files, one updated substrate test, the re-opened runner.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-CONTEXT.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-B2-RESEARCH.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-01-SUMMARY.md

Navigator rulings this plan implements (358-CONTEXT.md b2_rulings, LOCKED 2026-09-23):
- Ruling 2: the governing question is a NEW explicit room-level record with ONE write door, lib/core/frame-provenance.cjs. It is NOT the MINTO governing_thought (a machine-written section summary). Do not touch MINTO, brain-derivation*, brain-md-staleness, navigation-engine*, sensors, gate, status, or lib/mcp/runtime-instructions.cjs.
- Ruling 3: origin is REQUIRED on every version. The four labels (chosen, tasking, prompt, inherited) live in ONE exported constant, FRAME_ORIGINS_ORDERED, marked `TODO(358)` until the paper author confirms the definitions. Same idiom as B1's VERIFICATION_RUNGS (lib/core/navigation/verification.cjs:30-40).
- Ruling 5: question, account and pending artifacts live under `<room>/.mindrian/frames/` (lib/vault/room-scanner.cjs:42 already skips `.mindrian`, so no section, no MINTO, no ROOM.md doctor noise).
- Ruling 4: Phase 345 goal.parent_question is untouched (lib/hmi/jtbd-state.cjs, lib/core/strategy/* are out of scope).
- Ruling 6: the chain_run pending-change halt is NOT in this plan set.

Navigator requirement 2026-09-23 (relayed by the coordinator), item 1 as it lands in THIS plan: the refused change is a Decision Gate rendered as an AskUserQuestion card ("What did the old question get wrong?" -> refines with account / relocates / cancel), never an ASCII box (SEED-021), and render-coverage-registry must cover it. SHAPE NOTE (deviation, flagged to the navigator in the planning return): the requirement named shape F.0. This plan renders the card as F.1 through the same pickShape door because lib/hmi/shape-f0-renderer.cjs is closed-vocab (exactly Approve / Reject / Defer, caller verbs ignored), draws a single-line ASCII border, and captures the Reject reason as a REJECTED_BECAUSE typed edge property: that would put the officer's account prose into graph metadata, breaking locked B2-AT4 ("account stored as an artifact handle, never free text in graph metadata"), and would show Approve / Reject / Defer instead of the three answers. F.1 is open-vocabulary, carries no box characters, keeps Free-Text last (the officer can type the account directly) and supports no recommended option (neither answer is presented as better). Switching to F.0 later is one token in renderQuestionChangeCard plus the 358-09 connectors entry, with the consequences above.

Parallel-session rules (every task): peers execute Phases 355, 357, 359, 360, 361 in this same working tree. Record `PLAN_BASE=$(git rev-parse HEAD)` at start. Stage explicit paths only and commit with `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git add .`, `git commit -a`, `git stash`, `--no-verify`, or any gsd-tools commit that sweeps the index. Never revert or stage a file this plan does not own. Never touch: docs/reviews/*, evals/plurai/*, scripts/eval-icm-writers.cjs, scripts/jev-devtime-client.cjs, tests/test-353-* (run only), .planning/phases/356-*, data/jev-policies/, .planning/STATE.md, lib/mcp/tools/gate.cjs, lib/mcp/tools/sensors.cjs, lib/mcp/tool-router.cjs, lib/mcp/runtime-instructions.cjs, lib/hmi/* (read only), lib/core/fusion-router.cjs (read only), CLAUDE.md. docs/FRAME-PROVENANCE-PRODUCT-WORKUP.md is PEER-UNTRACKED: read only, never stage. No em-dashes anywhere (hyphens only). CJS only.

Substrate facts (verified at planning time, HEAD eb393d9a0):
- typed-frame.cjs: FRAME_ORIGINS is a Set literal at :60; writeFrameNode :119-199 silently DROPS an invalid origin and still returns ok (:140); insertNode is called with the default on_conflict 'update' so re-writing a version overwrites it (:166-172); readFrameProvenance :213-237 has no ORDER BY and no created_at; readOpenFrames :259-305 returns every frame row, and lib/core/fusion-router.cjs:119 calls it with sessionId '' so governing-question versions would appear as FUSION open frames.
- lib/core/node-insert.cjs insertNode(conn, id, type, properties, overrides): overrides.on_conflict accepts 'update' or 'nothing'; created_at is Date.now() milliseconds and is preserved on conflict; epistemic_type is REQUIRED.
- lib/core/navigation/edges.cjs writeEdge(db, {source_id, target_id, edge_type, properties}); REFINES and FOLLOWS_FROM are in ALLOWED_EDGE_TYPES and not deprecated; an edge failure returns {ok:false} and never throws.
- lib/core/room-path-containment.cjs exports assertRealpathContained(roomDir, absPath, label) and writeFileContained(roomDir, absFilePath, content) (atomic temp plus rename, never follows a symlink leaf).
- lib/hmi/selector-dispatcher.cjs pickShape({requestedShape:'F.1', roomDir:null, operator:null, tier, payload:{header, verbs, recommendedVerb}}) returns {shape:'F.1', rendered:{zones:{header, body, signals, footer}, contract:{verbs, recommended, ...}, askuserquestion_marker, askuserquestion_binding}}. With tier 1 (the tier-check.cjs default when MINDRIAN_BRAIN_KEY is unset, which is the silent-registration norm) it prepends "Brain unreachable; running on local graph only." to the header; with an explicit tier 2 it does not. The card is local-only and says nothing about the Brain, so pass tier 2 with recommendedVerb null (no option marked recommended). Measured: F.1 body rows use the triangle glyph and no box characters.
- scripts/build-render-coverage.cjs walks every non-test .cjs under lib/ and scripts/ for a `pickShape(` call site (comments ignored) and records it; scripts/check-render-coverage.cjs --check byte-compares data/render-coverage-registry.json and classifies a pickShape entry as covered. The pre-commit hook runs check-render-coverage --check whenever any lib/core/*.cjs is staged, so the commit that adds the pickShape call site MUST include the regenerated registry. Both checks exit 0 at planning time.
- scripts/check-substrate.cjs: lib/core/frame-provenance.cjs is NOT on the allow-list, so it must not require node:sqlite, room-db.cjs, lazygraph-ops.cjs or memory-ops.cjs and must not contain raw INSERT / UPDATE / DELETE on nodes or edges. All SQL lives in typed-frame.cjs (allow-listed as lib/core/navigation/). Transaction control (`db.exec('BEGIN IMMEDIATE')`, COMMIT, ROLLBACK on the caller-owned handle) is allowed and mirrors verification.cjs:182-232.
- scripts/check-tool-honesty.cjs resolves ONE hop into a called function. 358-09's question_set handler calls setGoverningQuestion, so the `navigation.writeFrameNode(` and `navigation.writeEdge(` calls MUST sit inline in setGoverningQuestion's own body, not in a helper it calls.
- navigation.cjs loads lib/core/brain-client.cjs transitively (measured), so B2-09 is proven by a DIRECT-require scan plus a zero-network runtime cycle, not by a require.cache scan.
- tests/test-b2-frame-provenance.cjs, tests/test-205-frame-node.cjs, tests/test-205-fusion-router.cjs, tests/test-348-one-supersession-door.cjs all exit 0 at planning time.

<interfaces>
Contract this plan creates (358-08 and 358-09 build against exactly these names and shapes):

lib/core/navigation/typed-frame.cjs (re-exported through navigation.cjs):
- FRAME_ORIGINS_ORDERED: frozen array of frozen {id, label} in this order: chosen "chosen by the officer", tasking "handed down as a tasking", prompt "suggested in conversation with the tool", inherited "carried over from earlier work". FRAME_ORIGINS stays a Set, now DERIVED from these ids (one source).
- frameOriginInfo(id) -> {id, label, position (1-based)} or null.
- GOVERNING_QUESTION_ROLE = 'governing_question'; GOVERNING_QUESTION_NODE_ID(version) -> FRAME_NODE_ID('room', 'governing-question:v' + version).
- writeFrameNode(db, params) gains a role branch when params.frameRole === GOVERNING_QUESTION_ROLE, with params {version, origin, governingThoughtHash, questionHandle, predecessorHash, predecessorNodeId, changeKind, refinementHandle, accountHash, setBy, setById}. Refusal reasons: taxonomy_not_allowed, invalid_origin, invalid_version, invalid_question_hash, invalid_question_handle, invalid_change_kind, missing_predecessor, refines_needs_account_handle, relocates_has_account, version_exists. Success: {ok:true, node_id, type:'frame', version}.
- readGoverningQuestionVersions(db) -> array oldest first of {node_id, version, origin, question_hash, question_handle, predecessor_hash, predecessor_node_id, change_kind, refinement_handle, account_hash, set_by, set_by_id, review_status, created_at (ms), set_at (ISO string)}; [] on any failure.

lib/core/frame-provenance.cjs (required directly by callers; NOT re-exported from navigation.cjs, which would be a require cycle):
- QUESTION_ASK = 'What did the old question get wrong?'; FRAMES_DIR_REL = '.mindrian/frames'; QUESTION_CARD_OPTIONS = frozen ['Write what it got wrong (files as refines)', 'It is a new question (files as relocates)', 'Cancel the change'].
- normalizeQuestion(text) -> trimmed, internal whitespace collapsed to one space, NFC, case kept. questionHash(text) -> 'sha256:' + 64 hex of the normalized text.
- renderQuestionChangeCard({previous, proposed}) -> {shape, header, options, rendered_text, askuserquestion_marker, askuserquestion_binding}: header is QUESTION_ASK on line 1, then 'Old question (version N): <text>' and 'New question: <text>'; options = the renderer's contract.verbs (the three QUESTION_CARD_OPTIONS then the renderer's Free-Text row); rendered_text = zones.header + '\n' + zones.body + '\n' + zones.footer.
- setGoverningQuestion(db, roomDir, params) with params {text, origin, account, relocate, cancel, based_on_version, set_by_id}. Results: {ok:true, result:'first'|'refines'|'relocates', version, node_id, question_handle, account_handle (refines only), account_text (refines only, echoed so the officer sees what was stored), edge:{ok, type} or null, pending_cleared}; {ok:true, result:'unchanged', version}; {ok:true, result:'cancelled'}. Refusals {ok:false, reason, ...}: invalid_params, no_room_dir, invalid_text, invalid_origin (+ allowed: ids), invalid_account, ambiguous_change, no_previous_question, nothing_pending, change_needs_account (+ ask, previous {version, text, origin, origin_label}, proposed {text, origin, origin_label}, options, pending:true, card), question_changed_meanwhile (+ current), artifact_write_failed (+ detail), version_write_failed (+ detail).
- readGoverningQuestion(db, roomDir) -> {ok:true, pending, current, total_versions, unresolved}. pending is null or {text, origin, origin_label, based_on_version, at, ask, card}. current is null or {version, text, origin, origin_label, set_at, change_kind, from_version}. unresolved is an array of {version, what:'question'|'account', reason:'file_changed'|'file_missing'|'invalid_handle'}.
- readQuestionHistory(db, roomDir) -> {ok:true, pending, versions, unresolved}; each version {version, text, origin, origin_label, set_at, change_kind, from_version, account_text (refines only), is_current}.
- renderQuestionLines(view) and renderHistoryLines(history) -> string[] (exact lines in Task 1 behavior).
- 358-08 adds QUESTION_TURN_PATTERNS, QUESTION_TURN_EXAMPLES and resolveQuestionTurn to this same file (additive); nothing in this plan pre-empts those names.
</interfaces>
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| Hardened `lib/core/navigation/typed-frame.cjs` (FRAME_ORIGINS_ORDERED with TODO(358), frameOriginInfo, role governing_question write rules, readGoverningQuestionVersions, ordered readFrameProvenance, readOpenFrames exclusion); new `lib/core/frame-provenance.cjs` (setGoverningQuestion, readGoverningQuestion, readQuestionHistory, renderQuestionLines, renderHistoryLines, renderQuestionChangeCard, questionHash, normalizeQuestion, QUESTION_ASK, QUESTION_CARD_OPTIONS, FRAMES_DIR_REL); `lib/core/navigation.cjs` re-exports; regenerated `data/render-coverage-registry.json` (card covered); `tests/test-358-b2-core.cjs`, `tests/test-358-b2-render.cjs`, `tests/test-358-b2-part8.cjs`; updated `tests/test-b2-frame-provenance.cjs`; `tests/run-all-358.sh` B2 legs | 07 (this plan) |
| Routing spec in `lib/core/frame-provenance.cjs` (QUESTION_TURN_PATTERNS, QUESTION_TURN_EXAMPLES, resolveQuestionTurn); `scripts/room-question.cjs` (show, history, set, cancel, origins, help); `/mos:room question`, `question history`, `question set <text>`, `question cancel` with the Larry trigger line and tightened description; regenerated `skills/room/SKILL.md`; `tests/test-358-b2-routing.cjs`, `tests/test-358-b2-cli.cjs` | 08 |
| `lib/mcp/tools/question.cjs` (question_read, question_set, connectors, descriptions built from the origin constant and the routing examples, the card in every refusal); `hostBlock` export on `lib/mcp/tools/claim-verify.cjs`; `tests/test-358-b2-surfaces.cjs`, `tests/helpers/b2-358-child.cjs`, `tests/test-358-b2-persistence.cjs` | 09 |
| `/mos:room` teaching line for Theo's command layer; regenerated `skills/room/SKILL.md`, `data/command-registry.json`, `data/mcp-tool-connectors.json`, `data/connector-registry.json`, `data/connector-coverage-ledger.json`, `data/harness-manifest.json`; test-270 AFTER re-baseline (plan 358-10); 276 honesty sweep re-freeze; recorded command-registry sha256 for the Theo check | 10 |
| `docs/2026-10-06-ROME-B2-GO-NO-GO.md` (operator runbook: AT1-AT4 on CLI / Desktop / Cowork, relevance probe, Theo command_neighborhood check after release + resync, covered / not-covered answer paths, ruled AT3 wording) + human checkpoint | 11 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - core, render and Part 8 tests for the governing question and its card; re-open the phase runner once</name>
  <files>tests/test-358-b2-core.cjs, tests/test-358-b2-render.cjs, tests/test-358-b2-part8.cjs, tests/run-all-358.sh</files>
  <read_first>
    - lib/core/navigation/typed-frame.cjs (whole file: writeFrameNode, classifyFrameChange, readFrameProvenance, readOpenFrames)
    - tests/test-b2-frame-provenance.cjs (the in-memory schema used there)
    - tests/test-358-b1-core.cjs (lines 1-80: hermetic HOME, temp room via lib/core/room-db.cjs openRoomDb/closeRoomDb, the check() counter and exit code)
    - tests/test-358-b1-portrait.cjs (how readVerificationPortrait is called on a seeded room)
    - lib/hmi/selector-dispatcher.cjs (lines 960-1010 pickShape, and the F.1 branch near line 778; read only)
    - tests/run-all-358.sh (whole file: run/run_if, the CIRS glob at the find with 358-0[1-6], PHASE_358_SURFACES, the em-dash glob test-358-b1-*.cjs)
    - scripts/check-substrate.cjs (lines 195-310: scanFiles(files, readContent) export, the rule regexes)
    - .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-B2-RESEARCH.md (Validation Architecture, Pitfalls 1-11)
  </read_first>
  <behavior>
    Common to all three files: hermetic env set BEFORE any lib require (fresh mkdtemp HOME, MINDRIAN_HOME, MINDRIAN_ROOMS_HOME; delete CLAUDE_ACTIVE_ROOM, MINDRIAN_MCP_FIRST and MINDRIAN_BRAIN_KEY); temp rooms made with openRoomDb/closeRoomDb then reopened per leg; frame-provenance.cjs required inside a try so a missing module is one FAIL line, not a crash; output lines `ok - <label>` / `FAIL - <label>`; labels in test-358-b2-core.cjs start with `S<n>:` for substrate legs (typed-frame through navigation.cjs) and `D<n>:` for door legs (frame-provenance.cjs); final line `passed=N failed=M`; exit 1 when any FAIL. Fixture texts: Q1 'Which camera solves the delay?', Q2 'Which camera works with sunglasses?', Q3 'Should the checkpoint move north?', A2 'It assumed the delay was the camera, but the officers wear sunglasses.'

    tests/test-358-b2-core.cjs:
    - S1: navigation.FRAME_ORIGINS_ORDERED ids are exactly chosen, tasking, prompt, inherited in that order, each with a non-empty label; navigation.FRAME_ORIGINS equals the Set of those ids; navigation.frameOriginInfo('tasking').position is 2; frameOriginInfo('bogus') is null.
    - S2: writeFrameNode role write with origin missing or 'bogus' returns {ok:false, reason:'invalid_origin'} and writes no row; taxonomy:true returns taxonomy_not_allowed; version 0 or 1.5 returns invalid_version; version 2 with changeKind 'refines' and no refinementHandle returns refines_needs_account_handle; version 2 with no changeKind returns invalid_change_kind; version 1 with a changeKind returns invalid_change_kind.
    - S3: a valid role write for version 1 lands node id navigation.GOVERNING_QUESTION_NODE_ID(1), type frame, review_status proposed, source_path 'frame:room:governing-question', props frame_role governing_question, version 1, origin; a second role write for version 1 with a different origin returns version_exists and the raw row (properties, created_at, review_status, read through the handle's prepare()) is byte-identical to before.
    - S4: readGoverningQuestionVersions returns versions oldest first with set_at an ISO string and created_at a number; readFrameProvenance returns version, set_at and question_handle for role rows and still returns plain frames ordered by insertion.
    - S5: readOpenFrames (required from lib/core/navigation/typed-frame.cjs, the same path fusion-router.cjs uses) with sessionId '' returns a plain FUSION frame written with writeFrameNode({frameKey:'fusion-a', sessionId:'s1', members:['section:a']}) and returns NO row with frame_role governing_question.
    - D1: first setGoverningQuestion({text:Q1, origin:'tasking'}) returns result 'first', version 1; the question file exists at <room>/<question_handle>, its handle matches /^\.mindrian\/frames\/q-v1-[0-9a-f]{8}-[0-9a-f]{6}\.md$/, and its trimmed content equals normalizeQuestion(Q1).
    - D2: normalization: text '  Which  camera\tsolves the delay? ' returns result 'unchanged' and the role row count stays 1; questionHash of both spellings is equal and matches /^sha256:[0-9a-f]{64}$/.
    - D3: invalid origin (missing, 'bogus') returns invalid_origin with allowed equal to the four ids, zero new rows, zero new files under .mindrian/frames.
    - D4: set Q2 with no account and no relocate returns ok false, reason change_needs_account, ask exactly 'What did the old question get wrong?', previous.version 1, previous.text Q1, previous.origin 'tasking', proposed.text Q2, pending true, and a card object whose shape is 'F.1'; role row count still 1; .mindrian/frames/pending.json exists with based_on_version 1; pending-question.md holds Q2.
    - D5: account '   ' (whitespace only) is treated as absent (change_needs_account); account plus relocate:true returns ambiguous_change and writes nothing; account of 4001 characters returns invalid_account; text of 1001 characters returns invalid_text.
    - D6: set Q2 with account A2 and origin 'chosen' returns result refines, version 2, account_text equal to A2; the v2 row has change_kind refines, refinement_handle matching /^\.mindrian\/frames\/a-v2-[0-9a-f]{8}-[0-9a-f]{6}\.md$/ whose file holds A2, account_hash /^sha256:/, predecessor_hash equal to the v1 hash, predecessor_node_id equal to the v1 node id; exactly one REFINES edge v2 -> v1 whose properties parse to {change_kind:'refines', origin:'chosen'}; pending.json no longer exists.
    - D7: set Q3 with relocate:true and origin 'prompt' returns result relocates, version 3; v3 row has change_kind relocates and no refinement_handle; exactly one FOLLOWS_FROM edge v3 -> v2 with properties {change_kind:'relocates', origin:'prompt'}.
    - D8: going back to Q1 text (same hash as v1) after v3 is a CHANGE vs the latest, so without account or relocate it is refused change_needs_account; with relocate:true it lands as version 4.
    - D9: after D1-D8 the v1 row is byte-identical to its D1 snapshot; every role row review_status is 'proposed'; no edge of type SUPERSEDES touches any frame node; no nodes row anywhere has review_status 'superseded'.
    - D10: based_on_version 2 while the latest is 4 returns question_changed_meanwhile with current.version 4 and writes nothing.
    - D11: cancel: a refused change then {cancel:true} returns result cancelled and removes pending.json and pending-question.md; {cancel:true} again returns nothing_pending.
    - D12: first-question edge cases on a fresh room: account or relocate on the very first set returns no_previous_question; roomDir null returns no_room_dir.
    - D13: every file under .mindrian/frames has a generated name (q-, a-, pending.json, pending-question.md); no file name contains any word of Q1, Q2, Q3 or A2.

    tests/test-358-b2-render.cjs:
    - R1: empty room: readGoverningQuestion returns current null and pending null; renderQuestionLines includes exactly 'No governing question recorded yet.'; renderHistoryLines includes the same line.
    - R2: after first Q1 (origin tasking): renderQuestionLines contains 'Governing question (version 1 of 1): Which camera solves the delay?', 'Origin: tasking - <label from frameOriginInfo>', a line starting 'Set at: ' followed by an ISO time, and 'Change: first question'.
    - R3: after a refused change to Q2: the FIRST line of renderQuestionLines and of renderHistoryLines is exactly 'A question change is waiting.'; the lines 'Proposed question: Which camera works with sunglasses?' and 'What did the old question get wrong?' both appear before the line starting 'Governing question'; readGoverningQuestion().pending.card is present (the waiting ask carries its card into a later session).
    - R4: after refines (v2) and relocates (v3): renderHistoryLines has header 'Question history (3 versions, oldest first)'; each version block starts with 'Version N - origin <id> - <label> - set at <ISO>' (the latest header alone ends with ' (current)'), followed by '  Question: <text>' and '  Change: ...' where v1 is '  Change: first question', v2 is '  Change: refines version 1 (account on file)' followed by '  What the old question got wrong: <A2>', v3 is '  Change: relocates from version 2 (no account)'; every block has the same line prefixes apart from the refines account line (equal weight); '(current)' appears exactly once.
    - R5: renderQuestionLines for v3 includes 'Change: relocates from version 2 (no account)' and 'Earlier versions: 2 (see the history)'.
    - R6: forbidden words: no rendered line (both renders, every state above, and the card rendered_text) matches /\b(better|improv\w*|upgrad\w*|abandon\w*|correct(ed|ion)|superseded|obsolete|outdated|replaced)\b/i or /wrong question/i or /governing thought/i.
    - R7: out-of-band edit: overwrite the v1 question file with other text: readQuestionHistory().unresolved contains {version:1, what:'question', reason:'file_changed'} and the render has the line 'Unresolved: the question file for version 1 changed outside the record.'; delete the v2 account file: unresolved contains {version:2, what:'account', reason:'file_missing'} and the line 'Unresolved: the account file for version 2 is missing.'.
    - R8: every origin id renders with its label from frameOriginInfo (loop over FRAME_ORIGINS_ORDERED setting a fresh room's first question with each origin).
    - C1 (the card): renderQuestionChangeCard({previous:{version:1, text:Q1}, proposed:{text:Q2}}) returns shape 'F.1'; header's first line is exactly 'What did the old question get wrong?' and the header contains 'Old question (version 1): ' + Q1 and 'New question: ' + Q2; options[0..2] equal QUESTION_CARD_OPTIONS in order and options has length 4 (the renderer's Free-Text row last); askuserquestion_marker equals '[AskUserQuestion contract: shape=F.1 verbs=4]'; rendered_text contains no character in U+2500..U+257F (no box drawing) and does not contain 'Brain unreachable'; no rendered line contains a recommended marker (the filled triangle U+25B6).
    - C2 (render coverage): data/render-coverage-registry.json has an entry with entry 'lib/core/frame-provenance.cjs', kind 'pickShape', render_coverage 'card-emission'; spawning `node scripts/check-render-coverage.cjs --check` and `node scripts/build-render-coverage.cjs --check` both exit 0.

    tests/test-358-b2-part8.cjs:
    - P1 (prose scan): after a full cycle (first Q1, refused Q2, refines Q2 with A2, relocates Q3) no nodes.properties and no edges.properties value in room.db contains Q1, Q2, Q3, A2 or any 12-character substring of A2; frame role props keys are a subset of {frameKey, members, topic, frame_role, version, origin, governing_thought_hash, question_handle, predecessor_hash, predecessor_node_id, change_kind, refinement_handle, account_hash, set_by, set_by_id, epistemic_type}; REFINES and FOLLOWS_FROM edge props keys are a subset of {change_kind, origin}; no edge of type REJECTED_BECAUSE exists (the account never rides a rejection edge).
    - P2 (direct require scan): for each of lib/core/frame-provenance.cjs, lib/core/navigation/typed-frame.cjs, scripts/room-question.cjs, lib/mcp/tools/question.cjs that exists, no non-comment line matches /require\(\s*['"][^'"]*(brain-client|brain-derivation|part8-egress|mindrian-brain|theo)[^'"]*['"]\s*\)/, /require\(\s*['"](node:)?(http|https|net|tls|dgram)['"]\s*\)/, or /\bfetch\s*\(/; a file not yet on disk prints 'static: <file> not landed yet' and counts as neither pass nor fail; the file ends by printing 'part8 static: <k> of 4 B2 files scanned'.
    - P3 (substrate guard): require('./scripts/check-substrate.cjs').scanFiles over the same existing files returns [] (no sqlite require, no room-db require, no raw graph write).
    - P4 (one door): across every .cjs and .js file under lib/, scripts/ and bin/ (tests excluded), non-comment lines containing 'governing_question' or 'GOVERNING_QUESTION_ROLE' appear only in lib/core/navigation/typed-frame.cjs, lib/core/navigation.cjs and lib/core/frame-provenance.cjs; non-comment lines containing 'setGoverningQuestion(' appear only in lib/core/frame-provenance.cjs, scripts/room-question.cjs and lib/mcp/tools/question.cjs.
    - P5 (zero network): replace globalThis.fetch and wrap require('node:http').request, require('node:https').request and require('node:net').connect with recorders BEFORE requiring frame-provenance.cjs; run the full cycle (the refusal renders the card) plus readGoverningQuestion and readQuestionHistory; assert zero recorded calls; restore the originals.
    - P6 (B1 counts untouched, Pitfall 5): seed one claim with navigation.writeClaimNode, take navigation.readVerificationPortrait(db), run the full question cycle, take it again: deep-equal.
    - P7: the literal ids 'tasking' and 'inherited' do not appear in any non-comment line of lib/core/frame-provenance.cjs (the one-constant rule, ruling 3); comment lines are those whose trimmed text starts with // or *.

    tests/run-all-358.sh (re-opened ONCE, deliberately): see action.
  </behavior>
  <action>
    Write the three test files exactly to the behavior above (plain node scripts, node:assert/strict, the check counter, hermetic env first). Use lib/core/room-db.cjs openRoomDb/closeRoomDb for temp rooms (tests are allowed to), read raw rows with the handle's prepare() for byte-identical and prose-scan legs, never trust a function's own ok to prove persistence. Keep each file free of em-dashes.

    Edit tests/run-all-358.sh (this is the ONE deliberate B2 re-open; say so in its header comment, replacing the sentence "The B2 plan set (later in this same phase) adds its own legs deliberately" with a sentence stating that 358-07 re-opened it once for B2, named every B2 test file up front, and that no later B2 plan edits it):
    1. After the last B1 run_if leg, add run_if legs, in wave order, guard file = the test file: "358: B2 core (B2-01/B2-02/B2-03/B2-07)" tests/test-358-b2-core.cjs; "358: B2 render and card (B2-04)" tests/test-358-b2-render.cjs; "358: B2 Part 8 (B2-09)" tests/test-358-b2-part8.cjs; "358: B2 routing spec (Larry reach)" tests/test-358-b2-routing.cjs; "358: B2 CLI surface (B2-05)" tests/test-358-b2-cli.cjs; "358: B2 MCP surfaces (B2-06)" tests/test-358-b2-surfaces.cjs; "358: B2 close-everything persistence (B2-08)" tests/test-358-b2-persistence.cjs.
    2. In the run legs block add: "358: B2 frame substrate (updated by 358-07)" node tests/test-b2-frame-provenance.cjs; "358: frame node (run only, never edited by 358)" node tests/test-205-frame-node.cjs; "358: fusion router (run only, never edited by 358)" node tests/test-205-fusion-router.cjs; "358: one supersession door (run only, never edited by 358)" node tests/test-348-one-supersession-door.cjs; "358: build command registry --check" node scripts/build-command-registry.cjs --check. Do NOT add a harness-manifest leg: data/harness-manifest.json was already stale at planning time from a COMMITTED peer edit (skills/larry-personality/SKILL.md, quick-260923-u8v), so a leg would fail for peer reasons; 358-10 regenerates and checks the manifest instead.
    3. Widen the CIRS find from -name '358-0[1-6]-PLAN.md' to -name '358-[0-9][0-9]-PLAN.md' and update its echo label to "existing 358-NN-PLAN.md files".
    4. Add to PHASE_358_SURFACES: lib/core/navigation/typed-frame.cjs, lib/core/frame-provenance.cjs, scripts/room-question.cjs, lib/mcp/tools/question.cjs, tests/helpers/b2-358-child.cjs, tests/test-b2-frame-provenance.cjs, data/mcp-tool-connectors.json, data/command-registry.json, docs/2026-10-06-ROME-B2-GO-NO-GO.md (a missing file is skipped, never an error).
    5. Widen the em-dash glob from 'test-358-b1-*.cjs' to 'test-358-b[12]-*.cjs'.
    Run `bash -n tests/run-all-358.sh`.

    Commit (RED): `git add tests/test-358-b2-core.cjs tests/test-358-b2-render.cjs tests/test-358-b2-part8.cjs tests/run-all-358.sh` then `git commit --only -m "test(358-07): RED core, render, card and Part 8 tests for the governing question; runner B2 legs" -- tests/test-358-b2-core.cjs tests/test-358-b2-render.cjs tests/test-358-b2-part8.cjs tests/run-all-358.sh`.
  </action>
  <verify>
    <automated>bash -n tests/run-all-358.sh && ! node tests/test-358-b2-core.cjs >/dev/null 2>&1 && ! node tests/test-358-b2-render.cjs >/dev/null 2>&1 && ! node tests/test-358-b2-part8.cjs >/dev/null 2>&1 && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - All three test files exist and each exits non-zero now (RED: frame-provenance.cjs and the hardened substrate are absent)
    - `grep -c '^run_if "358: B2' tests/run-all-358.sh` is 7, and each of the seven B2 test file names (core, render, part8, routing, cli, surfaces, persistence) appears in tests/run-all-358.sh
    - `grep -c "358-\[0-9\]\[0-9\]-PLAN.md" tests/run-all-358.sh` is 1 and `grep -c "test-358-b\[12\]-\*.cjs" tests/run-all-358.sh` is 1
    - `grep -c "lib/core/frame-provenance.cjs" tests/run-all-358.sh` is at least 1 and `grep -c "docs/2026-10-06-ROME-B2-GO-NO-GO.md" tests/run-all-358.sh` is 1
    - `grep -c "tests/test-205-fusion-router.cjs" tests/run-all-358.sh` is 1, `grep -c "tests/test-348-one-supersession-door.cjs" tests/run-all-358.sh` is 1, `grep -c "build-command-registry.cjs --check" tests/run-all-358.sh` is 1
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-358-b2-core.cjs tests/test-358-b2-render.cjs tests/test-358-b2-part8.cjs tests/run-all-358.sh` prints 0 for every file
    - `git show --stat HEAD --format=%s` lists exactly the four owned paths
  </acceptance_criteria>
  <done>The B2 contract, card included, is written down as failing tests and the runner already knows every B2 leg.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN (substrate) - harden typed-frame.cjs, add the origin constant, re-export through navigation.cjs</name>
  <files>lib/core/navigation/typed-frame.cjs, lib/core/navigation.cjs, tests/test-b2-frame-provenance.cjs</files>
  <read_first>
    - lib/core/navigation/typed-frame.cjs (whole file)
    - lib/core/navigation/verification.cjs (lines 25-50: the VERIFICATION_RUNGS constant, its TODO(358) marker and rungInfo; lines 180-235: the BEGIN IMMEDIATE ownership idiom)
    - lib/core/node-insert.cjs (lines 180-265: insertNode overrides, on_conflict 'nothing')
    - lib/core/navigation.cjs (lines 460-490: the typed-frame re-export block ending at readFrameProvenance)
    - lib/core/fusion-router.cjs (lines 100-135: how readOpenFrames is called; read only)
    - tests/test-b2-frame-provenance.cjs, tests/test-205-frame-node.cjs (what must stay green)
    - tests/test-358-b2-core.cjs (the S legs written in Task 1)
  </read_first>
  <behavior>
    - Every S leg in tests/test-358-b2-core.cjs passes (`FAIL - S` count 0); D legs may still fail (frame-provenance.cjs does not exist yet).
    - tests/test-b2-frame-provenance.cjs, tests/test-205-frame-node.cjs, tests/test-205-fusion-router.cjs and tests/test-348-one-supersession-door.cjs exit 0.
  </behavior>
  <action>
    In lib/core/navigation/typed-frame.cjs (per rulings 2, 3 and 5; additive, FUSION behavior unchanged):
    1. Replace the FRAME_ORIGINS Set literal with FRAME_ORIGINS_ORDERED, a frozen array of frozen {id, label} entries in this order and with these provisional labels: chosen "chosen by the officer", tasking "handed down as a tasking", prompt "suggested in conversation with the tool", inherited "carried over from earlier work". Put the marker comment directly above it: `// TODO(358): replace these origin labels with the paper author's definitions once confirmed (slide A1 names the four origins without defining them).` Then derive `FRAME_ORIGINS = Object.freeze(new Set(FRAME_ORIGINS_ORDERED.map((o) => o.id)))` so there is one source. Add frameOriginInfo(id) returning {id, label, position} (1-based) or null. Add GOVERNING_QUESTION_ROLE = 'governing_question', GOVERNING_QUESTION_FRAME_KEY = 'governing-question', GOVERNING_QUESTION_SCOPE = 'room', and GOVERNING_QUESTION_NODE_ID(version) = FRAME_NODE_ID('room', 'governing-question:v' + version).
    2. In writeFrameNode, when params.frameRole === GOVERNING_QUESTION_ROLE, take a separate role branch BEFORE the existing lenient path (the lenient FUSION path stays byte-for-byte in behavior so test-205 stays green). Validate in this order and return {ok:false, reason} without writing: taxonomy === true -> taxonomy_not_allowed; origin not in FRAME_ORIGINS -> invalid_origin; version not a positive integer -> invalid_version; governingThoughtHash not matching /^sha256:[0-9a-f]{64}$/ -> invalid_question_hash; questionHandle not matching /^\.mindrian\/frames\/q-v\d+-[0-9a-f]{8}-[0-9a-f]{6}\.md$/ -> invalid_question_handle; version 1 with any changeKind, or version above 1 whose changeKind is not 'refines' or 'relocates' -> invalid_change_kind; version above 1 without predecessorHash (sha256 shape) or predecessorNodeId (non-empty string) -> missing_predecessor; changeKind 'refines' without a refinementHandle matching /^\.mindrian\/frames\/a-v\d+-[0-9a-f]{8}-[0-9a-f]{6}\.md$/ or without an accountHash of sha256 shape -> refines_needs_account_handle; changeKind 'relocates' with any refinementHandle or accountHash -> relocates_has_account. The caller's frameKey, sessionId and versionKey are ignored for the role: id = GOVERNING_QUESTION_NODE_ID(version), source_path 'frame:room:governing-question'. Props (additive JSON, generic handles and enums only, never prose): frameKey 'governing-question', members [], topic '', frame_role, version, origin, governing_thought_hash (the question hash; the field name is kept for substrate compatibility, it is never rendered as "governing thought"), question_handle, and when present predecessor_hash, predecessor_node_id, change_kind, refinement_handle, account_hash, set_by ('user' when setBy === 'user', else 'system'), set_by_id (only when a string of 1..128 chars). Before inserting, SELECT 1 FROM nodes WHERE id = ?; if a row exists return {ok:false, reason:'version_exists'} (immutable history, Pitfall 1). Insert via insertNode with created_by 'system', epistemic_type 'observation', on_conflict 'nothing', review_status left at the column default 'proposed'. Return {ok:true, node_id, type:'frame', version}. Never throw: wrap in try and return {ok:false, reason:'frame_write_failed', detail} on error.
    3. Add readGoverningQuestionVersions(db): SELECT id, properties, review_status, created_at FROM nodes WHERE type = 'frame' AND json_extract(properties, '$.frame_role') = 'governing_question' ORDER BY CAST(json_extract(properties, '$.version') AS INTEGER) ASC, created_at ASC, rowid ASC. Map each row to the interface shape (set_at = new Date(created_at).toISOString(), origin only when in FRAME_ORIGINS, change_kind only when refines or relocates). Return [] on any failure or a non-db argument; never throw.
    4. Harden readFrameProvenance: ORDER BY COALESCE(CAST(json_extract(properties, '$.version') AS INTEGER), 0) ASC, created_at ASC, rowid ASC; add to each returned object version (integer or null), created_at (number or null), set_at (ISO or null), question_handle (string or null), frame_role (string or null); keep every existing field. If the query throws (a legacy schema without created_at), retry the same query without created_at, ordered by rowid, with created_at and set_at null.
    5. In readOpenFrames add the SQL condition `AND (json_extract(properties, '$.frame_role') IS NULL OR json_extract(properties, '$.frame_role') <> 'governing_question')` to both query shapes (before LIMIT, so LIMIT stays correct). Add one comment line: governing-question versions are room-level provenance records, not FUSION open frames (Pitfall 3).
    6. Export the new names beside the existing ones: FRAME_ORIGINS_ORDERED, frameOriginInfo, GOVERNING_QUESTION_ROLE, GOVERNING_QUESTION_FRAME_KEY, GOVERNING_QUESTION_SCOPE, GOVERNING_QUESTION_NODE_ID, readGoverningQuestionVersions.

    In lib/core/navigation.cjs, edit ONLY the existing typed-frame re-export block (the lines ending `readFrameProvenance: typedFrame.readFrameProvenance,`): first `grep -n` each new key and STOP if it already exists elsewhere in the export map; then add `FRAME_ORIGINS_ORDERED: typedFrame.FRAME_ORIGINS_ORDERED`, `frameOriginInfo: typedFrame.frameOriginInfo`, `GOVERNING_QUESTION_ROLE: typedFrame.GOVERNING_QUESTION_ROLE`, `GOVERNING_QUESTION_NODE_ID: typedFrame.GOVERNING_QUESTION_NODE_ID`, `readGoverningQuestionVersions: typedFrame.readGoverningQuestionVersions`, with a two-line comment: Phase 358-07 (Rome B2): the governing question is a room-level record written only by lib/core/frame-provenance.cjs; these are local reads and constants, nothing crosses to the Brain (Canon Part 8).

    Update tests/test-b2-frame-provenance.cjs to the hardened contract: keep every existing assertion; add that each returned frame has a numeric created_at and an ISO set_at; add one role write through navigation.writeFrameNode (frameRole navigation.GOVERNING_QUESTION_ROLE, version 1, origin 'chosen', a sha256-shaped hash and a valid q-v1 handle) and assert readFrameProvenance returns it with version 1 and that navigation.readGoverningQuestionVersions returns exactly that one row. Keep it under 90 lines and free of em-dashes.

    Run: `node tests/test-358-b2-core.cjs` (expect every S leg ok), `node tests/test-b2-frame-provenance.cjs`, `node tests/test-205-frame-node.cjs`, `node tests/test-205-fusion-router.cjs`, `node tests/test-348-one-supersession-door.cjs`, `node tests/test-358-b1-core.cjs` (B1 regression), `node scripts/check-render-coverage.cjs --check` (still 0: no pickShape added in this task).

    Commit: `git add lib/core/navigation/typed-frame.cjs lib/core/navigation.cjs tests/test-b2-frame-provenance.cjs` then `git commit --only -m "feat(358-07): harden frame substrate for the governing question (required origin, immutable versions, ordered readers)" -- lib/core/navigation/typed-frame.cjs lib/core/navigation.cjs tests/test-b2-frame-provenance.cjs`.
  </action>
  <verify>
    <automated>test "$(node tests/test-358-b2-core.cjs 2>&1 | grep -c '^FAIL - S')" = 0 && node tests/test-b2-frame-provenance.cjs >/dev/null && node tests/test-205-frame-node.cjs >/dev/null && node tests/test-205-fusion-router.cjs >/dev/null && node tests/test-348-one-supersession-door.cjs >/dev/null && node tests/test-358-b1-core.cjs >/dev/null && echo SUBSTRATE-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-358-b2-core.cjs 2>&1 | grep -c '^FAIL - S'` is 0 and `node tests/test-358-b2-core.cjs 2>&1 | grep -c '^ok - S'` is at least 5
    - `grep -c "TODO(358)" lib/core/navigation/typed-frame.cjs` is at least 1 and `grep -c "FRAME_ORIGINS_ORDERED" lib/core/navigation/typed-frame.cjs` is at least 3
    - `grep -c "on_conflict: 'nothing'" lib/core/navigation/typed-frame.cjs` is at least 1
    - `grep -c "readGoverningQuestionVersions: typedFrame.readGoverningQuestionVersions" lib/core/navigation.cjs` is 1
    - `node --no-warnings -e "const n=require('./lib/core/navigation.cjs'); if(n.FRAME_ORIGINS_ORDERED.map(o=>o.id).join()!=='chosen,tasking,prompt,inherited'||!n.FRAME_ORIGINS.has('inherited')||n.frameOriginInfo('prompt').position!==3) process.exit(1)"` exits 0
    - tests/test-b2-frame-provenance.cjs, tests/test-205-frame-node.cjs, tests/test-205-fusion-router.cjs, tests/test-348-one-supersession-door.cjs and tests/test-358-b1-core.cjs each exit 0
    - `git diff "$PLAN_BASE" -- lib/core/navigation.cjs | grep '^-' | grep -v '^---' | wc -l` is 0 (additive only)
    - `grep -c "$(printf '\xe2\x80\x94')" lib/core/navigation/typed-frame.cjs lib/core/navigation.cjs tests/test-b2-frame-provenance.cjs` prints 0 for every file
  </acceptance_criteria>
  <done>The frame substrate refuses a missing origin, never rewrites a version, orders and times its readers, and keeps governing-question versions out of FUSION.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN (door) - lib/core/frame-provenance.cjs with the pause, readers, renders and the card; regenerate the render-coverage registry in the same commit</name>
  <files>lib/core/frame-provenance.cjs, data/render-coverage-registry.json</files>
  <read_first>
    - lib/core/navigation/typed-frame.cjs (as hardened in Task 2)
    - lib/core/navigation/verification.cjs (lines 175-235: BEGIN IMMEDIATE ownership, ROLLBACK paths; and its render helpers for line-array style)
    - lib/core/room-path-containment.cjs (lines 98-199: realRoomRoot, assertRealpathContained, writeFileContained)
    - lib/core/room-chooser.cjs (lines 186-215: renderRoomChooserCard, the in-function require of ../hmi/selector-dispatcher.cjs and the pickShape call shape to copy)
    - lib/core/brain-derivation.cjs (lines 115-122: the 'sha256:' + sha256(text.normalize('NFC')) hashing convention; read only)
    - scripts/build-render-coverage.cjs (lines 60-70 WALK_SCOPES, 160-215 hasCallSite and the walk) and scripts/check-render-coverage.cjs (header: the pinned predicate)
    - .git/hooks/pre-commit (lines 452-460: the render-coverage guard fires on any staged lib/core/*.cjs)
    - tests/test-358-b2-core.cjs, tests/test-358-b2-render.cjs, tests/test-358-b2-part8.cjs (the D, R, C and P legs)
  </read_first>
  <behavior>
    - tests/test-358-b2-core.cjs, tests/test-358-b2-render.cjs and tests/test-358-b2-part8.cjs each exit 0.
    - part8 prints 'part8 static: 2 of 4 B2 files scanned' at this point (the CLI and MCP files land in 358-08 and 358-09).
    - `node scripts/check-render-coverage.cjs --check` and `node scripts/build-render-coverage.cjs --check` exit 0 with the new frame-provenance entry recorded as card-emission.
  </behavior>
  <action>
    Create lib/core/frame-provenance.cjs (CJS, 'use strict', header comment naming Phase 358-07, rulings 2, 3 and 5, Canon Parts 3, 8 and 9, the one-door rule, the SEED-020 card door, the F.1-not-F.0 shape note from the context block in one sentence, and "no em-dashes"). Top-level requires ONLY node:fs, node:path, node:crypto, `./navigation.cjs` and `./room-path-containment.cjs`; lib/hmi/selector-dispatcher.cjs is required INSIDE renderQuestionChangeCard (the room-chooser.cjs idiom). No node:sqlite, no room-db.cjs, no raw SQL other than transaction control on the caller's handle, no Brain or network module.

    Constants and helpers: QUESTION_ASK = 'What did the old question get wrong?'; QUESTION_CARD_OPTIONS = Object.freeze(['Write what it got wrong (files as refines)', 'It is a new question (files as relocates)', 'Cancel the change']); FRAMES_DIR_REL = '.mindrian/frames'; MAX_QUESTION_CHARS = 1000; MAX_ACCOUNT_CHARS = 4000; normalizeQuestion(text) (String, NFC, trim, /\s+/g to one space); normalizeAccount(text) (String, NFC, trim; internal newlines kept); questionHash(text) = 'sha256:' + sha256 hex of normalizeQuestion(text); accountHash(text) = 'sha256:' + sha256 hex of normalizeAccount(text); ensureFramesDir(roomDir) = assert <room>/.mindrian and <room>/.mindrian/frames realpath-contained before and after fs.mkdirSync(recursive) (the 354-05 assert, act, re-assert discipline); a nonce is crypto.randomBytes(3).toString('hex'); artifact names are generated ONLY from version, the first 8 hex of the question hash and the nonce: q-v<N>-<h8>-<nonce>.md and a-v<N>-<h8>-<nonce>.md; if the target already exists, draw a new nonce once, then refuse artifact_write_failed (never overwrite). Question file content = normalized question + '\n'; account file content = normalized account + '\n'; both written with writeFileContained. Pending files: .mindrian/frames/pending.json ({proposed_handle: '.mindrian/frames/pending-question.md', proposed_hash, origin, based_on_version, at: ISO}) and .mindrian/frames/pending-question.md (normalized proposed text + '\n'); a new refusal replaces an older pending; clearing removes both (best effort, missing is fine). readArtifact(roomDir, handle) only reads a handle matching /^\.mindrian\/frames\/[a-z0-9.-]+\.md$/ and whose resolved path is realpath-contained, else reports invalid_handle.

    renderQuestionChangeCard({previous, proposed}) is the ONE pickShape call site for B2 (the SEED-020 door; navigator requirement item 1): require '../hmi/selector-dispatcher.cjs' inside the function and call its pickShape with requestedShape 'F.1', roomDir null, operator null, tier 2 (comment: the card is local-only, tier 2 only keeps the dispatcher from printing a Brain-reachability banner that says nothing about this card), payload {header: QUESTION_ASK + '\n' + 'Old question (version ' + previous.version + '): ' + previous.text + '\n' + 'New question: ' + proposed.text, verbs: Array.from(QUESTION_CARD_OPTIONS), recommendedVerb: null} (no option recommended: neither answer is presented as better). Return {shape, header: zones.header, options: contract.verbs, rendered_text: zones.header + '\n' + zones.body + '\n' + zones.footer, askuserquestion_marker, askuserquestion_binding}. On any dispatcher failure return {shape:null, header: the same header text, options: QUESTION_CARD_OPTIONS plus 'Free-Text', rendered_text: header plus one line per option, askuserquestion_marker:null, askuserquestion_binding:null} so the ask is never lost (never throw).

    setGoverningQuestion(db, roomDir, params), validation order, each refusal writing nothing: params not a plain object -> invalid_params; roomDir not an existing directory -> no_room_dir; cancel === true -> if no pending.json return nothing_pending else clear pending and return {ok:true, result:'cancelled'}; text normalized length 0 or above MAX_QUESTION_CHARS -> invalid_text; origin not in navigation.FRAME_ORIGINS -> invalid_origin with allowed = FRAME_ORIGINS_ORDERED ids; account present but not a string, or normalized length above MAX_ACCOUNT_CHARS -> invalid_account (an empty or whitespace-only account counts as absent); relocate present and not boolean -> invalid_params; account present AND relocate === true -> ambiguous_change (never guess the officer's choice). Then latest = last of navigation.readGoverningQuestionVersions(db). No latest: account or relocate given -> no_previous_question; else land version 1 with no change kind. Latest with equal hash -> {ok:true, result:'unchanged', version}. based_on_version given (integer) and (no latest or latest.version !== based_on_version) -> question_changed_meanwhile with current (the readGoverningQuestion current or null). Neither account nor relocate === true -> write the pending files and return {ok:false, reason:'change_needs_account', ask: QUESTION_ASK, previous:{version, text (read from the artifact), origin, origin_label}, proposed:{text, origin, origin_label}, options: Array.from(QUESTION_CARD_OPTIONS), pending:true, card: renderQuestionChangeCard({previous, proposed})}. A missing choice NEVER defaults to relocates (Pitfall 8).

    Landing (per research Pattern 2 and the Architecture diagram), INLINE in setGoverningQuestion's own body so the tool-honesty scanner reaches the write in one hop: const owns = db.isTransaction !== true; if owns, db.exec('BEGIN IMMEDIATE'). Inside a try: re-read latest with navigation.readGoverningQuestionVersions(db); if its version or hash differs from the pre-read latest, ROLLBACK (when owned) and return question_changed_meanwhile; N = latest ? latest.version + 1 : 1; ensureFramesDir; write the question artifact and, for refines, the account artifact; call navigation.writeFrameNode(db, {frameRole: navigation.GOVERNING_QUESTION_ROLE, version: N, origin, governingThoughtHash: hash, questionHandle, predecessorHash, predecessorNodeId, changeKind, refinementHandle, accountHash, setBy: 'user', setById: params.set_by_id}); on !ok ROLLBACK (when owned), unlink this call's artifacts, return version_write_failed with detail = the substrate reason; when a latest exists call navigation.writeEdge(db, {source_id: new node id, target_id: latest.node_id, edge_type: refines ? 'REFINES' : 'FOLLOWS_FROM', properties: {change_kind, origin}}) and keep its {ok, type} in the result (an edge failure never fails the version, Part 9 floor); COMMIT when owned. On any thrown error: ROLLBACK when owned, unlink this call's artifacts, return artifact_write_failed or version_write_failed with a short detail. After COMMIT clear the pending files and return {ok:true, result: 'first' | 'refines' | 'relocates', version: N, node_id, question_handle, account_handle, account_text (refines only), edge, pending_cleared}.

    readGoverningQuestion(db, roomDir) and readQuestionHistory(db, roomDir): build from navigation.readGoverningQuestionVersions(db) and readArtifact; pending from pending.json plus pending-question.md (unreadable text -> text null), and when pending and a current version exist, pending.card = renderQuestionChangeCard({previous: current, proposed: pending}); each version's text is the artifact text (null when missing), with unresolved entries per the interface when a normalized re-hash of the file differs from the node hash (file_changed), when the file is missing (file_missing), or when the handle fails the shape check (invalid_handle); for refines the same check runs on the account file against account_hash; from_version = version - 1 for refines and relocates; is_current = the last version. Never throw; return {ok:false, reason:'read_failed'} only on a non-db argument.

    renderQuestionLines(view) and renderHistoryLines(history) produce EXACTLY the lines in Task 1's R legs. Pending block (always first when pending): 'A question change is waiting.', 'Proposed question: <text>' (or 'Proposed question: (the proposed text could not be read)'), 'Proposed origin: <id> - <label>', QUESTION_ASK, 'Answer in your own words to file it as refines, say it is a new question to file it as relocates, or cancel the change.', then ''. No versions: 'No governing question recorded yet.'. Current: 'Governing question (version N of M): <text or (question file missing)>', 'Origin: <id> - <label>', 'Set at: <ISO>', the change line ('Change: first question' | 'Change: refines version K (account on file)' | 'Change: relocates from version K (no account)'), and when M > 1 'Earlier versions: <M-1> (see the history)'. History: header 'Question history (1 version, oldest first)' or 'Question history (M versions, oldest first)', then per version 'Version N - origin <id> - <label> - set at <ISO>' plus ' (current)' on the last only, '  Question: <text>', '  Change: ...', and for refines '  What the old question got wrong: <account text or (account file missing)>'. Unresolved lines last: 'Unresolved: the question file for version N changed outside the record.' / '... is missing.' and the same with 'account file'. Origin labels always come from navigation.frameOriginInfo (never a literal list in this file, ruling 3). Never use ranking words and never the words "governing thought" (Pitfall 6).

    module.exports = { setGoverningQuestion, readGoverningQuestion, readQuestionHistory, renderQuestionLines, renderHistoryLines, renderQuestionChangeCard, questionHash, normalizeQuestion, QUESTION_ASK, QUESTION_CARD_OPTIONS, FRAMES_DIR_REL, MAX_QUESTION_CHARS, MAX_ACCOUNT_CHARS }.

    Regenerate the render-coverage registry (this commit adds a pickShape call site under lib/core, and the pre-commit render-coverage guard blocks every lib/core/*.cjs commit in the shared tree until the registry matches): precondition `git status --short -- data/render-coverage-registry.json` prints nothing; run `node scripts/build-render-coverage.cjs`; `git diff -- data/render-coverage-registry.json` must show exactly one added entry (entry lib/core/frame-provenance.cjs, kind pickShape, render_coverage card-emission) and render_counts.card_emission increased by exactly 1; if the diff shows ANY other change, it comes from a peer's uncommitted .cjs file in lib/ or scripts/: STOP, run `git checkout -- data/render-coverage-registry.json` (this plan's own regenerate only), name the peer path in the SUMMARY, and resume when it is committed. Then `node scripts/check-render-coverage.cjs --check` and `node scripts/build-render-coverage.cjs --check` exit 0.

    Run all three B2 tests, the substrate regression set from Task 2, `node tests/test-358-b1-portrait.cjs`, and `bash tests/run-all-358.sh; echo "exit=$?"` (expected exit 77 with FAILED=0: the routing, cli, surfaces and persistence legs are SKIPPED until 358-08 and 358-09 land; any FAILED leg must be fixed or, if a peer's uncommitted file caused it, recorded with the owning phase).

    Commit together (the guard needs both): `git add lib/core/frame-provenance.cjs data/render-coverage-registry.json` then `git commit --only -m "feat(358-07): one write door for the room's governing question (refines, relocates, the pending ask, one Decision Gate card)" -- lib/core/frame-provenance.cjs data/render-coverage-registry.json`.
  </action>
  <verify>
    <automated>node tests/test-358-b2-core.cjs >/dev/null && node tests/test-358-b2-render.cjs >/dev/null && node tests/test-358-b2-part8.cjs >/dev/null && node tests/test-b2-frame-provenance.cjs >/dev/null && node tests/test-205-fusion-router.cjs >/dev/null && node tests/test-358-b1-portrait.cjs >/dev/null && node scripts/check-render-coverage.cjs --check >/dev/null && node scripts/build-render-coverage.cjs --check >/dev/null && echo DOOR-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-358-b2-core.cjs, tests/test-358-b2-render.cjs and tests/test-358-b2-part8.cjs each exit 0
    - `node tests/test-358-b2-part8.cjs | grep -c "part8 static: 2 of 4 B2 files scanned"` is 1
    - `node --no-warnings -e "const s=require('./scripts/check-substrate.cjs'); const fs=require('fs'); const v=s.scanFiles(['lib/core/frame-provenance.cjs'], f=>fs.readFileSync(f,'utf8')); process.exit(v.length?1:0)"` exits 0
    - `awk '/^function setGoverningQuestion/,/^}/' lib/core/frame-provenance.cjs | grep -c "navigation\.write"` is 2 (writeFrameNode and writeEdge inline in the door)
    - `grep -v '^\s*//' lib/core/frame-provenance.cjs | grep -c "pickShape("` is 1 (the single card call site)
    - `grep -c "What did the old question get wrong?" lib/core/frame-provenance.cjs` is 1 (the constant, reused everywhere else)
    - `node --no-warnings -e "const r=require('./data/render-coverage-registry.json'); const e=r.entries.find(x=>x.entry==='lib/core/frame-provenance.cjs'&&x.kind==='pickShape'); process.exit(e&&e.render_coverage==='card-emission'?0:1)"` exits 0
    - `node scripts/check-render-coverage.cjs --check` and `node scripts/build-render-coverage.cjs --check` exit 0
    - `git show --stat HEAD --format=%s` lists exactly lib/core/frame-provenance.cjs and data/render-coverage-registry.json
    - `bash tests/run-all-358.sh` summary line shows FAILED=0 (exit 77 is expected until 358-09 lands the last B2 test file)
    - `grep -c "$(printf '\xe2\x80\x94')" lib/core/frame-provenance.cjs` is 0
  </acceptance_criteria>
  <done>The room has one governing question record with a single door that refuses a silent change, fires the ask as one covered card, files refines and relocates honestly, keeps every version, and shows the waiting ask first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (CLI script / MCP tool) -> frame-provenance.cjs | question text, origin, account and flags arrive from an officer through Larry; untrusted input |
| frame-provenance.cjs -> room filesystem (.mindrian/frames) | artifact names and paths must stay inside the room |
| room.db graph -> anything that could project toward the Brain | no user prose may live in node or edge properties (Part 8) |
| frame-provenance.cjs -> selector-dispatcher (card) | question text rides into the card header; the card is rendered locally and returned to the local host only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-30 | Information disclosure | question / account prose in graph properties | mitigate | Prose only in .mindrian/frames artifacts; props carry enums, hashes and handles; P1 scans every nodes and edges property for the texts and 12-char substrings and asserts no REJECTED_BECAUSE edge (the F.0 renderer's reason edge is never used) |
| T-358-31 | Tampering | history rewrite of an existing version | mitigate | Role writes pre-check the id and refuse version_exists, insert with on_conflict 'nothing'; S3 and D9 assert byte-identical rows |
| T-358-32 | Tampering | path traversal or symlink escape through artifact names or tampered handles | mitigate | Names generated only from version, hash prefix and nonce; handle shape regex on write (typed-frame) and on read (readArtifact); assertRealpathContained around mkdir; writeFileContained never follows a symlink leaf |
| T-358-33 | Repudiation | a question change landing silently as relocates | mitigate | No default: relocate must be the literal boolean true; account plus relocate is ambiguous_change; neither returns the ask, the card and records pending (D4, D5); the card marks no option recommended |
| T-358-34 | Tampering | lost update between two Cowork officers | mitigate | BEGIN IMMEDIATE with an in-transaction re-read, plus optional based_on_version (D10) |
| T-358-35 | Tampering | an artifact edited outside the record goes unnoticed | mitigate | Readers re-hash question and account files against node hashes and render Unresolved lines (R7) |
| T-358-36 | Elevation of privilege | a second writer bypasses the door | mitigate | P4 one-door static scan over lib/, scripts/, bin/ |
| T-358-37 | Information disclosure | a B2 file reaching the Brain or the network | mitigate | P2 direct-require scan, P3 check-substrate scan, P5 zero-network runtime cycle (card rendering included; tier passed explicitly so tier-check is never consulted) |
| T-358-38 | Tampering | regenerating the shared render registry over a peer's uncommitted render file | mitigate | Clean-file precondition, diff must show exactly the one frame-provenance entry, otherwise STOP and revert only this plan's regenerate |
</threat_model>

<verification>
- `node tests/test-358-b2-core.cjs && node tests/test-358-b2-render.cjs && node tests/test-358-b2-part8.cjs`
- `node tests/test-b2-frame-provenance.cjs && node tests/test-205-frame-node.cjs && node tests/test-205-fusion-router.cjs && node tests/test-348-one-supersession-door.cjs`
- `node scripts/check-render-coverage.cjs --check && node scripts/build-render-coverage.cjs --check`
- B1 regression: `node tests/test-358-b1-core.cjs && node tests/test-358-b1-portrait.cjs`
- `bash tests/run-all-358.sh` shows FAILED=0 (SKIPPED only for the four B2 test files 358-08 and 358-09 create)
</verification>

<success_criteria>
- B2-01, B2-02, B2-03, B2-04, B2-07, B2-09 proven offline by tests that were red first
- The question-change ask is one covered Decision Gate card through the SEED-020 door (navigator requirement item 1, shape F.1 with the F.0 deviation stated)
- B2-10 (runner part): every B2 leg named up front in tests/run-all-358.sh; CIRS and em-dash globs cover the B2 plans and tests
- Rulings 2, 3 and 5 implemented exactly; rulings 4 and 6 untouched
</success_criteria>

<output>
Create `.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-07-SUMMARY.md` when done (stage it with `git add -f` and commit it with `git commit --only`).
</output>
