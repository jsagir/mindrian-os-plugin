---
phase: 358-rome-b1-checking-record-and-b2-frame-provenance-user-visible
plan: 05
type: execute
wave: 4
depends_on: ["358-03", "358-04"]
files_modified:
  - data/mcp-tool-connectors.json
  - data/connector-registry.json
  - data/brain-orchestration-projection.json
  - tests/test-270-tool-schema-budget.cjs
  - tests/fixtures/tool-honesty/276-dispositions.json
autonomous: true
requirements: [B1-07]
canon_parts: [11]
tags: [b1, cirs, born-wired, registry, baseline, rome]

must_haves:
  truths:
    - "data/mcp-tool-connectors.json and data/connector-registry.json are regenerated fresh by scripts/build-connector-registry.cjs AFTER every B1 surface landed, contain the mcp:claim_read record, pass --check, and are committed alone with git commit --only (collision rule: regenerated shared artifacts last)"
    - "data/brain-orchestration-projection.json passes --check; it is regenerated and committed only if --check reports drift"
    - "tests/test-270-tool-schema-budget.cjs is green: AFTER is re-baselined with the numbers measured on a clean lib/mcp tree, and the commit message and a comment block name the measured tool count, total bytes and percentage change (the file's own protocol; DRIFT_TOLERANCE_PCT stays 10)"
    - "tests/test-276-tool-honesty-findings-closed.cjs is green: frozen_sweep matches the live scanAll tool and branch counts, and refrozen_at names this re-freeze and the 276-15 freeze it supersedes"
    - "Every test and gate left red by substrate commit 42191a6ae (test-234 description floor, test-270 schema budget, test-276 honesty sweep) is green at phase end (navigator ruling 2026-09-23)"
    - "`bash tests/run-all-358.sh` exits 0 (not 77) with every B1 leg PASSED"
  artifacts:
    - path: "data/mcp-tool-connectors.json"
      provides: "regenerated MCP tool connector list including claim_read"
      contains: "mcp:claim_read"
    - path: "data/connector-registry.json"
      provides: "regenerated connector registry including claim_read"
      contains: "mcp:claim_read"
    - path: "tests/test-270-tool-schema-budget.cjs"
      provides: "AFTER re-baselined by plan 358-05 with measured numbers"
      contains: "plan: '358-05'"
    - path: "tests/fixtures/tool-honesty/276-dispositions.json"
      provides: "frozen_sweep re-frozen to the live scan; refrozen_at names plan 358-05"
      contains: "358-05"
  key_links:
    - from: "lib/mcp/tools/claim-verify.cjs connectors"
      to: "data/mcp-tool-connectors.json"
      via: "node scripts/build-connector-registry.cjs"
      pattern: "mcp:claim_read"
    - from: "tests/test-270-tool-schema-budget.cjs AFTER"
      to: "live tools/list over stdio"
      via: "measure() over listToolsOverStdio"
      pattern: "measuredAt"
---

<objective>
Close B1's born-wired and baseline work LAST, after every B1 surface has landed: regenerate the connector registry for claim_read and commit it alone; re-baseline the tool-schema budget and re-freeze the tool-honesty sweep with measured numbers; then prove the whole phase green with `bash tests/run-all-358.sh` and `node scripts/doctor.cjs --acceptance`.

Purpose: Canon Part 11 (born wired, generated never hand-written) and the navigator ruling that the three tests left red by 42191a6ae are green at phase end. Doing this last (collision rule) means peers 354-15 and 355-05, which also regenerate these files and move the same byte totals, do not churn against B1 mid-phase.

Output: two (possibly three) regenerated data files in one commit; the re-baselined test-270 and re-frozen 276 ledger in a second commit; a green phase runner.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-RESEARCH.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-04-SUMMARY.md

Parallel-session rules (every task): peers execute Phases 354-357 in this same working tree. Record `PLAN_BASE=$(git rev-parse HEAD)` at start. Stage explicit paths only and commit with `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git add .`, `git commit -a`, `git stash`, or `--no-verify`. Never touch: docs/reviews/*, evals/plurai/*, scripts/eval-icm-writers.cjs, scripts/jev-devtime-client.cjs, tests/test-353-* (run only), .planning/phases/356-*, data/jev-policies/, .planning/STATE.md, lib/mcp/tools/gate.cjs, lib/mcp/tools/views.cjs, lib/mcp/tool-router.cjs, lib/mcp/register-core-tools.cjs, lib/mcp/tools/status.cjs, CLAUDE.md. No em-dashes (hyphens only).

Why a clean tree matters here (memory rule "acceptance only on a clean owned tree"): the registry generator and the budget measurement both read the WORKING TREE, not HEAD. A peer's uncommitted edit to a command, skill, agent or tools file would be swept into B1's regenerated registry or into the measured baseline, and the committed numbers would then be wrong for everyone. Each task below starts with a clean-tree precondition; if it fails, STOP, report which peer paths are dirty, and resume only when they are committed (never stash, revert or commit a peer's file).
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `lib/core/navigation/verification.cjs` (rung constant, record fields, sticky disputed, claim view + list readers, portrait by rung, render helpers), `lib/core/navigation.cjs` re-exports, core/portrait/separation tests, updated `tests/test-b1-verification.cjs` | 01 |
| `lib/core/navigation/typed-claim.cjs` carry-forward + PROTECTED_CLAIM_KEYS, `tests/test-358-b1-refile.cjs`, `lib/mcp/surface-detect.cjs` claude-ai tier0 host, `tests/test-234-host-tier.cjs` additions, `tests/run-all-358.sh` | 02 |
| `scripts/claim-checks.cjs`, `/mos:room checks`, `/mos:room claim`, `/mos:room check`, regenerated `skills/room/SKILL.md`, `tests/test-358-b1-cli.cjs` | 03 |
| `claim_verify` (rung required, checked_by_id, resolves_dispute, description >= 120 chars), new `claim_read` MCP tool, surfaces test, child helper, persistence test | 04 |
| Regenerated `data/mcp-tool-connectors.json` + `data/connector-registry.json`, test-270 AFTER re-baseline, 276 ledger re-freeze | 05 (this plan) |
| `docs/2026-10-06-ROME-B1-GO-NO-GO.md` (operator runbook) + human checkpoint | 06 |

<tasks>

<task type="auto">
  <name>Task 1: Fresh regenerate of the connector registry for claim_read, committed alone</name>
  <files>data/mcp-tool-connectors.json, data/connector-registry.json, data/brain-orchestration-projection.json</files>
  <read_first>
    - scripts/build-connector-registry.cjs (header and the --check mode: what it reads and which files it writes)
    - lib/mcp/tools/claim-verify.cjs (the connectors export: claim_verify F.1 unchanged, claim_read none)
    - data/mcp-tool-connectors.json (lines 55-75: the existing mcp:claim_verify record shape)
    - .git/hooks/pre-commit (lines 174-215: the connector-registry and orchestration-projection guards that run when these data files are staged)
  </read_first>
  <action>
    1. Precondition: `git status --short -- data/ lib/mcp/tools/ commands/ skills/ agents/` must print nothing. If it prints anything, STOP (clean-tree rule in the context block).
    2. Run `node scripts/build-connector-registry.cjs --check` and record its output (expected: drift naming mcp:claim_read only, carried from 358-04).
    3. Regenerate fresh: `node scripts/build-connector-registry.cjs`. Then `git status --short -- data/` must list only data/mcp-tool-connectors.json and data/connector-registry.json. Inspect `git diff -- data/mcp-tool-connectors.json data/connector-registry.json`: the expected change is one added mcp:claim_read record in each (hitl_shape none, layer harness) and nothing else. If the diff also changes other surfaces, those come from code already committed by someone else without a regenerate: keep them only if `git log` shows the source change is committed, and name each extra surface in the commit message; if any extra change traces to an uncommitted file, STOP. If data/connector-coverage-ledger.json also changed, include it in the commit only when the change is the claim-verify.cjs entry, and add it to the SUMMARY's key files.
    4. Run `node scripts/build-orchestration-projection.cjs --check`. Expected OK (no command connector changed). Only if it reports drift: run `node scripts/build-orchestration-projection.cjs`, confirm the diff is explained by B1, and include data/brain-orchestration-projection.json in the same commit.
    5. Re-run `node scripts/build-connector-registry.cjs --check` and `node scripts/build-orchestration-projection.cjs --check` (both exit 0) and `node tests/test-270-connector-coverage.cjs` (exit 0).
    6. Commit ALONE with --only (the pre-commit hook re-runs both guards on the staged data files): `git add data/mcp-tool-connectors.json data/connector-registry.json` (plus data/brain-orchestration-projection.json or data/connector-coverage-ledger.json only if steps 3-4 required them) then `git commit --only -m "chore(358-05): regenerate connector registry for claim_read" -- data/mcp-tool-connectors.json data/connector-registry.json` (append the extra paths when included).
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check && node tests/test-270-connector-coverage.cjs >/dev/null && grep -c '"mcp:claim_read"' data/mcp-tool-connectors.json data/connector-registry.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-connector-registry.cjs --check` and `node scripts/build-orchestration-projection.cjs --check` exit 0
    - `grep -c '"surface": "mcp:claim_read"' data/mcp-tool-connectors.json` is 1 and `grep -c '"surface": "mcp:claim_read"' data/connector-registry.json` is 1
    - `git show --stat HEAD` lists only data/ files (the regenerate commit is alone)
    - `node tests/test-270-connector-coverage.cjs` exits 0
    - `git status --short -- data/` prints nothing after the commit
  </acceptance_criteria>
  <done>The registry carries claim_read, every born-wired check is green, and the regenerate sits in its own commit.</done>
</task>

<task type="auto">
  <name>Task 2: Re-baseline the tool-schema budget, re-freeze the honesty sweep, prove the phase green</name>
  <files>tests/test-270-tool-schema-budget.cjs, tests/fixtures/tool-honesty/276-dispositions.json</files>
  <read_first>
    - tests/test-270-tool-schema-budget.cjs (lines 225-300: BASELINE, the 276-12 re-baseline comment block and AFTER; the protocol: move AFTER deliberately, name the measured percentage in the commit message, never relax DRIFT_TOLERANCE_PCT)
    - tests/fixtures/tool-honesty/276-dispositions.json (top-level keys schema_version, frozen_at_commit, frozen_sweep, dispositions, refrozen_at; the refrozen_at object written by plan 276-15)
    - tests/test-276-tool-honesty-findings-closed.cjs (lines 96-110 and 255-275: GROUP A requires every non-OK live row to have a disposition; GROUP F compares frozen_sweep.tools and .branches to scanAll)
    - scripts/check-tool-honesty.cjs (scanAll export: { rows, toolCount, branchCount }; the verdict strings used in rows)
  </read_first>
  <action>
    1. Precondition: `git status --short -- lib/mcp/ bin/` must print nothing (the measurement spawns the real server from the working tree). If not, STOP (clean-tree rule).
    2. Measure: run `node tests/test-270-tool-schema-budget.cjs` and copy the numbers from its `budget:` line (tools, desc bytes, schema bytes, total, approx tokens, router, atomic). At planning time the line read `budget: 41 tools, 17306 desc bytes, 25809 schema bytes, 43115 total, ~10779 approx tokens (router 9 / atomic 32)`; after 358-04 expect 42 tools and a larger total.
    3. Edit tests/test-270-tool-schema-budget.cjs: keep BASELINE and the 276-12 comment block as history; add a new comment block directly above AFTER in the same style explaining: commit 42191a6ae (B1 substrate) added claim_verify (40 -> 41 tools) without re-measuring and left this file red at 43115 total bytes, 10.64 percent over the 276-12 AFTER; plan 358-04 then added claim_read (41 -> 42) and lengthened claim_verify's description and schema (the rung field); the measured numbers and the signed percentage change of totalBytes against 38970, computed with the file's own pctChange rule (two decimals); DRIFT_TOLERANCE_PCT is not relaxed. Replace the AFTER object's values with the measured ones, `measuredAt` = today's date (YYYY-MM-DD), `plan: '358-05'`. Change nothing else.
    4. Re-freeze: compute the live sweep with `node -e` over `require('./scripts/check-tool-honesty.cjs').scanAll()` (toolCount, branchCount, and the verdict counts mapped onto the existing frozen_sweep keys high_risk, medium, low, unknown, ok). Confirm high_risk is 0 and that every non-OK row already has a disposition (claim_verify and claim_read rows must be OK; if either is not, STOP and fix the description in 358-04's file under a follow-up note rather than adding a disposition). Update frozen_sweep with those values. Replace refrozen_at with `{ commit: <short sha of HEAD, i.e. Task 1's registry commit>, reason: <one paragraph: re-frozen by plan 358-05 (Phase 358 B1); the 276-15 freeze at e484f4b3 (37 tools / 131 branches) was invalidated by commit 42191a6ae adding claim_verify and by plan 358-04 adding claim_read; the live scan is now N tools / B branches; claim_verify and claim_read rows are OK so no disposition was added; every prior disposition re-checked by tests/test-276-tool-honesty-findings-closed.cjs at this commit; HIGH_RISK is 0> }`. Leave schema_version, frozen_at_commit and dispositions untouched. Write the JSON back with the file's existing formatting (2-space indent, trailing newline) and confirm `git diff` touches only frozen_sweep and refrozen_at.
    5. Run `node tests/test-270-tool-schema-budget.cjs` and `node tests/test-276-tool-honesty-findings-closed.cjs` (both exit 0) and `node tests/test-234-tool-description-floor.cjs` (exit 0).
    6. Commit: `git add tests/test-270-tool-schema-budget.cjs tests/fixtures/tool-honesty/276-dispositions.json` then `git commit --only -m "test(358-05): re-baseline tool schema budget (<N> tools, <T> bytes, <+P>% vs 276-12) and re-freeze honesty sweep (<N>/<B>)" -- tests/test-270-tool-schema-budget.cjs tests/fixtures/tool-honesty/276-dispositions.json` with the real numbers substituted.
    7. Phase gate: run `bash tests/run-all-358.sh; echo "exit=$?"`; it must print exit=0 (77 means a leg was skipped: find and fix the missing file; 1 means a failure: fix the owning file under this phase, or, if a peer's uncommitted change caused it, record it). Then run `node scripts/doctor.cjs --acceptance` and classify every non-pass line per the CLAUDE.md QA rule (WORKING, known tracked bug, ENV GAP, NEW FAILURE); any NEW FAILURE caused by B1 files is fixed before this plan is marked done; peer-caused findings are listed in the SUMMARY with the owning phase. Record both outputs' summary lines in the SUMMARY.
  </action>
  <verify>
    <automated>node tests/test-270-tool-schema-budget.cjs >/dev/null && node tests/test-276-tool-honesty-findings-closed.cjs >/dev/null && node tests/test-234-tool-description-floor.cjs >/dev/null && bash tests/run-all-358.sh >/dev/null 2>&1; echo "runner exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-270-tool-schema-budget.cjs`, `node tests/test-276-tool-honesty-findings-closed.cjs` and `node tests/test-234-tool-description-floor.cjs` each exit 0 (the three tests red since 42191a6ae)
    - `grep -c "plan: '358-05'" tests/test-270-tool-schema-budget.cjs` is 1 and `grep -c "DRIFT_TOLERANCE_PCT = 10" tests/test-270-tool-schema-budget.cjs` equals its value at PLAN_BASE
    - `node -e "const j=require('./tests/fixtures/tool-honesty/276-dispositions.json'); const s=require('./scripts/check-tool-honesty.cjs').scanAll(); if(j.frozen_sweep.tools!==s.toolCount||j.frozen_sweep.branches!==s.branchCount||!/358-05/.test(j.refrozen_at.reason)) process.exit(1)"` exits 0
    - `git log -1 --format=%s` names the measured tool count, total bytes and percentage
    - `bash tests/run-all-358.sh` exits 0 and its summary line shows FAILED=0 SKIPPED=0
    - The SUMMARY records the doctor --acceptance summary and a classification for every non-pass line
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-270-tool-schema-budget.cjs tests/fixtures/tool-honesty/276-dispositions.json` prints 0 for both
  </acceptance_criteria>
  <done>Every test left red by the substrate commit is green, the baselines moved deliberately with measured numbers, and the whole B1 runner passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> generated registry and measured baseline | Generators and the budget test read the working tree, which peers share |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-23 | Tampering | regenerating a shared registry over a peer's uncommitted work | mitigate | Clean-tree precondition on data/, lib/mcp/tools/, commands/, skills/, agents/ before regenerating; diff inspection limits the commit to claim_read; commit --only |
| T-358-24 | Repudiation | a baseline moved silently to hide drift | mitigate | Measured numbers and percentage written into a comment block and the commit message; tolerance unchanged (acceptance criterion) |
| T-358-25 | Tampering | honesty findings suppressed by a re-freeze | mitigate | Re-freeze touches only frozen_sweep and refrozen_at; dispositions untouched; GROUP A still demands a disposition for every non-OK row; HIGH_RISK must be 0 |
</threat_model>

<verification>
- `node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check && node scripts/check-render-coverage.cjs --check && node scripts/build-skill-mirrors.cjs --check && node scripts/check-tool-honesty.cjs --check`
- `bash tests/run-all-358.sh` exits 0
- `node scripts/doctor.cjs --acceptance` run and classified
</verification>

<success_criteria>
- B1-07: claim_read born wired in the generated registry; test-234 floor, test-270 budget and test-276 sweep green; the phase runner exits 0
</success_criteria>

<output>
Create `.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-05-SUMMARY.md` when done (stage it with `git add -f`).
</output>
