---
phase: 358-rome-b1-checking-record-and-b2-frame-provenance-user-visible
plan: 08
type: execute
wave: 2
depends_on: ["358-07"]
files_modified:
  - tests/test-358-b2-routing.cjs
  - tests/test-358-b2-cli.cjs
  - lib/core/frame-provenance.cjs
  - scripts/room-question.cjs
  - commands/room.md
  - skills/room/SKILL.md
autonomous: true
requirements: [B2-05, B2-03, B2-04]
canon_parts: [3, 7, 8, 9, 11, 12]
tags: [b2, cli, mos-room, governing-question, routing, rome, tdd, cirs]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: ["commands/room.md", "skills/room/SKILL.md"]
  surfaces_removed: []
  spine_consumed: ["frameProvenance.setGoverningQuestion", "frameProvenance.readGoverningQuestion", "frameProvenance.readQuestionHistory", "frameProvenance.renderQuestionChangeCard", "frameProvenance.resolveQuestionTurn", "navigation.FRAME_ORIGINS_ORDERED", "navigation.resolveByUser", "navigation.detectActiveRoom", "openRoomDbForCaller"]
  gate_impact: "none new in the generated registries. /mos:room gains the question subcommands and a Larry routing section in its body; its frontmatter changes are description, argument-hint and hitl_why only, and none of those feeds data/connector-registry.json (reads the connector block), data/command-registry.json (reads teaching, inputs and serves_jtbd) or data/render-coverage-registry.json (reads hitl_shape and the body AskUserQuestion mention). hitl_shape stays F.1 so /mos:room stays in the render-coverage md keyspace, wired. The teaching line Theo reads changes in 358-10 together with the registry regenerate. skills/room/SKILL.md is regenerated by scripts/build-skill-mirrors.cjs, never hand-edited. scripts/room-question.cjs is a helper script, not an invocable surface. This plan must commit BEFORE 358-09 creates lib/mcp/tools/question.cjs, because the pre-commit connector-registry guard runs on any staged commands/*.md and would report drift for the new MCP tools until 358-10 regenerates."
  explanation: "Canon Part 7 reuse-before-build: /mos:jtbd sets the per-turn JTBD label, /mos:beautiful-question is a question-crafting method, /mos:status shows MINTO section summaries; none stores the room's question. /mos:room already owns viewing the room and hosts the B1 subcommands, so B2 extends it instead of minting a command. The script is one thin caller of lib/core/frame-provenance.cjs, the same door the MCP tools call (Part 9). The declared F.1 shape stays true: the question-change ask is one card through the SEED-020 pickShape door. Larry reaches the door through the tightened description and the routing section, both built from the same routing examples the spec tests; skills/larry-personality/SKILL.md is owned by four open peer plans and is not touched."

must_haves:
  truths:
    - "On the Claude Code CLI an officer shows the room's governing question with its origin and time (`/mos:room question`), opens every earlier version (`/mos:room question history`), and records or changes it (`/mos:room question set <question>`) with no developer involved (B2-AT1, B2-AT2 on CLI; B2-05)"
    - "The CLI set flow asks where the question came from from the one origin constant, and on a change the script refuses without an account or an explicit relocate, prints the ask 'What did the old question get wrong?' plus the Decision Gate card, and records the waiting change; Larry fires that card and passes the officer's own words as --account or --relocate on an explicit choice, never a default (B2-AT3 part a on CLI, B2-AT4; B2-03)"
    - "Every CLI read prints the waiting change FIRST, then the current question, then history, verbatim from the core renders (B2-AT3 part b on CLI; B2-04)"
    - "A deterministic routing spec (QUESTION_TURN_PATTERNS, QUESTION_TURN_EXAMPLES, resolveQuestionTurn in lib/core/frame-provenance.cjs) resolves question-change turns to question_set, question asks to question_read, and unrelated turns (an ordinary question, a claim check, a room summary) to no door; the /mos:room description and its routing section teach Larry exactly those examples (navigator requirement 2026-09-23 item 1)"
    - "scripts/room-question.cjs is a thin caller: no direct database access, no Brain reach, no origin literal, no card construction of its own; exit 0 ok, 1 refusal or no room, 2 usage"
    - "The /mos:room connector block, hitl_shape F.1, layer, body_shape and teaching are byte-unchanged here; tests/test-358-b1-cli.cjs stays green"
  artifacts:
    - path: "scripts/room-question.cjs"
      provides: "argv switch-case CLI: show | history | set | cancel | origins | help"
      min_lines: 150
    - path: "lib/core/frame-provenance.cjs"
      provides: "routing spec: QUESTION_TURN_PATTERNS, QUESTION_TURN_EXAMPLES, resolveQuestionTurn (additive)"
      contains: "resolveQuestionTurn"
    - path: "commands/room.md"
      provides: "question subcommands, the routing section, tightened description"
      contains: "## Subcommand: question"
    - path: "skills/room/SKILL.md"
      provides: "regenerated mirror"
      contains: "## When Larry routes a turn to the question door"
    - path: "tests/test-358-b2-routing.cjs"
      provides: "routing spec corpus legs and room.md routing-text legs"
      min_lines: 90
    - path: "tests/test-358-b2-cli.cjs"
      provides: "CLI legs and room.md wiring legs for B2-05"
      min_lines: 180
  key_links:
    - from: "scripts/room-question.cjs"
      to: "lib/core/frame-provenance.cjs setGoverningQuestion / readGoverningQuestion / readQuestionHistory"
      via: "navigation.openRoomDbForCaller handle, closed in finally"
      pattern: "frameProvenance\\.setGoverningQuestion\\("
    - from: "commands/room.md question set flow"
      to: "scripts/room-question.cjs set"
      via: "Bash call after the origin card; the card from the refusal fires AskUserQuestion"
      pattern: "room-question\\.cjs\" set"
    - from: "commands/room.md routing section"
      to: "lib/core/frame-provenance.cjs QUESTION_TURN_EXAMPLES"
      via: "every example phrase quoted verbatim (tested)"
      pattern: "When Larry routes a turn to the question door"
---

<objective>
Give officers on the Claude Code CLI the governing question in the normal flow, and teach Larry when to reach for it. Add a deterministic routing spec to lib/core/frame-provenance.cjs, a thin argv CLI (scripts/room-question.cjs) over the 358-07 door, and `/mos:room question`, `question history`, `question set <question>`, `question cancel` with a routing section that names exactly when a turn goes to the question door and when it does not. Tests first.

Purpose: B2-AT1, AT2, AT3 (parts a and b) and AT4 on the CLI half of the Tri-Polar surface (B2-05), plus the navigator requirement (2026-09-23, item 1) that Larry triggers the door relevantly: a question-change utterance resolves to the door, an unrelated turn does not. Reuse-before-build (Canon Part 7): /mos:room already hosts B1's checks, claim and check.

Output: the routing spec (additive in frame-provenance.cjs), scripts/room-question.cjs, the extended commands/room.md and its regenerated skill mirror, two new test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-CONTEXT.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-B2-RESEARCH.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-07-SUMMARY.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-03-SUMMARY.md

Interfaces this plan consumes (created by 358-07; read lib/core/frame-provenance.cjs and the 358-07 SUMMARY before starting, and STOP if any name below is missing): setGoverningQuestion(db, roomDir, params), readGoverningQuestion(db, roomDir), readQuestionHistory(db, roomDir), renderQuestionLines(view), renderHistoryLines(history), renderQuestionChangeCard({previous, proposed}), QUESTION_ASK, QUESTION_CARD_OPTIONS, MAX_QUESTION_CHARS, MAX_ACCOUNT_CHARS; navigation.FRAME_ORIGINS_ORDERED, navigation.frameOriginInfo, navigation.openRoomDbForCaller, navigation.closeRoomDbForCaller, navigation.detectActiveRoom, navigation.resolveByUser. A refusal with reason change_needs_account carries {ask, previous, proposed, options, pending, card}, and card carries {shape, header, options, rendered_text, askuserquestion_marker, askuserquestion_binding}.

Routing spec (navigator requirement 2026-09-23 item 1, "Larry triggers it relevantly"). What is and is not possible for 6 October, stated honestly: on every host Larry picks a surface by reading its description (Claude Code skills and MCP tools alike). The per-turn CLI router (scripts/intent-classifier.cjs, open plan 360-07) and the next-move engine (lib/core/navigation-engine.cjs, open plan 355-22) are peer territory, sensors are a hard do-not-touch, and skills/larry-personality/SKILL.md is owned by open plans 355-23, 357-10, 359-09 and 359-11. So the routing lives in (a) the /mos:room description and a routing section in its body (mirrored into skills/room/SKILL.md, which Claude Code reads for skill routing), (b) the MCP descriptions in 358-09, and (c) one deterministic spec, resolveQuestionTurn, whose example phrases are the single source both descriptions quote, so the test proves the routing text and the spec agree. A runtime per-turn router is a logged follow-up in the 358-11 handoff, not a 6 October item. The patterns below were run against the change, ask and unrelated corpora in Task 1 at planning time with zero misses.

Parallel-session rules (every task): record `PLAN_BASE=$(git rev-parse HEAD)`. Stage explicit paths only and commit with `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git add .`, `git commit -a`, `git stash`, `--no-verify`, or a gsd-tools commit that sweeps the index. Never revert or stage a file this plan does not own. Never touch: docs/reviews/*, evals/plurai/*, scripts/eval-icm-writers.cjs, scripts/jev-devtime-client.cjs, tests/test-353-* (run only), .planning/phases/356-*, data/jev-policies/, .planning/STATE.md, lib/mcp/**, skills/larry-personality/SKILL.md, agents/larry-extended.md, scripts/intent-classifier.cjs, lib/core/navigation-engine*.cjs, lib/core/sensors/*, data/*.json, CLAUDE.md. docs/FRAME-PROVENANCE-PRODUCT-WORKUP.md is PEER-UNTRACKED: read only. No em-dashes. CJS only. Before editing commands/room.md or skills/room/SKILL.md, `git status --short -- commands/room.md skills/room/SKILL.md` must print nothing; if it does, STOP (a peer is editing them).

Ordering rule inside this phase: this plan must be fully committed before 358-09 starts (358-09 depends on it). The moment lib/mcp/tools/question.cjs exists on disk, `node scripts/build-connector-registry.cjs --check` reports drift until 358-10 regenerates, and the pre-commit hook runs that check on every staged commands/*.md or skills/*/SKILL.md, so a later room.md commit would be blocked.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| Hardened `lib/core/navigation/typed-frame.cjs`; new `lib/core/frame-provenance.cjs` (the one door, readers, renders, the Decision Gate card); `lib/core/navigation.cjs` re-exports; regenerated `data/render-coverage-registry.json`; core, render and Part 8 tests; updated `tests/test-b2-frame-provenance.cjs`; `tests/run-all-358.sh` B2 legs | 07 |
| Routing spec in `lib/core/frame-provenance.cjs` (QUESTION_TURN_PATTERNS, QUESTION_TURN_EXAMPLES, resolveQuestionTurn); `scripts/room-question.cjs` (show, history, set, cancel, origins, help); `/mos:room question`, `question history`, `question set <question>`, `question cancel`, the routing section and tightened description; regenerated `skills/room/SKILL.md`; `tests/test-358-b2-routing.cjs`, `tests/test-358-b2-cli.cjs` | 08 (this plan) |
| `lib/mcp/tools/question.cjs` (question_read, question_set, connectors, descriptions from the origin constant and the routing examples, the card in every refusal); `hostBlock` export on `lib/mcp/tools/claim-verify.cjs`; `tests/test-358-b2-surfaces.cjs`, `tests/helpers/b2-358-child.cjs`, `tests/test-358-b2-persistence.cjs` | 09 |
| `/mos:room` teaching line for Theo's command layer; regenerated `skills/room/SKILL.md`, `data/command-registry.json`, `data/mcp-tool-connectors.json`, `data/connector-registry.json`, `data/connector-coverage-ledger.json`, `data/harness-manifest.json`; test-270 AFTER re-baseline; 276 re-freeze; recorded command-registry sha256 | 10 |
| `docs/2026-10-06-ROME-B2-GO-NO-GO.md` + human checkpoint | 11 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - routing spec and CLI tests</name>
  <files>tests/test-358-b2-routing.cjs, tests/test-358-b2-cli.cjs</files>
  <read_first>
    - lib/core/frame-provenance.cjs (as landed by 358-07: exports, refusal shapes, render lines, the card)
    - tests/test-358-b1-cli.cjs (lines 1-120 hermetic env, makeRoom, spawn helper, exit-code checks; lines 360-404 the room.md wiring legs that must stay green)
    - scripts/claim-checks.cjs (parseArgs, resolveRoomDir, withRoomHandle, printRefusal, usageError: the conventions to mirror)
    - commands/room.md (frontmatter lines 1-35 and the checks / claim / check sections, lines 254-384)
  </read_first>
  <behavior>
    Both files: hermetic env set before any require (fresh HOME, MINDRIAN_HOME, MINDRIAN_ROOMS_HOME; delete CLAUDE_ACTIVE_ROOM, MINDRIAN_MCP_FIRST, MINDRIAN_BRAIN_KEY); `ok - ` / `FAIL - ` lines; exit 1 on any FAIL. Fixtures Q1 'Which camera solves the delay?', Q2 'Which camera works with sunglasses?', Q3 'Should the checkpoint move north?', A2 'It assumed the delay was the camera, but the officers wear sunglasses.'

    tests/test-358-b2-routing.cjs (labels `T<n>:` for spec legs, `W<n>:` for routing-text legs):
    - T1 change corpus -> resolveQuestionTurn(u) returns {door:'question_set', intent:'change'} for each of: 'Our question is now: which camera works with sunglasses?', 'Change our governing question to which camera works with sunglasses', "Let's reframe the question: should the checkpoint move north?", "Record the room's governing question: which camera solves the delay?", 'The tasking changed, so we need to look again at the checkpoint', 'Set our question to which camera works with sunglasses'.
    - T2 ask corpus -> {door:'question_read'} with intent 'ask' or 'history' for each of: 'What is our governing question?', 'Where did our question come from?', 'Show me the question history', 'How has our question changed?', "What's the room's question?".
    - T3 unrelated corpus -> door null for each of: 'What is a camera?', 'I have a question about the bridge', 'Question: how long is the bridge?', 'Any questions before we file?', 'Check the bridge claim against the field note, exercise 02', 'Summarize the room', 'Can you answer this question for me: which camera is cheapest?', "That's a good question", 'Record that I checked the bridge claim', "Let's change the plan", 'The question of cost is still open', 'Show me the room', 'What is the question behind the cost estimate about?', 'Please update the claim about the bridge'.
    - T4 self-consistency: every QUESTION_TURN_EXAMPLES.change phrase, with '...' replaced by 'which camera works with sunglasses?', resolves to question_set; every QUESTION_TURN_EXAMPLES.ask phrase (with '?' appended) resolves to question_read.
    - T5 robustness: resolveQuestionTurn(null), (42), ('') and a 10000-character string return door null without throwing; curly apostrophes (U+2019) in "What’s the room’s question?" still resolve to question_read; QUESTION_TURN_PATTERNS and QUESTION_TURN_EXAMPLES are frozen.
    - W1: the commands/room.md frontmatter description line contains "governing question" and does not contain the bare word "questions".
    - W2: commands/room.md contains the exact header '## When Larry routes a turn to the question door', and that section contains every QUESTION_TURN_EXAMPLES phrase verbatim and the exact sentence "An ordinary question asked in conversation is not the room's governing question; do not route it here."
    - W3: skills/room/SKILL.md contains the same header and sentence (the mirror carries the routing text to Claude Code's skill router).
    - The file ends by printing 'routing text: <k> of 3 checked' where k counts W1-W3 legs that ran green (358-10 requires 3 of 3).

    tests/test-358-b2-cli.cjs (spawn `node scripts/room-question.cjs ...` with the hermetic env; temp rooms via openRoomDb/closeRoomDb):
    - L1: `origins --json` prints a JSON array whose ids and labels equal navigation.FRAME_ORIGINS_ORDERED (each entry {id, label, position}); exit 0.
    - L2: `show --room R` on an empty room exits 0 and prints 'No governing question recorded yet.'.
    - L3: `set --room R --origin tasking Which camera solves the delay?` exits 0 and prints 'Recorded: version 1 (first question)'; then `show --room R` prints 'Governing question (version 1 of 1): Which camera solves the delay?' and 'Origin: tasking - ' + frameOriginInfo('tasking').label.
    - L4: `set --room R --origin chosen --based-on 1 Which camera works with sunglasses?` exits 1; stderr contains 'x The question was not changed yet', '  Why: change_needs_account', '  Previous question (version 1): Which camera solves the delay?', '  Proposed question: Which camera works with sunglasses?', '  What did the old question get wrong?'; stdout contains '[AskUserQuestion contract: shape=F.1 verbs=4]' and each QUESTION_CARD_OPTIONS string; then `show --room R` stdout's first line is 'A question change is waiting.'.
    - L5: `set --room R --origin chosen --based-on 1 --account "<A2>" Which camera works with sunglasses?` exits 0, prints 'Recorded: version 2 (refines version 1)' and 'What the old question got wrong: ' + A2; `history --room R` prints 'Question history (2 versions, oldest first)' and no longer starts with the waiting block.
    - L6: `set --room R --origin prompt --relocate Should the checkpoint move north?` exits 0 and prints 'Recorded: version 3 (relocates from version 2)'.
    - L7: `set --room R Which camera?` (no --origin) and `set --room R --origin bogus Which camera?` both exit 1 with '  Why: invalid_origin' and '  Fix: allowed origins: ' + the ids joined by ', ' (built from the constant in the test).
    - L8: `set --room R --origin chosen --account x --relocate Another question?` exits 1 with '  Why: ambiguous_change'.
    - L9: after a refused change, `cancel --room R` exits 0 and prints 'The waiting question change was cancelled.'; a second `cancel --room R` exits 1 with '  Why: nothing_pending'.
    - L10: `show --room R --json`, `history --room R --json` and a refused `set ... --json` each print exactly one parseable JSON line with a boolean ok; the refused one has reason change_needs_account and a card object.
    - L11: `show` with no --room and no active room exits 1 with 'x No Data Room found'.
    - L12: `help` exits 0 and names show, history, set, cancel, origins; no subcommand exits 2; an unknown subcommand exits 2.
    - L13 (static): scripts/room-question.cjs requires only node builtins, ../lib/core/navigation.cjs and ../lib/core/frame-provenance.cjs (path.join forms allowed); no node:sqlite, room-db, brain-client or network module; its non-comment lines contain neither 'tasking' nor 'inherited' nor 'pickShape'; no em-dash.
    - L14 (room.md wiring): '## Subcommand: question' present; 'scripts/room-question.cjs' appears at least 5 times; 'What did the old question get wrong?' appears; 'AskUserQuestion' appears between '## Subcommand: question' and the next '## Subcommand:' header; the argument-hint line contains 'question' and still contains 'checks', 'claim' and 'check'; the file still contains 'before the [section] fallback' and the matched-before sentence names `question`; the question section contains 'Governing Thought' only inside the sentence saying it is a different thing; the line 'hitl_shape: "F.1"' is present exactly once.
    - L15: spawning `node scripts/build-skill-mirrors.cjs --check` exits 0.
  </behavior>
  <action>
    Write both test files to the behavior above, mirroring tests/test-358-b1-cli.cjs's harness (spawnSync with the hermetic env, `cwd` the repo root). Resolve every expected label and allowed-value list from navigation.cjs or frame-provenance.cjs at test time, never a second literal list. Run both: they must fail now (resolveQuestionTurn, the script and the room.md sections do not exist yet).
    Commit (RED): `git add tests/test-358-b2-routing.cjs tests/test-358-b2-cli.cjs` then `git commit --only -m "test(358-08): RED routing spec and CLI tests for the governing question" -- tests/test-358-b2-routing.cjs tests/test-358-b2-cli.cjs`.
  </action>
  <verify>
    <automated>! node tests/test-358-b2-routing.cjs >/dev/null 2>&1 && ! node tests/test-358-b2-cli.cjs >/dev/null 2>&1 && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and each exits non-zero now
    - `grep -c "Can you answer this question for me" tests/test-358-b2-routing.cjs` is 1 and `grep -c "When Larry routes a turn to the question door" tests/test-358-b2-routing.cjs` is at least 1
    - `grep -c "change_needs_account" tests/test-358-b2-cli.cjs` is at least 2
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-358-b2-routing.cjs tests/test-358-b2-cli.cjs` prints 0 for both
    - `git show --stat HEAD --format=%s` lists exactly the two test files
  </acceptance_criteria>
  <done>The CLI contract and the routing rule are failing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - routing spec in frame-provenance.cjs and scripts/room-question.cjs</name>
  <files>lib/core/frame-provenance.cjs, scripts/room-question.cjs</files>
  <read_first>
    - lib/core/frame-provenance.cjs (whole file as landed by 358-07; add only, change nothing that exists)
    - scripts/claim-checks.cjs (whole file: the argv router, BOOLEAN_FLAGS, resolveRoomDir, withRoomHandle, printNoRoom, printRefusal, usageError, exit codes 0 / 1 / 2)
    - tests/test-358-b2-routing.cjs and tests/test-358-b2-cli.cjs (Task 1)
  </read_first>
  <behavior>
    - Every T leg in tests/test-358-b2-routing.cjs passes (W legs wait for Task 3).
    - Every L leg in tests/test-358-b2-cli.cjs except L14 and L15 passes.
    - tests/test-358-b2-core.cjs, test-358-b2-render.cjs and test-358-b2-part8.cjs stay green; part8 now prints 'part8 static: 3 of 4 B2 files scanned'.
  </behavior>
  <action>
    Routing spec, appended to lib/core/frame-provenance.cjs (additive; existing exports untouched). Define `const QTERM = "(?:room'?s\\s+)?(?:governing\\s+)?question"` and QUESTION_TURN_PATTERNS = Object.freeze({history, change, ask}), each a frozen array of case-insensitive RegExps built from QTERM, evaluated in that precedence order (history first, so "how has our question changed" is an ask, then change, then ask):
    - history: `\bhow\s+(?:has|have|did)\s+(?:the|our|this)\s+` + QTERM + `\s+change`; `\bquestion\s+history\b`; `\bwhere\s+did\s+(?:the|our|this)\s+` + QTERM + `\s+come\s+from\b`.
    - change: `\b(?:change|changing|reframe|reframing|update|updating|replace|replacing|set|record|recording)\s+(?:the|our|this)\s+` + QTERM + `\b`; `\b(?:our|the|this)\s+` + QTERM + `\s+(?:is\s+now|has\s+changed|changed|should\s+(?:now\s+)?be|becomes|is\s+no\s+longer)\b`; `\bnew\s+governing\s+question\b`; `\b(?:our|the)\s+tasking\s+(?:has\s+)?changed\b`.
    - ask: `\b(?:what\s+is|what's|show(?:\s+me)?|remind\s+me\s+of)\s+(?:the|our|this)\s+` + QTERM + `(?:\s*[?.!]|\s*$|\s+and\b)`.
    QUESTION_TURN_EXAMPLES = Object.freeze({change: Object.freeze(['our question is now ...', 'change our governing question to ...']), ask: Object.freeze(['what is our governing question', 'where did our question come from', 'how has our question changed'])}) with a comment: the single source quoted by the /mos:room routing section and the question_read / question_set descriptions (358-09); change one, change all, the tests hold them together.
    resolveQuestionTurn(utterance): never throws; non-string or empty or longer than 4000 characters -> {door:null, intent:null}; normalize (U+2018 and U+2019 to an apostrophe, whitespace collapsed, trimmed); first matching group wins: history -> {door:'question_read', intent:'history'}, change -> {door:'question_set', intent:'change'}, ask -> {door:'question_read', intent:'ask'}; otherwise {door:null, intent:null}. Add a comment stating what this is and is not: the executable specification of the routing rule the descriptions teach; hosts route by description; it is not a runtime per-turn router (that would live in scripts/intent-classifier.cjs or the next-move engine, both peer territory). Export QUESTION_TURN_PATTERNS, QUESTION_TURN_EXAMPLES, resolveQuestionTurn.

    Create scripts/room-question.cjs (`#!/usr/bin/env node`, 'use strict', header comment: Phase 358-08, one thin caller of lib/core/frame-provenance.cjs, the same door the MCP tools call; zero direct database access, zero Brain reach; every origin shown is read from navigation.FRAME_ORIGINS_ORDERED at run time; no em-dashes). Mirror scripts/claim-checks.cjs's conventions exactly: parseArgs with BOOLEAN_FLAGS = new Set(['json', 'relocate']) and value flags room, origin, account, based-on; positionals joined with one space become the question text; resolveRoomDir (--room or navigation.detectActiveRoom()); a withRoomHandle that opens navigation.openRoomDbForCaller(roomDir) and closes it in finally; printNoRoom ('x No Data Room found' 3-line block, exit 1); usage to stdout for help (exit 0) and to stderr for a missing or unknown subcommand (exit 2). Subcommands:
    - `origins [--json]`: JSON array of {id, label, position} from FRAME_ORIGINS_ORDERED (text mode: one line per origin 'N. <id> - <label>').
    - `show [--room <dir>] [--json]`: renderQuestionLines(readGoverningQuestion(db, roomDir)) printed one line each; --json prints the view object as one line.
    - `history [--room <dir>] [--json]`: renderHistoryLines(readQuestionHistory(db, roomDir)).
    - `set [--room <dir>] --origin <id> [--account <words> | --relocate] [--based-on <N>] [--json] <question text>`: calls frameProvenance.setGoverningQuestion(db, roomDir, {text, origin, account, relocate: flags.relocate === true ? true : undefined, based_on_version: integer or undefined, set_by_id: navigation.resolveByUser(roomDir)}). Landed: print 'Recorded: version N (first question)' | 'Recorded: version N (refines version K)' | 'Recorded: version N (relocates from version K)'; for refines the next line is 'What the old question got wrong: ' + account_text (the officer sees exactly what was stored); then a blank line and renderQuestionLines of a fresh readGoverningQuestion; exit 0. Unchanged: 'Unchanged: the governing question is already this (version N).', exit 0.
    - `cancel [--room <dir>] [--json]`: setGoverningQuestion with {cancel:true}; ok prints 'The waiting question change was cancelled.' exit 0.
    Refusals (exit 1): --json prints the core result as one line; text mode writes to stderr a first line 'x The question was not changed yet' for change_needs_account and question_changed_meanwhile, else 'x Could not record the question', then '  Why: <reason>', then per reason: change_needs_account -> '  Previous question (version N): <text>', '  Proposed question: <text>', '  What did the old question get wrong?', '  Fix: re-run set with --account "<the officer\'s own words>" (files as refines), or --relocate (files as relocates), or run cancel', and ALSO print card.rendered_text to STDOUT (its footer carries the AskUserQuestion contract and the fire-if-fork binding Larry acts on); invalid_origin -> '  Fix: allowed origins: ' + ids joined by ', '; ambiguous_change -> '  Fix: pass --account or --relocate, not both'; question_changed_meanwhile -> '  Fix: run show, then set again with --based-on <current version>'; nothing_pending -> '  Fix: there is no waiting question change'; no_previous_question -> '  Fix: the first question needs no --account or --relocate'; invalid_text -> '  Fix: the question must be 1 to ' + MAX_QUESTION_CHARS + ' characters'; invalid_account -> '  Fix: the account must be at most ' + MAX_ACCOUNT_CHARS + ' characters'; any other reason -> '  Fix: check the value and try again'. Never construct a card here (no dispatcher require, no pickShape token) and never write the account for the officer. module.exports = { main, parseArgs }; run main only when require.main === module.

    Run: `node tests/test-358-b2-routing.cjs` (T legs green), `node tests/test-358-b2-cli.cjs` (all but L14, L15 green), `node tests/test-358-b2-core.cjs`, `node tests/test-358-b2-render.cjs`, `node tests/test-358-b2-part8.cjs`, `node scripts/check-render-coverage.cjs --check` (still 0: no new pickShape call site), `node --no-warnings -e "const s=require('./scripts/check-substrate.cjs'); const fs=require('fs'); process.exit(s.scanFiles(['scripts/room-question.cjs','lib/core/frame-provenance.cjs'], f=>fs.readFileSync(f,'utf8')).length?1:0)"`.

    Commit: `git add lib/core/frame-provenance.cjs scripts/room-question.cjs` then `git commit --only -m "feat(358-08): routing spec for question turns and scripts/room-question.cjs over the one door" -- lib/core/frame-provenance.cjs scripts/room-question.cjs`.
  </action>
  <verify>
    <automated>test "$(node tests/test-358-b2-routing.cjs 2>&1 | grep -c '^FAIL - T')" = 0 && test "$(node tests/test-358-b2-cli.cjs 2>&1 | grep '^FAIL - ' | grep -vc 'L1[45]')" = 0 && node tests/test-358-b2-core.cjs >/dev/null && node tests/test-358-b2-part8.cjs >/dev/null && node scripts/check-render-coverage.cjs --check >/dev/null && echo GREEN-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-358-b2-routing.cjs 2>&1 | grep -c '^FAIL - T'` is 0
    - `node tests/test-358-b2-cli.cjs 2>&1 | grep '^FAIL - ' | grep -vc 'L1[45]'` is 0
    - `node tests/test-358-b2-part8.cjs | grep -c "part8 static: 3 of 4 B2 files scanned"` is 1
    - `node scripts/room-question.cjs origins --json` prints a JSON array of 4 entries and exits 0
    - `grep -v '^\s*//' scripts/room-question.cjs | grep -c "pickShape"` is 0 and `grep -v '^\s*//' scripts/room-question.cjs | grep -cE "'(tasking|inherited)'"` is 0
    - `grep -c "resolveQuestionTurn" lib/core/frame-provenance.cjs` is at least 2
    - `git diff "$PLAN_BASE" -- lib/core/frame-provenance.cjs | grep '^-' | grep -v '^---' | wc -l` is 0 (additive only)
    - `node scripts/check-render-coverage.cjs --check` exits 0
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/room-question.cjs lib/core/frame-provenance.cjs` prints 0 for both
  </acceptance_criteria>
  <done>The routing rule is executable and the CLI can show, open the history of, set, and cancel a change to the room's question through the one door.</done>
</task>

<task type="auto">
  <name>Task 3: /mos:room question subcommands, the Larry routing section, regenerated mirror, gates green</name>
  <files>commands/room.md, skills/room/SKILL.md</files>
  <read_first>
    - commands/room.md (whole file: frontmatter lines 1-35, the parse sentence near line 65, the UI Format bullets, the checks / claim / check sections lines 254-384, Voice Rules lines 511-520)
    - scripts/room-question.cjs (Task 2 output: subcommands, flags, the refusal text and the card on stdout)
    - lib/core/frame-provenance.cjs (QUESTION_TURN_EXAMPLES and QUESTION_CARD_OPTIONS, to quote verbatim)
    - scripts/build-skill-mirrors.cjs (how the mirror is generated; never hand-edit skills/room/SKILL.md)
    - docs/HITL-SHAPE-DECLARATION-CONTRACT.md (lines 35-90: hitl_why is one plain sentence)
    - .git/hooks/pre-commit (lines 150-260 and 470-510: the guards that run when commands/*.md and skills/*/SKILL.md are staged)
  </read_first>
  <action>
    Precondition: `git status --short -- commands/room.md skills/room/SKILL.md lib/mcp/tools/` prints nothing (no peer edit, and lib/mcp/tools/question.cjs does not exist yet). If it prints anything, STOP.

    Edit commands/room.md (Canon Part 7 reuse; rulings 1, 2 and 3; navigator requirement item 1):
    1. Frontmatter, three lines only: `description:` becomes `View, launch, or navigate the Data Room, and show, record or change the room's governing question with its origin and history`; `argument-hint:` becomes `"[overview|<section>|checks|claim <id or words>|check <id or words>|question [history|set <question>|cancel]]"`; `hitl_why:` becomes `"Room navigation offers one next move from the current room, and a governing-question change asks one thing first, what the old question got wrong, on a single card."`. hitl_shape stays `"F.1"` (a hitl_stages declaration would drop /mos:room out of the render-coverage md keyspace, because scripts/stamp-firing-block.cjs parseShape reads hitl_shape only). The connector block, layer, body_shape, teaching, serves_jtbd and every other key stay byte-unchanged (the teaching line changes in 358-10 with the command-registry regenerate).
    2. Parse sentence near line 65: name `question` with the others: "The subcommands `checks`, `claim`, `check` and `question` are matched before the [section] fallback, so a section can never shadow them."
    3. UI Format: add the bullet "- **question subcommands:** render inside the Room Card 4-zone anatomy (Shape C); the Content Body prints the `scripts/room-question.cjs` output verbatim, and the waiting change (when there is one) always comes first."
    4. After the `## Subcommand: check` section and before `## Subcommand: add`, insert `## When Larry routes a turn to the question door` with: one plain sentence that the room's governing question is the one question the room's work serves, recorded with where it came from, and that it is NOT the "Governing Thought" line a section card prints (that line is a machine summary of what a section knows); "Route here when the officer:" then three bullets quoting QUESTION_TURN_EXAMPLES verbatim: states or changes the room's question (for example: "our question is now ...", "change our governing question to ..."), or asks about it (for example: "what is our governing question", "where did our question come from", "how has our question changed"); then the exact sentence "An ordinary question asked in conversation is not the room's governing question; do not route it here."; then: "If the officer raises a new question in conversation and you are about to work on it, ask first whether it is the room's new governing question; if yes, run the set flow below before answering." (the CLI half of the AT3 demo discipline).
    5. Then insert `## Subcommand: question` with triggers `/mos:room question`, `/mos:room question history`, `/mos:room question set <question>`, `/mos:room question cancel`, and these steps:
       - Step 1 Check for Room: `bash "${CLAUDE_PLUGIN_ROOT}/scripts/resolve-room"`, the 3-line no-room error, STOP (same as checks).
       - Step 2 Show (`question` alone): run `node "${CLAUDE_PLUGIN_ROOT}/scripts/room-question.cjs" show --room "<room path>"`; Shape C: Header `-- [Room Name] -- governing question -- [Venture Stage] --`, Content Body = the script output verbatim (never reorder: a waiting change is printed first by design); Action Footer `▶ /mos:room question history   See every version of the question` and `▷ /mos:room question set <question>   Record or change the question`.
       - Step 3 History: run `node "${CLAUDE_PLUGIN_ROOT}/scripts/room-question.cjs" history --room "<room path>"`, print verbatim, then one plain sentence: every version is kept exactly as it was written, and no version is ranked above another.
       - Step 4 Set: (a) run `... show --room "<room path>" --json` to read current.version and any pending change; (b) take the officer's question text exactly as written after `set` (if none, ask in plain text "What is the question this room's work serves?"); (c) run `... origins --json` and fire an AskUserQuestion card "Where did this question come from?" with one option per origin labeled `<id> - <label>` verbatim from the JSON (more than 4 origins: first 3 plus "More options"); (d) run `node "${CLAUDE_PLUGIN_ROOT}/scripts/room-question.cjs" set --room "<room path>" --origin <id> --based-on <current version, omitted when there is none> "<question text>"`; (e) branch: exit 0 -> Shape E mini report with the output verbatim; exit 1 with `Why: change_needs_account` -> the change is now waiting and the script printed the Decision Gate card with its AskUserQuestion contract: fire AskUserQuestion with exactly the card's options ("Write what it got wrong (files as refines)", "It is a new question (files as relocates)", "Cancel the change"); on "Write what it got wrong" ask in plain text "In your own words: what did the old question get wrong?" and pass the answer VERBATIM as `--account "<the officer's words>"` (escape double quotes; never write, shorten or polish it; if the officer typed the answer into the card's free-text field, that text is the account); on "It is a new question" re-run with `--relocate`; on "Cancel the change" run `... cancel --room "<room path>"`; exit 1 with `question_changed_meanwhile` -> show the block and restart from (a); any other refusal -> show the 3-line block verbatim and re-ask only the field it names.
       - Step 5 Cancel: `... cancel --room "<room path>"`, print verbatim.
       - Rules: the account is the officer's own words; never choose "It is a new question" for the officer and never treat a missing answer as one; never present either version as better, improved or corrected, both stay on the record; call it "Governing question", never "Governing Thought"; a question change only discussed in conversation is not recorded, the set flow is the only way to change it; recording a question never confirms or ranks anything.
    6. Voice Rules: add "- For question: the governing question is the officer's recorded question with its origin; it is not a section's Governing Thought, and no version is ranked above another."
    Keep hyphens only, no em-dashes. Do not add the word "verified" anywhere (the B1 test keeps `grep -ci verified commands/room.md` at 0).

    Regenerate the mirror: `node scripts/build-skill-mirrors.cjs`; `git status --short -- skills/` must list only skills/room/SKILL.md.

    Gates (all must exit 0 before committing): `node tests/test-358-b2-cli.cjs`, `node tests/test-358-b2-routing.cjs`, `node tests/test-358-b1-cli.cjs`, `node scripts/build-skill-mirrors.cjs --check`, `node scripts/check-shape-declaration.cjs --check`, `node scripts/build-connector-registry.cjs --check`, `node scripts/build-command-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs --check`, `node scripts/build-render-coverage.cjs --check`, `node scripts/check-help-coverage.cjs`, `node scripts/check-cirs-declaration.cjs --check .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-08-PLAN.md`. If build-connector-registry, build-command-registry or build-orchestration-projection report drift, the frontmatter edit touched something it must not: revert that part of the edit, never regenerate data/*.json here.

    Commit: `git add commands/room.md skills/room/SKILL.md` then `git commit --only -m "feat(358-08): /mos:room question, history, set and cancel, with the Larry routing section" -- commands/room.md skills/room/SKILL.md`. Then run `bash tests/run-all-358.sh; echo "exit=$?"` (expected exit 77 with FAILED=0: surfaces and persistence are SKIPPED until 358-09).
  </action>
  <verify>
    <automated>node tests/test-358-b2-cli.cjs >/dev/null && node tests/test-358-b2-routing.cjs >/dev/null && node tests/test-358-b1-cli.cjs >/dev/null && node scripts/build-skill-mirrors.cjs --check >/dev/null && node scripts/check-shape-declaration.cjs --check >/dev/null && node scripts/build-connector-registry.cjs --check >/dev/null && node scripts/build-command-registry.cjs --check >/dev/null && node scripts/check-render-coverage.cjs --check >/dev/null && echo WIRED-OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-358-b2-cli.cjs, tests/test-358-b2-routing.cjs and tests/test-358-b1-cli.cjs each exit 0; `node tests/test-358-b2-routing.cjs | grep -c "routing text: 3 of 3 checked"` is 1
    - `grep -c "^## Subcommand: question" commands/room.md` is 1 and `grep -c "^## When Larry routes a turn to the question door" skills/room/SKILL.md` is 1
    - `grep -c 'hitl_shape: "F.1"' commands/room.md` is 1 and the connector block is unchanged: `git diff "$PLAN_BASE" -- commands/room.md | grep -E '^[-+] +(connects_to_spine|sensor_triggers|reach_id|sub_mode|posture|hierarchy_rank|filing|surface):' | wc -l` is 0, and `git diff "$PLAN_BASE" -- commands/room.md | grep -c '^[-+]teaching:'` is 0
    - `node scripts/build-connector-registry.cjs --check`, `node scripts/build-command-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/build-skill-mirrors.cjs --check`, `node scripts/check-shape-declaration.cjs --check`, `node scripts/check-render-coverage.cjs --check` each exit 0
    - `node scripts/check-cirs-declaration.cjs --check .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-08-PLAN.md` exits 0
    - `git diff --quiet "$PLAN_BASE" -- skills/larry-personality/SKILL.md` exits 0 (peer-owned file untouched by this plan)
    - `grep -ci verified commands/room.md` is 0 and `grep -c "$(printf '\xe2\x80\x94')" commands/room.md skills/room/SKILL.md` prints 0 for both
    - `bash tests/run-all-358.sh` summary shows FAILED=0
  </acceptance_criteria>
  <done>An officer on the CLI can see, open the history of, set and turn around the room's question in the normal flow, and Larry's routing text names exactly when to use it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| officer -> Larry -> Bash argv | question and account text typed by the officer are passed by Larry as shell arguments |
| scripts/room-question.cjs -> frame-provenance.cjs | the script passes values as data to the one door |
| skill description -> host routing | the description decides when Claude Code reaches /mos:room |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-39 | Repudiation | Larry writes or polishes the account instead of the officer | mitigate | room.md rules: verbatim, never written or shortened by Larry; the script echoes 'What the old question got wrong: ...' so the officer sees the stored words. Residual (the tool cannot know who typed) accepted and stated in the 358-11 runbook |
| T-358-40 | Tampering | shell quoting of officer text in Bash | mitigate | room.md instructs double-quote escaping; the script never shells out and treats every value as data; the core validates lengths |
| T-358-41 | Repudiation | a missing answer landing as relocates | mitigate | --relocate is an explicit boolean flag; the flow re-runs with it only after the officer picks "It is a new question" |
| T-358-42 | Spoofing | loose skill description auto-firing /mos:room on any "question" | mitigate | Tight description (governing question, never the bare word "questions"), routing section with a do-not-route sentence, T3 unrelated corpus legs |
| T-358-43 | Tampering | regenerating the skill mirror over a peer's edit | mitigate | Clean-file precondition on commands/room.md and skills/room/SKILL.md; git status after regenerate must list only skills/room/SKILL.md |
</threat_model>

<verification>
- `node tests/test-358-b2-routing.cjs && node tests/test-358-b2-cli.cjs && node tests/test-358-b1-cli.cjs`
- `node scripts/build-skill-mirrors.cjs --check && node scripts/check-shape-declaration.cjs --check && node scripts/build-connector-registry.cjs --check && node scripts/build-command-registry.cjs --check && node scripts/check-render-coverage.cjs --check`
- `bash tests/run-all-358.sh` shows FAILED=0 (surfaces and persistence SKIPPED until 358-09)
</verification>

<success_criteria>
- B2-05: `/mos:room question`, `question history`, `question set <question>`, `question cancel` backed by scripts/room-question.cjs, born on the already-wired /mos:room surface
- B2-03 and B2-04 on the CLI: the refusal is the ask with its card, the waiting change prints first
- Navigator requirement item 1 (CLI half): routing spec tested against change, ask and unrelated turns; the /mos:room description and routing section teach exactly those examples
</success_criteria>

<output>
Create `.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-08-SUMMARY.md` when done (stage it with `git add -f` and commit it with `git commit --only`).
</output>
