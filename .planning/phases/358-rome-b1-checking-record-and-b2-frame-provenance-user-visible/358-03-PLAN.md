---
phase: 358-rome-b1-checking-record-and-b2-frame-provenance-user-visible
plan: 03
type: execute
wave: 2
depends_on: ["358-01"]
files_modified:
  - tests/test-358-b1-cli.cjs
  - scripts/claim-checks.cjs
  - commands/room.md
  - skills/room/SKILL.md
autonomous: true
requirements: [B1-02, B1-04, B1-05, B1-07]
canon_parts: [7, 8, 9, 11, 12]
tags: [b1, cli, mos-room, checking-record, rome, tdd, cirs]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: ["commands/room.md", "skills/room/SKILL.md"]
  surfaces_removed: []
  spine_consumed: ["navigation.recordClaimVerification", "navigation.readClaimVerification", "navigation.listClaimsForChecking", "navigation.readVerificationPortrait", "navigation.resolveByUser", "navigation.detectActiveRoom", "openRoomDbForCaller"]
  gate_impact: "none new. /mos:room gains three body subcommands (checks, claim, check); its frontmatter connector block, hitl_shape F.1, hitl_why, layer and body_shape declarations stay byte-unchanged, so data/connector-registry.json and data/brain-orchestration-projection.json do not move. argument-hint is the only frontmatter line that changes. skills/room/SKILL.md is regenerated by scripts/build-skill-mirrors.cjs, never hand-edited. scripts/claim-checks.cjs is a helper script, not an invocable surface."
  explanation: "Canon Part 7 reuse-before-build: no command shows claims from room.db today; /mos:room already owns viewing the room and has subcommands, so B1's CLI surface extends it instead of minting /mos:check (a new command would add a surface to every CIRS gate, the skill mirror and the dist bundles). The script is one thin caller of the same navigation functions the MCP tools call (Part 9: one write door, reads through navigation.cjs). Part 11 R16: the declared F.1 shape stays true because the new check flow asks the navigator through AskUserQuestion cards and never confirms a claim."

must_haves:
  truths:
    - "On the Claude Code CLI an officer records, on a claim, what it was checked against, the rung, the method and the result through `/mos:room check <claim id or words>`, backed by `node scripts/claim-checks.cjs record`, with no developer involved (AT1, CLI half)"
    - "`/mos:room claim <id or words>` reopens a claim in any later session and prints Confirmation status and Checking record side by side (AT2, AT4)"
    - "`/mos:room checks` prints the room portrait with checked, disputed, inconclusive and unchecked always shown (unchecked even at 0) and never a score (AT3)"
    - "The CLI write goes through navigation.recordClaimVerification only, stamps checked_by user and checked_by_id from navigation.resolveByUser(roomDir) (navigator ruling: Who checked), and never changes review_status (AT4)"
    - "The CLI never duplicates an enum or a rung label: every option list and usage line is generated from navigation.VERIFICATION_RUNGS, VERIFICATION_AGAINST_KINDS, VERIFICATION_METHODS and VERIFICATION_RESULTS (Rung decision: swapping the list is a one-constant edit)"
    - "--resolves-dispute is passed only after the person explicitly answers Yes to 'Does this check answer the earlier contradiction?' (navigator ruling: Disputed sticks, only a person resolves it)"
    - "scripts/claim-checks.cjs never requires node:sqlite and never reaches the Brain (Canon Part 8, check-substrate)"
  artifacts:
    - path: "scripts/claim-checks.cjs"
      provides: "argv switch-case CLI: rungs | options | portrait | list | show | record | help"
      exports: ["main", "parseArgs"]
      min_lines: 180
    - path: "tests/test-358-b1-cli.cjs"
      provides: "CLI legs: rungs, options, empty portrait, record, words resolution, ambiguity, invalid enums, usage, show in a new process, list, JSON parity, resolves-dispute, no room, substrate hygiene, room.md wiring"
      min_lines: 170
    - path: "commands/room.md"
      provides: "Subcommands checks, claim, check matched before the [section] fallback"
      contains: "## Subcommand: checks"
    - path: "skills/room/SKILL.md"
      provides: "regenerated byte mirror of commands/room.md"
      contains: "## Subcommand: checks"
  key_links:
    - from: "commands/room.md"
      to: "scripts/claim-checks.cjs"
      via: "node \"${CLAUDE_PLUGIN_ROOT}/scripts/claim-checks.cjs\" record|show|list|portrait|options"
      pattern: "scripts/claim-checks\\.cjs"
    - from: "scripts/claim-checks.cjs"
      to: "lib/core/navigation.cjs"
      via: "recordClaimVerification, readClaimVerification, listClaimsForChecking, readVerificationPortrait, render helpers"
      pattern: "navigation\\.recordClaimVerification"
---

<objective>
Give the Claude Code CLI its normal-flow B1 surface: a small argv-routed script `scripts/claim-checks.cjs` that calls the core functions from 358-01, and three new `/mos:room` subcommands (`checks`, `claim`, `check`) that drive it, with Larry collecting the four fields through AskUserQuestion cards. Tests first.

Purpose: AT1 requires the CLI half ("a /mos: command or Larry-driven turn") and AT2/AT3 need a way to reopen a claim and see the room counts from the terminal. Implements CONTEXT Claude's Discretion "Exact CLI surface ... chosen by reuse-before-build" (the research's Part 7 search found no command that shows claims; /mos:room owns "view the room"), plus the Disputed sticks and Who checked rulings on the CLI side.

Ordering note (why this plan is wave 2 and the MCP plan is wave 3): committing commands/room.md triggers the pre-commit hook, which runs `node scripts/build-connector-registry.cjs --check` over the WORKING TREE. Once 358-04 adds the claim_read connector, that check reports drift until 358-05 regenerates the registry. Landing this plan first keeps this commit's hook green.

Output: `scripts/claim-checks.cjs`, `tests/test-358-b1-cli.cjs`, the edited `commands/room.md`, the regenerated `skills/room/SKILL.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-CONTEXT.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-RESEARCH.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-01-SUMMARY.md
@commands/room.md

Parallel-session rules (every task): peers execute Phases 354-357 in this same working tree. Record `PLAN_BASE=$(git rev-parse HEAD)` at start. Before editing any file run `git status --short -- <file>` and STOP if it shows a diff you did not make (never revert, stash, checkout or overwrite an unowned diff). Stage explicit paths only and commit with `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git add .`, `git commit -a`, `git stash`, or `--no-verify`. If the pre-commit hook fails on a drift this plan did not cause (a peer's uncommitted edit to a command, skill, agent or tools file), STOP and report it; never regenerate a shared registry over a peer's work and never bypass the hook. Never touch: docs/reviews/*, evals/plurai/*, scripts/eval-icm-writers.cjs, scripts/jev-devtime-client.cjs, tests/test-353-* (run only), .planning/phases/356-*, data/jev-policies/, .planning/STATE.md, lib/mcp/tools/gate.cjs, lib/mcp/tools/views.cjs, lib/mcp/tool-router.cjs, lib/mcp/register-core-tools.cjs, lib/mcp/tools/status.cjs, CLAUDE.md, any data/*.json. No em-dashes (hyphens only).

Interfaces this plan consumes (created by 358-01, re-exported by lib/core/navigation.cjs):
- `navigation.recordClaimVerification(db, { claim_id, against_id, against_kind, method, result, rung, checked_by, checked_by_id, note_handle, resolves_dispute })` -> `{ ok, claim_id, review_status, verification }` or `{ ok: false, reason }` (reasons include invalid_rung, invalid_method, invalid_against_kind, invalid_result, claim_not_found, resolution_requires_person, invalid_resolution, no_dispute_to_resolve)
- `navigation.readClaimVerification(db, claimId)` -> `{ ok, claim }` or `{ ok: false, reason: 'claim_not_found' }`
- `navigation.listClaimsForChecking(db, { query, limit })` -> `{ ok, total_matched, claims }`
- `navigation.readVerificationPortrait(db)` -> portrait object
- `navigation.renderVerificationPortraitLines(p)`, `navigation.renderClaimViewLines(claim)`, `navigation.renderClaimListLines(claims)` -> string[]
- `navigation.VERIFICATION_RUNGS` (frozen `{id, label}` array, rung = index + 1), `navigation.VERIFICATION_AGAINST_KINDS`, `navigation.VERIFICATION_METHODS`, `navigation.VERIFICATION_RESULTS` (frozen Sets)
- `navigation.resolveByUser(roomDir)`, `navigation.detectActiveRoom()` -> `{ roomDir, hasRoomDb } | null`, `navigation.openRoomDbForCaller(roomDir)` / `navigation.closeRoomDbForCaller(db)`
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `lib/core/navigation/verification.cjs` (rung constant, record fields, sticky disputed, claim view + list readers, portrait by rung, render helpers), `lib/core/navigation.cjs` re-exports, core/portrait/separation tests, updated `tests/test-b1-verification.cjs` | 01 |
| `lib/core/navigation/typed-claim.cjs` carry-forward + PROTECTED_CLAIM_KEYS, `tests/test-358-b1-refile.cjs`, `lib/mcp/surface-detect.cjs` claude-ai tier0 host, `tests/test-234-host-tier.cjs` additions, `tests/run-all-358.sh` | 02 |
| `scripts/claim-checks.cjs`, `/mos:room checks`, `/mos:room claim`, `/mos:room check`, regenerated `skills/room/SKILL.md`, `tests/test-358-b1-cli.cjs` | 03 (this plan) |
| `claim_verify` (rung required, checked_by_id, resolves_dispute, description >= 120 chars), new `claim_read` MCP tool, surfaces test, child helper, persistence test | 04 |
| Regenerated `data/mcp-tool-connectors.json` + `data/connector-registry.json`, test-270 AFTER re-baseline, 276 ledger re-freeze | 05 |
| `docs/2026-10-06-ROME-B1-GO-NO-GO.md` (operator runbook) + human checkpoint | 06 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - CLI test for claim-checks.cjs and the /mos:room wiring</name>
  <files>tests/test-358-b1-cli.cjs</files>
  <read_first>
    - .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-01-SUMMARY.md (the exact export names and render line formats that landed)
    - lib/core/navigation/verification.cjs (render helpers, reasons, VERIFICATION_RUNGS)
    - tests/test-358-b1-core.cjs (hermetic temp-room setup to copy)
    - lib/core/navigation/dashboard-helpers.cjs (lines 45-75: detectActiveRoom reads MINDRIAN_ROOMS_HOME or HOME/MindrianRooms registry)
    - scripts/check-substrate.cjs (lines 1-40: default mode is an informational full scan of lib/ and scripts/)
  </read_first>
  <behavior>
    - `rungs` prints one line per VERIFICATION_RUNGS entry, 'N. <label>', in order; `rungs --json` prints an array of { rung, id, label }
    - `options` prints JSON { rungs, against_kinds, methods, results } equal to the navigation constants (Sets as arrays)
    - `portrait --room <empty room>` exits 0 and prints 'checked: 0', 'disputed: 0', 'inconclusive: 0' and 'unchecked (no checking record yet): 0'; the output never matches /score|percent|pct|ratio|grade|coverage|%/i
    - with two seeded claims ('The bridge at grid 42 is passable.' and 'The ford at grid 17 is too deep.'): `record --room R --claim <bridge id> --against "field note, exercise 02" --against-kind observation --rung 3 --method compare --result contradicts` exits 0 and prints 'Its confirmation status did not change.', a line starting 'Confirmation status: proposed' and a line starting 'Checking record: disputed' and 'rung 3 ('
    - after that record, nodes.review_status is still 'proposed'; the stored record has checked_by 'user' and checked_by_id equal to navigation.resolveByUser(R) evaluated in the test process under the same env ('navigator' for a room without USER.md)
    - `record ... --claim bridge` (words) resolves the unique claim; `--claim grid` (matches both) exits 1 and prints 'ambiguous_claim' plus both claim ids; `--claim tank` exits 1 with 'claim_not_found'
    - `--rung 9` exits 1 and prints 'invalid_rung'; omitting --rung exits 2 and the usage names --rung; `--method guess` exits 1 and prints 'invalid_method' followed by the allowed methods from the navigation Set
    - `show --room R <bridge id>` run in a NEW process prints the record (checked against, rung, method, result, by, when) and 'Confirmation status: proposed'; `show --room R bridge` works by words; `show --json` prints { ok: true, room_dir, claim } with claim.checking_record.status 'disputed'
    - `list --room R` prints both claims with their checking status; `list --room R --query ford` prints only the ford claim
    - `portrait --room R --json` deep-equals navigation.readVerificationPortrait computed in the test process
    - a later `record ... --result supports` keeps the bridge claim disputed; `record ... --result supports --resolves-dispute` makes it checked, and the claim view prints 'a person marked the earlier contradiction answered'
    - `portrait` with no --room, with MINDRIAN_ROOMS_HOME and HOME pointed at an empty temp dir, exits 1 and prints 'No Data Room found'
    - comment-stripped scripts/claim-checks.cjs contains no 'node:sqlite', 'DatabaseSync', 'better-sqlite3' or 'brain'; contains openRoomDbForCaller and closeRoomDbForCaller; contains no quoted literal of any method, result, against_kind or rung label (enums come from navigation); `node scripts/check-substrate.cjs` output has no line naming scripts/claim-checks.cjs
    - commands/room.md contains '## Subcommand: checks', '## Subcommand: claim' and '## Subcommand: check', names scripts/claim-checks.cjs at least 4 times, mentions AskUserQuestion inside the check section, contains the phrase 'before the [section] fallback' (case-insensitive match), and its argument-hint line mentions checks, claim and check
  </behavior>
  <action>
    Create tests/test-358-b1-cli.cjs in the repo test convention (check counter, final `N passed, M failed`, exit code). Hermetic: a fresh temp dir used as HOME, MINDRIAN_HOME and MINDRIAN_ROOMS_HOME for BOTH the test process and every child (set process.env before requiring navigation so in-process resolveByUser sees the same env); delete CLAUDE_ACTIVE_ROOM. Build temp rooms with lib/core/room-db.cjs openRoomDb/closeRoomDb, seed claims in-process with navigation.writeClaimNode through openRoomDbForCaller (close the handle before spawning). Spawn the CLI with `child_process.spawnSync(process.execPath, [path.join(REPO_ROOT, 'scripts', 'claim-checks.cjs'), ...args], { env, encoding: 'utf8' })` and assert on `status`, `stdout` and `stderr` (search both streams for refusal words). Read DB rows back in-process after each child exits. Pull every expected enum and label from navigation so the test never hard-codes a rung label. Implement every behavior above. Run: it MUST fail now (the script and the room.md sections do not exist). Commit: `git add tests/test-358-b1-cli.cjs` then `git commit --only -m "test(358-03): failing CLI checking-record test" -- tests/test-358-b1-cli.cjs`.
  </action>
  <verify>
    <automated>(node tests/test-358-b1-cli.cjs >/dev/null 2>&1; test $? -ne 0) && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-358-b1-cli.cjs exists and exits non-zero before Task 2 (RED proven; exit code in the SUMMARY)
    - `grep -c "spawnSync" tests/test-358-b1-cli.cjs` is at least 1 and `grep -c "resolveByUser" tests/test-358-b1-cli.cjs` is at least 1
    - `grep -c "No Data Room found" tests/test-358-b1-cli.cjs` is at least 1
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-358-b1-cli.cjs` is 0
  </acceptance_criteria>
  <done>A failing test pins the CLI contract and the /mos:room wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - scripts/claim-checks.cjs (one thin caller of the core functions)</name>
  <files>scripts/claim-checks.cjs</files>
  <read_first>
    - tests/test-358-b1-cli.cjs (Task 1 output)
    - scripts/mos-status.cjs (lines 55-95: lazy require of navigation.cjs as the only door to room.db, argv parsing style)
    - lib/core/navigation.cjs (the B1 re-export block from 358-01; detectActiveRoom at :533; openRoomDbForCaller / closeRoomDbForCaller at :564-565; resolveByUser at :597)
    - lib/core/navigation/verification.cjs (reasons and render helpers)
  </read_first>
  <behavior>
    - every script-behavior leg of tests/test-358-b1-cli.cjs passes (the room.md legs pass after Task 3)
  </behavior>
  <action>
    Create scripts/claim-checks.cjs: 'use strict'; header comment: Phase 358 B1 CLI for the checking record; one thin caller of lib/core/navigation.cjs, the same functions the claim_verify / claim_read MCP tools call (Canon Part 9, one write door); never requires node:sqlite; zero Brain and zero network (Canon Part 8); every option list comes from navigation so a rung swap is a one-constant edit; hyphens only. Require navigation via `path.join(__dirname, '..', 'lib', 'core', 'navigation.cjs')`. Implement:
    1. `parseArgs(argv)` (argv = process.argv.slice(2)): first token = subcommand; flags `--room`, `--json`, `--query`, `--limit`, `--claim`, `--against`, `--against-kind`, `--rung`, `--method`, `--result`, `--note-handle` take one value; `--resolves-dispute` is boolean; remaining positionals are joined with a space as `ref`. A switch-case router over the subcommand (no Commander or yargs).
    2. Room resolution: `--room` (path.resolve; must be an existing directory) else `navigation.detectActiveRoom()` roomDir else fail. When no room, or `<room>/.mindrian/room.db` is missing, print the 3-line error to stderr ('x No Data Room found' / '  Why: no --room given and no active room in the MindrianRooms registry' / '  Fix: /mos:new-project, or pass --room <dir>') and exit 1. Open with `navigation.openRoomDbForCaller(roomDir)` (null -> same error, exit 1) and ALWAYS close with `closeRoomDbForCaller` in a finally.
    3. `resolveClaimRef(db, ref)`: a ref starting with 'claim:' is an id (checked via readClaimVerification); otherwise `listClaimsForChecking(db, { query: ref, limit: 100 })`: 0 matches -> claim_not_found; more than 1 -> ambiguous_claim with the candidate list lines (renderClaimListLines); exactly 1 -> its claim_id.
    4. Subcommands. `rungs`: 'N. <label>' per VERIFICATION_RUNGS entry (`--json`: [{ rung, id, label }]). `options`: always JSON { rungs, against_kinds, methods, results } from the navigation constants. `portrait`: renderVerificationPortraitLines(readVerificationPortrait(db)) (`--json`: { ok: true, room_dir, portrait }). `list`: renderClaimListLines plus 'showing N of M claims' (`--json`: { ok: true, room_dir, total_matched, claims }); `--limit` default 20. `show <ref>`: resolve then renderClaimViewLines(readClaimVerification(...).claim) (`--json`: { ok: true, room_dir, claim }). `record`: require --claim, --against, --against-kind, --rung, --method and --result (any missing -> usage naming the missing flags, exit 2); resolve --claim like show; call `navigation.recordClaimVerification(db, { claim_id, against_id: --against, against_kind, method, result, rung: Number(--rung), checked_by: 'user', checked_by_id: navigation.resolveByUser(roomDir), note_handle (only when given), resolves_dispute: true only when --resolves-dispute was passed })`; on ok print 'Recorded a check on this claim. Its confirmation status did not change.' then the claim view lines (`--json`: { ok: true, room_dir, claim_id, review_status, claim }). `help` or no subcommand: usage, exit 0 for help and 2 for none/unknown. The usage text lists allowed values generated from navigation (rungs as 'N = label').
    5. Refusals print a 3-line block to stderr: 'x Check not recorded' (or 'x Claim not found' / 'x More than one claim matches'), '  Why: <reason>', '  Fix: <hint>' where the hint for invalid_rung / invalid_method / invalid_against_kind / invalid_result lists the allowed values from navigation, and for ambiguous_claim prints the candidate lines. `--json` refusals print { ok: false, reason, ... } to stdout. Exit codes: 0 ok, 1 refused or not found, 2 usage.
    6. Never write SQL, never call confirmNode or promoteNodeStatus, never shell out. Export { main, parseArgs } and run main only under `require.main === module`.
    Commit: `git add scripts/claim-checks.cjs` then `git commit --only -m "feat(358-03): claim-checks CLI for the checking record" -- scripts/claim-checks.cjs`.
  </action>
  <verify>
    <automated>node scripts/claim-checks.cjs rungs && node scripts/claim-checks.cjs options >/dev/null && node tests/test-358-b1-cli.cjs 2>&1 | grep -E "FAIL" | grep -v "room.md" | wc -l | grep -qx 0 && echo SCRIPT-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - Every non-room.md leg of `node tests/test-358-b1-cli.cjs` passes (only the commands/room.md legs may still fail until Task 3)
    - `grep -v '^\s*//' scripts/claim-checks.cjs | grep -cE "node:sqlite|DatabaseSync|better-sqlite3|brain|confirmNode|promoteNodeStatus|child_process"` is 0
    - `node scripts/check-substrate.cjs 2>&1 | grep -c "scripts/claim-checks.cjs"` is 0
    - `grep -c "A database or a document" scripts/claim-checks.cjs` is 0 and `grep -cE "'compare'|\"compare\"" scripts/claim-checks.cjs` is 0 (no duplicated enum or label)
    - `node scripts/claim-checks.cjs rungs | wc -l` equals `node -e "console.log(require('./lib/core/navigation.cjs').VERIFICATION_RUNGS.length)"`
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/claim-checks.cjs` is 0
  </acceptance_criteria>
  <done>The CLI records, reopens, lists and counts through the core functions only.</done>
</task>

<task type="auto">
  <name>Task 3: /mos:room checks | claim | check subcommands, mirror regeneration, CIRS gates</name>
  <files>commands/room.md, skills/room/SKILL.md</files>
  <read_first>
    - commands/room.md (whole file, 387 lines: frontmatter :1-35, firing block, "Parse the user's input" :64, subcommand sections, "## Voice Rules" :379)
    - skills/ui-system/SKILL.md (4-zone anatomy and the 3-line error format; skim only the zone and error sections)
    - scripts/build-skill-mirrors.cjs (how skills/room/SKILL.md is generated from commands/room.md)
    - scripts/claim-checks.cjs (Task 2 output: subcommands, flags, output lines)
    - docs/HITL-SHAPE-DECLARATION-CONTRACT.md (only to confirm F.1 still describes the command; do not edit it)
  </read_first>
  <action>
    Edit commands/room.md (per CONTEXT decisions AT1-AT4, Separation from approval, and the Disputed sticks ruling):
    1. Frontmatter: change ONLY the argument-hint value to `"[overview|<section>|checks|claim <id or words>|check <id or words>]"`. Do not touch description, help_jtbd, body_shape*, layer*, hitl_shape, hitl_why, allowed-tools (Bash and AskUserQuestion are already granted) or the connector block.
    2. In "## UI Format" add three bullets: checks, claim and check render inside the Room Card 4-zone anatomy (Shape C); the Content Body prints the script output verbatim.
    3. Right after the "Parse the user's input" sentence add: the subcommands `checks`, `claim` and `check` are matched BEFORE the [section] fallback, so a section can never shadow them.
    4. Before "## Subcommand: add" insert three sections. Each starts with Step 1 "Check for Room" exactly like overview (run `bash "${CLAUDE_PLUGIN_ROOT}/scripts/resolve-room"`, the same 3-line error on failure, STOP) and passes the resolved path as `--room "<room path>"`.
       - "## Subcommand: checks" (Trigger: `/mos:room checks`): run `node "${CLAUDE_PLUGIN_ROOT}/scripts/claim-checks.cjs" portrait --room "<room>"` and `node "${CLAUDE_PLUGIN_ROOT}/scripts/claim-checks.cjs" list --room "<room>" --limit 20`; render Header Panel, the portrait lines verbatim, the claim list verbatim, Action Footer offering `/mos:room claim <claim>` and `/mos:room check <claim>`. Rules: print all four state lines every time, unchecked included even at 0; never add a percentage, grade, ranking or single summary number; never say verified.
       - "## Subcommand: claim" (Trigger: `/mos:room claim <claim id or words>`): run `claim-checks.cjs show --room "<room>" <id or words>`; on 'More than one claim matches' fire an AskUserQuestion card with up to 4 of the candidates (fewer words, ask for more words when more than 4 match); on not found use the 3-line error with Fix `/mos:room checks`. Render the claim view verbatim; state in one plain sentence that Confirmation status and Checking record are separate: checked is not confirmed, and only a person confirms a claim, through the confirmation gate.
       - "## Subcommand: check" (Trigger: `/mos:room check <claim id or words>`): Step 2 find and show the claim as in claim. Step 3 run `claim-checks.cjs options` and gather the four fields: ask in plain text "What did you check it against?" (a document, a field note, a person, a source; the answer becomes --against); then fire AskUserQuestion cards for against kind, rung, method and result with option labels taken verbatim from the options JSON (rungs shown as 'Rung N - <label>', with rung 1 the lowest). AskUserQuestion allows at most 4 options per question: for any list longer than 4, show the first 3 values plus a 4th option 'More options', and if chosen ask a second question with the remaining values; never drop, merge or reword a value. Step 4 only when the claim view showed `Checking record: disputed` and the chosen result is supports or inconclusive, fire one card "Does this check answer the earlier contradiction?" with options "Yes, mark it answered" and "No, keep it disputed"; pass `--resolves-dispute` ONLY on an explicit Yes from the person, never on your own judgment. Step 5 run `node "${CLAUDE_PLUGIN_ROOT}/scripts/claim-checks.cjs" record --room "<room>" --claim "<claim id>" --against "<answer>" --against-kind <kind> --rung <N> --method <method> --result <result>` (plus `--resolves-dispute` when allowed); render a Shape E mini report with the script output verbatim, then the Action Footer offering `/mos:room checks`. Rules: recording a check never confirms, promotes or demotes the claim; never run a confirmation, gate_answer or any other command after recording; on a refusal show the script's 3-line block and re-ask only the field it names.
    5. In "## Voice Rules" add one bullet: in checks, claim and check, say "checked" and "confirmed" as different words; never call a checked claim verified or confirmed.
    6. Keep every line hyphen-only (no em-dashes) and keep all other sections byte-identical.
    Then regenerate the mirror: `node scripts/build-skill-mirrors.cjs` and confirm `git status --short skills/` shows ONLY skills/room/SKILL.md changed (if any other skill mirror changes, a peer's command edit is uncommitted: restore nothing, STOP and report). Run the gates: `node scripts/build-skill-mirrors.cjs --check`, `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs --check`, `node scripts/build-render-coverage.cjs --check`, `node scripts/check-shape-declaration.cjs --check` (advisory: record any NEW warning naming room.md and fix it if it is caused by this edit), `node scripts/check-help-coverage.cjs`, `node scripts/build-command-registry.cjs --check`, and `node scripts/check-cirs-declaration.cjs --check .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-03-PLAN.md`. If a gate reports drift caused by this edit (for example render coverage keyed on a new body_shape mention), fix the edit rather than regenerating data/ files (data/*.json are outside this plan).
    Commit (the pre-commit hook runs the connector, projection, command-registry and mirror checks): `git add commands/room.md skills/room/SKILL.md` then `git commit --only -m "feat(358-03): /mos:room checks, claim and check for the checking record" -- commands/room.md skills/room/SKILL.md`.
  </action>
  <verify>
    <automated>node tests/test-358-b1-cli.cjs && node scripts/build-skill-mirrors.cjs --check && node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check && node scripts/check-render-coverage.cjs --check && node scripts/check-cirs-declaration.cjs --check .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-03-PLAN.md && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-358-b1-cli.cjs` exits 0 (every leg, including the room.md legs)
    - `grep -c "## Subcommand: checks\|## Subcommand: claim\|## Subcommand: check" commands/room.md` is 3 and `grep -c "scripts/claim-checks.cjs" commands/room.md` is at least 4
    - `grep -c "resolves-dispute" commands/room.md` is at least 1 and the same section contains "explicit Yes"
    - `diff <(git show $PLAN_BASE:commands/room.md | awk '/^---$/{c++; next} c==1' | grep -v '^argument-hint:') <(awk '/^---$/{c++; next} c==1' commands/room.md | grep -v '^argument-hint:')` prints nothing (every frontmatter line except argument-hint is byte-unchanged, so the connector block and shape declarations are untouched)
    - `node scripts/build-skill-mirrors.cjs --check` exits 0 and `git diff --name-only $PLAN_BASE -- skills/` lists only skills/room/SKILL.md
    - `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs --check`, `node scripts/build-render-coverage.cjs --check`, `node scripts/check-help-coverage.cjs` and `node scripts/build-command-registry.cjs --check` all exit 0
    - `grep -ci "verified" commands/room.md` is no higher than it was at PLAN_BASE (`git show $PLAN_BASE:commands/room.md | grep -ci verified`)
    - `grep -c "$(printf '\xe2\x80\x94')" commands/room.md skills/room/SKILL.md` prints 0 for both
  </acceptance_criteria>
  <done>An officer on the CLI can check, reopen and count claims through /mos:room, every CIRS gate is green, and the command's declared shape and connector are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| officer / Larry -> argv | Free-text "against" answers and claim words arrive as CLI arguments |
| CLI script -> room.db | Only through navigation.cjs (openRoomDbForCaller, recordClaimVerification) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-12 | Tampering | argv values reaching SQL | mitigate | The script never builds SQL and never shells out; every value goes to navigation functions that use prepared statements with ? binds; enums validated by the library (closed sets) |
| T-358-13 | Elevation of privilege | CLI flow auto-resolving a dispute or confirming a claim | mitigate | room.md passes --resolves-dispute only on the person's explicit Yes; the library refuses it for checked_by system; the script never calls confirmNode, promoteNodeStatus or gate_answer (static test) |
| T-358-14 | Information disclosure | Canon Part 8 | mitigate | No Brain import in scripts/claim-checks.cjs (static test); output goes to the local terminal only |
| T-358-15 | Spoofing | checked_by_id | accept | Resolved from the room's own USER.md by resolveByUser, which coerces agent identities to 'navigator'; local only |
| T-358-16 | Repudiation | room.md edit silently changes the declared HITL shape or connector | mitigate | Acceptance criterion diffs the frontmatter; connector, projection, render, mirror and CIRS gates must stay green |
</threat_model>

<verification>
- `node tests/test-358-b1-cli.cjs`
- `node scripts/build-skill-mirrors.cjs --check && node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check && node scripts/check-render-coverage.cjs --check && node scripts/check-help-coverage.cjs`
- `node scripts/check-cirs-declaration.cjs --check .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-03-PLAN.md`
- `node tests/test-358-b1-core.cjs && node tests/test-358-b1-portrait.cjs` still pass
</verification>

<success_criteria>
- B1-02 (CLI half): an officer records a check through /mos:room check without a developer
- B1-04: /mos:room claim shows Confirmation status and Checking record side by side in a later session
- B1-05: /mos:room checks shows all four states, unchecked always, never a score
- B1-07: /mos:room stays born wired; mirror, connector, projection, render, help and CIRS gates green
</success_criteria>

<output>
Create `.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-03-SUMMARY.md` when done (stage it with `git add -f`).
</output>
