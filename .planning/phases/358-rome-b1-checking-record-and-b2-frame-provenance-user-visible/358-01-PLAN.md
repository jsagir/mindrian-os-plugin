---
phase: 358-rome-b1-checking-record-and-b2-frame-provenance-user-visible
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/verification.cjs
  - lib/core/navigation.cjs
  - tests/test-b1-verification.cjs
  - tests/test-358-b1-core.cjs
  - tests/test-358-b1-portrait.cjs
  - tests/test-358-b1-separation.cjs
autonomous: true
requirements: [B1-01, B1-04, B1-05, B1-06]
canon_parts: [8, 9, 12]
tags: [b1, checking-record, rung, portrait, rome, tdd]

must_haves:
  truths:
    - "VERIFICATION_RUNGS is ONE exported, frozen, ordered constant in lib/core/navigation/verification.cjs (rung 1 lowest) carrying the exact marker `TODO(358): replace with the paper author's verification hierarchy from The Orientation Problem`, and no rung label string appears in any other file under lib/, scripts/ or commands/ (per Rung decision)"
    - "recordClaimVerification stores `rung` (1-based integer) and `rung_label` (label snapshot) when a valid rung is given, refuses an out-of-range or non-integer rung with reason invalid_rung and writes nothing, and still accepts a legacy call with no rung"
    - "A stored record with no rung reads back as `rung unknown` and counts under records_by_rung.unknown; no migration touches existing rows (additive only)"
    - "Disputed sticks (navigator ruling 2026-09-23): once any record contradicts, the derived status is disputed; a later plain supports does not clear it; every record stays in records[]; only a record carrying resolves_dispute true with checked_by user clears it, and a later contradicts re-disputes"
    - "Optional checked_by_id (navigator ruling 2026-09-23) is stored on the record when supplied, local only, and never crosses to Theo or the Brain"
    - "readClaimVerification returns review_status and the checking record side by side under the keys `confirmation` and `checking_record`, and renderClaimViewLines prints `Confirmation status:` and `Checking record:` lines and never the word verified (Separation from approval decision)"
    - "readVerificationPortrait counts checked, disputed, inconclusive and unchecked across every claim in the room plus records_by_result and records_by_rung; renderPortraitLines always prints all four state lines including unchecked at 0; no portrait key and no rendered line matches score, percent, pct, ratio, grade, coverage or % (Counts are never a score decision, Canon Part 12)"
    - "Recording any result (supports, contradicts, inconclusive, with or without resolves_dispute) leaves nodes.review_status and nodes.confidence byte-identical for both a proposed and a confirmed claim (AT4)"
  artifacts:
    - path: "lib/core/navigation/verification.cjs"
      provides: "VERIFICATION_RUNGS, rungInfo, deriveCheckStatus, recordClaimVerification (rung, checked_by_id, resolves_dispute, owned transaction), readClaimVerification, listClaimsForChecking, readVerificationPortrait (records_by_rung), renderPortraitLines, renderClaimViewLines, renderClaimListLines"
      exports: ["VERIFICATION_RUNGS", "rungInfo", "deriveCheckStatus", "recordClaimVerification", "readClaimVerification", "listClaimsForChecking", "readVerificationPortrait", "renderPortraitLines", "renderClaimViewLines", "renderClaimListLines"]
      contains: "TODO(358): replace with the paper author's verification hierarchy from The Orientation Problem"
    - path: "lib/core/navigation.cjs"
      provides: "additive re-exports of the new B1 readers, rung constant and render helpers through the read chokepoint"
      contains: "VERIFICATION_RUNGS"
    - path: "tests/test-358-b1-core.cjs"
      provides: "B1-01 / B1-04 legs: rung constant, single source, invalid_rung, legacy rung unknown, checked_by_id, sticky disputed, resolution rules, claim view, claim list, transaction hygiene"
      min_lines: 150
    - path: "tests/test-358-b1-portrait.cjs"
      provides: "B1-05 legs: empty room, mixed room, all-checked room still shows unchecked 0, no-score guard, legacy latest-wins status re-derived"
      min_lines: 90
    - path: "tests/test-358-b1-separation.cjs"
      provides: "B1-06 legs: review_status and confidence byte-identical across every result for proposed and confirmed claims; record survives confirmNode; static no-promote / no-Brain guard"
      min_lines: 70
  key_links:
    - from: "lib/core/navigation/verification.cjs"
      to: "lib/core/node-insert.cjs insertNode"
      via: "writeClaimProperties, the only write in the file"
      pattern: "insertNode\\(db, row\\.id"
    - from: "lib/core/navigation.cjs"
      to: "lib/core/navigation/verification.cjs"
      via: "additive re-export block next to recordClaimVerification"
      pattern: "readClaimVerification: verification\\.readClaimVerification"
    - from: "readVerificationPortrait"
      to: "deriveCheckStatus"
      via: "status derived from records, never read from the stored status field"
      pattern: "deriveCheckStatus\\("
---

<objective>
Build the B1 checking-record contract in the core library: the ONE ordered rung constant, the new record fields (rung, rung_label, checked_by_id, resolves_dispute), the sticky "disputed" rule, the claim-view and claim-list readers, the room portrait with counts by rung, and the plain-text render helpers that the CLI (358-03) and the MCP tools (358-04) will both print. Tests first (RED), then the implementation (GREEN).

Purpose: AT1-AT4 all read or write through these functions. Putting the labels ("Confirmation status", "Checking record", "unchecked (no checking record yet)") in ONE place means the CLI and Desktop show the same words, and "checked" can never be read as "confirmed". Implements the CONTEXT decisions: Rung, Counts are never a score, Separation from approval, Canon constraints (Part 8, Part 9), and the navigator rulings "Disputed sticks" and "Who checked" (checked_by_id storage; the MCP/CLI layers stamp it from resolveByUser in 358-03 and 358-04).

Output: `lib/core/navigation/verification.cjs` (extended), additive re-exports in `lib/core/navigation.cjs`, three new phase tests, and the updated `tests/test-b1-verification.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-CONTEXT.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-RESEARCH.md
@lib/core/navigation/verification.cjs
@tests/test-b1-verification.cjs

Parallel-session rules (every task): peers execute Phases 354-357 in this same working tree. Record `PLAN_BASE=$(git rev-parse HEAD)` at start. Before editing any file run `git status --short -- <file>` and STOP if it shows a diff you did not make (never revert, stash, checkout or overwrite an unowned diff). Stage explicit paths only (`git add <path>`; `git add -f` for anything under `.planning/`) and commit with `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git add .`, `git commit -a`, `git stash`, or `--no-verify`. Never touch: docs/reviews/*, evals/plurai/*, scripts/eval-icm-writers.cjs, scripts/jev-devtime-client.cjs, tests/test-353-* (run only), .planning/phases/356-*, data/jev-policies/, .planning/STATE.md, lib/mcp/tools/gate.cjs, lib/mcp/tools/views.cjs, lib/mcp/tool-router.cjs, lib/mcp/register-core-tools.cjs, lib/mcp/tools/status.cjs, lib/core/strategy/rung-vocabulary.cjs, lib/core/part8-egress-guard.cjs, lib/core/write-lock.cjs, CLAUDE.md. Never name anything in B1 a "stamp" (Phase 355 owns lib/core/verification-stamp.cjs). Never import or edit lib/core/strategy/rung-vocabulary.cjs: that is the PROBLEM-TYPE rung vocabulary, a different thing from the verification rung. No em-dashes in any file or commit message (hyphens only).

Interfaces this plan creates (358-03 and 358-04 build against exactly these; keep the names and shapes):

- `VERIFICATION_RUNGS`: frozen array of frozen `{ id, label }`; rung number = index + 1; rung 1 is lowest. Provisional content (paper author's published hierarchy, what-ai-cannot-know.vercel.app Part V practice 3): 1 `own_intuition` "Your own intuition"; 2 `model_counter_argument` "A counter-argument generated by the model"; 3 `database_or_document` "A database or a document"; 4 `primary_source` "A primary source"; 5 `dissenting_person` "A person who disagrees with you".
- `rungInfo(n)` returns `{ rung: n, id, label }` for an integer 1..length, else `null`.
- `deriveCheckStatus(records)` returns one of `unchecked | checked | disputed | inconclusive`.
- `recordClaimVerification(db, params)`; params = `{ claim_id, against_id, against_kind, method, result, checked_by?, checked_at?, note_handle?, rung?, checked_by_id?, resolves_dispute? }`; returns `{ ok: true, claim_id, review_status, verification: { status, records } }` or `{ ok: false, reason }` where the new reasons are `invalid_rung`, `invalid_checked_by_id`, `invalid_resolves_dispute`, `resolution_requires_person`, `invalid_resolution`, `no_dispute_to_resolve`.
- `readClaimVerification(db, claimId)` returns `{ ok: true, claim: { claim_id, text, knowledge_type, confirmation: { review_status, note }, checking_record: { status, status_note, records_total, records: [ { ...stored record fields, rung_display } ] } } }` or `{ ok: false, reason: 'claim_not_found' }`.
- `listClaimsForChecking(db, { query?, limit? })` returns `{ ok: true, total_matched, claims: [ { claim_id, text_preview, review_status, checking_status, records_total } ] }`, newest first.
- `readVerificationPortrait(db)` returns the shipped keys plus `records_by_rung` (keys `'1'`..String(length) always present, any other stored integer rung as its own key, and `unknown`).
- `renderPortraitLines(portrait)`, `renderClaimViewLines(claim)`, `renderClaimListLines(claims)` each return an array of strings.
- navigation.cjs re-export names: `VERIFICATION_RUNGS`, `verificationRungInfo`, `deriveVerificationStatus`, `readClaimVerification`, `listClaimsForChecking`, `renderVerificationPortraitLines`, `renderClaimViewLines`, `renderClaimListLines` (plus the existing `recordClaimVerification`, `readVerificationPortrait`, `VERIFICATION_*` sets, unchanged).
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `lib/core/navigation/verification.cjs` (rung constant, record fields, sticky disputed, claim view + list readers, portrait by rung, render helpers), `lib/core/navigation.cjs` re-exports, `tests/test-358-b1-core.cjs`, `tests/test-358-b1-portrait.cjs`, `tests/test-358-b1-separation.cjs`, updated `tests/test-b1-verification.cjs` | 01 (this plan) |
| `lib/core/navigation/typed-claim.cjs` carry-forward + PROTECTED_CLAIM_KEYS, `tests/test-358-b1-refile.cjs`, `lib/mcp/surface-detect.cjs` claude-ai tier0 host, `tests/test-234-host-tier.cjs` additions, `tests/run-all-358.sh` | 02 |
| `scripts/claim-checks.cjs`, `/mos:room checks`, `/mos:room claim`, `/mos:room check` in `commands/room.md`, regenerated `skills/room/SKILL.md`, `tests/test-358-b1-cli.cjs` | 03 |
| `claim_verify` (rung required, checked_by_id, resolves_dispute, description >= 120 chars), new `claim_read` MCP tool, `tests/test-358-b1-surfaces.cjs`, `tests/helpers/b1-358-child.cjs`, `tests/test-358-b1-persistence.cjs` | 04 |
| Regenerated `data/mcp-tool-connectors.json` + `data/connector-registry.json`, `tests/test-270-tool-schema-budget.cjs` AFTER re-baseline, `tests/fixtures/tool-honesty/276-dispositions.json` re-freeze | 05 |
| `docs/2026-10-06-ROME-B1-GO-NO-GO.md` (operator runbook) + human checkpoint | 06 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - core, portrait and separation tests for the checking record</name>
  <files>tests/test-358-b1-core.cjs, tests/test-358-b1-portrait.cjs, tests/test-358-b1-separation.cjs</files>
  <read_first>
    - lib/core/navigation/verification.cjs (the whole file, 157 lines: enums :11-19, recordClaimVerification :67-103, readVerificationPortrait :109-143)
    - tests/test-b1-verification.cjs (42 lines; the shipped assertion style)
    - tests/test-276-claim-write-primitive.cjs (lines 100-130: MINDRIAN_ROOMS_HOME hermetic setup, makeTempRoom via openRoomDb/closeRoomDb)
    - lib/core/room-db.cjs (module.exports at :348: openRoomDb, closeRoomDb)
    - lib/core/navigation/confirm-node.cjs (lines 34-44 confirmNode(db, id, byUser, reason); tests may call it, B1 production code never does)
    - .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-RESEARCH.md (Pattern 1, Pattern 3, Pitfalls 5 and 6, Validation Architecture)
  </read_first>
  <behavior>
    - core: VERIFICATION_RUNGS is a frozen array (Object.isFrozen true), every entry frozen with a non-empty string id and label, ids unique, and navigation.VERIFICATION_RUNGS is the same array object
    - core: rungInfo(1).label equals VERIFICATION_RUNGS[0].label; rungInfo(0), rungInfo(length + 1), rungInfo(2.5), rungInfo('3') all return null
    - core: verification.cjs source contains the exact TODO(358) marker string; every rung label occurs in exactly one file across lib/, scripts/ and commands/ (walk recursively, .cjs .js .md, skip node_modules), and that file is lib/core/navigation/verification.cjs
    - core: a record with rung 3 stores rung 3 and rung_label equal to rungInfo(3).label; filtering stored records with `r.rung >= 3` returns it (rung three or above is expressible)
    - core: rung 0, length + 1, 2.5 and '3' each return reason invalid_rung and leave the claim's records length unchanged
    - core: a record with no rung is accepted; readClaimVerification shows its rung_display as `rung unknown`
    - core: checked_by_id 'officer-7' is stored; omitted leaves no checked_by_id key; a 129-character string or a number returns invalid_checked_by_id
    - core: contradicts then supports gives status disputed with 2 records and the contradicts record still at index 0
    - core: resolves_dispute true with checked_by system returns resolution_requires_person; with result contradicts returns invalid_resolution; on a claim that is not disputed returns no_dispute_to_resolve; resolves_dispute 'yes' (non-boolean) returns invalid_resolves_dispute; a valid resolution (user, supports) on a disputed claim gives status checked with 3 records and resolves_dispute true on the last; a further contradicts gives disputed again
    - core: supports then inconclusive gives inconclusive; inconclusive then supports gives checked; deriveCheckStatus([]) is unchecked; deriveCheckStatus ignores null, strings and objects with an unknown result
    - core: the recordClaimVerification success return carries review_status 'proposed'
    - core: readClaimVerification returns claim.claim_id, claim.text, claim.confirmation.review_status 'proposed', claim.confirmation.note containing 'Only a person can confirm', claim.checking_record.status and records; an unknown id and a non-claim node id both return reason claim_not_found
    - core: renderClaimViewLines output has a line starting 'Confirmation status: proposed' and a line starting 'Checking record: ', and contains 'checked against: ', 'rung 3 (', 'method: compare', 'result: contradicts', 'by: user', 'when: '; after navigation.confirmNode(db, id, 'navigator', 'test') it shows 'Confirmation status: confirmed'; no line matches /\bverified\b/i
    - core: listClaimsForChecking over three claims: query 'BRIDGE' returns exactly the bridge claim with claim_id, text_preview, review_status, checking_status and records_total; limit 1 returns 1 claim with total_matched 3; limit 0 is treated as 1 and limit 500 as 100; every text_preview is at most 120 characters
    - core: after a row-dependent refusal (no_dispute_to_resolve) db.isTransaction is false; when the caller has run db.exec('BEGIN') first, recordClaimVerification succeeds and db.isTransaction is still true until the caller commits
    - portrait: empty room gives all counts 0 and records_by_rung with keys '1'..String(length) and 'unknown', all 0; renderPortraitLines prints 'checked: 0', 'disputed: 0', 'inconclusive: 0', 'unchecked (no checking record yet): 0'
    - portrait: four claims (one supports at rung 4; one contradicts at rung 3 then supports at rung 5; one inconclusive with no rung; one with no record) gives checked 1, disputed 1, inconclusive 1, unchecked 1, claims_total 4, records_total 4, records_by_result supports 2 contradicts 1 inconclusive 1, records_by_rung '3' 1, '4' 1, '5' 1, unknown 1, all other rung keys 0
    - portrait: a room where every claim is checked still renders the unchecked line with 0
    - portrait: no key anywhere in the portrait object (deep walk) and no rendered line matches /score|percent|pct|ratio|grade|coverage|%/i
    - portrait: a legacy claim whose stored verification is { status: 'checked', records: [contradicts, supports] } (written with a raw test-only UPDATE of nodes.properties) is counted as disputed; a non-claim node in the room is not counted
    - separation: for a proposed claim and for a confirmed claim (confirmNode), recording supports, contradicts, inconclusive and a valid resolution leaves review_status and confidence byte-identical to the values read before the first record
    - separation: confirmNode called after two records leaves props.verification.records length 2
    - separation: comment-stripped source of lib/core/navigation/verification.cjs, and of lib/mcp/tools/claim-verify.cjs and scripts/claim-checks.cjs when those files exist, contains none of: confirmNode(, promoteNodeStatus(, brain-client, brainClient, brain_; claim-verify.cjs and claim-checks.cjs also contain no node:sqlite and no DatabaseSync
  </behavior>
  <action>
    Create the three test files in the repo convention: 'use strict', node:assert/strict, a small `check(label, fn)` pass/fail counter that prints `ok - <label>` or `FAIL - <label>` plus detail, a final `N passed, M failed` line, and `process.exit(failed === 0 ? 0 : 1)`. Hermetic setup at the top of each file: set `process.env.MINDRIAN_ROOMS_HOME` to a fresh `fs.mkdtempSync` dir and delete `process.env.CLAUDE_ACTIVE_ROOM`. Each scenario builds a fresh temp room: mkdtemp, `openRoomDb(dir)` then `closeRoomDb(handle)` from lib/core/room-db.cjs (this creates the migrated schema), then `navigation.openRoomDbForCaller(dir)` for the handle, closed in a `finally` with `navigation.closeRoomDbForCaller(db)`. Seed claims with `navigation.writeClaimNode(db, { knowledge_type: 'fact', text, sessionId: 's358', sourceSegment: '<unique>' })`. Read rows back with `db.prepare('SELECT review_status, confidence, properties FROM nodes WHERE id = ?').get(id)`. Use the claim text 'The bridge at grid 42 is passable.' for the bridge claim. For the single-source walk use fs.readdirSync recursively from the repo root dirs lib, scripts and commands. For the comment strip use `src.replace(/\/\*[\s\S]*?\*\//g, '').split('\n').filter(l => !/^\s*\/\//.test(l)).join('\n')`. Build every expected rung key list from `VERIFICATION_RUNGS.length` so a later one-constant rung swap only needs value edits, never structural test edits. Tests may call navigation.confirmNode and may run a raw UPDATE on nodes (tests are not scanned by check-substrate); production code never does either. Run all three: they MUST fail now (the new exports do not exist yet). Commit: `git add tests/test-358-b1-core.cjs tests/test-358-b1-portrait.cjs tests/test-358-b1-separation.cjs` then `git commit --only -m "test(358-01): add failing B1 core, portrait and separation tests" -- tests/test-358-b1-core.cjs tests/test-358-b1-portrait.cjs tests/test-358-b1-separation.cjs`.
  </action>
  <verify>
    <automated>for t in core portrait separation; do node tests/test-358-b1-$t.cjs >/dev/null 2>&1; echo "$t exit=$?"; done | grep -c "exit=0" | grep -qx 0 && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist and each exits non-zero before Task 2 (RED proven; the SUMMARY records the three failing exit codes)
    - `grep -c "TODO(358): replace with the paper author's verification hierarchy from The Orientation Problem" tests/test-358-b1-core.cjs` is at least 1
    - `grep -c "unchecked (no checking record yet)" tests/test-358-b1-portrait.cjs` is at least 1
    - `grep -cE "score|percent|pct|ratio|grade|coverage" tests/test-358-b1-portrait.cjs` is at least 1 (the no-score guard exists)
    - `grep -c "confirmNode" tests/test-358-b1-separation.cjs` is at least 1
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-358-b1-core.cjs tests/test-358-b1-portrait.cjs tests/test-358-b1-separation.cjs` prints 0 for every file
  </acceptance_criteria>
  <done>Three RED test files encode every B1-01, B1-04, B1-05 and B1-06 behavior above and fail against the shipped verification.cjs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - rung constant, record fields, sticky disputed, readers, portrait by rung, render helpers, re-exports</name>
  <files>lib/core/navigation/verification.cjs, lib/core/navigation.cjs, tests/test-b1-verification.cjs</files>
  <read_first>
    - lib/core/navigation/verification.cjs (whole file)
    - lib/core/navigation.cjs (lines 50-60 the require block; lines 480-495 the existing B1 re-export block, the ONLY region this task edits)
    - lib/core/navigation/ranker-weights.cjs (lines 105-130: the transaction ownership idiom `ownsTransaction = db.isTransaction !== true`)
    - lib/core/node-insert.cjs (lines 225-252: DO UPDATE sets type, properties, last_seen_at and never review_status)
    - tests/test-358-b1-core.cjs, tests/test-358-b1-portrait.cjs, tests/test-358-b1-separation.cjs (Task 1 output)
  </read_first>
  <behavior>
    - All three Task 1 tests pass
    - tests/test-b1-verification.cjs passes with its portrait deepEqual extended by records_by_rung (built from VERIFICATION_RUNGS.length, its one no-rung record under unknown)
  </behavior>
  <action>
    In lib/core/navigation/verification.cjs (per the Rung decision, the Disputed sticks and Who checked rulings, and Canon Part 9: the only write stays insertNode through writeClaimProperties):
    1. Directly under the existing enums add the constant with these three comment lines above it: `// TODO(358): replace with the paper author's verification hierarchy from The Orientation Problem.` then `// Provisional source: what-ai-cannot-know.vercel.app Part V practice 3. Rung 1 is lowest; rung number = index + 1.` then `// NOT the problem-type rung vocabulary (lib/core/strategy/rung-vocabulary.cjs); never mix or import the two.` The constant is `VERIFICATION_RUNGS = Object.freeze([...])` of `Object.freeze({ id, label })` entries with exactly the five ids and labels listed in the context Interfaces block. This file is the ONLY place the labels are written; every other surface derives from it.
    2. Add `rungInfo(n)` (integer 1..length returns `{ rung: n, id, label }`, anything else null), `normalizeRecords(records)` (keeps plain objects whose result is in RESULTS), and `deriveCheckStatus(records)`: normalize; none means `unchecked`; find `lastResolve` = the last index of a record with `resolves_dispute === true` and `checked_by === 'user'` (-1 when none); if any normalized record at an index greater than lastResolve has result contradicts, return `disputed`; otherwise map the LAST normalized record's result: inconclusive gives `inconclusive`, supports gives `checked`.
    3. Add two frozen note maps. STATUS_NOTES: unchecked 'no checking record yet'; checked 'checked is not the same as confirmed'; disputed 'at least one check contradicts this claim; it stays disputed until a person marks the contradiction answered'; inconclusive 'the latest check was inconclusive'. CONFIRMATION_NOTES: proposed 'Proposed. Only a person can confirm this claim. A checking record never confirms it.'; confirmed 'Confirmed by a person.'; any other status uses 'Only a person changes this status. A checking record never does.'
    4. Extend recordClaimVerification. Keep the existing validation, then, still before touching the db: if `params.rung` is not undefined and not null, `rungInfo(params.rung)` must be non-null, else return `{ ok: false, reason: 'invalid_rung' }`; if `params.checked_by_id` is not undefined, not null and not '', it must be a string of length 1..128, else `invalid_checked_by_id`; if `params.resolves_dispute` is not undefined it must be a boolean, else `invalid_resolves_dispute`; when it is true, checkedBy must be 'user' (else `resolution_requires_person`) and result must not be contradicts (else `invalid_resolution`). Then wrap the read-append-write in the ownership idiom: `const owns = db.isTransaction !== true; if (owns) db.exec('BEGIN IMMEDIATE');` with COMMIT on success and ROLLBACK on every refusal and on any throw (never leave a transaction open; when the caller owns the transaction, never COMMIT or ROLLBACK it). Inside: read the row (claim_not_found as today); copy the existing records array as-is (every stored entry stays, including malformed ones); if resolves_dispute is true and deriveCheckStatus(existing records) is not `disputed`, refuse `no_dispute_to_resolve`; build the record exactly as today, then add `rung` and `rung_label` (the label snapshot from rungInfo) when a rung was given, `checked_by_id` when given, and `resolves_dispute: true` only when true; push; set `props.verification = { status: deriveCheckStatus(records), records }`; write through writeClaimProperties (unchanged: insertNode with source_path 'verification:'+id, created_by 'system', the existing epistemic_type fallback; never pass review_status or confidence). Return `{ ok: true, claim_id, review_status: row.review_status, verification }`. The latest-result-wins line at the old :97-99 is REMOVED (replaced by deriveCheckStatus).
    5. Add `readClaimVerification(db, claimId)`: readNode; missing row or type other than 'claim' gives `{ ok: false, reason: 'claim_not_found' }`; parse props; `records` for display = the stored plain-object records, each shallow-copied with `rung_display` = `'rung ' + r.rung + ' (' + label + ')'` when r.rung is an integer >= 1 (label = r.rung_label when a non-empty string, else rungInfo(r.rung).label, else 'label not on file'), otherwise `'rung unknown'`; status = deriveCheckStatus(stored records). Return the shape in the Interfaces block with `text: props.text || ''`, `knowledge_type: props.knowledge_type || ''`, `confirmation: { review_status: row.review_status, note: CONFIRMATION_NOTES[...] }`, `checking_record: { status, status_note: STATUS_NOTES[status], records_total: records.length, records }`.
    6. Add `listClaimsForChecking(db, opts)`: query = trimmed lower-cased string capped at 200 characters ('' when absent); limit = integer, default 20, clamped to 1..100 (0 and negatives become 1); SELECT id, properties, review_status FROM nodes WHERE type = 'claim' ORDER BY rowid DESC (newest first); keep rows whose lower-cased text or id includes the query; total_matched = kept count; slice to limit; map to `{ claim_id, text_preview, review_status, checking_status: deriveCheckStatus(records), records_total }` where text_preview is the text when 120 characters or fewer, else the first 117 characters plus '...'. Wrap the SELECT in try/catch returning `{ ok: true, total_matched: 0, claims: [] }` on error.
    7. Change readVerificationPortrait: status now comes from `deriveCheckStatus(verification.records)` (NOT the stored status field, so legacy latest-wins data is re-read under the sticky rule); add `records_by_rung` initialised with keys '1'..String(VERIFICATION_RUNGS.length) at 0 plus `unknown: 0`; for each normalized record, an integer rung >= 1 increments its String(rung) key (creating it if beyond the list), anything else increments unknown. Keep every shipped key and its meaning; claims_unchecked stays "claims with no checking record".
    8. Add the render helpers (plain ASCII, hyphens only, the word verified never appears): `renderPortraitLines(p)` returns, in order: 'Checking record across this room (counts only)', '  checked: N', '  disputed: N', '  inconclusive: N', '  unchecked (no checking record yet): N', '  claims in this room: N', '  checks recorded: N (supports N, contradicts N, inconclusive N)', '  checks by rung: rung 1: N, rung 2: N, ... , rung unknown: N' (numeric key order, unknown last), 'A count is not a verdict. Only a person confirms a claim.'. All four state lines are ALWAYS printed, including zeros. `renderClaimViewLines(claim)` returns: 'Claim: <text>', 'Claim id: <claim_id>', 'Confirmation status: <review_status> - <note>', 'Checking record: <status> - <status_note>', then per record i: '  <i+1>. checked against: <against_id> (<against_kind>)', '     <rung_display> | method: <method> | result: <result>', '     by: <checked_by>' plus ' (<checked_by_id>)' when present plus ' | when: <checked_at>', '     note: <note_handle>' when present, '     a person marked the earlier contradiction answered' when resolves_dispute is true; with no records, '  (no checks recorded yet)'. `renderClaimListLines(claims)` returns one line per claim: '  - <claim_id> | checking: <checking_status> | confirmation: <review_status> | <text_preview>', or '  (no claims filed in this room yet)' for an empty list.
    9. Export the new names listed in the must_haves artifacts entry alongside the existing exports.
    In lib/core/navigation.cjs, edit ONLY the existing B1 re-export block (next to `recordClaimVerification: verification.recordClaimVerification`): add `VERIFICATION_RUNGS: verification.VERIFICATION_RUNGS`, `verificationRungInfo: verification.rungInfo`, `deriveVerificationStatus: verification.deriveCheckStatus`, `readClaimVerification: verification.readClaimVerification`, `listClaimsForChecking: verification.listClaimsForChecking`, `renderVerificationPortraitLines: verification.renderPortraitLines`, `renderClaimViewLines: verification.renderClaimViewLines`, `renderClaimListLines: verification.renderClaimListLines`. First `grep -n "<name>" lib/core/navigation.cjs` for each new key and STOP if any name already exists elsewhere in the export map. Extend the block comment by one line: the claim view and portrait are local reads; nothing here reaches the Brain (Canon Part 8).
    In tests/test-b1-verification.cjs, extend only the portrait deepEqual with `records_by_rung` built from `navigation.VERIFICATION_RUNGS.length` (all rung keys 0, unknown 1, because its record has no rung). Everything else in that file stays.
    Commit: `git add lib/core/navigation/verification.cjs lib/core/navigation.cjs tests/test-b1-verification.cjs` then `git commit --only -m "feat(358-01): B1 rung constant, sticky disputed, claim view and portrait readers" -- lib/core/navigation/verification.cjs lib/core/navigation.cjs tests/test-b1-verification.cjs`.
  </action>
  <verify>
    <automated>node tests/test-358-b1-core.cjs && node tests/test-358-b1-portrait.cjs && node tests/test-358-b1-separation.cjs && node tests/test-b1-verification.cjs && node tests/test-276-claim-write-primitive.cjs >/dev/null && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-358-b1-core.cjs`, `node tests/test-358-b1-portrait.cjs`, `node tests/test-358-b1-separation.cjs` and `node tests/test-b1-verification.cjs` each exit 0
    - `grep -c "TODO(358): replace with the paper author's verification hierarchy from The Orientation Problem" lib/core/navigation/verification.cjs` is 1
    - `grep -rl "A counter-argument generated by the model" lib scripts commands` prints exactly `lib/core/navigation/verification.cjs`
    - `grep -v '^\s*//' lib/core/navigation/verification.cjs | grep -cE "confirmNode|promoteNodeStatus|brain"` is 0
    - `grep -v '^\s*//' lib/core/navigation/verification.cjs | grep -c "BEGIN IMMEDIATE"` is 1
    - `grep -v '^\s*//' lib/core/navigation/verification.cjs | grep -ci "verified"` is 0
    - `grep -c "readClaimVerification: verification.readClaimVerification" lib/core/navigation.cjs` is 1 and `git diff $PLAN_BASE -- lib/core/navigation.cjs | grep -c '^-[^-]'` is 0 (additive only)
    - `node tests/test-276-claim-write-primitive.cjs` and `node tests/test-348-validity-window.cjs` still exit 0
    - `grep -c "$(printf '\xe2\x80\x94')" lib/core/navigation/verification.cjs lib/core/navigation.cjs tests/test-b1-verification.cjs` prints 0 for every file
  </acceptance_criteria>
  <done>The core library carries the full B1 contract; the CLI and MCP plans can call it without touching the database themselves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP / CLI caller -> core library | Parameters for a checking record arrive from an agent turn or argv; untrusted until validated |
| core library -> room.db | The only write is insertNode through writeClaimProperties |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-01 | Tampering | recordClaimVerification params | mitigate | Closed-enum checks for against_kind, method, result, checked_by; integer range check for rung (invalid_rung); length and type checks for checked_by_id and resolves_dispute; all before BEGIN, so a refusal writes nothing |
| T-358-02 | Elevation of privilege | recording a check promotes a claim | mitigate | writeClaimProperties never passes review_status or confidence; insertNode DO UPDATE excludes them; test-358-b1-separation proves byte-identical review_status and confidence; static guard forbids confirmNode and promoteNodeStatus in B1 files |
| T-358-03 | Repudiation | a later supports hides an earlier contradiction | mitigate | deriveCheckStatus keeps disputed until a user-marked resolution; records are append-only and all stay visible; portrait re-derives legacy latest-wins data |
| T-358-04 | Tampering | lost update when two officers check at once (Cowork) | mitigate | BEGIN IMMEDIATE ownership idiom around read-append-write; never nests a caller transaction |
| T-358-05 | Information disclosure | Canon Part 8 egress | mitigate | No Brain import in verification.cjs (static guard); rung, method, result, against_kind stay local enums; checked_by_id is local only |
| T-358-06 | Spoofing | an agent marks a dispute resolved | accept | resolves_dispute requires checked_by user and is refused for system; residual risk equals the existing checked_by trust level; the CLI and MCP layers ask the person first (358-03, 358-04) |
</threat_model>

<verification>
- `node tests/test-358-b1-core.cjs && node tests/test-358-b1-portrait.cjs && node tests/test-358-b1-separation.cjs && node tests/test-b1-verification.cjs`
- Claim-writer regressions still green: `node tests/test-276-claim-write-primitive.cjs && node tests/test-348-validity-window.cjs && node tests/test-353-filing-gate.cjs`
- `git status --short` shows no modified file outside this plan's files_modified that this plan touched
</verification>

<success_criteria>
- B1-01: one ordered rung constant with the TODO marker, rung stored with a label snapshot, legacy records read as rung unknown
- B1-04: claim view shows confirmation and checking record side by side with labels that never say verified
- B1-05: portrait counts all four states plus by result and by rung, always renders unchecked, never a score
- B1-06: recording never changes review_status or confidence, proved for proposed and confirmed claims
</success_criteria>

<output>
Create `.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-01-SUMMARY.md` when done (stage it with `git add -f`).
</output>
