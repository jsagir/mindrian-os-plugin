---
phase: 358-rome-b1-checking-record-and-b2-frame-provenance-user-visible
plan: 10
type: execute
wave: 4
depends_on: ["358-09"]
files_modified:
  - commands/room.md
  - skills/room/SKILL.md
  - data/command-registry.json
  - data/mcp-tool-connectors.json
  - data/connector-registry.json
  - data/connector-coverage-ledger.json
  - data/harness-manifest.json
  - data/brain-orchestration-projection.json
  - tests/test-270-tool-schema-budget.cjs
  - tests/fixtures/tool-honesty/276-dispositions.json
autonomous: true
requirements: [B2-10]
canon_parts: [8, 11]
tags: [b2, cirs, born-wired, registry, theo, baseline, rome]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: ["commands/room.md", "skills/room/SKILL.md"]
  surfaces_removed: []
  spine_consumed: ["scripts/build-connector-registry.cjs", "scripts/build-command-registry.cjs", "scripts/build-harness-manifest.cjs", "scripts/build-skill-mirrors.cjs", "scripts/build-orchestration-projection.cjs"]
  gate_impact: "Closes the born-wired loop for B2 in one commit on a clean tree: data/mcp-tool-connectors.json, data/connector-registry.json and data/connector-coverage-ledger.json gain exactly the question_read and question_set records and the question.cjs ledger row; data/command-registry.json gains the new /mos:room teaching line (the only frontmatter key changed here), which is what Theo's command layer maps; data/harness-manifest.json is regenerated because it digests the connector and command registries; data/brain-orchestration-projection.json only if --check reports drift. The /mos:room connector block, hitl_shape F.1 and layer stay byte-unchanged."
  explanation: "Canon Part 11: every invocable surface is born wired and the registries are generated, never hand-written. Doing every shared regenerate LAST and together (the 358-05 lesson) keeps peers 355-05, 357-10 and 359 regenerating the same files from churning against B2 mid-phase. The teaching line is the navigator requirement that Theo is aware: after the release that contains this commit and the Theo resync, command_neighborhood for /mos:room describes the question subcommands."

must_haves:
  truths:
    - "data/mcp-tool-connectors.json, data/connector-registry.json and data/connector-coverage-ledger.json are regenerated fresh by scripts/build-connector-registry.cjs AFTER every B2 surface landed, carry mcp:question_read (hitl_shape none) and mcp:question_set (hitl_shape F.1), pass --check, and are committed with --only (B2-10, Part 11)"
    - "commands/room.md teaching now names the question subcommands, data/command-registry.json carries it, and the SUMMARY records the sha256 of the committed data/command-registry.json as the registryHash Theo should report for /mos:room after the release that contains this commit and the theo-resync (navigator requirement 2026-09-23 item 2)"
    - "data/harness-manifest.json is regenerated and passes --check; every non-B2 change in its diff traces to an already-committed source and is named in the commit message"
    - "tests/test-270-tool-schema-budget.cjs is green with AFTER re-baselined to the measured numbers (plan '358-10', DRIFT_TOLERANCE_PCT unchanged at 10) and tests/test-276-tool-honesty-findings-closed.cjs is green with frozen_sweep re-frozen to the live scan and refrozen_at naming 358-10"
    - "`bash tests/run-all-358.sh` exits 0 (not 77) with every B1 and B2 leg PASSED; tests/test-358-b2-routing.cjs prints 'routing text: 3 of 3 checked' and tests/test-358-b2-part8.cjs prints 'part8 static: 4 of 4 B2 files scanned'"
  artifacts:
    - path: "data/mcp-tool-connectors.json"
      provides: "regenerated MCP tool connector list including question_read and question_set"
      contains: "mcp:question_set"
    - path: "data/connector-registry.json"
      provides: "regenerated connector registry including both B2 tools"
      contains: "mcp:question_read"
    - path: "data/command-registry.json"
      provides: "/mos:room teaching line naming the question subcommands (Theo command layer source)"
      contains: "/mos:room question"
    - path: "tests/test-270-tool-schema-budget.cjs"
      provides: "AFTER re-baselined by plan 358-10 with measured numbers"
      contains: "plan: '358-10'"
    - path: "tests/fixtures/tool-honesty/276-dispositions.json"
      provides: "frozen_sweep re-frozen; refrozen_at names plan 358-10"
      contains: "358-10"
  key_links:
    - from: "lib/mcp/tools/question.cjs connectors"
      to: "data/mcp-tool-connectors.json"
      via: "node scripts/build-connector-registry.cjs"
      pattern: "mcp:question_set"
    - from: "commands/room.md teaching"
      to: "data/command-registry.json -> Theo command_neighborhood('/mos:room')"
      via: "node scripts/build-command-registry.cjs, then release.sh Step 5.6 repository_dispatch theo-resync"
      pattern: "/mos:room question"
---

<objective>
Close B2's born-wired and baseline work LAST, after every B2 surface has landed, in the same discipline as 358-05: one commit that sets the /mos:room teaching line Theo reads and regenerates every shared registry (skill mirror, command registry, connector registry and ledger, MCP tool connectors, harness manifest, projection only if drifted) on a clean tree; then a second commit that re-baselines the tool-schema budget and re-freezes the tool-honesty sweep with measured numbers; then prove the whole phase green.

Purpose: B2-10 (born wired and green) and the navigator requirement (2026-09-23, item 2) that the new commands land in Theo's command layer: Theo maps data/command-registry.json, and the /mos:room entry's teaching field is what command_neighborhood('/mos:room') returns. It goes live only after the release that contains this commit and the Theo resync fired by scripts/release.sh Step 5.6 (docs/THEO-NOTIFY-CONTRACT.md); 358-11 verifies it on the demo date.

Output: one regenerate commit (room.md teaching, mirror, six data files, projection if needed), one baseline commit (test-270, the 276 ledger), a green runner.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-05-PLAN.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-05-SUMMARY.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-09-SUMMARY.md
@docs/THEO-NOTIFY-CONTRACT.md

Facts verified at planning time (HEAD eb393d9a0):
- data/command-registry.json is built by scripts/build-command-registry.cjs from command frontmatter: command, kind, inputs, serves_jtbd, teaching (1-2 sentences, 50-300 chars, no em-dashes), and derived jtbd fields. It does NOT carry description, argument-hint or hitl_why (358-03 and 358-08 changed those without moving it). The /mos:room entry today: teaching "When you need to view or launch the active Data Room, /mos:room opens the room view with its current state. The default entry point for room navigation." Nothing in tests pins that sentence.
- Theo maps its command layer from data/command-registry.json (docs/THEO-NOTIFY-CONTRACT.md Ruling 2: registryHash = sha256 of that file's bytes at the tagged commit); command_neighborhood is read the same way scripts/release-lib/theo-stamp-gate.sh does it, through lib/core/brain-client.cjs callTool('command_neighborhood', {command}), which sends only a command name.
- data/harness-manifest.json digests data/connector-registry.json (wiring role) and data/command-registry.json (posture role); the pre-commit hook re-checks it whenever either is staged (358-05 lesson). At planning time `node scripts/build-harness-manifest.cjs --check` ALREADY FAILS for a peer reason: skills/larry-personality/SKILL.md was changed by COMMITTED quick-260923-u8v (commit 5fffa21d6) without a manifest regenerate. That digest change is committed source, so it is carried into this regenerate and named in the commit message; it is not reverted and not attributed to B2.
- The pre-commit hook runs build-command-registry --check and build-connector-registry --check when commands/*.md is staged, and build-harness-manifest --check and build-orchestration-projection --check when data/command-registry.json or data/connector-registry.json is staged. So the teaching edit, the mirror and every regenerated file go in ONE commit.
- Open peer plans that regenerate the same files: 355-05 (data/mcp-tool-connectors.json, data/connector-registry.json, also edits lib/mcp/tools/sensors.cjs descriptions, which moves the test-270 byte total), 357-10, 359-09, 359-11 (data/harness-manifest.json). Generators read the WORKING TREE, so a peer's uncommitted edit would be swept into B2's regenerate.
- tests/test-270-tool-schema-budget.cjs asserts the measured toolCount equals AFTER.toolCount exactly and totalBytes within 10 percent of AFTER (358-05 AFTER: 42 tools, 45606 bytes). After 358-09 expect 44 tools. tests/fixtures/tool-honesty/276-dispositions.json frozen_sweep is 39 tools / 133 branches (358-05); expect 41 / 135 after 358-09.
- dist/ bundles (Zed, generic .claude dir) are not Rome surfaces, are not built by release.sh, and were not regenerated by B1; left untouched.

Parallel-session rules (every task): record `PLAN_BASE=$(git rev-parse HEAD)`. Stage explicit paths only; commit with `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git add .`, `git commit -a`, `git stash`, `--no-verify`, or a gsd-tools commit that sweeps the index. Never revert, stash or commit a peer's file. Never touch: skills/larry-personality/SKILL.md, lib/mcp/runtime-instructions.cjs, lib/mcp/tools/sensors.cjs, lib/mcp/tools/gate.cjs, docs/reviews/*, evals/plurai/*, scripts/eval-icm-writers.cjs, tests/test-353-* (run only), .planning/STATE.md, CLAUDE.md, dist/*. No em-dashes.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| Hardened `lib/core/navigation/typed-frame.cjs`; new `lib/core/frame-provenance.cjs` (the one door, readers, renders, the Decision Gate card); `lib/core/navigation.cjs` re-exports; regenerated `data/render-coverage-registry.json`; core, render and Part 8 tests; updated `tests/test-b2-frame-provenance.cjs`; `tests/run-all-358.sh` B2 legs | 07 |
| Routing spec in `lib/core/frame-provenance.cjs`; `scripts/room-question.cjs`; `/mos:room question` subcommands, routing section and tightened description; regenerated `skills/room/SKILL.md`; routing and CLI tests | 08 |
| `lib/mcp/tools/question.cjs` (question_read, question_set, connectors); `hostBlock` export on `lib/mcp/tools/claim-verify.cjs`; surfaces and persistence tests plus the child helper | 09 |
| `/mos:room` teaching line for Theo's command layer; regenerated `skills/room/SKILL.md`, `data/command-registry.json`, `data/mcp-tool-connectors.json`, `data/connector-registry.json`, `data/connector-coverage-ledger.json`, `data/harness-manifest.json` (and `data/brain-orchestration-projection.json` only on drift); test-270 AFTER re-baseline (plan 358-10); 276 re-freeze; recorded command-registry sha256 and the pre-release command_neighborhood reading | 10 (this plan) |
| `docs/2026-10-06-ROME-B2-GO-NO-GO.md` + human checkpoint | 11 |

<tasks>

<task type="auto">
  <name>Task 1: Teaching line for Theo plus one born-wired regenerate of every shared registry, committed together on a clean tree</name>
  <files>commands/room.md, skills/room/SKILL.md, data/command-registry.json, data/mcp-tool-connectors.json, data/connector-registry.json, data/connector-coverage-ledger.json, data/harness-manifest.json, data/brain-orchestration-projection.json</files>
  <read_first>
    - commands/room.md (frontmatter lines 1-35: the teaching line)
    - scripts/build-command-registry.cjs (lines 300-400: which frontmatter keys become registry fields; lines 520-560: the teaching tripwire)
    - scripts/build-connector-registry.cjs (header and --check mode: the three files it writes)
    - scripts/build-harness-manifest.cjs (lines 60-110 and 700-730: its source files and the --check message)
    - lib/mcp/tools/question.cjs (the connectors export as landed by 358-09)
    - .git/hooks/pre-commit (lines 150-260 and 420-460: the guards that fire on the staged paths)
    - docs/THEO-NOTIFY-CONTRACT.md (Ruling 2 registryHash, and the Step 5.6 description)
    - scripts/release-lib/theo-stamp-gate.sh (lines 15-35 and 90-100: the command_neighborhood read shape)
  </read_first>
  <action>
    1. Precondition: `git status --short -- data/ lib/mcp/ commands/ skills/ agents/ bin/ lib/core/chain-executor.cjs lib/core/navigation-engine.cjs lib/core/directive-envelope.cjs lib/statusline/cockpit-renderer.cjs` must print nothing (the generator inputs, including every file data/harness-manifest.json digests; lib/core/navigation-engine.cjs is open plan 355-22 territory). If it prints anything, STOP and report which peer paths are dirty (never stash, revert or commit them); resume when they are committed. Also run `ls .planning/phases/355-*/355-05-SUMMARY.md 2>/dev/null` and record whether 355-05 has landed.
    2. Edit ONLY the `teaching:` line of commands/room.md (navigator requirement item 2) to: `teaching: "When you need to view the active Data Room, /mos:room opens it. /mos:room question shows the room's governing question with its origin and history, and asks what the old question got wrong before a changed question is recorded."` (227 characters, two sentences, no em-dash). Nothing else in the file changes.
    3. `node scripts/build-skill-mirrors.cjs`; `git status --short -- skills/` must list only skills/room/SKILL.md.
    4. `node scripts/build-command-registry.cjs`; `git diff -- data/command-registry.json` must show only the /mos:room teaching value changing. Any other change: keep it only if `git log` shows its source command file change is committed (name it in the commit message and SUMMARY); if it traces to an uncommitted file, STOP.
    5. `node scripts/build-connector-registry.cjs`; the diff of data/mcp-tool-connectors.json and data/connector-registry.json must be exactly the added mcp:question_read (hitl_shape none, layer harness) and mcp:question_set (hitl_shape F.1, layer harness) records, and data/connector-coverage-ledger.json exactly one added row surface mcp_tool_file:lib/mcp/tools/question.cjs. Extra changes follow the same committed-source rule (for example 355-05's sensors.cjs if its source landed without a regenerate).
    6. `node scripts/build-orchestration-projection.cjs --check`; regenerate and include data/brain-orchestration-projection.json ONLY if it reports drift and the diff is explained by steps 2-5.
    7. `node scripts/build-harness-manifest.cjs`; the diff of data/harness-manifest.json is expected to change the wiring role (connector registry digest and source_count), the posture role (command registry digest), and the personality_skill digest carried from committed peer commit 5fffa21d6 (skills/larry-personality/SKILL.md). Any other role change must trace to a committed source, else STOP.
    8. Gates, all exit 0: `node scripts/build-connector-registry.cjs --check`, `node scripts/build-command-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/build-harness-manifest.cjs --check`, `node scripts/build-skill-mirrors.cjs --check`, `node scripts/check-shape-declaration.cjs --check`, `node scripts/check-render-coverage.cjs --check`, `node scripts/build-render-coverage.cjs --check`, `node tests/test-270-connector-coverage.cjs`, `node tests/test-358-b2-cli.cjs`, `node tests/test-358-b2-routing.cjs`, `node scripts/check-cirs-declaration.cjs --check .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-10-PLAN.md`.
    9. Commit together (the hook re-runs every guard on the staged set): `git add commands/room.md skills/room/SKILL.md data/command-registry.json data/mcp-tool-connectors.json data/connector-registry.json data/connector-coverage-ledger.json data/harness-manifest.json` (plus data/brain-orchestration-projection.json only if step 6 regenerated it) then `git commit --only -m "chore(358-10): born-wired regenerate for question_read and question_set; /mos:room question teaching for Theo's command layer" -- <the same paths>`; the commit body names every non-B2 change carried (at least the larry-personality manifest digest from 5fffa21d6). Record the commit sha as REGEN_SHA.
    10. Theo evidence for 358-11 (navigator requirement item 2): record in the SUMMARY `sha256sum data/command-registry.json` at REGEN_SHA (the registryHash Theo should report for /mos:room once a release containing REGEN_SHA is cut and resynced), and the /mos:room entry's teaching value from data/command-registry.json. Then take the pre-release reading, read-only, sending only the command name: `node --no-warnings -e "const bc=require('./lib/core/brain-client.cjs'); bc.callTool('command_neighborhood',{command:'/mos:room'}).then(function(r){var row=((r&&r.rows)||[])[0]||{}; console.log(JSON.stringify({mappedBy:row.mappedBy||null, registryHash:row.registryHash||null}));}).catch(function(e){console.log('unreachable: '+String((e&&e.message)||e).slice(0,80));})"`. Record the output verbatim. Expected now: an older mappedBy and a registryHash that differs from the recorded sha256, or 'unreachable'. Either is the honest pre-release state (a main commit is not live until released and picked up); it is not a failure of this plan.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check && node scripts/build-command-registry.cjs --check && node scripts/build-harness-manifest.cjs --check && node scripts/build-orchestration-projection.cjs --check && node scripts/build-skill-mirrors.cjs --check && node tests/test-270-connector-coverage.cjs >/dev/null && grep -c '"surface": "mcp:question_set"' data/mcp-tool-connectors.json data/connector-registry.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-connector-registry.cjs --check`, `node scripts/build-command-registry.cjs --check`, `node scripts/build-harness-manifest.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/build-skill-mirrors.cjs --check`, `node scripts/check-render-coverage.cjs --check` each exit 0
    - `grep -c '"surface": "mcp:question_read"' data/mcp-tool-connectors.json` is 1 and `grep -c '"surface": "mcp:question_set"' data/connector-registry.json` is 1
    - `grep -c "mcp_tool_file:lib/mcp/tools/question.cjs" data/connector-coverage-ledger.json` is 1
    - `node --no-warnings -e "const r=require('./data/command-registry.json'); const c=r.commands.find(x=>x.command==='/mos:room'); process.exit(c&&/\/mos:room question/.test(c.teaching)?0:1)"` exits 0
    - `git diff "$PLAN_BASE" -- commands/room.md | grep '^[-+]' | grep -v '^[-+][-+]' | grep -vc '^[-+]teaching:'` is 0 (only the teaching line changed)
    - `git show --stat HEAD --format=%s` lists only commands/room.md, skills/room/SKILL.md and data/ files
    - `git status --short -- data/ commands/ skills/` prints nothing after the commit
    - The SUMMARY records REGEN_SHA, the sha256 of data/command-registry.json, the /mos:room teaching value, and the verbatim pre-release command_neighborhood output
  </acceptance_criteria>
  <done>Both B2 tools and the /mos:room question teaching are in every generated registry, in one clean commit, and the Theo check has its expected hash on record.</done>
</task>

<task type="auto">
  <name>Task 2: Re-baseline the tool-schema budget, re-freeze the honesty sweep, prove the phase green</name>
  <files>tests/test-270-tool-schema-budget.cjs, tests/fixtures/tool-honesty/276-dispositions.json</files>
  <read_first>
    - tests/test-270-tool-schema-budget.cjs (lines 225-300: BASELINE, the 276-12 and 358-05 comment blocks, AFTER; the protocol: move AFTER deliberately, name the measured percentage in the commit message, never relax DRIFT_TOLERANCE_PCT)
    - tests/fixtures/tool-honesty/276-dispositions.json (schema_version, frozen_at_commit, frozen_sweep, dispositions, refrozen_at written by 358-05)
    - tests/test-276-tool-honesty-findings-closed.cjs (GROUP A: every non-OK live row needs a disposition; GROUP F: frozen_sweep vs scanAll)
    - scripts/check-tool-honesty.cjs (scanAll: { rows, toolCount, branchCount })
    - .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-05-SUMMARY.md (the measured 358-05 numbers and the peer-commit-between-tasks lesson for refrozen_at.commit)
  </read_first>
  <action>
    1. Precondition: `git status --short -- lib/mcp/ bin/` prints nothing (the measurement spawns the real server from the working tree). If not, STOP.
    2. Measure: `node tests/test-270-tool-schema-budget.cjs` and copy its `budget:` line (tools, desc bytes, schema bytes, total, approx tokens, router, atomic). A spawn timeout on the first run is transient contention (358-05 saw it): re-run once before treating it as a failure.
    3. Edit tests/test-270-tool-schema-budget.cjs: keep BASELINE and every existing comment block as history; add a new dated comment block directly above AFTER explaining that plan 358-09 added question_read and question_set (toolCount 42 -> 44 expected), naming the measured numbers and the signed percentage change of totalBytes against the 358-05 AFTER total (45606) computed with the file's own pctChange rule (two decimals); if 355-05 landed after 358-05 and changed tool descriptions, say that part of the delta is 355-05's and name its commit; DRIFT_TOLERANCE_PCT is not relaxed. Replace AFTER's values with the measured ones, `measuredAt` = today (YYYY-MM-DD), `plan: '358-10'`. Change nothing else.
    4. Re-freeze: compute the live sweep with `node -e` over `require('./scripts/check-tool-honesty.cjs').scanAll()` (toolCount, branchCount and the verdict counts mapped onto frozen_sweep keys high_risk, medium, low, unknown, ok). Confirm high_risk is 0 and that question_read and question_set rows are OK (if either is not, STOP: fix the tool in a follow-up under this phase, never add a disposition to hide it). Update frozen_sweep; replace refrozen_at with { commit: REGEN_SHA from Task 1 (short sha; never whatever HEAD happens to be if a peer committed in between), reason: one paragraph: re-frozen by plan 358-10 (Phase 358 B2); the 358-05 freeze at bdf77d1ee (39 tools / 133 branches) was invalidated by plan 358-09 adding question_read and question_set; the live scan is now N tools / B branches; both new rows are OK so no disposition was added; every prior disposition re-checked by tests/test-276-tool-honesty-findings-closed.cjs; HIGH_RISK is 0 }. Leave schema_version, frozen_at_commit and dispositions untouched; keep 2-space indent and the trailing newline; `git diff` touches only frozen_sweep and refrozen_at.
    5. Run `node tests/test-270-tool-schema-budget.cjs`, `node tests/test-276-tool-honesty-findings-closed.cjs`, `node tests/test-234-tool-description-floor.cjs` (all exit 0).
    6. Commit: `git add tests/test-270-tool-schema-budget.cjs tests/fixtures/tool-honesty/276-dispositions.json` then `git commit --only -m "test(358-10): re-baseline tool schema budget (<N> tools, <T> bytes, <+P>% vs 358-05) and re-freeze honesty sweep (<N>/<B>)" -- tests/test-270-tool-schema-budget.cjs tests/fixtures/tool-honesty/276-dispositions.json` with the real numbers.
    7. Phase gate: `bash tests/run-all-358.sh; echo "exit=$?"` must print exit=0 (77 means a leg was skipped: find the missing file; 1 means a failure: fix the owning 358 file, or record a peer-caused failure with its owning phase). Confirm `node tests/test-358-b2-routing.cjs | grep "routing text: 3 of 3 checked"` and `node tests/test-358-b2-part8.cjs | grep "part8 static: 4 of 4 B2 files scanned"`. Then `node scripts/doctor.cjs --acceptance` and classify every non-pass line per the CLAUDE.md QA rule (WORKING, known tracked bug, ENV GAP, NEW FAILURE); a NEW FAILURE caused by B2 files is fixed before this plan is done; peer-caused findings are listed with their owning phase. Record the runner summary line and the doctor summary in the SUMMARY.
  </action>
  <verify>
    <automated>node tests/test-270-tool-schema-budget.cjs >/dev/null && node tests/test-276-tool-honesty-findings-closed.cjs >/dev/null && node tests/test-234-tool-description-floor.cjs >/dev/null && bash tests/run-all-358.sh >/dev/null 2>&1; echo "runner exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-270-tool-schema-budget.cjs`, `node tests/test-276-tool-honesty-findings-closed.cjs` and `node tests/test-234-tool-description-floor.cjs` each exit 0
    - `grep -c "plan: '358-10'" tests/test-270-tool-schema-budget.cjs` is 1 and `grep -c "DRIFT_TOLERANCE_PCT = 10" tests/test-270-tool-schema-budget.cjs` equals its value at PLAN_BASE
    - `node -e "const j=require('./tests/fixtures/tool-honesty/276-dispositions.json'); const s=require('./scripts/check-tool-honesty.cjs').scanAll(); if(j.frozen_sweep.tools!==s.toolCount||j.frozen_sweep.branches!==s.branchCount||!/358-10/.test(j.refrozen_at.reason)) process.exit(1)"` exits 0
    - `git log -1 --format=%s` names the measured tool count, total bytes and percentage
    - `bash tests/run-all-358.sh` exits 0 and its summary line shows FAILED=0 SKIPPED=0
    - `node tests/test-358-b2-routing.cjs | grep -c "routing text: 3 of 3 checked"` is 1 and `node tests/test-358-b2-part8.cjs | grep -c "part8 static: 4 of 4 B2 files scanned"` is 1
    - The SUMMARY records the doctor --acceptance summary and a classification for every non-pass line
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-270-tool-schema-budget.cjs tests/fixtures/tool-honesty/276-dispositions.json` prints 0 for both
  </acceptance_criteria>
  <done>Every baseline moved deliberately with measured numbers, and the whole Phase 358 runner (B1 and B2) passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> generated registries and measured baselines | generators and the budget test read the shared working tree |
| plugin registry -> Theo command layer | Theo maps data/command-registry.json after a release and resync |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-49 | Tampering | regenerating shared registries over a peer's uncommitted work | mitigate | Clean-tree precondition over data/, lib/mcp/, commands/, skills/, agents/, bin/ and every file the harness manifest digests; each diff inspected; extra changes kept only when their source is committed and named in the commit message |
| T-358-50 | Repudiation | a baseline moved silently to hide drift | mitigate | Measured numbers and the signed percentage in a comment block and the commit message; tolerance unchanged |
| T-358-51 | Tampering | honesty findings suppressed by a re-freeze | mitigate | Only frozen_sweep and refrozen_at change; dispositions untouched; HIGH_RISK must be 0; new rows must be OK, never dispositioned |
| T-358-52 | Information disclosure | the Theo probe carrying room content | mitigate | The probe sends only the literal command name '/mos:room' through lib/core/brain-client.cjs, the same call release-lib/theo-stamp-gate.sh makes; no room path or text |
</threat_model>

<verification>
- `node scripts/build-connector-registry.cjs --check && node scripts/build-command-registry.cjs --check && node scripts/build-harness-manifest.cjs --check && node scripts/build-orchestration-projection.cjs --check && node scripts/check-render-coverage.cjs --check && node scripts/build-skill-mirrors.cjs --check && node scripts/check-tool-honesty.cjs --check`
- `bash tests/run-all-358.sh` exits 0
- `node scripts/doctor.cjs --acceptance` run and classified
</verification>

<success_criteria>
- B2-10: question_read and question_set born wired in every generated registry; /mos:room question teaching in data/command-registry.json; test-234 floor, test-270 budget and test-276 sweep green; the phase runner exits 0
- Navigator requirement item 2: the registry Theo maps carries the new subcommands, with the expected registryHash recorded for the post-release check
</success_criteria>

<output>
Create `.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-10-SUMMARY.md` when done (stage it with `git add -f` and commit it with `git commit --only`).
</output>
