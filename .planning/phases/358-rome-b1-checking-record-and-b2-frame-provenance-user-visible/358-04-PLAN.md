---
phase: 358-rome-b1-checking-record-and-b2-frame-provenance-user-visible
plan: 04
type: execute
wave: 3
depends_on: ["358-01", "358-02", "358-03"]
files_modified:
  - tests/test-358-b1-surfaces.cjs
  - lib/mcp/tools/claim-verify.cjs
  - tests/helpers/b1-358-child.cjs
  - tests/test-358-b1-persistence.cjs
autonomous: true
requirements: [B1-02, B1-03, B1-04, B1-05, B1-06, B1-07]
canon_parts: [8, 9, 11, 12]
tags: [b1, mcp, claim-read, claim-verify, desktop, cowork, rome, tdd, cirs]
cirs_relationship:
  surfaces_added: ["claim_read"]
  surfaces_modified: ["claim_verify"]
  surfaces_removed: []
  spine_consumed: ["navigation.recordClaimVerification", "navigation.readClaimVerification", "navigation.listClaimsForChecking", "navigation.readVerificationPortrait", "navigation.resolveByUser", "resolveEffectiveSessionId", "resolveSessionRoomDir", "openRoomDbForCaller"]
  gate_impact: "claim_read is born wired: its connectors entry (hitl_shape none, layer harness) ships in lib/mcp/tools/claim-verify.cjs in the same commit, and register-core-tools.cjs auto-discovers it (no router or registration edit). data/connector-registry.json and data/mcp-tool-connectors.json are regenerated by 358-05, never hand-edited here. claim_verify keeps its F.1 connector entry byte-unchanged; only its description and input schema change."
  explanation: "Tri-Polar: officers on Claude Desktop and Cowork drive B1 through MCP tools, so the write tool gains the rung and the read side (claim view plus room counts) becomes one tool call. A separate read tool is used instead of a mode on claim_verify because the write gate would then block reads on any host where writes are off, and tool-honesty would see a read branch inside a write tool (research, Alternatives Considered). Part 9: both tools call navigation.cjs only. Part 8: no Brain call in either tool."

must_haves:
  truths:
    - "On Claude Desktop (clientInfo.name claude-ai) an officer files a claim with claim_write and records on it what it was checked against, the rung, the method and the result with claim_verify, over the real stdio server, with MINDRIAN_MCP_FIRST unset (AT1, Desktop half; Cowork via the same tools once its client name is probed or the documented env fallback is set)"
    - "claim_verify requires rung (integer 1..VERIFICATION_RUNGS.length, described from the constant), stamps checked_by_id from navigation.resolveByUser(roomDir) for checked_by user (navigator ruling: Who checked), accepts resolves_dispute only as the person's explicit call, and returns the claim view showing Confirmation status unchanged"
    - "claim_read reopens a claim by id or by words in a brand-new process and session and shows confirmation (review_status) and checking_record side by side, plus the room portrait and its rendered text, and it works even where writes are refused (AT2, AT4)"
    - "claim_read always returns the portrait with checked, disputed, inconclusive and unchecked, and its rendered text always prints the unchecked line, never a score (AT3)"
    - "Close everything, new session, reopen: a child process writes and re-files the claim in session S1, a separate child process reads it by words in session S2, and the full record (against, rung and rung label, method, result, by, when) is there with review_status proposed (AT2 literal)"
    - "No MCP write in this plan changes review_status or confidence (AT4)"
    - "The Cowork client-name probe is possible from inside any host: claim_read returns a `host` block { client_name, host, host_tier, write_path_enabled } and a claim_verify write_path_disabled refusal carries the same block plus a hint naming the MINDRIAN_MCP_FIRST=desktop,cowork demo-machine fallback (navigator ruling: Cowork is added only after a live probe confirms its client name)"
    - "tests/test-234-tool-description-floor.cjs is green again: claim_verify and claim_read descriptions are at least 120 characters, start with a capital, end with a sentence terminator, stay under 2048 bytes and contain no em-dash"
    - "check-tool-honesty rows for claim_verify and claim_read are OK, so tests/test-276-tool-honesty-findings-closed.cjs GROUP A needs no new disposition"
  artifacts:
    - path: "lib/mcp/tools/claim-verify.cjs"
      provides: "claim_verify (rung required, checked_by_id stamp, resolves_dispute, claim view in the response) and the new claim_read read tool, plus both connectors entries"
      exports: ["register", "connectors"]
      contains: "claim_read"
    - path: "tests/test-358-b1-surfaces.cjs"
      provides: "stub-server legs (schemas, descriptions, Desktop write, unknown-host refusal, claim_read shapes, disputed sticks, review_status unchanged, static guards) and the real stdio wire leg as claude-ai"
      min_lines: 200
    - path: "tests/helpers/b1-358-child.cjs"
      provides: "child process modes write | refile | file-again | read for the cross-session test"
      min_lines: 60
    - path: "tests/test-358-b1-persistence.cjs"
      provides: "AT2 literal: separate processes and sessions write, re-file, read; duplicate re-file in a new session is visible"
      min_lines: 80
  key_links:
    - from: "lib/mcp/tools/claim-verify.cjs claim_verify handler"
      to: "navigation.recordClaimVerification"
      via: "after isWritePathEnabled and session room resolution"
      pattern: "navigation\\.recordClaimVerification\\("
    - from: "lib/mcp/tools/claim-verify.cjs claim_read handler"
      to: "navigation.readClaimVerification / listClaimsForChecking / readVerificationPortrait"
      via: "no write gate (reads are unconditional)"
      pattern: "navigation\\.readVerificationPortrait\\("
    - from: "lib/mcp/register-core-tools.cjs"
      to: "lib/mcp/tools/claim-verify.cjs register"
      via: "auto-discovery of every tools/*.cjs (no edit to register-core-tools.cjs or tool-router.cjs)"
      pattern: "server\\.tool\\('claim_read'"
---

<objective>
Make B1 work for officers on Claude Desktop and Cowork through MCP tools: extend `claim_verify` (rung required, checked_by_id stamped server-side, resolves_dispute, claim view in the response, a description long enough to clear the 120-character floor) and add a read tool `claim_read` beside it in the same file (claim view by id or words, claim list, room portrait, rendered text). Prove it over the real stdio server as the Claude Desktop client, and prove AT2 literally with separate processes and sessions. Tests first.

Purpose: the Desktop / Cowork half of AT1, and the MCP halves of AT2, AT3 and AT4. Implements CONTEXT Claude's Discretion "Exact MCP surface: extend claim_verify and add or extend a read tool for the claim view and the room portrait", the Who checked and Disputed sticks rulings on MCP, and turns tests/test-234-tool-description-floor.cjs green (red since 42191a6ae).

Transient drift, stated up front: after this plan commits, `node scripts/build-connector-registry.cjs --check` reports drift for mcp:claim_read, and test-270 budget / test-276 frozen sweep stay red, until 358-05 (the very next wave) regenerates and re-baselines. Do NOT regenerate data/*.json here. If a peer reports a commit blocked by that drift, run 358-05 Task 1 immediately.

Output: the extended `lib/mcp/tools/claim-verify.cjs`, `tests/test-358-b1-surfaces.cjs`, `tests/helpers/b1-358-child.cjs`, `tests/test-358-b1-persistence.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-CONTEXT.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-RESEARCH.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-01-SUMMARY.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-02-SUMMARY.md
@lib/mcp/tools/claim-verify.cjs

Parallel-session rules (every task): peers execute Phases 354-357 in this same working tree. Record `PLAN_BASE=$(git rev-parse HEAD)` at start. Before editing any file run `git status --short -- <file>` and STOP if it shows a diff you did not make. Stage explicit paths only and commit with `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git add .`, `git commit -a`, `git stash`, or `--no-verify`. Never touch: docs/reviews/*, evals/plurai/*, scripts/eval-icm-writers.cjs, scripts/jev-devtime-client.cjs, tests/test-353-* (run only), .planning/phases/356-*, data/jev-policies/, .planning/STATE.md, lib/mcp/tools/gate.cjs, lib/mcp/tools/views.cjs, lib/mcp/tool-router.cjs, lib/mcp/register-core-tools.cjs (auto-discovery makes an edit unnecessary), lib/mcp/tools/status.cjs (so the portrait does NOT go into status_read), lib/mcp/tools/dual-path.cjs, lib/mcp/tools/sensors.cjs, lib/mcp/runtime-instructions.cjs (tool descriptions are how Desktop Larry discovers the tools; the instruction text is budget- and parity-guarded), any data/*.json, CLAUDE.md. Do not require any tests/ file from the other test files except the new helper. No em-dashes (hyphens only). Never name anything a "stamp".

Interfaces this plan consumes (from 358-01 via lib/core/navigation.cjs): `recordClaimVerification(db, params)` -> `{ ok, claim_id, review_status, verification }` | `{ ok: false, reason }`; `readClaimVerification(db, id)` -> `{ ok, claim: { claim_id, text, knowledge_type, confirmation: { review_status, note }, checking_record: { status, status_note, records_total, records } } }`; `listClaimsForChecking(db, { query, limit })` -> `{ ok, total_matched, claims: [{ claim_id, text_preview, review_status, checking_status, records_total }] }`; `readVerificationPortrait(db)`; `renderVerificationPortraitLines`, `renderClaimViewLines`, `renderClaimListLines`; `VERIFICATION_RUNGS`, `VERIFICATION_AGAINST_KINDS`, `VERIFICATION_METHODS`, `VERIFICATION_RESULTS`; `resolveByUser(roomDir)`. From 358-02: Claude Desktop `claude-ai` is a write-enabled tier0 host; a re-file keeps the checking record.

Tool-honesty rules for the two descriptions (scripts/check-tool-honesty.cjs :1049-1080): its persistence verbs are file, write, store, save, persist, insert, mint, archive, commit, generate, build, index, rebuild, render, update, add, log, produce, publish, snapshot. claim_verify DOES write, so its description must NOT use any negation phrase from NEGATION_PATTERNS (writes nothing, does not write, never writes, nothing is written, no write occurs, reference only, nothing is persisted) - that would be a dishonest global cancel; "record" and "never changes review_status" are fine. claim_read writes nothing, so its description ends with the sentence "This tool writes nothing." (the recognized global disclaimer). Both scanAll rows must be OK.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `lib/core/navigation/verification.cjs` (rung constant, record fields, sticky disputed, claim view + list readers, portrait by rung, render helpers), `lib/core/navigation.cjs` re-exports, core/portrait/separation tests, updated `tests/test-b1-verification.cjs` | 01 |
| `lib/core/navigation/typed-claim.cjs` carry-forward + PROTECTED_CLAIM_KEYS, `tests/test-358-b1-refile.cjs`, `lib/mcp/surface-detect.cjs` claude-ai tier0 host, `tests/test-234-host-tier.cjs` additions, `tests/run-all-358.sh` | 02 |
| `scripts/claim-checks.cjs`, `/mos:room checks`, `/mos:room claim`, `/mos:room check`, regenerated `skills/room/SKILL.md`, `tests/test-358-b1-cli.cjs` | 03 |
| `claim_verify` (rung required, checked_by_id, resolves_dispute, description >= 120 chars), new `claim_read` MCP tool, `tests/test-358-b1-surfaces.cjs`, `tests/helpers/b1-358-child.cjs`, `tests/test-358-b1-persistence.cjs` | 04 (this plan) |
| Regenerated `data/mcp-tool-connectors.json` + `data/connector-registry.json`, test-270 AFTER re-baseline, 276 ledger re-freeze | 05 |
| `docs/2026-10-06-ROME-B1-GO-NO-GO.md` (operator runbook) + human checkpoint | 06 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - MCP surfaces test (stub server legs + real stdio wire leg as claude-ai)</name>
  <files>tests/test-358-b1-surfaces.cjs</files>
  <read_first>
    - lib/mcp/tools/claim-verify.cjs (whole file, 51 lines)
    - lib/mcp/tools/claim.cjs (lines 95-130: claim_write schema; lines 160-175: writeClaimNode call and response)
    - tests/test-276-claim-write-primitive.cjs (lines 100-175: hermetic room, registerAndCapture stub server, callTool)
    - tests/test-234-host-tier.cjs (lines 309-380: driveServer, spawning bin/mindrian-mcp-server.cjs over stdio with a chosen clientInfo.name; lines 500-530: capability_floor assertions)
    - tests/test-270-tool-schema-budget.cjs (lines 80-135: listToolsOverStdio JSON-RPC framing)
    - tests/test-234-tool-description-floor.cjs (lines 300-345: the prose-shape checks and HOST_DESCRIPTION_CAP_BYTES)
    - scripts/check-tool-honesty.cjs (lines 1049-1080 vocabulary and negation patterns; scanAll export)
  </read_first>
  <behavior>
    - stub A1: registering all core tools captures both claim_verify and claim_read
    - stub A2: `z.object(<captured claim_verify schema>)` rejects args without rung, rejects rung 0 and rung VERIFICATION_RUNGS.length + 1 and 2.5, accepts rung 3; resolves_dispute is optional boolean; the rung field's description contains every VERIFICATION_RUNGS label
    - stub A3: claim_verify and claim_read descriptions are each at least 120 characters and at most 2048 bytes, start with a capital, end with '.', contain no em-dash and no /\bverified\b/i; claim_verify's contains 'review_status' and 'gate_answer' and matches none of check-tool-honesty's NEGATION_PATTERNS; claim_read's contains 'checked', 'disputed', 'inconclusive', 'unchecked' and 'This tool writes nothing.'
    - stub A4 (Desktop): with MINDRIAN_MCP_FIRST deleted and the stub exposing `server.server.getClientVersion = () => ({ name: 'claude-ai', version: '0.1.0' })`, ctx surface 'desktop': claim_write of 'The bridge at grid 42 is passable.' succeeds; claim_verify (against_id 'field note, exercise 02', against_kind observation, rung 3, method compare, result contradicts) returns ok true, review_status 'proposed', claim.checking_record.status 'disputed', and rendered text containing 'Confirmation status: proposed'; the stored record has checked_by 'user' and checked_by_id equal to navigation.resolveByUser(room)
    - stub A5 (unknown host): getClientVersion returning { name: 'SomeCoworkClient', version: '1' }: claim_verify is refused with reason write_path_disabled and isError true, and the refusal carries host.client_name 'SomeCoworkClient', host.host 'unknown', host.write_path_enabled false and a hint containing 'MINDRIAN_MCP_FIRST'; claim_read still returns ok true with the portrait and the same host block; with getClientVersion returning undefined, host.client_name is null
    - stub A6: claim_read { query: 'bridge' } returns ok, exactly 1 claim in claims, a claim object with confirmation and checking_record, portrait with claims_checked, claims_disputed, claims_inconclusive and claims_unchecked, rendered.portrait containing 'unchecked (no checking record yet): ', rungs listing every VERIFICATION_RUNGS label, and host { client_name: 'claude-ai', host: 'claude-desktop', host_tier: 'tier0', write_path_enabled: true } under the Desktop identity; no key in the payload (deep walk) and no rendered string matches /score|percent|pct|ratio|grade|coverage|%/i; no rendered string matches /\bverified\b/i
    - stub A7: claim_read {} lists claims newest first (at most 20); claim_read { claim_id: 'claim:none:0' } returns ok false, reason claim_not_found, isError true, portrait still present; the schema rejects limit 0 and limit 101
    - stub A8 (disputed sticks): claim_verify supports after the contradicts keeps status disputed; resolves_dispute true with checked_by system is refused resolution_requires_person; resolves_dispute true (checked_by default user) with supports gives status checked and 3 records
    - stub A9: nodes.review_status and confidence are byte-identical before and after every claim_verify call in A4 and A8
    - stub A10 (static): comment-stripped lib/mcp/tools/claim-verify.cjs contains no confirmNode, promoteNodeStatus, brain, node:sqlite or DatabaseSync, and no hard-coded enum fallback list; its connectors export has a claim_read entry { tool: 'claim_read', surface: 'claim_read', connector: 'mcp-tool', hitl_shape: 'none', layer: 'harness' } with non-empty hitl_why and layer_why, and the claim_verify entry is unchanged (hitl_shape 'F.1', hitl_why exactly 'Records local verification metadata on a claim; it never promotes truth state.')
    - stub A11: `require('./scripts/check-tool-honesty.cjs').scanAll().rows` rows for claim_verify and claim_read all have verdict OK
    - wire B1: spawn bin/mindrian-mcp-server.cjs with env MINDRIAN_ROOM=<temp room>, MINDRIAN_ROOMS_HOME=<empty temp>, HOME=<temp>, MINDRIAN_MCP_FIRST and CLAUDE_ACTIVE_ROOM deleted; initialize with clientInfo { name: 'claude-ai', version: '0.1.0' }; tools/list includes claim_write, claim_verify and claim_read; tools/call claim_write then claim_verify (using the returned node_id) then claim_read { query: 'bridge' } then status_read: claim_verify ok true, claim_read shows the record with rung 3, status_read segments.capability_floor.host_tier.host is 'claude-desktop' and write_path_enabled is true
  </behavior>
  <action>
    Create tests/test-358-b1-surfaces.cjs in the repo test convention (check counter, `N passed, M failed`, exit code; wrap the async parts in an async IIFE). Hermetic: fresh temp HOME, MINDRIAN_HOME and MINDRIAN_ROOMS_HOME set BEFORE requiring any lib module; delete MINDRIAN_MCP_FIRST and CLAUDE_ACTIVE_ROOM. Stub server: copy the registerAndCapture pattern from tests/test-276-claim-write-primitive.cjs (do not require that file) and add `server: { getClientVersion: () => currentClient }` on the stub so each leg can switch the identity; ctx `{ fallbackRoomDir: roomDir, pluginRoot: REPO_ROOT, surface: 'desktop' }`; handler extra `{ sessionId: 'test-358-surfaces' }`. Validate schemas with `require('zod').z.object(captured.schema)`. For the wire leg write a small promise-based JSON-RPC client in this file (spawn with stdio pipes, newline-delimited JSON, a map from request id to resolver, send `initialize`, then the `notifications/initialized` notification, then each request one at a time awaiting its response; 30-second overall timeout; always kill the child in a finally); parse each tools/call result from `result.content[0].text`. Build the room with lib/core/room-db.cjs openRoomDb/closeRoomDb before spawning. Pull every expected enum and label from navigation. Run it: the claim_read, rung, description and checked_by_id legs MUST fail now. Commit: `git add tests/test-358-b1-surfaces.cjs` then `git commit --only -m "test(358-04): failing MCP surfaces test for claim_verify and claim_read" -- tests/test-358-b1-surfaces.cjs`.
  </action>
  <verify>
    <automated>(node tests/test-358-b1-surfaces.cjs >/dev/null 2>&1; test $? -ne 0) && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-358-b1-surfaces.cjs exists and exits non-zero before Task 2 (RED proven; exit code in the SUMMARY)
    - `grep -c "claude-ai" tests/test-358-b1-surfaces.cjs` is at least 2 and `grep -c "mindrian-mcp-server.cjs" tests/test-358-b1-surfaces.cjs` is at least 1
    - `grep -c "write_path_disabled" tests/test-358-b1-surfaces.cjs` is at least 1 and `grep -c "scanAll" tests/test-358-b1-surfaces.cjs` is at least 1
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-358-b1-surfaces.cjs` is 0
  </acceptance_criteria>
  <done>A failing test pins both MCP tools, the Desktop write path and the wire behavior as the real Desktop client.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - extend claim_verify and add claim_read in lib/mcp/tools/claim-verify.cjs</name>
  <files>lib/mcp/tools/claim-verify.cjs</files>
  <read_first>
    - lib/mcp/tools/claim-verify.cjs (whole file)
    - tests/test-358-b1-surfaces.cjs (Task 1 output)
    - lib/mcp/tools/graph.cjs (lines 186-209: an unconditional read handler; lines 284-316: connectors entries with hitl_shape 'none' vs 'F.1')
    - lib/mcp/register-core-tools.cjs (lines 53-75: auto-discovery; read only, never edit)
    - lib/core/navigation.cjs (the B1 re-export block from 358-01)
  </read_first>
  <behavior>
    - every leg of tests/test-358-b1-surfaces.cjs passes
    - tests/test-234-tool-description-floor.cjs passes
  </behavior>
  <action>
    Edit lib/mcp/tools/claim-verify.cjs (Canon Part 9: both tools call navigation.cjs only; Part 8: no Brain; per CONTEXT Claude's Discretion "extend claim_verify and add a read tool"):
    1. Header comment: B1 MCP surface; claim_verify appends one local checking record; claim_read reopens claims and shows the room counts; neither ever confirms a claim (only gate_answer approve by a person does, gate.cjs, untouched); hyphens only.
    2. Remove the hard-coded enum fallback arrays: take against_kind, method and result straight from `navigation.VERIFICATION_AGAINST_KINDS / VERIFICATION_METHODS / VERIFICATION_RESULTS` (always exported), so every enum has one source. Build `RUNG_DESCRIPTION` from `navigation.VERIFICATION_RUNGS`: 'Rung of the verification hierarchy the check reached, 1 is lowest: ' + entries joined as 'N = <label>' with '; ' + '.'.
    3. claim_verify: new description, for example "Record, on one local claim, what it was checked against, the rung it reached, the method and the result. The claim's review_status never changes: a checked claim stays proposed until a person approves it through gate_answer. Use claim_read first to find the claim_id." (at least 120 characters, capital first, ends with '.', none of the NEGATION_PATTERNS phrases, none of the scanner's persistence verbs listed in the context). Schema: keep claim_id, against_id, against_kind, method, result, checked_by (user|system, optional), checked_at, note_handle; ADD `rung: z.number().int().min(1).max(navigation.VERIFICATION_RUNGS.length).describe(RUNG_DESCRIPTION)` (required) and `resolves_dispute: z.boolean().optional().describe('Set true only when the person explicitly says this check answers an earlier contradiction on this claim. Requires checked_by user.')`. Add a small helper `hostBlock(server, ctx)` in this file: read clientVersion the same way the handler already does, and return `{ client_name: (clientVersion && typeof clientVersion.name === 'string') ? clientVersion.name.slice(0, 64) : null, host: detectHostTier(clientVersion).host, host_tier: detectHostTier(clientVersion).hostTier, write_path_enabled: isWritePathEnabled({ surface: ctx && ctx.surface, clientVersion }) }` (require detectHostTier from '../surface-detect.cjs'; this is the live probe the 6 October runbook uses to learn Cowork's client name). Handler: keep the write gate and room resolution as they are, except that the write_path_disabled refusal now returns `{ ok: false, reason: 'write_path_disabled', host: hostBlock(server, ctx), hint: 'This MCP host is not recognized as write-enabled. On a demo machine set MINDRIAN_MCP_FIRST=desktop,cowork in the MCP server env block and restart, and report host.client_name so it can be added to the host list.' }` with isError; build `params = Object.assign({}, args)`; when `params.checked_by !== 'system'` set `params.checked_by_id = navigation.resolveByUser(roomDir)` (server-side only; not a tool input; local only, per the Who checked ruling); call `navigation.recordClaimVerification(db, params)`; on ok also read `navigation.readClaimVerification(db, result.claim_id)` and return `{ room_dir, ok: true, claim_id, review_status, verification, claim, rendered: renderClaimViewLines(claim).join('\n') }`; on refusal return the refusal with room_dir and isError. Close the db in finally (unchanged).
    4. claim_read (new, registered in the same `register` function right after claim_verify): description, for example "Reopen one local claim by id or by words from its text and show its checking record beside its confirmation status (review_status), or list the room's claims. Every call also returns the room's counts of checked, disputed, inconclusive and unchecked claims; a count is not a verdict. This tool writes nothing." Schema: `claim_id: z.string().min(1).max(512).optional()`, `query: z.string().min(1).max(200).optional()`, `limit: z.number().int().min(1).max(100).optional()`, each with a short describe. Handler: NO write gate (reads are unconditional, like graph_query); resolve sessionId and roomDir exactly as claim_verify does; open with openRoomDbForCaller (null -> `{ ok: false, reason: 'no_room_db', room_dir }`, isError); in a try/finally closing the db: `portrait = navigation.readVerificationPortrait(db)`; `rendered = { portrait: renderVerificationPortraitLines(portrait).join('\n') }`; `rungs = VERIFICATION_RUNGS.map((r, i) => ({ rung: i + 1, id: r.id, label: r.label }))`; `note = 'Checked is not confirmed. Only a person confirms a claim, through gate_answer approve.'`; `host = hostBlock(server, ctx)`. If claim_id is given: readClaimVerification; not ok -> `{ ok: false, reason: 'claim_not_found', room_dir, portrait, rendered, rungs, note }` with isError; ok -> payload.claim and rendered.claim. Otherwise: `listClaimsForChecking(db, { query, limit })` -> payload.claims, payload.total_matched, rendered.claims; when a query matched exactly one claim, also attach that claim's view as payload.claim and rendered.claim. Return `{ ok: true, room_dir, portrait, claims?, total_matched?, claim?, rendered, rungs, note, host }` (the claim_not_found refusal carries host too).
    5. connectors: keep the claim_verify entry byte-identical; append `{ tool: 'claim_read', surface: 'claim_read', connector: 'mcp-tool', hitl_shape: 'none', hitl_why: 'Pure read of local claims, their checking records and the room counts; no fork and no write.', layer: 'harness', layer_why: 'Local graph read through the navigation chokepoint.' }`.
    Do NOT edit register-core-tools.cjs, tool-router.cjs, status.cjs or any data/*.json (358-05 regenerates the registry).
    Commit: `git add lib/mcp/tools/claim-verify.cjs` then `git commit --only -m "feat(358-04): claim_verify takes a rung and claim_read reopens claims with room counts" -- lib/mcp/tools/claim-verify.cjs`.
  </action>
  <verify>
    <automated>node tests/test-358-b1-surfaces.cjs && node tests/test-234-tool-description-floor.cjs >/dev/null && node scripts/check-tool-honesty.cjs --check >/dev/null 2>&1 && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-358-b1-surfaces.cjs` exits 0 (stub legs and the stdio wire leg)
    - `node tests/test-234-tool-description-floor.cjs` exits 0 (red since 42191a6ae, green now)
    - `node scripts/check-tool-honesty.cjs --check` exits 0 and `node -e "const s=require('./scripts/check-tool-honesty.cjs').scanAll(); const bad=s.rows.filter(r=>/^claim_(verify|read)$/.test(r.tool)&&r.verdict!=='OK'); if(bad.length){console.error(JSON.stringify(bad));process.exit(1)}"` exits 0
    - `grep -v '^\s*//' lib/mcp/tools/claim-verify.cjs | grep -cE "confirmNode|promoteNodeStatus|brain|node:sqlite|DatabaseSync"` is 0
    - `grep -cE "'compare'|'observe'|'supports'|'contradicts'" lib/mcp/tools/claim-verify.cjs` is 0 (enum fallbacks removed)
    - `git diff $PLAN_BASE --stat -- lib/mcp/register-core-tools.cjs lib/mcp/tool-router.cjs lib/mcp/tools/status.cjs data/` shows no change made by this plan
    - `node tests/test-358-b1-core.cjs && node tests/test-358-b1-cli.cjs && node tests/test-276-claim-write-primitive.cjs` still exit 0
    - `grep -c "$(printf '\xe2\x80\x94')" lib/mcp/tools/claim-verify.cjs` is 0
  </acceptance_criteria>
  <done>Desktop and Cowork officers can record a check with a rung and reopen claims and room counts through MCP; the description floor is green; honesty rows are OK.</done>
</task>

<task type="auto">
  <name>Task 3: AT2 literal - separate processes and sessions write, re-file and reopen the record</name>
  <files>tests/helpers/b1-358-child.cjs, tests/test-358-b1-persistence.cjs</files>
  <read_first>
    - lib/mcp/tools/claim-verify.cjs (Task 2 output: claim_read payload shape)
    - tests/test-358-b1-surfaces.cjs (the stub-server pattern with getClientVersion)
    - lib/core/navigation/typed-claim.cjs (CLAIM_NODE_ID: ids are claim:<sessionId>:<hash>, so a new session re-filing the same sentence mints a NEW claim; research Pitfall 3)
    - .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-RESEARCH.md (Session Persistence section, cross-process helper design)
  </read_first>
  <action>
    Create tests/helpers/b1-358-child.cjs: 'use strict'; usage `node tests/helpers/b1-358-child.cjs <mode> <roomDir> <sessionId> [query]`; it inherits HOME / MINDRIAN_HOME / MINDRIAN_ROOMS_HOME from the parent env and deletes MINDRIAN_MCP_FIRST and CLAUDE_ACTIVE_ROOM before requiring lib modules; registers core tools on a stub server whose `server.getClientVersion` returns `{ name: 'claude-ai', version: '0.1.0' }` with ctx `{ fallbackRoomDir: roomDir, pluginRoot: <repo root>, surface: 'desktop' }`, and calls handlers with `{ sessionId }`. Modes: `write` (claim_write of 'The bridge at grid 42 is passable.' with source_segment 'b1-358-bridge', then claim_verify on the returned node_id with against_id 'field note, exercise 02', against_kind observation, rung 3, method compare, result contradicts); `refile` (the identical claim_write again in the same session); `file-again` (claim_write of the same sentence, same source_segment, in whatever session is given); `read` (claim_read with { query }). Each mode prints exactly one JSON line to stdout (`{ mode, ok, node_id?, payload? }`) and exits 0 on ok, 1 otherwise. It never requires another tests/ file.
    Create tests/test-358-b1-persistence.cjs (repo test convention): one temp room (openRoomDb/closeRoomDb), one temp HOME/MINDRIAN_HOME/MINDRIAN_ROOMS_HOME shared by all children, children run with `child_process.spawnSync(process.execPath, [helper, mode, room, session, ...], { env, encoding: 'utf8' })`. Sequence and assertions:
    1. `write` in session 'S1' exits 0; remember node_id A.
    2. `refile` in session 'S1' (a separate process) exits 0 and returns the same node_id A.
    3. `read` in session 'S2' with query 'bridge' (a brand-new process and session, nothing held in memory): exactly one claim listed; payload.claim.claim_id is A; checking_record.status 'disputed'; its single record has against_id 'field note, exercise 02', against_kind 'observation', rung 3, rung_label equal to navigation.VERIFICATION_RUNGS[2].label, method 'compare', result 'contradicts', checked_by 'user', a checked_at that parses as a date, and a checked_by_id; confirmation.review_status 'proposed'; rendered.claim contains 'Confirmation status: proposed' and 'rung 3 ('; portrait.claims_disputed is 1.
    4. In the test process after the children exit, open the room through navigation.openRoomDbForCaller and read the same record straight from nodes.properties (the record is on disk, not in any process).
    5. `file-again` in session 'S2' returns a node_id B different from A (Pitfall 3: a new session re-filing mints a new claim); then `read` in session 'S3' with query 'bridge' lists 2 claims, A with checking_status 'disputed' and B with 'unchecked', and attaches no single claim view (two matches), so the duplicate is visible and the demo script says "reopen by words, do not re-file".
    Commit: `git add tests/helpers/b1-358-child.cjs tests/test-358-b1-persistence.cjs` then `git commit --only -m "test(358-04): cross-process, cross-session persistence of the checking record" -- tests/helpers/b1-358-child.cjs tests/test-358-b1-persistence.cjs`.
  </action>
  <verify>
    <automated>node tests/test-358-b1-persistence.cjs && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-358-b1-persistence.cjs` exits 0
    - `grep -c "spawnSync" tests/test-358-b1-persistence.cjs` is at least 3 (separate processes for write, refile and read)
    - `grep -c "require(.*tests/" tests/helpers/b1-358-child.cjs` is 0
    - `grep -c "file-again" tests/test-358-b1-persistence.cjs` is at least 1 (the new-session duplicate case is covered) and the SUMMARY names tests/test-358-b1-refile.cjs as the negative control for the re-file step (never revert the fix in the tree to prove it)
    - `grep -c "$(printf '\xe2\x80\x94')" tests/helpers/b1-358-child.cjs tests/test-358-b1-persistence.cjs` prints 0 for both
  </acceptance_criteria>
  <done>"Close everything, reopen in a new session" is proved with separate OS processes and session ids, including a same-session re-file, and the duplicate-in-new-session case is visible rather than silent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client (Desktop, Cowork, CLI) -> claim_verify / claim_read | Tool arguments come from an agent turn; clientInfo.name is unauthenticated |
| MCP tools -> room.db | Only through navigation.cjs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-17 | Tampering | claim_verify arguments | mitigate | zod schema at the boundary (rung integer range from the constant, closed enums, length caps) plus the library's own fail-closed validation |
| T-358-18 | Elevation of privilege | an agent confirms or resolves on its own | mitigate | Neither tool calls confirmNode, promoteNodeStatus or gate code (static test); resolves_dispute refused for checked_by system; description tells Larry to set it only on the person's explicit say-so; review_status byte-identical (A9) |
| T-358-19 | Spoofing | checked_by_id | mitigate | Stamped server-side from resolveByUser(roomDir), not accepted as a tool input (zod strips unknown keys), agent identities coerced to 'navigator' |
| T-358-20 | Information disclosure | claim_read returns room content | accept | Returned only to the local MCP client that is bound to this room, same trust as room_search and graph_query; no Brain call in the file (static test, Canon Part 8) |
| T-358-21 | Denial of service | large claim lists | mitigate | limit capped at 100, query capped at 200 characters, one SELECT per call |
| T-358-22 | Spoofing | a client claiming to be claude-ai to gain writes | accept | Per T-234-08 and the 2026-09-23 ruling; writes land proposed through navigation validation |
</threat_model>

<verification>
- `node tests/test-358-b1-surfaces.cjs && node tests/test-358-b1-persistence.cjs && node tests/test-234-tool-description-floor.cjs`
- `node scripts/check-tool-honesty.cjs --check`
- `node scripts/check-cirs-declaration.cjs --check .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-04-PLAN.md`
- Expected and accepted until 358-05: `node scripts/build-connector-registry.cjs --check` reports drift naming mcp:claim_read only; test-270 budget and test-276 frozen sweep still red. Record the exact drift line in the SUMMARY.
</verification>

<success_criteria>
- B1-02 (Desktop half): claim_write + claim_verify work as claude-ai over real stdio with no env override
- B1-03: record survives separate processes, a re-file, and a new session
- B1-04 / B1-05: claim_read shows confirmation and checking record side by side plus the four counts, never a score
- B1-06: no MCP write changes review_status
- B1-07: description floor green; honesty rows OK; claim_read born wired in-file
</success_criteria>

<output>
Create `.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-04-SUMMARY.md` when done (stage it with `git add -f`).
</output>
