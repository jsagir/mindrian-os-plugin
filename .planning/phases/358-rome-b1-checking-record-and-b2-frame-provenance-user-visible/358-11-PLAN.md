---
phase: 358-rome-b1-checking-record-and-b2-frame-provenance-user-visible
plan: 11
type: execute
wave: 5
depends_on: ["358-10"]
files_modified:
  - docs/2026-10-06-ROME-B2-GO-NO-GO.md
autonomous: false
requirements: [B2-10]
canon_parts: [8, 11, 12]
tags: [b2, go-no-go, runbook, release, theo, desktop, cowork, rome, checkpoint]

must_haves:
  truths:
    - "An operator can execute docs/2026-10-06-ROME-B2-GO-NO-GO.md on the real demo machines without a developer: pre-flight, release cut, live install check, the Theo command_neighborhood check, Desktop and Cowork client-name probes, the relevance probe, AT1 on CLI, Desktop and Cowork, AT2, AT3 parts a, b and c, AT4, and a PASS / FAIL table per test and surface"
    - "The runbook quotes the four published B2 acceptance tests and the go / no-go rule verbatim, applies navigator ruling 1 exactly (AT3 passes when (a) the change door refuses without account or relocate on CLI and Desktop, (b) every B2 read surface shows the waiting ask first, (c) the demo script routes every question change through the door), and uses the ruled sentence verbatim: 'When the governing question changes, the room asks what the old question got wrong before it records the new question, and keeps both questions on the record.'"
    - "The runbook lists every answer path honestly as COVERED or NOT COVERED on 6 October, including 'Larry answering in chat without recording the question' as NOT COVERED, and the demo-script rule plus the facilitator line that route every question change through the door"
    - "The runbook includes the Theo check (navigator requirement 2026-09-23 item 2): after the release and the theo-resync, command_neighborhood for /mos:room reports the released command-registry version and the registryHash 358-10 recorded; before that, an older mapping is the expected state and it is recorded as such, never as a pass"
    - "The runbook logs the later reconciliations: Phase 345 goal.parent_question untouched (ruling 4), the chain_run pending-change halt not built (ruling 6), no runtime per-turn router, larry-personality not touched (peer-owned), and the F.0 versus F.1 card-shape decision for the navigator"
    - "Every expected-output block was captured from a real hermetic dry run of the shipped B2 code in this plan, not typed from memory"
    - "A human checkpoint confirms the runbook is executable, records observed Desktop and Cowork client names and the relevance probe if run, and records the navigator's F.0 / F.1 decision"
  artifacts:
    - path: "docs/2026-10-06-ROME-B2-GO-NO-GO.md"
      provides: "6 October B2 go/no-go operator runbook"
      contains: "before it records the new question"
      min_lines: 200
  key_links:
    - from: "docs/2026-10-06-ROME-B2-GO-NO-GO.md"
      to: "question_read host block"
      via: "client-name probe step"
      pattern: "client_name"
    - from: "docs/2026-10-06-ROME-B2-GO-NO-GO.md"
      to: "Theo command_neighborhood('/mos:room')"
      via: "post-release, post-resync check"
      pattern: "command_neighborhood"
    - from: "docs/2026-10-06-ROME-B2-GO-NO-GO.md"
      to: "scripts/release.sh"
      via: "release cut step (Step 5.6 fires theo-resync)"
      pattern: "scripts/release\\.sh"
---

<objective>
Produce the 6 October 2026 GO / NO-GO checklist for B2 (slide A1: "which direction" and "can I turn around"): an operator runbook for the real CLI, Claude Desktop and Cowork demo machines that walks the four published acceptance tests under navigator ruling 1, the release cut, the live install check, the Theo command-layer check, the client-name probes and the relevance probe, with an honest covered / not-covered table of answer paths and expected outputs captured from a real dry run. Then stop at a human checkpoint.

Purpose: a green main is not a laptop that runs it (memory rule "not live until released"), Theo only learns the new subcommands after the release and resync, and AT3 is true only in the ruled sense. This document is what the navigator executes on 6 October to decide whether each A1 move goes on the deck. It is a separate file from the B1 checklist (docs/2026-10-06-ROME-B1-GO-NO-GO.md, owned by 358-06) so neither plan edits the other's file.

Output: docs/2026-10-06-ROME-B2-GO-NO-GO.md (tracked, so it travels to the demo machines), then a blocking human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-CONTEXT.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-B2-RESEARCH.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-08-SUMMARY.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-09-SUMMARY.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-10-SUMMARY.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-06-PLAN.md
@.claude/includes/release-process.md
@docs/THEO-NOTIFY-CONTRACT.md

Parallel-session rules: record `PLAN_BASE=$(git rev-parse HEAD)`; `git status --short -- docs/2026-10-06-ROME-B2-GO-NO-GO.md` must be empty before writing; stage the one path and commit with `git commit --only`; never `git add -A`, `git commit -a`, `git stash` or `--no-verify`. Never touch docs/2026-10-06-ROME-B1-GO-NO-GO.md (358-06), docs/reviews/*, docs/OPEN-HANDOFFS.md, docs/FRAME-PROVENANCE-PRODUCT-WORKUP.md (peer-untracked, read only), CLAUDE.md, .planning/STATE.md. This plan does NOT cut the release: releasing is the navigator's call, executed from the runbook.

Writing rules for the runbook: hyphens only, never an em-dash; Feynman-plain sentences; no personal names anywhere (say "the paper author", "the navigator", "the operator", "the facilitator", "an officer"; the published page names people, do not copy those names); call it "governing question", never "governing thought"; never present either question version as better.

Source text to quote VERBATIM (from https://mindrian-explainer-gate.vercel.app/nato.html as shown to the paper author, and from 358-CONTEXT.md b2_rulings):
- Published B2 tests (slide A1): 1. "The room shows where its current question came from: chosen, tasking, prompt or inherited." 2. "Changing the question keeps the old one visible, as a history an officer can open." 3. "Before an answer to a changed question appears, the officer is asked what the old question got wrong." 4. "With an answer, the change is filed as a refinement. Without one, it is filed as a relocation. Neither is presented as better."
- Published split rule: "If B2 passes tests 1 and 2 but not the pause (3 and 4), "which direction" goes up and "can I turn around" stays out. Each move is judged on its own."
- Published go / no-go rule: on 6 October each one "either passes its acceptance test below or stays off the slides. No partial credit, and no "almost"."
- Published "Today" lines (what the slide says if a move stays off): "Which direction?" Today: "the room stores the governing question, but not where it came from, and not its history." "Can I turn around?" Today: "nothing asks you anything when the question changes."
- Published Practice 5 fallback: "Pairs change their question once. The room asks what the old one got wrong, or the facilitator does if the pause is not in."
- Navigator ruling 1 (LOCKED): the slide / fallback sentence is now "When the governing question changes, the room asks what the old question got wrong before it records the new question, and keeps both questions on the record." AT3 passes when (a) the change door refuses without account / relocate on CLI and Desktop, (b) every B2 read surface shows the pending ask first, (c) the demo script routes every question change through the door. The published Layer 2 sentence still says "before it shows a new answer"; updating that page is a navigator action listed in the runbook, not a repo change.

Facts the runbook relies on: B1's runbook facts in 358-06-PLAN.md context (versioned plugin cache ~/.claude/plugins/cache/mindrian-marketplace/mos/<version>/; claude_desktop_config.json paths for macOS, Linux, Windows; Cowork MCP endpoint http://127.0.0.1:3847/mcp; `node lib/core/repo-version.cjs`; releases only through `scripts/release.sh <version>`, with docs/RELEASE-CEREMONY-RULING-SYSTEM.md RULE 5 the single home of the lockstep count); release.sh Step 5.6 fires the theo-resync repository_dispatch (docs/THEO-NOTIFY-CONTRACT.md); the command_neighborhood read and the recorded registryHash and pre-release reading come from 358-10-SUMMARY.md; the card is F.1 (the navigator asked for F.0; the F.0 renderer is closed-vocab Approve / Reject / Defer with an ASCII border and writes the Reject reason into a graph edge, which would break locked AT4).
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| Hardened `lib/core/navigation/typed-frame.cjs`; new `lib/core/frame-provenance.cjs` (the one door, readers, renders, the Decision Gate card); `lib/core/navigation.cjs` re-exports; regenerated `data/render-coverage-registry.json`; core, render and Part 8 tests; updated `tests/test-b2-frame-provenance.cjs`; `tests/run-all-358.sh` B2 legs | 07 |
| Routing spec in `lib/core/frame-provenance.cjs`; `scripts/room-question.cjs`; `/mos:room question` subcommands, routing section and tightened description; regenerated `skills/room/SKILL.md`; routing and CLI tests | 08 |
| `lib/mcp/tools/question.cjs` (question_read, question_set, connectors); `hostBlock` export on `lib/mcp/tools/claim-verify.cjs`; surfaces and persistence tests plus the child helper | 09 |
| `/mos:room` teaching line for Theo's command layer; regenerated registries, mirror and manifest; test-270 re-baseline; 276 re-freeze; recorded command-registry sha256 | 10 |
| `docs/2026-10-06-ROME-B2-GO-NO-GO.md` (operator runbook) + human checkpoint | 11 (this plan) |

<tasks>

<task type="auto">
  <name>Task 1: Dry-run the shipped B2 flow and write the 6 October B2 go / no-go runbook</name>
  <files>docs/2026-10-06-ROME-B2-GO-NO-GO.md</files>
  <read_first>
    - .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-CONTEXT.md (B2 acceptance tests and the b2_rulings block)
    - .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-B2-RESEARCH.md ("Answer producers: covered vs NOT covered on 6 October" table, Assumptions A1-A6, Pitfall 10)
    - .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-10-SUMMARY.md (REGEN_SHA, the command-registry sha256, the pre-release command_neighborhood reading, the runner and doctor summaries)
    - commands/room.md (the routing section and the question subcommand as landed by 358-08)
    - lib/mcp/tools/question.cjs (descriptions, inputs, the write_path_disabled hint, the refusal payload, the host block)
    - scripts/room-question.cjs (subcommands, flags, text output)
    - tests/helpers/b2-358-child.cjs (the one-call child used for the MCP dry run)
    - .planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-06-PLAN.md (the B1 runbook's release, install and probe sections: reuse their facts and wording, write a separate B2 file)
    - skills/setup/SKILL.md (lines 55-85: the Desktop config snippet and the Cowork URL)
    - scripts/release-lib/theo-stamp-gate.sh (lines 15-35, 90-100: the command_neighborhood read)
  </read_first>
  <action>
    1. Hermetic dry run in the scratchpad, never a real room: fresh HOME, MINDRIAN_HOME, MINDRIAN_ROOMS_HOME, a temp room created with node -e over lib/core/room-db.cjs openRoomDb/closeRoomDb. Capture verbatim: `node scripts/room-question.cjs show --room <room>` (empty), `set --room <room> --origin tasking Which camera solves the delay?`, `show`, a refused `set --room <room> --origin chosen --based-on 1 Which camera works with sunglasses?` (stderr block and the stdout card), `show` (waiting change first), `set ... --account "It assumed the delay was the camera, but the officers wear sunglasses." ...` (refines), `set --room <room> --origin prompt --relocate Should the checkpoint move north?` (relocates), `history`, `origins`. Then the MCP side through `node tests/helpers/b2-358-child.cjs question_read <room> S9`: the rendered.question, rendered.history and host fields, plus one refused question_set payload's ask, card.options, next and host. Replace temp paths with `<room>`.
    2. Write docs/2026-10-06-ROME-B2-GO-NO-GO.md with these sections, in order, each step numbered, each with an "Expected" line or block and a PASS / FAIL box:
       - Header: title, decision date (6 October 2026), what is decided (slide A1, two moves: "which direction" = tests 1 and 2; "can I turn around" = tests 3 and 4), the published go / no-go rule verbatim, and one line that B1 (slide A2) has its own checklist, docs/2026-10-06-ROME-B1-GO-NO-GO.md.
       - `## 0. The four tests and the ruling`: the four published tests verbatim; the published split rule verbatim; navigator ruling 1 with its three AT3 pass conditions (a), (b), (c) exactly; the ruled sentence verbatim; a map from each test to the sections that test it.
       - `## 1. Dev-machine pre-flight`: `cd ~/dev/MindrianOS-Plugin`, `git fetch origin main`, `git log origin/main --oneline | grep -cE "358-(07|08|09|10)"` non-zero, `bash tests/run-all-358.sh` exits 0, `node scripts/doctor.cjs --acceptance` summary recorded, `node lib/core/repo-version.cjs` recorded.
       - `## 2. Release cut`: the navigator runs `scripts/release.sh <next version>` (never bump by hand; RULE 5 of docs/RELEASE-CEREMONY-RULING-SYSTEM.md is the single lockstep reference, no count here); if B1 and B2 are decided the same day, one release covers both; note that Step 5.6 fires the theo-resync repository_dispatch; record the released version.
       - `## 3. Live install check on each demo machine`: `/plugin marketplace update`, `claude plugin update mos@mindrian-marketplace`, confirm the versioned cache directory for the released version, re-point Desktop's claude_desktop_config.json mindrian-os `args` at `<that version directory>/bin/mindrian-mcp-server.cjs` (paths for macOS, Linux, Windows), fully quit and reopen Desktop; Cowork: confirm http://127.0.0.1:3847/mcp and restart on the released install; one plain sentence why (a commit on main is not live until released and picked up).
       - `## 4. Theo knows the new subcommands`: after the release AND the theo-resync, run the read-only one-liner from 358-10 (callTool('command_neighborhood', {command:'/mos:room'}) through lib/core/brain-client.cjs; it sends only the command name). Expected: mappedBy names the released version and registryHash equals `git show v<version>:data/command-registry.json | sha256sum`; the pre-release reading from 358-10-SUMMARY.md is quoted for comparison. If mappedBy is still older, Theo has not resynced yet: record it, check Theo's health and the resync, and do not mark the row PASS; this row is release completeness, not a slide criterion.
       - `## 5. Client-name probes`: Desktop then Cowork, in a new conversation bound to the demo room, ask "What is our governing question?" (Larry calls question_read); read the host block. Expected Desktop: client_name claude-ai, host claude-desktop, write_path_enabled true. Cowork: record client_name exactly. If write_path_enabled is false: the documented demo-machine fallback `"MINDRIAN_MCP_FIRST": "desktop,cowork"` in the server env, restart, re-probe; file the observed client name for a /gsd-quick that adds it to HOST_TIER_MAP.tier0 in lib/mcp/surface-detect.cjs, then re-cut and repeat section 3. The env fallback is a demo-machine setting, removed after Rome.
       - `## 6. Relevance probe (does Larry reach for the question door only when he should)`: on the CLI and on Desktop, say each turn in a fresh conversation bound to the demo room: question turns "Our question is now: which camera works with sunglasses?", "What is our governing question?", "Where did our question come from?" (Expected: Larry uses /mos:room question or question_read / question_set; the first one ends at the card, nothing recorded yet); unrelated turns "What is a camera?", "Check the bridge claim against the field note, exercise 02", "Any questions before we file?" (Expected: no question_read or question_set call, no /mos:room question). The question-turn rows count toward AT1 and AT3 (c) on that surface; an unrelated-turn FAIL is a routing bug to fix before Rome (the /mos:room routing section and the MCP descriptions), not a slide NO-GO by itself.
       - `## 7. AT1 (where the question came from)`: CLI `/mos:room question set Which camera solves the delay?` -> origin card -> tasking; Expected: the captured "Recorded: version 1 (first question)" and the captured show block with the Origin line. Desktop and Cowork: "Record our governing question: which camera solves the delay? It came from a tasking." then "What is our governing question and where did it come from?"; Expected: the captured question_read render with the Origin line.
       - `## 8. AT2 (the old question stays visible)`: change the question with an account, then close everything (quit Desktop from its menu, end the Cowork session, exit the CLI), open a NEW session, `/mos:room question history` or "Show me the question history"; Expected: the captured history block, every version with origin and time.
       - `## 9. AT3 (asked before the new question is recorded)`: part (a) CLI `/mos:room question set Which camera works with sunglasses?` -> the card "What did the old question get wrong?" appears and `/mos:room question` shows the waiting change; Desktop "Change our governing question to which camera works with sunglasses" -> question_set refuses and Larry asks; Expected: the captured refusal (ask, card options, next) and NOTHING recorded until the officer answers. Part (b) close the session with the ask unanswered, open a new one, `/mos:room question` or "What is our governing question?"; Expected: first line "A question change is waiting." (captured). Part (c) the demo script rule: every question change in the demo goes through one of two doors, `/mos:room question set <new question>` on the CLI, or "Change our governing question to: <new question>" to Larry on Desktop / Cowork; facilitator line when Larry starts working on a new question without recording it: "Record that as our governing question first."
       - `## 10. AT4 (refines or relocates, neither ranked)`: answer the card in the officer's own words -> "Recorded: version 2 (refines version 1)" and the echoed account line (captured); then change again and pick "It is a new question" -> "Recorded: version 3 (relocates from version 2)" (captured); open the history: every version has the same layout, no option on the card was marked recommended, and no line ranks one version above another.
       - `## 11. Answer paths on 6 October: covered and NOT covered`: a table with columns Path, Surface, Coverage, Why. COVERED rows: question_set (Desktop, Cowork, CLI via MCP); /mos:room question set (CLI); question_read and /mos:room question and question history (all surfaces). NOT COVERED rows, each with the research reason: "Larry answering in chat without recording the question" (all surfaces; no hook on Desktop or Cowork, nothing sees a question never recorded; mitigated only by the section 9 (c) demo rule); MCP instructions standing order (Desktop, Cowork; 2000-byte budget, Phase 359 territory); claim_read (B1 read tool; shows no waiting question notice); suggest_next, reach_candidates, framework_run, whitespace_scan (Phase 355 territory; suggest_next runs after the answer); chain_resolve and chain_run (stretch per ruling 6, not built); gate_render and gate_answer (peer territory); status_read (Phase 354 territory); context_assemble, graph_reason, room_state_bound (not in the answer loop); the next-move engine (reads the section summary, not the question); UserPromptSubmit hooks and the session-start banner (Phase 360 territory; no runtime per-turn router exists). N/A rows: MINTO regeneration and the brain-derivation queue; the strategy goal card (Phase 345).
       - `## 12. Decision table`: rows AT1-CLI, AT1-Desktop, AT1-Cowork, AT2, AT3a-CLI, AT3a-Desktop, AT3b, AT3c, AT4, Relevance-CLI, Relevance-Desktop, Theo (release completeness), one column per demo machine, PASS / FAIL, operator initials and time. Rules under it: "which direction" goes on slide A1 only if AT1 (every surface in Rome) and AT2 pass; "can I turn around" goes on only if AT3 (a, b and c) and AT4 pass; each move is judged on its own, no partial credit; if "can I turn around" is off, the slide keeps the published "Today" line for it and Practice 5 uses the printed fallback card (quote both verbatim); if "which direction" is off, same with its "Today" line.
       - `## 13. Slide wording`: the ruled sentence verbatim, and the navigator action: update the published Layer 2 sentence from "before it shows a new answer" to the ruled wording before the deck is final.
       - `## 14. Origin labels are provisional`: the four labels live in FRAME_ORIGINS_ORDERED in lib/core/navigation/typed-frame.cjs, marked TODO(358); when the paper author's definitions arrive, the swap is that one constant plus its test, then re-release and re-run sections 1-10.
       - `## 15. Known limits, stated honestly`: Larry chatting about a new question without recording it is not caught; on MCP the account is whatever Larry passes (it is echoed back so the officer can read and correct it; the tool cannot know who typed); the Cowork client name may still be unconfirmed; routing is by description (no runtime per-turn router); Theo knows the subcommands only after release plus resync; the card is F.1, not the requested F.0 (reason in one sentence).
       - `## 16. Later reconciliation (handoff)`: Phase 345 goal.parent_question was left untouched (ruling 4); the strategy card's taxonomy climb (lib/core/strategy/strategy-card.cjs:303) still reads that always-null slot and a later phase should make it read the B2 governing question as the single source; the chain_run pending-change halt (ruling 6, stretch) was not built; a runtime per-turn router (scripts/intent-classifier.cjs or the next-move engine) is a follow-up; a larry-personality trigger line waits for its owning plans (355-23, 357-10, 359-09, 359-11) to land; claim_read shows no waiting-question notice; the F.0 / F.1 card-shape decision is the navigator's (section 15).
    3. Paste the captured outputs into the Expected blocks. Run the checks below before committing.
    Commit: `git add docs/2026-10-06-ROME-B2-GO-NO-GO.md` then `git commit --only -m "docs(358-11): B2 go/no-go runbook for 6 October (frame provenance, slide A1)" -- docs/2026-10-06-ROME-B2-GO-NO-GO.md`. Then `bash tests/run-all-358.sh` still exits 0 (the doc is inside the runner's em-dash guard).
  </action>
  <verify>
    <automated>f=docs/2026-10-06-ROME-B2-GO-NO-GO.md; test -f $f && grep -q "before it records the new question, and keeps both questions on the record" $f && grep -q "The room shows where its current question came from: chosen, tasking, prompt or inherited." $f && grep -q "No partial credit" $f && grep -q "Larry answering in chat without recording the question" $f && grep -q "command_neighborhood" $f && grep -q "scripts/release.sh" $f && grep -q "client_name" $f && grep -q "MINDRIAN_MCP_FIRST" $f && grep -q "goal.parent_question" $f && test "$(grep -c "$(printf '\xe2\x80\x94')" $f)" = 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/2026-10-06-ROME-B2-GO-NO-GO.md exists with at least 200 lines and sections 0 through 16 in order (`grep -cE "^## ([0-9]|1[0-6])\. " docs/2026-10-06-ROME-B2-GO-NO-GO.md` is 17)
    - The four published tests appear verbatim (each of "The room shows where its current question came from: chosen, tasking, prompt or inherited.", "Changing the question keeps the old one visible, as a history an officer can open.", "Before an answer to a changed question appears, the officer is asked what the old question got wrong.", "With an answer, the change is filed as a refinement. Without one, it is filed as a relocation. Neither is presented as better." greps at least 1)
    - The ruled sentence appears verbatim: `grep -c "When the governing question changes, the room asks what the old question got wrong before it records the new question, and keeps both questions on the record." <file>` is at least 1
    - `grep -c "NOT COVERED" <file>` is at least 8, and the line containing "Larry answering in chat without recording the question" also contains "NOT COVERED"
    - `grep -c "command_neighborhood" <file>` is at least 1, `grep -c "theo-resync" <file>` is at least 1, `grep -c "claude plugin update mos@mindrian-marketplace" <file>` is at least 1
    - Captured output present: `grep -c "A question change is waiting." <file>` is at least 2, `grep -c "Governing question (version" <file>` is at least 2, `grep -c "Question history (" <file>` is at least 1, `grep -c "\[AskUserQuestion contract: shape=F.1 verbs=4\]" <file>` is at least 1
    - `grep -c "TODO(358)" <file>` is at least 1 and `grep -c "goal.parent_question" <file>` is at least 1
    - `grep -ciE "lawrence|aronhime|jonathan|sagir" <file>` is 0, `grep -ci "governing thought" <file>` counts only the known-limits / routing explanation lines (at most 2), and `grep -c "$(printf '\xe2\x80\x94')" <file>` is 0
    - `git show --stat HEAD --format=%s` lists only docs/2026-10-06-ROME-B2-GO-NO-GO.md
  </acceptance_criteria>
  <done>A tracked, executable runbook with real captured outputs decides both A1 moves on 6 October and says plainly what is and is not covered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Navigator confirms the B2 runbook, runs the live probes if possible, and rules on the card shape</name>
  <files>docs/2026-10-06-ROME-B2-GO-NO-GO.md (read only)</files>
  <read_first>
    - docs/2026-10-06-ROME-B2-GO-NO-GO.md (Task 1 output)
  </read_first>
  <action>Present the checkpoint below and wait. Do not cut a release, edit the host list, change the card shape, or change the runbook until the navigator answers; record the answer, any observed client names, the relevance probe results and the card-shape ruling in the SUMMARY.</action>
  <what-built>B2 on main (plans 358-07 to 358-10): one room-level governing question record with one write door; required origin from one TODO(358) constant; immutable version history; the pause as a refusal plus a waiting ask shown first on every B2 read, fired as one Decision Gate card through the SEED-020 door; refines with an account artifact, relocates without one; /mos:room question, history, set and cancel on the CLI with a Larry routing section; question_read and question_set MCP tools for Desktop and Cowork; every registry regenerated, the /mos:room teaching line ready for Theo; a green phase runner. Plus docs/2026-10-06-ROME-B2-GO-NO-GO.md with outputs captured from a real dry run.</what-built>
  <how-to-verify>
    1. Read docs/2026-10-06-ROME-B2-GO-NO-GO.md end to end and confirm every step can be run on the Rome demo machines as written (paths, commands, prompts, expected outputs), and that the covered / not-covered table is honest.
    2. Card shape ruling: you asked for F.0; the plan set ships F.1 because the F.0 renderer only shows Approve / Reject / Defer inside an ASCII border and writes the Reject reason into a graph edge, which would put the officer's account in graph metadata against locked AT4. Reply "F.1 ok" or "F.0 anyway" (the latter becomes a follow-up task with that trade-off).
    3. Recommended now (required by 6 October): on a real Claude Desktop whose mindrian-os entry points at this build (a dev checkout path to bin/mindrian-mcp-server.cjs is fine for the probe), bind a scratch room and ask "What is our governing question?". Expected host block: client_name claude-ai, host claude-desktop, write_path_enabled true.
    4. Same on Cowork: record client_name exactly.
    5. Optional: run runbook section 6 (relevance probe) and section 9 (AT3) on Desktop in the scratch room.
  </how-to-verify>
  <acceptance_criteria>
    - The navigator replied "approved" (or listed issues that were then fixed in the runbook and re-approved)
    - The SUMMARY records the card-shape ruling ("F.1 ok" or "F.0 anyway")
    - The SUMMARY records whether the Desktop and Cowork probes and the relevance probe were run, with the exact client_name values observed (or "not yet run, due before 6 October")
  </acceptance_criteria>
  <resume-signal>Type "approved" plus "F.1 ok" or "F.0 anyway", and the observed client names (Desktop / Cowork) if you ran the probes, or describe what needs to change in the runbook.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev repo -> demo machines | code reaches Rome only through a release and a plugin update |
| plugin release -> Theo | Theo learns the new subcommands only through the theo-resync after a release |
| demo machine config -> MCP server | the Desktop config path and env block decide which code runs and whether writes are allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-53 | Tampering | demo runs stale code | mitigate | Sections 2-3: release cut, versioned cache check, Desktop args re-point, full restart, then probes before any acceptance test |
| T-358-54 | Repudiation | a GO decided on "almost" or on an overclaimed AT3 | mitigate | Decision table per test and surface with initials; published rule quoted verbatim; ruling 1 conditions (a), (b), (c) spelled out; ruled sentence used verbatim; covered / not-covered table |
| T-358-55 | Repudiation | Theo treated as aware before the resync | mitigate | Section 4 compares mappedBy and registryHash against the recorded values; an older mapping is recorded as not yet live, never PASS |
| T-358-56 | Elevation of privilege | MINDRIAN_MCP_FIRST fallback widens writes on a demo machine | accept | Demo-machine only, documented, removed after Rome; question_set still refuses a changed question without the officer's answer |
| T-358-57 | Information disclosure | personal names in a tracked doc | mitigate | Roles only; the acceptance grep bans the names that appear on the published page |
</threat_model>

<verification>
- The Task 1 verify command prints OK
- `bash tests/run-all-358.sh` still exits 0 (the doc is inside the runner's em-dash guard)
- The checkpoint reply, the card-shape ruling and any probe results are recorded in the SUMMARY
</verification>

<success_criteria>
- B2-10 (runbook part): a live, executable go / no-go runbook for CLI, Desktop and Cowork with the release cut, the install check, the Theo check, the client-name probes, the relevance probe and an honest covered / not-covered answer-path table
- Ruling 1 applied verbatim; rulings 4 and 6 logged as later reconciliation; navigator requirement item 2 verified by a post-release Theo step
- The navigator has approved the runbook and ruled on the card shape
</success_criteria>

<output>
Create `.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-11-SUMMARY.md` when done (stage it with `git add -f` and commit it with `git commit --only`).
</output>
