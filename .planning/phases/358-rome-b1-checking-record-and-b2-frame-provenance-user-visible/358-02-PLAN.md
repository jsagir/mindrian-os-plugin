---
phase: 358-rome-b1-checking-record-and-b2-frame-provenance-user-visible
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/run-all-358.sh
  - tests/test-358-b1-refile.cjs
  - lib/core/navigation/typed-claim.cjs
  - lib/mcp/surface-detect.cjs
  - tests/test-234-host-tier.cjs
autonomous: true
requirements: [B1-02, B1-03, B1-07]
canon_parts: [8, 9]
tags: [b1, refile, carry-forward, host-tier, desktop, rome, tdd]

must_haves:
  truths:
    - "Re-filing a claim keeps its checking record byte-for-byte: via claim_write, via a graph-derivation style writeClaimNode re-save (knowledge_type, text, sessionId, sourceSegment only), and via an extraProps re-projection (pipeline, run_id) - the writeClaimNode verification-wipe bug is fixed (navigator ruling 2026-09-23, blocks AT2)"
    - "An extraProps bag can never write, replace or forge `verification`: PROTECTED_CLAIM_KEYS includes 'verification' and still includes valid_from and valid_until"
    - "A claim that never had a checking record re-files with no `verification` key, so every other writer's props shape is unchanged"
    - "A re-file never changes review_status: a confirmed claim re-filed stays confirmed and keeps its record"
    - "Claude Desktop (clientInfo.name `claude-ai`) resolves to host `claude-desktop`, tier0, and isWritePathEnabled returns true with MINDRIAN_MCP_FIRST unset (navigator ruling 2026-09-23: Desktop writes YES, same pattern as commit 5f0a55993); look-alike names such as `claude-ai-beta` stay host unknown and refused; Cowork is not added"
    - "tests/run-all-358.sh exists, names every planned B1 test file up front, runs the existing MCP-surface tests and the CIRS gates plus an em-dash guard, exits 1 on any failure, 77 when any leg was skipped, and 0 only when every leg passed"
  artifacts:
    - path: "tests/run-all-358.sh"
      provides: "Phase 358 B1 aggregator (run/run_if legs, gates, em-dash guard, strict exit)"
      contains: "run_if"
      min_lines: 90
    - path: "tests/test-358-b1-refile.cjs"
      provides: "B1-03 re-file legs: claim_write re-file, graph-derivation style re-save, extraProps re-projection, forgery refused, no-record shape unchanged, confirmed stays confirmed, malformed prior props, transaction hygiene"
      min_lines: 110
    - path: "lib/core/navigation/typed-claim.cjs"
      provides: "CARRY_FORWARD_CLAIM_KEYS carry-forward inside writeClaimNode; 'verification' in PROTECTED_CLAIM_KEYS"
      contains: "CARRY_FORWARD_CLAIM_KEYS"
    - path: "lib/mcp/surface-detect.cjs"
      provides: "claude-desktop tier0 host entry"
      contains: "claude-desktop"
    - path: "tests/test-234-host-tier.cjs"
      provides: "claude-ai recognition and write-gate assertions"
      contains: "claude-ai"
  key_links:
    - from: "lib/core/navigation/typed-claim.cjs writeClaimNode"
      to: "nodes.properties.verification of the prior row"
      via: "prior-properties SELECT before insertNode, inside the owned transaction"
      pattern: "CARRY_FORWARD_CLAIM_KEYS"
    - from: "lib/mcp/mcp-first-flag.cjs isWritePathEnabled"
      to: "lib/mcp/surface-detect.cjs HOST_TIER_MAP.tier0"
      via: "detectHostTier returns tier0 for claude-ai"
      pattern: "claude-desktop"
---

<objective>
Fix the two defects the research PROVED would fail Rome acceptance tests on their own, and lay down the phase test runner:
1. Re-filing a claim wipes its checking record (typed-claim.cjs rebuilds props; insertNode's upsert overwrites properties). Fix it in the one claim writer, protect the key from extraProps, and cover claim_write re-files and graph-derivation style re-saves with a regression test (navigator ruling, blocks AT2 "visible months later").
2. Claude Desktop cannot write at all (clientInfo.name `claude-ai` floors to host unknown). Add it to the tier0 host list (navigator ruling: Desktop writes YES; writes still land as proposed through the chokepoint). Cowork is NOT added until a live probe confirms its name; the demo-machine env fallback `MINDRIAN_MCP_FIRST=desktop,cowork` is documented in 358-06, never relied on in code.
3. `tests/run-all-358.sh`, written once here, naming every B1 test file the later plans create.

Purpose: AT1 on Desktop and AT2 are impossible without these two fixes. Output: carry-forward in typed-claim.cjs, the host entry in surface-detect.cjs, two test files, the runner.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-CONTEXT.md
@.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-RESEARCH.md
@lib/core/navigation/typed-claim.cjs
@lib/mcp/surface-detect.cjs

Parallel-session rules (every task): peers execute Phases 354-357 in this same working tree. Record `PLAN_BASE=$(git rev-parse HEAD)` at start. Before editing any file run `git status --short -- <file>` and STOP if it shows a diff you did not make (never revert, stash, checkout or overwrite an unowned diff). Stage explicit paths only (`git add <path>`; `git add -f` for anything under `.planning/`) and commit with `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git add .`, `git commit -a`, `git stash`, or `--no-verify`. Never touch: docs/reviews/*, evals/plurai/*, scripts/eval-icm-writers.cjs, scripts/jev-devtime-client.cjs, tests/test-353-* (run only), .planning/phases/356-*, data/jev-policies/, .planning/STATE.md, lib/mcp/tools/gate.cjs, lib/mcp/tools/views.cjs, lib/mcp/tool-router.cjs, lib/mcp/register-core-tools.cjs, lib/mcp/tools/status.cjs, lib/mcp/mcp-first-flag.cjs (not needed: tier0 hosts are already write-enabled there), lib/core/write-lock.cjs, CLAUDE.md. No em-dashes (hyphens only).

Coordination check for lib/mcp/surface-detect.cjs (collision rule): at planning time (2026-09-23) no 354, 355 or 357 PLAN.md lists lib/mcp/surface-detect.cjs or lib/mcp/mcp-first-flag.cjs in files_modified. Before Task 3 re-run `grep -l "surface-detect\|mcp-first-flag" .planning/phases/354-*/*-PLAN.md .planning/phases/355-*/*-PLAN.md .planning/phases/357-*/*-PLAN.md` (never open 356). If any hit lists either file in its files_modified frontmatter, STOP Task 3 and message the Phase 354 session (jsagi-25) naming the one-line tier0 addition; proceed only after the reply, and record it in the SUMMARY.

Both defects are wave-1 work that runs in parallel with 358-01: this plan must not depend on 358-01's new fields. The refile test uses only the SHIPPED recordClaimVerification fields (claim_id, against_id, against_kind, method, result, checked_by, checked_at) and only portrait keys that exist before and after 358-01 (claims_total, claims_disputed, claims_unchecked, records_total).
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `lib/core/navigation/verification.cjs` (rung constant, record fields, sticky disputed, claim view + list readers, portrait by rung, render helpers), `lib/core/navigation.cjs` re-exports, core/portrait/separation tests, updated `tests/test-b1-verification.cjs` | 01 |
| `lib/core/navigation/typed-claim.cjs` carry-forward + PROTECTED_CLAIM_KEYS, `tests/test-358-b1-refile.cjs`, `lib/mcp/surface-detect.cjs` claude-ai tier0 host, `tests/test-234-host-tier.cjs` additions, `tests/run-all-358.sh` | 02 (this plan) |
| `scripts/claim-checks.cjs`, `/mos:room checks`, `/mos:room claim`, `/mos:room check`, regenerated `skills/room/SKILL.md`, `tests/test-358-b1-cli.cjs` | 03 |
| `claim_verify` (rung required, checked_by_id, resolves_dispute, description >= 120 chars), new `claim_read` MCP tool, surfaces test, child helper, persistence test | 04 |
| Regenerated `data/mcp-tool-connectors.json` + `data/connector-registry.json`, test-270 AFTER re-baseline, 276 ledger re-freeze | 05 |
| `docs/2026-10-06-ROME-B1-GO-NO-GO.md` (operator runbook) + human checkpoint | 06 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase runner + RED re-file regression test</name>
  <files>tests/run-all-358.sh, tests/test-358-b1-refile.cjs</files>
  <read_first>
    - tests/run-all-354.sh (136 lines: the run/run_if/counter shape, the em-dash guard block, the header rule "written ONCE, no later plan edits it")
    - lib/core/navigation/typed-claim.cjs (lines 80-235: PROTECTED_CLAIM_KEYS :89-92, CLAIM_NODE_ID :102-110, writeClaimNode :120-229)
    - lib/core/node-insert.cjs (lines 225-252: `ON CONFLICT(id) DO UPDATE SET type, properties, last_seen_at`)
    - lib/core/graph-derivation.cjs (lines 328-336: the re-save call shape writeClaimNode({knowledge_type, text, sessionId, sourceSegment}))
    - tests/test-276-claim-write-primitive.cjs (lines 36-60 MINDRIAN_MCP_FIRST='all' for this process only; lines 100-175 hermetic room, stub server registerAndCapture, callTool)
    - lib/core/navigation/verification.cjs (recordClaimVerification params and readVerificationPortrait keys, shipped version)
  </read_first>
  <behavior>
    - refile 1: writeClaimNode(sessionId 's358r', sourceSegment 'seg-1') then recordClaimVerification (contradicts) then the identical writeClaimNode again: props.verification deep-equals the value read before the re-file; portrait claims_disputed 1 and records_total 1
    - refile 2 (graph-derivation style): a re-save passing ONLY knowledge_type, text, sessionId and sourceSegment keeps the record
    - refile 3 (extraProps re-projection, domain-insight-sweep / close-loop style): a re-save with extraProps { pipeline: 'p358', run_id: 'r1' } keeps the record AND stores pipeline and run_id
    - refile 4 (forgery): extraProps { verification: { status: 'checked', records: [] } } on a claim holding a contradicts record leaves the stored verification unchanged; the same extraProps on a claim with no record leaves NO verification key; PROTECTED_CLAIM_KEYS includes 'verification', 'valid_from' and 'valid_until'
    - refile 5 (no record): re-filing a claim that never had a record produces props with no verification key
    - refile 6 (claim_write): with MINDRIAN_MCP_FIRST='all' for this process, claim_write (knowledge_type fact, text T, source_segment 'seg-mcp') then a record through navigation, then the identical claim_write again: the record is still there
    - refile 7 (review_status): a re-file keeps review_status 'proposed'; after navigation.confirmNode the re-file keeps 'confirmed' and keeps the record
    - refile 8 (malformed prior): a claim row whose properties are the string 'not json' (set by a raw test-only UPDATE) re-files with ok true
    - refile 9 (transactions): after writeClaimNode returns, db.isTransaction is false; when the caller ran db.exec('BEGIN') first, writeClaimNode succeeds and db.isTransaction is still true until the caller commits
  </behavior>
  <action>
    A) Create `tests/run-all-358.sh` (chmod +x), modeled on tests/run-all-354.sh: `#!/usr/bin/env bash`, `set -uo pipefail`, cd to repo root, PASS/FAIL/SKIP counters, `run` and `run_if` functions copied in shape (exit 0 PASSED, 77 SKIPPED (ENV GAP), anything else FAILED). Header comment: Phase 358 B1 aggregator; written ONCE here in 358-02; no later B1 plan edits it (each leg already names its guard file, so a landed test file is picked up automatically); the B2 plan set adds its own legs deliberately; hyphens only. Legs, in this order:
    1. Guarded phase tests (run_if, guard = the test file): "358: core contract (B1-01/B1-04)" tests/test-358-b1-core.cjs; "358: portrait (B1-05)" tests/test-358-b1-portrait.cjs; "358: separation (B1-06)" tests/test-358-b1-separation.cjs; "358: re-file keeps record (B1-03)" tests/test-358-b1-refile.cjs; "358: CLI surface (B1-02/B1-04/B1-05)" tests/test-358-b1-cli.cjs; "358: MCP surfaces (B1-02/B1-04/B1-05/B1-06)" tests/test-358-b1-surfaces.cjs; "358: new-session persistence (B1-03)" tests/test-358-b1-persistence.cjs.
    2. Existing tests that must stay or turn green (run): tests/test-b1-verification.cjs, tests/test-234-host-tier.cjs, tests/test-234-tool-description-floor.cjs, tests/test-270-tool-schema-budget.cjs, tests/test-270-connector-coverage.cjs, tests/test-276-tool-honesty-findings-closed.cjs, tests/test-276-claim-write-primitive.cjs, tests/test-276-meeting-gate-wiring.cjs, tests/test-348-validity-window.cjs, tests/test-353-filing-gate.cjs (label it "run only, never edited by 358").
    3. Gates (run): `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs --check`, `node scripts/build-render-coverage.cjs --check`, `node scripts/build-skill-mirrors.cjs --check`, `node scripts/check-shape-declaration.cjs --check`, `node scripts/check-help-coverage.cjs`, `node scripts/check-tool-honesty.cjs --check`, and a CIRS leg that collects the existing files matching `.planning/phases/358-*/358-0[1-6]-PLAN.md` into an array and runs `node scripts/check-cirs-declaration.cjs --check "${arr[@]}"` (SKIPPED when the array is empty).
    4. Em-dash guard (always), same loop as 354 over: lib/core/navigation/verification.cjs, lib/core/navigation/typed-claim.cjs, lib/core/navigation.cjs, lib/mcp/tools/claim-verify.cjs, lib/mcp/surface-detect.cjs, scripts/claim-checks.cjs, commands/room.md, skills/room/SKILL.md, docs/2026-10-06-ROME-B1-GO-NO-GO.md, tests/run-all-358.sh, tests/helpers/b1-358-child.cjs, tests/test-234-host-tier.cjs, tests/test-270-tool-schema-budget.cjs, tests/fixtures/tool-honesty/276-dispositions.json, tests/test-b1-verification.cjs, plus a find of tests/test-358-b1-*.cjs; a missing file is skipped, never an error.
    Summary line `PASSED=$PASS FAILED=$FAIL SKIPPED=$SKIP`; exit 1 when FAIL > 0, else 77 when SKIP > 0, else 0 (a phase is only green at exit 0; 77 is never reported as PASSED).
    B) Create `tests/test-358-b1-refile.cjs` in the repo test convention (check counter, `N passed, M failed`, exit code), hermetic setup (fresh `MINDRIAN_ROOMS_HOME` mkdtemp, delete `CLAUDE_ACTIVE_ROOM`, `process.env.MINDRIAN_MCP_FIRST = 'all'` for this process only, exactly as tests/test-276-claim-write-primitive.cjs:36-60 explains), temp rooms via lib/core/room-db.cjs openRoomDb/closeRoomDb then `navigation.openRoomDbForCaller` / `closeRoomDbForCaller`. For refile 6 copy the stub-server pattern (registerCoreTools with a stub `{ tool: (name, desc, schema, handler) => captured.set(name, {handler}) }`, ctx `{ fallbackRoomDir: roomDir, pluginRoot: REPO_ROOT, surface: 'cli' }`, handler called with `{ sessionId: 'test-358-refile' }`), reading node_id from claim_write's JSON response. Implement every behavior above. Run it: refile 1, 2, 3, 4 (first half), 6 and 7 MUST fail now (the wipe is live). Run `bash tests/run-all-358.sh; echo $?` once and record the exit code (expected non-zero at this point).
    Commit: `git add tests/run-all-358.sh tests/test-358-b1-refile.cjs` then `git commit --only -m "test(358-02): phase runner and failing re-file regression test" -- tests/run-all-358.sh tests/test-358-b1-refile.cjs`.
  </action>
  <verify>
    <automated>bash -n tests/run-all-358.sh && test -x tests/run-all-358.sh && (node tests/test-358-b1-refile.cjs >/dev/null 2>&1; test $? -ne 0) && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/run-all-358.sh` exits 0 and the file is executable
    - `grep -c "run_if" tests/run-all-358.sh` is at least 8 (the function plus seven guarded legs)
    - `grep -c "tests/test-358-b1-persistence.cjs" tests/run-all-358.sh` is at least 1 and `grep -c "check-cirs-declaration" tests/run-all-358.sh` is at least 1
    - `grep -c "exit 77\|77" tests/run-all-358.sh` is at least 1 (skips are never PASSED)
    - `node tests/test-358-b1-refile.cjs` exits non-zero before Task 2 (RED proven, exit code in the SUMMARY)
    - `grep -c "$(printf '\xe2\x80\x94')" tests/run-all-358.sh tests/test-358-b1-refile.cjs` prints 0 for both
  </acceptance_criteria>
  <done>The runner exists with every B1 leg named up front, and a failing test reproduces the re-file wipe through claim_write and graph-derivation style re-saves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - carry the checking record forward in writeClaimNode and protect it from extraProps</name>
  <files>lib/core/navigation/typed-claim.cjs</files>
  <read_first>
    - lib/core/navigation/typed-claim.cjs (lines 80-235)
    - lib/core/navigation/ranker-weights.cjs (lines 105-130: transaction ownership idiom)
    - tests/test-358-b1-refile.cjs (Task 1 output)
    - tests/test-348-validity-window.cjs (lines 240-250: asserts PROTECTED_CLAIM_KEYS still contains valid_from and valid_until)
  </read_first>
  <behavior>
    - tests/test-358-b1-refile.cjs passes in full
    - every existing claim-writer regression test still passes
  </behavior>
  <action>
    In lib/core/navigation/typed-claim.cjs (per the navigator ruling "Bug to fix ... carry it forward and add it to PROTECTED_CLAIM_KEYS"):
    1. Append 'verification' to PROTECTED_CLAIM_KEYS (keep every existing entry and order). Extend the Phase 223-02 comment above it by two sentences: the B1 checking record (Phase 358) is owned by lib/core/navigation/verification.cjs, whose recordClaimVerification is its only writer; an extraProps bag can never write, replace or forge it.
    2. Add `const CARRY_FORWARD_CLAIM_KEYS = Object.freeze(['verification']);` with a comment: keys a re-file must carry forward from the existing row because a different writer owns them; an explicit named list, deliberately NOT "preserve every old key", so no other writer's semantics change. Export it next to PROTECTED_CLAIM_KEYS.
    3. In writeClaimNode move the `sid` / `segmentKey` / `nodeId` / `sourcePath` computation ABOVE the props serialization. Then, around the prior read and the insertNode call, use the ownership idiom: `const owns = db.isTransaction !== true;` and when owned try `db.exec('BEGIN IMMEDIATE')`; if that BEGIN throws, continue WITHOUT a transaction (never block a claim write on the lock idiom) and remember that nothing is owned. Inside: read `SELECT properties FROM nodes WHERE id = ?` for nodeId in its own try/catch (any read or JSON.parse failure is ignored: a carry-forward read never blocks the write); when the parsed prior props are a plain object, copy each CARRY_FORWARD_CLAIM_KEYS key whose prior value is a plain object into `props` (this runs AFTER the protected extraProps merge, so the stored record always wins). Then serialize props (the existing properties_serialize_failed path stays, and must ROLLBACK when owned) and call insertNode exactly as today. COMMIT when owned and insertNode succeeded; ROLLBACK when owned and anything threw, returning the existing claim_write_failed shape. Never COMMIT or ROLLBACK a caller-owned transaction. Update the writeClaimNode header comment with one line: a re-file carries CARRY_FORWARD_CLAIM_KEYS forward from the existing row (Phase 358 B1, the verification-wipe fix).
    4. Do not change KNOWLEDGE_TYPES, CLAIM_NODE_ID, the review_status / confidence / epistemic_type options, or any other key's handling. A claim with no prior record must serialize byte-identically to today.
    Commit: `git add lib/core/navigation/typed-claim.cjs` then `git commit --only -m "fix(358-02): writeClaimNode carries the checking record forward on re-file" -- lib/core/navigation/typed-claim.cjs`.
  </action>
  <verify>
    <automated>node tests/test-358-b1-refile.cjs && for t in test-348-validity-window test-276-claim-write-primitive test-223-close-loop test-223-supersedes-chain test-224-proposed-only test-233-derivation-default-gate test-348-supersession-e2e test-abstraction-gate test-347-meeting-fanout-records test-276-meeting-gate-wiring test-353-filing-gate test-b1-verification; do node tests/$t.cjs >/dev/null 2>&1 || { echo "REGRESSION $t"; exit 1; }; done && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-358-b1-refile.cjs` exits 0
    - All twelve regression tests in the verify loop exit 0 (each was green at planning time, 2026-09-23)
    - `node -e "const t=require('./lib/core/navigation/typed-claim.cjs'); if(!t.PROTECTED_CLAIM_KEYS.includes('verification')||!t.PROTECTED_CLAIM_KEYS.includes('valid_from')||!t.CARRY_FORWARD_CLAIM_KEYS||t.CARRY_FORWARD_CLAIM_KEYS.length!==1) process.exit(1)"` exits 0
    - `grep -v '^\s*//' lib/core/navigation/typed-claim.cjs | grep -c "BEGIN IMMEDIATE"` is 1
    - `grep -c "$(printf '\xe2\x80\x94')" lib/core/navigation/typed-claim.cjs` is 0
  </acceptance_criteria>
  <done>A checking record survives every re-file path through the one claim writer, and no extraProps bag can touch it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Recognize Claude Desktop (claude-ai) as a write-enabled tier0 host</name>
  <files>tests/test-234-host-tier.cjs, lib/mcp/surface-detect.cjs</files>
  <read_first>
    - lib/mcp/surface-detect.cjs (lines 110-190: HOST_TIER_MAP and detectHostTier)
    - lib/mcp/mcp-first-flag.cjs (lines 115-143: isWritePathEnabled; a recognized tier0 host returns true)
    - tests/test-234-host-tier.cjs (lines 1-120 header, check() and withFlag(); lines 156-200 Part A3/A4; lines 200-260 the write-gate assertions)
    - `git show 5f0a55993 --stat` (the precedent: quick 260819-bql enabled the Claude Code host for the same reason)
  </read_first>
  <behavior>
    - detectHostTier({ name: 'claude-ai', version: '0.1.0' }) returns { host: 'claude-desktop', hostTier: 'tier0' }; 'Claude-AI' does too
    - 'claude-ai-beta', 'my-claude-ai' and 'claude-aix' return host 'unknown'
    - with MINDRIAN_MCP_FIRST unset: isWritePathEnabled({ surface: 'desktop', clientVersion: { name: 'claude-ai' } }) is true; the same on surface 'cowork' is true; { name: 'claude-ai-beta' } on 'desktop' is false
    - every pre-existing assertion in tests/test-234-host-tier.cjs still passes unchanged
  </behavior>
  <action>
    Run the coordination check from the context block first. Then RED: in tests/test-234-host-tier.cjs add, without editing any existing assertion, (a) a short block after the SEED-068 tier0 names in Part A3 with the detectHostTier assertions above, labeled as the 2026-09-23 navigator ruling (Phase 358 B1: Claude Desktop identifies as clientInfo.name 'claude-ai'); (b) three write-gate assertions next to the existing `flag unset + Cursor` checks, using the existing `withFlag(null, ...)` helper; (c) one header comment paragraph naming the ruling and that Cowork is intentionally not recognized until a live probe confirms its client name. Run it: the new assertions fail. Commit the RED test: `git add tests/test-234-host-tier.cjs` then `git commit --only -m "test(358-02): failing claude-ai host tier assertions" -- tests/test-234-host-tier.cjs`.
    GREEN: in lib/mcp/surface-detect.cjs add `{ host: 'claude-desktop', re: /^claude-ai$/i },` as the FIRST entry of HOST_TIER_MAP.tier0 (anchored so look-alike names stay unknown), with a comment: Claude Desktop sends clientInfo.name 'claude-ai' (MCP discussions #325, medium confidence; the live probe in docs/2026-10-06-ROME-B1-GO-NO-GO.md confirms it on the demo machine); navigator ruling 2026-09-23, Phase 358 B1: Desktop is write-enabled, same pattern as quick 260819-bql (commit 5f0a55993), and writes still land as proposed through navigation.cjs; the name is unauthenticated (T-234-08, accepted); Cowork is NOT listed until a live probe confirms its client name, and until then the demo-machine fallback is MINDRIAN_MCP_FIRST=desktop,cowork. Also add "Claude Desktop" to the tier0 line of the HOST_TIER_MAP doc comment. Change nothing else in the file (detectSurface and CAPABILITY_MAP untouched).
    Commit: `git add lib/mcp/surface-detect.cjs` then `git commit --only -m "feat(358-02): Claude Desktop (claude-ai) is a write-enabled tier0 host" -- lib/mcp/surface-detect.cjs`.
  </action>
  <verify>
    <automated>node tests/test-234-host-tier.cjs 2>&1 | tail -1 | grep -q " 0 failed" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-234-host-tier.cjs` exits 0 and its final line reports 0 failed; the pass count is at least 101 + the new assertions (101 passed at planning time)
    - `node -e "const {detectHostTier}=require('./lib/mcp/surface-detect.cjs'); const a=detectHostTier({name:'claude-ai'}), b=detectHostTier({name:'claude-ai-beta'}); if(a.host!=='claude-desktop'||a.hostTier!=='tier0'||b.host!=='unknown') process.exit(1)"` exits 0
    - `env -u MINDRIAN_MCP_FIRST node -e "const {isWritePathEnabled}=require('./lib/mcp/mcp-first-flag.cjs'); if(!isWritePathEnabled({surface:'desktop',clientVersion:{name:'claude-ai'}})) process.exit(1)"` exits 0
    - `git diff $PLAN_BASE -- lib/mcp/surface-detect.cjs | grep -c '^-[^-]'` is at most 1 (only the tier0 doc-comment line may change; everything else is additive)
    - No Cowork host entry was added: `grep -c "host: 'cowork'" lib/mcp/surface-detect.cjs` is 0 (the existing detectSurface / CAPABILITY_MAP cowork surface is untouched)
    - `grep -c "$(printf '\xe2\x80\x94')" lib/mcp/surface-detect.cjs tests/test-234-host-tier.cjs` prints 0 for both
  </acceptance_criteria>
  <done>Claude Desktop can call claim_write and claim_verify with writes landing as proposed; unknown and look-alike clients stay refused; Cowork waits for its probe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> MCP server | clientInfo.name is self-reported and unauthenticated |
| re-projection writers -> claim node | graph-derivation, domain-insight-sweep, close-loop, unknowns and claim_write all re-file through writeClaimNode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-07 | Spoofing | surface-detect claude-ai entry | accept | Per T-234-08 the host name is a UX capability signal, not auth; writes still pass navigation.cjs validation and land as proposed; only gate_answer approve by a person confirms; anchored regex keeps look-alikes unknown (tested) |
| T-358-08 | Tampering | extraProps forging a checking record | mitigate | 'verification' in PROTECTED_CLAIM_KEYS; carry-forward runs after the merge so the stored record always wins; refile 4 proves both cases |
| T-358-09 | Tampering (integrity) | re-file wipes the record | mitigate | CARRY_FORWARD_CLAIM_KEYS copy inside writeClaimNode; refile 1-3 and 6 cover every re-projection shape |
| T-358-10 | Denial of service | carry-forward read failure blocks claim writes | mitigate | the prior read and JSON.parse are in their own try/catch; a failed BEGIN falls back to no transaction; refile 8 proves malformed prior props still write |
| T-358-11 | Elevation of privilege | widened write gate on Desktop | accept | Navigator ruling 2026-09-23; same governed door as Claude Code since 5f0a55993; Cowork stays refused until probed |
</threat_model>

<verification>
- `node tests/test-358-b1-refile.cjs && node tests/test-234-host-tier.cjs`
- The twelve claim-writer regression tests in Task 2's verify loop all exit 0
- `bash tests/run-all-358.sh; echo "exit=$?"` runs to its summary line (legs owned by later plans may be SKIPPED or red at this point; the SUMMARY records the exit code)
</verification>

<success_criteria>
- B1-03: a checking record survives claim_write re-files and graph-derivation style re-saves
- B1-02: Claude Desktop's write path is open for claim_write and claim_verify
- B1-07: the phase runner exists with every B1 leg and gate named up front
</success_criteria>

<output>
Create `.planning/phases/358-rome-b1-checking-record-and-b2-frame-provenance-user-visible/358-02-SUMMARY.md` when done (stage it with `git add -f`).
</output>
