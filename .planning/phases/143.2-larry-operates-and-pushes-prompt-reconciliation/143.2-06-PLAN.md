---
phase: 143.2-larry-operates-and-pushes-prompt-reconciliation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/mullins-scaffold/scaffold.json
  - skills/mullins-scaffold/SKILL.md
  - lib/core/mullins-scaffold.cjs
  - lib/core/mullins-scaffold.test.cjs
autonomous: true
requirements: [MULL-01, MULL-02]
canon_parts: [Part 2, Part 3, Part 7, Part 8]

must_haves:
  truths:
    - "Beyond the 7 fixed Mullins domains, the scaffold proposes Brain-driven cross-framework folders (Value Proposition, Disruptive Innovation, Systems Thinking) from the frameworks Mullins FEEDS_INTO -- offered at a Decision Gate, never auto-imposed"
    - "The Brain-folder proposal query carries generic framework handles only (framework names + problem-type enums) -- never user artifacts, meetings, decisions, or content (Part 8)"
    - "The scaffold renders the Ackoff bidirectional traversal: the ASCENT fills the domains toward Validation-at-center (wisdom); the DESCENT is Validation decomposing back to re-test each domain's data/assumptions"
  artifacts:
    - path: "skills/mullins-scaffold/scaffold.json"
      provides: "the additive brain_folders[] proposal set (3 cross-framework folders, FEEDS_INTO source labels) + the ackoff_traversal block (ascent + descent), version bumped to 2; the 20 existing sections[] byte-unchanged"
      contains: "brain_folders"
    - path: "lib/core/mullins-scaffold.cjs"
      provides: "additive read-only accessors: listBrainFolders() + getAckoffTraversal() + buildBrainFolderQuery() (the Part-8-clean generic-handle query builder); the 5 existing exports byte-unchanged"
      exports: ["listBrainFolders", "getAckoffTraversal", "buildBrainFolderQuery"]
    - path: "lib/core/mullins-scaffold.test.cjs"
      provides: "the fixture test: the 3 brain_folders present + the FEEDS_INTO source labels + the Part-8 boundary assertion on buildBrainFolderQuery (zero user-content keys) + the ascent/descent traversal present + the 20 existing sections unchanged"
  key_links:
    - from: "lib/core/mullins-scaffold.cjs buildBrainFolderQuery"
      to: "references/brain/query-patterns.md pattern 1 (brain_framework_chain / FEEDS_INTO)"
      via: "the query template carries only the framework handle 'Mullins Seven Domains' + a problem-type enum -- the generic-handle contract"
      pattern: "brain_framework_chain|FEEDS_INTO|framework"
    - from: "skills/mullins-scaffold/scaffold.json ackoff_traversal"
      to: "skills/larry-personality/SKILL.md the bidirectional Ackoff ascent doctrine"
      via: "the ascent/descent labels reuse the existing larry-personality posture doctrine -- additive, never contradicting"
      pattern: "ascent|descent|Validation"
---

<objective>
Extend the shipped mullins-scaffold (the 7-domain New Business Road Test skeleton) with two additive Brain-informed capabilities, reusing the existing loader and the brain_framework_chain query pattern (Part 7 -- extend, do not rebuild):

1. MULL-01: a Brain-driven cross-framework folder proposal set. Beyond the 7 fixed Mullins domains, the scaffold proposes complementary folders from the frameworks Mullins FEEDS_INTO (Value Proposition, Disruptive Innovation, Systems Thinking). The Brain returns framework names + domain labels ONLY (Part 8 -- generic handles, never user content). The folders are OFFERED at a Decision Gate (the navigator approves which to add), never auto-imposed.
2. MULL-02: the Ackoff up-down/down-up traversal that structures the scaffold. The ASCENT fills the domains toward Validation-at-center (Data in the domain folders -> Information -> Knowledge -> Wisdom at Validation); the DESCENT is Validation decomposing back to re-test each domain's data/assumptions (the New Business Road Test validation loop). Brain-chained to Systems Thinking + MAP THE HIERARCHY.

Purpose: the scaffold becomes Brain-enriched and Ackoff-structured without losing its near-decomposable 7-domain core; the cross-framework folders are the Brain's moat surfaced at scaffold time.
Output: scaffold.json (brain_folders[] + ackoff_traversal, version 2) + mullins-scaffold.cjs (3 additive accessors) + the fixture test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-CONTEXT.md
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-CONTEXT-ADDENDUM-conv-mull-ackoff.md

<interfaces>
<!-- The shipped scaffold loader this plan extends. Reuse these; do not rebuild. -->
From lib/core/mullins-scaffold.cjs (the 5 shipped exports -- keep byte-unchanged):
  loadScaffold()            -- lazy-load + cache skills/mullins-scaffold/scaffold.json; degrades to {version:0, generated_from:'', sections:[]} on error
  listSections()            -- shallow-cloned sections[] (the 20 Mullins sections)
  getSection(id)            -- single section by kebab id, or null
  sectionExists(id)         -- boolean
  getDomainsForSection(id)  -- domain slug or null
  SCAFFOLD_PATH = path.join(__dirname,'..','..','skills','mullins-scaffold','scaffold.json')
  Pattern to mirror for the new accessors: try/catch with MOS_DEBUG stderr line + safe fallback ([] / null); shallow-clone via Object.assign({}, x) so callers cannot mutate the module cache.

From references/brain/query-patterns.md pattern 1 (brain_framework_chain):
  Tool: mcp__mindrian-brain__brain_ask (ungated)
  Question template: "recommend a framework for a {problem_type} venture that has already applied {current_frameworks}"
  Read: next_gate.options[].framework (ranked chain), directive.guided.framework (anchor)
  Graceful degradation: local routing table references/methodology/problem-types.md
  Part-8 contract: the query carries ONLY framework handles + a problem-type enum -- never artifact bodies, meeting content, identifiers, or numbers.

From skills/larry-personality/SKILL.md (the bidirectional Ackoff ascent doctrine -- REUSE, do not contradict):
  The posture is the bidirectional Usher traversal: push_forward = "the bidirectional Ackoff ascent confirms confidence is backed"; pull_back = step-4 gap re-tests assumptions. The ASCENT earns evidence up the levels; the DESCENT (pull_back) decomposes back to re-test data. The scaffold's ascent/descent labels MUST align with this (fill-up toward Validation = ascent; Validation decomposing back to re-test domains = descent).

Mullins framework neighbors (from the addendum Brain findings, FEEDS_INTO):
  Value Proposition (precedes Mullins), Disruptive Innovation (sustaining-vs-disruptive), Systems Thinking (via the Ackoff chain).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: add brain_folders[] + ackoff_traversal to scaffold.json (additive; 20 sections byte-unchanged; version 2)</name>
  <files>skills/mullins-scaffold/scaffold.json, skills/mullins-scaffold/SKILL.md</files>
  <read_first>
    - skills/mullins-scaffold/scaffold.json (the 20 sections[] -- these stay byte-identical; only top-level keys are added)
    - skills/mullins-scaffold/SKILL.md (the human-facing index -- add a "Brain-driven cross-framework folders" section + an "Ackoff bidirectional traversal" section pointing at the new JSON keys)
    - skills/larry-personality/SKILL.md lines 83-91 (the bidirectional Ackoff ascent doctrine the traversal labels must align with)
    - .planning/phases/143.2-.../143.2-CONTEXT-ADDENDUM-conv-mull-ackoff.md (MULL-01/02 design + the Brain framework neighbors)
  </read_first>
  <action>
    Edit skills/mullins-scaffold/scaffold.json. Keep the existing sections[] array byte-identical (do NOT reorder, retitle, or re-key any of the 20 sections). Bump "version" 1 -> 2. Add two additive top-level keys.

    (a) brain_folders[]: the 3 cross-framework folder proposals, each an object with id (kebab), framework (the source framework name -- the generic handle), domain (a domain label slug), title, prompt (the opening question for the folder), feeds_into_source (the literal string "Mullins Seven Domains" -- the FEEDS_INTO origin), and offered: true (a Decision-Gate flag -- these are OFFERED, never auto-materialized). The 3 folders, per MULL-01 and the addendum Brain findings:
      - id "value-proposition" / framework "Value Proposition" / domain "value-proposition" / title "Value Proposition" / prompt "What precise value does the offering deliver to the beachhead segment, and how is it worth paying for?"
      - id "disruptive-innovation" / framework "Disruptive Innovation" / domain "disruption-posture" / title "Disruptive vs Sustaining Posture" / prompt "Is this venture sustaining an incumbent trajectory or disrupting from a new-market or low-end foothold?"
      - id "systems-thinking" / framework "Systems Thinking" / domain "systems-map" / title "Systems Map" / prompt "Which feedback loops, stocks, and leverage points govern this venture as a nested system?"
    Each carries offered:true so consumers know to route through a Decision Gate (per D-Part-3, never auto-impose).

    (b) ackoff_traversal: an object with two arrays -- ascent[] and descent[] -- plus brain_chain (an array of the chained framework names: ["Systems Thinking", "MAP THE HIERARCHY"]) and validation_center (the string "Validation"). The ascent[] are ordered DIKW rungs (Data -> Information -> Knowledge -> Wisdom) each mapping to a scaffold movement toward Validation-at-center; the descent[] are the validation-loop rungs (Validation decomposing back to re-test each domain's data/assumptions). Use 4 ascent rungs keyed {level, label, movement} (level in Data/Information/Knowledge/Wisdom; movement = the fill-up action toward Validation) and 4 descent rungs keyed {level, label, movement} (movement = the re-test-down action from Validation back to the domain data). The labels must align with the larry-personality ascent doctrine (fill-up earns evidence; descent re-tests assumptions) -- do NOT contradict it.

    Then edit skills/mullins-scaffold/SKILL.md: add a "## Brain-driven cross-framework folders (additive)" section describing the 3 brain_folders, that they are OFFERED at a Decision Gate (never auto-imposed), and that the Brain returns framework names + domain labels only (Part 8). Add a "## Ackoff bidirectional traversal" section describing the ascent (fill toward Validation = wisdom) and descent (Validation re-tests each domain's data), and that it reuses the larry-personality bidirectional Ackoff ascent doctrine (additive, not a new doctrine). Hyphens only, no em-dashes, in both files.
  </action>
  <acceptance_criteria>
    - `node -e "const s=require('./lib/core/mullins-scaffold.cjs'); const d=s.loadScaffold(); if(d.version!==2) throw new Error('version not bumped'); if(d.sections.length!==20) throw new Error('sections count changed'); if(!Array.isArray(d.brain_folders)||d.brain_folders.length!==3) throw new Error('brain_folders missing'); if(!d.ackoff_traversal||!Array.isArray(d.ackoff_traversal.ascent)||!Array.isArray(d.ackoff_traversal.descent)) throw new Error('traversal missing'); console.log('OK')"` prints OK
    - `git diff skills/mullins-scaffold/scaffold.json` shows the 20 existing section objects unchanged (only version bump + 2 new top-level keys added)
    - `grep -c 'value-proposition\|disruptive-innovation\|systems-thinking' skills/mullins-scaffold/scaffold.json` >= 3 (the 3 brain_folders present)
    - zero em-dashes: `grep -cP '\xe2\x80\x94|\xe2\x80\x93' skills/mullins-scaffold/scaffold.json skills/mullins-scaffold/SKILL.md` returns 0 for both
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('./lib/core/mullins-scaffold.cjs'); const d=s.loadScaffold(); if(d.version!==2||d.sections.length!==20||!Array.isArray(d.brain_folders)||d.brain_folders.length!==3||!d.ackoff_traversal) throw new Error('scaffold shape wrong'); console.log('OK')"</automated>
  </verify>
  <done>scaffold.json carries version 2 + the 3 brain_folders (each with framework + feeds_into_source + offered:true) + the ackoff_traversal (ascent[] + descent[] + brain_chain + validation_center); the 20 Mullins sections are byte-unchanged; SKILL.md documents both additions; zero em-dashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: add the 3 read-only accessors to mullins-scaffold.cjs + the Part-8-clean Brain query builder, with a fixture test</name>
  <files>lib/core/mullins-scaffold.cjs, lib/core/mullins-scaffold.test.cjs</files>
  <behavior>
    - listBrainFolders() returns the 3 shallow-cloned brain_folder objects (each with framework + feeds_into_source + offered:true); returns [] when scaffold load fails (mirror the existing listSections degradation)
    - getAckoffTraversal() returns the shallow-cloned ackoff_traversal object (ascent[] + descent[] + brain_chain + validation_center); returns null on load failure
    - buildBrainFolderQuery(problemTypeEnum) returns the brain_framework_chain question payload: an object {tool:'mcp__mindrian-brain__brain_ask', question:<the FEEDS_INTO template with current_frameworks="Mullins Seven Domains" + the problem_type enum>, handles:{current_frameworks:['Mullins Seven Domains'], problem_type:<enum>}}. PART-8 BOUNDARY: the payload carries ONLY the framework handle 'Mullins Seven Domains' + the problem-type enum -- no artifact text, no room slug, no user content. If problemTypeEnum is not one of the allowed enums (Un-Defined, Ill-Defined, Well-Defined, Wicked), default to 'Ill-Defined' (never echo arbitrary caller strings into the query -- the enum allow-list IS the Part-8 guard)
    - Test: the 3 brain_folders present + each carries feeds_into_source==='Mullins Seven Domains' + offered===true; the traversal ascent/descent each non-empty; buildBrainFolderQuery output contains 'Mullins Seven Domains' AND the problem_type enum AND NOTHING ELSE user-shaped (assert the JSON.stringify of the payload matches /^[\s\S]*Mullins Seven Domains[\s\S]*$/ and contains no key outside {tool,question,handles} and handles has exactly {current_frameworks, problem_type}); an arbitrary caller string ("Lawrence's CAR-T financial model") passed as problemTypeEnum is REJECTED to the default enum (Part-8 guard: the user string never reaches the query); the 20 existing sections unchanged via listSections().length===20
  </behavior>
  <read_first>
    - lib/core/mullins-scaffold.cjs (the full module -- mirror loadScaffold/listSections try/catch + Object.assign shallow-clone + MOS_DEBUG stderr pattern; KEEP the 5 existing exports byte-unchanged)
    - references/brain/query-patterns.md pattern 1 (the brain_framework_chain question template + the Part-8 generic-handle contract)
    - tests/test-sensors-part8-sweep.cjs OR tests/test-feynman-timeline-canon-part-9-invariant.cjs (the forbidden-user-content sweep pattern to mirror for the buildBrainFolderQuery Part-8 assertion)
  </read_first>
  <action>
    Edit lib/core/mullins-scaffold.cjs. Keep loadScaffold/listSections/getSection/sectionExists/getDomainsForSection byte-unchanged. Add three functions mirroring the existing try/catch + safe-fallback + shallow-clone style:
      listBrainFolders(): loadScaffold(); if doc.brain_folders is an array, return doc.brain_folders.map(f => Object.assign({}, f)); else []. Wrap in try/catch returning [] with the MOS_DEBUG stderr line.
      getAckoffTraversal(): loadScaffold(); if doc.ackoff_traversal is an object, return a shallow clone (Object.assign({}, doc.ackoff_traversal) with ascent/descent/brain_chain arrays also shallow-cloned via .slice()); else null. try/catch -> null.
      buildBrainFolderQuery(problemTypeEnum): define const ALLOWED = ['Un-Defined','Ill-Defined','Well-Defined','Wicked']; const pt = ALLOWED.indexOf(problemTypeEnum) >= 0 ? problemTypeEnum : 'Ill-Defined'; build question = 'recommend a framework for a ' + pt + ' venture that has already applied "Mullins Seven Domains"'; return { tool: 'mcp__mindrian-brain__brain_ask', question: question, handles: { current_frameworks: ['Mullins Seven Domains'], problem_type: pt } }. The enum allow-list is the Part-8 guard -- an arbitrary caller string can NEVER reach the query body. Add the 3 to module.exports.

    Create lib/core/mullins-scaffold.test.cjs (Node, node:assert/strict, zero deps). Assert: listBrainFolders().length===3 and each carries feeds_into_source==='Mullins Seven Domains' and offered===true; getAckoffTraversal().ascent.length>0 and .descent.length>0 and brain_chain includes 'Systems Thinking' and 'MAP THE HIERARCHY' and validation_center==='Validation'; buildBrainFolderQuery('Wicked').handles.problem_type==='Wicked' and the only top-level keys are tool/question/handles and handles has only current_frameworks/problem_type; buildBrainFolderQuery("Lawrence's CAR-T financial model").handles.problem_type==='Ill-Defined' (the Part-8 guard rejected the user string) AND JSON.stringify(payload) does NOT contain 'Lawrence' or 'CAR-T' or 'financial'; listSections().length===20 (existing surface intact). Print a single PASS line and exit 0; throw on any miss. Hyphens only in the test file.
    No fenced code blocks in this action. The test file is code; write it directly.
  </action>
  <acceptance_criteria>
    - `node lib/core/mullins-scaffold.test.cjs` exits 0 and prints PASS
    - the Part-8 guard holds: the test proves an arbitrary user string passed to buildBrainFolderQuery never reaches the query payload (defaults to the enum; JSON.stringify carries no user-content substring)
    - the 5 existing exports are byte-unchanged: `git diff lib/core/mullins-scaffold.cjs` shows only additive functions + the 3 new export keys, no edits to loadScaffold/listSections/getSection/sectionExists/getDomainsForSection bodies
    - zero em-dashes in both files: `grep -cP '\xe2\x80\x94|\xe2\x80\x93' lib/core/mullins-scaffold.cjs lib/core/mullins-scaffold.test.cjs` returns 0
  </acceptance_criteria>
  <verify>
    <automated>node lib/core/mullins-scaffold.test.cjs</automated>
  </verify>
  <done>mullins-scaffold.cjs gains listBrainFolders + getAckoffTraversal + buildBrainFolderQuery (the 5 existing exports byte-unchanged); the fixture test proves the 3 brain_folders + the ascent/descent traversal + the Part-8 enum-allow-list guard (arbitrary user strings never reach the query); `node lib/core/mullins-scaffold.test.cjs` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scaffold Brain-folder query -> Brain MCP | the buildBrainFolderQuery payload crosses LOCAL -> BRAIN; it must carry only generic framework handles + a problem-type enum, never user content (Part 8) |
| Decision Gate -> folder materialization | the brain_folders are OFFERED (offered:true); materialization is a separate Decision-Gate approval, never auto-imposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1432.06-01 | Information Disclosure | buildBrainFolderQuery leaking user content into the Brain query | mitigate | the enum allow-list (Un-Defined/Ill-Defined/Well-Defined/Wicked) is the structural Part-8 guard -- an arbitrary caller string defaults to the enum and never reaches the query body; the fixture test asserts JSON.stringify carries no user-content substring |
| T-1432.06-02 | Elevation of Privilege | a Brain-folder auto-imposed without navigator approval | mitigate | every brain_folder carries offered:true; the SKILL.md documents materialization as a Decision-Gate approval per Canon Part 3 (GUIDED default) |
| T-1432.06-03 | Tampering | the additive keys breaking the 20-section core or the 5 shipped exports | mitigate | the fixture test asserts listSections().length===20 and the 5 existing exports byte-unchanged (git diff additive-only) |
| T-1432.06-SC | Tampering | npm/pip/cargo installs | accept | no package installs (Node built-ins only; zero new deps -- the loader is pure fs + JSON.parse) |
</threat_model>

<verification>
- `node lib/core/mullins-scaffold.test.cjs` exits 0 (the 3 brain_folders + traversal + Part-8 enum guard + 20 sections intact)
- scaffold.json: version 2, 20 sections byte-unchanged, brain_folders[3] + ackoff_traversal present
- Part 8: buildBrainFolderQuery carries only the 'Mullins Seven Domains' handle + a problem-type enum; an arbitrary user string defaults to the enum
- Part 7: the shipped loadScaffold loader is reused; the 5 existing exports byte-unchanged; no rebuild
- zero em-dashes across scaffold.json, SKILL.md, mullins-scaffold.cjs, mullins-scaffold.test.cjs
</verification>

<success_criteria>
MULL-01 lands: the scaffold proposes 3 Brain-driven cross-framework folders (Value Proposition, Disruptive Innovation, Systems Thinking) from the Mullins FEEDS_INTO chain, OFFERED at a Decision Gate, with a Part-8-clean generic-handle query. MULL-02 lands: the Ackoff bidirectional traversal (ascent fills toward Validation-at-center wisdom; descent re-tests each domain's data) is rendered and reuses the larry-personality bidirectional Ackoff ascent doctrine additively. The 7-domain core + the 5 shipped exports are untouched (Part 7 reuse).
</success_criteria>

<output>
Create `.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-06-SUMMARY.md` when done.
</output>
