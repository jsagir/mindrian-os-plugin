---
phase: 143.2-larry-operates-and-pushes-prompt-reconciliation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/ui-system/SKILL.md
  - agents/larry-extended.md
  - skills/conversation-mode/SKILL.md
autonomous: true
requirements: [OPS-04, OPS-05]
canon_parts: [Part 2, Part 3, Part 8, Part 9, Part 10]

must_haves:
  truths:
    - "The render contract documents Shape F.7 (the dial-TUI selector) so the F render is no longer undocumented"
    - "The F-shape count off-by-one is corrected and the Quick Reference lists the F sub-shapes"
    - "larry-extended points at the shipped machinery without duplicating it; the stale line-30 greeting is gone; the frozen frontmatter is byte-unchanged"
    - "conversation-mode notes the reaches + sensors operate in no-room sessions via Shape F.1 with Tier-0 degradation, without removing the shipped scratchpad block"
  artifacts:
    - path: "skills/ui-system/SKILL.md"
      provides: "Shape F.7 dial-TUI section + corrected counts + Quick Reference update (OPS-04)"
      contains: "Shape F.7"
    - path: "agents/larry-extended.md"
      provides: "Operating-the-machinery pointer + stale-greeting deletion + shipped-phase reframe (OPS-05)"
      contains: "Operating the machinery"
    - path: "skills/conversation-mode/SKILL.md"
      provides: "Conversational Reaches note (OPS-05)"
      contains: "Conversational Reaches"
  key_links:
    - from: "skills/ui-system/SKILL.md Shape F.7"
      to: "the 5 reach-ids + SEED-020 (no bespoke widget)"
      via: "F.7 documents the dial as an F.1 specialization reusing renderShapeF1"
      pattern: "F.7|SEED-020"
---

<objective>
Document the should-surfaces that trail the flip-critical core: the ui-system render contract gains Shape F.7 (the shipped dial-TUI) + the F-count fixes; the larry-extended agent gains a pointer to the shipped machinery + loses the stale line-30 greeting (without touching the frozen frontmatter); conversation-mode gains a Tier-0 Conversational-Reaches note (without removing the shipped scratchpad). These describe how reaches LOOK and degrade -- they trail the flip without a correctness gap.

Purpose: Keep the render doc + agent + no-room surface in sync with the now-operated dial, and fix the pre-existing stale greeting + off-by-one surfaced by the audit.
Output: skills/ui-system/SKILL.md (F.7 + counts), agents/larry-extended.md (pointer + delete), skills/conversation-mode/SKILL.md (note).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-CONTEXT.md
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/AUDIT-1-tui-operation.json

<interfaces>
<!-- The frozen larry-extended frontmatter that MUST stay byte-unchanged (lines 1-26). -->
agents/larry-extended.md frontmatter (DO NOT EDIT):
  initialPrompt + 10 persona_variants (default, founder, researcher, researcher_ind, founder_grant,
  investor, operator, mentor, domain_expert, student) + mcpServers: [mindrian-os].
  The Part-8 "ZERO user-content egress" phrase + the dual-path detector contract (upload>=+3 /
  type<=-3) in the body MUST stay verbatim -- do NOT soften "ZERO" to "minimal".

Shape F.7 facts (from AUDIT-1 contract.dial_tui_operation + 143.1-UI-SPEC):
  5 render states S1-S5; confidence column right-aligned NN% or "--"; filled triangle = RECOMMENDED,
  empty = standard; zero markers in Mode B / Tier 0 is INTENTIONAL; frozen 0.70/0.15 gate is a
  DIAL-ONLY variant (does NOT lower the F.0-F.6 0.7 gate); two-K separation DIAL_REACH_K=5 preview /
  MAX_K=3 chooser; the 5 reach-ids are an F.7 specialization (NOT a replacement for the 10 canonical
  verbs); SELECTED_REACH / PIVOTED close mechanics; SEED-020 (no bespoke widget; reuses renderShapeF1).

COUNT FACTS: line 68 "one of five F sub-shapes" is WRONG; after adding F.7 the catalog is EIGHT
(F.0-F.7). Fix line 68 and extend the Quick Reference SHAPES line (line 381).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OPS-04 -- add Shape F.7 + fix the F-shape counts + Quick Reference</name>
  <files>skills/ui-system/SKILL.md</files>
  <read_first>
    - skills/ui-system/SKILL.md line 40 (catalog note "reserved for future lens-aware variants (e.g. v1.14.0 dual-graph work)" -- the dial-TUI WAS the additive variant, shipped in 143.1), line 68 ("one of five F sub-shapes" -- WRONG), the F.6 block (219-245), the insertion point (after 245, before "## 3. Symbol Vocabulary" at 247), the Quick Reference SHAPES line (381)
    - AUDIT-1-tui-operation.json -> per_surface_edits[2].edits + contract.dial_tui_operation
    - .planning/seeds/SEED-020-shape-f-is-the-universal-mindrian-ui.md (no-bespoke-widget rule)
  </read_first>
  <action>
    Line 40: flip the "reserved for future lens-aware variants (e.g. v1.14.0 dual-graph work)" staleness -- the additive variant (the dial-TUI / Shape F.7) shipped in Phase 143.1; update the count to "eight sub-shapes (F.0 through F.7)".
    Line 68: fix "one of five F sub-shapes" to "one of eight (F.0-F.7)".
    Insert "#### Shape F.7 - Dial-TUI Capability Selector (Phase 143.1)" after the F.6 block (after 245, before "## 3. Symbol Vocabulary"). Document: 5 render states S1-S5 (S1 mode_a clear leader; S2 near-tie two filled; S3 mode_b all empty; S4 tier_0 cold-room "--"; S5 partial-slot degradation); the confidence-column dial (right-aligned NN% or "--"; filled triangle = RECOMMENDED, empty = standard; zero markers in Mode B / Tier 0 is INTENTIONAL, not broken); the frozen 0.70/0.15 gate as a DIAL-ONLY variant that does NOT lower the F.0-F.6 0.7 gate; the two-K separation (DIAL_REACH_K=5 preview / MAX_K=3 chooser); the 5 reach-ids (context_block, contradiction, cross_room, brain_consult, deep_research) as an F.7 specialization, NOT a replacement for the 10 canonical verbs; the SELECTED_REACH / PIVOTED close mechanics; cite SEED-020 (no bespoke widget -- reuses renderShapeF1 / shape-f1-renderer.cjs). Mirror the existing F.x block format (Purpose / When to use / Options / Verb constraints / Keyboard / State-update hook / Shipped in). Add a one-line honest-144 note: dial-TUI shipped (143.1), engine flip is Phase 144. Do NOT edit docs/CANON-PHASE-MAP.md here (that row flips in Phase 144's commit per the audit sequencing).
    Line 381 Quick Reference SHAPES line: extend to note the F family spans F.0-F.7 (e.g. add "F=Selector(F.0-F.7)").
    No fenced code blocks beyond the existing header-format style in the file. Hyphens only, no em-dashes.
  </action>
  <acceptance_criteria>
    - F.7 block present: `grep -c "Shape F.7" skills/ui-system/SKILL.md` >= 1
    - count fixed: `grep -c "one of five F sub-shapes" skills/ui-system/SKILL.md` == 0; `grep -c "F.0-F.7\|F.0 through F.7" skills/ui-system/SKILL.md` >= 1
    - dial facts: `grep -c "DIAL_REACH_K" skills/ui-system/SKILL.md` >= 1 AND `grep -c "SEED-020" skills/ui-system/SKILL.md` >= 1
    - all 5 reach-ids documented in the F.7 area
    - zero em-dashes in skills/ui-system/SKILL.md
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Shape F.7" skills/ui-system/SKILL.md && ! grep -q "one of five F sub-shapes" skills/ui-system/SKILL.md && grep -q "SEED-020" skills/ui-system/SKILL.md && grep -q "DIAL_REACH_K" skills/ui-system/SKILL.md</automated>
  </verify>
  <done>A "Shape F.7 - Dial-TUI Capability Selector (Phase 143.1)" block exists in the catalog format; line 40 + 68 staleness/off-by-one fixed; the Quick Reference SHAPES line lists F.0-F.7; SEED-020 + DIAL_REACH_K + the 5 reach-ids documented; zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: OPS-05a -- larry-extended pointer + stale-greeting delete + shipped-phase reframe (frozen frontmatter preserved)</name>
  <files>agents/larry-extended.md</files>
  <read_first>
    - agents/larry-extended.md lines 1-26 (FROZEN frontmatter -- byte-unchanged), line 30 (stale "First line to new users: I'm Larry. What are you working on?" -- contradicts shipped initialPrompt at line 14), lines 77-135 (Phase 115 dual-path body: the "ZERO user-content egress" phrase + the upload>=+3 / type<=-3 contract MUST stay verbatim), lines 96-97, 116, 131-133 (stale "future Phase 100 / Phase 118 ... later / via Phase 109" forward-references to SHIPPED phases)
    - AUDIT-1-tui-operation.json -> per_surface_edits[1].edits (pointer text, line-30 delete, shipped-phase reframe, frozen-keep list)
  </read_first>
  <action>
    DELETE line 30 entirely -- it contradicts the shipped initialPrompt at line 14. Do NOT alter line 14 or any frontmatter.
    ADD a short "## Operating the machinery (see larry-personality skill)" pointer after the Voice section, naming the shipped surfaces and DEFERRING detail to the skill (do NOT duplicate SKILL.md instructions): 5 reach-ids LIVE (Phase 141 getRoomContext), 7 sensors LIVE (Phase 143), dial-TUI LIVE (Phase 143.1), engine flip executing (Phase 144 at skill-activation-router.cjs Precedence Rule 1). One paragraph, pointer-not-respec.
    Flip the stale forward-references on SHIPPED phases (96-97, 116, 131-133): "future Phase 100 / Phase 118 ... instruments later / via Phase 109" -> operating-baseline wording (109 + 115 are LIVE). Keep light -- the agent delegates the machinery to the skill.
    KEEP FROZEN VERBATIM: the frontmatter (1-26), the dual-path detector contract (upload>=+3 / type<=-3), and the Part-8 "ZERO user-content egress" phrase (do NOT soften "ZERO").
    Hyphens only, no em-dashes.
  </action>
  <acceptance_criteria>
    - stale greeting gone: `grep -c "What are you working on" agents/larry-extended.md` == 0
    - pointer present: `grep -c "Operating the machinery" agents/larry-extended.md` >= 1
    - frozen frontmatter unchanged: no changed line in 1-26 -- `git diff agents/larry-extended.md` shows zero +/- lines touching initialPrompt/persona_variants/mcpServers; `grep -c "What decision is stuck" agents/larry-extended.md` >= 11
    - Part-8 verbatim: `grep -c "ZERO user-content egress" agents/larry-extended.md` >= 1
    - dual-path contract intact: `grep -c "upload" agents/larry-extended.md` >= 1
    - zero em-dashes in agents/larry-extended.md
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "What are you working on" agents/larry-extended.md && grep -q "Operating the machinery" agents/larry-extended.md && grep -q "ZERO user-content egress" agents/larry-extended.md && [ "$(git diff agents/larry-extended.md | grep -E '^[-+]' | grep -E 'initialPrompt|persona_variants|mcpServers' | wc -l)" -eq 0 ]</automated>
  </verify>
  <done>Line 30 stale greeting deleted; an "Operating the machinery" pointer paragraph added (deferring detail to the skill); stale shipped-phase forward-references reframed to operating-baseline; the frontmatter + dual-path contract + "ZERO user-content egress" phrase byte-unchanged; zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 3: OPS-05b -- conversation-mode Conversational Reaches note (scratchpad preserved)</name>
  <files>skills/conversation-mode/SKILL.md</files>
  <read_first>
    - skills/conversation-mode/SKILL.md lines 10-13 (the intro -- the Conversational Reaches note lands here, near the top, before Mode 1), line 26 (the "follow the corresponding framework chain" framing to soften), lines 82-101 (the SHIPPED scratchpad CLI block -- DO NOT remove it; the audit's delete proposal is premature, scratchpad-ops.cjs is the shipped no-room persistence path)
    - AUDIT-1-tui-operation.json -> per_surface_edits[3].edits (the Tier-0 reaches note + the soften + the explicit "do not remove scratchpad" caveat)
  </read_first>
  <action>
    ADD a "## Conversational Reaches (Part 2/3)" note near the top (after line 12, before "## Mode 1") stating that the 5 reach-ids and 7 sensors ALSO operate in no-room sessions, surfaced via the same Shape F.1 selector with Tier-0 degradation (hardcoded minimal reach set, "--" confidence, zero markers -- the cold-room render is intentional). Frame the no-room dial as the SURFACE; frameworks stay INTERNAL to reach selection.
    Soften line 26 ("follow the corresponding framework chain") so frameworks read as internal to reach selection rather than the user-facing surface (the surface is the reach selector).
    Do NOT remove or alter the scratchpad CLI block (lines 82-101) -- scratchpad-ops.cjs is the shipped no-room persistence path.
    Hyphens only, no em-dashes.
  </action>
  <acceptance_criteria>
    - note present: `grep -c "Conversational Reaches" skills/conversation-mode/SKILL.md` >= 1
    - Tier-0 degradation stated: `grep -ci "Tier-0\|Tier 0\|zero markers\|hardcoded minimal" skills/conversation-mode/SKILL.md` >= 1
    - scratchpad block intact: `grep -c "scratchpad-ops.cjs" skills/conversation-mode/SKILL.md` >= 1 (the shipped block is preserved)
    - zero em-dashes in skills/conversation-mode/SKILL.md
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Conversational Reaches" skills/conversation-mode/SKILL.md && grep -q "scratchpad-ops.cjs" skills/conversation-mode/SKILL.md</automated>
  </verify>
  <done>A "Conversational Reaches (Part 2/3)" note exists near the top stating the reaches + sensors operate in no-room sessions via Shape F.1 with Tier-0 degradation; line 26 softened; the scratchpad CLI block preserved; zero em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen frontmatter -> edits | the larry-extended initialPrompt + 10 persona_variants + dual-path contract must stay byte-unchanged |
| Part-8 phrase -> softening | "ZERO user-content egress" must not be softened to "minimal" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1432.03-01 | Tampering | the frozen larry-extended frontmatter | mitigate | git diff gate asserts zero +/- lines touching initialPrompt/persona_variants/mcpServers; `grep -c "What decision is stuck"` >= 11 |
| T-1432.03-02 | Information Disclosure | the Part-8 absolute phrase | mitigate | `grep -c "ZERO user-content egress"` >= 1; do not soften to "minimal" |
| T-1432.03-03 | Denial of Service | premature scratchpad delete | mitigate | hard-fence: do NOT remove the scratchpad block; `grep -c "scratchpad-ops.cjs"` >= 1 gate |
| T-1432.03-SC | Tampering | npm/pip/cargo installs | accept | no package installs (pure markdown doctrine edit) |
</threat_model>

<verification>
After all three tasks:
- F.7 documented + counts fixed in ui-system; SEED-020 + DIAL_REACH_K present
- larry-extended: stale greeting gone, pointer present, frozen frontmatter byte-unchanged, "ZERO user-content egress" verbatim
- conversation-mode: Conversational Reaches note present, scratchpad block intact
- Zero em-dashes across all three files
</verification>

<success_criteria>
ROADMAP success criterion 4 is satisfied: ui-system Shape F.7 (dial-TUI) is documented + the F-count off-by-one fixed (OPS-04); the larry-extended pointer added + stale-greeting deleted with the frozen frontmatter preserved + the conversation-mode Conversational-Reaches note added (OPS-05).
</success_criteria>

<output>
Create `.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-03-SUMMARY.md` when done.
</output>
