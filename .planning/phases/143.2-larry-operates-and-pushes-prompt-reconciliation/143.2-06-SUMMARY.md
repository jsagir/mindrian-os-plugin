---
phase: 143.2-larry-operates-and-pushes-prompt-reconciliation
plan: 06
subsystem: mullins-scaffold
tags: [mullins-scaffold, brain-folders, ackoff-traversal, feeds-into, canon-part-7, canon-part-8, decision-gate, dikw]

# Dependency graph
requires:
  - phase: 84-04
    provides: skills/mullins-scaffold/scaffold.json (the 7-domain / 20-section New Business Road Test skeleton) + lib/core/mullins-scaffold.cjs (the 5 shipped read-only exports)
  - phase: 90-brain-derivation-layer
    provides: references/brain/query-patterns.md pattern 1 (brain_framework_chain) -- the FEEDS_INTO question template + Part-8 generic-handle contract
  - phase: 141-local-retrieval-spine-and-capability-dial
    provides: skills/larry-personality SKILL.md bidirectional Ackoff ascent doctrine (the ascent/descent labels reuse it)
provides:
  - "MULL-01: scaffold.json brain_folders[3] -- Value Proposition / Disruptive Innovation / Systems Thinking, FEEDS_INTO source-labelled, offered:true Decision-Gate flag"
  - "MULL-01: lib/core/mullins-scaffold.cjs listBrainFolders() + buildBrainFolderQuery() -- the Part-8-clean enum-allow-list query builder"
  - "MULL-02: scaffold.json ackoff_traversal (ascent[4] fills toward Validation-at-center, descent[4] re-tests each domain; brain_chain Systems Thinking + MAP THE HIERARCHY) + getAckoffTraversal()"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additive scaffold.json extension: version bump + new top-level keys ONLY; the 20 section objects stay byte-unchanged (Part 7)"
    - "Enum allow-list as the structural Part-8 guard: an arbitrary caller string can never reach a Brain query body -- it defaults to the Ill-Defined enum"
    - "New accessors mirror the shipped loadScaffold/listSections try/catch + Object.assign shallow-clone + MOS_DEBUG stderr degradation"

key-files:
  created:
    - lib/core/mullins-scaffold.test.cjs
    - .planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-06-SUMMARY.md
  modified:
    - skills/mullins-scaffold/scaffold.json
    - skills/mullins-scaffold/SKILL.md
    - lib/core/mullins-scaffold.cjs

key-decisions:
  - "scaffold version 1 -> 2; the 20 Mullins section objects verified byte-unchanged (only the version line + the array-close line touched, both re-added additively)"
  - "The 5 shipped exports kept byte-unchanged; the only diff to existing code is one extended docblock comment line (the Exports list), no function body edited"
  - "buildBrainFolderQuery carries ONLY {tool, question, handles}; handles holds ONLY {current_frameworks:['Mullins Seven Domains'], problem_type:<enum>} -- the enum allow-list (Un-Defined/Ill-Defined/Well-Defined/Wicked) is the Part-8 fence"
  - "brain_folders carry offered:true so consumers route materialization through a Decision Gate (Canon Part 3 GUIDED default), never auto-impose"
  - "ascent/descent labels reuse the larry-personality bidirectional Ackoff ascent doctrine (ascent earns evidence = push_forward; descent re-tests assumptions = pull_back); additive, never contradicting"

patterns-established:
  - "Brain-folder proposals are OFFERED methodology-graph reads (framework names + domain labels), never user-content egress"

requirements-completed: [MULL-01, MULL-02]

# Metrics
duration: ~20min
completed: 2026-06-07
tasks: 2
files-created: 2
files-modified: 3
commits: 4
---

# Phase 143.2 Plan 06: Mullins Scaffold Brain Folders + Ackoff Traversal Summary

Extended the shipped mullins-scaffold additively (Part 7 reuse): scaffold.json gains 3 Brain-driven cross-framework folders (FEEDS_INTO, offered at a Decision Gate) and the Ackoff bidirectional DIKW traversal, while the loader gains 3 read-only accessors plus the Part-8-clean enum-allow-list query builder, all proven by a fixture test.

## What shipped

### MULL-01 -- Brain-driven cross-framework folders
- `scaffold.json` `brain_folders[3]`: Value Proposition, Disruptive vs Sustaining Posture, Systems Map. Each carries `id`, `framework` (the generic source handle), `domain`, `title`, `prompt`, `feeds_into_source: "Mullins Seven Domains"`, and `offered: true`.
- `lib/core/mullins-scaffold.cjs` `listBrainFolders()`: returns the 3 shallow-cloned folders; `[]` on load failure.
- `lib/core/mullins-scaffold.cjs` `buildBrainFolderQuery(enum)`: returns the brain_framework_chain payload `{tool, question, handles}`. The enum allow-list (Un-Defined / Ill-Defined / Well-Defined / Wicked) is the structural Part-8 guard -- any non-enum caller string defaults to `Ill-Defined` and never reaches the query body.

### MULL-02 -- Ackoff bidirectional traversal
- `scaffold.json` `ackoff_traversal`: `ascent[4]` (Data -> Information -> Knowledge -> Wisdom, filling toward Validation-at-center), `descent[4]` (Validation decomposing back to re-test each domain's connections, structure, and data), `brain_chain: ["Systems Thinking", "MAP THE HIERARCHY"]`, `validation_center: "Validation"`.
- `lib/core/mullins-scaffold.cjs` `getAckoffTraversal()`: returns the shallow-cloned traversal; `null` on load failure.
- `SKILL.md`: documents both additions; ascent/descent reuse the larry-personality bidirectional Ackoff ascent doctrine additively.

## Tasks and commits

| Task | Name | Commit |
| ---- | ---- | ------ |
| 1 | scaffold.json brain_folders + ackoff_traversal (version 2, 20 sections byte-unchanged) + SKILL.md | da83445d |
| 2 (RED) | failing fixture test for the 3 accessors | 08a5ffb8 |
| 2 (GREEN) | listBrainFolders + getAckoffTraversal + buildBrainFolderQuery + test passes | c55cc6fb |

## Verification

- `node lib/core/mullins-scaffold.test.cjs` exits 0, prints PASS (3 brain_folders with feeds_into_source + offered:true; ascent/descent non-empty; brain_chain includes Systems Thinking + MAP THE HIERARCHY; Part-8 guard rejects "Lawrence's CAR-T financial model" to Ill-Defined and JSON.stringify carries no user-content fragment; listSections().length === 20).
- scaffold shape: version 2, 20 sections, brain_folders[3], ackoff_traversal present.
- Part 7: the shipped loadScaffold loader reused; the 5 existing exports byte-unchanged (only a docblock comment line extended); no rebuild.
- Part 8: buildBrainFolderQuery serialization carries only `Mullins Seven Domains` + the enum.
- Zero em-dashes across scaffold.json, SKILL.md, mullins-scaffold.cjs, mullins-scaffold.test.cjs (all grep counts 0).

## Threat model dispositions

- T-1432.06-01 (Info Disclosure, query leak): MITIGATED -- enum allow-list defaults arbitrary strings to the enum; the fixture test asserts JSON.stringify carries no user-content substring.
- T-1432.06-02 (Elevation, auto-imposed folder): MITIGATED -- every brain_folder carries `offered:true`; SKILL.md documents Decision-Gate materialization per Canon Part 3.
- T-1432.06-03 (Tampering, core/exports broken): MITIGATED -- test asserts listSections().length === 20; git diff confirms additive-only (5 exports byte-unchanged).
- T-1432.06-SC (package installs): ACCEPTED -- zero new deps; Node built-ins only.

## Deviations from Plan

None -- plan executed exactly as written.

## Known Stubs

None -- both accessors are fully wired to live scaffold.json data; the fixture test exercises real loaded values.

## Self-Check: PASSED

- FOUND: skills/mullins-scaffold/scaffold.json (version 2, brain_folders + ackoff_traversal)
- FOUND: skills/mullins-scaffold/SKILL.md (both new sections)
- FOUND: lib/core/mullins-scaffold.cjs (8 exports)
- FOUND: lib/core/mullins-scaffold.test.cjs (PASS)
- FOUND: commit da83445d (Task 1)
- FOUND: commit 08a5ffb8 (Task 2 RED)
- FOUND: commit c55cc6fb (Task 2 GREEN)
