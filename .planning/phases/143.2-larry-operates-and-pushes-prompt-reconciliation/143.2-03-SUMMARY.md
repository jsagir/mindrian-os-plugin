---
phase: 143.2-larry-operates-and-pushes-prompt-reconciliation
plan: 03
subsystem: ui
tags: [shape-f, dial-tui, reaches, larry-extended, conversation-mode, canon-part-3, canon-part-10, doctrine]

# Dependency graph
requires:
  - phase: 143.1-dial-tui
    provides: the shipped dial-TUI capability selector (dial-presenter.cjs render states, DIAL_REACH_K/MAX_K two-K separation, closeReach 4-outcome transaction, renderShapeF1 reuse)
  - phase: 143-insight-sensors
    provides: the 7 insight sensors + frozen 5 REACH_IDS that the dial surfaces
  - phase: 141-local-retrieval-spine
    provides: getRoomContext + the 5 reach-ids + 3 posture-ids doctrine
provides:
  - "ui-system Shape F.7 (Dial-TUI Capability Selector) documented in the render contract"
  - "F-shape catalog off-by-one corrected (now eight: F.0-F.7) + Quick Reference SHAPES line extended"
  - "larry-extended Operating-the-machinery pointer + stale line-30 greeting deleted + shipped-phase reframe, frozen frontmatter byte-unchanged"
  - "conversation-mode Conversational-Reaches Tier-0 note (no-room reaches via Shape F.1), scratchpad block preserved"
affects: [144-navigation-engine, larry-personality, ui-system, SEED-020-followup]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "F.7 documented as an F.1 specialization (reuses renderShapeF1), not a new selector primitive (SEED-020 no-bespoke-widget)"
    - "dial-only 0.70/0.15 recommended gate documented as a variant that does NOT lower the F.0-F.6 0.7 gate"
    - "agent body defers operating detail to the skill (pointer-not-respec) rather than duplicating SKILL.md"

key-files:
  created:
    - .planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-03-SUMMARY.md
  modified:
    - skills/ui-system/SKILL.md
    - agents/larry-extended.md
    - skills/conversation-mode/SKILL.md

key-decisions:
  - "F.7 written as an F.1 specialization reusing renderShapeF1 per SEED-020 (no bespoke widget)"
  - "The frozen 0.70/0.15 dial gate is documented as DIAL-ONLY and explicitly does NOT lower the F.0-F.6 0.7 gate"
  - "The 5 reach-ids documented as an F.7 specialization, NOT a replacement for the 10 canonical verbs (no 6th reach-id minted)"
  - "Strengthened larry-extended Part-8 line to carry the verbatim canonical absolute 'ZERO user-content egress' (never softened to 'minimal') to satisfy the acceptance gate AND the threat-mitigation intent"
  - "CANON-PHASE-MAP NOT edited here -- the Part 3 / Part 10 engine-flip row flips in Phase 144's commit per the audit sequencing"

patterns-established:
  - "Render-contract additions cite the shipped library (dial-presenter.cjs / dial-reach-orchestrator.cjs / dial-close-reach.cjs) + the seed (SEED-020) rather than re-paste the UI-SPEC"
  - "Agent bodies point at the skill for operating detail; the skill owns the contract"

requirements-completed: [OPS-04, OPS-05]

# Metrics
duration: 18min
completed: 2026-06-07
---

# Phase 143.2 Plan 03: ui-system Shape F.7 + larry-extended pointer + conversation-mode reaches Summary

**Documented the shipped dial-TUI as Shape F.7 in the render contract (5 render states, confidence-column dial, dial-only 0.70/0.15 gate, two-K separation, 5 reach-ids, SEED-020 reuse), fixed the F-shape off-by-one to eight (F.0-F.7), deleted the stale larry-extended greeting and added a machinery pointer with the frozen frontmatter byte-unchanged, and added the no-room Conversational-Reaches Tier-0 note while preserving the shipped scratchpad block.**

## Performance

- **Duration:** ~18 min
- **Started:** 2026-06-07
- **Completed:** 2026-06-07
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- Shape F.7 (Dial-TUI Capability Selector, Phase 143.1) added to `skills/ui-system/SKILL.md` after the F.6 block in the catalog format, documenting the 5 render states S1-S5, the right-aligned confidence-column dial (filled triangle = RECOMMENDED, empty = standard, zero markers in Mode B / Tier 0 INTENTIONAL), the frozen 0.70/0.15 gate as a DIAL-ONLY variant that does NOT lower the F.0-F.6 gate, the two-K separation (DIAL_REACH_K=5 preview / MAX_K=3 chooser), the 5 reach-ids as an F.7 specialization, the closeReach 4-outcome mechanics, and SEED-020 (no bespoke widget).
- F-shape count off-by-one corrected: line-40 staleness flipped (eight sub-shapes F.0-F.7; F.7 named as the shipped additive variant), the "one of five F sub-shapes" error removed, and the Quick Reference SHAPES line extended with `F=Selector(F.0-F.7)`.
- larry-extended stale line-30 greeting deleted, an "Operating the machinery" pointer added (5 reaches / 7 sensors / dial LIVE; 144 flip executing) that defers detail to the larry-personality skill, and stale shipped-phase forward-references (Phase 100 / 118 / v1.14.0) reframed to operating-baseline. Frozen frontmatter (initialPrompt + 10 persona_variants + mcpServers) and the dual-path detector contract are byte-unchanged.
- conversation-mode gained a "Conversational Reaches (Part 2/3)" note (reaches + sensors operate in no-room sessions via Shape F.1 with Tier-0 degradation), the framework-chain line softened to read as internal-to-reach-selection, and the shipped scratchpad CLI block preserved unchanged.

## Task Commits

Each task was committed atomically:

1. **Task 1: OPS-04 Shape F.7 + F-count fix + Quick Reference** - `dbf82288` (docs)
2. **Task 2: OPS-05a larry-extended pointer + stale-greeting delete + reframe** - `159867c3` (docs)
3. **Task 3: OPS-05b conversation-mode Conversational Reaches note** - `b6b730a2` (docs)

**Plan metadata:** (final docs commit -- SUMMARY + STATE + ROADMAP)

## Files Created/Modified
- `skills/ui-system/SKILL.md` - Added Shape F.7 dial-TUI block; fixed F-shape catalog count to eight (F.0-F.7); extended Quick Reference SHAPES line.
- `agents/larry-extended.md` - Deleted stale line-30 greeting; added Operating-the-machinery pointer; reframed shipped-phase forward-references; strengthened Part-8 absolute to verbatim "ZERO user-content egress"; frozen frontmatter untouched.
- `skills/conversation-mode/SKILL.md` - Added Conversational Reaches (Part 2/3) Tier-0 note; softened framework-chain line; scratchpad block preserved.

## Decisions Made
- F.7 documented as an F.1 specialization reusing `renderShapeF1` (SEED-020 no-bespoke-widget rule), not a new selector primitive.
- The 0.70/0.15 dial gate is documented as DIAL-ONLY and explicitly does NOT lower the F.0-F.6 0.7 gate.
- The 5 reach-ids are documented as an F.7 specialization, NOT a replacement for the 10 canonical verbs (no 6th reach-id minted; WFL-01 honored).
- CANON-PHASE-MAP.md was deliberately NOT edited here; its Part 3 / Part 10 engine-flip row flips in Phase 144's commit per the audit sequencing.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Acceptance gate referenced a Part-8 phrase that was never present in larry-extended.md**
- **Found during:** Task 2 (larry-extended pointer + reframe)
- **Issue:** The plan's `<interfaces>` and acceptance criteria require `grep -c "ZERO user-content egress" agents/larry-extended.md >= 1` and instruct to keep that phrase "verbatim", but the literal string never existed in the file (HEAD had only the lowercase "NO LEAK to Brain" absolute on the Part-8 line). The grep gate was unsatisfiable as written.
- **Fix:** Strengthened the existing Part-8 line (the file's real absolute) to carry the canonical verbatim phrase: "This first-touch surface carries ZERO user-content egress to Brain -- not 'minimal,' ZERO. NO LEAK to Brain." This satisfies the acceptance grep AND the threat-mitigation intent of T-1432.03-02 (the absolute stays absolute and is explicitly never softened to "minimal").
- **Files modified:** agents/larry-extended.md
- **Verification:** `grep -c "ZERO user-content egress" agents/larry-extended.md` = 1; the existing "NO LEAK to Brain" / "LOCAL ... only" framing is preserved; zero em-dashes.
- **Committed in:** 159867c3 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug -- unsatisfiable acceptance gate reconciled by strengthening, not softening, the Part-8 absolute)
**Impact on plan:** The auto-fix strengthened the Part-8 guarantee rather than weakening it, fully aligned with the threat register. No scope creep. No 6th reach-id, no new sensor, no new selector format.

## Issues Encountered
- The frozen-frontmatter git-diff gate passed cleanly: `git diff HEAD agents/larry-extended.md` shows zero +/- lines touching initialPrompt / persona_variants / mcpServers, and `grep -c "What decision is stuck"` = 12 (>= 11).

## User Setup Required
None - pure markdown doctrine edit, no external service configuration required.

## Next Phase Readiness
- The render contract, the larry-extended agent, and the no-room surface now match the shipped dial; this clears ROADMAP success criterion 4 for Phase 143.2.
- Phase 144 (navigation-engine legacy-to-engine flip) can now flip the engine into a prompt surface that already documents the dial it fires. Phase 144's commit owns the CANON-PHASE-MAP Part 3 / Part 10 engine-flip row update.
- SEED-020 follow-up (Shape F as the universal /mos:help selector) remains dormant; F.7 is now the documented proof-of-pattern it can cite.

## Self-Check: PASSED

- All 3 modified files exist on disk.
- All 3 task commits exist in git (dbf82288, 159867c3, b6b730a2).
- Zero em-dashes across all three edited files and this SUMMARY.
- Frozen larry-extended frontmatter byte-unchanged (git-diff gate clean; "What decision is stuck" count = 12).

---
*Phase: 143.2-larry-operates-and-pushes-prompt-reconciliation*
*Completed: 2026-06-07*
