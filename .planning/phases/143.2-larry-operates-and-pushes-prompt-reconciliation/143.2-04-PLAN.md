---
phase: 143.2-larry-operates-and-pushes-prompt-reconciliation
plan: 04
type: execute
wave: 3
depends_on: ["143.2-01", "143.2-02", "143.2-03", "143.2-05", "143.2-06"]
files_modified:
  - tests/test-143.2-doctrine-presence.cjs
  - tests/run-all-1432.sh
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, PUSH-01, PUSH-02, PUSH-03, PUSH-04, PUSH-05, PUSH-06, CONV-01, CONV-02, MULL-01, MULL-02]
canon_parts: [Part 2, Part 3, Part 7, Part 8, Part 9, Part 10]

must_haves:
  truths:
    - "A doctrine-presence test asserts each operate-block (OPS-01/02/03), each of the 6 push-families (PUSH-01..06), the CONV lane-picker + DIKW mapping (CONV-01/02), and the MULL Brain-folder set + Ackoff traversal (MULL-01/02) are present in their target surfaces"
    - "The composition fences stay green: the 5-reach drift test, the posture drift test, the routing fence, the Part-8 sensor sweep, AND the new mullins-scaffold fixture test (with its Part-8 Brain-folder boundary) all pass after the edits"
    - "The MULL Brain-folder query logic carries generic handles only -- a Part-8 grep over the new buildBrainFolderQuery logic finds no user-content path; the frozen larry-extended frontmatter is byte-unchanged; the touched prompt + scaffold files carry zero em-dashes"
  artifacts:
    - path: "tests/test-143.2-doctrine-presence.cjs"
      provides: "the grep-presence + em-dash + frozen-frontmatter doctrine gate for the phase, extended to cover CONV (conversation-mode lane-picker + DIKW) + MULL (scaffold.json brain_folders + ackoff_traversal) + the MULL Part-8 Brain-folder-query boundary grep"
      exports: []
    - path: "tests/run-all-1432.sh"
      provides: "the phase-scoped runner mirroring run-all-143.sh / run-all-1431.sh, composing the doctrine gate + the four shipped fences + the mullins-scaffold fixture test"
  key_links:
    - from: "tests/run-all-1432.sh"
      to: "the four shipped fences (reach-drift, posture-drift, routing-fence, part8-sweep) + the new doctrine-presence test + the mullins-scaffold fixture test (Part-8 Brain-folder boundary)"
      via: "the runner composes them so the whole gate runs in one command"
      pattern: "test-reach-ids-drift|test-sensors-routing-fence|test-143.2-doctrine-presence|mullins-scaffold.test"
---

<objective>
Lock the phase with a doctrine-shaped verification: a grep-presence test that asserts every operate-block + every push-family landed AND the 4 new scope-addition requirements landed (CONV-01/02 in conversation-mode, MULL-01/02 in scaffold.json + the loader), plus a phase-scoped runner that re-asserts the four shipped composition fences stay green AND the new mullins-scaffold fixture test (with its Part-8 Brain-folder boundary) passes AND the frozen frontmatter is byte-unchanged AND the touched files carry zero em-dashes. This is the gate the ROADMAP success criteria collapse to (criterion 3 -- composition held -- is the spine; the scope-addition success criteria for CONV + MULL ride the same gate).

Purpose: Prove the reconciliation landed AND broke nothing (no 6th reach-id, no egress regression, no softened Part-8 absolute, no user content in the MULL Brain-folder query, no em-dash).
Output: tests/test-143.2-doctrine-presence.cjs + tests/run-all-1432.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-CONTEXT.md
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-CONTEXT-ADDENDUM-conv-mull-ackoff.md

<interfaces>
<!-- The four shipped composition fences this runner composes (all currently green). -->
  tests/test-reach-ids-drift.cjs       -- exactly-5 reach-id set (the no-6th-reach gate)
  tests/test-posture-ids-drift.cjs     -- exactly-3 posture-id set
  tests/test-sensors-routing-fence.cjs -- Phase 144 fence (no routing_source=engine in sensors)
  tests/test-sensors-part8-sweep.cjs   -- Part-8 5-tripwire (egress paths clean)

<!-- The new fixture test this runner also composes (shipped by Plan 06). -->
  lib/core/mullins-scaffold.test.cjs   -- the 3 brain_folders + the ascent/descent traversal + the Part-8 buildBrainFolderQuery enum-allow-list guard (arbitrary user strings never reach the query)

  Runner pattern to mirror: tests/run-all-143.sh and tests/run-all-1431.sh
  (SHELL_SUITES + CJS_SUITES arrays; per-suite PASS/FAIL line; non-zero exit on any fail;
   bash only, no emoji, no em-dashes).

Doctrine presence anchors (grep tokens the edits guarantee):
  OPS-01: "comes later" absent in skills/larry-personality/SKILL.md; "insight-sensors.cjs" present
  OPS-02: "Operating the Dial" + SELECTED_REACH + PIVOTED + recordSelectorMiss + navigation.cjs
  OPS-03: "routing_source" + "CONSEQUENCE" + "zero user-content egress"
  OPS-04: "Shape F.7" + "SEED-020" + "DIAL_REACH_K" in skills/ui-system/SKILL.md; "one of five F sub-shapes" absent
  OPS-05: "Operating the machinery" present + "What are you working on" absent in agents/larry-extended.md;
          "Conversational Reaches" + "scratchpad-ops.cjs" present in skills/conversation-mode/SKILL.md
  PUSH-01: "SENS-03" in larry-personality
  PUSH-02: "Lagging-component pattern stated" present; "Specific methodology" absent
  PUSH-03: "/mos:whitespace" + "/mos:score-innovation" + "20+" gate + "7e"
  PUSH-04: "/mos:find-connections" + (pws-methodology) "borrowed structure"
  PUSH-05: "/mos:think-hats" + (pws-methodology) "BONO Team Perspective" + "Accept, reshape, or stay"
  PUSH-06: "Deep Research Escalation Explicit Triggers"
  CONV-01: "Lane Picker" + ("Shape F.1" OR "SEED-020") + "Decision Gate" in skills/conversation-mode/SKILL.md
  CONV-02: ("DIKW" OR "Data/Information") + "Wisdom" + "ascent" + "descent" + ("Systems Thinking" OR "MAP THE HIERARCHY") in skills/conversation-mode/SKILL.md
  MULL-01: "brain_folders" + "value-proposition" + "disruptive-innovation" + "systems-thinking" + "Mullins Seven Domains" in skills/mullins-scaffold/scaffold.json; "offered" present (the Decision-Gate flag)
  MULL-02: "ackoff_traversal" + "ascent" + "descent" + "Validation" in skills/mullins-scaffold/scaffold.json
  MULL Part-8 boundary: in lib/core/mullins-scaffold.cjs the buildBrainFolderQuery defines an enum allow-list (Un-Defined/Ill-Defined/Well-Defined/Wicked) so the query payload carries only the 'Mullins Seven Domains' handle + a problem-type enum -- grep that the allow-list + 'Mullins Seven Domains' are present and that buildBrainFolderQuery does NOT interpolate an unbounded caller string into the question (the enum-guard is the structural Part-8 gate; the fixture test in mullins-scaffold.test.cjs proves the runtime behavior).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: write the doctrine-presence gate (the grep + em-dash + frozen-frontmatter assertions for OPS + PUSH)</name>
  <files>tests/test-143.2-doctrine-presence.cjs</files>
  <behavior>
    - Asserts (presence) each OPS + PUSH token from the interfaces block is found in its target file
    - Asserts (absence) the stale tokens are gone: "comes later", "Specific methodology", "one of five F sub-shapes", "What are you working on"
    - Asserts the Part-8 absolute is verbatim: "ZERO user-content egress" present in agents/larry-extended.md (not softened to "minimal" on that seam)
    - Asserts the scratchpad block is preserved: "scratchpad-ops.cjs" present in skills/conversation-mode/SKILL.md
    - Asserts zero em-dashes (the U+2014 / U+2013 chars) across the 5 touched prompt files (larry-personality, pws-methodology, ui-system, larry-extended, conversation-mode)
    - Asserts the frozen larry-extended frontmatter is byte-unchanged vs HEAD where it matters: initialPrompt + the 10 persona_variant keys + mcpServers are byte-identical to HEAD:agents/larry-extended.md (read HEAD via git show, compare the frontmatter region)
    - Prints a single PASS line and exits 0 on success; throws/exits non-zero with the failing token named on any miss
  </behavior>
  <read_first>
    - tests/test-reach-ids-drift.cjs (the git-show-HEAD + section-slice + code-span extraction pattern to mirror for the frozen-frontmatter byte-compare; reuse execFileSync('git', ['show', 'HEAD:...']))
    - tests/test-feynman-timeline-canon-part-9-invariant.cjs (the forbidden-substring sweep pattern referenced in the canon map for em-dash / leak sweeps -- mirror its structure)
    - skills/larry-personality/SKILL.md, skills/pws-methodology/SKILL.md, skills/ui-system/SKILL.md, agents/larry-extended.md, skills/conversation-mode/SKILL.md (the 5 touched files -- the targets of the presence/absence/em-dash assertions)
  </read_first>
  <action>
    Create tests/test-143.2-doctrine-presence.cjs (Node, node:assert/strict, zero deps). Read each of the 5 touched files from disk (path.resolve from __dirname/..). Build a presence table keyed by requirement id (OPS-01..05, PUSH-01..06) mapping each to {file, mustContain[], mustNotContain[]} from the interfaces anchors. Loop and assert each. Then assert zero em-dash codepoints (match the no-em-dash house rule -- scan for the em-dash and en-dash unicode chars) across all 5 files. Then assert the frozen larry-extended frontmatter: git show HEAD:agents/larry-extended.md is N/A for a same-commit compare, so instead assert the frontmatter region (between the first two "---" fences) still contains the exact initialPrompt string, all 10 persona_variant keys, and the mcpServers entry verbatim (a structural byte-presence check, since the plan forbids editing them). Structure the presence table so Task 3 can extend it with the CONV + MULL entries (export the table or keep it module-scoped and have Task 3 append to it in the same file). Print "PASS test-143.2-doctrine-presence.cjs" and exit 0; on any failed assertion, the assert message names the requirement id + token. House rule: hyphens only, no em-dashes IN THE TEST FILE ITSELF (reference the chars by codepoint (U+2014 em-dash, U+2013 en-dash) via String.fromCharCode / \u escapes, never a literal em-dash).
    No fenced code blocks in this action. The test file is code; write it directly.
  </action>
  <acceptance_criteria>
    - the test runs green AFTER plans 01-03 land: `node tests/test-143.2-doctrine-presence.cjs` exits 0 and prints PASS (the OPS + PUSH half; the CONV + MULL half is added in Task 3)
    - the test FAILS if any operate-block or push-family token is missing (the assertion messages name the requirement id)
    - the test file itself carries zero literal em-dashes (em-dash chars appear only as U+2014 / U+2013 escapes in the search set)
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-143.2-doctrine-presence.cjs</automated>
  </verify>
  <done>tests/test-143.2-doctrine-presence.cjs exists, asserts all OPS + PUSH requirement tokens present + the 4 stale tokens absent + the Part-8 absolute verbatim + scratchpad preserved + zero em-dashes across the 5 files + the frozen frontmatter intact, and exits 0 green after plans 01-03. The presence table is structured for Task 3 to extend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: extend the doctrine-presence gate with the CONV + MULL requirements + the MULL Part-8 Brain-folder boundary grep</name>
  <files>tests/test-143.2-doctrine-presence.cjs</files>
  <behavior>
    - Asserts (presence) the CONV-01 tokens in skills/conversation-mode/SKILL.md: "Lane Picker" AND ("Shape F.1" OR "SEED-020") AND "Decision Gate"
    - Asserts (presence) the CONV-02 tokens in skills/conversation-mode/SKILL.md: ("DIKW" OR "Data/Information") AND "Wisdom" AND "ascent" AND "descent" AND ("Systems Thinking" OR "MAP THE HIERARCHY")
    - Asserts (presence) the MULL-01 tokens in skills/mullins-scaffold/scaffold.json: "brain_folders" AND "value-proposition" AND "disruptive-innovation" AND "systems-thinking" AND "Mullins Seven Domains" AND "offered"
    - Asserts (presence) the MULL-02 tokens in skills/mullins-scaffold/scaffold.json: "ackoff_traversal" AND "ascent" AND "descent" AND "Validation"
    - Asserts the MULL Part-8 Brain-folder boundary in lib/core/mullins-scaffold.cjs: the buildBrainFolderQuery enum allow-list is present (all four of Un-Defined / Ill-Defined / Well-Defined / Wicked) AND 'Mullins Seven Domains' is the only framework handle in the query AND the function does NOT interpolate an unbounded caller variable directly into the question string (assert the question is built from the validated enum var, not the raw parameter -- a structural grep that the raw parameter name is not concatenated into the question; the runtime proof is delegated to mullins-scaffold.test.cjs)
    - Asserts zero em-dashes across the 2 new scope-addition surfaces (skills/mullins-scaffold/scaffold.json, skills/mullins-scaffold/SKILL.md) in addition to the 5 prompt files from Task 1 (conversation-mode already in the Task-1 set)
    - Prints the final PASS line naming the total requirement count (15: 11 OPS/PUSH + CONV-01/02 + MULL-01/02) and exits 0; throws with the requirement id named on any miss
  </behavior>
  <read_first>
    - tests/test-143.2-doctrine-presence.cjs (the Task-1 presence table to extend -- append the CONV + MULL entries; reuse the same loop + em-dash sweep)
    - skills/conversation-mode/SKILL.md (the CONV-01/02 target -- the Lane Picker + DIKW sections from Plan 05)
    - skills/mullins-scaffold/scaffold.json (the MULL-01/02 target -- brain_folders + ackoff_traversal from Plan 06)
    - lib/core/mullins-scaffold.cjs (the buildBrainFolderQuery Part-8 enum-guard from Plan 06 -- the grep target for the boundary assertion)
  </read_first>
  <action>
    Extend tests/test-143.2-doctrine-presence.cjs (do NOT create a second file). Append to the presence table four entries keyed CONV-01, CONV-02, MULL-01, MULL-02 with {file, mustContain[]} per the behavior tokens. CONV-01/02 target skills/conversation-mode/SKILL.md; MULL-01/02 target skills/mullins-scaffold/scaffold.json. For the OR-clauses (Shape F.1 OR SEED-020; DIKW OR Data/Information; Systems Thinking OR MAP THE HIERARCHY) implement an anyOf check (at least one token present) alongside the allOf mustContain check. Add the MULL Part-8 boundary block: read lib/core/mullins-scaffold.cjs, assert all four enum strings (Un-Defined, Ill-Defined, Well-Defined, Wicked) are present in the buildBrainFolderQuery region AND 'Mullins Seven Domains' is present AND the question string is assembled from the validated enum variable -- assert by grepping that the function does not concatenate the raw parameter identifier into the question (the structural Part-8 gate: the user string is replaced by the enum before it reaches the query). Add skills/mullins-scaffold/scaffold.json + skills/mullins-scaffold/SKILL.md to the em-dash sweep set. Update the final PASS line to name 15 requirements. Hyphens only in the test file (em-dash chars referenced only as U+2014 / U+2013 escapes in the search set).
    No fenced code blocks in this action. The test file is code; extend it directly.
  </action>
  <acceptance_criteria>
    - `node tests/test-143.2-doctrine-presence.cjs` exits 0 and prints a PASS line naming 15 requirements, AFTER plans 01-03 + 05 + 06 land
    - the CONV + MULL tokens fail the test if missing (the assert message names CONV-01/02 or MULL-01/02)
    - the MULL Part-8 boundary grep is a gate: the test fails if the buildBrainFolderQuery enum allow-list is absent OR if the raw caller parameter is interpolated into the query question
    - zero em-dashes across all 7 surfaces (5 prompt files + scaffold.json + mullins-scaffold SKILL.md)
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-143.2-doctrine-presence.cjs</automated>
  </verify>
  <done>the doctrine-presence test now also asserts CONV-01/02 (lane-picker + DIKW in conversation-mode) + MULL-01/02 (brain_folders + ackoff_traversal in scaffold.json) + the MULL Part-8 Brain-folder-query boundary (the enum allow-list guard in mullins-scaffold.cjs) + zero em-dashes across the 2 new scaffold surfaces; the final PASS line names 15 requirements; exits 0 green after plans 01-03 + 05 + 06.</done>
</task>

<task type="auto">
  <name>Task 2: write tests/run-all-1432.sh composing the doctrine gate + the four shipped fences + the mullins-scaffold fixture test</name>
  <files>tests/run-all-1432.sh</files>
  <read_first>
    - tests/run-all-143.sh (the runner template: SHELL_SUITES + CJS_SUITES arrays, per-suite PASS/FAIL loop, START_TIME/summary, non-zero exit on any fail, bash only / no emoji / no em-dashes)
    - tests/run-all-1431.sh (the 143.1 sibling runner -- confirm naming + invocation convention)
  </read_first>
  <action>
    Create tests/run-all-1432.sh mirroring run-all-143.sh exactly in structure. CJS_SUITES = [ test-143.2-doctrine-presence.cjs, test-reach-ids-drift.cjs, test-posture-ids-drift.cjs, test-sensors-routing-fence.cjs, test-sensors-part8-sweep.cjs, ../lib/core/mullins-scaffold.test.cjs ] (the mullins-scaffold fixture test lives under lib/core/, not tests/ -- invoke it by its path; mirror how run-all-143.sh handles any non-tests/ suite, or add an absolute/relative path entry). SHELL_SUITES empty. Header comment documents per-suite ownership: doctrine-presence = the 15 OPS/PUSH/CONV/MULL requirements landed; reach-ids-drift = no 6th reach-id (the composition spine); posture-ids-drift = exactly-3 postures; routing-fence = Phase 144 fence held; part8-sweep = sensor egress paths clean; mullins-scaffold.test = the MULL brain_folders + ackoff traversal + the Part-8 Brain-folder-query boundary. The runner runs to completion even when a suite fails, prints per-suite PASS/FAIL + a summary, exits non-zero if any failed. bash only, no emoji, no em-dashes. chmod +x.
  </action>
  <acceptance_criteria>
    - `bash tests/run-all-1432.sh` runs all 6 suites and exits 0 (all green) after plans 01-03 + 05 + 06
    - the runner composes the four shipped fences + the mullins-scaffold fixture test by name (grep the file for each suite name)
    - the file is executable and carries zero em-dashes
  </acceptance_criteria>
  <verify>
    <automated>bash tests/run-all-1432.sh</automated>
  </verify>
  <done>tests/run-all-1432.sh exists (executable, mirrors run-all-143.sh), composes the doctrine-presence test + the four shipped composition fences + the mullins-scaffold fixture test (Part-8 Brain-folder boundary), and exits 0 green after plans 01-03 + 05 + 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edited doctrine -> shipped fences | the verification proves the edits did not break the frozen-5 reach set, the routing fence, or the Part-8 sweep |
| MULL Brain-folder query -> Brain MCP | the verification proves the buildBrainFolderQuery enum allow-list guard holds (no user content reaches the query) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1432.04-01 | Tampering | a silent 6th reach-id slipping in via a push-line | mitigate | the runner re-asserts test-reach-ids-drift.cjs as the composition spine |
| T-1432.04-02 | Information Disclosure | a softened Part-8 absolute, a sensor egress regression, or a user string in the MULL Brain-folder query | mitigate | the doctrine-presence test asserts "ZERO user-content egress" verbatim + the MULL enum-guard grep; the runner re-asserts test-sensors-part8-sweep.cjs + mullins-scaffold.test.cjs |
| T-1432.04-03 | Repudiation | an operate-block, push-family, CONV, or MULL requirement silently missing | mitigate | the doctrine-presence test asserts all 15 requirement tokens present, failing with the requirement id named |
| T-1432.04-SC | Tampering | npm/pip/cargo installs | accept | no package installs (Node built-ins + bash only; zero new deps) |
</threat_model>

<verification>
- `node tests/test-143.2-doctrine-presence.cjs` exits 0 (all 15 requirements present, stale tokens gone, Part-8 verbatim + the MULL enum-guard, scratchpad preserved, zero em-dashes across 7 surfaces, frozen frontmatter intact)
- `bash tests/run-all-1432.sh` exits 0 (doctrine-presence + the 4 shipped fences + the mullins-scaffold fixture test all green)
- the four shipped fences individually: reach-drift PASS, posture-drift PASS, routing-fence 0-exit, part8-sweep 0-exit; the mullins-scaffold fixture test PASS
</verification>

<success_criteria>
ROADMAP success criterion 3 (COMPOSITION HELD) is satisfied and machine-checked: zero new reach-ids (the 5-reach drift test green), no new sensor, team_perspective/whitespace are render labels, the Part-8 sweeps + routing fence green, the MULL Brain-folder enum-guard holds, the frozen frontmatter byte-unchanged, zero em-dashes. Criteria 1, 2, 4 are re-asserted by the doctrine-presence grep gate, plus the scope-addition CONV-01/02 + MULL-01/02 success criteria (the lane-picker + DIKW mapping + the brain_folders + Ackoff traversal + the Part-8 Brain-folder boundary).
</success_criteria>

<output>
Create `.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-04-SUMMARY.md` when done.
</output>
