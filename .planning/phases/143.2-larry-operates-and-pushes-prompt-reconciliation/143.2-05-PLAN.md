---
phase: 143.2-larry-operates-and-pushes-prompt-reconciliation
plan: 05
type: execute
wave: 2
depends_on: ["143.2-03"]
files_modified:
  - skills/conversation-mode/SKILL.md
autonomous: true
requirements: [CONV-01, CONV-02]
canon_parts: [Part 2, Part 3, Part 8, Part 10]

must_haves:
  truths:
    - "conversation-mode surfaces the no-room lane decision (just-chat / brainstorm / build-a-room) as an EXPLICIT Shape F.1 selector -- a Decision Gate, not a silent classifier"
    - "The 3 lanes map to Ackoff DIKW positions: chat=Data/Information, brainstorm=Knowledge, build=Wisdom/Understanding (the UP ascent); the DOWN descent = a built artifact returns to validate its assumptions/data"
    - "When the lane crosses into build, Larry offers the Brain chain (Ackoff Pyramid FEEDS_INTO Systems Thinking + MAP THE HIERARCHY) with generic handles only -- the offer is a Decision Gate, never auto-routed"
  artifacts:
    - path: "skills/conversation-mode/SKILL.md"
      provides: "the explicit lane-picker (CONV-01, Shape F.1 / SEED-020) + the DIKW mapping with the bidirectional ascent/descent (CONV-02), reusing the larry-personality Ackoff doctrine; the shipped 3-mode behavior + scratchpad block + the Plan-03 Conversational Reaches note preserved"
      contains: "Lane Picker"
  key_links:
    - from: "skills/conversation-mode/SKILL.md the lane-picker"
      to: "Canon Part 3 Shape F.1 / SEED-020 (the universal AskUserQuestion selector)"
      via: "the lane decision renders as a Shape F.1 Decision Gate, not an implicit persona classifier"
      pattern: "Shape F.1|SEED-020|Decision Gate"
    - from: "skills/conversation-mode/SKILL.md the DIKW mapping"
      to: "skills/larry-personality/SKILL.md the bidirectional Ackoff ascent doctrine"
      via: "the chat/brainstorm/build = Data/Knowledge/Wisdom ascent + the build->validate descent reuse the existing posture doctrine, additive"
      pattern: "DIKW|ascent|descent|Wisdom"
---

<objective>
Make conversation-mode the EXPLICIT lane-picker traffic cop and wire it to the Ackoff DIKW bidirectional position. This is prompt/doctrine -- markdown edits to skills/conversation-mode/SKILL.md only. No code.

1. CONV-01: today the no-room lane (Just Talk / Explore+Capture / Build a Room) is an IMPLICIT classifier inferred from persona signals. Make the lane decision an EXPLICIT Shape F.1 selector -- "are we just chatting, brainstorming, or building something?" -> the navigator picks the lane. Reuse the Shape F.1 primitive (SEED-020 -- the shipped AskUserQuestion card-selector); the lane-pick is a Decision Gate, NOT a silent classification.
2. CONV-02: map the 3 lanes to Ackoff DIKW. chat = Data/Information (low structure), brainstorm = Knowledge (divergent synthesis), build = Wisdom/Understanding (convergent artifact) -- the UP-ascent. The DOWN-descent = a built artifact returns to validate its assumptions/data (the honesty loop: building does not skip validation). Wire the lane-picker to read/advance the DIKW position; offer the Brain chain (Ackoff Pyramid FEEDS_INTO Systems Thinking + MAP THE HIERARCHY) when the lane crosses into build. Reuse the existing larry-personality bidirectional Ackoff ascent doctrine; do NOT contradict it. Part 8: generic framework handles only to the Brain.

Purpose: the lane is a navigator decision (GUIDED default, Canon Part 3), not a silent guess; the DIKW position makes "where are we in the thinking" legible and Brain-chained at the build crossing.
Output: the edited conversation-mode/SKILL.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-CONTEXT.md
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-CONTEXT-ADDENDUM-conv-mull-ackoff.md

<interfaces>
<!-- The shipped conversation-mode surface this plan edits. Reuse + preserve; do not rebuild. -->
From skills/conversation-mode/SKILL.md (the shipped 3-mode skill):
  Mode 1: Just Talk      -- pure thinking partner, no filing, one upgrade offer/session
  Mode 2: Explore+Capture -- thinking partner + pattern detector, banks to ~/.mindrian/scratchpad.json via bank-opportunity CLI
  Mode 3: Build a Room    -- invokes /mos:new-project
  Persona Detection + Framework Chain Selection (TTO / Researcher / Business chains)
  Scratchpad Persistence block -- ~/.mindrian/scratchpad.json + scratchpad-ops.cjs (PRESERVE byte-intact; the Plan-03 OPS-05 note also lands here)
  NOTE: Plan 03 (wave 1, OPS-05) adds a "Conversational Reaches" note to this same file. This plan (wave 2) lands AFTER 03 -- preserve that note; do not remove the scratchpad CLI block.

From .planning/seeds/SEED-020-shape-f-is-the-universal-mindrian-ui.md:
  Shape F = the host-native AskUserQuestion card-selector (label + description per card, arrow-navigable, Enter to select). It IS the Mindrian UI for ANY chooser. F.1 = Next Move (3-5 options, the default after a discuss chunk). The lane-picker is exactly a Shape F.1 block.

From skills/larry-personality/SKILL.md (the bidirectional Ackoff ascent doctrine -- REUSE, do not contradict):
  The posture is the bidirectional Usher traversal: push_forward = "the bidirectional Ackoff ascent confirms confidence is backed"; pull_back = step-4 gap re-tests assumptions / None-tier evidence near commit. The ASCENT earns evidence up the levels; the DESCENT (pull_back) decomposes back to re-test data. The lane DIKW ascent (chat->brainstorm->build) and the build->validate descent MUST align with this.

From references/brain/query-patterns.md pattern 1 (brain_framework_chain):
  Tool: mcp__mindrian-brain__brain_ask; FEEDS_INTO chain via next_gate.options[].framework; Part-8 contract = framework handles + problem-type enum ONLY, never user content. The build-crossing offer (Ackoff FEEDS_INTO Systems Thinking + MAP THE HIERARCHY) is this pattern with generic handles.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: add the explicit Shape F.1 Lane Picker (CONV-01) + the Ackoff DIKW bidirectional mapping (CONV-02) to conversation-mode</name>
  <files>skills/conversation-mode/SKILL.md</files>
  <read_first>
    - skills/conversation-mode/SKILL.md (the full shipped file -- the 3 modes, Persona Detection, Framework Chain Selection, Scratchpad Persistence block; PRESERVE the scratchpad-ops.cjs block + any Plan-03 Conversational Reaches note byte-intact)
    - .planning/seeds/SEED-020-shape-f-is-the-universal-mindrian-ui.md (the Shape F.1 AskUserQuestion selector primitive to reuse for the lane-picker)
    - skills/larry-personality/SKILL.md lines 64-95 (the Hierarchical Navigator / bidirectional Ackoff ascent doctrine the DIKW mapping must align with, not contradict)
    - .planning/phases/143.2-.../143.2-CONTEXT-ADDENDUM-conv-mull-ackoff.md (CONV-01/02 design + the DIKW->lane mapping + the build-crossing Brain chain)
  </read_first>
  <action>
    Edit skills/conversation-mode/SKILL.md. This is additive doctrine -- the 3 shipped modes, Persona Detection, Framework Chain Selection, and the Scratchpad Persistence block (with the scratchpad-ops.cjs lines) stay; the Plan-03 Conversational Reaches note (if present) stays. Add two sections.

    (a) CONV-01 -- "## Lane Picker (Shape F.1 -- the explicit traffic cop)" near the top, before "## Mode 1: Just Talk". State: at the start of a no-room session (and whenever the lane is ambiguous), Larry does NOT silently classify the user's persona into a mode. He surfaces an EXPLICIT Shape F.1 selector (the SEED-020 AskUserQuestion card-selector, reuse renderShapeF1 / the host primitive -- no bespoke widget) with three lane cards: "Just chatting" (-> Mode 1 Just Talk), "Brainstorming" (-> Mode 2 Explore+Capture), "Building something" (-> Mode 3 Build a Room). Each card carries a one-line description. The navigator picks the lane; the pick is a Decision Gate (Canon Part 3, GUIDED default), never an inferred classification. Persona Detection still runs WITHIN the chosen lane to shape Larry's questions (not to pick the lane). Note: the implicit "offer to upgrade" line in Mode 1 and the "I am ready to build" transition in Mode 2 are the SAME lane-picker re-surfaced -- a re-pick, not a silent switch. One re-surface per turn-cluster; never auto-switch lanes.

    (b) CONV-02 -- "## Lanes as Ackoff DIKW position (bidirectional)" after the Lane Picker section. Map: chat = Data/Information (low structure, raw thinking); brainstorm = Knowledge (divergent synthesis, patterns banked); build = Wisdom/Understanding (convergent artifact, the room). This is the UP-ascent (Data -> Information -> Knowledge -> Wisdom). The DOWN-descent: a built artifact returns to validate its assumptions and data -- building does NOT skip validation; the descent is the honesty loop. State that this REUSES the larry-personality bidirectional Ackoff ascent doctrine (the push_forward ascent / pull_back descent posture) -- it is the same traversal named at the conversation surface, additive, not a contradiction. The lane-picker reads/advances the DIKW position: when the navigator picks "Building" (the lane crosses into build = Wisdom), Larry OFFERS the Brain chain -- Ackoff Pyramid FEEDS_INTO Systems Thinking + MAP THE HIERARCHY -- as a Decision Gate offer (never auto-routed), carrying generic framework handles only (Part 8: framework names + problem-type enum, never the user's artifacts or content). When a built artifact later fails validation (None-tier evidence, contradiction), Larry surfaces the descent: drop back a DIKW level to re-test the data before re-ascending. Hyphens only, no em-dashes.

    Do NOT add code. Do NOT remove the scratchpad block or the Plan-03 note. Do NOT contradict the larry-personality Ackoff doctrine.
  </action>
  <acceptance_criteria>
    - the lane-picker landed: `grep -c "Lane Picker" skills/conversation-mode/SKILL.md` >= 1 AND `grep -c "Shape F.1\|SEED-020" skills/conversation-mode/SKILL.md` >= 1
    - the DIKW mapping landed: `grep -c "DIKW\|Data/Information\|Wisdom" skills/conversation-mode/SKILL.md` >= 1 AND both "ascent" and "descent" present (`grep -c "ascent" ...` >=1 and `grep -c "descent" ...` >=1)
    - the build-crossing Brain chain present: `grep -c "Systems Thinking\|MAP THE HIERARCHY" skills/conversation-mode/SKILL.md` >= 1
    - Decision-Gate-not-silent gate: `grep -c "Decision Gate" skills/conversation-mode/SKILL.md` >= 1
    - the shipped surface preserved: `grep -c "scratchpad-ops.cjs" skills/conversation-mode/SKILL.md` >= 1 (the scratchpad block intact)
    - zero em-dashes: `grep -cP '\xe2\x80\x94|\xe2\x80\x93' skills/conversation-mode/SKILL.md` returns 0
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=skills/conversation-mode/SKILL.md; grep -q "Lane Picker" "$f" && grep -qE "Shape F.1|SEED-020" "$f" && grep -qE "DIKW|Data/Information|Wisdom" "$f" && grep -q "ascent" "$f" && grep -q "descent" "$f" && grep -qE "Systems Thinking|MAP THE HIERARCHY" "$f" && grep -q "Decision Gate" "$f" && grep -q "scratchpad-ops.cjs" "$f" && [ "$(grep -cP "\xe2\x80\x94|\xe2\x80\x93" "$f")" = "0" ] && echo PASS'</automated>
  </verify>
  <done>conversation-mode/SKILL.md carries the explicit Shape F.1 Lane Picker (CONV-01, a Decision Gate not a silent classifier) + the Ackoff DIKW bidirectional mapping (CONV-02, chat/brainstorm/build = Data/Knowledge/Wisdom ascent + the build->validate descent) reusing the larry-personality Ackoff doctrine, with the build-crossing Brain chain (Ackoff FEEDS_INTO Systems Thinking + MAP THE HIERARCHY, generic handles only); the scratchpad block + Plan-03 note preserved; zero em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lane decision -> mode routing | the lane-pick crosses from an inferred classification to an explicit Decision Gate; the navigator owns the pick (GUIDED default) |
| build-crossing offer -> Brain MCP | the Ackoff FEEDS_INTO chain offer queries the Brain with generic framework handles only -- never the user's conversation content (Part 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1432.05-01 | Elevation of Privilege | a lane silently auto-switched instead of navigator-picked | mitigate | the doctrine states the lane is a Shape F.1 Decision Gate (GUIDED default, Canon Part 3); never auto-switch -- re-surface the picker; the presence test asserts "Decision Gate" present |
| T-1432.05-02 | Information Disclosure | the build-crossing Brain chain leaking conversation content | mitigate | the doctrine states the offer carries generic framework handles only (framework names + problem-type enum), Part 8; the Brain chain is an OFFER (Decision Gate), not a fetch of user content |
| T-1432.05-03 | Tampering | the additive sections breaking the shipped scratchpad / Plan-03 note | mitigate | the verify asserts scratchpad-ops.cjs present; the action forbids removing the scratchpad block or the Conversational Reaches note |
| T-1432.05-SC | Tampering | npm/pip/cargo installs | accept | no package installs (markdown doctrine edit only; zero deps) |
</threat_model>

<verification>
- the lane-picker + DIKW mapping + build-crossing Brain chain + Decision-Gate framing all present (the grep gate above)
- the shipped scratchpad block + the Plan-03 Conversational Reaches note preserved
- Part 8: the build-crossing Brain offer carries generic framework handles only
- Ackoff additive: the DIKW ascent/descent reuses the larry-personality doctrine, no contradiction
- zero em-dashes
</verification>

<success_criteria>
CONV-01 lands: conversation-mode surfaces the no-room lane decision as an explicit Shape F.1 selector (a Decision Gate, not a silent classifier), reusing the SEED-020 Shape F primitive. CONV-02 lands: the 3 lanes map to Ackoff DIKW (chat=Data/Information, brainstorm=Knowledge, build=Wisdom) as the UP-ascent, with the build->validate DOWN-descent, reusing the larry-personality bidirectional Ackoff ascent doctrine additively; the build-crossing offers the Brain chain (Ackoff FEEDS_INTO Systems Thinking + MAP THE HIERARCHY) with generic handles only.
</success_criteria>

<output>
Create `.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-05-SUMMARY.md` when done.
</output>
