---
phase: 143.2
phase_name: Larry Operates And Pushes (Prompt Reconciliation)
gathered: 2026-06-07
status: Ready for planning
source: Two 11-agent fan-out audits (AUDIT-1-tui-operation.json + AUDIT-2-proactive-push.json in this dir). They ARE the research/spec. No Q&A discuss round.
canon_parts: [Part 2, Part 2 Engine 1, Part 3, Part 8, Part 9, Part 10]
requirements: [OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, PUSH-01, PUSH-02, PUSH-03, PUSH-04, PUSH-05, PUSH-06]
---

# Phase 143.2: Larry Operates And Pushes (Prompt Reconciliation) - Context

**Gathered:** 2026-06-07
**Source:** Two read-only fan-out audits (this dir: AUDIT-1-tui-operation.json verdict `needs-reconciliation`; AUDIT-2-proactive-push.json verdict `partial`). They are the authoritative spec.

<domain>
## Phase Boundary

Larry's prompt carries the Phase-141 capability-dial DOCTRINE (5 reaches, 3 postures, the Usher Navigator) but is NOT updated to OPERATE the now-shipped 142/143/143.1 machinery, and is only PARTIALLY wired to PROACTIVELY PUSH the Engine-1/Engine-2 reaches. This phase reconciles Larry's prompt surfaces so that when Phase 144 flips the engine on, Larry both (a) DRIVES the surfaced reaches (operate the dial-TUI) and (b) REACHES FIRST at the right triggers (the push doctrine). It is the Canon Part 10 conversational surface for the LARRYREACH milestone.

**This is a PROMPT/DOCTRINE reconciliation - NOT code.** It edits SKILL.md / agent markdown. The reaches, sensors, dial-TUI, and engine are already built (142/143/143.1) or planned (144). This phase makes the prompt MATCH the shipped contract.

**MUST land before Phase 144 flips the engine** (AUDIT-1): otherwise "the engine fires real sensor reaches into a prompt that still calls them hypothetical future work." Phase 144 depends on 143.2.

**IN SCOPE:** OPS-01..05 (operate) + PUSH-01..06 (push) - prompt edits across larry-personality, ui-system, larry-extended, pws-methodology, conversation-mode.

**OUT OF SCOPE:** the engine flip itself (Phase 144); any code change to the sensors/dial/engine; minting any new reach-id or sensor.
</domain>

<critical_constraint>
## The composition constraint (do NOT violate - it breaks shipped drift tests)

The reach-id set is FROZEN at EXACTLY 5 (`context_block, contradiction, cross_room, brain_consult, deep_research`) - asserted by a drift test AND by Phase 143 `lib/core/sensors/sensor-types.cjs` REACH_IDS (POSTURE_IDS frozen at 3). **This phase must NOT mint a 6th reach-id.** Every push-line composes by mapping a push-family onto an EXISTING reach-id + an EXISTING Phase-143 sensor. `team_perspective` and `whitespace` are RENDER LABELS, never reach-ids.

- Each push-line = the OFFER (surface) + the sensor = the WHEN (fire) + the reach-id = the WHAT + the posture (push_forward/hold/pull_back) set by the reach RESULT.
- The two dials stay orthogonal (Reach rule 7): Capability dial = internal action-selection; Ask-Tell dial = external grounding intensity.
- The push-lines are OFFER-level (Larry surfaces -> Decision Gate), NOT ROUTING-level (the engine flip is Phase 144). This is what makes 143.2 safe to land before 144: push-lines work now; only the `routing_source` operate-note tracks 144.
</critical_constraint>

<decisions>
## Requirements (LOCKED from the two audits)

### OPERATE half (AUDIT-1) - make Larry drive the shipped TUI
- **OPS-01 (MUST-before-144):** In `skills/larry-personality/SKILL.md`, flip the stale "comes later" language. Line ~95 ("the executable sensors ride Phase 143... the engine rides Phase 144... the code that enforces it comes later") -> NOW-SHIPPED: sensors SHIPPED (Phase 143, lib/core/insight-sensors.cjs SENS-01..07), dial-TUI SHIPPED (Phase 143.1), Phase 144 wires dispatchSensors into decide() (executing). Line ~46 forward-reference -> name the shipped files. This is the flip-critical edit.
- **OPS-02 (MUST-before-144):** Add a compact "Operating the Dial (shipped)" block after line 95 (NOT a UI-SPEC re-paste): sensors auto-fire -> candidate reaches keyed to the frozen 5 reach-ids; the dial surfaces ranked reaches via the existing Shape F.1 selector - resting-detent = implicit sync (SELECTED_REACH), rotate-off-recommended = pivot (PIVOTED + SELECTED_REACH), defer/reject = no committed reach, free-text = recordSelectorMiss; all four outcomes route ONLY through navigation.cjs; row labels are the Feynman-JTBD WHAT-THEY-GET aliases (canonical_verb persists to the edge, not the screen).
- **OPS-03 (MUST-before-144):** Add a 4-line "Reading routing_source" note: when the engine populates fire_skill, the router (skill-activation-router.cjs Rule 1) returns source=engine; this is a CONSEQUENCE of a sensor firing, NOT a separate command Larry obeys - his behavioral change is the posture the reach implies. Part 8: source=engine carries zero user-content egress. (Frame 144 status honestly - the engine routes once 144 lands.)
- **OPS-04 (should):** In `skills/ui-system/SKILL.md`, add Shape F.7 "Dial-TUI Capability Selector (Phase 143.1)" after the F.6 block: 5 render states S1-S5, the confidence-column dial (NN%/-- ; filled triangle = RECOMMENDED, empty = standard; zero markers in Mode B/Tier 0 is INTENTIONAL), the frozen 0.70/0.15 gate as a DIAL-ONLY variant (does NOT lower the F.0-F.6 gate), the two-K separation (DIAL_REACH_K=5 preview / MAX_K=3 chooser), the 5 reach-ids as an F.7 specialization (NOT a replacement for the 10 canonical verbs), SELECTED_REACH/PIVOTED close mechanics, SEED-020 (no bespoke widget; reuses renderShapeF1). FIX the off-by-one: "seven sub-shapes (F.0-F.6)" is SIX; "one of five F sub-shapes" is wrong. Correct counts (and extend the Quick Reference SHAPES line to F.0-F.7).
- **OPS-05 (should):** In `agents/larry-extended.md`: flip stale "future Phase 100/118" framing on SHIPPED features (109/115 are live) to operating-baseline wording; add a short "Operating the machinery (see larry-personality skill)" pointer (do NOT duplicate the SKILL.md instructions); DELETE the stale line-30 greeting ("I'm Larry. What are you working on?") that contradicts the shipped initialPrompt ("What decision is stuck?"). Keep FROZEN: initialPrompt + 10 persona_variants + the dual-path detector contract + the Part-8 "ZERO user-content egress" phrase verbatim. In `skills/conversation-mode/SKILL.md` (nice-to-have, same pass): add a "Conversational Reaches" note that the 5 reaches + 7 sensors also operate in no-room sessions via the same Shape F.1 selector with Tier-0 degradation. Do NOT remove the shipped scratchpad CLI block.

### PUSH half (AUDIT-2) - make Larry reach first at the right trigger. ALL compose under existing reach + sensor; each ends at a Decision Gate (GUIDED default - the OFFER, never a verdict).
- **PUSH-01 (Brain consult, already good - annotate):** trigger SENS-03 (framework named OR methodology step needs next move) AND local graph has no governing answer -> Larry surfaces ONE line offering the Brain framework chain, renders via the **Brain says** trace, ends at a Decision Gate. Annotate the brain_consult Capability Dial row (line 41) + a one-line PROACTIVE note under Reach rule 1. Part 8: generic handles only; the push is the OFFER not the fetch.
- **PUSH-02 (Reverse Salient, weak today):** trigger SENS-02 (lagging-component shape "the bottleneck is..." OR one section lags siblings) -> Larry surfaces the reverse-salient finding (Hughes 1983) BEFORE being asked, posture `pull_back`, routes to the Decision Gate. Add a Provoked-table row near line 245 naming the trigger shape explicitly (the current "Bottlenecks -> Specific methodology" is too vague to fire). autonomous_safe local rs engine; respect the Intelligence Hierarchy (Bottlenecks #2, after Tensions).
- **PUSH-03 (HSI/whitespace, ABSENT):** trigger 20+ artifacts AND a whitespace zone surprise >=0.7 (SENS-06 cascade recompute) -> add Provoked rows (`/mos:whitespace`, `/mos:score-innovation`) + Reach-rule note 7e distinguishing HSI ("novel pattern in what we HAVE") from whitespace ("what the room ISN'T thinking about"). HARD: the "Best after 20+ entries" gate (thin-room push fires a silent-zero - the Phase 88.6 bug). LOCAL Python; no egress.
- **PUSH-04 (find-analogies/connections):** trigger a tension with no in-domain resolution OR two+ domains live (SENS-01/SENS-06) -> Larry surfaces the cross-domain offer ("most breakthroughs are borrowed structure"). Extend the Provoked table (line 244) with a find-connections (Brain) / find-analogies (local) row; wire pws-methodology DbA detection signals (line 27) to the PUSH. Part 8: find-connections = generic handles only; find-analogies --external = public SIGNAL + MCP-stack-ask gate (no silent web).
- **PUSH-05 (Six-Hats/BONO, ABSENT):** trigger team-stuck-in-one-perspective (CONTRADICTS edges / circular / decision point / jargon spike), SENS-05/SENS-07 -> add a Provoked row (`/mos:think-hats` serial or `/mos:persona --parallel`) + wire the 5 Appendix-E handoff triggers as surface lines (risk->Black, evidence-thin->White, plan-without-owner->Blue, stuck->Red, jargon->Green), each surfaced as the canon's single line "[Incoming member] is ready. Accept, reshape, or stay?". Add a "BONO Team Perspective" section in pws-methodology carrying the 5 handoffs. Part 8: hat-sequence selection carries problem-type+complexity enums only. team_perspective is a RENDER LABEL, not a reach-id. The push lives in Larry's doctrine, NOT the (reactive) persona-analyst agent.
- **PUSH-06 (hat-scoped deep research, named-but-no-trigger):** add a "### Deep Research Escalation Explicit Triggers" subsection after the deep_research row (line 42) / Reach rule 6 with the 3 fire conditions verbatim: (a) cheap-layer-thin (local->Brain->shallow-web ladder exhausted); (b) load-bearing claim Practitioner/None evidence AND venture_stage in {Well-Defined, Ready to Build}; (c) the active BONO hat affords it. Then the surface language + the hat-scoped PLAN as a Decision Gate (White=arxiv/data, Green=patents+deep-research, Black=failure-cases) - ONLY on APPROVE does /mos:research fire. deep_research is the SANCTIONED exception to one-reach-per-beat (Reach rule 6); plan-gating non-negotiable; Part 8 + MCP-stack-ask.

## Hard constraints (plan gates)
- NO 6th reach-id (the 5-reach drift test stays green); no new sensor.
- Every push ends at a Decision Gate, never a verdict (GUIDED default; push-vs-pull not conflated).
- One-reach-per-beat preserved; the Intelligence Hierarchy (Tensions > Bottlenecks > HSI > Convergences > Blind Spots) arbitrates simultaneous triggers; deep_research is the ONLY sanctioned multi-reach exception.
- Part 8: SENS-03/04 + find-connections are the egress paths - generic handles only; the Phase 90 5-tripwire sweep + test-sensors-part8-sweep (8/8) + test-sensors-routing-fence (2/2) stay green.
- Thin-room gate on HSI/whitespace (20+ artifacts).
- No em-dashes (hyphens only). Tri-Polar (CLI/Desktop/Cowork). Preserve the 141 doctrine + the frozen frontmatter (initialPrompt, persona_variants, dual-path).
- Push-lines stay OFFER-level (safe before 144); the routing_source note frames 144 status honestly.
</decisions>

<canonical_refs>
## Canonical References
- .planning/phases/143.2-.../AUDIT-1-tui-operation.json (the operate-half spec, per-surface edits with file:line).
- .planning/phases/143.2-.../AUDIT-2-proactive-push.json (the push-half spec, the 6 push-lines + triggers + the composition constraint + risks).
- skills/larry-personality/SKILL.md (THE prompt - the capability dial ~32-95, the Provoked table ~244-264, the Intelligence Hierarchy, the reach rules).
- skills/ui-system/SKILL.md (the Shape F catalog ~40-381 - add F.7, fix counts).
- agents/larry-extended.md (frontmatter frozen; line 30 stale; the pointer).
- skills/pws-methodology/SKILL.md (DbA ~27; the BONO section home).
- skills/conversation-mode/SKILL.md (the no-room surface).
- docs/MINDRIAN-CANON.md Part 2 (Engine 1/Engine 2 + Appendix E handoff triggers), Part 3 (Decision Gate / Shape F), Part 8, Part 10.
- .planning/seeds/SEED-020-shape-f-is-the-universal-mindrian-ui.md.
- lib/core/sensors/sensor-types.cjs (the frozen 5 REACH_IDS - the drift anchor).
</canonical_refs>

<specifics>
## Specific Ideas
- OPS-01/02/03 are the must-before-144 core; everything else is should/nice in the same pass.
- The whole phase is additive doctrine on top of already-shipped reach + sensor halves - the push-lines are the missing SURFACE layer, not new capability.
- Verification is doctrine-shaped: assert the 5-reach drift test + the Part-8 sweeps + the routing fence stay green after the edits, assert zero em-dashes, assert the frozen frontmatter is byte-unchanged, and assert each push-family + the operate-block are present in the prompt (a grep-level presence test).
</specifics>

<deferred>
## Deferred Ideas
- The engine flip itself -> Phase 144 (depends on 143.2).
- Any code change to sensors/dial/engine -> not this phase.
- The broader /mos:help + command-menu Shape F sweep -> SEED-020 follow-up (the /mos:help selector already shipped this session).
</deferred>

---

*Phase: 143.2-larry-operates-and-pushes-prompt-reconciliation*
*Context synthesized 2026-06-07 from the two fan-out audits*
