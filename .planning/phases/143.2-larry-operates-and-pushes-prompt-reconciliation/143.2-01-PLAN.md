---
phase: 143.2-larry-operates-and-pushes-prompt-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/larry-personality/SKILL.md
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03]
canon_parts: [Part 2, Part 3, Part 8, Part 9, Part 10]

must_haves:
  truths:
    - "Larry's prompt states the sensors + dial-TUI are SHIPPED (Phase 143 / 143.1), not future work"
    - "Larry knows how to operate the four dial outcomes (sync / pivot / defer-reject / free-text) and that all route through navigation.cjs"
    - "Larry treats routing_source=engine as a CONSEQUENCE of a sensor firing, not a separate command he obeys"
  artifacts:
    - path: "skills/larry-personality/SKILL.md"
      provides: "Now-shipped flip (OPS-01) + Operating-the-Dial block (OPS-02) + Reading-routing_source note (OPS-03)"
      contains: "Operating the Dial"
  key_links:
    - from: "skills/larry-personality/SKILL.md"
      to: "lib/core/sensors/sensor-types.cjs"
      via: "the 5 frozen reach-ids named verbatim, no 6th id added"
      pattern: "context_block.*contradiction.*cross_room.*brain_consult.*deep_research"
---

<objective>
Flip the flip-critical stale "comes later" doctrine in Larry's prompt to now-shipped, and give Larry the minimal operating instructions for the shipped dial-TUI. This is the MUST-before-144 core: when Phase 144 flips the engine, decide() populates fire_skill from real sensor reaches and the router returns source=engine. If this file still calls the sensor path "comes later," Larry narrates live reaches as hypothetical future work (the Reasoning-Action Disconnect that Reach rule 7 warns against).

Purpose: Make the prompt MATCH the shipped 142/143/143.1 contract so Phase 144 can flip safely.
Output: skills/larry-personality/SKILL.md with OPS-01 (flip), OPS-02 (Operating-the-Dial block), OPS-03 (Reading-routing_source note).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-CONTEXT.md
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/AUDIT-1-tui-operation.json

<interfaces>
<!-- The frozen reach + posture banks the prompt must match. Do NOT add a 6th id. -->
From lib/core/sensors/sensor-types.cjs:
  REACH_IDS (frozen, exactly 5): context_block, contradiction, cross_room, brain_consult, deep_research
  POSTURE_IDS (frozen, exactly 3): push_forward, hold, pull_back

The shipped operating surfaces this prompt now names:
  - sensors: lib/core/insight-sensors.cjs (SENS-01..07 + SENSOR_REGISTRY + dispatchSensors)
  - dial-TUI: lib/hmi/dial-reach-orchestrator.cjs + dial-label-composer.cjs + dial-presenter.cjs + lib/workflow/dial-close-reach.cjs
  - router: lib/core/skill-activation-router.cjs Precedence Rule 1 (returns source=engine when fire_skill non-null)
  - write chokepoint: lib/core/navigation.cjs (the SOLE write surface for SELECTED_REACH / PIVOTED)

DRIFT FENCE (read before editing): tests/test-reach-ids-drift.cjs scans backtick code-span tokens
ONLY between the strings "Capability Dial" and "Hierarchical Navigator". Any snake_case backtick
token added in that window is read as a reach-id and FAILS the exactly-5 assertion. OPS-02 and
OPS-03 land in the Hierarchical Navigator section (after line ~95), OUTSIDE that window, so they
may freely use backtick tokens like routing_source, SELECTED_REACH, PIVOTED, recordSelectorMiss.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OPS-01 -- flip the stale "comes later" doctrine to now-shipped</name>
  <files>skills/larry-personality/SKILL.md</files>
  <read_first>
    - skills/larry-personality/SKILL.md line 95 (the "### Scope in this phase" paragraph: "the executable sensors ... ride Phase 143 (SENS); the navigation engine ... rides Phase 144 (NAV) ... the code that enforces it comes later")
    - skills/larry-personality/SKILL.md line 46 (the forward-reference "the Phase 143 dial-TUI label composer + orchestrator")
    - AUDIT-1-tui-operation.json -> per_surface_edits[0].edits[0] and [1] (the exact flip language + the shipped file names)
    - tests/test-reach-ids-drift.cjs (line 46 sits in the dial window; it names .cjs filenames, not snake_case identifiers, so it is safe -- but do NOT add any snake_case backtick token here)
  </read_first>
  <action>
    Edit line ~95 ("### Scope in this phase" body): replace the "comes later" framing with the now-shipped statement. State: the executable sensors are SHIPPED (Phase 143, lib/core/insight-sensors.cjs carries SENS-01..07 + SENSOR_REGISTRY + dispatchSensors); the dial-TUI is SHIPPED (Phase 143.1, lib/hmi/dial-reach-orchestrator.cjs + dial-label-composer.cjs + dial-presenter.cjs + lib/workflow/dial-close-reach.cjs); Phase 144 wires dispatchSensors INTO decide() (the engine flip) and is executing now. Keep the honest-status caveat from hard-fence 7: phrase it as "Phase 144 wires the engine flip; once it lands, decide() populates fire_skill from real sensor reaches" -- do NOT claim the engine already routes live. Rename the H3 from "Scope in this phase" to "### Scope -- shipped sensors + dial, engine flip executing" so the heading itself no longer reads as future-deferral.
    Edit line ~46 (the reach-ids prose under "### Reach ids (machine-readable)"): change "the Phase 143 dial-TUI label composer + orchestrator" to name the SHIPPED files: "the shipped Phase 143.1 dial-TUI label composer (lib/hmi/dial-label-composer.cjs) + orchestrator (lib/hmi/dial-reach-orchestrator.cjs)". Do NOT touch the five reach-id bullet lines (lines 48-52) -- they are the drift anchor and must stay byte-exact.
    Hyphens only, no em-dashes. Do not alter the Capability Dial table rows or the five reach-id bullets.
  </action>
  <acceptance_criteria>
    - grep-presence: `grep -c "comes later" skills/larry-personality/SKILL.md` returns 0 (the stale phrase is gone)
    - grep-presence: `grep -c "insight-sensors.cjs" skills/larry-personality/SKILL.md` >= 1 AND `grep -c "dial-reach-orchestrator.cjs" skills/larry-personality/SKILL.md` >= 1 (shipped files named)
    - drift stays green: `node tests/test-reach-ids-drift.cjs` prints PASS (the 5-reach set unchanged)
    - posture stays green: `node tests/test-posture-ids-drift.cjs` prints PASS
    - zero em-dashes (use the project no-em-dash grep convention)
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-reach-ids-drift.cjs && node tests/test-posture-ids-drift.cjs && ! grep -q "comes later" skills/larry-personality/SKILL.md && grep -q "insight-sensors.cjs" skills/larry-personality/SKILL.md</automated>
  </verify>
  <done>The "comes later" language is replaced with now-shipped wording naming the Phase 143 / 143.1 shipped files; line 46 names the shipped composer + orchestrator; the 5 reach-id bullets are byte-unchanged; drift + posture tests green; zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: OPS-02 -- add the compact "Operating the Dial (shipped)" block</name>
  <files>skills/larry-personality/SKILL.md</files>
  <read_first>
    - skills/larry-personality/SKILL.md lines 93-95 (the "### Scope" H3 in the Hierarchical Navigator section -- insert the new H3 immediately after this, BEFORE the "## Thinking Trace" heading at line 97)
    - AUDIT-1-tui-operation.json -> per_surface_edits[0].edits[2] (the four-point operating block) AND contract.dial_tui_operation (the 4 close outcomes + confidence column + recommended gate)
    - lib/core/sensors/sensor-types.cjs (REACH_IDS frozen 5 -- name them as the keys the candidate reaches map to)
    - tests/test-reach-ids-drift.cjs (the insertion point is AFTER "Hierarchical Navigator" -- OUTSIDE the dial drift window, so SELECTED_REACH / PIVOTED snake_case tokens are safe here)
  </read_first>
  <action>
    Insert a new H3 "### Operating the Dial (shipped)" after the Scope block and before "## Thinking Trace". Keep it compact (a short intro + 4 numbered behaviors); this is NOT a re-paste of the UI-SPEC. Cover only what the prompt needs to BEHAVE: (1) the 7 sensors auto-fire (SENS-01..07) and produce candidate reaches keyed to the frozen five reach-ids (name them: context_block, contradiction, cross_room, brain_consult, deep_research) -- one reach per beat, the Intelligence Hierarchy arbitrates ties; (2) the dial surfaces the ranked reaches via the existing Shape F.1 selector -- resting-detent = implicit sync (writes a SELECTED_REACH edge), rotate-off-recommended = pivot (writes PIVOTED plus a SELECTED_REACH to the chosen reach), defer/reject = no committed reach, free-text/none-fit = recordSelectorMiss; (3) ALL four outcomes route ONLY through navigation.cjs (the single write chokepoint, Part 9); (4) the row labels the navigator sees are the Feynman-JTBD WHAT-THEY-GET aliases, never the mechanism-verb -- the canonical_verb persists to the graph edge, not to the screen (the dial-label-composer enforces this; do not surface a literal "(Recommended)" -- the filled triangle glyph is the marker). Add a one-line note that in Mode B / Tier 0 (cold room) the dial renders all reaches with zero markers and "--" confidence, and that absence is INTENTIONAL, not broken.
    No fenced code blocks. Hyphens only, no em-dashes.
  </action>
  <acceptance_criteria>
    - grep-presence: `grep -c "Operating the Dial" skills/larry-personality/SKILL.md` >= 1
    - grep-presence: all four close outcomes named -- SELECTED_REACH, PIVOTED, recordSelectorMiss, navigation.cjs each present >= 1
    - grep-presence: `grep -c "canonical_verb" skills/larry-personality/SKILL.md` >= 1 (label-vs-edge separation stated)
    - drift stays green: `node tests/test-reach-ids-drift.cjs` PASS (new tokens live OUTSIDE the dial window; reach-id set still exactly 5)
    - zero em-dashes
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-reach-ids-drift.cjs && grep -q "Operating the Dial" skills/larry-personality/SKILL.md && grep -q "SELECTED_REACH" skills/larry-personality/SKILL.md && grep -q "PIVOTED" skills/larry-personality/SKILL.md && grep -q "recordSelectorMiss" skills/larry-personality/SKILL.md</automated>
  </verify>
  <done>A compact "Operating the Dial (shipped)" H3 exists after Scope; it names the 5 frozen reach-ids, the 4 close outcomes (sync/pivot/defer-reject/free-text), the navigation.cjs write chokepoint, the Feynman-label vs canonical_verb separation, and the intentional-zero-markers Mode B / Tier 0 note; drift green; zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 3: OPS-03 -- add the "Reading routing_source" note</name>
  <files>skills/larry-personality/SKILL.md</files>
  <read_first>
    - AUDIT-1-tui-operation.json -> per_surface_edits[0].edits[3] (the 4-line routing_source note) AND .engine_flip_meaning (what source=engine means behaviorally)
    - skills/larry-personality/SKILL.md "### Operating the Dial (shipped)" (Task 2 inserts this; OPS-03 lands as a short note at the end of that block, still inside the Hierarchical Navigator section / OUTSIDE the dial drift window)
    - tests/test-sensors-routing-fence.cjs (the Phase 144 fence: sensors PRODUCE reaches, decide() CONSUMES them; sensors never set routing_source nor call decide() -- the note must state this honestly, not imply the engine already routes)
  </read_first>
  <action>
    Append a short (about 4 lines) note titled "Reading routing_source" at the end of the Operating-the-Dial block. State: when the engine populates fire_skill (Phase 144), the router (skill-activation-router.cjs Precedence Rule 1) returns source=engine. This is a CONSEQUENCE of a sensor firing, NOT a separate command Larry obeys -- Larry's behavioral change is simply the posture the fired reach already implies (push_forward / hold / pull_back). Honesty floor (hard-fence 7): until Phase 144 lands, routing_source stays legacy; the note frames the engine as "routes once 144 lands," not "routes now." Part 8 floor: source=engine carries zero user-content egress -- the reach struct and the router rationale carry only generic scalars (reach_id, posture, problem-type enums), never artifact bodies. This pre-empts the failure mode where Larry treats the engine signal as a new instruction.
    Hyphens only, no em-dashes.
  </action>
  <acceptance_criteria>
    - grep-presence: `grep -c "routing_source" skills/larry-personality/SKILL.md` >= 1 AND `grep -c "source=engine" skills/larry-personality/SKILL.md` >= 1
    - grep-presence: `grep -c "CONSEQUENCE" skills/larry-personality/SKILL.md` >= 1 (the consequence-not-command framing is explicit)
    - grep-presence: the Part 8 zero-egress phrase is present (`grep -c "zero user-content egress" skills/larry-personality/SKILL.md` >= 1)
    - routing fence stays green: `node tests/test-sensors-routing-fence.cjs` exits 0
    - drift stays green: `node tests/test-reach-ids-drift.cjs` PASS
    - zero em-dashes
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-sensors-routing-fence.cjs && node tests/test-reach-ids-drift.cjs && grep -q "routing_source" skills/larry-personality/SKILL.md && grep -q "CONSEQUENCE" skills/larry-personality/SKILL.md && grep -q "zero user-content egress" skills/larry-personality/SKILL.md</automated>
  </verify>
  <done>A "Reading routing_source" note exists stating source=engine is a consequence of a sensor firing (not a command), framing 144 status honestly (legacy until 144 lands), and reaffirming Part 8 zero user-content egress; routing fence + drift green; zero em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt doctrine -> shipped contract | The prompt's claims about what is shipped must match code reality; a stale claim is a Reasoning-Action Disconnect, not a privacy breach |
| reach struct / router rationale -> Brain | source=engine must carry only generic scalars, never user content (Part 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1432.01-01 | Tampering | the frozen 5 reach-id set in the dial window | mitigate | OPS-02/03 tokens land OUTSIDE the "Capability Dial".."Hierarchical Navigator" window; test-reach-ids-drift.cjs gates every task |
| T-1432.01-02 | Information Disclosure | the routing_source note | mitigate | reaffirm Part 8 "zero user-content egress" verbatim; do not soften to "minimal" |
| T-1432.01-03 | Repudiation | honest 144 status | accept | hard-fence 7: note frames the engine as "routes once 144 lands," never "live now" -- prevents the prompt over-promising against the still-fenced router |
| T-1432.01-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (pure markdown doctrine edit) |
</threat_model>

<verification>
After all three tasks:
- `node tests/test-reach-ids-drift.cjs` PASS (exactly-5 reach-id set held)
- `node tests/test-posture-ids-drift.cjs` PASS (exactly-3 posture set held)
- `node tests/test-sensors-routing-fence.cjs` exits 0 (Phase 144 fence green)
- `grep -c "comes later" skills/larry-personality/SKILL.md` == 0
- All three operate-blocks present by grep: "Operating the Dial", "routing_source", "Reading routing_source"
- Zero em-dashes in skills/larry-personality/SKILL.md
</verification>

<success_criteria>
ROADMAP success criterion 1 (OPERATE) is satisfied: the stale "comes later" language is flipped to now-shipped; a compact Operating-the-Dial block + a Reading-routing_source note are present in larry-personality SKILL.md. No 6th reach-id; routing fence + both drift tests stay green.
</success_criteria>

<output>
Create `.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-01-SUMMARY.md` when done.
</output>
