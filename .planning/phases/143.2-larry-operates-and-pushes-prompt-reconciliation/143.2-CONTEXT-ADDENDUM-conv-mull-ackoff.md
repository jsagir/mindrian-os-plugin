---
phase: 143.2
type: context-addendum
gathered: 2026-06-07
status: AUTHORITATIVE - extends 143.2-CONTEXT.md with the navigator's 2026-06-07 scope addition (conversation-mode + mullins-scaffold, Brain-enhanced, Ackoff bidirectional)
brain_consulted: yes (mcp brain_ask + brain_search, 2026-06-07; Part 8 clean - generic methodology only)
requirements_added: [CONV-01, CONV-02, MULL-01, MULL-02]
---

# Phase 143.2 Addendum: conversation-mode + mullins-scaffold (Brain + Ackoff)

The navigator directed (2026-06-07) that two skills fold into 143.2 and be enhanced using the Brain + Ackoff DIKW bidirectional traversal. The Brain was consulted (generic methodology only, Part 8 clean).

## Brain findings (consulted 2026-06-07)

1. **Ackoff Pyramid FEEDS_INTO Systems Thinking AND FEEDS_INTO MAP THE HIERARCHY** (brain_ask DirectiveEnvelope). The DIKW bidirectional traversal is a CANONICAL chain, not a bolt-on - it connects to the two frameworks Larry's Hierarchical Navigator already uses ("bidirectional Ackoff ascent" is already in larry-personality doctrine). So "Ackoff up-down/down-up" FITS (the navigator's "if fits" condition is met).
2. **Mullins = a 2x2 (Macro/Micro x Market/Industry) with VALIDATION at the center** (5 interconnected matrix elements; 7 domains in the full New Business Road Test). The Brain surfaced its framework neighbors: Value Proposition (precedes Mullins), Disruptive Innovation (sustaining-vs-disruptive), the problem/solution/business-case validity-feasibility-value frame, and Systems Thinking (via the Ackoff chain). These are the "folders from other frameworks" the Brain adds.
3. The DIKW->conversation-mode mapping: **chat = Data/Information, brainstorm = Knowledge, build = Wisdom/Understanding** (the up-ascent); the **down-descent** = validation (a built artifact returns to test its assumptions/data).

## The 4 added requirements (Brain-informed)

### CONV-01 - conversation-mode as the explicit lane-picker traffic cop
Today conversation-mode is an implicit classifier (just-talk / explore-capture / build-room). Make the lane decision an EXPLICIT Shape F selector in no-room sessions: "are we just chatting, brainstorming, or building something?" -> pick the lane. Reuse the Shape F.1 primitive (SEED-020); the lane-pick is a Decision Gate, not a silent classification. Surface: skills/conversation-mode/SKILL.md (extends the OPS-05 conversational-reaches note into a fuller lane-picker).

### CONV-02 - the lanes mapped to Ackoff DIKW (bidirectional)
The 3 lanes ARE DIKW positions: chat = Data/Information (low structure), brainstorm = Knowledge (divergent synthesis), build = Wisdom/Understanding (convergent artifact) - the UP-ascent. The DOWN-descent = a built artifact returns to validate its assumptions/data (Validation). Wire the lane-picker to read/advance the DIKW position; Brain-chained (Ackoff FEEDS_INTO Systems Thinking + MAP THE HIERARCHY - offer the chain when the lane crosses into build). Part 8: only generic framework handles to the Brain. The descent is the honesty loop: building does not skip validation.

### MULL-01 - mullins-scaffold Brain-driven cross-framework folders
mullins-scaffold today is the 7 fixed Mullins domains (Market, Industry, Sustainable Advantage, Mission/Aspirations, Ability to Execute, Connectedness Up, Connectedness Down - per skills/mullins-scaffold/scaffold.json). Add a Brain-driven ADDITIVE folder set: when scaffolding, the Brain proposes complementary folders from the frameworks Mullins chains to (Value Proposition, Disruptive Innovation, Systems Thinking via FEEDS_INTO). Generic framework handles only (Part 8): the Brain returns framework names + domain labels, never user content. The scaffold.json gains the additive folder definitions; the folders are OFFERED at a Decision Gate (the navigator approves which frameworks' folders to add), never auto-imposed. NOTE: this touches the scaffold SKILL (scaffold.json + the Brain-folder logic) - heavier than prompt doctrine; it is a small build, not just a doctrine edit.

### MULL-02 - Ackoff up-down/down-up structures the scaffold
The scaffold gets the bidirectional Ackoff traversal: the ASCENT fills the domains toward Validation-at-center (Data in the domain folders -> Information -> Knowledge -> Wisdom at Validation); the DESCENT is Validation decomposing back to re-test each domain's data/assumptions (the New Business Road Test validation loop). Brain-chained to Systems Thinking + MAP THE HIERARCHY. Render the traversal so the navigator sees both directions (fill-up and validate-down).

## Constraints (inherit the parent CONTEXT + add)
- Brain consults carry generic framework handles only (framework names, problem-type enums) - never user artifacts (Part 8). The Brain-folder proposals (MULL-01) and the Ackoff chains (CONV-02/MULL-02) are all methodology-graph reads.
- Every lane-pick (CONV-01) and every Brain-folder add (MULL-01) is a Decision Gate, never auto-imposed (GUIDED default; Canon Part 3).
- The Ackoff bidirectional is ADDITIVE doctrine/structure - it reuses the existing "bidirectional Ackoff ascent" already in larry-personality; do not contradict it.
- MULL-01/02 are skill enhancements (scaffold.json + logic) - keep them reuse-first (Part 7): extend the shipped scaffold, do not rebuild it.

## Plan-shape impact
The re-plan should add: a conversation-mode lane-picker plan (CONV-01/02, prompt/skill doctrine) + a mullins-scaffold enhancement plan (MULL-01/02, the scaffold.json + Brain-folder logic + the Ackoff traversal render). These are distinct from the OPS/PUSH prompt-doctrine plans. Consider whether MULL (the heavier skill build) warrants its own wave or a verification of the Brain-folder Part-8 boundary.
