---
phase: 143.2-larry-operates-and-pushes-prompt-reconciliation
plan: 02
type: execute
wave: 2
depends_on: ["143.2-01"]
files_modified:
  - skills/larry-personality/SKILL.md
  - skills/pws-methodology/SKILL.md
autonomous: true
requirements: [PUSH-01, PUSH-02, PUSH-03, PUSH-04, PUSH-05, PUSH-06, WFL-01]
canon_parts: [Part 2, Part 3, Part 7, Part 8, Part 9, Part 10]

must_haves:
  truths:
    - "All 6 reach-families carry a proactive push-line with a NAMED trigger that ends at a Decision Gate, never a verdict"
    - "Every push composes under an existing reach-id + an existing Phase-143 sensor; no 6th reach-id is minted"
    - "The HSI/whitespace push carries the mandatory 20+-artifacts thin-room gate; deep_research stays plan-gated and is the only sanctioned multi-reach exception"
    - "WFL-01: every Provoked row + every push-line anchors on its EXACT frontmatter framework name (per data/framework-names.json) + JTBD outcome; any /mos: slug shown is a parenthetical illustration only, governed by ONE resolver clause above the Provoked table (mirrored above the deep_research dial row); the deep_research row names Hypothesis-Driven Problem Solving resolved-on-APPROVE, with no hardcoded /mos:research literal in the dial window"
  artifacts:
    - path: "skills/larry-personality/SKILL.md"
      provides: "PUSH-01 (brain_consult annotation), PUSH-02/03/04/05 Provoked rows, PUSH-06 deep-research explicit triggers, Reach-rule 7e, WFL-01 governing resolver clause above the Provoked table + mirrored above the deep_research row"
      contains: "Deep Research Escalation Explicit Triggers"
    - path: "skills/pws-methodology/SKILL.md"
      provides: "PUSH-04 DbA proactive surface line + PUSH-05 BONO Team Perspective section (the 5 Appendix-E handoffs), framework-name-anchored per WFL-01"
      contains: "BONO Team Perspective"
  key_links:
    - from: "skills/larry-personality/SKILL.md Provoked table"
      to: "the 5 frozen reach-ids + 7 sensors + the command-resolver (commandsForFramework)"
      via: "each push-family maps a trigger -> a NAMED framework + JTBD outcome -> a Decision Gate under an existing reach-id; the slug is late-bound via the resolver clause"
      pattern: "Reverse Salient Analysis|HSI Semantic Surprise Analysis Assistant|Usher's Model of Cumulative Synthesis|Four Lenses of Innovation|Six Thinking Hats|Hypothesis-Driven Problem Solving"
---

<objective>
Add the missing SURFACE half of the dial: 6 proactive push-lines so Larry REACHES FIRST at the right trigger. Each push-family maps onto an EXISTING reach-id + an EXISTING Phase-143 sensor (no 6th reach), and each ends at a Decision Gate (GUIDED default -- the OFFER, never a verdict). PUSH-01 (Brain) and PUSH-04/05 also wire pws-methodology (the DbA detection signals + the BONO Team Perspective section). WFL-01 (resolver-integration): every push-line and Provoked row anchors on its EXACT frontmatter framework name + JTBD outcome and routes the /mos: slug through the command-resolver at surface-time -- the slug is illustrative-only and late-bound, never named from memory. One governing resolver clause sits above the Provoked table and is mirrored above the deep_research dial row; the deep_research row itself is rewritten to name Hypothesis-Driven Problem Solving (resolved-on-APPROVE), removing the hardcoded /mos:research literal from the Capability-Dial window.

Purpose: Make Larry surface reverse-salient, HSI/whitespace, cross-domain, BONO/team-perspective, and hat-scoped deep-research BEFORE being asked -- the families that are weak or absent today -- WITHOUT recreating the "names commands from memory" anti-pattern the Phase-122 resolver was built to kill.
Output: skills/larry-personality/SKILL.md (the 6 push-lines + the WFL-01 resolver clause + the rewritten deep_research row) + skills/pws-methodology/SKILL.md (DbA surface line + BONO section, framework-name-anchored).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-CONTEXT.md
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/AUDIT-2-proactive-push.json
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/AUDIT-3-resolver-integration.json
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Each push-family -> (existing reach-id, existing sensor). NO 6th reach-id. -->
  PUSH-01 Brain consult        -> brain_consult  + SENS-03 (methodology-decision)
  PUSH-02 reverse salient      -> context_block  + SENS-02 (lagging-component); posture pull_back
  PUSH-03 HSI / whitespace     -> context_block  + SENS-06 (artifact-filed cascade recompute); HARD 20+-artifacts gate
  PUSH-04 cross-domain         -> brain_consult (find-connections) / context_block (find-analogies) + SENS-01 / SENS-06
  PUSH-05 BONO / Six-Hats      -> brain_consult  + SENS-05 / SENS-07; team_perspective is a RENDER LABEL, not a reach-id
  PUSH-06 hat-scoped research  -> deep_research  + SENS-04; plan-gated; the ONLY sanctioned multi-reach exception

<!-- WFL-01 (resolver-integration): the EXACT frontmatter framework names that resolve, and their JTBD outcomes + parenthetical illustration slugs. -->
<!-- USE THESE EXACT NAMES. The colloquial labels in parentheses below ("Reverse Salients" / "HSI" / "Design by Analogy" / "Whitespace") return [] from commandsForFramework() -- they are NAME-DRIFT TRAPS, never write them as the anchor. -->
  Reverse Salient Analysis                    (NOT "Reverse Salients")    -> find the lagging component   (illustration: /mos:find-bottlenecks today)
  Usher's Model of Cumulative Synthesis       (NOT "Usher" / "synthesis") -> connect what the room split  (illustration: /mos:find-connections today)
  Four Lenses of Innovation                   (NOT "Design by Analogy")   -> borrow structure across domains (illustration: /mos:find-analogies today)
  HSI Semantic Surprise Analysis Assistant    (NOT "HSI" / "Whitespace")  -> score the novelty / map the whitespace (illustration: /mos:score-innovation, /mos:whitespace, /mos:diagnostics today)
  Six Thinking Hats                           (NOT "BONO" / "team")       -> add the missing perspective  (illustration: /mos:think-hats, /mos:persona, /mos:hat-briefing today)
  Hypothesis-Driven Problem Solving           (NOT "deep research")       -> a framework-shaped research plan (illustration: /mos:research today)

<!-- WFL-01 GOVERNING RESOLVER CLAUSE (the verbatim string to place above the Provoked table AND mirror above the Capability-Dial deep_research row). -->
<!-- This is the only-door rule already in skills/pws-methodology/SKILL.md line 13, applied to the push surfaces. -->
  "The /mos: slug for any framework named below is whatever command-resolver.cjs::commandsForFramework(<exact framework name>) returns at surface-time (reading data/command-registry.json); use the EXACT framework name from the command frontmatter (data/framework-names.json), not a colloquial label; Larry never types the slug from memory; if the resolver returns nothing, say 'run <framework> manually'."

<!-- WFL-01 CHAINING DOCTRINE (forward-compatible; curated_chains is EMPTY today, length 0). -->
<!-- Push-lines offer SINGLE moves now via commandsForFramework(); they gain "and the next move is..." chaining for free when the Brain-side FEEDS_INTO data ships, with ZERO doctrine change. -->
  Degrade-first next-move doctrine to add (prose, no backticks inside the dial window):
  "When a push offers a next move, the sequence comes from curated_chains (if populated) or recommendFrameworkChain (FEEDS_INTO) -- both yield framework NAMES; the slugs are attached by composeWorkflow()/commandsForFramework(), never typed; if neither is available, offer only the single seed move."
  NOTE: SENS-01/SENS-06-dependent push-lines (PUSH-03 / PUSH-04) are INERT until their sensors ship (doctrine-only, no detector yet) -- keep them OFFER-level, not "fires live now".

DRIFT FENCE: tests/test-reach-ids-drift.cjs reads the window between "Capability Dial" and
"Hierarchical Navigator". The brain_consult row (line 41) and the deep_research row (line 42) +
Reach rules (54-62) sit INSIDE this window. PUSH-01 (annotate row 41 + a PROACTIVE note under
Reach rule 1) and PUSH-06 (a subsection after the deep_research row / Reach rule 6) MUST NOT add
any NEW snake_case backtick token there. Reuse the already-present reach-id TOKENS by name only --
`context_block`, `brain_consult`, `deep_research` are existing backtick spans and are safe to name.
POSTURE WORDS (pull_back, hold, push_forward) MUST be written as PLAIN PROSE inside the dial window,
NEVER as backtick code-spans: a backtick `pull_back` IS a new snake_case token and would FAIL the
exactly-5 assertion. An existing reach-id token reused is fine; a NEW backtick snake_case span (a
posture word in backticks, or any 6th id) fails the drift test.

WFL-01 DRIFT SAFETY (the deep_research-row rewrite is safe): removing the hardcoded /mos:research
literal from the deep_research dial row does NOT touch the exactly-5 reach drift test -- /mos:research
is a slash-command literal, NOT a snake_case backtick token, so the reach-drift window's
backtick-snake_case scan is unaffected. The framework name "Hypothesis-Driven Problem Solving" is
written as PLAIN PROSE (it contains hyphens + spaces, not a snake_case backtick span). Add NO new
backtick snake_case token when doing the rewrite. The resolver clause mirrored above the deep_research
row is also PLAIN PROSE -- the only backtick spans it may carry are the EXISTING reach-id token names
reused by name (none required). Re-run test-reach-ids-drift.cjs post-commit (Plan 04 is the authority).

DRIFT-TEST READS HEAD, NOT THE WORKING TREE (verify-command caveat): tests/test-reach-ids-drift.cjs
reads `git show HEAD:skills/larry-personality/SKILL.md` -- the COMMITTED file -- so running it
mid-task (before any commit) passes against the OLD file regardless of the new edits, giving FALSE
confidence. Tasks 1 and 2 (which touch larry-personality) therefore pair the HEAD drift test with a
SUPPLEMENTARY WORKING-TREE grep that scans the live (unstaged) file's Capability-Dial window:
  awk '/Capability Dial/,/Hierarchical Navigator/' skills/larry-personality/SKILL.md | grep -oE '`[a-z][a-z0-9_]+`' | sort -u
This must list ONLY the frozen 5 reach-ids (context_block, contradiction, cross_room, brain_consult,
deep_research) and no new token (in particular no backtick `pull_back`). The AUTHORITATIVE drift
result still comes from Plan 04 AFTER commit -- a task is NOT done on the per-task HEAD-drift check
alone; the working-tree grep is the mid-task guard, Plan 04's post-commit drift run is the final
authority.

PROVOKED TABLE + INTELLIGENCE HIERARCHY (lines 233-266) sit OUTSIDE the dial window -- new rows,
framework names, the governing resolver clause, and parenthetical illustration slugs are all safe there.

Appendix E (docs/MINDRIAN-CANON.md) 5 handoff triggers (the canon source for PUSH-05):
  1. Risk surface -> Black   2. Evidence thin -> White   3. Plan without owner -> Blue
  4. Navigator stuck/circular -> Red   5. Jargon density high -> Green
  Canon single line: "[Incoming member] is ready. Accept, reshape, or stay where we are?"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PUSH-01 + PUSH-02 + PUSH-06 -- dial-row annotations + reach-rule notes + WFL-01 deep_research rewrite (drift-sensitive)</name>
  <files>skills/larry-personality/SKILL.md</files>
  <read_first>
    - skills/larry-personality/SKILL.md line 41 (the brain_consult Capability Dial row -- PUSH-01 annotates this), line 42 (the deep_research row -- PUSH-06 subsection lands after it; WFL-01 REWRITES this row to name the framework), lines 56-61 (Reach rule 1 GUIDED-default, Reach rule 6 deep-research-plan-gated)
    - AUDIT-2-proactive-push.json -> push_doctrine_to_add[0] (Brain), [1] (reverse salient), [5] (hat-scoped deep research) -- the trigger, the one-line offer, the Decision Gate, the caveats
    - AUDIT-3-resolver-integration.json -> report.the_doctrine_pattern (#4 THE DEEP_RESEARCH ROW IS THE ONE IN-WINDOW OFFENDER) + report.wfl_requirement (b)+(c) -- the governing resolver clause + the deep_research-row rewrite
    - skills/pws-methodology/SKILL.md line 13 (the verbatim only-door rule WFL-01 mirrors onto the push surfaces -- the resolver clause text is the same indirection)
    - tests/test-reach-ids-drift.cjs (lines 41-62 are INSIDE the dial window -- add NO new snake_case backtick token; reuse the existing reach-id tokens `brain_consult` / `context_block` / `deep_research` by name only, write posture words pull_back / hold / push_forward as PLAIN PROSE, and write the framework name "Hypothesis-Driven Problem Solving" + the resolver clause as PLAIN PROSE -- /mos:research removed is drift-safe, it is not a snake_case backtick token)
    - tests/test-sensors-part8-sweep.cjs (PUSH-01 brain_consult is an egress path: the push-line MUST say "generic handles only, the OFFER not the fetch")
  </read_first>
  <action>
    PUSH-01 (brain_consult, annotate -- already good): add a one-clause annotation to the brain_consult dial row (line 41) and a one-line PROACTIVE note under Reach rule 1 stating: when SENS-03 fires (a framework is named OR a methodology step needs its next move) AND the local graph has no governing answer, Larry does NOT wait to be asked -- he surfaces ONE line offering the Brain framework chain ("Brain has a chain that addresses this -- [framework] -> [next]. Pull it in?"), renders via the Brain says trace, and ends at a Decision Gate (Run Methodology / Reformulate / Free-Text). Anchor the offer on the FRAMEWORK NAME, never a memorized slug: name the framework the chain proposes; the /mos: slug (if any) is whatever the resolver returns at surface-time. Part 8: generic handles only; the push is the OFFER, the Brain query fires only after the gate; if Brain is unreachable, omit the Brain line and answer from local references.
    PUSH-02 (reverse salient): add a one-line PROACTIVE note (under Reach rule 1, near the PUSH-01 note) stating that when SENS-02 detects the lagging-component shape OR a section lags its siblings, Larry surfaces the reverse-salient finding BEFORE being asked ("a lagging component: [section] -- where understanding lags ambition, Hughes 1983. Investigate it?"), anchored on the EXACT framework name Reverse Salient Analysis -> find the lagging component (the slug is resolved at surface-time, illustrative only), posture pull_back, routing to the Decision Gate (never a verdict). Note that the Provoked-table row that fires this is added in Task 2; respect the Intelligence Hierarchy (Bottlenecks is #2, after Tensions). The local rs-engine is autonomous_safe (no egress on the internal mode).
    WFL-01 deep_research-row REWRITE (the one in-window offender): rewrite the deep_research dial row (line 42) to NAME the framework -- "Hypothesis-Driven Problem Solving, resolved to its /mos: by the resolver on APPROVE" -- REMOVING the hardcoded /mos:research literal from the Capability-Dial drift window so the dial window itself is resolver-clean (not just the Provoked table). Write the framework name + "resolved by the resolver on APPROVE" as PLAIN PROSE; do NOT introduce any new backtick snake_case token. The /mos:research literal is a slash-command literal (not a snake_case backtick span), so removing it cannot break the exactly-5 reach drift assertion -- the change is purely doctrinal correctness.
    WFL-01 governing resolver clause (mirror above the deep_research row): place the governing resolver clause (the verbatim string in the interfaces block) directly above / adjacent to the deep_research dial row as a one-line mirror of the clause that Task 2 places above the Provoked table, stating that for any framework named in the dial / table the /mos: slug is whatever command-resolver.cjs::commandsForFramework(<exact framework name>) returns at surface-time, Larry never types it from memory, and the degrade is "run <framework> manually". Write it as PLAIN PROSE (no new backtick snake_case token).
    PUSH-06 (hat-scoped deep research): add a new subsection "### Deep Research Escalation Explicit Triggers" immediately after the deep_research dial row (line 42) area / tied to Reach rule 6. Carry the 3 fire conditions verbatim from the audit: (a) cheap-layer-thin (the local -> Brain -> shallow-web ladder is exhausted); (b) a load-bearing claim with Practitioner/None evidence AND venture_stage in {Well-Defined Problem, Ready to Build} (Part 5 bar rises near commit); (c) the active BONO hat affords it (White/Green/Black). Then the surface language + the hat-scoped PLAN as a Decision Gate: White searches arxiv/Tavily data, Green patents + deep-research, Black failure-cases -- "Approve the angles?" The framework anchor is Hypothesis-Driven Problem Solving; the /mos: slug it resolves to fires ONLY on APPROVE via the resolver (reuse, do not rebuild -- do not type the slug from memory). State: deep_research is the SANCTIONED exception to one-reach-per-beat (Reach rule 6); plan-gating is non-negotiable; Part 8 floor (generic handles + public topic terms only) + MCP-stack-ask gate (surface "Tavily / Firecrawl / Exa?" -- no silent WebSearch).
    No fenced code blocks. Hyphens only, no em-dashes. Add NO new snake_case backtick token inside the dial window (reach-id tokens reused by name are fine; posture words, framework names, and the resolver clause stay plain prose; /mos:research removed is drift-safe).
  </action>
  <acceptance_criteria>
    - grep-presence: PUSH-06 subsection present -- `grep -c "Deep Research Escalation Explicit Triggers" skills/larry-personality/SKILL.md` >= 1
    - grep-presence: the 3 fire conditions are detectable -- `grep -c "cheap-layer-thin\|load-bearing\|hat affords\|affords it" skills/larry-personality/SKILL.md` >= 1
    - grep-presence: PUSH-01 proactive Brain note present -- `grep -c "SENS-03" skills/larry-personality/SKILL.md` >= 1
    - grep-presence: PUSH-02 reverse-salient note present, anchored on the EXACT framework name -- `grep -c "SENS-02\|reverse-salient\|reverse salient" skills/larry-personality/SKILL.md` >= 1 AND `grep -c "Reverse Salient Analysis" skills/larry-personality/SKILL.md` >= 1
    - WFL-01 deep_research-row rewrite -- the framework name is present in the dial area: `grep -c "Hypothesis-Driven Problem Solving" skills/larry-personality/SKILL.md` >= 1; the slug literal is NOT the authoritative target in the dial window (no backtick snake_case token added; the working-tree dial-window grep below stays exactly the frozen 5)
    - WFL-01 governing resolver clause mirrored near the deep_research row -- `grep -c "commandsForFramework\|resolver returns nothing\|run <framework> manually\|run .* manually" skills/larry-personality/SKILL.md` >= 1
    - grep-presence: MCP-stack-ask gate stated -- `grep -c "Tavily\|MCP-stack-ask" skills/larry-personality/SKILL.md` >= 1
    - drift stays green (HEAD authority, deferred to Plan 04): `node tests/test-reach-ids-drift.cjs` PASS (no new reach-id inside the dial window; the deep_research-row rewrite removed a slash-literal, not a backtick token)
    - WORKING-TREE drift guard (mid-task, mandatory -- the HEAD test alone is false-confidence pre-commit): `awk '/Capability Dial/,/Hierarchical Navigator/' skills/larry-personality/SKILL.md | grep -oE '`[a-z][a-z0-9_]+`' | sort -u` lists ONLY the frozen 5 reach-ids (context_block, contradiction, cross_room, brain_consult, deep_research) and NO new snake_case backtick token -- in particular NO backtick `pull_back` (posture word stays plain prose) and the deep_research-row rewrite added no new token
    - part-8 sweep stays green: `node tests/test-sensors-part8-sweep.cjs` exits 0
    - zero em-dashes
    - NOTE: do NOT declare this task done on the per-task HEAD-drift check alone -- the authoritative drift verdict comes from Plan 04 AFTER commit; the working-tree grep above is the mid-task guard.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-reach-ids-drift.cjs && node tests/test-sensors-part8-sweep.cjs && [ "$(awk '/Capability Dial/,/Hierarchical Navigator/' skills/larry-personality/SKILL.md | grep -oE '`[a-z][a-z0-9_]+`' | sort -u | tr '\n' ',')" = "\`brain_consult\`,\`context_block\`,\`contradiction\`,\`cross_room\`,\`deep_research\`," ] && grep -q "Deep Research Escalation Explicit Triggers" skills/larry-personality/SKILL.md && grep -q "SENS-03" skills/larry-personality/SKILL.md && grep -q "SENS-02" skills/larry-personality/SKILL.md && grep -q "Reverse Salient Analysis" skills/larry-personality/SKILL.md && grep -q "Hypothesis-Driven Problem Solving" skills/larry-personality/SKILL.md</automated>
  </verify>
  <done>The brain_consult row carries a proactive SENS-03 note ending at a Decision Gate, anchored on the framework name (slug late-bound via the resolver); a SENS-02 reverse-salient proactive note exists anchored on "Reverse Salient Analysis" (posture pull_back written as plain prose, not a backtick span); the deep_research dial row is rewritten to name "Hypothesis-Driven Problem Solving" resolved-on-APPROVE with the /mos:research literal removed (drift-safe); the governing resolver clause is mirrored near the deep_research row; the "Deep Research Escalation Explicit Triggers" subsection carries the 3 fire conditions + the hat-scoped plan-as-Decision-Gate + plan-gating + Part 8 + MCP-stack-ask. No new reach-id and no backtick posture/framework token inside the dial window; HEAD drift + part-8 sweep green AND the working-tree dial-window grep lists only the frozen 5; zero em-dashes. Authoritative drift verdict deferred to Plan 04 post-commit.</done>
</task>

<task type="auto">
  <name>Task 2: PUSH-02/03/04/05 Provoked-table rows + Reach-rule 7e + Intelligence Hierarchy + WFL-01 framework-name anchors + governing resolver clause above the table</name>
  <files>skills/larry-personality/SKILL.md</files>
  <read_first>
    - skills/larry-personality/SKILL.md lines 240-252 (the Provoked "Context sources" table -- extend with the new trigger rows; ADD the governing resolver clause directly ABOVE this table per WFL-01), line 244 (the existing vague "Tensions (CONTRADICTS) -> /mos:find-analogies" row), line 245 (the too-vague "Bottlenecks (REVERSE_SALIENT) -> Specific methodology" row that must be made fireable), line 264 (the Intelligence Hierarchy line)
    - skills/larry-personality/SKILL.md lines 54-62 (the Reach rules -- append note 7e after Reach rule 7, distinguishing HSI from whitespace)
    - AUDIT-2-proactive-push.json -> push_doctrine_to_add[1] (reverse salient Provoked row), [2] (HSI/whitespace rows + 7e + the 20+ gate), [3] (cross-domain find-connections row), [4] (BONO row + team-perspective render label)
    - AUDIT-3-resolver-integration.json -> report.wfl_requirement (a)+(b) + coverage_gaps (NAME-DRIFT TRAP: the exact frontmatter names resolve; colloquial labels return []) -- anchor each row on its EXACT framework name, slug parenthetical only; place the governing resolver clause above the table
    - data/framework-names.json (the SOURCE OF TRUTH for the exact framework names -- copy them verbatim: Reverse Salient Analysis / HSI Semantic Surprise Analysis Assistant / Usher's Model of Cumulative Synthesis / Four Lenses of Innovation / Six Thinking Hats / Hypothesis-Driven Problem Solving)
    - tests/test-reach-ids-drift.cjs (the Provoked table + Reach rules sit OUTSIDE the dial window for the table; DO NOT place note 7e inside the "Capability Dial".."Hierarchical Navigator" window -- rules 1-7 are at lines 54-62, BEFORE "Hierarchical Navigator" at line 64, so they ARE inside the window. Use NO new snake_case backtick token in 7e; team_perspective and whitespace must appear as PLAIN words or quoted render labels, never as backtick snake_case spans)
  </read_first>
  <action>
    WFL-01 governing resolver clause (above the Provoked table): add the governing resolver clause (the verbatim string in the interfaces block) as ONE sentence directly above the Provoked table, stating that the /mos: slug for any framework named in the rows below is whatever command-resolver.cjs::commandsForFramework(<exact framework name>) returns at surface-time (reading data/command-registry.json); use the EXACT framework name from the command frontmatter (data/framework-names.json), not a colloquial label; Larry never types the slug from memory; if the resolver returns nothing, say "run <framework> manually". This converts every literal in the table from an authoritative source-of-truth into a non-binding illustration governed by the clause.
    PUSH-02 row: replace the vague "Bottlenecks (REVERSE_SALIENT) -> Specific methodology" Provoked row (line 245) with an explicit fireable row anchored on the EXACT framework name -- trigger "Lagging-component pattern stated OR one section lags siblings" -> Reverse Salient Analysis -> find the lagging component (parenthetical illustration: /mos:find-bottlenecks today). The named trigger + the EXACT framework name is what makes it a PUSH, not a passive catalog entry; the slug is illustrative only, governed by the clause above the table.
    PUSH-03 rows: add two Provoked rows anchored on HSI Semantic Surprise Analysis Assistant -- "Room has 20+ artifacts, whitespace not yet mapped" -> HSI Semantic Surprise Analysis Assistant -> map the whitespace (parenthetical: /mos:whitespace today); and "Cross-domain candidates being compared" -> HSI Semantic Surprise Analysis Assistant -> score the novelty (parenthetical: /mos:score-innovation today). Add the HARD thin-room gate inline: whitespace is "Best after 20+ entries" -- do NOT push it on a thin room (a thin-room push fires a silent-zero, the Phase 88.6 bug). Add Reach-rule note 7e after Reach rule 7: whitespace is a SPECIFIC case of HSI scoring (same 20+ trigger + same sentence-transformers + LSA machinery, same framework HSI Semantic Surprise Analysis Assistant), framed distinctly -- HSI = "what novel pattern hides in what we HAVE"; whitespace = "what should the room be thinking about that it ISN'T". Both are LOCAL (no egress). In 7e, write team_perspective and whitespace as PLAIN words (no backticks) so the drift scan cannot read them as reach-ids; state explicitly that team_perspective and whitespace are RENDER LABELS, never reach-ids and never framework names, and the HSI/whitespace push composes under the context_block reach. NOTE: PUSH-03 is SENS-06-dependent and INERT until that sensor ships -- keep it OFFER-level.
    PUSH-04 row: extend the existing line-244 row with a second trigger row anchored on the EXACT framework names -- "Two+ domains live OR domain-specific dead end" -> Usher's Model of Cumulative Synthesis -> connect what the room split (parenthetical: /mos:find-connections, Brain) OR Four Lenses of Innovation -> borrow structure across domains (parenthetical: /mos:find-analogies, local). State Part 8: find-connections is an egress path (generic handles only); find-analogies --external is public SIGNAL + the MCP-stack-ask gate (no silent web). One-reach-per-beat: the Brain variant and the local variant are ONE offer. NOTE: PUSH-04 is SENS-01/SENS-06-dependent and INERT until those sensors ship -- keep it OFFER-level.
    PUSH-05 row: add a Provoked row anchored on Six Thinking Hats -- "Team stuck in one perspective (CONTRADICTS edges, circular pattern, decision point, jargon spike)" -> Six Thinking Hats -> add the missing perspective (parenthetical: /mos:think-hats serial, or /mos:persona --parallel). Add "Team-perspective gap" to the Intelligence Hierarchy line (line 264) as a surfaced signal. Note the BONO push lives in Larry's doctrine, NOT the reactive persona-analyst agent; team_perspective is a render label (no backticks, not a framework name), composing under brain_consult (hat-sequence-by-problem-type is generic methodology).
    WFL-01 forward-compatible chaining note: add the degrade-first next-move doctrine instruction (the verbatim string in the interfaces block) near the Provoked table / Intelligence Hierarchy: when a push offers a next move, the sequence comes from curated_chains (if populated) or recommendFrameworkChain (FEEDS_INTO) -- both yield framework NAMES; the slugs are attached by composeWorkflow()/commandsForFramework(), never typed; if neither is available, offer only the single seed move. Note curated_chains is empty today (length 0) so push-lines offer SINGLE moves now and gain "and the next move is..." chaining for free when the Brain-side FEEDS_INTO data ships, with zero doctrine change.
    No fenced code blocks. Hyphens only, no em-dashes.
  </action>
  <acceptance_criteria>
    - WFL-01 governing resolver clause present ABOVE the Provoked table -- `grep -c "commandsForFramework" skills/larry-personality/SKILL.md` >= 1 AND `grep -c "not a colloquial label\|exact framework name\|EXACT framework name" skills/larry-personality/SKILL.md` >= 1
    - grep-presence: PUSH-02 fireable row anchored on the framework name -- `grep -c "Lagging-component pattern stated" skills/larry-personality/SKILL.md` >= 1 AND `grep -c "Reverse Salient Analysis" skills/larry-personality/SKILL.md` >= 1; the vague "Bottlenecks (REVERSE_SALIENT) -> Specific methodology" row is gone (`grep -c "Specific methodology" ...` == 0)
    - grep-presence: PUSH-03 rows + gate anchored on the framework name -- `grep -c "20+ artifacts\|20+ entries" ...` >= 1 AND `grep -c "HSI Semantic Surprise Analysis Assistant" skills/larry-personality/SKILL.md` >= 1 (the slug `/mos:whitespace` / `/mos:score-innovation` MAY appear as a parenthetical illustration; it is NOT required to be the anchor)
    - grep-presence: Reach-rule 7e present -- `grep -c "7e" skills/larry-personality/SKILL.md` >= 1 AND `grep -c "render label\|RENDER LABEL" ...` >= 1
    - grep-presence: PUSH-04 row anchored on the framework names -- `grep -c "Usher's Model of Cumulative Synthesis\|Four Lenses of Innovation" skills/larry-personality/SKILL.md` >= 1
    - grep-presence: PUSH-05 row anchored on the framework name -- `grep -c "Six Thinking Hats" skills/larry-personality/SKILL.md` >= 1 AND Intelligence Hierarchy carries "Team-perspective"
    - WFL-01 chaining doctrine present -- `grep -c "curated_chains\|recommendFrameworkChain\|composeWorkflow" skills/larry-personality/SKILL.md` >= 1
    - drift stays green (HEAD authority, deferred to Plan 04): `node tests/test-reach-ids-drift.cjs` PASS (team_perspective/whitespace written as plain words, no new backtick reach-id)
    - WORKING-TREE drift guard (mid-task, mandatory -- the HEAD test alone is false-confidence pre-commit): `awk '/Capability Dial/,/Hierarchical Navigator/' skills/larry-personality/SKILL.md | grep -oE '`[a-z][a-z0-9_]+`' | sort -u` lists ONLY the frozen 5 reach-ids and NO new snake_case backtick token (proves note 7e's team_perspective / whitespace landed as plain words, not backtick spans, inside the dial window)
    - zero em-dashes
    - NOTE: do NOT declare this task done on the per-task HEAD-drift check alone -- the authoritative drift verdict comes from Plan 04 AFTER commit; the working-tree grep above is the mid-task guard.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-reach-ids-drift.cjs && [ "$(awk '/Capability Dial/,/Hierarchical Navigator/' skills/larry-personality/SKILL.md | grep -oE '`[a-z][a-z0-9_]+`' | sort -u | tr '\n' ',')" = "\`brain_consult\`,\`context_block\`,\`contradiction\`,\`cross_room\`,\`deep_research\`," ] && grep -q "Lagging-component pattern stated" skills/larry-personality/SKILL.md && ! grep -q "Specific methodology" skills/larry-personality/SKILL.md && grep -q "HSI Semantic Surprise Analysis Assistant" skills/larry-personality/SKILL.md && grep -q "Six Thinking Hats" skills/larry-personality/SKILL.md && grep -q "commandsForFramework" skills/larry-personality/SKILL.md && grep -q "7e" skills/larry-personality/SKILL.md</automated>
  </verify>
  <done>The governing resolver clause sits above the Provoked table; the table carries fireable rows anchored on the EXACT frontmatter framework names -- Reverse Salient Analysis (PUSH-02), HSI Semantic Surprise Analysis Assistant with the 20+ thin-room gate (PUSH-03), Usher's Model of Cumulative Synthesis / Four Lenses of Innovation (PUSH-04), Six Thinking Hats (PUSH-05) -- each with its JTBD outcome and a parenthetical illustration slug; Reach-rule 7e distinguishes HSI from whitespace and names both as render labels; the Intelligence Hierarchy lists Team-perspective; the forward-compatible chaining doctrine (curated_chains/recommendFrameworkChain/composeWorkflow, single-move floor) is present; HEAD drift green (no new reach-id) AND the working-tree dial-window grep lists only the frozen 5; zero em-dashes. Authoritative drift verdict deferred to Plan 04 post-commit.</done>
</task>

<task type="auto">
  <name>Task 3: PUSH-04 + PUSH-05 pws-methodology wiring -- DbA surface line + BONO Team Perspective section (framework-name-anchored per WFL-01)</name>
  <files>skills/pws-methodology/SKILL.md</files>
  <read_first>
    - skills/pws-methodology/SKILL.md lines 25-35 (the Design-by-Analogy (DbA) section -- line 27 lists the 4 detection signals as passive availability; PUSH-04 turns them into a proactive surface line)
    - AUDIT-2-proactive-push.json -> push_doctrine_to_add[3].surface (wire DbA signals to the push) AND [4].surface (the BONO Team Perspective section is the natural home for the 5 handoffs)
    - AUDIT-3-resolver-integration.json -> report.wfl_requirement (a) + report.plan_impact (Task 3 already half-acknowledges the only-door rule -- extend it: anchor on the EXACT framework names, slug late-bound via the resolver)
    - docs/MINDRIAN-CANON.md Appendix E (the 5 handoff triggers + the verbatim canon single line "[Incoming member] is ready. Accept, reshape, or stay where we are?")
    - skills/pws-methodology/SKILL.md line 13 (the resolver-is-the-only-door rule -- do NOT name /mos: commands from memory; the BONO section anchors on Six Thinking Hats and the DbA line on Usher's Model of Cumulative Synthesis / Four Lenses of Innovation, framed as JTBD outcomes; the resolver owns the slugs)
  </read_first>
  <action>
    PUSH-04 (DbA): turn the line-27 detection signals into a proactive surface line. Add one sentence after line 27 stating that when these signals fire (a tension with no in-domain resolution OR two+ domains live), Larry surfaces the cross-domain offer proactively -- "Cross-domain pattern between [Domain A] and [Domain B] -- most breakthroughs are borrowed structure. Run analogy discovery?" -- ending at a Decision Gate, anchored on the EXACT framework names Usher's Model of Cumulative Synthesis (connect what the room split) composing under brain_consult, or Four Lenses of Innovation (borrow structure across domains) composing under context_block. The /mos: slug is whatever the resolver returns at surface-time (line-13 only-door rule); never typed from memory. Part 8: find-connections carries generic handles only; find-analogies --external is public SIGNAL + MCP-stack-ask.
    PUSH-05 (BONO): add a new "## BONO Team Perspective" section anchored on the EXACT framework name Six Thinking Hats, carrying the 5 Appendix-E handoff triggers as named surface lines: risk surface -> Black, evidence thin -> White, plan without owner -> Blue, navigator stuck/circular -> Red, jargon density high -> Green. Each is surfaced as the canon's single Decision-Gate line "[Incoming member] is ready. Accept, reshape, or stay where we are?" (the navigator always decides -- one handoff = ONE offer, never a six-hat stack in one beat). State: the BONO hat-sequence selection is generic methodology (problem-type + complexity enums only to Brain, never the user's contradiction text -- Part 8); team_perspective is a render label, not a reach-id and not a framework name; the framework is Six Thinking Hats and the /mos: slug it resolves to (think-hats / persona / hat-briefing) comes from the resolver at surface-time, never typed from memory; the push composes under brain_consult + SENS-05/SENS-07; the push lives in Larry's doctrine, the persona-analyst agent stays reactive.
    No fenced code blocks. Hyphens only, no em-dashes. Honor the resolver-is-the-only-door rule (anchor on the EXACT framework names as JTBD outcomes; the resolver owns the slugs; any slug shown is a parenthetical illustration).
  </action>
  <acceptance_criteria>
    - grep-presence: BONO section present, anchored on the framework name -- `grep -c "BONO Team Perspective" skills/pws-methodology/SKILL.md` >= 1 AND `grep -c "Six Thinking Hats" skills/pws-methodology/SKILL.md` >= 1
    - grep-presence: the 5 handoffs detectable -- `grep -ci "risk surface\|evidence thin\|plan without owner\|navigator stuck\|jargon" skills/pws-methodology/SKILL.md` >= 1
    - grep-presence: the canon single line -- `grep -c "Accept, reshape, or stay" skills/pws-methodology/SKILL.md` >= 1
    - grep-presence: DbA proactive line anchored on the framework names -- `grep -ci "borrowed structure\|Run analogy discovery\|cross-domain pattern" skills/pws-methodology/SKILL.md` >= 1 AND `grep -c "Usher's Model of Cumulative Synthesis\|Four Lenses of Innovation" skills/pws-methodology/SKILL.md` >= 1
    - grep-presence: Part 8 enum-only on BONO -- `grep -ci "problem-type\|enum" skills/pws-methodology/SKILL.md` >= 1
    - zero em-dashes in skills/pws-methodology/SKILL.md
  </acceptance_criteria>
  <verify>
    <automated>grep -q "BONO Team Perspective" skills/pws-methodology/SKILL.md && grep -q "Six Thinking Hats" skills/pws-methodology/SKILL.md && grep -q "Accept, reshape, or stay" skills/pws-methodology/SKILL.md && grep -qi "borrowed structure" skills/pws-methodology/SKILL.md</automated>
  </verify>
  <done>The DbA section carries a proactive cross-domain surface line ending at a Decision Gate, anchored on Usher's Model of Cumulative Synthesis / Four Lenses of Innovation with the slug late-bound via the resolver (PUSH-04); a "BONO Team Perspective" section anchored on Six Thinking Hats carries the 5 Appendix-E handoffs as named surface lines using the verbatim canon Decision-Gate single line, with Part 8 enum-only framing and team_perspective named as a render label (not a framework name) (PUSH-05); the resolver owns every slug; zero em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| push-line offer -> reach-id set | a push read as a new reach-id breaks the frozen-5 drift test |
| SENS-03 / SENS-04 / find-connections -> Brain/web | the egress paths; push-lines must say generic handles only |
| thin room -> HSI/whitespace push | a push before 20+ artifacts fires a silent-zero (Phase 88.6 bug) |
| framework name -> /mos: slug | WFL-01: the slug must come from the resolver at surface-time, never named from memory; a colloquial label resolves to [] (name-drift trap) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1432.02-01 | Tampering | reach-id inflation (team_perspective / whitespace / a backtick posture word / a framework name read as 6th id) | mitigate | write them as PLAIN words (no backticks) inside the dial window; posture words pull_back/hold/push_forward stay plain prose; framework name "Hypothesis-Driven Problem Solving" stays plain prose; state explicitly team_perspective/whitespace are render labels; test-reach-ids-drift.cjs (HEAD) gates every task AND a working-tree dial-window grep guards mid-task before commit |
| T-1432.02-02 | Information Disclosure | PUSH-01 brain_consult + PUSH-06 deep_research + PUSH-04 find-connections | mitigate | every push-line says "generic handles only, the OFFER not the fetch"; test-sensors-part8-sweep.cjs stays green |
| T-1432.02-03 | Denial of Service | thin-room HSI/whitespace push | mitigate | mandatory "Best after 20+ entries" gate inline in the PUSH-03 row (hard-fence 5) |
| T-1432.02-04 | Elevation of Privilege | push-vs-pull conflation | mitigate | every push-line ends at a Decision Gate, never a verdict (GUIDED default, hard-fence 2) |
| T-1432.02-05 | Tampering | one-reach-per-beat erosion | accept | the Intelligence Hierarchy arbitrates ties; deep_research is the only sanctioned multi-reach exception (Reach rule 6) |
| T-1432.02-06 | Tampering | hardcoded /mos: literal re-asserting the "names commands from memory" anti-pattern (false-clean illusion) | mitigate | WFL-01: anchor every row + push-line on the EXACT framework name + JTBD outcome; route the slug through commandsForFramework() at surface-time; the governing resolver clause above the Provoked table + mirrored above the deep_research row marks any shown slug as illustration-only; the deep_research-row rewrite removes the one in-window literal; Plan 04 asserts the clause is PRESENT (not that legacy slugs are absent) + adds the 6-framework resolvability assertion |
| T-1432.02-SC | Tampering | npm/pip/cargo installs | accept | no package installs (pure markdown doctrine edit) |
</threat_model>

<verification>
After all three tasks:
- `node tests/test-reach-ids-drift.cjs` PASS (still exactly 5 reach-ids; team_perspective/whitespace are plain-word render labels; posture words + framework names are plain prose; the deep_research-row rewrite removed a slash-literal, not a backtick token -- HEAD authority, Plan 04 is the final post-commit gate)
- WORKING-TREE dial-window grep lists only the frozen 5 reach-ids after Tasks 1 and 2 (the mid-task guard the HEAD test cannot give pre-commit; in particular no backtick `pull_back`, no new framework-name token)
- `node tests/test-sensors-part8-sweep.cjs` exits 0 (egress paths still clean)
- `node tests/test-sensors-routing-fence.cjs` exits 0
- WFL-01: every push-family + Provoked row anchors on its EXACT frontmatter framework name (grep Reverse Salient Analysis / HSI Semantic Surprise Analysis Assistant / Usher's Model of Cumulative Synthesis / Four Lenses of Innovation / Six Thinking Hats / Hypothesis-Driven Problem Solving); the governing resolver clause (commandsForFramework) is present above the Provoked table AND mirrored near the deep_research row; the deep_research row names the framework with no hardcoded /mos:research literal in the dial window
- All 6 push-families present by grep: SENS-03 (PUSH-01), Reverse Salient Analysis/SENS-02 (PUSH-02), HSI Semantic Surprise Analysis Assistant + 20+ gate (PUSH-03), Usher's/Four Lenses (PUSH-04), Six Thinking Hats + BONO Team Perspective (PUSH-05), Deep Research Escalation Explicit Triggers (PUSH-06)
- Every push ends at a Decision Gate (grep the offer-then-gate phrasing); the 20+ thin-room gate present; deep_research plan-gated
- Zero em-dashes in both files
</verification>

<success_criteria>
ROADMAP success criterion 2 (PUSH) is satisfied: all 6 reach-families carry a proactive push-line with a NAMED trigger (Brain/SENS-03, reverse-salient/SENS-02, HSI-whitespace/SENS-06, find-analogies/SENS-01-06, BONO-hats/SENS-05-07, hat-scoped deep-research/SENS-04), each anchored on its EXACT frontmatter framework name + JTBD outcome and ending at a Decision Gate, never a verdict. WFL-01 (resolver-integration) is satisfied: every Provoked row + push-line anchors on the exact framework name; the governing resolver clause governs the table (mirrored above the deep_research row); the deep_research row names Hypothesis-Driven Problem Solving with no hardcoded /mos:research literal in the dial window; the forward-compatible chaining doctrine (curated_chains/recommendFrameworkChain, single-move floor) is present. Composition held: no 6th reach-id; team_perspective/whitespace are render labels; posture words + framework names stay plain prose; the 20+ gate and the deep-research plan-gate are present.
</success_criteria>

<output>
Create `.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
