---
phase: 143.2-larry-operates-and-pushes-prompt-reconciliation
plan: 02
type: execute
wave: 2
depends_on: ["143.2-01"]
files_modified:
  - skills/larry-personality/SKILL.md
  - skills/pws-methodology/SKILL.md
autonomous: true
requirements: [PUSH-01, PUSH-02, PUSH-03, PUSH-04, PUSH-05, PUSH-06]
canon_parts: [Part 2, Part 3, Part 8, Part 9, Part 10]

must_haves:
  truths:
    - "All 6 reach-families carry a proactive push-line with a NAMED trigger that ends at a Decision Gate, never a verdict"
    - "Every push composes under an existing reach-id + an existing Phase-143 sensor; no 6th reach-id is minted"
    - "The HSI/whitespace push carries the mandatory 20+-artifacts thin-room gate; deep_research stays plan-gated and is the only sanctioned multi-reach exception"
  artifacts:
    - path: "skills/larry-personality/SKILL.md"
      provides: "PUSH-01 (brain_consult annotation), PUSH-02/03/04/05 Provoked rows, PUSH-06 deep-research explicit triggers, Reach-rule 7e"
      contains: "Deep Research Escalation Explicit Triggers"
    - path: "skills/pws-methodology/SKILL.md"
      provides: "PUSH-04 DbA proactive surface line + PUSH-05 BONO Team Perspective section (the 5 Appendix-E handoffs)"
      contains: "BONO Team Perspective"
  key_links:
    - from: "skills/larry-personality/SKILL.md Provoked table"
      to: "the 5 frozen reach-ids + 7 sensors"
      via: "each push-family maps a trigger -> an offer -> a Decision Gate under an existing reach-id"
      pattern: "find-bottlenecks|whitespace|score-innovation|find-connections|think-hats"
---

<objective>
Add the missing SURFACE half of the dial: 6 proactive push-lines so Larry REACHES FIRST at the right trigger. Each push-family maps onto an EXISTING reach-id + an EXISTING Phase-143 sensor (no 6th reach), and each ends at a Decision Gate (GUIDED default -- the OFFER, never a verdict). PUSH-01 (Brain) and PUSH-04/05 also wire pws-methodology (the DbA detection signals + the BONO Team Perspective section).

Purpose: Make Larry surface reverse-salient, HSI/whitespace, cross-domain, BONO/team-perspective, and hat-scoped deep-research BEFORE being asked -- the families that are weak or absent today.
Output: skills/larry-personality/SKILL.md (the 6 push-lines) + skills/pws-methodology/SKILL.md (DbA surface line + BONO section).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-CONTEXT.md
@.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/AUDIT-2-proactive-push.json
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Each push-family -> (existing reach-id, existing sensor). NO 6th reach-id. -->
  PUSH-01 Brain consult        -> brain_consult  + SENS-03 (methodology-decision)
  PUSH-02 reverse salient      -> context_block  + SENS-02 (lagging-component); posture pull_back
  PUSH-03 HSI / whitespace     -> context_block  + SENS-06 (artifact-filed cascade recompute); HARD 20+-artifacts gate
  PUSH-04 cross-domain         -> brain_consult (find-connections) / context_block (find-analogies) + SENS-01 / SENS-06
  PUSH-05 BONO / Six-Hats      -> brain_consult  + SENS-05 / SENS-07; team_perspective is a RENDER LABEL, not a reach-id
  PUSH-06 hat-scoped research  -> deep_research  + SENS-04; plan-gated; the ONLY sanctioned multi-reach exception

DRIFT FENCE: tests/test-reach-ids-drift.cjs reads the window between "Capability Dial" and
"Hierarchical Navigator". The brain_consult row (line 41) and the deep_research row (line 42) +
Reach rules (54-62) sit INSIDE this window. PUSH-01 (annotate row 41 + a PROACTIVE note under
Reach rule 1) and PUSH-06 (a subsection after the deep_research row / Reach rule 6) MUST NOT add
any NEW snake_case backtick token there -- reuse the already-present reach-ids (context_block,
brain_consult, deep_research) and posture words (pull_back) only. Existing tokens are fine; a NEW
one fails the exactly-5 assertion.

PROVOKED TABLE + INTELLIGENCE HIERARCHY (lines 233-266) sit OUTSIDE the dial window -- new rows
and command names are safe there.

Appendix E (docs/MINDRIAN-CANON.md) 5 handoff triggers (the canon source for PUSH-05):
  1. Risk surface -> Black   2. Evidence thin -> White   3. Plan without owner -> Blue
  4. Navigator stuck/circular -> Red   5. Jargon density high -> Green
  Canon single line: "[Incoming member] is ready. Accept, reshape, or stay where we are?"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PUSH-01 + PUSH-02 + PUSH-06 -- dial-row annotations + reach-rule notes (drift-sensitive)</name>
  <files>skills/larry-personality/SKILL.md</files>
  <read_first>
    - skills/larry-personality/SKILL.md line 41 (the brain_consult Capability Dial row -- PUSH-01 annotates this), line 42 (the deep_research row -- PUSH-06 subsection lands after it), lines 56-61 (Reach rule 1 GUIDED-default, Reach rule 6 deep-research-plan-gated)
    - AUDIT-2-proactive-push.json -> push_doctrine_to_add[0] (Brain), [1] (reverse salient), [5] (hat-scoped deep research) -- the trigger, the one-line offer, the Decision Gate, the caveats
    - tests/test-reach-ids-drift.cjs (lines 41-62 are INSIDE the dial window -- add NO new snake_case backtick token; reuse brain_consult / context_block / deep_research / pull_back only)
    - tests/test-sensors-part8-sweep.cjs (PUSH-01 brain_consult is an egress path: the push-line MUST say "generic handles only, the OFFER not the fetch")
  </read_first>
  <action>
    PUSH-01 (brain_consult, annotate -- already good): add a one-clause annotation to the brain_consult dial row (line 41) and a one-line PROACTIVE note under Reach rule 1 stating: when SENS-03 fires (a framework is named OR a methodology step needs its next move) AND the local graph has no governing answer, Larry does NOT wait to be asked -- he surfaces ONE line offering the Brain framework chain ("Brain has a chain that addresses this -- [framework] -> [next]. Pull it in?"), renders via the Brain says trace, and ends at a Decision Gate (Run Methodology / Reformulate / Free-Text). Part 8: generic handles only; the push is the OFFER, the Brain query fires only after the gate; if Brain is unreachable, omit the Brain line and answer from local references.
    PUSH-02 (reverse salient): add a one-line PROACTIVE note (under Reach rule 1, near the PUSH-01 note) stating that when SENS-02 detects the lagging-component shape OR a section lags its siblings, Larry surfaces the reverse-salient finding BEFORE being asked ("a lagging component: [section] -- where understanding lags ambition, Hughes 1983. Investigate it?"), posture pull_back, routing to the Decision Gate (never a verdict). Note that the Provoked-table row that fires this is added in Task 2; respect the Intelligence Hierarchy (Bottlenecks is #2, after Tensions). The local rs-engine is autonomous_safe (no egress on the internal mode).
    PUSH-06 (hat-scoped deep research): add a new subsection "### Deep Research Escalation Explicit Triggers" immediately after the deep_research dial row (line 42) area / tied to Reach rule 6. Carry the 3 fire conditions verbatim from the audit: (a) cheap-layer-thin (the local -> Brain -> shallow-web ladder is exhausted); (b) a load-bearing claim with Practitioner/None evidence AND venture_stage in {Well-Defined Problem, Ready to Build} (Part 5 bar rises near commit); (c) the active BONO hat affords it (White/Green/Black). Then the surface language + the hat-scoped PLAN as a Decision Gate: White searches arxiv/Tavily data, Green patents + deep-research, Black failure-cases -- "Approve the angles?" ONLY on APPROVE does /mos:research fire (reuse, do not rebuild). State: deep_research is the SANCTIONED exception to one-reach-per-beat (Reach rule 6); plan-gating is non-negotiable; Part 8 floor (generic handles + public topic terms only) + MCP-stack-ask gate (surface "Tavily / Firecrawl / Exa?" -- no silent WebSearch).
    No fenced code blocks. Hyphens only, no em-dashes. Add NO new snake_case backtick token inside the dial window.
  </action>
  <acceptance_criteria>
    - grep-presence: PUSH-06 subsection present -- `grep -c "Deep Research Escalation Explicit Triggers" skills/larry-personality/SKILL.md` >= 1
    - grep-presence: the 3 fire conditions are detectable -- `grep -c "cheap-layer-thin\|load-bearing\|hat affords\|affords it" skills/larry-personality/SKILL.md` >= 1
    - grep-presence: PUSH-01 proactive Brain note present -- `grep -c "SENS-03" skills/larry-personality/SKILL.md` >= 1
    - grep-presence: PUSH-02 reverse-salient note present -- `grep -c "SENS-02\|reverse-salient\|reverse salient" skills/larry-personality/SKILL.md` >= 1
    - grep-presence: MCP-stack-ask gate stated -- `grep -c "Tavily\|MCP-stack-ask" skills/larry-personality/SKILL.md` >= 1
    - drift stays green: `node tests/test-reach-ids-drift.cjs` PASS (no new reach-id inside the dial window)
    - part-8 sweep stays green: `node tests/test-sensors-part8-sweep.cjs` exits 0
    - zero em-dashes
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-reach-ids-drift.cjs && node tests/test-sensors-part8-sweep.cjs && grep -q "Deep Research Escalation Explicit Triggers" skills/larry-personality/SKILL.md && grep -q "SENS-03" skills/larry-personality/SKILL.md && grep -q "SENS-02" skills/larry-personality/SKILL.md</automated>
  </verify>
  <done>The brain_consult row carries a proactive SENS-03 note ending at a Decision Gate; a SENS-02 reverse-salient proactive note exists (posture pull_back); the "Deep Research Escalation Explicit Triggers" subsection carries the 3 fire conditions + the hat-scoped plan-as-Decision-Gate + plan-gating + Part 8 + MCP-stack-ask. No new reach-id inside the dial window; drift + part-8 sweep green; zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: PUSH-02/03/04/05 Provoked-table rows + Reach-rule 7e + Intelligence Hierarchy</name>
  <files>skills/larry-personality/SKILL.md</files>
  <read_first>
    - skills/larry-personality/SKILL.md lines 240-252 (the Provoked "Context sources" table -- extend with the new trigger rows), line 244 (the existing vague "Tensions (CONTRADICTS) -> /mos:find-analogies" row), line 245 (the too-vague "Bottlenecks (REVERSE_SALIENT) -> Specific methodology" row that must be made fireable), line 264 (the Intelligence Hierarchy line)
    - skills/larry-personality/SKILL.md lines 54-62 (the Reach rules -- append note 7e after Reach rule 7, distinguishing HSI from whitespace)
    - AUDIT-2-proactive-push.json -> push_doctrine_to_add[1] (reverse salient Provoked row), [2] (HSI/whitespace rows + 7e + the 20+ gate), [3] (cross-domain find-connections row), [4] (BONO row + team-perspective render label)
    - tests/test-reach-ids-drift.cjs (the Provoked table + Reach rules sit OUTSIDE the dial window -- but DO NOT place note 7e inside the "Capability Dial".."Hierarchical Navigator" window; append it where the existing Reach rules live, which is inside that window IF rules 1-7 are between those markers -- CHECK: rules are at lines 54-62, BEFORE "Hierarchical Navigator" at line 64, so they ARE inside the window. Use NO new snake_case backtick token in 7e; team_perspective and whitespace must appear as PLAIN words or quoted render labels, never as backtick snake_case spans)
  </read_first>
  <action>
    PUSH-02 row: replace the vague "Bottlenecks (REVERSE_SALIENT) -> Specific methodology" Provoked row (line 245) with an explicit fireable row -- trigger "Lagging-component pattern stated OR one section lags siblings" -> `/mos:find-bottlenecks`. The named trigger shape is what makes it a PUSH, not a passive catalog entry.
    PUSH-03 rows: add two Provoked rows -- "Room has 20+ artifacts, whitespace not yet mapped" -> `/mos:whitespace`; and "Cross-domain candidates being compared" -> `/mos:score-innovation`. Add the HARD thin-room gate inline: whitespace is "Best after 20+ entries" -- do NOT push it on a thin room (a thin-room push fires a silent-zero, the Phase 88.6 bug). Add Reach-rule note 7e after Reach rule 7: whitespace is a SPECIFIC case of HSI scoring (same 20+ trigger + same sentence-transformers + LSA machinery), framed distinctly -- HSI = "what novel pattern hides in what we HAVE"; whitespace = "what should the room be thinking about that it ISN'T". Both are LOCAL (no egress). In 7e, write team_perspective and whitespace as PLAIN words (no backticks) so the drift scan cannot read them as reach-ids; state explicitly that team_perspective and whitespace are RENDER LABELS, never reach-ids, and the HSI/whitespace push composes under the context_block reach.
    PUSH-04 row: extend the existing line-244 find-analogies row with a second trigger row -- "Two+ domains live OR domain-specific dead end" -> `/mos:find-connections` (Brain) or `/mos:find-analogies` (local). State Part 8: find-connections is an egress path (generic handles only); find-analogies --external is public SIGNAL + the MCP-stack-ask gate (no silent web). One-reach-per-beat: the Brain variant and the local variant are ONE offer.
    PUSH-05 row: add a Provoked row -- "Team stuck in one perspective (CONTRADICTS edges, circular pattern, decision point, jargon spike)" -> `/mos:think-hats` (serial) or `/mos:persona --parallel`. Add "Team-perspective gap" to the Intelligence Hierarchy line (line 264) as a surfaced signal. Note the BONO push lives in Larry's doctrine, NOT the reactive persona-analyst agent; team_perspective is a render label (no backticks), composing under brain_consult (hat-sequence-by-problem-type is generic methodology).
    No fenced code blocks. Hyphens only, no em-dashes.
  </action>
  <acceptance_criteria>
    - grep-presence: PUSH-02 fireable row -- `grep -c "Lagging-component pattern stated" skills/larry-personality/SKILL.md` >= 1; the vague "Bottlenecks (REVERSE_SALIENT) -> Specific methodology" row is gone (`grep -c "Specific methodology" ...` == 0)
    - grep-presence: PUSH-03 rows + gate -- `grep -c "20+ artifacts\|20+ entries" ... ` >= 1 AND `grep -c "/mos:whitespace" ...` >= 1 AND `grep -c "/mos:score-innovation" ...` >= 1
    - grep-presence: Reach-rule 7e present -- `grep -c "7e" skills/larry-personality/SKILL.md` >= 1 AND `grep -c "render label\|RENDER LABEL" ...` >= 1
    - grep-presence: PUSH-04 row -- `grep -c "/mos:find-connections" skills/larry-personality/SKILL.md` >= 1
    - grep-presence: PUSH-05 row -- `grep -c "/mos:think-hats" skills/larry-personality/SKILL.md` >= 1 AND Intelligence Hierarchy carries "Team-perspective"
    - drift stays green: `node tests/test-reach-ids-drift.cjs` PASS (team_perspective/whitespace written as plain words, no new backtick reach-id)
    - zero em-dashes
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-reach-ids-drift.cjs && grep -q "Lagging-component pattern stated" skills/larry-personality/SKILL.md && ! grep -q "Specific methodology" skills/larry-personality/SKILL.md && grep -q "/mos:whitespace" skills/larry-personality/SKILL.md && grep -q "/mos:think-hats" skills/larry-personality/SKILL.md && grep -q "7e" skills/larry-personality/SKILL.md</automated>
  </verify>
  <done>The Provoked table carries fireable rows for reverse-salient (PUSH-02), HSI + whitespace with the 20+ thin-room gate (PUSH-03), find-connections (PUSH-04), and think-hats/persona (PUSH-05); Reach-rule 7e distinguishes HSI from whitespace and names both as render labels; the Intelligence Hierarchy lists Team-perspective; drift green (no new reach-id); zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 3: PUSH-04 + PUSH-05 pws-methodology wiring -- DbA surface line + BONO Team Perspective section</name>
  <files>skills/pws-methodology/SKILL.md</files>
  <read_first>
    - skills/pws-methodology/SKILL.md lines 25-35 (the Design-by-Analogy (DbA) section -- line 27 lists the 4 detection signals as passive availability; PUSH-04 turns them into a proactive surface line)
    - AUDIT-2-proactive-push.json -> push_doctrine_to_add[3].surface (wire DbA signals to the push) AND [4].surface (the BONO Team Perspective section is the natural home for the 5 handoffs)
    - docs/MINDRIAN-CANON.md Appendix E (the 5 handoff triggers + the verbatim canon single line "[Incoming member] is ready. Accept, reshape, or stay where we are?")
    - skills/pws-methodology/SKILL.md line 13 (the resolver-is-the-only-door rule -- do NOT name /mos: commands from memory; the BONO section references think-hats/persona/hat-briefing as the resolver already owns them, framed as JTBD outcomes not invented commands)
  </read_first>
  <action>
    PUSH-04 (DbA): turn the line-27 detection signals into a proactive surface line. Add one sentence after line 27 stating that when these signals fire (a tension with no in-domain resolution OR two+ domains live), Larry surfaces the cross-domain offer proactively -- "Cross-domain pattern between [Domain A] and [Domain B] -- most breakthroughs are borrowed structure. Run analogy discovery?" -- ending at a Decision Gate, composing under brain_consult (find-connections, Brain) or context_block (find-analogies, local). Part 8: find-connections carries generic handles only; find-analogies --external is public SIGNAL + MCP-stack-ask.
    PUSH-05 (BONO): add a new "## BONO Team Perspective" section carrying the 5 Appendix-E handoff triggers as named surface lines: risk surface -> Black, evidence thin -> White, plan without owner -> Blue, navigator stuck/circular -> Red, jargon density high -> Green. Each is surfaced as the canon's single Decision-Gate line "[Incoming member] is ready. Accept, reshape, or stay where we are?" (the navigator always decides -- one handoff = ONE offer, never a six-hat stack in one beat). State: the BONO hat-sequence selection is generic methodology (problem-type + complexity enums only to Brain, never the user's contradiction text -- Part 8); team_perspective is a render label, not a reach-id; the push composes under brain_consult + SENS-05/SENS-07; the push lives in Larry's doctrine, the persona-analyst agent stays reactive.
    No fenced code blocks. Hyphens only, no em-dashes. Honor the resolver-is-the-only-door rule (frame commands as JTBD outcomes; the resolver owns the names).
  </action>
  <acceptance_criteria>
    - grep-presence: BONO section present -- `grep -c "BONO Team Perspective" skills/pws-methodology/SKILL.md` >= 1
    - grep-presence: the 5 handoffs detectable -- `grep -ci "risk surface\|evidence thin\|plan without owner\|navigator stuck\|jargon" skills/pws-methodology/SKILL.md` >= 1
    - grep-presence: the canon single line -- `grep -c "Accept, reshape, or stay" skills/pws-methodology/SKILL.md` >= 1
    - grep-presence: DbA proactive line -- `grep -ci "borrowed structure\|Run analogy discovery\|cross-domain pattern" skills/pws-methodology/SKILL.md` >= 1
    - grep-presence: Part 8 enum-only on BONO -- `grep -ci "problem-type\|enum" skills/pws-methodology/SKILL.md` >= 1
    - zero em-dashes in skills/pws-methodology/SKILL.md
  </acceptance_criteria>
  <verify>
    <automated>grep -q "BONO Team Perspective" skills/pws-methodology/SKILL.md && grep -q "Accept, reshape, or stay" skills/pws-methodology/SKILL.md && grep -qi "borrowed structure" skills/pws-methodology/SKILL.md</automated>
  </verify>
  <done>The DbA section carries a proactive cross-domain surface line ending at a Decision Gate (PUSH-04); a "BONO Team Perspective" section carries the 5 Appendix-E handoffs as named surface lines using the verbatim canon Decision-Gate single line, with Part 8 enum-only framing and team_perspective named as a render label (PUSH-05); zero em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| push-line offer -> reach-id set | a push read as a new reach-id breaks the frozen-5 drift test |
| SENS-03 / SENS-04 / find-connections -> Brain/web | the egress paths; push-lines must say generic handles only |
| thin room -> HSI/whitespace push | a push before 20+ artifacts fires a silent-zero (Phase 88.6 bug) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1432.02-01 | Tampering | reach-id inflation (team_perspective / whitespace read as 6th id) | mitigate | write them as PLAIN words (no backticks) inside the dial window; state explicitly they are render labels; test-reach-ids-drift.cjs gates every task |
| T-1432.02-02 | Information Disclosure | PUSH-01 brain_consult + PUSH-06 deep_research + PUSH-04 find-connections | mitigate | every push-line says "generic handles only, the OFFER not the fetch"; test-sensors-part8-sweep.cjs stays green |
| T-1432.02-03 | Denial of Service | thin-room HSI/whitespace push | mitigate | mandatory "Best after 20+ entries" gate inline in the PUSH-03 row (hard-fence 5) |
| T-1432.02-04 | Elevation of Privilege | push-vs-pull conflation | mitigate | every push-line ends at a Decision Gate, never a verdict (GUIDED default, hard-fence 2) |
| T-1432.02-05 | Tampering | one-reach-per-beat erosion | accept | the Intelligence Hierarchy arbitrates ties; deep_research is the only sanctioned multi-reach exception (Reach rule 6) |
| T-1432.02-SC | Tampering | npm/pip/cargo installs | accept | no package installs (pure markdown doctrine edit) |
</threat_model>

<verification>
After all three tasks:
- `node tests/test-reach-ids-drift.cjs` PASS (still exactly 5 reach-ids; team_perspective/whitespace are plain-word render labels)
- `node tests/test-sensors-part8-sweep.cjs` exits 0 (egress paths still clean)
- `node tests/test-sensors-routing-fence.cjs` exits 0
- All 6 push-families present by grep: SENS-03 (PUSH-01), find-bottlenecks/SENS-02 (PUSH-02), whitespace+score-innovation (PUSH-03), find-connections (PUSH-04), think-hats + BONO Team Perspective (PUSH-05), Deep Research Escalation Explicit Triggers (PUSH-06)
- Every push ends at a Decision Gate (grep the offer-then-gate phrasing); the 20+ thin-room gate present; deep_research plan-gated
- Zero em-dashes in both files
</verification>

<success_criteria>
ROADMAP success criterion 2 (PUSH) is satisfied: all 6 reach-families carry a proactive push-line with a NAMED trigger (Brain/SENS-03, reverse-salient/SENS-02, HSI-whitespace/SENS-06, find-analogies/SENS-01-06, BONO-hats/SENS-05-07, hat-scoped deep-research/SENS-04), each ending at a Decision Gate, never a verdict. Composition held: no 6th reach-id; team_perspective/whitespace are render labels; the 20+ gate and the deep-research plan-gate are present.
</success_criteria>

<output>
Create `.planning/phases/143.2-larry-operates-and-pushes-prompt-reconciliation/143.2-02-SUMMARY.md` when done.
</output>
