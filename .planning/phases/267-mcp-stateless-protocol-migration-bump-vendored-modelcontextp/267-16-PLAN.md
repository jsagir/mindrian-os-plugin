---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 16
type: execute
wave: 14
locked_stage: "W4 (remove v1 last): move the remaining v1 importers in tests/ and docs/reviews probes to the v2 packages, so nothing imports @modelcontextprotocol/sdk before it is uninstalled"
depends_on: [267-15]
files_modified:
  - tests/test-354-concurrency-surfaces.cjs
  - tests/test-354-egress-typed-question.cjs
  - tests/test-354-extract-shallow-contract.cjs
  - tests/test-354-registration-diagnostics.cjs
  - tests/test-354-room-symlink-containment.cjs
  - tests/test-354-theo-journey.cjs
  - docs/reviews/phase-354-probes/registration.cjs
  - docs/reviews/phase-354-probes/resources.cjs
autonomous: true
requirements: [MCPV2-18]
canon_parts: [8, 11]
tags: [v1-removal, test-migration, phase-354-tests]

must_haves:
  truths:
    - "The six tests/test-354-*.cjs files and the two docs/reviews/phase-354-probes scripts import McpServer and InMemoryTransport from @modelcontextprotocol/server, Client from @modelcontextprotocol/client, and StdioClientTransport from @modelcontextprotocol/client/stdio; none imports @modelcontextprotocol/sdk"
    - "Each migrated test's outcome equals its 267-BASELINE.md outcome (a PASS stays PASS; a baseline FAIL stays the same FAIL with the same assertion), so the migration changes the harness, never the verdict"
    - "Assertions are not weakened: the diff of each test touches only require lines and API-shape adaptations the v2 surface forces (for example constructor or method names), each named in the SUMMARY"
  artifacts:
    - path: "tests/test-354-theo-journey.cjs"
      provides: "v2-hosted Theo journey test"
      contains: "@modelcontextprotocol/server"
    - path: "docs/reviews/phase-354-probes/resources.cjs"
      provides: "v2-hosted resources probe"
      contains: "@modelcontextprotocol/server"
  key_links:
    - from: "tests/test-354-*.cjs"
      to: "@modelcontextprotocol/server InMemoryTransport.createLinkedPair"
      via: "require"
      pattern: "InMemoryTransport"
---

<objective>
Migrate the eight remaining v1 importers that belong to Phase 354's test and probe set to the v2 packages, verdict-for-verdict, so the v1 SDK can be removed in 267-17 without a single test losing its harness.

Purpose: CTX "Scope and sequencing": the v1 dependency is removed LAST, "only once nothing imports it". The research's import-site table lists these files (11 test files plus docs/reviews/phase-354-probes). Because every registrar moved to registerTool/registerPrompt/registerResource on v1 first, these tests kept passing on a v1 host until now; switching the host package is mechanical.
Output: eight migrated files, outcomes equal to baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md

Session and scope rules (every task in this plan):
- Workspace /home/jsagi/dev/MindrianOS-Plugin only; record `PLAN_BASE=$(git rev-parse HEAD)` in the SUMMARY.
- Before editing any file run `git status --short -- <that file>`; a diff you did not make means STOP and report (Phase 354/355 sessions are active in this tree).
- Commit by explicit path only, `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git commit -a`, `git stash`.
- Never edit production code in this plan (bin/, lib/), `mcp-server-brain/**`, `/home/jsagi/Theo/**`.
- v2 import map (verified at plan time on 2.1.0; re-verify on the installed V): `McpServer`, `InMemoryTransport` (with `createLinkedPair()`) from '@modelcontextprotocol/server'; `Client`, `StreamableHTTPClientTransport` from '@modelcontextprotocol/client'; `StdioClientTransport` from '@modelcontextprotocol/client/stdio'. v2 has no variadic `server.tool`; a test that registers its own fixture tool must use `registerTool(name, { description, inputSchema }, cb)`.
- A test whose assertions would need to change to pass is NOT silently rewritten: STOP that file and report the API difference.
- No em-dashes. CJS only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate test-354 concurrency-surfaces, egress-typed-question, extract-shallow-contract, registration-diagnostics</name>
  <files>tests/test-354-concurrency-surfaces.cjs, tests/test-354-egress-typed-question.cjs, tests/test-354-extract-shallow-contract.cjs, tests/test-354-registration-diagnostics.cjs</files>
  <read_first>
    - tests/test-354-concurrency-surfaces.cjs:56-57, tests/test-354-egress-typed-question.cjs:37-38, tests/test-354-extract-shallow-contract.cjs:60-62, tests/test-354-registration-diagnostics.cjs:51-53 (the v1 requires) and each file's use of those symbols
    - 267-BASELINE.md rows for these four files
    - node_modules/@modelcontextprotocol/server/dist/index.d.cts and node_modules/@modelcontextprotocol/client/dist/index.d.cts (constructor and method signatures for McpServer, Client, InMemoryTransport)
  </read_first>
  <action>For each file (one commit per file): replace the v1 requires with the v2 import map; adapt only what the v2 surface forces (for example a fixture tool registered with the removed variadic form becomes registerTool); keep every assertion; run the file and compare with 267-BASELINE.md. Record per file in the SUMMARY: the lines changed and the before/after outcome.</action>
  <verify>
    <automated>for f in concurrency-surfaces egress-typed-question extract-shallow-contract registration-diagnostics; do node tests/test-354-$f.cjs; echo "test-354-$f exit=$?"; done; grep -c "@modelcontextprotocol/sdk" tests/test-354-concurrency-surfaces.cjs tests/test-354-egress-typed-question.cjs tests/test-354-extract-shallow-contract.cjs tests/test-354-registration-diagnostics.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v "^\s*//\|^\s*\*" <file> | grep -c "@modelcontextprotocol/sdk"` prints 0 for all four files.
    - Each file's exit code equals its 267-BASELINE.md outcome (PASS stays 0).
    - `git diff $PLAN_BASE -- <file> | grep "^[-+]" | grep -c "assert"` is 0 for each file, or the SUMMARY justifies each changed assertion line as a pure API rename.
  </acceptance_criteria>
  <done>Four test-354 files on v2 with baseline verdicts.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate test-354 room-symlink-containment, theo-journey and the two phase-354 review probes</name>
  <files>tests/test-354-room-symlink-containment.cjs, tests/test-354-theo-journey.cjs, docs/reviews/phase-354-probes/registration.cjs, docs/reviews/phase-354-probes/resources.cjs</files>
  <read_first>
    - tests/test-354-room-symlink-containment.cjs:60-62, tests/test-354-theo-journey.cjs:88-90, docs/reviews/phase-354-probes/registration.cjs:5, docs/reviews/phase-354-probes/resources.cjs:17-19
    - 267-BASELINE.md rows for the two tests
  </read_first>
  <action>Same procedure as Task 1, one commit per file. The two probe scripts under docs/reviews/ are not in any runner: run each once with `node <file>` before and after and record that the output shape is unchanged (they are reproduction probes, kept runnable). test-354-theo-journey must still need no network (it hosts over InMemoryTransport).</action>
  <verify>
    <automated>node tests/test-354-room-symlink-containment.cjs; echo "symlink exit=$?"; node tests/test-354-theo-journey.cjs; echo "theo exit=$?"; test -z "$(grep -v '^\s*//\|^\s*\*' tests/test-354-*.cjs docs/reviews/phase-354-probes/*.cjs | grep '@modelcontextprotocol/sdk')" && echo "no v1 in 354 set"</automated>
  </verify>
  <acceptance_criteria>
    - Zero non-comment `@modelcontextprotocol/sdk` references across tests/test-354-*.cjs and docs/reviews/phase-354-probes/*.cjs.
    - Both tests' exit codes equal 267-BASELINE.md; both probes run to completion (SUMMARY records their output line counts before and after).
  </acceptance_criteria>
  <done>Every Phase 354 test and probe runs on the v2 packages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> verdicts | a harness migration that quietly weakens an assertion would hide a real regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-34 | Repudiation / Tampering | test verdict integrity (Part 8 egress, symlink containment, registration diagnostics tests) | mitigate | outcomes must equal 267-BASELINE.md; assertion-line diff gate; a file whose assertions would need to change is stopped and reported, never rewritten |
| T-267-09 | Information disclosure | test-354-egress-typed-question and theo-journey (Part 8 tests) | mitigate | they keep their hermetic, no-network setup; only import lines change |
</threat_model>

<verification>
- No non-comment v1 import in the Phase 354 test and probe set; every verdict equals baseline.
</verification>

<success_criteria>
The largest block of remaining v1 importers is gone without changing a single test verdict.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| six tests/test-354-*.cjs files and two docs/reviews/phase-354-probes scripts on the v2 packages | migrated test harnesses | run-all-354, 267-17 removal |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-16-SUMMARY.md` when done (commit with `git add -f`).
</output>
