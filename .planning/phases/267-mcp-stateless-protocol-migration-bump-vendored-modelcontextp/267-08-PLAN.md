---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 08
type: execute
wave: 6
locked_stage: "W2 (local server stdio branch): registration rewrites of the remaining ten tools/*.cjs modules (20 sites)"
depends_on: [267-07]
files_modified:
  - lib/mcp/tools/context.cjs
  - lib/mcp/tools/dual-path.cjs
  - lib/mcp/tools/graph-reason.cjs
  - lib/mcp/tools/graph.cjs
  - lib/mcp/tools/identity.cjs
  - lib/mcp/tools/room.cjs
  - lib/mcp/tools/sensors.cjs
  - lib/mcp/tools/status.cjs
  - lib/mcp/tools/stop-gate.cjs
  - lib/mcp/tools/views.cjs
autonomous: true
requirements: [MCPV2-02, MCPV2-08]
canon_parts: [8, 9, 11]
tags: [registration-rewrite, registerTool, cirs]

must_haves:
  truths:
    - "All 20 remaining server.tool sites under lib/mcp/tools/ use registerTool config objects: context (1), dual-path (2), graph-reason (1), graph (3), identity (1), room (3), sensors (5), status (1), stop-gate (1), views (2)"
    - "Every file is its own commit; the per-commit gate (registration-API --file, CIRS gate, zod4 contract) passes after each of the ten commits"
    - "Handler bodies, including every `extra && extra.sessionId` read, are untouched; room binding and write-scope behavior are unchanged (room-bind, resolver-census and symlink-containment tests at baseline)"
    - "After this plan zero server.tool( sites remain anywhere under lib/mcp/"
  artifacts:
    - path: "lib/mcp/tools/sensors.cjs"
      provides: "5 registerTool registrations"
      contains: "server.registerTool("
    - path: "lib/mcp/tools/room.cjs"
      provides: "3 registerTool registrations"
      contains: "server.registerTool("
    - path: "lib/mcp/tools/views.cjs"
      provides: "2 registerTool registrations (artifact_file write path unchanged)"
      contains: "server.registerTool("
  key_links:
    - from: "lib/mcp/tools/*.cjs register(server, ctx)"
      to: "lib/mcp/register-core-tools.cjs"
      via: "auto-discovery; a module that throws at registration is recorded partial, so the live count check in registration-api full mode is the backstop"
      pattern: "registerTool"
---

<objective>
Finish the tool registration rewrites: the ten remaining lib/mcp/tools/*.cjs modules, 20 sites, one commit per file, gated per commit.

Purpose: CTX "Scope and sequencing" (51 hand-migrated sites) and CTX specifics (per-file commits, CIRS gates after each). These modules own the room read/write, graph, sensors, status and stop-gate surfaces; the rewrite changes only the registration call form, so Canon Part 9 write paths and Part 8 boundaries are untouched by construction and checked by their existing tests.
Output: 20 migrated sites across 10 commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-06-PLAN.md

Session and scope rules (every task in this plan):
- Workspace /home/jsagi/dev/MindrianOS-Plugin only; record `PLAN_BASE=$(git rev-parse HEAD)` in the SUMMARY.
- Before editing any file run `git status --short -- <that file>`; a diff you did not make means STOP and report (sensors.cjs carried peer edits during research).
- Commit by explicit path only, `git commit --only -m "<msg>" -- <path>`. Never `git add -A`, `git commit -a`, `git stash`.
- Never edit `lib/mcp/gate-render.cjs`, `lib/core/navigation.cjs`, `lib/core/session-binding.cjs`, `lib/core/room-path-containment.cjs`, `lib/core/brain-client.cjs`, `lib/core/part8-egress-guard.cjs`, `mcp-server-brain/**`, `/home/jsagi/Theo/**`.
- Registration rewrite rule: exactly the one in 267-06-PLAN.md's context block (verbatim DESC and SHAPE inside `z.object(...)`, untouched callback, mechanical Title Case TITLE, no strictness change, connectors exports untouched).
- Per-commit gate after EVERY file: `node tests/test-267-mcpv2-registration-api.cjs --file <file>`, `node tests/test-267-mcpv2-cirs-gates.cjs`, `node tests/test-267-mcpv2-zod4-contract.cjs`, plus that file's own tests (find them: `grep -l "tools/<basename>" tests/*.cjs lib/**/*.test.cjs`; run each that PASSED in 267-BASELINE.md or that the grep finds, and compare to baseline).
- No em-dashes. CJS only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: context, dual-path, graph-reason, graph, identity (8 sites, 5 commits)</name>
  <files>lib/mcp/tools/context.cjs, lib/mcp/tools/dual-path.cjs, lib/mcp/tools/graph-reason.cjs, lib/mcp/tools/graph.cjs, lib/mcp/tools/identity.cjs</files>
  <read_first>
    - lib/mcp/tools/context.cjs :34 (context_assemble)
    - lib/mcp/tools/dual-path.cjs :42 (detect_dual_path), :54 (extract_shallow)
    - lib/mcp/tools/graph-reason.cjs :59 (graph_reason)
    - lib/mcp/tools/graph.cjs :186 (graph_query), :217 (graph_write), :253 (memory_event)
    - lib/mcp/tools/identity.cjs :87 (identity_write)
    - 267-06-PLAN.md context block (the rewrite rule)
  </read_first>
  <action>
In order context.cjs, dual-path.cjs, graph-reason.cjs, graph.cjs, identity.cjs: verify the file is clean in git status; rewrite every `server.tool(` site in it by the rule (line numbers are plan-time anchors; locate by name literal); run the per-commit gate plus these file-specific tests at minimum: dual-path -> tests/test-354-extract-shallow-contract.cjs (still hosts the tools on the v1 McpServer, which has registerTool); graph and graph-reason -> every tests/test-*graph* file that PASSED at baseline; identity -> tests/test-270-* identity_write tests and tests/test-267-2-cr-01-identity-write-merge.cjs; context -> tests/test-198-contract-schema.test.cjs (compare to its baseline outcome) and tests/test-347-context-focus.cjs. Commit each file alone with message "refactor(267-08): <file> registerTool (<n> sites)".
  </action>
  <verify>
    <automated>for f in context dual-path graph-reason graph identity; do node tests/test-267-mcpv2-registration-api.cjs --file lib/mcp/tools/$f.cjs || exit 1; done; node tests/test-267-mcpv2-cirs-gates.cjs && node tests/test-267-mcpv2-zod4-contract.cjs && node tests/test-354-extract-shallow-contract.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "server\.tool(" lib/mcp/tools/{context,dual-path,graph-reason,graph,identity}.cjs` prints 0 for each.
    - `git log --oneline $PLAN_BASE..HEAD -- lib/mcp/tools/graph.cjs | wc -l` prints 1 (and 1 for each of the other four).
    - SUMMARY lists, per commit, the sha and the cirs-gate result line.
    - Every file-specific test's outcome equals 267-BASELINE.md (or, for tests not in the baseline, PASS).
  </acceptance_criteria>
  <done>Five files migrated in five green commits.</done>
</task>

<task type="auto">
  <name>Task 2: room, sensors, status, stop-gate, views (12 sites, 5 commits)</name>
  <files>lib/mcp/tools/room.cjs, lib/mcp/tools/sensors.cjs, lib/mcp/tools/status.cjs, lib/mcp/tools/stop-gate.cjs, lib/mcp/tools/views.cjs</files>
  <read_first>
    - lib/mcp/tools/room.cjs :350 (room_list), :372 (room_state_bound), :393 (room_search)
    - lib/mcp/tools/sensors.cjs :170 (suggest_next), :229 (reach_candidates), :254 (contradiction_check), :296 (whitespace_scan), :332 (framework_run)
    - lib/mcp/tools/status.cjs :130 (status_read; its capability_floor.tool_registration reports partial registration, which must still read complete after the rewrite)
    - lib/mcp/tools/stop-gate.cjs :54 (stop_gate_check)
    - lib/mcp/tools/views.cjs :388 (view_compile), :407 (artifact_file; the realpath-contained write path from Phase 354-05)
  </read_first>
  <action>
Same per-file procedure as Task 1, in order room.cjs, sensors.cjs, status.cjs, stop-gate.cjs, views.cjs. File-specific tests at minimum: room -> tests/test-248-room-bind-session-authoritative.cjs, tests/test-248-room-bind-honest-return.cjs, tests/test-248-resolver-census.cjs; sensors -> every tests/test-*sensor* / *suggest* / *whitespace* / *framework-run* file that PASSED at baseline; status -> tests/test-354-registration-diagnostics.cjs (and assert a live status_read via the wire helper reports tool_registration complete); stop-gate -> tests/test-198-stop-gate-retry-ceiling.test.cjs; views -> tests/test-354-room-symlink-containment.cjs. After the fifth commit run `grep -rn "server\.tool(" lib/mcp/` (must be empty) and `bash tests/run-all-198.sh` (baseline leg set).
  </action>
  <verify>
    <automated>for f in room sensors status stop-gate views; do node tests/test-267-mcpv2-registration-api.cjs --file lib/mcp/tools/$f.cjs || exit 1; done; node tests/test-267-mcpv2-cirs-gates.cjs && node tests/test-267-mcpv2-zod4-contract.cjs && test -z "$(grep -rn 'server\.tool(' lib/mcp/)" && node tests/test-354-room-symlink-containment.cjs && node tests/test-198-stop-gate-retry-ceiling.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "server\.tool(" lib/mcp/ | wc -l` prints 0.
    - One commit per file (`git log --oneline $PLAN_BASE..HEAD -- lib/mcp/tools/sensors.cjs | wc -l` prints 1, same for the other four).
    - A live status_read through tests/helpers/mcp-wire-267.cjs rpcOverStdio reports tool registration complete (no failed module).
    - `bash tests/run-all-198.sh` leg outcomes equal 267-BASELINE.md.
    - `git diff $PLAN_BASE -- lib/mcp/tools | grep "^[-+]" | grep -c "connectors\s*=\|hitl_shape"` prints 0.
  </acceptance_criteria>
  <done>All 39 server.tool sites in the repo are migrated (12 in 267-06, 7 in 267-07, 20 here).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> room/graph/views write tools | model-authored arguments that become room.db rows and files (Canon Part 9) |
| session identity -> room write scope | `extra.sessionId` selects the bound room for writes (V3/V4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-20 | Tampering | input validation of write tools | mitigate | shapes move verbatim inside z.object (identical strip semantics); zod4 contract test proves identical advertised schemas per commit |
| T-267-24 | Elevation of privilege | room write scope (views artifact_file, graph_write, memory_event) | mitigate | handler bodies untouched; test-354-room-symlink-containment and test-248 room-bind tests at baseline after the owning commit |
| T-267-22 | Spoofing | session identity read (`extra.sessionId`) | mitigate | same property name in v1 extra and v2 ctx (verified in v2 BaseContext); room-bind session-authoritative test green |
| T-267-21 | Elevation of privilege | CIRS declarations | mitigate | per-commit cirs gate; connectors exports untouched (diff grep) |
</threat_model>

<verification>
- `grep -rn "server\.tool(" lib/mcp/` empty.
- cirs gate and zod4 contract green; run-all-198 at baseline.
</verification>

<success_criteria>
Every MCP tool the local server registers is on the v2-compatible registration API, with zero wire and CIRS drift.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `title` on the 20 tools in context, dual-path, graph-reason, graph, identity, room, sensors, status, stop-gate, views | new wire field | host tool pickers |
| ten migrated tools/*.cjs registrars (registerTool) | migrated code | 267-11 swap |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-08-SUMMARY.md` when done (commit with `git add -f`).
</output>
