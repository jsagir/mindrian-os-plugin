---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 06
type: execute
wave: 4
locked_stage: "W2 (local server stdio branch): registration rewrites on v1 first, file by file; this plan: the registration-API test, tool-router.cjs (11 sites), contract-version.cjs (1 site)"
depends_on: [267-05]
files_modified:
  - tests/test-267-mcpv2-registration-api.cjs
  - lib/mcp/tool-router.cjs
  - lib/mcp/contract-version.cjs
autonomous: true
requirements: [MCPV2-02, MCPV2-08]
canon_parts: [7, 11]
tags: [registration-rewrite, registerTool, cirs, tool-router]

must_haves:
  truths:
    - "A registration-API test exists that FAILS against pre-migration code: it records every registration call a registrar makes and rejects the v1 variadic forms (server.tool, server.prompt, server.resource), a missing title, or an inputSchema that is not a zod object; it runs per file (--file) and in full mode (live tools/list: every tool has a title, tool count equals the baseline snapshot)"
    - "tool-router.cjs's 11 server.tool sites and contract-version.cjs's 1 site use server.registerTool(name, { title, description, inputSchema: z.object(<same shape>) }, <same callback>) on the STILL-v1 McpServer (v1 1.30.1 has registerTool), so each commit is independently green before the v2 swap in 267-11"
    - "Every description string and every input shape is byte-identical; only the call form and the new title change; the zod4 contract test proves the wire schemas did not move"
    - "Each file's rewrite is its own commit, and node tests/test-267-mcpv2-cirs-gates.cjs passes after EACH commit (connector registry --check clean, no new shape violation, descriptor count unchanged): the research's pitfall that this class of rewrite silently drops CIRS connector declarations is checked per commit, not at the end"
  artifacts:
    - path: "tests/test-267-mcpv2-registration-api.cjs"
      provides: "recorder-based per-file registration check plus live full-mode check"
      min_lines: 100
    - path: "lib/mcp/tool-router.cjs"
      provides: "11 registerTool registrations"
      contains: "server.registerTool("
    - path: "lib/mcp/contract-version.cjs"
      provides: "1 registerTool registration"
      contains: "server.registerTool("
  key_links:
    - from: "lib/mcp/tool-router.cjs"
      to: "MCP_TOOL_CONNECTORS export"
      via: "unchanged sibling connectors export read by scripts/build-connector-registry.cjs"
      pattern: "MCP_TOOL_CONNECTORS"
    - from: "tests/test-267-mcpv2-registration-api.cjs"
      to: "lib/mcp/register-core-tools.cjs"
      via: "full mode counts live tools, catching a module that fails registration and is silently skipped"
      pattern: "tools/list"
---

<objective>
Write the wire-and-source assertion that fails against the pre-migration registration code, then rewrite the two named-module registrars (tool-router.cjs: 11 sites; contract-version.cjs: 1 site) from the removed-in-v2 variadic `server.tool()` form to `server.registerTool()` config objects, one commit per file with the CIRS gate after each.

Purpose: CTX "Scope and sequencing" (hand-migrate all 51 sites, no codemod: measured zero rewrites on CJS and a harmful manifest edit) and CTX specifics ("Sequence registration-rewrite commits by file, and run build-connector-registry --check + check-shape-declaration after each"). Rewriting on v1 first is deliberate: v1 1.30.1 already implements `registerTool` with zod-object inputSchema (the brain shim has used it since Phase 257), and the callback's second argument exposes `sessionId` under the same property name in v1 (`extra.sessionId`) and v2 (`ctx.sessionId`, verified in the v2 type declarations at plan time), so handler bodies do not change and each file lands green on its own. `title` is folded in now (CTX Claude's Discretion: free while every registration is touched; zero host-permission impact). Tool `annotations` are deliberately NOT added in this phase (see threat T-267-14 and the 267-18 seed).
Output: the RED test, then two rewritten registrars.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-RESEARCH.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-05-SUMMARY.md
@tests/helpers/mcp-wire-267.cjs
@lib/mcp/register-core-tools.cjs

Session and scope rules (every task in this plan):
- Workspace /home/jsagi/dev/MindrianOS-Plugin only; record `PLAN_BASE=$(git rev-parse HEAD)` in the SUMMARY.
- Before editing any file run `git status --short -- <that file>`; a diff you did not make means STOP and report (tool-router.cjs and tools/sensors.cjs had peer edits during research).
- Commit by explicit path only, `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git commit -a`, `git stash`, never revert a diff you did not make.
- Never edit `mcp-server-brain/**`, `/home/jsagi/Theo/**`, `lib/core/brain-client.cjs`, `lib/core/part8-egress-guard.cjs`, `lib/core/session-binding.cjs`, `lib/mcp/gate-render.cjs`. Never run `@modelcontextprotocol/codemod`.
- Registration rewrite rule for tools (applies to 267-06..267-08): `server.tool(NAME, DESC, SHAPE, CB)` becomes `server.registerTool(NAME, { title: TITLE, description: DESC, inputSchema: z.object(SHAPE) }, CB)`. DESC and SHAPE move verbatim (same expressions, same comments); CB is untouched, including its parameter names. TITLE is derived mechanically: split NAME on `_` and `-`, capitalize each word, join with spaces (`room_state` -> `Room State`, `eureka_critic` -> `Eureka Critic`); no curated wording. A tool whose SHAPE is `{}` gets `z.object({})`. Wrap in `z.object` (the non-deprecated v2 overload; raw shapes route to v2's `LegacyToolCallback` path); strictness is NOT changed (never `z.strictObject` here, CTX accept-looser ruling).
- Do not touch any `connectors` / `MCP_TOOL_CONNECTORS` export, any `hitl_shape`, or any handler body.
- No em-dashes. CJS only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Registration-API test (RED against every unmigrated registrar)</name>
  <files>tests/test-267-mcpv2-registration-api.cjs</files>
  <read_first>
    - lib/mcp/register-core-tools.cjs (how tools/*.cjs modules are discovered and `register(server, ctx)` is called; how a module that throws is recorded as partial rather than failing boot, which is why a live COUNT check is required)
    - bin/mindrian-mcp-server.cjs lines 158-221 (createServer: the registrar call order and each registrar's arguments)
    - lib/mcp/tool-router.cjs lines 830-860 (registerRouterTools signature), lib/mcp/prompts.cjs lines 88-100, lib/mcp/resources.cjs lines 60-80, lib/mcp/contract-version.cjs, lib/mcp/app-views.cjs lines 229-240
    - tests/fixtures/267/wire-snapshot-zod4.json (the baseline tool count)
  </read_first>
  <behavior>
    - `--file lib/mcp/tool-router.cjs` on the unmigrated file: FAIL (11 server.tool calls recorded, 0 titles).
    - `--file` on a migrated file: PASS when every call is registerTool/registerPrompt/registerResource, every registerTool config has non-empty title and description and an inputSchema with a callable safeParse, every registerPrompt config has a non-empty description, every registerResource call passes a config object as its third argument.
    - Full mode (no --file): runs every registrar through the recorder AND a live wireSnapshot of the local server; FAILS while any registrar is unmigrated; PASSES when all are migrated, every live tool has a non-empty title, and the live tool count equals the wire-snapshot-zod4.json count.
  </behavior>
  <action>
Create tests/test-267-mcpv2-registration-api.cjs. Build a recording fake server object exposing `tool`, `prompt`, `resource` (record kind 'v1-variadic') and `registerTool`, `registerPrompt`, `registerResource` (record kind 'v2-config' with the config argument), plus a `server` property with no-op `getClientCapabilities`, `getClientVersion`, `elicitInput`, and a no-op `sendResourceListChanged`, `sendToolListChanged`, `sendPromptListChanged`, `isConnected`. Map `--file <relpath>` to the registrar call for that file: tool-router.cjs -> `registerRouterTools(fake, roomDir, pluginRoot, larryContext, 'cli')` (build larryContext with lib/mcp/larry-context.cjs loadLarryContext); contract-version.cjs -> its exported `registerContractVersion(fake)`; each lib/mcp/tools/*.cjs -> `mod.register(fake, ctx)` with the same ctx shape `registerCoreTools(server, ctx)` in lib/mcp/register-core-tools.cjs passes (read where it is called to get the real ctx fields); prompts.cjs -> `registerPrompts(fake, roomDir, pluginRoot)`; resources.cjs -> `registerResources(fake, { fallbackRoomDir, pluginRoot, surface: 'cli' })`; app-views.cjs -> `registerAppViews(fake, roomDir)` (its registerAppTool delegates to fake.registerTool). Use a hermetic temp room dir for roomDir. Attribution matters because `registerRouterTools` itself calls `registerCoreTools(server, { fallbackRoomDir, pluginRoot, surface })` (lib/mcp/tool-router.cjs:2297), which registers contract_version and every tools/*.cjs module on the same server: each recorded call therefore captures `new Error().stack` and is attributed to the first stack frame whose path is under `lib/mcp/` (skip node_modules frames, so ext-apps' registerAppTool attributes to app-views.cjs); `--file X` checks ONLY calls attributed to X. For each recorded call apply the behavior rules and print one PASS/FAIL line per call (`<file>: <kind> <name>`). Full mode iterates all registrar files above, then calls wireSnapshot(LOCAL_SERVER) and checks titles and count. Exit 1 on any FAIL.

Run `node tests/test-267-mcpv2-registration-api.cjs --file lib/mcp/tool-router.cjs` and `--file lib/mcp/contract-version.cjs`: both must FAIL now. Commit the test (RED). Note in the commit message and SUMMARY that full mode stays RED until 267-09 migrates the last registrar; per-file mode is the per-commit gate until then.
  </action>
  <verify>
    <automated>(node tests/test-267-mcpv2-registration-api.cjs --file lib/mcp/tool-router.cjs; test $? -ne 0) && (node tests/test-267-mcpv2-registration-api.cjs --file lib/mcp/contract-version.cjs; test $? -ne 0) && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - Both --file runs exit non-zero and print 11 and 1 `v1-variadic` FAIL lines respectively.
    - `node tests/test-267-mcpv2-registration-api.cjs --file lib/mcp/app-views.cjs` prints three `v2-config` lines (registerAppTool already delegates to registerTool) and reports the title check as PASS for them.
    - The test requires no `@modelcontextprotocol/*` package directly (it drives registrars through the recorder and the SDK-free wire helper).
  </acceptance_criteria>
  <done>Registration-API test committed RED.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite tool-router.cjs (11 sites) then contract-version.cjs (1 site), one commit each, CIRS gate after each</name>
  <files>lib/mcp/tool-router.cjs, lib/mcp/contract-version.cjs</files>
  <read_first>
    - lib/mcp/tool-router.cjs, the 11 registrations at lines 852 (room_state), 920 (room_content), 1115 (room_graph), 1363 (methodology), 1408 (analysis), 1462 (intelligence), 1618 (meeting), 1780 (export), 1831 (orchestration, whose description argument is preceded by a multi-line comment that must move with it), 2076 (room_bind), 2210 (eureka_critic); plus lines 830-850 and the module's zod require
    - lib/mcp/contract-version.cjs (1 registration at line 31 with an empty shape `{}`)
    - 267-RESEARCH.md Pattern 4 (registration rewrite) and Pitfall 4 (a rewrite drops a CIRS connector)
  </read_first>
  <action>
1. `git status --short -- lib/mcp/tool-router.cjs lib/mcp/contract-version.cjs` must be clean; otherwise STOP.
2. tool-router.cjs: apply the registration rewrite rule from the context block to all 11 sites. Line numbers above are plan-time anchors; locate each by `server.tool(` plus its name literal. The orchestration site's leading comment block stays attached to the description value inside the config object. Do not reformat unrelated lines. Confirm `z` is already in scope in the module (it is, via its zod require); add nothing else.
3. Gate for this commit, all must pass before committing: `node tests/test-267-mcpv2-registration-api.cjs --file lib/mcp/tool-router.cjs`, `node tests/test-267-mcpv2-cirs-gates.cjs`, `node tests/test-267-mcpv2-zod4-contract.cjs` (descriptions and schemas unchanged), `node tests/test-234-tool-description-floor.cjs`, `node tests/test-248-resolver-census.cjs`, `node tests/test-248-room-bind-session-authoritative.cjs`, `node tests/test-248-room-bind-honest-return.cjs`, `bash tests/run-all-198.sh` (same leg set as 267-BASELINE.md). Commit tool-router.cjs alone.
4. contract-version.cjs: rewrite its single site (`z.object({})`, title `Contract Version`); ensure `z` is required in the file (add `const { z } = require('zod');` if absent; this is an MCP registrar, inside the CTX zod boundary). Gate: `--file lib/mcp/contract-version.cjs`, cirs gates, zod4 contract. Commit alone.
5. Record in the SUMMARY: the commit sha per file and the cirs-gate output line per commit.
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-registration-api.cjs --file lib/mcp/tool-router.cjs && node tests/test-267-mcpv2-registration-api.cjs --file lib/mcp/contract-version.cjs && node tests/test-267-mcpv2-cirs-gates.cjs && node tests/test-267-mcpv2-zod4-contract.cjs && node tests/test-234-tool-description-floor.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "server\.tool(" lib/mcp/tool-router.cjs lib/mcp/contract-version.cjs` prints 0 for both files; `grep -c "server\.registerTool(" lib/mcp/tool-router.cjs` prints 11.
    - `git log --oneline $PLAN_BASE..HEAD -- lib/mcp/tool-router.cjs | wc -l` is 1 and the same for contract-version.cjs (one commit per file).
    - `node tests/test-267-mcpv2-cirs-gates.cjs` exits 0 at each of the two commits (SUMMARY records both outputs).
    - `node tests/test-267-mcpv2-zod4-contract.cjs` exits 0 (no description or schema moved).
    - `git diff $PLAN_BASE -- lib/mcp/tool-router.cjs | grep "^[-+]" | grep -c "MCP_TOOL_CONNECTORS\|hitl_shape"` prints 0.
    - `bash tests/run-all-198.sh` leg outcomes equal 267-BASELINE.md.
  </acceptance_criteria>
  <done>12 of 51 sites migrated, each file in its own green commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> tool input (tool-router, contract_version) | model-authored arguments validated by zod at registration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-20 | Tampering | input validation drift during the rewrite | mitigate | shapes move verbatim inside z.object (same default strip behavior as the raw shape v1 wrapped internally); zod4 contract test proves byte-identical advertised schemas; no strictness change |
| T-267-21 | Elevation of privilege | CIRS born-wired declarations | mitigate | per-commit cirs gate: registry --check, no new shape violation, descriptor count equal to baseline; connectors exports untouched (diff grep) |
| T-267-14 | Elevation of privilege | tool annotations (readOnlyHint/destructiveHint) | accept | deliberately not added: absent annotations leave host defaults at their most cautious (MCP spec: destructiveHint defaults true, readOnlyHint false), so omission cannot loosen host auto-approval; adding them is recorded as a seed with the derivation rule (Phase 356 ledger for router tools, code read for direct tools) |
| T-267-22 | Repudiation | session identity in handlers | mitigate | handler bodies untouched; `sessionId` is the same property name on v1 extra and v2 ctx; room-bind session tests (test-248) run at the tool-router commit |
</threat_model>

<verification>
- Per-file registration-API checks green for both files; full mode expected RED until 267-09 (documented).
- `node tests/test-267-mcpv2-cirs-gates.cjs` green after each commit.
- `bash tests/run-all-198.sh` at baseline.
</verification>

<success_criteria>
The largest registrar is on the v2-compatible registration API with zero wire drift and zero CIRS drift, proven per commit.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `tests/test-267-mcpv2-registration-api.cjs` (`--file <relpath>` scoped mode + full mode) | new test / per-commit CLI check (MCPV2-02, MCPV2-08) | 267-07, 267-08, 267-09, 267-11 |
| `title` on 12 tools (tool-router 11, contract_version 1) | new wire field | hosts' tool pickers |
| `lib/mcp/tool-router.cjs`, `lib/mcp/contract-version.cjs` on `registerTool` | migrated registrars | 267-11 swap |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-06-SUMMARY.md` when done (commit with `git add -f`).
</output>
