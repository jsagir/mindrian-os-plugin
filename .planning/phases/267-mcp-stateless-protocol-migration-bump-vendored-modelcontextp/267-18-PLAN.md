---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 18
type: execute
wave: 16
locked_stage: "W4 close-out: post-migration Tri-Polar verification, requirement registration, seeds, dual-home research trail"
depends_on: [267-17]
files_modified:
  - .planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-TRIPOLAR-PROBES.md
  - .planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-VERIFICATION-GATES.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/CANON-PHASE-MAP.md
  - CHANGELOG.md
  - .planning/seeds/SEED-NNN-mcp-daemon-icm-native-identity-handle.md
  - .planning/seeds/SEED-NNN-mcp-tool-annotations-from-irreversibility-ledger.md
  - .planning/seeds/SEED-NNN-theo-v2-migration-keep-legacy-stateless.md
  - .planning/seeds/SEED-NNN-mcp-server-brain-disposition-and-stale-ajv-comments.md
autonomous: false
requirements: [MCPV2-13, MCPV2-01, MCPV2-02, MCPV2-03, MCPV2-04, MCPV2-05, MCPV2-06, MCPV2-07, MCPV2-08, MCPV2-09, MCPV2-10, MCPV2-11, MCPV2-12, MCPV2-14, MCPV2-15, MCPV2-16, MCPV2-17, MCPV2-18, MCPV2-19]
canon_parts: [6, 7, 8, 9, 11]
tags: [close-out, tri-polar, requirements, seeds, dev-research-compositing]

must_haves:
  truths:
    - "The full phase gate is measured on the final tree: run-all-267 FAIL=0, every suite equal to or better than 267-BASELINE.md, doctor --acceptance points at baseline, and the live CLI probe re-run against the migrated servers"
    - "Tri-Polar coverage is claimed only for surfaces with recorded evidence: CLI by the automated probe, Desktop and Cowork by the navigator's post-migration smoke (or explicitly recorded as deferred, in which case the phase does not claim them)"
    - "MCPV2-01..MCPV2-19 are registered in .planning/REQUIREMENTS.md under a Phase 267 section with each row's evidence (test file and plan), per the Phase 254/257/265/267.2/340 minted-at-plan-time precedent"
    - "Four follow-up seeds exist: the ICM-native per-request identity handle for a 2026-native daemon (icm-architect consult, carrying RCA 7), tool annotations derived from the Phase 356 irreversibility ledger (deferred by the planner), the Theo cross-repo note (keep legacy: 'stateless' if Theo ever adopts v2), and mcp-server-brain/ disposition plus the stale 'ajv via sdk' comment in lib/core/brain-client.cjs"
    - "The phase close-out reasoning trail is filed in both homes per CLAUDE.md Dev-Research Compositing (phase dir plus rethinking-mindrianos research, or the documented plugin-side fallback with mirror_status PENDING if the room write is refused), cross-linked"
  artifacts:
    - path: ".planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-VERIFICATION-GATES.md"
      provides: "final measured gate table versus baseline"
      contains: "run-all-267"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 267 MCPV2 requirement rows"
      contains: "MCPV2-19"
  key_links:
    - from: ".planning/REQUIREMENTS.md Phase 267 section"
      to: "tests/test-267-mcpv2-*.cjs"
      via: "each requirement row names its proving test"
      pattern: "test-267-mcpv2-"
---

<objective>
Close Phase 267 honestly: measure the final gates, verify the three surfaces on the migrated servers (automated for CLI, navigator smoke for Desktop and Cowork), register the minted requirements, record the follow-up seeds, and file the reasoning trail in both homes.

Purpose: CTX Claude's Discretion ("must be verified at some point in this phase before claiming Tri-Polar coverage, not assumed identical to CLI"); CLAUDE.md Tri-Polar rule, Dev-Research Compositing, QA/RCA rule; CTX Deferred items and the icm-architect follow-up seed. No code changes.
Output: gate table, updated probe doc, REQUIREMENTS/ROADMAP/CANON-PHASE-MAP/CHANGELOG entries, four seeds, research trail.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-RESEARCH.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-TRIPOLAR-PROBES.md
@.planning/debug/mcp-shim-preseeded-session-id-rejected.md

Session and scope rules (every task in this plan):
- Workspace /home/jsagi/dev/MindrianOS-Plugin only; record `PLAN_BASE=$(git rev-parse HEAD)` in the SUMMARY.
- Before editing any file run `git status --short -- <that file>`; a diff you did not make means STOP and report (REQUIREMENTS.md, ROADMAP.md and CHANGELOG.md are shared with peer sessions: edit only your own section and commit that path alone).
- Commit by explicit path only (`git add -f` for .planning/ paths, including new seeds), `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git commit -a`, `git stash`.
- No production code changes in this plan. Never edit `mcp-server-brain/**`, `/home/jsagi/Theo/**`, `lib/core/brain-client.cjs`.
- Seeds: SEED-NNN in the frontmatter file list means the next free number at execute time (`ls .planning/seeds | sort -V | tail -3`); number the four seeds consecutively.
- No em-dashes. Feynman-plain prose in every doc (CLAUDE.md).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final gate measurement and the post-migration CLI probe</name>
  <files>.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-VERIFICATION-GATES.md, .planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-TRIPOLAR-PROBES.md</files>
  <read_first>
    - 267-BASELINE.md (every baseline row)
    - 267-TRIPOLAR-PROBES.md (pre-migration CLI/Desktop/Cowork records)
    - tests/run-all-267.sh
  </read_first>
  <action>
1. Run and record every command from 267-BASELINE.md step 5 again, plus `bash tests/run-all-267.sh`, `node tests/test-267-mcpv2-cirs-gates.cjs`, `bash tests/run-all-199.sh`, `node scripts/check-release-payload-ceiling.cjs`, `node scripts/build-connector-registry.cjs --check`, `node scripts/doctor.cjs --acceptance`, and a live wireSnapshot count of both stdio servers. Write 267-VERIFICATION-GATES.md as a table: command, baseline outcome, final outcome, delta (same / fixed / NEW RED). Any NEW RED is a phase failure: STOP and report it rather than closing.
2. Run `MOS_267_LIVE_CLI_PROBE=1 node tests/test-267-mcpv2-cli-probe.cjs` against the migrated servers. Append a "## CLI (post-migration)" section to 267-TRIPOLAR-PROBES.md: claude --version, opening method and protocolVersion for both servers, capabilityKeys, whether server/discover was seen, and whether the connection still reached the gate's rung (a) conditions (elicitation declared). Record an ENV GAP honestly if the probe cannot run.
3. Check that no RCA from this phase is still open except RCA 7: `ls .planning/debug/ | grep -E "mcp-http-flag-off|app-views-schema|gate-elicitation|runtime-loop-prompts|mcp-server-sigterm|mcp-http-listen-error"` prints nothing, and `.planning/debug/mcp-shim-preseeded-session-id-rejected.md` exists with fix PENDING a navigator decision.
4. Commit both docs.
  </action>
  <verify>
    <automated>bash tests/run-all-267.sh && node tests/test-267-mcpv2-cirs-gates.cjs && node scripts/build-connector-registry.cjs --check && grep -q "## CLI (post-migration)" .planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-TRIPOLAR-PROBES.md && test -z "$(ls .planning/debug/ | grep -E 'mcp-http-flag-off|app-views-schema|gate-elicitation|runtime-loop-prompts|mcp-server-sigterm|mcp-http-listen-error')"</automated>
  </verify>
  <acceptance_criteria>
    - 267-VERIFICATION-GATES.md has a row for every baseline command and zero rows with delta "NEW RED".
    - `bash tests/run-all-267.sh` ends FAIL=0 with no SKIP except the opt-in CLI probe leg (and any leg SKIPped with a named ENV GAP).
    - Six of seven RCAs are under .planning/debug/resolved/; RCA 7 remains open with fix PENDING a navigator decision.
  </acceptance_criteria>
  <done>Final gates measured against baseline; CLI verified post-migration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Navigator smoke on Claude Desktop and Cowork against the migrated servers</name>
  <files>.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-TRIPOLAR-PROBES.md</files>
  <read_first>
    - 267-TRIPOLAR-PROBES.md (pre-migration Desktop/Cowork results, including any "deferred")
  </read_first>
  <action>Claude cannot drive the Desktop GUI or a Cowork VM. Present the steps, wait, then record the navigator's report verbatim into new "## Desktop (post-migration)" and "## Cowork (post-migration)" sections and commit the file.</action>
  <what-built>Both local MCP servers (mindrian-os and mindrian-brain) now run on the v2 SDK; stdio serves both protocol eras; the Cowork-routed HTTP branch serves every request (it used to die after one); three runtime-loop prompts (bind-room, status, act) and the three MCP Apps views accept their arguments.</what-built>
  <how-to-verify>
    Point Claude Desktop at this dev workspace's servers (the same way you normally test a dev build: the plugin's .mcp.json entries resolving to /home/jsagi/dev/MindrianOS-Plugin/bin/...), restart Desktop, then:
    1. Say "list my rooms and bind to one". Expected: a room list and a binding confirmation, no tool error.
    2. Open the prompt menu and pick "Status" (the runtime-loop prompt). Expected: it runs (before this phase it failed with -32603).
    3. Ask for something that raises a decision gate (for example "pick my next methodology"). Expected: a gate appears. Tell us which form it took: a native Desktop dialog (elicitation, rung a) or an in-chat card (rung b). Either is acceptable; we need the fact.
    4. Ask Larry to open the room dashboard view. Expected: the dashboard renders for the bound room.
    If you ran the tee probe before (267-04), optionally repeat it and paste the first line: we want the opening protocolVersion after migration.
    Cowork: in a Cowork session with this build, ask Larry to run status_read, then list rooms, then call one tool again. Expected: all three work (before this phase the HTTP branch answered only the first request). Paste any error text.
    If you cannot reach Desktop or Cowork now, reply with "desktop-deferred" or "cowork-deferred": the phase will then claim only the surfaces actually verified.
  </how-to-verify>
  <resume-signal>Type "approved" with the gate form you saw on Desktop (dialog or card), or describe what failed, or "desktop-deferred" / "cowork-deferred".</resume-signal>
  <verify>
    <automated>grep -q "## Desktop (post-migration)" .planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-TRIPOLAR-PROBES.md && grep -q "## Cowork (post-migration)" .planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-TRIPOLAR-PROBES.md</automated>
  </verify>
  <acceptance_criteria>
    - Both post-migration sections exist and hold the navigator's report or an explicit dated deferral.
    - Any failure the navigator reports is filed as a new RCA per docs/RCA-TEMPLATE.md before Task 3 runs, and Task 3 does not mark the affected requirement complete.
  </acceptance_criteria>
  <done>Desktop and Cowork post-migration facts recorded (or explicitly deferred).</done>
</task>

<task type="auto">
  <name>Task 3: Register requirements, update ROADMAP, canon map and CHANGELOG, write four seeds, file the dual-home research trail</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, docs/CANON-PHASE-MAP.md, CHANGELOG.md, .planning/seeds/SEED-NNN-mcp-daemon-icm-native-identity-handle.md, .planning/seeds/SEED-NNN-mcp-tool-annotations-from-irreversibility-ledger.md, .planning/seeds/SEED-NNN-theo-v2-migration-keep-legacy-stateless.md, .planning/seeds/SEED-NNN-mcp-server-brain-disposition-and-stale-ajv-comments.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md "### Phase 267.2 - First-Install Hooked Loop Repair" section (the minted-at-plan-time registration format to copy) and the list of phase sections (append a new `### Phase 267 - MCP SDK v2 migration (MCPV2 family) (minted at plan time 2026-09-23)` section in the same style)
    - .planning/ROADMAP.md "### Phase 267" section
    - docs/CANON-PHASE-MAP.md row format
    - CHANGELOG.md `## [Unreleased]` section
    - two recent seeds in .planning/seeds/ (frontmatter and body format)
    - 267-RESEARCH.md "Grounding Consults" (icm-architect and Jev sections), CONTEXT "Cross-repo boundaries" and "Deferred Ideas"
    - `git -C /home/jsagi show --stat 55e077bef` (the precedent for filing a Phase 267 research trail with mirror_status PENDING when the room write is refused)
  </read_first>
  <action>
1. REQUIREMENTS.md: add the Phase 267 section with MCPV2-01..MCPV2-19, one row each, `[x]` only when 267-VERIFICATION-GATES.md and (for MCPV2-13) the Task 2 record prove it, otherwise `[ ]` with the reason. Row wording: MCPV2-01 era adoption by package not version; -02 local server dual-era tools/list with titles; -03 zod 4 contract with the accepted looser strictness pinned; -04 MCP Apps real schemas with contained room_path; -05 flag-OFF HTTP multi-request both eras; -06 flag-ON era routing with session binding intact and in-repo clients on 2025 default; -07 elicitation premise current and ladder pinned; -08 CIRS gates per commit; -09 dep-heal probes; -10 allowlist entries; -11 brain shim canary; -12 payload ceiling and shrinkwrap; -13 Tri-Polar probes; -14 lifecycle exit and honest listen failure; -15 RCAs filed and resolved; -16 runtime-loop prompts; -17 loopback Host/Origin guards; -18 v1 removed; -19 manifest and lockfile lockstep. Each row cites its test file and plan id. Note that the IDs extend the research's own MCPV2-01..14 table (267-RESEARCH.md Validation Architecture).
2. ROADMAP.md "### Phase 267": leave the historical text; append a dated line recording completion, the plan count, the RCA 7 open item, and pointers to 267-VERIFICATION-GATES.md and 267-TRIPOLAR-PROBES.md; tick the plan checkboxes.
3. docs/CANON-PHASE-MAP.md: add one row for Phase 267 (status shipped, Parts 7, 8, 9, 11, one-line note: local MCP server family on SDK v2; Part 8 brain shim boundary re-verified).
4. CHANGELOG.md `## [Unreleased]`: Changed (MCP SDK v2, zod 4, serveStdio, HTTP createMcpHandler, ext-apps 2.x) and Fixed (six RCAs by slug); no version bump (release.sh owns versions).
5. Seeds (next free numbers): (a) ICM-native identity handle for a 2026-native daemon: an explicit per-request room/session handle (reverse-DNS `_meta` key such as `io.mindrian/sessionId`, or the existing room/sessionId tool args) resolved against the session file, never transport state; carries RCA 7 and its two candidate designs; first step verifies assumption A9 (custom `_meta` keys survive v2's envelope lift). (b) Tool annotations: derive destructiveHint for the 11 router tools as the OR of `data/command-irreversibility-ledger.json` flags over each tool's command enum, code-read the 31 direct tools, assert readOnlyHint only from a code reading; explain why the planner deferred it (absent annotations keep host defaults at their most cautious, so deferral cannot loosen anything, while a wrong readOnlyHint could). (c) Theo note: if Theo ever moves to v2 createMcpHandler it must keep `legacy: 'stateless'`, never `'reject'`, because every installed plugin's lib/core/brain-client.cjs is a hand-rolled 2025-era client (initialize at 2024-11-05, bare tools/call); reference Theo's own D-04 decision; no action in this repo. (d) mcp-server-brain/ disposition (dead service per docs/257-NOTE-part8-enforcement-locus-rulings.md:22; delete or archive) plus the stale "ajv arrives via @modelcontextprotocol/sdk" comment in lib/core/brain-client.cjs:2358, which this phase was not allowed to edit.
6. Dev-Research Compositing: write the close-out trail (what shipped, the seven NEW FAILURES and how each was found, the plan-time probes that made the plan deterministic, the Tri-Polar record, the open RCA 7 decision) to `~/MindrianRooms/rethinking-mindrianos/research/<YYYY-MM-DD>-phase-267-mcp-v2-closeout/`. If the write-scope-check hook refuses it (active room elsewhere), do not route around it: file the identical content at `/home/jsagi/MindrianOS/research/<YYYY-MM-DD>-mcp-sdk-v2-migration-closeout-267.md` with `mirror_status: PENDING` and commit it in the /home/jsagi repository by explicit path with a `rethinking-mindrianos:` message, following commit 55e077bef. Cross-link it from 267-VERIFICATION-GATES.md (amend that file in this task's last commit).
7. Commit each shared file (REQUIREMENTS.md, ROADMAP.md, CANON-PHASE-MAP.md, CHANGELOG.md) as its own path-scoped commit; seeds together with `git add -f`.
  </action>
  <verify>
    <automated>grep -c "MCPV2-" .planning/REQUIREMENTS.md | awk '{exit ($1>=19)?0:1}' && grep -q "Phase 267" docs/CANON-PHASE-MAP.md && ls .planning/seeds | grep -c -E "icm-native-identity-handle|tool-annotations-from-irreversibility|theo-v2-migration-keep-legacy|mcp-server-brain-disposition" | awk '{exit ($1==4)?0:1}' && ! grep -q $'\xe2\x80\x94' .planning/seeds/*identity-handle* .planning/seeds/*tool-annotations* .planning/seeds/*keep-legacy* .planning/seeds/*mcp-server-brain-disposition*</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains MCPV2-01 through MCPV2-19 exactly once each as row IDs, each citing a test-267-mcpv2-*.cjs file or plan id.
    - MCPV2-13 is `[x]` only if both post-migration Desktop and Cowork sections record a verified result; otherwise `[ ]` naming the deferred surface.
    - Four seed files exist with consecutive numbers; seed (a) cites .planning/debug/mcp-shim-preseeded-session-id-rejected.md.
    - The research trail exists in the room or at the fallback path with `mirror_status: PENDING`, and 267-VERIFICATION-GATES.md links it.
    - `git diff $PLAN_BASE -- bin lib scripts package.json | wc -l` prints 0 (no code change in this plan).
  </acceptance_criteria>
  <done>Phase 267 closed with measured gates, recorded Tri-Polar evidence, registered requirements, seeds and the dual-home trail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase records -> future sessions | REQUIREMENTS/ROADMAP/seeds are what the next session trusts |
| research trail -> rethinking-mindrianos room | a Data Room write governed by the write-scope hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-36 | Repudiation | false completion claims (the repo's false-success watch item) | mitigate | every `[x]` requires a measured row in 267-VERIFICATION-GATES.md or a recorded human result; deferred surfaces stay `[ ]` with the reason |
| T-267-37 | Elevation of privilege | bypassing the write-scope hook to file the room mirror | mitigate | on refusal, file at the documented fallback with mirror_status PENDING (commit 55e077bef precedent); never route around the hook |
| T-267-12 | Information disclosure | research trail and probe records | mitigate | protocol metadata and test outcomes only; no room content, no keys |
</threat_model>

<verification>
- 267-VERIFICATION-GATES.md: zero NEW RED rows.
- Tri-Polar post-migration sections recorded.
- REQUIREMENTS, ROADMAP, CANON-PHASE-MAP, CHANGELOG, four seeds and the research trail committed.
</verification>

<success_criteria>
A future session can see exactly what Phase 267 shipped, what it proved on which surface, and what it deliberately left for a decision, without re-deriving any of it.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `267-VERIFICATION-GATES.md` | phase doc (final gate table) | verifier, navigator |
| post-migration sections in `267-TRIPOLAR-PROBES.md` | phase doc | Tri-Polar claim |
| Phase 267 MCPV2-01..19 section in `.planning/REQUIREMENTS.md` | requirement registration | future planning |
| four `.planning/seeds/SEED-NNN-*.md` files | follow-up seeds | backlog |
| Phase 267 row in `docs/CANON-PHASE-MAP.md`; `CHANGELOG.md` Unreleased entries | docs | release ceremony |
| close-out research trail (room or fallback, mirror_status recorded) | dev-research record | rethinking-mindrianos |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-18-SUMMARY.md` when done (commit with `git add -f`).
</output>
