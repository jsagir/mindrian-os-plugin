---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 15
type: execute
wave: 13
locked_stage: "W3 (HTTP branches): the two in-repo MCP clients (stdio<->daemon shim, hook adapter-client) to @modelcontextprotocol/client, default 2025 mode"
depends_on: [267-14]
files_modified:
  - tests/test-267-mcpv2-clients.cjs
  - bin/mindrian-mcp-shim.cjs
  - lib/mcp/adapter-client.cjs
  - tests/test-267-mcpv2-sdk-era.cjs
autonomous: true
requirements: [MCPV2-06, MCPV2-01]
canon_parts: [9, 11]
tags: [clients, shim, adapter-client, session-binding]

must_haves:
  truths:
    - "bin/mindrian-mcp-shim.cjs bridges stdio to the daemon with StdioServerTransport from @modelcontextprotocol/server/stdio and StreamableHTTPClientTransport from @modelcontextprotocol/client, keeping the hook-derived MINDRIAN_SESSION_ID pass-through line unchanged (its known defect, RCA 7, is pinned, not fixed, in this phase)"
    - "lib/mcp/adapter-client.cjs uses Client and StreamableHTTPClientTransport from @modelcontextprotocol/client and never sets versionNegotiation, so its initialize is a byte-for-byte 2025-era handshake served by the daemon's legacy sessionful path (RESEARCH Pattern 5)"
    - "Through the shim (no MINDRIAN_SESSION_ID, the path that works today) a 2025 initialize is answered at 2025-11-25, tools/list matches the snapshot count, and a room_bind made through the shim creates exactly one new session binding file naming the bound room"
    - "RCA 7 is pinned, not fixed: with MINDRIAN_SESSION_ID set, the daemon still answers the shim's pre-seeded session id with 400 'No valid session ID provided' after the migration exactly as before, so the migration cannot silently change a behavior that awaits a navigator decision on the identity model"
    - "queryDaemon() completes a real tool call against the daemon and terminates its session"
    - "A client test fails against the pre-migration source (it requires the v2 packages) and passes after"
  artifacts:
    - path: "tests/test-267-mcpv2-clients.cjs"
      provides: "MCPV2-06 client test"
      min_lines: 70
    - path: "bin/mindrian-mcp-shim.cjs"
      provides: "v2 stdio<->HTTP bridge"
      contains: "@modelcontextprotocol/client"
    - path: "lib/mcp/adapter-client.cjs"
      provides: "v2 hook adapter client"
      contains: "@modelcontextprotocol/client"
  key_links:
    - from: "bin/mindrian-mcp-shim.cjs"
      to: "daemon legacy session path"
      via: "clientOpts.sessionId = MINDRIAN_SESSION_ID"
      pattern: "clientOpts\\.sessionId = hookSessionId"
---

<objective>
Move the two in-repo MCP clients to the v2 client package, keeping them on the default 2025-era handshake, and prove the shim's one-namespace session binding and adapter-client's daemon query still work end to end.

Purpose: CTX Phase Boundary names `bin/mindrian-mcp-shim.cjs` and `lib/mcp/adapter-client.cjs` in scope. RESEARCH Pattern 5: do not set `versionNegotiation: 'auto'`; the default is a byte-identical 2025 initialize, which is what the daemon's sessionful legacy path (267-14) expects, and auto mode would add a probe round trip.
Output: RED client test, two migrated clients.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md
@bin/mindrian-mcp-shim.cjs
@lib/mcp/adapter-client.cjs
@tests/test-267-mcpv2-flag-on.cjs

Session and scope rules (every task in this plan):
- Workspace /home/jsagi/dev/MindrianOS-Plugin only; record `PLAN_BASE=$(git rev-parse HEAD)` in the SUMMARY.
- Before editing any file run `git status --short -- <that file>`; a diff you did not make means STOP and report.
- Commit by explicit path only, `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git commit -a`, `git stash`.
- Never edit `bin/mindrian-mcp-server.cjs`, `lib/mcp/daemon-lifecycle.cjs`, `lib/core/session-binding.cjs`, `lib/core/brain-client.cjs` (a hand-rolled client to Theo that is explicitly NOT migrated), `mcp-server-brain/**`, `/home/jsagi/Theo/**`.
- Daemon tests: hermetic rooms home; SIGKILL only spawned PIDs; leave no daemon running.
- No em-dashes. CJS only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Client test (RED)</name>
  <files>tests/test-267-mcpv2-clients.cjs</files>
  <read_first>
    - bin/mindrian-mcp-shim.cjs (the zero-logic bridge; hookSessionId -> clientOpts.sessionId; daemonLifecycle.ensureDaemon)
    - lib/mcp/adapter-client.cjs (queryDaemon: connect, callTool, terminateSession, close)
    - tests/test-267-mcpv2-flag-on.cjs (daemon spawn, fixture rooms, pidfile port discovery)
    - lib/core/session-binding.cjs lines 130-150 (binding file location <rooms home>/.rooms/sessions/<sessionId>.json and id precedence)
  </read_first>
  <behavior>
    - Source arm: both files require `@modelcontextprotocol/client` (the shim also `@modelcontextprotocol/server/stdio`) and neither requires `@modelcontextprotocol/sdk` in a non-comment line; adapter-client never mentions `versionNegotiation`. RED now.
    - Shim arm (unbound, MINDRIAN_SESSION_ID unset, MINDRIAN_MCP_FIRST=cli): spawn the shim (it starts the hermetic daemon via ensureDaemon), drive a raw 2025-11-25 initialize over its stdio (tests/helpers/mcp-wire-267.cjs rpcOverStdio), get protocolVersion 2025-11-25 back, tools/list count equals the snapshot count, call room_bind {room:'room-x'} and assert exactly one new file appeared under <rooms home>/.rooms/sessions/ and it names room-x.
    - RCA 7 pin arm: same setup with MINDRIAN_SESSION_ID=shim-267-pin: the initialize gets no stdout response within 10000 ms and the shim's stderr contains `No valid session ID provided` (today's behavior per .planning/debug/mcp-shim-preseeded-session-id-rejected.md). The arm's output line names the RCA so a future fix flips it deliberately.
    - Adapter arm: queryDaemon('contract_version', {}) with daemonOpts pointing at the hermetic home returns a result whose content parses and names the contract version; a second queryDaemon call in the same test also succeeds (each call opens and terminates its own session, which the daemon's legacy path must keep serving).
  </behavior>
  <action>Write tests/test-267-mcpv2-clients.cjs implementing the four arms (the shim, pin and adapter arms are expected to pass on the v1 clients already: that is the wire parity baseline the migration must preserve; record their outcomes in the SUMMARY). Kill the daemon PID from the hermetic pidfile and every spawned shim in finally. Run: the source arm FAILS. Commit (RED).</action>
  <verify>
    <automated>node tests/test-267-mcpv2-clients.cjs; rc=$?; test $rc -ne 0 -a $rc -ne 77 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - Exits 1 on the current tree with only the source arm failing (SUMMARY records the shim and adapter arm outcomes on v1).
    - No daemon or shim started by the test survives.
  </acceptance_criteria>
  <done>RED client test committed.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the shim and adapter-client to the v2 client package</name>
  <files>bin/mindrian-mcp-shim.cjs, lib/mcp/adapter-client.cjs, tests/test-267-mcpv2-sdk-era.cjs</files>
  <read_first>
    - node_modules/@modelcontextprotocol/client/dist/index.d.cts: StreamableHTTPClientTransport options (sessionId), Client constructor, terminateSession
    - node_modules/@modelcontextprotocol/server/dist/stdio.d.cts: StdioServerTransport (the shim uses the raw transport, not serveStdio, because it bridges messages verbatim and runs no server)
  </read_first>
  <action>
1. bin/mindrian-mcp-shim.cjs: `const { StdioServerTransport } = require('@modelcontextprotocol/server/stdio');` and `const { StreamableHTTPClientTransport } = require('@modelcontextprotocol/client');`. Keep every other line: the clientOpts.sessionId pass-through, the verbatim onmessage bridging both ways, error/close wiring, start order, logs. Update the header comment's SDK mention (hyphens only). No versionNegotiation.
2. lib/mcp/adapter-client.cjs: `const { Client, StreamableHTTPClientTransport } = require('@modelcontextprotocol/client');`. Keep the Client constructor arguments (`{ name, version }, { capabilities: {} }`) and the connect/callTool/terminateSession/close flow unchanged. No versionNegotiation option anywhere.
3. Append both paths to EXPECT_V2 in tests/test-267-mcpv2-sdk-era.cjs.
4. Verify, compared with 267-BASELINE.md: `node tests/test-267-mcpv2-clients.cjs` (GREEN), `node tests/test-267-mcpv2-sdk-era.cjs`, `node tests/test-198-concurrency-mcp.test.cjs`, `node tests/test-198-adapter-budget.test.cjs` and `node tests/test-198-local-only.test.cjs` (pre-existing reds in the research baseline: must fail with the SAME assertion text as 267-BASELINE.md, not a new one), `bash tests/parity-198.sh`, `bash tests/run-all-198.sh`, `node tests/test-248-surface-probes.cjs`, `node tests/test-267-mcpv2-flag-on.cjs`. Commit the three files together.
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-clients.cjs && node tests/test-267-mcpv2-sdk-era.cjs && node tests/test-198-concurrency-mcp.test.cjs && node tests/test-267-mcpv2-flag-on.cjs && bash tests/run-all-267.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v "^\s*//\|^\s*\*" bin/mindrian-mcp-shim.cjs lib/mcp/adapter-client.cjs | grep -c "@modelcontextprotocol/sdk"` prints 0.
    - `grep -c "versionNegotiation" lib/mcp/adapter-client.cjs bin/mindrian-mcp-shim.cjs` prints 0 for both.
    - `grep -c "clientOpts.sessionId = hookSessionId" bin/mindrian-mcp-shim.cjs` prints 1 (line kept; RCA 7 is pinned by the pin arm, not fixed here).
    - test-198-adapter-budget and test-198-local-only outcomes and failure texts equal 267-BASELINE.md; run-all-198 leg set equal.
    - `node tests/test-267-mcpv2-clients.cjs` exits 0.
  </acceptance_criteria>
  <done>Both in-repo clients on the v2 client package, 2025 handshake, session binding intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook process env (MINDRIAN_SESSION_ID) -> daemon session id | the shim forwards a caller-chosen session id as the binding key |
| adapter-client -> daemon | hook-side tool calls over loopback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-07 | Spoofing | caller-supplied session id through the shim | accept | unchanged behavior: today the daemon REJECTS a caller-supplied id (RCA 7), so no caller can choose another session's binding key; the pin arm keeps it that way until the navigator rules on the identity model; loopback-only with Host/Origin guards (267-14); session-binding.cjs untouched |
| T-267-15 | Tampering | client era auto-negotiation | mitigate | no versionNegotiation in either client (grep gate), so both stay on the 2025 legacy path the daemon keeps sessionful |
| T-267-09 | Information disclosure | Canon Part 8 | accept | neither client talks to the Brain; lib/core/brain-client.cjs (the Brain client) is explicitly untouched |
</threat_model>

<verification>
- Client test green; concurrency, parity, flag-ON and surface-probe tests green; pre-existing reds unchanged.
</verification>

<success_criteria>
Every MCP client this repo ships uses the v2 package and still binds sessions exactly as before.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `tests/test-267-mcpv2-clients.cjs` | new test (MCPV2-06) | aggregator |
| `bin/mindrian-mcp-shim.cjs`, `lib/mcp/adapter-client.cjs` on @modelcontextprotocol/client | migrated clients | CLI hooks, flag-ON daemon users |
| EXPECT_V2 entries for both files | test ledger change | Arm C |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-15-SUMMARY.md` when done (commit with `git add -f`).
</output>
