---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 01
type: execute
wave: 1
locked_stage: "W0 (dependency bump stage: baseline and scaffolding, before any dependency or registration change)"
depends_on: []
files_modified:
  - tests/helpers/mcp-wire-267.cjs
  - tests/run-all-267.sh
  - tests/test-267-mcpv2-sdk-era.cjs
  - tests/test-267-mcpv2-lockstep.cjs
  - tests/test-267-mcpv2-cirs-gates.cjs
  - tests/fixtures/267/wire-snapshot-zod3.json
  - tests/fixtures/267/shape-violations-baseline.txt
  - tests/fixtures/267/zod-importers-baseline.txt
  - .planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md
autonomous: true
requirements: [MCPV2-01, MCPV2-08, MCPV2-19]
canon_parts: [7, 8, 11]
tags: [mcp, sdk-v2, baseline, test-scaffolding, wave-0]

must_haves:
  truths:
    - "Every measurement a later plan compares against (suite pass/fail per leg, shape-violation set, connector-descriptor count, wire snapshot of both stdio servers, zod importer set) is captured on the UNCHANGED tree before any dependency or registration edit, and written to 267-BASELINE.md plus tests/fixtures/267/"
    - "One SDK-independent wire helper (raw newline-delimited JSON-RPC over stdio) exists, so the same snapshot/compare code works before the migration, after it, and after the v1 SDK is gone"
    - "tests/run-all-267.sh pre-declares every Phase 267 leg behind run_if guards, so later plans only ADD test files and never edit the runner's leg list"
    - "The per-commit CIRS gate is one command (node tests/test-267-mcpv2-cirs-gates.cjs) that fails on connector-registry drift, on ANY new check-shape-declaration violation versus the recorded baseline, and on a changed MCP connector-descriptor count"
    - "A bumped v1 SDK version is never counted as 2026-07-28 adoption: the era test decides adoption by which package each entry point requires, not by a version number (Pitfall 1)"
  artifacts:
    - path: "tests/helpers/mcp-wire-267.cjs"
      provides: "hermeticEnv, wireSnapshot, rpcOverStdio, normalizeSchema, compareToSnapshot, killTree"
      exports: ["hermeticEnv", "wireSnapshot", "rpcOverStdio", "normalizeSchema", "compareToSnapshot", "killTree", "LOCAL_SERVER", "BRAIN_SHIM"]
    - path: "tests/run-all-267.sh"
      provides: "Phase 267 aggregator, every leg pre-declared"
      contains: "run_if \"267:"
    - path: "tests/test-267-mcpv2-sdk-era.cjs"
      provides: "MCPV2-01 era test: Arm A falsification, Arm B v2 era support, Arm C EXPECT_V2 migration ledger, Arm D v1-removed sweep"
      contains: "EXPECT_V2"
    - path: "tests/test-267-mcpv2-lockstep.cjs"
      provides: "MCPV2-19 manifest/lockfile/shrinkwrap lockstep and single-instance checks"
      min_lines: 60
    - path: "tests/test-267-mcpv2-cirs-gates.cjs"
      provides: "MCPV2-08 per-commit CIRS gate"
      min_lines: 50
    - path: ".planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md"
      provides: "pre-change measurements every later plan diffs against"
      contains: "PLAN_BASE"
  key_links:
    - from: "tests/test-267-mcpv2-cirs-gates.cjs"
      to: "scripts/build-connector-registry.cjs"
      via: "spawn --check plus require listMcpToolConnectorDescriptors"
      pattern: "listMcpToolConnectorDescriptors"
    - from: "tests/test-267-mcpv2-cirs-gates.cjs"
      to: "tests/fixtures/267/shape-violations-baseline.txt"
      via: "set comparison against check-shape-declaration.cjs --check --strict output"
      pattern: "shape-violations-baseline"
    - from: "tests/helpers/mcp-wire-267.cjs"
      to: "bin/mindrian-mcp-server.cjs"
      via: "child_process.spawn with a hermetic env"
      pattern: "mindrian-mcp-server\\.cjs"
---

<objective>
Lay Phase 267's measurement floor and test scaffolding BEFORE anything changes: one SDK-independent wire helper, the phase aggregator (written once), the era test, the lockfile-lockstep test, the per-commit CIRS gate, and a recorded baseline of every suite and wire surface a later plan will compare against.

Purpose: locked stage W0 (CTX "Scope and sequencing", navigator-approved 2026-09-23) says the zod 4 + SDK 1.30.1 bump ships green on v1 FIRST. "Green" is only provable against a baseline taken on the unchanged tree, including the pre-existing reds the research measured (run-all-198: 21 PASS / 3 FAIL; test-257 Arm F stale and Arm B flaky), so later plans can say "unchanged" rather than guess. This plan changes no production file.
Output: 1 helper, 1 aggregator, 3 tests, 3 fixtures, 267-BASELINE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-RESEARCH.md
@lib/core/doctor/mcp-surface-module.cjs
@scripts/build-connector-registry.cjs
@tests/run-all-357.sh

Session and scope rules (every task in this plan):
- Workspace is /home/jsagi/dev/MindrianOS-Plugin only (confirm with `pwd`). Read the version with `node lib/core/repo-version.cjs`, never by searching the tree.
- Record `PLAN_BASE=$(git rev-parse HEAD)` at plan start; write it into 267-BASELINE.md and the SUMMARY.
- Before editing any file run `git status --short -- <that file>`. A diff you did not make means a peer session owns it: STOP and report, never edit over it.
- Commit only by explicit path: `git add <path>` (`git add -f <path>` for anything under `.planning/`), then `git commit --only -m "<msg>" -- <path> [<path> ...]`. Never `git add -A`, never `git commit -a`, never `git stash`, never revert or checkout a diff you did not make.
- Never edit: `mcp-server-brain/**`, anything under `/home/jsagi/Theo`, `lib/core/brain-client.cjs`, `lib/core/part8-egress-guard.cjs`, `lib/core/session-binding.cjs`, `lib/mcp/gate-render.cjs`.
- Never run `@modelcontextprotocol/codemod` (measured harmful on this repo, RESEARCH Pitfall 7).
- Test servers: never kill a process this task did not spawn. Kill every spawned PID with SIGKILL in a `finally`, and assert port 3847 is free before and after any HTTP-mode case (RESEARCH Pitfall 9).
- No em-dashes anywhere (hyphens only). CJS only, Node built-ins only in tests and helpers.
- Test-file naming: every new Phase 267 test is `tests/test-267-mcpv2-*.cjs`. Never use the `test-267-1-`, `test-267-2-` or `test-267.3-` prefixes (those are globbed by the 267.1, 267.2 and 267.3 runners).
</context>

<tasks>

<task type="auto">
  <name>Task 1: SDK-independent wire helper, the aggregator (written once), the era test and the lockstep test</name>
  <files>tests/helpers/mcp-wire-267.cjs, tests/run-all-267.sh, tests/test-267-mcpv2-sdk-era.cjs, tests/test-267-mcpv2-lockstep.cjs</files>
  <read_first>
    - lib/core/doctor/mcp-surface-module.cjs lines 99-230 (buildInitializeTransaction, listToolsOverStdio: the in-repo hand-rolled stdio JSON-RPC precedent to mirror, Canon Part 7)
    - tests/test-257-shim-honest-refusal.cjs lines 60-110 (the hermetic spawn env recipe for the brain shim: isolated HOME, no MINDRIAN_BRAIN_KEY, tracked PIDs force-killed in finally)
    - tests/run-all-357.sh (run/run_if aggregator shape, exit 77 = SKIPPED ENV GAP)
    - 267-RESEARCH.md sections "Common Pitfalls" (1, 2, 8, 9) and "Validation Architecture" (the MCPV2-01..14 table this phase's IDs extend)
    - package.json, npm-shrinkwrap.json, package-lock.json (current pins: sdk 1.29.0, zod 3.25.76, ext-apps 1.5.0; the two lockfiles differ ONLY in their root version fields today)
  </read_first>
  <action>
Create `tests/helpers/mcp-wire-267.cjs` (CJS, Node built-ins only, NO `@modelcontextprotocol/*` require so it survives the v1 removal in 267-17). Exports:
- `LOCAL_SERVER` (abs path of bin/mindrian-mcp-server.cjs) and `BRAIN_SHIM` (abs path of bin/mindrian-brain-mcp-client.cjs).
- `hermeticEnv(overrides)`: copies process.env, deletes MINDRIAN_BRAIN_KEY, MINDRIAN_MCP_FIRST, MINDRIAN_MCP_DAEMON, CLAUDE_CODE_SESSION_ID; sets HOME, MINDRIAN_ROOMS_HOME and MINDRIAN_ROOM to fresh mkdtemp dirs (rooms home contains one fixture room dir named `room-267` with a minimal STATE.md); sets MINDRIAN_TRANSPORT=stdio and MINDRIAN_BRAIN_URL=http://127.0.0.1:9 (unreachable loopback, so no test can reach a real Brain); merges overrides last; returns `{ env, dirs, cleanup }`.
- `rpcOverStdio(entryPath, requests, opts)`: async; spawns `node entryPath` with opts.env, writes an `initialize` (protocolVersion opts.protocolVersion default '2025-11-25', capabilities opts.capabilities default {}, clientInfo {name:'mcp-wire-267',version:'1'}) then `notifications/initialized` then each request with ids 2..n, keeps stdin OPEN until every id has a response or opts.timeoutMs (default 20000) elapses, then ends stdin, SIGKILLs the child in a finally, and resolves `{ initialize, responses: Map(id -> response), stderr }`. Never throws on a JSON-RPC error; errors come back as the response object.
- `wireSnapshot(entryPath, opts)`: calls rpcOverStdio with tools/list, prompts/list, resources/list, resources/templates/list and returns `{ protocolVersion, instructions, tools:[{name,title,description,inputSchema}], prompts:[{name,title,description,arguments}], resources:[{uri,name}], templates:[{uriTemplate,name}] }` sorted by name/uri.
- `normalizeSchema(schema)`: deep clone with the top-level `$schema` key removed and object keys sorted recursively (key-order-insensitive comparison; draft-07 under v1 versus draft 2020-12 under v2 is not a contract change, RESEARCH Pitfall 2).
- `compareToSnapshot(live, snapshot, acceptedDeltas)`: returns an array of diff records `{kind, name, field, expected, actual}` covering: tool set membership, description byte-equality, normalized inputSchema equality, prompt description and arguments equality; a diff listed in acceptedDeltas (matched by kind + name + field) is excluded. Titles are NOT compared here (later plans add them on purpose).
- `killTree(pid)`: SIGKILL, swallow ESRCH.

Create `tests/run-all-267.sh` modeled on tests/run-all-357.sh (run, run_if, PASS/FAIL/SKIP counters, exit 77 = SKIPPED ENV GAP, final summary line `PASS=N FAIL=N SKIP=N`, non-zero exit on any FAIL). Pre-declare EVERY Phase 267 leg now, each guarded on its own test file so it SKIPs until the owning plan lands it:
- "267: sdk era (MCPV2-01)" tests/test-267-mcpv2-sdk-era.cjs
- "267: lockstep (MCPV2-19)" tests/test-267-mcpv2-lockstep.cjs
- "267: CIRS gates (MCPV2-08)" tests/test-267-mcpv2-cirs-gates.cjs
- "267: zod4 contract (MCPV2-03)" tests/test-267-mcpv2-zod4-contract.cjs
- "267: tee helper (MCPV2-13)" tests/test-267-mcpv2-tee.cjs
- "267: CLI live wire probe, opt-in (MCPV2-13)" tests/test-267-mcpv2-cli-probe.cjs
- "267: brain shim canary (MCPV2-11)" tests/test-267-mcpv2-brain-shim.cjs
- "267: registration API and titles (MCPV2-02, MCPV2-08)" tests/test-267-mcpv2-registration-api.cjs
- "267: gate elicitation premise (MCPV2-07)" tests/test-267-mcpv2-gate-premise.cjs
- "267: runtime-loop prompts (MCPV2-16)" tests/test-267-mcpv2-prompts.cjs
- "267: MCP Apps schemas and containment (MCPV2-04)" tests/test-267-mcpv2-app-views.cjs
- "267: local server dual era (MCPV2-02)" tests/test-267-mcpv2-dual-era.cjs
- "267: HTTP flag-OFF multi-request and rebinding (MCPV2-05, MCPV2-17)" tests/test-267-mcpv2-http-flag-off.cjs
- "267: process lifecycle (MCPV2-14)" tests/test-267-mcpv2-lifecycle.cjs
- "267: flag-ON routing (MCPV2-06, MCPV2-17)" tests/test-267-mcpv2-flag-on.cjs
- "267: in-repo clients (MCPV2-06)" tests/test-267-mcpv2-clients.cjs
Plus an em-dash guard leg over every `tests/test-267-mcpv2-*.cjs`, `tests/helpers/mcp-wire-267.cjs`, `tests/helpers/mcp-stdio-tee.cjs` that exist (grep for the U+2014 character (bash: $'\xe2\x80\x94'); FAIL on any hit). Leave a clearly marked `# REGRESSION LEGS (filled by 267-01 Task 2)` section. The HTTP legs must run sequentially (they share port 3847).

Create `tests/test-267-mcpv2-sdk-era.cjs` (plain Node, PASS/FAIL lines, exit 1 on any FAIL, exit 77 only if every arm SKIPs):
- Arm A (Pitfall 1 falsification): if `node_modules/@modelcontextprotocol/sdk/dist/cjs/types.js` exists, assert its LATEST_PROTOCOL_VERSION literal is not '2026-07-28' and count files under `node_modules/@modelcontextprotocol/sdk/dist/cjs` containing each of the ten markers (server/discover, inputResponses, inputRequests, requestState, Mcp-Method, Mcp-Name, ttlMs, cacheScope, resultType, 2026-07-28); PASS when all ten counts are 0 and print "v1 installed: does NOT implement 2026-07-28 (a version bump is not adoption)". Beware the trap RESEARCH names: `input_required` is a v1 Tasks enum value and is NOT in the marker list. SKIP when v1 is absent.
- Arm B: if `@modelcontextprotocol/server` resolves, assert its exported SUPPORTED_PROTOCOL_VERSIONS includes both '2026-07-28' and '2025-11-25'. SKIP otherwise.
- Arm C (migration ledger): a top-of-file array `EXPECT_V2` (starts EMPTY in this plan; each migrating plan appends its entry path). For each listed file, read its source and assert it references `@modelcontextprotocol/server` or `@modelcontextprotocol/client` or `@modelcontextprotocol/node` inside a `require(` or `requireWithHeal(` call and references `@modelcontextprotocol/sdk` in NO `require(`/`requireWithHeal(` call (comment lines ignored: strip lines whose trimmed start is `//` or `*`).
- Arm D (v1 removed): only when package.json dependencies lack `@modelcontextprotocol/sdk`: walk bin/, lib/, scripts/, tests/ (skip node_modules, mcp-server-brain/) and assert zero non-comment lines containing `require('@modelcontextprotocol/sdk` or `requireWithHeal('@modelcontextprotocol/sdk` (both quote styles). SKIP otherwise.

Create `tests/test-267-mcpv2-lockstep.cjs`:
- package.json `dependencies` deep-equals npm-shrinkwrap.json `packages[""].dependencies` and package-lock.json `packages[""].dependencies`.
- The two lockfiles' `packages` maps are deep-equal after deleting the root entry's `version` field and the top-level `version` field (today they differ only there).
- npm-shrinkwrap.json has zero entries with `dev: true`.
- If any `node_modules/**/@modelcontextprotocol/core/package.json` exists, exactly one distinct version is installed (walk node_modules; a second core instance would reintroduce the class-identity crossing the research ruled out).
- Exactly one distinct installed `zod` version across node_modules.
- Every `@modelcontextprotocol/*` name in package.json dependencies has a `disposition: "VETTED"` entry in `references/security/cve-db.json` `surfaces.supply_chain.allowlist` (name match).
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-sdk-era.cjs && node tests/test-267-mcpv2-lockstep.cjs && bash -n tests/run-all-267.sh && node -e "const h=require('./tests/helpers/mcp-wire-267.cjs');h.wireSnapshot(h.LOCAL_SERVER,{env:h.hermeticEnv().env}).then(s=>{if(!s.tools.length)process.exit(1);console.log('tools',s.tools.length)})"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-267-mcpv2-sdk-era.cjs` exits 0 with Arm A PASS (v1 1.29.0 installed, ten marker counts all 0), Arm B/C/D SKIP or PASS (EXPECT_V2 is empty).
    - `node tests/test-267-mcpv2-lockstep.cjs` exits 0 on the unchanged tree.
    - `grep -c "run_if \"267:" tests/run-all-267.sh` is at least 16.
    - `grep -c "@modelcontextprotocol" tests/helpers/mcp-wire-267.cjs` returns 0 (no SDK dependency in the helper).
    - The node one-liner in verify prints a tool count greater than 0 and leaves no child process running (`pgrep -f mindrian-mcp-server.cjs` count unchanged from before the run).
    - `grep -c $'\xe2\x80\x94' tests/helpers/mcp-wire-267.cjs tests/run-all-267.sh tests/test-267-mcpv2-sdk-era.cjs tests/test-267-mcpv2-lockstep.cjs` reports 0 for every file.
  </acceptance_criteria>
  <done>Helper, aggregator, era test and lockstep test committed by explicit path; both tests green on the unchanged tree.</done>
</task>

<task type="auto">
  <name>Task 2: Record the baseline (suites, wire snapshots, shape set, zod importers) and ship the per-commit CIRS gate</name>
  <files>tests/test-267-mcpv2-cirs-gates.cjs, tests/fixtures/267/wire-snapshot-zod3.json, tests/fixtures/267/shape-violations-baseline.txt, tests/fixtures/267/zod-importers-baseline.txt, .planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md, tests/run-all-267.sh</files>
  <read_first>
    - scripts/check-shape-declaration.cjs lines 55-75 and the usage line (the correct invocation is `--check --strict`; a bare `--strict` is a usage error, exit 2, measured at plan time)
    - scripts/build-connector-registry.cjs lines 338-375 (listMcpToolConnectorDescriptors: a module that throws on require is silently skipped, which is exactly why the gate must also assert the descriptor COUNT)
    - tests/run-all-198.sh, tests/run-all-234.sh, tests/run-all-199.sh, tests/run-all-266.sh, tests/run-all-127.sh (the suites to baseline)
    - 267-RESEARCH.md "Validation Architecture" (baseline claims: run-all-198 21 PASS / 3 FAIL; test-257 Arm F stale, Arm B flaky)
  </read_first>
  <action>
1. Capture the wire snapshots with the Task 1 helper and write `tests/fixtures/267/wire-snapshot-zod3.json` as `{ captured_at, plan_base, sdk_version, zod_version, env_note, local: <wireSnapshot(LOCAL_SERVER)>, brain: <wireSnapshot(BRAIN_SHIM)> }`. Record the counts (tools, prompts, resources, templates; the research measured 42 / 9 / 61 / 3 on its run, but record what THIS run measures, never a literal).

2. Run `node scripts/check-shape-declaration.cjs --check --strict`, extract every line beginning `  - surface `, reduce each to its `surface <path>:` token, sort unique, write `tests/fixtures/267/shape-violations-baseline.txt` (one surface path per line). Plan-time measurement: 53 violations, zero under lib/mcp.

3. Write `tests/fixtures/267/zod-importers-baseline.txt`: every non-test production file under bin/, lib/, scripts/, hooks/ whose non-comment source contains `require('zod` or `require("zod` or `requireWithHeal('zod` (this INCLUDES the `zod/v4` subpath users lib/core/pitch-feedback-schemas.cjs, lib/core/skillopt-schemas.cjs, scripts/skillopt-codereview.cjs, scripts/skillopt-genqueries.cjs), sorted. This is the CTX "zod boundary discipline" floor: later plans may not grow this set outside lib/mcp registrars and bin entry points.

4. Create `tests/test-267-mcpv2-cirs-gates.cjs`: (a) spawn `node scripts/build-connector-registry.cjs --check`, require exit 0; (b) spawn `node scripts/check-shape-declaration.cjs --check --strict`, build the violation surface set the same way as step 2, FAIL listing any surface NOT in shape-violations-baseline.txt and FAIL on any surface path under `lib/mcp/`; a baseline surface that disappeared is reported as INFO (improvement), not a failure; (c) require scripts/build-connector-registry.cjs and assert `listMcpToolConnectorDescriptors().length` equals the count recorded in 267-BASELINE.md (read it from a `CONNECTOR_DESCRIPTORS=<n>` line; 29 at plan time). This one command is the per-commit gate every registration-rewrite commit in 267-06..267-09 runs.

5. Run and record, per leg or per arm, PASS/FAIL (and the failing assertion text for every FAIL): `bash tests/run-all-198.sh`, `bash tests/run-all-234.sh`, `bash tests/run-all-199.sh`, `bash tests/run-all-266.sh`, `bash tests/run-all-127.sh`, every `node tests/test-257-*.cjs` (all five files, arm-level detail for test-257-strict-input-shapes.cjs), `node lib/core/mcp-dep-heal.test.cjs`, `node tests/test-265-gate-render-elicit-schema.cjs`, `node tests/test-265-mcp-surface-organ.cjs`, `node tests/test-248-surface-probes.cjs`, `node tests/test-248-resolver-census.cjs`, each of the six `tests/test-354-*.cjs` files that require `@modelcontextprotocol/sdk` (concurrency-surfaces, egress-typed-question, extract-shallow-contract, registration-diagnostics, room-symlink-containment, theo-journey), `node tests/test-276-claim-write-primitive.cjs`, `node tests/test-198-contract-schema.test.cjs`, `node scripts/check-release-payload-ceiling.cjs`, `node tests/test-341-shrinkwrap-no-dev.cjs`, and `node scripts/doctor.cjs --acceptance` (record each acceptance point's pass/fail; if it exceeds 10 minutes, record the timeout and the points reached). Also run the zod-4 breaking-pattern scan over the zod importer set plus tests (patterns: `._def`, `.errors`, `invalid_type_error`, `required_error`, `errorMap`, `z.coerce`, `.flatten(`, `.format(`, `z.nativeEnum`, `.merge(`, `.deepPartial(`, `.strict()`, `.passthrough()`, `.superRefine(`, single-argument `z.record(`) and record per-pattern counts with file:line.

6. Write `267-BASELINE.md`: PLAN_BASE sha, `node lib/core/repo-version.cjs` output, installed versions of sdk/zod/ext-apps/express, the wire counts, `CONNECTOR_DESCRIPTORS=<n>` on its own line, `SHAPE_VIOLATIONS=<n>` on its own line, the full result table from step 5 with every pre-existing FAIL named, the zod scan table, and a "Pre-existing reds (do not fix in this phase unless a plan says so)" list.

7. Fill the `# REGRESSION LEGS` section of tests/run-all-267.sh with run_if legs ONLY for individual test files that PASSED in step 5 (so the aggregator is green-able), plus the CIRS gate leg. Suites with pre-existing reds (run-all-198 and any other) are NOT aggregator legs; plans compare them against 267-BASELINE.md by hand.
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-cirs-gates.cjs && test -s tests/fixtures/267/wire-snapshot-zod3.json && grep -q "^CONNECTOR_DESCRIPTORS=" .planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md && bash tests/run-all-267.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-267-mcpv2-cirs-gates.cjs` exits 0 on the unchanged tree.
    - `wc -l < tests/fixtures/267/shape-violations-baseline.txt` equals the `SHAPE_VIOLATIONS=` value in 267-BASELINE.md, and `grep -c "^lib/mcp" tests/fixtures/267/shape-violations-baseline.txt` is 0.
    - `node -e "const s=require('./tests/fixtures/267/wire-snapshot-zod3.json');process.exit(s.local.tools.length>0&&s.brain.tools.length===6?0:1)"` exits 0.
    - `grep -c "zod/v4\|pitch-feedback-schemas\|skillopt-schemas" tests/fixtures/267/zod-importers-baseline.txt` is at least 2.
    - 267-BASELINE.md contains a line for every command listed in step 5 with PASS or FAIL, and names the run-all-198 pre-existing failures.
    - `bash tests/run-all-267.sh` ends with `FAIL=0`.
    - `git status --short` shows no modified production file (only the files in this plan's frontmatter).
  </acceptance_criteria>
  <done>Baseline recorded, fixtures committed, CIRS gate green, aggregator green on the unchanged tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> spawned MCP server processes | the helper spawns real servers; a leaked child could hold port 3847 or reach a real Brain |
| baseline fixtures -> later plans | later plans treat these fixtures as truth; a wrong baseline would hide a regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-12 | Information disclosure | tests/helpers/mcp-wire-267.cjs, wire-snapshot fixtures | mitigate | hermeticEnv deletes MINDRIAN_BRAIN_KEY and points MINDRIAN_BRAIN_URL at unreachable 127.0.0.1:9, so no fixture capture can send anything to a real Brain (Canon Part 8); snapshots hold tool metadata only, never room content (fixture room is synthetic) |
| T-267-10 | Denial of service | spawned test servers | mitigate | killTree SIGKILL in finally for every spawned PID; HTTP cases assert port 3847 free before and after (Pitfall 9) |
| T-267-13 | Tampering | lockfile drift | mitigate | test-267-mcpv2-lockstep.cjs asserts manifest, package-lock and shrinkwrap agree and zero dev entries; every later dependency plan runs it |
| T-267-16 | Repudiation | baseline integrity | mitigate | PLAN_BASE sha recorded; the baseline is captured before any edit, so a later "unchanged" claim is checkable against a named commit |
</threat_model>

<verification>
- `bash tests/run-all-267.sh` FAIL=0.
- `node tests/test-267-mcpv2-cirs-gates.cjs` exit 0.
- `git diff --stat $PLAN_BASE -- bin lib scripts package.json package-lock.json npm-shrinkwrap.json` is empty (no production change).
</verification>

<success_criteria>
Every later Phase 267 plan can prove "no regression" by pointing at a named baseline number, and every registration-rewrite commit has a one-command CIRS gate.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `tests/helpers/mcp-wire-267.cjs` (hermeticEnv, rpcOverStdio, wireSnapshot, normalizeSchema, compareToSnapshot, killTree, LOCAL_SERVER, BRAIN_SHIM) | new test helper module | 267-03, 267-05, 267-06, 267-09, 267-10, 267-11, 267-15 |
| `tests/run-all-267.sh` | new phase aggregator (all legs pre-declared) | every plan, phase gate |
| `tests/test-267-mcpv2-sdk-era.cjs` (EXPECT_V2 ledger, Arms A-D) | new test (MCPV2-01) | appended by 267-05, 267-11, 267-15; Arm D binds in 267-17 |
| `tests/test-267-mcpv2-lockstep.cjs` | new test (MCPV2-19) | every dependency plan |
| `tests/test-267-mcpv2-cirs-gates.cjs` | new per-commit CLI check (MCPV2-08) | 267-06..267-11 per commit |
| `tests/fixtures/267/wire-snapshot-zod3.json`, `shape-violations-baseline.txt`, `zod-importers-baseline.txt` | new fixtures | 267-03 onward |
| `267-BASELINE.md` (`CONNECTOR_DESCRIPTORS=`, `SHAPE_VIOLATIONS=` keys) | phase doc | every plan's regression comparison |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-01-SUMMARY.md` when done (commit with `git add -f`).
</output>
