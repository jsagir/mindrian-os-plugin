---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 05
type: execute
wave: 3
locked_stage: "W1 (canary: brain stdio shim to v2 serveStdio, migrated and fully verified FIRST)"
depends_on: [267-03]
files_modified:
  - package.json
  - package-lock.json
  - npm-shrinkwrap.json
  - references/security/cve-db.json
  - lib/core/mcp-dep-heal.cjs
  - lib/core/mcp-dep-heal.test.cjs
  - tests/test-267-mcpv2-brain-shim.cjs
  - tests/test-267-mcpv2-sdk-era.cjs
  - bin/mindrian-brain-mcp-client.cjs
autonomous: true
requirements: [MCPV2-11, MCPV2-10, MCPV2-09, MCPV2-19, MCPV2-01, MCPV2-12]
canon_parts: [7, 8]
tags: [canary, brain-shim, serveStdio, sdk-v2, supply-chain]

must_haves:
  truths:
    - "The v2 package family (@modelcontextprotocol/server, core, client) is installed at ONE version re-checked with npm view at execute time (the family moved 2.0.0 -> 2.1.0 inside the research session; never hardcode 2.1.0), with exactly one @modelcontextprotocol/core instance in node_modules"
    - "Each new package has a dated VETTED allowlist entry naming its exact installed version, and package.json, package-lock.json and npm-shrinkwrap.json agree (Phase 341 shrinkwrap delivery)"
    - "bin/mindrian-brain-mcp-client.cjs serves through serveStdio from @modelcontextprotocol/server/stdio and requires nothing from @modelcontextprotocol/sdk"
    - "The shim still exposes exactly the same 6 tools with the same descriptions and strict input schemas in both protocol eras: a default v2 client negotiates 2025-11-25, an auto-negotiating v2 client negotiates 2026-07-28"
    - "Canon Part 8 holds unchanged: the shim stays a transport wrapper with zero network code, every Brain call still goes through lib/core/brain-client.cjs (untouched), and the Phase 257 egress and honest-refusal tests match 267-BASELINE.md"
    - "The dependency self-heal probe knows about the new package (FALLBACK list) while the v1 package is still present"
  artifacts:
    - path: "tests/test-267-mcpv2-brain-shim.cjs"
      provides: "MCPV2-11 canary wire test, RED before migration"
      min_lines: 80
    - path: "bin/mindrian-brain-mcp-client.cjs"
      provides: "v2 brain stdio shim"
      contains: "serveStdio"
    - path: "references/security/cve-db.json"
      provides: "VETTED entries for @modelcontextprotocol/server, core, client"
      contains: "@modelcontextprotocol/server"
  key_links:
    - from: "bin/mindrian-brain-mcp-client.cjs"
      to: "@modelcontextprotocol/server/stdio"
      via: "requireWithHeal on the connect path"
      pattern: "requireWithHeal\\('@modelcontextprotocol/server/stdio'"
    - from: "bin/mindrian-brain-mcp-client.cjs"
      to: "lib/core/brain-client.cjs"
      via: "unchanged require; every network call stays there"
      pattern: "require\\('../lib/core/brain-client.cjs'\\)"
---

<objective>
Migrate the smallest, most isolated stdio surface, the brain shim, to the v2 SDK first, and prove it on the wire in both protocol eras, so any mistake in the migration pattern (subpath requires under requireWithHeal, serveStdio wiring, zod 4 schemas under v2, dep-heal probes, lockfile lockstep) is caught cheaply before the 51-site local server moves.

Purpose: CTX "Scope and sequencing" names the brain stdio shim as the canary ("smallest, most isolated stdio surface, migrate and fully verify FIRST"). CTX "Supply-chain and release gates" requires dated VETTED allowlist entries at the exact pinned version and all three lockfiles. The shim already uses `registerTool` config objects with `z.strictObject` (Phase 257), so this plan changes only the SDK import and the serving entry, never a tool.
Output: v2 installed + vetted, RED-then-GREEN canary test, migrated shim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-RESEARCH.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md
@bin/mindrian-brain-mcp-client.cjs
@tests/helpers/mcp-wire-267.cjs
@tests/fixtures/267/wire-snapshot-zod4.json

Session and scope rules (every task in this plan):
- Workspace /home/jsagi/dev/MindrianOS-Plugin only; record `PLAN_BASE=$(git rev-parse HEAD)` in the SUMMARY.
- Before editing any file run `git status --short -- <that file>`; a diff you did not make means STOP and report.
- Commit by explicit path only (`git add -f` for .planning/), `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git commit -a`, `git stash`.
- Never edit `mcp-server-brain/**`, `/home/jsagi/Theo/**`, `lib/core/brain-client.cjs` (a hand-rolled 2025-era JSON-RPC client to Theo, NOT an SDK consumer; CTX cross-repo boundary), `lib/core/part8-egress-guard.cjs`, `lib/core/session-binding.cjs`, `lib/mcp/gate-render.cjs`. Never run `@modelcontextprotocol/codemod`.
- Every npm install: `npm install --ignore-scripts --no-audit --no-fund ...`. Keep `@modelcontextprotocol/sdk` installed (it is removed LAST, in 267-17).
- Hermetic test env: no MINDRIAN_BRAIN_KEY, MINDRIAN_BRAIN_URL=http://127.0.0.1:9, temp HOME (tests/helpers/mcp-wire-267.cjs hermeticEnv), so no test reaches a real Brain.
- No em-dashes. CJS only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install and vet the v2 family in lockstep, teach dep-heal about it, and write the canary wire test RED</name>
  <files>package.json, package-lock.json, npm-shrinkwrap.json, references/security/cve-db.json, lib/core/mcp-dep-heal.cjs, lib/core/mcp-dep-heal.test.cjs, tests/test-267-mcpv2-brain-shim.cjs</files>
  <read_first>
    - 267-RESEARCH.md "Standard Stack" and "Package Legitimacy Audit" (all [OK] via slopcheck; official monorepo github.com/modelcontextprotocol/typescript-sdk; server pins core EXACTLY; client depends on core exactly too, so server and client must be the same version or two cores install)
    - references/security/cve-db.json `surfaces.supply_chain.allowlist` (entry format; the `ajv` entry's dated note is the format to copy)
    - lib/core/mcp-dep-heal.cjs lines 340-420 (productionDepNames FALLBACK at :364; probes by directory existence, which works for v2 packages that do not export ./package.json)
    - lib/core/mcp-dep-heal.test.cjs lines 35-120 (the FALLBACK literal at :39 and the bug_011 fixtures)
    - tests/test-257-shim-honest-refusal.cjs lines 60-110 (the shim spawn recipe to copy for the keyless path)
  </read_first>
  <behavior>
    - A v2 Client (from @modelcontextprotocol/client, default options) over StdioClientTransport to the shim negotiates protocolVersion 2025-11-25 and lists 6 tools equal (name, description, normalized inputSchema) to the brain section of wire-snapshot-zod4.json; all 6 schemas carry additionalProperties:false.
    - A v2 Client with versionNegotiation in auto mode negotiates 2026-07-28 and lists the same 6 tools. RED before the shim migrates (a v1 server cannot serve 2026-07-28).
    - Keyless: brain_stats returns the tier-0 refusal shape in both eras (no network reached).
    - Source: the shim references @modelcontextprotocol/server inside requireWithHeal and never references @modelcontextprotocol/sdk in a require. RED before migration.
  </behavior>
  <action>
1. Re-check at execute time: `npm view @modelcontextprotocol/server version dependencies`, `npm view @modelcontextprotocol/client version dependencies`, `npm view @modelcontextprotocol/core version`, and for the chosen version V: `npm view @modelcontextprotocol/server@V repository.url scripts`, same for client and core. Require: server, core and client all at the same V; server's and client's `dependencies['@modelcontextprotocol/core']` equal V exactly; repository is github.com/modelcontextprotocol/typescript-sdk; no `preinstall`/`install`/`postinstall` script. If any check fails, STOP and report (do not pick a different package). Record every output in the SUMMARY.

2. `npm install --ignore-scripts --no-audit --no-fund @modelcontextprotocol/server@V @modelcontextprotocol/core@V @modelcontextprotocol/client@V`; manifest ranges `^V` (core is listed directly because ext-apps 2.x declares it as a required peer in 267-10). Bring package-lock.json and npm-shrinkwrap.json into agreement exactly as 267-03 Task 2 step 2 did. `npm ls @modelcontextprotocol/core --all` must show one version.

3. Append three allowlist entries to `surfaces.supply_chain.allowlist` in references/security/cve-db.json, each `{ "name": "@modelcontextprotocol/<server|core|client>", "disposition": "VETTED", "note": "<role>. MIT, official monorepo github.com/modelcontextprotocol/typescript-sdk, v<V> exact at install, NO install scripts (npm view scripts empty), slopcheck [OK] (267-RESEARCH Package Legitimacy Audit), transitive surface: <deps>. Reviewed <YYYY-MM-DD> for Phase 267." }`. Edit JSON via a `node -e` read-modify-write that preserves 2-space formatting; do not reorder existing entries.

4. mcp-dep-heal: change the FALLBACK literal at lib/core/mcp-dep-heal.cjs:364 to `['@modelcontextprotocol/server', '@modelcontextprotocol/sdk', 'zod']` (sdk stays until 267-17) and update its doc comment; update the matching FALLBACK constant and any assertion on it in lib/core/mcp-dep-heal.test.cjs:39 so the test still describes reality. Do not change probe logic.

5. Write tests/test-267-mcpv2-brain-shim.cjs implementing the behavior list with `Client` from `@modelcontextprotocol/client` and `StdioClientTransport` from `@modelcontextprotocol/client/stdio` (subpath exports verified at plan time), spawn env from hermeticEnv(). In auto mode the SDK probes on a disposable sibling process (RESEARCH Pattern 1 note), so the shim boots twice: collect every child PID the transport exposes, SIGKILL them in finally, and assert no `mindrian-brain-mcp-client.cjs` process started by this test survives. Compare schemas with normalizeSchema (the $schema draft changes from draft-07 to 2020-12 under v2; that is not a contract change).

6. Run it: it must FAIL on the 2026 arm and the source arm, and PASS the 2025 arm (v1 shim, v2 client). Run `node tests/test-267-mcpv2-lockstep.cjs`, `node lib/core/mcp-dep-heal.test.cjs`, `node scripts/check-release-payload-ceiling.cjs`, `bash tests/run-all-199.sh` (supply-chain self-scan now sees the new names as VETTED). Commit (a) deps + lockfiles + allowlist, (b) dep-heal pair, (c) the RED test.
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-lockstep.cjs && node lib/core/mcp-dep-heal.test.cjs && node scripts/check-release-payload-ceiling.cjs && bash tests/run-all-199.sh && (node tests/test-267-mcpv2-brain-shim.cjs; test $? -ne 0 && echo "canary RED as expected")</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const fs=require('fs');for(const n of ['server','core','client'])console.log(n,JSON.parse(fs.readFileSync('node_modules/@modelcontextprotocol/'+n+'/package.json')).version)"` prints the same V three times (read from disk: v2 packages do not export ./package.json, so `require('<pkg>/package.json')` is not usable).
    - `node tests/test-267-mcpv2-lockstep.cjs` exits 0 (one core, one zod, lockfiles agree, all @modelcontextprotocol/* names VETTED).
    - `node -e "const a=require('./references/security/cve-db.json').surfaces.supply_chain.allowlist;for(const n of ['server','core','client']){const e=a.find(x=>x.name==='@modelcontextprotocol/'+n);if(!e||e.disposition!=='VETTED'||!/v[0-9]/.test(e.note))process.exit(1)}"` exits 0.
    - `bash tests/run-all-199.sh` has no FAIL beyond 267-BASELINE.md.
    - The canary test fails on exactly the 2026 arm and the source arm; its output says so.
  </acceptance_criteria>
  <done>v2 family installed at one re-checked version, vetted, in lockstep; dep-heal aware; canary test committed RED.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the shim to serveStdio and prove Part 8, strictness and both eras</name>
  <files>bin/mindrian-brain-mcp-client.cjs, tests/test-267-mcpv2-sdk-era.cjs</files>
  <read_first>
    - bin/mindrian-brain-mcp-client.cjs (all 333 lines: requireWithHeal at :58-60, McpServer at :115, six registerTool calls :163-308, main() at :310-328 with the prewarm)
    - tests/test-266-connect-path-process-budget.cjs (the connect-path heal-call census for this file: ensureDepsPresent plus three requireWithHeal calls must stay)
    - 267-RESEARCH.md Pattern 1 (serveStdio with a module-level singleton is fine: a stdio process serves one connection; the factory is called once per connection)
    - node_modules/@modelcontextprotocol/server/dist/stdio.d.cts (serveStdio(factory, options) returns a handle with close(); options.legacy defaults to 'serve')
  </read_first>
  <action>
1. Replace `requireWithHeal('@modelcontextprotocol/sdk/server/mcp.js', ...)` with `requireWithHeal('@modelcontextprotocol/server', { log: healLog, connectPath: true })` destructuring `McpServer`, and `requireWithHeal('@modelcontextprotocol/sdk/server/stdio.js', ...)` with `requireWithHeal('@modelcontextprotocol/server/stdio', { log: healLog, connectPath: true })` destructuring `serveStdio`. Keep the zod requireWithHeal. That keeps exactly three connect-path requireWithHeal calls plus ensureDepsPresent (test-266 census).

2. In main(): replace `new StdioServerTransport()` + `server.connect(transport)` with `serveStdio(() => server)` (keep the returned handle in a module variable for future shutdown use); keep the started log line and the prewarm block exactly after it. Do not touch the six registerTool calls, their descriptions, their z.strictObject schemas, `z.record(z.any())` in brain_query (zod 4.6.5 still accepts the single-argument form; research: do not spend effort there), tier0Response, honestRefusal, or any require of lib/core/*.

3. Update the header comment's SDK mention (one line naming @modelcontextprotocol/server v2 and serveStdio, no em-dashes). Keep the Canon Part 8 delegation note verbatim.

4. Append `'bin/mindrian-brain-mcp-client.cjs'` to `EXPECT_V2` in tests/test-267-mcpv2-sdk-era.cjs.

5. Verify against 267-BASELINE.md (no newly red arm): `node tests/test-267-mcpv2-brain-shim.cjs` (now GREEN), `node tests/test-267-mcpv2-sdk-era.cjs` (Arm B PASS, Arm C PASS for the shim), every `node tests/test-257-*.cjs` (strict shapes Arms A-E and G, egress invariant, honest refusal, envelope passthrough, refusal egress kind), `bash tests/run-all-127.sh` (shim handshake, acceptance gates including the zero network-token scan of this file, no em-dashes), `node tests/test-266-connect-path-process-budget.cjs`, `node tests/test-266-dep-heal-connect-budget.cjs`, `node tests/test-354-egress-typed-question.cjs`, `node tests/test-354-theo-mcp-exposure.cjs`, `node tests/test-339-brain-prewarm-cold-start.cjs`, `node tests/test-252-guard-census.cjs`, `node tests/test-250-refusal-shapes.cjs`, `node tests/test-265-mcp-surface-organ.cjs` (the doctor organ's hand-rolled 2024-11-05 spawnSync handshake with stdin closed immediately: verified at plan time that v2 serveStdio answers before closing on EOF), `node tests/test-267-mcpv2-zod4-contract.cjs`, `node tests/test-267-mcpv2-tee.cjs`, and `DOCTOR_SKIP_MCP_SURFACE= node scripts/doctor.cjs --acceptance` points `mcp-surface-tool-count` and the L4 server-version point (compare to baseline).

6. Commit shim + era test by explicit path. Append a short "Canary lessons" section to the SUMMARY: anything about subpath requires, serveStdio, schema drafts or process lifecycles that 267-11 must repeat or avoid.
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-brain-shim.cjs && node tests/test-267-mcpv2-sdk-era.cjs && for t in tests/test-257-*.cjs; do node $t || echo "CHECK-AGAINST-BASELINE $t"; done; bash tests/run-all-127.sh && node tests/test-266-connect-path-process-budget.cjs && node tests/test-265-mcp-surface-organ.cjs && bash tests/run-all-267.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v "^\s*//\|^\s*\*" bin/mindrian-brain-mcp-client.cjs | grep -c "@modelcontextprotocol/sdk"` prints 0.
    - `grep -c "serveStdio(() => server)" bin/mindrian-brain-mcp-client.cjs` prints 1 and `grep -c "registerTool(" bin/mindrian-brain-mcp-client.cjs` prints 6.
    - `grep -c "requireWithHeal(" bin/mindrian-brain-mcp-client.cjs` equals `git show $PLAN_BASE:bin/mindrian-brain-mcp-client.cjs | grep -c "requireWithHeal("` (three connect-path calls at plan time).
    - `node tests/test-267-mcpv2-brain-shim.cjs` exits 0 with the 2026 arm reporting protocolVersion 2026-07-28 and the 2025 arm 2025-11-25.
    - Every test-257 file's per-arm outcome matches 267-BASELINE.md (Arm F stale and Arm B flaky may keep their baseline state; any other arm newly failing is a failure).
    - `git diff $PLAN_BASE -- lib/core/brain-client.cjs lib/core/part8-egress-guard.cjs mcp-server-brain | wc -l` prints 0.
    - `bash tests/run-all-267.sh` ends FAIL=0.
  </acceptance_criteria>
  <done>Brain shim on v2 serveStdio, green in both eras, Part 8 and strictness proven unchanged, lessons recorded for 267-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> node_modules / npm-shrinkwrap.json | three new third-party packages enter every user install |
| MCP host -> brain shim tool input | model-authored arguments that may try to smuggle room content toward the Brain |
| brain shim -> brain-client.cjs -> Theo | the Canon Part 8 LOCAL -> BRAIN boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-SC | Tampering | npm installs of @modelcontextprotocol/server, core, client | mitigate | research slopcheck [OK] for all three (no [ASSUMED]/[SUS], so no blocking legitimacy checkpoint is required); re-verified at execute time: official repository URL, no install scripts, exact version; --ignore-scripts; dated VETTED allowlist entries; single core instance asserted |
| T-267-09 | Information disclosure | Canon Part 8 on the shim | mitigate | shim keeps zero network code (run-all-127 network-token scan), brain-client.cjs and part8-egress-guard.cjs untouched (diff gate), all Phase 257 egress/refusal tests at baseline |
| T-267-02 | Tampering | undeclared-key smuggling into brain_* tools | mitigate | the six z.strictObject schemas are unchanged and asserted to advertise additionalProperties:false in both eras; runtime rejection covered by test-257 strict-input-shapes arms |
| T-267-15 | Denial of service | auto-negotiation sibling process in tests | mitigate | every spawned PID SIGKILLed in finally; test asserts no surviving shim process it started; production hosts do not use auto mode on stdio (research, verified) |
| T-267-13 | Tampering | lockfile drift | mitigate | lockstep test, shrinkwrap no-dev test, payload ceiling |
</threat_model>

<verification>
- `bash tests/run-all-267.sh` FAIL=0.
- test-257, run-all-127, run-all-199, test-266 at baseline.
- `git diff $PLAN_BASE --stat` lists only this plan's files.
</verification>

<success_criteria>
The migration pattern is proven end to end on the smallest surface, with Part 8 intact, before the local server is touched.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `@modelcontextprotocol/server`, `@modelcontextprotocol/core`, `@modelcontextprotocol/client` at execute-time version V (package.json + package-lock.json + npm-shrinkwrap.json) | new dependencies | 267-10 onward |
| three VETTED entries in `references/security/cve-db.json` `surfaces.supply_chain.allowlist` | new config entries | AgentShield supply-chain scan |
| `lib/core/mcp-dep-heal.cjs` FALLBACK now includes `@modelcontextprotocol/server` | config literal change | dep self-heal |
| `tests/test-267-mcpv2-brain-shim.cjs` | new test (MCPV2-11) | aggregator |
| `bin/mindrian-brain-mcp-client.cjs` served by `serveStdio` | migrated entry point | every surface |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-05-SUMMARY.md` when done (commit with `git add -f`), including the "Canary lessons" section.
</output>
