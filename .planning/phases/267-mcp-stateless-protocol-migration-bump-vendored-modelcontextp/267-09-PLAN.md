---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 09
type: execute
wave: 7
locked_stage: "W2 (local server stdio branch): runtime-loop prompts fix (RCA 4) plus prompts.cjs (3 sites) and resources.cjs (9 sites) registration rewrites"
depends_on: [267-08]
files_modified:
  - tests/test-267-mcpv2-prompts.cjs
  - lib/mcp/prompts.cjs
  - lib/mcp/resources.cjs
  - tests/fixtures/267/zod4-accepted-deltas.json
  - .planning/debug/runtime-loop-prompts-bogus-args-schema.md
  - .planning/debug/resolved/runtime-loop-prompts-bogus-args-schema.md
  - .planning/debug/knowledge-base.md
autonomous: true
requirements: [MCPV2-16, MCPV2-02, MCPV2-08, MCPV2-15]
canon_parts: [7, 11]
tags: [prompts, resources, rca, registration-rewrite]

must_haves:
  truths:
    - "prompts/list advertises bind-room, status and act WITH their description and title; bind-room and status take no arguments; act takes exactly one optional argument `goal`; no prompt advertises arguments named `description` or `arguments`"
    - "prompts/get succeeds for every one of the 9 prompts (no -32603 keyValidator error), and act embeds a supplied goal in its message"
    - "A prompts wire test fails against the pre-fix code and passes after it (test-first for a bug fix)"
    - "The 9 server.resource sites use registerResource(name, uriOrTemplate, metadata, callback) with the SAME metadata objects and callbacks; resources/list and resources/templates/list match the baseline snapshot"
    - "After this plan zero variadic server.tool/prompt/resource registrations remain under lib/mcp/, and registration-api full mode passes (every live tool titled, live count equals the snapshot)"
    - "RCA 4 is resolved with real verification output and a knowledge-base block"
  artifacts:
    - path: "tests/test-267-mcpv2-prompts.cjs"
      provides: "MCPV2-16 prompts wire test"
      min_lines: 50
    - path: "lib/mcp/prompts.cjs"
      provides: "9 registerPrompt registrations"
      contains: "argsSchema: {"
    - path: "lib/mcp/resources.cjs"
      provides: "9 registerResource registrations"
      contains: "server.registerResource("
  key_links:
    - from: "lib/mcp/prompts.cjs act prompt"
      to: "args.goal"
      via: "argsSchema raw shape { goal: z.string().optional() }"
      pattern: "goal: z\\.string\\(\\)\\.optional\\(\\)"
---

<objective>
Fix the three dead runtime-loop prompts test-first (RCA 4), then move the last variadic registrations (3 prompts, 9 resources) to the config-object API.

Purpose: planning found (and reproduced live, 2026-09-23) that `lib/mcp/prompts.cjs:358,378,398` pass prompt metadata `{description, arguments}` in the v1 `server.prompt()` argsSchema slot: bind-room, status and act advertise two bogus required arguments and no description, and prompts/get fails with -32603 on every surface. These three prompts are the hookless-surface runtime loop in Desktop's prompt menu. The fix is the registration rewrite itself done correctly (a mechanical copy of the broken object into `argsSchema` would carry the bug into v2), so it lands in the same wave and file, test-first (CTX "Scope and sequencing": bugs fixed in the wave that owns the file). Resources are a pure rename: all 9 already pass metadata as the third argument.
Output: prompts test (RED then GREEN), fixed and migrated prompts.cjs, migrated resources.cjs, resolved RCA 4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md
@.planning/debug/runtime-loop-prompts-bogus-args-schema.md
@lib/mcp/prompts.cjs
@tests/helpers/mcp-wire-267.cjs

Session and scope rules (every task in this plan):
- Workspace /home/jsagi/dev/MindrianOS-Plugin only; record `PLAN_BASE=$(git rev-parse HEAD)` in the SUMMARY.
- Before editing any file run `git status --short -- <that file>`; a diff you did not make means STOP and report.
- Commit by explicit path only (`git add -f` for .planning/), `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git commit -a`, `git stash`.
- Never edit `lib/core/room-path-containment.cjs` (resources.cjs uses it; unchanged), `lib/mcp/session-room.cjs`, `lib/mcp/gate-render.cjs`, `lib/core/brain-client.cjs`, `lib/core/part8-egress-guard.cjs`, `lib/core/session-binding.cjs`, `mcp-server-brain/**`, `/home/jsagi/Theo/**`.
- Prompts keep RAW-SHAPE argsSchema (the only form v1 registerPrompt accepts, per the v1 type declarations; v2 accepts it too), matching the six existing registerPrompt calls in the same file.
- Per-commit gate: `node tests/test-267-mcpv2-registration-api.cjs --file <file>`, `node tests/test-267-mcpv2-cirs-gates.cjs`, `node tests/test-267-mcpv2-zod4-contract.cjs`.
- No em-dashes. CJS only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prompts wire test RED, then fix and migrate the three runtime-loop prompts</name>
  <files>tests/test-267-mcpv2-prompts.cjs, lib/mcp/prompts.cjs, tests/fixtures/267/zod4-accepted-deltas.json</files>
  <read_first>
    - lib/mcp/prompts.cjs (all 421 lines: the six correct registerPrompt calls at :101-351 and the three broken server.prompt calls at :358, :378, :398)
    - .planning/debug/runtime-loop-prompts-bogus-args-schema.md (Evidence and Required Code Changes)
    - tests/helpers/mcp-wire-267.cjs (rpcOverStdio for prompts/list and prompts/get)
    - tests/fixtures/267/zod4-accepted-deltas.json (the `prompts_fix` array this task fills)
  </read_first>
  <behavior>
    - prompts/list: bind-room, status, act each have a non-empty description equal to the description string in source; bind-room and status have zero arguments; act has exactly [{name:'goal', required:false}] with the description 'Optional goal or focus to steer the pick'; no prompt has an argument named `description` or `arguments`. RED now.
    - prompts/get succeeds for all 9 prompts (supply minimal fixture values for required args: transcript for file-meeting, and whatever the other six declare as required); act with {goal:'focus on pricing'} returns a first message whose text begins with 'Goal: focus on pricing'; act with {} returns a message not containing 'Goal:'. RED now on bind-room, status, act.
  </behavior>
  <action>
1. Write tests/test-267-mcpv2-prompts.cjs per the behavior list over the live local server (hermeticEnv). Run: must FAIL on the three runtime-loop prompts. Commit (RED).
2. In lib/mcp/prompts.cjs replace the three `server.prompt(...)` calls: bind-room -> `server.registerPrompt('bind-room', { title: 'Bind Room', description: <the existing description string verbatim> }, <existing callback>)`; status -> same shape with title 'Status'; act -> `server.registerPrompt('act', { title: 'Act', description: <existing string verbatim>, argsSchema: { goal: z.string().optional().describe('Optional goal or focus to steer the pick') } }, <existing callback>)`. The callbacks keep their bodies; act's callback keeps reading `args && args.goal`. Confirm `z` is in scope (the six siblings use it). Update the JSDoc import-type comment at :91 only if it blocks nothing (leave it; 267-11 updates JSDoc types).
3. Add the three prompts' now-correct list entries to `prompts_fix` in tests/fixtures/267/zod4-accepted-deltas.json as accepted deltas (kind 'prompt', name, field 'description' and 'arguments', with a note citing RCA 4), so the zod4 contract test accepts this intentional change and nothing else.
4. Run the prompts test (GREEN), the per-commit gate for prompts.cjs, and `node tests/test-267-mcpv2-zod4-contract.cjs`. Commit prompts.cjs + the fixture as the RCA 4 fix commit.
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-prompts.cjs && node tests/test-267-mcpv2-registration-api.cjs --file lib/mcp/prompts.cjs && node tests/test-267-mcpv2-cirs-gates.cjs && node tests/test-267-mcpv2-zod4-contract.cjs</automated>
  </verify>
  <acceptance_criteria>
    - The RED test commit precedes the fix commit in `git log --oneline $PLAN_BASE..HEAD`.
    - `grep -c "server\.prompt(" lib/mcp/prompts.cjs` prints 0; `grep -c "server\.registerPrompt(" lib/mcp/prompts.cjs` prints 9.
    - `grep -c "arguments: \[" lib/mcp/prompts.cjs` prints 0 (the misplaced metadata key is gone).
    - `node tests/test-267-mcpv2-prompts.cjs` exits 0 and prints PASS for prompts/get on all 9 prompts.
    - `node tests/test-267-mcpv2-zod4-contract.cjs` exits 0 with `prompts_fix` holding exactly the three prompts.
  </acceptance_criteria>
  <done>Runtime-loop prompts work on every surface; prompts.cjs fully on registerPrompt.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate resources.cjs (9 sites), prove registration-api full mode, resolve RCA 4</name>
  <files>lib/mcp/resources.cjs, .planning/debug/runtime-loop-prompts-bogus-args-schema.md, .planning/debug/resolved/runtime-loop-prompts-bogus-args-schema.md, .planning/debug/knowledge-base.md</files>
  <read_first>
    - lib/mcp/resources.cjs (sites :79 room-state, :94 room-sections, :128 room-section template, :198 room-meetings, :244 room-intelligence, :259 reasoning-state, :283 reasoning-section template, :314 mos-tree, :359 mos-room-tree template; ResourceTemplate require at :21)
    - tests/test-354-room-symlink-containment.cjs header (the realpath containment the room-section and reasoning-section reads rely on)
    - .planning/debug/knowledge-base.md top 40 lines (block format)
  </read_first>
  <action>
1. `git status --short -- lib/mcp/resources.cjs` clean. Replace each `server.resource(NAME, URI_OR_TEMPLATE, METADATA, CB)` with `server.registerResource(NAME, URI_OR_TEMPLATE, METADATA, CB)`: same four arguments, same order, verbatim (v1 1.30.1 and v2 both take the metadata object as the required third argument). Do NOT change the ResourceTemplate require in this plan (267-11 moves it; plan-time probe showed v1 and v2 ResourceTemplate instances are accepted by either McpServer, so there is no ordering hazard). Do not add titles to resources (out of this plan's scope; tools and prompts only).
2. Gate: `--file lib/mcp/resources.cjs`, cirs gate, zod4 contract, `node tests/test-354-room-symlink-containment.cjs`, and a wire check that resources/list and resources/templates/list equal wire-snapshot-zod4.json (compareToSnapshot over resources and templates). Commit resources.cjs alone.
3. Run registration-api FULL mode: `node tests/test-267-mcpv2-registration-api.cjs` (no --file) must now PASS (every registrar migrated, every live tool titled, live count equals the snapshot). Run `grep -rnE "server\.(tool|prompt|resource)\(" lib/mcp/ bin/` (must be empty) and `bash tests/run-all-198.sh` (baseline).
4. Resolve RCA 4 exactly as 267-07 Task 2 step 3 resolved RCA 3 (Resolution from real output, status resolved, git mv to resolved/, knowledge-base block with Error patterns: server.prompt, argsSchema, keyValidator._parse, -32603, prompts/get, bind-room, runtime-loop prompts; Pattern lesson: a variadic API that guesses argument roles by position will silently accept metadata as a schema, which is one reason v2 removed it). Commit the RCA move and knowledge-base.
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-registration-api.cjs && node tests/test-267-mcpv2-cirs-gates.cjs && node tests/test-354-room-symlink-containment.cjs && test -z "$(grep -rnE 'server\.(tool|prompt|resource)\(' lib/mcp/ bin/)" && grep -q "^status: resolved" .planning/debug/resolved/runtime-loop-prompts-bogus-args-schema.md && bash tests/run-all-267.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "server\.registerResource(" lib/mcp/resources.cjs` prints 9 and `grep -c "server\.resource(" lib/mcp/resources.cjs` prints 0.
    - `node tests/test-267-mcpv2-registration-api.cjs` (full mode) exits 0: the first time it is green in this phase (SUMMARY says so).
    - `grep -rnE "server\.(tool|prompt|resource)\(" lib/mcp/ bin/ | wc -l` prints 0: all 51 variadic sites migrated.
    - RCA 4 exists only under .planning/debug/resolved/ with status resolved; knowledge-base.md's newest block is its slug.
    - `bash tests/run-all-267.sh` ends FAIL=0.
  </acceptance_criteria>
  <done>All 51 variadic registration sites migrated; RCA 4 resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host -> prompts/get arguments | host-supplied prompt arguments (act's `goal`) interpolated into a prompt message |
| MCP host -> resources/read URIs | host-supplied URI template variables (section names, room slugs) that map to local file reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-25 | Tampering | act prompt `goal` argument | accept | the goal string is echoed into a message the host's own model reads, the same trust level as the user typing it; zod validates it is a string; no file, graph or Brain write happens in a prompt callback |
| T-267-24 | Information disclosure | resources/read (room-section, reasoning-section, mos-room-tree) | mitigate | callbacks move verbatim, so Phase 354-05 realpath containment is unchanged; test-354-room-symlink-containment runs at the resources commit |
| T-267-21 | Elevation of privilege | CIRS declarations | mitigate | per-commit cirs gate |
</threat_model>

<verification>
- Prompts test and registration-api full mode green.
- Zero variadic registrations under lib/mcp/ and bin/.
- RCA 4 resolved; run-all-198 at baseline; run-all-267 FAIL=0.
</verification>

<success_criteria>
The local server's whole registration surface is on the v2-compatible API while still running on v1, and the three dead prompts work.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `tests/test-267-mcpv2-prompts.cjs` | new test (MCPV2-16) | aggregator |
| bind-room, status, act prompts with title, description and correct arguments | fixed wire surface (RCA 4) | Desktop prompt menu, all hosts |
| `prompts_fix` entries in `tests/fixtures/267/zod4-accepted-deltas.json` | accepted-delta ledger entries | zod4 contract test |
| `lib/mcp/resources.cjs` on registerResource | migrated registrar | 267-11 swap |
| `.planning/debug/resolved/runtime-loop-prompts-bogus-args-schema.md` + knowledge-base block | resolved RCA | gsd-debugger |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-09-SUMMARY.md` when done (commit with `git add -f`).
</output>
