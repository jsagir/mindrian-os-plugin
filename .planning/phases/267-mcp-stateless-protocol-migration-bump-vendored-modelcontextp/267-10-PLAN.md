---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 10
type: execute
wave: 8
locked_stage: "W2 (local server stdio branch): ext-apps 2.x plus the app-views.cjs schema: -> inputSchema: fix (RCA 2) with room_path containment"
depends_on: [267-09]
files_modified:
  - tests/test-267-mcpv2-app-views.cjs
  - package.json
  - package-lock.json
  - npm-shrinkwrap.json
  - references/security/cve-db.json
  - lib/mcp/app-views.cjs
  - tests/fixtures/267/zod4-accepted-deltas.json
  - .planning/debug/app-views-schema-key-drops-input-schemas.md
  - .planning/debug/resolved/app-views-schema-key-drops-input-schemas.md
  - .planning/debug/knowledge-base.md
autonomous: true
requirements: [MCPV2-04, MCPV2-10, MCPV2-19, MCPV2-12, MCPV2-15]
canon_parts: [7, 8, 9]
tags: [mcp-apps, ext-apps-2, rca, containment, dependency-bump]

must_haves:
  truths:
    - "@modelcontextprotocol/ext-apps moves from 1.5.0 to the latest 2.x at execute time (re-checked with npm view: its required peers @modelcontextprotocol/core and client ^2 and its zod peer must be satisfied by what is already installed), in lockstep across package.json, package-lock.json and npm-shrinkwrap.json"
    - "room-dashboard, room-wiki and room-graph publish real input schemas (room_path; section; layout enum of cose, circle, grid, breadthfirst) and their handlers receive those arguments"
    - "Because the fix turns a previously dark input live, room_path is honored only when its realpath is STRICTLY INSIDE the rooms home (a real, individual room folder) -- the rooms home root itself is refused too (plan-checker finding W2: isRealpathContained alone returns true when resolved === home, which would let a caller pass the rooms home as room_path and have scanRoomData read every room's top-level .md files as if they were one room's sections -- a cross-room read inside the trust boundary, not an escape from it, but still not the single-room scope this tool is meant to expose); anything outside or equal to the rooms home is refused with an honest isError result and no read happens; an omitted room_path keeps today's fallback to the boot room"
    - "An app-views wire test fails against the pre-fix code and passes after it (test-first for a bug fix), and the fix lands in the same commit as the ext-apps 2.x move that rewrites app-views.cjs's registration config (CTX: 'Fix it in the same commit that touches app-views.cjs for the registration-API rewrite')"
    - "RCA 2 is resolved with real verification output"
  artifacts:
    - path: "tests/test-267-mcpv2-app-views.cjs"
      provides: "MCPV2-04 schema, argument delivery and containment test"
      min_lines: 70
    - path: "lib/mcp/app-views.cjs"
      provides: "inputSchema keys and realpath containment of room_path"
      contains: "isRealpathContained"
  key_links:
    - from: "lib/mcp/app-views.cjs"
      to: "lib/core/room-path-containment.cjs isRealpathContained"
      via: "require; Canon Part 7 reuse of the Phase 354-05 helper"
      pattern: "room-path-containment"
    - from: "lib/mcp/app-views.cjs"
      to: "lib/core/icm-forest.cjs listRoomRoots"
      via: "rooms-home resolution shared with tree-watcher and mos://tree"
      pattern: "listRoomRoots"
---

<objective>
Fix the MCP Apps empty-schema bug test-first (RCA 2), in the same commit that moves app-views.cjs to ext-apps 2.x, and close the new read surface the fix opens with realpath containment.

Purpose: CTX NEW FAILURE 2 (lib/mcp/app-views.cjs:242/267/296 pass `schema:` instead of `inputSchema:`; all three MCP Apps tools publish empty schemas on v1 and v2, so room_path, section and layout never arrive) and the CTX instruction to fix it in the same commit that touches app-views.cjs for the registration rewrite. ext-apps 2.0.0 (2026-09-08) unblocked this phase; its `./server` helpers are duck-typed wrappers over `server.registerTool`/`registerResource` with zero runtime imports (research tarball read), so it runs on the still-v1 McpServer here and on v2 after 267-11. Security: until now `room_path` was unreachable; making it live gives any caller (including a prompt-injected model on Desktop, which otherwise has no filesystem reach) a directory-read primitive over .md files. The Phase 354-05 realpath helper closes that (Canon Part 7 reuse).
Output: RED test, ext-apps 2.x in lockstep, fixed and contained app-views.cjs, resolved RCA 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-RESEARCH.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md
@.planning/debug/app-views-schema-key-drops-input-schemas.md
@lib/mcp/app-views.cjs
@lib/core/room-path-containment.cjs
@tests/helpers/mcp-wire-267.cjs

Session and scope rules (every task in this plan):
- Workspace /home/jsagi/dev/MindrianOS-Plugin only; record `PLAN_BASE=$(git rev-parse HEAD)` in the SUMMARY.
- Before editing any file run `git status --short -- <that file>`; a diff you did not make means STOP and report.
- Commit by explicit path only (`git add -f` for .planning/), `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git commit -a`, `git stash`.
- Never edit `lib/core/room-path-containment.cjs`, `lib/core/icm-forest.cjs` (reuse, do not modify), `lib/mcp/gate-render.cjs`, `lib/core/brain-client.cjs`, `lib/core/part8-egress-guard.cjs`, `lib/core/session-binding.cjs`, `mcp-server-brain/**`, `/home/jsagi/Theo/**`.
- Every npm install: `npm install --ignore-scripts --no-audit --no-fund ...`.
- No em-dashes. CJS only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: App-views wire test RED (schemas, argument delivery, containment)</name>
  <files>tests/test-267-mcpv2-app-views.cjs</files>
  <read_first>
    - lib/mcp/app-views.cjs lines 90-120 (scanRoomData reads STATE.md and section .md files under the given dir) and 229-322 (the three registrations)
    - lib/mcp/surface-detect.cjs CAPABILITY_MAP and lib/mcp/capability-registry.cjs (which surface env registers MCP Apps; the test env must be one that does, the same env the zod4 snapshot used)
    - lib/core/room-path-containment.cjs lines 90-140 (realRoomRoot, isRealpathContained semantics, symlink policy)
    - lib/core/icm-forest.cjs lines 205-240 (listRoomRoots(home) -> { home, rooms })
  </read_first>
  <behavior>
    - tools/list: room-dashboard inputSchema has property room_path; room-wiki has room_path and section; room-graph has room_path and layout with enum [cose, circle, grid, breadthfirst]. RED now (properties {}).
    - room-wiki called with room_path = <hermetic rooms home>/room-267 and section 'problem-definition' returns JSON whose name is 'room-267' and _activeSection is 'problem-definition'. RED now.
    - room-graph with layout 'circle' returns _layout 'circle'. RED now.
    - room-dashboard with room_path = a temp dir OUTSIDE the rooms home containing a STATE.md with sentinel text APPVIEW-OUTSIDE-SENTINEL returns isError true, and the sentinel appears nowhere in the response. Also a symlink INSIDE the rooms home pointing at that outside dir is refused the same way.
    - room-dashboard with no room_path returns data for the boot MINDRIAN_ROOM (today's fallback, unchanged).
  </behavior>
  <action>
Write tests/test-267-mcpv2-app-views.cjs using tests/helpers/mcp-wire-267.cjs (hermeticEnv gives a rooms home containing room-267; add the outside dir and the in-home symlink in the test's own setup; the test creates a `problem-definition` section folder with one .md file inside room-267). Call tools via rpcOverStdio tools/call. Implement every behavior bullet. Add a fourth containment arm (plan-checker W2): room_path equal to the rooms home realpath itself (not a subdirectory) must return isError true, with a distinct assertion from the outside-path arm -- this proves the guard rejects the root, not just paths outside it. Run it: the schema and argument-delivery arms FAIL now; note the containment arms may currently "pass" vacuously because room_path is ignored (it falls back to the boot room), so assert explicitly that the response for the outside path AND for the rooms-home-itself path are both isError true, which FAILS now. Commit the test (RED).
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-app-views.cjs; test $? -ne 0 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - The test exits non-zero on the unchanged tree and its output names the schema arm, the argument arm and the containment (isError) arm as failing.
    - The sentinel string check exists in the test (`grep -c "APPVIEW-OUTSIDE-SENTINEL" tests/test-267-mcpv2-app-views.cjs` at least 2: planted and asserted absent).
  </acceptance_criteria>
  <done>RED app-views test committed.</done>
</task>

<task type="auto">
  <name>Task 2: ext-apps 2.x in lockstep plus the inputSchema fix and room_path containment (one fix commit), then resolve RCA 2</name>
  <files>package.json, package-lock.json, npm-shrinkwrap.json, references/security/cve-db.json, lib/mcp/app-views.cjs, tests/fixtures/267/zod4-accepted-deltas.json, .planning/debug/app-views-schema-key-drops-input-schemas.md, .planning/debug/resolved/app-views-schema-key-drops-input-schemas.md, .planning/debug/knowledge-base.md</files>
  <read_first>
    - 267-RESEARCH.md Summary paragraph 1 (ext-apps 2.0.0: duck-typed helpers; raw-shape inputSchema deprecated but working; lockfiles pinned 1.5.0) and "Code Examples: ext-apps 2.0.0 from CJS against v2"
    - .planning/debug/app-views-schema-key-drops-input-schemas.md (Required Code Changes)
    - references/security/cve-db.json (the existing `@modelcontextprotocol/ext-apps` VETTED entry)
  </read_first>
  <action>
1. Re-check: `npm view @modelcontextprotocol/ext-apps version peerDependencies peerDependenciesMeta repository.url scripts`. Require: latest is 2.x; required peers `@modelcontextprotocol/core` and `@modelcontextprotocol/client` ranges are satisfied by the version V installed in 267-05; `zod` peer satisfied by the installed zod; react/react-dom/server peers are optional (plan-time: optional); no install scripts; repository github.com/modelcontextprotocol/ext-apps. If a required peer is unsatisfied, STOP and report.
2. `npm install --ignore-scripts --no-audit --no-fund @modelcontextprotocol/ext-apps@<that 2.x>`; manifest `^<2.x>`; lockfile lockstep exactly as 267-03 Task 2 step 2; `npm ls react react-dom` must show neither installed (optional peers stay out of the user payload). Update the existing ext-apps allowlist entry with a dated note (version, review date, "2.x ./server entry has zero runtime imports; duck-typed over McpServer.registerTool/registerResource", no install scripts); do not duplicate the entry.
3. In lib/mcp/app-views.cjs: rename `schema:` to `inputSchema:` at the three registerAppTool configs (keep the z.object shapes verbatim). Add containment: require `isRealpathContained` from '../core/room-path-containment.cjs' and `listRoomRoots` from '../core/icm-forest.cjs'; add one local function `resolveAppRoomDir(roomPathArg, bootRoomDir)` returning `{ dir }` or `{ refused: reason }`: when roomPathArg is absent or empty, return bootRoomDir (unchanged fallback); otherwise resolve it with path.resolve, get `home` from listRoomRoots(); if home is falsy or missing on disk, refuse ('no rooms home to contain room_path'); if `isRealpathContained(home, resolved)` is false, refuse ('room_path is outside the rooms home'); ALSO (plan-checker W2) compute `realHome = fs.realpathSync(home)` and `realResolved = fs.realpathSync(resolved)` and refuse when `realResolved === realHome` ('room_path must be inside a room, not the rooms home itself') -- isRealpathContained alone treats the root as contained, which is correct for other callers but wrong here: this tool must expose exactly one room, and the rooms home itself is not a room; else return the resolved dir. Each of the three handlers calls it first and, on refusal, returns `{ isError: true, content: [{ type: 'text', text: <reason> }] }` without reading anything; the refusal text never echoes file contents. Update the file's JSDoc import-type comment only if needed for correctness (267-11 owns JSDoc types).
4. Add the three tools' now-real schemas to `app_views_schema_fix` in tests/fixtures/267/zod4-accepted-deltas.json (note citing RCA 2).
5. Verify: `node tests/test-267-mcpv2-app-views.cjs` (GREEN), `node tests/test-267-mcpv2-registration-api.cjs` (full mode still green), `node tests/test-267-mcpv2-zod4-contract.cjs`, `node tests/test-267-mcpv2-lockstep.cjs`, `node tests/test-267-mcpv2-cirs-gates.cjs`, `bash tests/run-all-199.sh`, `node scripts/check-release-payload-ceiling.cjs`, `node tests/test-341-shrinkwrap-no-dev.cjs`, `node lib/core/mcp-dep-heal.test.cjs`, `bash tests/run-all-198.sh` (baseline). Commit package.json, both lockfiles, cve-db.json, app-views.cjs and the fixture TOGETHER as the RCA 2 fix commit ("fix(267-10): MCP Apps input schemas on ext-apps 2.x, room_path contained (RCA 2)").
6. Resolve RCA 2 (Resolution from real output including the containment arm; status resolved; git mv to resolved/; knowledge-base block with Error patterns: schema:, inputSchema, registerAppTool, empty properties, room_path, ext-apps; Pattern lesson: fixing a dropped input can open a new trust boundary, so the fix and its guard ship together). Commit.
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-app-views.cjs && node tests/test-267-mcpv2-registration-api.cjs && node tests/test-267-mcpv2-zod4-contract.cjs && node tests/test-267-mcpv2-lockstep.cjs && node scripts/check-release-payload-ceiling.cjs && bash tests/run-all-199.sh && grep -q "^status: resolved" .planning/debug/resolved/app-views-schema-key-drops-input-schemas.md && bash tests/run-all-267.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('node_modules/@modelcontextprotocol/ext-apps/package.json')).version)"` prints a 2.x version.
    - `grep -c "^\s*schema:" lib/mcp/app-views.cjs` prints 0; `grep -c "inputSchema:" lib/mcp/app-views.cjs` prints 3; `grep -c "isRealpathContained" lib/mcp/app-views.cjs` is at least 1.
    - `node tests/test-267-mcpv2-app-views.cjs` exits 0 including the outside-path, in-home-symlink AND rooms-home-itself refusal arms (plan-checker W2) and the sentinel-absent check.
    - `grep -n "realHome\|realResolved" lib/mcp/app-views.cjs` shows the rooms-home-itself check is present alongside `isRealpathContained`, not a bare pass-through of it.
    - `npm ls react 2>/dev/null | grep -c "react@"` prints 0.
    - The fix commit contains package.json, npm-shrinkwrap.json and lib/mcp/app-views.cjs together (`git show --stat <fix sha>`).
    - RCA 2 resolved under .planning/debug/resolved/ with a knowledge-base block.
    - `bash tests/run-all-267.sh` ends FAIL=0.
  </acceptance_criteria>
  <done>MCP Apps tools take real, contained arguments on ext-apps 2.x; RCA 2 resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host (Desktop/Cowork model) -> room_path argument | a model-chosen filesystem path, newly reachable after the fix |
| npm registry -> ext-apps 2.x | third-party package in every user install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-03 | Information disclosure | room-dashboard/room-wiki/room-graph room_path (arbitrary directory .md read) | mitigate | resolveAppRoomDir refuses any room_path whose realpath is outside the rooms home (isRealpathContained, Phase 354-05 helper, follows symlinks by realpath), fails closed when no rooms home exists, never reads before the check, refusal text never echoes file content; sentinel and symlink arms in the test |
| T-267-03b (plan-checker W2) | Information disclosure | room_path equal to the rooms home realpath itself | mitigate | resolveAppRoomDir additionally refuses when the resolved realpath equals the rooms home's own realpath (not just "outside" it) -- isRealpathContained alone treats the root as contained, which would let a caller point room_path at the rooms home and have scanRoomData read every room's top-level .md files as if they were one room's sections (a cross-room read inside the trust boundary, not an escape from it, but still broader than this tool's single-room contract); dedicated test arm asserts isError true for this exact input, distinct from the outside-path arm |
| T-267-26 | Tampering | layout / section arguments | accept | layout is a closed zod enum; section is only echoed as _activeSection in the returned JSON (no path join), so it cannot steer a read |
| T-267-SC | Tampering | npm install of ext-apps 2.x | mitigate | name already VETTED; research slopcheck [OK]; re-verified repository and no install scripts; optional react peers kept out of the payload; lockstep and payload-ceiling gates |
| T-267-09 | Information disclosure | Canon Part 8 | accept | app views read LOCAL room files and return them to the LOCAL host; no Brain call exists on this path (no brain-client require in app-views.cjs) |
</threat_model>

<verification>
- App-views test GREEN including containment; registration-api full mode, zod4 contract, lockstep, CIRS, supply-chain and payload gates green.
- RCA 2 resolved.
</verification>

<success_criteria>
The three MCP Apps views accept the arguments they always claimed to, safely, on the ext-apps line that unblocked this phase.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `tests/test-267-mcpv2-app-views.cjs` | new test (MCPV2-04) | aggregator |
| `resolveAppRoomDir(roomPathArg, bootRoomDir)` in `lib/mcp/app-views.cjs` | new function (containment guard) | the three MCP Apps handlers |
| `@modelcontextprotocol/ext-apps` 2.x (manifest + both lockfiles) and its dated allowlist note | dependency change + config entry | MCP Apps on Desktop/Cowork |
| `app_views_schema_fix` entries in `tests/fixtures/267/zod4-accepted-deltas.json` | accepted-delta ledger entries | zod4 contract test |
| `.planning/debug/resolved/app-views-schema-key-drops-input-schemas.md` + knowledge-base block | resolved RCA | gsd-debugger |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-10-SUMMARY.md` when done (commit with `git add -f`).
</output>
