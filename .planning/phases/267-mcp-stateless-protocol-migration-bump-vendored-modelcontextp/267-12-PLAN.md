---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 12
type: execute
wave: 10
locked_stage: "W3 (HTTP branches): flag-OFF branch to createMcpHandler, which fixes the one-request-per-process bug by construction (RCA 1)"
depends_on: [267-11]
files_modified:
  - tests/test-267-mcpv2-http-flag-off.cjs
  - package.json
  - package-lock.json
  - npm-shrinkwrap.json
  - references/security/cve-db.json
  - bin/mindrian-mcp-server.cjs
  - .planning/debug/mcp-http-flag-off-one-request-per-process.md
  - .planning/debug/resolved/mcp-http-flag-off-one-request-per-process.md
  - .planning/debug/knowledge-base.md
autonomous: true
requirements: [MCPV2-05, MCPV2-17, MCPV2-10, MCPV2-19, MCPV2-12, MCPV2-15]
canon_parts: [7, 8, 9]
tags: [http, createMcpHandler, rca, dns-rebinding, cowork]

must_haves:
  truths:
    - "The flag-OFF HTTP branch serves any number of sequential requests: initialize, notifications/initialized, tools/list and tools/call all succeed repeatedly in one process (the pre-fix code answered exactly one request and then 500 'Stateless transport cannot be reused across requests')"
    - "It serves both eras: a 2025 client through the legacy stateless leg and a 2026-07-28 client through the modern leg, via toNodeHandler(createMcpHandler(() => createServer(), { legacy: 'stateless' })) with the already-parsed req.body passed through, the composition the retired PWS Brain ran in production against this plugin's own 2025-era client (CTX Reuse precedent)"
    - "createServer() has no side effects: the tree watcher starts once per process in main() with a per-branch target (stdio: the serveStdio singleton; HTTP flag-OFF: the handler's notify.resourcesChanged), never once per request"
    - "The loopback endpoint refuses DNS-rebinding: a request whose Host is not localhost/127.0.0.1/[::1], or whose Origin is present and not localhost, gets 403 before any MCP handling; non-browser clients (no Origin) on 127.0.0.1 are served"
    - "Per-request server construction cost is measured and bounded (every one of 10 sequential tools/list requests under 5000 ms; p50/p95 recorded)"
    - "RCA 1 is resolved with real verification output"
  artifacts:
    - path: "tests/test-267-mcpv2-http-flag-off.cjs"
      provides: "MCPV2-05 and MCPV2-17 test, RED before the fix"
      min_lines: 100
    - path: "bin/mindrian-mcp-server.cjs"
      provides: "flag-OFF createMcpHandler route with localhost Host/Origin validation; side-effect-free createServer"
      contains: "createMcpHandler("
  key_links:
    - from: "bin/mindrian-mcp-server.cjs flag-OFF /mcp route"
      to: "@modelcontextprotocol/node toNodeHandler + localhostHostValidation + localhostOriginValidation"
      via: "require inside the HTTP branch"
      pattern: "localhostHostValidation"
    - from: "tree watcher"
      to: "handler.notify.resourcesChanged"
      via: "adapter object with sendResourceListChanged"
      pattern: "notify\\.resourcesChanged"
---

<objective>
Replace the flag-OFF HTTP branch's single reused stateless transport with a per-request v2 handler, test-first, which fixes the independently broken one-request-per-process bug (RCA 1) and adds 2026-07-28 support on the one surface where Claude hosts actually negotiate it (direct HTTP).

Purpose: CTX "Flag-OFF HTTP branch: real, independent bug, fix it as part of this phase" and CTX "Reuse precedent" (the retired PWS Brain's `createMcpHandler(..., { legacy: 'stateless' })` behind `toNodeHandler`). RESEARCH Pattern 2 preconditions: hoist the tree watcher out of the factory, measure per-request build cost, keep express.json() and pass the parsed body. Whether real Cowork reaches this branch is recorded in 267-TRIPOLAR-PROBES.md (A7); the fix is required regardless.
Output: RED test, @modelcontextprotocol/node vetted and installed, fixed flag-OFF branch, resolved RCA 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-RESEARCH.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-TRIPOLAR-PROBES.md
@.planning/debug/mcp-http-flag-off-one-request-per-process.md
@bin/mindrian-mcp-server.cjs
@lib/mcp/tree-watcher.cjs

Session and scope rules (every task in this plan):
- Workspace /home/jsagi/dev/MindrianOS-Plugin only; record `PLAN_BASE=$(git rev-parse HEAD)` in the SUMMARY.
- Before editing any file run `git status --short -- <that file>`; a diff you did not make means STOP and report.
- Commit by explicit path only (`git add -f` for .planning/), `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git commit -a`, `git stash`.
- Never edit `lib/core/session-binding.cjs`, `lib/mcp/session-registry.cjs`, `lib/mcp/session-catchup.cjs`, `lib/mcp/tree-watcher.cjs` (reuse its existing target-object contract), `lib/mcp/gate-render.cjs`, `lib/core/brain-client.cjs`, `lib/core/part8-egress-guard.cjs`, `mcp-server-brain/**`, `/home/jsagi/Theo/**`.
- The flag-ON branch (session-keyed transport map, /event route, pidfile) is NOT restructured here (267-14 owns it). The only flag-ON-visible change allowed in this plan is the tree-watcher hoist, which keeps its current no-op behavior there (see Task 2).
- HTTP tests: port 3847 is fixed for flag-OFF. Assert it is free before each case (if a process this test did not spawn holds it, exit 77 ENV GAP naming the PID; never kill it); SIGKILL only spawned PIDs in finally; assert free after.
- Every npm install: `npm install --ignore-scripts --no-audit --no-fund ...`.
- No em-dashes. CJS only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flag-OFF multi-request, dual-era, rebinding and latency test (RED)</name>
  <files>tests/test-267-mcpv2-http-flag-off.cjs</files>
  <read_first>
    - bin/mindrian-mcp-server.cjs lines 249-440 (HTTP branch: express.json() at :266, flag-OFF stateless transport at :318, route at :321-336, listen at :403-433)
    - lib/mcp/surface-detect.cjs (MINDRIAN_TRANSPORT=http selects HTTP; unset MINDRIAN_MCP_FIRST keeps flag-OFF)
    - .planning/debug/mcp-http-flag-off-one-request-per-process.md (Evidence reproduction)
    - 267-RESEARCH.md Pitfalls 9 and 10
  </read_first>
  <behavior>
    - Legacy 2025 over raw HTTP POSTs (Accept: application/json, text/event-stream; Content-Type: application/json; parse JSON or SSE `data:` frames): initialize 200; notifications/initialized 202 or 200; tools/list 200 with a tool count equal to the zod4 snapshot count; tools/call contract_version 200; then 5 more tools/list, each 200. RED now (second request 500).
    - 2026: v2 Client (from @modelcontextprotocol/client) with StreamableHTTPClientTransport to http://127.0.0.1:3847/mcp and versionNegotiation auto negotiates 2026-07-28 and lists the same tool names. RED now.
    - Rebinding: POST initialize with Host: evil.example:3847 gets 403; with Origin: http://evil.example gets 403; Host 127.0.0.1:3847 with no Origin gets 200; Origin http://localhost:3847 gets 200. RED now (no validation, 200 everywhere).
    - Latency: 10 sequential tools/list requests each complete under 5000 ms; print p50 and p95.
  </behavior>
  <action>
Write tests/test-267-mcpv2-http-flag-off.cjs: spawn `node bin/mindrian-mcp-server.cjs` with hermeticEnv({ MINDRIAN_TRANSPORT: 'http' }) (MINDRIAN_MCP_FIRST unset), wait for the stderr line containing `HTTP on 127.0.0.1:3847`, run the behavior arms using Node's global fetch for raw requests, kill in finally. Run it: the multi-request, 2026 and rebinding arms must FAIL. Commit (RED).
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-http-flag-off.cjs; rc=$?; test $rc -ne 0 -a $rc -ne 77 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - The test exits 1 on the pre-fix tree with the second-request arm failing on a 500 status and the rebinding arm failing on a 200 for Host evil.example.
    - Port 3847 is free after the run (`node -e` connect probe fails with ECONNREFUSED).
  </acceptance_criteria>
  <done>RED HTTP flag-OFF test committed.</done>
</task>

<task type="auto">
  <name>Task 2: Install and vet @modelcontextprotocol/node, hoist the tree watcher, mount createMcpHandler with Host/Origin validation, resolve RCA 1</name>
  <files>package.json, package-lock.json, npm-shrinkwrap.json, references/security/cve-db.json, bin/mindrian-mcp-server.cjs, .planning/debug/mcp-http-flag-off-one-request-per-process.md, .planning/debug/resolved/mcp-http-flag-off-one-request-per-process.md, .planning/debug/knowledge-base.md</files>
  <read_first>
    - 267-RESEARCH.md Pattern 2 (code shape and three preconditions) and "Supporting" row for @modelcontextprotocol/node (depends on @hono/node-server; hono peer optional)
    - node_modules/@modelcontextprotocol/server/dist/createMcpHandler-*.d.cts: CreateMcpHandlerOptions (legacy 'stateless' | 'reject', onerror, maxRequestBodySize not applied to parsedBody) and the handler's `notify` (ServerNotifier.resourcesChanged)
    - node_modules/@modelcontextprotocol/node/dist/index.d.cts after install: toNodeHandler(handler, { onerror }) returns (req, res, parsedBody?) => Promise; localhostHostValidation() and localhostOriginValidation() return (req, res) => boolean and answer 403 themselves when false
    - lib/mcp/tree-watcher.cjs lines 75-125 (startTreeWatcher(server, opts) only needs an object with sendResourceListChanged; a second call is a no-op singleton)
  </read_first>
  <action>
1. Re-check `npm view @modelcontextprotocol/node version dependencies peerDependencies peerDependenciesMeta repository.url scripts`. Require version V (the same V as server/core/client from 267-05; if npm now shows a newer family version, still install node@V to keep one family version, and record the newer version in the SUMMARY as a follow-up); peer `@modelcontextprotocol/server` range satisfied; hono peer optional; no install scripts; official repository. Install `@modelcontextprotocol/node@V`, manifest `^V`, lockstep both lockfiles as in 267-03, add a dated VETTED allowlist entry (note: toNodeHandler adapter plus localhost Host/Origin guards, depends on @hono/node-server, hono peer optional and not installed, version V, no install scripts). `npm ls hono` shows it absent or only as @hono/node-server's own dependency; record which.
2. createServer(): remove the `startTreeWatcher(s, {})` call (and its try/catch) so the factory only builds and registers. In main(): stdio branch and express-missing fallback call `startTreeWatcher(server, {})` right after serveStdio (same try/catch and log as before); HTTP flag-OFF calls `startTreeWatcher({ sendResourceListChanged: () => handler.notify.resourcesChanged() }, {})` after the handler exists; HTTP flag-ON does NOT start the watcher in this plan and gets a comment stating why (before this change its only watcher bound to the never-connected boot singleton, so it delivered nothing; 267-14 wires it to the modern handler). Build the module-level `server` singleton lazily (only on the stdio path and the express-missing fallback), so HTTP mode never constructs an unused server with registrations at boot.
3. Flag-OFF route: require `createMcpHandler` from '@modelcontextprotocol/server' and `toNodeHandler`, `localhostHostValidation`, `localhostOriginValidation` from '@modelcontextprotocol/node' inside the HTTP branch (plain require, same as the existing lazy express/transport requires). Build `const handler = createMcpHandler(() => createServer(), { legacy: 'stateless', onerror: (e) => process.stderr.write('[mindrian-os] mcp handler: ' + e.message + '\n') })`, `const nodeHandler = toNodeHandler(handler, { onerror: same })`, and the two guards once. Route: `app.all('/mcp', ...)` runs the Host guard, then the Origin guard (return if either returns false), then `nodeHandler(req, res, req.body)` (keep `app.use(express.json())`: its 100 KB limit applies first and is stricter than the SDK's 4 MiB default, which does not apply to a passed parsedBody). Delete the flag-OFF `statelessTransport` construction and its `server.connect(statelessTransport)`. Keep the flag-ON code path byte-identical except that it no longer shares variables with the removed flag-OFF ones.
4. Verify: `node tests/test-267-mcpv2-http-flag-off.cjs` GREEN (record p50/p95 in the SUMMARY), `node tests/test-267-mcpv2-dual-era.cjs` (stdio unaffected by the hoist), tree-watcher tests (`grep -l tree-watcher tests/*.cjs`, all at baseline), `node tests/test-248-surface-probes.cjs` and `bash tests/run-all-198.sh` (flag-ON legs unchanged), `node tests/test-267-mcpv2-lockstep.cjs`, `bash tests/run-all-199.sh`, `node scripts/check-release-payload-ceiling.cjs`, `node tests/test-341-shrinkwrap-no-dev.cjs`, `node tests/test-266-connect-path-process-budget.cjs`, `grep -rn "createRequestStateCodec\|inputRequired(" bin lib | wc -l` prints 0. Commit deps+lockfiles+allowlist, then bin/mindrian-mcp-server.cjs as the RCA 1 fix commit ("fix(267-12): flag-OFF HTTP serves every request via createMcpHandler (RCA 1)").
5. Resolve RCA 1 (Resolution from real output: request-by-request statuses before and after, both eras, the rebinding arm; note A7 status from 267-TRIPOLAR-PROBES.md; status resolved; git mv; knowledge-base block with Error patterns: Stateless transport cannot be reused, 500 on second request, sessionIdGenerator undefined, flag-OFF, Cowork HTTP; Pattern lesson: a stateless transport is per request by definition, so share the factory, never the transport). Commit.
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-http-flag-off.cjs && node tests/test-267-mcpv2-dual-era.cjs && node tests/test-248-surface-probes.cjs && node tests/test-267-mcpv2-lockstep.cjs && bash tests/run-all-199.sh && node scripts/check-release-payload-ceiling.cjs && grep -q "^status: resolved" .planning/debug/resolved/mcp-http-flag-off-one-request-per-process.md && bash tests/run-all-267.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "statelessTransport" bin/mindrian-mcp-server.cjs` prints 0; `grep -c "createMcpHandler(" bin/mindrian-mcp-server.cjs` is at least 1; `grep -c "localhostHostValidation()" bin/mindrian-mcp-server.cjs` is at least 1.
    - Inside `function createServer`, `startTreeWatcher` no longer appears (`awk '/^function createServer/,/^}/' bin/mindrian-mcp-server.cjs | grep -c startTreeWatcher` prints 0).
    - `node tests/test-267-mcpv2-http-flag-off.cjs` exits 0: 7+ sequential legacy requests all 2xx, 2026 negotiated, rebinding 403s, latency bound met.
    - run-all-198 and test-248 surface probes equal 267-BASELINE.md.
    - `@modelcontextprotocol/node` has a VETTED allowlist entry and appears in package.json and both lockfiles at V.
    - RCA 1 resolved under .planning/debug/resolved/ with a knowledge-base block.
  </acceptance_criteria>
  <done>Flag-OFF HTTP fixed by construction, both eras served, rebinding refused, RCA 1 resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any local process or browser page -> 127.0.0.1:3847/mcp | loopback HTTP; a browser page can reach it through DNS rebinding |
| MCP client -> per-request server instances | each request gets a fresh McpServer from the same factory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-04 | Spoofing / Elevation of privilege | DNS rebinding against loopback /mcp (a web page invoking room write tools) | mitigate | localhostHostValidation + localhostOriginValidation before any MCP handling; rebinding arm in the test (Host and Origin cases) |
| T-267-05 | Denial of service | request body size / batch | mitigate | express.json() 100 KB limit runs first (unchanged, stricter than the SDK default); SDK batch bound present in V |
| T-267-06 | Denial of service | per-request factory cost, watcher leak | mitigate | side-effect-free createServer (grep gate), watcher started once in main(), latency bound arm with p50/p95 recorded |
| T-267-07 | Spoofing | session identity on flag-OFF HTTP | accept | stateless by design (as before the fix): ctx.sessionId is undefined per request, so identity resolves from the process env exactly like stdio; gate ledger entries minted and consumed without a transport session behave as on stdio; loopback-only plus Host/Origin guards bound who can reach it |
| T-267-SC | Tampering | npm install of @modelcontextprotocol/node | mitigate | same family version V, official repo, no install scripts, dated VETTED entry, lockstep and payload gates |
| T-267-11 | Tampering | requestState / MRTR | mitigate | grep gate zero createRequestStateCodec / inputRequired( |
</threat_model>

<verification>
- HTTP flag-OFF test green; dual-era, surface probes, run-all-198, lockstep, supply-chain and payload gates green.
- RCA 1 resolved.
</verification>

<success_criteria>
The Cowork-routed HTTP branch serves a whole conversation instead of one request, in both protocol eras, and is closed to browser-origin rebinding.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `tests/test-267-mcpv2-http-flag-off.cjs` | new test (MCPV2-05, MCPV2-17) | aggregator |
| `@modelcontextprotocol/node` at V (manifest, both lockfiles) and its VETTED entry | new dependency + config entry | HTTP branches |
| flag-OFF `/mcp` route: `createMcpHandler(() => createServer(), { legacy: 'stateless' })` + Host/Origin guards | new HTTP behavior | Cowork |
| side-effect-free `createServer()`; per-branch `startTreeWatcher` targets; lazy stdio singleton | refactor | 267-13, 267-14 |
| `.planning/debug/resolved/mcp-http-flag-off-one-request-per-process.md` + knowledge-base block | resolved RCA | gsd-debugger |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-12-SUMMARY.md` when done (commit with `git add -f`).
</output>
