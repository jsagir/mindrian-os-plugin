---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 07
type: execute
wave: 5
locked_stage: "W2 (local server stdio branch): gate.cjs stale-premise fix (RCA 3) plus registration rewrites of gate, chain, claim-verify, claim (7 sites)"
depends_on: [267-06, 267-02, 267-04]
files_modified:
  - tests/test-267-mcpv2-gate-premise.cjs
  - lib/mcp/tools/gate.cjs
  - lib/mcp/tools/chain.cjs
  - lib/mcp/tools/claim-verify.cjs
  - lib/mcp/tools/claim.cjs
  - .planning/debug/gate-elicitation-premise-stale-comment.md
  - .planning/debug/resolved/gate-elicitation-premise-stale-comment.md
  - .planning/debug/knowledge-base.md
autonomous: true
requirements: [MCPV2-07, MCPV2-08, MCPV2-02, MCPV2-15]
canon_parts: [3, 11]
tags: [gate, elicitation, rca, registration-rewrite]

must_haves:
  truths:
    - "The detectClientCapabilities comment in lib/mcp/tools/gate.cjs states the current, verified fact: Claude Code declares elicitation (2.1.280, live wire tee 2026-09-23), so gate rung (a) inline elicitInput is the live CLI gate path; Desktop and Cowork are as recorded in 267-TRIPOLAR-PROBES.md; a 2026-07-28 connection returns undefined capabilities and falls to rung (b)/(c)"
    - "Gate ladder BEHAVIOR is unchanged: elicitation declared -> rung (a); no capability object -> rung (b)/(c). Nothing suppresses or forces rung (b) (CTX 'Elicitation gate UX': let elicitation take over on CLI)"
    - "A premise test fails against the stale comment and passes after the fix (test-first for a bug fix, CTX-TESTFIRST)"
    - "gate.cjs (2), chain.cjs (2), claim-verify.cjs (2) and claim.cjs (1) use registerTool config objects, one commit per file, CIRS gate green after each"
    - "RCA 3 is resolved: moved to .planning/debug/resolved/ with Resolution filled from real command output, and a knowledge-base.md block added"
  artifacts:
    - path: "tests/test-267-mcpv2-gate-premise.cjs"
      provides: "MCPV2-07 premise + ladder behavior pin"
      min_lines: 40
    - path: "lib/mcp/tools/gate.cjs"
      provides: "current elicitation premise comment; 2 registerTool registrations"
      contains: "2.1.280"
    - path: ".planning/debug/resolved/gate-elicitation-premise-stale-comment.md"
      provides: "resolved RCA 3"
      contains: "status: resolved"
  key_links:
    - from: "lib/mcp/tools/gate.cjs detectClientCapabilities"
      to: "server.server.getClientCapabilities()"
      via: "unchanged capability read; v2 keeps it on 2025-era connections"
      pattern: "getClientCapabilities"
---

<objective>
Fix the stale elicitation premise in gate.cjs test-first (RCA 3), then move the four gate/chain/claim registrars to `registerTool`, one commit per file.

Purpose: CTX "Elicitation gate UX (navigator-approved 2026-09-23)": let elicitation take over on CLI; update the stale comment at gate.cjs:145-152; file and resolve an RCA; do not suppress or force rung (b). CTX "Scope and sequencing": the bug is fixed in the same wave as the file it lives in, not a separate bug-fix wave. The MRTR / requestState rework of lib/mcp/gate-render.cjs rung (a) is OUT of scope (CTX Deferred): gate-render.cjs is not touched.
Output: premise test (RED then GREEN), corrected comment, 7 migrated sites, resolved RCA 3.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-RESEARCH.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-TRIPOLAR-PROBES.md
@.planning/debug/gate-elicitation-premise-stale-comment.md
@lib/mcp/tools/gate.cjs

Session and scope rules (every task in this plan):
- Workspace /home/jsagi/dev/MindrianOS-Plugin only; record `PLAN_BASE=$(git rev-parse HEAD)` in the SUMMARY.
- Before editing any file run `git status --short -- <that file>`; a diff you did not make means STOP and report.
- Commit by explicit path only (`git add -f` for .planning/ paths, including the RCA move), `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git commit -a`, `git stash`.
- Never edit `lib/mcp/gate-render.cjs` (no MRTR rework, CTX Deferred), `lib/mcp/gate-ledger.cjs`, `mcp-server-brain/**`, `/home/jsagi/Theo/**`, `lib/core/brain-client.cjs`, `lib/core/part8-egress-guard.cjs`, `lib/core/session-binding.cjs`.
- Registration rewrite rule: identical to 267-06's context block (verbatim DESC and SHAPE inside `z.object(...)`, untouched callback, mechanical Title Case TITLE from the tool name, no strictness change, connectors exports untouched).
- Per-commit gate after EVERY registrar commit: `node tests/test-267-mcpv2-registration-api.cjs --file <file>` and `node tests/test-267-mcpv2-cirs-gates.cjs` and `node tests/test-267-mcpv2-zod4-contract.cjs`.
- No em-dashes. CJS only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Premise test RED, then correct the gate.cjs comment (RCA 3 fix commit)</name>
  <files>tests/test-267-mcpv2-gate-premise.cjs, lib/mcp/tools/gate.cjs</files>
  <read_first>
    - lib/mcp/tools/gate.cjs lines 120-175 (the comment block at :145-152 and detectClientCapabilities, CLAUDE_HOST_SURFACES) and its `_internal` exports at the bottom
    - .planning/debug/gate-elicitation-premise-stale-comment.md (Required Code Changes)
    - 267-TRIPOLAR-PROBES.md (the recorded CLI, Desktop and Cowork facts to cite; if Desktop/Cowork are deferred, the comment says "unprobed")
    - tests/test-198-gate-renderers.test.cjs and tests/test-265-gate-render-elicit-schema.cjs (existing ladder tests to keep green)
  </read_first>
  <behavior>
    - Source arm: the comment above detectClientCapabilities no longer contains the phrase "do not declare it" and does contain `elicitation` together with `2.1.280`. RED now.
    - Behavior arm (pins, already green): detectClientCapabilities(fakeServer returning {elicitation:{}}, {surface:'cli'}) reports elicitation true; with getClientCapabilities returning undefined it reports false; with a throwing getClientCapabilities it reports false.
  </behavior>
  <action>
1. Write tests/test-267-mcpv2-gate-premise.cjs implementing the behavior list; reach detectClientCapabilities through whatever the module exports (read the bottom of gate.cjs for `_internal`; if it is not exported, test through the exported register path with a fake server, never by editing exports just for the test). Run it: the source arm must FAIL. Commit the test (RED).
2. Rewrite ONLY the JSDoc/comment block above detectClientCapabilities (gate.cjs:145-152 at plan time) to state, in plain words: capability comes from the MCP initialize handshake; as of Claude Code 2.1.280 (live wire tee, 2026-09-23) the CLI declares `elicitation: {}`, so rung (a), the inline elicitInput round trip, is the live CLI gate path by navigator ruling (CTX 2026-09-23, "let elicitation take over on CLI"); Desktop and Cowork per 267-TRIPOLAR-PROBES.md (or "unprobed as of <date>"); on a 2026-07-28 connection getClientCapabilities() returns undefined, so the ladder falls to rung (b)/(c); CLAUDE_HOST_SURFACES still identifies Claude hosts for rung (b). No code line changes.
3. Run the premise test (GREEN), `node tests/test-198-gate-renderers.test.cjs`, `node tests/test-265-gate-render-elicit-schema.cjs`. Commit gate.cjs alone as the RCA 3 fix commit ("fix(267-07): gate elicitation premise is current (RCA 3)").
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-gate-premise.cjs && node tests/test-198-gate-renderers.test.cjs && node tests/test-265-gate-render-elicit-schema.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `git show <test commit>` exists before `git show <fix commit>` in `git log --oneline $PLAN_BASE..HEAD` order (RED committed first).
    - `grep -c "do not declare it" lib/mcp/tools/gate.cjs` prints 0; `grep -c "2.1.280" lib/mcp/tools/gate.cjs` is at least 1.
    - `git diff <fix commit>~1 <fix commit> -- lib/mcp/tools/gate.cjs | grep "^[-+]" | grep -v "^[-+]\s*\(//\|\*\|/\*\*\)" | grep -v "^[-+][-+]" | wc -l` prints 0 (comment-only commit).
    - All three verify commands exit 0.
  </acceptance_criteria>
  <done>Premise test committed RED then GREEN; comment corrected in a comment-only commit.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite gate.cjs, chain.cjs, claim-verify.cjs, claim.cjs registrations (one commit each) and resolve RCA 3</name>
  <files>lib/mcp/tools/gate.cjs, lib/mcp/tools/chain.cjs, lib/mcp/tools/claim-verify.cjs, lib/mcp/tools/claim.cjs, .planning/debug/gate-elicitation-premise-stale-comment.md, .planning/debug/resolved/gate-elicitation-premise-stale-comment.md, .planning/debug/knowledge-base.md</files>
  <read_first>
    - lib/mcp/tools/gate.cjs registrations at :179 (gate_render) and :236 (gate_answer)
    - lib/mcp/tools/chain.cjs registrations at :775 (chain_resolve) and :788 (chain_run)
    - lib/mcp/tools/claim-verify.cjs registrations at :62 (claim_verify) and :133 (claim_read)
    - lib/mcp/tools/claim.cjs registration at :100 (claim_write)
    - .planning/debug/knowledge-base.md top 40 lines (newest-first block format: slug heading, Date, Error patterns, Root cause, Fix, Verification, Files changed, Pattern lesson)
  </read_first>
  <action>
1. For each file in order gate.cjs, chain.cjs, claim-verify.cjs, claim.cjs: check `git status --short -- <file>` clean; apply the rewrite rule to every `server.tool(` site in it (line numbers above are plan-time anchors); run the per-commit gate plus the file's own tests: gate -> test-198-gate-renderers, test-265-gate-render-elicit-schema, test-267-mcpv2-gate-premise; chain -> test-198-chain-run-halt.test.cjs plus any `tests/test-*chain*` files that PASSED in 267-BASELINE.md; claim-verify and claim -> test-276-claim-write-primitive.cjs, test-276-meeting-gate-wiring.cjs and any `tests/*claim*` files that PASSED at baseline. Commit each file alone.
2. After the four commits: `bash tests/run-all-198.sh` equals 267-BASELINE.md leg set.
3. Resolve RCA 3: fill Resolution (root_cause confirmed; fix: the comment-only commit sha from Task 1; verification: paste the three Task 1 verify outputs; files_changed; commits), set `status: resolved`, `updated:` now; `git mv` it to .planning/debug/resolved/ (use `git add -f` if needed); prepend a knowledge-base.md block (after the header, before the newest existing block) with slug `gate-elicitation-premise-stale-comment`, Error patterns (elicitation, getClientCapabilities, rung (a), AskUserQuestion, 2.1.280, stale comment), Root cause, Fix, Verification, Files changed, and the Pattern lesson (a comment that encodes a host's behavior goes stale when the host ships; cite the version and date inside such comments). Commit the RCA move and knowledge-base together.
  </action>
  <verify>
    <automated>for f in gate chain claim-verify claim; do node tests/test-267-mcpv2-registration-api.cjs --file lib/mcp/tools/$f.cjs || exit 1; done; node tests/test-267-mcpv2-cirs-gates.cjs && node tests/test-267-mcpv2-zod4-contract.cjs && node tests/test-198-gate-renderers.test.cjs && test -f .planning/debug/resolved/gate-elicitation-premise-stale-comment.md && grep -q "^status: resolved" .planning/debug/resolved/gate-elicitation-premise-stale-comment.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "server\.tool(" lib/mcp/tools/gate.cjs lib/mcp/tools/chain.cjs lib/mcp/tools/claim-verify.cjs lib/mcp/tools/claim.cjs` prints 0 for each file.
    - `git log --oneline $PLAN_BASE..HEAD -- lib/mcp/tools/chain.cjs | wc -l` is 1 (same for claim-verify.cjs and claim.cjs; gate.cjs has 2: the comment fix and the registration rewrite).
    - `node tests/test-267-mcpv2-cirs-gates.cjs` exited 0 after every one of the four registrar commits (SUMMARY records each).
    - `test ! -f .planning/debug/gate-elicitation-premise-stale-comment.md` and the resolved copy has `status: resolved` and a commits line with real shas.
    - `head -20 .planning/debug/knowledge-base.md | grep -c "gate-elicitation-premise-stale-comment"` is 1.
    - `git diff $PLAN_BASE -- lib/mcp/gate-render.cjs lib/mcp/gate-ledger.cjs | wc -l` prints 0.
  </acceptance_criteria>
  <done>19 of 51 sites migrated; RCA 3 resolved with evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host -> gate_render / gate_answer | a human decision gate; the answer must be the human's, single-use, session-scoped |
| MCP host capability declaration -> gate ladder choice | the host's `elicitation` capability selects rung (a) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-08 | Spoofing / Repudiation | gate ledger (mint/consume) | mitigate | gate-ledger.cjs untouched (diff gate); single-use, 30-minute TTL and session keying pinned by test-198-gate-renderers, run after each gate commit |
| T-267-23 | Tampering | gate ladder rung selection | mitigate | behavior arm of the premise test pins elicitation-declared -> rung (a), undefined -> rung (b)/(c); the fix is comment-only (diff gate), so no rung is forced or suppressed (CTX ruling) |
| T-267-11 | Tampering | requestState / MRTR | accept | not introduced (CTX Deferred); gate-render.cjs untouched; if MRTR ever lands it must use createRequestStateCodec with opaque handles only (RESEARCH Pitfall 5) |
| T-267-21 | Elevation of privilege | CIRS declarations | mitigate | per-commit cirs gate |
</threat_model>

<verification>
- Premise test, gate renderer and elicit-schema tests green.
- Per-file registration-API checks green for the four files; cirs gate green per commit.
- RCA 3 in resolved/ with a knowledge-base block.
</verification>

<success_criteria>
The gate path's documentation matches the live host, its behavior is pinned, and the gate/chain/claim registrars are on the v2-compatible API.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `tests/test-267-mcpv2-gate-premise.cjs` | new test (MCPV2-07) | aggregator |
| corrected detectClientCapabilities comment in `lib/mcp/tools/gate.cjs` | doc fix (RCA 3) | maintainers |
| `title` on gate_render, gate_answer, chain_resolve, chain_run, claim_verify, claim_read, claim_write | new wire field | host tool pickers |
| `.planning/debug/resolved/gate-elicitation-premise-stale-comment.md` + knowledge-base block | resolved RCA | gsd-debugger |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-07-SUMMARY.md` when done (commit with `git add -f`).
</output>
