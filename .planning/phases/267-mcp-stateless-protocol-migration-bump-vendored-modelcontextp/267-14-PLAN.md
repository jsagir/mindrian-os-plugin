---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 14
type: execute
wave: 12
locked_stage: "W3 (HTTP branches): flag-ON daemon stays sessionful and 2025-era via isLegacyRequest routing to a v2 sessionful transport; 2026 requests go to createMcpHandler({ legacy: 'reject' })"
depends_on: [267-13]
files_modified:
  - tests/test-267-mcpv2-flag-on.cjs
  - bin/mindrian-mcp-server.cjs
  - tests/test-267-mcpv2-sdk-era.cjs
autonomous: true
requirements: [MCPV2-06, MCPV2-17]
canon_parts: [3, 9, 11]
tags: [flag-on, daemon, isLegacyRequest, session-binding, dns-rebinding]

must_haves:
  truths:
    - "The flag-ON daemon keeps its session-keyed transport map and its room binding exactly as today for 2025-era traffic (legacy requests, including body-less GET/DELETE session operations), now on the v2 NodeStreamableHTTPServerTransport with the same sessionIdGenerator/onsessioninitialized/onsessionclosed wiring and session-registry calls (CTX: 'Flag-ON daemon path stays sessionful, 2025-era, unchanged in architecture'; icm-architect consult)"
    - "Requests that isLegacyRequest classifies as modern (2026-07-28 envelope claims) go to createMcpHandler(() => createServer(), { legacy: 'reject' }); nothing modern is ever sent to the legacy path (the SDK's routing contract)"
    - "/mcp and /event refuse non-localhost Host and non-localhost Origin with 403 on the daemon too"
    - "The daemon's tree watcher targets the modern handler's notify.resourcesChanged (legacy sessions keep today's behavior: no tree notifications)"
    - "bin/mindrian-mcp-server.cjs requires nothing from @modelcontextprotocol/sdk; the sdk-era test's TEMPORARY ALLOWANCE for it is deleted"
    - "No re-architecture of room binding: lib/core/session-binding.cjs and lib/mcp/session-registry.cjs are untouched; the ICM-native per-request identity handle is a follow-up seed, not work here"
  artifacts:
    - path: "tests/test-267-mcpv2-flag-on.cjs"
      provides: "MCPV2-06 and MCPV2-17 daemon routing test, RED before the change"
      min_lines: 90
    - path: "bin/mindrian-mcp-server.cjs"
      provides: "isLegacyRequest routing for the flag-ON daemon"
      contains: "isLegacyRequest("
  key_links:
    - from: "bin/mindrian-mcp-server.cjs flag-ON /mcp route"
      to: "@modelcontextprotocol/node NodeStreamableHTTPServerTransport"
      via: "per-session legacy transport map keyed by mcp-session-id"
      pattern: "NodeStreamableHTTPServerTransport"
    - from: "bin/mindrian-mcp-server.cjs flag-ON /mcp route"
      to: "@modelcontextprotocol/server isLegacyRequest + @modelcontextprotocol/node toWebRequest"
      via: "classification with the already-parsed body"
      pattern: "toWebRequest\\(req, req\\.body\\)"
---

<objective>
Route the flag-ON daemon's traffic by protocol era: 2025-era requests keep the existing sessionful, session-keyed path (now on v2's Node transport), 2026-07-28 requests go to a modern-only per-request handler. Then no server code in the repo imports the v1 SDK.

Purpose: CTX "Flag-ON daemon path stays sessionful, 2025-era, unchanged in architecture... Route it through the guide's supported isLegacyRequest(request) + createMcpHandler(factory, { legacy: 'reject' }) pattern so the existing session-keyed lib/core/session-binding.cjs binding keeps working exactly as today." createMcpHandler alone would silently kill session-keyed room binding (RESEARCH anti-pattern), which is why the legacy leg keeps its own transport map. MINDRIAN_MCP_FIRST is default OFF, so this is low-traffic code, but test-198 concurrency, parity-198 and test-248 surface probes pin it.
Output: RED routing test, the routed daemon, the removed temporary allowance.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-RESEARCH.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md
@bin/mindrian-mcp-server.cjs
@tests/test-248-surface-probes.cjs
@tests/test-198-concurrency-mcp.test.cjs

Session and scope rules (every task in this plan):
- Workspace /home/jsagi/dev/MindrianOS-Plugin only; record `PLAN_BASE=$(git rev-parse HEAD)` in the SUMMARY.
- Before editing any file run `git status --short -- <that file>`; a diff you did not make means STOP and report.
- Commit by explicit path only, `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git commit -a`, `git stash`.
- Never edit `lib/core/session-binding.cjs`, `lib/mcp/session-registry.cjs`, `lib/mcp/daemon-lifecycle.cjs`, `lib/mcp/sse-event-bus.cjs`, `lib/mcp/mcp-first-flag.cjs`, `lib/mcp/gate-render.cjs`, `lib/core/brain-client.cjs`, `mcp-server-brain/**`, `/home/jsagi/Theo/**`. Do not re-architect room identity (CTX, icm-architect: a follow-up seed).
- Do not touch the flag-OFF route built in 267-12 or the lifecycle code from 267-13 beyond appending the modern handler to `mcpHandlers`.
- Daemon tests use a hermetic MINDRIAN_ROOMS_HOME (pidfile and session files land in the temp dir) and SIGKILL only spawned PIDs.
- No em-dashes. CJS only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flag-ON routing test (RED)</name>
  <files>tests/test-267-mcpv2-flag-on.cjs</files>
  <read_first>
    - bin/mindrian-mcp-server.cjs flag-ON block (session map, 400 on unknown session id, per-session createServer, /event route, discoverPort + pidfile)
    - tests/test-248-surface-probes.cjs (the Cowork-equivalent recipe: MINDRIAN_TRANSPORT=http + MINDRIAN_MCP_FIRST=cowork, two concurrent sessions, room_bind isolation, stale-pointer fixture)
    - lib/mcp/daemon-lifecycle.cjs (readPidfile to discover the port)
  </read_first>
  <behavior>
    - Legacy: two concurrent v2 Clients (default options, 2025) against the daemon's /mcp each receive a distinct mcp-session-id; room_bind to room-x in session A, then room_state_bound in A resolves room-x while a fresh unbound session B does not (the test-248 isolation recipe, minimal); a session file for A exists under <rooms home>/.rooms/sessions/.
    - Legacy: a request carrying an unknown mcp-session-id gets 400 (existing behavior preserved).
    - Modern: a v2 Client with versionNegotiation auto negotiates 2026-07-28 against the daemon and lists the same tool names as the legacy session. RED now.
    - Rebinding: POST /mcp with Host evil.example gets 403; GET /event with Host evil.example gets 403; Host 127.0.0.1:<port> without Origin is served. RED now.
  </behavior>
  <action>Write tests/test-267-mcpv2-flag-on.cjs: spawn `node bin/mindrian-mcp-server.cjs` with hermeticEnv({ MINDRIAN_TRANSPORT: 'http', MINDRIAN_MCP_FIRST: 'cowork' }), create room-x and room-y folders plus registry entries the way test-248 does (reuse its fixture helper if exported, else copy the minimal setup), read the port from the pidfile, implement the behavior arms with `Client` and `StreamableHTTPClientTransport` from '@modelcontextprotocol/client' and raw fetch for header cases. Run: the modern and rebinding arms FAIL; legacy arms PASS (today's behavior). Commit (RED).</action>
  <verify>
    <automated>node tests/test-267-mcpv2-flag-on.cjs; rc=$?; test $rc -ne 0 -a $rc -ne 77 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - Exits 1 on the current tree with the modern and rebinding arms failing and the legacy isolation and 400 arms passing.
    - No daemon started by the test survives; the temp pidfile is gone or its PID is dead after the run.
  </acceptance_criteria>
  <done>RED routing test committed.</done>
</task>

<task type="auto">
  <name>Task 2: Era routing on the daemon, v2 sessionful transport, guards, watcher target; drop the last v1 server import</name>
  <files>bin/mindrian-mcp-server.cjs, tests/test-267-mcpv2-sdk-era.cjs</files>
  <read_first>
    - 267-RESEARCH.md Pattern 3 and Anti-Patterns ("createMcpHandler on the flag-ON daemon path silently kills session-keyed room binding")
    - node_modules/@modelcontextprotocol/server/dist/createMcpHandler-*.d.cts: isLegacyRequest(request, parsedBody?, options?) semantics (true for claim-less POSTs incl. initialize, body-less GET/DELETE, legacy batches; false traffic must go to the modern handler, never to legacy)
    - node_modules/@modelcontextprotocol/node/dist/index.d.cts: NodeStreamableHTTPServerTransport options (sessionIdGenerator, onsessioninitialized, onsessionclosed) and handleRequest(req, res, parsedBody); toWebRequest(req, parsedBody)
  </read_first>
  <action>
1. In the flag-ON branch: replace `require('@modelcontextprotocol/sdk/server/streamableHttp.js')`'s `StreamableHTTPServerTransport` with `NodeStreamableHTTPServerTransport` from '@modelcontextprotocol/node', keeping constructor options, `onsessioninitialized` (sessionTransports.set + sessionRegistry.open), `onsessionclosed` (delete + sessionRegistry.close), `onclose`, the per-session `createServer()` + `connect`, the 400 on unknown session id, and `handleRequest(req, res, req.body)` exactly as they are. Wrap that existing logic in a local async function `serveLegacySession(req, res)`.
2. Build once: `const modernHandler = createMcpHandler(() => createServer(), { legacy: 'reject', onerror })`, `const modernNode = toNodeHandler(modernHandler, { onerror })`; push modernHandler into the module-level `mcpHandlers` (267-13 shutdown seam). New flag-ON `/mcp` route: run the Host guard and Origin guard (same `localhostHostValidation()`/`localhostOriginValidation()` instances as flag-OFF, hoisted so both branches share them), then `const webReq = await toWebRequest(req, req.body)`; `if (await isLegacyRequest(webReq, req.body)) return serveLegacySession(req, res);` else `return modernNode(req, res, req.body);`. Add the same two guards to `app.get('/event', ...)` before `sseEventBus.subscribe(res)`.
3. Tree watcher for flag-ON: `startTreeWatcher({ sendResourceListChanged: () => modernHandler.notify.resourcesChanged() }, {})` with the same try/catch as the other branches; replace the 267-12 "why not yet" comment.
4. Delete the now-unused v1 require; confirm `grep -v "^\s*//\|^\s*\*" bin/mindrian-mcp-server.cjs | grep -c "@modelcontextprotocol/sdk"` prints 0. In tests/test-267-mcpv2-sdk-era.cjs delete the TEMPORARY ALLOWANCE added by 267-11.
5. Verify, compared with 267-BASELINE.md: `node tests/test-267-mcpv2-flag-on.cjs` (GREEN), `node tests/test-198-concurrency-mcp.test.cjs`, `bash tests/parity-198.sh` (if present at baseline), `bash tests/run-all-198.sh`, `node tests/test-248-surface-probes.cjs`, `node tests/test-354-concurrency-surfaces.cjs`, `node tests/test-267-mcpv2-lifecycle.cjs` (flag-ON SIGTERM arm still exits and clears the pidfile), `node tests/test-267-mcpv2-http-flag-off.cjs`, `node tests/test-267-mcpv2-sdk-era.cjs`, `grep -rn "createRequestStateCodec\|inputRequired(" bin lib | wc -l` prints 0, `git diff $PLAN_BASE -- lib/core/session-binding.cjs lib/mcp/session-registry.cjs | wc -l` prints 0. Commit both files together.
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-flag-on.cjs && node tests/test-198-concurrency-mcp.test.cjs && node tests/test-248-surface-probes.cjs && node tests/test-267-mcpv2-lifecycle.cjs && node tests/test-267-mcpv2-http-flag-off.cjs && node tests/test-267-mcpv2-sdk-era.cjs && bash tests/run-all-267.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isLegacyRequest(" bin/mindrian-mcp-server.cjs` is at least 1; `grep -c "NodeStreamableHTTPServerTransport" bin/mindrian-mcp-server.cjs` is at least 1; `grep -c "legacy: 'reject'" bin/mindrian-mcp-server.cjs` is 1.
    - `grep -v "^\s*//\|^\s*\*" bin/mindrian-mcp-server.cjs | grep -c "@modelcontextprotocol/sdk"` prints 0 and `grep -c "TEMPORARY ALLOWANCE" tests/test-267-mcpv2-sdk-era.cjs` prints 0.
    - `node tests/test-267-mcpv2-flag-on.cjs` exits 0 (legacy isolation, 400, modern 2026 negotiation, rebinding 403 on /mcp and /event).
    - test-198-concurrency, run-all-198, test-248 surface probes and test-354-concurrency-surfaces equal 267-BASELINE.md.
    - session-binding.cjs and session-registry.cjs unchanged since PLAN_BASE.
  </acceptance_criteria>
  <done>Flag-ON daemon routes by era with session binding intact; the local server entry point is fully on v2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local clients (shim, adapter-client, statusline) -> daemon /mcp and /event | multiple concurrent sessions on one loopback daemon |
| transport-minted session id -> room binding file | the D-02 one-namespace binding key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-07 | Spoofing | session identity on the daemon (V3) | mitigate | legacy sessions keep randomUUID ids from the transport, unknown ids get 400 (preserved and tested), session isolation arm (room_bind in A does not affect B); session-binding.cjs untouched |
| T-267-32 | Spoofing | 2026 requests on the daemon carry no transport session | accept | modern requests are stateless by protocol; they resolve room identity through the same fallback ladder as stdio; the daemon is flag-gated (default OFF) and loopback-only with Host/Origin guards; the explicit per-request identity handle is the icm-architect follow-up seed (registered by 267-18) |
| T-267-04 | Spoofing / Elevation of privilege | DNS rebinding on /mcp and /event | mitigate | localhost Host and Origin guards on both routes; rebinding arms in the test |
| T-267-33 | Tampering | misrouting modern traffic to the legacy transport | mitigate | route strictly on isLegacyRequest with the parsed body; false always goes to the modern handler (SDK contract); modern arm in the test |
| T-267-10 | Denial of service | modern handler left open on shutdown | mitigate | modern handler appended to mcpHandlers and closed by exitAfterTeardown (lifecycle flag-ON arm) |
</threat_model>

<verification>
- Flag-ON routing test green; concurrency, parity, surface probes, lifecycle and flag-OFF tests green; run-all-198 at baseline.
- No v1 import in bin/mindrian-mcp-server.cjs.
</verification>

<success_criteria>
The daemon keeps today's session-keyed room binding for every existing client and answers 2026-era clients correctly, with no identity re-architecture.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `tests/test-267-mcpv2-flag-on.cjs` | new test (MCPV2-06, MCPV2-17) | aggregator |
| `serveLegacySession(req, res)` and the isLegacyRequest route in `bin/mindrian-mcp-server.cjs` | new function / routing | flag-ON daemon |
| Host/Origin guards on the daemon's `/mcp` and `/event` | new security behavior | statusline, shim, adapter-client |
| removal of the sdk-era TEMPORARY ALLOWANCE | test ledger change | Arm C |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-14-SUMMARY.md` when done (commit with `git add -f`).
</output>
