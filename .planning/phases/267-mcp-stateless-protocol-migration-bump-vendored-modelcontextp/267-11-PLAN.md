---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 11
type: execute
wave: 9
locked_stage: "W2 (local server stdio branch): the swap, v2 McpServer plus serveStdio on the stdio branch; HTTP branches untouched until W3"
depends_on: [267-10, 267-04]
files_modified:
  - tests/test-267-mcpv2-dual-era.cjs
  - bin/mindrian-mcp-server.cjs
  - lib/mcp/resources.cjs
  - lib/mcp/tool-router.cjs
  - lib/mcp/prompts.cjs
  - lib/mcp/capability-registry.cjs
  - lib/mcp/app-views.cjs
  - tests/test-267-mcpv2-sdk-era.cjs
autonomous: true
requirements: [MCPV2-02, MCPV2-01, MCPV2-07, MCPV2-03]
canon_parts: [3, 7, 8, 11]
tags: [serveStdio, sdk-v2, swap, dual-era, stdio]

must_haves:
  truths:
    - "The local server builds its McpServer from @modelcontextprotocol/server (v2) and serves stdio (and the express-missing fallback) through serveStdio from @modelcontextprotocol/server/stdio; the HTTP branch code is unchanged in this plan (plan-time probe: a v2 McpServer serves correctly over the v1 sessionful StreamableHTTPServerTransport, including per-session ctx.sessionId)"
    - "Over stdio a default v2 client negotiates 2025-11-25 and an auto-negotiating v2 client negotiates 2026-07-28; in both eras instructions are delivered and tools, prompts, resources and templates match the snapshot (plus only the accepted deltas); every tool has a title"
    - "On a 2025-era connection whose client declares elicitation, gate_render uses rung (a) (the client receives an elicitation request); on a 2026-07-28 connection it does not and falls to rung (b)/(c): the navigator's 'let elicitation take over on CLI' holds under v2 and no MRTR code is introduced"
    - "The adoption is future-proofing on stdio, not a CLI behavior change (CTX: Claude Code 2.1.280 opens stdio with a plain 2025-11-25 initialize), and it does not proceed if 267-TRIPOLAR-PROBES.md shows Desktop opening 2026 on stdio without a recorded navigator ruling"
    - "The doctor mcp-surface organ (hand-rolled 2024-11-05 handshake with stdin closed immediately), the connect-path heal census (at least 4 connect-path heal calls in the server entry) and every suite in 267-BASELINE.md stay at baseline"
  artifacts:
    - path: "tests/test-267-mcpv2-dual-era.cjs"
      provides: "MCPV2-02 dual-era wire test incl. the elicitation arm, RED before the swap"
      min_lines: 90
    - path: "bin/mindrian-mcp-server.cjs"
      provides: "v2 McpServer + serveStdio stdio branch"
      contains: "serveStdio(() => server)"
  key_links:
    - from: "bin/mindrian-mcp-server.cjs"
      to: "@modelcontextprotocol/server"
      via: "requireWithHeal on the connect path (McpServer) and '@modelcontextprotocol/server/stdio' (serveStdio)"
      pattern: "requireWithHeal\\('@modelcontextprotocol/server"
    - from: "lib/mcp/resources.cjs"
      to: "@modelcontextprotocol/server ResourceTemplate"
      via: "require"
      pattern: "ResourceTemplate"
---

<objective>
Swap the local server's stdio branch to the v2 SDK: v2 McpServer everywhere createServer() builds one, serveStdio for stdio, and prove both protocol eras on the wire, including the elicitation gate path.

Purpose: CTX "Scope and sequencing" wave 2 (the local server's stdio branch after the canary) and CTX "serveStdio adoption is future-proofing, not a behavior change today". Every registrar is already on the v2-compatible API (267-06..267-10), so this is a small, reviewable commit. The HTTP branch keeps its v1 transport until 267-12/267-14 (locked order), which a plan-time probe showed is a working composition.
Output: dual-era test (RED then GREEN), swapped entry point and imports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-RESEARCH.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-TRIPOLAR-PROBES.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-05-SUMMARY.md
@bin/mindrian-mcp-server.cjs
@tests/helpers/mcp-wire-267.cjs

Session and scope rules (every task in this plan):
- Workspace /home/jsagi/dev/MindrianOS-Plugin only; record `PLAN_BASE=$(git rev-parse HEAD)` in the SUMMARY.
- Before editing any file run `git status --short -- <that file>`; a diff you did not make means STOP and report.
- Commit by explicit path only, `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git commit -a`, `git stash`.
- Never edit `lib/mcp/gate-render.cjs` (no MRTR, CTX Deferred), `lib/core/session-binding.cjs` (no identity re-architecture), `lib/core/brain-client.cjs`, `lib/core/part8-egress-guard.cjs`, `mcp-server-brain/**`, `/home/jsagi/Theo/**`. Do not modify the HTTP branch code in bin/mindrian-mcp-server.cjs (lines from `if (surface.transport === 'http')` through its closing brace, except the express-missing stdio fallback inside it) in this plan.
- Do not introduce `createRequestStateCodec`, `inputRequired(`, or any `requestState` handling (MRTR is out of scope).
- Apply the "Canary lessons" section of 267-05-SUMMARY.md.
- No em-dashes. CJS only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dual-era wire test for the local server (RED before the swap)</name>
  <files>tests/test-267-mcpv2-dual-era.cjs</files>
  <read_first>
    - tests/test-267-mcpv2-brain-shim.cjs (the canary's two-era client pattern and sibling-process cleanup, to reuse)
    - lib/mcp/runtime-instructions.cjs (RUNTIME_INSTRUCTIONS, the instructions string both eras must deliver)
    - tests/test-198-gate-renderers.test.cjs and tests/test-265-gate-render-elicit-schema.cjs (a minimal valid gate_render card fixture to reuse)
    - tests/fixtures/267/wire-snapshot-zod4.json and tests/fixtures/267/zod4-accepted-deltas.json
  </read_first>
  <behavior>
    - Era 2025: v2 Client (default options) over StdioClientTransport -> getServerVersion/negotiated protocol 2025-11-25; server instructions equal RUNTIME_INSTRUCTIONS; tools/prompts/resources/templates equal the zod4 snapshot via compareToSnapshot with the accepted deltas; every tool has a non-empty title.
    - Era 2026: v2 Client with versionNegotiation auto -> negotiated 2026-07-28; instructions delivered; same tool set and schemas. RED before the swap.
    - Elicitation, era 2025: the client declares capabilities { elicitation: {} } and registers an elicitation request handler that records the request and answers accept with the first option; calling gate_render with the fixture card causes exactly one elicitation request to reach the client (rung (a) live).
    - Elicitation, era 2026: same client in auto mode; gate_render causes NO elicitation request (capabilities are not visible to the server on a 2026 connection, so the ladder uses rung (b)/(c)); the result still carries a gate_id usable by gate_answer.
  </behavior>
  <action>
Write tests/test-267-mcpv2-dual-era.cjs with `Client` from '@modelcontextprotocol/client' and `StdioClientTransport` from '@modelcontextprotocol/client/stdio', env from hermeticEnv(). Auto mode spawns a disposable sibling server process for the probe, and this server has boot side effects (dep-heal check, tree watcher, session catch-up): every child PID is SIGKILLed in finally and the test asserts no `mindrian-mcp-server.cjs` process it started survives. Implement the four behavior bullets; if the elicitation arm needs a gate card shape, copy the minimal fixture from test-198-gate-renderers verbatim. Run on the current tree: era 2025 and elicitation-2025 arms should PASS (v1 server, v2 client), the era 2026 and elicitation-2026 arms FAIL (a v1 server cannot negotiate 2026-07-28). Record the arm-by-arm outcome in the SUMMARY. Commit (RED).
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-dual-era.cjs; test $? -ne 0 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - The test exits non-zero on the pre-swap tree with the 2026 arms named as failing, and the 2025 arms passing.
    - After the run, `pgrep -f "bin/mindrian-mcp-server.cjs" | wc -l` equals its value before the run.
  </acceptance_criteria>
  <done>Dual-era test committed RED.</done>
</task>

<task type="auto">
  <name>Task 2: Swap to v2 McpServer and serveStdio (gated on the Desktop probe), move ResourceTemplate and JSDoc types, prove every baseline</name>
  <files>bin/mindrian-mcp-server.cjs, lib/mcp/resources.cjs, lib/mcp/tool-router.cjs, lib/mcp/prompts.cjs, lib/mcp/capability-registry.cjs, lib/mcp/app-views.cjs, tests/test-267-mcpv2-sdk-era.cjs</files>
  <read_first>
    - 267-TRIPOLAR-PROBES.md sections "Desktop (human probe)" and "Decision input for 267-11"
    - bin/mindrian-mcp-server.cjs lines 63-112 (requireWithHeal calls at :86-87), 158-247 (createServer, module singleton), 249-262 (express-missing fallback), 434-440 (stdio branch)
    - tests/test-266-connect-path-process-budget.cjs (the connect-path heal census for this file: at least 4)
    - lib/mcp/resources.cjs :21 (ResourceTemplate require); JSDoc `import('@modelcontextprotocol/sdk/server/mcp.js').McpServer` at lib/mcp/tool-router.cjs:834, lib/mcp/prompts.cjs:91, lib/mcp/capability-registry.cjs:32, lib/mcp/app-views.cjs:232
    - node_modules/@modelcontextprotocol/server/dist/stdio.d.cts (serveStdio signature and handle)
  </read_first>
  <action>
1. Gate: read 267-TRIPOLAR-PROBES.md. If the Desktop section records `server/discover` or a 2026-07-28 initialize on stdio AND the Decision input section does not record the navigator ruling "accept rung-b on desktop", STOP without editing and report (the ruling "hold serveStdio" also means STOP). "desktop-deferred" does not block (267-18 re-probes before Tri-Polar is claimed); note it in the SUMMARY.
2. bin/mindrian-mcp-server.cjs: replace `requireWithHeal('@modelcontextprotocol/sdk/server/mcp.js', ...)` with `requireWithHeal('@modelcontextprotocol/server', { log: healLog, connectPath: true })` destructuring McpServer, and `requireWithHeal('@modelcontextprotocol/sdk/server/stdio.js', ...)` with `requireWithHeal('@modelcontextprotocol/server/stdio', { log: healLog, connectPath: true })` destructuring serveStdio. Keep the zod requireWithHeal in createServer and ensureDepsPresent (census stays at least 4). Stdio branch: replace `new StdioServerTransport()` + `server.connect(transport)` with `serveStdio(() => server)` (keep the handle in a module variable), keeping the started log line. Express-missing fallback inside the HTTP branch: same replacement. Leave `require('@modelcontextprotocol/sdk/server/streamableHttp.js')` and all HTTP code as is. Update the header comment block and the createServer() comment to say which SDK serves which branch now (hyphens only).
3. lib/mcp/resources.cjs: `const { ResourceTemplate } = require('@modelcontextprotocol/server');` (plan-time probe: v1 and v2 ResourceTemplate instances work with either McpServer, so tests still hosting on v1 keep passing until 267-16/267-17 migrate them).
4. JSDoc only: change the four `import('@modelcontextprotocol/sdk/server/mcp.js').McpServer` type annotations to `import('@modelcontextprotocol/server').McpServer`. No code change in those files.
5. Append `'bin/mindrian-mcp-server.cjs'` and `'lib/mcp/resources.cjs'` to EXPECT_V2 in tests/test-267-mcpv2-sdk-era.cjs. Arm C forbids any sdk require in an EXPECT_V2 file, and the lazy HTTP transport require legitimately stays until 267-14, so add a narrowly scoped, commented allowance in the test: for bin/mindrian-mcp-server.cjs only, the single `@modelcontextprotocol/sdk/server/streamableHttp.js` require is permitted until 267-14 removes it (267-14 deletes this allowance; the test prints it as a TEMPORARY ALLOWANCE line so it cannot hide).
6. Verify, every result compared with 267-BASELINE.md: `node tests/test-267-mcpv2-dual-era.cjs` (GREEN, all four arms), `node tests/test-267-mcpv2-sdk-era.cjs`, `node tests/test-267-mcpv2-registration-api.cjs`, `node tests/test-267-mcpv2-zod4-contract.cjs`, `node tests/test-267-mcpv2-prompts.cjs`, `node tests/test-267-mcpv2-app-views.cjs`, `node tests/test-267-mcpv2-cirs-gates.cjs`, `node tests/test-267-mcpv2-tee.cjs`, `node tests/test-266-connect-path-process-budget.cjs`, `bash tests/run-all-266.sh`, `node tests/test-265-mcp-surface-organ.cjs`, `bash tests/run-all-234.sh`, `bash tests/run-all-198.sh` (includes the flag-ON HTTP concurrency legs on the mixed v2-server/v1-transport composition), `node tests/test-248-surface-probes.cjs` (HTTP flag-ON legs), the six `tests/test-354-*.cjs` that host on v1, `node tests/test-265-gate-render-elicit-schema.cjs`, `node tests/test-198-gate-renderers.test.cjs`, tree-watcher tests (`grep -l tree-watcher tests/*.cjs`), and `node scripts/doctor.cjs --acceptance` point mcp-surface-tool-count. Also `grep -rn "createRequestStateCodec\|inputRequired(" bin lib | wc -l` must print 0. Commit bin + resources + the four JSDoc files + the era test in one commit ("feat(267-11): local server on v2 McpServer, stdio via serveStdio").
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-dual-era.cjs && node tests/test-267-mcpv2-sdk-era.cjs && node tests/test-267-mcpv2-registration-api.cjs && node tests/test-267-mcpv2-zod4-contract.cjs && node tests/test-266-connect-path-process-budget.cjs && node tests/test-265-mcp-surface-organ.cjs && node tests/test-248-surface-probes.cjs && bash tests/run-all-267.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "serveStdio(() => server)" bin/mindrian-mcp-server.cjs` prints 2 (stdio branch and express-missing fallback); `grep -c "StdioServerTransport" bin/mindrian-mcp-server.cjs` counts only comment lines (`grep -v "^\s*//\|^\s*\*" bin/mindrian-mcp-server.cjs | grep -c StdioServerTransport` prints 0).
    - `grep -v "^\s*//\|^\s*\*" bin/mindrian-mcp-server.cjs | grep -c "@modelcontextprotocol/sdk"` prints 1 (only the HTTP streamableHttp require, removed in 267-14).
    - `node tests/test-267-mcpv2-dual-era.cjs` exits 0 and its output shows 2025-11-25 and 2026-07-28 negotiated, one elicitation request in the 2025 arm and zero in the 2026 arm.
    - `bash tests/run-all-198.sh` and `node tests/test-248-surface-probes.cjs` equal 267-BASELINE.md (the HTTP legs prove the untouched HTTP branch still serves on the v2 McpServer).
    - `git diff $PLAN_BASE -- lib/mcp/gate-render.cjs lib/core/session-binding.cjs | wc -l` prints 0; the createRequestStateCodec/inputRequired grep prints 0.
    - SUMMARY states the Desktop gate outcome from step 1.
  </acceptance_criteria>
  <done>Local server's stdio branch on v2 serveStdio, both eras proven, gate ladder behavior proven, baselines held.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host -> local server over stdio (both eras) | the main CLI/Desktop surface |
| host capability declaration -> gate ladder | elicitation visibility differs by era |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-23 | Tampering | gate ladder under v2 | mitigate | dual-era elicitation arms prove rung (a) on 2025 and rung (b)/(c) on 2026; gate-render.cjs untouched |
| T-267-22 | Spoofing | room/session identity on stdio | mitigate | on stdio ctx.sessionId is undefined in both eras (research, live) and identity resolves from CLAUDE_CODE_SESSION_ID exactly as today (session-binding.cjs untouched); room-bind tests in run-all-198 and test-248 at baseline |
| T-267-11 | Tampering | requestState / MRTR | mitigate | grep gate: zero createRequestStateCodec / inputRequired( in bin and lib |
| T-267-27 | Denial of service | auto-negotiation sibling spawn with boot side effects | accept | only the test uses auto mode; hosts do not (Claude Code verified); the test kills every child it spawns |
| T-267-28 | Elevation of privilege | Desktop gate-ladder shift if Desktop negotiates 2026 | mitigate | Task 2 step 1 blocks the swap without the navigator's recorded ruling |
</threat_model>

<verification>
- Dual-era test green (four arms); era, registration, zod4, prompts, app-views, CIRS, tee tests green.
- run-all-198, run-all-234, run-all-266, test-248 surface probes, doctor organ at baseline.
</verification>

<success_criteria>
The local server serves both protocol eras over stdio on v2, with the host-driven elicitation behavior preserved and pinned.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `tests/test-267-mcpv2-dual-era.cjs` | new test (MCPV2-02, MCPV2-07) | aggregator, 267-18 |
| `bin/mindrian-mcp-server.cjs` stdio via `serveStdio` (module-level handle) | migrated entry point | CLI, Desktop |
| EXPECT_V2 entries + TEMPORARY ALLOWANCE line in `tests/test-267-mcpv2-sdk-era.cjs` | test ledger change | removed by 267-14 |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-11-SUMMARY.md` when done (commit with `git add -f`).
</output>
