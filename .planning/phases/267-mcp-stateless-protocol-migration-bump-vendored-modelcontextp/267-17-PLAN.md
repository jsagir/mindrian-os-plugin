---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 17
type: execute
wave: 15
locked_stage: "W4 (remove v1 last): migrate the last four v1 importers, then uninstall @modelcontextprotocol/sdk from package.json, package-lock.json and npm-shrinkwrap.json"
depends_on: [267-16]
files_modified:
  - tests/test-248-surface-probes.cjs
  - tests/test-257-strict-input-shapes.cjs
  - tests/test-265-gate-render-elicit-schema.cjs
  - tests/test-276-claim-write-primitive.cjs
  - package.json
  - package-lock.json
  - npm-shrinkwrap.json
  - lib/core/mcp-dep-heal.cjs
  - lib/core/mcp-dep-heal.test.cjs
  - lib/mcp/gate-render.cjs
  - lib/mcp/tree-watcher.cjs
  - scripts/build-brain-packet-schema.cjs
autonomous: true
requirements: [MCPV2-18, MCPV2-09, MCPV2-12, MCPV2-19, MCPV2-01]
canon_parts: [7, 8, 11]
tags: [v1-removal, dependency-removal, shrinkwrap, dep-heal]

must_haves:
  truths:
    - "test-248-surface-probes, test-257-strict-input-shapes, test-265-gate-render-elicit-schema and test-276-claim-write-primitive run on the v2 packages with their baseline verdicts; test-257's Part 8 strictness arms are re-expressed against v2 (v2 has no positional registration overload, so the v1-only Arm Z4 mechanism is restated as 'registerTool with z.strictObject rejects an undeclared key', not deleted)"
    - "@modelcontextprotocol/sdk is absent from package.json, package-lock.json and npm-shrinkwrap.json, and from node_modules; no non-comment require or requireWithHeal of it remains in bin/, lib/, scripts/ or tests/ (mcp-server-brain/ excluded: dead service, out of scope)"
    - "Every package the v1 SDK used to bring in transitively that repo code requires (ajv, express, zod) is a declared direct dependency, so the removal breaks no require"
    - "The dep-heal FALLBACK no longer names the removed package; the release payload ceiling and the shrinkwrap gates hold"
    - "Comments that cite node_modules/@modelcontextprotocol/sdk paths are updated to v2 paths, comment-only, with no logic change in gate-render.cjs (no MRTR rework) and lib/core/brain-client.cjs left untouched (its stale 'ajv is transitive via the sdk' comment is recorded for the 267-18 seed, never edited here)"
  artifacts:
    - path: "package.json"
      provides: "dependency manifest without @modelcontextprotocol/sdk"
      contains: "@modelcontextprotocol/server"
    - path: "lib/core/mcp-dep-heal.cjs"
      provides: "FALLBACK ['@modelcontextprotocol/server', 'zod']"
      contains: "@modelcontextprotocol/server"
  key_links:
    - from: "tests/test-267-mcpv2-sdk-era.cjs Arm D"
      to: "package.json dependencies"
      via: "binds only once @modelcontextprotocol/sdk is absent, then sweeps bin/lib/scripts/tests"
      pattern: "Arm D"
---

<objective>
Finish the migration: move the last four v1 importers, then remove the v1 SDK from the manifest, both lockfiles and node_modules, with a transitive-dependency census proving nothing breaks.

Purpose: CTX "Scope and sequencing": "v1 dependency (@modelcontextprotocol/sdk) is removed from package.json/package-lock.json/npm-shrinkwrap.json LAST, only once nothing imports it." CTX "Supply-chain and release gates": Phase 341 ships dependencies through npm-shrinkwrap.json and the payload ceiling must stay green. The sdk-era test's Arm D binds the moment the manifest drops the package and sweeps for any leftover import.
Output: four migrated tests, the removal, updated dep-heal, comment-only doc fixes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-RESEARCH.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md
@tests/test-267-mcpv2-sdk-era.cjs

Session and scope rules (every task in this plan):
- Workspace /home/jsagi/dev/MindrianOS-Plugin only; record `PLAN_BASE=$(git rev-parse HEAD)` in the SUMMARY.
- Before editing any file run `git status --short -- <that file>`; a diff you did not make means STOP and report.
- Commit by explicit path only, `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git commit -a`, `git stash`.
- Never edit `lib/core/brain-client.cjs`, `lib/core/part8-egress-guard.cjs`, `lib/core/session-binding.cjs`, `mcp-server-brain/**` (its own node_modules keeps its own v1 copy; dead service, out of scope), `/home/jsagi/Theo/**`. In `lib/mcp/gate-render.cjs` and `lib/mcp/tree-watcher.cjs` only comment lines may change.
- Keep the existing `@modelcontextprotocol/sdk` VETTED entry in references/security/cve-db.json (name-only allowlist; harmless when unused, and mcp-server-brain/ still carries the package on disk).
- Every npm command: `--ignore-scripts --no-audit --no-fund`.
- A test whose assertions would need to change to pass is NOT silently rewritten: STOP that file and report, except the one sanctioned restatement of test-257 Arm Z4 below.
- No em-dashes. CJS only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate test-248, test-257, test-265 elicit-schema and test-276 to the v2 packages</name>
  <files>tests/test-248-surface-probes.cjs, tests/test-257-strict-input-shapes.cjs, tests/test-265-gate-render-elicit-schema.cjs, tests/test-276-claim-write-primitive.cjs</files>
  <read_first>
    - tests/test-248-surface-probes.cjs:69-71 (Client, StdioClientTransport, StreamableHTTPClientTransport requires)
    - tests/test-257-strict-input-shapes.cjs lines 1-60 (the arm catalogue: Z1-Z4, A-G), 280-360 (pin reporting from node_modules/@modelcontextprotocol/sdk/package.json at :292-294; McpServer requires at :325, :341, :352; Arm Z4 asserts the v1 positional overload throws on a ZodObject)
    - tests/test-265-gate-render-elicit-schema.cjs lines 125-175 (requires ../node_modules/@modelcontextprotocol/sdk/dist/cjs/types.js for TitledSingleSelectEnumSchemaSchema / TitledMultiSelectEnumSchemaSchema)
    - tests/test-276-claim-write-primitive.cjs:260-270 (a comment citing the v1 mcp.js path)
    - 267-BASELINE.md rows for all four files (test-257 Arm F stale and Arm B flaky are pre-existing)
  </read_first>
  <action>
1. test-248-surface-probes.cjs: `Client` and `StreamableHTTPClientTransport` from '@modelcontextprotocol/client', `StdioClientTransport` from '@modelcontextprotocol/client/stdio'. Default client options only (no versionNegotiation). Run; outcome must equal baseline. Commit alone.
2. test-257-strict-input-shapes.cjs: McpServer from '@modelcontextprotocol/server'. Pin reporting reads versions from disk (`node_modules/@modelcontextprotocol/server/package.json`, `node_modules/zod/package.json`) since v2 packages do not export ./package.json, and prints the server version instead of the sdk version. Arm Z4 existed to prove that the v1 POSITIONAL overload misreads a ZodObject; v2 has no positional overload, so restate Z4 as: `registerTool(name, { description, inputSchema: z.strictObject({...}) }, cb)` on the v2 McpServer, then a tools/call with an undeclared key over InMemoryTransport is rejected (unrecognized key) and the handler never runs. Keep its arm id and add a comment naming the v1-to-v2 reason. Every other arm keeps its assertion. Run; per-arm outcomes equal baseline (Arm F stale and Arm B flaky may keep their baseline state). Commit alone.
3. test-265-gate-render-elicit-schema.cjs: take `TitledSingleSelectEnumSchemaSchema`, `TitledMultiSelectEnumSchemaSchema` and `EnumSchemaSchema` from `require('@modelcontextprotocol/core')` (exported at the package root on 2.1.0, verified at plan time; re-verify on V) instead of the relative v1 dist path; keep the fallback logic and every assertion; update the arm's comment. Run; must PASS (green at baseline). Commit alone.
4. test-276-claim-write-primitive.cjs: comment-only update of the v1 mcp.js path citation to the v2 behavior it describes. Run; baseline outcome. Commit alone.
  </action>
  <verify>
    <automated>node tests/test-248-surface-probes.cjs; echo "248 exit=$?"; node tests/test-257-strict-input-shapes.cjs; echo "257 exit=$?"; node tests/test-265-gate-render-elicit-schema.cjs && node tests/test-276-claim-write-primitive.cjs; test -z "$(grep -v '^\s*//\|^\s*\*' tests/test-248-surface-probes.cjs tests/test-257-strict-input-shapes.cjs tests/test-265-gate-render-elicit-schema.cjs tests/test-276-claim-write-primitive.cjs | grep '@modelcontextprotocol/sdk')" && echo "no v1 in the four files"</automated>
  </verify>
  <acceptance_criteria>
    - Zero non-comment `@modelcontextprotocol/sdk` references in the four files.
    - test-248 and test-276 exit codes equal 267-BASELINE.md; test-265 elicit-schema exits 0.
    - test-257 per-arm outcomes equal 267-BASELINE.md, with Arm Z4 PASS in its restated v2 form (SUMMARY quotes the before and after Z4 assertion).
    - One commit per file.
  </acceptance_criteria>
  <done>No test in the repo imports the v1 SDK.</done>
</task>

<task type="auto">
  <name>Task 2: Uninstall the v1 SDK in lockstep, update dep-heal and stale comments, prove the removal</name>
  <files>package.json, package-lock.json, npm-shrinkwrap.json, lib/core/mcp-dep-heal.cjs, lib/core/mcp-dep-heal.test.cjs, lib/mcp/gate-render.cjs, lib/mcp/tree-watcher.cjs, scripts/build-brain-packet-schema.cjs</files>
  <read_first>
    - lib/core/mcp-dep-heal.cjs FALLBACK (updated in 267-05) and lib/core/mcp-dep-heal.test.cjs FALLBACK constant
    - lib/mcp/gate-render.cjs:10-16 and lib/mcp/tree-watcher.cjs:20-30 (comments citing node_modules/@modelcontextprotocol/sdk paths)
    - scripts/build-brain-packet-schema.cjs:15-20 (comment claiming ajv arrives via the sdk)
    - 267-RESEARCH.md "Runtime State Inventory: Build artifacts" row
  </read_first>
  <action>
1. Pre-removal census: run `grep -rn "@modelcontextprotocol/sdk" bin lib scripts tests hooks --include=*.cjs --include=*.js --include=*.sh | grep -v "^\s*//"` and list every hit in the SUMMARY; only comment lines may remain before uninstalling. Transitive census: for every dependency name in node_modules/@modelcontextprotocol/sdk/package.json `dependencies`, grep bin/, lib/, scripts/, hooks/ for `require('<name>` and `require("<name>`; every hit must be a name in package.json `dependencies` (plan-time: ajv, express, zod, all direct). Any other hit is a STOP.
2. `npm uninstall --ignore-scripts --no-audit --no-fund @modelcontextprotocol/sdk`; bring package-lock.json and npm-shrinkwrap.json into agreement as in 267-03 Task 2 step 2; confirm `node_modules/@modelcontextprotocol/sdk` is gone and `npm ls @modelcontextprotocol/sdk` reports nothing at the root tree.
3. lib/core/mcp-dep-heal.cjs FALLBACK becomes `['@modelcontextprotocol/server', 'zod']` with its comment updated; lib/core/mcp-dep-heal.test.cjs FALLBACK constant and any assertion on it follow. Fixture package.json objects inside the test that mention the sdk as a fake dependency name may stay (they test probe mechanics, not this repo's manifest); change them only if the test fails.
4. Comment-only updates: gate-render.cjs and tree-watcher.cjs citations of v1 dist paths point at the v2 package that now provides the same thing (for example `@modelcontextprotocol/core` TitledSingleSelectEnumSchemaSchema; `McpServer.sendResourceListChanged` in `@modelcontextprotocol/server`); scripts/build-brain-packet-schema.cjs's comment says ajv is a declared direct dependency. `git diff` of these three files must show only comment lines. Do NOT touch lib/core/brain-client.cjs:2358's identical stale comment (out of scope); note it in the SUMMARY for the 267-18 seed.
5. Verify: `node tests/test-267-mcpv2-sdk-era.cjs` (Arm D now binds and PASSES), `node tests/test-267-mcpv2-lockstep.cjs`, `node lib/core/mcp-dep-heal.test.cjs`, `node scripts/check-release-payload-ceiling.cjs`, `node tests/test-341-shrinkwrap-no-dev.cjs`, `node tests/test-341-payload-shrinkwrap-present.cjs`, `bash tests/run-all-199.sh`, `bash tests/run-all-267.sh`, `bash tests/run-all-198.sh`, `bash tests/run-all-234.sh`, `bash tests/run-all-266.sh`, `bash tests/run-all-127.sh`, every `tests/test-257-*.cjs`, every `tests/test-354-*.cjs` that requires an MCP package, and `node lib/memory/packet-schema-validation.test.cjs` (ajv consumer) plus `node scripts/build-brain-packet-schema.cjs --check` if it supports --check (else run it into a temp path), all compared to 267-BASELINE.md. Commit (a) manifest + lockfiles, (b) dep-heal pair, (c) the three comment-only files.
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-sdk-era.cjs && node tests/test-267-mcpv2-lockstep.cjs && node lib/core/mcp-dep-heal.test.cjs && node scripts/check-release-payload-ceiling.cjs && node tests/test-341-shrinkwrap-no-dev.cjs && bash tests/run-all-199.sh && bash tests/run-all-267.sh && test ! -d node_modules/@modelcontextprotocol/sdk && node -e "const p=require('./package.json');process.exit(p.dependencies['@modelcontextprotocol/sdk']?1:0)"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "for(const f of ['package-lock.json','npm-shrinkwrap.json']){const l=require('./'+f);if(l.packages['node_modules/@modelcontextprotocol/sdk']||(l.packages[''].dependencies||{})['@modelcontextprotocol/sdk'])process.exit(1)}"` exits 0.
    - `node tests/test-267-mcpv2-sdk-era.cjs` output shows Arm D ran (not SKIP) and passed.
    - `grep -c "@modelcontextprotocol/sdk" lib/core/mcp-dep-heal.cjs` counts no FALLBACK entry (`node -e "console.log(require('./lib/core/mcp-dep-heal.cjs').productionDepNames('/nonexistent'))"` prints the new two-name fallback).
    - `git diff $PLAN_BASE -- lib/mcp/gate-render.cjs lib/mcp/tree-watcher.cjs scripts/build-brain-packet-schema.cjs | grep "^[-+]" | grep -v "^[-+][-+]" | grep -v "^[-+]\s*\(//\|\*\|/\*\)" | wc -l` prints 0 (comment-only).
    - `git diff $PLAN_BASE -- lib/core/brain-client.cjs mcp-server-brain | wc -l` prints 0.
    - run-all-198, run-all-234, run-all-266, run-all-127, run-all-199, test-257 and test-354 outcomes equal 267-BASELINE.md; run-all-267 FAIL=0.
  </acceptance_criteria>
  <done>The v1 SDK is gone from the manifest, both lockfiles and node_modules, with every gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm-shrinkwrap.json -> every user install | what the loader installs per machine |
| test verdicts -> Part 8 guarantees | test-257 and test-354 egress tests prove the Brain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-35 | Denial of service | a require that silently relied on a v1 transitive package | mitigate | transitive census before uninstall (ajv, express, zod are direct); full suite comparison after |
| T-267-13 | Tampering | manifest/lockfile drift during removal | mitigate | lockstep test, shrinkwrap no-dev and payload-present tests, payload ceiling |
| T-267-34 | Tampering | weakening the Part 8 strictness test while migrating it | mitigate | only Arm Z4 is restated (v1-only mechanism), keeping its security claim: an undeclared key is rejected before the handler runs; every other arm unchanged and at baseline |
| T-267-09 | Information disclosure | Canon Part 8 | mitigate | brain-client.cjs and part8-egress-guard.cjs untouched (diff gate); all test-257 files at baseline |
| T-267-11 | Tampering | gate-render.cjs logic | mitigate | comment-only diff gate; no MRTR |
</threat_model>

<verification>
- sdk-era Arm D bound and green; lockstep, dep-heal, payload and shrinkwrap gates green; every suite at baseline.
</verification>

<success_criteria>
Nothing in the repo depends on the v1 SDK, and users' installs carry only the v2 family.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| removal of `@modelcontextprotocol/sdk` from package.json, package-lock.json, npm-shrinkwrap.json | dependency change | every user install |
| `lib/core/mcp-dep-heal.cjs` FALLBACK `['@modelcontextprotocol/server', 'zod']` | config literal change | dep self-heal |
| test-257 Arm Z4 restated for v2 | test change (Part 8 strictness) | run-all-257 |
| four migrated tests; three comment-only doc fixes | test and doc changes | suites |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-17-SUMMARY.md` when done (commit with `git add -f`).
</output>
