---
phase: 267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp
plan: 03
type: execute
wave: 2
locked_stage: "W0 (zod 4 + @modelcontextprotocol/sdk 1.30.1 on v1, zero registration changes)"
depends_on: [267-01]
files_modified:
  - tests/fixtures/267/zod4-accepted-deltas.json
  - tests/fixtures/267/wire-snapshot-zod4.json
  - tests/test-267-mcpv2-zod4-contract.cjs
  - tests/test-198-contract-schema.test.cjs
  - package.json
  - package-lock.json
  - npm-shrinkwrap.json
autonomous: true
requirements: [MCPV2-03, MCPV2-12, MCPV2-19]
canon_parts: [7, 8, 11]
tags: [zod4, dependency-bump, sdk-1.30.1, wire-contract, wave-0]

must_haves:
  truths:
    - "zod lands at the exact range the v2 family requires at execute time (re-checked with npm view, not copied from the research's ^4.2.0), and @modelcontextprotocol/sdk lands at 1.30.1 per the locked Wave 0 decision, with zero registration-code changes"
    - "Every tool and prompt description is byte-identical to the zod 3 snapshot after the bump"
    - "The advertised-strictness loosening is ACCEPTED as ruled (CTX 'zod 3 -> 4 schema strictness: accept the looser default') and pinned: the observed set of schema changes must EQUAL the recorded accepted set, so any FURTHER drift fails the test"
    - "The brain shim's z.strictObject tools still advertise additionalProperties:false after the bump (the Part 8 undeclared-key guard is untouched)"
    - "package.json, package-lock.json and npm-shrinkwrap.json agree after the bump, one zod is installed, the payload ceiling holds, and every suite matches 267-BASELINE.md (pre-existing reds unchanged, nothing newly red)"
  artifacts:
    - path: "tests/test-267-mcpv2-zod4-contract.cjs"
      provides: "MCPV2-03 wire-contract test (RED on zod 3, GREEN on zod 4)"
      min_lines: 80
    - path: "tests/fixtures/267/zod4-accepted-deltas.json"
      provides: "the accepted zod-4 wire deltas, measured before the bump"
      contains: "zod4_additionalProperties_dropped"
    - path: "tests/fixtures/267/wire-snapshot-zod4.json"
      provides: "post-bump wire snapshot that later plans compare against"
      contains: "\"brain\""
  key_links:
    - from: "tests/test-267-mcpv2-zod4-contract.cjs"
      to: "tests/helpers/mcp-wire-267.cjs"
      via: "wireSnapshot + compareToSnapshot"
      pattern: "compareToSnapshot"
    - from: "package.json"
      to: "npm-shrinkwrap.json"
      via: "lockstep asserted by tests/test-267-mcpv2-lockstep.cjs"
      pattern: "\"zod\""
---

<objective>
Ship the zod 3 -> 4 and @modelcontextprotocol/sdk 1.29.0 -> 1.30.1 bump ON V1, in isolation, green, with the wire-contract change measured before the bump and pinned after it.

Purpose: CTX "Scope and sequencing" Wave 0 (de-risk the dependency bump before any registration code moves) and CTX "zod 3 -> 4 schema strictness: accept the looser default". 1.30.1 also brings the 4 MiB request-body limit and 100-message batch bound (a security backport, RESEARCH "Supporting"). This is NOT 2026-07-28 adoption: 1.30.1's types.js is byte-identical to 1.29.0 (the sdk-era test's Arm A keeps saying so).
Output: accepted-delta fixture and contract test (RED first), then the bump, the zod-4-safe rewrite of one test's introspection, and a post-bump wire snapshot.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-CONTEXT.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-RESEARCH.md
@.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-BASELINE.md
@tests/helpers/mcp-wire-267.cjs
@tests/fixtures/267/wire-snapshot-zod3.json

Session and scope rules (every task in this plan):
- Workspace /home/jsagi/dev/MindrianOS-Plugin only; record `PLAN_BASE=$(git rev-parse HEAD)` in the SUMMARY.
- Before editing any file run `git status --short -- <that file>`; a diff you did not make means STOP and report.
- Commit by explicit path only (`git add <path>`, `git add -f` for .planning/), `git commit --only -m "<msg>" -- <paths>`. Never `git add -A`, `git commit -a`, `git stash`, or revert a diff you did not make.
- Never edit `mcp-server-brain/**`, `/home/jsagi/Theo/**`, `lib/core/brain-client.cjs`, `lib/core/part8-egress-guard.cjs`, `lib/core/session-binding.cjs`, `lib/mcp/gate-render.cjs`. Never run `@modelcontextprotocol/codemod`.
- Every npm install: `npm install --ignore-scripts --no-audit --no-fund ...` (the loader installs with `--ignore-scripts`, so nothing may depend on install scripts).
- Zero registration-code changes in this plan (locked). Zero new `require('zod')` sites (CTX zod boundary discipline). Accept the looser `additionalProperties` default; do NOT convert any tool to `z.strictObject` (CTX ruling).
- No em-dashes. CJS only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Measure the zod-4 wire delta without touching the repo, pin it as the accepted set, and write the contract test RED</name>
  <files>tests/fixtures/267/zod4-accepted-deltas.json, tests/test-267-mcpv2-zod4-contract.cjs</files>
  <read_first>
    - 267-RESEARCH.md "Code Examples: Measuring the zod 4 blast radius without touching the repo" (the Module._resolveFilename preload that redirects every `zod` and `zod/...` require to a scratch zod@4 install) and Pitfall 2 (38 sites / 35 of 42 tools lose additionalProperties:false; 3 z.record gain propertyNames; 1 .int() gains safe-integer bounds; 0 description diffs)
    - tests/helpers/mcp-wire-267.cjs (wireSnapshot, compareToSnapshot, hermeticEnv)
    - tests/fixtures/267/wire-snapshot-zod3.json
    - bin/mindrian-brain-mcp-client.cjs lines 118-145 (why the 6 brain tools use z.strictObject: the Part 8 undeclared-key smuggling gap)
  </read_first>
  <behavior>
    - Under zod 3 (unchanged tree) the observed delta set is EMPTY, which is not equal to the accepted set, so the test FAILS (RED).
    - Under zod 4 the observed delta set equals the accepted set exactly, so it PASSES; one extra or one missing delta FAILS.
    - Every tool and prompt description is byte-identical to the zod 3 snapshot in both states.
    - All 6 brain-shim tools advertise additionalProperties:false in both states.
  </behavior>
  <action>
1. Re-check versions at execute time, never from the research snapshot: run `npm view @modelcontextprotocol/server version dependencies.zod`, `npm view @modelcontextprotocol/ext-apps version peerDependencies.zod`, `npm view zod version`, `npm view @modelcontextprotocol/sdk@1.30.1 version dependencies.zod`. Choose ZOD_RANGE = the server package's `dependencies.zod` range (e.g. `^4.2.0` at plan time) and confirm the chosen latest zod satisfies the ext-apps peer and the sdk 1.30.1 range. Record all four outputs in the SUMMARY.

2. In the session scratchpad (NOT the repo) `npm install --ignore-scripts zod@<latest satisfying ZOD_RANGE>`; write the research's preload there, pointing its redirect at the scratch node_modules. Boot both servers under the preload via tests/helpers/mcp-wire-267.cjs (pass `NODE_OPTIONS=-r <scratch preload>` in the env) and diff each against tests/fixtures/267/wire-snapshot-zod3.json with `compareToSnapshot(live, snapshot, {})`. Also confirm `require('zod/v4')` resolves to the same module instance as `require('zod')` under zod 4 (plan-time check on 4.6.5: true), because lib/core/pitch-feedback-schemas.cjs, lib/core/skillopt-schemas.cjs and two scripts use the `zod/v4` subpath.

3. Write `tests/fixtures/267/zod4-accepted-deltas.json`: `{ "measured_at", "zod_version", "note": "CTX ruling 2026-09-23: accept the looser default (strip, not strict); runtime stripping unchanged", "zod4_additionalProperties_dropped": [ {kind, name, field, path} ... ], "zod4_other": [ ...propertyNames, safe-integer bounds... ], "app_views_schema_fix": [], "prompts_fix": [] }` holding exactly the diffs step 2 observed (the two empty arrays are filled by 267-09 and 267-10, which change those surfaces on purpose). If step 2 shows ANY description diff, STOP and report (that would contradict the research and the accept ruling does not cover it).

4. Write `tests/test-267-mcpv2-zod4-contract.cjs`: snapshot the live local server and brain shim with the helper; compute diffs against wire-snapshot-zod3.json; (a) FAIL on any description diff (tools and prompts); (b) read the installed zod major from node_modules/zod/package.json: if 4, the diff set (excluding entries listed in app_views_schema_fix and prompts_fix) must EQUAL zod4_additionalProperties_dropped plus zod4_other exactly, set-wise, printing extras and missing; if 3, print "zod 3 installed: accepted zod-4 delta not yet present" and FAIL (this is the RED state); (c) all 6 brain tools' normalized inputSchema contains `additionalProperties: false`; (d) the production zod importer set (same rule as 267-01 step 3) is a subset of tests/fixtures/267/zod-importers-baseline.txt, except files under lib/mcp/ or bin/ (CTX zod boundary: zod only at the MCP input boundary).

5. Run the test on the unchanged tree: it must FAIL on (b) only. Commit fixture + test (RED) by explicit path.
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-zod4-contract.cjs; test $? -ne 0 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const d=require('./tests/fixtures/267/zod4-accepted-deltas.json');process.exit(d.zod4_additionalProperties_dropped.length>0?0:1)"` exits 0 and the SUMMARY states the measured count (research measured 38 sites across 35 tools; record the actual).
    - `node tests/test-267-mcpv2-zod4-contract.cjs` exits non-zero on the unchanged tree and its output names check (b) as the only failing check.
    - The repo's package.json, lockfiles and node_modules are untouched by this task (`git status --short -- package.json package-lock.json npm-shrinkwrap.json` empty; `node -p "require('zod/package.json').version"` still 3.25.76).
    - The SUMMARY records the four `npm view` outputs from step 1.
  </acceptance_criteria>
  <done>Accepted-delta fixture and RED contract test committed; repo dependencies untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Bump zod and sdk 1.30.1 in lockstep, make test-198-contract-schema zod-4 safe, prove green against the baseline, snapshot post-bump</name>
  <files>package.json, package-lock.json, npm-shrinkwrap.json, tests/test-198-contract-schema.test.cjs, tests/fixtures/267/wire-snapshot-zod4.json</files>
  <read_first>
    - tests/test-198-contract-schema.test.cjs lines 195-230 (the `_def.typeName` walk at :205-213; in zod 4 `_def.typeName` is undefined and the kind lives at `_zod.def.type`; RESEARCH Pitfall 2 "Test infra")
    - .claude/includes/release-process.md and docs/RELEASE-CEREMONY-RULING-SYSTEM.md RULE 8 (dependencies ship via npm-shrinkwrap.json, tracked; node_modules is never vendored)
    - scripts/release-lib/shrinkwrap-gate.sh lines 59-120 (what release.sh asserts about the shrinkwrap)
    - 267-BASELINE.md (every suite result to compare against)
  </read_first>
  <action>
1. Install: `npm install --ignore-scripts --no-audit --no-fund zod@"<ZOD_RANGE from Task 1>" @modelcontextprotocol/sdk@1.30.1`. Set package.json ranges to `"zod": "<ZOD_RANGE>"` and `"@modelcontextprotocol/sdk": "^1.30.1"` (caret style matches the manifest). Plan-time dry run in scratch resolved to a single zod 4.6.5 shared by sdk 1.30.1, ext-apps 1.5.0 and zod-to-json-schema, with no peer conflict.

2. Lockstep: npm updates whichever lockfile it prefers (npm-shrinkwrap.json takes precedence when both exist). Bring the other file into agreement so the two differ ONLY in their root `version` fields (the state they were in at 267-BASELINE), using a `node -e` script that copies `packages` and every non-version top-level key from the updated file into the other; never hand-edit JSON. Run `node tests/test-267-mcpv2-lockstep.cjs` (must pass) and `npm ls zod` (exactly one version, deduped everywhere).

3. Rewrite the schema walk in tests/test-198-contract-schema.test.cjs (:205-213) to be zod-version-agnostic: read the kind from `zType._zod && zType._zod.def && zType._zod.def.type` when present (zod 4, lowercase kinds like 'object', 'string', 'optional'), else from `_def.typeName` (zod 3), mapping both vocabularies onto the same sample generator. Do not change what the test asserts. Its baseline outcome was already FAIL on zod 3 (at context_assemble); after the rewrite it must no longer crash in introspection, and if it still fails, the failure must be the same pre-existing contract failure named in 267-BASELINE.md (record the exact text).

4. Verify, comparing every result to 267-BASELINE.md (a baseline FAIL may stay FAIL with the same assertion; anything newly red is a failure of this task): `node tests/test-267-mcpv2-zod4-contract.cjs` (now GREEN), `node tests/test-267-mcpv2-sdk-era.cjs` (Arm A still reports 1.30.1 does NOT implement 2026-07-28), `node tests/test-267-mcpv2-cirs-gates.cjs`, `bash tests/run-all-198.sh`, `bash tests/run-all-234.sh`, `bash tests/run-all-266.sh`, `bash tests/run-all-127.sh`, every `node tests/test-257-*.cjs` (research: Arms Z2, Z3, Z4 and A pass under zod 4; Arm F stale and Arm B flaky are pre-existing), `node tests/test-265-gate-render-elicit-schema.cjs`, `node tests/test-265-mcp-surface-organ.cjs`, the six sdk-importing `tests/test-354-*.cjs`, `node tests/test-276-claim-write-primitive.cjs`, and the tests covering the non-MCP zod importers (find them with `grep -l` for `verification-stamp`, `observation-schema`, `pitch-feedback-schemas`, `skillopt-schemas` under tests/ and lib/**/*.test.cjs; run each), `node lib/core/mcp-dep-heal.test.cjs`, `node scripts/check-release-payload-ceiling.cjs`, `node tests/test-341-shrinkwrap-no-dev.cjs`, `node tests/test-341-payload-shrinkwrap-present.cjs`.

5. Write `tests/fixtures/267/wire-snapshot-zod4.json` with the helper (same shape as the zod3 snapshot plus the installed versions). Later plans compare against THIS file.

6. Commit in two commits by explicit path: (a) package.json + package-lock.json + npm-shrinkwrap.json with message naming both versions; (b) the test-198 rewrite + zod4 snapshot.
  </action>
  <verify>
    <automated>node tests/test-267-mcpv2-zod4-contract.cjs && node tests/test-267-mcpv2-lockstep.cjs && node tests/test-267-mcpv2-sdk-era.cjs && node tests/test-267-mcpv2-cirs-gates.cjs && node scripts/check-release-payload-ceiling.cjs && node tests/test-341-shrinkwrap-no-dev.cjs && bash tests/run-all-267.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node -p "require('zod/package.json').version"` prints a 4.x version satisfying ZOD_RANGE; `node -p "require('@modelcontextprotocol/sdk/package.json').version"` prints 1.30.1.
    - `npm ls zod --all 2>/dev/null | grep -o "zod@[0-9.]*" | sort -u | wc -l` prints 1.
    - `node tests/test-267-mcpv2-zod4-contract.cjs` exits 0 (was non-zero in Task 1).
    - `bash tests/run-all-198.sh` has the same PASS/FAIL leg set as 267-BASELINE.md (research baseline 21 PASS / 3 FAIL); the SUMMARY lists any leg whose outcome changed and why.
    - `node tests/test-198-contract-schema.test.cjs` output contains no `typeName` TypeError.
    - `git diff $PLAN_BASE -- lib bin | wc -l` prints 0 (zero registration or production code change).
    - `bash tests/run-all-267.sh` ends `FAIL=0`.
  </acceptance_criteria>
  <done>zod 4 and sdk 1.30.1 shipped green on v1 in lockstep; contract pinned; post-bump snapshot recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> tool input schemas | bytes from the host's tool call, validated by zod at registration |
| npm registry -> node_modules / npm-shrinkwrap.json | third-party code entering every user's install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-02 | Tampering / Information disclosure | 35 tools that stop ADVERTISING additionalProperties:false | accept | Not a security regression, stated explicitly: (1) runtime behavior is unchanged, zod 4's default object still STRIPS undeclared keys before the handler runs, exactly as zod 3 did, so no new byte reaches any handler (research measured identical suite outcomes under both zods); (2) the advertisement was never an enforcement point: the host does not reject on it and no local handler treats "extra keys absent" as a trust decision (CTX ruling); (3) the only tools where an undeclared key is a Part 8 smuggling risk are the 6 brain-shim tools, which use z.strictObject and keep both rejection and the advertisement, asserted by check (c) of the contract test; (4) any FURTHER loosening fails the contract test because the observed delta must equal the pinned set |
| T-267-01 | Tampering | npm install of zod 4 and sdk 1.30.1 | mitigate | both names already VETTED in references/security/cve-db.json; research Package Legitimacy Audit rated zod [OK]; install with --ignore-scripts; lockstep test and shrinkwrap no-dev test after install |
| T-267-05 | Denial of service | loopback HTTP body and batch size | mitigate | sdk 1.30.1 adds the 4 MiB body limit and 100-message batch bound (security backport); express.json()'s 100 KB limit still runs first and is unchanged |
| T-267-13 | Tampering | manifest/lockfile drift | mitigate | test-267-mcpv2-lockstep.cjs plus the release shrinkwrap gate tests |
</threat_model>

<verification>
- `bash tests/run-all-267.sh` FAIL=0.
- Every suite in 267-BASELINE.md re-run with no newly red leg.
- `git diff $PLAN_BASE -- lib bin` empty.
</verification>

<success_criteria>
The dependency half of the migration is shipped and proven on v1, so every later failure can be attributed to registration or transport changes, not to zod.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `tests/test-267-mcpv2-zod4-contract.cjs` | new test (MCPV2-03) | every later plan's verification |
| `tests/fixtures/267/zod4-accepted-deltas.json` (keys `zod4_additionalProperties_dropped`, `zod4_other`, `app_views_schema_fix`, `prompts_fix`) | new fixture / accepted-delta ledger | extended by 267-09 and 267-10 |
| `tests/fixtures/267/wire-snapshot-zod4.json` | new fixture | 267-05, 267-11 |
| package.json `zod` range and `@modelcontextprotocol/sdk ^1.30.1`; package-lock.json and npm-shrinkwrap.json regenerated | dependency change | whole repo |
| tests/test-198-contract-schema.test.cjs zod-version-agnostic kind reader | test infra fix | run-all-198 |

<output>
Create `.planning/phases/267-mcp-stateless-protocol-migration-bump-vendored-modelcontextp/267-03-SUMMARY.md` when done (commit with `git add -f`).
</output>
