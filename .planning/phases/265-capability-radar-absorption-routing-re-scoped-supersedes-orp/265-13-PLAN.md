---
phase: 265-capability-radar-absorption-routing-re-scoped-supersedes-orp
plan: 13
type: execute
wave: 2
depends_on: ["265-03"]
files_modified:
  - commands/analyze-needs.md
  - commands/analyze-timing.md
  - commands/beautiful-question.md
  - commands/build-knowledge.md
  - commands/build-thesis.md
  - commands/dominant-designs.md
  - commands/explore-domains.md
  - commands/explore-futures.md
  - commands/explore-trends.md
  - commands/find-bottlenecks.md
  - commands/futures.md
  - commands/grade.md
  - commands/macro-trends.md
  - commands/map-unknowns.md
  - commands/root-cause.md
  - commands/scenario-plan.md
  - commands/structure-argument.md
  - commands/think-hats.md
  - commands/user-needs.md
  - commands/validate.md
  - commands/value-proposition.md
  - tests/test-265-declaration-truth.cjs
autonomous: true
requirements: [RADAR-18, RADAR-19, RADAR-20]
canon_parts: [3, 5, 7, 11]

must_haves:
  truths:
    - "No shipped command tells the user their work connects to a literal unfilled placeholder"
    - "A command that reaches the web declares that it reaches the web"
    - "The requires_evidence contract, specified since Phase 131 and used by zero commands, has live consumers wired to the producer that already declares the reciprocal"
    - "No second research-dispatch mechanism and no second confirmation gate are introduced anywhere"
  artifacts:
    - path: "tests/test-265-declaration-truth.cjs"
      provides: "Tripwire: zero unfilled placeholders, web_scope matches fetchCorpus reality, every requires_evidence block is well-formed and points at a declared producer"
    - path: "commands/build-thesis.md"
      provides: "The requires_evidence declaration the docs have cited as the canonical example since Phase 131"
      contains: "requires_evidence"
    - path: "commands/futures.md"
      provides: "web_scope corrected from null to green to match seedGrounding and perRingResearch"
  key_links:
    - from: "commands/build-thesis.md"
      to: "commands/research.md"
      via: "requires_evidence dispatch pointing at the command that declares emits_evidence_claims true"
      pattern: "requires_evidence"
    - from: "commands/futures.md"
      to: "lib/core/futures/orchestrator.cjs"
      via: "seedGrounding and perRingResearch reaching research-corpus.fetchCorpus"
      pattern: "web_scope"
---

<objective>
Three declaration defects, zero new dispatch machinery. Every fix here is frontmatter or prose.

1. NINETEEN command files ship a literal unfilled `[methodology]` placeholder in their
   suggested-next line. The research found nine within its 20-command scope; a repo-wide grep of
   `commands/*.md` returns 19 files, one occurrence each. Users are being told "The X you've found
   connects to [methodology]. Want to explore that next?" verbatim. Recording the corrected count
   is part of this plan's job: the research undercounted because it only looked at its own scope.

2. `/mos:futures` declares `connector.web_scope: null` while shipping two live web fire points,
   `seedGrounding` (`lib/core/futures/orchestrator.cjs:1193`) and `perRingResearch` (`:1227`), both
   reaching `research-corpus.fetchCorpus`. `docs/CONNECTOR-CONTRACT.md` line 62 states plainly:
   "A surface with `web_scope: null` does not reach the web." The declaration is factually wrong.

3. The `requires_evidence:` frontmatter contract is fully specified in
   `docs/RESEARCH-AS-WORKFLOW-STEP.md` section 3, `commands/research.md` declares the reciprocal
   `emits_evidence_claims: true` at line 20, and `grep -rn "requires_evidence"` returns only the
   doc and the producer describing it. ZERO consumers exist - not even `/mos:build-thesis`, which
   both documents name as THE example. This is a documented-but-unused hook that has sat idle
   since Phase 131.

The navigator's decision governs the shape of fix 3: `/mos:research` is the shared research
primitive. Any command needing external research declares the EXISTING contract; it does not grow
its own dispatch logic. The confirmation gate already lives in `/mos:research`; do not add a
second one.

Purpose: three surfaces are telling users something untrue, and the highest-leverage
zero-new-code lever in the whole online-research audit has never been pulled.
Output: 19 real command names, one corrected web_scope, four wired evidence consumers, and a
tripwire that fails on any of the three defect classes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/265-capability-radar-absorption-routing-re-scoped-supersedes-orp/265-RESEARCH-online-research-gap.md
@docs/RESEARCH-AS-WORKFLOW-STEP.md
</context>

## Artifacts this phase produces

| Symbol / path | Kind | Notes |
|---|---|---|
| `tests/test-265-declaration-truth.cjs` | new test file | Three arms: placeholders, web_scope truth, requires_evidence well-formedness |
| `requires_evidence:` blocks | new frontmatter on 4 commands | build-thesis, value-proposition, macro-trends, analyze-timing |

No new runtime code, no new command, no new mechanism. `requires_evidence` is consumed by the
existing workflow layer; `emits_evidence_claims` on `commands/research.md` is untouched.

**Mirror regeneration is NOT this plan's job.** Plan 265-24 owns the second-pass generator run for
every `commands/*.md` edit added after 265-07.

<tasks>

<task type="auto">
  <name>Task 1: Replace all 19 unfilled methodology placeholders with real command names</name>
  <files>commands/analyze-needs.md, commands/analyze-timing.md, commands/beautiful-question.md, commands/build-knowledge.md, commands/build-thesis.md, commands/dominant-designs.md, commands/explore-domains.md, commands/explore-futures.md, commands/explore-trends.md, commands/find-bottlenecks.md, commands/grade.md, commands/macro-trends.md, commands/map-unknowns.md, commands/root-cause.md, commands/scenario-plan.md, commands/structure-argument.md, commands/think-hats.md, commands/user-needs.md, commands/validate.md</files>
  <read_first>
    - The output of `grep -rn '\[methodology\]' commands/*.md` (19 hits, one per file) so you edit the actual lines rather than a remembered list. Note that three of them are NOT the shared boilerplate: `commands/build-thesis.md:78` and `commands/grade.md:237` use the shape "Your weakest category is [X]. Want to run /mos:[methodology] to address it?", and `commands/map-unknowns.md:72` uses "Your matrix reveals [pattern]. Want to explore that with [methodology]?". Those three need a different repair than the sixteen boilerplate lines.
    - commands/systems-thinking.md, the M3 (`st-archetype`) move: it is the ONLY one of the twenty that already does this correctly, surfacing the handoff as a Decision Gate line that names and offers rather than auto-invoking. Copy its shape.
    - .planning/phases/265-.../265-RESEARCH-online-research-gap.md, the Summary table (all 20 verdicts) and the "ALREADY SOLVED (detail)" section. It tells you WHICH command each externally-facing framework should hand off to, and it tells you that `map-unknowns` runs OUTBOUND: the Rumsfeld matrix's product IS a research agenda, so its known-unknowns column should hand off TO `/mos:research`, not consume it.
    - references/methodology/curated_chains wherever the command registry declares them (grep for `curated_chains`), so a suggested handoff names a chain that actually exists: `PEST -> Adoption-Capacity` at 0.7, `PEST -> Scenario Planning` at 0.6, `Domain Selection -> Scenario Planning` at 0.68, `Futures Wheel -> Scenario Planning` at 0.73.
    - The `name:` frontmatter value of every command you intend to name, to confirm the `/mos:<name>` you write actually resolves. A suggested next step that names a command that does not exist is worse than the placeholder.
  </read_first>
  <action>
Replace every one of the 19 `[methodology]` occurrences with a REAL `/mos:` command name.

RULES, in order of precedence:

1. NAME A COMMAND THAT EXISTS. Every `/mos:<name>` you write must have a matching
   `commands/<name>.md` on disk. Verify each one before writing it.
2. PREFER A CURATED CHAIN. Where a `curated_chains` edge already exists from this framework, name
   its target. That is the repo's own answer and it is already weighted.
3. FOR EXTERNALLY-FACING FRAMEWORKS, NAME `/mos:research`. Per the navigator's decision that
   `/mos:research` is the shared research primitive, the frameworks whose raw material is public-
   world fact hand off to it: macro-trends (PEST), analyze-timing (S-curve), dominant-designs,
   explore-futures, explore-trends, scenario-plan.
4. FOR `map-unknowns`, INVERT THE DIRECTION. Its output is a research agenda. Its line must offer
   to hand the known-unknowns column TO `/mos:research`, phrased as producing rather than
   consuming. Do not write it as "your matrix connects to research".
5. FOR THE INWARD-FACING FRAMEWORKS (analyze-needs, user-needs, beautiful-question,
   structure-argument, root-cause, explore-domains, think-hats, validate, build-knowledge,
   find-bottlenecks), name the methodology command that genuinely follows, NOT `/mos:research`.
   The online-research audit marks these NOT APPLICABLE for external evidence on purpose: JTBD
   needs a real person, 5-Whys traces the navigator's own symptom, and substituting web copy for a
   customer conversation is a methodology failure. Naming research here would be worse than the
   placeholder.
6. FOR THE THREE NON-BOILERPLATE LINES (build-thesis:78, grade:237, map-unknowns:72), repair the
   surrounding sentence too. `/mos:[methodology]` with a bracket inside a slash-command is doubly
   broken: it renders as an invalid command string. Rewrite so the command name is a plain literal
   and any remaining `[X]` or `[pattern]` is clearly a fill-in-the-blank the model substitutes at
   runtime, not a shipped literal. If the surrounding sentence cannot be made unambiguous, replace
   it with the systems-thinking M3 shape instead.
7. KEEP IT AN OFFER, NEVER AN AUTO-INVOKE. Every line stays a Decision Gate offer in Larry's
   voice. Do not add an AskUserQuestion call, do not add a chain, do not add `allowed-tools`.

Record in the SUMMARY a table of all 19: file, old line, new line, and which rule decided it. Also
record the count correction: the research said nine within its own 20-command scope; the repo-wide
count is 19 files.

Hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>test "$(grep -rl '\[methodology\]' commands/*.md | wc -l)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc '\[methodology\]' commands/*.md | grep -v ':0'` produces no output (zero remaining occurrences across all command files).
    - `grep -rc '/mos:\[' commands/*.md | grep -v ':0'` produces no output (no bracket survives inside a slash-command string).
    - Every `/mos:<name>` newly written resolves to an existing file: run `for n in $(grep -rho '/mos:[a-z-]*' commands/*.md | sort -u | sed 's|/mos:||'); do test -f "commands/$n.md" || echo "MISSING $n"; done` and confirm it prints nothing.
    - `commands/map-unknowns.md`'s line offers to hand its known-unknowns TO `/mos:research` (outbound phrasing), and the SUMMARY quotes the new line.
    - None of analyze-needs, user-needs, beautiful-question, structure-argument or root-cause names `/mos:research` in the replaced line.
    - The SUMMARY contains the 19-row table with the deciding rule per file, and states the corrected count against the research's nine.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' commands/*.md | grep -v ':0'` produces no output.
  </acceptance_criteria>
  <done>No shipped command tells a user their work connects to a bracketed placeholder, every named handoff resolves to a real command, and the inward-facing frameworks were deliberately not pointed at research.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the futures web_scope declaration to match its shipped fetch points</name>
  <files>commands/futures.md</files>
  <read_first>
    - commands/futures.md frontmatter, specifically `connector.web_scope: null` at line 34, plus the FW-13 section in the body naming the two fire points.
    - lib/core/futures/orchestrator.cjs around line 1193 (`async function seedGrounding(roomDir, seed, opts)`) and line 1227 (`async function perRingResearch(roomDir, ringConsequences, domainHandle, opts)`). Confirm by reading that both are cache-first (30-day TTL via research-cache) and pass only a generic domain or concept handle to `research-corpus.fetchCorpus`, never a consequence body. That Part 8 property is what makes `green` the correct value rather than a problem.
    - docs/CONNECTOR-CONTRACT.md line 62 and its surrounding paragraph: the definition of `web_scope: null` and the enumeration of the four commands currently declaring `green` (research, explore-opportunity, intel-pipeline, bono).
    - .planning/phases/265-.../265-RESEARCH-online-research-gap.md, the two corrections at the end: `/mos:futures` is not research-free, and `web_scope` currently has NO runtime enforcement consumer (grep finds it only in the registry builders, the contract doc and scaffolding, never in a gate that blocks a fetch).
    - scripts/build-connector-registry.cjs, to confirm changing this value does not break the `--check` gate and to see what the registry does with `web_scope`.
  </read_first>
  <action>
Change `connector.web_scope` in `commands/futures.md` from `null` to `green`, and record why in a
YAML comment immediately above the key, in this shape: `/mos:futures` reaches the web at two
declared fire points, `seedGrounding` and `perRingResearch` in
`lib/core/futures/orchestrator.cjs`, both cache-first with a 30-day TTL and both passing only a
generic domain or concept handle to `research-corpus.fetchCorpus`; a consequence body never
crosses. Cite `docs/CONNECTOR-CONTRACT.md` line 62 as the definition being honored.

Then check the CLASS, not just the instance. Run
`grep -rn 'fetchCorpus\|research-corpus' lib/ --include=*.cjs` and cross-reference every module it
names against the `web_scope` declaration of the commands that call it. Any OTHER command
declaring `null` while its orchestrator reaches `fetchCorpus` is the same defect and must be
listed in the SUMMARY with its file and line. Do NOT fix additional commands in this plan without
reading their fetch path in full: record them as findings so a follow-up is scoped honestly. If
the sweep finds none, say so explicitly.

Also record in the SUMMARY the governance fact the research surfaced: `web_scope` has no runtime
enforcement consumer today, so this declaration is documentation and audit surface, not a gate.
That is worth knowing before anyone treats a corrected declaration as a control.

Do not change the FW-13 body text, the frontmatter's other connector keys, or the orchestrator.

Hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check &amp;&amp; grep -q 'web_scope: green' commands/futures.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'web_scope: null' commands/futures.md` returns 0 and `grep -c 'web_scope: green' commands/futures.md` returns 1.
    - A YAML comment adjacent to the key names both `seedGrounding` and `perRingResearch` and the literal `fetchCorpus`.
    - `node scripts/build-connector-registry.cjs --check` exits 0.
    - `node scripts/build-orchestration-projection.cjs --check` exits 0.
    - The SUMMARY lists the result of the class sweep: every other command whose orchestrator reaches `fetchCorpus`, with its declared `web_scope`, or an explicit statement that none was found.
    - The SUMMARY records that `web_scope` has no runtime enforcement consumer today.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' commands/futures.md` returns 0.
  </acceptance_criteria>
  <done>The futures declaration matches its shipped behavior, the same defect class was swept for rather than assumed unique, and the limits of what the declaration actually enforces are recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the requires_evidence contract onto its four flagship consumers and tripwire all three defect classes</name>
  <files>commands/build-thesis.md, commands/value-proposition.md, commands/macro-trends.md, commands/analyze-timing.md, tests/test-265-declaration-truth.cjs</files>
  <read_first>
    - docs/RESEARCH-AS-WORKFLOW-STEP.md section 3 in full: the exact YAML block shape (`requires_evidence: {tier, on, dispatch}`), the GUIDED-default rule ("a requires_evidence: declaration NEVER auto-fires material work"), the F.1 selector wording the navigation engine surfaces, the Mode A confidence >= 0.7 render invariant, and section 4's chain-back contract (the producer RETURNS accepted EvidenceClaim node IDs so the caller resumes with the exact evidence it needed).
    - commands/research.md line 20 (`emits_evidence_claims: true`) and lines 74 to 85 (the passage that names `/mos:build-thesis` as the canonical consumer example), plus wherever `/mos:research` fires its own ask-the-user confirmation, so this plan does not add a second gate.
    - commands/value-proposition.md setup step 6 (it already reads `room/market-analysis/` for existing market evidence) and its two numeric gates (Gate 1 >= 6.0, Gate 2 >= 5.5) and its explicit anti-optimism rules ("'Everyone has this problem' = score 0 on market sizing"; "Score with evidence, not optimism"). This is a fully-built evidence CONSUMER with no producer wired to it, which is why it is the strongest of the twenty.
    - commands/macro-trends.md and commands/analyze-timing.md bodies: PEST's four factors are public-world facts by definition; the S-curve command refuses vague technologies by design ("do not accept 'AI' or 'blockchain' - demand specificity"), so the research target is fully determined the moment the navigator names the performance metric.
    - The Canon Part 5 evidence tier vocabulary, so `tier:` is set to a real tier name and not invented. Grep for the tier enum wherever it is defined before writing a value.
    - .planning/phases/265-.../265-RESEARCH-online-research-gap.md, "The three flagship requires_evidence candidates" and recommended action 1.
  </read_first>
  <action>
PART A. Add a `requires_evidence:` block to the frontmatter of exactly four commands. Use the
verbatim shape from `docs/RESEARCH-AS-WORKFLOW-STEP.md` section 3: `tier`, `on` (an array of room
section slugs), `dispatch: /mos:research`.

- `commands/build-thesis.md` - the canonical example both documents already cite and which
  currently declares nothing. Choose `on:` from the sections its own body reads for premises, and
  set `tier:` to the Canon Part 5 floor its argument actually requires. State both choices in the
  SUMMARY with the body line that justified each.
- `commands/value-proposition.md` - `on: [market-analysis]`, at the tier its numeric gates demand.
  It already reads that section at setup step 6, so the declaration wires a consumer that exists.
- `commands/macro-trends.md` - PEST is external by definition and is already a curated-chain hub
  (`PEST -> Adoption-Capacity` 0.7, `PEST -> Scenario Planning` 0.6), so grounding it improves
  three downstream commands rather than one.
- `commands/analyze-timing.md` - single-topic once the performance metric is named; the textbook
  mid-flow single-pass offer.

For each, also add a one-line YAML comment above the block naming what the declaration DOES: it
lets the navigation engine OFFER `/mos:research` at an F.1 selector when room evidence is below
the tier; it never fetches behind the navigator's back.

PART B. Add ONE prose line to each of the four bodies, at the point in the flow where external
signal is genuinely useful, copying the `systems-thinking` M3 shape: a named handoff offered at a
gate, never auto-invoked. One line, not a section. Do NOT add `web_scope: green`, do NOT add
`allowed-tools` entries, and do NOT add a confirmation prompt: `/mos:research` already asks before
it fires and a second gate is a worse experience, not a safer one. State that reasoning in the
SUMMARY.

PART C. Create `tests/test-265-declaration-truth.cjs`, a plain Node tripwire with three arms over
every `commands/*.md`:

1. PLACEHOLDER ARM. Zero occurrences of `[methodology]`, zero occurrences of `/mos:[`, and every
   `/mos:<name>` referenced anywhere in `commands/` resolves to an existing `commands/<name>.md`.
   Print every unresolvable name with its file and line.
2. WEB_SCOPE ARM. For every command declaring `connector.web_scope: null`, assert its body does not
   name `fetchCorpus`, `seedGrounding`, `perRingResearch`, `tavily-search` or `WebSearch` in a fire
   instruction. This is a heuristic and must SAY SO in its failure message: it catches the
   declaration-versus-reality class, it does not prove absence of reach. Do not overstate it.
3. REQUIRES_EVIDENCE ARM. Every `requires_evidence:` block found in `commands/` has all three keys
   (`tier`, `on`, `dispatch`), `on` is a non-empty array, `dispatch` names a command that exists
   AND whose frontmatter declares `emits_evidence_claims: true`. Assert at least four consumers
   exist, so the contract cannot silently return to zero use.

Print pass and fail counts per arm and `process.exit(1)` on any failure. Hyphens only, no
em-dashes.
  </action>
  <verify>
    <automated>node tests/test-265-declaration-truth.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl 'requires_evidence' commands/*.md | wc -l` returns 5 (the four new consumers plus `commands/research.md`, which only describes the contract).
    - Each of the four consumer files contains a `requires_evidence:` block with all three keys `tier`, `on` and `dispatch`, and `dispatch` is `/mos:research`.
    - `node tests/test-265-declaration-truth.cjs` exits 0 with three arms reported.
    - Arm 3 asserts the producer link: `grep -c 'emits_evidence_claims' tests/test-265-declaration-truth.cjs` returns at least 1.
    - No consumer file gained `web_scope: green`, an `allowed-tools` entry, or a second confirmation prompt: `git diff --stat commands/build-thesis.md commands/value-proposition.md commands/macro-trends.md commands/analyze-timing.md` shows only frontmatter and one-line body additions, and the SUMMARY quotes each added body line.
    - `node scripts/build-connector-registry.cjs --check` exits 0.
    - Negative proof recorded in the SUMMARY: setting `dispatch:` to a non-existent command on one consumer makes the test exit 1 naming arm 3; reverted, it exits 0.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' commands/build-thesis.md commands/value-proposition.md commands/macro-trends.md commands/analyze-timing.md tests/test-265-declaration-truth.cjs` returns 0 for every file.
  </acceptance_criteria>
  <done>The requires_evidence contract has four live consumers wired to the producer that already declared the reciprocal, no second dispatch mechanism or gate was introduced, and all three declaration defect classes are pinned by one tripwire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command declaration to navigation engine | `requires_evidence` and `web_scope` are read by the workflow and registry layers; a false declaration misroutes rather than merely misinforms |
| methodology command to the web | Only `/mos:research` and the three other `green` surfaces reach out; a consumer declares a need and never fetches itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-265-55 | Spoofing | A suggested next step naming a command that does not exist | mitigate | Task 1 requires verifying every `/mos:<name>` against disk; Task 3 arm 1 asserts it repo-wide and prints unresolvable names. |
| T-265-56 | Repudiation | A `web_scope: null` declaration on a surface that reaches the web | mitigate | Task 2 corrects `futures` and sweeps the class; Task 3 arm 2 pins the heuristic and states its own limits in the failure message rather than overclaiming. |
| T-265-57 | Elevation of Privilege | Flipping nineteen commands to `green` to buy research they can get by chaining | mitigate | The navigator's decision is that `/mos:research` is the shared primitive; this plan adds `requires_evidence` declarations and ZERO new `green` scopes. Task 3's acceptance criteria assert no consumer gained `web_scope: green`. |
| T-265-58 | Denial of Service | Auto-firing costed web research from a methodology command | mitigate | `requires_evidence` is GUIDED by default and only OFFERS at an F.1 selector; the existing `/mos:research` confirmation gate stays the single gate and Task 3 forbids adding a second. |
| T-265-59 | Information Disclosure | Canon Part 8 | accept | No change to what crosses. `futures` already reached `fetchCorpus` with generic handles only; correcting its declaration changes documentation, not egress. |
</threat_model>

<verification>
1. `node tests/test-265-declaration-truth.cjs` exits 0 with all three arms passing.
2. `grep -rc '\[methodology\]' commands/*.md | grep -v ':0'` produces no output.
3. `node scripts/build-connector-registry.cjs --check` and `node scripts/build-orchestration-projection.cjs --check` both exit 0.
4. `node scripts/check-render-coverage.cjs` exits 0.
5. `LC_ALL=C.UTF-8 grep -cP '\x{2014}' commands/*.md | grep -v ':0'` produces no output.
</verification>

<success_criteria>
- Zero unfilled placeholders ship, and every replacement names a command that resolves.
- The one command whose web declaration contradicted its own code is corrected, and the defect class was swept rather than assumed unique.
- A contract specified in Phase 131 and used by nobody has four live consumers, wired to the existing producer with no new machinery and no second gate.
</success_criteria>

<output>
Create `.planning/phases/265-capability-radar-absorption-routing-re-scoped-supersedes-orp/265-13-SUMMARY.md` when done.
</output>
