---
phase: 265-capability-radar-absorption-routing-re-scoped-supersedes-orp
plan: 06
type: execute
wave: 2
depends_on: ["265-01", "265-04"]
files_modified:
  - .planning/seeds/SEED-003-claude-code-2-1-x-capability-adoption.md
  - .planning/phases/138-capability-radar-absorption-and-routing/138-CONTEXT.md
  - .planning/phases/138/DRIFT.md
  - docs/CANON-PHASE-MAP.md
  - docs/RADAR-ABSORPTION-265.md
  - data/capability-ledger.json
  - tests/test-265-capability-ledger-schema.cjs
  - tests/test-265-supersede-chain.cjs
autonomous: true
requirements: [RADAR-08, RADAR-10]
canon_parts: [6, 7]

must_haves:
  truths:
    - "SEED-003 and Phase 138 are marked superseded by Phase 265 and both remain on disk with their bodies intact"
    - "Drift finding W007-138 is closed with a forward pointer, not deleted"
    - "A future reader can find, in one durable document, why the five PWS engines were NOT the destination for default forking and why BONO's Promise.all fan-out is deliberate"
    - "The navigator's explore-opportunity decision is recorded as a ledger status, not only in a plan SUMMARY"
  artifacts:
    - path: "docs/RADAR-ABSORPTION-265.md"
      provides: "The durable decision record: settled A4, the corrected destination map, the BONO disposition, and the out-of-scope list"
    - path: ".planning/seeds/SEED-003-claude-code-2-1-x-capability-adoption.md"
      provides: "Seed marked superseded by Phase 265, body preserved"
    - path: ".planning/phases/138/DRIFT.md"
      provides: "W007-138 closed with the retirement reason"
    - path: "tests/test-265-supersede-chain.cjs"
      provides: "Tripwire asserting the supersede markers exist and nothing was deleted"
  key_links:
    - from: "data/capability-ledger.json"
      to: "docs/RADAR-ABSORPTION-265.md"
      via: "cross-reference key on the decision-bearing rows"
      pattern: "RADAR-ABSORPTION-265"
    - from: ".planning/seeds/SEED-003-claude-code-2-1-x-capability-adoption.md"
      to: "Phase 265"
      via: "superseded_by frontmatter key"
      pattern: "superseded_by"
---

<objective>
Retire the predecessors that rotted, and write down the reasoning so the next reader does not
re-derive it. Phase 138 died as an orphan on disk; SEED-003 pointed forward at 138 and waited.
The drift detector caught the orphaning sixteen days before a human did, which is evidence the
check works and is worth preserving rather than tidying away.

This plan also records the three findings that would otherwise be lost: A4 is SETTLED by the
platform rather than an open adopt-versus-supersede question; the real fan-out surfaces are
act, persona, grade, and trending-to-absurd, NOT the five PWS engines; and BONO's in-process
`Promise.all` fan-out is deliberate and independently validated, so no future phase should
"fix" it.

Purpose: 138's fatal error was that its ledger and its reasoning lived inside one phase's
CONTEXT.md, invisible to every tool and every later reader. This plan puts both somewhere
durable and cross-linked.
Output: two retired predecessors, one closed drift finding, one decision record, and the
ledger rows that point at it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/265-capability-radar-absorption-routing-re-scoped-supersedes-orp/265-RESEARCH.md
@.planning/phases/265-capability-radar-absorption-routing-re-scoped-supersedes-orp/265-04-SUMMARY.md
@data/capability-ledger.json
</context>

## Artifacts this phase produces

| Symbol / path | Kind | Notes |
|---|---|---|
| `docs/RADAR-ABSORPTION-265.md` | new file | The phase decision record |
| `tests/test-265-supersede-chain.cjs` | new test file | |
| `decision_ref` | new optional ledger row key | cross-reference to `docs/RADAR-ABSORPTION-265.md`; the schema tripwire from 265-01 must be extended to allow it |
| `superseded_by` value naming Phase 265 | updated frontmatter value | replaces the pending Phase 138 value in SEED-003 |
| `closed_date` on row W007-138 | updated table cell | in `.planning/phases/138/DRIFT.md` |

**`.planning/` is gitignored** (`.planning/*`). The three `.planning/` files this plan edits
must be staged with `git add -f`, matching the convention CLAUDE.md documents for
`.planning/debug/` RCA files.

<tasks>

<task type="auto">
  <name>Task 1: Retire SEED-003 and Phase 138 by marking, and close W007-138</name>
  <files>.planning/seeds/SEED-003-claude-code-2-1-x-capability-adoption.md, .planning/phases/138-capability-radar-absorption-and-routing/138-CONTEXT.md, .planning/phases/138/DRIFT.md, docs/CANON-PHASE-MAP.md, tests/test-265-supersede-chain.cjs</files>
  <read_first>
    - .planning/seeds/SEED-003-claude-code-2-1-x-capability-adoption.md frontmatter lines 1 to 21 (it already carries `superseded_by: Phase 138 ... (pending its v1.14.0 ship)` and a `needs_author_touch` line instructing exactly this flip)
    - .planning/phases/138-capability-radar-absorption-and-routing/138-CONTEXT.md frontmatter lines 1 to 25 (status, sequencing, origin, depends_on; note the false claim in depends_on that the cache "already carries the 2.1.148-159 findings as of 2026-06-01")
    - .planning/phases/138/DRIFT.md (single row W007-138, status open, first_seen 2026-08-10)
    - docs/CANON-PHASE-MAP.md tail entries (the row format: version, TBD, date, then a prose paragraph naming the phase, its canon_parts, and what changed)
    - .planning/phases/265-.../265-RESEARCH.md section 1, both tables "What 138 got RIGHT" and "What 138 got WRONG" (E-1 through E-5)
    - tests/test-223-supersedes-chain.cjs (an existing supersede-assertion test in this repo; reuse its shape rather than inventing one)
  </read_first>
  <action>
Retire by marking, never by deleting.

SEED-003: change `superseded_by` to name Phase 265 and this phase's slug. Change `status` from
`dormant` to `superseded`. Rewrite `needs_author_touch` to record what actually happened: A1
and A3 shipped, A2 / A4 / A5 carried forward to Phase 138 which itself orphaned, and A4
(forked subagents) is now SETTLED by the platform at 2.1.232 rather than an open
adopt-versus-supersede question. Add `data/capability-ledger.json` to `companion_artifacts` as
the living successor to the two markdown reference files. Do not delete any body prose.

138-CONTEXT.md: add a `superseded_by` frontmatter key naming Phase 265 and change `status` to
name it superseded with a forward pointer. Do NOT edit or delete the body. Append a short
"Superseded by Phase 265" section at the END recording, one line each: errors E-1 through E-5
with their corrections, and the four elements Phase 265 carried forward verbatim (the problem
framing, the reuse-not-rebuild reframe, the four-part mechanism, and the supersede-never-delete
discipline). Writing the corrections next to the original is the point: the next reader sees
both.

.planning/phases/138/DRIFT.md: close row W007-138. Set the row `status` to `closed`, set
`last_seen` and `closed_date` to today, and extend the `detail` cell with the closure reason:
Phase 138 is retired as superseded by Phase 265 rather than added to ROADMAP.md. Change the
frontmatter `status` key from `open` to `closed`. Do not delete the row or the file. The
sixteen-day detection lag is the evidence the drift check works.

docs/CANON-PHASE-MAP.md: append one entry in the existing row format for Phase 265, naming
canon_parts 6, 7, and 8, and stating in prose: the capability ledger moved from a markdown
table inside one phase's CONTEXT.md to `data/capability-ledger.json` with a freshness tripwire;
SEED-003 and Phase 138 are superseded, not deleted; this is Canon Part 6 dog-fooding, the
plugin applying its own drift discipline to itself. Map entry only, no canon text change.

Create tests/test-265-supersede-chain.cjs as a plain Node script modeled on
tests/test-223-supersedes-chain.cjs. Arms: SEED-003's frontmatter `superseded_by` names Phase
265 and `status` is `superseded`; 138-CONTEXT.md has a `superseded_by` naming Phase 265 and
still contains its original `origin:` line (proving the body was not gutted); DRIFT.md row
W007-138 has `closed` in its status cell and a non-empty closed_date; all three files still
exist on disk. Print the failing arm and process.exit(1).

Stage the three `.planning/` files with `git add -f`.
  </action>
  <verify>
    <automated>node tests/test-265-supersede-chain.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'superseded_by: Phase 265' .planning/seeds/SEED-003-claude-code-2-1-x-capability-adoption.md` returns 1, and the file's line count is greater than or equal to its pre-task count.
    - `grep -c 'status: superseded' .planning/seeds/SEED-003-claude-code-2-1-x-capability-adoption.md` returns 1.
    - `.planning/phases/138-capability-radar-absorption-and-routing/138-CONTEXT.md` contains a `superseded_by` key naming Phase 265, still contains its original `origin:` line, and gained a trailing section whose heading contains `Superseded by Phase 265`.
    - That appended section names all five error ids `E-1`, `E-2`, `E-3`, `E-4`, `E-5`.
    - `.planning/phases/138/DRIFT.md` row W007-138 has `closed` in its status cell, a non-empty closed_date cell, and frontmatter `status: closed`.
    - `git ls-files --error-unmatch .planning/phases/138/DRIFT.md` succeeds after `git add -f`.
    - `docs/CANON-PHASE-MAP.md` gained exactly one new row mentioning Phase 265 and canon_parts 6, 7, 8.
    - `git status --porcelain | grep -E '^ ?D'` produces no output (nothing deleted).
    - `node tests/test-265-supersede-chain.cjs` exits 0.
    - Lines added by this task in `docs/CANON-PHASE-MAP.md` contain zero em-dashes (check the added lines only, via `git diff -U0 docs/CANON-PHASE-MAP.md | grep '^+' | LC_ALL=C.UTF-8 grep -cP '\x{2014}'` returning 0).
  </acceptance_criteria>
  <done>Both predecessors are marked superseded with bodies intact, W007-138 is closed with a forward pointer, the canon phase map records the retirement, and a tripwire proves nothing was deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Write the durable decision record</name>
  <files>docs/RADAR-ABSORPTION-265.md</files>
  <read_first>
    - .planning/phases/265-.../265-RESEARCH.md section 2 in full (the central finding: none of the five named engines spawns a subagent; the real fan-out surfaces; the run_in_background defect) and section 3 (the sequential-by-design audit, which found zero platform-workaround sequencing)
    - .planning/phases/265-.../265-RESEARCH.md the langtalks grounding section, finding 1 (the data4sci level-synchronous DAG walker plus semaphore plus isolated-failure pattern that independently validates cell-fanout.cjs) and the gaps table G-1 through G-4
    - lib/core/bono/cell-fanout.cjs lines 1 to 15 and 245 to 262 (the misleading header comment at lines 4-5 and the actual Promise.all at line 251)
    - .planning/phases/265-.../265-RESEARCH.md "Scope discipline" list (the three explicitly out-of-scope items)
    - .planning/phases/265-.../265-04-SUMMARY.md (the navigator's explore-opportunity decision and the ledger status it implies)
  </read_first>
  <action>
Create docs/RADAR-ABSORPTION-265.md. It is the phase's reasoning record, the thing 138 never
had. Keep it readable, roughly 120 to 180 lines. Required sections:

1. **A4 is settled.** Platform default forking at 2.1.232 supersedes SEED-003 A4. No
   hand-rolled harness ships. `CLAUDE_CODE_FORK_SUBAGENT` still exists but inverted: `=0` opts
   out everywhere, `=1` opts in for non-interactive and the Agent SDK. Phase 138-04's
   no-literal tripwire is kept, for the opposite reason, and now lives at
   `tests/test-265-no-fork-subagent-literal.cjs`.

2. **The corrected destination map, with the reasoning preserved.** State the ROADMAP goal's
   own stale fact plainly: the phase brief named `/mos:eureka`, `/mos:bono`,
   `/mos:find-connections`, `/mos:whitespace`, and `/mos:find-analogies` as the destination for
   default-forking adoption, and not one of them spawns a Claude Code subagent. Give the actual
   mechanism per surface in a table (eureka fires one detached Node process and polls; bono
   uses Promise.all over an in-process grid; find-connections runs sequential Brain MCP queries
   in the main context; whitespace shells one `node scripts/whitespace-command.cjs` per
   subcommand; find-analogies uses Brain calls plus a report script). Then name the surfaces
   that DO dispatch subagents: `act --swarm`, `persona --parallel`, `grade --full`, and
   `trending-to-absurd` Expert path. Include the trap: `cell-fanout.cjs` line 4 calls itself
   "a REAL PARALLEL sub-agent fan-out" and that comment is misleading, because it dispatches
   JavaScript promises, not agents.

3. **BONO's fan-out is deliberate and validated. Do not "fix" it.** Cite `cell-fanout.cjs:251`
   (Promise.all over a cap-clamped grid), the `FUTURES_FANOUT_CAP` default of 5, and the
   `defensiveStub` per-node failure isolation. Cite the langtalks grounding: the data4sci
   *Building an Advanced Agentic Harness* level-synchronous DAG walker with
   `asyncio.Semaphore(MAX_CONCURRENT)` and per-node failure isolation is the same shape, so an
   independent production source validates the shipped design. Record that the navigator
   confirmed this as the intended final design, not a placeholder.

4. **Dispatch shapes now explicit.** One line each for trending-to-absurd (parallel, with the
   weak topological evidence stated as weak) and explore-opportunity (sequential through
   `runChain`, coupled by `quality_early_stop` and `gateFn`, with the cost consequence). Record
   the navigator's Task 3 decision from `265-04-SUMMARY.md` verbatim, including the option id
   chosen.

5. **Out of scope, recorded not forgotten.** The `context: fork` skill capability (2.1.218,
   zero current usage); the commands-merged-into-skills consolidation (touches 239 files and
   the Windows compositing rationale at `build-skill-mirrors.cjs:10`); ingesting Fragmented
   #305 and #309 into the langtalks corpus to close gap G-1. Each with one line on why it is a
   phase of its own.

6. **Honest gaps.** Name G-1 explicitly: *Fragmented #305: Subagents explained: what they are,
   when (not) to spawn them* is in the langtalks graph by TITLE ONLY, ingested from #307's
   shownotes link list, with no transcript. The single most on-point corpus source is an empty
   node. Do not cite it as if it argued anything.

Cross-link both ways: this document references `data/capability-ledger.json`, and Task 3 adds
the reverse reference from the ledger. Hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>test -f docs/RADAR-ABSORPTION-265.md &amp;&amp; LC_ALL=C.UTF-8 grep -cP '\x{2014}' docs/RADAR-ABSORPTION-265.md; test $? -ne 0 || grep -c 'cell-fanout' docs/RADAR-ABSORPTION-265.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/RADAR-ABSORPTION-265.md` exists and contains all six required section headings.
    - It names all five PWS engines (`eureka`, `bono`, `find-connections`, `whitespace`, `find-analogies`) and states for each the actual dispatch mechanism.
    - It names all four real fan-out surfaces (`act`, `persona`, `grade`, `trending-to-absurd`).
    - It contains the literals `cell-fanout.cjs:251`, `FUTURES_FANOUT_CAP`, and `2.1.232`.
    - It contains the literal `Fragmented #305` and states the title-only gap.
    - It contains the option id the navigator chose in `265-04-SUMMARY.md` Task 3.
    - It contains the literal `data/capability-ledger.json`.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' docs/RADAR-ABSORPTION-265.md` returns 0.
  </acceptance_criteria>
  <done>The reasoning behind every Phase 265 disposition lives in one durable, cross-linked document, including the honest corpus gap.</done>
</task>

<task type="auto">
  <name>Task 3: Point the ledger at the decision record and land the 265-04 decision as a row status</name>
  <files>data/capability-ledger.json, tests/test-265-supersede-chain.cjs</files>
  <read_first>
    - data/capability-ledger.json (as written by plan 265-01: the eight required row keys and the five status values)
    - tests/test-265-capability-ledger-schema.cjs (the schema tripwire; it currently rejects extra keys, so it must be extended to permit the new optional key rather than worked around)
    - .planning/phases/265-.../265-04-SUMMARY.md (the navigator's chosen option id and the ledger status it implies: dormant for defer-to-followup-phase, adopting for build-now-in-265, superseded for keep-sequential)
    - docs/RADAR-ABSORPTION-265.md (created in Task 2)
  </read_first>
  <action>
Add one optional ninth row key, `decision_ref`, to the ledger schema: a string path to the
document that carries the reasoning for that row. Extend
`tests/test-265-capability-ledger-schema.cjs` so `decision_ref` is permitted as an OPTIONAL key
(the eight required keys stay required, and any key outside the nine is still rejected). Do not
loosen the extra-key rejection into a general allow; that rejection is the injection fence from
plan 265-05's threat register.

Set `decision_ref` to `docs/RADAR-ABSORPTION-265.md` on every row whose disposition needed a
judgment call rather than a mechanical read. At minimum: the fork-mode-default row, the
`context: fork` dormant row, the commands-merged-into-skills dormant row, and the concurrency-
cap row.

Add two new rows recording this phase's own dispatch-shape findings, each with the eight
required keys plus `decision_ref`:
- capability handle naming the trending-to-absurd Expert-path dispatch shape, version
  `2.1.232`, domain `code`, destination `commands/trending-to-absurd.md`, status `shipped`,
  evidence naming the Phase 265 scout audit and the Brain topological query.
- capability handle naming the explore-opportunity four-leg parallelization question, version
  `2.1.232`, domain `code`, destination `lib/core/eureka/explore-chain.cjs`, and status set
  from the navigator's decision in `265-04-SUMMARY.md`: `dormant` for defer-to-followup-phase,
  `adopting` for build-now-in-265, `superseded` for keep-sequential. Evidence names
  `quality_early_stop` as the coupling and the `tests/run-all-264.sh` zero-diff arm as the
  constraint.

Also add a `dormant` row for a future reviewed `allowed-tools: Task` grant on
`commands/trending-to-absurd.md`, flagged in plan 265-04's SUMMARY, so the candidate is tracked
rather than forgotten.

Finally extend `tests/test-265-supersede-chain.cjs` with one arm: every ledger row whose
`status` is `dormant` or whose `destination` is not `none` and whose disposition required a
judgment call carries a `decision_ref` pointing at a file that exists on disk. A dangling
`decision_ref` must fail.
  </action>
  <verify>
    <automated>node tests/test-265-capability-ledger-schema.cjs &amp;&amp; node tests/test-265-supersede-chain.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-265-capability-ledger-schema.cjs` exits 0 with the ninth optional key permitted and any tenth key still rejected (prove the rejection arm by a temporary edit, then revert; record the proof in the SUMMARY).
    - `node -e "const l=require('./data/capability-ledger.json'); process.exit(l.entries.filter(e=>e.decision_ref==='docs/RADAR-ABSORPTION-265.md').length>=4?0:1)"` exits 0.
    - A ledger row exists whose `destination` is `commands/trending-to-absurd.md` with `status` `shipped`.
    - A ledger row exists whose `destination` is `lib/core/eureka/explore-chain.cjs` and whose `status` matches the mapping implied by the option id recorded in `265-04-SUMMARY.md`.
    - A `dormant` ledger row exists tracking the future `allowed-tools: Task` grant candidate for trending-to-absurd.
    - `node -e "const fs=require('fs'); const l=require('./data/capability-ledger.json'); process.exit(l.entries.every(e=>!e.decision_ref||fs.existsSync(e.decision_ref))?0:1)"` exits 0 (no dangling reference).
    - `node tests/test-265-supersede-chain.cjs` exits 0.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' data/capability-ledger.json` returns 0.
  </acceptance_criteria>
  <done>The ledger and the decision record point at each other, and the navigator's explore-opportunity choice is durable data rather than prose in a plan SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger rows to plan-time reader | `data/capability-ledger.json` is read by Claude at plan time; `decision_ref` adds a path a reader may follow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-265-23 | Tampering | Widening the ledger schema to accept arbitrary keys | mitigate | `decision_ref` is added as ONE named optional key; the extra-key rejection stays, and an acceptance criterion requires proving a tenth key still fails. |
| T-265-24 | Tampering | A `decision_ref` pointing at a path that does not exist or is attacker-supplied | mitigate | `tests/test-265-supersede-chain.cjs` asserts every non-empty `decision_ref` resolves to a file that exists on disk. Values are hand-authored in this plan, never written by `--fetch` (plan 265-05's Step 3b writes only the eight typed keys). |
| T-265-25 | Repudiation | Deleting the rotted predecessors and losing the evidence | mitigate | Supersede-never-delete, enforced by `tests/test-265-supersede-chain.cjs` and by the no-deletions git check. The false 2.1.148-159 claim and the sixteen-day drift lag are preserved on purpose (Canon Part 6). |
| T-265-26 | Information Disclosure | Canon Part 8 | accept | Documentation and JSON edits only; zero network, zero Brain, zero user data. |
</threat_model>

<verification>
1. `node tests/test-265-supersede-chain.cjs` exits 0.
2. `node tests/test-265-capability-ledger-schema.cjs` exits 0.
3. `git status --porcelain | grep -E '^ ?D'` produces no output.
4. `git ls-files --error-unmatch` succeeds for all three `.planning/` files this plan edited.
</verification>

<success_criteria>
- SEED-003 and Phase 138 are retired by marking, their bodies and their errors preserved, and W007-138 is closed with a forward pointer.
- `docs/RADAR-ABSORPTION-265.md` carries every judgment call this phase made, including the corrected destination map and the honest corpus gap.
- The ledger and the decision record cross-reference each other, and every reference resolves.
</success_criteria>

<output>
Create `.planning/phases/265-capability-radar-absorption-routing-re-scoped-supersedes-orp/265-06-SUMMARY.md` when done.
</output>
