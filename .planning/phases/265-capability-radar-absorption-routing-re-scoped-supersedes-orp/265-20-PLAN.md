---
phase: 265-capability-radar-absorption-routing-re-scoped-supersedes-orp
plan: 20
type: execute
wave: 4
depends_on: ["265-03", "265-12", "265-13"]
files_modified:
  - agents/grading.md
  - commands/deep-grade.md
  - commands/grade.md
  - tests/test-265-deep-grade-rubric.cjs
autonomous: false
requirements: [RADAR-23]
canon_parts: [3, 7, 12]

must_haves:
  truths:
    - "One rubric component count is authoritative and every file that names it agrees, BEFORE any fan-out is designed around it"
    - "Each rubric component is scored by an agent that read the room through that lens only, against a calibration table pulled once"
    - "When two component agents disagree about the same claim, the disagreement is rendered ABOVE the score instead of averaged away"
    - "The percentile and the weighted aggregate stay in the orchestrator, because neither is a per-component operation"
  artifacts:
    - path: "agents/grading.md"
      provides: "The rubric reconciled to the authoritative component set with the correction recorded"
    - path: "commands/deep-grade.md"
      provides: "A three-phase panel: calibrate once, fan per component, consolidate fail-closed with disputes above the score"
      contains: "consolidatePanel"
    - path: "tests/test-265-deep-grade-rubric.cjs"
      provides: "Rubric-consistency tripwire across all four files that name a component count, plus panel-shape checks"
  key_links:
    - from: "commands/deep-grade.md"
      to: "lib/core/eureka/grade-grant-examine.cjs"
      via: "consolidatePanel's fail-closed merge semantics copied rather than reinvented"
      pattern: "consolidatePanel"
    - from: "tests/test-265-deep-grade-rubric.cjs"
      to: "agents/grading.md"
      via: "component-count equality across every file that states one"
      pattern: "rubric"
---

<objective>
`/mos:deep-grade` is the only one of three grading surfaces with no fan-out mode, and the files
that describe its rubric disagree about how many components it has. The disagreement must be
settled FIRST, because a fan-out designed per component would freeze the wrong number into
structure.

THE DISCREPANCY, as it exists on disk right now:

- `commands/deep-grade.md:81`: "Score 5 components (Vision 20%, Problem Definition 25%,
  Feasibility 20%, Market 20%, Completeness 15%)".
- `commands/grade.md:77` independently says the same five with the same weights.
- `agents/grading.md:89-101` and its scoring table at `:144-150` say SEVEN entirely different
  components: Methodology Application 25%, Reality Check 15%, Cross-Domain Innovation 15%,
  Implementation Feasibility 15%, Evidence Quality 10%, Strategic Vision 10%, Executive Summary
  10%.
- `commands/deep-grade.md:106` adds a THIRD number in its own comparison table: "Scoring model |
  6-component | 5-component weighted".
- `references/methodology/grade.md` defines a SIX-component rubric (Problem Reality 35%, Problem
  Discovery 25%, Framework Integration 20%, Mindrian Thinking 10%, Can We Win? 5%, Is It Worth It?
  5%). That is `/mos:grade`'s STATIC local rubric and it is a genuinely different rubric, correctly
  six. It is not part of the disagreement; do not "reconcile" it away.

THE EVIDENCE THAT SETTLES IT, found while planning and to be VERIFIED not trusted:
`docs/superpowers/specs/2026-03-22-brain-mcp-toolbox-design.md:46` declares
`brain_grade_calibrate`'s output contract as
`{scores: {vision, problem, feasibility, market, completeness}, percentile, feedback[]}` and labels
it "Calibrated 5-component grading". The deep-grade path is Brain-calibrated by definition (the
command refuses to run without Brain), so the Brain tool's own return shape is the authority, and
its five keys map one-to-one onto the five components both command files already name. Two
independent command files plus the tool contract agree on five; `agents/grading.md` is the lone
outlier and no other file in the repo names its seven.

Task 1 is a BLOCKING reconciliation and it runs before Task 2 designs anything around the number.

Then the fan-out. The right model is `grade-grant`'s reviewer panel, NOT `grade --full`'s section
split, and the distinction matters: `grade --full` partitions by SECTION so inputs are disjoint (a
data partition), while deep-grade partitions by RUBRIC DIMENSION so every agent reads the SAME room
(a lens partition). If `grade --full` were the only precedent the honest verdict would be
leave-as-is, because duplicating a full room read five times is a bad trade. `grade-grant` settles
it: `commands/grade-grant.md` step 4b already dispatches `agents/grant-reviewer.md` per rubric
CATEGORY over one shared source through `runCellFanout`, it shipped, and its stated justification
is QUALITY rather than speed.

Purpose: close a genuine three-way inconsistency across the grading surfaces, on the correct
number.
Output: one authoritative rubric, and a panel that fails closed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/265-capability-radar-absorption-routing-re-scoped-supersedes-orp/265-RESEARCH-generative-redesign.md
@commands/deep-grade.md
@agents/grading.md
</context>

## Artifacts this phase produces

| Symbol / path | Kind | Notes |
|---|---|---|
| `tests/test-265-deep-grade-rubric.cjs` | new test file | Component-count equality across every file that states one, plus panel-shape arms |
| "Phase 0 / Phase 1 / Phase 2" headings in `commands/deep-grade.md` | rewritten doc sections | Calibrate once, fan per component, consolidate |
| Reconciliation note in `agents/grading.md` | new doc block | Records what changed and on what evidence |

No new runtime code. `consolidatePanel`, `runCellFanout`, `resolveFanoutCap`, `FUTURES_FANOUT_CAP`
and `resolveModel` are all pre-existing. `commands/deep-grade.md` already declares `Agent` in
`allowed-tools`, so no new grant token is needed here.

**Mirror regeneration is NOT this plan's job.** Plan 265-24 owns the second-pass generator run.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Reconcile the rubric component count before anything is built on it</name>
  <files>agents/grading.md, commands/deep-grade.md, commands/grade.md</files>
  <read_first>
    - agents/grading.md in full (192 lines), especially Phase 3's seven rubric sections at lines 89 to 101 with their weights, the scoring table at 144 to 150, the per-section feedback format ("Strong: <specific evidence> / Missing: <specific gap with calibration comparison>"), and the Never Do list (including "Give a score without specific evidence from the submission" and "never present percentiles as statistically precise").
    - commands/deep-grade.md in full (115 lines): the Brain-required refusal at line 54, the `model-profiles.cjs resolve <roomDir> grading` call at 62 to 72 and its `skip` handling, step 3's five-component sentence at line 81, step 3's `brain_grade_calibrate` and `brain_gap_assess` calls, and the comparison table at 100 to 108 including line 106's "6-component | 5-component weighted".
    - commands/grade.md lines 70 to 85: the Brain-connected delegation path that independently names the SAME five components with the same weights, and line 81's statement that the static 6-component rubric runs when Brain is not connected.
    - references/methodology/grade.md lines 43 to 95: the six-component static rubric (Problem Reality 35%, Problem Discovery 25%, Framework Integration 20%, Mindrian Thinking 10%, Can We Win? 5%, Is It Worth It? 5%). Confirm for yourself that this is a DIFFERENT rubric belonging to `/mos:grade`'s Brain-less path, not a fourth opinion about deep-grade's.
    - docs/superpowers/specs/2026-03-22-brain-mcp-toolbox-design.md line 46: `brain_grade_calibrate`'s declared output `{scores: {vision, problem, feasibility, market, completeness}, percentile, feedback[]}` labelled "Calibrated 5-component grading".
    - references/brain/query-patterns.md section 2 (`brain_grade_calibrate`), for anything it says about the returned shape.
  </read_first>
  <action>
VERIFY, then reconcile. Do not accept this plan's conclusion without running the verification.

VERIFICATION, in this order, and stop at the first authoritative answer:

1. PROBE THE LIVE CONTRACT. If Brain is reachable in this session, call
   `mcp__mindrian-brain__brain_schema` and, if available, `brain_grade_calibrate`, and record the
   ACTUAL returned `scores` keys. A live return is the strongest evidence available and it settles
   the question outright.
2. IF BRAIN IS UNREACHABLE, fall back to the declared contract at
   `docs/superpowers/specs/2026-03-22-brain-mcp-toolbox-design.md:46` plus the two independent
   command files. Record explicitly in the SUMMARY that the probe was unavailable and that the
   verdict rests on the spec plus two command files rather than on a live return.
3. SEARCH FOR A FOURTH SOURCE before concluding: `grep -rn "Cross-Domain Innovation\|Methodology
   Application\|Executive Summary" --include=*.md --include=*.cjs --include=*.json . | grep -v
   node_modules | grep -v ^./dist | grep -v ^./skills`. If `agents/grading.md`'s seven appear
   NOWHERE else, that is decisive corroboration that it is the outlier. If they DO appear elsewhere
   (a filed artifact template, a Brain-side rubric, a test), STOP and present the conflict at the
   checkpoint below instead of choosing.

EXPECTED VERDICT, to be confirmed rather than assumed: FIVE components, named
`Vision 20%, Problem Definition 25%, Feasibility 20%, Market 20%, Completeness 15%`, anchored on
`brain_grade_calibrate`'s `{vision, problem, feasibility, market, completeness}` keys.

THEN RECONCILE, three edits:

1. `agents/grading.md`. Replace the seven rubric sections at 89 to 101 and the scoring table at
   144 to 150 with the authoritative five and their weights. Preserve everything that is NOT the
   component list: the calibration discipline, the evidence-with-citations requirement, the
   per-section "Strong / Missing" feedback format, the Never Do list, and the percentile honesty
   rule. Add a short block headed "Rubric reconciliation (Phase 265)" recording what the file used
   to say, what it says now, and the evidence that decided it, so the correction is auditable
   rather than a silent overwrite.
2. `commands/deep-grade.md:106`. The comparison table row "Scoring model | 6-component |
   5-component weighted" is correct as written IF the left column means `/mos:grade`'s static
   `references/methodology/grade.md` rubric. Make that explicit in the cell so the row stops
   reading as a third opinion, for example by naming the reference file in the cell.
3. `commands/grade.md:77` and `:81`. Confirm both still agree with the authoritative five and with
   the static six respectively. Correct only what disagrees; do not rewrite what is already right.

RECORD IN THE SUMMARY: the verification path actually taken, the authoritative count, the five
component names with weights, the file that was wrong, and the explicit statement that
`references/methodology/grade.md`'s six is a DIFFERENT rubric that was deliberately left alone.
  </action>
  <decision>Which rubric is authoritative for /mos:deep-grade: the five components named by both command files and by brain_grade_calibrate's declared output contract, or the seven named only by agents/grading.md?</decision>
  <context>
Three numbers are in play across four files and one of them belongs to a different rubric entirely.
The five appear in `commands/deep-grade.md`, `commands/grade.md` AND the Brain tool's declared
output keys. The seven appear only in `agents/grading.md`. The six in
`references/methodology/grade.md` is `/mos:grade`'s separate Brain-less static rubric and is not
part of the disagreement.

This checkpoint exists because the executor must confirm the verification result before a
per-component fan-out is designed around the number, and because a fourth source discovered during
the grep would change the answer. If the verification comes back clean and matches the expected
verdict, the navigator approves and Task 2 proceeds. If it comes back ambiguous, the navigator
decides rather than the executor guessing.
  </context>
  <options>
    <option id="five-brain-anchored">
      <name>Five components, anchored on brain_grade_calibrate's output contract</name>
      <pros>Three independent sources agree. The deep-grade path is Brain-calibrated by definition, so the tool's own return shape is the natural authority. The fan-out sizes to five, inside FUTURES_FANOUT_CAP, with no batching idiom needed.</pros>
      <cons>agents/grading.md's seven components carry real assessment content (Cross-Domain Innovation, Evidence Quality) that has no home in the five unless it is folded into their definitions.</cons>
    </option>
    <option id="seven-agent-anchored">
      <name>Seven components, anchored on the agent file that actually runs</name>
      <pros>agents/grading.md is what the command tells Claude to read and follow, so it is what executes today.</pros>
      <cons>It contradicts the Brain tool's declared output keys, so a calibrated score could not be assembled from what the tool returns. It also contradicts two command files. Would need grade-grant's two-batch idiom (5 + 2) rather than a single fan.</cons>
    </option>
    <option id="stop-fourth-source-found">
      <name>Stop: the grep found a fourth source and the conflict is wider than two files</name>
      <pros>Prevents freezing a wrong number into a fan-out structure, which is the specific failure this task exists to avoid.</pros>
      <cons>Blocks Task 2 until the wider conflict is scoped, probably as its own phase.</cons>
    </option>
  </options>
  <verify>
    <automated>node tests/test-265-deep-grade-rubric.cjs</automated>
  </verify>
  <resume-signal>Select: five-brain-anchored, seven-agent-anchored, or stop-fourth-source-found.</resume-signal>
  <acceptance_criteria>
    - The SUMMARY records the verification path taken (live probe or spec fallback), the grep result for the seven component names, and the chosen option id verbatim.
    - Every file that states a deep-grade component count states the SAME count after this task: `agents/grading.md`, `commands/deep-grade.md` and `commands/grade.md`.
    - `agents/grading.md` contains a block headed `Rubric reconciliation (Phase 265)` naming the old set, the new set and the deciding evidence.
    - `agents/grading.md` retains its calibration discipline, its evidence-with-citations requirement, its Strong/Missing feedback format, its Never Do list and its percentile honesty rule (assert each by grep and list them in the SUMMARY).
    - `references/methodology/grade.md` is UNMODIFIED: `git diff --stat references/methodology/grade.md` produces no output, and the SUMMARY states why it was left alone.
    - `commands/deep-grade.md:106`'s comparison cell names `references/methodology/grade.md` so the 6-versus-5 row stops reading as a third opinion.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' agents/grading.md commands/deep-grade.md commands/grade.md` returns 0 for every file.
  </acceptance_criteria>
  <done>One component count is authoritative, verified rather than assumed, every file that names one agrees, the correction is auditable, and the separate static rubric was deliberately left intact.</done>
</task>

<task type="auto">
  <name>Task 2: Build the per-component panel with grade-grant's fail-closed consolidation</name>
  <files>commands/deep-grade.md</files>
  <read_first>
    - commands/deep-grade.md as reconciled in Task 1, in full: the Brain verification at step 1, the model resolution at step 2 (`model-profiles.cjs resolve <roomDir> grading`, the `skip` stop, the model-alias pass-through, the `inherit` case), step 3's agent delegation, step 4's Larry wrap, step 5's file offer, and the `allowed-tools` block which already contains `Agent`.
    - commands/grade-grant.md step 4b in full: how it dispatches `agents/grant-reviewer.md` per rubric CATEGORY over one shared source through `runCellFanout`, how it renders the panel, and its two-batch idiom (5 + 2) for exceeding `FUTURES_FANOUT_CAP` honestly rather than raising the cap.
    - lib/core/eureka/grade-grant-examine.cjs `consolidatePanel`: the exact merge semantics, especially the rule that "a merge collision fails CLOSED: the stricter status wins and is recorded", and how the `disputes` list is assembled and rendered ABOVE the percentage because "burying it under the score would be the false-success failure class".
    - lib/core/bono/cell-fanout.cjs lines 40 to 80: `runCellFanout`, `resolveFanoutCap`, `FUTURES_FANOUT_CAP` default 5.
    - agents/grading.md as reconciled: the per-component scoring definitions, the calibration anchor usage, and the Never Do list, because each subagent receives its own component definition verbatim from this file.
    - .planning/phases/265-.../265-RESEARCH-generative-redesign.md section 3, points 4 and 5 in full: the three-phase shape, the Completeness special case, the consistency-check requirement, and the note that grade-grant's sequential `runDebate` second half is a LARGER build to be treated as a separate task rather than folded in here.
  </read_first>
  <action>
Rewrite `commands/deep-grade.md` step 3 into three phases. Keep steps 1, 2, 4 and 5 as they are.

PHASE 0, SEQUENTIAL, ORCHESTRATOR ONLY:
- Verify Brain exactly as step 1 already does (`brain_schema`, then `get_neo4j_schema` fallback,
  then the honest refusal and stop). Unchanged.
- Resolve the model ONCE via `model-profiles.cjs resolve <roomDir> grading` and honor `skip` by
  stopping. Unchanged.
- **PULL THE CALIBRATION ANCHORS ONCE** via `brain_grade_calibrate` and pass the resulting anchor
  table to EVERY component agent. Write the reason into the file: five agents each making their own
  calibration call would be five identical Brain round-trips for one answer, and that is the main
  cost objection to this redesign. Also call `brain_gap_assess` once here, not per agent.

PHASE 1, PARALLEL FAN-OUT, one agent per rubric component, sized to the authoritative count from
Task 1. Clamp through the `resolveFanoutCap` / `FUTURES_FANOUT_CAP` idiom. If Task 1 settled on
seven, use grade-grant's two-batch idiom (5 + 2) rather than raising the cap, and say so; if it
settled on five, state that five sits exactly at the cap so no batching is needed.

EACH SUBAGENT'S CONTRACT, written into the file:
- Input: the component name, its weight, its FULL scoring definition verbatim from
  `agents/grading.md`, the shared calibration anchor table pulled in Phase 0, the room path, and the
  Never Do rules, especially "always cite specific calibration submissions" and "never present
  percentiles as statistically precise".
- Work: read the room through THAT component's lens only and score 1 to 10 with cited evidence.
  State plainly that a score without specific evidence from the submission is forbidden by the
  agent's own Never Do list.
- Returns: `{component, score, weight, evidence[], gap, calibration_comparison}`.
- Constraint: no writes, no percentile computation, no aggregate. Those are Phase 2.

**THE COMPLETENESS SPECIAL CASE, and it must be handled explicitly.** If the authoritative rubric is
the five, `Completeness 15%` is inherently cross-section and cannot be scored by an agent holding
one lens: it would produce a completeness judgment made without seeing what the other four found
missing. Handle it ONE of two ways and state which in the file: either score it in the orchestrator
AFTER the fan-out returns, using the other agents' `gap` fields as input, or give its agent the
section INVENTORY rather than a lens. Do not let it fan out naively. Whichever way is chosen, write
the reason next to it.

PHASE 2, SEQUENTIAL CONSOLIDATION, ORCHESTRATOR ONLY:
1. **CONSISTENCY CHECK, FAIL CLOSED, copying `consolidatePanel` rather than reinventing it.** If
   one agent finds a claim unsupported while another scored that same claim as validated, the panel
   SURFACES the disagreement instead of averaging it away. Name
   `lib/core/eureka/grade-grant-examine.cjs` and `consolidatePanel` literally in the file, and copy
   its rendering rule verbatim: the `disputes` list renders ABOVE the percentage, explicitly because
   burying it under the score would be the false-success failure class.
2. Compute the weighted aggregate and the percentile IN THE ORCHESTRATOR. State why: a percentile
   compares the whole submission against the whole distribution and is not a per-component
   operation. Keep the existing percentile honesty rule.
3. Keep in the orchestrator, unchanged: the Sci-Fi connection, the Visual Synthesis prompt (both
   whole-submission syntheses), the Top 3 Improvements ranking (a cross-component priority call) and
   the Professor Aronhime quote.
4. Hand the consolidated assessment to step 4's Larry wrap exactly as before. Larry still does the
   teaching; the panel does the scoring.

SCOPE FENCE, state it in the file so a future reader does not think it was forgotten: grade-grant's
panel is a HYBRID whose second half is a SEQUENTIAL `runDebate` where each reviewer reads
`previousOutput` and may emit downgrade-only challenges (downward only, evidenced to asserted to
absent, never upward, so the panel is at least as strict as its strictest reviewer). That is where
the deepest quality gain lives and it is a larger build. This plan ships the parallel extraction
plus fail-closed consolidation ONLY; name the debate as a follow-on candidate for the capability
ledger (plan 265-23 owns the ledger rows).

DISPATCH IDIOM: no `run_in_background`; explicit `subagent_type` resolving to a real `agents/*.md`
verified on disk; name the ceiling of 20 and `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`; print a
`[DEEP-GRADE] Dispatching N component agents` status block. `commands/deep-grade.md` already
declares `Agent` in `allowed-tools` and has a reviewed registry row from plan 265-12, so do NOT add
a second token and do NOT edit the registry.

Hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-265-deep-grade-rubric.cjs &amp;&amp; node tests/test-265-swarm-task-grant.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `commands/deep-grade.md` contains the literals `consolidatePanel`, `grade-grant-examine.cjs`, `brain_grade_calibrate`, `resolveFanoutCap`, `subagent_type` and `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`.
    - `grep -c 'run_in_background' commands/deep-grade.md` returns 0.
    - The file states that calibration anchors are pulled ONCE in Phase 0 and passed to every agent, with the five-round-trips reason written next to it.
    - The Completeness special case is handled explicitly and the chosen handling is stated with its reason.
    - The file states that `disputes` render ABOVE the percentage and names the false-success failure class.
    - The file states that the percentile and weighted aggregate are computed in the orchestrator, with the reason.
    - The scope fence naming `runDebate` as a follow-on candidate is present.
    - `allowed-tools` still contains exactly one dispatch token (`Agent`), no `Task` was added, and `git diff --stat data/subagent-dispatch-grants.json` produces no output.
    - `node tests/test-265-swarm-task-grant.cjs` exits 0.
    - `node tests/test-265-dispatch-shape-explicit.cjs` exits 0.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' commands/deep-grade.md` returns 0.
  </acceptance_criteria>
  <done>The panel fans one agent per component over one shared calibration pull, the consolidation copies grade-grant's fail-closed merge with disputes above the score, whole-submission operations stayed in the orchestrator, and the debate half is fenced as a named follow-on rather than half-built.</done>
</task>

<task type="auto">
  <name>Task 3: Tripwire rubric agreement across every file that states a count</name>
  <files>tests/test-265-deep-grade-rubric.cjs</files>
  <read_first>
    - agents/grading.md, commands/deep-grade.md and commands/grade.md as edited in Tasks 1 and 2.
    - references/methodology/grade.md's six-component rubric, which the test must deliberately EXCLUDE from the equality check, since it is a different rubric.
    - tests/test-265-dispatch-shape-explicit.cjs from plan 265-04 (arm b already checks `subagent_type` resolution repo-wide, so this test must not duplicate it).
    - tests/run-all-265.sh (plan 265-01) for the glob pattern and the no-em-dash fence.
  </read_first>
  <action>
Create `tests/test-265-deep-grade-rubric.cjs`, a plain Node text-analysis tripwire. Four arms.

1. COUNT AGREEMENT ARM (the load-bearing one). Extract the stated deep-grade component count from
   each of `agents/grading.md`, `commands/deep-grade.md` and `commands/grade.md` by matching the
   phrase pattern `N component` (and `N rubric section`) with a case-insensitive regex, and assert
   all extracted values are EQUAL. Print each file's extracted value on every run so the agreement
   is visible, not just asserted. Explicitly EXCLUDE `references/methodology/grade.md` from this
   arm and put a comment saying why: it defines `/mos:grade`'s separate Brain-less static rubric.
2. NAME AGREEMENT ARM. Extract the component NAMES from `agents/grading.md`'s scoring table and
   from `commands/deep-grade.md`'s step 3 sentence, and assert the two sets are identical after
   normalizing case and whitespace. A matching count with mismatched names is the subtler version of
   the same bug and this arm is what catches it.
3. WEIGHT SUM ARM. Extract the per-component weights from `agents/grading.md`'s scoring table and
   assert they sum to 100. A reconciliation that drops a component and forgets to redistribute its
   weight is an easy mistake and it silently deflates every grade.
4. PANEL SHAPE ARM. Assert `commands/deep-grade.md` contains `consolidatePanel`, contains a
   statement that disputes render above the score, contains zero occurrences of
   `run_in_background`, and that the byte offset of `brain_grade_calibrate` is BEFORE the byte
   offset of `Dispatching` (calibration is pulled once, before the fan).

Print pass and fail counts, print the extracted values on every run, `process.exit(1)` on any
failure. Header comment states the three-numbers-in-four-files finding in two sentences so the next
reader knows what this test is defending.

Hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-265-deep-grade-rubric.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-265-deep-grade-rubric.cjs` exits 0 with four arms reported and the extracted per-file counts printed.
    - The test explicitly excludes `references/methodology/grade.md` and carries a comment explaining why.
    - Arm 3 asserts the weights sum to exactly 100.
    - Negative proof recorded in the SUMMARY: temporarily changing `commands/deep-grade.md`'s count to a different number makes the test exit 1 naming arm 1; reverted, it exits 0.
    - Negative proof recorded in the SUMMARY: temporarily renaming one component in `agents/grading.md` makes the test exit 1 naming arm 2; reverted, it exits 0.
    - `bash tests/run-all-265.sh` discovers the new file.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' tests/test-265-deep-grade-rubric.cjs` returns 0.
  </acceptance_criteria>
  <done>Count, names and weights are pinned across every file that states them, the separate static rubric is deliberately excluded, and the panel's calibrate-once ordering is asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Brain to grading panel | Calibration anchors cross once, in the orchestrator; only generic methodology handles go toward Brain |
| component agent to room | Every agent reads the SAME room through one lens; this is a lens partition, not a data partition |
| panel to navigator | The rendered score is what the navigator acts on, so a suppressed disagreement is a correctness failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-265-94 | Tampering | Freezing the wrong component count into the fan-out structure | mitigate | Task 1 is a BLOCKING reconciliation with a verification procedure and a stop option when a fourth source is found; Task 3 arms 1 through 3 pin count, names and weight sum afterwards. |
| T-265-95 | Repudiation | Averaging away a disagreement between two component agents and reporting a clean score | mitigate | Phase 2 copies `consolidatePanel`'s fail-closed merge and renders `disputes` ABOVE the percentage, the rule grade-grant adopted explicitly because burying it is the false-success class. |
| T-265-96 | Denial of Service | Five identical `brain_grade_calibrate` round-trips, one per agent | mitigate | Phase 0 pulls the anchors once and passes them to every agent, with the reason written next to the instruction. |
| T-265-97 | Spoofing | A Completeness score produced without seeing what the other components found missing | mitigate | Task 2 forbids naive fan-out of Completeness and requires one of two explicit handlings, each with a stated reason. |
| T-265-98 | Elevation of Privilege | Adding a second dispatch token to a command that already holds one | mitigate | `commands/deep-grade.md` keeps its single pre-existing `Agent` grant, which plan 265-12 recorded as a reviewed row; the acceptance criteria assert no `Task` was added and the registry was not edited. |
| T-265-99 | Information Disclosure | Canon Part 8 | accept | No egress change. `brain_grade_calibrate` and `brain_gap_assess` are the same calls the command already makes, moved to the orchestrator and made once instead of N times, which strictly reduces traffic. |
</threat_model>

<verification>
1. `node tests/test-265-deep-grade-rubric.cjs` exits 0 with all four arms passing.
2. `node tests/test-265-dispatch-shape-explicit.cjs` exits 0.
3. `node tests/test-265-swarm-task-grant.cjs` exits 0.
4. `git diff --stat references/methodology/grade.md data/subagent-dispatch-grants.json` produces no output.
5. `node scripts/build-connector-registry.cjs --check` exits 0.
6. `node scripts/check-shape-declaration.cjs` reports no new violation for `commands/deep-grade.md`.
</verification>

<success_criteria>
- One component count is authoritative, verified against the Brain tool's own contract, and every file that states one agrees.
- The panel fans per component over a single shared calibration pull and fails closed on disagreement, with disputes rendered above the score.
- Whole-submission operations stayed in the orchestrator and the sequential debate half is fenced as a named follow-on rather than half-built.
</success_criteria>

<output>
Create `.planning/phases/265-capability-radar-absorption-routing-re-scoped-supersedes-orp/265-20-SUMMARY.md` when done.
</output>
