---
phase: 265-capability-radar-absorption-routing-re-scoped-supersedes-orp
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/mindrian-mcp-server.cjs
  - docs/research/PLATFORM-INDEPENDENCE-STRATEGY.md
  - docs/research/MCP-APPS-STRATEGIC-RESEARCH.md
  - docs/LAWRENCE-BRIEFING-v1.6.3.md
autonomous: true
requirements: [RADAR-17]
canon_parts: [7, 11]

must_haves:
  truths:
    - "No shipped comment or doc states an MCP tool count or an eager-load token budget that the wire contradicts"
    - "The corrected facts point at the test that measures them rather than being re-typed as a new frozen literal that will rot again"
    - "A versioned briefing document is corrected with an annotation rather than silently rewritten"
    - "The one counted fact living in a Phase 266 file is handed to Phase 266 instead of being edited across the phase boundary"
  artifacts:
    - path: "bin/mindrian-mcp-server.cjs"
      provides: "A header comment that describes the four registration seams and defers the live numbers to the test that measures them"
  key_links:
    - from: "bin/mindrian-mcp-server.cjs"
      to: "tests/test-234-tool-description-floor.cjs"
      via: "the header pointing at the measurement instead of restating a literal"
      pattern: "test-234-tool-description-floor"
    - from: "bin/mindrian-mcp-server.cjs"
      to: "lib/mcp/register-core-tools.cjs"
      via: "the auto-discovery seam named as the mechanism by which the count grows silently"
      pattern: "register-core-tools"
---

<objective>
**SCOPE COORDINATION.** Phase 266 (MCP Layer Correctness Fixes) owns four MCP defects so they can
ship on their own schedule: MCPFIX-01 (`lib/mcp/runtime-instructions.cjs` and
`lib/mcp/no-instructions.test.cjs`), MCPFIX-02 (`lib/mcp/tool-router.cjs` and
`lib/mcp/larry-context.cjs`), MCPFIX-03 (`lib/core/mcp-dep-heal.cjs`) and MCPFIX-04
(`tests/test-234-tool-description-floor.cjs`). This plan touches NONE of those files. It closes the
one thing left over: the counted facts that are simply WRONG in shipped comments and docs, which no
MCPFIX covers.

WHAT IS WRONG, all measured on the wire on 2026-08-27:

- `bin/mindrian-mcp-server.cjs:7-8`, repeated at `:135`: "hierarchical tool router (9 tools covering
  64 CLI commands) to stay under 7000 token budget". The router contributes 11 tools; the SERVER
  ships 36 across four registration seams; and the measured description-plus-schema total is about
  7,062 tokens, so the stated budget is already breached and nothing checks it.
- `docs/research/PLATFORM-INDEPENDENCE-STRATEGY.md:207` and
  `docs/research/MCP-APPS-STRATEGIC-RESEARCH.md:525`: both say "9 tools".
- `docs/LAWRENCE-BRIEFING-v1.6.3.md:26`: "49 MCP tools" against an actual 36 plus 6.

The mechanism behind the growth is worth naming in the fix rather than only the number:
`lib/mcp/register-core-tools.cjs:56` reads `lib/mcp/tools/` at load and registers every `*.cjs` it
finds, so a new module adds eager tokens to every session with no gate, no budget check and no
review step.

**THE BEST FORM OF THE FIX IS NOT A NEW NUMBER.** Re-typing 36 into a comment today just schedules
the next drift. The in-repo precedent is Canon Part 11's surface count, which CLAUDE.md states is
"enumerated from disk at run time ... never a frozen literal". So the header points at the
measurement rather than restating it.

Purpose: these counted facts are the exact rot pattern Phase 265 exists to end, and they are the
cheapest possible instance of it.
Output: a header that will still be true next year, three corrected docs, and one item handed
across the phase boundary rather than edited across it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/265-capability-radar-absorption-routing-re-scoped-supersedes-orp/265-RESEARCH-mcp-layer-audit.md
@bin/mindrian-mcp-server.cjs
</context>

## Artifacts this phase produces

| Symbol / path | Kind | Notes |
|---|---|---|
| `--tool-count` CLI arm on `bin/mindrian-mcp-server.cjs` | optional new arm | Executor's judgment; see Task 1 |

No new module and no runtime behavior change. Everything here is a comment or a doc line.

**Mirror regeneration is NOT this plan's job.** Nothing here lives under `commands/`.

<tasks>

<task type="auto">
  <name>Task 1: Retire the frozen counted facts in the server header</name>
  <files>bin/mindrian-mcp-server.cjs</files>
  <read_first>
    - bin/mindrian-mcp-server.cjs lines 1 to 60 (the header comment carrying the "9 tools covering 64 CLI commands" and "under 7000 token budget" claims at lines 7 to 8), the repetition at about line 135, the `ensureDepsPresent` call at line 53 (READ ONLY, Phase 266 MCPFIX-03 territory), the two inline `server.tool(` registrations at lines 154 and 166 (`detect_dual_path`, `extract_shallow`) and `createServer()` at about line 196, so the header can describe the four registration seams accurately.
    - lib/mcp/register-core-tools.cjs line 56: the auto-discovery loop that registers every `lib/mcp/tools/*.cjs` it finds. This is the mechanism by which the count grows silently and it belongs in the corrected header.
    - lib/mcp/tool-router.cjs's 11 `server.tool(` calls and lib/mcp/app-views.cjs's three surface-gated MCP Apps registrations, so "four seams" is a verified statement rather than a repeated one. Do NOT edit either file: `lib/mcp/tool-router.cjs` is Phase 266 MCPFIX-02 territory.
    - CLAUDE.md's Canon Part 11 paragraph, specifically "The surface count is enumerated from disk at run time (currently 126 declared + 5 skill-exempt), never a frozen literal". This is the precedent the fix follows.
    - .planning/REQUIREMENTS.md, the MCPFIX-04 entry, so the header's pointer names the test that will actually be doing the measuring after Phase 266 ships.
  </read_first>
  <action>
Rewrite the header comment at lines 7 to 8 and its repetition at about line 135. The new text must
state:

- the router (`lib/mcp/tool-router.cjs`) contributes 11 hierarchical tools over roughly 64 CLI
  commands;
- the SERVER's full surface is larger because three other seams register beside the router:
  `lib/mcp/register-core-tools.cjs` auto-discovering `lib/mcp/tools/*.cjs`, two inline registrations
  in this file, and the surface-gated MCP Apps views in `lib/mcp/app-views.cjs`;
- `lib/mcp/register-core-tools.cjs:56` registers every new `tools/*.cjs` with no gate, which is HOW
  the count grows silently and why a frozen literal here rots;
- the live count and the eager-load token total are MEASURED by
  `tests/test-234-tool-description-floor.cjs`, which Phase 266's MCPFIX-04 expands to cover every
  registered tool and to state its own coverage.

**DO NOT type a new frozen count of 36 or 42 anywhere in this file.** Point at the measurement. That
is the whole fix; replacing a wrong literal with a right one only resets the clock.

OPTIONAL, executor's judgment: if a `--tool-count` CLI arm on this entry point would be genuinely
useful to an operator (it would print the live count without a test run), add it and say so in the
SUMMARY. If it would only duplicate what `/mos:doctor` will report once plan 265-23's MCP surface
organ lands, say that instead and skip it. Either answer is acceptable; an unstated choice is not.

Do NOT edit `ensureDepsPresent` or anything on the dependency-heal path: Phase 266's MCPFIX-03 owns
it.

**ONE ITEM IS HANDED ACROSS THE PHASE BOUNDARY, NOT EDITED.** `lib/mcp/tool-router.cjs:5` carries
the same two false claims ("9 tools", "under 7000 token budget") and that file belongs to Phase
266's MCPFIX-02. Do NOT edit it. Record it in the SUMMARY as a Phase 266 coordination item with the
exact line and the exact replacement text you would have written, so plan 266-02 can apply it in one
line while it is already in that file. Plan 265-23 also records it as a ledger row.

Hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node lib/mcp/no-instructions.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '9 tools' bin/mindrian-mcp-server.cjs` returns 0 and `grep -c 'under 7000 token budget' bin/mindrian-mcp-server.cjs` returns 0.
    - The header names `register-core-tools.cjs`, `app-views.cjs` and `tests/test-234-tool-description-floor.cjs`.
    - The header contains no frozen literal `36` or `42` used as a tool count (grep and read the matches; incidental numbers elsewhere are fine and should be listed in the SUMMARY).
    - `node lib/mcp/no-instructions.test.cjs` exits 0 (the server still boots after the header edit).
    - `git diff --stat lib/mcp/tool-router.cjs lib/mcp/larry-context.cjs lib/mcp/runtime-instructions.cjs lib/core/mcp-dep-heal.cjs tests/test-234-tool-description-floor.cjs` produces no output.
    - The SUMMARY records the `--tool-count` decision with its reason, and records the `lib/mcp/tool-router.cjs:5` handoff with the exact replacement text.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' bin/mindrian-mcp-server.cjs` returns 0.
  </acceptance_criteria>
  <done>The server header describes the four registration seams truthfully, defers the live numbers to the test that measures them, names the mechanism by which the count grows silently, and hands the one Phase 266 file's copy of the same defect across the boundary rather than editing it.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the three docs that re-typed the tool count</name>
  <files>docs/research/PLATFORM-INDEPENDENCE-STRATEGY.md, docs/research/MCP-APPS-STRATEGIC-RESEARCH.md, docs/LAWRENCE-BRIEFING-v1.6.3.md</files>
  <read_first>
    - docs/research/PLATFORM-INDEPENDENCE-STRATEGY.md around line 207 and docs/research/MCP-APPS-STRATEGIC-RESEARCH.md around line 525: read enough surrounding context in each to judge whether the "9 tools" figure is LOAD-BEARING to the argument (for example feeding a context-budget calculation) or merely incidental. The repair differs.
    - docs/LAWRENCE-BRIEFING-v1.6.3.md around line 26 ("49 MCP tools"), plus enough of the document's front matter to confirm it is a VERSIONED briefing tied to v1.6.3 rather than a living doc.
    - .planning/phases/265-.../265-RESEARCH-mcp-layer-audit.md D-1, D-6, D-8 and recommendation R-10.
    - CHANGELOG.md around line 2145 ("SEED-003 A1's Brain portion remains dormant"), superseded by Phase 127-00 commit `5308e678`. Read it, do not edit it.
  </read_first>
  <action>
THREE doc corrections and ONE deliberate non-edit.

1. `docs/research/PLATFORM-INDEPENDENCE-STRATEGY.md:207` and
   `docs/research/MCP-APPS-STRATEGIC-RESEARCH.md:525`. Both say "9 tools". Correct each to say the
   router contributes 11 and the server's full surface is larger and is enumerated by
   `tests/test-234-tool-description-floor.cjs`. If the 9 is LOAD-BEARING to the surrounding argument,
   repair the argument too, not only the number, and state in the SUMMARY what the argument was and
   how it changes. If it is incidental, say that in the SUMMARY so the reader knows the check was
   made rather than skipped.

2. `docs/LAWRENCE-BRIEFING-v1.6.3.md:26` says "49 MCP tools"; the actual is 36 plus 6. This is a
   VERSIONED briefing document, so do not silently rewrite history. Correct the number AND add a
   short bracketed annotation recording that it was corrected in Phase 265 with the measured figure
   and the date. Record in the SUMMARY that this is a versioned doc and why the correction was
   annotated rather than rewritten.

3. `CHANGELOG.md:2145`. Do NOT edit it. It is release-managed history and the entry was true when
   written; it is superseded by Phase 127-00 commit `5308e678`, not wrong. Record it in the SUMMARY
   as a superseded historical statement so plan 265-23 adds it to the capability ledger as
   `superseded` with its evidence. State this as a deliberate decision so it does not read as an
   oversight.

Hyphens only, no em-dashes, in every edited file.
  </action>
  <verify>
    <automated>test "$(grep -l '9 tools' docs/research/PLATFORM-INDEPENDENCE-STRATEGY.md docs/research/MCP-APPS-STRATEGIC-RESEARCH.md 2>/dev/null | wc -l)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '9 tools' docs/research/PLATFORM-INDEPENDENCE-STRATEGY.md` returns 0 and the same for `docs/research/MCP-APPS-STRATEGIC-RESEARCH.md`.
    - Both corrected passages name `tests/test-234-tool-description-floor.cjs` as the enumeration source rather than stating a new frozen count.
    - `grep -c '49 MCP tools' docs/LAWRENCE-BRIEFING-v1.6.3.md` returns 0, and the corrected line carries a bracketed Phase 265 annotation with the measured figure and the date.
    - `git diff --stat CHANGELOG.md` produces no output, and the SUMMARY records the deliberate decision plus the ledger handoff to plan 265-23.
    - The SUMMARY states, for each of the two research docs, whether the count was load-bearing to its argument and what was done about it.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' docs/research/PLATFORM-INDEPENDENCE-STRATEGY.md docs/research/MCP-APPS-STRATEGIC-RESEARCH.md docs/LAWRENCE-BRIEFING-v1.6.3.md` returns 0 for every file.
  </acceptance_criteria>
  <done>No doc states a tool count the wire contradicts, the versioned briefing is annotated rather than rewritten, the arguments those numbers fed were checked rather than assumed incidental, and the one release-managed file was deliberately left alone with a ledger handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool registration to context budget | `register-core-tools.cjs` auto-registers every new `tools/*.cjs`, adding eager tokens to every session with no gate |
| Phase 265 to Phase 266 | Both phases touch the MCP layer; the file split is the coordination mechanism |
| release-managed history to correction | `CHANGELOG.md` entries were true when written; superseding is not the same as wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-265-77 | Repudiation | A shipped comment stating a tool count and a token budget the wire contradicts | mitigate | Task 1 removes both claims and points at the measuring test; Task 2 corrects the three docs that re-typed the count. |
| T-265-78 | Repudiation | Re-typing a new frozen count that rots again | mitigate | Task 1 forbids a new frozen literal and follows the Canon Part 11 run-time-enumeration precedent; the acceptance criteria assert no `36` or `42` is used as a count. |
| T-265-79 | Tampering | Silently rewriting a versioned briefing document's history | mitigate | Task 2 requires a bracketed correction annotation with the measured figure and date rather than an in-place overwrite. |
| T-265-80 | Tampering | Editing a Phase 266 file across the phase boundary and colliding with a fast-shipping fix | mitigate | The `lib/mcp/tool-router.cjs:5` copy of the same defect is HANDED to Phase 266 with its exact replacement text; the acceptance criteria assert a zero-line diff across all five Phase 266 files. |
| T-265-81 | Information Disclosure | Canon Part 8 | accept | No egress change. Every edit here is a comment or a doc line. |
</threat_model>

<verification>
1. `node lib/mcp/no-instructions.test.cjs` exits 0.
2. `grep -c '9 tools' bin/mindrian-mcp-server.cjs docs/research/PLATFORM-INDEPENDENCE-STRATEGY.md docs/research/MCP-APPS-STRATEGIC-RESEARCH.md` returns 0 for every file.
3. `git diff --stat CHANGELOG.md lib/mcp/tool-router.cjs lib/mcp/larry-context.cjs lib/mcp/runtime-instructions.cjs lib/core/mcp-dep-heal.cjs tests/test-234-tool-description-floor.cjs` produces no output.
4. `LC_ALL=C.UTF-8 grep -cP '\x{2014}' bin/mindrian-mcp-server.cjs docs/research/PLATFORM-INDEPENDENCE-STRATEGY.md docs/research/MCP-APPS-STRATEGIC-RESEARCH.md docs/LAWRENCE-BRIEFING-v1.6.3.md` returns 0 for every file.
</verification>

<success_criteria>
- No shipped comment or doc states an MCP tool count or token budget the wire contradicts.
- The corrected facts point at the measurement rather than resetting the rot clock with a new literal.
- The one copy of the same defect living in a Phase 266 file is handed across with its replacement text instead of edited across the boundary.
</success_criteria>

<output>
Create `.planning/phases/265-capability-radar-absorption-routing-re-scoped-supersedes-orp/265-17-SUMMARY.md` when done.
</output>
