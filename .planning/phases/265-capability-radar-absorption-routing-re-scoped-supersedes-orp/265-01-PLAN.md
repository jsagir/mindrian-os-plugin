---
phase: 265-capability-radar-absorption-routing-re-scoped-supersedes-orp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/run-all-265.sh
  - tests/test-265-capability-ledger-schema.cjs
  - tests/test-265-ledger-freshness.cjs
  - data/capability-ledger.json
  - lib/core/doctor/capability-ledger-module.cjs
  - data/doctor-modules.json
autonomous: true
requirements: [RADAR-01, RADAR-02]
canon_parts: [6, 7, 8]

must_haves:
  truths:
    - "A machine-readable capability ledger exists at data/capability-ledger.json and every row carries capability, version, date, leverage, destination, status, evidence"
    - "The ledger declares ledger_covers.from and ledger_covers.to so freshness has an unambiguous anchor"
    - "A developer running the freshness tripwire on a stale ledger gets a non-zero exit, not a green pass"
    - "node scripts/doctor.cjs --acceptance surfaces ledger staleness through a registered doctor module"
    - "bash tests/run-all-265.sh discovers and runs every tests/test-265-* file and fails when it discovers zero"
  artifacts:
    - path: "data/capability-ledger.json"
      provides: "The living capability ledger, backfilled 2.1.128 to the installed Claude Code version"
      contains: "ledger_covers"
    - path: "tests/test-265-capability-ledger-schema.cjs"
      provides: "Row-shape and enum validation for every ledger row"
    - path: "tests/test-265-ledger-freshness.cjs"
      provides: "Version-lag tripwire against installed claude --version"
    - path: "lib/core/doctor/capability-ledger-module.cjs"
      provides: "check(ctx) organ that reports ledger staleness in doctor --acceptance"
    - path: "tests/run-all-265.sh"
      provides: "Phase 265 glob-discovery aggregator with a found-eq-0 guard and no-em-dash fence"
  key_links:
    - from: "data/doctor-modules.json"
      to: "lib/core/doctor/capability-ledger-module.cjs"
      via: "modules[] runner entry"
      pattern: "capability-ledger-module\\.cjs"
    - from: "tests/test-265-ledger-freshness.cjs"
      to: "data/capability-ledger.json"
      via: "ledger_covers.to compared against claude --version"
      pattern: "ledger_covers"
---

<objective>
Build the ledger that cannot rot. Phase 138 died because its capability ledger was a markdown
table inside one phase's CONTEXT.md, invisible to every tool. This plan replaces that with a
machine-readable ledger plus a freshness tripwire that fails a build when the ledger falls
behind the installed Claude Code version.

Purpose: this is the point of the phase. Everything else in Phase 265 is proof the ledger
produces action; this plan is the mechanism that keeps producing action after the phase ships.
Output: data/capability-ledger.json, two tripwire tests, a doctor organ module, and the phase
test aggregator every other plan verifies against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/265-capability-radar-absorption-routing-re-scoped-supersedes-orp/265-RESEARCH.md
@CLAUDE.md
</context>

## Artifacts this phase produces

New symbols and files introduced by this plan (not pre-existing, do not flag as drift):

| Symbol / path | Kind | Notes |
|---|---|---|
| `data/capability-ledger.json` | new file | Top-level keys `schema_version`, `ledger_covers`, `entries` |
| `ledger_covers` | new JSON key | `{ from, to, fetched_at, installed_claude_version }` |
| `entries[]` row keys | new JSON keys | `capability`, `version`, `date`, `domain`, `leverage`, `destination`, `status`, `evidence` |
| `status` enum values | new enum | `dormant`, `adopting`, `shipped`, `superseded`, `no-op` |
| `tests/test-265-capability-ledger-schema.cjs` | new test file | |
| `tests/test-265-ledger-freshness.cjs` | new test file | |
| `tests/run-all-265.sh` | new harness | |
| `lib/core/doctor/capability-ledger-module.cjs` | new module | exports `check(ctx)` |
| `capability-ledger` | new doctor module id | new row in `data/doctor-modules.json` `modules[]` |
| `MINDRIAN_LEDGER_MAX_VERSION_LAG` | new env var | integer patch-version lag threshold, default 40 |
| `TEST_265_ALLOW_PENDING` | new env var | harness escape used by waves 1 and 2 only |

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 265 test aggregator</name>
  <files>tests/run-all-265.sh</files>
  <read_first>
    - tests/run-all-264.sh (the donor pattern: glob discovery, found-eq-0 guard, run/run_may_skip helpers, no-em-dash fence with the rc greater-than-or-equal-2 SCAN BROKE arm)
    - CLAUDE.md, Verification section (phase suites are bash tests/run-all-N.sh)
  </read_first>
  <action>
Create tests/run-all-265.sh by cloning the structure of tests/run-all-264.sh. Keep these
load-bearing properties verbatim in shape:

1. `set -uo pipefail`, ROOT resolution via BASH_SOURCE, `cd "$ROOT"`.
2. A `PREFIX="${TEST_265_PREFIX:-tests/test-265-}"` variable so the found-eq-0 guard is
   provable without editing the file.
3. `run` and `run_may_skip` helpers copied from the donor. Use bare `node "$t"` for .cjs
   discovery, not `node --test` - every Phase 265 test is a plain script that throws or calls
   process.exit(1).
4. Glob discovery over `"$PREFIX"*.cjs` and `"$PREFIX"*.sh`, skipping the runner itself.
5. The `if [ $found -eq 0 ]` guard that exits 1. Do not soften it.
6. Explicit always-present gate lines: `node scripts/build-connector-registry.cjs --check`
   (Canon Part 11 born-wired gate) and `node scripts/build-skill-mirrors.cjs --check`
   (the commands-to-mirrors drift gate). Both already exist and both already support --check.
7. A no-em-dash fence over the Phase 265 target list, cloned including the SCAN BROKE arm.
   EMDASH_TARGETS for this phase: data/capability-ledger.json,
   lib/core/doctor/capability-ledger-module.cjs, lib/mcp/gate-render.cjs, commands/act.md,
   commands/persona.md, commands/grade.md, commands/radar.md,
   commands/trending-to-absurd.md, commands/explore-opportunity.md,
   references/capability-radar/capabilities-index.md,
   references/capability-radar/changelog-cache.md, docs/RADAR-ABSORPTION-265.md, and every
   tests/test-265-* file plus tests/run-all-265.sh.
8. The missing-target escape is `TEST_265_ALLOW_PENDING` (not the donor's name). Waves 1 and 2
   run with `TEST_265_ALLOW_PENDING=1`; plan 265-06 runs the phase gate WITHOUT it, which is
   what proves every target file actually exists.

House rules: bash only, hyphens only, no em-dashes, no emoji. chmod +x the file.
  </action>
  <verify>
    <automated>chmod +x tests/run-all-265.sh &amp;&amp; TEST_265_PREFIX=tests/test-265-nonexistent- bash tests/run-all-265.sh; test $? -ne 0 &amp;&amp; echo "FOUND-EQ-0 GUARD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `TEST_265_PREFIX=tests/test-265-nonexistent- bash tests/run-all-265.sh` exits non-zero and prints a "no ... files discovered" line (the found-eq-0 guard is real, not decorative).
    - `grep -c 'TEST_265_ALLOW_PENDING' tests/run-all-265.sh` returns at least 1.
    - `grep -c 'build-skill-mirrors.cjs --check' tests/run-all-265.sh` returns at least 1.
    - `grep -c 'build-connector-registry.cjs --check' tests/run-all-265.sh` returns at least 1.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' tests/run-all-265.sh` returns 0.
    - `test -x tests/run-all-265.sh` succeeds.
  </acceptance_criteria>
  <done>tests/run-all-265.sh exists, is executable, discovers by glob, fails on zero discovery, and carries the ALLOW_PENDING escape plus both standing gate lines.</done>
</task>

<task type="auto">
  <name>Task 2: Build data/capability-ledger.json and its schema tripwire</name>
  <files>data/capability-ledger.json, tests/test-265-capability-ledger-schema.cjs</files>
  <read_first>
    - .planning/phases/265-.../265-RESEARCH.md sections 4a, 4b, 4c, 4d, 4e and the "State of the Art" table (these ARE the seed rows; do not re-derive them from the changelog)
    - .planning/phases/265-.../265-RESEARCH.md Pitfall 4 (do NOT copy 138-CONTEXT.md's 14-row table forward; reuse its schema thinking, not its rows)
    - data/command-registry.json (the sibling machine-readable data file this joins in data/)
    - data/doctor-modules.json header comment (the "hand-maintained registry with a $schema_note" convention this file follows)
  </read_first>
  <action>
Create data/capability-ledger.json as a hand-maintained, machine-readable ledger. Top-level
shape: `$schema_note` (one paragraph explaining what this file is, why it lives in data/ and
not .planning/, and that a markdown table is the documented root cause of the Phase 138 rot),
`schema_version` set to 1, `phase` set to
"265-capability-radar-absorption-routing-re-scoped-supersedes-orp", `canon_parts` [6, 7, 8],
`ledger_covers`, and `entries`.

`ledger_covers` carries exactly four keys: `from` set to "2.1.128" (the newest entry the old
changelog-cache reached), `to` set to the newest screened version in `entries`, `fetched_at`
set to the ISO date the executor writes the file, and `installed_claude_version` set to the
literal output prefix of `claude --version` at write time (currently 2.1.247; read it, do not
assume 2.1.246 - the research baseline already moved by one version, which is exactly the
staleness this ledger exists to catch).

Every row in `entries` carries these eight keys and no others: `capability` (short handle
string), `version` (the Claude Code or SDK version string that shipped it), `date` (ISO date
or null when the changelog gives none), `domain` (one of models, code, desktop_cowork,
plugins_mcp, visualization - the same five domains commands/radar.md already uses), `leverage`
(one sentence on what it buys MindrianOS), `destination` (a repo path or the literal string
"none"), `status` (one of dormant, adopting, shipped, superseded, no-op), and `evidence` (a
citation string naming the changelog version, the docs URL, or the file:line that verified it).

Backfill only screened-relevant entries, not the whole changelog. Seed from the research's own
verified tables. The required minimum row set, with the status each must carry:

- Fork mode on by default plus Agent tool `run_in_background` removal, version 2.1.232,
  destination commands/act.md commands/persona.md commands/grade.md, status adopting.
- Concurrent-subagent cap of 20 via CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS, version 2.1.217,
  destination commands/act.md swarm sizing, status adopting.
- Agent tool clear error on missing subagent_type, version 2.1.235, destination the three
  swarm commands' dispatch prose, status adopting.
- SDK titled enum schemas (TitledSingleSelectEnumSchemaSchema and
  TitledMultiSelectEnumSchemaSchema), version "sdk-1.29.0", destination lib/mcp/gate-render.cjs,
  status adopting.
- MCP interrupted-tool-call explicit error, version 2.1.246, destination none, status no-op.
- Elicitation form-height fix, version 2.1.239, destination none, status no-op, evidence noting
  the plugin emits a single-property form so it cannot bite.
- Elicitation URL-length fix, version 2.1.238, destination none, status no-op, evidence noting
  the plugin uses form mode only.
- Subagent nesting depth default raised to 3, version 2.1.219, destination none, status no-op,
  evidence noting agents/framework-runner.md's no-nesting rule is now a design choice.
- 200-per-session spawn cap removed, version 2.1.224, status no-op.
- Restricted-subagent-model warning, version 2.1.223, status no-op.
- Parallel Bash batch failure isolation, version 2.1.232, status no-op.
- Task tool `mode` parameter deprecated, version 2.1.212, status no-op.
- Agent tool clear error on an empty resolved tools list, version 2.1.208, status no-op.
- Skills `context: fork` running in background by default, version 2.1.218, destination none,
  status dormant, leverage naming eureka's fire-and-return flow as the candidate.
- Custom commands merged into skills, version "docs-current", destination none, status dormant,
  leverage noting the 113-command plus 126-mirror duplication may be re-examinable and that
  build-skill-mirrors.cjs line 10 records a Windows compositing rationale that may now be moot.
- CLAUDE_CODE_FORK_SUBAGENT polarity inversion (was an opt-in for external builds, is now
  `0` to opt out or `1` to opt in for headless and SDK), version 2.1.232, destination
  references/capability-radar/capabilities-index.md, status adopting.

Then create tests/test-265-capability-ledger-schema.cjs as a plain Node script (no node:test,
matching the phase harness contract). It must: parse the JSON with JSON.parse and never eval;
assert schema_version is 1; assert ledger_covers has all four keys and that `from` and `to`
are dotted version strings; assert entries is a non-empty array; assert every row has exactly
the eight required keys with no extras and no missing; assert `status` is one of the five
enum values and `domain` is one of the five domain values; assert `evidence` is a non-empty
string on every row (a row with no citation is the rot pattern re-entering); and assert that
`ledger_covers.to` equals the highest `version` among entries whose version matches the
Claude-Code dotted-triple pattern. On any failure print the offending row index and key and
call process.exit(1).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('data/capability-ledger.json','utf8'))" &amp;&amp; node tests/test-265-capability-ledger-schema.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-265-capability-ledger-schema.cjs` exits 0.
    - `node -e "const l=require('./data/capability-ledger.json'); process.exit(l.entries.length>=16?0:1)"` exits 0 (all sixteen named seed rows present).
    - `node -e "const l=require('./data/capability-ledger.json'); const s=new Set(l.entries.map(e=>e.status)); process.exit([...s].every(v=>['dormant','adopting','shipped','superseded','no-op'].includes(v))?0:1)"` exits 0.
    - `node -e "const l=require('./data/capability-ledger.json'); process.exit(l.ledger_covers.from==='2.1.128'&&/^\d+\.\d+\.\d+$/.test(l.ledger_covers.to)?0:1)"` exits 0.
    - `node -e "const l=require('./data/capability-ledger.json'); process.exit(l.entries.every(e=>typeof e.evidence==='string'&&e.evidence.length>0)?0:1)"` exits 0.
    - A row whose `destination` is `lib/mcp/gate-render.cjs` exists with `status` `adopting`.
    - A row whose `version` is `2.1.232` exists and whose `capability` names `run_in_background`.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' data/capability-ledger.json tests/test-265-capability-ledger-schema.cjs` returns 0 for both files.
  </acceptance_criteria>
  <done>data/capability-ledger.json parses, carries at least the sixteen screened seed rows with valid enums and non-empty evidence on every row, and the schema tripwire passes.</done>
</task>

<task type="auto">
  <name>Task 3: Freshness tripwire plus doctor organ registration</name>
  <files>tests/test-265-ledger-freshness.cjs, lib/core/doctor/capability-ledger-module.cjs, data/doctor-modules.json</files>
  <read_first>
    - lib/core/doctor/mode-select-checkpoint-module.cjs (the check(ctx) contract: returns { status, detail }, ctx keys are test seams, fix is absent for check-only organs)
    - data/doctor-modules.json (the $schema_note contract: id, introduced_version, cadence, flag, fix_supported explicit boolean, runner, description)
    - .planning/phases/265-.../265-RESEARCH.md, Validation Architecture section (the ledger age tripwire row)
    - data/capability-ledger.json (created in Task 2)
  </read_first>
  <action>
Create lib/core/doctor/capability-ledger-module.cjs exporting `check(ctx)` and nothing else
(no fix - this organ is check-only). Behavior:

- Read data/capability-ledger.json relative to the plugin root. A parse failure or missing file
  returns status 'error' with a detail naming the path.
- Determine the installed Claude Code version. Default source: shell out to `claude --version`
  and take the leading dotted triple from output shaped like "2.1.247 (Claude Code)". Accept
  `ctx.installed_version` as a test seam that wins when explicitly provided, matching the
  ctx-seam convention in mode-select-checkpoint-module.cjs.
- When the claude binary is unavailable and no ctx seam is given, return status 'warn' with a
  detail saying the installed version could not be read. Never return 'ok' on an unknown
  version - a silent pass is the failure mode this whole phase exists to end.
- Compute the patch-version lag: same major and minor, `installed.patch minus
  ledger_covers.to.patch`. Read the threshold from the MINDRIAN_LEDGER_MAX_VERSION_LAG env var,
  defaulting to 40. Lag at or below threshold returns 'ok'; lag above threshold returns 'warn'
  with a detail naming both versions, the lag, and the literal remediation string
  "run /mos:radar --fetch to refresh data/capability-ledger.json".
- A major or minor mismatch (installed major.minor differs from the ledger's) returns 'warn'
  regardless of patch arithmetic.
- Canon Part 8: this module makes zero network calls beyond the local `claude --version`
  subprocess. No Brain, no fetch.

Register it in data/doctor-modules.json as one new entry in `modules[]`: id
"capability-ledger", introduced_version set to the current version in package.json,
cadence "always", flag null (so it runs on a bare, --all, and --acceptance run),
fix_supported false, runner "lib/core/doctor/capability-ledger-module.cjs", description one
line naming ledger staleness against installed claude --version.

Create tests/test-265-ledger-freshness.cjs as a plain Node script that requires the module and
exercises three arms through the ctx seam: a fresh ledger (ctx.installed_version equal to
ledger_covers.to) asserts status 'ok'; a stale ledger (ctx.installed_version with patch equal
to ledger_covers.to patch plus 200) asserts status 'warn' and asserts the detail string
contains "/mos:radar --fetch"; an unreadable version (ctx.installed_version set to null with
the env var MINDRIAN_LEDGER_FORCE_NO_BINARY set, or by passing an unparseable string) asserts
status is NOT 'ok'. The test also runs the real no-seam path once and, if the claude binary is
absent, prints a single loud line beginning "SKIP" for that arm only and still exits 0 - it
must never skip the three seeded arms.
  </action>
  <verify>
    <automated>node tests/test-265-ledger-freshness.cjs &amp;&amp; node -e "const m=require('./data/doctor-modules.json'); process.exit(m.modules.some(x=>x.id==='capability-ledger'&&x.fix_supported===false&&x.cadence==='always')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-265-ledger-freshness.cjs` exits 0 and its output contains no line beginning "FAIL".
    - `node -e "const m=require('./lib/core/doctor/capability-ledger-module.cjs'); const r=m.check({installed_version:'2.1.999'}); process.exit(r.status==='warn'&&/radar --fetch/.test(r.detail)?0:1)"` exits 0.
    - `node -e "const l=require('./data/capability-ledger.json'); const m=require('./lib/core/doctor/capability-ledger-module.cjs'); process.exit(m.check({installed_version:l.ledger_covers.to}).status==='ok'?0:1)"` exits 0.
    - `node -e "const m=require('./lib/core/doctor/capability-ledger-module.cjs'); process.exit(typeof m.check==='function'&&typeof m.fix==='undefined'?0:1)"` exits 0 (check-only organ).
    - `node -e "const d=require('./data/doctor-modules.json'); const e=d.modules.find(x=>x.id==='capability-ledger'); process.exit(e&&e.runner==='lib/core/doctor/capability-ledger-module.cjs'&&e.flag===null?0:1)"` exits 0.
    - `node scripts/doctor.cjs --acceptance` runs to completion and its output mentions the capability-ledger organ.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' lib/core/doctor/capability-ledger-module.cjs tests/test-265-ledger-freshness.cjs` returns 0 for both files.
  </acceptance_criteria>
  <done>The freshness tripwire fails a stale ledger, warns on an unreadable version, passes a fresh one, and is registered as a cadence-always check-only doctor organ visible to --acceptance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote changelog to repo file | `/mos:radar --fetch` (plan 265-05) writes into this ledger; the ledger is then read by a check script and by Claude at plan time |
| local subprocess | `claude --version` output is parsed by the doctor organ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-265-01 | Tampering / Elevation | data/capability-ledger.json read as instructions | mitigate | Ledger rows are typed structured fields only; the schema tripwire rejects extra keys, so free-form prose cannot ride into the file unnoticed. JSON.parse only, never eval. |
| T-265-02 | Repudiation | Stale ledger presenting false assurance | mitigate | The freshness tripwire is the control. `warn` on unknown version, never `ok`. Wired to doctor --acceptance so it runs on the release gate path. |
| T-265-03 | Spoofing | `claude --version` output parsing | accept | Local trusted binary; a malformed string yields `warn`, not a crash and not a pass. |
| T-265-04 | Information Disclosure | Canon Part 8 boundary | accept | Zero network egress in this plan; no Brain call, no user data. |
</threat_model>

<verification>
1. `bash tests/run-all-265.sh` with `TEST_265_ALLOW_PENDING=1` runs green for the three files this plan creates.
2. `TEST_265_PREFIX=tests/test-265-nonexistent- bash tests/run-all-265.sh` exits non-zero.
3. `node scripts/doctor.cjs --acceptance` completes and the capability-ledger organ appears in its output.
4. `node scripts/build-connector-registry.cjs --check` stays green (this plan adds no invocable surface).
</verification>

<success_criteria>
- data/capability-ledger.json exists, parses, and passes its schema tripwire with at least sixteen screened rows.
- A stale ledger produces a non-zero / warn signal on two independent paths (the test and the doctor organ).
- tests/run-all-265.sh is the phase's discovery harness and fails on zero discovery.
- No em-dash appears in any file this plan creates.
</success_criteria>

<output>
Create `.planning/phases/265-capability-radar-absorption-routing-re-scoped-supersedes-orp/265-01-SUMMARY.md` when done.
</output>
