---
phase: 265-capability-radar-absorption-routing-re-scoped-supersedes-orp
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/act.md
  - commands/persona.md
  - commands/grade.md
  - tests/test-265-no-run-in-background.cjs
  - tests/test-265-swarm-task-grant.cjs
  - tests/test-265-resolve-model-argorder.cjs
autonomous: true
requirements: [RADAR-05, RADAR-07, RADAR-09]
canon_parts: [6, 7, 11]

must_haves:
  truths:
    - "No Agent-tool dispatch instruction in the three swarm commands names run_in_background, a parameter Claude Code removes in fork mode"
    - "Each of the three swarm commands names its subagent_type explicitly instead of pointing at an agents/*.md file path"
    - "Swarm sizing clamps N to the platform concurrent-subagent cap of 20"
    - "A user running /mos:grade --full or /mos:persona --parallel is not asked to approve each individual agent spawn"
    - "resolveModel call sites in commands/ pass (roomDir, agentType) so venture-stage hints and per-agent overrides actually apply"
  artifacts:
    - path: "commands/act.md"
      provides: "Corrected swarm dispatch block with explicit subagent_type and the 20-cap clamp"
    - path: "commands/persona.md"
      provides: "Corrected parallel dispatch block and corrected resolveModel argument order"
    - path: "commands/grade.md"
      provides: "Corrected parallel dispatch block and corrected resolveModel argument order"
    - path: "tests/test-265-no-run-in-background.cjs"
      provides: "Scoped tripwire asserting the literal is absent"
    - path: "tests/test-265-resolve-model-argorder.cjs"
      provides: "Lint asserting resolveModel call sites use (roomDir, agentType)"
    - path: "tests/test-265-swarm-task-grant.cjs"
      provides: "Assertion that exactly the three reviewed commands carry allowed-tools Task"
  key_links:
    - from: "commands/act.md"
      to: "agents/framework-runner.md"
      via: "explicit subagent_type in the dispatch instruction"
      pattern: "subagent_type"
    - from: "commands/persona.md"
      to: "lib/core/model-profiles.cjs resolveModel"
      via: "positional argument order (roomDir, agentType)"
      pattern: "resolveModel\\(roomPath"
---

<objective>
Pay the actual absorption debt. Three shipped commands instruct Claude to pass
`run_in_background: true` to the Agent tool, a parameter Claude Code removes whenever fork
mode is on, which has been the interactive default since 2.1.232. The same three commands
point at agent FILE PATHS instead of naming a `subagent_type`, which was survivable while a
silent `general-purpose` fallback existed and is a hard error since 2.1.235. Two of them call
`resolveModel` with reversed arguments and therefore always resolve to `sonnet`, silently
bypassing every venture-stage hint and per-agent override.

Purpose: this is the proof the ledger produces action rather than rows. Each retrofit ships
with the tripwire that keeps it fixed.
Output: three corrected command files and three tripwire tests.

Locked decision honored: this plan makes ZERO changes to /mos:eureka, /mos:bono,
/mos:find-connections, /mos:whitespace, /mos:find-analogies, or grade-grant's extraction stage.
BONO's Promise.all in-process fan-out in lib/core/bono/cell-fanout.cjs is the intended final
design, navigator-confirmed. Do not touch it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/265-capability-radar-absorption-routing-re-scoped-supersedes-orp/265-RESEARCH.md
@CLAUDE.md
</context>

## Artifacts this phase produces

| Symbol / path | Kind | Notes |
|---|---|---|
| `tests/test-265-no-run-in-background.cjs` | new test file | |
| `tests/test-265-swarm-task-grant.cjs` | new test file | |
| `tests/test-265-resolve-model-argorder.cjs` | new test file | |
| `TEST_265_RIB_SCOPE` | new env var | `commands` or `all` (default `all`); scopes the run_in_background tripwire's tree walk |
| `Task` entry in `allowed-tools` | new frontmatter value | added to `commands/act.md`, `commands/persona.md`, `commands/grade.md` only |

No new runtime code. `resolveModel`, `MODEL_PROFILES`, and `planDispatch` are pre-existing
exports of `lib/core/model-profiles.cjs` and `lib/core/dispatch-optimizer.cjs`.

**Mirror regeneration is NOT this plan's job.** Plan 265-06 owns the single
`scripts/build-skill-mirrors.cjs` and `scripts/build-dist-bundles.cjs` run for the whole phase,
because those generators write every mirror on every run and two plans running them in the same
wave is a git-index race. Do not run them here. Edit `commands/*.md` only.

<tasks>

<task type="auto">
  <name>Task 1: Remove run_in_background, name subagent_type, clamp to the 20-cap</name>
  <files>commands/act.md, commands/persona.md, commands/grade.md, tests/test-265-no-run-in-background.cjs</files>
  <read_first>
    - commands/act.md lines 425 to 450 (the "### Swarm Dispatch" block; the offending instruction is line 432) and lines 195 to 215 (Step 4b Model Resolution, which already gets the CLI form right and is the model to imitate)
    - commands/persona.md lines 125 to 150 (the "Dispatch 6 agents in parallel" block; the offending instruction is line 136)
    - commands/grade.md lines 100 to 130 (the "Dispatch agents in parallel" block; the offending instruction is line 116)
    - .planning/phases/265-.../265-RESEARCH.md section 2c (the verbatim official-docs quote on run_in_background removal), section 4c (the missing-subagent_type hard error), section 4d row 2.1.217 (the concurrency cap), and the "Code Examples" entry titled "The run_in_background retrofit (2c)" which carries the exact replacement text
    - agents/framework-runner.md, agents/persona-analyst.md, agents/grading.md (confirm the three subagent_type names: framework-runner, persona-analyst, grading)
  </read_first>
  <action>
Rewrite the three dispatch instructions. Use the corrected block already drafted in the
research's Code Examples section as the template, adapted per command. Every rewritten block
must carry all four of these properties:

1. NO mention of `run_in_background`. State plainly that Claude Code runs spawned subagents in
   the background by default under fork mode, the interactive default since 2.1.232, and that
   the platform removes the Agent tool's `run_in_background` parameter in that mode so it must
   not be passed. Cite code.claude.com/docs/en/sub-agents.
2. An explicit `subagent_type` value, not a file path. act.md uses `framework-runner`,
   persona.md uses `persona-analyst`, grade.md uses `grading`. Keep any existing "Instructions
   from agents/<name>.md" line as supporting context, but the dispatch instruction itself must
   name the type string, because an Agent tool call that cannot resolve a `subagent_type` is a
   hard error since 2.1.235 rather than a silent general-purpose fallback.
3. A "dispatch all N in ONE message" instruction so the spawns actually batch.
4. A concurrency clamp naming the platform cap of 20 and the
   `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS` env var, with the instruction to clamp N to 20. In
   act.md this attaches to the existing `plan.agents` sizing from `dispatch-optimizer.cjs`
   (which caps on context budget, not agent count, so a large room can in principle exceed 20).
   In persona.md N is fixed at 6 and in grade.md N is at most 8, so both are already under the
   cap; state the cap anyway as the standing rule so a future author does not reintroduce an
   unbounded fan-out.

Preserve everything else in those blocks verbatim: the per-agent input lists, the isolation
statement in act.md line 436, the ASCII status displays, and the surrounding gate prose.

Then create tests/test-265-no-run-in-background.cjs as a plain Node script. It walks a file set
determined by the `TEST_265_RIB_SCOPE` env var: `commands` walks `commands/**/*.md` only;
anything else, including unset, walks `commands/**/*.md`, `skills/**/SKILL.md`, and
`dist/**/*.md`. For each file it strips nothing (the literal must be absent from prose too,
because the prose IS the instruction) and asserts the string `run_in_background` does not
appear. On a hit it prints the file path and the matching line number, then process.exit(1).
The test also asserts, positively, that each of commands/act.md, commands/persona.md, and
commands/grade.md contains the string `subagent_type` and the string
`CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS` - an absence tripwire alone would pass on a file that
deleted the dispatch block entirely.

Do not run the mirror generators. Plan 265-06 owns that.
  </action>
  <verify>
    <automated>TEST_265_RIB_SCOPE=commands node tests/test-265-no-run-in-background.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c run_in_background commands/act.md commands/persona.md commands/grade.md` returns 0 for all three files.
    - `grep -c 'subagent_type: framework-runner\|subagent_type` framework-runner' commands/act.md` returns at least 1 (act names its type).
    - `grep -c 'persona-analyst' commands/persona.md` returns at least 1 in the dispatch block AND the dispatch instruction contains the literal `subagent_type`.
    - `grep -c 'subagent_type' commands/grade.md` returns at least 1 and the value `grading` appears in the same block.
    - `grep -c 'CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS' commands/act.md commands/persona.md commands/grade.md` returns at least 1 for each file.
    - `grep -c '2.1.232' commands/act.md commands/persona.md commands/grade.md` returns at least 1 for each file (the version pin is present so a reader can date the claim).
    - `TEST_265_RIB_SCOPE=commands node tests/test-265-no-run-in-background.cjs` exits 0.
    - `git diff --stat skills/ dist/` shows zero changed files (this plan did not run the generators).
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' commands/act.md commands/persona.md commands/grade.md tests/test-265-no-run-in-background.cjs` returns 0 for every file.
  </acceptance_criteria>
  <done>All three dispatch instructions are free of run_in_background, name an explicit subagent_type, batch into one message, and state the 20-cap; the scoped tripwire passes.</done>
</task>

<task type="auto">
  <name>Task 2: Grant allowed-tools Task on the three swarm commands (reviewed privilege change)</name>
  <files>commands/act.md, commands/persona.md, commands/grade.md, tests/test-265-swarm-task-grant.cjs</files>
  <read_first>
    - commands/act.md frontmatter, the `allowed-tools` block (currently Read, Write, Glob, Bash, mcp__mindrian-brain__brain_ask, mcp__mindrian-brain__brain_search)
    - commands/persona.md frontmatter, the `allowed-tools` block at lines 46 to 50 (currently Read, Write, Bash, Glob)
    - commands/grade.md frontmatter, the `allowed-tools` block (currently Read, Write, Bash, Glob, AskUserQuestion)
    - .planning/phases/265-.../265-RESEARCH.md section 2d (allowed-tools is a PRE-APPROVAL list, not a restriction list) and Pitfall 2 (do not frame this as a bug fix; it is a UX improvement and a privilege grant)
    - .planning/phases/265-.../265-RESEARCH.md Security Domain, ASVS V4 row (adding Task removes the permission prompt for spawning arbitrary subagents during that turn)
  </read_first>
  <action>
This task exists separately from Task 1 on purpose. It is a genuine privilege grant, not a
mechanical docs edit, and the navigator settled it as its own decision (Open Question 4:
SETTLED as "Add Task"). Do not fold it back into Task 1 even though it touches the same three
files' frontmatter.

Add `Task` as an entry in the `allowed-tools` list of exactly three files:
commands/act.md, commands/persona.md, commands/grade.md. Add it to no other file.

Immediately above or beside each `allowed-tools` block, add a one-line YAML comment recording
WHY, in this shape: Task is pre-approved here because this command dispatches N subagents in
one turn and the permission prompt per spawn is up to 8 on grade --full and 6 on persona
--parallel; `allowed-tools` is a pre-approval list, not a restriction list, so this removes
prompts rather than granting a capability the command did not already have. Reference the
Phase 265 ledger row and the navigator decision.

Do not add `Task` to commands/trending-to-absurd.md or commands/explore-opportunity.md in this
plan. Their dispatch shapes are still being settled in plan 265-04; granting a privilege ahead
of the decision is exactly the silent-widening pattern the threat register forbids.

Create tests/test-265-swarm-task-grant.cjs as a plain Node script. It parses the YAML
frontmatter of every commands/*.md file (a simple fence-delimited read is sufficient; do not
add a YAML dependency - use the same frontmatter-slicing approach already used elsewhere in
tests/) and asserts a set equality: exactly the three files commands/act.md,
commands/persona.md, commands/grade.md carry `Task` in `allowed-tools`, and no other
commands/*.md file does. A fourth file acquiring the grant must fail this test. Print the
offending file list on failure and process.exit(1).
  </action>
  <verify>
    <automated>node tests/test-265-swarm-task-grant.cjs</automated>
  </verify>
  <acceptance_criteria>
    - The `allowed-tools` list in each of commands/act.md, commands/persona.md, commands/grade.md contains an entry equal to `Task`.
    - `grep -l '^\s*-\s*Task\s*$' commands/*.md | wc -l` returns exactly 3, and the three paths are act.md, persona.md, grade.md.
    - Each of the three files carries a comment line adjacent to `allowed-tools` containing the substring `pre-approval` (the stated reason is present, not just the grant).
    - `node tests/test-265-swarm-task-grant.cjs` exits 0.
    - Adding `Task` to a fourth commands/*.md file and re-running the test exits non-zero (prove the set-equality arm by temporary edit, then revert; record the proof in the SUMMARY).
    - `node scripts/build-connector-registry.cjs --check` still exits 0 (the frontmatter edit did not break the born-wired gate).
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' commands/act.md commands/persona.md commands/grade.md tests/test-265-swarm-task-grant.cjs` returns 0 for every file.
  </acceptance_criteria>
  <done>Exactly three commands carry the Task pre-approval, each with a written reason, and a set-equality tripwire prevents silent widening to a fourth.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the reversed resolveModel arguments</name>
  <files>commands/persona.md, commands/grade.md, tests/test-265-resolve-model-argorder.cjs</files>
  <read_first>
    - lib/core/model-profiles.cjs lines 110 to 155 (the signature `function resolveModel(roomDir, agentType)` at line 119, the MODEL_PROFILES map at 18-26, and the Step 5 default-to-sonnet fallthrough at line 148 that the reversed call currently always hits)
    - commands/persona.md line 132 (`const model = resolveModel('persona-analyst', roomPath);`)
    - commands/grade.md line 112 (`const model = resolveModel('grading', roomPath);`)
    - commands/act.md line 203 (the CLI form that gets it right: `node ".../model-profiles.cjs" resolve <roomDir> framework-runner`)
    - .planning/phases/265-.../265-RESEARCH.md section 4e and its "Code Examples" entry "The resolveModel argument fix (4e)"
  </read_first>
  <action>
Swap the argument order at both call sites so the first positional argument is the room path
and the second is the agent type. commands/persona.md line 132 becomes a call passing
`roomPath` first and the string `'persona-analyst'` second. commands/grade.md line 112 becomes
a call passing `roomPath` first and the string `'grading'` second.

While in each block, add one sentence of context so the fix does not silently regress: state
that the signature is `resolveModel(roomDir, agentType)` and that reversing it makes the config
load fail and the agent-type lookup miss, so the function falls through to its Step 5 default
and always returns `sonnet`, bypassing venture-stage hints and per-agent overrides. That
sentence is the reason a future author will not swap them back.

Create tests/test-265-resolve-model-argorder.cjs as a plain Node script lint. It scans every
commands/*.md file for occurrences of `resolveModel(` and, for each, asserts the first
positional argument is NOT a quoted string literal that appears as a key in
`MODEL_PROFILES` (require lib/core/model-profiles.cjs and read the real key set rather than
hardcoding agent names - a new agent type must be caught automatically). Positively assert
that both commands/persona.md and commands/grade.md contain a `resolveModel(` call whose
second positional argument IS a quoted string in that key set. Print the file, line number, and
the offending call text on failure, then process.exit(1).
  </action>
  <verify>
    <automated>node tests/test-265-resolve-model-argorder.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resolveModel('persona-analyst'" commands/persona.md` returns 0.
    - `grep -c "resolveModel('grading'" commands/grade.md` returns 0.
    - `grep -c "resolveModel(roomPath, 'persona-analyst')" commands/persona.md` returns 1.
    - `grep -c "resolveModel(roomPath, 'grading')" commands/grade.md` returns 1.
    - Both files contain a sentence naming the signature `resolveModel(roomDir, agentType)` near the call site.
    - `node tests/test-265-resolve-model-argorder.cjs` exits 0.
    - The lint reads its agent-type key set from `require('./lib/core/model-profiles.cjs')` rather than a hardcoded array (grep the test file for `model-profiles` returns at least 1).
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' commands/persona.md commands/grade.md tests/test-265-resolve-model-argorder.cjs` returns 0 for every file.
  </acceptance_criteria>
  <done>Both call sites pass (roomDir, agentType), each carries the reason inline, and a lint reading the real MODEL_PROFILES key set keeps them that way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command frontmatter to permission system | `allowed-tools` entries are pre-approvals consumed by the Claude Code permission flow for the invoking turn |
| command prose to Agent tool | Dispatch instructions are read by Claude and turned into Agent tool calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-265-08 | Elevation of Privilege | `allowed-tools: Task` on act, persona, grade | mitigate | Task 2 is its own reviewed task with a written reason in each file and a set-equality tripwire (`tests/test-265-swarm-task-grant.cjs`) that fails when a fourth command acquires the grant. The grant is scoped to the invoking turn per the official frontmatter contract and clears on the next user message. |
| T-265-09 | Denial of Service | Unbounded subagent fan-out | mitigate | Each rewritten dispatch block states the platform cap of 20 and instructs Claude to clamp N. Mirrors the existing `FUTURES_FANOUT_CAP` discipline in lib/core/bono/cell-fanout.cjs. |
| T-265-10 | Tampering | A regenerated mirror silently reverting a fix | mitigate | `commands/*.md` is the single source of truth; `scripts/build-skill-mirrors.cjs --check` is a standing gate line in `tests/run-all-265.sh` and plan 265-06 runs the generators once and re-asserts the tripwire at full `all` scope. |
| T-265-11 | Repudiation | An undated platform claim in command prose | mitigate | Every rewritten block carries the `2.1.232` version pin and the docs URL, so a future reader can date and re-verify the claim rather than trusting it. |
</threat_model>

<verification>
1. `TEST_265_RIB_SCOPE=commands node tests/test-265-no-run-in-background.cjs` exits 0.
2. `node tests/test-265-swarm-task-grant.cjs` exits 0.
3. `node tests/test-265-resolve-model-argorder.cjs` exits 0.
4. `node scripts/build-connector-registry.cjs --check` exits 0.
5. `git diff --stat skills/ dist/` is empty (generators were not run here).
6. `git diff --stat lib/ commands/eureka.md commands/bono.md commands/find-connections.md commands/whitespace.md commands/find-analogies.md commands/grade-grant.md` is empty (locked decision 1 honored).
</verification>

<success_criteria>
- The three swarm commands instruct a dispatch shape that matches the platform as it actually behaves at 2.1.232 and later.
- The permission-prompt storm on `/mos:grade --full` and `/mos:persona --parallel` is gone, via an explicit reviewed grant with a written reason.
- Venture-stage model resolution actually applies on both parallel-dispatch commands.
- Three tripwires exist that fail if any of the above regresses.
</success_criteria>

<output>
Create `.planning/phases/265-capability-radar-absorption-routing-re-scoped-supersedes-orp/265-03-SUMMARY.md` when done.
</output>
