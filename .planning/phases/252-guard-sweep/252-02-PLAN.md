---
phase: 252-guard-sweep
plan: 02
type: execute
wave: 2
depends_on: [252-01]
files_modified:
  - tests/fixtures/127-03-acceptance/no-identity-refusal/README.md
  - tests/fixtures/127-03-acceptance/clean-install/README.md
  - tests/test-127-03-acceptance-gates.sh
autonomous: false
requirements: [SWEEP-02]

must_haves:
  truths:
    - "check-flagship-floor.cjs exits 0 with data/flagship-floor-set.json present BEFORE any fixture work lands; a red floor pauses the plan honestly at the checkpoint, never fakes green"
    - "The artifact that proved the install works without a key now proves it refuses honestly without one: the fixture asserts refusal framing and that a keyless methodology call can never yield a methodology payload"
    - "Coverage is kept, never deleted: the fixture directory is renamed (history follows), every acceptance gate still runs, and the DIRECTOR_NOT_AVAILABLE wire assertion survives byte-identical"
    - "Keyless means registration-failed (SEED-011 Option A): the fixture deterministically forces the no-identity edge via fresh HOME + MINDRIAN_DISABLE_AUTO_REGISTER=1, never reaching the live /register endpoint"
  artifacts:
    - path: "tests/fixtures/127-03-acceptance/no-identity-refusal/README.md"
      provides: "The inverted fixture's purpose statement: proves the no-identity edge refuses honestly"
      contains: "refuses honestly"
    - path: "tests/test-127-03-acceptance-gates.sh"
      provides: "Gate 1 inversion assertions (refusal framing present, graceful framing absent, no methodology payload keyless) on top of the surviving sentinel assertion"
  key_links:
    - from: "tests/test-127-03-acceptance-gates.sh"
      to: "tests/fixtures/127-03-acceptance/no-identity-refusal/"
      via: "gate 4 fixture path reference updated with the rename"
      pattern: "no-identity-refusal"
    - from: "tests/test-127-03-acceptance-gates.sh"
      to: "scripts/check-flagship-floor.cjs"
      via: "SWEEP-02 entry gate re-run at execution time"
      pattern: "check-flagship-floor"
---

<objective>
SWEEP-02: invert the tier-0-no-key acceptance fixture into a no-identity refusal fixture -
coverage kept, assertion inverted, never deleted. "Keyless" post SEED-011 Option A means the
registration-failed edge (no MINDRIAN_BRAIN_KEY, no cached ~/.mindrian-install.json, silent
registration suppressed), not a default mode. The entire plan is HARD-gated on the ENRICH-04
flagship floor: scripts/check-flagship-floor.cjs must exit 0 live at execution time. It was
exit 1 with 24/28 misses and data/flagship-floor-set.json ABSENT at plan time (re-verified
2026-08-10); the floor closes through 249-03's enrichment cadence, not through this plan.

Purpose: the strongest artifact of the old doctrine becomes the strongest artifact of the new.
Output: the renamed + inverted fixture, updated acceptance harness gate assertions, a
recorded green floor-gate run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/252-guard-sweep/252-RESEARCH.md
@.planning/phases/250-honesty-rail-doctrine-amendment/250-04-SUMMARY.md
@tests/test-127-03-acceptance-gates.sh
@tests/fixtures/127-03-acceptance/tier-0-no-key/README.md

BINDING re-verifications at plan time (2026-08-10), superseding 252-RESEARCH.md's branches:

1. The SEED-011 registration client LANDED (250-04): the research's "if it has not landed"
   branch is DEAD - plan against the landed mechanism. The fixture's determinism control
   ALREADY EXISTS: 250-04 added MINDRIAN_DISABLE_AUTO_REGISTER=1 to the keyless spawn env of
   BOTH tests/test-127-00-shim-handshake.sh and test-127-03-acceptance-gates.sh gate 1
   (250-04 deviation 2). Keep it; it is the network-isolation answer to research Pitfall 5.
2. Refusal kind for the no-identity edge: RESOLVED as reuse - 250-04 reframed the no_key
   copy itself via _noKeyDetail(c) / getAutoRegisterFailureReason(): "Methodology needs the
   Brain, and registration has not completed (offline, or the attempt failed)...". No fifth
   kind was minted; assert against no_key with the reframed copy. (Research Open Question 2
   closed.)
3. The byte-locked keyless wire shape is UNTOUCHED by 250-04: tier0Response()'s
   reason 'MINDRIAN_BRAIN_KEY not set' stays exact; only UPGRADE_HINT was reworded and
   existing tests match it with a loose /brain-access/ regex. The gate 1 sentinel assertion
   survives as-is.
4. Floor gate re-run at plan time: `node scripts/check-flagship-floor.cjs` exit 1,
   data/flagship-floor-set.json ABSENT. RED. 249-03 (2/3 plans complete in ROADMAP) owns
   turning it green.
5. After 252-01, the chokepoint file is lib/core/refusal-messaging.cjs. The acceptance
   harness itself carries no module-path reference (verified by grep), so only assertion
   text changes here.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The ENRICH-04 floor gate, run live, honored honestly</name>
  <files>(none - gate only)</files>
  <action>
    Run `node scripts/check-flagship-floor.cjs` and check `data/flagship-floor-set.json`
    exists (the ratified denominator, the only legal producer per 249-02). BOTH must hold:
    exit 0 AND the floor-set file present.
    IF GREEN: record the full gate output (frameworks passing, denominator count) in the
    SUMMARY as the SWEEP-02 entry evidence, then proceed to Task 2.
    IF RED (exit non-zero or floor-set absent): STOP THIS PLAN IMMEDIATELY. Do not touch the
    fixture, do not "partially" invert, do not soften the gate, do not mark anything done.
    This is the ROADMAP's explicit gate ("the hard-require in Phase 252 does not land until
    this floor holds") - landing the inversion on a red floor produces the state the gate
    exists to prevent. Proceed to the checkpoint below and report blocked-not-broken.
  </action>
  <verify>
    <automated>node scripts/check-flagship-floor.cjs && test -f data/flagship-floor-set.json</automated>
  </verify>
  <done>Either: gate green with output filed, plan continues - or: plan halted at the checkpoint with the honest red output filed, zero fixture edits made.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Nothing yet if this checkpoint fires - it fires ONLY when Task 1 found the floor red. The plan paused honestly instead of faking the gate green (the floor was 24/28 misses at last recorded run; it closes through 249-03's enrichment cadence).</what-built>
  <how-to-verify>
    1. Confirm 249-03 has landed its enrichment batches and ratified the denominator:
       `ls data/flagship-floor-set.json` exists.
    2. Run `node scripts/check-flagship-floor.cjs` yourself with the Brain key present
       (the floor cannot be evaluated offline); expect exit 0.
    3. If still red: this plan stays parked. The blocker is Phase 249, not this plan.
  </how-to-verify>
  <resume-signal>Type "floor-green" once both checks pass; the executor re-runs Task 1 and continues. Any other reply keeps the plan parked as blocked-not-broken.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invert the fixture - rename, README rewrite, gate 1 refusal assertions</name>
  <files>tests/fixtures/127-03-acceptance/no-identity-refusal/README.md, tests/fixtures/127-03-acceptance/clean-install/README.md, tests/test-127-03-acceptance-gates.sh</files>
  <behavior>
    - RED-FIRST legs, run BEFORE the rewrite lands (file outputs in SUMMARY):
      (a) a framing fence over the fixture README reds while the old works-without-key
      framing ("gets unblocked", the silent-failure-cohort purpose statement) is still
      present; (b) the new no-methodology-payload assertion is demonstrated ABLE to fail by
      running it against a deliberately wrong stub payload that carries a frameworks array
      (the eval-honesty rule: a test that cannot fail is not evidence).
    - GREEN after: gate 1 asserts, in order: parsed.status === DIRECTOR_NOT_AVAILABLE
      (surviving, byte-locked); refusal framing present in the fallback/refusal copy
      (regex on the 250 rail copy, e.g. /will not improvise|registration has not completed/i);
      graceful-degradation framing ABSENT (a /graceful/i hit fails); a methodology-shaped
      keyless call (the derive/chain-shaped tool) returns a refusal-status payload and NEVER
      a frameworks/chain array.
    - Gates 2-5 keep passing; gate 4's canonical startup-line assertion survives against the
      renamed fixture dir; class-m 5-layer cascade parity (gate 5) unchanged.
  </behavior>
  <action>
    (1) RENAME tests/fixtures/127-03-acceptance/tier-0-no-key/ to
    tests/fixtures/127-03-acceptance/no-identity-refusal/ (git mv; coverage preserved,
    history follows - this satisfies "never deleted"; the literal tier-0 path name would
    permanently trip the extended docs fence landing in 252-03). Update every path reference
    in test-127-03-acceptance-gates.sh (gate 4 and any shared fixture setup).
    (2) REWRITE the fixture README to state the inverted purpose, anchored on the ROADMAP
    text verbatim: "the artifact that proved the install works without a key now proves it
    refuses honestly without one." Define the no-identity edge precisely per SEED-011
    Option A (SEED-011 ruling, REQUIREMENTS.md HONEST-03): post-registration, keyless is the
    registration-failed edge - no MINDRIAN_BRAIN_KEY, no cached ~/.mindrian-install.json
    (fresh HOME=$TMPDIR isolates it), registration suppressed
    (MINDRIAN_DISABLE_AUTO_REGISTER=1, already in the spawn env per 250-04) so the hermetic
    edge is deterministic and no test run ever reaches the live /register endpoint. Name the
    identity model in the README so the meaning survives the transition. Update
    clean-install/README.md's sentinel reference in the same pass.
    (3) ADD the gate 1 inversion assertions per <behavior>, keeping every existing assertion.
    Assert against the landed 250 copy only - never draft new refusal prose (250 renders,
    252 asserts). The no-methodology-payload leg calls the derive/chain-shaped tool through
    the same keyless shim spawn and asserts the refusal-status set plus the absence of any
    frameworks array (the structural proof that no counterfeit path survived 252-01's chain
    deletion).
    (4) Run the full harness plus the class-m parity leg; verify
    MINDRIAN_DISABLE_AUTO_REGISTER=1 and the fresh-HOME isolation are present in every
    keyless spawn the harness makes.
  </action>
  <verify>
    <automated>bash tests/test-127-03-acceptance-gates.sh && bash tests/test-127-00-shim-handshake.sh</automated>
  </verify>
  <done>Fixture renamed with history, README states the inverted registration-failed purpose, gate 1 carries the three inversion assertions with both RED-first proofs filed, all gates green, no live-network reach possible from the harness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> live /register endpoint | A hermetic fixture must never cross it (flake + egress) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-252-04 | Spoofing (false-green) | fixture accidentally registers post SEED-011 | mitigate | MINDRIAN_DISABLE_AUTO_REGISTER=1 + fresh HOME in every keyless spawn (landed in 250-04, verified in Task 2 step 4) |
| T-252-05 | Repudiation | landing the hard-require on a red floor | mitigate | Task 1 gate + blocking checkpoint; blocked-not-broken is the only honest red-floor state |
| T-252-SC | Tampering | package installs | accept | Zero installs this phase |
</threat_model>

<verification>
- `node scripts/check-flagship-floor.cjs` exit 0 + `data/flagship-floor-set.json` present, output filed (or plan honestly parked at the checkpoint)
- `bash tests/test-127-03-acceptance-gates.sh` green with the inversion assertions live
- `bash tests/test-127-00-shim-handshake.sh` 9/9 green (wire shape untouched)
- Both RED-first proofs (README framing fence, wrong-stub payload) filed in SUMMARY
- `grep -r "tier-0-no-key" tests/` returns only historical README prose if any, zero live path references
</verification>

<success_criteria>
SWEEP-02 complete: floor gate green and recorded, fixture repurposed (coverage kept,
assertion inverted, never deleted), keyless defined as the registration-failed edge per
SEED-011 Option A, no methodology payload obtainable keyless, harness deterministic and
network-isolated.
</success_criteria>

<output>
Create `.planning/phases/252-guard-sweep/252-02-SUMMARY.md` when done. MUST include: the
green floor-gate output (or the honest red + parked state), both RED-first proofs verbatim,
and the renamed fixture path map.
</output>
