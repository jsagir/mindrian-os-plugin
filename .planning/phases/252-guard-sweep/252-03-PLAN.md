---
phase: 252-guard-sweep
plan: 03
type: execute
wave: 3
depends_on: [252-01, 252-02]
files_modified:
  - .claude/includes/decisions.md
  - tests/test-250-amendment-unit.cjs
  - docs/MINDRIAN-CANON.md
  - CLAUDE.md
  - docs/install/BRAIN-SETUP.md
  - docs/install/HEAL.md
  - docs/THE-BRAIN.md
  - docs/MWP-SPECIFICATION.md
  - docs/ENV-TUNING.md
  - docs/WORKFLOWS.md
  - docs/ARCHITECTURE-DEEP-DIVE.md
  - docs/F-SELECTOR-CONSUMER-GUIDE.md
  - docs/ORCHESTRATION-PROJECTION-CONTRACT.md
  - docs/TELEMETRY-SCHEMA.md
  - docs/CANON-PHASE-MAP.md
  - docs/AGENTIC-SURFACING-PATTERN.md
  - docs/RCA-TEMPLATE.md
  - docs/IDEA-DOCUMENT.md
  - agents/opportunity-scanner.md
  - skills/brain-connector/SKILL.md
  - tests/test-250-doctrine-fence.cjs
  - tests/test-250-provenance-fence.cjs
  - README.md
  - dist/BUNDLE-VERSION.json
autonomous: false
requirements: [SWEEP-03]

must_haves:
  truths:
    - "decisions.md rows 1, 5, and 8 carry the amendment's verbatim replacement text, applied in the SAME commit that flips test-250-amendment-unit.cjs's negative Test 4 to positive - the test pair IS the lockstep enforcement"
    - "MINDRIAN-CANON.md's line-21 sentence no longer teaches without-Brain Tier-0 fallbacks; canon line ~193's cold-start option set is renamed, behavior kept, never amended as doctrine"
    - "No living doc, skill, or command teaches Brain-optional graceful degradation; historical records (CHANGELOG, dated handoffs, archived specs) are byte-untouched"
    - "The release cut that carries the amendment rows also carries the complete sweep, cut by the operator at a checkpoint - the executor never cuts releases"
  artifacts:
    - path: ".claude/includes/decisions.md"
      provides: "Rows 1/5/8 applied verbatim from docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md sections 3/4/5"
      contains: "Honest refusal everywhere"
    - path: "tests/test-250-amendment-unit.cjs"
      provides: "Test 4 flipped: asserts the APPLIED rows, red if decisions.md ever regresses"
    - path: "tests/test-250-doctrine-fence.cjs"
      provides: "Scope extended to the living-docs set + graceful-degradation-as-doctrine (one fence, wider scope, Part 7)"
  key_links:
    - from: ".claude/includes/decisions.md"
      to: "docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md"
      via: "verbatim row copy, never re-drafted"
      pattern: "the Brain is part of what installs"
    - from: "tests/test-250-amendment-unit.cjs"
      to: ".claude/includes/decisions.md"
      via: "flipped Test 4 positive assertion"
      pattern: "Honest refusal everywhere"
---

<objective>
SWEEP-03: docs and constitution agree in the same release. Apply the ratified amendment's
verbatim rows (docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md, RATIFIED 2026-08-10,
doc-now/rows-at-sweep) to .claude/includes/decisions.md, flip 250-02's negative
amendment-unit assertion in the SAME commit, work the full consequential-edits ledger
(canon:21, the CLAUDE.md install/zero-infrastructure claims, BRAIN-SETUP prose, the
re-measured ~72 living docs, dist regen), and close with the operator's lockstep release
cut: the single v2.0.0-beta.N that carries amendment + sweep together, per the HARD
amendment-sweep lockstep rule.

Purpose: no released state ever exists where docs claim Brain-required while guards silently
degrade, or the reverse.
Output: applied constitution, swept living docs, extended fence, flipped test pair,
operator-cut release with live three-surface verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md
@.planning/phases/250-honesty-rail-doctrine-amendment/250-02-SUMMARY.md
@.planning/phases/252-guard-sweep/252-RESEARCH.md
@.claude/includes/decisions.md
@tests/test-250-amendment-unit.cjs

BINDING re-verifications at plan time (2026-08-10) against the FINAL 250 SUMMARYs:

1. 250-02's consequential-edits ledger (amendment doc section 6) is AUTHORITATIVE for this
   plan - it already folds in MINDRIAN-CANON.md:21 (research Open Question 1 is CLOSED; no
   addendum needed) and cites the re-measured 72-non-dist / 114-with-dist living-docs count.
2. CLAUDE.md line numbers drifted AGAIN after the amendment's own correction: the ledger
   says :29/:94; at plan time the one-command claim is at :30, the second install claim at
   :93, zero-infrastructure at :95. LOCATE BY CONTENT, never by line number - grep for
   "One command installs it", "One-command install gives", "zero infrastructure".
3. MINDRIAN-CANON.md:21 verified still carrying the dying sentence at exactly line 21
   ("Larry operates with Brain (Full Loop) or without Brain (Local Only)... Tier 0
   methodology fallbacks... enriched but never replaced").
4. tests/test-250-amendment-unit.cjs Test 4 (lines ~96-104) asserts decisions.md still
   contains "Zero config; Larry works immediately." and "graceful degradation everywhere" -
   this is the negative assertion this plan flips. The test normalizes whitespace
   (hard-wrap newlines, blockquote markers) before matching; reuse its normalizeWhitespace
   helper for the new positive assertions.
5. skills/brain-connector/SKILL.md:83's Provenance bullet reads "(until Phase 252 flips the
   chains)" - stale once 252-01 deleted them. The provenance fence
   (tests/test-250-provenance-fence.cjs) does NOT assert that parenthetical (verified by
   grep), so the bullet can be updated; re-run the fence after the edit regardless.
6. 250-04 is at status checkpoint: its Task 3 (brain-repo push + Render deploy + released-
   build three-surface matrix) is the SAME operator ceremony window as this plan's release
   cut. The release checkpoint below names it so one operator session can clear both.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The atomic constitution flip (one commit)</name>
  <files>.claude/includes/decisions.md, tests/test-250-amendment-unit.cjs, docs/MINDRIAN-CANON.md, CLAUDE.md</files>
  <behavior>
    - RED-FIRST (the flip direction IS the proof): first flip Test 4 in
      tests/test-250-amendment-unit.cjs from the negative assertion (rows unchanged) to the
      positive assertion (rows applied verbatim), run it, and record the RED - decisions.md
      still carries the old rows, proving the lockstep pair can fail. Then apply the rows;
      the suite goes green. BOTH edits land in ONE commit - between them nothing is
      committed. This test-pair flip is the mechanical lockstep enforcement designed into
      250-02; no release-side machinery replaces it.
    - After the commit: node --test tests/test-250-amendment-unit.cjs 9/9 green with Test 4
      asserting "the Brain is part of what installs" / "Honest refusal everywhere" and the
      row-5 replacement rationale.
  </behavior>
  <action>
    In one commit, mechanically and verbatim - copied from
    docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md, NEVER re-drafted, per the ratification
    ("nothing more, nothing less"):
    (1) decisions.md row 1 gets section 3's replacement text; row 8 gets section 4's; row 5's
    rationale gets section 5's replacement wording. Table format preserved.
    (2) MINDRIAN-CANON.md:21 (ledger item 3; locate by content): amend the sentence so the
    constitution matches the amended decisions - Larry's methodology comes from the Brain
    and says so; a Brain failure surfaces as honest refusal per Decision #8; the pedagogy
    remains Larry's, the methodology remains the graph's. Keep the surrounding bullet's
    voice; do not import the amendment's full prose. Canon Part 8 untouched. Canon :193's
    "Tier 0 fallback" is NOT part of this edit (it is Task 2's rename).
    (3) CLAUDE.md (ledger item 2; locate by content, line numbers have drifted to ~:30/:93/:95):
    rewrite the one-command-install sentences per the amendment's new Decision #1 framing
    (one command survives; what it delivers changes - the Brain is part of what installs,
    keyless gets an honest refusal) and the zero-infrastructure claim (the remote Brain is
    required for methodology; the plugin itself still runs serverless).
    (4) Flip Test 4 per <behavior>, reusing the file's normalizeWhitespace helper.
    Run the amendment unit suite + the doctrine fence before committing.
  </action>
  <verify>
    <automated>node --test tests/test-250-amendment-unit.cjs && node --test tests/test-250-doctrine-fence.cjs && grep -c "Honest refusal everywhere" .claude/includes/decisions.md</automated>
  </verify>
  <done>Rows 1/5/8 applied verbatim, canon:21 amended, CLAUDE.md claims rewritten, Test 4 flipped - all in one commit, with the flip-first RED recorded in SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Living-docs sweep + fence extension + dist regen</name>
  <files>docs/install/BRAIN-SETUP.md, docs/install/HEAL.md, docs/THE-BRAIN.md, docs/MWP-SPECIFICATION.md, docs/ENV-TUNING.md, docs/WORKFLOWS.md, docs/ARCHITECTURE-DEEP-DIVE.md, docs/F-SELECTOR-CONSUMER-GUIDE.md, docs/ORCHESTRATION-PROJECTION-CONTRACT.md, docs/TELEMETRY-SCHEMA.md, docs/CANON-PHASE-MAP.md, docs/AGENTIC-SURFACING-PATTERN.md, docs/RCA-TEMPLATE.md, docs/IDEA-DOCUMENT.md, docs/MINDRIAN-CANON.md, agents/opportunity-scanner.md, skills/brain-connector/SKILL.md, README.md, tests/test-250-doctrine-fence.cjs, tests/test-250-provenance-fence.cjs, dist/BUNDLE-VERSION.json</files>
  <behavior>
    - RED-FIRST: extend tests/test-250-doctrine-fence.cjs's scope FIRST - add the living-docs
      set (docs/ living contracts, README, includes) and "graceful degradation" as a doctrine
      phrase, SCOPED per 250-01's enumeration (the ENV-TUNING/env-parse "silently falls back"
      comments are legitimate; historical paths excluded; never lib/) - run it, record the
      red hits (the un-swept living docs), then sweep to green. One fence, wider scope,
      Part 7: no second fence file.
    - The fence's exclusion list is explicit and frozen: CHANGELOG.md, docs/2026-*-HANDOFF-*,
      docs/2026-05-12-*, docs/UI-UX-CONVERGENCE-2026-05-10/*, docs/superpowers/specs/*,
      docs/reviews/*, docs/testers/outbox/*, docs/research/*, docs/POWERHOUSE-1.6.0-SPEC.md,
      docs/autopsies/*, docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md (the amendment quotes the
      dead doctrine as causal record and must never be "corrected").
  </behavior>
  <action>
    RE-MEASURE FIRST (ledger item 6's own instruction): re-run the tier-0/graceful-
    degradation doc grep at execution time; the 72/114 count is the plan-time baseline, not
    the operative list. Then sweep the LIVING bucket only, per the research's
    living-vs-historical policy table:
    (1) docs/install/BRAIN-SETUP.md FULL rewrite (ledger item 4 - 250-01 fixed only the
    keyless sentence; the document's remaining Tier-0 framing dies here): Brain-required,
    silent registration as the default path, honest refusal for the failure edge, key
    override as the escape hatch.
    (2) The remaining living docs/ set + agents/opportunity-scanner.md + README: rewrite
    tier-0/graceful-degradation DOCTRINE to refusal doctrine. Historical records stay
    byte-untouched (rewriting dated records falsifies the causal record the amendment cites).
    (3) MINDRIAN-CANON.md:193 "Tier 0 fallback" (the cold-start Decision Gate option set):
    RENAME to "cold-start minimal option set", behavior text kept - this is the fourth
    vocabulary near-collision, a UI floor, not Brain doctrine.
    (4) skills/brain-connector/SKILL.md:83: update the Provenance TIER0 CHAINS bullet - the
    chains are now deleted (252-01), so the bullet states that no source:'tier0' path exists
    and any pre-252 derivation of that kind was never graph-grounded. Re-run
    tests/test-250-provenance-fence.cjs after (it does not assert the stale parenthetical,
    verified, but the fence must stay green).
    (5) Skills/commands beyond brain-connector: 252-01 already handled the rs-* and
    conversation-mode instruction surfaces; here sweep only remaining tier-0-as-doctrine
    prose the re-measure finds in skills/ commands/, respecting the same vocabulary
    exemptions (rs-* Tier-0/Tier-1 = local-mirror-vs-Aura, KEEP; the user's own Aura graph
    stays optional, Decision #6).
    (6) Regenerate dist (`node scripts/build-dist-bundles.cjs`), verify --check-stale exit 0,
    commit mirrors.
    (7) Run the extended fence to green plus the census (252-01) to confirm nothing regressed.
  </action>
  <verify>
    <automated>node --test tests/test-250-doctrine-fence.cjs && node --test tests/test-250-provenance-fence.cjs && node tests/test-252-guard-census.cjs && node scripts/build-dist-bundles.cjs --check-stale</automated>
  </verify>
  <done>Extended fence green with its red-first hit list filed; living docs swept with the re-measured operative list in SUMMARY; historical paths byte-untouched (verify: git diff touches no excluded path); canon:193 renamed; dist fresh.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The complete sweep: SWEEP-01 (census green, counterfeit dead, chokepoint renamed), SWEEP-02 (fixture inverted on a green floor), SWEEP-03 Tasks 1-2 (constitution applied + docs swept + fence extended, amendment-unit test pair flipped atomically). Everything is merged; nothing is RELEASED - and per the standing rule (feedback_dev_repo_fix_not_live_until_released), the sweep is not done at merge.</what-built>
  <how-to-verify>
    OPERATOR CEREMONY (the executor never cuts releases). Preconditions first:
    1. Gate 0 (official v1.15.0 close-out) and Gate 1 (v1.16.0 fold-in, already ruled) are
       settled per ROADMAP Progress. If Gate 0 is still open, PAUSE HERE - no v2.0.0 cut
       may precede it. This checkpoint parks honestly until the train is open.
    2. Adjacent ceremony in the same window: 250-04 Task 3 (push ProblemsWorthSolving-Brain's
       local commit 01ac1fc, confirm the Render redeploy, probe /register live) - the same
       released build should carry silent registration, or the inverted fixture's
       registration-failed edge is the DEFAULT live experience instead of the rare edge.
    THE CUT:
    3. Run `scripts/verify-release`, then `scripts/release.sh --start-prerelease` to cut the
       v2.0.0-beta.N carrying amendment + sweep together (the HARD lockstep: this one cut
       carries the rewritten Decisions #1/#8 AND the complete sweep; they are never split).
       Five-gate lockstep + marketplace pin per release-process.md.
    LIVE PROOF (the only verification that counts; doctor --acceptance self-skips live Brain
    arms via class-m, so CI green is NOT live proof):
    4. Update the plugin install, RESTART a session on the released build, and on each of
       CLI, Desktop, Cowork: (a) a methodology consult with the Brain reachable answers with
       the provenance mark; (b) a keyless/no-identity session (MINDRIAN_DISABLE_AUTO_REGISTER=1
       or an isolated HOME) refuses honestly with the 250 rail copy, never a hardcoded chain,
       never a silent degrade; (c) `node scripts/doctor.cjs --acceptance` green on the
       released build.
  </how-to-verify>
  <resume-signal>Type "released-and-verified vN" (with the beta number) once the cut is live, picked up, and the three-surface matrix passes - or describe any live failure verbatim for a fix cycle before re-cutting.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| constitution text -> shipped behavior | Docs claiming Brain-required while guards degrade (or the reverse) is the named contradiction |
| repo history -> causal record | Historical docs the amendment cites must never be rewritten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-252-06 | Repudiation | decisions.md rows vs sweep state | mitigate | The amendment-unit test-pair flip lands rows + assertion in one commit; between 250 and this commit the negative assertion blocks premature application |
| T-252-07 | Tampering | historical records | mitigate | Frozen fence exclusion list; done-criteria includes a diff check that no excluded path changed |
| T-252-08 | Repudiation | declaring done at merge | mitigate | checkpoint:human-action release ceremony + restarted-session three-surface live proof; doctor self-skip caveat named in the checkpoint |
| T-252-SC | Tampering | package installs | accept | Zero installs this phase |
</threat_model>

<verification>
- `node --test tests/test-250-amendment-unit.cjs` 9/9 green with Test 4 flipped (and its flip-first RED filed)
- `node --test tests/test-250-doctrine-fence.cjs` green at extended scope (red-first hit list filed)
- `node tests/test-252-guard-census.cjs`, `bash tests/run-all-252.sh`, `bash tests/test-127-03-acceptance-gates.sh` all green (the sweep holds under the doc changes)
- `node scripts/build-dist-bundles.cjs --check-stale` exit 0; shape-declaration no new warns; connector registry OK; em-dash fence zero hits
- git diff for Tasks 1-2 touches ZERO excluded historical paths
- Release checkpoint resolved with the beta number and the three-surface live matrix recorded
</verification>

<success_criteria>
SWEEP-03 complete and the phase closes: constitution and docs agree, the lockstep is
suite-enforced and release-delivered in one cut, historical record untouched, live refusal
proven on the released build on all three surfaces.
</success_criteria>

<output>
Create `.planning/phases/252-guard-sweep/252-03-SUMMARY.md` when done. MUST include: the
Test-4 flip RED output, the extended-fence red hit list, the re-measured living-docs
operative list, and the release + live-verification record (or the honest parked state at
the checkpoint).
</output>
