---
phase: 252-guard-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/run-all-252.sh
  - tests/test-252-guard-census.cjs
  - lib/core/refusal-messaging.cjs
  - lib/core/refusal-messaging.test.cjs
  - lib/core/brain-client.cjs
  - bin/mindrian-brain-mcp-client.cjs
  - lib/core/doctor/class-m-brain-smoke.cjs
  - lib/mcp/brain-router.cjs
  - lib/brain/chain-recommender.cjs
  - lib/brain/framework-chain-slice.cjs
  - lib/core/research-corpus.cjs
  - lib/core/brain-derivation.cjs
  - lib/hmi/tier-check.cjs
  - lib/core/rs-chain-feeder.cjs
  - lib/core/rs-brain-substrate.cjs
  - lib/core/rs-expert-brain-projection.cjs
  - scripts/rs-explain-command.cjs
  - scripts/brain-derive-command.cjs
  - skills/conversation-mode/SKILL.md
  - skills/rs-experts/SKILL.md
  - skills/rs-fetch/SKILL.md
  - skills/rs-explain/SKILL.md
  - commands/rs-experts.md
  - commands/rs-fetch.md
  - commands/rs-explain.md
  - tests/run-all-250.sh
  - tests/run-all-127.sh
  - tests/test-127-00-shim-handshake.sh
  - tests/test-127-03-canon-part-8-adversarial.sh
  - tests/test-127-03-no-em-dashes.sh
  - tests/test-250-refusal-shapes.cjs
  - tests/test-250-refusal-queue.cjs
  - tests/test-250-silent-registration.cjs
  - dist/generic-claude-dir/.claude/skills/conversation-mode/SKILL.md
  - dist/zed/.agents/skills/conversation-mode/SKILL.md
  - dist/generic-claude-dir/.claude/skills/rs-experts/SKILL.md
  - dist/zed/.agents/skills/rs-experts/SKILL.md
  - dist/generic-claude-dir/.claude/skills/rs-fetch/SKILL.md
  - dist/zed/.agents/skills/rs-fetch/SKILL.md
  - dist/generic-claude-dir/.claude/skills/rs-explain/SKILL.md
  - dist/zed/.agents/skills/rs-explain/SKILL.md
  - dist/BUNDLE-VERSION.json
autonomous: true
requirements: [SWEEP-01]

must_haves:
  truths:
    - "A methodology consult with the Brain unavailable renders a visible refusal through the 250-01 rail; no consult surface serves a hardcoded chain or a silent empty result"
    - "getTier0Chain and getFrameworkChain no longer exist anywhere in executable source; the conversation-mode instruction that consumed them instructs refusal instead"
    - "tests/test-252-guard-census.cjs is green against a frozen KEEP allowlist and was demonstrably RED before the sweep (output filed in SUMMARY)"
    - "resolveTierMode's tier_0 enum, the rs-* Tier-0/Tier-1 local-mirror-vs-Aura vocabulary, and the user's optional Aura graph are untouched (census.5 canaries prove it)"
    - "The DIRECTOR_NOT_AVAILABLE wire string, the five sentinel keys, tier0Response()'s shape, and reason 'MINDRIAN_BRAIN_KEY not set' are byte-identical before and after the rename"
  artifacts:
    - path: "tests/test-252-guard-census.cjs"
      provides: "Census rules 1-5: frozen allowlist, seam-liveness ROUTE leg, counterfeit-gone, doctrine-fence extension hook, vocabulary exemption canaries"
    - path: "tests/run-all-252.sh"
      provides: "Phase runner: glob discovery of tests/test-252-*.cjs, found-eq-0 guard, em-dash fence over phase-touched files"
    - path: "lib/core/refusal-messaging.cjs"
      provides: "The renamed chokepoint (was tier0-messaging.cjs); every export name unchanged"
  key_links:
    - from: "tests/test-252-guard-census.cjs"
      to: "lib/core/seam-liveness.cjs"
      via: "checkClaimedModuleLiveness over the ROUTE set"
      pattern: "checkClaimedModuleLiveness"
    - from: "bin/mindrian-brain-mcp-client.cjs"
      to: "lib/core/refusal-messaging.cjs"
      via: "require path updated by the rename"
      pattern: "refusal-messaging\\.cjs"
    - from: "lib/mcp/brain-router.cjs"
      to: "lib/core/refusal-messaging.cjs"
      via: "refusalResponse on the unavailable branch"
      pattern: "refusal-messaging\\.cjs"
---

<objective>
SWEEP-01: the classification-first mechanical sweep over the re-measured 29-file / 45-site
executable guard census. Route the ~8 consult-surface files through the 250-01 refusal rail,
conform the ~6 already-typed files to refusal vocabulary, keep the ~15 legitimate guards on a
frozen allowlist with recorded reasons, delete the counterfeit getTier0Chain/getFrameworkChain
hardcoded chains, rename tier0-messaging.cjs to refusal-messaging.cjs with the wire contract
byte-locked, and prove all of it with tests/test-252-guard-census.cjs on the 248-01 census
template - demonstrably RED before the sweep, green after.

Purpose: "no silent-degradation guard survives" becomes a census verdict, not a claim.
Output: the census test + phase runner, the renamed chokepoint, routed/conformed guard sites,
re-pointed degradation tests, rewritten instruction surfaces, regenerated dist.

Task structure (per plan-checker ruling): the judgment-heavy refusal-routing work (Task 3a,
code sites + test re-pointing) is isolated from the mechanical instruction-surface vocabulary
pass (Task 3b, skills/commands + census-to-green). Each task gets its own verify and its own
commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/252-guard-sweep/252-RESEARCH.md
@.planning/phases/250-honesty-rail-doctrine-amendment/250-01-SUMMARY.md
@.planning/phases/250-honesty-rail-doctrine-amendment/250-04-SUMMARY.md
@tests/test-248-resolver-census.cjs
@lib/core/seam-liveness.cjs
@lib/core/tier0-messaging.cjs

BINDING re-verifications performed at plan time (2026-08-10) against the FINAL 250 SUMMARYs.
These supersede 252-RESEARCH.md where they differ:

1. 250-01's exports landed exactly as researched: REFUSAL_KINDS, refusalResponse,
   larryRefusalLine, renderRefusal, refuseNotReady on lib/core/tier0-messaging.cjs. The
   doctrine fence is tests/test-250-doctrine-fence.cjs scoped to skills/ commands/ agents/
   dist/. Assumption A1 CONFIRMED.
2. 250-04 SHIPPED the silent-registration client (assumption A2 resolved to the
   "registration client HAS landed" branch): brain-client.cjs now carries _tryAutoRegister,
   ensureAvailable, getAutoRegisterFailureReason; the shim's 6 tool gates are now
   `await ensureAvailable()` (8 occurrences), NOT bare isAvailable() (2 residual occurrences).
   CONSEQUENCE: the census guard regex MUST match both forms:
   /\b(is|ensure)Available\s*\(/ - a bare-isAvailable census would silently drop the shim,
   the single largest routed surface, out of scope.
3. The tier0-messaging consumer set GREW since research (was 3; grep at plan time found 11
   referencing files plus the module itself): bin/mindrian-brain-mcp-client.cjs,
   lib/core/brain-client.cjs, lib/core/doctor/class-m-brain-smoke.cjs,
   lib/core/tier0-messaging.test.cjs, tests/test-250-refusal-shapes.cjs,
   tests/test-250-refusal-queue.cjs, tests/test-250-silent-registration.cjs,
   tests/run-all-250.sh, tests/run-all-127.sh, tests/test-127-03-canon-part-8-adversarial.sh,
   tests/test-127-03-no-em-dashes.sh. RE-ENUMERATE with grep at execution time; do not trust
   this list or the research's list of 3.
4. conversation-mode/SKILL.md:146 STILL carries "When Brain is NOT connected (Tier 0), use
   these hardcoded chains" (verified by read at plan time). 250-01 rewrote brain-connector's
   SKILL.md, NOT conversation-mode's. The rewrite is THIS plan's job.
5. getTier0Chain/getFrameworkChain: grep at plan time confirms lib/core/brain-client.cjs is
   the ONLY executable file containing either name. Zero CJS consumers. Delete BOTH.
6. NOT this plan's scope: tests/test-127-03-acceptance-gates.sh (its assertion changes are
   SWEEP-02 / plan 252-02 territory; it does not reference the tier0-messaging path, verified
   by grep, so the rename does not touch it either).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Census test + phase runner, born RED</name>
  <files>tests/test-252-guard-census.cjs, tests/run-all-252.sh</files>
  <behavior>
    - census.1 ALLOWLIST: every file under lib/ bin/ scripts/ hooks/ with an executable
      (comment-stripped) match of /\b(is|ensure)Available\s*\(/ is in the frozen
      KEEP+CHOKEPOINT+ROUTE+CONFORM classification lists; an unclassified new guard file reds.
      tests/ excluded per SWEEP-01.
    - census.2 SEAM-LIVENESS: every ROUTE-classified file requires refusal-messaging.cjs on an
      executable line - claims = the ROUTE set, verdict via
      seam-liveness.cjs checkClaimedModuleLiveness (no override parameter exists by design).
    - census.3 COUNTERFEIT-GONE: zero executable getTier0Chain( or getFrameworkChain(
      occurrences anywhere in lib/ bin/ scripts/ hooks/; zero executable source: 'tier0'
      occurrences in the same scope.
    - census.4 FENCE HOOK: re-assert the two 250-01 doctrine phrases stay dead in skills/
      commands/ agents/ dist/ (the living-docs scope extension itself rides plan 252-03).
    - census.5 VOCABULARY CANARIES: lib/core/navigation-engine-shared.cjs still contains
      'tier_0' (the resolveTierMode enum survives); skills/rs-experts/SKILL.md still contains
      'Tier 1' (the local-mirror-vs-Aura vocabulary survives). A zealot sweep that removes
      either reds the census.
  </behavior>
  <action>
    Build tests/test-252-guard-census.cjs on the tests/test-248-resolver-census.cjs template
    VERBATIM: copy its stripComments helper (line-comments-first order - the block-first order
    has a documented misfire on this repo's doc comments), its explicit rule numbering, and its
    seam-liveness aggregation style. Freeze the classification lists as top-of-file constants
    with a one-line reason per KEEP entry, taken from 252-RESEARCH.md's per-file disposition
    table: ROUTE = bin/mindrian-brain-mcp-client.cjs, lib/mcp/brain-router.cjs,
    lib/brain/chain-recommender.cjs, scripts/brain-derive-command.cjs, lib/core/brain-client.cjs.
    CONFORM = lib/brain/framework-chain-slice.cjs, lib/core/research-corpus.cjs,
    scripts/rs-explain-command.cjs, lib/core/brain-derivation.cjs, lib/hmi/tier-check.cjs.
    CHOKEPOINT = lib/core/refusal-messaging.cjs (post-rename name; the census reds on the old
    name existing). KEEP = the ~15-file table set (part8-egress-guard-hook safe-state,
    intent-classifier Part 8 permitted scalar, opportunity-ops optional enrichment,
    brain-derivation-queue re-enqueue discipline, the two mva enrichment agents,
    brain-md-staleness probe, rs-chain-feeder visible fail-open, rs-brain-substrate,
    rs-expert-brain-projection, eureka-offer offline-by-design, backfill-correlation-id
    throws-loud, admin-brain-write exits-loud, fetch-brain-baseline explicit-reason,
    check-dual-graph-health inconclusive-fail-closed, build-command-registry +
    build-connector-registry committed-snapshot, whitespace-to-brain + interpret-whitespace
    dev-pipeline visible skip). Re-enumerate the actual file set with the comment-stripped
    grep at execution time - the counts drift with every landed phase; the ALLOWLIST is
    frozen by review, the scan is live. Grep hygiene: comment-strip before counting; never a
    bare ==0 gate on unfiltered file content.
    Build tests/run-all-252.sh on the tests/run-all-250.sh precedent: glob-discover
    tests/test-252-*.cjs, fail if found count is 0, em-dash fence over every phase-touched file.
    RED-FIRST PROOF (mandatory, the 249-02 honest-starting-line precedent): run
    `node tests/test-252-guard-census.cjs` BEFORE any sweep edit lands. Expected red legs at
    minimum: census.2 (no file requires refusal-messaging.cjs yet - it does not exist),
    census.3 (getTier0Chain/getFrameworkChain still live in brain-client.cjs), census.1
    (chokepoint listed under the post-rename name that does not exist yet). File the verbatim
    red output in the SUMMARY.
  </action>
  <verify>
    <automated>node tests/test-252-guard-census.cjs; test $? -ne 0</automated>
  </verify>
  <done>Census file + runner exist; census runs and is RED for the expected reasons (recorded verbatim); stripComments is the 248-01 line-comments-first order; the KEEP allowlist carries a reason per entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Counterfeit kill + chokepoint rename, wire byte-locked</name>
  <files>lib/core/brain-client.cjs, lib/core/refusal-messaging.cjs, lib/core/refusal-messaging.test.cjs, bin/mindrian-brain-mcp-client.cjs, lib/core/doctor/class-m-brain-smoke.cjs, skills/conversation-mode/SKILL.md, dist/generic-claude-dir/.claude/skills/conversation-mode/SKILL.md, dist/zed/.agents/skills/conversation-mode/SKILL.md, dist/BUNDLE-VERSION.json, tests/run-all-250.sh, tests/run-all-127.sh, tests/test-127-00-shim-handshake.sh, tests/test-127-03-canon-part-8-adversarial.sh, tests/test-127-03-no-em-dashes.sh, tests/test-250-refusal-shapes.cjs, tests/test-250-refusal-queue.cjs, tests/test-250-silent-registration.cjs</files>
  <behavior>
    - After this task, grep for getTier0Chain/getFrameworkChain returns zero executable hits
      repo-wide outside dist history and dated handoffs.
    - lib/core/refusal-messaging.cjs exists; lib/core/tier0-messaging.cjs does not; every
      export name (tier0Response, DIRECTOR_NOT_AVAILABLE constant, REFUSAL_KINDS,
      refusalResponse, renderRefusal, larryRefusalLine, refuseNotReady, and all others) is
      unchanged; the renamed module test (refusal-messaging.test.cjs) passes 8/8 with its
      byte-lock assertions intact.
    - conversation-mode SKILL.md line ~146 instructs the not_ready/unreachable refusal per the
      brain-connector Refusal section, never "use these hardcoded chains".
  </behavior>
  <action>
    (1) DELETE getTier0Chain() and getFrameworkChain() (brain-client.cjs ~lines 1137-1234,
    site 11 of 250-01's enumeration, comment-marked "flip deferred to Phase 252") including
    the hardcoded persona chain data and every source: 'tier0' literal they carry. Their only
    consumer is the conversation-mode SKILL instruction rewritten in step 3 (re-verify with a
    fresh grep first; if a new consumer appeared since plan time, route it through
    refusalResponse('not_ready', ...) instead of deleting blind). Keep the 247-02
    do-not-widen null contract on callTool() untouched.
    (2) RENAME lib/core/tier0-messaging.cjs to lib/core/refusal-messaging.cjs and
    lib/core/tier0-messaging.test.cjs to lib/core/refusal-messaging.test.cjs (git mv so
    history follows). FILE rename only: zero export renames, zero value edits. The wire
    contract is byte-locked: DIRECTOR_NOT_AVAILABLE, the five sentinel keys, tier0Response()'s
    exact shape including reason 'MINDRIAN_BRAIN_KEY not set', and the 250-04 reframed no_key
    copy all stay byte-identical (asserted by the renamed module test, test-127-00, and the
    252-02 fixture harness). Grep-enumerate EVERY reference to 'tier0-messaging' at execution
    time (plan-time list is in <context> item 3, 11 files) and update each require/path;
    plan-02 owns tests/test-127-03-acceptance-gates.sh and it carries no module-path
    reference, so do not touch it.
    (3) REWRITE skills/conversation-mode/SKILL.md's hardcoded-chains instruction (line ~146,
    locate by content "use these hardcoded chains"): when the Brain cannot serve a chain, the
    skill follows brain-connector's Refusal section (not_ready or unreachable kind as
    applicable) - it never serves a local chain and never re-drafts refusal copy (252 routes,
    250 renders; new refusal prose in this diff is scope drift). Delete the hardcoded chain
    listing itself. Regenerate dist via `node scripts/build-dist-bundles.cjs` and verify
    `--check-stale` exits 0.
    (4) Run the renamed module test, the shim test (lib/core/mindrian-brain-shim.test.cjs),
    test-127-00-shim-handshake.sh, and the 250 suite runner; all must stay green - the rename
    is proven invisible on the wire.
  </action>
  <verify>
    <automated>node lib/core/refusal-messaging.test.cjs && bash tests/test-127-00-shim-handshake.sh && bash tests/run-all-250.sh && node scripts/build-dist-bundles.cjs --check-stale</automated>
  </verify>
  <done>Chains deleted, module renamed with all consumers re-pointed, conversation-mode instruction rewritten, dist fresh, every named suite green, wire strings byte-identical (diff the renamed file against the old one: rename-only).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3a: Route + conform the code sites; re-point the degradation tests (with escape valve)</name>
  <files>lib/mcp/brain-router.cjs, lib/brain/chain-recommender.cjs, scripts/brain-derive-command.cjs, lib/brain/framework-chain-slice.cjs, lib/core/research-corpus.cjs, scripts/rs-explain-command.cjs, lib/core/brain-derivation.cjs, lib/hmi/tier-check.cjs, lib/core/rs-chain-feeder.cjs, lib/core/rs-brain-substrate.cjs, lib/core/rs-expert-brain-projection.cjs</files>
  <behavior>
    - Every ROUTE-classified code site renders a rail refusal (250 exports only; zero new
      refusal prose) on its Brain-unavailable branch; every CONFORM site's typed markers align
      with refusal-kind vocabulary and render visibly.
    - Each flipped degradation test is red-first per flip; the enumerated re-pointed list is
      the SUMMARY's operative count.
    - tests/test-acpt-05-brain-derive-tier-rise.cjs (tier_mode vocabulary, EXEMPT) and
      tests/test-249-capture-seam.cjs (hot-path fence) stay green untouched.
  </behavior>
  <action>
    This is the judgment-heavy half; apply the frozen disposition table site by site. Do NOT
    re-derive classifications, and do NOT judge any site "too hard" - every borderline was
    already ruled (research A4).
    ROUTE (through 250's exports, never new refusal prose):
    - lib/mcp/brain-router.cjs line ~277: the silent `return null` on unavailable becomes
      refusalResponse('unreachable' or 'no_key' per the actual failed resolution, {tool}).
    - lib/brain/chain-recommender.cjs lines ~345-369: the [seed]-chain degrade is methodology;
      refuse via the rail OR serve with the 250-03 disclosed-partial marking - pick refusal
      unless a caller already renders the partial provenance mark; never an unmarked serve.
    - scripts/brain-derive-command.cjs: the "Brain offline" soft-fail exit 0 keeps its exit
      code but renders renderRefusal(kind) copy instead of ad-hoc prose.
    - bin/mindrian-brain-mcp-client.cjs: already routed by 250-01/250-04; VERIFY all 6
      handlers reach refusalResponse and no residual tier0 framing remains (conformance
      check, not a rewrite; any fix rides this commit).
    - lib/core/brain-client.cjs internal guards: conform to the 247-02 sentinels; no change
      to the null contract.
    CONFORM (already typed and honest; align vocabulary only): framework-chain-slice's
    _degraded/brain_unreachable maps onto refusal kinds; research-corpus's
    failure_class 'engine_unavailable' aligns with kind names; rs-explain-command's
    _brain_degraded markers must RENDER visibly in output (verify, align copy);
    brain-derivation's typed reason stays; tier-check adopts refusal vocabulary in display
    copy.
    KEEP rulings on the two flagged borderlines (planner rulings, final): rs-chain-feeder's
    fail-open stays (it gates a workflow step, not methodology content) but its stderr warn
    upgrades to a visible line using refusal vocabulary; rs-brain-substrate and
    rs-expert-brain-projection stay KEEP after a per-site check of WHICH dependency each
    guard actually gates (Brain key vs Aura connection).
    TEST RE-POINTING: for each ROUTE file, grep tests/ for its name and its old degrade
    markers; ENUMERATE THE FULL SET FIRST and file it. ESCAPE VALVE (hard cap, plan-checker
    ruling): if the enumerated set exceeds 20 test files, STOP - commit the completed
    enumeration list plus whatever flips are already green, do NOT degrade mid-task, and
    return a supplemental-plan request (252-04, test re-pointing remainder) to the
    orchestrator with the enumerated list attached. Under the cap: flip each test asserting
    SILENT degrade semantics (empty-array serves, unmarked fallbacks) to assert refusal
    semantics, red-first per flip. Null-contract tests do NOT flip wholesale (null still
    means transport failure). tier_mode tests (acpt-05 family) and rs-* vocabulary tests are
    EXEMPT. The enumerated list in the SUMMARY is the operative replacement for the folklore
    "82" count (research Open Question 3).
    Commit this task on its own (route/conform + re-pointing), separate from Task 3b.
  </action>
  <verify>
    <automated>node tests/test-249-capture-seam.cjs && node --test tests/test-acpt-05-brain-derive-tier-rise.cjs && bash tests/run-all-250.sh</automated>
  </verify>
  <done>ROUTE sites render rail refusals; CONFORM sites aligned; borderline KEEP rulings applied with visible logging; re-pointed test list enumerated and filed (or the escape valve fired with the list + supplemental-plan request); exempt suites and the hot-path fence green; own commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3b: Instruction-surface vocabulary pass + census to green</name>
  <files>skills/rs-experts/SKILL.md, skills/rs-fetch/SKILL.md, skills/rs-explain/SKILL.md, commands/rs-experts.md, commands/rs-fetch.md, commands/rs-explain.md, dist/generic-claude-dir/.claude/skills/rs-experts/SKILL.md, dist/zed/.agents/skills/rs-experts/SKILL.md, dist/generic-claude-dir/.claude/skills/rs-fetch/SKILL.md, dist/zed/.agents/skills/rs-fetch/SKILL.md, dist/generic-claude-dir/.claude/skills/rs-explain/SKILL.md, dist/zed/.agents/skills/rs-explain/SKILL.md, dist/BUNDLE-VERSION.json, tests/test-252-guard-census.cjs</files>
  <behavior>
    - node tests/test-252-guard-census.cjs exits 0: allowlist clean, ROUTE set seam-live,
      counterfeit gone, canaries intact (the census.5 canaries prove this pass did not touch
      the local-mirror-vs-Aura vocabulary).
    - bash tests/run-all-252.sh green; the 250 doctrine fence stays green.
  </behavior>
  <action>
    The mechanical vocabulary half, isolated from Task 3a's routing judgment.
    INSTRUCTION SURFACES (per-site, the three-vocabulary rule): in skills/rs-experts,
    rs-fetch, rs-explain and their commands (the research-measured 6 md files / 8 sites;
    re-grep at execution time), rewrite ONLY sites where "Tier 0" means keyless-Brain; KEEP
    every local-mirror-vs-Aura "Tier 0/Tier 1" usage (the user's own Aura graph stays
    optional by design, Decision #6). Fix the rs-experts SKILL.md ~:64 conflation: "Detects
    Aura availability via brainClient.isAvailable()" names the WRONG probe - Aura
    availability is the Aura driver / NEO4J_URI check alone; isAvailable() is the Brain key
    and must not proxy for Aura. Instruction-text fix only; a code-level Aura probe is a
    filed follow-up, not this sweep.
    Regenerate dist (`node scripts/build-dist-bundles.cjs`), verify --check-stale exit 0.
    Then run the census to green and the full phase runner. If the census reds on anything
    Task 3a missed, fix at the classified site (never by widening the allowlist - the
    allowlist is frozen by review; an allowlist edit requires a recorded reason in the
    SUMMARY).
    Commit this task on its own, separate from Task 3a.
  </action>
  <verify>
    <automated>node tests/test-252-guard-census.cjs && bash tests/run-all-252.sh && node --test tests/test-250-doctrine-fence.cjs && node scripts/build-dist-bundles.cjs --check-stale</automated>
  </verify>
  <done>Census green (red-first output already filed in Task 1); rs-* instruction sites rewritten per-site with Aura semantics untouched; :64 probe conflation fixed; dist fresh; full phase runner and doctrine fence green; own commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin -> Brain wire | Part 8: only generic handles/enums cross; unchanged by this plan |
| refusal copy -> user | Rendered strings must never carry key values or user bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-252-01 | Repudiation | swept guard sites | mitigate | Census.1-3 make silent degrade structurally unreachable; red-first proof filed |
| T-252-02 | Information Disclosure | refusal rendering at routed sites | mitigate | Only 250's closed-enum kinds interpolate; no new refusal prose in 252 diffs (drift check at review) |
| T-252-03 | Tampering | part8-egress-guard-hook.cjs | accept (KEEP) | Unavailable = safe state; routing it would be actively wrong; allowlisted with reason |
| T-252-SC | Tampering | package installs | accept | Zero installs this phase (research Package Legitimacy Audit: not applicable; any install task is invalid) |
</threat_model>

<verification>
- `node tests/test-252-guard-census.cjs` green (and its RED pre-sweep output filed in SUMMARY)
- `bash tests/run-all-252.sh` green
- `node lib/core/refusal-messaging.test.cjs`, `bash tests/test-127-00-shim-handshake.sh`, `bash tests/run-all-250.sh` green (wire byte-lock proof)
- `node tests/test-249-capture-seam.cjs`, `node --test tests/test-250-doctrine-fence.cjs` green (adjacent fences unbroken)
- `node scripts/build-dist-bundles.cjs --check-stale` exit 0
- `node scripts/check-shape-declaration.cjs --check` no NEW warns vs the 53-warn baseline; `node scripts/build-connector-registry.cjs --check` OK
- Em-dash check over every touched file: zero hits
- Four task commits (census-RED, kill+rename, route/conform+re-point, instruction+green) - or three plus the escape-valve state if the re-pointing cap fired
</verification>

<success_criteria>
SWEEP-01 executable half complete: counterfeit deleted, chokepoint renamed with wire
byte-locked, ~8 routed / ~6 conformed / ~15 kept with reasons, degradation tests re-pointed
with an enumerated operative list (or the >20-file escape valve fired cleanly into a
supplemental-plan request), census green with a filed red-first proof, all three vocabulary
collisions respected.
</success_criteria>

<output>
Create `.planning/phases/252-guard-sweep/252-01-SUMMARY.md` when done. MUST include: the
verbatim census RED output, the enumerated re-pointed test list (and, if the escape valve
fired, the supplemental-plan request for 252-04), and the live consumer-grep result used for
the rename.
</output>
