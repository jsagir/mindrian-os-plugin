---
phase: 192-shape-f-hitl-selector-completion
plan: 05
type: execute
wave: 3
depends_on: [192-01, 192-02, 192-03, 192-04]
files_modified:
  - tests/run-all-192.sh
  - .planning/phases/192-shape-f-hitl-selector-completion/192-VERDICT.md
autonomous: true
requirements: [SFC-01, SFC-02, SFC-03, SFC-04, SFC-05, SFC-06, SFC-07, SFC-08, SFC-09]

must_haves:
  truths:
    - "One aggregator (tests/run-all-192.sh) composes every suite this phase shipped (192-01 menu sweep, 192-02 F.7-max, 192-03 stance core, 192-04 statusline+eval) plus the CARRIED frozen-scalar/posture-ids/reach-ids drift fences, and exits 0 end to end"
    - "The born-wired and render-coverage gates (build-connector-registry.cjs --check, check-render-coverage.cjs) both stay green with the new commands/stance.md surface included"
    - "A structured 192-VERDICT.md records PASS/FAIL per suite, the em-dash sweep result, and the Part 8 (Brain-egress) grep sweep result across every file this phase touched"
  artifacts:
    - path: "tests/run-all-192.sh"
      provides: "the single PASS/FAIL phase gate for shape-f-hitl-selector-completion"
      contains: "run-all-192"
    - path: ".planning/phases/192-shape-f-hitl-selector-completion/192-VERDICT.md"
      provides: "the adversarial structured verdict closing the phase"
      contains: "VERDICT"
  key_links:
    - from: "tests/run-all-192.sh"
      to: "tests/test-posture-ids-drift.cjs"
      via: "CARRIED frozen-set fence -- proves the stance work (192-03/04) never touched the exactly-3 posture-id vocabulary"
      pattern: "posture-ids-drift"
    - from: "tests/run-all-192.sh"
      to: "scripts/build-connector-registry.cjs"
      via: "--check leg confirming commands/stance.md is WIRED-or-EXCLUDED, not dark"
      pattern: "build-connector-registry"
---

<objective>
Phase gate aggregator + structured verdict. Composes every suite the four Wave-1/Wave-2 plans
shipped into one `tests/run-all-192.sh` (mirroring the `run-all-188.sh` / `run-all-191.sh` house
pattern), re-runs the carried frozen-scalar and posture-id drift fences to prove zero regression,
confirms the born-wired and render-coverage gates stay green with the new `commands/stance.md`
surface, and produces the adversarial structured verdict that closes the phase.

Purpose: no phase in this codebase closes on an unaggregated pile of green test files -- one
command, one PASS/FAIL/SKIP readout, one written verdict.
Output: tests/run-all-192.sh, 192-VERDICT.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-shape-f-hitl-selector-completion/192-CONTEXT.md
@.planning/phases/192-shape-f-hitl-selector-completion/192-01-PLAN.md
@.planning/phases/192-shape-f-hitl-selector-completion/192-02-PLAN.md
@.planning/phases/192-shape-f-hitl-selector-completion/192-03-PLAN.md
@.planning/phases/192-shape-f-hitl-selector-completion/192-04-PLAN.md
@CLAUDE.md
@tests/run-all-188.sh
@tests/run-all-191.sh
</context>

## Artifacts this phase produces (this plan's share)

- New `tests/run-all-192.sh` -- the single phase-gate aggregator.
- New `.planning/phases/192-shape-f-hitl-selector-completion/192-VERDICT.md` -- the structured,
  adversarial closing verdict.

<rules>
## RULES (Phase 192 harness -- restate every plan)
- R1 Bash only for the runner. No em-dashes anywhere (hyphens only).
- R2 Mirror `tests/run-all-188.sh` / `tests/run-all-191.sh` exactly: a `run()` helper that increments
  PASS/FAIL, a `run_if()` helper that SKIPs a leg whose test file does not exist (defensive against
  wave-ordering surprises), and a final PASS/FAIL/SKIP tally with a non-zero exit on any FAIL.
- R3 Compose EVERY suite this phase shipped: `tests/test-192-menu-sweep-live-selectors.cjs` (192-01),
  `tests/test-acpt-06-dial-atomic-emission.cjs` + `tests/test-f7max-preview-confidence-bar.cjs` +
  `tests/test-f7max-modifier-pane.cjs` + `tests/test-dial-render-states.cjs` (192-02),
  `tests/test-stance-state.cjs` + `tests/test-stance-toggle-f0-gate.cjs` +
  `tests/test-stance-voice-glyph-override.cjs` (192-03), `tests/test-192-statusline-stance-chip.cjs`
  (192-04). Plus the CARRIED frozen-set drift fences: `tests/test-posture-ids-drift.cjs` (exactly 3,
  no 4th -- proves zero stance/posture collision) and `tests/test-reach-ids-drift.cjs` (exactly 6,
  no 7th -- proves this phase minted no new reach). Plus the two born-wired/render-coverage gates:
  `node scripts/build-connector-registry.cjs --check` and `node scripts/check-render-coverage.cjs`.
- R4 Every leg is `run_if`-guarded by file existence (a leg whose file is missing SKIPs, it does not
  FAIL) -- this runner may be authored/run before every Wave-1/2 plan has landed in a given
  execution order, mirroring the `run-all-188.sh` Wave-0-authored-early convention.
- R5 The 192-VERDICT.md is an ADVERSARIAL read: it does not just restate "all green," it explicitly
  checks (a) the stance/posture naming disambiguation held (grep sweep confirms
  `lib/core/stance-state.cjs` never contains `push_forward`/`pull_back`/bare `hold`), (b) the F.0
  closed-vocab was never widened (grep sweep over `lib/hmi/shape-f0-renderer.cjs` confirms it is
  BYTE-UNCHANGED by this phase -- diff against git HEAD before this phase's first commit), (c) the
  frozen Part 3 scalars (MAX_K=3, DIAL_REACH_K=6, 0.70/0.15) are unchanged in `lib/hmi/dial-presenter.cjs`
  and `lib/hmi/dial-reach-orchestrator.cjs`, and (d) an em-dash sweep across every file this phase
  touched returns zero matches.
- R6 Exclusive file ownership: this plan owns only `tests/run-all-192.sh` and `192-VERDICT.md`. It
  reads (never edits) every other file the four prior plans produced.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: tests/run-all-192.sh -- the phase gate aggregator</name>
  <files>tests/run-all-192.sh</files>
  <read_first>
    - tests/run-all-188.sh (the run()/run_if() helper pattern + PASS/FAIL/SKIP tally + the carried frozen-set drift-fence legs, byte-for-pattern the template to mirror)
    - tests/run-all-191.sh (a second, more recent example of the same aggregator pattern)
    - the four sibling plans in this phase (192-01..04) for the exact list of test files each shipped
  </read_first>
  <action>
    Author `tests/run-all-192.sh` mirroring `tests/run-all-188.sh`'s structure exactly (shebang,
    `set -uo pipefail`, `ROOT` resolution, `PASS`/`FAIL`/`SKIP` counters, `run()` and `run_if()`
    helpers). Compose the legs listed in RULES R3 above, in this order: (1) the 192-01 menu-sweep
    suite, (2) the 192-02 Finding-1 ACPT-06 suite + the two new Finding-2 suites +
    `test-dial-render-states.cjs` regression, (3) the 192-03 stance-state/toggle/voice-glyph
    suites, (4) the 192-04 statusline-chip suite, (5) the CARRIED `test-posture-ids-drift.cjs` +
    `test-reach-ids-drift.cjs` fences, (6) `node scripts/build-connector-registry.cjs --check` and
    `node scripts/check-render-coverage.cjs` as direct `run()` legs (not file-existence-guarded --
    these scripts already exist in the repo today). Head-comment the file explaining what Phase 192
    ships (mirroring the doc-comment style at the top of run-all-188.sh) and citing the SFC-01..09
    requirement range.
  </action>
  <acceptance_criteria>
    <automated>bash tests/run-all-192.sh; echo "EXIT:$?"</automated>
    Passes when: the aggregator runs every leg listed in RULES R3, prints a final PASS/FAIL/SKIP
    tally, and exits 0 (0 FAIL) once all four sibling plans have landed. A leg whose test file is
    not yet present SKIPs rather than failing (verifiable by temporarily running this script before
    a sibling plan's file exists, if executed out of order).
  </acceptance_criteria>
  <done>tests/run-all-192.sh aggregates all Phase 192 suites plus the carried drift fences plus the two coverage gates, exits 0 once every sibling plan is complete.</done>
</task>

<task type="auto">
  <name>Task 2: 192-VERDICT.md -- the adversarial structured verdict</name>
  <files>.planning/phases/192-shape-f-hitl-selector-completion/192-VERDICT.md</files>
  <read_first>
    - .planning/phases/191-brain-orchestration-advisor/191-VERDICT.md (the structured-verdict format precedent to mirror: per-suite PASS/FAIL table, named invariant checks, a closing disposition)
    - lib/hmi/shape-f0-renderer.cjs (diff target for the closed-vocab-unwidened check)
    - lib/hmi/dial-presenter.cjs, lib/hmi/dial-reach-orchestrator.cjs (diff/grep target for the frozen-scalar check)
  </read_first>
  <action>
    Run `bash tests/run-all-192.sh` and capture its tally. Run the four adversarial checks named in
    RULES R5: (a) `grep -c "push_forward\|pull_back" lib/core/stance-state.cjs` must be 0; (b)
    `git diff HEAD~<N> -- lib/hmi/shape-f0-renderer.cjs` (or an equivalent pre-phase ref) must show
    NO changes to that file across this phase's commits; (c) grep `lib/hmi/dial-presenter.cjs` and
    `lib/hmi/dial-reach-orchestrator.cjs` for the literal strings `0.70`, `0.15`, `MAX_K`,
    `DIAL_REACH_K` and confirm the counts match the pre-phase baseline (no scalar value changed,
    only usage sites may have grown); (d) run an em-dash sweep (`grep -rn "—" <every file this
    phase's five plans touched>`) and confirm zero matches. Write `192-VERDICT.md` with: a per-suite
    PASS/FAIL/SKIP table (one row per `run-all-192.sh` leg), a named-invariant checklist (the four
    checks above, each with its command and result), a one-paragraph disposition (PASS/FAIL for the
    phase overall), and a short "known debts" section noting anything explicitly deferred (e.g. a
    Plurai hand-labeled degrade if the hosted eval was unavailable in 192-04, or the
    `commands/rooms.md` subcommands intentionally left as terminal text confirmations per 192-01
    Task 2's documented scope note).
  </action>
  <acceptance_criteria>
    <automated>test -f .planning/phases/192-shape-f-hitl-selector-completion/192-VERDICT.md && grep -qi "PASS\|FAIL" .planning/phases/192-shape-f-hitl-selector-completion/192-VERDICT.md && bash tests/run-all-192.sh; echo "EXIT:$?"</automated>
    Passes when: the verdict file exists, records an explicit PASS/FAIL disposition, and the
    aggregator it reports on exits 0.
  </acceptance_criteria>
  <done>192-VERDICT.md records a per-suite PASS/FAIL table, all four adversarial invariant checks, and a closing disposition for the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| aggregator exit code -> phase-completion decision | the single boolean gate downstream tooling (doctor --acceptance, release.sh) will eventually read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-192-05-01 | Repudiation | 192-VERDICT.md hand-edited after the fact | accept | Same class of trust as every other `*-VERDICT.md` in this repo (191, etc.); not a new risk this plan introduces |
| T-192-05-02 | Tampering | run-all-192.sh silently SKIPping a leg that should FAIL | mitigate | `run_if()` only SKIPs on file-ABSENCE, never on a failing assertion inside an existing file; a present-but-failing suite always counts as FAIL, never SKIP |
</threat_model>

<verification>
- `bash tests/run-all-192.sh` exits 0.
- `.planning/phases/192-shape-f-hitl-selector-completion/192-VERDICT.md` exists and states a clear disposition.
- All four adversarial invariant checks (stance/posture collision, F.0 closed-vocab unwidened, frozen-scalar counts unchanged, em-dash sweep) pass.
</verification>

<success_criteria>
- One aggregator command proves the entire phase green.
- The structured verdict is adversarial, not celebratory: it names what it checked and what it found, including any deferred debt.
- Phase 192 closes with zero drift against the frozen posture-id set, the frozen Part 3 scalars, and the F.0 closed-vocab contract.
</success_criteria>

<output>
Create `.planning/phases/192-shape-f-hitl-selector-completion/192-05-SUMMARY.md` when done
</output>
