---
phase: 192-shape-f-hitl-selector-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/suggest-next.md
  - commands/rooms.md
  - commands/onboard.md
  - tests/test-192-menu-sweep-live-selectors.cjs
autonomous: true
requirements: [SFC-01, SFC-02, SFC-03, SFC-04]

must_haves:
  truths:
    - "The navigator gap-checks /mos:help and /mos:mos and confirms they already route through a live AskUserQuestion selector (no regression introduced)"
    - "/mos:suggest-next ends its recommendation with a live AskUserQuestion F.1 card (the 2-3 ranked next moves + Free-Text), not a narrative-only paragraph"
    - "/mos:rooms list (and where) end with a live AskUserQuestion F.1 card built from the same Action Footer options today rendered as bullet text"
    - "/mos:onboard Step 1 (Three Ways to Work) renders as a live AskUserQuestion card (3 modes + the free-text 'just start talking' floor), never only the flat 'Type 1, 2, or 3' text block"
    - "--list / --all / non-TTY / Desktop text fallbacks are preserved byte-equivalent wherever they existed before this plan"
  artifacts:
    - path: "commands/suggest-next.md"
      provides: "F.1 live-selector close over the resolver-composed command sequence"
      contains: "AskUserQuestion"
    - path: "commands/rooms.md"
      provides: "F.1 live-selector close on the list/where Action Footer"
      contains: "AskUserQuestion"
    - path: "commands/onboard.md"
      provides: "F.1 live-selector close on the Step 1 mode picker"
      contains: "AskUserQuestion"
    - path: "tests/test-192-menu-sweep-live-selectors.cjs"
      provides: "grep-structural regression fence over all three conversions + the help/mos gap-check note"
      min_lines: 40
  key_links:
    - from: "commands/suggest-next.md"
      to: "lib/hmi/shape-f1-renderer.cjs"
      via: "reuse note citing renderShapeF1 / the SEED-020 selector-dispatcher door, no bespoke widget"
      pattern: "shape-f1-renderer|selector-dispatcher"
    - from: "commands/rooms.md"
      to: "lib/hmi/shape-f1-renderer.cjs"
      via: "reuse note citing renderShapeF1 / the SEED-020 selector-dispatcher door"
      pattern: "shape-f1-renderer|selector-dispatcher"
    - from: "commands/onboard.md"
      to: "lib/hmi/shape-f1-renderer.cjs"
      via: "reuse note citing renderShapeF1 / the SEED-020 selector-dispatcher door"
      pattern: "shape-f1-renderer|selector-dispatcher"
---

<objective>
SEED-020 GAP-CHECK + FINISH (Phase 192 part a). Confirm /mos:help and /mos:mos already satisfy
"command menus render as live selectors, never bare text" (Canon Part 10 + Part 3 Shape F), then
close the three real gaps the sweep found: /mos:suggest-next (narrative-only close), /mos:rooms
(text Action Footer on list/where), and /mos:onboard Step 1 (flat "Type 1, 2, or 3" menu). All
three route through the SAME shipped SEED-020 construction door (shape-f1-renderer.cjs /
selector-dispatcher.cjs) -- no new widget, no bespoke AskUserQuestion construction.

Purpose: A text-list chooser is the exact "command-driven, not conversation-driven" anti-shape
Canon Part 10 argues against. This plan makes the navigator's actual next-move choice a live,
arrow-navigable card everywhere the roadmap named it, while the --list/--text non-interactive
floor stays intact for Desktop/Cowork/piped/CI callers.
Output: three edited command prompt files + one regression-fence test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-shape-f-hitl-selector-completion/192-CONTEXT.md
@CLAUDE.md
@docs/MINDRIAN-CANON.md
@commands/help.md
@commands/suggest-next.md
@commands/rooms.md
@commands/onboard.md
@lib/hmi/shape-f1-renderer.cjs
@lib/hmi/selector-dispatcher.cjs
@tests/test-help-selector-lanes.cjs
</context>

## Artifacts this phase produces (this plan's share)

- Edited `commands/suggest-next.md` -- narrative close replaced with a live F.1 AskUserQuestion card.
- Edited `commands/rooms.md` -- `list`/`where` Action Footer becomes a live F.1 AskUserQuestion card; `new`/`open`/`close`/`archive`/`git-setup`/`git-status` keep their existing 2-3 line text footer (out of scope this pass -- they are terminal confirmations, not multi-option next-move choices; noted as a named follow-up, not silently dropped).
- Edited `commands/onboard.md` -- Step 1 "Three Ways to Work" becomes a live F.1 AskUserQuestion card (3 modes + Free-Text), replacing the flat `Type 1, 2, or 3` text block.
- New `tests/test-192-menu-sweep-live-selectors.cjs` -- grep-structural fence locking all three conversions + recording the /mos:help and /mos:mos gap-check result.

<rules>
## RULES (Phase 192 harness -- restate every plan)
- R1 CJS only for any script/test; command files stay Markdown+YAML frontmatter. No em-dashes anywhere (hyphens only).
- R2 SEED-020 single door: every new selector close cites `lib/hmi/shape-f1-renderer.cjs` (`renderShapeF1`) and/or `lib/hmi/selector-dispatcher.cjs` (`pickShape`/`appendAskUserQuestionTrailer`). No bespoke AskUserQuestion JSON hand-built inside a command prompt; the prompt instructs Larry to call the AskUserQuestion tool with the SAME verb/option shape the renderer already composes.
- R3 --list / --all / non-TTY / Desktop / Cowork text fallback is the FLOOR everywhere. Never remove or narrow it; only ADD the live-selector close.
- R4 Frozen Part 3 contracts (MAX_K=3, DIAL_REACH_K=6, 0.70/0.15, the F.1 keyboard contract, Free-Text-always-last) are UNCHANGED. This plan touches ZERO renderer code -- it is a prompt-doctrine-only pass over three command files.
- R5 Exclusive file ownership: this plan owns `commands/suggest-next.md`, `commands/rooms.md`, `commands/onboard.md`, and the one new test file. No other Wave-1 plan (192-02, 192-03) touches these paths.
- R6 Part 8: no Brain wire changes. Zero new external calls.
- R7 Do NOT touch /mos:help (`commands/help.md`) or /mos:mos (`commands/mos.md`) production content -- the gap-check confirms they are already compliant; if the gap-check finds a regression, STOP and report it in the SUMMARY rather than silently patching scope creep into this plan.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: SEED-020 gap-check -- confirm /mos:help and /mos:mos are already live selectors</name>
  <files>tests/test-192-menu-sweep-live-selectors.cjs (gap-check assertions only, no command-file edits)</files>
  <read_first>
    - commands/help.md lines 25-42 (the two-axis lanes-as-tabs AskUserQuestion selector, already shipped per commit 9a18fe81 / SUMMARY 20260606-help-shape-f)
    - commands/mos.md (the state-aware router -- Shape E Action Report; it delegates to onboard/status/suggest-next and explicitly does NOT itself render a chooser: "the routing IS the response")
    - tests/test-help-selector-lanes.cjs (the existing 5-assertion lane-coverage fence)
    - scripts/check-help-coverage.cjs
  </read_first>
  <action>
    Confirm by reading (not re-implementing) that /mos:help already satisfies SEED-020: it renders
    ONE AskUserQuestion call with up to 4 lane-tab questions, each lane's options are that lane's
    commands, and `--list`/`--all`/non-TTY fall back to the De Stijl text card view. Confirm /mos:mos
    is correctly OUT of scope for a selector conversion: it is a Shape E router that resolves and
    hands off to a target command in the SAME turn ("Do NOT echo the routing decision and then ask
    the user to type the target command"), so the chooser experience it needs is inherited from
    whichever target it routes to (onboard / status / suggest-next), not owned by mos.md itself. Run
    `node tests/test-help-selector-lanes.cjs` and `node scripts/check-help-coverage.cjs` and capture
    their exit codes. Write the gap-check result as a documentation block (a top-of-file comment,
    NOT a new runtime assertion against files this plan does not touch) in the new test file created
    for Task 2/3, recording: help.md = COMPLIANT (already live selector, no change), mos.md = N/A
    (router, inherits from delegate), suggest-next.md / rooms.md / onboard.md = GAP (fixed by Tasks
    2-3 below). This keeps the gap-check auditable without inventing a synthetic pass/fail on files
    outside this plan's ownership.
  </action>
  <acceptance_criteria>
    <automated>node tests/test-help-selector-lanes.cjs && node scripts/check-help-coverage.cjs; echo "EXIT:$?"</automated>
    Passes when: both commands exit 0 (confirms no regression exists in the already-shipped /mos:help
    selector before this plan adds new conversions alongside it).
  </acceptance_criteria>
  <done>Gap-check performed and recorded; help.md/mos.md confirmed already-compliant with zero edits made to either file.</done>
</task>

<task type="auto">
  <name>Task 2: Convert /mos:suggest-next and /mos:rooms (list/where) to live F.1 selectors</name>
  <files>commands/suggest-next.md, commands/rooms.md, tests/test-192-menu-sweep-live-selectors.cjs</files>
  <read_first>
    - commands/suggest-next.md section "5. Present Through Larry's Voice" (the narrative-only close: "End with: Want me to start any of these right now?") and its `connector.surface: F.1` frontmatter (the command ALREADY declares F.1; only the body's close is text-only)
    - commands/rooms.md "Subcommand: list" Step 3 ("Action Footer (Zone 4)... Suggest 2-3 grounded next steps") and "Subcommand: where" Step 3
    - lib/hmi/shape-f1-renderer.cjs (renderShapeF1 API: tier, recommendedVerb, verbs, header; Free-Text always appended)
    - lib/hmi/selector-dispatcher.cjs appendAskUserQuestionTrailer (the trailer contract the host reads to fire the real tool call)
  </read_first>
  <action>
    In commands/suggest-next.md, replace step 5's narrative close ("Present recommendations
    conversationally... End with: Want me to start any of these right now?") with an instruction to
    render the 2-3 ranked next-move recommendations (already computed by the resolver in "The resolver
    is the only door" section) as a live F.1 AskUserQuestion card: one option per recommended
    `/mos:` command sequence entry (label = the command, description = the one-line why-this-sequence
    reason already computed), capped at the existing 3-5 option F.1 rule, Free-Text last ("something
    else / just tell me more"). State explicitly that this reuses `renderShapeF1`'s verb/option shape
    (cite the file) -- the command markdown instructs Larry to compose the AskUserQuestion tool call
    with that shape, not to invent new JSON structure. Preserve the "When the Room is Empty" fallback
    text path unchanged (that branch has no ranked list to choose from, so it stays prose).
    In commands/rooms.md, change the `list` subcommand's Step 3 ("Suggest 2-3 grounded next steps")
    from a plain bullet list to a live F.1 AskUserQuestion card with the SAME 2-3 options (open a
    parked room / create a new room / where) as its choices, Free-Text appended. Apply the identical
    treatment to the `where` subcommand's closing suggestion block. Leave `new`/`open`/`close`/
    `archive`/`git-setup`/`git-status` footers untouched (they are single terminal confirmations, not
    multi-option next-move choices -- note this explicitly in a one-line comment in rooms.md so a
    future sweep does not re-discover this as an unexplained gap). Update the `commands/rooms.md`
    frontmatter comment (not the `connector.excluded` block, which stays true) to note the F.1 close
    on `list`/`where`.
  </action>
  <acceptance_criteria>
    <automated>grep -q "AskUserQuestion" commands/suggest-next.md && grep -q "AskUserQuestion" commands/rooms.md && grep -q -- "--list" commands/help.md && echo OK</automated>
    Passes when: both edited files reference the live AskUserQuestion close, and the unrelated
    --list floor documented in help.md (used as the cross-file non-interactive-floor invariant
    check) is still present, proving no floor text was deleted project-wide by this change.
  </acceptance_criteria>
  <done>suggest-next.md's recommendation close and rooms.md's list/where footers render as live F.1 cards; all other rooms.md subcommands are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Convert /mos:onboard Step 1 mode-picker to a live F.1 selector + finalize the sweep test</name>
  <files>commands/onboard.md, tests/test-192-menu-sweep-live-selectors.cjs</files>
  <read_first>
    - commands/onboard.md "Step 1: The Three Ways to Work" (the flat `Type 1, 2, or 3 -- or just start talking (defaults to Mode 1)` text block)
    - commands/onboard.md "Step 6: Wrap + Suggested First Action" (the ALREADY-shipped AskUserQuestion F.1 close at line 369 -- the pattern to mirror)
    - Canon Part 10 sub-claim 2 ("Conversation IS the surface... whether they paste a CV or type a stuck-decision sentence") -- the free-text/"just start talking" path MUST remain first-class, never demoted to a hidden Other row
  </read_first>
  <action>
    Replace the flat `======` ASCII text menu in Step 1 with an instruction to render the three modes
    (Just Talk / Explore + Capture / Build a Room) as a live F.1 AskUserQuestion card, mirroring the
    exact pattern already shipped at Step 6 (cite that section instead of re-deriving the mechanism).
    Each option's label is the mode name, description is its one-line JTBD framing (already written
    in the surrounding prose -- reuse verbatim, do not rewrite the persona-example copy below it).
    Free-Text is the built-in "just start talking" path (AskUserQuestion's Other/free-text slot IS the
    "no wrong door" floor Canon Part 10 requires) -- state this explicitly so Larry never suppresses
    it. After the card fires and the navigator picks (or types past it), continue exactly as today:
    walk through the persona-specific examples for the chosen mode. Preserve the "just start talking
    (defaults to Mode 1)" default behavior for a bare conversational turn with no selector engagement.
    Then finalize tests/test-192-menu-sweep-live-selectors.cjs: it must assert (a) the Task-1 gap-check
    documentation block is present, (b) `commands/suggest-next.md` contains "AskUserQuestion" AND no
    longer contains the retired literal string "Want me to start any of these right now", (c)
    `commands/rooms.md` contains "AskUserQuestion", (d) `commands/onboard.md` contains "AskUserQuestion"
    at BOTH Step 1 and Step 6 (two occurrences minimum), and (e) `commands/onboard.md` still contains
    "just start talking" (the free-text floor phrase) so the conversion did not remove the no-wrong-door
    guarantee.
  </action>
  <acceptance_criteria>
    <automated>node tests/test-192-menu-sweep-live-selectors.cjs; echo "EXIT:$?"</automated>
    Passes when: all five assertions in the finalized test pass and the script exits 0.
  </acceptance_criteria>
  <done>onboard.md Step 1 is a live F.1 card with the free-text floor intact; the sweep regression test is green and locks all three conversions plus the help/mos gap-check note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| navigator input -> AskUserQuestion option pick | the navigator's card selection or free-text reply is untrusted input entering the command's next step |
| command prompt -> Skill tool invocation | the picked option string routes to a `/mos:<command>` Skill call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-192-01-01 | Tampering | commands/suggest-next.md option labels | mitigate | Labels are sourced ONLY from the resolver's already-registry-validated command sequence (never typed from memory), per the existing "Larry NEVER names a /mos: command from memory" rule -- unchanged by this plan |
| T-192-01-02 | Spoofing | Free-Text reply in onboard.md Step 1 | accept | Free-text is interpreted by Larry, not executed as a shell command; no injection surface (no bash/eval on the raw reply) |
| T-192-01-03 | Information Disclosure | rooms.md list/where footer options | accept | Options are room-registry-derived room names/slugs already shown in the same render pass; no new data surfaced |
| T-192-01-04 | Denial of Service | pagination loops in help.md (unaffected by this plan) | accept | Out of scope; help.md is read-only reference context here, not modified |
</threat_model>

<verification>
- `node tests/test-192-menu-sweep-live-selectors.cjs` exits 0.
- `node tests/test-help-selector-lanes.cjs` and `node scripts/check-help-coverage.cjs` both exit 0 (no regression to the already-shipped help selector).
- Manual grep confirms `commands/rooms.md`'s untouched subcommands (`new`, `open`, `close`, `archive`, `git-setup`, `git-status`) are byte-identical apart from the one documentation comment noted in Task 2.
</verification>

<success_criteria>
- SEED-020 gap-check documented: help.md/mos.md already compliant, three real gaps closed.
- suggest-next.md, rooms.md (list/where), onboard.md (Step 1) all close with a live AskUserQuestion F.1 card reusing shape-f1-renderer.cjs's shape.
- --list/--all/non-TTY/free-text floors preserved everywhere.
- Zero renderer code touched; zero frozen Part 3 scalar changed.
</success_criteria>

<output>
Create `.planning/phases/192-shape-f-hitl-selector-completion/192-01-SUMMARY.md` when done
</output>
