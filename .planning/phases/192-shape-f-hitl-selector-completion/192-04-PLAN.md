---
phase: 192-shape-f-hitl-selector-completion
plan: 04
type: execute
wave: 2
depends_on: [192-03]
files_modified:
  - lib/statusline/cockpit-signals.cjs
  - lib/statusline/cockpit-renderer.cjs
  - tests/test-192-statusline-stance-chip.cjs
  - evals/plurai/09-posture-framing-fidelity.csv
  - evals/plurai/192-baseline.json
  - evals/plurai/README.md
autonomous: true
requirements: [SFC-09]

must_haves:
  truths:
    - "When a stance override is active, the CLI statusline shows a persistent, passive [stance] chip naming the current pole; when no override is active (default), the statusline renders byte-identical to today"
    - "The forced voice-glyph color (redteam=red, tell-act=blue) established in 192-03 is reflected in the SAME statusline render pass -- the pole IS the voice glyph, not a second signal"
    - "A Plurai eval GATE ships for this phase: a synthetic CSV judging posture-framing fidelity (does a sampled Larry turn match its declared stance's framing) plus a persisted baseline, reusing the Phase 196 plurai-baseline harness pattern -- the phase does not close without it"
  artifacts:
    - path: "lib/statusline/cockpit-signals.cjs"
      provides: "readStanceState() -- reads lib/core/stance-state.cjs's readStance()/forcedVoiceColorForStance() into the cockpit signal set"
      exports: ["collectSignals"]
    - path: "lib/statusline/cockpit-renderer.cjs"
      provides: "the optional [stance] chip + the forced voice_color override, additive to _render()"
      exports: ["renderCockpit"]
    - path: "evals/plurai/09-posture-framing-fidelity.csv"
      provides: "synthetic Sample,Label,Reasoning rows judging stance-vs-framing fidelity"
      contains: "Sample,Label,Reasoning"
    - path: "evals/plurai/192-baseline.json"
      provides: "the persisted precision/recall (or hand-labeled degrade) baseline gating phase closure"
      contains: "method"
  key_links:
    - from: "lib/statusline/cockpit-signals.cjs"
      to: "lib/core/stance-state.cjs"
      via: "require + readStance()/forcedVoiceColorForStance() -- read-only consumption, no re-implementation"
      pattern: "stance-state"
    - from: "evals/plurai/09-posture-framing-fidelity.csv"
      to: "evals/plurai/196-baseline.json"
      via: "mirrors the same CSV schema + offline-eval-then-degrade workflow the Phase 196 harness established"
      pattern: "method"
---

<objective>
SEED-042 POSTURE DIAL, CLI enhancement + eval gate (Phase 192 part c, continued). Adds the
statusline "always visible, survives every turn" persistence layer (the CLI enhancement named in
the roadmap, on top of 192-03's cross-surface footer-affordance baseline), and ships the Plurai
eval GATE leg the phase does not close without.

Purpose: SEED-042 explicitly ranks the statusline segment as the CLI enhancement ("Best
persistence") over the footer-affordance baseline (192-03, cross-surface). This plan wires the
READ side onto the already-shipped Phase 187 cockpit using the exact same LOCAL-JSON-side-channel
pattern the voice-switch chip already established, then closes the phase-level evaluation debt with
a Plurai suite judging whether a stance-declared turn actually reads as that stance.
Output: two edited statusline modules, one new test, one new eval CSV + baseline JSON.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-shape-f-hitl-selector-completion/192-CONTEXT.md
@.planning/phases/192-shape-f-hitl-selector-completion/192-03-PLAN.md
@.planning/seeds/SEED-042-always-on-act-redteam-toggle.md
@CLAUDE.md
@lib/statusline/cockpit-signals.cjs
@lib/statusline/cockpit-renderer.cjs
@docs/STATUSLINE-CONTRACT.md
@evals/plurai/README.md
@.planning/phases/196-part8-runtime-slm-guardrail/196-02-plurai-baseline-PLAN.md
@lib/core/stance-state.cjs
</context>

## Artifacts this phase produces (this plan's share)

- Edited `lib/statusline/cockpit-signals.cjs` -- a new `readStanceState()` reader consuming
  `lib/core/stance-state.cjs` (from 192-03), folded into `collectSignals()`'s returned state.
- Edited `lib/statusline/cockpit-renderer.cjs` -- an additive `[stance]` chip in `_render()` shown
  only when a stance override is active, plus the forced-voice-color override applied to the
  existing `resolveVoiceGlyph` input.
- New `tests/test-192-statusline-stance-chip.cjs`.
- New `evals/plurai/09-posture-framing-fidelity.csv` + `evals/plurai/192-baseline.json`, and a new
  README.md table row documenting the judge prompt (mirrors the Phase 196 CSV+baseline pattern).

<rules>
## RULES (Phase 192 harness -- restate every plan)
- R1 CJS only, no TypeScript. No em-dashes (hyphens only).
- R2 Depends on 192-03: import `lib/core/stance-state.cjs`'s `readStance()` /
  `forcedVoiceColorForStance()` READ-ONLY. Do not re-implement, re-export, or fork either function;
  do not re-declare `STANCES` locally.
- R3 Statusline is a PASSIVE render surface (cannot capture keys/Enter, redrawn every ~300ms per
  docs/STATUSLINE-CONTRACT.md). This plan adds a READ + DISPLAY chip only -- it never renders an
  AskUserQuestion card from the statusline path, and it never adds a "4th chip" to the existing
  Tier-1 WHO sub-segment (that segment stays exactly as shipped: HEX + optional voiceGlyph +
  agentSeg). The stance chip is its own NEW, INDEPENDENT, conditionally-rendered segment appended
  to the segment array, following the SAME pattern as the existing `brainOn`/`risk`/`bar` optional
  segments -- not a modification of the WHO sub-segment's internal composition.
- R4 Default path byte-stability: when `readStance()` returns `null` (the default, no override
  active), `_render()`'s output MUST be byte-identical to pre-plan output. The new chip and the
  forced-color override apply ONLY when a non-null stance is active.
- R5 Part 8: zero Brain wire, zero network. `readStanceState()` mirrors the existing
  `readVoiceSwitchState()` try/catch-degrade idiom in the SAME file -- never throws.
- R6 Plurai (evals@plurai-plugins) is BUILD/CI-only, per the Phase 196 precedent -- it is never a
  runtime dependency. If the hosted eval cannot run (no credentials, no network), degrade to
  hand-labeled rows and mark `evals/plurai/192-baseline.json`'s `method` field
  `"hand-labeled"` + `"deferred": true`. Do not block the phase on Plurai availability.
- R7 Every CSV row is SYNTHETIC (Canon Part 8 applies to eval training data too) -- manufactured
  Larry-turn samples, never real navigator content. No real names/emails/room metrics.
- R8 Exclusive file ownership: this plan owns `lib/statusline/cockpit-signals.cjs`,
  `lib/statusline/cockpit-renderer.cjs`, its one new test file, and the two new
  `evals/plurai/*` files plus the README.md row it adds. No other plan in this phase touches these
  paths.
- R9 Frozen scalars unchanged (MAX_K=3, DIAL_REACH_K=6, 0.70/0.15); the 5-color De Stijl palette is
  unchanged (this plan APPLIES `forcedVoiceColorForStance`'s red/blue mapping, it does not add a
  color); the statusline's existing voice-switch chip (Phase 187.1) is untouched.
</rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: statusline [stance] chip + forced voice-color wiring</name>
  <files>lib/statusline/cockpit-signals.cjs, lib/statusline/cockpit-renderer.cjs, tests/test-192-statusline-stance-chip.cjs</files>
  <read_first>
    - lib/statusline/cockpit-signals.cjs `readVoiceSwitchState()` + `collectSignals()` (the exact side-channel-read-then-fold-into-state pattern to mirror for `readStanceState()`)
    - lib/statusline/cockpit-renderer.cjs `_render()` (the `segs.push(...)` optional-segment pattern used for `brainOn`/`risk`/`bar`; `resolveVoiceGlyph(s)` -- the function whose INPUT this task overrides when a forced color is active)
    - lib/core/stance-state.cjs (from 192-03) -- `readStance()`, `forcedVoiceColorForStance()`
    - docs/STATUSLINE-CONTRACT.md (the passive-render-surface constraint; the "never a 4th chip on the WHO sub-segment" precedent from the voice-switch feature)
  </read_first>
  <behavior>
    - `readStanceState()` (new, in cockpit-signals.cjs) -> calls `stanceState.readStance()`;
      returns `{ stance: <one of STANCES or null>, forced_voice_color: <'red'|'blue'|null> }`;
      never throws (wraps in try/catch mirroring `readVoiceSwitchState`).
    - `collectSignals()` folds `readStanceState()`'s result into the returned signal object as
      `stance` and `stance_forced_color` (or omits both when `readStanceState()` throws/degrades,
      matching the existing degrade-silently convention).
    - `_render(s)`: when `s.stance` is a non-null value, (a) push a new segment `'[' + s.stance +
      ']'` into the existing healthy/caution segment array (positioned immediately after the
      identity segment, before the room/health segment), and (b) when `s.stance_forced_color` is
      non-null, pass it into `resolveVoiceGlyph`'s effective input so the rendered voice glyph
      matches (red for redteam, blue for tell-act) rather than whatever the natural detection would
      have returned.
    - When `s.stance` is `null`/absent (default): `_render()`'s output is byte-identical to the
      pre-plan function (no new segment, no color override).
  </behavior>
  <action>
    Add `readStanceState()` to `lib/statusline/cockpit-signals.cjs`, requiring
    `../core/stance-state.cjs` the same defensive way `voiceMark` is required at the top of the
    file (wrapped in try/catch so a missing module degrades gracefully rather than crashing the
    statusline). Fold its result into `collectSignals()`'s returned object as described in
    this task's Behavior block above. In `lib/statusline/cockpit-renderer.cjs`, extend `_render(s)` with the new
    conditional `[stance]` segment and the forced-color override into `resolveVoiceGlyph`'s call
    site, following the exact additive `segs.push(...)` idiom already used for `brainOn`/`risk`.
    Do not touch the `ctx_cliff` or `doctor_fix` hero branches (this is additive to the
    healthy/caution "Next move" hero only, mirroring where `brainOn` already lives). Update both
    files' header comments to document the new signal source
    (`~/.mindrian/stance-state.json` via `lib/core/stance-state.cjs`, read-only) and cite this as
    the CLI-enhancement half of SEED-042 (the footer-affordance baseline is 192-03).
  </action>
  <acceptance_criteria>
    <automated>node tests/test-192-statusline-stance-chip.cjs; echo "EXIT:$?"</automated>
    Passes when: the suite proves (a) with `readStance()` mocked/overridden to `null`,
    `renderCockpit()`'s output is byte-identical to a captured pre-plan baseline string for the same
    input state, (b) with a mocked `'redteam'` stance, the rendered line contains `[redteam]` AND
    the forced-red voice glyph (never the naturally-detected color), (c) with a mocked `'tell-act'`
    stance, the rendered line contains `[tell-act]` AND the forced-blue voice glyph, (d) with a
    mocked `'research'` or `'ask'` stance, the rendered line contains the chip but does NOT force a
    color (natural detection, or none, still governs), and (e) `readStanceState()` never throws even
    when `lib/core/stance-state.cjs` is temporarily unrequireable (simulated failure degrades to
    `{stance:null, forced_voice_color:null}`).
  </acceptance_criteria>
  <done>Statusline shows a passive [stance] chip only when an override is active, forces the locked red/blue voice-glyph mapping in the same render pass, and is byte-stable by default.</done>
</task>

<task type="auto">
  <name>Task 2: Plurai eval GATE leg -- posture-framing fidelity</name>
  <files>evals/plurai/09-posture-framing-fidelity.csv, evals/plurai/192-baseline.json, evals/plurai/README.md</files>
  <read_first>
    - evals/plurai/README.md (the CSV schema: Sample JSON-encoded / Label / Reasoning; the judge-model standing choice "fable"; the Part 8 synthetic-only rule)
    - .planning/phases/196-part8-runtime-slm-guardrail/196-02-plurai-baseline-PLAN.md (the exact Build/CI offline-eval-then-degrade workflow + the `evals/plurai/196-baseline.json` schema to mirror for `192-baseline.json`)
    - .planning/seeds/SEED-042-always-on-act-redteam-toggle.md (the 4 stance framings: research=evidence-pulling hedged; tell-act=decisive delivery; ask=Socratic single-question; redteam=devil's-advocate challenge)
  </read_first>
  <action>
    Create `evals/plurai/09-posture-framing-fidelity.csv` with >= 24 SYNTHETIC rows (>= 6 per
    stance), schema `Sample,Label,Reasoning` where `Sample` is a JSON-encoded
    `{"declared_stance": "<one of research|tell-act|ask|redteam>", "larry_turn": "<synthetic
    manufactured Larry response text>"}` and `Label` is `compliant` (the turn's framing matches its
    declared stance) or `violation` (it does not -- e.g. a `redteam`-declared turn that reads as
    plain agreement, or a `research`-declared turn that delivers a bare verdict with no evidence
    hedge). Manufacture near-miss rows (a redteam turn that is ONLY mildly skeptical; a tell-act
    turn that sneaks in a Socratic question) so the fidelity boundary is stressed, not just the
    obvious cases. Every `Reasoning` cell names which framing tell fired or was missing. No real
    names/content (Canon Part 8 applies to eval data). Add one README.md table row (mirroring the
    existing 8-row table format) documenting this CSV's Plurai use case ("Agent Response / Policy
    Compliance"), the canon contract (Part 12 + SEED-042), and the exact judge prompt to paste into
    Plurai: "Classify whether the Larry turn's tone and content match its declared stance
    (research = evidence-pulling and hedged; tell-act = decisive delivery; ask = a single Socratic
    question; redteam = devil's-advocate challenge) or violation (the turn ignores its declared
    stance)." Attempt the OFFLINE Plurai eval run per the 196-02 workflow (uv-invoked, Build/CI
    only, never runtime); on success persist `evals/plurai/192-baseline.json` with the same
    precision/recall + `method: "plurai-eval"` shape as `196-baseline.json`; on any unavailability
    (no credentials, no network, hosted service down), degrade to hand-labeling the CSV rows and
    persist `192-baseline.json` with `method: "hand-labeled"` and `deferred: true` -- do NOT block
    this plan or the phase on Plurai availability.
  </action>
  <acceptance_criteria>
    <automated>test $(tail -n +2 evals/plurai/09-posture-framing-fidelity.csv | grep -c .) -ge 24 && grep -qi "compliant" evals/plurai/09-posture-framing-fidelity.csv && grep -qi "violation" evals/plurai/09-posture-framing-fidelity.csv && grep -q "method" evals/plurai/192-baseline.json && echo OK</automated>
    Passes when: >= 24 synthetic data rows exist across both labels, every one of the 4 stance
    names appears in at least one Sample, and `192-baseline.json` records a `method` field (either
    `plurai-eval` with precision/recall, or `hand-labeled` with `deferred: true`) -- the phase-level
    Plurai GATE is satisfied either way per the Phase 196 precedent.
  </acceptance_criteria>
  <done>A Plurai eval CSV + baseline JSON ships judging posture-framing fidelity across all 4 stances, gating this phase's closure per the established Phase 196 pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| stance-state.json -> statusline render | LOCAL state crosses into a passively-rendered, always-visible display surface |
| synthetic eval CSV -> Plurai hosted service (Build/CI only) | manufactured data crosses to a third-party service, never on the runtime path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-192-04-01 | Information Disclosure | evals/plurai/09-posture-framing-fidelity.csv | mitigate | Every row is SYNTHETIC/manufactured per Canon Part 8; no real navigator content, names, or room metrics ever enter the CSV |
| T-192-04-02 | Tampering | statusline stance chip sourced from a hand-editable JSON file | accept | Same accepted risk as the existing voice-switch chip (Phase 187.1); worst case is a cosmetic misdisplay, not a functional compromise -- `readStance()`'s validation (192-03) already rejects unrecognized values |
| T-192-04-03 | Denial of Service | statusline render loop (~300ms redraw) | accept | The new chip is one string concatenation and one JSON read guarded by try/catch; no new blocking I/O added to the hot path |
</threat_model>

<verification>
- `node tests/test-192-statusline-stance-chip.cjs` exits 0.
- Manual diff: `renderCockpit()` output for a `null`-stance input state matches the pre-plan captured baseline byte-for-byte.
- `test $(tail -n +2 evals/plurai/09-posture-framing-fidelity.csv | grep -c .) -ge 24` (>= 24 synthetic rows).
- `evals/plurai/192-baseline.json` parses as valid JSON and carries a `method` field.
</verification>

<success_criteria>
- Statusline shows the stance chip + forced voice-color only when an override is active; default path byte-stable.
- Plurai eval GATE leg ships (CSV + baseline JSON), never blocking on hosted-service availability.
- Zero frozen scalar or existing chip (voice-switch, WHO sub-segment) modified.
</success_criteria>

<output>
Create `.planning/phases/192-shape-f-hitl-selector-completion/192-04-SUMMARY.md` when done
</output>
