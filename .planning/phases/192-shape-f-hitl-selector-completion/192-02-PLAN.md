---
phase: 192-shape-f-hitl-selector-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/hmi/dial-presenter.cjs
  - tests/test-f7max-preview-confidence-bar.cjs
  - tests/test-f7max-modifier-pane.cjs
autonomous: true
requirements: [SFC-05, SFC-06, SFC-07]

must_haves:
  truths:
    - "The dial's triangle/dial text and its AskUserQuestion card contract are proven to still emit atomically (never text-only) via the existing Phase 150.5 ACPT-06 suite -- no regression"
    - "Each ranked reach row in the dial carries a De Stijl preview panel (Mondrian box-drawing + confidence bar + JTBD one-liner + evidence count when known) instead of a bare NN% column"
    - "The confidence column renders as a bar-glyph composite (e.g. blocks-then-percentage), never a bare NN%, on every render state (S1-S5)"
    - "The tier-0 cold room (S4) still renders the honest '--' / start-anywhere card -- unchanged behavior, just verified"
    - "A second AskUserQuestion question (multiSelect:true) rides the SAME card as modifiers on the chosen reach (consult Brain first / file outcome as decision / open evidence trail), decorating -- never multiplying -- the single-pick reach choice"
  artifacts:
    - path: "lib/hmi/dial-presenter.cjs"
      provides: "renderDial() gains preview composition, confidence-bar glyphs, and a modifier-question contract, additive over the existing 5 render states"
      exports: ["renderDial"]
    - path: "tests/test-f7max-preview-confidence-bar.cjs"
      provides: "unit coverage for the De Stijl preview panel + confidence-bar glyph composition across S1-S5"
      min_lines: 40
    - path: "tests/test-f7max-modifier-pane.cjs"
      provides: "unit coverage for the Q2 multiSelect modifier contract riding the same card"
      min_lines: 30
  key_links:
    - from: "lib/hmi/dial-presenter.cjs"
      to: "lib/hmi/shape-f1-renderer.cjs"
      via: "renderShapeF1 still owns the base F.1 chooser rows; dial-presenter composes ON TOP, never forks a parallel renderer"
      pattern: "renderShapeF1"
    - from: "tests/test-f7max-preview-confidence-bar.cjs"
      to: "tests/test-acpt-06-dial-atomic-emission.cjs"
      via: "the Finding-1 verify task runs this pre-existing suite as its acceptance gate"
      pattern: "ACPT-06"
---

<objective>
SEED-021 F.7-max (Phase 192 part b). Finding 1: verify the atomic render-coupling acceptance
criterion (Phase 144 / Phase 150.5) still holds -- it is expected GREEN already, this is a
verify-or-fix pass, not a rebuild. Finding 2: add the additive F.7-max UX riding the SAME dial
card -- De Stijl preview panels per reach, confidence-bar glyphs replacing bare NN%, and the Q2
multiSelect modifier pane -- while confirming the tier-0 honest cold card (already shipped) is
unchanged.

Purpose: F.7 uses only a fraction of what the AskUserQuestion host primitive can do (previews,
multiSelect, up to 4 questions). This plan exploits the unused keyboard surface without inventing
a bespoke widget or a second renderer.
Output: dial-presenter.cjs additive extension + two new unit suites; a documented Finding-1 verify
result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-shape-f-hitl-selector-completion/192-CONTEXT.md
@.planning/seeds/SEED-021-f7-max-keyboard-dial-atomic-render-coupling.md
@CLAUDE.md
@lib/hmi/dial-presenter.cjs
@lib/hmi/shape-f1-renderer.cjs
@lib/hmi/dial-reach-orchestrator.cjs
@lib/hmi/dial-label-composer.cjs
@lib/hmi/reach-component-map.json
@tests/test-acpt-06-dial-atomic-emission.cjs
@tests/test-dial-render-states.cjs
</context>

## Artifacts this phase produces (this plan's share)

- Additive extension of `lib/hmi/dial-presenter.cjs`: preview-panel composition, confidence-bar
  glyph formatting, and a Q2 modifier-question contract field, all format-layer-only (no change to
  `buildReachList`'s resolve half, no change to the frozen 0.70/0.15/MAX_K=3/DIAL_REACH_K=6 gate).
- New `tests/test-f7max-preview-confidence-bar.cjs`.
- New `tests/test-f7max-modifier-pane.cjs`.
- A documented Finding-1 verify result (recorded in the SUMMARY, backed by the pre-existing
  `tests/test-acpt-06-dial-atomic-emission.cjs`).

<rules>
## RULES (Phase 192 harness -- restate every plan)
- R1 CJS only, no TypeScript. No em-dashes (hyphens only).
- R2 Frozen Part 3 contracts UNCHANGED: MAX_K=3 (hard 3 chooser rows), DIAL_REACH_K=6 (the preview-rank bank), the 0.70/0.15 gate, the 5 render states (S1-S5), the single-marker glyph rule (never a second '▶' outside S2's near-tie exception). This plan is format-layer-additive ONLY.
- R3 Reuse, never fork: `renderDial` still calls `renderShapeF1` for the base chooser body. Preview/confidence-bar/modifier additions decorate the RETURNED zones/contract object; they do not introduce a second rendering path.
- R4 Previews XOR checkboxes PER QUESTION, but BOTH ride ONE card across TWO questions (Q1 single-pick + preview, Q2 multiSelect modifiers) -- per the SEED-021 spec. Never propose a THIRD question; never make Q1 multiSelect.
- R5 Part 8: dial-presenter.cjs makes ZERO Brain calls today and must make ZERO after this plan. Preview content (JTBD one-liner, evidence count) draws ONLY from data already resolved into the ReachList/label by upstream callers (composeLabel, reach.score, reach.evidence_count if present) -- never a new fetch. Degrade honestly (omit the evidence-count line) when the field is absent; never fabricate a number.
- R6 Exclusive file ownership: this plan owns `lib/hmi/dial-presenter.cjs` and its two new test files. No other Wave-1 plan (192-01, 192-03) touches these paths.
- R7 Do not raise MAX_TOGGLE_N-style bounds; the Q2 modifier set is a small fixed list (3 items per the SEED-021 spec: consult Brain first / file outcome as decision / open evidence trail) -- do not add a 4th without a spec update.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: Verify Finding 1 (atomic render coupling) -- no code change expected</name>
  <files>none (verification only; no files_modified for this task)</files>
  <read_first>
    - .planning/seeds/SEED-021-f7-max-keyboard-dial-atomic-render-coupling.md "Finding 1" and "Phase 144 acceptance criterion"
    - .planning/ROADMAP.md Phase 150.5 entry (DIAL-ATOM-01/02: the contract-threading fix + the ACPT-06 harness leg, already shipped)
    - tests/test-acpt-06-dial-atomic-emission.cjs (the 5-assertion atomic-emission + honest-negative + Part-8 leak suite)
    - scripts/intent-classifier.cjs renderEngineDecisionWithDial + the appendAskUserQuestionTrailer call site (confirms the contract is threaded through, not discarded)
  </read_first>
  <action>
    Run `node tests/test-acpt-06-dial-atomic-emission.cjs` and confirm all 5 assertions pass: (A)
    a real sensor fires from a production-shaped turn; (B) the engine arm emits dial text AND the
    AskUserQuestion card contract together, never text-only; (C) a cold room with no side-channel
    emits NEITHER (honest negative, legacy arm); (D) no Part-8 leak of the synthetic sentinel into
    the emitted block or decision_trace; (E) the sensor-fire guard proves the real sensor path, not
    the test stub. If ALL FIVE pass (expected, since Phase 150.5 shipped this), do NOT modify any
    production file -- record the verified-green result plainly in the plan's SUMMARY, citing the
    exact command and its exit code. If ANY assertion fails (unexpected regression), STOP before
    starting Task 2/3: file the specific failing assertion and its cause in the SUMMARY as a blocking
    finding, and escalate rather than silently patching scope beyond this plan's file ownership --
    a genuine regression here would need its own follow-up plan against scripts/intent-classifier.cjs
    (a file this plan does not own).
  </action>
  <acceptance_criteria>
    <automated>node tests/test-acpt-06-dial-atomic-emission.cjs; echo "EXIT:$?"</automated>
    Passes when: exit code 0 and the printed summary reads "5 passed, 0 failed".
  </acceptance_criteria>
  <done>Finding 1 acceptance criterion (Phase 144, hardened by Phase 150.5) reconfirmed green with zero production-file changes; result recorded in SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: De Stijl preview panel + confidence-bar glyphs (Finding 2, items 1 + 3 + 4-verify)</name>
  <files>lib/hmi/dial-presenter.cjs, tests/test-f7max-preview-confidence-bar.cjs</files>
  <read_first>
    - lib/hmi/dial-presenter.cjs full file (the 5 render states S1-S5, the MARKER_RECOMMENDED/MARKER_ROW glyphs, the existing bare "NN%" confidence-column composition, the tier_0 FRAMING_TIER_0 cold-card path)
    - lib/hmi/dial-label-composer.cjs (composeLabel -- the JTBD one-liner source; elevationDefault -- Phase 205's what-you-get framing, reusable for the preview body)
    - .planning/seeds/SEED-021-f7-max-keyboard-dial-atomic-render-coupling.md "F.7-max spec" items 1, 3, 4
    - tests/test-dial-render-states.cjs (the existing S1-S5 fixture shapes to extend, not replace)
  </read_first>
  <behavior>
    - Confidence-bar composer: given a score in [0,1] (or null for cold-room/offline), returns a
      fixed-width block-glyph bar (e.g. filled blocks proportional to the score, using the approved
      glyph vocabulary -- no new Unicode outside what CLAUDE.md/skills/ui-system/SKILL.md already
      approve) followed by the existing "NN%" text, and returns "--" (no bar) when score is null.
      Byte-stable width across all rows so the column still aligns.
    - Preview-panel composer: given a reach (reach_id, score, canonical_verb) and its composed JTBD
      label (from composeLabel), returns a short monospace panel string: the JTBD one-liner line,
      the confidence-bar line, and an evidence-count line ONLY when `reach.evidence_count` is a
      number (omit the line entirely when absent -- never print "evidence: unknown" or a fabricated
      count).
    - Tier-0 cold room (S4): the preview composer receives a null score and STILL returns a
      preview panel (the "start anywhere" framing line + the "--" bar), proving the card renders
      per-reach previews even when nothing is ranked (composes with the existing atomic-coupling
      rule: a cold turn still fires a card, just an honest one).
    - Byte-stability: when a caller does not opt into previews (no `opts.withPreview`), `renderDial`'s
      return shape is UNCHANGED from today (existing callers/tests must not need any edit).
  </behavior>
  <action>
    Add two new pure functions to `lib/hmi/dial-presenter.cjs`: a confidence-bar composer and a
    preview-panel composer, matching the behavior above exactly. Wire them behind a new optional
    `opts.withPreview` flag on `renderDial` (or the equivalent entry point already used by the
    intent-classifier call site) so the byte-stable default path is preserved and the new preview
    surfaces only when explicitly requested. When enabled, each of the (up to 3) chooser rows gains
    a `preview` field alongside its existing label + confidence text, matching the AskUserQuestion
    host's per-option `preview` field shape (single-select-only, per the SEED-021 host constraint
    inventory -- do not attempt to add a preview to a multiSelect question). Export the two new pure
    functions for direct unit testing (do not bury them as un-exported closures). Update the header
    comment block to document the new opt-in surface, citing SEED-021 Finding 2 items 1/3/4, and note
    that MAX_K=3 / DIAL_REACH_K=6 / the 0.70/0.15 gate remain untouched (format-layer-only change).
  </action>
  <acceptance_criteria>
    <automated>node tests/test-f7max-preview-confidence-bar.cjs && node tests/test-dial-render-states.cjs; echo "EXIT:$?"</automated>
    Passes when: the new suite proves (a) the confidence-bar composer never prints a bare "NN%"
    without a bar prefix when a score exists, (b) it prints "--" with no bar on null score, (c) the
    preview composer omits the evidence-count line when the field is absent (never fabricates a
    number), (d) tier-0 (S4) still returns a preview panel with the honest cold framing, and (e) the
    pre-existing `test-dial-render-states.cjs` suite is UNCHANGED and still green (byte-stable default
    path proof).
  </acceptance_criteria>
  <done>dial-presenter.cjs exports a confidence-bar composer and a preview-panel composer wired behind an opt-in flag; default render path byte-stable; tier-0 honest cold card re-verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Q2 multiSelect modifier pane (Finding 2, item 2)</name>
  <files>lib/hmi/dial-presenter.cjs, tests/test-f7max-modifier-pane.cjs</files>
  <read_first>
    - .planning/seeds/SEED-021-f7-max-keyboard-dial-atomic-render-coupling.md "F.7-max spec" item 2 + "Constraints honored" (Q2 toggles land as PROPERTIES on the same decision event, never new edge types -- Canon Part 4 vocabulary frozen)
    - lib/hmi/reach-component-map.json (the archetype vocabulary; `multiSelect` already means "AskUserQuestion multiSelect:true")
    - lib/hmi/selector-dispatcher.cjs archetypeToContractHints (`hints.multiSelect = true` for the `multiSelect`/`group` archetypes) -- confirms the host-hint pattern to mirror, without routing through F.8's fan-out-edges machinery (this is NOT F.8; F.8 is the independent-basket-with-N-typed-edges canonical shape and is out of scope here)
  </read_first>
  <behavior>
    - Modifier-set composer: returns a fixed 3-item list (consult Brain first / file outcome as a
      decision / open the evidence trail), each with a short label + one-line description, as a
      SECOND AskUserQuestion question object (`multiSelect: true`) riding the SAME card as Q1.
    - The modifier question is ONLY appended when `opts.withModifiers` is true; default path
      (existing callers) is byte-unchanged.
    - Q1 (the reach pick) stays single-select regardless of `opts.withModifiers` -- one reach per
      beat is preserved; modifiers decorate the CHOSEN reach, they never multiply it into N reaches.
    - The returned `contract` object gains a `modifiers` array field (the 3 items) and
      `modifiersMultiSelect: true` when enabled, but does NOT gain a new edge-type field --
      confirm (via a code comment + a test assertion) that no new typed-edge vocabulary string
      appears anywhere in this module.
  </behavior>
  <action>
    Extend `renderDial` with an `opts.withModifiers` flag that appends the Q2 modifier question
    described above to the returned contract, alongside the existing Q1 reach chooser. Cite the
    exact 3 modifier items from SEED-021 verbatim (do not invent a 4th or rename them). Add a code
    comment at the composition site stating this rides `multiSelect: true` as a native AskUserQuestion
    parameter on Q2 -- it is NOT the F.8 canonical shape (no fan-out to N typed edges via
    navigation.cjs); the toggles are recorded as properties on the SAME SELECTED_REACH decision event
    Q1 already writes. Keep the default (`opts.withModifiers` absent/false) path byte-identical to
    pre-plan behavior.
  </action>
  <acceptance_criteria>
    <automated>node tests/test-f7max-modifier-pane.cjs; echo "EXIT:$?"</automated>
    Passes when: the suite proves (a) `opts.withModifiers` appends exactly the 3 named modifier
    items as a `multiSelect:true` Q2, (b) Q1 remains single-select in the same contract, (c) no new
    typed-edge-vocabulary string is introduced (grep sweep over the diff for edge-name-shaped
    identifiers finds none beyond the frozen Part-4 set), and (d) the default (no-opts) path returns
    a contract with no `modifiers` key, proving byte-stability.
  </acceptance_criteria>
  <done>Q2 multiSelect modifier pane ships behind an opt-in flag, decorating (not multiplying) the single-pick reach choice; default render path unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| ReachList/label input -> preview panel text | upstream-resolved reach data (score, canonical_verb, evidence_count) crosses into the render layer |
| navigator Q2 toggle picks -> decision-event properties | modifier toggle state crosses into the persisted decision event |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-192-02-01 | Tampering | preview-panel evidence-count line | mitigate | Composer omits the line entirely when `reach.evidence_count` is not a number -- never renders a fabricated or coerced value |
| T-192-02-02 | Information Disclosure | preview panel JTBD one-liner | accept | Sourced from composeLabel, which already passes the Part-8 dual-egress audit for any {framework} slot; no new content class introduced |
| T-192-02-03 | Repudiation | Q2 modifier toggle state | accept | Modifiers land as properties on the existing SELECTED_REACH event (already timestamped + typed); no new anonymous-write path |
| T-192-02-04 | Elevation of Privilege | opts.withPreview / opts.withModifiers flags | accept | Both are render-layer, caller-supplied booleans with no side effects beyond text composition; neither flag can trigger a write or a network call |
</threat_model>

<verification>
- `node tests/test-acpt-06-dial-atomic-emission.cjs` exits 0 (Finding 1 reconfirmed).
- `node tests/test-f7max-preview-confidence-bar.cjs` exits 0.
- `node tests/test-f7max-modifier-pane.cjs` exits 0.
- `node tests/test-dial-render-states.cjs` exits 0 (byte-stable default path, no regression).
- Grep sweep: `grep -c "0\.70\|0\.15\|MAX_K\s*=\s*3\|DIAL_REACH_K\s*=\s*6" lib/hmi/dial-presenter.cjs` count unchanged before/after (frozen scalars untouched).
</verification>

<success_criteria>
- Finding 1 (atomic coupling) reverified green with zero code change.
- Finding 2 additive UX (preview panel, confidence-bar glyphs, Q2 modifier pane) ships behind opt-in flags; tier-0 honest cold card reconfirmed.
- Zero frozen Part 3 scalar changed; default (no-opts) render path byte-identical to pre-plan.
</success_criteria>

<output>
Create `.planning/phases/192-shape-f-hitl-selector-completion/192-02-SUMMARY.md` when done
</output>
