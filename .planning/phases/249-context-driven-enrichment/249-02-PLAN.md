---
phase: 249-context-driven-enrichment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/src/arm1-orchestrator.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/arm1-orchestrator.test.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/eval-framework-structure.test.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/beautiful-question.json
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-framework-evals.mjs
  - scripts/check-flagship-floor.cjs
  - tests/test-249-floor-gate.cjs
autonomous: true
execution_model: sonnet
requirements: [ENRICH-02, ENRICH-04]
must_haves:
  truths:
    - "orchestration_readiness responses additionally carry a dimensions vector (pattern_type/structure/techniques/flow, each 0|1), hermetically proven, with every existing response key unchanged (contract v1 pins tool names and arg keys, not response schemas - additive is safe)"
    - "The eval harness turns a deliberately wrong fixture red, proven by an in-suite assertion (eval-gate-can-fail precedent), not by prose"
    - "scripts/check-flagship-floor.cjs exits non-zero against today's live graph, printing the per-framework 24-miss baseline - the honest red starting line for the SWEEP-02 gate"
    - "The floor script enumerates the invoked set from disk frontmatter at run time and prints BOTH denominator candidates (50 kind:methodology commands / 28 frameworks vs the canon-25 prose), naming the navigator ratification as OPEN (blocked by 249-03 Task 2)"
    - "No brain-repo commit from this plan is pushed - deploy rides 249-03's operator checkpoint after 247-03's own push, avoiding cross-executor collision"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/src/arm1-orchestrator.mjs"
      provides: "Additive dimensions field in the T6 orchestration_readiness envelope"
      contains: "dimensions"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/eval-framework-structure.test.mjs"
      provides: "Fixture-driven known-answer checker + mandatory red proof"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/beautiful-question.json"
      provides: "First fixture (graph-regression class, guards existing curated 4/4 content)"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-framework-evals.mjs"
      provides: "Live leg: same fixtures probed against the deployed service, status-honest"
    - path: "scripts/check-flagship-floor.cjs"
      provides: "The machine form of the SWEEP-02 gate: exactly-1-match AND readiness >= 3 per invoked framework, exit non-zero on any miss"
    - path: "tests/test-249-floor-gate.cjs"
      provides: "Hermetic floor-gate logic test + sabotage red proof, zero network"
  key_links:
    - from: "scripts/check-flagship-floor.cjs"
      to: "scripts/build-brain-census.cjs"
      via: "require of exported scanMethodologyCommands + brainCall + BRAIN_URL"
      pattern: "require.*build-brain-census"
    - from: "tests/eval-framework-structure.test.mjs"
      to: "tests/fixtures/framework-evals/"
      via: "fixture glob - every enrichment adds a fixture, not a test file"
      pattern: "framework-evals"
    - from: "scripts/probe-framework-evals.mjs"
      to: "pws-brain-mcp.onrender.com"
      via: "read-tier HTTP probe, key from ~/.mindrian.env"
      pattern: "onrender"
---

<objective>
ENRICH-02 machinery + the ENRICH-04 instrument, everything autonomous-now and local/read-tier: (1) the additive server-side dimensions field for orchestration_readiness - the first brain-repo task, without which ENRICH-01's missing_dimensions is lossy; (2) the known-answer eval harness with a mandatory wrong-answer red proof plus its live probe leg; (3) scripts/check-flagship-floor.cjs, the machine gate that MUST run red today with 24 misses - that red baseline is the honest ENRICH-04 starting line, and its future green run is the SWEEP-02 gate artifact Phase 252 cites.

Purpose: the write path (ingest_framework dry-run diffs) already exists in the brain repo; what is missing is the measurement instrument (dimensions), the proof instrument (evals that CAN fail), and the finish-line instrument (the floor gate). This plan builds all three so 249-03's operator checkpoints have something rigorous to run.
Output: brain-repo local commits (NOT pushed) + plugin floor gate + filed red baseline.

Execution model: sonnet (per config GSD models directive).

Coordination: the 247 executor may still be active in /home/jsagi/dev/ProblemsWorthSolving-Brain (247-03 not landed). Task 1 re-reads repo state before editing and touches ONLY arm1-orchestrator.mjs + its test; Tasks 2-3 create only NEW files. Never touch server.mjs, src/ingest/*, or README there. The concurrent plugin executor is in lib/mcp/* (248-02) - this plan touches no plugin lib/ file at all. 247-02's landed state (tier_denied sentinel, 5 loop wrappers) is the contracted client surface; treat 247-03's deploy state as PENDING, never as current.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/249-context-driven-enrichment/249-RESEARCH.md
@.planning/phases/247-brain-surface-contract/247-02-SUMMARY.md
@docs/BRAIN-GRAPH-CENSUS.generated.md
@scripts/build-brain-census.cjs
@scripts/probe-brain-contract.cjs
@/home/jsagi/dev/ProblemsWorthSolving-Brain/src/arm1-orchestrator.mjs
@/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/eval-gate-can-fail.test.mjs
@/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/helpers/red-proof.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive dimensions field in orchestration_readiness (brain repo, local commit only)</name>
  <files>/home/jsagi/dev/ProblemsWorthSolving-Brain/src/arm1-orchestrator.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/arm1-orchestrator.test.mjs</files>
  <behavior>
    - The T6 response carries dimensions: { pattern_type, structure, techniques, flow }, each exactly 0 or 1
    - dimensions values sum to readiness_score on every fixture (internal consistency proof)
    - Every pre-existing response key (name, readiness_score, orchestration_status) is byte-identical to before - purely additive
  </behavior>
  <action>
    Preflight (mandatory, the 247 executor is in flux there): run git -C /home/jsagi/dev/ProblemsWorthSolving-Brain status --short and log --oneline -5. If arm1-orchestrator.mjs or its test has UNCOMMITTED foreign changes, STOP and report the collision instead of editing over another executor's work. A moved-forward HEAD is fine - re-read the file's current state before editing (this plan's research citations are snapshots).

    Write the failing test extension first (RED), then implement (GREEN). In src/arm1-orchestrator.mjs T6 (orchestration_readiness), the four CASE dimension values (pattern_type known; structure via HAS_PHASE|HAS_STAGE|HAS_PROCESS_STEP|HAS_STEP; techniques via USES_TECHNIQUE|EQUIPS_WITH; flow via LEADS_TO on structure) are already computed to produce the summed score - RETURN them individually as a dimensions object in the Cypher RETURN plus the JS envelope. Keep the Cypher fully parameterized as it is today; interpolate nothing. This is a few additive lines (research finding, verified by direct code reading of the file): contract v1 pins tool names and arg keys, not response schemas, so no contract file changes.

    Extend tests/arm1-orchestrator.test.mjs with the behaviors above, following the file's existing hermetic style. Commit locally in the brain repo with a clear message referencing 249-02. Do NOT push: the push + Render redeploy ride 249-03 Task 3's operator checkpoint, sequenced after 247-03's own push (Pitfall 8, cross-executor coordination).
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/arm1-orchestrator.test.mjs</automated>
  </verify>
  <done>dimensions field present and consistent hermetically; existing keys untouched; committed locally in the brain repo, unpushed; no foreign uncommitted work overwritten.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Known-answer eval harness + red proof + live probe leg (brain repo, new files only)</name>
  <files>/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/eval-framework-structure.test.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/beautiful-question.json, /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-framework-evals.mjs</files>
  <behavior>
    - The checker, given a fixture and a conforming response set, passes; given the SAME fixture and a sabotaged response (one extra structure member + shuffled order), it REPORTS the mismatch - asserted inside the suite, the eval-gate-can-fail shape
    - Fixture checks are content assertions, never bare counts: normalize resolves to exactly 1 canonical; structure cardinality AND member-set equality; order/flow matches the documented sequence OR asserts NO chain exists (shape honesty cuts both ways - a non-linear framework legitimately caps at 3/4, which is why 3 is the floor); readiness floor with expected dimensions vector; negative controls (a fabricated component name is ABSENT, a fabricated framework returns grounded:false)
    - The harness discovers fixtures by glob over tests/fixtures/framework-evals/*.json - every future enrichment adds a fixture, not a test file
    - The live probe leg never returns silent success on a non-OK HTTP status, and marks dimension assertions PENDING-DEPLOY (printed loudly, counted separately, never silently passed) while the live payload lacks the dimensions field
  </behavior>
  <action>
    Write the checker test with the red proof first (RED), then the harness (GREEN). ESM (.mjs) per brain-repo convention.

    tests/eval-framework-structure.test.mjs: a pure checker function (fixture expectations vs a response bundle: normalize result, discover_structure result, orchestration_readiness result, feeds_into_chains result) exercised hermetically with fixture-derived good responses and with sabotaged ones via the tests/helpers/red-proof.mjs sabotage-seam pattern. The founding negative example binds: the old text2cypher suite scored 10/10 with every question a count() - "passing a test that cannot fail is not evidence."

    First fixture beautiful-question.json for "Beautiful Question Framework" (live 4/4, 3 structure rows per the census). Author it from the CURRENT live curated structure via read-tier calls, and record in a _fixture_class field plus a comment-style _note that this is a GRAPH-REGRESSION fixture guarding already-curated content (legitimate: that content was not produced by any 249 projector, so it cannot self-confirm). Enrichment fixtures from 249-03 onward carry _fixture_class source_authored and MUST be written from the named source document by the checkpoint reviewer, never generated from the payload (Pitfall 2) - encode that rule as a harness assertion: a fixture whose _fixture_class is missing fails the suite.

    scripts/probe-framework-evals.mjs: the live leg. Iterates the same fixture glob, probes https://pws-brain-mcp.onrender.com with the read-tier key from ~/.mindrian.env, runs the SAME checker, exits non-zero on any content miss, prints per-fixture evidence. Status-honest per the probe-brain-contract.cjs precedent (surface httpStatus verbatim, never collapse to null). Dimension assertions go PENDING-DEPLOY while absent live (Task 1 is unpushed by design). Run it once read-only and record the output in the SUMMARY - expected: beautiful-question content checks PASS, dimensions PENDING.

    Commit locally in the brain repo; do NOT push (same coordination rule as Task 1).
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/eval-framework-structure.test.mjs</automated>
  </verify>
  <done>Harness + first fixture + demonstrated red proof green hermetically; live leg executed once with honest PENDING-DEPLOY dimensions; fixture-class rule enforced by the suite; committed locally, unpushed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: check-flagship-floor.cjs + hermetic red proof + the filed red baseline (plugin)</name>
  <files>scripts/check-flagship-floor.cjs, tests/test-249-floor-gate.cjs</files>
  <behavior>
    - Enumerates the invoked-framework set from commands/*.md frontmatter AT RUN TIME via build-brain-census.cjs's exported scanMethodologyCommands (a navigator ruling changes data, not code)
    - Per framework requires BOTH: normalize_framework_name returns exactly 1 canonical match AND orchestration_readiness readiness_score >= 3 (a multi-match name makes every readiness probe ambiguous - T6 takes exact-first LIMIT 1, so exactly-1 is load-bearing, Pitfall 7)
    - Prints one evidence line per framework (name, uses, matches, score, verdict) plus a summary, and exits non-zero on ANY miss
    - Header prints BOTH denominator candidates with the ratification status: frontmatter scan (50 kind:methodology commands / 28 frameworks) vs canon prose (25), marked OPEN until the 249-03 Task 2 decision is recorded; if data/flagship-floor-set.json exists (written ONLY by that ratification outcome), the enumeration filters to it and the header cites the ratification instead
    - Hermetic test: gate logic driven by injected probe results; an all-green fixture with ONE sabotaged score or ONE 2-match name turns the gate red (the red proof)
  </behavior>
  <action>
    Write the hermetic test first (RED), then the script (GREEN).

    scripts/check-flagship-floor.cjs requires scanMethodologyCommands, brainCall, and BRAIN_URL from ./build-brain-census.cjs (all exported - verified this session). Structure the gate logic as a pure exported function (probe results in, verdicts + exit code out) so tests/test-249-floor-gate.cjs injects fixtures with ZERO network; the CLI main wires live brainCall over it with the read-tier key from ~/.mindrian.env. This script is the thin assertion over the census machinery the research prescribes - do not mint a second HTTP client or a second frontmatter parser (Part 7).

    The 25-vs-50 denominator (research OQ1) is an OPEN navigator ratification: the script must CARRY both numbers in its header output and never silently pick - the frontmatter enumeration is the operating superset until data/flagship-floor-set.json exists, and creating that file is EXCLUSIVELY the 249-03 Task 2 checkpoint's outcome. Refuse (exit 2 with a distinct message) if the override file exists but is malformed.

    Then the BASELINE RED RUN, live, read-tier: run node scripts/check-flagship-floor.cjs once. REQUIRED result per the census arithmetic: non-zero exit, 4 frameworks passing (Beautiful Question 4/4, Problem Definition Transformation 4/4, Ackoff Pyramid 3/4, Usher's Model 3/4), 24 below floor including PEST (0 matches) and the 5 multi-match names (Scenario Planning 6, Systems Thinking/Four Lenses/Mullins/Red Teaming 2 each) failing the exactly-1 leg. File the full per-framework output in the SUMMARY as the honest ENRICH-04 baseline - today's red IS the deliverable; a green run today would mean the gate cannot fail. If live counts differ from the census (the graph may have moved), report the delta honestly - the gate reads live truth, the census is the citable snapshot.
  </action>
  <verify>
    <automated>node --test tests/test-249-floor-gate.cjs && node scripts/check-flagship-floor.cjs > /tmp/claude-1000/-home-jsagi/615b6850-300c-4aad-a506-a4eafae0db6e/scratchpad/floor-baseline.txt; test $? -ne 0 && grep -c "MISS" /tmp/claude-1000/-home-jsagi/615b6850-300c-4aad-a506-a4eafae0db6e/scratchpad/floor-baseline.txt</automated>
  </verify>
  <done>Gate script + hermetic red proof green; live baseline run exits non-zero with per-framework evidence filed in the SUMMARY; both denominators printed with ratification OPEN; run-all-249.sh (from 249-01) auto-discovers the new test by glob.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin scripts -> deployed Brain (read tier) | live probes with the read key; responses are untrusted input |
| brain repo local commits -> Render deploy | code true in git but stale live until 249-03's checkpoint push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-249-05 | Repudiation | "fixed in git, stale on Render" false done | mitigate | live legs are status-honest; dimensions marked PENDING-DEPLOY loudly; floor gate reads live URL only; deploy deferred to 249-03 checkpoint by design |
| T-249-06 | Tampering | T6 Cypher change | mitigate | keep fully parameterized statements; zero string interpolation added |
| T-249-07 | Spoofing | evals that cannot fail (self-confirming gate) | mitigate | in-suite sabotage red proofs for BOTH the eval checker and the floor gate; fixture-class rule enforced |
| T-249-08 | Information Disclosure | fixtures/probe output | mitigate | methodology content only; keys read from ~/.mindrian.env, never printed or committed |
| T-249-SC | Tampering | package installs | accept | no packages installed this phase (research Package Legitimacy Audit: none) |
</threat_model>

<verification>
Brain repo: node --test tests/arm1-orchestrator.test.mjs and tests/eval-framework-structure.test.mjs green; full brain suite node --test tests/ still green. Plugin: node --test tests/test-249-floor-gate.cjs green; bash tests/run-all-249.sh green; live floor run non-zero with filed evidence.
</verification>

<success_criteria>
The three ENRICH-02/04 instruments exist and are proven able to fail: dimensions vector hermetically consistent, eval harness with demonstrated red proof and honest live leg, floor gate red at the 24-miss baseline with both denominators carried and the ratification held OPEN for 249-03.
</success_criteria>

<output>
Create `.planning/phases/249-context-driven-enrichment/249-02-SUMMARY.md` when done.
</output>
