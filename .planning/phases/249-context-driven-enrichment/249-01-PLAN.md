---
phase: 249-context-driven-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/enrichment-queue.cjs
  - lib/core/brain-client.cjs
  - lib/brain/framework-chain-slice.cjs
  - lib/core/navigation/packet.cjs
  - scripts/enrichment-queue-append.cjs
  - skills/brain-connector/SKILL.md
  - tests/test-249-enrichment-queue.cjs
  - tests/test-249-capture-seam.cjs
  - tests/run-all-249.sh
autonomous: true
execution_model: sonnet
requirements: [ENRICH-01]
must_haves:
  truths:
    - "A resolved orchestrationReadiness of 0-2/4, or a discoverStructure grounded:false, with roomDir known, lands a typed entry in <roomDir>/.mindrian/enrichment-queue.json without throwing into or blocking the caller"
    - "Re-missing the same canonical framework increments hit_count and refreshes last_seen + missing_dimensions instead of duplicating the entry"
    - "The queue file carries ONLY generic handles: the Part-8 forbidden-substring audit test proves no user prose passes the entry validator"
    - "The queue is appendable from a one-line CLI (the brain-connector skill leg and Phase 250's refusal leg both need this)"
    - "The census gap table pre-seeds a queue with source: census_seed provenance, never disguised as a live miss"
    - "No readiness probe was added to decide(), any sensor, or any per-turn hook path (1200ms NAV budget untouched)"
  artifacts:
    - path: "lib/core/enrichment-queue.cjs"
      provides: "Derivation-queue clone: enqueue, readQueue, writeQueueAtomic, captureReadinessMiss, closed-enum entry schema"
      min_lines: 200
    - path: "scripts/enrichment-queue-append.cjs"
      provides: "One-line CLI append plus --from-census seeding mode"
    - path: "tests/test-249-enrichment-queue.cjs"
      provides: "Part-8 audit (Test-12 pattern), idempotency, hit_count, closed enums, self-healing"
    - path: "tests/test-249-capture-seam.cjs"
      provides: "Wrapper capture + drain piggyback behavior, sentinel discipline, never-throws"
    - path: "tests/run-all-249.sh"
      provides: "Glob-discovery phase runner with found-eq-0 guard and em-dash fence"
  key_links:
    - from: "lib/core/brain-client.cjs"
      to: "lib/core/enrichment-queue.cjs"
      via: "lazy require inside orchestrationReadiness/discoverStructure capture path"
      pattern: "enrichment-queue"
    - from: "lib/brain/framework-chain-slice.cjs"
      to: "orchestrationReadiness per-seed probe"
      via: "post-slice async piggyback in the same drain pass"
      pattern: "orchestrationReadiness"
    - from: "lib/core/navigation/packet.cjs"
      to: "lib/brain/framework-chain-slice.cjs"
      via: "roomDir threaded into the fetcher args in _surfaceFrameworkChainHint"
      pattern: "roomDir"
    - from: "scripts/enrichment-queue-append.cjs"
      to: "lib/core/enrichment-queue.cjs"
      via: "require"
      pattern: "require.*enrichment-queue"
---

<objective>
ENRICH-01: a live reach that hits a 0-2/4 framework becomes a typed enrichment-queue entry - generic handles only, Part 8 clean - captured at ONE chokepoint (the 247-02 brain-client loop wrappers, which are LANDED), riding the async derivation drain, NEVER the 1200ms per-turn hot path. The queue is a clone of the shipped, Part-8-audited brain-derivation-queue pattern at <roomDir>/.mindrian/enrichment-queue.json, seeded day one from the census gap table with honest census_seed provenance.

Purpose: the enrichment backlog is built by live usage (navigator direction: context and relevancy, no big-bang graph project). Phase 250's visible refusal auto-queues through this exact API, so it must be callable from CJS and from a one-line Bash invocation.
Output: queue module + capture seams + CLI + skill leg + phase runner, all tested hermetically.

Execution model: sonnet (per config GSD models directive).

Coordination: a concurrent executor is working in lib/mcp/* (248-02). This plan touches lib/core, lib/brain, scripts, skills, tests - zero overlap. Do NOT require or reference lib/mcp state as current; the queue takes roomDir as an explicit argument precisely so no room-resolution ladder is consulted here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/249-context-driven-enrichment/249-RESEARCH.md
@.planning/phases/249-context-driven-enrichment/249-VALIDATION.md
@.planning/phases/247-brain-surface-contract/247-02-SUMMARY.md
@lib/core/brain-derivation-queue.cjs
@lib/core/brain-client.cjs
@lib/brain/framework-chain-slice.cjs
@lib/core/navigation/packet.cjs
@data/dispatch-framework-map.json
@docs/BRAIN-GRAPH-CENSUS.generated.md
@data/brain-census.generated.json
@tests/run-all-246.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clone the derivation queue as lib/core/enrichment-queue.cjs + the append CLI</name>
  <files>lib/core/enrichment-queue.cjs, scripts/enrichment-queue-append.cjs, tests/test-249-enrichment-queue.cjs</files>
  <behavior>
    - enqueue(roomDir, entry) with canonical framework name as the unique key: same framework re-enqueued increments hit_count, refreshes last_seen and missing_dimensions, queue size unchanged; different framework appends
    - Entry allowlist keys exactly: framework, normalized, readiness_score, missing_dimensions, context_class, source, hit_count, first_seen, last_seen, probe_provenance, dimensions_inferred (optional bool); any other key is stripped by the self-healing reader
    - source is the frozen vocabulary live_reach | refusal | census_seed; any other value rejects the entry
    - context_class members validate against CLOSED enums: reach_id against REACH_IDS (lib/core/sensors/sensor-types.cjs), trigger_tier against TRIGGER_TIERS (same file), dispatch against the key set of data/dispatch-framework-map.json, problem_type against the frozen problem-type enum (grep the codebase for where it is frozen; sensor-types.cjs is the first place to look); an invalid member is DROPPED from the entry, never stored
    - Part-8 audit test (Test-12 pattern from tests around brain-derivation-queue): a queue file written by the module contains NO forbidden substrings (seed the audit list from the derivation queue's test: user prose markers, artifact-body markers, person names, meeting fragments) AND every entry matches the allowlist shape
    - Self-healing reader: corrupt JSON, wrong version, or invalid shape resets to empty with a stderr warn, never throws
    - Atomic write: tmp + fsync + rename, random tmp suffix (copy writeQueueAtomic verbatim)
    - SOFT_CAP 500 warning / HARD_CAP 1000 rejection, same semantics as the donor module
    - enqueue and captureReadinessMiss NEVER throw into the caller; every failure returns a result object
    - CLI: append mode writes one entry from flags; --from-census mode reads the gap table and enqueues every below-3/4 framework with source census_seed and hit_count 0
  </behavior>
  <action>
    Write the failing tests first (RED commit), then implement (GREEN commit).

    Clone lib/core/brain-derivation-queue.cjs mechanics verbatim per Canon Part 7 (this is the module's own lineage: it was itself cloned from minto-debouncer.cjs). QUEUE_RELATIVE is '.mindrian/enrichment-queue.json', SCHEMA_VERSION 1. Keep the donor's structure: queuePath, emptyQueue, nowIso (seconds precision), readQueue (self-healing, filters entries to the allowlist), writeQueueAtomic, SOFT_CAP/HARD_CAP.

    Replace the entry schema with the ENRICH-01 typed entry (research section "The typed queue entry"): framework (canonical name string, 1-120 chars), normalized (bool - false when normalize_framework_name returned 0 matches and the raw handle is stored, the PEST shape), readiness_score (integer 0-4 or null), missing_dimensions (array drawn only from the frozen set pattern_type|structure|techniques|flow), context_class (object of closed-enum members as behavior above), source (frozen: live_reach|refusal|census_seed - refusal is RESERVED for Phase 250, validate it now so 250 wires without touching this module), hit_count (int >= 0), first_seen/last_seen (ISO), probe_provenance (string like 'orchestration_readiness@<iso>'), dimensions_inferred (optional bool for the lossy client-side fallback when the server dimensions field is not yet deployed).

    Export: enqueue(roomDir, entry), readQueue, writeQueueAtomic, captureReadinessMiss(roomDir, frameworkName, probeResult, contextClass) - the convenience used by the capture seams: builds the entry from a resolved orchestration_readiness or discover_structure payload, deriving missing_dimensions from probeResult.dimensions when present (the 249-02 server field), else inferring what it can and setting dimensions_inferred true - plus the frozen constants (ALLOWED_ENTRY_KEYS, ALLOWED_SOURCES, ALLOWED_DIMENSIONS, caps, SCHEMA_VERSION) and a _test surface mirroring the donor.

    scripts/enrichment-queue-append.cjs: process.argv switch-case router per repo convention (no Commander). Append mode: --room <dir> --framework "<name>" --score N --missing structure,flow --source live_reach [--reach-id X --dispatch Y --problem-type Z --trigger-tier W]. Seed mode: --from-census <path to data/brain-census.generated.json> --room <dir>, enqueueing each gap-table framework below 3/4 with source census_seed, hit_count 0, probe_provenance carrying the census probe timestamps. Exit 0 on success with a one-line JSON result; exit 1 with the error result otherwise. Content-free: only names, enums, scores, timestamps ever reach the file (Part 8).

    Tests in tests/test-249-enrichment-queue.cjs (node --test, temp dirs via fs.mkdtempSync): every behavior above, plus a CLI subprocess test for both modes, plus the forbidden-substring audit over a file produced by a full enqueue-heavy run.
  </action>
  <verify>
    <automated>node --test tests/test-249-enrichment-queue.cjs</automated>
  </verify>
  <done>Module + CLI exist; idempotency, hit_count, closed enums, caps, self-healing, Part-8 audit, and both CLI modes proven by the suite; TDD RED and GREEN commits in history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture seams - wrapper chokepoint + derivation-drain piggyback (never the hot path)</name>
  <files>lib/core/brain-client.cjs, lib/brain/framework-chain-slice.cjs, lib/core/navigation/packet.cjs, tests/test-249-capture-seam.cjs</files>
  <behavior>
    - orchestrationReadiness(name, opts) with opts.roomDir set: a REAL payload with readiness_score <= 2 triggers a best-effort captureReadinessMiss; readiness_score >= 3 does not; a tier_denied or invalid_key sentinel does not; a null transport failure does not (failure visibility is Phase 250's refusal territory, not an enrichment miss)
    - discoverStructure(name, opts) with opts.roomDir set: grounded === false triggers capture; grounded true does not
    - No opts / no roomDir: zero capture, zero throw, wrapper return value byte-identical to today (backward compatible - the wrappers currently have zero production callers, keep it that way for existing signatures)
    - An enqueue failure (unwritable dir) never surfaces to the wrapper caller
    - fetchFrameworkChainSlice with opts.roomDir: after a successful slice fetch, each sanitized seed gets an orchestrationReadiness probe in the same async drain pass; a probe failure never degrades the slice result
    - _surfaceFrameworkChainHint passes roomDir into the fetcher args, sourced from buildBrainPacket's existing options.roomDir; when options.roomDir is null the fetcher receives roomDir null and captures nothing
    - Each successful capture logs a scalar memory_event (event type + framework handle + score only), mirroring the existing brain_packet_rejected precedent in brain-client.cjs
    - Hot-path fence: no new require of brain-client or enrichment-queue appears in lib/core/sensors/ or in navigation-engine's decide() path
  </behavior>
  <action>
    Write the failing tests first (RED), then implement (GREEN).

    In lib/core/brain-client.cjs, extend the two 247-02 wrappers orchestrationReadiness and discoverStructure with an OPTIONAL second param opts = { roomDir, contextClass }. These wrappers are thin callTool passthroughs by 247-02 design ("zero result reshaping so sentinels propagate unchanged") - PRESERVE that: capture happens after resolution in a try/catch side branch, and the returned value is passed through untouched in every case. Sentinel discipline: an object carrying .error (tier_denied, invalid_key) or a null is NEVER a capture trigger. Lazy-require lib/core/enrichment-queue.cjs inside the capture branch (mirrors the drain's lazy-require idiom) and call captureReadinessMiss best-effort. Log the scalar memory_event by mirroring the existing brain_packet_rejected memory-event precedent already inside brain-client.cjs (grep for it; reuse its exact mechanism, do not add a new require path to navigation.cjs if the precedent does not).

    In lib/brain/framework-chain-slice.cjs, add optional o.roomDir to fetchFrameworkChainSlice. After a successful askOp slice (past Gate 4), when o.roomDir is a non-empty string AND typeof brainClient.orchestrationReadiness === 'function', probe each sanitized seed sequentially with await brainClient.orchestrationReadiness(seed, { roomDir: o.roomDir, contextClass: { reach_id: 'brain_consult' } }), the whole block wrapped so no probe failure ever alters the slice envelope. This is the literal "live reach that hits an unready framework" - already async by construction (Phase 245 constraint honored for free).

    roomDir threading - the PINNED call site (verified by direct code reading 2026-08-10, adjust line anchors only if the file has moved): lib/core/navigation/packet.cjs, function _surfaceFrameworkChainHint(db, roomState, opts), the fetcher invocation at line ~286 reading "slice = await fetcher({ active_frameworks: activeNames, max_hops: depth });" - add roomDir to that args object. The value comes from buildBrainPacket (line ~296), which ALREADY computes "const roomDir = options.roomDir || null;" at line ~300: forward it through the opts argument _surfaceFrameworkChainHint already receives from buildBrainPacket (if the current call passes a narrower object, widen it to carry roomDir; keep the _mocks seam intact). No other file needs threading: the production drain path (scripts/brain-derivation-drain.cjs --room -> deriveSection -> buildBrainPacket options.roomDir) already supplies it, so the drain script itself is NOT modified by this plan. Do NOT add any probe to decide(), sensors, or hook paths (Pitfall 6; the Part-8 sweep grep fence must stay green).

    Tests in tests/test-249-capture-seam.cjs: use the loopback mock-server pattern from tests/test-247-brain-client-403.cjs to drive the wrappers with real HTTP shapes (readiness 1/4 payload, readiness 4/4 payload, 403 sentinel, transport failure) against a temp roomDir, asserting queue file contents after each; a stub brainClient object for the chain-slice piggyback (seed probes fired per seed, slice envelope unchanged on probe failure); a _surfaceFrameworkChainHint test via the existing opts._mocks.fetchFrameworkChainSlice seam asserting the mock fetcher receives roomDir when buildBrainPacket options carry it and null when they do not; a grep-based fence test asserting lib/core/sensors/*.cjs and lib/core/navigation-engine.cjs contain no enrichment-queue or new brain-client require.
  </action>
  <verify>
    <automated>node --test tests/test-249-capture-seam.cjs && node --test tests/test-249-enrichment-queue.cjs</automated>
  </verify>
  <done>Both wrappers capture on genuine misses only, with roomDir explicit, sentinels and transport failures excluded, callers never blocked or thrown into; chain-slice piggyback probes seeds in the drain pass with roomDir threaded from the pinned packet.cjs seam; hot-path fence test green; TDD commits in history.</done>
</task>

<task type="auto">
  <name>Task 3: Census seed, brain-connector skill leg, phase runner</name>
  <files>skills/brain-connector/SKILL.md, tests/run-all-249.sh</files>
  <action>
    Seed proof: run node scripts/enrichment-queue-append.cjs --from-census data/brain-census.generated.json --room <mkdtemp dir> and assert the queue holds one census_seed entry per gap-table framework below 3/4 (24 rows in the 2026-08-10 census; derive the expected count from the JSON at run time, never hardcode 24). This is the day-one operator backlog with honest provenance (research OQ3 recommendation): live_reach entries outrank seeds at equal usage downstream; a seed never pretends to be a live miss.

    Extend skills/brain-connector/SKILL.md (Canon Part 7: extend brain-connector, never a fourth brain skill) with the Larry-direct leg for Desktop/Cowork where Larry reaches the Brain via MCP tools directly, bypassing brain-client: when a methodology consult returns orchestration_readiness 0-2/4 or empty structure, run the one-line append CLI with the canonical name, score, missing dimensions, and source live_reach. Keep the instruction content-free (names, enums, scores only cross - Part 8). This extends an existing wired skill's instructions; it mints NO new invocable surface, so confirm no regression with the advisory shape lint and the born-wired gate rather than adding declarations.

    Create tests/run-all-249.sh mirroring tests/run-all-246.sh exactly: glob discovery of tests/test-249-* (.cjs and .sh), the found-eq-0 guard verbatim (a harness that discovers nothing FAILS), and the no-em-dash fence sweeping every file this phase touches via the U+2014 codepoint escape, skipping not-yet-existing paths.
  </action>
  <verify>
    <automated>bash tests/run-all-249.sh && node scripts/check-shape-declaration.cjs; node scripts/build-connector-registry.cjs --check</automated>
  </verify>
  <done>Seeding a temp room from the census produces the full gap-table backlog with census_seed provenance; SKILL.md carries the Larry-direct append instruction; run-all-249.sh discovers and passes both phase suites; shape lint and born-wired gate show no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room filesystem -> queue file | local bookkeeping write; future callers could try to smuggle prose |
| queue -> wire (later phases) | only canonical names/enums may ever leave via ENRICH-02 reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-249-01 | Information Disclosure | enrichment-queue.cjs entries (Part 8) | mitigate | closed-enum schema + allowlist reader + forbidden-substring audit test (Test-12 pattern) |
| T-249-02 | Tampering | queue file on disk | mitigate | self-healing reader filters to allowlist; atomic tmp+fsync+rename writes |
| T-249-03 | Denial of Service | unbounded queue growth | mitigate | SOFT_CAP 500 warn / HARD_CAP 1000 reject, cloned from donor |
| T-249-04 | Denial of Service | readiness probe in per-turn path | mitigate | capture only at async seams; grep fence test over sensors/ and decide() |
| T-249-SC | Tampering | package installs | accept | no packages installed this phase (research Package Legitimacy Audit: none) |
</threat_model>

<verification>
bash tests/run-all-249.sh green (both suites + em-dash fence + found-eq-0 guard). node --test on the two phase test files individually. Shape lint + born-wired gate unchanged. Grep fence: no brain-client/enrichment-queue require in sensors/ or decide() path.
</verification>

<success_criteria>
ENRICH-01 mechanism complete: typed Part-8-clean queue entries from live wrapper misses and drain piggyback, idempotent with hit_count, CLI-appendable, census-seeded with honest provenance, hot path untouched, all hermetically tested with TDD commits.
</success_criteria>

<output>
Create `.planning/phases/249-context-driven-enrichment/249-01-SUMMARY.md` when done.
</output>
