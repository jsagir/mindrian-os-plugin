---
phase: 249-context-driven-enrichment
plan: 03
type: execute
wave: 2
depends_on: [249-01, 249-02]
files_modified:
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-XX-RUNBOOK-249-alias-collapse.md
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/reverse-salient-analysis.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/jtbd.json
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/reverse-salient-analysis.json
  - data/flagship-floor-set.json
  - docs/BRAIN-GRAPH-CENSUS.generated.md
  - data/brain-census.generated.json
autonomous: false
execution_model: sonnet
requirements: [ENRICH-02, ENRICH-03, ENRICH-04]
must_haves:
  truths:
    - "Every enumerated JTBD variant AND the loose fragments (jobs to be done, jtbd) normalize live to exactly ONE canonical node - proven by the read-tier probe leg after the collapse, not asserted"
    - "No graph write happened without an ingest_framework dry-run statement plan (or a verbatim prepared raw_cypher runbook for edge surgery) reviewed at an operator checkpoint, snapshot-first - the admin key never touches this machine"
    - "At least one below-floor framework reaches orchestration_readiness >= 3/4 LIVE on pws-brain-mcp.onrender.com, with its source-authored eval fixture green including the red proof, and the census regenerated (never hand-edited)"
    - "The 25-vs-50 floor denominator is ratified by the navigator and recorded BEFORE any ENRICH-04 floor-closure claim; no code path silently picks a denominator"
    - "The 41 ALIAS_OF self-loops are deleted in the same admin session (alias hygiene)"
    - "The floor gate re-run status is filed honestly (expected: still red, fewer misses) with the repeatable per-framework batch protocol recorded - the floor completes across the milestone tail at a usage-ranked cadence, never bulk; its green run is SWEEP-02's gate, not this phase's exit"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-XX-RUNBOOK-249-alias-collapse.md"
      provides: "Verbatim operator-pasteable statements: snapshot, ALIAS_OF merges, edge relinks, self-loop delete, proof probes, plus the triage tiering (repair vs author vs absent)"
      contains: "ALIAS_OF"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/reverse-salient-analysis.mjs"
      provides: "First Tier-B payload with SOURCE header + SHAPE NOTE per the invention-disclosure convention (target may change per triage; whichever framework is first, its payload follows this convention)"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/jtbd.json"
      provides: "JTBD known-answer fixture including the post-collapse normalize expectations"
    - path: "docs/BRAIN-GRAPH-CENSUS.generated.md"
      provides: "Regenerated tracked census reflecting the post-collapse, post-enrichment live graph"
  key_links:
    - from: "payloads/*.mjs"
      to: "ingest_framework dry-run statement plan"
      via: "the SINGLE admin write seam - no second write path"
      pattern: "ingest_framework"
    - from: "operator checkpoint"
      to: "live re-probe + census regen + floor gate re-run"
      via: "cross-repo definition of done: ingested AND live AND re-probed"
      pattern: "check-flagship-floor"
---

<objective>
The operator half of the phase: ENRICH-03's alias collapse (read-tier enumeration first, verbatim runbook, snapshot-first admin surgery, normalize proof), the first live ENRICH-02 enrichment cycle end to end (repair-first triage, source-authored payload, dry-run diff reviewed at a genuine Decision-Gate checkpoint, live re-probe, census regen, eval fixture), and the ENRICH-04 denominator ratification that BLOCKS floor closure.

Purpose: collapse-before-enrich is the locked ordering - a multi-match name makes every readiness probe ambiguous and dedup's LIMIT-1 fan-out can enrich the wrong node (Pitfall 7). JTBD collapses under ENRICH-03; the five other multi-match invoked names (Scenario Planning x6, Systems Thinking, Four Lenses of Innovation, Mullins Model, Red Teaming x2 each) follow the same runbook pattern under ENRICH-04's floor. Each enrichment ships one at a time through the dry-run diff gate: context and relevancy set the ORDER, the floor sets the FINISH LINE.
Output: collapsed aliases proven live, >= 1 framework crossed to >= 3/4 live, ratified denominator, regenerated census, honest floor status, and the repeatable batch protocol for the milestone tail.

Execution model: sonnet (per config GSD models directive).

EXTERNAL GATE (hard): every LIVE write in this plan is gated on Phase 247-03 having pushed the reconciled brain commits and redeployed Render. Task 3 verifies this with scripts/probe-brain-contract.cjs BEFORE pausing for the operator; if any leg fails, the task reports blocked-on-247-03 and stops - no partial write path is improvised. Tasks 1-2 are runnable before that gate (read-tier + local authoring + a decision).

Coordination: concurrent executor in plugin lib/mcp/* (248-02) - zero file overlap; do not reference lib/mcp state. Brain repo may have moved (247 executor): re-read actual file state at execution time; research citations are snapshots.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/249-context-driven-enrichment/249-RESEARCH.md
@.planning/phases/249-context-driven-enrichment/249-01-PLAN.md
@.planning/phases/249-context-driven-enrichment/249-02-PLAN.md
@docs/BRAIN-GRAPH-CENSUS.generated.md
@data/brain-census.generated.json
@scripts/probe-brain-contract.cjs
@/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/pipeline.mjs
@/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/dedup.mjs
@/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/invention-disclosure.mjs
@/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/project-list-structure.mjs
@.planning/phases/247-brain-surface-contract/247-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read-tier enumeration, repair-first triage, collapse runbook, first source-authored payload</name>
  <files>/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-XX-RUNBOOK-249-alias-collapse.md, /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/reverse-salient-analysis.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/jtbd.json, /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/reverse-salient-analysis.json</files>
  <action>
    All probes in this task are READ TIER (key in ~/.mindrian.env) - no admin key needed, no writes.

    1. DUPLICATE ENUMERATION (A3 resolves here): via the 247-02 wrappers or build-brain-census.cjs brainCall, run normalize_framework_name with the LOOSE fragments "jobs to be done" and "jtbd" (the CONTAINS semantics that surfaced 4 variants), then with loose fragments for the five other multi-match invoked names (scenario planning, systems thinking, four lenses, mullins, red teaming). Record every variant node name returned. The collapse plan is written against MEASURED nodes, not the remembered "4". If JTBD duplicates are already collapsed (one canonical from loose fragments), ENRICH-03 closes on the proof probe alone - record that honestly and skip its surgery statements.

    2. REPAIR-OVERLAP PROBE (A1 resolves here): for each of the 24 gap frameworks, use discover_structure and search on the read tier to classify: Tier A repair (structure already in the graph mis-shaped - orphaned ProcessSteps carrying parent_framework, the 9 list-prop frameworks project-list-structure.mjs already projects, or a missing pattern_type only), Tier B author-from-source, Tier C node absent (PEST). This tiering decides how much of ENRICH-04 is repair vs authoring - file the counts.

    3. COLLAPSE RUNBOOK at docs/2026-08-XX-RUNBOOK-249-alias-collapse.md (date the filename at execution): the 247-03 drop-runbook precedent - statements prepared VERBATIM for operator pasting, never improvised at the checkpoint. Contents: (a) create_snapshot step first; (b) canonical choice per the rule: the node carrying the structure rows, preferring the frontmatter name "Jobs to Be Done (JTBD)" so command-registry validation and the census keep resolving unchanged; (c) one MERGE (variant)-[:ALIAS_OF]->(canonical) per measured variant - ALIAS_OF, never delete (the variants remain as alias anchors, the dedup module's own design); (d) RELINK statements re-pointing any HAS_*/LEADS_TO/MENTIONS/FEEDS_INTO/USES_TECHNIQUE edges off variant nodes onto the canonical - without relink, readiness stays fragmented and the floor lies; (e) the single DELETE for the 41 ALIAS_OF self-loops (dedup.mjs documents them as pure traversal noise); (f) the post-collapse proof probe list. Verify the Memgraph Cypher syntax for the relink statements via Context7 before finalizing (the 247-03 Task 1 step 6 precedent) - raw_cypher is legal here ONLY because edge surgery between existing nodes has no ingest payload shape; everything else goes through ingest_framework.

    4. FIRST PAYLOAD: expected target Reverse Salient Analysis (5 uses, 1 canonical match, 0/4) UNLESS step 2 shows a Tier-A repair covers it - repair always beats authoring. Follow the invention-disclosure convention exactly: SOURCE header naming the document actually read in full with char count (first-party sources only: the plugin's commands/*.md and skills/ teaching content for this framework, ~/MindrianV2/prompts/*.py, or a curriculum doc the operator supplies at the checkpoint), SHAPE NOTE defending the modeling choice (is the LEADS_TO chain real? alternatives/matrix shapes REFUSE chaining - the Klein lesson; a shapeless-by-nature framework legitimately caps at 3/4), explicit decision-basis notes for anything asserted on navigator judgment. NO named read source = NO payload - stop and surface at the Task 3 checkpoint (never training data, Pitfall 4). Author its eval fixture FROM THE SOURCE (never from the payload, _fixture_class source_authored per the 249-02 harness rule) plus jtbd.json carrying the post-collapse expectations (every measured variant string and both loose fragments -> the one canonical; 3 structure rows per current census; flow expectations only if a named source asserts an order).

    5. Per the dev-research compositing rule (CLAUDE.md), mirror the enumeration + triage findings as a dated research trail in the rethinking-mindrianos room, cross-linked to this phase dir (the recent room's research-trail commits are the format precedent).

    Commit locally in the brain repo; do NOT push (Task 3 owns push + deploy sequencing).
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/eval-framework-structure.test.mjs && grep -q "create_snapshot" docs/*RUNBOOK-249-alias-collapse.md && grep -q "ALIAS_OF" docs/*RUNBOOK-249-alias-collapse.md && node -e "import('./payloads/reverse-salient-analysis.mjs').then(()=>console.log('payload parses'))"</automated>
  </verify>
  <done>Measured variant lists filed for all 6 multi-match names; repair/author/absent tiering counted; runbook complete with snapshot-first verbatim statements and proof probes; first payload + 2 source-authored fixtures authored and parsing; harness suite still green with the new fixtures; findings mirrored to the rethinking-mindrianos room.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Navigator ratifies the ENRICH-04 floor denominator (BLOCKS floor closure)</name>
  <files>data/flagship-floor-set.json</files>
  <action>
    Present the decision and PAUSE for the navigator. Decision: which framework set is THE ENRICH-04 / SWEEP-02 floor - the frontmatter enumeration or the canon-25 subset?

    Context to present: canon prose says "the 25 methodology commands"; the dated frontmatter scan counts 50 kind:methodology commands referencing 28 distinct frameworks. The census recorded this discrepancy as unresolved 2026-08-10 and the floor gate currently prints BOTH numbers with the ratification marked OPEN. The floor script takes its enumeration from disk, so this ruling changes data, not code. No ENRICH-04 completion claim may be made before this decision is recorded - whatever the choice.

    Option option-frontmatter (research recommendation): the 50-command / 28-framework frontmatter enumeration. Pros: the citable superset, the census's own enumeration source, zero code or data change - the gate already operates on it. Cons: the larger floor - 24 frameworks below 3/4 today, closed across the milestone tail.

    Option option-canon25: the canon-25 subset, with the navigator naming the 25 commands. Pros: smaller floor, faster to green. Cons: the navigator must enumerate the 25; the executor then writes data/flagship-floor-set.json (the ONLY legal way that file comes to exist) and the gate narrows; canon prose and frontmatter still need reconciling in the commands themselves eventually.

    Option option-defer: defer the ruling. Pros: no decision debt taken today. Cons: ENRICH-04 floor closure stays formally OPEN; the gate keeps printing both numbers; SWEEP-02 cannot cite a green floor until ratified - deferral is recorded, never silent.

    After the navigator answers: record the choice + reasoning in the runbook doc and the SUMMARY; on option-canon25 additionally write data/flagship-floor-set.json from the named 25 and re-run node scripts/check-flagship-floor.cjs to confirm the gate cites the ratification in its header.
  </action>
  <verify>
    <automated>node scripts/check-flagship-floor.cjs 2>&1 | head -5</automated>
  </verify>
  <done>The ratification (or explicit deferral) is recorded in the runbook + SUMMARY; the floor gate header reflects the ruling (or still prints both numbers with OPEN on deferral); data/flagship-floor-set.json exists only if option-canon25 was chosen.</done>
  <resume-signal>Select: option-frontmatter, option-canon25 (with the 25 names), or option-defer.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Post-247-03 admin sessions - collapse, deploy, first enrichment through the dry-run gate, live proof</name>
  <files>docs/BRAIN-GRAPH-CENSUS.generated.md, data/brain-census.generated.json</files>
  <action>
    Everything is staged before this pause: the collapse runbook with verbatim statements (Task 1), the first payload + source-authored fixtures (Task 1), the unpushed brain-repo commits (dimensions field, eval harness - 249-02), the floor gate and its red baseline (249-02), the ratified denominator (Task 2). This checkpoint is where the operator supplies the admin key (never on this machine, never stored) and the writes land - the 246-02 / 247-03 operator pattern.

    PRECONDITION (run BEFORE pausing): node scripts/probe-brain-contract.cjs - ALL legs must PASS, proving 247-03 pushed + redeployed. Any failing leg = report "blocked on 247-03" with the leg evidence and STOP; do not improvise a partial write path. Also confirm ingest_framework admin reachability at session start (research A2): if the HTTP admin path is not reachable, the fallback is an operator-run local admin session against the Render Bolt endpoint - decided at the checkpoint, recorded in the SUMMARY.

    Then present the operator ceremony, in this exact order (collapse BEFORE any enrichment of a multi-match name):
    1. create_snapshot (admin key supplied by operator, used in-session only).
    2. SESSION A - ENRICH-03: paste the runbook statements verbatim: ALIAS_OF merges for every measured JTBD variant, edge relinks onto the canonical, the 41 self-loop DELETE.
    3. Pull the brain repo (247-03's pushed state), then push the local 249 commits (dimensions field, eval harness, payloads, fixtures, runbook); trigger Render redeploy; wait until the service reports the new revision healthy.
    4. READ-TIER PROOFS: every measured variant string + both loose fragments normalize to exactly 1 canonical (the runbook's proof probe list); node scripts/probe-framework-evals.mjs - jtbd fixture green, dimensions assertions now LIVE (no longer PENDING-DEPLOY).
    5. SESSION B - first ENRICH-02 cycle: run ingest_framework in dryRun for the staged payload (or the Tier-A repair payload per triage); render the statement plan + warns + before/after readiness prediction. OPERATOR REVIEWS THE DIFF - a genuine Decision-Gate fork (Part 11): APPROVE / REJECT with reason / DEFER. The reviewer is explicitly asked: "is this chain real?" (Klein lesson). On APPROVE only: snapshot, commit the ingest (reject-fails-whole, single explicit tx), then LIVE re-probe orchestration_readiness on pws-brain-mcp.onrender.com - the target framework must report >= 3/4 with the expected dimensions vector.
    6. Regenerate the tracked instruments (never hand-edit): node scripts/build-brain-census.cjs --lane-a, then node scripts/check-flagship-floor.cjs - file the new honest count (expected: still red, fewer misses).
    7. Record the REPEATABLE BATCH PROTOCOL in the SUMMARY: subsequent frameworks ride this same Task-3 ceremony ONE AT A TIME by usage rank (JTBD flow completion next at 5 uses, then Six Thinking Hats at 4, the remaining multi-match names collapsing via the same runbook pattern before their probes count), each with dry-run diff review, source-authored fixture + red proof, live re-probe, census regen. The floor gate's eventual green run is the SWEEP-02 gate artifact - it completes across the milestone tail, never as a bulk batch (navigator direction).
  </action>
  <verify>
    <automated>node scripts/probe-brain-contract.cjs && node /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-framework-evals.mjs && node scripts/build-brain-census.cjs --lane-a && node scripts/check-flagship-floor.cjs; echo "floor exit: $? (non-zero expected until the milestone tail completes)"</automated>
  </verify>
  <done>Collapse proven live by the normalize proof; brain commits deployed and dimensions live; >= 1 framework at >= 3/4 live with its eval green; census + floor status regenerated and filed honestly; batch protocol recorded.</done>
  <resume-signal>Type "approved" with the session evidence (snapshot ids, dry-run verdicts, live readiness scores, floor count), or describe issues / "blocked on 247-03".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this machine -> admin tier (live Memgraph) | admin key is operator-side only; every write crosses at a checkpoint |
| payload content -> the curated graph | uncited content would corrode the moat's curated-calibration claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-249-09 | Tampering | partial/corrupting graph write | mitigate | snapshot-first; ingest_framework single explicit tx with reject-fails-whole; dry-run diff reviewed before any commit |
| T-249-10 | Tampering | improvised Cypher at the checkpoint | mitigate | runbook statements prepared verbatim in Task 1, Context7-verified Memgraph syntax; raw_cypher scoped to edge surgery only |
| T-249-11 | Repudiation | "ingested locally, stale on Render" false done | mitigate | probe-brain-contract precondition; live re-probe mandatory per enrichment; census + floor regenerated from the live URL |
| T-249-12 | Elevation of Privilege | admin key exposure | mitigate | key supplied by operator in-session, never stored, never logged, never on this machine (246-02/247-03 precedent) |
| T-249-13 | Information Disclosure | payload/runbook content | mitigate | methodology content from named first-party sources only; no user bytes, no training-data authoring (no source = no payload) |
| T-249-SC | Tampering | package installs | accept | no packages installed this phase (research Package Legitimacy Audit: none) |
</threat_model>

<verification>
Read-tier normalize proof green for every measured variant + loose fragments. probe-framework-evals.mjs green live including dimensions. Target framework >= 3/4 live. Census regenerated, floor gate re-run with honest status filed. Denominator ratification recorded. bash tests/run-all-249.sh and the brain suite green.
</verification>

<success_criteria>
ENRICH-03 proven live (collapse + normalize proof + self-loop hygiene). One full ENRICH-02 cycle shipped end to end (source-authored payload, reviewed dry-run diff, live re-probe, census regen, eval with red proof). ENRICH-04's denominator ratified and its cadence + gate machinery filed with an honest red status - the floor's green run is SWEEP-02's gate, reached one framework at a time.
</success_criteria>

<output>
Create `.planning/phases/249-context-driven-enrichment/249-03-SUMMARY.md` when done.
</output>
