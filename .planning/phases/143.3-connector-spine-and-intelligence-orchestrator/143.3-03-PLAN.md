---
phase: 143.3
plan: 03
type: execute
wave: 3
depends_on: ["143.3-01", "143.3-02"]
files_modified:
  - skills/intelligence-orchestrator/SKILL.md
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03, ORCH-04]
canon_parts: [Part 2, Part 3, Part 4, Part 8, Part 9]
must_haves:
  truths:
    - "skills/intelligence-orchestrator/SKILL.md exists and is the generic reach dispatcher that READS data/connector-registry.json, never a hardcoded table (ORCH-01)"
    - "The doctrine specifies the 5-step core loop: read spine LIVE (tier-gated) -> map sub-mode via registry -> gate to ONE via the Intelligence Hierarchy -> offer as a Shape-F Decision Gate (never auto-fire) -> on APPROVE resolve via commandsForFramework(<exact framework>) + fire (ORCH-02)"
    - "Filing is specified as fileEvidenceWithReadback (fallback wireAccept on readback error) + surfaceFileEvidenceResult + memory_event + cascade edge (ORCH-03)"
    - "The live-call tier gate is specified (mode_a/mode_b live, tier_0 degrade to doctrine-sim) and coexistence with Phase 144 + room-proactive is one-reach-per-beat arbitrated by the hierarchy (ORCH-04)"
    - "deep_research is the SANCTIONED multi-reach exception; the dispatch slug mos:research is translated via data/dispatch-framework-map.json to the EXACT framework name before the resolver (WFL-01)"
  artifacts:
    - path: "skills/intelligence-orchestrator/SKILL.md"
      provides: "The first consumer of the dispatchSensors spine - the registry-reading reach dispatcher doctrine"
      min_lines: 120
      contains: "data/connector-registry.json"
  key_links:
    - from: "skills/intelligence-orchestrator/SKILL.md"
      to: "data/connector-registry.json"
      via: "registry read (sensor_index lookup on a fired sensor)"
      pattern: "connector-registry"
    - from: "skills/intelligence-orchestrator/SKILL.md"
      to: "lib/workflow/command-resolver.cjs commandsForFramework"
      via: "WFL-01 resolver door on APPROVE"
      pattern: "commandsForFramework"
    - from: "skills/intelligence-orchestrator/SKILL.md"
      to: "lib/core/navigation/file-evidence-readback.cjs"
      via: "fileEvidenceWithReadback + surfaceFileEvidenceResult"
      pattern: "fileEvidenceWithReadback"
---

<objective>
Ship skills/intelligence-orchestrator/SKILL.md: the FIRST CONSUMER of the dispatchSensors spine and the first READER of data/connector-registry.json (ORCH-01..04). The skill is conversational doctrine (markdown), so it works across all three surfaces (CLI / Desktop / Cowork) - it surfaces reaches through the existing Shape-F selector (143.1 dial-TUI) and documents no new UI. It is ~90% wiring: it consumes 6 shipped exports (dispatchSensors, commandsForFramework, fileEvidenceWithReadback/surfaceFileEvidenceResult, wireAccept, reverse-salient-agent.surfaceFinding, hatScopeFor) and reads the registry; it builds no new engine.

Purpose: The Phase-143 sensor spine has ZERO production consumers - every SENS-0x fires into the void. This skill closes that gap and proves the Connector Contract: the orchestrator routes by READING the registry, so any future surface joins the spine by declaring, never by editing this skill.

Output: skills/intelligence-orchestrator/SKILL.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-CONTEXT.md
@.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/ROUTING-TABLE-intelligence-orchestrator.md
@.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/RESEARCH-HATS-WIRING.md
@.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-PATTERNS.md
@docs/CONNECTOR-CONTRACT.md
</context>

<artifacts_this_phase_produces>
NEW file (excluded from drift verification):
- skills/intelligence-orchestrator/SKILL.md (the orchestrator doctrine)

NEW frontmatter keys on the skill (per RESEARCH-HATS-WIRING.md lines 43-55): consumes, resolver, registry, dispatch_map, reach_ids, posture_ids, filing, live_call (on top of the room-proactive frontmatter shape: name, description, canon_parts, allowed-tools)

This skill CONSUMES (does NOT create) the 6 shipped exports + the 2 data files from Plans 01/02. Those are inputs, not new symbols of this plan. The 6 consumed exports: dispatchSensors (lib/core/insight-sensors.cjs:230), commandsForFramework (lib/workflow/command-resolver.cjs:85), fileEvidenceWithReadback + surfaceFileEvidenceResult (lib/core/navigation/file-evidence-readback.cjs:72,201), wireAccept (lib/core/findings-wirer.cjs:133), reverse-salient-agent.surfaceFinding (lib/agents/reverse-salient-agent.cjs:479), hatScopeFor (lib/core/sensors/hat-scoping-table.cjs:92).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the orchestrator frontmatter + the registry-reading dispatcher core (ORCH-01)</name>
  <read_first>
    skills/room-proactive/SKILL.md lines 1-31 (frontmatter shape: name, description folded block, activation, paths, triggers) and lines 85-178 (the side-channel read + Decision Capture flow to mirror);
    skills/brain-connector/SKILL.md lines 1-9 (frontmatter) and lines 94-106 (the commandsForFramework resolver-discipline doctrine to copy verbatim);
    .planning/phases/143.3-connector-spine-and-intelligence-orchestrator/RESEARCH-HATS-WIRING.md lines 43-56 (the proposed orchestrator frontmatter + the composition note: hats compose UNDER brain_consult, RS/HSI/whitespace/analogies under context_block/brain_consult as render labels, deep-research IS deep_research, NO 6th reach);
    data/connector-registry.json (the runtime input: sensor_index + connectors[] from Plan 02);
    docs/CONNECTOR-CONTRACT.md (the schema the skill reads);
    lib/core/insight-sensors.cjs lines 230-247 (dispatchSensors signature + return shape)
  </read_first>
  <action>
    Create skills/intelligence-orchestrator/SKILL.md. Frontmatter (mirror room-proactive YAML style, no em-dashes): name: intelligence-orchestrator; a folded description block stating it is the reach dispatcher that consumes the SENS sensor spine, maps each candidate-reach to an intelligence sub-mode via the registry, applies posture + one-reach-per-beat gating, surfaces ONE as a Shape-F Decision Gate, never auto-executes; canon_parts: [Part 2, Part 3, Part 4, Part 8, Part 9]; phase: 143.3; consumes: lib/core/insight-sensors.cjs::dispatchSensors; resolver: lib/workflow/command-resolver.cjs; registry: data/connector-registry.json; dispatch_map: data/dispatch-framework-map.json; reach_ids: [context_block, contradiction, cross_room, brain_consult, deep_research]; posture_ids: [push_forward, hold, pull_back]; filing: fileEvidenceWithReadback (fallback wireAccept); live_call: dispatchSensors gated behind tier_mode (degrade to doctrine-sim at tier_0); allowed-tools: [Read, Bash, Agent, WebSearch, WebFetch, mcp__pinecone__search-docs, mcp__brain_*]. Body opens with the REGISTRY-IS-THE-TABLE doctrine: the orchestrator reads data/connector-registry.json (NEVER a hardcoded routing table); on a fired sensor it looks up the connectors whose sensor_triggers match via the registry sensor_index. State the composition rule (hats compose under brain_consult; RS/HSI/whitespace/analogies under context_block/brain_consult as render labels via sub_mode; deep-research IS the deep_research reach; NO 6th reach_id - cite the frozen 5 + frozen 3 verbatim). State the Part 8 boundary up front: every Brain query carries generic framework handles + phase ids + enums ONLY; turn text and artifact bodies NEVER enter commandsForFramework, a Brain packet, or a web query. NO em-dashes.
  </action>
  <acceptance_criteria>
    - skills/intelligence-orchestrator/SKILL.md exists and references the registry: grep -c "connector-registry.json" skills/intelligence-orchestrator/SKILL.md returns >= 1
    - It declares it reads the registry rather than a hardcoded table: grep -iE "never a hardcoded|reads.*registry|registry.*never" skills/intelligence-orchestrator/SKILL.md returns a match
    - The frozen 5 reach_ids and frozen 3 postures are cited verbatim: grep -c -E "context_block|contradiction|cross_room|brain_consult|deep_research" returns >= 5 and grep -E "push_forward|hold|pull_back" returns a match
    - The Part 8 boundary is stated: grep -i "Part 8\|generic.*handle\|never.*user content\|never.*turn text" skills/intelligence-orchestrator/SKILL.md returns a match
    - No em-dashes: grep -c "—" skills/intelligence-orchestrator/SKILL.md returns 0
  </acceptance_criteria>
  <done>The skill exists with the consumer frontmatter (frozen 5 + frozen 3, registry, dispatch_map, live_call, filing) and the REGISTRY-IS-THE-TABLE + Part 8 doctrine; it routes by reading the registry, not a hardcoded table.</done>
</task>

<task type="auto">
  <name>Task 2: Specify the 5-step core loop + filing + WFL-01 resolution (ORCH-02, ORCH-03)</name>
  <read_first>
    .planning/phases/143.3-connector-spine-and-intelligence-orchestrator/ROUTING-TABLE-intelligence-orchestrator.md lines 16-23 (the 5-step core loop with decisions baked in) and lines 26-36 (the routing table the loop maps through + the hat-scope map for PUSH-06);
    skills/room-proactive/SKILL.md lines 153-178 (the After-Filing Decision Capture APPROVE/REJECT/DEFER flow - the Shape-F gate render to mirror; note F.0 caveat at line 118);
    skills/brain-connector/SKILL.md lines 94-106 (the resolver discipline: commandsForFramework is the only door, never name a /mos: from memory, degrade to run manually);
    lib/workflow/command-resolver.cjs lines 85-90 (commandsForFramework) + 110-121 (composeWorkflow for the deep_research multi-angle plan);
    lib/core/navigation/file-evidence-readback.cjs lines 41-72 (fileEvidenceWithReadback param shape + return) + 194-240 (surfaceFileEvidenceResult, the FILEVAL honesty remind);
    lib/core/findings-wirer.cjs lines 117-133 (wireAccept params + return - the fallback);
    data/dispatch-framework-map.json (the OPEN-1 translation layer from Plan 01)
  </read_first>
  <action>
    Add a "## The 5-step core loop" section to the skill spelling out, as numbered doctrine (no fenced code, no implementations - directive prose naming the exact functions): STEP 1 read the spine LIVE via dispatchSensors(turn, tuple, ctx) returning Array of {reach_id, posture, dispatch, companions, signal, evidence}; GATE: authoritative only when tier_mode is mode_a or mode_b; at tier_0 degrade to the doctrine-sim floor (Larry's Provoked-table doctrine) so the skill is safe pre-144 (ORCH-04 predicate). STEP 2 map each fired reach's dispatch handle / sub_mode to its EXACT framework name via data/dispatch-framework-map.json (the raw mos:research handle MUST translate to "Hypothesis-Driven Problem Solving" - never pass the slug; this is the WFL-01 guard); the sub_mode is a render label, never a reach_id. STEP 3 gate to ONE (one-reach-per-beat): when multiple fired, rank by the Intelligence Hierarchy (Tensions > Bottlenecks > HSI Surprises > Convergences > Blind Spots) then evidence strength; deep_research is the SANCTIONED exception (may chain a multi-angle plan via composeWorkflow, plan_gated). STEP 4 offer, never fire: surface the chosen reach as the connector's surface (Shape-F sub-shape, e.g. F.0/F.1) Decision Gate (Part 3) using the room-proactive APPROVE/REJECT/DEFER Decision-Capture flow; generic handles only to Brain (Part 8); web hat-scoped via hatScopeFor (Part 2). STEP 5 on APPROVE: resolve the real command via commandsForFramework(<exact framework name>) (WFL-01, never hardcode the slug; degrade to "run X manually" when it returns empty); fire it. Add a "## Filing" section (ORCH-03): file the result via the connector's filing field - fileEvidenceWithReadback for evidence-producing families, fallback wireAccept on readback error (decision 2); memory_event_only for surface-only families (six-hats); then surfaceFileEvidenceResult to remind the navigator what landed (the FILEVAL honesty rule); every decision becomes a memory_event + a typed cascade edge (Part 4 / Part 9). State the Part 9 truth-state rule: filed EvidenceClaim lands review_status: proposed, never confirmed (only a human gate promotes truth-claim nodes). NO em-dashes.
  </action>
  <acceptance_criteria>
    - All 5 steps are present and named: grep -c -E "dispatchSensors|dispatch-framework-map|Intelligence Hierarchy|Decision Gate|commandsForFramework" skills/intelligence-orchestrator/SKILL.md returns >= 5
    - The WFL-01 slug-to-name rule is stated: grep -i "mos:research\|never.*slug\|Hypothesis-Driven Problem Solving" skills/intelligence-orchestrator/SKILL.md returns a match
    - The Intelligence Hierarchy order is stated: grep -i "Tensions.*Bottlenecks\|Bottlenecks.*HSI\|one-reach-per-beat" skills/intelligence-orchestrator/SKILL.md returns a match
    - Filing doctrine names all three surfaces: grep -c -E "fileEvidenceWithReadback|wireAccept|surfaceFileEvidenceResult" skills/intelligence-orchestrator/SKILL.md returns >= 3
    - The Part 9 proposed-not-confirmed rule is stated: grep -i "proposed\|never.*confirmed\|human.*gate" skills/intelligence-orchestrator/SKILL.md returns a match
    - "never auto-fire" / "offer, never fire" doctrine present (Part 3): grep -i "never auto\|offer, never\|never.*fire" skills/intelligence-orchestrator/SKILL.md returns a match
    - No em-dashes: grep -c "—" skills/intelligence-orchestrator/SKILL.md returns 0
  </acceptance_criteria>
  <done>The skill spells out the 5-step loop (LIVE tier-gated read -> dispatch-map translation -> hierarchy gate -> Shape-F offer never-fire -> resolver-fire) and the filing doctrine (fileEvidenceWithReadback/wireAccept/surfaceFileEvidenceResult + memory_event + cascade edge + proposed truth-state), all naming the exact shipped functions, no fenced code.</done>
</task>

<task type="auto">
  <name>Task 3: Specify the tier gate + coexistence (ORCH-04) and resolve OPEN-4/OPEN-5</name>
  <read_first>
    .planning/phases/143.3-connector-spine-and-intelligence-orchestrator/ROUTING-TABLE-intelligence-orchestrator.md lines 45-48 (OPEN-2 tier predicate + non-double-fire with Phase 144; OPEN-4 the rs-agent [BRAIN] header vs the local context_block reach; OPEN-5 coexistence with room-proactive);
    .planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-CONTEXT.md lines 26-30 (locked decision 1: live tier-gated, Phase 144 is engine-side, this is prompt-side, they coexist; decision 3: new skill, arbitrate via the hierarchy);
    skills/room-proactive/SKILL.md lines 22-30 + 87-102 (the PostToolUse last-cascade.json side-channel read - the OPEN-5 coexistence model);
    lib/agents/reverse-salient-agent.cjs lines 479-530 (surfaceFinding + the locked [BRAIN] chip header at line 530 - OPEN-4) and lines 112-115 (the BRAIN.md quadruple read is LOCAL-only via folder-memory, Canon Part 8 - the proof the header is a render choice, not a Brain call)
  </read_first>
  <action>
    Add a "## Tier gate + coexistence" section (ORCH-04). Tier predicate (OPEN-2, locked decision 1): the live dispatchSensors call is authoritative only at tier_mode mode_a or mode_b; at tier_0 (cold room) degrade to the doctrine-sim floor; state explicitly that Phase 144 is the ENGINE-side consumer (decide()) and this skill is the PROMPT-side consumer of the same spine - they COEXIST without double-firing because both honor one-reach-per-beat arbitrated by the Intelligence Hierarchy (a single reach surfaces per beat regardless of which consumer surfaced it). Resolve OPEN-5 (coexistence with room-proactive): room-proactive surfaces CASCADE findings from filing (Part-4 edges) while the orchestrator surfaces SENSOR reaches; both want the Decision Gate; the Intelligence Hierarchy ranks BOTH on the same scale (a room-proactive CONTRADICTS finding = Tensions = rank 1, outranks a bottleneck reach); one-reach-per-beat across BOTH skills. Specify the chosen arbitration: the orchestrator MAY read room-proactive's <roomDir>/.mindrian/last-cascade.json (same LOCAL side-channel, Part 8 clean - never the Brain) to fold cascade findings into the same hierarchy ranking, OR they stay separate surfaces the hierarchy arbitrates downstream - state the decision (read last-cascade.json into the ranking so one-reach-per-beat holds across both surfaces) and that the read is LOCAL-only per Part 8. Resolve OPEN-4 (the rs-agent [BRAIN] header vs the local context_block reach): the [BRAIN] chip that reverse-salient-agent.surfaceFinding renders (line 530) is a RENDER CHOICE only - the underlying read is folder-memory.readQuadruple, which is LOCAL-only (Canon Part 8), so the header does NOT imply a Brain call and does NOT contradict PUSH-02's context_block (LOCAL) reach; state this reconciliation explicitly so the Part-8 check passes. Add a closing "## Tri-Polar surfaces" note: the skill is conversational doctrine, so CLI fires it via the sensor hook + Shape-F selector, Desktop surfaces it conversationally through Larry, Cowork shows the same SELECTED_REACH gate with a per-actor {topic} label (mirror the 143.1 surface-agnostic core); no new UI. NO em-dashes.
  </action>
  <acceptance_criteria>
    - The tier predicate is stated (mode_a/mode_b live, tier_0 degrade): grep -i "tier_0\|mode_a\|mode_b\|doctrine-sim" skills/intelligence-orchestrator/SKILL.md returns a match
    - Phase 144 coexistence (engine-side vs prompt-side, no double-fire) is stated: grep -i "144\|engine-side\|prompt-side\|one-reach-per-beat" skills/intelligence-orchestrator/SKILL.md returns a match
    - OPEN-5 room-proactive coexistence + last-cascade.json is stated: grep -i "room-proactive\|last-cascade" skills/intelligence-orchestrator/SKILL.md returns a match
    - OPEN-4 [BRAIN] header reconciliation is stated as a render choice / LOCAL read: grep -i "BRAIN\].*render\|render choice\|readQuadruple\|LOCAL only" skills/intelligence-orchestrator/SKILL.md returns a match
    - Tri-Polar note covers all three surfaces: grep -i "CLI" && grep -i "Desktop" && grep -i "Cowork" each return a match in skills/intelligence-orchestrator/SKILL.md
    - No em-dashes: grep -c "—" skills/intelligence-orchestrator/SKILL.md returns 0
  </acceptance_criteria>
  <done>The skill specifies the tier predicate, the Phase 144 + room-proactive coexistence (one-reach-per-beat across both, last-cascade.json folded into the hierarchy), resolves OPEN-4 ([BRAIN] header = LOCAL render choice, no Part 8 breach) and OPEN-5, and notes the Tri-Polar surface behavior with no new UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator -> Brain (commandsForFramework / mcp__brain_*) | Only generic framework handles + phase ids + enums may cross; turn text and artifact bodies must never |
| orchestrator -> web (WebSearch / WebFetch) | Must be hat-scoped via hatScopeFor (Part 2); Red hat = no web; never an unscoped search |
| orchestrator -> last-cascade.json | A LOCAL side-channel read (Part 8 clean); never a Brain or network read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.3-09 | Information Disclosure | commandsForFramework call on APPROVE | mitigate | the skill doctrine mandates passing only the EXACT framework name (a generic published-methodology handle) resolved via the dispatch map; turn text / artifact bodies are explicitly forbidden from the resolver call (stated in Task 1 + Task 2 doctrine) |
| T-143.3-10 | Information Disclosure | the deep_research / SENS-04 web call | mitigate | the web call is hat-scoped via hatScopeFor (Part 2); Red hat returns web_enabled:false; the skill carries only a generic topic handle + hat + focus into the search, never user text (RESEARCH-HATS-WIRING.md line 24); deep_research is plan_gated (fires only on APPROVE) |
| T-143.3-11 | Information Disclosure | a Brain packet built during a reach | mitigate | the doctrine states every Brain query carries generic framework handles + phase ids + enums ONLY; the Part 8 boundary is stated up front (Task 1) and reaffirmed in the filing/loop sections |
| T-143.3-12 | Tampering | OPEN-4 rs-agent [BRAIN] header | accept | reconciled as a RENDER CHOICE only - the underlying read is folder-memory.readQuadruple (LOCAL-only, Canon Part 8 verified at reverse-salient-agent.cjs lines 112-115); no Brain call, no contradiction with the context_block LOCAL reach |
| T-143.3-13 | Information Disclosure | last-cascade.json read (OPEN-5) | accept | a LOCAL side-channel file (same one room-proactive reads, Part 8 clean per skills/room-proactive/SKILL.md line 95); never the Brain or any network surface |
| T-143.3-SC | Tampering | npm/pip/cargo installs | mitigate | NONE - this plan adds a markdown skill only; zero dependencies, no install task exists |
</threat_model>

<verification>
- skills/intelligence-orchestrator/SKILL.md exists, references data/connector-registry.json, and declares it reads the registry not a hardcoded table
- The 5-step loop names dispatchSensors, the dispatch-framework map (WFL-01), the Intelligence Hierarchy, the never-fire Decision Gate, and commandsForFramework
- Filing names fileEvidenceWithReadback + wireAccept fallback + surfaceFileEvidenceResult + memory_event + cascade edge + proposed truth-state
- The tier gate, Phase 144 coexistence, OPEN-4 and OPEN-5 resolutions, and the Tri-Polar note are all present
- grep -c "—" skills/intelligence-orchestrator/SKILL.md returns 0 (no em-dashes)
- Part 8: the doctrine forbids turn text / artifact bodies in any Brain or web call; web is hat-scoped
</verification>

<success_criteria>
- ORCH-01: the skill is the registry-reading generic reach dispatcher (never a hardcoded table)
- ORCH-02: the 5-step core loop is fully specified with WFL-01 resolution and the never-fire Decision Gate
- ORCH-03: filing via fileEvidenceWithReadback (fallback wireAccept) + surfaceFileEvidenceResult + memory_event + cascade edge
- ORCH-04: the tier gate + Phase 144 + room-proactive coexistence (one-reach-per-beat across surfaces) are specified; OPEN-4 and OPEN-5 resolved
</success_criteria>

<output>
Create `.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-03-SUMMARY.md` when done
</output>
