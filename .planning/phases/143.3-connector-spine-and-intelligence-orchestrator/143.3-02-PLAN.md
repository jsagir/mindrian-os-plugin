---
phase: 143.3
plan: 02
type: execute
wave: 2
depends_on: ["143.3-01"]
files_modified:
  - commands/find-bottlenecks.md
  - commands/find-connections.md
  - commands/find-analogies.md
  - commands/score-innovation.md
  - commands/whitespace.md
  - commands/think-hats.md
  - commands/research.md
  - data/connector-registry.json
  - scripts/build-connector-registry.cjs
  - scripts/hooks/pre-commit
  - .git/hooks/pre-commit
  - tests/test-connector-tripwire.cjs
autonomous: true
requirements: [CONN-03, CONN-04]
canon_parts: [Part 3, Part 7, Part 8]
must_haves:
  truths:
    - "The 7 algorithmic-cohort commands (the 6 PUSH families + whitespace) declare connector: frontmatter and auto-join the spine (CONN-04)"
    - "data/connector-registry.json now contains every retrofitted connector with a populated sensor_index"
    - "The --check tripwire validates: every reach_id is one of the frozen 5, every framework resolves via commandsForFramework(), every posture is one of the frozen 3, no duplicate (sensor, reach, sub_mode) collision (CONN-03)"
    - "The pre-commit hook runs --check on staged commands/skills/registry changes so drift is impossible to commit (CONN-03)"
    - "A new methodology command without a connector: block triggers a WARN (stderr, not exit 1) - the opt-in nudge"
  artifacts:
    - path: "commands/find-bottlenecks.md"
      provides: "The reverse-salient connector (reach context_block, sub_mode reverse-salient, posture pull_back)"
      contains: "connector:"
    - path: "scripts/build-connector-registry.cjs"
      provides: "The --check body extended with the four CONN-03 validations + the WARN nudge"
      contains: "commandsForFramework"
    - path: "scripts/hooks/pre-commit"
      provides: "The staged-file trigger that runs --check (tracked source-of-record)"
      contains: "build-connector-registry"
  key_links:
    - from: "commands/*.md connector.framework"
      to: "data/framework-names.json + commandsForFramework()"
      via: "the --check WFL-01 resolver-resolution guard"
      pattern: "commandsForFramework"
    - from: "scripts/hooks/pre-commit"
      to: "scripts/build-connector-registry.cjs --check"
      via: "staged-file git diff trigger"
      pattern: "connector-registry.*--check"
---

<objective>
Retrofit the algorithmic cohort (CONN-04) so the 7 commands the orchestrator routes today declare connector: frontmatter and auto-join the spine, and wire the CI tripwire (CONN-03) so drift is impossible to commit. After this plan the registry is fully populated and the four connector validations + the pre-commit guard + the WARN nudge are live.

Purpose: A surface is either wired (declares its connector, participates in the spine) or explicitly legacy. Making that STRUCTURAL and CI-enforced is the moat (CLAUDE.md MWP mandate; Phase 122 precedent). The cohort already carries exact framework names from Phase 122, so the retrofit is declarative + additive.

Output: 7 connector: blocks, a fully populated data/connector-registry.json, the extended --check tripwire, the pre-commit wiring (tracked + installed), and the tripwire test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-CONTEXT.md
@.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/CONNECTOR-CONTRACT-spine.md
@.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-PATTERNS.md
@.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/ROUTING-TABLE-intelligence-orchestrator.md
@docs/CONNECTOR-CONTRACT.md
</context>

<artifacts_this_phase_produces>
NEW files (excluded from drift verification):
- tests/test-connector-tripwire.cjs (the CONN-03 validation test)

MODIFIED (connector: block added - additive, the existing frameworks: value is reused unchanged):
- commands/find-bottlenecks.md, find-connections.md, find-analogies.md, score-innovation.md, whitespace.md, think-hats.md, research.md

EXTENDED:
- scripts/build-connector-registry.cjs (the four CONN-03 --check validations + WARN nudge, building on Plan 01's generator)
- scripts/hooks/pre-commit + .git/hooks/pre-commit (new staged-file trigger)
- data/connector-registry.json (regenerated, now populated)

NEW registry content: the 7 cohort connector entries + the populated sensor_index (SENS-02/03/04/05/06/07 -> connectors[])
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit the 7 algorithmic-cohort commands with connector: frontmatter (CONN-04)</name>
  <read_first>
    commands/find-bottlenecks.md lines 1-20 (the Phase 122 frameworks: block under the "# --- Phase 122 workflow-layer frontmatter ---" delimiter at line 6; line 8/10 frameworks: ["Reverse Salient Analysis"] - the value the connector.framework MUST equal);
    commands/research.md lines 1-26 (the analog with frameworks: ["Hypothesis-Driven Problem Solving"] at line 11);
    commands/find-connections.md, find-analogies.md, score-innovation.md, whitespace.md, think-hats.md (each line 10-11 frameworks: value - confirmed: Usher's Model of Cumulative Synthesis, Four Lenses of Innovation, HSI Semantic Surprise Analysis Assistant x2, Six Thinking Hats);
    docs/CONNECTOR-CONTRACT.md (the schema this block conforms to - from Plan 01);
    .planning/phases/143.3-connector-spine-and-intelligence-orchestrator/ROUTING-TABLE-intelligence-orchestrator.md lines 26-33 (the per-family reach_id / sub_mode / posture / hierarchy_rank / filing values to declare per command);
    .planning/phases/143.3-connector-spine-and-intelligence-orchestrator/RESEARCH-HATS-WIRING.md line 24 (SENS-04 -> research) and lines 30-36 (the hat-scope + filing-per-family for OPEN-3)
  </read_first>
  <action>
    Add an additive connector: block to each of the 7 command frontmatters under a new "# --- Phase 143.3 connector frontmatter ---" delimiter (mirror the Phase 122 delimiter pattern). The connector.framework value MUST equal the file's existing frameworks: entry (do NOT change frameworks:). Per the routing table: find-bottlenecks.md -> connects_to_spine: true, sensor_triggers: [SENS-02], reach_id: context_block, sub_mode: reverse-salient, framework: "Reverse Salient Analysis", posture: pull_back, hierarchy_rank: 2, filing: fileEvidenceWithReadback, plan_gated: false, web_scope: null, surface: F.0. find-connections.md -> sensor_triggers: [SENS-01], reach_id: brain_consult, sub_mode: cross-domain-connect, framework: "Usher's Model of Cumulative Synthesis", posture: hold, hierarchy_rank: 1, filing: fileEvidenceWithReadback, surface: F.1. find-analogies.md -> sensor_triggers: [SENS-01], reach_id: context_block, sub_mode: cross-domain-analogy, framework: "Four Lenses of Innovation", posture: hold, hierarchy_rank: 1, filing: fileEvidenceWithReadback, surface: F.1. score-innovation.md -> sensor_triggers: [SENS-06], reach_id: context_block, sub_mode: hsi, framework: "HSI Semantic Surprise Analysis Assistant", posture: hold, hierarchy_rank: 3, filing: fileEvidenceWithReadback, surface: F.1. whitespace.md -> sensor_triggers: [SENS-06], reach_id: context_block, sub_mode: whitespace, framework: "HSI Semantic Surprise Analysis Assistant", posture: hold, hierarchy_rank: 3, filing: fileEvidenceWithReadback, surface: F.1. think-hats.md -> sensor_triggers: [SENS-05], reach_id: brain_consult, sub_mode: six-hats, framework: "Six Thinking Hats", posture: hold, hierarchy_rank: 4, filing: memory_event_only (OPEN-3: six-hats SURFACES perspectives, no EvidenceClaim), surface: F.1. research.md -> sensor_triggers: [SENS-04], reach_id: deep_research, sub_mode: hat-scoped-research, framework: "Hypothesis-Driven Problem Solving", posture: hold, hierarchy_rank: 6, filing: fileEvidenceWithReadback, plan_gated: true (the sanctioned deep_research exception), web_scope: green, surface: F.1. Note the OPEN-3 split: fileEvidenceWithReadback for evidence-producing families (research, find-connections, find-analogies, score-innovation, whitespace, find-bottlenecks); memory_event_only for the surface-only family (think-hats). Keep find-bottlenecks at fileEvidenceWithReadback per routing table (it produces a reverse-salient finding). Resolve the (sensor, reach, sub_mode) uniqueness: score-innovation and whitespace share SENS-06 + context_block but differ in sub_mode (hsi vs whitespace) - that is a legal distinct tuple. NO em-dashes. Regenerate the registry: node scripts/build-connector-registry.cjs.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files carry a connector: block: for f in find-bottlenecks find-connections find-analogies score-innovation whitespace think-hats research; do grep -q "connector:" commands/$f.md || exit 1; done
    - The registry contains all 5 frozen reach_ids represented by the cohort plus the populated sensor_index: node -e "const r=require('./data/connector-registry.json'); const ks=Object.keys(r.sensor_index||{}); if(ks.length<5)process.exit(1)" exits 0
    - connector.framework equals the existing frameworks: value (no drift): node -e "const r=require('./data/connector-registry.json'); const c=(r.connectors||[]).find(x=>String(x.command||'').includes('find-bottlenecks')); if(!c||c.framework!=='Reverse Salient Analysis')process.exit(1)" exits 0 (the registry array key is `connectors` per Plan 01 line 86 - reference it directly so a silently-empty find() cannot trivially pass the negative)
    - think-hats uses memory_event_only (OPEN-3): grep -A12 "connector:" commands/think-hats.md | grep -q "memory_event_only" || exit 1
    - research declares plan_gated: true + web_scope: green (the deep_research exception): grep -A12 "connector:" commands/research.md | grep -q "plan_gated: true" && grep -A12 "connector:" commands/research.md | grep -q "web_scope: green"
    - node scripts/build-connector-registry.cjs --check exits 0
    - No em-dashes added: grep -c "—" commands/find-bottlenecks.md commands/research.md returns 0 for each
  </acceptance_criteria>
  <done>The 7 cohort commands declare additive connector: blocks reusing their Phase 122 framework names; the registry is regenerated and populated with the connectors + sensor_index; OPEN-3 filing-per-family and the deep_research plan-gate are encoded; --check exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Extend --check with the four CONN-03 validations + the WARN nudge</name>
  <read_first>
    scripts/build-connector-registry.cjs (the generator from Plan 01 - the --check branch + buildRegistry inverse-index build to extend);
    scripts/build-command-registry.cjs lines 347-390 (the --check byte-compare + per-row errs.push idiom), lines 367-382 (the missingTeaching error pattern to mirror for the four validations), lines 406-417 (the missingServesJtbd WARNING - stderr, NOT exit 1 - the WARN-nudge pattern);
    lib/core/sensors/sensor-types.cjs lines 38-44 (REACH_IDS) + 48-52 (POSTURE_IDS) - require these for validation;
    lib/workflow/command-resolver.cjs lines 85-90 (commandsForFramework - the resolver door the tripwire asserts resolution through)
  </read_first>
  <action>
    In scripts/build-connector-registry.cjs --check branch, add the four CONN-03 validations as errs.push checks (mirroring the missingTeaching error at build-command-registry.cjs lines 367-382), iterating the built connectors: (1) every connector.reach_id is in REACH_IDS (required from lib/core/sensors/sensor-types.cjs) else push "connector <command> reach_id <id> not in frozen 5"; (2) every connector.framework resolves: require lib/workflow/command-resolver.cjs and assert commandsForFramework(connector.framework).length > 0 for connectors that declare a filing/surface, else push "connector <command> framework <name> does not resolve via commandsForFramework (WFL-01)"; (3) every connector.posture is in POSTURE_IDS else push "connector <command> posture <p> not in frozen 3"; (4) no duplicate (sensor, reach, sub_mode) tuple - build a Set over the tuple while walking (mirror the inverse-index build), push "duplicate connector tuple (sensor,reach,sub_mode)" on collision. exit(1) on any errs. Add the WARN nudge (stderr, NOT exit 1, mirror missingServesJtbd at lines 406-417): when a commands/*.md file matches a methodology heuristic (has a frameworks: block) but lacks a connector: block, print "WARNING: <command> ships a methodology framework but has no connector: block (opt-in nudge)". Keep the build-time WFL-01 has() guard from Plan 01 (it complements the resolver-resolution check). NO em-dashes.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The four validations are present by name: grep -c -E "frozen 5|frozen 3|commandsForFramework|duplicate connector tuple" scripts/build-connector-registry.cjs returns >= 4
    - REACH_IDS and POSTURE_IDS are required from the frozen source: grep -q "sensor-types" scripts/build-connector-registry.cjs
    - The resolver is required for the WFL-01 resolution check: grep -q "command-resolver" scripts/build-connector-registry.cjs
    - The WARN nudge is stderr (not a hard fail): grep -i "WARNING" scripts/build-connector-registry.cjs returns a match
    - A deliberately bad reach_id fails --check: introduce a temp connector with reach_id: bogus_reach in a scratch fixture or via a unit assertion in test-connector-tripwire.cjs (Task 3) and confirm --check exits 1 on it; the clean tree exits 0: node scripts/build-connector-registry.cjs --check exits 0
    - No em-dashes: grep -c "—" scripts/build-connector-registry.cjs returns 0
  </acceptance_criteria>
  <done>--check now enforces the four CONN-03 validations (frozen-5 reach, resolver-resolution framework, frozen-3 posture, no tuple collision) and emits the WARN nudge for methodology commands missing a connector block; the clean tree exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the pre-commit tripwire (tracked + installed) and the CONN-03 test</name>
  <read_first>
    scripts/hooks/pre-commit lines 132-145 (the Phase 122 staged-file-trigger block for command-registry - the pattern to mirror; this is the tracked source-of-record);
    .git/hooks/pre-commit lines 132-145 (the installed copy that actually runs - must get the same trigger);
    .planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-PATTERNS.md lines 98-104 (the exact staged-file grep trigger for the connector registry);
    tests/test-command-registry.cjs (the test-entrypoint pattern);
    lib/core/sensors/sensor-types.cjs lines 38-52 (the frozen banks the test asserts against)
  </read_first>
  <action>
    Add a staged-file trigger to BOTH scripts/hooks/pre-commit (tracked) and .git/hooks/pre-commit (installed) mirroring the Phase 122 command-registry trigger: when git diff --cached --name-only matches commands/.*\.md, skills/.*/SKILL\.md, data/connector-registry\.json, or data/framework-names\.json, run node scripts/build-connector-registry.cjs --check and on non-zero echo "connector-registry drift -- run: node scripts/build-connector-registry.cjs" to stderr and exit 2. Place it adjacent to the existing command-registry trigger so both run. Create tests/test-connector-tripwire.cjs: programmatically construct an in-memory bad-connector case (a connector with reach_id not in REACH_IDS, a connector with posture not in POSTURE_IDS, a connector with an unresolvable framework, and a duplicate (sensor,reach,sub_mode) tuple) and assert the generator's validation logic flags each (either by invoking the exported buildRegistry/validation helpers, or by spawning node scripts/build-connector-registry.cjs --check against a temp fixture dir and asserting exit 1); assert the CLEAN tree (the real commands/) passes --check with exit 0. Print PASS/FAIL, exit non-zero on any failure. NO em-dashes.
  </action>
  <verify>
    <automated>node tests/test-connector-tripwire.cjs</automated>
  </verify>
  <acceptance_criteria>
    - The tracked hook carries the trigger: grep -c "build-connector-registry.cjs --check" scripts/hooks/pre-commit returns >= 1
    - The installed hook carries the trigger: grep -c "build-connector-registry.cjs --check" .git/hooks/pre-commit returns >= 1
    - The trigger fires on the right staged paths: grep -E "skills/.*SKILL|connector-registry" scripts/hooks/pre-commit returns a match
    - node tests/test-connector-tripwire.cjs exits 0 (clean tree passes, all four bad cases are flagged)
    - No em-dashes: grep -c "—" scripts/hooks/pre-commit tests/test-connector-tripwire.cjs returns 0 for each
  </acceptance_criteria>
  <done>Both pre-commit copies run --check on staged connector-relevant changes (drift cannot be committed) and tests/test-connector-tripwire.cjs proves the clean tree passes while each of the four bad-connector cases is flagged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connector frontmatter -> registry | A retrofitted connector's framework/web_scope is a generic handle; must never carry user content |
| staged commit -> registry | The pre-commit tripwire is the structural enforcement that drift (incl. an unscoped framework) cannot land |
| --check resolver call | commandsForFramework() makes zero Brain calls (command-resolver.cjs lines 21-26); the tripwire stays LOCAL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.3-05 | Tampering | data/connector-registry.json hand-edit | mitigate | pre-commit --check byte-compare on both hook copies; a hand-edited registry fails the staged-file trigger and exit 2 blocks the commit |
| T-143.3-06 | Elevation of Privilege | a connector declaring a 6th reach_id or new posture | mitigate | the frozen-5 / frozen-3 validations require REACH_IDS/POSTURE_IDS from sensor-types.cjs; any out-of-bank value fails --check (proven by test-connector-tripwire.cjs) |
| T-143.3-07 | Information Disclosure | connector.web_scope on research.md | accept | web_scope is a frozen hat enum (green) per Part 2 hat-scoping; it selects a hat-scoped tool set, carries no user content; the actual web call is hat-scoped at runtime by hatScopeFor (Plan 03) |
| T-143.3-08 | Spoofing | a connector.framework that does not resolve | mitigate | the WFL-01 resolver-resolution validation (commandsForFramework().length > 0) fails --check, so a fake/typo framework cannot enter the registry |
| T-143.3-SC | Tampering | npm/pip/cargo installs | mitigate | NONE - zero dependencies added (frontmatter edits + CJS + bash hook only); no install task exists |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check exits 0 (populated registry in sync)
- node tests/test-connector-tripwire.cjs exits 0 (four validations + clean-tree pass)
- Both pre-commit copies carry the connector-registry --check trigger
- All 7 cohort commands carry a connector: block whose framework equals their frameworks: value
- grep -c "—" across modified files returns 0 (no em-dashes)
</verification>

<success_criteria>
- CONN-04: the 7 algorithmic-cohort commands declare connector: frontmatter and auto-join the spine (registry populated); the incremental-retrofit policy for the ~70 others is documented (in docs/CONNECTOR-CONTRACT.md from Plan 01 and the SUMMARY)
- CONN-03: the four validations + the WARN nudge + the pre-commit guard make connector drift impossible to commit
</success_criteria>

<output>
Create `.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-02-SUMMARY.md` when done
</output>
