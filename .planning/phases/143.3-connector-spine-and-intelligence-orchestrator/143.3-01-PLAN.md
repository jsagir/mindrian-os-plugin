---
phase: 143.3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CONNECTOR-CONTRACT.md
  - scripts/build-connector-registry.cjs
  - data/connector-registry.json
  - data/dispatch-framework-map.json
  - tests/test-connector-registry.cjs
  - lib/memory/connector-registry.test.cjs
  - tests/test-dispatch-framework-map-drift.cjs
autonomous: true
requirements: [CONN-01, CONN-02]
canon_parts: [Part 7, Part 8]
must_haves:
  truths:
    - "A connector: frontmatter schema is documented (CONN-01) so any skill/command can declare how it plugs into the spine"
    - "scripts/build-connector-registry.cjs walks commands/*.md AND skills/*/SKILL.md, parses the NESTED connector: map, and emits data/connector-registry.json (CONN-02)"
    - "The generator's --check mode regenerates in memory and exits non-zero on stale registry (byte-compare) or unresolved framework"
    - "The raw dispatch handle 'mos:research' translates to the EXACT framework name 'Hypothesis-Driven Problem Solving' via data/dispatch-framework-map.json (OPEN-1), never passes the slug through"
    - "Every framework value in the dispatch map resolves to a name present in data/framework-names.json (drift-tested)"
  artifacts:
    - path: "docs/CONNECTOR-CONTRACT.md"
      provides: "The connector: frontmatter schema reference (11 sub-keys, frozen reach_ids/postures, additive-degrade rule)"
      contains: "connector:"
    - path: "scripts/build-connector-registry.cjs"
      provides: "The generator with the nested-map parser extension + --check tripwire + Part-8 read-only allowlist reuse"
      exports: ["buildRegistry", "serializeRegistry", "parseConnectorFrontmatter"]
    - path: "data/connector-registry.json"
      provides: "The generated registry the orchestrator reads at runtime"
      contains: "generated_note"
    - path: "data/dispatch-framework-map.json"
      provides: "The OPEN-1 dispatch-handle -> EXACT framework-name map"
      contains: "mos:research"
  key_links:
    - from: "scripts/build-connector-registry.cjs"
      to: "data/framework-names.json"
      via: "build-time allowlist reuse (no new Brain query)"
      pattern: "framework-names"
    - from: "data/dispatch-framework-map.json"
      to: "data/framework-names.json"
      via: "drift test asserts every mapped name exists in the allowlist"
      pattern: "framework-names"
---

<objective>
Ship the foundation of the Connector Contract: the connector: frontmatter SCHEMA (CONN-01), the GENERATOR that compiles every surface's connector frontmatter into data/connector-registry.json (CONN-02), and the OPEN-1 dispatch-handle -> EXACT-framework-name map that the orchestrator consumes. This plan delivers the spine's source-of-truth contract and the build tool; Plan 02 retrofits the cohort and wires the CI tripwire; Plan 03 ships the orchestrator that reads the registry.

Purpose: Phase 122 made framework -> command reliable and self-extending via a generated registry. This generalizes that pattern to the ENTIRE reach-wiring so any skill/command joins the spine by declaring, never by editing the orchestrator (the moat made self-extending, CLAUDE.md MWP mandate).

Output: docs/CONNECTOR-CONTRACT.md, scripts/build-connector-registry.cjs, data/connector-registry.json, data/dispatch-framework-map.json, and their drift tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-CONTEXT.md
@.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/CONNECTOR-CONTRACT-spine.md
@.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-PATTERNS.md
@.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/ROUTING-TABLE-intelligence-orchestrator.md
</context>

<artifacts_this_phase_produces>
NEW files (excluded from drift verification - they did not exist before this phase):
- docs/CONNECTOR-CONTRACT.md (the schema reference doc)
- scripts/build-connector-registry.cjs (the generator)
- data/connector-registry.json (the generated registry)
- data/dispatch-framework-map.json (the OPEN-1 map)
- tests/test-connector-registry.cjs (bash-runner entrypoint)
- lib/memory/connector-registry.test.cjs (the shared test implementation)
- tests/test-dispatch-framework-map-drift.cjs (the OPEN-1 drift test)

NEW frontmatter keys (the connector: block + its 11 sub-keys): connects_to_spine, sensor_triggers, reach_id, sub_mode, framework, posture, hierarchy_rank, filing, plan_gated, web_scope, surface

NEW generator exports: buildRegistry, serializeRegistry, parseConnectorFrontmatter (the nested-map parser extension)

NEW registry indexes: sensor_index (sensor_id -> connectors[]), connector_index (reusing the framework_index convention)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Document the connector: frontmatter schema (CONN-01)</name>
  <read_first>
    docs/COMMAND-FRONTMATTER.md (the Phase 122 schema-doc analog, 111 lines - mirror its layout and tone);
    .planning/phases/143.3-connector-spine-and-intelligence-orchestrator/CONNECTOR-CONTRACT-spine.md lines 20-42 (the schema block + the four pieces);
    lib/core/sensors/sensor-types.cjs lines 38-52 (REACH_IDS frozen 5 + POSTURE_IDS frozen 3 - the canonical constants the doc must cite verbatim);
    .planning/phases/143.3-connector-spine-and-intelligence-orchestrator/ROUTING-TABLE-intelligence-orchestrator.md lines 26-33 (the per-family connector values the doc uses as examples)
  </read_first>
  <action>
    Create docs/CONNECTOR-CONTRACT.md mirroring docs/COMMAND-FRONTMATTER.md. Document the connector: block and its 11 sub-keys exactly: connects_to_spine (boolean, opt-in; absent/false = legacy/not wired), sensor_triggers (array of SENS ids, 0..n), reach_id (one of the frozen 5: context_block, contradiction, cross_room, brain_consult, deep_research - NEVER a 6th), sub_mode (free render label registered under that reach_id), framework (EXACT name matching data/framework-names.json - the resolver key, MUST equal the surface's existing frameworks: value), posture (one of the frozen 3: push_forward, hold, pull_back), hierarchy_rank (integer, the Intelligence Hierarchy position for one-reach-per-beat arbitration), filing (one of: fileEvidenceWithReadback, memory_event_only, none), plan_gated (boolean, true ONLY for the deep_research escalation), web_scope (null or one of white|green|black|yellow|red|blue, hat-scoped per Part 2), surface (the Shape-F sub-shape the Decision Gate renders as, e.g. F.0, F.1). State the additive-degrade rule: each surface declares ONLY relevant parts; absent fields mean the surface does not participate in that hook. State the frozen-bank rule: reach_id and posture stay frozen (drift-tested); sub_mode carries the intelligence-tool identity as a render label, NEVER a new reach_id. State the Part 8 boundary: framework and web_scope are GENERIC handles only - a connector never carries user content; the only Brain touch in this whole spine is the generator's build-time read-only --refresh-names allowlist read. Cite the routing-table 6-family rows as worked examples. NO em-dashes (use hyphens). Add a "# --- Phase 143.3 connector frontmatter ---" delimiter convention note matching the Phase 122 "# --- Phase 122 workflow-layer frontmatter ---" pattern (find-bottlenecks.md line 6).
  </action>
  <acceptance_criteria>
    - docs/CONNECTOR-CONTRACT.md exists and documents all 11 sub-keys by name: grep -c -E "connects_to_spine|sensor_triggers|reach_id|sub_mode|framework|posture|hierarchy_rank|filing|plan_gated|web_scope|surface" docs/CONNECTOR-CONTRACT.md returns >= 11
    - The frozen 5 reach_ids appear verbatim: grep -c -E "context_block|contradiction|cross_room|brain_consult|deep_research" docs/CONNECTOR-CONTRACT.md returns >= 5
    - The frozen 3 postures appear verbatim: grep -E "push_forward|hold|pull_back" docs/CONNECTOR-CONTRACT.md returns matches
    - The Part 8 boundary is stated: grep -i "Part 8\|generic handle\|never.*user content" docs/CONNECTOR-CONTRACT.md returns a match
    - No em-dashes: grep -c "—" docs/CONNECTOR-CONTRACT.md returns 0
  </acceptance_criteria>
  <done>docs/CONNECTOR-CONTRACT.md documents the full additive connector schema, the frozen banks, the degrade rule, and the Part 8 boundary, with the 6 routing-table families as examples and zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: Build the connector-registry generator with the nested-map parser extension (CONN-02)</name>
  <read_first>
    scripts/build-command-registry.cjs lines 1-51 (header doc + COMMANDS_DIR/REGISTRY_PATH constants), 100-137 (the flat hand-rolled parseFrontmatter - the parser to EXTEND for the nested connector: map; note line 132 comment "deeper indentation is ignored" - that is exactly what must change), 162-265 (curated-extras read + buildRegistry walk + serializeRegistry), 268-424 (--refresh-names Brain branch + main() three-branch structure + --check byte-compare);
    scripts/build-command-registry.cjs line 222 (the fwNames.has(fw) WFL-01 unresolved-push guard to mirror);
    data/command-registry.json lines 1-19 (top-level shape: ontology_ref, generated_note, commands[], framework_index, curated_chains - mirror this layout);
    lib/core/sensors/sensor-types.cjs lines 38-44 (REACH_IDS) and 48-52 (POSTURE_IDS) for the validation banks;
    lib/workflow/command-resolver.cjs lines 85-90 (commandsForFramework - the resolver door);
    data/framework-names.json lines 1-30 (the FEEDS_INTO allowlist to REUSE - no new Brain query)
  </read_first>
  <action>
    Copy scripts/build-command-registry.cjs as the skeleton for scripts/build-connector-registry.cjs. Keep the header-doc Part-8 note (the ONLY Brain touch is the read-only --refresh-names allowlist read; the generator and --check never load brain-client). Add a SKILLS_DIR constant alongside COMMANDS_DIR and walk BOTH commands/*.md and skills/*/SKILL.md (sorted, deterministic). Add parseConnectorFrontmatter: extend the flat parser at lines 100-137 to DESCEND one indent level into the connector: nested map - when a top-level key is "connector" with an empty value followed by indented "  key: value" lines, accumulate them into out.connector as an object (handle key:value, JSON-array key: [..], indented dash-lists for sensor_triggers, null, booleans, integers, quote-stripping - same value coercions the flat parser already does). This nested descent is the ONE genuine net-new parsing concern flagged in PATTERNS.md. In buildRegistry: filter to surfaces where connector.connects_to_spine === true (absent/false = legacy, skipped). For each wired surface push a connector entry carrying the 11 sub-keys; build a sensor_index (each sensor_id in sensor_triggers -> push this connector) AND a framework_index (framework -> [surface] mirroring the Phase 122 inverse index). Apply the WFL-01 build-time guard: if connector.framework is not in the framework-names allowlist (fwNames.has), push to _unresolved (mirror line 222). serializeRegistry strips _unresolved and emits JSON.stringify(clean, null, 2) + newline (copy line 256-265 verbatim so byte-compare is stable). Keep the generated_note first-line guard. main() keeps the three branches: --refresh-names (reuse the SHIPPED data/framework-names.json - do NOT add a new Brain query; if a refresh is needed it reads the same FEEDS_INTO query as Phase 122), --check (regenerate in memory, byte-compare on-disk, exit 1 on stale OR unresolved), default (write, refuse on unresolved). Run the generator once to emit data/connector-registry.json (it will be near-empty until Plan 02 retrofits the cohort - that is expected; the registry has the top-level shape + empty/partial arrays). Export buildRegistry, serializeRegistry, parseConnectorFrontmatter for the test. NO em-dashes.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - node scripts/build-connector-registry.cjs --check exits 0 (registry in sync with frontmatter)
    - The generator walks BOTH source dirs: grep -c "SKILLS_DIR\|skills" scripts/build-connector-registry.cjs returns >= 1 and grep -c "COMMANDS_DIR\|commands" returns >= 1
    - The nested parser exists and is exported: grep -c "parseConnectorFrontmatter" scripts/build-connector-registry.cjs returns >= 2
    - The registry has the canonical shape: node -e "const r=require('./data/connector-registry.json'); if(!r.generated_note||!r.sensor_index||!('framework_index' in r)){process.exit(1)}" exits 0
    - Part 8: the generator never top-level-requires brain-client: grep -nv '^[[:space:]]*//' scripts/build-connector-registry.cjs | grep -c "require.*brain-client" returns 0 OR brain-client require appears ONLY inside the --refresh-names branch (mirror build-command-registry.cjs lines 272-274)
    - No em-dashes: grep -c "—" scripts/build-connector-registry.cjs returns 0
  </acceptance_criteria>
  <done>scripts/build-connector-registry.cjs mirrors the Phase 122 generator, extends the parser to descend the nested connector: map, walks commands + skills, emits data/connector-registry.json with sensor_index + framework_index, applies the WFL-01 build-time guard, and --check exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Ship the OPEN-1 dispatch-handle map + its drift test, and the registry test entrypoint</name>
  <read_first>
    .planning/phases/143.3-connector-spine-and-intelligence-orchestrator/ROUTING-TABLE-intelligence-orchestrator.md lines 26-33 (the dispatch handle -> framework column - the source of the map) and line 44 (OPEN-1: the raw 'mos:research' handle MUST translate to "Hypothesis-Driven Problem Solving");
    .planning/phases/143.3-connector-spine-and-intelligence-orchestrator/RESEARCH-HATS-WIRING.md line 24 (SENS-04 emits dispatch 'mos:research (hat-scoped WebSearch)' - the slug smell WFL-01 flags);
    data/framework-names.json lines 1-118 (the drift target - every mapped name MUST exist here);
    tests/test-command-registry.cjs (the 1-line bash-runner entrypoint pattern that requires the lib implementation);
    tests/test-reach-ids-drift.cjs (the drift-test idiom for asserting a data file matches a frozen source)
  </read_first>
  <action>
    Create data/dispatch-framework-map.json: a flat JSON object mapping each raw sensor dispatch handle / sub_mode to its EXACT framework name. Seed it from the routing-table column 3->6 mapping: "mos:research" and "mos:research (hat-scoped WebSearch)" -> "Hypothesis-Driven Problem Solving"; "find-bottlenecks" / "reverse-salient" -> "Reverse Salient Analysis"; "find-connections" / "cross-domain-connect" -> "Usher's Model of Cumulative Synthesis"; "find-analogies" / "cross-domain-analogy" -> "Four Lenses of Innovation"; "hsi" / "whitespace" / "score-innovation" -> "HSI Semantic Surprise Analysis Assistant"; "think-hats" / "six-hats" / "team_perspective" -> "Six Thinking Hats". Add a generated_note-style "_note" first key stating it is the OPEN-1 WFL-01 translation layer and is drift-tested against data/framework-names.json. NO em-dashes. Create tests/test-dispatch-framework-map-drift.cjs: load data/dispatch-framework-map.json + data/framework-names.json, assert EVERY mapped value (skip the _note key) is present in the framework-names allowlist, assert NO value is a slug (no value starts with "mos:" or contains "/mos:"), assert "mos:research" maps to exactly "Hypothesis-Driven Problem Solving". Print PASS/FAIL and exit non-zero on failure. Create lib/memory/connector-registry.test.cjs (the shared implementation: asserts node scripts/build-connector-registry.cjs --check passes, asserts the registry has sensor_index + framework_index + generated_note, asserts every connector reach_id is in REACH_IDS and posture in POSTURE_IDS by requiring lib/core/sensors/sensor-types.cjs). Create tests/test-connector-registry.cjs as the 1-line require('../lib/memory/connector-registry.test.cjs') entrypoint (mirror tests/test-command-registry.cjs) so the bash runner and Feynman runner share one implementation.
  </action>
  <verify>
    <automated>node tests/test-dispatch-framework-map-drift.cjs && node tests/test-connector-registry.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-dispatch-framework-map-drift.cjs exits 0
    - "mos:research" resolves to the exact framework name: node -e "const m=require('./data/dispatch-framework-map.json'); if(m['mos:research']!=='Hypothesis-Driven Problem Solving'){process.exit(1)}" exits 0
    - No slug leaks through the map: node -e "const m=require('./data/dispatch-framework-map.json'); for(const k of Object.keys(m)){if(k==='_note')continue; if(String(m[k]).startsWith('mos:')||String(m[k]).includes('/mos:'))process.exit(1)}" exits 0
    - Every mapped name exists in the allowlist: node -e "const m=require('./data/dispatch-framework-map.json'),a=require('./data/framework-names.json'); const names=Array.isArray(a)?a:(a.framework_names||[]).concat(a.curated_extras||[]); for(const k of Object.keys(m)){if(k==='_note')continue; if(!names.includes(m[k]))process.exit(1)}" exits 0 (read names from the explicit framework_names + curated_extras keys, NOT Object.values().flat() - the catch-all would accept a name smuggled in as the snapshot_note/date prose, a latent Part-8 hole)
    - node tests/test-connector-registry.cjs exits 0
    - No em-dashes: grep -c "—" data/dispatch-framework-map.json tests/test-dispatch-framework-map-drift.cjs returns 0 for each
  </acceptance_criteria>
  <done>data/dispatch-framework-map.json translates every raw dispatch handle to an EXACT framework name present in framework-names.json (never a slug), the drift test enforces it, and the connector-registry test entrypoint asserts --check + frozen-bank conformance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator build-time -> Brain | The --refresh-names branch is the ONLY Brain touch in the whole spine; it must stay read-only and carry no local strings |
| connector frontmatter -> registry | A connector's framework/web_scope fields are generic handles; the registry must never carry user content |
| dispatch map -> resolver | The map is the WFL-01 translation layer; a slug leaking through would break resolution and could carry an unscoped handle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.3-01 | Information Disclosure | scripts/build-connector-registry.cjs --refresh-names | mitigate | brain-client required ONLY inside the --refresh-names branch (mirror build-command-registry.cjs lines 272-274); the query reuses the shipped FEEDS_INTO read-only :Framework name read; no local strings in the payload; verified by the grep gate in Task 2 acceptance |
| T-143.3-02 | Tampering | data/connector-registry.json | mitigate | generated_note first-line guard + --check byte-compare in pre-commit (Plan 02 wires the hook); hand-edits are rejected before commit |
| T-143.3-03 | Spoofing | data/dispatch-framework-map.json | mitigate | drift test asserts every value is a real framework-names.json entry and NO value is a /mos: slug, so a smuggled slug or fake framework fails CI |
| T-143.3-04 | Information Disclosure | connector frontmatter framework field | accept | framework names are generic published-methodology handles (Part 8 allows generic framework handles to Brain); no user content can appear in a frozen-vocabulary enum field |
| T-143.3-SC | Tampering | npm/pip/cargo installs | mitigate | NONE - this plan adds zero dependencies (pure Node CJS + existing data files per CLAUDE.md no-new-deps rule); no install task exists |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check exits 0
- node tests/test-connector-registry.cjs exits 0
- node tests/test-dispatch-framework-map-drift.cjs exits 0
- grep -c "—" docs/CONNECTOR-CONTRACT.md scripts/build-connector-registry.cjs data/dispatch-framework-map.json returns 0 for each (no em-dashes)
- The generator never top-level-requires brain-client (Part 8): brain-client appears only inside the --refresh-names branch
</verification>

<success_criteria>
- CONN-01: the connector: schema is documented with all 11 sub-keys, frozen banks, degrade rule, Part 8 boundary
- CONN-02: the generator walks commands + skills, parses the nested connector: map, emits data/connector-registry.json with sensor_index + framework_index, and --check is a working tripwire
- OPEN-1: the dispatch-framework map translates raw handles (incl. mos:research) to EXACT framework names, drift-tested against framework-names.json, never passing a slug
</success_criteria>

<output>
Create `.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-01-SUMMARY.md` when done
</output>
