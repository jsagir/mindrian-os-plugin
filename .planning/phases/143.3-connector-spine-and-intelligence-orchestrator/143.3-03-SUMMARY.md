---
phase: 143.3
plan: 03
subsystem: intelligence-orchestrator
tags: [connector-spine, intelligence-orchestrator, orch-01, orch-02, orch-03, orch-04, wfl-01, part-3, part-4, part-8, part-9, reach-dispatcher]
requires:
  - data/connector-registry.json (Plan 02: 7 connectors + 5-key sensor_index - the registry this skill reads)
  - data/dispatch-framework-map.json (Plan 01: the OPEN-1 slug -> EXACT framework-name map)
  - lib/core/insight-sensors.cjs::dispatchSensors (Phase 143: the sensor spine being consumed)
  - lib/workflow/command-resolver.cjs::commandsForFramework (Phase 122: the WFL-01 resolver door)
  - lib/core/navigation/file-evidence-readback.cjs (fileEvidenceWithReadback + surfaceFileEvidenceResult)
  - lib/core/findings-wirer.cjs::wireAccept (the filing fallback)
  - lib/agents/reverse-salient-agent.cjs::surfaceFinding (the PUSH-02 F.0 render)
  - lib/core/sensors/hat-scoping-table.cjs::hatScopeFor (PUSH-06 hat-scoped web)
provides:
  - skills/intelligence-orchestrator/SKILL.md (the FIRST consumer of the dispatchSensors spine and the FIRST reader of data/connector-registry.json)
affects:
  - Phase 144 (engine-side decide() consumer - coexists with this prompt-side consumer via one-reach-per-beat)
  - room-proactive (cascade-finding surface - arbitrated by the same Intelligence Hierarchy)
tech-stack:
  added: []
  patterns:
    - registry-reading reach dispatcher (sensor_index lookup, never a hardcoded routing table)
    - dispatch-framework-map slug-to-name translation before the WFL-01 resolver door
    - one-reach-per-beat arbitration via the Intelligence Hierarchy across multiple consumers
    - LOCAL side-channel coexistence read (last-cascade.json) mirroring room-proactive, Part 8 clean
key-files:
  created:
    - skills/intelligence-orchestrator/SKILL.md
  modified: []
decisions:
  - The registry field for the Shape-F gate is decision_surface (not surface); the skill renders the connector's decision_surface as the Decision Gate.
  - OPEN-5 resolved by READING room-proactive's last-cascade.json into the same hierarchy ranking (single ranked pool, one-reach-per-beat across both skills), LOCAL only per Part 8.
  - OPEN-4 resolved as a render choice: the rs-agent [BRAIN] chip sits over a LOCAL folder-memory.readQuadruple, no Brain call, no contradiction with the context_block LOCAL reach.
  - Tier predicate (OPEN-2): live dispatchSensors authoritative at mode_a/mode_b, degrade to doctrine-sim at tier_0; the skill is safe before Phase 144 lands.
metrics:
  duration: ~7 min (single sequential session)
  completed: 2026-06-07
  tasks: 3
  files: 1
---

# Phase 143.3 Plan 03: Intelligence Orchestrator Summary

One-liner: skills/intelligence-orchestrator/SKILL.md ships as the FIRST production consumer of the dispatchSensors spine and the FIRST reader of data/connector-registry.json -- a registry-reading reach dispatcher (never a hardcoded table) that runs the 5-step loop (LIVE tier-gated read -> dispatch-map translation -> Intelligence-Hierarchy gate -> Shape-F offer never-fire -> resolver-fire on APPROVE), files via fileEvidenceWithReadback (fallback wireAccept) + surfaceFileEvidenceResult, and resolves OPEN-4/OPEN-5 plus the tier-gate coexistence with Phase 144 and room-proactive.

## What shipped

The Phase-143 sensor spine had zero production consumers: every SENS-0x fired into the void. This plan closes that gap with a single conversational-doctrine skill (markdown, so it works across CLI / Desktop / Cowork with no new UI). It is roughly 90% wiring (Canon Part 7): it CONSUMES six shipped exports and reads the two generated data files from Plans 01/02; it builds no new engine.

### Task 1 -- Frontmatter + REGISTRY-IS-THE-TABLE dispatcher (ORCH-01) [commit c14bcdad]
Created skills/intelligence-orchestrator/SKILL.md with the consumer frontmatter mirroring room-proactive YAML style: name, folded description, canon_parts [Part 2, Part 3, Part 4, Part 8, Part 9], phase 143.3, consumes (dispatchSensors), resolver, registry, dispatch_map, the frozen-5 reach_ids, the frozen-3 posture_ids, filing, live_call (tier-gated, degrade to doctrine-sim at tier_0), and allowed-tools. Body opens with the REGISTRY-IS-THE-TABLE doctrine: on a fired sensor the orchestrator looks the sensor id up in the registry sensor_index, reads the matching connector records, and routes from them -- never a hardcoded table. States the composition rule (hats compose under brain_consult; RS/HSI/whitespace/analogies under context_block/brain_consult as sub_mode render labels; deep-research IS the deep_research reach; NO 6th reach_id), cites the frozen 5 + frozen 3 verbatim, and states the Part 8 boundary up front (generic handles + phase ids + enums only to resolver/Brain/web; turn text and artifact bodies never cross; web hat-scoped, Red = no web).

### Task 2 -- The 5-step core loop + filing + WFL-01 (ORCH-02, ORCH-03) [commit 422ddfa7]
Added "## The 5-step core loop" as directive prose (no fenced code) naming the exact shipped functions: STEP 1 read the spine LIVE via dispatchSensors (tier gate: authoritative at mode_a/mode_b, degrade to doctrine-sim at tier_0; never mutates routing_source, never calls decide() -- the Phase 144 fence); STEP 2 translate each fired reach's dispatch handle / sub_mode to its EXACT framework name via data/dispatch-framework-map.json (mos:research -> "Hypothesis-Driven Problem Solving", never the slug -- the WFL-01 guard); STEP 3 gate to ONE via the Intelligence Hierarchy (Tensions > Bottlenecks > HSI Surprises > Convergences > Blind Spots) then evidence strength, with deep_research the sanctioned plan-gated multi-angle exception (composeWorkflow); STEP 4 offer never fire -- surface the connector's decision_surface as a Shape-F Decision Gate using the room-proactive APPROVE/REJECT/DEFER flow (Part 3); STEP 5 on APPROVE resolve via commandsForFramework(<exact name>) (the only door, degrade to "run X manually" on empty) and fire. Added "## Filing" (ORCH-03): fileEvidenceWithReadback for evidence families (fallback wireAccept on readback error per decision 2), memory_event_only for six-hats, always surfaceFileEvidenceResult (FILEVAL honesty), every decision -> memory_event + typed cascade edge (Part 4 / Part 9). Stated the Part 9 truth-state rule: filed EvidenceClaim lands review_status proposed, never confirmed; only a human gate promotes truth-claim nodes (audit-node carve-out exempts the memory_event/cascade bookkeeping writes).

### Task 3 -- Tier gate + coexistence + OPEN-4/OPEN-5 (ORCH-04) [commit fbd38a6a]
Added "## Tier gate + coexistence": the tier predicate (OPEN-2 / locked decision 1, mode_a/mode_b live, tier_0 degrade); Phase 144 coexistence (engine-side decide() vs prompt-side skill, no double-fire because both honor one-reach-per-beat via the shared Intelligence Hierarchy; the prompt-side never calls decide() or flips routing_source); OPEN-5 (reads room-proactive's last-cascade.json LOCAL side-channel into the SAME hierarchy ranking so one-reach-per-beat holds across both surfaces from one ranked pool, soft-fails if absent, Part 8 clean -- never the Brain); OPEN-4 (the rs-agent [BRAIN] chip is a render choice over a LOCAL folder-memory.readQuadruple, no Brain call, no contradiction with the context_block LOCAL reach, Part-8 check passes). Added "## Tri-Polar surfaces": CLI fires via the sensor hook + Shape-F selector, Desktop surfaces conversationally through Larry, Cowork shows the same SELECTED_REACH gate with a per-actor {topic} label; no new UI on any surface.

## Verification (all pass)

- skills/intelligence-orchestrator/SKILL.md exists, 253 lines (min 120 required), references data/connector-registry.json (4x) and declares it reads the registry not a hardcoded table
- Task 1 ACs: registry ref >= 1 (4), reads-not-hardcoded match, frozen-5 >= 5 (6) + frozen-3 match, Part 8 stated, em-dash 0
- Task 2 ACs: 5 steps named >= 5 (18), WFL-01 slug rule, Intelligence Hierarchy order + one-reach-per-beat, filing 3 surfaces >= 3 (7), Part 9 proposed rule, never-auto-fire, em-dash 0
- Task 3 ACs: tier predicate, 144 coexistence, OPEN-5 last-cascade, OPEN-4 render-choice, CLI + Desktop + Cowork each present, em-dash 0
- `node scripts/build-connector-registry.cjs --check` -> connector-registry: OK (exit 0); the WARN nudge for the 34 not-yet-retrofitted methodology commands is the documented opt-in behavior, not a failure
- key-links present: connector-registry, commandsForFramework, fileEvidenceWithReadback all grep-confirmed in the skill body
- All three task commits passed the live pre-commit hooks with no --no-verify

## Deviations from Plan

### Auto-fixed Issues

None affecting content. One field-name reconciliation documented below.

### Acceptance-criterion / field-name reconciliation (not a code deviation)

- The plan and the routing table refer to the connector's Shape-F field as `surface`, but the Plan 01/02 registry (which this plan consumes unchanged) emits the field as `decision_surface` (see data/connector-registry.json). The skill doctrine renders the connector's `decision_surface` as the Decision Gate and names that field explicitly. No registry shape was changed -- the registry shape is the Plan 01/02 contract; the doctrine names the field as it actually ships.

## Known Stubs

None. This skill is conversational doctrine (markdown) that consumes already-shipped exports and the populated registry; there are no hardcoded empty values, placeholders, or unwired data sources. The 34-command WARN nudge from the connector --check is the documented incremental-retrofit policy (the algorithmic cohort of 7 is wired; the rest opt in as touched), not a stub introduced by this plan.

## Threat surface

No new threat surface beyond the plan's `<threat_model>`. The three mitigate-disposition Information-Disclosure threats are addressed by doctrine stated in the skill: T-143.3-09 (commandsForFramework gets only the EXACT framework name via the dispatch map, never turn text / artifact bodies), T-143.3-10 (the deep_research / web call is hat-scoped via hatScopeFor, Red = web_enabled:false, generic topic handle only, plan_gated), T-143.3-11 (every Brain query carries generic handles + phase ids + enums only, stated up front in Task 1 and reaffirmed in the loop/filing sections). The two accept-disposition threats hold by reconciliation: T-143.3-12 (the [BRAIN] header is a render choice over a LOCAL readQuadruple) and T-143.3-13 (last-cascade.json is the same LOCAL side-channel room-proactive reads, never the Brain). T-143.3-SC holds: this plan adds a markdown skill only -- zero dependencies, no install task.

## Self-Check: PASSED

Files (all FOUND):
- skills/intelligence-orchestrator/SKILL.md

Commits (all FOUND in git log):
- c14bcdad feat(143.3-03): orchestrator frontmatter + REGISTRY-IS-THE-TABLE dispatcher (ORCH-01)
- 422ddfa7 feat(143.3-03): 5-step core loop + filing + WFL-01 resolution (ORCH-02, ORCH-03)
- fbd38a6a feat(143.3-03): tier gate + coexistence (ORCH-04), OPEN-4/OPEN-5 resolved
