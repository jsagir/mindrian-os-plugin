---
phase: 143.3
plan: 04
type: execute
wave: 4
depends_on: ["143.3-01", "143.3-02", "143.3-03"]
files_modified:
  - tests/run-all-1433.sh
  - tests/test-connector-part8-boundary.cjs
  - tests/test-orchestrator-doctrine-presence.cjs
autonomous: true
requirements: [CONN-01, CONN-02, CONN-03, CONN-04, ORCH-01, ORCH-02, ORCH-03, ORCH-04]
canon_parts: [Part 6, Part 8]
must_haves:
  truths:
    - "tests/run-all-1433.sh composes the connector-registry --check, the dispatch-map drift test, the tripwire test, the Part-8 boundary grep, the orchestrator-doctrine-presence gate, AND the carried reach-drift / posture-drift fences into one PASS/FAIL run"
    - "A Part-8 boundary test proves the connector registry + the generator + the orchestrator doctrine create no user-content-to-Brain path (block on HIGH)"
    - "The orchestrator doctrine-presence gate asserts ORCH-01..04 are all present in skills/intelligence-orchestrator/SKILL.md"
    - "The suite exits non-zero if any constituent fails (mirror tests/run-all-122.sh)"
  artifacts:
    - path: "tests/run-all-1433.sh"
      provides: "The phase verification aggregator composing all connector + orchestrator + carried-fence checks"
      contains: "build-connector-registry"
    - path: "tests/test-connector-part8-boundary.cjs"
      provides: "The Canon Part 8 boundary scan over the connector spine"
      contains: "Part 8"
  key_links:
    - from: "tests/run-all-1433.sh"
      to: "tests/test-reach-ids-drift.cjs + tests/test-posture-ids-drift.cjs"
      via: "carried 143/143.1 drift fences composed into the suite"
      pattern: "reach-ids-drift|posture-ids-drift"
    - from: "tests/run-all-1433.sh"
      to: "scripts/build-connector-registry.cjs --check"
      via: "the CI tripwire run in the suite"
      pattern: "build-connector-registry.*--check"
---

<objective>
Ship tests/run-all-1433.sh: the phase verification aggregator that composes every connector + orchestrator check plus the carried drift fences (reach-drift, posture-drift) and a Part-8 boundary scan into one PASS/FAIL run, mirroring tests/run-all-122.sh and the 143.x run-all pattern. This is the gate that proves the phase is correct and that the constitutional boundary (Part 8) holds across the new spine.

Purpose: A run-all suite (mirror tests/run-all-143*.sh) must compose the reach-drift / posture-drift / Part-8 fences + the new connector-registry --check (hard-fence requirement). This plan is that suite plus the two net-new tests it needs (the Part-8 boundary scan + the orchestrator doctrine-presence gate).

Output: tests/run-all-1433.sh, tests/test-connector-part8-boundary.cjs, tests/test-orchestrator-doctrine-presence.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-CONTEXT.md
@.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-PATTERNS.md
@docs/CONNECTOR-CONTRACT.md
</context>

<artifacts_this_phase_produces>
NEW files (excluded from drift verification):
- tests/run-all-1433.sh (the phase aggregator)
- tests/test-connector-part8-boundary.cjs (the Part 8 boundary scan over the spine)
- tests/test-orchestrator-doctrine-presence.cjs (the ORCH-01..04 presence gate)

This plan COMPOSES (does not re-author) the tests created in Plans 01-03: tests/test-connector-registry.cjs, tests/test-dispatch-framework-map-drift.cjs, tests/test-connector-tripwire.cjs, and the CARRIED fences tests/test-reach-ids-drift.cjs + tests/test-posture-ids-drift.cjs (shipped in Phase 143).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The Part-8 boundary scan over the connector spine</name>
  <read_first>
    tests/test-auto-explore-canon-part-8.cjs (a Canon Part 8 forbidden-substring scan analog - mirror its structure);
    tests/test-decide-part8-invariant.cjs (the per-invariant Part-8 assertion idiom);
    scripts/build-connector-registry.cjs (the generator from Plans 01/02 - assert it does NOT top-level require brain-client; the only Brain touch is inside --refresh-names);
    data/connector-registry.json + data/dispatch-framework-map.json (assert the generated artifacts carry only generic handles + enums - no free-text body fields);
    skills/intelligence-orchestrator/SKILL.md (assert the doctrine forbids turn text / artifact bodies into Brain/web; web is hat-scoped);
    .planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-CONTEXT.md (the threat surface: framework/web_scope smuggling, --refresh-names leak, turn text into commandsForFramework, unscoped WebSearch)
  </read_first>
  <action>
    Create tests/test-connector-part8-boundary.cjs (mirror tests/test-auto-explore-canon-part-8.cjs). Assert the FOUR threat-model paths from the CONTEXT threat_model are closed: (1) the generator scripts/build-connector-registry.cjs has NO top-level require of brain-client - brain-client may appear ONLY inside the --refresh-names branch (parse the file, confirm the require sits below the --refresh-names guard, mirroring build-command-registry.cjs lines 272-274); (2) data/connector-registry.json + data/dispatch-framework-map.json contain ONLY the schema fields (connects_to_spine, sensor_triggers, reach_id, sub_mode, framework, posture, hierarchy_rank, filing, plan_gated, web_scope, surface + the index/note scaffolding) - assert no entry carries a free-text body/summary/content field that could smuggle user content; assert every framework value is a generic name present in data/framework-names.json and every web_scope is null or a frozen hat enum; (3) the orchestrator doctrine forbids turn text / artifact bodies into commandsForFramework or a Brain packet - grep skills/intelligence-orchestrator/SKILL.md for the forbidding statement (generic handles only / never user content / never turn text) and FAIL if absent; (4) the orchestrator never specifies an unscoped WebSearch - assert the doctrine ties web to hatScopeFor (Part 2). Any failure is HIGH severity (a user-content-to-Brain path) and exits non-zero. Print per-check PASS/FAIL. NO em-dashes.
  </action>
  <verify>
    <automated>node tests/test-connector-part8-boundary.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-connector-part8-boundary.cjs exits 0 (all four boundary paths closed)
    - The test asserts the generator's brain-client locality: grep -c "refresh-names\|brain-client" tests/test-connector-part8-boundary.cjs returns >= 1
    - The test scans the orchestrator doctrine for the no-user-content rule: grep -c "intelligence-orchestrator" tests/test-connector-part8-boundary.cjs returns >= 1
    - The test asserts hat-scoped web (no unscoped WebSearch): grep -i "hatScopeFor\|hat-scoped\|web_scope" tests/test-connector-part8-boundary.cjs returns a match
    - No em-dashes: grep -c "—" tests/test-connector-part8-boundary.cjs returns 0
  </acceptance_criteria>
  <done>tests/test-connector-part8-boundary.cjs proves the four Part-8 threat paths (generator Brain locality, registry/map generic-handles-only, orchestrator no-user-content doctrine, hat-scoped web) are all closed; any breach is HIGH and fails the test.</done>
</task>

<task type="auto">
  <name>Task 2: The orchestrator doctrine-presence gate (ORCH-01..04)</name>
  <read_first>
    skills/intelligence-orchestrator/SKILL.md (the doctrine from Plan 03 - the file under test);
    .planning/phases/143.2-.../143.2-04 doctrine-presence-gate pattern (the 143.2 verification plan uses a doctrine-presence gate asserting N requirements are present in prose - mirror that idiom);
    .planning/phases/143.3-connector-spine-and-intelligence-orchestrator/RESEARCH-HATS-WIRING.md lines 43-59 (the frontmatter + consumer-integration the gate checks)
  </read_first>
  <action>
    Create tests/test-orchestrator-doctrine-presence.cjs: load skills/intelligence-orchestrator/SKILL.md and assert each of ORCH-01..04 is present as doctrine. ORCH-01: the file references data/connector-registry.json AND states it reads the registry not a hardcoded table. ORCH-02: the 5-step loop is present - assert mentions of dispatchSensors, the dispatch-framework map / WFL-01 translation, the Intelligence Hierarchy (Tensions > Bottlenecks > HSI > Convergences > Blind Spots), the never-fire Decision Gate, and commandsForFramework. ORCH-03: filing names fileEvidenceWithReadback, wireAccept (fallback), surfaceFileEvidenceResult, memory_event, and cascade edge. ORCH-04: the tier predicate (tier_0 degrade / mode_a / mode_b), Phase 144 coexistence (engine-side vs prompt-side, one-reach-per-beat), and room-proactive coexistence. Also assert the frozen 5 reach_ids + frozen 3 postures appear and NO 6th reach id string appears (the canonical fence). Assert zero em-dashes in the skill. Print per-requirement PASS/FAIL; exit non-zero on any miss. NO em-dashes in the test.
  </action>
  <verify>
    <automated>node tests/test-orchestrator-doctrine-presence.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-orchestrator-doctrine-presence.cjs exits 0
    - The gate checks all four requirements by id: grep -c -E "ORCH-01|ORCH-02|ORCH-03|ORCH-04" tests/test-orchestrator-doctrine-presence.cjs returns >= 4
    - The gate asserts the frozen banks + no-6th-reach fence: grep -i "reach\|posture\|6th" tests/test-orchestrator-doctrine-presence.cjs returns a match
    - No em-dashes: grep -c "—" tests/test-orchestrator-doctrine-presence.cjs returns 0
  </acceptance_criteria>
  <done>tests/test-orchestrator-doctrine-presence.cjs asserts ORCH-01..04 are all present in the orchestrator doctrine plus the frozen-bank / no-6th-reach fence and zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 3: Compose the run-all-1433 aggregator</name>
  <read_first>
    tests/run-all-122.sh (the CJS_SUITES + SHELL_SUITES PASS/FAIL loop pattern, 101 lines - the closest aggregator template);
    tests/run-all-143.sh (the 143 sibling aggregator - mirror its layout for naming consistency);
    tests/run-all-1431.sh (the 143.1 aggregator - the immediate sibling);
    tests/test-reach-ids-drift.cjs + tests/test-posture-ids-drift.cjs (the CARRIED fences this suite must compose);
    tests/test-connector-registry.cjs, tests/test-dispatch-framework-map-drift.cjs, tests/test-connector-tripwire.cjs (the Plan 01/02 tests to compose);
    tests/test-connector-part8-boundary.cjs, tests/test-orchestrator-doctrine-presence.cjs (the Task 1/2 tests to compose)
  </read_first>
  <action>
    Create tests/run-all-1433.sh mirroring tests/run-all-122.sh: a bash PASS/FAIL loop that runs each constituent to completion and exits non-zero if any failed. Compose: (a) the CI tripwire as a direct invocation - node scripts/build-connector-registry.cjs --check; (b) the CJS suites - tests/test-connector-registry.cjs, tests/test-dispatch-framework-map-drift.cjs, tests/test-connector-tripwire.cjs, tests/test-connector-part8-boundary.cjs, tests/test-orchestrator-doctrine-presence.cjs; (c) the CARRIED drift fences - tests/test-reach-ids-drift.cjs, tests/test-posture-ids-drift.cjs (the reach-drift / posture-drift hard fences the suite MUST compose per the phase contract); (d) a Part-8 grep sweep over the new artifacts (scripts/build-connector-registry.cjs, data/connector-registry.json, skills/intelligence-orchestrator/SKILL.md) for forbidden user-content-to-Brain patterns, mirroring the run-all-143.sh Part-8 grep line if present. Make it executable (chmod +x). Print a per-suite PASS/FAIL line and a final tally; exit 1 if any failed. NO em-dashes.
  </action>
  <verify>
    <automated>bash tests/run-all-1433.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - bash tests/run-all-1433.sh exits 0 with all constituents green
    - The suite composes the CI tripwire: grep -c "build-connector-registry.cjs --check" tests/run-all-1433.sh returns >= 1
    - The suite composes the carried drift fences: grep -c -E "reach-ids-drift|posture-ids-drift" tests/run-all-1433.sh returns >= 2
    - The suite composes all five CJS tests: grep -c -E "test-connector-registry|test-dispatch-framework-map-drift|test-connector-tripwire|test-connector-part8-boundary|test-orchestrator-doctrine-presence" tests/run-all-1433.sh returns >= 5
    - The suite is executable: test -x tests/run-all-1433.sh
    - No em-dashes: grep -c "—" tests/run-all-1433.sh returns 0
  </acceptance_criteria>
  <done>tests/run-all-1433.sh composes the connector-registry --check, the three Plan 01/02 tests, the two Plan 04 tests, the Part-8 sweep, AND the carried reach-drift / posture-drift fences into one PASS/FAIL run that exits non-zero on any failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification suite -> nothing external | The suite is LOCAL-only; it must run without network and must itself assert the Part 8 boundary holds |
| Part-8 scan -> the new spine artifacts | The scan is the structural enforcement that the connector spine creates no user-content-to-Brain path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.3-14 | Information Disclosure | the whole connector spine (Part 8 dog-fooding, Canon Part 6) | mitigate | tests/test-connector-part8-boundary.cjs + the run-all-1433.sh Part-8 grep sweep assert the four threat paths are closed; a regression that adds a user-content-to-Brain path fails the suite (HIGH, blocks) |
| T-143.3-15 | Tampering | a future 6th reach_id or new posture | mitigate | the carried tests/test-reach-ids-drift.cjs + test-posture-ids-drift.cjs composed into run-all-1433.sh fail on any drift from the frozen banks |
| T-143.3-16 | Repudiation | the orchestrator silently dropping an ORCH requirement | mitigate | tests/test-orchestrator-doctrine-presence.cjs asserts ORCH-01..04 are all present; a dropped requirement fails the gate |
| T-143.3-SC | Tampering | npm/pip/cargo installs | mitigate | NONE - the suite is bash + CJS over existing files; zero dependencies, no install task exists |
</threat_model>

<verification>
- bash tests/run-all-1433.sh exits 0 with every constituent green
- node tests/test-connector-part8-boundary.cjs exits 0 (four Part-8 paths closed)
- node tests/test-orchestrator-doctrine-presence.cjs exits 0 (ORCH-01..04 present)
- The suite composes the CI tripwire + the carried reach-drift / posture-drift fences + all five CJS tests + the Part-8 sweep
- grep -c "—" across the three new files returns 0 (no em-dashes)
</verification>

<success_criteria>
- The phase verification suite (run-all-1433.sh) is the single gate proving CONN-01..04 + ORCH-01..04 hold and the Part 8 boundary is intact (Canon Part 6 dog-fooding: the plugin honors its own canon)
- The carried reach-drift / posture-drift fences + the new connector-registry --check are all composed, so the frozen banks and the registry stay drift-free
</success_criteria>

<output>
Create `.planning/phases/143.3-connector-spine-and-intelligence-orchestrator/143.3-04-SUMMARY.md` when done
</output>
