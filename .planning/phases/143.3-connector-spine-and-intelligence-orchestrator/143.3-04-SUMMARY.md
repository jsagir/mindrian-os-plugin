---
phase: 143.3
plan: 04
subsystem: connector-spine-verification
tags: [connector-spine, intelligence-orchestrator, verification-aggregator, part-8-boundary, orch-presence-gate, drift-fence, part-6, part-7, part-8]
requires:
  - scripts/build-connector-registry.cjs (Plan 01/02: the generator + --check tripwire)
  - data/connector-registry.json (Plan 02: 7 connectors + 5-key sensor_index)
  - data/dispatch-framework-map.json (Plan 01: the OPEN-1 slug -> EXACT framework-name map)
  - data/framework-names.json (the FEEDS_INTO-linked allowlist, Phase 122)
  - skills/intelligence-orchestrator/SKILL.md (Plan 03: the ORCH-01..04 doctrine under test)
  - tests/test-connector-registry.cjs (Plan 01)
  - tests/test-dispatch-framework-map-drift.cjs (Plan 01)
  - tests/test-connector-tripwire.cjs (Plan 02)
  - tests/test-reach-ids-drift.cjs (Phase 141: the carried reach-drift fence)
  - tests/test-posture-ids-drift.cjs (Phase 141: the carried posture-drift fence)
provides:
  - tests/test-connector-part8-boundary.cjs (the Canon Part 8 boundary scan over the spine, 4 threat paths)
  - tests/test-orchestrator-doctrine-presence.cjs (the ORCH-01..04 presence gate + frozen-bank/no-6th-reach fence)
  - tests/run-all-1433.sh (the phase verification aggregator, 9 constituents, exits 0 clean)
affects:
  - Phase 143.3 closure (this is the final gate proving CONN-01..04 + ORCH-01..04 hold and Part 8 is intact)
  - Phase 146 (the FULLY-WIRED acceptance gate can compose run-all-1433.sh without rework)
tech-stack:
  added: []
  patterns:
    - PASS/FAIL bash aggregator mirroring run-all-122.sh / run-all-143.sh (exits non-zero on any failure)
    - Part-8 boundary scan asserting the four CONTEXT threat paths are closed (forbidden-substring + structural)
    - doctrine-presence gate asserting N requirements are present in prose (143.2 idiom)
    - standalone Part-8 grep sweep over the new artifacts (LOW-3: 143.sh has no single grep line to mirror)
    - em-dash hygiene via code-point escape (no literal em-dash in the scanning test)
key-files:
  created:
    - tests/test-connector-part8-boundary.cjs
    - tests/test-orchestrator-doctrine-presence.cjs
    - tests/run-all-1433.sh
  modified: []
decisions:
  - The Part-8 boundary test reads the generic-framework allowlist from the EXPLICIT framework_names + curated_extras keys, never Object.values().flat() (closes the latent catch-all hole, mirroring the Plan 01 dispatch-map drift test).
  - CHECK 1 proves generator Brain locality structurally - every brain-client require line must sit BELOW the refreshNames() declaration, and the top-level header must carry zero brain-client requires.
  - Per the PATTERNS.md LOW-3 advisory, the aggregator adds a STANDALONE Part-8 grep sweep over the new artifacts rather than mirroring a run-all-143.sh single Part-8 grep line (143.sh has none - it delegates to test-sensors-part8-sweep.cjs).
  - The doctrine-presence gate's em-dash hygiene assertion uses String.fromCharCode(0x2014) so the test file itself carries zero literal em-dashes (the AC mandates grep -c returns 0).
metrics:
  duration: ~12 min (single sequential session)
  completed: 2026-06-07
  tasks: 3
  files: 3
---

# Phase 143.3 Plan 04: Connector Spine Verification Suite Summary

One-liner: tests/run-all-1433.sh ships as the single PASS/FAIL gate that proves the connector spine + the intelligence orchestrator are correct -- composing the CI tripwire, the three Plan 01/02 tests, the two net-new Plan 04 tests (a Canon Part 8 boundary scan over the spine and the ORCH-01..04 doctrine-presence gate), the carried reach-drift / posture-drift hard fences, and a standalone Part-8 grep sweep into one 9-constituent run that exits 0 on a clean tree and non-zero if any check fails. This closes Phase 143.3 (Canon Part 6 dog-fooding: the plugin honors its own canon).

## What shipped

Plans 01-03 shipped the contract, the generator + populated registry, the four --check validations + pre-commit tripwire, and the intelligence-orchestrator skill. This plan is the verification gate that proves they all hold together and that the Part 8 boundary is intact across the new spine. Two net-new tests + the aggregator that composes everything.

### Task 1 -- The Part-8 boundary scan over the connector spine [commit 949cd5fa]
Created tests/test-connector-part8-boundary.cjs (mirrors tests/test-sensors-part8-sweep.cjs + the per-invariant idiom of test-decide-part8-invariant.cjs). Asserts the FOUR CONTEXT threat-model paths are closed, any breach HIGH and non-zero exit:
- CHECK 1 (generator Brain locality): scripts/build-connector-registry.cjs has NO top-level brain-client require; every brain-client require line sits BELOW the refreshNames() declaration (parsed positionally, mirroring build-command-registry.cjs); the top-level header carries zero brain-client requires.
- CHECK 2 (registry + map generic-handles-only): every connector carries ONLY the allowed schema fields (no off-schema field, no free-text body field summary/content/body/text/note/description/evidence/turn); every framework is a generic name in the EXPLICIT framework_names + curated_extras allowlist; every web_scope is null or a frozen hat enum; every dispatch-map value is a generic framework name and never a slug.
- CHECK 3 (orchestrator no-user-content doctrine): skills/intelligence-orchestrator/SKILL.md states Brain queries carry only generic handles, explicitly forbids user content / turn text crossing the boundary, names both turn text AND artifact bodies as forbidden, and names commandsForFramework as a generic-handle-only channel.
- CHECK 4 (hat-scoped web): the doctrine ties web to hatScopeFor (Part 2), states web is hat-scoped, and states the Red hat returns no web (web_enabled: false). No unscoped WebSearch.

### Task 2 -- The orchestrator doctrine-presence gate (ORCH-01..04) [commit e7705afd]
Created tests/test-orchestrator-doctrine-presence.cjs (the 143.2 doctrine-presence-gate idiom). Loads skills/intelligence-orchestrator/SKILL.md and asserts each requirement is present as doctrine:
- ORCH-01: references data/connector-registry.json + states it reads the registry not a hardcoded table + names the sensor_index.
- ORCH-02: the 5-step loop -- dispatchSensors, the dispatch-framework map, WFL-01, the Intelligence Hierarchy (Tensions > Bottlenecks > HSI Surprises > Convergences > Blind Spots), the never-fire Decision Gate, commandsForFramework.
- ORCH-03: filing names fileEvidenceWithReadback, wireAccept (fallback), surfaceFileEvidenceResult, memory_event, and a cascade edge.
- ORCH-04: the tier predicate (tier_0 / mode_a / mode_b), Phase 144 coexistence (engine-side vs prompt-side, one-reach-per-beat), and room-proactive coexistence.
Plus the canonical fence: the frozen 5 reach_ids + frozen 3 postures all appear as code-span tokens, the no-6th-reach rule is stated, and the skill carries zero em-dashes. The test's own em-dash hygiene assertion uses String.fromCharCode(0x2014) so the test file itself carries zero literal em-dashes (Rule 1 fix below).

### Task 3 -- Compose the run-all-1433 aggregator [commit 161b8082]
Created tests/run-all-1433.sh (chmod +x) mirroring run-all-122.sh / run-all-143.sh: a bash PASS/FAIL loop that runs each constituent to completion and exits non-zero if any failed. Composes:
- (a) the CI tripwire as a direct invocation: node scripts/build-connector-registry.cjs --check
- (b) the five CJS suites: test-connector-registry, test-dispatch-framework-map-drift, test-connector-tripwire, test-connector-part8-boundary, test-orchestrator-doctrine-presence
- (c) the CARRIED drift fences: test-reach-ids-drift, test-posture-ids-drift
- (d) a standalone Part-8 grep sweep over the new artifacts (data/connector-registry.json, data/dispatch-framework-map.json, skills/intelligence-orchestrator/SKILL.md) for forbidden projection/hash tokens + free-text body fields. Per PATTERNS.md LOW-3, this is a standalone sweep, not a mirror of a nonexistent run-all-143.sh grep line.

9 constituents total; per-suite PASS/FAIL line + final tally; exit 1 if any failed.

## Verification (all exit 0)

- `bash tests/run-all-1433.sh` -> 9/9 PASSED, exit 0 (the 34-command WARN nudge from --check is the documented opt-in retrofit policy, not a failure)
- `node tests/test-connector-part8-boundary.cjs` -> 4 passed, 0 failed (four Part-8 paths closed)
- `node tests/test-orchestrator-doctrine-presence.cjs` -> 8 passed, 0 failed (ORCH-01..04 + fence)
- Task 1 ACs: refresh-names|brain-client grep 12 (>=1), intelligence-orchestrator grep 3 (>=1), hat-scope match present, em-dash 0
- Task 2 ACs: ORCH-01..04 grep 36 (>=4), reach|posture|6th match present, em-dash 0
- Task 3 ACs: build-connector-registry.cjs --check grep 1 (>=1), reach-ids-drift|posture-ids-drift grep 4 (>=2), five CJS tests grep 10 (>=5), test -x yes, em-dash 0
- The carried fences (test-reach-ids-drift / test-posture-ids-drift) confirmed by exact filename in tests/ before composing -- no name invented.
- All three task commits passed the live pre-commit hooks with no --no-verify.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Em-dash literal in the doctrine-presence test's hygiene assertion**
- **Found during:** Task 2 (em-dash AC check)
- **Issue:** The hygiene assertion that scans the skill for em-dashes used a literal em-dash in its regex (`/—/g` written as the raw character). The Task 2 AC mandates `grep -c` for an em-dash returns 0 over the test file itself, so the scanning test failed its own no-em-dash rule (count 1).
- **Fix:** Replaced the literal-character regex with `String.fromCharCode(0x2014)` + a `split().length - 1` count, so the test file carries zero literal em-dashes while still asserting the skill has zero.
- **Files modified:** tests/test-orchestrator-doctrine-presence.cjs
- **Commit:** e7705afd (folded into the Task 2 commit)

## Known Stubs

None. All three deliverables are executable tests over already-shipped artifacts; there are no hardcoded empty values, placeholders, or unwired data sources. The 34-command WARN nudge surfaced by --check is the documented incremental-retrofit policy (the algorithmic cohort of 7 is wired; the rest opt in as touched, per CONNECTOR-CONTRACT.md), not a stub introduced by this plan.

## Threat surface

No new threat surface beyond the plan's `<threat_model>`. The three mitigate-disposition threats are addressed by this plan's deliverables: T-143.3-14 (a user-content-to-Brain path across the whole spine) is blocked by tests/test-connector-part8-boundary.cjs (four threat paths) + the run-all-1433.sh Part-8 grep sweep -- a regression that adds such a path fails the suite (HIGH, blocks); T-143.3-15 (a future 6th reach_id or new posture) is blocked by the carried test-reach-ids-drift.cjs + test-posture-ids-drift.cjs composed into the suite; T-143.3-16 (the orchestrator silently dropping an ORCH requirement) is blocked by tests/test-orchestrator-doctrine-presence.cjs. T-143.3-SC holds: this plan is pure bash + CJS over existing files; git diff package.json is empty, zero dependencies, no install task. The verification suite itself runs LOCAL-only with no network surface.

## Threat Flags

None. No file created in this plan introduces a new network endpoint, auth path, file-access pattern, or schema change at a trust boundary; the deliverables are read-only tests over existing artifacts.

## Self-Check: PASSED

Files (all FOUND):
- tests/test-connector-part8-boundary.cjs
- tests/test-orchestrator-doctrine-presence.cjs
- tests/run-all-1433.sh

Commits (all FOUND in git log):
- 949cd5fa test(143.3-04): Part-8 boundary scan over the connector spine
- e7705afd test(143.3-04): orchestrator doctrine-presence gate (ORCH-01..04)
- 161b8082 test(143.3-04): compose the run-all-1433 verification aggregator
