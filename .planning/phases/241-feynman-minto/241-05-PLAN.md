---
phase: 241-feynman-minto
plan: 05
type: execute
wave: 4
depends_on: ["241-01", "241-02", "241-03", "241-04"]
files_modified:
  - lib/mcp/stop-gate-handler.cjs
  - scripts/on-stop
  - tests/test-241-guardian-tripolar-parity.cjs
  - tests/run-all-241.sh
autonomous: true
requirements: [MINTO-01, MINTO-02]

must_haves:
  truths:
    - "A session closing through the shared mindrian-core Stop path receives the guardian's invariant finding, so Desktop, Cowork, and the CLI running under MINDRIAN_MCP_FIRST are no longer blind to it"
    - "The shared path writes .mindrian/invariant-report.json and prunes ghost stale entries exactly as the CLI legacy path does"
    - "One phase harness runs every Phase 241 leg and reports a single PASS or FAIL"
    - "Removing the guardian call from the shared handler turns the parity test red"
  artifacts:
    - path: "lib/mcp/stop-gate-handler.cjs"
      provides: "_closeOutGuardianOnStop, the shared-path guardian invocation, and guardian_sm on the business object"
      contains: "_closeOutGuardianOnStop"
    - path: "scripts/on-stop"
      provides: "The MINDRIAN_MCP_FIRST thin branch folds business.guardian_sm into its systemMessage"
      contains: "guardian_sm"
    - path: "tests/test-241-guardian-tripolar-parity.cjs"
      provides: "Both Stop paths produce the same guardian finding on the same fixture, plus the mutation proof"
      min_lines: 100
    - path: "tests/run-all-241.sh"
      provides: "One PASS or FAIL gate over every Phase 241 leg"
  key_links:
    - from: "lib/mcp/stop-gate-handler.cjs closeOutRoom"
      to: "scripts/feynman-minto-guardian.cjs on-stop"
      via: "execFileSync on the guardian, mirroring _closeOutStateMd"
      pattern: "_closeOutGuardianOnStop\\(roomDir\\)"
    - from: "stop_gate_check business.guardian_sm"
      to: "scripts/on-stop thin-branch systemMessage"
      via: "JSON round trip through the daemon reply"
      pattern: "guardian_sm"
---

<objective>
Close the phase: give the shared Stop path the same guardian reporting the CLI legacy path got in plan 241-01, then wire one harness that proves every Phase 241 leg at once, then file the reasoning trail the repo's Dev-Research Compositing rule requires.

**Resolution R-01, RESEARCH.md Open Question 2, answered as WIRE BOTH rather than CLI-only.** CLAUDE.md's Tri-Polar rule treats a surface skip as needing a stated, deliberate call. This plan states the opposite call and its evidence, because the reading in RESEARCH.md's Open Question 2 understated the hole. `lib/mcp/stop-gate-handler.cjs` contains zero references to `feynman-minto-guardian`, so it never runs the invariant check at all. That was framed as a Desktop and Cowork gap, but it is also a CLI gap: `scripts/on-stop`'s `MINDRIAN_MCP_FIRST` thin adapter EARLY-EXITS the entire script once the daemon answers, so on a CLI session with that flag naming `cli` or `all`, the Phase 88-13 guardian block roughly 400 lines below is never reached either. Plan 241-01's fix therefore covers the default CLI path and only that. A one-surface fix here would leave two install targets plus one supported CLI configuration silently unreported, which is precisely the shape this milestone exists to remove. The wiring itself is small: a new `_closeOutGuardianOnStop` sibling in an existing helper family, following `_closeOutStateMd`'s `execFileSync` idiom exactly, plus one fold in the thin branch.

Purpose: make success criterion 1 true on every surface, not on one, and leave the phase with a single runnable gate.

Output: guardian parity across both Stop paths, `tests/run-all-241.sh`, and the compositing filing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/241-feynman-minto/241-RESEARCH.md
@.planning/phases/241-feynman-minto/241-PATTERNS.md
@.planning/phases/241-feynman-minto/241-01-SUMMARY.md
@.planning/phases/241-feynman-minto/241-02-SUMMARY.md
@.planning/phases/241-feynman-minto/241-04-SUMMARY.md
</context>

## Artifacts this phase produces

| Symbol | Kind | Location |
|---|---|---|
| `_closeOutGuardianOnStop()` | function | `lib/mcp/stop-gate-handler.cjs` |
| `business.guardian_sm` | returned object field | `lib/mcp/stop-gate-handler.cjs` `closeOutRoom` |
| `tests/test-241-guardian-tripolar-parity.cjs` | new file | test |
| `tests/run-all-241.sh` | new file | phase harness |

<tasks>

<task type="auto">
  <name>Task 1: Run the guardian on the shared mindrian-core Stop path and surface its finding</name>
  <files>lib/mcp/stop-gate-handler.cjs, scripts/on-stop</files>
  <read_first>
    - `lib/mcp/stop-gate-handler.cjs` lines 30-45 (the file's own contract header, item 5, "owns the server-side share of on-stop's business close-out"), lines 92-145 (the `_closeOut*` helper family, including `_closeOutStateMd`'s `execFileSync('bash', [computeStatePath, roomDir], { timeout: 2000, encoding: 'utf8' })` which is the exact idiom to mirror, and `_closeOutMintoDrain` as plan 241-02 left it), and lines 220-245 (`closeOutRoom`, its JSDoc return type, and its call sequence).
    - `lib/mcp/stop-gate-handler.cjs` lines 360-500, where `business` is assembled by `closeOutRoom` and returned on all three exit shapes (`{fire:false, reason, business}` at two sites and `Object.assign({fire:true, business}, rendered)` at the third). Every shape must carry the new field or one surface stays blind.
    - `scripts/on-stop` lines 40-120, the `MINDRIAN_MCP_FIRST` thin adapter: the `queryDaemon('stop_gate_check', ...)` call, the `r.fire === true` branch, and the else branch that builds its message from `r.business.sections`. Plan 241-02 relabelled that message; read its current text before editing.
    - `scripts/feynman-minto-guardian.cjs` `runOnStop` as plans 241-01 and 241-03 left it, specifically what it writes to stdout and under what severity gate, so the parse here matches what plan 241-01's shell parse does.
    - `.planning/phases/241-feynman-minto/241-PATTERNS.md` section on `lib/mcp/stop-gate-handler.cjs`, which names the `_closeOutGuardianOnStop` shape and the `execFileSync` analog.
    - CLAUDE.md's Part 11 note and RESEARCH.md's grounding-source note: this is a BODY-level change to an already-registered handler, not a new matcher or a new registration, so no CIRS registration declaration is added. Re-confirm this holds for what you actually write before committing.
  </read_first>
  <action>
    Add `_closeOutGuardianOnStop(roomDir)` to `lib/mcp/stop-gate-handler.cjs`, placed with its sibling `_closeOut*` helpers and following their exact shape: everything inside one try/catch, never throwing, returning a benign value on any fault. It resolves the guardian at `path.join(PLUGIN_ROOT, 'scripts', 'feynman-minto-guardian.cjs')`, returns null when that file is absent, and otherwise runs `execFileSync(process.execPath, [guardianPath, 'on-stop', roomDir], { timeout: N, encoding: 'utf8' })`. Use `process.execPath` rather than the string `node`, matching how the rest of this repo spawns node from inside node. Set the timeout to 3000, matching the ceiling plan 241-01 established on the CLI path so the two surfaces have the same budget rather than two different silent truncation points. Parse the LAST non-empty line of the captured stdout with `JSON.parse` inside its own try/catch and return `systemMessage` when it is a non-empty string, otherwise null.

    Call it from `closeOutRoom`, positioned AFTER `_closeOutFolderMemorySnapshot` and `_closeOutMintoDrain`. Ordering is behavioral, not cosmetic: the guardian's `queue-health` and `snapshot-integrity` validators read state those two helpers produce, and running the guardian first would have it validate a pre-close-out snapshot. This mirrors `scripts/on-stop`, where the Phase 88-13 guardian block deliberately sits after the Phase 88-06 drain and snapshot.

    Capture the returned string and add it to `closeOutRoom`'s return object as `guardian_sm`, defaulting to null. Update the JSDoc `@returns` line, which currently reads `{room_dir, sections, stale}` and after plan 241-02 also carries `minto_pending`. Also update the early-return branch for a missing or invalid `roomDir` so its object shape stays identical to the success shape; a return that omits keys on one branch is how downstream `undefined` reads start.

    Verify the field survives to every caller. `business` is returned from three sites in the handler's main function. Confirm each one passes the whole `business` object through rather than reconstructing a subset, and fix any that do not.

    In `scripts/on-stop`'s `MINDRIAN_MCP_FIRST` thin branch, fold the finding into the message that branch emits. In the else branch, after the existing message is built from `r.business.sections`, append `r.business.guardian_sm` when it is a non-empty string, using the same space-pipe-space separator the legacy path uses so the two surfaces read identically. Do NOT touch the `r.fire === true` branch: it emits a `decision: 'block'` envelope with a deliberately terse `systemMessage`, and the comment above it documents two prior schema regressions at that exact site.

    Update the file header contract note in `lib/mcp/stop-gate-handler.cjs` (item 5 in its own list) to include the guardian on-stop invariant check among the business close-out steps this handler owns. Use hyphens, never em-dashes.
  </action>
  <verify>
    <automated>bash -n scripts/on-stop && node -e "const h=require('./lib/mcp/stop-gate-handler.cjs');console.log('loaded')" && node lib/memory/on-stop-snapshot.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_closeOutGuardianOnStop' lib/mcp/stop-gate-handler.cjs` is 2 (definition plus the call in `closeOutRoom`).
    - `grep -c 'feynman-minto-guardian' lib/mcp/stop-gate-handler.cjs` is at least 1, up from 0 before this task.
    - `grep -c 'guardian_sm' lib/mcp/stop-gate-handler.cjs` is at least 2 (assembly plus the JSDoc).
    - `grep -c 'guardian_sm' scripts/on-stop` is at least 1.
    - The call sits after `_closeOutFolderMemorySnapshot` in `closeOutRoom`: confirm by reading the function's ordered body and record the order in the SUMMARY.
    - `closeOutRoom`'s early-return object and its success-return object have identical key sets. Prove with `node -e` calling `closeOutRoom(null)` and `closeOutRoom(<real fixture>)` and comparing `Object.keys(...).sort()`.
    - Behavioral proof, recorded in the SUMMARY: against a scratch room seeded with a missing MINTO.md, calling `closeOutRoom(roomDir, 'test-session')` directly returns a `guardian_sm` string containing `guardian:`, AND `.mindrian/invariant-report.json` now exists in that room, proving the shared path also produces the report and not just the message.
    - `bash -n scripts/on-stop` exits 0 and `node lib/memory/on-stop-snapshot.test.cjs` exits 0.
    - No new matcher or registration entry was added to `hooks/hooks.json` or any registry, confirming the Part 11 body-level-change exemption still holds. State this explicitly in the SUMMARY.
  </acceptance_criteria>
  <done>Both Stop paths run the guardian, write the report, prune ghosts, and surface the same finding.</done>
</task>

<task type="auto">
  <name>Task 2: Prove parity between the two Stop paths, with a mutation proof</name>
  <files>tests/test-241-guardian-tripolar-parity.cjs</files>
  <read_first>
    - `lib/memory/guardian-onstop-reaches-user.test.cjs` as plan 241-01 created it, for the fixture builder and the last-line-of-stdout assertion technique this test reuses on the other path.
    - `lib/mcp/stop-gate-handler.cjs`'s `module.exports`, to see whether `closeOutRoom` is exported. If it is not, drive it through whatever IS exported rather than exporting a private helper purely for a test; a test-only export is a weaker proof than driving the real entry point.
    - `tests/` for the `test-241-*` naming convention and any existing `tests/test-2*.cjs` file's structure, since `tests/run-all-234.sh` glob-discovers `tests/test-234-*` and Task 3's harness will do the same for 241.
    - `.planning/phases/241-feynman-minto/241-01-SUMMARY.md` for the exact systemMessage shape plan 241-01 settled on, so this test asserts the same substring on both paths rather than two different ones.
  </read_first>
  <action>
    Create `tests/test-241-guardian-tripolar-parity.cjs`. Three tests.

    Test 1, the shared path reports. Build a room fixture with one section holding a `ROOM.md` and no `MINTO.md`. Drive the shared handler's close-out for that room. Assert THREE facts: a guardian systemMessage string comes back and contains `guardian:`, `.mindrian/invariant-report.json` exists on disk, and that report's `sections` object names the seeded section. The report assertion matters independently: a message without a report would mean the shared path emits a claim it did not substantiate.

    Test 2, parity. Run the SAME fixture shape through both paths, the shared handler and `bash scripts/on-stop` on the default (unset `MINDRIAN_MCP_FIRST`) legacy path, in two separate room copies so neither pollutes the other. Assert the guardian portion of the message is equal between them. Comparing exact equality is the point: two surfaces that both mention the guardian but disagree about the finding is the drift this test exists to catch, and a substring-only check would not see it.

    Test 3, mutation proof. Remove the `_closeOutGuardianOnStop` call from `closeOutRoom` in a tmp copy of `lib/mcp/stop-gate-handler.cjs`, load that copy, and assert no guardian message comes back and no `invariant-report.json` is produced. Removing the wiring must turn this red. Do not mutate the real file.

    Fixtures live under `os.tmpdir()` and are cleaned up. The test writes nothing inside the repo tree.
  </action>
  <verify>
    <automated>node tests/test-241-guardian-tripolar-parity.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-241-guardian-tripolar-parity.cjs` exits 0 with 3 passing tests.
    - Test 1 asserts on `.mindrian/invariant-report.json` contents, not only on the returned string.
    - Test 2 asserts EQUALITY of the guardian portion across both paths, not merely that both are non-empty.
    - Test 3 fails when the mutation is inverted. Demonstrate once by hand, paste the observed output into the SUMMARY, revert.
    - No private helper was exported from `lib/mcp/stop-gate-handler.cjs` solely to make this test possible. If the existing export surface made a real drive impossible, say so explicitly in the SUMMARY and justify what was added.
    - The file name matches `tests/test-241-*` so Task 3's harness glob discovers it.
  </acceptance_criteria>
  <done>Both Stop paths are proven to report the same finding on the same fixture, and unwiring either turns a named test red.</done>
</task>

<task type="auto">
  <name>Task 3: Phase harness and the Dev-Research Compositing filing</name>
  <files>tests/run-all-241.sh</files>
  <read_first>
    - `tests/run-all-234.sh` in full. It is the current best example of this repo's harness idiom: a `run` and `run_may_skip` helper pair, GLOB discovery of `tests/test-234-*` so downstream plans add coverage without editing the harness, explicitly wired legs that the glob cannot see, an honest note about any leg expected red while the phase is in flight, and a final `PASS=n FAIL=n SKIP=n` summary.
    - The four SUMMARY files this phase produced, `241-01` through `241-04`, for the exact command each plan's tests are run by, so the harness runs the real commands rather than reinvented ones.
    - CLAUDE.md's "Dev-Research Compositing (Rethinking Room)" section for the filing contract, and its "QA and RCA Reporting" section confirming `.planning/` is gitignored so committing anything under it needs `git add -f`.
    - Check whether `~/MindrianRooms/rethinking-mindrianos/research/` exists before writing there. If the room is absent on this machine, record that in the SUMMARY and file only the in-repo half rather than creating a room directory as a side effect.
  </read_first>
  <action>
    Create `tests/run-all-241.sh` following the `run-all-234.sh` shape. Header prose states what this phase had to prove, in plain language: the guardian is heard on both Stop paths, a slow write is no longer dropped, the severity ladder reaches the enqueue gate for the two breaches navigators actually hit, and pre-commit warns instead of blocking.

    Wire these legs explicitly, because they live under `lib/memory/` where a `tests/test-241-*` glob cannot see them, and a test that is never discovered is worse than no test since the harness still prints green: `node lib/memory/guardian-onstop-reaches-user.test.cjs`, `node lib/memory/minto-debounce-consumer-census.test.cjs`, `node lib/memory/precommit-real-commit.test.cjs`, `node lib/memory/feynman-minto-guardian.test.cjs`, `node lib/memory/feynman-minto-invariants.test.cjs`, `node lib/memory/debouncer-drain-at-prompt.test.cjs`, and the whole-suite roll-up `node lib/memory/run-feynman-tests.cjs`.

    Also GLOB-discover `tests/test-241-*` for both `.cjs` and `.sh`, so Task 2's parity test is picked up without being named and any later addition needs no harness edit. Fail the run if the glob discovers ZERO files, since a silently empty glob prints the same green as a clean sweep.

    Add one grep-gate leg in the permanent-tripwire idiom those harnesses use: assert that no production file reintroduces `--older-than=0` or `olderThanMs: 0`, and that `scripts/on-stop` contains neither `timeout 1 node` nor a `>/dev/null 2>&1` on the guardian invocation. Strip comment lines before counting, using a `grep -v '^[[:space:]]*#'` filter ahead of the match, so this harness's own explanatory prose cannot satisfy or invalidate its own gate.

    Use `run`, not `run_may_skip`, for every leg. Nothing in this phase is legitimately skippable.

    Then file the Dev-Research Compositing half. Phase 241 is MindrianOS's own architecture, not user-facing feature work, so CLAUDE.md's standing rule requires the reasoning trail to land in both homes. Write a dated entry under `~/MindrianRooms/rethinking-mindrianos/research/2026-07-28-feynman-minto-guardian-reachability/` capturing what this phase actually learned, cross-linked back to `.planning/phases/241-feynman-minto/` and to the resolved RCA. Lead with the finding that matters most to a future reader: the debounce consumer was never missing, an extension-scoped grep over a repo whose hook entry points are deliberately extensionless produced a decisive-sounding conclusion about the wrong file, and the real defect was the unconditional queue vacuum at both stop paths. Also record the guardian reachability finding, which no RCA had covered, and the Tri-Polar decision R-01 with the `MINDRIAN_MCP_FIRST` early-exit evidence behind it.
  </action>
  <verify>
    <automated>bash tests/run-all-241.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-241.sh` exits 0 and prints a `PASS=n FAIL=0 SKIP=0` summary.
    - The harness discovers `tests/test-241-guardian-tripolar-parity.cjs` via the glob, without naming it. Confirm by reading the run output.
    - The harness FAILS when the glob discovers zero files. Prove once by moving the parity test aside, observing the failure, and restoring it.
    - The regression grep-gate bites: restore `--older-than=0` in a working copy of `scripts/on-stop`, watch the harness go red, revert. Same for restoring `timeout 1 node`. Record both observations.
    - The grep-gate strips comments before counting: `grep -c "grep -v '\^\[\[:space:\]\]\*#'" tests/run-all-241.sh` is at least 1, so the harness's own header prose cannot trip or satisfy its own gate.
    - `tests/run-all-241.sh` is executable (`chmod +x`) and passes `bash -n`.
    - The compositing entry exists at the named path, or the SUMMARY explicitly records that the `rethinking-mindrianos` room is absent on this machine and that only the in-repo half was filed. Silence on this point fails the criterion.
    - The compositing entry leads with the RCA correction, not with the code changes.
    - No em-dashes anywhere in the harness prose or the compositing entry.
  </acceptance_criteria>
  <done>One command proves the whole phase, the regression tripwires bite, and the reasoning trail lives in both homes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `lib/mcp/stop-gate-handler.cjs` -> guardian subprocess | The shared handler now spawns a child process during Stop close-out, on Desktop, Cowork, and CLI-under-flag. |
| guardian stdout -> daemon reply -> `scripts/on-stop` thin branch | A string crosses a JSON-RPC boundary and lands in a schema-validated Stop-hook envelope. |
| harness grep-gates -> phase regression detection | The gates are the standing proof that the fixes stay fixed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-241-20 | Repudiation | one surface reporting while another stays silent | mitigate | The whole reason for this plan. A CLI-only fix would leave Desktop, Cowork, and CLI-under-`MINDRIAN_MCP_FIRST` reporting nothing while the default CLI path looked healthy, which reads as a fixed system. Task 2 test 2 asserts EQUALITY of the guardian message across both paths, so drift between surfaces fails rather than hides. |
| T-241-21 | Denial of Service | a new subprocess inside the shared Stop close-out | mitigate | `_closeOutGuardianOnStop` carries an explicit 3000ms `execFileSync` timeout matching the ceiling plan 241-01 set on the CLI path, so the two surfaces truncate at the same point rather than at two different unstated ones. The guardian's own soft walk budget from plan 241-01 is the primary internal bound. The helper follows the never-throw contract of every sibling, so a timeout degrades the finding, never the close-out. |
| T-241-22 | Information Disclosure | guardian message crossing the daemon boundary | mitigate | Canon Part 8: the message carries section NAMES and integer counts only, never MINTO content, a restriction plan 241-01 preserved at the emission site. The daemon is a LOCAL stdio process, not a network service, so no egress boundary is crossed. Task 1 changes no Brain call. |
| T-241-23 | Tampering | a harness that prints green over an empty glob | mitigate | `tests/run-all-234.sh` learned this the hard way and the lesson carries: Task 3 requires the harness to FAIL on zero glob discoveries and requires that failure to be demonstrated once by moving the parity test aside. |
| T-241-24 | Repudiation | a grep-gate invalidated by its own comment prose | mitigate | `grep -c` counts comment lines, so a harness that explains its own rule can satisfy or break that rule with prose. Task 3 requires comment stripping ahead of every match and asserts the filter is present. |
| T-241-25 | Repudiation | reintroducing a fixed defect silently | mitigate | The harness's permanent tripwire legs assert `olderThanMs: 0`, `--older-than=0`, `timeout 1 node`, and the guardian stdout discard are all absent from production files, and each tripwire is proven to bite by a demonstrated mutation rather than assumed to work. |
| T-241-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable. Zero packages installed or added across the entire phase. |

ASVS note: no auth, session, access-control, or crypto surface touched anywhere in this plan. Canon Part 8 is unaffected: every path added here is LOCAL-only with no Brain call.
</threat_model>

<verification>
1. `bash tests/run-all-241.sh` exits 0 with `FAIL=0 SKIP=0`.
2. `node tests/test-241-guardian-tripolar-parity.cjs` exits 0 with 3 tests.
3. `node lib/memory/run-feynman-tests.cjs` exits 0.
4. `bash -n scripts/on-stop` exits 0 and `lib/mcp/stop-gate-handler.cjs` loads without throwing.
5. Every mutation proof named in the acceptance criteria demonstrated once and recorded.
6. The compositing filing exists, or its absence is explicitly explained.
</verification>

<success_criteria>
- The shared mindrian-core Stop path runs the guardian, writes the report, prunes ghosts, and returns the finding.
- Both Stop paths produce an equal guardian message on the same fixture.
- Unwiring the shared-path call turns the parity test red.
- One harness command proves the whole phase and fails on an empty glob.
- The permanent tripwires against the four retired defects all bite when their defect is restored.
- The Tri-Polar decision is stated with its evidence, not left silent.
</success_criteria>

<output>
Create `.planning/phases/241-feynman-minto/241-05-SUMMARY.md` when done.
</output>
