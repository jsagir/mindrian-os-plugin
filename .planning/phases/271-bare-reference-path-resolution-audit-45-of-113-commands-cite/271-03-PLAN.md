---
phase: 271-bare-reference-path-resolution-audit
plan: 03
type: execute
wave: 3
depends_on: ["271-02"]
files_modified:
  - commands/act.md
  - commands/analyze-needs.md
  - commands/analyze-systems.md
  - commands/analyze-timing.md
  - commands/beautiful-question.md
  - commands/build-knowledge.md
  - commands/build-thesis.md
  - commands/causal.md
  - commands/challenge-assumptions.md
  - commands/compare-ventures.md
  - commands/deck.md
  - commands/deep-grade.md
  - commands/diagnose.md
  - commands/diffusion.md
  - commands/dominant-designs.md
  - commands/explore-domains.md
  - commands/explore-futures.md
  - commands/explore-trends.md
  - commands/find-analogies.md
  - commands/find-bottlenecks.md
  - commands/find-connections.md
  - commands/futures.md
  - commands/grade-grant.md
  - commands/grade.md
  - commands/leadership.md
  - commands/lean-canvas.md
  - commands/macro-trends.md
  - commands/map-unknowns.md
  - commands/mullins.md
  - commands/pipeline.md
  - commands/publish.md
  - commands/root-cause.md
  - commands/scenario-plan.md
  - commands/score-innovation.md
  - commands/setup.md
  - commands/show.md
  - commands/structure-argument.md
  - commands/suggest-next.md
  - commands/systems-thinking.md
  - commands/think-hats.md
  - commands/trending-to-absurd.md
  - commands/user-needs.md
  - commands/validate.md
  - commands/value-proposition.md
  - skills/*/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every bare `references/...` citation in the 44 in-scope command files resolves against the plugin install directory, not the session cwd."
    - "`commands/file-meeting.md` is byte-identical to its already-shipped fixed state; this phase never re-touched it."
    - "`commands/radar.md` is untouched or handled exactly as plan 271-02's ruling directed, and nothing else."
    - "Every generated skill mirror reflects the anchored source, and `build-skill-mirrors.cjs --check` still reports 112/112."
    - "The sweep changed only citation prefixes; no sentence, code fence, or frontmatter key was altered."
  artifacts:
    - path: "commands/setup.md"
      provides: "6 anchored voice-dna citations (lines 44, 140, 284, 350, 439, 583) -- the highest-count single file"
      contains: "${CLAUDE_PLUGIN_ROOT}/references/personality/voice-dna.md"
    - path: "skills/grade/SKILL.md"
      provides: "Regenerated mirror carrying the fail-closed portability wrapper"
      contains: "MINDRIAN_OS_ROOT"
  key_links:
    - from: "commands/*.md"
      to: "skills/*/SKILL.md"
      via: "node scripts/build-skill-mirrors.cjs"
      pattern: "MINDRIAN_OS_ROOT:-\\$\\{CLAUDE_PLUGIN_ROOT"
---

<objective>
Apply the proven fix pattern to the 93 bare citations across 44 command files.

This is the mechanical core of the phase. The fix shape is not novel: the
`commands/file-meeting.md` RCA already applied it to 19 sites in one file, verified it, and
committed it at `242e32db`. Its own Resolution section records the method verbatim: "Applied
via a scoped sed (`` `references/ `` -> `` `${CLAUDE_PLUGIN_ROOT}/references/ ``), diff-reviewed
to confirm only the intended lines changed."

The risk here is not the fix, it is the blast radius: 44 files at once. The whole plan is
built around making the diff provably minimal and provably complete.

Purpose: make 44 shipped commands work from a real user's Data Room.
Output: 93 anchored citations plus regenerated mirrors, with the mirror gate still at 112/112.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/271-02-SUMMARY.md
@.planning/debug/resolved/file-meeting-missing-reference-files.md
</context>

<assumptions>
- **Shared working tree.** Re-run `git status --short` and `git log --oneline -3` before the
  first edit. If any `commands/*.md`, `skills/*/SKILL.md`, or `scripts/build-skill-mirrors.cjs`
  shows as modified by another session, STOP and report rather than sweeping over live edits.
  At plan time HEAD was `75b44778` with only four generated `data/*.json` modified.
- **`commands/file-meeting.md` is NOT in `files_modified` and must not be.** It carries zero bare
  `references/` citations (already fixed). Confirm with
  `grep -c '\`references/' commands/file-meeting.md` returning 0 before and after.
- **`commands/radar.md` is NOT in `files_modified`** under plan 271-02's recommended ruling. If
  the human chose option-b or option-c, add it back explicitly and record why in the summary.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Anchor the 93 citations across 44 command files</name>
  <files>commands/act.md, commands/analyze-needs.md, commands/analyze-systems.md, commands/analyze-timing.md, commands/beautiful-question.md, commands/build-knowledge.md, commands/build-thesis.md, commands/causal.md, commands/challenge-assumptions.md, commands/compare-ventures.md, commands/deck.md, commands/deep-grade.md, commands/diagnose.md, commands/diffusion.md, commands/dominant-designs.md, commands/explore-domains.md, commands/explore-futures.md, commands/explore-trends.md, commands/find-analogies.md, commands/find-bottlenecks.md, commands/find-connections.md, commands/futures.md, commands/grade-grant.md, commands/grade.md, commands/leadership.md, commands/lean-canvas.md, commands/macro-trends.md, commands/map-unknowns.md, commands/mullins.md, commands/pipeline.md, commands/publish.md, commands/root-cause.md, commands/scenario-plan.md, commands/score-innovation.md, commands/setup.md, commands/show.md, commands/structure-argument.md, commands/suggest-next.md, commands/systems-thinking.md, commands/think-hats.md, commands/trending-to-absurd.md, commands/user-needs.md, commands/validate.md, commands/value-proposition.md</files>
  <read_first>
    - `.planning/debug/resolved/file-meeting-missing-reference-files.md` Resolution.fix and
      Resolution.verification -- the exact sed shape and the exact five-step verification the
      precedent used. Reproduce both, do not invent a new method.
    - `271-AUDIT.md` sections 1 and 3 -- the live per-file site counts and the LOAD-BEARING
      versus DESCRIPTIVE PROSE disposition rule this phase adopted.
    - `commands/setup.md` lines 40-50 -- one representative citation in situ, so the diff shape
      is understood before running the sweep.
  </read_first>
  <action>
    Capture the pre-state first: run
    `node scripts/check-plugin-path-anchoring.cjs --json > /tmp/271-pre.json` and record the
    per-file counts. This is the completeness oracle for the post-state check.

    Apply the anchoring with a scoped, per-file sed over exactly the 44 files listed above,
    replacing the literal `` `references/ `` with `` `${CLAUDE_PLUGIN_ROOT}/references/ ``.
    Use the SHORT form here, `${CLAUDE_PLUGIN_ROOT}/references/...`, matching both the
    file-meeting precedent and the 53 command files that already use `${CLAUDE_PLUGIN_ROOT}` for
    their `bash`/`node` script invocations. Do NOT hand-write the long
    `${MINDRIAN_OS_ROOT:-${CLAUDE_PLUGIN_ROOT:?...}}` form into `commands/` -- Task 2's mirror
    generator applies that wrapper automatically when producing `skills/*/SKILL.md`
    (`build-skill-mirrors.cjs` Exception Class 3, `applyPluginRootPortability`, phase 234-06,
    threat T-234-11). Writing it by hand into the source would double-wrap on regeneration.

    Anchor the anchor: the sed pattern must require the opening backtick so it only matches
    citation tokens. Do NOT use an unanchored `s|references/|...|g`, which would corrupt prose
    like "the references directory" and any `.planning/` path containing the substring.

    Do NOT touch `commands/file-meeting.md`. Do NOT touch `commands/radar.md` unless plan 271-02
    ruled option-b or option-c.

    Note the mirror asymmetry for `commands/trending-to-absurd.md`: its skill counterpart
    `skills/trending-to-absurd/SKILL.md` is on `build-skill-mirrors.cjs`'s `SKIP_LIST` (line
    175) because it is hand-authored and divergent by design. Fixing the command here does NOT
    propagate to that skill. Its 1 bare citation is plan 271-04's work. Record this explicitly
    in the summary so nobody assumes mirror regeneration covered it.

    Review the full diff before considering the task done. Every changed line must differ only
    by the inserted `${CLAUDE_PLUGIN_ROOT}/` prefix. If any line shows any other change, revert
    that file and redo it individually.
  </action>
  <verify>
    <automated>test $(git diff --numstat -- commands/ | awk '{s+=$1} END {print s}') -eq 93 &amp;&amp; test $(git diff -- commands/ | grep -c '^+' ) -eq $(git diff -- commands/ | grep -c '^-')</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat -- commands/` shows exactly 44 files changed with added lines equal to
      removed lines (pure in-place substitution, zero net line change).
    - `git diff -U0 -- commands/ | grep '^+' | grep -v '^+++' | grep -vc 'CLAUDE_PLUGIN_ROOT'`
      returns 0, proving every single added line contains the anchor and nothing else changed.
    - `grep -rc '\`references/' commands/file-meeting.md` returns 0 AND
      `git diff --stat -- commands/file-meeting.md` is empty (the already-fixed file was not
      re-touched).
    - `node scripts/check-plugin-path-anchoring.cjs --report` reports a `commands` group
      violation total of 0 non-allowlisted (5 allowlisted radar sites remain under option-a/d).
    - Every anchored target still resolves: `grep -rho '\${CLAUDE_PLUGIN_ROOT}/references/[^\`]*' commands/*.md | sed 's|\${CLAUDE_PLUGIN_ROOT}/||' | sort -u | while read p; do test -f "$p" || echo "MISSING $p"; done` prints nothing.
    - `grep -c 'MINDRIAN_OS_ROOT' commands/*.md 2>/dev/null | grep -v ':0'` returns nothing (the
      long wrapper was not hand-written into any source command).
  </acceptance_criteria>
  <done>44 command files carry 93 anchored citations, the diff contains nothing but the inserted prefix, every anchored target resolves to a real file, and file-meeting.md is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate skill mirrors and prove nothing else moved</name>
  <files>skills/*/SKILL.md</files>
  <read_first>
    - `scripts/build-skill-mirrors.cjs` lines 95-135 (`applyPluginRootPortability`, Exception
      Class 3) -- to know exactly what wrapper the generator will inject and why.
    - `scripts/build-skill-mirrors.cjs` lines 160-180 (`SKIP_LIST`) and lines 560-600 (the
      skip-list divergence verification) -- so the `trending-to-absurd` skip is expected, not a
      surprise failure.
    - `skills/file-meeting/SKILL.md` line 49 -- the reference rendering of the fail-closed
      wrapper, to compare the newly generated mirrors against.
  </read_first>
  <action>
    Run `node scripts/build-skill-mirrors.cjs` in write mode. Record its own summary line
    (it prints an "overwritten N / unchanged M" count) into the plan summary verbatim -- expect
    44 overwritten and 68 unchanged, and if the numbers differ, investigate before proceeding
    rather than accepting the delta.

    Then run `node scripts/build-skill-mirrors.cjs --check` and confirm it reports
    `OK (112 mirrors match expected content; skip-list verified: trending-to-absurd)`.

    Confirm the generator applied Exception Class 3 to the new tokens: every regenerated mirror
    must carry the long `${MINDRIAN_OS_ROOT:-${CLAUDE_PLUGIN_ROOT:?...}}` form, not the bare
    `${CLAUDE_PLUGIN_ROOT}` form the command source uses. This is the whole point of letting the
    generator do it: hand-authored sources stay readable, mirrors get the foreign-host hardening.

    Then run the two release gates the file-meeting RCA specifically named as touching the same
    parsed files, so any collateral damage to the connector or render pipelines surfaces now
    rather than at release: `node scripts/build-connector-registry.cjs --check` and
    `node scripts/check-render-coverage.cjs`.

    Finally re-run `node tests/test-265-file-meeting-gates.cjs` to prove the 44-file sweep did
    not disturb the already-shipped fix's regression suite.
  </action>
  <verify>
    <automated>node scripts/build-skill-mirrors.cjs --check &amp;&amp; node scripts/build-connector-registry.cjs --check &amp;&amp; node scripts/check-render-coverage.cjs &amp;&amp; node tests/test-265-file-meeting-gates.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-skill-mirrors.cjs --check` exits 0 and its output contains both
      `112` and `trending-to-absurd`.
    - `git diff --numstat -- skills/ | wc -l` returns 44 (exactly the mirrors of the 44 swept
      commands, no more).
    - `git diff --stat -- skills/trending-to-absurd/SKILL.md` is empty (skip-listed, correctly
      untouched by the generator).
    - `git diff -U0 -- skills/ | grep '^+' | grep -v '^+++' | grep -vc 'MINDRIAN_OS_ROOT'`
      returns 0, proving every added mirror line carries the fail-closed wrapper.
    - `node scripts/build-connector-registry.cjs --check` exits 0.
    - `node scripts/check-render-coverage.cjs` exits 0.
    - `node tests/test-265-file-meeting-gates.cjs` exits 0 with 4/4 arms passing.
  </acceptance_criteria>
  <done>44 mirrors regenerated with the fail-closed wrapper applied by the generator, mirror check at 112/112 with the skip-list verified, both release gates green, and the file-meeting regression suite still 4/4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sed pattern -> 44 shipped command files | An over-broad pattern silently corrupts prose or frontmatter across the whole methodology surface |
| commands/ source -> skills/ mirrors | A hand-edited mirror diverges from the generator and fails `--check` at release |
| concurrent session -> same working tree | Another session's in-flight edit to commands/ gets swept over |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-271-06 | Tampering | 44 `commands/*.md` swept by sed | mitigate | Backtick-anchored pattern only; Task 1 acceptance requires every added line to contain `CLAUDE_PLUGIN_ROOT` and requires added-line count to equal removed-line count |
| T-271-07 | Tampering | `skills/*/SKILL.md` | mitigate | Never hand-edited; regenerated by `build-skill-mirrors.cjs` and gated by its own `--check` at 112/112 |
| T-271-08 | Tampering | Concurrent-session edits in the shared tree | mitigate | `git status --short` precondition in `<assumptions>`; STOP-and-report if any in-scope file is already modified |
| T-271-09 | Denial of Service | Dangling citation anchored to a nonexistent file | mitigate | Task 1 acceptance resolves every anchored path against disk and fails on any MISSING |

No package-manager installs occur in this plan.
</threat_model>

<verification>
1. `git diff --numstat -- commands/` -> 44 files, added == removed, 93 lines.
2. `git diff -U0 -- commands/ | grep '^+' | grep -v '^+++' | grep -vc 'CLAUDE_PLUGIN_ROOT'` -> 0.
3. `node scripts/build-skill-mirrors.cjs --check` -> OK 112/112, skip-list verified.
4. `node scripts/build-connector-registry.cjs --check` -> exit 0.
5. `node scripts/check-render-coverage.cjs` -> exit 0.
6. `node tests/test-265-file-meeting-gates.cjs` -> exit 0, 4/4.
7. `git diff --stat -- commands/file-meeting.md` -> empty.
</verification>

<success_criteria>
- 93 citations anchored across 44 files with a diff containing nothing but the inserted prefix.
- Every anchored path resolves to a real file under the plugin root.
- Mirrors regenerated by the generator, never by hand, still at 112/112.
- The already-shipped file-meeting fix is provably untouched and its suite still passes.
- The `trending-to-absurd` mirror asymmetry is recorded, not assumed away.
</success_criteria>

<output>
Create `.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/271-03-SUMMARY.md` when done.
</output>
