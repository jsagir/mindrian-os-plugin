---
phase: 271-bare-reference-path-resolution-audit
plan: 04
type: execute
wave: 4
depends_on: ["271-03"]
files_modified:
  - skills/larry-personality/SKILL.md
  - skills/room-passive/SKILL.md
  - skills/ui-system/SKILL.md
  - skills/pws-methodology/SKILL.md
  - skills/trending-to-absurd/SKILL.md
  - agents/brain-query.md
  - agents/framework-runner.md
  - agents/grading.md
  - agents/investor.md
  - agents/meeting-perspective-extractor.md
  - agents/research.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The five hand-authored skills that mirror regeneration never reaches are anchored by hand, so no surface is left behind by the sweep."
    - "All six agents that cite reference files are anchored, using a form verified to actually expand in agent markdown rather than assumed to."
    - "`node scripts/check-plugin-path-anchoring.cjs --check` exits 0 across the whole repo for the first time."
    - "`skills/trending-to-absurd/SKILL.md` remains divergent from its command, so the generator's skip-list verification still passes."
  artifacts:
    - path: "skills/larry-personality/SKILL.md"
      provides: "4 anchored citations (lines 211, 418, 458, 459) in the highest-count hand-authored skill"
      contains: "references/personality/voice-dna.md"
    - path: "agents/meeting-perspective-extractor.md"
      provides: "6 anchored citations (lines 31, 45, 46, 47, 54, 76) in the highest-count agent"
      contains: "references/meeting/extraction-perspectives.md"
  key_links:
    - from: "scripts/check-plugin-path-anchoring.cjs --check"
      to: "commands/, skills/, agents/, pipelines/"
      via: "fail-closed exit 0 across all four surfaces"
      pattern: "check-plugin-path-anchoring"
---

<objective>
Close the two surfaces the mirror generator cannot reach, and turn the gate green.

Mirror regeneration in plan 271-03 covered 44 of 45 commands and their generated mirrors. It
covered nothing else. Two blind spots remain, both discovered by the plan-time audit and both
invisible to the RCA that motivated this phase:

1. **Five hand-authored skills** carry 11 bare citations. Four of them
   (`larry-personality`, `room-passive`, `ui-system`, `pws-methodology`) have no command source
   at all, so the generator never writes them. The fifth (`trending-to-absurd`) has a command
   source but sits on the generator's `SKIP_LIST` because it is divergent by design. Running
   the generator a thousand times fixes none of these.
2. **Six agents** carry 15 bare citations, and `agents/` has ZERO existing
   `${CLAUDE_PLUGIN_ROOT}` usage anywhere in the repo. There is no local precedent proving the
   variable even expands in agent markdown, so this plan verifies it rather than assuming.

Purpose: leave no plugin markdown surface cwd-dependent.
Output: 26 anchored citations plus the first green `--check` run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/271-03-SUMMARY.md
@CLAUDE.md
</context>

<assumptions>
- **Shared working tree.** Re-run `git status --short` before the first edit. Phase 270's
  surface (`lib/mcp/`, `lib/core/navigation`, `lib/core/icm-forest.cjs`, `data/connector-*.json`)
  does not overlap this plan, but confirm no `skills/` or `agents/` file is already modified by
  another session.
- **Site counts verified at plan time, re-verify with the gate:** hand-authored skills
  `larry-personality` 4, `pws-methodology` 3, `ui-system` 2, `room-passive` 1,
  `trending-to-absurd` 1 (11 total); agents `meeting-perspective-extractor` 6,
  `framework-runner` 4, `brain-query` 2, `grading` 1, `investor` 1, `research` 1 (15 total).
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Verify the anchor form actually expands in agent markdown</name>
  <files>.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/271-AUDIT.md</files>
  <read_first>
    - `CLAUDE.md`, the "Consult ALL Relevant Grounding Sources During Dev Work (MANDATORY)"
      section -- it names `claude-code-guide` agent and the `claude-api` skill as the
      authoritative sources for "Claude Code's own hooks/matchers, MCP tool registration,
      subagent-registry behavior", which is exactly the question here.
    - `agents/framework-runner.md` lines 38-70 -- the citation sites, including the
      `references/methodology/{framework}.md` placeholder form at line 65 and the directory
      probe at line 69.
    - `lib/core/active-plugin-root.cjs` -- the repo's own runtime resolution helper, and the
      fallback the generator's fail-closed message points readers at.
  </read_first>
  <action>
    Answer one question with a citable source before editing any agent file: does
    `${CLAUDE_PLUGIN_ROOT}` expand inside `agents/*.md` (subagent prompt markdown), the same way
    it demonstrably does inside `commands/*.md`?

    This is not a rhetorical check. `grep -rc 'CLAUDE_PLUGIN_ROOT' agents/*.md` returns zero
    matches across all agents, so this repo has no working precedent to copy. The
    `file-meeting` RCA's own external confirmation (Claude Code plugins-reference plus GitHub
    issue anthropics/claude-code#9354) covered command and skill markdown, not agents.

    Consult, in this order, and record what each returned:
    1. The `claude-code-guide` agent or the `claude-api` skill on plugin variable expansion
       scope across the command / skill / agent / hook surfaces.
    2. If that is inconclusive, Context7 or the Claude Code plugins-reference docs directly.

    Record the finding as a new section 6 in `271-AUDIT.md` titled "Anchor form per surface",
    with the source cited by name and URL or tool call. Three possible outcomes, all acceptable:
    - CONFIRMED expands: use the short `${CLAUDE_PLUGIN_ROOT}/references/...` form in agents,
      matching commands.
    - CONFIRMED does not expand: record it, and anchor agents using the same fail-closed long
      form the skills use, OR flag agents as an out-of-scope finding for a follow-up phase if
      neither form works. Do not invent a third mechanism.
    - INCONCLUSIVE: say so plainly (this repo treats "not in the corpus" as a valid answer, per
      CLAUDE.md), adopt the short form on the strength of the command-surface precedent, and
      record the residual uncertainty as a named blind spot in the summary.

    Do not paper over a gap with an ungrounded guess, and do not treat a single source's miss as
    proof that no grounding exists.
  </action>
  <verify>
    <automated>grep -c 'Anchor form per surface' .planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/271-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `271-AUDIT.md` contains a section headed `Anchor form per surface`.
    - That section names at least one consulted source explicitly (tool name plus what it
      returned), and states one of the three outcomes verbatim: `CONFIRMED expands`,
      `CONFIRMED does not expand`, or `INCONCLUSIVE`.
    - The section states which literal anchor form Task 2 and Task 3 must use for `agents/` and
      which for `skills/`, as two separate lines.
    - `grep -v '^#' 271-AUDIT.md | grep -c '—'` returns 0.
  </acceptance_criteria>
  <done>The agent-surface anchor form is decided on cited evidence, recorded in the audit, and handed to the two edit tasks as a literal string.</done>
</task>

<task type="auto">
  <name>Task 2: Anchor the five hand-authored skills</name>
  <files>skills/larry-personality/SKILL.md, skills/room-passive/SKILL.md, skills/ui-system/SKILL.md, skills/pws-methodology/SKILL.md, skills/trending-to-absurd/SKILL.md</files>
  <read_first>
    - `skills/file-meeting/SKILL.md` line 49 -- the exact rendering of the fail-closed wrapper
      the generator produces, which these hand-authored skills must match character for
      character so the two skill populations read identically.
    - `scripts/build-skill-mirrors.cjs` lines 560-600 -- the `SKIP_LIST` divergence
      verification, which fails if `skills/trending-to-absurd/SKILL.md` ever "reverted to a
      plain copy of its command". Confirm the edit keeps it divergent.
    - `271-AUDIT.md` section 6 (from Task 1) -- the decided skill anchor form.
  </read_first>
  <action>
    Anchor all 11 citations across the five files. These are skills, which can be loaded by
    foreign Agent-Skills hosts that do not set `CLAUDE_PLUGIN_ROOT` at all, so use the SAME
    fail-closed long form the generator emits into every other mirror:

    `${MINDRIAN_OS_ROOT:-${CLAUDE_PLUGIN_ROOT:?MindrianOS install root not found. Set MINDRIAN_OS_ROOT (see lib/core/active-plugin-root.cjs) or run from Claude Code.}}/references/...`

    Copy that string from `skills/file-meeting/SKILL.md:49` rather than retyping it; a single
    character of drift makes the two skill populations inconsistent and defeats the point.

    Per-file site list to work through (verify against the gate's live report first):
    - `skills/larry-personality/SKILL.md` lines 211, 418, 458, 459
    - `skills/pws-methodology/SKILL.md` lines 24, 62, 92 (line 62 carries THREE citations on one
      line: `references/methodology/triz-matrix.json`, `triz-principles.md`,
      `sapphire-encoding.md` -- anchor all three)
    - `skills/ui-system/SKILL.md` lines 394, 401
    - `skills/room-passive/SKILL.md` line 83
    - `skills/trending-to-absurd/SKILL.md` line 89

    Several of these are DESCRIPTIVE PROSE rather than Read instructions (for example
    `ui-system/SKILL.md:394` "will consolidate to `references/visual/palette.json`"). Anchor them
    anyway, per the disposition rule recorded in `271-AUDIT.md` section 3: every cited target was
    verified to exist under the plugin root, so anchoring makes the prose accurate rather than
    merely verbose, and a split rule would need per-site judgment the gate cannot enforce.

    After editing, run `node scripts/build-skill-mirrors.cjs --check` and confirm it still
    reports 112/112 with the skip-list verified. The four command-less skills are outside the
    generator's expected-content set entirely; `trending-to-absurd` must still register as
    divergent.
  </action>
  <verify>
    <automated>node scripts/build-skill-mirrors.cjs --check &amp;&amp; test $(git diff --numstat -- skills/larry-personality skills/room-passive skills/ui-system skills/pws-methodology skills/trending-to-absurd | awk '{s+=$1} END {print s}') -eq 11</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat` over the five files shows 5 files changed, 11 added and 11 removed
      lines (pure in-place substitution).
    - `git diff -U0 -- skills/larry-personality skills/room-passive skills/ui-system skills/pws-methodology skills/trending-to-absurd | grep '^+' | grep -v '^+++' | grep -vc 'MINDRIAN_OS_ROOT'` returns 0.
    - The wrapper string is byte-identical to `skills/file-meeting/SKILL.md:49`'s: verify with
      `grep -oh 'MINDRIAN_OS_ROOT:-\${CLAUDE_PLUGIN_ROOT:?[^}]*}' skills/*/SKILL.md | sort -u | wc -l` returning 1.
    - `node scripts/build-skill-mirrors.cjs --check` exits 0, output contains `112` and
      `trending-to-absurd`, and contains no `SKIP-LIST NOT DIVERGENT` failure.
    - Every anchored target still resolves on disk (same MISSING-check shape as plan 271-03).
  </acceptance_criteria>
  <done>11 citations anchored across the five generator-unreachable skills using a byte-identical wrapper, with the mirror gate and its skip-list divergence check both still green.</done>
</task>

<task type="auto">
  <name>Task 3: Anchor the six agents and turn the gate green</name>
  <files>agents/brain-query.md, agents/framework-runner.md, agents/grading.md, agents/investor.md, agents/meeting-perspective-extractor.md, agents/research.md</files>
  <read_first>
    - `271-AUDIT.md` section 6 (from Task 1) -- the decided agent anchor form, as a literal
      string.
    - `agents/framework-runner.md` line 69 -- the directory-probe citation ("try
      `references/methodology/` with common variations"), which is a bare DIRECTORY not a file
      and must be anchored too, since the probe is exactly as cwd-dependent as a file read.
  </read_first>
  <action>
    Anchor all 15 citations across the six agents using the form Task 1 decided and recorded.

    Per-file site list (verify against the gate's live report first):
    - `agents/meeting-perspective-extractor.md` lines 31, 45, 46 (two citations on line 46),
      47, 54, 76
    - `agents/framework-runner.md` lines 40, 65, 66, 69
    - `agents/brain-query.md` lines 43, 44
    - `agents/research.md` line 48
    - `agents/investor.md` line 59
    - `agents/grading.md` line 97

    Two sites need care and must not be skipped:
    - `agents/framework-runner.md:65` uses a `{framework}` placeholder
      (`references/methodology/{framework}.md`). Anchor the prefix; leave the placeholder
      intact. The gate's `targetExists` check will flag this one as unresolvable, which is
      correct and expected for a templated path -- record it in the summary so it is not mistaken
      for a dangling citation.
    - `agents/framework-runner.md:69` cites a bare DIRECTORY, `references/methodology/`. Anchor
      it; a directory probe from the wrong cwd fails identically to a file read.

    Then run the full phase aggregator: `bash tests/run-all-271.sh`. This is the moment the
    `--check` arm flips from its intended RED baseline (plan 271-01) to green. If it does not,
    read the gate's own recovery line and close the named sites rather than relaxing the gate.
  </action>
  <verify>
    <automated>bash tests/run-all-271.sh</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat -- agents/` shows 6 files changed with added lines equal to removed
      lines, 15 total.
    - `git diff -U0 -- agents/ | grep '^+' | grep -v '^+++' | grep -vc 'CLAUDE_PLUGIN_ROOT'`
      returns 0.
    - `node scripts/check-plugin-path-anchoring.cjs --check` exits 0 for the first time in the
      phase.
    - `bash tests/run-all-271.sh` exits 0 with all four arms green: the fixture test, the
      anchoring gate, `build-skill-mirrors --check` at 112/112, and
      `test-265-file-meeting-gates` at 4/4.
    - `node scripts/check-plugin-path-anchoring.cjs --report | grep -c 'MISSING-TARGET'` returns
      exactly 1, and that one hit is `agents/framework-runner.md:65`'s `{framework}` placeholder.
      Any other MISSING-TARGET is a real dangling citation and blocks the task.
    - `node scripts/build-connector-registry.cjs --check`, `node scripts/check-render-coverage.cjs`,
      and `node scripts/check-shape-declaration.cjs` all exit 0.
  </acceptance_criteria>
  <done>15 agent citations anchored, the repo-wide gate exits 0 for the first time, and the full phase aggregator is green across all four arms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unverified env-var assumption -> agents/ | Anchoring with a variable that does not expand ships a literal `${CLAUDE_PLUGIN_ROOT}` string into every agent path, which is strictly worse than the current bare path |
| hand-edited skill -> generator skip-list verification | An over-eager edit could make a skip-listed skill look like a plain copy of its command and fail the release gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-271-10 | Denial of Service | `agents/*.md` anchored with a non-expanding variable | mitigate | Task 1 is a dedicated verification task consulting `claude-code-guide` / `claude-api` before any agent edit, with three named outcomes including "flag as out of scope" |
| T-271-11 | Tampering | `skills/trending-to-absurd/SKILL.md` skip-list status | mitigate | Task 2 acceptance requires `build-skill-mirrors --check` to report no `SKIP-LIST NOT DIVERGENT` failure |
| T-271-12 | Elevation of Privilege | Relaxing the gate to make it green | mitigate | Task 3 action explicitly forbids relaxing the gate; the recovery path is closing named sites, and the `MISSING-TARGET` count is pinned to exactly 1 with its identity named |

No package-manager installs occur in this plan.
</threat_model>

<verification>
1. `271-AUDIT.md` carries an `Anchor form per surface` section with a cited source.
2. `git diff --numstat -- skills/ agents/` -> 11 files, added == removed, 26 lines.
3. `node scripts/check-plugin-path-anchoring.cjs --check` -> exit 0.
4. `bash tests/run-all-271.sh` -> exit 0, all four arms green.
5. `node scripts/build-skill-mirrors.cjs --check` -> OK 112/112, skip-list verified.
6. Release gates `build-connector-registry --check`, `check-render-coverage`,
   `check-shape-declaration` -> all exit 0.
</verification>

<success_criteria>
- No plugin markdown surface is left cwd-dependent for its reference citations.
- The agent anchor form rests on a cited source, not on an assumption copied from the command
  surface.
- The generator-unreachable skills are handled by hand and recorded as such, so a future reader
  does not assume mirror regeneration covered them.
- The gate is green, and it got there by closing sites rather than by loosening the predicate.
</success_criteria>

<output>
Create `.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/271-04-SUMMARY.md` when done.
</output>
