---
phase: 271-bare-reference-path-resolution-audit
plan: 05
type: execute
wave: 5
depends_on: ["271-04"]
files_modified:
  - scripts/verify-release
  - CHANGELOG.md
  - .planning/ROADMAP.md
  - .planning/debug/knowledge-base.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A newly authored command citing a bare `references/...` path fails the release gate before it can ship."
    - "The fix is recorded in CHANGELOG so the next release picks it up, since a dev-repo commit alone is not live."
    - "The bare `scripts/` invocation class is registered as a named follow-up phase with its measured count, not dropped."
    - "The reasoning trail exists in both the phase directory and the rethinking-mindrianos room, cross-linked in both directions."
  artifacts:
    - path: "scripts/verify-release"
      provides: "The bare-plugin-path gate wired into the release five-gate lockstep"
      contains: "check-plugin-path-anchoring"
    - path: "CHANGELOG.md"
      provides: "Unreleased Fixed entry naming the sweep and its user-visible consequence"
      contains: "CLAUDE_PLUGIN_ROOT"
    - path: ".planning/debug/knowledge-base.md"
      provides: "Summary block closing the file-meeting RCA's named follow-up audit"
  key_links:
    - from: "scripts/verify-release"
      to: "scripts/check-plugin-path-anchoring.cjs"
      via: "node invocation with --check, fail-closed"
      pattern: "check-plugin-path-anchoring\\.cjs"
    - from: ".planning/ROADMAP.md Phase 271 entry"
      to: "the new bare-scripts follow-up phase entry"
      via: "named cross-reference in both directions"
      pattern: "bare.*script"
---

<objective>
Make the guard structural, close the paper trail, and hand off what this phase deliberately did
not fix.

Two standing project rules bear directly on this plan. First, confirmed four independent times
over three weeks: **a commit on `main` is NOT live until a release ships and is actually picked
up.** A CHANGELOG entry is how this fix reaches a real user. Second, CLAUDE.md's Dev-Research
Compositing mandate: every GSD phase touching MindrianOS's own architecture files in BOTH the
phase directory and the `rethinking-mindrianos` room, cross-linked, and "a room entry that
reaches a verdict and never lands in a phase/seed" counts as incomplete.

And one lesson this phase exists to encode: the file-meeting RCA fixed one file and named the
structural guard as future work. That guard only becomes real when it runs at release time.

Purpose: make the class unable to reappear, and be honest about the class this phase did not fix.
Output: release-gate wiring, changelog and knowledge-base entries, the compositing trail, and
the follow-up phase registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/271-04-SUMMARY.md
@.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/271-AUDIT.md
@CLAUDE.md
</context>

<assumptions>
- **Shared working tree, and this plan touches three shared files.** `scripts/verify-release`,
  `CHANGELOG.md`, and `.planning/ROADMAP.md` are high-traffic files other sessions edit. Re-run
  `git status --short` and `git log --oneline -3` immediately before each edit; if any is
  already modified, read the pending change first and append rather than overwrite.
- **Known `phase.add` heading bug.** `docs/2026-08-27-HANDOFF-goal-directed-phase-sweep-265-271.md`
  names a `phase.add` heading bug among the recurring GSD tooling failures hit during this
  sweep. Edit `.planning/ROADMAP.md` directly with Edit, not via `phase.add`, and verify the
  resulting heading renders as `### Phase <N>: <title>` matching its siblings.
- **No release cut.** The same handoff carries an explicit do-not list including "no
  `release.sh` without go-ahead". This plan writes the CHANGELOG entry and stops there.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Wire the gate into the release lockstep</name>
  <files>scripts/verify-release</files>
  <read_first>
    - `scripts/verify-release` lines 320-345 -- the `build-skill-mirrors.cjs --check` gate block
      (`MIRROR_OUT` / `MIRROR_CODE`), the closest structural sibling: same surface, same
      fail-closed shape. Copy its block structure exactly.
    - `scripts/verify-release` lines 470-510 -- the `check-kuzu-reintroduction` and
      `check-gate-seam` blocks, for the house convention on the explanatory comment preceding
      each gate.
  </read_first>
  <action>
    Add one gate block to `scripts/verify-release`, placed immediately after the existing
    `build-skill-mirrors.cjs --check` block so the two markdown-surface gates sit together.

    Follow the surrounding convention exactly: a comment block explaining WHAT the gate proves
    and WHY it exists, then
    `ANCHOR_OUT=$(node "$PLUGIN_ROOT/scripts/check-plugin-path-anchoring.cjs" --check 2>&1) && ANCHOR_CODE=0 || ANCHOR_CODE=$?`
    followed by the `pass`/`fail` branch matching its siblings.

    The comment must state the one sentence a reader six months from now needs: bare
    plugin-relative paths in command, skill, and agent markdown resolve against the session's
    current working directory, not the plugin install directory, so they work by coincidence in
    this dev repo and fail in every real Data Room. Cite the originating RCA by path,
    `.planning/debug/resolved/file-meeting-missing-reference-files.md`, and note that the gate
    exists because that RCA fixed one file and named the structural guard as missing work.

    Do NOT wire this into `doctor.cjs --acceptance` in this plan. `doctor.cjs` is a
    higher-traffic shared file, and the release gate alone satisfies the RCA's named
    requirement; adding it there is a separate, lower-value edit not worth the collision risk in
    a shared tree. Record that deliberate omission in the summary.

    Prove the gate actually fires rather than assuming it. Create a throwaway fixture file under
    `os.tmpdir()` containing a bare citation, point the checker at it, and confirm it exits 1;
    then confirm the real repo run exits 0. Never leave a deliberately broken file in the tree.
  </action>
  <verify>
    <automated>grep -c 'check-plugin-path-anchoring' scripts/verify-release &amp;&amp; bash scripts/verify-release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'check-plugin-path-anchoring.cjs' scripts/verify-release` returns at least 1.
    - `grep -c 'file-meeting-missing-reference-files' scripts/verify-release` returns at least 1
      (the RCA is cited by path in the gate comment).
    - `bash scripts/verify-release` runs to completion and its output contains a PASS line
      naming the anchoring gate.
    - `git diff --numstat -- scripts/verify-release` shows 0 removed lines (pure addition,
      nothing displaced).
    - The summary records in one sentence that `doctor.cjs --acceptance` wiring was deliberately
      omitted, and why.
  </acceptance_criteria>
  <done>The anchoring gate runs inside `scripts/verify-release`, its comment cites the originating RCA by path, and the gate was proven to fire against a fixture rather than assumed to.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG, ROADMAP, knowledge-base, and the follow-up phase registration</name>
  <files>CHANGELOG.md, .planning/ROADMAP.md, .planning/debug/knowledge-base.md</files>
  <read_first>
    - `CHANGELOG.md` lines 1-40 -- the current top entry, to match its heading level and its
      Fixed / Added subsection convention.
    - `.planning/ROADMAP.md` lines 573-583 -- the Phase 271 entry, including its
      `**Plans:** 0 plans` line and its `- [ ] TBD` placeholder list.
    - `.planning/debug/knowledge-base.md` -- the last two summary blocks, for the exact block
      shape this repo uses on RCA resolve.
    - `.planning/debug/resolved/file-meeting-missing-reference-files.md`, "Non-Code Follow-ups"
      -- it names this exact audit as the follow-up and names the CHANGELOG and knowledge-base
      entries as required. This task closes those three items.
  </read_first>
  <action>
    **CHANGELOG.md.** Add a `Fixed` entry under the current unreleased heading (create the
    unreleased heading only if none exists; do not invent a version number and do not run
    `release.sh`). Wording in the house Feynman register: name the counts, the surfaces, and the
    user-visible consequence. Shape to follow, using the LIVE counts from `271-AUDIT.md` rather
    than these illustrative ones: "Reference-file citations across 44 commands, 5 hand-authored
    skills, and 6 agents are now anchored with `${CLAUDE_PLUGIN_ROOT}`, so their Setup steps load
    correctly from a real Data Room instead of only from the plugin dev repo. Added
    `scripts/check-plugin-path-anchoring.cjs` to the release gates so the class cannot silently
    return." No em-dashes.

    **ROADMAP.md, Phase 271 entry.** Replace `**Plans:** 0 plans` with
    `**Plans:** 5/5 plans complete`, and replace the `- [ ] TBD` placeholder with the
    wave-grouped checked list:
    - Wave 1: `271-01-PLAN.md` - the anchoring gate, its fixture test, the aggregator, and the
      RED-baseline audit register
    - Wave 2: `271-02-PLAN.md` - the `/mos:radar` dev-repo-cwd disposition and the reasoned
      allowlist
    - Wave 3: `271-03-PLAN.md` - 93 citations anchored across 44 commands plus mirror regeneration
    - Wave 4: `271-04-PLAN.md` - 26 citations anchored across 5 hand-authored skills and 6 agents,
      gate green
    - Wave 5: `271-05-PLAN.md` - release-gate wiring, changelog, compositing trail, follow-up
      registration

    Append a short outcome paragraph to the Phase 271 entry answering the two questions the
    ROADMAP's own scoping text asked:
    (a) YES, the bug class extends beyond `references/`. Give the measured bare `scripts/`
    invocation count from `271-AUDIT.md` section 5, and name the 3 sites in
    `commands/file-meeting.md` (lines 771, 978, 983) that the RCA's references-only fix did not
    cover, plus the `commands/status.md:13` permission-matcher exclusion as an example of a
    match that is NOT a defect.
    (b) The `/mos:radar` exclusion and the human ruling behind it, quoting the reason.
    Also record the denominator reconciliation in one sentence (RCA said 44 of 121, ROADMAP said
    45 of 113, the live audit found 45 of 113 with `file-meeting.md` already fixed and excluded).

    **ROADMAP.md, new follow-up phase.** Register a new phase for the bare `scripts/` invocation
    class. Edit the file directly with Edit; do NOT use `phase.add` (known heading bug, see
    `<assumptions>`). Match the sibling section shape exactly: `### Phase <N>: <title>`, a
    `**Goal:**` paragraph carrying the measured count and named file:line evidence, a
    `**Requirements**: TBD` line, a `**Depends on:**` line naming Phase 271 as the phase that
    measured the class and `scripts/check-plugin-path-anchoring.cjs --include-scripts` as the
    ready-made measuring instrument, `**Plans:** 0 plans`, and a `- [ ] TBD` placeholder. Read
    the existing headings to pick the next free phase number; do not assume 272 is free.

    **knowledge-base.md.** Add the summary block the file-meeting RCA's Non-Code Follow-ups
    section requires, recording that its named "Follow-up audit (separate, out of scope for this
    RCA)" item is now closed by Phase 271, with the final counts, the guard's script path, and
    the one residual blind spot the RCA itself named and this phase also could not close: no
    live end-user session with cwd set to an actual separate Data Room was available to verify
    against, so verification is by path resolution plus gates, not by live invocation.
  </action>
  <verify>
    <automated>grep -c 'CLAUDE_PLUGIN_ROOT' CHANGELOG.md &amp;&amp; grep -c '271-05-PLAN.md' .planning/ROADMAP.md &amp;&amp; grep -c 'check-plugin-path-anchoring' .planning/debug/knowledge-base.md</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` contains a Fixed entry mentioning `CLAUDE_PLUGIN_ROOT` and
      `check-plugin-path-anchoring.cjs`, under an unreleased heading, with no new version number
      invented.
    - `.planning/ROADMAP.md` Phase 271 entry contains `5/5 plans complete` and all five
      `271-0N-PLAN.md` filenames.
    - The Phase 271 outcome paragraph contains the literal strings `commands/file-meeting.md`
      and `commands/status.md:13`, and the measured bare-`scripts/` count.
    - A new `### Phase ` heading exists for the bare-scripts follow-up, its number is not already
      used elsewhere in the file (`grep -c '### Phase <N>:'` returns exactly 1), and it contains
      `--include-scripts`.
    - `.planning/debug/knowledge-base.md` gained one summary block naming Phase 271 and the
      residual live-session blind spot.
    - `grep -v '^#' CHANGELOG.md | head -60 | grep -c '—'` returns 0 (no em-dashes in the new
      entry).
  </acceptance_criteria>
  <done>The changelog carries the Fixed entry, ROADMAP records 5/5 plus both scoping answers plus the reconciled denominator, a follow-up phase is registered with a free number and the measuring instrument named, and the knowledge-base closes the RCA's follow-up item honestly including the blind spot.</done>
</task>

<task type="auto">
  <name>Task 3: Dev-Research Compositing trail, cross-linked both directions</name>
  <files>.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/271-AUDIT.md</files>
  <read_first>
    - `CLAUDE.md`, the "Dev-Research Compositing (Rethinking Room)" section -- the mandate's
      exact terms: same finding, two homes, cross-linked, and neither substitutes for the other.
    - `~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-memory-context-operator/` (or
      whatever the most recent dated research entry is) -- the house shape for a room research
      entry, so this one matches its siblings.
    - `271-AUDIT.md` in full -- the material being mirrored.
  </read_first>
  <action>
    Create the durable reasoning trail at
    `~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-bare-plugin-path-resolution/`
    (adjust the date to the actual execution date). It carries the REASONING and EVIDENCE, not a
    copy of the executable plan: why bare plugin-relative paths are a structural authoring
    hazard rather than a typo class, why the dev repo's own `references/` directory made the
    defect invisible for the entire life of the project, why a one-file fix predictably left 55
    more files broken, and why the durable answer is a gate rather than a sweep.

    Include the cross-domain observation worth keeping: this is the SECOND time this exact
    disease class was fixed in this repo (the first was
    `.planning/debug/resolved/intern-w1-rooms-skill-script-path.md`, which fixed
    `bash scripts/<name>` in 11 files), and BOTH fixes were scoped by grep pattern rather than by
    defect class, which is precisely why each one left the other pattern untouched. Name that as
    the generalizable lesson: scope a portability fix by the RESOLUTION MECHANISM it repairs, not
    by the string that happened to surface it.

    Mirror the entry to `mindrianOS/research/` as source-of-record per the mandate.

    Then close the loop in both directions:
    - Add a `Room mirror:` line to `271-AUDIT.md` naming the room path.
    - Add a line in the room entry naming this phase directory and the ROADMAP Phase 271 entry.
    Verify BOTH links resolve with `fs.existsSync` or `test -e`, the same way plan 267.1-04
    verified its own bidirectional cross-link, rather than assuming the paths are right.
  </action>
  <verify>
    <automated>test -d ~/MindrianRooms/rethinking-mindrianos/research/*bare-plugin-path* &amp;&amp; grep -c 'rethinking-mindrianos' .planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/271-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - A dated research directory exists under
      `~/MindrianRooms/rethinking-mindrianos/research/` whose name contains
      `bare-plugin-path`, and it is mirrored under `mindrianOS/research/`.
    - `271-AUDIT.md` contains a `Room mirror:` line whose path passes `test -e`.
    - The room entry contains the literal string
      `.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite` and
      that path passes `test -e`.
    - The room entry names `intern-w1-rooms-skill-script-path.md` and states the
      scope-by-mechanism-not-by-string lesson explicitly.
    - Both link checks are run and their output recorded in the summary, not asserted.
  </acceptance_criteria>
  <done>The reasoning trail exists in the room and its mirror, both cross-links are verified to resolve, and the generalizable lesson about scoping portability fixes by mechanism is recorded where the next architecture session will find it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-repo commit -> live user install | A fix that never reaches a release is invisible to every user, and a passing local gate creates false confidence that it shipped |
| concurrent session -> shared CHANGELOG / ROADMAP / verify-release | An overwrite silently destroys another session's in-flight entry |
| gate wired but never exercised | A gate added to `verify-release` that does not actually fire gives a false all-clear on every future release |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-271-13 | Spoofing | A `verify-release` gate that passes vacuously | mitigate | Task 1 requires proving the gate exits 1 against a temp fixture containing a bare citation BEFORE accepting the green run on the real tree |
| T-271-14 | Tampering | Concurrent-session edits to `CHANGELOG.md`, `.planning/ROADMAP.md`, `scripts/verify-release` | mitigate | `git status --short` precondition per file in `<assumptions>`; append rather than overwrite when a pending change exists |
| T-271-15 | Repudiation | The unfixed bare-`scripts/` class silently dropped | mitigate | Task 2 registers it as a numbered ROADMAP phase carrying its measured count, its file:line evidence, and the ready-made `--include-scripts` measuring instrument |
| T-271-16 | Information Disclosure | Room research entry under `~/MindrianRooms/` | accept | Contains repo file paths and design reasoning only; no user room content, no Brain content, no credentials. Canon Part 8 is not engaged because nothing crosses the LOCAL to BRAIN boundary |

No package-manager installs occur in this plan, so no `T-271-SC` supply-chain row applies.
</threat_model>

<verification>
1. `grep -c 'check-plugin-path-anchoring.cjs' scripts/verify-release` -> at least 1.
2. `bash scripts/verify-release` -> completes with the new gate PASSing.
3. `bash tests/run-all-271.sh` -> still exit 0, all four arms green after the wiring change.
4. `CHANGELOG.md` carries the unreleased Fixed entry; no version number was invented and
   `release.sh` was not run.
5. `.planning/ROADMAP.md` Phase 271 reads `5/5 plans complete` with both scoping answers
   recorded, and the new follow-up phase heading uses a free number.
6. Both compositing cross-links resolve under `test -e`.
</verification>

<success_criteria>
- The defect class is blocked at release time, not just cleaned up once.
- The fix has a CHANGELOG entry, which is the only thing that makes it reachable by a real user.
- The class this phase deliberately did not fix has a number, a count, evidence, and an
  instrument, so it is handed off rather than forgotten.
- The reasoning trail lives in both homes with verified cross-links, per the compositing mandate.
- The residual blind spot the RCA named (no live separate-cwd Data Room session available) is
  carried forward honestly rather than quietly closed.
</success_criteria>

<output>
Create `.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/271-05-SUMMARY.md` when done.
</output>
