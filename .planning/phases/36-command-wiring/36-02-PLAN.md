---
phase: 36-command-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/speakers.md
  - commands/reanalyze.md
  - commands/graph.md
autonomous: true
requirements:
  - WIRE-03
  - WIRE-04
  - WIRE-05

must_haves:
  truths:
    - "User types /mos:speakers and sees formatted speaker profiles from filed meetings"
    - "User types /mos:reanalyze and intelligence re-runs on filed meetings with progress feedback"
    - "User types /mos:graph and gets natural language KuzuDB exploration"
    - "All three commands show 3-line error when prerequisites are missing"
  artifacts:
    - path: "commands/speakers.md"
      provides: "/mos:speakers command wiring to room/team/ speaker profiles"
      contains: "team"
    - path: "commands/reanalyze.md"
      provides: "/mos:reanalyze command wiring to compute-meetings-intelligence"
      contains: "compute-meetings-intelligence"
    - path: "commands/graph.md"
      provides: "/mos:graph command wiring to lazygraph-ops.cjs"
      contains: "lazygraph-ops"
  key_links:
    - from: "commands/speakers.md"
      to: "room/team/"
      via: "directory read for PROFILE.md files"
      pattern: "team.*PROFILE"
    - from: "commands/reanalyze.md"
      to: "scripts/compute-meetings-intelligence"
      via: "bash invocation"
      pattern: "compute-meetings-intelligence"
    - from: "commands/graph.md"
      to: "lib/core/lazygraph-ops.cjs"
      via: "node require for queryGraph"
      pattern: "lazygraph-ops"
---

<objective>
Create three markdown command files that wire /mos:speakers, /mos:reanalyze, and /mos:graph to existing meeting and graph infrastructure.

Purpose: Users can view speaker profiles, re-run meeting intelligence, and explore their knowledge graph through simple slash commands with Larry providing natural-language results.
Output: commands/speakers.md, commands/reanalyze.md, commands/graph.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-command-wiring/36-CONTEXT.md

@commands/splash.md
@commands/onboard.md
@skills/ui-system/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /mos:speakers command (WIRE-03)</name>
  <files>commands/speakers.md</files>
  <action>
Create commands/speakers.md following the command pattern from splash.md and onboard.md.

**read_first:** commands/splash.md, commands/onboard.md, scripts/create-speaker-profile (first 30 lines for folder structure)

**YAML frontmatter:**
- name: speakers
- description: See who has been in your meetings -- roles, expertise, and what they brought to the table
- body_shape: C (Room Card)
- body_shape_detail: Each speaker as a card with role, expertise, meeting count
- ui_reference: skills/ui-system/SKILL.md
- allowed-tools: Read, Bash

**Body instructions for Larry:**

1. **Pre-flight check:** Check if room/team/ directory exists and contains any subdirectories. Per D-08, if no speakers exist:
   ```
   x No speaker profiles found
     Why: Speaker profiles are created when you file meetings
     Fix: Tell me about a meeting you had -- I will identify the speakers
   ```

2. **Discover speakers** per D-06: Scan room/team/ recursively. The structure is room/team/{role-plural}/{speaker-slug}/PROFILE.md. Read each PROFILE.md found.

3. **Present in Room Card format** per D-07 (Body Shape C). For each speaker show:
   - Display name and role
   - Expertise areas (from PROFILE.md)
   - Number of meetings they appeared in (count meeting references)
   - Key insights or advice attributed to them (from insights/ subfolder if populated)

4. **Natural language framing:** Larry introduces the list: "Here is everyone who has been part of your conversations so far:" and after listing: "Want to dig deeper into anyone's contributions? Just ask."

5. **Zone 4 (Action Footer):** Suggest: "Want to see what patterns emerged across meetings? /mos:reanalyze" and "File another meeting: just paste a transcript or tell me about it."

**acceptance_criteria:**
- File exists at commands/speakers.md
- Contains YAML frontmatter with name: speakers
- Contains reference to room/team/ directory scanning
- Contains Body Shape C / Room Card reference
- Contains 3-line error for missing speakers
  </action>
  <verify>
    <automated>grep -q "name: speakers" commands/speakers.md && grep -q "team" commands/speakers.md && grep -q "PROFILE" commands/speakers.md && grep -q "body_shape" commands/speakers.md && grep -q "No speaker" commands/speakers.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>commands/speakers.md exists with correct frontmatter, reads room/team/ profiles, presents as Room Cards, has error handling</done>
</task>

<task type="auto">
  <name>Task 2: Create /mos:reanalyze command (WIRE-04)</name>
  <files>commands/reanalyze.md</files>
  <action>
Create commands/reanalyze.md following the command pattern.

**read_first:** commands/splash.md, scripts/compute-meetings-intelligence (first 30 lines for usage)

**YAML frontmatter:**
- name: reanalyze
- description: Re-run intelligence on your filed meetings -- find new patterns, connections, and action items
- body_shape: E (Action Report)
- body_shape_detail: Before/after delta showing new insights discovered
- ui_reference: skills/ui-system/SKILL.md
- allowed-tools: Bash, Read

**Body instructions for Larry:**

1. **Pre-flight check:** Check if room/meetings/ directory exists and contains meeting subdirectories. Per D-11, if no meetings:
   ```
   x No meetings found
     Why: You need filed meetings before intelligence can run
     Fix: Tell me about a meeting -- paste a transcript or describe what happened
   ```

2. **Capture before-state** per D-10: Before running, read room/MEETINGS-INTELLIGENCE.md if it exists. Note what insights/connections/action-items existed before.

3. **Run intelligence** per D-09:
   ```bash
   bash "${CLAUDE_PLUGIN_ROOT}/scripts/compute-meetings-intelligence" room/
   ```

4. **Show before/after delta** per D-10 (Body Shape E -- Action Report): Read the new MEETINGS-INTELLIGENCE.md and compare. Larry reports:
   - How many meetings were analyzed
   - New convergence signals found (topics appearing across 3+ meetings)
   - New action items extracted
   - New contradictions or changes detected
   - Frame as: "Re-analyzed N meetings. Found X new patterns since last run."

5. **Natural language framing:** Larry explains what changed: "After looking at everything again, here is what stands out now that was not visible before..."

6. **Zone 4 (Action Footer):** Suggest: "Want to see the full graph of connections? /mos:dashboard" and "See who contributed most? /mos:speakers"

**acceptance_criteria:**
- File exists at commands/reanalyze.md
- Contains YAML frontmatter with name: reanalyze
- Contains reference to compute-meetings-intelligence script
- Contains before/after delta logic description
- Contains 3-line error for missing meetings
  </action>
  <verify>
    <automated>grep -q "name: reanalyze" commands/reanalyze.md && grep -q "compute-meetings-intelligence" commands/reanalyze.md && grep -q "body_shape" commands/reanalyze.md && grep -q "No meetings" commands/reanalyze.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>commands/reanalyze.md exists with correct frontmatter, wires to compute-meetings-intelligence, shows before/after delta, has error handling</done>
</task>

<task type="auto">
  <name>Task 3: Create /mos:graph command (WIRE-05)</name>
  <files>commands/graph.md</files>
  <action>
Create commands/graph.md following the command pattern.

**read_first:** commands/splash.md, lib/core/lazygraph-ops.cjs (first 50 lines for exports: queryGraph, graphStats)

**YAML frontmatter:**
- name: graph
- description: Explore your knowledge graph -- ask questions about connections, patterns, and gaps in natural language
- body_shape: C (Room Card)
- body_shape_detail: Query results as cards, graph stats as header
- ui_reference: skills/ui-system/SKILL.md
- allowed-tools: Bash, Read

**Body instructions for Larry:**

1. **Pre-flight check:** Check if room/.lazygraph/ directory exists. Per D-14, if KuzuDB is empty or missing:
   ```
   x No knowledge graph found
     Why: The graph builds as you file content -- meetings, documents, methodology sessions
     Fix: Tell me about a meeting or paste a document to start building your graph
   ```

2. **Mode: Interactive exploration** per D-12 and D-13. Larry wraps lib/core/lazygraph-ops.cjs for natural language graph queries. The user asks questions in plain English and Larry translates them to KuzuDB Cypher queries using the lazygraph-ops queryGraph function.

3. **On first invocation:** Show graph stats first (using graphStats from lazygraph-ops.cjs). Larry says: "Your graph has N nodes and M edges across K sections. Here is what I can help you explore:" then lists example questions:
   - "What connects problem-definition to market-analysis?"
   - "Where are the contradictions?"
   - "Which sections have the most connections?"
   - "What topics appear in 3 or more sections?"

4. **Query translation** per D-13: When user asks a question, Larry translates to appropriate KuzuDB query using the exports from lazygraph-ops.cjs. Present results as Room Cards (Body Shape C) showing node title, section, and relationship context.

5. **Natural language framing:** Larry explains graph results in context: "The strongest connection is between X and Y -- they share three convergence points..." Not raw query results.

6. **Zone 4 (Action Footer):** Suggest: "Want to see this visually? /mos:dashboard" and "Want to re-run the analysis? /mos:reanalyze"

**acceptance_criteria:**
- File exists at commands/graph.md
- Contains YAML frontmatter with name: graph
- Contains reference to lazygraph-ops.cjs or lazygraph
- Contains reference to queryGraph or graphStats
- Contains 3-line error for missing graph
  </action>
  <verify>
    <automated>grep -q "name: graph" commands/graph.md && grep -q "lazygraph" commands/graph.md && grep -q "body_shape" commands/graph.md && grep -q "No knowledge graph" commands/graph.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>commands/graph.md exists with correct frontmatter, wires to lazygraph-ops.cjs for natural language graph exploration, has error handling</done>
</task>

</tasks>

<verification>
All three command files exist, have valid YAML frontmatter, reference the correct existing scripts/modules, and include error handling patterns consistent with the established command pattern.
</verification>

<success_criteria>
- commands/speakers.md wires /mos:speakers to room/team/ profile reading
- commands/reanalyze.md wires /mos:reanalyze to compute-meetings-intelligence with delta reporting
- commands/graph.md wires /mos:graph to lazygraph-ops.cjs for natural language graph queries
- All three follow the 4-zone UI anatomy pattern
- All three have 3-line error handling for missing prerequisites
- All three use natural-language-first framing (D-NEW-2)
</success_criteria>

<output>
After completion, create `.planning/phases/36-command-wiring/36-02-SUMMARY.md`
</output>
