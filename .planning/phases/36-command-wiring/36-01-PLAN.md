---
phase: 36-command-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/present.md
  - commands/dashboard.md
autonomous: true
requirements:
  - WIRE-01
  - WIRE-02

must_haves:
  truths:
    - "User types /mos:present and gets 6 presentation views generated, then dashboard opens in browser"
    - "User types /mos:dashboard and gets interactive graph with chat panel in browser"
    - "Both commands show 3-line error when room or graph data is missing"
  artifacts:
    - path: "commands/present.md"
      provides: "/mos:present command wiring to generate-presentation.cjs + serve-presentation"
      contains: "generate-presentation.cjs"
    - path: "commands/dashboard.md"
      provides: "/mos:dashboard command wiring to serve-presentation targeting graph.html"
      contains: "serve-presentation"
  key_links:
    - from: "commands/present.md"
      to: "scripts/generate-presentation.cjs"
      via: "bash/node invocation in command instructions"
      pattern: "generate-presentation\\.cjs"
    - from: "commands/present.md"
      to: "scripts/serve-presentation"
      via: "bash invocation after generation"
      pattern: "serve-presentation"
    - from: "commands/dashboard.md"
      to: "scripts/serve-presentation"
      via: "bash invocation targeting graph view"
      pattern: "serve-presentation"
---

<objective>
Create two markdown command files that wire /mos:present and /mos:dashboard to existing presentation infrastructure.

Purpose: Users can generate and view their room's 6-view presentation suite and interactive graph dashboard through simple slash commands, with Larry providing natural-language framing of what they see.
Output: commands/present.md, commands/dashboard.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-command-wiring/36-CONTEXT.md

@commands/splash.md
@commands/onboard.md
@skills/ui-system/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /mos:present command (WIRE-01)</name>
  <files>commands/present.md</files>
  <action>
Create commands/present.md following the exact pattern of commands/splash.md and commands/onboard.md.

**read_first:** commands/splash.md, commands/onboard.md, scripts/generate-presentation.cjs (first 30 lines for usage)

**YAML frontmatter:**
- name: present
- description: Generate your room's 6-view visual presentation and open it in browser (per D-02, JTBD framing -- what user GETS)
- body_shape: E (Action Report)
- body_shape_detail: Generation results as action items, 6 views listed as outputs
- ui_reference: skills/ui-system/SKILL.md
- allowed-tools: Bash, Read

**Body instructions for Larry:**

1. **Pre-flight check:** Read room/STATE.md. If no room/ directory exists, show 3-line error per D-03:
   ```
   x No room found
     Why: You need an active project room to generate views
     Fix: /mos:new-project
   ```

2. **Generate all 6 views:** Run:
   ```bash
   node "${CLAUDE_PLUGIN_ROOT}/scripts/generate-presentation.cjs" room/
   ```

3. **Report generation results** per D-02. Larry says something like: "Your room is now a visual dashboard your investors can browse. Six views ready:" then lists the 6 views (Dashboard, Wiki, Deck, Insights, Diagrams, Graph) with one-line descriptions of what each shows.

4. **Open in browser:** Run:
   ```bash
   bash "${CLAUDE_PLUGIN_ROOT}/scripts/serve-presentation"
   ```

5. **Natural language framing per D-NEW-2:** Larry tells the user what they can do: "The dashboard is live at localhost. Share it, screenshot it, or just explore."

6. **Error handling:** If generate-presentation.cjs fails, show 3-line error with the script's stderr. If serve-presentation fails, show 3-line error suggesting the user check if another server is running on port 8422.

7. **Zone 4 (Action Footer):** Suggest next actions: "Want to focus on just the graph? /mos:dashboard" and "Need to add more content first? Just tell me about a meeting or paste a document."

**acceptance_criteria:**
- File exists at commands/present.md
- Contains YAML frontmatter with name: present
- Contains reference to generate-presentation.cjs
- Contains reference to serve-presentation
- Contains 3-line error pattern for missing room
- Contains body_shape in frontmatter
  </action>
  <verify>
    <automated>grep -q "name: present" commands/present.md && grep -q "generate-presentation.cjs" commands/present.md && grep -q "serve-presentation" commands/present.md && grep -q "body_shape" commands/present.md && grep -q "No room" commands/present.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>commands/present.md exists with correct frontmatter, wires to both scripts, has error handling, and follows the 4-zone UI anatomy</done>
</task>

<task type="auto">
  <name>Task 2: Create /mos:dashboard command (WIRE-02)</name>
  <files>commands/dashboard.md</files>
  <action>
Create commands/dashboard.md following the same command pattern.

**read_first:** commands/splash.md, commands/present.md (just created)

**YAML frontmatter:**
- name: dashboard
- description: Open your room's interactive knowledge graph with chat panel in browser
- body_shape: raw
- ui_reference: skills/ui-system/SKILL.md
- allowed-tools: Bash, Read

**Body instructions for Larry:**

1. **Pre-flight check:** Check if room/exports/presentation/graph.html exists (or room/.lazygraph/ directory). Per D-05, if graph.json doesn't exist, Larry explains:
   ```
   x No graph data found
     Why: Your room needs filed content before the graph has anything to show
     Fix: Tell me about a meeting or paste a document to get started
   ```

2. **Open graph view directly** per D-04. Run serve-presentation targeting the graph view:
   ```bash
   bash "${CLAUDE_PLUGIN_ROOT}/scripts/serve-presentation"
   ```
   Then tell the user to navigate to the graph tab, or if the script supports a path argument, target graph.html directly.

3. **Natural language framing:** Larry describes what they're seeing: "This is your knowledge graph -- every concept, connection, and contradiction in your room, visualized. The chat panel on the right lets you ask questions about what you see."

4. **Zone 4 (Action Footer):** Suggest: "Want all 6 views, not just the graph? /mos:present" and "Want to query the graph from here in the terminal? /mos:graph"

**acceptance_criteria:**
- File exists at commands/dashboard.md
- Contains YAML frontmatter with name: dashboard
- Contains reference to serve-presentation
- Contains 3-line error pattern for missing graph data
- Contains body_shape in frontmatter
  </action>
  <verify>
    <automated>grep -q "name: dashboard" commands/dashboard.md && grep -q "serve-presentation" commands/dashboard.md && grep -q "graph" commands/dashboard.md && grep -q "body_shape" commands/dashboard.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>commands/dashboard.md exists with correct frontmatter, wires to serve-presentation for graph view, has error handling for missing graph data</done>
</task>

</tasks>

<verification>
Both command files exist, have valid YAML frontmatter, reference the correct existing scripts, and include error handling patterns consistent with the onboard.md command.
</verification>

<success_criteria>
- commands/present.md wires /mos:present to generate-presentation.cjs + serve-presentation
- commands/dashboard.md wires /mos:dashboard to serve-presentation (graph view)
- Both follow the 4-zone UI anatomy pattern
- Both have 3-line error handling for missing prerequisites
- Both use natural-language-first framing (D-NEW-2)
</success_criteria>

<output>
After completion, create `.planning/phases/36-command-wiring/36-01-SUMMARY.md`
</output>
