---
phase: 267.1-hooked-model-completeness-audit-of-the-first-session-onboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/session-start
  - tests/test-267-1-first-install-hooked-audit.cjs
  - tests/run-all-267.1.sh
autonomous: true
requirements: [AUDIT-A1, AUDIT-TEST]
canon_parts: [6, 11, 12]

must_haves:
  truths:
    - "The FIRST_INSTALL prose block carries the SEED-021 AskUserQuestion card mandate, matching the two sibling cold-start branches"
    - "A single test file pins every machine-checkable finding of the 267.1 audit, so a future edit to scripts/session-start or lib/ breaks the build instead of silently invalidating the audit"
    - "The phase aggregator runs the pinned test and fences the audit deliverable plus the room mirror against em-dashes"
    - "scripts/session-start still parses (bash -n exits 0) after the prose edit"
  artifacts:
    - path: "tests/test-267-1-first-install-hooked-audit.cjs"
      provides: "Regression pins for GAP I-1, GAP R-1 (x2), Action-leg SEED-021, GAP G-1, virgin-HOME reachability, bash syntax"
      min_lines: 90
      contains: "First install detected."
    - path: "tests/run-all-267.1.sh"
      provides: "Phase 267.1 aggregator with glob discovery, found-eq-0 guard, and a two-array em-dash fence"
      min_lines: 80
      contains: "EMDASH_ABS_TARGETS"
    - path: "scripts/session-start"
      provides: "FIRST_INSTALL branch with the SEED-021 card mandate"
      contains: "SEED-021"
  key_links:
    - from: "tests/run-all-267.1.sh"
      to: "tests/test-267-1-first-install-hooked-audit.cjs"
      via: "glob discovery on PREFIX tests/test-267-1-"
      pattern: "PREFIX=\\$\\{TEST_267_1_PREFIX:-tests/test-267-1-\\}"
    - from: "tests/test-267-1-first-install-hooked-audit.cjs"
      to: "scripts/session-start"
      via: "src.indexOf literal anchor, never a line number"
      pattern: "indexOf\\('\\[MindrianOS Onboarding\\] First install detected\\.'\\)"
---

<objective>
Land the Wave 0 validation scaffold for the 267.1 Hooked audit, and close the one piece of
in-scope remediation this phase owns: the missing SEED-021 `AskUserQuestion` card mandate in
the FIRST_INSTALL prose block.

**OQ-3 CALL, LOCKED: the SEED-021 one-line prose fix is IN SCOPE for this phase.** Reasoning,
stated here so no executor re-opens it:

1. It is independent of Phase 269. Phase 269 removes the Brain-key friction that sits BEFORE
   the trigger fires; it does not change how the three-option menu is rendered once the prose
   reaches the model. There is no throwaway risk.
2. It is a rendering-contract consistency fix, not a new engagement mechanic. Canon Appendix D
   entry 31's self-binding clause ("no entry 32 lands until entry 31 returns a real two-gauge
   reading") bites on new engagement mechanics. It does not bite on making a third branch obey
   a contract the other two already obey.
3. `tests/test-209-session-start-exemplar.cjs` already pins the SEED-021 mandate for BOTH
   sibling branches (the `MODE_MENU=` region and the `Other rooms detected` region). FIRST_INSTALL
   is the only cold-start branch outside that contract. The guard exists; the surface was missed.
4. It converts the Action-leg finding from a brittle negative pin ("this string is still absent",
   which fails the moment somebody does the right thing) into a durable positive pin.

Everything else stays audit-only. No remediation for GAP R-1 (reward routing), GAP I-1 (USER.md
writer), or GAP S-1 (Brain-key friction) lands in `scripts/session-start` in this phase. Those
are registered as follow-up phases in plan 267.1-05.

Purpose: an audit produces a document, and documents rot. Pinning the findings as tests turns a
point-in-time snapshot into a live tripwire.

Output: one modified prose line in `scripts/session-start`, one new test file, one new aggregator.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-RESEARCH.md
@.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-PATTERNS.md
@.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-VALIDATION.md
</context>

<artifacts_this_phase_produces>

This plan (267.1-01) produces:

| Artifact | Kind | Path |
|---|---|---|
| SEED-021 card mandate in FIRST_INSTALL | modified prose line inside a bash string | `scripts/session-start` (line-666 payload, anchored on the literal `[MindrianOS Onboarding] First install detected.`) |
| `test-267-1-first-install-hooked-audit.cjs` | new test file, 8 `ok()` assertions | `tests/test-267-1-first-install-hooked-audit.cjs` |
| `run-all-267.1.sh` | new phase aggregator | `tests/run-all-267.1.sh` |

Later plans in this phase produce: `.planning/research/2026-08-27-hooked-first-install-audit.md`
(267.1-03), `~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-hooked-first-install-audit/2026-08-27-hooked-first-install-audit.md`
(267.1-04), and ROADMAP entries `### Phase 267.2` and `### Phase 267.3` (267.1-05).

</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the SEED-021 card mandate to the FIRST_INSTALL prose block</name>

  <files>scripts/session-start</files>

  <read_first>
    - `scripts/session-start` - read the whole FIRST_INSTALL payload first. It is ONE line (line 666 at commit 8a65c9ee6), a double-quoted bash string roughly 3000 characters long containing literal two-character `\n` escape sequences, not real newlines. Read it by locating the literal `[MindrianOS Onboarding] First install detected.` and slicing to the next REAL newline. Do not read it with a line-window tool that truncates (RESEARCH.md Pitfall 8).
    - `scripts/session-start` sibling branches that already carry the contract: the `MODE_MENU=` assignment region, and the `Other rooms detected` injection region. Read both verbatim so the new wording matches house voice.
    - `tests/test-209-session-start-exemplar.cjs` - the existing guard that pins SEED-021 for those two sibling regions. Your edit must not break it.
    - `.planning/phases/267.1-.../267.1-RESEARCH.md` section "The SEED-021 card mandate present in siblings, absent in FIRST_INSTALL" and the ACTION row of the leg-by-leg TARI map.
  </read_first>

  <action>
    Inside the FIRST_INSTALL payload, replace the exact literal `Offer three approaches:\n1. Conversational Q&A`
    with `Offer three approaches - fire the AskUserQuestion card with these three as options; default to option 3 (Skip) if the user starts talking instead of picking; never render this menu as a prose list (SEED-021):\n1. Conversational Q&A`.

    Two load-bearing details:

    - The `\n` in that literal is the two-character escape sequence as it appears in the source, not a real newline. Match it as source text.
    - The default is option 3 (Skip), NOT option 1. The sibling `MODE_MENU=` branch defaults to Just Talk (the lowest-friction path) when the navigator talks instead of picking. The lowest-friction path here is Skip. This is also the only choice consistent with `docs/reward-before-investment-rule.md` line 21, which forbids demanding input before a first reward. Record that reasoning in the plan SUMMARY so plan 267.1-03 can cite it.

    Change exactly this one substring. Do NOT touch the reward line ("Based on your work in [domain]..."), do NOT touch the USER.md line, do NOT touch `COLD_START_MENU=`, do NOT add `/mos:ignite` to the menu. Those are GAP R-1 and GAP I-1 and are explicitly out of scope for this phase (they are registered as Phase 267.2 in plan 267.1-05).

    Your added text contains no `$`, no backtick, and no double quote, so the surrounding bash double-quoted string stays valid. Prove it with `bash -n` in the verify step rather than assuming it.

    Record in the SUMMARY: the commit SHA of the pre-fix state (`git rev-parse HEAD` before you edit) so plan 267.1-03 can pin the audited commit honestly and state that this one gap was found AND closed inside the audit phase.
  </action>

  <verify>
    <automated>bash -n scripts/session-start && node tests/test-209-session-start-exemplar.cjs && node -e "const s=require('fs').readFileSync('scripts/session-start','utf8');const i=s.indexOf('[MindrianOS Onboarding] First install detected.');const r=s.slice(i,s.indexOf('\n',i));if(i===-1)throw new Error('anchor gone');if(r.indexOf('AskUserQuestion')===-1)throw new Error('no AskUserQuestion in FIRST_INSTALL');if(r.indexOf('SEED-021')===-1)throw new Error('no SEED-021 in FIRST_INSTALL');if(r.indexOf('sweepDomainInsights')!==-1)throw new Error('scope creep: reward routing');console.log('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n scripts/session-start` exits 0.
    - `node tests/test-209-session-start-exemplar.cjs` still prints `PASS` with the same assertion count as before the edit (5).
    - The source region from the literal `[MindrianOS Onboarding] First install detected.` to the next real newline contains both `AskUserQuestion` and `SEED-021`.
    - That same region still contains all three original option labels: `1. Conversational Q&A`, `2. Document paste`, `3. Skip`.
    - That same region still contains `Based on your work in [domain]` and `~/.mindrian-user.md` UNCHANGED (proof that no out-of-scope remediation leaked in).
    - `git diff --stat scripts/session-start` shows exactly 1 file changed, 1 insertion, 1 deletion.
    - `grep -c 'COLD_START_MENU="---' scripts/session-start` returns 1 and `grep -c 'mos:ignite' scripts/session-start` returns 0 (cold-start menu untouched).
  </acceptance_criteria>

  <done>The FIRST_INSTALL branch obeys the same SEED-021 rendering contract as the two sibling cold-start branches; the script still parses; no other prose in the block changed.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test-267-1-first-install-hooked-audit.cjs pinning the audit findings</name>

  <files>tests/test-267-1-first-install-hooked-audit.cjs</files>

  <read_first>
    - `tests/test-209-session-start-exemplar.cjs` - THE donor. Port its harness verbatim: `let n = 0; function ok(desc, fn) { fn(); n += 1; console.log('  ok   ' + desc); }`, throw-to-fail with no try/catch, no `process.exit(0)` and no `process.exit(1)` path, closing line `console.log('\nPASS <name> (' + n + ' assertions)')`. Also port its `src.indexOf(<literal>)` region-slicing idiom and its `execFileSync('bash', ['-n', SESSION_START])` syntax proof.
    - `tests/test-onboarding-gate.cjs` lines 21-41 - port ONLY the `makeTmpHome()` + `HOME`/`USERPROFILE` env-override seam. Do NOT port its reporting style (`pass()`/`failTest()`/`process.exit(1)`); mixing the two harnesses in one file is explicitly forbidden by PATTERNS.md.
    - `.planning/phases/267.1-.../267.1-PATTERNS.md` section 2 - the full boilerplate and the "anchor on literals, never line numbers" rule.
    - `.planning/phases/267.1-.../267.1-VALIDATION.md` Per-Task Verification Map rows 267.1-01-01 through 267.1-01-06. NOTE: that table's `sed -n '665,667p'` and `sed -n '657p'` commands are SUPERSEDED. Line numbers in `scripts/session-start` are unstable and task 1 of this plan just shifted the file. Use literal anchors.
    - `lib/core/mva-rule-linter.cjs` (header comment and `scanCommands` around line 224, module.exports around line 245) and `scripts/check-reward-before-investment.cjs` (around line 127) - the two sources for the GAP G-1 jurisdiction assertion.
    - `lib/core/user-archetype.cjs` around line 61-64 - the single `~/.mindrian-user.md` read under `lib/`.
  </read_first>

  <action>
    Create `tests/test-267-1-first-install-hooked-audit.cjs` in the 209 harness style. Header comment states:
    "Phase 267.1 - regression pins for the FIRST_INSTALL Hooked Model audit. Source-assertions over
    scripts/session-start and lib/, anchored on literals (never line numbers - the FIRST_INSTALL payload
    is one ~3000-char line and its line number moves)."

    Constants: `REPO = path.join(__dirname, '..')`, `SESSION_START = path.join(REPO,'scripts','session-start')`,
    `CHECK_ONBOARD = path.join(REPO,'scripts','check-onboard')`,
    `FIRST_INSTALL_ANCHOR = '[MindrianOS Onboarding] First install detected.'`,
    `MENU_ANCHOR = 'COLD_START_MENU='`.
    Read `src = fs.readFileSync(SESSION_START,'utf8')` once at module top, as 209 does.

    Add a small local helper `region(anchor)` that does `const i = src.indexOf(anchor); assert.notEqual(i, -1, 'anchor missing: ' + anchor); return src.slice(i, src.indexOf('\n', i));` - slicing to the next REAL newline, which for both anchors captures the entire single-line payload. Assert the FIRST_INSTALL region length is greater than 2000 as a sanity guard that the slice actually grabbed the payload and not a stub.

    Write exactly these seven `ok()` assertions, in this order, with these descriptions:

    1. `'GAP I-1: ~/.mindrian-user.md has no writer under lib/, scripts/, hooks/'`
       Shell out with `spawnSync('grep', ['-rn', 'mindrian-user\\.md', 'lib/', 'scripts/', 'hooks/'], {cwd: REPO, encoding: 'utf8'})`. Tolerate grep exit code 1 (no match) without throwing. Assert: the non-empty result lines number exactly 1; that line's path is `lib/core/user-archetype.cjs`; that line contains `safeReadFile` (it is a READ). Then run a second `spawnSync('grep', ['-rn', 'mindrian-user\\.md', 'lib/', 'scripts/', 'hooks/', 'skills/', 'commands/'], ...)` and assert that NO returned line matches `/writeFileSync|writeUserMdAtomic|appendFileSync|createWriteStream/`. Failure message must name GAP I-1 explicitly, e.g. `'GAP I-1 closed or changed shape: a writer for ~/.mindrian-user.md now exists - re-run the 267.1 audit'`.

    2. `'GAP R-1: sweepDomainInsights is never invoked from scripts/session-start'`
       Assert `src.indexOf('sweepDomainInsights') === -1` and `src.indexOf('domain-insight-sweep') === -1`. Failure message names GAP R-1 and says the audit needs re-running.

    3. `'GAP R-1: the cold-start menu does not route the first-time user to /mos:ignite'`
       `const menu = region(MENU_ANCHOR)`. Assert `menu.indexOf('/mos:new-project') !== -1` FIRST (proof the slice grabbed the real menu), then `menu.indexOf('mos:ignite') === -1`. Failure message names GAP R-1 and `skills/ignite/SKILL.md:50` ("the canonical front door").

    4. `'Action leg: the FIRST_INSTALL block carries the SEED-021 AskUserQuestion mandate (closed in 267.1-01)'`
       `const fi = region(FIRST_INSTALL_ANCHOR)`. Assert `fi.length > 2000`; `fi.indexOf('AskUserQuestion') !== -1`; `fi.indexOf('SEED-021') !== -1`; and all three of `'1. Conversational Q&A'`, `'2. Document paste'`, `'3. Skip'` are present. This is a POSITIVE pin: it guards the fix task 1 just landed and keeps FIRST_INSTALL inside the same contract the two sibling branches already obey.

    5. `'Action leg: FIRST_INSTALL still asserts the reward and investment legs as prose only (GAP R-1 / GAP I-1 unremediated by design)'`
       On the same `fi` region assert `fi.indexOf('Based on your work in [domain]') !== -1` and `fi.indexOf('~/.mindrian-user.md') !== -1`. Comment above the assertion states these two literals are the audit's cited evidence for GAP R-1 and GAP I-1; if they disappear, the audit's citations are stale and the deliverable must be revisited (Phase 267.2 is where they are supposed to change).

    6. `'GAP G-1: the reward-before-investment guard has jurisdiction over commands/ only, never hooks'`
       `require('../lib/core/mva-rule-linter.cjs')` and assert `typeof linter.scanCommands === 'function'` and `Array.isArray(linter.REWARD_TYPES) || typeof linter.REWARD_TYPES === 'object'`. Then read `lib/core/mva-rule-linter.cjs` and `scripts/check-reward-before-investment.cjs` as text and assert: the CLI source contains `path.join(__dirname, '..', 'commands')`; neither source contains the literal `session-start`; neither source contains the literal `'hooks'`. Failure message names GAP G-1 and says the guard's scope changed, so the audit's governance finding needs re-checking.

    7. `'Reachability: a virgin HOME still yields FIRST_INSTALL from scripts/check-onboard'`
       Use the ported `makeTmpHome()` (`fs.mkdtempSync(path.join(os.tmpdir(), '267-1-'))`), then `spawnSync('bash', [CHECK_ONBOARD], {env: Object.assign({}, process.env, {HOME: tmp, USERPROFILE: tmp}), encoding: 'utf8'})`. Assert `res.status === 0` and the first stdout line trimmed equals `FIRST_INSTALL`. Clean up with `fs.rmSync(tmp, {recursive: true, force: true})` in a `finally`. Do NOT touch or delete the real `~/.mindrian-onboarded` (RESEARCH.md Runtime State Inventory says so explicitly).

    Close with the 209 syntax proof as an eighth `ok()`: `'bash -n scripts/session-start exits 0 (the script still parses)'` using `execFileSync('bash', ['-n', SESSION_START], {cwd: REPO, stdio: 'pipe'})`.

    Final line: `console.log('\nPASS test-267-1-first-install-hooked-audit (' + n + ' assertions)');`

    No em-dashes (U+2014) anywhere in the file.
  </action>

  <verify>
    <automated>node tests/test-267-1-first-install-hooked-audit.cjs && ! LC_ALL=C.UTF-8 grep -qP '\x{2014}' tests/test-267-1-first-install-hooked-audit.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-267-1-first-install-hooked-audit.cjs` exits 0 and prints `PASS test-267-1-first-install-hooked-audit (8 assertions)`.
    - `grep -c "sed -n" tests/test-267-1-first-install-hooked-audit.cjs` returns 0 (no line-number anchoring anywhere).
    - `grep -c "process.exit" tests/test-267-1-first-install-hooked-audit.cjs` returns 0 (throw-to-fail contract, matching the 209 donor).
    - `grep -c "failTest\|function pass(" tests/test-267-1-first-install-hooked-audit.cjs` returns 0 (the `test-onboarding-gate.cjs` reporting style was not ported).
    - `grep -F -c "[MindrianOS Onboarding] First install detected." tests/test-267-1-first-install-hooked-audit.cjs` returns at least 1.
    - `grep -F -c "COLD_START_MENU=" tests/test-267-1-first-install-hooked-audit.cjs` returns at least 1.
    - Negative-control proof, run and recorded in the SUMMARY: temporarily `sed -i 's/sweepDomainInsights/XsweepDomainInsightsX/' /tmp/copy-of-session-start` is NOT the method. Instead prove the pins bite by running `node -e` against a mutated in-memory copy, or simply confirm assertion 2 and 3 fail when their anchors are removed, by reasoning stated in the SUMMARY. Do not mutate the real `scripts/session-start`.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' tests/test-267-1-first-install-hooked-audit.cjs` returns empty.
  </acceptance_criteria>

  <done>Every machine-checkable claim in the 267.1 audit is pinned by an assertion that names its gap ID in the failure message, anchored on literals, and the suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/run-all-267.1.sh aggregator with a two-array em-dash fence</name>

  <files>tests/run-all-267.1.sh</files>

  <read_first>
    - `tests/run-all-266.sh` - THE donor, read in full. Port: the `set -uo pipefail` + `ROOT`/`cd` header, the `run()` and `run_may_skip()` helpers, the `shopt -s nullglob` glob discovery over `.cjs` and `.sh` with the self-skip guard, the load-bearing `if [ $found -eq 0 ]` guard and its `!!! no ${PREFIX}* files discovered` message, the explicit-gate-lines block for tests that carry no phase prefix, the em-dash fence including its `rc >= 2` SCAN BROKE arm and its MISSING arm, and the footer `PASS=/FAIL=/SKIP=` block ending in `[ "$FAIL" -eq 0 ]`.
    - `.planning/phases/267.1-.../267.1-PATTERNS.md` section 3 - the extracted excerpts of every block named above.
    - `.planning/phases/267.1-.../267.1-VALIDATION.md` row 267.1-02-01 - the em-dash check this fence is replacing with automation.
  </read_first>

  <action>
    Create `tests/run-all-267.1.sh`, mirroring `tests/run-all-266.sh`. Header comment states it mirrors
    `tests/run-all-266.sh` and that it runs `node "$t"` bare, never `node --test "$t"`.

    Set `PREFIX="${TEST_267_1_PREFIX:-tests/test-267-1-}"`. Note the dash form `267-1`, not `267.1`, in the
    TEST FILE prefix - every existing phase test is `tests/test-<int>-<slug>.cjs` and a dot would collide with
    extension parsing. The AGGREGATOR filename keeps the dot (`run-all-267.1.sh`) to match `run-all-266.sh`'s
    phase-named form.

    Em-dash fence, split into TWO arrays because one target lives outside the repo:

    `EMDASH_TARGETS` (repo-relative, resolved as `$ROOT/$t`, MISSING is a failure unless `TEST_267_1_ALLOW_MISSING=1`):
      - `tests/test-267-1-first-install-hooked-audit.cjs`
      - `tests/run-all-267.1.sh`
      - `scripts/session-start`
      - `.planning/research/2026-08-27-hooked-first-install-audit.md`

    `EMDASH_ABS_TARGETS` (absolute paths, MISSING increments SKIP and prints an explicit line, never a failure,
    because the rethinking room is machine-local and does not exist on every checkout):
      - `${MINDRIAN_ROOMS_HOME:-$HOME/MindrianRooms}/rethinking-mindrianos/research/2026-08-27-hooked-first-install-audit/2026-08-27-hooked-first-install-audit.md`

    Both arrays use the same scan body as 266: `hits="$(LC_ALL=C.UTF-8 grep -lP '\x{2014}' "$f" 2>/dev/null)"; rc=$?`
    with the `rc >= 2` SCAN BROKE arm setting `EMDASH_OK=0`, and a `-n "$hits"` arm printing `FORBIDDEN em-dash in: $t`.
    Keep 266's `TEST_267_1_ALLOW_MISSING` escape for the repo-relative array, since the audit deliverable does not
    exist until plan 267.1-03 runs and Wave 1 must still be able to go green.

    Add an explicit gate line (266's "a test that is never discovered is worse than no test" block) for
    `tests/test-209-session-start-exemplar.cjs`, because plan 267.1-01 task 1 edits the exact file that test guards
    and the 209 test carries no `test-267-1-` prefix so glob discovery will never find it.

    Footer prints `Phase 267.1: PASS=$PASS FAIL=$FAIL SKIP=$SKIP` and ends with `[ "$FAIL" -eq 0 ]`.

    No em-dashes (U+2014) in the file. `--` hyphen pairs are fine.
  </action>

  <verify>
    <automated>TEST_267_1_ALLOW_MISSING=1 bash tests/run-all-267.1.sh</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n tests/run-all-267.1.sh` exits 0.
    - `TEST_267_1_ALLOW_MISSING=1 bash tests/run-all-267.1.sh` exits 0 and its output contains `discovered 1 test file(s)` and `Phase 267.1: PASS=` with `FAIL=0`.
    - Output contains a line running `test-267-1-first-install-hooked-audit.cjs` and a line running `test-209-session-start-exemplar.cjs` (the explicit gate line).
    - `bash tests/run-all-267.1.sh` WITHOUT `TEST_267_1_ALLOW_MISSING` exits non-zero at this point in the phase, and its output names `.planning/research/2026-08-27-hooked-first-install-audit.md` as MISSING. Record this expected-red state in the SUMMARY; plan 267.1-03 turns it green by creating the deliverable.
    - `grep -c "node --test" tests/run-all-267.1.sh` returns 0.
    - `grep -c "EMDASH_ABS_TARGETS" tests/run-all-267.1.sh` returns at least 2 (declaration plus loop).
    - `grep -c 'found -eq 0' tests/run-all-267.1.sh` returns 1.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' tests/run-all-267.1.sh` returns empty.
  </acceptance_criteria>

  <done>The phase has a discoverable aggregator that runs the new pins, gates the 209 exemplar it put at risk, and machine-checks the em-dash rule on both the in-repo deliverable and the out-of-repo room mirror.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `scripts/session-start` -> Claude `additionalContext` | Injected prose crosses into the model's context. The added SEED-021 clause is static, operator-authored text with no interpolation, so it introduces no new untrusted input. |
| test harness -> filesystem | The reachability assertion spawns `bash scripts/check-onboard` with a rewritten `HOME`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-267.1-01 | Tampering | `scripts/session-start` injected prose | mitigate | The added clause contains no `$`, no backtick, no double quote. `bash -n` proof is an acceptance criterion, and the existing bounded-read posture at the STATE.md injection site (`:775-777`) is untouched by this edit. |
| T-267.1-02 | Denial of Service | test task 7 (virgin HOME) | mitigate | `makeTmpHome()` writes only under `os.tmpdir()` and is removed in a `finally`. The real `~/.mindrian-onboarded` is never read for write and never deleted (RESEARCH.md Runtime State Inventory). |
| T-267.1-03 | Information Disclosure | test failure messages | accept | Failure strings name gap IDs and repo paths only. No secrets, no tester identity, no room content. |
| T-267.1-04 | Elevation of Privilege | Canon Part 8 graph boundary | accept | This plan performs local reads and one local prose edit. No Brain wire is opened, nothing egresses. Trivially satisfied. |
| T-267.1-SC | Tampering | package-manager installs | accept | This phase installs zero packages. `package.json` declares zero devDependencies. No slopcheck gate is warranted; if any later task adds an install, the Package Legitimacy Gate must be run first. |
</threat_model>

<verification>
```bash
bash -n scripts/session-start
node tests/test-209-session-start-exemplar.cjs
node tests/test-267-1-first-install-hooked-audit.cjs
TEST_267_1_ALLOW_MISSING=1 bash tests/run-all-267.1.sh
git diff --stat scripts/session-start
```
</verification>

<success_criteria>
- FIRST_INSTALL carries the SEED-021 `AskUserQuestion` mandate; the two sibling branches are unchanged; `bash -n` exits 0.
- `git diff --stat scripts/session-start` shows exactly 1 insertion and 1 deletion. No reward routing, no USER.md writer, no menu change.
- `tests/test-267-1-first-install-hooked-audit.cjs` exists, passes with 8 assertions, and contains zero `sed -n` line anchors.
- `TEST_267_1_ALLOW_MISSING=1 bash tests/run-all-267.1.sh` exits 0 and discovers the new test plus the 209 explicit gate line.
- The SUMMARY records: the pre-fix commit SHA, the "default to Skip" reasoning, and the expected-red state of the un-flagged aggregator run.
</success_criteria>

<output>
Create `.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-01-SUMMARY.md` when done.

Commit with `git add -f` for any `.planning/` path (`.planning/*` is gitignored in this repo; see CLAUDE.md).
</output>

