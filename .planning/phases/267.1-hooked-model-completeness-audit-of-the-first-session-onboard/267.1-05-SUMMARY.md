---
phase: 267.1
plan: 05
subsystem: docs
tags: [hooked-model, roadmap, follow-up-phase-registration, cross-references, gap-tracking]

requires:
  - phase: 267.1-03
    provides: "The scored audit deliverable (.planning/research/2026-08-27-hooked-first-install-audit.md) whose Roadmap section names GAP R-1, GAP I-1, GAP G-1 as deferred and GAP S-1 as owned by Phase 269"
provides:
  - "Phase 267.2 (First-Install Hooked Loop Repair - Reward + Investment) registered in .planning/ROADMAP.md with a two-workstream Goal an executor could plan from"
  - "Phase 267.3 (Reward-Before-Investment Guard Jurisdiction) registered in .planning/ROADMAP.md, independent of Phase 269 and 267.2"
  - "A Cross-references paragraph appended to the Phase 267.1 entry's Goal, naming the audit path, its 30/70 score and band, the SEED-021 fix it shipped, both new follow-up phases, and the GAP S-1 handoff to Phase 269"
affects: ["267.1-06 (phase gate roll-up will read these registrations)", "267.2 and 267.3 (now exist as real ROADMAP.md entries a future /gsd-plan-phase can execute against)", "Phase 269 (receives an explicit, non-duplicating reciprocal reference; its own text is untouched)"]

tech-stack:
  added: []
  patterns:
    - "Follow-up phases minted by an audit phase are registered as decimal '(INSERTED)' entries using the exact five-line house skeleton (Goal / Requirements / Depends on / Plans count / Plans list)"
    - "Cross-phase collision is expressed in ROADMAP.md's own idiom (a 'Cross-references:' prose paragraph, Form A: 'Also touches Phase <N> (<name> - <clause>)'; Form B: a qualified 'Depends on:' line), never the audit document's 'COLLISION NOTE' literal"

key-files:
  modified:
    - .planning/ROADMAP.md

key-decisions:
  - "Two follow-up phases, not three, per the plan's explicit call: GAP R-1 and GAP I-1 share the same Phase 269 collision and become one phase (267.2) with a W1/W2 two-workstream Goal shape (borrowed from the Phase 268 entry); GAP G-1 has no Phase 269 collision and gets its own independent phase (267.3)."
  - "GAP S-1 is not registered as a new phase. Phase 269 already claims it at .planning/ROADMAP.md:578 ('Directly resolves the Brain-key friction Gaurav Thorat's trial-install testimonial flagged'). This plan hands it over via the 267.1 Cross-references paragraph rather than minting a duplicate phase."
  - "First verification run for the 267.1 Cross-references paragraph failed its own automated check: the sentence named Phase 267.2 and 267.3 together in one clause ('Phase 267.2 (...) and Phase 267.3 (...)'), so the literal substring 'Also touches Phase 267.3' was never produced. Split into two separate 'Also touches Phase 267.2' / 'Also touches Phase 267.3' sentences to satisfy the plan's own machine-checkable verify script. [Rule 1 - Bug, caught before commit]"
  - "Each ROADMAP.md edit was staged and committed with an explicit pathspec (git add -f .planning/ROADMAP.md -- never git add -A), per this plan's own shared-working-tree caution, since a concurrent Phase 265/269 track is actively committing to the same file."

requirements-completed: [AUDIT-ROADMAP, AUDIT-R1, AUDIT-I1, AUDIT-G1, AUDIT-S1]

metrics:
  duration: "~25 minutes"
  completed: "2026-08-27"
---

# Phase 267.1 Plan 05: Follow-up Phase Registrations Summary

Registered two new ROADMAP.md phases (267.2, reward+investment repair; 267.3, guard-jurisdiction fix) from the audit's deferred gaps, and appended a Cross-references paragraph to the Phase 267.1 entry recording the audit's score, its one shipped fix, and the GAP S-1 handoff to Phase 269 - so none of the audit's findings die in a research document nobody re-reads.

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-08-27
- **Tasks:** 2/2 completed
- **Files modified:** 1 (`.planning/ROADMAP.md`, two separate targeted edits)

## Accomplishments

- Inserted `### Phase 267.2: First-Install Hooked Loop Repair (Reward + Investment) (INSERTED)` between the Phase 267.1 entry and Phase 268, with a two-workstream Goal (W1 reward routing / GAP R-1, W2 investment writer / GAP I-1) citing the audit's concrete file evidence (`lib/core/domain-insight-sweep.cjs`, `scripts/check-pending-breakthrough.cjs`, `lib/core/user-md-ops.cjs`, `lib/core/user-archetype.cjs`), the `/mos:profile-user`-does-not-exist constraint, and a qualified `Depends on:` line sequencing it behind Phase 269.
- Inserted `### Phase 267.3: Reward-Before-Investment Guard Jurisdiction (hooks and injected-prose surfaces) (INSERTED)` immediately after, citing `lib/core/mva-rule-linter.cjs` and `scripts/check-reward-before-investment.cjs`'s `commands/*.md`-only jurisdiction, and declaring independence from both Phase 269 and Phase 267.2.
- Appended a `Cross-references:` paragraph to the Phase 267.1 entry's Goal: names the audit path, its 30/70 score and Fragile-loop band (labelled diagnostic per Canon Appendix D entry 31, not a gate), the SEED-021 card-mandate fix as the one remediation this phase shipped, pointers to both new follow-up phases, and the GAP S-1 handoff to Phase 269 citing `docs/testers/gaurav-thorat/FEEDBACK.md` by path only.
- Zero changes to the Phase 267, 268, or 269 entries - confirmed by `git diff .planning/ROADMAP.md` showing 0 deletions across both commits (36 total insertions, 0 deletions).
- `COLLISION NOTE` appears zero times in `.planning/ROADMAP.md`; the roadmap uses only its own `Also touches Phase <N>` / qualified `Depends on:` idiom.
- `bash tests/run-all-267.1.sh` still exits 0 (PASS=3 FAIL=0 SKIP=0) after both edits.

## Task Commits

1. **Task 1: Register Phase 267.2 and Phase 267.3** - `31e08c43` (docs)
2. **Task 2: Add the Cross-references paragraph to Phase 267.1, hand GAP S-1 to Phase 269** - `27a5d035` (docs)

## Files Created/Modified

- `.planning/ROADMAP.md` - two targeted edits, both staged and committed with an explicit `-- .planning/ROADMAP.md` pathspec: (1) 34 lines inserted between the Phase 267.1 entry and Phase 268 (the two new phase entries), (2) 2 lines inserted inside the Phase 267.1 Goal, before its `**Requirements**: TBD` line (the Cross-references paragraph). No other line in the file changed.

## Decisions Made

See `key-decisions` in the frontmatter above. The most consequential: two follow-up phases instead of three, per the plan's own instruction, because GAP R-1 and GAP I-1 share one collision and one surface (FIRST_INSTALL's Reward and Investment legs), while GAP G-1 (the linter's `commands/*.md`-only jurisdiction) is a structurally separate governance fix with no Phase 269 dependency at all.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Cross-references paragraph failed its own literal-substring verify on first write**
- **Found during:** Task 2 automated verify, before commit
- **Issue:** The first draft combined both follow-up-phase references into one sentence ("Also touches Phase 267.2 (...) and Phase 267.3 (...)"), which never produces the plan's required literal substring `Also touches Phase 267.3` - the verify script's `indexOf('Also touches Phase 267.3')` check failed.
- **Fix:** Split the combined sentence into two independent `Also touches Phase 267.2 (...)` and `Also touches Phase 267.3 (...)` sentences, matching Form A's per-phase pattern exactly.
- **Files modified:** `.planning/ROADMAP.md`
- **Verification:** Re-ran the plan's own `node -e` verify script; passed. Re-ran `bash tests/run-all-267.1.sh`; PASS=3 FAIL=0 SKIP=0.
- **Committed in:** `27a5d035`

No other deviations. Plan executed exactly as written otherwise.

## Verification

```
node -e "...task 1 heading/gap-id/no-COLLISION-NOTE/em-dash check..."   -> ok roadmap registrations
git diff --stat .planning/ROADMAP.md (task 1)                            -> 1 file changed, 34 insertions(+), 0 deletions
grep -c "GAP R-1" / "GAP I-1" / "GAP G-1" .planning/ROADMAP.md           -> 2 / 2 / 2
grep -c "/mos:profile-user" .planning/ROADMAP.md                         -> 1
grep -c "lib/core/domain-insight-sweep.cjs" .planning/ROADMAP.md         -> 1
grep -c "scripts/check-pending-breakthrough.cjs" .planning/ROADMAP.md    -> 1
grep -c "lib/core/user-md-ops.cjs" .planning/ROADMAP.md                  -> 1
grep -c "lib/core/user-archetype.cjs" .planning/ROADMAP.md               -> 1
grep -c "lib/core/mva-rule-linter.cjs" .planning/ROADMAP.md              -> 1
grep -c "scripts/check-reward-before-investment.cjs" .planning/ROADMAP.md -> 1
LC_ALL=C.UTF-8 grep -cP '\x{2014}' .planning/ROADMAP.md                  -> 0 (both tasks)
node -e "...task 2 267.1-entry cross-reference check..."                 -> ok 267.1 cross-references
grep -cE "mbr_|Bearer " .planning/ROADMAP.md                             -> 0
git diff --stat .planning/ROADMAP.md (task 2)                            -> 1 file changed, 2 insertions(+), 0 deletions
bash tests/run-all-267.1.sh                                              -> PASS=3 FAIL=0 SKIP=0, exit 0
```

## Threat Flags

None beyond the plan's own threat register (T-267.1-17 through T-267.1-20, all `mitigate`, all satisfied): the GAP S-1 handoff cites `docs/testers/gaurav-thorat/FEEDBACK.md` by path only (`grep -cE "mbr_|Bearer "` returns 0); `git diff` shows zero changes inside the Phase 267, 268, or 269 entries in either commit; every deferred gap ID appears in the entry that owns it; no install-path, key-provisioning, or `mindrian-website` change was made.

## Known Stubs

None. Both new phase entries carry real Goal prose an executor could plan from, not placeholder text - each cites concrete file paths and gap IDs from the audit rather than a generic "TBD" beyond the required `**Requirements**: TBD` and `**Plans:** 0 plans` house-skeleton fields (which mirror every other not-yet-planned phase in this roadmap, e.g. Phase 267 and 268).

## Self-Check: PASSED

- FOUND: `### Phase 267.2: First-Install Hooked Loop Repair (Reward + Investment) (INSERTED)` in `.planning/ROADMAP.md`
- FOUND: `### Phase 267.3: Reward-Before-Investment Guard Jurisdiction (hooks and injected-prose surfaces) (INSERTED)` in `.planning/ROADMAP.md`
- FOUND: `Cross-references:` paragraph inside the Phase 267.1 entry
- FOUND: commit `31e08c43` (`git log --oneline --all | grep 31e08c43`)
- FOUND: commit `27a5d035` (`git log --oneline --all | grep 27a5d035`)
- `bash tests/run-all-267.1.sh` -> PASS=3 FAIL=0 SKIP=0, exit 0

---
*Phase: 267.1-hooked-model-completeness-audit-of-the-first-session-onboard*
*Completed: 2026-08-27*
