---
phase: 267.1
plan: 03
subsystem: docs
tags: [hooked-model, first-install, session-start, audit, scorecard]

requires:
  - phase: 267.1-01
    provides: "SEED-021 AskUserQuestion fix in scripts/session-start FIRST_INSTALL branch; pre-fix commit SHA; Wave 0 test scaffold"
  - phase: 267.1-02
    provides: "Settled A5 (hard-rule provenance, DIVERGENT non-blocking), A1 (additionalContext concatenation, no truncation/suppression), A2 (card-fire backstop does not rescue prose menus) grounding verdicts"
provides:
  - "The phase's central deliverable: .planning/research/2026-08-27-hooked-first-install-audit.md, a scored /70 Hooked Model audit of FIRST_INSTALL"
  - "Turned tests/run-all-267.1.sh green (repo-relative em-dash fence) without TEST_267_1_ALLOW_MISSING"
affects: ["267.1-04 (room mirror copies this document verbatim)", "267.1-05 (roadmap entries for Phase 269-adjacent and standalone follow-up phases cite the gap IDs this document defines)"]

tech-stack:
  added: []
  patterns:
    - "flat .planning/research/YYYY-MM-DD-slug.md, no frontmatter, house header-blockquote shape (2026-06-09 audit precedent)"
    - "compact 8-line loop-map gutter format, big ASCII code-path diagram demoted to an appendix"
    - "frozen 8-row scorecard labels for cross-audit comparability (45/70, 38/70, now 30/70)"

key-files:
  created:
    - .planning/research/2026-08-27-hooked-first-install-audit.md
    - .planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-03-SUMMARY.md
  modified: []

key-decisions:
  - "Task 1, Task 2, and Task 3's review-pass fix were all authored in a single Write plus two small pre-commit Edits, then committed together as one commit (e04704d4), because the plan's three tasks all modify the same brand-new file with no natural git-diff boundary between them until the document exists in full. The em-dash sweep, secrets sweep, and citation spot-check (Task 3's checks) were run against the complete document before the single commit rather than after a separate one, so no red-then-green commit pair exists for this plan -- the checks gated the commit instead of following it."
  - "Scored the FIRST_INSTALL surface 30/70 (Fragile loop band), not imported from the 45/70 LarryReach or 38/70 ignite precedents. Derived independently leg by leg: Trigger External 8/10 (deterministic banner), Trigger Internal 2/10 (no named emotion), Action B=MAP 7/10 (SEED-021 fixed but no registered gate per A2), Variable Reward 2/10 (GAP R-1, fails the decisive attributable-to-MindrianOS test), Investment 1/10 (GAP I-1, zero writers anywhere in the repo), Loop Closure 1/10 (nothing read back as a return cue), Ethics 9/10 (clean Facilitator, explicit opt-out)."
  - "GAP A-1 severity was NOT softened despite the plan's action text framing severity as contingent on the A2 answer -- A2 came back HIGH-confidence 'no rescue,' so the finding stays a defect, and the document says so explicitly rather than hedging."

requirements-completed: [AUDIT-DOC, AUDIT-R1, AUDIT-I1, AUDIT-G1, AUDIT-A1, AUDIT-S1]

metrics:
  duration: "~50 minutes"
  completed: "2026-08-27"
---

# Phase 267.1 Plan 03: FIRST_INSTALL Hooked Audit Deliverable Summary

Wrote and scored the phase's central artifact -- a 339-line, cited, `/70` Hooked Model audit of the FIRST_INSTALL onboarding surface, scoring 30/70 (Fragile loop band, diagnostic not a gate), transcribing the prior waves' settled findings (SEED-021 fix, A5/A1/A2 grounding verdicts) rather than re-deriving them, and turning the phase's em-dash-fence aggregator green without an escape flag.

## What Was Built

`.planning/research/2026-08-27-hooked-first-install-audit.md` -- the scored Hooked audit, structured per the house format established by `.planning/research/2026-06-09-hooked-model-larryreach-loop-audit.md`:

- **H1** pins both the plugin version (`v2.0.0-beta.12`, read from `.claude-plugin/plugin.json`) and the audited commit (`86a9af2728077e715e5f6a0ebf7ac9d6dcc1d50c`, the pre-fix SHA named by 267.1-01), with a header-blockquote release caveat that a `main` commit is not live until released and picked up.
- **TL;DR** answers both of the phase Goal's two open questions in plain language, not buried in a table: Reward fails because the "domain intelligence" line is unattributable improvisation with no MindrianOS machinery behind it; Investment fails because nothing in the repo ever writes `~/.mindrian-user.md`.
- **Scope section** (4 paragraphs): the Canon Appendix D entry 31 retired-as-gate-not-as-mandate distinction; the Tri-Polar CLI-only scope boundary stated as deliberate, not an oversight; the FIRST_INSTALL-not-`/mos:onboard` subject clarification; and the correction to the Goal's own JTBD-framing premise (`scripts/session-start:680` is the UPDATE branch, not FIRST_INSTALL).
- **Hard rule section** quotes `docs/reward-before-investment-rule.md` line 21 and the four-part reward test verbatim, then states the A5 verdict (DIVERGENT, non-blocking -- the tested content is byte-identical across both copies) and BIRTH-FLOW-BRIEF Decision 8's install-as-external-trigger clause.
- **Compact 8-line loop map** in the house gutter format (the big ASCII code-path diagram from RESEARCH.md moved to Appendix A, updated to reflect the shipped SEED-021 fix).
- **Leg-by-leg gap register**: 7 rows (Trigger external/internal, Action, Reward, Investment, Loop closure, Ethics), every row's Citation column carrying a `path:line` reference, with the A1 and A2 verdicts folded directly into the Trigger-external and Action rows respectively.
- **Five `###` gap subsections** with the exact frozen IDs `GAP R-1`, `GAP I-1`, `GAP G-1`, `GAP A-1`, `GAP S-1` -- GAP A-1 marked found-and-closed-in-this-phase, GAP S-1 marked owned-by-Phase-269 with the no-secrets/no-real-name constraints observed.
- **Score card**: the 8 frozen row labels in the frozen order, TOTAL 30/70, band named (Fragile loop, 30-44), diagnostic-not-a-gate label immediately below citing Appendix D entry 31.
- **Roadmap**: 5 Quick wins (3 with COLLISION NOTE naming Phase 269 at `.planning/ROADMAP.md:570`, 1 with STATUS registering a standalone follow-up phase for GAP G-1, 1 with STATUS recording the SEED-021 fix as shipped) plus a Medium-term loop-closure design paragraph.
- **Ethics: Facilitator (9/10)** with a Watch-item naming which proposed fixes push toward Dealer, plus the Canon Part 12 invisibility note.
- **Loop-health metrics** section stating that 4 of 5 skill-defined metrics are currently uninstantiable (not merely unmeasured) because the underlying behavior does not exist yet.
- **Appendices A/B/C**: the code-path diagram (updated for the SEED-021 fix), the verbatim FIRST_INSTALL prose (post-fix, with a note on what changed), and the reproduction recipe naming the isolation seam and both test-suite pins.
- **Canon compliance note**: Part 6 dog-fooding, Part 8 trivially satisfied (local reads only), Part 12 invisibility constraint on every recommendation.

## Task Commits

Tasks 1 (evidence half), 2 (judgment half), and 3 (review pass) were all authored against the same brand-new file in one continuous editing session -- the document did not exist as a partial artifact between tasks, so there is no natural git-diff boundary to split into three separate commits without reverting and re-diffing content that was never independently in a committed state. All three tasks' work is captured in one commit:

1. **Tasks 1 + 2 + 3 combined:** `e04704d4` -- `docs(267.1-03): write scored Hooked audit of FIRST_INSTALL (evidence + judgment)`. Staged and committed with an explicit pathspec (`-- .planning/research/2026-08-27-hooked-first-install-audit.md`), confirmed via `git show --stat HEAD` to touch exactly one file, per the shared-working-tree caution in force this session.

Task 3's review-pass fixes (the four checks: no-secrets sweep, em-dash fence, citation spot-check, readability pass) were run against the complete document **before** this commit, not after a separate one -- so the checks gated the commit rather than following a red-then-green pair. This is recorded as a deviation from the plan's literal three-task-three-commit expectation (see Deviations below), not a skipped step: every one of Task 3's four checks was actually performed, and the "Doe" state (the FAIL scenario the plan anticipated needing a fix for) never existed because the sweeps found nothing to fix beyond one dangling appendix cross-reference (see below).

## Task 3 review-pass results (recorded per the plan's acceptance criteria)

1. **No-secrets sweep:** `grep -cE "mbr_|Bearer |@gmail|@[a-z]+\.(com|org|edu)"` on the deliverable returns `0`. The GAP S-1 subsection cites `docs/testers/gaurav-thorat/FEEDBACK.md` by path and finding only; no key fragment, bearer token, or email address was copied in.
2. **Em-dash fence:** `LC_ALL=C.UTF-8 grep -cP '\x{2014}'` on the deliverable returns `0`. One violation was caught and fixed during authoring (an em-dash typed in the initial Reward-test table reasoning was rewritten to a hyphen before the automated check was even run) -- final state is clean.
3. **Citation spot-check (five picked, all resolve at HEAD, none corrected):**
   - `lib/core/domain-insight-sweep.cjs:5-7` (header, "THE single entry point for the Hooked variable-reward leg") -- resolves, verified via `cat -n`.
   - `lib/core/domain-insight-sweep.cjs:106` (`async function sweepDomainInsights({ domains, roomDir, sessionId, db } = {})`) -- resolves.
   - `lib/core/user-archetype.cjs:64` (the `~/.mindrian-user.md` read, third fallback) -- resolves.
   - `lib/core/navigation/room-birth.cjs:572` (the only production `writeUserMdAtomic` caller writing to `roomDir/USER.md`) -- resolves.
   - `skills/ignite/SKILL.md:50` ("`/mos:ignite` is the canonical front door for new room creation... users are encouraged to use `/mos:ignite` for the full Hooked first-cycle experience") -- resolves.

   All five citations were pulled from commit `8a65c9ee6` (RESEARCH.md's pinned commit) and independently re-verified against current HEAD in this plan; none of plan 267.1-01's or 267.1-02's changes touched `lib/` or `skills/`, only `scripts/session-start` and `tests/`, so none of these citations drifted. Verdict for all five: **resolves**, no corrections needed.
4. **Readability pass:** the `## TL;DR` section contains one sentence answering the Reward question ("the 'domain intelligence' line is the model improvising from its own training, with no MindrianOS machinery behind it, so it fails the rule's own 'attributable to MindrianOS uniquely' test") and one sentence answering the Investment question ("nothing in the entire repository ever writes that file"), both in plain language, neither requiring a table.

**One fix applied during the review pass:** the initial draft of the GAP I-1 subsection referenced "the State of the Art table below," a section this document does not actually contain (that table exists in RESEARCH.md, not in the deliverable). Corrected to point at the Roadmap's Quick win 3 instead, which is where the `/mos:profile-user`-does-not-exist warning actually lives in this document. Classified as a Rule 1 (auto-fix bug) correction, fixed inline before the commit.

## Verification

```
node tests/test-267-1-first-install-hooked-audit.cjs             -> PASS (8 assertions)
bash tests/run-all-267.1.sh (no flag)                             -> PASS=3 FAIL=0 SKIP=1, exit 0
LC_ALL=C.UTF-8 grep -cP '\x{2014}' <deliverable>                  -> 0
grep -cE "mbr_|Bearer " <deliverable>                              -> 0
grep -c "COLLISION NOTE" <deliverable>                             -> 3
wc -l <deliverable>                                                -> 339 (>= 180 minimum)
grep -c "TOTAL" <deliverable>                                      -> present, 30/70, band named
```

The SKIP line in `run-all-267.1.sh`'s output names the absolute room-mirror path (`~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-hooked-first-install-audit/2026-08-27-hooked-first-install-audit.md`) as not present on this checkout -- correct, since plan 267.1-04 creates that mirror. No `FORBIDDEN em-dash` line appears anywhere in the aggregator output.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Dangling cross-reference to a non-existent section**
- **Found during:** Task 3 readability pass
- **Issue:** The GAP I-1 subsection's first draft said "see the State of the Art table below" -- that table exists in RESEARCH.md (this plan's read-only source), not in the deliverable this plan produces. A reader following that pointer would find nothing.
- **Fix:** Redirected the cross-reference to the Roadmap's Quick win 3 sub-bullet, which is where the actual `/mos:profile-user` warning lives in this document.
- **Files modified:** `.planning/research/2026-08-27-hooked-first-install-audit.md`
- **Commit:** `e04704d4` (fixed pre-commit, part of the single combined commit)

**2. [Rule 1 - Bug] Missing lowercase "diagnostic" literal for the automated verify**
- **Found during:** Task 1 automated verify (the plan's Node-based grep-style check)
- **Issue:** The document used "DIAGNOSTIC ONLY" (all-caps) throughout, but Task 1's `<verify>` block does a case-sensitive `s.indexOf('diagnostic')` check that failed against the all-caps form.
- **Fix:** Added the lowercase phrase "diagnostic only" alongside the existing all-caps emphasis in the retired-gate-distinction paragraph, satisfying both the verify script and the document's own emphatic style.
- **Files modified:** `.planning/research/2026-08-27-hooked-first-install-audit.md`
- **Commit:** `e04704d4` (fixed pre-commit, part of the single combined commit)

### Deviation from the plan's implicit three-commit structure

**[Rule 3 - Blocking issue, resolved without architectural change]** The plan's `<output>` and general house convention (per the `task_commit_protocol`) call for committing each task individually. Because all three tasks write to the same brand-new file with no natural point where a partial version was itself a valid, independently-committable state (Task 1 alone would have been an evidence-only document with no scorecard -- not a coherent standalone artifact per the plan's own `min_lines: 180` and `contains: "TOTAL"` artifact requirements, which only the complete document satisfies), all three tasks' work landed in one commit. This is recorded here rather than fabricated into three synthetic commits with reverted intermediate content, which would misrepresent the actual authoring sequence.

## Threat Flags

None beyond what the plan's own threat model already covers. T-267.1-08 (GAP S-1 information disclosure) and T-267.1-09 (real-name exposure) were both actively mitigated during authoring (no secret fragment, no identity detail beyond the file path) and confirmed via the Task 3 no-secrets sweep. T-267.1-10 (prompt-injection posture) and T-267.1-11 (Canon Part 8 boundary) are both `accept`-disposition in the plan's threat register and this plan changed no code, only wrote a document, so neither applies. T-267.1-12 (unversioned claims) is satisfied by the H1 version+commit pin and the header blockquote's release caveat.

## Known Stubs

None. The deliverable is a complete, self-contained document -- every section the plan's `<action>` blocks specify is present and populated with real, cited content, not placeholder text. No hardcoded empty values, no "coming soon" language, no unwired data sources (this is a documentation artifact, not a UI component).

## Self-Check: PASSED

- FOUND: `.planning/research/2026-08-27-hooked-first-install-audit.md` (339 lines)
- FOUND: commit `e04704d4` (`git log --oneline -1` on the file)
- `node tests/test-267-1-first-install-hooked-audit.cjs` -> PASS (8 assertions), exit 0
- `bash tests/run-all-267.1.sh` -> PASS=3 FAIL=0 SKIP=1, exit 0
- `LC_ALL=C.UTF-8 grep -cP '\x{2014}' <deliverable>` -> 0
- `grep -c "GAP R-1\|GAP I-1\|GAP G-1\|GAP A-1\|GAP S-1"` presence individually confirmed -> all 5 present
- `git show --stat HEAD` confirms exactly one file touched by this plan's commit, no concurrent-session files swept in

---
*Phase: 267.1-hooked-model-completeness-audit-of-the-first-session-onboard*
*Completed: 2026-08-27*
