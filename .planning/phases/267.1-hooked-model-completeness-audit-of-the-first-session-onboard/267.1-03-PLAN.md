---
phase: 267.1-hooked-model-completeness-audit-of-the-first-session-onboard
plan: 03
type: execute
wave: 2
depends_on: ["267.1-01", "267.1-02"]
files_modified:
  - .planning/research/2026-08-27-hooked-first-install-audit.md
autonomous: true
requirements: [AUDIT-DOC, AUDIT-R1, AUDIT-I1, AUDIT-G1, AUDIT-A1, AUDIT-S1]
canon_parts: [6, 8, 10, 12]

must_haves:
  truths:
    - "A reader can see, for each of the four TARI legs, whether it is IMPLEMENTED or merely ASSERTED, with a file:line citation for the claim"
    - "The Reward leg question the phase Goal asked is answered explicitly: FIRST_INSTALL delivers a promise, not a payoff, and the four-part reward test is scored criterion by criterion"
    - "The Investment leg question the phase Goal asked is answered explicitly: nothing in the repo writes ~/.mindrian-user.md, so the asserted investment has no writer"
    - "The Goal's own incorrect premise about JTBD framing in FIRST_INSTALL is corrected in the document, not silently planned around"
    - "The /70 score is present, uses the frozen eight row labels, and is labelled diagnostic rather than a gate"
    - "GAP S-1 is documented and handed to Phase 269 without being fixed or re-decided here"
    - "The document pins the plugin version and the audited commit, so no claim about user-visible behavior floats free of a version"
  artifacts:
    - path: ".planning/research/2026-08-27-hooked-first-install-audit.md"
      provides: "Scored /70 Hooked Model audit of the FIRST_INSTALL surface with a fully cited leg-by-leg gap register"
      min_lines: 180
      contains: "TOTAL"
  key_links:
    - from: ".planning/research/2026-08-27-hooked-first-install-audit.md"
      to: "scripts/session-start"
      via: "file:line citations for every behavioral claim"
      pattern: "scripts/session-start"
    - from: ".planning/research/2026-08-27-hooked-first-install-audit.md"
      to: "tests/test-267-1-first-install-hooked-audit.cjs"
      via: "each machine-checkable finding names the assertion that pins it"
      pattern: "test-267-1-first-install-hooked-audit"
    - from: ".planning/research/2026-08-27-hooked-first-install-audit.md"
      to: ".planning/ROADMAP.md"
      via: "Roadmap section COLLISION NOTE sub-bullets naming Phase 269 and the follow-up phases"
      pattern: "COLLISION NOTE"
---

<objective>
Write the phase's central deliverable: a scored `/70` Hooked Model audit of the FIRST_INSTALL
surface, in the house format the two prior audits established, with a file:line citation behind
every claim.

**This is a transcription and structuring job, not a research job.** RESEARCH.md already did the
citation work at HIGH confidence: GAP R-1, GAP I-1, GAP G-1, GAP S-1, the Action-leg SEED-021
omission, the leg-by-leg TARI map, the code-path diagram, the verbatim FIRST_INSTALL prose, and
the exhaustive grep results are all in it with line numbers. Do not re-derive them. Read them,
verify the load-bearing ones still hold with a spot-check, structure them into the house
instrument, and score.

**Scope, locked.** Audit-only. The document RECOMMENDS fixes; it does not ship them. The single
exception already shipped in plan 267.1-01 (the SEED-021 card mandate) is recorded in the document
as found-and-closed-in-phase, with the pre-fix commit named, so the audit stays honest about what
it changed. GAP R-1, GAP I-1 and GAP G-1 get roadmap entries and nothing else. GAP S-1 gets
documented and handed to Phase 269.

Purpose: this repo has already built the entire audit apparatus - the framework skill, the hard
rule, the scoring precedent, the tester evidence, and the reward machinery itself. The value here
is the one thing nobody has done: pointing the existing instrument at the first surface a user
ever touches, and writing down the answer with citations.

Output: `.planning/research/2026-08-27-hooked-first-install-audit.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-RESEARCH.md
@.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-PATTERNS.md
@.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-01-SUMMARY.md
@.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-02-SUMMARY.md
@.planning/research/2026-06-09-hooked-model-larryreach-loop-audit.md
</context>

<artifacts_this_phase_produces>

This plan (267.1-03) produces:

| Artifact | Kind | Path |
|---|---|---|
| The scored `/70` Hooked audit of FIRST_INSTALL | new research document | `.planning/research/2026-08-27-hooked-first-install-audit.md` |

Full phase artifact set: `scripts/session-start` SEED-021 line, `tests/test-267-1-first-install-hooked-audit.cjs`,
`tests/run-all-267.1.sh` (267.1-01); this deliverable (267.1-03);
`~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-hooked-first-install-audit/2026-08-27-hooked-first-install-audit.md`
(267.1-04); ROADMAP entries `### Phase 267.2` and `### Phase 267.3` (267.1-05).

</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write the evidence half - header, TL;DR, loop map, and the cited leg-by-leg gap register</name>

  <files>.planning/research/2026-08-27-hooked-first-install-audit.md</files>

  <read_first>
    - `.planning/phases/267.1-.../267.1-RESEARCH.md` IN FULL. This is the load-bearing artifact. Every citation you need is already in it: the "Leg-by-Leg TARI Map" table, GAP R-1, GAP I-1, GAP G-1, GAP S-1, "The Actual FIRST_INSTALL Surface" verbatim prose, the "System Architecture Diagram: the first-install path" ASCII block, the "Code Examples" section with all verified line numbers against commit `8a65c9ee6`, the "Correction to the Goal's own premise" section, the Pitfalls, and the Runtime State Inventory.
    - `~/.claude/skills/hooked-model/SKILL.md` - the canonical TARI definitions and the Step 2 through Step 7 audit questions you must actually answer per leg. Do NOT re-derive the framework; this file is it.
    - `.planning/research/2026-06-09-hooked-model-larryreach-loop-audit.md` - the 45/70 precedent. Copy its section order, its header blockquote shape, and its compact 8-line loop-map gutter format.
    - `.planning/phases/267.1-.../267.1-PATTERNS.md` section 1 - the extracted skeleton, the blockquote pattern, and the loop-map format.
    - `.planning/phases/267.1-.../267.1-02-SUMMARY.md` - the settled A5 verdict (quote its hard-rule text rather than re-opening `docs/reward-before-investment-rule.md`) and the settled A1/A2 answers.
    - `.planning/phases/267.1-.../267.1-01-SUMMARY.md` - the pre-fix commit SHA and the "default to Skip" reasoning for the SEED-021 line that shipped.
    - `.claude-plugin/plugin.json` - read the `version` field for the header pin.
  </read_first>

  <action>
    Create `.planning/research/2026-08-27-hooked-first-install-audit.md`. Flat file directly under
    `.planning/research/`, no folder, no YAML frontmatter (the house convention for this document class).

    **H1:** `# Hooked Model Audit: FIRST_INSTALL first-session onboarding (v<version> / main @ <commit>)`
    using the `version` from `.claude-plugin/plugin.json` and the audited commit SHA from
    `267.1-01-SUMMARY.md`. Never write "users currently see X" anywhere in the document without that pin
    (RESEARCH.md Pitfall 5; the standing rule that a `main` commit is not live until released and picked up).

    **Header blockquote**, in the 2026-06-09 shape: `> Filed 2026-08-27. Scope: ... Grounded in the live
    code on branch main.` followed by a blank `>` line and a `> Milestone: ... Sibling docs: ...` line
    naming `.planning/research/2026-06-09-hooked-model-larryreach-loop-audit.md` (45/70) and the
    `/mos:ignite` audit at `.planning/debug/beta13-curing-sequence-persona-and-commands-bisect.md:187-209`
    (38/70) as the comparability set. Add a third blockquote line stating the release caveat: this audit
    describes `main` at the named commit, and the shipped plugin a user is running may differ.

    **`## TL;DR`** - short. Lead with the spine finding: the FIRST_INSTALL surface is a single interpolated
    bash string injected as `additionalContext` (`scripts/session-start:666` at the audited commit, anchored
    on the literal `[MindrianOS Onboarding] First install detected.`). It is not a program. Three of the four
    TARI legs are asserted as prose and only one has deterministic machinery behind it. Then one sentence each
    for Reward and Investment - the two legs the phase Goal named as unverified.

    **`## Scope and what this instrument is not`** - four short paragraphs, each load-bearing:

    1. **The retired-gate distinction.** Canon Appendix D entry 31 (`docs/MINDRIAN-CANON.md:397-403`) retired
       the Hooked composite AS A RATIFICATION GATE for Canon Part 10 and replaced it with the welded two-gauge
       metric. It did NOT retire the Hooked DESIGN mandate. BIRTH-FLOW-BRIEF.md Decision 8 (a product design
       rule, navigator-locked 2026-06-12) and `docs/reward-before-investment-rule.md`
       (`type: non-negotiable-constraint`) are separate instruments and remain binding. The one Hooked piece
       entry 31 explicitly KEEPS is the Manipulation Matrix / Facilitator check. Consequence, stated in the
       document: the `/70` in this audit is DIAGNOSTIC ONLY and is never a pass/fail threshold.
    2. **Tri-Polar boundary (RESEARCH.md OQ-4, decided).** `scripts/session-start` is a Claude Code SessionStart
       hook. It does not run on Desktop or Cowork, which get their first-session behavior from the MCP server
       instructions instead. This audit scores the CLI surface in depth. Desktop and Cowork first-session Hooked
       coverage is a DELIBERATE, STATED scope boundary, not an oversight, and is named here as a candidate for
       its own audit. That sentence exists specifically to satisfy the Tri-Polar Design Rule's requirement that
       a skip be a deliberate stated call.
    3. **The subject is FIRST_INSTALL, not `/mos:onboard`** (RESEARCH.md Pitfall 3). `/mos:onboard` is a
       user-invoked command a first-time user may never run; FIRST_INSTALL fires automatically. `/mos:onboard`
       appears in this document only as a comparison reference.
    4. **Correction to the phase Goal's own premise.** State it plainly: the Goal recorded "JTBD-formula framing
       for returning users" as a confirmed FIRST_INSTALL property. It is not in the FIRST_INSTALL branch. The
       JTBD-formula mandate ("CRITICAL: Frame EVERY new capability using the JTBD formula") lives in the UPDATE
       branch at `scripts/session-start:680`. FIRST_INSTALL frames capabilities as a flat bulleted list of
       "You can ask Larry to..." lines with no situation/progress/outcome structure. The Goal's "returning users"
       therefore refers to the UPDATE branch, and FIRST_INSTALL does not inherit JTBD framing.

    **`## The hard rule this is judged against`** - quote line 21 of `docs/reward-before-investment-rule.md`
    verbatim, plus the four-part reward test (Unpredictable / Intelligible / Valuable / Attributable to
    MindrianOS uniquely) and the explicit negative examples ("A grade is not a reward... A status report is not
    a reward. The Instant Brief IS a reward. The Breakthrough Scan IS a reward."). Take the text from
    `267.1-02-SUMMARY.md`, and immediately after it record the A5 provenance verdict
    (`IDENTICAL` / `DIVERGENT` / `SOURCE ABSENT`) in one sentence, so a reader knows whether the judging
    instrument was confirmed current. Also quote BIRTH-FLOW-BRIEF Decision 8's clause naming "the FIRST STEP of
    any Mindrian surface" and "external trigger (install ...)", which is what puts FIRST_INSTALL unambiguously
    in scope.

    **`## Loop map (live as of v<version>)`** - the COMPACT eight-line house format from the 2026-06-09 audit,
    NOT RESEARCH.md's large code-path diagram. Four `LEG` rows with a `v` gutter continuation line under each,
    closing with a `'---->` arc line. Left column is the leg name, right column is the actual code path or the
    verdict. Something structurally like: `TRIGGER` row naming the De Stijl banner at `scripts/session-start:626-630`
    and marking it deterministic; `ACTION` row naming the three-option menu and the SEED-021 card mandate now
    present; `REWARD` row naming the promise line and marking it NO WIRED CALL; `INVEST` row naming
    `~/.mindrian-user.md` and marking it NO WRITER; and the closing arc line stating that the loop does not
    close because nothing written during FIRST_INSTALL is read back as a return cue. Keep it to eight lines.
    RESEARCH.md's larger first-install code-path diagram goes in the appendix in task 2, not here.

    **`## Leg-by-leg gap register`** - the core evidence table. Six rows, one per: `Trigger (external)`,
    `Trigger (internal)`, `Action (B=MAP)`, `Reward (variable)`, `Investment`, `Loop closure`, plus `Ethics`.
    Four columns: `Leg | Status | Citation | Verdict`. `Status` uses exactly one of `IMPLEMENTED`,
    `ASSERTED, NOT IMPLEMENTED`, `NOT DESIGNED`, `NOT IMPLEMENTED`, `PASS`. Every `Citation` cell carries a
    `path:line` reference. Transcribe RESEARCH.md's "Leg-by-Leg TARI Map" table directly. Two edits to it:
    the Action row's SEED-021 finding now reads as found-and-closed (see below), and the Action row's confidence
    reflects the A2 answer from `267.1-02-SUMMARY.md`.

    Then, below the table, one `###` subsection per major gap, each transcribing RESEARCH.md's already-verified
    analysis. Use these exact gap IDs so the roadmap entries in plan 267.1-05 and the test failure messages in
    `tests/test-267-1-first-install-hooked-audit.cjs` all key to the same names:

    - `### GAP R-1: Reward is a promise, not a payoff` - the prose line, the fact that nothing is behind it, the
      four-criterion scoring table (2 of 4 FAIL including the decisive "Attributable to MindrianOS uniquely"),
      the two shipped-but-unreachable reward engines (`lib/core/domain-insight-sweep.cjs:5-7` and its
      db-handle requirement at `:106`; `scripts/check-pending-breakthrough.cjs` and its `{continue:true}`
      empty-state silence), and the routing gap (`skills/ignite/SKILL.md:50` names `/mos:ignite` the canonical
      front door; `COLD_START_MENU` offers `/mos:new-project`, `/mos:help`, `/mos:diagnose`, `/mos:update` and
      never names it). Name the pinning assertions in `tests/test-267-1-first-install-hooked-audit.cjs`.
    - `### GAP I-1: Investment has no writer` - the strongest finding. The prose instruction, the exhaustive
      grep result (three hits: `lib/core/user-archetype.cjs:64` READ, `skills/onboard/SKILL.md:420` PROSE,
      `commands/onboard.md:424` PROSE, zero writers), the single deterministic writer `writeUserMdAtomic`
      (`lib/core/user-md-ops.cjs:440`) reachable only from `lib/core/navigation/room-birth.cjs:572` and `:801`
      writing to `roomDir/USER.md` which does not exist at first install, the dogfood confirmation
      (`~/.mindrian-onboarded` present dated 2026-04-05, `~/.mindrian-user.md` absent), the BIRTH-FLOW-BRIEF
      corroboration that the 7-axis `role_blend` is populated by no onboarding flow, and the table of what
      FIRST_INSTALL actually deposits (`~/.mindrian/auto-update` empty flag at `:662`; `~/.mindrian-onboarded`
      version-plus-date via `scripts/check-onboard:17-20`) with the verdict that neither is Investment in the
      Hooked sense. Include the fragility bonus finding: `check-onboard --write` is itself an LLM instruction,
      so the onboarding state machine's own advance step depends on model compliance, and if the model skips it
      FIRST_INSTALL fires again next session.
    - `### GAP G-1: the enforcement mechanism has no jurisdiction here` - the linter's `commands/*.md`-only scope
      proven in three independent places (`lib/core/mva-rule-linter.cjs:7` header, `scanCommands` at `:224-240`,
      `scripts/check-reward-before-investment.cjs:127` CLI default target), and the consequence: `scripts/session-start`
      is a bash hook with no frontmatter, structurally outside the guard. Note that `/mos:onboard` at least carries
      `interactive_first_reward: reframe_question` with an honest inline "Remediation tracked as follow-up phase"
      comment (`commands/onboard.md:12`) while FIRST_INSTALL carries no declaration at all. State the framing
      RESEARCH.md reached: this is a governance gap, not just a content gap, and it explains WHY the other gaps
      survived - nothing was ever built to catch them.
    - `### GAP A-1: the Action leg was outside the SEED-021 rendering contract (FOUND AND CLOSED IN THIS PHASE)`
      - the only gap this audit closed rather than registered. State the pre-fix evidence at the audited commit
      (the FIRST_INSTALL block instructed a prose list while `MODE_MENU=` and the `Other rooms detected` region
      both mandate the `AskUserQuestion` card and name SEED-021), the B=MAP cost of that inconsistency (reading
      and typing instead of one click, on the single highest-stakes turn in the product), the fix that shipped in
      plan 267.1-01 including the "default to option 3 (Skip)" choice and why Skip and not option 1, and the
      A2 answer from `267.1-02-SUMMARY.md` with its effect on this finding's severity label. Be explicit that
      this is the ONE remediation in an otherwise audit-only phase and say why (independent of Phase 269;
      consistency fix rather than a new engagement mechanic; the guard test already existed for the siblings).
    - `### GAP S-1: pre-session Investment demanded before any Reward (OWNED BY PHASE 269)` - the tester evidence
      from `docs/testers/gaurav-thorat/FEEDBACK.md` (2026-08-25, two platforms, two sign-ins across two domains,
      15 minutes on macOS and 25-30 on Windows, root-caused as a cross-origin session-cookie break), the Hooked
      reading (the product demands a multi-step credential Investment BEFORE the Trigger fires, a
      reward-before-investment inversion at the outermost boundary), and then the handoff sentence: this is
      claimed by Phase 269 at `.planning/ROADMAP.md:564` and is documented here, quantified here, and NOT fixed
      or re-decided here. **Two hard constraints on writing this subsection:** (a) the tester file contains a
      live, unrotated Brain API key in plaintext - cite the file and its findings, never reproduce the key or
      any fragment of it; (b) the standing no-real-names rule applies - reference the file path and the finding,
      do not amplify identity details beyond the path itself.

    Write in Feynman-simplified, JTBD-oriented prose. No em-dashes (U+2014) anywhere; hyphens only.
    Do not use fenced code blocks for anything except the loop map and short verbatim citations.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/research/2026-08-27-hooked-first-install-audit.md','utf8');const need=['GAP R-1','GAP I-1','GAP G-1','GAP A-1','GAP S-1','Loop map','Leg-by-leg gap register','scripts/session-start:680','lib/core/user-md-ops.cjs:440','lib/core/domain-insight-sweep.cjs','skills/ignite/SKILL.md:50','check-pending-breakthrough','Appendix D entry 31','Tri-Polar','diagnostic'];const miss=need.filter(x=>s.indexOf(x)===-1);if(miss.length)throw new Error('missing: '+miss.join(', '));if(/\u2014/.test(s))throw new Error('em-dash found');if(/mbr_/.test(s))throw new Error('possible API key literal in deliverable');console.log('ok evidence half')"</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/research/2026-08-27-hooked-first-install-audit.md` exists and its H1 contains both the `version` string read from `.claude-plugin/plugin.json` and a commit SHA.
    - The document contains all five gap headings with the exact IDs `GAP R-1`, `GAP I-1`, `GAP G-1`, `GAP A-1`, `GAP S-1`.
    - The `## Leg-by-leg gap register` table has a `Citation` column in which every non-header row contains a `path:line`-shaped reference.
    - The document contains the literal string `scripts/session-start:680` (the JTBD correction) and the phrase correcting the Goal's premise.
    - The document contains a paragraph naming Desktop and Cowork as a deliberate stated scope boundary (Tri-Polar rule).
    - The document contains the phrase `Appendix D entry 31` and states the score is diagnostic, not a gate.
    - The `GAP S-1` subsection contains no substring matching `mbr_` and reproduces no API key: `grep -c "mbr_" <deliverable>` returns 0.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' .planning/research/2026-08-27-hooked-first-install-audit.md` returns empty.
    - The loop map block is at most 10 lines and uses the `v` gutter and closing `'---->` arc line.
    - Spot-check proof recorded in the SUMMARY: re-run `grep -rn "mindrian-user\.md" lib/ scripts/ hooks/` and `grep -c "sweepDomainInsights" scripts/session-start` and confirm the transcribed GAP I-1 and GAP R-1 claims still hold at HEAD.
  </acceptance_criteria>

  <done>Every leg of the loop has a status and a citation, the two legs the Goal named as unverified are answered, and the Goal's own incorrect premise is corrected in the document.</done>
</task>

<task type="auto">
  <name>Task 2: Write the judgment half - scorecard, roadmap with collision notes, ethics, metrics, appendix</name>

  <files>.planning/research/2026-08-27-hooked-first-install-audit.md</files>

  <read_first>
    - The document you just wrote in task 1.
    - `~/.claude/skills/hooked-model/SKILL.md` Steps 2 through 8 - you must actually answer each step's scoring question per leg, plus the Manipulation Matrix in Step 7 and the scoring bands in Step 8. The bands are `60-70` Habit Zone, `45-59` Emerging hook, `30-44` Fragile loop, `<30` No habit forming.
    - `.planning/phases/267.1-.../267.1-PATTERNS.md` section 1 - the FROZEN scorecard row labels and the `- COLLISION NOTE:` sub-bullet idiom, plus the ethics-section shape (score in the heading).
    - `.planning/research/2026-06-09-hooked-model-larryreach-loop-audit.md` - the 45/70 scorecard and its Roadmap and Ethics sections, for tone and column content.
    - `.planning/ROADMAP.md` around the Phase 269 entry - so the COLLISION NOTE sub-bullets name the real interaction, not a guess.
    - `.planning/phases/267.1-.../267.1-02-SUMMARY.md` - the A1 and A2 answers and their named effect on the Trigger (External) and Action (B=MAP) rows.
  </read_first>

  <action>
    Append to the same file.

    **`## Score card`** - a four-column markdown table, `Phase | Score | Critical gap | Priority fix`, with
    EXACTLY these eight row labels in this order, frozen by precedent so the result stays comparable with the
    45/70 and 38/70 prior audits: `Trigger (External)`, `Trigger (Internal)`, `Action (B=MAP)`,
    `Variable Reward`, `Investment`, `Loop Closure`, `Ethics`, `TOTAL`. Do not rename, reorder, add, or drop a
    row. Scores are `N/10` for the seven legs and `N/70` for TOTAL. The TOTAL row's `Critical gap` cell names
    the band by name and range, exactly as the 2026-06-09 audit does.

    Derive each score by actually answering the corresponding skill step against the evidence in task 1's gap
    register. Do not import the 45/70 or 38/70 numbers. Every score must be justified by the text in its own
    `Critical gap` cell, and the A1/A2 answers from `267.1-02-SUMMARY.md` must be reflected in the
    `Trigger (External)` and `Action (B=MAP)` rows.

    Immediately below the table, in the same visual block, a bold one-line label:
    `Diagnostic instrument, not a gate. Canon Appendix D entry 31 retired the Hooked composite as a Part 10
    ratification gate; the Manipulation Matrix is the one piece explicitly kept. This score compares against
    45/70 (LarryReach, 2026-06-09) and 38/70 (/mos:ignite) and means nothing on its own.`

    **`## Roadmap`** with two subsections, following the 2026-06-09 numbered-item format: a one-line title, the
    mechanism, then an indented `- COLLISION NOTE:` or `- STATUS:` sub-bullet.

    `### Quick wins (high impact, low effort, machinery already exists)` - at minimum:

    1. Route the first-time user to the Hooked front door. `COLD_START_MENU` advertises `/mos:new-project`, the
       scaffold backend, and never names `/mos:ignite`, which `skills/ignite/SKILL.md:50` calls the canonical
       front door for the full Hooked first-cycle experience. One menu line.
       `- COLLISION NOTE:` names Phase 269 (removing key friction changes the onboarding Trigger/Reward/Investment
       legs, `.planning/ROADMAP.md:564`) and states this belongs to the follow-up phase registered by plan
       267.1-05, not to this audit.
    2. Give the reward engines a pre-room path. `sweepDomainInsights` degrades to `extractDomains`-only without a
       db handle, per its own header, so a text-only first-session variant is reachable in principle.
       `- COLLISION NOTE:` same Phase 269 note plus the follow-up phase.
    3. Give the Investment assertion a writer, or delete the assertion. Either wire a home-directory USER.md
       writer, or remove the instruction so the prose stops promising something the product does not do. State
       both options; do not pre-decide which.
       `- COLLISION NOTE:` follow-up phase; also note `/mos:profile-user` is referenced by USER.md stubs in
       production rooms and DOES NOT EXIST, so it must not be recommended as the mechanism.
    4. Put the guard's jurisdiction over the surface that needs it most. GAP G-1 is the only finding with NO
       Phase 269 collision, which makes it the most immediately actionable item in the audit.
       `- STATUS:` registered as its own follow-up phase by plan 267.1-05, independent of Phase 269.
    5. The SEED-021 rendering-contract fix. `- STATUS: shipped in this phase (plan 267.1-01).` with the
       pre-fix commit SHA named.

    `### Medium-term` - loop-closure design. At minimum: nothing written during FIRST_INSTALL is read back as a
    return cue, so the Investment-reloads-Trigger arc never closes across sessions. Name what would have to
    become durable for it to close. Also name the fragility item: the onboarding state machine's advance step
    (`check-onboard --write`) is an LLM instruction rather than a deterministic hook action.

    **`## Ethics: <Quadrant> (N/10)`** - score in the heading itself, house style. Apply the Manipulation Matrix
    (`hooked-model` skill lines 49-56). The expected classification is Facilitator: the maker dog-foods it
    (Canon Part 6), and option 3 is an unconditional explicitly-worded opt-out ("You can skip this entirely and
    jump straight to working"), which is a textbook meaningful opt-out. One paragraph naming the quadrant and
    the deliberate anti-dark-pattern choices, then a `Watch-item:` paragraph naming which of the proposed fixes
    push toward the Dealer quadrant. Carry forward the 2026-06-09 audit's watch-item verbatim as the standing
    constraint on every recommendation above: the re-entry trigger must carry utility every time, and the
    honesty floor never bends to manufacture delight. Add the Canon Part 12 note: the Facilitator posture and
    Larry's invisibility are not tradeable for engagement.

    **`## Loop-health metrics`** - the skill's metric list, adapted: which of Session Frequency, D7/D30/D90,
    Internal Trigger Proxy, Investment Depth, Reward Satisfaction are measurable TODAY on this surface and which
    are not, and why. Note explicitly that Investment Depth is currently zero by construction (GAP I-1), so the
    metric has nothing to read.

    **`## Appendix A: the first-install code path`** - transcribe RESEARCH.md's large ASCII diagram
    ("System Architecture Diagram: the first-install path"). This is where the big diagram lives; the compact
    loop map stays up top.

    **`## Appendix B: the FIRST_INSTALL prose, verbatim`** - the unescaped payload from RESEARCH.md's
    "Verbatim FIRST_INSTALL prose" section, updated to include the SEED-021 clause that shipped in plan
    267.1-01, with a one-line note stating it was updated in-phase and naming the pre-fix commit.

    **`## Appendix C: how to reproduce these findings`** - the isolation recipe (`TMPHOME=$(mktemp -d)` with
    `HOME` and `USERPROFILE` overridden against `scripts/check-onboard`, expecting `FIRST_INSTALL`), the
    `MINDRIAN_ROOMS_ROOT`/`MINDRIAN_ROOMS_HOME` seam, the warning NOT to delete the real `~/.mindrian-onboarded`,
    and the pointer to `tests/test-267-1-first-install-hooked-audit.cjs` and `bash tests/run-all-267.1.sh` as the
    living pins for every machine-checkable claim in the document.

    **`## Canon compliance note`** - one short paragraph: this audit is a Canon Part 6 dog-fooding act; Part 8
    is trivially satisfied because the audit performs local reads only, opens no Brain wire, and egresses
    nothing; Part 12's invisibility mandate constrains every recommendation above.

    No em-dashes (U+2014). No API keys. No tester identity details beyond the file path.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/research/2026-08-27-hooked-first-install-audit.md','utf8');const rows=['Trigger (External)','Trigger (Internal)','Action (B=MAP)','Variable Reward','Investment','Loop Closure','Ethics','TOTAL'];const miss=rows.filter(r=>s.indexOf('| '+r+' |')===-1);if(miss.length)throw new Error('scorecard rows missing: '+miss.join(', '));if(!/\/70/.test(s))throw new Error('no /70 total');if(s.indexOf('COLLISION NOTE')===-1)throw new Error('no COLLISION NOTE');const secs=['## Score card','## Roadmap','### Quick wins','### Medium-term','## Ethics','## Loop-health metrics','## Appendix A','## Appendix B','## Appendix C'];const ms=secs.filter(x=>s.indexOf(x)===-1);if(ms.length)throw new Error('sections missing: '+ms.join(', '));if(/\u2014/.test(s))throw new Error('em-dash found');if(/mbr_/.test(s))throw new Error('possible API key literal');console.log('ok judgment half')"</automated>
  </verify>

  <acceptance_criteria>
    - The scorecard table contains all eight frozen row labels, in order, and no others: `Trigger (External)`, `Trigger (Internal)`, `Action (B=MAP)`, `Variable Reward`, `Investment`, `Loop Closure`, `Ethics`, `TOTAL`.
    - The `TOTAL` row contains an `N/70` value and names its band by name and numeric range.
    - The diagnostic-not-a-gate label appears within 5 lines of the scorecard table and contains the string `Appendix D entry 31`.
    - `grep -c "COLLISION NOTE" <deliverable>` returns at least 3, and each occurrence is inside the `### Quick wins` section as a `- COLLISION NOTE:` sub-bullet.
    - The `### Quick wins` section contains a `- STATUS:` sub-bullet recording the SEED-021 fix as shipped in plan 267.1-01, with a commit SHA.
    - The Ethics heading carries both a quadrant name and an `N/10` score.
    - The document contains `/mos:profile-user` alongside a statement that it does not exist, so no future reader recommends it.
    - Appendix C contains the literal `tests/test-267-1-first-install-hooked-audit.cjs` and `bash tests/run-all-267.1.sh`.
    - `grep -c "mbr_" <deliverable>` returns 0.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' <deliverable>` returns empty.
  </acceptance_criteria>

  <done>The audit is scored on the frozen instrument, comparable with 45/70 and 38/70, labelled diagnostic, and every recommendation carries either a collision note or a shipped status.</done>
</task>

<task type="auto">
  <name>Task 3: Review pass - no-secrets sweep, em-dash fence, and turn the aggregator green</name>

  <files>.planning/research/2026-08-27-hooked-first-install-audit.md</files>

  <read_first>
    - The complete deliverable you just wrote, end to end. Read it as a reader who has not seen RESEARCH.md.
    - `.planning/phases/267.1-.../267.1-RESEARCH.md` Security Domain table - the four review checks this pass enforces: no secret leakage, no real-name amplification, no prompt-injection posture change, Canon Part 8 statement present.
    - `tests/run-all-267.1.sh` - the `EMDASH_TARGETS` array now includes this deliverable's path, so the un-flagged aggregator run should go green once the file exists.
  </read_first>

  <action>
    Four checks, then fix whatever fails.

    1. **No-secrets sweep.** Search the deliverable for any Brain API key fragment (`mbr_`), any bearer token
       shape, any `.env` value, and any email address. The tester feedback file this audit cites contains a
       live unrotated key in plaintext; the audit must carry the FINDING without the key. Also confirm no
       tester identity detail beyond the file path was copied in.
    2. **Em-dash fence.** `LC_ALL=C.UTF-8 grep -nP '\x{2014}' <deliverable>`. Every hit is a violation of the
       repo hard rule. Replace with hyphens. ASCII `--` pairs are fine; only U+2014 is forbidden.
    3. **Citation spot-check.** Pick five `path:line` citations at random from the gap register and verify each
       one still resolves to what the document claims at HEAD. If any has drifted because plan 267.1-01 shifted
       `scripts/session-start`, fix the citation and note the shift in the document rather than leaving a stale
       number. Record which five you checked in the SUMMARY.
    4. **Readability pass.** Confirm the document answers, in plain language a reader can re-explain, the two
       questions the phase Goal actually asked: does the onboarding flow deliver a genuine variable Reward
       visible in the same first session, and does it build real Investment. If either answer is buried in a
       table, lift it into the TL;DR as a sentence.

    Then run `bash tests/run-all-267.1.sh` WITHOUT `TEST_267_1_ALLOW_MISSING`. Plan 267.1-01 recorded this as
    expected-red because the deliverable did not exist. It should now be green for every repo-relative
    em-dash target. The absolute room-mirror target will still print a SKIP line, which is correct: plan
    267.1-04 creates it.
  </action>

  <verify>
    <automated>bash tests/run-all-267.1.sh && node tests/test-267-1-first-install-hooked-audit.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-all-267.1.sh` (no `TEST_267_1_ALLOW_MISSING`) exits 0 and reports `FAIL=0`.
    - Its output contains a SKIP line naming the room-mirror path as not yet created (created by plan 267.1-04), and no `FORBIDDEN em-dash` line.
    - `grep -cE "mbr_|Bearer |@gmail|@[a-z]+\.(com|org|edu)" <deliverable>` returns 0.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' <deliverable>` returns 0.
    - The `## TL;DR` contains one sentence answering the Reward question and one answering the Investment question, both in plain language, neither requiring the reader to consult a table.
    - The SUMMARY names the five citations spot-checked and their verdicts (`resolves` or `corrected`).
    - `node tests/test-267-1-first-install-hooked-audit.cjs` still passes with 8 assertions.
  </acceptance_criteria>

  <done>The deliverable is clean of secrets and em-dashes, its citations resolve at HEAD, its TL;DR answers the Goal's two questions in plain language, and the phase aggregator is green without an escape flag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `docs/testers/gaurav-thorat/FEEDBACK.md` -> the audit deliverable | A tracked file containing a live unrotated Brain API key and a real tester identity is cited by this document. |
| the audit deliverable -> future readers and the room mirror | Whatever this document says gets copied to the rethinking room in plan 267.1-04 and read by future phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-267.1-08 | Information Disclosure | GAP S-1 subsection citing the tester feedback file | mitigate | Cite the file path and the finding only. Task 1's action forbids reproducing the key; task 3 runs a `grep -cE "mbr_\|Bearer \|@..."` sweep as a blocking acceptance criterion. |
| T-267.1-09 | Information Disclosure | real-name exposure | mitigate | Reference the file path and the finding; do not amplify identity details. Standing hard rule: no real names or emails of testers in tracked repos. Enforced by the same task 3 sweep. |
| T-267.1-10 | Tampering | prompt-injection posture of the injection path | accept | This plan writes a document only. It changes no injection code. The bounded-read posture at `scripts/session-start:775-777` is untouched, and the audit records it as the existing mitigation rather than proposing a change. |
| T-267.1-11 | Information Disclosure | Canon Part 8 graph boundary | accept | The audit performs local reads only, opens no Brain wire, egresses nothing. Stated explicitly in the deliverable's Canon compliance note per house convention. |
| T-267.1-12 | Repudiation | unversioned claims about user-visible behavior | mitigate | H1 pins the plugin version and the audited commit; the header blockquote carries the release caveat. Acceptance criteria check for both. |
| T-267.1-SC | Tampering | package-manager installs | accept | Zero packages installed by this plan. |
</threat_model>

<verification>
```bash
bash tests/run-all-267.1.sh
node tests/test-267-1-first-install-hooked-audit.cjs
LC_ALL=C.UTF-8 grep -cP '\x{2014}' .planning/research/2026-08-27-hooked-first-install-audit.md
grep -cE "mbr_|Bearer " .planning/research/2026-08-27-hooked-first-install-audit.md
grep -c "COLLISION NOTE" .planning/research/2026-08-27-hooked-first-install-audit.md
```
</verification>

<success_criteria>
- `.planning/research/2026-08-27-hooked-first-install-audit.md` exists, is at least 180 lines, and carries the version and commit pin in its H1.
- All five gap IDs present with `path:line` citations; the leg-by-leg register has a status for every TARI leg.
- The scorecard uses the eight frozen row labels and produces an `N/70` with its band named, labelled diagnostic and not a gate.
- The Goal's incorrect JTBD premise is corrected in the document, citing `scripts/session-start:680`.
- GAP S-1 is documented and handed to Phase 269 without being fixed or re-decided.
- Zero em-dashes, zero secret fragments, zero tester identity details beyond the file path.
- `bash tests/run-all-267.1.sh` is green without `TEST_267_1_ALLOW_MISSING`.
</success_criteria>

<output>
Create `.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-03-SUMMARY.md` when done.

Commit with `git add -f` for any `.planning/` path (`.planning/*` is gitignored in this repo; see CLAUDE.md).
</output>
