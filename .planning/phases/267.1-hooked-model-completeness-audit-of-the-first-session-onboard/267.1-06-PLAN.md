---
phase: 267.1-hooked-model-completeness-audit-of-the-first-session-onboard
plan: 06
type: execute
wave: 4
depends_on: ["267.1-04", "267.1-05"]
files_modified: []
autonomous: false
requirements: [AUDIT-DOC, AUDIT-MIRROR, AUDIT-ROADMAP, AUDIT-TEST, AUDIT-A1]
canon_parts: [6, 8, 10, 12]

must_haves:
  truths:
    - "The full phase gate is green: the pinned findings pass, the aggregator passes without an escape flag, and the repo-wide pre-commit gates CLAUDE.md names are clean"
    - "Both compositing homes exist and name each other with paths that resolve"
    - "The navigator has seen and approved the /70 scores, the OQ-3 scope call, and the two follow-up phase registrations before the phase closes"
  artifacts:
    - path: ".planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-06-SUMMARY.md"
      provides: "Phase close-out record: gate results, compositing proof, and the navigator's verdict"
      min_lines: 30
  key_links:
    - from: "tests/run-all-267.1.sh"
      to: "all phase artifacts"
      via: "aggregator run with no escape flag, plus the CLAUDE.md pre-commit gate trio"
      pattern: "FAIL=0"
---

<objective>
Close the phase: prove every gate is green, prove the Dev-Research Compositing mandate is satisfied
in both directions, and put the three judgment calls in front of the navigator before the phase is
declared done.

The three calls needing a human eye, and nothing else:

1. **The `/70` scores.** Scores are a judgment, not a computation. The navigator has run this
   instrument twice before (45/70 LarryReach, 38/70 `/mos:ignite`) and is the only person who can
   say whether this score is comparable to those.
2. **The OQ-3 scope call.** This phase was framed audit-only and shipped exactly one line of
   remediation (the SEED-021 card mandate). The reasoning is written down in plan 267.1-01 and in
   the audit's `GAP A-1` section. The navigator either ratifies that call or says it should have
   been deferred, and the answer is worth having on the record either way.
3. **The two follow-up phase registrations.** Phase 267.2 is sequenced behind Phase 269;
   Phase 267.3 is declared independent. If either sequencing is wrong, it is far cheaper to fix in
   the roadmap now than after somebody plans against it.

Purpose: this phase's own subject is a surface where nobody ever checked whether the asserted thing
was the implemented thing. Closing it without checking would be the same failure, one level up.

Output: a phase close-out SUMMARY carrying gate results, compositing proof, and the verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/2026-08-27-hooked-first-install-audit.md
@.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-VALIDATION.md
@.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-03-SUMMARY.md
@.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-04-SUMMARY.md
@.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>

This plan (267.1-06) produces no new repo artifact. It verifies the full set and records the verdict:

| Artifact | Produced by | Path |
|---|---|---|
| SEED-021 card mandate in FIRST_INSTALL | 267.1-01 | `scripts/session-start` |
| `test-267-1-first-install-hooked-audit.cjs` | 267.1-01 | `tests/test-267-1-first-install-hooked-audit.cjs` |
| `run-all-267.1.sh` | 267.1-01 | `tests/run-all-267.1.sh` |
| Scored `/70` Hooked audit | 267.1-03 | `.planning/research/2026-08-27-hooked-first-install-audit.md` |
| Room mirror entry | 267.1-04 | `~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-hooked-first-install-audit/2026-08-27-hooked-first-install-audit.md` |
| `### Phase 267.2` roadmap entry | 267.1-05 | `.planning/ROADMAP.md` |
| `### Phase 267.3` roadmap entry | 267.1-05 | `.planning/ROADMAP.md` |
| `Cross-references:` paragraph on the 267.1 entry | 267.1-05 | `.planning/ROADMAP.md` |

</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run the full phase gate and prove the compositing mandate in both directions</name>

  <files>none (verification only; results go to the plan SUMMARY)</files>

  <read_first>
    - `.planning/phases/267.1-.../267.1-VALIDATION.md` - the Per-Task Verification Map and the Validation Sign-Off checklist you are closing out. Note that its `sed -n` command column was superseded by literal anchors in plan 267.1-01; record that supersession explicitly rather than leaving the map self-contradictory.
    - `CLAUDE.md` Verification section - the pre-commit gate trio it names: `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs`. Plan 267.1-01 edited `scripts/session-start`, so these must be clean before the phase closes.
    - `.planning/research/2026-08-27-hooked-first-install-audit.md` - the finished deliverable, read end to end once as a reader.
    - The three plan SUMMARYs from 267.1-03, 267.1-04, 267.1-05.
  </read_first>

  <action>
    Run and record, each with its exit status, in this order:

    1. `bash tests/run-all-267.1.sh` with NO `TEST_267_1_ALLOW_MISSING` flag. Expect `FAIL=0` and no
       `FORBIDDEN em-dash` line. Both em-dash arrays should now resolve: the repo-relative targets (test file,
       aggregator, `scripts/session-start`, the audit deliverable) and the absolute room-mirror target.
    2. `node tests/test-267-1-first-install-hooked-audit.cjs` - expect `PASS` with 8 assertions.
    3. `node tests/test-209-session-start-exemplar.cjs` - the guard plan 267.1-01 put at risk. Expect `PASS`.
    4. `bash -n scripts/session-start`.
    5. The CLAUDE.md pre-commit gate trio: `node scripts/build-connector-registry.cjs --check`,
       `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs`.
    6. `node scripts/doctor.cjs --acceptance` - the acceptance roll-up. Record the result; if it surfaces a
       pre-existing failure unrelated to this phase, say so explicitly and name it rather than silently passing
       or silently failing. A pre-existing unrelated failure is not this phase's to fix, but it IS this phase's
       to name.

    Then prove the compositing mandate programmatically, not by eye. For each direction, confirm the referenced
    path EXISTS on disk:

    - forward: the audit deliverable's `> Room mirror:` line names a path; `fs.existsSync` it.
    - back: the room entry's `> Lands in:` blockquote names the phase directory and the audit path;
      `fs.existsSync` both.

    Then run the no-secrets sweep across every artifact this phase produced, including the roadmap and the room
    mirror: search for `mbr_`, `Bearer `, and any email address. Expect zero hits everywhere.

    Finally, reconcile `267.1-VALIDATION.md`'s sign-off checklist. For each of its six boxes state MET or NOT MET
    with the evidence. Explicitly record the two supersessions so a future reader is not misled:
    (a) rows 267.1-01-03 and 267.1-01-04's `sed -n` commands were replaced by literal anchors, because line
    numbers in `scripts/session-start` are unstable and plan 267.1-01 shifted the file;
    (b) row 267.1-01-04 was inverted from a negative pin (SEED-021 absent) to a positive pin (SEED-021 present),
    because the OQ-3 call brought that one fix in scope.
    Also state that row 267.1-02-01 (em-dash lint) and row 267.1-02-02 (compositing) are now AUTOMATED by
    `tests/run-all-267.1.sh` rather than manual.

    Write everything into the SUMMARY as a table: check, command, exit status, verdict.
  </action>

  <verify>
    <automated>bash tests/run-all-267.1.sh && node tests/test-267-1-first-install-hooked-audit.cjs && node tests/test-209-session-start-exemplar.cjs && bash -n scripts/session-start && node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check && node scripts/check-render-coverage.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-all-267.1.sh` (no escape flag) exits 0, reports `FAIL=0`, and its output contains no `FORBIDDEN em-dash` and no `MISSING (not yet created)` line.
    - `node tests/test-267-1-first-install-hooked-audit.cjs` exits 0 with 8 assertions.
    - `node tests/test-209-session-start-exemplar.cjs` exits 0.
    - All three CLAUDE.md pre-commit gates exit 0.
    - `node scripts/doctor.cjs --acceptance` result is recorded; any failure is classified as `pre-existing and unrelated` (with the named check) or `caused by this phase` (which blocks close-out).
    - Both cross-link directions verified with `fs.existsSync`, not by reading: the forward `> Room mirror:` path and both back-link paths resolve.
    - No-secrets sweep returns 0 hits for `mbr_`, `Bearer `, and email-shaped strings across `.planning/research/2026-08-27-hooked-first-install-audit.md`, `.planning/ROADMAP.md`, and the room mirror file.
    - The SUMMARY contains a reconciliation of all six `267.1-VALIDATION.md` sign-off boxes, each MET or NOT MET with evidence.
    - The SUMMARY records both VALIDATION.md supersessions (the `sed -n` to literal-anchor substitution, and the 267.1-01-04 negative-to-positive pin inversion) and the two rows that became automated.
  </acceptance_criteria>

  <done>Every gate is green with a recorded exit status, both compositing homes resolve programmatically, no secret leaked into any artifact, and the validation contract is reconciled rather than left self-contradictory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Navigator review of the scores, the scope call, and the two registrations</name>

  <files>none (review checkpoint; any correction the navigator orders is applied to `.planning/research/2026-08-27-hooked-first-install-audit.md` or `.planning/ROADMAP.md` after the verdict, then re-verified)</files>

  <read_first>
    - `.planning/research/2026-08-27-hooked-first-install-audit.md` `## TL;DR` and `## Score card` sections - the numbers under review.
    - `.planning/ROADMAP.md` `### Phase 267.2` and `### Phase 267.3` entries - the sequencing under review.
    - `.planning/phases/267.1-.../267.1-06-SUMMARY.md` task 1 gate table - so you can tell the navigator that everything machine-checkable is already green and only judgment is left.
  </read_first>

  <action>
    Present the three judgment calls below to the navigator, in the order given, and stop. Do not pre-empt the
    verdict, do not soften a score to avoid a question, and do not make any correction before the navigator rules.

    Present each as a short block: what the call is, what it currently says, and the one question that decides it.
    Keep the whole presentation short. The navigator reads the score card and the two roadmap entries directly;
    the job here is to name the three decisions, not to re-narrate the audit.

    After the verdict: if the navigator names score rows to change, edit the `## Score card` table in
    `.planning/research/2026-08-27-hooked-first-install-audit.md` and update the `Cross-references:` paragraph on
    the Phase 267.1 entry in `.planning/ROADMAP.md` so the score quoted there matches. If the navigator corrects
    a sequencing call, edit only the affected `**Depends on:**` line. If the navigator reverses the OQ-3 call,
    record the reversal and the follow-up action in the SUMMARY and do NOT revert `scripts/session-start` inside
    this task; a revert plus test-pin inversion is its own change and belongs in a follow-up.

    Re-run `bash tests/run-all-267.1.sh` after any edit and record the result. Then write the phase close-out
    verdict into the SUMMARY as exactly `APPROVED` or `CHANGES REQUESTED` followed by the change list.
  </action>

  <what-built>
    Everything in this phase is complete and machine-verified. Task 1's gate table is in the SUMMARY.

    - `scripts/session-start` FIRST_INSTALL now carries the SEED-021 `AskUserQuestion` card mandate, matching
      the two sibling cold-start branches. One line, defaulting to option 3 (Skip).
    - `tests/test-267-1-first-install-hooked-audit.cjs` pins all six machine-checkable audit findings plus a
      `bash -n` proof, anchored on literal strings rather than line numbers.
    - `tests/run-all-267.1.sh` runs the pins, gates the 209 exemplar, and fences the audit deliverable and the
      room mirror against em-dashes.
    - `.planning/research/2026-08-27-hooked-first-install-audit.md` is the scored `/70` audit with the
      leg-by-leg cited gap register.
    - The room mirror is filed and cross-linked in both directions.
    - `.planning/ROADMAP.md` carries Phase 267.2 and Phase 267.3 and a Cross-references paragraph on 267.1.
  </what-built>

  <how-to-verify>
    Three things to look at, in this order. Everything else is already machine-checked.

    1. **The scores.** Open `.planning/research/2026-08-27-hooked-first-install-audit.md` and read the
       `## Score card` table plus the `## TL;DR`. Question to answer: is this score comparable to the 45/70
       LarryReach audit and the 38/70 `/mos:ignite` audit, or did this run drift to a different standard? The
       row labels are frozen precisely so the three are comparable. If any leg's number feels wrong, say which
       row and what it should be.

    2. **The OQ-3 scope call.** Read the `### GAP A-1` section of the same document, and
       `git show --stat` the `scripts/session-start` change from plan 267.1-01. The phase was framed audit-only
       and shipped exactly one line of remediation. The reasoning: independent of Phase 269, a rendering-contract
       consistency fix rather than a new engagement mechanic, and the guard test already existed for the two
       sibling branches. Question to answer: ratify, or should it have been deferred to Phase 267.2 as well?

    3. **The two follow-up registrations.** Read `### Phase 267.2` and `### Phase 267.3` in
       `.planning/ROADMAP.md`. Two sequencing calls to check: 267.2 (reward routing plus investment writer) is
       sequenced BEHIND Phase 269, because removing the key friction changes the very legs it would repair.
       267.3 (guard jurisdiction) is declared INDEPENDENT of Phase 269, because it is the one finding with no
       collision. Question to answer: are both sequencing calls right, and should GAP R-1 and GAP I-1 really be
       one phase with two workstreams rather than two phases?

    Fast path if you want it: `grep -A 12 "## Score card" .planning/research/2026-08-27-hooked-first-install-audit.md`
    and `node -e "const s=require('fs').readFileSync('.planning/ROADMAP.md','utf8');const i=s.indexOf('### Phase 267.2');console.log(s.slice(i, s.indexOf('### Phase 268')))"`.
  </how-to-verify>

  <verify>
    <automated>bash tests/run-all-267.1.sh</automated>
    <human-check>Navigator confirms the /70 scores are comparable to the 45/70 and 38/70 precedents, ratifies or reverses the OQ-3 scope call, and approves or corrects the Phase 267.2 / Phase 267.3 sequencing.</human-check>
  </verify>

  <acceptance_criteria>
    - The navigator's verdict on each of the three items is recorded verbatim in the SUMMARY, one line each.
    - If the scores are corrected: the named rows and their new values are recorded, and the audit deliverable and the roadmap Cross-references paragraph are updated to match before the phase closes.
    - If the OQ-3 call is reversed: the SUMMARY records that the SEED-021 line should be reverted and moved into Phase 267.2's scope, and a follow-up action is named. Do not revert it inside this checkpoint task.
    - If either sequencing call is corrected: the affected `Depends on:` line in `.planning/ROADMAP.md` is updated and `bash tests/run-all-267.1.sh` is re-run green.
    - The SUMMARY states the phase close-out verdict explicitly: `APPROVED` or `CHANGES REQUESTED` with the change list.
  </acceptance_criteria>

  <resume-signal>Type "approved" to close the phase, or name the rows/sequencing to change.</resume-signal>

  <done>The navigator has ruled on the three judgment calls and the ruling is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| phase artifacts -> the repo's shared history | Everything this phase produced becomes input to future planning sessions once committed. |
| the navigator checkpoint -> phase closure | The only place a human judgment enters the phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-267.1-21 | Information Disclosure | final artifact set | mitigate | Task 1 runs a no-secrets sweep for `mbr_`, `Bearer `, and email-shaped strings across the audit deliverable, ROADMAP.md, and the room mirror, as a blocking acceptance criterion. The cited tester file carries a live unrotated key and is referenced by path only. |
| T-267.1-22 | Repudiation | a green harness that never discovered the tests | mitigate | The aggregator's `found -eq 0` guard and its explicit gate line for `tests/test-209-session-start-exemplar.cjs` are both verified in task 1. "A test that is never discovered is worse than no test, because the harness still prints green" is the house reason and it is checked, not assumed. |
| T-267.1-23 | Tampering | `scripts/session-start` edited without the repo-wide gates run | mitigate | The CLAUDE.md pre-commit trio (`build-connector-registry --check`, `build-orchestration-projection --check`, `check-render-coverage`) runs in task 1's automated verify and must exit 0. |
| T-267.1-24 | Elevation of Privilege | closing the phase on an unreviewed judgment | mitigate | Task 2 is a blocking human checkpoint. `workflow.auto_advance` is `false` in `.planning/config.json`, so it will actually block. |
| T-267.1-25 | Information Disclosure | Canon Part 8 graph boundary | accept | The entire phase performed local reads, local writes, and one bounded Claude Code mechanics consultation carrying no room content. No Brain wire was opened. |
| T-267.1-SC | Tampering | package-manager installs | accept | Zero packages installed across the whole phase. `package.json` declares zero devDependencies and the Package Legitimacy Gate is not applicable. |
</threat_model>

<verification>
```bash
bash tests/run-all-267.1.sh
node tests/test-267-1-first-install-hooked-audit.cjs
node tests/test-209-session-start-exemplar.cjs
node scripts/build-connector-registry.cjs --check
node scripts/build-orchestration-projection.cjs --check
node scripts/check-render-coverage.cjs
node scripts/doctor.cjs --acceptance
ls -la ~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-hooked-first-install-audit/
grep -c "COLLISION NOTE" .planning/ROADMAP.md
```
</verification>

<success_criteria>
- Every gate in the verification block recorded with an exit status; all phase-owned gates green.
- Both compositing directions verified with `fs.existsSync`; both paths resolve.
- Zero secret fragments and zero email addresses across the audit deliverable, ROADMAP.md, and the room mirror.
- All six `267.1-VALIDATION.md` sign-off boxes reconciled, and both supersessions recorded.
- Navigator verdict recorded for the scores, the OQ-3 scope call, and the two follow-up registrations.
- Phase close-out verdict stated explicitly as `APPROVED` or `CHANGES REQUESTED`.
</success_criteria>

<output>
Create `.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-06-SUMMARY.md` when done.

Commit with `git add -f` for any `.planning/` path (`.planning/*` is gitignored in this repo; see CLAUDE.md).
Do not commit the room path from this repo.
</output>
