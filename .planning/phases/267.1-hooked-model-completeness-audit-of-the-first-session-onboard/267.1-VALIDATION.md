---
phase: 267.1
slug: hooked-model-completeness-audit-of-the-first-session-onboard
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-27
---

# Phase 267.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Plain `node:assert`, standalone executable `.cjs` scripts under `tests/` (no test runner, no devDependencies) |
| **Config file** | none — Wave 0 installs `tests/test-267-1-first-install-hooked-audit.cjs` and `tests/run-all-267.1.sh` |
| **Quick run command** | `node tests/test-267-1-first-install-hooked-audit.cjs` |
| **Full suite command** | `bash tests/run-all-267.1.sh` |
| **Estimated runtime** | ~5 seconds (grep/read assertions only, no network, no room fixtures) |

---

## Sampling Rate

- **After every task commit:** Run `node tests/test-267-1-first-install-hooked-audit.cjs`
- **After every plan wave:** Run `bash tests/run-all-267.1.sh`
- **Before `/gsd-verify-work`:** Full suite must be green, plus the pre-commit gates CLAUDE.md names (`build-connector-registry.cjs --check`, `build-orchestration-projection.cjs --check`, `check-render-coverage.cjs`)
- **Max feedback latency:** 5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 267.1-01-01 | 01 | 0 | GAP-I-1 | — | `~/.mindrian-user.md` has no writer in the repo | grep assertion | `! grep -rn "mindrian-user\.md" lib/ scripts/ hooks/ \| grep -qE "writeFile\|writeUserMdAtomic"` | ❌ W0 | ⬜ pending |
| 267.1-01-02 | 01 | 0 | GAP-R-1 | — | `sweepDomainInsights` is never called from `scripts/session-start` | grep assertion | `! grep -q "sweepDomainInsights" scripts/session-start` | ❌ W0 | ⬜ pending |
| 267.1-01-03 | 01 | 0 | GAP-R-1 | — | Cold-start menu (`scripts/session-start:657`) omits `/mos:ignite` | grep assertion | `! sed -n '657p' scripts/session-start \| grep -q "mos:ignite"` | ❌ W0 | ⬜ pending |
| 267.1-01-04 | 01 | 0 | Action-leg | — | FIRST_INSTALL block (lines 665-667) lacks the SEED-021 `AskUserQuestion` mandate | grep assertion | `[ "$(sed -n '665,667p' scripts/session-start \| grep -c AskUserQuestion)" = "0" ]` | ❌ W0 | ⬜ pending |
| 267.1-01-05 | 01 | 0 | GAP-G-1 | — | Reward-before-investment linter scans `commands/*.md` only, never hooks | unit | `node -e "require('./lib/core/mva-rule-linter.cjs')"` + assert `scanCommands` targets `commands/` | ❌ W0 | ⬜ pending |
| 267.1-01-06 | 01 | 0 | Reachability | — | Virgin `HOME` yields `FIRST_INSTALL` from `check-onboard` | integration | `HOME=$(mktemp -d) USERPROFILE=$(mktemp -d) bash scripts/check-onboard \| head -1` equals `FIRST_INSTALL` | ⚠️ pattern exists (`tests/test-onboarding-gate.cjs`) | ⬜ pending |
| 267.1-02-01 | 02 | — | Deliverable hygiene | — | Deliverable contains no em-dashes | lint | `! grep -P '\x{2014}' <deliverable path>` | ❌ W0 | ⬜ pending |
| 267.1-02-02 | 02 | — | Compositing mandate | — | Deliverable exists in both `.planning/` and the rethinking room | manual | `ls <planning-path> && ls ~/MindrianRooms/rethinking-mindrianos/research/<entry>/` | manual only | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test-267-1-first-install-hooked-audit.cjs` — one test file asserting all machine-checkable audit claims above (GAP-I-1, GAP-R-1 x2, Action-leg, GAP-G-1, reachability), so the findings become regression-pinned rather than a point-in-time snapshot. Per research: "the single highest-value validation artifact of the phase — without it, a future edit silently invalidates the audit."
- [ ] `tests/run-all-267.1.sh` — phase aggregator, following the `run-all-264.sh` / `run-all-266.sh` idiom
- [ ] Framework install: NOT needed (plain node scripts, zero deps; `package.json` declares zero devDependencies)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|--------------------|
| Deliverable filed in both `.planning/phases/267.1-.../` and `~/MindrianRooms/rethinking-mindrianos/research/<dated-entry>/`, cross-linked | Compositing mandate (CLAUDE.md) | No automated check for cross-repo room filing | Confirm both paths exist and each references the other's path in prose |
| Audit deliverable correctly labels the `/70` score as diagnostic, not a gate (Canon Appendix D entry 31) | Hard-rule interpretation | Requires human judgment on prose framing, not a grep-able string | Read the scorecard section; confirm the diagnostic-not-gate label appears adjacent to the score |
| OQ-2 (SessionStart `additionalContext` delivery + card-fire backstop on turn 1) resolved via claude-code-guide consultation, if the plan scopes it in | Assumption A1/A2 | Requires live Claude Code hook-firing knowledge, not machine-checkable from this repo alone | Consult claude-code-guide narrowly on the two questions in RESEARCH.md OQ-2; record the answer's effect on the Action-leg confidence rating |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
