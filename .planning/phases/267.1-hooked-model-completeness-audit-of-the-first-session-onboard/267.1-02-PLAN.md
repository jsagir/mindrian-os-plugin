---
phase: 267.1-hooked-model-completeness-audit-of-the-first-session-onboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [AUDIT-A1, AUDIT-DOC]
canon_parts: [3, 6, 8]

must_haves:
  truths:
    - "Assumption A5 is settled: the in-repo copy of docs/reward-before-investment-rule.md is confirmed either identical to, or divergent from, its canonical room source, with the divergence named"
    - "Assumption A1 is settled: whether the FIRST_INSTALL additionalContext reaches the model intact across the nine competing SessionStart hooks"
    - "Assumption A2 is settled: whether the check-card-fire.cjs Stop-hook backstop rescues the FIRST_INSTALL three-option prose into an AskUserQuestion card on turn 1 of a virgin session"
    - "Each answer carries an explicit confidence level and an explicit statement of which audit score it moves and by how much"
  artifacts:
    - path: ".planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-02-SUMMARY.md"
      provides: "The three settled assumptions in a form plan 267.1-03 transcribes directly into the audit deliverable"
      min_lines: 40
      contains: "A5"
  key_links:
    - from: "267.1-02-SUMMARY.md"
      to: ".planning/research/2026-08-27-hooked-first-install-audit.md"
      via: "plan 267.1-03 reads this SUMMARY as the source for the Action-leg confidence rating and the Hard Rule provenance note"
      pattern: "Action leg"
---

<objective>
Settle the three assumptions RESEARCH.md flagged as unresolved before the audit deliverable is
written, so the scored document rests on verified ground rather than on caveats.

This plan modifies no repo file. Its entire output is its SUMMARY, which plan 267.1-03 reads.

Three assumptions, in RESEARCH.md's own numbering:

- **A5** - whether `docs/reward-before-investment-rule.md` (the in-repo copy of the hard rule this
  audit judges against) has diverged from its canonical source. Its own frontmatter says to treat
  the source room as authoritative on divergence. If the audit cites a stale copy of the rule, every
  verdict derived from the four-part reward test is suspect.
- **A1** - whether the FIRST_INSTALL `additionalContext` reaches the model intact across the nine
  SessionStart hooks registered on the same `startup|clear|compact` matcher.
- **A2** - whether `scripts/check-card-fire.cjs`'s Stop-hook backstop rescues the FIRST_INSTALL
  three-option prose into an `AskUserQuestion` card on turn 1 of a virgin session.

**Scope fence, non-negotiable.** A1 and A2 are the ONE legitimate claude-code-guide / claude-api
consultation in this phase (RESEARCH.md OQ-2, and CLAUDE.md's grounding mandate: "any claim about
Claude Code's own hooks/matchers"). Ask exactly those two questions. Do not let it expand into a
general SessionStart hook audit, a hooks.json review, or a card-fire redesign. The answers affect
only a confidence rating on the Action leg. They do not produce a build task.

**Explicitly NOT consulted in this plan** (RESEARCH.md rules both out and the reasoning is recorded
so nobody re-opens it): langtalks-graph-expert (this is product-UX and Claude-Code-internal, not the
agent-engineering corpus it covers) and Context7 (no library or API contract is in question). No
WebSearch or Tavily either: nothing here is time-sensitive.

Purpose: an audit whose load-bearing rule might be stale, and whose friction finding might be
rescued by a backstop nobody checked, is not a finished audit.

Output: `267.1-02-SUMMARY.md` with three settled answers, each with a confidence level and a named
effect on the scorecard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-RESEARCH.md
@docs/reward-before-investment-rule.md
</context>

<artifacts_this_phase_produces>

This plan (267.1-02) produces NO repo file. Its sole artifact is:

| Artifact | Kind | Path |
|---|---|---|
| Settled assumptions A5, A1, A2 | plan SUMMARY (consumed by 267.1-03) | `.planning/phases/267.1-.../267.1-02-SUMMARY.md` |

Full phase artifact set: `scripts/session-start` SEED-021 line, `tests/test-267-1-first-install-hooked-audit.cjs`,
`tests/run-all-267.1.sh` (all 267.1-01); `.planning/research/2026-08-27-hooked-first-install-audit.md` (267.1-03);
`~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-hooked-first-install-audit/2026-08-27-hooked-first-install-audit.md`
(267.1-04); ROADMAP entries `### Phase 267.2` and `### Phase 267.3` (267.1-05).

</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Settle assumption A5 - is the in-repo hard rule a faithful copy of its canonical source?</name>

  <files>none (read-only; findings go to the plan SUMMARY)</files>

  <read_first>
    - `docs/reward-before-investment-rule.md` in full. Note especially: the frontmatter (`type: non-negotiable-constraint`, `applies_to`, and the clause naming the source room as authoritative on divergence), line 21 (the rule itself), lines 36-43 (the violating-ask list), lines 52-56 (the four-part reward test), lines 58-62 (the positive and negative reward examples), lines 81-84 (the `/mos:onboard` remediation the rule prescribes), lines 97-108 (the detection mechanism and `interactive_first_reward` frontmatter contract), lines 131-137 (the REWARD_TYPES closed vocabulary).
    - `.planning/phases/267.1-.../267.1-RESEARCH.md` section "The Hard Rule: what is actually asserted" and Assumptions Log row A5.
  </read_first>

  <action>
    Locate the canonical source copy at `~/MindrianRooms/mindrian/mindrianOS/sub-rooms/communications/conversion-fix/solution-design/`
    (RESEARCH.md A5 names this path). If the directory or the file is not there, search the `mindrian` room
    for a filename containing `reward-before-investment` before concluding it is absent.

    Then produce one of three verdicts, and only one:

    - **IDENTICAL** - `diff` returns empty (ignore trailing-whitespace-only differences). State the diff command you ran and its exit status.
    - **DIVERGENT** - enumerate every substantive difference. "Substantive" means: a change to line 21's rule text, to the violating-ask list, to the four-part reward test, to the positive/negative examples, or to the REWARD_TYPES vocabulary. Formatting and heading-level differences are NOT substantive; say so and move on. For each substantive difference, state which of this audit's verdicts it would move.
    - **SOURCE ABSENT** - the canonical copy is not on this machine. Say so plainly, name the paths you checked, and state that the in-repo copy is therefore treated as authoritative for this audit with the divergence risk left open. Do NOT invent a canonical version and do NOT edit the in-repo file.

    Do not modify `docs/reward-before-investment-rule.md` under any verdict. If the copies have diverged,
    that is a finding for the audit deliverable and, if material, a follow-up phase - not an edit this plan makes.

    Write the verdict into the SUMMARY under a heading `## A5: hard-rule provenance` in a form plan 267.1-03
    can transcribe verbatim into the audit deliverable's Hard Rule section.
  </action>

  <verify>
    <automated>test -f docs/reward-before-investment-rule.md && grep -q "non-negotiable-constraint" docs/reward-before-investment-rule.md && git diff --quiet -- docs/reward-before-investment-rule.md && echo "hard rule present and unmodified"</automated>
  </verify>

  <acceptance_criteria>
    - `git diff --quiet -- docs/reward-before-investment-rule.md` exits 0 (the file was read, never edited).
    - The SUMMARY contains a `## A5: hard-rule provenance` heading whose body states exactly one of `IDENTICAL`, `DIVERGENT`, or `SOURCE ABSENT`.
    - The SUMMARY names every filesystem path checked for the canonical copy, whether or not it was found.
    - If `DIVERGENT`: every substantive difference is listed with the audit verdict it moves. If `IDENTICAL`: the exact diff command and its exit status are recorded. If `SOURCE ABSENT`: the SUMMARY states the in-repo copy is treated as authoritative and the risk is left open.
    - The SUMMARY quotes line 21's rule text and the four criteria of the reward test verbatim, so plan 267.1-03 does not have to re-open the file.
  </acceptance_criteria>

  <done>Assumption A5 is closed with a named verdict, and the audit's judging instrument is either confirmed current or its staleness is named and bounded.</done>
</task>

<task type="auto">
  <name>Task 2: Settle assumptions A1 and A2 via one tightly-scoped claude-code-guide consultation</name>

  <files>none (read-only; findings go to the plan SUMMARY)</files>

  <read_first>
    - `hooks/hooks.json` - the nine SessionStart registrations on the `startup|clear|compact` matcher, in order, including their `async` settings.
    - `scripts/session-start` - the injection site around the `additionalContext` emission and the bounded-read guard on STATE.md (the ~30-line read, backtick strip, 120-char cap). You need to know the shape of the envelope this hook emits.
    - `scripts/check-card-fire.cjs` - the Stop-hook backstop, specifically `classifyCardFire` and its pass-reasons `no-gate-signal`, `gate-irrelevant-to-turn`, `primary-gate-existence-unconfirmed`, `gate-is-simple-binary`.
    - `.planning/phases/267.1-.../267.1-RESEARCH.md` Assumptions Log rows A1 and A2, and Open Question OQ-2.
    - `tests/test-209-card-fire-gate.cjs` and `tests/test-209-backstop-tuning.cjs` - the existing coverage of the backstop, so you can state what is already pinned versus genuinely unknown.
  </read_first>

  <action>
    Consult the `claude-code-guide` agent (or the `claude-api` skill if the agent is unavailable) with EXACTLY
    two questions and no others:

    Q1 (A1): When several SessionStart hooks are registered on the same matcher and more than one emits
    `hookSpecificOutput.additionalContext`, how does Claude Code combine them? Specifically: are the envelopes
    concatenated, does a later hook's envelope replace an earlier one, is there a size cap or truncation
    behavior, and does a non-zero exit or an `async` setting on one hook suppress another's context? The
    concrete case is a roughly 3000-character `additionalContext` payload emitted by hook number 2 of 9.

    Q2 (A2): On turn 1 of a session, does the `Stop` hook's card-fire backstop have anything to act on when
    the model rendered a three-option menu as prose because its SessionStart instruction did not name
    `AskUserQuestion`? Put concretely: can a Stop-hook backstop convert an already-emitted prose menu into an
    `AskUserQuestion` card, or can it only flag/log the miss for a subsequent turn?

    Rules for this consultation:

    - Two questions. If the answer opens an interesting third question, write it down as a note for a future
      phase and stop. Do not ask it.
    - Do not ask for a hooks.json review, a redesign, or a general reliability audit.
    - Record the answer with a confidence level (HIGH / MEDIUM / LOW) and note whether it came from
      documented behavior, from reading the Claude Code binary or its bundled docs, or from inference.
      An honest "not documented, here is the inference and its basis" is a valid and expected answer.
    - Cross-check Q2 against what `scripts/check-card-fire.cjs` actually does in this repo before accepting
      a generic answer. The repo's own implementation is authoritative for what THIS backstop does.

    Then write the effect on the audit, explicitly, in this shape:

    - A1 answer -> effect on the Trigger (External) and Action (B=MAP) scores. If context delivery is
      reliable, the Trigger leg stays as assessed. If it is not, say by how much the baseline shifts.
    - A2 answer -> effect on the Action (B=MAP) score and on the severity label of the SEED-021 finding.
      If the backstop DOES rescue the prose, the finding is downgraded from "defect" to "undeclared reliance
      on a backstop" - still a finding, different severity, and plan 267.1-01's fix remains correct either way
      because the two sibling branches declare the mandate rather than relying on the backstop.
    - State plainly that neither answer creates a build task in this phase.

    Write both under a heading `## A1 and A2: SessionStart context delivery and the card-fire backstop` in the SUMMARY.
  </action>

  <verify>
    <automated>git diff --quiet && git status --porcelain | grep -v '^?? .planning/' | wc -l | grep -qx '0' && echo "no repo files modified by consultation"</automated>
  </verify>

  <acceptance_criteria>
    - `git status --porcelain` shows no modified tracked files attributable to this plan (consultation is read-only).
    - The SUMMARY contains a `## A1 and A2: SessionStart context delivery and the card-fire backstop` heading.
    - Exactly two questions were asked; the SUMMARY reproduces both question texts verbatim and labels any spawned third question as `DEFERRED - not asked`.
    - Each answer carries an explicit confidence level (`HIGH`, `MEDIUM`, or `LOW`) and an explicit provenance label (`documented`, `binary/bundled-docs`, or `inference`).
    - The SUMMARY states, for A1, the named effect on the Trigger (External) and Action (B=MAP) scores; for A2, the named effect on the Action (B=MAP) score and on the SEED-021 finding's severity label.
    - The SUMMARY contains the sentence stating that neither answer creates a build task in this phase.
    - The SUMMARY cross-references at least one concrete `scripts/check-card-fire.cjs` pass-reason by name when answering Q2.
    - `grep -c "langtalks\|Context7\|WebSearch\|Tavily" ` over the SUMMARY returns 0 hits used as a consulted source (a sentence recording that they were deliberately NOT consulted is allowed and expected).
  </acceptance_criteria>

  <done>A1 and A2 are answered with confidence and provenance, their effect on two named scorecard rows is stated, and the consultation stayed inside its two-question fence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local repo -> external consultation agent | Only generic Claude Code hook-mechanics questions cross this boundary. No room content, no user data, no secrets. |
| `~/MindrianRooms/mindrian/...` -> this plan | The canonical hard-rule copy lives in a user room; this plan reads it and reports a diff verdict only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-267.1-05 | Information Disclosure | claude-code-guide consultation | mitigate | The two questions are fixed text about Claude Code hook mechanics. They name no room, no venture, no tester, and carry no file contents. Canon Part 8 holds by construction: only generic methodology-shaped handles cross the wire. |
| T-267.1-06 | Information Disclosure | reading the `mindrian` room for the canonical rule copy | mitigate | Read-only, local, never egressed. The plan reports only a diff verdict and the rule text already published in the tracked in-repo copy. Room content beyond the rule file is not read. |
| T-267.1-07 | Tampering | `docs/reward-before-investment-rule.md` | mitigate | The plan is forbidden from editing it under any verdict. Enforced by the `git diff --quiet` acceptance criterion. |
| T-267.1-SC | Tampering | package-manager installs | accept | Zero packages installed by this plan. |
</threat_model>

<verification>
```bash
git diff --quiet -- docs/reward-before-investment-rule.md && echo "hard rule unmodified"
git status --porcelain
grep -c "## A5: hard-rule provenance" .planning/phases/267.1-*/267.1-02-SUMMARY.md
grep -c "## A1 and A2" .planning/phases/267.1-*/267.1-02-SUMMARY.md
```
</verification>

<success_criteria>
- A5 closed with exactly one of `IDENTICAL` / `DIVERGENT` / `SOURCE ABSENT`, every checked path named.
- A1 and A2 answered, each with a confidence level, a provenance label, and a named effect on a specific scorecard row.
- Exactly two questions asked in the consultation; any third question recorded as `DEFERRED - not asked`.
- No tracked repo file modified by this plan.
- The SUMMARY is written so plan 267.1-03 can transcribe from it without re-opening `docs/reward-before-investment-rule.md` or re-running the consultation.
</success_criteria>

<output>
Create `.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-02-SUMMARY.md` when done.

Commit with `git add -f` for any `.planning/` path (`.planning/*` is gitignored in this repo; see CLAUDE.md).
</output>
