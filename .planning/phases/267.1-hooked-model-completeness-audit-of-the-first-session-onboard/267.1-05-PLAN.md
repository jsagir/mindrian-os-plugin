---
phase: 267.1-hooked-model-completeness-audit-of-the-first-session-onboard
plan: 05
type: execute
wave: 3
depends_on: ["267.1-03"]
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: [AUDIT-ROADMAP, AUDIT-R1, AUDIT-I1, AUDIT-G1, AUDIT-S1]
canon_parts: [6, 7]

must_haves:
  truths:
    - "Every gap the audit registered rather than fixed has a named follow-up phase in ROADMAP.md, so no finding dies in a research document"
    - "The reward and investment remediation is sequenced behind Phase 269, because removing key friction changes the very legs it would repair"
    - "The governance gap is registered as independent of Phase 269, because it is the one finding with no collision"
    - "Phase 269's existing claim on the Brain-key friction is honored: GAP S-1 is handed over, not re-decided"
    - "The registrations use the roadmap's own prose idiom for cross-phase interaction, not the audit document's COLLISION NOTE literal"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Follow-up phase registrations 267.2 and 267.3 plus a Cross-references paragraph on the 267.1 entry"
      contains: "### Phase 267.2"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/research/2026-08-27-hooked-first-install-audit.md"
      via: "each follow-up phase Goal cites the audit and its gap ID"
      pattern: "2026-08-27-hooked-first-install-audit"
    - from: ".planning/ROADMAP.md Phase 267.2"
      to: ".planning/ROADMAP.md Phase 269"
      via: "Also touches Phase N prose idiom plus a qualified Depends on line"
      pattern: "Also touches Phase 269"
---

<objective>
Turn the audit's deferred findings into roadmap entries, so nothing the audit found dies in a
research document.

**Registration only. No remediation code.** GAP R-1, GAP I-1 and GAP G-1 are registered here as
future phases with real Goal text an executor could plan from. None of them is implemented in this
phase. GAP S-1 is not registered at all: Phase 269 already claims it at `.planning/ROADMAP.md:564`,
so this plan hands it over and adds the reciprocal reference rather than minting a duplicate phase.

**Use the roadmap's own idiom, not the audit document's.** PATTERNS.md confirmed the literal string
`COLLISION NOTE` appears ZERO times in `.planning/ROADMAP.md`. The roadmap expresses cross-phase
interaction two ways, both verbatim from the Phase 269 entry:

- Form A, inside a `Cross-references:` paragraph at the end of a Goal:
  `Also touches Phase <N> (<short name> - <one clause naming the concrete interaction>)`
- Form B, a qualified `Depends on:` line carrying its reason inline, for example Phase 269's
  "none technically, but sequenced after ... (building a check against an interim cutover state
  risks throwaway work)".

`COLLISION NOTE:` belongs in the audit deliverable's `### Quick wins` sub-bullets, where plan
267.1-03 already put it. It must not appear in ROADMAP.md.

**Two follow-up phases, not three.** GAP R-1 and GAP I-1 are the two halves of one Hooked loop
repair on one surface, and they share the same Phase 269 collision, so they become one phase with
two workstreams (the `W1` / `W2` shape the Phase 268 entry already uses). GAP G-1 is a separate
governance and lint-scope fix with no Phase 269 collision at all, so it gets its own entry and can
be planned immediately.

Purpose: the audit's most damning structural finding is that nothing was ever built to catch these
gaps. Registering them is the minimum act that changes that.

Output: two new phase entries and one Cross-references paragraph in `.planning/ROADMAP.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/2026-08-27-hooked-first-install-audit.md
@.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-PATTERNS.md
@.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>

This plan (267.1-05) produces:

| Artifact | Kind | Path |
|---|---|---|
| `### Phase 267.2: First-Install Hooked Loop Repair (Reward + Investment) (INSERTED)` | new roadmap phase entry | `.planning/ROADMAP.md` |
| `### Phase 267.3: Reward-Before-Investment Guard Jurisdiction (hooks and injected-prose surfaces) (INSERTED)` | new roadmap phase entry | `.planning/ROADMAP.md` |
| `Cross-references:` paragraph on the Phase 267.1 entry | edit to an existing entry | `.planning/ROADMAP.md` |

Full phase artifact set: `scripts/session-start` SEED-021 line, `tests/test-267-1-first-install-hooked-audit.cjs`,
`tests/run-all-267.1.sh` (267.1-01); `.planning/research/2026-08-27-hooked-first-install-audit.md` (267.1-03);
`~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-hooked-first-install-audit/2026-08-27-hooked-first-install-audit.md`
(267.1-04); these two roadmap entries (267.1-05).

</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Register Phase 267.2 (reward routing + investment writer) and Phase 267.3 (guard jurisdiction)</name>

  <files>.planning/ROADMAP.md</files>

  <read_first>
    - `.planning/ROADMAP.md`, the Phase 267 through Phase 269 range. Read the Phase 267.1 entry's own skeleton verbatim (it is the exact `(INSERTED)` form to copy), the Phase 268 entry (for the `W1` / `W2` two-workstream Goal shape), and the Phase 269 entry IN FULL (for the `Cross-references:` paragraph idiom, the qualified `Depends on:` idiom, and its existing claim on the Brain-key friction).
    - `.planning/research/2026-08-27-hooked-first-install-audit.md` - the `### Quick wins` section and the `GAP R-1`, `GAP I-1`, `GAP G-1` subsections. The Goal text you write must cite these gap IDs and their file:line evidence, not restate them loosely.
    - `.planning/phases/267.1-.../267.1-PATTERNS.md` section 5 - the extracted phase-entry skeleton and both cross-reference forms, plus the "No Analog Found" row explaining why `COLLISION NOTE` must not appear here.
    - `tests/test-267-1-first-install-hooked-audit.cjs` - so each Goal can name the assertion that will go red when the gap is closed, telling the future executor exactly which pin to flip.
  </read_first>

  <action>
    Insert both entries into `.planning/ROADMAP.md` immediately after the Phase 267.1 entry's `Plans:` list and
    before `### Phase 268`, preserving the file's existing entry spacing and `---` separators if any are used in
    that range.

    **Entry 1**, heading exactly:
    `### Phase 267.2: First-Install Hooked Loop Repair (Reward + Investment) (INSERTED)`

    Goal, one prose paragraph followed by two workstream paragraphs in the Phase 268 shape:

    - Opening paragraph: the FIRST_INSTALL surface asserts a variable reward and an investment deposit as prose
      instructions to the model, and neither is wired. Cite the audit by path and the two gap IDs. State the
      version and commit the audit pinned.
    - `**W1 - Route the first session to a real reward (GAP R-1).**` Name the concrete evidence: the reward
      promise line lives in the FIRST_INSTALL payload with no wired call; `lib/core/domain-insight-sweep.cjs`
      declares itself "THE single entry point for the Hooked variable-reward leg" but is invoked only from
      `/mos:ignite` and requires an open `db` handle that a first install has no room to provide;
      `scripts/check-pending-breakthrough.cjs` returns `{continue:true}` in silence when no rooms directory
      exists, so the flagship automatic reward is structurally silent on the one session where a first reward
      matters most; and `COLD_START_MENU` routes the first-time user to `/mos:new-project`, the scaffold backend,
      while `skills/ignite/SKILL.md:50` names `/mos:ignite` the canonical front door. Name the two candidate
      mechanisms without pre-deciding: add `/mos:ignite` to the cold-start menu, and/or give
      `sweepDomainInsights` a pre-room text-only path using the degradation its own header already documents.
    - `**W2 - Give the investment assertion a writer, or delete the assertion (GAP I-1).**` Name the evidence:
      `~/.mindrian-user.md` has zero writers repo-wide; `lib/core/user-archetype.cjs:64` READS it; the only
      deterministic writer `writeUserMdAtomic` (`lib/core/user-md-ops.cjs:440`) is reachable only from
      `lib/core/navigation/room-birth.cjs:572` and `:801` and writes to `roomDir/USER.md`, which a first install
      does not have. State the fork honestly: either wire a home-directory writer, or remove the prose
      instruction so the product stops promising something it does not do. Add the hard constraint that
      `/mos:profile-user` is referenced by USER.md stubs in production rooms and DOES NOT EXIST, so it must not
      be adopted as the mechanism. Also name the fragility item: `check-onboard --write` is itself an LLM
      instruction, so the onboarding state machine's advance step depends on model compliance.
    - Name, for each workstream, the assertion in `tests/test-267-1-first-install-hooked-audit.cjs` that goes
      red when the gap closes, and state that flipping the pin from a negative to a positive assertion is part
      of that phase's work, not a test failure to route around.
    - `Cross-references:` paragraph using Form A. At minimum:
      `Also touches Phase 269 (Moat Shift - Install/Update Entitlement Gate - removing the Brain-key friction
      step changes the onboarding Trigger/Reward/Investment legs this phase repairs, so building against the
      current key-gated flow risks rework).` Add the reciprocal pointer back to Phase 267.1 and the audit path.
    - `**Requirements**: TBD`
    - `**Depends on:**` using Form B, qualified with its reason inline, in the shape of:
      `none technically, but sequenced after Phase 269 lands its onboarding-flow change (repairing the reward
      and investment legs against the current key-gated first session risks throwaway work, and the audit that
      motivates this phase says so explicitly).`
    - `**Plans:** 0 plans` and a `Plans:` list containing the single line
      `- [ ] TBD (run /gsd-plan-phase 267.2 to break down)`.

    **Entry 2**, heading exactly:
    `### Phase 267.3: Reward-Before-Investment Guard Jurisdiction (hooks and injected-prose surfaces) (INSERTED)`

    Goal, one prose paragraph:

    - The reward-before-investment hard rule has a real enforcement mechanism, and that mechanism cannot see
      the surface that needs it most. Cite the three independent proofs of its scope: the
      `lib/core/mva-rule-linter.cjs` header stating it scans `commands/*.md` frontmatter, `scanCommands`
      reading `commandsDir` only, and `scripts/check-reward-before-investment.cjs` defaulting its target to
      `path.join(__dirname, '..', 'commands')`. State the consequence: `scripts/session-start` is a bash hook
      with no frontmatter to carry an `interactive_first_reward` declaration, so the single most-first flow in
      the product, the one every user hits before any command, is structurally outside the guard. Note that
      `/mos:onboard` at least carries `interactive_first_reward: reframe_question` with an honest inline
      "Remediation tracked as follow-up phase" comment (`commands/onboard.md:12`) while FIRST_INSTALL carries no
      declaration at all. Frame it the way the audit does: this is a governance gap, not just a content gap, and
      it explains WHY the other gaps survived - nothing was ever built to catch them.
    - Name the open design question without deciding it: how does a bash hook or an injected-prose surface
      declare a first-reward contract the linter can read? A sidecar declaration file, a manifest, a comment
      convention the linter parses, or an extension of the born-wired connector registry are all candidates.
      Note the Canon Part 11 adjacency, since that is the repo's existing machinery for "every invocable surface
      is born declared".
    - `Cross-references:` paragraph stating this is the ONE audit finding with no Phase 269 collision and can be
      planned immediately, and pointing back to Phase 267.1 and the audit path.
    - `**Requirements**: TBD`
    - `**Depends on:** none - independent of Phase 269 and of Phase 267.2; this is a lint-scope and declaration-contract change, not an onboarding-flow change.`
    - `**Plans:** 0 plans` and a `Plans:` list containing `- [ ] TBD (run /gsd-plan-phase 267.3 to break down)`.

    Hard constraints on both entries: the literal string `COLLISION NOTE` must NOT appear anywhere in
    `.planning/ROADMAP.md`. No em-dashes (U+2014). No Brain API key fragment. Do not modify the Phase 267,
    Phase 268 or Phase 269 entries in this task (the Phase 267.1 entry is edited in task 2).
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/ROADMAP.md','utf8');for(const h of ['### Phase 267.2: First-Install Hooked Loop Repair (Reward + Investment) (INSERTED)','### Phase 267.3: Reward-Before-Investment Guard Jurisdiction (hooks and injected-prose surfaces) (INSERTED)'])if(s.indexOf(h)===-1)throw new Error('missing heading: '+h);if(s.indexOf('COLLISION NOTE')!==-1)throw new Error('COLLISION NOTE must not appear in ROADMAP.md');if(s.indexOf('Also touches Phase 269')===-1)throw new Error('missing Form A cross-reference');for(const g of ['GAP R-1','GAP I-1','GAP G-1'])if(s.indexOf(g)===-1)throw new Error('missing gap id: '+g);if(s.indexOf('2026-08-27-hooked-first-install-audit')===-1)throw new Error('roadmap does not cite the audit');if(s.indexOf('/gsd-plan-phase 267.2')===-1||s.indexOf('/gsd-plan-phase 267.3')===-1)throw new Error('missing Plans TBD lines');if(/\u2014/.test(s))throw new Error('em-dash found in ROADMAP.md');console.log('ok roadmap registrations')"</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains both new headings verbatim, including the `(INSERTED)` suffix, and both sit between the Phase 267.1 entry and `### Phase 268`.
    - Each entry carries all five structural lines the house skeleton uses: a `**Goal:**` prose paragraph, `**Requirements**: TBD`, a `**Depends on:**` line, `**Plans:** 0 plans`, and a `Plans:` list with a single `- [ ] TBD (run /gsd-plan-phase <N> to break down)` line.
    - `grep -c "COLLISION NOTE" .planning/ROADMAP.md` returns 0.
    - `grep -c "Also touches Phase 269" .planning/ROADMAP.md` returns at least 1 and it sits inside a `Cross-references:` paragraph in the Phase 267.2 entry.
    - The Phase 267.2 `Depends on:` line is qualified with its reason inline (Form B), not a bare `none`.
    - The Phase 267.3 `Depends on:` line states independence from both Phase 269 and Phase 267.2.
    - All three gap IDs `GAP R-1`, `GAP I-1`, `GAP G-1` appear in ROADMAP.md, each in the entry that owns it.
    - Each entry cites `.planning/research/2026-08-27-hooked-first-install-audit.md` by path.
    - Phase 267.2's Goal names at least four concrete file paths from the audit's evidence (`lib/core/domain-insight-sweep.cjs`, `scripts/check-pending-breakthrough.cjs`, `lib/core/user-md-ops.cjs`, `lib/core/user-archetype.cjs`), and Phase 267.3's Goal names `lib/core/mva-rule-linter.cjs` and `scripts/check-reward-before-investment.cjs`.
    - Phase 267.2's Goal contains the constraint that `/mos:profile-user` does not exist and must not be adopted as the mechanism.
    - `git diff .planning/ROADMAP.md` shows no change inside the Phase 267, Phase 268, or Phase 269 entries.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' .planning/ROADMAP.md` returns 0.
  </acceptance_criteria>

  <done>Every gap the audit deferred has a roadmap entry with Goal text an executor could plan from, sequenced correctly against Phase 269, in the roadmap's own idiom.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Cross-references paragraph to the Phase 267.1 entry and hand GAP S-1 to Phase 269</name>

  <files>.planning/ROADMAP.md</files>

  <read_first>
    - `.planning/ROADMAP.md` Phase 267.1 entry - the exact current text, so the addition sits at the end of the Goal paragraph in the house shape rather than as a bolted-on section.
    - `.planning/ROADMAP.md` Phase 269 entry - its existing `Cross-references:` paragraph, which already names Phase 267.1 ("Hooked Model onboarding audit - removing the key-friction step changes the onboarding Trigger/Reward/Investment legs"). Your addition makes the reference reciprocal; do NOT edit Phase 269's own text.
    - `.planning/research/2026-08-27-hooked-first-install-audit.md` `### GAP S-1` subsection - the handoff you are recording.
  </read_first>

  <action>
    Append a `Cross-references:` paragraph to the END of the Phase 267.1 Goal, before its `**Requirements**: TBD`
    line, using Form A. It must state four things:

    1. The audit shipped, with its path (`.planning/research/2026-08-27-hooked-first-install-audit.md`), its
       score and band, and the label that the score is diagnostic and not a gate (Canon Appendix D entry 31).
    2. The one remediation this phase shipped: the SEED-021 `AskUserQuestion` card mandate added to the
       FIRST_INSTALL prose, bringing it into the rendering contract the two sibling cold-start branches already
       obeyed, with a note that it was an in-scope doctrine-consistency fix independent of Phase 269.
    3. What it registered rather than fixed:
       `Also touches Phase 267.2 (First-Install Hooked Loop Repair - GAP R-1 reward routing and GAP I-1
       investment writer, registered by this audit) and Phase 267.3 (Reward-Before-Investment Guard Jurisdiction
       - GAP G-1, the one finding with no Phase 269 collision).`
    4. The GAP S-1 handoff:
       `Also touches Phase 269 (Moat Shift - Install/Update Entitlement Gate - the audit's GAP S-1, the two
       sign-ins across two domains before the Trigger ever fires, is documented and quantified here and OWNED
       there; this phase deliberately did not fix or re-decide it).`
       Cite `docs/testers/gaurav-thorat/FEEDBACK.md` by path as the primary evidence. Do NOT reproduce any
       API key fragment or any tester identity detail beyond the path.

    Also add a `Room mirror:` sentence naming
    `~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-hooked-first-install-audit/`, so a reader of the
    roadmap alone can find the reasoning trail (Dev-Research Compositing mandate).

    Do NOT touch the Phase 267.1 `**Plans:**` count or its `Plans:` list. Those are maintained by the planner's
    own roadmap-update step, and editing them here would fight it.

    Do NOT edit the Phase 269 entry. The reciprocity is achieved by 267.1 pointing at 269, since 269 already
    points at 267.1.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/ROADMAP.md','utf8');const i=s.indexOf('### Phase 267.1:');const j=s.indexOf('### Phase 267.2:');if(i===-1||j===-1||j<i)throw new Error('267.1/267.2 entries out of order or missing');const e=s.slice(i,j);for(const k of ['Cross-references:','Also touches Phase 267.2','Also touches Phase 267.3','Also touches Phase 269','2026-08-27-hooked-first-install-audit','SEED-021','GAP S-1','Appendix D entry 31','rethinking-mindrianos'])if(e.indexOf(k)===-1)throw new Error('267.1 entry missing: '+k);if(/mbr_/.test(e))throw new Error('key fragment in roadmap');if(/\u2014/.test(s))throw new Error('em-dash found');console.log('ok 267.1 cross-references')"</automated>
  </verify>

  <acceptance_criteria>
    - The Phase 267.1 entry contains a `Cross-references:` paragraph positioned at the end of its `**Goal:**` and before `**Requirements**: TBD`.
    - That paragraph contains all three Form A references: `Also touches Phase 267.2`, `Also touches Phase 267.3`, `Also touches Phase 269`.
    - It names the audit path `.planning/research/2026-08-27-hooked-first-install-audit.md`, the score with its band, and the phrase `Appendix D entry 31` labelling the score diagnostic rather than a gate.
    - It records the SEED-021 fix as the one remediation this phase shipped, and states it was independent of Phase 269.
    - It records `GAP S-1` as documented here and OWNED by Phase 269, citing `docs/testers/gaurav-thorat/FEEDBACK.md` by path only.
    - It names the room mirror directory path.
    - `grep -cE "mbr_|Bearer " .planning/ROADMAP.md` returns 0.
    - `git diff .planning/ROADMAP.md` shows NO change inside the Phase 269 entry and NO change to the Phase 267.1 `**Plans:**` count or `Plans:` list.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' .planning/ROADMAP.md` returns 0.
    - `bash tests/run-all-267.1.sh` still exits 0.
  </acceptance_criteria>

  <done>The roadmap alone tells the whole story: what the audit found, the one thing it fixed, the two phases it minted, and the one gap it handed to Phase 269 without touching.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| the audit deliverable -> `.planning/ROADMAP.md` | Findings copied into the roadmap inherit the same secret-hygiene obligations, since the roadmap is read by every future planning session. |
| Phase 267.1 -> Phase 269 | A scope handoff. The risk is re-deciding something another phase owns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-267.1-17 | Information Disclosure | GAP S-1 handoff text in ROADMAP.md | mitigate | `docs/testers/gaurav-thorat/FEEDBACK.md` is cited by path only. The `grep -cE "mbr_\|Bearer "` sweep over ROADMAP.md returning 0 is a blocking acceptance criterion, and the no-real-names rule forbids amplifying identity detail past the path. |
| T-267.1-18 | Tampering | edits to phases this plan does not own | mitigate | `git diff .planning/ROADMAP.md` must show no change inside the Phase 267, 268, or 269 entries. Reciprocity with Phase 269 is achieved by 267.1 pointing at 269, since 269 already points at 267.1. |
| T-267.1-19 | Repudiation | a finding that dies in a research document | mitigate | Every deferred gap ID (`GAP R-1`, `GAP I-1`, `GAP G-1`) must appear in ROADMAP.md in the entry that owns it, checked programmatically. GAP S-1 is checked as a handoff reference rather than a new entry. |
| T-267.1-20 | Elevation of Privilege | scope creep into Phase 269's territory | mitigate | This plan registers and hands off. It writes no install-path, key-provisioning, or `mindrian-website` change, and its acceptance criteria forbid editing the Phase 269 entry at all. |
| T-267.1-SC | Tampering | package-manager installs | accept | Zero packages installed by this plan. |
</threat_model>

<verification>
```bash
node -e "const s=require('fs').readFileSync('.planning/ROADMAP.md','utf8');console.log(['### Phase 267.2','### Phase 267.3','Also touches Phase 269','GAP R-1','GAP I-1','GAP G-1','GAP S-1'].map(k=>k+': '+(s.indexOf(k)!==-1)).join('\n'))"
grep -c "COLLISION NOTE" .planning/ROADMAP.md
LC_ALL=C.UTF-8 grep -cP '\x{2014}' .planning/ROADMAP.md
git diff --stat .planning/ROADMAP.md
bash tests/run-all-267.1.sh
```
</verification>

<success_criteria>
- Phases 267.2 and 267.3 exist in `.planning/ROADMAP.md` with full house-skeleton entries an executor could run `/gsd-plan-phase` against.
- Phase 267.2 is sequenced behind Phase 269 with the reason inline; Phase 267.3 declares independence from both.
- The Phase 267.1 entry carries a `Cross-references:` paragraph naming all three related phases, the audit path, the shipped SEED-021 fix, the GAP S-1 handoff, and the room mirror.
- `COLLISION NOTE` appears zero times in ROADMAP.md; it lives only in the audit deliverable's `### Quick wins` sub-bullets.
- Zero em-dashes and zero secret fragments in ROADMAP.md.
- No change to the Phase 267, 268, or 269 entries.
</success_criteria>

<output>
Create `.planning/phases/267.1-hooked-model-completeness-audit-of-the-first-session-onboard/267.1-05-SUMMARY.md` when done.

Commit with `git add -f` for any `.planning/` path (`.planning/*` is gitignored in this repo; see CLAUDE.md).
</output>
