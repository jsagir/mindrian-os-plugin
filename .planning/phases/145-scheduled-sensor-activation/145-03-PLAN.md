---
phase: 145-scheduled-sensor-activation
plan: 03
type: execute
wave: 3
depends_on: ["145-01", "145-02"]
files_modified:
  - tests/run-all-145.sh
  - tests/test-scout-cadence-fires.cjs
  - tests/test-scout-cadence-part8-hardening.cjs
autonomous: true
requirements: [SCHED-01, SCHED-02]
canon_parts: ["Part 2 Engine 1", "Part 4", "Part 8"]
user_setup: []

must_haves:
  truths:
    - "tests/run-all-145.sh runs to completion (no crash even when a suite fails), prints a per-suite PASS/FAIL line, and exits non-zero on any failure -- mirroring tests/run-all-144.sh exactly"
    - "A test proves the cadence fires the full suite without manual invocation (SCHED-01) and that the SCHED-02 four (whitespace + reverse-salient + opportunity-bank + competitor) appear in the fired steps"
    - "A test proves the throttle blocks re-fire inside the interval (no every-session runaway)"
    - "A test proves the Phase-140 hardening holds under auto-fire: no health-check crash (HARD-01), no NULL source_path (HARD-02), no backup pollution (HARD-03) -- and that the runner + guard carry zero Brain/web egress (Part 8) and that findings become graph data (Part 4)"
  artifacts:
    - path: "tests/run-all-145.sh"
      provides: "The Phase 145 scoped acceptance aggregator, mirroring run-all-144.sh so the Phase 146 loop-fires gate can compose it"
      min_lines: 60
    - path: "tests/test-scout-cadence-fires.cjs"
      provides: "SCHED-01 + SCHED-02 cadence-fires + throttle assertions"
      min_lines: 70
    - path: "tests/test-scout-cadence-part8-hardening.cjs"
      provides: "Phase-140-hardening-holds + Part-8-zero-egress + Part-4-findings-become-graph-data assertions"
      min_lines: 70
  key_links:
    - from: "tests/run-all-145.sh"
      to: "tests/test-scout-cadence-fires.cjs + tests/test-scout-cadence-part8-hardening.cjs"
      via: "CJS_SUITES array, resolved relative to tests/"
      pattern: "CJS_SUITES"
    - from: "tests/test-scout-cadence-fires.cjs"
      to: "scripts/scout-cadence-runner.cjs + scripts/scout-cadence-guard.cjs"
      via: "spawns the runner against a fixture room and asserts the fired steps + throttle"
      pattern: "scout-cadence-(runner|guard)"
---

<objective>
Write the Phase 145 verification suite that proves the three success criteria are TRUE: the scout suite fires on a cadence without manual invocation, the SCHED-02 four fire on the same cadence, and a scheduled run produces signal without errors with the Phase-140 hardening holding under auto-fire.

Purpose: Success Criterion 3 ("the Phase 140 hardening holds under auto-fire -- no health-check crash, no NULL source_path, no backup pollution") is only credible if PROVEN by instrumentation, not promised. This mirrors the Phase-140 and Phase-144 acceptance-test discipline and feeds the Phase 146 loop-fires gate.

Output: tests/run-all-145.sh (aggregator) + two CJS suites covering cadence-fires/throttle (SCHED-01/02) and Phase-140-hardening/Part-8/Part-4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/MINDRIAN-CANON.md
@.planning/phases/145-scheduled-sensor-activation/145-01-PLAN.md
@.planning/phases/145-scheduled-sensor-activation/145-02-PLAN.md

# The runner to mirror EXACTLY (structure, per-suite header, exit discipline):
@tests/run-all-144.sh
</context>

<part_7_reuse_statement>
This plan writes NO new sensor and NO new runner. It reuses:
- tests/run-all-144.sh as the EXACT structural template (SHELL_SUITES/CJS_SUITES arrays, per-suite PASS/FAIL line, run-to-completion-then-exit-non-zero-on-failure, the relative-path resolution comment block).
- scripts/scout-cadence-runner.cjs + scripts/scout-cadence-guard.cjs (Plan 01) as the units under test.
- The Phase-140 invariant semantics (HARD-01/02/03) the guard already encodes.
Justification: the only net-new is the two test suites + the aggregator; no production surface is added.
</part_7_reuse_statement>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the cadence-fires + throttle test (SCHED-01 + SCHED-02)</name>
  <read_first>
    - tests/run-all-144.sh (full) -- the exit discipline + per-suite line format the aggregator must reproduce
    - scripts/scout-cadence-runner.cjs (the --json summary shape: fired, throttled, steps[], findings_to_graph, signal_query_plan)
    - scripts/scout-cadence-guard.cjs (shouldFire / recordRun contract for the throttle assertion)
  </read_first>
  <behavior>
    - Test 1 (SCHED-01 fires): build a minimal fixture room (temp dir with STATE.md + a couple of sections with ROOM.md), run scout-cadence-runner.cjs <room> --json, parse the summary, assert fired:true and that steps[] contains snapshot, health-check, deadline-monitor, competitor (query plan), hsi, opportunity-scan -- i.e. the full scout suite ran without anyone typing /mos:scout.
    - Test 2 (SCHED-02 fires): assert steps[] also contains whitespace recompute, reverse-salient, opportunity-bank scan, and competitor watch -- the four SCHED-02 sensors -- on the same fired run.
    - Test 3 (throttle blocks re-fire): immediately re-run the runner against the same room WITHOUT --force; assert throttled:true and that zero sensors ran (steps[] empty or all marked skipped-throttled). Then backdate the last-run timestamp file past the interval and assert it fires again.
    - Test 4 (no-room soft-fail): run against a nonexistent path; assert exit 0 and fired:false (the hook contract -- never crash the host).
  </behavior>
  <action>
    Create tests/test-scout-cadence-fires.cjs (node:test or a plain assert harness consistent with the other tests/*.cjs files in this repo -- inspect a sibling like tests/test-decide-sensor-fire.cjs for the in-repo convention). Use os.tmpdir() fixtures, clean them up in a finally. For Python-dependent steps (HSI/whitespace), accept either "ran" or "skipped (sklearn absent)" as a PASS for the step's PRESENCE assertion -- the test asserts the step was ATTEMPTED in the suite, not that sklearn is installed (the CI box may lack it). Mock/stub nothing in the runner itself -- drive the real runner. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-scout-cadence-fires.cjs && echo FIRES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Test 1+2 assert all 10 sensors are present in a single fired run (the 6 scout sub-sensors + the SCHED-02 four), proving SCHED-01 and SCHED-02
    - Test 3 proves the throttle (re-fire blocked inside interval, fires again after backdating)
    - Test 4 proves no-room soft-fail (exit 0)
    - Suite exits 0 (FIRES_OK) on a clean run; exits non-zero on any failed assertion
    - No em-dashes
  </acceptance_criteria>
  <done>The cadence-fires suite proves SCHED-01 + SCHED-02 fire without manual invocation and the throttle holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the Phase-140-hardening + Part-8 + Part-4 test, and the run-all-145.sh aggregator</name>
  <read_first>
    - scripts/scout-cadence-guard.cjs (safeAutoFireCheck: the HARD-01/02/03 violation semantics + the violation string tags)
    - tests/run-all-144.sh (the aggregator structure to clone)
    - one sibling Part-8 leak test (e.g. tests/test-decide-part8-invariant.cjs) for the in-repo forbidden-substring sweep convention
  </read_first>
  <behavior>
    - Test 1 (HARD-01 no health-check crash): drive a fired run against a fixture room and assert the health-check step status is not "crash"/non-zero-unhandled; assert the runner CAPTURED the health-check exit (did not swallow it) by checking the step carries an exit field.
    - Test 2 (HARD-02 no NULL source_path): after a fired run that writes HSI edges, assert safeAutoFireCheck reports no HARD-02 violation on a clean room; then synthesize a NULL-source_path node in a fixture room.db and assert safeAutoFireCheck DOES flag HARD-02 (proving the guard actually detects the regression).
    - Test 3 (HARD-03 no backup pollution): place a .heal-backup/ path into a synthesized result set and assert safeAutoFireCheck flags HARD-03; assert a clean room reports none.
    - Test 4 (Part 8 zero egress): forbidden-substring sweep over scripts/scout-cadence-runner.cjs + scripts/scout-cadence-guard.cjs (code lines only, comments filtered) asserting zero fetch(/https?:///tavily/brain_query/mcp__brain matches; assert the competitor step is emitted as a SIGNAL query plan, not an executed fetch.
    - Test 5 (Part 4 findings become graph data): assert the fired-run summary carries findings_to_graph (opportunity-bank ADD candidates + HSI/whitespace/RS edges destined for room.db), confirming scheduled findings become graph data per Canon Part 4.
  </behavior>
  <action>
    (A) Create tests/test-scout-cadence-part8-hardening.cjs implementing the five behaviors above against tmpdir fixtures (synthesize room.db rows via node:sqlite consistent with the repo's existing sqlite test fixtures; clean up in finally). For the Part-8 sweep, read the two script files, strip comment lines, and assert zero forbidden matches. No em-dashes.
    (B) Create tests/run-all-145.sh by cloning tests/run-all-144.sh's structure EXACTLY: the same set -uo pipefail, SCRIPT_DIR, START_TIME, SHELL_SUITES (empty) + CJS_SUITES = [test-scout-cadence-fires.cjs, test-scout-cadence-part8-hardening.cjs], the per-suite PASS/FAIL loop that runs to completion, the summary block, and exit 1 on any failure / exit 0 otherwise. Add a header comment block (matching run-all-144.sh's style) naming per-suite plan ownership and stating the three Success Criteria it proves (cadence-fires, hardening-holds, Part-8 + Part-4). bash only, no emoji, no em-dashes.
  </action>
  <verify>
    <automated>chmod +x tests/run-all-145.sh && bash tests/run-all-145.sh && echo RUN_ALL_145_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - safeAutoFireCheck is proven to DETECT (not just claim) each of HARD-01/02/03 via synthesized-regression fixtures, and to report clean on a hardened room
    - The Part-8 sweep asserts zero egress in both Plan-01 scripts and confirms competitor-watch is a query plan, not a fetch
    - Part-4 assertion confirms findings_to_graph is populated on a fired run
    - tests/run-all-145.sh mirrors run-all-144.sh structure, runs to completion, and exits non-zero on any suite failure (prints RUN_ALL_145_GREEN on all-pass)
    - No em-dashes in either created file
  </acceptance_criteria>
  <done>The hardening/Part-8/Part-4 suite + the run-all-145.sh aggregator prove Success Criterion 3 by instrumentation and compose into the Phase 146 gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture room.db -> guard | the test must drive REAL guard detection, not a stub, or the proof is hollow |
| CI box without sklearn -> Python sensor steps | absence must degrade to "skipped" PASS, not a false FAIL that hides a real regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-09 | Repudiation | hardening test that only asserts the happy path | mitigate | Tests 2+3 synthesize a NULL-source_path node and a .heal-backup path and assert the guard FLAGS them -- proving detection works, not just that a clean room passes (the Phase-140/144 adversarial-fixture discipline) |
| T-145-10 | Tampering | Part-8 sweep false-clean from commented-out matches | mitigate | The sweep strips comment lines before grepping (grep -v on comment prefixes) per the planner grep-gate-hygiene rule, so header prose cannot self-invalidate the gate |
| T-145-SC | Tampering | npm/pip/cargo installs | accept | No package installs -- tests use node built-ins (node:test/assert, node:sqlite, os, fs) only |
</threat_model>

<artifacts_this_phase_produces>
- tests/run-all-145.sh -- the Phase 145 scoped acceptance aggregator
- tests/test-scout-cadence-fires.cjs -- SCHED-01/02 cadence-fires + throttle proof
- tests/test-scout-cadence-part8-hardening.cjs -- Phase-140-hardening + Part-8 + Part-4 proof
</artifacts_this_phase_produces>

<verification>
- bash tests/run-all-145.sh exits 0 on a clean tree and prints a per-suite PASS line for both CJS suites
- node tests/test-scout-cadence-fires.cjs and node tests/test-scout-cadence-part8-hardening.cjs each pass standalone
- the aggregator exits non-zero when a suite is made to fail (sanity-check the exit discipline mirrors run-all-144.sh)
- zero em-dashes across all three created files
</verification>

<success_criteria>
- The suite proves all three Phase 145 Success Criteria: cadence fires without manual invocation (SCHED-01), the SCHED-02 four fire on the cadence (SCHED-02), and a scheduled run holds the Phase-140 hardening (no health-check crash, no NULL source_path, no backup pollution) with zero Brain/web egress (Part 8) and findings becoming graph data (Part 4)
- run-all-145.sh mirrors run-all-144.sh and is composable into the Phase 146 loop-fires gate
- All created files are em-dash-free
</success_criteria>

<output>
Create `.planning/phases/145-scheduled-sensor-activation/145-03-SUMMARY.md` when done
</output>
