---
phase: 145-scheduled-sensor-activation
plan: 02
type: execute
wave: 2
depends_on: ["145-01"]
files_modified:
  - scripts/session-start
  - commands/scout.md
  - commands/scheduled-tasks.md
autonomous: true
requirements: [SCHED-01, SCHED-02]
canon_parts: ["Part 2 Engine 1", "Part 4", "Part 8"]
user_setup: []

must_haves:
  truths:
    - "On CLI, the scout suite fires on a session-start-throttled cadence: the session-start hook calls the Plan-01 runner, which self-throttles to at most once per interval, with zero manual /mos:scout"
    - "The cron path is documented as the CLI power-user option (the runner is cron-callable as a non-interactive command); the DEFERRED note in scout.md is replaced with the live session-start-throttle default + the cron-optional path"
    - "On Cowork, the scout-sentinel scheduled task points at the Plan-01 cadence runner so the same composition fires under Cowork's scheduler"
    - "The session-start wiring is best-effort and NEVER blocks startup (soft-fail per the hook contract); a throttled session adds no measurable latency"
  artifacts:
    - path: "scripts/session-start"
      provides: "A cadence slot (mirroring the Phase-124 timeline / Phase-143.1 dial-memory best-effort slot) that fires scout-cadence-runner.cjs in the background, self-throttled"
      contains: "scout-cadence-runner"
    - path: "commands/scout.md"
      provides: "Replaces the Cron Integration DEFERRED section with the live cadence model (session-start-throttle default, cron optional, Cowork scheduled-tasks)"
      contains: "session-start-throttle"
    - path: "commands/scheduled-tasks.md"
      provides: "Task 6 scout-sentinel repointed at the cadence runner so Cowork fires the full composition"
      contains: "scout-cadence-runner"
  key_links:
    - from: "scripts/session-start"
      to: "scripts/scout-cadence-runner.cjs"
      via: "background node invocation in a best-effort cadence slot, self-throttled by the runner's guard"
      pattern: "scout-cadence-runner"
    - from: "commands/scheduled-tasks.md"
      to: "scripts/scout-cadence-runner.cjs"
      via: "Cowork scout-sentinel task execution block"
      pattern: "scout-cadence-runner"
---

<objective>
Wire the Plan-01 cadence runner into all three Tri-Polar surfaces: the session-start-throttled cadence (CLI default, zero-infra), the cron-callable path (CLI power-user option), and the Cowork scout-sentinel scheduled task. After this plan, the scout suite fires on a cadence without anyone typing /mos:scout.

Purpose: SCHED-01 requires the suite to fire "on a cadence (session-start-throttled or cron)" without manual invocation. The canonical default is session-start-throttle per the repo's "session start IS the trigger" pattern (CLAUDE.md, Opportunity Bank section); cron is the optional CLI path; Cowork uses scheduled-tasks. This plan resolves the DEFERRED cron note into a live cadence.

Output: a self-throttled best-effort cadence slot in scripts/session-start, an updated commands/scout.md cadence section, and a repointed Cowork scout-sentinel task in commands/scheduled-tasks.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/MINDRIAN-CANON.md
@.planning/phases/145-scheduled-sensor-activation/145-01-PLAN.md

# Reuse patterns this plan mirrors (read the slot shape before editing):
@commands/scout.md
@commands/scheduled-tasks.md
</context>

<part_7_reuse_statement>
This plan adds NO new sensor and NO new runner. It wires the Plan-01 runner into three EXISTING surfaces:
- scripts/session-start (the cadence host) -- mirror the EXISTING best-effort background slot pattern at the Phase-124 timeline-refresh slot (~line 1430) and the Phase-143.1 dial-memory-refresh slot (~line 1432-1476): env-prefixed node invocation, `|| true`, 2>/dev/null, no-op when no room resolves.
- commands/scout.md (the manual trigger doc) -- the "Cron Integration (DEFERRED)" section already exists; this plan REPLACES its body, it does not add a new section.
- commands/scheduled-tasks.md (the Cowork surface) -- Task 6 "Scout Sentinel" already exists and currently lists the raw sentinel-* scripts; this plan repoints its Execution block at the single cadence runner.
Justification: the session-start cadence slot is the only net-new wiring; it reuses the exact soft-fail slot template already proven twice (124, 143.1).
</part_7_reuse_statement>

<tasks>

<task type="auto">
  <name>Task 1: Add the self-throttled session-start cadence slot (CLI default)</name>
  <read_first>
    - scripts/session-start (lines 1432-1476) -- the Phase-143.1 dial-memory-refresh best-effort slot: the EXACT env-prefix + node -e + `|| true` + 2>/dev/null + no-op-when-no-room template to copy
    - scripts/session-start (lines 1506-1549) -- the once-per-day cache pattern (context for why a throttle belongs in the runner, not duplicated here)
    - scripts/scout-cadence-runner.cjs (the Plan-01 runner: its argv contract and its built-in throttle)
  </read_first>
  <action>
    Insert a new best-effort cadence slot into scripts/session-start, placed AFTER the Phase-143.1 dial-memory-refresh slot (after line ~1476, before the Phase-87-01a pre-commit hook block). Mark it with sentinel comments `# --- BEGIN Phase 145 scout cadence (best-effort, self-throttled) ---` / `# --- END Phase 145 scout cadence ---`.
    Behavior: when ROOM_DIR resolves to a directory, fire `node "${PLUGIN_ROOT}/scripts/scout-cadence-runner.cjs" "${ROOM_DIR}"` in the BACKGROUND (trailing &) so it never adds latency to startup, redirect stdout+stderr to /dev/null, and append `|| true`. Do NOT pass --force -- the runner self-throttles via its Plan-01 guard, so most sessions are a sub-millisecond throttle short-circuit. No-op silently when ROOM_DIR is empty or not a dir. Gate the whole slot behind an opt-out env var `SCOUT_CADENCE_SKIP` (when set to 1, the slot is skipped) mirroring the BRAIN_STALENESS_SKIP emergency-disable convention already used in this file.
    Add a header comment block (matching the surrounding slots' style) stating: Canon Part 8 LOCAL-only (the runner is zero-egress per Plan 01); soft-fail contract (never blocks startup); the throttle lives in the runner's guard so the cadence is at-most-once-per-interval. No em-dashes.
  </action>
  <verify>
    <automated>grep -q 'scout-cadence-runner' scripts/session-start && grep -q 'SCOUT_CADENCE_SKIP' scripts/session-start && bash -n scripts/session-start && echo SLOT_OK</automated>
  </verify>
  <acceptance_criteria>
    - The slot invokes scout-cadence-runner.cjs in the background with ROOM_DIR, soft-fails with || true, and is bounded by BEGIN/END sentinels
    - bash -n scripts/session-start parses clean (no syntax error introduced)
    - SCOUT_CADENCE_SKIP=1 short-circuits the slot
    - The slot is a no-op when no room resolves; it adds no foreground work to startup
    - No em-dashes in the added block
  </acceptance_criteria>
  <done>session-start fires the cadence runner in the background on every CLI session, self-throttled, soft-failing, opt-out-able. SCHED-01 CLI session-start-throttled cadence is live.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the scout.md DEFERRED cron note with the live Tri-Polar cadence model + repoint the Cowork scout-sentinel task</name>
  <read_first>
    - commands/scout.md (lines 241-259) -- the Tri-Polar Notes + the "Cron Integration (DEFERRED)" section being replaced
    - commands/scheduled-tasks.md (lines 217-265) -- Task 6 Scout Sentinel (the Execution block to repoint) + the Tri-Polar Notes
    - scripts/scout-cadence-runner.cjs (argv contract for the cron + Cowork call examples)
  </read_first>
  <action>
    (A) In commands/scout.md, REPLACE the "## Cron Integration (DEFERRED)" section body with a "## Scheduled Cadence (LIVE)" section documenting the three surfaces: (1) CLI default = session-start-throttle -- "the session-start hook fires scripts/scout-cadence-runner.cjs in the background, self-throttled to at most once per interval (default 24h); no manual /mos:scout needed; opt out with SCOUT_CADENCE_SKIP=1"; (2) cron optional (CLI power users) -- give the exact crontab line calling `node <plugin>/scripts/scout-cadence-runner.cjs <room> --force` on a chosen schedule, noting --force bypasses the session throttle because cron owns the cadence; (3) Cowork = the scout-sentinel scheduled task (cross-reference commands/scheduled-tasks.md Task 6). State that /mos:scout remains the manual on-demand trigger. Keep the existing Tri-Polar Notes section intact. No em-dashes.
    (B) In commands/scheduled-tasks.md Task 6 "Scout Sentinel", REPLACE the Execution block (the raw sentinel-snapshot/health-check/deadline-monitor + HSI lines) with a single call to `node "${PLUGIN_ROOT}/scripts/scout-cadence-runner.cjs" "$ROOM_DIR" --force` (the Cowork scheduler owns the cadence, so --force bypasses the per-session throttle), and note that this single runner composes all six scout sub-sensors PLUS the SCHED-02 four (whitespace recompute + reverse-salient + opportunity-bank scan + competitor watch) behind the Phase-140 safe-auto-fire guard. Update the Output section to mention the structured summary + the safeAutoFireCheck violations surface. Keep the Requirements line and add SCHED-01. No em-dashes.
  </action>
  <verify>
    <automated>grep -q 'session-start-throttle' commands/scout.md && grep -q 'scout-cadence-runner' commands/scheduled-tasks.md && ! grep -q 'DEFERRED)' commands/scout.md && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - scout.md documents all three surfaces (session-start-throttle default, cron optional with an exact crontab line, Cowork cross-reference) and no longer carries a "Cron Integration (DEFERRED)" header
    - scheduled-tasks.md Task 6 Execution calls scout-cadence-runner.cjs --force and names the SCHED-02 four sensors it composes
    - Both docs name the Phase-140 safe-auto-fire guard
    - No em-dashes in either edited file
  </acceptance_criteria>
  <done>The cron-DEFERRED debt is resolved into a live Tri-Polar cadence model; Cowork fires the same composition via the runner. SCHED-01 cron-optional + Cowork paths documented and wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session-start hook -> scout-cadence-runner | a hook that blocks or crashes degrades every Claude Code launch |
| Cowork scheduler -> runner (--force) | Cowork owns the cadence; --force must not enable a runaway because the scheduler interval is the throttle |
| cron -> runner (--force) | a misconfigured crontab could over-fire external web/API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-05 | Denial of Service | session-start cadence slot blocking startup | mitigate | Slot runs in the BACKGROUND (&), redirects to /dev/null, appends || true, no-ops when no room; bash -n gate in Task 1 verify; mirrors the proven 124/143.1 soft-fail slot template |
| T-145-06 | Denial of Service | cron/Cowork --force over-firing external APIs | mitigate | --force is documented as "the scheduler/cron owns the cadence"; the underlying runner still emits competitor-watch as a query plan (not an unbounded fetch loop) and respects buildCompetitorQueries' max-5 cap; the session-start default path is throttled, not --force |
| T-145-07 | Information Disclosure | Cowork scheduled run egress | mitigate | The runner is zero-egress (Plan 01 T-145-01/02); repointing the Cowork task at the runner inherits that posture; competitor-watch stays hat-scoped public SIGNAL |
| T-145-08 | Tampering | opt-out / emergency disable | accept | SCOUT_CADENCE_SKIP=1 provides an emergency kill switch mirroring BRAIN_STALENESS_SKIP; acceptable that an operator can disable the cadence |
| T-145-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan -- documentation + hook wiring only |
</threat_model>

<artifacts_this_phase_produces>
- A session-start cadence slot (sentinel-bounded edit to scripts/session-start)
- A "Scheduled Cadence (LIVE)" section in commands/scout.md replacing the DEFERRED note
- A repointed Cowork scout-sentinel task in commands/scheduled-tasks.md
</artifacts_this_phase_produces>

<verification>
- grep confirms scout-cadence-runner is referenced from session-start + scheduled-tasks.md and session-start-throttle is documented in scout.md
- bash -n scripts/session-start parses clean
- the DEFERRED cron header is gone from scout.md
- zero em-dashes across all three edited files
</verification>

<success_criteria>
- CLI: scout suite fires on the session-start-throttled cadence with no manual /mos:scout (SCHED-01)
- The cron path is a documented CLI option; Cowork fires the same composition via the runner (SCHED-01 + SCHED-02 across all three surfaces)
- The session-start wiring is soft-fail, background, opt-out-able, and adds no startup latency
- All edits are em-dash-free and Part 8 zero-egress (inherited from the runner)
</success_criteria>

<output>
Create `.planning/phases/145-scheduled-sensor-activation/145-02-SUMMARY.md` when done
</output>
