---
phase: 145-scheduled-sensor-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/scout-cadence-runner.cjs
  - scripts/scout-cadence-guard.cjs
autonomous: true
requirements: [SCHED-01, SCHED-02]
canon_parts: ["Part 2 Engine 1", "Part 4", "Part 8"]
user_setup: []

must_haves:
  truths:
    - "A single command fires the full scout suite (snapshot + health-check + deadline-monitor + competitor-watch + HSI-recompute + opportunity-scan) plus the SCHED-02 four (whitespace recompute + reverse-salient + opportunity-bank scan + competitor watch) without a human typing /mos:scout"
    - "A last-run timestamp guard blocks a second fire inside the configured interval (default 24h), so auto-fire does not run every session"
    - "The runner reuses the Phase-140-hardened sentinel-* scripts and the existing whitespace/RS/opportunity surfaces -- it composes them, it does not re-implement any sensor"
    - "A scheduled run carries zero user content to Brain or web-unscoped: HSI-recompute is LOCAL, competitor-watch web is hat-scoped public SIGNAL only"
  artifacts:
    - path: "scripts/scout-cadence-runner.cjs"
      provides: "The single cadence entry point that composes the 6 scout sub-sensors + the SCHED-02 four behind a throttle"
      min_lines: 120
    - path: "scripts/scout-cadence-guard.cjs"
      provides: "The last-run timestamp throttle + Phase-140 safe-auto-fire pre/post guard (no health-check crash, no NULL source_path, no backup pollution)"
      min_lines: 80
  key_links:
    - from: "scripts/scout-cadence-runner.cjs"
      to: "scripts/sentinel-snapshot + sentinel-health-check + sentinel-deadline-monitor"
      via: "child_process exec of the existing hardened scripts"
      pattern: "sentinel-(snapshot|health-check|deadline-monitor)"
    - from: "scripts/scout-cadence-runner.cjs"
      to: "scripts/scout-cadence-guard.cjs"
      via: "shouldFire(interval) gate before any sensor runs; recordRun() after"
      pattern: "scout-cadence-guard"
---

<objective>
Build the cadence mechanism for the scout suite: a single runner that fires all six scout sub-sensors plus the four SCHED-02 sensors, gated by a last-run throttle and a Phase-140 safe-auto-fire guard. This is the WIRING layer (Canon Part 7 reuse) -- it composes existing hardened scripts, it builds no new sensor.

Purpose: SCHED-01 + SCHED-02 require the scout suite + whitespace/reverse-salient/opportunity-bank/competitor to fire on a cadence without manual /mos:scout. The session-start cadence host (Plan 02) and the verification suite (Plan 03) both depend on this runner + guard existing first.

Output: scripts/scout-cadence-runner.cjs (the composer) and scripts/scout-cadence-guard.cjs (the throttle + safe-auto-fire guard).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/MINDRIAN-CANON.md

# Part 7 reuse surfaces this plan COMPOSES (read before writing the runner):
@commands/scout.md
@scripts/scout-telemetry-aggregator.cjs
</context>

<part_7_reuse_statement>
This plan writes NO new sensor. It wires existing, Phase-140-hardened surfaces:
- snapshot: scripts/sentinel-snapshot (SENT-07; hardened HARD-03 backup exclusion)
- health-check: scripts/sentinel-health-check (SENT-01; hardened HARD-01 arithmetic guard)
- deadline-monitor: scripts/sentinel-deadline-monitor (SENT-02; hardened HARD-05 reads .planning/STATE.md)
- HSI-recompute: scripts/compute-hsi.py + scripts/detect-reverse-salients.py + scripts/hsi-to-graph.cjs (HARD-02 NOT-NULL-safe node insert)
- whitespace recompute: scripts/compute-whitespace-gaps.py + scripts/whitespace-to-graph.cjs
- opportunity-bank scan: scripts/compute-opportunity-state + lib/core/opportunity-ops.cjs
- competitor-watch: lib/core/scheduled-scanner.cjs (buildCompetitorQueries + fileCompetitorResults)
- efficiency telemetry: scripts/scout-telemetry-aggregator.cjs
The runner is the single composition point. None of the 25 methodology commands does scheduled multi-sensor composition behind a throttle, so the runner is the justified net-new wiring; every sensor it calls is reuse.
</part_7_reuse_statement>

<tasks>

<task type="auto">
  <name>Task 1: Build the safe-auto-fire guard (throttle + Phase-140 invariant pre/post checks)</name>
  <read_first>
    - scripts/sentinel-snapshot (full) -- the SNAPSHOT_CREATED / SNAPSHOT_PRUNED contract + the 12-week prune window
    - scripts/sentinel-health-check (lines 1-60) -- the BASELINE/HEALTHY/DRIFT contract + the HARD-01 arithmetic guard it now carries
    - scripts/session-start (lines 1506-1549) -- the /tmp/mindrian-update-check daily-cache throttle pattern to mirror for the last-run timestamp
  </read_first>
  <action>
    Create scripts/scout-cadence-guard.cjs (CJS, process.argv switch-case per the gsd-tools pattern; no commander/yargs per CLAUDE.md What-NOT-to-Use). Export three functions and expose them via subcommands:
    (1) shouldFire(stateDir, intervalHours) -- reads a last-run timestamp from $HOME/.mindrian/scout-cadence/last-run-<roomHash>.txt (roomHash = sha256 of the absolute room path, first 12 hex chars; LOCAL only, no room content in the path). Returns {fire:true} when the file is absent OR (now - lastRun) >= intervalHours*3600; otherwise {fire:false, nextEligible:<iso>}. Default intervalHours=24. This is the throttle that stops auto-fire running every session (hard fence: throttle).
    (2) recordRun(stateDir, roomDir) -- writes the current ISO timestamp to the same last-run file (mkdir -p the dir first; soft-fail on write error, never throw).
    (3) safeAutoFireCheck(roomDir) -- the Phase-140 invariant pre/post guard. Returns {ok, violations[]} asserting: NO health-check crash (the sentinel-health-check exit code surfaced by the runner is captured, not swallowed -- this function records the expectation, the runner enforces it); NO NULL source_path (after HSI-to-graph, query room.db via lib/core/navigation.cjs / lazygraph-ops.cjs read path for any nodes row with source_path IS NULL -- presence is a violation); NO backup pollution (scan the latest health/RS result set for any path under .heal-backup/ -- presence is a violation). Each violation is a string naming which HARD-NN invariant it maps to (HARD-01/HARD-02/HARD-03).
    Canon Part 8: this file makes ZERO network calls -- no fetch/http/curl/brain/tavily. The room-path hash is LOCAL identity only. Add a header comment stating the Part 8 LOCAL-only posture. No em-dashes anywhere in the file.
  </action>
  <verify>
    <automated>node scripts/scout-cadence-guard.cjs should-fire "$PWD" 24 && grep -vE '^\s*(//|\*|/\*)' scripts/scout-cadence-guard.cjs | grep -cE 'fetch\(|https?://|tavily|brain_query|mcp__' | grep -qx 0 && echo PART8_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - shouldFire returns fire:true on a fresh room (no last-run file), fire:false within the interval after recordRun, fire:true again after the interval elapses (simulate by backdating the timestamp file)
    - safeAutoFireCheck returns ok:true on a clean room and a violations[] entry tagged HARD-02 when a NULL source_path node is present, HARD-03 when a .heal-backup/ path appears in the result set
    - Part 8 grep gate (code lines only, comments filtered) shows zero fetch/http/tavily/brain matches: prints PART8_CLEAN
    - No em-dashes in the file
  </acceptance_criteria>
  <done>The guard module exposes shouldFire / recordRun / safeAutoFireCheck; the throttle and the three HARD-NN invariant checks are implemented and LOCAL-only.</done>
</task>

<task type="auto">
  <name>Task 2: Build the cadence runner that composes the 6 scout sub-sensors + the SCHED-02 four behind the guard</name>
  <read_first>
    - commands/scout.md (full) -- the canonical Step 1..6 ordering (snapshot first, then health, deadlines, competitor, HSI, efficiency) + the D-03 "do NOT swallow the HSI-to-graph exit" contract at lines 176-182
    - lib/core/scheduled-scanner.cjs (buildCompetitorQueries + fileCompetitorResults + the insufficient-competitors skip path) -- the hat-scoped public-SIGNAL competitor surface
    - scripts/whitespace-to-graph.cjs (header) + scripts/compute-whitespace-gaps.py (header) -- the whitespace recompute surface
    - scripts/scout-cadence-guard.cjs (the module Task 1 just wrote)
  </read_first>
  <action>
    Create scripts/scout-cadence-runner.cjs (CJS, process.argv: `node scout-cadence-runner.cjs <roomDir> [--interval-hours=N] [--force] [--json]`). Flow:
    (1) Resolve roomDir from argv[2]; if absent or not a dir, print a non-fatal "no room" notice and exit 0 (this runs from a hook -- soft-fail is the contract, never crash the host).
    (2) Call guard.shouldFire(roomDir, intervalHours). If fire:false and --force absent, print the nextEligible time and exit 0 (the throttle short-circuit -- no sensor runs).
    (3) When firing, run the suite in the canonical scout.md order, each step capturing exit code + stderr WITHOUT swallowing (per scout.md D-03). Steps: sentinel-snapshot, sentinel-health-check (capture exit -- a non-zero is a HARD-01 regression the guard must catch, not hide), sentinel-deadline-monitor, competitor-watch via scheduled-scanner.buildCompetitorQueries (skip cleanly when insufficient; the runner does NOT itself fetch -- it emits the query plan + a SIGNAL-scope marker so the LLM/Cowork layer executes the hat-scoped public web search, keeping the runner itself zero-egress), HSI-recompute (compute-hsi.py then detect-reverse-salients.py then hsi-to-graph.cjs, surfacing the degraded-mode advisory non-fatally per scout.md), whitespace recompute (compute-whitespace-gaps.py then whitespace-to-graph.cjs), opportunity-bank scan (compute-opportunity-state + opportunity-ops.listOpportunities to surface ADD candidates as graph-bound findings per Canon Part 4), efficiency telemetry (scout-telemetry-aggregator.cjs --json). Each Python step gates on check-hsi-deps and skips with a note when sklearn absent (reuse scout.md Step 5a contract).
    (4) After the suite, call guard.safeAutoFireCheck(roomDir); if violations exist, surface them prominently in the summary (these prove the Phase-140 hardening held or flag a regression) but stay non-fatal.
    (5) Call guard.recordRun(roomDir) so the throttle is armed for the interval.
    (6) Emit a structured summary object {fired, throttled, steps:[{name,status,exit}], violations, signal_query_plan, findings_to_graph}; print human-readable by default, JSON under --json.
    Canon Part 8: the runner itself performs NO Brain query and NO web fetch -- HSI/whitespace/RS/opportunity are all LOCAL compute; competitor-watch is emitted as a hat-scoped public-SIGNAL query plan for the surface layer to execute, never carrying room content into the query beyond public competitor names already in competitive-analysis/. Add a header comment stating this. No em-dashes.
  </action>
  <verify>
    <automated>node scripts/scout-cadence-runner.cjs /nonexistent-room --json | grep -q '"fired"' && grep -vE '^\s*(//|\*|/\*)' scripts/scout-cadence-runner.cjs | grep -cE 'fetch\(|tavily|brain_query|mcp__brain' | grep -qx 0 && echo RUNNER_OK</automated>
  </verify>
  <acceptance_criteria>
    - Running against a real room with no prior last-run fires the full suite (all 6 scout steps + whitespace + RS + opportunity-bank + competitor query-plan present in the steps[] array)
    - Running again immediately reports throttled:true and runs zero sensors; --force overrides
    - The summary surfaces safeAutoFireCheck violations (empty array on a clean Phase-140-hardened room)
    - The runner itself makes zero Brain/web calls (grep gate prints RUNNER_OK); competitor-watch is emitted as a SIGNAL query plan, not executed inside the runner
    - sklearn-absent path skips HSI/whitespace Python steps with a note, suite still completes
    - No em-dashes in the file
  </acceptance_criteria>
  <done>scout-cadence-runner.cjs composes all 10 sensors behind the throttle + guard, surfaces Phase-140 invariant results, and is zero-egress; SCHED-01 + SCHED-02 sensor composition is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduled run -> Brain | unattended auto-fire must carry NO user content to the Brain MCP |
| scheduled run -> external web (competitor-watch) | only hat-scoped PUBLIC competitor names may cross; never room artifacts/assumptions |
| unattended run -> local room state (room.db, .snapshots/, .intelligence/) | a crash or NULL write or backup-dir pollution corrupts state silently |
| cadence trigger -> external APIs | a missing throttle hammers Grants.gov / web search every session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-01 | Information Disclosure | HSI-recompute / whitespace / opportunity-scan in scout-cadence-runner.cjs | mitigate | All compute is LOCAL (compute-hsi.py, compute-whitespace-gaps.py, opportunity-ops.cjs); runner makes zero Brain/fetch calls; Part 8 grep gate in Task 2 verify (RUNNER_OK) enforces zero fetch/tavily/brain_query in code lines |
| T-145-02 | Information Disclosure | competitor-watch web path | mitigate | Runner does NOT fetch; it emits a buildCompetitorQueries query plan scoped to PUBLIC competitor names already in competitive-analysis/; the SIGNAL-scope marker confines the surface layer to hat-scoped public web; no room artifact text enters the query |
| T-145-03 | Denial of Service | unattended scheduled fire | mitigate | scout-cadence-guard.shouldFire throttle (default 24h, LOCAL last-run timestamp) blocks re-fire inside the interval; runner short-circuits when fire:false |
| T-145-04 | Tampering | unattended run corrupts room.db / pollutes results | mitigate | guard.safeAutoFireCheck asserts no NULL source_path (HARD-02), no .heal-backup pollution (HARD-03), and the runner captures sentinel-health-check exit (HARD-01) instead of swallowing; violations surfaced non-fatally |
| T-145-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan -- composes existing shipped scripts only; no Package Legitimacy Gate triggered |
</threat_model>

<artifacts_this_phase_produces>
- scripts/scout-cadence-runner.cjs -- the single throttled cadence composer for the 10 sensors
- scripts/scout-cadence-guard.cjs -- the last-run throttle + Phase-140 safe-auto-fire invariant guard
- A LOCAL last-run timestamp under $HOME/.mindrian/scout-cadence/ (runtime artifact, not committed)
</artifacts_this_phase_produces>

<verification>
- node scripts/scout-cadence-guard.cjs should-fire "$PWD" 24 runs clean and the Part 8 grep gate prints PART8_CLEAN
- node scripts/scout-cadence-runner.cjs <room> --json fires the full 10-sensor suite on a fresh room and reports throttled:true on immediate re-run
- grep -rn -- '--' on both new files returns zero em-dash characters
</verification>

<success_criteria>
- The runner fires the full scout suite + the SCHED-02 four from a single non-interactive command (SCHED-01 + SCHED-02 sensor composition)
- The throttle blocks re-fire inside the interval (no every-session runaway)
- safeAutoFireCheck encodes the three Phase-140 invariants (HARD-01/02/03) and the runner surfaces them
- Both files are zero-egress (Canon Part 8) and em-dash-free
</success_criteria>

<output>
Create `.planning/phases/145-scheduled-sensor-activation/145-01-SUMMARY.md` when done
</output>
