---
phase: 140-sentinel-and-instrumentation-hardening
plan: 04
type: execute
wave: 2
depends_on: [140-01, 140-02, 140-03]
files_modified:
  - CHANGELOG.md
  - .claude-plugin/plugin.json
  - package.json
autonomous: true
requirements: []
canon_parts: [Part 6, Part 7]
must_haves:
  truths:
    - "CHANGELOG.md carries a new '## [1.13.1-beta.6] - 2026-06-04' entry summarizing the 5 sentinel/instrumentation fixes (HARD-01..05) under Fixed/Changed"
    - "All five version surfaces report 1.13.1-beta.6: CHANGELOG top entry, .claude-plugin/plugin.json version, package.json version, the git tag v1.13.1-beta.6, and the marketplace source.ref pin"
    - "The local release commit + tag exist; the external publish (git push --tags + marketplace publish) is human-gated, NOT auto-pushed"
    - "The 57x-claim reconciliation note (D-01a, produced by Plan 03) is consulted before tagging so the published number is not silently redefined"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v1.13.1-beta.6 release entry summarizing HARD-01..05"
      contains: "1.13.1-beta.6"
    - path: ".claude-plugin/plugin.json"
      provides: "plugin.json version bumped to 1.13.1-beta.6"
      contains: "1.13.1-beta.6"
    - path: "package.json"
      provides: "package.json version bumped to 1.13.1-beta.6"
      contains: "1.13.1-beta.6"
  key_links:
    - from: ".claude-plugin/plugin.json"
      to: "CHANGELOG.md"
      via: "version field matches the CHANGELOG top entry (release lockstep)"
      pattern: "1.13.1-beta.6"
    - from: "package.json"
      to: ".claude-plugin/plugin.json"
      via: "version field matches plugin.json (release lockstep)"
      pattern: "1.13.1-beta.6"
---

<objective>
Cut the v1.13.1-beta.6 prerelease that ships the 5 Phase 140 sentinel/instrumentation fixes (HARD-01..05). The current version is 1.13.1-beta.5; the v1.13.1-EXECUTION-PLAN.md assigns Phase 140 to the beta.6 band. Per CLAUDE.md / release-process.md, a release is only a release when all five surfaces are in lockstep: CHANGELOG.md entry + .claude-plugin/plugin.json version + package.json version + git tag v1.13.1-beta.6 + marketplace source.ref pin. This plan performs the LOCAL half of the release (CHANGELOG, version bumps, commit, tag) and leaves the external publish (git push --tags + marketplace publish) as a human-gated step because the operator owns the call to push to origin.

Purpose: The Phase 140 fixes change user-visible sentinel behavior (scout no longer aborts, HSI edges land, backup dirs are skipped, telemetry records, phase deadlines surface) -- these are user-facing fixes, and CLAUDE.md mandates a lockstep version bump for any user-facing fix. Without this plan the fixes ship untagged and users get a silent version mismatch.

Output: CHANGELOG.md with the beta.6 entry, plugin.json + package.json at 1.13.1-beta.6, a local release commit, and the git tag v1.13.1-beta.6 -- with the external push deferred to the operator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-sentinel-and-instrumentation-hardening/140-RESEARCH.md
@.planning/phases/140-sentinel-and-instrumentation-hardening/140-CONTEXT.md
@.planning/phases/140-sentinel-and-instrumentation-hardening/140-01-PLAN.md
@.planning/phases/140-sentinel-and-instrumentation-hardening/140-02-PLAN.md
@.planning/phases/140-sentinel-and-instrumentation-hardening/140-03-PLAN.md

<interfaces>
<!-- Release lockstep contract (CLAUDE.md Release Process + .claude/includes/release-process.md "Version Consistency Rule"): -->
<!--   A release is only a release when ALL FIVE are in sync: -->
<!--     1. CHANGELOG.md top entry == version -->
<!--     2. .claude-plugin/plugin.json "version" == version -->
<!--     3. package.json "version" == version -->
<!--     4. git tag v<version> points at the release commit -->
<!--     5. ~/mindrian-marketplace/.claude-plugin/marketplace.json version + source.ref == v<version> -->
<!-- Current version (verified 2026-06-04): plugin.json = 1.13.1-beta.5, package.json = 1.13.1-beta.5. -->
<!-- Target: 1.13.1-beta.6 (the beta.6 band per v1.13.1-EXECUTION-PLAN.md). -->

<!-- PREFERRED ROUTE: scripts/release.sh --prerelease (verified to exist, with the --prerelease flag): -->
<!--   `bash scripts/release.sh --prerelease`  performs semver.inc(beta.N -> beta.N+1) = beta.5 -> beta.6, -->
<!--   then bumps plugin.json + package.json + marketplace.json (source.ref pinned), writes the CHANGELOG entry, -->
<!--   makes Commit A (the release commit) + tag, and (later steps) npm publish + minisite sync + git push. -->
<!--   The script's Step 0.5 requires node_modules/semver and will REFUSE to auto-npm-install (legitimacy guard). -->
<!--   The script's later steps (9, 9.5, 9.6) PUSH to origin + npm + minisite -- those are the human-gated external publish. -->
<!--   Use `--dry-run` to preview the bump algebra WITHOUT mutating anything (release.sh supports --dry-run). -->

<!-- D-01a precondition (from Plan 03): -->
<!--   The release process "consumes the 57x claim before tagging." Plan 03 Task 2 produces -->
<!--   .planning/phases/140-.../140-57X-CLAIM-RECONCILIATION.md. Read it before tagging: if it flags that the relaxed -->
<!--   all-turns denominator materially changes the published "up to 57x" number, surface that to the operator. The -->
<!--   claim-language change itself is DEFERRED (CONTEXT Deferred Ideas); this plan only consults + surfaces, does not rewrite copy. -->

<!-- Em-dash rule: hyphens only, no em-dashes anywhere in the CHANGELOG entry or any edited surface. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-tag preflight -- confirm Wave 1 is merged, versions, and consult the 57x reconciliation note</name>
  <read_first>
    - .claude-plugin/plugin.json (confirm current version is 1.13.1-beta.5)
    - package.json (confirm current version is 1.13.1-beta.5)
    - scripts/release.sh (confirm the --prerelease and --dry-run flags; Step 0.5 semver preflight; the push steps that are human-gated)
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-57X-CLAIM-RECONCILIATION.md (the D-01a note produced by Plan 03 Task 2 -- consult before tagging)
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-01-SUMMARY.md, 140-02-SUMMARY.md, 140-03-SUMMARY.md (confirm the three Wave-1 plans completed and their regression tests are green)
  </read_first>
  <action>Confirm the three Wave-1 plans (140-01/02/03) are complete by checking their SUMMARY files exist and report green regression tests. Confirm the current version is 1.13.1-beta.5 in BOTH plugin.json and package.json (`grep '"version"' .claude-plugin/plugin.json package.json`). Confirm scripts/release.sh exists and supports `--prerelease` (beta.5 -> beta.6) and `--dry-run`. Run a dry-run preview to confirm the computed NEW_VERSION is 1.13.1-beta.6 WITHOUT mutating anything: `bash scripts/release.sh --prerelease --dry-run 2>&1 | tail -30`. Read the 140-57X-CLAIM-RECONCILIATION.md note (D-01a) and record in the SUMMARY whether the relaxed all-turns denominator materially changes the published "up to 57x" number; if it does, flag it as a release-process follow-up for the operator (the claim-language change is DEFERRED, not done here). If node_modules/semver is absent, surface it as a blocker (release.sh refuses to auto-install per its legitimacy guard -- the operator must run `npm install`); do NOT run npm install from within the plan. Do NOT mutate any version surface in this task.</action>
  <verify>
    <automated>grep '"version"' .claude-plugin/plugin.json package.json; bash scripts/release.sh --prerelease --dry-run 2>&1 | grep -iE "beta\.6|NEW_VERSION|1\.13\.1" | head -10; test -f .planning/phases/140-sentinel-and-instrumentation-hardening/140-57X-CLAIM-RECONCILIATION.md && echo "RECON NOTE PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - Both plugin.json and package.json currently report 1.13.1-beta.5 (the pre-bump baseline).
    - The dry-run preview computes NEW_VERSION = 1.13.1-beta.6 (the --prerelease beta.N -> beta.N+1 algebra).
    - The 140-57X-CLAIM-RECONCILIATION.md note exists and has been consulted; the SUMMARY records whether the published 57x number is materially redefined (and flags a release-process follow-up if so).
    - The three Wave-1 SUMMARY files exist and report green regression tests (the fixes being shipped are validated).
    - No version surface was mutated in this task (preflight only).
  </acceptance_criteria>
  <done>Preflight confirms beta.5 baseline, the beta.6 target via dry-run, and the 57x reconciliation note has been consulted -- ready to cut the release locally.</done>
</task>

<task type="auto">
  <name>Task 2: Author the CHANGELOG.md v1.13.1-beta.6 entry (HARD-01..05 summary)</name>
  <read_first>
    - CHANGELOG.md (the top of the file -- confirm the current top entry is the 1.13.1-beta.5 release and match its Keep-a-Changelog heading + Added/Fixed/Changed format)
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-CONTEXT.md (the D-01..D-04 decisions to summarize accurately)
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-01-SUMMARY.md, 140-02-SUMMARY.md, 140-03-SUMMARY.md (the actual shipped fixes to cite)
  </read_first>
  <action>Add a new entry at the TOP of CHANGELOG.md (above the 1.13.1-beta.5 entry), with the exact heading `## [1.13.1-beta.6] - 2026-06-04`. Summarize the 5 Phase 140 sentinel/instrumentation fixes under `### Fixed` and `### Changed`, matching the existing CHANGELOG format. Suggested content (adjust to match the actual SUMMARY outcomes): under `### Fixed` -- HARD-01 (sentinel-health-check no longer aborts under set -euo pipefail on a zero-edge snapshot; numeric captures sanitized to single-line integers), HARD-02 (HSI-to-graph node inserts route through one shared NOT-NULL-safe helper so edges land in a Phase-109-migrated room.db without "NOT NULL constraint failed: nodes.source_path"; both-schema safe), HARD-03 (compute-hsi.py and rs-engine.py exclude .heal-backup/ so backup-dir duplicates stop polluting HSI / reverse-salient results), HARD-04 (query-efficiency telemetry hook relaxed to measure ALL turns so it records events instead of logging 0), HARD-05 (deadline monitor now reads .planning/STATE.md phase deadlines, not just funding/ + opportunity-bank/, so phase deadlines surface as DUE instead of CLEAR); under `### Changed` -- the scout HSI-to-graph step no longer swallows write failures with 2>/dev/null || true (D-03, surfaces a degraded-step advisory), and the telemetry aggregator gained a --mos-only population filter so the published "up to 57x" claim stays measurable on its original /mos: denominator (D-01a). If the 57x reconciliation note flagged a material change, add a one-line note that the claim language reconciliation is tracked as a release-process follow-up. Use HYPHENS only -- no em-dashes anywhere in the entry. Do NOT bump plugin.json or package.json in this task (Task 3 does the lockstep bump + tag).</action>
  <verify>
    <automated>grep -n "## \[1.13.1-beta.6\] - 2026-06-04" CHANGELOG.md; head -40 CHANGELOG.md | grep -iE "HARD-0[1-5]|sentinel|hsi|telemetry|deadline" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md has a new top entry with the exact heading `## [1.13.1-beta.6] - 2026-06-04`.
    - The entry summarizes all 5 fixes (HARD-01..05) under Fixed/Changed in the existing CHANGELOG format.
    - The entry mentions the D-03 scout-swallow removal and the D-01a --mos-only aggregator filter.
    - No em-dashes anywhere in the new entry (`grep -nP "\x{2014}" CHANGELOG.md` returns nothing in the new entry region).
    - plugin.json and package.json are still at 1.13.1-beta.5 at the end of this task (the bump + tag is Task 3).
  </acceptance_criteria>
  <done>The CHANGELOG.md v1.13.1-beta.6 entry exists at the top, summarizing HARD-01..05 plus the D-03 and D-01a changes, em-dash-free.</done>
</task>

<task type="auto">
  <name>Task 3: Cut the local release -- bump all version surfaces to 1.13.1-beta.6, commit, and tag (5-gate lockstep, external push deferred)</name>
  <read_first>
    - scripts/release.sh (the --prerelease path: Steps 3-6 bump plugin.json + package.json + marketplace.json source.ref + CHANGELOG; Step 7 Commit A + tag vN; the push steps 9/9.5/9.6 that are human-gated; the --no-next-bump / --dry-run flags)
    - .claude-plugin/plugin.json (the version field to land at 1.13.1-beta.6)
    - package.json (the version field to land at 1.13.1-beta.6)
    - .claude/includes/release-process.md (the Version Consistency Rule -- all five gates; "scripts/release.sh is the ONLY way to release")
  </read_first>
  <action>Bring all five lockstep surfaces to 1.13.1-beta.6 and create the local release commit + tag, WITHOUT pushing to origin or publishing externally.

PREFERRED ROUTE (run release.sh as far as the local commit + tag, then STOP before the push):
  - If release.sh can be driven to perform only the local steps (bump + Commit A + tag) and stop before the push/publish steps, use `bash scripts/release.sh --prerelease`, but the moment it reaches the external-publish steps (Step 9 push, Step 9.5 npm publish, Step 9.6 minisite sync, Step 9.7 npx self-test) those are the human-gated boundary -- do NOT let the plan complete a `git push --tags`, an `npm publish`, or a marketplace push. If release.sh cannot be cleanly halted before its push step in this environment, fall back to the manual 5-step form below.

MANUAL 5-STEP FALLBACK (only if release.sh cannot stop before push):
  1. CHANGELOG.md entry -- already authored in Task 2 (verify the top entry is `## [1.13.1-beta.6] - 2026-06-04`).
  2. Bump `.claude-plugin/plugin.json` "version" to "1.13.1-beta.6".
  3. Bump `package.json` "version" to "1.13.1-beta.6".
  4. Stage the three lockstep files individually (CHANGELOG.md, .claude-plugin/plugin.json, package.json) and commit: `release: v1.13.1-beta.6 -- Phase 140 sentinel + instrumentation hardening (HARD-01..05)`. Then create the tag: `git tag v1.13.1-beta.6` pointing at the release commit.
  5. Marketplace source.ref pin: record in the SUMMARY that the marketplace.json source.ref must be pinned to v1.13.1-beta.6 as part of the human-gated external publish (the marketplace repo lives at ~/mindrian-marketplace and is pushed by the operator; do NOT push it here).

EITHER ROUTE -- HARD CONSTRAINTS:
  - The local release commit + the v1.13.1-beta.6 tag are fine to create (autonomous).
  - `git push` / `git push --tags`, `npm publish`, and the marketplace publish are HUMAN-GATED -- do NOT execute them. Leave clear operator instructions in the SUMMARY (the two-command upgrade path + `git push origin main --tags` + marketplace update).
  - Use HYPHENS only in the commit message and tag annotation; no em-dashes.
  - After the bump, all four local gates must report 1.13.1-beta.6: CHANGELOG top entry, plugin.json, package.json, and the tag. The fifth gate (marketplace source.ref) is completed by the operator during the external publish.</action>
  <verify>
    <automated>grep '"version"' .claude-plugin/plugin.json package.json | grep -c "1.13.1-beta.6"; git tag --list "v1.13.1-beta.6"; head -3 CHANGELOG.md | grep -c "1.13.1-beta.6"; git log --oneline -1</automated>
  </verify>
  <acceptance_criteria>
    - `.claude-plugin/plugin.json` "version" == "1.13.1-beta.6" (`grep '"version"' .claude-plugin/plugin.json` shows beta.6).
    - `package.json` "version" == "1.13.1-beta.6".
    - CHANGELOG.md top entry == `## [1.13.1-beta.6] - 2026-06-04` (carried from Task 2).
    - The git tag `v1.13.1-beta.6` exists and points at the local release commit (`git tag --list "v1.13.1-beta.6"` returns the tag).
    - NO push to origin and NO external publish was performed: `git log origin/main..HEAD` shows the release commit is local-only (ahead of origin), and no `npm publish` / marketplace push ran.
    - The SUMMARY records the human-gated external-publish steps (git push origin main --tags, marketplace source.ref pin to v1.13.1-beta.6 + marketplace update, npm publish if applicable) as the operator's next action.
    - No em-dashes in the commit message, tag, or any edited surface.
  </acceptance_criteria>
  <done>All four local lockstep gates report 1.13.1-beta.6 (CHANGELOG + plugin.json + package.json + tag); the external publish (push + marketplace + npm) is left as a documented human-gated step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local release commit -> origin/GitHub | Pushing to origin distributes the release to every user; gated to the operator. |
| version surfaces -> users' installs | A drift between the five lockstep surfaces produces silent version mismatches for users. |
| 57x published claim -> release tagging | Tagging a release that ships a relaxed telemetry denominator could silently redefine a published number (D-01a). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-09 | Tampering / Integrity | five-surface version lockstep | mitigate | All five gates land at 1.13.1-beta.6 (four local in this plan, marketplace ref by the operator); release.sh is the canonical lockstep enforcer |
| T-140-10 | Elevation / Repudiation | external publish (push + npm + marketplace) | mitigate | Human-gated: the plan creates only the local commit + tag; push/publish is the operator's explicit call (release infrastructure ships as beta first, per release-process.md) |
| T-140-11 | Repudiation / Integrity | 57x published claim denominator | mitigate | Consult the D-01a reconciliation note before tagging; flag any material change as a release-process follow-up (no silent redefinition) |
| T-140-SC | Tampering | npm/pip/cargo installs | mitigate | N/A -- zero external packages added; release.sh REFUSES to auto-npm-install (its Step 0.5 legitimacy guard); if semver is missing the operator runs npm install |
</threat_model>

<verification>
- `grep '"version"' .claude-plugin/plugin.json package.json` both report 1.13.1-beta.6.
- CHANGELOG.md top entry is `## [1.13.1-beta.6] - 2026-06-04` summarizing HARD-01..05.
- `git tag --list "v1.13.1-beta.6"` returns the tag, pointing at the local release commit.
- `git log origin/main..HEAD` shows the release commit is local-only (no push performed).
- The SUMMARY documents the human-gated external publish (push + marketplace source.ref pin + npm) and consults the D-01a 57x reconciliation note.
- No em-dashes across all modified files.
</verification>

<success_criteria>
- Four local lockstep gates at 1.13.1-beta.6: CHANGELOG entry + plugin.json + package.json + git tag v1.13.1-beta.6.
- The Phase 140 user-facing fixes (HARD-01..05) are summarized in the CHANGELOG per CLAUDE.md release process.
- The external publish (git push --tags + marketplace publish + npm) is correctly left as a human-gated step, not auto-pushed.
- The D-01a 57x-claim reconciliation note was consulted before tagging (no silent redefinition of the published number).
</success_criteria>

<output>
Create `.planning/phases/140-sentinel-and-instrumentation-hardening/140-04-SUMMARY.md` when done
</output>
