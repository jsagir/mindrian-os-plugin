---
phase: 140-sentinel-and-instrumentation-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sentinel-health-check
  - scripts/compute-hsi.py
  - scripts/rs-engine.py
  - tests/test-sentinel-health-check.sh
  - tests/test-hsi-skip-heal-backup.sh
autonomous: true
requirements: [HARD-01, HARD-03]
canon_parts: [Part 4, Part 7]
must_haves:
  truths:
    - "sentinel-health-check runs to completion on a snapshot with zero edge-keyword matches, without the line-132 arithmetic syntax error and without aborting under set -euo pipefail (HARD-01)"
    - "compute-hsi.py excludes .heal-backup/ from its artifact walk -- a room with .heal-backup/<TS>/dup.md yields zero artifacts under that path (HARD-03)"
    - "rs-engine.py excludes .heal-backup/ from its walk (HARD-03 -- both scanners fixed per Pitfall 3)"
  artifacts:
    - path: "scripts/sentinel-health-check"
      provides: "Arithmetic-safe numeric capture for edge/entry counts"
      contains: "current_edges"
    - path: "scripts/compute-hsi.py"
      provides: "SKIP_DIRS set including .heal-backup"
      contains: ".heal-backup"
    - path: "scripts/rs-engine.py"
      provides: "SKIP_DIRS set including .heal-backup"
      contains: ".heal-backup"
    - path: "tests/test-sentinel-health-check.sh"
      provides: "HARD-01 zero-edge-snapshot regression smoke test"
      contains: "sentinel-health-check"
    - path: "tests/test-hsi-skip-heal-backup.sh"
      provides: "HARD-03 both-scanner skip regression smoke test"
      contains: ".heal-backup"
  key_links:
    - from: "tests/test-sentinel-health-check.sh"
      to: "scripts/sentinel-health-check"
      via: "invokes the script against a zero-edge fixture room"
      pattern: "sentinel-health-check"
    - from: "tests/test-hsi-skip-heal-backup.sh"
      to: "scripts/compute-hsi.py"
      via: "invokes the walker against a fixture room with .heal-backup"
      pattern: "compute-hsi"
---

<objective>
Fix HARD-01 (the sentinel-health-check arithmetic abort) and HARD-03 (the .heal-backup scanner pollution). HARD-01: the `grep -ciE ... || echo "0"` numeric captures (lines 85-86) and the `bc ... || echo "0"` captures (lines 81-82) produce a two-line `0\n0` when grep matches zero (grep -c prints 0 AND exits 1, so `|| echo "0"` appends a SECOND line), which breaks `$(( ))` at the deltas (lines 118, 132) under set -euo pipefail. HARD-03: add `.heal-backup` to the two independent SKIP_DIRS sets (compute-hsi.py:100 and rs-engine.py:119) so backup-dir duplicates stop polluting HSI / reverse-salient results (D-04 -- ONLY .heal-backup this phase; the general ignore-list is deferred).

Purpose: A scheduled scout (Phase 145) must run health-check to completion and must not index backup-dir duplicates. These are the two bash/Python scanner bugs in the scout suite.

Output: arithmetic-safe numeric captures in sentinel-health-check, .heal-backup added to both SKIP_DIRS sets, and two bash smoke tests (one per bug).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-sentinel-and-instrumentation-hardening/140-RESEARCH.md
@.planning/phases/140-sentinel-and-instrumentation-hardening/140-CONTEXT.md

<interfaces>
<!-- HARD-01 root cause (RESEARCH Anti-Patterns + Pitfall 1 + Code Examples): -->
<!--   scripts/sentinel-health-check:85-86 -->
<!--     current_edges=$(grep -ciE 'INFORMS|CONTRADICTS|...' "$CURRENT" || echo "0") -->
<!--     previous_edges=$(grep -ciE '...' "$PREVIOUS" || echo "0") -->
<!--   grep -c prints "0" AND exits 1 on zero matches -> "|| echo 0" appends -> var becomes "0\n0" -->
<!--   then line 118 edge/entry delta: $((current_edges - previous_edges)) -> syntax error in expression -->
<!--   ALSO lines 81-82: current_total / previous_total via `... | bc 2>/dev/null || echo "0"` can emit empty or multi-line. -->
<!--   Sanitize EVERY numeric capture (81-82, 85-86) before the deltas at 118 + 132. -->
<!--   Fix idiom (Don't-Hand-Roll): use `grep -c ...; true` then coerce, OR `| tr -dc '0-9'`, OR `${var:-0}` + tail -1. -->
<!--   Do NOT only guard the empty-string case (that already coerces to 0); the failure is the TWO-LINE 0\n0. -->

<!-- HARD-03 sites (both omit .heal-backup; both must be fixed -- Pitfall 3): -->
<!--   scripts/compute-hsi.py:100  SKIP_DIRS = {".lazygraph", ".git", "node_modules", ".hsi-cache.json"} -->
<!--   scripts/rs-engine.py:119    SKIP_DIRS = {".lazygraph", ".git", ".mindrian", "node_modules", ".obsidian"} -->
<!--   .heal-backup/<TS>/ is created by scripts/heal-command.cjs:937,1021 and exists live in the dogfood room. -->
<!--   D-04: add ONLY .heal-backup this phase. The broader dot-dir ignore-list (.snapshots/.intelligence/.heal) is DEFERRED. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 -- create both HARD-01 and HARD-03 regression smoke tests (RED)</name>
  <read_first>
    - scripts/sentinel-health-check (lines 78-90 numeric captures, 115-135 deltas; the abort site)
    - scripts/compute-hsi.py (the os.walk + SKIP_DIRS at :100; the discover/parse path)
    - scripts/rs-engine.py (SKIP_DIRS at :119)
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-RESEARCH.md (Validation Architecture test map lines 249,251; Pitfall 1 zero-edge fixture; Pitfall 3 both-scanner)
  </read_first>
  <behavior>
    - HARD-01 test (tests/test-sentinel-health-check.sh): build a fixture room with a snapshot pair where the PREVIOUS snapshot has ZERO edge-keyword matches (no INFORMS/CONTRADICTS/CONVERGES/ENABLES/INVALIDATES lines). Invoke scripts/sentinel-health-check against it. Assert the script exits 0 AND writes a valid health report file AND its output does not contain "syntax error in expression" or "unbound variable". Before the fix this RED-fails (abort under set -e).
    - HARD-03 test (tests/test-hsi-skip-heal-backup.sh): build a fixture room containing a normal artifact AND a .heal-backup/<TS>/dup.md duplicate. Invoke the compute-hsi.py walker (its discover_artifacts path -- exercisable without sklearn per RESEARCH Environment Availability) and assert no discovered artifact id or path contains ".heal-backup". Repeat the same assertion driving rs-engine.py's walk. Before the fix this RED-fails (the dup is indexed).
  </behavior>
  <action>Create tests/test-sentinel-health-check.sh and tests/test-hsi-skip-heal-backup.sh as self-contained bash smoke harnesses (mirror the existing tests/*.sh style). Each builds its fixture under a mktemp -d dir and cleans up on exit. The HARD-01 harness must construct a PREVIOUS snapshot with zero edge keywords (the exact Pitfall 1 trigger) and assert the script survives. The HARD-03 harness must place a file under .heal-backup/<TS>/ and assert neither walker indexes it; if sklearn/numpy are unavailable, exercise only the artifact-discovery/walk function (not the embedding step) so the skip-list assertion still runs. Both tests MUST be RED before Tasks 2-3. Use exit-code based pass/fail (exit 1 on any failed assertion). No em-dashes.</action>
  <verify>
    <automated>bash tests/test-sentinel-health-check.sh; echo "HC exit=$?"; bash tests/test-hsi-skip-heal-backup.sh; echo "SKIP exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist and are bash harnesses with mktemp-based fixtures and trap cleanup.
    - Before Tasks 2-3: `bash tests/test-sentinel-health-check.sh` exits non-zero (RED -- reproduces the line-132 abort) AND `bash tests/test-hsi-skip-heal-backup.sh` exits non-zero (RED -- the .heal-backup dup is indexed).
    - Neither test writes outside its mktemp fixture dir (no real room touched).
    - No em-dashes in either file.
  </acceptance_criteria>
  <done>Both regression smoke tests exist and are RED against the unfixed scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Fix HARD-01 -- arithmetic-safe numeric captures in sentinel-health-check</name>
  <read_first>
    - scripts/sentinel-health-check (lines 80-90 captures: current_total/previous_total via bc at 81-82, current_edges/previous_edges via grep -ciE at 85-86; lines 115-135 deltas at 118 + 132)
    - tests/test-sentinel-health-check.sh (the RED contract from Task 1)
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-RESEARCH.md (Pitfall 1 -- guard the TWO-LINE 0\n0, not just the empty case; Don't-Hand-Roll fix idiom)
  </read_first>
  <action>In scripts/sentinel-health-check, make every numeric capture feeding an arithmetic expression single-line and numeric. For current_edges and previous_edges (lines 85-86), replace the `grep -ciE '...' || echo "0"` form with a form that does not append a second line on zero matches and exits cleanly under set -e -- for example capture grep -ciE output with its exit code neutralized (a trailing `; true` or by piping through `tr -dc '0-9' | head -c 8`) then coerce empty to 0 with `${var:-0}`. Apply the same single-line numeric sanitization to current_total and previous_total (lines 81-82, the `... | bc ... || echo "0"` captures, which can emit empty or multi-line). Ensure the deltas at line 118 (entry_delta) and line 132 (edge_delta) operate on guaranteed-single-line integers. Do NOT change the report content or the drift-detection semantics; only make the numeric coercion robust. Verify the fix addresses the double-zero case specifically (per Pitfall 1: the empty-string case already coerces safely; the actual abort is the two-line 0\n0 from grep -c exit 1).</action>
  <verify>
    <automated>bash tests/test-sentinel-health-check.sh && echo "HARD-01 GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/test-sentinel-health-check.sh` exits 0 (GREEN) -- the script runs to completion on the zero-edge snapshot and writes a valid report.
    - The script no longer contains a numeric capture of the form `$(grep -c... || echo "0")` feeding an arithmetic expression: `grep -nE "grep -ci?E.*\|\| echo" scripts/sentinel-health-check` returns no line that feeds current_edges/previous_edges (the double-zero pattern is gone).
    - Running the script against a zero-edge fixture produces no "syntax error in expression" and no "unbound variable" on stderr.
    - No em-dashes in scripts/sentinel-health-check changes.
  </acceptance_criteria>
  <done>sentinel-health-check survives a zero-edge snapshot and writes a valid report (HARD-01 closed).</done>
</task>

<task type="auto">
  <name>Task 3: Fix HARD-03 -- add .heal-backup to both SKIP_DIRS sets</name>
  <read_first>
    - scripts/compute-hsi.py (SKIP_DIRS at :100)
    - scripts/rs-engine.py (SKIP_DIRS at :119)
    - tests/test-hsi-skip-heal-backup.sh (the RED contract from Task 1)
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-CONTEXT.md (D-04 -- ONLY .heal-backup; the general ignore-list is DEFERRED)
  </read_first>
  <action>Add the string ".heal-backup" to the SKIP_DIRS set literal in scripts/compute-hsi.py (line 100) AND to the SKIP_DIRS set literal in scripts/rs-engine.py (line 119). Fix BOTH scanners (Pitfall 3 -- two independent walkers, two independent SKIP_DIRS sets; detect-reverse-salients.py inherits compute-hsi.py output and needs no edit). Per D-04 add ONLY ".heal-backup" -- do NOT add .snapshots / .intelligence / .heal or build a general ignore-list (that is explicitly deferred). Make no other change to the walk or scoring logic.</action>
  <verify>
    <automated>bash tests/test-hsi-skip-heal-backup.sh && grep -c '\.heal-backup' scripts/compute-hsi.py && grep -c '\.heal-backup' scripts/rs-engine.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.heal-backup" scripts/compute-hsi.py` returns >= 1 and the string sits inside the SKIP_DIRS set literal.
    - `grep -c "\.heal-backup" scripts/rs-engine.py` returns >= 1 and the string sits inside the SKIP_DIRS set literal.
    - `bash tests/test-hsi-skip-heal-backup.sh` exits 0 (GREEN) -- neither walker indexes the .heal-backup/<TS>/dup.md fixture.
    - No general ignore-list was added: `grep -E "\.snapshots|\.intelligence" scripts/compute-hsi.py scripts/rs-engine.py` shows no NEW skip entries beyond pre-existing ones (D-04 scope held).
    - No em-dashes in either file.
  </acceptance_criteria>
  <done>Both HSI and reverse-salient scanners exclude .heal-backup/; backup-dir duplicates no longer pollute results (HARD-03 closed, D-04 scope held).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot file -> health-check arithmetic | Untrusted count data (could be zero, empty, or malformed) crosses into bash `$(( ))`. |
| room filesystem -> HSI/RS walker | Backup-dir artifacts cross into the indexed result set / artifact IDs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-04 | Denial of Service | sentinel-health-check arithmetic abort | mitigate | Sanitize every numeric capture to a single-line integer so a zero-edge snapshot cannot abort the scheduled scout (HARD-01) |
| T-140-05 | Tampering | HSI/RS artifact IDs with .heal-backup/<TS>/ path segments | mitigate | Exclude .heal-backup from both SKIP_DIRS sets so spurious path-segmented artifact IDs never enter the graph (HARD-03) |
| T-140-SC | Tampering | npm/pip/cargo installs | mitigate | N/A -- zero external packages added (pure bash + Python skip-list edits) |
</threat_model>

<verification>
- `bash tests/test-sentinel-health-check.sh` passes (zero-edge snapshot survives).
- `bash tests/test-hsi-skip-heal-backup.sh` passes (both scanners skip .heal-backup).
- D-04 scope held: only .heal-backup added to SKIP_DIRS, no general ignore-list.
- No em-dashes across all modified files.
</verification>

<success_criteria>
- HARD-01 closed: health-check runs to completion without the line-132 arithmetic syntax error on a zero-edge snapshot.
- HARD-03 closed: compute-hsi.py and rs-engine.py both exclude .heal-backup/ (D-04 scope: ONLY .heal-backup).
</success_criteria>

<output>
Create `.planning/phases/140-sentinel-and-instrumentation-hardening/140-02-SUMMARY.md` when done
</output>
