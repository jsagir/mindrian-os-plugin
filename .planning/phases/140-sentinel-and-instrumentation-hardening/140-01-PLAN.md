---
phase: 140-sentinel-and-instrumentation-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/node-insert.cjs
  - lib/core/hsi-to-graph.test.cjs
  - scripts/hsi-to-graph.cjs
  - lib/core/lazygraph-ops.cjs
  - commands/scout.md
autonomous: true
requirements: [HARD-02]
canon_parts: [Part 4, Part 7, Part 8]
must_haves:
  truths:
    - "Running HSI lands its Section + edge nodes in a Phase-109-migrated room.db with no NOT NULL constraint failed: nodes.source_path"
    - "Running HSI lands its nodes in an un-migrated 3-column room.db with no error (both-schema safe per D-02a)"
    - "A write failure in the scout HSI-to-graph step surfaces instead of being swallowed (D-03)"
    - "All four bare-3-column node inserts (hsi-to-graph plus 3 lazygraph-ops siblings) route through one shared NOT-NULL-safe helper (D-02)"
  artifacts:
    - path: "lib/core/node-insert.cjs"
      provides: "One shared NOT-NULL-safe node insert helper covering the Phase-109 provenance columns"
      exports: ["insertNode"]
      min_lines: 25
    - path: "scripts/hsi-to-graph.cjs"
      provides: "HSI-to-graph writer routed through the shared helper"
      contains: "node-insert"
    - path: "lib/core/lazygraph-ops.cjs"
      provides: "3 sibling node-insert sites routed through the shared helper"
      contains: "node-insert"
    - path: "lib/core/hsi-to-graph.test.cjs"
      provides: "HARD-02 regression test against both schemas"
      contains: "source_path"
  key_links:
    - from: "scripts/hsi-to-graph.cjs"
      to: "lib/core/node-insert.cjs"
      via: "require + insertNode call"
      pattern: "require.*node-insert"
    - from: "lib/core/lazygraph-ops.cjs"
      to: "lib/core/node-insert.cjs"
      via: "require + insertNode call"
      pattern: "require.*node-insert"
    - from: "commands/scout.md"
      to: "scripts/hsi-to-graph.cjs"
      via: "invocation without the 2>/dev/null swallow"
      pattern: "hsi-to-graph"
---

<objective>
Fix HARD-02 (the NOT NULL constraint failed: nodes.source_path defect) by fixing the WHOLE bug-class through ONE shared NOT-NULL-safe node insert helper (D-02), routing all four bare-3-column inserts through it (scripts/hsi-to-graph.cjs plus the 3 latent siblings in lib/core/lazygraph-ops.cjs), making the helper robust to BOTH the Phase-109-migrated 4-col schema AND the un-migrated 3-col schema (D-02a), and unmasking the scout HSI-to-graph write failure by removing the 2>/dev/null || true swallow (D-03).

Purpose: HSI edges currently never reach room.db on a migrated room because the bare insert violates the Phase-109 NOT NULL columns, and the failure is hidden by a silent swallow in scout. A scout that auto-fires on a schedule (Phase 145) must write its signal and be loud on failure. This is a HARD prerequisite for Phase 145.

Output: lib/core/node-insert.cjs (the shared helper), four call sites routed through it, the scout.md swallow removed, and lib/core/hsi-to-graph.test.cjs proving both-schema safety.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-sentinel-and-instrumentation-hardening/140-RESEARCH.md
@.planning/phases/140-sentinel-and-instrumentation-hardening/140-CONTEXT.md

<interfaces>
<!-- The correct wide-insert column-set (the live correct pattern to copy, per RESEARCH Don't-Hand-Roll). -->
<!-- Source: lib/core/navigation/evidence-claim.cjs:110 -->
<!-- Phase-109 migrated nodes table requires these NOT NULL columns the legacy 3-col insert omits: -->
<!--   source_path, created_by, created_at, last_seen_at -->
<!-- created_by CHECK constraint accepts one of: 'user','larry','import','brain','system' -->
<!-- For HSI-to-graph the correct provenance values are: -->
<!--   source_path: 'system:hsi-to-graph'   created_by: 'system'   review_status: 'proposed' (or DEFAULT) -->
<!--   created_at: Date.now()   last_seen_at: Date.now() -->
<!-- The four bare-3-column insert sites to consolidate: -->
<!--   scripts/hsi-to-graph.cjs:68-70 (upsertNode), :116-117 (Section upsert) -->
<!--   lib/core/lazygraph-ops.cjs:357, :362, :729, :911 -->
<!-- Open path divergence (Pitfall 2): openGraph() runs only initSchema(), NOT the Phase 109 migration; -->
<!--   openRoomDb() runs the migration. So the writer can hit EITHER schema. The helper must be both-schema safe -->
<!--   via PRAGMA table_info(nodes) column-detection OR by routing the open through openRoomDb (migrate-then-write). -->
<!-- Reuse anchor: lib/core/room-db.cjs::openRoomDb is the single composition point that migrates. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 -- create the HARD-02 both-schema regression test</name>
  <read_first>
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-RESEARCH.md (HARD-02 row + Pitfall 2 + Validation Architecture test map line 250,262 + Code Examples HARD-02 reproduction)
    - scripts/hsi-to-graph.cjs (the writer under test; the 68-70 and 116-117 insert sites)
    - lib/core/room-db.cjs (openRoomDb -- the migrate-then-write composition point used to build the migrated fixture)
    - lib/core/lazygraph-ops.cjs (openGraph -- the initSchema-only un-migrated path)
  </read_first>
  <behavior>
    - Test A (migrated schema): build a fixture room.db via openRoomDb (which runs the Phase 109 migration so nodes has source_path/created_by/created_at/last_seen_at NOT NULL), run the hsi-to-graph node insert path, assert Section + edge nodes land with NO NOT NULL constraint failed: nodes.source_path error.
    - Test B (un-migrated schema): build a fixture room.db with the legacy 3-column nodes table (id, type, properties only), run the same insert path, assert it completes with no error (D-02a both-schema safety -- a migrated db rejects the bare 3-col insert; the helper must work on both).
    - Test C (provenance values): assert inserted nodes carry source_path = 'system:hsi-to-graph' and created_by = 'system' on the migrated schema (the CHECK constraint on created_by is satisfied).
  </behavior>
  <action>Create lib/core/hsi-to-graph.test.cjs as a node:test (.test.cjs) suite. Build two temp room.db fixtures under a per-test tmp dir: the migrated one via lib/core/room-db.cjs openRoomDb, the un-migrated one by creating a nodes table with only (id, type, properties). Exercise the hsi-to-graph node-insert path against each. Assert no NOT NULL error on either schema and assert the provenance scalars on the migrated schema. This test MUST be RED before Task 2 (the helper does not exist yet and the bare insert still fails on the migrated fixture). Do not write to any real room; use only tmp fixtures. Keep all data scalar and local (Canon Part 8).</action>
  <verify>
    <automated>node --test lib/core/hsi-to-graph.test.cjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/hsi-to-graph.test.cjs exists and is a node:test suite (uses node:test + node:assert).
    - Before Task 2: `node --test lib/core/hsi-to-graph.test.cjs` FAILS on the migrated-schema case (RED), proving the test reproduces HARD-02.
    - The test never reads or writes any path under ~/MindrianRooms or any real room; only os.tmpdir fixtures.
    - No em-dashes in the file (hyphens only).
  </acceptance_criteria>
  <done>The HARD-02 regression test exists and is RED against the unfixed bare insert on a migrated room.db.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the shared NOT-NULL-safe insert helper and route all four sites through it (D-02 + D-02a)</name>
  <read_first>
    - lib/core/hsi-to-graph.test.cjs (the RED test from Task 1 -- the contract this task must turn GREEN)
    - lib/core/navigation/evidence-claim.cjs (the correct wide-insert column-set at :110 to copy -- RESEARCH Don't-Hand-Roll)
    - lib/core/lazygraph-ops.cjs (the 4 sibling insert sites at :357, :362, :729, :911 + openGraph)
    - scripts/hsi-to-graph.cjs (the :68-70 upsertNode + :116-117 Section upsert)
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-RESEARCH.md (Pitfall 2 -- the openGraph-vs-openRoomDb schema divergence)
  </read_first>
  <action>Create lib/core/node-insert.cjs exporting one function insertNode(conn, id, type, properties) (plus an optional provenance overrides arg). The helper must be both-schema safe per D-02a: run PRAGMA table_info(nodes) once to detect whether the migrated provenance columns (source_path, created_by, created_at, last_seen_at) exist, then build the correct INSERT column-set accordingly -- the wide NOT-NULL insert on the migrated schema (supplying source_path = 'system:hsi-to-graph', created_by = 'system', review_status DEFAULT or 'proposed', created_at = Date.now(), last_seen_at = Date.now()) and the legacy 3-column insert on the un-migrated schema. Preserve the existing ON CONFLICT(id) DO UPDATE upsert semantics. Route all four bare-3-column inserts through insertNode: the two in scripts/hsi-to-graph.cjs (the upsertNode prepared statement at :68-70 and the Section upsert at :116-117) and the three siblings in lib/core/lazygraph-ops.cjs (:357, :362, :729, :911 -- consolidate the indexArtifact helper and the two upserts). Do NOT change edge inserts. The created_by value MUST satisfy the Phase-109 CHECK constraint (one of user/larry/import/brain/system); use 'system' for these system-written nodes (Canon Part 9 system-bookkeeping, not a truth-claim). Keep the writer's data scalar and local (Canon Part 8 -- no artifact bytes, no file paths beyond the synthetic source_path handle).</action>
  <verify>
    <automated>node --test lib/core/hsi-to-graph.test.cjs 2>&1 | tail -20 && grep -l "node-insert" scripts/hsi-to-graph.cjs lib/core/lazygraph-ops.cjs</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/node-insert.cjs exists and exports insertNode.
    - `node --test lib/core/hsi-to-graph.test.cjs` PASSES (GREEN) -- all three behaviors (migrated, un-migrated, provenance) green.
    - `grep -c "node-insert" scripts/hsi-to-graph.cjs` returns at least 1 (the writer requires the helper).
    - `grep -c "node-insert" lib/core/lazygraph-ops.cjs` returns at least 1 (the siblings require the helper).
    - `grep -vE '^\s*//' lib/core/lazygraph-ops.cjs | grep -c "INSERT INTO nodes (id, type, properties) VALUES (?, ?, ?)"` returns 0 OUTSIDE the helper (no remaining bare-3-column literal insert in the siblings; the only such literal allowed is inside node-insert.cjs itself).
    - The PRAGMA table_info(nodes) column-detect path is present in node-insert.cjs (`grep -c "table_info" lib/core/node-insert.cjs` >= 1).
    - No em-dashes in any modified file.
  </acceptance_criteria>
  <done>One shared NOT-NULL-safe insert helper exists, all four sites route through it, both-schema safety proven by the GREEN regression test.</done>
</task>

<task type="auto">
  <name>Task 3: Unmask the scout HSI-to-graph write failure (D-03)</name>
  <read_first>
    - commands/scout.md (line 164: the `node "${PLUGIN_ROOT}/scripts/hsi-to-graph.cjs" "$ROOM_DIR" 2>/dev/null || true` swallow)
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-CONTEXT.md (D-03 -- a silent scout is how HARD-02 hid for weeks)
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-RESEARCH.md (Anti-Patterns: the 2>/dev/null || true masking + Pitfall on graceful degradation)
  </read_first>
  <action>In commands/scout.md at the line invoking scripts/hsi-to-graph.cjs (currently line 164), remove the `2>/dev/null || true` swallow so a write failure surfaces in the scout output. Keep the step non-fatal to the overall scout run (the scout should report a degraded HSI step rather than aborting the whole scout), but stderr from hsi-to-graph.cjs MUST reach the user/log instead of being discarded. Replace the silent swallow with a form that captures the exit status and prints a visible degraded-step line on failure (for example, run the node command, then on non-zero exit echo a clear "HSI-to-graph step failed" advisory that names the failing step) -- do NOT redirect stderr to /dev/null and do NOT mask the exit with a bare `|| true`. Because Task 2 fixes the schema defect, the expected normal path now succeeds; the unmask is the honesty guarantee for future failures (D-03).</action>
  <verify>
    <automated>grep -n "hsi-to-graph" commands/scout.md | grep -c "2>/dev/null" | grep -qx 0 && echo "SWALLOW REMOVED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "hsi-to-graph" commands/scout.md` shows the invocation no longer contains `2>/dev/null`.
    - The invocation no longer ends in a bare `|| true` that discards the exit status (a visible failure advisory is printed on non-zero exit instead).
    - The scout run still does not hard-abort on an HSI-to-graph failure (the step is reported as degraded, not fatal).
    - No em-dashes in commands/scout.md changes.
  </acceptance_criteria>
  <done>The scout HSI-to-graph step surfaces write failures instead of swallowing them, while remaining non-fatal to the scout run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HSI writer -> room.db | Local node/edge writes cross into the SQLite local graph. Untrusted only in the sense of schema-shape (migrated vs un-migrated). |
| scout step -> user/log | A swallowed failure is a repudiation/integrity risk (the system pretends success). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-01 | Repudiation / Integrity | commands/scout.md hsi-to-graph swallow | mitigate | Remove 2>/dev/null || true so write failures surface (D-03); HARD-02 hid for weeks behind this swallow |
| T-140-02 | Tampering | node-insert.cjs created_by value | mitigate | created_by fixed to 'system' (Phase-109 CHECK constraint); these are system-bookkeeping nodes (Canon Part 9), not human truth-claims |
| T-140-03 | Information Disclosure | HSI write payload | accept | source_path is a synthetic handle 'system:hsi-to-graph', not a real path; node properties stay scalar/local; zero Brain egress (Canon Part 8) |
| T-140-SC | Tampering | npm/pip/cargo installs | mitigate | N/A -- zero external packages added this plan (RESEARCH: Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `node --test lib/core/hsi-to-graph.test.cjs` passes (both schemas + provenance).
- All four insert sites route through lib/core/node-insert.cjs (grep confirms).
- commands/scout.md no longer swallows the HSI-to-graph stderr/exit.
- Canon Part 8: `grep -rE "fetch|http|curl|brain\.mindrian|tavily" lib/core/node-insert.cjs scripts/hsi-to-graph.cjs` returns zero network surface.
- No em-dashes across all modified files.
</verification>

<success_criteria>
- HARD-02 closed: HSI lands its edges in a migrated room.db with no NOT NULL constraint failure, and in an un-migrated room.db with no error (D-02a both-schema safe).
- The whole bug-class is fixed via one shared helper (D-02): four sites, one insertNode.
- The scout HSI-to-graph failure is no longer silent (D-03).
</success_criteria>

<output>
Create `.planning/phases/140-sentinel-and-instrumentation-hardening/140-01-SUMMARY.md` when done
</output>
