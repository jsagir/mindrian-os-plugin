---
phase: 140-sentinel-and-instrumentation-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/query-efficiency-telemetry.cjs
  - scripts/scout-telemetry-aggregator.cjs
  - scripts/sentinel-deadline-monitor
  - lib/memory/query-efficiency-telemetry.test.cjs
  - tests/test-deadline-monitor-planning-state.sh
autonomous: true
requirements: [HARD-04, HARD-05]
canon_parts: [Part 4, Part 7, Part 8]
must_haves:
  truths:
    - "The query-efficiency telemetry hook writes a JSONL line for a Read/Grep/Glob turn in a room even when no /mos: command context is set -- the gate measures ALL turns (HARD-04 / D-01)"
    - "The existing query-efficiency-telemetry.test.cjs still passes after the gate relaxation (Canon Part 7 reuse -- one test file, extended, not forked)"
    - "The scout-telemetry-aggregator can still report the /mos:-command-specific median so the published 'up to 57x' claim is measured on the same population it was defined against (D-01a -- the relaxed all-turns gate does not silently redefine the published number)"
    - "The deadline monitor reports a phase deadline read from .planning/STATE.md, not just funding/ + opportunity-bank/ -- a near phase deadline shows as DUE/URGENT, not CLEAR (HARD-05)"
  artifacts:
    - path: "scripts/query-efficiency-telemetry.cjs"
      provides: "Relaxed all-turns gate (no /mos: context required) preserving Part-8 scalar-only invariants"
      contains: "exitSilent"
    - path: "scripts/scout-telemetry-aggregator.cjs"
      provides: "Command-population filter so the 57x claim stays measurable on /mos: turns"
      contains: "command"
    - path: "scripts/sentinel-deadline-monitor"
      provides: "A .planning/STATE.md phase-deadline scan branch"
      contains: "STATE.md"
    - path: "lib/memory/query-efficiency-telemetry.test.cjs"
      provides: "Extended HARD-04 regression -- a JSONL line is written for an all-turns (no-/mos:-context) turn"
      contains: "all-turns"
    - path: "tests/test-deadline-monitor-planning-state.sh"
      provides: "HARD-05 .planning/STATE.md phase-deadline regression smoke test"
      contains: "STATE.md"
  key_links:
    - from: "lib/memory/query-efficiency-telemetry.test.cjs"
      to: "scripts/query-efficiency-telemetry.cjs"
      via: "invokes the hook with no command context and asserts a JSONL line lands"
      pattern: "query-efficiency-telemetry"
    - from: "tests/test-deadline-monitor-planning-state.sh"
      to: "scripts/sentinel-deadline-monitor"
      via: "invokes the monitor against a fixture with a .planning/STATE.md phase deadline"
      pattern: "sentinel-deadline-monitor"
    - from: "scripts/scout-telemetry-aggregator.cjs"
      to: "scripts/query-efficiency-telemetry.cjs"
      via: "reads the JSONL command field to filter the published-claim population"
      pattern: "command"
---

<objective>
Fix HARD-04 (the telemetry hook logs 0 events because it gates on a /mos: command context nothing sets) and HARD-05 (the deadline monitor never reads .planning/STATE.md phase deadlines). HARD-04 per D-01: RELAX THE GATE to measure ALL turns (not only /mos: command turns) while preserving the hook's Canon Part-8 scalar-only LOCAL invariant. D-01a: the relaxed all-turns denominator changes what the published "up to 57x" efficiency claim is measured against -- the aggregator must remain able to report the /mos:-command-specific population so the published number is not silently redefined, and a reconciliation note must be produced. HARD-05: add a .planning/STATE.md phase-deadline scan branch to the deadline monitor, reusing the script's own frontmatter-extraction idiom.

Purpose: The 57x-claim release gate needs real telemetry data, and the deadline monitor must surface phase deadlines (the NATO 2026-06-01 example was invisible while it reported CLEAR). Both feed a safe scheduled scout (Phase 145).

Output: relaxed all-turns gate, an aggregator command-population filter + reconciliation note (D-01a), a .planning/STATE.md scan branch in the deadline monitor, an EXTENDED lib/memory/query-efficiency-telemetry.test.cjs, and a new HARD-05 bash smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-sentinel-and-instrumentation-hardening/140-RESEARCH.md
@.planning/phases/140-sentinel-and-instrumentation-hardening/140-CONTEXT.md

<interfaces>
<!-- HARD-04 hook gate (scripts/query-efficiency-telemetry.cjs:285-286 region): -->
<!--   const tool = extractToolName(payload); if (!tool || !/^(Read|Grep|Glob)$/.test(tool)) return exitSilent();  // KEEP -->
<!--   const command = extractSlashCommand(payload); if (!command) return exitSilent();  // <-- THE BLOCKING GATE (relax this) -->
<!--   const roomRoot = resolveRoomRoot(); if (!roomRoot) return exitSilent();  // KEEP -->
<!--   if (tokensUsed <= 0) return exitSilent();  // KEEP -->
<!-- The JSONL event (line ~317-325) carries: event, ts, command, tool, tokens_used, tokens_naive_estimate, ratio, room_slug. -->
<!--   command is currently the /mos: handle; after relaxation it is null for non-/mos: turns. -->
<!-- D-01: measure ALL turns. Minimal correct fix: DO NOT exitSilent when command is absent; let command be null/'' and still write the line. -->
<!--   Preserve the tool gate, the room gate, the tokensUsed>0 gate, and the scalar-only + LOCAL-slug-only invariant (Canon Part 8; Pitfall 4 -- add NO new fields that could carry user strings). -->

<!-- D-01a 57x-claim surface: -->
<!--   scripts/scout-telemetry-aggregator.cjs RELEASE_GATE_THRESHOLD_X = 40 (PASS bar); reports median ratio + top-5 commands by ratio. -->
<!--   The "up to 57x" claim (CHANGELOG ~2052-2057) was /mos:-command-specific. -->
<!--   Relaxing to all-turns adds cheap Reads with low naive-baseline -> shifts the median population. -->
<!--   The aggregator already reads the per-record command field; add a way to filter to records whose command starts with '/mos:' -->
<!--   so the published claim stays measured on the same population. -->

<!-- HARD-05 deadline monitor (scripts/sentinel-deadline-monitor): -->
<!--   :9  ROOM_DIR="${1:-./room}"  :10 ALERT_DAYS="${2:-7}"  :18 portable_date_to_epoch()  :31 TODAY_EPOCH -->
<!--   :42 scans funding/*/STATUS.md   :84 scans opportunity-bank/*.md   (the only two scan targets today) -->
<!--   Existing extraction idiom (REUSE per Don't-Hand-Roll): -->
<!--     deadline=$(grep -m1 '^deadline:' "$f" | sed 's/deadline:[[:space:]]*//' | tr -d '"' | xargs) -->
<!--     deadline_epoch=$(portable_date_to_epoch "$deadline"); [ "$deadline_epoch" -eq 0 ] && continue  (V5 input-validation guard, RESEARCH Security) -->
<!--     days_until=$(( (deadline_epoch - TODAY_EPOCH)/86400 )); classify OVERDUE/URGENT/UPCOMING vs ALERT_DAYS -->
<!-- CONFIRMED phase-deadline field in .planning/STATE.md: the prose line `Hard deadline: 2026-06-01 (NATO ...)` (and a sibling `Soft deadline:` line). -->
<!--   .planning/STATE.md is gitignored and lives at REPO ROOT, NOT under ROOM_DIR (Pitfall 5: two different STATE.md files). -->
<!--   The scan branch must resolve .planning/STATE.md relative to the repo root (or an explicit path), extract the date token after `Hard deadline:` (and `Soft deadline:` when present), reuse portable_date_to_epoch + the epoch==0 skip guard, and emit a phase alert through the same alerts[] classification path. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relax the telemetry gate to all turns + extend the existing test (HARD-04 / D-01)</name>
  <read_first>
    - scripts/query-efficiency-telemetry.cjs (the gate chain at :280-296, extractSlashCommand at :164-187, the event build at :317-325, exitSilent at :99, the Part-8 invariant comment at :25-51)
    - lib/memory/query-efficiency-telemetry.test.cjs (the EXISTING test to EXTEND -- Canon Part 7 reuse; do NOT create a parallel tests/query-efficiency-telemetry.test.cjs)
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-RESEARCH.md (HARD-04 row + Pitfall 4 -- keep scalar-only/LOCAL-only; the relaxed gate must not over-correct into carrying user strings)
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-CONTEXT.md (D-01 -- RELAX THE GATE, measure all turns)
  </read_first>
  <behavior>
    - Test (extend lib/memory/query-efficiency-telemetry.test.cjs): invoke the hook with a Read/Grep/Glob payload in a resolvable room with NO /mos: command context, and assert a JSONL line IS written (the all-turns case). This is the new HARD-04 regression -- before the fix the hook exitSilent()s and writes nothing.
    - Test (preserve): the existing assertions (a /mos:-context turn still logs; the non-Read/Grep/Glob tool still exits silent; no-room still exits silent; tokensUsed<=0 still exits silent) MUST stay green.
    - Test (Part-8): assert the written JSONL line carries ONLY the existing scalar fields (event, ts, command, tool, tokens_used, tokens_naive_estimate, ratio, room_slug) and no new field; command may be null/empty for the all-turns case.
  </behavior>
  <action>In scripts/query-efficiency-telemetry.cjs, relax the command gate per D-01: remove the `if (!command) return exitSilent();` early-return so a turn with no /mos: context still proceeds to write the JSONL line (measure ALL turns). Keep the tool gate (Read|Grep|Glob), the room gate (resolveRoomRoot), and the tokensUsed>0 gate -- those stay. Keep extractSlashCommand and write whatever it returns into the event.command field (null/'' when no /mos: context); add NO new fields and change no other field, preserving the scalar-only + LOCAL-slug-only Canon Part 8 invariant (Pitfall 4 -- do not let relaxation introduce any user-string-carrying field). Then EXTEND the existing lib/memory/query-efficiency-telemetry.test.cjs (do NOT create a new parallel test file -- Canon Part 7) with the all-turns assertion and the Part-8 field-set assertion described in <behavior>. The all-turns test MUST be RED before the source edit (the hook currently exits silent with no command).</action>
  <verify>
    <automated>node --test lib/memory/query-efficiency-telemetry.test.cjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `node --test lib/memory/query-efficiency-telemetry.test.cjs` passes ALL cases including the new all-turns case (GREEN).
    - scripts/query-efficiency-telemetry.cjs no longer early-returns exitSilent solely because the /mos: command context is absent: `grep -n "if (!command) return exitSilent" scripts/query-efficiency-telemetry.cjs` returns nothing.
    - The tool gate, room gate, and tokensUsed>0 gate are all still present (grep confirms each exitSilent guard remains).
    - No new field added to the JSONL event object beyond the existing eight (Part-8 invariant held): the event literal still lists only event/ts/command/tool/tokens_used/tokens_naive_estimate/ratio/room_slug.
    - `grep -rE "fetch|http|curl|brain\.mindrian|tavily" scripts/query-efficiency-telemetry.cjs` returns zero network surface (Canon Part 8).
    - Only ONE query-efficiency-telemetry.test.cjs exists (the lib/memory one); no parallel tests/ copy was created.
    - No em-dashes in modified files.
  </acceptance_criteria>
  <done>The hook measures all turns and writes JSONL for a no-/mos:-context Read/Grep/Glob turn; the existing test is extended and green; Part-8 invariant held (HARD-04 / D-01 closed).</done>
</task>

<task type="auto">
  <name>Task 2: Preserve the 57x-claim population in the aggregator + reconciliation note (D-01a)</name>
  <read_first>
    - scripts/scout-telemetry-aggregator.cjs (parseArgv at :53, the JSONL reader at :78-99, filterWindow at :103, the median/top-5 aggregation, RELEASE_GATE_THRESHOLD_X = 40 at :48, the report at :147-190)
    - scripts/query-efficiency-telemetry.cjs (the JSONL command field semantics after Task 1 -- null for non-/mos: turns)
    - CHANGELOG.md (the "57x claim defensibility gate" entry around line 2052 -- the published-claim measurement surface)
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-CONTEXT.md (D-01a -- do not let a relaxed gate quietly redefine a published number; Deferred Ideas -- claim language reconciliation is a release/docs concern, not a code task)
  </read_first>
  <action>In scripts/scout-telemetry-aggregator.cjs add a command-population filter so the published "up to 57x" claim can still be measured on the SAME population it was defined against (/mos: command turns). Add a flag (for example --mos-only) that, when set, restricts the records considered for the median/top-5/threshold to those whose command field starts with '/mos:'; when the flag is absent, the default keeps reporting all turns (the new relaxed denominator) so both views are available. Do NOT change RELEASE_GATE_THRESHOLD_X. Then produce the D-01a reconciliation: run the aggregator both ways against whatever telemetry exists (if the JSONL is empty/NO_DATA on this box, state that explicitly), and write a short reconciliation note to .planning/phases/140-sentinel-and-instrumentation-hardening/140-57X-CLAIM-RECONCILIATION.md (git add -f, since .planning is gitignored) recording: (a) the all-turns median vs the /mos:-only median (or NO_DATA if the JSONL is empty), (b) whether the relaxed denominator materially changes the published "up to 57x" number, and (c) a flag to the release process if the claim language needs updating (the actual claim-language change is DEFERRED per CONTEXT Deferred Ideas -- this task only surfaces the need, it does not rewrite README/CHANGELOG copy). No em-dashes.</action>
  <verify>
    <automated>node scripts/scout-telemetry-aggregator.cjs --mos-only 2>&1 | tail -8; echo "exit=$?"; test -f .planning/phases/140-sentinel-and-instrumentation-hardening/140-57X-CLAIM-RECONCILIATION.md && echo "NOTE EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/scout-telemetry-aggregator.cjs --mos-only` runs without error and restricts the population to /mos: command records (or reports NO_DATA cleanly when the JSONL is empty).
    - The default invocation (no flag) still reports the all-turns population (relaxed denominator) so both views exist.
    - RELEASE_GATE_THRESHOLD_X is unchanged at 40.
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-57X-CLAIM-RECONCILIATION.md exists, committed with git add -f, and records the all-turns vs /mos:-only comparison (or explicit NO_DATA) plus a release-process flag if claim language needs reconciling.
    - The note does NOT rewrite README/CHANGELOG "up to 57x" copy (that reconciliation is DEFERRED per CONTEXT); it only surfaces the need.
    - No em-dashes in modified files or the note.
  </acceptance_criteria>
  <done>The aggregator can report the /mos:-only population so the published 57x claim stays measurable on its original denominator; the D-01a reconciliation note exists and flags any release-process follow-up (no silent redefinition of the published number).</done>
</task>

<task type="auto">
  <name>Task 3: Add the .planning/STATE.md phase-deadline scan branch to the deadline monitor (HARD-05)</name>
  <read_first>
    - scripts/sentinel-deadline-monitor (ROOM_DIR/ALERT_DAYS at :9-10, portable_date_to_epoch at :18, TODAY_EPOCH at :31, the funding scan at :42, the opportunity-bank scan at :84, the alerts[] classification + the epoch==0 skip guard)
    - .planning/STATE.md (the confirmed phase-deadline lines: `Hard deadline: 2026-06-01 (NATO ...)` at line 232 and `Soft deadline:` at line 233 -- this is the field to extract; STATE.md is at REPO ROOT, gitignored, NOT under ROOM_DIR)
    - .planning/phases/140-sentinel-and-instrumentation-hardening/140-RESEARCH.md (HARD-05 row + Pitfall 5 -- two STATE.md files, resolve .planning/STATE.md at repo root; Don't-Hand-Roll -- reuse the script's own extraction idiom; Security V5 -- reuse the epoch==0 skip guard for malformed dates)
    - tests/test-deadline-monitor-planning-state.sh (built in this task; see <action>)
  </read_first>
  <action>Add a scan branch to scripts/sentinel-deadline-monitor that reads phase deadlines from .planning/STATE.md (resolved relative to the repo root -- the plugin-dev dogfood STATE.md per Canon Part 6, NOT a user room's STATE.md; Pitfall 5). Extract the date token from the `Hard deadline:` line (and the `Soft deadline:` line when it carries a real date, skipping the `--` placeholder) using the script's existing idiom adapted to the prose form: grep the line, strip the label, strip trailing parenthetical context, trim, then feed portable_date_to_epoch and apply the existing `[ "$epoch" -eq 0 ] && continue` guard (V5 input-validation; malformed/placeholder dates are skipped, not fatal). Classify via the same days_until vs ALERT_DAYS logic and push a phase alert into the same alerts[] array with a source label of "phase" (mirroring the "funding"/"opportunity" source labels) so it renders through the existing report path. Make the branch resilient when .planning/STATE.md is absent (skip silently -- a user room without the plugin-dev .planning/ must not error). Also build tests/test-deadline-monitor-planning-state.sh: a bash smoke harness that constructs a fixture with a .planning/STATE.md carrying a near-future `Hard deadline:` line, invokes the monitor, and asserts the deadline is reported as DUE/URGENT/UPCOMING (not CLEAR). The test MUST be RED before the source edit.</action>
  <verify>
    <automated>bash tests/test-deadline-monitor-planning-state.sh && echo "HARD-05 GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - Before the source edit: `bash tests/test-deadline-monitor-planning-state.sh` exits non-zero (RED -- the monitor reports CLEAR while a near phase deadline exists).
    - After the source edit: `bash tests/test-deadline-monitor-planning-state.sh` exits 0 (GREEN -- the .planning/STATE.md phase deadline surfaces as DUE/URGENT/UPCOMING).
    - `grep -c "STATE.md" scripts/sentinel-deadline-monitor` returns >= 1 (the scan branch references .planning/STATE.md).
    - The branch reuses portable_date_to_epoch and the `[ "$epoch" -eq 0 ]` skip guard (no bespoke date parser; Don't-Hand-Roll + V5).
    - When .planning/STATE.md is absent the monitor still exits 0 (a user room without plugin-dev .planning/ does not error).
    - The phase alert renders through the existing alerts[] report path with a distinct source label (not silently merged into funding/opportunity).
    - No em-dashes in modified files or the test.
  </acceptance_criteria>
  <done>The deadline monitor reports .planning/STATE.md phase deadlines through its existing alert path; a near phase deadline shows as DUE, not CLEAR (HARD-05 closed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostToolUse payload -> telemetry JSONL | Tool/command/token data crosses into the LOCAL JSONL. Part-8 boundary: must stay scalar + LOCAL-slug only. |
| telemetry JSONL -> 57x published claim | The measurement population backs a public number; changing the denominator silently is an integrity risk (D-01a). |
| .planning/STATE.md date -> deadline arithmetic | Untrusted/placeholder date strings cross into bash epoch arithmetic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-06 | Information Disclosure | relaxed telemetry gate (HARD-04) | mitigate | Keep the JSONL scalar-only + LOCAL-slug-only; add NO new field; zero network surface (Canon Part 8, Pitfall 4) |
| T-140-07 | Repudiation / Integrity | 57x published claim denominator (D-01a) | mitigate | Aggregator retains a /mos:-only population view; reconciliation note flags any release-process follow-up so the published number is not silently redefined |
| T-140-08 | Denial of Service | malformed/placeholder date in .planning/STATE.md (HARD-05) | mitigate | Reuse the epoch==0 skip guard (V5 input validation) -- malformed dates are skipped, not fatal; absent STATE.md skips silently |
| T-140-SC | Tampering | npm/pip/cargo installs | mitigate | N/A -- zero external packages added (Node CJS + bash edits to shipped scripts) |
</threat_model>

<verification>
- `node --test lib/memory/query-efficiency-telemetry.test.cjs` passes (all-turns + preserved cases + Part-8 field-set).
- `node scripts/scout-telemetry-aggregator.cjs --mos-only` runs; the D-01a reconciliation note exists.
- `bash tests/test-deadline-monitor-planning-state.sh` passes.
- Canon Part 8: `grep -rE "fetch|http|curl|brain\.mindrian|tavily" scripts/query-efficiency-telemetry.cjs scripts/scout-telemetry-aggregator.cjs` returns zero network surface.
- No em-dashes across all modified files.
</verification>

<success_criteria>
- HARD-04 closed: the hook measures all turns and writes JSONL for a no-/mos:-context turn (D-01), preserving Part-8 scalar-only invariants.
- D-01a satisfied: the aggregator keeps the /mos:-only population view and a reconciliation note documents the denominator change (no silent redefinition of the published 57x number).
- HARD-05 closed: the deadline monitor surfaces .planning/STATE.md phase deadlines as DUE, not CLEAR.
</success_criteria>

<output>
Create `.planning/phases/140-sentinel-and-instrumentation-hardening/140-03-SUMMARY.md` when done
</output>
