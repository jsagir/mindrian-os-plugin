---
phase: 131-research-as-graph-aware-workflow
plan: 04
type: tdd
wave: 3
depends_on: ["131-01", "131-03"]
files_modified:
  - lib/core/findings-wirer.cjs
  - lib/core/research-filing-selector.cjs
  - tests/test-131-findings-wirer.cjs
autonomous: true
requirements: [RES-131-WIRER, RES-131-FWD-EDGES, RES-131-CORRELATION, RES-131-FWD-SELECTOR, RES-131-REJECT-AS-DATA]
must_haves:
  truths:
    - "An ACCEPT writes an EvidenceClaim node (review_status proposed) + an INFORMS edge through navigation.cjs; CONTRADICTS if the finding kills an existing claim; SUPERSEDES if a better evidence tier"
    - "A REJECT writes a REJECTED_BECAUSE edge carrying the captured reason (rejection IS data per Canon Part 4)"
    - "A DEFER writes a research_deferred memory_event queued to milestone audit"
    - "Every cascade-edge target is a canonical correlation_id from Phase 130.7, never a raw name"
    - "The F.1 filing selector requires lib/hmi/selector-dispatcher.cjs (it is NOT a bespoke selector) so Phase 136's gate widget is a strict superset"
  artifacts:
    - path: "lib/core/findings-wirer.cjs"
      provides: "Stage 7 typed-node + cascade-edge writer on correlation_ids"
      contains: "writeEvidenceClaim"
    - path: "lib/core/research-filing-selector.cjs"
      provides: "Stage 6 F.1 filing selector mirroring selector-dispatcher"
      contains: "selector-dispatcher"
  key_links:
    - from: "lib/core/findings-wirer.cjs"
      to: "navigation.writeEdge"
      via: "INFORMS / CONTRADICTS / SUPERSEDES / REJECTED_BECAUSE on correlation_ids"
      pattern: "writeEdge"
    - from: "lib/core/research-filing-selector.cjs"
      to: "lib/hmi/selector-dispatcher.cjs"
      via: "dispatchShapeFSubShape F.1"
      pattern: "selector-dispatcher"
---

<objective>
Build the Stage 6 F.1 filing selector and the Stage 7 findings-wirer. The selector mirrors
the shipped `lib/hmi/selector-dispatcher.cjs` F.1 sub-shape (it is NOT a bespoke research
selector) with computed candidate target sections (% match) per finding -- the LOCKED
forward contract that makes Phase 136's richer gate widget a strict superset. The wirer
turns each gate decision into typed graph data through navigation.cjs:
- ACCEPT -> writeEvidenceClaim (proposed) + INFORMS edge (+ CONTRADICTS / SUPERSEDES when applicable) + research_filed event
- REJECT -> REJECTED_BECAUSE edge with the captured reason + research_rejected event
- DEFER  -> research_deferred event queued to milestone audit

RE-BASELINE HARD CONSTRAINTS:
- Cascade-edge targets are canonical correlation_ids (Phase 130.7), never raw names.
- The F.1 selector mirrors selector-dispatcher (gate-as-write-node forward contract for 136).
- EvidenceClaim node schema + the four cascade predicates are the LOCKED Phase 136 contracts.

Output: research-filing-selector.cjs + findings-wirer.cjs + the suite (already registered
in run-all-131.sh by Plan 01; this plan only CREATES the suite file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/131-research-as-graph-aware-workflow/131-CONTEXT.md
@.planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-CONTEXT.md
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Contracts this plan consumes. -->

Plan 01 (navigation.cjs): writeEvidenceClaim(db, { topic, source, url, retrieved_at,
evidence_tier, summary, sessionId }) -> { ok, node_id }; the four cascade predicates
INFORMS / CONTRADICTS / SUPERSEDES / REJECTED_BECAUSE are ALLOWED_EDGE_TYPES members;
research_filed / research_rejected / research_deferred are EVENT_TYPES members.
writeEdge(db, { source_id, target_id, edge_type, properties }) with enum-only properties.
logMemoryEvent(db, eventType, payload).

Plan 03 (source-lens-driver): runSourceLens returns top-5 findings each with
{ source, url, retrieved_at, evidence_tier, relevance, summary, title }.

Phase 130.7 (a hard dependency that lands BEFORE 131; see depends_on):
lib/core/correlation.cjs (or lib/brain/chain-recommender.cjs return tuples) resolves a
{ correlation_id, canonical_name, primary_label } per target. The wirer resolves a target
SECTION / claim to its correlation_id BEFORE writing any cascade edge, so edges land on
canonical targets, not cross-label duplicates. correlation_id is name-based / embedding-
independent (stable hash of canonical_name + primary_label). If 130.7 is absent at execution
time, the require fails loudly (the dependency is honored, not stubbed).

The F.1 selector (lib/hmi/selector-dispatcher.cjs, Phase 88.2):
dispatchShapeFSubShape('F.1', { tier, mode, payload: { verbs, header, recommendedVerb } })
-> { shape:'F.1', rendered }. The research filing selector MUST route through this (NO
bespoke selector). The five filing options map to the closed verb vocabulary; the recommend
marker only renders in Mode A at confidence >= 0.7 (Phase 88.2 invariant). Per the 4.8
re-baseline (open-decision 1 re-resolved): pre-fill ONE confident recommendation rather than
a generic prompt, but keep the human gate (Canon Part 9 role 5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED suite for the wirer + the F.1 filing selector</name>
  <files>tests/test-131-findings-wirer.cjs</files>
  <read_first>
    - tests/test-131-substrate.cjs (the Plan 01 seed + writeEvidenceClaim contract; the edge-FK anchor-node seeding pattern so edge writes do not fail on FOREIGN KEY)
    - lib/hmi/selector-dispatcher.cjs (dispatchShapeFSubShape F.1 return shape; the closed-verb contract)
    - .planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-CONTEXT.md (the correlation_id resolution contract the edge targets use)
  </read_first>
  <behavior>
    - Test: buildFilingSelector(finding, candidateSections) returns a Shape F.1 envelope (shape 'F.1') produced via selector-dispatcher.dispatchShapeFSubShape -- the verbs include File-to-primary / File-to-secondary / Split / Defer / Reject; the primary carries the highest % match.
    - Test: a source-grep proves research-filing-selector.cjs requires lib/hmi/selector-dispatcher.cjs (NOT a bespoke selector) -- the gate-as-write-node forward contract.
    - Test: wireAccept writes an EvidenceClaim node (review_status proposed) AND an INFORMS edge whose target is the resolved correlation_id (NOT the raw section name); a research_filed event is emitted carrying url + retrieved_at + evidence_tier.
    - Test: wireAccept writes a CONTRADICTS edge when the decision flags kills_claim, and a SUPERSEDES edge when the decision flags better_tier_than an existing claim.
    - Test: wireReject writes EXACTLY ONE REJECTED_BECAUSE edge carrying the captured reason scalar + a research_rejected event; ZERO INFORMS edges for the rejected finding.
    - Test: wireDefer writes a research_deferred memory_event (queued to milestone audit) and NO cascade edge.
    - Test: every cascade-edge target written is a correlation_id (assert the target matches the correlation_id resolver output, never the raw name).
  </behavior>
  <action>Write the RED behavior suite (CREATE tests/test-131-findings-wirer.cjs; do NOT touch run-all-131.sh -- Plan 01 already pre-registered this suite). Seed a room (room + 2 sections + the FK anchor nodes the INFORMS / CONTRADICTS / SUPERSEDES / REJECTED_BECAUSE edges reference -- pre-empt the recurring FOREIGN KEY incident by seeding the correlation_id-keyed target nodes, mirroring the 130-04 seed). Inject a correlation_id resolver stub (mapping section name -> { correlation_id, canonical_name, primary_label }) via an opts seam so the suite proves the wirer resolves to a correlation_id before writing. Assert each gate path (accept/reject/defer) writes the right node + edge + event through navigation. The suite MUST fail (both modules absent).</action>
  <verify>
    <automated>node tests/test-131-findings-wirer.cjs; test $? -ne 0 && echo "RED OK" || (echo "should fail RED" && exit 1)</automated>
  </verify>
  <done>The suite runs and FAILS (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: research-filing-selector.cjs (Stage 6, mirrors selector-dispatcher)</name>
  <files>lib/core/research-filing-selector.cjs</files>
  <read_first>
    - lib/hmi/selector-dispatcher.cjs (dispatchShapeFSubShape; the F.1 payload contract; the Mode A/B/Tier-0 + recommend>=0.7 invariant)
    - docs/MINDRIAN-CANON.md Part 3 (the F.1 Next Move sub-shape; the 10-verb closed vocabulary; the tri-context gate)
    - .planning/phases/131-research-as-graph-aware-workflow/131-CONTEXT.md (Stage 6 options; the LOCKED F.1-selector-equals-gate-widget forward contract)
  </read_first>
  <behavior>
    - Test (from Task 1): buildFilingSelector returns a Shape F.1 envelope via the dispatcher; verbs cover File-primary / File-secondary / Split / Defer / Reject; primary carries the highest % match; requires selector-dispatcher.cjs.
  </behavior>
  <action>Create lib/core/research-filing-selector.cjs. It requires lib/hmi/selector-dispatcher.cjs and produces the Stage 6 filing gate by calling dispatchShapeFSubShape('F.1', { tier, mode, payload }) -- it does NOT build a bespoke selector (the LOCKED forward contract so Phase 136's multi-select gate is a strict superset). Export buildFilingSelector(finding, candidateSections, opts) where candidateSections is the [{ section, match_pct, correlation_id }] list (sorted descending by match_pct). Compose the payload.verbs from the five filing options (File to primary section N% match / File to secondary section M% match / Split: primary + reference / Defer to milestone audit / Reject) and set payload.recommendedVerb to the File-to-primary option when mode is A and the primary match_pct clears the 0.7 confidence gate (the 4.8 re-baseline: a confident pre-filled recommendation, not a generic prompt; still human-confirmed). Return the dispatcher envelope plus a parsed { options } map the wirer consumes. Defensive: empty candidateSections degrades to a Defer/Reject-only selector. NO bespoke AskUserQuestion construction. NO em-dashes. NO fenced code.</action>
  <verify>
    <automated>node tests/test-131-findings-wirer.cjs 2>&1 | grep -i "selector" && node -e "const s=require('fs').readFileSync('lib/core/research-filing-selector.cjs','utf8'); process.exit(/selector-dispatcher/.test(s)?0:1)"</automated>
  </verify>
  <done>buildFilingSelector returns a Shape F.1 envelope via selector-dispatcher; the selector-requires-dispatcher test passes; no bespoke selector.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: findings-wirer.cjs (Stage 7, cascade edges on correlation_ids)</name>
  <files>lib/core/findings-wirer.cjs</files>
  <read_first>
    - lib/core/navigation.cjs (writeEvidenceClaim + writeEdge + logMemoryEvent + confirmNode + resolveByUser re-exports; the chokepoint-only contract)
    - lib/core/lens-engine.cjs applyAccept / applyReject (the existing accept-writes-INFORMS / reject-writes-REJECTED_BECAUSE idiom to mirror for the wirer)
    - .planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-CONTEXT.md (correlation_id resolution -- the edge target MUST be a correlation_id)
    - docs/MINDRIAN-CANON.md Part 4 (every choice is graph data; rejection-as-data) + Part 9 (proposed, not confirmed; the human gate)
  </read_first>
  <behavior>
    - Test (from Task 1): accept writes EvidenceClaim (proposed) + INFORMS (+ CONTRADICTS / SUPERSEDES on flags) on the correlation_id target + research_filed; reject writes exactly one REJECTED_BECAUSE + research_rejected, zero INFORMS; defer writes research_deferred, no edge; every edge target is a correlation_id.
  </behavior>
  <action>Create lib/core/findings-wirer.cjs. It requires lib/core/navigation.cjs ONLY for room.db (Canon Part 9; NOT substrate-allow-listed -- a check-substrate verify proves no room-db.cjs / node:sqlite require) and lib/core/correlation.cjs (Phase 130.7) to resolve a target section / claim to its { correlation_id, canonical_name, primary_label } BEFORE any edge write. Export three functions taking a caller-owned db handle:
- wireAccept(db, { finding, decision, roomDir, sessionId, _resolveCorrelation }): writeEvidenceClaim (review_status proposed -- NEVER confirmed; promotion is the human APPROVE at the gate, but the wirer only files the proposed node + edge), then resolve decision.target_section to a correlation_id and writeEdge INFORMS from the EvidenceClaim node to that correlation_id; if decision.kills_claim, also writeEdge CONTRADICTS to the killed claim's correlation_id; if decision.better_tier_than is set, writeEdge SUPERSEDES to that claim's correlation_id (edge properties enum-only: a reason scalar + the evidence_tier). Emit research_filed with provenance (url + retrieved_at + evidence_tier + node_id). For a Split decision, file the primary INFORMS and a reference INFORMS to the secondary correlation_id.
- wireReject(db, { finding, reason, roomDir, _resolveCorrelation }): resolve the target correlation_id and writeEdge REJECTED_BECAUSE carrying the captured reason scalar (rejection IS data per Part 4; per open-decision 4 RESOLVED, retain the source url + retrieved_at on the edge properties as enum/scalar provenance); emit research_rejected (teaches the next run's dedup). Write ZERO INFORMS edges.
- wireDefer(db, { finding, roomDir }): emit a research_deferred memory_event queued to milestone audit; write NO cascade edge.
Every edge target MUST be a correlation_id (never a raw name) -- centralize this in a single resolveTarget(section, _resolveCorrelation) helper so the invariant is enforced in one place. _resolveCorrelation defaults to correlation.resolve and is overridable for tests. Defensive: an edge-write failure returns { ok:false, reason } and never throws. NO em-dashes. NO fenced code.</action>
  <verify>
    <automated>node tests/test-131-findings-wirer.cjs && node scripts/check-substrate.cjs --baseline | grep "findings-wirer" && echo "UNEXPECTED bypass" && exit 1 || echo "green + clean"</automated>
  </verify>
  <done>test-131-findings-wirer.cjs is GREEN; accept/reject/defer write the right node + edge + event; every cascade edge lands on a correlation_id; the wirer passes the substrate guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate decision -> room.db | only via navigation.cjs; a confirmed truth-claim must not be written silently |
| edge target -> graph | must be a canonical correlation_id, not a forkable raw name |
| reason / provenance -> edge properties | enum/scalar only (Part 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-04-01 | Elevation of privilege | wirer writing a confirmed EvidenceClaim without the human gate (Part 9 role 5) | mitigate | wireAccept writes review_status proposed ONLY; promotion to confirmed flows through the F.1 gate -> navigation.confirmNode + resolveByUser (no agent-confirmed truth-claim); Task 1 test asserts proposed |
| T-131-04-02 | Tampering | a cascade edge to a non-canonical (raw-name) target forking the graph | mitigate | resolveTarget forces every edge target through the 130.7 correlation_id resolver; Task 1 test asserts the target equals the correlation_id, never the raw name |
| T-131-04-03 | Repudiation | a silent rejection losing the reason (Part 4 failure mode) | mitigate | wireReject requires a captured reason and writes it on the REJECTED_BECAUSE edge + research_rejected event; the F.1 selector makes reason-capture low-friction |
| T-131-04-04 | Information disclosure | freeform user content on an edge / event payload | mitigate | edge properties + event payloads are enum/scalar + node-id handles + provenance scalars; finding prose lives on the EvidenceClaim node, never on the edge |
| T-131-04-SC | Tampering | npm/pip/cargo installs | accept | zero dependencies added (consumes navigation + selector-dispatcher + correlation, all in-repo) |
</threat_model>

<verification>
- node tests/run-all-131.sh -> substrate + extractor + driver + findings-wirer suites PASS
- node scripts/check-substrate.cjs --baseline -> clean on findings-wirer.cjs + research-filing-selector.cjs
- node -e "require('fs').readFileSync('lib/core/research-filing-selector.cjs','utf8').includes('selector-dispatcher')" -> true (no bespoke selector)
- grep -nP "\x{2014}" lib/core/findings-wirer.cjs lib/core/research-filing-selector.cjs -> zero em-dashes
</verification>

<success_criteria>
- ACCEPT writes EvidenceClaim (proposed) + INFORMS / CONTRADICTS / SUPERSEDES on correlation_ids + research_filed
- REJECT writes REJECTED_BECAUSE + reason + research_rejected; zero INFORMS
- DEFER writes research_deferred; no edge
- The F.1 selector mirrors selector-dispatcher (gate-as-write-node forward contract for 136)
- Every cascade-edge target is a canonical correlation_id
</success_criteria>

<output>
Create `.planning/phases/131-research-as-graph-aware-workflow/131-04-SUMMARY.md` when done
</output>
