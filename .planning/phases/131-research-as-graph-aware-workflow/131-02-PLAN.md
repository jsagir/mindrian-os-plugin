---
phase: 131-research-as-graph-aware-workflow
plan: 02
type: tdd
wave: 2
depends_on: ["131-01"]
files_modified:
  - lib/core/research-context-extractor.cjs
  - tests/test-131-context-extractor.cjs
autonomous: true
requirements: [RES-131-EXTRACTOR, RES-131-PREFLIGHT, RES-131-LENSSET, RES-131-CONTEXTSUMMARY]
must_haves:
  truths:
    - "The extractor produces a one-paragraph Larry-voiced context summary AND a weighted source-lens set in ONE pre-flight+plan pass (Stages 2+3 collapsed per the 4.8 re-baseline)"
    - "The lens set is computed from the room context (section gap / JTBD / persona role_blend), not hardcoded"
    - "Every room.db read routes through navigation.cjs getResearchPreflight; the extractor opens no db handle of its own outside the chokepoint"
  artifacts:
    - path: "lib/core/research-context-extractor.cjs"
      provides: "Stage 1+2+3 pre-flight + context summary + weighted lens set"
      contains: "computeLensSet"
  key_links:
    - from: "lib/core/research-context-extractor.cjs"
      to: "navigation.getResearchPreflight"
      via: "single batched call"
      pattern: "getResearchPreflight"
---

<objective>
Build the Stage 1+2+3 pre-flight+plan extractor: it calls the batched
`navigation.getResearchPreflight` (Plan 01) once, renders a one-paragraph Larry-voiced
context summary, and computes the weighted source-lens set from the room context. Per the
4.8 re-baseline (131-REVIEW-4.8 section 2), Stages 2 (context summary) and 3 (lens-set
computation) COLLAPSE into a single reasoning pass, and Stage 1's 8 reads are ONE batched
navigation.cjs call.

Purpose: this is the explicit moment research becomes context-aware rather than blind. The
driver (Plan 03) consumes the lens set; the command (Plan 05) surfaces the summary.

Output: research-context-extractor.cjs (a pure-over-navigation module) + its behavior suite.
The suite is already registered in run-all-131.sh by Plan 01 (this plan only CREATES the
suite file; it does not edit the aggregator).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/131-research-as-graph-aware-workflow/131-CONTEXT.md
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Plan 01 contracts this plan consumes. Use directly; no exploration. -->

navigation.getResearchPreflight(db, { roomDir, sessionId, topic }) -> {
  active_workflow, active_jtbd, operator, current_section,
  recent_changes, evidence_gaps, prior_research, role_blend
}  (all 8 keys; missing inputs are null / []).

The lens-set rule (from 131-CONTEXT Stage 3):
- current_section is financial-model gap -> scholarly + industry + patent lenses
- active_jtbd is thesis-build -> add brain (methodology chain) lens
- role_blend has investor weight -> add competitive-intelligence weight
- role_blend has researcher weight -> add scholarly weight
- role_blend has founder.grant -> add grants lens
Output: an ORDERED list of { lens, weight } tuples.

The persona-aware framing source (Canon Part 2a + Phase 115): role_blend is a weighted
tuple e.g. { founder: 0.6, researcher: 0.4 }. Frame the summary in the dominant role's voice.

The Body Shape A decision (131-CONTEXT open-decision 3, RESOLVED recommend): the context
summary renders as Body Shape A (one conversational paragraph), NOT Body Shape E.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED suite for the extractor (batched read + summary + weighted lens set)</name>
  <files>tests/test-131-context-extractor.cjs</files>
  <read_first>
    - tests/test-130-lens-engine.cjs (the behavior-suite idiom: seed a room, drive a pure module over navigation, node:assert/strict)
    - tests/test-131-substrate.cjs (the Plan 01 seed pattern + getResearchPreflight contract)
    - lib/core/lens-engine.cjs readRoleBlend (the role_blend tuple shape the summary frames against)
  </read_first>
  <behavior>
    - Test: extractContext(opts) calls getResearchPreflight exactly ONCE (spy-counted) -- no 8 sequential reads.
    - Test: the returned context_summary is a single non-empty paragraph (Body Shape A: no selector block, no Shape E header) mentioning the active workflow + JTBD + current section + evidence-gap count when present.
    - Test: computeLensSet over a financial-model gap + thesis-build JTBD + investor-weighted role_blend returns an ordered { lens, weight } list containing scholarly, industry, patent, brain, and a competitive-intelligence weight bump -- NOT a hardcoded fixed list (a different context yields a different set; assert two different contexts produce different lens sets).
    - Test: a founder.grant role_blend adds the grants lens.
    - Test: an empty / cold room (all preflight inputs null) yields a defensible Tier-0 default lens set (scholarly only) and a graceful summary, never a throw.
  </behavior>
  <action>Write the RED behavior suite (CREATE tests/test-131-context-extractor.cjs; do NOT touch run-all-131.sh, which Plan 01 already pre-registered this suite in). Seed two contrasting rooms (one financial-model + thesis-build + investor; one cold/empty) so the "lens set is computed, not hardcoded" invariant is provable by inequality. Inject a getResearchPreflight spy (or assert call count via a wrapped navigation) to prove ONE batched read. The suite MUST fail (extractor absent).</action>
  <verify>
    <automated>node tests/test-131-context-extractor.cjs; test $? -ne 0 && echo "RED OK" || (echo "should fail RED" && exit 1)</automated>
  </verify>
  <done>The suite runs and FAILS (RED) because research-context-extractor.cjs does not exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: research-context-extractor.cjs (Stage 1+2+3 in one pass)</name>
  <files>lib/core/research-context-extractor.cjs</files>
  <read_first>
    - lib/core/navigation.cjs (getResearchPreflight re-export from Plan 01; the chokepoint-only contract)
    - lib/core/lens-engine.cjs readRoleBlend + the runOneLens ctx shape (how role_blend flows; reuse the framing-hook idiom)
    - lib/core/navigation/research-preflight.cjs (the 8-key return shape from Plan 01)
    - docs/MINDRIAN-CANON.md Part 2a (role-blend x journey-stage framing) + Part 5 (evidence tiers, for the gap framing)
  </read_first>
  <behavior>
    - Test (from Task 1): one batched read; Body Shape A summary; computed weighted lens set varies by context; founder.grant adds grants; cold room defaults to scholarly with a graceful summary.
  </behavior>
  <action>Create lib/core/research-context-extractor.cjs. It requires lib/core/navigation.cjs ONLY for room.db access (Canon Part 9: navigation is the only door; the module is NOT substrate-allow-listed, so it must never require room-db.cjs / node:sqlite -- a check-substrate.cjs verify proves this). Export extractContext({ roomDir, sessionId, topic, db }) that: (1) calls navigation.getResearchPreflight(db, {roomDir, sessionId, topic}) ONCE; (2) renders a Body Shape A one-paragraph Larry-voiced context_summary string framed by the dominant role in role_blend (per Canon Part 2a), naming the active workflow + JTBD + current section + the evidence_gaps count when non-empty (the 131-CONTEXT Stage 2 example sentence is the template); (3) calls the local pure helper computeLensSet(preflight) which applies the 131-CONTEXT Stage 3 rules (section-gap -> scholarly/industry/patent; thesis-build JTBD -> add brain; investor weight -> competitive-intelligence; researcher weight -> scholarly bump; founder.grant -> grants) and returns an ORDERED [{ lens, weight }] list. The 4th source-family weighting (weighted-by-context) is computed HERE as the ordered list; the engine's source-family rotation in Plan 03 consumes it. Return { ok:true, context_summary, lens_set, preflight }. Defensive: a null preflight (cold room) returns a scholarly-only default lens set + a graceful summary; never throws. ZERO Python, ZERO child_process, ZERO corpus fetch (this module is read-only over room.db + USER.md; the fetch is Plan 03 via the 130.5 corpus). NO em-dashes.</action>
  <verify>
    <automated>node tests/test-131-context-extractor.cjs && node scripts/check-substrate.cjs --baseline | grep "research-context-extractor" && echo "UNEXPECTED bypass" && exit 1 || echo "green + clean"</automated>
  </verify>
  <done>test-131-context-extractor.cjs is GREEN; the extractor passes the substrate guard; one batched read; the lens set is context-derived.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extractor -> navigation.cjs | the only door to room.db; extractor must not bypass it |
| USER.md role_blend -> summary | a poisoned role_blend must not crash framing or smuggle an agent identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-02-01 | Elevation of privilege | extractor requiring room-db.cjs directly (substrate bypass) | mitigate | extractor requires navigation.cjs only; Task 2 verify runs check-substrate.cjs against it |
| T-131-02-02 | Denial of service | a malformed preflight crashing the pass | mitigate | every read defensive; cold-room path returns a default lens set + graceful summary (Task 1 cold-room test) |
| T-131-02-03 | Information disclosure | user content leaking into a later Brain query via the summary | accept | the summary is LOCAL-only (rendered to the user, never sent to Brain); the pre-egress audit on the actual fetch is Plan 03 (T-131-03) + the inherited 130.5 hook |
| T-131-02-SC | Tampering | npm/pip/cargo installs | accept | zero dependencies added; no install task |
</threat_model>

<verification>
- node tests/run-all-131.sh -> substrate + context-extractor suites PASS
- node scripts/check-substrate.cjs --baseline -> clean on research-context-extractor.cjs
- grep -v '^[[:space:]]*//' lib/core/research-context-extractor.cjs | grep -cE "child_process|spawn|\.py" -> 0 (zero-Python invariant for this module)
- grep -nP "\x{2014}" lib/core/research-context-extractor.cjs -> zero em-dashes
</verification>

<success_criteria>
- The extractor calls getResearchPreflight exactly once (Stages 1 batched)
- Context summary + weighted lens set produced in ONE pass (Stages 2+3 collapsed)
- Lens set is context-derived, not hardcoded (proven by two contrasting rooms)
- Module passes the substrate guard; zero Python; zero corpus fetch
</success_criteria>

<output>
Create `.planning/phases/131-research-as-graph-aware-workflow/131-02-SUMMARY.md` when done
</output>
