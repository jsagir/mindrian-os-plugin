---
phase: 131-research-as-graph-aware-workflow
plan: 05
type: execute
wave: 4
depends_on: ["131-02", "131-03", "131-04"]
files_modified:
  - commands/research.md
  - scripts/check-research-isomorphism.cjs
  - tests/test-131-isomorphism.cjs
autonomous: true
requirements: [RES-131-COMMAND, RES-131-CHAIN-BACK, RES-131-CI-GUARD, RES-131-STAGES]
must_haves:
  truths:
    - "/mos:research invokes the 7-stage pipeline (pre-flight+plan / execute / present / F.1 gate / wire / post-filing) in sequence via the Plan 02-04 modules"
    - "Called BY another methodology, /mos:research returns the EvidenceClaim IDs the caller resumes with; called STANDALONE it surfaces an F.1 next-move selector"
    - "scripts/check-research-isomorphism.cjs fails when a finding lacks provenance or a cascade edge lacks a typed predicate"
  artifacts:
    - path: "commands/research.md"
      provides: "the rewritten command orchestrating Stages 1-8 (collapsed to 7)"
      contains: "research-context-extractor"
    - path: "scripts/check-research-isomorphism.cjs"
      provides: "the CI guard asserting the typed-node + typed-edge contract"
      contains: "EvidenceClaim"
  key_links:
    - from: "commands/research.md"
      to: "lib/core/research-context-extractor.cjs"
      via: "Stage 1+2+3 invocation"
      pattern: "research-context-extractor"
    - from: "commands/research.md"
      to: "lib/core/findings-wirer.cjs"
      via: "Stage 7 wiring"
      pattern: "findings-wirer"
---

<objective>
Rewrite `/mos:research` from a prose-and-agent command into the canonical workflow step
that invokes the Plan 02-04 pipeline modules in sequence (the 8-stage spec, collapsed to 7
per the 4.8 re-baseline: Stage 1 batched pre-flight + Stages 2+3 merged pre-flight+plan).
And ship the CI guard `scripts/check-research-isomorphism.cjs` asserting /mos:research
output matches the typed-node contract (every finding has provenance; every cascade edge
has a typed predicate).

The command must support BOTH invocation modes (the load-bearing pilot capability):
- called BY another methodology (e.g. /mos:build-thesis declaring requires_evidence) ->
  return the EvidenceClaim IDs so the caller resumes with the evidence it needed
- called STANDALONE -> surface the F.1 next-move selector ("now /mos:build-thesis can
  consume these claims")

Output: the rewritten commands/research.md + check-research-isomorphism.cjs + its suite
(already registered in run-all-131.sh by Plan 01; this plan only CREATES the suite file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/131-research-as-graph-aware-workflow/131-CONTEXT.md
@docs/MINDRIAN-CANON.md
@commands/research.md

<interfaces>
<!-- The pipeline modules this command orchestrates (Plans 02-04). -->

lib/core/research-context-extractor.cjs :: extractContext({ roomDir, sessionId, topic, db })
  -> { ok, context_summary, lens_set, preflight }  (Stages 1+2+3).
lib/lens-engine/source-lens-driver.cjs :: runSourceLens({ roomDir, topic, lensSet, preflight,
  stage, db, sessionId }) -> { ok, findings: top5, lens_set }  (Stage 4).
lib/core/research-filing-selector.cjs :: buildFilingSelector(finding, candidateSections, opts)
  -> a Shape F.1 envelope via selector-dispatcher  (Stage 6).
lib/core/findings-wirer.cjs :: wireAccept / wireReject / wireDefer (db, {...})  (Stage 7).

The command frontmatter (preserve Phase 122 workflow-layer fields):
kind: methodology, frameworks, produces, inputs, autonomous_safe. The command already
declares serves_jtbd + body_shape. Per 131-CONTEXT deliverable 7, a calling methodology
declares requires_evidence: to auto-dispatch /mos:research; the command reads an inbound
"called-by" handle and returns EvidenceClaim IDs on that path.

The auto-dispatch rule (open-decision 1, RESOLVED per 4.8): ask with a pre-computed
confident recommendation in the F.1 selector ("evidence is thin here -- run /mos:research?"),
never auto-fire material work (GUIDED-default Brain rule, Canon Part 9).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: rewrite commands/research.md to invoke Stages 1-7 in sequence</name>
  <files>commands/research.md</files>
  <read_first>
    - commands/research.md (the current command: frontmatter to preserve + the prose flow to replace)
    - lib/core/research-context-extractor.cjs (Stage 1+2+3 entry) + lib/lens-engine/source-lens-driver.cjs (Stage 4) + lib/core/research-filing-selector.cjs (Stage 6) + lib/core/findings-wirer.cjs (Stage 7)
    - commands/think-hats.md (a Phase 122/130 thin-lens-engine-client command -- the frontmatter + invocation idiom to mirror)
    - docs/MINDRIAN-CANON.md Part 2 Engine 1 (research as a graph-aware Act 1 surface) + Part 3 (the F.1 gate)
  </read_first>
  <action>Rewrite commands/research.md so /mos:research orchestrates the pipeline by invoking the Plan 02-04 modules (via node ${CLAUDE_PLUGIN_ROOT}/... entry points, the established command-invokes-cjs idiom) rather than free-prose agent dispatch. Preserve ALL Phase 122 workflow-layer frontmatter (kind, frameworks, produces, inputs, autonomous_safe) and add the requires_evidence auto-dispatch documentation. The 7 stages in order: (1) PRE-FLIGHT+PLAN -- call research-context-extractor.extractContext; surface the Body Shape A context_summary to the user (the "research becomes context-aware" moment) and the computed lens_set; (2) EXECUTION -- call source-lens-driver.runSourceLens with the lens_set + the stage flag; (3) PRESENTATION -- render the top-5 findings, each with title + 1-line summary + source + url + retrieved_at + evidence_tier + the pre-mapped candidate room location(s) with % match, persona-framed per role_blend; (4) F.1 FILING SELECTOR -- per finding, call research-filing-selector.buildFilingSelector and present the gate (the pre-computed confident recommendation per the 4.8 re-resolution); (5) WIRING -- route the user decision to findings-wirer wireAccept / wireReject / wireDefer; (6) POST-FILING -- if called BY a methodology (inbound called-by handle), RETURN the accepted EvidenceClaim IDs so the caller resumes; if STANDALONE, surface the F.1 next-move selector naming the methodologies that can now consume the claims. Document the --broad legacy mode as superseded-by-the-pipeline OR retained as a thin wrapper that runs the pipeline with a 3-lens set (do NOT delete user-facing capability; either keep --broad as a lens_set preset or document its migration). Remove the dead step-5 reference to scripts/compute-hsi.py (the zero-Python directive: the command must NOT call any Python). Keep allowed-tools coherent with the new flow. NO em-dashes (hyphens only).</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('commands/research.md','utf8'); const hasAll=['research-context-extractor','source-lens-driver','research-filing-selector','findings-wirer'].every(m=>s.includes(m)); const noPy=!/compute-hsi\.py|hsi-.*\.py/.test(s); process.exit(hasAll&&noPy?0:1)"</automated>
  </verify>
  <done>commands/research.md invokes all four pipeline modules across the 7 stages, supports both invocation modes, and references zero Python scripts.</done>
</task>

<task type="auto">
  <name>Task 2: check-research-isomorphism.cjs CI guard + suite</name>
  <files>scripts/check-research-isomorphism.cjs, tests/test-131-isomorphism.cjs</files>
  <read_first>
    - scripts/check-substrate.cjs (the CI-guard idiom: scanFiles pure API + a --diff staged mode + exit codes; the grep-guard structure to mirror)
    - lib/core/navigation/evidence-claim.cjs (the locked EvidenceClaim provenance schema the guard asserts) + lib/core/navigation/edges.cjs (the four typed predicates)
    - tests/test-130-lens-substrate.cjs (the suite idiom for a guard test)
    - lib/memory/run-feynman-tests.cjs (the additive Phase-block registration idiom -- the 130 block to mirror)
  </read_first>
  <action>Create scripts/check-research-isomorphism.cjs mirroring the scripts/check-substrate.cjs structure (a pure scanRoom(db|fixtures) API + a CLI mode + exit-0-informational / exit-1-on-violation; node built-ins only; zero network). The guard asserts the /mos:research typed-node contract over a room.db (or a fixture set): (a) every EvidenceClaim node carries the locked provenance schema (source + url + retrieved_at + evidence_tier all present and evidence_tier in the closed Part 5 set); (b) every cascade edge FROM an EvidenceClaim node has a typed predicate in {INFORMS, CONTRADICTS, SUPERSEDES, REJECTED_BECAUSE} (no untyped / off-allowlist edge); (c) every cascade-edge target resolves to a correlation_id (not a raw name -- a heuristic: the target id matches the 130.7 correlation_id shape, not a bare section slug); (d) a REJECTED_BECAUSE edge carries a non-empty reason scalar. It is ALSO a directive guard: include a grep-mode that scans lib/lens-engine/source-lens-driver.cjs + lib/core/research-context-extractor.cjs + lib/core/findings-wirer.cjs + commands/research.md for ZERO Python spawn (child_process / spawn / execSync / .py / scripts/hsi), stripping comment-only lines before counting (grep-gate hygiene). Write tests/test-131-isomorphism.cjs (CREATE it; do NOT touch run-all-131.sh -- Plan 01 already pre-registered this suite): a clean fixture passes; a planted violation (an EvidenceClaim missing url; an edge with an untyped predicate; a Python spawn) fails closed. Wire the guard into lib/memory/run-feynman-tests.cjs is NOT this plan's task (Plan 06 owns the run-feynman-tests Phase 131 block) -- only register the suite via run-all-131.sh's pre-registration (Plan 01). If a pre-commit hook chains the substrate guard, add check-research-isomorphism to that chain additively here (the hook file, if present, is this plan's to extend).</action>
  <verify>
    <automated>node tests/test-131-isomorphism.cjs && node scripts/check-research-isomorphism.cjs --help >/dev/null 2>&1; echo "isomorphism guard + suite OK"</automated>
  </verify>
  <done>check-research-isomorphism.cjs fails closed on a finding lacking provenance, an untyped cascade edge, a non-correlation_id target, or a Python spawn; the clean fixture passes; the suite is created (Plan 01 pre-registered it in run-all-131.sh).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command -> pipeline modules | the command must route every write through the Plan 02-04 modules (navigation.cjs), never raw |
| called-by handle -> EvidenceClaim IDs | the chain-back path must return only node-id handles, not user content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-05-01 | Tampering | the command re-introducing a Python HSI call (dead compute-hsi.py reference) | mitigate | Task 1 removes the compute-hsi.py reference; Task 2 isomorphism guard greps the command + modules for zero Python spawn and fails closed |
| T-131-05-02 | Spoofing | an untyped / off-allowlist cascade edge slipping past review | mitigate | check-research-isomorphism asserts every EvidenceClaim edge has a predicate in the four-member allow-list; planted-violation test fails closed |
| T-131-05-03 | Information disclosure | the chain-back returning finding prose instead of node-id handles to the caller | mitigate | the post-filing path returns EvidenceClaim node IDs only; the prose lives on the LOCAL node |
| T-131-05-SC | Tampering | npm/pip/cargo installs | accept | zero dependencies added (command + node built-in guard) |
</threat_model>

<verification>
- node tests/run-all-131.sh -> all suites through isomorphism PASS
- node scripts/check-research-isomorphism.cjs against a clean fixture -> exit 0; planted-violation fixture -> exit 1
- grep -v '^[[:space:]]*//' lib/lens-engine/source-lens-driver.cjs lib/core/findings-wirer.cjs lib/core/research-context-extractor.cjs | grep -cE "child_process|\.py|scripts/hsi" -> 0
- node scripts/build-command-registry.cjs --check (if present) -> research.md frontmatter still valid
- grep -nP "\x{2014}" commands/research.md scripts/check-research-isomorphism.cjs -> zero em-dashes
</verification>

<success_criteria>
- /mos:research orchestrates the 7-stage pipeline via the Plan 02-04 modules
- Both invocation modes work (chain-back EvidenceClaim IDs; standalone next-move selector)
- The command references zero Python
- check-research-isomorphism.cjs fails closed on missing provenance / untyped edge / non-correlation_id target / Python spawn
</success_criteria>

<output>
Create `.planning/phases/131-research-as-graph-aware-workflow/131-05-SUMMARY.md` when done
</output>
