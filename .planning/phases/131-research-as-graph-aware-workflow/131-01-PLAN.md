---
phase: 131-research-as-graph-aware-workflow
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/edges.cjs
  - lib/core/navigation/memory-events.cjs
  - lib/core/navigation/evidence-claim.cjs
  - lib/core/navigation/research-preflight.cjs
  - lib/core/navigation.cjs
  - docs/architecture/SUBSTRATE-CONTRACT.md
  - tests/test-131-substrate.cjs
  - tests/run-all-131.sh
autonomous: true
requirements: [RES-131-FWD-EVCLAIM, RES-131-FWD-EDGES, RES-131-PREFLIGHT, RES-131-EVENTS]
must_haves:
  truths:
    - "CONTRADICTS and SUPERSEDES are members of ALLOWED_EDGE_TYPES (additive, alongside the shipped INFORMS + REJECTED_BECAUSE)"
    - "writeEvidenceClaim writes a typed EvidenceClaim node with review_status proposed and the locked provenance schema (source, url, retrieved_at, evidence_tier)"
    - "getResearchPreflight returns all 8 pre-flight inputs in one navigation.cjs round-trip"
    - "research_filed, research_rejected, research_deferred are members of EVENT_TYPES"
  artifacts:
    - path: "lib/core/navigation/evidence-claim.cjs"
      provides: "the locked EvidenceClaim node writer (Phase 136 forward contract)"
      contains: "writeEvidenceClaim"
    - path: "lib/core/navigation/research-preflight.cjs"
      provides: "getResearchPreflight batched 8-input pre-flight read"
      contains: "getResearchPreflight"
  key_links:
    - from: "lib/core/navigation.cjs"
      to: "lib/core/navigation/evidence-claim.cjs"
      via: "re-export"
      pattern: "writeEvidenceClaim"
    - from: "lib/core/navigation/edges.cjs"
      to: "ALLOWED_EDGE_TYPES"
      via: "additive Set members"
      pattern: "CONTRADICTS"
---

<objective>
Land the Wave-1 shared substrate every later Phase 131 plan depends on, and lock the
three forward contracts Phase 136 consumes without a migration: (1) the EvidenceClaim
node property schema, (2) the CONTRADICTS / SUPERSEDES cascade-edge predicates added
ADDITIVELY to the closed allow-list (the 130-01 INFORMS / REJECTED_BECAUSE idiom), and
(3) the batched `getResearchPreflight` neighborhood read that collapses Stage 1's 8
sequential reads into one navigation.cjs round-trip (4.8 re-baseline change).

Purpose: the driver (Plan 03), the wirer + F.1 selector (Plan 04), and the command
orchestration (Plan 05) all write against these contracts. Defining them first prevents
the scavenger-hunt anti-pattern and lets the downstream plans run file-disjoint in
parallel. This plan is also the SINGLE owner of the phase aggregator tests/run-all-131.sh,
which it pre-registers with the full Phase 131 suite list so later plans never edit it.

Output: evidence-claim.cjs (the locked node writer), research-preflight.cjs (the batched
read), two additive ALLOWED_EDGE_TYPES members, three additive EVENT_TYPES members,
navigation.cjs re-exports, a SUBSTRATE-CONTRACT M11 amendment, and the RED-first 131
substrate suite + the pre-registered aggregator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-research-as-graph-aware-workflow/131-CONTEXT.md
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Contracts the executor uses directly. Extracted from the codebase; no exploration needed. -->

The additive-edge idiom (lib/core/navigation/edges.cjs, Phase 130-01 verbatim):
ALLOWED_EDGE_TYPES is a frozen Set. INFORMS + REJECTED_BECAUSE were added as the LAST two
members with a per-block comment citing the canon. writeEdge(db, {source_id, target_id,
edge_type, properties}) rejects any edge_type not in the Set with reason invalid_edge_type.
Properties are ENUM-ONLY (Canon Part 8) -- never freeform user content.

The lens_finding node writer shape to mirror (lib/core/navigation/lens-nodes.cjs):
writeLensFinding UPSERTs INTO nodes (id, type, properties, source_path, created_by,
confidence, review_status, created_at, last_seen_at) with created_by 'system'. The new
EvidenceClaim writer mirrors this INSERT shape but is a TRUTH-CLAIM node type, so it lands
review_status 'proposed' and is NEVER auto-confirmed (Canon Part 9 role 5; promotion is the
human gate in Plan 04). It takes a caller-owned db handle exactly like writeEdge / writeLensFinding.

The EVENT_TYPES additive idiom (lib/core/navigation/memory-events.cjs):
EVENT_TYPES is a frozen Set. The Phase 130-02 lens events were added as the LAST members.
logEvent(db, eventType, payload, opts) rejects an event_type not in the Set. Tests assert a
DELTA (exactly +N net-new), never an absolute size.

The batched-read targets (already live on navigation.cjs):
- getActiveFocus(db, sessionId), getNeighborhood(db, focusNodeId, opts)
- findRecentChanges(db, opts) -- the memory_event tail
- getCurrentJTBD(roomDir), getCurrentOperator(roomDir) -- event-log-authoritative
- findUnsupportedClaims(db), findBlockingAssumptions(db) -- the needs_evidence claims (Stage 1 input 6)
- resolveByUser(roomDir) reads USER.md; lib/core/lens-engine.cjs readRoleBlend reads role_blend (Stage 1 input 8)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED substrate suite + pre-register the full Phase 131 aggregator</name>
  <files>tests/test-131-substrate.cjs, tests/run-all-131.sh</files>
  <read_first>
    - tests/test-130-lens-substrate.cjs (the RED-first substrate-suite idiom: direct-CJS node:assert/strict, a trimmed seed, delta assertions on the Sets)
    - tests/run-all-130.sh (the CJS_SUITES aggregator shape to mirror as run-all-131.sh)
    - lib/core/navigation/edges.cjs (ALLOWED_EDGE_TYPES Set; writeEdge reason strings)
    - lib/core/navigation/memory-events.cjs (EVENT_TYPES Set; logEvent contract)
  </read_first>
  <behavior>
    - Test: ALLOWED_EDGE_TYPES has CONTRADICTS and SUPERSEDES (and STILL has INFORMS + REJECTED_BECAUSE -- no regression).
    - Test: the net-new edge-type delta over the pre-131 Set is exactly 2 (named-membership delta, not an absolute size).
    - Test: EVENT_TYPES has research_filed, research_rejected, research_deferred; net-new delta exactly 3.
    - Test: writeEvidenceClaim(db, params) UPSERTs a node with type EvidenceClaim, review_status 'proposed', and properties carrying source / url / retrieved_at / evidence_tier; returns { ok, node_id }.
    - Test: writeEvidenceClaim rejects a missing url / source with a reason scalar (defensive, never throws).
    - Test: an EvidenceClaim node written via writeEvidenceClaim does NOT appear in getConfirmedFacts (it is proposed, not confirmed) -- proving the human-gate invariant holds before Plan 04 wires the gate.
  </behavior>
  <action>Write the RED suite asserting the substrate that Tasks 2-3 build. Mirror the test-130-lens-substrate.cjs structure: a setupRoom that seeds a minimal room (room + 1 section + 1 claim FK anchor for the edge-write tests), node:assert/strict, no new npm deps. Assert edge + event deltas by NAMED membership against a captured pre-131 baseline count so additive extensions cannot regress the floor (the 129-01 delta-of-N idiom). Create tests/run-all-131.sh mirroring run-all-130.sh and PRE-REGISTER the FULL Phase 131 CJS_SUITES list (test-131-substrate.cjs, test-131-context-extractor.cjs, test-131-source-lens-driver.cjs, test-131-findings-wirer.cjs, test-131-isomorphism.cjs, test-131-e2e.cjs) so this plan is the SINGLE owner of the aggregator -- later plans only CREATE their suite files, they never edit run-all-131.sh (this keeps the same-wave parallel plans 02/03 file-disjoint). The aggregator must tolerate a not-yet-created suite gracefully (skip-with-note when the file is absent; exit non-zero only on a PRESENT-suite failure) so it is runnable in every wave. The substrate suite itself MUST fail now (evidence-claim.cjs absent, edges + events not yet in the Sets).</action>
  <verify>
    <automated>node tests/test-131-substrate.cjs; test $? -ne 0 && echo "RED OK (expected fail)" || (echo "should have failed RED" && exit 1)</automated>
  </verify>
  <done>The substrate suite runs and FAILS (RED) because evidence-claim.cjs does not exist and the two edge types + three event types are not yet members. run-all-131.sh exists, is executable, pre-registers all 6 suites, and skips not-yet-created ones gracefully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: add CONTRADICTS + SUPERSEDES edges and the three research events (additive)</name>
  <files>lib/core/navigation/edges.cjs, lib/core/navigation/memory-events.cjs</files>
  <read_first>
    - lib/core/navigation/edges.cjs (the per-block additive comment idiom on the LAST Set members -- INFORMS + REJECTED_BECAUSE at 130-01)
    - lib/core/navigation/memory-events.cjs (the EVENT_TYPES additive block idiom; lens events at 130-02)
    - docs/MINDRIAN-CANON.md Part 4 (cascade edges) + Part 8 (enum-only edge properties)
  </read_first>
  <behavior>
    - Test (from Task 1): the two edges and three events are now Set members; the deltas pass; INFORMS / REJECTED_BECAUSE / lens events are byte-unchanged.
  </behavior>
  <action>Add CONTRADICTS and SUPERSEDES to ALLOWED_EDGE_TYPES as the new LAST members, each preceded by a Phase 131-01 comment block (mirroring the 130-01 INFORMS / REJECTED_BECAUSE block verbatim): CONTRADICTS is the finding-kills-an-existing-claim edge; SUPERSEDES is the better-evidence-tier-version edge; both carry ENUM-ONLY properties (a reason scalar), never freeform user content, never cross to Brain. These are added ADDITIVELY to the closed allow-list per the locked forward contract (CONTRADICTS/SUPERSEDES were never invented per-phase; they extend the shipped vocabulary). Add research_filed, research_rejected, research_deferred to EVENT_TYPES as the new LAST members with a Phase 131-01 block citing Canon Part 9 (mandatory memory_event on every wiring decision) and Part 8 (payload carries scalar enums + node-id handles + provenance scalars only; raw finding prose lives on the EvidenceClaim node, never in the event). Do NOT touch the lens block or any prior block.</action>
  <verify>
    <automated>node -e "const e=require('./lib/core/navigation/edges.cjs'); const m=require('./lib/core/navigation/memory-events.cjs'); const ok=e.ALLOWED_EDGE_TYPES.has('CONTRADICTS')&&e.ALLOWED_EDGE_TYPES.has('SUPERSEDES')&&e.ALLOWED_EDGE_TYPES.has('INFORMS')&&e.ALLOWED_EDGE_TYPES.has('REJECTED_BECAUSE')&&m.EVENT_TYPES.has('research_filed')&&m.EVENT_TYPES.has('research_rejected')&&m.EVENT_TYPES.has('research_deferred'); process.exit(ok?0:1)"</automated>
  </verify>
  <done>Both edge types and all three event types are members; the shipped INFORMS / REJECTED_BECAUSE / lens events and prior events are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: evidence-claim.cjs writer (the locked Phase 136 node schema) + getResearchPreflight</name>
  <files>lib/core/navigation/evidence-claim.cjs, lib/core/navigation/research-preflight.cjs, lib/core/navigation.cjs, docs/architecture/SUBSTRATE-CONTRACT.md</files>
  <read_first>
    - lib/core/navigation/lens-nodes.cjs (the writeLensFinding INSERT shape + caller-owned-db-handle convention to mirror; the allow-listed-submodule contract)
    - lib/core/navigation/room-home.cjs (getConfirmedFacts / getRiskyAssumptions / getEvidenceByTier -- the read shapes getResearchPreflight composes; the truth-claim type set)
    - lib/core/navigation/focus.cjs (getActiveFocus signature) + lib/core/navigation/neighborhood.cjs (getNeighborhood signature)
    - lib/core/navigation.cjs (the additive-re-export block idiom + the M11 amendment requirement)
    - lib/core/lens-engine.cjs readRoleBlend (the USER.md role_blend read for Stage 1 input 8)
  </read_first>
  <behavior>
    - Test (from Task 1): writeEvidenceClaim writes type EvidenceClaim, review_status proposed, with source/url/retrieved_at/evidence_tier; rejects missing url/source; the written node is absent from getConfirmedFacts.
    - Test: getResearchPreflight(db, {roomDir, sessionId, topic}) returns an object with all 8 keys (active_workflow, active_jtbd, operator, current_section, recent_changes, evidence_gaps, prior_research, role_blend) in one call; missing inputs degrade to null/empty, never throw.
  </behavior>
  <action>Create lib/core/navigation/evidence-claim.cjs as an allow-listed navigation submodule (covered by the /^lib\/core\/navigation\// substrate-guard regex; it takes a caller-owned db handle and NEVER requires node:sqlite or opens room.db itself -- exactly like lens-nodes.cjs writeLensFinding). Export writeEvidenceClaim(db, params) where params = { topic, source, url, retrieved_at, evidence_tier, summary, sessionId }. UPSERT INTO nodes with type 'EvidenceClaim', created_by 'system', review_status 'proposed' (a TRUTH-CLAIM node per Canon Part 9 -- never auto-confirmed; promotion is the Plan 04 human gate), confidence NULL, and properties = the locked schema { source, url, retrieved_at, evidence_tier, topic, summary }. evidence_tier MUST validate against the closed Part 5 set {Academic, Operational, Practitioner, None}; reject invalid_evidence_tier otherwise. Reject invalid_url / invalid_source on empty. This is the LOCKED Phase 136 forward-contract schema -- name the four provenance fields in an in-source comment citing "Forward contracts (LOCKED 2026-06-01) -- consumed by Phase 136". Create lib/core/navigation/research-preflight.cjs (also an allow-listed navigation submodule; caller-owned db handle for the db-keyed reads, roomDir for the roomDir-keyed reads) exporting getResearchPreflight(db, opts) that composes the 8 Stage-1 inputs in ONE pass by calling the EXISTING chokepoint readers: getActiveFocus + getCurrentJTBD + getCurrentOperator (active_workflow / active_jtbd / operator + current_section), findRecentChanges (recent_changes tail), findUnsupportedClaims + findBlockingAssumptions (evidence_gaps = needs_evidence claims), readRoleBlend-style USER.md read (role_blend). prior_research is a placeholder array the driver fills via the 130.5 cache + Pinecone dedup in Plan 03 -- getResearchPreflight returns prior_research: [] with a documented "filled by source-lens-driver Stage 4 dedup" note (do NOT call the corpus here; this module is read-only over room.db + USER.md). Defensive: every sub-read wrapped so one failure degrades that key to null/[], never aborts the batch. Re-export writeEvidenceClaim + getResearchPreflight on navigation.cjs with a per-export justification block mirroring the 130-01 lens-node re-export comment. Add a Phase 131-01 amendment paragraph to docs/architecture/SUBSTRATE-CONTRACT.md M11 naming the two source modules, the two exports, the consumers (Plan 03 driver, Plan 04 wirer + selector, Plan 05 command), and the caller-owned-db-handle / zero-bypass invariant.</action>
  <verify>
    <automated>node tests/test-131-substrate.cjs && node scripts/check-substrate.cjs --baseline | grep -E "evidence-claim\.cjs|research-preflight\.cjs" && echo "UNEXPECTED substrate violation" && exit 1 || echo "substrate clean + suite green"</automated>
  </verify>
  <done>test-131-substrate.cjs is GREEN. writeEvidenceClaim + getResearchPreflight are re-exported on navigation.cjs; both new submodules pass the substrate guard (no bypass); SUBSTRATE-CONTRACT M11 carries the Phase 131-01 amendment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> navigation.cjs | the only door to room.db; new writers/readers must not bypass it |
| edge/event properties -> room.db | enum-only scalars; no freeform user content may land |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01-01 | Tampering | evidence-claim.cjs writing review_status confirmed | mitigate | writeEvidenceClaim hardcodes review_status 'proposed'; Task 1 test asserts the node is absent from getConfirmedFacts (no agent-confirmed truth-claim per Canon Part 9 role 5) |
| T-131-01-02 | Information disclosure | freeform user content on a CONTRADICTS/SUPERSEDES edge | mitigate | edge properties are enum-only reason scalars per Part 8; in-source comment + writeEdge contract enforce it |
| T-131-01-03 | Elevation of privilege | a new submodule requiring node:sqlite (substrate bypass) | mitigate | both new modules take a caller-owned db handle, never require node:sqlite; Task 3 verify runs check-substrate.cjs against them |
| T-131-01-SC | Tampering | npm/pip/cargo installs | accept | this plan adds ZERO dependencies (node built-ins only); no install task, so the legitimacy gate does not apply |
</threat_model>

<verification>
- node tests/run-all-131.sh -> test-131-substrate.cjs PASS (other suites skip-with-note until created)
- node tests/run-all-130.sh -> still GREEN (zero regression on the shipped edge/event Sets)
- node scripts/check-substrate.cjs --baseline -> clean on the two new submodules
- grep -nE "\x{2014}" lib/core/navigation/evidence-claim.cjs lib/core/navigation/research-preflight.cjs -> zero em-dashes
</verification>

<success_criteria>
- CONTRADICTS + SUPERSEDES are additive ALLOWED_EDGE_TYPES members; INFORMS + REJECTED_BECAUSE unchanged
- research_filed / research_rejected / research_deferred are additive EVENT_TYPES members
- writeEvidenceClaim writes the LOCKED Phase 136 schema (proposed + source/url/retrieved_at/evidence_tier)
- getResearchPreflight returns all 8 inputs in one navigation.cjs round-trip
- Both new modules pass the substrate guard; navigation.cjs re-exports both; M11 amended
- run-all-131.sh is the single aggregator, pre-registering all 6 suites
</success_criteria>

<output>
Create `.planning/phases/131-research-as-graph-aware-workflow/131-01-SUMMARY.md` when done
</output>
