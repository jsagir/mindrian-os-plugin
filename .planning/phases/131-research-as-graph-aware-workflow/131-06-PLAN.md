---
phase: 131-research-as-graph-aware-workflow
plan: 06
type: execute
wave: 5
depends_on: ["131-05"]
files_modified:
  - tests/test-131-e2e.cjs
  - tests/fixtures/phase-131/sample-room/seed.sql
  - lib/memory/run-feynman-tests.cjs
  - docs/RESEARCH-AS-WORKFLOW-STEP.md
autonomous: true
requirements: [RES-131-E2E, RES-131-DOC, RES-131-RELEASE-GATE]
must_haves:
  truths:
    - "All 5 E2E tests pass: standalone, called-by-build-thesis (chains back EvidenceClaim IDs), called-by-user-needs (different lens set + destination), rejection-as-data, dedup"
    - "The full Phase 131 pipeline runs end-to-end through navigation.cjs producing typed graph artifacts; all memory_event writes flow through the navigation.cjs logMemoryEvent re-export (the chokepoint), not the raw logEvent"
    - "At least one E2E exercises the REAL teaching-graph correlation resolver (lib/lens-engine/correlation-resolver.cjs over the seeded correlation_labels index), asserting a teaching-graph-targeted edge lands on a real correlation_id -- the release gate does not close green on a stub-only correlation path; LOCAL-section edges land on local room.db node ids"
    - "docs/RESEARCH-AS-WORKFLOW-STEP.md documents how methodologies declare requires_evidence and consume returned EvidenceClaim IDs (the v1.14.0 fan-out template)"
  artifacts:
    - path: "tests/test-131-e2e.cjs"
      provides: "the 5 E2E tests (the phase release gate)"
      contains: "EvidenceClaim"
    - path: "docs/RESEARCH-AS-WORKFLOW-STEP.md"
      provides: "the v1.14.0 fan-out template doc"
      contains: "requires_evidence"
  key_links:
    - from: "tests/test-131-e2e.cjs"
      to: "lib/core/research-context-extractor.cjs + source-lens-driver + findings-wirer"
      via: "full-pipeline drive through navigation.cjs"
      pattern: "findings-wirer"
    - from: "lib/memory/run-feynman-tests.cjs"
      to: "tests/test-131-e2e.cjs"
      via: "additive Phase 131 registration block"
      pattern: "131"
---

<objective>
Ship the 5 E2E tests (the Phase 131 release gate) that drive the FULL pipeline through
navigation.cjs and prove the source-lens pilot works end-to-end, plus the documentation
that makes this pilot the template for the v1.14.0 fan-out to the 13 other research surfaces.

The 5 E2E tests (from the CONTEXT deliverable 6 + the quality gate):
1. STANDALONE invocation -- the full 7-stage pipeline produces typed graph artifacts
2. Called by /mos:build-thesis -- chains back the accepted EvidenceClaim IDs to the caller
3. Called by /mos:user-needs -- a DIFFERENT lens set + a different filing destination
4. Rejection-as-data -- a REJECT writes a REJECTED_BECAUSE edge + captured reason + event
5. Dedup against prior research -- Stage 1 input 7 + the 130.5 cache suppresses a re-fetch

Output: test-131-e2e.cjs + the phase-131 fixture seed + the Feynman-runner registration +
docs/RESEARCH-AS-WORKFLOW-STEP.md. The E2E suite is already pre-registered in run-all-131.sh
by Plan 01; this plan only CREATES the suite file and registers it in the Feynman runner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/131-research-as-graph-aware-workflow/131-CONTEXT.md
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- The full pipeline under test (Plans 01-05). -->

navigation.cjs: writeEvidenceClaim, getResearchPreflight, writeEdge, logMemoryEvent,
confirmNode, resolveByUser, findRecentChanges (the chokepoint the E2E asserts everything
flows through). All memory_event writes in the pipeline go through the navigation.cjs
RE-EXPORT logMemoryEvent (the chokepoint), NEVER the raw logEvent from
lib/core/navigation/memory-events.cjs -- the E2E asserts events were written via the
re-export path (the fs-instrument gate proves room.db was touched only through navigation).
research-context-extractor.extractContext (Stage 1+2+3), source-lens-driver.runSourceLens
(Stage 4; inject a fetchCorpus stub so the E2E needs no live network), research-filing-selector
.buildFilingSelector (Stage 6), findings-wirer.wireAccept/wireReject/wireDefer (Stage 7).

The teaching-graph correlation resolver (Plan 04 lib/lens-engine/correlation-resolver.cjs,
built from 130.7's REAL computeCorrelationId + parseLabelIndex -- there is NO correlation.resolve):
at least ONE E2E must exercise the REAL resolver (NOT the injected stub) against a seed
fixture carrying the correlation_labels index + a teaching-graph target, asserting a
teaching-graph-targeted edge lands on a real correlation_id. The other E2E flows may use the
stub for non-resolver concerns, but the release gate must NOT close green while the only
correlation path tested is a stub.

The fs-instrument zero-leak idiom (the gate the 130-04 + 109 + 129 E2E tests use): install a
proxy that records every non-SQLite filesystem read during the flow; assert the only
allow-listed non-room.db read is USER.md (role_blend). The seed fixture is trimmed from the
130-04 seed (room + sections + LOCAL claim/section target nodes + the teaching-graph
correlation_id target node + the correlation_labels index body, pre-empting the recurring
FOREIGN KEY incident on BOTH local-node-id and correlation_id edge targets).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase-131 fixture seed + the 5 E2E tests</name>
  <files>tests/test-131-e2e.cjs, tests/fixtures/phase-131/sample-room/seed.sql</files>
  <read_first>
    - tests/test-130-lens-engine-e2e.cjs (the 8 instrumented E2E tests: setupRoom / cleanup / run, the fs-instrument proxy installed BEFORE each flow, the prewarm pass, the allow-listed USER.md read)
    - tests/fixtures/phase-130/sample-room/seed.sql (the trimmed seed + the FK anchor nodes to mirror; add BOTH local section/claim target nodes AND a teaching-graph correlation_id-keyed target node for the cascade edges; add the correlation_labels index body so the REAL resolver has a data source)
    - lib/core/research-context-extractor.cjs + lib/lens-engine/source-lens-driver.cjs + lib/core/research-filing-selector.cjs + lib/core/findings-wirer.cjs + lib/lens-engine/correlation-resolver.cjs (the modules the E2E drives; the REAL resolver is exercised in at least one flow)
    - lib/core/correlation.cjs + lib/core/correlation-label-index.cjs (130.7 REAL exports: computeCorrelationId + serializeLabelIndex/parseLabelIndex -- to build the seed correlation_labels index body; there is NO correlation.resolve)
    - tests/test-131-findings-wirer.cjs (the fetchCorpus stub seam to reuse; note the correlation path is exercised REAL here, not only stubbed)
  </read_first>
  <action>Create tests/fixtures/phase-131/sample-room/seed.sql trimmed from the phase-130 seed: room + 2-3 sections (including financial-model + a user-needs target section) + a few claims (some review_status needs_evidence so evidence_gaps is non-empty) + the FK anchor nodes the INFORMS / CONTRADICTS / SUPERSEDES / REJECTED_BECAUSE edges reference, keyed by the 130.7 correlation_id shape so the edge-write assertions do not trip the FOREIGN KEY (the recurring 129/130 incident, pre-empted by construction). Seed the correlation_labels index body via 130.7's serializeLabelIndex on a fixture that includes a teaching-graph target name (and a cross-label-duplicate), supplied as the brainSections.correlation_labels section so the REAL resolver (lib/lens-engine/correlation-resolver.cjs) runs against real data. Write tests/test-131-e2e.cjs with the 5 instrumented E2E tests, each driving the FULL pipeline ONLY through navigation.cjs (direct-CJS node:assert/strict, the fs-instrument zero-leak gate with USER.md the single allow-listed non-room.db read, a prewarm pass before instrumentation, a fetchCorpus stub so no live network). The correlation resolver is NOT stubbed in at least one flow: test 3 (and/or a dedicated assertion) exercises the REAL resolveCorrelation against the seeded correlation_labels index, asserting a teaching-graph-targeted edge lands on a real correlation_id (NOT the injected stub) -- the release gate must not close green on a stub-only correlation path. (CREATE the suite file; do NOT touch run-all-131.sh -- Plan 01 already pre-registered it):
1. STANDALONE: extractContext -> runSourceLens (stubbed corpus) -> buildFilingSelector -> wireAccept on a top finding filed to a LOCAL room section. Assert: an EvidenceClaim node (review_status proposed) + an INFORMS edge whose target is the LOCAL room.db node id (NOT a correlation_id, per the Plan 04 local-vs-teaching-graph scoping) + a research_filed memory_event written via the navigation.cjs logMemoryEvent re-export (NOT raw logEvent) all landed; the standalone post-filing surfaces an F.1 next-move selector.
2. CALLED-BY-BUILD-THESIS: drive the pipeline with an inbound called-by handle; assert the accepted EvidenceClaim node IDs are RETURNED to the caller (chain-back), and findRecentChanges shows the research_filed event.
3. CALLED-BY-USER-NEEDS: a user-needs context (different current_section + JTBD) yields a DIFFERENT computed lens_set than test 2 AND a different filing destination (assert both inequalities). This flow files a finding whose target is a TEACHING-GRAPH concept and uses the REAL resolveCorrelation (NOT the stub) over the seeded correlation_labels index: assert the resulting INFORMS edge target equals computeCorrelationId(canonical_name, primary_label) for the canonical pick -- a real correlation_id, never the raw name. This is the non-stubbed correlation assertion the release gate requires.
4. REJECTION-AS-DATA: wireReject on a finding writes EXACTLY ONE REJECTED_BECAUSE edge carrying the captured reason scalar + a research_rejected event; ZERO INFORMS edges for that finding.
5. DEDUP: seed a prior EvidenceClaim (same url as a stubbed fetched item); assert runSourceLens dedups it out (the finding does not re-file) and the 130.5 cache path is taken on the repeat (no second fetchCorpus call -- spy-counted).
The suite is the phase release gate (it must be GREEN to close Phase 131).</action>
  <verify>
    <automated>node tests/test-131-e2e.cjs</automated>
  </verify>
  <done>All 5 E2E tests pass; the full pipeline runs through navigation.cjs (events via the logMemoryEvent re-export, not raw logEvent) with the fs-instrument zero-leak gate holding; at least one E2E exercises the REAL correlation resolver (not the stub) and asserts a teaching-graph edge lands on a real correlation_id while LOCAL-section edges land on local node ids; the seed pre-empts the FK incident on both target kinds.</done>
</task>

<task type="auto">
  <name>Task 2: Feynman-runner registration + zero-regression gate</name>
  <files>lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - lib/memory/run-feynman-tests.cjs (the additive per-phase registration block idiom -- the 130 block to mirror; every prior block stays byte-unchanged)
    - tests/run-all-131.sh (the full Phase 131 suite list to register; Plan 01 owns this file)
  </read_first>
  <action>Add an additive Phase 131 registration block to lib/memory/run-feynman-tests.cjs registering all Phase 131 suites (test-131-substrate, test-131-context-extractor, test-131-source-lens-driver, test-131-findings-wirer, test-131-isomorphism, test-131-e2e) in CI, mirroring the Phase 130 block verbatim with every existing block (including 129 / 129.5 / 130) byte-unchanged. Run the full Phase 131 aggregator + the prior aggregators to prove zero regression. NOTE the out-of-scope pre-existing em-dashes the 130 SUMMARY ledgered (DI-130-04-01 lines 1128/1134) are NOT this plan's to fix -- do not touch them; the 131 block must carry zero em-dashes.</action>
  <verify>
    <automated>node tests/run-all-131.sh && node tests/run-all-130.sh && node tests/test-navigation-acceptance.cjs</automated>
  </verify>
  <done>run-feynman-tests.cjs registers all 6 Phase 131 suites additively; run-all-131 is fully GREEN; run-all-130 + navigation acceptance show zero regression.</done>
</task>

<task type="auto">
  <name>Task 3: docs/RESEARCH-AS-WORKFLOW-STEP.md (the v1.14.0 fan-out template)</name>
  <files>docs/RESEARCH-AS-WORKFLOW-STEP.md</files>
  <read_first>
    - .planning/phases/131-research-as-graph-aware-workflow/131-CONTEXT.md (deliverable 7 + the Forward contracts section to document)
    - docs/WORKFLOWS.md (the Phase 122 workflow-layer doc style to mirror) + docs/MINDRIAN-CANON.md Part 2 Engine 1 + Part 3 + Part 9
    - commands/research.md (the rewritten command this doc explains)
  </read_first>
  <action>Write docs/RESEARCH-AS-WORKFLOW-STEP.md documenting the source-lens pilot as the template for the v1.14.0 fan-out to the 13 remaining research surfaces (scout / opportunities / scheduled-tasks / radar / rs-fetch / 4 GSD researchers / file-meeting research / reanalyze / find-analogies external arm). Cover: (a) how a methodology declares requires_evidence: in its command frontmatter to auto-dispatch /mos:research (with the GUIDED-default ask-with-confident-recommendation rule, never auto-fire); (b) how the calling command consumes the returned EvidenceClaim IDs (the chain-back contract); (c) the LOCKED forward contracts for Phase 136 (the EvidenceClaim provenance schema, the four cascade predicates as additive ALLOWED_EDGE_TYPES members, the F.1-selector-equals-gate-widget, cascade-edge targets are correlation_ids); (d) the consume-130.5 / zero-Python invariants the fan-out must honor; (e) the 7-stage pipeline diagram (pre-flight+plan / execute / present / F.1 gate / wire / post-filing). Add a CANON-PHASE-MAP cross-reference note (this doc implements Part 2 Engine 1 + Part 3 + Part 4 + Part 5 + Part 8 + Part 9 for the research surface). NO em-dashes (hyphens only).</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/RESEARCH-AS-WORKFLOW-STEP.md','utf8'); const ok=['requires_evidence','EvidenceClaim','correlation_id','130.5','F.1'].every(t=>s.includes(t)) && !/\u2014/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <done>docs/RESEARCH-AS-WORKFLOW-STEP.md documents the auto-dispatch declaration, the chain-back contract, the locked forward contracts, the invariants, and the pipeline; zero em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E flow -> room.db | the E2E must drive everything through navigation.cjs; the fs-instrument gate proves no non-SQLite leak |
| fixture seed -> edge FK | the seed must carry the correlation_id target anchors so edge writes do not fail FK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-06-01 | Tampering | the E2E asserting against a non-chokepoint path (false-green) | mitigate | the fs-instrument zero-leak gate (USER.md the only allow-listed non-room.db read) proves the pipeline touched room.db only via navigation; mirrors the 130-04 / 109 / 129 idiom |
| T-131-06-02 | Repudiation | the rejection E2E not proving the reason was captured | mitigate | E2E test 4 asserts the REJECTED_BECAUSE edge carries the reason scalar + a research_rejected event |
| T-131-06-03 | Denial of service | a flaky live-network fetch making the gate non-deterministic | mitigate | runSourceLens is driven with a fetchCorpus stub + a correlation resolver stub; no live network in the E2E |
| T-131-06-SC | Tampering | npm/pip/cargo installs | accept | zero dependencies added (tests + docs; node built-ins only) |
</threat_model>

<verification>
- node tests/run-all-131.sh -> 6/6 suites GREEN (the phase release gate)
- node tests/run-all-130.sh + node tests/run-all-129.sh + node tests/test-navigation-acceptance.cjs -> zero regression
- node scripts/check-substrate.cjs --baseline -> clean across all Phase 131 production files
- grep -nP "\x{2014}" tests/test-131-e2e.cjs docs/RESEARCH-AS-WORKFLOW-STEP.md (131 block in run-feynman-tests.cjs) -> zero em-dashes
</verification>

<success_criteria>
- All 5 E2E tests pass (standalone / build-thesis chain-back / user-needs different-lens / rejection-as-data / dedup)
- At least one E2E exercises the REAL correlation resolver (not the stub): a teaching-graph edge lands on a real correlation_id; LOCAL-section edges land on local room.db node ids
- All pipeline memory_event writes flow through the navigation.cjs logMemoryEvent re-export (not raw logEvent)
- The full pipeline runs end-to-end through navigation.cjs with the zero-leak gate holding
- run-feynman-tests.cjs registers all 6 Phase 131 suites additively; zero regression on 129/130
- docs/RESEARCH-AS-WORKFLOW-STEP.md is the v1.14.0 fan-out template
</success_criteria>

<output>
Create `.planning/phases/131-research-as-graph-aware-workflow/131-06-SUMMARY.md` when done.
This is the phase release gate -- when 131-06 is GREEN, Phase 131 is COMPLETE.
</output>
