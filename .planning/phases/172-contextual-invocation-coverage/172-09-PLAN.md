---
phase: 172-contextual-invocation-coverage
plan: 09
type: execute
wave: 6
depends_on: ["172-08"]
files_modified:
  - lib/hmi/dial-reach-orchestrator.cjs
  - lib/hmi/dial-label-composer.cjs
  - lib/core/act-jtbd-blurb.cjs
  - tests/test-act-standing-suggestion.cjs
autonomous: true
requirements: [INV-19]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["lib/hmi/dial-reach-orchestrator.cjs", "lib/hmi/dial-label-composer.cjs", "lib/hmi/jtbd-state.cjs"]
  gate_impact: "Adds a PINNED additive /mos:act standing-suggestion row to the dial host that NEVER displaces the MAX_K=3 ranked reaches and is NOT a 7th reach. No coverage gap change; makes the one governed autonomous-execution path always available and self-explaining."
  explanation: "This plan implements INV-19: /mos:act as an always-on JTBD-contextualized standing suggestion rendered on the dial host. It consumes the dial orchestrator/composer + jtbd-state; the frozen MAX_K=3 / DIAL_REACH_K=6 contracts are untouched."
user_setup: []

must_haves:
  truths:
    - "Larry ALWAYS surfaces /mos:act as a PINNED additive option (first or last) that NEVER displaces the MAX_K=3 ranked context-reaches and is NOT a 7th reach_id."
    - "The pinned act row renders a JTBD-contextualized blurb (what act would do, what it helps with, how/which framework+why) derived LOCALLY from active JTBD + STATE.md + MINTO.md."
    - "DIAL_REACH_K=6 and MAX_K=3 are unchanged; act is a standing UI suggestion to invoke a command, not a reach."
  artifacts:
    - path: "lib/core/act-jtbd-blurb.cjs"
      provides: "The JTBD-blurb generator: reads active JTBD + STATE + MINTO LOCALLY, returns the per-state what/what-it-helps/how blurb (enum/scalar + local state only)."
      exports: ["buildActBlurb"]
    - path: "lib/hmi/dial-reach-orchestrator.cjs"
      provides: "A pinned additive act row in the offered set that does not count against MAX_K."
      contains: "pinned"
  key_links:
    - from: "lib/hmi/dial-reach-orchestrator.cjs"
      to: "lib/core/act-jtbd-blurb.cjs"
      via: "pinned act row render"
      pattern: "act"
    - from: "lib/core/act-jtbd-blurb.cjs"
      to: "lib/hmi/jtbd-state.cjs"
      via: "active JTBD LOCAL read"
      pattern: "jtbd"
---

<objective>
Implement INV-19 / D-172-j: make /mos:act an ALWAYS-ON standing suggestion - a PINNED additive row on the dial host that NEVER displaces the MAX_K=3 ranked context-reaches and is NOT a 7th reach, rendering a JTBD-contextualized blurb (what act would do, what it helps with, how/which framework+why) derived LOCALLY from the active JTBD + STATE.md + MINTO.md.

Purpose: INV-18 made act spine-GOVERNED; INV-19 makes the one governed autonomous-execution path always PRESENT and self-explaining per room state, without touching the frozen reach contracts.

Output: a JTBD-blurb generator, a pinned additive act row in the dial orchestrator + label composer, and the standing-suggestion test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@docs/MINDRIAN-CANON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the LOCAL JTBD-blurb generator</name>
  <files>lib/core/act-jtbd-blurb.cjs</files>
  <read_first>
    - lib/hmi/jtbd-state.cjs (getCurrent / history - the active JTBD LOCAL read) + lib/hmi/jtbd-classifier.cjs + lib/hmi/jtbd-taxonomy.json (the JTBD taxonomy).
    - commands/act.md serves_jtbd (the per-state framing act carries - the source of the per-JTBD blurb) + lib/brain/chain-recommender.cjs recommendFrameworkChain (the framework/chain act would run - for the "how" line; framework names + problem-type enums only, Part 8).
    - 172-CONTEXT.md D-172-j (the blurb is LOCAL-derived: enum/scalar + local state only; no Brain egress to compose it).
    - lib/core/feynman or MINTO readers (how STATE.md + MINTO.md are read LOCALLY - reuse the existing reader, do not re-implement).
  </read_first>
  <action>
    Write lib/core/act-jtbd-blurb.cjs exporting `buildActBlurb({ jtbd, state, minto })` returning a structured blurb `{ what, helps_with, how }`: WHAT /mos:act would do in THIS JTBD/problem-state, WHAT it helps with, HOW (which framework/chain it would run and why). It reads the active JTBD (via jtbd-state.cjs), STATE.md, and MINTO.md LOCALLY; the framework/chain comes from recommendFrameworkChain (framework names + problem-type enums only). The output carries ONLY enum/scalar + local-derived text; NO Brain egress to compose it (Part 8). Tier-0 resilient: degrade to a generic act blurb when JTBD/STATE/MINTO are absent.
  </action>
  <verify>
    <automated>node -e "const b=require('./lib/core/act-jtbd-blurb.cjs'); const r=b.buildActBlurb({jtbd:null,state:null,minto:null}); if(!r||typeof r.what!=='string')process.exit(1); console.log('blurb ok')"</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/act-jtbd-blurb.cjs exports buildActBlurb returning {what, helps_with, how}.
    - With null inputs it degrades to a generic act blurb (Tier-0 resilient), never throws.
    - The generator makes zero Brain/network calls (grep source for fetch/http/brain returns 0 non-comment matches).
    - The "how" line carries a framework name + problem-type enum only (no user content).
  </acceptance_criteria>
  <done>The LOCAL JTBD-blurb generator produces the per-state what/helps/how blurb without Brain egress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the additive act row on the dial host (no MAX_K displacement, no 7th reach)</name>
  <files>lib/hmi/dial-reach-orchestrator.cjs, lib/hmi/dial-label-composer.cjs</files>
  <read_first>
    - lib/hmi/dial-reach-orchestrator.cjs:52-65 (DIAL_REACH_K=6 + OFFERED_CAP = ranker.MAX_K=3), :199-218 (buildReachList output shape: reaches + offered_count), :227-284 (buildReachList + the frozen-gate apply).
    - lib/hmi/dial-label-composer.cjs:47-124 (the 6 frozen template families; the hats family at :109 - the non-egress render-only family precedent for a render-only act blurb family).
    - lib/core/act-jtbd-blurb.cjs (Task 1 output).
    - docs/MINDRIAN-CANON.md Part 11 R4 + INV-19 (act is a PINNED additive row, NOT a reach_id; DIAL_REACH_K / MAX_K untouched).
  </read_first>
  <behavior>
    - Test 1: buildReachList output carries a pinned act row distinct from the ranked reaches (a separate field, e.g. pinned_suggestion).
    - Test 2: the pinned act row does NOT count against offered_count (MAX_K=3 ranked reaches are unchanged whether or not act is pinned).
    - Test 3: DIAL_REACH_K is still 6 and REACH_IDS still length 6 (act mints no 7th reach).
    - Test 4: the act row carries a render-only blurb (from buildActBlurb) with NO {framework} egress slot (mirroring the hats non-egress family).
  </behavior>
  <action>
    In lib/hmi/dial-reach-orchestrator.cjs, add a PINNED additive act standing-suggestion to the buildReachList output as a SEPARATE field (e.g. pinned_suggestion) - first OR last in the rendered set - that does NOT consume an offered_count slot (MAX_K=3 ranked reaches untouched) and is NOT added to REACH_IDS/REACH_DEFS (no 7th reach). In lib/hmi/dial-label-composer.cjs, add a render-only act blurb family (mirroring the hats non-egress family at :109 - no {framework} egress slot) that renders buildActBlurb's output. Keep DIAL_REACH_K=6 + MAX_K=3 + the frozen REACH_IDS untouched (constraint). Add the standing-suggestion test.
  </action>
  <verify>
    <automated>node tests/test-act-standing-suggestion.cjs</automated>
  </verify>
  <acceptance_criteria>
    - buildReachList output carries a pinned act suggestion as a separate field that does not count against MAX_K=3.
    - DIAL_REACH_K === 6 and REACH_IDS length === 6 are unchanged (no 7th reach minted).
    - The act row renders via a non-egress render-only family (no {framework} egress slot, like hats).
    - tests/test-act-standing-suggestion.cjs passes all four behaviors.
  </acceptance_criteria>
  <done>The pinned act row is always present, self-explaining, and does not touch the frozen reach contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| act blurb composition -> Brain | MUST NOT cross. The blurb is composed LOCALLY from JTBD/STATE/MINTO + framework-name enums; no Brain egress (Part 8). |
| dial render -> Brain | The dial host renders LOCAL strings; no Brain wire (Part 11 R7). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-18 | Information disclosure | act JTBD blurb | mitigate | The blurb family is render-only with no {framework} egress slot (the hats non-egress family precedent); buildActBlurb composes LOCALLY with no Brain call (Task 1 asserts zero Brain calls). |
| T-172-19 | Tampering | frozen reach contract | mitigate | Test 3 asserts DIAL_REACH_K===6 and REACH_IDS length 6; the pinned act row is a separate field, not a reach. |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; pure-Node dial + blurb edits. |
</threat_model>

<verification>
- node tests/test-act-standing-suggestion.cjs exit 0.
- DIAL_REACH_K=6 / MAX_K=3 / REACH_IDS length 6 unchanged.
- buildActBlurb makes zero Brain calls.
</verification>

<success_criteria>
/mos:act is an always-on, JTBD-self-explaining pinned dial suggestion that never displaces the ranked reaches and mints no 7th reach - INV-19 met.
</success_criteria>

<artifacts_this_phase_produces>
- NEW FILE: lib/core/act-jtbd-blurb.cjs (the LOCAL JTBD-blurb generator)
- NEW FILE: tests/test-act-standing-suggestion.cjs
- MODIFIED: the pinned additive act row in lib/hmi/dial-reach-orchestrator.cjs + the render-only act family in lib/hmi/dial-label-composer.cjs
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-09-SUMMARY.md` when done
</output>
