---
phase: 172-contextual-invocation-coverage
plan: 16
type: execute
wave: 7
depends_on: ["172-06", "172-12"]
files_modified:
  - commands/dashboard.md
  - commands/wiki.md
  - commands/present.md
  - commands/status.md
  - commands/room.md
  - commands/explain-decision.md
  - commands/speakers.md
  - commands/visualize.md
  - commands/query.md
  - data/connector-registry.json
  - data/connector-coverage-ledger.json
autonomous: true
requirements: [INV-01, INV-02, INV-03]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_modified: ["/mos:dashboard", "/mos:wiki", "/mos:present", "/mos:status", "/mos:room", "/mos:explain-decision", "/mos:speakers", "/mos:visualize", "/mos:query"]
  surfaces_added: []
  surfaces_removed: []
  spine_consumed: ["data/connector-registry.json", "data/connector-coverage-ledger.json", "scripts/build-connector-registry.cjs coverageReport()"]
  gate_impact: "Navigator-directed reclassification (2026-06-23): re-reads the room-presentation + Larry-explains + meeting-intelligence surfaces against MindrianOS current state (Part 10 Conversation-as-Product, Part 9 Larry-explains). 7 surfaces move EXCLUDE -> WIRE (they are reachable product, not utilities); 2 deprecated surfaces keep EXCLUDE with a corrected deprecated reason. Mints no new reach/edge/node/Brain-wire."
  explanation: "Corrects the Plan-06 over-exclusion: dashboard/wiki/present/status/room present the room to the navigator, explain-decision is Larry-explains (Part 9 role 4), speakers surfaces meeting intelligence (Decision 11). All WIRE with context_block + framework:null + sensor_triggers:[] (reachable/offerable, not necessarily auto-fired). visualize + query stay EXCLUDE but as DEPRECATED redirects, not utilities."
user_setup: []

must_haves:
  truths:
    - "7 surfaces (dashboard, wiki, present, status, room, explain-decision, speakers) are WIRED with a connector block (reach_id context_block, framework null, sensor_triggers []); they are no longer excluded."
    - "visualize + query remain EXCLUDED but their reason is corrected to DEPRECATED-redirect (visualize -> /mos:dashboard; query -> /mos:graph), not 'render utility'."
    - "After regen, the connector coverage ledger still has counts.gap === 2 OR fewer (only /mos:act + /mos:ingest-methodology may remain, both already wired by 08/12 - so gap should be 0); no NEW gap is introduced; no new reach/edge/node/Brain-wire minted."
  artifacts:
    - path: "data/connector-coverage-ledger.json"
      provides: "Regenerated ledger with the 7 surfaces flipped excluded -> wired; ~88 wired / ~36 excluded / 0 gap."
      contains: "wired"
    - path: "commands/dashboard.md"
      provides: "A WIRED connector block (context_block, framework null) replacing the excluded block."
      contains: "connects_to_spine"
  key_links:
    - from: "the 7 presentation/intelligence surfaces"
      to: "data/connector-coverage-ledger.json"
      via: "EXCLUDE -> WIRE reclassification"
      pattern: "wired"
---

<objective>
Apply the navigator-directed reclassification (2026-06-23, "ok"): re-read the room-presentation + Larry-explains + meeting-intelligence surfaces against MindrianOS current state and move the 7 that are reachable PRODUCT from EXCLUDE to WIRE; correct the 2 deprecated surfaces' reasons. This corrects the Plan-06 over-exclusion before the Plan-13 hard flip.

Purpose: In the current product (Part 10 Conversation-as-Product, Part 9 Larry-explains), the surfaces that SHOW the navigator their room (dashboard, wiki, present, status, room), EXPLAIN their state (explain-decision), or SURFACE meeting intelligence (speakers) are reachable product, not dead plumbing. They were over-excluded as "render utilities" in Plan 06. visualize + query are genuinely retired (redirects), so they stay EXCLUDE but with an honest DEPRECATED reason.

Output: 7 WIRE connector blocks + 2 corrected deprecated reasons + the regenerated registry/ledger. Autonomous - the navigator approved the exact set.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@docs/CONNECTOR-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reclassify the 7 presentation/intelligence surfaces EXCLUDE -> WIRE; correct 2 deprecated reasons</name>
  <files>commands/dashboard.md, commands/wiki.md, commands/present.md, commands/status.md, commands/room.md, commands/explain-decision.md, commands/speakers.md, commands/visualize.md, commands/query.md</files>
  <read_first>
    - docs/CONNECTOR-CONTRACT.md (the WIRE connector block sub-keys + the additive-degrade framework:null shape).
    - commands/rs-fetch.md (a WIRED template: connects_to_spine/sensor_triggers/reach_id/sub_mode/framework/posture/hierarchy_rank/filing/plan_gated/web_scope) + one of the Plan-06 framework:null WIRE surfaces (e.g. commands/graph.md or commands/memory.md) as the framework:null template.
    - the CURRENT excluded blocks in commands/dashboard.md, wiki.md, present.md, status.md, room.md, explain-decision.md, speakers.md (replace these) + visualize.md, query.md (keep excluded, fix reason).
  </read_first>
  <action>
    REPLACE the `connector:{excluded:true,reason}` block in each of these 7 with a WIRED connector block - connects_to_spine:true, sensor_triggers:[] (reachable/offerable, not auto-fired), reach_id:context_block, framework:null (additive-degrade - these are presentation/explain surfaces, not Brain frameworks), a distinct sub_mode, a posture (hold), a hierarchy_rank, filing:memory_event_only (or the contract default for a null-framework surface), plan_gated:false, web_scope:null:
      - commands/dashboard.md (sub_mode room-dashboard) - the room dashboard view
      - commands/wiki.md (sub_mode room-wiki) - the room as navigable wiki
      - commands/present.md (sub_mode room-present) - the 6-view room presentation
      - commands/status.md (sub_mode room-status) - governing thought per section + health
      - commands/room.md (sub_mode room-view) - view / navigate the Data Room
      - commands/explain-decision.md (sub_mode decision-explain) - Larry-explains a decision (Part 9 role 4)
      - commands/speakers.md (sub_mode meeting-speakers) - surfaces meeting intelligence (Decision 11)
    For commands/visualize.md and commands/query.md: KEEP connector:{excluded:true} but REPLACE the reason with an honest DEPRECATED reason ("Deprecated - redirects to /mos:dashboard --mermaid; scheduled removal v1.14.0" / "Deprecated - superseded by /mos:graph"). Use the EXACT sub-key spelling the connector generator reads (mirror rs-fetch.md). Mint no new reach/edge/node/Brain-wire; reach_id stays one of the frozen 6 (context_block). No em-dashes.
  </action>
  <verify>
    <automated>for f in dashboard wiki present status room explain-decision speakers; do grep -q "connects_to_spine: true" commands/$f.md || { echo "MISSING wire: $f"; exit 1; }; done; grep -qi "deprecat" commands/visualize.md && grep -qi "deprecat" commands/query.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The 7 surfaces each carry a WIRED connector block (connects_to_spine:true, reach_id:context_block, framework:null, sensor_triggers:[]).
    - visualize + query stay excluded:true with a DEPRECATED reason (not "render utility").
    - No new reach/edge/node/Brain-wire minted; reach_id is a frozen-6 value; no em-dashes.
  </acceptance_criteria>
  <done>The room-presentation + Larry-explains + meeting surfaces are WIRE; the deprecated pair is honestly labeled.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate registry + ledger; verify no new gap, 7 flipped to wired</name>
  <files>data/connector-registry.json, data/connector-coverage-ledger.json</files>
  <read_first>
    - scripts/build-connector-registry.cjs (the generator + --check).
    - data/connector-coverage-ledger.json (the rolling ledger - confirm the 7 flip excluded -> wired and no NEW gap appears).
  </read_first>
  <action>
    Run `node scripts/build-connector-registry.cjs` to regenerate the registry + coverage ledger. Confirm the 7 surfaces are state:wired, visualize+query remain excluded, and counts.gap did NOT increase (it should be 0, since act + ingest-methodology were wired by Plans 08/12). If a harness-manifest STALE tripwire fires, run `node scripts/build-harness-manifest.cjs` and stage it in the same commit. Do NOT hand-edit the JSON. Report the new wired/excluded/gap split in the SUMMARY.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs && node scripts/build-connector-registry.cjs --check && node -e "const l=require('./data/connector-coverage-ledger.json'); const arr=l.surfaces||[]; const w=new Set(arr.filter(s=>s.state==='wired').map(s=>s.surface)); const need=['/mos:dashboard','/mos:wiki','/mos:present','/mos:status','/mos:room','/mos:explain-decision','/mos:speakers']; const miss=need.filter(n=>!w.has(n)); if(miss.length){console.log('NOT wired:',miss.join(','));process.exit(1)} if(l.counts.gap>0){console.log('gap regressed:',l.counts.gap);process.exit(1)} console.log('wired:',l.counts.wired,'excluded:',l.counts.excluded,'gap:',l.counts.gap)"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-connector-registry.cjs --check` exits 0.
    - The 7 surfaces are state:wired; counts.gap === 0 (no regression).
    - The SUMMARY records the new wired/excluded/gap split.
  </acceptance_criteria>
  <done>The corrected classification is registry-reflected; gap stays 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connector block -> ledger | The blocks carry only frozen connector enums + a null framework; no user content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-50 | Tampering | an over-eager WIRE | mitigate | The navigator approved the exact 7-surface set (2026-06-23); each WIRE is a presentation/explain/meeting surface with a clear current-state rationale. |
| T-172-51 | Information disclosure | connector metadata | accept | reach_id/sub_mode/posture/rank are generic machinery enums; framework:null; no user content. |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; frontmatter + a generator run. |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check exit 0; the 7 are wired; gap === 0.
- No new reach/edge/node/Brain-wire; reach_id frozen-6; no em-dashes.
</verification>

<success_criteria>
The room-presentation + Larry-explains + meeting-intelligence surfaces are WIRE (reachable product, not utilities), visualize+query are honestly deprecated, and the coverage ledger stays gap=0 - the corrected baseline for the Plan-13 hard flip.
</success_criteria>

<artifacts_this_phase_produces>
- MODIFIED: 7 command frontmatter EXCLUDE -> WIRE connector blocks (dashboard, wiki, present, status, room, explain-decision, speakers)
- MODIFIED: visualize.md + query.md exclude reasons -> DEPRECATED
- REGENERATED: data/connector-registry.json, data/connector-coverage-ledger.json
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-16-SUMMARY.md` when done
</output>
