---
phase: 172-contextual-invocation-coverage
plan: 04
type: execute
wave: 2
depends_on: ["172-01"]
files_modified:
  - commands/rs-fetch.md
  - commands/rs-explain.md
  - commands/rs-experts.md
  - commands/rs-thesis.md
  - data/connector-registry.json
  - data/connector-coverage-ledger.json
autonomous: true
requirements: [INV-02]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: ["/mos:rs-fetch", "/mos:rs-explain", "/mos:rs-experts", "/mos:rs-thesis"]
  surfaces_removed: []
  spine_consumed: ["data/connector-registry.json", "scripts/build-connector-registry.cjs"]
  gate_impact: "Moves the entire reverse-salient rs-* family from half-wired (frameworks: but no connector:) to WIRED, shrinking the gap count in the coverage ledger by 4. R1 conformance for the Engine-1 pillar family."
  explanation: "This plan wires the rs-* family FIRST (Engine-1 pillar, currently dark) by adding connector: blocks; it consumes the connector spine + the coverage ledger but mints no reach/edge/node."
user_setup: []

must_haves:
  truths:
    - "Each rs-* command (fetch/explain/experts/thesis) carries a connector: block with reach_id context_block, sub_mode reverse-salient, posture pull_back."
    - "The connector registry includes the four rs-* surfaces and the coverage ledger reflects them as wired."
    - "build-connector-registry.cjs --check passes with the rs-* family wired."
  artifacts:
    - path: "commands/rs-fetch.md"
      provides: "A connector: block wiring rs-fetch into the reverse-salient reach."
      contains: "reach_id: context_block"
    - path: "data/connector-registry.json"
      provides: "Regenerated registry including the four rs-* connectors."
      contains: "rs-fetch"
  key_links:
    - from: "commands/rs-fetch.md connector block"
      to: "data/connector-registry.json"
      via: "build-connector-registry.cjs regeneration"
      pattern: "rs-fetch"
    - from: "rs-* connector reach_id"
      to: "lib/core/sensors/sensor-types.cjs REACH_IDS"
      via: "context_block (frozen 6)"
      pattern: "context_block"
---

<objective>
Wire the reverse-salient rs-* family (rs-fetch, rs-explain, rs-experts, rs-thesis) into the reach spine - FIRST among the half-wired set because Reverse Salient is a Canon Engine-1 pillar yet the entire family is currently dark (INV-02, D-172-c).

Purpose: The rs-* family ships a frameworks: block ("Reverse Salient Analysis") but no connector: block, so the engine never reaches for it contextually. This is the single largest thinking-surface gap and the Engine-1 priority.

Output: four connector: blocks, the regenerated connector registry, and the updated coverage ledger.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@docs/CONNECTOR-CONTRACT.md
@.planning/phases/172-contextual-invocation-coverage/research/recon-connector-spine.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add connector: blocks to the four rs-* commands</name>
  <files>commands/rs-fetch.md, commands/rs-explain.md, commands/rs-experts.md, commands/rs-thesis.md</files>
  <read_first>
    - docs/CONNECTOR-CONTRACT.md section 6 "PUSH-02 -- reverse-salient (find-bottlenecks)" worked example (the EXACT template for reach_id context_block + sub_mode reverse-salient + posture pull_back + framework "Reverse Salient Analysis") + section 7 (the delimiter convention "# --- Phase 143.3 connector frontmatter ---").
    - commands/find-bottlenecks.md (the live analog that already carries this connector block + fires SENS-02 via sensor-lagging-component.cjs).
    - commands/rs-fetch.md / rs-explain.md / rs-experts.md / rs-thesis.md current frontmatter (each has frameworks: ["Reverse Salient Analysis"] but no connector: - the half-wired state).
    - lib/core/sensors/sensor-lagging-component.cjs:49-59 (LAGGING_PATTERNS) + :82-111 (the SENS-02 reverse-salient sensor that already fires context_block - the trigger these connectors hang on).
  </read_first>
  <action>
    To each of the four rs-* command files, append a `# --- Phase 143.3 connector frontmatter ---` delimited connector: block with: connects_to_spine: true; sensor_triggers: [SENS-02] (the shipped reverse-salient sensor); reach_id: context_block (frozen 6 - mint no new reach); sub_mode: reverse-salient; framework: "Reverse Salient Analysis" (EXACT match to the existing frameworks: value, the WFL-01 resolver key); posture: pull_back (per D-172-c); hierarchy_rank: assign distinct ranks (rs-fetch, rs-explain, rs-experts, rs-thesis each a distinct integer so the no-duplicate-tuple check passes - vary sub_mode OR rank to avoid the (sensor,reach,sub_mode) collision per validateConnectors check 4; use distinct sub_modes rs-fetch/rs-explain/rs-experts/rs-thesis under the reverse-salient family if rank alone is insufficient); filing: fileEvidenceWithReadback; plan_gated: false; web_scope: null; surface: F.1. Honor the additive-degrade rule - declare only relevant keys. No fenced code in the command body; the connector block IS frontmatter YAML.
  </action>
  <verify>
    <automated>for f in rs-fetch rs-explain rs-experts rs-thesis; do grep -q "reach_id: context_block" commands/$f.md && grep -q "connects_to_spine: true" commands/$f.md || exit 1; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All four rs-* command files contain a connector: block with `connects_to_spine: true` and `reach_id: context_block`.
    - Each carries `framework: "Reverse Salient Analysis"` matching its existing frameworks: value.
    - Each carries `posture: pull_back`.
    - No two rs-* connectors collide on (sensor_triggers, reach_id, sub_mode) (distinct sub_mode or the validateConnectors tuple check would fail).
  </acceptance_criteria>
  <done>The rs-* family declares connector blocks wiring it into the reverse-salient reach.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the connector registry + coverage ledger, verify --check</name>
  <files>data/connector-registry.json, data/connector-coverage-ledger.json</files>
  <read_first>
    - scripts/build-connector-registry.cjs:614-651 (the --check branch: STALE byte-compare + the four CONN-03 validations + the WARN nudge).
    - data/connector-coverage-ledger.json (the Plan 172-01 output the regeneration updates - rs-* should move from gap to wired).
  </read_first>
  <action>
    Run `node scripts/build-connector-registry.cjs` to regenerate data/connector-registry.json AND data/connector-coverage-ledger.json with the four rs-* connectors now wired. Confirm `--check` passes (frozen-6 reach, frozen-3 posture, WFL-01 framework resolution for "Reverse Salient Analysis", no tuple collision). The gap count in the coverage ledger drops by exactly 4 (the rs-* family). Do NOT hand-edit either JSON - they are generated.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs && node scripts/build-connector-registry.cjs --check && node -e "const l=require('./data/connector-coverage-ledger.json'); const w=l.surfaces.filter(s=>['/mos:rs-fetch','/mos:rs-explain','/mos:rs-experts','/mos:rs-thesis'].includes(s.surface)&&s.state==='wired'); if(w.length!==4)process.exit(1); console.log('rs-* wired:',w.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-connector-registry.cjs --check` exits 0.
    - data/connector-registry.json contains connectors for /mos:rs-fetch, /mos:rs-explain, /mos:rs-experts, /mos:rs-thesis.
    - All four rs-* surfaces show state:wired in data/connector-coverage-ledger.json.
    - The "Reverse Salient Analysis" framework resolves via the WFL-01 check (no unresolvable-framework error).
  </acceptance_criteria>
  <done>The registry and ledger reflect the rs-* family as wired and the gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connector frontmatter -> registry | The connector block carries only generic handles (framework name, reach_id, sensor ids); the generator is structurally incapable of carrying user content. |
| connector framework value -> Brain | Only the build-time --refresh-names read touches the Brain (read-only generic :Framework names); not invoked in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-07 | Information disclosure | rs-* connector blocks | accept | Connector sub-keys are frozen-vocabulary enums + a published-methodology name + a render label; no user content path exists (CONNECTOR-CONTRACT.md section 5). |
| T-172-08 | Tampering | duplicate trigger tuple | mitigate | validateConnectors check 4 fails on a (sensor,reach,sub_mode) collision; Task 1 assigns distinct sub_modes/ranks. |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; frontmatter edits + a generator run only. |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check exit 0.
- All four rs-* surfaces state:wired in the coverage ledger.
</verification>

<success_criteria>
The reverse-salient rs-* family - the Engine-1 pillar that was entirely dark - is contextually triggerable via context_block/pull_back, the gate passes, and the coverage gap shrinks by 4.
</success_criteria>

<artifacts_this_phase_produces>
- MODIFIED: connector: blocks on commands/rs-fetch.md, rs-explain.md, rs-experts.md, rs-thesis.md
- REGENERATED: data/connector-registry.json, data/connector-coverage-ledger.json (4 surfaces flipped gap -> wired)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-04-SUMMARY.md` when done
</output>
