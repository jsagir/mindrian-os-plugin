---
phase: 172-contextual-invocation-coverage
plan: 07
type: execute
wave: 2
depends_on: []
files_modified:
  - lib/core/insight-sensors.cjs
  - lib/core/sensors/sensor-types.cjs
  - tests/test-context-driven-trigger.cjs
  - docs/172-SYSTEMS-MODEL.md
autonomous: true
requirements: [INV-07, INV-23]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["lib/core/insight-sensors.cjs dispatchSensors", "lib/core/navigation.cjs (LOCAL problem-state)", "commands/systems-thinking.md (already-wired connector)"]
  gate_impact: "Establishes the R3 context-driven trigger tier (problem-state via navigation.cjs) with keyword demoted to fallback, and confirms /mos:systems-thinking is CIRS-conformant. No new gap; quality of triggering improves."
  explanation: "This plan implements R3 (context-triggered, keyword=fallback) at the dispatchSensors layer and records the Meadows systems model of the invocation surface (INV-23). It consumes the sensor spine + the navigation.cjs chokepoint; mints no reach."
user_setup: []

must_haves:
  truths:
    - "Sensors prefer navigator problem-state (stage/JTBD/graph-gap) read LOCALLY via navigation.cjs; keyword match is a fallback tier, not the basis (R3/INV-07)."
    - "The trigger model carries a documented tier order: context-signal first, keyword fallback second."
    - "/mos:systems-thinking is confirmed CIRS-conformant (R1 wired) and the invocation surface is recorded as a Meadows systems model (INV-23)."
  artifacts:
    - path: "lib/core/insight-sensors.cjs"
      provides: "A context-first / keyword-fallback tiering in the dispatch/normalize path, reading problem-state locally."
      contains: "fallback"
    - path: "docs/172-SYSTEMS-MODEL.md"
      provides: "The Meadows systems model: stocks/flows/feedback/leverage/delays/hierarchy of the invocation surface (INV-23/D-172-n)."
      contains: "balancing loop"
  key_links:
    - from: "lib/core/insight-sensors.cjs"
      to: "navigation.cjs problem-state"
      via: "LOCAL enum/scalar read"
      pattern: "navigation"
    - from: "trigger tiering"
      to: "keyword fallback"
      via: "context-signal-first precedence"
      pattern: "fallback"
---

<objective>
Implement R3 / INV-07: make contextual triggering context-driven (navigator problem-state read LOCALLY via navigation.cjs - stage / JTBD / graph-gap) with keyword match demoted to a fallback tier, and record the invocation surface as a Meadows systems model (INV-23 / D-172-n) while confirming /mos:systems-thinking itself is CIRS-conformant.

Purpose: Pure keyword/embedding routing collapses past ~100 tools (124 surfaces is in the collapse zone). The Context-aware FSM pattern keys on room conditions. Today most sensors lead with keyword/lexicon matching; INV-07 demotes that to fallback behind the LOCAL problem-state signal. INV-23 requires the gate to be designed AS the balancing loop at the highest-leverage point.

Output: a context-first/keyword-fallback tiering in the sensor dispatch path, a documented systems model, and a context-driven-trigger test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@.planning/phases/172-contextual-invocation-coverage/research/recon-connector-spine.md
@docs/MINDRIAN-CANON.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tier the trigger model - context problem-state first, keyword fallback</name>
  <files>lib/core/insight-sensors.cjs, lib/core/sensors/sensor-types.cjs</files>
  <read_first>
    - lib/core/insight-sensors.cjs:171-256 (normalizeTurn + deriveTurnSignals - the one-seam turn normalization; the place a context-tier signal joins), :367-383 (SENSOR_REGISTRY order), :401-429 (dispatchSensors).
    - lib/core/sensors/sensor-types.cjs:80-120 (makeReach - the pure factory; the evidence object carries the fire MODE enum). Look at how sensor-diffusion-adoption.cjs:126-156 already records a `mode` ('keyword'|'marker'|'signal') in evidence - that mode enum is the precedent for tiering.
    - lib/core/navigation.cjs (the 13-function LOCAL chokepoint - read problem-state/stage/JTBD via it; enum/scalar only, Part 8/9; do NOT add a non-SQLite read).
    - lib/core/sensors/sensor-diffusion-adoption.cjs:113-156 (the problem_type-off-tuple read + the mode enum - the analog for demoting keyword to fallback that Plan 09 will extend for SENS-09).
  </read_first>
  <behavior>
    - Test 1: when a LOCAL problem-state signal is present, the dispatch records mode 'signal'/'context' (the context tier) and prefers it over keyword.
    - Test 2: when only keyword/lexicon matches (no problem-state), the dispatch records mode 'keyword' (the fallback tier) and still fires.
    - Test 3: the evidence object carries only enum/scalar (the mode enum + the problem-state enum), never the matched user text (Part 8).
    - Test 4: dispatchSensors never mutates routing_source and never calls decide() (the Phase 144 fence preserved).
  </behavior>
  <action>
    In the sensor dispatch/normalize path, establish a documented TIER ORDER: a context tier (problem-state stage/JTBD/graph-gap read LOCALLY via navigation.cjs, enum/scalar only) is PREFERRED; keyword/lexicon match is a FALLBACK tier recorded with a `mode` enum (mirroring sensor-diffusion-adoption's mode field). Surface a shared helper (e.g. on deriveTurnSignals or a small sensor-types utility) that exposes the LOCAL problem-state enum so individual sensors can prefer it. Do NOT mint a new reach or rewrite every sensor - establish the tiering seam + the mode enum convention and apply it where the turn signals are derived; per-sensor adoption (e.g. SENS-09) is Plan 09. Keep makeReach + the frozen banks untouched. Preserve the dispatchSensors fence (no routing_source mutation, no decide() call).
  </action>
  <verify>
    <automated>node tests/test-context-driven-trigger.cjs</automated>
  </verify>
  <acceptance_criteria>
    - The sensor dispatch path records a `mode` enum distinguishing the context tier from the keyword fallback tier.
    - The LOCAL problem-state read goes through navigation.cjs (enum/scalar only); grep shows no new non-SQLite filesystem read added to the hot path.
    - dispatchSensors still never mutates routing_source and never calls decide().
    - The evidence object carries no matched user text (only enums/scalars).
  </acceptance_criteria>
  <done>The trigger model is context-first with keyword as a recorded fallback tier.</done>
</task>

<task type="auto">
  <name>Task 2: Record the Meadows systems model + confirm systems-thinking CIRS-conformance</name>
  <files>docs/172-SYSTEMS-MODEL.md, tests/test-context-driven-trigger.cjs</files>
  <read_first>
    - .planning/phases/172-contextual-invocation-coverage/172-SPEC.md INV-23 (the full STOCKS/FLOWS/FEEDBACK/LEVERAGE/DELAYS/HIERARCHY model) + 172-CONTEXT.md D-172-n.
    - commands/systems-thinking.md (the already-present connector block: reach_id context_block, sub_mode systems-thinking-loop, framework "Systems Thinking" - confirm it is R1-wired, no change needed unless --check flags it).
    - docs/CONNECTOR-CONTRACT.md (to confirm the systems-thinking connector is conformant).
  </read_first>
  <action>
    Author docs/172-SYSTEMS-MODEL.md recording the invocation surface AS a Meadows system per INV-23: STOCKS (dark/wired/excluded surface counts, un-ranked counterparts, placeholder chains); FLOWS (surfaces born/modified/removed, wired/excluded, chains earned); FEEDBACK (the coverage gate is the BALANCING loop holding the dark-surface stock at zero; the prior regression was a broken loop - WARN-only + CI-orphaned = weak feedback + long delay); LEVERAGE POINTS (the born-wired hard gate is Meadows #5 rules / #4 self-organization, NOT a parameter tweak); DELAYS (the gate fires at MERGE not audit-time); HIERARCHY (the fractal rollup is Simon near-decomposability). Confirm /mos:systems-thinking is CIRS-conformant (its connector block is R1-wired) - add a test assertion in tests/test-context-driven-trigger.cjs that the systems-thinking connector appears in the registry with connects_to_spine:true. No em-dashes.
  </action>
  <verify>
    <automated>grep -q "balancing loop" docs/172-SYSTEMS-MODEL.md && grep -q "leverage" docs/172-SYSTEMS-MODEL.md && node tests/test-context-driven-trigger.cjs</automated>
  </verify>
  <acceptance_criteria>
    - docs/172-SYSTEMS-MODEL.md documents all six Meadows dimensions (stocks/flows/feedback/leverage/delays/hierarchy) of the invocation surface.
    - The doc names the coverage gate as the balancing loop at the highest-leverage point (the rule, not the parameter).
    - tests/test-context-driven-trigger.cjs asserts /mos:systems-thinking is registry-wired (CIRS-conformant).
    - No em-dashes in docs/172-SYSTEMS-MODEL.md.
  </acceptance_criteria>
  <done>The systems model is recorded and systems-thinking conformance is fenced - INV-23 met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sensor dispatch -> navigation.cjs | Problem-state read is LOCAL enum/scalar via the navigation chokepoint; no user content. |
| sensor evidence -> Brain | MUST NOT cross. Evidence carries only the mode enum + problem-state enum, never matched user text (Part 8). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-13 | Information disclosure | sensor evidence object | mitigate | Test 3 asserts the evidence carries only enums/scalars, never the matched user text; the makeReach Part-8 contract is preserved. |
| T-172-14 | Tampering | hot-path filesystem read | mitigate | The problem-state read goes through navigation.cjs (the SQL chokepoint); the acceptance test asserts no new non-SQLite read on the dispatch path (Part 9 instrumentation idiom). |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; pure-Node sensor edit + a doc. |
</threat_model>

<verification>
- node tests/test-context-driven-trigger.cjs exit 0.
- docs/172-SYSTEMS-MODEL.md covers the six Meadows dimensions.
- dispatchSensors fence (no routing_source mutation / no decide() call) preserved.
</verification>

<success_criteria>
Triggering is context-first with keyword as a recorded fallback tier (R3/INV-07), the invocation surface is modeled as a Meadows balancing loop (INV-23), and systems-thinking is confirmed CIRS-conformant.
</success_criteria>

<artifacts_this_phase_produces>
- NEW FILE: docs/172-SYSTEMS-MODEL.md (the Meadows model of the invocation surface)
- NEW FILE: tests/test-context-driven-trigger.cjs
- MODIFIED: the context-first/keyword-fallback tiering seam + mode enum in lib/core/insight-sensors.cjs
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-07-SUMMARY.md` when done
</output>
