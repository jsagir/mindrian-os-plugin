---
phase: 172-contextual-invocation-coverage
plan: 08
type: execute
wave: 5
depends_on: ["172-06"]
files_modified:
  - commands/act.md
  - scripts/act-command.cjs
  - commands/pipeline.md
  - data/connector-registry.json
  - data/connector-coverage-ledger.json
  - tests/test-act-governed-selection.cjs
autonomous: false
requirements: [INV-18, INV-20, INV-21]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: ["/mos:act", "/mos:pipeline"]
  surfaces_removed: []
  spine_consumed: ["lib/core/navigation-engine.cjs decide()", "lib/core/chain-executor.cjs runChain", "lib/hmi/shape-f1-renderer.cjs", "data/connector-registry.json"]
  gate_impact: "Collapses /mos:act to ONE governed selection brain (R4): adds an act connector block (autonomous_safe false), feeds the real decide() as decideFn (drops ()=>null), unifies act's gate onto the Shape F.1 host, adds the intent-calibration step, and classifies /mos:pipeline + act --swarm wired-or-excluded."
  explanation: "This plan makes /mos:act spine-GOVERNED, F.1-RENDERED, and intent-CALIBRATED (R4). It consumes decide()/runChain/the F.1 renderer; mints no reach. autonomous_safe stays false (navigator-gated), hence autonomous:false."
user_setup: []

must_haves:
  truths:
    - "/mos:act carries a connector block (autonomous_safe false) so the spine can surface it; pipeline + act --swarm are each wired-or-excluded under the gate."
    - "act --chain feeds the REAL navigation-engine decide() as its decideFn (the ()=>null at act-command.cjs:219 is gone) - one selection authority."
    - "act's option gate renders on the canonical Shape F.1 host (the bespoke body_shape:E yes/pick/cancel prose is replaced), and act runs an internal intent-calibration step before it selects or executes."
  artifacts:
    - path: "scripts/act-command.cjs"
      provides: "decideFn = the real decide() (no longer () => null)."
      contains: "decideFn"
    - path: "commands/act.md"
      provides: "A connector block + the F.1-rendered gate + the intent-calibration step (replacing the yes/pick/cancel prose)."
      contains: "connector:"
  key_links:
    - from: "scripts/act-command.cjs"
      to: "lib/core/navigation-engine.cjs decide()"
      via: "decideFn passed to runChain"
      pattern: "decide"
    - from: "commands/act.md gate"
      to: "lib/hmi/shape-f1-renderer.cjs"
      via: "Shape F.1 AskUserQuestion host"
      pattern: "F.1"
---

<objective>
Collapse /mos:act to ONE governed selection brain (R4 / INV-18): add an act connector block (autonomous_safe stays false), feed the REAL navigation-engine decide() as act's decideFn (drop the ()=>null at act-command.cjs:219), unify act's option gate onto the canonical Shape F.1 host (INV-20), add act's internal intent-calibration step (INV-21), and classify /mos:pipeline + act --swarm wired-or-excluded.

Purpose: /mos:act is a SECOND ungoverned invocation brain - dark to dispatchSensors, and act --chain plans its own chain off recommendFrameworkChain instead of the spine's decide(). 166 gave act ONE governed EXECUTION loop; this closes the SELECTION gap so there is one selection authority everywhere, made legible (F.1) and safe (calibration).

Output: the act connector block, the real decideFn, the F.1-rendered gate, the calibration step, the pipeline classification, and the governed-selection test. Navigator-gated (autonomous:false) because autonomous_safe stays false and the act surface is a material reconciliation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@docs/CONNECTOR-CONTRACT.md
@.planning/phases/172-contextual-invocation-coverage/research/recon-internal-synthesis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the act connector block + classify pipeline/swarm; feed real decide() as decideFn</name>
  <files>commands/act.md, scripts/act-command.cjs, commands/pipeline.md</files>
  <read_first>
    - scripts/act-command.cjs:160-224 (buildRunChainPlan - the runChain delegation; :219 decideFn: function(){return null;} - the ()=>null to replace), :213-221 (the runChain callback set).
    - lib/core/navigation-engine.cjs:596 (decide()) + :427-477 (resolveFireSkill) - decide() is the spine's selection authority act must consume.
    - commands/act.md:1-20 (frontmatter, autonomous_safe:false at :14, body_shape:E at :6) + :185-196 (the Standard Mode yes/pick another/cancel prose gate).
    - commands/ignite.md:21 (the WIRED sibling connector block - the template for act's connector) + commands/pipeline.md (currently dark like act).
    - docs/CONNECTOR-CONTRACT.md section 4 additive-degrade (act is a meta-orchestrator: connector with framework:null + filing:memory_event_only is the legal degrade shape, like the memory-cortex reach) + Canon Part 11 R4/INV-18.
  </read_first>
  <action>
    (a) Add a `# --- Phase 172 CIRS connector ---` connector block to commands/act.md: connects_to_spine:true; sensor_triggers a sensible shipped SENS id (or [] - act is a standing meta-orchestrator); reach_id one of the frozen 6 (brain_consult or context_block - act surfaces a selection, NOT a 7th reach); framework:null (act is a meta-orchestrator, additive-degrade); filing:memory_event_only; posture hold; surface F.1; KEEP autonomous_safe:false in act's own frontmatter (the spine OFFERS it, the navigator confirms). (b) In scripts/act-command.cjs, replace `decideFn: function () { return null; }` with the REAL navigation-engine decide() wired as decideFn so each runChain step's next-reach comes from the SAME spine (import decide from lib/core/navigation-engine.cjs; adapt its signature to the decideFn contract runChain expects). (c) Classify /mos:pipeline and act --swarm: wire /mos:pipeline with a connector block (it is a sibling orchestrator) OR exclude it with a reason; for act --swarm (a sub-mode of act, not a separate file) record the WIRE-or-EXCLUDE decision in act.md's connector notes. Mint no reach/edge/node.
  </action>
  <verify>
    <automated>grep -q "connector:" commands/act.md && ! grep -q "function () { return null; }" scripts/act-command.cjs && (grep -q "connector:" commands/pipeline.md || grep -q "excluded: true" commands/pipeline.md) && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - commands/act.md carries a connector: block with connects_to_spine:true and a frozen-6 reach_id; autonomous_safe:false is preserved.
    - scripts/act-command.cjs no longer contains `decideFn: () => null` / `function () { return null; }` - it feeds the real decide().
    - commands/pipeline.md is either wired (connector block) or excluded (excluded:true + reason).
    - act --swarm's WIRE-or-EXCLUDE decision is recorded in act.md.
  </acceptance_criteria>
  <done>act has one governed SELECTION authority (the real decide()); pipeline/swarm are classified.</done>
</task>

<task type="auto">
  <name>Task 2: Unify act's gate onto the Shape F.1 host + add the intent-calibration step</name>
  <files>commands/act.md</files>
  <read_first>
    - commands/act.md:185-196 (the bespoke body_shape:E "yes / pick another / cancel" prose gate to REPLACE).
    - lib/hmi/shape-f1-renderer.cjs (the shipped F.1 renderer + the FROZEN F.1 keyboard contract: UP/DOWN option nav + SIDE toggle) + lib/hmi/reach-component-map.json (the toggleable archetype components).
    - docs/MINDRIAN-CANON.md Part 3 (Shape F + the 3-layer loop; the frozen F.1 keyboard contract, MAX_K=3, DIAL_REACH_K=6) + Part 11 R4 (does not touch the Part 3 render contract) + INV-20 / INV-21.
    - the discuss-phase pattern referenced by INV-21 (the lightweight internal discuss / calibration the act intent step reuses) - look at how other commands run a short calibration before acting.
  </read_first>
  <action>
    In commands/act.md: (a) REPLACE the body_shape:E yes/pick another/cancel prose gate (act.md:191-196) with the canonical Shape F.1 host - act's option/next-step presentation renders via the AskUserQuestion Shape F.1 primitive (lib/hmi/shape-f1-renderer.cjs), honoring the FROZEN F.1 keyboard contract (UP/DOWN + SIDE toggle), never a hand-rolled selector (INV-20, Phase-148 unify-onto-F.1 contract). Update body_shape accordingly. (b) Add an INTENT-CALIBRATION step (INV-21): before act SELECTS or EXECUTES, it runs a short internal discuss/calibration (reusing the discuss-phase pattern + the F.1 gate) that confirms the navigator's actual intent (what they want act to do in this room state, scope, constraints, definition of done) BEFORE routing through F.1 -> decide() -> runChain. Calibration reads LOCAL state only (active JTBD + STATE.md + MINTO.md; no Brain egress, Part 8); the intent is journaled as a memory_event via navigation.cjs (Part 9) so the run is auditable. The post-gate handoff: calibrate -> approve -> auto-run autonomous_safe prefix -> halt at first material step. Keep MAX_K=3 / DIAL_REACH_K=6 / the F.1 keyboard contract FROZEN (constraint). No fenced code blocks in the command body prose.
  </action>
  <verify>
    <automated>grep -q "F.1" commands/act.md && grep -qi "calibrat" commands/act.md && ! grep -q "yes / pick another / cancel" commands/act.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - commands/act.md renders its gate via the Shape F.1 host (references shape-f1-renderer.cjs / AskUserQuestion) and no longer contains the "yes / pick another / cancel" bespoke prose.
    - commands/act.md describes an internal intent-calibration step that runs BEFORE selection/execution and reads LOCAL state only (no Brain egress).
    - The calibration outcome is journaled as a memory_event via navigation.cjs (Part 9).
    - MAX_K=3 / DIAL_REACH_K=6 / the F.1 keyboard contract are unchanged (no frozen-contract edit).
  </acceptance_criteria>
  <done>act renders on the F.1 host and calibrates intent before acting - legible and safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regenerate registry + ledger; governed-selection test</name>
  <files>data/connector-registry.json, data/connector-coverage-ledger.json, tests/test-act-governed-selection.cjs</files>
  <read_first>
    - scripts/build-connector-registry.cjs:614-651 (the --check branch).
    - scripts/act-command.cjs (Task 1 output - the real decideFn).
    - tests/test-diffusion-adoption-sensor.cjs (the sensor/connector test idiom to mirror).
  </read_first>
  <behavior>
    - Test 1: /mos:act appears in data/connector-registry.json with connects_to_spine:true.
    - Test 2: scripts/act-command.cjs source contains no `() => null` / `function () { return null; }` decideFn (the second selection brain is gone).
    - Test 3: act's decideFn references the real decide() import.
    - Test 4: /mos:pipeline is wired-or-excluded in the coverage ledger (not gap).
  </behavior>
  <action>
    Run `node scripts/build-connector-registry.cjs` to regenerate the registry + ledger with act (and pipeline) classified. Write tests/test-act-governed-selection.cjs covering the four behaviors (source-grep + registry assertions, zero subprocess to a live Brain). Confirm `--check` exits 0.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs && node scripts/build-connector-registry.cjs --check && node tests/test-act-governed-selection.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-connector-registry.cjs --check` exits 0.
    - `node tests/test-act-governed-selection.cjs` exits 0 with all four behaviors passing.
    - /mos:act is state:wired and /mos:pipeline is wired-or-excluded in the coverage ledger.
  </acceptance_criteria>
  <done>The one-governed-selection-brain reconciliation is registry-reflected and test-fenced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| act calibration -> Brain | MUST NOT cross. Calibration reads LOCAL JTBD/STATE/MINTO only; no Brain egress (Part 8). |
| act decideFn -> decide() | decide() reads LOCAL context; feeding it as decideFn opens no new Brain wire (Part 11 R7). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-15 | Elevation of privilege | act autonomy | mitigate | autonomous_safe stays false; the F.1 calibration gate confirms intent BEFORE act acts; the run halts at the first material step (Part 3 Decision Gate). Plan is autonomous:false (navigator-gated). |
| T-172-16 | Information disclosure | calibration inputs | mitigate | Calibration reads LOCAL state only; the memory_event journal carries enum/scalar intent, never the venture content (Part 9 typed packet). |
| T-172-17 | Spoofing | second selection brain | mitigate | Test 2 asserts the ()=>null decideFn is gone, so act cannot run an ungoverned selection path. |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; act.md + act-command.cjs edits + a generator run. |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check exit 0.
- node tests/test-act-governed-selection.cjs exit 0.
- MAX_K=3 / DIAL_REACH_K=6 / F.1 keyboard contract unchanged.
- NAVIGATOR GATE: act reconciliation reviewed before merge (autonomous:false).
</verification>

<success_criteria>
/mos:act is spine-GOVERNED (real decide()), F.1-RENDERED, and intent-CALIBRATED; pipeline/swarm are classified; the second selection brain is gone - R4/INV-18/20/21 met.
</success_criteria>

<artifacts_this_phase_produces>
- MODIFIED: commands/act.md (connector block + F.1 gate + calibration step)
- MODIFIED: scripts/act-command.cjs (real decideFn replacing ()=>null)
- MODIFIED/CLASSIFIED: commands/pipeline.md (wired or excluded)
- NEW FILE: tests/test-act-governed-selection.cjs
- REGENERATED: data/connector-registry.json, data/connector-coverage-ledger.json
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-08-SUMMARY.md` when done
</output>
