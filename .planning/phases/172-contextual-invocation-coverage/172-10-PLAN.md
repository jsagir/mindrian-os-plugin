---
phase: 172-contextual-invocation-coverage
plan: 10
type: execute
wave: 3
depends_on: ["172-03", "172-05"]
files_modified:
  - data/command-registry.json
  - data/brain-orchestration-projection.json
  - lib/workflow/local-chain-recommender.cjs
  - tests/test-curated-chains-ranking.cjs
autonomous: true
requirements: [INV-08]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["data/command-registry.json curated_chains", "data/brain-orchestration-projection.json", "scripts/build-orchestration-projection.cjs"]
  gate_impact: "Populates curated FEEDS_INTO confidences (R6), regenerates the projection's chain layer, and ranks suggest-next off the LOCAL projection. Earned (not uniform/absent) confidence becomes the rule; R6 enforcement stays warn/aspirational (deferred-enforcement)."
  explanation: "This plan implements R6/INV-08: curated chain confidences on the PROJECTION FEEDS_INTO (not the navigation edge), surfaced via the LOCAL projection in suggest-next. It consumes the projection generator + command-registry; mints no edge type."
user_setup: []

must_haves:
  truths:
    - "data/command-registry.json curated_chains carries curated-confidence FEEDS_INTO edges (no longer an empty array)."
    - "The regenerated brain-orchestration-projection.json materializes FEEDS_INTO chain edges from curated_chains; confidence is earned, never uniform/absent."
    - "suggest-next ranks chain candidates off the LOCAL projection (no live Brain at rank time, INV-12)."
  artifacts:
    - path: "data/command-registry.json"
      provides: "Populated curated_chains: [{kind, from, to, confidence}] entries (was [])."
      contains: "feeds_into"
    - path: "lib/workflow/local-chain-recommender.cjs"
      provides: "Ranks chain candidates off the LOCAL projection's FEEDS_INTO + curated confidence."
      contains: "confidence"
  key_links:
    - from: "data/command-registry.json curated_chains"
      to: "data/brain-orchestration-projection.json FEEDS_INTO edges"
      via: "build-orchestration-projection.cjs regeneration"
      pattern: "FEEDS_INTO"
    - from: "lib/workflow/local-chain-recommender.cjs"
      to: "data/brain-orchestration-projection.json"
      via: "LOCAL projection read at rank time"
      pattern: "projection"
---

<objective>
Implement R6 / INV-08: populate curated FEEDS_INTO confidences in command-registry curated_chains (was []), regenerate the projection's chain layer, and rank suggest-next off the LOCAL projection so chain candidates are USEFUL next-steps with EARNED (not uniform/absent) confidence - all Local-Only (INV-12).

Purpose: Chain confidence is absent end-to-end in production (the BRAIN.md Pinecone derivation drops r.confidence; curated_chains is []). Placeholder-by-omission means suggest-next has nothing to rank. R6 makes earned confidence the rule (learned weights defer to SEED-009).

Output: populated curated_chains, the regenerated projection chain layer, the LOCAL-projection-ranked suggest-next, and the ranking test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@docs/ORCHESTRATION-PROJECTION-CONTRACT.md
@.planning/phases/172-contextual-invocation-coverage/research/recon-internal-synthesis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate curated_chains with curated-confidence FEEDS_INTO edges</name>
  <files>data/command-registry.json</files>
  <read_first>
    - data/command-registry.json:1773 ("curated_chains": [] - the empty array to populate).
    - docs/ORCHESTRATION-PROJECTION-CONTRACT.md section 3 (the curated_chains -> edge-type mapping: {kind, from, to} where kind in chain|feeds_into|prerequisite; from/to resolve to framework:<name> or reach:<id>; an unknown framework/reach makes addEdge THROW).
    - data/framework-names.json (the resolvable framework names - every curated_chains from/to MUST resolve to one, or the projection build FAILs referential integrity).
    - 172-CONTEXT.md D-172-d (curated for v1; learned DEFERRED to SEED-009) + the ROADMAP/Phase-170 FEEDS_INTO chain (ACE chain: S-Curve / Reverse Salient / PEST / Macro Trends / Sustaining-vs-Disruptive INTO ACE; ACE INTO Scenario Planning / Mullins / etc.) as a real venture-flow seed.
  </read_first>
  <action>
    Populate data/command-registry.json curated_chains with curated-confidence FEEDS_INTO entries `{kind:"feeds_into", from:"<framework>", to:"<framework>", confidence:<0..1>}` (plus chain/prerequisite where warranted). Use REAL framework chains (seed from the Phase-170 ACE FEEDS_INTO chain + the established framework chaining the Brain carries), each from/to an EXACT framework name in data/framework-names.json (so addEdge does not throw). Confidence is CURATED (per-edge, not uniform - a placeholder-uniform value is the defect to remove per R6); assign defensible per-edge values. The schema extension (a confidence field on curated_chains entries) carries enum/scalar only (Part 8). Do NOT add a property to the frozen navigation ALLOWED_EDGE_TYPES FEEDS_INTO - confidence lives on the PROJECTION's FEEDS_INTO (the curated_chains entry), not the navigation edge (constraint C, Part 11 R6).
  </action>
  <verify>
    <automated>node -e "const r=require('./data/command-registry.json'); if(!Array.isArray(r.curated_chains)||r.curated_chains.length===0)process.exit(1); const bad=r.curated_chains.filter(c=>typeof c.confidence!=='number'); if(bad.length)process.exit(1); console.log('curated_chains:',r.curated_chains.length)"</automated>
  </verify>
  <acceptance_criteria>
    - data/command-registry.json curated_chains is non-empty with {kind, from, to, confidence} entries.
    - Every from/to resolves to an exact framework name in data/framework-names.json (or a reach id).
    - Confidence is per-edge (not a single uniform value across all entries).
    - No property is added to lib/core/navigation/edges.cjs ALLOWED_EDGE_TYPES (the navigation FEEDS_INTO is untouched).
  </acceptance_criteria>
  <done>curated_chains carries earned per-edge FEEDS_INTO confidence.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the projection chain layer; verify FEEDS_INTO edges materialize</name>
  <files>data/brain-orchestration-projection.json</files>
  <read_first>
    - scripts/build-orchestration-projection.cjs (the curated_chains -> FEEDS_INTO/CHAINS/PREREQUISITE mapping + addEdge referential-integrity throw + the chain_layer_note).
    - docs/ORCHESTRATION-PROJECTION-CONTRACT.md section 3 "CEILING, not floor" (the source-empty chain layer + the chain_layer_note that flips once curated_chains is populated).
  </read_first>
  <action>
    Run `node scripts/build-orchestration-projection.cjs` to regenerate data/brain-orchestration-projection.json with the now-populated curated_chains materializing FEEDS_INTO (+ CHAINS/PREREQUISITE) edges. Confirm the chain_layer_note no longer reports SOURCE-EMPTY (or reports the materialized count). Confirm `--check` exits 0 (no STALE, no dangling edge - every from/to resolved). Do NOT hand-edit the projection JSON.
  </action>
  <verify>
    <automated>node scripts/build-orchestration-projection.cjs && node scripts/build-orchestration-projection.cjs --check && node -e "const p=require('./data/brain-orchestration-projection.json'); const f=p.edges.filter(e=>e.type==='FEEDS_INTO'); if(f.length===0)process.exit(1); console.log('FEEDS_INTO edges:',f.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-orchestration-projection.cjs --check` exits 0.
    - data/brain-orchestration-projection.json carries >=1 FEEDS_INTO edge (the chain layer is no longer source-empty).
    - No dangling edge (referential integrity held - the build did not throw).
  </acceptance_criteria>
  <done>The projection chain layer is materialized from curated_chains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rank suggest-next off the LOCAL projection</name>
  <files>lib/workflow/local-chain-recommender.cjs, tests/test-curated-chains-ranking.cjs</files>
  <read_first>
    - lib/workflow/local-chain-recommender.cjs (the existing local chain recommender - the rank-off-local surface; if absent, lib/memory/local-chain-recommender.test.cjs names it) + lib/workflow/f-selector-ranker.cjs (the MAX_K ranker that orders surfaced candidates - Part 3, NOT CIRS; do not change its ordering authority).
    - docs/ORCHESTRATION-PROJECTION-CONTRACT.md section 2 (rankReachesForProblem - the pure helper that reads only the projection).
    - 172-CONTEXT.md D-172-d + INV-12 (Local-Only: no live Brain at rank time).
  </read_first>
  <behavior>
    - Test 1: the recommender reads the LOCAL projection (data/brain-orchestration-projection.json) and returns chain candidates ranked by curated confidence.
    - Test 2: a higher-confidence FEEDS_INTO edge ranks above a lower-confidence one.
    - Test 3: the recommender makes zero Brain/network calls at rank time (Local-Only, INV-12).
    - Test 4: ORDERING of the final surfaced set defers to the Part-3 MAX_K ranker (R6: CIRS supplies confidence, Part 3 orders).
  </behavior>
  <action>
    Wire lib/workflow/local-chain-recommender.cjs to read the LOCAL projection's FEEDS_INTO edges + curated confidence and return ranked chain candidates for suggest-next. The recommender is Local-Only (reads the committed projection file; zero Brain at rank time, INV-12). Per R6, CIRS supplies the confidence but the final surfaced ORDERING defers to the Part-3 MAX_K ranker - do not duplicate the ranker. Write tests/test-curated-chains-ranking.cjs covering the four behaviors against an in-memory projection fixture.
  </action>
  <verify>
    <automated>node tests/test-curated-chains-ranking.cjs</automated>
  </verify>
  <acceptance_criteria>
    - lib/workflow/local-chain-recommender.cjs ranks chain candidates off the LOCAL projection's curated confidence.
    - A higher-confidence edge ranks above a lower-confidence edge.
    - The recommender makes zero Brain/network calls at rank time (grep + test assertion).
    - tests/test-curated-chains-ranking.cjs passes all four behaviors.
  </acceptance_criteria>
  <done>suggest-next ranks chain candidates off the LOCAL projection with earned confidence, Local-Only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated_chains -> projection | Curated confidence is generic machinery metadata (framework-name pairs + a scalar); no user content. |
| suggest-next rank -> Brain | MUST NOT cross. Ranking reads the LOCAL projection file only; zero Brain at rank time (INV-12). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-20 | Information disclosure | curated_chains confidence | accept | Entries are framework-name pairs + a scalar confidence (generic machinery); the Phase 157 projection boundary scan field-allowlist sweep fences any user-content field. |
| T-172-21 | Tampering | uniform/placeholder confidence | mitigate | Task 1 asserts per-edge (non-uniform) confidence; R6 makes absent/uniform confidence the defect to remove. |
| T-172-22 | Information disclosure | live Brain at rank time | mitigate | Test 3 asserts zero Brain calls at rank time (INV-12 Local-Only). |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; data + recommender edits. |
</threat_model>

<verification>
- node scripts/build-orchestration-projection.cjs --check exit 0.
- node tests/test-curated-chains-ranking.cjs exit 0.
- No property added to navigation ALLOWED_EDGE_TYPES FEEDS_INTO.
</verification>

<success_criteria>
curated_chains carries earned per-edge confidence, the projection chain layer is materialized, and suggest-next ranks off the LOCAL projection - R6/INV-08 met, Local-Only.
</success_criteria>

<artifacts_this_phase_produces>
- POPULATED: data/command-registry.json curated_chains (was [])
- REGENERATED: data/brain-orchestration-projection.json (FEEDS_INTO chain layer materialized)
- NEW FILE: tests/test-curated-chains-ranking.cjs
- MODIFIED: lib/workflow/local-chain-recommender.cjs (ranks off the LOCAL projection)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-10-SUMMARY.md` when done
</output>
