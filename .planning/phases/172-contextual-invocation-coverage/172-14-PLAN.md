---
phase: 172-contextual-invocation-coverage
plan: 14
type: execute
wave: 8
depends_on: ["172-13"]
files_modified:
  - tests/test-cirs-adversarial-verify.cjs
  - tests/run-all-172.sh
  - .planning/phases/172-contextual-invocation-coverage/172-VERDICT.md
autonomous: false
requirements: [INV-15]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["scripts/build-connector-registry.cjs --check", "scripts/build-orchestration-projection.cjs --check", "data/connector-coverage-ledger.json", "lib/core/navigation-engine.cjs", "scripts/act-command.cjs"]
  gate_impact: "The adversarial VERIFY plan (harness-as-code Phase V): a red-team that TRIES to ship a dark surface and a second selection brain, returning a STRUCTURED pass/fail verdict over the whole coverage contract. No surface change; the structured verdict over the gate."
  explanation: "This plan is INV-15's adversarial-verify-with-structured-verdict property. It consumes both gates + the engine + act-command and emits a single pass/fail verdict over every CIRS assertion; navigator-gated (the phase verdict)."
user_setup: []

must_haves:
  truths:
    - "An adversarial red-team test TRIES to ship a dark surface AND a second selection brain; the gate catches both."
    - "A structured pass/fail verdict over the whole coverage contract is emitted (every CIRS assertion checked)."
    - "R13 (surface retirement) and R14 (trigger-overlap) are recorded in the verdict as DECLARED-but-DEFERRED-ENFORCEMENT (no owning implementation plan in 172; substrate-gated), alongside R6 + R11 - documented, not silently missing."
    - "A single run-all-172.sh phase aggregator runs every 172 test and is green."
  artifacts:
    - path: "tests/test-cirs-adversarial-verify.cjs"
      provides: "The red-team: synthesizes a dark surface + a second selection brain and asserts the gate catches both; emits the structured verdict."
      contains: "verdict"
    - path: "tests/run-all-172.sh"
      provides: "The phase test aggregator running every 172 test."
      contains: "test-"
    - path: ".planning/phases/172-contextual-invocation-coverage/172-VERDICT.md"
      provides: "The structured pass/fail verdict over the coverage contract."
      contains: "VERDICT"
  key_links:
    - from: "tests/test-cirs-adversarial-verify.cjs"
      to: "the coverage gate + engine + act-command"
      via: "red-team assertions"
      pattern: "gate"
    - from: "tests/run-all-172.sh"
      to: "every 172 test"
      via: "phase aggregator"
      pattern: "test-"
---

<objective>
Ship the harness-as-code Phase V (INV-15): an ADVERSARIAL verify with a STRUCTURED pass/fail verdict over the whole coverage contract. A red-team that TRIES to ship a dark surface AND a second selection brain - the exhaustive coverage test must catch both. Plus the run-all-172.sh phase aggregator and the 172-VERDICT.md structured verdict.

Purpose: The 9-property harness-as-code architecture requires an adversarial verify returning a structured verdict (property 6). This is the final barrier: prove by construction that the gate catches the exact two failure modes that caused prior regressions (a dark surface; a second ungoverned selection brain).

Output: the adversarial red-team test, the phase aggregator, and the structured verdict file. Navigator-gated (autonomous:false) - the phase verdict is a navigator decision point.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@.planning/phases/172-contextual-invocation-coverage/172-RECALIBRATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The adversarial red-team verify (dark surface + second selection brain)</name>
  <files>tests/test-cirs-adversarial-verify.cjs</files>
  <read_first>
    - tests/test-coverage-gate-hardfail.cjs (Plan 13 - the dark-surface fixture pattern to extend).
    - scripts/act-command.cjs (Plan 08 - assert the ()=>null decideFn is gone, the second selection brain is closed).
    - lib/core/navigation-engine.cjs (Plan 05 - assert the hats case exists so a hats reach flips routing_source).
    - scripts/build-connector-registry.cjs coverageReport() + scripts/build-orchestration-projection.cjs validateProjection() (the gate the red-team attacks).
    - 172-RECALIBRATION.md Phase V (the adversarial verify spec: a red-team agent that TRIES to ship a dark surface and a second selection brain; the exhaustive coverage test must catch both; structured verdict) + the orchestrator's adversarial-verify checkable assertions list.
  </read_first>
  <behavior>
    - Test 1 (dark surface): synthesizing a surface with no connector and no exclude makes the coverage gate flag it (the gate catches the dark surface).
    - Test 2 (second selection brain): act-command.cjs source carries no ()=>null decideFn AND act.md carries a connector block (no second ungoverned selection brain).
    - Test 3 (rs-* family fires context_block): each rs-* connector declares reach_id context_block on its sensor.
    - Test 4 (hats engine-mapped): navigation-engine.cjs reachIdToSkillFamily has a hats case.
    - Test 5 (every surface wired-or-excluded): the coverage ledger has zero un-decided gaps (counts.gap === 0).
    - Test 6 (frozen invariants): REACH_IDS length 6, POSTURE_IDS length 3, DIAL_REACH_K 6, MAX_K 3 unchanged.
  </behavior>
  <action>
    Write tests/test-cirs-adversarial-verify.cjs as the red-team: it ATTACKS the gate by synthesizing (a) a dark surface and asserting the gate catches it, (b) checking act-command.cjs no longer carries the ()=>null decideFn and act.md is connector-wired (the second selection brain is closed). It also asserts the positive coverage contract: rs-* fires context_block on its sensor; navigation-engine has the hats case; the coverage ledger has zero un-decided gaps; the frozen invariants (6 reaches, 3 postures, DIAL_REACH_K=6, MAX_K=3) are intact. Each assertion is checkable (source-grep, registry/ledger read, or coverageReport). It EMITS a structured verdict object `{ pass: boolean, assertions: [{id, name, status}] }` (printed + returned). Zero live Brain.
  </action>
  <verify>
    <automated>node tests/test-cirs-adversarial-verify.cjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-cirs-adversarial-verify.cjs synthesizes a dark surface and asserts the gate catches it (the red-team's first attack fails to ship).
    - It asserts act-command.cjs carries no ()=>null decideFn and act.md is connector-wired (the second selection brain is closed).
    - It asserts rs-* fires context_block, navigation-engine has the hats case, counts.gap===0, and the frozen invariants are intact.
    - It emits a structured verdict {pass, assertions[]} and exits non-zero if any assertion fails.
  </acceptance_criteria>
  <done>The adversarial red-team catches both regression failure modes and emits a structured verdict.</done>
</task>

<task type="auto">
  <name>Task 2: The run-all-172.sh phase aggregator + the structured 172-VERDICT.md</name>
  <files>tests/run-all-172.sh, .planning/phases/172-contextual-invocation-coverage/172-VERDICT.md</files>
  <read_first>
    - tests/run-all-169.sh / tests/run-all-164.sh (the phase-aggregator idiom: a bash runner invoking every phase test and reporting N/N green - mirror it).
    - every tests/test-*.cjs this phase created (test-connector-coverage-ledger, test-cirs-declaration, test-orchestration-counterpart-coverage, test-context-driven-trigger, test-act-governed-selection, test-act-standing-suggestion, test-curated-chains-ranking, test-coverage-rollup, test-170-171-cirs-conformance, test-coverage-gate-hardfail, test-cirs-adversarial-verify) - register them all.
    - 172-RECALIBRATION.md Phase V (the structured verdict shape).
  </read_first>
  <action>
    Write tests/run-all-172.sh registering every 172 test (the eleven test files across Plans 01-14) plus the two --check gates, reporting N/N green (mirror run-all-169.sh). Write .planning/phases/172-contextual-invocation-coverage/172-VERDICT.md as the structured pass/fail verdict over the coverage contract: a table of every CIRS assertion (R1..R14 + INV-01..23 coverage) with PASS/FAIL/DEFERRED-ENFORCEMENT status, the adversarial-verify result, the coverage counts (wired/excluded/gap), and the explicit note that R6 + R11 are DECLARED-but-DEFERRED-ENFORCEMENT (warn/aspirational, not hard-FAIL). ALSO record R13 (surface retirement: RETIRED ledger state + dangling-FEEDS_INTO gate-FAIL) and R14 (trigger-overlap WARN detection) as DECLARED-but-DEFERRED-ENFORCEMENT with a one-line reason: 172 ships no surface retirement and the overlap detector is substrate-gated, so both are declared-law-now / enforce-later (mirroring the R6/R11 precedent the SPEC establishes) rather than owned by an implementation plan in this phase. The verdict is the navigator decision point for closing the phase. No em-dashes.
  </action>
  <verify>
    <automated>bash tests/run-all-172.sh && grep -q "VERDICT" .planning/phases/172-contextual-invocation-coverage/172-VERDICT.md && grep -q "DEFERRED-ENFORCEMENT" .planning/phases/172-contextual-invocation-coverage/172-VERDICT.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-172.sh` runs every 172 test + both --check gates and reports N/N green.
    - 172-VERDICT.md is a structured verdict table covering R1..R14 + INV-01..23 with PASS/FAIL/DEFERRED-ENFORCEMENT status.
    - 172-VERDICT.md records R6 + R11 AND R13 + R14 as DECLARED-but-DEFERRED-ENFORCEMENT (each with a one-line reason).
    - No em-dashes in 172-VERDICT.md.
  </acceptance_criteria>
  <done>The phase aggregator is green and the structured verdict over the coverage contract is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adversarial verify -> Brain | The red-team reads LOCAL sources + runs the gates in memory; zero Brain/network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-31 | Tampering | a dark surface slips past the gate | mitigate | The adversarial red-team synthesizes the exact attack (a dark surface) and asserts the gate catches it; a regression in the gate makes this test RED. |
| T-172-32 | Spoofing | a second selection brain reappears | mitigate | The red-team asserts act-command has no ()=>null decideFn and act.md is wired; a reintroduced second brain makes this test RED. |
| T-172-33 | Tampering | a frozen invariant drifts | mitigate | The verdict asserts REACH_IDS=6, POSTURE_IDS=3, DIAL_REACH_K=6, MAX_K=3; a drift fails the verdict. |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; pure-Node test + a bash aggregator + a verdict doc. |
</threat_model>

<verification>
- node tests/test-cirs-adversarial-verify.cjs exit 0 (structured verdict pass).
- bash tests/run-all-172.sh N/N green.
- 172-VERDICT.md records the structured verdict + R6/R11 deferred-enforcement.
- NAVIGATOR GATE: the phase verdict reviewed before close (autonomous:false).
</verification>

<success_criteria>
The adversarial red-team catches both regression failure modes (dark surface + second selection brain), every 172 test is green via run-all-172.sh, and a structured pass/fail verdict over the whole coverage contract is recorded - INV-15's adversarial-verify-with-structured-verdict property met.
</success_criteria>

<artifacts_this_phase_produces>
- NEW FILE: tests/test-cirs-adversarial-verify.cjs (the red-team + structured verdict)
- NEW FILE: tests/run-all-172.sh (the phase aggregator)
- NEW FILE: .planning/phases/172-contextual-invocation-coverage/172-VERDICT.md (the structured verdict)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-14-SUMMARY.md` when done
</output>
