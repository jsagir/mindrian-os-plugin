---
phase: 172-contextual-invocation-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-orchestration-projection.cjs
  - data/orchestration-command-ledger.json
  - docs/ORCHESTRATION-PROJECTION-CONTRACT.md
  - tests/test-orchestration-counterpart-coverage.cjs
autonomous: true
requirements: [INV-04, INV-05, INV-06]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["scripts/build-orchestration-projection.cjs (generator + --check)", "data/brain-orchestration-projection.json"]
  gate_impact: "Inverts the UN-RANKED early-continue so a bare command FAILs unless ranked-or-excluded (R5); adds the command-grained wired-XOR-excluded ledger + the mindrian-operation counterpart classification + the documented promotion path (R8). WARN+report at this stage; hard flip in Wave 4."
  explanation: "This plan builds the R5/R8 substrate on the shipped Phase 157 projection: it makes the projection gate command-grained (not just framework-grained) and documents the dark -> counterpart -> pws frontier promotion path."
user_setup: []

must_haves:
  truths:
    - "A non-framework command that should trigger/chain/monitor gets a mindrian-operation counterpart node with its connector ranking inputs."
    - "The projection --check classifies every command node as ranked, excluded, or gap (command-grained), not only framework-grained UN-WIRED."
    - "The promotion path dark -> mindrian-operation counterpart -> pws frontier framework is documented and exercised once."
  artifacts:
    - path: "data/orchestration-command-ledger.json"
      provides: "The command-grained wired-XOR-excluded ledger mirroring data/orchestration-unwired-allowlist.json, the R5 gate source of truth for command counterparts."
      contains: "\"state\""
    - path: "scripts/build-orchestration-projection.cjs"
      provides: "Inverted UN-RANKED logic (bare command FAILs unless excluded) + counterpart classification + command-grained coverage."
      contains: "mindrian-operation"
    - path: "docs/ORCHESTRATION-PROJECTION-CONTRACT.md"
      provides: "The counterpart node + promotion path (R8) documentation section."
      contains: "promotion path"
  key_links:
    - from: "scripts/build-orchestration-projection.cjs validateProjection()"
      to: "data/orchestration-command-ledger.json"
      via: "command-grained ranked-or-excluded check"
      pattern: "orchestration-command-ledger"
    - from: "data/orchestration-command-ledger.json"
      to: "mindrian-operation counterpart"
      via: "command node classification"
      pattern: "mindrian-operation"
---

<objective>
Build the R5/R8 counterpart substrate on the shipped Phase 157 orchestration projection: invert the UN-RANKED early-continue so a bare command counterpart FAILs unless ranked-or-excluded (INV-11/D-172 inversion), add the command-grained wired-XOR-excluded ledger, classify warranted dark commands as `mindrian-operation` counterparts, and document the promotion path (INV-06).

Purpose: Today the projection's UN-RANKED check early-continues on any node lacking reach_id (build-orchestration-projection.cjs:824), so dark commands ship silently. INV-04/05 require every surface to have a counterpart node and warranted non-framework commands to be ranked-or-excluded at the COMMAND grain, not just the framework grain.

Output: an inverted projection gate, data/orchestration-command-ledger.json, the promotion-path doc section, and the counterpart-coverage test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@docs/ORCHESTRATION-PROJECTION-CONTRACT.md
@.planning/phases/172-contextual-invocation-coverage/research/recon-remote-projection.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invert the UN-RANKED early-continue + add command-grained coverage</name>
  <files>scripts/build-orchestration-projection.cjs</files>
  <read_first>
    - scripts/build-orchestration-projection.cjs:733-840 (validateProjection), :824 (the UN-RANKED early-continue to INVERT), :842 (runCheck), :106-122 (the frozen ALLOWED_EDGE_TYPES - do NOT touch), :436 (addEdge referential-integrity throw).
    - docs/ORCHESTRATION-PROJECTION-CONTRACT.md section 4a (the 3-mode STALE/UN-WIRED/UN-RANKED taxonomy + the wired-XOR-allowlisted ledger data/orchestration-unwired-allowlist.json idiom this command ledger mirrors).
    - .planning/phases/172-contextual-invocation-coverage/research/recon-remote-projection.md (the :824 early-continue + the 46-bare/38-dark command counts + the 4 net-new items).
  </read_first>
  <action>
    In validateProjection(), add a command-grained coverage pass: every command-kind node (methodology_tier mindrian-operation) is classified `ranked` (carries reach_id+hierarchy_rank+posture), `excluded` (listed in data/orchestration-command-ledger.json with a reason), or `gap`. INVERT the :824 early-continue: a command node lacking reach_id is no longer silently skipped - it is collected into a `command_gaps[]` array unless it is excluded in the ledger. Keep the existing framework-grained UN-WIRED (data/orchestration-unwired-allowlist.json) and skill EXEMPTION untouched. Return the new `command_gaps` array alongside the existing `{stale, unwired, unranked}`. At THIS stage report command_gaps as WARN (the hard FAIL flip is Wave 4 / Plan 172-10) so CI does not go RED mid-sweep (D-172-e). Mint NO new edge type, NO new node type - reuse the frozen ALLOWED_EDGE_TYPES + the methodology_tier boundary-keeper (constraint).
  </action>
  <verify>
    <automated>node -e "const g=require('./scripts/build-orchestration-projection.cjs'); const p=require('./data/brain-orchestration-projection.json'); const v=g.validateProjection(p); if(!Array.isArray(v.command_gaps))process.exit(1); console.log('command_gaps',v.command_gaps.length)"</automated>
  </verify>
  <acceptance_criteria>
    - validateProjection() returns a `command_gaps` array (node require shows it).
    - The :824 early-continue no longer silently skips a bare command node (a command without reach_id and not in the ledger appears in command_gaps).
    - The frozen ALLOWED_EDGE_TYPES set is unchanged (git diff touches no edge-type line).
    - Skill nodes remain EXEMPT from the ranking gate (an un-ranked skill is not a command_gap).
  </acceptance_criteria>
  <done>The projection gate is command-grained; bare commands surface as gaps instead of silently shipping.</done>
</task>

<task type="auto">
  <name>Task 2: Emit data/orchestration-command-ledger.json + register STALE check</name>
  <files>scripts/build-orchestration-projection.cjs, data/orchestration-command-ledger.json</files>
  <read_first>
    - data/orchestration-unwired-allowlist.json (the bare-JSON-array {framework, reason} ledger shape to mirror as {command, reason} - or the {surface, source, state} shape coverageReport uses; choose the {command, source, state} shape for command counterparts).
    - scripts/build-orchestration-projection.cjs (the serialize + byte-compare STALE idiom).
  </read_first>
  <action>
    Add a `serializeCommandLedger(projection)` emitting data/orchestration-command-ledger.json as `{ generated_note, counts, commands: [{command, source, state}] }` where state is ranked|excluded|gap, sorted by command for determinism. Write it on the default run; STALE-byte-check it in --check. The EXCLUDED entries are authored by listing a command in the ledger with state:excluded + a reason (the utility commands: doctor, dashboard, admin, models, setup, help, export, publish, rooms, snapshot, ingest-methodology, and the like). Generate the committed ledger by running the generator once; warranted-dark utility commands land as excluded-with-reason, the rest that warrant counterparts will be wired in Wave 2/3. Do NOT FAIL on gap yet (warn+report).
  </action>
  <verify>
    <automated>node scripts/build-orchestration-projection.cjs && node scripts/build-orchestration-projection.cjs --check 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-orchestration-projection.cjs` writes data/orchestration-command-ledger.json with top-level keys `generated_note`, `counts`, `commands`.
    - Each command entry has state in {ranked, excluded, gap} and counts sum to the command-node count (XOR invariant).
    - `--check` STALE-fails (exit non-zero) if the on-disk command ledger is hand-edited out of sync.
    - At least the core utility commands (doctor, dashboard, setup, help) appear as state:excluded with a reason.
  </acceptance_criteria>
  <done>The command-grained wired-XOR-excluded ledger is generated, committed, and byte-checked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Document the promotion path (R8) + counterpart-coverage test</name>
  <files>docs/ORCHESTRATION-PROJECTION-CONTRACT.md, tests/test-orchestration-counterpart-coverage.cjs</files>
  <read_first>
    - docs/ORCHESTRATION-PROJECTION-CONTRACT.md sections 2 (node schema), 4 (methodology_tier boundary-keeper), 4a (the --check taxonomy) - add the counterpart + promotion-path section after 4a.
    - .planning/phases/172-contextual-invocation-coverage/172-SPEC.md INV-05/INV-06 + R8 (the dark -> mindrian-operation counterpart -> pws frontier framework path, navigator-gated).
    - lib/core/methodology-ingest.cjs ingestPlan (the Phase 171 ingest pipeline the promotion path rides) + commands/ingest-methodology.md.
  </read_first>
  <behavior>
    - Test 1: every command node in the live projection is classified ranked|excluded|gap (full coverage).
    - Test 2: a synthesized bare command (no reach_id, not in ledger) is flagged as a command_gap by validateProjection.
    - Test 3: a synthesized excluded command (in the ledger with reason) is NOT a command_gap.
    - Test 4: the command ledger XOR invariant: counts.ranked + counts.excluded + counts.gap === command node count.
  </behavior>
  <action>
    Add an ORCHESTRATION-PROJECTION-CONTRACT.md section documenting: (1) the `mindrian-operation` COUNTERPART node for non-framework commands (it carries reach_id+hierarchy_rank+posture+sensor_triggers; it chains via OPERATES/CHAINS/PREREQUISITE without a pws framework - INV-05); (2) the PROMOTION PATH (R8/INV-06): dark command -> mindrian-operation counterpart (this phase) -> pws frontier framework via the Phase 171 ingest pipeline, navigator-gated (Part 3 + Part 9 role 5); (3) that promotion is metadata reclassification within the sanctioned projection, minting no new node type (constraint). Write tests/test-orchestration-counterpart-coverage.cjs covering the four behaviors. Reference Canon Part 11 R5/R8.
  </action>
  <verify>
    <automated>node tests/test-orchestration-counterpart-coverage.cjs && grep -q "promotion path" docs/ORCHESTRATION-PROJECTION-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-orchestration-counterpart-coverage.cjs` exits 0 with all four behaviors passing.
    - docs/ORCHESTRATION-PROJECTION-CONTRACT.md documents the mindrian-operation counterpart node + the R8 promotion path.
    - The doc states promotion mints no new node type (Part 11 constraint).
  </acceptance_criteria>
  <done>The counterpart node + promotion path are documented and the command-grained XOR coverage is test-fenced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| projection generator -> committed data files | Generator reads LOCAL registries + file walk only; zero Brain I/O (the projection has no --refresh-names sibling). |
| projection/ledger -> Brain | MUST NOT cross. Counterpart nodes carry only command slugs + reach_ids + ranking enums (generic machinery metadata, methodology_tier-tagged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-05 | Information disclosure | mindrian-operation counterpart node | mitigate | Counterpart carries only {reach_id, hierarchy_rank, posture, sensor_triggers, command slug}; the Phase 157 boundary scan (test-orchestration-projection-part8-boundary.cjs) field-allowlist sweep already fences any user-content field. Task 3 asserts no value matches a room/ path or email heuristic. |
| T-172-06 | Tampering | command ledger exclusion | mitigate | An excluded command REQUIRES a reason; state is XOR (ranked|excluded|gap), so a command cannot be silently both. |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; pure-Node generator. |
</threat_model>

<verification>
- node scripts/build-orchestration-projection.cjs --check exit 0 on clean repo.
- node tests/test-orchestration-counterpart-coverage.cjs exit 0.
- git diff shows no change to ALLOWED_EDGE_TYPES or the methodology_tier rule.
</verification>

<success_criteria>
The projection gate is command-grained, the command-level wired-XOR-excluded ledger exists, warranted dark utility commands are excluded-with-reason, and the dark -> counterpart -> frontier promotion path is documented - the R5/R8 substrate.
</success_criteria>

<artifacts_this_phase_produces>
- NEW FILE: data/orchestration-command-ledger.json (command-grained wired-XOR-excluded ledger)
- NEW FILE: tests/test-orchestration-counterpart-coverage.cjs
- NEW SYMBOL: serializeCommandLedger() + command_gaps in scripts/build-orchestration-projection.cjs
- NEW DOC SECTION: the mindrian-operation counterpart + R8 promotion path in ORCHESTRATION-PROJECTION-CONTRACT.md
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-03-SUMMARY.md` when done
</output>
