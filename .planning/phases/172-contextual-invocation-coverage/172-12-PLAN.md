---
phase: 172-contextual-invocation-coverage
plan: 12
type: execute
wave: 6
depends_on: ["172-04", "172-05", "172-06", "172-07"]
files_modified:
  - lib/core/sensors/sensor-diffusion-adoption.cjs
  - lib/core/methodology-ingest.cjs
  - commands/analyze-timing.md
  - data/connector-registry.json
  - data/connector-coverage-ledger.json
  - tests/test-170-171-cirs-conformance.cjs
autonomous: false
requirements: [INV-11, INV-17]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: ["/mos:analyze-timing"]
  surfaces_removed: []
  spine_consumed: ["lib/core/insight-sensors.cjs", "lib/core/methodology-ingest.cjs ingestPlan step-5", "lib/core/navigation.cjs", "scripts/build-connector-registry.cjs"]
  gate_impact: "Brings Phase 170 (ACE/diffusion) and Phase 171 (methodology-ingest) into CIRS conformance: SENS-09 gets a context branch (R3, keyword demoted to fallback), analyze-timing joins the gate, and ingest step-5 becomes a thin caller of CIRS so every FUTURE methodology is born-wired (R2). 170/171 do not release until conformant."
  explanation: "This plan is the FIRST proof CIRS works on real surfaces (INV-11/17). It consumes the sensor spine + the ingest pipeline + navigation.cjs; reconciles 170/171 under the coverage contract; mints no reach (SENS-09 reuses brain_consult)."
user_setup: []

must_haves:
  truths:
    - "SENS-09 (diffusion-adoption) gains a CONTEXT branch keyed on tuple.problem_type via navigation.cjs; KEYWORD is demoted to fallback (R3/INV-07)."
    - "/mos:analyze-timing carries a connector block and is in the coverage gate."
    - "methodology-ingest step-5 is rewritten as a THIN CALLER of CIRS so every ingested methodology is born with a connector block + passes the gate at ingest time (R2)."
  artifacts:
    - path: "lib/core/sensors/sensor-diffusion-adoption.cjs"
      provides: "A context branch (problem_type via navigation.cjs) with keyword demoted to fallback tier."
      contains: "problem_type"
    - path: "lib/core/methodology-ingest.cjs"
      provides: "step-5 as a thin caller of the CIRS born-wired wiring rules."
      contains: "connector"
  key_links:
    - from: "lib/core/sensors/sensor-diffusion-adoption.cjs"
      to: "navigation.cjs problem_type"
      via: "context branch (keyword fallback)"
      pattern: "problem_type"
    - from: "lib/core/methodology-ingest.cjs step-5"
      to: "connector wiring rules"
      via: "thin caller"
      pattern: "connector"
---

<objective>
Reconcile Phases 170 + 171 under the CIRS coverage contract (INV-11/17) - the FIRST proof CIRS works on real shipped surfaces. 170: SENS-09 gains a CONTEXT branch (problem_type via navigation.cjs), keyword demoted to fallback, analyze-timing added to the gate. 171: methodology-ingest step-5 becomes a THIN CALLER of CIRS so every FUTURE methodology is born contextually-invocable.

Purpose: 170 + 171 are committed and release-HELD until they conform to this phase's contract. 172 is the release-gate. Making 171's step-5 a thin caller makes CIRS self-propagating to all future methodologies (R2 at ingest time).

Output: the SENS-09 context branch, analyze-timing wiring, the ingest step-5 rewrite, and the conformance test. Navigator-gated (autonomous:false) because it gates the held 170/171 release.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@.planning/phases/172-contextual-invocation-coverage/research/recon-internal-synthesis.md
@docs/CONNECTOR-CONTRACT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SENS-09 context branch (problem_type via navigation.cjs) + wire analyze-timing</name>
  <files>lib/core/sensors/sensor-diffusion-adoption.cjs, commands/analyze-timing.md</files>
  <read_first>
    - lib/core/sensors/sensor-diffusion-adoption.cjs:113-156 (the problem_type read off tuple at :118 + the mode enum keyword/marker/signal at :126-156 + the makeReach frozen brain_consult reach) - the context branch decorates problem_type today; promote it to the primary signal.
    - lib/core/insight-sensors.cjs (the tiering seam Plan 07 added - the context-first/keyword-fallback convention) + lib/core/navigation.cjs (the LOCAL problem-state read).
    - commands/analyze-timing.md (the ACE command currently dark - add a connector block; Phase 170 mapped adoption-capacity -> "Adoption-Capacity Theory" -> /mos:analyze-timing in data/dispatch-framework-map.json).
    - 172-CONTEXT.md "Reconciliation work owned by 172 (INV-11)" 170 bullet (CONTEXT branch keyed on tuple.problem_type via navigation.cjs; KEYWORD demoted to fallback; analyze-timing connector added to the gate; NO canon amendment - reuses brain_consult).
  </read_first>
  <behavior>
    - Test 1: when problem_type indicates adoption/diffusion (the context signal via navigation.cjs), SENS-09 fires the context tier.
    - Test 2: when only the diffusion lexicon matches (no problem_type), SENS-09 fires mode 'keyword' (the demoted fallback tier).
    - Test 3: SENS-09 still fires the frozen brain_consult reach (no new reach minted).
    - Test 4: the evidence carries only enums (mode + problem_type), never matched user text (Part 8).
  </behavior>
  <action>
    Promote SENS-09's problem_type read to the PRIMARY context branch (read via navigation.cjs, enum/scalar only) and DEMOTE the diffusion keyword/lexicon match to the fallback tier (using the mode enum convention from Plan 07). SENS-09 keeps firing the frozen brain_consult reach (constraint: no new reach). Add a connector block to commands/analyze-timing.md (connects_to_spine:true; sensor_triggers [SENS-09]; reach_id brain_consult; sub_mode analyze-timing; framework "Adoption-Capacity Theory"; posture push_forward; surface F.1). Extend the SENS-09 sensor test with the four behaviors.
  </action>
  <verify>
    <automated>grep -q "connector:" commands/analyze-timing.md && node -e "const s=require('fs').readFileSync('lib/core/sensors/sensor-diffusion-adoption.cjs','utf8'); if(!/problem_type/.test(s))process.exit(1); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - SENS-09 prefers the problem_type context signal (read via navigation.cjs) and records the keyword match as a fallback-tier mode.
    - commands/analyze-timing.md carries a connector block with reach_id brain_consult (the frozen reach, no new reach minted).
    - SENS-09 still fires brain_consult; the evidence carries enums only (no matched user text).
    - The SENS-09 sensor test passes the four behaviors.
  </acceptance_criteria>
  <done>Phase 170's diffusion trigger is context-driven (keyword demoted) and analyze-timing is in the gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite methodology-ingest step-5 as a thin CIRS caller</name>
  <files>lib/core/methodology-ingest.cjs, tests/test-170-171-cirs-conformance.cjs</files>
  <read_first>
    - lib/core/methodology-ingest.cjs:200-217 (ingestPlan - the 6-step plan; step-5 at :213 is the hand-wiring checklist to rewrite as a thin caller) + :87-134 (buildFrameworkCypher chains_in/chains_out FEEDS_INTO write, untouched).
    - scripts/build-connector-registry.cjs coverageReport() (Plan 01) + the connector-contract worked examples - the born-wired rules step-5 calls.
    - 172-CONTEXT.md "Reconciliation work owned by 172 (INV-11)" 171 bullet (step-5 rewritten as a THIN CALLER of INV-02/03/10; born with a connector block + passes the gate; step-2 Part-8 audit untouched).
    - commands/ingest-methodology.md (the maintainer command surface).
  </read_first>
  <behavior>
    - Test 1: ingestPlan step-5 detail references the CIRS born-wired wiring (a connector block + the coverage gate), not a free-text hand-wiring checklist.
    - Test 2: an ingest spec yields a connector-block recommendation (frozen-6 reach_id + the ingested framework name) so the methodology is born-wired.
    - Test 3: the step-2 Part-8 boundary audit (auditSpecPart8) is unchanged (still fail-closed).
    - Test 4: a methodology ingested via this path would PASS the coverage gate at ingest (born-wired, not dark).
  </behavior>
  <action>
    Rewrite ingestPlan step-5 as a THIN CALLER of the CIRS born-wired wiring rules: it emits the connector-block specification (connects_to_spine:true + a frozen-6 reach_id + the ingested framework name + a sensor trigger) the new methodology must carry to be born-wired, and asserts it would pass the coverage gate (the Plan 01 coverageReport / Plan 13 born-wired check). The FEEDS_INTO chain write (buildFrameworkCypher) and the step-2 Part-8 audit are untouched. Write tests/test-170-171-cirs-conformance.cjs covering Task 1 + Task 2 behaviors.
  </action>
  <verify>
    <automated>node tests/test-170-171-cirs-conformance.cjs</automated>
  </verify>
  <acceptance_criteria>
    - ingestPlan step-5 references the CIRS born-wired connector wiring (not a free-text checklist).
    - An ingest spec yields a connector-block recommendation with a frozen-6 reach_id + the framework name.
    - auditSpecPart8 (step-2) is behaviorally unchanged.
    - tests/test-170-171-cirs-conformance.cjs passes.
  </acceptance_criteria>
  <done>171's ingest step-5 is a thin CIRS caller so every future methodology is born-wired.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate registry + ledger; navigator-gated 170/171 release conformance</name>
  <files>data/connector-registry.json, data/connector-coverage-ledger.json</files>
  <read_first>
    - scripts/build-connector-registry.cjs:614-651 (the --check branch).
    - data/connector-coverage-ledger.json (the rolling ledger - analyze-timing should now be wired).
  </read_first>
  <action>
    Run `node scripts/build-connector-registry.cjs` to regenerate the registry + ledger with /mos:analyze-timing wired. Confirm `--check` exits 0. This is the navigator gate: 170 + 171 do NOT release until this conformance is confirmed. Record in the SUMMARY that the 170/171 release-hold is cleared CONDITIONAL on the navigator confirming the conformance test passes. Do NOT hand-edit the JSON.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs && node scripts/build-connector-registry.cjs --check && node -e "const l=require('./data/connector-coverage-ledger.json'); const a=l.surfaces.find(s=>s.surface==='/mos:analyze-timing'); if(!a||a.state!=='wired')process.exit(1); console.log('analyze-timing wired')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-connector-registry.cjs --check` exits 0.
    - /mos:analyze-timing shows state:wired in the coverage ledger.
    - The SUMMARY records the 170/171 release-hold as navigator-gated on the conformance test.
  </acceptance_criteria>
  <done>170/171 are CIRS-conformant and the release-hold is navigator-gated on the green conformance test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SENS-09 problem_type read -> navigation.cjs | LOCAL enum/scalar via the chokepoint; no user content. |
| ingest spec -> Brain | The step-2 Part-8 audit (untouched) fails closed on user/venture data; step-5 emits generic connector metadata only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-26 | Information disclosure | SENS-09 evidence | mitigate | Evidence carries mode + problem_type enums only; test asserts no matched user text. |
| T-172-27 | Tampering | ingest born-dark methodology | mitigate | step-5 as a thin CIRS caller makes every ingested methodology born-wired + gate-passing. |
| T-172-28 | Information disclosure | ingest spec | mitigate | The step-2 Part-8 boundary audit (auditSpecPart8) is preserved fail-closed. |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; sensor + ingest edits + a generator run. |
</threat_model>

<verification>
- node tests/test-170-171-cirs-conformance.cjs exit 0.
- node scripts/build-connector-registry.cjs --check exit 0.
- NAVIGATOR GATE: 170/171 release-hold cleared only on green conformance (autonomous:false).
</verification>

<success_criteria>
170 (ACE/diffusion context-triggered) and 171 (ingest step-5 thin CIRS caller, born-wired) conform to CIRS; analyze-timing is in the gate; the release-hold is navigator-gated on the green conformance test - INV-11/17 met.
</success_criteria>

<artifacts_this_phase_produces>
- MODIFIED: lib/core/sensors/sensor-diffusion-adoption.cjs (context branch, keyword fallback)
- MODIFIED: lib/core/methodology-ingest.cjs (step-5 thin CIRS caller)
- WIRED: commands/analyze-timing.md (connector block)
- NEW FILE: tests/test-170-171-cirs-conformance.cjs
- REGENERATED: data/connector-registry.json, data/connector-coverage-ledger.json
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-12-SUMMARY.md` when done
</output>
