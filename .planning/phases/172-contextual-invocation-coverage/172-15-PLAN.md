---
phase: 172-contextual-invocation-coverage
plan: 15
type: execute
wave: 6
depends_on: ["172-08", "172-10"]
files_modified:
  - data/command-registry.json
  - scripts/build-orchestration-projection.cjs
  - data/brain-orchestration-projection.json
  - lib/workflow/local-chain-recommender.cjs
  - tests/test-chain-transform-composition.cjs
autonomous: true
requirements: [INV-08]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["data/command-registry.json curated_chains", "scripts/build-orchestration-projection.cjs", "data/brain-orchestration-projection.json FEEDS_INTO", "lib/workflow/local-chain-recommender.cjs"]
  gate_impact: "Aligns the LOCAL chain model to the VERIFIED Brain schema (research/172-SPFO-CHAIN-MODEL-REFERENCE.md): curated_chains + projection FEEDS_INTO carry `transform` (not just confidence), and the local recommender composes multi-hop confidence MULTIPLICATIVELY so the Brain-off ranker matches the Brain-on model. Refines R6/INV-08; mints no new edge/node/reach/Brain-wire."
  explanation: "This plan grounds the 172 chain edges in the verified Brain FEEDS_INTO {confidence, transform} schema and the multiplicative chain-confidence composition, so the LOCAL projection is a faithful Brain-derived mirror (INV-12). It consumes the curated_chains + projection generator + recommender; adds no primitive."
user_setup: []

must_haves:
  truths:
    - "curated_chains entries carry a `transform` field (a generic handoff descriptor) beside confidence; the projection FEEDS_INTO edges materialize it, mirroring the verified Brain edge {confidence, transform}."
    - "lib/workflow/local-chain-recommender.cjs composes multi-hop chain confidence MULTIPLICATIVELY (reduce c*=coalesce(r.confidence,0.5)), matching the SPFO/Brain model, ordered by hops then confidence."
    - "No new edge type/node type/reach/Brain-wire is minted; `transform` rides the PROJECTION FEEDS_INTO (the curated_chains entry), NOT the frozen navigation ALLOWED_EDGE_TYPES; Local-Only at rank time (INV-12)."
  artifacts:
    - path: "data/command-registry.json"
      provides: "curated_chains entries carry {kind, from, to, confidence, transform}."
      contains: "transform"
    - path: "lib/workflow/local-chain-recommender.cjs"
      provides: "Multiplicative multi-hop confidence composition (the SPFO reduce formula)."
      contains: "confidence"
  key_links:
    - from: "data/command-registry.json curated_chains transform"
      to: "data/brain-orchestration-projection.json FEEDS_INTO edges"
      via: "build-orchestration-projection.cjs regeneration"
      pattern: "transform"
    - from: "lib/workflow/local-chain-recommender.cjs"
      to: "the multi-hop chain confidence"
      via: "multiplicative reduce composition"
      pattern: "reduce"
---

<objective>
Align the LOCAL chain model to the VERIFIED Brain schema (research/172-SPFO-CHAIN-MODEL-REFERENCE.md, navigator-provided 2026-06-23): (1) carry `transform` on curated_chains + the projection FEEDS_INTO edges so the local cache mirrors the real Brain edge {confidence, transform}; (2) compose multi-hop chain confidence MULTIPLICATIVELY in the local recommender so the Brain-off ranker matches the Brain-on model. Refines R6/INV-08; the projection stays a faithful Brain-derived mirror.

Purpose: SPFO v2.1 grounds the chain machinery in verified schema: FEEDS_INTO carries confidence AND transform, and chain confidence composes via `reduce(c=1.0, r | c * coalesce(r.confidence,0.5))`. 172's curated_chains carry only confidence and the recommender does not yet use multiplicative composition - so the local mirror is incomplete. This closes that gap WITHOUT minting any new primitive (transform is a scalar/string prop on the projection FEEDS_INTO, not the frozen navigation edge).

Output: the transform-bearing curated_chains + projection, the multiplicative recommender, and the composition test. Autonomous - this is a schema-fidelity refinement, not a material gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@.planning/phases/172-contextual-invocation-coverage/research/172-SPFO-CHAIN-MODEL-REFERENCE.md
@docs/ORCHESTRATION-PROJECTION-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carry `transform` on curated_chains + the projection FEEDS_INTO</name>
  <files>data/command-registry.json, scripts/build-orchestration-projection.cjs, data/brain-orchestration-projection.json</files>
  <read_first>
    - .planning/phases/172-contextual-invocation-coverage/research/172-SPFO-CHAIN-MODEL-REFERENCE.md (the verified FEEDS_INTO {confidence, transform} schema).
    - data/command-registry.json curated_chains (the entries Plan 10 + Plan 08 populated - {kind, from, to, confidence}; add transform).
    - scripts/build-orchestration-projection.cjs curatedChainEdges() (the curated_chains -> FEEDS_INTO mapping that copies confidence onto the edge; extend it to copy transform too).
    - docs/ORCHESTRATION-PROJECTION-CONTRACT.md section 3 (the edge-property contract) + 172-CONTEXT.md INV-08 + the Part 8 field-allowlist (transform is a generic handoff descriptor - a short machinery string, never user content).
  </read_first>
  <action>
    Add an OPTIONAL `transform` field to each curated_chains entry in data/command-registry.json - a SHORT generic handoff descriptor (what the chain step transforms, e.g. "diverge-to-structured", "explore-to-evidence"); enum/scalar string only (Part 8), never user content. Backfill defensible transforms on the existing entries (leave it absent where genuinely unknown - the projection coalesces). Extend scripts/build-orchestration-projection.cjs curatedChainEdges() so the materialized FEEDS_INTO edge carries `transform` alongside `confidence` (mirror the existing confidence-copy line; do NOT touch the frozen navigation ALLOWED_EDGE_TYPES - transform lives on the PROJECTION edge only). Regenerate data/brain-orchestration-projection.json via the generator (no hand-edit); confirm --check exits 0 and the FEEDS_INTO edges carry transform where the source did. If a harness-manifest STALE tripwire fires, regenerate + stage it in the same commit.
  </action>
  <verify>
    <automated>node scripts/build-orchestration-projection.cjs && node scripts/build-orchestration-projection.cjs --check && node -e "const r=require('./data/command-registry.json'); const wt=r.curated_chains.filter(c=>typeof c.transform==='string'); if(wt.length===0)process.exit(1); const p=require('./data/brain-orchestration-projection.json'); const ft=p.edges.filter(e=>e.type==='FEEDS_INTO'&&e.transform); console.log('curated transform:',wt.length,'projection FEEDS_INTO w/transform:',ft.length)"</automated>
  </verify>
  <acceptance_criteria>
    - data/command-registry.json curated_chains entries carry a string `transform` field (at least the backfilled subset); it is enum/scalar machinery metadata, never user content.
    - scripts/build-orchestration-projection.cjs copies `transform` onto the materialized FEEDS_INTO edge beside confidence; --check exits 0.
    - data/brain-orchestration-projection.json FEEDS_INTO edges carry transform where the source entry had it.
    - lib/core/navigation/edges.cjs ALLOWED_EDGE_TYPES is UNTOUCHED (transform is a projection-edge property only).
  </acceptance_criteria>
  <done>The local chain edges mirror the verified Brain {confidence, transform} schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multiplicative multi-hop confidence composition in the local recommender</name>
  <files>lib/workflow/local-chain-recommender.cjs, tests/test-chain-transform-composition.cjs</files>
  <read_first>
    - lib/workflow/local-chain-recommender.cjs (the Plan-10 recommender that ranks single-edge chain candidates off the LOCAL projection - extend it to compose MULTI-HOP confidence multiplicatively).
    - research/172-SPFO-CHAIN-MODEL-REFERENCE.md (the verified composition: `reduce(c=1.0, r IN rels | c * coalesce(r.confidence, 0.5))`, ordered by hops then confidence DESC).
    - 172-CONTEXT.md INV-12 (Local-Only: zero Brain at rank time) + Part 3 (the MAX_K ranker still owns final surfaced ordering; R6 - CIRS supplies confidence, Part 3 orders).
  </read_first>
  <behavior>
    - Test 1: a 2-hop FEEDS_INTO chain composes confidence multiplicatively: c = c1 * c2 (with coalesce default 0.5 for a missing confidence), matching the SPFO reduce formula.
    - Test 2: multi-hop candidates are ordered by hops then composed-confidence DESC.
    - Test 3: a surfaced candidate carries its `transform` chain (the per-hop transforms) where present.
    - Test 4: the recommender makes zero Brain/network calls at rank time (Local-Only, INV-12); final surfaced ordering still defers to the Part-3 MAX_K ranker (no duplicate ranker).
  </behavior>
  <action>
    Extend lib/workflow/local-chain-recommender.cjs to compose multi-hop chain confidence MULTIPLICATIVELY using the verified formula (`reduce(c=1.0, r | c * coalesce(r.confidence, 0.5))`), order candidates by hops then composed-confidence DESC, and carry the per-hop `transform` descriptors on the surfaced candidate where present. Keep it Local-Only (reads the committed projection; zero Brain at rank time) and keep the final surfaced ordering deferring to the Part-3 MAX_K ranker (do not duplicate it). Write tests/test-chain-transform-composition.cjs covering the four behaviors against an in-memory projection fixture; register it in tests/run-all-172.sh.
  </action>
  <verify>
    <automated>node tests/test-chain-transform-composition.cjs && bash tests/run-all-172.sh</automated>
  </verify>
  <acceptance_criteria>
    - lib/workflow/local-chain-recommender.cjs composes multi-hop confidence multiplicatively (the SPFO reduce formula) and orders by hops then composed-confidence.
    - A surfaced multi-hop candidate carries its per-hop transform descriptors where present.
    - The recommender makes zero Brain/network calls at rank time (grep + test assertion); final ordering defers to the Part-3 MAX_K ranker.
    - tests/test-chain-transform-composition.cjs passes; registered in run-all-172.sh; bash tests/run-all-172.sh green.
  </acceptance_criteria>
  <done>The Brain-off ranker matches the Brain-on multiplicative chain-confidence model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transform field -> Brain | transform is a generic handoff descriptor (a short machinery string), never user content; it rides the PROJECTION FEEDS_INTO (LOCAL cache). |
| recommender compose -> Brain | MUST NOT cross. Multi-hop composition reads the LOCAL projection file only; zero Brain at rank time (INV-12). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-40 | Information disclosure | transform descriptor | accept | transform is generic machinery metadata (a chain-step descriptor); the Phase 157 projection boundary scan field-allowlist fences any user-content field. |
| T-172-41 | Information disclosure | live Brain at rank time | mitigate | Test 4 asserts zero Brain calls at rank time (INV-12 Local-Only). |
| T-172-42 | Tampering | a new edge property on the frozen navigation edge | mitigate | transform rides the PROJECTION FEEDS_INTO only; Task 1 acceptance asserts navigation ALLOWED_EDGE_TYPES is untouched. |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; data + generator + recommender edits. |
</threat_model>

<verification>
- node scripts/build-orchestration-projection.cjs --check exit 0; FEEDS_INTO carries transform where sourced.
- node tests/test-chain-transform-composition.cjs exit 0; bash tests/run-all-172.sh green.
- No property added to navigation ALLOWED_EDGE_TYPES; Local-Only at rank time.
</verification>

<success_criteria>
The LOCAL chain model mirrors the verified Brain schema: curated_chains + projection FEEDS_INTO carry transform, and the local recommender composes multi-hop confidence multiplicatively - the Brain-off ranker is a faithful mirror of the Brain-on model (R6/INV-08 refined), with no new primitive minted.
</success_criteria>

<artifacts_this_phase_produces>
- MODIFIED: data/command-registry.json curated_chains (transform field added)
- MODIFIED: scripts/build-orchestration-projection.cjs (FEEDS_INTO carries transform)
- REGENERATED: data/brain-orchestration-projection.json (FEEDS_INTO edges carry transform)
- MODIFIED: lib/workflow/local-chain-recommender.cjs (multiplicative multi-hop composition)
- NEW FILE: tests/test-chain-transform-composition.cjs
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-15-SUMMARY.md` when done
</output>
