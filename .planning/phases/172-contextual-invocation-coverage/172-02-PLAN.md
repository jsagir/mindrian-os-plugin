---
phase: 172-contextual-invocation-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CIRS-RELATIONSHIP-CONTRACT.md
  - docs/CANON-PHASE-MAP.md
  - scripts/check-cirs-declaration.cjs
  - tests/test-cirs-declaration.cjs
autonomous: true
requirements: [INV-22, INV-13]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["docs/CANON-PHASE-MAP.md CIRS column"]
  gate_impact: "Ships the R12/INV-22 forward-declaration contract + the gate hook that fails a phase touching a surface without a conformant cirs_relationship block. This plan dog-foods the contract: its own frontmatter is the gate's first test case."
  explanation: "This plan IS the self-propagation mechanism of CIRS: it defines the cirs_relationship schema, adds the CANON-PHASE-MAP CIRS column, and lands the validator hook so no future phase can silently change the invocation surface."
user_setup: []

must_haves:
  truths:
    - "A phase plan that adds/modifies/removes an invocable surface must carry a conformant cirs_relationship block or the validator FAILS it."
    - "The cirs_relationship schema (surfaces_added/surfaces_modified/surfaces_removed/spine_consumed/gate_impact + prose) is documented as a contract."
    - "CANON-PHASE-MAP.md carries a CIRS column keyed on phase SLUG, mirroring the canon_parts forward-compatibility rule."
  artifacts:
    - path: "docs/CIRS-RELATIONSHIP-CONTRACT.md"
      provides: "The R12 declaration contract: the cirs_relationship frontmatter schema + the 'declaring any cirs_relationship field auto-implies 11 in canon_parts' derivation rule."
      contains: "cirs_relationship"
    - path: "scripts/check-cirs-declaration.cjs"
      provides: "The R12 gate hook: scans plan frontmatter; a plan that lists surfaces in files_modified under commands/skills/agents but lacks a conformant cirs_relationship FAILS."
      exports: ["checkCirsDeclaration"]
    - path: "docs/CANON-PHASE-MAP.md"
      provides: "A CIRS column section keyed on phase slug."
      contains: "cirs_relationship"
  key_links:
    - from: "scripts/check-cirs-declaration.cjs"
      to: "cirs_relationship frontmatter block"
      via: "frontmatter parse + canon_parts-11 derivation"
      pattern: "cirs_relationship"
    - from: "docs/CANON-PHASE-MAP.md CIRS column"
      to: "phase slug"
      via: "slug-keyed row"
      pattern: "Phase 172"
---

<objective>
Ship the CIRS R12 / INV-22 forward-declaration + explainability contract: the `cirs_relationship:` frontmatter schema, the CANON-PHASE-MAP CIRS column (slug-keyed), and the validator hook that gate-FAILS any phase touching an invocable surface without a conformant declaration.

Purpose: CIRS is self-propagating only if it is self-enforcing. Without this, a future phase silently changes the invocation surface and the moat regresses at the phase boundary - exactly the failure mode 143.x/144.1 demonstrated. This plan makes "understand, declare, and explain your relationship to the invocation ruling system" a gate, not a guideline.

Output: docs/CIRS-RELATIONSHIP-CONTRACT.md, a CIRS column in CANON-PHASE-MAP.md, scripts/check-cirs-declaration.cjs, and its test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@docs/MINDRIAN-CANON.md
@docs/CANON-PHASE-MAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the CIRS-RELATIONSHIP-CONTRACT.md declaration schema</name>
  <files>docs/CIRS-RELATIONSHIP-CONTRACT.md</files>
  <read_first>
    - docs/MINDRIAN-CANON.md Part 11 R12 (the forward-declaration rule + the canon_parts-11 specialization derivation: "declaring any cirs_relationship field auto-implies 11 in canon_parts; the gate derives one from the other so they cannot disagree").
    - docs/CANON-PHASE-MAP.md "Forward-compatibility rule" section (the canon_parts declaration discipline this specializes) + the "Phase-number collision (Part 6 fragility)" note (why R12 keys on SLUG not number).
    - .planning/phases/172-contextual-invocation-coverage/172-SPEC.md INV-22.
  </read_first>
  <action>
    Author docs/CIRS-RELATIONSHIP-CONTRACT.md specifying: (1) the `cirs_relationship:` frontmatter block with its five fields - `surfaces_added`, `surfaces_modified`, `surfaces_removed` (arrays of surface slugs), `spine_consumed` (array of spine assets read), `gate_impact` (string) - plus a required prose `explanation` field; (2) the rule that declaring ANY cirs_relationship field auto-implies `11` in `canon_parts` (the gate derives one from the other so they cannot disagree, per R12); (3) the trigger: a phase whose plans modify files under commands/, skills/, or agents/, OR that consume the spine (build-connector-registry, build-orchestration-projection, navigation-engine decide(), chain-executor runChain, the connector/projection registries), MUST declare; (4) that the declaration is recorded in CANON-PHASE-MAP via a CIRS column keyed on phase SLUG (absorbing the map's number-collision warning). No fenced code in the body beyond the frontmatter example block. Reference Canon Part 11 R12 + Appendix D entry 25.
  </action>
  <verify>
    <automated>grep -q "cirs_relationship" docs/CIRS-RELATIONSHIP-CONTRACT.md && grep -q "auto-implies" docs/CIRS-RELATIONSHIP-CONTRACT.md && grep -q "slug" docs/CIRS-RELATIONSHIP-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/CIRS-RELATIONSHIP-CONTRACT.md documents all five cirs_relationship fields + the prose explanation requirement.
    - The doc states the canon_parts-11 auto-derivation rule (R12 specialization).
    - The doc states the gate triggers on surface-touching OR spine-consuming phases, keyed on phase slug.
    - No em-dashes anywhere in the file.
  </acceptance_criteria>
  <done>The R12 declaration contract is a documented, slug-keyed, canon-anchored schema.</done>
</task>

<task type="auto">
  <name>Task 2: Add the slug-keyed CIRS column to CANON-PHASE-MAP.md</name>
  <files>docs/CANON-PHASE-MAP.md</files>
  <read_first>
    - docs/CANON-PHASE-MAP.md the existing "### Part 11 - The Invocation Constitution (CIRS)" section (already present with a CIRS-relationship table + implementer re-tags) - extend it; do NOT duplicate it.
    - docs/CANON-PHASE-MAP.md "Forward-compatibility rule" + "## How to read this map".
  </read_first>
  <action>
    In the existing Part 11 section of CANON-PHASE-MAP.md, formalize the CIRS column as the slug-keyed forward-declaration ledger: add a short rubric line stating every future phase row in this section is keyed on phase SLUG and carries its cirs_relationship summary (surfaces touched + spine consumed + gate impact). Confirm the Phase 172 row reads "IMPLEMENTS CIRS R1-R14" and that the 170/171 conformance-held rows are present (they already are). Add a one-line pointer to docs/CIRS-RELATIONSHIP-CONTRACT.md as the schema source. Touch ONLY this section (exclusive ownership; Plan 172-01 and others do not edit CANON-PHASE-MAP).
  </action>
  <verify>
    <automated>grep -q "Part 11 - The Invocation Constitution" docs/CANON-PHASE-MAP.md && grep -q "CIRS-RELATIONSHIP-CONTRACT.md" docs/CANON-PHASE-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - The CANON-PHASE-MAP Part 11 section references docs/CIRS-RELATIONSHIP-CONTRACT.md as the schema source.
    - The section states the CIRS column is keyed on phase slug.
    - The Phase 172 + 170 + 171 rows are intact (grep finds all three).
  </acceptance_criteria>
  <done>The CIRS column is the slug-keyed, contract-anchored forward-declaration ledger.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land the R12 gate hook validator + test</name>
  <files>scripts/check-cirs-declaration.cjs, tests/test-cirs-declaration.cjs</files>
  <read_first>
    - scripts/build-connector-registry.cjs:605-651 (the --check exit-code + recovery-line idiom to mirror).
    - .planning/phases/172-contextual-invocation-coverage/172-02-PLAN.md (THIS plan's own frontmatter - the validator's first conformant test case) and 172-01-PLAN.md (also conformant) - read their cirs_relationship blocks as positive fixtures.
    - docs/CIRS-RELATIONSHIP-CONTRACT.md (Task 1 output - the schema the validator enforces).
  </read_first>
  <behavior>
    - Test 1: a plan whose files_modified touches commands/foo.md and carries a conformant cirs_relationship block PASSES.
    - Test 2: a plan whose files_modified touches skills/x/SKILL.md but has NO cirs_relationship block FAILS with a named error.
    - Test 3: a plan declaring a cirs_relationship field but missing 11 from canon_parts FAILS (the auto-derivation disagreement).
    - Test 4: a plan that touches no invocable surface and has no cirs_relationship block PASSES (declaration only required when triggered).
  </behavior>
  <action>
    Write scripts/check-cirs-declaration.cjs exporting `checkCirsDeclaration(planFrontmatter)` returning `{ok, errors[]}` and a CLI `--check <planpath...>` mode that exits non-zero on any error with a recovery line pointing at docs/CIRS-RELATIONSHIP-CONTRACT.md. Logic: if files_modified contains any path under commands/|skills/|agents/ OR spine_consumed is non-empty, REQUIRE a conformant cirs_relationship block (all five fields present, explanation prose non-empty) AND require `11` in canon_parts (R12 auto-derivation). Reads LOCAL plan files only; zero Brain, zero network. Write tests/test-cirs-declaration.cjs covering the four behaviors with in-memory frontmatter fixtures.
  </action>
  <verify>
    <automated>node tests/test-cirs-declaration.cjs && node scripts/check-cirs-declaration.cjs --check .planning/phases/172-contextual-invocation-coverage/172-02-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-cirs-declaration.cjs` exits 0 with all four behaviors passing.
    - `node scripts/check-cirs-declaration.cjs --check` on this plan (172-02) exits 0 (self-conformant).
    - A surface-touching plan with no cirs_relationship block exits non-zero with a recovery line.
    - The validator makes zero Brain/network calls (grep the source for fetch/http/brain returns 0 non-comment matches).
  </acceptance_criteria>
  <done>The R12 gate hook fails a phase touching a surface without a conformant declaration; it self-validates this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan frontmatter -> validator | The validator reads LOCAL plan markdown only; emits no network traffic. |
| cirs_relationship content -> Brain | MUST NOT cross. The block carries surface slugs + spine asset names (generic machinery metadata), never user content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-03 | Tampering | a phase silently changing the invocation surface | mitigate | check-cirs-declaration.cjs FAILS a surface-touching plan with no conformant declaration (R12). This is the structural cure. |
| T-172-04 | Repudiation | undeclared spine consumption | mitigate | spine_consumed non-empty also triggers the requirement, so consuming the spine without declaring is gate-failed. |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; pure-Node validator. |
</threat_model>

<verification>
- node tests/test-cirs-declaration.cjs exit 0.
- node scripts/check-cirs-declaration.cjs --check on 172-02 and 172-01 exit 0.
- docs/CIRS-RELATIONSHIP-CONTRACT.md + CANON-PHASE-MAP CIRS column present.
</verification>

<success_criteria>
The R12 declaration contract is documented, slug-keyed in the phase map, and gate-enforced by a validator that self-validates this plan - CIRS is self-propagating and self-enforcing.
</success_criteria>

<artifacts_this_phase_produces>
- NEW FILE: docs/CIRS-RELATIONSHIP-CONTRACT.md (the R12 schema)
- NEW FILE: scripts/check-cirs-declaration.cjs (the R12 gate hook)
- NEW FILE: tests/test-cirs-declaration.cjs
- NEW SECTION: the slug-keyed CIRS column rubric in CANON-PHASE-MAP.md
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-02-SUMMARY.md` when done
</output>
