---
phase: 172-contextual-invocation-coverage
plan: 06
type: execute
wave: 4
depends_on: ["172-05"]
files_modified:
  - commands/*.md
  - skills/*/SKILL.md
  - agents/*.md
  - data/connector-registry.json
  - data/connector-coverage-ledger.json
autonomous: false
requirements: [INV-01, INV-03]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: ["the residual dark command set (admin, dashboard, doctor, export, graph, heal, help, models, present, publish, room, rooms, setup, snapshot, status, update, vault, visualize, wiki, and the rest of the ~32 fully-dark + the 3 extra half-wired dogfood-flush/feynman-timeline-refresh/suggest-next)"]
  surfaces_removed: []
  spine_consumed: ["data/connector-registry.json", "data/connector-coverage-ledger.json", "scripts/build-connector-registry.cjs coverageReport()"]
  gate_impact: "Classifies the ENTIRE residual dark surface set (commands + skills + agents) as WIRE or EXCLUDE-with-reason (R1) per the navigator-approved 2026-06-23 split (11 WIRE / 43 EXCLUDE, /mos:act + /mos:ingest-methodology left to Plans 08/12), driving counts.gap to 2 (then 0 at the Plan 13 flip). The full INV-01 classification pass."
  explanation: "This plan exhaustively classifies every remaining dark surface (INV-01) and excludes the correctly-manual utilities (INV-03). It consumes the exclude mechanism + coverageReport; mints no reach/edge/node. Navigator-gated because it is the exhaustive coverage decision that unblocks the hard flip."
user_setup: []

must_haves:
  truths:
    - "EVERY remaining surface (commands + skills + agents) is classified WIRE (connector) or EXCLUDE (connector:{excluded:true,reason}) per the navigator-approved 2026-06-23 classification; none is dark-by-accident (INV-01/INV-03)."
    - "After this plan the ONLY remaining gap surfaces are /mos:act (Plan 08) and /mos:ingest-methodology (Plan 12); counts.gap === 2 and equals exactly that set. True gap===0 is asserted at the Wave-7 hard flip (Plan 13), AFTER 08+12 wire those two."
    - "Each EXCLUDE carries a specific reason; each WIRE carries a frozen-6 reach + a framework or the additive-degrade null-framework shape. Pipeline is WIRED (navigator directive 2026-06-23), not excluded."
  artifacts:
    - path: "data/connector-coverage-ledger.json"
      provides: "Regenerated ledger: only /mos:act + /mos:ingest-methodology remain gap (counts.gap === 2, owned by Plans 08/12); every other surface wired-or-excluded."
      contains: "excluded"
    - path: "commands/doctor.md"
      provides: "An excluded connector block with a specific reason (representative of the utility set)."
      contains: "excluded: true"
  key_links:
    - from: "the residual dark commands"
      to: "data/connector-coverage-ledger.json"
      via: "WIRE or EXCLUDE classification"
      pattern: "excluded"
    - from: "coverageReport() counts.gap"
      to: "zero"
      via: "exhaustive classification"
      pattern: "gap"
---

<objective>
Exhaustively classify the ENTIRE residual dark surface set - commands + skills + agents (INV-01): every surface not wired by Plans 04/05/08/12 is marked WIRE (a connector block) or EXCLUDE-with-reason (connector:{excluded:true,reason}, INV-03), per the NAVIGATOR-APPROVED 2026-06-23 split (11 WIRE / 43 EXCLUDE). This drives counts.gap to exactly 2 (/mos:act + /mos:ingest-methodology, owned by Plans 08/12); true gap===0 is asserted at the Wave-7 hard flip after those land.

Purpose: 54 surfaces are dark (38 commands + 14 skills + 2 agents). Most are correctly-manual utilities, render commands, or ambient always-on infra. The sin is not being dark; it is being dark WITHOUT A DECISION (RETRO-07). Every surface must carry a WIRE or EXCLUDE decision in its own frontmatter so the hard gate can flip green. The exact WIRE/EXCLUDE assignment is fixed by the navigator (Task 1 action) - do not re-decide it.

Output: WIRE or EXCLUDE blocks across the full residual set (commands + skills + agents) + the regenerated registry/ledger with counts.gap === 2 (the act + ingest residual). Navigator-gated (autonomous:false) - this is the exhaustive coverage decision, already approved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@docs/CONNECTOR-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the residual gap set and classify each WIRE-candidate vs EXCLUDE-utility</name>
  <files>commands/*.md</files>
  <read_first>
    - data/connector-coverage-ledger.json (the AUTHORITATIVE residual gap set after Plan 05's regeneration - run coverageReport() to enumerate every state:gap surface; this is the exact work list, do NOT rely on a hand-list).
    - docs/CONNECTOR-CONTRACT.md the section-2 exclude sub-keys (Plan 01) + section 4 additive-degrade + Canon Part 11 R1.
    - 172-CONTEXT.md coverage_baseline (the named utility set) + INV-05 (a non-framework command that should trigger/chain/monitor gets a mindrian-operation counterpart - those are WIRE, not EXCLUDE).
    - representative dark command files: commands/doctor.md, dashboard.md, graph.md, heal.md, present.md, vault.md, visualize.md, wiki.md, status.md, update.md, room.md, organize.md, onboard.md, radar.md, query.md, brain-derive.md, explain-decision.md, memory.md, splash.md, scheduled-tasks.md, speakers.md, correct-reference-now.md, mos.md, hmi-status.md, dogfood-flush.md, feynman-timeline-refresh.md, suggest-next.md.
  </read_first>
  <action>
    Run coverageReport() to enumerate every state:gap surface (commands + skills + agents), then apply the NAVIGATOR-APPROVED classification below VERBATIM (decided 2026-06-23; do NOT re-litigate it). Add a `connector:` block for WIRE; a `connector:{excluded:true, reason}` block with a SPECIFIC reason for EXCLUDE. Mint no reach/edge/node/Brain-wire (frozen-6 reaches only). Leave /mos:act and /mos:ingest-methodology ALONE - they are wired by Plans 08 and 12 respectively, so they remain gap after this plan (that is expected; do not exclude them).

    WIRE (9 here; reach = context_block unless noted; framework or additive-degrade null-framework shape per CONNECTOR-CONTRACT):
    - /mos:pipeline (class pipeline; chain TARGET + chainer - act sequences into it, its own FEEDS_INTO express the steps)
    - /mos:suggest-next (class intelligence; renders ranked chain candidates off the projection)
    - /mos:graph (class intelligence; graph exploration)
    - /mos:memory (class intelligence; memory inspection/navigation)
    - skill:trending-to-absurd (class framework; visionary-innovation methodology)
    - skill:mva-pipeline (class pipeline; MVA chain)
    - skill:mullins-scaffold (class framework; reach brain_consult; Mullins 7-Domains)
    - skill:MOSDeckEngine (class intelligence; Feynman deck engine)
    - skill:client-discovery-interview (class framework; discovery methodology)

    EXCLUDE-with-reason (43; each reason specific, naming the class + why no problem-state trigger):
    - Ambient always-on infra (run every turn, not problem-state triggered): skill:larry-personality, skill:context-engine, skill:conversation-mode, skill:intelligence-orchestrator, skill:brain-connector, skill:pws-methodology, skill:room-passive, skill:room-proactive, skill:ui-system, agent:larry-extended, agent:framework-runner
    - Utility / render (navigator-invoked, no problem-state trigger): doctor, admin, dashboard, export, present, visualize, wiki, setup, help, models, mos, splash, status, room, rooms, speakers
    - Lifecycle / maintenance (navigator or scheduled, not contextual): heal, update, snapshot, publish, scheduled-tasks, dogfood-flush, feynman-timeline-refresh, correct-reference-now, onboard, vault, hmi-status, brain-derive, organize, query (deprecated), radar, explain-decision

    For agents (agents/*.md) and skills (skills/*/SKILL.md), use the same connector:{excluded:true,reason} frontmatter shape the connector generator already reads. The 4 promotion-path candidates the navigator reviewed (explain-decision, radar, brain-derive, organize) are EXCLUDED with a reason noting INV-06 promotion candidacy. No em-dashes in reasons.
  </action>
  <verify>
    <automated>node -e "const g=require('./scripts/build-connector-registry.cjs'); const r=g.coverageReport(); const gaps=r.surfaces.filter(s=>s.state==='gap').map(s=>s.surface).sort(); console.log('gap:',r.counts.gap,gaps.join(',')); const ok=r.counts.gap===2 && gaps.join(',')==='/mos:act,/mos:ingest-methodology'; if(!ok)process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - coverageReport().counts.gap === 2 and the ONLY two gaps are exactly /mos:act and /mos:ingest-methodology (owned by Plans 08/12). Every other surface (command, skill, agent) is WIRE or EXCLUDE.
    - Every EXCLUDE carries a specific reason (not a bare "utility").
    - Every WIRE carries a frozen-6 reach_id (no new reach minted); /mos:pipeline is WIRED.
    - The 4 promotion-path candidates (explain-decision, radar, brain-derive, organize) are EXCLUDED-with-reason noting INV-06 candidacy.
  </acceptance_criteria>
  <done>The entire residual dark set is classified; counts.gap is zero.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the registry + ledger; verify counts.gap === 0</name>
  <files>data/connector-registry.json, data/connector-coverage-ledger.json</files>
  <read_first>
    - scripts/build-connector-registry.cjs (the exclude path + ledger emission + the --check branch).
    - data/connector-coverage-ledger.json (the rolling ledger).
  </read_first>
  <action>
    Run `node scripts/build-connector-registry.cjs` to regenerate the registry + coverage ledger. Confirm `--check` exits 0 (warn-only on the 2 residual gaps - the hard flip is Plan 13) and that counts.gap === 2 with the two gaps being exactly /mos:act + /mos:ingest-methodology. Report the final wired/excluded split in the SUMMARY. If a harness-manifest STALE tripwire fires, run `node scripts/build-harness-manifest.cjs` and stage it in the same commit (the sanctioned lockstep plans 172-04/05 hit). Do NOT hand-edit the JSON.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs && node scripts/build-connector-registry.cjs --check && node -e "const l=require('./data/connector-coverage-ledger.json'); const arr=l.surfaces||[]; const gaps=arr.filter(s=>s.state==='gap').map(s=>s.surface).sort(); if(!(l.counts.gap===2 && gaps.join(',')==='/mos:act,/mos:ingest-methodology'))process.exit(1); console.log('wired:',l.counts.wired,'excluded:',l.counts.excluded,'gap:',l.counts.gap,gaps.join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-connector-registry.cjs --check` exits 0 (warn-only this stage).
    - data/connector-coverage-ledger.json counts.gap === 2, the two gaps being exactly /mos:act + /mos:ingest-methodology.
    - The SUMMARY records the final wired/excluded split (target: ~79 wired / ~43 excluded / 2 gap).
  </acceptance_criteria>
  <done>The coverage ledger has zero gaps; the baseline is fully wired-or-excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exclude/wire frontmatter -> ledger | The blocks carry only {excluded, reason} or frozen connector enums; reasons are author rationales, never user content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-11 | Tampering | silent exclusion | mitigate | A missing reason is a build error (Plan 01); each exclusion carries a specific rationale, so no command is silently dark. |
| T-172-12 | Information disclosure | exclude reasons | accept | Reasons are machinery rationales (invocation semantics), never user content. |
| T-172-34 | Tampering | an over-eager WIRE | mitigate | Plan is navigator-gated; the navigator confirms each WIRE/EXCLUDE decision is reasonable before the hard flip. |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; frontmatter + a generator run. |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check exit 0.
- coverageReport().counts.gap === 0.
- NAVIGATOR GATE: the exhaustive classification reviewed before merge (autonomous:false).
</verification>

<success_criteria>
The entire residual dark command set is classified WIRE or EXCLUDE-with-reason, counts.gap is zero, and the gate passes - the exhaustive INV-01 coverage that unblocks the Wave-7 hard flip.
</success_criteria>

<artifacts_this_phase_produces>
- MODIFIED: WIRE or EXCLUDE blocks across the full residual dark command set (~32 commands + 3 extra half-wired)
- REGENERATED: data/connector-registry.json, data/connector-coverage-ledger.json (counts.gap -> 0)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-06-SUMMARY.md` when done
</output>
