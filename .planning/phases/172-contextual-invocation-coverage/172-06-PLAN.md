---
phase: 172-contextual-invocation-coverage
plan: 06
type: execute
wave: 4
depends_on: ["172-05"]
files_modified:
  - commands/*.md
  - data/connector-registry.json
  - data/connector-coverage-ledger.json
autonomous: false
requirements: [INV-01, INV-03]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: ["the residual dark command set (admin, dashboard, doctor, export, graph, heal, help, models, present, publish, room, rooms, setup, snapshot, status, update, vault, visualize, wiki, and the rest of the ~32 fully-dark + the 3 extra half-wired dogfood-flush/feynman-timeline-refresh/suggest-next)"]
  surfaces_removed: []
  spine_consumed: ["data/connector-registry.json", "data/connector-coverage-ledger.json", "scripts/build-connector-registry.cjs coverageReport()"]
  gate_impact: "Classifies the ENTIRE residual dark command set as WIRE (connector) or EXCLUDE-with-reason (R1), driving counts.gap to zero so the Wave-7 hard-fail flip can pass. The full INV-01 classification pass."
  explanation: "This plan exhaustively classifies every remaining dark surface (INV-01) and excludes the correctly-manual utilities (INV-03). It consumes the exclude mechanism + coverageReport; mints no reach/edge/node. Navigator-gated because it is the exhaustive coverage decision that unblocks the hard flip."
user_setup: []

must_haves:
  truths:
    - "EVERY remaining command is classified WIRE (connector) or EXCLUDE (connector:{excluded:true,reason}); none is dark-by-accident (INV-01/INV-03)."
    - "After this plan counts.gap in the coverage ledger is ZERO (or only navigator-confirmed deferred items), the precondition for the Wave-7 hard flip."
    - "Each EXCLUDE carries a specific reason; each WIRE carries a frozen-6 reach + a framework or the additive-degrade null-framework shape."
  artifacts:
    - path: "data/connector-coverage-ledger.json"
      provides: "Regenerated ledger with counts.gap === 0 (every surface wired-or-excluded)."
      contains: "\"gap\": 0"
    - path: "commands/doctor.md"
      provides: "An excluded connector block with a specific reason (representative of the utility set)."
      contains: "excluded: true"
  key_links:
    - from: "the residual dark commands"
      to: "data/connector-coverage-ledger.json"
      via: "WIRE or EXCLUDE classification"
      pattern: "excluded"
    - from: "coverageReport() counts.gap"
      to: "zero"
      via: "exhaustive classification"
      pattern: "gap"
---

<objective>
Exhaustively classify the ENTIRE residual dark command set (INV-01): every command not wired by Plans 04/05/08/12 is marked WIRE (a connector block) or EXCLUDE-with-reason (connector:{excluded:true,reason}, INV-03), driving counts.gap to ZERO - the precondition for the Wave-7 hard-fail flip.

Purpose: ~32 commands are fully dark (admin, dashboard, doctor, export, graph, heal, help, models, present, publish, room, rooms, setup, snapshot, status, update, vault, visualize, wiki, and the rest) plus 3 extra half-wired (dogfood-flush, feynman-timeline-refresh, suggest-next). Most are correctly-manual utilities. The sin is not being dark; it is being dark WITHOUT A DECISION (RETRO-07). Every one must carry a WIRE or EXCLUDE decision in its own frontmatter so the hard gate can flip green.

Output: WIRE or EXCLUDE blocks across the full residual set + the regenerated registry/ledger with counts.gap === 0. Navigator-gated (autonomous:false) - this is the exhaustive coverage decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@docs/CONNECTOR-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the residual gap set and classify each WIRE-candidate vs EXCLUDE-utility</name>
  <files>commands/*.md</files>
  <read_first>
    - data/connector-coverage-ledger.json (the AUTHORITATIVE residual gap set after Plan 05's regeneration - run coverageReport() to enumerate every state:gap surface; this is the exact work list, do NOT rely on a hand-list).
    - docs/CONNECTOR-CONTRACT.md the section-2 exclude sub-keys (Plan 01) + section 4 additive-degrade + Canon Part 11 R1.
    - 172-CONTEXT.md coverage_baseline (the named utility set) + INV-05 (a non-framework command that should trigger/chain/monitor gets a mindrian-operation counterpart - those are WIRE, not EXCLUDE).
    - representative dark command files: commands/doctor.md, dashboard.md, graph.md, heal.md, present.md, vault.md, visualize.md, wiki.md, status.md, update.md, room.md, organize.md, onboard.md, radar.md, query.md, brain-derive.md, explain-decision.md, memory.md, splash.md, scheduled-tasks.md, speakers.md, correct-reference-now.md, mos.md, hmi-status.md, dogfood-flush.md, feynman-timeline-refresh.md, suggest-next.md.
  </read_first>
  <action>
    Run coverageReport() (scripts/build-connector-registry.cjs) to enumerate EVERY state:gap surface. For EACH: decide WIRE (it warrants contextual triggering - add a connector block with a frozen-6 reach_id + framework or the additive-degrade null-framework shape; e.g. organize / radar / query / brain-derive / memory may warrant a context_block or cross_room connector) or EXCLUDE (correctly-manual utility/admin/render - add connector:{excluded:true,reason} with a SPECIFIC reason). Most of the ~32 (admin, dashboard, doctor, export, graph, heal, help, hmi-status, models, mos, present, publish, room, rooms, scheduled-tasks, setup, snapshot, splash, status, update, vault, visualize, wiki, correct-reference-now, speakers, onboard, dogfood-flush, feynman-timeline-refresh) are EXCLUDE-utilities; classify generously toward EXCLUDE-with-reason where a command is genuinely navigator-invoked with no problem-state trigger. For a genuinely ambiguous command, EXCLUDE with a reason noting it is a promotion-path candidate (INV-06) rather than leaving it a gap. Every reason must be specific. Mint no reach/edge/node. The 10 commands named in the prior 06 draft (doctor, dashboard, setup, help, export, publish, rooms, snapshot, models, admin) are a subset - this task covers ALL residual gaps, not just those 10.
  </action>
  <verify>
    <automated>node -e "const g=require('./scripts/build-connector-registry.cjs'); const r=g.coverageReport(); console.log('gap:',r.counts.gap); if(r.counts.gap!==0)process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - coverageReport().counts.gap === 0 (every command is WIRE or EXCLUDE; none dark-by-accident).
    - Every EXCLUDE carries a specific reason (not a bare "utility").
    - Every WIRE carries a frozen-6 reach_id (no new reach minted).
    - Any ambiguous command is EXCLUDED-with-reason noting promotion-path candidacy, not left a gap.
  </acceptance_criteria>
  <done>The entire residual dark set is classified; counts.gap is zero.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the registry + ledger; verify counts.gap === 0</name>
  <files>data/connector-registry.json, data/connector-coverage-ledger.json</files>
  <read_first>
    - scripts/build-connector-registry.cjs (the exclude path + ledger emission + the --check branch).
    - data/connector-coverage-ledger.json (the rolling ledger).
  </read_first>
  <action>
    Run `node scripts/build-connector-registry.cjs` to regenerate the registry + coverage ledger. Confirm `--check` exits 0 and counts.gap === 0 in the ledger. Report the final wired/excluded split in the SUMMARY. This is the navigator gate: confirm every residual surface decision is reasonable before the Wave-7 hard flip. Do NOT hand-edit the JSON.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs && node scripts/build-connector-registry.cjs --check && node -e "const l=require('./data/connector-coverage-ledger.json'); if(l.counts.gap!==0)process.exit(1); console.log('wired:',l.counts.wired,'excluded:',l.counts.excluded,'gap:',l.counts.gap)"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-connector-registry.cjs --check` exits 0.
    - data/connector-coverage-ledger.json counts.gap === 0.
    - The SUMMARY records the final wired/excluded split.
  </acceptance_criteria>
  <done>The coverage ledger has zero gaps; the baseline is fully wired-or-excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exclude/wire frontmatter -> ledger | The blocks carry only {excluded, reason} or frozen connector enums; reasons are author rationales, never user content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-11 | Tampering | silent exclusion | mitigate | A missing reason is a build error (Plan 01); each exclusion carries a specific rationale, so no command is silently dark. |
| T-172-12 | Information disclosure | exclude reasons | accept | Reasons are machinery rationales (invocation semantics), never user content. |
| T-172-34 | Tampering | an over-eager WIRE | mitigate | Plan is navigator-gated; the navigator confirms each WIRE/EXCLUDE decision is reasonable before the hard flip. |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; frontmatter + a generator run. |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check exit 0.
- coverageReport().counts.gap === 0.
- NAVIGATOR GATE: the exhaustive classification reviewed before merge (autonomous:false).
</verification>

<success_criteria>
The entire residual dark command set is classified WIRE or EXCLUDE-with-reason, counts.gap is zero, and the gate passes - the exhaustive INV-01 coverage that unblocks the Wave-7 hard flip.
</success_criteria>

<artifacts_this_phase_produces>
- MODIFIED: WIRE or EXCLUDE blocks across the full residual dark command set (~32 commands + 3 extra half-wired)
- REGENERATED: data/connector-registry.json, data/connector-coverage-ledger.json (counts.gap -> 0)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-06-SUMMARY.md` when done
</output>
