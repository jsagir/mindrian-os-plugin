---
phase: 172-contextual-invocation-coverage
plan: 05
type: execute
wave: 3
depends_on: ["172-04"]
files_modified:
  - commands/causal.md
  - commands/diagnostics.md
  - commands/hat-briefing.md
  - commands/persona.md
  - lib/core/navigation-engine.cjs
  - data/connector-registry.json
  - data/connector-coverage-ledger.json
autonomous: true
requirements: [INV-02]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: ["/mos:causal", "/mos:diagnostics", "/mos:hat-briefing", "/mos:persona"]
  surfaces_removed: []
  spine_consumed: ["data/connector-registry.json", "lib/core/navigation-engine.cjs decide()"]
  gate_impact: "Wires the remaining 4 half-wired thinking surfaces (causal+diagnostics -> context_block; hat-briefing+persona -> hats) AND adds the missing 'hats' case to reachIdToSkillFamily so a fired hats reach can flip routing_source. Shrinks the gap count by 4."
  explanation: "This plan completes the half-wired set and repairs the engine-side hats gap (the 6th reach was connector-wired only, engine-unmapped). It consumes the spine + the engine decide() path; mints no new reach."
user_setup: []

must_haves:
  truths:
    - "causal and diagnostics carry connector blocks with reach_id context_block; hat-briefing and persona carry reach_id hats."
    - "reachIdToSkillFamily in navigation-engine.cjs has a 'hats' case so a fired hats reach maps to a canonical verb and flips routing_source."
    - "All eight half-wired commands (rs-* from Plan 04 + these 4) are now wired; --check passes."
  artifacts:
    - path: "lib/core/navigation-engine.cjs"
      provides: "A 'hats' case in reachIdToSkillFamily mapping the 6th reach to a canonical verb."
      contains: "case 'hats'"
    - path: "commands/hat-briefing.md"
      provides: "A connector block wiring hat-briefing into the hats reach."
      contains: "reach_id: hats"
  key_links:
    - from: "lib/core/navigation-engine.cjs reachIdToSkillFamily"
      to: "hats canonical verb"
      via: "switch case 'hats'"
      pattern: "case 'hats'"
    - from: "commands/hat-briefing.md connector"
      to: "data/connector-registry.json"
      via: "build-connector-registry.cjs regeneration"
      pattern: "hat-briefing"
---

<objective>
Complete the half-wired thinking-surface set: wire causal + diagnostics to context_block and hat-briefing + persona to the `hats` reach (D-172-c), and repair the engine-side `hats` gap by adding the missing `hats` case to reachIdToSkillFamily so a fired `hats` reach can flip routing_source legacy->engine.

Purpose: After Plan 04 wires rs-*, four half-wired commands remain. hat-briefing + persona route to the Phase-148 6th reach `hats`, but the engine's reachIdToSkillFamily has no `hats` case (navigation-engine.cjs:399 default returns null), so a `hats` reach cannot flip routing - the 6th reach is connector-wired only, not contextually invocable. This plan closes both gaps.

Output: four connector blocks, the `hats` engine case, and the regenerated registry + ledger.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@docs/CONNECTOR-CONTRACT.md
@.planning/phases/172-contextual-invocation-coverage/research/recon-connector-spine.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire causal + diagnostics to context_block, hat-briefing + persona to hats</name>
  <files>commands/causal.md, commands/diagnostics.md, commands/hat-briefing.md, commands/persona.md</files>
  <read_first>
    - docs/CONNECTOR-CONTRACT.md section 6 worked examples (PUSH-02 context_block for causal/diagnostics; PUSH-05 six-hats for the hats family) + section 7 delimiter convention.
    - commands/causal.md (frameworks: ["Root Cause Analysis"]), commands/diagnostics.md, commands/hat-briefing.md (frameworks: ["Six Thinking Hats"]), commands/persona.md (frameworks: ["Six Thinking Hats"]) - current half-wired frontmatter.
    - commands/think-hats.md (the live analog that already carries reach_id: hats + sub_mode six-hats from Phase 148 - the template + the sub_mode to avoid colliding with).
    - lib/core/insight-sensors.cjs (SENS-03 methodology-decision, SENS-07 gate-approach, SENS-02 lagging-component, SENS-01 first-material - the shipped sensor ids to choose triggers from).
  </read_first>
  <action>
    Add `# --- Phase 143.3 connector frontmatter ---` connector: blocks: (a) commands/causal.md - reach_id context_block, sub_mode causal-trace, framework "Root Cause Analysis", posture pull_back (causal trace per D-172-c), filing fileEvidenceWithReadback, surface F.1, sensor_triggers a shipped SENS id (SENS-02 lagging-component); (b) commands/diagnostics.md - reach_id context_block, sub_mode wave1-fingerprint, framework matching its frameworks: value, posture hold, surface F.1, sensor_triggers [SENS-01]; (c) commands/hat-briefing.md - reach_id hats, sub_mode hat-briefing, framework "Six Thinking Hats", posture hold, sensor_triggers [SENS-07], surface F.1; (d) commands/persona.md - reach_id hats, sub_mode persona, framework "Six Thinking Hats", posture hold, sensor_triggers [SENS-05], surface F.1. Assign distinct hierarchy_rank + distinct sub_modes so no (sensor,reach,sub_mode) tuple collides (validateConnectors check 4; think-hats already holds reach_id hats + sub_mode six-hats, so hat-briefing/persona MUST use different sub_modes). Mint no new reach (context_block + hats are both in the frozen 6). connects_to_spine: true on all four.
  </action>
  <verify>
    <automated>grep -q "reach_id: context_block" commands/causal.md && grep -q "reach_id: context_block" commands/diagnostics.md && grep -q "reach_id: hats" commands/hat-briefing.md && grep -q "reach_id: hats" commands/persona.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - commands/causal.md and commands/diagnostics.md carry connector blocks with reach_id: context_block.
    - commands/hat-briefing.md and commands/persona.md carry connector blocks with reach_id: hats.
    - hat-briefing and persona use sub_modes distinct from each other AND from think-hats' six-hats (no tuple collision).
    - All four carry connects_to_spine: true and a framework matching their existing frameworks: value.
  </acceptance_criteria>
  <done>The remaining four half-wired commands declare connector blocks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the 'hats' case to reachIdToSkillFamily in navigation-engine.cjs</name>
  <files>lib/core/navigation-engine.cjs</files>
  <read_first>
    - lib/core/navigation-engine.cjs:392-402 (reachIdToSkillFamily - the switch lacking a 'hats' case; :399 default returns null), :427-477 (resolveFireSkill - where sensorReaches[0] becomes a canonical verb), :478-488 (the canonical-verb -> skill-id map).
    - lib/core/sensors/sensor-types.cjs:43-50 (frozen REACH_IDS including 'hats').
    - docs/MINDRIAN-CANON.md Appendix D entry 15 (hats is the 6th machine reach_id) + Part 11 R4 (one governed path).
    - the CANONICAL_VERBS source the router validates against (navigation-engine.cjs:53 area) - pick an EXISTING verb the hats reach maps to; do NOT invent a verb.
  </read_first>
  <behavior>
    - Test 1: reachIdToSkillFamily('hats') returns a non-null canonical verb (no longer the default null).
    - Test 2: the returned verb is a member of the existing CANONICAL_VERBS set (mints no new verb).
    - Test 3: a sensorReaches array whose top reach is {reach_id:'hats'} resolves to a non-null fireSkill in resolveFireSkill (so routing_source can flip).
    - Test 4: the five existing cases (context_block/contradiction/cross_room/brain_consult/deep_research) are unchanged in behavior.
  </behavior>
  <action>
    Add a `case 'hats':` returning the most appropriate EXISTING canonical verb to reachIdToSkillFamily (read CANONICAL_VERBS + the verb -> skill-id map at :478-488; the hats reach runs a hat-briefing / Six Thinking Hats pass, so map to "Run Methodology" or "Synthesize" - whichever existing verb's skill-id best fits a hats pass). Mint NO new canonical verb, NO new reach (constraint: the frozen 6 reaches + the Part-3 verb set are untouched; this only fills the missing engine MAPPING for the already-frozen 6th reach). Add or extend the navigation-engine test covering the four behaviors.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('lib/core/navigation-engine.cjs','utf8'); if(!/case 'hats'/.test(s))process.exit(1); console.log('hats case present')"</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/navigation-engine.cjs reachIdToSkillFamily contains a `case 'hats'` returning a non-null verb.
    - The returned verb is an existing member of CANONICAL_VERBS (no new verb minted).
    - A top sensor reach of reach_id 'hats' resolves to a non-null fireSkill (routing_source can flip to engine).
    - The five pre-existing reach cases are behaviorally unchanged (the navigation-engine test suite still passes).
  </acceptance_criteria>
  <done>The engine maps the 6th reach 'hats' to a canonical verb so a fired hats reach flips routing_source.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the connector registry + coverage ledger, verify --check</name>
  <files>data/connector-registry.json, data/connector-coverage-ledger.json</files>
  <read_first>
    - scripts/build-connector-registry.cjs:614-651 (the --check branch).
    - data/connector-coverage-ledger.json (the rolling ledger Plan 04 last updated - these four should flip gap -> wired).
  </read_first>
  <action>
    Run `node scripts/build-connector-registry.cjs` to regenerate data/connector-registry.json AND data/connector-coverage-ledger.json with causal, diagnostics, hat-briefing, persona now wired. Confirm `--check` passes (frozen-6 reach, frozen-3 posture, WFL-01 framework resolution, no tuple collision). The eight previously half-wired commands (rs-* + these four) are now all state:wired. Do NOT hand-edit the JSON.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs && node scripts/build-connector-registry.cjs --check && node -e "const l=require('./data/connector-coverage-ledger.json'); const t=['/mos:causal','/mos:diagnostics','/mos:hat-briefing','/mos:persona']; const w=l.surfaces.filter(s=>t.includes(s.surface)&&s.state==='wired'); if(w.length!==4)process.exit(1); console.log('wired:',w.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-connector-registry.cjs --check` exits 0.
    - causal, diagnostics, hat-briefing, persona all show state:wired in the coverage ledger.
    - The eight original half-wired commands are all wired (the half-wired bucket is empty).
  </acceptance_criteria>
  <done>The registry and ledger reflect all eight half-wired commands as wired and the gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connector frontmatter -> registry | Connector blocks carry only generic handles; the generator cannot carry user content. |
| navigation-engine decide() -> Brain | decide() reads LOCAL context only; the hats mapping adds no Brain wire (Part 11 R7). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-09 | Tampering | hats reach engine mapping | mitigate | The hats case returns an EXISTING canonical verb; a test asserts membership in CANONICAL_VERBS so no new verb/reach is minted. |
| T-172-10 | Information disclosure | hat-briefing/persona connectors | accept | Sub-keys are frozen enums + a published-framework name + a render label; no user-content path (CONNECTOR-CONTRACT.md section 5). |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; frontmatter + one engine case + a generator run. |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check exit 0.
- navigation-engine test suite passes with the hats case.
- All eight half-wired commands state:wired in the coverage ledger.
</verification>

<success_criteria>
The full half-wired set (8 commands) is contextually triggerable, the `hats` 6th reach is engine-mapped (so it can flip routing_source), and the gate passes.
</success_criteria>

<artifacts_this_phase_produces>
- MODIFIED: connector blocks on commands/causal.md, diagnostics.md, hat-briefing.md, persona.md
- MODIFIED: a 'hats' case in lib/core/navigation-engine.cjs reachIdToSkillFamily
- REGENERATED: data/connector-registry.json, data/connector-coverage-ledger.json (4 more surfaces gap -> wired)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-05-SUMMARY.md` when done
</output>
