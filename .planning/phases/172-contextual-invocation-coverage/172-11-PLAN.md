---
phase: 172-contextual-invocation-coverage
plan: 11
type: execute
wave: 3
depends_on: []
files_modified:
  - lib/core/coverage-rollup.cjs
  - lib/core/coverage-monitor.cjs
  - tests/test-coverage-rollup.cjs
autonomous: true
requirements: [INV-09, INV-16]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["lib/core/navigation/edges.cjs NESTED_WITHIN", "data/brain-orchestration-projection.json", "data/connector-coverage-ledger.json"]
  gate_impact: "Adds the R11 scale-invariant fractal coverage rollup over NESTED_WITHIN (depth-3 capped, aggregate-scalar-only across room boundaries) + projection-level monitoring (UN-WIRED/UN-RANKED/STALE/chain-reachability). R11 enforcement stays warn/aspirational (deferred-enforcement)."
  explanation: "This plan implements R11/INV-16 (one scale-invariant rollup operator) + INV-09 (projection-level monitoring). It consumes the NESTED_WITHIN substrate + the projection + the coverage ledger; reads child coverage scalars only, never child lineage edges across room boundaries."
user_setup: []

must_haves:
  truths:
    - "ONE scale-invariant rollup(room) operator rolls coverage up the NESTED_WITHIN hierarchy, depth-3 capped, aggregate-SCALAR-only across room boundaries."
    - "Coverage + chain health is monitored at the PROJECTION level (UN-WIRED / UN-RANKED / STALE / chain reachability)."
    - "Per-user invocation telemetry stays LOCAL; cross-room aggregation reads child coverage scalars, never child lineage edges (entry 23 / Part 11 R11)."
  artifacts:
    - path: "lib/core/coverage-rollup.cjs"
      provides: "The scale-invariant rollup(room) operator applied recursively up NESTED_WITHIN, depth-3 capped, normalized by subtree size."
      exports: ["rollupCoverage"]
    - path: "lib/core/coverage-monitor.cjs"
      provides: "Projection-level coverage + chain-health monitoring (UN-WIRED/UN-RANKED/STALE/chain reachability)."
      exports: ["monitorCoverage"]
  key_links:
    - from: "lib/core/coverage-rollup.cjs"
      to: "NESTED_WITHIN edges"
      via: "recursive parent walk, depth-3 cap"
      pattern: "NESTED_WITHIN"
    - from: "lib/core/coverage-monitor.cjs"
      to: "data/brain-orchestration-projection.json"
      via: "projection-level health read"
      pattern: "projection"
---

<objective>
Implement R11 / INV-16 (the scale-invariant fractal coverage rollup over NESTED_WITHIN, depth-3 capped, aggregate-scalar-only across room boundaries) and INV-09 (projection-level coverage + chain-health monitoring).

Purpose: Coverage + chain monitoring must respect the nested-room fractal (Simon near-decomposability). ONE rollup(room) operator applied recursively up NESTED_WITHIN, precomputing a per-room aggregate summary per level, normalized by subtree size, depth-3 capped (= the SEED-022 memory contract), with cross-room boundary signals travelling ONLY as aggregates. This is the substrate SEED-022/Phase-169 began; 172 MONITORS coverage across it, it does not rebuild it.

Output: the rollup operator, the projection-level monitor, and the rollup test. R11 enforcement is warn/aspirational (deferred-enforcement) - direction is law, hard-FAIL is gated on substrate, so this lands as a monitor, not a hard gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@docs/MINDRIAN-CANON.md
@docs/ORCHESTRATION-PROJECTION-CONTRACT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the scale-invariant coverage rollup operator over NESTED_WITHIN</name>
  <files>lib/core/coverage-rollup.cjs, tests/test-coverage-rollup.cjs</files>
  <read_first>
    - lib/core/navigation/edges.cjs (the frozen ALLOWED_EDGE_TYPES including NESTED_WITHIN, source room:<child> target room:<parent>, props {relation:'nested', parent:<slug>} - the room-lineage edge Phase 169 minted; read-only here).
    - tests/run-all-169.sh + the Phase 169 rollup walk (Plan 04 of 169 - "the rollup walk" - the existing NESTED_WITHIN walk this reuses; do NOT rebuild it, ride it).
    - docs/MINDRIAN-CANON.md Part 11 R11 (depth-bounded, aggregate-SCALAR-only across boundaries; reads child coverage scalars never child lineage edges) + Appendix D entry 23 (cross-room aggregation of NESTED_WITHIN forbidden) + 172-CONTEXT.md D-172-h (depth-3 cap = Simon OOM cutoff = SEED-022 memory contract; GraphRAG hierarchical-Leiden aggregate-vertex; LCA query routing).
    - data/connector-coverage-ledger.json (the per-room coverage scalar source - counts.wired/excluded/gap).
  </read_first>
  <behavior>
    - Test 1: rollupCoverage(room) returns an aggregate coverage summary for the room normalized by subtree size.
    - Test 2: the rollup walks UP NESTED_WITHIN to depth 3 and STOPS (a 4th-level ancestor is not walked - the depth-3 cap).
    - Test 3: across a room boundary, only aggregate SCALARS travel (the rollup reads child coverage counts, never child NESTED_WITHIN lineage edges - entry 23).
    - Test 4: the operator is scale-invariant (the same rollup function applies at every level).
  </behavior>
  <action>
    Write lib/core/coverage-rollup.cjs exporting `rollupCoverage(room, opts)`: ONE scale-invariant operator that reads a room's local coverage scalars (from the coverage ledger / a per-room equivalent), walks UP NESTED_WITHIN (reusing the Phase-169 rollup walk - do not rebuild it), precomputes a per-room aggregate summary per level normalized by subtree size, caps recursion at DEPTH 3 (a frozen constant, = SEED-022), and aggregates cross-room boundary signals as SCALARS ONLY (reads child coverage scalars, NEVER child lineage edges across room.db boundaries). The operator carries no user content (counts/scalars only, Part 8). Write tests/test-coverage-rollup.cjs covering the four behaviors against a synthesized 4-level nested-room fixture.
  </action>
  <verify>
    <automated>node tests/test-coverage-rollup.cjs</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/coverage-rollup.cjs exports rollupCoverage returning an aggregate summary normalized by subtree size.
    - The recursion caps at depth 3 (a 4th-level ancestor is not walked).
    - Cross-room aggregation carries SCALARS only (no child lineage edge is read across a room boundary).
    - tests/test-coverage-rollup.cjs passes all four behaviors.
  </acceptance_criteria>
  <done>The scale-invariant depth-3 fractal coverage rollup over NESTED_WITHIN exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build projection-level coverage + chain-health monitoring</name>
  <files>lib/core/coverage-monitor.cjs, tests/test-coverage-rollup.cjs</files>
  <read_first>
    - scripts/build-orchestration-projection.cjs validateProjection (the UN-WIRED/UN-RANKED/STALE + command_gaps from Plan 03 - the monitor surfaces these at the projection level).
    - data/brain-orchestration-projection.json (the projection the monitor reads) + the FEEDS_INTO chain layer from Plan 10 (chain reachability).
    - docs/MINDRIAN-CANON.md INV-09 (MONITOR: coverage + chain health at the PROJECTION level; per-user invocation telemetry stays LOCAL, Part 8; the remote graph monitors GENERIC machinery shape + chain integrity only).
  </read_first>
  <behavior>
    - Test 1: monitorCoverage(projection) returns a health report {unwired, unranked, stale, command_gaps, chain_reachability}.
    - Test 2: chain_reachability flags a FEEDS_INTO chain whose target framework is absent/unreachable.
    - Test 3: the monitor reads only the LOCAL projection (zero Brain/network; per-user telemetry stays LOCAL).
    - Test 4: the report carries only generic machinery shape (counts + framework/reach names), never user content.
  </behavior>
  <action>
    Write lib/core/coverage-monitor.cjs exporting `monitorCoverage(projection)`: a projection-level health report composing the validateProjection categories (UN-WIRED/UN-RANKED/STALE/command_gaps) + a chain-reachability check over the FEEDS_INTO edges (a chain whose target framework is absent/unreachable is flagged). The monitor reads only the LOCAL projection (zero Brain, INV-12; per-user invocation telemetry stays LOCAL, Part 8; the monitor sees only GENERIC machinery shape + chain integrity). Extend tests/test-coverage-rollup.cjs (or a sibling) with the four monitor behaviors.
  </action>
  <verify>
    <automated>node -e "const m=require('./lib/core/coverage-monitor.cjs'); const p=require('./data/brain-orchestration-projection.json'); const r=m.monitorCoverage(p); if(!r||!('chain_reachability' in r))process.exit(1); console.log('monitor ok')" && node tests/test-coverage-rollup.cjs</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/coverage-monitor.cjs exports monitorCoverage returning {unwired, unranked, stale, command_gaps, chain_reachability}.
    - chain_reachability flags an unreachable FEEDS_INTO target.
    - The monitor makes zero Brain/network calls (grep + test assertion).
    - The report carries only generic machinery shape (counts + names), no user content.
  </acceptance_criteria>
  <done>Coverage + chain health is monitored at the projection level, Local-Only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cross-room rollup -> parent room | Only aggregate SCALARS cross a room boundary; child lineage edges never leave the child room.db (entry 23). |
| coverage monitor -> Brain | MUST NOT cross. The monitor reads the LOCAL projection; per-user telemetry stays LOCAL (Part 8/INV-09). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-23 | Information disclosure | cross-room aggregation | mitigate | Test 3 asserts only scalars cross a room boundary; the rollup reads child coverage scalars, never child lineage edges (Part 11 R11 / entry 23). |
| T-172-24 | Information disclosure | coverage monitor report | mitigate | The monitor report carries counts + framework/reach names only (generic machinery shape); test asserts no user content. |
| T-172-25 | Denial of service | unbounded recursion | mitigate | The depth-3 cap (a frozen constant) bounds the rollup recursion (Simon OOM cutoff / SEED-022). |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; pure-Node rollup + monitor. |
</threat_model>

<verification>
- node tests/test-coverage-rollup.cjs exit 0.
- monitorCoverage returns a projection-level health report.
- Cross-room aggregation carries scalars only; depth-3 cap enforced.
</verification>

<success_criteria>
ONE scale-invariant depth-3 coverage rollup over NESTED_WITHIN exists, projection-level coverage + chain health is monitored Local-Only, and cross-room signals travel only as aggregates - R11/INV-16 + INV-09 met (warn/aspirational enforcement per deferred-enforcement).
</success_criteria>

<artifacts_this_phase_produces>
- NEW FILE: lib/core/coverage-rollup.cjs (the scale-invariant fractal rollup operator)
- NEW FILE: lib/core/coverage-monitor.cjs (projection-level monitoring)
- NEW FILE: tests/test-coverage-rollup.cjs
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-11-SUMMARY.md` when done
</output>
