---
phase: 172-contextual-invocation-coverage
plan: 13
type: execute
wave: 7
depends_on: ["172-08", "172-10", "172-12"]
files_modified:
  - scripts/build-connector-registry.cjs
  - scripts/build-orchestration-projection.cjs
  - scripts/hooks/pre-commit
  - scripts/install-pre-commit.sh
  - scripts/release.sh
  - scripts/doctor.cjs
  - tests/test-coverage-gate-hardfail.cjs
  - docs/MINDRIAN-CANON.md
  - docs/CANON-PHASE-MAP.md
  - tests/test-cirs-four-class-floor.cjs
autonomous: false
requirements: [INV-10, INV-13, INV-14]
canon_parts: [2, 3, 4, 6, 7, 8, 9, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["scripts/build-connector-registry.cjs --check", "scripts/build-orchestration-projection.cjs --check", "scripts/hooks/pre-commit", "scripts/release.sh", "scripts/doctor.cjs"]
  gate_impact: "Flips the coverage gate from WARN to hard-FAIL (R2/R9/INV-10 step 3) and wires --check into pre-commit + install-pre-commit + release.sh + doctor --acceptance. A new dark surface now FAILs the build at merge. This is the structural cure for the recurring regression."
  explanation: "This plan lands the born-wired hard gate (R2/R9/INV-14) across all four enforcement surfaces. It consumes both generators' --check; flips warn->fail ONLY after the baseline is wired/excluded so CI never goes RED mid-sweep. Navigator-gated."
user_setup: []

must_haves:
  truths:
    - "build-connector-registry --check and build-orchestration-projection --check FAIL (not warn) on any surface neither WIRED nor EXCLUDED, and on any un-ranked counterpart."
    - "The gate is wired into pre-commit + install-pre-commit + release.sh + doctor --acceptance (the four enforcement surfaces)."
    - "The flip to hard-FAIL happens AFTER the baseline is wired/excluded so CI never goes RED mid-sweep (D-172-e)."
    - "CIRS R1's unit-of-coverage is sharpened to enumerate the four governed surface CLASSES (mechanical / framework / intelligence / pipeline) as a one-line Part 11 R1 canon amendment; the coverage ledger records a `class` per surface and the gate is class-aware (navigator-LOCKED 2026-06-23, externally grounded in research/172-GOVERNOR-RESEARCH.md - the ArbiterOS governance-ISA paradigm)."
    - "doctor --drift is recorded as the SCHEDULED (periodic) reconciliation surface beside the merge gate (the Wiz/HashiCorp two-timeframe pattern); continuous Brain-sync stays deferred to Phase 137."
  artifacts:
    - path: "scripts/build-connector-registry.cjs"
      provides: "The gap WARN converted to a hard exit-1 FAIL backed by the coverage ledger."
      contains: "gap"
    - path: "scripts/hooks/pre-commit"
      provides: "A staged-path guard running both --check gates."
      contains: "build-connector-registry"
    - path: "tests/test-coverage-gate-hardfail.cjs"
      provides: "Asserts a synthesized dark surface makes --check exit non-zero."
      contains: "exit"
  key_links:
    - from: "scripts/build-connector-registry.cjs --check"
      to: "data/connector-coverage-ledger.json"
      via: "hard-fail on gap"
      pattern: "gap"
    - from: "scripts/release.sh"
      to: "build-connector-registry --check"
      via: "release gate step"
      pattern: "connector"
---

<objective>
Flip the coverage gate from WARN to hard-FAIL (INV-10 step 3 / R2 / R9 / INV-14) and wire `--check` into all four enforcement surfaces: pre-commit + install-pre-commit + release.sh + doctor --acceptance. A new dark surface (neither wired nor excluded) now FAILs at merge - the structural cure for the recurring 143.x/144.1 regression.

Purpose: Today the gate is WARN-only AND CI-orphaned (install-pre-commit checks build-harness-manifest, not the connector check; not in release.sh; not in doctor). This plan makes the born-wired invariant enforced at every merge. The flip happens AFTER Waves 1-3 wired/excluded the baseline so CI never goes RED mid-sweep.

Output: the hard-fail flip in both generators, the four enforcement-surface wirings, and the hard-fail test. Navigator-gated (autonomous:false) - flipping a gate to blocking is a material change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-contextual-invocation-coverage/172-SPEC.md
@.planning/phases/172-contextual-invocation-coverage/research/recon-internal-synthesis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the gap WARN to a hard-FAIL in both generators</name>
  <files>scripts/build-connector-registry.cjs, scripts/build-orchestration-projection.cjs</files>
  <read_first>
    - scripts/build-connector-registry.cjs:632-647 (the WARN nudge to flip; the coverageReport()/ledger gap set from Plan 01) + :614-630 (the existing hard-exit-1 STALE/validation path the gap-fail joins).
    - scripts/build-orchestration-projection.cjs:824 (the inverted early-continue from Plan 03) + the command_gaps array + validateProjection - flip command_gaps from WARN to FAIL.
    - 172-CONTEXT.md D-172-e (warn -> fail; flip ONLY once the baseline is wired/excluded so CI never goes RED mid-sweep) + recon-internal-synthesis.md section B (the two defects: WARN-only + CI-orphaned).
    - data/connector-coverage-ledger.json + data/orchestration-command-ledger.json (the post-Wave-1-3 baseline - confirm gap count is zero or only explicitly-excluded before flipping).
  </read_first>
  <action>
    In scripts/build-connector-registry.cjs --check, convert the gap WARN to a hard exit-1 FAIL: any surface in coverageReport() classified `gap` (neither wired nor excluded) makes --check exit 1 with a recovery line (wire it or exclude it with a reason). In scripts/build-orchestration-projection.cjs --check, convert command_gaps from WARN to a hard FAIL (a bare command counterpart neither ranked nor excluded fails). Guard the flip: confirm the live ledgers show zero un-decided gaps FIRST (Waves 1-3 wired/excluded the baseline); if any genuine gap remains, STOP and surface it to the navigator rather than shipping a RED gate. Keep STALE + the four CONN-03 validations + the frozen banks untouched.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check && echo "both gates green"</automated>
  </verify>
  <acceptance_criteria>
    - Both `--check` gates exit 0 on the clean post-Wave-1-3 repo (baseline fully wired/excluded).
    - A surface classified `gap` makes build-connector-registry --check exit non-zero (hard fail, not warn).
    - A bare command counterpart (not ranked, not excluded) makes build-orchestration-projection --check exit non-zero.
    - The frozen banks + STALE + CONN-03 validations are unchanged.
  </acceptance_criteria>
  <done>The gate is hard-FAIL on any accidental-dark surface, green on the wired/excluded baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Wire both --check gates into pre-commit + install-pre-commit + release.sh + doctor --acceptance</name>
  <files>scripts/hooks/pre-commit, scripts/install-pre-commit.sh, scripts/release.sh, scripts/doctor.cjs</files>
  <read_first>
    - scripts/install-pre-commit.sh:36 + :80-83 + :127-130 (the build-harness-manifest --check staged-path guard - the EXACT idiom to mirror for build-connector-registry + build-orchestration-projection; note the projection --check is ALREADY wired per the recon, confirm and add the connector --check).
    - scripts/hooks/pre-commit (the committed hook - add the connector --check staged-path guard alongside the existing projection one at :184-186 per the recon).
    - scripts/release.sh:200-280 (the Step 2.5 / 6.6 / 9.8 doctor --acceptance gate structure - add the connector + projection --check as a release gate step, or confirm doctor --acceptance covers it).
    - scripts/doctor.cjs:148-234 (the --acceptance flag + the 7-point checklist - add the coverage gate as an acceptance check).
    - recon-internal-synthesis.md section B (build-connector-registry --check is NOT in pre-commit / release / doctor today; the projection --check IS in pre-commit + the Feynman runner).
  </read_first>
  <action>
    Wire the connector --check into the same four surfaces the projection --check uses: (a) scripts/hooks/pre-commit + scripts/install-pre-commit.sh - add a staged-path guard running `node scripts/build-connector-registry.cjs --check` when any commands/*.md, skills/*/SKILL.md, agents/*.md, or the connector/coverage-ledger JSONs are staged (mirror the build-harness-manifest guard idiom; the projection --check is already wired - confirm both are present); (b) scripts/release.sh - add a release gate step running both --check gates (or fold into doctor --acceptance); (c) scripts/doctor.cjs --acceptance - add the coverage gate (both --check) to the acceptance checklist. Reuse the existing guard idiom (Part 7); do not invent a new gate harness.
  </action>
  <verify>
    <automated>grep -q "build-connector-registry.cjs --check" scripts/hooks/pre-commit scripts/install-pre-commit.sh && grep -q "build-connector-registry.cjs --check\|build-orchestration-projection.cjs --check" scripts/release.sh scripts/doctor.cjs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - scripts/hooks/pre-commit + scripts/install-pre-commit.sh run build-connector-registry --check on the relevant staged paths.
    - scripts/release.sh runs the coverage gate (both --check, directly or via doctor --acceptance).
    - scripts/doctor.cjs --acceptance includes the coverage gate.
    - Both --check gates are present in all four enforcement surfaces (connector + projection).
  </acceptance_criteria>
  <done>The born-wired gate is enforced at pre-commit, install, release, and doctor --acceptance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hard-fail adversarial test (a synthesized dark surface FAILs --check)</name>
  <files>tests/test-coverage-gate-hardfail.cjs</files>
  <read_first>
    - tests/fixtures/orchestration-unwired/UNWIRED-FIXTURE.md (the existing un-wired fixture pattern under tests/fixtures/ that is NEVER walked by the live generator - mirror it for a dark-command fixture).
    - scripts/build-connector-registry.cjs coverageReport() + the --check exit-code contract.
  </read_first>
  <behavior>
    - Test 1: a synthesized dark surface (no connector, no exclude) makes coverageReport classify it `gap` and --check would exit non-zero.
    - Test 2: a synthesized excluded surface (excluded:true+reason) does NOT trip the gate.
    - Test 3: the clean live repo passes --check exit 0 (the fixture lives under tests/fixtures, never walked by the live generator).
    - Test 4: the gate FAIL message names the offending surface + the recovery line.
  </behavior>
  <action>
    Write tests/test-coverage-gate-hardfail.cjs proving the hard-fail by construction: feed coverageReport()/validateConnectors a synthesized dark-surface fixture (in-memory or under tests/fixtures, never walked by the live generator) and assert the gate flags it as gap and the --check exit-code contract would fail; assert an excluded fixture does not trip it; assert the real repo stays exit 0. Register the test in the Feynman runner / the connector test runner.
  </action>
  <verify>
    <automated>node tests/test-coverage-gate-hardfail.cjs && node scripts/build-connector-registry.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-coverage-gate-hardfail.cjs proves a synthesized dark surface trips the gate (RED) and the real repo stays GREEN.
    - An excluded fixture does not trip the gate.
    - The test is registered in the test runner.
  </acceptance_criteria>
  <done>The hard-fail is proven adversarially and the real repo stays green.</done>
</task>

<task type="auto">
  <name>Task 4: Four-class governance-ISA - class-aware gate + the one-line Part 11 R1 canon amendment</name>
  <files>scripts/build-connector-registry.cjs, docs/MINDRIAN-CANON.md, docs/CANON-PHASE-MAP.md, tests/test-cirs-four-class-floor.cjs</files>
  <read_first>
    - .planning/phases/172-contextual-invocation-coverage/research/172-GOVERNOR-RESEARCH.md (the ArbiterOS governance-ISA anchor + the four-class sharpening, navigator-approved 2026-06-23).
    - docs/MINDRIAN-CANON.md Part 11 R1 (the current unit-of-coverage: "a surface = one command file, one skill SKILL.md, one agent file") + Appendix D entry 25 + the header/footer Version line + the Version history table in docs/CANON-PHASE-MAP.md.
    - scripts/build-connector-registry.cjs coverageReport() (add a `class` field per surface; do NOT change the wired/excluded/gap counting contract).
    - the prior frozen-set amendment lockstep pattern (Appendix D entries 21/22/23: canon text + map row + version bump + a canonical FLOOR test, all in one wave so CI never goes RED).
  </read_first>
  <action>
    (a) CANON: add a ONE-LINE sharpening to Canon Part 11 R1 (docs/MINDRIAN-CANON.md): the unit-of-coverage enumerates FOUR governed surface classes - mechanical (non-framework command), framework (pws methodology), intelligence (engine/sensor), pipeline (chain/workflow) - each subject to the same born-wired R1/R2 treatment; the gate is class-aware. Add the matching Appendix D entry (navigator-LOCKED 2026-06-23, via the Part 6 dog-fooding canon-amendment-on-itself mechanism, mirroring entries 21/22/23) + bump the header/footer Version line + add the Version-history row in docs/CANON-PHASE-MAP.md. Also fold in the "capability vs permission" framing for the two wires (a prose clarification, not a new primitive) and record doctor --drift as the SCHEDULED reconciliation surface (the Wiz/HashiCorp two-timeframe pattern; full continuous Brain-sync stays Phase 137). This MINTS no new reach/edge/node/Brain-wire - it names existing surface types and is purely additive/reversible. (b) CODE: extend coverageReport() so each ledger surface carries a `class` enum (mechanical|framework|intelligence|pipeline|utility-excluded); the wired/excluded/gap counts are UNCHANGED. (c) FLOOR TEST: write tests/test-cirs-four-class-floor.cjs asserting the four class enum members are present, every surface in the ledger carries exactly one class, and the wired/excluded/gap counting contract is byte-stable; register it in tests/run-all-172.sh. NO em-dashes. This is the navigator gate for the canon change - confirm the one-line R1 amendment text with the navigator before the canon bytes land.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs && node tests/test-cirs-four-class-floor.cjs && grep -q "mechanical" docs/MINDRIAN-CANON.md && grep -q "governance" docs/CANON-PHASE-MAP.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Canon Part 11 R1 carries the one-line four-class enumeration; a matching Appendix D entry + version bump + CANON-PHASE-MAP version-history row land in the SAME commit (lockstep, CI never RED).
    - coverageReport() surfaces carry a `class` enum; the wired/excluded/gap counts are unchanged from pre-amendment.
    - tests/test-cirs-four-class-floor.cjs asserts the four classes + one-class-per-surface + the byte-stable counting contract; registered in run-all-172.sh.
    - No new reach/edge/node/Brain-wire; no em-dashes.
  </acceptance_criteria>
  <done>The governor recognizes the four surface classes; the Part 11 R1 sharpening is canon, class-aware, and floor-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate --check -> Brain | The gates regenerate in memory from LOCAL sources; zero Brain/network at --check (Part 8). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-29 | Tampering | a new dark surface lands | mitigate | The hard-fail gate at pre-commit + release + doctor blocks a surface that is neither wired nor excluded (R2 born-wired). Task 3 proves it adversarially. |
| T-172-30 | Denial of service | CI goes RED mid-sweep | mitigate | The flip is guarded: it lands ONLY after Waves 1-3 wired/excluded the baseline (Task 1 confirms zero un-decided gaps first); navigator-gated. |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No package installs; generator + hook + release + doctor edits. |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check exit 0; build-orchestration-projection --check exit 0.
- Both gates wired into pre-commit + install-pre-commit + release.sh + doctor --acceptance.
- node tests/test-coverage-gate-hardfail.cjs exit 0.
- NAVIGATOR GATE: the warn->fail flip reviewed before merge (autonomous:false).
</verification>

<success_criteria>
The coverage gate is hard-FAIL on any accidental-dark surface, wired into all four enforcement surfaces, green on the wired/excluded baseline, and proven adversarially - the structural cure (R2/R9/INV-10/INV-14).
</success_criteria>

<artifacts_this_phase_produces>
- MODIFIED: the gap WARN -> hard-FAIL flip in scripts/build-connector-registry.cjs + scripts/build-orchestration-projection.cjs
- MODIFIED: gate wiring in scripts/hooks/pre-commit, scripts/install-pre-commit.sh, scripts/release.sh, scripts/doctor.cjs
- NEW FILE: tests/test-coverage-gate-hardfail.cjs
- AMENDED: docs/MINDRIAN-CANON.md (Part 11 R1 four-class unit-of-coverage + Appendix D entry + version bump) + docs/CANON-PHASE-MAP.md (version-history row)
- MODIFIED: scripts/build-connector-registry.cjs coverageReport() carries a per-surface `class` enum (counts unchanged)
- NEW FILE: tests/test-cirs-four-class-floor.cjs (the four-class FLOOR test)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/172-contextual-invocation-coverage/172-13-SUMMARY.md` when done
</output>
