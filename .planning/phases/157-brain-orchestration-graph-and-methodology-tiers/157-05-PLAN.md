---
phase: 157-brain-orchestration-graph-and-methodology-tiers
slug: brain-orchestration-graph-and-methodology-tiers
plan: 05
type: execute
wave: 5
depends_on: ["157-04"]
files_modified:
  - docs/ORCHESTRATION-PROJECTION-CONTRACT.md
  - tests/test-orchestration-projection-part8-boundary.cjs
  - lib/memory/orchestration-projection.test.cjs
  - lib/memory/run-feynman-tests.cjs
  - scripts/build-orchestration-projection.cjs
autonomous: true
requirements: [BOG-09, BOG-10]
canon_parts: [Part 8, Part 9]
user_setup: []

must_haves:
  truths:
    - "The cache-contract schema is documented: the projection artifact IS the Brain-derived LOCAL cache the nav engine will read (the deferred consumer's target), with a documented schema and Tier-0 resilience framing (mirrors the BRAIN.md derivation pattern)"
    - "A Part 8 boundary scan over the projection artifact + the generator finds ZERO user-content fields (BOG-10)"
    - "methodology_tier is present on every node as the legibility marker (the scan asserts it)"
    - "The boundary scan is an adversarial forbidden-field sweep (mirroring the Phase 90 5-tripwire / Phase 110 adversarial-seed idiom): node/edge fields are restricted to a documented generic-machinery allowlist; any field outside it fails"
    - "No code in Phase 157 makes a live Brain query or write at runtime (the scan + a grep gate prove it across the generator + check)"
  artifacts:
    - path: "docs/ORCHESTRATION-PROJECTION-CONTRACT.md"
      provides: "The cache-contract schema section (deferred nav-engine consumer target) + Part 8 boundary statement"
      contains: "cache contract"
    - path: "tests/test-orchestration-projection-part8-boundary.cjs"
      provides: "Adversarial Part 8 boundary scan: zero user-content fields; tier on every node; zero live Brain"
      contains: "methodology_tier"
  key_links:
    - from: "tests/test-orchestration-projection-part8-boundary.cjs"
      to: "data/brain-orchestration-projection.json + scripts/build-orchestration-projection.cjs"
      via: "field allowlist sweep over the artifact + forbidden-pattern grep over the generator"
      pattern: "ALLOWLIST"
---

<objective>
Close the phase with the local cache-contract schema doc and the Part 8 boundary scan. Document that the projection artifact IS the Brain-derived LOCAL cache the nav engine will read (the deferred consumer's target; the contract is specified even though the consumption is deferred; BOG-09), with a documented schema and Tier-0 resilience framing that mirrors the BRAIN.md derivation pattern. Then prove Part 8 locality (BOG-10): an adversarial boundary scan over the projection artifact + the generator finds ZERO user-content fields, methodology_tier is present on every node as the legibility marker, and no code makes a live Brain query or write at runtime.

Purpose: the projection must be a committed local file with a documented schema and zero runtime Brain dependency (BOG-09, Part 9: Brain as external cortex, local cache is the consumable). And the projection must carry ONLY generic machinery metadata, never user content (BOG-10, Part 8). This plan specifies the cache contract so the deferred nav-engine consumer has a target, and ships the boundary scan that makes the locality constitutional rather than assumed, mirroring the 5-tripwire forbidden-substring sweep from Phase 90 and the adversarial-seed idiom from Phase 110.

Output: docs/ORCHESTRATION-PROJECTION-CONTRACT.md gains the cache-contract schema section; tests/test-orchestration-projection-part8-boundary.cjs is the adversarial boundary scan; the generator exports field allowlists so the scan is deterministic; the boundary test is wired into the Feynman runner.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/157-brain-orchestration-graph-and-methodology-tiers/157-SPEC.md
@.planning/phases/157-brain-orchestration-graph-and-methodology-tiers/157-CONTEXT.md
@scripts/build-orchestration-projection.cjs
@data/brain-orchestration-projection.json
@docs/ORCHESTRATION-PROJECTION-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the local cache-contract schema (the deferred nav-engine consumer target)</name>
  <files>docs/ORCHESTRATION-PROJECTION-CONTRACT.md, scripts/build-orchestration-projection.cjs</files>
  <action>
Add a "## Cache contract (the deferred nav-engine consumer target)" section to docs/ORCHESTRATION-PROJECTION-CONTRACT.md (BOG-09). It must state: (1) data/brain-orchestration-projection.json IS the Brain-derived LOCAL cache the nav engine will read at decide() time; the actual nav-engine read is DEFERRED (157-CONTEXT deferred list), but the contract is specified here so the deferred consumer has a stable target; (2) the resilience framing mirrors the BRAIN.md derivation pattern - the cache is regenerated deterministically from local sources (the registries plus the walk plus analogues), so it survives a Brain outage (Tier-0 resilient): no live Brain dependency at read time; (3) the exact consumable shape: top-level { ontology_ref, generated_note, nodes[], edges[] }, the node field allowlist (id, kind, methodology_tier, name, reach_id, sub_mode, hierarchy_rank, posture, sensor_triggers, and the chain-provenance field names from Plan 03), and the edge field allowlist (type, from, to, plus any documented edge property); (4) a forward-reference to Phase 137 (continuous remote sync) plus the deferred live Brain write as the fast-follows, explicitly OUT of Phase 157. To make the scan deterministic, export the node-field allowlist plus edge-field allowlist from scripts/build-orchestration-projection.cjs as frozen arrays NODE_FIELD_ALLOWLIST and EDGE_FIELD_ALLOWLIST so the boundary test (Task 2) asserts against the same source of truth the doc documents. No em-dashes.
  </action>
  <verify>
    <automated>grep -c "cache contract\|Cache contract" docs/ORCHESTRATION-PROJECTION-CONTRACT.md && grep -c "Phase 137\|deferred" docs/ORCHESTRATION-PROJECTION-CONTRACT.md && node -e "const g=require('./scripts/build-orchestration-projection.cjs'); if(!Array.isArray(g.NODE_FIELD_ALLOWLIST)||!Array.isArray(g.EDGE_FIELD_ALLOWLIST)) throw new Error('allowlists not exported'); console.log('allowlists exported');"</automated>
  </verify>
  <done>The contract doc has the cache-contract section framing the projection as the Brain-derived LOCAL cache (deferred consumer target, Tier-0 resilient, Phase 137 fast-follow out of scope); the generator exports NODE_FIELD_ALLOWLIST and EDGE_FIELD_ALLOWLIST frozen arrays matching the documented schema; no em-dashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The adversarial Part 8 boundary scan (zero user-content; tier on every node; zero live Brain)</name>
  <files>tests/test-orchestration-projection-part8-boundary.cjs, lib/memory/orchestration-projection.test.cjs, lib/memory/run-feynman-tests.cjs</files>
  <behavior>
    - The scan loads data/brain-orchestration-projection.json and asserts every node field key is in NODE_FIELD_ALLOWLIST and every edge field key is in EDGE_FIELD_ALLOWLIST; any field outside the allowlist FAILS (proving zero user-content fields, BOG-10).
    - The scan asserts methodology_tier is present on EVERY node and is exactly pws or mindrian-operation (the legibility marker, BOG-10).
    - The scan asserts no node or edge value matches a forbidden user-content heuristic (a room/ path, an at-sign email, or free-text longer than a documented short cap) - mirroring the Phase 90 forbidden-substring sweep and the Phase 110 adversarial seed.
    - The scan greps scripts/build-orchestration-projection.cjs (comment lines filtered out) for any top-level require of brain-client and for any fetch/http/curl/brain.query/brain.write call, asserting the count is zero (this generator has NO Brain touch at all, unlike the connector --refresh-names sibling), proving zero live Brain read/write at runtime (BOG-09).
  </behavior>
  <action>
Create tests/test-orchestration-projection-part8-boundary.cjs, the adversarial Part 8 boundary scan (mirror the Phase 90 5-tripwire forbidden-substring sweep, the Phase 110-05 adversarial-seed pattern, and the Phase 109-10 fs-instrument allow-list idiom). It must: (1) load data/brain-orchestration-projection.json and require scripts/build-orchestration-projection.cjs; (2) field-allowlist sweep - assert every node's keys are a subset of NODE_FIELD_ALLOWLIST and every edge's keys are a subset of EDGE_FIELD_ALLOWLIST, failing on any extra field (BOG-10 zero user-content fields); (3) tier sweep - assert methodology_tier present and valued pws or mindrian-operation on every node (BOG-10 legibility marker); (4) forbidden-value heuristic sweep - assert no field value contains a room/ path, an at-sign email, or free-text longer than a documented short cap (machinery names, enums, and small ints only); (5) zero-live-Brain sweep - read the generator source, strip comment lines (filter lines beginning with whitespace then / or *), and assert there is no top-level brain-client require and no fetch/http/curl/brain.query/brain.write call; match on the actual call/require syntax, never a bare substring, so header prose naming "Brain" does not self-invalidate the gate. Add the same assertions as a group to lib/memory/orchestration-projection.test.cjs for the bash suite, and register tests/test-orchestration-projection-part8-boundary.cjs in lib/memory/run-feynman-tests.cjs TEST_FILES so the Feynman runner enforces it. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-orchestration-projection-part8-boundary.cjs && node lib/memory/orchestration-projection.test.cjs && grep -c "orchestration-projection-part8-boundary" lib/memory/run-feynman-tests.cjs</automated>
  </verify>
  <done>The boundary scan passes: every node/edge field is in the allowlist (zero user-content fields), methodology_tier is on every node, no forbidden value heuristic matches, and the generator has zero live Brain require/call; the scan is registered in the Feynman runner; no em-dashes.</done>
</task>

</tasks>

<verification>
- The projection is a committed local file with a documented cache-contract schema and zero runtime Brain dependency (BOG-09).
- The Part 8 boundary scan over the projection + generator returns zero user-content fields (BOG-10).
- methodology_tier is present on every node as the legibility marker (BOG-10).
- No live Brain query/write at runtime anywhere in the generator or --check (BOG-09, Part 8).
- The boundary scan is registered in the Feynman runner.
- No em-dashes across touched files.
</verification>

<success_criteria>
- The projection is the documented Brain-derived LOCAL cache; the deferred nav-engine consumer has a target; Phase 137 + live write stay out of scope (BOG-09).
- Part 8 boundary scan returns zero user-content fields; tier on every node (BOG-10).
- Zero live Brain read/write at runtime, proven by the scan + grep gate (BOG-09).
</success_criteria>

<output>
Create `.planning/phases/157-brain-orchestration-graph-and-methodology-tiers/157-05-SUMMARY.md` when done.
</output>
