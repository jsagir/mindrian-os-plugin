---
phase: 157-brain-orchestration-graph-and-methodology-tiers
slug: brain-orchestration-graph-and-methodology-tiers
plan: 04
type: execute
wave: 4
depends_on: ["157-03"]
files_modified:
  - data/orchestration-unwired-allowlist.json
  - commands/diagnose.md
  - data/connector-registry.json
  - scripts/build-orchestration-projection.cjs
  - scripts/hooks/pre-commit
  - lib/memory/run-feynman-tests.cjs
  - lib/memory/orchestration-projection.test.cjs
  - tests/fixtures/orchestration-unwired/UNWIRED-FIXTURE.md
  - docs/ORCHESTRATION-PROJECTION-CONTRACT.md
autonomous: true
requirements: [BOG-06, BOG-08, BOG-11]
canon_parts: [Part 6, Part 7, Part 8]
user_setup: []

must_haves:
  truths:
    - "scripts/build-orchestration-projection.cjs --check distinguishes THREE named failure modes, each with its own message + non-zero exit: STALE, UN-WIRED, UN-RANKED (D-04)"
    - "STALE fires when the repo's commands/skills/agents/frameworks diverge from the committed projection (byte-compare)"
    - "UN-WIRED is FRAMEWORK-GRAINED (plan-check resolved, matches BOG-06 'every framework is reachable'): it fires when a FRAMEWORK is missing from the projection OR is not reachable to one of the 6 frozen reaches via any OPERATES->reach chain, UNLESS that framework is in data/orchestration-unwired-allowlist.json with a documented reason (wired-XOR-allowlisted, the Phase 144.1 idiom). A framework reachable via a sibling command's connector counts as wired (so a command lacking its OWN connector does not fire UN-WIRED as long as its framework reaches a reach elsewhere)"
    - "UN-RANKED fires when a mindrian-operation node that declares a connector lacks its ranking inputs (reach_id/hierarchy_rank/posture)"
    - "Skills are EXEMPT from the UN-WIRED wiring-completeness gate (D-01: name-only nodes, behavior-as-context, not reach-dispatched)"
    - "A deliberately un-wired fixture framework makes --check exit non-zero with a clear UN-WIRED message; the live repo passes (exit 0)"
    - "The --check is registered in BOTH the pre-commit hook AND the Feynman runner, mirroring the connector --check idiom (BOG-11)"
    - "The hats reach is handled correctly (plan-check fix: /mos:think-hats DOES carry SENS-05, so the prose 'no sensor' is WRONG): hats remains a first-class pre-scored reach node, exempt from the sensor-FIRING leg of UN-WIRED; whether SENS-05 firing actually mints the hats reach at runtime is a navigator-gated open item (RESEARCH Q6), NOT 'has no sensor'. Never silently dropped"
  artifacts:
    - path: "scripts/build-orchestration-projection.cjs"
      provides: "--check branch with the 3-mode STALE/UN-WIRED/UN-RANKED taxonomy"
      contains: "UN-WIRED"
    - path: "tests/fixtures/orchestration-unwired/UNWIRED-FIXTURE.md"
      provides: "A deliberately un-wired framework-declaring surface proving --check fails RED"
      contains: "framework"
    - path: "lib/memory/orchestration-projection.test.cjs"
      provides: "Tests proving --check exits 0 clean and non-zero on each of the 3 failure modes via the fixture"
      contains: "UN-WIRED"
  key_links:
    - from: "scripts/hooks/pre-commit"
      to: "scripts/build-orchestration-projection.cjs --check"
      via: "staged-path guard mirroring the connector-registry guard (lines 162-166)"
      pattern: "build-orchestration-projection.cjs --check"
    - from: "lib/memory/run-feynman-tests.cjs TEST_FILES"
      to: "lib/memory/orchestration-projection.test.cjs"
      via: "registration in the TEST_FILES array (mirrors connector-registry.test.cjs)"
      pattern: "orchestration-projection.test.cjs"
---

<objective>
Build the --check drift tripwire: the wiring-pipeline gate that fails on any un-projected, un-wired, or un-ranked surface (BOG-08), with the THREE named failure modes STALE / UN-WIRED / UN-RANKED (D-04), each with its own message + non-zero exit. UN-WIRED is the generalization of the /mos:futures gap (BOG-06): a framework or framework-declaring command that is missing from the projection or not wired to one of the 6 frozen reaches fails by construction, not by remembering. Register the --check in BOTH gate surfaces (pre-commit hook + Feynman runner) so the pipeline is enforced on every commit (BOG-11), mirroring the connector --check idiom exactly. Ship a deliberately un-wired fixture proving --check fails RED, and resolve the hats-orphan open item in the wiring matrix.

Purpose: build-connector-registry.cjs --check catches connector-registry staleness only; nothing checks orchestration-projection drift. The motivating pain is that /mos:futures shipped un-wired because the connector-registry was not regenerated; --check caught it only on a manual run. This plan makes that failure automatic and by-construction at author time. Per Part 6 the harness gains the leg that would have caught the futures gap (mirroring the ACPT-06 dogfood-leg precedent). Per Part 8 the check never touches the Brain.

Output: scripts/build-orchestration-projection.cjs gains the --check branch (3-mode taxonomy); scripts/hooks/pre-commit gains a staged-path guard; lib/memory/run-feynman-tests.cjs registers the projection test; a deliberately un-wired fixture + tests proving each failure mode; the contract doc records the failure taxonomy + the hats-orphan resolution + the sensor-firability assumption note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/157-brain-orchestration-graph-and-methodology-tiers/157-SPEC.md
@.planning/phases/157-brain-orchestration-graph-and-methodology-tiers/157-CONTEXT.md
@.planning/phases/157-brain-orchestration-graph-and-methodology-tiers/157-RESEARCH.md
@scripts/build-orchestration-projection.cjs
@scripts/build-connector-registry.cjs
@scripts/hooks/pre-commit
</context>

<tasks>

<task type="auto">
  <name>Task 0 (PRE-FLIGHT, plan-check-added): resolve the live framework-grained un-wired set so clean-repo --check can exit 0</name>
  <files>data/orchestration-unwired-allowlist.json, commands/diagnose.md, data/connector-registry.json</files>
  <behavior>
    - The live-repo framework-grained un-wired audit is QUANTIFIED (plan-check, against data/connector-registry.json): exactly 2 true-orphan frameworks reach NO frozen reach via any connector -- "Problem Definition Transformation Framework" (only /mos:diagnose, which has no connector) and "MECE" (only /mos:structure-argument, whose connector framework is "The Pyramid Principle", so MECE never reaches a reach).
    - Each of the 2 is RESOLVED as wire-XOR-allowlist-with-reason BEFORE the --check lands, so `node scripts/build-orchestration-projection.cjs --check` exits 0 on the clean repo by construction (not by improvisation inside Task 1).
  </behavior>
  <action>
Create data/orchestration-unwired-allowlist.json (the wired-XOR-allowlisted ledger, mirroring the Phase 144.1 out-of-spine allow-list idiom): a JSON array of { framework, reason } entries naming frameworks intentionally NOT reach-wired, each with a documented reason. Resolve the 2 audited orphans:
  (a) "Problem Definition Transformation Framework" / /mos:diagnose: PREFER WIRING -- add a connector: frontmatter block to commands/diagnose.md (mirror a sibling diagnostic command's connector, e.g. the reach_id that /mos:diagnose's job fits, likely context_block; declare sensor_triggers only if a real sensor applies, else leave the connector reach-only) and regenerate data/connector-registry.json via `node scripts/build-connector-registry.cjs`. If wiring /mos:diagnose proves unsafe (frozen-adjacent surprise), FALL BACK to allowlisting it with the reason "diagnostic command, reach-wiring deferred to a dedicated connector-retrofit phase".
  (b) "MECE": ALLOWLIST with reason "MECE is operated as a COMPONENT under The Pyramid Principle (its /mos:structure-argument connector framework); it reaches a reach via the Pyramid Principle node, not as a standalone reach target" -- unless a clean wire is trivially available.
The allowlist file is READ by Task 1's validateProjection UN-WIRED check (a framework in the allowlist is wired-XOR-allowlisted and does NOT fire UN-WIRED). Do NOT touch frozen-148 constants. If commands/diagnose.md is edited, re-run the connector --check to confirm the registry is consistent. No em-dashes.
  </action>
  <verify>
    <automated>test -f data/orchestration-unwired-allowlist.json && node -e "const a=require('./data/orchestration-unwired-allowlist.json'); if(!Array.isArray(a)||a.some(e=>!e.framework||!e.reason)) process.exit(1); console.log('allowlist ok',a.length)" && node scripts/build-connector-registry.cjs --check</automated>
  </verify>
  <done>The 2 audited framework-grained orphans are each resolved (wired or allowlisted-with-reason); data/orchestration-unwired-allowlist.json exists and is well-formed; the connector registry stays consistent; so Task 1's --check can reach exit 0 on the clean repo without an uncosted mid-execution retrofit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add the --check branch with the STALE / UN-WIRED / UN-RANKED 3-mode taxonomy</name>
  <files>scripts/build-orchestration-projection.cjs, lib/memory/orchestration-projection.test.cjs</files>
  <behavior>
    - `node scripts/build-orchestration-projection.cjs --check` on the clean live repo exits 0 and prints an OK line.
    - STALE: when the in-memory regenerated projection differs byte-for-byte from the committed data/brain-orchestration-projection.json, --check exits non-zero with a message containing "STALE" + the recovery command (node scripts/build-orchestration-projection.cjs).
    - UN-WIRED: when a framework OR a framework-declaring command is absent from the projection, OR is not wired to one of the 6 frozen reaches (REACH_IDS), --check exits non-zero with a message containing "UN-WIRED" naming the offending surface. Skills are EXEMPT (D-01): a name-only skill node with no connector never triggers UN-WIRED.
    - UN-RANKED: when a connector-derived mindrian-operation node lacks reach_id, hierarchy_rank, or posture, --check exits non-zero with a message containing "UN-RANKED" naming the node.
    - Each failure mode is independently testable via validateProjection(projection) returning categorized error arrays (mirroring validateConnectors), exercised directly by the test without spawning a subprocess.
  </behavior>
  <action>
Extend scripts/build-orchestration-projection.cjs main() with a --check branch mirroring build-connector-registry.cjs (the byte-compare + categorized-validation + exit-1 + recovery-line idiom). Implement validateProjection(projection) returning three categorized error arrays per D-04: STALE (byte-compare the serialized regeneration vs the committed file), UN-WIRED (BOG-06, FRAMEWORK-GRAINED per plan-check: a FRAMEWORK missing from nodes[] OR not reachable to one of the 6 frozen REACH_IDS via any OPERATES->reach chain, UNLESS the framework is listed in data/orchestration-unwired-allowlist.json with a reason -- wired-XOR-allowlisted, Phase 144.1 idiom; a framework reachable via a SIBLING command's connector counts as wired, so a command lacking its own connector does NOT fire UN-WIRED as long as its framework reaches a reach elsewhere; EXEMPT skills per D-01 - the gate applies to frameworks, NOT every command surface and NOT skills), and UN-RANKED (BOG-07/D-04: a connector-derived mindrian-operation node missing reach_id/hierarchy_rank/posture). Each mode prints its own named message (the literal tokens STALE, UN-WIRED, UN-RANKED) + the offending surface + the one-line recovery command, and exits non-zero. The clean live repo passes exit 0 because Task 0 already resolved the audited 2-orphan set (wired or allowlisted); validateProjection reads data/orchestration-unwired-allowlist.json so allowlisted frameworks are not flagged. Reuse the REACH_IDS frozen-6 require from lib/core/sensors/sensor-types.cjs. Make ZERO Brain/network calls (Part 8). Add the validateProjection assertions to lib/memory/orchestration-projection.test.cjs. No em-dashes.
  </action>
  <verify>
    <automated>node scripts/build-orchestration-projection.cjs --check && node lib/memory/orchestration-projection.test.cjs && grep -c "STALE\|UN-WIRED\|UN-RANKED" scripts/build-orchestration-projection.cjs</automated>
  </verify>
  <done>--check exits 0 on the clean repo; validateProjection returns categorized STALE/UN-WIRED/UN-RANKED errors; skills are exempt from UN-WIRED; each mode prints its named token + recovery command + non-zero exit; tests pass; no em-dashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship the deliberately un-wired fixture proving --check fails RED</name>
  <files>tests/fixtures/orchestration-unwired/UNWIRED-FIXTURE.md, lib/memory/orchestration-projection.test.cjs</files>
  <behavior>
    - The test builds a projection from a source set that includes a deliberately un-wired surface (declares a framework but no reach connection) and asserts validateProjection flags it UN-WIRED.
    - The fixture is isolated: it lives under tests/fixtures/orchestration-unwired/ and is NOT picked up by the live generator walk (so the live repo --check stays green); the test injects it via a buildProjection variant that accepts an override source list, OR by parsing the fixture and calling validateProjection on a synthesized projection.
    - A clean (fully-wired) synthesized projection passes validateProjection with zero UN-WIRED errors, proving the gate is not a false-positive generator.
  </behavior>
  <action>
Create tests/fixtures/orchestration-unwired/UNWIRED-FIXTURE.md: a deliberately un-wired surface with a frameworks: block (declaring a framework) but NO connector: reach wiring - the exact shape that let /mos:futures ship un-wired (BOG-06 acceptance: "a deliberately un-wired fixture framework makes --check exit non-zero with a clear un-wired message"). In lib/memory/orchestration-projection.test.cjs, add a test that synthesizes a projection containing the un-wired surface (either by pointing a buildProjection override at the fixture dir, or by hand-constructing the minimal projection object and calling validateProjection) and asserts the UN-WIRED error array is non-empty and names the fixture surface. Add the complementary positive test: a fully-wired synthesized projection yields zero UN-WIRED errors. Keep the fixture out of the live generator's source walk so `node scripts/build-orchestration-projection.cjs --check` on the real repo stays exit 0. No em-dashes.
  </action>
  <verify>
    <automated>test -f tests/fixtures/orchestration-unwired/UNWIRED-FIXTURE.md && node lib/memory/orchestration-projection.test.cjs && node scripts/build-orchestration-projection.cjs --check</automated>
  </verify>
  <done>The un-wired fixture exists; the test proves validateProjection flags it UN-WIRED (fails RED) while a fully-wired projection passes; the live repo --check stays green; no em-dashes.</done>
</task>

<task type="auto">
  <name>Task 3: Register --check in pre-commit + Feynman runner; document hats-orphan + sensor-firability</name>
  <files>scripts/hooks/pre-commit, lib/memory/run-feynman-tests.cjs, docs/ORCHESTRATION-PROJECTION-CONTRACT.md</files>
  <action>
Register the orchestration --check in BOTH gate surfaces (BOG-11), mirroring the connector --check idiom exactly. (1) In scripts/hooks/pre-commit, add a guard block modeled on the connector-registry guard (lines 162-166): when any of commands/*.md, skills/*/SKILL.md, agents/*.md, data/connector-registry.json, data/command-registry.json, data/cross-domain-analogues.json, or data/brain-orchestration-projection.json is staged, run `node scripts/build-orchestration-projection.cjs --check` and exit non-zero on failure with the recovery line. (2) In lib/memory/run-feynman-tests.cjs, register lib/memory/orchestration-projection.test.cjs in the TEST_FILES array (mirroring the command-registry.test.cjs / connector test registration at line ~1257), so the Feynman runner exercises the projection --check on every run. (3) In docs/ORCHESTRATION-PROJECTION-CONTRACT.md, document the 3-mode failure taxonomy (STALE/UN-WIRED/UN-RANKED) and resolve the two carried open items: (a) the HATS resolution (plan-check correction: do NOT write "hats has no sensor" - it is FALSE; /mos:think-hats carries sensor_triggers [SENS-05] and reach_id hats in the connector registry): record hats in the wiring-completeness matrix as a first-class PRE-SCORED reach node that is exempt from the sensor-FIRING leg of UN-WIRED; the genuinely open item (RESEARCH Q6) is whether SENS-05 FIRING actually mints the hats reach at runtime vs dispatching a different reach - that is a navigator-gated decision (mint a dedicated hats-minting sensor vs formally de-scope hats to pre-scored-only), NOT an engineer call. hats is NEVER silently dropped from the matrix; (b) the SENSOR-FIRABILITY assumption note - the wiring-completeness gate assumes sensors SENS-02..05/07 fire on a fresh-room turn, but that firability is an EMPIRICAL validation (a live trace), not a code read; document explicitly that UN-WIRED checks structural reach-wiring (a command/framework reaches one of the 6 reaches) and does NOT assert that the firing sensor actually mints a reach at runtime - that empirical leg is out of this build-time gate's scope. No em-dashes.
  </action>
  <verify>
    <automated>grep -c "build-orchestration-projection.cjs --check" scripts/hooks/pre-commit && grep -c "orchestration-projection.test.cjs" lib/memory/run-feynman-tests.cjs && grep -c "hats\|HATS\|pre-scored" docs/ORCHESTRATION-PROJECTION-CONTRACT.md && grep -c "sensor.firability\|empirical\|SENS-02\|firing sensor" docs/ORCHESTRATION-PROJECTION-CONTRACT.md && ! grep -P "\x{2014}" scripts/hooks/pre-commit docs/ORCHESTRATION-PROJECTION-CONTRACT.md</automated>
  </verify>
  <done>The --check is invoked in the pre-commit staged-path guard AND registered in the Feynman runner TEST_FILES; the contract doc documents the 3-mode taxonomy, resolves the hats-orphan as a documented pre-scored-only reach node (navigator-gated, not silently dropped), and records the sensor-firability empirical-validation caveat; no em-dashes.</done>
</task>

</tasks>

<verification>
- --check distinguishes STALE / UN-WIRED / UN-RANKED, each named, each non-zero exit (D-04, BOG-08).
- UN-WIRED generalizes the /mos:futures gap: a framework/command missing or unreached-to-a-frozen-reach fails; skills exempt (BOG-06, D-01).
- The deliberately un-wired fixture fails RED; the live repo passes exit 0 (BOG-06 acceptance).
- --check registered in pre-commit + Feynman runner (BOG-11).
- hats-orphan documented as a pre-scored-only reach node (navigator-gated, not dropped); sensor-firability caveat recorded.
- No em-dashes across touched files; zero Brain/network in --check (Part 8).
</verification>

<success_criteria>
- The --check wiring-pipeline gate fails on un-projected/un-wired/un-ranked surfaces and passes clean (BOG-08, D-04).
- Framework->reach wiring completeness enforced; the futures-gap class is dead-by-construction (BOG-06).
- Generator + check wired into pre-commit + Feynman runner (BOG-11).
- The hats-orphan and sensor-firability open items are handled, not dropped.
</success_criteria>

<output>
Create `.planning/phases/157-brain-orchestration-graph-and-methodology-tiers/157-04-SUMMARY.md` when done.
</output>
