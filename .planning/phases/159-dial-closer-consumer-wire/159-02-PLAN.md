---
phase: 159-dial-closer-consumer-wire
slug: dial-closer-consumer-wire
plan: 02
type: execute
wave: 2
depends_on: ["159-01"]
files_modified:
  - scripts/intent-classifier.cjs
  - tests/test-159-turn-start-wiring.cjs
autonomous: false
requirements: [DCW-01, DCW-04, DCW-05, DCW-06, DCW-08]
canon_parts: [4, 8, 9]
user_setup: []

must_haves:
  truths:
    - "At turn start, before the new turn's dial render, the consumer reads the PRIOR turn's persisted f1_closer_payload from the decision-trace file and routes the navigator's pick through consumeF1Pick over a navigation-chokepoint-owned room.db handle."
    - "The wiring is best-effort (try/catch); a missing/old payload, a missing room.db, or an unmatched pick degrades to a no-op and never disrupts the user prompt."
    - "Every non-dial turn is byte-unchanged from today: the consumer block emits nothing and writes nothing when there is no prior f1_closer_payload."
    - "Wire-2 precondition holds: the grounding-to-frozen-reach_id mapping already on the producer side means the consumer keys on a frozen reach_id or writes unkeyed, never mis-keyed (DCW-08)."
    - "The room.db handle the consumer uses is opened and closed through navigation.openRoomDbForCaller / closeRoomDbForCaller in a finally (no leak, Part 9)."
  artifacts:
    - path: "scripts/intent-classifier.cjs"
      provides: "turn-start consumer attachment reading the prior trace and routing the pick"
      contains: "consumeF1Pick"
    - path: "tests/test-159-turn-start-wiring.cjs"
      provides: "DCW-01/04 turn-start read + byte-unchanged-on-non-dial proof"
  key_links:
    - from: "scripts/intent-classifier.cjs"
      to: "lib/workflow/f1-pick-consumer.cjs::consumeF1Pick"
      via: "lazy-require + best-effort call at turn start"
      pattern: "consumeF1Pick"
    - from: "scripts/intent-classifier.cjs"
      to: "the prior decision-trace file"
      via: "read last trace entry's f1_closer_payload"
      pattern: "f1_closer_payload"
    - from: "scripts/intent-classifier.cjs"
      to: "lib/core/navigation.cjs::openRoomDbForCaller"
      via: "caller-owned handle for the consumer write"
      pattern: "openRoomDbForCaller"
---

<objective>
Attach the Wave-1 consumer at TURN START in scripts/intent-classifier.cjs (HOW-4), reading the prior turn's persisted f1_closer_payload and routing the navigator's pick through consumeF1Pick before the new turn's dial render. This is the single read seam that makes the dial loop record in production: the producer already persists the payload (intent-classifier.cjs:1890); this wave reads it back next turn.

Purpose: f1_closer_payload is dead bytes today (written, never read). This wave reads it. It is the one production-code wiring change in the phase and the only plan touching the shared intent-classifier hot path, hence autonomous:false (a human-verify checkpoint confirms the non-dial byte-unchanged invariant before the integration gate).
Output: the turn-start consumer attachment + a falsifiable turn-start wiring suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-dial-closer-consumer-wire/159-SPEC.md
@.planning/phases/159-dial-closer-consumer-wire/159-CONTEXT.md
@.planning/phases/159-dial-closer-consumer-wire/159-01-SUMMARY.md
@docs/MINDRIAN-CANON.md
@scripts/intent-classifier.cjs
@lib/core/navigation.cjs
@lib/workflow/f1-pick-consumer.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Attach the turn-start consumer in intent-classifier (HOW-4, DCW-01, DCW-04, DCW-06)</name>
  <files>scripts/intent-classifier.cjs, tests/test-159-turn-start-wiring.cjs</files>
  <behavior>
    - Test 1 (turn-start read records): given a prior decision-trace file whose last entry carries an f1_closer_payload with reachIds {SkillX:'deep_research'} and a current turn whose pick resolves to SkillX/reject, the turn-start consumer writes exactly one keyed f_selector_decision reject row to room.db. Drive the wiring through the exported turn-start function (added in this task) over a temp room dir, not by spawning the whole hook.
    - Test 2 (non-dial byte-unchanged): a prior trace with NO f1_closer_payload (or no prior trace) produces zero rows and no emitted block; the consumer is a clean no-op (DCW-04, byte-unchanged invariant).
    - Test 3 (missing room.db no-op): no room.db present -> the consumer no-ops, raises nothing (DCW-04); the handle is never leaked.
    - Test 4 (Part 9 handle): the consumer opens/closes room.db ONLY via navigation.openRoomDbForCaller / closeRoomDbForCaller; a source-level assertion confirms no direct better-sqlite3 / node:sqlite open in the new block (DCW-06).
  </behavior>
  <action>In scripts/intent-classifier.cjs, add a best-effort turn-start consumer that fires BEFORE the new turn's dial render (HOW-4: at the READ side of the turn, on the same engine arm that later persists the next payload near the producer block 1806-1842). Extract the new logic into a small exported helper (e.g. consumePriorF1Pick(roomDir, sessionId, currentTurnText) added to module.exports alongside renderEngineDecisionWithDial) so the test can drive it without spawning the whole hook. The helper: (1) reads the prior turn's persisted trace via the SAME decision-trace file path convention the producer uses (path.join(roomDir,'.mindrian','decision-traces',sessionId+'.json'); read the LAST entry's f1_closer_payload -- mirror appendTraceTurnNumber's read at :1631-1639); (2) when no f1_closer_payload exists, return a no-op immediately (the byte-unchanged guarantee for non-dial turns, DCW-04); (3) lazy-require lib/hmi/f1-pick-capture-cli.cjs and lib/workflow/f1-pick-consumer.cjs in a try/catch (a load failure degrades to no-op, mirroring the producer's :1806-1855 lazy-require idiom); (4) capture the pick from the current turn text via captureCliPick, open a caller-owned room.db via navigation.openRoomDbForCaller(roomDir), call consumeF1Pick({priorPayload, pick, roomState:{db, roomDir}}), and ALWAYS close the handle via navigation.closeRoomDbForCaller in a finally (Part 9, no leak). Wrap the WHOLE helper body in try/catch so a fault never disrupts the user prompt. Call the helper from the engine arm at turn start (guarded by STDIN_MESSAGE non-empty, like the producer). Do NOT change the producer block, the reach_presented emit, or the 158 penalty fold. Do NOT place fenced code in this action. The grounding-to-reach_id map is already on the producer side (intent-classifier.cjs:1823-1834); the consumer reads the persisted reachIds verbatim, so Wire-2 (DCW-08 keyed-or-unkeyed-never-mis-keyed) holds by construction. CJS, no em-dashes.</action>
  <verify>
    <automated>node tests/test-159-turn-start-wiring.cjs</automated>
  </verify>
  <done>The exported turn-start helper reads the prior f1_closer_payload, records the keyed reject row, no-ops cleanly on non-dial / missing-db turns without disrupting the prompt, and opens/closes room.db only via the navigation chokepoint; the suite exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The turn-start consumer is wired into the shared intent-classifier hot path (HOW-4). It reads the prior turn's f1_closer_payload and routes the pick through consumeF1Pick over a navigation-chokepoint-owned room.db handle, best-effort.</what-built>
  <how-to-verify>
    1. Run `node tests/test-159-turn-start-wiring.cjs` -- all cases PASS (turn-start read records a keyed row; non-dial turn writes zero rows and emits nothing).
    2. Run the existing intent-classifier regression to confirm non-dial turns are byte-unchanged: pick any shipped intent-classifier / engine-arm suite already in the repo (e.g. a run-all that exercises scripts/intent-classifier.cjs) and confirm it stays green.
    3. Eyeball the diff to scripts/intent-classifier.cjs: confirm the new block is best-effort (wrapped in try/catch), fires only when STDIN_MESSAGE is non-empty, closes the room.db handle in a finally, and does NOT modify the producer block (1806-1842), the reach_presented emit (1503-1558), or the 158 penalty fold (1559-1599).
  </how-to-verify>
  <resume-signal>Type "approved" once the non-dial byte-unchanged invariant and the no-leak finally are confirmed, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| current turn text -> consumer | Untrusted navigator turn text crosses into the consumer; pick text must stay LOCAL (Part 8). |
| consumer -> room.db (hot path) | The caller-owned handle must be opened/closed via navigation.cjs only (Part 9). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-05 | Denial of service | a fault in the turn-start consumer disrupts the user prompt | mitigate | whole helper body in try/catch; non-dial turns return an immediate no-op; mirrors the producer's fire-and-forget idiom (DCW-04). |
| T-159-06 | Information disclosure | leaked room.db handle on the hot path | mitigate | openRoomDbForCaller paired with closeRoomDbForCaller in a finally; Task 1 Test 4 asserts the chokepoint-only open (Part 9, DCW-06). |
| T-159-07 | Tampering | non-dial turns drift from byte-unchanged | mitigate | early no-op return when f1_closer_payload absent; human-verify checkpoint confirms the regression stays green (DCW-04). |
| T-159-08 | Tampering | mis-keyed reach_id reaches the write | mitigate | consumer reads the producer-side persisted reachIds verbatim; the frozen enum-gate is the final guard (DCW-08). |
| T-159-SC | Tampering | npm/pip/cargo installs | mitigate | zero new dependencies; no install task. |
</threat_model>

<verification>
- node tests/test-159-turn-start-wiring.cjs exits 0 (DCW-01, DCW-04, DCW-06)
- a shipped intent-classifier regression stays green (non-dial byte-unchanged, DCW-04)
- diff review confirms best-effort try/catch + finally-close + producer block untouched
- human-verify checkpoint approved
</verification>

<success_criteria>
The dial pick is read back at turn start and recorded in production through the navigation chokepoint; non-dial turns are byte-unchanged; the room.db handle is never leaked; the producer block, reach_presented emit, and 158 penalty fold are untouched.
</success_criteria>

<output>
Create `.planning/phases/159-dial-closer-consumer-wire/159-02-SUMMARY.md` when done.
</output>
