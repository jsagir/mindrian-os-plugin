---
phase: 159-dial-closer-consumer-wire
slug: dial-closer-consumer-wire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/workflow/offer-closer.cjs
  - lib/workflow/f1-pick-consumer.cjs
  - lib/hmi/f1-pick-capture-cli.cjs
  - tests/test-159-closeoffer-reachid-forward.cjs
  - tests/test-159-consume-f1-pick.cjs
  - tests/test-159-cli-capture-adapter.cjs
autonomous: true
requirements: [DCW-01, DCW-02, DCW-03, DCW-04, DCW-05, DCW-06, DCW-07, DCW-08]
canon_parts: [4, 7, 8, 9]
user_setup: []

must_haves:
  truths:
    - "closeOffer forwards a frozen reach_id into recordSelectorDecision; an off-set reach_id is dropped by the existing enum-gate (unkeyed row); no reach_id yields the byte-identical pre-159 write shape."
    - "consumeF1Pick reads a prior-turn f1_closer_payload, matches the navigator's pick to a verb deterministically, resolves {verb, outcome, reach_id}, and calls closeOffer over a caller-owned roomState.db."
    - "consumeF1Pick is a graceful no-op (writes nothing, raises nothing) when there is no prior payload, the pick matches no verb, or roomState.db is absent."
    - "All four dial outcomes route correctly: accept / defer / reject -> recordSelectorDecision; Free-Text -> recordSelectorMiss; reject is the path Phase 158 consumes."
    - "The CLI capture adapter turns an AskUserQuestion answer into the {pick} shape consumeF1Pick accepts; the navigator's raw pick text never enters a stored row value or a Brain packet (Part 8 LOCAL lane)."
    - "consumeF1Pick reaches room.db only through navigation.cjs / the closer modules; no direct better-sqlite3 / node:sqlite / fs read of room data (Part 9)."
  artifacts:
    - path: "lib/workflow/offer-closer.cjs"
      provides: "closeOffer extended to carry reach_id into decisionArgs (enum-gated downstream)"
      contains: "reach_id"
    - path: "lib/workflow/f1-pick-consumer.cjs"
      provides: "consumeF1Pick shared-core consumer: validate -> resolve {verb,outcome,reach_id} -> closeOffer"
      exports: ["consumeF1Pick"]
      min_lines: 60
    - path: "lib/hmi/f1-pick-capture-cli.cjs"
      provides: "CLI capture adapter: AskUserQuestion answer -> {pick} for consumeF1Pick"
      exports: ["captureCliPick"]
    - path: "tests/test-159-closeoffer-reachid-forward.cjs"
      provides: "DCW-02/DCW-08 keyed/unkeyed/never-mis-keyed forward proof"
    - path: "tests/test-159-consume-f1-pick.cjs"
      provides: "DCW-01/03/04 four-outcome + no-op proof"
    - path: "tests/test-159-cli-capture-adapter.cjs"
      provides: "DCW-07 CLI adapter + DCW-05 pick-text-stays-LOCAL proof"
  key_links:
    - from: "lib/workflow/f1-pick-consumer.cjs"
      to: "lib/workflow/offer-closer.cjs::closeOffer"
      via: "direct require + call with {pick, offer, reach_id, roomState}"
      pattern: "closeOffer\\("
    - from: "lib/workflow/offer-closer.cjs::closeOffer"
      to: "lib/workflow/selector-decisions.cjs::recordSelectorDecision"
      via: "decisionArgs.reach_id forwarded"
      pattern: "reach_id"
---

<objective>
Build the shared-core turn-N+1 consumer and close the reach_id forward gap, so the dial pick can be recorded keyed by reach_id. This is the foundation wave: it produces consumeF1Pick (the pure-ish shared core per HOW-3), extends closeOffer to carry reach_id into decisionArgs (HOW-1), and ships the CLI capture adapter (HOW-3 CLI-now). No turn-start wiring yet (that is Wave 2) so this wave stays fully autonomous and testable in isolation.

Purpose: today f1_closer_payload is persisted but never read back, and closeOffer omits reach_id (offer-closer.cjs:329-336). This wave wires both halves of the write path so Wave 2 only has to attach the read at turn-start.
Output: closeOffer reach_id forward + consumeF1Pick core + CLI capture adapter + three falsifiable suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-dial-closer-consumer-wire/159-SPEC.md
@.planning/phases/159-dial-closer-consumer-wire/159-CONTEXT.md
@docs/MINDRIAN-CANON.md
@lib/workflow/offer-closer.cjs
@lib/workflow/dial-close-reach.cjs
@lib/workflow/selector-decisions.cjs
@lib/memory/offer-closer.test.cjs
@tests/test-158-reach-id-keying.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend closeOffer to carry reach_id into decisionArgs (HOW-1, DCW-02, DCW-08)</name>
  <files>lib/workflow/offer-closer.cjs, tests/test-159-closeoffer-reachid-forward.cjs</files>
  <behavior>
    - Test 1 (keyed): closeOffer({pick:'reject', offer, reach_id:'deep_research', roomState:{db}}) writes an f_selector_decision row whose properties.reach_id === 'deep_research' (a frozen REACH_IDS member). Read back via navigation.findRecentChanges({eventType:'f_selector_decision'}).
    - Test 2 (off-set dropped): closeOffer({..., reach_id:'not_a_reach'}) writes a row with NO reach_id property (the existing recordSelectorDecision enum-gate drops it; closeOffer just forwards). Never mis-keyed (DCW-08).
    - Test 3 (absent): closeOffer with NO reach_id arg writes a row byte-identical to today's no-reach write (no reach_id property). Pre-159 byte-stability.
    - Test 4 (sibling untouched): dial-close-reach.cjs::closeReach is NOT modified; assert its module exports/signature are unchanged (HOW-1 sibling-not-re-routed).
  </behavior>
  <action>In closeOffer (offer-closer.cjs around the decisionArgs block at :329-336), forward an OPTIONAL reach_id onto decisionArgs additively, mirroring the FIX-05 optional-sentence merge idiom directly above it (lines 337-344) and the closeReach reach_id forward idiom (dial-close-reach.cjs:244-252). Read o.reach_id (a string); when present and non-empty, set decisionArgs.reach_id = o.reach_id BEFORE the recordSelectorDecision call. Do NOT add any membership check in closeOffer itself: the frozen REACH_IDS enum-gate already lives in recordSelectorDecision (selector-decisions.cjs:224), which drops off-set values, so an off-set reach_id degrades to an unkeyed write (DCW-08 never-mis-keyed). The verbs array and all other decisionArgs fields stay byte-unchanged when reach_id is absent (DCW-02 byte-stable). Do NOT touch dial-close-reach.cjs (HOW-1: closeReach is a sibling, not re-routed this phase). Do NOT place fenced code in this action; the FIX-05 block at offer-closer.cjs:337-344 is the exact pattern to clone for the reach_id key. Update the closeOffer JSDoc header to name the new optional reach_id arg.</action>
  <verify>
    <automated>node tests/test-159-closeoffer-reachid-forward.cjs</automated>
  </verify>
  <done>closeOffer forwards reach_id when present, drops off-set values via the downstream enum-gate, and is byte-identical to pre-159 when reach_id is absent; closeReach is unchanged; the suite exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build consumeF1Pick shared-core consumer (HOW-2, HOW-3, DCW-01, DCW-03, DCW-04, DCW-06)</name>
  <files>lib/workflow/f1-pick-consumer.cjs, tests/test-159-consume-f1-pick.cjs</files>
  <behavior>
    - Test 1 (reject keyed): a prior f1_closer_payload with verbs ['SkillX','Free-Text'] and reachIds {SkillX:'deep_research'}, fed a reject pick for SkillX over a real roomState.db, writes exactly ONE f_selector_decision row with properties.reach_id === 'deep_research' AND properties.decision === 'reject' (MEDIUM-2 load-bearing: explicitly assert decision is NOT coerced to 'accept'; _normalizePick at offer-closer.cjs:226-239 falls through any non-keyword string to 'accept', so this assertion is the single highest-leverage correctness guard in Wave 1 -- without it the reject row 158 consumes silently lands as accept and the whole phase is vacuous).
    - Test 2 (accept / defer / Free-Text): each of the other three outcomes routes correctly (accept/defer -> recordSelectorDecision via closeOffer; Free-Text -> recordSelectorMiss); assert the correct outcome row/event for each (DCW-03).
    - Test 3 (no prior payload no-op): consumeF1Pick({priorPayload:null, ...}) returns a structured no-op result and writes ZERO rows; no throw (DCW-04).
    - Test 4 (unmatched pick no-op): a pick that matches no verb in payload.verbs returns no-op, zero rows, no throw (DCW-04).
    - Test 5 (absent db no-op): roomState.db absent -> no-op result surfaced from closeOffer's invalid_db guard, zero rows, no throw (DCW-04).
    - Test 6 (Part 9): a source-level assertion that f1-pick-consumer.cjs contains no direct better-sqlite3 / node:sqlite require and no fs.read of room data (DCW-06).
  </behavior>
  <action>Create lib/workflow/f1-pick-consumer.cjs exporting consumeF1Pick({ priorPayload, pick, roomState }) -> { ok, recorded?, reason?, outcome?, reach_id? }. Per HOW-2 (deterministic verb-match, NO fuzzy NLP): validate priorPayload is an object with a non-empty verbs array; resolve the matched verb by deterministic membership of the pick against payload.verbs (the AskUserQuestion answer is a known enum from the rendered card) OR by the decision-keyword normalization closeOffer already applies (accept/defer/reject/Free-Text). When no verb/keyword matches, return {ok:false, reason:'unmatched'} writing nothing. MEDIUM-2 (the two-channel split -- make this EXPLICIT, do NOT conflate): resolve TWO independent values from the pick. (a) OUTCOME CHANNEL: the decision keyword accept|defer|reject|Free-Text, which is what gets passed as closeOffer({pick:<outcome-keyword>}) -- pass the OUTCOME keyword string, NEVER the matched verb (passing the verb makes _normalizePick fall through to 'accept' and the reject is lost). (b) REACH CHANNEL: the matched verb, used ONLY to look up payload.reachIds[verb] (forward as reach_id, a string) and to populate offer.command. Build the offer object closeOffer expects ({command:<matched verb>, framework?, confidence?}) from the matched verb, but the {pick} field MUST be the outcome keyword. Delegate ALL persistence to closeOffer (Part 7 thin glue; HOW-1 ride offer-closer) over the caller-supplied roomState.db -- consumeF1Pick NEVER opens room.db itself (Part 9: the closer + the Wave-2 caller own the handle via navigation.openRoomDbForCaller). Forward payload.sentence through to closeOffer({sentence}) when present (HOW-5 FIX-05 LOCAL lane) so the navigator's pick text is classified to a scalar at the write seam and never stored. Lazy-require offer-closer with a try/catch so a load failure degrades to {ok:false, reason:'closer_unavailable'} (mirror the producer's best-effort idiom). CJS, no em-dashes, no new dependency.</action>
  <verify>
    <automated>node tests/test-159-consume-f1-pick.cjs</automated>
  </verify>
  <done>consumeF1Pick records exactly one keyed reject row for a reach-grounded reject pick, routes all four outcomes correctly, no-ops on cold/unmatched/absent-db turns without throwing, and contains no direct db/fs handle; the suite exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CLI capture adapter + Part 8 pick-text-stays-LOCAL proof (HOW-3, DCW-05, DCW-07)</name>
  <files>lib/hmi/f1-pick-capture-cli.cjs, tests/test-159-cli-capture-adapter.cjs</files>
  <behavior>
    - Test 1 (adapter shape): captureCliPick(askUserQuestionAnswer, priorPayload) returns the {pick} shape consumeF1Pick accepts; the pick is the selected option/verb (deterministic enum from the rendered F.1 card), not free prose.
    - Test 2 (end-to-end CLI path): feeding a captured CLI pick through consumeF1Pick over a real roomState.db writes the expected keyed reject row (the CLI live path exercised here, completed by the Wave-3 integration test).
    - Test 3 (Part 8 SECRETREASON tripwire): seed a SECRETREASON159 marker into the navigator's raw pick text / sentence; after the write, assert that marker appears in ZERO stored row property values (mirrors the Phase 158 SECRETREASON123 idiom). The pick text rides the LOCAL lane only.
    - Test 4 (seam doc presence): assert the module exports the documented capture-adapter seam signature for Desktop/Cowork (a contract stub / JSDoc the Wave-3 seam doc references).
  </behavior>
  <action>Create lib/hmi/f1-pick-capture-cli.cjs exporting captureCliPick(answer, priorPayload) -> { pick, sentence? }. The CLI F.1 renders via the AskUserQuestion Shape F.1 trailer; the navigator's pick arrives as the next user turn's selected option. Map that option/answer to the matching verb in priorPayload.verbs deterministically (HOW-2), returning {pick} as the matched verb/keyword. Carry the raw answer text ONLY as the optional sentence field destined for the FIX-05 LOCAL lane (HOW-5) -- never as a row body. Document (JSDoc + an exported CAPTURE_ADAPTER_CONTRACT constant naming the function signature and expected shape) the seam Desktop/Cowork adapters implement, so Wave-3 can reference it for the deferred-capture seam doc (DCW-07). The Part 8 proof lives in the test: seed SECRETREASON159 into the pick text, run the full capture -> consumeF1Pick -> closeOffer path, and grep every stored row property value for the marker (must be absent). CJS, no em-dashes.</action>
  <verify>
    <automated>node tests/test-159-cli-capture-adapter.cjs</automated>
  </verify>
  <done>The CLI adapter produces the consumeF1Pick {pick} shape, the end-to-end CLI path writes a keyed row, a seeded SECRETREASON159 never lands in any stored value, and the Desktop/Cowork seam contract is exported; the suite exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator pick text -> stored row / Brain packet | Untrusted free text from the navigator's turn crosses here; must classify to scalar/enum and stay LOCAL (Part 8). |
| consumer -> room.db | All reads/writes must cross via navigation.cjs / the closers (Part 9). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-01 | Information disclosure | pick text -> f_selector_decision row | mitigate | sentence rides FIX-05 LOCAL lane; closeOffer stores only scalar bool + source enum; SECRETREASON159 tripwire in Task 3 (DCW-05). |
| T-159-02 | Tampering | off-set / mis-keyed reach_id on the write | mitigate | closeOffer forwards reach_id verbatim; the frozen REACH_IDS enum-gate in recordSelectorDecision drops off-set values (DCW-08 never mis-keyed). |
| T-159-03 | Information disclosure | consumer opens room.db directly | mitigate | consumeF1Pick never opens room.db; delegates to closeOffer over a caller-owned handle; Part 9 source assertion in Task 2 Test 6 (DCW-06). |
| T-159-04 | Denial of service | malformed prior payload crashes the turn | mitigate | consumeF1Pick validates priorPayload and returns a structured no-op on any malformed/cold input (DCW-04). |
| T-159-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies (Phase 87 invariant); no install task in this plan. |
</threat_model>

<verification>
- node tests/test-159-closeoffer-reachid-forward.cjs exits 0 (DCW-02, DCW-08)
- node tests/test-159-consume-f1-pick.cjs exits 0 (DCW-01, DCW-03, DCW-04, DCW-06)
- node tests/test-159-cli-capture-adapter.cjs exits 0 (DCW-05, DCW-07)
- grep confirms dial-close-reach.cjs unchanged (HOW-1 sibling untouched)
- grep confirms no direct better-sqlite3 / node:sqlite / fs.read in f1-pick-consumer.cjs (Part 9)
</verification>

<success_criteria>
closeOffer carries reach_id with keyed/unkeyed/never-mis-keyed behavior proven; consumeF1Pick records the keyed reject row and no-ops cleanly on cold/unmatched/absent-db turns; the CLI capture adapter is wired and tested with the pick text proven to stay LOCAL; closeReach and all frozen-148 constants are untouched.
</success_criteria>

<output>
Create `.planning/phases/159-dial-closer-consumer-wire/159-01-SUMMARY.md` when done.
</output>
