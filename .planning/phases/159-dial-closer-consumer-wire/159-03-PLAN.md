---
phase: 159-dial-closer-consumer-wire
slug: dial-closer-consumer-wire
plan: 03
type: execute
wave: 3
depends_on: ["159-02"]
files_modified:
  - tests/test-159-integration-2turn-suppress.cjs
  - tests/run-all-159.sh
  - docs/F1-PICK-CAPTURE-ADAPTER-SEAM.md
  - tests/test-159-part8-secretreason-sweep.cjs
autonomous: true
requirements: [DCW-05, DCW-06, DCW-07, DCW-09, DCW-10]
canon_parts: [4, 6, 7, 8, 9]
user_setup: []

must_haves:
  truths:
    - "A deterministic 2-turn integration test drives turn N (persist f1_closer_payload with a reach_id) then turn N+1 (feed a reject pick) against a real room.db and lands exactly one keyed f_selector_decision reject row, repeated to 3 real rejects of ONE reach."
    - "After 3 real recorded rejects, that reach is ABSENT from buildReachList's rendered output -- suppression fires from REAL recorded rejections (computeReachPenalties), not injected ones (DCW-09)."
    - "The integration test goes RED if the consumer (consumeF1Pick / the turn-start wiring) is removed -- proving it is load-bearing, not incidental."
    - "tests/run-all-159.sh is a one-command phase gate that exits 0, includes the 159 suites + the Part 8 SECRETREASON sweep + the Part 9 chokepoint sweep, and carries run-all-158.sh (14/14) + run-all-148.sh (18/18) passthroughs."
    - "No frozen-148 constant, the 0.40/0.30/0.30 weights, or any closer outcome semantic is edited anywhere in the phase (DCW-10)."
    - "A documented Desktop/Cowork capture-adapter seam (function signature + expected shape) exists and the live-capture deferral is recorded (DCW-07)."
  artifacts:
    - path: "tests/test-159-integration-2turn-suppress.cjs"
      provides: "DCW-09 producer->consumer->penalty live-suppression proof; RED on consumer removal"
      min_lines: 80
    - path: "tests/run-all-159.sh"
      provides: "one-command phase gate with 158 + 148 passthroughs + Part 8/9 sweeps (DCW-10)"
      contains: "run-all-158.sh"
    - path: "docs/F1-PICK-CAPTURE-ADAPTER-SEAM.md"
      provides: "Desktop/Cowork capture-adapter seam contract + deferral record (DCW-07)"
    - path: "tests/test-159-part8-secretreason-sweep.cjs"
      provides: "DCW-05 behavioral SECRETREASON sweep over the new consumer path"
  key_links:
    - from: "tests/test-159-integration-2turn-suppress.cjs"
      to: "lib/workflow/reach-reject-reader.cjs::computeReachPenalties + lib/hmi/dial-reach-orchestrator.cjs::buildReachList"
      via: "assert reach absent from rendered output after 3 real rejects"
      pattern: "buildReachList|computeReachPenalties"
    - from: "tests/run-all-159.sh"
      to: "tests/run-all-158.sh + tests/run-all-148.sh"
      via: "carried passthroughs"
      pattern: "run-all-15[48]\\.sh"
---

<objective>
Prove the whole loop fires LIVE and lock it behind a one-command gate. The headline acceptance gate (DCW-09): a scripted 2-turn integration test drives producer -> consumer -> penalty end to end, asserts 3 REAL rejects of one reach make that reach drop out of the rendered top-K, and goes RED if the consumer is removed. Then assemble tests/run-all-159.sh (the phase gate, with 158 + 148 passthroughs + Part 8/9 sweeps) and write the Desktop/Cowork capture-adapter seam doc with the deferral recorded.

Purpose: Phase 158 is green only via seeded reach_id-keyed rows; no test drives the real loop. This wave proves suppression fires from REAL recorded rejections -- the exact behavior the phase exists to deliver (159-CONTEXT specifics) -- and freezes the spine (DCW-10).
Output: the 2-turn integration suite + run-all-159.sh gate + the SECRETREASON sweep + the seam doc.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-dial-closer-consumer-wire/159-SPEC.md
@.planning/phases/159-dial-closer-consumer-wire/159-CONTEXT.md
@.planning/phases/159-dial-closer-consumer-wire/159-01-SUMMARY.md
@.planning/phases/159-dial-closer-consumer-wire/159-02-SUMMARY.md
@docs/MINDRIAN-CANON.md
@lib/workflow/reach-reject-reader.cjs
@tests/run-all-158.sh
@tests/test-158-reach-hard-suppress.cjs
@lib/hmi/dial-reach-orchestrator.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scripted 2-turn producer->consumer->penalty integration test (DCW-09, DCW-06)</name>
  <files>tests/test-159-integration-2turn-suppress.cjs</files>
  <behavior>
    - Test 1 (one loop): turn N persists an f1_closer_payload carrying reachIds {SkillX:'deep_research'} to the decision-trace file over a temp room dir + real room.db; turn N+1 feeds a reject pick for SkillX through the Wave-2 turn-start helper; assert exactly ONE keyed f_selector_decision reject row with properties.reach_id === 'deep_research' lands (read back via the navigation chokepoint).
    - Test 2 (3 real rejects -> suppressed): repeat the loop so 'deep_research' accrues 3 REAL recorded rejects, AND emit M (MIN_PRESENTATIONS=2) REAL reach_presented rows via navigation.logMemoryEvent(db,'reach_presented',{reach_id:'deep_research'}) so the M-floor is satisfied by real db rows; then run computeReachPenalties over the same room.db and feed its suppressedReachIds into the PURE buildReachList; assert 'deep_research' is ABSENT from the rendered reaches (DCW-09: live suppression from REAL rejections). MEDIUM-1 (load-bearing anti-vacuous guard): the roomState passed to computeReachPenalties MUST NOT carry the injected-counter test seam -- assert it has NO presentationsCount key and NO rejectCountInWindow key, so the suppression is provably READ FROM THE DB, not the seam. Otherwise "reach absent" could pass for the wrong reason and the LIVE proof is vacuous.
    - Test 3 (RED on consumer removal): a guard that proves the test is load-bearing -- when the consumer path is bypassed (no consumeF1Pick / no turn-start record), the 3 rejects never land, computeReachPenalties reads 0, and the reach is NOT suppressed; assert the test would FAIL in that branch (an explicit negative-control assertion, the RED-on-removal proof).
  </behavior>
  <action>Create tests/test-159-integration-2turn-suppress.cjs. For the REAL-ROW fixture mechanism, mirror test-158-reach-presentation-counter.cjs + test-158-reach-reject-only.cjs (both write REAL rows via navigation.logMemoryEvent / recordSelectorDecision) -- NOT test-158-reach-hard-suppress.cjs's roomState injection seam (use that file ONLY as the reference for a parole-safe presentation count: pres meets M=2 and is not on a P=5 boundary). Temp room.db via the shipped opener, seed anchor nodes, read back via navigation.findRecentChanges. Drive the REAL path: persist a real f1_closer_payload to the decision-trace file (the producer's persisted shape), then invoke the Wave-2 exported turn-start helper (consumePriorF1Pick) to record the reject -- do NOT inject f_selector_decision rows directly and do NOT pass presentationsCount / rejectCountInWindow on roomState (that is what 158's unit fixtures do; this test must prove the LIVE wire reading from the db). Satisfy the M-floor with REAL reach_presented emits (logMemoryEvent(db,'reach_presented',{reach_id})). After 3 real rejects, call reach-reject-reader.computeReachPenalties (passing a roomState with NO injected-counter keys) and pass suppressedReachIds into dial-reach-orchestrator.buildReachList; assert the reach is absent; assert the roomState carried no presentationsCount/rejectCountInWindow keys. Add the negative-control (Test 3): a code path that skips the consumer and asserts the reach is NOT suppressed, so removing the consumer flips Test 2 RED. Read/write room.db ONLY through navigation.cjs / the shipped opener (Part 9). Use the frozen REACH_IDS member 'deep_research'; do NOT edit any frozen constant. CJS, no em-dashes, no RNG, no live Brain.</action>
  <verify>
    <automated>node tests/test-159-integration-2turn-suppress.cjs</automated>
  </verify>
  <done>The 2-turn loop lands a keyed reject row from the LIVE consumer; 3 real rejects suppress the reach from buildReachList; the negative-control proves RED-on-consumer-removal; the suite exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Part 8 SECRETREASON behavioral sweep over the new consumer path (DCW-05)</name>
  <files>tests/test-159-part8-secretreason-sweep.cjs</files>
  <action>Create tests/test-159-part8-secretreason-sweep.cjs mirroring the Phase 158 SECRETREASON123 tripwire idiom (and the Phase 90 5-tripwire forbidden-substring sweep). Seed a unique marker (SECRETREASON159) into the navigator's raw pick text / sentence, drive the full capture -> consumeF1Pick -> closeOffer -> recordSelectorDecision path over a real room.db, then read back EVERY stored f_selector_decision row property value (via the navigation chokepoint) and assert the marker appears in ZERO of them -- the pick text rode the FIX-05 LOCAL lane and was classified to a scalar boolean + source enum at the write seam, never stored (HOW-5). Add a source-level assertion that the new consumer files (f1-pick-consumer.cjs + f1-pick-capture-cli.cjs) contain no path that forwards pick text toward buildBrainPacket. CJS, no em-dashes.</action>
  <verify>
    <automated>node tests/test-159-part8-secretreason-sweep.cjs</automated>
  </verify>
  <done>A seeded SECRETREASON159 in the pick never lands in any stored row value and no consumer path forwards pick text toward the Brain packet; the suite exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: run-all-159.sh one-command gate + Desktop/Cowork seam doc (DCW-09, DCW-10, DCW-07)</name>
  <files>tests/run-all-159.sh, docs/F1-PICK-CAPTURE-ADAPTER-SEAM.md</files>
  <action>Create tests/run-all-159.sh mirroring tests/run-all-158.sh structure exactly (set -uo pipefail, strip_comments grep-gate hygiene, per-suite PASS/FAIL line, final tally, exit 1 if anything failed). It runs, in order: (a) every test-159-*.cjs falsifiable suite (the three Wave-1 suites, the Wave-2 turn-start suite, the Wave-3 integration suite, and the Part 8 sweep) -- a missing file gates to a FAIL line, never a crash; (b) Part 8 -- the standalone SECRETREASON sweep is invoked as a suite in (a), and additionally a grep sweep over the new consumer artifacts (lib/workflow/f1-pick-consumer.cjs + lib/hmi/f1-pick-capture-cli.cjs + the new intent-classifier consumer block) for a forbidden pick-text-to-Brain token (comment lines stripped first via strip_comments so header prose never self-invalidates -- the grep-gate-hygiene idiom); (c) Part 9 -- a chokepoint grep sweep asserting f1-pick-consumer.cjs reads/writes only via navigation.cjs / the closers (no node:sqlite / better-sqlite3 / fs.read on executable lines); (d) the CARRIED passthroughs: bash tests/run-all-158.sh (14/14) AND bash tests/run-all-148.sh (18/18) -- so a frozen-148 break OR a 158 regression fails the 159 gate (DCW-10). Note run-all-158.sh already carries run-all-148.sh internally, but the SPEC requires both passthroughs present in run-all-159.sh, so invoke BOTH explicitly. Then create docs/F1-PICK-CAPTURE-ADAPTER-SEAM.md documenting the Tri-Polar capture-adapter seam (HOW-3): the shared-core consumeF1Pick({priorPayload,pick,roomState}) contract, the captureCliPick(answer,priorPayload) CLI adapter (live now), and the Desktop/Cowork adapter contract (the function signature + expected {pick, sentence?} shape they must produce) with the LIVE conversational capture explicitly recorded as DEFERRED to a follow-up (cross-reference the 159-CONTEXT deferred-items). Reference the exported CAPTURE_ADAPTER_CONTRACT constant from Wave 1. bash only, no em-dashes, no emoji.</action>
  <verify>
    <automated>bash tests/run-all-159.sh</automated>
  </verify>
  <done>tests/run-all-159.sh exits 0, runs all 159 suites + Part 8/9 sweeps + the 158 and 148 passthroughs (both present), and the Desktop/Cowork seam doc records the contract and the deferral.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pick text -> stored row / Brain packet | The behavioral SECRETREASON sweep proves the LOCAL lane holds across the real path (Part 8). |
| phase code -> frozen-148 spine | The carried passthroughs prove no frozen constant moved (Part 6 dog-fooding). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-09 | Repudiation | the loop claims to fire live but only seeded rows are tested | mitigate | the 2-turn test drives the REAL consumer (not injected rows) and the negative-control proves RED-on-removal (DCW-09). |
| T-159-10 | Tampering | a frozen-148 constant or the 0.40/0.30/0.30 weights drift | mitigate | run-all-159.sh carries run-all-148.sh (18/18) + run-all-158.sh (14/14) passthroughs; any drift fails the gate (DCW-10). |
| T-159-11 | Information disclosure | pick text leaks through the real consumer path | mitigate | SECRETREASON159 behavioral sweep over the full capture->record path asserts zero stored marker (DCW-05). |
| T-159-12 | Information disclosure | grep-gate self-invalidation by header prose | mitigate | strip_comments before every grep sweep (the shipped grep-gate-hygiene idiom). |
| T-159-SC | Tampering | npm/pip/cargo installs | mitigate | zero new dependencies; no install task. |
</threat_model>

<verification>
- node tests/test-159-integration-2turn-suppress.cjs exits 0 (DCW-09; RED on consumer removal)
- node tests/test-159-part8-secretreason-sweep.cjs exits 0 (DCW-05)
- bash tests/run-all-159.sh exits 0 with run-all-158.sh + run-all-148.sh passthroughs present (DCW-10)
- docs/F1-PICK-CAPTURE-ADAPTER-SEAM.md documents the seam + records the deferral (DCW-07)
</verification>

<success_criteria>
The full producer->consumer->penalty loop is proven LIVE: 3 real rejects suppress the reach from buildReachList, RED on consumer removal; run-all-159.sh is the one-command gate with both 158 and 148 passthroughs green inside it; the pick text stays LOCAL across the real path; the Desktop/Cowork seam is documented with capture deferred; no frozen constant is edited.
</success_criteria>

<output>
Create `.planning/phases/159-dial-closer-consumer-wire/159-03-SUMMARY.md` when done.
</output>
