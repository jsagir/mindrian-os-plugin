---
phase: 177-larry-behavioral-channel
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/shallow-doc-parser.cjs
  - lib/core/graph-self-heal.cjs
  - tests/test-bch-18-persona-write.cjs
autonomous: true
requirements: [BCH-18]
canon_parts: [4, 8, 9]
cirs_relationship:
  surfaces_modified: [shallow-doc-parser, graph-self-heal]
  surfaces_added: []
  surfaces_removed: []
  spine_consumed:
    - lib/core/navigation/room-birth.cjs
  gate_impact: "RE-SCOPED per 177-RESEARCH.md sec 0/3: the role_blend WRITE producer already exists at room-birth.cjs:426-430 (Phase 155). This plan does NOT build a new writeUserMdAtomic producer. The real (upstream) work: compute a single-axis weighted blend { canonical_role: 1.0 } from the dual-path/shallow-doc canonical_role scalar (mirroring persona-override.cjs:280-281 role_blend[key]=1.0) and thread it to birthRoom's opts.roleBlend. ROOM-LOCAL; zero Brain egress of role_blend (Part 8)."
must_haves:
  truths:
    - "a room born from a canonical_role scalar yields role_blend { role: 1.0 } in USER.md"
    - "with no scalar present, the birth degrades to cold-start neutral (no fabricated blend)"
    - "role_blend never egresses to the Brain (Part 8 tripwire holds)"
  artifacts:
    - path: "lib/core/shallow-doc-parser.cjs"
      provides: "a single-axis blend helper that turns a canonical_role scalar into { canonical_role: 1.0 }"
      contains: "role_blend"
    - path: "lib/core/graph-self-heal.cjs"
      provides: "the birthRoom caller threading the computed blend to opts.roleBlend"
      contains: "roleBlend"
  key_links:
    - from: "lib/core/graph-self-heal.cjs"
      to: "lib/core/navigation/room-birth.cjs birthRoom"
      via: "birthRoom({ ..., roleBlend: <computed single-axis blend> })"
      pattern: "roleBlend"
    - from: "the canonical_role scalar"
      to: "the role_blend { role: 1.0 } written to USER.md"
      via: "the existing writeUserMdAtomic producer at room-birth.cjs:426-430 (NOT rebuilt)"
      pattern: "role_blend"
---

<objective>
Close the UPSTREAM persona gap: compute a single-axis weighted blend from the
canonical_role scalar and thread it into the existing room-birth producer.

BCH-18 is RE-SCOPED. The bundle's "GAP 1" (no role_blend write at room birth) is STALE: the
producer already EXISTS at lib/core/navigation/room-birth.cjs:426-430 (Phase 155) -- birthRoom
already calls writeUserMdAtomic({ canonical_role, role_blend: roleBlend, journey_stage }).
The real gap is upstream: nothing computes a WEIGHTED blend; shallow-doc-parser emits only a
canonical_role SCALAR. This plan computes a single-axis blend { canonical_role: 1.0 }
(mirroring persona-override.cjs:280-281 which already does role_blend[key]=1.0) and threads
it to birthRoom's opts.roleBlend. It does NOT build a new writeUserMdAtomic producer.

Purpose: make the ignite persona prior (read by Plan 01's BCH-17 seed) actually PRESENT in
USER.md at room birth, so the deterministic dial-default seed has a real role_blend to read,
while the degrade path (no scalar -> cold-start neutral) stays honest.
Output: a single-axis blend helper, the birth caller threading it to opts.roleBlend, and a
green test-bch-18-persona-write suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-larry-behavioral-channel/177-CONTEXT.md
@.planning/phases/177-larry-behavioral-channel/177-SPEC.md
@.planning/phases/177-larry-behavioral-channel/177-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compute a single-axis blend from the canonical_role scalar (BCH-18 upstream)</name>
  <read_first>
    - lib/core/shallow-doc-parser.cjs:42-50 (parseRoleHints -> { canonical_role, confidence }; 4 of 7 roles) and :118-136 (extractShallow which already emits the canonical_role scalar) and :310+ (module.exports)
    - lib/core/persona-override.cjs:80-93 (ROLE_BLEND_KEYS frozen 7-role set + normalizeRoleKey) and :275-282 (the role_blend[key]=1.0 single-axis idiom this task MIRRORS verbatim)
    - .planning/phases/177-larry-behavioral-channel/177-RESEARCH.md sec 3 (re-scope: set a single-axis blend { <canonical_role>: 1.0 } from the dual-path/shallow-doc scalar, mirroring persona-override.cjs:280-281; the degrade test stays valid: with no blend, fall back to the canonical_role scalar)
  </read_first>
  <action>
    In lib/core/shallow-doc-parser.cjs add and export a pure helper blendFromCanonicalRole(scalar)
    that turns a canonical_role scalar string into a single-axis role_blend object: normalize the
    scalar via normalizeRoleKey (require it from lib/core/persona-override.cjs) and, when it maps to
    one of the 7 ROLE_BLEND_KEYS, return { [key]: 1.0 } -- mirroring persona-override.cjs:280-281
    verbatim. When the scalar is absent/empty/unmappable, return null (NOT an invented blend) so
    the caller can degrade to cold-start neutral; NEVER fabricate a blend. Keep the helper pure
    (no I/O, no Brain call). Do NOT change extractShallow's existing canonical_role scalar emission
    (Part 10 dual-path first-touch stays byte-stable); this helper is additive. The blend object
    carries ONLY the generic role key + the 1.0 weight (enum/scalar; no user content), so it is
    Part-8-clean by construction.
  </action>
  <behavior>
    - blendFromCanonicalRole('student') -> { student: 1.0 }.
    - blendFromCanonicalRole('founder') -> { founder: 1.0 }; blendFromCanonicalRole('investor') -> { investor: 1.0 }.
    - blendFromCanonicalRole('') / undefined / 'not-a-role' -> null (no fabricated blend).
  </behavior>
  <verify>
    <automated>node -e "const s=require('./lib/core/shallow-doc-parser.cjs'); const a=s.blendFromCanonicalRole('student'); if(!a||a.student!==1.0) throw new Error('student blend wrong: '+JSON.stringify(a)); const z=s.blendFromCanonicalRole(''); if(z!==null) throw new Error('empty must degrade to null, got '+JSON.stringify(z)); const f=s.blendFromCanonicalRole('founder'); if(!f||f.founder!==1.0) throw new Error('founder blend wrong'); console.log('OK BCH-18 single-axis blend helper');"</automated>
  </verify>
  <done>blendFromCanonicalRole returns { role: 1.0 } for a mappable scalar and null otherwise (never fabricated); extractShallow's scalar emission is byte-stable; the helper is pure and Part-8-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the computed blend to birthRoom opts.roleBlend, then turn test-bch-18 green (BCH-18)</name>
  <read_first>
    - lib/core/navigation/room-birth.cjs:319-355 (birthRoom signature; opts.canonicalRole + opts.roleBlend reads at :349-350) and :420-433 (the EXISTING producer: writeUserMdAtomic({ canonical_role, role_blend: roleBlend, journey_stage }) -- do NOT rebuild this)
    - lib/core/graph-self-heal.cjs:67 (require birthRoom) and :172-200 (the birthRoom CALLER -- the upstream site where canonical_role is known and opts.roleBlend is threaded)
    - lib/core/shallow-doc-parser.cjs blendFromCanonicalRole (Task 1)
    - tests/test-bch-18-persona-write.cjs (the scaffold STUB to REPLACE)
    - tests/test-bch-14-part8-egress.cjs (the already-green Part 8 tripwire -- the egress assertion shape to mirror for the role_blend no-egress check)
  </read_first>
  <action>
    In lib/core/graph-self-heal.cjs, at the birthRoom caller (:187 area), compute the single-axis
    blend from the canonical_role in hand via shallow-doc-parser.blendFromCanonicalRole(...) and
    thread it to birthRoom's opts.roleBlend. When the helper returns null (no mappable scalar), pass
    NO roleBlend (or {}) so birthRoom's existing default (:350 roleBlend = {}) takes the cold-start
    neutral path -- do NOT invent a blend. Keep the caller's other opts (approvedBy, parent, slug,
    canonicalRole) byte-unchanged. Do NOT modify room-birth.cjs's writeUserMdAtomic producer (it
    already writes role_blend at :426-430). Then REPLACE tests/test-bch-18-persona-write.cjs with
    real asserts: birth a room (in a temp dir) from a canonical_role scalar (e.g. 'student') and
    assert USER.md carries role_blend { student: 1.0 }; birth a room with NO mappable scalar and
    assert the degrade is real -- role_blend is empty/absent (cold-start neutral), NOT a fabricated
    blend; assert (mirroring the test-bch-14 tripwire idiom) that the role_blend never appears in
    any Brain-bound payload -- buildBrainPacket (navigation.cjs:87) does not read role_blend, so the
    blend is structurally LOCAL (Part 8). Exit 0 on pass. Keep the test self-contained (its own temp
    room dir, cleaned up) and offline (no Brain).
  </action>
  <behavior>
    - birthRoom with canonicalRole:'student' (blend threaded) -> USER.md role_blend === { student: 1.0 }.
    - birthRoom with no mappable scalar -> USER.md role_blend empty/absent (cold-start neutral; no fabricated role).
    - role_blend is never present in any buildBrainPacket output (Part 8 tripwire).
  </behavior>
  <verify>
    <automated>node tests/test-bch-18-persona-write.cjs</automated>
    <automated>node tests/test-bch-14-part8-egress.cjs</automated>
  </verify>
  <done>The graph-self-heal birthRoom caller threads the computed single-axis blend to opts.roleBlend (and passes none when the scalar is unmappable); room-birth's producer is untouched; a room born from 'student' carries role_blend { student: 1.0 } in USER.md; the degrade is real; role_blend never egresses; test-bch-18-persona-write is real and exits 0, and test-bch-14-part8-egress stays GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shallow-doc scalar -> role_blend | The canonical_role scalar (LOCAL, dual-path first touch) is widened to a single-axis blend in-process; no new source of truth, no external read. |
| role_blend -> Brain | role_blend is LOCAL room state written only to USER.md via the existing producer; it is never read by buildBrainPacket (Part 8 invariant). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-03-01 | Information disclosure | role_blend egress | mitigate | The blend object carries only the generic role key + 1.0 weight (enum/scalar); the test asserts role_blend never appears in any buildBrainPacket output, mirroring the already-green test-bch-14 tripwire (Part 8). |
| T-177-03-02 | Spoofing | a fabricated blend | mitigate | blendFromCanonicalRole returns null for an absent/unmappable scalar; the caller passes no roleBlend so birthRoom's cold-start default holds; the degrade test asserts no role is invented (Part 9: never fabricate). |
| T-177-03-03 | Tampering | the existing producer | mitigate | room-birth.cjs writeUserMdAtomic is NOT modified; the change is upstream-only (compute + thread), so the Phase 155 producer + its tests stay byte-stable. |
| T-177-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds NO package installs; pure CJS edits to existing files. |
</threat_model>

<verification>
- node tests/test-bch-18-persona-write.cjs exits 0.
- node tests/test-bch-14-part8-egress.cjs stays GREEN (role_blend no-egress invariant holds).
- The single-axis-blend-helper probe confirms { role: 1.0 } for a mappable scalar and null otherwise.
- bash tests/run-all-177.sh shows test-bch-18 flipped GREEN, test-bch-14 still GREEN, and the frozen-set drift fences (6 reaches, 3 postures) still GREEN.
</verification>

<success_criteria>
- blendFromCanonicalRole returns { role: 1.0 } for a mappable scalar, null otherwise (never fabricated).
- The graph-self-heal birthRoom caller threads the blend to opts.roleBlend; room-birth's producer is untouched.
- A room born from 'student' carries role_blend { student: 1.0 } in USER.md; the no-scalar degrade is real (cold-start neutral).
- role_blend never egresses (test-bch-14 stays GREEN); test-bch-18-persona-write GREEN; no reach minted, no posture minted, no Brain wire opened.
</success_criteria>

<output>
Create `.planning/phases/177-larry-behavioral-channel/177-03-SUMMARY.md` when done.
</output>
