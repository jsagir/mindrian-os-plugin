---
phase: 177-larry-behavioral-channel
plan: 09
type: execute
wave: 4
depends_on: [177-08]
canon_parts: [2, 3, 8, 9, 11]
autonomous: true
requirements: [BCH-07, BCH-08]
files_modified:
  - lib/core/navigation-engine.cjs
  - tests/test-bch-07-seam3-insertion.cjs
  - tests/test-bch-08-signal-tier.cjs
must_haves:
  truths:
    - "The SEAM 3 modifier exists inside resolveFireSkill() between the sensor-reach branch (nav-engine :458-466) and the Brain-verb path (:468)."
    - "When BEHAVIORAL_CHANNEL_ARMED is false (always today), the SEAM 3 modifier returns null and the legacy decide() output is BYTE-IDENTICAL to the pre-seam engine -- per D-177-BCH-07."
    - "routing_source stays legacy in the dormant (null) path -- the seam fires nothing semantic when unarmed."
    - "A SIGNAL-tier cue outranks a keyword-tier cue inside the protected band (0.50-0.85) -- per BCH-08, sensor-types.cjs TRIGGER_TIERS :73-77 signal>keyword."
    - "A SIGNAL-tier cue inside the protected band LOSES to a Brain RECOMMENDED confidence >= 0.70 (RECOMMENDED_CONFIDENCE_FLOOR, nav-engine :74) -- per BCH-08, both directions tested."
    - "The SEAM 3 modifier ROUTES only; it adds NO new Brain egress (Canon Part 8) -- a source scan finds no user-data egress on the modifier path."
  artifacts:
    - path: "lib/core/navigation-engine.cjs"
      provides: "behavioralChannelModifier() (the SEAM 3 modifier fn) + its insertion in resolveFireSkill() between :466 and :468, gated by the ARMED flag"
      contains: "behavioralChannelModifier"
    - path: "tests/test-bch-07-seam3-insertion.cjs"
      provides: "BCH-07 GREEN: null defers, legacy byte-identical, routing_source stays legacy"
    - path: "tests/test-bch-08-signal-tier.cjs"
      provides: "BCH-08 GREEN: signal>keyword, signal loses to Brain>=0.70 -- both directions"
  key_links:
    - from: "lib/core/navigation-engine.cjs resolveFireSkill()"
      to: "lib/core/navigation/calibration-gate.cjs BEHAVIORAL_CHANNEL_ARMED"
      via: "require + gate check (modifier returns null when not armed)"
      pattern: "BEHAVIORAL_CHANNEL_ARMED"
    - from: "lib/core/navigation-engine.cjs behavioralChannelModifier()"
      to: "lib/core/sensors/sensor-types.cjs TRIGGER_TIERS"
      via: "tier comparison signal>keyword"
      pattern: "TRIGGER_TIERS|signal|keyword"
---

<objective>
Wave 4 ships the SEAM 3 behavioral-channel modifier WIRED-BUT-SHADOWED inside
the navigation engine. The modifier inserts at the one clean seam in
resolveFireSkill() -- after the sensor reach resolves (nav-engine :458-466) and
before the Brain-verb path (:468) -- reads the fired reach plus the calibrated
cue, and returns {verb, confidence, reason} OR null. null means defer to the
Brain path, exactly as today.

The modifier is GATED by BEHAVIORAL_CHANNEL_ARMED (default LOCKED, owned by
lib/core/navigation/calibration-gate.cjs, shipped in 177-08). When ARMED is
false -- which is ALWAYS, today, because no real calibration PASS has armed it --
the modifier ALWAYS returns null, the legacy Brain path runs unchanged, and the
decide() output is byte-identical to the pre-seam engine. We build the mechanism
dormant. We do NOT fire it.

The LIVE flip (a SIGNAL-tier cue actually outranking the graph) happens only
when a real calibration PASS arms the flag. That arming is data-gated (it needs
ground-truth-labeled shadow data, deferred per 177-08's fit.method:'deferred')
and is OUT of scope for this wave. A Canon Custodian / brain-boundary review is
REQUIRED before any live arming (Canon Part 8 PR gate).

Purpose: realize BCH-07 (the seam exists; null defers byte-identical) and BCH-08
(the protected-band tier ordering: SIGNAL-tier cue outranks keyword but loses to
Brain >= 0.70) as code, turning test-bch-07-seam3-insertion and
test-bch-08-signal-tier GREEN, while keeping the legacy path byte-identical and
routing_source legacy in the dormant path.

Output: behavioralChannelModifier() + its gated insertion in resolveFireSkill();
two RED test stubs replaced with real asserts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/177-larry-behavioral-channel/177-SPEC.md
@.planning/phases/177-larry-behavioral-channel/177-RESEARCH.md
@.planning/phases/177-larry-behavioral-channel/177-08-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `behavioralChannelModifier(...)` -- the SEAM 3 modifier function in
  `lib/core/navigation-engine.cjs`. Reads the fired top reach + the calibrated
  cue (passed in / read from the turn signal that the sensor spine already
  surfaced) + the ARMED flag, and returns `{ verb, confidence, reason }` or
  `null`. Returns null UNCONDITIONALLY when `BEHAVIORAL_CHANNEL_ARMED` is false.
- Its INSERTION point: inside `resolveFireSkill()`, after the sensor-reach branch
  (the `if (Array.isArray(sensorReaches) ...)` block ending at :466) and BEFORE
  the Brain-verb `mode_a` path (the `if (tierMode === 'mode_a' ...)` block at
  :468). A null return falls through to the existing :468 path (byte-identical).
- The ARMED-gate check: a `require('./navigation/calibration-gate.cjs')` read of
  `BEHAVIORAL_CHANNEL_ARMED`, the constitutional kill-switch for this wave.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SEAM 3 modifier + ARMED gate (BCH-07, dormant byte-identical path)</name>
  <read_first>
    - lib/core/navigation-engine.cjs :442-486 (resolveFireSkill -- the seam is
      between the sensor branch ending :466 and the mode_a Brain path at :468),
      :404-414 (reachIdToSkillFamily), :608-660 + :1118-1132 (module.exports),
      :880-918 (where decide() sets decision.fire_skill via resolveFireSkill and
      where the Phase 144 routing_source flip is consequenced).
    - lib/core/navigation/calibration-gate.cjs :48-52 + :276-284
      (BEHAVIORAL_CHANNEL_ARMED default LOCKED; module.exports).
    - lib/core/sensors/sensor-types.cjs :43-50 (frozen 6 REACH_IDS),
      :73-77 (TRIGGER_TIERS signal/context/keyword order), :174-179
      (classifyTriggerTier).
  </read_first>
  <behavior>
    - Test 1 (dormant returns null): with BEHAVIORAL_CHANNEL_ARMED false (the
      shipped default), behavioralChannelModifier(...) returns null for ANY input
      (fired reach present, cue present, any tier, any confidence in band).
    - Test 2 (byte-identical legacy): capture decide(turn, context) output for a
      representative turn (a fired sensor reach + a fresh mode_a Brain quadruple),
      JSON-serialize it; assert it is byte-identical to the output produced when
      the modifier is bypassed entirely (the pre-seam path). Because the modifier
      returns null when unarmed, the live decide() output equals the bypass
      output -- prove decision.fire_skill and trace are unchanged vs pre-seam.
    - Test 3 (routing_source stays legacy in the dormant path): for a turn where
      NO sensor reach fires and NO Brain verb resolves, decide() leaves
      routing_source legacy; the dormant modifier does not flip it.
  </behavior>
  <action>
    Add behavioralChannelModifier(topReach, cue, armed) to navigation-engine.cjs,
    co-located with resolveFireSkill(). Signature: it takes the fired top reach
    object (sensorReaches[0], or null), the calibrated cue (a scalar
    {tier, confidence} drawn from the turn signal the sensor spine already
    surfaced -- NEVER user prose; Part 8), and the armed boolean. FIRST statement:
    if armed is not true, return null. This is the constitutional kill-switch --
    when BEHAVIORAL_CHANNEL_ARMED is false the function is provably inert. The
    armed-true body (which never runs today) maps the fired reach to its canonical
    verb via reachIdToSkillFamily and returns {verb, confidence, reason}; when the
    reach does not map, or the cue is below BEHAVIORAL_CHANNEL_FLOOR, return null
    (defer to Brain). Read BEHAVIORAL_CHANNEL_ARMED via a top-of-file
    require('./navigation/calibration-gate.cjs') (mirror the existing require
    idiom; do NOT re-type the literal false). Insert the modifier CALL inside
    resolveFireSkill() AFTER the sensor-reach branch that ends at :466 and BEFORE
    the mode_a Brain-verb path at :468: compute const seam =
    behavioralChannelModifier(sensorReaches && sensorReaches[0], cue,
    BEHAVIORAL_CHANNEL_ARMED); if seam is non-null return seam.verb; otherwise
    fall through unchanged to the existing :468 path. Because seam is ALWAYS null
    today, the fall-through is byte-identical to the pre-seam engine. Add a
    top-of-function doc comment naming the WIRED-BUT-SHADOWED contract: the seam
    is dormant, the legacy path is byte-identical when unarmed, the live flip is
    data-gated and Canon-Custodian-reviewed. Per BCH-07 (177-SPEC :45). Hyphens
    only, no em-dashes (CLAUDE.md house rule).
  </action>
  <verify>
    <automated>node tests/test-bch-07-seam3-insertion.cjs</automated>
  </verify>
  <done>
    behavioralChannelModifier exists, returns null when BEHAVIORAL_CHANNEL_ARMED
    is false, is inserted between resolveFireSkill :466 and :468; test-bch-07
    exits 0 with the byte-identical legacy assertion and the routing_source-legacy
    dormant-path assertion both green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SIGNAL-tier ordering inside the protected band (BCH-08, both directions)</name>
  <read_first>
    - lib/core/navigation-engine.cjs :70-74 (WICKED + RECOMMENDED_CONFIDENCE_FLOOR
      0.70 -- the Brain floor), :442-486 (resolveFireSkill incl. the new seam from
      Task 1), :807-834 (the Section 6 Brain RECOMMENDED >= 0.70 gate evaluation).
    - lib/workflow/f-selector-ranker.cjs :102-104 (BEHAVIORAL_CHANNEL_FLOOR 0.50,
      BEHAVIORAL_CHANNEL_CEILING 0.85; the protected band is [floor, ceiling]).
    - lib/core/sensors/sensor-types.cjs :73-77 (TRIGGER_TIERS: signal ranks above
      keyword), :174-179 (classifyTriggerTier returns 'signal' / 'keyword').
    - tests/test-bch-04-shadow-log.cjs :22-27 (the ok()/failed counter idiom to
      mirror).
  </read_first>
  <behavior>
    - Test A (signal > keyword, in band): given two competing cues inside the
      protected band (0.50 <= confidence < 0.85) -- one SIGNAL-tier, one
      KEYWORD-tier -- the modifier's ordering picks the SIGNAL-tier cue's reach
      over the keyword-tier cue. (Exercised against the modifier's armed-true
      ordering helper directly, since the gated call returns null at runtime.)
    - Test B (signal loses to Brain >= 0.70, in band): given a SIGNAL-tier cue in
      the protected band AND a Brain RECOMMENDED confidence >= 0.70, the Brain path
      wins -- the protected-band cue CANNOT override Brain >= 0.70
      (RECOMMENDED_CONFIDENCE_FLOOR). Assert the decision favors the Brain verb,
      not the cue.
    - Test C (band boundary sanity): the ceiling 0.85 is >= 0.70 + 0.15 (BCH-06,
      f-selector-ranker), so a cue at the band ceiling still loses to Brain 0.70 --
      assert the 15-point margin holds (read the constants, never re-type them).
  </behavior>
  <action>
    Make the SIGNAL-tier ordering a pure, separately-testable helper so BCH-08 can
    assert tier precedence WITHOUT arming the live flag: factor the armed-true
    ordering of Task 1's modifier into a pure function (e.g.
    rankBehavioralCue(cueA, cueB) or an internal compare) that orders two cues by
    TRIGGER_TIERS precedence first (signal above keyword, read from sensor-types
    TRIGGER_TIERS -- never a re-typed literal order) and confidence second, and a
    pure predicate that says a protected-band cue (BEHAVIORAL_CHANNEL_FLOOR <=
    confidence < BEHAVIORAL_CHANNEL_CEILING) LOSES to a Brain RECOMMENDED
    confidence >= RECOMMENDED_CONFIDENCE_FLOOR (0.70). Export the helper(s) for the
    test. The protected-band-loses-to-Brain rule must read RECOMMENDED_CONFIDENCE_
    FLOOR from nav-engine and BEHAVIORAL_CHANNEL_FLOOR/CEILING from
    f-selector-ranker via the existing require idiom (no threshold literal lands in
    a prompt; BCH-05). Then write test-bch-08-signal-tier.cjs with the ok()/failed
    idiom from test-bch-04: Test A signal>keyword, Test B signal-loses-to-Brain,
    Test C the 15-point margin. Per BCH-08 (177-SPEC :46). Hyphens only, no
    em-dashes.
  </action>
  <verify>
    <automated>node tests/test-bch-08-signal-tier.cjs</automated>
  </verify>
  <done>
    test-bch-08-signal-tier exits 0: signal-tier cue outranks keyword-tier in
    band, signal-tier cue loses to Brain >= 0.70, and the ceiling-vs-Brain-floor
    15-point margin holds -- all reading the engine-owned constants, none re-typed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Part 8 brain-boundary scan + full-phase gate</name>
  <read_first>
    - CLAUDE.md (Canon Part 8: the modifier ROUTES, it adds NO new Brain egress;
      brain_consult deferral uses the EXISTING typed-packet Brain path).
    - tests/run-all-177.sh (the phase gate; expects bch-07 + bch-08 GREEN this
      wave, bch-09 still RED as Wave 5).
    - lib/core/navigation-engine.cjs (the modifier + seam from Tasks 1-2).
  </read_first>
  <action>
    Run a Part-8 brain-boundary source scan over the modifier path: grep the
    behavioralChannelModifier function body and its insertion site in
    navigation-engine.cjs for any user-data egress surface (fetch / http / https /
    buildBrainPacket / sendPacket / a Brain write call) and assert NONE is added
    by this wave -- the seam reads only enum/scalar handles (reach_id, posture,
    tier, confidence) and the ARMED boolean, never user prose, never an artifact
    body, never a meeting transcript (Canon Part 8 LOCAL -> BRAIN: NO). The seam
    introduces no new Brain wire; brain_consult, if ever reached when armed, rides
    the existing typed-packet path. Then run the full phase gate. Confirm: bch-07
    + bch-08 flip GREEN (12 pass / 3 fail -> 14 pass / 1 fail), bch-09 stays RED
    (Wave 5, out of scope), the carried frozen-set fences stay GREEN
    (test-reach-ids-drift frozen 6, test-posture-ids-drift frozen 3 -- this wave
    mints NO 7th reach, NO 4th posture). Hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>grep -nE "fetch|http|buildBrainPacket|sendPacket" lib/core/navigation-engine.cjs | grep -iv "^[0-9]*: *//" | grep -i behavioral; echo "scan-exit:$?"; bash tests/run-all-177.sh</automated>
  </verify>
  <done>
    The brain-boundary scan finds zero new user-data egress on the modifier path;
    bash tests/run-all-177.sh shows bch-07 + bch-08 PASSED, bch-09 still FAILED
    (Wave 5), and both drift fences PASSED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine -> Brain | navigation-engine.cjs may query the Brain via the existing typed-packet path; the SEAM 3 modifier MUST NOT add a new egress here |
| turn signal -> modifier | the calibrated cue arrives as an enum/scalar handle (tier, confidence) the sensor spine surfaced; it must never carry user prose |
| ARMED flag -> live flip | BEHAVIORAL_CHANNEL_ARMED is the constitutional kill-switch; flipping it to true is data-gated AND Canon-Custodian-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-01 | Information Disclosure | behavioralChannelModifier egress | mitigate | Task 3 source scan asserts no fetch/http/buildBrainPacket/sendPacket added on the modifier path; modifier reads enum/scalar only (Part 8 LOCAL->BRAIN: NO) |
| T-177-02 | Elevation of Privilege | premature live arming | mitigate | modifier returns null unconditionally when ARMED false; the live flip requires a real calibration PASS (data-gated, 177-08 fit.method:'deferred') PLUS a Canon Custodian / brain-boundary review before arming |
| T-177-03 | Tampering | legacy path drift | mitigate | BCH-07 byte-identical assertion proves the dormant path equals the pre-seam engine; routing_source stays legacy in the null path |
| T-177-04 | Tampering | threshold literal in prompt | mitigate | floor/ceiling/Brain-floor read via require idiom from f-selector-ranker + nav-engine; no threshold literal re-typed or landed in a prompt (BCH-05) |
| T-177-SC | Tampering | npm/pip/cargo installs | accept | this wave adds NO package installs (pure-CJS engine edit + tests); package legitimacy gate N/A |
</threat_model>

<verification>
- node tests/test-bch-07-seam3-insertion.cjs exits 0 (null defers; legacy
  byte-identical; routing_source legacy in the dormant path).
- node tests/test-bch-08-signal-tier.cjs exits 0 (signal>keyword in band; signal
  loses to Brain>=0.70; 15-point margin holds).
- bash tests/run-all-177.sh: bch-07 + bch-08 PASSED (12 pass / 3 fail ->
  14 pass / 1 fail); bch-09 still FAILED (Wave 5, out of scope); the carried
  frozen-set fences (reach-ids-drift frozen 6, posture-ids-drift frozen 3) PASSED.
- Part 8 source scan over the modifier path finds zero new user-data egress.
</verification>

<success_criteria>
- behavioralChannelModifier() exists in navigation-engine.cjs, inserted between
  resolveFireSkill :466 and :468, gated by BEHAVIORAL_CHANNEL_ARMED (default
  LOCKED), returning null unconditionally when unarmed.
- The legacy decide() output is byte-identical to the pre-seam engine in the
  dormant (null / unarmed) path; routing_source stays legacy there.
- SIGNAL-tier cue outranks keyword-tier cue inside the protected band; the same
  cue loses to a Brain RECOMMENDED confidence >= 0.70; both directions tested.
- No threshold literal lands in a prompt (constants read via require idiom).
- No new Brain egress on the modifier path (Canon Part 8).
- Frozen sets untouched (6 reaches, 3 postures); no 7th reach, no 4th posture.
</success_criteria>

<output>
Create `.planning/phases/177-larry-behavioral-channel/177-09-SUMMARY.md` when done.
</output>
