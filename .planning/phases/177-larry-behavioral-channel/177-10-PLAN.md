---
phase: 177-larry-behavioral-channel
plan: 10
type: execute
wave: 5
depends_on: [177-08]
canon_parts: [2, 3, 8, 9, 11]
autonomous: true
requirements: [BCH-09]
files_modified:
  - lib/core/chain-executor.cjs
  - tests/test-bch-09-forced-material.cjs
must_haves:
  truths:
    - "An autonomous_safe step whose command carries an IRREVERSIBLE_HINT (e.g. 'deploy') STILL HALTS at the gate -- even with the escape-hatch gate-suppression branch ACTIVE and BEHAVIORAL_CHANNEL_ARMED true. The FORCED-MATERIAL guardrail wins UNCONDITIONALLY (per BCH-09, 177-SPEC :47)."
    - "isIrreversibleStep() is still checked FIRST inside the gate predicate (chain-executor.cjs :242-245), BEFORE the escape-hatch suppression branch, the quality carry, and the posture/autonomous_safe check -- no tag, no escape hatch, no armed flag can route around it."
    - "With BEHAVIORAL_CHANNEL_ARMED false (always today), the escape-hatch suppresses NOTHING: makeGateFn() returns the byte-identical verb it returns pre-seam for every step (autonomous_safe -> 'run', non-safe -> 'halt', irreversible -> 'halt'). The existing Decision Gate behavior is unchanged (per the DORMANT contract)."
    - "When armed, the escape-hatch may suppress the gate ('run' instead of 'halt') ONLY for an autonomous_safe + reversible step -- NEVER for an irreversible/forced-material step (which already returned 'halt' at the unconditional first check)."
    - "The gate-suppression branch reads only the ARMED boolean + the step's escape_hatch scalar handle + the resolved posture -- never user prose, never an artifact body (Canon Part 8 LOCAL -> BRAIN: NO; the gate adds no Brain wire)."
    - "The frozen sets are untouched: no 7th reach, no 4th posture, no IRREVERSIBLE_HINT removed; runChain + _runChainResilient halt-reason ('forced_material' vs 'gate_halt') unchanged for the dormant path."
  artifacts:
    - path: "lib/core/chain-executor.cjs"
      provides: "the escape-hatch gate-suppression branch added to makeGateFn(), gated by BEHAVIORAL_CHANNEL_ARMED, placed AFTER the unconditional isIrreversibleStep() first-check and the quality carry, BEFORE the autonomous_safe/posture return"
      contains: "BEHAVIORAL_CHANNEL_ARMED"
    - path: "tests/test-bch-09-forced-material.cjs"
      provides: "BCH-09 GREEN: deploy-still-halts headline (armed + escape-hatch active); dormant byte-identical gate when unarmed; forced-material checked FIRST; autonomous_safe+reversible may suppress only when armed"
  key_links:
    - from: "lib/core/chain-executor.cjs makeGateFn()"
      to: "lib/core/navigation/calibration-gate.cjs BEHAVIORAL_CHANNEL_ARMED"
      via: "lazy require + gate check (suppression branch inert when not armed)"
      pattern: "BEHAVIORAL_CHANNEL_ARMED"
    - from: "lib/core/chain-executor.cjs makeGateFn() gateFn"
      to: "lib/core/chain-executor.cjs isIrreversibleStep()"
      via: "unconditional first-check guards the suppression branch"
      pattern: "isIrreversibleStep"
---

<objective>
Wave 5 (the LAST wave, highest blast radius) ships the escape-hatch
gate-SUPPRESSION consequence WIRED-BUT-SHADOWED inside the gated chain executor.
The escape hatch may suppress the Decision Gate -- letting an autonomous_safe step
run instead of halting for human confirmation -- but ONLY when the LOCKED
BEHAVIORAL_CHANNEL_ARMED flag is true, and NEVER for an irreversible /
FORCED-MATERIAL step.

The control path for the escape hatch already landed in Wave 1 (BCH-16: the
register_override scratch + the escape_hatch observation field). This wave adds
ONLY the gate-suppression CONSEQUENCE, gated AND guarded:

- GATED by BEHAVIORAL_CHANNEL_ARMED (default LOCKED, owned by
  lib/core/navigation/calibration-gate.cjs, shipped in 177-08). When ARMED is
  false -- which is ALWAYS, today, because no real calibration PASS has armed it --
  the suppression branch is provably inert: makeGateFn() returns the
  byte-identical verb it returns pre-seam, for every step. The existing Decision
  Gate fires exactly as it does today. We wire the dangerous consequence dormant;
  we do NOT enable it.
- GUARDED, UNCONDITIONALLY, by the FORCED-MATERIAL rule. isIrreversibleStep()
  (chain-executor.cjs :164-173, IRREVERSIBLE_HINTS = email/deploy/publish/send/
  release/external-write/external_write) is checked FIRST inside gateFn
  (:242-245), BEFORE the quality carry, the escape-hatch suppression, and the
  posture/autonomous_safe check. An irreversible step ALWAYS halts -- armed or
  not, escape-hatch or not. The guardrail is INDEPENDENT of ARMED.

The HEADLINE invariant: an autonomous_safe step whose command carries "deploy"
(or any IRREVERSIBLE_HINT) STILL HALTS, even with the escape-hatch override
active AND the flag armed. The guardrail wins.

The LIVE flip (the escape hatch actually suppressing a real gate) happens only
when a real calibration PASS arms the flag. That arming is data-gated (it needs
ground-truth-labeled shadow data, deferred per 177-08's fit.method:'deferred')
AND Canon-Custodian-gated (Canon Part 8 PR gate). It is OUT of scope for this
wave. This wave does NOT weaken the human-in-the-loop Decision Gate today.

Purpose: realize BCH-09 (the FORCED-MATERIAL guardrail; an autonomous_safe deploy
step still halts) as code, turning test-bch-09-forced-material GREEN and the full
phase gate (tests/run-all-177.sh) all-green (16 pass / 0 fail).

Output: the gate-suppression branch inside makeGateFn(), gated by the ARMED flag
and guarded by the unconditional forced-material first-check; the bch-09 scaffold
stub replaced with real asserts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/177-larry-behavioral-channel/177-SPEC.md
@.planning/phases/177-larry-behavioral-channel/177-RESEARCH.md
@.planning/phases/177-larry-behavioral-channel/177-08-SUMMARY.md
@.planning/phases/177-larry-behavioral-channel/177-09-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- The escape-hatch gate-SUPPRESSION branch inside `makeGateFn()` in
  `lib/core/chain-executor.cjs`. The branch is the consequence: when armed AND
  the step carries an active escape_hatch AND the step is autonomous_safe +
  reversible, the gate returns 'run' (suppressed) instead of falling through to
  its normal autonomous_safe/posture evaluation. It is placed AFTER the
  unconditional `isIrreversibleStep()` first-check (:245) and AFTER the quality
  carry (:249), and BEFORE the final autonomous_safe/posture return (:258).
- The ARMED gate: a lazy `require('./navigation/calibration-gate.cjs')` read of
  `BEHAVIORAL_CHANNEL_ARMED` -- the constitutional kill-switch for this wave.
  When false (always today) the suppression branch never fires; makeGateFn()
  is byte-identical to its pre-seam behavior.
- The forced-material-wins assertion suite: `tests/test-bch-09-forced-material.cjs`
  -- the deploy-still-halts headline (armed + escape-hatch active), the dormant
  byte-identical gate when unarmed, the forced-material-checked-FIRST ordering,
  and the armed autonomous_safe+reversible suppression path.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escape-hatch gate-suppression branch in makeGateFn (BCH-09, ARMED-gated, forced-material-guarded)</name>
  <read_first>
    - lib/core/chain-executor.cjs :147-173 (IRREVERSIBLE_HINTS frozen 7-keyword
      array + isIrreversibleStep -- the forced-material classifier), :229-261
      (makeGateFn + the gateFn predicate: the unconditional isIrreversibleStep
      first-check at :242-245, the quality carry at :249, the posture/
      autonomous_safe resolution :251-256, the final return :258-259), :88-115
      (the lazy-require idiom for seams -- mirror it for calibration-gate),
      :722-728 (module.exports).
    - lib/core/chain-executor.cjs :295-410 (runChain) + :500-600
      (_runChainResilient): both call makeGateFn at :326 / :507 and both set the
      halt reason via `isIrreversibleStep(step) ? 'forced_material' : 'gate_halt'`
      (:398 / :593) -- confirm the suppression branch does NOT change these reason
      strings for the dormant path.
    - lib/core/navigation/calibration-gate.cjs :48-52 (BEHAVIORAL_CHANNEL_ARMED
      default false, the Wave-4/5 kill-switch; "Lowering ... to unblock a wave is
      forbidden"), :276-284 (module.exports includes BEHAVIORAL_CHANNEL_ARMED).
    - lib/core/behavioral/observation-schema.cjs (177-06): the escape_hatch field
      is a pure-observation boolean handle (NEVER a composed dial/reach value) --
      the suppression branch reads only this scalar handle off the step, never
      user prose (Part 8).
    - lib/core/navigation/projections.cjs::computeInvestmentLevel (177-01): the
      investment_jump_escape_hatch field is read-if-present, reserved-for-later --
      this wave is the consumer of the escape_hatch handle at the GATE seam.
  </read_first>
  <behavior>
    - Test HEADLINE (deploy-still-halts, the load-bearing assert): build a gateFn
      for a step that is autonomous_safe (posture push_forward / 'run') AND carries
      an active escape_hatch AND whose command contains "deploy" (an
      IRREVERSIBLE_HINT). Force armed true for this assert by passing the opts.armed
      seam (or calling the suppression helper with armed=true) so the test does NOT
      mutate the shipped constant. Assert the gate returns 'halt'. The
      forced-material guardrail wins even with the escape hatch active AND armed.
      Repeat for one more IRREVERSIBLE_HINT (e.g. a command containing "publish")
      and for step.irreversible === true with a non-hint command -- both still
      'halt'.
    - Test DORMANT (byte-identical when unarmed): with BEHAVIORAL_CHANNEL_ARMED
      false (the shipped default), for the SAME inputs above plus a reversible
      autonomous_safe step carrying an active escape_hatch, the gate returns
      EXACTLY the verb it returns pre-seam: irreversible -> 'halt', reversible
      autonomous_safe -> 'run' (the escape hatch suppresses NOTHING when unarmed),
      non-safe -> 'halt', low-quality priorOutput -> 'halt'. Prove the escape_hatch
      field has zero effect on the verb when unarmed.
    - Test FORCED-MATERIAL-FIRST (ordering): assert isIrreversibleStep() is the
      first decision -- an irreversible step with a low-quality priorOutput AND an
      active escape_hatch AND armed true STILL returns 'halt' via the first check
      (not via the quality carry, not via the suppression branch). The headline
      cannot be reached by re-ordering.
    - Test SUPPRESSION-PATH (armed, reversible only): with armed forced true via
      the opts.armed seam, an autonomous_safe + REVERSIBLE step carrying an active
      escape_hatch returns 'run' (the branch is reachable and does not throw), and
      a NON-autonomous_safe step carrying an active escape_hatch STILL returns
      'halt' even armed -- the escape hatch may suppress only an already
      autonomous_safe + reversible step's gate, never escalate a non-safe step.
  </behavior>
  <action>
    Add the escape-hatch gate-suppression branch to the gateFn predicate returned
    by makeGateFn() in chain-executor.cjs. ORDERING IS CONSTITUTIONAL and must not
    change: the existing unconditional isIrreversibleStep() first-check at :245
    (the `if (isIrreversibleStep(step)) return 'halt';` statement) STAYS the very
    first statement -- do NOT move it, do NOT add anything before it. Keep the
    quality carry at :249 next. Insert the new suppression branch AFTER the quality
    carry and BEFORE the posture/autonomous_safe resolution at :251, so the order
    is: (1) forced-material halt, (2) quality halt, (3) escape-hatch suppression,
    (4) posture/autonomous_safe return. The suppression branch reads the ARMED flag
    and only acts when armed: gate on `armed && _escapeHatchActive(step)`. Because
    the irreversible step already returned 'halt' at step (1), the suppression
    branch can NEVER reach an irreversible step -- the guardrail is structurally
    upstream. Inside the branch, resolve the posture (reuse the same postureFn /
    explicit-posture resolution the final return uses; factor it so both share one
    read) and return 'run' ONLY when the step is autonomous_safe + push_forward;
    otherwise fall through to the normal return (the escape hatch may suppress only
    an already-autonomous-safe + reversible step's gate, never escalate a non-safe
    step). Read BEHAVIORAL_CHANNEL_ARMED via a LAZY require of
    './navigation/calibration-gate.cjs' (mirror the existing lazy-seam idiom at
    :88-115 -- do NOT re-type the literal false, and do NOT require it eagerly at
    top-of-file in a way that could crash the legacy synchronous path). Make armed
    overridable via an opts seam (opts.armed, defaulting to the calibration-gate
    read) so the test can exercise the armed-true path WITHOUT mutating the shipped
    constant -- the constant stays false on disk (the calibration-gate
    change-control rule forbids flipping it to unblock a wave). Add
    `_escapeHatchActive(step)` as a small pure helper reading only the step's
    escape_hatch scalar handle (a boolean / truthy enum surfaced by the Wave-1
    observation schema), never user prose (Part 8). Add a doc comment above the
    branch naming the WIRED-BUT-SHADOWED contract: the suppression is dormant when
    unarmed (byte-identical gate), the FORCED-MATERIAL guardrail is unconditional
    and checked first (deploy always halts), the live arming is data-gated AND
    Canon-Custodian-gated, and this branch does NOT weaken the human-in-the-loop
    Decision Gate today. Confirm runChain/_runChainResilient halt-reason strings
    ('forced_material' vs 'gate_halt' at :398 / :593) are UNCHANGED. Per BCH-09
    (177-SPEC :47). Hyphens only, no em-dashes (CLAUDE.md house rule). Then replace
    the tests/test-bch-09-forced-material.cjs scaffold stub with the four behavior
    groups above, using the ok()/failed counter idiom from
    tests/test-bch-04-shadow-log.cjs :22-27.
  </action>
  <verify>
    <automated>node tests/test-bch-09-forced-material.cjs</automated>
  </verify>
  <done>
    The escape-hatch suppression branch exists in makeGateFn, gated by
    BEHAVIORAL_CHANNEL_ARMED (read lazily, overridable via opts.armed for the
    test), placed AFTER the unconditional isIrreversibleStep first-check and the
    quality carry and BEFORE the autonomous_safe return; the shipped
    BEHAVIORAL_CHANNEL_ARMED constant stays false on disk;
    tests/test-bch-09-forced-material.cjs exits 0 with the deploy-still-halts
    headline (armed + escape-hatch active), the dormant byte-identical gate when
    unarmed, the forced-material-checked-FIRST ordering, and the armed
    autonomous_safe+reversible suppression path all green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Part 8 brain-boundary scan + full-phase all-green gate</name>
  <read_first>
    - CLAUDE.md (Canon Part 8: the gate predicate ROUTES locally; it opens NO Brain
      wire -- runChain itself opens no Brain wire per chain-executor.cjs :43).
    - tests/run-all-177.sh (the phase gate; after this wave bch-09 flips GREEN and
      the summary should read 16 pass / 0 fail; the carried frozen-set fences
      reach-ids-drift frozen 6 + posture-ids-drift frozen 3 stay GREEN).
    - lib/core/chain-executor.cjs (the suppression branch + helper from Task 1).
  </read_first>
  <action>
    Run a Part-8 brain-boundary source scan over the new suppression branch and its
    helper in chain-executor.cjs: grep the makeGateFn body, the _escapeHatchActive
    helper, and the new lazy calibration-gate require for any user-data egress
    surface (fetch / http / https / buildBrainPacket / sendPacket / a Brain write
    call) and assert NONE is added by this wave -- the branch reads only the ARMED
    boolean, the step's escape_hatch scalar handle, and the resolved posture enum,
    never user prose, never an artifact body, never a meeting transcript (Canon
    Part 8 LOCAL -> BRAIN: NO). The gate introduces no new Brain wire. Then run the
    full phase gate. Confirm: bch-09 flips GREEN (15 pass / 1 fail -> 16 pass /
    0 fail), the carried frozen-set fences stay GREEN (test-reach-ids-drift frozen
    6, test-posture-ids-drift frozen 3 -- this wave mints NO 7th reach, NO 4th
    posture, removes NO IRREVERSIBLE_HINT), and the Wave 1-4 BCH suites stay GREEN
    (no regression on the dormant path). Hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>grep -nE "fetch|http|buildBrainPacket|sendPacket" lib/core/chain-executor.cjs | grep -v '^[0-9]*: *//' | grep -iE "escapehatch|escape_hatch|suppress|behavioral"; echo "scan-exit-nonzero-means-clean:$?"; bash tests/run-all-177.sh 2>&1 | tail -8</automated>
  </verify>
  <done>
    The brain-boundary scan finds zero new user-data egress on the suppression
    branch (the grep matches nothing -> non-zero exit, the clean state); bash
    tests/run-all-177.sh shows bch-09 PASSED (16 pass / 0 fail), the two drift
    fences (reach-ids-drift frozen 6, posture-ids-drift frozen 3) PASSED, and the
    Wave 1-4 BCH suites stay PASSED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| step -> gate predicate | the escape_hatch arrives as a scalar boolean/enum handle (the Wave-1 observation schema surfaced it); it must never carry user prose |
| ARMED flag -> live suppression | BEHAVIORAL_CHANNEL_ARMED is the constitutional kill-switch; flipping it to true is data-gated AND Canon-Custodian-gated (Part 8 PR gate) |
| forced-material guardrail -> ARMED | the guardrail is INDEPENDENT of ARMED -- an irreversible step halts whether armed or not, escape-hatch or not |
| chain executor -> Brain | runChain opens no Brain wire (chain-executor.cjs :43); the suppression branch MUST NOT add one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-09-01 | Elevation of Privilege | escape hatch suppresses a material gate | mitigate | isIrreversibleStep() is the UNCONDITIONAL first statement in gateFn (:245), upstream of the suppression branch; the deploy-still-halts headline test proves an irreversible step halts even armed + escape-hatch active; IRREVERSIBLE_HINTS frozen 7-keyword array unchanged |
| T-177-09-02 | Elevation of Privilege | premature live arming weakens the human-in-the-loop gate | mitigate | the suppression branch is inert when BEHAVIORAL_CHANNEL_ARMED is false (always today); the shipped constant stays false on disk (the test uses an opts.armed seam, never mutates it); the live flip requires a real calibration PASS (data-gated, 177-08 fit.method:'deferred') PLUS a Canon Custodian / brain-boundary review |
| T-177-09-03 | Tampering | gate behavior drift on the dormant path | mitigate | the DORMANT byte-identical test proves makeGateFn returns the pre-seam verb for every step when unarmed; halt-reason strings ('forced_material' / 'gate_halt') at :398 / :593 unchanged |
| T-177-09-04 | Information Disclosure | suppression branch egresses user data | mitigate | Task 2 source scan asserts no fetch/http/buildBrainPacket/sendPacket added on the branch; the branch reads only the ARMED boolean + escape_hatch scalar + posture enum (Part 8 LOCAL->BRAIN: NO) |
| T-177-09-SC | Tampering | npm/pip/cargo installs | accept | this wave adds NO package installs (pure-CJS executor edit + tests); package legitimacy gate N/A |
</threat_model>

<verification>
- node tests/test-bch-09-forced-material.cjs exits 0: the deploy-still-halts
  headline (autonomous_safe + escape-hatch active + armed STILL halts on an
  IRREVERSIBLE_HINT command and on step.irreversible===true), the dormant
  byte-identical gate when unarmed (escape_hatch has zero effect), the
  forced-material-checked-FIRST ordering (an irreversible step halts via the first
  check even with low-quality priorOutput + escape_hatch + armed), and the armed
  autonomous_safe+reversible suppression path all green.
- The shipped BEHAVIORAL_CHANNEL_ARMED constant stays false on disk; the test
  arms via an opts.armed seam, never mutating the constant.
- Part 8 source scan over the suppression branch finds zero new user-data egress.
- bash tests/run-all-177.sh: bch-09 PASSED (15 pass / 1 fail -> 16 pass / 0 fail);
  the carried frozen-set fences (reach-ids-drift frozen 6, posture-ids-drift
  frozen 3) PASSED; the Wave 1-4 BCH suites stay PASSED.
</verification>

<success_criteria>
- The escape-hatch gate-suppression branch exists in makeGateFn(), gated by
  BEHAVIORAL_CHANNEL_ARMED (default LOCKED), placed AFTER the unconditional
  isIrreversibleStep() first-check and the quality carry, BEFORE the
  autonomous_safe/posture return.
- The FORCED-MATERIAL guardrail is UNCONDITIONAL and checked FIRST: an irreversible
  step (deploy/publish/send/... or step.irreversible===true) ALWAYS halts -- armed
  or not, escape-hatch or not.
- The suppression is DORMANT today: with the flag false, makeGateFn() is
  byte-identical to its pre-seam behavior; the escape_hatch handle has zero effect
  on the returned verb; the existing Decision Gate fires unchanged.
- The branch reads only enum/scalar handles (ARMED boolean, escape_hatch scalar,
  posture enum); no new Brain egress (Canon Part 8).
- Frozen sets untouched (6 reaches, 3 postures, the 7 IRREVERSIBLE_HINTS); no 7th
  reach, no 4th posture, no hint removed.
- This wave does NOT weaken the human-in-the-loop Decision Gate today; the live
  flip is data-gated AND Canon-Custodian-gated.
- bash tests/run-all-177.sh is all-green (16 pass / 0 fail).
</success_criteria>

<output>
Create `.planning/phases/177-larry-behavioral-channel/177-10-SUMMARY.md` when done.
</output>
