---
phase: 177-larry-behavioral-channel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/projections.cjs
  - lib/workflow/f-selector-ranker.cjs
  - tests/test-bch-17-ignite-persona.cjs
autonomous: true
requirements: [BCH-S1, BCH-05, BCH-06, BCH-PERSONA, BCH-17]
canon_parts: [3, 8, 11]
cirs_relationship:
  surfaces_modified: [f-selector-ranker, projections]
  surfaces_added: []
  surfaces_removed: []
  spine_consumed:
    - lib/core/navigation/projections.cjs
    - lib/workflow/f-selector-ranker.cjs
  gate_impact: "Extends the engine-owned investment_level (the single canonical runtime number per D-177-Q5) to fold in a NEW runtime turn_count term plus the jump conditions, and pins the behavioral-channel floor/ceiling/band thresholds in f-selector-ranker.cjs (NEVER a prompt). Seeds the dial default deterministically from the ignite persona role_blend (BCH-17). No reach minted, no posture minted, no Brain wire opened. Realizes Part 11 one-governed-path: the engine composes; the model only observes."
must_haves:
  truths:
    - "investment_level folds in a runtime turn_count term on top of the existing framework_invocations term (one canonical number)"
    - "BEHAVIORAL_CHANNEL_CEILING is defined in f-selector-ranker.cjs as RECOMMENDED_CONFIDENCE_FLOOR + 0.15 (== 0.85), at least 15 points above the Brain 0.70 floor"
    - "the floor (0.50), ceiling (0.85), and protected band live in f-selector-ranker.cjs and in NO prompt"
    - "a role_blend top=student seeds an ASK-leaning dial default plus the teaching-density branch (< 0.4); a role_blend top=founder/investor seeds a TELL-leaning terser default"
    - "no role_blend present degrades to the canonical_role scalar; neither present cold-starts neutral; a blend is never fabricated"
  artifacts:
    - path: "lib/core/navigation/projections.cjs"
      provides: "computeInvestmentLevel extended with a turn_count term + the deterministic jump conditions"
      contains: "computeInvestmentLevel"
    - path: "lib/workflow/f-selector-ranker.cjs"
      provides: "BEHAVIORAL_CHANNEL_FLOOR / BEHAVIORAL_CHANNEL_CEILING / protected-band consts + the ignite-persona dial-default seed"
      contains: "BEHAVIORAL_CHANNEL_CEILING"
  key_links:
    - from: "lib/workflow/f-selector-ranker.cjs"
      to: "lib/core/navigation/projections.cjs"
      via: "projections.computeInvestmentLevel(effectiveRoomState)"
      pattern: "projections\\.computeInvestmentLevel"
    - from: "lib/workflow/f-selector-ranker.cjs"
      to: "the 0.30*investment_level problem_type_bind term"
      via: "the ignite-persona seed biases investment_level before the score formula"
      pattern: "problem_type_bind"
---

<objective>
Establish THE single canonical runtime investment number and the behavioral-channel
thresholds, and seed the dial default deterministically from the ignite persona prior.

This is the honest first commit of Phase 177: everything downstream reads ONE number.
Per D-177-Q5, investment_level (engine-owned, computed in projections.cjs and consumed by
the ranker) IS the canonical number. This plan EXTENDS it to fold in a NEW runtime
turn_count term (and leaves room for the later jump conditions) on top of the existing
framework_invocations term, pins the floor/ceiling/band thresholds in the ranker (NEVER a
prompt, per BCH-05), and seeds the dial default from role_blend (BCH-17/BCH-PERSONA).

Purpose: realize the bright line (the engine owns the composed decision; the model emits
observations only) and "one read, two consumers" -- the ranker reads investment_level for
routing density; Pass 2 prose is INJECTED from the same number, not a second source.
Output: an extended computeInvestmentLevel, the BEHAVIORAL_CHANNEL_* threshold consts, a
deterministic ignite-persona dial seed, and a green test-bch-17-ignite-persona suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-larry-behavioral-channel/177-CONTEXT.md
@.planning/phases/177-larry-behavioral-channel/177-SPEC.md
@.planning/phases/177-larry-behavioral-channel/177-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend computeInvestmentLevel with a runtime turn_count term (BCH-S1)</name>
  <read_first>
    - lib/core/navigation/projections.cjs:202-235 (the EXISTING computeInvestmentLevel: level = min(1, max(0, framework_invocations/10)); the D4 formula docstring; the module.exports block at :232-235)
    - lib/workflow/f-selector-ranker.cjs:392-394 (the SOLE consumer: const { level: investment_level } = projections.computeInvestmentLevel(effectiveRoomState))
    - .planning/phases/177-larry-behavioral-channel/177-RESEARCH.md sec 5c (D-177-Q5: investment_level is THE canonical number; BCH-S1 extends it to fold turn_count + jump conditions on top of framework_invocations)
    - .planning/phases/177-larry-behavioral-channel/177-RESEARCH.md sec 5b Q5 (there is NO runtime turn counter feeding the engine today; venture-shape-nudge derives one from nodes only; BCH-S1 must BUILD the counter term)
  </read_first>
  <action>
    In lib/core/navigation/projections.cjs extend computeInvestmentLevel(roomState) so the
    returned level folds in a NEW runtime turn_count term ADDITIVELY on top of the existing
    framework_invocations/10 term, keeping the result clamped to [0,1] and keeping the
    function pure (same inputs -> same output, no I/O). Read roomState.turn_count as the
    counter (a number; absent or non-number contributes 0, exactly mirroring the existing
    framework_invocations defensive read at :214-217). Keep the EXISTING framework_invocations
    behavior byte-stable when turn_count is absent (so every prior caller and the
    test-f-selector-ranker suites stay green). Document in the docstring (mirroring the
    :202-212 D3 block) that turn_count is the per-turn runtime term and that the jump
    conditions (escape hatch / saturation / pushback / evidence) are folded in by later
    waves -- name them as reserved roomState fields the function reads-if-present, defaulting
    to no-op when absent. Keep computeInvestmentLevel in module.exports (already exported at
    :235). Do NOT change the ranker consumer signature -- it still destructures { level }.
    Do NOT add any prompt-side dial computation; the prose Ask-Tell dial is INJECTED from
    this number in Pass 2 (a later wave), not computed by the model (D-177-Q5).
  </action>
  <behavior>
    - With { framework_invocations: 5 } and no turn_count: level === 0.5 (byte-stable with today).
    - With { framework_invocations: 0, turn_count: N }: level rises monotonically with turn_count, clamped to <= 1.0.
    - With {} (neither field): level === 0 and label === 'fresh room, ranking with Brain priors' (cold start unchanged).
    - With a negative turn_count: clamped to >= 0 (defensive, mirrors the framework_invocations floor).
  </behavior>
  <verify>
    <automated>node -e "const p=require('./lib/core/navigation/projections.cjs'); const a=p.computeInvestmentLevel({framework_invocations:5}); const b=p.computeInvestmentLevel({framework_invocations:5,turn_count:8}); const c=p.computeInvestmentLevel({}); if(a.level!==0.5) throw new Error('byte-stability broken: '+a.level); if(!(b.level>=a.level&&b.level<=1.0)) throw new Error('turn_count term not folded: '+b.level); if(c.level!==0) throw new Error('cold start broke: '+c.level); console.log('OK BCH-S1 turn_count term');"</automated>
  </verify>
  <done>computeInvestmentLevel folds a clamped turn_count term on top of framework_invocations; absent-turn_count behavior is byte-stable; cold start is unchanged; jump-condition fields are read-if-present and documented as reserved.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the behavioral-channel floor/ceiling/band thresholds in the ranker (BCH-05, BCH-06)</name>
  <read_first>
    - lib/workflow/f-selector-ranker.cjs:71-141 (MAX_K=3 const at :77; the D9 selectWhyContent density branches < 0.4 / 0.4-0.7 / >= 0.7 at :130-141 -- the precedent for threshold literals living in THIS file)
    - lib/core/navigation-engine.cjs:74 (const RECOMMENDED_CONFIDENCE_FLOOR = 0.7; the Brain floor the ceiling must sit >= 15 points above)
    - .planning/phases/177-larry-behavioral-channel/177-SPEC.md Architecture-decision table (Below floor < 0.50 / Protected band 0.50-0.85 / Above ceiling > 0.85; "All three numbers live in f-selector-ranker.cjs, NONE in any prompt")
    - .planning/phases/177-larry-behavioral-channel/177-RESEARCH.md ledger row "0.85 channel ceiling -- NOT in code -- corrected (add as const)"
  </read_first>
  <action>
    In lib/workflow/f-selector-ranker.cjs add module-level frozen consts near the MAX_K
    declaration (:77): BEHAVIORAL_CHANNEL_FLOOR = 0.50, BEHAVIORAL_CHANNEL_CEILING defined as
    RECOMMENDED_CONFIDENCE_FLOOR + 0.15 (require RECOMMENDED_CONFIDENCE_FLOOR from
    lib/core/navigation-engine.cjs, or mirror it as a named local const that documents its
    source line so the >= 15-point invariant is computed, not hand-typed). The protected band
    is the half-open interval [BEHAVIORAL_CHANNEL_FLOOR, BEHAVIORAL_CHANNEL_CEILING]. Add a
    short comment block (mirroring the :118-129 D9 comment idiom) naming the three bands
    (below-floor cue discarded but still logged in a later wave; protected band fires
    SIGNAL-tier and cannot override Brain >= 0.70; above-ceiling may outrank the graph). Export
    the three consts on the module so the test and later-wave seams can read them. Do NOT
    write any of these numbers into a prompt, a SKILL.md, or larry-server-instructions.md
    (BCH-05 adversarial grep). Add NO dial/fire/investment_level field to any model-facing
    schema (BCH-01 ownership invariant -- this file is engine-side).
  </action>
  <verify>
    <automated>node -e "const r=require('./lib/workflow/f-selector-ranker.cjs'); const flr=require('./lib/core/navigation-engine.cjs'); if(typeof r.BEHAVIORAL_CHANNEL_CEILING!=='number') throw new Error('ceiling const missing'); if(Math.abs(r.BEHAVIORAL_CHANNEL_CEILING-0.85)>1e-9) throw new Error('ceiling != 0.85: '+r.BEHAVIORAL_CHANNEL_CEILING); if(r.BEHAVIORAL_CHANNEL_CEILING - 0.70 < 0.15 - 1e-9) throw new Error('ceiling not >=15pts above Brain floor'); if(Math.abs(r.BEHAVIORAL_CHANNEL_FLOOR-0.50)>1e-9) throw new Error('floor != 0.50'); console.log('OK BCH-05/06 thresholds');"</automated>
    <automated>bash -c "grep -RnE '0\\.50|0\\.85|behavioral_channel|BEHAVIORAL_CHANNEL' skills/ lib/mcp/larry-server-instructions.md 2>/dev/null | grep -v '^#' | grep -ci 'ceiling\\|floor\\|band' | { read c; if [ \"\$c\" -ne 0 ]; then echo \"PROMPT LEAK: \$c\"; exit 1; fi; echo 'OK no threshold literal in any prompt'; }"</automated>
  </verify>
  <done>BEHAVIORAL_CHANNEL_FLOOR (0.50) and BEHAVIORAL_CHANNEL_CEILING (== RECOMMENDED_CONFIDENCE_FLOOR + 0.15 == 0.85) are defined and exported in f-selector-ranker.cjs; the ceiling is computed from the Brain floor (>= 15 points above); no threshold literal appears in any prompt or SKILL.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Seed the dial default from the ignite persona prior, then turn test-bch-17 green (BCH-17, BCH-PERSONA)</name>
  <read_first>
    - lib/workflow/f-selector-ranker.cjs:359-394 (rankForSelector entry; effectiveRoomState patching at :384-390; the investment_level snapshot at :392-394) and :280-292 (the score formula with the 0.30*investment_level problem_type_bind term)
    - lib/core/persona-override.cjs:80-93 (ROLE_BLEND_KEYS frozen 7-role set + normalizeRoleKey) and :275-282 (the role_blend[key]=1.0 single-axis idiom this plan mirrors for the seed read)
    - lib/core/persona-taxonomy.cjs (the 7 canonical role keys; align the seed mapping to them 1:1)
    - .planning/phases/177-larry-behavioral-channel/177-RESEARCH.md sec 4 BCH-17 (read role_blend -> seed dial default + the 0.30*inv term; student top -> ASK-leaning + teaching density < 0.4; founder/investor top -> TELL-leaning + terse; degrade to canonical_role scalar; cold-start neutral; never fabricate)
    - tests/test-bch-17-ignite-persona.cjs (the scaffold STUB to REPLACE with real asserts)
  </read_first>
  <action>
    In lib/workflow/f-selector-ranker.cjs add a pure helper that reads the ignite persona
    prior off roomState and returns a deterministic dial-default seed in [0,1]: read
    roomState.role_blend first (an object of { role: weight }); take the top-weighted role
    (ties broken by ROLE_BLEND_KEYS order for determinism); map student -> an ASK-leaning low
    seed (lands in the < 0.4 teaching-density branch of selectWhyContent), founder/investor ->
    a TELL-leaning high seed (lands in the >= 0.7 terse branch), other known roles -> neutral
    mid-seed. If role_blend is absent or empty, degrade to roomState.canonical_role scalar
    (same mapping via normalizeRoleKey from persona-override.cjs). If neither is present,
    cold-start NEUTRAL (a mid seed). NEVER fabricate a blend. Thread this seed so it BIASES
    the investment_level used by the score formula (the 0.30*investment_level problem_type_bind
    term at :288-289) -- e.g. fold the seed into effectiveRoomState before the
    computeInvestmentLevel snapshot at :392-394, or expose the seed on the returned items so
    the Pass-2 prose injection (a later wave) reads it. Keep rankForSelector pure and
    synchronous (no Brain call, no db write -- the D10 invariant at :355-358). Then REPLACE the
    scaffold body of tests/test-bch-17-ignite-persona.cjs with real assertions: a role_blend
    top=student yields an ASK-leaning seed (lands in the teaching-density < 0.4 branch with no
    model cue); the adversarial twin role_blend top=founder (and a second case top=investor)
    yields a TELL-leaning terser seed (>= 0.7 branch), proving the founder twin does NOT get
    the patient default; the no-role_blend case degrades to the canonical_role scalar; the
    neither case cold-starts neutral; assert a blend is never fabricated (an empty input
    yields the neutral seed, not an invented role). Make the test exit 0 on pass, non-zero on
    fail.
  </action>
  <behavior>
    - role_blend { student: 1.0 } -> seed < 0.4 (ASK-leaning, teaching-density branch).
    - role_blend { founder: 1.0 } -> seed >= 0.7 (TELL-leaning, terse branch); role_blend { investor: 1.0 } likewise.
    - role_blend absent, canonical_role: 'student' -> degrade to the same ASK-leaning seed.
    - neither role_blend nor canonical_role -> neutral mid seed; no role invented.
  </behavior>
  <verify>
    <automated>node tests/test-bch-17-ignite-persona.cjs</automated>
  </verify>
  <done>The ignite-persona seed is a pure deterministic read (role_blend -> canonical_role scalar -> cold-start neutral, never fabricated); student leans ASK, founder/investor lean TELL; the seed biases investment_level; tests/test-bch-17-ignite-persona.cjs is real and exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model -> engine | The model emits observations only; the ranker (engine) computes investment_level + the dial. No composed state crosses INTO the engine from the model. |
| engine -> prompt | The dial number is INJECTED into Pass-2 prose; the prompt never computes or owns it. |
| LOCAL -> Brain | role_blend, canonical_role, turn_count are LOCAL; none egress (Part 8). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-01-01 | Tampering | f-selector-ranker thresholds | mitigate | Floor/ceiling/band are frozen consts in f-selector-ranker.cjs; adversarial grep over skills/*/SKILL.md + larry-server-instructions.md proves no prompt copy can drift them (BCH-05). |
| T-177-01-02 | Elevation of Privilege | the model dial | mitigate | The model emits no dial/investment_level field; the engine computes it (BCH-01). This file is engine-side; Task 2 adds no model-facing schema field. |
| T-177-01-03 | Information disclosure | ignite persona read | accept | role_blend / canonical_role are LOCAL room state read in-process; no Brain wire is opened (Part 8); cross-room egress is not in scope. |
| T-177-01-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds NO package installs; pure CJS edits to existing files. |
</threat_model>

<verification>
- node tests/test-bch-17-ignite-persona.cjs exits 0.
- The byte-stability probe in Task 1 confirms framework_invocations-only behavior is unchanged.
- The adversarial prompt-leak grep in Task 2 returns zero threshold literals in prompts/SKILL.md.
- bash tests/run-all-177.sh shows test-bch-17 flipped GREEN and the frozen-set drift fences (6 reaches, 3 postures) still GREEN.
</verification>

<success_criteria>
- computeInvestmentLevel folds a clamped turn_count term ADDITIVELY on framework_invocations; absent-turn_count and cold-start behavior byte-stable.
- BEHAVIORAL_CHANNEL_FLOOR (0.50) + BEHAVIORAL_CHANNEL_CEILING (== RECOMMENDED_CONFIDENCE_FLOOR + 0.15 == 0.85) defined and exported in f-selector-ranker.cjs; ceiling >= 15 points above the Brain 0.70 floor; zero threshold literals in any prompt.
- The ignite-persona dial seed is deterministic, degrades real (blend -> scalar -> neutral), never fabricates, and biases investment_level; test-bch-17-ignite-persona is GREEN.
- No reach minted, no posture minted, no Brain wire opened.
</success_criteria>

<output>
Create `.planning/phases/177-larry-behavioral-channel/177-01-SUMMARY.md` when done.
</output>
