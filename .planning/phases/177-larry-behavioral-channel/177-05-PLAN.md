---
phase: 177-larry-behavioral-channel
plan: 05
type: execute
wave: 2
depends_on: ["177-01"]
canon_parts: [8, 9, 11]
files_modified:
  - lib/core/memory-ops.cjs
  - lib/core/navigation/memory-events.cjs
  - lib/core/navigation/calibration-log.cjs
  - lib/core/navigation.cjs
  - tests/test-bch-04-shadow-log.cjs
  - tests/run-all-177.sh
autonomous: true
requirements: [BCH-04, BCH-REG-AUDIT]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: []
  gate_impact: "none -- a LOCAL schema + a thin LOCAL writer + one additive EVENT_TYPES audit-marker string. No invocable surface TRIGGER wire is added, modified, or removed; no reach minted. R12 declaration filed because this plan touches the navigation.cjs chokepoint family (Part 9 read/write surface), so the cirs_relationship block is declared for completeness; build-connector-registry.cjs --check is unaffected."

must_haves:
  truths:
    - "A below-floor cue (confidence < 0.50) STILL writes a calibration_observations row (shadow before trust)."
    - "Every logged cue row carries a cue_disposition column recording whether it was discarded (below floor), fired (protected band), or above-ceiling."
    - "The calibration_observations table is a DEDICATED physical table that buildBrainPacket never reads (Part 8 zero-egress holds BY CONSTRUCTION)."
    - "initCalibrationSchema(db) is idempotent (CREATE TABLE IF NOT EXISTS) and co-located in the memory-ops.cjs LOCAL-schema family."
  artifacts:
    - path: "lib/core/memory-ops.cjs"
      provides: "initCalibrationSchema(db) idempotent CREATE TABLE IF NOT EXISTS calibration_observations"
      contains: "initCalibrationSchema"
    - path: "lib/core/navigation/calibration-log.cjs"
      provides: "logCalibrationObservation(db, obs) thin LOCAL row writer (the dedicated-table store)"
      contains: "logCalibrationObservation"
    - path: "lib/core/navigation/memory-events.cjs"
      provides: "ONE additive EVENT_TYPES string: calibration_observation_logged (scalar audit marker only)"
      contains: "calibration_observation_logged"
    - path: "tests/test-bch-04-shadow-log.cjs"
      provides: "real asserts replacing the RED scaffold stub"
      contains: "calibration_observations"
  key_links:
    - from: "lib/core/navigation/calibration-log.cjs"
      to: "calibration_observations table"
      via: "prepared INSERT through the passed db handle"
      pattern: "INSERT INTO calibration_observations"
    - from: "lib/core/navigation.cjs"
      to: "calibration-log.logCalibrationObservation"
      via: "re-export on the closed navigation chokepoint"
      pattern: "logCalibrationObservation"
---

<objective>
Build the GREENFIELD calibration substrate: a dedicated, LOCAL-only calibration_observations
table plus a thin writer that logs EVERY pre-threshold behavioral cue -- including the
discarded below-floor ones -- so Wave 3 (BCH-CAL) has shadow data to calibrate against.

This wave is SHADOW-ONLY. The writer collects data. Nothing fires. routing_source stays
legacy. No reach is minted, no semantic seam opens. This is data collection, by construction
invisible to the Brain.

Purpose: "Shadow before trust." A behavioral channel that has never observed real cues cannot
have its floor/ceiling pinned. BCH-04 is the observation tape; BCH-CAL (Wave 3) reads it.

Output: initCalibrationSchema(db) in the memory-ops.cjs family; a calibration-log.cjs writer;
one additive EVENT_TYPES audit-marker string; the navigation.cjs re-export; test-bch-04 GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/177-larry-behavioral-channel/177-SPEC.md
@.planning/phases/177-larry-behavioral-channel/177-RESEARCH.md
@.planning/phases/177-larry-behavioral-channel/177-WAVE-2-PLANNING-BRIEF.md
@docs/MINDRIAN-CANON.md

# The LOCAL-schema family this co-locates with (initMemorySchema idiom at :23):
@lib/core/memory-ops.cjs
# The memory-event log path -- the EVENT_TYPES frozen Set (audit marker rides here)
# and logEvent (which INSERTs memory_event NODES -- swept by buildBrainPacket, so the
# ROW STORE must NOT be a memory_event; it is the dedicated table):
@lib/core/navigation/memory-events.cjs
# The Part 8 egress fence this must NOT trip (regression fence, stays GREEN):
@tests/test-bch-14-part8-egress.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mint the dedicated calibration_observations table (initCalibrationSchema)</name>
  <read_first>
    - lib/core/memory-ops.cjs:16-129 (the initMemorySchema idiom: db.exec with a block of
      CREATE TABLE IF NOT EXISTS ... + CREATE INDEX IF NOT EXISTS; node:sqlite DatabaseSync
      handle passed in by the caller after openGraph; module.exports at :582-600).
    - lib/core/navigation/memory-events.cjs:502-553 (logEvent INSERTs into the nodes table as
      a memory_event NODE -- this is WHY calibration rows MUST live in a separate physical
      table: findRecentChanges (called inside buildBrainPacket, packet.cjs:330) sweeps
      memory_event nodes into the Brain packet).
  </read_first>
  <behavior>
    - initCalibrationSchema(db) called twice on the same db does not throw and creates the
      table exactly once (idempotent CREATE TABLE IF NOT EXISTS).
    - The table has the columns needed to calibrate: an id, a turn handle (turn_id / turn_count
      scalar), the cue handle (cue_kind enum string e.g. reframe_cue / escape_hatch), a numeric
      confidence, a cue_disposition enum string (discarded | fired | above_ceiling), the band
      label, created_at epoch ms, and created_by (defaults system).
    - After initCalibrationSchema, a raw INSERT of a below-floor row (confidence 0.30,
      cue_disposition 'discarded') succeeds and SELECT COUNT(*) returns 1.
  </behavior>
  <action>
    In lib/core/memory-ops.cjs add an idempotent initCalibrationSchema(db) mirroring the
    initMemorySchema idiom (a single db.exec block, CREATE TABLE IF NOT EXISTS
    calibration_observations plus a CREATE INDEX IF NOT EXISTS on created_at). Column set:
    id TEXT PRIMARY KEY; turn_id TEXT; turn_count INTEGER; cue_kind TEXT NOT NULL (the
    observation cue family handle -- enum string, never prose); confidence REAL; cue_disposition
    TEXT NOT NULL (one of discarded | fired | above_ceiling -- the load-bearing column that
    proves below-floor cues are logged, not dropped); band TEXT (the threshold-band label);
    created_by TEXT DEFAULT 'system'; created_at INTEGER. Per Canon Part 8 the columns hold ONLY
    scalars + enum handles -- NEVER artifact prose, meeting text, or user identifiers. Add
    initCalibrationSchema to module.exports. Co-locate here (NOT lazygraph-ops.cjs, which is the
    reduced legacy id/type/properties schema per 177-RESEARCH MG-12). The table is a DEDICATED
    physical table so it is structurally invisible to buildBrainPacket (packet.cjs reads only
    nodes + identity), satisfying BCH-14 by construction.
  </action>
  <verify>
    <automated>node -e "const{DatabaseSync}=require('node:sqlite');const db=new DatabaseSync(':memory:');const m=require('./lib/core/memory-ops.cjs');m.initCalibrationSchema(db);m.initCalibrationSchema(db);db.prepare(\"INSERT INTO calibration_observations (id,turn_count,cue_kind,confidence,cue_disposition,band,created_at) VALUES ('c1',1,'reframe_cue',0.30,'discarded','below_floor',1)\").run();const n=db.prepare('SELECT COUNT(*) AS c FROM calibration_observations').get().c;if(n!==1)throw new Error('expected 1 row, got '+n);console.log('OK idempotent + below-floor row stored');"</automated>
  </verify>
  <done>initCalibrationSchema(db) exists in memory-ops.cjs, is idempotent, is exported, and a below-floor (0.30 / discarded) row inserts and counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the thin LOCAL writer + the scalar audit marker, re-export through the chokepoint</name>
  <read_first>
    - lib/core/navigation/memory-events.cjs:10-145 (the frozen EVENT_TYPES Set + the additive
      extension idiom -- each prior phase appended named strings with a comment block; tests
      assert a FLOOR + named membership, never an exact count).
    - lib/core/navigation/memory-events.cjs:502-553 (logEvent -- the audit-marker emit path;
      created_by defaults system, review_status confirmed, which is canon-legal for a
      system-bookkeeping node per the Part 9 audit-node carve-out).
    - lib/core/navigation.cjs:87-100 (the closed chokepoint re-export surface -- logMemoryEvent
      / findRecentChanges live here; add the calibration re-export alongside).
  </read_first>
  <behavior>
    - logCalibrationObservation(db, obs) writes exactly ONE calibration_observations row and
      returns { ok: true, id }.
    - A below-floor observation (confidence 0.30) writes a row with cue_disposition 'discarded'
      -- it is NOT dropped (the BCH-04 load-bearing assert).
    - A protected-band observation (0.50 <= confidence <= 0.85) writes cue_disposition 'fired';
      an above-ceiling (> 0.85) writes 'above_ceiling'.
    - The writer optionally emits ONE memory_event of type calibration_observation_logged as a
      SCALAR AUDIT MARKER (turn handle + disposition enum + confidence scalar ONLY) -- the ROW
      STORE remains the dedicated table; the audit marker carries no row body.
    - navigation.logCalibrationObservation is reachable through the chokepoint re-export.
  </behavior>
  <action>
    Create lib/core/navigation/calibration-log.cjs exporting logCalibrationObservation(db, obs).
    Derive cue_disposition + band from confidence against the engine-owned thresholds already
    pinned in Wave 1 (BEHAVIORAL_CHANNEL_FLOOR 0.50 / CEILING 0.85, per 177-01-SUMMARY and
    f-selector-ranker.cjs:47-52): confidence < FLOOR -> disposition 'discarded' band
    'below_floor'; FLOOR <= confidence <= CEILING -> 'fired' band 'protected'; confidence >
    CEILING -> 'above_ceiling' band 'above_ceiling'. Read the const from its engine home rather
    than re-typing the literal (computed-not-hand-typed, mirroring the Wave-1 ceiling pattern);
    if importing the const is awkward, accept the band/disposition pre-computed by the caller but
    keep the table the source of truth. The writer does a prepared INSERT INTO
    calibration_observations (parameterized, no string interpolation). Generate id like the
    logEvent idiom (a stable prefix + epoch + random hex). Then, additively, append ONE string
    'calibration_observation_logged' to the frozen EVENT_TYPES Set in memory-events.cjs with a
    comment block in the established idiom (Phase 177 BCH-04; scalar audit marker -- turn handle +
    disposition enum + confidence scalar ONLY; the ROW STORE is the dedicated calibration table,
    NOT this memory_event, so buildBrainPacket never sees a row body). Have logCalibrationObservation
    optionally call logEvent(db,'calibration_observation_logged',{turn_count, cue_disposition,
    confidence, dedupe? }) for the audit trail. Finally re-export logCalibrationObservation on the
    lib/core/navigation.cjs closed surface alongside logMemoryEvent. Per Q10 (CIRS born-wired):
    this adds NO invocable surface TRIGGER wire, so build-connector-registry.cjs --check is
    unaffected; the cirs_relationship block in frontmatter declares this explicitly (R12).
  </action>
  <verify>
    <automated>node -e "const{DatabaseSync}=require('node:sqlite');const db=new DatabaseSync(':memory:');const m=require('./lib/core/memory-ops.cjs');m.initCalibrationSchema(db);const nav=require('./lib/core/navigation.cjs');const r=nav.logCalibrationObservation(db,{turn_count:2,cue_kind:'reframe_cue',confidence:0.30});if(!r||!r.ok)throw new Error('below-floor write not ok');const row=db.prepare(\"SELECT cue_disposition FROM calibration_observations LIMIT 1\").get();if(row.cue_disposition!=='discarded')throw new Error('below-floor must be discarded, got '+row.cue_disposition);const ET=require('./lib/core/navigation/memory-events.cjs').EVENT_TYPES;if(!ET.has('calibration_observation_logged'))throw new Error('audit marker not in EVENT_TYPES');console.log('OK writer + below-floor disposition + audit marker + chokepoint re-export');"</automated>
  </verify>
  <done>logCalibrationObservation re-exported on navigation.cjs; below-floor cue stores a 'discarded' row; calibration_observation_logged is in EVENT_TYPES; protected/above-ceiling dispositions derive correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace the RED test-bch-04 stub with real asserts; register stays in run-all-177</name>
  <read_first>
    - tests/test-bch-04-shadow-log.cjs (the current RED scaffold stub -- exit 1; replace the body).
    - tests/test-bch-14-part8-egress.cjs:1-45 (the pure-node assert idiom: an ok(cond,msg)
      helper + a failed counter + process.exit(failed?1:0); mirror this style).
    - tests/run-all-177.sh:43-45 (the bch-04 line is already registered; do NOT duplicate it).
  </read_first>
  <behavior>
    - The suite opens an in-memory db, runs initCalibrationSchema, and asserts:
      (1) a below-floor cue (confidence 0.30) writes a row whose cue_disposition is 'discarded'
          (the BCH-04 headline: discarded cues STILL log);
      (2) a protected-band cue (0.70) writes cue_disposition 'fired';
      (3) an above-ceiling cue (0.92) writes 'above_ceiling';
      (4) calibration_observation_logged is a member of EVENT_TYPES;
      (5) the calibration_observations table name does NOT appear in packet.cjs or
          navigation.cjs egress path (mirror the BCH-14 source-scan tripwire -- a Part 8
          regression fence local to this suite).
    - Exit 0 when all pass, exit 1 on any failure.
  </behavior>
  <action>
    Replace the stub body of tests/test-bch-04-shadow-log.cjs with real pure-node asserts using
    the ok(cond,msg) + failed-counter idiom from test-bch-14. Drive writes through
    navigation.logCalibrationObservation (the chokepoint), not a raw INSERT, so the suite
    exercises the real surface. Include the Part 8 source-scan assert: read lib/core/navigation/
    packet.cjs and assert the string calibration_observations does not appear (regression fence
    -- the dedicated table must never be wired into the Brain packet). End with
    process.exit(failed ? 1 : 0). The bch-04 line already exists in run-all-177.sh:44 -- leave it.
  </action>
  <verify>
    <automated>node tests/test-bch-04-shadow-log.cjs</automated>
  </verify>
  <done>node tests/test-bch-04-shadow-log.cjs exits 0; the below-floor / protected / above-ceiling dispositions all assert; the Part 8 packet source-scan passes; bch-04 turns GREEN in run-all-177.sh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LOCAL room.db -> BRAIN packet | The dedicated calibration table must never be read by buildBrainPacket. This is the Part 8 constitutional boundary. |
| caller obs -> calibration row | Untrusted observation fields cross into the row store; only scalars + enum handles are permitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-05-01 | Information Disclosure | calibration_observations table read by Brain egress | mitigate | DEDICATED physical table is structurally invisible to buildBrainPacket (reads only nodes + identity); Task 3 adds a source-scan fence asserting the table name is absent from packet.cjs; the carried test-bch-14-part8-egress fence stays GREEN. |
| T-177-05-02 | Tampering | SQL injection via observation fields | mitigate | All writes use prepared statements with ? parameters (no string interpolation), per the memory-ops idiom. |
| T-177-05-03 | Information Disclosure | prose/PII smuggled into a calibration column | mitigate | Columns hold only scalars + enum handles (cue_kind, cue_disposition, band, confidence, turn handles); no free-text column exists; the audit marker carries no row body. |
| T-177-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (node:sqlite is a built-in; zero new deps). |
</threat_model>

<verification>
- node tests/test-bch-04-shadow-log.cjs exits 0 (was a RED scaffold stub).
- node tests/test-bch-14-part8-egress.cjs still exits 0 (Part 8 fence unbroken).
- bash tests/run-all-177.sh shows bch-04 PASSED; no previously-green suite regresses.
- grep confirms 'calibration_observations' appears in memory-ops.cjs + calibration-log.cjs but NOT in lib/core/navigation/packet.cjs.
</verification>

<success_criteria>
- A below-floor cue (confidence < 0.50) writes a calibration_observations row with cue_disposition 'discarded'. The discarded cue is logged, not dropped.
- The calibration table is dedicated + LOCAL; buildBrainPacket never reads it (Part 8 holds by construction).
- initCalibrationSchema is idempotent and co-located in the memory-ops.cjs family; logCalibrationObservation is reachable on the navigation chokepoint; calibration_observation_logged is an additive EVENT_TYPES audit marker.
- SHADOW-ONLY: this plan logs, it does not fire. No reach minted, routing_source untouched, no semantic seam.
</success_criteria>

<output>
Create `.planning/phases/177-larry-behavioral-channel/177-05-SUMMARY.md` when done.
</output>
