---
phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig
plan: 08
subsystem: docs-and-mcp-shim
tags: [mcp, brain-client, connector-docs, theo, skill-mirrors, dist-bundles, tool-descriptions]

requires:
  - phase: 339-01
    provides: Phase 339 test/verification scaffolding (tests/run-all-339.sh, etc.)
  - phase: 339-02
    provides: earlier PREP-cut groundwork this plan's docs cross-reference
  - phase: 339-03
    provides: earlier PREP-cut groundwork this plan's docs cross-reference
provides:
  - "Six hand-authored Desktop/Cowork connector-instruction sources naming Theo's /mcp endpoint under the unchanged mindrian-brain key, with no Authorization header"
  - "Regenerated skills/setup/SKILL.md and dist/generic-claude-dir + dist/zed mirrors matching the updated sources"
  - "Backend-agnostic descriptions for the Brain shim's five brain_* MCP tools (bin/mindrian-brain-mcp-client.cjs)"
affects: [339-09, 339-11, verify-release-gate-10b, verify-release-gate-19]

tech-stack:
  added: []
  patterns:
    - "Connector docs state the URL asymmetry explicitly (direct connector needs /mcp, brain-client.cjs default is the bare origin) everywhere a source shows either form"
    - "Retired-harness reason strings reference lib/core/brain-client.cjs's BRAIN_URL default by name instead of hardcoding a host, so they cannot go stale again on the next backend move"

key-files:
  created: []
  modified:
    - docs/brain-setup.md
    - docs/install/BRAIN-SETUP.md
    - docs/THE-BRAIN.md
    - commands/setup.md
    - commands/pws-brain.md
    - skills/pws-brain/SKILL.md
    - skills/setup/SKILL.md
    - dist/generic-claude-dir/.claude/skills/pws-brain/SKILL.md
    - dist/generic-claude-dir/.claude/skills/setup/SKILL.md
    - dist/zed/.agents/skills/pws-brain/SKILL.md
    - dist/zed/.agents/skills/setup/SKILL.md
    - dist/BUNDLE-VERSION.json
    - bin/mindrian-brain-mcp-client.cjs

key-decisions:
  - "Every literal occurrence of pws-brain-mcp.onrender.com in the six Task-1 files was replaced, including the CLI's own Stage 1/2 key-verification curl targets in commands/setup.md and the two retired-harness reason strings, per the plan's unconditional 'None of the six files contains pws-brain-mcp.onrender.com' acceptance bullet. The CLI's own Authorization-header-based key-verification flow was left untouched otherwise (that mechanism is unrelated to the Desktop/Cowork connector header being removed)."
  - "The two retired-harness reason strings (commands/pws-brain.md, skills/pws-brain/SKILL.md) were reworded to reference lib/core/brain-client.cjs's BRAIN_URL default by name rather than hardcoding either host, so they satisfy the grep requirement without asserting a URL that is not yet true mid-soak (brain-client.cjs itself is not touched by this PREP-cut plan)."
  - "brain_write's description was softened to 'the remote may refuse writes unconditionally regardless of key' rather than naming WRITE_PATH_DISABLED explicitly, per the plan's PREP-versus-FLIP instruction; the explicit sentinel name is deferred to the FLIP cut or the CHANGELOG."

requirements-completed: [FLIP-06]

duration: 22min
completed: 2026-09-04
---

# Phase 339 Plan 08: Theo Connector Docs + Backend-Agnostic Brain Tool Descriptions Summary

**Six Desktop/Cowork connector-instruction sources now point at Theo's `/mcp` endpoint under the unchanged `mindrian-brain` key with no auth header, their generated mirrors regenerated, and the Brain shim's five tool descriptions rewritten to name no specific backend, embedding model, or auth tier.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-09-04T05:11:00Z
- **Completed:** 2026-09-04T05:33:00Z
- **Tasks:** 3
- **Files modified:** 13 (6 hand-authored sources + 1 skill mirror + 4 dist mirror files + BUNDLE-VERSION.json + the Brain shim)

## Accomplishments

- Every Desktop/Cowork connector JSON example across `docs/brain-setup.md`, `docs/install/BRAIN-SETUP.md`, `commands/setup.md` now points at `https://theo-mcp.onrender.com/mcp` under the unchanged `mindrian-brain` connector key, with the `Authorization` header block removed and a plain-language explanation of why (Theo ignores it entirely).
- Each of the six sources carries the "the connector key names the plugin's Brain slot, not the server" sentence, cross-referencing `docs/339-NOTE-theo-desktop-connector-key.md`, plus a URL-asymmetry sentence (`/mcp` path for a direct connector vs. the bare origin `lib/core/brain-client.cjs` defaults to).
- The two identical retired-harness `reason:` strings in `commands/pws-brain.md` and `skills/pws-brain/SKILL.md` no longer hardcode the incumbent host as a second source of truth; both point to `lib/core/brain-client.cjs`'s `BRAIN_URL` default by name instead.
- `skills/setup/SKILL.md` and both `dist/` bundles were regenerated by their own generators (never hand-edited) and both `--check` / `--check-stale` gates report fresh.
- The Brain shim's five `brain_*` tool descriptions (`bin/mindrian-brain-mcp-client.cjs`) no longer claim Memgraph, multilingual-e5-large, or a write-tier gate a better key would open; the tool NAME set is byte-identical before and after (verified via a live `tools/list` probe).

## Task Commits

Each task was committed atomically:

1. **Task 1: Update Desktop/Cowork connector instructions (D-09)** - `be7966bf` (docs) — includes the Task-2 skill-mirror regen, forced into the same commit by this repo's pre-commit hook (see Deviations).
2. **Task 2: Regenerate `skills/setup/SKILL.md` and the dist bundles** - `226b16ed` (chore) — the dist-bundle half; the skill-mirror half landed in `be7966bf` above.
3. **Task 3: Make the Brain shim's tool descriptions backend-agnostic (D-13, PREP half)** - `5674d985` (fix)

**Plan metadata:** this file's own commit (docs, after STATE.md/ROADMAP.md updates)

## Files Created/Modified

- `docs/brain-setup.md` - Desktop connector JSON now targets Theo's `/mcp` endpoint, header removed, key-naming + URL-asymmetry sentences added
- `docs/install/BRAIN-SETUP.md` - historical pre-beta.20 `claude mcp add` example and its sentences updated to match
- `docs/THE-BRAIN.md` - "hosted at" sentence now names Theo, plus a new paragraph on the connector-key/URL-asymmetry mechanism
- `commands/setup.md` - two Desktop/Cowork connector JSON blocks, the Cowork prose line, and the Stage 1/2 CLI key-verification curl targets all updated
- `commands/pws-brain.md` / `skills/pws-brain/SKILL.md` - retired-harness `reason:` string and body blockquote reworded to stop hardcoding the incumbent host
- `skills/setup/SKILL.md` - regenerated by `node scripts/build-skill-mirrors.cjs` from the updated `commands/setup.md`
- `dist/generic-claude-dir/`, `dist/zed/`, `dist/BUNDLE-VERSION.json` - regenerated by `node scripts/build-dist-bundles.cjs` from the updated `skills/`
- `bin/mindrian-brain-mcp-client.cjs` - five tool descriptions rewritten backend-agnostic; a Phase-339 comment-as-contract block added above them

## Decisions Made

- Removed every occurrence of `pws-brain-mcp.onrender.com` from all six Task-1 files (including `commands/setup.md`'s CLI-side test-connection curl commands and both retired-harness reason strings), per the plan's unconditional acceptance bullet, rather than treating those as historically-frozen exceptions.
- Reworded the two retired-harness reason strings to reference `lib/core/brain-client.cjs`'s `BRAIN_URL` default by name instead of a literal URL, since that file is NOT touched by this PREP-cut plan and still points at the incumbent host during the soak; hardcoding `theo-mcp.onrender.com` there would have made a false claim about the current running default.
- Softened `brain_write`'s description to "the remote may refuse writes unconditionally regardless of key" rather than naming `WRITE_PATH_DISABLED`, per the plan's explicit PREP-versus-FLIP instruction. The explicit sentinel name is deferred to the FLIP cut or the CHANGELOG.

## Phase 276 TOOLHON-12 Collision Check (Task 3, run first)

Ran `node tests/test-276-theo-description-parity.cjs` before editing `bin/mindrian-brain-mcp-client.cjs`. Result: **zero collision.** TOOLHON-12 pins five DIFFERENT description constants — `ROOM_BIND_DESCRIPTION`, `GRAPH_WRITE_DESCRIPTION`, `GATE_RENDER_DESCRIPTION`, `GATE_ANSWER_DESCRIPTION`, `CHAIN_RUN_DESCRIPTION` — against `lib/mcp/tool-router.cjs` and `lib/mcp/tools/{graph,gate,chain}.cjs` (the `room_bind`, `graph_write`, `gate_render`, `gate_answer`, `chain_run` tools). None of these overlap the Brain shim's `brain_*` strings this plan edits. No pin needed updating; `bash tests/run-all-276.sh` still passes in full (`PASS=13 FAIL=0 SKIP=0`) after this plan's edits.

## Generator Commands and Check Outputs (Task 2, verbatim)

```
$ node scripts/build-skill-mirrors.cjs
build-skill-mirrors: created 0, unchanged 111, overwritten 1, skipped 1 (skip-list: trending-to-absurd) | sensor_triggers:[] desensitized 63, skill-spec normalized 112, plugin-root portable 92, pure byte copy 0

$ node scripts/build-skill-mirrors.cjs --check
build-skill-mirrors --check: OK (112 mirrors match expected content; skip-list verified: trending-to-absurd)

$ node scripts/build-dist-bundles.cjs
Wrote dist/generic-claude-dir/ (126 skills, nested .claude/skills/**)
  .mcp.json install root: /home/jsagi/.claude/plugins/cache/mindrian-marketplace/mos/2.0.0-beta.13 (source: installed_plugins.json)
Wrote dist/zed/ (126 skills, flat .agents/skills/**, 13240/51200 catalog bytes = 26%)
  2 subdirectory omission(s) across 1 skill(s), recorded in dist/zed/OMITTED-ASSETS.md
Wrote dist/BUNDLE-VERSION.json (source_version 2.0.0-beta.16, 126 skills, 13240 catalog bytes)
Wrote dist/README.md (no-auto-update statement + staleness check)

$ node scripts/build-dist-bundles.cjs --check-stale
dist bundle fresh: stale=false (source_version 2.0.0-beta.16)
```

Every changed line under `skills/setup/SKILL.md` and `dist/` traces 1:1 to Task 1's `commands/setup.md` and `skills/pws-brain/SKILL.md` edits (verified via `git diff`). The one unexplained generated change is `dist/BUNDLE-VERSION.json`'s `generated_at` timestamp field, which the generator stamps on every run regardless of content.

## Brain Shim `tools/list` Name Set, Before and After (Task 3)

Captured via a live stdio JSON-RPC `tools/list` probe against `bin/mindrian-brain-mcp-client.cjs`, both before and after the description edits:

```
["brain_ask","brain_query","brain_schema","brain_search","brain_stats","brain_write"]
```

Identical both times. No tool name changed; only the five `brain_ask`/`brain_schema`/`brain_search`/`brain_stats`/`brain_write` description strings were rewritten. `brain_query`'s description already carried no backend-specific claim and was left untouched.

## `brain_write` PREP-vs-FLIP Wording Decision (Task 3)

- **Before:** `"Write Cypher to the Brain. Admin-tier; requires a write-capable key. Generic methodology framework writes only (Canon Part 8)."` — implies a tier gate a better key would open.
- **After (this PREP cut):** `"Write Cypher to the Brain. The remote may refuse writes unconditionally regardless of key. Generic methodology framework writes only (Canon Part 8)."` — true of both the incumbent and Theo.
- **Deferred:** the explicit `WRITE_PATH_DISABLED` sentinel name is reserved for the FLIP cut plan or the CHANGELOG, per the plan's instruction, since naming it now would be premature (the incumbent backend does not return that sentinel).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Task 2's skill-mirror regen had to run before Task 1 could commit**
- **Found during:** Task 1's commit attempt
- **Issue:** This repo's git pre-commit hook (`.git/hooks/pre-commit`) runs `node scripts/build-skill-mirrors.cjs --check` and hard-blocks (`exit 2`) any commit that leaves `skills/setup/SKILL.md` stale relative to `commands/setup.md`. Committing Task 1's `commands/setup.md` edits alone (as the plan's task ordering implies) was rejected by the hook with `skill-mirror drift -- run: node scripts/build-skill-mirrors.cjs`.
- **Fix:** Ran `node scripts/build-skill-mirrors.cjs` (Task 2's first generator step) immediately, verified the resulting `skills/setup/SKILL.md` diff traced 1:1 to the `commands/setup.md` edits (`git diff`), and included it in Task 1's commit rather than leaving the commit blocked.
- **Files modified:** `skills/setup/SKILL.md` (included in commit `be7966bf` instead of a separate Task-2 commit).
- **Verification:** `node scripts/build-skill-mirrors.cjs --check` reports OK after the commit; `git diff skills/setup/SKILL.md` traced entirely to the `commands/setup.md` edit before staging.
- **Committed in:** `be7966bf` (Task 1 commit, documented inline in the commit message).

**2. [Rule 3 - Blocking] Own added prose accidentally re-introduced the word "Authorization"**
- **Found during:** Task 1's own verify block
- **Issue:** The new "no auth header needed" sentence added to `docs/brain-setup.md` used the literal word `Authorization`, tripping the task's own `! grep -q 'Authorization' docs/brain-setup.md` acceptance check.
- **Fix:** Reworded to "No auth header is needed" (same meaning, no literal token match).
- **Files modified:** `docs/brain-setup.md`.
- **Verification:** `grep -c 'Authorization' docs/brain-setup.md` returns 0; full Task 1 verify block passes.
- **Committed in:** `be7966bf` (Task 1 commit).

---

**Total deviations:** 2 auto-fixed (both Rule 3, blocking issues internal to this plan's own execution). No scope creep; both were necessary to make the plan's own tasks committable and pass their own verify blocks.

## Issues Encountered

None beyond the two deviations above.

## Known Stubs

None. No hardcoded empty values, placeholder text, or unwired data sources were introduced by this plan; every edit was a prose/description change to existing, already-wired connector docs and tool registrations.

## Threat Flags

None. This plan's edits stay within the threat register already declared in `339-08-PLAN.md`'s `<threat_model>` (T-339-02, T-339-06, T-339-23, T-339-24, T-339-25, T-339-SC), all dispositioned `mitigate` or `accept` there, and no new network endpoint, auth path, file-access pattern, or schema change at a trust boundary was introduced.

## Note on the Phase-Level `<verification>` Grep Scope

The plan's phase-level `<verification>` block asserts `grep -rn 'pws-brain-mcp.onrender.com' docs/ commands/ skills/ dist/` should return hits ONLY in dated handoffs, RCAs, autopsies, tester outbox files, and CHANGELOG history. A broader `docs/`-wide grep still surfaces the incumbent host in several files this plan's `<files_modified>` frontmatter does NOT list (e.g. `docs/ARCHITECTURE-DEEP-DIVE.md`, `docs/WORKFLOWS.md`, `docs/MINDRIAN-CANON.md`, `docs/IDEA-DOCUMENT.md`, `docs/AGENTIC-SURFACING-PATTERN.md`, plus several dated/RCA/handoff files that DO match the exemption). Those non-exempt files were out of this plan's declared scope (only the six files in Task 1's `<files>` list and `bin/mindrian-brain-mcp-client.cjs` in Task 3 were assigned here) and were left untouched per the scope-boundary rule; whether a later plan in this phase (e.g. the FLIP cut, 339-09 onward) is meant to sweep them is worth confirming before `339-11`'s `verify-release` run, but is not a defect in this plan's own completed tasks.

## Next Phase Readiness

- FLIP-06 is green for this plan's scope: connector docs name Theo's `/mcp` endpoint under the unchanged key with no header, both generated mirrors are fresh, and no Brain tool description claims a backend the plugin is about to move away from.
- `docs/339-NOTE-theo-desktop-connector-key.md`, cross-referenced from every edited source in this plan, is expected from plan 339-09 (not yet on disk as of this plan's completion) -- confirm it lands before shipping user-facing docs that cite it.
- The broader `docs/`-wide sweep noted above (architecture/reference docs outside this plan's six-file scope) is a candidate for a later plan or for explicit descoping before `339-11`'s `verify-release`.

---
*Phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig*
*Completed: 2026-09-04*

## Self-Check: PASSED

All 9 created/modified files verified present on disk; all 4 commits (`be7966bf`, `226b16ed`, `5674d985`, `0fe28d65`) verified present in `git log --oneline --all`.
