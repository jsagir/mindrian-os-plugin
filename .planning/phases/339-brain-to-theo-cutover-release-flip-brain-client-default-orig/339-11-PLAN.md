---
phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig
plan: 11
type: execute
wave: 4
depends_on: ["339-10"]
files_modified:
  - .claude-plugin/plugin.json
  - package.json
  - CHANGELOG.md
  - dist/BUNDLE-VERSION.json
autonomous: false
requirements: [FLIP-01, FLIP-02, FLIP-03, FLIP-04, FLIP-05, FLIP-06, FLIP-07, FLIP-08]
canon_parts: [7, 8, 11]

must_haves:
  truths:
    - "The PREP release is cut from a clean tree with Phase 276 paused at a plan boundary, confirmed by a human"
    - "All 309-plus local commits are on origin/main before any tag is created"
    - "The five-gate version lockstep runs through scripts/release.sh, never by hand"
    - "The PREP release is NOT gated by Theo's coverage ruling, because it is safe against both Brains by construction"
    - "The released artifact is verified on npm, on the tag, in the marketplace pin, and in the install cache"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "The [Unreleased] heading rewritten by release.sh Step 6 to the released version and date"
    - path: ".claude-plugin/plugin.json"
      provides: "Version bumped by release.sh Step 3, then pre-bumped by Commit B"
  key_links:
    - from: "scripts/release.sh"
      to: "~/mindrian-marketplace/.claude-plugin/marketplace.json"
      via: "Step 4 bumps the version and pins source.ref to the new tag; Step 9 pushes it"
      pattern: "marketplace.json"
    - from: "scripts/release.sh"
      to: "npm @mindrian_os/cli"
      via: "Step 9.5, dist-tag next then promoted to latest"
      pattern: "mindrian_os"
---

<objective>
The PREP CUT. Everything in plans 339-01 through 339-10 ships as one human-held release.

This release is safe against BOTH Brains by construction (D-01), which is why it is NOT gated by
Theo's coverage ruling (D-07). Every adaptation it carries is provably unreachable by an incumbent
payload, the alias selector still selects the incumbent table because line 24 has not moved, and
the connector docs describe a change users make on their own schedule.

Version arithmetic, computed live and stated once: `scripts/release.sh` takes a bump MODE, never a
version literal (`CLAUDE.md:83` is stale on this point). It increments from `plugin.json`'s CURRENT
value, which is already `2.0.0-beta.16` because the previous release's Commit B pre-bumped it. This
cut runs `bash scripts/release.sh --prerelease` and is expected to produce
**v2.0.0-beta.17**. Its own Commit B then pre-bumps `plugin.json` to `2.0.0-beta.18`, which is why
the FLIP cut in plan 339-13 is expected to produce **v2.0.0-beta.19**. That pair is the choice;
`--no-next-bump` is NOT used on either cut.

Do not hard-code either number anywhere. Confirm with `--dry-run` first and read the shipped
version back from the tag.

Output: one released prerelease, verified on four surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md
@.claude/includes/release-process.md
@scripts/release.sh
</context>

<artifacts_this_phase_produces>
This plan creates no source symbol. It produces a RELEASE: a git tag, an npm publish of
`@mindrian_os/cli`, a marketplace pin, and a website `FALLBACK_VERSION` sync.

Files `scripts/release.sh` mutates on this plan's behalf, listed so the diff is expected rather
than surprising: `.claude-plugin/plugin.json`, `package.json`, `CHANGELOG.md`,
`~/mindrian-marketplace/.claude-plugin/marketplace.json`, and
`$HOME/mindrian-website/website/src/lib/version.ts`. None of them is hand-edited by any task here.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: BLOCKING GATE - confirm Phase 276 is paused at a plan boundary and the tree is clean of its churn</name>

  <read_first>
    - `git status --porcelain` in `/home/jsagi/dev/MindrianOS-Plugin` (READ ONLY in this task, never fix anything here)
    - `git log --oneline -5` and `git log origin/main..HEAD --oneline | wc -l`
    - `ls .planning/phases/276-*/276-*-SUMMARY.md` and `ls .planning/phases/276-*/276-*-PLAN.md`: which 276 plans have a SUMMARY and which do not. The pause point is AFTER a plan whose SUMMARY exists, never mid-plan
    - `339-RESEARCH.md` section `What "pause Phase 276 at a wave boundary" means operationally`, all four numbered conditions
    - `339-RESEARCH.md` `Release Mechanics` Step 2.5: `doctor --acceptance --pre-flight` is described in the dry-run preview as a clean-tree gate before any mutation, and Step 8 is an explicit dirty-repo guard. `verify-release` gate 12 is only a WARN, so a dirty tree does not block `verify-release`; treat a clean tree as REQUIRED regardless
    - `339-RESEARCH.md` Assumptions Log A4, A5 and A6: the npm credential, the website repo path and the `claude` CLI. All three are unverified and each can hard-abort a release mid-flight, AFTER Commit A and the tag
  </read_first>

  <action>
STOP. Do not run Task 2, 3 or 4 until a human confirms every item below.

Present this checklist and require an explicit confirmation for each. Quote the CURRENT observed
value for each item so the human can see what changed since this plan was written.

1. Phase 276 is PAUSED at a plan boundary, not mid-plan. Show the current `276-*-SUMMARY.md` list
   and the highest-numbered plan with a SUMMARY. As of 2026-09-03 the tree's HEAD commit was
   `feat(276-12): regenerate connector registries with claim_write (Task 3)`, so 276 had reached at
   least plan 12. Confirm with the 276 executor that no plan is mid-execution.
2. `git status --porcelain | grep -v '^??'` is EMPTY. As of 2026-09-03 it was NOT: one modified
   `scripts/__pycache__/compute-hsi.cpython-312.pyc` and six deleted persona fixtures under
   `tests/fixtures/sample-room-personas/personas/`. These must be resolved (committed as deletions,
   restored, or gitignored) BEFORE the cut. The `.pyc` in particular should be gitignored rather
   than committed; check whether `scripts/__pycache__/` is already tracked. Untracked `??` entries
   do not block, but say which ones exist so the human can decide.
3. The commits `main` is ahead of `origin/main` are known and intended. Show the count from
   `git log origin/main..HEAD --oneline | wc -l`. Research measured 309 on 2026-09-03. Task 2
   pushes them; this item only confirms the human expects that number.
4. `npm whoami` succeeds and the account can publish `@mindrian_os/cli`. Step 9.5 HALTS the release
   on a publish failure, after Commit A and the tag already exist.
5. `$HOME/mindrian-website/website` exists with an `origin` remote. Step 9.6b HARD ABORTS mid-release
   without it, and its recovery is a `gh repo clone`. If the human prefers to skip it, the decision
   is to pass `--no-website`, and that decision is recorded here, not improvised at Step 9.6b.
6. `claude plugin validate` is on PATH. `verify-release` gates 1, 2 and 5 depend on it.
7. `grep -qE '^## \[Unreleased\]' CHANGELOG.md` succeeds. Without that heading, `release.sh` Step 6
   issues an interactive `read -r REPLY` and opens `$EDITOR`, which hangs forever inside an agent
   task.

If ANY item fails, HALT and report which item failed. Do not partially execute. Do not "fix" the
dirty tree from inside this task: resolving another phase's working-tree state is that phase's
decision, not this gate's. Reporting the block IS the correct outcome on any run before the
preconditions are met.

Perform no repository writes in this task.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && echo "porcelain-tracked: $(git status --porcelain | grep -v '^??' | wc -l)"; echo "ahead: $(git log origin/main..HEAD --oneline | wc -l)"; ls .planning/phases/276-*/276-*-SUMMARY.md 2>/dev/null | tail -3; grep -cE '^## \[Unreleased\]' CHANGELOG.md; npm whoami 2>&1 | head -1; test -d "$HOME/mindrian-website/website" && echo "website: present" || echo "website: MISSING (decide --no-website)"; command -v claude >/dev/null && echo "claude CLI: present" || echo "claude CLI: MISSING"</automated>
  </verify>

  <acceptance_criteria>
    - A human explicitly confirmed all seven items, item by item, and the confirmation is recorded in the SUMMARY.
    - `git status --porcelain | grep -v '^??' | wc -l` returns 0, quoted in the SUMMARY.
    - The Phase 276 pause point is named in the SUMMARY by plan number and by the SUMMARY file that proves it.
    - The `npm whoami` result, the website-directory decision and the `claude` CLI presence are each recorded, not assumed.
    - `grep -cE '^## \[Unreleased\]' CHANGELOG.md` returns at least 1.
    - Zero files were written by this task: `git status --porcelain` is byte-identical before and after it.
    - If the gate did NOT clear, the SUMMARY records which item failed and Tasks 2 through 4 are left untouched. That is a successful outcome for this task, not a failure.
  </acceptance_criteria>

  <resume-signal>Type "tree clean" plus the `git status --porcelain | grep -v '^??' | wc -l` output and the Phase 276 pause point, or "still dirty" plus which item failed.</resume-signal>

  <done>Either all seven preconditions are confirmed by a human and Tasks 2 through 4 may proceed, or the block is recorded with its failing item and the plan halts.</done>
</task>

<task type="auto">
  <name>Task 2: Push, run the suites, and clear every release gate before any mutation</name>

  <files>none (read-only checks plus one push)</files>

  <read_first>
    - `339-VALIDATION.md` `Test Infrastructure` and `Sampling Rate`: the pre-cut gate is the phase suite green, then `verify-release` with zero FAIL, then `doctor --acceptance`
    - `339-RESEARCH.md` `scripts/verify-release gates, and what actually blocks`: gate 10b (skill mirrors) FAILS on drift, gate 10f (script-invocation anchoring) FAILS zero-tolerance, gate 12 (git state) is WARN only, gate 13 (CHANGELOG has current version) is WARN only and is EXPECTED to warn today, gate 19 (Brain tool liveness) is offline and hermetic so the flip cannot break it
    - `339-RESEARCH.md` `Release Mechanics` Step ordering, especially Step 2 (`verify-release`), Step 2.4 (the three coverage gates) and Step 2.5 (`doctor --acceptance --pre-flight`, the clean-tree gate before any mutation)
    - `scripts/release.sh` usage at `:88`: the modes and flags, and `--dry-run` at `:173-180` which short-circuits after the version arithmetic with no mutation
  </read_first>

  <action>
Run, in this exact order, and record every output verbatim in the SUMMARY. Stop at the first
failure and report it; do not proceed to Task 3 with any FAIL outstanding.

1. `git push origin main`. This lands the commits `main` is ahead of `origin/main` (D-02, navigator
   directive). Do NOT push tags here; `release.sh` Step 9 does that.
2. `bash tests/run-all-339.sh`. Every arm must be GREEN. The wave-1 red-by-design arms
   (`test-339-origin-single-source.cjs`, `test-254` Arms 4-5, `test-250`'s update-path pin) were all
   turned green by plans 339-04 through 339-07. If any is still red, a wave-2 plan did not land and
   this cut must not proceed.
3. `bash tests/run-all-250.sh` and `bash tests/run-all-252.sh`.
4. `node tests/test-254-normalize-roundtrip-probe.cjs`,
   `node tests/test-245-skill-frontmatter-inert-keys.cjs`,
   `node tests/test-247-contract-client.cjs`, `node tests/test-brain-response-sanitize.cjs`,
   `node tests/test-265-mcp-description-hygiene.cjs`, `node tests/test-257-shim-honest-refusal.cjs`,
   `bash tests/test-127-00-shim-handshake.sh`,
   `node lib/core/doctor/class-m-brain-smoke.test.cjs`.
5. `bash tests/run-all-276.sh`. Phase 276 shares this working tree; a cut that breaks its suite is a
   cut that breaks another phase's work.
6. `node scripts/build-connector-registry.cjs --check`,
   `node scripts/build-orchestration-projection.cjs --check`,
   `node scripts/check-render-coverage.cjs`, `node scripts/check-shape-declaration.cjs`,
   `node scripts/build-skill-mirrors.cjs --check`,
   `node scripts/build-dist-bundles.cjs --check-stale`,
   `node scripts/check-plugin-path-anchoring.cjs --check-scripts`.
7. `bash scripts/verify-release`. ZERO FAIL required. Gate 12 (git state) and gate 13 (CHANGELOG
   has current version) are WARN-only and gate 13 is EXPECTED to warn, because `plugin.json` reads
   `2.0.0-beta.16` while the CHANGELOG heading names beta.17. Record every WARN so the human can
   see which were expected.
8. `node scripts/doctor.cjs --acceptance`.
9. `bash scripts/release.sh --prerelease --dry-run`. This short-circuits after the version
   arithmetic with NO mutation. Record the computed version verbatim. It is expected to be
   `2.0.0-beta.17`. If it is not, STOP and report: the version arithmetic moved and every downstream
   expectation in this phase needs re-reading before a cut.

Perform no version bump by hand. Do not edit `.claude-plugin/plugin.json`, `package.json`,
`CHANGELOG.md` or `marketplace.json` in this task.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && bash tests/run-all-339.sh && bash scripts/verify-release && node scripts/doctor.cjs --acceptance && bash scripts/release.sh --prerelease --dry-run</automated>
  </verify>

  <acceptance_criteria>
    - `git log origin/main..HEAD --oneline | wc -l` returns 0 after the push.
    - `bash tests/run-all-339.sh` exits 0 with every arm green and zero skipped arms that should have run.
    - `bash scripts/verify-release` reports ZERO FAIL. Every WARN is recorded in the SUMMARY with a note on whether it was expected.
    - `node scripts/doctor.cjs --acceptance` exits 0.
    - `bash tests/run-all-276.sh` exits 0.
    - The `--dry-run` computed version is recorded verbatim and equals `2.0.0-beta.17`. If it does not, the task HALTED and reported rather than proceeding.
    - No version file was hand-edited: `git diff .claude-plugin/plugin.json package.json` is empty.
  </acceptance_criteria>

  <done>Every gate the release train will run has already been run and passed, and the version arithmetic is confirmed rather than assumed, so Task 3's human-held invocation has no surprises left in it.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: HUMAN-HELD RELEASE - run scripts/release.sh --prerelease for the PREP cut</name>

  <read_first>
    - `339-RESEARCH.md` `Release Mechanics`, the full ordered step table: Steps 0 through 11, what each does and what its failure mode is. Steps 6.7, 7, 9.5, 7.5, 9, 9.6b, 9.7, 9.8, 10 and 11 all run inside this single invocation
    - `339-RESEARCH.md` `Pitfall 10`: Step 6's interactive prompt hazard, already precluded by Task 1 item 7
    - `339-RESEARCH.md` `Pitfall 2`: never write the version into a document; read it back from the tag
    - `.claude/includes/release-process.md`: the five-gate version consistency rule
    - Task 2's SUMMARY record of the `--dry-run` computed version
  </read_first>

  <action>
STOP. This is the release. It is human-held by decision (D-01: both cuts are human-held).

Present this to the human and require an explicit go-ahead before invoking anything:

- The command is exactly: `bash scripts/release.sh --prerelease`
- The mode is `--prerelease`. It takes a bump MODE, not a version literal.
- The expected version is `v2.0.0-beta.17`, confirmed by Task 2's `--dry-run`.
- Commit B (Step 7.5) will then PRE-BUMP `plugin.json` to `2.0.0-beta.18` and rewrite the CHANGELOG
  heading to `[Unreleased] -- v2.0.0-beta.18`. That is why every released beta tag in this repo is
  odd, and it is why the FLIP cut in plan 339-13 is expected to produce beta.19. This is normal, not
  a mistake to correct.
- `--no-next-bump` is deliberately NOT used. The pair for this phase is beta.17 (PREP) and beta.19
  (FLIP).
- If Task 1 recorded a decision to skip the website sync, append `--no-website`. Otherwise do not.
- This publishes to npm as `@mindrian_os/cli`, dist-tag `next`, then promoted to `latest`.

After the human gives the go-ahead, run the command once. Do not re-run it on a partial failure
without reading the failure first: Steps 6.5 and 6.6 roll back `plugin.json`, `package.json` and
`CHANGELOG.md` on their own failures, but a failure AFTER Step 7 leaves a commit and a tag in place
and needs a human decision, not a retry.

Record the full output verbatim in the SUMMARY, and record the ACTUAL shipped version read back
from `git describe --tags --abbrev=0`, never from this plan's prose.

Never bump any version by hand, in this task or after it.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && git describe --tags --abbrev=0 && grep '"version"' .claude-plugin/plugin.json && grep -m1 '^## \[' CHANGELOG.md</automated>
  </verify>

  <acceptance_criteria>
    - A human explicitly approved the invocation, and the approval is recorded in the SUMMARY.
    - `bash scripts/release.sh --prerelease` completed without a HARD ABORT.
    - `git describe --tags --abbrev=0` returns the new tag, recorded verbatim. It is expected to be `v2.0.0-beta.17`.
    - `.claude-plugin/plugin.json` now reads the NEXT prerelease (expected `2.0.0-beta.18`) because Commit B pre-bumped it, and `CHANGELOG.md`'s top heading is the RELEASED version with its date, followed by a fresh `[Unreleased]` heading.
    - No version was bumped by hand anywhere: every version change traces to a `release:` commit made by the script.
    - If the run aborted, the SUMMARY records at which numbered step, what the abort message said, and whether a commit or tag was left behind. That is a recorded outcome, not a retry trigger.
  </acceptance_criteria>

  <resume-signal>Type "prep cut released" plus the tag from `git describe --tags --abbrev=0`, or "prep cut aborted" plus the step number and the abort message.</resume-signal>

  <done>The PREP release exists as a tag, an npm publish and a marketplace pin, or the abort is recorded with enough detail to decide the next move.</done>
</task>

<task type="auto">
  <name>Task 4: Post-release verification of the PREP cut on all four surfaces</name>

  <files>none (read-only verification)</files>

  <read_first>
    - `339-RESEARCH.md` `Release Mechanics` Steps 9.5, 9, 10 and 11: the npm publish and dist-tag promotion, the git push with tags, the marketplace catalog update, and the built-in post-release verification
    - `339-RESEARCH.md` `Runtime State Inventory`, the OS-registered-state row: the plugin install cache at `~/.claude/plugins/cache/mindrian-marketplace/mos/<version>/` contains a FULL copy of `lib/core/brain-client.cjs`, and it is moved ONLY by the two-command update path
    - `339-RESEARCH.md` `Pitfall 6`: verifying by reading the dev tree proves nothing about what users run
    - `339-RESEARCH.md` Assumptions Log A7: the installed cache on this machine was reported at `2.0.0-beta.13` by Session T and is UNVERIFIED. Check it, do not assume it
    - `.claude/includes/release-process.md` lines 19 to 30: the two-command update path
  </read_first>

  <action>
Verify the release landed on four surfaces. Record every observed value verbatim; a verification
that reports "looks right" without a quoted value is not a verification.

1. npm. `npm view @mindrian_os/cli versions --json | tail -20` and
   `npm view @mindrian_os/cli dist-tags`. The shipped version must be present, and it must carry
   both the `next` and `latest` dist-tags per Step 9.5's promotion. Note the package name is
   `@mindrian_os/cli`, not `@mindrian/os`.
2. The tag, at origin. `git ls-remote --tags origin | grep "$(git describe --tags --abbrev=0)"`.
   The tag must exist at `origin`, not only locally.
3. The marketplace pin. `grep -A3 '"version"' ~/mindrian-marketplace/.claude-plugin/marketplace.json`
   and confirm `source.ref` is pinned to the new tag. Confirm the marketplace repo is clean and
   pushed: `cd ~/mindrian-marketplace && git status --porcelain && git log origin/main..HEAD --oneline`.
4. The install cache. `ls ~/.claude/plugins/cache/mindrian-marketplace/mos/` to see which versions
   are present, then for the newest one:
   `grep '"version"' ~/.claude/plugins/cache/mindrian-marketplace/mos/<v>/.claude-plugin/plugin.json`
   and `grep -n 'BRAIN_URL' ~/.claude/plugins/cache/mindrian-marketplace/mos/<v>/lib/core/brain-client.cjs`.

On the cache: run the two-command update path first, `/plugin marketplace update` then
`claude plugin update mos@mindrian-marketplace`, then re-check. This is the
`feedback_dev_repo_fix_not_live_until_released` rule made mechanical: a `main` commit is not live
until released AND picked up.

For THIS cut, the expected cache state is important and specific: the origin in the cached
`brain-client.cjs` must still be the INCUMBENT. The PREP cut does not move line 24. If the cache
shows Theo, something shipped that should not have and the FLIP cut must not proceed until it is
explained.

Also confirm the PREP adaptations are present in the CACHE, not just in the dev tree: grep the
cached `lib/core/brain-client.cjs` for `THEO_ORIGINS` and `_schemaCacheOrigin`, and confirm
`lib/core/update-path.cjs` exists in the cache. That is the proof the release carried the phase's
work rather than just a version bump.

Finally, note in the SUMMARY whether `mindrian-os.com`'s `FALLBACK_VERSION` sync (Step 9.6b) ran or
was skipped with `--no-website`, and if it ran, that the deploy push succeeded.

WORKSPACE GUARD: read the install cache, never write to it. Every git operation in this task runs
in `/home/jsagi/dev/MindrianOS-Plugin` or `~/mindrian-marketplace`, never in
`~/.claude/plugins/*`.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && TAG=$(git describe --tags --abbrev=0) && echo "tag=$TAG" && git ls-remote --tags origin | grep -F "$TAG" && npm view @mindrian_os/cli dist-tags && grep -F "${TAG#v}" ~/mindrian-marketplace/.claude-plugin/marketplace.json && ls ~/.claude/plugins/cache/mindrian-marketplace/mos/</automated>
  </verify>

  <acceptance_criteria>
    - The shipped version appears in `npm view @mindrian_os/cli versions` and carries both the `next` and `latest` dist-tags. Both outputs quoted in the SUMMARY.
    - The tag exists at `origin`, proven by `git ls-remote --tags origin`.
    - `~/mindrian-marketplace/.claude-plugin/marketplace.json` carries the new version and its `source.ref` is pinned to the new tag; the marketplace repo is clean and pushed.
    - The install cache, AFTER the two-command update, carries the new version, and its `lib/core/brain-client.cjs` still names the INCUMBENT origin. Both values quoted.
    - The cached `lib/core/brain-client.cjs` contains `THEO_ORIGINS` and `_schemaCacheOrigin`, and `lib/core/update-path.cjs` exists in the cache.
    - Nothing was written under `~/.claude/plugins/`.
    - The `FALLBACK_VERSION` sync outcome (ran or skipped) is recorded.
  </acceptance_criteria>

  <done>The PREP release is verified where users actually get it, and the cache proves the release carried the phase's adaptations while leaving line 24 exactly where it was.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev working tree -> released artifact | a release publishes whatever is in the tree; another phase's partial work would ship with it |
| repo -> npm tarball | Step 9.5's payload gate is the only thing between the tarball and this repo's planning and docs directories |
| released artifact -> user machine | reached only by the two-command update; the source tree proves nothing about it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-04 | Tampering | a release cut from a dirty tree carrying Phase 276's partial work | mitigate | Task 1 is a blocking human gate requiring `git status --porcelain | grep -v '^??'` to be empty and Phase 276 to be paused at a plan boundary, plus `release.sh` Step 2.5's own clean-tree gate before any mutation |
| T-339-06 | Information Disclosure | the npm tarball carrying `.planning/`, `docs/`, `tests/` or `node_modules/` | mitigate | `release.sh` Step 9.5's payload gate refuses such a tarball and exits 1; it is run, never bypassed, and Task 4 verifies the published artifact |
| T-339-32 | Denial of Service | `release.sh` hard-aborting mid-flight at Step 9.5 or 9.6b, after Commit A and the tag already exist | mitigate | Task 1 items 4 and 5 check `npm whoami` and the website directory BEFORE the cut, and the `--no-website` decision is recorded in advance rather than improvised at the abort |
| T-339-30 | Denial of Service | Step 6 hanging on an interactive editor prompt | mitigate | Task 1 item 7 asserts the `## [Unreleased]` heading exists |
| T-339-33 | Repudiation | verifying the release by reading the dev tree rather than the install cache | mitigate | Task 4 runs the two-command update and greps the CACHED `brain-client.cjs`, and asserts the cached origin is still the incumbent for this cut |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | this phase installs zero packages. `release.sh` Step 6.7 runs `npm ci --omit=dev` to vendor production `node_modules` from the existing `package-lock.json`; that is a lockfile-pinned reinstall of already-audited dependencies, not a new package addition, and no new package name enters the tree |
</threat_model>

<verification>
- `git log origin/main..HEAD --oneline | wc -l` returns 0 both before the cut (after Task 2's push) and after it (after Step 9's push).
- `bash tests/run-all-339.sh`, `bash scripts/verify-release` and `node scripts/doctor.cjs --acceptance` all passed before any mutation.
- `git describe --tags --abbrev=0` returns the new tag and it exists at `origin`.
- `npm view @mindrian_os/cli dist-tags` shows the new version on both `next` and `latest`.
- The install cache, after the two-command update, carries the new version AND still names the incumbent origin.
- Nothing under `~/.claude/plugins/` was written.
</verification>

<success_criteria>
- FLIP-01 through FLIP-08 are shipped in a released artifact, not just committed.
- The tree was clean and Phase 276 was paused, confirmed by a human before the cut.
- The version came from `release.sh`'s arithmetic and was read back from the tag, never hard-coded.
- The cache proves the PREP cut carried the adaptations and did NOT move line 24.
</success_criteria>

<output>
Create `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-11-SUMMARY.md` when done. Record: the shipped tag verbatim, the `plugin.json` version after Commit B, every `verify-release` WARN with whether it was expected, the npm dist-tags output, and the cached `BRAIN_URL` line proving line 24 did not move.
</output>
