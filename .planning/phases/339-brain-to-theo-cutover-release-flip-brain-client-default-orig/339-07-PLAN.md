---
phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig
plan: 07
type: execute
wave: 2
depends_on: ["339-01", "339-02", "339-03"]
files_modified:
  - scripts/probe-brain-contract.cjs
  - scripts/build-brain-census.cjs
  - lib/core/mcp-profiles.cjs
  - scripts/rs-experts-command.cjs
  - scripts/rs-thesis-command.cjs
  - scripts/sessionstart-post-update-preflight.cjs
  - scripts/session-start
  - .env.brain.template
  - lib/core/doctor/class-m-brain-smoke.cjs
  - lib/core/doctor/class-m-brain-smoke.test.cjs
  - tests/fixtures/246-census-fixture.json
autonomous: true
requirements: [FLIP-01, FLIP-11]
canon_parts: [7, 8]

must_haves:
  truths:
    - "No runtime site outside brain-client.cjs and the doctor's canon constant resolves the Brain origin from its own literal"
    - "scripts/probe-brain-contract.cjs tests the origin the plugin actually ships, so it cannot silently probe the incumbent after the flip"
    - "No user-visible session banner names a host that the release moved away from"
    - "Doctor's class M layer 6 reads a node count from BOTH the incumbent's totalRecordCount and Theo's nodes, and the dual read is provably inert against the incumbent because totalRecordCount is still recognized first"
    - "The doctor smoke suite's injected mock bags derive the origin from the module's exported canon constant instead of typing a host, so the FLIP cut moves one constant and the suite follows with no test edit"
    - "The below-floor test arm sits below BOTH the incumbent floor and the Theo floor the FLIP cut will add, so it cannot silently invert into a pass"
    - "No test fixture in this repo names either Brain's host: the fixture asserts census SHAPE, and the generator resolves its brain_url field through getBrainUrl()"
  artifacts:
    - path: "scripts/probe-brain-contract.cjs"
      provides: "Origin resolved through getBrainUrl(), env override still honored by that resolver"
      contains: "getBrainUrl"
    - path: "scripts/build-brain-census.cjs"
      provides: "Origin resolved through getBrainUrl(), removing the self-declared mirror of line 24"
      contains: "getBrainUrl"
    - path: "lib/core/doctor/class-m-brain-smoke.cjs"
      provides: "Dual-shape node count read plus an exported CANON_BRAIN_URL, both additive and inert against the incumbent"
      contains: "statsResult.nodes"
    - path: "lib/core/doctor/class-m-brain-smoke.test.cjs"
      provides: "Mock bags parameterized off the exported canon origin, a Theo-shaped stats arm, a neither-field arm, and a below-floor arm below both floors"
      contains: "CANON_BRAIN_URL"
    - path: "tests/fixtures/246-census-fixture.json"
      provides: "Origin-neutral brain_url fields, so the fixture pins census shape and never a host"
  key_links:
    - from: "scripts/probe-brain-contract.cjs"
      to: "lib/core/brain-client.cjs getBrainUrl()"
      via: "require, replacing its own BRAIN_URL const"
      pattern: "getBrainUrl"
    - from: "scripts/build-brain-census.cjs"
      to: "lib/core/brain-client.cjs getBrainUrl()"
      via: "require, replacing the const whose own comment admitted it mirrors line 24"
      pattern: "getBrainUrl"
    - from: "lib/core/doctor/class-m-brain-smoke.test.cjs"
      to: "lib/core/doctor/class-m-brain-smoke.cjs CANON_BRAIN_URL"
      via: "the newly exported constant, so every mock origin follows the FLIP cut with no test edit"
      pattern: "CANON_BRAIN_URL"
    - from: "lib/core/doctor/class-m-brain-smoke.cjs layer 6 node count read"
      to: "Theo brain_stats {nodes, relationships, labels}"
      via: "one adjacent dual-shape block that still recognizes totalRecordCount first"
      pattern: "statsResult.nodes"
---

<objective>
The literal sweep (D-12, FLIP-01) plus the doctor and fixture PREP half (D-12b, FLIP-11). Two
runtime sites stop declaring their own origin and start asking the one place that decides it. Six
prose sites stop naming a host they are about to be wrong about. And three files that would
otherwise have been dragged into the human-held FLIP commit are made correct against BOTH origins
here, ahead of the flip, so the flip stays the five files D-13 locks it to.

This is not tidying. `scripts/build-brain-census.cjs:59` documents itself as mirroring
`lib/core/brain-client.cjs` line 24, which is a second source of truth admitting in writing to
being one. `scripts/probe-brain-contract.cjs:74` is worse: post-flip it would silently probe the
INCUMBENT while reporting on "the Brain", which is exactly the class of false-success this repo has
spent phases closing.

Deliberately OUT of this plan and named so nobody sweeps them by accident:
- `lib/core/brain-client.cjs:24` and its `:4-7` docblock. The FLIP cut owns them (339-12).
- The CONSTANT VALUES at `lib/core/doctor/class-m-brain-smoke.cjs:76-78`. `CANON_BRAIN_URL`
  answers "is the resolved endpoint the canon one", so deriving it from `getBrainUrl()` would make
  the comparison always true and destroy the check. The FLIP cut moves those VALUES in the same
  commit as line 24 (339-12, D-13), together with the per-origin node floor, whose Theo value is a
  value in exactly the same sense. Task 3 below ships the MECHANISM that makes those moves a
  constants-only edit, and it changes no value.
- `lib/core/enrichment-queue.cjs:465`. Plan 339-05 rewrites that comment as part of FLIP-03.
- `tests/test-245-skill-frontmatter-inert-keys.cjs`. FLIP cut (339-12): its hard equality assert
  is a pinned VALUE that must move in the same commit as line 24, and that pairing is the whole
  point of the tripwire.
- `data/brain-census.generated.json` and `docs/BRAIN-GRAPH-CENSUS.generated.md`. LEAVE, generated,
  reasons recorded below.
- Every dated handoff, RCA, autopsy, tester outbox file, CHANGELOG history entry and
  `.planning/debug/*` under `docs/`. Historical records. LEAVE.

Task 3 is the reason this plan is not just tidying. Three files would otherwise have had to change
inside the human-held FLIP commit, widening it past the five files D-13 locks and weakening the
one-line-rollback rationale the whole two-cut shape exists to protect: the doctor smoke layer's
dual-shape stats read, the doctor smoke TEST file's hard-coded mock origins, and the census
fixture's `brain_url` fields. None of the three carries a VALUE that has to move with the origin, so
all three belong here, in the PREP cut, written to be correct against BOTH Brains. What is left for
the flip is exactly what D-13 names: constants, one test pin, `CLAUDE.md`, line 24 and the
CHANGELOG.

The test file deserves the sharpest statement of the risk, because it is the one that would fail
SILENTLY. Its mock bags are INJECTED, so they never touch the live default and they would stay
green through the flip on their own. That is the danger: once `CANON_BRAIN_URL` moves, a bag still
returning the incumbent exercises the NOT-canon early-exit branch instead of the canon branch, and
the suite keeps printing PASSED while proving something different from what it claims. The fix is
to stop typing the host in the test at all and derive it from the module under test.

Output: eleven files changed, `tests/test-339-origin-single-source.cjs` turned green, and the FLIP
cut's file set reduced to the five D-13 locks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-PATTERNS.md
@lib/core/brain-client.cjs
</context>

<artifacts_this_phase_produces>
This plan creates NO new constants and no new modules. It removes two symbols and newly EXPORTS
one that already existed:

| Symbol | Kind | File | Change |
|--------|------|------|--------|
| `BRAIN_URL` (module-local) | const | `scripts/probe-brain-contract.cjs` | REMOVED, replaced by `getBrainUrl()` |
| `BRAIN_URL` (module-local) | const | `scripts/build-brain-census.cjs` | REMOVED, replaced by `getBrainUrl()` |
| `CANON_BRAIN_URL` | const | `lib/core/doctor/class-m-brain-smoke.cjs` | now added to `module.exports` at `:424`; its VALUE is untouched |

Symbols this plan CONSUMES that were created earlier in the phase: none. `getBrainUrl()` has
existed since before this phase (`lib/core/brain-client.cjs:1163`).

Symbols this plan deliberately does NOT create, so nobody adds them here by accident:
`THEO_NODE_FLOOR` and the per-origin floor selector. Both need `THEO_ORIGINS`, which plan 339-04
exports in this SAME wave and is therefore not safely available to this plan, and both are VALUES
that D-13 binds to the flip commit. Plan 339-12 owns them.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Point the two runtime origin sites at getBrainUrl() (FLIP-01)</name>

  <files>scripts/probe-brain-contract.cjs, scripts/build-brain-census.cjs</files>

  <behavior>
    - `tests/test-339-origin-single-source.cjs`, red at the end of wave 1, goes GREEN.
    - Neither script declares its own origin literal on a non-comment line.
    - Both resolve the origin by requiring `lib/core/brain-client.cjs` and calling `getBrainUrl()`.
    - A `MINDRIAN_BRAIN_URL` override still works identically in both scripts, because that override is honored inside `getBrainUrl()`'s own resolution.
    - Both scripts still run to completion with their existing CLI arguments and produce the same output shape as before.
  </behavior>

  <read_first>
    - `scripts/probe-brain-contract.cjs` lines 30 to 90: the header enumerating its five legs, and the `BRAIN_URL` const at `:74`
    - `scripts/build-brain-census.cjs` lines 24 to 65: the header stating it has NO `--check` release gate by design because a release gate must never depend on live network, the Lane A / Lane B split and the admin-key requirement at `:36-39`, the `MD_PATH` and `JSON_PATH` at `:56-57`, the `GENERATED_NOTE` at `:59-60`, and the `BRAIN_URL` const at `:61` whose own comment says it "mirrors lib/core/brain-client.cjs line 24"
    - `lib/core/brain-client.cjs:1155-1176`: `getBrainUrl()` and the docblock stating the one-place-decides rule, including that the env override is resolved inside `BRAIN_URL` itself
    - `339-RESEARCH.md` section `Runtime sites, exact`: the disposition column for each of these two files
    - `339-RESEARCH.md` `Don't Hand-Roll` first row: do not build a second `const BRAIN_URL = process.env.MINDRIAN_BRAIN_URL || '...'` anywhere
    - `339-PATTERNS.md` `Shared Patterns` section `Single source of truth for the origin (D-12)`
  </read_first>

  <action>
In BOTH scripts, delete the module-local `BRAIN_URL` const and replace it with a resolution
through the shared resolver. Use a relative require to `lib/core/brain-client.cjs` matching each
script's own existing require style and depth, then call `getBrainUrl()`.

Do NOT re-implement the env override. `getBrainUrl()` returns `BRAIN_URL`, which is already
`process.env.MINDRIAN_BRAIN_URL || <default>`. Adding a second `process.env` read here would
reintroduce exactly the duplication this task removes. Verify behaviorally: run each script once
with `MINDRIAN_BRAIN_URL` set to a throwaway value and confirm the script reports that value.

In `scripts/build-brain-census.cjs`, DELETE the comment at `:59-60` that says the const "mirrors
lib/core/brain-client.cjs line 24". It is no longer true and it was the written confession of the
second source of truth. Replace it with a one-line comment naming phase 339 and 2026-09-03 and
stating that the origin is resolved through `getBrainUrl()` so there is nothing left to mirror.

In `scripts/probe-brain-contract.cjs`, add a comment-as-contract block at the new resolution site
naming: phase id 339, date 2026-09-03, the observed risk it closes (post-flip this probe would
otherwise silently test the INCUMBENT while its output claims to describe "the Brain", and its
five legs would then all report on the wrong service), and what it deliberately does NOT change
(the five legs themselves, their assertions, and the expected-red status of leg e pending the
seven operator index DROPs; plan 339-14 records which legs invert against Theo and those
inversions are EXPECTED, not failures).

Both files are executed scripts, not libraries. Confirm each still runs: `probe-brain-contract.cjs`
performs live network calls, so run it only far enough to prove the origin resolves and the
process starts, or run it fully if the network is available and record the output verbatim.
`build-brain-census.cjs` requires live network for Lane A and an admin key for Lane B; do NOT run
a full census in this task. Proving the origin resolves is enough, and re-running the census is
explicitly out of scope (see Task 2's disposition note).
  </action>

  <verify>
    <automated>node tests/test-339-origin-single-source.cjs && ! grep -n "onrender.com" scripts/probe-brain-contract.cjs | grep -v '^\s*[0-9]*:\s*[/*#]' | grep -q . && grep -Fq 'getBrainUrl' scripts/probe-brain-contract.cjs && grep -Fq 'getBrainUrl' scripts/build-brain-census.cjs && MINDRIAN_BRAIN_URL=https://example.invalid node -e "console.log(require('./lib/core/brain-client.cjs').getBrainUrl())" | grep -Fq 'https://example.invalid' && echo SWEEP_OK</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-339-origin-single-source.cjs` exits 0. This is the wave-2 flip from red to green that plan 339-01 predicted.
    - Neither script contains a `process.env.MINDRIAN_BRAIN_URL` read of its own.
    - Each script, run with `MINDRIAN_BRAIN_URL` set to a throwaway value, reports that value. Record both observed outputs in the SUMMARY.
    - The `mirrors lib/core/brain-client.cjs line 24` comment is gone from `scripts/build-brain-census.cjs`.
    - `scripts/probe-brain-contract.cjs`'s five legs and their assertions are byte-identical: `git diff` shows hunks only around the origin resolution and its comment.
    - `grep -c` for the em-dash character returns 0 in both files.
  </acceptance_criteria>

  <done>FLIP-01's runtime half is closed. The probe cannot silently test the wrong service after the flip, and the census generator no longer documents itself as a duplicate authority.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the comment-level and user-visible prose sites</name>

  <files>lib/core/mcp-profiles.cjs, scripts/rs-experts-command.cjs, scripts/rs-thesis-command.cjs, scripts/sessionstart-post-update-preflight.cjs, scripts/session-start, .env.brain.template</files>

  <read_first>
    - `lib/core/mcp-profiles.cjs` around line 22: the comment reference
    - `scripts/rs-experts-command.cjs` around line 10 and `scripts/rs-thesis-command.cjs` around line 10: the comment references
    - `scripts/sessionstart-post-update-preflight.cjs` around line 38: the comment reference, plus the file's own defensive rule (never block the hook chain on internal error, exit 0 silently when the state file is absent) which must not be disturbed
    - `scripts/session-start` around line 1896: `echo "Brain: HTTP client active (pws-brain-mcp.onrender.com)"` and the surrounding comment at `:1889-1891` describing it as prose. This is a user-visible banner printed on EVERY session start, and it is NOT in CONTEXT D-12's list; research added it
    - `.env.brain.template` around line 9: the incumbent origin in a comment
    - `339-RESEARCH.md` section `Runtime sites, exact`, the `scripts/session-start:1896` row and its recommendation
    - `339-RESEARCH.md` section `Generated artifacts: LEAVE, and say why in writing`: the three reasons re-running the census is wrong for this phase
  </read_first>

  <action>
Six prose edits. None changes behavior; all six change what a reader or a user is told.

`lib/core/mcp-profiles.cjs`, `scripts/rs-experts-command.cjs`, `scripts/rs-thesis-command.cjs`,
`scripts/sessionstart-post-update-preflight.cjs`: rewrite each comment so it names the RESOLVER
rather than a host. Say that the Brain origin is whatever `lib/core/brain-client.cjs`'s
`getBrainUrl()` resolves, and drop the hard-coded hostname. Where the surrounding prose needs a
concrete example, say "the origin resolved by `getBrainUrl()`" rather than naming either host.
Add the phase attribution: phase 339, 2026-09-03.

`scripts/session-start` line 1896: drop the host from the banner entirely. Change
`Brain: HTTP client active (pws-brain-mcp.onrender.com)` to `Brain: HTTP client active`. Do NOT
shell out to node to resolve the origin for a banner string: this is a bash script on the session
start path, and adding a node spawn to print a hostname would cost startup latency on every
session for a cosmetic gain. Add a one-line comment naming phase 339 and 2026-09-03 and stating
the reason the host was dropped rather than derived: the banner is prose a user reads on every
session start, so a wrong host there is a lie repeated constantly, and the origin already has one
authoritative reporter (`node scripts/doctor.cjs`, class M layer 6).

`.env.brain.template` line 9: rewrite the comment so it explains what `MINDRIAN_BRAIN_URL` is for
rather than naming today's default. Two facts must be stated, both security-relevant per the
research Security Domain V14 row: the value must be a BARE ORIGIN with no path and no trailing
slash (the client appends `/mcp` and `/register` itself; `/mcp` produces `/mcp/mcp` and a trailing
slash a double slash, both 404, and the client renders a 404 as "Brain unreachable" rather than a
config error), and this variable is the per-install ROLLBACK lever for an origin change, valid
only while the previous origin is still running.

Finally, record the GENERATED-ARTIFACT DISPOSITION. Do NOT edit
`data/brain-census.generated.json` or `docs/BRAIN-GRAPH-CENSUS.generated.md`, and do NOT re-run
`scripts/build-brain-census.cjs`. Three reasons, which must be written into this plan's SUMMARY so
plan 339-10 can carry them into the CHANGELOG: (1) the generator's own header states it has NO
`--check` release gate by design because a release gate must never depend on live network, and
`grep build-brain-census scripts/verify-release` returns nothing, so a stale census blocks no
gate; (2) Lane B requires an operator-supplied ADMIN key, and Theo has no admin key because
`brain_write` returns `WRITE_PATH_DISABLED` unconditionally; (3) re-running Lane A against Theo
would replace a census of the incumbent's 29,200-node graph with a census of Theo's 1,253-node
graph, silently discarding the record the 2026-08 phases were measured against. Register
"re-census against Theo" as a deferred item in the SUMMARY.
  </action>

  <verify>
    <automated>! grep -q 'pws-brain-mcp.onrender.com' scripts/session-start && ! grep -q 'pws-brain-mcp.onrender.com' .env.brain.template && ! grep -q 'pws-brain-mcp.onrender.com' lib/core/mcp-profiles.cjs && ! grep -q 'pws-brain-mcp.onrender.com' scripts/rs-experts-command.cjs && ! grep -q 'pws-brain-mcp.onrender.com' scripts/rs-thesis-command.cjs && ! grep -q 'pws-brain-mcp.onrender.com' scripts/sessionstart-post-update-preflight.cjs && git diff --stat data/brain-census.generated.json docs/BRAIN-GRAPH-CENSUS.generated.md | wc -l | grep -q '^0$' && echo PROSE_OK</automated>
  </verify>

  <acceptance_criteria>
    - None of the six files contains `pws-brain-mcp.onrender.com` anywhere, including comments.
    - `scripts/session-start`'s banner reads `Brain: HTTP client active` with no host, and carries the one-line reason comment.
    - `.env.brain.template` states both the bare-origin rule and the rollback-lever role.
    - `data/brain-census.generated.json` and `docs/BRAIN-GRAPH-CENSUS.generated.md` are UNCHANGED: `git diff` on both is empty.
    - No behavior changed: `bash tests/run-all-339.sh` and `node scripts/doctor.cjs --acceptance` produce the same pass/fail profile as before this task, and `scripts/sessionstart-post-update-preflight.cjs` still exits 0 when its state file is absent.
    - The SUMMARY records the three-reason generated-artifact disposition verbatim and registers the re-census as deferred.
    - `grep -c` for the em-dash character returns 0 in all six files.
  </acceptance_criteria>

  <done>No prose in this repo's runtime tree tells a reader or a user the wrong host, and the deliberate decision to leave the census artifacts stale is recorded with its three reasons rather than looking like an oversight.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the doctor smoke layer, its test file and the census fixture correct against BOTH origins (FLIP-11, D-12b prep half)</name>

  <files>lib/core/doctor/class-m-brain-smoke.cjs, lib/core/doctor/class-m-brain-smoke.test.cjs, tests/fixtures/246-census-fixture.json</files>

  <behavior>
    - `node lib/core/doctor/class-m-brain-smoke.test.cjs` passes NOW, against the incumbent default, and passes again AFTER plan 339-12 moves `CANON_BRAIN_URL` and adds the Theo floor, with NO further edit to the test file.
    - Layer 6 reads a node count from BOTH `statsResult.totalRecordCount` and `statsResult.nodes`, in ONE adjacent block, each guarded on `typeof === 'number'` and `Number.isFinite`.
    - Against the incumbent the layer's behavior is byte-identical to today: `totalRecordCount` is recognized FIRST, so the currently shipping payload takes exactly the branch it takes now.
    - A payload carrying NEITHER field still fails honestly, and the reason names both field names instead of only `totalRecordCount`.
    - `CANON_BRAIN_URL` is EXPORTED, and every mock bag in the test file derives its origin from that export rather than typing a host.
    - A Theo-shaped stats arm (`{nodes, relationships, labels}`) passes, and an incumbent-shaped arm (`totalRecordCount`) still passes, so the dual read is proven in BOTH directions rather than retargeted.
    - The below-floor arm's count sits below BOTH the incumbent floor (29000) and the Theo floor plan 339-12 will add (1000), so it fails on both sides of the flip.
    - `tests/fixtures/246-census-fixture.json` names neither Brain's host, and `node tests/test-246-census-render.cjs` still passes.
    - VALUES ARE UNCHANGED: `CANON_BRAIN_URL` still resolves to the incumbent, `CANON_NODE_FLOOR` is still 29000, `STALE_REPLICA_NODE_COUNT` is still 28325. Those move in the flip commit (D-13) and nowhere else.
  </behavior>

  <read_first>
    - `lib/core/doctor/class-m-brain-smoke.cjs:62-79`: the L6 constants block with its per-constant failure-semantics comments. `CANON_BRAIN_URL` at `:76`, `CANON_NODE_FLOOR = 29000` at `:77`, `STALE_REPLICA_NODE_COUNT = 28325` at `:78`
    - `lib/core/doctor/class-m-brain-smoke.cjs:299-358` (`_layer6`): the injection seams `brainUrlFn` / `mockStats` / `mockQuery` at `:301-305`, the canon-versus-override gate at `:307-311`, the `statsResult.totalRecordCount` read at `:318`, the `brain_stats carried no usable totalRecordCount` reason immediately below it, the stale-replica check ordered BEFORE the generic floor, and the `GRAPHRAG_STAMP_CYPHER` silent degrade at `:340-352`
    - `lib/core/doctor/class-m-brain-smoke.cjs:424`: the single `module.exports` line, which today exports `checkBrainSmoke`, `LAYERS`, `fixBrainSmoke`, `STDIO_TIMEOUT_MS` and `STRUCTURED_REFUSAL_STATUSES` and does NOT export `CANON_BRAIN_URL`. That omission is why the test file types the host
    - `lib/core/doctor/class-m-brain-smoke.test.cjs:24-30`: the hermetic contract, NO real network IO and NO real spawn, every all-pass bag carrying `mockBrainUrl` / `mockStats` / `mockQuery` so L6 never reaches `lib/core/brain-client.cjs`
    - `lib/core/doctor/class-m-brain-smoke.test.cjs:70-78`: `L6_PASS_SEAMS`, the shared trio spread into every all-pass bag, with `mockBrainUrl: () => 'https://pws-brain-mcp.onrender.com'` and `mockStats: async () => ({ totalRecordCount: 30000 })`
    - `lib/core/doctor/class-m-brain-smoke.test.cjs:322-395` (Test 11): `passBase.mockBrainUrl` at `:333`, Case A's 28325 stale arm, Case B's below-floor arm which currently feeds `totalRecordCount: 1000`, Case C's pass arm which asserts `payload.endpoint` against the typed host, Case D's null-stats fail-closed arm, and Case E's stamp arm
    - `scripts/doctor.cjs:1400-1417`: the acceptance arm hard-fails on exactly two conditions, the `store_identity` layer being ABSENT or its reason matching `/stale_replica_signature/`. Any other not-ok is information only
    - `tests/fixtures/246-census-fixture.json` lines 6 and 173: the two `brain_url` fields
    - `tests/test-246-census-render.cjs`: the consumer. It asserts census SHAPE (both variants present, the scanned frontmatter count, the gap table) and never asserts a host
    - `scripts/build-brain-census.cjs:469` and `:860`: `brain_url` is rendered as one table cell and populated from the module origin const, which Task 1 of this plan just repointed at `getBrainUrl()`
    - `339-CONTEXT.md` D-12b: the three ways layer 6 breaks, and the instruction to read both stats shapes and keep a per-origin floor rather than dropping the assertion
    - `339-CONTEXT.md` `D-13 corrected`: the FLIP cut's locked five-file set. This task exists so that set stays five
    - `339-RESEARCH.md` section `Consumer 5: lib/core/doctor/class-m-brain-smoke.cjs layer 6 breaks THREE ways`: the full table and the recommended dual-shape read
    - `339-RESEARCH.md` Assumptions Log A10: Theo's `brain_stats` field names were read from `brain-stats.ts:211-216` source, and the wire wrapping through `to-tool-result.ts` was NOT inspected. VERIFY WITH ONE LIVE READ-ONLY `brain_stats` CALL at execution time
    - `339-PATTERNS.md` section `MODIFIED lib/core/doctor/class-m-brain-smoke.cjs layer 6`
  </read_first>

  <action>
FIRST, discharge assumption A10, because the dual-shape read written below is shaped by the answer.
Make ONE live read-only `brain_stats` call against Theo and record the exact TOP-LEVEL shape of the
tool result in the SUMMARY. `brain_stats` is a READ, so this is safe; never call a write-shaped
endpoint during verification, which rules out `brain_write` and a deliberate `POST /register`. If
the payload is nested under a wrapper such as `content` or `structuredContent` rather than carrying
`nodes` at the top level, the guard below needs one more level of unwrapping. Record the observed
shape whichever way it lands, so plan 339-12 inherits a fact rather than an assumption.

THEN make three edits, none of which changes a VALUE.

Edit 1, `lib/core/doctor/class-m-brain-smoke.cjs`, the dual-shape node count read. Replace the
single `statsResult.totalRecordCount` read at `:318` with one adjacent block that recognizes
`totalRecordCount` FIRST and `nodes` SECOND, each guarded on `typeof === 'number'` and
`Number.isFinite`, falling through to the existing honest failure when neither is present. Order
matters and is the proof of inertness: the incumbent emits `totalRecordCount`, so it is recognized
before `nodes` is ever consulted and today's behavior cannot change. Adjacent recognition in one
block, not two separate branches, because the layer must work on either side of a rollback rather
than being retargeted from one shape to the other. Widen the failure reason from
`brain_stats carried no usable totalRecordCount` to name BOTH field names, so a future reader who
meets that reason learns what the layer actually looked for.

Then add `CANON_BRAIN_URL` to the `module.exports` object at `:424`. That is the whole export
change. Do NOT export the floors and do NOT export `STALE_REPLICA_NODE_COUNT`: nothing needs them,
and a wider export surface is a wider thing to keep true.

Do NOT touch `CANON_BRAIN_URL`'s value, `CANON_NODE_FLOOR`, `STALE_REPLICA_NODE_COUNT`, the
canon-versus-override gate, the stale-check ordering, or the `GRAPHRAG_STAMP_CYPHER` degrade. This
file keeps its own origin literal and stays the second allowlisted entry in
`tests/test-339-origin-single-source.cjs`, so re-run that test and confirm the allowlist is
unchanged.

Comment-as-contract at the node count read, extending the existing per-constant prose rather than
replacing it. Name: phase id 339, date 2026-09-03, the observed payload shapes (the incumbent emits
`totalRecordCount`; Theo's `brain_stats` returns `nodes`, `relationships`, `labels` and
`diagnostics` with no `totalRecordCount` at all, source
`/home/jsagi/Theo/src/mcp/content/brain-stats.ts:211-216`, wire shape confirmed live at execution
time by the A10 discharge above), and what this deliberately does NOT change: no constant VALUE
moves here, because the values are what the flip commit carries (D-13), and this read is written so
that when they move, only they move.

Edit 2, `lib/core/doctor/class-m-brain-smoke.test.cjs`, stop typing the host and cover both shapes.
Require the module's exported `CANON_BRAIN_URL` and use it wherever the origin is currently typed:
`L6_PASS_SEAMS.mockBrainUrl` at `:74`, `passBase.mockBrainUrl` at `:333`, and Case C's
`payload.endpoint` assertion. After this edit the string `onrender.com` must not appear anywhere in
the test file, including comments, so write the explanatory comment without naming a host.

Move Case B's below-floor count from 1000 to a count below BOTH floors, 500, and say in a comment
why the number matters: 1000 is the exact Theo floor plan 339-12 will introduce, so a below-floor
arm sitting at 1000 would stop failing the moment the flip lands and the arm would silently invert
from proving a failure to proving a pass. Confirm 500 is not `STALE_REPLICA_NODE_COUNT`, so Case
B keeps testing the generic floor rather than the named signature.

Add two arms to Test 11, both green on both sides of the flip:
one Theo-SHAPED stats arm feeding `{ nodes: 30000, relationships: 40000, labels: 14 }` and
asserting `ok` true with `payload.node_count` 30000, which proves the `nodes` half of the dual read;
and one neither-field arm feeding `{ labels: 14 }` and asserting a failure whose reason names both
field names, which proves the fallback is still honest. Use 30000 for the Theo-shaped arm rather
than Theo's real 1253, deliberately: 30000 clears the incumbent floor of 29000 AND the coming Theo
floor of 1000, so the arm tests the SHAPE READ and nothing else, and it cannot be knocked over by a
floor change. Keep Case C's incumbent-shaped arm exactly as it is apart from the derived endpoint,
so the two shapes are proven side by side.

Preserve the hermetic contract exactly: every new bag carries `mockBrainUrl`, `mockStats` and
`mockQuery`, so L6 never reaches `lib/core/brain-client.cjs` and therefore never the network.

Edit 3, `tests/fixtures/246-census-fixture.json`, lines 6 and 173. Replace both `brain_url` values
with an origin-neutral RFC 2606 placeholder host so the fixture names neither Brain. This is the
right answer rather than retyping Theo's host, for a reason worth writing into the SUMMARY: the
fixture's job is to pin the SHAPE of a census artifact, `tests/test-246-census-render.cjs` never
asserts the value, and the generator now populates the real field from `getBrainUrl()` after Task 1,
so the live value has one source and the fixture has none. A fixture that names a host is a fixture
that has to be revisited at every origin change for no test benefit.

Run `node tests/test-246-census-render.cjs` after the fixture edit and confirm the consumer passes.
Leave `data/brain-census.generated.json` and `docs/BRAIN-GRAPH-CENSUS.generated.md` untouched, per
Task 2's three-reason disposition.
  </action>

  <verify>
    <automated>node lib/core/doctor/class-m-brain-smoke.test.cjs && node tests/test-246-census-render.cjs && node scripts/doctor.cjs --acceptance && node tests/test-339-origin-single-source.cjs && grep -Fq 'statsResult.nodes' lib/core/doctor/class-m-brain-smoke.cjs && grep -Fq 'statsResult.totalRecordCount' lib/core/doctor/class-m-brain-smoke.cjs && grep -Fq "const CANON_BRAIN_URL = 'https://pws-brain-mcp.onrender.com';" lib/core/doctor/class-m-brain-smoke.cjs && grep -Fq 'CANON_NODE_FLOOR = 29000' lib/core/doctor/class-m-brain-smoke.cjs && grep -Fq 'STALE_REPLICA_NODE_COUNT = 28325' lib/core/doctor/class-m-brain-smoke.cjs && grep -Fq 'CANON_BRAIN_URL' lib/core/doctor/class-m-brain-smoke.test.cjs && ! grep -q 'onrender.com' lib/core/doctor/class-m-brain-smoke.test.cjs && ! grep -q 'onrender.com' tests/fixtures/246-census-fixture.json && ! grep -q 'totalRecordCount: 1000' lib/core/doctor/class-m-brain-smoke.test.cjs && echo DOCTOR_PREP_OK</automated>
  </verify>

  <acceptance_criteria>
    - The live `brain_stats` top-level shape from the A10 discharge is recorded VERBATIM in the SUMMARY, including whether the payload is nested, so plan 339-12 inherits a fact.
    - `node lib/core/doctor/class-m-brain-smoke.test.cjs` passes, and the suite's PASSED count is strictly higher than before this task, proving arms were ADDED rather than retargeted.
    - `node scripts/doctor.cjs --acceptance` passes with the same profile as before this task: this change is inert against the incumbent and must not move a single verdict.
    - INERTNESS PROOF, recorded in the SUMMARY: `git diff lib/core/doctor/class-m-brain-smoke.cjs` shows hunks ONLY at the node count read, its reason string, its comment, and the `module.exports` line. No constant line appears in the diff.
    - `CANON_BRAIN_URL` still equals the incumbent origin, `CANON_NODE_FLOOR` is still 29000, and `STALE_REPLICA_NODE_COUNT` is still 28325, each asserted by `grep -F` in the verify block. This is the D-13 guard: the PREP cut moves no value.
    - The dual read recognizes `totalRecordCount` BEFORE `nodes`, and at least one test arm exercises each shape, plus one arm exercising neither.
    - The string `onrender.com` appears nowhere in `lib/core/doctor/class-m-brain-smoke.test.cjs`, including comments.
    - Case B feeds 500, not 1000, and a comment in place states that 1000 is the coming Theo floor and would have inverted the arm.
    - `tests/fixtures/246-census-fixture.json` names neither Brain, and `node tests/test-246-census-render.cjs` passes.
    - `node tests/test-339-origin-single-source.cjs` passes with its two-entry allowlist unchanged.
    - `data/brain-census.generated.json` and `docs/BRAIN-GRAPH-CENSUS.generated.md` are unchanged.
    - `grep -c` for the em-dash character returns 0 in all three files, comments included.
  </acceptance_criteria>

  <done>The doctor layer can read Theo's stats shape and its test suite proves it in both directions, both against the incumbent that is still the default, so the FLIP commit is left carrying only the values D-13 binds to it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin process -> remote Brain origin | every runtime origin read is a decision about where user traffic goes |
| session-start banner -> user | prose printed on every session is a claim the user trusts without checking |
| injected test mock -> the assertion it is supposed to prove | a mock that stops matching the constant it mirrors keeps printing PASSED while proving something else |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-01 | Tampering | a second origin literal surviving anywhere under `lib/`, `bin/` or `scripts/` | mitigate | `tests/test-339-origin-single-source.cjs` scans comment-stripped lines with a two-entry allowlist, and this plan is what turns it green |
| T-339-20 | Repudiation | `probe-brain-contract.cjs` silently probing the incumbent post-flip while its output claims to describe the Brain | mitigate | the probe resolves through `getBrainUrl()`, so it always tests the origin the plugin actually ships; the comment-as-contract names this risk by name |
| T-339-21 | Repudiation | a user-visible session banner naming a host the release moved away from | mitigate | the host is dropped from the banner rather than derived, with the reason written in place; `node scripts/doctor.cjs` class M layer 6 remains the one authoritative reporter of the resolved origin |
| T-339-22 | Repudiation | re-running the census against Theo, silently discarding the incumbent-graph record the 2026-08 phases were measured against | mitigate | the two generated artifacts are left untouched, `git diff` on both is asserted empty, and the three reasons plus a deferred re-census item are recorded in the SUMMARY and carried into the CHANGELOG by plan 339-10 |
| T-339-36 | Tampering | a doctor mock bag still returning the incumbent after `CANON_BRAIN_URL` moves, silently inverting what the suite proves from the canon branch to the not-canon early exit | mitigate | every mock origin derives from the newly exported `CANON_BRAIN_URL`, the string `onrender.com` is asserted absent from the whole test file including comments, and the below-floor arm is moved off the coming Theo floor value so it cannot invert either |
| T-339-37 | Repudiation | assuming Theo's `brain_stats` top-level shape from source without checking the wire wrapping (Assumption A10), then writing a guard against a shape that never arrives | mitigate | one live read-only `brain_stats` call at execution time, the observed top-level shape recorded verbatim in the SUMMARY, and the guard given one more level of unwrapping if the payload is nested |
| T-339-38 | Repudiation | a test fixture naming a host, so every future origin change has to revisit a file whose job is to pin SHAPE and whose consumer never asserts the value | mitigate | both `brain_url` fields set to an origin-neutral RFC 2606 placeholder, with the live field populated from `getBrainUrl()` by Task 1's generator change |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | zero package installs in this plan and in the phase |
</threat_model>

<verification>
- `node tests/test-339-origin-single-source.cjs` passes.
- `bash tests/run-all-339.sh` shows the origin arm green.
- `node scripts/doctor.cjs --acceptance` profile is unchanged by this plan.
- `git diff --stat data/brain-census.generated.json docs/BRAIN-GRAPH-CENSUS.generated.md` is empty.
- `node scripts/check-plugin-path-anchoring.cjs --check-scripts` passes: this plan adds no new bare `bash scripts/x` or `node scripts/x` line to any command, skill, agent or pipeline markdown, and gate 10f is zero-tolerance with no grandfather clause.
- `node lib/core/doctor/class-m-brain-smoke.test.cjs` passes with a PASSED count strictly higher than before this plan.
- `node tests/test-246-census-render.cjs` passes against the neutralized fixture.
- The three D-13 constants are proven UNMOVED by `grep -F`: `CANON_BRAIN_URL` still the incumbent, `CANON_NODE_FLOOR` still 29000, `STALE_REPLICA_NODE_COUNT` still 28325.
- `grep -rc 'onrender.com' lib/core/doctor/class-m-brain-smoke.test.cjs tests/fixtures/246-census-fixture.json` returns 0 for both files.
</verification>

<success_criteria>
- FLIP-01's PREP half is green: the two runtime sites resolve through `getBrainUrl()` and no prose site names the outgoing host.
- FLIP-11's PREP half is green: the doctor layer can read Theo's stats shape, its test suite proves both shapes, and neither change moved a value.
- The FLIP cut's remaining surface is exactly the five files D-13 locks: `lib/core/brain-client.cjs` line 24 and its docblock, the doctor's constants, one test pin, `CLAUDE.md` and `CHANGELOG.md`. Nothing this plan leaves behind adds a sixth.
- The generated-artifact disposition is recorded, not silently skipped.
- Assumption A10 is discharged with a live observation rather than carried into the flip.
- No em-dashes in any file this plan touched.
</success_criteria>

<output>
Create `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-07-SUMMARY.md` when done. Record verbatim: the two `MINDRIAN_BRAIN_URL` override observations, the three-reason generated-artifact disposition for plan 339-10's CHANGELOG entry, the live `brain_stats` top-level shape that discharges Assumption A10, and the inertness proof that `git diff lib/core/doctor/class-m-brain-smoke.cjs` contains no constant line. Plan 339-12 reads this SUMMARY to inherit the stats shape as a fact.
</output>
