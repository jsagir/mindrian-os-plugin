---
phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig
plan: 07
type: execute
wave: 2
depends_on: ["339-01", "339-02", "339-03"]
files_modified:
  - scripts/probe-brain-contract.cjs
  - scripts/build-brain-census.cjs
  - lib/core/mcp-profiles.cjs
  - scripts/rs-experts-command.cjs
  - scripts/rs-thesis-command.cjs
  - scripts/sessionstart-post-update-preflight.cjs
  - scripts/session-start
  - .env.brain.template
autonomous: true
requirements: [FLIP-01]
canon_parts: [7, 8]

must_haves:
  truths:
    - "No runtime site outside brain-client.cjs and the doctor's canon constant resolves the Brain origin from its own literal"
    - "scripts/probe-brain-contract.cjs tests the origin the plugin actually ships, so it cannot silently probe the incumbent after the flip"
    - "No user-visible session banner names a host that the release moved away from"
  artifacts:
    - path: "scripts/probe-brain-contract.cjs"
      provides: "Origin resolved through getBrainUrl(), env override still honored by that resolver"
      contains: "getBrainUrl"
    - path: "scripts/build-brain-census.cjs"
      provides: "Origin resolved through getBrainUrl(), removing the self-declared mirror of line 24"
      contains: "getBrainUrl"
  key_links:
    - from: "scripts/probe-brain-contract.cjs"
      to: "lib/core/brain-client.cjs getBrainUrl()"
      via: "require, replacing its own BRAIN_URL const"
      pattern: "getBrainUrl"
    - from: "scripts/build-brain-census.cjs"
      to: "lib/core/brain-client.cjs getBrainUrl()"
      via: "require, replacing the const whose own comment admitted it mirrors line 24"
      pattern: "getBrainUrl"
---

<objective>
The literal sweep (D-12, FLIP-01). Two runtime sites stop declaring their own origin and start
asking the one place that decides it. Six prose sites stop naming a host they are about to be
wrong about.

This is not tidying. `scripts/build-brain-census.cjs:59` documents itself as mirroring
`lib/core/brain-client.cjs` line 24, which is a second source of truth admitting in writing to
being one. `scripts/probe-brain-contract.cjs:74` is worse: post-flip it would silently probe the
INCUMBENT while reporting on "the Brain", which is exactly the class of false-success this repo has
spent phases closing.

Deliberately OUT of this plan and named so nobody sweeps them by accident:
- `lib/core/brain-client.cjs:24` and its `:4-7` docblock. The FLIP cut owns them (339-12).
- `lib/core/doctor/class-m-brain-smoke.cjs:76-78`. Its constants answer "is the resolved endpoint
  the canon one", so deriving them from `getBrainUrl()` would make the comparison always true and
  destroy the check. The FLIP cut moves their VALUES in the same commit as line 24 (339-12).
- `lib/core/enrichment-queue.cjs:465`. Plan 339-05 rewrites that comment as part of FLIP-03.
- `tests/test-245-skill-frontmatter-inert-keys.cjs` and `tests/fixtures/246-census-fixture.json`.
  FLIP cut (339-12).
- `data/brain-census.generated.json` and `docs/BRAIN-GRAPH-CENSUS.generated.md`. LEAVE, generated,
  reasons recorded below.
- Every dated handoff, RCA, autopsy, tester outbox file, CHANGELOG history entry and
  `.planning/debug/*` under `docs/`. Historical records. LEAVE.

Output: eight files changed, `tests/test-339-origin-single-source.cjs` turned green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-PATTERNS.md
@lib/core/brain-client.cjs
</context>

<artifacts_this_phase_produces>
This plan creates NO new symbols. It removes two: the module-local `BRAIN_URL` const in
`scripts/probe-brain-contract.cjs` and the one in `scripts/build-brain-census.cjs`, each replaced
by a call to the existing exported `getBrainUrl()`.

Symbols this plan CONSUMES that were created earlier in the phase: none. `getBrainUrl()` has
existed since before this phase (`lib/core/brain-client.cjs:1163`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Point the two runtime origin sites at getBrainUrl() (FLIP-01)</name>

  <files>scripts/probe-brain-contract.cjs, scripts/build-brain-census.cjs</files>

  <behavior>
    - `tests/test-339-origin-single-source.cjs`, red at the end of wave 1, goes GREEN.
    - Neither script declares its own origin literal on a non-comment line.
    - Both resolve the origin by requiring `lib/core/brain-client.cjs` and calling `getBrainUrl()`.
    - A `MINDRIAN_BRAIN_URL` override still works identically in both scripts, because that override is honored inside `getBrainUrl()`'s own resolution.
    - Both scripts still run to completion with their existing CLI arguments and produce the same output shape as before.
  </behavior>

  <read_first>
    - `scripts/probe-brain-contract.cjs` lines 30 to 90: the header enumerating its five legs, and the `BRAIN_URL` const at `:74`
    - `scripts/build-brain-census.cjs` lines 24 to 65: the header stating it has NO `--check` release gate by design because a release gate must never depend on live network, the Lane A / Lane B split and the admin-key requirement at `:36-39`, the `MD_PATH` and `JSON_PATH` at `:56-57`, the `GENERATED_NOTE` at `:59-60`, and the `BRAIN_URL` const at `:61` whose own comment says it "mirrors lib/core/brain-client.cjs line 24"
    - `lib/core/brain-client.cjs:1155-1176`: `getBrainUrl()` and the docblock stating the one-place-decides rule, including that the env override is resolved inside `BRAIN_URL` itself
    - `339-RESEARCH.md` section `Runtime sites, exact`: the disposition column for each of these two files
    - `339-RESEARCH.md` `Don't Hand-Roll` first row: do not build a second `const BRAIN_URL = process.env.MINDRIAN_BRAIN_URL || '...'` anywhere
    - `339-PATTERNS.md` `Shared Patterns` section `Single source of truth for the origin (D-12)`
  </read_first>

  <action>
In BOTH scripts, delete the module-local `BRAIN_URL` const and replace it with a resolution
through the shared resolver. Use a relative require to `lib/core/brain-client.cjs` matching each
script's own existing require style and depth, then call `getBrainUrl()`.

Do NOT re-implement the env override. `getBrainUrl()` returns `BRAIN_URL`, which is already
`process.env.MINDRIAN_BRAIN_URL || <default>`. Adding a second `process.env` read here would
reintroduce exactly the duplication this task removes. Verify behaviorally: run each script once
with `MINDRIAN_BRAIN_URL` set to a throwaway value and confirm the script reports that value.

In `scripts/build-brain-census.cjs`, DELETE the comment at `:59-60` that says the const "mirrors
lib/core/brain-client.cjs line 24". It is no longer true and it was the written confession of the
second source of truth. Replace it with a one-line comment naming phase 339 and 2026-09-03 and
stating that the origin is resolved through `getBrainUrl()` so there is nothing left to mirror.

In `scripts/probe-brain-contract.cjs`, add a comment-as-contract block at the new resolution site
naming: phase id 339, date 2026-09-03, the observed risk it closes (post-flip this probe would
otherwise silently test the INCUMBENT while its output claims to describe "the Brain", and its
five legs would then all report on the wrong service), and what it deliberately does NOT change
(the five legs themselves, their assertions, and the expected-red status of leg e pending the
seven operator index DROPs; plan 339-14 records which legs invert against Theo and those
inversions are EXPECTED, not failures).

Both files are executed scripts, not libraries. Confirm each still runs: `probe-brain-contract.cjs`
performs live network calls, so run it only far enough to prove the origin resolves and the
process starts, or run it fully if the network is available and record the output verbatim.
`build-brain-census.cjs` requires live network for Lane A and an admin key for Lane B; do NOT run
a full census in this task. Proving the origin resolves is enough, and re-running the census is
explicitly out of scope (see Task 2's disposition note).
  </action>

  <verify>
    <automated>node tests/test-339-origin-single-source.cjs && ! grep -n "onrender.com" scripts/probe-brain-contract.cjs | grep -v '^\s*[0-9]*:\s*[/*#]' | grep -q . && grep -Fq 'getBrainUrl' scripts/probe-brain-contract.cjs && grep -Fq 'getBrainUrl' scripts/build-brain-census.cjs && MINDRIAN_BRAIN_URL=https://example.invalid node -e "console.log(require('./lib/core/brain-client.cjs').getBrainUrl())" | grep -Fq 'https://example.invalid' && echo SWEEP_OK</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-339-origin-single-source.cjs` exits 0. This is the wave-2 flip from red to green that plan 339-01 predicted.
    - Neither script contains a `process.env.MINDRIAN_BRAIN_URL` read of its own.
    - Each script, run with `MINDRIAN_BRAIN_URL` set to a throwaway value, reports that value. Record both observed outputs in the SUMMARY.
    - The `mirrors lib/core/brain-client.cjs line 24` comment is gone from `scripts/build-brain-census.cjs`.
    - `scripts/probe-brain-contract.cjs`'s five legs and their assertions are byte-identical: `git diff` shows hunks only around the origin resolution and its comment.
    - `grep -c` for the em-dash character returns 0 in both files.
  </acceptance_criteria>

  <done>FLIP-01's runtime half is closed. The probe cannot silently test the wrong service after the flip, and the census generator no longer documents itself as a duplicate authority.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the comment-level and user-visible prose sites</name>

  <files>lib/core/mcp-profiles.cjs, scripts/rs-experts-command.cjs, scripts/rs-thesis-command.cjs, scripts/sessionstart-post-update-preflight.cjs, scripts/session-start, .env.brain.template</files>

  <read_first>
    - `lib/core/mcp-profiles.cjs` around line 22: the comment reference
    - `scripts/rs-experts-command.cjs` around line 10 and `scripts/rs-thesis-command.cjs` around line 10: the comment references
    - `scripts/sessionstart-post-update-preflight.cjs` around line 38: the comment reference, plus the file's own defensive rule (never block the hook chain on internal error, exit 0 silently when the state file is absent) which must not be disturbed
    - `scripts/session-start` around line 1896: `echo "Brain: HTTP client active (pws-brain-mcp.onrender.com)"` and the surrounding comment at `:1889-1891` describing it as prose. This is a user-visible banner printed on EVERY session start, and it is NOT in CONTEXT D-12's list; research added it
    - `.env.brain.template` around line 9: the incumbent origin in a comment
    - `339-RESEARCH.md` section `Runtime sites, exact`, the `scripts/session-start:1896` row and its recommendation
    - `339-RESEARCH.md` section `Generated artifacts: LEAVE, and say why in writing`: the three reasons re-running the census is wrong for this phase
  </read_first>

  <action>
Six prose edits. None changes behavior; all six change what a reader or a user is told.

`lib/core/mcp-profiles.cjs`, `scripts/rs-experts-command.cjs`, `scripts/rs-thesis-command.cjs`,
`scripts/sessionstart-post-update-preflight.cjs`: rewrite each comment so it names the RESOLVER
rather than a host. Say that the Brain origin is whatever `lib/core/brain-client.cjs`'s
`getBrainUrl()` resolves, and drop the hard-coded hostname. Where the surrounding prose needs a
concrete example, say "the origin resolved by `getBrainUrl()`" rather than naming either host.
Add the phase attribution: phase 339, 2026-09-03.

`scripts/session-start` line 1896: drop the host from the banner entirely. Change
`Brain: HTTP client active (pws-brain-mcp.onrender.com)` to `Brain: HTTP client active`. Do NOT
shell out to node to resolve the origin for a banner string: this is a bash script on the session
start path, and adding a node spawn to print a hostname would cost startup latency on every
session for a cosmetic gain. Add a one-line comment naming phase 339 and 2026-09-03 and stating
the reason the host was dropped rather than derived: the banner is prose a user reads on every
session start, so a wrong host there is a lie repeated constantly, and the origin already has one
authoritative reporter (`node scripts/doctor.cjs`, class M layer 6).

`.env.brain.template` line 9: rewrite the comment so it explains what `MINDRIAN_BRAIN_URL` is for
rather than naming today's default. Two facts must be stated, both security-relevant per the
research Security Domain V14 row: the value must be a BARE ORIGIN with no path and no trailing
slash (the client appends `/mcp` and `/register` itself; `/mcp` produces `/mcp/mcp` and a trailing
slash a double slash, both 404, and the client renders a 404 as "Brain unreachable" rather than a
config error), and this variable is the per-install ROLLBACK lever for an origin change, valid
only while the previous origin is still running.

Finally, record the GENERATED-ARTIFACT DISPOSITION. Do NOT edit
`data/brain-census.generated.json` or `docs/BRAIN-GRAPH-CENSUS.generated.md`, and do NOT re-run
`scripts/build-brain-census.cjs`. Three reasons, which must be written into this plan's SUMMARY so
plan 339-10 can carry them into the CHANGELOG: (1) the generator's own header states it has NO
`--check` release gate by design because a release gate must never depend on live network, and
`grep build-brain-census scripts/verify-release` returns nothing, so a stale census blocks no
gate; (2) Lane B requires an operator-supplied ADMIN key, and Theo has no admin key because
`brain_write` returns `WRITE_PATH_DISABLED` unconditionally; (3) re-running Lane A against Theo
would replace a census of the incumbent's 29,200-node graph with a census of Theo's 1,253-node
graph, silently discarding the record the 2026-08 phases were measured against. Register
"re-census against Theo" as a deferred item in the SUMMARY.
  </action>

  <verify>
    <automated>! grep -q 'pws-brain-mcp.onrender.com' scripts/session-start && ! grep -q 'pws-brain-mcp.onrender.com' .env.brain.template && ! grep -q 'pws-brain-mcp.onrender.com' lib/core/mcp-profiles.cjs && ! grep -q 'pws-brain-mcp.onrender.com' scripts/rs-experts-command.cjs && ! grep -q 'pws-brain-mcp.onrender.com' scripts/rs-thesis-command.cjs && ! grep -q 'pws-brain-mcp.onrender.com' scripts/sessionstart-post-update-preflight.cjs && git diff --stat data/brain-census.generated.json docs/BRAIN-GRAPH-CENSUS.generated.md | wc -l | grep -q '^0$' && echo PROSE_OK</automated>
  </verify>

  <acceptance_criteria>
    - None of the six files contains `pws-brain-mcp.onrender.com` anywhere, including comments.
    - `scripts/session-start`'s banner reads `Brain: HTTP client active` with no host, and carries the one-line reason comment.
    - `.env.brain.template` states both the bare-origin rule and the rollback-lever role.
    - `data/brain-census.generated.json` and `docs/BRAIN-GRAPH-CENSUS.generated.md` are UNCHANGED: `git diff` on both is empty.
    - No behavior changed: `bash tests/run-all-339.sh` and `node scripts/doctor.cjs --acceptance` produce the same pass/fail profile as before this task, and `scripts/sessionstart-post-update-preflight.cjs` still exits 0 when its state file is absent.
    - The SUMMARY records the three-reason generated-artifact disposition verbatim and registers the re-census as deferred.
    - `grep -c` for the em-dash character returns 0 in all six files.
  </acceptance_criteria>

  <done>No prose in this repo's runtime tree tells a reader or a user the wrong host, and the deliberate decision to leave the census artifacts stale is recorded with its three reasons rather than looking like an oversight.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin process -> remote Brain origin | every runtime origin read is a decision about where user traffic goes |
| session-start banner -> user | prose printed on every session is a claim the user trusts without checking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-01 | Tampering | a second origin literal surviving anywhere under `lib/`, `bin/` or `scripts/` | mitigate | `tests/test-339-origin-single-source.cjs` scans comment-stripped lines with a two-entry allowlist, and this plan is what turns it green |
| T-339-20 | Repudiation | `probe-brain-contract.cjs` silently probing the incumbent post-flip while its output claims to describe the Brain | mitigate | the probe resolves through `getBrainUrl()`, so it always tests the origin the plugin actually ships; the comment-as-contract names this risk by name |
| T-339-21 | Repudiation | a user-visible session banner naming a host the release moved away from | mitigate | the host is dropped from the banner rather than derived, with the reason written in place; `node scripts/doctor.cjs` class M layer 6 remains the one authoritative reporter of the resolved origin |
| T-339-22 | Repudiation | re-running the census against Theo, silently discarding the incumbent-graph record the 2026-08 phases were measured against | mitigate | the two generated artifacts are left untouched, `git diff` on both is asserted empty, and the three reasons plus a deferred re-census item are recorded in the SUMMARY and carried into the CHANGELOG by plan 339-10 |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | zero package installs in this plan and in the phase |
</threat_model>

<verification>
- `node tests/test-339-origin-single-source.cjs` passes.
- `bash tests/run-all-339.sh` shows the origin arm green.
- `node scripts/doctor.cjs --acceptance` profile is unchanged by this plan.
- `git diff --stat data/brain-census.generated.json docs/BRAIN-GRAPH-CENSUS.generated.md` is empty.
- `node scripts/check-plugin-path-anchoring.cjs --check-scripts` passes: this plan adds no new bare `bash scripts/x` or `node scripts/x` line to any command, skill, agent or pipeline markdown, and gate 10f is zero-tolerance with no grandfather clause.
</verification>

<success_criteria>
- FLIP-01's PREP half is green: the two runtime sites resolve through `getBrainUrl()` and no prose site names the outgoing host.
- The FLIP cut's remaining origin surface is exactly what 339-12 owns: line 24, the docblock, the doctor's canon constants, one test pin, `CLAUDE.md` and one fixture.
- The generated-artifact disposition is recorded, not silently skipped.
- No em-dashes in any file this plan touched.
</success_criteria>

<output>
Create `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-07-SUMMARY.md` when done. Record the two `MINDRIAN_BRAIN_URL` override observations verbatim, and the three-reason generated-artifact disposition verbatim for plan 339-10's CHANGELOG entry.
</output>
