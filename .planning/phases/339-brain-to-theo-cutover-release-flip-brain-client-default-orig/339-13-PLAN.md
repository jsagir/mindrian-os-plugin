---
phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig
plan: 13
type: execute
wave: 6
depends_on: ["339-12"]
files_modified:
  - .claude-plugin/plugin.json
  - package.json
  - CHANGELOG.md
  - dist/BUNDLE-VERSION.json
autonomous: false
requirements: [FLIP-09, FLIP-10, FLIP-11]
canon_parts: [7, 8]

must_haves:
  truths:
    - "The FLIP release cannot be cut until a human confirms Theo's coverage ruling, read live at run time, with zero repository writes in either repo"
    - "Session T is told the flip is happening BEFORE it happens, so it can take a same-minute reading"
    - "The flip release is cut from a clean tree with Phase 276 paused, exactly like the PREP cut"
    - "A coverage HOLD is a successful gate outcome that stops before release.sh, not a stall to work around"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "The [Unreleased] heading rewritten by release.sh Step 6 to the released flip version and date"
    - path: ".claude-plugin/plugin.json"
      provides: "Version bumped by release.sh Step 3, then pre-bumped by Commit B"
  key_links:
    - from: "this plan's Task 1 gate"
      to: "/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md"
      via: "heading-scoped awk extraction plus eleven grep -F literals, read only, re-read at run time"
      pattern: "Coverage does NOT block Task 2, the flip"
    - from: "this plan's Task 2"
      to: "Session T"
      via: "an orchestrator-sent message, because the executor has no messaging tool"
      pattern: "cutting the flip now"
---

<objective>
The FLIP CUT. Three tasks, in a fixed order that is the whole point of the plan.

1. The BLOCKING COVERAGE GATE (D-05, D-06, FLIP-09). Every automatable task in this phase has
   already landed: the sweep, the adaptations, the docs, the note, the checklist rewrite, the flip
   edits and both CHANGELOG entries. Nothing automatable remains between this gate and
   `release.sh`. The gate reads ONE cross-repo file, read-only, re-read at RUN TIME, and makes ZERO
   repository writes in either repo.
2. Tell Session T the flip is happening, so it takes a same-minute `/health` and first-call latency
   reading for flip-record section 4. This is an ORCHESTRATOR action, not an executor action: the
   plan executor has no messaging tool.
3. The cut itself: the same clean-tree confirmation, push, suites, `verify-release`, `doctor`, and
   the human-held `bash scripts/release.sh --prerelease`.

Version: the PREP cut's Commit B pre-bumped `plugin.json` to `2.0.0-beta.18`, so this cut is
expected to produce **v2.0.0-beta.19**. `--no-next-bump` is deliberately NOT used, on either cut.
Confirm with `--dry-run` and read the shipped version back from the tag; never hard-code it.

A coverage HOLD is a SUCCESSFUL outcome of Task 1, not a failure. If the ruling has been amended,
the heading has moved, or the ruling sentence is gone, the gate reports "coverage held" and this
plan stops BEFORE `release.sh`. Reporting the block is the correct result on any run before the
precondition is met.

Output: the flip released, or the hold recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md
@.claude/includes/release-process.md
@scripts/release.sh
</context>

<artifacts_this_phase_produces>
This plan creates no source symbol. It produces a RELEASE: a git tag, an npm publish of
`@mindrian_os/cli`, a marketplace pin and a website `FALLBACK_VERSION` sync.

Files `scripts/release.sh` mutates on this plan's behalf: `.claude-plugin/plugin.json`,
`package.json`, `CHANGELOG.md`, `~/mindrian-marketplace/.claude-plugin/marketplace.json`, and
`$HOME/mindrian-website/website/src/lib/version.ts`. None is hand-edited by any task here.

Files this plan must NOT write, under any circumstance: anything under `/home/jsagi/Theo/`. Session
T owns that repo, and Task 1's zero-write contract is asserted with a porcelain hash comparison in
BOTH repos.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: BLOCKING GATE - coverage ruling</name>

  <read_first>
    - `/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md` (READ ONLY, never edit), specifically `## 1. Authorization evidence` and within it the subsection headed exactly `### Coverage re-measurement, 2026-09-03, and the ruling on it`
    - `/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md` `## 2. Flip instructions for the plugin release` and `## 3. Rollback` (READ ONLY, never edit)
    - `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-05-PLAN.md` Task 1: the repo's own blocking human-action gate precedent, whose structure this task copies verbatim
    - `339-RESEARCH.md` Design 5 IN FULL: the contract, the repo precedent's eight features, the exact verify block, and the gate-placement reasoning
    - `339-RESEARCH.md` Pitfall 7 (a write inside the zero-write gate) and Pitfall 8 (`grep -E` against the coverage literals)
    - `tests/test-339-gate-zero-write.sh`, written by plan 339-03: this task's verify block is that script, already proven green
  </read_first>

  <action>
STOP. Do not run Task 2 or Task 3 until a human confirms every item below.

Read `/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md` READ ONLY.
Never edit it. Never `git fetch` in that repo. Never write a scratch file anywhere. Perform no
repository writes in this task, in either repo.

Extract the subsection headed exactly:

  `### Coverage re-measurement, 2026-09-03, and the ruling on it`

Scope by HEADING, never by line number. It sat at line 301 at Theo commit `81dfac8`, and any edit
Session T makes above it moves that number. Use `awk` anchored on the exact heading with an
explicit exit on the next heading of two or three hashes, not a `sed` range: the heading is
comma-heavy and a `sed` range over it is fragile.

Present this checklist and require an explicit confirmation for each. Quote the CURRENT observed
value for each item so the human can see what changed. Use `grep -F` for every literal, never
`grep -E`: `88.4%` contains a regex dot, and `-F` removes the whole class of problem. Pipe with
`printf` and a plain format string, never a here-string, so the subsection's own backticks and
dollar signs cannot be re-interpreted.

1. The subsection exists under that exact heading, inside `## 1. Authorization evidence` and before
   `## 2. Flip instructions for the plugin release`.
2. The Brain pin `Brain@56bf75a` is present. Grep the `Brain@` form specifically; the bare
   `56bf75a` also appears in the pins table and is ambiguous.
3. The Theo pin `83a1ce2` is present.
4. The canon figures `1,253`, `1,522` and `420` are all present.
5. The Brain figures `29,200`, `24,375` and `258` are all present.
6. The name-level coverage line `Covered: 228 of 258, 88.4%` is present.
7. `Uncovered: 30 of 258` is present.
8. The ruling sentence `Coverage does NOT block Task 2, the flip` is present.
9. The subsection does NOT carry a `HOLD` token. If it does, report it and STOP.
10. `git status --porcelain` piped to `sha256sum` in `/home/jsagi/dev/MindrianOS-Plugin` is
    byte-identical before and after this task.
11. `git status --porcelain` piped to `sha256sum` in `/home/jsagi/Theo` is byte-identical before
    and after this task.

Quote the ruling sentence VERBATIM into the SUMMARY. Do not paraphrase it and do not summarize the
ruling; the point of re-reading at run time is that the exact words are the authorization.

If ANY item fails, HALT and report which item failed, using the phrase "coverage held". Do not
partially execute. Do not proceed to Task 2 or Task 3. A HOLD means Session T amended the ruling or
moved the heading, and both are findings the flip must act on rather than route around. Reporting
the hold IS a successful outcome for this task, not a failure.

Two facts for the human's judgment, from the ruling itself, so the confirmation is informed rather
than mechanical: the 30 uncovered names bind Theo's DECOMMISSION task, not this flip; and the
named-Frameworks RATIO is retired as a coverage measure by ruling clause 4, because coverage is a
set question (of the Brain's 258 names, how many resolve in Theo by name) rather than a ratio with
a moving denominator.
  </action>

  <verify>
    <automated>bash tests/test-339-gate-zero-write.sh && awk '/^### Coverage re-measurement, 2026-09-03, and the ruling on it$/{f=1;next} f&&/^#{2,3} /{exit} f' /home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md > /dev/stdout | grep -F 'Coverage does NOT block Task 2, the flip' && echo "GATE: coverage ruled"</automated>
  </verify>

  <acceptance_criteria>
    - A human explicitly confirmed all eleven items, item by item, and the confirmation is recorded in the SUMMARY.
    - The ruling sentence `Coverage does NOT block Task 2, the flip` is quoted VERBATIM in the SUMMARY, read at run time and not copied from this plan.
    - The subsection was extracted by HEADING, not by line number, and the extraction was proven scoped: the string `Flip instructions for the plugin release` is NOT present in the extraction.
    - Every literal was matched with `grep -F`. No `grep -E` was used anywhere in this task.
    - Zero files were written by this task: the porcelain sha256 is byte-identical before and after in BOTH `/home/jsagi/dev/MindrianOS-Plugin` and `/home/jsagi/Theo`. Both hash pairs are recorded in the SUMMARY.
    - No `git fetch` was run in the Theo repo, no scratch file was created, and no `sed` was run against any Theo file.
    - If the gate did NOT clear, the SUMMARY records which item failed and Tasks 2 and 3 are left untouched. That is a successful outcome for this task, not a failure.
  </acceptance_criteria>

  <resume-signal>Type "coverage ruled" plus the ruling sentence verbatim, or "coverage held" plus what changed in the subsection.</resume-signal>

  <done>Either the coverage ruling is confirmed live by a human and the flip cut may proceed, or the hold is recorded with what changed, and this plan stops before `release.sh`.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: ORCHESTRATOR ACTION - tell Session T the flip is being cut now, and wait for its same-minute reading</name>

  <read_first>
    - `/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-12-CLOSEOUT-STAGING.md` section E (READ ONLY, never edit): the flip-record section 4 template, including the slot Session T fills on this cue, which reads "Same-minute remote reading taken by Session T on the cutting-the-flip-now cue" and then asks for GET /health seconds, first tools/list frame seconds, first graph-touching call seconds, against the 20 second budget, plus the served build stamp and the instance tier
    - `339-CONTEXT.md` D-15, final sentence: before the flip cut, M messages T "cutting the flip now" so T takes the same-minute `/health` and first-call latency reading for flip-record section 4
    - `339-CONTEXT.md` domain section: the two-session protocol and the seam, which is exactly two artifacts
  </read_first>

  <action>
STOP. This task is performed by the ORCHESTRATOR, not by the plan executor. The executor has no
messaging tool and cannot reach another session. Do not attempt to simulate the message, do not
write it to a file as a substitute, and do not mark this task done without a real reply.

The orchestrator sends Session T, working in `/home/jsagi/Theo`, this message:

  cutting the flip now

and waits for Session T's reply carrying its same-minute reading. Session T fills flip-record
section 4 from it, using the section E template in `09-12-CLOSEOUT-STAGING.md`. The numbers T
should return are:

- `GET /health`, seconds
- first `tools/list` frame, seconds
- first graph-touching call, seconds
- the served build stamp, and the instance tier (paid and non-spin-down is the flip precondition
  from flip-record section 1)

all against the 20 second abort budget.

Why the ordering matters and must not be inverted: the reading is only meaningful if it is taken
around the moment of the cut. Taking it after the release has landed measures a different process
under different load, and taking it long before measures a service nobody was calling. This is why
the message goes out BEFORE `release.sh` runs in Task 3, not after.

Record Session T's reply VERBATIM in the SUMMARY, including all four numbers. Plan 339-14 carries
them into the final report.

If Session T does not reply, HALT and report. Do not cut the flip without T's acknowledgement: the
whole coordination protocol exists so the two sessions do not step on each other, and cutting
unannounced discards the one measurement only T can take.

Perform no repository writes in this task, in either repo.
  </action>

  <verify>
    <automated>test -f /home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-12-CLOSEOUT-STAGING.md && awk '/^## E\./{f=1} f&&/^## F\./{exit} f' /home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-12-CLOSEOUT-STAGING.md | grep -Fq 'Same-minute remote reading' && echo "TEMPLATE_PRESENT: section E awaits T numbers"</automated>
  </verify>

  <acceptance_criteria>
    - The orchestrator sent the message `cutting the flip now` to Session T, and that send is recorded in the SUMMARY with its timestamp.
    - Session T replied, and its reply is quoted VERBATIM in the SUMMARY including all four numbers and the instance tier.
    - The reply was received BEFORE Task 3 invoked `release.sh`. The ordering is recorded.
    - Zero files were written by this task in either repo: the porcelain output is byte-identical before and after in both.
    - If T did not reply, the SUMMARY records the halt and Task 3 is left untouched.
  </acceptance_criteria>

  <resume-signal>Type "T acknowledged" plus Session T's four numbers verbatim, or "T silent" plus how long the wait was.</resume-signal>

  <done>Session T knows the flip is happening and has taken the same-minute reading only it can take, or the halt is recorded and the cut has not happened.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: HUMAN-HELD RELEASE - confirm the tree, push, run every gate, and cut the flip</name>

  <read_first>
    - `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-11-SUMMARY.md`: the PREP cut's shipped tag, the post-Commit-B `plugin.json` version, the `verify-release` WARN list with which were expected, and the recorded `--no-website` decision if one was made
    - `339-RESEARCH.md` `Release Mechanics`, the full ordered step table and the WARN-versus-FAIL gate table
    - `339-RESEARCH.md` section `What "pause Phase 276 at a wave boundary" means operationally`, all four conditions
    - `339-RESEARCH.md` Pitfall 2 (never hard-code the version) and Pitfall 10 (the interactive CHANGELOG prompt)
    - `339-VALIDATION.md` `Sampling Rate`, the pre-cut gate
  </read_first>

  <action>
STOP. This is the flip release. It is human-held by decision (D-01: both cuts are human-held).

PART A, the clean-tree confirmation. Present this and require explicit confirmation for each,
quoting the current observed value:

1. Phase 276 is PAUSED at a plan boundary, not mid-plan. Show the current `276-*-SUMMARY.md` list
   and confirm with the 276 executor. It reached at least plan 12 as of 2026-09-03; that number
   will have moved.
2. `git status --porcelain` filtered to remove untracked entries returns zero lines.
3. The count from `git log origin/main..HEAD --oneline` is known and intended.
4. `npm whoami` succeeds; `$HOME/mindrian-website/website` exists or the `--no-website` decision
   from the PREP cut still stands; `claude plugin validate` is on PATH.
5. `grep -qE '^## \[Unreleased\]' CHANGELOG.md` succeeds. Without it, Step 6 opens an interactive
   editor and hangs forever inside an agent task.

PART B, push and gates. Run in this order and record every output verbatim. Stop at the first
failure.

1. `git push origin main`. No tags; Step 9 pushes those.
2. `bash tests/run-all-339.sh`. Fully green, every arm.
3. `bash tests/run-all-250.sh`, `bash tests/run-all-252.sh`, `bash tests/run-all-276.sh`.
4. `node tests/test-245-skill-frontmatter-inert-keys.cjs`,
   `node tests/test-246-census-render.cjs`, `node tests/test-247-contract-client.cjs`,
   `node tests/test-254-normalize-roundtrip-probe.cjs`,
   `node tests/test-brain-response-sanitize.cjs`,
   `node lib/core/doctor/class-m-brain-smoke.test.cjs`.
5. `node scripts/build-connector-registry.cjs --check`,
   `node scripts/build-orchestration-projection.cjs --check`,
   `node scripts/check-render-coverage.cjs`, `node scripts/check-shape-declaration.cjs`,
   `node scripts/build-skill-mirrors.cjs --check`,
   `node scripts/build-dist-bundles.cjs --check-stale`,
   `node scripts/check-plugin-path-anchoring.cjs --check-scripts`.
6. `bash scripts/verify-release`. ZERO FAIL. Record every WARN and whether it was expected. Gate 19
   (Brain tool liveness) is offline and hermetic: tool registration happens at module load before
   any network call, so the flip cannot break it. If gate 19 FAILS, a tool NAME changed, which this
   phase never intended, and that is a stop.
7. `node scripts/doctor.cjs --acceptance`. Layer 6's `store_identity` should now be `ok` against
   Theo, per plan 339-12 Task 2.
8. `bash scripts/release.sh --prerelease --dry-run`. Record the computed version. Expected
   `2.0.0-beta.19`. If it is not, STOP and report.

PART C, the cut. Present to the human and require explicit go-ahead:

- The command is exactly `bash scripts/release.sh --prerelease` (plus `--no-website` only if the
  PREP cut's recorded decision was to skip it).
- The mode is `--prerelease`. It takes a bump MODE, not a version literal.
- The expected version is `v2.0.0-beta.19`, confirmed by the `--dry-run`.
- Commit B will pre-bump `plugin.json` to `2.0.0-beta.20` and rewrite the CHANGELOG heading. That
  is normal.
- `--no-next-bump` is deliberately NOT used, on either cut.
- This publishes `@mindrian_os/cli`, dist-tag `next`, then promoted to `latest`.

Run it once after the go-ahead. Do not re-run on a partial failure without reading the failure
first: Steps 6.5 and 6.6 roll back the version files on their own failures, but a failure AFTER
Step 7 leaves a commit and a tag behind and needs a human decision, not a retry.

Read the shipped version back from `git describe --tags --abbrev=0`. Never from this plan's prose.

Never bump any version by hand.
  </action>

  <verify>
    <automated>bash tests/run-all-339.sh && bash scripts/verify-release && node scripts/doctor.cjs --acceptance && git describe --tags --abbrev=0 && grep '"version"' .claude-plugin/plugin.json && grep -m1 '^## \[' CHANGELOG.md</automated>
  </verify>

  <acceptance_criteria>
    - A human explicitly confirmed all five Part A items and approved the Part C invocation. Both are recorded in the SUMMARY.
    - `git status --porcelain` filtered of untracked entries returned zero lines before the cut, quoted in the SUMMARY.
    - `bash tests/run-all-339.sh` exited 0 with every arm green.
    - `bash scripts/verify-release` reported ZERO FAIL. Every WARN is recorded with whether it was expected.
    - `node scripts/doctor.cjs --acceptance` exited 0 and layer 6's `store_identity` was `ok`.
    - The `--dry-run` computed version was recorded and equals `2.0.0-beta.19`; if it did not, the task HALTED rather than proceeding.
    - `git describe --tags --abbrev=0` returns the new tag, recorded verbatim.
    - `.claude-plugin/plugin.json` now reads the NEXT prerelease because Commit B pre-bumped it.
    - No version was bumped by hand anywhere; every version change traces to a `release:` commit made by the script.
    - If the run aborted, the SUMMARY records at which numbered step, what the abort message said, and whether a commit or tag was left behind.
  </acceptance_criteria>

  <resume-signal>Type "flip cut released" plus the tag from `git describe --tags --abbrev=0`, or "flip cut aborted" plus the step number and the abort message.</resume-signal>

  <done>The flip release exists as a tag, an npm publish and a marketplace pin, or the abort is recorded with enough detail to decide the next move.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theo repo -> this repo's gate | the authorization artifact belongs to Session T; Session M reads it and never writes it |
| dev working tree -> released artifact | a release publishes whatever is in the tree, including another phase's partial work |
| repo -> npm tarball | Step 9.5's payload gate is the only thing between the tarball and this repo's planning and docs directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-09 | Tampering | the gate task writing to the Theo repo it must only read | mitigate | zero-write acceptance criterion with a porcelain sha256 comparison in BOTH repos, no `git fetch`, no scratch file, no `sed`; `tests/test-339-gate-zero-write.sh` mechanizes the same check and was proven green in wave 1 |
| T-339-12 | Repudiation | the release cut against an amended or moved coverage ruling | mitigate | the gate re-reads the file AT RUN TIME, scopes by HEADING rather than line number, requires eleven `grep -F` literals, and quotes the ruling sentence verbatim into the SUMMARY so the authorization is recorded rather than assumed |
| T-339-04 | Tampering | a release cut from a dirty tree carrying Phase 276's partial work | mitigate | Part A's blocking clean-tree confirmation plus `release.sh` Step 2.5's own clean-tree gate before any mutation |
| T-339-06 | Information Disclosure | the npm tarball carrying `.planning/`, `docs/`, `tests/` or `node_modules/` | mitigate | `release.sh` Step 9.5's payload gate refuses such a tarball and exits 1; it is run, never bypassed |
| T-339-38 | Repudiation | cutting the flip without telling Session T, discarding the one same-minute measurement only T can take | mitigate | Task 2 is a blocking orchestrator action ordered BEFORE the cut, and its acceptance criteria require T's reply quoted verbatim and the ordering recorded |
| T-339-30 | Denial of Service | Step 6 hanging on an interactive editor prompt | mitigate | Part A item 5 asserts the `## [Unreleased]` heading exists, which plan 339-12 Task 4 created |
| T-339-39 | Denial of Service | a tool NAME change slipping in and breaking `verify-release` gate 19 | mitigate | gate 19 is run and a FAIL there is an explicit STOP rather than a warning; this phase changed descriptions only, never names, and plan 339-08 recorded the before-and-after name set |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | this phase installs zero packages. `release.sh` Step 6.7 runs `npm ci --omit=dev` from the existing `package-lock.json`, which is a lockfile-pinned reinstall of already-audited dependencies, not a new package addition |
</threat_model>

<verification>
- `bash tests/test-339-gate-zero-write.sh` exits 0, and the porcelain sha256 in both repos is unchanged across Task 1.
- The ruling sentence is quoted verbatim in the SUMMARY from a run-time read.
- Session T's reply is quoted verbatim and was received before `release.sh` ran.
- `bash tests/run-all-339.sh`, `bash scripts/verify-release` and `node scripts/doctor.cjs --acceptance` all passed before any mutation.
- `git describe --tags --abbrev=0` returns the new tag.
- `git log origin/main..HEAD --oneline` returns nothing after Step 9's push.
- Nothing under `/home/jsagi/Theo/` was modified by any task in this plan.
</verification>

<success_criteria>
- FLIP-09 is satisfied: the release was gated on a live human confirmation of Theo's ruling, with zero writes to either repo.
- The flip is released, or the coverage hold is recorded and nothing was cut.
- Session T took its same-minute reading because it was told before, not after.
- The version came from `release.sh`'s arithmetic and was read back from the tag.
</success_criteria>

<output>
Create `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-13-SUMMARY.md` when done. Record: the ruling sentence verbatim, both porcelain hash pairs, Session T's reply verbatim with its timestamp, the shipped tag, and every `verify-release` WARN with whether it was expected.
</output>
