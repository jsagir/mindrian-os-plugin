---
phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig
plan: 05
type: execute
wave: 2
depends_on: ["339-01", "339-02", "339-03"]
files_modified:
  - lib/core/enrichment-queue.cjs
  - lib/mcp/brain-router.cjs
autonomous: true
requirements: [FLIP-03]
canon_parts: [7, 8]

must_haves:
  truths:
    - "A Theo-shaped readiness miss is captured in the enrichment queue instead of silently returning invalid_probe_result"
    - "Both of Theo's readiness shapes are handled: the scored one and the refusal-only one where score, inputs, evidence and framework are omitted entirely"
    - "An empty canon layer and a canon that simply does not carry your framework are never treated as the same answer"
    - "A Tier-3 router miss caused by a shape Theo never emits is DISCLOSED, not indistinguishable from the Brain never answering"
  artifacts:
    - path: "lib/core/enrichment-queue.cjs"
      provides: "Two additive Theo capture arms plus the THEO_INPUT_DIMENSIONS mapping"
      contains: "THEO_INPUT_DIMENSIONS"
    - path: "lib/mcp/brain-router.cjs"
      provides: "Additive disclosure when a brainResult arrived carrying no next_gate"
      contains: "next_gate"
  key_links:
    - from: "lib/core/enrichment-queue.cjs"
      to: "Theo orchestration_readiness payload"
      via: "two shape-guarded arms inserted between the readiness_score arm and the final else"
      pattern: "pr.coverage"
    - from: "lib/mcp/brain-router.cjs"
      to: "lib/brain/chain-recommender.cjs closed-vocabulary note idiom"
      via: "a distinct disclosed note rather than a silent fall-through"
      pattern: "note"
---

<objective>
The PREP cut's silent-failure half. Two files, two distinct silent degrades, both fixed additively
so the incumbent cannot enter either new branch.

1. `lib/core/enrichment-queue.cjs` (D-03 consumer 3, FLIP-03). This is the phase's one TRUE silent
   failure: `_maybeCaptureEnrichmentMiss` requires `typeof pr.grounded === 'boolean'` or a finite
   `pr.readiness_score`, Theo emits neither name, both arms miss, `:493` returns
   `{captured: false, reason: 'invalid_probe_result'}`, and `brain-client.cjs:1631` logs only on
   `queued`. Nothing is written anywhere. Two additive arms fix it, not one: Theo's refusal branch
   omits `score`, `inputs`, `evidence` and `framework` ENTIRELY, so a lone `typeof pr.score` check
   would leave `FRAMEWORK_NOT_FOUND` falling through.
2. `lib/mcp/brain-router.cjs` (D-03b, the fourth consumer research found that D-03 never scoped).
   `:307` reads `brainResult.next_gate.options[]`, a shape no Theo tool emits, and falls through to
   the Tier-2 heuristic with the Phase 252-01 disclosure suppressed because that disclosure is
   gated on `!isAvailable()` and availability stays true post-flip.

Purpose: after the flip, a user asking for methodology on a framework Theo does not carry should
see an honest empty answer AND have that gap queued for enrichment. Today they would see a
heuristic guess and the gap would leave no trace.

Output: two files changed, `tests/test-339-enrichment-theo-shapes.cjs` fully green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-PATTERNS.md
@lib/core/enrichment-queue.cjs
@lib/mcp/brain-router.cjs
</context>

<artifacts_this_phase_produces>
Symbols this PLAN creates (new to the repo, excluded from drift verification):

| Symbol | Kind | File |
|--------|------|------|
| `THEO_INPUT_DIMENSIONS` | frozen array of frozen objects | `lib/core/enrichment-queue.cjs` |
| `reason: 'layer_empty'` | new return value (never stored, return-field only) | `lib/core/enrichment-queue.cjs` |
| `probe_provenance` prefix `orchestration_readiness_theo@` | new provenance string | `lib/core/enrichment-queue.cjs` |
| `probe_provenance` prefix `orchestration_readiness_theo_refusal@` | new provenance string | `lib/core/enrichment-queue.cjs` |

`reason: 'layer_empty'` needs no allowlist entry: `reason` is only ever a return field on the
capture result and is never written into a queue entry. Verify that by reading, do not assume it.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the two additive Theo capture arms to lib/core/enrichment-queue.cjs (FLIP-03a/b/c, D-03 consumer 3)</name>

  <files>lib/core/enrichment-queue.cjs</files>

  <behavior>
    - `tests/test-339-enrichment-theo-shapes.cjs`, red on fixtures 1 through 4 at the end of wave 1, goes fully GREEN on all six fixtures.
    - Theo RESOLVED shape with `score: 1` captures, `dimensions_inferred` is `false`, `missing_dimensions` is the exact vector of false inputs, and any input listed in `unsynced_inputs` is excluded from that vector.
    - Theo RESOLVED shape with `score: 3` returns `not_a_miss`. The `> 2` threshold ports as-is with no arithmetic change.
    - Theo REFUSAL shape with `coverage: {matched: 0, total: N greater than 0}` captures.
    - Theo REFUSAL shape with `coverage: {matched: 0, total: 0}` returns `reason: 'layer_empty'` and does NOT capture.
    - Incumbent `{grounded: false}` and `{readiness_score: 1}` both still capture exactly as before.
    - No queue entry anywhere contains a Theo `refusal.detail` string.
  </behavior>

  <read_first>
    - `lib/core/enrichment-queue.cjs:435-520` IN FULL: the capture function, the four locals, the incumbent `grounded` arm at `:471-477` with its DELIBERATE BOOLEAN CONTRACT comment, the `readiness_score` arm at `:478-492`, the `else` at `:493`, the entry assembly with `source: 'live_reach'` at `:502`, and the export at `:519`
    - `lib/core/enrichment-queue.cjs:47-48` (`SOFT_CAP` 500, `HARD_CAP` 1000), `:69` (`ALLOWED_SOURCES`), `:74` (`ALLOWED_DIMENSIONS`), `:186-188` (`isValidReadinessScore`, which accepts null), `:220` and `:355` (`probe_provenance` is a free string), `:378` and `:397` (dedupe by framework name with a `hit_count` bump), `:426-432` (`inferMissingDimensionsFromScore`)
    - `lib/core/enrichment-queue.cjs:465`: the comment describing the incumbent refusal shape. It must be REWRITTEN as part of this task, because it will otherwise describe only half of what the seam now handles. It is also a sweep site under D-12 and this plan owns it
    - `/home/jsagi/Theo/src/mcp/content/orchestration-readiness.ts` lines 44-46, 52-73, 92-101, 106-113, 130, 344-350, 446-449, 479-486, 492-503 (READ ONLY, never edit). The two shapes, the four inputs and their live layers, the five refusal codes, the `NO THRESHOLD, NO VERDICT STRING` rule, and Theo's own reason for omitting `score` on the refusal branch
    - `339-RESEARCH.md` Design 2 IN FULL, including the recommended implementation block, the score semantics, the dimensions mapping, the honesty subtlety about `unsynced_inputs`, and the operational risk paragraph about the queue filling fast during soak
    - `339-PATTERNS.md` section `MODIFIED lib/core/enrichment-queue.cjs two additive arms`
  </read_first>

  <action>
Insert TWO new arms BETWEEN the existing `readiness_score` arm and the final `else` at `:493`.
Copy the existing arms' shape verbatim: shape guard, early `not_a_miss` return, assign the four
locals, then a `probe_provenance` string with an at-sign-suffixed timestamp.

Arm A, Theo RESOLVED. Guard on the PAIR `typeof pr.score === 'number' && Number.isFinite(pr.score)
&& pr.inputs && typeof pr.inputs === 'object'`. Guarding on `score` alone would be a key-presence
test in disguise, because Theo omits `score` entirely on its refusal branch. Inside:
- `if (pr.score > 2) return { captured: false, reason: 'not_a_miss' };`
- `readiness_score = Math.round(pr.score);`
- Read `unsynced_inputs` defensively as an array or an empty array.
- `missing_dimensions` = the `THEO_INPUT_DIMENSIONS` entries whose Theo input is `=== false` AND
  whose input name is NOT in `unsynced_inputs`, mapped to their `dimension` name.
- `dimensions_inferred = false;` because the vector is exact, not a positional guess.
- `probe_provenance = 'orchestration_readiness_theo@' + nowTs;`

Arm B, Theo REFUSAL. Guard on `pr.refusal && typeof pr.refusal === 'object' && pr.coverage &&
typeof pr.coverage === 'object' && typeof pr.coverage.matched === 'number' && typeof
pr.coverage.total === 'number'`. Inside, in this order:
- `if (pr.coverage.matched > 0) return { captured: false, reason: 'not_a_miss' };`
- `if (pr.coverage.total === 0) return { captured: false, reason: 'layer_empty' };`
- `readiness_score = null;`
- `missing_dimensions = ['structure'];`
- `dimensions_inferred = true;`
- `probe_provenance = 'orchestration_readiness_theo_refusal@' + refusalCode + '@' + nowTs;` where
  `refusalCode` is `pr.refusal.code` when it is a string, otherwise the literal `unknown`.

CANON PART 8, absolute: embed only `pr.refusal.code`, which is a closed vocabulary
(`FRAMEWORK_NOT_FOUND`, `ALIAS_CYCLE`, `ALIAS_FORK`, and two more from `resolveFramework`). NEVER
embed `pr.refusal.detail`. `detail` is a free-form server string and the same rule
`_unrecognizedQueryShape` already states applies here: key NAMES only, never VALUES.

Add the mapping as ONE frozen constant beside `ALLOWED_DIMENSIONS` at `:74`, named
`THEO_INPUT_DIMENSIONS`, an array of frozen objects each with `input` and `dimension`:
`has_structure` to `structure`, `has_ordering` to `flow`, `has_technique` to `techniques`,
`pattern_known` to `pattern_type`.

Rewrite the comment at `:465` so it describes BOTH the incumbent refusal shape it names today AND
the two Theo shapes now handled below it. Keep the DELIBERATE BOOLEAN CONTRACT comment on the
`grounded` arm exactly as it is; it is still true of that arm.

Comment-as-contract on each new arm, house style. Each must name: phase id 339, date 2026-09-03,
the observed payload shape (cite the Theo source file and line range), and what it deliberately
does NOT key on. Three things must be written explicitly, because a later reader will otherwise
"fix" them:
1. Theo's practical score ceiling is 3, not 4: `pattern_known` is `orchestration_status ===
   PROMOTED_ORCHESTRATION_STATUS` and Theo's own header says that answer is `false` for virtually
   every Framework. The existing `> 2` threshold is therefore ALREADY CORRECT and must not be
   rescaled.
2. The two zeros are DIFFERENT ANSWERS and are never collapsed. `{matched: 0, total: N greater
   than 0}` means canon has N frameworks and none is yours: a real miss. `{matched: 0, total: 0}`
   means the layer itself is empty: not a miss about this framework, and queueing it would blame
   the framework for an empty graph.
3. A dimension whose Theo input appears in `unsynced_inputs` is FILTERED OUT. Theo declares that
   input answered by its own local proxy rather than by the contract's source, so reporting the
   dimension as missing on its strength would queue enrichment for a gap that may not exist.

Also record the operational expectation as a comment near the arms so the soak observer is not
surprised: post-flip, nearly every `orchestration_readiness` call will score 0 or 1 and therefore
capture, because Theo's own header reports 148 of 149 Frameworks with zero technique linkage and
roughly 3 of 149 carrying structure at all. The queue is bounded (`SOFT_CAP` warns at 500,
`HARD_CAP` rejects at 1000) and `enqueue` dedupes by framework name with a `hit_count` bump, so
the blast radius is one entry per distinct framework. That is canon thinness correctly measured,
not a leak.

Verify by reading, and record in the SUMMARY: `entry.source` stays `'live_reach'` (already in
`ALLOWED_SOURCES`); `readiness_score: null` passes `isValidReadinessScore`; `normalized` resolves
to `true` on a Theo payload that has no `normalized` key; and `reason: 'layer_empty'` is only ever
a return field and is never stored, so it needs no allowlist entry.
  </action>

  <verify>
    <automated>node tests/test-339-enrichment-theo-shapes.cjs && grep -Fq 'THEO_INPUT_DIMENSIONS' lib/core/enrichment-queue.cjs && grep -Fq "reason: 'layer_empty'" lib/core/enrichment-queue.cjs && ! grep -q 'refusal.detail' lib/core/enrichment-queue.cjs && echo CAPTURE_OK</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-339-enrichment-theo-shapes.cjs` exits 0 with all six fixtures green.
    - `grep -c 'refusal\.detail' lib/core/enrichment-queue.cjs` returns 0. Only `refusal.code` is read.
    - The two arms are inserted BETWEEN the `readiness_score` arm and the final `else`: `git diff` shows the `else { return { captured: false, reason: 'invalid_probe_result' }; }` block still last and unchanged.
    - The incumbent `grounded` arm and its DELIBERATE BOOLEAN CONTRACT comment are byte-identical to before.
    - `THEO_INPUT_DIMENSIONS` sits adjacent to `ALLOWED_DIMENSIONS` and has exactly four entries.
    - All three explicit comment facts are present in the source: the ceiling-is-3 threshold note, the two-zeros distinction, and the `unsynced_inputs` filter reason.
    - The SUMMARY records the four verified storage-contract facts by line number.
    - `grep -c` for the em-dash character returns 0.
  </acceptance_criteria>

  <done>The phase's one true silent failure is closed, and the fix is provably unreachable by an incumbent payload, so it is safe to ship in the PREP cut ahead of the flip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Disclose the Tier-3 router miss when a brainResult arrived without next_gate (D-03b)</name>

  <files>lib/mcp/brain-router.cjs</files>

  <behavior>
    - When `brainResult` is present but carries no `next_gate`, the router's return carries a distinct, disclosed note naming that cause, instead of being indistinguishable from "the Brain never answered".
    - When `brainResult` carries `next_gate` (every incumbent response), behavior is byte-identical to today. The new branch cannot be entered by an incumbent payload.
    - The existing Phase 252-01 disclosure block at `:411-431`, gated on `!isAvailable()`, is untouched and still fires on its own condition.
    - The new field is ADDITIVE: no existing consumer destructuring `{chain, confidence, source, reasoning, target_sections}` can regress.
  </behavior>

  <read_first>
    - `lib/mcp/brain-router.cjs:295-330`: the Tier-3 path, the `next_gate.options[]` read at `:307-309`, and the `directive.guided.framework` read at `:310-312`
    - `lib/mcp/brain-router.cjs:400-435`: the Phase 252-01 additive-disclosure block, its comment contract ("ADDITIVE field ... never replaces localRec's shape ... cannot regress anyone destructuring"), its best-effort `try` wrapper, and its `!brainClient.isAvailable()` gate at `:429` which is why it does NOT fire post-flip
    - `lib/brain/chain-recommender.cjs:548-567`: `_noteForCallToolResult` and the `_disclosureOffer`, the closed-vocabulary note idiom this task copies
    - `docs/254-NOTE-theo-adaptation-list-additions.md` section 2: the recorded grep proving `next_gate` is an INCUMBENT-ONLY shape, `grep -rn "next_gate" /home/jsagi/Theo/src/mcp/content/*.ts` returns nothing, confirmed 2026-09-02
    - `bin/mindrian-brain-mcp-client.cjs:197-199`: the shim's `wrapDirective` DirectiveEnvelope, which means the CLI path may still see a `directive` skeleton post-flip even when Theo's payload has no `mode_signals`. What it will NOT see is `next_gate.options`, which is what the ranked chain is built from
    - `339-RESEARCH.md` section `Consumer 4: lib/mcp/brain-router.cjs Tier 3, silent degrade to heuristic` IN FULL, and Open Question 1
    - `339-PATTERNS.md` section `MODIFIED lib/mcp/brain-router.cjs additive disclosure`
  </read_first>

  <action>
Add ONE additive branch. Do not adapt the shape and do not build a Theo chain reader; that is not
this phase's scope and D-03 deliberately leaves the chain path alone.

At the Tier-3 miss, distinguish two causes that are currently indistinguishable:
- `brainResult` is absent or falsy: the Brain never answered. Existing behavior, unchanged.
- `brainResult` is present and truthy but `brainResult.next_gate` is absent or its `options` is not
  an array: the Brain ANSWERED and the response carried no gate shape. This is the new branch.

In the new branch, attach a distinct disclosed note to the returned recommendation as an ADDITIVE
field, following the Phase 252-01 block's own contract: never replace the existing shape, never
mutate a field an existing consumer reads, wrap in a best-effort `try` that never blocks the
return. Use the closed-vocabulary note idiom from `chain-recommender.cjs:548-557`: a short
enum-shaped string, not free prose and not an interpolated server value.

Canon Part 8: the note must be a LOCAL closed-vocabulary constant. Do not interpolate anything
from `brainResult` into it. A Theo response body value must never reach a log line or a
user-visible warning; key names only, never values.

Comment-as-contract, house style. Name: phase id 339, date 2026-09-03, the observed shape it keys
on (`brainResult` truthy AND `next_gate.options` not an array), and what it deliberately does NOT
key on and does NOT do. Write these facts explicitly:
- `next_gate` is an INCUMBENT-ONLY shape; no Theo tool emits it, per the recorded grep in
  `docs/254-NOTE-theo-adaptation-list-additions.md` section 2.
- The incumbent ALWAYS carries `next_gate`, so this branch is unreachable against the incumbent
  and the change is safe to ship in the PREP cut, ahead of the flip.
- The Phase 252-01 disclosure at `:411-431` does NOT cover this case, because it is gated on
  `!isAvailable()` and availability stays TRUE post-flip. That is precisely why the degrade is
  silent today and why a second, differently-gated disclosure is needed rather than a widening of
  the first.
- This is a DISCLOSURE, not a shape adaptation. Reading Theo's chain shape is a named follow-up
  (D-03 consumer 2, `lib/brain/chain-recommender.cjs`), deliberately out of this phase.

Do not touch `lib/brain/chain-recommender.cjs`.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('lib/mcp/brain-router.cjs','utf8'); if(!/next_gate/.test(s)) process.exit(1); const n=(s.match(/isAvailable\(\)/g)||[]).length; console.log('isAvailable occurrences:', n); console.log('OK')" && node scripts/build-orchestration-projection.cjs --check && node scripts/build-connector-registry.cjs --check</automated>
  </verify>

  <acceptance_criteria>
    - The new branch fires only when `brainResult` is truthy AND `next_gate.options` is not an array; a unit-level `node -e` exercise with an incumbent-shaped `brainResult` (carrying `next_gate.options: []`) produces a return object byte-identical to the pre-change output.
    - The Phase 252-01 block at `:411-431` is byte-identical to before: `git diff` shows no hunk in that range.
    - The attached field is new; no existing key in the returned object changed name, type or value.
    - The note string is a local constant with no interpolation from `brainResult`.
    - `node scripts/build-connector-registry.cjs --check` and `node scripts/build-orchestration-projection.cjs --check` both pass.
    - `grep -c` for the em-dash character returns 0.
  </acceptance_criteria>

  <done>The fourth silent-degrade consumer, which D-03 never scoped and research found, is disclosed rather than shipped unnamed. Post-flip, a Tier-3 miss says why it missed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theo response body -> local enrichment queue on disk | server-authored strings cross into a persisted local artifact here; this is the strictest Part 8 mirror surface in the phase |
| Theo response body -> router note / log line | a response value reaching a user-visible warning would leak server content the boundary just governed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-08 | Information Disclosure | a Theo `refusal.detail` free-form string stored in a queue entry's `probe_provenance` | mitigate | only `pr.refusal.code`, a closed vocabulary, is embedded; `tests/test-339-enrichment-theo-shapes.cjs` reads the written entry back from disk and asserts the fixture `detail` string is absent; `grep -c 'refusal\.detail'` on the file must return 0 |
| T-339-03 | Information Disclosure | a Theo response value interpolated into the router's disclosed note | mitigate | the note is a local closed-vocabulary constant with zero interpolation, following the `_noteForCallToolResult` idiom |
| T-339-16 | Denial of Service | the enrichment queue filling to `HARD_CAP` during soak and being read as a leak | accept | bounded by design (`SOFT_CAP` 500 warns, `HARD_CAP` 1000 rejects) and deduped by framework name with a `hit_count` bump, so the ceiling is one entry per distinct framework; the expectation is written as a code comment and carried into the CHANGELOG by plan 339-10 |
| T-339-17 | Repudiation | the two zeros collapsed, blaming a framework for an empty graph | mitigate | two separate early returns with distinct reasons, and a test that asserts both directions in the same file so neither can be half-tested |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | zero package installs in this plan and in the phase |
</threat_model>

<verification>
- `node tests/test-339-enrichment-theo-shapes.cjs` passes on all six fixtures.
- `bash tests/run-all-339.sh` shows the enrichment arm green.
- `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs`, `node scripts/check-shape-declaration.cjs` all pass.
- `node tests/test-252-*.cjs` style suites covering the Phase 252-01 disclosure still pass, proving the existing disclosure was not disturbed. Discover them with `ls tests/ | grep 252` and run each.
- `grep -c 'refusal\.detail' lib/core/enrichment-queue.cjs` returns 0.
</verification>

<success_criteria>
- FLIP-03a, FLIP-03b and FLIP-03c are green.
- Neither new branch can be entered by an incumbent payload, so the PREP cut stays safe against both Brains.
- The two silent degrades this phase found are now one captured miss and one disclosed note.
- No em-dashes in either file.
</success_criteria>

<output>
Create `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-05-SUMMARY.md` when done. Record the four verified storage-contract facts with line numbers, and state plainly that D-03b (the fourth consumer) was folded into the PREP cut as a disclosure rather than a shape adaptation.
</output>
