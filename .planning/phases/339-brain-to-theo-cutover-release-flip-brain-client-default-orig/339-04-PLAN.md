---
phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig
plan: 04
type: execute
wave: 2
depends_on: ["339-01", "339-02", "339-03"]
files_modified:
  - lib/core/brain-client.cjs
autonomous: true
requirements: [FLIP-02, FLIP-03, FLIP-05]
canon_parts: [5, 7, 8]

must_haves:
  truths:
    - "The problem-type vocabulary the plugin sends is selected by the resolved origin, so a MINDRIAN_BRAIN_URL change moves vocabulary and URL together"
    - "The mechanism is inert against the incumbent: nothing about today's wire traffic changes when this ships"
    - "The brain_schema memo cannot serve a schema fetched from a different origin than the one currently resolved"
    - "The enrichment capture log line reports a readiness number for a Theo payload instead of always null"
  artifacts:
    - path: "lib/core/brain-client.cjs"
      provides: "Origin-derived alias selector, origin-keyed schema memo, dual-shape capture log"
      contains: "THEO_ORIGINS"
  key_links:
    - from: "lib/core/brain-client.cjs _brainProblemTypeAliases"
      to: "lib/core/brain-client.cjs BRAIN_URL"
      via: "the origin-keyed selector, so one switch moves vocabulary and URL together"
      pattern: "THEO_ORIGINS"
    - from: "lib/core/brain-client.cjs schema()"
      to: "lib/core/brain-client.cjs getBrainUrl()"
      via: "_schemaCacheOrigin compared before the memo is served"
      pattern: "_schemaCacheOrigin"
---

<objective>
The PREP cut's `brain-client.cjs` half. Three additive, incumbent-safe changes in one file, each
provably inert against the current backend and each load-bearing the moment line 24 moves.

1. The origin-derived alias table (D-03 consumer 1, FLIP-02). Two frozen tables plus one
   origin-keyed selector. This is the change that makes the FLIP cut exactly one line: the
   selector reads `BRAIN_URL` and picks Theo's vocabulary automatically the moment line 24
   changes, with no second edit anywhere.
2. The origin-keyed `brain_schema` memo (D-13 as corrected, FLIP-05). Three lines. No
   `flushSchemaMemo()` is created, deliberately, and 339-02's test asserts its absence.
3. The enrichment capture log line recognizing Theo's `score` alongside the incumbent's
   `readiness_score` (part of FLIP-03). One line.

Purpose: the map ITSELF is what breaks the post-flip match. Theo matches
`toLower(m.id) = toLower($problemType)` and nothing more, so the plugin's `Undefined Problem`
matches nothing while an UNMAPPED `Undefined` would have matched Theo's `UnDefined`. Every mapped
type would return a SUCCESS carrying an empty chain and a `PROBLEM_TYPE_NOT_FOUND` refusal. This
plan fixes that before the flip, not with it.

Output: one file changed, three tests turned green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-PATTERNS.md
@lib/core/brain-client.cjs
</context>

<artifacts_this_phase_produces>
Symbols this PLAN creates (new to the repo, excluded from drift verification):

| Symbol | Kind | File |
|--------|------|------|
| `THEO_ORIGINS` | frozen array constant, EXPORTED | `lib/core/brain-client.cjs` |
| `BRAIN_PROBLEM_TYPE_ALIASES_INCUMBENT` | frozen object constant | `lib/core/brain-client.cjs` |
| `BRAIN_PROBLEM_TYPE_ALIASES_THEO` | frozen object constant | `lib/core/brain-client.cjs` |
| `_brainProblemTypeAliases` | private selector function | `lib/core/brain-client.cjs` |
| `_schemaCacheOrigin` | module-scope let | `lib/core/brain-client.cjs` |

Symbols this plan deliberately does NOT create: `flushSchemaMemo`. D-13 as originally written
asked for a flush on the flip cut; research corrected the premise (`BRAIN_URL` is a module-scope
const and `_schemaCache` is process-local, so no process can observe an origin change and an
exported flush would have zero callers). `tests/test-339-schema-memo-origin-keyed.cjs` asserts its
absence.

Symbol renamed away: `BRAIN_PROBLEM_TYPE_ALIASES` (the single table at `:1713`) is replaced by the
two named tables above. Any reader looking for the old identifier will not find it; that is
intentional and `tests/test-254-normalize-roundtrip-probe.cjs` Arm 4 enforces the new names.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the single alias table with two origin-selected tables (FLIP-02, D-03 consumer 1)</name>

  <files>lib/core/brain-client.cjs</files>

  <behavior>
    - `tests/test-254-normalize-roundtrip-probe.cjs` Arms 4 and 5, red at the end of wave 1, go GREEN.
    - Both tables carry the identical 8 keys: `undefined`, `udp`, `ill-defined`, `ill_defined`, `idp`, `well-defined`, `well_defined`, `wdp`.
    - The incumbent table's three values are `Undefined Problem`, `Ill-Defined Problem`, `Well-Defined Problem`; the Theo table's three are `UnDefined`, `IllDefined`, `WellDefined`.
    - With `BRAIN_URL` at the incumbent origin, the selector returns the incumbent table: today's wire traffic is byte-identical to before this change.
    - The pass-through at the end of `_normalizeBrainProblemType` still returns the trimmed input, so `Wicked`, `Trinity` and `Compass` continue to pass through unmapped on BOTH origins.
    - `PROBLEM_TYPE_HANDLE_RE` is untouched.
  </behavior>

  <read_first>
    - `lib/core/brain-client.cjs:1700-1790` IN FULL: the current single table at `:1713-1722` with its "deliberately NOT the same map as chain-recommender" comment, `_normalizeBrainProblemType` at `:1734-1744` including `PROBLEM_TYPE_HANDLE_RE` at `:1737` and the pass-through `return trimmed;` at `:1743`, and `recommendChain`'s send at `:1778`
    - `lib/core/brain-client.cjs:1155-1176`: `getBrainUrl()` and its docblock, which already states the one-place-decides rule
    - `lib/core/brain-client.cjs:927-945`: the shipped `brain_query` dual-shape branch (commit `21fdd7bc`). D-04 names this the mandatory pattern: guard on shape, both shapes in ONE adjacent block, a comment naming the Theo source file, a loud fallback last
    - `/home/jsagi/Theo/src/mcp/content/recommend-chain.ts` lines 27 to 47 (the six ids), 59 to 69 (the matching rule and Theo's own statement that this is plugin-side work), 320 to 321 (the implemented `WHERE toLower(m.id) = toLower($problemType)`), and 530 to 548 (the unknown-type response envelope). READ ONLY, never edit
    - `339-RESEARCH.md` Design 1 IN FULL, especially `Recommended mechanism` and `Why not the retry-on-PROBLEM_TYPE_NOT_FOUND alternative`
    - `339-PATTERNS.md` section `MODIFIED lib/core/brain-client.cjs alias-table selector`
    - `tests/test-254-normalize-roundtrip-probe.cjs` Arms 4 and 5 as extended by plan 339-03: they are the target state
  </read_first>

  <action>
Replace the single `BRAIN_PROBLEM_TYPE_ALIASES` table at `:1713-1722` with two frozen tables plus
one selector, per D-03 (locked) and the origin-derived mechanism named as Claude's discretion in
CONTEXT and recommended by research.

Declare, in this order:
- `BRAIN_PROBLEM_TYPE_ALIASES_INCUMBENT`, frozen, the exact 8 key-to-value pairs that exist today,
  moved verbatim.
- `BRAIN_PROBLEM_TYPE_ALIASES_THEO`, frozen, the same 8 keys mapped onto `UnDefined`,
  `IllDefined` and `WellDefined`.
- `THEO_ORIGINS`, a frozen ARRAY containing exactly `https://theo-mcp.onrender.com`. An array and
  not a bare string literal, so a Theo staging origin is a one-line addition rather than a
  re-architecture. Export it from `module.exports` so
  `lib/core/doctor/class-m-brain-smoke.cjs` can key its per-origin node floor on the SAME set in
  plan 339-12, rather than minting a second copy of the origin list.
- `_brainProblemTypeAliases()`, a private function returning the Theo table when
  `THEO_ORIGINS.indexOf(BRAIN_URL) !== -1` and the incumbent table otherwise. Anything not in the
  set gets the incumbent vocabulary, which is the correct default while line 24 names the
  incumbent and the conservative default afterwards.

Then change exactly two lines inside `_normalizeBrainProblemType`: introduce
`const table = _brainProblemTypeAliases();`, then use `table` in both the `hasOwnProperty` check
and the return. Everything else in that function is UNTOUCHED, including the
`PROBLEM_TYPE_HANDLE_RE` gate (Canon Part 5, the enum-only wire gate installed by quick
260819-c8j) and the final `return trimmed;`.

KEEP and EXTEND the existing "deliberately NOT the same map as this file's sibling,
`lib/brain/chain-recommender.cjs`'s `PROBLEM_TYPE_ALIASES`" comment. It stays true of both tables
and Arm 5 still proves it.

Comment-as-contract, house style, on the new block. It must name: phase id 339, the date
2026-09-03, the observed payload shape it keys on (Theo's `DomainConcept` ids verbatim from
`/home/jsagi/Theo/src/mcp/content/recommend-chain.ts:47`, matched by
`toLower(m.id) = toLower($problemType)` at `:320-321` and NOTHING MORE: no trimming, no hyphen
folding, no suffix stripping, no fuzzy fallback), and what it deliberately does NOT key on (it
does not key on a response, does not retry on `PROBLEM_TYPE_NOT_FOUND` using
`available_problem_types`, and does not sniff the server; a runtime retry would cost a round trip
on every miss, would paper over a vocabulary bug at runtime, and would break the property that a
`MINDRIAN_BRAIN_URL` revert moves vocabulary and URL together). Also state the Part 8 check: the
tables carry problem-type handles only, never room content.

Do not touch `lib/brain/chain-recommender.cjs`. D-03 leaves consumer 2 as-is because it degrades
DISCLOSED rather than silently.
  </action>

  <verify>
    <automated>node tests/test-254-normalize-roundtrip-probe.cjs && grep -Fq "const THEO_ORIGINS = Object.freeze(['https://theo-mcp.onrender.com'])" lib/core/brain-client.cjs && grep -Fq 'THEO_ORIGINS,' lib/core/brain-client.cjs && node -e "const b=require('./lib/core/brain-client.cjs'); if(!Array.isArray(b.THEO_ORIGINS)) process.exit(1); console.log('EXPORT_OK', b.THEO_ORIGINS.join(','))"</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-254-normalize-roundtrip-probe.cjs` exits 0. Arms 4 and 5 are green.
    - `lib/core/brain-client.cjs` contains `BRAIN_PROBLEM_TYPE_ALIASES_INCUMBENT`, `BRAIN_PROBLEM_TYPE_ALIASES_THEO`, `THEO_ORIGINS` and `_brainProblemTypeAliases`, and no longer contains the bare identifier `BRAIN_PROBLEM_TYPE_ALIASES` outside those names.
    - `THEO_ORIGINS` is exported: `node -e "console.log(require('./lib/core/brain-client.cjs').THEO_ORIGINS)"` prints an array with one element.
    - `PROBLEM_TYPE_HANDLE_RE` and the final `return trimmed;` in `_normalizeBrainProblemType` are byte-identical to before: `git diff` shows no hunk touching them.
    - Inertness against the incumbent, proven: `node -e` requiring the module with no `MINDRIAN_BRAIN_URL` set and calling `recommendChain`'s normalization path through a public entry returns `Undefined Problem` for input `undefined`, exactly as before.
    - `node tests/test-247-contract-client.cjs` still passes untouched (it is envelope-insensitive and the flip record says it stays green).
    - `grep -c` for the em-dash character on `lib/core/brain-client.cjs` returns 0.
  </acceptance_criteria>

  <done>The plugin's problem-type vocabulary follows the origin. Changing line 24 in the FLIP cut, or setting `MINDRIAN_BRAIN_URL` on one install, moves both the URL and the vocabulary in one move.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Key the brain_schema memo on the resolved origin (FLIP-05, D-13 as corrected)</name>

  <files>lib/core/brain-client.cjs</files>

  <behavior>
    - `tests/test-339-schema-memo-origin-keyed.cjs`, red at the end of wave 1, goes GREEN.
    - `_schemaCacheOrigin` is declared adjacent to `_schemaCacheAt`.
    - `schema()` compares `_schemaCacheOrigin` against `getBrainUrl()` BEFORE returning the memo.
    - `_schemaCacheOrigin` is assigned in the same block that assigns `_schemaCacheAt`.
    - The 30-minute TTL and the Phase 247-02 sentinel guard are unchanged.
    - No `flushSchemaMemo` function is created or exported.
  </behavior>

  <read_first>
    - `lib/core/brain-client.cjs:1045-1075`: the memo block in full, including the Phase 247-02 audit-fix comment that must survive verbatim
    - `339-RESEARCH.md` Design 4 IN FULL. The premise correction matters more than the code: the memo is in-memory and process-local, `BRAIN_URL` is a module-scope const resolved once at require time, so within a single process the origin CANNOT change and the failure mode D-13 guards against cannot occur through the shipped code path. Key-by-origin ships anyway as defense in depth that makes the invariant explicit and testable, at a cost of three lines
    - `339-RESEARCH.md` Design 4 subsection `Flag for the navigator`: this is a stated deviation from D-13 as written. D-13 says the flush rides the FLIP cut; the corrected reading is that there is no flush to schedule and the origin key ships entirely in PREP. Record the deviation in the SUMMARY, do not implement something narrower silently
    - `339-PATTERNS.md` section `MODIFIED lib/core/brain-client.cjs schema memo`
  </read_first>

  <action>
Add `let _schemaCacheOrigin = null;` immediately beside `let _schemaCacheAt = 0;`.

In `schema()`, extend the cache-hit guard to also require
`_schemaCacheOrigin === getBrainUrl()`, placed BEFORE the TTL comparison in the same `if`. In the
success block that assigns `_schemaCache` and `_schemaCacheAt`, add
`_schemaCacheOrigin = getBrainUrl();` as a third assignment in the same block.

Do not create `flushSchemaMemo()`. Do not export anything new from this task.

Comment-as-contract, house style, on the three new lines. It must name: phase id 339, date
2026-09-03, the observed shape it keys on (the resolved origin string returned by `getBrainUrl()`,
compared by identity), and what it deliberately does NOT key on. State the correction plainly in
the comment so the next reader does not re-derive it: `BRAIN_URL` is a module-scope const, so no
process can observe an origin change today and this guard is provably inert against the incumbent
(the comparison is always true). It ships as defense in depth and becomes load-bearing the day
anything makes the origin mutable per process, which is exactly the class of future change that
would otherwise reintroduce the bug silently. Also state, in the same comment, that no
`flushSchemaMemo()` exists BY DECISION: an exported flush would have zero callers, which Canon
Part 7 and this repo's dead-surface discipline both argue against, and
`tests/test-339-schema-memo-origin-keyed.cjs` asserts its absence.

The flip-day addendum field in Theo's `09-12-CLOSEOUT-STAGING.md` section F is answered by this
task, verbatim, and plan 339-14 reports it: `no flush needed: memo is process-local, keyed by
origin since beta.17`.
  </action>

  <verify>
    <automated>node tests/test-339-schema-memo-origin-keyed.cjs && grep -Fq '_schemaCacheOrigin' lib/core/brain-client.cjs && ! grep -q 'flushSchemaMemo' lib/core/brain-client.cjs && echo MEMO_OK</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-339-schema-memo-origin-keyed.cjs` exits 0.
    - `grep -c 'flushSchemaMemo' lib/core/brain-client.cjs` returns 0.
    - The Phase 247-02 sentinel-guard comment and its `result.error` check are byte-identical to before: `git diff` shows the hunk adds three lines and modifies one `if` condition, nothing else.
    - `SCHEMA_CACHE_TTL_MS = 30 * 60 * 1000` is unchanged.
    - The SUMMARY records the D-13 deviation explicitly, in the wording from `339-RESEARCH.md` Design 4 `Flag for the navigator`, so Session T can correct the premise if it was based on a mechanism this research did not find.
    - `grep -c` for the em-dash character returns 0.
  </acceptance_criteria>

  <done>The memo's origin invariant is explicit and testable, and the decision NOT to build a flush is recorded in the code, in a test, and in the SUMMARY rather than being discovered later as a missing feature.</done>
</task>

<task type="auto">
  <name>Task 3: Teach the enrichment capture log line Theo's score field (FLIP-03)</name>

  <files>lib/core/brain-client.cjs</files>

  <read_first>
    - `lib/core/brain-client.cjs:1611-1645`: `_isCapturableResult` at `:1617` (which requires `!result.error`), the `if (captureResult && captureResult.queued)` guard at `:1631`, the `_logEventBestEffort(opts.db, 'enrichment_queue_captured', {` call at `:1632`, and the `readiness_score:` expression at `:1634`
    - `339-RESEARCH.md` Design 2 subsection `The log line`: against a Theo payload the current expression is always `null`, which makes the enrichment event unreadable
    - `339-RESEARCH.md` Design 2 subsection on `_isCapturableResult`: Theo's honest-empty payloads carry `refusal`, not `error`, so they pass the gate unchanged. No change is needed there, but the fact must be verified rather than assumed
    - `lib/core/brain-client.cjs:927-945`: the D-04 dual-shape precedent
  </read_first>

  <action>
Change ONE expression at `:1634`. Today it reads the incumbent's `result.readiness_score` and
coerces anything else to `null`. Extend it to recognize Theo's `result.score` as a second shape in
the SAME expression, following D-04: guard on `typeof === 'number'` for each, never on key
presence, and keep `null` as the final fallback so an unrecognized shape is still honestly null
rather than a coerced zero.

Order matters: check `result.readiness_score` first, then `result.score`. The incumbent's field
wins when both are somehow present, which keeps today's log bytes identical.

Verify, do not assume, that `_isCapturableResult` at `:1617` admits a Theo refusal payload. Read
it, confirm that a payload carrying `refusal` but no `error` passes, and record the confirmation
in the SUMMARY. If it does NOT pass, stop and report: that would make the whole D-03 fix
unreachable and it is a finding, not something to patch around inside this task.

Comment-as-contract on the changed line: phase id 339, date 2026-09-03, the observed shape
(Theo's `orchestration_readiness` payload carries `score`, an integer 0 to 4 whose practical
ceiling is 3, from `/home/jsagi/Theo/src/mcp/content/orchestration-readiness.ts:494`), and what
it deliberately does NOT do (it does not rescale, does not default a missing score to 0, and does
not read `coverage`; the coverage semantics live in the capture path in
`lib/core/enrichment-queue.cjs`, which plan 339-05 owns).

Do not touch `lib/core/enrichment-queue.cjs` in this plan. It belongs to 339-05 and the two plans
run in the same wave.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('lib/core/brain-client.cjs','utf8'); const i=s.indexOf('enrichment_queue_captured'); const w=s.slice(i,i+900); if(!/readiness_score/.test(w)||!/result\.score/.test(w)){console.error('log line not extended');process.exit(1)} console.log('LOG_OK')" && node tests/test-247-contract-client.cjs</automated>
  </verify>

  <acceptance_criteria>
    - The `readiness_score:` expression inside the `enrichment_queue_captured` log payload recognizes both `result.readiness_score` and `result.score`, in that order, each guarded by `typeof === 'number'`.
    - `null` is still the final fallback; no branch coerces a missing score to 0.
    - The SUMMARY records the verified answer on `_isCapturableResult`: does a payload carrying `refusal` but no `error` pass its gate, yes or no, with the line number read.
    - `git diff` on this task shows exactly one changed expression plus its comment. No other line in the file moved.
    - `node tests/test-247-contract-client.cjs` still passes.
    - `grep -c` for the em-dash character returns 0.
  </acceptance_criteria>

  <done>When 339-05's capture arms land, the resulting enrichment event carries a readable readiness number instead of a permanent null, so the soak window can be observed rather than guessed at.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin -> remote graph wire | the alias table's values are the only vocabulary that crosses; Canon Part 8 permits generic handles only |
| module load -> process lifetime | `BRAIN_URL` is resolved once at require time; every invariant in this plan rests on that immutability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-14 | Information Disclosure | the alias table carrying anything other than a generic problem-type handle | mitigate | `PROBLEM_TYPE_HANDLE_RE` at `:1737` is untouched and remains the enum-only wire gate; both tables are frozen and contain only closed-vocabulary names; `tests/test-254` Arm 4 pins both value sets exactly |
| T-339-01 | Tampering | a second origin literal introduced by `THEO_ORIGINS` | mitigate | `THEO_ORIGINS` is exported so `class-m-brain-smoke.cjs` consumes it in 339-12 rather than declaring a second copy; `tests/test-339-origin-single-source.cjs` allowlists `brain-client.cjs` as the one file permitted to hold origin literals |
| T-339-11 | Repudiation | a future `flushSchemaMemo()` added without revisiting the corrected premise | mitigate | the reason is written in the code comment AND asserted absent by `tests/test-339-schema-memo-origin-keyed.cjs` |
| T-339-15 | Denial of Service | a runtime retry loop on `PROBLEM_TYPE_NOT_FOUND` costing a round trip per miss | accept | explicitly not built; the origin-keyed table is chosen instead and the reason is written in the comment-as-contract block |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | zero package installs in this plan and in the phase |
</threat_model>

<verification>
- `node tests/test-254-normalize-roundtrip-probe.cjs` passes (Arms 4 and 5 green).
- `node tests/test-339-schema-memo-origin-keyed.cjs` passes.
- `node tests/test-247-contract-client.cjs` passes untouched.
- `node tests/test-339-origin-single-source.cjs` is unchanged in outcome by this plan: `brain-client.cjs` is allowlisted.
- `bash tests/run-all-339.sh` shows two more green arms than at the end of wave 1.
- `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs`, `node scripts/check-shape-declaration.cjs` all pass.
</verification>

<success_criteria>
- FLIP-02 and FLIP-05 are green.
- The FLIP cut stays exactly one line plus docblock plus CHANGELOG, because the vocabulary switch already shipped here.
- Every change is provably inert against the incumbent, so the PREP cut is safe against both Brains by construction (D-01, D-07).
- No em-dashes in `lib/core/brain-client.cjs`.
</success_criteria>

<output>
Create `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-04-SUMMARY.md` when done. Record: the D-13 deviation statement verbatim, and the verified answer on `_isCapturableResult` with its line number.
</output>
