---
phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-339-enrichment-theo-shapes.cjs
  - tests/test-339-update-path-single-source.cjs
  - tests/test-339-schema-memo-origin-keyed.cjs
autonomous: true
requirements: [FLIP-03, FLIP-04, FLIP-05]
canon_parts: [7, 8]

must_haves:
  truths:
    - "A Theo-shaped readiness payload, in BOTH of Theo's two shapes, is proven to reach the enrichment capture path before any capture code is written"
    - "The two-zeros distinction is pinned: {matched:0,total:N>0} captures, {matched:0,total:0} does not, and no test lets them collapse"
    - "The two-command update path is proven to live in exactly one place, cross-checked against .claude/includes/release-process.md"
    - "The brain_schema memo is proven to compare the resolved origin before serving a cached schema"
  artifacts:
    - path: "tests/test-339-enrichment-theo-shapes.cjs"
      provides: "FLIP-03a/b/c: five fixtures driven through the exported captureReadinessMiss"
      contains: "layer_empty"
      min_lines: 120
    - path: "tests/test-339-update-path-single-source.cjs"
      provides: "FLIP-04: three-way drift check across release-process.md, update-path.cjs and the rendered refusal copy"
      contains: "release-process.md"
      min_lines: 70
    - path: "tests/test-339-schema-memo-origin-keyed.cjs"
      provides: "FLIP-05: structural proof that schema() compares _schemaCacheOrigin before the early return"
      contains: "_schemaCacheOrigin"
      min_lines: 50
  key_links:
    - from: "tests/test-339-enrichment-theo-shapes.cjs"
      to: "lib/core/enrichment-queue.cjs captureReadinessMiss"
      via: "the exported test entry point at :519"
      pattern: "captureReadinessMiss"
    - from: "tests/test-339-update-path-single-source.cjs"
      to: ".claude/includes/release-process.md"
      via: "byte-exact extraction of the two fenced commands"
      pattern: "plugin marketplace update"
---

<objective>
Wave 0 tests-first, part 2 of 3 (GSD wave 1). Three new `.cjs` test files, one per PREP-cut
adaptation, each written before its implementation exists.

- `tests/test-339-enrichment-theo-shapes.cjs` (FLIP-03a/b/c) proves the enrichment capture seam
  handles Theo's TWO payload shapes without regressing the incumbent's two.
- `tests/test-339-update-path-single-source.cjs` (FLIP-04) proves the two-command update path
  lives in exactly one constant module and matches `.claude/includes/release-process.md`
  byte for byte.
- `tests/test-339-schema-memo-origin-keyed.cjs` (FLIP-05) proves structurally that `schema()`
  compares `_schemaCacheOrigin` before serving from the memo.

Purpose: the enrichment miss is the phase's one TRUE silent failure (D-03 consumer 3: both
guards miss, `:493` returns `invalid_probe_result`, and `brain-client.cjs:1631` logs only on
`queued`, so nothing is written anywhere). A test written before the fix is the only thing that
makes the fix provable rather than plausible.

Output: three test files. All three are RED at the end of this plan, by design.

Wave 0 is RED BY DESIGN. `test-339-enrichment-theo-shapes.cjs` is red until 339-05 lands the two
arms; `test-339-update-path-single-source.cjs` is red until 339-06 creates
`lib/core/update-path.cjs`; `test-339-schema-memo-origin-keyed.cjs` is red until 339-04 adds
`_schemaCacheOrigin`. Do NOT soften any of them to reach green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-PATTERNS.md
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols this PLAN creates (new to the repo, excluded from drift verification):

| Symbol | Kind | File |
|--------|------|------|
| `tests/test-339-enrichment-theo-shapes.cjs` | new file (test) | itself |
| `tests/test-339-update-path-single-source.cjs` | new file (test) | itself |
| `tests/test-339-schema-memo-origin-keyed.cjs` | new file (test) | itself |

Symbols these tests assert about but do NOT create (created later in this phase by 339-04,
339-05 and 339-06): `THEO_INPUT_DIMENSIONS`, `_schemaCacheOrigin`, `lib/core/update-path.cjs`,
`MARKETPLACE_UPDATE_COMMAND`, `PLUGIN_UPDATE_COMMAND`, `UPDATE_PATH_SENTENCE`. Their absence is
why all three tests are red in wave 1.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/test-339-enrichment-theo-shapes.cjs (FLIP-03a/b/c)</name>

  <files>tests/test-339-enrichment-theo-shapes.cjs</files>

  <behavior>
    - Fixture 1, Theo RESOLVED shape (`{framework, score, inputs, evidence, unsynced_inputs, coverage, diagnostics}`) with `score: 1`: captures, `readiness_score` is 1, `dimensions_inferred` is `false`, `missing_dimensions` is the exact vector of dimensions whose Theo input is `false`, and any dimension whose Theo input appears in `unsynced_inputs` is EXCLUDED.
    - Fixture 2, Theo RESOLVED shape with `score: 3`: returns `{captured: false, reason: 'not_a_miss'}`. Three is Theo's practical ceiling, so the existing `> 2` threshold is already correct and must not be rescaled.
    - Fixture 3, Theo REFUSAL shape (`{coverage: {matched: 0, total: 149, status: 'empty'}, refusal: {code: 'FRAMEWORK_NOT_FOUND', detail: '...'}, diagnostics}`), with `score`, `inputs`, `evidence` and `framework` ABSENT: captures, and `probe_provenance` contains the refusal CODE but NOT the refusal `detail`.
    - Fixture 4, Theo REFUSAL shape with `coverage: {matched: 0, total: 0}`: does NOT capture, returns `reason: 'layer_empty'`. This is the two-zeros distinction and it is the single most important assertion in the file.
    - Fixture 5, incumbent `{grounded: false, anchor: null, note: '...'}`: captures unchanged, `probe_provenance` starts `discover_structure@`.
    - Fixture 6, incumbent `{readiness_score: 1}`: captures unchanged, `probe_provenance` starts `orchestration_readiness@`.
    - No fixture stores a Theo `refusal.detail` string anywhere in the queue entry.
  </behavior>

  <read_first>
    - `lib/core/enrichment-queue.cjs:435-520` in full: the capture function, its four locals (`readiness_score`, `missing_dimensions`, `dimensions_inferred`, `probe_provenance`), the incumbent arms at `:471-491`, the `else` at `:493`, the entry assembly, and the exported test entry point `captureReadinessMiss` at `:519`
    - `lib/core/enrichment-queue.cjs:47-48` (`SOFT_CAP`, `HARD_CAP`), `:69` (`ALLOWED_SOURCES`), `:74` (`ALLOWED_DIMENSIONS`), `:186-188` (`isValidReadinessScore`)
    - `/home/jsagi/Theo/src/mcp/content/orchestration-readiness.ts` lines 44-46, 52-73, 92-101, 344-350, 446-449, 479-486, 492-503 (READ ONLY, never edit): the two payload shapes, the four inputs, the `unsynced_inputs` honesty rule, and Theo's own statement that `score: 0` on a nonexistent Framework would be a claim, which is why the refusal branch omits the key entirely
    - `339-RESEARCH.md` `Design 2` in full, especially `The score semantics, precisely`, `The dimensions mapping is exact`, and the `Recommended implementation` block
    - `tests/test-250-refusal-shapes.cjs:36-49`: the `node:test` + `node:assert/strict` idiom
    - `339-PATTERNS.md` section `MODIFIED lib/core/enrichment-queue.cjs two additive arms`
  </read_first>

  <action>
Create `tests/test-339-enrichment-theo-shapes.cjs` using `node:test` and `node:assert/strict`.

Drive all six fixtures through the exported `captureReadinessMiss` directly (exported at
`enrichment-queue.cjs:519`), against a temp `roomDir` created with `fs.mkdtempSync` under
`os.tmpdir()` and removed in a teardown. Do NOT reach into module internals and do NOT add a new
export to `enrichment-queue.cjs` to make the test easier; the entry point already exists.

The four Theo input names, verbatim from `orchestration-readiness.ts`, and the
`ALLOWED_DIMENSIONS` member each maps to (verbatim from `339-RESEARCH.md` Design 2):
`has_structure` -> `structure`, `has_ordering` -> `flow`, `has_technique` -> `techniques`,
`pattern_known` -> `pattern_type`.

Fixture 1 must set `pattern_known: false` AND list `'pattern_known'` in `unsynced_inputs`, then
assert `pattern_type` is NOT in `missing_dimensions`. That single assertion is the honesty
subtlety: reporting a dimension as missing on the strength of an input Theo itself flags as
unsynced would queue enrichment for a gap that may not exist.

Fixture 4 must assert BOTH that `captured` is `false` AND that `reason` is exactly
`'layer_empty'`, and a sibling assertion must confirm fixture 3 (`total: 149`) DID capture, so
the test proves the two zeros are different answers rather than merely testing one of them.

Add an explicit assertion that `_isCapturableResult`'s gate does not reject a Theo payload:
Theo's honest-empty payloads carry `refusal`, not `error`, so they must pass. Verify this by
observation through the capture path rather than by requiring the private helper.

Add a Canon Part 8 assertion: read back the written queue entry from the temp `roomDir` and
assert its serialized JSON does NOT contain the fixture's `refusal.detail` string. Only the
closed-vocabulary `refusal.code` may travel into `probe_provenance`.

Header per house style: numbered one-sentence claims, the framework line, `No em-dashes.`, and a
comment-as-contract block naming phase 339, date 2026-09-03, the payload shapes it keys on, and
what it deliberately does NOT key on (it never keys on key PRESENCE, only on shape: `typeof
pr.score === 'number' && pr.inputs`, per D-04 and the shipped `brain_query` precedent at
`brain-client.cjs:927-945`).

This test is RED until 339-05 lands the two additive arms. Fixtures 1-4 will currently fall
through to `{captured: false, reason: 'invalid_probe_result'}`. Do not weaken the assertions.
  </action>

  <verify>
    <automated>node tests/test-339-enrichment-theo-shapes.cjs; echo "exit=$? (non-zero is CORRECT in wave 1: 339-05 has not run)"; grep -Fq "'layer_empty'" tests/test-339-enrichment-theo-shapes.cjs && grep -Fq 'unsynced_inputs' tests/test-339-enrichment-theo-shapes.cjs && echo STRUCTURE_OK</automated>
  </verify>

  <acceptance_criteria>
    - The file exists, runs under bare `node`, makes no network call and spawns no process.
    - It currently FAILS on fixtures 1 through 4 with `invalid_probe_result`, and PASSES fixtures 5 and 6. That mixed result is the correct wave-1 state and must be recorded verbatim in the SUMMARY.
    - Exactly six fixtures are present and each is named in a `test(...)` title that states the shape it drives.
    - The `layer_empty` assertion and the `total: 149` capture assertion are in the same file, so the two-zeros distinction cannot be half-tested.
    - The Part 8 assertion reads the written queue entry from disk and asserts the `refusal.detail` string is absent.
    - `grep -c $'\u2014' tests/test-339-enrichment-theo-shapes.cjs` returns 0.
  </acceptance_criteria>

  <done>The one true silent failure in this phase has a test that fails today and can only be made green by writing the two additive arms correctly, including the two-zeros distinction and the unsynced-input filter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/test-339-update-path-single-source.cjs (FLIP-04)</name>

  <files>tests/test-339-update-path-single-source.cjs</files>

  <behavior>
    - `.claude/includes/release-process.md` contains a fenced bash block whose two command lines, with trailing comments stripped and whitespace trimmed, are exactly `/plugin marketplace update` and `claude plugin update mos@mindrian-marketplace`.
    - `lib/core/update-path.cjs` exports `MARKETPLACE_UPDATE_COMMAND` and `PLUGIN_UPDATE_COMMAND` byte-identical to those two extracted strings.
    - `lib/core/update-path.cjs` exports `UPDATE_PATH_SENTENCE`, a single-line string containing both commands.
    - The rendered `refusal-messaging.cjs` copy for kind `unreachable` contains `PLUGIN_UPDATE_COMMAND`.
    - The rendered `refusal-messaging.cjs` copy for kind `no_key` contains `PLUGIN_UPDATE_COMMAND`.
    - Every exported value is frozen: mutating it throws or is a no-op under strict mode.
  </behavior>

  <read_first>
    - `.claude/includes/release-process.md` lines 19-30: the canonical fenced block and its two commented commands. This file is the SOURCE; the module mirrors it
    - `lib/core/refusal-messaging.cjs:241-320` (`REASONS`, `NEXT_MOVES`) and `:326-380` (`RENDER_COPY`): the two tables and the render entry points
    - `lib/core/refusal-messaging.cjs:454-472` (`larryRefusalLine`): ALREADY VERIFIED at plan time, its returns are its own hard-coded switch strings, independent of both `REASONS` and `RENDER_COPY`. Adding the update path to `RENDER_COPY` therefore CANNOT break `test-250`'s 120-character pin. Confirm this by reading, then rely on it
    - `lib/core/refusal-messaging.cjs:275-283`: the V5 interpolation rule the new copy must satisfy (only closed-enum kinds, coerced tool names and integers may cross into refusal copy)
    - `339-RESEARCH.md` `Design 3` in full, especially `Where the constant lives` and `The drift test (Wave 0)`
    - `lib/core/brain-response-sanitize.cjs:54-61`: the repo's existing shape for a constant policed by a parity test, which is the pattern this test instantiates
  </read_first>

  <action>
Create `tests/test-339-update-path-single-source.cjs` using `node:test` and `node:assert/strict`.

Extract the two commands from `.claude/includes/release-process.md` by reading the file, finding
the fenced ```bash block that contains `plugin marketplace update`, splitting its lines,
stripping any trailing `#` comment and trimming. Assert the extraction found exactly two
non-empty command lines, so a future edit that adds a third command to that block fails loudly
rather than being silently ignored.

Then `require('../lib/core/update-path.cjs')` and assert byte equality against the two extracted
strings. Use `assert.equal`, not a regex or a `.includes`, because the whole point is byte
identity.

Then assert the rendered refusal copy: call the module's public render entry point for kind
`unreachable` and for kind `no_key`, join the returned lines, and assert each joined string
contains `PLUGIN_UPDATE_COMMAND`. Read `refusal-messaging.cjs`'s exports to pick the correct
public entry point (`renderRefusal` or the `RENDER_COPY` accessor it exposes); do not reach into
an unexported table.

Add an explicit ANTI-DRIFT assertion in the opposite direction: scan `lib/` and `scripts/` for
any other occurrence of the literal string `claude plugin update mos@mindrian-marketplace` on a
non-comment line, and assert the only hit is `lib/core/update-path.cjs`. Three copies drift;
that is exactly what D-08 asks to prevent.

Header per house style with a comment-as-contract block: phase 339, date 2026-09-03, the shape
it keys on (a fenced bash block in a markdown include, and three named module exports), and what
it deliberately does NOT key on (it does not read `release-process.md` at RUNTIME; the file is a
CLAUDE.md `@include`, not a shipped runtime asset, and `package.json`'s files allowlist plus
`release.sh` Step 9.5's payload gate mean a runtime read of a doc path is a distribution hazard).

This test is RED until 339-06 creates `lib/core/update-path.cjs`. The `require` will throw
`MODULE_NOT_FOUND`. Catch it and report it as a named failure rather than letting the process
crash with a stack trace, so the wave-1 red output stays readable.
  </action>

  <verify>
    <automated>node tests/test-339-update-path-single-source.cjs; echo "exit=$? (non-zero is CORRECT in wave 1: 339-06 has not run)"; grep -Fq 'release-process.md' tests/test-339-update-path-single-source.cjs && grep -Fq 'MARKETPLACE_UPDATE_COMMAND' tests/test-339-update-path-single-source.cjs && echo STRUCTURE_OK</automated>
  </verify>

  <acceptance_criteria>
    - The file exists and runs under bare `node`.
    - It currently FAILS with a named `lib/core/update-path.cjs not found` style failure, not an unhandled `MODULE_NOT_FOUND` stack trace.
    - The extraction arm asserts exactly two command lines were found in the fenced block.
    - The anti-drift arm scans `lib/` and `scripts/` and asserts a single hit.
    - `grep -c $'\u2014' tests/test-339-update-path-single-source.cjs` returns 0.
  </acceptance_criteria>

  <done>The update-path string cannot exist in more than one place without a red test, in either direction: the doc drifting from the module, or the module being copied into a second file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write tests/test-339-schema-memo-origin-keyed.cjs (FLIP-05)</name>

  <files>tests/test-339-schema-memo-origin-keyed.cjs</files>

  <behavior>
    - `lib/core/brain-client.cjs` declares `_schemaCacheOrigin` adjacent to `_schemaCacheAt`.
    - Inside `async function schema()`, the index of `_schemaCacheOrigin` occurs BEFORE the index of the early `return _schemaCache;`, proving the origin is compared before the memo is served.
    - `_schemaCacheOrigin` is assigned inside the same block that assigns `_schemaCacheAt`, proving the origin is recorded whenever the timestamp is.
    - The existing 30-minute TTL constant and the Phase 247-02 sentinel guard (`!(typeof result === 'object' && result.error)`) are both still present and unmodified.
  </behavior>

  <read_first>
    - `lib/core/brain-client.cjs:1045-1075`: the memo block in full, including the Phase 247-02 audit-fix comment that must survive
    - `lib/core/brain-client.cjs:1155-1176`: `getBrainUrl()` and its docblock
    - `339-RESEARCH.md` `Design 4` in full, including `The consequence, stated plainly` and `Flag for the navigator`: the premise correction. There is NOTHING to flush; `BRAIN_URL` is a module-scope const so the origin cannot change within one process. Key-by-origin ships as defense in depth that makes the invariant explicit and testable, and no `flushSchemaMemo()` is created because it would have zero callers
    - `tests/test-254-normalize-roundtrip-probe.cjs` Arm 4 (`:258-303`): the structural source-scan idiom, and its own header stating the hermetic constraint
    - `339-PATTERNS.md` section `MODIFIED lib/core/brain-client.cjs schema memo`
  </read_first>

  <action>
Create `tests/test-339-schema-memo-origin-keyed.cjs` as a STRUCTURAL source scan over
`lib/core/brain-client.cjs`, copying the `test-254` Arm 4 idiom. Do not write a behavioral test:
a behavioral one would need a test-only setter for `BRAIN_URL`, which permanently widens the
module's public surface for no gain (`339-RESEARCH.md` Don't Hand-Roll, last row).

Read `lib/core/brain-client.cjs` as text. Extract the body of `async function schema()` by
locating its declaration and the matching close. Within that body assert, in this order:

1. `_schemaCacheOrigin` appears at least once.
2. `indexOf('_schemaCacheOrigin')` is LESS THAN `indexOf('return _schemaCache;')`, which is the
   structural proof that the origin is compared before the cached value is served.
3. The assignment `_schemaCacheOrigin =` occurs in the same statement block as
   `_schemaCacheAt = Date.now()`, proven by asserting both appear between the same
   `if (result != null` guard and its closing brace.

Outside the function body, assert `let _schemaCacheOrigin` is declared adjacent to
`let _schemaCacheAt`, within three lines of it.

Add two regression assertions so this test also protects what must NOT change:
`SCHEMA_CACHE_TTL_MS = 30 * 60 * 1000` is still present, and the Phase 247-02 sentinel guard
string `result.error` still appears inside `schema()`.

Add an explicit NEGATIVE assertion with a written reason: `flushSchemaMemo` must NOT appear
anywhere in `lib/core/brain-client.cjs`. Reason, stated in the test source: D-13 as originally
written asked for a flush on the flip cut, and research corrected the premise. `BRAIN_URL` is a
module-scope const and `_schemaCache` is process-local, so no process can observe an origin
change and an exported flush would have zero callers. Adding one later without revisiting this
decision is the drift this assertion catches.

Header per house style with the comment-as-contract block: phase 339, date 2026-09-03, the shape
it keys on (source text positions inside one named function), and what it deliberately does NOT
key on (it does not invoke `schema()`, does not touch the network, and does not assert on the
memo's runtime behavior).

This test is RED until 339-04 adds `_schemaCacheOrigin`.
  </action>

  <verify>
    <automated>node tests/test-339-schema-memo-origin-keyed.cjs; echo "exit=$? (non-zero is CORRECT in wave 1: 339-04 has not run)"; grep -Fq '_schemaCacheOrigin' tests/test-339-schema-memo-origin-keyed.cjs && grep -Fq 'flushSchemaMemo' tests/test-339-schema-memo-origin-keyed.cjs && echo STRUCTURE_OK</automated>
  </verify>

  <acceptance_criteria>
    - The file exists, runs under bare `node`, and makes no network call.
    - It currently FAILS on the `_schemaCacheOrigin` presence assertion. That is the correct wave-1 state.
    - The negative `flushSchemaMemo` assertion is present and carries its written reason in the source, not just in the plan.
    - The two regression assertions (TTL constant, Phase 247-02 sentinel guard) are present.
    - `grep -c $'\u2014' tests/test-339-schema-memo-origin-keyed.cjs` returns 0.
  </acceptance_criteria>

  <done>FLIP-05 has a structural proof, and the deliberate decision NOT to build a flush is recorded as an assertion rather than as prose someone can miss.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theo response body -> plugin logs and local queue | Theo-authored strings cross here; Canon Part 8's mirror rule applies (key NAMES only, never VALUES) |
| doc include -> shipped runtime code | `.claude/includes/release-process.md` is not shipped; a runtime read of it would be a distribution hazard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-03 | Information Disclosure | a Theo `refusal.detail` string captured into the enrichment queue entry | mitigate | `tests/test-339-enrichment-theo-shapes.cjs` reads the written entry back from the temp `roomDir` and asserts the fixture's `detail` string is absent; only the closed-vocabulary `refusal.code` may reach `probe_provenance` |
| T-339-05 | Tampering | the update-path string drifting between `release-process.md`, `update-path.cjs` and the refusal copy | mitigate | `tests/test-339-update-path-single-source.cjs` checks all three, in both directions, with byte equality and a repo-wide single-hit scan |
| T-339-11 | Repudiation | a future `flushSchemaMemo()` added without revisiting the D-13 premise correction | mitigate | negative assertion in `tests/test-339-schema-memo-origin-keyed.cjs` with the reason written in the test source |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | zero package installs in this plan and in the phase; `node:test` and `node:assert/strict` are Node built-ins |
</threat_model>

<verification>
- All three test files exist and run under bare `node` with no arguments.
- All three currently exit non-zero, and their failure messages name the missing symbol rather than crashing with an unhandled stack trace.
- `bash tests/run-all-339.sh` discovers all three (glob on `tests/test-339-`).
- `node tests/test-250-refusal-shapes.cjs` and `node tests/test-254-normalize-roundtrip-probe.cjs` are unaffected by this plan (it writes no production code).
</verification>

<success_criteria>
- FLIP-03a, FLIP-03b, FLIP-03c, FLIP-04 and FLIP-05 each have a failing automated test that can only be made green by the correct implementation.
- The two-zeros distinction and the `unsynced_inputs` filter are both pinned before any capture code exists.
- The decision not to build `flushSchemaMemo()` is mechanized, not just documented.
- No em-dashes in any file this plan wrote.
</success_criteria>

<output>
Create `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-02-SUMMARY.md` when done. Record the exact pass/fail split of `tests/test-339-enrichment-theo-shapes.cjs` (fixtures 5 and 6 green, 1 through 4 red) because 339-05's executor uses it as the target state.
</output>
