---
phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/run-all-339.sh
  - tests/test-339-origin-single-source.cjs
autonomous: true
requirements: [FLIP-01]
canon_parts: [7, 8, 11]

must_haves:
  truths:
    - "The FLIP-01..FLIP-12 requirement family is recorded in .planning/REQUIREMENTS.md under a Phase 339 heading, so no plan in this phase carries an unregistered requirement id"
    - "A single command, bash tests/run-all-339.sh, runs every test this phase owns and exits non-zero when discovery finds nothing"
    - "A source scan proves no runtime file resolves the Brain origin from its own literal outside a written allowlist"
  artifacts:
    - path: "tests/run-all-339.sh"
      provides: "Phase 339 test aggregator: glob discovery, found-eq-0 guard, run_may_skip, origin-literal sweep, no-em-dash fence, dist staleness check"
      contains: "TEST_339_PREFIX"
      min_lines: 150
    - path: "tests/test-339-origin-single-source.cjs"
      provides: "FLIP-01 source scan: no raw incumbent origin literal on a non-comment line outside the allowlist"
      contains: "ALLOWLIST"
      min_lines: 70
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 339 FLIP-01..FLIP-12 family record following the Phase 254 D-05 precedent"
      contains: "FLIP-01"
  key_links:
    - from: "tests/run-all-339.sh"
      to: "tests/test-339-*.cjs and tests/test-339-*.sh"
      via: "glob discovery on a variable prefix"
      pattern: "TEST_339_PREFIX"
    - from: "tests/run-all-339.sh"
      to: "scripts/build-dist-bundles.cjs --check-stale"
      via: "the only gate this repo has for dist staleness"
      pattern: "check-stale"
---

<objective>
Wave 0 of Phase 339 in `339-VALIDATION.md`'s vocabulary, executed as GSD wave 1: the tests come
before the code. This plan lands the phase's requirement family and the two pieces of test
infrastructure every later plan reports into.

Three deliverables:
1. `.planning/REQUIREMENTS.md` gains a Phase 339 section recording FLIP-01..FLIP-12, minted at
   plan time, following the Phase 254 D-05 precedent that `.planning/REQUIREMENTS.md` already
   documents in its Traceability section.
2. `tests/run-all-339.sh`, the phase aggregator, ported structurally from `tests/run-all-276.sh`.
3. `tests/test-339-origin-single-source.cjs`, the FLIP-01 scan that makes the literal sweep
   provable rather than asserted.

Purpose: this phase's whole disease is a SECOND SOURCE OF TRUTH (one origin, one vocabulary
switch, one update-path string, one coverage ruling). A source scan is the only thing that keeps
a second origin literal from being reintroduced later by someone who never read this phase.

Output: one requirement record, one runner, one test.

Wave 0 is RED BY DESIGN. `tests/test-339-origin-single-source.cjs` fails on this plan's own run,
because `scripts/probe-brain-contract.cjs:74` and `scripts/build-brain-census.cjs:61` still carry
their own literals until plan 339-07 lands. A red run at the end of wave 1 is the CORRECT state,
not a defect. Do NOT soften the test to make it green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-PATTERNS.md
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-VALIDATION.md
@tests/run-all-276.sh
</context>

<artifacts_this_phase_produces>
Symbols this PLAN creates (new to the repo, excluded from any drift verification):

| Symbol | Kind | File |
|--------|------|------|
| `tests/run-all-339.sh` | new file (aggregator) | itself |
| `TEST_339_PREFIX` | env-var discovery override | `tests/run-all-339.sh` |
| `TEST_339_ALLOW_MISSING` | env-var escape for the em-dash fence | `tests/run-all-339.sh` |
| `tests/test-339-origin-single-source.cjs` | new file (test) | itself |
| `ALLOWLIST` | frozen constant | `tests/test-339-origin-single-source.cjs` |
| `FLIP-01` .. `FLIP-12` | requirement ids | `.planning/REQUIREMENTS.md` |

Symbols the FULL PHASE creates, listed here once so the source-grounding pass can exclude them
everywhere: `lib/core/update-path.cjs`, `MARKETPLACE_UPDATE_COMMAND`, `PLUGIN_UPDATE_COMMAND`,
`UPDATE_PATH_SENTENCE`, `THEO_ORIGINS`, `BRAIN_PROBLEM_TYPE_ALIASES_INCUMBENT`,
`BRAIN_PROBLEM_TYPE_ALIASES_THEO`, `_brainProblemTypeAliases`, `_schemaCacheOrigin`,
`THEO_INPUT_DIMENSIONS`, `THEO_NODE_FLOOR`, `CANON_ORIGIN_NODE_FLOORS`,
`docs/339-NOTE-theo-desktop-connector-key.md`, `docs/testers/outbox/2026-09-03-theo-cutover.md`,
`tests/test-339-enrichment-theo-shapes.cjs`, `tests/test-339-update-path-single-source.cjs`,
`tests/test-339-schema-memo-origin-keyed.cjs`, `tests/test-339-cross-repo-note.sh`,
`tests/test-339-269-05-checklist.sh`, `tests/test-339-gate-zero-write.sh`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Record the FLIP-01..FLIP-12 requirement family in .planning/REQUIREMENTS.md</name>

  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` lines 1100-1130 and its final `## Traceability` section: the Phase 254 D-05 precedent it names verbatim ("WIRE-01..04 / COMP-01..02 in `254-CONTEXT.md` D-05, ratifying `254-RESEARCH.md`'s proposed family, registered to this document at phase close by `254-06-PLAN.md`"), plus the standing caveat paragraph that every plan-time-minted family carries
    - `.planning/REQUIREMENTS.md` section `### Phase 254 - Orchestration projection consumption wiring ...` (line ~786) and `### Phase 257 - Part 8 enforcement locus ...` (line ~868): the two closest structural models for a new phase section
    - `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md` section `<phase_requirements>`: the twelve rows, their Cut column and their research support
    - `.planning/ROADMAP.md` section `### Phase 339`, the `**Requirements**: TBD` line
  </read_first>

  <action>
`phase_req_ids` is null for Phase 339 and no requirement family exists. Mint FLIP-01..FLIP-12
exactly as `339-RESEARCH.md`'s `<phase_requirements>` table proposes, following the Phase 254
D-05 precedent for minting at plan time.

Append a new section to `.planning/REQUIREMENTS.md` immediately before the final
`## Traceability` section, headed exactly:

`### Phase 339 - Brain-to-Theo cutover release (minted at plan time 2026-09-03)`

Under it, twelve `- [ ]` rows, one per id, each carrying the requirement text from the research
table plus its Cut (PREP, FLIP, PREP + FLIP, or POST). Use the research table's wording, not a
paraphrase. The twelve ids and their one-line subjects, so nothing is dropped:

- FLIP-01: no runtime site resolves the Brain origin from its own literal (PREP + FLIP)
- FLIP-02: `BRAIN_PROBLEM_TYPE_ALIASES` projects onto the resolved origin's vocabulary (PREP)
- FLIP-03: `_maybeCaptureEnrichmentMiss` captures BOTH Theo readiness shapes, and
  `{matched:0,total:0}` is never collapsed with `{matched:0,total:N>0}` (PREP)
- FLIP-04: `unreachable` and `no_key` refusal copy names the two-command update path from ONE
  shared constant (PREP)
- FLIP-05: the `brain_schema` memo cannot serve a schema fetched from a different origin (PREP)
- FLIP-06: Desktop and Cowork connector docs name Theo's `/mcp` endpoint under the unchanged
  `mindrian-brain` key with no `Authorization` header, mirrors regenerated not hand-edited (PREP)
- FLIP-07: `docs/339-NOTE-theo-desktop-connector-key.md` exists and states the egress-guard
  reason the key matters (PREP)
- FLIP-08: Phase 269-05 Task 1's checklist reads the three real legs against live sources (PREP)
- FLIP-09: the FLIP release cannot be cut until a human confirms Theo's coverage ruling, read
  live, with zero repository writes (FLIP)
- FLIP-10: `brain-client.cjs:24` resolves to `https://theo-mcp.onrender.com`, bare origin, and
  the `:4-7` docblock no longer names the incumbent (FLIP)
- FLIP-11: `class-m-brain-smoke.cjs` layer 6 reports an honest verdict against Theo (FLIP)
- FLIP-12: an installed session running the FLIP release returns structured Theo answers and the
  result is reported to Session T as 09-12 Task 2's resume signal (POST)

Then update the `## Traceability` section in TWO places, matching the existing prose exactly in
form:
1. Raise the active-requirement count from 131 to 143 in both the opening sentence and the
   closing `Roadmap phases must map all N active requirements with no orphans.` line, and add
   `plus FLIP-01..12 (Phase 339)` to the enumeration.
2. Add one sentence to the minting paragraph: `FLIP-01..12 were minted in Phase 339's plan set
   (2026-09-03), ratifying 339-RESEARCH.md's proposed family, and are registered here at plan
   time as - [ ] rows to be finalized with measured proof at phase close.` Add `FLIP` to the
   list of families named in the standing caveat paragraph ("carried on the MCPFIX, MEMOP,
   GUARD, PYPORT, ANCHOR, WIRE/COMP, LOCUS and HOOK families alike").

Also update `.planning/ROADMAP.md`'s Phase 339 entry: change `**Requirements**: TBD` to
`**Requirements**: FLIP-01, FLIP-02, FLIP-03, FLIP-04, FLIP-05, FLIP-06, FLIP-07, FLIP-08,
FLIP-09, FLIP-10, FLIP-11, FLIP-12`.

`.planning/` is gitignored: stage both files with `git add -f` by path. Never `git add .` or
`git add -A`. No em-dashes anywhere in what you write.
  </action>

  <verify>
    <automated>grep -c '^- \[ \] \*\*FLIP-' .planning/REQUIREMENTS.md; grep -Fq '### Phase 339 - Brain-to-Theo cutover release (minted at plan time 2026-09-03)' .planning/REQUIREMENTS.md && grep -Fq 'FLIP-01, FLIP-02' .planning/ROADMAP.md && ! grep -q $'\u2014' .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains the heading `### Phase 339 - Brain-to-Theo cutover release (minted at plan time 2026-09-03)`.
    - Exactly twelve FLIP rows exist, ids FLIP-01 through FLIP-12, no gaps and no duplicates: `grep -o 'FLIP-[0-9][0-9]' .planning/REQUIREMENTS.md | sort -u | wc -l` returns 12.
    - The Traceability count reads 143 in both the opening sentence and the closing orphan line.
    - `.planning/ROADMAP.md` Phase 339 `**Requirements**:` line no longer reads `TBD`.
    - Zero em-dashes in either file: `grep -c $'\u2014'` returns 0 for both.
    - Both files staged by explicit path with `git add -f`; `git status --porcelain` shows no unintended additions.
  </acceptance_criteria>

  <done>FLIP-01..FLIP-12 are registered requirement ids in this repo, and every other plan in this phase can cite them without carrying an unregistered id.</done>
</task>

<task type="auto">
  <name>Task 2: Port tests/run-all-339.sh from tests/run-all-276.sh</name>

  <files>tests/run-all-339.sh</files>

  <read_first>
    - `tests/run-all-276.sh` IN FULL. This is a structural port, not an inspiration. The six load-bearing features and their line cites are enumerated in `339-PATTERNS.md` section `NEW tests/run-all-339.sh`: the variable discovery prefix (`:77-80`), `run` and `run_may_skip` (`:85-106`), glob discovery plus the self-exclusion guard (`:114-136`), the Part 8 source sweep (`:144-181`), the no-em-dash fence including the `grep -P` rc>=2 scan-broke arm (`:188-222`), and the Wave-0-red header (`:44-59`)
    - `tests/run-all-273.sh` header: the second documented precedent for the Wave-0-red-by-design convention
    - `339-RESEARCH.md` section `The run-all-276.sh conventions this phase must copy`, especially item 4: this phase's own production targets legitimately contain `brain-client` and `https://`, so the forbidden-token list MUST be scoped differently here than in 276
    - `339-RESEARCH.md` section `The dist/ generation chain, in required order`: `build-dist-bundles.cjs --check-stale` is wired to no gate anywhere in the repo, which is why this runner must call it
    - `339-VALIDATION.md` `Wave 0 Requirements` list: the eight new test files plus the two modified ones
  </read_first>

  <action>
Create `tests/run-all-339.sh`, executable (`chmod +x`), porting `tests/run-all-276.sh`'s
structure. Copy these six features:

1. `PREFIX="${TEST_339_PREFIX:-tests/test-339-}"` with the comment 276 carries verbatim in
   shape: the prefix is a variable ONLY so the found-eq-0 guard is provable without editing this
   file, `TEST_339_PREFIX=test-339-nonexistent- bash tests/run-all-339.sh` must exit non-zero,
   production runs never set it.
2. The `found -eq 0` guard, unsoftened, with 276's own reason quoted in shape: a harness
   reporting green over zero discovery is itself the false-success disease.
3. `run` for `.cjs` arms (hard) and `run_may_skip` for `.sh` arms (a leading `SKIP` line is a
   skip, never a pass).
4. An origin-literal sweep arm, scoped as the OPPOSITE property to 276's forbidden-token list:
   invoke `node tests/test-339-origin-single-source.cjs` as the sweep rather than re-implementing
   a grep loop, because this phase's production targets legitimately contain `brain-client` and
   `https://` and a copied 276 token list would fire on every one of them. State that reason in
   a comment so a later reader does not "restore" the 276 list.
5. A no-em-dash fence over every file this phase touches plus the runner itself, with
   `EMDASH_MISSING` counting a missing target as a FAILURE unless `TEST_339_ALLOW_MISSING=1`,
   and the `grep -P` rc>=2 scan-broke arm. The fence target list, written in full:
   `lib/core/brain-client.cjs`, `lib/core/enrichment-queue.cjs`, `lib/mcp/brain-router.cjs`,
   `lib/core/refusal-messaging.cjs`, `lib/core/update-path.cjs`,
   `lib/core/doctor/class-m-brain-smoke.cjs`, `lib/core/doctor/class-m-brain-smoke.test.cjs`,
   `lib/core/mcp-profiles.cjs`, `bin/mindrian-brain-mcp-client.cjs`,
   `scripts/probe-brain-contract.cjs`, `scripts/build-brain-census.cjs`,
   `scripts/rs-experts-command.cjs`, `scripts/rs-thesis-command.cjs`,
   `scripts/sessionstart-post-update-preflight.cjs`, `scripts/session-start`,
   `docs/brain-setup.md`, `docs/install/BRAIN-SETUP.md`, `docs/THE-BRAIN.md`,
   `commands/setup.md`, `commands/pws-brain.md`, `skills/pws-brain/SKILL.md`,
   `docs/339-NOTE-theo-desktop-connector-key.md`, and every `tests/test-339-*` file.
6. A Wave-0-red-by-design header naming the RED tests BY NAME:
   `tests/test-339-origin-single-source.cjs` (red until 339-07 sweeps the two script literals),
   `tests/test-254-normalize-roundtrip-probe.cjs` Arms 4-5 (red until 339-04 lands the two-table
   selector), and `tests/test-250-refusal-shapes.cjs`'s new update-path pin (red until 339-06
   lands the copy). Say plainly that a red run at the end of wave 1 is the CORRECT state and
   that `tests/test-339-gate-zero-write.sh` is the one wave-1 arm that is GREEN on day one,
   because Theo's ruling already exists.

Additionally wire the two generated-artifact checks that no other gate in this repo runs:
`node scripts/build-skill-mirrors.cjs --check` and `node scripts/build-dist-bundles.cjs
--check-stale`. Both are hard arms. `--check-stale` is the named Wave 0 gap: `verify-release`,
`release.sh` and `doctor --acceptance` all skip it, so if this runner does not call it, nothing
will.

Also run the two related suites this phase must not regress, as named arms:
`node tests/test-254-normalize-roundtrip-probe.cjs` and `node tests/test-250-refusal-shapes.cjs`.

House style: a header block naming the phase id 339, the date 2026-09-03, what the runner
proves, and what it deliberately does NOT prove (it does not prove the flip works against a live
Theo; that is FLIP-12, manual by design). No em-dashes.
  </action>

  <verify>
    <automated>test -x tests/run-all-339.sh && TEST_339_PREFIX=tests/test-339-nonexistent- bash tests/run-all-339.sh; test $? -ne 0 && grep -Fq 'TEST_339_PREFIX' tests/run-all-339.sh && grep -Fq 'check-stale' tests/run-all-339.sh && echo GUARD_OK</automated>
  </verify>

  <acceptance_criteria>
    - `tests/run-all-339.sh` exists and is executable.
    - `TEST_339_PREFIX=tests/test-339-nonexistent- bash tests/run-all-339.sh` exits NON-ZERO (the found-eq-0 guard is real, not decorative).
    - `grep -Fc 'build-dist-bundles.cjs --check-stale' tests/run-all-339.sh` returns at least 1.
    - `grep -Fc 'build-skill-mirrors.cjs --check' tests/run-all-339.sh` returns at least 1.
    - The header names all three RED-by-design arms by filename and states that red at the end of wave 1 is correct.
    - The em-dash fence lists at least 20 targets and `TEST_339_ALLOW_MISSING` appears in the file.
    - `grep -c $'\u2014' tests/run-all-339.sh` returns 0.
  </acceptance_criteria>

  <done>`bash tests/run-all-339.sh` is the one command that runs everything this phase owns, it cannot report green over zero discovery, and it is the only gate in this repo that catches `dist/` staleness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write tests/test-339-origin-single-source.cjs (FLIP-01)</name>

  <files>tests/test-339-origin-single-source.cjs</files>

  <behavior>
    - Scanning `lib/`, `bin/`, `scripts/` finds ZERO occurrences of a raw `onrender.com` Brain origin literal on a NON-COMMENT line, except for the entries on a written allowlist.
    - The allowlist has exactly two entries, each carrying a written reason string in the source: `lib/core/brain-client.cjs` (the module-scope `BRAIN_URL` const at line 24, the single source of truth by design) and `lib/core/doctor/class-m-brain-smoke.cjs` (the canon-endpoint constants, which must move in the same commit as line 24 because deriving them from `getBrainUrl()` would destroy the check).
    - A comment-only line carrying the literal is NOT a failure (prose is swept for accuracy, not for FLIP-01).
    - Adding a third allowlist entry without a reason string fails the test.
  </behavior>

  <read_first>
    - `tests/test-254-normalize-roundtrip-probe.cjs` Arm 4 (`:258-303`): the structural source-scan idiom this test copies, including its hand-rolled `record()` harness and `process.exit(failed === 0 ? 0 : 1)`
    - `tests/test-254-normalize-roundtrip-probe.cjs:4-45`: the header form (numbered one-sentence claims, framework line, `No em-dashes.`)
    - `339-RESEARCH.md` section `The Literal Sweep, Sized and Classified`, subsections `Runtime sites, exact` and `Tests and fixtures`: the exact file:line inventory and the disposition column
    - `339-RESEARCH.md` `Wave 0 Gaps` bullet for this file: it must allowlist exactly `brain-client.cjs:24` and `class-m-brain-smoke.cjs`'s canon constant, each with a written reason
    - `lib/core/brain-client.cjs:1155-1176` (`getBrainUrl`) and its docblock, which already states the one-place-decides rule this test enforces
  </read_first>

  <action>
Create `tests/test-339-origin-single-source.cjs`, a hermetic source scan. No network, no spawn.

Scan every `.cjs`, `.js` and extensionless-executable file under `lib/`, `bin/` and `scripts/`,
excluding `node_modules/`. For each line, strip `//` line comments and `/* */` block comments and
`#` shell comments before matching, then match the regex for an `onrender.com` Brain origin
literal (cover both `pws-brain-mcp.onrender.com` and `theo-mcp.onrender.com`, because the point
is that no site outside the allowlist owns ANY origin literal, not that the incumbent one is
gone).

Declare the allowlist as a frozen constant named `ALLOWLIST`, an array of objects each with
`file` and `reason`, exactly two entries:

- `lib/core/brain-client.cjs`, reason: the module-scope `BRAIN_URL` const is the single source of
  truth every other site resolves through `getBrainUrl()`; this is the one place the literal is
  allowed to exist.
- `lib/core/doctor/class-m-brain-smoke.cjs`, reason: layer 6's canon-endpoint constant answers
  "is the resolved endpoint the canon one", so deriving it from `getBrainUrl()` would make the
  comparison always true and destroy the check; it moves in the same commit as line 24 instead.

Assert that every allowlist entry has a non-empty `reason` string of at least 40 characters, so
a future third entry cannot be added silently.

Comment-as-contract header per house style: phase id 339, date 2026-09-03, the observed shape it
keys on (a comment-stripped line containing an `onrender.com` host literal), and what it
deliberately does NOT key on (comment prose, `docs/`, `tests/fixtures/`, `.planning/`, and any
dated handoff or RCA, all of which are historical records the sweep deliberately leaves alone
per D-12).

This test is RED on this plan's run. `scripts/probe-brain-contract.cjs:74` and
`scripts/build-brain-census.cjs:61` each still declare their own `BRAIN_URL` const; plan 339-07
removes them. Do not add either file to the allowlist to make the test green. Print the offending
`file:line` for every violation so 339-07's executor has the list.
  </action>

  <verify>
    <automated>node tests/test-339-origin-single-source.cjs; echo "exit=$? (non-zero is CORRECT in wave 1: 339-07 has not run)"; grep -Fq 'ALLOWLIST' tests/test-339-origin-single-source.cjs && grep -Fq 'class-m-brain-smoke.cjs' tests/test-339-origin-single-source.cjs && echo STRUCTURE_OK</automated>
  </verify>

  <acceptance_criteria>
    - The file exists and runs under bare `node` with no arguments and no network access.
    - It currently FAILS, and its failure output names `scripts/probe-brain-contract.cjs` and `scripts/build-brain-census.cjs` with line numbers. That is the correct wave-1 state.
    - `ALLOWLIST` has exactly two entries and each `reason` is at least 40 characters.
    - A comment-only line carrying the literal does not fail the scan: verified by the test's own self-check arm over a fixture string.
    - `grep -c $'\u2014' tests/test-339-origin-single-source.cjs` returns 0.
  </acceptance_criteria>

  <done>FLIP-01 has a mechanical proof. When 339-07 and 339-12 land, this test turns green and stays the tripwire against a second origin literal being reintroduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin process -> remote Brain origin | every Brain call crosses here; the origin literal is the address of that boundary |
| repo source -> released bytes | what this plan asserts about the tree is only true of users after a release |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-01 | Tampering | any file under `lib/`, `bin/`, `scripts/` reintroducing its own origin literal | mitigate | `tests/test-339-origin-single-source.cjs` scans comment-stripped lines and allows exactly two entries, each requiring a written reason of 40+ characters |
| T-339-04 | Denial of Service | a release cut from a tree whose tests were never actually discovered | mitigate | the `found -eq 0` guard in `tests/run-all-339.sh`, proven by `TEST_339_PREFIX=tests/test-339-nonexistent-` exiting non-zero |
| T-339-10 | Repudiation | a `dist/` bundle shipped stale because no gate checks it | mitigate | `build-dist-bundles.cjs --check-stale` wired into `run-all-339.sh` as a hard arm; it is wired to no other gate in the repo |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | this phase installs ZERO packages (`339-RESEARCH.md` Sources: "No packages installed"); `node:test` and `node:assert/strict` are built-ins. No legitimacy checkpoint is required because there is no install task anywhere in the phase |
</threat_model>

<verification>
- `bash tests/run-all-339.sh` runs and discovers at least one arm.
- `TEST_339_PREFIX=tests/test-339-nonexistent- bash tests/run-all-339.sh` exits non-zero.
- `node tests/test-339-origin-single-source.cjs` exits non-zero and names the two script files that still carry their own literal.
- `grep -o 'FLIP-[0-9][0-9]' .planning/REQUIREMENTS.md | sort -u | wc -l` returns 12.
- `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs` and `node scripts/check-shape-declaration.cjs` all still pass (this plan touches no invocable surface, so they must be unchanged).
</verification>

<success_criteria>
- FLIP-01..FLIP-12 are registered in `.planning/REQUIREMENTS.md` and cited in `.planning/ROADMAP.md`.
- `tests/run-all-339.sh` exists, is executable, cannot report green over zero discovery, and is the only place in this repo that runs `build-dist-bundles.cjs --check-stale`.
- `tests/test-339-origin-single-source.cjs` exists, is RED, and its output is the work list for plan 339-07.
- No em-dashes in any file this plan wrote.
</success_criteria>

<output>
Create `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-01-SUMMARY.md` when done. Record the exact failure output of `tests/test-339-origin-single-source.cjs` verbatim, because it is plan 339-07's input.
</output>
