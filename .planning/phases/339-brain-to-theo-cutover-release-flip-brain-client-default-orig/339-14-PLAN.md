---
phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig
plan: 14
type: execute
wave: 7
depends_on: ["339-13"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - docs/testers/outbox/2026-09-03-theo-cutover.md
autonomous: false
requirements: [FLIP-12]
canon_parts: [7, 8]

must_haves:
  truths:
    - "An installed session running the FLIP release returns structured Theo answers through Larry, verified against the install cache and not the dev tree"
    - "The probe's inverted legs are recorded as EXPECTED, by name, so nobody invokes rollback on a documented red"
    - "Session T receives the resume signal plus the six flip-day fields its close-out template leaves blank"
    - "Phase 339 is closed in STATE.md and ROADMAP.md with the shipped versions recorded"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 339 marked complete with both shipped versions and the plan list checked off"
      contains: "Phase 339"
    - path: ".planning/STATE.md"
      provides: "Phase 339 close-out recorded"
  key_links:
    - from: "this plan's Task 2"
      to: "/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-12-CLOSEOUT-STAGING.md section F"
      via: "the six flip-day fields M's report fills, sent by the orchestrator to Session T"
      pattern: "brain_schema"
---

<objective>
Post-release verification and the hand-back to Session T. Three tasks.

1. FLIP-12, the one manual-only verification in this phase. An installed session running the FLIP
   release must return structured Theo answers through Larry. This cannot be automated: it requires
   a released artifact, a plugin-cache update, and a live Larry turn. Automating it would mean
   publishing a release from a test, which is the exact thing the human gate exists to prevent.
2. The resume signal to Session T: the shipped version plus "flip verified", carrying the six
   flip-day fields `09-12-CLOSEOUT-STAGING.md` section F leaves blank. This is an ORCHESTRATOR
   action; the executor has no messaging tool.
3. Close-out in STATE.md and ROADMAP.md.

The single most important discipline here: verify against the INSTALL CACHE, never the dev tree. A
`main` commit is not live until released AND picked up. Reading `lib/core/brain-client.cjs` in the
working tree proves nothing about what a user runs.

The second most important: the probe's inverted legs are EXPECTED. Legs b, c and e go red against
Theo by design, each for a documented reason. Treating them as failures and invoking rollback would
undo a correct release.

Output: FLIP-12 verified or its failure recorded, the resume signal sent, the phase closed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates no source symbol. It fills the `version` placeholder in
`docs/testers/outbox/2026-09-03-theo-cutover.md` with the actually-shipped flip version, read back
from the tag, and it closes the phase in `.planning/STATE.md` and `.planning/ROADMAP.md`.

It does NOT send the tester note. `status` stays `drafted` and `sent_to` stays empty. Sending is the
operator's, after Theo 09-12 Task 3 fixes a suspend date.

It does NOT write anything under `/home/jsagi/Theo/`. Session T fills its own flip record and
close-out staging from the report this plan sends.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: FLIP-12 - verify the installed release returns structured Theo answers</name>

  <what-built>
    Plans 339-01 through 339-13 shipped two releases. The PREP cut carried the alias selector, the
    two enrichment capture arms, the router disclosure, the refusal copy and its single update-path
    constant, the literal sweep, the connector docs, the cross-repo note and the 269-05 checklist
    rewrite. The FLIP cut moved `lib/core/brain-client.cjs` line 24 to
    `https://theo-mcp.onrender.com` and moved the doctor constants, the `test-245` tripwire and
    `CLAUDE.md` with it. Everything automatable has been automated and every suite is green in the
    dev tree. What no automation can do is prove the INSTALLED bytes work end to end.
  </what-built>

  <read_first>
    - `339-RESEARCH.md` Design 8 IN FULL: the five probe legs with their post-flip status, the cache-verification two-step, the expected `brain_stats` shape, and the D-03 regression check
    - `339-RESEARCH.md` Pitfall 5: treating the probe's red legs as failures and invoking rollback
    - `339-RESEARCH.md` Pitfall 6: verifying against the source tree rather than the cache
    - `339-RESEARCH.md` `Runtime State Inventory`, the OS-registered-state row: the cache contains a full copy of `lib/core/brain-client.cjs` with the origin compiled in
    - `339-RESEARCH.md` Assumptions Log A7: the cache on this machine was reported at `2.0.0-beta.13` by Session T and is UNVERIFIED. Check it
    - `.claude/includes/release-process.md` lines 19 to 30: the two-command update path
    - `.planning/phases/339-.../339-13-SUMMARY.md`: the shipped flip tag
  </read_first>

  <action>
STOP. This is FLIP-12, the one manual-only verification in this phase, and it is the last gate
before Session T is told the flip is verified.

Every automatable check has already run and passed in the dev tree. What no automation can do is
prove the INSTALLED bytes work end to end through a live Larry turn, because doing so would require
publishing a release from a test. Run the numbered steps in the how-to-verify block below, in
order, and record every observed value verbatim. A verification that reports "looks right" without
a quoted value is not a verification.

Two disciplines bind this whole task. First, verify against the INSTALL CACHE at
`~/.claude/plugins/cache/mindrian-marketplace/mos/`, never the working tree: a main commit is not
live until released AND picked up, so reading `lib/core/brain-client.cjs` in the dev tree proves
nothing about what a user runs. Read the cache; never write to it (WORKSPACE GUARD). Second, read
the expected-inversion list in step 5 BEFORE running the probe: legs b, c and e go red against Theo
by design, each for a documented reason, and treating a documented red as a failure would roll back
a correct release.

Call no write endpoint. `brain_write` and a deliberate `POST /register` are both write-shaped. The
automatic `/register` fired by `_tryAutoRegister` when no key resolves is the shipped path and is
fine.
  </action>

  <how-to-verify>
1. Update the plugin cache using the two-command path, exactly as a user would:
   `/plugin marketplace update` then `claude plugin update mos@mindrian-marketplace`.

2. Confirm the cache actually moved. Run `ls ~/.claude/plugins/cache/mindrian-marketplace/mos/` and
   note every version present. For the newest, run
   `grep '"version"' ~/.claude/plugins/cache/mindrian-marketplace/mos/<v>/.claude-plugin/plugin.json`
   and `grep -n 'BRAIN_URL' ~/.claude/plugins/cache/mindrian-marketplace/mos/<v>/lib/core/brain-client.cjs`.
   EXPECTED: the version matches the flip tag, and the cached `BRAIN_URL` line names
   `https://theo-mcp.onrender.com` as a BARE origin with no path and no trailing slash. If the
   cached line still names the incumbent, the release did not pick up and nothing below is
   meaningful. READ the cache; never write to it (WORKSPACE GUARD).

3. Start a FRESH Claude Code session running the updated cache. Ask Larry for `brain_stats`.
   Exercise it THROUGH LARRY, not through a direct script: the point is to prove the installed wire
   works end to end, not that the source does.
   EXPECTED: a Neo4j-shaped payload, `{nodes, relationships, labels}`, with roughly 1,253 nodes and
   1,522 relationships. NOT the incumbent's `backend memgraph` with 29,200 nodes. If you see 29,200,
   you are talking to the incumbent and the flip did not take.

4. In the same session, ask Larry one `brain_ask` question.
   EXPECTED: structured MATERIAL, not composed prose. Theo hands back material and Larry composes
   from it. A polished paragraph with no visible material underneath is the wrong shape.

5. Run `node scripts/probe-brain-contract.cjs` and record its FULL output verbatim.
   EXPECTED, and these are DOCUMENTED, not failures. Read this list BEFORE running the probe:
   - Leg a (`tools/list` contains the loop tools): PASSES. Ports cleanly.
   - Leg b: INVERTS. Theo SERVES `text2cypher`, so the expected 403 does not arrive.
     `brain_ask_anything` still draws an unknown-tool error, but because Theo has no allowlist gate
     at all rather than because it is delisted.
   - Leg c: PARTIALLY INVERTS. The `BoundedReadRefusal` marker is incumbent-authored text; Theo's
     refusals are typed codes. A count-store query plan may also draw `PLAN_REJECTED`.
   - Leg d (`search` and `brain_search` carry no local-path strings): PASSES.
   - Leg e: INVERTS ENTIRELY. It asserts Memgraph index names that Theo's Aura instance does not
     have, and Theo's `brain_stats` carries no index block at all.
   Do NOT invoke rollback on legs b, c or e. Record which legs actually inverted against this list.

6. The D-03 regression check, both shapes. Call `orchestration_readiness` for a framework Theo does
   NOT carry, then for one Theo carries with thin structure. Then inspect the room's enrichment
   queue file.
   EXPECTED: one entry whose `probe_provenance` starts `orchestration_readiness_theo_refusal@`, and
   a second whose `probe_provenance` starts `orchestration_readiness_theo@` with
   `dimensions_inferred: false`. Both shapes captured, or the D-03 fix did not land in the shipped
   bytes. Also confirm no entry contains a Theo `refusal.detail` string (Canon Part 8).

7. Record the COLD-START LATENCY: the wall-clock seconds of the FIRST Brain call in the fresh
   session, against the 20 second abort budget. Session T needs this number for its flip record.

8. Never call a WRITE endpoint during verification. `brain_write` and a deliberate `POST /register`
   are both write-shaped. The automatic `/register` fired by `_tryAutoRegister` when no key resolves
   is the shipped path and is fine; a manual one is not.

If step 2 or step 3 fails, STOP and report. That is the rollback trigger, and the rollback is one
line: revert `lib/core/brain-client.cjs` line 24 in a patch release, or set
`MINDRIAN_BRAIN_URL=https://pws-brain-mcp.onrender.com` per install. Both are valid ONLY while the
incumbent still runs, which it does, because decommission waits for the soak.
  </how-to-verify>

  <verify>
    <human-check>
      Cached plugin version matches the flip tag; cached BRAIN_URL is the bare Theo origin;
      brain_stats through Larry returns {nodes, relationships, labels} and NOT memgraph 29,200;
      brain_ask returns material rather than composed prose; probe legs b, c and e inverted as
      documented while a and d passed; the enrichment queue gained one Theo-refusal entry and one
      Theo-scored entry with dimensions_inferred false; cold-start latency recorded in seconds.
    </human-check>
  </verify>

  <acceptance_criteria>
    - The cached plugin version and the cached `BRAIN_URL` line are both quoted verbatim in the SUMMARY, read from `~/.claude/plugins/cache/`, not from the working tree.
    - The `brain_stats` payload shape is quoted verbatim and is `{nodes, relationships, labels}` shaped. The node count is recorded.
    - The `brain_ask` response is characterized as material or prose, with a short quote.
    - `node scripts/probe-brain-contract.cjs`'s FULL output is recorded verbatim, and each inverted leg is matched against the expected-inversion list by name.
    - Two enrichment queue entries are recorded with their `probe_provenance` prefixes, and the absence of any `refusal.detail` string is confirmed.
    - Cold-start latency is recorded in seconds against the 20 second budget.
    - Nothing was written under `~/.claude/plugins/`. No write endpoint was called.
    - If step 2 or 3 failed, the SUMMARY records the rollback decision and the exact command used, and Tasks 2 and 3 report the failure rather than the success signal.
  </acceptance_criteria>

  <resume-signal>Type "flip verified" plus the cached version and the brain_stats node count, or "flip failed" plus which step failed and whether rollback was invoked.</resume-signal>

  <done>The installed release is proven to talk to Theo end to end through Larry, with every documented red recorded as expected, or the failure is recorded with the rollback decision.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: ORCHESTRATOR ACTION - send Session T the resume signal and the six flip-day fields</name>

  <read_first>
    - `/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-12-CLOSEOUT-STAGING.md` section F (READ ONLY, never edit): the `09-MOS-LEARNING.md` addendum template whose blanks this report fills
    - `/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-12-PLAN.md` Task 2 (READ ONLY): the resume-signal contract this report satisfies
    - `.planning/phases/339-.../339-13-SUMMARY.md`: the shipped flip tag and Session T's same-minute reading
    - `.planning/phases/339-.../339-04-SUMMARY.md`: the D-13 deviation statement, which answers the `brain_schema` flush field
    - This plan's Task 1 SUMMARY: the cached version, the `brain_stats` shape, the probe leg inversions and the cold-start latency
  </read_first>

  <action>
STOP. This task is performed by the ORCHESTRATOR, not by the plan executor. The executor has no
messaging tool. Do not simulate the message and do not write it to a file as a substitute.

The orchestrator sends Session T, working in `/home/jsagi/Theo`, the resume signal for 09-12
Task 2. The first token is the shipped version, followed by the phrase:

  <version> flip verified

where `<version>` is read from `git describe --tags --abbrev=0` in
`/home/jsagi/dev/MindrianOS-Plugin`, never from this plan's prose.

Carry with it the SIX flip-day fields that `09-12-CLOSEOUT-STAGING.md` section F leaves blank.
Fill each from a recorded observation, never from an expectation:

1. Live base URL the plugin default now names: `https://theo-mcp.onrender.com`, the BARE origin;
   the client appends `/mcp` and `/register`. Quote the cached `BRAIN_URL` line from Task 1 as the
   evidence, because that is what users actually run.
2. Plugin release carrying it: the shipped flip tag, read back from the tag.
3. Cold-start latency: the number from Task 1 step 7, in seconds, against the 20 second abort
   budget. Note that a post-spin-down cold start does not exist on the paid tier, so this is
   first-execution latency on the process serving at flip time.
4. `/register` route status at flip time: the HTTP code and body key observed from the INSTALLED
   client. This comes from `_tryAutoRegister`'s own automatic call, which is the shipped path. Do
   NOT fire a manual `POST /register` to produce this number.
5. Decommission date: BLANK. That is Session T's Task 3, after the soak. Say "not yet; incumbent
   still running as the rollback path" rather than leaving the field ambiguous.
6. `brain_schema` flush: answer verbatim, per the D-13 premise correction recorded in 339-04's
   SUMMARY:

   `no flush needed: memo is process-local, keyed by origin since beta.17`

Also carry, because Session T's flip-record section 4 and its content note both need them:
- Which probe legs inverted, by name, and that they are EXPECTED and documented.
- The content note verbatim from the coverage ruling: `/mos:leadership` and due-diligence consults
  answer thinner through Theo until the 30 uncovered names are ingested; the response is an
  honest-empty coverage block, not an error.
- The two enrichment queue entries proving the D-03 fix landed in the shipped bytes.

If Task 1 reported a FAILURE rather than a verification, send that instead: what failed, at which
step, and whether rollback was invoked and by which of the two paths. Do not send "flip verified"
on a failed verification under any circumstance. The whole value of this seam is that the signal
means what it says.

Perform no repository writes under `/home/jsagi/Theo/`.
  </action>

  <verify>
    <automated>awk '/^## F\./{f=1} f' /home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-12-CLOSEOUT-STAGING.md | grep -Fq 'brain_schema' && git -C /home/jsagi/dev/MindrianOS-Plugin describe --tags --abbrev=0 && echo "SECTION_F_PRESENT: six fields awaiting this report"</automated>
  </verify>

  <acceptance_criteria>
    - The orchestrator sent the resume signal to Session T, and the exact message is quoted in the SUMMARY with its timestamp.
    - The version token in the signal matches `git describe --tags --abbrev=0` exactly.
    - All six section F fields are present in the message, each traceable to a recorded observation in a SUMMARY rather than to an expectation.
    - Field 6 reads verbatim: `no flush needed: memo is process-local, keyed by origin since beta.17`.
    - Field 5 is explicitly blank with the "not yet; incumbent still running as the rollback path" wording.
    - The probe leg inversions, the content note and the two enrichment entries are all carried.
    - If Task 1 failed, the message reports the failure and rollback decision, and the phrase "flip verified" does NOT appear in it.
    - Zero files under `/home/jsagi/Theo/` were modified.
  </acceptance_criteria>

  <resume-signal>Type "T signalled" plus the exact message sent, or "T not reachable" plus how long the wait was.</resume-signal>

  <done>Theo's Phase 9 plan 09-12 Task 2 has the resume signal it has been waiting for, with every blank in its close-out template filled from a real observation.</done>
</task>

<task type="auto">
  <name>Task 3: Close out Phase 339 in STATE.md and ROADMAP.md, and fill the tester note version</name>

  <files>.planning/STATE.md, .planning/ROADMAP.md, docs/testers/outbox/2026-09-03-theo-cutover.md</files>

  <read_first>
    - `.planning/ROADMAP.md` section `### Phase 339`: the goal, the six numbered work items, the Tri-Polar note, the Rollback note, the Coordination note, the `**Requirements**:` line updated by plan 339-01, and the `**Plans:**` and `Plans:` list
    - `.planning/STATE.md` frontmatter: `stopped_at`, `last_activity`, and the `progress` block
    - `.planning/STATE.md` header comments: the documented resync-clobber bug, sixty-plus occurrences. If the SDK's own `state.update-progress` or `state.record-session` clobbers a counter, hand-correct it and add a NOTE comment in the same style as the existing ones rather than re-investigating
    - All thirteen prior SUMMARY files in this phase directory: the facts this close-out records must come from them
    - `docs/testers/outbox/2026-09-03-theo-cutover.md`: the `version` placeholder token to fill
  </read_first>

  <action>
Three close-out edits. Every number comes from a SUMMARY, never from this plan's prose.

1. `docs/testers/outbox/2026-09-03-theo-cutover.md`: replace the `version` placeholder token in the
   front matter with the actually-shipped FLIP version, read from `git describe --tags --abbrev=0`.
   Leave `status: drafted` and `sent_to: []` UNCHANGED. Leave the suspend-date placeholder in the
   body UNCHANGED: Theo 09-12 Task 3 has not run and no date exists. This phase files a draft and
   never sends.

2. `.planning/ROADMAP.md` Phase 339 entry:
   - Update `**Plans:**` from `0 plans` to `14/14 plans complete`, or to the actual completed count
     if any plan halted at a gate.
   - Replace the `- [ ] TBD (run /gsd-plan-phase 339 to break down)` line with the real plan list,
     grouped by wave, each line naming the plan file and a one-line objective, checked off for
     every plan that completed.
   - Add a close-out paragraph recording, from the SUMMARYs: both shipped versions (the PREP tag
     and the FLIP tag), the fact that line 24 now names `https://theo-mcp.onrender.com` as a bare
     origin, the cold-start latency, which probe legs inverted, and that the resume signal was sent
     to Session T.
   - Record what is deliberately NOT closed by this phase and who owns it: the decommission
     (Theo 09-12 Task 3, operator-held, after a soak the navigator sets, suspend and never delete,
     compute before data), the tester note send (the operator's, after a date exists), the
     re-census against Theo (deferred, with plan 339-07's three reasons), the
     `lib/brain/chain-recommender.cjs` Theo-shape adaptation (D-03 consumer 2, named follow-up),
     and the `brain_write` and `ingest_framework` paths meeting `WRITE_PATH_DISABLED` (its own
     phase).

3. `.planning/STATE.md`: record the phase close via the SDK where it works
   (`gsd-tools query state.record-session` and `state.update-progress`), then VERIFY the frontmatter
   afterwards. This repo's STATE.md has a documented resync-clobber bug with sixty-plus recorded
   occurrences: the SDK writes can silently revert `completed_phases`, `completed_plans`, `percent`
   or `last_activity` to stale values. Read the frontmatter back after every SDK write. If a
   counter was clobbered, hand-correct it and add a NOTE comment in the same style as the existing
   ones, naming this plan and the occurrence. Do not re-investigate the root cause here; it is
   tracked for a dedicated `/gsd-debug` session.

`.planning/` is gitignored: stage `STATE.md` and `ROADMAP.md` with `git add -f` by explicit path.
`docs/testers/outbox/2026-09-03-theo-cutover.md` is tracked and staged normally. Never `git add .`
and never `git add -A`.

Do NOT mark FLIP-01 through FLIP-12 complete in `.planning/REQUIREMENTS.md` in this task. That
finalization belongs to `/gsd-verify-work`, which measures rather than asserts, and the rows were
registered as `- [ ]` by plan 339-01 for exactly that reason.

No em-dashes.
  </action>

  <verify>
    <automated>grep -A2 '^### Phase 339' .planning/ROADMAP.md | head -5 && grep -c '339-[0-9][0-9]-PLAN.md' .planning/ROADMAP.md && grep -Fq 'status: drafted' docs/testers/outbox/2026-09-03-theo-cutover.md && grep -Fq 'sent_to: []' docs/testers/outbox/2026-09-03-theo-cutover.md && head -12 .planning/STATE.md && ! grep -q $'\u2014' .planning/ROADMAP.md && echo CLOSEOUT_OK</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 339's `**Plans:**` line records the real completed count, and the plan list names all fourteen plan files grouped by wave.
    - The close-out paragraph records both shipped tags, the bare Theo origin, the cold-start latency, the inverted probe legs and the resume signal, each traceable to a SUMMARY.
    - The five deliberately-not-closed items are named with their owners.
    - `docs/testers/outbox/2026-09-03-theo-cutover.md` carries the real shipped version, and still carries `status: drafted`, `sent_to: []` and the unfilled suspend-date placeholder.
    - `.planning/STATE.md` frontmatter was READ BACK after every SDK write, and any clobbered counter was hand-corrected with a NOTE comment naming this plan.
    - `.planning/REQUIREMENTS.md` FLIP rows are still `- [ ]`; this task did not mark them complete.
    - All three files staged by explicit path, `.planning/` ones with `git add -f`.
    - `grep -c` for the em-dash character returns 0 in `.planning/ROADMAP.md` and in the tester note.
  </acceptance_criteria>

  <done>Phase 339 is closed with both shipped versions recorded, and everything this phase deliberately did not close is named with its owner rather than left to be rediscovered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install cache -> verification claim | the dev tree and the cache can disagree, and only the cache is what users run |
| this repo -> Session T | the resume signal is a claim another session acts on; a false one triggers a decommission against an unverified flip |
| live Brain surface -> verification | a verification that calls a write endpoint changes the thing it measures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-33 | Repudiation | verifying the flip by reading the dev tree rather than the install cache | mitigate | every acceptance criterion in Task 1 requires a value quoted from `~/.claude/plugins/cache/`, and step 2 fails the whole verification if the cached `BRAIN_URL` still names the incumbent |
| T-339-40 | Repudiation | sending "flip verified" on a failed or partial verification, which would authorize Session T's decommission against an unverified flip | mitigate | Task 2 forbids the phrase on any failed verification and requires the failure and rollback decision to be sent instead; Task 1's resume signal has an explicit failure branch |
| T-339-41 | Denial of Service | treating the probe's documented red legs as failures and invoking rollback on a correct release | mitigate | the expected-inversion list is read BEFORE running the probe, each leg is matched by name, and the CHANGELOG carries the same list |
| T-339-42 | Tampering | a deliberate write call (`brain_write` or a manual `POST /register`) fired during verification | mitigate | stated prohibition in Task 1 step 8 and in Task 2 field 4; the `/register` number comes from `_tryAutoRegister`'s own automatic call, which is the shipped path |
| T-339-08 | Information Disclosure | a Theo `refusal.detail` string reaching the local enrichment queue in the SHIPPED bytes | mitigate | Task 1 step 6 inspects the real queue file after two live calls and confirms no `detail` string is present, which tests the shipped artifact rather than the source |
| T-339-43 | Repudiation | the tester note going out with a suspend date that does not exist | mitigate | Task 3 fills only the `version` field and leaves `status: drafted`, `sent_to: []` and the date placeholder untouched; sending is the operator's after Theo 09-12 Task 3 |
| T-339-44 | Repudiation | STATE.md's resync-clobber silently reverting the close-out counters | mitigate | the frontmatter is read back after every SDK write, any clobber is hand-corrected with a NOTE comment in the documented style, and the root cause stays tracked for its own debug session |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | zero package installs in this plan and in the phase |
</threat_model>

<verification>
- The cached plugin version matches the flip tag, and the cached `BRAIN_URL` names the bare Theo origin. Both quoted from the cache.
- `brain_stats` through Larry returns `{nodes, relationships, labels}` and not `backend memgraph` with 29,200 nodes.
- `node scripts/probe-brain-contract.cjs` output is recorded verbatim, with legs b, c and e matched against the expected-inversion list.
- Two enrichment queue entries carry the two Theo `probe_provenance` prefixes, and neither carries a `refusal.detail` string.
- The resume signal sent to Session T is quoted verbatim and its version token matches `git describe --tags --abbrev=0`.
- `.planning/ROADMAP.md` Phase 339 lists all fourteen plans and records both shipped tags.
- Nothing was written under `~/.claude/plugins/` or under `/home/jsagi/Theo/`.
</verification>

<success_criteria>
- FLIP-12 is verified against the installed artifact, or its failure is recorded with the rollback decision.
- Session T has the resume signal and all six flip-day fields, each from a real observation.
- Phase 339 is closed with both shipped versions, and every deliberately-open item is named with its owner.
- No em-dashes in any file this plan touched.
</success_criteria>

<output>
Create `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-14-SUMMARY.md` when done. Record verbatim: the cached version and `BRAIN_URL` line, the `brain_stats` payload shape, the full probe output, the two enrichment `probe_provenance` prefixes, the cold-start latency, and the exact resume-signal message sent to Session T.
</output>
