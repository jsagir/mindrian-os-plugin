---
phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig
plan: 09
type: execute
wave: 2
depends_on: ["339-01", "339-02", "339-03"]
files_modified:
  - docs/339-NOTE-theo-desktop-connector-key.md
  - .planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-05-PLAN.md
autonomous: true
requirements: [FLIP-07, FLIP-08]
canon_parts: [7, 8]

must_haves:
  truths:
    - "The path Theo's shipped README already cites resolves to a real document in this repo instead of a 404"
    - "The note states the MECHANISM, not just the instruction: why the connector key and not the host is what matters"
    - "Phase 269-05's gate can no longer read PASS while the leg it exists to test goes unchecked"
    - "The three retired checklist items are retired IN PLACE with a dated reason, never silently deleted"
  artifacts:
    - path: "docs/339-NOTE-theo-desktop-connector-key.md"
      provides: "The plugin-side half of a decision Theo already shipped: the connector key, the URL, the header, and the egress-guard reason"
      contains: "BRAIN_TOOL_MATCHER"
      min_lines: 60
    - path: ".planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-05-PLAN.md"
      provides: "Task 1 rewritten to the three real legs against live sources"
      contains: "09-FLIP-RECORD.md"
  key_links:
    - from: "docs/339-NOTE-theo-desktop-connector-key.md"
      to: "lib/core/brain-response-sanitize.cjs BRAIN_TOOL_MATCHER"
      via: "the mechanism section, naming the matcher and the unconditional allow"
      pattern: "part8-egress-guard-hook.cjs"
    - from: "269-05-PLAN.md Task 1 leg (a)"
      to: "/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md"
      via: "the coverage ruling subsection, NOT SEED-004"
      pattern: "Coverage re-measurement"
---

<objective>
Two written artifacts, both of which fix a document that currently reads greener than the evidence.

1. `docs/339-NOTE-theo-desktop-connector-key.md` (D-10 as corrected, FLIP-07). This was scoped as an
   ASK: write a note requesting Theo's README prescribe `mindrian-brain`. Research found the ask
   already inverted. Theo commit `11d6f82` landed the README side, already prescribes
   `mindrian-brain`, already drops the `Authorization` header, and already forward-references this
   exact path in this repo. That path 404s today. The note is therefore a RECIPROCAL RECORD, a
   hard deliverable of the PREP cut, not a proposal.
2. Phase 269-05 Task 1's checklist rewrite (D-14 as corrected, FLIP-08). Its six items read 3-of-3
   PASS on the first three today, because Theo Phase 9 has plans and Phases 7 and 8 are closed,
   while the real content and infrastructure legs were never tested. Item 4 names the INCUMBENT
   host for a `/register` check, which is the wrong host post-flip. Item 6's validity window
   expires 2026-09-10.

Purpose: a checklist that can read green while its real leg is untested is worse than no
checklist, because it converts an unknown into a false confidence. That is the exact failure mode
D-14 names and this plan closes.

Output: one new doc, one planning artifact rewritten, two tests turned green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-PATTERNS.md
@.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-05-PLAN.md
</context>

<artifacts_this_phase_produces>
Symbols this PLAN creates (new to the repo, excluded from drift verification):

| Symbol | Kind | File |
|--------|------|------|
| `docs/339-NOTE-theo-desktop-connector-key.md` | new file (cross-repo note) | itself |

Files this plan REWRITES in place: `269-05-PLAN.md` Task 1 only. Tasks 2 and 3 of that plan, its
frontmatter and its objective are untouched. `.planning/` is gitignored, so it is staged with
`git add -f` by path.

Files this plan explicitly does NOT touch: anything under `/home/jsagi/Theo/` (Session T owns that
repo), `commands/leadership.md` (naming a temporary 30-name coverage gap in a command's help text
would be a user-facing behavior claim about a state that goes stale the moment the names are
ingested), and `lib/core/brain-response-sanitize.cjs` or `hooks/hooks.json` (the matcher is
described by the note, never widened by it).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write docs/339-NOTE-theo-desktop-connector-key.md (FLIP-07, D-10 as corrected)</name>

  <files>docs/339-NOTE-theo-desktop-connector-key.md</files>

  <behavior>
    - `bash tests/test-339-cross-repo-note.sh`, red at the end of wave 1, goes GREEN.
    - The file exists at exactly `docs/339-NOTE-theo-desktop-connector-key.md`, which is the path Theo's shipped README cites. The filename is fixed, not chosen.
    - It contains `mindrian-brain`, `https://theo-mcp.onrender.com/mcp`, `BRAIN_TOOL_MATCHER`, `part8-egress-guard-hook.cjs`, and the Theo commit `11d6f82`.
    - It contains zero em-dashes.
  </behavior>

  <read_first>
    - `docs/257-NOTE-part8-enforcement-locus-rulings.md` lines 1 to 13: the metadata header form (`**Phase:**`, `**Date:**`, `**Status:**`) and the "this document does NOT amend" clause. This is the closer of the two analogs
    - `docs/254-NOTE-theo-adaptation-list-additions.md` lines 1 to 33: the section spine (`## 1. The Ask, Up Front`, `## 2. The Break, Precisely`, `## 3. What This Phase Did, and What It Deliberately Did Not`) and the "give the recipient the current list" courtesy
    - `lib/core/brain-response-sanitize.cjs:54-61`: `BRAIN_TOOL_MATCHER` and its byte-parity comment
    - `hooks/hooks.json` lines 239 and 341: the two byte-identical matcher twins
    - `tests/test-brain-response-sanitize.cjs:285-315`: the parity assertion that keeps all three in lockstep
    - `scripts/part8-egress-guard-hook.cjs:142-155`: the `if (!sanitizer.isBrainTool(toolName)) return allow();` at `:152` and the fail-OPEN catch at `:153-155`. This is the mechanism sentence the whole note exists to state
    - `/home/jsagi/Theo/README.md` lines 127 to 149 (READ ONLY, never edit): the callout at Theo commit `11d6f82` that already prescribes `mindrian-brain`, already drops the header, already cites this path, and the three-way header-indifference finding at `:146-149`
    - `339-RESEARCH.md` Design 7 IN FULL: the recommended structure section by section, and the `/mcp` versus bare-origin asymmetry sentence
    - `339-RESEARCH.md` DRIFT-2: why the framing changes from "an ask" to "the reciprocal record"
    - `339-CONTEXT.md` `<specifics>`: the coverage-ruling heading contract is the ONE thing Session T must be told verbatim, and it belongs in this note alongside the connector-key record
  </read_first>

  <action>
Create `docs/339-NOTE-theo-desktop-connector-key.md`. The path is fixed by Theo's shipped README;
do not rename it, do not add a date prefix, do not move it.

Metadata header in the `257-NOTE` form: `**Phase:**` the full 339 slug, `**Date:** 2026-09-03`,
`**Status:** RECIPROCAL RECORD.` State up front that Theo's README at commit `11d6f82` already
prescribes `mindrian-brain`, already drops the `Authorization` header and already cites this file
by path, so this is the plugin-side half of a decision Session T has already shipped, not a
proposal. Then the "does NOT amend" clause: this document does not amend `docs/MINDRIAN-CANON.md`,
does not widen `BRAIN_TOOL_MATCHER`, and does not change `hooks/hooks.json`.

Five sections, following the `254-NOTE` spine with section 1 renamed because the ask is already
satisfied:

`## 1. The record, up front` - use `mindrian-brain` as the Claude Desktop and Cowork connector key.
Point it at `https://theo-mcp.onrender.com/mcp`, WITH the `/mcp` path. No `Authorization` header.
State the asymmetry in its own sentence: a direct connector hits the MCP endpoint itself, while
`lib/core/brain-client.cjs` line 24 takes the BARE origin because the client appends `/mcp` and
`/register` itself; getting that backwards produces `/mcp/mcp`, a 404 the client renders as
"Brain unreachable" rather than a config error.

`## 2. Why the key and not the host` - the mechanism, quoted with file and line. Give
`BRAIN_TOOL_MATCHER`'s exact value from `lib/core/brain-response-sanitize.cjs:61`, note it is
byte-mirrored at `hooks/hooks.json:239` and `:341` with parity enforced by
`tests/test-brain-response-sanitize.cjs`, and state that a connector registered as `theo` produces
`mcp__theo__*` names that match neither. Then the consequence:
`scripts/part8-egress-guard-hook.cjs:152` allows UNCONDITIONALLY when `isBrainTool(toolName)` is
false, so those calls run outside the Canon Part 8 egress guard AND outside the response
sanitizer. Say plainly that NO test catches this, because no test can see a connector a user
registered by hand, and name that as the residual risk.

`## 3. The two paths that already exist, and the one that does not` - `mindrian-brain` and
`pws-brain-mcp` are both recognized by the matcher; `theo` is not. Give the current alternation
verbatim so the reader can see this is a statement about a known list rather than a new claim.

`## 4. Why the plugin is not widening the matcher` - reasoned, not asserted. A third alternation
token would legitimize a key whose only purpose is standalone Theo use, and it would make the
guard's own vocabulary a moving target that every future backend adds to. The fix belongs where
the instruction is, which is Theo's README, and Theo has already made it at `11d6f82`.

`## 5. The other thing Session T must be told verbatim` - quote the D-06 coverage-ruling heading
contract exactly: the gate in this repo reads the subsection headed exactly
`### Coverage re-measurement, 2026-09-03, and the ruling on it` inside `## 1. Authorization
evidence`, scoped by HEADING and never by line number, and requires eleven literal `grep -F`
matches inside that subsection including the ruling sentence
`Coverage does NOT block Task 2, the flip`. State that amending the heading, moving the subsection
or changing that sentence will report "coverage held" and stop the flip release before
`release.sh`, which is a successful gate outcome and not a stall.

Also record, in section 3 or a short closing section, the two dispositions this phase made that a
Theo reader would otherwise have to discover: the census artifacts
(`data/brain-census.generated.json`, `docs/BRAIN-GRAPH-CENSUS.generated.md`) are deliberately left
describing the incumbent's graph, with a re-census registered as deferred; and the plugin's
`brain_write` and `ingest_framework` paths meet Theo's `WRITE_PATH_DISABLED`, which is a named
follow-up phase rather than part of this cutover.

Also name the three Theo-side hashes on the seam so the record is complete: `81dfac8` (the
coverage ruling), `221df3e` (the close-out staging), `11d6f82` (the README).

No em-dashes anywhere. Feynman-simplified prose, no jargon the mechanism does not require.
  </action>

  <verify>
    <automated>bash tests/test-339-cross-repo-note.sh && grep -Fq 'BRAIN_TOOL_MATCHER' docs/339-NOTE-theo-desktop-connector-key.md && grep -Fq 'Coverage does NOT block Task 2, the flip' docs/339-NOTE-theo-desktop-connector-key.md && grep -Fq '11d6f82' docs/339-NOTE-theo-desktop-connector-key.md && grep -Fq '81dfac8' docs/339-NOTE-theo-desktop-connector-key.md && grep -Fq '221df3e' docs/339-NOTE-theo-desktop-connector-key.md && echo NOTE_OK</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/test-339-cross-repo-note.sh` exits 0.
    - The file is at exactly `docs/339-NOTE-theo-desktop-connector-key.md` and is at least 60 lines.
    - All five sections are present with the section-1 heading reading "The record, up front", not "The ask".
    - The mechanism section quotes `BRAIN_TOOL_MATCHER`'s value, names `hooks/hooks.json:239` and `:341`, names `part8-egress-guard-hook.cjs:152`, and states in plain words that no test catches a hand-registered connector.
    - The `/mcp` versus bare-origin asymmetry has its own sentence.
    - All three Theo hashes (`81dfac8`, `221df3e`, `11d6f82`) appear.
    - Zero files under `/home/jsagi/Theo/` were modified: `git status --porcelain` in that repo is byte-identical before and after this task.
    - `grep -c` for the em-dash character returns 0.
  </acceptance_criteria>

  <done>The path Theo's shipped README sends readers to resolves to a real document, and the reason the connector key matters is written down in the repo that owns the guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite Phase 269-05 Task 1's checklist to the three real legs (FLIP-08, D-14 as corrected)</name>

  <files>.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-05-PLAN.md</files>

  <behavior>
    - `bash tests/test-339-269-05-checklist.sh`, red at the end of wave 1, goes GREEN.
    - Task 1's checklist reads three legs, labelled `Leg (a)`, `Leg (b)` and `Leg (c)`, each with a live source path.
    - Leg (a) points at `09-FLIP-RECORD.md`'s coverage subsection, NOT at SEED-004.
    - The three retired items appear in place with a dated retirement reason on the same line, never deleted.
    - The `/register` host in the retained item reads `theo-mcp.onrender.com`.
    - The XML skeleton of Task 1 is unchanged: it is still `checkpoint:human-action gate="blocking"`, still has read_first, action, verify, acceptance_criteria, resume-signal and done blocks.
  </behavior>

  <read_first>
    - `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-05-PLAN.md` Task 1 IN FULL (lines 82 to 136): the six items, the verify block, the acceptance criteria, the resume signal and the done line. This is a REWRITE OF THE ITEMS INSIDE AN UNCHANGED SKELETON
    - `339-RESEARCH.md` Design 6 IN FULL: what Task 1 says today item by item, the confirmed danger, the three rewritten legs with their sources and automated checks, the retirement wording, and the item-6 recommendation
    - `339-RESEARCH.md` DRIFT-3: SEED-004's latest dated `## UPDATE` is the 2026-09-02 one and it reads "not yet" on both legs. The 2026-09-03 re-measurement and ruling live in `09-FLIP-RECORD.md:301-470` and explicitly supersede that addendum on leg one. CONTEXT D-14's instruction to read "SEED-004 latest dated UPDATE (never from the 09-02 addendum)" is self-contradictory at HEAD, because the latest dated UPDATE IS the 09-02 one
    - `/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md` lines 295 to 470 (READ ONLY): the coverage subsection, the ruling, and the "Leg two, re-read against what is on disk today" paragraph with its 06.3 caveat
    - `/home/jsagi/Theo/.planning/phases/08.4-remote-hosting-mcp-server/08.4-MOS-LEARNING.md` lines 45 to 52 (READ ONLY): the `POST /register` compat route record
    - `/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-12-CLOSEOUT-STAGING.md` section B (READ ONLY): the drift clause now bound to Task 3
    - `tests/test-339-269-05-checklist.sh` as written by plan 339-03: it is the target state
  </read_first>

  <action>
Rewrite the ITEMS inside `269-05-PLAN.md` Task 1. Do not change the task type, the gate attribute,
the block structure, or Tasks 2 and 3 of that plan.

Replace the six items with three legs plus a retirement note.

`Leg (a): coverage re-measured live against a PINNED Brain count.` Source:
`/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md`, the subsection
headed exactly `### Coverage re-measurement, 2026-09-03, and the ruling on it`, or any later dated
re-measurement recorded in the same section. Explicitly NOT SEED-004. State in the item that this
is a navigator RULING recorded in writing, not a mechanical threshold, and that the named-Frameworks
RATIO is RETIRED as a coverage measure by ruling clause 4: coverage is a SET question, meaning of
the Brain's 258 names, how many resolve in Theo by name. Record the current reading verbatim:
`Covered: 228 of 258, 88.4%` and `Uncovered: 30 of 258`. The automated check is the same
heading-scoped `awk` extraction plus `grep -F` block the 339 gate uses, reduced to the ruling
sentence plus the `Brain@56bf75a` pin.

`Leg (b): Theo Phase 06.2 live, meaning summaries on disk.` Automated check:
`ls /home/jsagi/Theo/.planning/phases/06.2-*/06.2-*-SUMMARY.md | wc -l` is at least 1. Current
state per the flip record: 4 of 4 summaries, closed 2026-09-02, so this passes on its own terms.
The item MUST also carry the flip record's own honesty caveat, or it reads greener than the
evidence: the mechanism that would keep the coverage table current is Theo Phase 06.3 (the
bidirectional drift detector, SEED-005), which has 0 plans and 0 summaries. Leg (b) as originally
specified passes; the part of leg (b) that bears on DRIFT does not. Write both.

`Leg (c): 09-12 infrastructure legs.` Three sub-checks, each with a source:
- Theo Phase 08.4 closed. Check: `08.4-MOS-LEARNING.md` exists and is non-empty.
- 09-11 remote parity, 0 mismatches. Check: `09-11-SUMMARY.md` exists, and the flip record's
  `### The remote ledger verdict (09-11, 2026-09-03)` subsection carries the verdict.
- `/register` compat route live. Check: `08.4-MOS-LEARNING.md` records that a `POST /register`
  route exists and hands an opaque token to anybody who asks. RETARGET the host from
  `pws-brain-mcp.onrender.com` to `theo-mcp.onrender.com`. Do NOT probe it live from the gate:
  `POST /register` is a WRITE-shaped call and the gate is contractually zero-write.

Retirement note, written IN PLACE, never a silent deletion, on lines that carry both the old text
and the reason so `tests/test-339-269-05-checklist.sh`'s "every `Plans: TBD` line also says
`Retired`" assertion holds:

  Retired 2026-09-03 (Phase 339, D-14). These three read PASS from 2026-08-27 onward while the
  real content and infrastructure legs went unchecked. Superseded by legs (a), (b), (c).

Item 6, the validity window: `269-RESEARCH.md` expires 2026-09-10. Replace the item with a pointer
to THIS phase's research rather than extending the window. The entitlement-gate research is no
longer what gates 269-05; Theo's readiness is. Say that in the item.

Update Task 1's `<verify><automated>` block to match the new legs: the heading-scoped `awk` plus
`grep -F` for the ruling sentence, the `ls` count for leg (b), and file-existence checks for leg
(c). Keep the zero-write acceptance criterion verbatim, because that plan's gate has the same
contract as this phase's.

`.planning/` is gitignored: stage with `git add -f` by explicit path. Never `git add .`.

No em-dashes.
  </action>

  <verify>
    <automated>bash tests/test-339-269-05-checklist.sh && grep -Fq 'Leg (a)' .planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-05-PLAN.md && grep -Fq '09-FLIP-RECORD.md' .planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-05-PLAN.md && grep -Fq 'theo-mcp.onrender.com' .planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-05-PLAN.md && grep -c 'Plans: TBD' .planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-05-PLAN.md</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/test-339-269-05-checklist.sh` exits 0.
    - Task 1 contains `Leg (a)`, `Leg (b)` and `Leg (c)`, each with a concrete source path.
    - Every line containing `Plans: TBD` also contains `Retired`.
    - `09-FLIP-RECORD.md` is named as leg (a)'s source; SEED-004 is not named as leg (a)'s source anywhere in the task.
    - The `/register` host reads `theo-mcp.onrender.com`; any occurrence of the incumbent host in the task sits on a `Retired` line.
    - Leg (b) carries the 06.3 drift caveat, so it cannot be read as a clean pass.
    - Task 1's task type is still `checkpoint:human-action` with `gate="blocking"`, and Tasks 2 and 3 of `269-05-PLAN.md` are byte-identical: `git diff` shows no hunk after Task 1's closing tag.
    - Zero files under `/home/jsagi/Theo/` were modified.
    - `grep -c` for the em-dash character returns 0.
  </acceptance_criteria>

  <done>Phase 269-05's gate now tests the thing it exists to test. Its first three items can no longer report PASS from a fact that was never in question while the real legs went unmeasured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo -> `/home/jsagi/Theo` | Session M reads Theo and never writes it; both tasks in this plan read cross-repo sources |
| a checklist's green reading -> a release decision | a gate that can pass without testing its real leg converts an unknown into false confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-02 | Information Disclosure | a Desktop connector registered under `theo`, escaping the egress guard and the response sanitizer | mitigate | the note states the mechanism with file and line, on both sides of the seam; Theo's README already carries its half at `11d6f82`. No test can catch a hand-registered connector, so the residual risk is stated in the note itself rather than implied |
| T-339-13 | Repudiation | the 269-05 checklist reading PASS while its real leg is unchecked | mitigate | three legs each with a live source and an automated check; the retired items stay visible with a dated reason so the history of the false green is not erased |
| T-339-26 | Repudiation | leg (a) sourced from SEED-004, whose latest dated UPDATE says "not yet" and is superseded | mitigate | leg (a) points at the flip record's coverage subsection explicitly, and `tests/test-339-269-05-checklist.sh` asserts `09-FLIP-RECORD.md` is named |
| T-339-09 | Tampering | either task writing to the Theo repo while reading it | mitigate | acceptance criteria on both tasks require `git status --porcelain` in `/home/jsagi/Theo` to be byte-identical before and after |
| T-339-27 | Repudiation | a live `POST /register` probe fired from a zero-write gate | mitigate | leg (c) checks the RECORD in `08.4-MOS-LEARNING.md`, never the live route; the reason is written into the item |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | zero package installs in this plan and in the phase |
</threat_model>

<verification>
- `bash tests/test-339-cross-repo-note.sh` and `bash tests/test-339-269-05-checklist.sh` both pass.
- `bash tests/run-all-339.sh` shows both arms green.
- `git status --porcelain` in `/home/jsagi/Theo` is byte-identical before and after this plan, verified and recorded.
- `docs/339-NOTE-theo-desktop-connector-key.md` is a NEW tracked file staged by explicit path; `269-05-PLAN.md` is staged with `git add -f`.
- No file under `commands/` was touched (the 30-name coverage gap is named in the CHANGELOG and the tester note by plan 339-10, never in `/mos:leadership` help text).
</verification>

<success_criteria>
- FLIP-07 and FLIP-08 are green.
- The cross-reference Theo already shipped now resolves in both directions.
- The 269-05 gate tests its real leg, and the false-green history is retired in place rather than erased.
- No em-dashes in either file.
</success_criteria>

<output>
Create `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-09-SUMMARY.md` when done. Record the `/home/jsagi/Theo` porcelain hash before and after, and quote the retirement line verbatim.
</output>
