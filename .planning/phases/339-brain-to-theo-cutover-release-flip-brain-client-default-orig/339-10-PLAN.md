---
phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig
plan: 10
type: execute
wave: 3
depends_on: ["339-04", "339-05", "339-06", "339-07", "339-08", "339-09"]
files_modified:
  - CHANGELOG.md
  - docs/testers/outbox/2026-09-03-theo-cutover.md
autonomous: true
requirements: [FLIP-04]
canon_parts: [7, 8, 12]

must_haves:
  truths:
    - "The PREP cut's CHANGELOG entry names every honesty limit rather than implying the release fixes what it cannot reach"
    - "The flip-day content fact travels with the release, verbatim from Session T, not paraphrased"
    - "A tester cutover note exists as a filed draft with the suspend date left as a placeholder, and it is not sent"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "The PREP cut entry under the [Unreleased] heading, with the stale label corrected"
      contains: "theo-mcp.onrender.com"
    - path: "docs/testers/outbox/2026-09-03-theo-cutover.md"
      provides: "The tester cutover note DRAFT, status drafted, sent_to empty, suspend date a placeholder"
      contains: "status: drafted"
      min_lines: 50
  key_links:
    - from: "CHANGELOG.md"
      to: "lib/core/refusal-messaging.cjs honest-limit comment"
      via: "the same sentence, carried verbatim so the CHANGELOG cannot imply more than the code delivers"
      pattern: "not live until released"
    - from: "docs/testers/outbox/2026-09-03-theo-cutover.md"
      to: ".claude/includes/release-process.md two-command update path"
      via: "the update path is the whole ask of the note"
      pattern: "plugin marketplace update"
---

<objective>
The PREP cut's written record. Two documents, both of which must be honest about what a release
cannot do.

1. The CHANGELOG entry for the PREP cut. Everything plans 339-04 through 339-09 shipped, in house
   form, each bullet naming the file, the phase id, the measured facts, and explicitly what did
   NOT change.
2. The tester cutover note DRAFT (D-11). Filed under `docs/testers/outbox/`, `status: drafted`,
   `sent_to: []`, with the suspend date left as an explicit placeholder because Theo 09-12 Task 3
   has not run and no date exists yet. This phase files a draft. Sending is the operator's.

Purpose: two populations are unreachable by any release. An install that has not run the
two-command update prints the OLD bytes, and a hand-registered Desktop or Cowork connector lives in
a config file that is not in any git repo. Neither is reachable by code. Both are addressed by the
soak window and the tester note, and by nothing else. If the CHANGELOG implies otherwise, the next
reader will believe the release fixed something it did not.

Output: one CHANGELOG entry, one filed draft.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-PATTERNS.md
@CHANGELOG.md
@docs/testers/STYLE-GUIDE.md
</context>

<artifacts_this_phase_produces>
Symbols this PLAN creates (new to the repo, excluded from drift verification):

| Symbol | Kind | File |
|--------|------|------|
| `docs/testers/outbox/2026-09-03-theo-cutover.md` | new file (email draft) | itself |

This plan writes NO code and creates no runtime symbol. It reads the SUMMARYs of plans 339-04
through 339-09 for its facts and must not restate a number those SUMMARYs did not measure.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write the PREP cut CHANGELOG entry, including the flip-day content fact verbatim</name>

  <files>CHANGELOG.md</files>

  <read_first>
    - `CHANGELOG.md` lines 1 to 60: the current `## [Unreleased] -- v2.0.0-beta.16 (in progress)` heading, its `### Added` subsection, and the released `## [2.0.0-beta.15] - 2026-08-28` entry below it for house form
    - `CHANGELOG.md` lines 34 to 38: the "What did NOT change" close, the shape this entry copies
    - The SUMMARY files of plans 339-04 through 339-09, all six, in the phase directory. Every fact in this entry comes from a SUMMARY, not from this plan's prose. Specifically needed: 339-04's D-13 deviation statement, 339-05's four storage-contract facts, 339-06's amended `RENDER_COPY` arrays, 339-07's three-reason generated-artifact disposition, 339-08's Phase 276 collision result and `brain_write` wording decision, 339-09's retirement line
    - `339-RESEARCH.md` section `Where the CHANGELOG entry goes`: `release.sh` Step 6 rewrites the whole `## [Unreleased]...` line to `## [<NEW_VERSION>] - <date>`, so the trailing `-- v2.0.0-beta.16 (in progress)` label is discarded and its being off by one is cosmetic. Correct it anyway as part of this plan
    - `339-RESEARCH.md` `Pitfall 2`: NEVER write a version number into a document. `release.sh --prerelease` produces beta.17 from a tree at beta.16, so a hard-coded version ships a number that does not exist
    - `339-RESEARCH.md` `Pitfall 10`: Step 6 issues an interactive `read -r REPLY` and opens `$EDITOR` if no `## [Unreleased]` heading exists. Inside an agent task that hangs forever. The heading must exist when 339-11 runs
    - `339-CONTEXT.md` D-06a: the flip-day content fact, verbatim from Session T
    - `339-RESEARCH.md` `Runtime State Inventory`, the closing paragraph: the two unreachable populations, stated plainly
  </read_first>

  <action>
Add this phase's entries under the existing `## [Unreleased]` heading, creating `### Changed` and
`### Fixed` subsections as needed alongside the existing `### Added`.

First, correct the heading LABEL from `-- v2.0.0-beta.16 (in progress)` to name the version this
cut will actually carry. Per `339-RESEARCH.md` DRIFT-1, `release.sh` takes a bump MODE and
increments from `plugin.json`'s CURRENT `2.0.0-beta.16`, so `--prerelease` produces
`v2.0.0-beta.17`. Write `## [Unreleased] -- v2.0.0-beta.17 (in progress)`. Do NOT write that
version anywhere else in this file or in the tester note; the heading is the one place it is
cosmetic, because Step 6 rewrites the whole line.

Then write the entries. House form: each bullet is a prose paragraph naming the file, the phase or
task id, the measured numbers, and explicitly what did NOT change. Cover:

- The origin-derived alias table (339-04, D-03 consumer 1). Name the mechanism: two frozen tables
  selected by the resolved origin through a `THEO_ORIGINS` set, so a `MINDRIAN_BRAIN_URL` change
  moves vocabulary and URL together. State what did NOT change: today's wire traffic is
  byte-identical, because the incumbent origin still selects the incumbent table.
- The origin-keyed `brain_schema` memo (339-04, D-13 as corrected). State the correction plainly:
  no flush exists to perform, because `BRAIN_URL` is a module-scope const and the memo is
  process-local, so no process can observe an origin change. Key-by-origin ships as defense in
  depth.
- The two additive enrichment capture arms (339-05, D-03 consumer 3, the phase's one true silent
  failure). Name the two-zeros distinction and the `unsynced_inputs` filter. Name the operational
  expectation so nobody reads it as a leak during soak: post-flip nearly every
  `orchestration_readiness` call will capture, because Theo's canon is thin (its own header reports
  148 of 149 Frameworks with zero technique linkage), the queue is bounded and deduped by framework
  name, and that is canon thinness correctly measured.
- The `brain-router.cjs` Tier-3 disclosure (339-05, D-03b). Name it as a fourth silent-degrade
  consumer that D-03 did not scope, now disclosed rather than shipped unnamed.
- The refusal copy and `lib/core/update-path.cjs` (339-06, D-08). Carry the honest limit VERBATIM
  from 339-06's SUMMARY: this copy ships in bytes, an install that has not updated prints the OLD
  string because a main commit is not live until released AND picked up, this is the durable fix
  for the next origin move and for honesty, and the levers that reach today's stale population are
  the soak window and the tester note. Do not soften that sentence.
- The literal sweep (339-07, D-12, FLIP-01). Name the two runtime sites now resolving through
  `getBrainUrl()`, and carry 339-07's three-reason generated-artifact disposition verbatim, with
  the re-census registered as deferred.
- The connector docs and regenerated mirrors (339-08, D-09, FLIP-06). Name the key that did NOT
  change (`mindrian-brain`), the URL that did (`https://theo-mcp.onrender.com/mcp`, WITH the path),
  the header that was dropped, and the reason (Theo returns byte-identical payloads for a real key,
  a garbage key and no header). State that `BRAIN_TOOL_MATCHER` and `hooks/hooks.json` were NOT
  touched.
- The cross-repo note and the 269-05 rewrite (339-09, D-10 and D-14, FLIP-07 and FLIP-08).

Then a dedicated paragraph carrying the flip-day content fact VERBATIM from Session T (D-06a). Do
not paraphrase it:

  `/mos:leadership` and due-diligence consults answer thinner through Theo until the 30 uncovered
  names are ingested; the response is an honest-empty coverage block, not an error.

State alongside it that the 30 uncovered names bind Theo's decommission task, NOT the flip, per the
coverage ruling, and that the named-Frameworks ratio is RETIRED as a coverage measure: coverage is
a set question, `Covered: 228 of 258, 88.4%`, `Uncovered: 30 of 258`.

Then a closing "What this release does NOT reach" paragraph naming the two populations plainly:
every install that has not run the two-command update, and every hand-registered Desktop or Cowork
connector, which lives in a config file no release touches. Say that the soak window and the tester
note are the only levers that reach them.

Also name, so nobody compresses it: decommission waits for the soak, the incumbent is SUSPENDED and
never deleted, compute before data, and the per-install `MINDRIAN_BRAIN_URL` rollback is valid ONLY
while the incumbent still runs.

Do NOT describe the flip itself. Line 24 has not moved yet; plan 339-12 writes that entry. Do not
claim any behavior that the PREP cut does not ship.

No em-dashes.
  </action>

  <verify>
    <automated>grep -qE '^## \[Unreleased\]' CHANGELOG.md && grep -Fq 'v2.0.0-beta.17' CHANGELOG.md && grep -Fq 'honest-empty coverage block, not an error' CHANGELOG.md && grep -Fq 'Covered: 228 of 258, 88.4%' CHANGELOG.md && grep -Fq 'not live until released' CHANGELOG.md && ! grep -q $'\u2014' CHANGELOG.md && echo CHANGELOG_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qE '^## \[Unreleased\]' CHANGELOG.md` succeeds. This is the Pitfall 10 precondition for `release.sh` Step 6; without it Step 6 opens an interactive editor and hangs.
    - The heading label reads `v2.0.0-beta.17`, and that version string appears nowhere else in `CHANGELOG.md` or in the tester note.
    - Every bullet names a file and a plan id, and at least four bullets carry an explicit "what did NOT change" clause.
    - The D-08 honest-limit sentence and the D-06a flip-day fact both appear VERBATIM, matched by `grep -F` against 339-06's SUMMARY and 339-CONTEXT.md respectively.
    - The three-reason generated-artifact disposition from 339-07's SUMMARY appears.
    - No entry describes the flip, line 24, or a change to the default origin. That belongs to 339-12.
    - `grep -c` for the em-dash character returns 0.
  </acceptance_criteria>

  <done>The PREP cut has a written record that names what it shipped, what it deliberately did not change, and what no release can reach.</done>
</task>

<task type="auto">
  <name>Task 2: File the tester cutover note as a DRAFT with the suspend date as a placeholder (D-11)</name>

  <files>docs/testers/outbox/2026-09-03-theo-cutover.md</files>

  <read_first>
    - `docs/testers/outbox/2026-07-23-v1.15.3-beta.44-everything-got-better.md`: the front matter template. Copy the key set: `type`, `version`, `status`, `gmail_draft_id`, `subject`, `sent_to`, `style_deviations`
    - `docs/testers/STYLE-GUIDE.md` IN FULL, especially the HARD RULE added 2026-05-25: every MindrianOS-family email links to the Mindrian website in three places, namely a body reference near the top, a corporate footer, and a logo wordmark linking back to the site. Note that the guide's own examples around lines 44 to 47 contain em-dashes; the repo hard rule OVERRIDES, and any divergence is recorded in the `style_deviations` block
    - `.claude/includes/release-process.md` lines 19 to 30: the two-command update path, verbatim
    - `339-CONTEXT.md` D-11: Feynman-simplified, LTR, no em-dashes, website link in three places, npm-led update path, M:OS v1.1 design tokens, signed `Js.`, standalone subject line, suspend date named, plus one reminder at suspend minus one week
    - `339-RESEARCH.md` Open Question 4 and its recommendation: draft BOTH notes at the flip cut, file both, send only the first, and state plainly in the draft that the note exists because an un-updated install will go dark at suspend and that the update path is the whole ask
    - `339-RESEARCH.md` `Runtime State Inventory`: the two unreachable populations
    - `339-CONTEXT.md` `<specifics>`: the two Theo failure modes the note should name rather than hide, `brain_write` always refusing with `WRITE_PATH_DISABLED` and `PLAN_REJECTED` on count-store query plans
  </read_first>

  <action>
Create `docs/testers/outbox/2026-09-03-theo-cutover.md`, a DRAFT. It is filed, not sent.

Front matter, copying the outbox template's key set exactly:
- `type`: a cutover notice
- `version`: leave it as an explicit placeholder token, NOT a number. Per Pitfall 2, the version is
  `release.sh`'s call at cut time and writing one here ships a number that may not exist. Plan
  339-14 fills it after the tag lands
- `status: drafted`
- `gmail_draft_id`: empty
- `subject`: a standalone subject line for this note, not a version announcement
- `sent_to: []`
- `style_deviations`: record any divergence from `docs/testers/STYLE-GUIDE.md`, and record
  explicitly that the guide's own em-dash examples are overridden by the repo hard rule

Body, Feynman-simplified, LTR, left-aligned, no em-dashes, M:OS v1.1 design tokens, signed `Js.`
Cover, in plain language:

1. What is changing, in one paragraph a non-engineer understands: the methodology graph Larry
   consults is moving to a new home. Nothing about how you use MindrianOS changes.
2. What YOU need to do, and this is the whole ask: run the two commands, verbatim from
   `.claude/includes/release-process.md`, `/plugin marketplace update` then
   `claude plugin update mos@mindrian-marketplace`. Lead with the npm-led update path per the
   standing rule.
3. WHY it matters, stated honestly rather than softened: the old service will be suspended after a
   soak window. An install that has not updated by then cannot reach the graph and Larry will
   refuse honestly rather than improvise. That is by design, and the fix is the two commands above.
4. The suspend date: an EXPLICIT PLACEHOLDER, written so it is obviously unfilled (a bracketed
   token, not a guessed date). State in the body that the date is not fixed yet and that a reminder
   follows one week before it. No date is promised before Theo 09-12 Task 3 fixes one.
5. What will feel different, named rather than hidden: `/mos:leadership` and due-diligence consults
   answer thinner for a while, because 30 framework names are still being moved across. That is an
   honest empty answer, not an error, and it closes as the names are ingested. Carry the D-06a fact
   in plain tester language, not the verbatim engineering sentence.
6. If you have a Claude Desktop or Cowork connector you set up by hand: you need to change its URL
   yourself, because no release can reach a config file on your machine. Give the new URL
   `https://theo-mcp.onrender.com/mcp` and say the connector KEY stays `mindrian-brain`. Point at
   `docs/brain-setup.md` for the full block.

Website link in THREE places per the standing hard rule: a body reference near the top, a corporate
footer, and a logo wordmark linking back to the site.

Also draft the SECOND note in the same file, under a clearly separated heading marked as the
suspend-minus-one-week reminder, so both exist and only the first is ever sent from this phase.
Keep it short: the date, the two commands, and nothing else.

Do NOT send. Do NOT create a Gmail draft. `status` stays `drafted` and `sent_to` stays empty.
Sending is the operator's, after Theo 09-12 Task 3 fixes a date.

Gate 10f note: this file is under `docs/`, not under `commands/`, `skills/`, `agents/` or
pipelines, so `check-plugin-path-anchoring.cjs --check-scripts` does not walk it. The two update
commands are user-facing CLI instructions, not repo script invocations, and must be written exactly
as a user types them.
  </action>

  <verify>
    <automated>test -f docs/testers/outbox/2026-09-03-theo-cutover.md && grep -Fq 'status: drafted' docs/testers/outbox/2026-09-03-theo-cutover.md && grep -Fq 'sent_to: []' docs/testers/outbox/2026-09-03-theo-cutover.md && grep -Fq '/plugin marketplace update' docs/testers/outbox/2026-09-03-theo-cutover.md && grep -Fq 'claude plugin update mos@mindrian-marketplace' docs/testers/outbox/2026-09-03-theo-cutover.md && grep -Fq 'https://theo-mcp.onrender.com/mcp' docs/testers/outbox/2026-09-03-theo-cutover.md && ! grep -q $'\u2014' docs/testers/outbox/2026-09-03-theo-cutover.md && node scripts/check-plugin-path-anchoring.cjs --check-scripts && echo NOTE_OK</automated>
  </verify>

  <acceptance_criteria>
    - The file exists at `docs/testers/outbox/2026-09-03-theo-cutover.md` and is at least 50 lines.
    - Front matter carries `status: drafted` and `sent_to: []`. No Gmail draft was created.
    - The `version` field is an obvious placeholder token, not a version number.
    - The suspend date is an obvious bracketed placeholder, and the body says the date is not fixed yet.
    - Both update commands appear verbatim and match `.claude/includes/release-process.md`.
    - The website appears in three places: a body reference near the top, a corporate footer, and a logo wordmark link.
    - The Desktop and Cowork connector paragraph names the new URL WITH the `/mcp` path and states the key stays `mindrian-brain`.
    - The suspend-minus-one-week reminder is drafted in the same file under its own heading.
    - `grep -c` for the em-dash character returns 0.
  </acceptance_criteria>

  <done>The one lever that reaches an un-updated install exists as a filed draft, honest about the suspend, honest about the thinner leadership answers, and holding no date it cannot keep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release record -> future reader | a CHANGELOG claim is trusted without re-verification; an overstated one becomes a false premise for a later decision |
| draft -> tester inbox | nothing in this plan sends; the boundary is crossed only by the operator, deliberately |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-28 | Repudiation | a CHANGELOG implying the refusal-copy fix reaches installs that have not updated | mitigate | the D-08 honest-limit sentence is carried VERBATIM from 339-06's SUMMARY and matched by `grep -F`, and a dedicated closing paragraph names both unreachable populations |
| T-339-29 | Repudiation | a version number written into a user-facing document before `release.sh` computes it | mitigate | the version appears only in the `## [Unreleased]` heading label, which Step 6 rewrites anyway; the tester note carries a placeholder token and plan 339-14 fills it from the tag |
| T-339-30 | Denial of Service | `release.sh` Step 6 hanging on an interactive editor prompt because no `## [Unreleased]` heading exists | mitigate | the acceptance criteria assert the heading matches `^## \[Unreleased\]`, and plan 339-11 re-asserts it before invoking `release.sh` |
| T-339-05 | Tampering | the two update commands retyped in the tester note and drifting from the source | mitigate | the verify block matches both commands with `grep -F` against the note, and `tests/test-339-update-path-single-source.cjs` polices the shipped-code copy |
| T-339-31 | Information Disclosure | a tester note naming a suspend date that does not exist yet, creating a commitment the operator cannot keep | mitigate | the date is an explicit bracketed placeholder, the body says it is not fixed, and `status: drafted` with `sent_to: []` keeps the note unsent |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | zero package installs in this plan and in the phase |
</threat_model>

<verification>
- `grep -qE '^## \[Unreleased\]' CHANGELOG.md` succeeds.
- The D-08 honest-limit sentence in `CHANGELOG.md` is byte-identical to the one in `lib/core/refusal-messaging.cjs`, verified with `grep -F`.
- The D-06a flip-day fact in `CHANGELOG.md` is byte-identical to the sentence in `339-CONTEXT.md` D-06a.
- `node scripts/check-plugin-path-anchoring.cjs --check-scripts` passes.
- `bash tests/run-all-339.sh` is unchanged in outcome by this plan (it writes no code).
- `grep -c` for the em-dash character returns 0 in both files.
</verification>

<success_criteria>
- The PREP cut has a CHANGELOG entry that names every honesty limit and claims nothing the release does not ship.
- The flip-day content fact travels verbatim rather than paraphrased.
- The tester note is a filed draft with no date it cannot keep, and it is not sent.
- No em-dashes in either file.
</success_criteria>

<output>
Create `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-10-SUMMARY.md` when done. Record the `## [Unreleased]` heading line verbatim, because plan 339-11 asserts it before invoking `release.sh`.
</output>
