---
phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-339-cross-repo-note.sh
  - tests/test-339-269-05-checklist.sh
  - tests/test-339-gate-zero-write.sh
  - tests/test-254-normalize-roundtrip-probe.cjs
  - tests/test-250-refusal-shapes.cjs
autonomous: true
requirements: [FLIP-02, FLIP-04, FLIP-07, FLIP-08, FLIP-09]
canon_parts: [7, 8]

must_haves:
  truths:
    - "The blocking coverage gate's own verify block is proven to be a pure read: running it leaves git status --porcelain byte-identical in BOTH repos"
    - "The cross-repo note path Theo already cites is proven to exist and to carry the egress-guard reason"
    - "The 269-05 checklist is proven to read the three real legs, and the retired items cannot silently come back as live checks"
    - "Both alias tables are pinned to identical key sets and to their own origin's value set, and both stay disjoint from the local router map"
    - "The refusal copy cannot drop the update path without a red test, and the 120-character line cap still holds for every refusal kind"
  artifacts:
    - path: "tests/test-339-gate-zero-write.sh"
      provides: "FLIP-09: the awk-scoped grep block against Theo's flip record plus a porcelain sha256 comparison in both repos"
      contains: "sha256sum"
      min_lines: 60
    - path: "tests/test-339-cross-repo-note.sh"
      provides: "FLIP-07: the note exists at the exact path Theo's README cites, non-empty, with the mechanism stated"
      contains: "339-NOTE-theo-desktop-connector-key.md"
      min_lines: 40
    - path: "tests/test-339-269-05-checklist.sh"
      provides: "FLIP-08: the three real legs present, the retired items not live"
      contains: "269-05-PLAN.md"
      min_lines: 40
  key_links:
    - from: "tests/test-339-gate-zero-write.sh"
      to: "/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md"
      via: "awk heading-scoped extraction, read only, never edited"
      pattern: "Coverage re-measurement"
    - from: "tests/test-254-normalize-roundtrip-probe.cjs"
      to: "lib/core/brain-client.cjs two alias tables and the selector"
      via: "structural source scan, extend Arms 4 and 5, never rewrite them"
      pattern: "BRAIN_PROBLEM_TYPE_ALIASES_THEO"
---

<objective>
Wave 0 tests-first, part 3 of 3 (GSD wave 1). Three new `.sh` arms plus two surgical edits to
existing tests.

The three `.sh` arms are file-and-shell assertions: the cross-repo note exists at the exact path
Theo's shipped README already cites, the 269-05 checklist reads the three real legs, and the
blocking gate's verify block is a pure read.

The two edits extend tests that already exist and already pin the surfaces this phase moves.
`tests/test-254-normalize-roundtrip-probe.cjs` Arms 4 and 5 currently pin ONE alias table onto
the incumbent's three names and prove disjointness from `lib/brain/chain-recommender.cjs`'s local
UDP/IDP/WDP map. They must pin BOTH tables. `tests/test-250-refusal-shapes.cjs` gains one pin
for the update path.

Purpose: `tests/test-339-gate-zero-write.sh` is the only wave-1 arm that is GREEN on day one, and
that is deliberate. Theo's ruling already exists at commit 81dfac8; this test's job is to keep
passing, and to fail loudly the day someone amends the ruling to HOLD or moves the heading.

Output: three test files, two test edits.

Wave 0 red-by-design applies to the two edits: test-254 Arms 4-5 go red until 339-04 lands the
two-table selector, and test-250's new pin goes red until 339-06 lands the copy. Extend the
existing arms, never rewrite them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-PATTERNS.md
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols this PLAN creates (new to the repo, excluded from drift verification):

| Symbol | Kind | File |
|--------|------|------|
| `tests/test-339-cross-repo-note.sh` | new file (test arm) | itself |
| `tests/test-339-269-05-checklist.sh` | new file (test arm) | itself |
| `tests/test-339-gate-zero-write.sh` | new file (test arm) | itself |

Symbols these tests assert about but do NOT create (created later by 339-04, 339-06 and 339-09):
`BRAIN_PROBLEM_TYPE_ALIASES_INCUMBENT`, `BRAIN_PROBLEM_TYPE_ALIASES_THEO`, `THEO_ORIGINS`,
`_brainProblemTypeAliases`, `UPDATE_PATH_SENTENCE`, `docs/339-NOTE-theo-desktop-connector-key.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the two file-assertion arms (FLIP-07, FLIP-08)</name>

  <files>tests/test-339-cross-repo-note.sh, tests/test-339-269-05-checklist.sh</files>

  <behavior>
    - `docs/339-NOTE-theo-desktop-connector-key.md` exists at exactly that path, is non-empty, and contains: `mindrian-brain`, the URL `https://theo-mcp.onrender.com/mcp`, `BRAIN_TOOL_MATCHER`, `part8-egress-guard-hook.cjs`, and the Theo commit hash `11d6f82`.
    - The note contains zero em-dashes.
    - `269-05-PLAN.md` Task 1 contains all three leg markers: `Leg (a)`, `Leg (b)`, `Leg (c)`.
    - `269-05-PLAN.md` Task 1 no longer carries the retired items as live checks: every line containing `Plans: TBD` also contains the word `Retired`.
    - `269-05-PLAN.md` Task 1 names `09-FLIP-RECORD.md` as leg (a)'s source, and does NOT name SEED-004 as leg (a)'s source.
    - `269-05-PLAN.md` Task 1's `/register` host reads `theo-mcp.onrender.com`; any occurrence of the incumbent host sits on a line that also carries `Retired`.
  </behavior>

  <read_first>
    - `tests/test-254-live-normalize-probe.sh` lines 14-46: the SKIP convention comment, the preamble, and the measure-versus-gate ruling comment. Copy all three shapes. Neither new arm needs a live Brain call, so neither SKIPs in practice; the convention is copied so the aggregator's `run_may_skip` arm behaves consistently
    - `339-RESEARCH.md` Design 7 in full: the note's required structure and the DRIFT-2 finding that Theo's README at commit `11d6f82` already cites this exact path, which is why the filename is fixed rather than chosen
    - `339-RESEARCH.md` Design 6 in full: what 269-05 Task 1 says today item by item, the three rewritten legs, and the DRIFT-3 correction that SEED-004's latest dated UPDATE is the stale 2026-09-02 one
    - `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-05-PLAN.md` Task 1 in full (lines 82 to 136): the six items, the verify block and the acceptance criteria that plan 339-09 rewrites
    - `/home/jsagi/Theo/README.md` around line 135 (READ ONLY, never edit): the callout that cites `docs/339-NOTE-theo-desktop-connector-key.md` by path
  </read_first>

  <action>
Create both `.sh` arms, executable, each a plain bash script exiting 0 on pass and non-zero on
fail, with a leading `SKIP` line reserved for the aggregator's `run_may_skip` handling.

`tests/test-339-cross-repo-note.sh`: assert `docs/339-NOTE-theo-desktop-connector-key.md` exists,
is at least 40 lines, and contains each of these five literals via `grep -Fq`: `mindrian-brain`,
`https://theo-mcp.onrender.com/mcp`, `BRAIN_TOOL_MATCHER`, `part8-egress-guard-hook.cjs`,
`11d6f82`. Then assert zero em-dashes using the `grep -P` rc greater-than-or-equal-2 scan-broke
arm the runner convention uses. Put the reason in a header comment: Theo's shipped README already
forward-references this exact path, so a missing file is a 404 inside a document users are told
to read; and the mechanism sentence is the whole point of the note, because a connector
registered under a different key produces tool names that match neither `BRAIN_TOOL_MATCHER` nor
the egress guard, which allows unconditionally when `isBrainTool` is false.

`tests/test-339-269-05-checklist.sh`: point at
`.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-05-PLAN.md`.
Extract Task 1's block with `awk`, from the line containing `Task 1: BLOCKING GATE` to the closing
task tag, then assert against that extracted block ONLY:
- `grep -Fq 'Leg (a)'`, `grep -Fq 'Leg (b)'` and `grep -Fq 'Leg (c)'` all succeed.
- `grep -Fq '09-FLIP-RECORD.md'` succeeds.
- `grep -Fq 'theo-mcp.onrender.com'` succeeds.
- Every line matching `pws-brain-mcp.onrender.com` also matches `Retired` (the retirement note may
  name the old host as history; a live check may not).
- Every line matching `Plans: TBD` also matches `Retired`.
Put the reason in a header comment, quoting the danger D-14 names: items 1 through 3 read PASS
from 2026-08-27 onward while the real content and infrastructure legs went unchecked, so a
checklist that can read green while its real leg is untested is worse than no checklist.

Both files are RED until 339-09 lands the note and the rewrite. Their failure output must name
the missing file or the failing assertion in one readable line, not a bash trace.

Header per house style on both: numbered one-sentence claims, phase id 339, date 2026-09-03, the
observed shape each arm keys on, what it deliberately does NOT key on, and which arms hard-gate
versus only report. No em-dashes anywhere.
  </action>

  <verify>
    <automated>bash tests/test-339-cross-repo-note.sh; echo "note exit=$? (non-zero CORRECT in wave 1)"; bash tests/test-339-269-05-checklist.sh; echo "checklist exit=$? (non-zero CORRECT in wave 1)"; test -x tests/test-339-cross-repo-note.sh && test -x tests/test-339-269-05-checklist.sh && echo EXEC_OK</automated>
  </verify>

  <acceptance_criteria>
    - Both files exist and are executable.
    - Both currently exit non-zero with a one-line readable failure, not a bash stack trace.
    - `tests/test-339-cross-repo-note.sh` asserts all five literals and the em-dash fence.
    - `tests/test-339-269-05-checklist.sh` scopes every assertion to the extracted Task 1 block, never to the whole file: verified by confirming the script contains an `awk` extraction before its first `grep`.
    - Neither script writes any file: `git status --porcelain | sha256sum` is unchanged across both runs.
    - `grep -c` for the em-dash character returns 0 for both files.
  </acceptance_criteria>

  <done>FLIP-07 and FLIP-08 have automated proofs that fail today and can only be made green by writing the note and rewriting the checklist correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/test-339-gate-zero-write.sh (FLIP-09, GREEN in wave 1 by design)</name>

  <files>tests/test-339-gate-zero-write.sh</files>

  <behavior>
    - The subsection headed exactly `### Coverage re-measurement, 2026-09-03, and the ruling on it` is extractable from Theo's flip record by `awk`, scoped from that heading to the next heading of two or three hashes.
    - All eleven required literals are present inside that extracted subsection, matched with `grep -F`: `Brain@56bf75a`, `83a1ce2`, `1,253`, `1,522`, `420`, `29,200`, `24,375`, `258`, `Covered: 228 of 258, 88.4%`, `Uncovered: 30 of 258`, `Coverage does NOT block Task 2, the flip`.
    - Running the whole extraction and grep block leaves `git status --porcelain | sha256sum` byte-identical in BOTH `/home/jsagi/dev/MindrianOS-Plugin` and `/home/jsagi/Theo`.
    - A literal that exists in the flip record but OUTSIDE the subsection is NOT found in the extraction, proving the scoping is real.
    - If the subsection is absent or any literal is missing, the script exits non-zero naming the missing literal. That is the "coverage held" signal, not a crash.
  </behavior>

  <read_first>
    - `339-RESEARCH.md` Design 5 subsection `The verify block, exact`: the full bash block verbatim, including the `awk` heading-scoped extraction, the eleven literals, the `printf` piping and the `sha256sum` before-and-after comparison. Use it as written
    - `339-RESEARCH.md` Pitfall 8: use `grep -F`, never `grep -E`, because `88.4%` contains a regex dot
    - `339-RESEARCH.md` Pitfall 7: no `git fetch`, no scratch file, no `sed` inside the zero-write block
    - `/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md` lines 295 to 470 (READ ONLY, never edit): the actual subsection, so every literal is verified present before the test is written
    - `339-PATTERNS.md` section for the new `.sh` arms and its `No Analog Found` row for the porcelain-hash arm: the zero-write assertion exists only as prose in `269-05-PLAN.md` Task 1 today, and this test is its first mechanization
  </read_first>

  <action>
Create `tests/test-339-gate-zero-write.sh`, executable. This is the mechanized twin of the
blocking gate task's verify block in plan 339-13, and it exists so the gate's zero-write property
is tested BEFORE the gate ever runs.

Implement exactly the block from `339-RESEARCH.md` Design 5:
- Set `FR=/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md`.
- Capture BEFORE as the sha256 of `git status --porcelain` in BOTH repos, each in a subshell
  `cd` that does not persist.
- Extract the subsection with an `awk` program anchored on the exact heading line, setting a flag
  after the heading and exiting on the next line matching two or three leading hashes followed by
  a space.
- Fail with `GATE: ruling subsection not found - coverage held` and exit 1 if the extraction is
  empty.
- Loop the eleven literals through `printf '%s' "$SEC" | grep -Fq "$LIT"`. Use `printf` piping,
  never a here-string, so the subsection's own backticks and dollar signs cannot be
  re-interpreted.
- Report, but do not fail on, a `HOLD` token inside the subsection: print
  `GATE: subsection carries HOLD - report and stop`.
- Capture AFTER in both repos and assert equality with BEFORE. On inequality print
  `GATE VIOLATION: this task wrote to the repository` and exit 1.

Add ONE thing the research block does not have: a scoping self-check. Assert that the string
`Flip instructions for the plugin release`, which is a heading living AFTER this subsection ends,
is NOT found in the extraction. Without that arm, an extraction bug returning the whole file
would still pass every positive assertion.

State in the header, in the Wave-0 vocabulary the runner uses: this arm is GREEN in wave 1, not
red. Theo's ruling already exists at commit 81dfac8. Its job is to keep passing; the day it goes
red, either Session T amended the ruling or the heading moved, and both are findings the flip
must act on rather than route around.

House style header: phase id 339, date 2026-09-03, the observed shape it keys on (a
heading-scoped markdown subsection in another repo, read only), and what it deliberately does NOT
key on (line numbers, which move whenever Session T edits anything above the subsection). No
em-dashes.
  </action>

  <verify>
    <automated>bash tests/test-339-gate-zero-write.sh && echo "GREEN (correct: the ruling already exists at Theo 81dfac8)"; test -x tests/test-339-gate-zero-write.sh && grep -Fq 'sha256sum' tests/test-339-gate-zero-write.sh && ! grep -q 'grep -E' tests/test-339-gate-zero-write.sh && echo STRUCTURE_OK</automated>
  </verify>

  <acceptance_criteria>
    - The file exists, is executable, and exits ZERO on this run. It is the one wave-1 arm that is green.
    - All eleven literals are present in the script and matched with `grep -F`. The script contains no `grep -E` anywhere.
    - The scoping self-check arm is present and asserts the out-of-subsection heading string is absent.
    - `git status --porcelain | sha256sum` in `/home/jsagi/dev/MindrianOS-Plugin` and in `/home/jsagi/Theo` are both byte-identical before and after the run, verified manually and recorded in the SUMMARY.
    - The script performs no `git fetch`, writes no scratch file, and runs no `sed`.
    - `grep -c` for the em-dash character returns 0.
  </acceptance_criteria>

  <done>The blocking gate's zero-write contract is mechanized rather than promised, and it is proven green against Theo's live ruling before the gate task is ever reached.</done>
</task>

<task type="auto">
  <name>Task 3: Extend tests/test-254-normalize-roundtrip-probe.cjs Arms 4 and 5 for the two-table selector (FLIP-02)</name>

  <files>tests/test-254-normalize-roundtrip-probe.cjs</files>

  <read_first>
    - `tests/test-254-normalize-roundtrip-probe.cjs` lines 1 to 45 (header and hermetic constraint), 258 to 303 (Arm 4) and 307 to 325 (Arm 5), IN FULL. This is an extend-do-not-rewrite task
    - `tests/test-254-normalize-roundtrip-probe.cjs` lines 261 to 276: the STATED DECISION comment that currently says the map is NOT re-pointed at Theo and explains why. That reasoning is superseded by this phase and must be REPLACED, not merely appended to
    - `339-RESEARCH.md` Design 1 subsection `How test-254 Arms 4 and 5 must change`: the seven required changes, numbered
    - `339-RESEARCH.md` Design 1 subsections `The ground truth` and `Recommended mechanism`: Theo's six ids verbatim from `recommend-chain.ts:47` and the two-table plus selector shape
    - `lib/core/brain-client.cjs:1705-1744`: the current single table, its "deliberately NOT the same map as chain-recommender" comment (which stays and is extended), and `_normalizeBrainProblemType`
    - `lib/brain/chain-recommender.cjs` around line 71: the local `PROBLEM_TYPE_ALIASES` targeting UDP/IDP/WDP, which Arm 5 proves disjoint from
    - `339-RESEARCH.md` Assumptions Log A3: `extractObjectLiteral` takes the declaration prefix as a parameter today, so calling it twice should work. Verify by reading it, and if it turns out to be hard-coded, change the helper rather than duplicating it
  </read_first>

  <action>
Extend Arms 4 and 5 in place. Do not create a new arm and do not rewrite the file.

Arm 4, seven changes:
1. Call `extractObjectLiteral` TWICE, once per table declaration prefix, for
   `BRAIN_PROBLEM_TYPE_ALIASES_INCUMBENT` and `BRAIN_PROBLEM_TYPE_ALIASES_THEO`.
2. Assert BOTH tables carry exactly the same 8 keys, and assert the two key SETS are identical.
   A key present in one and missing from the other is the drift this arm now exists to catch.
3. Assert the incumbent value set is exactly `Ill-Defined Problem`, `Undefined Problem`,
   `Well-Defined Problem` (unchanged from today), and the Theo value set is exactly `IllDefined`,
   `UnDefined`, `WellDefined`.
4. Assert every Theo value is a member of Theo's live id list. Source that list from a frozen
   array declared IN THE TEST with a comment citing
   `/home/jsagi/Theo/src/mcp/content/recommend-chain.ts:47`, never from a live read: the arm's own
   header states its hermetic constraint. The six ids are `UnDefined`, `IllDefined`,
   `WellDefined`, `Wicked`, `Trinity`, `Compass`.
5. Replace the structural probe's accessor: it currently asserts
   `lastIndexOf('return trimmed;')` comes AFTER `indexOf('BRAIN_PROBLEM_TYPE_ALIASES[lc]')`.
   Retarget it at the new accessor (`table[lc]`) and KEEP the ordering assertion. The pass-through
   `return trimmed;` must still come after the table lookup, which is what keeps `Wicked`,
   `Trinity` and `Compass` working on both origins.
6. Add a NEW assertion for the selector: assert the body of `_brainProblemTypeAliases` contains
   the identifier `BRAIN_URL` and contains BOTH table identifiers. That proves vocabulary and URL
   are wired to one switch without invoking an unexported function.
7. DELETE the stale STATED DECISION comment at `:261-276` and replace it with the new decision:
   Phase 339 re-points the map by ORIGIN rather than leaving it incumbent-only, because Theo
   matches `toLower(m.id) = toLower($problemType)` and nothing more, so the incumbent's
   `Undefined Problem` matches nothing on Theo while an unmapped `Undefined` would have matched.
   Cite Phase 339 and the date 2026-09-03.

Arm 5, one change: compute the UNION of both brain tables' values and assert the union is
disjoint from `lib/brain/chain-recommender.cjs`'s local map values. UDP, IDP and WDP collide with
neither incumbent names nor Theo ids today; the point is to keep that holding across a future
edit to either table.

Also add a NEW structural assertion in Arm 4 for `THEO_ORIGINS`: assert it is declared as a frozen
ARRAY (a set of origins), not a bare string literal, with a comment stating the reason recorded in
research: a Theo staging origin must be a one-line addition rather than a re-architecture.

Both arms go RED immediately and stay red until 339-04 lands. That is the correct wave-1 state and
`tests/run-all-339.sh`'s header already names this file as red by design.
  </action>

  <verify>
    <automated>node tests/test-254-normalize-roundtrip-probe.cjs; echo "exit=$? (non-zero CORRECT in wave 1: 339-04 has not run)"; grep -Fq 'BRAIN_PROBLEM_TYPE_ALIASES_THEO' tests/test-254-normalize-roundtrip-probe.cjs && grep -Fq 'THEO_ORIGINS' tests/test-254-normalize-roundtrip-probe.cjs && grep -Fq 'recommend-chain.ts' tests/test-254-normalize-roundtrip-probe.cjs && echo STRUCTURE_OK</automated>
  </verify>

  <acceptance_criteria>
    - Arms 4 and 5 were EXTENDED: the file's arm count and arm numbering are unchanged, and Arms 1 through 3 are byte-identical to before (`git diff` shows no hunk before line 255).
    - The stale STATED DECISION comment block is gone and its replacement cites Phase 339 and 2026-09-03.
    - The test currently FAILS, and its failure names the missing `BRAIN_PROBLEM_TYPE_ALIASES_THEO` declaration.
    - The Theo id list is a frozen array declared in the test with a citation comment; the test makes no cross-repo read at runtime.
    - Arm 5 asserts disjointness against the UNION of both tables, not against either one alone.
    - `grep -c` for the em-dash character returns 0.
  </acceptance_criteria>

  <done>FLIP-02 is pinned in the test that already owned this surface, extended rather than replaced, and the alias table cannot be re-pointed at one origin without the other being checked.</done>
</task>

<task type="auto">
  <name>Task 4: Add the update-path pin to tests/test-250-refusal-shapes.cjs (FLIP-04b)</name>

  <files>tests/test-250-refusal-shapes.cjs</files>

  <read_first>
    - `tests/test-250-refusal-shapes.cjs` IN FULL (189 lines, seven tests). The relevant pins are at `:61` (the frozen `REFUSAL_KINDS` list), `:72-73` (`no_key` status and `upgrade_hint`), `:80-81` (`unreachable` status and its absent `upgrade_hint`), `:96-97` (the unreachable reason constraints), `:127-130` (the tier0Response key set), and `:140-143` (Test 6, the 120-character single-line cap for every kind)
    - `lib/core/refusal-messaging.cjs:454-472` (`larryRefusalLine`): ALREADY VERIFIED at plan time. Its returns are its own hard-coded switch strings, independent of both `REASONS` and `RENDER_COPY`. Adding the update path to `RENDER_COPY` therefore CANNOT break Test 6's 120-character cap. Read it to confirm, then rely on it
    - `lib/core/refusal-messaging.cjs:326-380` (`RENDER_COPY`) and `:301-320` (`NEXT_MOVES`)
    - `339-RESEARCH.md` Design 3 subsections `What test-250-refusal-shapes.cjs pins today` and `Recommended split`
    - `339-PATTERNS.md` section for `lib/core/refusal-messaging.cjs`, pattern 3: the precedent comment for adding a `NEXT_MOVES` handle ahead of any consumer
  </read_first>

  <action>
Add ONE new test to `tests/test-250-refusal-shapes.cjs`, appended after the existing seven. Do not
modify any existing test. Do not relax any existing pin.

The new test asserts three things:
1. The rendered `unreachable` copy, joined, contains the literal
   `claude plugin update mos@mindrian-marketplace`.
2. The rendered `no_key` copy, joined, contains the same literal.
3. `NEXT_MOVES.unreachable` contains the handle `update` in addition to its existing `retry` and
   `continue_without`, and the array is still frozen.

Then add a guard assertion that documents WHY the update path went into `RENDER_COPY` and not into
`REASONS`: assert that for every kind in `REFUSAL_KINDS`, the `REASONS` entry does NOT contain the
literal `claude plugin update`. The 120-character cap Test 6 enforces cannot hold a 76-character
command pair with any framing, and putting it in `REASONS` would break Test 6 on the next edit
even though `larryRefusalLine` does not read `REASONS` today. Write that reason in the test as a
comment naming phase 339 and the date 2026-09-03.

Do NOT change Test 6. Its cap stays at 120 characters for every kind, and it must still pass after
339-06 lands the copy. If it does not, the copy went in the wrong table.

This test is RED until 339-06 lands. The other seven stay green throughout.
  </action>

  <verify>
    <automated>node tests/test-250-refusal-shapes.cjs; echo "exit=$? (non-zero CORRECT in wave 1: 339-06 has not run)"; grep -Fq 'claude plugin update mos@mindrian-marketplace' tests/test-250-refusal-shapes.cjs && grep -c 'length <= 120' tests/test-250-refusal-shapes.cjs</automated>
  </verify>

  <acceptance_criteria>
    - Exactly one new test was appended; `git diff` shows no hunk touching the seven existing tests.
    - Test 6's `length <= 120` assertion is byte-identical to before.
    - The new test currently FAILS on assertion 1 (the rendered unreachable copy has no update path yet), and the seven existing tests still PASS. Record that split in the SUMMARY.
    - The negative `REASONS` guard is present with its written reason naming phase 339 and 2026-09-03.
    - `grep -c` for the em-dash character returns 0.
  </acceptance_criteria>

  <done>FLIP-04b is pinned: the refusal copy cannot drop the update path, and the 120-character line cap that makes the split necessary is protected by an explicit guard rather than by luck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo -> `/home/jsagi/Theo` | Session M reads Theo, never writes it; the flip record is the authorization artifact and it belongs to Session T |
| planning artifact -> release decision | `269-05-PLAN.md`'s checklist is a gate whose green reading authorizes work; a checklist that reads green while untested is a repudiation risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-09 | Tampering | the gate task writing to a repo it must only read | mitigate | `tests/test-339-gate-zero-write.sh` captures `git status --porcelain | sha256sum` in BOTH repos before and after, and fails on any difference; the script contains no `git fetch`, no `sed`, and writes no scratch file |
| T-339-12 | Repudiation | the flip record's ruling subsection amended, moved or renamed after the gate was written | mitigate | the extraction is scoped by HEADING, never by line number, and the eleven literals are re-read at run time; a missing literal reports "coverage held" rather than passing |
| T-339-13 | Repudiation | the 269-05 checklist reading PASS while its real leg is unchecked | mitigate | `tests/test-339-269-05-checklist.sh` requires all three leg markers and requires every `Plans: TBD` line to carry `Retired` |
| T-339-02 | Information Disclosure | a Desktop connector registered under a key outside `BRAIN_TOOL_MATCHER` | mitigate | `tests/test-339-cross-repo-note.sh` requires the note to state the mechanism (`BRAIN_TOOL_MATCHER` plus `part8-egress-guard-hook.cjs`); no test can catch a hand-registered connector, so documentation on both sides is the only control and the residual risk is named |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | zero package installs in this plan and in the phase |
</threat_model>

<verification>
- `bash tests/test-339-gate-zero-write.sh` exits 0, and the porcelain hashes in both repos are unchanged.
- `bash tests/test-339-cross-repo-note.sh` and `bash tests/test-339-269-05-checklist.sh` both exit non-zero with a readable one-line failure.
- `node tests/test-254-normalize-roundtrip-probe.cjs` exits non-zero naming the missing Theo table.
- `node tests/test-250-refusal-shapes.cjs` exits non-zero on the new test only; the seven pre-existing tests still pass.
- `bash tests/run-all-339.sh` discovers all three new `.sh` arms through its glob.
</verification>

<success_criteria>
- FLIP-09 is proven green before the gate it protects ever runs.
- FLIP-02, FLIP-04b, FLIP-07 and FLIP-08 each have a failing test that names exactly what is missing.
- No existing test assertion was relaxed anywhere in this plan.
- No em-dashes in any file this plan wrote or edited.
</success_criteria>

<output>
Create `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-03-SUMMARY.md` when done. Record the porcelain sha256 values (before and after) for both repos from Task 2, verbatim, because that is the first evidence entry for FLIP-09.
</output>
