---
phase: 339-brain-to-theo-cutover-release-flip-brain-client-default-orig
plan: 12
type: execute
wave: 5
depends_on: ["339-11"]
files_modified:
  # EXACTLY the five files D-13 corrected locks to the FLIP cut. Adding a sixth
  # breaks the one-line-rollback rationale that the two-cut shape exists to
  # protect (D-01). Everything else that would otherwise have been dragged in
  # here shipped in the PREP cut: the doctor's dual-shape stats read, its test
  # file's mock parameterization and the census fixture all landed in 339-07.
  - lib/core/brain-client.cjs
  - lib/core/doctor/class-m-brain-smoke.cjs
  - tests/test-245-skill-frontmatter-inert-keys.cjs
  - CLAUDE.md
  - CHANGELOG.md
autonomous: true
requirements: [FLIP-01, FLIP-10, FLIP-11]
canon_parts: [7, 8]

must_haves:
  truths:
    - "lib/core/brain-client.cjs line 24 resolves to https://theo-mcp.onrender.com, a BARE origin with no path and no trailing slash"
    - "The rollback stays one line: reverting line 24, or setting MINDRIAN_BRAIN_URL on one install, moves the URL and the vocabulary together"
    - "Doctor's class M layer 6 reports an honest verdict against Theo instead of going permanently red on a real signal, and keeps a floor rather than dropping the assertion"
    - "The test-245 tripwire is honored, not defeated: its equality assertion moves in the same commit as line 24 and CLAUDE.md"
    - "The flip commit set is EXACTLY the five files D-13 locks, proven by git status rather than asserted in prose"
    - "The doctor smoke suite passes with ZERO edits to its test file, because the PREP cut parameterized its mocks off the constant this plan moves"
  artifacts:
    - path: "lib/core/brain-client.cjs"
      provides: "The flip: line 24 and the :4-7 docblock"
      contains: "theo-mcp.onrender.com"
    - path: "lib/core/doctor/class-m-brain-smoke.cjs"
      provides: "Per-origin canon constants and a per-origin node floor, consuming the dual-shape brain_stats read that shipped in the PREP cut"
      contains: "THEO_ORIGINS"
  key_links:
    - from: "lib/core/doctor/class-m-brain-smoke.cjs"
      to: "lib/core/brain-client.cjs THEO_ORIGINS"
      via: "require, so the origin set has one definition and a rollback moves floor and vocabulary together"
      pattern: "THEO_ORIGINS"
    - from: "tests/test-245-skill-frontmatter-inert-keys.cjs"
      to: "CLAUDE.md"
      via: "the tripwire's own failure message requires both to move in one commit"
      pattern: "theo-mcp.onrender.com"
---

<objective>
The FLIP. Everything else in this phase existed so this could be one line.

The required change, verbatim after the edit:
`const BRAIN_URL = process.env.MINDRIAN_BRAIN_URL || 'https://theo-mcp.onrender.com';`

BARE origin. No path. No trailing slash. The client appends `/mcp` for tool calls and `/register`
for the key ladder, so `/mcp` produces `/mcp/mcp` and a trailing slash a double slash. Both 404,
and the client renders a 404 as "Brain unreachable" rather than a configuration error. Theo's own
flip record calls this the typo class that fails silently in exactly the direction that costs the
most time to diagnose.

Riding in the SAME commit set, because each of these carries a VALUE that must move with the
origin or it is wrong on both sides of it. This list is D-13 corrected's locked FIVE, and it is a
ceiling, not a starting point:
- `lib/core/brain-client.cjs`: line 24 and the stale docblock at `:4-7`, which the flip record says
  belongs in the same commit.
- `lib/core/doctor/class-m-brain-smoke.cjs`: `CANON_BRAIN_URL`, and the node floor, whose Theo value
  is a value in exactly the same sense (D-12b).
- `tests/test-245-skill-frontmatter-inert-keys.cjs`'s hard equality assert.
  That test is a tripwire built for exactly this event, and its own failure message says: if the
  backend genuinely moved, update this test AND CLAUDE.md together, because that pairing is the
  entire point of the claim.
- `CLAUDE.md:51,131`.
- `CHANGELOG.md`, the entry for the flip cut.

Nothing else. Three files that an earlier draft of this plan carried are gone from it, and their
absence is the point rather than an omission: the doctor layer's dual-shape stats read, the doctor
TEST file's mock origins, and `tests/fixtures/246-census-fixture.json` all shipped in the PREP cut
(339-07), written to be correct against BOTH Brains. None of the three carried a value that had to
move with the origin, so none of them belonged in a human-held commit whose entire justification is
that reverting it is a one-line revert. If this plan finds itself editing a sixth file, that is a
finding to report, not a diff to widen.

The alias selector, the schema memo key, the capture arms, the refusal copy, the sweep, the docs,
the note and the doctor's shape-read mechanism all shipped in the PREP cut. Nothing in this plan
touches them. That is what keeps the rollback a one-line revert.

Output: the flip, committed and tested, ready for plan 339-13's gate and cut.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md
@.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-PATTERNS.md
@lib/core/brain-client.cjs
@lib/core/doctor/class-m-brain-smoke.cjs
</context>

<artifacts_this_phase_produces>
Symbols this PLAN creates (new to the repo, excluded from drift verification):

| Symbol | Kind | File |
|--------|------|------|
| `CANON_ORIGIN_NODE_FLOORS` or an equivalent per-origin floor selector | constant | `lib/core/doctor/class-m-brain-smoke.cjs` |
| `THEO_NODE_FLOOR` | constant, value 1000 | `lib/core/doctor/class-m-brain-smoke.cjs` |

Symbols this plan CONSUMES that plan 339-04 created: `THEO_ORIGINS`, exported from
`lib/core/brain-client.cjs`. It is required here rather than re-declared, so the origin set has one
definition and a rollback moves the node floor and the wire vocabulary together.

Symbols this plan CONSUMES that plan 339-07 created: the dual-shape node count read in
`lib/core/doctor/class-m-brain-smoke.cjs` and the exported `CANON_BRAIN_URL` its test file now
derives every mock origin from. Because of that export, moving the constant below moves the whole
suite with it and this plan edits NO test file.

Symbols whose VALUES change (not new, but changed in the same commit as line 24):
`CANON_BRAIN_URL`, the node floor for the resolved origin, the `test-245` CLAIM b expected literal,
and `CLAUDE.md` lines 51 and 131.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flip line 24 and the :4-7 docblock (FLIP-10)</name>

  <files>lib/core/brain-client.cjs</files>

  <behavior>
    - `grep -Fq "|| 'https://theo-mcp.onrender.com';" lib/core/brain-client.cjs` succeeds. Byte-exact, no path, no trailing slash.
    - The docblock at `:4-7` no longer names the incumbent host, Memgraph, or the 2026-07-22 migration as current.
    - `getBrainUrl()` now returns the Theo origin with no `MINDRIAN_BRAIN_URL` set, and still returns the override value when one is set.
    - The alias selector added in plan 339-04 now picks the Theo table automatically, with no edit here.
    - `tests/test-245-skill-frontmatter-inert-keys.cjs` goes RED until Task 3 lands. That is expected and Task 3 is in this same plan.
  </behavior>

  <read_first>
    - `lib/core/brain-client.cjs` lines 1 to 30: the docblock at `:4-7` naming the incumbent, Memgraph and the 2026-07-22 migration, and the const at `:24`
    - `lib/core/brain-client.cjs:337-346`: `_tryAutoRegister`, which appends `/register` to `BRAIN_URL`
    - `lib/core/brain-client.cjs` `callTool`, which appends `/mcp`
    - `/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md` section 2, `## 2. Flip instructions for the plugin release` (READ ONLY, never edit): the one required change, the current value quoted verbatim, the new value, the BARE-origin rule and its reasoning, the docblock instruction, and the explicit NOT-required list
    - `/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md` section 3, `## 3. Rollback` (READ ONLY): the per-install `MINDRIAN_BRAIN_URL` path, the fleet-wide patch-release path, and the fact that BOTH are valid only while `pws-brain-mcp` is still running
    - `339-RESEARCH.md` `Pitfall 1`: the `/mcp` suffix, with the exact `grep -F` assertion to use
    - `tests/test-245-skill-frontmatter-inert-keys.cjs:127-131`: the extraction regex `/const BRAIN_URL = [^\n]*'(https:\/\/[^']+)'/` constrains any refactor of line 24. KEEP THE LINE'S SHAPE
  </read_first>

  <action>
Change exactly one line and one docblock.

Line 24 becomes, byte for byte:
`const BRAIN_URL = process.env.MINDRIAN_BRAIN_URL || 'https://theo-mcp.onrender.com';`

Keep the LINE SHAPE. `tests/test-245-skill-frontmatter-inert-keys.cjs:127` extracts the URL with
`/const BRAIN_URL = [^\n]*'(https:\/\/[^']+)'/`. A refactor that splits the const, renames it, or
moves the literal into a helper breaks the tripwire's extraction and defeats a test that exists
precisely for this event.

Rewrite the docblock at `:4-7` so it names the CURRENT backend without a stale migration history.
State: the resolved origin is Theo at `https://theo-mcp.onrender.com`, a BARE origin because the
client appends `/mcp` for tool calls and `/register` for the key ladder; `MINDRIAN_BRAIN_URL`
overrides it and is the per-install rollback lever; and that lever is valid only while the previous
origin is still running, because pointing it at a suspended service is not a rollback but a second
outage on top of the first, arriving at the exact moment the operator is under pressure.

Comment-as-contract at the const, house style. Name: phase id 339, date 2026-09-03, the observed
constraint it keys on (a BARE base origin, because `${BRAIN_URL}/mcp` and `${BRAIN_URL}/register`
are both formed by appending; a `/mcp` suffix yields `/mcp/mcp` and a trailing slash a double
slash, both of which 404, and the client renders a 404 as "Brain unreachable" rather than a
configuration error), and what it deliberately does NOT change. The NOT list, from the flip record
section 2, verbatim in substance: input arg keys are byte-for-byte unchanged and
`tests/test-247-contract-client.cjs` stays green untouched; the shim server key `mindrian-brain` is
unchanged; `BRAIN_TOOL_MATCHER` and `hooks/hooks.json` are unchanged for the CLI surface; and the
`brain_query` `{rows, diagnostics}` shape adaptation is already shipped at `:927-945`, commit
`21fdd7bc`.

Do NOT touch anything else in this file. The alias selector, the schema memo key and the capture
log line all shipped in the PREP cut and engage automatically the moment this line changes. That
automatic engagement is the design, and re-editing them here would break the one-line-revert
property that the whole two-cut shape exists to protect.

Immediately after the edit, verify the rollback property by observation, not by reasoning:
run `MINDRIAN_BRAIN_URL=https://pws-brain-mcp.onrender.com node -e` requiring the module and
printing both `getBrainUrl()` and the normalization of the input `undefined`. The URL must be the
incumbent AND the normalized value must be `Undefined Problem`, proving the selector moved the
vocabulary back with the URL. Record both observations verbatim in the SUMMARY. This is the single
most important observation in the phase: it is the proof that the rollback is one lever, not two.
  </action>

  <verify>
    <automated>grep -Fq "const BRAIN_URL = process.env.MINDRIAN_BRAIN_URL || 'https://theo-mcp.onrender.com';" lib/core/brain-client.cjs && ! grep -n 'pws-brain-mcp' lib/core/brain-client.cjs && node -e "console.log('default:', require('./lib/core/brain-client.cjs').getBrainUrl())" && MINDRIAN_BRAIN_URL=https://pws-brain-mcp.onrender.com node -e "console.log('override:', require('./lib/core/brain-client.cjs').getBrainUrl())" && node tests/test-247-contract-client.cjs && node tests/test-254-normalize-roundtrip-probe.cjs</automated>
  </verify>

  <acceptance_criteria>
    - Line 24 matches the required string byte for byte, asserted with `grep -F` on the full literal including the closing `';`, so a `/mcp` suffix or a trailing slash cannot pass.
    - `lib/core/brain-client.cjs` contains no occurrence of `pws-brain-mcp` anywhere, including the docblock.
    - `getBrainUrl()` returns the Theo origin by default and the override value when `MINDRIAN_BRAIN_URL` is set. Both outputs quoted in the SUMMARY.
    - The rollback observation is recorded: with `MINDRIAN_BRAIN_URL` set to the incumbent, the normalization of `undefined` returns `Undefined Problem`, proving vocabulary and URL move together.
    - `node tests/test-247-contract-client.cjs` passes untouched.
    - `node tests/test-254-normalize-roundtrip-probe.cjs` passes: Arms 4 and 5 are origin-agnostic structural proofs and must not have been disturbed by the flip.
    - `git diff lib/core/brain-client.cjs` shows hunks ONLY at the docblock and at line 24 with its comment. No other line moved.
    - `grep -c` for the em-dash character returns 0.
  </acceptance_criteria>

  <done>The one required change is in the tree, the rollback is proven to be one lever rather than two, and the PREP cut's selector engaged automatically with no second edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move the VALUES that must move in the same commit as the origin (FLIP-11, FLIP-01, D-13)</name>

  <files>lib/core/doctor/class-m-brain-smoke.cjs, tests/test-245-skill-frontmatter-inert-keys.cjs, CLAUDE.md</files>

  <behavior>
    - `CANON_BRAIN_URL` resolves to the Theo origin, so layer 6 no longer exits at `endpoint is neither canon nor an explicit override` and reports `canon` true against the shipped default.
    - The node floor is PER ORIGIN: 29000 for the incumbent, 1000 for Theo, selected by the SAME `THEO_ORIGINS` set the alias table uses. The assertion is not dropped.
    - A per-install rollback with `MINDRIAN_BRAIN_URL` set to the incumbent restores the 29000 floor in the same motion, because the floor keys on the RESOLVED origin rather than being retargeted.
    - `STALE_REPLICA_NODE_COUNT` stays 28325 and still wins the reason line over the generic floor.
    - `node lib/core/doctor/class-m-brain-smoke.test.cjs` passes with ZERO edits to the test file. That is a load-bearing assertion, not a convenience: plan 339-07 derived every mock origin from the exported `CANON_BRAIN_URL` and moved the below-floor arm off 1000 precisely so this constant move would carry the suite with it. If the suite needs a test edit here, the PREP cut's parameterization was wrong and that is a finding to report, not a file to add to this commit.
    - `tests/test-245-skill-frontmatter-inert-keys.cjs`'s CLAIM b expects the Theo origin, and the assertion is still an equality against one literal.
    - `CLAUDE.md` lines 51 and 131 name the current default, and 131 records BOTH cutovers rather than erasing the first.
  </behavior>

  <read_first>
    - `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-07-SUMMARY.md`: the live `brain_stats` top-level shape that discharged Assumption A10, and the inertness proof that the PREP cut moved no constant. Read this FIRST: it is why the shape read below is already done and this task is a values-only edit
    - `lib/core/doctor/class-m-brain-smoke.cjs:62-79`: the L6 constants block with its per-constant failure-semantics comments. `CANON_BRAIN_URL` at `:76`, `CANON_NODE_FLOOR = 29000` at `:77`, `STALE_REPLICA_NODE_COUNT = 28325` at `:78`
    - `lib/core/doctor/class-m-brain-smoke.cjs` `_layer6`: the endpoint comparison and its early exit, the dual-shape node count read that plan 339-07 installed, the stale-replica check ordered BEFORE the generic floor, and the `GRAPHRAG_STAMP_CYPHER` silent degrade
    - `lib/core/doctor/class-m-brain-smoke.cjs` `module.exports`: `CANON_BRAIN_URL` is exported as of plan 339-07, which is why the test file needs no edit here
    - `lib/core/brain-client.cjs` `THEO_ORIGINS`: the frozen origin set plan 339-04 exported. Require it; do not re-declare the origin list
    - `scripts/doctor.cjs:1400-1417`: the acceptance arm hard-fails on exactly two conditions, the `store_identity` layer being ABSENT or its reason matching `/stale_replica_signature/`. Any other not-ok is carried as information only and does NOT flip ok
    - `339-RESEARCH.md` Open Question 2 and its recommendation: a per-origin floor selected by the same `THEO_ORIGINS` mechanism, Theo's floor set conservatively at 1000, with a comment that it is a floor and not a target, because Theo's canon is actively growing (1,253 nodes on 09-03, up from 712 on 09-02)
    - `tests/test-245-skill-frontmatter-inert-keys.cjs:127-131`: the HARD ASSERT `assert.ok(url === 'https://pws-brain-mcp.onrender.com', ...)` and its failure message, which reads: if the backend genuinely moved, update this test AND CLAUDE.md together, because that pairing is the entire point of this claim
    - `tests/test-245-skill-frontmatter-inert-keys.cjs:32`: the comment that also names the incumbent
    - `CLAUDE.md` line 51 (the Three Layers table's Brain row) and line 131 (the technology stack table's Memgraph and Brain MCP row, which also names the 2026-07-22 Neo4j-to-Memgraph cutover and cites `lib/core/brain-client.cjs:24` as the single source of the default URL)
    - `339-RESEARCH.md` `Pitfall 4`: the temptation is to relax the assertion into a regex or delete it. Its own failure message forbids this. Change the expected LITERAL and keep the assertion an EQUALITY
    - `339-CONTEXT.md` `D-13 corrected`: the locked five-file set this task must not widen
  </read_first>

  <action>
Three files, and every edit is a VALUE. No mechanism lands here. If an edit in this task requires
new machinery, the PREP cut missed something and the right move is to say so rather than to build
it inside the human-held flip commit.

`lib/core/doctor/class-m-brain-smoke.cjs`, edit 1: change `CANON_BRAIN_URL` at `:76` to the Theo
origin. Do NOT derive it from `getBrainUrl()`. This constant answers "is the resolved endpoint the
canon one", so deriving it from the resolver would make the comparison always true and destroy the
check outright. This is the one deliberate second origin literal in the repo, and
`tests/test-339-origin-single-source.cjs` allowlists this file for exactly that reason, so the
allowlist stays a two-entry list and its reason string is unchanged.

`lib/core/doctor/class-m-brain-smoke.cjs`, edit 2: make the node floor PER ORIGIN. Do NOT drop the
assertion and do NOT simply retarget 29000 down to 1000; a floor that is never checked is worse
than a wrong one, and a single retargeted floor would silently stop protecting the incumbent the
moment somebody rolls back with `MINDRIAN_BRAIN_URL`. Keep `CANON_NODE_FLOOR = 29000` as the
incumbent's floor, add `THEO_NODE_FLOOR = 1000` beside it, and select between them on the RESOLVED
origin using the `THEO_ORIGINS` set REQUIRED from `lib/core/brain-client.cjs`. Use those two
constant names exactly; the verify block greps for them, and a floor buried in an inline literal is
a floor nobody can find when it next needs changing. Requiring rather than
re-declaring is what makes the rollback one lever: reverting line 24 moves the wire vocabulary, the
origin and the floor in the same motion. The module is already required through the injection seams,
so this adds no network reach and the hermetic test contract is preserved. Write in the comment
that 1000 is a FLOOR and not a target: Theo's canon is actively growing, 1,253 nodes on 2026-09-03
up from 712 the previous day, and a moving denominator argues against a tight number.

`STALE_REPLICA_NODE_COUNT = 28325` stays untouched, and its check must still run BEFORE the generic
floor so the named reason keeps winning the reason line. `scripts/doctor.cjs:1402-1417` hard-fails
the acceptance arm on that named reason specifically, so the ordering is load-bearing rather than
cosmetic. Verify the ordering is unchanged rather than assuming it.

Comment-as-contract on the constants block, extending the existing per-constant failure-semantics
prose rather than replacing it. Name: phase id 339, date 2026-09-03, the observed payload shape
carried forward verbatim from `339-07-SUMMARY.md`, and what this deliberately does NOT key on.
State plainly why this file is allowed its own origin literal when nothing else in the repo is: the
constant answers a canon-ness question, so deriving it from the resolver would make the answer
always yes. Also state that a not-ok from this layer is information-only for the release train
(`scripts/doctor.cjs:1400-1401`) EXCEPT for the stale-replica reason, and that this is precisely
why leaving it broken would be an honesty failure rather than a build break: it would leave a real
signal permanently red and destroy the sense's ability to detect a genuinely wrong endpoint later.

`tests/test-245-skill-frontmatter-inert-keys.cjs`: change the expected literal at `:127` to
`https://theo-mcp.onrender.com`. Keep it an `assert.ok(url === '...')` EQUALITY. Do not relax it
into a regex, a `.includes`, or a set membership, and do not delete it. This test is a tripwire
built for exactly this event and honoring it is the point. Update the comment at `:32` to name the
current backend, and extend the failure message so the NEXT origin move gets the same discipline:
name the three files that must move together (this test, `CLAUDE.md`, and `lib/core/brain-client.cjs`
line 24) and state that changing any one alone is the failure this claim exists to catch.

`CLAUDE.md` line 51, the Three Layers table Brain row: change the host to `theo-mcp.onrender.com`
and replace the Memgraph description with Theo's actual backend. Do not invent capability claims.
State the backend plainly and keep the existing pointer to `docs/CORPUS-STATS.generated.md` for live
numbers if it is still accurate; if it describes the incumbent's corpus, say so rather than implying
the numbers moved.

`CLAUDE.md` line 131, the technology stack table: rewrite the row so it names the CURRENT default
and its single source (`lib/core/brain-client.cjs` line 24), and records the cutover history
honestly. The row currently records a 2026-07-22 Neo4j-to-Memgraph cutover; ADD the 2026-09-03
Memgraph-to-Theo cutover as the current state rather than overwriting the history. A stack table
that erases its own migration history loses the thing that makes it useful.

All three files change in the SAME commit as Task 1's line 24. That pairing is what the tripwire's
failure message demands, and splitting it across commits would leave a commit in history where the
test and the code disagree.

Finally, prove the ceiling rather than asserting it. After the edits and before committing, run
`git status --porcelain` and confirm every listed path is a member of the five-file set D-13 locks.
A sixth path means this plan reached past its mandate; stop and report it.
  </action>

  <verify>
    <automated>node lib/core/doctor/class-m-brain-smoke.test.cjs && node scripts/doctor.cjs --acceptance && node tests/test-245-skill-frontmatter-inert-keys.cjs && node tests/test-339-origin-single-source.cjs && grep -Fq 'THEO_ORIGINS' lib/core/doctor/class-m-brain-smoke.cjs && grep -Fq 'STALE_REPLICA_NODE_COUNT = 28325' lib/core/doctor/class-m-brain-smoke.cjs && grep -Fq 'CANON_NODE_FLOOR = 29000' lib/core/doctor/class-m-brain-smoke.cjs && grep -Fq 'THEO_NODE_FLOOR = 1000' lib/core/doctor/class-m-brain-smoke.cjs && grep -Fq "CANON_BRAIN_URL = 'https://theo-mcp.onrender.com'" lib/core/doctor/class-m-brain-smoke.cjs && grep -Fq "url === 'https://theo-mcp.onrender.com'" tests/test-245-skill-frontmatter-inert-keys.cjs && grep -Fq 'theo-mcp.onrender.com' CLAUDE.md && git diff --name-only lib/core/doctor/class-m-brain-smoke.test.cjs tests/fixtures/246-census-fixture.json | wc -l | grep -q '^0$' && echo VALUES_MOVED_OK</automated>
  </verify>

  <acceptance_criteria>
    - `node lib/core/doctor/class-m-brain-smoke.test.cjs` passes with the test file UNCHANGED. `git diff --name-only lib/core/doctor/class-m-brain-smoke.test.cjs` is empty, asserted in the verify block. This is the proof that the PREP cut's parameterization worked.
    - `git diff --name-only tests/fixtures/246-census-fixture.json` is empty: the fixture is not this commit's business.
    - `node scripts/doctor.cjs --acceptance` passes, and layer 6's `store_identity` result is `ok` rather than carrying a not-ok reason.
    - The node floor is per-origin, selected through `THEO_ORIGINS` REQUIRED from `lib/core/brain-client.cjs`, not re-declared. Both floors are present as named constants: `CANON_NODE_FLOOR = 29000` for the incumbent and `THEO_NODE_FLOOR = 1000` for Theo, each matched by `grep -F` in the verify block so neither can hide in an inline literal.
    - The Theo origin appears exactly ONCE as a string literal in `lib/core/doctor/class-m-brain-smoke.cjs`, at `CANON_BRAIN_URL`, counted on code lines only by stripping comment lines before counting. The origin SET is required from `brain-client.cjs`, never re-typed here.
    - The rollback property is observed, not argued: with `MINDRIAN_BRAIN_URL` set to the incumbent, layer 6 reports the 29000 floor in its reason line. Quote that line verbatim in the SUMMARY.
    - `STALE_REPLICA_NODE_COUNT` is unchanged at 28325 and is still checked BEFORE the generic floor, proven by the existing test arm that feeds exactly that count and asserts the `stale_replica_signature` reason.
    - `node tests/test-245-skill-frontmatter-inert-keys.cjs` passes, and its assertion at `:127` is still an equality against a single literal, not a regex or a set. Its failure message names all three paired files.
    - `CLAUDE.md` lines 51 and 131 both name `theo-mcp.onrender.com`, and line 131 records BOTH cutovers rather than erasing the 2026-07-22 one.
    - `node tests/test-339-origin-single-source.cjs` still passes with its two-entry allowlist unchanged.
    - `git status --porcelain` lists ONLY paths from the D-13 five-file set. Record the output verbatim in the SUMMARY.
    - `grep -c` for the em-dash character returns 0 in all three files.
  </acceptance_criteria>

  <done>Every value that had to move with the origin moved in the same commit as it, the doctor keeps a real floor on both sides of a rollback, and the flip commit did not grow a sixth file.</done>
</task>

<task type="auto">
  <name>Task 3: Write the FLIP cut CHANGELOG entry</name>

  <files>CHANGELOG.md</files>

  <read_first>
    - `CHANGELOG.md` lines 1 to 20: the fresh `## [Unreleased]` heading that `release.sh` Commit B created during the PREP cut, and the released PREP entry below it
    - `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-11-SUMMARY.md`: the PREP cut's shipped tag and the post-Commit-B `plugin.json` version
    - This plan's Tasks 1 and 2 outputs, for the facts to record
    - `339-CONTEXT.md` D-06a: the flip-day content fact, verbatim
    - `339-CONTEXT.md` `<specifics>`: the two Theo failure modes to name rather than hide, `brain_write` always refusing with `WRITE_PATH_DISABLED` and `PLAN_REJECTED` on count-store query plans
    - `339-RESEARCH.md` Design 8, the probe leg table: which legs invert post-flip and why each is EXPECTED rather than a failure
    - `339-RESEARCH.md` `Pitfall 2`: no hard-coded version numbers
    - `339-RESEARCH.md` `Pitfall 9`: the enrichment queue filling during soak is canon thinness correctly measured, not a leak
  </read_first>

  <action>
Add the FLIP cut's entries under the current `## [Unreleased]` heading. Correct the heading label to
the version this cut will carry: the PREP cut's Commit B pre-bumped `plugin.json` to
`2.0.0-beta.18`, so `release.sh --prerelease` will produce `v2.0.0-beta.19`. Write
`## [Unreleased] -- v2.0.0-beta.19 (in progress)` and put that version nowhere else.

Entries to write:

- THE FLIP itself. `lib/core/brain-client.cjs` line 24's default moves from the incumbent to
  `https://theo-mcp.onrender.com`, a BARE origin. Say what that means for a user in one sentence:
  Larry's methodology now comes from Theo, and nothing about how you use MindrianOS changes. Name
  the rollback in the same paragraph: revert line 24 in a patch release, or set
  `MINDRIAN_BRAIN_URL` on one install, and BOTH are valid only while the previous service is still
  running. State that the previous service is SUSPENDED and never deleted, after a soak window, and
  that compute is suspended before data.
- The doctor layer 6 fix, completed across both cuts. Name all three breaks and say which cut
  carried each: the dual-shape `brain_stats` read shipped in the PREP cut because it is safe against
  both Brains, and the canon endpoint plus the per-origin node floor moved here because they are
  values that had to move with the origin. Name the floors, 29000 for the incumbent and 1000 for
  Theo, and say the floor keys on the resolved origin so a `MINDRIAN_BRAIN_URL` rollback restores
  the incumbent's floor in the same motion.
- The `test-245` tripwire and `CLAUDE.md` pairing (Task 2).
- The flip-day content fact, VERBATIM from D-06a: `/mos:leadership` and due-diligence consults
  answer thinner through Theo until the 30 uncovered names are ingested; the response is an
  honest-empty coverage block, not an error. State that the 30 names bind the DECOMMISSION, not the
  flip, per the coverage ruling, and record `Covered: 228 of 258, 88.4%` and
  `Uncovered: 30 of 258`.
- Two Theo behaviors users and maintainers will meet, named rather than hidden: `brain_write`
  always refuses with `WRITE_PATH_DISABLED`, because canon writes go through Theo's own governed
  payload path and this is not a tier a better key opens; and a count-store query plan may draw
  `PLAN_REJECTED`, which is answered by adding a property filter rather than by concluding the tool
  is broken. Neither is this repo's code to change, and both are named follow-ups.
- The expected probe leg inversions, by name, so a later reader does not treat
  `scripts/probe-brain-contract.cjs` as broken: legs a and d port cleanly; leg b inverts because
  Theo SERVES `text2cypher` so the expected 403 does not arrive, and `brain_ask_anything` still
  draws an unknown-tool error but for a different reason (Theo has no allowlist gate at all); leg c
  partially inverts because `BoundedReadRefusal` is incumbent-authored text while Theo's refusals
  are typed codes; leg e inverts entirely because it asserts Memgraph index names that Theo's Aura
  instance does not have. These are DOCUMENTED, not failures.
- The enrichment-queue expectation during soak, so nobody reads it as a leak: nearly every
  `orchestration_readiness` call will now capture, because Theo's canon is thin. Bounded and
  deduped by framework name. Canon thinness correctly measured.

Do NOT restate the PREP cut's entries; they are already in the released section above.

No em-dashes.
  </action>

  <verify>
    <automated>grep -qE '^## \[Unreleased\]' CHANGELOG.md && grep -Fq 'v2.0.0-beta.19' CHANGELOG.md && grep -Fq 'theo-mcp.onrender.com' CHANGELOG.md && grep -Fq 'honest-empty coverage block, not an error' CHANGELOG.md && grep -Fq 'WRITE_PATH_DISABLED' CHANGELOG.md && grep -Fq 'PLAN_REJECTED' CHANGELOG.md && ! grep -q $'\u2014' CHANGELOG.md && echo FLIP_CHANGELOG_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qE '^## \[Unreleased\]' CHANGELOG.md` succeeds (the Pitfall 10 precondition for plan 339-13's cut).
    - The heading label reads `v2.0.0-beta.19` and that string appears nowhere else in the file.
    - The flip entry names the BARE origin, the one-line rollback, the `MINDRIAN_BRAIN_URL` lever, and the fact that both rollback paths are valid only while the incumbent runs.
    - The D-06a fact appears VERBATIM, matched by `grep -F` against `339-CONTEXT.md`.
    - All four probe legs' expected post-flip status are named.
    - `WRITE_PATH_DISABLED` and `PLAN_REJECTED` are both named with their correct meaning.
    - `grep -c` for the em-dash character returns 0.
  </acceptance_criteria>

  <done>The flip has a written record that names what changed, what a rollback costs, what will feel thinner, and which red signals are expected rather than broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin default origin -> every installed surface | one line moves every CLI, Desktop-via-shim and Cowork call at once |
| doctor's canon constant -> the endpoint check | a constant derived from the thing it checks answers yes unconditionally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-06 | Denial of Service | line 24 landing with a `/mcp` suffix or a trailing slash, producing `/mcp/mcp` and a 404 the client renders as "Brain unreachable" | mitigate | byte-exact `grep -F` on the full literal including the closing quote and semicolon, plus a live `getBrainUrl()` observation recorded in the SUMMARY |
| T-339-01 | Tampering | a second origin literal reintroduced by the doctor fix | mitigate | `THEO_ORIGINS` is REQUIRED from `brain-client.cjs`, not re-declared; `tests/test-339-origin-single-source.cjs` allowlists exactly two files, each with a written 40-plus-character reason, and it is re-run in this plan's verify |
| T-339-34 | Repudiation | defeating `test-245` by relaxing its equality into a regex or deleting it | mitigate | the assertion stays an equality against one literal, the failure message is extended to name all three paired files, and the acceptance criteria assert the equality form |
| T-339-35 | Repudiation | a doctor layer left permanently red on a real signal, destroying its ability to detect a genuinely wrong endpoint later | mitigate | all three breaks fixed across the two cuts, the floor kept PER ORIGIN rather than dropped or retargeted so a `MINDRIAN_BRAIN_URL` rollback restores the incumbent's 29000 in the same motion, and the stale-replica named reason kept winning over the generic floor with its existing test arm |
| T-339-39 | Tampering | the human-held flip commit growing past the five files D-13 locks, so the advertised one-line rollback quietly becomes a wide revert nobody has rehearsed | mitigate | `files_modified` is the locked five and carries the reason inline; Task 2 runs `git status --porcelain` before committing and stops on a sixth path; the verify block asserts the doctor TEST file and the census fixture are both untouched by this plan |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | zero package installs in this plan and in the phase |
</threat_model>

<verification>
- `grep -F` on the full line-24 literal succeeds; `grep -c 'pws-brain-mcp' lib/core/brain-client.cjs` returns 0.
- `node tests/test-245-skill-frontmatter-inert-keys.cjs`, `node tests/test-247-contract-client.cjs`, `node tests/test-254-normalize-roundtrip-probe.cjs` and `node lib/core/doctor/class-m-brain-smoke.test.cjs` all pass, the last of them with its test file UNCHANGED by this plan.
- `git status --porcelain` at the end of every task lists only paths from the D-13 five-file set: `lib/core/brain-client.cjs`, `lib/core/doctor/class-m-brain-smoke.cjs`, `tests/test-245-skill-frontmatter-inert-keys.cjs`, `CLAUDE.md`, `CHANGELOG.md`.
- `node tests/test-339-origin-single-source.cjs` passes with its two-entry allowlist unchanged.
- `bash tests/run-all-339.sh` is fully green.
- `node scripts/doctor.cjs --acceptance` passes and layer 6's `store_identity` is `ok`.
- `git diff --stat data/brain-census.generated.json docs/BRAIN-GRAPH-CENSUS.generated.md` is empty.
- The rollback observation is recorded: with `MINDRIAN_BRAIN_URL` set to the incumbent, `getBrainUrl()` returns the incumbent AND the normalization of `undefined` returns `Undefined Problem`.
</verification>

<success_criteria>
- FLIP-10, FLIP-11 and the remaining half of FLIP-01 are green.
- The flip is exactly one line plus a docblock plus the values that had to move with it, across EXACTLY the five files D-13 locks, so the rollback stays a one-line revert.
- The doctor smoke suite passed without this plan editing its test file, proving the PREP cut's parameterization did its job.
- The rollback is PROVEN to move URL and vocabulary together, not argued to.
- Assumption A10 is discharged with a live observation rather than carried into the release.
- No em-dashes in any file this plan touched.
</success_criteria>

<output>
Create `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-12-SUMMARY.md` when done. Record verbatim: line 24 after the edit, the two `getBrainUrl()` observations (default and override), the rollback vocabulary observation, the layer 6 reason line observed with `MINDRIAN_BRAIN_URL` set to the incumbent (proving the 29000 floor came back with it), and the `git status --porcelain` output proving the commit set is the D-13 five.
</output>
