---
phase: 127.1
slug: brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-19
---

# Phase 127.1 -- Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `127.1-CONTEXT.md` (4-lock non-demotion protocol) + `127.1-RESEARCH.md` (Validation Architecture section).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js native test runner (node --test) for harness scripts; existing MCP integration suite for tool-surface stability |
| **Config file** | none for harness scripts (CJS); `tests/run-all-127.sh` aggregator pattern (per Phase 127-03) |
| **Quick run command** | `node --test tests/127.1-*.test.cjs` (per-task harness assertions) |
| **Full suite command** | `bash tests/run-all-127.sh` (extends Phase 127 aggregator to include 127.1 harnesses) |
| **Estimated runtime** | ~30-60 seconds (embedding checksum manifest is the slowest step) |

---

## Sampling Rate

- **After every task commit:** Run `node --test tests/127.1-{surface}.test.cjs` for the surface that task touches
- **After every plan wave:** Run `bash tests/run-all-127.sh` (aggregator picks up new 127.1 harnesses)
- **Before /gsd:verify-work:** Full suite must be green AND non-regression harness top-5 overlap >= 80% across all 20 queries
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

> Plans will fill task IDs. Each task references one of the four Validation Architecture surfaces.

| Surface | Plan Wave Anchor | Test Type | Automated Command | Status |
|---------|------------------|-----------|-------------------|--------|
| 1: Embedding integrity (1,427 vectors byte-identical) | pre-cutover wave | manifest + checksum | `node tests/127.1-embedding-integrity.test.cjs` | ⬜ pending |
| 2: Index configuration (dim=1024, cosine) | post-load wave | Cypher DDL assert | `node tests/127.1-index-config.test.cjs` | ⬜ pending |
| 3: Non-regression queries (top-5 overlap >= 80%) | cutover gate wave (BLOCKING) | harness vs both backends | `node tests/127.1-graphrag-overlap.test.cjs` | ⬜ pending |
| 4: Tool surface stability (brain_search_semantic unchanged) | every wave | existing MCP integration suite | `bash tests/brain-mcp-integration.sh` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠ flaky*

---

## Wave 0 Requirements

- [ ] `tests/127.1-embedding-integrity.test.cjs` -- harness for Surface 1 (checksum manifest pre/post migration)
- [ ] `tests/127.1-index-config.test.cjs` -- harness for Surface 2 (Neo4j DDL introspection)
- [ ] `tests/127.1-graphrag-overlap.test.cjs` -- harness for Surface 3 (BLOCKING cutover gate, 20-query corpus)
- [ ] `tests/127.1-graphrag-overlap-corpus.json` -- 20 representative fuzzy queries (sourced from tester telemetry or hand-built per CONTEXT.md Lock 4)
- [ ] `tests/run-all-127.sh` -- extend existing aggregator to include 127.1-* harnesses (do NOT create a parallel aggregator)

---

## Manual-Only Verifications

| Behavior | Surface | Why Manual | Test Instructions |
|----------|---------|------------|-------------------|
| Tester reports of "different fuzzy-match behavior" in first beta cycle after cutover | 3 (non-regression) | Empirical tester signal only surfaces post-deploy | Monitor tester channels for 7 days post v1.13.0-beta.21 release; cross-reference any report against the known harness divergence in the phase summary |
| Pinecone API key actually removed from mindrian-brain Render env vars | -- | Cloud console verification | Log into Render, confirm `PINECONE_API_KEY` env var absent on mindrian-brain service post-deploy |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify referencing one of the four surfaces, OR Wave 0 dependency declared
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING harness files listed above
- [ ] No watch-mode flags (CI-safe one-shot only)
- [ ] Feedback latency < 60s for quick run; < 5 min for full suite including 20-query harness
- [ ] `nyquist_compliant: true` set in frontmatter after planner passes verification

**Approval:** pending
