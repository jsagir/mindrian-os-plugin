---
phase: 127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/127.1-embedding-integrity.test.cjs
  - tests/127.1-index-config.test.cjs
  - tests/127.1-graphrag-overlap.test.cjs
  - tests/127.1-graphrag-overlap-corpus.json
  - tests/run-all-127.sh
  - lib/memory/run-feynman-tests.cjs
autonomous: true
gap_closure: false
requirements:
  - GRAPHRAG-COLLAPSE-127.1-01  # Surface 1 harness exists (byte-identical embedding integrity)
  - GRAPHRAG-COLLAPSE-127.1-02  # Surface 2 harness exists (Neo4j vector index DDL assertions)
  - GRAPHRAG-COLLAPSE-127.1-03  # Surface 3 harness exists (20-query top-5 overlap, BLOCKING)
  - GRAPHRAG-COLLAPSE-127.1-04  # 20-query corpus seeded
  - GRAPHRAG-COLLAPSE-127.1-05  # run-all-127.sh extended with 127.1 suites
user_setup: []

must_haves:
  truths:
    - Four Surface harness files exist on disk and are independently runnable.
    - tests/run-all-127.sh discovers and executes all four 127.1 suites without crash.
    - The 20-query corpus is committed JSON, not hand-typed at runtime.
    - Each harness fails loudly (non-zero exit) until its consumer plan delivers the substrate it asserts against, never silently green.
  artifacts:
    - path: tests/127.1-embedding-integrity.test.cjs
      provides: Surface 1 (byte-identical SHA256 manifest assertion across 1,427 vectors)
      min_lines: 80
    - path: tests/127.1-index-config.test.cjs
      provides: Surface 2 (Cypher SHOW INDEXES + assert dim=1024, similarity_function=cosine)
      min_lines: 60
    - path: tests/127.1-graphrag-overlap.test.cjs
      provides: Surface 3 (BLOCKING -- aggregate top-5 overlap >= 0.80 across 20 queries)
      min_lines: 120
    - path: tests/127.1-graphrag-overlap-corpus.json
      provides: 20 representative fuzzy queries (Lock 4 input contract)
      min_lines: 60
    - path: tests/run-all-127.sh
      provides: aggregator extended with 4 new 127.1 suites; bash exits non-zero on any miss/fail
      contains: "127.1-embedding-integrity"
  key_links:
    - from: tests/run-all-127.sh
      to: tests/127.1-*.test.cjs
      via: CJS_SUITES array additions
      pattern: "127\\.1-(embedding-integrity|index-config|graphrag-overlap)\\.test\\.cjs"
    - from: lib/memory/run-feynman-tests.cjs
      to: tests/127.1-*.test.cjs
      via: Phase 127.1 block registration
      pattern: "127\\.1-"
---

<objective>
Scaffold the Wave 0 validation substrate for Phase 127.1 BEFORE any migration code lands. Three test files cover the four Validation Architecture surfaces (Surface 4 is the existing MCP integration suite; no new file). One JSON corpus seeds Surface 3 with 20 representative fuzzy queries. The aggregator gets extended so every downstream task in this phase has a single command that exercises everything.

Purpose: Surface 3 (top-5 overlap >= 0.80) is the BLOCKING cutover gate. The cutover plan (Wave 2) cannot ship until this harness exists AND passes against the real Neo4j HNSW substrate Wave 1 produces. Scaffolding all four surfaces in Wave 0 enforces the Nyquist "test scaffold before substrate" rule and gives Wave 1/2/3 deterministic acceptance criteria they can grep against.

Output:
- tests/127.1-embedding-integrity.test.cjs (Surface 1 harness; RED until Wave 1 Plan A migrates)
- tests/127.1-index-config.test.cjs (Surface 2 harness; RED until Wave 1 Plan B creates the index)
- tests/127.1-graphrag-overlap.test.cjs (Surface 3 harness; RED until Wave 2 flips the router)
- tests/127.1-graphrag-overlap-corpus.json (20 queries; committed input data, not generated)
- tests/run-all-127.sh extended (now exercises 7 shell suites + 8 CJS suites including the 3 new 127.1 tests)
- lib/memory/run-feynman-tests.cjs registration block for Phase 127.1
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-CONTEXT.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-RESEARCH.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-VALIDATION.md
@tests/run-all-127.sh
@tests/run-all-126.sh
@lib/memory/run-feynman-tests.cjs

<interfaces>
The harnesses MUST contract against env vars set by the executor (no hardcoded creds; the unit tests use stubs but the integration mode reads live env):

```
process.env.NEO4J_URI            # neo4j+s://<host>.databases.neo4j.io  (Render env)
process.env.NEO4J_USER           # 'neo4j' default
process.env.NEO4J_PASSWORD       # Render-managed
process.env.PINECONE_API_KEY     # Render-managed (read-only baseline source for Surface 1 + 3 pre-cutover)
process.env.PINECONE_INDEX       # 'pws-brain'
process.env.MINDRIAN_127_1_FIXTURE_MODE  # '1' uses recorded fixtures; unset hits live; tests MUST run in fixture mode by default
```

When MINDRIAN_127_1_FIXTURE_MODE='1' (the default for run-all-127.sh CI execution), each harness consumes a fixture file rather than the live backends:
- tests/fixtures/127.1/embedding-manifest.fixture.json   (Surface 1 -- 1,427 SHA256 pairs; Plan 127.1-01 produces it)
- tests/fixtures/127.1/index-config.fixture.json         (Surface 2 -- SHOW INDEXES record; Plan 127.1-02 produces it)
- tests/fixtures/127.1/overlap-baseline.fixture.json     (Surface 3 -- Pinecone pre-cutover top-5; Plan 127.1-03 produces it)
- tests/fixtures/127.1/overlap-neo4j.fixture.json        (Surface 3 -- Neo4j post-cutover top-5; Plan 127.1-03 produces it)

Until the fixtures exist the harnesses MUST exit non-zero with a clear "fixture missing" message naming the producing plan. This is the Wave 0 NYQUIST contract: harness scaffold today, RED-by-design until the substrate lands, GREEN when consumed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 20-query corpus JSON + Surface 1 + Surface 2 harnesses</name>
  <files>tests/127.1-graphrag-overlap-corpus.json, tests/127.1-embedding-integrity.test.cjs, tests/127.1-index-config.test.cjs</files>
  <read_first>
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-CONTEXT.md (four-lock protocol)
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-VALIDATION.md (Surface 1 + 2 contracts)
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-RESEARCH.md ("Validation Architecture" section + A127.1.4 recall@10 recommendation)
    - tests/run-all-127.sh (existing aggregator pattern; do not break)
    - mcp-server-brain/lib/pinecone-tools.cjs (the current Pinecone index name + namespace contract)
    - mcp-server-brain/lib/neo4j-tools.cjs (the existing read-only Cypher tool surface)
    - references/brain/query-patterns.md (Pattern #8 fuzzy-match semantics that the corpus must cover)
  </read_first>
  <action>
    Three files, all bash-creatable, no em-dashes anywhere (use hyphens or commas; pre-commit harness enforces).

    FILE 1: tests/127.1-graphrag-overlap-corpus.json -- 20 representative fuzzy queries.
    Schema (top-level JSON object):
    ```
    {
      "schema_version": 1,
      "phase": "127.1",
      "purpose": "20 representative fuzzy-match queries gating the Pinecone -> Neo4j HNSW cutover (Lock 4)",
      "source": "Hand-built from references/brain/query-patterns.md Pattern #8 examples + the 14-Cypher-pattern catalogue plus 5 cross-domain analogy probes; no tester PII per Canon Part 8.",
      "embedding_model_baseline": "multilingual-e5-large",
      "embedding_dims_baseline": 1024,
      "similarity_metric_baseline": "cosine",
      "queries": [
        { "id": "Q01", "text": "frameworks for navigating wicked problems", "category": "fuzzy-framework-lookup" },
        { "id": "Q02", "text": "tools that help with stakeholder analysis", "category": "fuzzy-framework-lookup" },
        { "id": "Q03", "text": "how to validate market assumptions cheaply", "category": "fuzzy-methodology-lookup" },
        { "id": "Q04", "text": "ways to find an underserved customer job", "category": "fuzzy-methodology-lookup" },
        { "id": "Q05", "text": "techniques for surfacing hidden contradictions in a venture", "category": "cross-domain-analogy" },
        { "id": "Q06", "text": "innovation lessons from biology to software", "category": "cross-domain-analogy" },
        { "id": "Q07", "text": "how should an investor read a thesis with thin evidence", "category": "persona-aware" },
        { "id": "Q08", "text": "what does a first-time founder usually miss in problem definition", "category": "persona-aware" },
        { "id": "Q09", "text": "frameworks that chain from SWOT into something more rigorous", "category": "framework-chaining" },
        { "id": "Q10", "text": "what comes after Beautiful Question in a discovery sequence", "category": "framework-chaining" },
        { "id": "Q11", "text": "tools for de-risking translational research timelines", "category": "domain-translational" },
        { "id": "Q12", "text": "how to score the innovation gap of an unmet need", "category": "scoring-and-ranking" },
        { "id": "Q13", "text": "what does Hughes mean by reverse salient in an expanding system", "category": "concept-lookup" },
        { "id": "Q14", "text": "how do you tell whether a problem is wicked or merely complex", "category": "concept-lookup" },
        { "id": "Q15", "text": "approaches for compressing time between insight and validated decision", "category": "core-jtbd" },
        { "id": "Q16", "text": "what should the team around the founder argue about first", "category": "team-composition" },
        { "id": "Q17", "text": "where do six hat sessions go wrong in early discovery", "category": "team-composition" },
        { "id": "Q18", "text": "how to surface evidence tier when claims feel intuitive", "category": "evidence-grading" },
        { "id": "Q19", "text": "tools that turn a pile of meeting notes into a problem map", "category": "meeting-intelligence" },
        { "id": "Q20", "text": "frameworks that explicitly handle the regression of a well-defined problem back to ill-defined", "category": "bidirectional-stage" }
      ]
    }
    ```
    Exactly 20 entries. Each id Q01..Q20. Category strings vary across the 11 categories above so the Surface 3 aggregate overlap is not skewed by a single query class.

    FILE 2: tests/127.1-embedding-integrity.test.cjs -- Surface 1 harness.
    Contract:
    - Uses `const test = require('node:test'); const assert = require('node:assert/strict');` (Node native runner).
    - Reads tests/fixtures/127.1/embedding-manifest.fixture.json (Plan 127.1-01 produces it).
    - If fixture absent OR MINDRIAN_127_1_FIXTURE_MODE !== '1': prints `fixture missing -- run Plan 127.1-01 to generate tests/fixtures/127.1/embedding-manifest.fixture.json` and exits non-zero (process.exitCode = 1).
    - If fixture present:
      - Assert manifest.schema_version === 1.
      - Assert manifest.vector_count === 1427.
      - Assert manifest.embedding_dims === 1024.
      - Assert manifest.similarity_metric === 'cosine'.
      - Assert manifest.checksums is an array of length 1427.
      - For each entry assert: typeof entry.node_id === 'string'; entry.node_id.length > 0; typeof entry.pinecone_sha256 === 'string'; entry.pinecone_sha256.length === 64; typeof entry.neo4j_sha256 === 'string'; entry.neo4j_sha256.length === 64; entry.pinecone_sha256 === entry.neo4j_sha256.
      - On any drift collect the failing node_ids into an array, print the first 5, fail the test asserting `drift.length === 0` with message "embedding integrity drift on N vectors -- first 5: <ids>".
    - Final assertion message on success MUST contain the literal string `1427 vectors verified` so downstream automated <verify> blocks can grep for it.
    - File ends with a process-level Boolean print: if all assertions pass, console.log('OK 127.1-embedding-integrity 1427 vectors verified'); else process.exitCode = 1 already set.
    - NO em-dashes in any string literal. NO hardcoded creds. NO writes outside the file.

    FILE 3: tests/127.1-index-config.test.cjs -- Surface 2 harness.
    Contract:
    - Uses node:test runner.
    - Reads tests/fixtures/127.1/index-config.fixture.json (Plan 127.1-02 produces it).
    - If fixture absent OR MINDRIAN_127_1_FIXTURE_MODE !== '1': prints `fixture missing -- run Plan 127.1-02 to generate tests/fixtures/127.1/index-config.fixture.json` and exits non-zero.
    - Fixture shape (the canonical SHOW INDEXES YIELD form a Neo4j 5.13+ vector index returns):
      ```
      {
        "schema_version": 1,
        "captured_at": "<ISO 8601 timestamp>",
        "rows": [
          {
            "name": "mindrian_methodology_vec",
            "state": "ONLINE",
            "type": "VECTOR",
            "entityType": "NODE",
            "labelsOrTypes": ["MethodologyChunk"],
            "properties": ["embedding"],
            "indexConfig": {
              "vector.dimensions": 1024,
              "vector.similarity_function": "cosine"
            }
          }
        ]
      }
      ```
    - Assert: rows is non-empty array.
    - Find the row where name === 'mindrian_methodology_vec' (locked index name; planners renaming requires CONTEXT amendment).
    - Assert row.state === 'ONLINE'.
    - Assert row.type === 'VECTOR'.
    - Assert row.indexConfig['vector.dimensions'] === 1024.
    - Assert row.indexConfig['vector.similarity_function'] === 'cosine' (Lock #3 -- defensive even though Neo4j 5.13+ defaults to cosine per Tavily A127.1.3).
    - Assert row.properties is array containing 'embedding' (the locked property name; vectors live on the `embedding` property of MethodologyChunk nodes).
    - On success console.log('OK 127.1-index-config dim=1024 similarity=cosine state=ONLINE').
    - NO em-dashes. NO live Neo4j call -- fixture only.

    Decisions D-01 (HNSW defaults) + D-02 (one-shot LOAD) referenced in CONTEXT.md are NOT this task's surface; this task creates the assertions a successful migration must satisfy, not the migration itself.
  </action>
  <verify>
    <automated>node tests/127.1-embedding-integrity.test.cjs; node tests/127.1-index-config.test.cjs; test -f tests/127.1-graphrag-overlap-corpus.json &amp;&amp; node -e "const c=require('./tests/127.1-graphrag-overlap-corpus.json'); if(c.queries.length!==20)process.exit(1); if(c.embedding_dims_baseline!==1024)process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/127.1-graphrag-overlap-corpus.json` exists; `node -e "const c=require('./tests/127.1-graphrag-overlap-corpus.json'); console.log(c.queries.length)"` prints `20`.
    - `node -e "const c=require('./tests/127.1-graphrag-overlap-corpus.json'); console.log(c.embedding_dims_baseline, c.similarity_metric_baseline)"` prints `1024 cosine`.
    - `grep -c '"id": "Q' tests/127.1-graphrag-overlap-corpus.json` equals `20`.
    - File `tests/127.1-embedding-integrity.test.cjs` exists and has at least 80 lines (`wc -l tests/127.1-embedding-integrity.test.cjs | awk '{print $1}'` >= 80).
    - File `tests/127.1-index-config.test.cjs` exists and has at least 60 lines.
    - `node tests/127.1-embedding-integrity.test.cjs; echo $?` prints non-zero (RED by design -- fixture not produced until Plan 127.1-01 ships); stdout includes the literal string `fixture missing -- run Plan 127.1-01`.
    - `node tests/127.1-index-config.test.cjs; echo $?` prints non-zero; stdout includes `fixture missing -- run Plan 127.1-02`.
    - `grep -c -- "$(printf '\xe2\x80\x94')" tests/127.1-graphrag-overlap-corpus.json tests/127.1-embedding-integrity.test.cjs tests/127.1-index-config.test.cjs` equals `0` (no em-dashes; the U+2014 character is forbidden by project HARD rule).
    - `grep -c "1427 vectors verified" tests/127.1-embedding-integrity.test.cjs` >= 1.
    - `grep -E "dim=1024|similarity_function.*cosine" tests/127.1-index-config.test.cjs` returns at least 2 lines.
  </acceptance_criteria>
  <done>The three files exist on disk; both .test.cjs files exit non-zero with a clear "fixture missing" message naming the producing plan; the corpus JSON parses and contains exactly 20 queries with category diversity; em-dash grep returns 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Surface 3 (BLOCKING) harness + extend aggregators</name>
  <files>tests/127.1-graphrag-overlap.test.cjs, tests/run-all-127.sh, lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - tests/127.1-graphrag-overlap-corpus.json (the 20-query corpus from Task 1 -- this harness CONSUMES it)
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-VALIDATION.md (Surface 3 is BLOCKING; gates cutover)
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-RESEARCH.md (A127.1.4 -- supplement top-5 overlap with recall@10 vs brute-force ground truth)
    - tests/run-all-127.sh (the existing aggregator -- preserve its set -uo pipefail + exit-code semantics; do not break Phase 127 suites)
    - tests/run-all-126.sh (canonical aggregator shape for reference)
    - lib/memory/run-feynman-tests.cjs (Phase block registration pattern; see Phase 125 block as canonical)
  </read_first>
  <action>
    Two files modified, one file created.

    FILE 1: tests/127.1-graphrag-overlap.test.cjs -- Surface 3 harness (BLOCKING the cutover wave).
    Contract:
    - Uses node:test runner + node:assert/strict.
    - Reads tests/127.1-graphrag-overlap-corpus.json (existence-checked at top; if missing print "corpus missing -- Wave 0 Plan 127.1-00 must run first" and exit non-zero).
    - Reads tests/fixtures/127.1/overlap-baseline.fixture.json (Pinecone pre-cutover top-5 results per query; Plan 127.1-03 produces it).
    - Reads tests/fixtures/127.1/overlap-neo4j.fixture.json (Neo4j post-cutover top-5 results per query; Plan 127.1-03 produces it).
    - Both fixtures have the same shape:
      ```
      {
        "schema_version": 1,
        "engine": "pinecone" | "neo4j",
        "captured_at": "<ISO 8601>",
        "embedding_model": "multilingual-e5-large",
        "embedding_dims": 1024,
        "results": [
          { "query_id": "Q01", "top_5": ["<node_id_1>", "<node_id_2>", "<node_id_3>", "<node_id_4>", "<node_id_5>"] },
          ...
          { "query_id": "Q20", "top_5": [...] }
        ]
      }
      ```
    - If either fixture missing, exit non-zero with message naming the producing plan (`Plan 127.1-03 must run before this gate`).
    - Per query: compute overlap_i = |set(pinecone.top_5) intersect set(neo4j.top_5)| / 5.0 (a float in [0.0, 1.0]).
    - Aggregate: aggregate_overlap = mean(overlap_i) across 20 queries.
    - Per-query reporting: print "Q01: 4/5 (0.80)" etc. on stdout so the divergence log is grep-able.
    - Assert aggregate_overlap >= 0.80 (the canonical Lock 4 gate from CONTEXT.md).
    - Add an INFO line (NOT a gate) for any individual query with overlap_i < 0.60: "warn Q07 below 0.60 (0.40) -- consider HNSW tuning". Useful for tuning telemetry; does not fail the test.
    - On success final stdout line: `OK 127.1-graphrag-overlap aggregate_top5_overlap=0.<NN> (gate>=0.80)`.
    - Also assert: pinecone.embedding_model === neo4j.embedding_model === 'multilingual-e5-large' (Lock #2 model lock).
    - Also assert: pinecone.embedding_dims === neo4j.embedding_dims === 1024 (Lock #2 dim consistency).
    - File length minimum 120 lines including comments + helpers.
    - NO em-dashes. NO live backend calls (fixture-only at this surface; Plan 127.1-03 owns the live capture).

    FILE 2: tests/run-all-127.sh -- aggregator extension.
    Use Edit-style additions (read existing file FIRST). Add to SHELL_SUITES nothing (127.1 has no .sh suite). Add to CJS_SUITES (preserve existing order + comments; append 3 new entries between current entries and the test-loop machinery):
    ```
      127.1-embedding-integrity.test.cjs   -> Plan 127.1-01 produces embedding-manifest.fixture.json
      127.1-index-config.test.cjs          -> Plan 127.1-02 produces index-config.fixture.json
      127.1-graphrag-overlap.test.cjs      -> Plan 127.1-03 produces overlap-baseline + overlap-neo4j fixtures (BLOCKING cutover gate)
    ```
    In the CJS_SUITES bash array add three entries (entries are paths RELATIVE to the tests/ directory per the existing convention; 127.1 tests live IN tests/ not ../lib so the entries are just the filenames):
    ```
      127.1-embedding-integrity.test.cjs
      127.1-index-config.test.cjs
      127.1-graphrag-overlap.test.cjs
    ```
    Preserve the script's set -uo pipefail, the START_TIME measurement, the per-suite PASS/FAIL line emission, and the non-zero exit-on-any-failure semantics. Do NOT introduce a parallel aggregator (per VALIDATION.md Wave 0 Requirements). Update the header comment block at the top of the file to list the three new entries.

    FILE 3: lib/memory/run-feynman-tests.cjs -- register the three new test files in the Phase 127.1 block.
    Use Edit. Find the existing Phase 127 block (search for the line that registers the 127 tests). Append a new Phase 127.1 block immediately AFTER it. The Phase 127.1 block mirrors the Phase 125 block shape (header comment line citing the aggregator + each path on its own path.join entry). The three entries to add (with the same path.join idiom the existing blocks use):
    ```
      path.join(__dirname, '..', '..', 'tests', '127.1-embedding-integrity.test.cjs'),
      path.join(__dirname, '..', '..', 'tests', '127.1-index-config.test.cjs'),
      path.join(__dirname, '..', '..', 'tests', '127.1-graphrag-overlap.test.cjs'),
    ```
    Header comment for the block: `// Phase 127.1 brain-graphrag-collapse-pinecone-neo4j-hnsw (Wave 0 harness scaffold + Wave 1/2 outputs)`.

    NO em-dashes anywhere. NO git push. Single commit message: `test(127.1-00): scaffold Wave 0 harnesses + extend run-all-127.sh + register Phase 127.1 block`.
  </action>
  <verify>
    <automated>bash -n tests/run-all-127.sh &amp;&amp; node --check tests/127.1-graphrag-overlap.test.cjs &amp;&amp; node --check lib/memory/run-feynman-tests.cjs &amp;&amp; grep -c "127.1-" tests/run-all-127.sh</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/127.1-graphrag-overlap.test.cjs` exists with at least 120 lines.
    - `bash -n tests/run-all-127.sh; echo $?` prints `0` (syntactically valid bash).
    - `grep -c "127.1-embedding-integrity" tests/run-all-127.sh` >= 1.
    - `grep -c "127.1-index-config" tests/run-all-127.sh` >= 1.
    - `grep -c "127.1-graphrag-overlap" tests/run-all-127.sh` >= 1.
    - `node --check lib/memory/run-feynman-tests.cjs; echo $?` prints `0`.
    - `grep -c "127.1-embedding-integrity\|127.1-index-config\|127.1-graphrag-overlap" lib/memory/run-feynman-tests.cjs` equals `3`.
    - `node tests/127.1-graphrag-overlap.test.cjs; echo $?` prints non-zero with stdout containing `Plan 127.1-03 must run before this gate`.
    - `bash tests/run-all-127.sh; echo $?` exits non-zero and the output prints `127.1-embedding-integrity.test.cjs: FAILED` (or MISSING), `127.1-index-config.test.cjs: FAILED`, `127.1-graphrag-overlap.test.cjs: FAILED` -- all three RED by design; this is the Wave 0 expected end state.
    - `grep -c "aggregate_top5_overlap" tests/127.1-graphrag-overlap.test.cjs` >= 1.
    - `grep -c "gate>=0.80" tests/127.1-graphrag-overlap.test.cjs` >= 1.
    - `grep -c -- "$(printf '\xe2\x80\x94')" tests/127.1-graphrag-overlap.test.cjs` equals `0` (no em-dashes).
    - Phase 127 suites still register correctly: `grep -c "127-00-shim-handshake" tests/run-all-127.sh` >= 1 (preservation invariant).
  </acceptance_criteria>
  <done>Three Surface harnesses + 20-query corpus + extended aggregator + Feynman runner registration committed. Running `bash tests/run-all-127.sh` reports the 3 new 127.1 suites as FAILED-by-design with messages naming the consumer plans. Phase 127 suites continue to register and behave per their prior contract. The Wave 0 NYQUIST gate is honored: harness scaffolds in place before any substrate code.</done>
</task>

</tasks>

<verification>
- `bash tests/run-all-127.sh` runs to completion (non-zero exit OK; Wave 0 RED-by-design).
- All five frontmatter requirement IDs (01-05) traceable to specific files: 01 -> embedding-integrity.test.cjs, 02 -> index-config.test.cjs, 03 -> graphrag-overlap.test.cjs, 04 -> corpus JSON, 05 -> run-all-127.sh edits.
- No em-dashes (`grep -c -- "$(printf '\xe2\x80\x94')" tests/127.1-*.test.cjs tests/127.1-graphrag-overlap-corpus.json` equals 0).
- No Pinecone client imported in the new test files (Surface 3 fixture-only at this stage; Plan 127.1-03 owns the live capture).
- No reference to ~/.claude.json or any non-LOCAL state written by the harnesses (Canon Part 8 invariant).
</verification>

<success_criteria>
- 3 .test.cjs files exist + 1 corpus JSON exists, all parseable.
- run-all-127.sh extended without regressing Phase 127 suites.
- Feynman runner block registers the three new files in the canonical Phase block idiom.
- All four Validation Architecture surfaces have their harness contract on disk (Surface 4 = existing MCP integration suite, no new file).
- Surface 3 has the explicit 0.80 gate hardcoded in the harness.
- Em-dash count across all created/modified files is 0.
</success_criteria>

<output>
After completion, create `.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-00-SUMMARY.md` per the standard SUMMARY template.
</output>
