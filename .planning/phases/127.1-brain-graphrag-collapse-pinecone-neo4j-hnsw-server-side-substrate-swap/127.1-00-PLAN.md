---
phase: 127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/127.1-embedding-integrity.test.cjs
  - tests/127.1-graphrag-overlap-corpus.json
autonomous: true
gap_closure: false
requirements:
  - GRAPHRAG-COLLAPSE-127.1-01  # Surface 1 harness re-pointed to 12,401 vectors
  - GRAPHRAG-COLLAPSE-127.1-04  # 20-query corpus baseline metadata corrected
re_scope_corrective: true   # corrects the harness shipped by 127.1-00-SUMMARY.md (stale 1,427 premise)

must_haves:
  truths:
    - "The Surface 1 harness asserts a 12,401-vector corpus, not 1,427"
    - "The Surface 1 harness still asserts 1024-dim e5-large cosine (Locks 2 and 3 untouched)"
    - "The 20-query corpus baseline metadata names the 12,401-vector corpus, not 1,427"
  artifacts:
    - path: "tests/127.1-embedding-integrity.test.cjs"
      provides: "Surface 1 byte-identity harness re-pointed to the corrected corpus size"
      contains: "EXPECTED_VECTOR_COUNT = 12401"
    - path: "tests/127.1-graphrag-overlap-corpus.json"
      provides: "20-query Lock-4 corpus with corrected baseline metadata"
      contains: "12401"
  key_links:
    - from: "tests/127.1-embedding-integrity.test.cjs"
      to: "tests/fixtures/127.1/embedding-manifest.fixture.json"
      via: "manifest.vector_count assertion"
      pattern: "manifest.vector_count"
---

<objective>
Plan 127.1-00 was executed on 2026-05-19 under the now-invalidated premise that
the Pinecone corpus was 1,427 vectors. A live read (deferred-items.md DI-127.1-02)
proved the corpus is 12,401 vectors across 5 searchable namespaces. The shipped
Surface 1 harness (`tests/127.1-embedding-integrity.test.cjs`) hardcodes
`EXPECTED_VECTOR_COUNT = 1427` and asserts `manifest.vector_count === 1427`.
That number is wrong and will fail the corrected manifest forever.

This corrective plan re-points the Surface 1 harness and the 20-query corpus
baseline metadata to the corrected reality (12,401 vectors). It does NOT touch
the 1024-dim e5-large lock or the cosine lock, which were always correct.

Purpose: make the Wave-0 validation substrate consistent with the re-scoped
corpus size so Plans 01-05 land against a harness that can actually flip GREEN.
Output: a Surface 1 harness and corpus file that assert 12,401, not 1,427.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-CONTEXT.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/deferred-items.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-00-SUMMARY.md

<interfaces>
The Surface 1 harness shipped by 127.1-00 (now being corrected). Current stale state:

From tests/127.1-embedding-integrity.test.cjs (lines 40-44):
```
const FIXTURE_MODE = process.env.MINDRIAN_127_1_FIXTURE_MODE === '1';
const EXPECTED_VECTOR_COUNT = 1427;
const EXPECTED_DIMS = 1024;
const EXPECTED_METRIC = 'cosine';
const SHA256_HEX_LEN = 64;
```

The harness also contains hardcoded "1427" string literals in test names and
the stdout OK line (the `vector_count` test name, the byte-identity test name,
the `process.on('exit')` OK line, and the header comment block). EXPECTED_DIMS
and EXPECTED_METRIC are correct (Locks 2 and 3) and MUST NOT change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point the Surface 1 harness count from 1,427 to 12,401</name>
  <read_first>
    - tests/127.1-embedding-integrity.test.cjs (the file being modified, in full)
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-CONTEXT.md (the corrected corpus size 12,401; the namespace breakdown core 8,543 / materials 1,775 / reference 1,690 / tools 242 / graphrag 144 / books 7)
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/deferred-items.md (DI-127.1-02 -- why 1,427 is wrong)
  </read_first>
  <action>
In `tests/127.1-embedding-integrity.test.cjs`:
- Change `const EXPECTED_VECTOR_COUNT = 1427;` to `const EXPECTED_VECTOR_COUNT = 12401;`.
- Leave `const EXPECTED_DIMS = 1024;` and `const EXPECTED_METRIC = 'cosine';` exactly as they are -- Locks 2 and 3 were never wrong.
- Replace every hardcoded "1427" string literal that names the corpus size with "12401":
  - The test name `'127.1 Surface 1 -- vector_count is exactly 1427'` becomes `'127.1 Surface 1 -- vector_count is exactly 12401'`.
  - The byte-identity test name `'127.1 Surface 1 -- pinecone and neo4j SHA256 are byte-identical (1427 vectors verified)'` becomes `'... (12401 vectors verified)'`.
  - The `process.on('exit')` stdout line `'OK 127.1-embedding-integrity 1427 vectors verified\n'` becomes `'OK 127.1-embedding-integrity 12401 vectors verified\n'`.
- Update the header comment block: the line "For each of the 1,427 methodology embeddings" becomes "For each of the 12,401 methodology embeddings (5 searchable namespaces: core 8,543 / materials 1,775 / reference 1,690 / tools 242 / graphrag 144 / books 7)".
- Do NOT change any assertion that derives its expected value from the `EXPECTED_VECTOR_COUNT` constant (the `checksums.length` test and the `vector_count` test reference the constant; once it is 12401 they are automatically correct).
- Use hyphens, not em-dashes, in all edited text.
  </action>
  <verify>
    <automated>grep -c "12401" tests/127.1-embedding-integrity.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "EXPECTED_VECTOR_COUNT = 12401" tests/127.1-embedding-integrity.test.cjs` returns exactly 1
    - `grep -c "1427" tests/127.1-embedding-integrity.test.cjs` returns exactly 0
    - `grep -c "EXPECTED_DIMS = 1024" tests/127.1-embedding-integrity.test.cjs` returns exactly 1 (Lock 2 untouched)
    - `grep -c "EXPECTED_METRIC = 'cosine'" tests/127.1-embedding-integrity.test.cjs` returns exactly 1 (Lock 3 untouched)
    - `grep -c "OK 127.1-embedding-integrity 12401 vectors verified" tests/127.1-embedding-integrity.test.cjs` returns exactly 1
    - `node --check tests/127.1-embedding-integrity.test.cjs` exits 0
    - `node tests/127.1-embedding-integrity.test.cjs` exits 1 with stdout containing `fixture missing` (RED-by-design; the fixture lands in Plan 127.1-01)
    - `grep -cP "\xe2\x80\x94" tests/127.1-embedding-integrity.test.cjs` returns 0 (no em-dashes)
  </acceptance_criteria>
  <done>
The Surface 1 harness asserts a 12,401-vector corpus. Zero "1427" literals
remain. The 1024-dim and cosine locks are byte-unchanged. The harness still
fails RED-by-design because Plan 127.1-01 has not yet produced the manifest.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the 20-query corpus baseline metadata</name>
  <read_first>
    - tests/127.1-graphrag-overlap-corpus.json (the file being modified, in full)
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-CONTEXT.md (the 12,401 figure and the 5-namespace breakdown)
  </read_first>
  <action>
In `tests/127.1-graphrag-overlap-corpus.json`:
- The 20 queries Q01..Q20 themselves are CORRECT as a fuzzy-query set -- do NOT add, remove, or reword any query.
- Update only the file-level metadata so it describes the corrected corpus:
  - Add a top-level field `"corpus_vector_count_baseline": 12401` immediately after the existing `"similarity_metric_baseline": "cosine"` field.
  - In the `"source"` string, append: " The vector corpus this corpus probes is 12,401 vectors across 5 searchable namespaces (core 8543, materials 1775, reference 1690, tools 242, graphrag 144; books 7)."
- Leave `"embedding_model_baseline": "multilingual-e5-large"`, `"embedding_dims_baseline": 1024`, and `"similarity_metric_baseline": "cosine"` unchanged.
- Keep the JSON valid (the file is consumed by `JSON.parse`).
- Use hyphens, not em-dashes.
  </action>
  <verify>
    <automated>node -e "const c=require('./tests/127.1-graphrag-overlap-corpus.json'); if(c.corpus_vector_count_baseline!==12401)throw new Error('bad count'); if(c.queries.length!==20)throw new Error('bad query count'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('tests/127.1-graphrag-overlap-corpus.json','utf8'))"` exits 0 (valid JSON)
    - `node -e "console.log(require('./tests/127.1-graphrag-overlap-corpus.json').corpus_vector_count_baseline)"` prints `12401`
    - `node -e "console.log(require('./tests/127.1-graphrag-overlap-corpus.json').queries.length)"` prints `20`
    - `node -e "console.log(require('./tests/127.1-graphrag-overlap-corpus.json').embedding_dims_baseline)"` prints `1024`
    - `grep -c "1427" tests/127.1-graphrag-overlap-corpus.json` returns exactly 0
    - `grep -cP "\xe2\x80\x94" tests/127.1-graphrag-overlap-corpus.json` returns 0
  </acceptance_criteria>
  <done>
The corpus file's baseline metadata names the 12,401-vector corpus. The 20
queries are byte-unchanged. The file parses as valid JSON.
  </done>
</task>

</tasks>

<verification>
- `node --check tests/127.1-embedding-integrity.test.cjs` exits 0
- `bash tests/run-all-127.sh` reports no NEW regressions vs the 127.1-02-SUMMARY
  baseline (Phase 127 suites green; 127.1 family in expected RED/GREEN state)
- Zero "1427" string literals remain in either modified file
- Locks 2 (1024-dim) and 3 (cosine) are byte-unchanged in the harness
- Commit the two files with `git commit --no-verify` (Phase 127.1 wave-protocol
  convention); stage files explicitly, no `git add -A`
</verification>

<success_criteria>
- The Surface 1 harness and the 20-query corpus both assert/describe 12,401
  vectors, never 1,427
- The 1024-dim e5-large lock and the cosine lock are untouched
- Both files are syntactically valid and committed
</success_criteria>

<output>
After completion, create `.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-00-corrective-SUMMARY.md` (do NOT overwrite the existing 127.1-00-SUMMARY.md).
</output>
