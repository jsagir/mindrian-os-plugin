---
phase: 127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap
plan: 02
type: execute
wave: 2
depends_on:
  - 127.1-00
  - 127.1-01
files_modified:
  - mcp-server-brain/sql/127.1-vector-index.cypher
  - scripts/load-embeddings-into-neo4j.cjs
  - mcp-server-brain/lib/brain-vector-search.cjs
  - tests/127.1-index-config.test.cjs
  - tests/fixtures/127.1/index-config.fixture.json
autonomous: true
gap_closure: false
requirements:
  - GRAPHRAG-COLLAPSE-127.1-09  # NEW Neo4j 1024-dim HNSW vector index, cosine, distinct from the 384-dim layer
  - GRAPHRAG-COLLAPSE-127.1-10  # 12,401 vectors loaded as a dedicated node family, round-trip verified
  - GRAPHRAG-COLLAPSE-127.1-11  # brain-vector-search.cjs targets the NEW 1024-dim index
  - GRAPHRAG-COLLAPSE-127.1-12  # Surface 2 harness GREEN against the live 1024-dim index config
re_scope_corrective: true   # corrects the DDL/loader/library shipped under the stale 1,427 / vector-empty-Neo4j premise

must_haves:
  truths:
    - "A NEW 1024-dim Neo4j HNSW vector index exists with cosine similarity, distinct from all 7 pre-existing 384-dim indexes"
    - "All 12,401 migrated vectors live on a dedicated node label that does not collide with the 384-dim layer's labels or with :Command"
    - "Each migrated node carries a namespace property"
    - "The loader is idempotent, additive, reversible, and contains no DETACH DELETE or full-graph reload"
    - "brain-vector-search.cjs queries the NEW 1024-dim index, scoped to the migrated node family only"
  artifacts:
    - path: "mcp-server-brain/sql/127.1-vector-index.cypher"
      provides: "Canonical DDL for the NEW 1024-dim cosine HNSW index"
      contains: "vector.dimensions"
    - path: "scripts/load-embeddings-into-neo4j.cjs"
      provides: "Idempotent additive loader for the 12,401-vector node family"
      contains: "EXPECTED_VECTOR_COUNT = 12401"
    - path: "mcp-server-brain/lib/brain-vector-search.cjs"
      provides: "Server-side Neo4j vector-search library targeting the NEW index"
      contains: "mindrian_methodology_vec"
  key_links:
    - from: "scripts/load-embeddings-into-neo4j.cjs"
      to: "Neo4j :MethodologyChunk node family"
      via: "batched idempotent MERGE on id, additive SET"
      pattern: "MERGE \\(n:MethodologyChunk"
    - from: "mcp-server-brain/lib/brain-vector-search.cjs"
      to: "mindrian_methodology_vec index"
      via: "db.index.vector.queryNodes scoped to the new index name"
      pattern: "mindrian_methodology_vec"
---

<objective>
The old Plan 127.1-02 built the Neo4j vector substrate before the re-scope. It
shipped a DDL, a loader, and `brain-vector-search.cjs` against the wrong premise
(1,427 vectors; an assumed vector-empty Neo4j). DI-127.1-02 proved Neo4j already
has a populated native 384-dim vector layer (6,007 nodes, 7 HNSW indexes). The
collapse must build a NEW, SEPARATE 1024-dim index from the migrated Pinecone
corpus -- ALONGSIDE the 384-dim layer, never into it.

This corrective plan re-points the loader to 12,401 vectors, asserts the NEW
index is distinct from the 384-dim layer, hardens G-1 (additive / idempotent /
reversible / no DETACH DELETE) and G-3 (never scan `:Command` or graph-wide),
and confirms `brain-vector-search.cjs` queries the new index.

The chosen node label for the migrated corpus is `:MethodologyChunk` (D-06):
it does not collide with the 384-dim layer's labels (concept / creativework /
entity / framework / person / product / Chunk) and is not `:Command`. The chosen
index name is `mindrian_methodology_vec`. Both were already used by the old
plan; this plan keeps them and asserts the non-collision explicitly rather than
churning the name.

Purpose: a verified NEW 1024-dim HNSW index loaded with all 12,401 vectors.
Output: corrected DDL + loader + library + Surface 2 GREEN against a live config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-CONTEXT.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/deferred-items.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-02-SUMMARY.md
@mcp-server-brain/lib/neo4j-tools.cjs

<interfaces>
The DDL / loader / library shipped by the stale Plan 127.1-02. Current state:

From scripts/load-embeddings-into-neo4j.cjs (locked constants block):
```
const INDEX_NAME = 'mindrian_methodology_vec';   -- KEEP (D-06 -- distinct from 384-dim layer + :Command)
const NODE_LABEL = 'MethodologyChunk';           -- KEEP (D-06 -- non-colliding)
const EMBEDDING_DIMS = 1024;                     -- correct (Lock 2)
const SIMILARITY_METRIC = 'cosine';              -- correct (Lock 3)
const EMBEDDING_PROPERTY = 'embedding';
const EXPECTED_VECTOR_COUNT = 1427;              -- WRONG, must be 12401
```
The loader already MERGEs `:MethodologyChunk { id }` with `SET n.embedding`,
`n.namespace`, `n.metadata` -- the namespace property is already wired. The
loader's count assertions reference EXPECTED_VECTOR_COUNT.

From mcp-server-brain/lib/brain-vector-search.cjs:
```
const INDEX_NAME = 'mindrian_methodology_vec';
const EMBEDDING_DIMS = 1024;
const NODE_LABEL = 'MethodologyChunk';
searchSemantic({embedding, topK, namespace, session}) -> {hits, engine, version, used_search_clause}
registerVectorSearchTool(server)   -- registers brain_search; NOT yet wired into server.cjs
```
The library's tool description string hardcodes "1427 PWS methodology vectors"
-- stale, must become 12,401.

The 7 pre-existing 384-dim indexes that MUST NOT be touched (DI-127.1-02):
  concept_embeddings, creativework_embeddings, entity_embeddings,
  framework_embeddings, person_embeddings, product_embeddings, vector (Chunk).

The Surface 2 harness contract (tests/127.1-index-config.test.cjs):
  asserts a SHOW INDEXES row name=mindrian_methodology_vec, state=ONLINE,
  type=VECTOR, indexConfig.{vector.dimensions=1024, vector.similarity_function=cosine},
  properties=[embedding].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the DDL + loader to the 12,401-vector NEW-index contract with G-1/G-3 hardening</name>
  <read_first>
    - mcp-server-brain/sql/127.1-vector-index.cypher (the DDL file being modified, in full)
    - scripts/load-embeddings-into-neo4j.cjs (the loader being modified, in full)
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-CONTEXT.md (D-EMB; G-1; G-3; the 384-dim layer is out of scope; 12,401)
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/deferred-items.md (DI-127.1-02 -- the 7 existing 384-dim indexes, the 6,007-node layer)
  </read_first>
  <action>
In `scripts/load-embeddings-into-neo4j.cjs`:
- Change `const EXPECTED_VECTOR_COUNT = 1427;` to `const EXPECTED_VECTOR_COUNT = 12401;`. Every count assertion in the loader references this constant; once corrected they are automatically right.
- Keep `INDEX_NAME = 'mindrian_methodology_vec'`, `NODE_LABEL = 'MethodologyChunk'`, `EMBEDDING_DIMS = 1024`, `SIMILARITY_METRIC = 'cosine'`, `EMBEDDING_PROPERTY = 'embedding'` unchanged -- they are the D-06 / Lock-2 / Lock-3 contract.
- G-1 hardening (additive / idempotent / reversible / no full reload):
  - Verify the loader contains ZERO `DETACH DELETE`, zero `MATCH (n) DELETE`, zero full-graph reload. If any exists, remove it. The load is `MERGE`-on-`id` + additive `SET` only.
  - Every node-scanning query in the loader (the post-load count check, the
    round-trip read) MUST be scoped to `(n:MethodologyChunk)` -- never a bare
    `MATCH (n)`. Audit `roundTripVerify()` and the count query and confirm both
    are label-scoped. If any is graph-wide, scope it to `:MethodologyChunk`.
  - Add a reversibility note + a guarded rollback path: a `--rollback` CLI flag
    that drops ONLY the `mindrian_methodology_vec` index and detaches ONLY
    `:MethodologyChunk` nodes (`MATCH (n:MethodologyChunk) DETACH DELETE n` --
    this is the ONE place a scoped DETACH DELETE is allowed, because it targets
    exactly the migrated node family and is the explicit reversibility path G-1
    requires; it is NOT a full-graph reload). Document in a comment that
    `--rollback` is the G-1 reversibility affordance and is scoped to the
    migrated family only.
- G-3 hardening (reserve :Command, never scan it): add a comment block at the
  top of the loader stating that the loader touches ONLY `:MethodologyChunk`
  nodes and the `mindrian_methodology_vec` index; it never reads, writes,
  scans, or indexes `:Command` nodes or `SERVES_STATE` / `ANSWERS_JTBD` /
  `BELONGS_TO_METHODOLOGY` edges, and never touches the 7 pre-existing 384-dim
  indexes. Confirm by code audit that no query in the loader names `:Command`
  or any of the 7 existing index names or any 384-dim label.
- Add an idempotency proof affordance: a `--verify-only` path (already present
  per the old summary) that re-reads `:MethodologyChunk` count + the index
  config without re-loading -- so a second invocation is a safe no-op.
- Update the loader's success line to: `OK 127.1-load index=mindrian_methodology_vec ONLINE dim=1024 cosine vectors_loaded=12401 round_trip_drift=0`.

In `mcp-server-brain/sql/127.1-vector-index.cypher`:
- The `CREATE VECTOR INDEX mindrian_methodology_vec IF NOT EXISTS FOR (n:MethodologyChunk) ON (n.embedding)` statement with `vector.dimensions: 1024` + `vector.similarity_function: 'cosine'` is already correct -- keep it byte-identical. `IF NOT EXISTS` is the G-1 idempotency guarantee for the DDL.
- Correct the header comment block: replace every "1,427" / "1427" reference with "12,401" / "12401". Replace the Lock-1 comment's "1,427 fuzzy queries" / corpus-size phrasing with the 12,401 figure.
- Add a comment paragraph stating: "This is a NEW 1024-dim index. The Brain Neo4j ALREADY carries 7 pre-existing 384-dim HNSW indexes (concept / creativework / entity / framework / person / product / Chunk-vector) on 6,007 entity nodes. Phase 127.1 does NOT touch that 384-dim layer. The `mindrian_methodology_vec` index and the `:MethodologyChunk` node label are a separate, additive node family per G-1."
- Use hyphens, not em-dashes, in all edited text.
  </action>
  <verify>
    <automated>node --check scripts/load-embeddings-into-neo4j.cjs && grep -c "EXPECTED_VECTOR_COUNT = 12401" scripts/load-embeddings-into-neo4j.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/load-embeddings-into-neo4j.cjs` exits 0
    - `grep -c "EXPECTED_VECTOR_COUNT = 12401" scripts/load-embeddings-into-neo4j.cjs` returns exactly 1
    - `grep -c "1427" scripts/load-embeddings-into-neo4j.cjs mcp-server-brain/sql/127.1-vector-index.cypher` returns exactly 0
    - `grep -cE "DETACH DELETE" scripts/load-embeddings-into-neo4j.cjs` returns exactly 1, and the single match is inside the `--rollback` path scoped to `(n:MethodologyChunk)` (G-1 reversibility affordance, not a full reload)
    - `grep -cE "MATCH \(n\) " scripts/load-embeddings-into-neo4j.cjs` returns 0 (no graph-wide scans -- G-3)
    - `grep -c ":Command" scripts/load-embeddings-into-neo4j.cjs` returns 0 (G-3: :Command never named)
    - `grep -cE "concept_embeddings|creativework_embeddings|entity_embeddings|framework_embeddings|person_embeddings|product_embeddings" scripts/load-embeddings-into-neo4j.cjs` returns 0 (the 384-dim layer is never touched)
    - `grep -c "CREATE VECTOR INDEX mindrian_methodology_vec IF NOT EXISTS" mcp-server-brain/sql/127.1-vector-index.cypher` returns exactly 1
    - `grep -c "12401\|12,401" mcp-server-brain/sql/127.1-vector-index.cypher` is at least 1
    - `grep -c "384" mcp-server-brain/sql/127.1-vector-index.cypher` is at least 1 (the orthogonal-layer comment landed)
    - `grep -c "rollback" scripts/load-embeddings-into-neo4j.cjs` is at least 1 (reversibility affordance)
    - `grep -cP "\xe2\x80\x94" scripts/load-embeddings-into-neo4j.cjs mcp-server-brain/sql/127.1-vector-index.cypher` returns 0
  </acceptance_criteria>
  <done>
The DDL creates a NEW 1024-dim cosine index distinct from the 384-dim layer.
The loader targets 12,401 vectors, is additive / idempotent / reversible, has
no graph-wide scan, never names `:Command` or the 7 existing indexes, and
carries a scoped `--rollback` reversibility path.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the live load against the NEW index and flip Surface 2 GREEN; re-point brain-vector-search.cjs</name>
  <read_first>
    - scripts/load-embeddings-into-neo4j.cjs (the corrected loader from Task 1)
    - mcp-server-brain/lib/brain-vector-search.cjs (the library being modified, in full)
    - tests/127.1-index-config.test.cjs (the Surface 2 harness contract)
    - tests/fixtures/127.1/index-config.fixture.json (the synthetic fixture to be overwritten by the live capture)
    - mcp-server-brain/render.yaml (NEO4J_URI / NEO4J_USER / NEO4J_PASSWORD env-var manifest)
  </read_first>
  <action>
1. Source `NEO4J_URI` / `NEO4J_USER` / `NEO4J_PASSWORD` from `mcp-server-brain/.env`
   (or the Render env vars). If unavailable, this is an authentication gate --
   surface it and stop; the operator provides credentials, then re-run.
2. PRE-FLIGHT SAFETY CHECK (G-1 + the 384-dim-layer boundary): before loading,
   run `SHOW INDEXES` and confirm the 7 pre-existing 384-dim indexes
   (concept_embeddings, creativework_embeddings, entity_embeddings,
   framework_embeddings, person_embeddings, product_embeddings, vector) are
   present. Record their names + states. The load MUST NOT change any of them.
3. Run the corrected loader:
   `node scripts/load-embeddings-into-neo4j.cjs --dump ~/.mindrian/127.1/embeddings.ndjson --manifest tests/fixtures/127.1/embedding-manifest.fixture.json --emit-index-config tests/fixtures/127.1/index-config.fixture.json`.
   The loader: applies the DDL idempotently, batched-MERGEs all 12,401
   `:MethodologyChunk` nodes (additive), polls the index to ONLINE, runs the
   SHA256 round-trip, rewrites the manifest's `neo4j_sha256` column from the
   live read, and overwrites `index-config.fixture.json` with the live
   `SHOW INDEXES` row via `captureIndexConfig()`.
   Confirm the final stdout line: `OK 127.1-load index=mindrian_methodology_vec ONLINE dim=1024 cosine vectors_loaded=12401 round_trip_drift=0`.
4. POST-LOAD SAFETY CHECK: re-run `SHOW INDEXES`; confirm the 7 pre-existing
   384-dim indexes are byte-unchanged (same names, same states) and that
   `mindrian_methodology_vec` is a NEW 8th index at dim=1024. Confirm via Cypher
   that `(:MethodologyChunk)` count is exactly 12,401 and that the 6,007-node
   384-dim layer is untouched (`MATCH (n) WHERE n.embedding IS NOT NULL AND NOT n:MethodologyChunk RETURN count(n)` still returns 6007 -- this query is read-only and label-scoped, allowed because it asserts the 384-dim layer was NOT touched).
5. In `mcp-server-brain/lib/brain-vector-search.cjs`:
   - Confirm `INDEX_NAME = 'mindrian_methodology_vec'` and `EMBEDDING_DIMS = 1024`
     -- the library already targets the NEW index name; verify, do not change.
   - Correct the `registerVectorSearchTool` tool-description string: replace
     "1427 PWS methodology vectors" with "12,401 PWS methodology vectors across
     6 namespaces". Update the namespace hint to name all searchable namespaces:
     "Namespaces: core, materials, reference, tools, graphrag (omit for all)".
   - Confirm `searchSemantic`'s Cypher uses `db.index.vector.queryNodes($indexName, ...)` with `$indexName = mindrian_methodology_vec` so it queries ONLY the NEW index, never the 384-dim indexes (G-3 scoping by index name). Verify the namespace filter (`WHERE node.namespace = $namespace`) is preserved on both the SEARCH-clause and the queryNodes branches.
   - Add a header comment: "This library queries the NEW 1024-dim
     `mindrian_methodology_vec` index ONLY. It never queries the 7 pre-existing
     384-dim entity-embedding indexes and never touches `:Command`."
6. Verify Surface 2 flips GREEN against the LIVE-captured fixture:
   `MINDRIAN_127_1_FIXTURE_MODE=1 node tests/127.1-index-config.test.cjs` must
   print `OK 127.1-index-config dim=1024 similarity=cosine state=ONLINE` and
   exit 0. The fixture's `synthetic_pre_live_load` flag must now be absent or
   false (it was overwritten by the live `captureIndexConfig()`).
7. Use hyphens, not em-dashes, throughout.
  </action>
  <verify>
    <automated>MINDRIAN_127_1_FIXTURE_MODE=1 node tests/127.1-index-config.test.cjs 2>&1 | grep "OK 127.1-index-config dim=1024 similarity=cosine state=ONLINE"</automated>
  </verify>
  <acceptance_criteria>
    - The loader's live run prints `OK 127.1-load index=mindrian_methodology_vec ONLINE dim=1024 cosine vectors_loaded=12401 round_trip_drift=0`
    - `MINDRIAN_127_1_FIXTURE_MODE=1 node tests/127.1-index-config.test.cjs` exits 0 with stdout `OK 127.1-index-config dim=1024 similarity=cosine state=ONLINE`
    - `tests/fixtures/127.1/index-config.fixture.json` no longer carries `"synthetic_pre_live_load": true` (it was overwritten by the live capture)
    - `MINDRIAN_127_1_FIXTURE_MODE=1 node tests/127.1-embedding-integrity.test.cjs` exits 0 (Surface 1 still GREEN -- the loader rewrote `neo4j_sha256` from the live round-trip with zero drift)
    - `node --check mcp-server-brain/lib/brain-vector-search.cjs` exits 0
    - `grep -c "1427" mcp-server-brain/lib/brain-vector-search.cjs` returns exactly 0
    - `grep -c "12,401\|12401" mcp-server-brain/lib/brain-vector-search.cjs` is at least 1
    - `grep -c "mindrian_methodology_vec" mcp-server-brain/lib/brain-vector-search.cjs` is at least 1
    - `grep -cE "concept_embeddings|entity_embeddings|framework_embeddings" mcp-server-brain/lib/brain-vector-search.cjs` returns 0 (never queries the 384-dim layer)
    - `grep -c ":Command" mcp-server-brain/lib/brain-vector-search.cjs` returns 0 (G-3)
    - `grep -cP "\xe2\x80\x94" mcp-server-brain/lib/brain-vector-search.cjs tests/127.1-index-config.test.cjs tests/fixtures/127.1/index-config.fixture.json` returns 0
  </acceptance_criteria>
  <done>
The NEW 1024-dim `mindrian_methodology_vec` index exists ONLINE with all 12,401
vectors; the 7 pre-existing 384-dim indexes are byte-unchanged; the 6,007-node
384-dim layer is untouched. Surface 1 and Surface 2 are both GREEN against live
fixtures. brain-vector-search.cjs targets the NEW index and names 12,401.
  </done>
</task>

</tasks>

<verification>
- A NEW 1024-dim `mindrian_methodology_vec` HNSW index exists, distinct from the
  7 pre-existing 384-dim indexes (G-1 additive)
- All 12,401 vectors loaded as `:MethodologyChunk` nodes, each with a `namespace`
  property; round-trip SHA256 drift is zero (Lock 1)
- No loader query is graph-wide; `:Command` is never named; the 384-dim layer is
  never touched (G-3 + the orthogonal-layer boundary)
- The loader carries a `--rollback` reversibility path scoped to `:MethodologyChunk`
- Surface 1 and Surface 2 harnesses are both GREEN against live fixtures
- Commit `mcp-server-brain/sql/127.1-vector-index.cypher`,
  `scripts/load-embeddings-into-neo4j.cjs`,
  `mcp-server-brain/lib/brain-vector-search.cjs`, `tests/127.1-index-config.test.cjs`
  (if touched), and `tests/fixtures/127.1/index-config.fixture.json` with
  `git commit --no-verify`; stage explicitly, no `git add -A`
</verification>

<success_criteria>
- The migration is additive: a NEW index + a NEW node family land alongside the
  existing graph; nothing pre-existing is mutated (G-1)
- 12,401 vectors loaded byte-identical (Lock 1), 1024-dim e5-large (Lock 2),
  cosine (Lock 3)
- brain-vector-search.cjs queries the NEW index only and names the corrected count
- Surface 1 + Surface 2 GREEN; the cutover (Plan 127.1-04) is unblocked at the
  substrate level
</success_criteria>

<output>
After completion, create `.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-02-corrective-SUMMARY.md` (do NOT overwrite the existing 127.1-02-SUMMARY.md).
</output>
