---
phase: 127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/export-pinecone-embeddings.cjs
  - tests/fixtures/127.1/embedding-manifest.fixture.json
  - mcp-server-brain/CLAUDE.md
autonomous: true
gap_closure: false
requirements:
  - GRAPHRAG-COLLAPSE-127.1-06  # Pinecone export script -- byte-identical NDJSON dump of all 12,401 vectors
  - GRAPHRAG-COLLAPSE-127.1-07  # Embedding manifest fixture exists; Surface 1 harness goes GREEN
  - GRAPHRAG-COLLAPSE-127.1-08  # Export honors Lock 1 (byte-identical, no re-embedding) and Lock 2 (e5-large 1024 dims)
re_scope_corrective: true   # corrects scripts/export-pinecone-embeddings.cjs (shipped under stale 1,427 / core-only premise)
user_setup:
  - service: pinecone
    why: "Server-side baseline read of the pws-brain index to produce the 12,401-vector dump across 5 namespaces that the Neo4j loader consumes"
    env_vars:
      - name: PINECONE_API_KEY
        source: "mcp-server-brain Render service env vars (already provisioned for the live Brain)"
      - name: PINECONE_INDEX
        source: "value is 'pws-brain' (the render.yaml default)"

must_haves:
  truths:
    - "The export script reads all 12,401 vectors across all 5 searchable namespaces, not just core"
    - "Each exported vector carries its source namespace, node id, 1024-dim float array, and metadata, byte-identical to Pinecone"
    - "The export never re-embeds; it copies the existing 1024-dim e5-large float arrays unchanged (Lock 1)"
    - "The manifest fixture exists with 12,401 entries and flips Surface 1 GREEN"
  artifacts:
    - path: "scripts/export-pinecone-embeddings.cjs"
      provides: "Idempotent multi-namespace Pinecone exporter producing the NDJSON dump + SHA256 manifest"
      contains: "EXPECTED_VECTOR_COUNT = 12401"
    - path: "tests/fixtures/127.1/embedding-manifest.fixture.json"
      provides: "schema-v1 manifest with 12,401 checksum entries (Surface 1 input)"
      contains: "12401"
  key_links:
    - from: "scripts/export-pinecone-embeddings.cjs"
      to: "~/.mindrian/127.1/embeddings.ndjson"
      via: "streamed NDJSON write, one record per vector, namespace field on every line"
      pattern: "namespace"
    - from: "scripts/export-pinecone-embeddings.cjs"
      to: "tests/fixtures/127.1/embedding-manifest.fixture.json"
      via: "SHA256-per-vector manifest emit"
      pattern: "embedding-manifest.fixture.json"
---

<objective>
The export script `scripts/export-pinecone-embeddings.cjs` was shipped by the
old Plan 127.1-01 under two wrong assumptions: (1) the corpus is 1,427 vectors,
(2) only the `core` namespace migrates. DI-127.1-01 + the re-scoped CONTEXT.md
correct both: the corpus is all 12,401 vectors across 5 searchable namespaces
(`core` 8,543 / `materials` 1,775 / `reference` 1,690 / `tools` 242 / `graphrag`
144) plus `books` (7) -- because retiring Pinecone means everything `brain_search`
can reach must migrate.

This plan re-points the exporter to a multi-namespace export of all 12,401
vectors, produces the byte-identical NDJSON dump, and emits the SHA256 manifest
fixture that flips the Surface 1 harness from RED to GREEN.

Purpose: produce the corpus the Neo4j loader (Plan 127.1-02) consumes.
Output: a 12,401-record NDJSON dump + a 12,401-entry manifest fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-CONTEXT.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/deferred-items.md
@mcp-server-brain/lib/pinecone-tools.cjs

<interfaces>
The exporter shipped by the stale Plan 127.1-01. Current stale state to correct:

From scripts/export-pinecone-embeddings.cjs:
```
const PINECONE_NAMESPACE = 'core';                 // single namespace -- WRONG, must be all 5
const EXPECTED_VECTOR_COUNT = 1427;                // WRONG, must be 12401
// connects: client.index(PINECONE_INDEX).namespace(PINECONE_NAMESPACE)
// asserts: if (ids.length !== EXPECTED_VECTOR_COUNT) -> error
// asserts: if (writtenCount !== EXPECTED_VECTOR_COUNT) -> error
```

The Surface 1 harness contract (tests/127.1-embedding-integrity.test.cjs, after
Plan 127.1-00 corrective) -- the manifest this plan must produce:
```
manifest.schema_version === 1
manifest.vector_count === 12401
manifest.embedding_dims === 1024
manifest.similarity_metric === 'cosine'
manifest.checksums  -- array, length 12401
  each entry: { node_id: string, pinecone_sha256: 64-hex, neo4j_sha256: 64-hex }
  pre-load: pinecone_sha256 === neo4j_sha256 by construction (Plan 127.1-02's
  loader rewrites neo4j_sha256 from the live round-trip read)
```

The 5 searchable namespaces with live counts (CONTEXT.md "Ground truth"):
  core 8543 / materials 1775 / reference 1690 / tools 242 / graphrag 144 / books 7
  Total = 12401.

The NDJSON record shape the Neo4j loader (scripts/load-embeddings-into-neo4j.cjs)
already expects, one JSON object per line:
```
{ "id": string, "values": number[1024], "namespace": string, "metadata": object }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point the exporter to a 5-namespace, 12,401-vector export</name>
  <read_first>
    - scripts/export-pinecone-embeddings.cjs (the file being modified, in full)
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-CONTEXT.md (the 12,401 figure, the 5-namespace breakdown, Lock 1 byte-identical)
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/deferred-items.md (DI-127.1-01 -- the namespace count table)
    - mcp-server-brain/lib/pinecone-tools.cjs (the namespace + searchRecords idiom this exporter mirrors)
  </read_first>
  <action>
Rewrite `scripts/export-pinecone-embeddings.cjs` so it exports the full
12,401-vector corpus across all 5 searchable namespaces, not just `core`:

- Replace `const PINECONE_NAMESPACE = 'core';` with a namespace manifest:
  `const PINECONE_NAMESPACES = [ {name:'core',count:8543}, {name:'materials',count:1775}, {name:'reference',count:1690}, {name:'tools',count:242}, {name:'graphrag',count:144}, {name:'books',count:7} ];`
  (6 namespaces total; `books` carries 7 records -- include it so the dump is the
  complete pre-cutover corpus.)
- Replace `const EXPECTED_VECTOR_COUNT = 1427;` with `const EXPECTED_VECTOR_COUNT = 12401;`.
- The export loop now iterates `PINECONE_NAMESPACES`: for each `{name}`, connect
  via `client.index(PINECONE_INDEX).namespace(name)`, list all vector ids in
  that namespace (paginated -- use the Pinecone `listPaginated` cursor pattern;
  do NOT assume a single page), then `fetch` each id's full record (id, values,
  metadata) and stream one NDJSON line per record to the dump file.
- Every NDJSON record MUST carry a `"namespace"` field set to the namespace it
  came from -- the Neo4j loader keys on this (`SET n.namespace = row.namespace`).
- Lock 1 (byte-identical, no re-embedding): the exporter copies the existing
  1024-dim float arrays from Pinecone verbatim into the `values` field. It MUST
  NOT call any embedding model, MUST NOT round-trip values through a lossy
  transform. Add a guard: if any fetched record's `values.length !== 1024`,
  abort with a precise error naming the offending id and namespace.
- Per-namespace count assertion: after listing a namespace, assert the observed
  id count equals that namespace's expected `count` from `PINECONE_NAMESPACES`.
  If a namespace drifts, abort with an error naming the namespace, the expected
  count, and the observed count -- do NOT silently continue (a count drift is a
  Rule-4 architectural signal that must stop execution, exactly as DI-127.1-01
  was raised).
- Total assertion: after all namespaces, assert `writtenCount === 12401`; abort
  with a precise error otherwise.
- Idempotent + re-runnable (G-1 applies to every 127.1 script): the dump path is
  `~/.mindrian/127.1/embeddings.ndjson`; if it already exists, overwrite it
  cleanly (truncate-then-write), so a second run produces an identical dump.
  The script makes ZERO writes to Neo4j and ZERO `DETACH DELETE` -- it is a
  read-from-Pinecone, write-to-local-file script only.
- The dump path stays under `~/.mindrian/127.1/` (gitignored) because records
  carry user/methodology content -- Canon Part 8: the dump is local, never
  committed, never egressed.
- CLI surface: keep `--dry-run` (prints the namespace plan + expected total
  without connecting); add `--dump <path>` (override the dump path, default
  `~/.mindrian/127.1/embeddings.ndjson`).
- Final stdout success line, grep-able: `OK 127.1-export namespaces=6 vectors=12401 dims=1024`.
- Use hyphens, not em-dashes.
  </action>
  <verify>
    <automated>node --check scripts/export-pinecone-embeddings.cjs && grep -c "EXPECTED_VECTOR_COUNT = 12401" scripts/export-pinecone-embeddings.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/export-pinecone-embeddings.cjs` exits 0
    - `grep -c "EXPECTED_VECTOR_COUNT = 12401" scripts/export-pinecone-embeddings.cjs` returns exactly 1
    - `grep -c "1427" scripts/export-pinecone-embeddings.cjs` returns exactly 0
    - `grep -cE "core|materials|reference|tools|graphrag|books" scripts/export-pinecone-embeddings.cjs` is at least 6 (all 6 namespace names present)
    - `grep -c "DETACH DELETE" scripts/export-pinecone-embeddings.cjs` returns exactly 0 (G-1)
    - `grep -c "listPaginated" scripts/export-pinecone-embeddings.cjs` is at least 1 (paginated listing, not single-page assumption)
    - `node scripts/export-pinecone-embeddings.cjs --dry-run 2>&1 | grep -E "12401|vectors"` prints a line naming 12401 (dry-run needs no live connection)
    - `grep -cP "\xe2\x80\x94" scripts/export-pinecone-embeddings.cjs` returns 0 (no em-dashes)
  </acceptance_criteria>
  <done>
The exporter targets all 12,401 vectors across 6 namespaces, carries the
namespace on every record, never re-embeds, asserts per-namespace and total
counts, and is idempotent. `--dry-run` prints the corrected plan.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the live export and emit the 12,401-entry manifest fixture</name>
  <read_first>
    - scripts/export-pinecone-embeddings.cjs (the corrected exporter from Task 1)
    - tests/127.1-embedding-integrity.test.cjs (the Surface 1 harness contract this fixture must satisfy)
    - mcp-server-brain/render.yaml (the PINECONE_API_KEY / PINECONE_INDEX env-var manifest)
  </read_first>
  <action>
Run the corrected exporter against the live `pws-brain` Pinecone index and
produce the manifest fixture that flips Surface 1 GREEN.

1. Source `PINECONE_API_KEY` from `mcp-server-brain/.env` (or the Render env
   vars); `PINECONE_INDEX` is `pws-brain`. If the key is not available in the
   environment, this is an authentication gate -- surface it and stop; the
   operator provides the key, then re-run.
2. Run: `node scripts/export-pinecone-embeddings.cjs --dump ~/.mindrian/127.1/embeddings.ndjson`.
   Confirm the final stdout line is `OK 127.1-export namespaces=6 vectors=12401 dims=1024`.
3. The exporter (Task 1) also emits the SHA256 manifest at
   `tests/fixtures/127.1/embedding-manifest.fixture.json`. The manifest schema:
   - `schema_version: 1`
   - `vector_count: 12401`
   - `embedding_dims: 1024`
   - `similarity_metric: 'cosine'`
   - `checksums`: array of 12,401 entries, each
     `{ node_id, pinecone_sha256, neo4j_sha256 }` where `pinecone_sha256` is the
     SHA256 of the vector's 1024-float array. PRE-LOAD, `neo4j_sha256` is set
     equal to `pinecone_sha256` by construction (the Surface 1 harness asserts
     byte-identity; Plan 127.1-02's loader rewrites the `neo4j_sha256` column
     from a live Neo4j round-trip read).
   - The SHA256 protocol MUST be byte-identical to the loader's:
     `sha256(Buffer.from(new Float64Array(values).buffer)).digest('hex')`.
     Mirror exactly what `scripts/load-embeddings-into-neo4j.cjs` already does.
4. The manifest fixture is checksums-only (no vector content, no methodology
   text, no source-file paths) -- it is safe to commit per Canon Part 8 (the
   shipped 127.1-00-SUMMARY confirmed this contract). The NDJSON dump itself is
   NOT committed (gitignored under `~/.mindrian/`).
5. Verify Surface 1 flips GREEN:
   `MINDRIAN_127_1_FIXTURE_MODE=1 node tests/127.1-embedding-integrity.test.cjs`
   must print `OK 127.1-embedding-integrity 12401 vectors verified` and exit 0.
6. Update `mcp-server-brain/CLAUDE.md`: in the 6-tool startup surface table,
   change the `brain_search` row's note from "Pinecone semantic search over
   methodology embeddings" to "Pinecone semantic search (12,401 vectors, 6
   namespaces) -- retiring to Neo4j HNSW in Phase 127.1". Add one sentence under
   the table: "Phase 127.1 migrates the 12,401-vector Pinecone corpus to a new
   1024-dim Neo4j HNSW index; the pre-existing 384-dim Neo4j entity-embedding
   layer is a separate orthogonal layer that Phase 127.1 does not touch."
   Use hyphens, not em-dashes.
  </action>
  <verify>
    <automated>MINDRIAN_127_1_FIXTURE_MODE=1 node tests/127.1-embedding-integrity.test.cjs 2>&1 | grep "OK 127.1-embedding-integrity 12401 vectors verified"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/127.1/embedding-manifest.fixture.json` exists and parses as valid JSON
    - `node -e "const m=require('./tests/fixtures/127.1/embedding-manifest.fixture.json'); if(m.vector_count!==12401)throw new Error('count'); if(m.checksums.length!==12401)throw new Error('len'); if(m.embedding_dims!==1024)throw new Error('dims'); if(m.similarity_metric!=='cosine')throw new Error('metric'); console.log('OK')"` exits 0
    - `node -e "const m=require('./tests/fixtures/127.1/embedding-manifest.fixture.json'); const bad=m.checksums.filter(c=>!/^[0-9a-f]{64}$/.test(c.pinecone_sha256)||!/^[0-9a-f]{64}$/.test(c.neo4j_sha256)); if(bad.length)throw new Error(bad.length+' bad checksums'); console.log('OK')"` exits 0
    - `MINDRIAN_127_1_FIXTURE_MODE=1 node tests/127.1-embedding-integrity.test.cjs` exits 0 with stdout `OK 127.1-embedding-integrity 12401 vectors verified`
    - `~/.mindrian/127.1/embeddings.ndjson` exists with exactly 12401 non-empty lines (`wc -l`)
    - `git check-ignore ~/.mindrian/127.1/embeddings.ndjson` confirms the dump is NOT tracked (path is under a gitignored tree)
    - `grep -c "127.1" mcp-server-brain/CLAUDE.md` is at least 1 (the Phase 127.1 note landed)
    - `grep -cP "\xe2\x80\x94" tests/fixtures/127.1/embedding-manifest.fixture.json mcp-server-brain/CLAUDE.md` returns 0
  </acceptance_criteria>
  <done>
The live export produced a 12,401-record NDJSON dump and a 12,401-entry manifest
fixture. Surface 1 is GREEN. mcp-server-brain/CLAUDE.md names the Phase 127.1
migration and the orthogonal-384-dim-layer boundary.
  </done>
</task>

</tasks>

<verification>
- Surface 1 harness (`tests/127.1-embedding-integrity.test.cjs`) is GREEN under
  `MINDRIAN_127_1_FIXTURE_MODE=1`
- The NDJSON dump has 12,401 records, each with a `namespace` field
- No migration script in this plan contains `DETACH DELETE` or any Neo4j write
  (G-1: the exporter is read-Pinecone / write-local-file only)
- The manifest fixture is checksums-only and safe to commit (Canon Part 8)
- Commit `scripts/export-pinecone-embeddings.cjs`,
  `tests/fixtures/127.1/embedding-manifest.fixture.json`, and
  `mcp-server-brain/CLAUDE.md` with `git commit --no-verify`; stage explicitly,
  no `git add -A`. The `~/.mindrian/127.1/embeddings.ndjson` dump is NOT committed.
</verification>

<success_criteria>
- All 12,401 vectors across 6 namespaces exported byte-identical (Lock 1)
- The manifest fixture flips Surface 1 GREEN with `vector_count: 12401`
- Every record carries its source namespace for the Neo4j loader
- No re-embedding occurred; the 1024-dim e5-large arrays are copied unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-01-SUMMARY.md`.
</output>
