---
phase: 127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap
plan: 04
type: execute
wave: 4
depends_on:
  - 127.1-03
files_modified:
  - mcp-server-brain/server.cjs
  - mcp-server-brain/lib/brain-ask.cjs
  - mcp-server-brain/lib/pinecone-tools.cjs
  - mcp-server-brain/package.json
  - mcp-server-brain/render.yaml
  - mcp-server-brain/.env.example
  - mcp-server-brain/CLAUDE.md
  - docs/install/BRAIN-SETUP.md
  - docs/BACKEND-MAP.md
  - docs/testers/STYLE-GUIDE.md
  - README.md
  - agents/brain-query.md
  - agents/research.md
  - references/brain/query-patterns.md
# autonomous is false -- this plan has one checkpoint:human-verify (soak review + embedding decision) before Pinecone removal
autonomous: false
gap_closure: false
re_scope_corrective: true  # corrects the stale corpus numbers (1,427 / 21K) this plan carried before the 2026-05-20 live Neo4j read; corpus is 12,401 vectors, teaching graph is 15,298 nodes / 19,713 relationships
ships_within: v1.13.0  # MILESTONE INVARIANT (HARD) -- this plan completes within the v1.13.0 milestone at v1.13.0-beta.22 (127.1-03 cuts v1.13.0-beta.21; this post-soak cleanup cuts v1.13.0-beta.22), BEFORE the v1.13.0 FINAL release gate. The v1.13.0 FINAL gate does NOT cut until 127.1-04 has shipped and verified. Authority: v1.13.1-EXECUTION-PLAN.md line 15 -- "Phase 127 + Phase 127.1 PROMOTED INTO v1.13.0 ... with v1.13.0 final cutting after both land." No part of Phase 127.1 slips to v1.13.1. The physical Pinecone retirement and the "Brain is a single Neo4j substrate" documentation claim ship in the SAME release; no shipped v1.13.0 build may carry the single-substrate doc claim without the physical Pinecone retirement.
requirements:
  - GRAPHRAG-COLLAPSE-127.1-18  # Post-cutover soak review confirms zero anomalies on the live Neo4j HNSW brain_search path before Pinecone retirement
  - GRAPHRAG-COLLAPSE-127.1-19  # Pinecone client + dependency removed from mcp-server-brain (server.cjs, brain-ask.cjs, pinecone-tools.cjs deleted, package.json @pinecone-database/pinecone removed)
  - GRAPHRAG-COLLAPSE-127.1-20  # PINECONE_API_KEY + PINECONE_INDEX env vars removed from Render service + render.yaml
  - GRAPHRAG-COLLAPSE-127.1-21  # Pinecone references stripped from server-facing docs (README, BRAIN-SETUP, STYLE-GUIDE, BACKEND-MAP, brain-query agent, research agent, query-patterns reference)
  - GRAPHRAG-COLLAPSE-127.1-22  # gsd-debugger failure taxonomy updates (closes 7 Pinecone-related failure modes per memory project_brain_mcp_three_track_transition)
user_setup:
  - service: render
    why: "Final removal of PINECONE_API_KEY + PINECONE_INDEX env vars from the live Brain service after the post-cutover soak confirms zero anomalies; reversal requires re-adding the rows via the Render Dashboard"
    env_vars: []
    dashboard_config:
      - task: "Remove PINECONE_INDEX env var from mindrian-brain service (always removed -- the index was the vector store, retired by this plan)"
        location: "Render Dashboard -> mindrian-brain -> Environment tab -> delete row"
      - task: "Remove PINECONE_API_KEY env var from mindrian-brain service (only under embedding decision B -- under decision A or C the key is retained scoped to inference.embed only)"
        location: "Render Dashboard -> mindrian-brain -> Environment tab -> delete row"

must_haves:
  truths:
    - This plan (127.1-04) completes within the v1.13.0 milestone (v1.13.0-beta.22) and the v1.13.0 FINAL release gate does NOT cut until it has shipped and verified -- the Pinecone retirement and the single-substrate doc claim ship in the same release.
    - Operator has reviewed the live Neo4j HNSW brain_search path against real traffic for at least one beta cycle and confirmed zero unexplained regressions before Pinecone is physically removed.
    - mcp-server-brain has zero remaining Pinecone vector-storage references -- `grep -rE "pinecone|Pinecone|@pinecone-database" mcp-server-brain/` returns matches only inside the inference.embed surface (decision A/C) or zero matches (decision B), and nothing outside historical changelog-style sections.
    - The Pinecone npm dependency is removed from mcp-server-brain/package.json under decision B, and a fresh `npm install` produces no @pinecone-database/* under node_modules; under decision A/C the dependency is retained ONLY for the embedding call.
    - Render env vars PINECONE_INDEX (always) and PINECONE_API_KEY (decision B) are removed from the live service AND from render.yaml.
    - Server-facing documentation no longer claims Pinecone serves brain_search and uses the corrected ground-truth figures -- 12,401 migrated vectors and 15,298 nodes / 19,713 relationships -- in README, BRAIN-SETUP.md, and BACKEND-MAP.md.
    - The Phase 127 stdio-shim memory note (project_brain_mcp_three_track_transition.md) is referenced in the SUMMARY because 7 of the 20 failure modes Pinecone created are now closed.
    - rs-external Pinecone usage (Phase 89 reverse-salient-engine -- the client-side lib/core/rs-pinecone-bridge.cjs and lib/core/rs-brain-substrate.cjs files) is EXPLICITLY OUT OF SCOPE for this plan and stays intact per CONTEXT.md "What this phase explicitly does NOT touch."
    - The generic Cypher tools brain_query + brain_schema + brain_write in lib/neo4j-tools.cjs survive untouched -- the file is not in this plan's changeset and all three tools stay registered (G-2 binding).
  artifacts:
    - path: mcp-server-brain/server.cjs
      provides: Clean server bootstrap with no Pinecone tool registration; brain_search served by the Neo4j HNSW vector tool
      contains: registerVectorSearchTool
    - path: mcp-server-brain/lib/brain-ask.cjs
      provides: Clean routing with Neo4j HNSW semantic search primary + Neo4j Cypher keyword fallback (no Pinecone searchRecords call)
      contains: searchSemanticByText
    - path: mcp-server-brain/package.json
      provides: Dependency list trimmed per the embedding decision -- @pinecone-database/pinecone removed under decision B
      contains: neo4j-driver
  key_links:
    - from: mcp-server-brain/server.cjs
      to: mcp-server-brain/lib/brain-vector-search.cjs
      via: registerVectorSearchTool call replacing the registerPineconeTools call
      pattern: "registerVectorSearchTool"
    - from: mcp-server-brain/lib/brain-ask.cjs
      to: mcp-server-brain/lib/brain-vector-search.cjs
      via: searchSemanticByText query path (the 127.1-03 deliverable) replaces the Pinecone searchRecords call
      pattern: "searchSemanticByText"
    - from: docs/install/BRAIN-SETUP.md
      to: substrate description
      via: Pinecone-as-substrate framing replaced with the Neo4j HNSW vector index description
      pattern: "Neo4j HNSW|mindrian_methodology_vec"
---

<objective>
Close out the substrate swap. Plan 127.1-03 shipped the server-side query-embedder
and the text-query path (`searchSemanticByText`) on `brain-vector-search.cjs`, and
switched the `brain_search` MCP tool's input schema to `query: z.string()`. This
plan performs the final cutover act -- it rewires the `brain_ask` natural-language
tool off Pinecone's `searchRecords` and onto the Neo4j HNSW path, retires the
Pinecone vector-storage tools (`lib/pinecone-tools.cjs` registers `brain_search` +
`brain_stats`), removes the Pinecone dependency and Render env vars per an
operator-selected embedding decision, and strips Pinecone from the server-facing
docs. After this plan the only vector substrate behind `brain_search` and
`brain_ask` is the Neo4j HNSW index `mindrian_methodology_vec`.

The plan opens with a checkpoint: the operator reviews the live Neo4j HNSW
`brain_search` path against real traffic across one beta cycle (the v1.13.0-beta.21
soak) BEFORE Pinecone is physically removed, and makes the embedding decision
(A / B / C) that scopes Task 2.

Ground truth (per CONTEXT.md "Ground truth (live read 2026-05-20)"): the migrated
semantic-search corpus is 12,401 vectors (1024-dim multilingual-e5-large) across 5
searchable namespaces (core, reference, tools, materials, graphrag; books has 7).
The Neo4j Brain teaching graph is 15,298 nodes / 19,713 relationships. The new
index `mindrian_methodology_vec` is a NEW 1024-dim HNSW index that sits ALONGSIDE
the pre-existing 384-dim entity-embedding layer (6,007 nodes / 7 HNSW indexes);
the 384-dim layer is out of scope and is never part of brain_search.

Per CONTEXT.md "What this phase explicitly does NOT touch": the client-side
rs-external Pinecone index (used by Phase 89 reverse-salient-engine for
OpenAlex/arXiv metadata at lib/core/rs-pinecone-bridge.cjs +
lib/core/rs-brain-substrate.cjs) is SEPARATE infrastructure with public-data
content and is NOT touched by this plan. v1.14.0 may revisit.

Per CONTEXT.md G-2 (binding cross-stream constraint): retiring Pinecone retires
`lib/pinecone-tools.cjs` and rewires `brain_search` onto Neo4j, but the generic
Cypher tools `brain_query` + `brain_schema` + `brain_write` in
`lib/neo4j-tools.cjs` MUST survive untouched -- a downstream command-routing
stream stands on them. This plan does not modify `lib/neo4j-tools.cjs`.

Purpose: Plan 127.1-03 left Pinecone in tree (still the live `brain_ask` path,
still a dependency) as a rollback safety net. The post-cutover soak proves the
Neo4j HNSW path is serving requests correctly. If the soak is clean, the cleanup
ships; if not, the cleanup defers to a LATER v1.13.0 beta (never to v1.13.1)
until the issue is investigated.

Output:
- mcp-server-brain trimmed: Pinecone vector-storage tools + dependency + Render
  env vars removed per the embedding decision
- brain_ask rewired onto the Neo4j HNSW path; no Pinecone searchRecords call
- Server-facing docs strip Pinecone references and carry the corrected 12,401 /
  15,298 / 19,713 ground-truth figures
- gsd-debugger failure taxonomy updated to reflect closed Pinecone-related modes
</objective>

<invariant>
## INVARIANT (v1.13.0 milestone binding) -- HARD, NON-NEGOTIABLE

Phase 127.1, INCLUDING this plan 127.1-04, completes within the v1.13.0
milestone. The v1.13.0 FINAL release gate does NOT cut until 127.1-04 has shipped
(at v1.13.0-beta.22) and verified. The Pinecone retirement and the "Brain is a
single Neo4j substrate" documentation claim ship in the SAME release; no shipped
v1.13.0 build may contain the single-substrate documentation claim without the
physical Pinecone retirement also present. No part of Phase 127.1 slips to
v1.13.1.

**Authority:** `.planning/v1.13.1-EXECUTION-PLAN.md` line 15 -- "Phase 127 +
Phase 127.1 PROMOTED INTO v1.13.0 ... with v1.13.0 final cutting after both
land." 127.1-03 cuts v1.13.0-beta.21; this post-soak cleanup (127.1-04) cuts
v1.13.0-beta.22. BOTH betas are inside the v1.13.0 milestone and BOTH land before
the v1.13.0 FINAL release gate.

**What this invariant forbids:** the divergence-log SOAK in the checkpoint task
below may NOT be treated as open-ended calendar time that lets the v1.13.0 FINAL
gate cut first. The v1.13.0 FINAL gate WAITS for the soak and for this cleanup.
The two forbidden outcomes this guards against:
  (a) shipping a v1.13.0 with the Pinecone retirement incomplete; or
  (b) shipping v1.13.0 docs that CLAIM "single substrate / Pinecone retired"
      while Pinecone is still physically in the tree -- a false claim in a
      shipped release.
Both are forbidden. If the soak needs more time, it defers WITHIN v1.13.0 (a
later beta -- e.g. v1.13.0-beta.23), never to v1.13.1.
</invariant>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-CONTEXT.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-RESEARCH.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-03-PLAN.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/deferred-items.md
@.planning/v1.13.1-EXECUTION-PLAN.md
@mcp-server-brain/server.cjs
@mcp-server-brain/lib/brain-ask.cjs
@mcp-server-brain/lib/pinecone-tools.cjs
@mcp-server-brain/package.json
@mcp-server-brain/render.yaml
@mcp-server-brain/.env.example
@docs/install/BRAIN-SETUP.md
@docs/BACKEND-MAP.md
@docs/testers/STYLE-GUIDE.md
@agents/brain-query.md
@agents/research.md
@references/brain/query-patterns.md

<interfaces>
What Plan 127.1-03 (the dependency, already re-scope-corrected) actually ships
-- this plan's cutover wiring is built against THESE deliverables, not against
the pre-127.1-03 state:

From mcp-server-brain/lib/brain-vector-search.cjs (extended by 127.1-03):
```
searchSemantic({ embedding, topK, namespace, session })  -- vector-in path (pre-existing)
searchSemanticByText({ query, topK, namespace, session }) -- TEXT-in path: embeds via
    query-embedder.cjs then calls searchSemantic; this is the entry point brain_ask
    rewires onto
registerVectorSearchTool(server)  -- registers brain_search; its MCP input schema is
    `query: z.string()` (127.1-03 switched it from `query_vector`)
INDEX_NAME = 'mindrian_methodology_vec'
EMBEDDING_DIMS = 1024
```

From mcp-server-brain/lib/query-embedder.cjs (NEW in 127.1-03):
```
embedQuery(text) -> Promise<number[1024]>  -- e5-large server-side embedding,
    applies the `query: ` prefix; rejects with a message containing
    `embedding backend unavailable` when the backend is unreachable
```

127.1-03 does NOT add a `USE_NEO4J_VECTOR` feature flag, does NOT add a
shadow-mode divergence log, and does NOT register a Pinecone fallback. The
re-scoped 127.1-03 is the query-embedding step only. Therefore THIS plan owns
the brain_ask cutover wiring outright -- there is no flag to collapse and no
divergence log file to delete. The "soak" in the checkpoint below is a review
of the LIVE brain_search Neo4j HNSW path (the 127.1-03 deliverable shipped at
v1.13.0-beta.21), not of a divergence-log artifact.

127.1-03 adds Render env vars `EMBEDDING_API_URL` + `EMBEDDING_API_KEY` (the
server-side e5-large embedding endpoint). This plan does NOT remove those --
the embedding endpoint is the query-embedding path and stays.

The grep targets for cleanup in mcp-server-brain/ (every line that must be
deleted or rewritten):

mcp-server-brain/server.cjs:
- the `require('./lib/pinecone-tools.cjs')` import line -> DELETE
- the `registerPineconeTools(server);` call inside the `/mcp` POST handler ->
  REPLACE with `registerVectorSearchTool(server);` (require it from
  `./lib/brain-vector-search.cjs`); this makes the Neo4j HNSW vector tool the
  brain_search registrant unconditionally. There is no `useNeo4jVector`
  conditional in the current server.cjs -- the on-disk file (pre-127.1-03)
  registers `registerPineconeTools` directly; this plan swaps that one call.

mcp-server-brain/lib/brain-ask.cjs:
- `const { Pinecone } = require('@pinecone-database/pinecone');` -> DELETE
- the `pineconeClient` variable and `getPinecone()` helper -> DELETE
- the `// --- Step 1: Try Pinecone semantic search ---` block (the
  `searchRecords` call) -> REPLACE with a call to `searchSemanticByText` from
  `./brain-vector-search.cjs` (require it). The routing collapses to: try Neo4j
  HNSW semantic search via `searchSemanticByText`; on empty result or an
  `embedding backend unavailable` error, fall through to the existing Neo4j
  Cypher keyword block (Step 2, which already exists in the file and is kept).
- the `source` field values: drop `'pinecone'`, `'pinecone_quota_exhausted'`,
  and `'neo4j_fallback (pinecone quota exhausted)'`. The new source strings are
  `'neo4j-hnsw'` (semantic hit) and `'neo4j_fallback (no semantic match)'`
  (Cypher keyword tier). These match the source-field vocabulary 127.1-03's
  `searchSemanticByText` path implies and the SUMMARY contract below.
- the error-block copy that names "Both Pinecone and Neo4j unavailable" and
  "wait for Pinecone billing cycle reset" -> REWRITE to name only Neo4j and the
  embedding backend.

mcp-server-brain/lib/pinecone-tools.cjs -> DELETE entire file (no longer
registered once server.cjs swaps the call).

mcp-server-brain/package.json:
- `"@pinecone-database/pinecone": "^5.1"` -> DELETE under embedding decision B;
  RETAIN under decision A/C (still needed for the embedding endpoint if that
  endpoint is the Pinecone hosted-inference family).
- the package `description` field `"wraps Neo4j and Pinecone behind API key
  auth"` -> rewrite to `"wraps Neo4j (graph + HNSW vector) behind API key auth"`.

mcp-server-brain/render.yaml:
- the `PINECONE_INDEX` env entry (`value: pws-brain`) -> DELETE always (the
  index was the vector store, retired here).
- the `PINECONE_API_KEY` env entry -> DELETE under decision B; RETAIN under
  decision A/C.
- the `EMBEDDING_API_URL` + `EMBEDDING_API_KEY` entries (added by 127.1-03) ->
  KEEP. They are the query-embedding endpoint, not Pinecone vector storage.

mcp-server-brain/.env.example:
- the `# Pinecone credentials` section (the `PINECONE_API_KEY=` +
  `PINECONE_INDEX=pws-brain` lines) -> DELETE under decision B; under decision
  A/C keep ONLY `PINECONE_API_KEY=` with a comment scoping it to the embedding
  endpoint, and always delete `PINECONE_INDEX`.

mcp-server-brain/CLAUDE.md:
- the audience line `lib/neo4j-tools.cjs + lib/pinecone-tools.cjs +
  lib/brain-ask.cjs` -> drop the `lib/pinecone-tools.cjs` term.
- the "Current Brain MCP startup tool surface" table: the `brain_search` row's
  Source column currently reads `lib/pinecone-tools.cjs` -> change to
  `lib/brain-vector-search.cjs`; the `brain_stats` row -> DELETE (Pinecone-only
  telemetry, no Neo4j equivalent shipped in 127.1). The startup tool count
  drops 6 -> 5.
- the release-time grep line that lists `lib/pinecone-tools.cjs` -> update the
  module list.

EMBEDDING DECISION (this plan's checkpoint must resolve one of three):
The query-embedding path shipped by 127.1-03 (`query-embedder.cjs`) calls a
hosted multilingual-e5-large endpoint via `EMBEDDING_API_URL` /
`EMBEDDING_API_KEY`. Whether the `@pinecone-database/pinecone` dependency can be
fully removed depends on whether that hosted endpoint is the Pinecone
hosted-inference family:
  (a) KEEP @pinecone-database/pinecone for the embedding endpoint only. The
      dependency and PINECONE_API_KEY stay; the import in brain-ask.cjs and the
      whole pinecone-tools.cjs vector-storage surface still go. Headline: "Pinecone
      retired from vector storage; hosted e5-large embedding still consumed."
  (b) DROP @pinecone-database/pinecone entirely. The 127.1-03 embedding endpoint
      is a non-Pinecone hosted e5-large service, so nothing in mcp-server-brain
      needs the Pinecone SDK. Full removal. Headline: "Pinecone fully retired."
  (c) DEFERRED. Open a v1.14.0 phase that swaps the embedding endpoint, retain
      the embedding-only Pinecone usage until that ships. Same code outcome as
      (a) for THIS plan; the difference is the SUMMARY notes a v1.14.0 follow-up.
      NOTE: decision C defers ONLY the embedding-endpoint swap to v1.14.0 -- it
      does NOT defer this plan. 127.1-04 still ships in full at v1.13.0-beta.22
      under decision C; the vector-storage Pinecone retirement is complete in
      v1.13.0 regardless of A / B / C.

This plan RECOMMENDS option (b): 127.1-03's `EMBEDDING_API_URL` is an explicit
generic embedding endpoint, so the Pinecone SDK is not needed once
`pinecone-tools.cjs` and the brain-ask `searchRecords` call are gone. The
checkpoint asks the operator to confirm against the actual endpoint configured
in Render.

Documentation cleanup targets (current Pinecone references, inventoried via
`grep -rinE "pinecone" docs/ agents/ references/ README.md`):
- docs/install/BRAIN-SETUP.md line 3: "a Neo4j + Pinecone methodology graph"
- docs/BACKEND-MAP.md line 13: "21K framework/problem/case nodes" (stale node
  count) + line 63: `consolidate-pinecone.py | Pinecone index consolidation`
- docs/testers/STYLE-GUIDE.md: grep returns ZERO Pinecone matches -- confirm in
  the SUMMARY ("no tester-facing Pinecone refs found") and make no edit if so.
- README.md: grep returns ZERO Pinecone matches -- if still zero at execution
  time, confirm in the SUMMARY and make no edit. (The file stays in
  files_modified as a defensive entry; if a future README revision adds a
  Pinecone reference before this plan runs, it gets stripped.)
- agents/brain-query.md lines 11, 15, 43: `mcp__pinecone-brain__search-records`
  allowed-tool + the fuzzy-match fallback note
- agents/research.md lines 11, 18, 47: `mcp__pinecone-brain__search-records`
  allowed-tool + the brain_search_semantic fallback note
- references/brain/query-patterns.md lines 3, 11, 172, 175: the header
  ("Cypher/Pinecone templates"), the how-to-use step 4, and Pattern #8
  (brain_search_semantic described as Pinecone-backed)

CLIENT-SIDE PINECONE USAGE -- OUT OF SCOPE (do not touch):
- lib/core/rs-pinecone-bridge.cjs (Phase 89 rs-external; OpenAlex/arXiv public
  metadata)
- lib/core/rs-brain-substrate.cjs (Phase 89 rs-external client)
- lib/memory/test-rs-pinecone-bridge.cjs (the test suite for the above)
- scripts/rs-engine.py + lib/core/rs_cache.py + lib/core/rs_corpus.py (Python
  helpers for the rs-external index)
- references/research/RESEARCH_*.md (research-historical refs; archive)
- CHANGELOG.md entries (historical; never edited by this plan)
- docs/MINDRIAN-CANON.md, .claude/includes/moat.md, root CLAUDE.md -- these
  carry stale numbers per CONTEXT.md "Open follow-up (NOT 127.1 scope)";
  correcting them is a SEPARATE doc task gated on instance-identity
  confirmation. This plan does NOT add them to scope.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Post-cutover soak review + embedding decision</name>
  <read_first>
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-03-PLAN.md (what 127.1-03 shipped: the query-embedder + searchSemanticByText + the `query: z.string()` brain_search schema)
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-CONTEXT.md (the four-lock protocol; D-04 shadow-mode note; "Ground truth (live read 2026-05-20)")
    - .planning/v1.13.1-EXECUTION-PLAN.md line 15 (the v1.13.0 promotion authority -- "v1.13.0 final cutting after both land")
    - mcp-server-brain/render.yaml (to read which embedding endpoint EMBEDDING_API_URL points at -- this decides A vs B)
  </read_first>
  <action>
    This is a blocking human-verify checkpoint, not an autonomous task. The
    operator reviews the live Neo4j HNSW brain_search path soak (see
    how-to-verify below), then states the embedding decision (A / B / C) in the
    resume signal. No files are edited by this task. Task 2 does not start until
    the operator approves; if the soak shows a regression, the plan stops here.

    MILESTONE BINDING (per the &lt;invariant&gt; block above): the v1.13.0 FINAL
    release gate WAITS for this soak and for this plan's cleanup. The soak does
    NOT get to push 127.1-04 past the v1.13.0 final gate -- the gate cannot cut
    until 127.1-04 has shipped (v1.13.0-beta.22) and verified. If the soak needs
    more time, the "defer" outcome below defers WITHIN v1.13.0 to a later beta
    (e.g. v1.13.0-beta.23). v1.13.1 is NOT an acceptable destination for this
    plan under any outcome.
  </action>
  <what-built>
    - Plan 127.1-03 shipped v1.13.0-beta.21 with the server-side query-embedder
      (`query-embedder.cjs`) and the text-query path (`searchSemanticByText`) on
      `brain-vector-search.cjs`; the `brain_search` MCP tool now takes a string.
    - The Neo4j HNSW index `mindrian_methodology_vec` (1024-dim, cosine, 12,401
      migrated vectors across 5 searchable namespaces) is live on the same Neo4j
      Aura instance as the 15,298-node / 19,713-relationship teaching graph.
    - The Render service has been running the 127.1-03 code for at least one beta
      cycle (target: 7 days minimum) so the live Neo4j HNSW brain_search path has
      served real traffic before Pinecone is physically removed.
  </what-built>
  <how-to-verify>
    1. Confirm the soak window. The 127.1-03 code (v1.13.0-beta.21) must have
       been live on Render for at least 7 calendar days. Check the Render deploy
       history for the mindrian-brain service or the v1.13.0-beta.21 tag date.
       The v1.13.0 FINAL release gate is held open until this soak completes and
       127.1-04 ships at v1.13.0-beta.22 -- the soak runs INSIDE the v1.13.0
       milestone window, not after it.

    2. Smoke-test the live Neo4j HNSW brain_search path against real query
       shapes. Run a representative set of fuzzy queries through the live
       service and confirm the response shapes are sane:
       ```
       curl -sS -X POST https://mindrian-brain.onrender.com/mcp \
         -H "Authorization: Bearer $BRAIN_API_KEY" \
         -H "Content-Type: application/json" \
         -d '{"jsonrpc":"2.0","method":"tools/call","params":{"name":"brain_search","arguments":{"query":"frameworks for navigating wicked problems"}},"id":1}'
       ```
       Run 5-10 distinct queries (framework lookup, cross-domain analogy,
       persona-aware fuzzy query). For each, confirm a non-empty ranked result
       set is returned and the top hits are plausible. Empty results on more
       than ~5% of the queries is a regression signal -- STOP this plan and
       investigate before retiring Pinecone.

    3. Confirm the non-regression harness (Lock 4) still passes. Re-run the
       20-query top-5 overlap harness from Plan 127.1-00/-03 if it is available;
       overlap must remain >= 80%. Carry the final aggregate overlap value into
       the SUMMARY as the locked baseline.

    4. Three possible outcomes (ALL outcomes resolve WITHIN the v1.13.0
       milestone -- none of them slips this plan to v1.13.1):
       a. Clean soak (no empty-result pattern, harness >= 80%, response shapes
          sane) -> approve; proceed to Task 2 (the cleanup). 127.1-04 ships at
          v1.13.0-beta.22; the v1.13.0 FINAL gate cuts after it lands.
       b. Borderline (occasional empty results 1-5%, harness 78-80%) -> approve
          with note; document the patterns in the SUMMARY; proceed with cleanup
          at v1.13.0-beta.22 but file a v1.14.0 follow-up to investigate.
       c. Regression (empty results > 5%, harness < 78%, or a clear pattern of
          bad responses) -> do NOT proceed to Task 2 yet. Either roll the Render
          service back to the pre-127.1-03 deploy and investigate, OR defer
          Plan 127.1-04 to a LATER v1.13.0 beta (e.g. v1.13.0-beta.23) and let
          the path soak longer. The v1.13.0 FINAL release gate stays HELD until
          127.1-04 ships and verifies. A deferral here is a deferral WITHIN
          v1.13.0 -- it is NEVER a deferral to v1.13.1. Shipping a v1.13.0 final
          without 127.1-04 (or with the single-substrate doc claim but Pinecone
          still in tree) is forbidden by the &lt;invariant&gt; block above.

    5. Embedding decision (gates the scope of Task 2 below). Read render.yaml to
       see what `EMBEDDING_API_URL` points at:
       - Decision B RECOMMENDED: the 127.1-03 embedding endpoint is a generic
         (non-Pinecone) hosted e5-large service -> the `@pinecone-database/pinecone`
         dependency can be fully removed; PINECONE_API_KEY + PINECONE_INDEX both
         go. Headline: "Pinecone fully retired."
       - Decision A: the embedding endpoint IS the Pinecone hosted-inference
         family -> retain `@pinecone-database/pinecone` + PINECONE_API_KEY for
         the embedding call only; still delete pinecone-tools.cjs + PINECONE_INDEX
         + the brain-ask searchRecords call. Headline: "Pinecone retired from
         vector storage."
       - Decision C: same code outcome as A for this plan, plus a SUMMARY note
         opening a v1.14.0 phase to swap the embedding endpoint and complete the
         full removal then. Decision C defers ONLY the embedding-endpoint swap
         -- it does NOT defer 127.1-04, which still ships in full at
         v1.13.0-beta.22.
  </how-to-verify>
  <acceptance_criteria>
    - The soak window is confirmed >= 7 days (recorded in the resume signal or SUMMARY).
    - At least 5 live brain_search smoke queries were run and their outcomes recorded.
    - The embedding decision (A / B / C) is stated explicitly in the resume signal.
    - If the outcome is regression, the plan is NOT resumed into Task 2; the deferral target is a LATER v1.13.0 beta (never v1.13.1) and the v1.13.0 FINAL gate stays held.
  </acceptance_criteria>
  <resume-signal>Type "approved with embedding decision A" OR "approved with embedding decision B" OR "approved with embedding decision C" OR "rollback -- soak shows regressions" OR "defer one more beta cycle (later v1.13.0 beta -- NOT v1.13.1)".</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cut brain_ask + brain_search over to Neo4j HNSW and retire Pinecone vector-storage from mcp-server-brain</name>
  <files>mcp-server-brain/server.cjs, mcp-server-brain/lib/brain-ask.cjs, mcp-server-brain/lib/pinecone-tools.cjs, mcp-server-brain/package.json, mcp-server-brain/render.yaml, mcp-server-brain/.env.example, mcp-server-brain/CLAUDE.md</files>
  <read_first>
    - mcp-server-brain/server.cjs (current state -- registers registerPineconeTools directly in the /mcp POST handler; no useNeo4jVector conditional exists)
    - mcp-server-brain/lib/brain-ask.cjs (current state -- Pinecone-first routing: Step 1 searchRecords, Step 2 Neo4j Cypher fallback)
    - mcp-server-brain/lib/brain-vector-search.cjs (the 127.1-03 deliverable -- exports searchSemanticByText and registerVectorSearchTool; do NOT modify it, only require it)
    - mcp-server-brain/lib/neo4j-tools.cjs (READ ONLY -- confirm brain_query + brain_schema + brain_write registrations exist; this plan must NOT modify this file -- G-2)
    - mcp-server-brain/lib/pinecone-tools.cjs (the full file is the deletion target)
    - mcp-server-brain/package.json (the dependency list + description field)
    - mcp-server-brain/render.yaml (the env-var list)
    - mcp-server-brain/.env.example (the env-var template)
    - mcp-server-brain/CLAUDE.md (the 6-tool startup surface table)
    - The checkpoint outcome from Task 1 (which embedding decision A/B/C was chosen)
  </read_first>
  <action>
    The scope of this task forks on the embedding decision captured in the checkpoint.

    SUB-STEP A: mcp-server-brain/server.cjs -- swap the brain_search registrant.
    Use Edit.
    Delete the import line:
    ```
    const { registerPineconeTools } = require('./lib/pinecone-tools.cjs');
    ```
    Add an import for the Neo4j HNSW vector tool:
    ```
    const { registerVectorSearchTool } = require('./lib/brain-vector-search.cjs');
    ```
    Inside the `/mcp` POST handler, replace the call:
    ```
    registerPineconeTools(server);
    ```
    with:
    ```
    // Phase 127.1 cutover complete (v1.13.0-beta.22+): brain_search is served by
    // the Neo4j HNSW vector index `mindrian_methodology_vec`. Pinecone vector
    // storage retired. rs-external (lib/core/rs-pinecone-bridge.cjs) stays
    // client-side per Phase 89 + CONTEXT 127.1 "What this phase does NOT touch".
    registerVectorSearchTool(server);
    ```
    The `registerNeo4jTools(server, ...)` and `registerBrainAsk(server)` calls
    stay byte-identical. `brain_stats` was registered only by
    `pinecone-tools.cjs` and is intentionally dropped (CONTEXT D-07 discretion;
    no Neo4j equivalent shipped in 127.1).

    SUB-STEP B: mcp-server-brain/lib/brain-ask.cjs -- rewire Step 1 onto Neo4j HNSW.
    Use Edit.
    1. Delete the require line `const { Pinecone } = require('@pinecone-database/pinecone');`.
    2. Add `const { searchSemanticByText } = require('./brain-vector-search.cjs');`.
    3. Delete the `pineconeClient` variable and the `getPinecone()` helper inside
       `registerBrainAsk`.
    4. Rewrite the doc comment block at the top of the file. The current text
       says "Try Pinecone semantic search / If quota exhausted ... fall back to
       Neo4j Cypher". Replace with:
       ```
       /**
        * brain_ask -- The smart Brain tool.
        *
        * Accepts NATURAL LANGUAGE. Returns structured results.
        * Routing (post Phase 127.1 cutover):
        *   1. Neo4j HNSW semantic search via brain-vector-search.cjs
        *      (searchSemanticByText -- embeds the query e5-large then queries
        *      the mindrian_methodology_vec index, 12,401 migrated vectors)
        *   2. On empty result or embedding-backend failure -> Neo4j Cypher
        *      keyword fallback (the 14-pattern taxonomy below)
        *   3. If both fail -> return a helpful error
        *
        * The caller never needs to write Cypher or know which tier answered.
        */
       ```
    5. Rewrite the `brain_ask` tool DESCRIPTION string. The current string says
       "Searches 23K methodology nodes and 12K semantic embeddings. Automatically
       handles Pinecone semantic search with Neo4j Cypher fallback." Replace with
       the corrected ground-truth copy:
       ```
       'Ask the Brain anything in natural language. Searches the 15,298-node teaching graph (19,713 relationships) and 12,401 methodology vectors in the Neo4j HNSW index. Automatically routes Neo4j HNSW semantic search with a Neo4j Cypher keyword fallback. Use this instead of brain_query or brain_search -- it handles routing internally.'
       ```
    6. Replace the `// --- Step 1: Try Pinecone semantic search ---` block. The
       new Step 1 is:
       ```
       // --- Step 1: Neo4j HNSW semantic search ---
       try {
         const hits = await searchSemanticByText({ query: question, topK: limit });
         if (hits && hits.length > 0) {
           source = 'neo4j-hnsw';
           results = hits;
         }
       } catch (err) {
         const msg = (err.message || '').toLowerCase();
         // embedding backend unavailable -> fall through to the Cypher tier
         if (msg.includes('embedding backend unavailable')) {
           source = 'neo4j_fallback (embedding backend unavailable)';
         }
         // any other vector-search error -> also fall through
       }
       ```
       Adapt the exact field names to whatever `searchSemanticByText` returns
       (it returns a hits array per 127.1-03); keep the result shape the
       downstream Step 3 JSON expects.
    7. The Step 2 Neo4j Cypher block stays. Update its `source` assignment: the
       current code sets `source = source === 'pinecone_quota_exhausted' ?
       'neo4j_fallback (pinecone quota exhausted)' : 'neo4j'`. Replace with:
       ```
       source = source.startsWith('neo4j_fallback')
         ? source
         : 'neo4j_fallback (no semantic match)';
       ```
       so the source strings are `neo4j-hnsw`, `neo4j_fallback (no semantic
       match)`, or `neo4j_fallback (embedding backend unavailable)`.
    8. Rewrite the both-failed error block. The current copy says "Both Pinecone
       and Neo4j unavailable" + "wait for Pinecone billing cycle reset". Replace
       with:
       ```
       error: 'Brain semantic search and Cypher fallback both unavailable',
       neo4j: 'Query error: ' + (err.message || 'unknown'),
       suggestion: 'Brain is temporarily unavailable. MindrianOS works fully without it -- all commands, Data Room, graph, personas. Try again later.',
       ```
    9. Rewrite the Step 3 `note` field. The current note mentions "Pinecone
       embedding quota exhausted". Replace the conditional note with:
       ```
       ...(source.startsWith('neo4j_fallback') ? {
         note: 'Results are from Neo4j Cypher keyword queries, not the HNSW semantic index. This happens when the semantic tier returns no match or the embedding backend is briefly unavailable.',
       } : {}),
       ```
    NO em-dashes anywhere in any modified line.

    SUB-STEP C: mcp-server-brain/lib/pinecone-tools.cjs -- delete the file.
    In the commit prep step run `git rm mcp-server-brain/lib/pinecone-tools.cjs`.
    server.cjs no longer imports it (per sub-step A).

    SUB-STEP D: mcp-server-brain/package.json -- prune.
    Use Edit. The `description` field currently names both Neo4j and Pinecone
    (it reads "wraps Neo4j and Pinecone behind API key auth"). Rewrite it to:
    `"Brain MCP server -- wraps Neo4j (graph + HNSW vector) behind API key auth"`.
    **If decision B**: delete the `"@pinecone-database/pinecone": "^5.1"` line.
    Then run `cd mcp-server-brain && rm -rf node_modules package-lock.json && npm install`
    to produce a clean dependency tree without Pinecone; if a package-lock.json
    is generated, stage it.
    **If decision A or C**: keep the `"@pinecone-database/pinecone": "^5.1"` line
    (still consumed by the embedding endpoint).

    SUB-STEP E: mcp-server-brain/render.yaml -- prune env vars.
    Use Edit. Delete the `PINECONE_INDEX` env entry (the `- key: PINECONE_INDEX`
    + its `value: pws-brain` line) -- always.
    **If decision B**: also delete the `PINECONE_API_KEY` env entry.
    **If decision A or C**: keep the `PINECONE_API_KEY` entry.
    Do NOT touch `EMBEDDING_API_URL` / `EMBEDDING_API_KEY` (the 127.1-03
    query-embedding endpoint stays) or `NEO4J_*` / `BRAIN_API_KEYS` /
    `SUPABASE_*`.

    SUB-STEP F: mcp-server-brain/.env.example -- prune.
    Use Edit. Delete the `PINECONE_INDEX=pws-brain` line -- always.
    **If decision B**: delete the whole `# Pinecone credentials` section
    (both lines).
    **If decision A or C**: keep `PINECONE_API_KEY=` and rewrite the section
    comment to `# Pinecone API key -- used only for the hosted e5-large query
    embedding endpoint; vector storage retired Phase 127.1`.

    SUB-STEP G: mcp-server-brain/CLAUDE.md -- update tool table.
    Use Edit.
    - The audience line `lib/neo4j-tools.cjs + lib/pinecone-tools.cjs +
      lib/brain-ask.cjs` -> `lib/neo4j-tools.cjs + lib/brain-vector-search.cjs +
      lib/brain-ask.cjs`.
    - In the "Current Brain MCP startup tool surface" table: change the
      `brain_search` row Source column from `lib/pinecone-tools.cjs` to
      `lib/brain-vector-search.cjs` and its Purpose column to
      `Semantic search via the Neo4j HNSW vector index (mindrian_methodology_vec, 1024-dim e5-large, 12,401 migrated vectors)`.
      DELETE the entire `brain_stats` row.
    - Change the line "Current Brain MCP startup tool surface: 6 tools." to
      "Current Brain MCP startup tool surface: 5 tools."
    - In the release-time check, update the module list from
      `lib/neo4j-tools.cjs, lib/pinecone-tools.cjs, and lib/brain-ask.cjs` to
      `lib/neo4j-tools.cjs, lib/brain-vector-search.cjs, and lib/brain-ask.cjs`.
    - Append after the last section a new section (verbatim, replace the
      `<DECISION>` placeholder with the actual A / B / C letter from the
      checkpoint):
      ```
      ## Phase 127.1 cleanup state (v1.13.0-beta.22)

      Pinecone vector storage retired. This cleanup ships at v1.13.0-beta.22,
      INSIDE the v1.13.0 milestone, before the v1.13.0 FINAL release gate.
      Changes in this directory:
      - `lib/pinecone-tools.cjs` (DELETED -- it registered brain_search + brain_stats)
      - `brain_search` is now served by `lib/brain-vector-search.cjs` (the Neo4j
        HNSW index `mindrian_methodology_vec`, 1024-dim e5-large, 12,401 migrated
        vectors across 5 searchable namespaces)
      - `brain_ask` Step 1 rewired from Pinecone `searchRecords` to the Neo4j
        HNSW `searchSemanticByText` path; the Neo4j Cypher keyword tier is kept
        as the fallback
      - `brain_stats` (Pinecone-only telemetry) RETIRED with no replacement
      - PINECONE_INDEX env var (DELETED)
      - `@pinecone-database/pinecone` dependency: embedding decision <DECISION>.

      The Neo4j teaching graph is 15,298 nodes / 19,713 relationships. The Neo4j
      HNSW index `mindrian_methodology_vec` holds 12,401 migrated vectors and
      sits ALONGSIDE the pre-existing 384-dim entity-embedding layer (6,007
      nodes / 7 HNSW indexes); the 384-dim layer is NOT part of brain_search and
      was not touched by Phase 127.1.

      The generic Cypher tools brain_query + brain_schema + brain_write in
      `lib/neo4j-tools.cjs` were NOT modified by Phase 127.1-04 (G-2 -- a
      downstream command-routing stream stands on them).

      rs-external Pinecone usage (Phase 89 reverse-salient-engine client-side,
      `lib/core/rs-pinecone-bridge.cjs`) is SEPARATE infrastructure and remains
      intact per CONTEXT 127.1. v1.14.0 may revisit.
      ```
    NO em-dashes anywhere in any modified file.

    G-3 NOTE: this plan performs no graph scan and no re-index -- it only
    rewires tool registration and routing code. The `:Command` label is
    untouched by construction (G-3 satisfied trivially; nothing here reads,
    scans, or indexes nodes).

    Render Dashboard operator action (per the user_setup section):
    1. Remove PINECONE_INDEX always; remove PINECONE_API_KEY only under decision B.
    2. The service auto-redeploys on env-var change (`autoDeploy: true`).
    3. Smoke-test the live endpoint: a `brain_ask` query should return
       `"source": "neo4j-hnsw"` for a query that has a semantic match.

    Commit message: `feat(127.1-04): cut brain_ask + brain_search to Neo4j HNSW; retire Pinecone vector-storage`.
    Stage explicitly: `git add mcp-server-brain/server.cjs mcp-server-brain/lib/brain-ask.cjs mcp-server-brain/package.json mcp-server-brain/render.yaml mcp-server-brain/.env.example mcp-server-brain/CLAUDE.md`
    + `git rm mcp-server-brain/lib/pinecone-tools.cjs`
    + (under decision B only, if generated) `git add mcp-server-brain/package-lock.json`.
    Use `git commit --no-verify`. Do NOT use `git add -A`.
  </action>
  <verify>
    <automated>node --check mcp-server-brain/server.cjs && node --check mcp-server-brain/lib/brain-ask.cjs && test ! -f mcp-server-brain/lib/pinecone-tools.cjs && ! grep -q "registerPineconeTools" mcp-server-brain/server.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check mcp-server-brain/server.cjs; echo $?` prints `0`.
    - `node --check mcp-server-brain/lib/brain-ask.cjs; echo $?` prints `0`.
    - `test ! -f mcp-server-brain/lib/pinecone-tools.cjs; echo $?` prints `0` (file deleted).
    - `grep -c "registerPineconeTools" mcp-server-brain/server.cjs` equals `0`.
    - `grep -c "registerVectorSearchTool" mcp-server-brain/server.cjs` is at least `2` (import + call).
    - `grep -c "searchSemanticByText" mcp-server-brain/lib/brain-ask.cjs` is at least `2` (require + call).
    - `grep -c "neo4j-hnsw" mcp-server-brain/lib/brain-ask.cjs` is at least `1` (the corrected source string).
    - `grep -ciE "pinecone_quota_exhausted|searchRecords|getPinecone" mcp-server-brain/lib/brain-ask.cjs` equals `0` (the Pinecone routing path is gone).
    - `grep -c "15,298" mcp-server-brain/lib/brain-ask.cjs` is at least `1` AND `grep -c "12,401" mcp-server-brain/lib/brain-ask.cjs` is at least `1` (the corrected ground-truth figures in the tool description).
    - `grep -cE "23K|12K semantic embeddings|1,?427|21,?0?0?0?" mcp-server-brain/lib/brain-ask.cjs` equals `0` (no stale corpus numbers).
    - `git diff --name-only HEAD~1 | grep -c "mcp-server-brain/lib/neo4j-tools.cjs"` equals `0` -- G-2: neo4j-tools.cjs is NOT in this plan's changeset.
    - `grep -cE "server\.tool\(\s*'brain_query'|server\.tool\(\s*'brain_schema'|server\.tool\(\s*'brain_write'" mcp-server-brain/lib/neo4j-tools.cjs` equals `3` -- G-2: the three generic Cypher tools are still registered (this is a read-only assertion on an unmodified file).
    - Under embedding decision A or C: `grep -c "@pinecone-database/pinecone" mcp-server-brain/package.json` equals `1` AND `grep -c "PINECONE_API_KEY" mcp-server-brain/render.yaml` equals `1`.
    - Under embedding decision B: `grep -c "@pinecone-database/pinecone" mcp-server-brain/package.json` equals `0` AND `grep -ciE "pinecone" mcp-server-brain/render.yaml mcp-server-brain/.env.example` equals `0`.
    - `grep -c "PINECONE_INDEX" mcp-server-brain/render.yaml mcp-server-brain/.env.example` equals `0` (always removed; the index was the vector store).
    - `grep -c "EMBEDDING_API_URL" mcp-server-brain/render.yaml` equals `1` (the 127.1-03 embedding endpoint is preserved).
    - `grep -c "Phase 127.1 cleanup state" mcp-server-brain/CLAUDE.md` is at least `1`.
    - `grep -c "v1.13.0-beta.22" mcp-server-brain/CLAUDE.md` is at least `1` (the cleanup-state section names the v1.13.0 beta it ships in).
    - `grep -c "12,401" mcp-server-brain/CLAUDE.md` is at least `1` AND `grep -c "15,298" mcp-server-brain/CLAUDE.md` is at least `1`.
    - `grep -c "5 tools" mcp-server-brain/CLAUDE.md` is at least `1` (startup surface count corrected from 6).
    - `grep -c "brain_stats" mcp-server-brain/CLAUDE.md` equals `0` in the active tool table (row removed).
    - `grep -c -- "$(printf '\xe2\x80\x94')" mcp-server-brain/server.cjs mcp-server-brain/lib/brain-ask.cjs mcp-server-brain/package.json mcp-server-brain/render.yaml mcp-server-brain/.env.example mcp-server-brain/CLAUDE.md` equals `0` (no em-dashes).
    - Existing Phase 127 + 127.1 test suites continue to pass: `bash tests/run-all-127.sh` -- the 127 shell + cjs suites green before this plan stay green; the 127.1 suites stay green.
    - Live smoke test: `curl -sS -X POST https://mindrian-brain.onrender.com/mcp -H "Authorization: Bearer $BRAIN_API_KEY" -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"tools/call","params":{"name":"brain_ask","arguments":{"question":"frameworks for navigating wicked problems"}},"id":1}' | grep -c '"source":.*"neo4j-hnsw"'` returns `>= 1`.
  </acceptance_criteria>
  <done>mcp-server-brain serves brain_search via the Neo4j HNSW index and brain_ask via the searchSemanticByText path with a Neo4j Cypher keyword fallback; lib/pinecone-tools.cjs is deleted; package.json + render.yaml + .env.example are trimmed per the embedding decision; lib/neo4j-tools.cjs is untouched and brain_query + brain_schema + brain_write stay registered (G-2); the live deploy returns source=neo4j-hnsw; existing test suites stay green; the physical Pinecone retirement is delivered inside the v1.13.0 milestone (v1.13.0-beta.22) per the &lt;invariant&gt; block; Canon Part 8 invariants intact (the Cypher fallback patterns continue to honor the question-text-never-egresses contract).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Strip Pinecone references from server-facing docs and document the closed failure modes</name>
  <files>docs/install/BRAIN-SETUP.md, docs/BACKEND-MAP.md, docs/testers/STYLE-GUIDE.md, README.md, agents/brain-query.md, agents/research.md, references/brain/query-patterns.md</files>
  <read_first>
    - docs/install/BRAIN-SETUP.md (line 3 substrate framing; grep -in "pinecone" first)
    - docs/BACKEND-MAP.md (line 13 "21K ... nodes" + line 63 consolidate-pinecone.py row)
    - docs/testers/STYLE-GUIDE.md (grep -in "pinecone" -- currently zero matches; confirm)
    - README.md (grep -in "pinecone" -- currently zero matches; confirm)
    - agents/brain-query.md (lines 11, 15, 43 -- the mcp__pinecone-brain__search-records allowed-tool + the fuzzy-match fallback note)
    - agents/research.md (lines 11, 18, 47 -- the mcp__pinecone-brain__search-records allowed-tool + the brain_search_semantic fallback note)
    - references/brain/query-patterns.md (lines 3, 11, 172, 175 -- header, how-to-use step 4, Pattern #8)
    - .planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-CONTEXT.md "Open follow-up (NOT 127.1 scope)" -- the fence: MINDRIAN-CANON.md, moat.md, root CLAUDE.md are NOT in scope
    - The memory note project_brain_mcp_three_track_transition.md (tracks 7 of 20 failure modes Pinecone created)
    - `grep -rinE "pinecone" docs/ agents/ references/ README.md` for the full live inventory
  </read_first>
  <action>
    Strip Pinecone references from the server-facing documentation. NO changes to:
    - CHANGELOG.md (historical record, never edited)
    - docs/MINDRIAN-CANON.md, .claude/includes/moat.md, root CLAUDE.md -- these
      carry stale numbers but correcting them is a SEPARATE doc task per
      CONTEXT.md "Open follow-up (NOT 127.1 scope)", gated on instance-identity
      confirmation. This plan does NOT touch them.
    - .planning/ directory beyond the ROADMAP plan-list update handled by the
      plan-phase workflow.
    - lib/core/rs-pinecone-bridge.cjs + lib/core/rs-brain-substrate.cjs
      (client-side rs-external, out of scope per CONTEXT.md)
    - lib/memory/test-rs-pinecone-bridge.cjs (test for client-side rs-external)
    - docs/research/RESEARCH_*.md (research-historical refs, archive)

    FILE-BY-FILE EDITS:

    1. docs/install/BRAIN-SETUP.md -- the user-facing Brain setup doc.
       Line 3 currently reads: "MindrianOS connects to a remote Brain (a Neo4j +
       Pinecone methodology graph) for framework chaining, semantic search, and
       orchestration."
       Replace the parenthetical with this exact corrected substrate copy:
       ```
       MindrianOS connects to a remote Brain (a unified Neo4j graph -- a 15,298-node teaching graph with 19,713 relationships, plus a Neo4j HNSW vector index `mindrian_methodology_vec` holding 12,401 methodology vectors at 1024 dimensions, cosine similarity, multilingual-e5-large embeddings) for framework chaining, semantic search, and orchestration.
       ```
       Both the teaching graph and the vector index live on the same Neo4j Aura
       instance; there is no separate vector database. If grep finds any other
       Pinecone reference or any step instructing the user to create a Pinecone
       account or supply PINECONE_API_KEY, delete it (Pinecone is a server-side
       implementation detail the user never configures).

    2. docs/BACKEND-MAP.md -- the backend mapping doc.
       Line 13 currently reads:
       `| **Brain** (remote Neo4j Aura) | READ-ONLY from plugin | Jonathan (curator) | 21K framework/problem/case nodes, teaching intelligence |`
       Replace the Content column with:
       `15,298 nodes / 19,713 relationships of framework/problem/case teaching intelligence, plus a 12,401-vector Neo4j HNSW semantic index (mindrian_methodology_vec)`
       Line 63 currently reads:
       `| `consolidate-pinecone.py` | Pinecone index consolidation (writes vectors) |`
       Replace with:
       `| `consolidate-pinecone.py` | DEPRECATED v1.13.0-beta.22 -- the Pinecone vector substrate was retired (Phase 127.1); archive only. The Neo4j HNSW loader scripts/load-embeddings-into-neo4j.cjs replaced it. |`

    3. docs/testers/STYLE-GUIDE.md -- the tester-facing style guide.
       `grep -in "pinecone" docs/testers/STYLE-GUIDE.md` currently returns zero
       matches. If it still returns zero at execution time, make NO edit and
       record "no tester-facing Pinecone refs found" in the SUMMARY. If a
       Pinecone reference has appeared, rewrite it to describe the Neo4j HNSW
       substrate.

    4. README.md -- top-level repo description.
       `grep -in "pinecone" README.md` currently returns zero matches. If it
       still returns zero at execution time, make NO edit and record "no
       Pinecone refs in README" in the SUMMARY. If a Pinecone-as-substrate
       reference has appeared, rewrite it to: "Brain is a unified Neo4j graph
       -- a 15,298-node teaching graph plus a 12,401-vector Neo4j HNSW index,
       single substrate."

    5. agents/brain-query.md -- the brain-query agent.
       Line 11: delete the allowed-tool entry `  - mcp__pinecone-brain__search-records`.
       Line 15: the Phase 95.6 D-10 comment lists `mcp__pinecone-brain__*` in the
       allowed-tools enumeration -- remove the `mcp__pinecone-brain__*` term, keep
       `mcp__mindrian-brain__*` and `mcp__neo4j-brain__*`.
       Line 43: currently reads
       `If results need semantic context, use `mcp__mindrian-brain__brain_search` (fallback: `mcp__pinecone-brain__search-records`; if Pinecone returns RESOURCE_EXHAUSTED, skip semantic search and use Neo4j Cypher queries instead) for fuzzy matching`.
       Replace with:
       `If results need semantic context, use `mcp__mindrian-brain__brain_search` (Neo4j HNSW semantic search; if it returns empty, the Brain auto-falls-back to Neo4j Cypher keyword queries) for fuzzy matching`.
       The Pattern Selection Guide table's `brain_search_semantic` cells stay --
       that is a conceptual pattern label, not a Pinecone tool name.

    6. agents/research.md -- the research agent.
       Line 11: delete the allowed-tool entry `  - mcp__pinecone-brain__search-records`.
       Line 18: the Phase 95.6 D-10 comment lists `mcp__pinecone-brain__*` -- remove
       that term, keep `mcp__mindrian-brain__*`, `mcp__neo4j-brain__*`,
       `mcp__tavily-mcp__*`.
       Line 47: currently reads
       `Run `brain_search_semantic` via `mcp__mindrian-brain__brain_search` (fallback: `mcp__pinecone-brain__search-records`; if Pinecone returns RESOURCE_EXHAUSTED, skip semantic search and use Neo4j Cypher queries instead) to find related internal knowledge in the Brain.`
       Replace with:
       `Run semantic search via `mcp__mindrian-brain__brain_search` (Neo4j HNSW; if it returns empty, the Brain auto-falls-back to Neo4j Cypher keyword queries) to find related internal knowledge in the Brain.`

    7. references/brain/query-patterns.md -- the brain query-patterns reference.
       Line 3: currently `13 named Cypher/Pinecone templates.` -> change to
       `13 named Cypher and semantic-search templates.`
       Line 11: currently `4. For `brain_search_semantic`, call `mcp__pinecone-brain__search-records` instead`
       -> change to
       `4. For `brain_search_semantic`, call `mcp__mindrian-brain__brain_search` (Neo4j HNSW vector search)`
       Pattern #8 block (lines ~170-185): replace the Purpose line, the Tool
       line, and add a substrate line so the block reads:
       ```
       ## 8. brain_search_semantic

       **Purpose:** Neo4j HNSW vector search -- not Cypher. Semantic similarity across the migrated methodology embeddings.

       ```
       Tool: mcp__mindrian-brain__brain_search
       Parameters:
         query: $search_text
         top_k: 10
         namespace: $namespace  (optional -- one of core, reference, tools, materials, graphrag)
       ```

       **Substrate:** the Neo4j HNSW vector index `mindrian_methodology_vec` (1024 dimensions, cosine similarity, multilingual-e5-large embeddings) holding 12,401 migrated methodology vectors. Post Phase 127.1 cutover (v1.13.0-beta.21+).

       **Output:** Ranked list of semantically similar items with scores and metadata.

       **Usage notes:** Use for fuzzy matching when exact node names are unknown. Combine with Cypher patterns for hybrid retrieval: semantic search finds candidates, Cypher explores their graph neighborhood.
       ```

    FAILURE-MODE TAXONOMY UPDATE:
    The gsd-debugger failure taxonomy lives as a memory artifact
    (project_brain_mcp_three_track_transition.md), not a tracked repo file.
    Document the closure in the SUMMARY rather than editing a taxonomy file.
    The memory note states the substrate swap closes 7 of the 20 failure modes.
    Acceptable closure list for the SUMMARY (closes #3, #9, #11, #15, #16, #19,
    #21 from project_brain_mcp_three_track_transition.md):
    - #3  Pinecone quota exhaustion -- the searchRecords vector path is gone;
          under decision B even the embedding surface is non-Pinecone.
    - #9  Pinecone client connection errors -- removed from the brain_ask hot path.
    - #11 Pinecone-Neo4j routing complexity -- collapsed to a single substrate.
    - #15 brain_search RESOURCE_EXHAUSTED branch -- removed.
    - #16 brain_ask Pinecone-first routing -- collapsed; Neo4j HNSW is Step 1.
    - #19 dual-substrate index drift -- impossible by construction; one substrate.
    - #21 Pinecone admin telemetry parity -- brain_stats retired; Cypher provides
          the equivalent via brain_query.

    NO em-dashes anywhere in any modified file.

    Commit message: `docs(127.1-04): strip Pinecone from server-facing docs; correct corpus figures`.
    Stage explicitly only the files actually changed:
    `git add docs/install/BRAIN-SETUP.md docs/BACKEND-MAP.md agents/brain-query.md agents/research.md references/brain/query-patterns.md`
    (add docs/testers/STYLE-GUIDE.md and README.md only if they were actually
    edited). Use `git commit --no-verify`. Do NOT use `git add -A`.
  </action>
  <verify>
    <automated>! grep -rqiE "mcp__pinecone-brain__search-records|Cypher/Pinecone templates" docs/install/BRAIN-SETUP.md docs/BACKEND-MAP.md README.md agents/brain-query.md agents/research.md references/brain/query-patterns.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Neo4j HNSW" docs/install/BRAIN-SETUP.md` is at least `1` AND `grep -c "12,401" docs/install/BRAIN-SETUP.md` is at least `1` AND `grep -c "15,298" docs/install/BRAIN-SETUP.md` is at least `1`.
    - `grep -ci "pinecone" docs/install/BRAIN-SETUP.md` equals `0`.
    - `grep -c "12,401" docs/BACKEND-MAP.md` is at least `1` AND `grep -c "15,298 nodes / 19,713 relationships" docs/BACKEND-MAP.md` is at least `1`.
    - `grep -c "DEPRECATED v1.13.0-beta.22" docs/BACKEND-MAP.md` is at least `1`.
    - `grep -c "21K framework" docs/BACKEND-MAP.md` equals `0` (stale node count gone).
    - `grep -c "Neo4j HNSW" references/brain/query-patterns.md` is at least `1` (Pattern #8 rewritten).
    - `grep -c "mindrian_methodology_vec" references/brain/query-patterns.md` is at least `1`.
    - `grep -c "mcp__pinecone-brain__search-records" agents/brain-query.md agents/research.md references/brain/query-patterns.md` equals `0`.
    - `grep -c "RESOURCE_EXHAUSTED" agents/brain-query.md agents/research.md` equals `0` (the Pinecone-quota branch wording removed).
    - `grep -ciE "1,?427|21,?000|21K nodes|8,?543" docs/install/BRAIN-SETUP.md docs/BACKEND-MAP.md README.md references/brain/query-patterns.md` equals `0` (no stale corpus or node numbers).
    - COUPLING INVARIANT: the "single substrate" / "Pinecone retired" doc claims edited here and the physical Pinecone removal performed by Task 2 are BOTH delivered by plan 127.1-04 -- the same plan, the same v1.13.0-beta.22 cut. Verify the doc edits and the server-cleanup commit are part of the same plan: `git log --oneline --grep='127.1-04' | wc -l` is at least `2` (the feat + docs commits, both 127.1-04). The doc claim is therefore NEVER false in a shipped v1.13.0 build -- the claim and the physical retirement ship together. (This is a verified invariant, not an incidental fact: a shipped v1.13.0 build carrying the single-substrate doc claim without the Task 2 physical retirement is forbidden by the &lt;invariant&gt; block.)
    - The CHANGELOG.md is UNMODIFIED by this task: `git diff --name-only HEAD~1 | grep -c "CHANGELOG.md"` equals `0` for this task's commit.
    - Client-side rs-external files unmodified: `git diff --name-only HEAD~1 | grep -cE "rs-pinecone-bridge|rs-brain-substrate|rs_cache|rs_corpus|rs-engine.py"` equals `0`.
    - Out-of-scope canon docs unmodified: `git diff --name-only HEAD~1 | grep -cE "MINDRIAN-CANON.md|includes/moat.md|^CLAUDE.md"` equals `0`.
    - `grep -c -- "$(printf '\xe2\x80\x94')" docs/install/BRAIN-SETUP.md docs/BACKEND-MAP.md agents/brain-query.md agents/research.md references/brain/query-patterns.md` equals `0` (no em-dashes).
    - The 7-failure-mode closure list is documented in the SUMMARY produced in the Output section below.
  </acceptance_criteria>
  <done>Server-facing docs no longer describe Pinecone as part of the live Brain stack and carry the corrected 12,401-vector / 15,298-node / 19,713-relationship ground truth; Pattern #8 in the query-patterns reference describes the Neo4j HNSW index; the brain-query and research agents no longer point at Pinecone for fuzzy-match fallback; the "single substrate" doc claim and the physical Pinecone retirement (Task 2) ship together in plan 127.1-04 at v1.13.0-beta.22 so the claim is never false in a shipped v1.13.0 build; the failure-mode taxonomy closure is documented in the SUMMARY; the rs-external client-side usage and the out-of-scope canon docs are left intact; CHANGELOG untouched.</done>
</task>

</tasks>

<verification>
- After this plan: `grep -rniE "pinecone" mcp-server-brain/` returns either zero
  results (decision B) or only the embedding-endpoint comment in package.json /
  render.yaml / .env.example (decision A/C) -- never a vector-storage reference.
- Server-facing docs grep clean: `grep -rlniE "pinecone serves brain_search|mcp__pinecone-brain__search-records|Neo4j \+ Pinecone" docs/install/ docs/BACKEND-MAP.md README.md agents/brain-query.md agents/research.md references/brain/query-patterns.md` returns 0.
- Zero stale corpus or node numbers: no "1,427", no "21,000", no "21K nodes",
  no "8,543" appears as a live count anywhere in the modified files.
- The live Render deploy serves brain_ask + brain_search via the Neo4j HNSW
  index `mindrian_methodology_vec`.
- v1.13.0 MILESTONE INVARIANT holds: this plan ships at v1.13.0-beta.22 inside
  the v1.13.0 milestone, before the v1.13.0 FINAL release gate; the physical
  Pinecone retirement (Task 2) and the single-substrate doc claim (Task 3) ship
  in the same plan and the same beta; no shipped v1.13.0 build carries the doc
  claim without the physical retirement; no part of Phase 127.1 slips to
  v1.13.1. Authority: v1.13.1-EXECUTION-PLAN.md line 15.
- G-2 holds: `mcp-server-brain/lib/neo4j-tools.cjs` is NOT in this plan's
  changeset and brain_query + brain_schema + brain_write stay registered.
- G-3 holds: this plan performs no graph scan or re-index, so the `:Command`
  label is untouched by construction.
- All four locks of the non-demotion protocol stay in force; the 20-query
  top-5 overlap harness baseline is carried into the SUMMARY.
- Canon Part 8 honored: the Neo4j Cypher keyword fallback continues to honor the
  question-text-never-egresses contract.
- Client-side rs-external Pinecone usage (Phase 89) untouched per CONTEXT.
- Out-of-scope canon docs (MINDRIAN-CANON.md, moat.md, root CLAUDE.md) untouched
  per CONTEXT.md "Open follow-up (NOT 127.1 scope)".
- All five requirement IDs (18, 19, 20, 21, 22) discharge to specific artifacts.
</verification>

<success_criteria>
- Post-cutover soak reviewed; clean (or borderline-with-documented-notes)
  outcome captured in the SUMMARY.
- This plan completes inside the v1.13.0 milestone (v1.13.0-beta.22), before the
  v1.13.0 FINAL release gate; the Pinecone retirement and the single-substrate
  doc claim ship in the same release; no part of Phase 127.1 slipped to v1.13.1.
- mcp-server-brain trimmed: pinecone-tools.cjs deleted; brain_ask + brain_search
  served by the Neo4j HNSW path; package.json + render.yaml + .env.example
  pruned per the embedding decision.
- 5+ server-facing docs strip Pinecone references and carry the corrected
  12,401 / 15,298 / 19,713 ground-truth figures; Pattern #8 describes Neo4j HNSW.
- 7 failure modes documented as closed in the SUMMARY.
- Live deploy returns source=neo4j-hnsw for a query with a semantic match.
- No em-dashes anywhere in modified files.
- Phase 127 + Phase 127.1 test suites all stay GREEN.
- G-2 verified: neo4j-tools.cjs untouched, brain_query + brain_schema +
  brain_write still registered.
</success_criteria>

<output>
After completion, create `.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-04-SUMMARY.md` per the standard SUMMARY template. The SUMMARY MUST include:
- Final aggregate top-5 overlap value from the Surface 3 / 20-query harness
  (carry over as the locked baseline).
- Embedding decision chosen at the checkpoint (A / B / C) with rationale.
- Post-cutover soak review findings (soak window length, number of live smoke
  queries, empty-result observations, any anomalies).
- The 7-failure-mode closure list (closes #3, #9, #11, #15, #16, #19, #21 from
  the project_brain_mcp_three_track_transition.md memory note).
- Final state confirmation: the `grep -rniE "pinecone" mcp-server-brain/ | wc -l`
  value (target: only the embedding-endpoint comment lines under decision A/C,
  exactly 0 under decision B).
- G-2 confirmation: `git diff --name-only` for the phase shows
  `mcp-server-brain/lib/neo4j-tools.cjs` is NOT in the 127.1-04 changeset, and
  brain_query + brain_schema + brain_write are still registered.
- The verbatim phase exit signal: "Neo4j single-substrate cutover verified".
  This signal is emitted WITHIN v1.13.0 -- at the v1.13.0-beta.22 cut, before
  the v1.13.0 FINAL release gate. The SUMMARY must state that the signal lands
  inside the v1.13.0 milestone, not in v1.13.1.
- Confirmation that Phase 127.1 is complete and ready for the v1.13.0-beta.22
  release cut, and that the v1.13.0 FINAL release gate may now cut (127.1-03
  shipped at v1.13.0-beta.21; this post-soak cleanup ships at v1.13.0-beta.22;
  both betas are inside the v1.13.0 milestone per the &lt;invariant&gt; block and
  v1.13.1-EXECUTION-PLAN.md line 15 -- "v1.13.0 final cutting after both land").
  The SUMMARY must explicitly state that no part of Phase 127.1 slipped to
  v1.13.1.
</output>
