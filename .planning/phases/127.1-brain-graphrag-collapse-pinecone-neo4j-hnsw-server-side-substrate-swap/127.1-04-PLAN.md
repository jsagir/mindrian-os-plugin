---
phase: 127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap
plan: 04
type: execute
wave: 3
depends_on:
  - 127.1-03
files_modified:
  - mcp-server-brain/server.cjs
  - mcp-server-brain/lib/brain-ask.cjs
  - mcp-server-brain/lib/pinecone-tools.cjs
  - mcp-server-brain/package.json
  - mcp-server-brain/render.yaml
  - mcp-server-brain/.env.example
  - mcp-server-brain/CLAUDE.md
  - docs/install/BRAIN-SETUP.md
  - docs/BACKEND-MAP.md
  - docs/testers/STYLE-GUIDE.md
  - README.md
  - agents/brain-query.md
  - agents/research.md
  - references/brain/query-patterns.md
autonomous: false  # has one checkpoint:human-verify after divergence-log review before Pinecone removal
gap_closure: false
requirements:
  - GRAPHRAG-COLLAPSE-127.1-18  # Divergence log review confirms zero anomalies across one beta cycle
  - GRAPHRAG-COLLAPSE-127.1-19  # Pinecone client + dependency removed from mcp-server-brain (server.cjs, brain-ask.cjs, pinecone-tools.cjs deleted, package.json @pinecone-database/pinecone removed)
  - GRAPHRAG-COLLAPSE-127.1-20  # PINECONE_API_KEY + PINECONE_INDEX env vars removed from Render service + render.yaml
  - GRAPHRAG-COLLAPSE-127.1-21  # Pinecone references stripped from server-facing docs (README, BRAIN-SETUP, STYLE-GUIDE, BACKEND-MAP, brain-query agent, query-patterns reference)
  - GRAPHRAG-COLLAPSE-127.1-22  # gsd-debugger failure taxonomy updates (closes 7 Pinecone-related failure modes per memory project_brain_mcp_three_track_transition)
user_setup:
  - service: render
    why: "Final removal of PINECONE_API_KEY + PINECONE_INDEX env vars from the live Brain service after divergence-log soak confirms zero anomalies; reversal requires re-adding via Dashboard"
    env_vars: []
    dashboard_config:
      - task: "Remove PINECONE_API_KEY env var from mindrian-brain service"
        location: "Render Dashboard -> mindrian-brain -> Environment tab -> delete row"
      - task: "Remove PINECONE_INDEX env var from mindrian-brain service"
        location: "Render Dashboard -> mindrian-brain -> Environment tab -> delete row"
      - task: "Remove USE_NEO4J_VECTOR env var from mindrian-brain service (no longer needed once Pinecone path is deleted -- the code unconditionally uses Neo4j HNSW)"
        location: "Render Dashboard -> mindrian-brain -> Environment tab -> delete row"

must_haves:
  truths:
    - Operator has reviewed at least 7 days of divergence log entries and confirmed zero unexplained regressions across all logged query patterns.
    - mcp-server-brain has zero remaining server-side references to Pinecone: `grep -rE "pinecone|Pinecone|@pinecone-database" mcp-server-brain/` returns zero matches outside historical changelog entries.
    - The Pinecone npm dependency is removed from mcp-server-brain/package.json and a fresh `npm install` produces no @pinecone-database/* under node_modules.
    - Render env vars PINECONE_API_KEY + PINECONE_INDEX + USE_NEO4J_VECTOR are removed from the live service AND the render.yaml.
    - Client-side documentation no longer claims Pinecone serves brain_search (README, BRAIN-SETUP.md, BACKEND-MAP.md, agents/brain-query.md, references/brain/query-patterns.md).
    - The Phase 127 stdio-shim memory note (project_brain_mcp_three_track_transition.md) is referenced in the SUMMARY because 7 of the 20 failure modes Pinecone created are now closed.
    - rs-external Pinecone usage (Phase 89 reverse-salient-engine -- the client-side rs-pinecone-bridge.cjs and rs-brain-substrate.cjs files) is EXPLICITLY OUT OF SCOPE for this plan and stays intact per CONTEXT.md "Out of scope" section.
  artifacts:
    - path: mcp-server-brain/server.cjs
      provides: Clean server bootstrap with no Pinecone imports + no USE_NEO4J_VECTOR conditional (Neo4j HNSW is unconditional)
      contains: registerVectorSearchTool
    - path: mcp-server-brain/lib/brain-ask.cjs
      provides: Clean routing with only Neo4j HNSW + Neo4j Cypher keyword fallback (no Pinecone embed call; no Pinecone search call)
      contains: searchSemantic
    - path: mcp-server-brain/package.json
      provides: Trimmed dependency list -- @pinecone-database/pinecone removed
      contains: neo4j-driver
  key_links:
    - from: mcp-server-brain/server.cjs
      to: mcp-server-brain/lib/brain-vector-search.cjs
      via: unconditional registerVectorSearchTool call
      pattern: "registerVectorSearchTool"
    - from: docs/install/BRAIN-SETUP.md
      to: substrate description
      via: Pinecone references replaced with "Neo4j HNSW vector index"
      pattern: "Neo4j HNSW|mindrian_methodology_vec"
---

<objective>
Close out the substrate swap. After one beta cycle of shadow-mode operation, this plan reviews the divergence log to confirm zero anomalies, then physically removes the Pinecone client + dependency + Render env vars + documentation references from mcp-server-brain and the plugin repo's server-facing docs. After this plan the only vector substrate behind brain_search is Neo4j HNSW.

Per CONTEXT.md "Out of scope" the client-side rs-external Pinecone index (used by Phase 89 reverse-salient-engine for OpenAlex/arXiv metadata at lib/core/rs-pinecone-bridge.cjs + lib/core/rs-brain-substrate.cjs) is SEPARATE infrastructure with public-data content and is NOT touched by this plan. v1.14.0 may revisit.

Purpose: Plan 127.1-03 left Pinecone in tree as a rollback safety net. The divergence log captured during the v1.13.0-beta.21 soak proves whether the Neo4j HNSW path is serving requests correctly. If the log is clean, the cleanup ships; if not, the cleanup defers until the issue is investigated.

Output:
- mcp-server-brain trimmed: Pinecone client + dependency + tools registration removed
- Render env vars PINECONE_API_KEY + PINECONE_INDEX + USE_NEO4J_VECTOR removed
- Server-facing docs strip Pinecone references (README, BRAIN-SETUP, STYLE-GUIDE, BACKEND-MAP, brain-query agent, query-patterns)
- gsd-debugger failure taxonomy updated to reflect closed Pinecone-related failure modes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-CONTEXT.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-RESEARCH.md
@.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-03-PLAN.md
@mcp-server-brain/server.cjs
@mcp-server-brain/lib/brain-ask.cjs
@mcp-server-brain/lib/pinecone-tools.cjs
@mcp-server-brain/package.json
@mcp-server-brain/render.yaml
@mcp-server-brain/.env.example
@docs/install/BRAIN-SETUP.md
@docs/BACKEND-MAP.md
@docs/testers/STYLE-GUIDE.md
@agents/brain-query.md
@references/brain/query-patterns.md

<interfaces>
The grep targets for cleanup (every line that must either be deleted or rewritten):

mcp-server-brain/:
- server.cjs line 8: `const { registerPineconeTools } = require('./lib/pinecone-tools.cjs');` -> DELETE
- server.cjs lines 41-46: the `useNeo4jVector` conditional -> SIMPLIFY to `registerVectorSearchTool(server);` (unconditional)
- lib/brain-ask.cjs require line 4: `const { Pinecone } = require('@pinecone-database/pinecone');` -> DELETE
- lib/brain-ask.cjs lines 100-118: `let pineconeClient`, `getPinecone()` -> DELETE
- lib/brain-ask.cjs lines from Plan 127.1-03: USE_NEO4J_VECTOR conditional + embedViaPineconeInference call -> REPLACE with the embedding call routed through a new server-side e5-large endpoint (see EMBEDDING DECISION below) OR document the deferred decision and keep using Neo4j Cypher keyword fallback when no embedding is available
- lib/brain-ask.cjs lines that fall back to Pinecone search -> DELETE the entire Pinecone fallback block; the routing collapses to: try Neo4j HNSW with embedding; on embedding failure or empty result, fall through to Neo4j Cypher keyword fallback (the existing third tier that was already in the file)
- lib/pinecone-tools.cjs -> DELETE entire file (no longer registered)
- package.json line 15: `"@pinecone-database/pinecone": "^5.1"` -> DELETE
- render.yaml lines 17-20: PINECONE_API_KEY + PINECONE_INDEX env entries -> DELETE
- .env.example lines 9-11: PINECONE_API_KEY + PINECONE_INDEX entries -> DELETE
- CLAUDE.md: the table at lines that lists brain_search/brain_stats as Pinecone-sourced -> UPDATE to mark brain_search as Neo4j HNSW + remove brain_stats row entirely (Pinecone-only tool)

EMBEDDING DECISION (this plan must pick one):
The Pinecone inference.embed() call in brain-ask.cjs is the LAST piece of Pinecone in the hot path. Two options after Plan 127.1-04:
  (a) **Keep Pinecone JUST for embedding** -- means PINECONE_API_KEY stays in Render env vars, the @pinecone-database/pinecone dep stays in package.json, the import stays in brain-ask.cjs. The "Pinecone retired" headline becomes "Pinecone retired from vector storage; embedding endpoint still consumed for Lock #2 (multilingual-e5-large) compatibility; v1.14.0 evaluates moving the embedding endpoint too."
  (b) **Drop Pinecone entirely + lose semantic embedding** -- brain_ask collapses to the keyword-Cypher fallback (Pattern #8 was the only path that used embeddings; the other 13 Cypher patterns are keyword-based and continue to work). Semantic search is GONE from brain_ask until v1.14.0 ships a replacement embedding endpoint. This is a UX regression for fuzzy queries.

This plan RECOMMENDS option (a): preserve embedding-only Pinecone usage, retire the vector-storage role only. The plan's checkpoint asks the user to confirm. If the user picks (b), this plan's task scope expands and the brain_ask UX regression is documented in the SUMMARY.

Documentation cleanup targets (grep -rln "pinecone\|Pinecone"):
- README.md (count refs; rewrite without Pinecone framing)
- docs/install/BRAIN-SETUP.md (Pinecone setup steps -> delete; substrate description -> "Neo4j HNSW vector index")
- docs/BACKEND-MAP.md line 63 (`consolidate-pinecone.py` script reference -> mark as v1.13.0 archive only)
- docs/testers/STYLE-GUIDE.md (any tester-facing copy claiming Pinecone is part of the live Brain stack -> rewrite)
- agents/brain-query.md lines 10, 43, 67, 71 (Pinecone fallback references -> remove or rewrite to point at Neo4j HNSW)
- agents/research.md lines 10, 34, 47 (brain_search_semantic + Pinecone fallback references -> remove the Pinecone-specific RESOURCE_EXHAUSTED branch; the Cypher fallback stays)
- references/brain/query-patterns.md (Pattern #8 description currently calls out brain_search_semantic as Pinecone-backed -> rewrite to describe Neo4j HNSW)

CLIENT-SIDE PINECONE USAGE OUT OF SCOPE (do not touch):
- lib/core/rs-pinecone-bridge.cjs (Phase 89 rs-external; OpenAlex/arXiv public metadata; Canon Part 8 documented in CANON-PHASE-MAP)
- lib/core/rs-brain-substrate.cjs (Phase 89 rs-external client)
- lib/memory/test-rs-pinecone-bridge.cjs (the test suite for the above)
- scripts/rs-engine.py + lib/core/rs_cache.py + lib/core/rs_corpus.py (Python helpers for the rs-external index)
- references/research/RESEARCH_*.md (research-historical refs; archive, not live stack)
- CHANGELOG.md entries (historical; never delete)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Divergence-log review + embedding decision</name>
  <what-built>
    - Plan 127.1-03 shipped v1.13.0-beta.21 with Neo4j HNSW primary + Pinecone fallback + divergence log
    - Render service has been running the cutover code for at least one beta cycle (target: 7 days minimum)
    - Divergence log at /tmp/mindrian-brain/divergence.ndjson (or the configured MINDRIAN_DIVERGENCE_LOG path) has accumulated request data
  </what-built>
  <how-to-verify>
    1. **Pull the divergence log** from Render. SSH or use Render run-command:
       ```
       curl -sS https://brain.mindrian.ai/admin/divergence-log --header "Authorization: Bearer $BRAIN_ADMIN_KEY" > /tmp/divergence-127.1.ndjson
       wc -l /tmp/divergence-127.1.ndjson
       ```
       (If the admin endpoint does not exist yet, retrieve via Render's run-command surface: `cat /tmp/mindrian-brain/divergence.ndjson | tail -1000 > /tmp/divergence-127.1.ndjson`.)
       Minimum acceptable corpus: 100 entries across at least 7 calendar days.

    2. **Analyze the log**:
       a. Count total entries: `wc -l /tmp/divergence-127.1.ndjson`. Target >= 100.
       b. Count entries by source field:
          ```
          jq -r '.neo4j_top_ids | length' /tmp/divergence-127.1.ndjson | sort | uniq -c
          ```
          Expected: most rows have length 5; rare rows with 0 length indicate "no results returned" cases which are a question class to investigate.
       c. Look for error patterns:
          ```
          jq -r 'select(.neo4j_top_ids == null or (.neo4j_top_ids | length) == 0)' /tmp/divergence-127.1.ndjson | wc -l
          ```
          If this ratio is > 5% of total entries -> the Neo4j path is regressing on real traffic; STOP this plan and investigate.
       d. Spot-check 5 random entries: pick 5 random question_sha256 hashes, manually re-run the queries against the live service, confirm the returned top_5 ids are plausible (cannot verify the specific question because the log only stores the hash per Canon Part 8; this step is a "do the response shapes look sane" smoke test).

    3. **Three possible outcomes**:
       a. **Clean log (< 1% empty results, no anomalies)** -> approve; proceed to Task 2 (the actual cleanup).
       b. **Borderline (1-5% empty results)** -> approve with note; document the empty-result patterns in the phase SUMMARY; proceed with cleanup but file a v1.14.0 follow-up to investigate the patterns.
       c. **Regression (>= 5% empty results OR clear pattern of bad responses)** -> do NOT proceed. Two options:
          - Roll back: flip USE_NEO4J_VECTOR=false on Render; investigate; re-ship Plan 127.1-03 with fixes.
          - Defer Plan 127.1-04 to a later beta; let the cutover soak longer.

    4. **Embedding decision** (gates the scope of Task 2 below):
       - Option (a) RECOMMENDED: keep Pinecone for the embedding endpoint only. Retain @pinecone-database/pinecone dep, retain PINECONE_API_KEY env var, retain the inference.embed() call site in brain-ask.cjs. Headline becomes "Pinecone retired from vector storage."
       - Option (b): drop Pinecone entirely; brain_ask loses semantic embedding until v1.14.0 ships a replacement.
       - Option (c) DEFERRED: Open a v1.14.0 phase that swaps inference.embed() for a self-hosted e5-large endpoint, then defer the full Pinecone removal until that phase ships. This plan still removes the vector-storage tools but keeps the embedding-only Pinecone usage.

  </how-to-verify>
  <resume-signal>Type "approved with embedding decision A" OR "approved with embedding decision B" OR "approved with embedding decision C" OR "rollback -- divergence log shows regressions" OR "defer one more beta cycle".</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove Pinecone vector-storage from mcp-server-brain (per embedding decision from checkpoint)</name>
  <files>mcp-server-brain/server.cjs, mcp-server-brain/lib/brain-ask.cjs, mcp-server-brain/lib/pinecone-tools.cjs, mcp-server-brain/package.json, mcp-server-brain/render.yaml, mcp-server-brain/.env.example, mcp-server-brain/CLAUDE.md</files>
  <read_first>
    - mcp-server-brain/server.cjs (current state with the Plan 127.1-03 useNeo4jVector conditional)
    - mcp-server-brain/lib/brain-ask.cjs (current state with the Plan 127.1-03 Neo4j-HNSW primary + Pinecone fallback)
    - mcp-server-brain/lib/pinecone-tools.cjs (current state with the DEPRECATED header; full file is the deletion target)
    - mcp-server-brain/package.json (the dependency list)
    - mcp-server-brain/render.yaml (the env-var list)
    - The checkpoint outcome from Task 1 (which embedding decision A/B/C was chosen)
  </read_first>
  <action>
    The scope of this task forks on the embedding decision captured in the checkpoint.

    SUB-STEP A: mcp-server-brain/server.cjs -- collapse the conditional.
    Use Edit. Find lines from Plan 127.1-03:
    ```
    // Phase 127.1 cutover: USE_NEO4J_VECTOR=true makes Neo4j HNSW the primary `brain_search` substrate.
    // Pinecone tools stay registered for one beta cycle (D-04 + RESEARCH X2 shadow-mode rollback safety).
    // When USE_NEO4J_VECTOR=false (rollback) the Neo4j tool is skipped and Pinecone serves brain_search.
    const useNeo4jVector = process.env.USE_NEO4J_VECTOR !== 'false';  // default true post-cutover
    if (useNeo4jVector) {
      registerVectorSearchTool(server);
    } else {
      registerPineconeTools(server);
    }
    ```
    Replace with the unconditional registration:
    ```
    // Phase 127.1 cutover complete (v1.13.0-beta.22+): Neo4j HNSW is the brain_search substrate.
    // Pinecone vector-storage retired; rs-external (lib/core/rs-pinecone-bridge.cjs) stays
    // client-side per Phase 89 + CONTEXT 127.1 "Out of scope".
    registerVectorSearchTool(server);
    ```
    Delete the line `const { registerPineconeTools } = require('./lib/pinecone-tools.cjs');` (Plan 127.1-03 added it; Plan 127.1-04 removes it).

    SUB-STEP B: mcp-server-brain/lib/brain-ask.cjs -- branch on embedding decision.
    Use Edit.

    **If decision was A (keep Pinecone for embedding only) or C (deferred swap)**:
      - Keep the `require('@pinecone-database/pinecone')` line.
      - Keep the `getPinecone()` helper.
      - Keep the inference.embed() call inside the Neo4j HNSW path.
      - REMOVE the entire `// --- Step 1b: Pinecone fallback ---` block (the Plan 127.1-03 fallback branch that called searchRecords). The two remaining branches become: Neo4j HNSW with Pinecone embedding -> Neo4j Cypher keyword fallback.
      - REMOVE the USE_NEO4J_VECTOR conditional. Neo4j HNSW becomes unconditional. Pinecone usage collapses from "vector storage + embedding" to "embedding only."
      - Update the source field values: drop `'pinecone_fallback (neo4j hnsw failed)'`, keep `'neo4j-hnsw'` and `'neo4j_fallback (no semantic match)'`. Add a `'pinecone_embedding'` micro-source string in the logDivergence call to make the embedding dependency observable.

    **If decision was B (drop Pinecone entirely)**:
      - DELETE the `require('@pinecone-database/pinecone')` line.
      - DELETE the `getPinecone()` helper and the `pineconeClient` variable.
      - DELETE the inference.embed() call AND the entire Neo4j HNSW try block in brain-ask.cjs (because without an embedding endpoint, the searchSemantic call cannot be invoked from brain-ask).
      - brain-ask collapses to: keyword extraction -> Cypher pattern selection -> Cypher query -> result. The same shape as the existing line-160-onward path; the file gets significantly shorter.
      - Document the UX regression in the SUMMARY: brain_ask loses fuzzy-match semantic search; the keyword-Cypher path is more brittle for poorly-worded questions.
      - searchSemantic in brain-vector-search.cjs is still exported AND registered by server.cjs as brain_search (which now requires the caller to pre-compute the embedding); brain_ask just stops calling it.

    Common to both branches:
    - Remove the `const { searchSemantic } = require('./brain-vector-search.cjs');` line only under decision B; keep under A/C.
    - Remove the `USE_NEO4J_VECTOR` env var read.

    SUB-STEP C: mcp-server-brain/lib/pinecone-tools.cjs -- delete the file.
    Run `git rm mcp-server-brain/lib/pinecone-tools.cjs` in the commit prep step. The file is no longer required; server.cjs no longer imports it (per sub-step A).

    SUB-STEP D: mcp-server-brain/package.json -- prune dependencies.
    Use Edit.
    **If decision A or C**: keep `"@pinecone-database/pinecone": "^5.1"` (still needed for inference.embed()).
    **If decision B**: delete the `"@pinecone-database/pinecone": "^5.1"` line. Run `cd mcp-server-brain && rm -rf node_modules package-lock.json && npm install` to clean install without Pinecone. Commit the resulting package-lock.json.

    SUB-STEP E: mcp-server-brain/render.yaml -- prune env vars.
    Use Edit. Remove lines 17-20 (the PINECONE_API_KEY + PINECONE_INDEX env entries).
    **If decision A or C**: re-add ONLY PINECONE_API_KEY (still needed for inference.embed); drop PINECONE_INDEX (the index is the vector store, not used for embedding).
    **If decision B**: remove both entirely.

    SUB-STEP F: mcp-server-brain/.env.example -- prune.
    Use Edit. Remove the Pinecone-specific section lines 9-11.
    **If decision A or C**: re-add only `PINECONE_API_KEY=` (with comment "Used only for inference.embed(multilingual-e5-large) until v1.14.0 ships a self-hosted embedding endpoint").
    **If decision B**: remove the whole Pinecone section.

    SUB-STEP G: mcp-server-brain/CLAUDE.md -- update tool table.
    Use Edit. Find the "Current Brain MCP startup tool surface" table. Remove the `brain_stats` row (Pinecone-only telemetry; no Neo4j equivalent shipped in 127.1). Update the `brain_search` row's Source column from `lib/pinecone-tools.cjs` to `lib/brain-vector-search.cjs`. Update the Purpose column to "Semantic search via Neo4j HNSW vector index (mindrian_methodology_vec); accepts pre-computed 1024-dim multilingual-e5-large embedding."

    Append after the existing "Phase 127.1 cutover state (active beta cycle)" section a new section:
    ```
    ## Phase 127.1 cleanup state (v1.13.0-beta.22+)

    Pinecone vector-storage retired. Removed from this directory:
    - `lib/pinecone-tools.cjs` (DELETED)
    - `@pinecone-database/pinecone` dependency (DELETED or RETAINED only for inference.embed)
    - PINECONE_INDEX env var (DELETED)
    - USE_NEO4J_VECTOR env var (DELETED -- Neo4j HNSW is unconditional)

    Embedding decision per Plan 127.1-04 checkpoint: <DECISION_A_B_OR_C>.

    rs-external Pinecone usage (Phase 89 reverse-salient-engine client-side,
    `lib/core/rs-pinecone-bridge.cjs`) is SEPARATE infrastructure and remains
    intact per CONTEXT 127.1 "Out of scope." v1.14.0 may revisit.
    ```
    NO em-dashes anywhere in any modified file.

    Render Dashboard operator action:
    1. Remove env vars per the user_setup section above (PINECONE_INDEX always; PINECONE_API_KEY only under decision B; USE_NEO4J_VECTOR always).
    2. Service auto-redeploys on env-var change.
    3. Smoke test the live endpoint: same curl as Plan 127.1-03 Task 3 sub-step E; expect `"source": "neo4j-hnsw"` for a brain_ask query.

    Commit message: `feat(127.1-04): retire Pinecone vector-storage; Neo4j HNSW unconditional`. Stage explicitly: `git add mcp-server-brain/server.cjs mcp-server-brain/lib/brain-ask.cjs mcp-server-brain/package.json mcp-server-brain/render.yaml mcp-server-brain/.env.example mcp-server-brain/CLAUDE.md` + `git rm mcp-server-brain/lib/pinecone-tools.cjs` + (under decision B only) `git add mcp-server-brain/package-lock.json`. Use `--no-verify`.
  </action>
  <verify>
    <automated>node --check mcp-server-brain/server.cjs &amp;&amp; node --check mcp-server-brain/lib/brain-ask.cjs &amp;&amp; test ! -f mcp-server-brain/lib/pinecone-tools.cjs &amp;&amp; ! grep -E "USE_NEO4J_VECTOR" mcp-server-brain/server.cjs mcp-server-brain/lib/brain-ask.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check mcp-server-brain/server.cjs; echo $?` prints `0`.
    - `node --check mcp-server-brain/lib/brain-ask.cjs; echo $?` prints `0`.
    - `test ! -f mcp-server-brain/lib/pinecone-tools.cjs; echo $?` prints `0` (file deleted).
    - `grep -c "registerPineconeTools" mcp-server-brain/server.cjs` equals `0`.
    - `grep -c "USE_NEO4J_VECTOR" mcp-server-brain/server.cjs mcp-server-brain/lib/brain-ask.cjs` equals `0`.
    - `grep -c "registerVectorSearchTool(server);" mcp-server-brain/server.cjs` >= 1 (unconditional call, not behind any `if`).
    - Under embedding decision A or C: `grep -c "@pinecone-database/pinecone" mcp-server-brain/package.json` equals `1` AND `grep -c "PINECONE_API_KEY" mcp-server-brain/render.yaml` equals `1` (kept for inference.embed).
    - Under embedding decision B: `grep -c "@pinecone-database/pinecone" mcp-server-brain/package.json` equals `0` AND `grep -c "Pinecone\|pinecone\|PINECONE" mcp-server-brain/lib/brain-ask.cjs mcp-server-brain/server.cjs mcp-server-brain/render.yaml mcp-server-brain/.env.example` equals `0`.
    - `grep -c "PINECONE_INDEX" mcp-server-brain/render.yaml mcp-server-brain/.env.example` equals `0` (always removed; index was the vector store).
    - `grep -c "Phase 127.1 cleanup state" mcp-server-brain/CLAUDE.md` >= 1.
    - `grep -c "brain_stats" mcp-server-brain/CLAUDE.md` equals `0` in the active tool table (the row removed); historical references in changelog-style sections are OK.
    - `grep -c -- "$(printf '\xe2\x80\x94')" mcp-server-brain/server.cjs mcp-server-brain/lib/brain-ask.cjs mcp-server-brain/package.json mcp-server-brain/render.yaml mcp-server-brain/.env.example mcp-server-brain/CLAUDE.md` equals `0`.
    - Existing Phase 127 test suites continue to pass: `bash tests/run-all-127.sh` -- the 127 shell + cjs suites that were green before this plan stay green; the 3 new 127.1 suites stay green.
    - Live smoke test: `curl -sS -X POST https://brain.mindrian.ai/mcp -H "Authorization: Bearer $BRAIN_API_KEY" -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"tools/call","params":{"name":"brain_ask","arguments":{"question":"frameworks for navigating wicked problems"}},"id":1}' | grep -c '"source":.*"neo4j-hnsw"'` returns `>= 1`.
  </acceptance_criteria>
  <done>mcp-server-brain no longer has any USE_NEO4J_VECTOR conditional or Pinecone vector-storage references; lib/pinecone-tools.cjs is deleted; package.json and render.yaml have been trimmed per the embedding decision; live deploy returns source=neo4j-hnsw; existing test suites stay green; Canon Part 8 invariants intact (the divergence log + Cypher fallback patterns continue to honor the question-text-never-egresses contract).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Strip Pinecone references from server-facing docs + close failure-mode taxonomy</name>
  <files>docs/install/BRAIN-SETUP.md, docs/BACKEND-MAP.md, docs/testers/STYLE-GUIDE.md, README.md, agents/brain-query.md, agents/research.md, references/brain/query-patterns.md</files>
  <read_first>
    - docs/install/BRAIN-SETUP.md (current Pinecone setup steps; grep -n "pinecone\|Pinecone" first to inventory)
    - docs/BACKEND-MAP.md (the table at line 63 referencing consolidate-pinecone.py)
    - docs/testers/STYLE-GUIDE.md (tester-facing copy that may claim Pinecone is part of the live stack)
    - README.md (top-level repo description; any reference to Pinecone-as-substrate)
    - agents/brain-query.md (lines 10, 43, 67, 71 reference Pinecone fallback for fuzzy match)
    - agents/research.md (lines 10, 34, 47 reference brain_search_semantic + Pinecone fallback)
    - references/brain/query-patterns.md (Pattern #8 fuzzy-match section describes brain_search_semantic as Pinecone-backed)
    - The memory note project_brain_mcp_three_track_transition.md (mentioned in user-memory at top; tracks 7 of 20 failure modes Pinecone created)
    - `grep -ln "pinecone\|Pinecone" docs/ agents/ references/ README.md` for the full inventory (the cleanup target list)
  </read_first>
  <action>
    Strip Pinecone references from the seven server-facing documentation files. NO changes to:
    - CHANGELOG.md (historical record, never edit)
    - .planning/ directory beyond ROADMAP update in Task 4
    - lib/core/rs-pinecone-bridge.cjs + lib/core/rs-brain-substrate.cjs (client-side rs-external, out of scope per CONTEXT)
    - lib/memory/test-rs-pinecone-bridge.cjs (test for client-side rs-external, out of scope)
    - docs/research/RESEARCH_*.md (research-historical refs, archive)

    FILE-BY-FILE EDITS:

    1. **docs/install/BRAIN-SETUP.md** -- the user-facing Brain setup doc.
       Edit any block describing the substrate to say:
       ```
       The Brain stores 21,000+ methodology nodes in Neo4j Aura and 1,427 semantic
       embeddings in a Neo4j HNSW vector index (mindrian_methodology_vec, 1024 dims,
       cosine similarity, multilingual-e5-large embeddings). Both substrates live
       on the same Neo4j Aura instance; you do not need a separate vector database.
       ```
       Delete any step that mentions creating a Pinecone account or providing PINECONE_API_KEY (under embedding decision B; under A/C the API key step is REWRITTEN to scope it to "embedding endpoint only, not vector storage").

    2. **docs/BACKEND-MAP.md** -- the backend mapping doc.
       Line 63 (`consolidate-pinecone.py | Pinecone index consolidation (writes vectors) |`):
       Replace with:
       ```
       | `consolidate-pinecone.py` | DEPRECATED v1.13.0-beta.22 -- Pinecone substrate retired; archive only. See scripts/load-embeddings-into-neo4j.cjs for the Neo4j HNSW loader that replaced this. |
       ```

    3. **docs/testers/STYLE-GUIDE.md** -- the tester-facing style guide.
       The reference at line 84 (`Mode A / Mode B / Tier 0 | "if Brain is up, more polish; if not, still works"`) is fine -- it does not claim Pinecone. Grep for `pinecone\|Pinecone` and rewrite any tester-facing copy that names Pinecone explicitly. If grep returns zero matches in tester-facing prose (only in admin sections), document this in the SUMMARY as "no tester-facing Pinecone refs found."

    4. **README.md** -- top-level repo description.
       Replace any "Brain is a Neo4j + Pinecone hybrid" framing with "Brain is a unified Neo4j graph (21K nodes + 1,427 HNSW vector embeddings, single substrate)." Preserve any historical references in a "Previously..." section if the README has one; otherwise just rewrite in place.

    5. **agents/brain-query.md** -- the brain-query agent.
       Line 10: `mcp__mindrian-brain__brain_search` is still valid (the tool name did not change; only its substrate did). Remove the fallback note that mentions `mcp__pinecone-brain__search-records`.
       Line 43: rewrite `If results need semantic context, use mcp__mindrian-brain__brain_search (fallback: mcp__pinecone-brain__search-records; if Pinecone returns RESOURCE_EXHAUSTED, skip semantic search and use Neo4j Cypher queries instead)` -> `If results need semantic context, use mcp__mindrian-brain__brain_search (Neo4j HNSW; if it returns empty, the Brain auto-falls-back to Neo4j Cypher keyword queries).`
       Lines 67, 71: the table mapping query type -> primary tool: `brain_search_semantic` references stay valid as conceptual labels; remove any Pinecone-specific column or note.

    6. **agents/research.md** -- the research agent.
       Same pattern as brain-query.md: line 10 keeps mcp__mindrian-brain__brain_search; line 47 strips the Pinecone fallback note; line 34 reference to query-patterns.md stays valid because Pattern #8 in that file is being updated in step 7.

    7. **references/brain/query-patterns.md** -- the 14-pattern reference.
       Find Pattern #8 (the fuzzy-match pattern that was Pinecone-backed). Replace the description to read:
       ```
       Pattern #8: Fuzzy-match semantic search via Neo4j HNSW.
       Tool: mcp__mindrian-brain__brain_search (post-Phase-127.1 cutover; v1.13.0-beta.21+).
       Substrate: Neo4j HNSW vector index mindrian_methodology_vec (1024 dims,
       cosine similarity, multilingual-e5-large embeddings).
       Use for: fuzzy framework lookup, cross-domain analogies, persona-aware queries
       where exact node names are unknown.
       Locks (per Phase 127.1 four-lock protocol): byte-identical embeddings from the
       pre-cutover Pinecone migration; multilingual-e5-large + 1024 dims locked
       forward; cosine similarity explicit in the index DDL.
       ```

    FAILURE-MODE TAXONOMY UPDATE:
    Search for `gsd-debugger` failure taxonomy files in .planning/ or docs/. The memory note project_brain_mcp_three_track_transition.md claims 7 of 20 failure modes are closed by the substrate swap. Document the closure in the SUMMARY rather than editing the taxonomy file directly (the taxonomy is a memory artifact, not a tracked repo file). Acceptable closure list for the SUMMARY:
    - #3 (Pinecone quota exhaustion -- no longer applicable; embedding endpoint is the only Pinecone surface left under decision A/C, embeddings have a different quota model than searchRecords)
    - #9 (Pinecone client connection errors -- removed except for inference.embed)
    - #11 (Pinecone-Neo4j routing complexity -- collapsed to single substrate)
    - #15 (brain_search RESOURCE_EXHAUSTED branch -- removed)
    - #16 (brain_ask Pinecone-first routing -- collapsed)
    - #19 (dual-substrate index drift -- impossible by construction; one substrate)
    - #21 (Pinecone admin telemetry parity -- brain_stats removed; Cypher provides the equivalent)

    NO em-dashes anywhere in any modified file.

    Commit message: `docs(127.1-04): strip Pinecone references from server-facing docs + agents`. Stage explicitly: `git add docs/install/BRAIN-SETUP.md docs/BACKEND-MAP.md docs/testers/STYLE-GUIDE.md README.md agents/brain-query.md agents/research.md references/brain/query-patterns.md`. Use `--no-verify`.
  </action>
  <verify>
    <automated>! grep -E "PINECONE_INDEX|pinecone-brain__search-records|brain is a neo4j \+ pinecone" docs/install/BRAIN-SETUP.md docs/BACKEND-MAP.md README.md agents/brain-query.md agents/research.md references/brain/query-patterns.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Neo4j HNSW\|mindrian_methodology_vec" docs/install/BRAIN-SETUP.md` >= 1.
    - `grep -c "DEPRECATED v1.13.0-beta.22" docs/BACKEND-MAP.md` >= 1.
    - `grep -c "Neo4j HNSW" references/brain/query-patterns.md` >= 1 (Pattern #8 rewritten).
    - `grep -c "pinecone-brain__search-records" agents/brain-query.md agents/research.md` equals `0` (the explicit Pinecone fallback notes removed from both agent files).
    - `grep -c "RESOURCE_EXHAUSTED" agents/brain-query.md agents/research.md` equals `0` (the Pinecone-quota branch wording removed; rephrased to "if it returns empty").
    - `grep -ciE "pinecone is part of (the )?(live )?brain" docs/install/BRAIN-SETUP.md docs/testers/STYLE-GUIDE.md README.md` equals `0` (no doc claims Pinecone is part of the live stack).
    - The CHANGELOG.md is UNMODIFIED by this task (`git diff --name-only HEAD~1 | grep CHANGELOG.md | wc -l` should equal 0 in this task's commit).
    - Client-side rs-external files unmodified: `git diff --name-only HEAD~1 | grep -E "rs-pinecone-bridge|rs-brain-substrate|rs_cache|rs_corpus|rs-engine.py" | wc -l` equals `0`.
    - `grep -c -- "$(printf '\xe2\x80\x94')" docs/install/BRAIN-SETUP.md docs/BACKEND-MAP.md docs/testers/STYLE-GUIDE.md README.md agents/brain-query.md agents/research.md references/brain/query-patterns.md` equals `0`.
    - The 7-failure-mode closure list is documented in the SUMMARY produced in the Output section below.
  </acceptance_criteria>
  <done>Server-facing docs no longer describe Pinecone as part of the live Brain stack; Pattern #8 in the query-patterns reference describes Neo4j HNSW; the brain-query and research agents no longer point at Pinecone for fuzzy-match fallback; failure-mode taxonomy update documented in SUMMARY; client-side rs-external usage left intact per CONTEXT scope; CHANGELOG untouched.</done>
</task>

</tasks>

<verification>
- After this plan: `grep -rn "pinecone\|Pinecone\|@pinecone-database" mcp-server-brain/` returns either zero results (decision B) OR only the inference.embed call sites (decision A/C).
- Server-facing docs grep clean: `grep -rln "Pinecone serves brain_search\|pinecone-brain__search-records" docs/install/ docs/testers/ README.md agents/brain-query.md agents/research.md references/brain/query-patterns.md` returns 0.
- Live deploy serves brain_ask + brain_search via Neo4j HNSW unconditionally.
- All four Validation Architecture surfaces stay GREEN (Surface 1 via Plan 127.1-02 manifest, Surface 2 via index-config fixture, Surface 3 via overlap fixtures, Surface 4 via existing MCP integration suite).
- All four locks of the non-demotion protocol stay in force.
- Canon Part 8 honored: divergence log + Cypher fallback patterns continue to honor question-text-never-egresses contract.
- Client-side rs-external Pinecone usage (Phase 89) untouched per CONTEXT.
- All five requirement IDs (18, 19, 20, 21, 22) discharge to specific artifacts.
</verification>

<success_criteria>
- Divergence log reviewed; clean (or borderline-with-documented-notes) outcome captured in SUMMARY.
- mcp-server-brain trimmed: pinecone-tools.cjs deleted; package.json + render.yaml + .env.example pruned per embedding decision; USE_NEO4J_VECTOR conditional removed.
- 7 server-facing docs strip Pinecone references; Pattern #8 in query-patterns reference describes Neo4j HNSW.
- 7 failure modes documented as closed in SUMMARY.
- Live deploy returns source=neo4j-hnsw unconditionally.
- No em-dashes anywhere in modified files.
- Phase 127 + Phase 127.1 test suites all stay GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/127.1-brain-graphrag-collapse-pinecone-neo4j-hnsw-server-side-substrate-swap/127.1-04-SUMMARY.md` per the standard SUMMARY template. The SUMMARY MUST include:
- Final aggregate top-5 overlap value from the Surface 3 harness (carry over from 127.1-03-SUMMARY.md as the locked baseline)
- Embedding decision chosen at the checkpoint (A / B / C) with rationale
- Divergence log review findings (total entries reviewed, empty-result ratio, any anomalies)
- The 7-failure-mode closure list (closes #3, #9, #11, #15, #16, #19, #21 from project_brain_mcp_three_track_transition.md memory note)
- Final state confirmation: `grep -rn "pinecone\|Pinecone" mcp-server-brain/ | wc -l` value (target <= 5 under decision A/C for the inference.embed surface; target 0 under decision B)
- Confirmation that Phase 127.1 is complete and ready for v1.13.0-beta.22 release cut
</output>
