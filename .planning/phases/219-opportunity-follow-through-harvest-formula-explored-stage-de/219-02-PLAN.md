---
phase: 219-opportunity-follow-through-harvest-formula-explored-stage-de
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/eureka/tri-modal-index.cjs
  - scripts/entity-extract.cjs
  - tests/test-219-fts5-degrade.cjs
  - tests/test-219-metadata.cjs
  - tests/helpers/fixture-room-219.cjs
autonomous: true
requirements: [REQ-5, REQ-7]
must_haves:
  truths:
    - "On a node build without FTS5, the eureka index/retrieval degrades to bi-modal (vector + graph) with honest provenance, never a 'no such module: fts5' crash"
    - "After extraction, artifact nodes carry frontmatter properties (methodology, date, status, section, confidence) queryable via graph_query"
    - "The extractor remains zero-egress and zero-LLM (deterministic parse)"
    - "runExtraction supports scoped-incremental extraction over an explicit path allowlist (the D-16 post-filing seam)"
    - "A fixture room exists that MODELS accumulated hub skew (high-degree artifact hubs vs low-degree entity family) - the fixture 218 lacked"
  artifacts:
    - path: "lib/core/eureka/tri-modal-index.cjs"
      provides: "ensureFtsAvailable capability probe + bi-modal degrade (clone of the vec0 probe, quick 260706-5b7)"
      contains: "ensureFtsAvailable"
    - path: "scripts/entity-extract.cjs"
      provides: "frontmatter metadata pass inside the D-05 batch + opts.paths scoped extraction"
      contains: "parseFrontmatter"
    - path: "tests/test-219-fts5-degrade.cjs"
      provides: "forced-absent probe path asserting bi-modal results + honest provenance + exit 0"
    - path: "tests/test-219-metadata.cjs"
      provides: "REQ-5 frontmatter props + zero-egress test"
    - path: "tests/helpers/fixture-room-219.cjs"
      provides: "buildFixtureRoom(tmpDir) seeding bridge + contradiction + hub skew + zero-connection isolate"
      exports: ["buildFixtureRoom"]
  key_links:
    - from: "lib/core/eureka/tri-modal-index.cjs"
      to: "node:sqlite DatabaseSync"
      via: "CREATE VIRTUAL TABLE probe on a :memory: handle, verdict cached per process"
      pattern: "fts5"
    - from: "scripts/entity-extract.cjs"
      to: "lib/core/opportunity-ops.cjs"
      via: "parseFrontmatter (shipped zero-dep parser, Part 7)"
      pattern: "parseFrontmatter"
---

<objective>
Two Wave-1 substrate slices plus the fixture countermeasure:

1. REQ-7 unblocker (live field finding, 2026-07-13): the eureka runner crashed on the navigator's Windows machine with SQLite "no such module: fts5" at tri-modal index open (tri-modal-index.cjs:194 creates the eureka_fts virtual table unconditionally). REQ-7's release gate requires a navigator-run corepower-isolation validation on THAT machine - without a graceful degrade the release gate cannot pass. Clone the shipped vec0 capability-probe pattern (vector-store.cjs:73-98, quick 260706-5b7): probe, degrade honestly to bi-modal, never crash. New risk logged: cross-platform SQLite feature variance (tri-polar rule - every feature works on all three surfaces).

2. REQ-5 metadata thin slice (D-11): artifact frontmatter becomes typed graph data - deterministic, zero-LLM, inside 218's D-05 batch transaction - enriching the Harvest Formula's Connection factor. Also mints the opts.paths scoped-extraction seam that D-16's post-filing extraction (Plan 05) consumes.

3. The fixture room builder that models accumulated hub skew - the exact thing 218's fixture lacked when fixture-green lied twice (R1).

Output: FTS5 probe + degrade, metadata pass, scoped-extraction seam, hub-skew fixture builder, two Wave 0 tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-SPEC.md
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-RESEARCH.md
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-PATTERNS.md
@lib/core/eureka/vector-store.cjs
@lib/core/eureka/tri-modal-index.cjs
@scripts/entity-extract.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FTS5 capability probe + bi-modal degrade (the Windows crash fix)</name>
  <files>lib/core/eureka/tri-modal-index.cjs, tests/test-219-fts5-degrade.cjs</files>
  <read_first>
    - lib/core/eureka/vector-store.cjs (:73-120 ensureVecLoaded - the vec0 capability probe precedent from quick 260706-5b7: probe, cache verdict, never throw; :252-330 createVecTable - backend selection driven by the probe, not by hope; the honest-provenance backend naming)
    - lib/core/eureka/tri-modal-index.cjs (:185-200 - the unconditional CREATE VIRTUAL TABLE eureka_fts USING fts5 at :194 that crashed; :259+ MATCH escaping; the lexical-leg query path)
  </read_first>
  <behavior>
    - Test 1: with the probe forced absent (MINDRIAN_FORCE_FTS_ABSENT=1), index creation completes without throwing and skips the eureka_fts DDL
    - Test 2: forced-absent retrieval returns bi-modal results (vector + graph legs contribute; lexical leg contributes zero) and the provenance/report field reads fts_backend 'absent (bi-modal degrade)'
    - Test 3: probe-available path (default on this dev machine) is byte-identical tri-modal behavior with fts_backend 'fts5'
    - Test 4: the probe itself never throws - a throwing CREATE VIRTUAL TABLE is caught and cached as { ok:false, detail }
  </behavior>
  <action>
    Add ensureFtsAvailable() to tri-modal-index.cjs, cloning the ensureVecLoaded shape: attempt CREATE VIRTUAL TABLE t USING fts5(x) on a FRESH :memory: DatabaseSync (new instance so no handle pollution), cache the { ok, detail } verdict at module level per process, never throw. Honor MINDRIAN_FORCE_FTS_ABSENT=1 as the test seam forcing ok:false (mirrors how the vec0 probe is exercised). Gate BOTH seams on the verdict: (a) index creation skips the eureka_fts DDL when absent instead of throwing; (b) the lexical query leg returns an empty contribution when absent, so retrieval degrades to bi-modal (vector + graph). Surface the verdict in the index/report provenance as fts_backend: 'fts5' | 'absent (bi-modal degrade)' - honest degrade, the vec0/lexical-fallback discipline. Write tests/test-219-fts5-degrade.cjs FIRST (RED) covering the behavior list against a temp room; then implement to GREEN. Do not touch the MATCH-escaping or ranking logic. CJS only, no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-219-fts5-degrade.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-219-fts5-degrade.cjs exits 0 (forced-absent: no throw, bi-modal results, degrade provenance string present; default path: tri-modal unchanged)
    - MINDRIAN_FORCE_FTS_ABSENT=1 node tests/test-219-fts5-degrade.cjs also exits 0 end-to-end (the whole test file runs under forced absence without a single fts5 error)
    - grep -n "ensureFtsAvailable" lib/core/eureka/tri-modal-index.cjs shows the probe gating both the DDL and the query leg
  </acceptance_criteria>
  <done>No machine can crash on missing FTS5; degrade is honest and test-pinned; the corepower Windows run is unblocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontmatter metadata pass in entity-extract.cjs + scoped-extraction seam</name>
  <files>scripts/entity-extract.cjs, tests/test-219-metadata.cjs</files>
  <read_first>
    - scripts/entity-extract.cjs (:173-225 collectArtifacts; :227-303 runExtraction with the D-05 BEGIN/COMMIT/ROLLBACK at :245-287; :319-348 open/work/close via openRoomDb in try/finally)
    - lib/core/opportunity-ops.cjs (:24 parseFrontmatter - the shipped zero-dep parser to reuse, Part 7)
    - .planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-02-PLAN.md (the D-05 SQLite write-safety pattern this pass must follow)
  </read_first>
  <behavior>
    - Test 1: extraction on a temp room whose artifact carries frontmatter (methodology, created, status, section, confidence) leaves those fields as JSON props on the memory_artifact node, queryable by reading the node's props
    - Test 2: an artifact without frontmatter is untouched (no prop churn, no error)
    - Test 3: malformed frontmatter never throws - the artifact is skipped with the pass continuing (batch survives)
    - Test 4: existing node props (218 entity data, review_status) are NOT clobbered - the metadata write is a merge UPSERT
    - Test 5: runExtraction with opts.paths = [one artifact path] touches ONLY that artifact's node (scoped incremental - the D-16 seam)
  </behavior>
  <action>
    Extend runExtraction per D-11: inside the SAME existing D-05 batch transaction, for each collected artifact, parse frontmatter via opportunity-ops.parseFrontmatter (reuse - never a new YAML parser) and write methodology/created-or-date/status/section/confidence as additive JSON props on that memory_artifact node. Merge discipline (assumption A5): read the node's current props, merge the metadata keys, write back through the navigation/insertNode UPSERT path - never clobber existing props, never touch review_status, never add DDL columns. Values are JSON scalars only; a serialize failure skips that artifact with a counted warning, never a throw (frontmatter-injection threat). Zero-LLM, zero network. Add an opts.paths allowlist parameter to runExtraction (and thread it through collectArtifacts): when present, only artifacts whose path is in the allowlist are collected - the scoped-incremental seam D-16's post-filing extraction (Plan 05) invokes so filing one explore-chain artifact never triggers a full-room re-extract. Default behavior (no opts.paths) is byte-identical to today. Write tests/test-219-metadata.cjs FIRST (RED) covering the behavior list, then implement to GREEN.
  </action>
  <verify>
    <automated>node tests/test-219-metadata.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-219-metadata.cjs exits 0 (props present, merge-safe, malformed-safe, scoped opts.paths honored)
    - bash tests/run-all-219.sh stays green (metadata leg flips from SKIP to PASS; 218 regression leg unaffected - the default path is unchanged)
    - Zero-egress grep gate in run-all-219.sh passes over the modified entity-extract.cjs
  </acceptance_criteria>
  <done>Frontmatter is typed graph data inside the D-05 batch; scoped extraction available for D-16; extractor still deterministic and zero-egress.</done>
</task>

<task type="auto">
  <name>Task 3: Fixture room builder modeling accumulated hub skew (the 218 R1 countermeasure)</name>
  <files>tests/helpers/fixture-room-219.cjs</files>
  <read_first>
    - .planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-VERIFICATION.md (the fixture-green-lied root cause: validated_demand degree-centrality buries low-degree node families under artifact hubs)
    - lib/core/navigation/typed-entity.cjs (writeEntityNode - seed entities the governed way)
    - lib/core/navigation/edges.cjs (edge types available for seeding: CONTRADICTS, SUPPORTS, DERIVED_FROM, RELATED_TO)
    - tests/test-218-noise-reduction.cjs (the 218 fixture-seeding idiom to improve on)
  </read_first>
  <action>
    Create tests/helpers/fixture-room-219.cjs exporting buildFixtureRoom(tmpDir) returning { roomDir, dbPath, ids }. Seed via navigation writers only (never raw SQL): (a) HUB SKEW - three memory_artifact hub nodes each carrying 10-20 edges (the accumulated-skew profile 218's fixture lacked); (b) a low-degree entity family - company/technology/market nodes with 1-2 edges each via writeEntityNode; (c) a KNOWN BRIDGE - a cross-section typed edge between two low-degree entities in different sections (the surprise/intersection candidate the harvest sensor must rank ABOVE artifact-vs-artifact pairs); (d) a KNOWN CONTRADICTION - a CONTRADICTS edge pair (the Challenging Orthodoxies lens candidate); (e) a ZERO-CONNECTION isolate - one node with no typed links (must be SUPPRESSED by the sensor: one dot is noise); (f) minimal room scaffolding (ROOM.md, .mindrian/ dir) so producers can write side-channels. Return stable ids for every planted feature so tests assert against exact expectations. Deterministic - no randomness, no network, no LLM.
  </action>
  <verify>
    <automated>node -e "const {buildFixtureRoom}=require('./tests/helpers/fixture-room-219.cjs'); const os=require('os'),fs=require('fs'),path=require('path'); const d=fs.mkdtempSync(path.join(os.tmpdir(),'f219-')); const r=buildFixtureRoom(d); if(!r||!r.ids||!r.ids.bridgeA||!r.ids.contradictionA||!r.ids.isolate) process.exit(1); console.log('fixture ok');"</automated>
  </verify>
  <acceptance_criteria>
    - The smoke command prints "fixture ok" and exits 0
    - The seeded room contains the planted bridge, contradiction pair, isolate, hub-skew artifacts, and low-degree entity family with the exact ids returned
    - grep gate: no raw INSERT INTO in the fixture builder (all seeding through navigation writers)
  </acceptance_criteria>
  <done>A fixture that can no longer lie the way 218's did; Plans 03/04 build their unit tests on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact frontmatter -> node props | untrusted user-authored YAML crosses into room.db props |
| eureka index -> platform SQLite | feature variance across CLI/Desktop/Windows builds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-05 | Tampering | frontmatter injection into props | mitigate | deterministic zero-LLM parseFrontmatter; JSON-scalar props only; serialize failure skips artifact, never throws; merge UPSERT never touches review_status |
| T-219-06 | Denial of Service | fts5-missing crash on Windows (blocks REQ-7) | mitigate | ensureFtsAvailable probe + bi-modal degrade + honest provenance; test-pinned forced-absent path |
| T-219-07 | Information Disclosure | extractor egress | mitigate | zero-network pass; run-all-219.sh grep gate over entity-extract.cjs |
| T-219-08 | Tampering | fixture bypassing navigation (untrustworthy tests) | mitigate | fixture seeds via navigation writers only; grep gate |
| T-219-SC | Tampering | package installs | accept (n/a) | zero installs this phase |
</threat_model>

<verification>
- node tests/test-219-fts5-degrade.cjs and node tests/test-219-metadata.cjs green
- bash tests/run-all-219.sh green (fts5 + metadata legs PASS, later legs SKIP)
- MINDRIAN_FORCE_FTS_ABSENT=1 bash-level smoke of the eureka index open path: no throw
</verification>

<success_criteria>
- SPEC acceptance 5 satisfied at fixture level (frontmatter props queryable, zero-egress; live proof on ador in Plan 06)
- REQ-7's Windows blocker removed: eureka/harvest can complete on an FTS5-less node build in bi-modal degrade
- buildFixtureRoom available for Plans 03/04 unit tests
</success_criteria>

## Artifacts this plan produces

- Functions: `ensureFtsAvailable()` (lib/core/eureka/tri-modal-index.cjs), `runExtraction(db, roomDir, sessionId, maxPerArtifact, opts)` with new `opts.paths` allowlist (scripts/entity-extract.cjs), `buildFixtureRoom(tmpDir)` (tests/helpers/fixture-room-219.cjs)
- Provenance vocabulary: `fts_backend: 'fts5' | 'absent (bi-modal degrade)'`
- Node prop keys on memory_artifact: methodology, created/date, status, section, confidence
- Env vars: `MINDRIAN_FORCE_FTS_ABSENT` (test seam, forces probe false)
- Test files: tests/test-219-fts5-degrade.cjs, tests/test-219-metadata.cjs, tests/helpers/fixture-room-219.cjs
- Risk register addition: R-new cross-platform SQLite feature variance (tri-polar rule)

<output>
Create `.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-02-SUMMARY.md` when done
</output>
