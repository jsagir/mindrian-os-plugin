---
phase: 219-opportunity-follow-through-harvest-formula-explored-stage-de
plan: 07
type: execute
wave: 5
depends_on: ["219-06"]
files_modified:
  - .planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-COREPOWER-VALIDATION-PROMPT.md
  - .planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-RELEASE-STAGING.md
  - .planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-VERIFICATION.md
autonomous: false
requirements: [REQ-7]
must_haves:
  truths:
    - "Corepower-isolation validation is navigator-run on the Desktop surface and explicitly confirmed, recorded in 219-VERIFICATION.md (D-13)"
    - "The corepower run uses the PRODUCTION /mos:eureka engine, never the manual baseline (closes the open session-memory item)"
    - "The eureka/harvest pass completes on the Windows machine - bi-modal degrade acceptable, NO fts5 crash (the Plan 02 fix proven live)"
    - "All release gates are green and STAGED; scripts/release.sh does NOT execute in this phase - the version cut is JOINT with Phases 220+221 (navigator decision)"
    - "README/marketplace/website updates are content-drafted with styling preserved (Feynman + JTBD), ready to apply at cut time (D-15)"
  artifacts:
    - path: ".planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-COREPOWER-VALIDATION-PROMPT.md"
      provides: "paste-ready Desktop validation prompt + pass/fail checklist (D-13)"
      contains: "/mos:eureka"
    - path: ".planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-RELEASE-STAGING.md"
      provides: "staged CHANGELOG entry + README content refresh + marketplace pin instruction + website fact-check + VERSION-BUMP-CHECKLIST + the Phase 220 handoff note"
      contains: "release.sh"
    - path: ".planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-VERIFICATION.md"
      provides: "corepower confirmation record completing the phase verification doc"
      contains: "corepower"
  key_links:
    - from: "219-COREPOWER-VALIDATION-PROMPT.md"
      to: "219-VERIFICATION.md"
      via: "navigator confirmation recorded BEFORE the (deferred) release gate opens"
      pattern: "confirmed"
---

<objective>
Implement REQ-7 as RELEASE-READINESS (navigator adjustment, 2026-07-13): the version cut is now JOINT across Phase 219 + Phase 220 + Phase 221 (web-ingestion-agent). This plan delivers everything the cut needs EXCEPT the cut itself: corepower-isolation validation recorded, all gates green, VERSION-BUMP-CHECKLIST prepared, README + marketplace + website fact-check STAGED (content drafted, styling preserved), and an explicit handoff note that scripts/release.sh <version> executes when Phase 221 completes (the last phase in the joint set). scripts/release.sh is NOT run here.

Implements D-13 (corepower validation is a NAVIGATOR-run Desktop step with a paste-ready prompt; confirmation recorded in 219-VERIFICATION.md before the release gate opens; closes the open session-memory item to re-run eureka on corepower post-218), D-14 (release only ever via release.sh five-gate lockstep - staged, deferred to the joint cut; never hand-bump), and D-15 (marketplace source.ref pin + README content refresh with EXISTING styling - staged, content updated, never restyled).

Purpose: Phase 219 closes fully validated and cut-ready; Phase 220 inherits a zero-friction release.
Output: validation prompt, navigator confirmation record, release staging doc with the handoff note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-SPEC.md
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-VERIFICATION.md
@.claude/includes/release-process.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Paste-ready corepower-isolation validation prompt + checklist (D-13)</name>
  <files>.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-COREPOWER-VALIDATION-PROMPT.md</files>
  <read_first>
    - .planning/phases/219-.../219-VERIFICATION.md (the live ador evidence - the corepower checklist mirrors what was proven there)
    - lib/core/eureka/tri-modal-index.cjs (the Plan 02 fts_backend provenance strings the checklist asserts)
    - .planning/phases/219-.../219-VALIDATION.md "Manual-Only Verifications" row 2 (the corepower contract)
  </read_first>
  <action>
    Write the paste-ready Desktop prompt the navigator runs verbatim in the corepower-isolation room. It MUST direct: (1) run the PRODUCTION eureka path - /mos:eureka against the room - NEVER a manual-baseline reconstruction (this closes the open session-memory item: the previous corepower eureka run used zero of the shipped 211-216 engine); (2) run the extraction + opportunity pass (entity extraction + metadata, then harvest); (3) qualify one candidate at the card if one surfaces. Include a pass/fail CHECKLIST the navigator ticks: eureka completes WITHOUT an fts5 crash on the Windows machine (bi-modal degrade is acceptable - the report/provenance line reads fts_backend 'absent (bi-modal degrade)' or 'fts5', either is PASS; a "no such module: fts5" error is FAIL); proposed opportunity nodes exist post-run; harvest candidates carry Four-Lens labels; the card renders as a real card, not an ASCII box. Include the exact expected-output snippets so the navigator pattern-matches without interpreting internals (Feynman-plain instructions, no jargon, no em-dashes). Close with: paste results back; confirmation is recorded in 219-VERIFICATION.md and gates the joint 219+220+221 release.
  </action>
  <verify>
    <automated>test -s .planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-COREPOWER-VALIDATION-PROMPT.md && grep -q "mos:eureka" .planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-COREPOWER-VALIDATION-PROMPT.md && grep -q "bi-modal" .planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-COREPOWER-VALIDATION-PROMPT.md && echo prompt-ok</automated>
  </verify>
  <acceptance_criteria>
    - The prompt file exists, directs the production /mos:eureka path explicitly, and contains the FTS5 pass/fail rule with both acceptable provenance strings
    - The checklist covers: no fts5 crash, proposed opportunity nodes, lens-labeled candidates, real card render
    - Instructions are runnable by the navigator without repo knowledge (paste-ready)
  </acceptance_criteria>
  <done>The navigator has a verbatim-runnable Desktop validation script with unambiguous pass/fail lines.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Navigator runs corepower validation on Desktop; confirmation recorded</name>
  <action>Hand the navigator the Task 1 prompt file, pause execution (blocking gate; never auto-approvable), then on confirmation record the pasted results + checklist ticks verbatim in the 219-VERIFICATION.md Corepower Validation section and mark the open post-218 eureka re-run memory item closed. On a FAIL, route the defect to the owning plan before release staging proceeds.</action>
  <what-built>The paste-ready validation prompt from Task 1. The Windows FTS5 crash is fixed (Plan 02, bi-modal degrade), the pipeline is ador-proven (Plan 06), and the corepower run is the last gate before release staging closes.</what-built>
  <how-to-verify>
    1. Open the corepower-isolation room in Claude Desktop on the Windows machine.
    2. Paste .planning/phases/219-.../219-COREPOWER-VALIDATION-PROMPT.md verbatim and run it.
    3. Tick the checklist: /mos:eureka completes (no "no such module: fts5" error; the fts_backend line may honestly read bi-modal degrade), opportunity nodes minted, candidates carry lens labels, the card is a real card.
    4. Paste the results back here.
  </how-to-verify>
  <resume-signal>Paste the corepower results and type "confirmed" - the confirmation + pasted evidence get recorded verbatim in 219-VERIFICATION.md (Corepower Validation section), closing the open post-218 eureka re-run memory item. If anything fails, describe it: an fts5 crash or engine failure routes back to the owning plan before release staging proceeds.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Release staging - everything but the cut (joint 219+220+221 handoff)</name>
  <files>.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-RELEASE-STAGING.md, .planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-VERIFICATION.md</files>
  <read_first>
    - .claude/includes/release-process.md (the five-gate lockstep release.sh enforces; the two-command user upgrade path)
    - CHANGELOG.md (top entries - the entry format to draft in; current line 1.15.3-beta.15)
    - README.md (the EXISTING Feynman + JTBD structure - content refresh only, zero restyle, D-15)
  </read_first>
  <action>
    Run the readiness gate sweep once more and record it (bash tests/run-all-219.sh, node scripts/doctor.cjs --acceptance, node scripts/build-connector-registry.cjs --check, scripts/verify-release as a consistency pre-check on the CURRENT version - expected green with no pending bump). Then write 219-RELEASE-STAGING.md containing, fully drafted and copy-applicable at cut time: (1) the CHANGELOG entry draft for the joint 219+220+221 release (219 content: opportunity follow-through lifecycle, harvest sensor SENS-14, qualification card, explore chain + research corpus, metadata slice, FTS5 bi-modal degrade; leave a marked slot for 220's web-ingestion-agent content); (2) the README.md content-refresh diff describing the new opportunity-follow-through capability in the EXISTING Feynman + JTBD voice and structure (content updated, never restyled - navigator-confirmable diff per SPEC acceptance); (3) the marketplace instruction: ~/mindrian-marketplace/.claude-plugin/marketplace.json source.ref pinned to the new tag (lockstep gate 5, D-15); (4) the website hand-typed-version fact-check checklist (the VERSION-BUMP discipline: every hand-typed version surface on mindrian-os.com reconciled after the bump); (5) the HANDOFF NOTE, verbatim and unambiguous: "The 219+220+221 version cut is JOINT (navigator decision 2026-07-13). scripts/release.sh <version> executes when Phase 221 completes (the last phase in the joint set) - default next increment on the 1.15.3-beta line unless the navigator directs otherwise (D-14). Never hand-bump; release.sh enforces all five gates + npm publish (step 9.5). Preconditions already met by Phase 219: corepower validation confirmed (219-VERIFICATION.md), all 219 gates green, this staging doc applies verbatim." Do NOT run scripts/release.sh; do NOT modify package.json/plugin.json/CHANGELOG.md/README.md in this task - everything lands as staged drafts. Finally, complete 219-VERIFICATION.md: corepower confirmation recorded (from Task 2), readiness sweep table, pointer to the staging doc.
  </action>
  <verify>
    <automated>bash tests/run-all-219.sh && node scripts/doctor.cjs --acceptance && grep -q "release.sh" .planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-RELEASE-STAGING.md && grep -q "corepower" .planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-VERIFICATION.md && git diff --exit-code package.json .claude-plugin/plugin.json CHANGELOG.md README.md && echo staging-ok</automated>
  </verify>
  <acceptance_criteria>
    - 219-RELEASE-STAGING.md contains all five staged sections including the verbatim handoff note naming release.sh and the joint-cut condition
    - git diff --exit-code proves package.json, plugin.json, CHANGELOG.md, README.md are UNTOUCHED (no hand-bump, no premature docs - the cut belongs to Phase 221 completion)
    - 219-VERIFICATION.md is complete: offline gates, live ador evidence, navigator card/explore checkpoint, corepower confirmation, readiness sweep
    - run-all-219.sh + doctor --acceptance green at close
  </acceptance_criteria>
  <done>Phase 219 is cut-ready with zero friction left: validation recorded, drafts staged, handoff explicit, nothing prematurely bumped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged release artifacts -> the joint cut | drift between staging and execution is a silent version mismatch |
| Desktop navigator run -> verification record | human-run evidence crosses back into the repo record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-25 | Tampering | hand-bumped versions / partial lockstep | mitigate | nothing bumps in 219 (git diff --exit-code gate); the cut runs ONLY via release.sh five-gate at Phase 221 completion (D-14) |
| T-219-26 | Repudiation | release gate opening without the corepower confirmation | mitigate | blocking human checkpoint; confirmation recorded in 219-VERIFICATION.md BEFORE staging closes (D-13) |
| T-219-27 | Denial of Service | fts5 crash blocking the navigator's validation run | mitigate | Plan 02 probe + degrade, pinned by the checklist's explicit pass/fail rule |
| T-219-28 | Information Disclosure | corepower room content pasted into the repo record | mitigate | the checklist asks for pass/fail ticks + generic output snippets (counts, provenance strings), never room prose; no-real-names rule holds |
| T-219-SC | Tampering | package installs | accept (n/a) | zero installs this phase |
</threat_model>

<verification>
- 219-VERIFICATION.md complete (all four sections filled, corepower confirmed)
- 219-RELEASE-STAGING.md staged with the handoff note; version files untouched (git diff clean)
- run-all-219.sh + doctor --acceptance green at phase close
</verification>

<success_criteria>
- SPEC acceptance 7/8 satisfied in the adjusted (joint-cut) form: corepower validation navigator-confirmed and recorded; release fully staged (CHANGELOG draft, README content refresh with no styling regression, marketplace pin instruction, website fact-check, VERSION-BUMP-CHECKLIST); the cut itself explicitly handed to Phase 221 completion
- The open session-memory item (re-run eureka on corepower post-218, production engine) is closed
</success_criteria>

## Artifacts this plan produces

- .planning/phases/219-.../219-COREPOWER-VALIDATION-PROMPT.md (paste-ready Desktop prompt + checklist)
- .planning/phases/219-.../219-RELEASE-STAGING.md (CHANGELOG draft, README refresh diff, marketplace source.ref pin instruction, website fact-check checklist, VERSION-BUMP-CHECKLIST, Phase 220 handoff note)
- .planning/phases/219-.../219-VERIFICATION.md (completed: corepower confirmation section)
- Explicitly NOT produced: a version bump, an npm publish, a git tag - all deferred to the joint 219+220+221 cut via scripts/release.sh

<output>
Create `.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-07-SUMMARY.md` when done
</output>
