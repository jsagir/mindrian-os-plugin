---
phase: 219-opportunity-follow-through-harvest-formula-explored-stage-de
plan: 03
type: execute
wave: 2
depends_on: ["219-01", "219-02"]
files_modified:
  - lib/core/eureka/opportunity-harvest.cjs
  - lib/core/sensors/sensor-opportunity-harvest.cjs
  - lib/core/insight-sensors.cjs
  - scripts/scout-cadence-runner.cjs
  - tests/test-219-harvest-sensor.cjs
autonomous: true
requirements: [REQ-2]
must_haves:
  truths:
    - "A fixture room with a known bridge + contradiction yields exactly the expected candidates with correct Four-Lens labels"
    - "A signal with zero typed links to the graph is NOT surfaced (one dot is noise - the Connection hard gate)"
    - "The top candidate is never an artifact-vs-artifact restatement (the 218 degree-centrality countermeasure)"
    - "The sensor rides the existing context_block reach with posture hold; no new reach id, no new hook surface"
    - "The scout-cadence opportunity slot writes real scored candidates, not a tally string"
  artifacts:
    - path: "lib/core/eureka/opportunity-harvest.cjs"
      provides: "harvestCandidates producer: graph-event scan -> Q1..Q8 rubric scoring -> Four-Lens -> last-opportunity-harvest.json"
      exports: ["harvestCandidates"]
    - path: "lib/core/sensors/sensor-opportunity-harvest.cjs"
      provides: "SENS-14 pure LOCAL detector (clone of sensor-eureka)"
      exports: ["sensorOpportunityHarvest"]
    - path: "lib/core/insight-sensors.cjs"
      provides: "SENS-14 registered (require + SENSOR_REGISTRY + module.exports, three touch points)"
      contains: "sensorOpportunityHarvest"
    - path: "tests/test-219-harvest-sensor.cjs"
      provides: "REQ-2 fixture test: lens labels, zero-connection suppression, hub-skew countermeasure"
  key_links:
    - from: "scripts/scout-cadence-runner.cjs"
      to: "lib/core/eureka/opportunity-harvest.cjs"
      via: "the :249-256 slot calls harvestCandidates (replacing the tally)"
      pattern: "harvestCandidates"
    - from: "lib/core/sensors/sensor-opportunity-harvest.cjs"
      to: "<roomDir>/.mindrian/last-opportunity-harvest.json"
      via: "stat-before-parse freshness-gated side-channel read"
      pattern: "last-opportunity-harvest"
    - from: "lib/core/sensors/sensor-opportunity-harvest.cjs"
      to: "lib/core/sensors/sensor-types.cjs"
      via: "makeReach({ reach_id:'context_block', posture:'hold', ... }) - frozen bank membership fail-closed"
      pattern: "context_block"
---

<objective>
Implement REQ-2: the harvest sensor (SENS-14) on the insight-sensor rail turns graph events into scored opportunity candidates implementing the BINDING Harvest Formula (CANDIDATE = Signal x Connection x Lens x Actor; Brain already asked and answered - do not re-derive) with Gibson Four-Lens classification and the canon-derived Q1..Q8 qualification rubric (219-RESEARCH.md "Brain-sourced qualification doctrine" - MUST be the rubric, not an improvised one).

Implements locked decisions D-01 (conform to the live insight-sensors.cjs contract; ride EXISTING rails - scout cadence slot + existing post-write seams; NO new hook surface) and D-02 (candidates surface as ranked reaches through the one governed reach path; no new reach id - rides context_block the way SENS-11 does).

Naming de-collision (checker MAJOR, logged): Phase 188's lib/hmi/harvest-scope-state.cjs (F.4 conversation capture) already owns the bare 'harvest' name and writes <roomDir>/.mindrian/harvest-scope-state.json. This plan's surfaces are therefore namespaced: sensor-opportunity-harvest.cjs and last-opportunity-harvest.json (export sensorOpportunityHarvest).

Also implements D-18 (versioned component model): candidates expose components from EXISTING measured signals - critic_gate (212 verdict), compression_score (213), portfolio_score (215 AHP, 3 dimensions kept), tail_flag (215, never folded in), evidence_readiness, follow_through_readiness - plus HarvestIndex_v1 (ADVISORY, versioned, explicit editable weights, status EXPERIMENTAL until calibrated). Missing input -> typed 'unknown'/'insufficient_evidence', NEVER a fabricated zero. Q2 Connection stays the ONLY hard gate; the rubric and the index never filter beyond it.

Planner decision (logged, resolves RESEARCH open question 1 / A2): the qualification-card reach is `context_block` with signal 'harvest' and posture 'hold' (a standing suggestion, never auto-open; deep_research stays reserved for [Explore]). Confirm against build-connector-registry --check at build time; adjust only if the registry flags it.

Purpose: opportunities are DETECTED from the graph, scored deterministically, and offered - never auto-filed.
Output: producer + side-channel, SENS-14, registration, fixture test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-SPEC.md
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-RESEARCH.md
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-PATTERNS.md
@lib/core/sensors/sensor-eureka.cjs
@lib/core/sensors/sensor-types.cjs
@lib/core/insight-sensors.cjs
</context>

<interfaces>
Side-channel contract `<roomDir>/.mindrian/last-opportunity-harvest.json` (this plan is the producer of record; Plan 04 consumes; the identical schema is restated there - any change lands in BOTH plans):

```json
{
  "schema_version": 1,
  "generated_at": "<ISO-8601>",
  "session_id": "<sid>",
  "candidates": [
    {
      "candidate_id": "<stable hash handle>",
      "node_handle": "<opportunity node id or null if not yet minted>",
      "source_event": "bridge | whitespace | contradiction | eureka_proposal | meeting_filing",
      "lens": "leveraging_resources | challenging_orthodoxies | understanding_needs | harnessing_trends",
      "band": "high | medium | low",
      "score": 0.87,
      "connection_count": 2,
      "evidence_handles": ["<node id>", "<node id>"],
      "rubric": { "q1": 1, "q2": 1, "q3": 1, "q4": 1, "q5": 1, "q6": 1, "q7": 1, "q8": 1 },
      "components": {
        "critic_gate": "pass | fail | unknown",
        "compression_score": 0.62,
        "portfolio_score": { "strategic_fit": 0.5, "validated_demand": 0.4, "feasibility": 0.7 },
        "tail_flag": false,
        "evidence_readiness": 0.5,
        "follow_through_readiness": "unknown"
      },
      "harvest_index": { "version": "HarvestIndex_v1", "status": "EXPERIMENTAL", "value": 0.44, "weights": { "compression": 0.3, "portfolio": 0.3, "evidence": 0.2, "follow_through": 0.2 } },
      "engine_mode": "engine | llm_manual_baseline"
    }
  ]
}
```

Enum/handle-only (Part 8 discipline even though the file is LOCAL): opaque node ids, closed enums, quantized scalars - ZERO user prose. The card host (Plan 04) dereferences handles against room.db locally for display. Own path - never `.mindrian/eureka/` or `.mindrian/entity-extract/` (218 Pitfall 5 collision rule).

D-18 component rules: every component sources an EXISTING measured signal (212 critic verdict, 213 compression, 215 AHP + tail flag) or a deterministic closed rubric (evidence_readiness from provenance/claim counts; follow_through_readiness from owner/next-experiment/decision-question presence). A missing input is the typed string 'unknown' (or 'insufficient_evidence'), NEVER a fabricated 0 - a zero means measured-zero. HarvestIndex_v1 = critic_gate x weighted sum of the numeric components; ADVISORY only (never a surfacing filter beyond the Q2 hard gate), weights an explicit editable module constant, version + EXPERIMENTAL status carried in every emission. engine_mode defaults to 'engine'; 'llm_manual_baseline' is stamped only by the D-20 offered fallback (Plan 04) and is EXCLUDED from calibration sets.

Rubric mapping onto the Harvest Formula (from the Brain-sourced doctrine): Signal = Q1 friction/creation + Q4 timing; Connection = Q2 (>=1 typed link, HARD gate) + Q3 surprise (cross-section/bridge, not within-cluster); Lens = Four-Lens classification; Actor = Q5 actor fit. Q6 definability, Q7 newness (dedup vs bank + REJECTED_BECAUSE edges), Q8 desirability act as disqualifiers. Multiplicative composition: any hard-gate zero means not surfaced. Exact weights/thresholds are Claude's discretion - deterministic, documented in the module header, tuned against the fixture and the ador live run.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The harvest producer (graph-event scan -> scored candidates -> side-channel) + scout-cadence slot</name>
  <files>lib/core/eureka/opportunity-harvest.cjs, scripts/scout-cadence-runner.cjs, tests/test-219-harvest-sensor.cjs</files>
  <read_first>
    - scripts/scout-cadence-runner.cjs (:245-262 - the opportunity-bank slot; the "N opportunity(ies) surfaced" tally at :256 is what gets replaced; runStep/steps.push shape)
    - .planning/phases/219-.../219-RESEARCH.md Pattern 3 (producer: no new hook; enum/handle-only side-channel) + "Brain-sourced qualification doctrine" (the Q1..Q8 rubric - BINDING)
    - lib/core/navigation/typed-opportunity.cjs (Plan 01's writer - the proposed opportunity nodes the eureka_proposal lane scans for)
    - tests/helpers/fixture-room-219.cjs (Plan 02's fixture - the planted features the producer must find)
  </read_first>
  <action>
    Create lib/core/eureka/opportunity-harvest.cjs exporting harvestCandidates(roomDir, opts) per D-01. READ-ONLY against room.db (the producer never writes nodes/edges - minting happens at the card, Plan 04): load the FULL graph in memory (D2: rooms are 70-2,400 nodes; never LazyGraph), scan for graph events: (a) bridges - cross-section typed edges between low-degree non-artifact nodes; (b) whitespace zones - read prior scout whitespace outputs when present, skip gracefully when absent; (c) CONTRADICTS edge pairs; (d) eureka_proposal - proposed `opportunity` nodes minted by Plan 01's banking; (e) meeting filings - memory_event nodes of the meeting kind. Score each per the Q1..Q8 rubric with the multiplicative Harvest Formula composition (interfaces section): Q2 Connection is the HARD gate (zero typed links = never surfaced), Q3 surprise demotes within-cluster restatements (the 218 degree-centrality countermeasure - an artifact-vs-artifact pair must never outrank the planted bridge), Q7 newness dedups against opportunity-bank/ entries (opportunity-ops.computeOpportunityBankState / listOpportunities) and existing REJECTED_BECAUSE edges. Compute the D-18 component bag per candidate (critic_gate / compression_score / portfolio_score / tail_flag / evidence_readiness / follow_through_readiness - existing measured signals only; typed 'unknown' when an input is missing, never a fabricated zero) and HarvestIndex_v1 (advisory, EXPERIMENTAL, explicit editable weights constant documented in the header). Classify Four-Lens: whitespace -> leveraging_resources, contradiction -> challenging_orthodoxies, meeting friction -> understanding_needs, macro signal -> harnessing_trends; bridge/eureka lanes take the lens of their dominant signal. Write <roomDir>/.mindrian/last-opportunity-harvest.json atomically (write-temp-then-rename) per the interfaces schema - enum/handle-only, own path. Document weights/thresholds in the module header. Write the PRODUCER sections of tests/test-219-harvest-sensor.cjs FIRST (RED) - Tasks 2 and 3 extend and finalize the same file (checker MINOR: the verify command exists from this task onward). Then replace the scout-cadence tally slot (:249-256): call harvestCandidates, push a step entry reporting candidate count + top band (keep the compute-opportunity-state step above it untouched); a producer failure degrades the step (the existing catch idiom), never kills the cadence.
  </action>
  <verify>
    <automated>node tests/test-219-harvest-sensor.cjs</automated>
  </verify>
  <acceptance_criteria>
    - On the Plan 02 fixture: the planted bridge and contradiction appear as candidates with lens labels challenging_orthodoxies (contradiction) and the bridge's dominant-signal lens; the zero-connection isolate is ABSENT; no candidate pairs two artifact hubs
    - last-opportunity-harvest.json validates against the interfaces schema: enums only, node-id handles only, no prose field
    - grep gate: zero writes (INSERT/UPDATE via any path) in opportunity-harvest.cjs - read-only producer
    - scout-cadence-runner.cjs slot replaced; bash -n scripts/scout-cadence-runner.cjs parses; the step name reports candidates, not a bare tally
  </acceptance_criteria>
  <done>Deterministic scored candidates on disk from real graph events; the tally gap (scout-cadence-runner:255-256) is closed.</done>
</task>

<task type="auto">
  <name>Task 2: SENS-14 sensor (clone sensor-eureka) + three-touch-point registration</name>
  <files>lib/core/sensors/sensor-opportunity-harvest.cjs, lib/core/insight-sensors.cjs</files>
  <read_first>
    - lib/core/sensors/sensor-eureka.cjs (:56-87 header/id-claim/frozen-reach fail-closed; :95-119 replicated readJsonSafe + isFreshFile with the WR-01 future-mtime guard; :146-190 fire body: stat-before-parse, schema_version check, gate check, enum/handle-only evidence bag; the soft-fail-to-null wrapper)
    - lib/core/sensors/sensor-expert-skill.cjs (:57-59 - the context_block REACH_ID precedent SENS-14 follows)
    - lib/core/sensors/sensor-types.cjs (:199-254 makeReach contract: reach_id/posture/dispatch/signal/evidence flat-scalar bag/companions string-only)
    - lib/core/insight-sensors.cjs (:130-137 require block; :609-617 SENSOR_REGISTRY; :700-710 module.exports - the three touch points; the dispatch loop :635-677 is FROZEN, Phase 144 fence - do not touch)
  </read_first>
  <action>
    Create lib/core/sensors/sensor-opportunity-harvest.cjs cloning sensor-eureka.cjs structurally. SENSOR_ID 'SENS-14' (verified free this session: SENS-13 eureka is last registered; re-verify against the live file before locking - the Brain answer itself mandates the re-check). REACH_ID 'context_block' with the load-time fail-closed throw if it drifts off the frozen REACH_IDS bank (D-02; planner decision per objective). Replicate readJsonSafe/isFreshFile LOCALLY (30-minute freshness window, WR-01 future-mtime guard, stat-before-parse) - NEVER require('../insight-sensors.cjs') (circular, Pitfall 4); only require ./sensor-types.cjs. Fire body: read <roomDir>/.mindrian/last-opportunity-harvest.json, check schema_version === 1, fire ONLY when candidates exist whose connection_count >= 1 (the Connection hard gate restated at the sensor); return makeReach({ reach_id:'context_block', posture:'hold', dispatch:'qualify-opportunity', signal:'harvest', evidence: { candidate_count, top_candidate_id, top_lens, top_band, top_score } }) - flat scalars only, zero prose (Part 8 five-tripwire sweep spans all sensors automatically). Soft-fail-to-null wrapper. Stale side-channel (>30 min) returns null. Register in insight-sensors.cjs at the three touch points exactly as the SENS-13 rows sit: require, SENSOR_REGISTRY append after sensorEureka with a "Phase 219 detector (SENS-14 -- opportunity-harvest -> qualification-card offer on context_block)" comment, module.exports re-export. Do NOT modify the dispatch loop. The dispatch slug 'qualify-opportunity' is the born-wired command surface Plan 04 mints - the born-wired-at-feature-time reachability proof (213 Plan 03 precedent) completes when Plan 04 lands; note this in the SUMMARY handoff.
  </action>
  <verify>
    <automated>node tests/test-219-harvest-sensor.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node -e "require('./lib/core/insight-sensors.cjs')" loads without circular-require or frozen-reach throw
    - dispatchSensors over the fixture room (fresh side-channel) yields exactly one SENS-14 reach with reach_id context_block, posture hold, and a flat scalar evidence bag
    - Stale (>30 min) or absent side-channel yields null from sensorOpportunityHarvest (soft-fail)
    - grep -c "require('../insight-sensors" lib/core/sensors/sensor-opportunity-harvest.cjs returns 0
  </acceptance_criteria>
  <done>SENS-14 live on the rail, riding existing reach semantics, fail-closed on the frozen bank, enum-only evidence.</done>
</task>

<task type="auto">
  <name>Task 3: REQ-2 fixture test (lens labels + suppression + hub-skew countermeasure)</name>
  <files>tests/test-219-harvest-sensor.cjs</files>
  <read_first>
    - tests/helpers/fixture-room-219.cjs (the planted ids: bridge, contradiction pair, isolate, hub artifacts)
    - .planning/phases/219-.../219-SPEC.md requirement 2 acceptance (offline-runnable; expected candidates with correct lens labels; zero-connection NOT surfaced)
    - tests/test-218-entity-writer.cjs (the standalone node-assertion test idiom: exit 0/1, temp dirs, no framework)
  </read_first>
  <action>
    Finalize tests/test-219-harvest-sensor.cjs (producer sections created RED-first in Task 1, sensor sections extended in Task 2) as a standalone node script (exit 0/1, no jest/mocha): build the fixture via buildFixtureRoom, run harvestCandidates, then assert: (1) the planted contradiction candidate carries lens challenging_orthodoxies; (2) the planted bridge candidate is present and ranks ABOVE any artifact-hub pairing (the 218 R1 regression pin - fail loudly if an artifact-vs-artifact candidate takes the top slot); (3) the zero-connection isolate produces NO candidate; (4) the side-channel is schema-valid and prose-free (walk every candidate field: enums in the closed sets, handles matching seeded node ids, scalars quantized); (5) sensorOpportunityHarvest fires a valid reach on the fresh side-channel and returns null on a stale one (mtime backdated). (6) D-18 component assertions: every candidate carries the components bag + versioned harvest_index (status EXPERIMENTAL); a candidate with a missing input carries the typed 'unknown', never 0; a Q2-passing candidate with unknown components STILL surfaces (the index is advisory, Q2 is the only hard gate); engine_mode reads 'engine' on the deterministic path. Also assert determinism: two runs produce identical candidate sets. Wire nothing new into run-all-219.sh - the Plan 01 harness already carries this leg file-gated.
  </action>
  <verify>
    <automated>node tests/test-219-harvest-sensor.cjs && bash tests/run-all-219.sh</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-219-harvest-sensor.cjs exits 0 with all six assertion groups green
    - bash tests/run-all-219.sh: harvest-sensor leg flips SKIP -> PASS; all grep gates stay green
    - node scripts/build-connector-registry.cjs --check stays green (sensor is a detector on the existing rail, not an unwired invocable)
  </acceptance_criteria>
  <done>REQ-2 acceptance pinned offline: expected candidates, correct lenses, suppression proven, hub-skew countermeasure regression-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room.db -> side-channel -> reach telemetry | graph-derived signals leave the DB as sensor evidence |
| scout cadence -> producer | scheduled code path runs against user rooms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-09 | Information Disclosure | user prose leaking via evidence bag / side-channel | mitigate | enum/handle-only contract; makeReach drops non-scalars by construction; Part 8 five-tripwire sweep spans lib/core/sensors/*; test walks every field |
| T-219-10 | Tampering | producer writing nodes outside the HITL gate | mitigate | producer is READ-ONLY by contract; grep gate; minting happens only at the card (Plan 04) |
| T-219-11 | Spoofing | forged/stale side-channel firing the sensor | mitigate | stat-before-parse freshness gate (30 min), WR-01 future-mtime guard, schema_version check, soft-fail-to-null |
| T-219-12 | Denial of Service | producer failure killing the scout cadence | mitigate | degraded-step catch idiom (existing :257-259 pattern); sensor soft-fails to null |
| T-219-SC | Tampering | package installs | accept (n/a) | zero installs this phase |
</threat_model>

<verification>
- node tests/test-219-harvest-sensor.cjs green; bash tests/run-all-219.sh green
- node scripts/build-connector-registry.cjs --check green
- node -e require-load of insight-sensors.cjs clean (no circular require)
</verification>

<success_criteria>
- SPEC acceptance 2 satisfied: fixture bridge + contradiction yield expected candidates with correct Four-Lens labels; zero-connection signals suppressed
- The Q1..Q8 rubric (canon-derived) is the scoring spine, documented in the producer header
- SENS-14 registered without touching the frozen dispatch loop; no new hook, no new reach id
</success_criteria>

## Artifacts this plan produces

- Functions: `harvestCandidates(roomDir, opts)` (lib/core/eureka/opportunity-harvest.cjs), `sensorOpportunityHarvest(ctx)` (lib/core/sensors/sensor-opportunity-harvest.cjs; registered SENS-14)
- Files/paths: `<roomDir>/.mindrian/last-opportunity-harvest.json` (schema_version 1, contract in interfaces)
- Enums: lens {leveraging_resources, challenging_orthodoxies, understanding_needs, harnessing_trends}; source_event {bridge, whitespace, contradiction, eureka_proposal, meeting_filing}; band {high, medium, low}; rubric keys q1..q8; engine_mode {engine, llm_manual_baseline}; D-18 components {critic_gate, compression_score, portfolio_score, tail_flag, evidence_readiness, follow_through_readiness} + HarvestIndex_v1 (ADVISORY, EXPERIMENTAL, explicit weights)
- Reach usage: context_block + posture hold + dispatch 'qualify-opportunity' + signal 'harvest' (no new reach id)
- Test files: tests/test-219-harvest-sensor.cjs

<output>
Create `.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-03-SUMMARY.md` when done
</output>
