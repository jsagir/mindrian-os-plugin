---
phase: 219-opportunity-follow-through-harvest-formula-explored-stage-de
plan: 05
type: execute
wave: 3
depends_on: ["219-03", "219-04"]
files_modified:
  - lib/core/eureka/explore-chain.cjs
  - lib/core/eureka/research-filing.cjs
  - lib/core/eureka/explored-artifact.cjs
  - commands/explore-opportunity.md
  - tests/test-219-explore-chain.cjs
autonomous: true
requirements: [REQ-4]
must_haves:
  truths:
    - "[Explore] on a qualified opportunity produces an opportunity-bank/ artifact with governing thought, SCQA structure, cited web sources, and >=2 typed evidence edges; lifecycle reads explored"
    - "The chain NEVER auto-fires on Qualify (explicit per-opportunity trigger, navigator-controlled cost)"
    - "The deep-research step files a research/ artifact with frontmatter + URL-cited sources, and post-filing extraction lands its entities as proposed nodes with DERIVED_FROM edges to the artifact node (D-16)"
    - "When the web leg is unavailable, the research step degrades to the room's own research corpus with provenance 'web: absent (room-corpus degrade)' - never a crash, never a silent skip (D-16)"
    - "Material steps halt at gates; the autonomous_safe prefix runs (Canon Part 3)"
  artifacts:
    - path: "lib/core/eureka/explore-chain.cjs"
      provides: "exploreOpportunity: composeWorkflow -> runChain with frozen deep_research reach + offline degrade"
      exports: ["exploreOpportunity"]
    - path: "lib/core/eureka/research-filing.cjs"
      provides: "fileResearchArtifact (frontmatter + citations via navigation) + runPostFilingExtraction (scoped incremental) + queryRoomCorpus (offline degrade)"
      exports: ["fileResearchArtifact", "runPostFilingExtraction", "queryRoomCorpus"]
    - path: "lib/core/eureka/explored-artifact.cjs"
      provides: "composeExploredArtifact: Minto shape (governing thought + SCQA + MECE) filed to opportunity-bank/section/name/name.md"
      exports: ["composeExploredArtifact"]
    - path: "commands/explore-opportunity.md"
      provides: "born-wired explicit [Explore] surface with hitl declaration (D-10)"
      contains: "hitl_shape"
    - path: "tests/test-219-explore-chain.cjs"
      provides: "REQ-4 chain + Minto shape + no-auto-fire + D-16 corpus-contract test"
  key_links:
    - from: "lib/core/eureka/explore-chain.cjs"
      to: "lib/workflow/command-resolver.cjs"
      via: "composeWorkflow(frameworkChain) - zero hardcoded /mos: slugs"
      pattern: "composeWorkflow"
    - from: "lib/core/eureka/explore-chain.cjs"
      to: "lib/core/chain-executor.cjs"
      via: "runChain(steps, { onStep, roomDir, postureFn, gateFn }) - the one shared gated loop"
      pattern: "runChain"
    - from: "lib/core/eureka/research-filing.cjs"
      to: "scripts/entity-extract.cjs"
      via: "runExtraction with opts.paths (the Plan 02 scoped-incremental seam)"
      pattern: "opts.paths"
    - from: "lib/core/eureka/explored-artifact.cjs"
      to: "lib/core/opportunity-ops.cjs"
      via: "problem_hash dedup scan + Decision 16 nested filing"
      pattern: "problem_hash"
---

<objective>
Implement REQ-4: the explored-stage analysis chain. An explicit [Explore] action on a qualified opportunity runs deep_research (FROZEN reach, generic handles) + diffusion/timing + find-analogies + web validation, and files a Minto-shaped artifact (governing thought + SCQA + typed evidence edges) into opportunity-bank/, advancing lifecycle to explored. The output must read as ANALYZED, not a candidate one-liner - that is the navigator complaint being fixed.

Implements locked decisions D-06 (compose through the SHIPPED chain machinery: composeWorkflow -> runChain; posture from the registry; autonomous_safe prefix runs, material steps halt), D-07 (web leg rides the FROZEN deep_research reach, generic handles only, existing audit seams; diffusion/find-analogies invoke their existing commands in current Track-1-unrewired form), D-08 (Minto via the existing structure-argument machinery; Obsidian nested filing per Decision 16), and D-16 (the Research Corpus Contract: research files as a corpus artifact, post-filing extraction makes it graph-visible at filing time, offline degrades to the room's own corpus).

The canon mandate (Brain-sourced doctrine): "opportunities are conceptual... they must be researched and explored to be turned into well-defined problems." This chain IS that step; its output target is a WELL-DEFINED problem statement (Minto governing thought), not a vibe.

Rejected alternatives (D-16, logged): new deps jsdom / Playwright / @lightfeed/extractor (Tavily covers DOM-to-clean-markdown server-side; zero new packages this phase); direct Neo4j triplet writes (Part 8 breach - room content never egresses; room.db via navigation.cjs is the ONLY graph target).

Purpose: qualified stops being terminal; exploration generates real, graph-visible analysis.
Output: chain composer, research filer + post-filing extraction + offline degrade, Minto artifact writer, explore surface, chain test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-SPEC.md
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-RESEARCH.md
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-PATTERNS.md
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-CONTEXT.md
@lib/core/chain-executor.cjs
@lib/workflow/command-resolver.cjs
@lib/core/recipe-maps.cjs
@commands/structure-argument.md
</context>

<interfaces>
Research artifact contract (D-16 item 1): filed under the room's research/ conventions (the /mos:research provenance style) as markdown with YAML frontmatter carrying: date, sources (each with url + accessed date), topic handles. Filed through navigation.cjs (the artifact gains a memory_artifact node like any room artifact).

Post-filing extraction contract (D-16 item 2): immediately after ANY explore-chain artifact files (research/ or opportunity-bank/), invoke the SAME runExtraction path entity-extract.cjs uses, scoped via opts.paths to exactly the new artifact paths (the Plan 02 seam) - never a full-room re-extract. Result: the artifact's entities/relations land as proposed typed nodes/edges with DERIVED_FROM edges to the artifact node, graph-visible to ALL engines at filing time.

Offline degrade contract (D-16 item 3): when the deep_research reach / web leg is unavailable, queryRoomCorpus runs tri-modal retrieval scoped to research/ artifacts (filter retrieval results to research-artifact nodes; consume the existing tri-modal API read-only - do not modify tri-modal-index.cjs, which Plan 02 owns this phase) and the step provenance reads "web: absent (room-corpus degrade)" - the same honest-degrade discipline as the vec0/FTS5 probes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The gated chain composer + explicit [Explore] surface + offline degrade</name>
  <files>lib/core/eureka/explore-chain.cjs, lib/core/eureka/research-filing.cjs, commands/explore-opportunity.md</files>
  <read_first>
    - lib/core/chain-executor.cjs (:432-475 runChain: six-callback contract, roomDir/retries/journal resilient path, makeGateFn material/irreversible halting)
    - lib/workflow/command-resolver.cjs (:110 composeWorkflow: framework NAMES -> registry commands, command:null optional:true degrade, never fabricates)
    - lib/core/recipe-maps.cjs (postureForCommand - the posture authority)
    - scripts/scout-cadence-runner.cjs (:247-260 runStep/steps.push step shape)
    - lib/core/eureka/vector-store.cjs (:73-98 - the honest-degrade probe discipline the offline path mirrors)
    - .planning/phases/219-.../219-RESEARCH.md Pattern 4 (the call shape) + open question 3 (Minto via chain step, validated by invariants)
  </read_first>
  <action>
    Create lib/core/eureka/explore-chain.cjs exporting exploreOpportunity(roomDir, opportunityNodeId, opts). Compose the frameworkChain via composeWorkflow with framework NAMES (deep-research leg, diffusion/timing leg, Analogical Reasoning (the find-analogies command in its CURRENT Track-1-unrewired form - the chain contract does not change when Track 1 rewires internals, D-07), web-validation leg) - resolve the exact registry framework names at build time from the command registry; zero hardcoded /mos: slugs (D-06). Run via runChain(steps, { onStep, roomDir, postureFn: recipe-maps default, gateFn: makeGateFn default, journal }) so the resilient path (retry/backoff, journaled position, graceful partial) engages; material/irreversible steps HALT at the gate for the navigator (Part 3); the autonomous_safe prefix runs. The deep-research step: rides the FROZEN deep_research reach with GENERIC handles only through the framework-runner's existing egress chokepoint - never a raw fetch in the chain (D-07, Part 8). On success it hands its output to research-filing (Task 2). OFFLINE DEGRADE (D-16 item 3): detect web-leg unavailability (reach unavailable / opts.forceOffline test seam) and swap the step's source to queryRoomCorpus with step provenance "web: absent (room-corpus degrade)" - never a crash, never a silent skip. exploreOpportunity is invoked ONLY from the explicit surface: create commands/explore-opportunity.md born-wired (D-10) with hitl_shape declaring the gate shape and hitl_why "Exploration spends navigator-controlled research cost and crosses material gates; explicit per-opportunity trigger only - never auto-fired on qualify." Also create the research-filing.cjs skeleton with queryRoomCorpus(roomDir, topicHandles): tri-modal retrieval consumed READ-ONLY, results filtered to research/ memory_artifact nodes (interfaces contract).
  </action>
  <verify>
    <automated>node tests/test-219-explore-chain.cjs</automated>
  </verify>
  <acceptance_criteria>
    - composeWorkflow returns the legs in order with resolved commands (or command:null + optional:true - degrade honest, never fabricated); asserted in the test with the registry loaded
    - A material step halts (haltedAt set, no auto-run past the gate) under the default gateFn
    - opts.forceOffline path returns room-corpus results with the exact provenance string "web: absent (room-corpus degrade)"
    - grep (comment-filtered) of lib/core/eureka/explore-chain.cjs shows zero '/mos:' literals and zero raw fetch/https requires
    - node scripts/build-connector-registry.cjs --check green: explore-opportunity born WIRED; node scripts/check-shape-declaration.cjs --check clean for the new surface
  </acceptance_criteria>
  <done>An explicit, gated, registry-resolved chain with an honest offline degrade; no hardcoded slugs, no raw egress.</done>
</task>

<task type="auto">
  <name>Task 2: Research corpus filing + post-filing scoped extraction (D-16 items 1+2)</name>
  <files>lib/core/eureka/research-filing.cjs</files>
  <read_first>
    - scripts/entity-extract.cjs (runExtraction signature with the Plan 02 opts.paths allowlist; the D-05 open/work/close discipline at :319-348)
    - lib/core/opportunity-ops.cjs (:24 parseFrontmatter - validate the filed frontmatter round-trips)
    - commands/opportunities.md + the room research/ filing conventions (the /mos:research provenance style: date, sources with url + accessed date, topic handles)
    - lib/core/navigation/typed-opportunity.cjs + lib/core/navigation.cjs (writers for the artifact node + DERIVED_FROM edges)
  </read_first>
  <action>
    Complete lib/core/eureka/research-filing.cjs. fileResearchArtifact(roomDir, { topicHandles, body, sources }): writes the research/ markdown artifact with YAML frontmatter (date, sources each carrying url + accessed date, topic handles - the interfaces contract), registers its memory_artifact node through navigation.cjs (never a bare file drop - the artifact must be a graph citizen at birth). runPostFilingExtraction(roomDir, sessionId, artifactPaths): open/work/close per D-05, invoke runExtraction with opts.paths = artifactPaths (the Plan 02 scoped seam - NEVER a full-room re-extract), then link extracted proposed entities to the artifact node with DERIVED_FROM edges where the extractor has not already done so. Wire BOTH filing sites to call runPostFilingExtraction immediately after filing: the research/ artifact (this module) and the explored bank artifact (Task 3 calls the same function) - D-16 item 2: filed research becomes graph-visible to ALL engines at filing time, not a dead markdown file. All writes through navigation; extraction stays deterministic and zero-egress.
  </action>
  <verify>
    <automated>node tests/test-219-explore-chain.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Filed research artifact parses back through parseFrontmatter with date + sources (url + accessed) + topic handles intact
    - After filing, room.db contains: the artifact's memory_artifact node AND >=1 proposed entity node/edge DERIVED_FROM the artifact node (the D-16 acceptance)
    - The extraction invocation carries opts.paths scoped to exactly the new artifact paths (asserted: pre-existing artifact nodes untouched)
    - grep gate: no raw INSERT, no network require in research-filing.cjs
  </acceptance_criteria>
  <done>Explore-chain research is a corpus: filed with provenance, graph-visible at filing time.</done>
</task>

<task type="auto">
  <name>Task 3: The Minto explored-artifact writer (Decision 16 nested filing + lifecycle explored)</name>
  <files>lib/core/eureka/explored-artifact.cjs</files>
  <read_first>
    - lib/core/opportunity-ops.cjs (:1123-1160 bankOpportunity: dedup-by-problem_hash frontmatter scan; note it files FLAT - this writer must nest per Decision 16)
    - commands/structure-argument.md (the Minto/SCQA/MECE composer contract reused via the chain step, D-08)
    - lib/core/feynman-minto-invariants.cjs (the pure-function validators that gate the composed artifact)
    - lib/memory/vault-section-minto-generator-atomic.worker.cjs (the shipped Minto generation machinery - reuse, never a new composer)
    - .planning/phases/219-.../219-CONTEXT.md specifics ("the explored output must read as ANALYZED... not a candidate one-liner")
  </read_first>
  <action>
    Create lib/core/eureka/explored-artifact.cjs exporting composeExploredArtifact({ roomDir, db, opportunityNodeId, chainTrace, citations, sessionId }). Compose the Minto shape from the chain outputs: governing thought (the WELL-DEFINED problem statement - the canon's conceptual-until-defined mandate), SCQA structure, MECE sections covering the deep-research findings + diffusion/timing verdict + analogies + web validation, and the citations list (url + accessed) - composed via the structure-argument machinery through the chain step (D-08: reuse, not a new composer; field mapping is Claude's discretion), then validated with feynman-minto-invariants before filing; a failed invariant returns { ok:false, violations } and does NOT file. File to opportunity-bank/<section>/<name>/<name>.md - the Obsidian nested-folder convention (Decision 16; extend beyond bankOpportunity's flat filing while REUSING its problem_hash dedup scan so re-exploration updates rather than duplicates). Then: write >=2 typed evidence edges (SUPPORTS/INFORMS via linkOpportunityEvidence) from the opportunity node to the graph evidence backing the analysis, advance lifecycle to 'explored' via the Plan 01 writer UPSERT, and call runPostFilingExtraction on the new bank artifact path (D-16 item 2). The artifact must read as ANALYZED - governing thought first, evidence-backed sections, cited sources - never a one-liner.
  </action>
  <verify>
    <automated>node tests/test-219-explore-chain.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Composed artifact contains a governing thought, SCQA sections, and >=1 citation; feynman-minto-invariants passes; an invariant-violating draft is REJECTED without filing
    - Artifact lands nested at opportunity-bank/<section>/<name>/<name>.md; a second compose for the same problem_hash updates in place (no duplicate folder)
    - room.db: >=2 typed evidence edges (SUPPORTS/INFORMS) from the opportunity node; lifecycle prop reads 'explored'; post-filing extraction produced the artifact-linked proposed nodes
  </acceptance_criteria>
  <done>Explored means analyzed, Minto-shaped, nested, graph-backed, and graph-visible.</done>
</task>

<task type="auto">
  <name>Task 4: REQ-4 + D-16 chain test (hermetic, stubbed web leg)</name>
  <files>tests/test-219-explore-chain.cjs</files>
  <read_first>
    - .planning/phases/219-.../219-SPEC.md requirement 4 acceptance (Minto artifact + cited sources + >=2 edges + lifecycle explored + never auto-fires)
    - lib/core/eureka/qualify-opportunity.cjs (Plan 04 - the no-auto-fire counterpart to assert against)
    - tests/helpers/fixture-room-219.cjs (fixture with a qualified opportunity seeded via Plan 04 handlers)
  </read_first>
  <action>
    Write tests/test-219-explore-chain.cjs as a standalone node script, fully hermetic (web leg stubbed via onStep; zero network - the run-all-219 zero-network preload applies): (1) chain composition order + registry resolution (or honest command:null degrade); (2) material-step halt; (3) NO-AUTO-FIRE: qualifying a fixture candidate produces NO chain trace and NO explored artifact (behavioral), and a comment-filtered grep proves qualify-opportunity.cjs never requires/invokes explore-chain; (4) full stubbed explore run on a qualified fixture opportunity: research/ artifact filed with frontmatter + citations, post-filing extraction landed >=1 proposed entity DERIVED_FROM the artifact node, Minto bank artifact nested correctly with governing thought + SCQA + citations, >=2 evidence edges, lifecycle 'explored' (the D-16 + REQ-4 acceptance set); (5) forced-offline run returns room-corpus results with provenance "web: absent (room-corpus degrade)" and still files an artifact honestly marked. Then run the aggregator: the explore leg flips SKIP -> PASS.
  </action>
  <verify>
    <automated>node tests/test-219-explore-chain.cjs && bash tests/run-all-219.sh && node scripts/doctor.cjs --acceptance</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-219-explore-chain.cjs exits 0 across all five assertion groups
    - bash tests/run-all-219.sh: ALL six phase legs now PASS (banking, fts5, metadata, harvest, qualify, explore); grep gates green
    - node scripts/doctor.cjs --acceptance green
  </acceptance_criteria>
  <done>REQ-4 and the D-16 corpus contract pinned offline; the live proof is Plan 06's checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chain web leg -> external | the ONLY egress leg this phase touches (deep_research frozen reach) |
| chain output -> room.db + bank files | LLM-composed research becomes graph data + filed artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-17 | Information Disclosure | room content egressing via the web leg | mitigate | FROZEN deep_research reach, generic handles only, existing framework-runner egress chokepoint; never a raw fetch (grep-gated); Part 8 boundary scan |
| T-219-18 | Denial of Service (cost) | auto-firing the costly chain | mitigate | explicit [Explore] surface only; behavioral + grep no-auto-fire test; material steps halt at gates |
| T-219-19 | Tampering | LLM-composed output injected as unvalidated truth | mitigate | feynman-minto-invariants gate before filing; extraction lands entities as review_status 'proposed' (never confirmed); all writes via navigation |
| T-219-20 | Information Disclosure | direct remote-graph (Neo4j) writes | mitigate (rejected alternative) | room.db via navigation.cjs is the ONLY graph target (D-16 item 4); no Neo4j client in any 219 module |
| T-219-21 | Denial of Service | web-leg outage crashing exploration | mitigate | room-corpus degrade with honest provenance (D-16 item 3); never a crash, never a silent skip |
| T-219-SC | Tampering | package installs (jsdom/Playwright/@lightfeed) | mitigate (rejected) | zero new deps - Tavily covers DOM-to-markdown server-side; Package Legitimacy Audit: no installs this phase |
</threat_model>

<verification>
- node tests/test-219-explore-chain.cjs green; bash tests/run-all-219.sh fully green (all six legs PASS)
- build-connector-registry --check + check-shape-declaration --check + check-render-coverage green
- node scripts/doctor.cjs --acceptance green
</verification>

<success_criteria>
- SPEC acceptance 4 satisfied offline: Minto-shaped bank artifact (governing thought + SCQA + cited sources + >=2 typed evidence edges), lifecycle explored, chain never auto-fires on Qualify
- D-16 satisfied offline: research corpus filed + post-filing extraction + honest offline degrade
- Zero new packages; zero hardcoded slugs; zero raw egress
</success_criteria>

## Artifacts this plan produces

- Functions: `exploreOpportunity(roomDir, opportunityNodeId, opts)` (lib/core/eureka/explore-chain.cjs), `fileResearchArtifact`, `runPostFilingExtraction`, `queryRoomCorpus` (lib/core/eureka/research-filing.cjs), `composeExploredArtifact` (lib/core/eureka/explored-artifact.cjs)
- CLI/command verbs: `explore-opportunity` born-wired explicit surface (commands/explore-opportunity.md)
- File conventions: research/ artifact with frontmatter {date, sources[{url, accessed}], topic handles}; opportunity-bank/<section>/<name>/<name>.md nested explored artifact
- Provenance vocabulary: `web: absent (room-corpus degrade)`
- Edge-type usages: SUPPORTS, INFORMS (evidence), DERIVED_FROM (artifact-to-entity provenance from post-filing extraction)
- Env/opts seams: `opts.forceOffline` (test seam for the degrade path)
- Test files: tests/test-219-explore-chain.cjs

<output>
Create `.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-05-SUMMARY.md` when done
</output>
