---
phase: 219-opportunity-follow-through-harvest-formula-explored-stage-de
plan: 04
type: execute
wave: 2
depends_on: ["219-01", "219-02"]
files_modified:
  - lib/core/eureka/qualify-opportunity.cjs
  - commands/qualify-opportunity.md
  - tests/test-219-qualify.cjs
autonomous: true
requirements: [REQ-3]
must_haves:
  truths:
    - "Candidates render as a real F.1 AskUserQuestion card with verbs [Qualify+file] [Ask Brain] [Rephrase] [Suggest next] [Skip] - no ASCII-box fallback on card-capable surfaces (SEED-021)"
    - "Skip writes a REJECTED_BECAUSE edge with the failed-check enum reason (rejection is data, Decision 13)"
    - "Qualify advances lifecycle to qualified via navigation.cjs and is the ONLY path that files into opportunity-bank/"
    - "The card shows WHY the candidate qualified (compressed Q1..Q8 rubric verdict lines + D-18 component readiness lines - pedagogy over verdict, Part 12)"
    - "Every qualify/skip transition APPENDS an immutable stage_history entry with actor/reason/evidence_ids/formula_version (D-17) - never a bare props overwrite"
    - "When the engine legs are unavailable, the card OFFERS [LLM manual scan (high effort)] at the gate - never default, never silent; manual results carry engine_mode llm_manual_baseline and are excluded from calibration (D-20)"
  artifacts:
    - path: "lib/core/eureka/qualify-opportunity.cjs"
      provides: "verb handlers: qualifyCandidate, skipCandidate, formatRubricLines, renderQualificationCard"
      exports: ["qualifyCandidate", "skipCandidate", "formatRubricLines", "formatComponentLines", "renderQualificationCard"]
    - path: "commands/qualify-opportunity.md"
      provides: "born-wired command surface with hitl_shape F.1 + hitl_why (D-10, Part 11 CIRS)"
      contains: "hitl_shape"
    - path: "tests/test-219-qualify.cjs"
      provides: "REQ-3 rejection edge + lifecycle advance + nothing-files-without-Qualify test"
  key_links:
    - from: "lib/core/eureka/qualify-opportunity.cjs"
      to: "lib/hmi/shape-f1-renderer.cjs"
      via: "renderShapeF1 + appendAskUserQuestionTrailer (the SEED-021 single render door)"
      pattern: "renderShapeF1"
    - from: "lib/core/eureka/qualify-opportunity.cjs"
      to: "lib/core/navigation.cjs"
      via: "writeOpportunityNode (idempotent mint) + confirmNode (human promote) + writeEdge REJECTED_BECAUSE"
      pattern: "confirmNode"
    - from: "lib/core/eureka/qualify-opportunity.cjs"
      to: "lib/core/card-fire-sidechannel.cjs"
      via: "recordReachedGate (render-coverage audit trail)"
      pattern: "recordReachedGate"
---

<objective>
Implement REQ-3: the qualification Decision Gate. Harvest candidates surface at a real F.1 AskUserQuestion card; nothing files without navigator approval. Qualify files the candidate (lifecycle qualified) via navigation.cjs; Skip writes a rejection edge (SEED-009 ranker learning).

Implements locked decisions D-05 (Skip writes a rejection edge per the f_selector_decision(outcome=reject) precedent), D-09 (card fires through the existing AskUserQuestion/gate_render dispatcher at the sensor's candidate-reach surface; SEED-021 holds; the 5-verb set per the Brain answer), and D-10 (every net-new invocable surface born WIRED with hitl_shape/hitl_why).

Planner decision (logged): BOTH Qualify and Skip mint the proposed opportunity node idempotently first (writeOpportunityNode UPSERT) - Qualify then confirms (confirmNode, the human promote) and advances lifecycle to qualified; Skip leaves the node proposed and hangs the REJECTED_BECAUSE edge on it. Rationale: rejection edges need a stable anchor node, and the dedup policy (Claude's discretion per CONTEXT) suppresses resurfacing via exactly these edges. Eureka-lane candidates already have their node from Plan 01 banking; the idempotent hash id makes the mint a no-op for them.

Advisory seam-check (folded todo 2026-07-08-f7-rescope): verify the card + sensor wiring against the CURRENT capability-registration surface (build-connector-registry --check), not a stale pre-registerCapability pattern.

Purpose: the human gate that makes qualification a material navigator decision (Canon Part 3), with rejection captured as graph data.
Output: verb handlers, born-wired command surface, qualify test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-SPEC.md
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-RESEARCH.md
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-PATTERNS.md
@lib/hmi/shape-f1-renderer.cjs
@lib/hmi/selector-dispatcher.cjs
@lib/core/lens-engine.cjs
@commands/opportunities.md
</context>

<interfaces>
Consumes the Plan 03 side-channel contract `<roomDir>/.mindrian/last-opportunity-harvest.json` (schema_version 1 - restated verbatim in 219-03-PLAN.md interfaces; Plan 03 is the producer of record; namespaced away from Phase 188's harvest-scope-state.json). The card host dereferences candidate node handles against room.db LOCALLY to render display prose - handles stay opaque in the side-channel itself. Also consumes the D-18 component bag + harvest_index: rendered as WHY-lines alongside the Q1..Q8 verdicts (components = machine readiness from existing measured signals; the rubric = the human card checks); typed 'unknown' values render verbatim, NEVER as 0.

D-20 engine-breaks fallback (this surface): when the harvest sensor's engine legs are unavailable (side-channel absent because the producer's engine probes failed beyond the graceful rungs / crash / missing substrate - the FTS5 class; test seam MINDRIAN_FORCE_ENGINE_ABSENT=1), the card may still surface carrying the OFFER verb [LLM manual scan (high effort)]: on acceptance the model reads the room artifacts directly and scores candidates by the SAME Q1..Q8 rubric + D-18 component definitions; every manual candidate carries engine_mode 'llm_manual_baseline' in provenance, node props, and the bank entry. NEVER the default, NEVER a silent substitution (the corepower lesson); manual results never flip calibration status and are excluded from calibration sets; Part 8 + navigation.cjs chokepoints identical.

Skip reason enum (written into the REJECTED_BECAUSE edge properties.reason - single enum scalar only, Part 8): `q1_no_friction | q3_within_cluster | q4_stale_window | q5_no_actor | q7_already_filed | q8_disqualified | off_topic`. These mirror the rubric's classic false positives (solution-in-search-of-a-problem, within-cluster restatement, stale window, no-actor, mirage-from-missing-data).

Handoff to Plan 05: qualifyCandidate NEVER invokes the explore chain (REQ-4: never auto-fire on Qualify - navigator cost control). [Explore] is a separate explicit surface Plan 05 mints.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verb handlers (qualify/skip/rubric formatter) - the material writes</name>
  <files>lib/core/eureka/qualify-opportunity.cjs, tests/test-219-qualify.cjs</files>
  <read_first>
    - lib/core/navigation/confirm-node.cjs (:38 confirmNode(db, id, byUser, reason) - proposed -> confirmed, the human promote)
    - lib/core/lens-engine.cjs (:263-277 applyReject - the reject-edge idiom to copy: writeEdge REJECTED_BECAUSE with a single enum reason scalar)
    - lib/core/navigation/typed-opportunity.cjs (Plan 01's writer: idempotent UPSERT + advanceOpportunityStage append-only stage_history semantics, D-17)
    - lib/core/opportunity-ops.cjs (:1123-1160 bankOpportunity - dedup-by-problem_hash bank filing the Qualify+file verb reuses)
    - .planning/phases/219-.../219-RESEARCH.md "Brain-sourced qualification doctrine" wiring note (rubric verdicts as card description lines; Skip writes the failed-check enum)
  </read_first>
  <behavior>
    - Test 1: qualifyCandidate on a fixture candidate mints (idempotent) + confirms the node (review_status confirmed), advances opportunity_stage/lifecycle to 'qualified' via advanceOpportunityStage APPENDING a stage_history entry {actor: byUser, reason, evidence_ids: the candidate's evidence_handles, formula_version: 'HarvestIndex_v1'} (D-17, prior entries preserved), and files a bank entry via bankOpportunity - all through navigation.cjs
    - Test 2: skipCandidate writes exactly one REJECTED_BECAUSE edge with properties.reason in the closed enum AND appends a stage_history entry setting opportunity_outcome 'rejected' with the failed-check enum as reason (D-17); the node stays review_status 'proposed'; opportunity-bank/ stays EMPTY
    - Test 3: nothing-files-without-Qualify - after render + skip + rephrase paths, opportunity-bank/ contains zero entries
    - Test 4: qualifyCandidate source contains NO call into the explore chain (behavioral: no explored artifact, no chain trace after qualify)
    - Test 5: formatRubricLines compresses the q1..q8 verdict bag into <= 8 short lines naming the passed/failed checks (the WHY, Part 12)
    - Test 6: formatComponentLines renders the D-18 component bag + harvest_index as compressed WHY-lines; a typed 'unknown' renders as unknown, never as 0; the index line carries its version + EXPERIMENTAL status
    - Test 7 (D-20): under MINDRIAN_FORCE_ENGINE_ABSENT=1 the card surfaces with the [LLM manual scan (high effort)] OFFER verb (never on the default path); a candidate qualified from manual mode carries engine_mode 'llm_manual_baseline' in node props AND the bank entry frontmatter; nothing scores silently without the offer being accepted
  </behavior>
  <action>
    Create lib/core/eureka/qualify-opportunity.cjs. qualifyCandidate(db, roomDir, candidate, byUser): writeOpportunityNode (idempotent mint per the logged planner decision - a no-op UPSERT for eureka-lane candidates that already have a node) -> confirmNode(db, nodeId, byUser, 'qualified via card') -> advanceOpportunityStage({ to: 'qualified', actor: byUser, reason: 'qualified via card', evidence_ids: candidate.evidence_handles, formula_version: 'HarvestIndex_v1' }) (D-17 append-only history - the bare new-value props overwrite is forbidden) -> file the bank entry via opportunity-ops.bankOpportunity, propagating candidate.engine_mode into node props + bank frontmatter (D-20: 'llm_manual_baseline' stays visible end-to-end and is excluded from calibration sets) (reusing its problem_hash dedup; this is the +file half of [Qualify+file]). skipCandidate(db, candidate, reasonEnum, targetId): idempotent mint + navigation.writeEdge REJECTED_BECAUSE with the single enum reason scalar (the applyReject idiom; SEED-009/D-05) + advanceOpportunityStage({ axis: 'outcome', to: 'rejected', actor: byUser, reason: reasonEnum, evidence_ids: candidate.evidence_handles, formula_version: 'HarvestIndex_v1' }) (D-17); reject an out-of-enum reason with { ok:false }. formatRubricLines(rubric): the compressed verdict lines. formatComponentLines(components, harvest_index): the D-18 machine-readiness WHY-lines (typed 'unknown' rendered verbatim, never 0; index line names HarvestIndex_v1 + EXPERIMENTAL). Also stub-light handlers for the remaining verbs: rephraseCandidate (returns the candidate for re-title, no writes), suggestNext (returns the next-ranked candidate handle), askBrain (generic framework handles only - Part 8; degrades gracefully to a recommend-never-trigger note when Brain is absent, the RESEARCH environment-availability contract). All handlers return { ok, ... } and never throw. Write tests/test-219-qualify.cjs FIRST (RED) against the Plan 02 fixture + a temp bank dir, then implement to GREEN.
  </action>
  <verify>
    <automated>node tests/test-219-qualify.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-219-qualify.cjs exits 0 across the five behavior groups
    - sqlite check inside the test: exactly one REJECTED_BECAUSE edge after skip; node review_status transitions proposed -> confirmed only on qualify
    - grep -n "explore" lib/core/eureka/qualify-opportunity.cjs returns no chain invocation (comment-filtered)
  </acceptance_criteria>
  <done>Qualify and Skip are governed material writes; rejection is data; qualify never auto-explores.</done>
</task>

<task type="auto">
  <name>Task 2: Card render wiring through the F.1 door + born-wired command surface</name>
  <files>lib/core/eureka/qualify-opportunity.cjs, commands/qualify-opportunity.md</files>
  <read_first>
    - lib/hmi/shape-f1-renderer.cjs (:134 renderShapeF1(input) - the F.1 layout contract; do not fork the renderer)
    - lib/hmi/selector-dispatcher.cjs (:553 appendAskUserQuestionTrailer(rendered, subShape); :1062-1090 the fire site where the trailer + recordReachedGate compose - the call shape to mirror)
    - lib/core/card-fire-sidechannel.cjs (:215-223 recordReachedGate({ sessionId, surface, shape, filePath? }) - the render-coverage audit record)
    - commands/opportunities.md (:1-20 - the frontmatter idiom: hitl_shape/hitl_why declaration, firing-block SEED-021 language)
    - docs/HITL-SHAPE-DECLARATION-CONTRACT.md (F.0-F.9 vocabulary; declaration checked by scripts/check-shape-declaration.cjs)
  </read_first>
  <action>
    Add renderQualificationCard(candidate, deref) to qualify-opportunity.cjs: builds the F.1 input (title from the dereferenced candidate name, description lines from formatRubricLines + formatComponentLines - Q1..Q8 human checks plus D-18 machine-readiness components; the navigator sees WHY, Part 12), renders via renderShapeF1, appends the AskUserQuestion trailer via appendAskUserQuestionTrailer, and records the fire via recordReachedGate({ surface:'qualify-opportunity', shape:'F.1' }). The 5-verb set [Qualify+file] [Ask Brain] [Rephrase] [Suggest next] [Skip] - plus, ONLY when the engine legs are unavailable (D-20; test seam MINDRIAN_FORCE_ENGINE_ABSENT=1), the OFFER verb [LLM manual scan (high effort)] presented at the gate (never default, never silent) - rides the renderer's EXISTING option layout contract - if renderShapeF1 caps primary options, follow its existing overflow/secondary-row convention rather than forking the renderer (D-09: the existing dispatcher door, SEED-021: never an ASCII-box fallback on card-capable surfaces). Create commands/qualify-opportunity.md: the born-wired command surface the SENS-14 reach dispatches to (dispatch slug 'qualify-opportunity' per Plan 03) - frontmatter declares hitl_shape "F.1" and hitl_why "Opportunity qualification is a material navigator decision; nothing files without approval." (the opportunities.md idiom); body documents the verb semantics, the rubric-lines pedagogy, and that [Explore] is a SEPARATE explicit action (never fired from here). Verify the surface registers against the CURRENT capability-registration seam by running build-connector-registry --check (the folded-todo advisory) and check-shape-declaration; fix any wiring flag it raises rather than suppressing it. Extend tests/test-219-qualify.cjs with render assertions: output contains the AskUserQuestion trailer, all five verbs, at least one rubric verdict line, and at least one D-18 component line; the D-20 offer verb appears ONLY under MINDRIAN_FORCE_ENGINE_ABSENT=1; the reached-gate side-channel records the F.1 fire.
  </action>
  <verify>
    <automated>node tests/test-219-qualify.cjs && node scripts/build-connector-registry.cjs --check && node scripts/check-shape-declaration.cjs --check; node scripts/check-render-coverage.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-219-qualify.cjs exits 0 including render assertions (trailer present, 5 verbs, rubric + component lines, D-20 offer only under the forced seam, reached-gate recorded)
    - node scripts/build-connector-registry.cjs --check green: qualify-opportunity is WIRED (born-wired, D-10) and the SENS-14 dispatch slug resolves - the reachability proof from Plan 03 completes
    - node scripts/check-shape-declaration.cjs --check reports zero violations for the new surface (advisory gate, but this phase lands clean)
    - node scripts/check-render-coverage.cjs green (no ASCII-box fallback path introduced)
  </acceptance_criteria>
  <done>The card fires as a real F.1 AskUserQuestion card, born-wired with declared shape, render-coverage clean.</done>
</task>

<task type="auto">
  <name>Task 3: Gate sweep + harness flip</name>
  <files>tests/test-219-qualify.cjs</files>
  <read_first>
    - tests/run-all-219.sh (the qualify leg registered file-gated by Plan 01)
    - .planning/phases/219-.../219-SPEC.md requirement 3 acceptance (card fires; Skip produces rejection edge; nothing lands in opportunity-bank/ without explicit Qualify)
  </read_first>
  <action>
    Final assertion pass on tests/test-219-qualify.cjs mapping one assertion to each SPEC acceptance clause for requirement 3 (name the clause in the assertion message so a failure names the requirement). Run the full aggregator and the three CIRS gates; confirm the qualify leg flips SKIP -> PASS and nothing else regressed. No new files.
  </action>
  <verify>
    <automated>bash tests/run-all-219.sh && node scripts/doctor.cjs --acceptance</automated>
  </verify>
  <acceptance_criteria>
    - bash tests/run-all-219.sh: qualify leg PASS; banking/metadata/fts5/harvest legs still PASS; explore leg still SKIP; grep gates green
    - node scripts/doctor.cjs --acceptance exits green
  </acceptance_criteria>
  <done>REQ-3 pinned offline; live card fire proven on ador in Plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator verb -> room.db | human decision becomes confirmed truth-claim + bank filing |
| card render -> Brain ([Ask Brain] verb) | the only network-adjacent verb |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-13 | Elevation of Privilege | auto-confirming without a human | mitigate | only qualifyCandidate calls confirmNode, and only the card's explicit verb invokes it; producer/sensor are read-only; test pins proposed->confirmed on qualify only |
| T-219-14 | Information Disclosure | [Ask Brain] leaking candidate prose | mitigate | generic framework handles only (Part 8); graceful degrade when Brain absent; no candidate text in the Brain call |
| T-219-15 | Denial of Service | costly explore chain fired from qualify | mitigate | no chain invocation in qualify-opportunity.cjs (grep + behavioral test); [Explore] is Plan 05's separate material surface |
| T-219-16 | Repudiation | card fires with no audit trail | mitigate | recordReachedGate on every render; render-coverage gate |
| T-219-SC | Tampering | package installs | accept (n/a) | zero installs this phase |
</threat_model>

<verification>
- node tests/test-219-qualify.cjs green; bash tests/run-all-219.sh green
- build-connector-registry --check, check-shape-declaration --check, check-render-coverage all green
- node scripts/doctor.cjs --acceptance green
</verification>

<success_criteria>
- SPEC acceptance 3 satisfied offline: card fires as a real AskUserQuestion card; Skip writes a rejection edge; nothing files without Qualify
- SENS-14's dispatch slug resolves to a born-wired surface (the Plan 03 reachability proof completes)
- Rubric verdicts visible on the card (pedagogy over verdict)
</success_criteria>

## Artifacts this plan produces

- Functions: `qualifyCandidate(db, roomDir, candidate, byUser)`, `skipCandidate(db, candidate, reasonEnum, targetId)`, `formatRubricLines(rubric)`, `formatComponentLines(components, harvest_index)`, `renderQualificationCard(candidate, deref)`, plus verb stubs `rephraseCandidate`, `suggestNext`, `askBrain` (lib/core/eureka/qualify-opportunity.cjs)
- CLI/command verbs: `qualify-opportunity` born-wired command surface (commands/qualify-opportunity.md, hitl_shape F.1) carrying [Qualify+file] [Ask Brain] [Rephrase] [Suggest next] [Skip]
- Edge-type usages: REJECTED_BECAUSE (existing member) with reason enum {q1_no_friction, q3_within_cluster, q4_stale_window, q5_no_actor, q7_already_filed, q8_disqualified, off_topic}
- Verbs: [LLM manual scan (high effort)] D-20 offer verb (engine-unavailable only); env var MINDRIAN_FORCE_ENGINE_ABSENT (test seam); provenance value engine_mode 'llm_manual_baseline'
- Test files: tests/test-219-qualify.cjs

<output>
Create `.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-04-SUMMARY.md` when done
</output>
