---
phase: 219-opportunity-follow-through-harvest-formula-explored-stage-de
plan: 06
type: execute
wave: 4
depends_on: ["219-01", "219-02", "219-03", "219-04", "219-05"]
files_modified:
  - .planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-VERIFICATION.md
autonomous: false
requirements: [REQ-6]
must_haves:
  truths:
    - "The final ador-ip-test opportunity-bank/ contains >=1 explored Minto artifact traceable to graph evidence (fixture-green alone is FAIL - 218 lesson R1)"
    - "Live artifact nodes carry frontmatter metadata properties post-extraction"
    - "The live banked opportunities are NOT artifact-vs-artifact restatements (the degree-centrality trap check)"
    - "The explored artifact's entities are visible via graph_query (D-16 post-filing extraction, live)"
    - "Every piece of live evidence is recorded in 219-VERIFICATION.md before /gsd-verify-work"
  artifacts:
    - path: ".planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-VERIFICATION.md"
      provides: "live checkpoint evidence: pre/post counts, node/edge queries, navigator confirmation"
      contains: "ador-ip-test"
  key_links:
    - from: "the live ador-ip-test room"
      to: "219-VERIFICATION.md"
      via: "recorded sqlite/graph_query evidence, not fixture output"
      pattern: "explored"
---

<objective>
Implement REQ-6 (D-12): the LIVE end-to-end acceptance on ador-ip-test - the claim room goes from a pre-218 scaffold (70 nodes / 57 edges, EMPTY opportunity bank, 164 real IP.com case-study artifacts) to a banked, qualified, explored Minto opportunity. This is its own plan with its own verification evidence because fixture-green lied TWICE in 218: the hermetic suite is NECESSARY, never SUFFICIENT. The live-room checkpoint is the only thing that catches the 218-class gap.

Purpose: prove the pipeline on the room where the claim is being made (the ador-IP.com conversations).
Output: recorded live evidence in 219-VERIFICATION.md + navigator sign-off on the qualification + exploration steps.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-SPEC.md
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-VALIDATION.md
@.planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full offline gate sweep (nothing touches the live room until everything is green)</name>
  <files>.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-VERIFICATION.md</files>
  <read_first>
    - tests/run-all-219.sh (all six legs must be PASS, zero SKIP on phase legs, before the live run)
    - .planning/phases/218-.../218-VERIFICATION.md (the evidence format to follow: before/after, file:line, honest gaps)
  </read_first>
  <action>
    Run the complete offline gate set and open 219-VERIFICATION.md with the results table: bash tests/run-all-219.sh (all six phase legs PASS, grep gates PASS), node scripts/build-connector-registry.cjs --check, node scripts/check-shape-declaration.cjs --check, node scripts/check-render-coverage.cjs, node scripts/doctor.cjs --acceptance. Record each command + exit status + the PASS/FAIL/SKIP counts verbatim. Any red gate STOPS this plan (fix in the owning plan, never patch forward here). Structure 219-VERIFICATION.md with sections: Offline Gates, Live ador Run (pre/post), Navigator Checkpoint (card + explore), Corepower Validation (filled by Plan 07), following the 218-VERIFICATION before/after discipline.
  </action>
  <verify>
    <automated>bash tests/run-all-219.sh && node scripts/build-connector-registry.cjs --check && node scripts/check-shape-declaration.cjs --check && node scripts/check-render-coverage.cjs && node scripts/doctor.cjs --acceptance</automated>
  </verify>
  <acceptance_criteria>
    - All five commands exit 0; counts recorded verbatim in 219-VERIFICATION.md
    - run-all-219.sh shows the six phase legs PASS (not SKIP)
  </acceptance_criteria>
  <done>Green board recorded; the live room may now be touched.</done>
</task>

<task type="auto">
  <name>Task 2: Live ador-ip-test pipeline run (extraction + metadata -> banking -> harvest) with recorded evidence</name>
  <files>.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/219-.../219-VALIDATION.md "Manual-Only Verifications" (the live-run instruction set)
    - .planning/phases/218-.../218-VERIFICATION.md (the R1 root cause: what an artifact-vs-artifact lie looks like in the evidence)
  </read_first>
  <action>
    Record PRE-state first (sqlite queries against ~/MindrianRooms/ador-ip-test room.db: node count by type, edge count, opportunity nodes = expected 0, bank state = empty). Then run the automated pipeline legs: (1) node scripts/entity-extract.cjs ~/MindrianRooms/ador-ip-test run (218 extraction + the Plan 02 metadata slice; verify artifact nodes carry methodology/date/status props via direct query - SPEC acceptance 5 live); (2) the eureka run via the production dispatcher path (the /mos:eureka engine, eureka-command.cjs fire-and-return - NEVER the manual baseline; verify >=1 proposed opportunity node per guard-cleared statement, each with >=1 DERIVED_FROM edge, via query not fixture); (3) the harvest producer (harvestCandidates on the live room; verify last-harvest.json candidates carry Four-Lens labels and every candidate has connection_count >= 1). THE 218-CLASS CHECK (mandatory): inspect the banked statements and top candidates - if they are memory_artifact-vs-memory_artifact restatements, record FAIL honestly and stop (the fix belongs upstream, likely predicate/rubric tuning via MINDRIAN_OPPORTUNITY_BANK_PREDICATE - Claude's-discretion tuning is allowed, silent acceptance is not). Record POST-state with the same queries + representative node/edge rows in 219-VERIFICATION.md.
  </action>
  <verify>
    <automated>node -e "const {DatabaseSync}=require('node:sqlite'); const os=require('os'); const db=new DatabaseSync(os.homedir()+'/MindrianRooms/ador-ip-test/.room-graph/room.db',{readOnly:true}); const n=db.prepare(\"SELECT COUNT(*) c FROM nodes WHERE type='opportunity'\").get(); if(!n||n.c<1){console.error('no opportunity nodes');process.exit(1)}; console.log('live opportunity nodes:',n.c);" </automated>
  </verify>
  <acceptance_criteria>
    - PRE and POST state tables recorded in 219-VERIFICATION.md with the exact queries used
    - POST: artifact nodes carry frontmatter props; >=1 proposed opportunity node with >=1 DERIVED_FROM edge; harvest candidates present with lens labels; zero-connection signals absent
    - The 218-class check recorded explicitly (banked opportunities traceable to real cross-entity evidence, or an honest FAIL + stop)
    - Note: adjust the sqlite path in the verify command to the room's actual room.db location before running (verify against lib/core config; the fixture helper knows the layout)
  </acceptance_criteria>
  <done>The live substrate is populated and evidenced; the navigator can now be handed the card.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Navigator qualifies + explores on the live room (the mandatory D-12 checkpoint)</name>
  <action>Present the verification steps below to the navigator, pause execution (blocking gate), then on approval run the confirming queries (rejection edge, lifecycle values, evidence edges, graph_query entity visibility) and record the evidence verbatim in 219-VERIFICATION.md. On issues, file the gap honestly and route the fix to the owning plan - never patch forward here.</action>
  <what-built>The full opportunity follow-through pipeline ran live on ador-ip-test: entities + metadata extracted, eureka statements banked as proposed opportunity nodes, harvest candidates scored and waiting. The qualification card and [Explore] chain are wired and offline-green.</what-built>
  <how-to-verify>
    1. In the ador-ip-test room session, trigger the qualification surface (the SENS-14 offer / qualify-opportunity command). Confirm a REAL F.1 AskUserQuestion card renders (no ASCII box) with the five verbs and the rubric WHY-lines.
    2. Skip one candidate. We verify a REJECTED_BECAUSE edge landed (we run the query and show you the row).
    3. Qualify >=1 candidate that looks genuinely intersectional (not an artifact-vs-artifact restatement). We verify lifecycle=qualified + the bank entry.
    4. Trigger [Explore] on the qualified opportunity (explicit - confirm it did NOT fire on its own at qualify time). The chain runs; material steps will ask you at their gates.
    5. Inspect the produced artifact at ador-ip-test/opportunity-bank/<section>/<name>/<name>.md: does it read as ANALYZED (governing thought + SCQA + cited web sources), not a one-liner?
    6. We then show you: >=2 typed evidence edges on the opportunity node, lifecycle=explored, and the D-16 proof - the explored artifact's extracted entities visible via graph_query with DERIVED_FROM edges to the artifact node.
  </how-to-verify>
  <resume-signal>Type "approved" (evidence gets recorded verbatim in 219-VERIFICATION.md) or describe what is wrong (we file the gap honestly and route the fix to the owning plan)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev pipeline -> live user room | real room data is touched; mistakes are visible and persistent |
| explore chain -> web (live) | the first LIVE egress exercise of the deep_research leg this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-22 | Information Disclosure | live room content egressing during the explore run | mitigate | frozen deep_research reach with generic handles (Plan 05 gates, offline-proven first); Part 8 boundary scan green before the live run |
| T-219-23 | Tampering | live room corrupted by a partial batch | mitigate | all writes batch-transactional (D-05); PRE-state recorded so any damage is diffable; proposed-only writes are reversible by review status |
| T-219-24 | Repudiation | "it worked" without evidence | mitigate | every step's queries + outputs recorded verbatim in 219-VERIFICATION.md; navigator confirmation is the recorded sign-off |
| T-219-SC | Tampering | package installs | accept (n/a) | zero installs this phase |
</threat_model>

<verification>
- 219-VERIFICATION.md contains: offline gate table, PRE/POST live state, the 218-class check verdict, navigator checkpoint record
- SPEC acceptance 6: >=1 explored Minto artifact in the live bank traceable to graph evidence
</verification>

<success_criteria>
- LIVE ador-ip-test end-to-end PASS recorded with evidence (fixture-green alone is FAIL by definition)
- Navigator has approved the card flow and the explored artifact quality
- D-16 live proof: explored artifact entities visible via graph_query
</success_criteria>

## Artifacts this plan produces

- .planning/phases/219-.../219-VERIFICATION.md (offline gates + live PRE/POST evidence + navigator checkpoint record; corepower section stubbed for Plan 07)
- Live room state: ador-ip-test opportunity nodes (proposed + >=1 qualified + >=1 explored), REJECTED_BECAUSE edge(s), populated opportunity-bank/ nested artifact, research/ corpus artifact

<output>
Create `.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-06-SUMMARY.md` when done
</output>
