---
phase: 101-selector-library-jtbd-aware
plan: 05
type: execute
wave: 2
depends_on: ["101-04"]
files_modified:
  - lib/hmi/selector-dispatcher.cjs
  - lib/hmi/shape-f6-renderer.cjs
  - lib/hmi/shape-g-renderer.cjs
  - lib/hmi/shape-h-renderer.cjs
  - tests/test-shape-f6-mode-b.cjs
autonomous: true
requirements:
  - HMI-101-05
must_haves:
  truths:
    - "Tier check at dispatcher entry; mode = (tier >= 2) ? 'A' : 'B'"
    - "Mode B: RECOMMENDED ▶ marker NEVER rendered on any shape (F.6, G, H)"
    - "Mode B: Zone 1 prefix line 'Brain unreachable; running on local graph only.' rendered before any output"
    - "Mode A regression check: `node tests/test-shape-f6.cjs` exits 0 after 101-05 lands"
    - "selector-dispatcher.cjs imports the tier helper from a single resolved path documented in the in-source comment block from Task 1"
---

<objective>
Wire Canon Part 3 Mode A/B graceful degradation across F.6, G, H. Defense-in-depth: tier check at dispatcher level + per-renderer suppression. RECOMMENDED markers and the Brain-unreachable warning are the only Mode-A/B-divergent visual elements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-selector-library-jtbd-aware/101-CONTEXT.md
@.planning/phases/101-selector-library-jtbd-aware/101-RESEARCH.md
@docs/MINDRIAN-CANON.md
@.planning/research/navigation-engine-brain-interface.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locate tier-check helper and document the contract</name>
  <files>(read-only investigation)</files>
  <action>
Find the tier-check primitive Phase 90 already shipped per `docs/CANON-PHASE-MAP.md` Part 3 row "Option generation tier-awareness shipped". Likely at `lib/core/tier-check.cjs`, `scripts/check-brain.cjs`, or imported by Phase 91 navigation engine.

Output: a comment block at top of `lib/hmi/selector-dispatcher.cjs` documenting:
- which helper provides the tier value
- expected return: integer 0|1|2|3
- mode mapping: tier >= 2 → A; tier 1 → B; tier 0 → degraded (refuse expensive shapes per Canon Part 3 Rule 2)

If no extant helper found, document a v1 stub: `lib/hmi/tier-check.cjs` returning `2` if `process.env.MINDRIAN_BRAIN_KEY` set, else `1`. Real wiring deferred to a Phase 90 follow-on. (This stub is acceptable because Phase 100 + 101 ship before v1.13.0 release gate per KICKOFF status note.)
  </action>
  <verify>
    <automated>test -f lib/core/tier-check.cjs || test -f lib/hmi/tier-check.cjs || grep -lr "checkTier\\|getTier" lib/ scripts/ 2>/dev/null | head -1</automated>
  </verify>
  <done>
    - tier-check helper located OR documented stub planned
    - dispatcher comment block written
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire tier check into dispatcher + renderers</name>
  <files>
    - lib/hmi/selector-dispatcher.cjs
    - lib/hmi/shape-f6-renderer.cjs
    - lib/hmi/shape-g-renderer.cjs
    - lib/hmi/shape-h-renderer.cjs
  </files>
  <action>
1. **Dispatcher:** at entry, if `tier` not supplied in args, call `require(<tier-helper-resolved-in-Task-1>).getTier()`. Compute `mode = (tier >= 2) ? 'A' : 'B'`. Pass `mode` to every renderer call.

2. **Per-renderer suppression (defense in depth):**
   - **shape-f6-renderer.cjs:** when `mode === 'B'`, NEVER prefix any verb with `▶` regardless of `recommendedVerb` argument. Internal guard: `const marker = (mode === 'A' && verb === recommendedVerb) ? '▶' : '▷';`.
   - **shape-g-renderer.cjs:** Mode B has no RECOMMENDED concept; verify Zone 4 footer doesn't carry a Brain-derived `▶` mark. Make the footer marker logic mode-aware (defaults to `▶` on first verb in Mode A; in Mode B all verbs prefix with `▷`).
   - **shape-h-renderer.cjs:** same Zone 4 mode-awareness.

3. **Mode B Zone 1 prefix:** dispatcher prepends a single line BEFORE the renderer's Zone 1 header when `mode === 'B'`:
   ```
   ⚠ Brain unreachable; running on local graph only.
   ```
   Color: yellow per SKILL.md §4. The prefix is a separate line; the underlying Zone 1 header from the renderer follows.

4. **Tier 0 refuse path:** if `tier === 0`, dispatcher returns `{ shape: 'error', rendered: { error: 'tier-0-refused', detail: '/mos:setup brain or graph required for selectors' } }`. Per Canon Part 3 Rule 2.
  </action>
  <verify>
    <automated>node -e "const d=require('./lib/hmi/selector-dispatcher.cjs'); const tmp=require('fs').mkdtempSync('/tmp/modeb-'); require('fs').writeFileSync(tmp+'/.room-root',''); require('./lib/hmi/jtbd-state.cjs').setCurrent(tmp,{jtbd:'find-bottleneck',confidence:0.8,evidence:['x'],trigger:'manual',manual:true}); const r=d.pickShape({requestedShape:'F',roomDir:tmp,tier:1,payload:{recommendedVerb:'Run Methodology'}}); const txt=JSON.stringify(r); if (txt.includes('▶')) { console.error('Mode B leaked RECOMMENDED:',txt.slice(0,200)); process.exit(1); } if (!txt.includes('Brain unreachable')) { console.error('Mode B Zone 1 prefix missing'); process.exit(2); } require('fs').rmSync(tmp,{recursive:true}); console.log('Mode B OK');"</automated>
  </verify>
  <done>
    - smoke case: F.6 with tier=1 has zero ▶ markers AND has Brain-unreachable Zone 1 prefix
    - tier=0 returns refuse error
    - tier>=2 unaffected (Mode A behavior preserved)
  </done>
</task>

<task type="auto">
  <name>Task 3: Fill in tests/test-shape-f6-mode-b.cjs</name>
  <files>tests/test-shape-f6-mode-b.cjs (replace stub)</files>
  <action>
IIFE harness. Assertions:

1. **mode_b_no_recommend:** F.6 with `tier:1, recommendedVerb:'Run Methodology'` → output contains zero `▶` markers.
2. **zone1_prefix:** F.6 with `tier:1` → output Zone 1 contains `Brain unreachable; running on local graph only.`.
3. **mode_a_recommend_preserved:** F.6 with `tier:2, recommendedVerb:'Run Methodology'` → output contains exactly 1 `▶` marker on the Run Methodology verb. (Sanity check: Mode A behavior preserved.)
4. **mode_a_no_prefix:** F.6 with `tier:2` → Zone 1 has NO Brain-unreachable prefix.
5. **g_mode_b:** Shape G with `tier:1` → Zone 4 footer marker is `▷` not `▶` on all verbs.
6. **h_mode_b:** Shape H with `tier:1` → same Zone 4 contract.
7. **tier_0_refuse:** dispatcher with `tier:0` returns `{ shape: 'error', rendered: { error: 'tier-0-refused' } }`.

Exit 0 on full pass.
  </action>
  <verify>
    <automated>node tests/test-shape-f6-mode-b.cjs</automated>
  </verify>
  <done>
    - 7 assertions pass
    - test exits 0
  </done>
</task>

</tasks>

<verification>
- `node tests/test-shape-f6-mode-b.cjs` exits 0
- `node tests/test-selector-dispatcher.cjs` exits 0 (regression check)
- `node tests/test-shape-f6.cjs` exits 0 (regression check)
- `grep "Brain unreachable" lib/hmi/selector-dispatcher.cjs` returns >= 1 line
</verification>

<success_criteria>
- Mode A / Mode B graceful degradation wired per Canon Part 3
- Defense in depth: dispatcher + per-renderer guards
- Tier 0 refuse path implemented
- All Phase 101 regression tests pass
</success_criteria>

<output>Create `.planning/phases/101-selector-library-jtbd-aware/101-05-SUMMARY.md`</output>
