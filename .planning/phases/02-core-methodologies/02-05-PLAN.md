---
phase: 02-core-methodologies
plan: 05
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - hooks/hooks.json
  - scripts/classify-insight.sh
  - skills/room-passive/SKILL.md
  - commands/help.md
autonomous: true
requirements: [PASS-01, PASS-02, PASS-03]

must_haves:
  truths:
    - "PostToolUse hook fires after Write operations to room/ directory"
    - "Keyword classification script identifies correct Data Room section for filed artifacts"
    - "Room passive skill enhanced to handle uncertain classification cases"
    - "Larry ALWAYS confirms before filing -- never files silently"
    - "Every filed artifact has provenance metadata in YAML frontmatter"
    - "Help command updated to recommend based on problem type and room state (no more 'coming soon')"
  artifacts:
    - path: "hooks/hooks.json"
      provides: "Updated hooks with PostToolUse entry"
      contains: "PostToolUse"
    - path: "scripts/classify-insight.sh"
      provides: "Keyword-based room section classification"
      contains: "SUGGEST.*MEDIUM"
    - path: "skills/room-passive/SKILL.md"
      provides: "Enhanced room-passive skill with filing logic"
      contains: "filing"
    - path: "commands/help.md"
      provides: "Updated help with methodology recommendations"
      contains: "diagnose"
  key_links:
    - from: "hooks/hooks.json"
      to: "scripts/classify-insight.sh"
      via: "PostToolUse hook invokes classify script"
      pattern: "post-write"
    - from: "skills/room-passive/SKILL.md"
      to: "references/methodology/index.md"
      via: "Filing uses routing index for section mapping"
      pattern: "references/methodology"
    - from: "commands/help.md"
      to: "references/methodology/problem-types.md"
      via: "Help reads problem types for recommendations"
      pattern: "problem-types"
---

<objective>
Build the Passive Room Intelligence system: PostToolUse hook for auto-classification, keyword classification script, enhanced room-passive skill for filing with confirmation, and updated help command that recommends methodologies based on room state and problem type.

Purpose: This completes the passive intelligence layer -- methodology artifacts are automatically classified, filed with user confirmation, and tagged with provenance metadata. The help command transforms from a "coming soon" list into an intelligent recommendation engine based on problem type classification.

Output: Updated hooks.json, new classify-insight.sh script, enhanced room-passive skill, updated help command
</objective>

<execution_context>
@/home/jsagi/.claude/get-shit-done/workflows/execute-plan.md
@/home/jsagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-methodologies/02-RESEARCH.md
@.planning/phases/02-core-methodologies/02-01-SUMMARY.md

@hooks/hooks.json
@skills/room-passive/SKILL.md
@commands/help.md
@references/methodology/index.md

<interfaces>
<!-- Existing hooks structure from Phase 1 -->
From hooks/hooks.json:
```json
{
  "hooks": {
    "SessionStart": [...],
    "Stop": [...]
  }
}
```

<!-- Existing hook runner pattern -->
hooks/run-hook.cmd routes to scripts/ directory (established in Phase 1 Plan 02)

<!-- Classification script pattern from RESEARCH.md -->
Script outputs: "CLASSIFIED:{section}:HIGH", "SUGGEST:{section}:MEDIUM", or "UNCERTAIN"

<!-- Provenance metadata template -->
```yaml
---
methodology: {name}
created: {date}
depth: {quick|deep}
problem_type: {type}
venture_stage: {stage}
room_section: {section}
---
```

<!-- Room sections -->
problem-definition, market-analysis, solution-design, business-model, competitive-analysis, team-execution, legal-ip, financial-model
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PostToolUse hook and classification script</name>
  <files>
    hooks/hooks.json,
    scripts/classify-insight.sh
  </files>
  <action>
    **Step 1: Create classify-insight.sh**
    File: `scripts/classify-insight.sh`
    - Use the EXACT script from RESEARCH.md "Example PostToolUse Hook Script" section as the starting point
    - Make executable (chmod +x)
    - Script takes FILE_PATH as $1 argument
    - First check: if file is NOT in room/ directory, exit 0 (skip)
    - Second check: if file is already in a known room section subdirectory (problem-definition, market-analysis, etc.), output "CLASSIFIED:{section}:HIGH" and exit
    - Third: keyword match on file content (head -50 lines, lowercased) against room section keyword patterns:
      - problem-definition: problem, domain, question, definition, scope, undefined, beautiful.question, ackoff, pyramid
      - market-analysis: market, customer, trend, timing, s.curve, scenario, macro, future, tta, need
      - solution-design: solution, design, system, hierarchy, bottleneck, architecture, hat
      - business-model: model, revenue, canvas, pricing, unit.economics, lean
      - competitive-analysis: competition, competitor, challenge, assumption, validate, evidence, devil, red.team
      - team-execution: team, leadership, hire, capability, execution
      - legal-ip: legal, ip, patent, regulation, compliance
      - financial-model: investment, thesis, financial, funding, valuation, grade, scoring
    - If keyword match found: "SUGGEST:{section}:MEDIUM"
    - If no match: "UNCERTAIN"
    - Script MUST complete in under 100ms (simple grep, no heavy processing)

    **Step 2: Update hooks.json**
    Read current `hooks/hooks.json`. Add a PostToolUse entry that fires after Write tool operations:
    ```json
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" post-write",
            "timeout": 3000
          }
        ]
      }
    ]
    ```
    Preserve existing SessionStart and Stop hooks exactly as they are. Add PostToolUse as a new sibling.

    **Step 3: Create post-write hook handler**
    The `run-hook.cmd` script (from Phase 1) already routes to scripts/ based on the first argument. Verify that a "post-write" case exists or that the routing handles it. If run-hook.cmd needs updating to support the new "post-write" argument, update it to call `scripts/classify-insight.sh` with the written file path.
    Note: Check how PostToolUse passes the file path to the hook command -- it may be via environment variable (e.g., $TOOL_INPUT_PATH) rather than command argument. The script should handle both patterns.
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && test -f scripts/classify-insight.sh && test -x scripts/classify-insight.sh && test -f hooks/hooks.json && grep -q "PostToolUse" hooks/hooks.json && grep -q "post-write" hooks/hooks.json && grep -q "SessionStart" hooks/hooks.json && echo "test content with market trend" | bash scripts/classify-insight.sh /dev/stdin 2>/dev/null; echo "PASS: Hook and classification script created"</automated>
  </verify>
  <done>
    - classify-insight.sh exists, is executable, and classifies content by keyword matching
    - hooks.json has PostToolUse entry that fires on Write operations
    - Existing SessionStart and Stop hooks preserved unchanged
    - Script runs in under 100ms for typical inputs
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance room-passive skill and update help command</name>
  <files>
    skills/room-passive/SKILL.md,
    commands/help.md
  </files>
  <action>
    **Step 1: Enhance room-passive skill**
    File: `skills/room-passive/SKILL.md`
    Read current content. Upgrade from Phase 1 read-only awareness to active filing support:

    Keep existing behavior (room structure awareness, empty room suggestions).
    ADD filing intelligence:
    - When a methodology session produces an artifact, Larry suggests the correct room section based on the methodology's default room (from references/methodology/index.md)
    - Confirm-then-file UX: Larry shows artifact summary, suggests "File this to {section}?", waits for user confirmation or redirect
    - If PostToolUse hook reports "UNCERTAIN" classification, room-passive skill helps Larry classify by analyzing artifact content in conversation context
    - Provenance metadata: remind Larry to include YAML frontmatter with methodology, created date, depth, problem_type, venture_stage, room_section
    - Cross-room references: when an artifact has relevance to multiple rooms, note the primary filing and mention secondary relevance

    Keep total SKILL.md under 400 tokens. This is auto-loaded -- must stay lean.

    **Step 2: Update help command**
    File: `commands/help.md`
    Read current content. Major update:

    - Remove ALL "Coming Soon" / "coming in next update" language -- all 26 commands exist now
    - Add problem type awareness: help command reads `references/methodology/problem-types.md` in addition to the routing index
    - Add `/mindrian-os:diagnose` as a recommended starting point for users who don't know where to begin
    - Update the "Recommendations by State" section to reference actual working commands (not "coming soon")
    - Update the `--all` flag section to list ALL 26 methodology commands grouped by venture stage, without "coming soon" markers
    - Add methodology count: "26 methodology commands available"
    - Keep Larry's voice and conversational framing
    - Keep the `disable-model-invocation` NOT set on help (help is a utility command that should be quick)

    Key changes to help text:
    - Pre-Opportunity: recommend explore-domains, beautiful-question, diagnose
    - Discovery: recommend analyze-needs, explore-trends, map-unknowns
    - Validation: recommend challenge-assumptions, validate, think-hats
    - Design: recommend structure-argument, scenario-plan, systems-thinking
    - Investment: recommend grade, build-thesis, score-innovation
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && grep -q "filing" skills/room-passive/SKILL.md && grep -q "confirm" skills/room-passive/SKILL.md && grep -q "provenance" skills/room-passive/SKILL.md && ! grep -q "coming soon" commands/help.md && ! grep -qi "coming soon" commands/help.md && grep -q "diagnose" commands/help.md && echo "PASS: Room-passive enhanced and help updated"</automated>
  </verify>
  <done>
    - Room-passive skill handles filing intelligence with confirm-then-file UX
    - Room-passive skill handles uncertain PostToolUse classifications
    - Room-passive skill reminds Larry about provenance metadata
    - Help command lists all 26 commands without "coming soon" markers
    - Help command recommends /mindrian-os:diagnose for users who don't know where to start
    - Help recommendations updated by venture stage with working commands
  </done>
</task>

</tasks>

<verification>
1. PostToolUse hook fires after Write operations (check hooks.json structure)
2. classify-insight.sh correctly classifies sample content to expected room sections
3. Room-passive skill includes filing logic with confirm-then-file pattern
4. Help command has zero "coming soon" references
5. Help command includes diagnose in recommendations
6. Provenance metadata format documented in room-passive skill
7. Full hybrid classification flow works: hook keyword match -> high confidence (Larry mentions) / uncertain (Larry asks via room-passive)
</verification>

<success_criteria>
- Passive Room intelligence operational: methodology artifacts auto-classified and filed with user confirmation
- Every filed artifact includes provenance metadata (PASS-03)
- PostToolUse hook classifies via keyword matching, defers uncertain to room-passive skill (PASS-01, PASS-02)
- Help command is a working methodology recommendation engine (no "coming soon")
- Phase 2 fully complete: all 26 commands + passive intelligence + problem type routing
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-methodologies/02-05-SUMMARY.md`
</output>
