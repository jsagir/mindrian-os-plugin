---
phase: 02-core-methodologies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/methodology/problem-types.md
  - references/methodology/index.md
  - commands/beautiful-question.md
  - commands/map-unknowns.md
  - commands/challenge-assumptions.md
  - commands/analyze-systems.md
  - commands/leadership.md
  - commands/lean-canvas.md
  - commands/systems-thinking.md
  - references/methodology/beautiful-question.md
  - references/methodology/map-unknowns.md
  - references/methodology/challenge-assumptions.md
  - references/methodology/analyze-systems.md
  - references/methodology/leadership.md
  - references/methodology/lean-canvas.md
  - references/methodology/systems-thinking.md
  - skills/pws-methodology/SKILL.md
autonomous: true
requirements: [METH-05, METH-08, METH-09, METH-10, ALLM-01, ALLM-02]

must_haves:
  truths:
    - "User can invoke /mindrian-os:beautiful-question and get a Larry-guided Beautiful Question session"
    - "User can invoke /mindrian-os:map-unknowns and get a Larry-guided Known/Unknown Matrix session"
    - "User can invoke all 5 Tier 2 commands (challenge-assumptions, analyze-systems, leadership, lean-canvas, systems-thinking)"
    - "Each command file is under 500 tokens with disable-model-invocation: true"
    - "Each reference file has framework phases, artifact template, voice section, cross-references, quick/deep calibration"
    - "Routing index updated to show all 26 commands as Phase 2 (no Phase 4 section)"
    - "Problem type classification reference exists with 2D matrix and routing table"
  artifacts:
    - path: "references/methodology/problem-types.md"
      provides: "Problem type classification reference with 2D matrix"
      contains: "undefined.*ill-defined.*well-defined"
    - path: "references/methodology/index.md"
      provides: "Updated routing index with all 26 commands"
      contains: "Phase 2"
    - path: "commands/beautiful-question.md"
      provides: "Tier 1 command -- pattern validation"
      contains: "disable-model-invocation: true"
    - path: "commands/lean-canvas.md"
      provides: "Tier 2 command -- Lean Canvas"
      contains: "disable-model-invocation: true"
  key_links:
    - from: "commands/beautiful-question.md"
      to: "references/methodology/beautiful-question.md"
      via: "Read instruction in command file"
      pattern: "Read.*references/methodology/beautiful-question"
    - from: "skills/pws-methodology/SKILL.md"
      to: "references/methodology/index.md"
      via: "Routing index reference"
      pattern: "references/methodology/index"
---

<objective>
Build the three-file methodology pattern (command + reference + routing index entry) validated on Tier 1-2 commands (7 simplest methodologies), plus the problem type classification reference and updated routing index for all 26 commands.

Purpose: Establishes and validates the pattern that all remaining 19 methodology commands will follow. The simplest commands (Tier 1: beautiful-question, map-unknowns; Tier 2: challenge-assumptions, analyze-systems, leadership, lean-canvas, systems-thinking) prove the pattern works before batch-producing complex tiers.

Output: 7 command files, 7 reference files, updated routing index, problem-types reference, updated pws-methodology skill
</objective>

<execution_context>
@/home/jsagi/.claude/get-shit-done/workflows/execute-plan.md
@/home/jsagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-methodologies/02-RESEARCH.md

@references/methodology/index.md
@skills/pws-methodology/SKILL.md
@commands/help.md
@references/personality/voice-dna.md

<interfaces>
<!-- Existing command pattern from Phase 1 -->
From commands/new-project.md:
```yaml
---
name: new-project
description: Start a new venture project with Larry
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
---
```

From skills/pws-methodology/SKILL.md:
```markdown
## Brain-First, References-Fallback
Check for Brain MCP tools first. If not connected, load references/methodology/index.md
```

<!-- Research-defined templates for this plan -->
Command file template: see 02-RESEARCH.md "Command File Template" section
Reference file template: see 02-RESEARCH.md "Reference File Template" section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create infrastructure files and Tier 1 pattern validation (beautiful-question + map-unknowns)</name>
  <files>
    references/methodology/problem-types.md,
    references/methodology/index.md,
    skills/pws-methodology/SKILL.md,
    commands/beautiful-question.md,
    references/methodology/beautiful-question.md,
    commands/map-unknowns.md,
    references/methodology/map-unknowns.md
  </files>
  <action>
    **Step 1: Create problem-types.md** -- the 2D classification reference.
    File: `references/methodology/problem-types.md`
    Content: The two-dimensional classification matrix from RESEARCH.md:
    - Axis 1 (Definition): Undefined, Ill-defined, Well-defined
    - Axis 2 (Complexity/Cynefin): Simple, Complicated, Complex, Wicked
    - Methodology routing table mapping each quadrant to recommended commands
    - Evolution rules: how problem type changes as Room sections fill (starts undefined, reclassifies progressively)
    - Brain-ready interface section: placeholder for Phase 4 ADDRESSES_PROBLEM_TYPE integration
    - Keep under 3000 tokens. This is a reference file loaded on demand by /mindrian-os:diagnose (Tier 5, later plan).

    **Step 2: Update routing index** -- change ALL commands to Phase 2.
    File: `references/methodology/index.md`
    Rewrite to:
    - Remove "Phase 2 Core" vs "Phase 4 Extended" split -- ALL 26 commands (25 methodologies + diagnose) are Phase 2
    - Add missing commands: lean-canvas, systems-thinking, score-innovation, diagnose
    - Keep venture stage routing table (Pre-Opportunity, Discovery, Validation, Design, Investment)
    - Keep Brain Enhancement section
    - Remove "Coming Soon" markers -- all commands are being built now
    - Keep under 2000 tokens

    **Step 3: Update pws-methodology skill** -- remove Phase 2/4 split language.
    File: `skills/pws-methodology/SKILL.md`
    Update to say "26 methodology commands available" instead of "8 in Phase 2, 14 in Phase 4".
    Keep Brain-first/references-fallback pattern. Keep under 300 tokens.

    **Step 4: Create Tier 1 command + reference files.**
    For each of beautiful-question and map-unknowns, create TWO files:

    **Command file** (`commands/{name}.md`):
    - YAML frontmatter: name, description, allowed-tools (Read, Write, Bash, Glob), `disable-model-invocation: true`
    - Body: "You are Larry" intro, setup steps (read reference file, read voice-dna, read STATE.md), session flow ("Quick pass or deep dive?"), artifact output instructions (file naming, provenance frontmatter), filing with confirmation, chaining suggestion
    - MUST be under 500 tokens total. The command file is THIN -- it tells Claude what to do, not how the framework works.
    - Use the EXACT template from 02-RESEARCH.md "Command File Template" section

    **Reference file** (`references/methodology/{name}.md`):
    - Framework Overview (1 paragraph)
    - The Voice section (3-4 signature Larry phrases specific to THIS methodology + anti-patterns to catch)
    - Phases section (the framework's actual teaching flow)
    - Artifact Template (markdown with YAML frontmatter provenance)
    - Default Room
    - Cross-References (which methodologies chain from this one)
    - Quick Pass vs Deep Dive calibration
    - Target 2000-4000 tokens

    **beautiful-question specifics** (from RESEARCH.md example):
    - 3 phases: WHY (turns 1-3), WHAT IF (turns 3-5), HOW (turns 5+)
    - Voice: "That's a good question. But there's a better one hiding underneath it."
    - Default room: problem-definition
    - Cross-refs: explore-domains, challenge-assumptions, explore-trends

    **map-unknowns specifics** (from V2 knowns.py):
    - 4-quadrant matrix: Known-Knowns, Known-Unknowns, Unknown-Knowns, Unknown-Unknowns
    - Voice: "The dangerous quadrant isn't what you don't know -- it's what you think you know that isn't true."
    - Phases: Surface knowns, Map unknowns, Reveal hidden assumptions, Identify blind spots
    - Default room: problem-definition
    - Cross-refs: challenge-assumptions, explore-domains, root-cause
    - Port from V2 `knowns.py` -- strip File Search tiers, structured JSON, temperature settings. Keep phase structure, voice, anti-patterns.

    Source material for porting: Read `/home/jsagi/MindrianV2/prompts/knowns.py` and `/home/jsagi/MindrianV2/prompts/beautiful_question.py` for framework content. Extract phase structure, signature phrases, anti-patterns, homework. Discard: File Search references, structured JSON output, temperature, CopilotKit patterns, mode-aware progression ranges.
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && test -f references/methodology/problem-types.md && test -f references/methodology/index.md && test -f commands/beautiful-question.md && test -f references/methodology/beautiful-question.md && test -f commands/map-unknowns.md && test -f references/methodology/map-unknowns.md && grep -q "disable-model-invocation: true" commands/beautiful-question.md && grep -q "disable-model-invocation: true" commands/map-unknowns.md && ! grep -q "Phase 4" references/methodology/index.md && wc -c commands/beautiful-question.md | awk '{if ($1 < 2500) exit 0; else exit 1}' && echo "PASS"</automated>
  </verify>
  <done>
    - problem-types.md exists with 2D classification matrix and routing table
    - Routing index lists all 26 commands as Phase 2 (no Phase 4 section)
    - pws-methodology skill updated to reflect 26 commands
    - beautiful-question and map-unknowns each have thin command file (under 500 tokens) + detailed reference file
    - All command files have disable-model-invocation: true
    - Reference files have framework phases, voice section, artifact template, cross-references
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Tier 2 methodology batch (5 commands: challenge-assumptions, analyze-systems, leadership, lean-canvas, systems-thinking)</name>
  <files>
    commands/challenge-assumptions.md,
    references/methodology/challenge-assumptions.md,
    commands/analyze-systems.md,
    references/methodology/analyze-systems.md,
    commands/leadership.md,
    references/methodology/leadership.md,
    commands/lean-canvas.md,
    references/methodology/lean-canvas.md,
    commands/systems-thinking.md,
    references/methodology/systems-thinking.md
  </files>
  <action>
    Create command + reference file pairs for all 5 Tier 2 methodologies using the EXACT same pattern validated in Task 1.

    For EACH methodology, create two files following the templates from Task 1:

    **1. challenge-assumptions** (V2: redteam.py)
    - Read `/home/jsagi/MindrianV2/prompts/redteam.py` for source content
    - 4 phases + 4 killing questions + pre-mortem exercise
    - Voice: adversarial, push-back, "What if you're wrong?"
    - Default room: competitive-analysis
    - Cross-refs: think-hats, validate, build-thesis
    - Artifact: Pre-mortem template with assumptions table, killing questions, risk matrix

    **2. analyze-systems** (V2: nested_hierarchies.py)
    - Read `/home/jsagi/MindrianV2/prompts/nested_hierarchies.py` for source content
    - 5 phases: system boundary, decompose levels, identify interfaces, find leverage points, synthesis
    - Voice: "Zoom out. Now zoom in. What changed?"
    - Default room: solution-design
    - Cross-refs: find-bottlenecks, root-cause, systems-thinking
    - Artifact: System hierarchy diagram (text), level descriptions, leverage points

    **3. leadership** (V2: leadership.py)
    - Read `/home/jsagi/MindrianV2/prompts/leadership.py` for source content
    - Turn-based coaching, no quantitative output
    - Voice: coaching, reflective, "What would your team say about this?"
    - Default room: team-execution
    - Cross-refs: challenge-assumptions, build-thesis
    - Artifact: Leadership assessment with strengths, gaps, action items

    **4. lean-canvas** (NEW -- no V2 source)
    - Standard Lean Canvas 9-box: Problem, Customer Segments, Unique Value Prop, Solution, Channels, Revenue Streams, Cost Structure, Key Metrics, Unfair Advantage
    - Apply Larry's voice: challenging, evidence-focused ("What's your actual unfair advantage? Not 'we're passionate' -- that's not an advantage.")
    - Default room: business-model
    - Cross-refs: analyze-needs, structure-argument, build-thesis
    - Artifact: 9-box canvas in markdown table format

    **5. systems-thinking** (V2: pws_systems_thinker.py)
    - Read `/home/jsagi/MindrianV2/prompts/pws_systems_thinker.py` for source content
    - Feedback loops, stocks/flows, delays, emergent behavior
    - Voice: "Everything is connected. Your job is to figure out which connections matter."
    - Default room: solution-design
    - Cross-refs: analyze-systems, find-bottlenecks, scenario-plan
    - Artifact: System map with feedback loops, stocks, flows, delays

    **CRITICAL porting rules for all V2 files:**
    - STRIP: File Search tiers (T1_Core etc), structured JSON output blocks, temperature settings, CopilotKit patterns, mode-aware progression ranges, Gemini model instructions
    - KEEP: Phase structure, Larry's per-methodology voice and signature phrases, anti-patterns/catch-and-correct, homework assignments, cross-framework connections
    - Each command file MUST have `disable-model-invocation: true` and be under 500 tokens
    - Each reference file should be 2000-5000 tokens with full framework detail
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && for cmd in challenge-assumptions analyze-systems leadership lean-canvas systems-thinking; do test -f "commands/${cmd}.md" || { echo "MISSING: commands/${cmd}.md"; exit 1; }; test -f "references/methodology/${cmd}.md" || { echo "MISSING: references/methodology/${cmd}.md"; exit 1; }; grep -q "disable-model-invocation: true" "commands/${cmd}.md" || { echo "NO DISABLE: ${cmd}"; exit 1; }; done && echo "PASS: All 5 Tier 2 command+reference pairs created"</automated>
  </verify>
  <done>
    - All 5 Tier 2 methodologies have thin command file + detailed reference file
    - All command files have disable-model-invocation: true and are under 500 tokens
    - All reference files have phases, voice, artifact template, cross-references, quick/deep
    - V2 content ported with platform artifacts stripped, teaching content preserved
    - lean-canvas created from scratch with Larry's voice applied to standard 9-box framework
  </done>
</task>

</tasks>

<verification>
Pattern validation checklist:
1. Every command file has YAML frontmatter with name, description, allowed-tools, disable-model-invocation: true
2. Every command file instructs Claude to read the corresponding reference file on-demand
3. Every reference file has: Framework Overview, The Voice, Phases, Artifact Template, Default Room, Cross-References, Quick Pass vs Deep Dive
4. Routing index lists all 26 commands without "Phase 4" or "Coming Soon" labels
5. Problem types reference has 2D matrix with methodology routing per quadrant
6. No V2 artifacts remain (no File Search tiers, no structured JSON, no temperature settings)
</verification>

<success_criteria>
- 7 methodology commands invocable as slash commands
- Each produces Larry-voiced session with framework-specific guidance
- Routing index and pws-methodology skill updated for full 26-command scope
- Problem type classification reference ready for /mindrian-os:diagnose (Plan 02-04)
- Pattern validated: subsequent plans can batch-produce remaining commands using identical structure
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-methodologies/02-01-SUMMARY.md`
</output>
