---
phase: 225-per-session-room-binding-and-multi-session-reconciliation-se
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/doctor.cjs
  - tests/test-225-wal-advisory.cjs
autonomous: true
requirements: [REQ-4]
must_haves:
  truths:
    - "doctor --bind-check on a room with a live co-session, under bundled SQLite < 3.51.3, prints a WARN finding that names 3.51.3 and the WAL-reset window"
    - "The advisory NEVER changes the exit code (always 0) and NEVER flips report.healthy - it is a findings-row watch item, not a health verdict"
    - "No live co-session, or SQLite >= 3.51.3, produces no WAL finding"
    - "A fault in the version probe or the presence read degrades to no-finding (never-block, never-crash)"
  artifacts:
    - path: "scripts/doctor.cjs"
      provides: "_walResetAdvisory helper with injectable seams + --bind-check wiring"
      contains: "_walResetAdvisory"
    - path: "tests/test-225-wal-advisory.cjs"
      provides: "unit test via injected seams + e2e never-block spawn"
      min_lines: 40
  key_links:
    - from: "scripts/doctor.cjs (_walResetAdvisory)"
      to: "lib/core/session-presence.cjs"
      via: "lazy require + hasCoSession inside try/catch"
      pattern: "hasCoSession"
    - from: "scripts/doctor.cjs (_walResetAdvisory)"
      to: "node:sqlite"
      via: "DatabaseSync(':memory:') sqlite_version() probe"
      pattern: "sqlite_version"
---

<objective>
Close the second Phase-225 gap: the Phase-218 WAL-reset corruption window (upstream
SQLite 3.7.0-3.51.2 checkpoint race, fixed in 3.51.3, commit 298a1c84) is code-unfixable
here - Node bundles SQLite - and the current environment (bundled 3.51.2, per
225-RESEARCH.md Environment Availability) IS inside the affected range. The correct and
only action is detection: a cheap, advisory doctor finding that fires when bundled
SQLite < 3.51.3 AND a live co-session is present in the room, riding the existing
bind-time --bind-check cadence (Phase 217 doctor discipline: diagnostics live in doctor,
never a bespoke side-channel; research anti-pattern: never per-turn).

Planning decision PD-2 (resolves 225-RESEARCH.md Open Question 2): NO extraction-worker
hasCoSession guard ships in this phase - advisory only. Rationale: (a) the research
recommendation is start-with-advisory; (b) dependency constraint - the drain worker files
are Phase 224's in-flight surface (224-02 executed, 224-03/04 pending), so adding a guard
there now creates a live cross-phase file collision; (c) the SQLITE_BUSY leg is already
fixed (218 D-05) and the WAL-reset leg is upstream - a worker guard would shrink but not
close the window. Revisit only if the navigator asks for belt-and-suspenders after 224
lands.

Purpose: the navigator learns the corruption window is open in THIS room right when a
second session binds - the exact moment the 2-connection precondition becomes true.
Output: _walResetAdvisory in scripts/doctor.cjs, wired into --bind-check, plus its test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/225-per-session-room-binding-and-multi-session-reconciliation-se/225-RESEARCH.md
@.planning/phases/225-per-session-room-binding-and-multi-session-reconciliation-se/225-PATTERNS.md
@scripts/doctor.cjs
@lib/core/session-presence.cjs
</context>

## Artifacts this phase produces (plan 02)

| Artifact | Kind | Where |
|----------|------|-------|
| `_walResetAdvisory(opts)` | new exported helper function | scripts/doctor.cjs |
| `_sqliteVersionLt(a, b)` | new helper function (numeric segment compare, zero deps) | scripts/doctor.cjs |
| WAL advisory wiring | new call site inside the existing `flags.bindCheck` block | scripts/doctor.cjs |
| `tests/test-225-wal-advisory.cjs` | new test file | tests/ |

<tasks>

<task type="auto">
  <name>Task 1: Add _walResetAdvisory to doctor.cjs and wire it into --bind-check</name>
  <files>scripts/doctor.cjs</files>
  <read_first>
    - scripts/doctor.cjs lines 2567-2620 (the flags.bindCheck block - the never-block analog this check joins: report shape, findings array, json/human output, unconditional process.exit(0)), lines 1385-1395 (the WARN-not-fail finding-string idiom), line 3707 (module.exports - the additive append target), lines 3702-3711 (require.main guard proving doctor.cjs is require-safe for unit tests)
    - .planning/phases/225-per-session-room-binding-and-multi-session-reconciliation-se/225-PATTERNS.md (doctor section: the exact never-block contract and the RESEARCH-prescribed node:sqlite version probe)
    - lib/core/session-presence.cjs lines 200-215 (hasCoSession signature and its opts shape, listLiveCoSessions semantics, the advisory-by-construction comment)
  </read_first>
  <action>
    Two new functions plus one wiring edit in scripts/doctor.cjs, all additive:

    1. `_sqliteVersionLt(a, b)`: compares two dotted numeric version strings by numeric
    segments (split on '.', parseInt each, compare left to right, shorter side padded
    with 0). Returns true when a < b. No semver package - zero new dependencies
    (CLAUDE.md: CJS only, node built-ins only). Guard non-string/garbage input by
    returning false (an unparseable version never fires the advisory).

    2. `_walResetAdvisory(opts)`: opts `{ roomDir, sessionId, home, sqliteVersion,
    hasCoSession }` where sqliteVersion and hasCoSession are injectable test seams.
    Production defaults inside try/catch: sqliteVersion from
    `new (require('node:sqlite').DatabaseSync)(':memory:')` prepared with
    `select sqlite_version() as v` (the 225-RESEARCH.md Code Examples probe, verbatim);
    hasCoSession from a lazy require of lib/core/session-presence.cjs, called with the
    opts shape session-presence actually expects (read it - roomDir plus the self
    session id so the CURRENT binding session does not count itself as its own
    co-session). Fire condition: `_sqliteVersionLt(version, '3.51.3')` AND
    hasCoSession(...) is true. On fire, return a single finding string following the
    doctor WARN idiom, e.g. 'WARN: bundled SQLite <version> is inside the WAL-reset
    corruption window (fixed in 3.51.3) and a live co-session is present in this room;
    concurrent checkpoints can corrupt room.db (Phase 218 finding, commit 298a1c84)'.
    On no-fire OR ANY fault (version probe throws, node:sqlite absent, presence read
    fails), return null. The entire body is one try/catch returning null in catch -
    the advisory can never crash or block a bind.

    3. Wiring: inside the existing `if (flags.bindCheck)` block, AFTER `report` is built
    (and after the room-health-cache persist, so the statusline glyph mapping sees the
    UNMODIFIED report), call _walResetAdvisory({ roomDir, sessionId, home }) and, when
    it returns a string, push it onto `report.findings` (creating the array if absent).
    Do NOT touch `report.healthy` - the WAL window is an environment watch item, not a
    room-structural verdict, and flipping healthy would degrade the statusline
    room-health glyph for a condition the user cannot repair locally. The block's
    unconditional `process.exit(0)` stays byte-identical (never-block, T-194-19).

    4. Append `_walResetAdvisory` (and `_sqliteVersionLt`) to the module.exports object
    at line 3707 - additive append, never reorder existing keys (the repo's
    additive-only export idiom; session-binding-consumer already requires doctor.cjs
    and guards on exported names, so additions are safe).

    Comment the helper citing REQ-4, PD-2, Pitfall 2 (the two-issue split: SQLITE_BUSY
    already fixed by 218 D-05; WAL-reset is upstream and detect-only). No em-dashes.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "_walResetAdvisory" scripts/doctor.cjs` >= 3 (definition, bindCheck call site, module.exports entry)
    - Source assertion: the flags.bindCheck block still ends in an unconditional `process.exit(0)` and contains no new write to `report.healthy`
    - Behavior assertion: `node -e "const d=require('./scripts/doctor.cjs'); console.log(d._walResetAdvisory({sqliteVersion:'3.51.2', hasCoSession:()=>true, roomDir:'/tmp/x', sessionId:'s1'}))"` prints a WARN string containing '3.51.3'
    - Behavior assertion: same invocation with sqliteVersion '3.51.3' prints null; with hasCoSession ()=>false prints null; with hasCoSession that throws prints null (never-crash)
    - CLI check: `node scripts/doctor.cjs --bind-check /tmp/nonexistent-room; echo $?` prints 0 (never-block contract intact)
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/doctor.cjs && node -e "const d=require('./scripts/doctor.cjs'); const w=d._walResetAdvisory({sqliteVersion:'3.51.2',hasCoSession:function(){return true;},roomDir:'/tmp/x',sessionId:'s1'}); if(!w||w.indexOf('3.51.3')===-1)throw new Error('advisory did not fire'); if(d._walResetAdvisory({sqliteVersion:'3.51.3',hasCoSession:function(){return true;},roomDir:'/tmp/x',sessionId:'s1'})!==null)throw new Error('fired on fixed version'); console.log('walResetAdvisory seams OK')"</automated>
  </verify>
  <done>
    _walResetAdvisory and _sqliteVersionLt exist, are exported, fire only on
    version < 3.51.3 AND live co-session, return null on any fault, and the
    --bind-check block appends the finding without touching healthy or exit code.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write tests/test-225-wal-advisory.cjs</name>
  <files>tests/test-225-wal-advisory.cjs</files>
  <read_first>
    - scripts/doctor.cjs (the just-added _walResetAdvisory + its bindCheck wiring)
    - tests/test-binding-gate-degrade.test.cjs (the SKIP-safe require + node:assert style precedent named in 225-RESEARCH.md)
    - lib/core/session-presence.cjs (registerPresence + presence file shape under <roomDir>/.mindrian/sessions/, for the e2e leg's fixture)
  </read_first>
  <action>
    Create tests/test-225-wal-advisory.cjs, self-running node:assert (repo convention),
    SKIP-safe if _walResetAdvisory is not yet exported (mirrors
    test-binding-gate-degrade.test.cjs's guard style). Legs:

    1. FIRE: _walResetAdvisory({ sqliteVersion: '3.51.2', hasCoSession: () => true,
    roomDir: <tmp>, sessionId: 's1' }) returns a string containing '3.51.3' and 'WARN'.
    2. NO-FIRE fixed version: sqliteVersion '3.51.3' returns null; also '3.52.0' and
    '4.0.0' return null (segment compare, not string compare - this is the leg that
    catches a lexicographic-compare bug where '3.51.10' < '3.51.3' as strings).
    3. NO-FIRE old version but no co-session: sqliteVersion '3.51.2',
    hasCoSession () => false, returns null.
    4. NEVER-CRASH: hasCoSession that throws returns null; sqliteVersion undefined with
    a hasCoSession () => true either returns null or a string but never throws (the
    production probe path is allowed to resolve the real bundled version).
    5. E2E never-block: build a temp roomDir with a .mindrian/sessions/ presence file
    for a DIFFERENT session id with a live pid (process.pid) and fresh mtime, then spawn
    `node scripts/doctor.cjs --bind-check <tmpRoomDir>` with CLAUDE_SESSION_ID set to
    the self session id; assert exit code 0 regardless of whether the WARN row appears
    (the bundled version is environment-dependent; the exit-0 contract is the
    assertion). If stdout DOES contain the WARN row, additionally assert the first
    output line still starts with 'OK' or 'ADVISORY' (report.healthy untouched by the
    advisory alone is proven in leg 1's pure call; here assert only shape sanity).

    Cleanup temp dirs in finally. No em-dashes. Print PASS on success, exit nonzero on
    assertion failure.
  </action>
  <acceptance_criteria>
    - Test command: `node tests/test-225-wal-advisory.cjs` prints PASS and exits 0 against the Task-1 implementation
    - Behavior assertion: leg 2 proves numeric segment comparison (a '3.51.10'-style version does not false-fire against '3.51.3')
    - Behavior assertion: leg 5 proves `doctor --bind-check` exits 0 with a live co-session fixture present
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-225-wal-advisory.cjs</automated>
  </verify>
  <done>
    The test file exists, runs standalone, covers fire / no-fire / never-crash /
    e2e never-block, and passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room presence files on disk -> doctor | Locally writable JSON presence ledgers are read by the advisory |
| node:sqlite bundled library -> doctor | The version probe opens an in-memory database (no user data) |

Mitigating constraint called out explicitly: this phase is room-local only (Canon
Part 8, no egress). The advisory reads the local presence ledger and an in-memory SQLite
handle only; the --bind-check path is explicitly dispatched BEFORE any Brain smoke
(documented at doctor.cjs:2570-2577) and this change adds zero network tokens.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-225-06 | Denial of Service | a crafted/corrupt presence file crashing the bind-check | mitigate | _walResetAdvisory's whole body is try/catch -> null; hasCoSession is advisory-by-construction (session-presence.cjs); the bindCheck block's unconditional exit(0) is untouched. |
| T-225-07 | Tampering | a forged presence file inducing a false WARN (co-session spoof) | accept | Local-filesystem trust domain (single-user machine, room-local files); worst case is a spurious advisory string, never a block or a data write; no privilege attaches to the WARN. |
| T-225-08 | Information Disclosure | the version probe or finding string egressing data | mitigate | Probe is an in-memory DatabaseSync holding zero user bytes; the finding string carries only a SQLite version literal and a commit hash; Part 8 source-grep floor covers doctor.cjs. |
| T-225-09 | Elevation of Privilege | new exported doctor functions widening the invocable surface | mitigate | No new CLI flag, no new command: the check rides the existing --bind-check door (CIRS Part 11 - no new surface minted; check-shape-declaration stays green). Exports are require-time helpers only. |
| T-225-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (225-RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `node --check scripts/doctor.cjs` passes
- `node tests/test-225-wal-advisory.cjs` passes (all 5 legs)
- `node scripts/doctor.cjs --bind-check /tmp/nonexistent; echo $?` prints 0
- `node scripts/doctor.cjs --acceptance` still passes (no acceptance-gate regression)
</verification>

<success_criteria>
- The WARN fires exactly on (bundled SQLite < 3.51.3) AND (live co-session), advisory-only
- exit code and report.healthy are provably untouched by the advisory
- PD-2 documented: no extraction-worker guard this phase, no Phase-224 file touched
</success_criteria>

<output>
Create `.planning/phases/225-per-session-room-binding-and-multi-session-reconciliation-se/225-02-SUMMARY.md` when done.
</output>
