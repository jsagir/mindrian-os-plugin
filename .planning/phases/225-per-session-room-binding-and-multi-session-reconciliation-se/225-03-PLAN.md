---
phase: 225-per-session-room-binding-and-multi-session-reconciliation-se
plan: 03
type: execute
wave: 2
depends_on: [225-01, 225-02]
files_modified:
  - tests/run-all-225.sh
  - lib/memory/run-feynman-tests.cjs
  - docs/ENV-TUNING.md
autonomous: true
requirements: [REQ-6]
must_haves:
  truths:
    - "bash tests/run-all-225.sh runs the three 225 test legs (SKIP-safe via run_if) plus an unconditional run-all-194.sh regression leg, and exits 0 with FAIL=0"
    - "The three test-225 files are registered in lib/memory/run-feynman-tests.cjs TEST_FILES (append-only, never reorder)"
    - "docs/ENV-TUNING.md documents MINDRIAN_ZERO_SCORE_GATE_MIN_TOKENS (default 8) with the PD-3 anti-overfire rationale"
    - "The Phase 225 reasoning trail is filed in the rethinking-mindrianos room per the CLAUDE.md dev-research compositing rule, cross-linked to 225-RESEARCH.md"
  artifacts:
    - path: "tests/run-all-225.sh"
      provides: "phase-gate aggregator on the run-all-194.sh scaffold"
      min_lines: 40
    - path: "lib/memory/run-feynman-tests.cjs"
      provides: "three appended test-225 TEST_FILES entries under one phase comment"
      contains: "test-225-zero-score-gate.cjs"
    - path: "docs/ENV-TUNING.md"
      provides: "MINDRIAN_ZERO_SCORE_GATE_MIN_TOKENS floor entry"
      contains: "MINDRIAN_ZERO_SCORE_GATE_MIN_TOKENS"
  key_links:
    - from: "tests/run-all-225.sh"
      to: "tests/run-all-194.sh"
      via: "final unconditional regression leg (shipped-substrate guard)"
      pattern: "run-all-194"
---

<objective>
Ship the Phase 225 verification spine (REQ-6): the run-all-225.sh phase gate proving the
proving_case_2 reframe fires the gate AND every legitimate zero-score silence is
regression-free, plus run-feynman-tests registration, the ENV-TUNING floor doc for the
PD-3 token floor, and the mandatory dev-research compositing filing (CLAUDE.md rule:
every phase touching MindrianOS's own architecture files its reasoning trail in the
rethinking-mindrianos room, cross-linked both ways).

Purpose: the phase gate is what /gsd-verify-work and future sessions run; without
registration and the 194 regression leg, the shipped Phase-194 substrate has no guard
against this phase's edits.
Output: tests/run-all-225.sh, TEST_FILES registration, ENV-TUNING entry, room filing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/225-per-session-room-binding-and-multi-session-reconciliation-se/225-RESEARCH.md
@.planning/phases/225-per-session-room-binding-and-multi-session-reconciliation-se/225-PATTERNS.md
@tests/run-all-194.sh
@lib/memory/run-feynman-tests.cjs
</context>

## Artifacts this phase produces (plan 03)

| Artifact | Kind | Where |
|----------|------|-------|
| `tests/run-all-225.sh` | new phase-gate aggregator script | tests/ |
| three `test-225-*.cjs` TEST_FILES entries | appended registration lines | lib/memory/run-feynman-tests.cjs |
| `MINDRIAN_ZERO_SCORE_GATE_MIN_TOKENS` floor entry | new docs section/row | docs/ENV-TUNING.md |
| Phase 225 research-trail entry | new dated room entry (cross-repo filing) | ~/MindrianRooms/rethinking-mindrianos/research/ |

<tasks>

<task type="auto">
  <name>Task 1: Create tests/run-all-225.sh and register the 225 tests in run-feynman-tests.cjs</name>
  <files>tests/run-all-225.sh, lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - tests/run-all-194.sh (the prescribed scaffold: header, set -uo pipefail, PASS/FAIL/SKIP counters, run and run_if helpers at lines 31-52, the final summary/exit block)
    - .planning/phases/225-per-session-room-binding-and-multi-session-reconciliation-se/225-PATTERNS.md (run-all section: the run_if SKIP-safe idiom verbatim and the instruction that the 194 regression leg is an unconditional run, not run_if)
    - lib/memory/run-feynman-tests.cjs lines 33-60 (TEST_FILES array shape: path.join(REPO_ROOT, 'tests', '...') entries with a phase-comment line above each group; append-only, never reorder - the same contract Phase 224-04's pending plan follows, so both phases' appends compose regardless of execution order)
  </read_first>
  <action>
    1. Create tests/run-all-225.sh cloning run-all-194.sh's header and run/run_if
    helpers verbatim (PATTERNS.md carries the exact block). Legs, in order:
    (a) run_if 'REQ-1/REQ-2 zero-score no-match gate (proving_case_2 fire + 4 silence legs)'
    gated on tests/test-225-zero-score-gate.cjs;
    (b) run_if 'REQ-3 gate degrade (poisoned binding/trace -> exit-0 silence)'
    gated on tests/test-225-gate-degrade.cjs;
    (c) run_if 'REQ-4 doctor WAL-reset advisory (fire/no-fire/never-crash/never-block)'
    gated on tests/test-225-wal-advisory.cjs;
    (d) unconditional run 'Phase-194 substrate regression guard (PSB suite)' invoking
    `bash tests/run-all-194.sh` (RESEARCH.md Sampling Rate: the shipped substrate must
    stay green under this phase's classifier edit). Close with the same summary + exit
    logic as run-all-194.sh (exit 1 if FAIL > 0, else 0). chmod +x.

    2. Append the three test files to TEST_FILES in lib/memory/run-feynman-tests.cjs
    under a single new phase comment line naming Phase 225 (zero-score no-match gate +
    WAL advisory). Entries follow the existing path.join(REPO_ROOT, 'tests', ...) line
    shape exactly. APPEND at the array end - never reorder or touch existing entries
    (additive-only; Phase 224-04 pends the same file with the same contract, so
    ordering between the two phases is immaterial).
  </action>
  <acceptance_criteria>
    - Test command: `bash tests/run-all-225.sh` exits 0 with FAIL=0 and zero SKIPs (all three 225 test files exist by this wave)
    - Source assertion: `grep -v '^#' tests/run-all-225.sh | grep -c "run_if"` equals 3 and `grep -v '^#' tests/run-all-225.sh | grep -c "run-all-194"` is >= 1
    - Behavior assertion: `node -e "const fs=require('node:fs');const s=fs.readFileSync('lib/memory/run-feynman-tests.cjs','utf8');for(const f of ['test-225-zero-score-gate.cjs','test-225-gate-degrade.cjs','test-225-wal-advisory.cjs']){if(!s.includes(f))throw new Error('unregistered: '+f);if(!fs.existsSync('tests/'+f))throw new Error('missing: '+f)}console.log('all 3 registered + present')"` prints 'all 3 registered + present'
    - CLI check: `bash -n tests/run-all-225.sh` passes and the file is executable
  </acceptance_criteria>
  <verify>
    <automated>bash -n tests/run-all-225.sh && bash tests/run-all-225.sh && node -e "const fs=require('node:fs');const s=fs.readFileSync('lib/memory/run-feynman-tests.cjs','utf8');for(const f of ['test-225-zero-score-gate.cjs','test-225-gate-degrade.cjs','test-225-wal-advisory.cjs']){if(!s.includes(f))throw new Error('unregistered: '+f)}console.log('registered')"</automated>
  </verify>
  <done>
    run-all-225.sh exists, is executable, runs three SKIP-safe 225 legs plus the
    unconditional 194 regression leg, exits 0; all three tests registered in TEST_FILES.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document the PD-3 floor in ENV-TUNING and run the full phase gate</name>
  <files>docs/ENV-TUNING.md</files>
  <read_first>
    - docs/ENV-TUNING.md (existing entry shape: env var name, default, effect, when to tune - match the surrounding format exactly)
    - .planning/phases/225-per-session-room-binding-and-multi-session-reconciliation-se/225-01-PLAN.md (the PD-1/PD-3 decision text this entry documents)
  </read_first>
  <action>
    Append a MINDRIAN_ZERO_SCORE_GATE_MIN_TOKENS entry to docs/ENV-TUNING.md matching
    the file's existing entry format: default 8; effect - the minimum count of surviving
    (post-stopword, length >= 2) message tokens before a zero-score message with a bound
    primary can fire the no-match F.8 gate; rationale - anti-overfire (Phase 210
    lesson, PD-3): trivial acknowledgements must preserve the legacy silence, while a
    substantive conversational reframe (SEED-039 proving_case_2) clears the floor;
    tuning guidance - raise it if the gate fires on routine short prompts, lower it
    only if a genuine reframe was missed. Append-only; do not touch existing entries
    (Phase 224-04 pends floor entries in the same file - both appends compose).

    Then run the full phase-gate proof for the wave: `bash tests/run-all-225.sh`
    followed by `node scripts/doctor.cjs --acceptance`, both must pass.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "MINDRIAN_ZERO_SCORE_GATE_MIN_TOKENS" docs/ENV-TUNING.md` >= 1 and the entry states default 8
    - Test command: `bash tests/run-all-225.sh` exits 0 with FAIL=0
    - CLI check: `node scripts/doctor.cjs --acceptance` passes (release-gate roll-up intact)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "MINDRIAN_ZERO_SCORE_GATE_MIN_TOKENS" docs/ENV-TUNING.md && bash tests/run-all-225.sh</automated>
  </verify>
  <done>
    The floor is documented in ENV-TUNING.md in the file's own format; the full 225
    suite and the acceptance roll-up are green.
  </done>
</task>

<task type="auto">
  <name>Task 3: File the Phase 225 reasoning trail in the rethinking-mindrianos room (compositing rule)</name>
  <files>~/MindrianRooms/rethinking-mindrianos/research/ (new dated entry, cross-repo)</files>
  <read_first>
    - The Phase 218 filing precedent: run `git show cfa48e3f9 --stat` in this repo to see exactly which files that commit created/changed for the Phase 218 research-trail filing, then read the room-side entry it created under ~/MindrianRooms/rethinking-mindrianos/research/ and mirror its structure
    - .planning/phases/225-per-session-room-binding-and-multi-session-reconciliation-se/225-RESEARCH.md (the content being trail-filed: the 194-shipped-the-pillars finding, the line-509 causal chain, the WAL two-issue split, the PD-1..PD-5 decisions)
  </read_first>
  <action>
    Per the CLAUDE.md dev-research compositing rule (this phase touches MindrianOS's own
    architecture): create a dated entry under
    ~/MindrianRooms/rethinking-mindrianos/research/ (e.g.
    2026-07-15-phase-225-zero-score-gate-and-wal-advisory/) replicating the Phase 218
    filing precedent's structure (discover it via git show cfa48e3f9 --stat and the
    room-side entry it references). Content: the durable reasoning trail - (a) Phase 194
    already shipped all four SEED-039 pillars, so 225 is gap-closure not a build; (b)
    the intent-classifier.cjs:509 zero-score causal chain and proving_case_2; (c) the
    Phase-218 WAL finding's two-issue split (SQLITE_BUSY fixed by D-05; WAL-reset
    upstream, detect-only); (d) the PD-1..PD-5 planning decisions with rationale.
    Cross-link BOTH ways: the room entry cites the dev-repo path
    .planning/phases/225-.../225-RESEARCH.md, and this phase's SUMMARY files note the
    room entry path. Mirror per the precedent commit's pattern. This is a filing task -
    no code changes; the room write is OUTSIDE this repo (do not git-add room files
    here; commit room-side per the room's own conventions as the 218 precedent shows).
  </action>
  <acceptance_criteria>
    - CLI check: `ls ~/MindrianRooms/rethinking-mindrianos/research/ | grep -c "phase-225"` >= 1
    - Source assertion: the new room entry file contains the literal string '225-RESEARCH.md' (the cross-link back to the dev repo)
    - Behavior assertion: the entry names all five PD decisions (PD-1 through PD-5) and the two-gap scope
  </acceptance_criteria>
  <verify>
    <automated>ls ~/MindrianRooms/rethinking-mindrianos/research/ | grep -q "phase-225" && grep -rq "225-RESEARCH.md" ~/MindrianRooms/rethinking-mindrianos/research/*phase-225*/</automated>
  </verify>
  <done>
    The dated room entry exists, mirrors the 218 precedent structure, carries the
    reasoning trail and PD decisions, and is cross-linked to 225-RESEARCH.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test aggregator -> repo scripts | run-all-225.sh executes repo-local test files only |
| dev repo -> rethinking-mindrianos room | The compositing filing writes a reasoning trail into a LOCAL room directory |

Mitigating constraint called out explicitly: this phase is room-local only (Canon
Part 8, no egress). The compositing entry carries this repo's own architecture reasoning
(MindrianOS-dev content into the MindrianOS-dev consultant room) - no user room content
crosses anywhere, and nothing reaches the Brain or any network surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-225-10 | Tampering | TEST_FILES / ENV-TUNING appends colliding with pending Phase 224-04 edits | mitigate | Both files follow the append-only never-reorder contract on both phases' plans; appends compose in either execution order. |
| T-225-11 | Repudiation | phase claims green without the shipped-substrate guard | mitigate | run-all-225.sh's final leg unconditionally runs run-all-194.sh, so a 194 regression fails THIS phase's gate. |
| T-225-12 | Information Disclosure | the room filing leaking non-public content | accept | The room is the standing LOCAL MindrianOS-dev consultant room named by CLAUDE.md for exactly this content; no tester real names or user data appear in the trail (repo hard rule honored). |
| T-225-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (225-RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `bash tests/run-all-225.sh` exits 0, FAIL=0, SKIP=0 (all upstream waves landed)
- registration proof: all three test-225 files present in TEST_FILES and on disk
- `grep -q MINDRIAN_ZERO_SCORE_GATE_MIN_TOKENS docs/ENV-TUNING.md`
- `node scripts/doctor.cjs --acceptance` passes
- room entry exists and cross-links 225-RESEARCH.md
</verification>

<success_criteria>
- REQ-6 proven: the reframe fires, every legitimate silence holds, 194 substrate green
- The phase is dischargeable by /gsd-verify-work via one command: bash tests/run-all-225.sh
- Compositing rule honored: same finding, two homes, cross-linked
</success_criteria>

<output>
Create `.planning/phases/225-per-session-room-binding-and-multi-session-reconciliation-se/225-03-SUMMARY.md` when done.
</output>
