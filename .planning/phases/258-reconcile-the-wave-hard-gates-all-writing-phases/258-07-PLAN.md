---
phase: 258-reconcile-the-wave-hard-gates-all-writing-phases
plan: 07
type: execute
wave: 5
depends_on: [258-01, 258-02, 258-06]
target_repo: ProblemsWorthSolving-Brain
files_modified:
  - ProblemsWorthSolving-Brain/docs/GRAPH-WRITE-LOG.md
  - ProblemsWorthSolving-Brain/docs/census-2026-08-20.md
  - ProblemsWorthSolving-Brain/payloads/order-collision-dishare-2026-08-20/README.md
  - ProblemsWorthSolving-Brain/payloads/order-collision-dishare-2026-08-20/manifest.json
  - ProblemsWorthSolving-Brain/SCHEMA.md
autonomous: true
requirements: [RECON-01, RECON-02]

must_haves:
  truths:
    - "Both order-collision nodes have exactly 1 parent Framework, proven by a read-tier probe run after the window closed"
    - "The first GRAPH-WRITE-LOG row exists, its 8 cells are well formed, and its commit_sha resolves in the Brain repo"
    - "A post-window census judges GraphWriteEvent as ok and never as UNKNOWN(GraphWriteEvent)"
    - "The payload README carries an execution record with predicted versus written side by side and every deviation recorded honestly"
    - "Every count in the record was measured by a separate read-tier probe, never taken from the write's own return value"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/GRAPH-WRITE-LOG.md"
      provides: "Ledger entry one for this phase's write session"
      contains: "RECON-02"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/order-collision-dishare-2026-08-20/README.md"
      provides: "The execution record and the honest-deviation list"
      contains: "recorded honestly"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/census-2026-08-20.md"
      provides: "The post-window census, overwriting the pre-window baseline in the worktree while both stay in git history"
  key_links:
    - from: "docs/GRAPH-WRITE-LOG.md row"
      to: "tests/graph-write-log-shape.test.mjs"
      via: "the shape test parses the row and resolves its commit_sha"
      pattern: "cat-file"
    - from: "91-verify.cypher parents assertion"
      to: "RECON-02 acceptance"
      via: "count(DISTINCT parent Framework) equals 1 on both nodes"
      pattern: "parents"
---

<objective>
Close the phase. Probes first, then records, both strictly AFTER the admin window closed in plan
258-06. That ordering is not stylistic: D-11 and RESEARCH.md Pitfall 5 both put the window close
ahead of probes and records precisely because the 2026-08-11 sitting did it the other way and
left the admin surface open for roughly two days.

Purpose: a write that is not verified by an independent read is not a verified write. `brain_write`
echoes no rows, which is one of the two recorded instances of this repo's false-success class
(the other being the silent prop drop). So every number that lands in the GRAPH-WRITE-LOG row and
in the execution record comes from a separate read-tier probe, never from what the write call
returned.

This plan also produces the phase's first ledger entry, which is the whole point of RECON-01's
convention leg: the log stops being an empty contract and becomes a used one, with its shape test
now exercising a real row rather than an empty table.

Dependency on plan 258-06's outcome, stated explicitly. If 258-06 ended in `defer` (the operator
never opened the window) or `abort` (the window opened and closed with no data write), then
tasks 1 and 2 below run in a reduced form: the census and the shape test still run, the payload
`manifest.json` stays `compile_only: true`, the GRAPH-WRITE-LOG gains NO row, and this plan
reports `human_needed` for RECON-02's execution leg alongside the `human_needed` D-06 already
directs for RECON-03 and RECON-04. Do not write a ledger row for a session that did not happen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-CONTEXT.md
@.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-RESEARCH.md
@.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-PATTERNS.md
@.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-VALIDATION.md
@.planning/REQUIREMENTS.md
</context>

<cross_repo_contract>
Every file in this plan lives in `/home/jsagi/dev/ProblemsWorthSolving-Brain`. All `git add` and
`git commit` run with that directory as the working directory. Nothing here commits to
MindrianOS-Plugin; the phase SUMMARY and the `.planning/` artifacts are the orchestrator's,
committed separately with `git add -f` because `.planning/` is gitignored there. Both repos
forbid em-dashes; hyphens only.
</cross_repo_contract>

<tasks>

<task type="auto">
  <name>Task 1: Post-window verification, read-tier only</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/census-2026-08-20.md (regenerated)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/order-collision-dishare-2026-08-20/91-verify.cypher` in full: the seven numbered read-only assertions, including the `parents = 1` done-signal.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/order-collision-dishare-2026-08-20/README.md` `## Dry-run recompile` and `## Commit log` (from plan 258-06): the predicted and reported values this task measures against.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-framework-evals.mjs`: the existing eval harness, including its line-129 comment about the 2026-08-11 batch coexistence.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/run-schema-census.mjs` `judgeLabelCombo`: the function that returns `UNKNOWN(GraphWriteEvent)` if plan 258-01's `TIER3_LABELS` edit were missing.
    - 258-RESEARCH.md Pattern 2 step 7, Pitfall 3, Pitfall 4, and the Validation Architecture requirement-to-test map.
  </read_first>

  <action>
Confirm first that the admin window is closed. List the MCP tool surface and expect `brain_write`
absent ("Tool brain_write not found"). If it is still exposed, STOP and return to the operator
immediately: an open window is a live Elevation-of-Privilege exposure and closing it outranks
every other item in this plan.

Then run four verifications, all read-only, with `MINDRIAN_BRAIN_URL` pinned explicitly to
`https://pws-brain-mcp.onrender.com` on every one of them (Pitfall 4).

1. `91-verify.cypher`, all seven statements, through `brain_query`. Record every result.
   `[91.1]` is RECON-02's acceptance assertion and must return `parents = 1` for BOTH
   `Identify Reverse Salients` and `Generate Innovation Opportunities`. Any other value is a
   RED result: record it, do not round it up to a pass, and surface it as a phase gap.
   `[91.2]` and `[91.3]` give the node and edge counts that must match what plan 258-06 wrote
   into the `GraphWriteEvent` node; if they disagree, that disagreement is itself a finding.
   `[91.4]` and `[91.5]` confirm the `GraphWriteEvent` singleton and the `GraphRagMeta` stamp
   landed with non-null values. `[91.6]` shows the two copies now carry different `order` values
   under different parents. `[91.7]` confirms the seven Cynefin `Phase` nodes were not collateral
   damage.

2. `node scripts/probe-framework-evals.mjs` from the Brain repo root. This is the existing
   structural-regression harness; it must stay green after the surgery. Do not modify it. If it
   goes red, record which fixture and why before touching anything.

3. `node scripts/run-schema-census.mjs`, which regenerates `docs/census-2026-08-20.md`. This
   overwrites plan 258-02's pre-window baseline in the working tree, which is intended and was
   flagged there: the pre-window version is already committed, so `git diff` between the two
   commits is the ceremony's own census delta. Then grep the fresh census for the literal string
   `UNKNOWN(GraphWriteEvent)` and assert it is ABSENT. Its presence would mean plan 258-01's
   `TIER3_LABELS` edit did not reach `CANONICAL_LABELS`, which is exactly the "fresh
   unconstrained label" outcome D-03 exists to prevent.

4. `npm test` in the Brain repo, including `tests/graph-write-log-shape.test.mjs`. It should pass
   with an empty ledger at this point; task 2 adds the first row and re-runs it.

Record all four results in a scratch note for task 2 to consume. Commit the regenerated census
with the message `docs(census): post-window picture after the 258 order-collision sitting`,
naming the pre-window commit in the body so the pair is findable.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node scripts/probe-framework-evals.mjs && ! grep -q 'UNKNOWN(GraphWriteEvent)' docs/census-2026-08-20.md && npm test</automated>
  </verify>

  <acceptance_criteria>
    - A live MCP tool-surface listing confirms `brain_write` is absent before any other step in this task runs.
    - All seven `91-verify.cypher` statements ran and their results are recorded verbatim.
    - `[91.1]` returned `parents = 1` for both collision node names. If it did not, the actual value is recorded and the phase reports a gap rather than a pass.
    - `[91.2]` and `[91.3]` counts are compared against the values plan 258-06 wrote into the `GraphWriteEvent` node, and any disagreement is recorded as a finding.
    - `node scripts/probe-framework-evals.mjs` exits 0, and the script itself is unmodified: `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain status --short scripts/probe-framework-evals.mjs` prints nothing.
    - `grep -c 'UNKNOWN(GraphWriteEvent)' docs/census-2026-08-20.md` outputs `0`.
    - `npm test` exits 0.
    - The regenerated census is committed as its own commit whose body names the pre-window census commit.
  </acceptance_criteria>

  <done>
    The window is confirmed closed, both collision nodes are proven to have exactly one parent
    Framework by an independent read, the eval harness is still green, the fresh census judges
    `GraphWriteEvent` as ok rather than UNKNOWN, and the full Brain suite passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the records, then close the ledger loop</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/GRAPH-WRITE-LOG.md (modify, append row one)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/order-collision-dishare-2026-08-20/README.md (modify, execution record)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/order-collision-dishare-2026-08-20/manifest.json (modify, flip compile_only)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/SCHEMA.md (modify, section 7 status)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/GRAPH-WRITE-LOG.md` sections 1, 2, 3 and 7 (from plan 258-01): the append-at-the-bottom rule, the 8 columns in order, and what `commit_sha` names.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/graph-write-log-shape.test.mjs` (from plan 258-01): the five assertions the new row must satisfy.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-11-RUNBOOK-249-alias-collapse.md` lines 380-487: the execution-record shape and its numbered "Deviations from this document's predictions, recorded honestly" list.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/chunk-document-repair/README.md` "Provenance (the rehearsal)": the `| Step | Predicted | Written |` table where a miss is annotated inline in the Written cell with its cause.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/SCHEMA.md` section 7: the `EXECUTED <date> (<evidence>)` status vocabulary.
    - 258-RESEARCH.md Pattern 3 and F-8's suggested row shape.
  </read_first>

  <action>
Step 1, append GRAPH-WRITE-LOG row one. Add exactly one row at the BOTTOM of the ledger table in
`docs/GRAPH-WRITE-LOG.md`, with the 8 cells in the declared column order: `date` as the actual
execution date from plan 258-06's commit log; `phase` as `258`; `requirement` as `RECON-02`;
`commit_sha` backticked, the same value passed as the `$commit_sha` param to
`03-graphwriteevent.cypher`, which names the Brain-repo commit that landed the payload directory;
`operator` as the name recorded in the commit log; `nodes` and `edges` from `91-verify.cypher`
`[91.2]` and `[91.3]`, which are measured values and not the dry-run predictions; and `summary`
as one line naming the dis-share and the order-channel ruling.

Immediately re-run `node --test tests/graph-write-log-shape.test.mjs`. This is the moment the
shape test stops guarding an empty table and starts guarding a real row: it re-parses the 8
cells, checks the date ordering, and resolves the `commit_sha` with `git cat-file -e`. If that
resolution fails, the SHA is wrong; fix the row rather than the test.

Step 2, append the execution record to the payload README. Fill in the `## Execution record`
section plan 258-05 left empty, and fill the Written column of the `## Provenance` table so
predicted and written sit side by side. Then write the numbered
`### Deviations from this document's predictions, recorded honestly` list. Every one of the
following is a deviation and gets its own numbered entry, with its cause:
- any dry-run row whose matches column read no;
- any gap between `[91.2]`/`[91.3]`'s measured counts and what was written into the
  `GraphWriteEvent` node;
- any card the navigator put on `hold`;
- any `LEADS_TO` edge left unrouted because its owning framework could not be attributed;
- the third `:Stage` claimant's ruling and what was done about it;
- a prediction that missed in the specific shape RESEARCH.md Pitfall 7 names, where a
  name-matching branch counts the canon twice and a predicted N measures N+1.
If there were genuinely no deviations, write that as an explicit sentence rather than omitting
the section. An absent section reads as an unasked question; a stated "none" reads as an answer.
Also record the window open and close times so the sitting's duration is in the permanent record,
which is the direct countermeasure to the 2-day-open incident.

Step 3, flip `manifest.json` `compile_only` from `true` to `false` and record the flip in the
execution record. If any card was held or aborted, leave `compile_only: true` and say why: a
partially executed batch is not an executed batch.

Step 4, update `SCHEMA.md` section 7. Plan 258-01 appended this phase's ledger row with status
`QUEUED (payloads/order-collision-dishare-2026-08-20/)`. Change that status to
`EXECUTED <date> (<evidence>)` using the existing vocabulary, with the evidence naming
`payloads/order-collision-dishare-2026-08-20/README.md` execution record. Do not invent a third
status vocabulary. If the sitting was deferred or aborted, leave the row as `QUEUED` and say so
in the summary.

Step 5, commit everything in the Brain repo in one commit, message
`docs(258): execution record, GRAPH-WRITE-LOG entry 1, schema ledger EXECUTED`. Then state, in
the plan summary, the phase-level requirement status for the orchestrator to carry into
VERIFICATION.md: RECON-01 and RECON-02 complete (or the honest partial), and RECON-03 and
RECON-04 `human_needed` per D-06, pointing at
`docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md`.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/graph-write-log-shape.test.mjs && node -e "const m=require('./payloads/order-collision-dishare-2026-08-20/manifest.json'); console.log('compile_only', m.compile_only)" && grep -q 'recorded honestly' payloads/order-collision-dishare-2026-08-20/README.md</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/graph-write-log-shape.test.mjs` exits 0 WITH the new row present, proving all five assertions hold against a real row and not only an empty table.
    - `docs/GRAPH-WRITE-LOG.md` has exactly one data row, it parses to 8 non-empty cells, and `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain cat-file -e <commit_sha>^{commit}` exits 0 for its `commit_sha` cell.
    - The row's `nodes` and `edges` cells equal the values returned by `91-verify.cypher` `[91.2]` and `[91.3]`, and the execution record says so explicitly. They are not dry-run predictions.
    - The payload README's `## Provenance` table has both Predicted and Written populated for every row, with any miss annotated inline in the Written cell with its cause.
    - A numbered `### Deviations from this document's predictions, recorded honestly` list exists and is non-empty, containing either the enumerated deviations or an explicit "none" sentence.
    - The window open and close times are both recorded in the execution record.
    - `manifest.json` `compile_only` is `false` if and only if both cards executed; otherwise it is `true` and the reason is stated.
    - `SCHEMA.md` section 7's Phase 258 row reads `EXECUTED` with evidence, or still reads `QUEUED` with a stated reason. `grep -c 'order-collision-dishare-2026-08-20' SCHEMA.md` outputs at least `1`.
    - The plan summary states the requirement-level status for VERIFICATION.md: RECON-01 and RECON-02 with their honest verdicts, RECON-03 and RECON-04 as `human_needed` per D-06, with a pointer to `docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md`.
    - `grep -Pn '\x{2014}' docs/GRAPH-WRITE-LOG.md payloads/order-collision-dishare-2026-08-20/README.md SCHEMA.md` exits 1 with no output for the lines this task added.
  </acceptance_criteria>

  <done>
    The ledger has its first real row and the shape test now guards it, the payload README carries
    a complete execution record with predicted versus written and an explicit honest-deviation
    list, the manifest reflects what actually executed, `SCHEMA.md`'s reconciliation ledger is
    updated in its existing vocabulary, and the phase's requirement-level status is stated for
    VERIFICATION.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| write return value to audit record | `brain_write` echoes no rows; trusting its return as a count manufactures a false measurement |
| execution reality to written record | the record is what future phases and future operators will believe; a flattering record is worse than none |
| window closed to phase closed | probes and records must run only after the close, never before |

## STRIDE Threat Register

Grounded in 258-RESEARCH.md "Security Domain" (ASVS V7 Error handling and logging is this plan's
central control) and its Known Threat Patterns table.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-258-32 | Repudiation | an unattributable production write | mitigate | this plan writes the FIRST GRAPH-WRITE-LOG row, which is RECON-01's whole deliverable turning from a contract into a used one; the shape test then resolves its `commit_sha` against real git history |
| T-258-33 | Repudiation | a ledger row whose `commit_sha` does not resolve, making the audit trail decorative | mitigate | `tests/graph-write-log-shape.test.mjs` runs `git cat-file -e <sha>^{commit}` per row, so a bad SHA is a failing test rather than a later discovery |
| T-258-34 | Spoofing | predicted counts recorded as measured counts | mitigate | the row's `nodes` and `edges` cells are taken from `91-verify.cypher` `[91.2]` and `[91.3]`, and the acceptance criteria require the record to name that source |
| T-258-35 | Repudiation | an execution record that quietly omits what went wrong | mitigate | the numbered honest-deviation list is mandatory and must contain either enumerated deviations or an explicit "none" sentence; RESEARCH.md Pattern 3 and the standing false-success WATCH memory are the source |
| T-258-36 | Elevation of Privilege | the admin window still open when the phase is declared done | mitigate | task 1's first action is a live tool-surface listing confirming `brain_write` is absent, and it outranks every other item in the plan |
| T-258-37 | Tampering | a mis-tiered `GraphWriteEvent` label silently entering the methodology projection | mitigate | the post-window census is grepped for `UNKNOWN(GraphWriteEvent)` and `npm test` re-runs the `RED:` projection assertion from plan 258-01 |
| T-258-SC | Tampering | npm/pip/cargo installs | mitigate | not applicable: this plan runs existing scripts and edits markdown and JSON. `npm test` runs the vendored suite and installs nothing. No package-manager install task exists, so no legitimacy checkpoint is required |

Remaining open at phase close, by design and not by omission: the standing admin key
`9e3da1a7-8b66-4b35-9c2d-8b0bc740a650`, the temp key `1148f416...`, the on-disk `.tmp-admin-key`
on the second machine, and the unverified Gemini key. All four are RECON-03 operator items
documented in `docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md`, and D-06 directs
VERIFICATION.md to report them as `human_needed` rather than as a phase failure.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

Every file and symbol below is NEW in this phase. Source-grounding passes must exclude these
from drift verification because they do not exist upstream of Phase 258. All paths are relative
to `/home/jsagi/dev/ProblemsWorthSolving-Brain`.

| Path | Created by |
|---|---|
| `docs/GRAPH-WRITE-LOG.md` | 258-01 |
| `tests/graph-write-log-shape.test.mjs` (exports `parseLedgerRows`) | 258-01 |
| `scripts/probe-wave-attribution.mjs` (exports `resolveReadKey`, `call`, `q`, `initHandshake`) | 258-02 |
| `docs/2026-08-20-ATTRIBUTION-probe-results.md` | 258-02 |
| `docs/census-2026-08-20.md` | 258-02, regenerated by 258-07 |
| `docs/2026-08-20-ATTRIBUTION-untracked-writes.md` | 258-02 |
| `docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md` | 258-03 |
| `scripts/probe-order-collisions.mjs` | 258-04 |
| `payloads/order-collision-dishare-2026-08-20/00-preflight-evidence.md` | 258-04 |
| `payloads/order-collision-dishare-2026-08-20/manifest.json` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/README.md` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/90-dry-run.cypher` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/01-dishare-24219.cypher` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/02-dishare-gen-innov-opp.cypher` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/03-graphwriteevent.cypher` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/04-graphragmeta-stamp.cypher` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/05-close-window.md` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/91-verify.cypher` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/99-undo.cypher` | 258-05 |

New graph entities, first written by plan 258-06: the `:GraphWriteEvent` label (registered by
258-01) with properties `id, date, phase, requirement_id, commit_sha, summary, node_count,
edge_count, operator, batch_id, created_by, created_at` on the singleton
`{id: 'gwe-258-recon02-2026-08-20'}`; two dis-shared `:ProcessStep` nodes (per-framework copies
of `Identify Reverse Salients` and `Generate Innovation Opportunities`, each stamped `batch_id`
and `created_by:'payload'`); and the `pws-ordercollision-2026-08-20` entry appended to
`GraphRagMeta{id:'canon'}.applied_batches`.

Pre-existing but NOT created by this phase, referenced only as a handoff pointer:
`payloads/relabel-fix-260820/`, a drafted and unexecuted alias-split-cluster payload belonging to
Phases 260 and 261.

Modified existing files: `SCHEMA.md` (258-01 Tier 3 row and section 7 QUEUED row, 258-07 status
flip to EXECUTED), `src/contracts/schema-contract.mjs`, `src/ontology.mjs`,
`src/ingest/allowlist.mjs`, `tests/schema-contract.test.mjs` (all 258-01).

No MindrianOS-Plugin code is modified by this phase.
</artifacts_this_phase_produces>

<verification>
- A live tool-surface listing confirms `brain_write` is absent before any record is written.
- `91-verify.cypher` `[91.1]` returns `parents = 1` for both collision nodes.
- `node scripts/probe-framework-evals.mjs` exits 0.
- `docs/census-2026-08-20.md` contains zero occurrences of `UNKNOWN(GraphWriteEvent)`.
- `npm test` exits 0 with the new GRAPH-WRITE-LOG row in place.
- `docs/GRAPH-WRITE-LOG.md` has one well-formed row whose `commit_sha` resolves.
- The payload README carries a complete execution record with an explicit honest-deviation list.
</verification>

<success_criteria>
- RECON-02's done-signal is proven by an independent read-tier probe, not by a write's return
  value.
- RECON-01's convention leg is closed the only way it can be: the ledger has a real, shape-tested,
  SHA-resolvable first row.
- The window is confirmed closed before any probe or record ran, which is D-11 honored in
  sequence and not just in prose.
- The record is honest: deviations enumerated or an explicit "none", window duration recorded,
  `compile_only` reflecting what actually executed.
- The phase reports RECON-03 and RECON-04 as `human_needed` per D-06, pointing at the operator
  checklist, rather than stopping the phase or overstating completion.
- All commits land in `/home/jsagi/dev/ProblemsWorthSolving-Brain`.
- Zero em-dashes added.
</success_criteria>

<output>
Create `.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-07-SUMMARY.md` when done.
</output>
