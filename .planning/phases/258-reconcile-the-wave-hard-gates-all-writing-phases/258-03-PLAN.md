---
phase: 258-reconcile-the-wave-hard-gates-all-writing-phases
plan: 03
type: execute
wave: 1
depends_on: []
target_repo: ProblemsWorthSolving-Brain
files_modified:
  - ProblemsWorthSolving-Brain/docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md
autonomous: true
requirements: [RECON-03, RECON-04]

must_haves:
  truths:
    - "RECON-03's seven operator prerequisites exist as a tracked checklist, each carrying an explicit OPEN or CLOSED state with its evidence source"
    - "The checklist corrects the requirement's wrong premise: the 2026-08-11 wave used a STANDING key, not a minted temp key, so revoking only the temp key does not close RECON-03"
    - "RECON-04's unblock trigger is documented as contingent on RECON-03, with the exact commands and the exact reasons a number measured before then would be untrustworthy"
    - "No task in this plan revokes a key, deletes a file, or runs the floor gate; the checklist is an artifact, not an execution"
    - "The checklist lives in a tracked repo, so it reaches the second machine at all"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md"
      provides: "The RECON-03 operator prerequisite checklist and the RECON-04 unblock trigger"
      contains: "9e3da1a7-8b66-4b35-9c2d-8b0bc740a650"
      min_lines: 80
  key_links:
    - from: "docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md"
      to: "docs/2026-08-18-SESSION-e2e-unquilting.md lines 36-52"
      via: "every checklist row cites the record that established its current state"
      pattern: "e2e-unquilting"
    - from: "the RECON-04 trigger section"
      to: "/home/jsagi/dev/MindrianOS-Plugin/scripts/check-flagship-floor.cjs"
      via: "named cross-repo command with its exit-code contract"
      pattern: "check-flagship-floor"
---

<objective>
Deliver RECON-03 and RECON-04 as DOCUMENTED artifacts, not as executed work. Both are locked
that way by user decision: D-04 (RECON-03 is a prerequisite checklist the navigator completes
separately, on their own timing, NOT a synchronous block on this run), D-05 (RECON-04 explicitly
WAITS for RECON-03, because baselining against an incomplete graph forces a re-baseline later),
D-06 (RECON-01 and RECON-02 land in this run regardless; VERIFICATION.md reports `human_needed`
for RECON-03 and RECON-04, not a full phase stop), and D-07 (the navigator resumes this same
conversation to do RECON-03 together; Claude verifies what to check on the second machine and
confirms the rotation was done correctly, not a silent checkbox).

Purpose: RECON-03 is genuinely un-automatable. No code path can verify that a Supabase
`brain_api_keys` row was revoked or that a file on a Windows machine at
`C:/Users/PC/mindrian-brain-ingestion` was deleted, and that machine is not reachable from this
filesystem. 258-RESEARCH.md's Validation Architecture already rules this manual-only and
justified. What IS automatable is writing down exactly what to check, in a place the operator
can actually find it from the other machine.

Why the Brain repo and not `.planning/`: MindrianOS-Plugin's `.planning/` is gitignored, so it
does not travel between machines. RECON-03 is inherently a cross-machine task. A checklist that
cannot reach the machine it describes is not a checklist. The Brain repo is tracked, and the
key-hygiene record this checklist is built from already lives there
(`docs/2026-08-18-SESSION-e2e-unquilting.md`).

Output: one tracked checklist document.

Hard constraint on this plan: no task here revokes anything, deletes anything, or runs the floor
gate. Writing "revoke key X" into a file is the deliverable. Actually revoking key X is the
operator's work, per D-04 and D-07.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-CONTEXT.md
@.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-RESEARCH.md
@.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-PATTERNS.md
@.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-VALIDATION.md
@.planning/REQUIREMENTS.md
</context>

<cross_repo_contract>
The single file this plan creates lives in `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/`.
Its `git add` and `git commit` run with that directory as the working directory. Nothing here
commits to MindrianOS-Plugin. The checklist REFERENCES a MindrianOS-Plugin script
(`/home/jsagi/dev/MindrianOS-Plugin/scripts/check-flagship-floor.cjs`) by absolute path, but does
not modify or run it. Both repos forbid em-dashes; hyphens only.
</cross_repo_contract>

<tasks>

<task type="auto">
  <name>Task 1: Author the RECON-03 operator prerequisite checklist</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-18-SESSION-e2e-unquilting.md` lines 25-60: the verbatim key-hygiene close-out, including the metadata-query finding that zero `brain_api_keys` rows were created 2026-08-11/12 and the operator's identification of the standing key.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/framework-command-map-2026-08-18/manifest.json` and `payloads/orphan-linking-2026-08-18/manifest.json`: the second-machine file names these manifests already record.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/wave2-worklist-2026-08-18.md` around line 305: the LangExtract span counts on the second machine.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/HANDOFF-store-sync-2026-08-19.md` section 5: the "one sitting, one open, one close" rule.
    - 258-RESEARCH.md F-7 in full (the seven-row table with per-item current state and source), Pitfall 6, and the Security Domain Known Threat Patterns table.
    - 258-PATTERNS.md "No Analog Found" section, which is explicit that no template exists for this artifact and that F-7's table should be lifted rather than pattern-matched to a container.
  </read_first>

  <action>
Create `docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md` using the Brain repo's
dated-artifact doc idiom: an `# <title>` heading, a `**Filed:**` line naming
`Phase 258 RECON-03 (D-04, D-07), 2026-08-20`, then numbered sections.

Section 0, "Read this first: the requirement's premise is wrong." RECON-01 through RECON-04's own
text says "the minted key is dead." A later, evidence-backed record in this repo contradicts it.
State the correction with its evidence: the key pasted into the 2026-08-11 chat transcript was a
STANDING key, not a temp, settled by a metadata query showing ZERO `brain_api_keys` rows created
on 2026-08-11 or 2026-08-12, so that run used a pre-existing key. The operator identifies it as
"Jonathan Sagir - Desktop Permanent", id `9e3da1a7-8b66-4b35-9c2d-8b0bc740a650`, plan pro,
created 2026-04-06. The temp revocation performed on 2026-08-18 does NOT cover it. Add the
secondary correction: the rethinking-room trail
`2026-08-11-admin-sitting-alias-collapse-execution.md` (filed 08-13) claims the window was "gated
to one minted key throughout"; the 08-18 record is later and is backed by a direct metadata
query, so it is authoritative and the 08-13 claim is superseded. Close the section with the
failure mode this exists to prevent, from RESEARCH.md Pitfall 6: a RECON-03 completion note that
names only one key is not a completion.

Section 1, "The seven prerequisites." Lift RESEARCH.md F-7's table into a checkbox list, one
numbered subsection per item, each carrying a line that begins with the literal token `State:`
whose value is exactly `OPEN` or `CLOSED` plus the source that established it. There must be
exactly seven such `State:` lines. Every state below is as recorded 2026-08-18; RESEARCH.md
assumption A6 notes the operator may have acted since, so add a line telling the operator to
re-verify rather than trust the recorded state.

1. Revoke standing admin key `9e3da1a7-8b66-4b35-9c2d-8b0bc740a650` ("Jonathan Sagir - Desktop
   Permanent"). State: OPEN. Rotation protocol, stated exactly: mint the replacement with
   `gen_random_uuid()` INSIDE the database so the value never appears in any transcript, revoke
   the old row, then pull the new value once from the Supabase dashboard into `.env`. Never
   hand-roll key generation and never paste a key value into a chat.
2. Revoke TEMP admin key `brain_api_keys 1148f416...` (labelled "revoke after run").
   State: OPEN, still active.
3. Delete `.tmp-admin-key` on the second machine, inside `mindrian-brain-ingestion`.
   State: OPEN, still on disk.
4. Verify the Gemini key was killed after the LangExtract passes. State: OPEN.
5. `BRAIN_HTTP_ADMIN=deny` on Render service `srv-d9gfa03tqb8s73csfmtg`.
   State: CLOSED, verified 2026-08-18 by post-deploy smoke ("Tool brain_write not found").
   Add the note that Phase 258's own mini-ceremony (plans 258-05 and 258-06) RE-OPENS this and
   re-closes it as the last scripted write item per D-11, so CLOSED here means "closed between
   windows", not "never opened again".
6. Second-machine untracked payload recovery from `C:/Users/PC/mindrian-brain-ingestion`.
   State: OPEN. List the files already named in tracked manifests so the operator knows what to
   look for: `VERIFICATION-RESULTS.md`, `commands-teaches-2026-08-18.json` (112 MindrianCommand),
   `frameworks-live-2026-08-18.json` (184 names), `langextract/out-weeks/*.json` and
   `langextract/out-children/*.json` (6,708 spans), and `notion/weeks/`. Record the two possible
   outcomes ROADMAP.md's research flag already anticipates: recovered into git, or back-filled
   graph-to-payload if the files are gone. Note that `VERIFICATION-RESULTS.md` is the primary
   source behind the "8 frameworks enriched" figure that plan 258-02's Section A reconciles
   against, so recovering it also strengthens RECON-01.
7. SSH tunnel closed. State: CLOSED, self-terminated. Record the re-open command for reference:
   `ssh -N -L 0.0.0.0:7689:pws-brain-db:7687 srv-d9gfa03tqb8s73csfmtg@ssh.oregon.render.com`.

Then add a clearly separated `### Optional stretch check (NOT one of the seven)` block. Write it
WITHOUT a `State:` line so the seven-item count above stays exactly seven. Content: the only
remaining channel that could ever name the batch operation which stripped `:Framework` and
applied `:Archived` to the id 28000-29000 block is an Aura console audit log for 2026-02-05, if
the account retains one. Plan 258-02's Section B established that the repo-history channel is
permanently closed, because `ProblemsWorthSolving-Brain`'s earliest commit is 2026-07-22 and
`mindrian-brain-local`'s is 2026-07-16, so neither history reaches 2026-02-05. An audit-log
lookup is a dashboard action on an account, unreachable from any code path in either repo, which
is why it sits here with the operator items rather than as a phase task. Mark it explicitly
OPTIONAL and NON-BLOCKING: RECON-01 does not wait on it, and RECON-04's trigger in section 4 is
gated on items 1 through 6 only, never on this one.

Section 2, "How this gets done (D-07)." State the protocol: the navigator completes these on
their own timing and then resumes the Phase 258 conversation. Claude's job in that resumed
session is to name what to check on the second machine, confirm the rotation followed the
`gen_random_uuid()`-inside-the-database protocol rather than a pasted value, re-verify the
recorded states rather than trusting them, and update each `State:` line in this file with the
date and evidence. This is not a silent checkbox.

Section 3, "Where the admin gate actually lives." A short reference block so the operator is not
guessing: `src/http/auth.mjs:85` computes `isAdmin` from `BRAIN_HTTP_ADMIN_KEYS` and read keys
never confer admin; `src/http/admin-tools.mjs:320-322` requires both the `brain:admin` scope AND
an admin-allowed surface, else `registerAdminTools()` returns an empty array and `brain_write` is
not even listed; `render.yaml` contains ZERO admin env vars, they are set in the Render dashboard
only, which is why every item here is a dashboard action rather than a repo edit.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && test "$(grep -c 'State:' docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md)" = "7" && grep -q '9e3da1a7-8b66-4b35-9c2d-8b0bc740a650' docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md && grep -q '1148f416' docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'State:' docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md` outputs exactly `7`.
    - `grep -c 'State: OPEN' <file>` outputs `5` and `grep -c 'State: CLOSED' <file>` outputs `2`, matching RESEARCH.md F-7's recorded states.
    - `grep -q '9e3da1a7-8b66-4b35-9c2d-8b0bc740a650' <file>` exits 0 AND `grep -q '1148f416' <file>` exits 0, proving BOTH keys are named (Pitfall 6).
    - `grep -q 'gen_random_uuid()' <file>` exits 0, proving the rotation protocol is stated rather than implied.
    - `grep -q 'C:/Users/PC/mindrian-brain-ingestion' <file>` exits 0 and `grep -q 'srv-d9gfa03tqb8s73csfmtg' <file>` exits 0.
    - An `### Optional stretch check (NOT one of the seven)` block exists naming the Aura console audit log for 2026-02-05, marked OPTIONAL and NON-BLOCKING, and it carries NO `State:` line, so `grep -c 'State:' <file>` still outputs `7`.
    - No Supabase row was modified, no file was deleted, and no admin env var was changed by this task. The only on-disk side effect is the new markdown file plus its git commit.
    - `grep -Pn '\x{2014}' <file>` exits 1 with no output.
  </acceptance_criteria>

  <done>
    A tracked checklist exists naming all seven prerequisites with explicit per-item state, both
    admin keys, the on-disk secret file, the rotation protocol, and the D-07 resume protocol.
    Nothing was executed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the RECON-04 unblock trigger to the same checklist</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md (modify, append section 4)
  </files>

  <read_first>
    - `/home/jsagi/dev/MindrianOS-Plugin/scripts/check-flagship-floor.cjs` lines 1-45: the header block stating that the invoked-framework set is enumerated from `commands/*.md` frontmatter at run time via `build-brain-census.cjs`'s `scanMethodologyCommands`, the two per-framework conditions, the exit-code contract, and the pure exported `evaluateFloor`.
    - `/home/jsagi/dev/MindrianOS-Plugin/data/flagship-floor-set.json`: the ratified 28-framework denominator (the 249-03 Task 2 ratification artifact).
    - `/home/jsagi/dev/MindrianOS-Plugin/.planning/REQUIREMENTS.md` lines 9-10 (the kickoff 8/28 number and the graph composition at kickoff) and the Phase family B bullets for TRUST-01 and TRUST-02.
    - 258-RESEARCH.md F-9 in full, Pitfall 7, and F-12's "Consistency note for RECON-04".
  </read_first>

  <action>
Append a section `## 4. RECON-04: the floor baseline trigger (blocked on section 1)` to
`docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md`. This documents a future action.
Do NOT run the floor gate in this task.

State the gating rule first with its reason (D-05): RECON-04 replaces the stale 8/28 kickoff
number (recorded 2026-08-13, pre-reconcile) with a fresh post-reconcile baseline, and it WAITS
until section 1's items are closed. Baselining against a graph still missing the second-machine
payload recovery measures an incomplete graph and forces a re-baseline later.

Then four subsections.

4.1 The instrument. `/home/jsagi/dev/MindrianOS-Plugin/scripts/check-flagship-floor.cjs`, run
from `/home/jsagi/dev/MindrianOS-Plugin`. Record its contract exactly: it enumerates the invoked
framework set from `commands/*.md` frontmatter at run time (never a frozen literal), and per
framework requires BOTH that `normalize_framework_name` returns exactly 1 canonical match AND
that `orchestration_readiness` returns a readiness_score of 3 or more. Exit codes: `0` all clear,
`1` at least one miss, `2` `data/flagship-floor-set.json` exists but is malformed. Exit code 2 is
a distinct code and must never be read as a miss. The zero-network fixture harness is
`node tests/test-249-floor-gate.cjs`.

4.2 The honesty precondition, the part that is easy to skip and expensive to skip. The number is
only trustworthy after Phase 259's TRUST-02 (the floor gate VOIDs on probe failure instead of
reporting a false MISS or RED) and TRUST-01 (a 429 no longer renders as `BRAIN_UNREACHABLE` with
zero retries). A 56-probe floor run against a rate-limited Render free tier is precisely the
shape that produces a silently-wrong baseline. ROADMAP.md marks 258 and 259 parallel-safe so this
is not a hard sequencing block, but since D-05 already defers RECON-04 behind operator timing,
running the baseline after 259 lands is strictly better and costs nothing. Record that as the
recommendation.

4.3 Numbers to supersede and to sanity-check against. The kickoff baseline is 8/28 (2026-08-13,
live, pre-reconcile). A prior point measurement was 5/28 (2026-08-11 post-sitting: 4 measured
plus Reverse Salient Analysis derived PASS). Graph composition at kickoff: 146 canonical
frameworks, 5 at 4/4, 20 at 3/4, 22 at 2/4, 9 at 1/4, 90 at 0/4. Add the cross-check from
RESEARCH.md F-12's consistency note: three documents researched the same day against the same
canon independently measured 86 total `USES_FRAMEWORK` edges graph-wide (75 correctly targeted,
11 unlabelled, 0 archived), 112 `:MindrianCommand` nodes, and 59 of 112 reaching zero frameworks
with alias traversal rescuing zero. Add a disambiguation line here so a later reader does not
conflate two different elevens: the `11` above counts UNLABELLED `USES_FRAMEWORK` EDGES, and it
is unrelated to the unaliased split-CLUSTER count, which was corrected on 2026-08-20 from 11 to
18 (live re-derived case-insensitively across all 186 `:Framework` nodes; 16 of the 18 resolved
in the drafted, unexecuted payload `payloads/relabel-fix-260820/`, 2 flagged as residue). The
cluster figure belongs to Phases 260 and 261; it is named here only to stop the stale 11
propagating into a RECON-04 sanity check where it does not apply. RECON-04's floor metric is a different measure (the
28-framework ratified denominator via `evaluateFloor`), but if RECON-04's probes produce a
materially different total-edges or total-commands number, that disagreement is itself a signal
to surface rather than silently trust.

4.4 The arithmetic trap. From RESEARCH.md Pitfall 7: any match-count prediction must count the
canon in BOTH `normalizeName` branches, because the direct-match branch and the alias branch can
each list the canonical, producing a measured N+1 against a predicted N. Recorded live in the
2026-08-11 deviations ("measured 6, not the predicted 5"). It applies to RECON-04's floor probes
as much as to a collapse card.

Close the section with the trigger sentence: when section 1 items 1 through 6 read CLOSED, run
the floor gate from `/home/jsagi/dev/MindrianOS-Plugin` with `MINDRIAN_BRAIN_URL` pinned to
`https://pws-brain-mcp.onrender.com`, record the result and the probe-failure count in this file,
and only then update `.planning/REQUIREMENTS.md`'s kickoff number.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -q '## 4. RECON-04' docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md && grep -q 'check-flagship-floor.cjs' docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md && grep -q 'TRUST-02' docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^## 4. RECON-04' <file>` outputs `1`.
    - The section names the absolute path `/home/jsagi/dev/MindrianOS-Plugin/scripts/check-flagship-floor.cjs` and records all three exit codes 0, 1 and 2, with exit code 2 distinguished from a miss.
    - `grep -q 'TRUST-01' <file>` and `grep -q 'TRUST-02' <file>` both exit 0, proving the honesty precondition is recorded rather than implied.
    - `grep -q '8/28' <file>` and `grep -q '5/28' <file>` both exit 0, proving both prior baselines are named as the numbers being superseded.
    - The section states the D-05 gating rule explicitly and reports no floor number measured during this task.
    - `node scripts/check-flagship-floor.cjs` was NOT executed by this task.
    - `grep -Pn '\x{2014}' <file>` exits 1 with no output.
  </acceptance_criteria>

  <done>
    The checklist carries a RECON-04 section naming the instrument, its exit-code contract, its
    honesty precondition on Phase 259, the numbers it supersedes, the arithmetic trap, and the
    exact trigger condition. No floor run happened.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator to Supabase `brain_api_keys` | admin credentials are minted and revoked here; no code path in either repo can observe or verify it |
| operator to Render dashboard env | `BRAIN_HTTP_ADMIN` and `BRAIN_HTTP_ADMIN_KEYS` live here, not in `render.yaml` |
| second machine filesystem to this filesystem | not reachable; everything about `C:/Users/PC/mindrian-brain-ingestion` is operator-reported |

## STRIDE Threat Register

Grounded in 258-RESEARCH.md "Security Domain" (ASVS V2, V4, V6) and its Known Threat Patterns
table. Every threat here is disposition `transfer` to the operator, because RESEARCH.md's
Validation Architecture already ruled that no code path can verify a Supabase key revocation or
a file deletion on an unreachable machine. This plan's mitigation is the written record itself;
it does not claim to close any of them.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-258-09 | Information Disclosure | admin key `9e3da1a7-8b66-4b35-9c2d-8b0bc740a650` pasted into a 2026-08-11 chat transcript, still live | transfer | checklist item 1 names it, states the `gen_random_uuid()`-inside-the-database rotation protocol, and section 0 corrects the requirement's premise so it cannot be skipped |
| T-258-10 | Elevation of Privilege | long-lived standing admin key | transfer | same item; the checklist records the preference for per-window temp keys |
| T-258-11 | Elevation of Privilege | temp key `1148f416...` labelled "revoke after run", still active | transfer | checklist item 2 |
| T-258-12 | Information Disclosure | `.tmp-admin-key` file still on disk on the second machine | transfer | checklist item 3 |
| T-258-13 | Information Disclosure | Gemini key state after the LangExtract passes, unverified | transfer | checklist item 4 |
| T-258-14 | Repudiation | RECON-03 marked done after revoking only one key | mitigate | Pitfall 6 is stated in section 0 as the named failure mode, and the acceptance criteria assert BOTH key ids appear in the file |
| T-258-SC | Tampering | npm/pip/cargo installs | mitigate | not applicable: this plan writes one markdown file and installs nothing. No package-manager install task exists, so no legitimacy checkpoint is required |

Threats this phase's own code DOES mitigate are registered in plans 258-01 (the GRAPH-WRITE-LOG
as the Repudiation control) and 258-06 (the admin-window close as the Elevation-of-Privilege
control). The five `transfer` rows above stay open at phase close, which is exactly why D-06
directs VERIFICATION.md to report `human_needed` for RECON-03 rather than a pass.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

Every file and symbol below is NEW in this phase. Source-grounding passes must exclude these
from drift verification because they do not exist upstream of Phase 258. All paths are relative
to `/home/jsagi/dev/ProblemsWorthSolving-Brain`.

| Path | Created by |
|---|---|
| `docs/GRAPH-WRITE-LOG.md` | 258-01 |
| `tests/graph-write-log-shape.test.mjs` (exports `parseLedgerRows`) | 258-01 |
| `scripts/probe-wave-attribution.mjs` (exports `resolveReadKey`, `call`, `q`, `initHandshake`) | 258-02 |
| `docs/2026-08-20-ATTRIBUTION-probe-results.md` | 258-02 |
| `docs/census-2026-08-20.md` | 258-02 |
| `docs/2026-08-20-ATTRIBUTION-untracked-writes.md` | 258-02 |
| `docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md` | 258-03 |
| `scripts/probe-order-collisions.mjs` | 258-04 |
| `payloads/order-collision-dishare-2026-08-20/00-preflight-evidence.md` | 258-04 |
| `payloads/order-collision-dishare-2026-08-20/manifest.json` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/README.md` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/90-dry-run.cypher` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/01-dishare-24219.cypher` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/02-dishare-gen-innov-opp.cypher` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/03-graphwriteevent.cypher` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/04-graphragmeta-stamp.cypher` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/05-close-window.md` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/91-verify.cypher` | 258-05 |
| `payloads/order-collision-dishare-2026-08-20/99-undo.cypher` | 258-05 |

New graph entities: the `:GraphWriteEvent` label (registered by 258-01, first node written by
258-06) with properties `id, date, phase, requirement_id, commit_sha, summary, node_count,
edge_count, operator, batch_id, created_by, created_at` on the singleton
`{id: 'gwe-258-recon02-2026-08-20'}`; and two dis-shared `:ProcessStep` nodes (per-framework
copies of `Identify Reverse Salients` and `Generate Innovation Opportunities`, each stamped
`batch_id` and `created_by:'payload'`), written by 258-06.

Modified existing files: `SCHEMA.md`, `src/contracts/schema-contract.mjs`, `src/ontology.mjs`,
`src/ingest/allowlist.mjs`, `tests/schema-contract.test.mjs` (all 258-01).

No MindrianOS-Plugin code is modified by this phase. RECON-04's instrument
(`/home/jsagi/dev/MindrianOS-Plugin/scripts/check-flagship-floor.cjs`) is referenced by absolute
path in this plan's checklist but deliberately not run or edited, per D-05.
</artifacts_this_phase_produces>

<verification>
- The checklist file exists in the Brain repo and is committed, so it travels to the second
  machine (unlike anything in MindrianOS-Plugin's gitignored `.planning/`).
- Seven `State:` lines, five OPEN and two CLOSED, each with a source.
- Both admin key ids, the on-disk secret file, the Gemini key, the Render service id and the
  second-machine path all appear verbatim.
- Section 4 documents the RECON-04 trigger without running it.
- Zero keys revoked, zero files deleted, zero floor runs performed by this plan.
</verification>

<success_criteria>
- RECON-03 is delivered as a tracked, findable, per-item-stated checklist rather than as an
  attempted automation, per D-04.
- RECON-04 is delivered as a documented, gated trigger rather than as a number measured too
  early, per D-05.
- The requirement's wrong "the minted key is dead" premise is corrected in writing with its
  evidence, so the operator cannot close RECON-03 by revoking the wrong key.
- The D-07 resume protocol is written down, so the navigator knows Claude is part of the loop.
- The commit lands in `/home/jsagi/dev/ProblemsWorthSolving-Brain`.
- Zero em-dashes added.
</success_criteria>

<output>
Create `.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-03-SUMMARY.md` when done.
</output>
