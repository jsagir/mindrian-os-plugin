---
phase: 258-reconcile-the-wave-hard-gates-all-writing-phases
plan: 02
type: execute
wave: 1
depends_on: []
target_repo: ProblemsWorthSolving-Brain
files_modified:
  - ProblemsWorthSolving-Brain/scripts/probe-wave-attribution.mjs
  - ProblemsWorthSolving-Brain/docs/2026-08-20-ATTRIBUTION-probe-results.md
  - ProblemsWorthSolving-Brain/docs/2026-08-20-ATTRIBUTION-untracked-writes.md
  - ProblemsWorthSolving-Brain/docs/census-2026-08-20.md
autonomous: true
requirements: [RECON-01]

must_haves:
  truths:
    - "A read-tier probe script exists that reuses the census script's HTTP helpers instead of minting a second client"
    - "Every probe run is pinned to https://pws-brain-mcp.onrender.com and refuses to report a number measured against the July replica or the local sandbox"
    - "The 2026-08-11/12 second-machine wave is attributed by framework name in a tracked artifact"
    - "The artifact states out loud that no pre-wave census exists, so a retroactive census subtraction is impossible"
    - "The id 28000-29000 archived-block finding has its own attribution section, distinct from the 08-11/12 wave, recording the repo-history channel as positively CLOSED (both local Brain histories begin 2026-07-22 and 2026-07-16; the operation dates to 2026-02-05) rather than as an unexplored gap"
    - "A forward census baseline exists so the NEXT wave is diffable even though this one was not"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-wave-attribution.mjs"
      provides: "Read-tier attribution probes plus reusable resolveReadKey/call/q/initHandshake exports"
      exports: ["resolveReadKey", "call", "q", "initHandshake"]
      min_lines: 120
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-ATTRIBUTION-probe-results.md"
      provides: "Machine-generated probe output, the measured evidence"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-ATTRIBUTION-untracked-writes.md"
      provides: "The two-target attribution narrative (Section A the wave, Section B the archived block)"
      contains: "Section B"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/census-2026-08-20.md"
      provides: "Forward census baseline written by the existing census script"
  key_links:
    - from: "scripts/probe-wave-attribution.mjs"
      to: "https://pws-brain-mcp.onrender.com/mcp brain_query"
      via: "SSE-aware JSON-RPC POST after an initialize handshake"
      pattern: "brain_query"
    - from: "docs/2026-08-20-ATTRIBUTION-untracked-writes.md"
      to: "docs/2026-08-20-ATTRIBUTION-probe-results.md"
      via: "every claim cites the probe id that produced it"
      pattern: "ATTRIBUTION-probe-results"
---

<objective>
Deliver RECON-01's attribution leg. Two distinct targets, kept distinguishable and never
conflated (258-RESEARCH.md F-12 is explicit that they are more likely two separate untracked
events than one):

- Target A: the 2026-08-11/12 second-machine enrichment wave from
  `C:/Users/PC/mindrian-brain-ingestion`, batch stamp `2026-08-11T07:56:31.708Z`. Attribution
  route is provenance forensics plus documentary cross-check, NOT a census subtraction.
- Target B: the id 28000-29000 archived block surfaced by the 2026-08-20 live Gate 0 diagnostic:
  186 `:Framework` nodes against an expected ~750, with 99 of 100 self-declared framework
  Concepts also carrying `:Archived` and 95 sitting in one contiguous internal-id block. The
  repo-history root-cause channel F-12 proposed has since been ATTEMPTED AND CLOSED (see task 3's
  superseding finding), so Target B's deliverable is a positively stated closed-channel verdict
  plus the forward GRAPH-WRITE-LOG guarantee, not a re-run of that hunt.

Purpose: RECON-01 says "fully attributed." The literal instrument it names (a read-tier census
diff) has no pre-wave operand, because the only census on disk (`docs/census-2026-08-18.md`) was
taken seven days after the wave AND after the Wave-1 reconciliation that itself moved ~4,100
nodes. Presenting an 08-18-to-08-20 subtraction as "the wave's delta" would be a false success
of exactly the class the standing WATCH memory tracks. This plan attributes by a route that
actually works and writes down why the named route does not.

Output: one probe script, one machine-generated evidence file, one narrative attribution doc
with two clearly separated sections, and a forward census baseline so the NEXT wave is diffable.

Hard-block honesty rule for this whole plan: if the read key is missing or the canon is
unreachable, the task STOPS and reports blocked. It never writes an attribution artifact from
memory, from RESEARCH.md's quoted numbers, or from a partial probe. Every number in the narrative
doc must trace to a probe id in the evidence file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-CONTEXT.md
@.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-RESEARCH.md
@.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-PATTERNS.md
@.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-VALIDATION.md
@.planning/debug/brain-gate0-diagnostic-260820.md
</context>

<cross_repo_contract>
Every file in this plan lives in `/home/jsagi/dev/ProblemsWorthSolving-Brain` (a separate,
normal git repo). All `git add` / `git commit` run with that directory as the working directory.
Nothing here commits to MindrianOS-Plugin. Both repos forbid em-dashes; hyphens only.
</cross_repo_contract>

<tasks>

<task type="auto">
  <name>Task 1: Check the read key, then author scripts/probe-wave-attribution.mjs</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-wave-attribution.mjs (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/run-schema-census.mjs` in full (188 lines): the shebang/banner idiom, `resolveReadKey()` lines 44-54, `call()` lines 56-73, `q()` lines 75-86, `judgeLabelCombo()` lines 88-96, the `initialize` handshake lines 99-104, the `say()` emitter and the file-write tail lines 106-107 and 181-188.
    - 258-PATTERNS.md Pattern Assignment 6, which quotes every one of those helpers verbatim and says which to copy as-is.
    - 258-RESEARCH.md Code Examples section, probes A, B, C, D and the note on E.
    - 258-RESEARCH.md F-12 (the archived-block finding and its named contents) and Pitfall 4 (probing the wrong store).
  </read_first>

  <action>
Step 1, the availability check (RESEARCH.md Open Question 3). Before writing anything, check
whether a read-tier key is resolvable: print only whether `MINDRIAN_BRAIN_KEY` is set in env and
whether `~/.mindrian.env` exists. Never print the key value. If BOTH are absent, STOP this plan
and report `BLOCKED: no read-tier key (MINDRIAN_BRAIN_KEY env or ~/.mindrian.env). Every RECON-01 probe hard-blocks.`
Do not proceed to author a script whose output cannot be produced.

Step 2, author `scripts/probe-wave-attribution.mjs`. Copy the preamble idiom from
`run-schema-census.mjs`: `#!/usr/bin/env node`, `'use strict';`, a block-comment banner naming
the file, stating it is Phase 258 RECON-01, stating READ-ONLY, listing the probe sections,
giving a `Usage:` line, and closing with the literal line `No em-dashes.`.

Copy `resolveReadKey()`, `call()` and `q()` VERBATIM from `run-schema-census.mjs` lines 44-86.
Do not mint a second HTTP client; Canon Part 7 and the repo's own Don't-Hand-Roll rule both
forbid it, and `q()`'s "unrecognized brain_query payload shape" throw is the control that stops
an all-zeros probe passing silently. Add a small `initHandshake(key)` wrapper around the
`initialize` call with `clientInfo: { name: 'wave-attribution', version: '1.0.0' }` and
`protocolVersion: '2024-11-05'`. Omitting the handshake is a silent failure mode.

Export `resolveReadKey`, `call`, `q` and `initHandshake` as named exports so plan 258-04's
`scripts/probe-order-collisions.mjs` imports them rather than copying them again. Keep the
`import.meta.url === pathToFileURL(process.argv[1]).href` main-guard from
`run-schema-census.mjs` lines 180-183 so the module stays importable without running `main`.

Set `BRAIN_URL` to `process.env.MINDRIAN_BRAIN_URL` falling back to the literal
`https://pws-brain-mcp.onrender.com`, and have `main()` print the resolved URL as its first line
of output.

Step 3, the store-identity guard (Pitfall 4, non-negotiable). Before any attribution probe, run
`MATCH (n) RETURN count(n) AS nodes;` and `MATCH ()-[r]->() RETURN count(r) AS edges;`. Print
both. Then hard-fail with a named diagnosis if the node count lands within 500 of `28325`
(the July replica) or within 500 of `30728` (the mutated local docker sandbox). The canon
signature recorded 2026-08-19 is 29,055 nodes and 24,018 edges; print the delta from that
signature rather than asserting equality, because the canon legitimately moves. The failure
message must name which wrong store the count matches.

Step 4, the probe set. Implement each probe as a labelled section that prints a markdown
heading of the form `### [P-n] <plain-language question>`, the Cypher it ran, and a markdown
table of rows. Copy the Cypher from 258-RESEARCH.md's Code Examples section exactly:

- `[P-1]` Target A fingerprint by absence: research probe A, matching nodes labelled `Framework`,
  `Phase`, `ProcessStep`, `FrameworkStep` or `Technique` where both `n.batch_id IS NULL` and
  `n.created_by IS NULL`, returning `labels(n)` and `count(*)` ordered descending, LIMIT 50.
- `[P-2]` Target A by timestamp: research probe B, `n.created_at STARTS WITH '2026-08-11'` or
  `STARTS WITH '2026-08-12'`, returning labels, name and created_at, LIMIT 200.
- `[P-3]` Target A edges by batch stamp: research probe C, `r.batch_id IS NOT NULL AND r.batch_id
  CONTAINS '2026-08-11'`, grouped by `type(r)` and `r.batch_id` with counts.
- `[P-4]` Target A named frameworks: research probe D, matching
  `(f:Framework)-[:HAS_PROCESS_STEP]->(s)` where `s.batch_id IS NULL AND s.created_by IS NULL`,
  returning `f.name` and `count(s)` ordered descending, LIMIT 50. This is the probe that
  satisfies "names every delta (frameworks touched)".
- `[P-5]` Target A exact-stamp confirm: nodes and edges carrying `batch_id` exactly equal to the
  string `2026-08-11T07:56:31.708Z`, LIMIT 200 each. The documentary anchors to reconcile against
  are "8 frameworks enriched" and "560/560 writes on bearer with zero MoatViolations".
- `[P-6]` Target B block census: nodes with `id(n) >= 28000 AND id(n) < 29000`, returning
  `labels(n)` and `count(*)` ordered descending.
- `[P-7]` Target B named contents: the same id range restricted to `'Archived' IN labels(n)`,
  returning `id(n)`, `n.name` and `labels(n)` ordered by node id, LIMIT 200.
- `[P-8]` Target B live Framework population: `MATCH (n:Framework) RETURN count(n)`. The
  diagnostic measured 186 against an expected ~750.
- `[P-9]` Target B provenance on the block: the same id range grouped by `n.batch_id` and
  `n.created_by` with counts, ordered descending. This is the single query most likely to name
  whoever ran it.

Write the accumulated markdown to `docs/2026-08-20-ATTRIBUTION-probe-results.md` using the
`say()` emitter plus the `fs.writeFileSync` tail idiom from `run-schema-census.mjs` lines
181-188, and also print to stdout. Header the file with the resolved brain URL, the run
timestamp, and the store-identity counts, so the evidence is self-describing about which graph
it measured.

Note for `[P-7]`: `.planning/seeds/SEED-079` records 327 nodes with SEP-concatenated names and
325 names over 200 chars, so some rows will look corrupted. Truncate name cells at 120 chars in
the table and do not treat a corrupted name as a probe failure. SEED-079 is out of scope here.

No em-dashes anywhere in the script or its output strings.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node -e "import('./scripts/probe-wave-attribution.mjs').then(m => process.exit(['resolveReadKey','call','q','initHandshake'].every(k => typeof m[k] === 'function') ? 0 : 1))"</automated>
  </verify>

  <acceptance_criteria>
    - The availability check printed a definite present/absent verdict for both key sources and the plan continued only if at least one was present.
    - The `node -e` import probe in the verify block exits 0, proving importing the module does not run `main` and all four helpers are exported.
    - `grep -c "pws-brain-mcp.onrender.com" scripts/probe-wave-attribution.mjs` outputs at least `1`.
    - `grep -q "28325" scripts/probe-wave-attribution.mjs && grep -q "30728" scripts/probe-wave-attribution.mjs` exits 0, proving the wrong-store guard names both wrong stores.
    - `grep -oE '\[P-[1-9]\]' scripts/probe-wave-attribution.mjs | sort -u | wc -l` outputs `9`.
    - `grep -Pn '\x{2014}' scripts/probe-wave-attribution.mjs` exits 1 with no output.
  </acceptance_criteria>

  <done>
    The probe script exists, exports its four helpers, pins the canon URL, refuses a wrong-store
    measurement by name, and implements all nine labelled probes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the probes and the forward census, then author Section A (the 08-11/12 wave)</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-ATTRIBUTION-probe-results.md (generated)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/census-2026-08-20.md (generated)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-ATTRIBUTION-untracked-writes.md (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-18-RUNBOOK-jtbd-rs-curation.md` lines 1-30: the Track B attribution paragraph naming `C:/Users/PC/mindrian-brain-ingestion`, batch `2026-08-11T07:56:31.708Z`, and "8 frameworks enriched".
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/jtbd.json` (the `_amendment_2026_08_18` block) and `.../reverse-salient-analysis.json` (the `_scope_note` block).
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-18-SESSION-e2e-unquilting.md` lines 25-60: the "08-11 wrote 560/560 on bearer with zero MoatViolations" figure.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/census-2026-08-18.md` sections 1 and 3. Section 3 is the only section that names individual frameworks.
    - 258-RESEARCH.md F-1, F-2, F-3, Pitfall 1, and Open Question 4.
  </read_first>

  <action>
Step 1, run the probes. From `/home/jsagi/dev/ProblemsWorthSolving-Brain`, run
`scripts/probe-wave-attribution.mjs` under node with `MINDRIAN_BRAIN_URL` set explicitly to
`https://pws-brain-mcp.onrender.com` (Pitfall 4: never rely on the default). The Render free
tier cold-starts and returns 429s, so a first-call failure is expected noise; retry once. If it
fails twice, record the failure verbatim in the summary and STOP. Do not synthesise results.

Step 2, run the forward census baseline. From the same directory and with the same
`MINDRIAN_BRAIN_URL` pin, run `scripts/run-schema-census.mjs` under node. It writes
`docs/census-2026-08-20.md`. Do not write a new census script; this 188-line one already judges
every label combination against `schema-contract.mjs` and already fails loud on an unrecognised
payload shape. Commit this file immediately as its own commit with the message
`docs(census): forward baseline before the 258 order-collision window` so the PRE-write census
is preserved in git history. Plan 258-07 re-runs the census after the ceremony, which overwrites
this same filename in the working tree; the git diff between the two commits is then the
ceremony's own census delta. Say this in the commit body so the overwrite is not mistaken for
data loss.

Step 3, author `docs/2026-08-20-ATTRIBUTION-untracked-writes.md`, Section A only. Use the Brain
repo's dated-artifact doc idiom: an `# <title>` heading, a `**Filed:**` line naming
`Phase 258 RECON-01, 2026-08-20`, then numbered sections.

Open with a section `## 0. Why this is not a census diff` that states plainly: RECON-01 names a
read-tier census diff as the instrument, and no pre-wave operand exists. The only census on disk
before today is `docs/census-2026-08-18.md`, taken seven days after the wave and after the Wave-1
`__Entity__` strip (4,357 to 232 bare), 32 case-variant alias MERGEs, a 118-Framework definition
backfill, a Louvain recompute over 28,847 nodes and the notion2 batch. Subtracting it from a
2026-08-20 census measures the 08-18/19 program, not the wave. Name the warning sign explicitly:
any delta table whose numbers reconcile to the `__Entity__` strip rather than to the wave's own
"8 frameworks / 560 writes" is measuring the wrong thing.

Then `## 1. Section A: the 2026-08-11/12 second-machine wave`, with these subsections:

- A.1 Documentary record. A source table with four rows: the 08-18 curation runbook lines 8-13,
  `tests/fixtures/framework-evals/jtbd.json`, `.../reverse-salient-analysis.json`, and
  `scripts/probe-framework-evals.mjs:129`. Each row records what that source independently names
  (source machine, batch stamp, counts, structures). Add the 08-18 session record's "560/560 on
  bearer with zero MoatViolations" as the write-count anchor.
- A.2 Live provenance forensics. The `[P-1]` through `[P-5]` results, each cited by probe id and
  pointing at `docs/2026-08-20-ATTRIBUTION-probe-results.md`. Cross-reference `[P-4]`'s framework
  names against `docs/census-2026-08-20.md` section 3 (frameworks using 2 or more of
  `HAS_PHASE`, `HAS_STAGE`, `HAS_PROCESS_STEP`, `HAS_STEP`), because that intersection is the
  strongest attribution evidence available: two writers using different structural vocabularies
  on the same framework is the exact signature of a second machine.
- A.3 Attribution table. One row per framework the wave touched, with the columns
  `framework | evidence (probe id or source path) | steps/nodes attributed | edges attributed | confidence`.
  This is what "names every delta" means in practice.
- A.4 Reconciliation against the anchors. Compare the probe-derived framework count against the
  documentary "8 frameworks" and the probe-derived write count against "560/560". Record any gap
  honestly rather than reconciling it by assertion.
- A.5 The bar, stated. Per RESEARCH.md Open Question 4: this artifact attributes at framework
  level plus aggregate node and edge counts per framework. Per-write enumeration of all 560
  writes is impossible without server-side request logs, which do not exist. Say so.

Leave a placeholder heading `## 2. Section B: the id 28000-29000 archived block` for task 3 with
a single line reading `(authored in task 3)` so the file structure is set.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && test -s docs/2026-08-20-ATTRIBUTION-probe-results.md && test -s docs/census-2026-08-20.md && grep -q '## 1. Section A' docs/2026-08-20-ATTRIBUTION-untracked-writes.md</automated>
  </verify>

  <acceptance_criteria>
    - `docs/2026-08-20-ATTRIBUTION-probe-results.md` is non-empty and its header names the resolved brain URL as `https://pws-brain-mcp.onrender.com` plus the measured node and edge counts.
    - The recorded node count is NOT within 500 of 28325 and NOT within 500 of 30728, and the results file says so.
    - `docs/census-2026-08-20.md` exists and is committed as its own commit before any write is made.
    - `grep -c '^## 0. Why this is not a census diff$' docs/2026-08-20-ATTRIBUTION-untracked-writes.md` outputs `1`.
    - `grep -oE '\[P-[1-5]\]' docs/2026-08-20-ATTRIBUTION-untracked-writes.md | sort -u | wc -l` outputs `5`, proving every Target A probe is cited in the narrative.
    - The A.3 attribution table has at least one row per framework returned by `[P-4]`.
    - `grep -Pn '\x{2014}' docs/2026-08-20-ATTRIBUTION-untracked-writes.md` exits 1 with no output.
  </acceptance_criteria>

  <done>
    Probes ran against the pinned canon, the forward census is committed as the pre-write
    baseline, and Section A attributes the wave by framework name with every claim traceable to
    a probe id or a tracked source path.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author Section B for the id 28000-29000 archived block (F-12), recording the repo-history channel as already closed</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-ATTRIBUTION-untracked-writes.md (modify)
  </files>

  <read_first>
    - `/home/jsagi/dev/MindrianOS-Plugin/.planning/debug/brain-gate0-diagnostic-260820.md` in full, especially sections 1-2, 5, 7-9 and 11 (the re-pointing table that names "258, RECON-01").
    - 258-RESEARCH.md F-12 in full, including the named contents of the archived block and Open Question 5. NOTE: F-12's "Root-cause hunt starting point" paragraph, which proposes cross-referencing the Brain repo git history against the 08-18/19 reconcile-in-place program, is SUPERSEDED. Read the action block below before acting on it.
    - `docs/2026-08-20-ATTRIBUTION-probe-results.md` probes `[P-6]` through `[P-9]` from task 2.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/SCHEMA.md` section 7 (the reconciliation ledger).
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/relabel-fix-260820/` (DRAFTED, NOT EXECUTED): the alias-split-cluster payload B.5 hands to Phases 260 and 261.
  </read_first>

  <action>
SUPERSEDING FINDING, read this before doing anything else in this task. A session working
directly in the ProblemsWorthSolving-Brain repo on 2026-08-20 already ATTEMPTED the repo-history
root-cause hunt that 258-RESEARCH.md F-12 proposed, and closed it out as a dead end. Do NOT
re-attempt it. The evidence, which this task RECORDS rather than re-derives:

- `ProblemsWorthSolving-Brain`'s earliest commit is 2026-07-22. The repo was extracted from
  `mindrian-brain-local`.
- `mindrian-brain-local`'s own earliest commit is 2026-07-16.
- The batch operation that stripped `:Framework` and applied `:Archived` to the id 28000-29000
  block dates to 2026-02-05, which neither history reaches.
- The conclusion that session reached, stated plainly: stop grepping repo history for it. If a
  record exists at all it lives in an Aura console audit log or in an environment that predates
  both repos.

So this task runs NO `git log` and NO `git show` root-cause hunt. Any earlier draft instruction
to grep the 21-commit 08-18/19 reconcile-in-place program is void on its own evidence: that
program ran roughly six months AFTER the operation and cannot be its cause.

Step 1, record the channel as positively closed. In the attribution doc, state the repo-history
channel's verdict as the literal token `CLOSED: PERMANENTLY UNATTRIBUTABLE VIA REPO HISTORY`,
with the three commit-date facts above as its evidence. This is a finding with a named basis, not
an unexplored gap, and it must not read like one.

Step 2, record the one remaining channel as operator-only. An Aura console audit log, if the
account retains one for 2026-02-05, is the only surface that could still name the operation. That
is a dashboard lookup on an account, not something any code path in either repo can reach, so it
is a STRETCH check for the operator and explicitly NOT a task in this phase. It is recorded as an
optional item in plan 258-03's checklist. Say plainly in the attribution doc that RECON-01 does
not block on it.

Step 3, state the real deliverable. The durable answer to this class of finding is not a
retroactive attribution the evidence cannot support. It is that from this phase forward every
write session appends a GRAPH-WRITE-LOG row and MERGEs a `GraphWriteEvent` node, so a future
block-scale relabel is attributable by construction. D-01, D-02 and D-03 cover that and plan
258-01 already landed it. Say so, and do not let a closed channel read as a phase failure.

Step 4, author `## 2. Section B: the id 28000-29000 archived block` in
`docs/2026-08-20-ATTRIBUTION-untracked-writes.md`, replacing the placeholder line. Subsections:

- B.1 What was measured. The `[P-6]` through `[P-9]` live results, side by side with the Gate 0
  diagnostic's same-day numbers (186 `:Framework` against an expected ~750; 99 of 100
  self-declared framework Concepts also `:Archived`; 95 in the contiguous id block). Where the
  live re-probe disagrees with the diagnostic, say so rather than adopting whichever number is
  more convenient.
- B.2 Named contents. The `[P-7]` list. RESEARCH.md's partial sample already names Six Thinking
  Hats, TRIZ, Design Thinking, Lean Startup, MECE, The Pyramid Principle, The Cynefin Framework,
  Four Lenses of Innovation, Jobs to Be Done (JTBD), Red Teaming, Pattern Recognition, Issue
  Trees, Causal Loop Diagrams, Problems Worth Solving, The PWS Value Proposition Framework, The
  Taxonomy of Problems, The Opportunity Bank Framework, White Space Analysis, Human-Centered
  Design, Effectuation and Open Innovation. Flag Red Teaming explicitly, because it is a claimant
  on the RECON-02 collision node 24219 and plan 258-04's pre-flight probe depends on this.
- B.3 Attribution verdict, in two parts. Part one, the repo-history channel: verdict
  `CLOSED: PERMANENTLY UNATTRIBUTABLE VIA REPO HISTORY`, evidenced by the 2026-07-22, 2026-07-16
  and 2026-02-05 dates from step 1. Part two, the Aura console audit log as the only remaining
  channel, operator-only and non-blocking, per step 2.
- B.4 Is this the same event as Section A? An explicit ruling. The evidence that argues NO is now
  stronger than when F-12 was written: Section A's wave is additive and dated 2026-08-11/12,
  Section B is subtractive and relabelling and dates to 2026-02-05, roughly six months earlier.
  Rule them SEPARATE events, both inside RECON-01's no-GRAPH-WRITE-LOG blast radius.
- B.5 Disposition and handoff. The relabel of the block is Phase 261's ceremony work, not this
  phase's; record it as a named handoff with the exact id range and count. Then record the
  CORRECTED split-cluster figure so downstream phases inherit it rather than the stale one: the
  count of unaliased split clusters is 18, NOT the 11 the Gate 0 diagnostic reported, live
  re-derived case-insensitively across all 186 `:Framework` nodes on 2026-08-20. 16 of the 18 are
  already resolved in a DRAFTED, NOT EXECUTED payload at `payloads/relabel-fix-260820/` in this
  repo. 2 are flagged residue needing a human survivor rule before they can resolve:
  `Jobs To Be Done` (id 45915, where 6 candidate Framework-labelled JTBD variants exist and
  picking one without a stated rule merely relocates the duplication problem) and
  `The Pyramid Principle` (id 39014, which is a `:Book` entity and therefore likely needs a
  `DESCRIBES` or `SOURCE_FOR` edge rather than `ALIAS_OF`, an entity-type mismatch rather than a
  duplicate). State that this alias work and its writes belong to Phases 260 and 261 and NOT to
  258; it is recorded here purely so those phases inherit the corrected number and the payload
  pointer. Close the subsection by noting that this section's finding is the reason plan 258-04
  runs a label-state pre-flight before authoring either order-collision card.

Add one closing section `## 3. What this changes going forward` recording that from this phase
on, every write session appends a row to `docs/GRAPH-WRITE-LOG.md` and MERGEs a `GraphWriteEvent`
node, so neither of these two attribution exercises is ever necessary again. Cross-link
`docs/GRAPH-WRITE-LOG.md`.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -q '## 2. Section B' docs/2026-08-20-ATTRIBUTION-untracked-writes.md && grep -q 'CLOSED: PERMANENTLY UNATTRIBUTABLE VIA REPO HISTORY' docs/2026-08-20-ATTRIBUTION-untracked-writes.md && grep -q 'relabel-fix-260820' docs/2026-08-20-ATTRIBUTION-untracked-writes.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^## 2. Section B' docs/2026-08-20-ATTRIBUTION-untracked-writes.md` outputs `1`, and the placeholder line `(authored in task 3)` no longer appears anywhere in the file.
    - Section B.3 contains the literal token `CLOSED: PERMANENTLY UNATTRIBUTABLE VIA REPO HISTORY` and names all three dates `2026-07-22`, `2026-07-16` and `2026-02-05` as its evidence.
    - This task ran ZERO `git log` and ZERO `git show` root-cause commands. The plan summary states the repo-history channel was inherited as already closed rather than re-attempted.
    - Section B.3 part two names the Aura console audit log as operator-only and non-blocking, and confirms RECON-01 does not block on it.
    - `grep -oE '\[P-[6-9]\]' docs/2026-08-20-ATTRIBUTION-untracked-writes.md | sort -u | wc -l` outputs `4`.
    - Section B.4 rules the two events SEPARATE and cites the roughly six-month date gap as its reason.
    - Section B.5 names the id range `28000` to `29000`, states the corrected cluster count as `18` while explicitly naming `11` as the superseded diagnostic figure, names `payloads/relabel-fix-260820/` as drafted and not executed, and names both residue items with their ids `45915` and `39014`.
    - `grep -q 'GRAPH-WRITE-LOG.md' docs/2026-08-20-ATTRIBUTION-untracked-writes.md` exits 0.
    - `grep -Pn '\x{2014}' docs/2026-08-20-ATTRIBUTION-untracked-writes.md` exits 1 with no output.
  </acceptance_criteria>

  <done>
    Section B exists as its own attribution target with a live re-measurement, a positively stated
    closed-channel verdict backed by three commit-date facts rather than an unexplored gap, an
    explicit SEPARATE-events ruling against Section A, the corrected 18-cluster figure with its
    payload pointer and two named residue items handed to Phases 260 and 261, and the forward
    GRAPH-WRITE-LOG guarantee named as the real deliverable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell -> remote canon over HTTPS | read-tier bearer token crosses here; a wrong URL crosses into the wrong store entirely |
| probe output -> tracked attribution artifact | an unverified number entering a tracked doc becomes an accepted fact |

## STRIDE Threat Register

Grounded in 258-RESEARCH.md "Security Domain" (ASVS V2 Authentication, V4 Access Control,
V7 Logging) and its Known Threat Patterns table.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-258-05 | Information Disclosure | the read-tier key in `~/.mindrian.env` | mitigate | the availability check prints presence only, never the value; `resolveReadKey()` is copied verbatim and never logs what it returns |
| T-258-06 | Elevation of Privilege | a probe accidentally holding an admin key | mitigate | ASVS V4 as implemented in `src/http/auth.mjs:83`: read keys can never confer admin, and `brain_query` is engine-read-only autocommit. Every probe in this plan uses `brain_query` only; `brain_write` is never called |
| T-258-07 | Tampering | a measurement taken against the July replica or the mutated local sandbox entering a tracked doc as canon truth | mitigate | the store-identity guard hard-fails within 500 of 28325 or 30728 and prints the delta from the 29,055 / 24,018 canon signature |
| T-258-08 | Repudiation | an attribution claim with no traceable evidence | mitigate | every number in the narrative doc cites a `[P-n]` probe id or a tracked source path; the acceptance criteria count the citations |
| T-258-SC | Tampering | npm/pip/cargo installs | mitigate | not applicable: zero new packages in this plan. The probe script uses only `node:fs`, `node:path`, `node:os`, `node:url` and global `fetch`. No package-manager install task exists, so no legitimacy checkpoint is required |

Out of scope and remaining open, per RESEARCH.md: the standing admin key
`9e3da1a7-8b66-4b35-9c2d-8b0bc740a650` pasted into a 2026-08-11 chat transcript, the still-active
temp key `1148f416...`, and the on-disk `.tmp-admin-key` on the second machine. All three are
RECON-03 operator items that no code path in this plan can fix; they are documented in plan
258-03's checklist.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

Every symbol and file below is NEW in this phase. Source-grounding passes must exclude these
from drift verification because they do not exist upstream of Phase 258.

**New files in `/home/jsagi/dev/ProblemsWorthSolving-Brain`:**

| Path | Created by | What it is |
|---|---|---|
| `docs/GRAPH-WRITE-LOG.md` | plan 258-01 | Append-only 8-column write ledger, D-01 source of truth |
| `tests/graph-write-log-shape.test.mjs` | plan 258-01 | fs + git structural guard, exports `parseLedgerRows` |
| `scripts/probe-wave-attribution.mjs` | plan 258-02 | Read-tier attribution probes, exports `resolveReadKey`, `call`, `q`, `initHandshake` |
| `docs/2026-08-20-ATTRIBUTION-probe-results.md` | plan 258-02 | Machine-generated probe evidence |
| `docs/census-2026-08-20.md` | plan 258-02 | Forward census baseline (written by the existing census script) |
| `docs/2026-08-20-ATTRIBUTION-untracked-writes.md` | plan 258-02 | Two-target attribution narrative |
| `docs/2026-08-20-CHECKLIST-recon03-operator-prerequisites.md` | plan 258-03 | RECON-03 operator checklist + RECON-04 unblock trigger |
| `scripts/probe-order-collisions.mjs` | plan 258-04 | Read-tier pre-flight probes for RECON-02 |
| `payloads/order-collision-dishare-2026-08-20/00-preflight-evidence.md` | plan 258-04 | Live measured state, authored before the cards |
| `payloads/order-collision-dishare-2026-08-20/manifest.json` | plan 258-05 | Batch declaration, `batch_id` `pws-ordercollision-2026-08-20` |
| `payloads/order-collision-dishare-2026-08-20/README.md` | plan 258-05 | The two cards in prose, evidence tables, execution record |
| `payloads/order-collision-dishare-2026-08-20/90-dry-run.cypher` | plan 258-05 | Read-only recompile |
| `payloads/order-collision-dishare-2026-08-20/01-dishare-24219.cypher` | plan 258-05 | Card 1 surgery |
| `payloads/order-collision-dishare-2026-08-20/02-dishare-gen-innov-opp.cypher` | plan 258-05 | Card 2 surgery |
| `payloads/order-collision-dishare-2026-08-20/03-graphwriteevent.cypher` | plan 258-05 | `GraphWriteEvent` singleton MERGE |
| `payloads/order-collision-dishare-2026-08-20/04-graphragmeta-stamp.cypher` | plan 258-05 | `GraphRagMeta{id:'canon'}` window stamp |
| `payloads/order-collision-dishare-2026-08-20/05-close-window.md` | plan 258-05 | The admin-window close as a numbered step file (D-11) |
| `payloads/order-collision-dishare-2026-08-20/91-verify.cypher` | plan 258-05 | Read-only done-signal assertions |
| `payloads/order-collision-dishare-2026-08-20/99-undo.cypher` | plan 258-05 | Full revert keyed on `batch_id` |

**New graph entities:**

| Entity | Created by | Shape |
|---|---|---|
| `:GraphWriteEvent` label | plan 258-01 registers it, plan 258-06 writes the first node | Tier 3 / agent-lane, edge-less by design |
| `GraphWriteEvent {id: 'gwe-258-recon02-2026-08-20'}` | plan 258-06 | props: `id, date, phase, requirement_id, commit_sha, summary, node_count, edge_count, operator, batch_id, created_by, created_at` |
| Two dis-shared `:ProcessStep` nodes | plan 258-06 | per-framework copies of `Identify Reverse Salients` and `Generate Innovation Opportunities`, each stamped `batch_id` and `created_by:'payload'` |

**Modified existing files:** `SCHEMA.md`, `src/contracts/schema-contract.mjs`, `src/ontology.mjs`,
`src/ingest/allowlist.mjs`, `tests/schema-contract.test.mjs` (all plan 258-01).

**No MindrianOS-Plugin code is modified by this phase.** RECON-04's instrument
(`/home/jsagi/dev/MindrianOS-Plugin/scripts/check-flagship-floor.cjs`) is referenced but
deliberately not run or edited here, per D-05.
</artifacts_this_phase_produces>

<verification>
- `docs/2026-08-20-ATTRIBUTION-probe-results.md` exists, names the pinned canon URL, and its
  node/edge counts pass the wrong-store guard.
- `docs/census-2026-08-20.md` exists and is committed before any graph write is made.
- `docs/2026-08-20-ATTRIBUTION-untracked-writes.md` has sections 0, 1 (A), 2 (B) and 3, with
  Target A and Target B kept separate and a same-event ruling in B.4.
- No `brain_write` call was made anywhere in this plan.
</verification>

<success_criteria>
- RECON-01's attribution leg is delivered for both targets, with every claim traceable to a probe
  id or a tracked source path.
- The impossibility of the literal census diff is written down in the artifact, not silently
  worked around.
- A forward census baseline exists so the next wave is diffable.
- All commits land in `/home/jsagi/dev/ProblemsWorthSolving-Brain`.
- Zero em-dashes added.
</success_criteria>

<output>
Create `.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-02-SUMMARY.md` when done.
</output>
