---
phase: 267.2-first-install-hooked-loop-repair-reward-investment-inserted
verified: 2026-09-03T12:37:25Z
status: passed
score: 12/12 must-haves verified (HOOK-01..HOOK-12)
overrides_applied: 0
deferred:
  - truth: "Desktop/Cowork carry a hook-side first-install router (Tri-Polar parity)"
    addressed_in: "a named future Tri-Polar follow-up phase (decision D-K / D-N item 4), plausibly alongside the blocked Phase 267 MCP-tool transition"
    evidence: "267.2-DECISIONS.md D-K: 'ship the classifier as a pure, exported, side-effect-free function ... so ONE implementation can serve both a CLI hook and a future MCP handler ... Do NOT ship an MCP-tool counterpart here.' Stated gap, not a phase 267.2 must-have -- CLAUDE.md's Tri-Polar rule treats a stated skip as a deliberate call, not an oversight."
  - truth: "The dev-research trail lands at ~/MindrianRooms/rethinking-mindrianos/research/2026-09-03-first-install-hooked-loop-repair/"
    addressed_in: "one navigator action (/mos:rooms switch rethinking-mindrianos), not a future phase"
    evidence: "267.2-10-SUMMARY.md Task 2: write-scope-check PreToolUse guard blocked the direct write (active room was jonathan-contractor-motj); guard correctly NOT bypassed per Phase 270/267.3-08 precedent; full body staged at 267.2-10-COMPOSITING-TRAIL-STAGED.md with the exact unblocking command named."
---

# Phase 267.2: First-Install Hooked Loop Repair (Reward + Investment) Verification Report

**Phase Goal:** Repair the FIRST_INSTALL surface's Reward leg (audit score 2/10) and Investment leg
(audit score 1/10) by wiring a deterministic UserPromptSubmit hook that (W1) classifies the user's
opening sentence locally and fires a real variable reward, and (W2) triggers the already-shipped
`identity_write`/`writeUserMdAtomic` path for a real investment deposit -- plus W0 (revert an
out-of-scope fix) and a folded todo (Door 1 persona-card `mentor` gap).
**Verified:** 2026-09-03T12:37:25Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | HOOK-01: pre-change baseline recorded before any code edit | VERIFIED | `267.2-BASELINE.md` records `run-all-267.1.sh` RED (exit 1, PASS=2 FAIL=1) at commit `3195ff79`; D-D's 5337ms-vs-3000ms measurement appended to DECISIONS.md, independently plausible (spawn architecture confirmed by code inspection) |
| 2 | HOOK-02: `run-all-267.2.sh` glob-discovers tests, fails on zero discovery, isolated-HOME fixture exported | VERIFIED | Ran `bash tests/run-all-267.2.sh` 3x clean: 19/0/0 each time (one earlier run showed 18/1/0, not reproducible across 3 immediate re-runs -- shared-working-tree flake, not a real regression) |
| 3 | HOOK-03/HOOK-04: W0 revert -- SEED-021 `AskUserQuestion` clause absent from FIRST_INSTALL, 267.1 audit pin inverted | VERIFIED | `node tests/test-267-2-w0-revert.cjs` 6/6 PASS; `node tests/test-267-1-first-install-hooked-audit.cjs` 8/8 PASS; read `scripts/session-start`'s FIRST_INSTALL region directly -- no `AskUserQuestion`/SEED-021 citation present |
| 4 | HOOK-05/HOOK-06: local, network-free classifier maps opening sentence to a bucket, buckets map exhaustively to 3 outcomes | VERIFIED | Read `lib/core/greeting-intent-detector.cjs` in full: pure function, zero `fetch`/`http`/`brain-client`/`process.env` references, additive-score classifier with a mandatory negative-weight HESITATION feature, tie/thin-margin falls to `ambiguous` (never the archetype.cjs first-declared-key bug) |
| 5 | HOOK-07: reward fires through wired machinery, no room, no API keys | VERIFIED | Read `_fireReward`/`_drainReward` in `scripts/first-install-router.cjs`: detached `spawn(...).unref()` of `scripts/mva-run.cjs`, never an inline `runPipeline` call; `tests/test-267-2-pre-room-reward.cjs` passing per aggregator |
| 6 | HOOK-08: router bucket/outcome instrumented scalar-only, no raw sentence on disk | VERIFIED | Telemetry write in `_classifyAndRoute` carries `sentence_sha256`, never `prompt`; `tests/test-267-2-router-telemetry.cjs` passing per aggregator |
| 7 | HOOK-09: `data/first-reward-surfaces.json` anchors stay truthful post-rewrite | VERIFIED | `bash tests/run-all-267.3.sh` 5/5 PASS, confirms all 4 declared-surface anchors live |
| 8 | HOOK-10: `~/.mindrian-user.md` written during FIRST_INSTALL, strictly after reward, through the one shipped writer, `detectArchetype` reads it correctly | VERIFIED | `node tests/test-267-2-user-md-roundtrip.cjs` 6/6 PASS including the explicit "NO SECOND WRITER (MEMOP-08)" and "C-3 FIXED" legs; independently grepped `renameSync` for the user-md path -- exactly one implementation (`lib/core/user-md-ops.cjs:509`) |
| 9 | HOOK-11 (folded todo): Door 1 reaches all 7 `ROLE_BLEND_KEYS` within the 4-option cap, `mentor` included | VERIFIED | `node tests/test-267-2-ignite-persona-coverage.cjs` 13/13 PASS; read `commands/ignite.md` directly -- two-step pick, `mentor -> blueprintFamily=exploration`, `portfolio_manager` absent (not minted, per D-J) |
| 10 | HOOK-12: hook born wired, HOOK ids registered, dev-research trail composited | VERIFIED (registration/wiring) / DEFERRED (room landing) | `hooks/hooks.json` registers `first-install-router.cjs` in the `UserPromptSubmit` chain after `mva-detect.cjs`; `node scripts/build-connector-registry.cjs --check` exits 0; all 12 HOOK ids present as `[x]` in `.planning/REQUIREMENTS.md`. Dev-research trail is staged, not landed -- blocked by `write-scope-check`, honestly recorded, one navigator command away (see Deferred Items) |
| 11 | Code-review BLOCKERs (CR-01, CR-02) genuinely fixed, not just claimed | VERIFIED | Read `lib/mcp/tools/identity.cjs` -- CR-01's bare write replaced with read-modify-write + nested `role_blend` merge + `ignoreOverride:true`. Read `_checkIgniteBirthCaptured`/`_askInvestment` in the router -- CR-02's partial fix (birth-journal check) present, with the honest residual documented in `267.2-CR-02-DEFERRED.md` |
| 12 | Code-review WARNINGs/INFO (WR-01..04, IN-01) genuinely fixed | VERIFIED | WR-01 (fail-closed arm write): confirmed at `main()`'s arm branch. WR-02 (sentence correlation): confirmed in `_fireReward`'s `pending_sentence_mismatch` guard. WR-03 (`ignoreOverride` option): confirmed in `lib/core/user-md-ops.cjs:221-234`, wired into both callers; `tests/test-267-2-wr-03-ignore-override.cjs` 4/4 PASS. WR-04 (misleading comment): confirmed corrected. IN-01 (`word_count` truncation): confirmed `word_count` now computed from `truncated`, not `trimmed` |

**Score:** 12/12 truths verified (HOOK-01..HOOK-12), all cross-checked against REQUIREMENTS.md's `[x]` rows and independently re-derived from source, not from SUMMARY.md prose alone.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Desktop/Cowork hook-side router (Tri-Polar parity) | A named future Tri-Polar follow-up phase | D-K/D-N item 4: classifier shipped as a pure function precisely so a future MCP handler can reuse it without redesign; MCP-tool counterpart explicitly out of scope this phase |
| 2 | Dev-research trail room landing | One navigator action (`/mos:rooms switch rethinking-mindrianos`) | `267.2-10-COMPOSITING-TRAIL-STAGED.md` carries the full body; blocked by `write-scope-check` (active room was `jonathan-contractor-motj`), guard correctly not bypassed |

Neither item blocks the phase goal: the Reward and Investment legs are both wired and working end to
end on the CLI surface (the surface the audit scored), and the compositing mandate's repo-to-room
cross-link half is satisfied (`267.2-DECISIONS.md`'s own "Dev-Research Compositing" section).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/first-install-router.cjs` | UserPromptSubmit hook, full `armed->routed->reward_pending->reward_delivered->investment_asked->done` state machine | VERIFIED | Read in full (969 lines). Every phase transition gated by construction of `main()`'s if/else chain; never blocks, never exits non-zero (uncaughtException net + every I/O wrapped) |
| `lib/core/greeting-intent-detector.cjs` | Pure local classifier, zero network/env/filesystem | VERIFIED | Read in full. Additive-score classifier, `HESITATION` negative-weight feature present, `word_count`/IN-01 fixed |
| `lib/mcp/tools/identity.cjs` | `identity_write` MCP tool, read-modify-write, no clobber | VERIFIED | CR-01 fix present: `readUserMd(path, {ignoreOverride:true})` -> `Object.assign` merge -> nested `role_blend` merge -> `writeUserMdAtomic` |
| `lib/core/user-md-ops.cjs` | Single atomic-write implementation, `ignoreOverride` option | VERIFIED | One `renameSync`-based write for the user-md path; `ignoreOverride` option present at line 221-234, documented as WR-03's fix |
| `commands/ignite.md` Door 1 | Two-step pick, all 7 `ROLE_BLEND_KEYS`, `mentor` included | VERIFIED | Read the Door 1 section directly; step 1/step 2 both stay inside the 4-option cap; Tri-Polar fallback line lists all 7 keys |
| `tests/run-all-267.2.sh` + 9 new test files | Glob-discovered, isolated-HOME, no-em-dash fence | VERIFIED | All 9 `tests/test-267-2-*.cjs` files plus the 4 fix-pass test files (`cr-01`, `cr-02`, `wr-01`..`wr-04`, `in-01` -- some combined) exist and pass |
| `267.2-CR-02-DEFERRED.md` | Honest deferral record for the CR-02 residual | VERIFIED | Read in full: names exactly what shipped, what did not, and two concrete follow-up options |
| Dev-research trail (`rethinking-mindrianos` room) | Landed research entry | NOT LANDED (staged) | Confirmed via `ls` -- target directory exists (created by `mkdir -p`) but is empty; full body staged in-repo at `267.2-10-COMPOSITING-TRAIL-STAGED.md` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `hooks/hooks.json` | `scripts/first-install-router.cjs` | `UserPromptSubmit` command entry | WIRED | Confirmed registered after `mva-detect.cjs`, before `brain-derivation-drain.cjs`; `build-connector-registry.cjs --check` exits 0 |
| `scripts/first-install-router.cjs` | `lib/core/greeting-intent-detector.cjs` | `require(...).classify(prompt)` in `_classifyAndRoute` | WIRED | Confirmed at line 335-338; degrades safely to `ambiguous`/`larry` on any throw |
| `scripts/first-install-router.cjs` | `scripts/mva-run.cjs` | detached `spawn(...).unref()` in `_fireReward` | WIRED | Confirmed at line 480-485; never an inline `runPipeline` call (architecturally impossible per the measured 5337ms-vs-3000ms budget) |
| `scripts/first-install-router.cjs` | `lib/core/user-md-ops.cjs::writeUserMdAtomic` | `_seedIdentityFile`'s read-modify-write | WIRED | Confirmed at line 707-729; gated strictly behind `reward_delivered` (D-L), test-pinned by `test-267-2-user-md-roundtrip.cjs`'s ORDERING assertion |
| `lib/mcp/tools/identity.cjs` | `lib/core/user-md-ops.cjs::writeUserMdAtomic` | read-modify-write (CR-01 fix) | WIRED | Confirmed at line 136-149; no longer a bare write |
| `scripts/first-install-router.cjs` | `lib/core/scratchpad-ops.cjs::readScratchpad` | `_checkIgniteBirthCaptured` (CR-02 partial fix) | WIRED | Confirmed at line 781-799; scoped to same-session `armed_at_ms`, tested against stale-answer false positive |
| `commands/ignite.md` Door 1 | `lib/core/persona-override.cjs::ROLE_BLEND_KEYS` | drift-pinned coverage test | WIRED | `test-267-2-ignite-persona-coverage.cjs` reads the live array, not a restated literal |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `_fireReward`'s Instant Brief | `pending` record | `lib/core/mva-state.cjs::readPending()`, written by `scripts/mva-detect.cjs` on the prior hook in the chain | Yes -- real classifier-scored pending state, correlated against `state.sentence_sha256` (WR-02 fix) rather than blindly trusted | FLOWING |
| `_seedIdentityFile`'s write | `journey_stage`/`last_detected_at` delta | Deterministic, locally computed (no model, no fetch) | Yes -- real timestamp + a valid `JOURNEY_STAGES` enum value (`ordinary_world`), confirmed round-trips correctly per `test-267-2-user-md-roundtrip.cjs`'s ROUNDTRIP leg | FLOWING |
| `identity_write`'s merged write | `existing` + `data` | `readUserMd(path, {ignoreOverride:true})` then caller-supplied MCP args | Yes -- real prior file state merged with new args, no longer a hardcoded/static overwrite | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full 267.2 suite | `bash tests/run-all-267.2.sh` (3 repeats) | 19/0/0, 19/0/0, 19/0/0 | PASS |
| 267.1 audit suite (delta) | `bash tests/run-all-267.1.sh` | 3/0/0 (was RED at baseline) | PASS |
| 267.3 guard suite | `bash tests/run-all-267.3.sh` | 5/0/0 | PASS |
| Connector registry | `node scripts/build-connector-registry.cjs --check` | `connector-registry: OK` | PASS |
| Acceptance roll-up | `node scripts/doctor.cjs --acceptance` | 17/18 (1 FAIL is `verify-release-clean-tree`, tracked-file drift from a concurrent unrelated session, confirmed via `git status --short`) | PASS (with named, unrelated pre-existing exception) |
| CR-01 regression | `node tests/test-267-2-cr-01-identity-write-merge.cjs` | PASS | PASS |
| CR-02 regression | `node tests/test-267-2-cr-02-ignite-birth-coordination.cjs` | 3/3 PASS | PASS |
| WR-01..04 / IN-01 regressions | individual `node tests/test-267-2-wr-0*.cjs` / `test-267-2-in-01-*.cjs` | all PASS | PASS |
| MEMOP-08 no-second-writer | `grep -rn renameSync lib/ scripts/ \| grep -i mindrian-user` | exactly 1 implementation site | PASS |

### Probe Execution

Not applicable -- this phase is not migration/tooling-probe-shaped; no `scripts/*/tests/probe-*.sh` files declared or discovered for 267.2.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|------------|--------------|--------|----------|
| HOOK-01 | 267.2-01 | Baseline recorded pre-change | SATISFIED | `267.2-BASELINE.md`, D-D measured confirmation |
| HOOK-02 | 267.2-02 | Test aggregator + isolated-HOME fixture | SATISFIED | `run-all-267.2.sh`, `test-267-2-helpers.cjs` |
| HOOK-03 | 267.2-03 | W0 revert | SATISFIED | `test-267-2-w0-revert.cjs` |
| HOOK-04 | 267.2-03 | GAP I-1 pin repaired | SATISFIED | `test-267-1-first-install-hooked-audit.cjs` |
| HOOK-05 | 267.2-05 | Local classifier | SATISFIED | `lib/core/greeting-intent-detector.cjs` |
| HOOK-06 | 267.2-05 | Exhaustive routing table | SATISFIED | `test-267-2-routing-table.cjs` |
| HOOK-07 | 267.2-07 | Reward fires, no room, no keys | SATISFIED | `_fireReward`/`_drainReward` |
| HOOK-08 | 267.2-06 | Scalar-only telemetry | SATISFIED | `_appendTelemetry` calls, `sentence_sha256` only |
| HOOK-09 | 267.2-08 | Declared-surfaces truthfulness | SATISFIED | `run-all-267.3.sh` |
| HOOK-10 | 267.2-09 | Investment write, no second writer, reader fix | SATISFIED | `test-267-2-user-md-roundtrip.cjs` |
| HOOK-11 | 267.2-04 | Door 1 `mentor` gap (folded todo) | SATISFIED | `test-267-2-ignite-persona-coverage.cjs` |
| HOOK-12 | 267.2-10 | Born wired + registered + composited | SATISFIED (registration/wiring) with a disclosed, deferred room-landing item | `hooks/hooks.json`, `.planning/REQUIREMENTS.md`, `267.2-10-COMPOSITING-TRAIL-STAGED.md` |
| MEMOP-08 | Phase 270 (binding constraint, not a 267.2 ID) | No second writer for `~/.mindrian-user.md` | SATISFIED | grep confirms exactly one `renameSync` site |

No orphaned requirements found: `grep -n "Phase 267.2"` against REQUIREMENTS.md maps exactly HOOK-01..12, all present in `267.2-DECISIONS.md` D-A's owning-plan table and all `[x]` in REQUIREMENTS.md.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none found) | -- | -- | -- | Grep for `TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER` across all touched files returned only false-positive substring matches inside the string "JTBD" (Jobs To Be Done) -- confirmed not a debt marker on inspection |

**Git hygiene note (info, not a gap):** commit `3a35f4f6` bundles the WR-03 (`ignoreOverride`) fix
into a commit whose message and stated file list describe an unrelated meeting-tool honesty fix
(`.planning/debug/meeting-file-meeting-false-success.md`). The actual diff confirms both changes
landed in that one commit (`lib/core/user-md-ops.cjs`, `scripts/first-install-router.cjs`,
`tests/test-267-2-wr-03-ignore-override.cjs` alongside `lib/mcp/tool-router.cjs`). This is consistent
with the documented heavily-shared-working-tree condition (two Claude Code sessions committing to
`main` concurrently, per this phase's own SUMMARY.md "Issues Encountered" section) rather than a
functional defect -- the WR-03 code itself is present, tested, and passing (`test-267-2-wr-03-ignore-override.cjs`
4/4 PASS). Flagged for hygiene awareness only.

### Human Verification Required

None outstanding. This phase's own blocking human-verify checkpoint (267.2-10 Task 3) was already
resolved in-conversation: the navigator (Jonathan Sagir) reviewed the diff surface, the regression
sweep, and the four judgment calls (D-C, D-D, D-H, D-K), and approved via a real `AskUserQuestion`
card (confirmed by `267.2-10-SUMMARY.md`'s "Task 3: Navigator sign-off (RESOLVED)" section, which
states the initial ASCII-box presentation was blocked by the Stop-hook `check-card-fire.cjs` and a
real selectable card was required and fired). No new human-verification need was discovered during
this independent check that the navigator's sign-off did not already cover.

One standing, pre-acknowledged manual-only item remains recorded in `267.2-VALIDATION.md`'s own
"Manual-Only Verifications" table (the real first-install experience's tone/feel can only be tested
after a release cut and pickup, per the repo's own 4x-proven install-cache staleness rule) -- this is
a structural limitation named by the phase itself, not a gap this verification discovered, and does
not block phase closure.

### Gaps Summary

No blocking gaps found. All 12 HOOK requirements are genuinely implemented (verified by direct code
read, not SUMMARY.md narrative) and pass their regression tests, re-run independently in this
verification (19/0/0 on `run-all-267.2.sh` across 3 repeats after an initial single-run flake proved
non-reproducible). The post-navigator-approval code-review fix pass (CR-01/CR-02/WR-01..04/IN-01) is
confirmed to be legitimate in-scope quality-gate work: every fix-pass commit touches only files this
phase shipped or made newly reachable (`scripts/first-install-router.cjs`,
`lib/core/greeting-intent-detector.cjs`, `lib/mcp/tools/identity.cjs`) plus its own new regression
test -- the same pattern Phase 257 used earlier in this session, not scope creep. CR-01 (the
`identity_write` clobber bug) is a full fix with a real regression test. CR-02 (the ignite-birth
collision) is honestly a partial fix: the common post-B1 case is closed, the harder
still-pending-card case is named and deferred with two concrete follow-up options in
`267.2-CR-02-DEFERRED.md`, not silently dropped.

Two items are recorded as deferred rather than gaps, because both are explicitly named,
reasoned, and owned rather than discovered as omissions: (1) Desktop/Cowork have no hook-side
router (Tri-Polar gap, D-K, a stated deliberate scope boundary with the classifier shipped
reuse-ready); (2) the dev-research trail is staged but not landed in the `rethinking-mindrianos`
room, blocked by `write-scope-check` and correctly not bypassed, needing exactly one navigator
action to land. Neither affects whether the FIRST_INSTALL Reward and Investment legs are actually
repaired on the surface the audit scored (Claude Code CLI) -- they are.

The regression sweep of `tests/run-all-179.sh` (11 passed / 1 failed) was independently re-run and
the failure (`179-08 ga4-card-fire-e2e`) independently confirmed unrelated: `git log --grep=267.2 --
scripts/check-card-fire.cjs` returns empty, and that file's real touch history (`caa54520`,
`3cfe3c0c`, `a9a9c064`, `4d26ff76`, and the unrelated `9eabf912`) predates this phase entirely.

---

_Verified: 2026-09-03T12:37:25Z_
_Verifier: Claude (gsd-verifier)_
