---
phase: 267.2-first-install-hooked-loop-repair-reward-investment-inserted
plan: 03
type: execute
wave: 2
depends_on: ["267.2-01", "267.2-02"]
files_modified:
  - scripts/session-start
  - tests/test-267-1-first-install-hooked-audit.cjs
  - tests/test-267-2-w0-revert.cjs
autonomous: true
requirements: [HOOK-03, HOOK-04]
canon_parts: [7]
must_haves:
  truths:
    - "The SEED-021 AskUserQuestion mandate is gone from the FIRST_INSTALL payload"
    - "The 267.1 Action-leg pin asserts the mandate's ABSENCE, matching its original pre-fix framing"
    - "tests/test-267-1-first-install-hooked-audit.cjs runs past its first assertion and executes every leg"
    - "scripts/session-start still parses under bash -n"
  artifacts:
    - path: "tests/test-267-2-w0-revert.cjs"
      provides: "Source assertion that the f39f24d9 clause is absent from the FIRST_INSTALL region"
      contains: "AskUserQuestion"
    - path: "scripts/session-start"
      provides: "FIRST_INSTALL payload restored to its pre-f39f24d9 three-approaches wording"
      contains: "Offer three approaches:"
  key_links:
    - from: "tests/test-267-2-w0-revert.cjs"
      to: "scripts/session-start"
      via: "readRegion anchored on the FIRST_INSTALL literal, never a line number"
      pattern: "First install detected"
    - from: "tests/test-267-1-first-install-hooked-audit.cjs"
      to: "lib/mcp/tools/identity.cjs"
      via: "GAP I-1 leg repaired to name the shipped 270-11 caller explicitly"
      pattern: "identity\\.cjs"
---

<objective>
W0: perform the navigator-ordered scope correction from the 267.1-06 Task 2 checkpoint, and repair
the 267.1 audit test so the whole file executes again.

Purpose: Phase 267.1 shipped the SEED-021 `AskUserQuestion` card mandate inside an otherwise
audit-only phase (commit `f39f24d9`). The navigator reversed that scope call and ordered the revert
deferred to this phase. Separately, research C-2 found the 267.1 test is already RED at its FIRST
assertion because Phase 270-11 added three new references to `~/.mindrian-user.md`, so every leg
below it, including the Action-leg pin this plan inverts, is currently unexecuted. Both edits land in
the SAME file and the file aborts on first failure, so research Pitfall 7 requires them in one wave.

Output: one clause removed from `scripts/session-start`, two legs repaired in
`tests/test-267-1-first-install-hooked-audit.cjs`, and a new phase-owned pin
`tests/test-267-2-w0-revert.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md
@.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-BASELINE.md
@.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-PATTERNS.md
</context>

## Artifacts this phase produces

Created by THIS plan: `tests/test-267-2-w0-revert.cjs` (new file).

Created by SIBLING plans, listed so a source-grounding pass does not flag them as drift:
`lib/core/greeting-intent-detector.cjs` (exports `classify`, `route`, `BUCKETS`, `OUTCOMES`,
`ROUTING_TABLE`), `scripts/first-install-router.cjs`, `tests/run-all-267.2.sh`,
`tests/test-267-2-helpers.cjs` (exports `withIsolatedHome`, `keylessEnv`, `readRegion`,
`assertNoRawText`), `tests/test-267-2-greeting-classifier.cjs`, `tests/test-267-2-routing-table.cjs`,
`tests/test-267-2-pre-room-reward.cjs`, `tests/test-267-2-router-telemetry.cjs`,
`tests/test-267-2-user-md-roundtrip.cjs`, `tests/test-267-2-ignite-persona-coverage.cjs`, the
`FIRST_INSTALL_HANDOFF` shell variable in `scripts/session-start`, the `~/.mindrian/first-install/`
state directory, and requirement ids `HOOK-01` through `HOOK-12`.

<tasks>

<task type="auto">
  <name>Task 1: Revert the f39f24d9 SEED-021 clause in the FIRST_INSTALL payload</name>
  <files>scripts/session-start</files>
  <read_first>
    - scripts/session-start (lines 657-705: the COLD_START_MENU assignment, the two FIRST-REWARD DECLARATION comment blocks, and the FIRST_INSTALL context= assignment)
    - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-RESEARCH.md (section "The exact line W0 reverts", which carries the exact before and after text)
    - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md (D-F, which forbids touching the COLD_START_MENU variable, and D-N item 6, which forbids merging this revert with plan 08's later prose rewrite)
  </read_first>
  <action>
    Inside the `if [ "$ONBOARD_TYPE" = "FIRST_INSTALL" ]` branch's single `context=` assignment,
    replace the clause

      `Offer three approaches - fire the AskUserQuestion card with these three as options; default to option 3 (Skip) if the user starts talking instead of picking; never render this menu as a prose list (SEED-021):`

    with the bare pre-`f39f24d9` wording

      `Offer three approaches:`

    Nothing else rides with it (CONTEXT.md D-06: "Exactly as scoped by the 267.1 checkpoint ...
    Nothing else rides with it"). Specifically do NOT in this task: touch the `COLD_START_MENU=`
    assignment, touch the UPDATE or MODE_ROUTING branches, change the three option labels
    (`1. Conversational Q&A`, `2. Document paste`, `3. Skip`), remove the
    `Based on your work in [domain]` literal, remove the `~/.mindrian-user.md` literal, or edit
    either FIRST-REWARD DECLARATION comment block. Plan 267.2-08 owns the prose rewrite; this is an
    ordered revert that ships as its own act.

    Confirm the target text against `git show f39f24d9 -- scripts/session-start` before editing, and
    anchor the edit on the literal string rather than a line number: the payload is one roughly
    3000-character physical line whose line number moves.

    After the edit, run `bash -n scripts/session-start`. No em-dashes anywhere.
  </action>
  <verify>
    <automated>bash -n scripts/session-start &amp;&amp; node -e "const fs=require('node:fs');const s=fs.readFileSync('scripts/session-start','utf8');const i=s.indexOf('[MindrianOS Onboarding] First install detected.');if(i<0)throw new Error('FIRST_INSTALL anchor missing');const fi=s.slice(i,s.indexOf('\n',i));if(fi.indexOf('AskUserQuestion')!==-1)throw new Error('AskUserQuestion still present in FIRST_INSTALL region');if(fi.indexOf('SEED-021')!==-1)throw new Error('SEED-021 still present in FIRST_INSTALL region');if(fi.indexOf('Offer three approaches:')===-1)throw new Error('pre-f39f24d9 wording not restored');if(s.indexOf('COLD_START_MENU=\"---\\\\nGet started:')===-1)throw new Error('COLD_START_MENU anchor changed');console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - The FIRST_INSTALL region contains `Offer three approaches:` and contains neither `AskUserQuestion` nor `SEED-021`.
    - The `COLD_START_MENU="---\nGet started:` anchor literal is byte-identical.
    - `bash -n scripts/session-start` exits 0.
    - `git diff --stat scripts/session-start` shows one changed line.
  </acceptance_criteria>
  <done>
    The FIRST_INSTALL region contains `Offer three approaches:` and contains neither `AskUserQuestion`
    nor `SEED-021`. The `COLD_START_MENU="---\nGet started:` anchor literal is byte-identical.
    `bash -n scripts/session-start` exits 0. `git diff --stat scripts/session-start` shows one changed
    line.
  </done>
</task>

<task type="auto">
  <name>Task 2: Invert the Action-leg pin and repair the GAP I-1 leg in the 267.1 audit test</name>
  <files>tests/test-267-1-first-install-hooked-audit.cjs</files>
  <read_first>
    - tests/test-267-1-first-install-hooked-audit.cjs (the whole file: the GAP I-1 leg at lines 31-60 and the Action-leg pin at lines 78-88 are both edited here)
    - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-BASELINE.md (the recorded pre-change failure text and the traceable grep behind the GAP I-1 count)
    - lib/mcp/tools/identity.cjs (the shipped 270-11 caller: USER_MD_PATH at lines 65-73, the writeUserMdAtomic call, and the CROSS-PHASE NOTE header naming this phase)
    - .planning/REQUIREMENTS.md (MEMOP-08, the binding "must not build a second writer" constraint the repaired leg must keep enforcing)
  </read_first>
  <action>
    Two edits in one file, in one commit, because the file aborts on first failure and splitting them
    across waves makes a green run prove nothing (research Pitfall 7).

    EDIT A, the Action-leg pin (currently the `ok('Action leg: the FIRST_INSTALL block carries the
    SEED-021 AskUserQuestion mandate (closed in 267.1-01)', ...)` block). Invert it to a NEGATIVE pin
    matching `267.1-VALIDATION.md` row 267.1-01-04's original pre-fix framing. Rename the description
    string so it reads as an absence pin and cites this phase's W0 and the 267.1-06 Task 2 navigator
    ruling as the reason. Change `assert.notEqual(fi.indexOf('AskUserQuestion'), -1, ...)` to
    `assert.equal(..., -1, ...)` and do the same for the `SEED-021` assertion, with failure messages
    that say the W0 revert regressed rather than that the Action leg regressed. Keep the
    `fi.length > 2000` sanity check and keep all three option-label assertions
    (`1. Conversational Q&A`, `2. Document paste`, `3. Skip`) asserted as PRESENT; plan 267.2-08 owns
    those labels, not this plan.

    EDIT B, the GAP I-1 leg (currently the first `ok(...)` block, which asserts
    `codeLines.length === 1`). Repair it to reflect post-270-11 reality without weakening what it
    guards. Replace the brittle integer count with a FILE-SET assertion derived from the grep, so the
    leg stays meaningful as files move:

    - Compute the set of distinct file paths from `grep -rn "mindrian-user\.md" lib/ scripts/ hooks/`
      after excluding matches whose path is `scripts/session-start` (that exclusion already exists and
      its reasoning comment stays).
    - Assert that set equals exactly `{ 'lib/core/user-archetype.cjs', 'lib/mcp/tools/identity.cjs' }`.
    - Assert the `lib/core/user-archetype.cjs` reference is still a `safeReadFile` call (the reader).
    - Assert `lib/mcp/tools/identity.cjs` is the ONLY writer-shaped reference, by keeping the existing
      `writerPattern` regex (`writeFileSync|writeUserMdAtomic|appendFileSync|createWriteStream`) and
      asserting every line it matches has the path prefix `lib/mcp/tools/identity.cjs:`.
    - Assert NO file under `scripts/` or `hooks/` matches the writer pattern for this path. This is
      the leg's real content after 270-11: the FIRST_INSTALL path still has no writer, which is what
      GAP I-1 always meant.

    Rewrite the leg's comment block to say, in plain language, that Phase 270-11 shipped
    `identity_write` as the MECHANISM half and that `.planning/REQUIREMENTS.md` MEMOP-08 binds Phase
    267.2 W2 to owning the TRIGGER without building a second writer, so a THIRD file appearing here is
    the signal to re-run the audit. Note in the same comment that plan 267.2-09 is the one authorised
    to add `scripts/first-install-router.cjs` to this set.

    Do not touch the GAP R-1 legs, the GAP G-1 leg, the reachability leg, or the `bash -n` leg.
    No em-dashes anywhere.
  </action>
  <verify>
    <automated>node tests/test-267-1-first-install-hooked-audit.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-267-1-first-install-hooked-audit.cjs` exits 0 and its printed assertion count is strictly greater than the count recorded in `267.2-BASELINE.md`, proving the file now executes past its first assertion.
    - The Action-leg block asserts `AskUserQuestion` and `SEED-021` are ABSENT and still asserts the three option labels are PRESENT.
    - The GAP I-1 block asserts a file SET rather than an integer count, and asserts no `scripts/` or `hooks/` file is writer-shaped for this path.
  </acceptance_criteria>
  <done>
    `node tests/test-267-1-first-install-hooked-audit.cjs` exits 0 and its printed assertion count is
    strictly greater than the count recorded in `267.2-BASELINE.md`, proving the file now executes past
    its first assertion. The Action-leg block asserts `AskUserQuestion` and `SEED-021` are ABSENT and
    still asserts the three option labels are PRESENT. The GAP I-1 block asserts a file SET rather
    than an integer count, and asserts no `scripts/` or `hooks/` file is writer-shaped for this path.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add the phase-owned W0 revert pin</name>
  <files>tests/test-267-2-w0-revert.cjs</files>
  <read_first>
    - tests/test-267-2-helpers.cjs (readRegion, the generalised one-line-payload slicer this test uses)
    - tests/test-267-1-first-install-hooked-audit.cjs (its header comment on anchoring to literals rather than line numbers, and its ok(desc, fn) reporting shape)
    - tests/run-all-267.2.sh (the aggregator that discovers this file by glob)
  </read_first>
  <action>
    Create `tests/test-267-2-w0-revert.cjs`, a plain CJS test using `node:assert/strict`, no framework,
    following the `ok(desc, fn)` reporting shape and final `PASS <name> (<n> assertions)` line used by
    `tests/test-267-1-first-install-hooked-audit.cjs`.

    It must pin, all anchored on literals via `readRegion` from `tests/test-267-2-helpers.cjs` and
    never on line numbers:

    1. The FIRST_INSTALL region of `scripts/session-start`, sliced from the anchor
       `[MindrianOS Onboarding] First install detected.`, contains neither `AskUserQuestion` nor
       `SEED-021`. Failure message must name commit `f39f24d9` and the 267.1-06 Task 2 navigator
       ruling so a future reader knows this is an ordered revert, not an accident.
    2. That same region contains the restored literal `Offer three approaches:`.
    3. A sanity guard that the sliced region is longer than 2000 characters, so a bad slice cannot
       make assertions 1 and 2 pass vacuously.
    4. The `COLD_START_MENU="---\nGet started:` anchor literal is still present in
       `scripts/session-start`, pinning decision D-F (the variable is not modified by this phase) and
       protecting `tests/test-267.3-session-start-declaration.cjs` from a silent break.
    5. `scripts/session-start` still parses: `execFileSync('bash', ['-n', SESSION_START])`.

    Why assertion 1 is a NEGATIVE pin and must stay one: this phase deliberately removes a mandate a
    prior phase added, so a future well-meaning session that re-adds it must fail loudly rather than
    quietly reintroduce the scope error the navigator reversed.

    No em-dashes anywhere.
  </action>
  <verify>
    <automated>node tests/test-267-2-w0-revert.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-267-2-w0-revert.cjs` exits 0 and prints a PASS line with at least 5 assertions.
    - The file references `f39f24d9` in a failure message, uses `readRegion` from `tests/test-267-2-helpers.cjs`, and contains no hardcoded line numbers into `scripts/session-start`.
  </acceptance_criteria>
  <done>
    `node tests/test-267-2-w0-revert.cjs` exits 0 and prints a PASS line with at least 5 assertions.
    The file references `f39f24d9` in a failure message, uses `readRegion` from
    `tests/test-267-2-helpers.cjs`, and contains no hardcoded line numbers into `scripts/session-start`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source -> injected model prose | `scripts/session-start`'s payload becomes `additionalContext` the model acts on |
| test assertions -> future sessions | A weakened pin lets a reversed scope decision silently return |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267.2-06 | Repudiation | `tests/test-267-2-w0-revert.cjs` | mitigate | negative pin on `AskUserQuestion` and `SEED-021` with a failure message naming commit `f39f24d9` and the navigator ruling, so a re-add is loud |
| T-267.2-07 | Tampering | `tests/test-267-1-first-install-hooked-audit.cjs` GAP I-1 leg | mitigate | repaired to a file-SET assertion plus a writer-shape assertion scoped to `lib/mcp/tools/identity.cjs`, and an explicit assertion that no `scripts/` or `hooks/` file is writer-shaped, keeping MEMOP-08 enforceable |
| T-267.2-08 | Tampering | `scripts/session-start` COLD_START_MENU | mitigate | pinned byte-identical by assertion 4, protecting the `session-start:COLD_START_MENU` registry anchor and `tests/test-267.3-session-start-declaration.cjs` |
| T-267.2-SC | Tampering | npm/pip/cargo installs | accept | zero external packages in this plan |
</threat_model>

<verification>
- `bash -n scripts/session-start` exits 0
- `node tests/test-267-1-first-install-hooked-audit.cjs` exits 0 with more assertions than the
  baseline recorded
- `node tests/test-267-2-w0-revert.cjs` exits 0
- `bash tests/run-all-267.3.sh` still passes (the COLD_START_MENU and FIRST_INSTALL anchors are
  unchanged by this plan)
- `bash tests/run-all-267.2.sh`
</verification>

<success_criteria>
- The `f39f24d9` clause is gone and the pre-fix wording is restored, one line changed
- The 267.1 audit test executes every leg and exits 0
- A phase-owned negative pin exists and passes
- `data/first-reward-surfaces.json` is untouched by this plan and `run-all-267.3.sh` stays green
</success_criteria>

<output>
Create `.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-03-SUMMARY.md` when done
</output>
