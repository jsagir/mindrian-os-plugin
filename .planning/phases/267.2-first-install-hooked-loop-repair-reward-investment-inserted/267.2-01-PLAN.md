---
phase: 267.2-first-install-hooked-loop-repair-reward-investment-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-BASELINE.md
  - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md
autonomous: true
requirements: [HOOK-01]
canon_parts: [7, 8]
must_haves:
  truths:
    - "The pre-change PASS/FAIL of the 267.1 suite is on the record before any code edit lands"
    - "The measured wall-clock cost of the Instant Brief pipeline is on the record, not inferred"
    - "D-D's architecture call carries a measured number rather than a design-constant inference alone"
  artifacts:
    - path: ".planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-BASELINE.md"
      provides: "Recorded pre-change state of bash tests/run-all-267.1.sh and node tests/test-267-1-first-install-hooked-audit.cjs"
      contains: "run-all-267.1.sh"
    - path: ".planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md"
      provides: "Planner decisions record, extended with the measured D-D confirmation section"
      contains: "D-D"
  key_links:
    - from: "267.2-BASELINE.md"
      to: "tests/test-267-1-first-install-hooked-audit.cjs"
      via: "recorded verbatim failure text of the first assertion"
      pattern: "GAP I-1"
---

<objective>
Record the two measurements every later plan in this phase reasons against, BEFORE any code edit
lands: (1) the honest pre-change state of the 267.1 test suite, which is RED today for a reason
unrelated to this phase, and (2) the real wall-clock cost of the Instant Brief pipeline, which
research left unmeasured at LOW confidence and which decides W1b's architecture.

Purpose: research finding C-2 established that `tests/test-267-1-first-install-hooked-audit.cjs` is
already failing on `main` at its FIRST assertion, so every leg below it is currently unexecuted. A
bare "green after my change" claim later in this phase would be dishonest without this record. And
research Assumption A6 / Open Question 3 flagged the `runPipeline` hook-budget question as LOW
confidence and explicitly said "measure in Wave 0" before committing to an architecture.

Output: `267.2-BASELINE.md` and a new measured-confirmation section appended to `267.2-DECISIONS.md`.
No production code is touched by this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md
@.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-RESEARCH.md
</context>

## Artifacts this phase produces

Created by THIS plan (new symbols and paths, not pre-existing):

- `.planning/phases/267.2-.../267.2-BASELINE.md` (new file)
- `267.2-DECISIONS.md` section `## D-D measured confirmation` (new section in a file the planner
  already created)

Created by SIBLING plans in this phase, listed so a source-grounding pass does not flag them as
drift: `lib/core/greeting-intent-detector.cjs` (exports `classify`, `route`, `BUCKETS`, `OUTCOMES`,
`ROUTING_TABLE`, and the exported regex constants), `scripts/first-install-router.cjs`,
`tests/test-267-2-helpers.cjs` (exports `withIsolatedHome`), `tests/run-all-267.2.sh`,
`tests/test-267-2-w0-revert.cjs`, `tests/test-267-2-greeting-classifier.cjs`,
`tests/test-267-2-routing-table.cjs`, `tests/test-267-2-pre-room-reward.cjs`,
`tests/test-267-2-router-telemetry.cjs`, `tests/test-267-2-user-md-roundtrip.cjs`,
`tests/test-267-2-ignite-persona-coverage.cjs`, the `FIRST_INSTALL_HANDOFF` shell variable in
`scripts/session-start`, the `~/.mindrian/first-install/` state directory, and requirement ids
`HOOK-01` through `HOOK-12`.

<tasks>

<task type="auto">
  <name>Task 1: Record 267.2-BASELINE.md before any code edit</name>
  <files>.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-BASELINE.md</files>
  <read_first>
    - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-RESEARCH.md (section "C-2", and the "Validation Architecture" table row for BASELINE)
    - tests/run-all-267.1.sh (the aggregator whose output is being recorded, and its header comment explaining its own gate lines)
    - tests/test-267-1-first-install-hooked-audit.cjs (the file whose first assertion is failing)
  </read_first>
  <action>
    Run, from the repo root, and capture the FULL stdout and stderr plus the exit code of each:
    `bash tests/run-all-267.1.sh`, then `node tests/test-267-1-first-install-hooked-audit.cjs`, then
    `git rev-parse --short HEAD`, then `node -e "console.log(require('./.claude-plugin/plugin.json').version)"`.

    Write `267.2-BASELINE.md` recording, in this order: the commit sha and plugin version measured
    against; the exact command line, exit code and verbatim output of the aggregator; the exact
    command line, exit code and verbatim first-failure text of the individual audit test; and a short
    plain-language reading that states which assertion aborts the file and which legs are therefore
    UNEXECUTED today. Name the specific legs by their `ok(...)` description strings so a later reader
    can tell exactly what a post-change green run does and does not prove.

    Also record the count and the file paths behind the `GAP I-1` grep, by running
    `grep -rn "mindrian-user\.md" lib/ scripts/ hooks/` and pasting the result, so the number in the
    assertion failure is traceable to specific files rather than to a bare integer.

    Do not fix anything. Do not edit any file under `scripts/`, `lib/`, `tests/`, `commands/`,
    `data/`, or `hooks/` in this task. If the suite is unexpectedly GREEN, record that outcome
    verbatim as well; research C-2 explicitly warns its snapshot may have been repaired by another
    session, and a green baseline is a valid recorded result, not a reason to skip the record.

    No em-dashes anywhere in the file; hyphens only.
  </action>
  <verify>
    <automated>test -f .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-BASELINE.md &amp;&amp; grep -q "run-all-267.1.sh" .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-BASELINE.md &amp;&amp; grep -q "mindrian-user" .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-BASELINE.md &amp;&amp; ! grep -qP '\\x{2014}' .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-BASELINE.md &amp;&amp; git diff --name-only | grep -Ev "^\.planning/" | grep -q . &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `267.2-BASELINE.md` exists, records the commit sha, the plugin version, both commands with exit codes and verbatim output, the traceable grep result behind the GAP I-1 count, and a named list of the assertion legs that are unexecuted.
    - `git status` shows no modified file outside `.planning/`.
  </acceptance_criteria>
  <done>
    `267.2-BASELINE.md` exists, records the commit sha, the plugin version, both commands with exit
    codes and verbatim output, the traceable grep result behind the GAP I-1 count, and a named list of
    the assertion legs that are unexecuted. `git status` shows no modified file outside `.planning/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Measure the Instant Brief pipeline cost and append the D-D confirmation</name>
  <files>.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md</files>
  <read_first>
    - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md (section D-D, the architecture call this measurement confirms or contradicts)
    - lib/core/mva-orchestrator.cjs (runPipeline, and the _mvaDir / _writeSideFile / _writeStateManifest helpers)
    - lib/core/mva-dispatcher.cjs (its header states binding decision B2: 45s global wall-clock budget, 35s per-agent budget)
    - lib/core/mva-state.cjs (writePending / readPending / markRunning / markComplete / isAlreadyRunning, and its homeDir() env-aware resolution)
    - scripts/mva-run.cjs (the thin CLI wrapper around runPipeline)
    - hooks/hooks.json (the UserPromptSubmit chain and every entry's timeout value)
  </read_first>
  <action>
    Measure, do not infer. Under a throwaway isolated HOME created with `fs.mkdtempSync` and passed
    as BOTH `HOME` and `USERPROFILE` (research Pitfall 6: `os.homedir()` reads /etc/passwd and ignores
    `process.env.HOME` on POSIX, so both must be set and the child must be a fresh process), and with
    `ANTHROPIC_API_KEY` and `TAVILY_API_KEY` explicitly UNSET in the child env so the measurement is
    the real fresh-install no-key path:

    1. Seed a pending MVA state by calling `mvaState.writePending` with a synthetic
       `sentence_sha256` (a sha256 of a fixed literal string, never a real user sentence) and the
       fields `mva-state.cjs`'s own schema comment documents.
    2. Time `node scripts/mva-run.cjs` end to end with `Date.now()` around a `spawnSync`, recording
       elapsed milliseconds, exit code, whether `~/.mindrian/mva/state.json` was written, and whether
       a `~/.mindrian/mva/briefs/<sha8>.json` side file appeared.
    3. Repeat the timing three times and record all three numbers plus the max, not an average, since
       the question is whether a WORST case fits a hook budget.
    4. Record the largest `timeout` value present on any `UserPromptSubmit` entry in
       `hooks/hooks.json` as the comparison ceiling.

    Clean up the temp HOME with `fs.rmSync(dir, {recursive:true, force:true})` in a `finally` block.
    Never write to the developer's real `$HOME`; research Pitfall 6 records a live incident where
    exactly this happened during the research session.

    Append a new section `## D-D measured confirmation` to `267.2-DECISIONS.md` recording: the three
    timings and the max, the hook ceiling, the exit code, the artifacts observed, the env under which
    it ran, and a one-line verdict in the form "max measured Xms versus hook ceiling Yms: D-D's
    detached-spawn architecture is CONFIRMED / CONTRADICTED". If CONTRADICTED, do not change the
    architecture: record it, and state explicitly that D-D's stated reason for keeping the detached
    shape anyway (a fast no-key run says nothing about a slow Brain-reachable run, and the hook must
    not have a bimodal latency profile) is what carries the decision.

    Do not edit any other section of `267.2-DECISIONS.md`. No em-dashes anywhere.
  </action>
  <verify>
    <automated>grep -q "D-D measured confirmation" .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md &amp;&amp; grep -qE "CONFIRMED|CONTRADICTED" .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md &amp;&amp; test ! -f "$HOME/.mindrian-user.md" &amp;&amp; ! grep -qP '\\x{2014}' .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `267.2-DECISIONS.md` carries a `## D-D measured confirmation` section with three measured timings, the max, the hook-chain ceiling in ms, the exit code, the observed artifacts, the no-key env, and an explicit CONFIRMED or CONTRADICTED verdict.
    - The developer's real `$HOME` is unmodified: no `~/.mindrian-user.md` was created and no pre-existing `~/.mindrian/` content was altered.
  </acceptance_criteria>
  <done>
    `267.2-DECISIONS.md` carries a `## D-D measured confirmation` section with three measured timings,
    the max, the hook-chain ceiling in ms, the exit code, the observed artifacts, the no-key env, and
    an explicit CONFIRMED or CONTRADICTED verdict. The developer's real `$HOME` is unmodified: no
    `~/.mindrian-user.md` was created and no pre-existing `~/.mindrian/` content was altered.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> developer `$HOME` | A measurement child process can write real dotfiles if HOME is not isolated |
| repo -> planning artifacts | Recorded output could carry a real user sentence if the measurement used one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267.2-01 | Tampering | `scripts/mva-run.cjs` measurement child | mitigate | `fs.mkdtempSync` HOME, both `HOME` and `USERPROFILE` overridden, `fs.rmSync` in a finally block; verify asserts no `~/.mindrian-user.md` in the real home (research Pitfall 6, threat T-270-13) |
| T-267.2-02 | Information disclosure | `267.2-BASELINE.md`, `267.2-DECISIONS.md` | mitigate | the measurement seeds a sha256 of a fixed literal, never a real user sentence; Canon Part 8 scalar-only discipline applies to the recorded numbers |
| T-267.2-SC | Tampering | npm/pip/cargo installs | accept | this phase installs zero external packages; `267.2-RESEARCH.md` Package Legitimacy Audit records the gate as not applicable and `package.json` dependencies are unchanged |
</threat_model>

<verification>
- `bash tests/run-all-267.1.sh` produces the same result as recorded in `267.2-BASELINE.md` when
  re-run immediately after this plan (this plan changes no code, so the baseline must reproduce)
- `git status --porcelain` lists only files under `.planning/`
- `test ! -f "$HOME/.mindrian-user.md"` (the real home is untouched)
</verification>

<success_criteria>
- `267.2-BASELINE.md` exists with commit sha, plugin version, both command exit codes, verbatim
  output, the traceable GAP I-1 grep, and the named list of unexecuted legs
- `267.2-DECISIONS.md` carries `## D-D measured confirmation` with three timings, a max, the hook
  ceiling, and a CONFIRMED / CONTRADICTED verdict
- No production file is modified by this plan
</success_criteria>

<output>
Create `.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-01-SUMMARY.md` when done
</output>
