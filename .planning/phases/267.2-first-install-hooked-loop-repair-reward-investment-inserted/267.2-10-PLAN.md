---
phase: 267.2-first-install-hooked-loop-repair-reward-investment-inserted
plan: 10
type: execute
wave: 7
depends_on: ["267.2-03", "267.2-04", "267.2-06", "267.2-08", "267.2-09"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-VALIDATION.md
autonomous: false
requirements: [HOOK-12]
canon_parts: [6, 7, 8]
must_haves:
  truths:
    - "Every HOOK id this phase used is registered in .planning/REQUIREMENTS.md with its real owning plan"
    - "The whole gate set runs green and is reported as a delta against the recorded baseline, not as a bare claim"
    - "The durable reasoning trail exists in the rethinking-mindrianos room, cross-linked in both directions"
    - "A human has confirmed the actual first-install experience before the phase closes"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "HOOK-01 through HOOK-12 registered with the phase-local minted-at-plan-time caveat"
      contains: "HOOK-01"
    - path: ".planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-VALIDATION.md"
      provides: "Task ID, Plan, Wave and Requirement columns reconciled against real plan numbers"
      contains: "267.2-0"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 267.2 Requirements line updated from TBD to the minted family"
      pattern: "HOOK-"
---

<objective>
Close the phase honestly: register the minted requirement ids, reconcile the validation contract, run
the full gate set as a delta against the recorded baseline, land the dev-research trail, and put the
actual first-install experience in front of a human before declaring it repaired.

Purpose: this phase changes what every new user sees on first contact. Automated tests exercise the
dev workspace, but `~/.claude/plugins/mindrian-os/` is a separate copy that does not update from a
`main` commit, a standing rule proven four times. `267.2-VALIDATION.md`'s Manual-Only Verifications
table already records that the real experience only exists after a release cut and pickup. A blocking
human-verify checkpoint is therefore warranted, following the Phase 257-09 Task 3 precedent.

Output: registered ids, a reconciled validation contract, a measured gate roll-up, the composited
research trail, and a navigator sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md
@.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-VALIDATION.md
</context>

## Artifacts this phase produces

Created by THIS plan: requirement rows `HOOK-01` through `HOOK-12` in `.planning/REQUIREMENTS.md`, and
the research trail directory
`~/MindrianRooms/rethinking-mindrianos/research/2026-09-03-first-install-hooked-loop-repair/`.

Created by SIBLING plans, listed so a source-grounding pass does not flag them as drift:
`lib/core/greeting-intent-detector.cjs` (exports `classify`, `route`, `BUCKETS`, `OUTCOMES`,
`ROUTING_TABLE`), `scripts/first-install-router.cjs`, `tests/run-all-267.2.sh`,
`tests/test-267-2-helpers.cjs` (exports `withIsolatedHome`, `keylessEnv`, `readRegion`,
`assertNoRawText`), `tests/test-267-2-w0-revert.cjs`, `tests/test-267-2-greeting-classifier.cjs`,
`tests/test-267-2-routing-table.cjs`, `tests/test-267-2-pre-room-reward.cjs`,
`tests/test-267-2-router-telemetry.cjs`, `tests/test-267-2-user-md-roundtrip.cjs`,
`tests/test-267-2-ignite-persona-coverage.cjs`, the `FIRST_INSTALL_HANDOFF` shell variable in
`scripts/session-start`, and the `~/.mindrian/first-install/` state directory.

<tasks>

<task type="auto">
  <name>Task 1: Register HOOK-01..HOOK-12, reconcile the validation contract, and run the full gate set</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-VALIDATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the MEMOP, CHOKE, GUARD, PYPORT and ANCHOR family rows, so the HOOK rows match the established row shape and carry the same minted-at-plan-time caveat)
    - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md (D-A, the id table with its owning-plan column, and D-M, the validation reconciliation this task performs)
    - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-VALIDATION.md (the Per-Task Verification Map whose Task ID, Plan, Wave and Requirement columns read TBD, and the Validation Sign-Off checklist)
    - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-BASELINE.md (the recorded pre-change state every green claim is measured against)
    - .planning/ROADMAP.md (the Phase 267.2 entry: its Requirements line reads TBD and its Plans line reads 0 plans)
  </read_first>
  <action>
    Three record-keeping acts, then one measurement.

    REGISTER. Add twelve rows to `.planning/REQUIREMENTS.md`, `HOOK-01` through `HOOK-12`, using the
    behaviour statements in `267.2-DECISIONS.md` D-A verbatim and matching the row shape the MEMOP and
    CHOKE families already use. Carry the same "phase-local working ids minted at plan time, registered
    at phase close" caveat those families carry, per the Phase 254 / 257 / 265 / 267.3 / 270 / 272 /
    274 precedent. Then update the Phase 267.2 `**Requirements**:` line in `.planning/ROADMAP.md` from
    `TBD` to `HOOK-01..HOOK-12` with the same caveat, and update its `**Plans:**` line to the real
    count and status.

    RECONCILE. Fill in the `Task ID`, `Plan`, `Wave` and `Requirement` columns of
    `267.2-VALIDATION.md`'s Per-Task Verification Map with the real plan numbers: BASELINE is 267.2-01
    wave 1; W0 and the GAP I-1 repair are 267.2-03 wave 2; W1a and W1c are 267.2-05 wave 2; W1d is
    267.2-06 wave 3; W1b is 267.2-07 wave 4; the C-5 registry row is 267.2-08 wave 5; W2 is 267.2-09
    wave 6; Folded is 267.2-04 wave 2; the two Cross rows are 267.2-06 and 267.2-10. Add a short note
    under the Wave 0 Requirements list recording decision D-M: the aggregator and the isolated-HOME
    fixture shipped in wave 1 as shared infrastructure, and every remaining `tests/test-267-2-*.cjs`
    shipped in the same plan as the behaviour it pins, with the filenames unchanged from what the list
    reserved. Set `nyquist_compliant: true` in the frontmatter only if every row's automated command
    now names a command that exists and passes; if any does not, leave it false and name which.

    MEASURE. Run the full gate set and record each command with its exit code in the summary:
    `bash tests/run-all-267.2.sh`, `bash tests/run-all-267.1.sh`, `bash tests/run-all-267.3.sh`,
    `node scripts/build-connector-registry.cjs --check`,
    `node scripts/build-skill-mirrors.cjs --check`,
    `node scripts/check-plugin-path-anchoring.cjs --check`,
    `node scripts/check-shape-declaration.cjs`, and `node scripts/doctor.cjs --acceptance`.

    Report the 267.1 result as an explicit DELTA against `267.2-BASELINE.md`: what was red before,
    what is green now, and which assertions the baseline shows were never executed at all before this
    phase. A bare "all green" claim is not acceptable output for this phase, because the suite was red
    at the start for a reason unrelated to it, and research C-2 is the reason the baseline exists.

    Do NOT run `scripts/release.sh` and do not cut any release. That is a standing do-not from the
    2026-08-27 handoff and requires an explicit go-ahead.

    No em-dashes anywhere.
  </action>
  <verify>
    <automated>grep -q "HOOK-01" .planning/REQUIREMENTS.md &amp;&amp; grep -q "HOOK-12" .planning/REQUIREMENTS.md &amp;&amp; grep -q "HOOK-" .planning/ROADMAP.md &amp;&amp; bash tests/run-all-267.2.sh &amp;&amp; bash tests/run-all-267.1.sh &amp;&amp; bash tests/run-all-267.3.sh &amp;&amp; node scripts/build-connector-registry.cjs --check &amp;&amp; node scripts/build-skill-mirrors.cjs --check &amp;&amp; node scripts/check-plugin-path-anchoring.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - Twelve HOOK rows exist in `.planning/REQUIREMENTS.md`.
    - The ROADMAP Phase 267.2 Requirements line names the family.
    - `267.2-VALIDATION.md` has no remaining TBD in its Plan, Wave or Requirement columns and carries the D-M note.
    - Every gate command listed above exits 0, and the summary reports the 267.1 result as a delta against the recorded baseline rather than as a bare green claim.
  </acceptance_criteria>
  <done>
    Twelve HOOK rows exist in `.planning/REQUIREMENTS.md`. The ROADMAP Phase 267.2 Requirements line
    names the family. `267.2-VALIDATION.md` has no remaining TBD in its Plan, Wave or Requirement
    columns and carries the D-M note. Every gate command listed above exits 0, and the summary reports
    the 267.1 result as a delta against the recorded baseline rather than as a bare green claim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Composite the dev-research trail into the rethinking-mindrianos room</name>
  <files>.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md</files>
  <read_first>
    - CLAUDE.md (the Dev-Research Compositing section: same finding, two homes, cross-linked, and the rule that a phase citing "per the room's research" without the citation counts as incomplete)
    - .planning/ROADMAP.md (the Phase 270 entry's Dev-Research Compositing paragraph, which records exactly how the write-scope-check guard blocked the same act and what the honest response was)
    - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-RESEARCH.md (the six Critical Corrections, the seven Pitfalls and the Assumptions Log, which are the substance of the trail)
    - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md (the fourteen planner decisions and the six registered-not-fixed findings)
  </read_first>
  <action>
    Write the durable reasoning trail to
    `~/MindrianRooms/rethinking-mindrianos/research/2026-09-03-first-install-hooked-loop-repair/`, and
    cross-link it in both directions per CLAUDE.md's Dev-Research Compositing mandate. The dev repo
    gets the executable decision; the room gets the evidence and the reasoning. Neither substitutes for
    the other.

    The trail must carry, with file and line citations rather than assertions: the six Critical
    Corrections from `267.2-RESEARCH.md` (C-1 the stale writer premise, C-2 the already-red suite, C-3
    the reproduced write-read disagreement, C-4 the doubly-illegal five-option card, C-5 the 267.3
    anchor tripwire, C-6 the room-free reward nobody had wired); the headline finding that in this repo
    the failure mode is almost never a missing mechanism but a mechanism with no caller, with all three
    instances named; the architecture call in decision D-D and the design constants that settled it
    (45s global and 35s per-agent dispatcher budget versus a 3000ms hook ceiling) plus the measured
    number plan 267.2-01 recorded; the stated Tri-Polar gap from decision D-K; and the six
    registered-not-fixed findings from D-N with their named owners.

    Add the room-to-repo half of the cross-link in the trail's own header, naming this phase directory.
    Add the repo-to-room half as a short `## Dev-Research Compositing` section at the end of
    `267.2-DECISIONS.md`, naming the room path.

    KNOWN GUARD, handle it the way Phase 270 did rather than around it: `scripts/write-scope-check` is
    a PreToolUse guard that blocks a write into a room that is not the registry's active room. If it
    fires, do NOT bypass it. Stage the content, record in the summary that landing it takes one
    navigator action (`/mos:rooms switch rethinking-mindrianos`) after which the staged file copies
    into place unchanged, and say plainly that the compositing mandate is not yet satisfied. A blocked
    write recorded honestly is a valid outcome; a bypassed guard is not.
  </action>
  <verify>
    <automated>grep -q "Dev-Research Compositing" .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md &amp;&amp; grep -q "rethinking-mindrianos" .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - The trail exists at the named room path with the six corrections, the mechanism-with-no-caller finding, the D-D architecture reasoning with its measured number, the D-K stated gap and the six D-N carried-forward items, each with citations.
    - Both halves of the cross-link exist.
    - If the write-scope guard blocked the room write, the summary records the exact one-command navigator action needed and states that the mandate is unsatisfied until then.
  </acceptance_criteria>
  <done>
    The trail exists at the named room path with the six corrections, the mechanism-with-no-caller
    finding, the D-D architecture reasoning with its measured number, the D-K stated gap and the six
    D-N carried-forward items, each with citations. Both halves of the cross-link exist. If the
    write-scope guard blocked the room write, the summary records the exact one-command navigator
    action needed and states that the mandate is unsatisfied until then.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Navigator sign-off on the actual first-install experience</name>
  <files>.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-10-SUMMARY.md</files>
  <read_first>
    - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md (the four judgment calls the navigator is being asked to rule on: D-C, D-D, D-H, D-K)
    - scripts/session-start (the rewritten FIRST_INSTALL payload, anchored on the literal "[MindrianOS Onboarding] First install detected.")
    - scripts/first-install-router.cjs (the three routing payloads, so the presented summary quotes what actually ships)
    - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-VALIDATION.md (the Manual-Only Verifications row, which records what automated tests structurally cannot cover)
  </read_first>
  <action>
    Pause and present the checkpoint below to the navigator. Do not proceed past this task and do not
    write a close-out verdict until a resume signal is received. Record the navigator's response
    verbatim in the plan summary, including any requested changes, following the Phase 257-09 Task 3
    precedent. If changes are requested, register them as follow-up work with named owners rather than
    folding them silently into this phase.
  </action>
  <what-built>
    The first-install Hooked loop was repaired end to end, on the CLI surface:

    - W0: the SEED-021 card mandate the navigator ordered reverted is gone from
      `scripts/session-start`, and the 267.1 audit test now executes every leg instead of aborting at
      its first assertion.
    - W1: `scripts/first-install-router.cjs` is a registered `UserPromptSubmit` hook that arms once on
      a virgin install, classifies the user's first free-text sentence locally through
      `lib/core/greeting-intent-detector.cjs` (four buckets, zero network, zero LLM), routes to one of
      three outcomes, fires the room-free Instant Brief pipeline out of band, and drains the rendered
      brief into the conversation on a later turn. The FIRST_INSTALL prose now asks ONE open question
      and renders no menu.
    - W2: the router seeds `~/.mindrian-user.md` through the shipped `writeUserMdAtomic` primitive and
      emits the investment ask, both strictly after the reward leg resolves. The write-read
      disagreement that made every new user read back as a student is fixed at the reader.
    - Folded todo: `/mos:ignite` Door 1 is a two-step pick that reaches all seven frozen
      `ROLE_BLEND_KEYS` within the four-option card cap, `mentor` included.
    - `data/first-reward-surfaces.json` declares `instant_brief` for the FIRST_INSTALL surface with an
      honest `why` naming the one-turn-later residual.
  </what-built>
  <how-to-verify>
    Three things to look at, in this order.

    1. READ THE DECISIONS RECORD FIRST.
       `.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md`.
       Four calls are worth a navigator eye because they were judgment, not derivation:
       - D-C: `ambiguous` routes to plain Larry, not to a clarifying question. Agree or not?
       - D-D: the reward lands ONE TURN AFTER the sentence that triggers it, because the pipeline's own
         budget is 45 seconds and a hook has 1.5. Acceptable, or does same-turn matter enough to change
         the design?
       - D-H: `mentor` maps to `blueprintFamily=exploration`. This is a Canon-adjacent vocabulary call.
       - D-K: Claude Desktop and Cowork get NO hook-side router in this phase. The gap is stated, not
         closed. Acceptable as a scoped call?

    2. LOOK AT WHAT A NEW USER ACTUALLY SEES. Read the rewritten FIRST_INSTALL payload in
       `scripts/session-start` (anchor on the literal `[MindrianOS Onboarding] First install detected.`)
       and the three routing payloads in `scripts/first-install-router.cjs`. The question is not whether
       the tests pass, it is whether the words sound like Larry and whether the first sixty seconds feel
       like a thinking partner rather than a form. This phase exists because of the navigator's own
       framing, "the harness that starts with Larry personality, and goes with it".

    3. THE HONEST LIMIT ON WHAT CAN BE TESTED HERE. The install cache at
       `~/.claude/plugins/mindrian-os/` is a separate copy that does NOT update from a `main` commit.
       Everything above was exercised against the dev workspace. The real first-install experience only
       exists after a release cut and pickup, which this phase deliberately did not do (standing
       do-not, requires explicit go-ahead). So the end-to-end greeting to reward to investment sequence
       against a genuinely fresh `~/.mindrian-onboarded`-absent session on a RELEASED build is still
       outstanding, and `267.2-VALIDATION.md`'s Manual-Only Verifications row records it as such.

    Say `approved` to close the phase with that manual verification carried as an open item, or name
    what should change.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe what should change</resume-signal>
  <verify>
    <automated>grep -qE "approved|CHANGES REQUESTED" .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-10-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - The navigator has responded, and the response is recorded verbatim in `267.2-10-SUMMARY.md` alongside a ruling on each of D-C, D-D, D-H and D-K.
    - If changes were requested, each is registered as follow-up work with a named owner.
    - The outstanding released-build manual verification is restated in the summary as an open item, not as done.
  </acceptance_criteria>
  <done>
    The navigator has responded, and the response is recorded verbatim in `267.2-10-SUMMARY.md`
    alongside a ruling on each of D-C, D-D, D-H and D-K. If changes were requested, each is registered
    as follow-up work with a named owner. The outstanding released-build manual verification is
    restated in the summary as an open item, not as done.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev repo -> rethinking-mindrianos room | A cross-repo write governed by the write-scope-check PreToolUse guard |
| phase close-out claim -> future sessions | An over-claimed green becomes a false premise for the next phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267.2-37 | Repudiation | the gate roll-up claim | mitigate | every gate result reported as a delta against `267.2-BASELINE.md`, naming which assertions were never executed before this phase; a bare all-green claim is explicitly disallowed |
| T-267.2-38 | Tampering | `scripts/write-scope-check` guard | mitigate | the guard is never bypassed; a blocked room write is staged and the exact navigator action recorded, following the Phase 270 precedent |
| T-267.2-39 | Elevation of privilege | release cut | mitigate | `scripts/release.sh` is explicitly out of scope for this plan; the standing do-not from the 2026-08-27 handoff requires explicit go-ahead |
| T-267.2-40 | Repudiation | manual-only verification | mitigate | the blocking human-verify task states plainly that the released-build check is outstanding, so the phase cannot close on an implied full verification |
| T-267.2-SC | Tampering | npm/pip/cargo installs | accept | zero external packages across the whole phase; `267.2-RESEARCH.md`'s Package Legitimacy Audit records the gate as not applicable and `package.json` dependencies are unchanged |
</threat_model>

<verification>
- `bash tests/run-all-267.2.sh`
- `bash tests/run-all-267.1.sh` reported as a delta against `267.2-BASELINE.md`
- `bash tests/run-all-267.3.sh`
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-skill-mirrors.cjs --check`
- `node scripts/check-plugin-path-anchoring.cjs --check`
- `node scripts/doctor.cjs --acceptance`
- Navigator sign-off recorded verbatim in the summary
</verification>

<success_criteria>
- `HOOK-01` through `HOOK-12` are registered and the ROADMAP Requirements line no longer reads TBD
- `267.2-VALIDATION.md` has real plan and wave numbers and carries the D-M reconciliation note
- The gate roll-up is reported as a measured delta, never as a bare green claim
- The research trail is landed or its blocked state is recorded with the exact unblocking action
- A navigator has signed off, or has named what should change
</success_criteria>

<output>
Create `.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-10-SUMMARY.md` when done
</output>
