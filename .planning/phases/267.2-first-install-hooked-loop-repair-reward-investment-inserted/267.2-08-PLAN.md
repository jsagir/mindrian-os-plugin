---
phase: 267.2-first-install-hooked-loop-repair-reward-investment-inserted
plan: 08
type: execute
wave: 5
depends_on: ["267.2-03", "267.2-07"]
files_modified:
  - scripts/session-start
  - data/first-reward-surfaces.json
  - tests/test-267-1-first-install-hooked-audit.cjs
autonomous: true
requirements: [HOOK-09]
canon_parts: [7, 12]
must_haves:
  truths:
    - "The FIRST_INSTALL payload hands first contact to the router instead of promising a reward it cannot deliver"
    - "The FIRST_INSTALL branch no longer appends the static cold-start command menu"
    - "data/first-reward-surfaces.json declares a truthful REWARD_TYPES value for FIRST_INSTALL"
    - "All four session-start anchor literals stay live so the 267.3 tripwire holds"
  artifacts:
    - path: "scripts/session-start"
      provides: "Rewritten FIRST_INSTALL payload plus a FIRST_INSTALL_HANDOFF block replacing the menu interpolation"
      contains: "FIRST_INSTALL_HANDOFF"
    - path: "data/first-reward-surfaces.json"
      provides: "FIRST_INSTALL record declaring instant_brief with a rewritten why field"
      contains: "instant_brief"
  key_links:
    - from: "data/first-reward-surfaces.json"
      to: "scripts/session-start"
      via: "anchor literal that must remain a live substring inside the FIRST_INSTALL branch"
      pattern: "First install detected"
    - from: "scripts/session-start"
      to: "scripts/first-install-router.cjs"
      via: "prose handoff naming the router as the surface that owns first contact"
      pattern: "first-install-router"
---

<objective>
W1 prose leg and the C-5 cross-cutting requirement: rewrite the FIRST_INSTALL payload so it hands
first contact to the router, and update the Phase 267.3 declaration registry in the same change so it
stays truthful.

Purpose: research finding C-5 established that Phase 267.3 registered all four `scripts/session-start`
prose branches in `data/first-reward-surfaces.json` with grep-able anchor literals and a tripwire test,
and that the in-place comment at `scripts/session-start` warns "Change the prose below and you must
revisit the record." Delivering a real reward means the `session-start:FIRST_INSTALL` record must move
from `--none (diagnostic surface)` to a real `REWARD_TYPES` member. `instant_brief` is already legal
(`lib/core/mva-rule-linter.cjs`) and is the honest value now that plan 267.2-07 wired it.

Decision D-F constrains the edit: `${COLD_START_MENU}` is interpolated in THREE branches, so the
variable itself stays byte-identical and only the FIRST_INSTALL branch's use of it changes.

Output: the rewritten payload, the updated registry record, and the flipped GAP R-1 prose-citation pin.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md
@.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-PATTERNS.md
@docs/reward-before-investment-rule.md
</context>

## Artifacts this phase produces

Created by THIS plan: the `FIRST_INSTALL_HANDOFF` shell variable in `scripts/session-start`, and the
`instant_brief` value on the `session-start:FIRST_INSTALL` record.

Created by SIBLING plans, listed so a source-grounding pass does not flag them as drift:
`lib/core/greeting-intent-detector.cjs` (exports `classify`, `route`, `BUCKETS`, `OUTCOMES`,
`ROUTING_TABLE`), `scripts/first-install-router.cjs`, `tests/run-all-267.2.sh`,
`tests/test-267-2-helpers.cjs` (exports `withIsolatedHome`, `keylessEnv`, `readRegion`,
`assertNoRawText`), `tests/test-267-2-w0-revert.cjs`, `tests/test-267-2-greeting-classifier.cjs`,
`tests/test-267-2-routing-table.cjs`, `tests/test-267-2-pre-room-reward.cjs`,
`tests/test-267-2-router-telemetry.cjs`, `tests/test-267-2-user-md-roundtrip.cjs`,
`tests/test-267-2-ignite-persona-coverage.cjs`, the `~/.mindrian/first-install/` state directory, and
requirement ids `HOOK-01` through `HOOK-12`.

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the FIRST_INSTALL payload to hand first contact to the router</name>
  <files>scripts/session-start</files>
  <read_first>
    - scripts/session-start (lines 648-710: the LIVE_COMMAND_COUNT computation, the COLD_START_MENU assignment and its FIRST-REWARD DECLARATION comment, the FIRST_INSTALL branch and its own declaration comment, and the UPDATE branch that shares the menu)
    - scripts/first-install-router.cjs (the three routing payloads it emits, so the prose does not duplicate or contradict them)
    - docs/reward-before-investment-rule.md (the violation list, in particular "picking from any menu longer than 3 options", and what counts as user input beyond one sentence)
    - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md (D-B for the one-open-question shape, D-F for the COLD_START_MENU constraint, D-N item 6 for why W0 shipped separately)
  </read_first>
  <action>
    Rewrite the `context=` assignment inside the `if [ "$ONBOARD_TYPE" = "FIRST_INSTALL" ]` branch.
    Keep the anchor literal `[MindrianOS Onboarding] First install detected.` byte-identical and in
    the same position: `data/first-reward-surfaces.json` points at it and
    `tests/test-267.3-session-start-declaration.cjs` asserts it lands INSIDE this branch.

    The rewritten payload must:

    - Keep the warm Larry opener instruction, the signature-opener examples, the no-emoji and
      no-filler rules, and add the Canon Part 12 requirement that the turn wears exactly one De Stijl
      glyph.
    - Replace the three-approach menu with ONE open question. Per decision D-B this is the legal shape
      on two independent grounds: `docs/reward-before-investment-rule.md` names a menu longer than 3
      options as a violation, and `AskUserQuestion` renders at most 4 options. Instruct the model to
      ask one open question about what the user is working on or what brought them here, in Larry's
      own voice, and to accept a free-text answer.
    - State explicitly that the model must NOT render a command menu, an options card, or a numbered
      list on this turn, and give the reason in one clause so a later reader does not undo it:
      the router classifies the free-text answer and injects the routing instruction itself.
    - Name `scripts/first-install-router.cjs` as the surface that owns what happens next, so a human
      reading the payload can find the machinery.
    - Remove the literal `Based on your work in [domain]` and the surrounding
      domain-intelligence promise. That sentence is the audit's own cited evidence for GAP R-1 and it
      is a promise with no machinery behind it; the Instant Brief is what replaces it, and the router
      delivers that without the model being asked to produce anything.
    - Remove the `~/.mindrian-user.md` capture instruction and its eight-field list
      (name, role, domain, subdomain, technical level, current focus, goal, expertise areas). Plan
      267.2-09 owns the investment leg and emits its ask from the router AFTER the reward, which is
      the ordering `docs/reward-before-investment-rule.md` requires. Leaving the prose ask here would
      put investment before reward again, which is the exact defect this phase exists to fix.
    - Keep the natural-language capability list (the "You can tell Larry about a meeting..." block),
      which is description rather than a menu to pick from.
    - Keep the `check-onboard --write` instruction unchanged. Its model-compliance fragility is a
      known ROADMAP item and decision D-N item 5 records that this phase deliberately does not add a
      second dependency on it; the router owns its own one-shot state instead.

    Then replace the trailing `${COLD_START_MENU}` interpolation IN THIS BRANCH ONLY with a new
    `FIRST_INSTALL_HANDOFF` variable, assigned just above the branch alongside `COLD_START_MENU`. Its
    content is a short block that names the one open question as the next move and says the router
    will take it from there. Do NOT modify the `COLD_START_MENU=` assignment itself, and do NOT touch
    the UPDATE branch (line 705) or the MODE_ROUTING branch (line 748), both of which still interpolate
    it. Editing the variable would change all three branches and would break the
    `session-start:COLD_START_MENU` anchor literal `COLD_START_MENU="---\nGet started:`.

    Update the FIRST-REWARD DECLARATION comment block above the FIRST_INSTALL branch so it describes
    the new reality: the branch now delivers `instant_brief` through
    `scripts/first-install-router.cjs`, and the registry record was updated in the same change. Keep
    the "change the prose and you must revisit the record" warning, which is what made this change
    discoverable.

    Run `bash -n scripts/session-start`. No em-dashes anywhere.
  </action>
  <verify>
    <automated>bash -n scripts/session-start &amp;&amp; node -e "const fs=require('node:fs');const s=fs.readFileSync('scripts/session-start','utf8');const i=s.indexOf('[MindrianOS Onboarding] First install detected.');if(i<0)throw new Error('FIRST_INSTALL anchor missing');const fi=s.slice(i,s.indexOf('\n',i));if(fi.indexOf('Based on your work in [domain]')!==-1)throw new Error('GAP R-1 prose promise still present');if(fi.indexOf('~/.mindrian-user.md')!==-1)throw new Error('investment prose ask still present in FIRST_INSTALL');if(fi.indexOf('AskUserQuestion')!==-1||fi.indexOf('SEED-021')!==-1)throw new Error('W0 revert regressed');if(fi.indexOf('COLD_START_MENU')!==-1)throw new Error('FIRST_INSTALL still interpolates the cold-start menu');if(fi.indexOf('FIRST_INSTALL_HANDOFF')===-1)throw new Error('handoff block not interpolated');if(s.indexOf('COLD_START_MENU=\"---\\\\nGet started:')===-1)throw new Error('COLD_START_MENU anchor changed');if(s.indexOf('first-install-router')===-1)throw new Error('router not named in the payload');console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - The FIRST_INSTALL region contains the anchor literal unchanged, contains `FIRST_INSTALL_HANDOFF` and not `COLD_START_MENU`, contains neither `Based on your work in [domain]` nor `~/.mindrian-user.md` nor `AskUserQuestion` nor `SEED-021`, and names `scripts/first-install-router.cjs`.
    - The `COLD_START_MENU=` assignment is byte-identical and both other branches still interpolate it.
    - `bash -n scripts/session-start` exits 0.
  </acceptance_criteria>
  <done>
    The FIRST_INSTALL region contains the anchor literal unchanged, contains `FIRST_INSTALL_HANDOFF`
    and not `COLD_START_MENU`, contains neither `Based on your work in [domain]` nor
    `~/.mindrian-user.md` nor `AskUserQuestion` nor `SEED-021`, and names
    `scripts/first-install-router.cjs`. The `COLD_START_MENU=` assignment is byte-identical and both
    other branches still interpolate it. `bash -n scripts/session-start` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update the first-reward declaration registry in the same change</name>
  <files>data/first-reward-surfaces.json</files>
  <read_first>
    - data/first-reward-surfaces.json (all four records: their id, file, kind, anchor, interactive_first_reward and why fields)
    - lib/core/mva-rule-linter.cjs (the REWARD_TYPES closed vocabulary and its two Phase 267.3 amendments, so the chosen value is a real member and not a respelling)
    - tests/test-267.3-session-start-declaration.cjs (its assertions: every anchor is a live substring, and a session-start record's anchor lands inside its own branch)
    - scripts/check-reward-before-investment.cjs (the --surfaces mode wired as gate 10d of scripts/verify-release, fail-closed)
  </read_first>
  <action>
    Edit the `session-start:FIRST_INSTALL` record only. Change `interactive_first_reward` from
    `--none (diagnostic surface)` to `instant_brief`, which is already a legal `REWARD_TYPES` member
    (`lib/core/mva-rule-linter.cjs` documents it as the 30-second MVA pipeline output). Do not invent,
    respell or amend the vocabulary: a new member is a Canon amendment, not a phase-level invention.

    Rewrite that record's `why` field to state what is now true, with citations a later reader can
    follow: the branch hands first contact to `scripts/first-install-router.cjs`, which classifies the
    user's free-text answer locally and fires the room-free Instant Brief pipeline
    (`lib/core/mva-orchestrator.cjs`) out of band, draining the rendered result as `additionalContext`
    on a later turn. Name the honest residual in the same field: the brief lands one turn after the
    triggering sentence, and it fires only when the shipped MVA classifier judges the sentence
    venture-shaped, so a session that never qualifies delivers no brief. State that plainly rather
    than declaring a reward the surface may not deliver.

    Leave the `session-start:UPDATE`, `session-start:MODE_ROUTING` and `session-start:COLD_START_MENU`
    records untouched, including all four `anchor` literals. The COLD_START_MENU record in particular
    is still accurate because decision D-F kept its variable byte-identical.

    Then run `bash tests/run-all-267.3.sh` and `node scripts/check-reward-before-investment.cjs --surfaces .`
    and confirm both pass before finishing.
  </action>
  <verify>
    <automated>node -e "const r=require('./data/first-reward-surfaces.json');const recs=Array.isArray(r)?r:(r.surfaces||r.records);const fi=recs.find(x=>x.id==='session-start:FIRST_INSTALL');if(!fi)throw new Error('FIRST_INSTALL record missing');if(fi.interactive_first_reward!=='instant_brief')throw new Error('expected instant_brief, got: '+fi.interactive_first_reward);const src=require('node:fs').readFileSync('scripts/session-start','utf8');for(const rec of recs){if(src.indexOf(rec.anchor)===-1)throw new Error('dead anchor for '+rec.id);}console.log('ok');" &amp;&amp; bash tests/run-all-267.3.sh</automated>
  </verify>
  <acceptance_criteria>
    - The `session-start:FIRST_INSTALL` record declares `instant_brief` with a rewritten `why` that names the router, the pipeline and the one-turn-later residual.
    - All four anchors are live substrings of `scripts/session-start`.
    - `bash tests/run-all-267.3.sh` exits 0.
  </acceptance_criteria>
  <done>
    The `session-start:FIRST_INSTALL` record declares `instant_brief` with a rewritten `why` that names
    the router, the pipeline and the one-turn-later residual. All four anchors are live substrings of
    `scripts/session-start`. `bash tests/run-all-267.3.sh` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip the 267.1 prose-citation pin to its post-repair shape</name>
  <files>tests/test-267-1-first-install-hooked-audit.cjs</files>
  <read_first>
    - tests/test-267-1-first-install-hooked-audit.cjs (the whole file as repaired by plan 267.2-03: in particular the Action-leg absence pin and the "still asserts the reward and investment legs as prose only" leg)
    - .planning/ROADMAP.md (the Phase 267.2 sentence "Each workstream flips a negative assertion in tests/test-267-1-first-install-hooked-audit.cjs to positive when its gap closes; flipping that pin is part of this phase's work, not a test failure to route around")
    - scripts/session-start (the rewritten FIRST_INSTALL region, so the flipped assertions match what actually shipped)
  </read_first>
  <action>
    Flip the leg currently described as "Action leg: FIRST_INSTALL still asserts the reward and
    investment legs as prose only (GAP R-1 / GAP I-1 unremediated by design)". It asserted two prose
    literals were PRESENT as the audit's cited evidence. Both are now gone by design, so the leg
    becomes a POSITIVE pin on the repair:

    - Assert `Based on your work in [domain]` is ABSENT from the FIRST_INSTALL region, with a failure
      message saying the GAP R-1 prose promise returned and that the reward is now delivered by
      `scripts/first-install-router.cjs`, not promised in prose.
    - Assert `~/.mindrian-user.md` is ABSENT from the FIRST_INSTALL region, with a failure message
      saying the investment ask must be emitted by the router AFTER the reward, per
      `docs/reward-before-investment-rule.md`, not asked for in the session-start payload.
    - Add a positive assertion that the region names `first-install-router`, so the pin proves the
      handoff exists rather than only proving two strings vanished.
    - Rename the leg's description string to say the FIRST_INSTALL payload hands first contact to the
      router and carries no unbacked reward or investment promise.

    Also update the Action-leg absence pin's three option-label assertions. The rewritten payload no
    longer carries `1. Conversational Q&A`, `2. Document paste` or `3. Skip`, because decision D-B
    replaced the menu with one open question. Replace those three assertions with a single assertion
    that the region contains no numbered-list menu marker sequence at all, and put the reason in the
    failure message: a menu on this turn is investment before reward.

    Leave the GAP R-1 cold-start-menu leg, the GAP G-1 leg, the reachability leg and the `bash -n` leg
    untouched. Leave the GAP I-1 grep leg untouched; plan 267.2-09 owns flipping that one.

    No em-dashes anywhere.
  </action>
  <verify>
    <automated>node tests/test-267-1-first-install-hooked-audit.cjs &amp;&amp; node tests/test-267-2-w0-revert.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-267-1-first-install-hooked-audit.cjs` exits 0 with the prose-citation leg flipped to absence assertions plus a positive `first-install-router` assertion, and the numbered-menu assertion in place of the three option labels.
    - `node tests/test-267-2-w0-revert.cjs` still exits 0.
  </acceptance_criteria>
  <done>
    `node tests/test-267-1-first-install-hooked-audit.cjs` exits 0 with the prose-citation leg flipped
    to absence assertions plus a positive `first-install-router` assertion, and the numbered-menu
    assertion in place of the three option labels. `node tests/test-267-2-w0-revert.cjs` still exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `scripts/session-start` payload -> model context | The payload is injected as `additionalContext` and shapes every first-install turn |
| declaration registry -> release gate | `data/first-reward-surfaces.json` feeds gate 10d of `scripts/verify-release`, fail-closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267.2-27 | Repudiation | `data/first-reward-surfaces.json` FIRST_INSTALL record | mitigate | the `why` field states the one-turn-later residual and the venture-shaped precondition, so the declaration cannot read as a stronger claim than the machinery supports |
| T-267.2-28 | Tampering | the four anchor literals | mitigate | the task-2 verify command asserts every anchor is still a live substring, and `bash tests/run-all-267.3.sh` is a required gate |
| T-267.2-29 | Tampering | `COLD_START_MENU` shared by three branches | mitigate | decision D-F keeps the variable byte-identical; the task-1 verify command asserts the assignment is unchanged and that FIRST_INSTALL no longer interpolates it |
| T-267.2-30 | Elevation of privilege | investment ask reappearing before the reward | mitigate | the prose capture instruction is removed here and the ask is emitted only by the router's post-reward phase (D-L), pinned by the flipped 267.1 leg and by plan 267.2-09's ordering assertion |
| T-267.2-SC | Tampering | npm/pip/cargo installs | accept | zero external packages in this plan |
</threat_model>

<verification>
- `bash -n scripts/session-start`
- `bash tests/run-all-267.3.sh`
- `node scripts/check-reward-before-investment.cjs --surfaces .`
- `node tests/test-267-1-first-install-hooked-audit.cjs`
- `node tests/test-209-session-start-exemplar.cjs`
- `bash tests/run-all-267.2.sh`
</verification>

<success_criteria>
- The FIRST_INSTALL payload asks one open question, renders no menu, and names the router
- Neither the GAP R-1 reward promise nor the GAP I-1 investment ask remains in the payload
- `data/first-reward-surfaces.json` declares `instant_brief` with an honest `why`
- All four anchors stay live and the 267.3 tripwire suite stays green
</success_criteria>

<output>
Create `.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-08-SUMMARY.md` when done
</output>
