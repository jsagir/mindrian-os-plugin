---
phase: 267.2-first-install-hooked-loop-repair-reward-investment-inserted
plan: 04
type: execute
wave: 2
depends_on: ["267.2-02"]
files_modified:
  - commands/ignite.md
  - skills/ignite/SKILL.md
  - tests/test-267-2-ignite-persona-coverage.cjs
  - .planning/todos/pending/2026-06-28-ignite-persona-card-under-shows-frozen-role-blend-vocabulary.md
  - .planning/todos/completed/2026-06-28-ignite-persona-card-under-shows-frozen-role-blend-vocabulary.md
autonomous: true
requirements: [HOOK-11]
canon_parts: [7, 11]
must_haves:
  truths:
    - "A navigator can select any one of the 7 frozen ROLE_BLEND_KEYS at /mos:ignite Door 1, mentor included"
    - "No AskUserQuestion step in Door 1 carries more than 4 options"
    - "Every selectable persona resolves a defined blueprintFamily"
    - "The card path and the Tri-Polar card-incapable fallback offer the same persona set"
  artifacts:
    - path: "commands/ignite.md"
      provides: "Door 1 two-step persona pick reaching all 7 ROLE_BLEND_KEYS within the 4-option cap"
      contains: "mentor"
    - path: "tests/test-267-2-ignite-persona-coverage.cjs"
      provides: "Drift pin comparing the parsed Door 1 option set against the frozen ROLE_BLEND_KEYS array"
      contains: "ROLE_BLEND_KEYS"
  key_links:
    - from: "tests/test-267-2-ignite-persona-coverage.cjs"
      to: "lib/core/persona-override.cjs"
      via: "require of the frozen vocabulary, never an inline restatement"
      pattern: "require.*persona-override"
    - from: "commands/ignite.md"
      to: "skills/ignite/SKILL.md"
      via: "node scripts/build-skill-mirrors.cjs regeneration"
      pattern: "mentor"
---

<objective>
Close the folded todo `2026-06-28-ignite-persona-card-under-shows-frozen-role-blend-vocabulary`:
`/mos:ignite`'s Gate B1 Door 1 persona pick under-shows the frozen 7-key `ROLE_BLEND_KEYS`
vocabulary, and over-shows against the `AskUserQuestion` render cap at the same time.

Purpose: this is folded into 267.2 by explicit user choice because some of W1's router outcomes land
in `/mos:ignite`, and Door 1 is the first card a routed user hits. Two defects compound: `mentor` is
frozen-legal but absent from the 6-option card, so a navigator can never select it; and 6 options
already exceed `AskUserQuestion`'s 4-option render cap, so the vocabulary is silently truncated TODAY,
before `mentor` is even counted. Adding a 7th option would make the truncation worse, which is why
decision D-I restructures the pick into two capped steps.

Output: a two-step Door 1 in `commands/ignite.md`, a matching Tri-Polar fallback, a regenerated skill
mirror, and a drift pin that compares the card's option set against `ROLE_BLEND_KEYS` at run time.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md
@.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-PATTERNS.md
@.planning/todos/pending/2026-06-28-ignite-persona-card-under-shows-frozen-role-blend-vocabulary.md
</context>

## Artifacts this phase produces

Created by THIS plan: `tests/test-267-2-ignite-persona-coverage.cjs` (new file); the two-step Door 1
structure in `commands/ignite.md` with the step-1 option labels `Building something`,
`Studying something`, `Backing or guiding someone`, `Something else`; and the `mentor` persona option
with `blueprintFamily=exploration`.

Created by SIBLING plans, listed so a source-grounding pass does not flag them as drift:
`lib/core/greeting-intent-detector.cjs` (exports `classify`, `route`, `BUCKETS`, `OUTCOMES`,
`ROUTING_TABLE`), `scripts/first-install-router.cjs`, `tests/run-all-267.2.sh`,
`tests/test-267-2-helpers.cjs` (exports `withIsolatedHome`, `keylessEnv`, `readRegion`,
`assertNoRawText`), `tests/test-267-2-w0-revert.cjs`, `tests/test-267-2-greeting-classifier.cjs`,
`tests/test-267-2-routing-table.cjs`, `tests/test-267-2-pre-room-reward.cjs`,
`tests/test-267-2-router-telemetry.cjs`, `tests/test-267-2-user-md-roundtrip.cjs`, the
`FIRST_INSTALL_HANDOFF` shell variable in `scripts/session-start`, the `~/.mindrian/first-install/`
state directory, and requirement ids `HOOK-01` through `HOOK-12`.

<tasks>

<task type="auto">
  <name>Task 1: Restructure Door 1 into a two-step capped pick that reaches all 7 frozen keys</name>
  <files>commands/ignite.md</files>
  <read_first>
    - commands/ignite.md (Gate B1 in full: the FIRE THE CARD doctrine block, the blueprintFamily derivation sentence, Door 1's six options, Doors 2-4, and the Tri-Polar card-incapable fallback line)
    - lib/core/persona-override.cjs (the frozen ROLE_BLEND_KEYS array, the single source of truth the doctrine cites by name and must never restate inline)
    - .planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-DECISIONS.md (D-H for mentor's blueprintFamily, D-I for the two-step structure, D-J for why Portfolio Manager is not minted)
    - .planning/todos/pending/2026-06-28-ignite-persona-card-under-shows-frozen-role-blend-vocabulary.md (the todo's own three defects and candidate directions)
  </read_first>
  <action>
    Replace Door 1's single six-option card with a TWO-STEP pick, per decision D-I. Both steps stay
    within `AskUserQuestion`'s 4-option-per-question cap, and every one of the 7 frozen
    `ROLE_BLEND_KEYS` becomes reachable without relying on a free-text "Other".

    Step 1, header `Arrival`, question `Who are you arriving as?`, exactly 4 options:
    `Building something`, `Studying something`, `Backing or guiding someone`, `Something else`.

    Step 2, fired only after step 1, narrowed by the step-1 pick, at most 3 options each, and each
    option resolving the same `{role_blend, blueprintFamily, arrival_asset}` tuple Door 1 already
    threads into B2's `birthRoom` opts:
    - `Building something` -> `Founder / business` (role_blend={founder:1.0}, blueprintFamily=venture)
      and `Operator` (role_blend={operator:1.0}, blueprintFamily=venture)
    - `Studying something` -> `Researcher` (role_blend={researcher:1.0}, blueprintFamily=exploration),
      `Student` (role_blend={student:1.0}, blueprintFamily=exploration), `Domain expert`
      (role_blend={domain_expert:1.0}, blueprintFamily=exploration)
    - `Backing or guiding someone` -> `Investor` (role_blend={investor:1.0}, blueprintFamily=venture)
      and `Mentor` (role_blend={mentor:1.0}, blueprintFamily=exploration)
    - `Something else` -> no step 2; falls through to the existing Door 2 (CV), Door 3 (hypothesis)
      and Door 4 (free-text routing), all unchanged by this plan

    Extend the existing blueprintFamily derivation sentence so it covers all seven keys. Its live text
    reads "researcher / student / domain_expert -> exploration; founder / operator / investor ->
    venture". Add `mentor` to the exploration side. Add a one-clause justification in the doctrine
    itself, in Feynman-plain language: a mentor guides someone else's work rather than owning a
    venture, which is the same reason `domain_expert` sits in exploration. Cite decision D-H by name
    so a later Canon review can find the reasoning.

    Update the Tri-Polar card-incapable fallback line so its letter list carries all seven personas
    plus the existing CV and hypothesis entries. A plain text list has no option cap, so it stays one
    step. Both surfaces must offer the same persona set or they drift (PATTERNS.md A9: "fix both or
    they drift").

    Keep unchanged, explicitly: the FIRE THE CARD / SEED-021 no-card-no-picture mandate, which now
    applies to BOTH steps; the instruction that `role_blend` is a single-axis `{key:1.0}` blend drawn
    ONLY from `ROLE_BLEND_KEYS` cited by name and never redefined inline; Doors 2, 3 and 4; the
    per-role hypothesis framing map; and the Act 1 auto-fire block.

    Do NOT mint a `portfolio_manager` key or any other new role (decision D-J): extending
    `ROLE_BLEND_KEYS` is a five-file order-sensitive change across `lib/workflow/f-selector-ranker.cjs`,
    `lib/core/shallow-doc-parser.cjs`, `lib/core/session-register.cjs`, `lib/core/user-md-ops.cjs` and
    `lib/core/persona-override.cjs`, plus a Canon ratification. `investor` remains the closest alias.

    No em-dashes anywhere; this file uses the double-hyphen form already.
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const s=fs.readFileSync('commands/ignite.md','utf8');const {ROLE_BLEND_KEYS}=require('./lib/core/persona-override.cjs');for(const k of ROLE_BLEND_KEYS){if(s.indexOf('role_blend={'+k+':1.0}')===-1)throw new Error('Door 1 missing role_blend for frozen key: '+k);}for(const l of ['Building something','Studying something','Backing or guiding someone','Something else']){if(s.indexOf(l)===-1)throw new Error('step-1 option missing: '+l);}if(s.indexOf('blueprintFamily=exploration')===-1||s.indexOf('blueprintFamily=venture')===-1)throw new Error('blueprintFamily labels missing');if(s.indexOf(String.fromCharCode(8212))!==-1)throw new Error('em-dash found');console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `commands/ignite.md` Door 1 is a two-step pick.
    - Step 1 has exactly 4 named options.
    - Every one of the 7 `ROLE_BLEND_KEYS` appears as `role_blend={<key>:1.0}` with an adjacent `blueprintFamily=` value.
    - `mentor` maps to `exploration` with its reasoning cited to D-H.
    - The Tri-Polar fallback line lists all seven personas plus CV and hypothesis.
    - No `portfolio_manager` string appears anywhere.
    - No em-dash.
  </acceptance_criteria>
  <done>
    `commands/ignite.md` Door 1 is a two-step pick. Step 1 has exactly 4 named options. Every one of
    the 7 `ROLE_BLEND_KEYS` appears as `role_blend={<key>:1.0}` with an adjacent `blueprintFamily=`
    value. `mentor` maps to `exploration` with its reasoning cited to D-H. The Tri-Polar fallback line
    lists all seven personas plus CV and hypothesis. No `portfolio_manager` string appears anywhere.
    No em-dash.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the ignite skill mirror and prove it in check mode</name>
  <files>skills/ignite/SKILL.md</files>
  <read_first>
    - scripts/build-skill-mirrors.cjs (its SKIP_LIST, its name-override behaviour, and its --check mode semantics)
    - skills/ignite/SKILL.md (the current generated mirror, so the diff is understood before regenerating)
    - commands/ignite.md (the source of truth just edited in task 1)
  </read_first>
  <action>
    `skills/ignite/SKILL.md` is a GENERATED mirror of `commands/ignite.md` and `ignite` is not on
    `scripts/build-skill-mirrors.cjs`'s `SKIP_LIST`, so it must be regenerated rather than hand-edited.
    Run `node scripts/build-skill-mirrors.cjs`, then `node scripts/build-skill-mirrors.cjs --check` and
    confirm it reports no stale mirror.

    Then re-read `skills/ignite/SKILL.md` and confirm the regenerated mirror actually carries the
    seven personas, the two-step structure and the extended Tri-Polar line. If the generator strips or
    reshapes the Door 1 block, do NOT hand-edit the mirror to compensate; report the generator
    behaviour in the summary and fix the source in `commands/ignite.md` so the generated output is
    correct.

    Separately, confirm the reward-before-investment ordering constraint documented in
    `skills/ignite/SKILL.md` is unchanged by the regeneration, since W1's reward work depends on it.
  </action>
  <verify>
    <automated>node scripts/build-skill-mirrors.cjs --check &amp;&amp; node -e "const fs=require('node:fs');const s=fs.readFileSync('skills/ignite/SKILL.md','utf8');const {ROLE_BLEND_KEYS}=require('./lib/core/persona-override.cjs');for(const k of ROLE_BLEND_KEYS){if(s.toLowerCase().indexOf(k)===-1)throw new Error('mirror missing frozen key: '+k);}console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-skill-mirrors.cjs --check` exits 0 with no stale mirror reported.
    - `skills/ignite/SKILL.md` mentions all 7 frozen key names.
    - The reward-before-investment ordering text in the skill is unchanged.
  </acceptance_criteria>
  <done>
    `node scripts/build-skill-mirrors.cjs --check` exits 0 with no stale mirror reported.
    `skills/ignite/SKILL.md` mentions all 7 frozen key names. The reward-before-investment ordering
    text in the skill is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ship the drift pin and close the folded todo</name>
  <files>tests/test-267-2-ignite-persona-coverage.cjs, .planning/todos/pending/2026-06-28-ignite-persona-card-under-shows-frozen-role-blend-vocabulary.md, .planning/todos/completed/2026-06-28-ignite-persona-card-under-shows-frozen-role-blend-vocabulary.md</files>
  <read_first>
    - lib/core/persona-override.cjs (ROLE_BLEND_KEYS, required by the test rather than restated)
    - commands/ignite.md (the Door 1 and Tri-Polar blocks the test parses)
    - tests/test-267-1-first-install-hooked-audit.cjs (the ok(desc, fn) reporting shape and the GAP G-1 leg's require-plus-source-grep species this test follows)
    - .planning/todos/pending/2026-06-28-ignite-persona-card-under-shows-frozen-role-blend-vocabulary.md (the three defects the closure note must answer one by one)
  </read_first>
  <action>
    Create `tests/test-267-2-ignite-persona-coverage.cjs`, plain CJS with `node:assert/strict`, the
    `ok(desc, fn)` reporting shape, and a final PASS line. It must `require('../lib/core/persona-override.cjs')`
    for `ROLE_BLEND_KEYS` and never restate the seven names inline, so the test cannot drift from the
    frozen vocabulary it guards.

    Assertions:
    1. For every key in `ROLE_BLEND_KEYS`, `commands/ignite.md` contains a `role_blend={<key>:1.0}`
       occurrence. Failure message names the missing key.
    2. Every `role_blend={<key>:1.0}` occurrence in `commands/ignite.md` has a `blueprintFamily=`
       value on the same line, and that value is one of the families the file's own derivation
       sentence declares. No option may resolve an undefined blueprint.
    3. `mentor` specifically resolves `blueprintFamily=exploration`, pinning decision D-H so a silent
       reversal is loud.
    4. The Tri-Polar card-incapable fallback line mentions every key in `ROLE_BLEND_KEYS`, so the card
       path and the card-incapable path offer the same set.
    5. Option-cap guard: parse the step-1 option list and assert its length is at most 4, and parse
       each step-2 branch list and assert each is at most 4. Derive the cap from a named constant in
       the test (`ASK_USER_QUESTION_MAX_OPTIONS = 4`) with a comment citing
       anthropics/claude-code#12420 and #26183, so the number is traceable rather than magic.
    6. Negative guard: `commands/ignite.md` contains no `portfolio_manager` string, pinning decision
       D-J so the deferred Canon question is not silently minted later.

    Then close the todo: move
    `.planning/todos/pending/2026-06-28-ignite-persona-card-under-shows-frozen-role-blend-vocabulary.md`
    to `.planning/todos/completed/` (that directory already exists), and append a closure note to the
    moved file answering the todo's three defects one at a time: (1) `mentor` added, reachable, with
    `blueprintFamily=exploration` per D-H; (2) the 4-option cap honored by the two-step restructure per
    D-I, with the drift pin as the guard; (3) Portfolio Manager NOT minted, carried forward as an open
    Canon question per D-J, with the five-file order-sensitive blast radius named. Cite this plan as
    the closer.

    No em-dashes anywhere.
  </action>
  <verify>
    <automated>node tests/test-267-2-ignite-persona-coverage.cjs &amp;&amp; test ! -f .planning/todos/pending/2026-06-28-ignite-persona-card-under-shows-frozen-role-blend-vocabulary.md &amp;&amp; test -f .planning/todos/completed/2026-06-28-ignite-persona-card-under-shows-frozen-role-blend-vocabulary.md</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-267-2-ignite-persona-coverage.cjs` exits 0 with at least 6 assertions.
    - The test requires `ROLE_BLEND_KEYS` rather than restating it.
    - The todo file has moved from `pending/` to `completed/` and carries a closure note answering all three of its defects with the deciding D-id for each.
  </acceptance_criteria>
  <done>
    `node tests/test-267-2-ignite-persona-coverage.cjs` exits 0 with at least 6 assertions. The test
    requires `ROLE_BLEND_KEYS` rather than restating it. The todo file has moved from `pending/` to
    `completed/` and carries a closure note answering all three of its defects with the deciding D-id
    for each.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command doctrine -> model behaviour | `commands/ignite.md` is prose the model executes; a truncated card silently narrows a user's choices |
| frozen vocabulary -> five downstream consumers | `ROLE_BLEND_KEYS` order and membership are read by index-parallel lookups |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267.2-09 | Tampering | `commands/ignite.md` Door 1 | mitigate | drift pin requires `ROLE_BLEND_KEYS` at run time and asserts every key is reachable, so a future option-list edit that drops a key fails loudly |
| T-267.2-10 | Elevation of privilege | `ROLE_BLEND_KEYS` vocabulary | mitigate | negative assertion on `portfolio_manager` prevents an inline mint of a key whose five-file order-sensitive blast radius has not been ratified (D-J) |
| T-267.2-11 | Information disclosure | none | accept | this plan reads and writes only plugin-source markdown and a test; no user data, no network, no Brain call |
| T-267.2-SC | Tampering | npm/pip/cargo installs | accept | zero external packages in this plan |
</threat_model>

<verification>
- `node tests/test-267-2-ignite-persona-coverage.cjs`
- `node scripts/build-skill-mirrors.cjs --check`
- `node scripts/build-connector-registry.cjs --check` (Door 1 changes touch a born-wired command
  surface; this proves the registry is not staled by the edit)
- `bash tests/run-all-267.2.sh`
</verification>

<success_criteria>
- All 7 frozen `ROLE_BLEND_KEYS` are selectable at Door 1, `mentor` included
- No `AskUserQuestion` step in Door 1 exceeds 4 options
- Card path and Tri-Polar fallback offer the same persona set
- `mentor` has a defined `blueprintFamily`
- The folded todo is closed with a closure note answering its three defects
</success_criteria>

<output>
Create `.planning/phases/267.2-first-install-hooked-loop-repair-reward-investment-inserted/267.2-04-SUMMARY.md` when done
</output>
