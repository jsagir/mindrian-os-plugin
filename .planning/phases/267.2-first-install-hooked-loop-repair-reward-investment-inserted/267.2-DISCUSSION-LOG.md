# Phase 267.2: First-Install Hooked Loop Repair (Reward + Investment) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-03
**Phase:** 267.2-first-install-hooked-loop-repair-reward-investment-inserted
**Areas discussed:** Greeting router logic, Investment trigger, W0 scope, Part 8 boundary for the router, Folded todo (ignite persona card), Theo forward-compatibility

---

## Router logic (W1)

The user opened the discussion by stating, unprompted, that 267.2 "need to also include harness that start with larry personality, and goes with it" — before any gray-area question was presented. This matched the navigator's own prior ruling from the Phase 267.1 checkpoint (2026-08-27) almost verbatim, which was already sitting in the phase's ROADMAP.md scope as "not yet decided or built." Framed the domain boundary around this and asked for the mechanics.

| Option | Description | Selected |
|--------|-------------|----------|
| Explicit answer only | Larry asks the 5-way question, routes strictly off the literal answer | |
| Brain-classified intent | Larry asks lightly, classifies intent from however the user responds via a Brain call | ✓ |
| Empirical-gated | Instrument the current static menu first, gather usage data, design router in a follow-up phase | |

**User's choice:** Brain-classified intent (recommended option).
**Notes:** This directly reinforces the navigator's own 267.1-06 checkpoint framing ("some cases should trigger ignite, some should just be Larry, some should be a conversation that figures out what to trigger based on context").

---

## Investment trigger (W2)

| Option | Description | Selected |
|--------|-------------|----------|
| Write during onboarding conversation itself | Larry writes ~/.mindrian-user.md directly during FIRST_INSTALL, before any room exists | ✓ |
| Defer until a room is created | No write at FIRST_INSTALL; investment happens naturally at room birth | |
| Lightweight stub now, room-birth backfills | Minimal home-dir write now, existing writer merges/upgrades later | |

**User's choice:** Write during the onboarding conversation itself.
**Notes:** Directly targets GAP I-1 (the audit's finding that the FIRST session specifically has zero investment ask). Requires a new home-directory writer since `writeUserMdAtomic` only reaches room paths today.

---

## W0 scope

| Option | Description | Selected |
|--------|-------------|----------|
| Exactly as scoped | Just the one-line revert + test-pin inversion, nothing more | ✓ |
| Something else needs to change too | Named separately if so | |

**User's choice:** Exactly as scoped.
**Notes:** Confirms the 267.1 checkpoint's own framing — a navigator-ordered scope correction, not a place to bundle other work.

---

## Part 8 boundary for the router's classifier

Follow-up question raised proactively (not user-initiated) because "Brain-classified intent" as selected above runs directly into Canon Part 8 (user content never egresses to Brain).

| Option | Description | Selected |
|--------|-------------|----------|
| Local classifier, Brain-shaped signal only | Classify locally (mirrors Phase 115's dual-path-detector pattern), Brain sees only the generic bucket, never raw text | ✓ |
| The model itself classifies inline | No separate classifier, no Brain call at all — "Brain-classified" becomes a misnomer | |

**User's choice:** Local classifier, Brain-shaped signal only.
**Notes:** Resolves the naming tension in "Brain-classified" from the prior question — the classification is local; Brain (or Theo, post-cutover) only ever sees a generic handle, matching Phase 115's existing precedent and Part 8's boundary exactly.

---

## Folded todo: ignite persona card vocabulary

Surfaced via `todo.match-phase` (0.9 score match) during `cross_reference_todos`.

| Option | Description | Selected |
|--------|-------------|----------|
| Keep separate | Different surface (ignite's Door 1 card, not the FIRST_INSTALL router), note as related-but-deferred | |
| Fold it in | 267.2 also fixes the missing `mentor` option and the 4-option cap truncation on the persona pick card | ✓ |

**User's choice:** Fold it in.
**Notes:** `mentor` is one of 7 frozen `ROLE_BLEND_KEYS` but missing from the card; the 4-option `AskUserQuestion` render cap silently truncates the vocabulary regardless. Relevant because some of W1's router outcomes land in `/mos:ignite`, and Door 1 is the first card a routed user hits.

---

## Theo forward-compatibility

The phase's own ROADMAP.md entry carries a standing 2026-09-02 navigator rule requiring this be confirmed before research locks. Surfaced proactively (user then explicitly asked to make sure the phase was Theo-ready before the question was fully posed). Reasoned through the actual state first: Phase 269 (already shipped) removed per-query key friction, so the "key-gated first session" concern the standing note names is largely already closed; what remains is whether the router's classifier bypasses the shared chokepoint.

| Option | Description | Selected |
|--------|-------------|----------|
| Confirm — chokepoint-generic | Router never calls Brain/Theo directly, only through the existing mindrian-brain shim; cutover requires zero code changes here | ✓ |
| Something more specific needed | A named Theo-specific query shape or pre-research check | |

**User's choice:** Confirm — chokepoint-generic.
**Notes:** Matches the exact abstraction Phase 257 (closed earlier this session) hardened. No Theo-specific query shape gets designed now; no current-Brain-specific shape gets hardcoded either.

---

## Claude's Discretion

- Exact routing table for which of the 5 greeting-question answers maps to ignite / plain Larry / clarify-first — left to research and planning.
- Mechanism for satisfying the navigator's "get empirical data before building" instruction without a second phase (candidate: instrument the new router itself).
- Code-organization call for where the new home-directory writer lives (new function in `user-md-ops.cjs` vs. a wrapper elsewhere).
- Merge behavior when a later room-birth USER.md write meets an earlier home-directory write from the same user/session.

## Deferred Ideas

None beyond the folded todo above — discussion stayed within phase scope. `/mos:ignite`'s internal B1/B2/B3 chain mechanics, Phase 269's entitlement gate internals, and Phase 267's blocked MCP-tool transition were all named as explicitly out of this phase's boundary, not touched.
