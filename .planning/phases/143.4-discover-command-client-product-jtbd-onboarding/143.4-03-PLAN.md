---
phase: 143.4
plan: 03
type: execute
wave: 2
depends_on: ["143.4-01", "143.4-02"]
files_modified:
  - tests/test-discover-part8.cjs
  - tests/fixtures/discover-acceptance/client-brief.md
  - tests/run-all-1434.sh
autonomous: true
requirements: [DISC-06, DISC-09]
canon_parts: [Part 8, Part 9]
user_setup: []

must_haves:
  truths:
    - "A Part-8 boundary test asserts no client, persona, or product strings can reach a Brain query through the discover dispatch path and that the single discover connector carries only schema fields plus frozen enums"
    - "The Part-8 sweep over the discover artifacts returns zero forbidden matches"
    - "An acceptance run confirms /mos:discover's wiring is sound: the connector registry includes EXACTLY ONE discover connector, --check is green, the DISC-10 Feynman dispatch is verified by resolver-resolvability of The Pyramid Principle (not a second registry entry), and the boundary sweep is zero"
    - "A fixture client exists so the acceptance flow has concrete input"
  artifacts:
    - path: "tests/test-discover-part8.cjs"
      provides: "The DISC-06 discovery-content forbidden-substring sweep + single-connector schema-field assertion + DISC-10 dispatch-by-resolver assertion"
      contains: "client-product-discovery"
    - path: "tests/run-all-1434.sh"
      provides: "The phase test runner that wires the Part-8 test + the --check + the registry assertions (DISC-09)"
      contains: "test-discover-part8.cjs"
  key_links:
    - from: "tests/test-discover-part8.cjs"
      to: "data/connector-registry.json discover entry"
      via: "asserts ALLOWED_CONNECTOR_FIELDS only + web_scope null + frozen reach/posture on the SINGLE discover connector"
      pattern: "ALLOWED_CONNECTOR_FIELDS"
    - from: "tests/run-all-1434.sh"
      to: "scripts/build-connector-registry.cjs --check"
      via: "the acceptance runner re-asserts the green --check"
      pattern: "--check"
---

<objective>
Ship the DISC-06 Part-8 boundary verification and the DISC-09 acceptance gate. This plan mirrors the shipped 143.3 connector-spine forbidden-substring sweep, narrowed to the discover surface: it proves the threat-model paths from the phase context are CLOSED (no client/persona/product strings can reach a Brain query; the single discover connector carries only allowed schema fields + frozen enums; web_scope is null), and it bundles the acceptance assertions (registry includes exactly one discover connector, --check green, DISC-10 dispatch-by-resolver real, sweep zero) into one phase runner.

FIX 1 in this plan: the prior plan's CHECK A asserted BOTH a discover sub_mode AND a plain-language-bridge sub_mode land in the registry. The shipped generator is one-connector-per-file, so there is now ONLY ONE discover connector. This plan asserts EXACTLY ONE discover connector (surface /mos:discover, sub_mode client-product-discovery), that --check is green, and that DISC-10's dispatch is verified by resolver-resolvability of "The Pyramid Principle" (commandsForFramework returns non-empty), NOT by a second registry entry. The runner must assert there is NO plain-language-bridge connector.

Purpose: prove by instrumentation, not by promise, that the most sensitive LOCAL content in the product cannot egress, and gate the phase on a single green acceptance run.
Output: `tests/test-discover-part8.cjs`, a fixture client at `tests/fixtures/discover-acceptance/client-brief.md`, and `tests/run-all-1434.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.4-discover-command-client-product-jtbd-onboarding/143.4-CONTEXT.md
@.planning/phases/143.4-discover-command-client-product-jtbd-onboarding/143.4-PATTERNS.md
@docs/CONNECTOR-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/test-discover-part8.cjs (the DISC-06 boundary sweep + single-connector schema assertions + DISC-10 dispatch-by-resolver)</name>
  <read_first>
    - tests/test-connector-part8-boundary.cjs lines 1-75 (the shipped sweep: the four CLOSED-path checks, the FROZEN_WEB_SCOPES set, the ALLOWED_CONNECTOR_FIELDS set, the FORBIDDEN_BODY_FIELDS list, the ok/fail harness) - copy the harness and the field sets verbatim
    - tests/test-sensors-part8-sweep.cjs (the forbidden-substring approach over a dispatch path - mirror its sweep idiom for the discover-content strings)
    - docs/CONNECTOR-CONTRACT.md section 5 (the connector's only Brain-bound value is the generic framework handle)
    - 143.4-PATTERNS.md block 6 (the discovery-specific sweep DISC-06 needs: no client/persona/product strings in any Brain query; the ALLOWED_CONNECTOR_FIELDS set; web_scope null)
    - data/connector-registry.json (the single discover entry the test reads, produced by Plan 01)
    - data/command-registry.json (the framework_index the DISC-10 dispatch-by-resolver check reads)
    - lib/workflow/command-resolver.cjs lines 85-90 (commandsForFramework - the resolver door the DISC-10 dispatch check exercises)
    - commands/discover.md (the command body the test scans to confirm Brain-bound calls carry only generic framework handles, produced by Plan 01)
  </read_first>
  <action>
Create tests/test-discover-part8.cjs reusing the harness, the FROZEN_WEB_SCOPES set, the ALLOWED_CONNECTOR_FIELDS set, and the FORBIDDEN_BODY_FIELDS list from test-connector-part8-boundary.cjs. Add these discover-specific checks, each PASS/FAIL via the ok/fail harness, exiting non-zero on any fail:

CHECK A (exactly ONE discover connector, schema fields only) - FIX 1: load data/connector-registry.json; find ALL entries with surface "/mos:discover" and assert there is EXACTLY ONE (length === 1; a second connector block would have been silently dropped by the generator, but a stray duplicate surface must also fail). Assert there is NO entry whose sub_mode is "plain-language-bridge" anywhere in the registry (DISC-10 declares no connector here - it dispatches via the existing Feynman surface). Assert the single discover entry carries ONLY keys in ALLOWED_CONNECTOR_FIELDS and NONE of the FORBIDDEN_BODY_FIELDS (no summary/content/body/text/note/description/evidence/turn field that could smuggle client content). Assert its sub_mode is "client-product-discovery".

CHECK B (web_scope + frozen banks): assert the single discover entry has web_scope in FROZEN_WEB_SCOPES and specifically web_scope === null; reach_id is brain_consult (in the frozen 5); posture is push_forward (in the frozen 3).

CHECK C (no client strings in the Brain-bound path): read commands/discover.md as text. Assert that the only Brain-bound framework handles named in the command are the generic published framework names ("Jobs to Be Done (JTBD)", "The Pyramid Principle", "Beautiful Question Framework", "Six Thinking Hats", "Lean Canvas"); confirm the command states the Part-8 rule that Brain queries carry generic handles only and that all discovery content files LOCAL via navigation.cjs. Forbidden-substring sweep: assert the command does NOT instruct sending turn-text, artifact bodies, persona names, product essence, or brand-soul content into a Brain query or a web call (sweep for the anti-patterns: a Brain call whose argument is conversation content). Mirror the test-sensors-part8-sweep forbidden-substring idiom.

CHECK D (the framework handle resolves and is generic): assert the discover connector's framework value is present in data/framework-names.json (a generic published methodology name, never a client string).

CHECK E (DISC-10 dispatch-by-resolver is real) - FIX 1: require lib/workflow/command-resolver.cjs and assert commandsForFramework("The Pyramid Principle") returns a non-empty array (this is how DISC-10 dispatches the Feynman surface - via the resolver door, NOT a second connector). Also assert commandsForFramework("Jobs to Be Done (JTBD)") includes "/mos:discover" (the discover connector framework resolves) and includes "/mos:jtbd" (the FIX-3 route).

Print per-check PASS/FAIL and exit non-zero if any check fails. House rule: hyphens only, no em-dashes.
  </action>
  <acceptance_criteria>
    - node tests/test-discover-part8.cjs exits 0 once Plans 01 and 02 are executed (all five checks PASS)
    - the test references ALLOWED_CONNECTOR_FIELDS and FORBIDDEN_BODY_FIELDS (grep returns at least 1 each)
    - the test asserts exactly one /mos:discover connector and zero plain-language-bridge connectors (grep for both assertions)
    - the test reads data/connector-registry.json and asserts web_scope === null for the discover entry
    - the test reads commands/discover.md and runs a forbidden-substring sweep for client-content-to-Brain anti-patterns
    - the test asserts commandsForFramework("The Pyramid Principle") is non-empty (DISC-10 dispatch-by-resolver, FIX 1)
    - a node check confirms no em-dash character appears in the test file
    - the test exits non-zero if the discover connector entry is mutated to carry web_scope "green" (negative-path: confirmed by the assertion strictly comparing to null)
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-discover-part8.cjs; echo "part8_exit=$?"; node -e "const s=require('fs').readFileSync('tests/test-discover-part8.cjs','utf8'); if(!/ALLOWED_CONNECTOR_FIELDS/.test(s))throw new Error('no allowed set'); if(!/FORBIDDEN_BODY_FIELDS/.test(s))throw new Error('no forbidden set'); if(!/web_scope/.test(s))throw new Error('no web_scope check'); if(!/The Pyramid Principle/.test(s))throw new Error('no DISC-10 dispatch-by-resolver check'); if(!/plain-language-bridge/.test(s))throw new Error('no second-connector-absent assertion'); if(/\u2014/.test(s))throw new Error('em-dash'); console.log('OK part8 test');"</automated>
  </verify>
  <done>tests/test-discover-part8.cjs runs the five CLOSED-path checks over the SINGLE discover connector and the command, asserts exactly one discover connector + zero plain-language-bridge connectors + DISC-10 dispatch-by-resolver (The Pyramid Principle resolves non-empty), exits 0 on the shipped artifacts, exits non-zero on any breach, and has zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: Author the acceptance fixture + the phase runner (DISC-09)</name>
  <read_first>
    - tests/run-all-1433.sh (the shipped 143.3 phase runner shape - the bash that sequences node test files and the --check, the pass/fail accounting) - mirror its structure
    - tests/run-all-1431.sh (a sibling runner for the same phase family - confirm the naming + invocation convention)
    - commands/new-project.md lines 91-190 (what a scaffolded room looks like: ROOM.md per section) - the fixture's expected-shape reference
    - 143.4-PATTERNS.md block 6 (DISC-09 acceptance: on a fixture client, the brief + scaffolded room are produced, the registry includes discover, --check green, the Part-8 sweep returns zero)
  </read_first>
  <action>
Create tests/fixtures/discover-acceptance/client-brief.md: a small SYNTHETIC fixture client (a clearly-fictional brand and product, marked as a fixture in a header comment) that gives the acceptance flow concrete input - a few sentences of brand soul, one product, one user with a job. This is the input a future end-to-end run feeds /mos:discover. Keep it tiny and obviously fictional (no real client names). No em-dashes.

Create tests/run-all-1434.sh mirroring run-all-1433.sh: a bash runner with pass/fail accounting that runs, in order: (1) node tests/test-discover-part8.cjs (DISC-06 boundary sweep returns zero + the five checks); (2) node scripts/build-connector-registry.cjs --check (must exit 0 with the single discover connector present - DISC-09 registry + tripwire); (3) a node one-liner asserting data/connector-registry.json includes EXACTLY ONE /mos:discover connector (sub_mode client-product-discovery) and ZERO plain-language-bridge connectors (FIX 1) AND data/command-registry.json framework_index["Jobs to Be Done (JTBD)"] includes both /mos:discover and /mos:jtbd (DISC-09 registry-includes-discover + WFL-01 + FIX 3) AND commandsForFramework("The Pyramid Principle") is non-empty (DISC-10 dispatch-by-resolver, FIX 1); (4) a node one-liner asserting the Plan 02 artifacts exist and parse (discovery-scaffold.json has 5 sections; discovery-brief-template.md contains "Plain-language message"); (5) a presence check that tests/fixtures/discover-acceptance/client-brief.md exists. The runner prints a final PASS/FAIL summary and exits non-zero if any step fails. Make it executable (chmod +x). House rule: no em-dashes.
  </action>
  <acceptance_criteria>
    - test -f tests/fixtures/discover-acceptance/client-brief.md succeeds and the file is clearly marked synthetic/fixture
    - test -x tests/run-all-1434.sh succeeds (executable)
    - bash tests/run-all-1434.sh exits 0 once Plans 01 and 02 are executed (all five steps pass)
    - the runner invokes tests/test-discover-part8.cjs and scripts/build-connector-registry.cjs --check (grep returns at least 1 each)
    - the runner asserts exactly one /mos:discover connector and zero plain-language-bridge connectors and that The Pyramid Principle resolves (grep for these assertions in the runner)
    - a node check confirms no em-dash character appears in either new file
  </acceptance_criteria>
  <verify>
    <automated>chmod +x tests/run-all-1434.sh; bash tests/run-all-1434.sh; echo "runner_exit=$?"; node -e "const fs=require('fs'); if(!fs.existsSync('tests/fixtures/discover-acceptance/client-brief.md'))throw new Error('no fixture'); const r=fs.readFileSync('tests/run-all-1434.sh','utf8'); if(!/test-discover-part8\.cjs/.test(r))throw new Error('runner missing part8'); if(!/--check/.test(r))throw new Error('runner missing --check'); if(!/plain-language-bridge/.test(r))throw new Error('runner missing no-bridge assert'); if(!/The Pyramid Principle/.test(r))throw new Error('runner missing DISC-10 dispatch assert'); if(/\u2014/.test(r))throw new Error('em-dash'); console.log('OK runner+fixture');"</automated>
  </verify>
  <done>The synthetic fixture client and the executable phase runner exist; the runner sequences the Part-8 sweep, the --check, the exactly-one-discover-connector + no-bridge + Pyramid-resolves assertions, the Plan 02 artifact checks, and the fixture presence check, exits 0 on the shipped artifacts, and has zero em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| discover dispatch path to Brain | The test's whole purpose: prove no client/persona/product string can cross from the discovery conversation into a Brain query. |
| fixture content to repo | The acceptance fixture ships in the repo; it must be obviously synthetic so it is never mistaken for real client data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.4-09 | Information Disclosure | discover connector smuggling a free-text body field | mitigate | CHECK A asserts only ALLOWED_CONNECTOR_FIELDS and none of FORBIDDEN_BODY_FIELDS on the single discover entry; any extra field fails the test (HIGH severity, exits non-zero). |
| T-143.4-10 | Tampering | web_scope drift away from null | mitigate | CHECK B asserts web_scope strictly equals null and is in FROZEN_WEB_SCOPES; a mutation to "green" or any value fails. |
| T-143.4-11 | Information Disclosure | command instructing client content into a Brain or web call | mitigate | CHECK C forbidden-substring sweep over commands/discover.md asserts only generic framework handles reach Brain and all content files LOCAL via navigation.cjs. |
| T-143.4-12 | Tampering | a smuggled second connector (plain-language-bridge) | mitigate | CHECK A + the runner step 3 assert exactly ONE /mos:discover connector and ZERO plain-language-bridge connectors (FIX 1); DISC-10 dispatch verified by CHECK E resolver-resolvability, not a second registry entry. |
| T-143.4-13 | Spoofing | a real client name baked into the fixture | mitigate | The fixture is authored as obviously fictional and marked synthetic; no real client names; acceptance test only checks presence + shape. |
| T-143.4-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (cjs test + markdown fixture + bash runner; pure Node built-ins, mirroring the shipped 143.3 tests). |
</threat_model>

<verification>
- node tests/test-discover-part8.cjs exits 0 (DISC-06 sweep zero, all five checks PASS).
- bash tests/run-all-1434.sh exits 0 (DISC-09 acceptance: registry includes exactly one discover connector, no bridge connector, --check green, The Pyramid Principle resolves, sweep zero, artifacts present).
- Em-dash sweep over all three new files returns zero.
- web_scope strictly null on the single discover connector; only ALLOWED_CONNECTOR_FIELDS present.
</verification>

<success_criteria>
- The Part-8 boundary test proves the threat-model paths are CLOSED and the discovery-content sweep returns zero (DISC-06).
- The phase runner gates DISC-09 acceptance in one green run: exactly one discover connector, no plain-language-bridge connector, --check green, DISC-10 dispatch-by-resolver verified (FIX 1).
- A synthetic fixture client exists.
- DISC-06, DISC-09 covered.
- Zero em-dashes; no real client content.
</success_criteria>

<output>
Create `.planning/phases/143.4-discover-command-client-product-jtbd-onboarding/143.4-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 03)

| Artifact | Type | Notes |
|----------|------|-------|
| `tests/test-discover-part8.cjs` | new test | DISC-06 forbidden-substring sweep + single-connector schema-field assertions + DISC-10 dispatch-by-resolver (The Pyramid Principle resolves) over the one discover connector and the command body |
| `tests/fixtures/discover-acceptance/client-brief.md` | new fixture | synthetic, obviously-fictional client input for the acceptance flow |
| `tests/run-all-1434.sh` | new runner | DISC-09 acceptance gate: sequences the Part-8 sweep, the --check, the exactly-one-discover-connector + no-bridge + Pyramid-resolves assertions, the Plan 02 artifact checks, the fixture presence check |
</output>
