---
phase: 143.4
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/client-discovery-interview/discovery-scaffold.json
  - templates/discovery/discovery-brief-template.md
autonomous: true
requirements: [DISC-04, DISC-07, DISC-10]
canon_parts: [Part 3, Part 4, Part 5, Part 7, Part 8, Part 9]
user_setup: []

must_haves:
  truths:
    - "A discovery-specific section set (brand-soul, product-essence, personas, positioning, habit-loop) exists as a versioned scaffold the command reads to create one ROOM.md per section (ICM Layer 0)"
    - "A Discovery Brief template exists carrying every required section including the Plain-language message section (DISC-10) and the Open-questions+assumptions section (Part 5 + Part 9)"
    - "The habit-loop section in the brief template carries the four Hooked stages plus the Facilitator ethics check (DISC-07), inlined (no dependency on a hooked-model skill dir)"
  artifacts:
    - path: "skills/client-discovery-interview/discovery-scaffold.json"
      provides: "The versioned discovery section set the command turns into ROOM.md per section (OPEN-3)"
      contains: "brand-soul"
    - path: "templates/discovery/discovery-brief-template.md"
      provides: "The Discovery Brief skeleton the synthesis step fills"
      contains: "Plain-language message"
  key_links:
    - from: "skills/client-discovery-interview/discovery-scaffold.json"
      to: "lib/core/mullins-scaffold.cjs reader pattern"
      via: "mirrors the {version, generated_from, sections:[{id,domain,title,prompt,required}]} shape so the same reader semantics apply"
      pattern: "sections"
    - from: "templates/discovery/discovery-brief-template.md"
      to: "commands/discover.md DISC-04 synthesis step"
      via: "the command writes the brief from this template by path"
      pattern: "Discovery Brief"
---

<objective>
Author the two net-new synthesis artifacts that commands/discover.md (Plan 01) references by path: the discovery-specific section set (`discovery-scaffold.json`, resolving OPEN-3) and the Discovery Brief template (`discovery-brief-template.md`). Both are pure data/markdown skeletons - no executable code, no new core module (Canon Part 7 reuse: the room scaffold backend and the brief writer already exist; this plan only supplies the named section set and the brief shape they consume).

This plan runs in the SAME wave as Plan 01 (disjoint file set: Plan 01 owns the ported skill files + commands/discover.md + commands/jtbd.md + the two registries; this plan owns discovery-scaffold.json and the brief template). The design-source skill is ported into the repo by Plan 01 Task 1; this plan's read_first anchors point at the REPO copy of the ported question-bank/SKILL files. Because both plans are wave 1, this plan reads the ported files only if Plan 01 has executed first; the section content here is fully specified from 143.4-PATTERNS.md and the CONTEXT, so this plan does not block on Plan 01's command body - it only mirrors the section-set shape and the brief template structure.

Purpose: give the synthesis step a versioned section set distinct from the venture due-diligence sections, and a brief skeleton whose section list is checkable (so DISC-04, DISC-07 habit-loop, and DISC-10 plain-language all land in fixed slots).
Output: `skills/client-discovery-interview/discovery-scaffold.json` and `templates/discovery/discovery-brief-template.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.4-discover-command-client-product-jtbd-onboarding/143.4-CONTEXT.md
@.planning/phases/143.4-discover-command-client-product-jtbd-onboarding/143.4-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author discovery-scaffold.json (the discovery-specific section set, OPEN-3)</name>
  <read_first>
    - skills/mullins-scaffold/scaffold.json lines 1-45 (the exact {version, generated_from, sections:[{id, domain, title, prompt, required}]} shape to mirror)
    - lib/core/mullins-scaffold.cjs (the reader whose semantics the new json must satisfy - field names and types must match what this reader expects)
    - skills/client-discovery-interview/SKILL.md (the REPO copy ported by Plan 01 Task 1: the six movements + the Discovery Brief section list that motivate the five discovery sections)
    - 143.4-PATTERNS.md block 4 (OPEN-3 recommendation (b): a discovery-specific section set authored as discovery-scaffold.json)
  </read_first>
  <action>
Create skills/client-discovery-interview/discovery-scaffold.json mirroring the mullins-scaffold.json shape exactly (so the shipped reader semantics apply, Canon Part 7). Top-level keys: version (integer 1), generated_from (a string crediting Phase 143.4 / the client-discovery-interview skill), and sections (an array).

Author exactly five section objects, each with id, domain, title, prompt, required. The five sections (resolving OPEN-3): (1) id brand-soul, domain brand, title "Brand soul", prompt drawn from SKILL.md movement 2 (archetype + voice + the never-says list); (2) id product-essence, domain product, title "Product essence", prompt from movement 3 (the one core job + business case + hard constraints); (3) id personas, domain users, title "Personas and JTBD", prompt from movement 4 (functional/emotional/social job + what they fire + the trigger); (4) id positioning, domain market, title "Positioning and whitespace", prompt from movement 5 (the sea of sameness to break + the one sentence only we can say); (5) id habit-loop, domain product, title "Habit loop", prompt from movement 6 (Trigger, Action B=MAP, Variable Reward, Investment + the Facilitator ethics check). Mark brand-soul, product-essence, and personas as required true; positioning and habit-loop as required false. Every prompt is generic methodology guidance - it carries NO client content (Canon Part 8). House rule: no em-dashes in any string.
  </action>
  <acceptance_criteria>
    - node -e parse of skills/client-discovery-interview/discovery-scaffold.json succeeds (valid JSON)
    - the parsed object has version === 1, a string generated_from, and sections array of length 5
    - the five section ids are exactly brand-soul, product-essence, personas, positioning, habit-loop
    - every section object has all five keys id, domain, title, prompt, required (matching the mullins-scaffold shape)
    - a node check confirms no em-dash character appears anywhere in the file
  </acceptance_criteria>
  <verify>
    <automated>node -e "const j=require('./skills/client-discovery-interview/discovery-scaffold.json'); if(j.version!==1)throw new Error('version'); if(typeof j.generated_from!=='string')throw new Error('gen'); if(!Array.isArray(j.sections)||j.sections.length!==5)throw new Error('sections len'); const ids=j.sections.map(s=>s.id).sort(); const want=['brand-soul','habit-loop','personas','positioning','product-essence'].sort(); if(JSON.stringify(ids)!==JSON.stringify(want))throw new Error('ids '+ids); for(const s of j.sections){for(const k of ['id','domain','title','prompt','required']){if(!(k in s))throw new Error('missing '+k)}} const raw=require('fs').readFileSync('./skills/client-discovery-interview/discovery-scaffold.json','utf8'); if(/\u2014/.test(raw))throw new Error('em-dash'); console.log('OK scaffold');"</automated>
  </verify>
  <done>discovery-scaffold.json exists, is valid JSON in the mullins-scaffold shape, carries exactly the five discovery sections with all required keys, and has zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Discovery Brief template with the DISC-07 habit-loop and DISC-10 plain-language sections</name>
  <read_first>
    - skills/client-discovery-interview/question-bank.md (the REPO copy ported by Plan 01 Task 1: the Discovery Brief template - Brand soul / Product essence / Personas+JTBD / Positioning / Habit loop / Design implications / Open questions+assumptions - plus the Hooked worksheet four stages + the Facilitator ethics check wording + the "describe to a smart twelve-year-old" line)
    - commands/new-project.md lines 216-234 (the artifact-entry frontmatter shape source/date the brief should adopt)
    - 143.4-PATTERNS.md block 4 (the brief is a Markdown artifact, no new module) and block 5 (the DISC-10 Plain-language message section + value-prop ladder)
  </read_first>
  <action>
Create templates/discovery/discovery-brief-template.md. Open with the artifact frontmatter shape from new-project.md Step 6 (source, date keys). Body is the brief skeleton from the ported question-bank.md Discovery Brief template, with these exact section headers as Markdown H2: "Brand soul", "Product essence", "Personas and JTBD", "Positioning", "Habit loop", "Plain-language message", "Design implications", "Open questions and assumptions".

Under "Habit loop" (DISC-07) INLINE the four labeled stages from the ported question-bank.md Hooked worksheet (there is no in-repo hooked-model skill to reference): Trigger (external + internal), Action (simplest valuable behavior; B=MAP notes), Variable reward (tribe / hunt / self), Investment (what loads the next trigger), plus an "Ethics" line that asks the Facilitator-quadrant question (improves the user's life AND the maker would use it).

Under "Plain-language message" (DISC-10) include two labeled slots: the one-liner (the "explain it to a smart twelve-year-old" sentence) and the value-prop ladder. Note inline that this section is the load-bearing handoff from discovery to any website or deck build and that the deck is produced on request only via MOSDeckEngine (dispatched by the command through the resolver door, not from this template).

Under "Open questions and assumptions" (Part 5 + Part 9) include the instruction that every guess is flagged as an assumption with review_status: proposed and is never allowed to harden into a fact.

Every slot is a blank placeholder or a guidance note - the template carries NO client content (Canon Part 8). House rule: no em-dashes.
  </action>
  <acceptance_criteria>
    - test -f templates/discovery/discovery-brief-template.md succeeds
    - the file contains H2 headers for all eight sections: Brand soul, Product essence, Personas and JTBD, Positioning, Habit loop, Plain-language message, Design implications, Open questions and assumptions
    - grep for "Plain-language message" returns at least 1 (DISC-10 section present)
    - grep for "Trigger" and "Investment" and "Facilitator" each return at least 1 (DISC-07 habit-loop + ethics present, inlined)
    - grep for "twelve-year-old" returns at least 1 (DISC-10 one-liner slot)
    - grep for "review_status: proposed" or "proposed" returns at least 1 (Part 5 + Part 9 assumption flagging)
    - a node check confirms no em-dash character appears in the file
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('templates/discovery/discovery-brief-template.md','utf8'); const need=['Brand soul','Product essence','Personas and JTBD','Positioning','Habit loop','Plain-language message','Design implications','Open questions and assumptions','Trigger','Investment','Facilitator','twelve-year-old','proposed']; for(const t of need){if(!s.includes(t))throw new Error('missing '+t)} if(/\u2014/.test(s))throw new Error('em-dash'); console.log('OK brief template');"</automated>
  </verify>
  <done>discovery-brief-template.md exists with all eight H2 sections, the DISC-07 four-stage habit loop + Facilitator ethics line (inlined from the ported question-bank), the DISC-10 plain-language one-liner + value-prop ladder, the Part 5/9 assumption-flagging instruction, and zero em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template/scaffold content to repo | These artifacts ship in the plugin; they must carry only generic methodology prompts, never any sample client content that could be mistaken for real data or leak a pattern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.4-06 | Information Disclosure | discovery-scaffold.json prompts | mitigate | Every prompt is generic methodology guidance; the acceptance test confirms the section set is structural; no client strings are authored into the file (Canon Part 8). |
| T-143.4-07 | Information Disclosure | discovery-brief-template.md slots | mitigate | All slots are blank placeholders or guidance notes; no example client content; the brief is filled at runtime into LOCAL room data only. |
| T-143.4-08 | Repudiation | a guess hardening into a fact | mitigate | The Open-questions-and-assumptions section instructs review_status: proposed on every guess (Canon Part 5 + Part 9 proposed-not-confirmed). |
| T-143.4-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (JSON + Markdown only); no dependency surface. |
</threat_model>

<verification>
- Both files parse / read cleanly with all required sections present.
- Em-dash sweep over both files returns zero.
- The scaffold matches the mullins-scaffold reader shape (Part 7 reuse).
- No client content authored into either artifact (Part 8).
</verification>

<success_criteria>
- discovery-scaffold.json carries the five discovery sections in the shipped scaffold shape (OPEN-3 resolved).
- discovery-brief-template.md carries all eight sections including the DISC-07 habit-loop + ethics (inlined) and the DISC-10 plain-language message.
- DISC-04, DISC-07, DISC-10 (the artifact halves) covered.
- Zero em-dashes; no client content.
</success_criteria>

<output>
Create `.planning/phases/143.4-discover-command-client-product-jtbd-onboarding/143.4-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 02)

| Artifact | Type | Notes |
|----------|------|-------|
| `skills/client-discovery-interview/discovery-scaffold.json` | new data file | versioned five-section set (brand-soul, product-essence, personas, positioning, habit-loop) in the mullins-scaffold shape; consumed by the command to write ROOM.md per section |
| `templates/discovery/discovery-brief-template.md` | new template | the Discovery Brief skeleton with the eight H2 sections including Plain-language message; consumed by the DISC-04 synthesis step |
</output>
