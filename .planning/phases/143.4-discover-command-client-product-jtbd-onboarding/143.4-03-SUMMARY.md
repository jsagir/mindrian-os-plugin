---
phase: 143.4-discover-command-client-product-jtbd-onboarding
plan: 03
subsystem: tests
tags: [part-8-boundary, acceptance-gate, connector-spine, command-resolver, discover, jtbd, canon-part-8, canon-part-7]

# Dependency graph
requires:
  - phase: 143.4-01
    provides: commands/discover.md + the single discover connector in data/connector-registry.json + the JTBD/Pyramid resolver routes
  - phase: 143.4-02
    provides: skills/client-discovery-interview/discovery-scaffold.json (5 sections) + templates/discovery/discovery-brief-template.md (Plain-language message)
  - phase: 143.3 connector-spine
    provides: tests/test-connector-part8-boundary.cjs (the sweep + field-set pattern) + scripts/build-connector-registry.cjs --check tripwire
  - phase: 122 workflow-layer
    provides: lib/workflow/command-resolver.cjs commandsForFramework
provides:
  - "tests/test-discover-part8.cjs - the DISC-06 boundary sweep (5 checks over the SINGLE discover connector + command body + resolver door)"
  - "tests/fixtures/discover-acceptance/client-brief.md - synthetic obviously-fictional fixture client for the acceptance flow"
  - "tests/run-all-1434.sh - the DISC-09 acceptance gate that sequences the sweep + --check + wiring assertions + Plan 02 artifact checks + fixture presence"
affects: [144.1 (connector sweep for the Feynman surfaces DISC-10 dispatches through)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Part-8 boundary sweep narrowed to a single product surface: copies the FROZEN_WEB_SCOPES + ALLOWED_CONNECTOR_FIELDS + FORBIDDEN_BODY_FIELDS sets verbatim from the 143.3 connector-spine sweep"
    - "DISC-10 dispatch-by-resolver proven by resolver-resolvability (commandsForFramework non-empty), NOT by a second registry entry (FIX 1)"
    - "phase runner with a run_step helper that runs every step to completion (no crash on failure) and exits 1 if any failed, mirroring run-all-1433.sh"

key-files:
  created:
    - tests/test-discover-part8.cjs
    - tests/fixtures/discover-acceptance/client-brief.md
    - tests/run-all-1434.sh
  modified: []

key-decisions:
  - "CHECK A asserts EXACTLY ONE /mos:discover connector (length === 1) and ZERO plain-language-bridge connectors (FIX 1); a stray duplicate surface fails too"
  - "CHECK B uses assert.strictEqual(web_scope, null) so any drift to a string value (e.g. green) fails the negative path"
  - "CHECK E proves DISC-10 dispatch via commandsForFramework(The Pyramid Principle) non-empty + JTBD resolves /mos:discover and /mos:jtbd (FIX 3), never a second connector"
  - "the fixture is authored as obviously fictional (Tindlewick Lantern Co.), marked SYNTHETIC in a header comment, no real client names (T-143.4-13)"

patterns-established:
  - "the runner cd's into REPO_ROOT first so the node one-liners resolve data/ + lib/ via process.cwd() regardless of invocation directory"
  - "forbidden-substring sweep pairs a content noun (turn text / artifact body / persona name / product essence / brand soul / client specific) with a Brain/web egress verb in one regex (mirrors test-sensors-part8-sweep idiom)"

requirements-completed: [DISC-06, DISC-09]

# Metrics
duration: ~10min
completed: 2026-06-07
---

# Phase 143.4 Plan 03: Part-8 Boundary Test + Acceptance Fixture + Phase Runner Summary

**Shipped the DISC-06 boundary verification (tests/test-discover-part8.cjs - 5 checks proving no client/persona/product string can reach Brain through the single discover connector, web_scope strictly null, DISC-10 dispatch-by-resolver real) and the DISC-09 acceptance gate (tests/run-all-1434.sh + a synthetic fixture client) that bundles the sweep, the CONN-03 --check, the exactly-one-discover-connector + no-bridge + JTBD/Pyramid-resolve wiring, and the Plan 02 artifact checks into one green run.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-06-07T18:37:21Z
- **Completed:** 2026-06-07T18:47:00Z
- **Tasks:** 2
- **Files modified:** 3 (all created)

## Accomplishments
- tests/test-discover-part8.cjs: five PASS/FAIL checks over the single discover connector, the command body, and the resolver door, each exiting non-zero on breach:
  - CHECK A: EXACTLY ONE /mos:discover connector (length === 1), ZERO plain-language-bridge connectors (FIX 1), schema-fields-only (ALLOWED_CONNECTOR_FIELDS), none of FORBIDDEN_BODY_FIELDS, sub_mode client-product-discovery.
  - CHECK B: web_scope strictly null (assert.strictEqual) inside FROZEN_WEB_SCOPES; reach_id brain_consult; posture push_forward.
  - CHECK C: the only Brain-bound handles named in commands/discover.md are the five generic published framework names; the command states the Part-8 rule (generic handles only, content files LOCAL via navigation.cjs); a forbidden-substring sweep finds no client-content-to-Brain/web anti-pattern.
  - CHECK D: the discover framework value is a generic name present in data/framework-names.json.
  - CHECK E: commandsForFramework("The Pyramid Principle") returns non-empty (DISC-10 dispatch-by-resolver, FIX 1); JTBD resolves both /mos:discover and /mos:jtbd (FIX 3).
- tests/fixtures/discover-acceptance/client-brief.md: a tiny, obviously-fictional fixture client (Tindlewick Lantern Co.) marked SYNTHETIC in a header comment, with one brand soul + one product + one user with a JTBD job statement (T-143.4-13 mitigated).
- tests/run-all-1434.sh: the DISC-09 acceptance gate sequencing the five steps, run to completion even on failure, with a per-step PASS/FAIL line and a final tally; chmod +x; exits 0 on the shipped artifacts.
- Verified the negative path: mutating the discover connector web_scope to "green" is caught by the strict-null comparison in CHECK B.

## Task Commits

Each task committed atomically:

1. **Task 1: Author tests/test-discover-part8.cjs (DISC-06 boundary sweep)** - `16bdccfc` (test)
2. **Task 2: Author the synthetic fixture + tests/run-all-1434.sh acceptance gate (DISC-09)** - `27bd8bdf` (feat)

**Plan metadata:** see final docs commit below.

## Files Created/Modified
- `tests/test-discover-part8.cjs` - the DISC-06 forbidden-substring sweep + single-connector schema-field assertions + DISC-10 dispatch-by-resolver over the one discover connector and the command body
- `tests/fixtures/discover-acceptance/client-brief.md` - synthetic, obviously-fictional client input for the acceptance flow
- `tests/run-all-1434.sh` - the DISC-09 acceptance gate: Part-8 sweep + CONN-03 --check + exactly-one-discover-connector + no-bridge + JTBD/Pyramid-resolve + Plan 02 artifact checks + fixture presence

## Decisions Made
- **CHECK A asserts ONE discover connector, not two (FIX 1):** the shipped generator is one-connector-per-file, so there is exactly ONE discover connector. The test asserts `discover.length === 1` and `bridge.length === 0`; DISC-10's dispatch is proven by CHECK E resolver-resolvability, not by a second registry entry. This corrects the prior plan's CHECK A that asserted both a discover sub_mode AND a plain-language-bridge sub_mode land in the registry.
- **Strict-null web_scope (CHECK B):** `assert.strictEqual(c.web_scope, null)` means any drift to a string value (e.g. "green") fails, satisfying T-143.4-10 by instrumentation rather than by promise.
- **Forbidden-substring sweep design (CHECK C):** three regexes each pair a content noun with a Brain/web egress verb in one phrase, mirroring the test-sensors-part8-sweep idiom, so the sweep catches an instruction to egress client content without flagging the command's legitimate generic-handle references.
- **Runner cd's to REPO_ROOT first:** so the embedded node one-liners resolve data/ and lib/ via process.cwd() regardless of the directory the runner is invoked from.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Runner cwd hardening so node one-liners resolve data/ + lib/ from any invocation directory**
- **Found during:** Task 2 (authoring run-all-1434.sh)
- **Issue:** The runner's embedded node one-liners resolve `data/connector-registry.json` and `lib/workflow/command-resolver.cjs` via `process.cwd()`. If the runner were invoked from a directory other than the repo root, those reads would fail. The plan's runner spec did not specify a working-directory anchor.
- **Fix:** Added `cd "$REPO_ROOT"` immediately after computing REPO_ROOT (with a fatal guard), so every step runs from the repo root. This is a correctness requirement for a runner that must pass from any caller (Canon Part 6 dog-fooding: the gate has to work the way CI invokes it).
- **Files modified:** tests/run-all-1434.sh
- **Verification:** `bash tests/run-all-1434.sh` exits 0; the node one-liners print their OK lines.
- **Committed in:** `27bd8bdf`

---

**Total deviations:** 1 auto-fixed (1 blocking).
**Impact on plan:** No scope change. The runner sequences exactly the five steps the plan specifies; the only addition is a working-directory anchor so the gate is invocation-robust.

## Threat Model Coverage
- **T-143.4-09** (free-text body field on the connector): CHECK A asserts ALLOWED_CONNECTOR_FIELDS only + none of FORBIDDEN_BODY_FIELDS. PASS.
- **T-143.4-10** (web_scope drift): CHECK B asserts strict null. PASS (negative path proven: web_scope=green is caught).
- **T-143.4-11** (command instructing client content to Brain/web): CHECK C forbidden-substring sweep + generic-handles-only assertion. PASS.
- **T-143.4-12** (smuggled second connector): CHECK A + runner step 3 assert exactly ONE discover connector + ZERO plain-language-bridge; CHECK E proves DISC-10 dispatch via the resolver door. PASS.
- **T-143.4-13** (real client name in the fixture): fixture authored as obviously fictional, marked SYNTHETIC, no real names. PASS.
- **T-143.4-SC** (package installs): none in this plan (pure Node built-ins + markdown fixture + bash runner). Accept.

## Issues Encountered
None beyond the deviation above. The DISC-06 boundary test passed all five checks on first run against the shipped Plan 01/02 artifacts; the runner passed all five steps on first run.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- The 143.4 phase is verification-complete: the Part-8 boundary is proven CLOSED by instrumentation and the DISC-09 acceptance gate is green in one run.
- The Feynman surfaces DISC-10 dispatches through (/mos:mos-reason, /mos:structure-argument, MOSDeckEngine) still need their own connector frontmatter in the 144.1 sweep; until then DISC-10 rides them via the documented resolver door, which CHECK E continues to assert.

## Self-Check: PASSED

- FOUND: tests/test-discover-part8.cjs
- FOUND: tests/fixtures/discover-acceptance/client-brief.md
- FOUND: tests/run-all-1434.sh
- FOUND commit: 16bdccfc (Task 1 boundary test)
- FOUND commit: 27bd8bdf (Task 2 fixture + runner)
- bash tests/run-all-1434.sh exits 0; node tests/test-discover-part8.cjs exits 0; zero em-dashes across all three files

---
*Phase: 143.4-discover-command-client-product-jtbd-onboarding*
*Completed: 2026-06-07*
