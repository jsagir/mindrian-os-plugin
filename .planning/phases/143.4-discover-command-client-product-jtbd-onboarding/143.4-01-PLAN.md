---
phase: 143.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/client-discovery-interview/SKILL.md
  - skills/client-discovery-interview/question-bank.md
  - commands/discover.md
  - commands/jtbd.md
  - data/connector-registry.json
  - data/command-registry.json
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-04, DISC-05, DISC-07, DISC-08, DISC-10]
canon_parts: [Part 2, Part 2a, Part 3, Part 4, Part 6, Part 7, Part 8, Part 9, Part 10]
user_setup: []

must_haves:
  truths:
    - "The design-source skill (SKILL.md + question-bank.md) is PORTED into the repo at skills/client-discovery-interview/ so every downstream read_first points at a repo path, never the ~/.claude cache (FIX 2)"
    - "Running /mos:discover opens a Larry-led six-movement conversation, one question at a time, that adapts order to navigator input"
    - "A cold navigator (no room) and a warm navigator (existing room) both get a graceful entry path"
    - "Each movement names the existing atom it reuses via commandsForFramework and never hardcodes a slug"
    - "Every choice point renders as a Shape F.1 selector, never bare prose"
    - "commands/discover.md carries EXACTLY ONE connector block (the discovery flow); DISC-10 dispatches via the EXISTING Feynman surface resolved by commandsForFramework(The Pyramid Principle), NOT a second connector (FIX 1)"
    - "commands/jtbd.md declares frameworks: [Jobs to Be Done (JTBD)] so commandsForFramework(Jobs to Be Done (JTBD)) includes /mos:jtbd (FIX 3)"
    - "The single discover connector joins data/connector-registry.json and the registry --check exits 0"
    - "The DISC-10 step produces a plain-language message by default and a deck only on request, dispatching MOSDeckEngine, never re-implementing the Feynman pipeline"
  artifacts:
    - path: "skills/client-discovery-interview/SKILL.md"
      provides: "The ported investigator method + six movements + Hooked-twice principle (repo copy of the design source)"
      min_lines: 40
    - path: "skills/client-discovery-interview/question-bank.md"
      provides: "The ported question library + JTBD job-interview script + Hooked worksheet + Discovery Brief template (repo copy)"
      min_lines: 40
    - path: "commands/discover.md"
      provides: "The /mos:discover Larry-led discovery flow + EXACTLY ONE connector block + synthesis + Feynman bridge dispatch"
      min_lines: 200
      contains: "connector:"
    - path: "data/connector-registry.json"
      provides: "Regenerated registry that includes the single discover connector"
      contains: "/mos:discover"
  key_links:
    - from: "commands/discover.md"
      to: "lib/workflow/command-resolver.cjs::commandsForFramework"
      via: "each movement and the DISC-10 bridge route through the resolver, not a hardcoded slug"
      pattern: "commandsForFramework"
    - from: "commands/discover.md connector framework"
      to: "data/command-registry.json framework_index"
      via: "Jobs to Be Done (JTBD) resolves to a command list of length > 0 once discover.md and jtbd.md regenerate"
      pattern: "Jobs to Be Done"
---

<objective>
Author `commands/discover.md`: the first product command authored to ride the 143.3 connector spine. It is a Larry-led, multi-turn, one-question-at-a-time discovery flow that sequences existing /mos: atoms (Canon Part 7 orchestration, NOT a new atom), declares EXACTLY ONE connector block (the discovery flow) so it auto-joins `connector-registry.json`, and renders every choice through the Shape F.1 dial (Canon Part 3). It WRAPS `/mos:new-project` and `/mos:onboard` rather than superseding them (OPEN-2).

This plan also lands two enabling prerequisites in the same wave-1 file set:
- FIX 2 (port): the design-source skill lives ONLY in the personal cache `~/.claude/skills/`, which the executor cannot read from the repo working tree (workspace guard). Task 1 PORTS it into the repo at `skills/client-discovery-interview/` so every downstream `read_first` (this plan and Plan 02) can point at the repo copy.
- FIX 3 (jtbd resolver route): `commands/jtbd.md` ships no `frameworks:` line today, so `commandsForFramework("Jobs to Be Done (JTBD)")` returns analyze-needs/user-needs/validate, NOT /mos:jtbd. Task 3 adds `frameworks: ["Jobs to Be Done (JTBD)"]` to jtbd.md and regenerates the command registry so /mos:jtbd resolves legitimately via the resolver door.

FIX 1 (single connector): the shipped `scripts/build-connector-registry.cjs` parses EXACTLY ONE `connector:` block per file (a second block silently overwrites the first - proven by reading the parser, lines 140-213 and 194). Therefore discover.md carries ONE connector (the discovery flow). DISC-10's Feynman bridge dispatches via the EXISTING Feynman surface (/mos:mos-reason + MOSDeckEngine) resolved by `commandsForFramework("The Pyramid Principle")` as a documented sub-step - those surfaces get their own connectors in the 144.1 sweep, not here.

Purpose: ship the conversational front door that takes a navigator from "I have a client or product" to a scaffolded Data Room plus Discovery Brief, sequencing the methodology atoms the plugin already has.
Output: the ported skill files, `commands/discover.md`, the `frameworks:` line on `commands/jtbd.md`, and the regenerated `data/connector-registry.json` + `data/command-registry.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.4-discover-command-client-product-jtbd-onboarding/143.4-CONTEXT.md
@.planning/phases/143.4-discover-command-client-product-jtbd-onboarding/143.4-PATTERNS.md
@docs/CONNECTOR-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PORT the design-source skill into the repo (FIX 2) and add the jtbd resolver route (FIX 3)</name>
  <read_first>
    - ~/.claude/skills/client-discovery-interview/SKILL.md (the ONLY readable location of the design source at this point - the cache; this task is the one place the cache is read, in order to copy it into the repo)
    - ~/.claude/skills/client-discovery-interview/question-bank.md (same: read from the cache to port into the repo)
    - commands/jtbd.md lines 1-19 (the frontmatter that is MISSING a frameworks: line - confirm it has serves_jtbd: ["audit-room"] but no frameworks: key; the new line is added without disturbing the other keys)
    - skills/MOSDeckEngine/SKILL.md lines 1-10 (confirm the canonical Feynman engine skill dir exists in the repo as a sibling shape for the port)
  </read_first>
  <action>
PORT step (FIX 2). Copy the two design-source files from the personal cache into the repo so the executor can read them in later tasks (the workspace guard makes the cache unreadable from the repo working tree for any later task). Use Bash to copy: create the directory skills/client-discovery-interview/ then copy ~/.claude/skills/client-discovery-interview/SKILL.md to skills/client-discovery-interview/SKILL.md and ~/.claude/skills/client-discovery-interview/question-bank.md to skills/client-discovery-interview/question-bank.md. This is a verbatim port ("promoted from the standalone skill" per the CONTEXT source line). After copying, if either ported file contains the unicode em-dash character (codepoint 2014), replace each occurrence with a hyphen so the repo copies honor the house no-em-dash rule. Do NOT otherwise rewrite the content. Note in the SUMMARY that the ported skill becomes a 144.1 sweep target (it will get its own connector frontmatter later, not here).

JTBD RESOLVER ROUTE step (FIX 3). Edit commands/jtbd.md frontmatter ONLY: add a single new line `frameworks: ["Jobs to Be Done (JTBD)"]` to the YAML frontmatter (the kind/frameworks workflow-layer key, mirroring how analyze-needs and user-needs declare it). Do NOT change any other key, the body, or the command behavior. This is the correct fix: /mos:jtbd SHOULD resolve under "Jobs to Be Done (JTBD)" because it is the JTBD inspection/set command. After this edit the command-registry regeneration in Task 3 makes commandsForFramework("Jobs to Be Done (JTBD)") include /mos:jtbd.

Do NOT regenerate any registry in this task (Task 3 owns the single regeneration so the byte-stable --check passes once at the end).
  </action>
  <acceptance_criteria>
    - test -f skills/client-discovery-interview/SKILL.md AND test -f skills/client-discovery-interview/question-bank.md both succeed (the port landed in the repo)
    - `wc -l skills/client-discovery-interview/SKILL.md` returns at least 40 lines (real content ported, not a stub)
    - an em-dash sweep (node check for codepoint 2014) over both ported files returns zero matches
    - `grep -c 'frameworks:' commands/jtbd.md` returns at least 1 AND the frameworks line contains "Jobs to Be Done (JTBD)"
    - the rest of commands/jtbd.md frontmatter is intact: grep still finds name: jtbd and serves_jtbd
    - an em-dash sweep over commands/jtbd.md returns zero matches
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const f of ['skills/client-discovery-interview/SKILL.md','skills/client-discovery-interview/question-bank.md']){if(!fs.existsSync(f))throw new Error('missing port '+f); const s=fs.readFileSync(f,'utf8'); if(s.split(/\n/).length<40)throw new Error('port too short '+f); if(/\u2014/.test(s))throw new Error('em-dash in '+f);} const j=fs.readFileSync('commands/jtbd.md','utf8'); if(!/frameworks:\s*\[\"Jobs to Be Done \(JTBD\)\"\]/.test(j))throw new Error('no jtbd frameworks line'); if(!/name: jtbd/.test(j))throw new Error('jtbd frontmatter disturbed'); if(/\u2014/.test(j))throw new Error('em-dash in jtbd'); console.log('OK task1');"</automated>
  </verify>
  <done>The design-source skill is ported into skills/client-discovery-interview/ (repo copies, em-dash-clean), and commands/jtbd.md declares frameworks: ["Jobs to Be Done (JTBD)"] without disturbing its other frontmatter. No registry regenerated yet.</done>
</task>

<task type="auto">
  <name>Task 2: Author discover.md shell + frontmatter + voice rules + dual-entry mode detection + the six-movement body + reuse map + F.1 gates + DISC-07 Hooked + DISC-04 synthesis + DISC-10 Feynman bridge</name>
  <read_first>
    - commands/onboard.md (full) - the multi-step conversational walkthrough analog; copy the LOCKED voice rules block (the "Voice rules (LOCKED)" section: no emoji, signature openers, Ask-Tell dial 0.15 ask-heavy, 12-glyph vocabulary, 3-line error pattern) verbatim; copy the Mode Detection pattern; copy the closing Shape F.1 Next Move selector via AskUserQuestion (the surface that writes STATE.md Decisions + a typed CHOSE edge)
    - commands/new-project.md lines 24-48 (Step 1 three-state registry check = the exact Mode A / Mode B / Tier 0 dual-entry shape DISC-08 needs), lines 54-89 (the ONE-question-at-a-time deep-exploration discipline + summarize-before-acting + wait-for-confirmation), lines 1-16 (frontmatter shape)
    - commands/research.md lines 1-38 (the shipped frontmatter + Phase 122 workflow-layer block layout - the connector block itself is authored in Task 3)
    - skills/client-discovery-interview/SKILL.md (the REPO copy ported in Task 1: the six movements, the investigator method, the Hooked-twice principle, the Discovery Brief section list, the common mistakes)
    - skills/client-discovery-interview/question-bank.md (the REPO copy ported in Task 1: the Hooked worksheet four stages + ethics check, the Discovery Brief template, the "describe to a smart twelve-year-old" line)
    - skills/MOSDeckEngine/SKILL.md lines 1-30 (the 6-stage Feynman pipeline DISC-10 dispatches, never re-implements)
    - 143.4-PATTERNS.md block 1 (frontmatter shape), block 3 (the per-movement reuse table with the exact framework to resolver-key mapping), block 5 (the DISC-10 Feynman bridge: MOSDeckEngine canonical, mos-reason for the pyramid, plain-language default + deck on request)
  </read_first>
  <action>
Create commands/discover.md (this file is owned solely by this plan). Author the YAML frontmatter with these exact keys: name: discover; description: a verb-first under-promise (for example "Discover a client, product, and its users before any build"); help_jtbd; body_shape: B (Semantic Tree); serves_jtbd of ["explore"]; teaching in the "When you ___, /mos:discover does ___" form (CLAUDE.md Feynman-JTBD rule); interactive_first_reward: reframe_question; ui_reference: skills/ui-system/SKILL.md. Then the Phase 122 workflow-layer block: kind: methodology; frameworks: ["Jobs to Be Done (JTBD)"] (this MUST equal the single discovery connector framework added in Task 3 so the resolver join works); allowed-tools of [Read, Write, Bash, Glob, WebFetch]. Leave the connector block for Task 3 (Task 3 adds it inside this same frontmatter, above the closing fence).

Write the LOCKED voice-rules block copied verbatim from onboard.md (no emoji, signature openers "Very simply..." and "Here's the thing...", Ask-Tell dial starts at 0.15 ask-heavy during discovery, the 12-glyph symbol vocabulary, the 3-line error pattern What / Why / Fix). House rule: hyphens only, NO em-dashes anywhere.

Write the dual-entry Mode Detection section (DISC-08) mirroring new-project.md Step 1's three states: COLD (no ~/MindrianRooms registry and no legacy room/ so scaffold a discovery room from scratch); WARM (registry exists so enrich the active or named room); Tier 0 graceful (brain unreachable or empty STATE.md so still run the conversation, file locally, no RECOMMENDED markers per Canon Part 3 Mode B). Branch on the command argument first (a client or product name is a warm-target hint).

Add the Tri-Polar note (DISC-09 and CLAUDE.md): /mos:discover is a conversational command so it works on CLI, Desktop, and Cowork; note the surface differences (CLI files via Bash plus hooks; Desktop is Larry-conversational; Cowork writes the room into shared 00_Context state).

Append the six-movement investigation. The movements (from the ported SKILL.md): (1) Warm-up and the real why; (2) The brand soul (archetype + voice + the "never says" list); (3) The product essence (one core job + business case + hard constraints); (4) Users and JTBD (functional, emotional, social job + what they fire + the trigger); (5) Competition and whitespace; (6) The product as a Hooked loop. State explicitly: ONE question at a time, follow the navigator's energy, push back on vague answers, mirror-and-confirm, adapt the order to what the navigator brings (DISC-02).

For EACH movement write the Part-7 reuse-map justification line (DISC-03) naming the existing atom and the resolver call, in this exact mapping: movement 1 reuses /mos:beautiful-question via commandsForFramework("Beautiful Question Framework"); movement 2 reuses /mos:persona and /mos:think-hats via commandsForFramework("Six Thinking Hats"); movement 3 reuses /mos:new-project scaffold and /mos:lean-canvas via commandsForFramework("Lean Canvas"); movement 4 reuses /mos:analyze-needs, /mos:user-needs, and /mos:jtbd via commandsForFramework("Jobs to Be Done (JTBD)") (and note that /mos:jtbd now resolves under this framework after the FIX-3 route lands); movement 5 reuses /mos:compare-ventures and /mos:whitespace via commandsForFramework on their declared frameworks; movement 6 INLINES the Hooked loop (see DISC-07 below), it is rendered into the brief, not dispatched. Each dispatching justification reads "reuses <command> via commandsForFramework(<framework>) because a thin orchestration of the shipped atom beats a new atom (Canon Part 7); net-new is the SEQUENCING, not the methodology." NEVER write a bare /mos: slug as the dispatch contract - the resolver door is the contract; a framework that resolves to an empty list degrades to "run it manually" (CONNECTOR-CONTRACT.md section 4).

Render EVERY choice point (movement transition, the post-synthesis "what next", the deck-on-request gate) as a Shape F.1 selector via AskUserQuestion - copy the onboard.md closing selector surface. The selection writes to STATE.md Decisions plus a typed (navigator)-[CHOSE {verb,reason}]->(artifact) edge. RECOMMENDED marker ONLY at Brain confidence at or above 0.7 (Phase 88.2 invariant); in Mode B and Tier 0 no RECOMMENDED marker. NO bare-prose recommendations (the Cluster 5 audit violation).

Add the DISC-07 Hooked-two-ways section. INLINE the four Hooked stages from the ported question-bank.md Hooked worksheet directly into this section (FIX 2: there is NO in-repo hooked-model skill to read_first; embed the stages). (a) The brief's habit-loop section design: Trigger (external + internal emotion), Action (Fogg B=MAP - simplest valuable behavior), Variable Reward (tribe / hunt / self), Investment (what loads the next trigger) WITH the Facilitator-quadrant ethics check (does the loop improve the user's life; would the maker use it). (b) The command's OWN engagement loop where the brief taking shape is the visible investment hook that keeps the navigator generous with the truth. You MAY name the globally-available hooked-model skill as a runtime reference only (do not point a read_first at a skills/hooked-model dir - it does not exist).

Add the DISC-04 synthesis step (prose; the brief template file and scaffold json are authored in Plan 02 and referenced by path): after the conversation, write the Discovery Brief using templates/discovery/discovery-brief-template.md (Brand soul / Product essence / Personas+JTBD cards / Positioning / Habit loop / Plain-language message / Design implications / Open-questions+assumptions); scaffold the room sections from skills/client-discovery-interview/discovery-scaffold.json with a ROOM.md per section (ICM Layer 0); file personas as room/team/ai-personas/* with an embedded JTBD card (OPEN-4); mark every guess as review_status: proposed (Canon Part 5 + Part 9). File ALL artifacts through lib/core/navigation.cjs fileEvidenceWithReadback - never folder-scan (Canon Part 9). Obsidian nested rule (CLAUDE.md decision 16): section/artifact-name/artifact-name.md.

Add the DISC-10 Feynman-bridge step (FIX 1: dispatch-by-resolver, NOT a second connector): after synthesis, run the plain-language bridge over product-essence and brand-soul. Default output is the plain-language message (the "explain to a smart twelve-year-old" one-liner from the ported question-bank.md, sharpened, plus the value-prop ladder) written into the brief's Plain-language message section and seeded into the room's per-section FEYNMAN.md via the shipped lib/core/folder-memory.cjs writer (never hand-write FEYNMAN.md). The reasoning pyramid + deck are dispatched via the EXISTING Feynman surface resolved through commandsForFramework("The Pyramid Principle") (which returns /mos:mos-reason and /mos:structure-argument) and the MOSDeckEngine skill. Deck ON REQUEST only (OPEN-5): an F.1 gate offers "build the deck" which dispatches MOSDeckEngine. State explicitly that DISC-10 does NOT declare its own connector on discover.md (the shipped generator is one-connector-per-file); it rides the Feynman surface's own connector, which lands in the 144.1 sweep. Do NOT re-implement the 6-stage pipeline; do NOT extend the generator parser.

The command may use the established node ${CLAUDE_PLUGIN_ROOT}/... invocation idiom for cjs calls; do not paste full command-file bodies.
  </action>
  <acceptance_criteria>
    - test -f commands/discover.md succeeds
    - `grep -c "name: discover" commands/discover.md` returns at least 1
    - the command frontmatter declares frameworks of exactly ["Jobs to Be Done (JTBD)"] (one line)
    - `grep -c "interactive_first_reward: reframe_question" commands/discover.md` returns 1
    - `grep -E "Mode A|Mode B|Tier 0|cold|warm|COLD|WARM" commands/discover.md` returns at least 3 lines (dual-entry present)
    - `grep -c "commandsForFramework" commands/discover.md` returns at least 5 (one resolver call cited per dispatching movement + the DISC-10 Pyramid Principle bridge)
    - `grep -E "Beautiful Question Framework|Six Thinking Hats|Lean Canvas|Jobs to Be Done|The Pyramid Principle" commands/discover.md` returns at least 4 lines (the resolver framework keys present, including the DISC-10 bridge key)
    - `grep -ci "one question at a time" commands/discover.md` returns at least 1
    - `grep -c "F.1" commands/discover.md` returns at least 2 (F.1 gate at choice points)
    - `grep -ci "AskUserQuestion" commands/discover.md` returns at least 1
    - `grep -ci "Facilitator" commands/discover.md` returns at least 1 (Hooked ethics check, DISC-07, inlined)
    - `grep -Ei "twelve-year-old|plain-language" commands/discover.md` returns at least 1 (DISC-10 plain-language default)
    - `grep -ci "MOSDeckEngine" commands/discover.md` returns at least 1 AND a grep for "on request" returns at least 1 (deck on request, OPEN-5)
    - `grep -Ei "fileEvidenceWithReadback|navigation.cjs" commands/discover.md` returns at least 1 (Part 9 filing chokepoint)
    - `grep -ci "proposed" commands/discover.md` returns at least 1 (Part 5 + Part 9 assumption flagging)
    - there is NO read_first or instruction pointing at a skills/hooked-model directory (FIX 2): `grep -c "skills/hooked-model" commands/discover.md` returns 0
    - em-dash sweep over commands/discover.md returns zero matches
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('commands/discover.md','utf8'); const f=(re,n)=>{if(!re.test(s))throw new Error('missing '+n)}; if(!/name: discover/.test(s))throw new Error('no name'); if(!/frameworks: \[\"Jobs to Be Done \(JTBD\)\"\]/.test(s))throw new Error('framework mismatch'); const n=(s.match(/commandsForFramework/g)||[]).length; if(n<5)throw new Error('need 5 resolver calls, got '+n); f(/F\.1/,'F.1 gate'); f(/Facilitator/i,'hooked ethics'); f(/MOSDeckEngine/,'deck engine'); f(/The Pyramid Principle/,'pyramid bridge key'); f(/fileEvidenceWithReadback|navigation\.cjs/,'part9 filing'); if(/skills\/hooked-model/.test(s))throw new Error('forbidden hooked-model read_first'); if(/\u2014/.test(s))throw new Error('em-dash'); console.log('OK task2');"</automated>
  </verify>
  <done>commands/discover.md is a complete Larry-led six-movement flow with frontmatter (frameworks Jobs to Be Done (JTBD)), LOCKED voice rules, three-state dual-entry, Tri-Polar note, the per-movement resolver-based reuse map (DISC-03), the F.1 gates (Part 3), the DISC-07 Hooked-two-ways section with the four stages INLINED and the Facilitator ethics check, the DISC-04 synthesis step filing via navigation.cjs, and the DISC-10 plain-language-default + deck-on-request step dispatching MOSDeckEngine via the Pyramid Principle resolver door (no second connector), with zero em-dashes and no skills/hooked-model read_first.</done>
</task>

<task type="auto">
  <name>Task 3: Add the SINGLE discover connector block (FIX 1) and regenerate both registries with a green --check</name>
  <read_first>
    - docs/CONNECTOR-CONTRACT.md sections 2 (the 11 sub-keys), 3 (frozen 5 reach_ids + frozen 3 postures), 5 (Part 8 generic-handles-only), 7 (the delimiter convention)
    - commands/research.md lines 25-38 (the exact shipped connector block shape + the "# --- Phase 143.3 connector frontmatter ---" delimiter inside YAML, above the closing ---)
    - commands/find-bottlenecks.md lines 19-32 (the simpler local-only connector shape: web_scope null, plan_gated false, filing fileEvidenceWithReadback)
    - data/connector-registry.json (the 7 shipped cohort connectors; find-connections holds SENS-01|brain_consult|cross-domain-connect and think-hats holds SENS-05|brain_consult|six-hats - the discover sub_mode MUST differ to avoid a tuple collision)
    - lib/core/sensors/sensor-types.cjs lines 38-52 (REACH_IDS frozen 5 + POSTURE_IDS frozen 3 - confirm brain_consult and push_forward are members)
    - scripts/build-connector-registry.cjs lines 140-213 (the parser - confirm it parses exactly ONE connector: block per file, so discover.md gets exactly one) and lines 397-456 (the four CONN-03 validations: frozen-5 reach, frozen-3 posture, WFL-01 framework-resolves, no duplicate sensor-reach-sub_mode tuple)
    - 143.4-PATTERNS.md block 2 (OPEN-1 enum picks + the web_scope null reconciliation: use null, NOT the string "none")
  </read_first>
  <action>
Inside the commands/discover.md YAML frontmatter, AFTER the pre-existing keys and ABOVE the closing fence, add the delimiter "# --- Phase 143.3 connector frontmatter ---" then EXACTLY ONE connector block (FIX 1: the shipped generator parses one connector: block per file; a second silently overwrites the first, so do NOT add a second).

The single discovery connector (resolving OPEN-1): connects_to_spine of true; sensor_triggers of [SENS-01, SENS-05] (SENS-01 first-material, SENS-05 JTBD set or changed - both exist in the registry's sensor set); reach_id of brain_consult (frozen 5 - discovery is a LOCAL conversation that consults Brain for framework chaining; NEVER a 6th reach); sub_mode of client-product-discovery (a UNIQUE render label so the tuples SENS-01|brain_consult|client-product-discovery and SENS-05|brain_consult|client-product-discovery do not collide with the shipped cross-domain-connect or six-hats tuples); framework of "Jobs to Be Done (JTBD)" (MUST equal the frontmatter frameworks value; resolves via commandsForFramework once the command-registry regenerates because analyze-needs, user-needs, validate, jtbd (after FIX 3), AND discover declare it); posture of push_forward (frozen 3 - discovery OPENS a domain); hierarchy_rank of 7 (below the 6 shipped ranks so it never wins a tie ahead of the shipped reaches); filing of fileEvidenceWithReadback; plan_gated of false; web_scope of null (Canon Part 8 and DISC-06 - discovery content is LOCAL; use the literal null, NOT the string "none"); surface of F.1.

Do NOT declare a DISC-10 plain-language-bridge connector here (FIX 1). DISC-10 dispatches via the EXISTING Feynman surface (/mos:mos-reason + MOSDeckEngine) resolved through commandsForFramework("The Pyramid Principle"); those surfaces get their own connectors in the 144.1 sweep. Do NOT modify the generator parser or the frozen banks.

Regenerate both registries in this single regeneration pass: run node scripts/build-command-registry.cjs (emits data/command-registry.json so both /mos:jtbd via FIX 3 and /mos:discover now appear under "Jobs to Be Done (JTBD)", making commandsForFramework non-empty for WFL-01) THEN node scripts/build-connector-registry.cjs (emits data/connector-registry.json with the single discover connector; the connector generator's WFL-01 check reads the freshly-regenerated command-registry, so the command-registry MUST regenerate first). Then run node scripts/build-connector-registry.cjs --check and confirm it exits 0 (the WARN about methodology commands missing a connector is allowed; a hard FAIL is not).
  </action>
  <acceptance_criteria>
    - node scripts/build-connector-registry.cjs --check exits 0 with "connector-registry: OK" in output
    - `grep -c "/mos:discover" data/connector-registry.json` returns at least 1 (the single discover connector present)
    - `grep -c "client-product-discovery" data/connector-registry.json` returns 1 (unique sub_mode registered, no collision)
    - there is NO plain-language-bridge connector in the registry (FIX 1): `grep -c "plain-language-bridge" data/connector-registry.json` returns 0
    - exactly ONE connector entry has surface "/mos:discover": a node assertion that connectors.filter(surface === "/mos:discover").length === 1 passes
    - a node assertion that data/command-registry.json framework_index["Jobs to Be Done (JTBD)"] includes BOTH "/mos:discover" and "/mos:jtbd" passes (WFL-01 + FIX 3 satisfied)
    - a node assertion over the discover connector entry confirms web_scope strictly equals null, reach_id equals "brain_consult", posture equals "push_forward"
    - a node assertion that commandsForFramework("The Pyramid Principle") returns a non-empty array passes (DISC-10 dispatch-by-resolver is real, FIX 1)
    - `grep -c "# --- Phase 143.3 connector frontmatter ---" commands/discover.md` returns 1 (delimiter present, single block)
  </acceptance_criteria>
  <verify>
    <automated>node scripts/build-command-registry.cjs >/dev/null 2>&1; node scripts/build-connector-registry.cjs >/dev/null 2>&1; node scripts/build-connector-registry.cjs --check; echo "check_exit=$?"; node -e "const c=require('./data/connector-registry.json'); const ds=c.connectors.filter(x=>x.surface==='/mos:discover'); if(ds.length!==1)throw new Error('expected exactly 1 discover connector, got '+ds.length); const e=ds[0]; if(e.web_scope!==null||e.reach_id!=='brain_consult'||e.posture!=='push_forward')throw new Error('bad connector values'); if(JSON.stringify(c).includes('plain-language-bridge'))throw new Error('unexpected bridge connector (FIX 1)'); const r=require('./data/command-registry.json'); const cmds=(r.framework_index||{})['Jobs to Be Done (JTBD)']||[]; if(!cmds.includes('/mos:discover'))throw new Error('discover not resolvable'); if(!cmds.includes('/mos:jtbd'))throw new Error('jtbd not resolvable (FIX 3)'); const {commandsForFramework}=require('./lib/workflow/command-resolver.cjs'); if(commandsForFramework('The Pyramid Principle').length===0)throw new Error('pyramid bridge does not resolve (FIX 1)'); console.log('registry OK');"</automated>
  </verify>
  <done>discover.md carries EXACTLY ONE connector (frozen-bank-legal: brain_consult reach, push_forward posture, web_scope null, sub_mode client-product-discovery), no plain-language-bridge connector, both registries regenerated, Jobs to Be Done (JTBD) resolves BOTH /mos:discover and /mos:jtbd via the command-resolver (WFL-01 + FIX 3 satisfied), The Pyramid Principle resolves non-empty for the DISC-10 dispatch-by-resolver (FIX 1), and --check exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator turn-text to Brain query | The MOST sensitive boundary in the product: the discovery conversation captures a real client's brand soul, product essence, personas, proprietary positioning. None of it may enter a Brain query. |
| discover connector fields to registry or Brain | The connector's framework and web_scope are the only Brain-bound values; both must stay generic handles. |
| DISC-10 Feynman bridge to web, Brain, or deck | The plain-language message and optional deck derive from client content; the dispatch (via the existing Feynman surface) must not egress client strings to web or Brain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.4-01 | Information Disclosure | brain_consult reach fired during a movement | mitigate | The single connector carries ONLY the generic framework handle ("Jobs to Be Done (JTBD)"); discover.md routes movements through commandsForFramework (a framework name, never client content); all artifacts file LOCAL via navigation.cjs fileEvidenceWithReadback (Canon Part 8 + Part 9). |
| T-143.4-02 | Tampering | web_scope mis-set | mitigate | web_scope is null on the discover connector (literal null, not "none"); acceptance test asserts web_scope strictly equals null; the frozen FROZEN_WEB_SCOPES enum rejects an off-enum value at --check. |
| T-143.4-03 | Information Disclosure | DISC-10 deck path | mitigate | The plain-language bridge defaults to a LOCAL message; the deck is dispatched to MOSDeckEngine on request only via the resolver door and operates on LOCAL room artifacts; no client strings to Brain (framework handle only). The DISC-10 path declares NO connector here (rides the Feynman surface's own 144.1 connector). |
| T-143.4-04 | Elevation of Privilege | a 6th reach_id or off-bank posture smuggled in | mitigate | The connector uses frozen-5 reach_id (brain_consult) and frozen-3 posture (push_forward); --check fails on any drift; Plan 03 Part-8 sweep re-asserts. |
| T-143.4-05 | Tampering | a smuggled second connector block on discover.md | mitigate | FIX 1: the shipped generator parses one connector: block per file; acceptance asserts exactly ONE /mos:discover entry and zero plain-language-bridge entries; the generator parser is NOT modified. |
| T-143.4-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (file port + markdown command + JSON regeneration only); no new dependency surface. |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check exits 0 with the single discover connector present.
- The node resolver assertion confirms Jobs to Be Done (JTBD) resolves BOTH /mos:discover and /mos:jtbd, and The Pyramid Principle resolves non-empty.
- Em-dash sweep over commands/discover.md, the two ported skill files, and commands/jtbd.md returns zero.
- Every dispatching movement plus the DISC-10 bridge cites commandsForFramework (at least 5 citations).
- Part 8: web_scope null on the discover connector; all filing via navigation.cjs.
</verification>

<success_criteria>
- The design-source skill is ported into the repo (FIX 2); commands/jtbd.md declares the JTBD framework (FIX 3).
- commands/discover.md is a complete Larry-led six-movement flow with dual-entry, F.1 gates, the Part-7 reuse map, DISC-07 Hooked-two-ways (stages inlined), DISC-04 synthesis, and DISC-10 plain-language-default + deck-on-request dispatched by the resolver door.
- EXACTLY ONE connector joins data/connector-registry.json with a unique tuple; no plain-language-bridge connector; --check green (FIX 1).
- DISC-01, DISC-02, DISC-03, DISC-04, DISC-05, DISC-07, DISC-08, DISC-10 covered.
- Zero em-dashes; Tri-Polar note present.
</success_criteria>

<output>
Create `.planning/phases/143.4-discover-command-client-product-jtbd-onboarding/143.4-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 01)

| Artifact | Type | Notes |
|----------|------|-------|
| `skills/client-discovery-interview/SKILL.md` | ported skill file | repo copy of the design source (FIX 2); becomes a 144.1 connector-sweep target |
| `skills/client-discovery-interview/question-bank.md` | ported skill file | repo copy of the question library + Hooked worksheet + Discovery Brief template (FIX 2) |
| `commands/discover.md` | new command | Larry-led /mos:discover flow + EXACTLY ONE connector block. The registry drift pass MUST exclude it from the "missing connector" hard-fail; it declares a connector. |
| `connector:` block (single, discovery) on discover.md | connector declaration | sensor_triggers [SENS-01, SENS-05], reach_id brain_consult, sub_mode client-product-discovery, framework "Jobs to Be Done (JTBD)", posture push_forward, hierarchy_rank 7, filing fileEvidenceWithReadback, plan_gated false, web_scope null, surface F.1 |
| `commands/jtbd.md` | edited frontmatter | adds frameworks: ["Jobs to Be Done (JTBD)"] (FIX 3) so /mos:jtbd resolves via the resolver door |
| `data/connector-registry.json` | regenerated artifact | now includes the single discover connector; do not hand-edit |
| `data/command-registry.json` | regenerated artifact | Jobs to Be Done (JTBD) now resolves both /mos:discover and /mos:jtbd |
</output>
