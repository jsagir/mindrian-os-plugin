---
phase: 143.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/discover.md
  - data/connector-registry.json
  - data/command-registry.json
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-05, DISC-07, DISC-08, DISC-10]
canon_parts: [Part 2, Part 2a, Part 3, Part 4, Part 6, Part 7, Part 8, Part 9, Part 10]
user_setup: []

must_haves:
  truths:
    - "Running /mos:discover opens a Larry-led six-movement conversation, one question at a time, that adapts order to navigator input"
    - "A cold navigator (no room) and a warm navigator (existing room) both get a graceful entry path"
    - "Each movement names the existing atom it reuses via commandsForFramework and never hardcodes a slug"
    - "Every choice point renders as a Shape F.1 selector, never bare prose"
    - "The discover connector and the DISC-10 plain-language-bridge connector both join data/connector-registry.json and the registry --check exits 0"
    - "The DISC-10 step produces a plain-language message by default and a deck only on request, dispatching MOSDeckEngine, never re-implementing the Feynman pipeline"
  artifacts:
    - path: "commands/discover.md"
      provides: "The /mos:discover Larry-led discovery flow + both connector blocks + synthesis + Feynman bridge steps"
      min_lines: 200
      contains: "connector:"
    - path: "data/connector-registry.json"
      provides: "Regenerated registry that includes the discover and plain-language-bridge connectors"
      contains: "/mos:discover"
  key_links:
    - from: "commands/discover.md"
      to: "lib/workflow/command-resolver.cjs::commandsForFramework"
      via: "each movement routes through the resolver, not a hardcoded slug"
      pattern: "commandsForFramework"
    - from: "commands/discover.md connector framework"
      to: "data/command-registry.json framework_index"
      via: "Jobs to Be Done (JTBD) resolves to a command list of length > 0 once discover.md regenerates"
      pattern: "Jobs to Be Done"
---

<objective>
Author `commands/discover.md`: the first product command authored to ride the 143.3 connector spine. It is a Larry-led, multi-turn, one-question-at-a-time discovery flow that sequences existing /mos: atoms (Canon Part 7 orchestration, NOT a new atom), declares TWO connector blocks (the discovery connector + the DISC-10 plain-language-bridge connector) so it auto-joins `connector-registry.json`, and renders every choice through the Shape F.1 dial (Canon Part 3). It WRAPS `/mos:new-project` and `/mos:onboard` rather than superseding them (OPEN-2).

Purpose: ship the conversational front door that takes a navigator from "I have a client or product" to a scaffolded Data Room plus Discovery Brief, sequencing the methodology atoms the plugin already has.
Output: `commands/discover.md` plus the regenerated `data/connector-registry.json` and `data/command-registry.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.4-discover-command-client-product-jtbd-onboarding/143.4-CONTEXT.md
@.planning/phases/143.4-discover-command-client-product-jtbd-onboarding/143.4-PATTERNS.md
@docs/CONNECTOR-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the discover.md shell, frontmatter, voice rules, and dual-entry mode detection</name>
  <read_first>
    - commands/onboard.md (full) - the multi-step conversational walkthrough analog; copy the LOCKED voice rules block (lines 23-32: no emoji, signature openers, Ask-Tell dial 0.15 ask-heavy, 12-glyph vocabulary, 3-line error pattern) verbatim; copy the Mode Detection pattern (lines 54-60)
    - commands/new-project.md lines 24-48 (Step 1 three-state registry check = the exact Mode A / Mode B / Tier 0 dual-entry shape DISC-08 needs) and lines 1-16 (frontmatter shape)
    - commands/research.md lines 1-38 (the shipped frontmatter + Phase 122 workflow-layer block + Phase 143.3 connector block layout and delimiter)
    - 143.4-PATTERNS.md block 1 (frontmatter shape to copy) and block 2 (delimiter convention)
  </read_first>
  <action>
Create `commands/discover.md` (this file is owned solely by this plan). Author the YAML frontmatter with these exact keys: name: discover; description: a verb-first under-promise (for example "Discover a client, product, and its users before any build"); help_jtbd; body_shape: B (Semantic Tree); serves_jtbd of ["explore"]; teaching in the "When you ___, /mos:discover does ___" form (CLAUDE.md Feynman-JTBD rule); interactive_first_reward: reframe_question; ui_reference: skills/ui-system/SKILL.md. Then the Phase 122 workflow-layer block: kind: methodology; frameworks: ["Jobs to Be Done (JTBD)"] (this MUST equal the discovery connector framework so the resolver join works); allowed-tools of [Read, Write, Bash, Glob, WebFetch].

Write the LOCKED voice-rules block copied verbatim from onboard.md lines 23-32 (no emoji, signature openers "Very simply..." and "Here's the thing...", Ask-Tell dial starts at 0.15 ask-heavy during discovery, the 12-glyph symbol vocabulary, the 3-line error pattern What / Why / Fix). House rule: hyphens only, NO em-dashes anywhere.

Write the dual-entry Mode Detection section (DISC-08) mirroring new-project.md Step 1's three states: COLD (no ~/MindrianRooms registry and no legacy room/ so scaffold a discovery room from scratch); WARM (registry exists so enrich the active or named room); Tier 0 graceful (brain unreachable or empty STATE.md so still run the conversation, file locally, no RECOMMENDED markers per Canon Part 3 Mode B). Branch on the command argument first (a client or product name is a warm-target hint).

Add the Tri-Polar note (DISC-09 and CLAUDE.md): /mos:discover is a conversational command so it works on CLI, Desktop, and Cowork; note the surface differences (CLI files via Bash plus hooks; Desktop is Larry-conversational; Cowork writes the room into shared 00_Context state).
  </action>
  <acceptance_criteria>
    - `test -f commands/discover.md` succeeds
    - `grep -c "name: discover" commands/discover.md` returns at least 1
    - the command frontmatter declares frameworks of exactly ["Jobs to Be Done (JTBD)"] (one line)
    - `grep -c "interactive_first_reward: reframe_question" commands/discover.md` returns 1
    - `grep -E "Mode A|Mode B|Tier 0|cold|warm|COLD|WARM" commands/discover.md` returns at least 3 lines (dual-entry present)
    - em-dash sweep: a node check for the unicode em-dash character (codepoint 2014) over commands/discover.md returns zero matches
    - `grep -ci "CLI" commands/discover.md` and `grep -ci "Desktop" commands/discover.md` each return at least 1 (Tri-Polar note present)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('commands/discover.md','utf8'); if(!/name: discover/.test(s)) throw new Error('no name'); if(!/frameworks: \[\"Jobs to Be Done \(JTBD\)\"\]/.test(s)) throw new Error('framework mismatch'); if(/\u2014/.test(s)) throw new Error('em-dash found'); console.log('OK task1');"</automated>
  </verify>
  <done>commands/discover.md exists with valid frontmatter declaring frameworks of ["Jobs to Be Done (JTBD)"], the LOCKED voice rules, the three-state dual-entry, the Tri-Polar note, and zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: Author the six-movement investigation body plus the Part-7 reuse map plus F.1 gates plus DISC-07 Hooked plus DISC-10 Feynman bridge synthesis steps</name>
  <read_first>
    - skills/client-discovery-interview/SKILL.md (the six movements, the investigator method, Hooked-twice principle, the Discovery Brief section list, the common mistakes)
    - skills/client-discovery-interview/question-bank.md lines 85-138 (the Hooked worksheet ethics check + the Discovery Brief template + line 38 "describe to a smart twelve-year-old")
    - commands/new-project.md lines 54-89 (the ONE-question-at-a-time deep-exploration discipline) and lines 85-89 (summarize-before-acting + wait-for-confirmation)
    - commands/onboard.md lines 348-367 (the closing Shape F.1 Next Move selector via AskUserQuestion that writes STATE.md Decisions + a typed CHOSE edge - copy this surface for every choice point)
    - 143.4-PATTERNS.md block 3 (the per-movement reuse table with the exact framework to resolver-key mapping) and block 5 (the DISC-10 Feynman bridge: MOSDeckEngine canonical, mos-reason for the pyramid, plain-language default + deck on request)
    - skills/MOSDeckEngine/SKILL.md lines 1-30 (the 6-stage Feynman pipeline this step dispatches, never re-implements)
  </read_first>
  <action>
Append the six-movement investigation to commands/discover.md. The movements (from SKILL.md): (1) Warm-up and the real why; (2) The brand soul (archetype + voice + the "never says" list); (3) The product essence (one core job + business case + hard constraints); (4) Users and JTBD (functional, emotional, social job + what they fire + the trigger); (5) Competition and whitespace; (6) The product as a Hooked loop. State explicitly: ONE question at a time, follow the navigator's energy, push back on vague answers, mirror-and-confirm, adapt the order to what the navigator brings (DISC-02).

For EACH movement write the Part-7 reuse-map justification line (DISC-03) naming the existing atom and the resolver call, in this exact mapping: movement 1 reuses /mos:beautiful-question via commandsForFramework("Beautiful Question Framework"); movement 2 reuses /mos:persona and /mos:think-hats via commandsForFramework("Six Thinking Hats"); movement 3 reuses /mos:new-project scaffold and /mos:lean-canvas via commandsForFramework("Lean Canvas"); movement 4 reuses /mos:analyze-needs, /mos:user-needs, and /mos:jtbd via commandsForFramework("Jobs to Be Done (JTBD)"); movement 5 reuses /mos:compare-ventures and /mos:whitespace via commandsForFramework on their declared frameworks; movement 6 cross-references the hooked-model skill (rendered into the brief, not dispatched). Each justification reads "reuses <command> via commandsForFramework(<framework>) because a thin orchestration of the shipped atom beats a new atom (Canon Part 7); net-new is the SEQUENCING, not the methodology." NEVER write a bare /mos: slug as the dispatch contract - the resolver door is the contract; a framework that resolves to an empty list degrades to "run it manually" (CONNECTOR-CONTRACT.md section 4).

Render EVERY choice point (movement transition, the post-synthesis "what next", the deck-on-request gate) as a Shape F.1 selector via AskUserQuestion - copy the onboard.md Step 6 surface. The selection writes to STATE.md Decisions plus a typed (navigator)-[CHOSE {verb,reason}]->(artifact) edge. RECOMMENDED marker ONLY at Brain confidence at or above 0.7 (Phase 88.2 invariant); in Mode B and Tier 0 no RECOMMENDED marker. NO bare-prose recommendations (the Cluster 5 audit violation).

Add the DISC-07 Hooked-two-ways section: (a) the brief's habit-loop section design (Trigger external + internal emotion, then Action with Fogg B=MAP, then Variable Reward of tribe/hunt/self, then Investment that loads the next trigger) WITH the Facilitator-quadrant ethics check (does the loop improve the user's life; would the maker use it); (b) the command's OWN engagement loop where the brief taking shape is the visible investment hook that keeps the navigator generous with the truth. Cross-reference the hooked-model skill; do NOT duplicate it.

Add the DISC-04 synthesis step (prose only here; the brief template file and scaffold json are authored in Plan 02 and referenced by path): after the conversation, write the Discovery Brief using templates/discovery/discovery-brief-template.md (Brand soul / Product essence / Personas+JTBD cards / Positioning / Habit loop / Plain-language message / Design implications / Open-questions+assumptions); scaffold the room sections from skills/client-discovery-interview/discovery-scaffold.json with a ROOM.md per section (ICM Layer 0); file personas as room/team/ai-personas/* with an embedded JTBD card (OPEN-4); mark every guess as review_status: proposed (Canon Part 5 + Part 9). File ALL artifacts through lib/core/navigation.cjs fileEvidenceWithReadback - never folder-scan (Canon Part 9). Obsidian nested rule (CLAUDE.md decision 16): section/artifact-name/artifact-name.md.

Add the DISC-10 Feynman-bridge step: after synthesis, run the plain-language bridge over product-essence and brand-soul. Default output is the plain-language message (the "explain to a smart twelve-year-old" one-liner from question-bank.md line 38, sharpened, plus the value-prop ladder) written into the brief's Plain-language message section and seeded into the room's per-section FEYNMAN.md via the shipped lib/core/folder-memory.cjs writer (never hand-write FEYNMAN.md). Deck ON REQUEST only (OPEN-5): an F.1 gate offers "build the deck" which dispatches the MOSDeckEngine skill; /mos:mos-reason supplies the reasoning pyramid (The Pyramid Principle). Do NOT re-implement the 6-stage pipeline.

The command may use the established node ${CLAUDE_PLUGIN_ROOT}/... invocation idiom for cjs calls; do not paste full command-file bodies.
  </action>
  <acceptance_criteria>
    - `grep -c "commandsForFramework" commands/discover.md` returns at least 5 (one resolver call cited per dispatching movement)
    - `grep -E "Beautiful Question Framework|Six Thinking Hats|Lean Canvas|Jobs to Be Done" commands/discover.md` returns at least 4 lines (the four resolver framework keys present)
    - `grep -ci "one question at a time" commands/discover.md` returns at least 1
    - `grep -c "F.1" commands/discover.md` returns at least 2 (F.1 gate at choice points)
    - `grep -ci "AskUserQuestion" commands/discover.md` returns at least 1
    - `grep -ci "Facilitator" commands/discover.md` returns at least 1 (Hooked ethics check, DISC-07)
    - `grep -ci "twelve-year-old"` or `grep -ci "plain-language"` over commands/discover.md returns at least 1 (DISC-10 plain-language default)
    - `grep -ci "MOSDeckEngine" commands/discover.md` returns at least 1 and a grep for "on request" returns at least 1 (deck on request, OPEN-5)
    - `grep -ci "fileEvidenceWithReadback"` or `grep -ci "navigation.cjs"` over commands/discover.md returns at least 1 (Part 9 filing chokepoint)
    - `grep -ci "proposed" commands/discover.md` returns at least 1 (Part 5 + Part 9 assumption flagging)
    - em-dash sweep over commands/discover.md returns zero matches
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('commands/discover.md','utf8'); const f=(re,n)=>{if(!re.test(s))throw new Error('missing '+n)}; const n=(s.match(/commandsForFramework/g)||[]).length; if(n<5)throw new Error('need 5 resolver calls, got '+n); f(/F\.1/,'F.1 gate'); f(/Facilitator/i,'hooked ethics'); f(/MOSDeckEngine/,'deck engine'); f(/fileEvidenceWithReadback|navigation\.cjs/,'part9 filing'); if(/\u2014/.test(s))throw new Error('em-dash'); console.log('OK task2');"</automated>
  </verify>
  <done>The six movements, the per-movement resolver-based reuse justifications (DISC-03), the F.1 gates (Part 3), the DISC-07 Hooked-two-ways section, the DISC-04 synthesis step, and the DISC-10 plain-language-default-deck-on-request step are all present, all filing through navigation.cjs, with zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 3: Add both connector blocks (discovery + plain-language-bridge) and regenerate the registries with a green --check</name>
  <read_first>
    - docs/CONNECTOR-CONTRACT.md sections 2 (the 11 sub-keys), 3 (frozen 5 reach_ids + frozen 3 postures), 5 (Part 8 generic-handles-only), 7 (the delimiter convention)
    - commands/research.md lines 25-38 (the exact shipped connector block shape + the "# --- Phase 143.3 connector frontmatter ---" delimiter inside YAML, above the closing ---)
    - data/connector-registry.json (the 7 shipped cohort connectors; note find-connections holds SENS-01 brain_consult cross-domain-connect and think-hats holds SENS-05 brain_consult six-hats - the discover sub_mode MUST differ to avoid a tuple collision)
    - lib/core/sensors/sensor-types.cjs lines 38-52 (REACH_IDS frozen 5 + POSTURE_IDS frozen 3 - confirm brain_consult and push_forward are members)
    - scripts/build-connector-registry.cjs lines 420-456 (the WFL-01 framework-resolves validation + the duplicate sensor-reach-sub_mode tuple check)
    - 143.4-PATTERNS.md block 2 (OPEN-1 enum picks + the web_scope null reconciliation: use null, NOT the string "none")
  </read_first>
  <action>
Inside the commands/discover.md YAML frontmatter, AFTER the pre-existing keys and ABOVE the closing fence, add the delimiter "# --- Phase 143.3 connector frontmatter ---" then the connector declarations.

Connector 1 (the discovery connector, resolving OPEN-1): connects_to_spine of true; sensor_triggers of [SENS-01, SENS-05] (SENS-01 first-material, SENS-05 JTBD set or changed - both exist in the registry's sensor set); reach_id of brain_consult (frozen 5 - discovery is a LOCAL conversation that consults Brain for framework chaining; NEVER a 6th reach); sub_mode of client-product-discovery (a UNIQUE render label so the tuples SENS-01 brain_consult client-product-discovery and SENS-05 brain_consult client-product-discovery do not collide with the shipped cross-domain-connect or six-hats tuples); framework of "Jobs to Be Done (JTBD)" (MUST equal the frameworks value; resolves via commandsForFramework once the command-registry regenerates because analyze-needs, user-needs, validate, AND now discover declare it); posture of push_forward (frozen 3 - discovery OPENS a domain); hierarchy_rank of 7 (below the 6 shipped ranks so it never wins a tie ahead of the shipped reaches); filing of fileEvidenceWithReadback; plan_gated of false; web_scope of null (Canon Part 8 and DISC-06 - discovery content is LOCAL; use the literal null, NOT the string "none"); surface of F.1.

Connector 2 (the DISC-10 plain-language-bridge connector, resolving OPEN-5): connects_to_spine of true; sensor_triggers of [SENS-05] (re-run on product-essence change); reach_id of brain_consult; sub_mode of plain-language-bridge (unique); framework of "The Pyramid Principle" (resolves via commandsForFramework to /mos:mos-reason and /mos:structure-argument); posture of hold (it consolidates the discovered truth into a message); hierarchy_rank of 8; filing of fileEvidenceWithReadback; plan_gated of false; web_scope of null; surface of F.1.

Generator shape note: first read scripts/build-connector-registry.cjs to confirm whether it parses a single connector key per file or a list/multi-block form. If the generator supports only one connector per surface, declare the discovery connector as the file's connector block and register the plain-language-bridge connector through whatever mechanism the SHIPPED generator already supports (a list form, a second delimited block the parser reads, or a sibling surface entry) - do NOT modify the generator parser or the frozen banks in this plan. Record the exact mechanism used in the SUMMARY. The Plan 03 acceptance test asserts BOTH /mos:discover and the plain-language-bridge sub_mode appear in the registry; satisfy that with the shipped generator's supported form.

Regenerate both registries: run node scripts/build-connector-registry.cjs (emits data/connector-registry.json) and node scripts/build-command-registry.cjs (emits data/command-registry.json so Jobs to Be Done (JTBD) now lists /mos:discover, making commandsForFramework non-empty for WFL-01). Then run node scripts/build-connector-registry.cjs --check and confirm it exits 0 (the WARN about methodology commands missing a connector is allowed; a hard FAIL is not).
  </action>
  <acceptance_criteria>
    - node scripts/build-connector-registry.cjs --check exits 0 with "connector-registry: OK" in output
    - `grep -c "/mos:discover" data/connector-registry.json` returns at least 1 (discover connector present)
    - `grep -c "client-product-discovery" data/connector-registry.json` returns 1 (unique sub_mode registered, no collision)
    - `grep -c "plain-language-bridge" data/connector-registry.json` returns 1 (the DISC-10 bridge connector registered)
    - a node assertion that data/command-registry.json framework_index["Jobs to Be Done (JTBD)"] includes "/mos:discover" passes (WFL-01 framework resolution satisfied)
    - a node assertion over the discover connector entry confirms web_scope strictly equals null, reach_id equals "brain_consult", posture equals "push_forward"
    - `grep -c "# --- Phase 143.3 connector frontmatter ---" commands/discover.md` returns at least 1 (delimiter present)
  </acceptance_criteria>
  <verify>
    <automated>node scripts/build-connector-registry.cjs >/dev/null 2>&1; node scripts/build-command-registry.cjs >/dev/null 2>&1; node scripts/build-connector-registry.cjs --check; echo "check_exit=$?"; node -e "const c=require('./data/connector-registry.json'); const e=c.connectors.find(x=>x.surface==='/mos:discover'); if(!e) throw new Error('no discover'); if(e.web_scope!==null||e.reach_id!=='brain_consult'||e.posture!=='push_forward') throw new Error('bad connector values'); if(!JSON.stringify(c).includes('plain-language-bridge')) throw new Error('no bridge'); const r=require('./data/command-registry.json'); const cmds=(r.framework_index||{})['Jobs to Be Done (JTBD)']||[]; if(!cmds.includes('/mos:discover')) throw new Error('discover not resolvable'); console.log('registry OK');"</automated>
  </verify>
  <done>Both connectors are declared on discover.md with frozen-bank-legal values (brain_consult reach, push_forward and hold postures, web_scope null), both registries are regenerated, the discover and plain-language-bridge sub_modes appear in connector-registry.json with no tuple collision, Jobs to Be Done (JTBD) resolves /mos:discover via the command-resolver (WFL-01 satisfied), and --check exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator turn-text to Brain query | The MOST sensitive boundary in the product: the discovery conversation captures a real client's brand soul, product essence, personas, proprietary positioning. None of it may enter a Brain query. |
| discover connector fields to registry or Brain | The connector's framework and web_scope are the only Brain-bound values; both must stay generic handles. |
| DISC-10 Feynman bridge to web, Brain, or deck | The plain-language message and optional deck derive from client content; the dispatch must not egress client strings to web or Brain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.4-01 | Information Disclosure | brain_consult reach fired during a movement | mitigate | The connector carries ONLY the generic framework handle ("Jobs to Be Done (JTBD)"); discover.md routes movements through commandsForFramework (a framework name, never client content); all artifacts file LOCAL via navigation.cjs fileEvidenceWithReadback (Canon Part 8 + Part 9). |
| T-143.4-02 | Tampering | web_scope mis-set | mitigate | web_scope is null on BOTH connectors (literal null, not "none"); acceptance test asserts web_scope strictly equals null; the frozen FROZEN_WEB_SCOPES enum rejects an off-enum value at --check. |
| T-143.4-03 | Information Disclosure | DISC-10 deck path | mitigate | The plain-language bridge defaults to a LOCAL message; the deck is dispatched to MOSDeckEngine on request only and operates on LOCAL room artifacts; bridge connector web_scope is null; no client strings to Brain (framework handle only). |
| T-143.4-04 | Elevation of Privilege | a 6th reach_id or off-bank posture smuggled in | mitigate | Both connectors use frozen-5 reach_id (brain_consult) and frozen-3 posture; --check fails on any drift; Plan 03 Part-8 sweep re-asserts. |
| T-143.4-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (markdown command + JSON regeneration only); no new dependency surface. |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check exits 0 with both connectors present.
- The node resolver assertion confirms Jobs to Be Done (JTBD) resolves /mos:discover.
- Em-dash sweep over commands/discover.md returns zero.
- Every dispatching movement cites commandsForFramework (at least 5 citations).
- Part 8: web_scope null on both connectors; all filing via navigation.cjs.
</verification>

<success_criteria>
- commands/discover.md is a complete Larry-led six-movement flow with dual-entry, F.1 gates, the Part-7 reuse map, DISC-07 Hooked-two-ways, DISC-04 synthesis, and DISC-10 plain-language-default + deck-on-request.
- Both connectors join data/connector-registry.json with unique tuples; --check green.
- DISC-01, DISC-02, DISC-03, DISC-05, DISC-07, DISC-08, DISC-10 covered.
- Zero em-dashes; Tri-Polar note present.
</success_criteria>

<output>
Create `.planning/phases/143.4-discover-command-client-product-jtbd-onboarding/143.4-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 01)

| Artifact | Type | Notes |
|----------|------|-------|
| `commands/discover.md` | new command | Larry-led /mos:discover flow + both connector blocks. The registry drift pass MUST exclude this from the "missing connector" hard-fail; it declares connectors. |
| `connector:` block (discovery) on discover.md | connector declaration | sensor_triggers [SENS-01, SENS-05], reach_id brain_consult, sub_mode client-product-discovery, framework "Jobs to Be Done (JTBD)", posture push_forward, hierarchy_rank 7, filing fileEvidenceWithReadback, plan_gated false, web_scope null, surface F.1 |
| `connector:` block (plain-language-bridge) on discover.md | connector declaration | sensor_triggers [SENS-05], reach_id brain_consult, sub_mode plain-language-bridge, framework "The Pyramid Principle", posture hold, hierarchy_rank 8, filing fileEvidenceWithReadback, plan_gated false, web_scope null, surface F.1 |
| `data/connector-registry.json` | regenerated artifact | now includes both discover connectors; do not hand-edit |
| `data/command-registry.json` | regenerated artifact | Jobs to Be Done (JTBD) now resolves /mos:discover |
