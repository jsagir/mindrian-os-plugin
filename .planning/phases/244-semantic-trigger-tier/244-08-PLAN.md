---
phase: 244-semantic-trigger-tier
plan: 08
type: execute
wave: 4
depends_on: [244-01, 244-02, 244-03, 244-04, 244-05, 244-06, 244-07]
files_modified:
  - docs/ENV-TUNING.md
  - docs/CANON-PHASE-MAP.md
  - .planning/phases/244-semantic-trigger-tier/244-RESIDUALS.md
autonomous: true
requirements: [TRIG-01, TRIG-02, TRIG-03]
canon_parts: [3, 6, 7, 8, 9, 11]

must_haves:
  truths:
    - "Every env var this phase introduced is documented where users and future maintainers look for it"
    - "Every open question, assumption and deliberate non-goal this phase leaves behind is written down in one place"
    - "The phase gate has been run end to end and its result is recorded, not assumed"
    - "The reasoning trail is mirrored to the rethinking-mindrianos room per the Dev-Research Compositing mandate"
  artifacts:
    - path: "docs/ENV-TUNING.md"
      provides: "A Semantic Trigger Tier section covering all five new env vars"
      contains: "TRIG_RRF_K"
    - path: ".planning/phases/244-semantic-trigger-tier/244-RESIDUALS.md"
      provides: "The residual register: open items, assumptions, non-goals, and navigator asks"
    - path: "docs/CANON-PHASE-MAP.md"
      provides: "Phase 244's canon_parts ledger entry"
  key_links:
    - from: "docs/ENV-TUNING.md"
      to: "lib/workflow/f-selector-ranker.cjs and lib/core/sensors/sensor-content-relevance.cjs"
      via: "documented default values matching the constants in source"
      pattern: "TRIG_"
---

<objective>
Close the phase: document what shipped, write down what did not, run the gate, and honor the compositing mandate.

This plan writes ZERO production code. That is the point, not a shortfall. Phase 240's closing plan set the convention this milestone follows, and the things it covers are exactly the things that rot when skipped: five new environment variables with no documentation, six research assumptions with no register, one navigator ask (the SC3 lambda inversion) with nowhere to land, one declared research gap (the langtalks consult that was never reachable) that would otherwise silently become a non-finding, and a phase gate that everyone assumes was run.

Purpose: a phase is not closed because its code landed. It is closed when the next person can find out what it did, what it deliberately did not do, and what is still owed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/244-semantic-trigger-tier/244-RESEARCH.md
@.planning/phases/244-semantic-trigger-tier/244-01-SUMMARY.md
@.planning/phases/244-semantic-trigger-tier/244-02-SUMMARY.md
@.planning/phases/244-semantic-trigger-tier/244-03-SUMMARY.md
@.planning/phases/244-semantic-trigger-tier/244-04-SUMMARY.md
@.planning/phases/244-semantic-trigger-tier/244-05-SUMMARY.md
@.planning/phases/244-semantic-trigger-tier/244-06-SUMMARY.md
@.planning/phases/244-semantic-trigger-tier/244-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the five new environment variables</name>
  <files>docs/ENV-TUNING.md</files>
  <read_first>
    - docs/ENV-TUNING.md IN FULL, especially the "Eureka Embedding Spine" section at `:36` and the "Reach Hedge Ranker (Phase 222, room-local, zero egress)" section at `:104`, which is the closest analog in both subject and register, plus the "Usage in settings.json" section at `:292` and the "Source" section at `:315` to learn what the file expects a new section to provide
    - lib/workflow/f-selector-ranker.cjs (`TRIG_RRF_K`, `TRIG_MMR_LAMBDA` and their resolvers and defaults, as landed by Plans 04 and 07)
    - lib/core/sensors/sensor-content-relevance.cjs (`TRIG_CONTENT_MIN_HITS`, `TRIG_CONTENT_MIN_COVERAGE`, as landed by Plan 05)
    - lib/core/eureka/fts-index-lifecycle.cjs (`MOS_NO_DETACHED_FTS_BUILD`, as landed by Plan 02)
  </read_first>
  <action>
Add ONE new section to `docs/ENV-TUNING.md`, titled for the Semantic Trigger Tier and naming Phase 244, placed after the Phase 222 Reach Hedge Ranker section since the two are neighbours in subject. Match that section's register: room-local, zero egress, per-variable table rows with the default, the valid range, the clamping behavior, and one sentence on what moving the knob actually does to what the user sees.

Cover all five:
- `TRIG_RRF_K`, default 25, positive finite, invalid values clamp back to 25. Say WHY 25 and not the textbook 60: this repo's own 2026-07-04 small-corpus validation prescribed k in 20-30 (`hybrid-retrieve.cjs:8-13`) because small corpora want less top-rank dampening, and with `MAX_K=3` the dial's list is 3 to 6 items. Say honestly what it does NOT do: at k=25 the rank-1-versus-rank-2 gap is about 3.8 percent, small relative to the frozen 0.15 margin, so this knob rarely flips the detent on its own.
- `TRIG_MMR_LAMBDA`, default 0.7, clamped to the closed interval 0..1. State the orientation explicitly and in the user's terms: this is the RELEVANCE weight, so 1.0 is pure relevance and 0.0 is pure diversity. Warn that the ROADMAP's original SC3 wording states the formula inverted and that the implementation follows the canonical Carbonell orientation, so this knob means what its name says.
- `TRIG_CONTENT_MIN_HITS`, with the default read from source.
- `TRIG_CONTENT_MIN_COVERAGE`, with the default read from source, plus one sentence on why the floor is token coverage rather than an absolute bm25 threshold: bm25 is not comparable across queries because it varies with query term count and corpus statistics.
- `MOS_NO_DETACHED_FTS_BUILD`, documented as a TEST SEAM, not a user knob, matching how the file treats other seams. Setting it to 1 suppresses the detached index-build spawn so a test owns the timing.

Read every default from the source at write time. Do NOT copy the numbers from this plan; if a plan and the source disagree, the source is truth and the disagreement is a finding for the residual register.

Verify against the file's own conventions: if it maintains a variable index, a settings.json example block or a source list, update each one. Do not add a section that the file's existing structure does not reach.
  </action>
  <verify>
    <automated>node -e "const d=require('fs').readFileSync('docs/ENV-TUNING.md','utf8'); ['TRIG_RRF_K','TRIG_MMR_LAMBDA','TRIG_CONTENT_MIN_HITS','TRIG_CONTENT_MIN_COVERAGE','MOS_NO_DETACHED_FTS_BUILD'].forEach(v=>{if(d.indexOf(v)===-1) throw new Error('undocumented: '+v)}); console.log('all five documented')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `all five documented`.
    - DEFAULT-PARITY CHECK, executed and transcribed: for each of the four numeric variables, read the default from source with a `node -e` require and compare it to the number written in the doc. Any mismatch is corrected in the DOC (source is truth) and recorded in the residual register.
    - `grep -c "TRIG_" docs/ENV-TUNING.md` counted over the comment-free body is at least 4.
    - `grep -lP '\x{2014}' docs/ENV-TUNING.md` returns nothing.
  </acceptance_criteria>
  <done>All five variables are documented with source-verified defaults, the lambda orientation is stated in the user's terms, and the RRF knob's honest limitation is written down rather than oversold.</done>
</task>

<task type="auto">
  <name>Task 2: Write the residual register and record the canon ledger entry</name>
  <files>.planning/phases/244-semantic-trigger-tier/244-RESIDUALS.md, docs/CANON-PHASE-MAP.md</files>
  <read_first>
    - All seven prior SUMMARY files in this phase directory, in full
    - .planning/phases/244-semantic-trigger-tier/244-RESEARCH.md, the "Assumptions Log" (A1 through A8), the "Open Questions" list (all six), the "Grounding Consultation Record" (the two unreachable MCP sources), and Finding F-10
    - docs/CANON-PHASE-MAP.md, to learn the ledger's actual row format before adding a row (read it; do not assume the shape)
    - .planning/phases/240-memory/ for the closing-plan residual-register precedent this milestone established, if such a file exists there
  </read_first>
  <action>
Create `.planning/phases/244-semantic-trigger-tier/244-RESIDUALS.md` with these sections.

SECTION 1, what shipped. One table row per requirement (TRIG-01, TRIG-02, TRIG-03) naming the plans that closed it and the specific artifacts. Keep it to what is verifiable from the SUMMARYs.

SECTION 2, navigator asks. Two items, each with the exact wording change proposed:
- ROADMAP SC3 states the MMR formula as `(1-lambda)*relevance - lambda*max_similarity_to_selected`, which is algebraically equivalent to the canonical Carbonell form but inverts the knob's semantics. The implementation follows canonical with `MMR_LAMBDA_RELEVANCE`. SC3 needs a one-line amendment so the document and the code agree.
- 244-RESEARCH.md Finding F-10 was already applied to the ROADMAP's exclusion rationale (latency budget, not architecture). Confirm the applied text is still present and consistent with what shipped, and flag it if any plan text reintroduced the falsified architecture rationale.

SECTION 3, assumptions carried forward. Transcribe A1 through A8 from the research and mark each one CONFIRMED, STILL OPEN, or SUPERSEDED with a one-line reason drawn from what actually happened during execution. A5 in particular (the coverage floor was measured on ONE room) must be marked STILL OPEN with an explicit ask: validate the floor against a second room before treating it as settled.

SECTION 4, the declared grounding gap. 244-RESEARCH.md's own Grounding Consultation Record states that both `langtalks-graph-expert` and Context7 MCP tools were stripped from the researching agent, that the Context7 gap was compensated with something stronger (the official SQLite FTS5 spec plus live execution), and that the langtalks gap is a REAL, OPEN gap: a consult on content-tier trigger design and on when lexical-only retrieval suffices versus hybrid is still owed. The research says explicitly, "do not treat the langtalks gap as closed. It is an open item, not a non-finding." Carry that forward verbatim in substance. If the executing agent HAS langtalks MCP tools available, run the consult now (`relationship_path` for point-to-point questions per the standing rule, `query_relationship` only for open-ended breadth), record the outcome including a "not in the corpus yet" answer if that is what comes back, and mark the item closed. If the tools are unavailable, say so plainly and leave it open. Do not paper over it either way.

SECTION 5, deliberate non-goals. State each with its reason and its evidence, so a future reader does not "fix" them:
- `fragments` is never indexed. Measured: with `fragments` in the corpus, "what is the weather in paris today" produced 3 confident hits; without it, that turn and two other irrelevant turns produced 0 while two relevant turns produced 5 each.
- No vector or embedding leg on the trigger path. The reason is LATENCY BUDGET, not architecture: `sqlite-vec` and `@huggingface/transformers` are already pinned dependencies and `vector-store.cjs` already runs a local embedding store, so an architecture-based rationale would be falsifiable against this repo's own `package.json`.
- No new FTS table, no second RRF implementation, no new npm dependency, no KuzuDB, Memgraph or Neo4j.
- `classifyTriggerTier` was deliberately not made to return `'content'`; the tier reaches evidence through the sensor's own bag.
- `eureka_fts` was NOT converted to an external-content table, with the three reasons from research Q2 Option B.

SECTION 6, known residual risks. At minimum: the `clearIndexerOwnedRows` third-call-site risk (research A7) with whatever disposition Plan 03 recorded; the cold-turn cost of lazy build-on-first-miss; and the fact that `rankForSelector` has exactly one live production caller supplying tier candidates, so a second consumer would need its own wiring.

SECTION 7, the phase gate result. Fill this in AFTER Task 3 runs, with the actual observed output.

Then add Phase 244's row to `docs/CANON-PHASE-MAP.md` using that file's real format, declaring `canon_parts` 3, 6, 7, 8, 9 and 11 with one clause each on how this phase touched that Part. If the file's format differs from a simple row, follow the format that is actually there.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.planning/phases/244-semantic-trigger-tier/244-RESIDUALS.md','utf8'); ['A1','A5','A7','langtalks','fragments','SC3','F-10'].forEach(k=>{if(f.indexOf(k)===-1) throw new Error('residual register missing: '+k)}); if(f.indexOf('TRIG-01')===-1||f.indexOf('TRIG-02')===-1||f.indexOf('TRIG-03')===-1) throw new Error('requirement roll-up incomplete'); console.log('residual register complete')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `residual register complete`.
    - All eight research assumptions A1 through A8 appear with a disposition and a reason.
    - The langtalks item states plainly whether the consult was run this session, and if it was, what came back including a corpus miss.
    - `docs/CANON-PHASE-MAP.md` carries a Phase 244 entry in the file's own format.
    - `grep -lP '\x{2014}' .planning/phases/244-semantic-trigger-tier/244-RESIDUALS.md docs/CANON-PHASE-MAP.md` returns nothing.
  </acceptance_criteria>
  <done>Every assumption, open question, navigator ask, non-goal and residual risk is written in one place with a disposition, and the canon ledger records what this phase touched.</done>
</task>

<task type="auto">
  <name>Task 3: Run the phase gate and mirror the reasoning trail</name>
  <files>.planning/phases/244-semantic-trigger-tier/244-RESIDUALS.md</files>
  <read_first>
    - CLAUDE.md, the Verification block (the named suites and gates) and the Dev-Research Compositing section (the two-homes rule and what each home is for)
    - .planning/phases/244-semantic-trigger-tier/244-RESEARCH.md, "Sampling Rate" under Validation Architecture, which names the phase-gate command set
    - all seven prior SUMMARY files, for the baselines each plan captured
  </read_first>
  <action>
Run the full phase gate and record every observed result verbatim into Section 7 of the residual register. Do not summarize a result you did not observe.

The gate, in order:
- `bash tests/run-all-244.sh` and record PASS/FAIL/SKIP.
- `bash tests/run-all-219.sh` (this phase touches the FTS surface Phase 219 owns) and compare against the Plan 01 baseline.
- `bash tests/run-all-236.sh` (this phase touches the transaction Phase 236 owns) and compare against the Plan 03 baseline.
- `bash tests/run-all-205.sh` and `node lib/memory/f-selector-ranker.test.cjs` and compare against the Plan 07 baselines.
- `node lib/memory/run-feynman-tests.cjs`.
- `node scripts/build-connector-registry.cjs --check`.
- `node scripts/build-orchestration-projection.cjs --check`.
- `node scripts/check-render-coverage.cjs`.
- `node scripts/check-shape-declaration.cjs`.
- `node scripts/check-substrate.cjs --diff`.
- `node scripts/doctor.cjs --acceptance`.
- A repo-wide no-em-dash sweep over every file this phase touched.

Where a suite has a KNOWN pre-existing failure unrelated to this phase, confirm it against a clean pre-244 checkout before attributing it, exactly as Phase 240 did with `run-all-127.3.sh`. Record the confirmation method, not just the conclusion.

Then honor the Dev-Research Compositing mandate. This phase is MindrianOS's own architecture work, so its reasoning trail files in TWO homes, cross-linked: the phase directory here (the executable decisions, already written) and `~/MindrianRooms/rethinking-mindrianos/research/<dated-entry>/` (the durable reasoning trail). Mirror `244-RESEARCH.md` and `244-RESIDUALS.md` into a dated entry there, with a short index note stating what the phase found and cross-referencing back to `.planning/phases/244-semantic-trigger-tier/`. The room entry must reach a verdict and name the phase it informed; a research entry that never lands in a phase is as incomplete as a phase that cites the room without the evidence.

Finally, hand-edit the tracking files. Do NOT call the `gsd-tools.cjs` write verbs: `state.record-session` has a reproduced corruption bug in this repo's current STATE.md shape (it overwrites `stopped_at` and `last_activity` with a stale unrelated value read from the Session Continuity block instead of the passed argument), hit four separate times in Phase 236 and again in Phases 237 and 240. The harness classifier that blocks any Bash argv containing the literal token that means "finished" was hit repeatedly too. Hand-edit and `git diff`-review every write before committing:
- `.planning/STATE.md`: a new dated entry for Phase 244 and the `progress` counters.
- `.planning/ROADMAP.md`: the Phase 244 plan list and its three Success Criteria checkboxes, checked ONLY for criteria the gate actually proved.
- `.planning/REQUIREMENTS.md`: TRIG-01, TRIG-02, TRIG-03 rows and their Traceability table entries.

Leave the phase-level ROADMAP checkbox UNCHECKED. Phase closure is the orchestrator's step after `gsd-verifier` runs, not this plan's.
  </action>
  <verify>
    <automated>bash tests/run-all-244.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-244.sh` exits 0 and its PASS/FAIL/SKIP counts are transcribed.
    - Every gate command in the list above has been RUN and its verbatim result recorded in Section 7. A gate that was skipped must be recorded as skipped with its reason, never omitted.
    - Every cross-phase suite comparison (219, 236, 205) names the baseline it was compared against and states whether it matched.
    - Any pre-existing unrelated failure is confirmed against a clean pre-244 checkout, with the confirmation method recorded.
    - The rethinking-mindrianos mirror exists at a dated path and cross-references the phase directory; the phase directory's residual register cross-references the room entry. Both directions, or the compositing mandate is not met.
    - STATE.md, ROADMAP.md and REQUIREMENTS.md were hand-edited and every diff was reviewed and is scoped to Phase 244's own lines. Transcribe the `git diff --stat` for the three files.
    - The phase-level ROADMAP checkbox is still unchecked.
    - `grep -rlP '\x{2014}' <every file this phase touched>` returns nothing. Enumerate the file list explicitly rather than globbing the repo.
  </acceptance_criteria>
  <done>The gate was run end to end with every result recorded verbatim, cross-phase suites were compared against named baselines, the reasoning trail lives in both homes cross-linked, and the tracking files were hand-edited and diff-reviewed with phase closure correctly left to the verifier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented defaults -> source constants | A doc that drifts from source becomes a false statement about behavior |
| phase-local trail -> rethinking-mindrianos room | Dev-research content crossing into a Data Room |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-244-33 | Repudiation | doc defaults drifting from source | mitigate | Task 1's default-parity check reads every default from source with `node -e` and treats source as truth. |
| T-244-34 | Repudiation / false success | a phase gate recorded but never run | mitigate | Every gate result must be transcribed verbatim; a skipped gate is recorded as skipped with a reason rather than omitted. |
| T-244-35 | Information disclosure | mirroring dev-research into a room | accept | `rethinking-mindrianos` is the standing MindrianOS-dev consultant room and is the mandated destination per CLAUDE.md. Content is this repo's own architecture reasoning, not user data. No Brain egress. |
| T-244-36 | Tampering | `gsd-tools.cjs` corrupting STATE.md | mitigate | Hand-edit only, with a diff review before commit; the corruption bug is reproduced and documented across Phases 236, 237 and 240. |
| T-244-SC | Tampering | npm/pip/cargo installs | mitigate | NONE. Zero packages in this phase; any install must be refused. |
</threat_model>

<verification>
- `bash tests/run-all-244.sh` exits 0.
- Every named gate command has a recorded verbatim result in the residual register Section 7.
- `docs/ENV-TUNING.md` documents all five env vars with source-verified defaults.
- `.planning/phases/244-semantic-trigger-tier/244-RESIDUALS.md` covers all seven sections.
- `docs/CANON-PHASE-MAP.md` carries a Phase 244 entry.
- The rethinking-mindrianos mirror exists and cross-links both ways.
- `git diff --stat` on the three tracking files shows only Phase 244 lines.
</verification>

<success_criteria>
- Five env vars documented with defaults read from source, not copied from a plan.
- A residual register covering what shipped, two navigator asks, eight assumptions with dispositions, the declared langtalks gap handled honestly, five deliberate non-goals with their evidence, three residual risks, and the gate result.
- The phase gate run end to end with every result observed and transcribed.
- Cross-phase suites 219, 236 and 205 compared against named baselines from the plans that captured them.
- The reasoning trail mirrored to `rethinking-mindrianos` and cross-linked in both directions.
- Tracking files hand-edited and diff-reviewed; the phase checkbox left for the verifier.
- Zero production code changed by this plan.
</success_criteria>

<artifacts_this_phase_produces>
Symbols created by THIS plan:

| Symbol | Kind | Location |
|--------|------|----------|
| Semantic Trigger Tier section | new doc section covering five env vars | `docs/ENV-TUNING.md` |
| `244-RESIDUALS.md` | new phase artifact, seven sections | `.planning/phases/244-semantic-trigger-tier/` |
| Phase 244 canon ledger entry | new row | `docs/CANON-PHASE-MAP.md` |
| dated research mirror | new room entry | `~/MindrianRooms/rethinking-mindrianos/research/` |

No new code symbol. No new env var (this plan documents the five the other plans introduced). No new test.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/244-semantic-trigger-tier/244-08-SUMMARY.md` when done. It MUST carry the full verbatim phase-gate output, the default-parity comparison for all four numeric env vars, the langtalks consult disposition, the two-way cross-link paths for the room mirror, and the `git diff --stat` for the three tracking files.
</output>
