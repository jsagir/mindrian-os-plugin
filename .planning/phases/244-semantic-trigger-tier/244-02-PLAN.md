---
phase: 244-semantic-trigger-tier
plan: 02
type: execute
wave: 2
depends_on: [244-01]
files_modified:
  - lib/core/eureka/fts-index-lifecycle.cjs
  - scripts/fts-index-drain.cjs
  - tests/test-244-fts-index-lifecycle.cjs
autonomous: true
requirements: [TRIG-01]
canon_parts: [7, 8, 9]

must_haves:
  truths:
    - "A caller can ask a room.db whether its FTS index is absent, empty, stale, or healthy, and get back a distinguishable reason string"
    - "A turn that finds the index absent enqueues a build request and returns immediately, never awaiting the build"
    - "The enqueued build is actually performed by an out-of-band drain, so the request seam is live at both ends"
    - "A failed build keeps its queue entry for retry instead of silently clearing it"
  artifacts:
    - path: "lib/core/eureka/fts-index-lifecycle.cjs"
      provides: "ftsIndexState, requestFtsBuild, spawnFtsBuildDrain"
      exports: ["ftsIndexState", "requestFtsBuild", "spawnFtsBuildDrain", "readQueue", "writeQueue", "queuePath", "FTS_BUILD_MAX_ATTEMPTS"]
    - path: "scripts/fts-index-drain.cjs"
      provides: "The expensive half: reads the queue, runs indexNodes, reconciles keep-on-failure"
    - path: "tests/test-244-fts-index-lifecycle.cjs"
      provides: "State-classification fence, enqueue dedupe fence, drain round-trip, keep-on-failure fence"
  key_links:
    - from: "lib/core/eureka/fts-index-lifecycle.cjs"
      to: "scripts/fts-index-drain.cjs"
      via: "detached unref'd spawn gated on queued === true"
      pattern: "detached: true"
    - from: "scripts/fts-index-drain.cjs"
      to: "tri-modal-index.indexNodes"
      via: "await indexNodes(db, { roomDir })"
      pattern: "indexNodes\\("
---

<objective>
Close RESEARCH BLOCKER B-2: `eureka_fts` has no production build lifecycle. It is created lazily by `openIndex`, which is only ever reached through `indexNodes`, whose complete set of production callers is three MANUAL report scripts (`scripts/entity-extract.cjs:924`, `scripts/eureka-room-report.cjs:304`, `scripts/eureka-portfolio-report.cjs`). None runs on a normal turn. There is no session-start build and no entry in `room-db.cjs`'s migration chain. Verified live against `~/MindrianRooms/rethinking-mindrianos/.mindrian/room.db` (8.4 MB, heavily used): the table does not exist.

Without this plan, Plan 05's sensor ships green, passes every fixture test, and fires ZERO times in production, reproducing the exact symptom that opened this phase.

Purpose: give the trigger path a lazy build-on-first-miss that costs one cold turn and never blocks one. `lexicalSearch` is sync and ~1 ms; `indexNodes` is async, reads artifact bodies off disk, and embeds when an encoder is present. The read path and the write path must stay strictly separate (244-RESEARCH.md Pitfall 5) and the 1200 ms NAV budget at `navigation-engine.cjs:820` must not be touched.

Output: one new pure-CJS lifecycle module and one new drain script, built as the fourth instance of a lifecycle pattern this repo already ships three times over.

CORRECTION TO 244-PATTERNS.md, load-bearing for this plan: the pattern map lists "a production build/refresh lifecycle for eureka_fts" under "No Analog Found". That is wrong, and the correct analog was found on disk during planning. `lib/core/intelligence-cascade.cjs:357-384` is an EXACT enqueue-then-detached-drain analog: it calls `enqueueDerive(roomDir, {filePath})` from `scripts/gsd-graph-derive-sweep.cjs:134-158`, then spawns `scripts/gsd-graph-derive-drain.cjs --worker --room <roomDir>` with `{ detached: true, stdio: 'ignore', env: process.env }` and `.unref()`, gated on `enq.queued` and suppressed by a `MOS_NO_DETACHED_DERIVE=1` test seam. `scripts/brain-derivation-drain.cjs:172` is a third instance of the same spawn shape. Copy this pattern; do not invent a fourth mechanism. Building a bespoke scheduler here would be a Canon Part 7 violation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/244-semantic-trigger-tier/244-RESEARCH.md
@.planning/phases/244-semantic-trigger-tier/244-PATTERNS.md
@.planning/phases/244-semantic-trigger-tier/244-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author lib/core/eureka/fts-index-lifecycle.cjs</name>
  <files>lib/core/eureka/fts-index-lifecycle.cjs</files>
  <read_first>
    - scripts/gsd-graph-derive-sweep.cjs at `:93-118` (`queuePath`, `readQueue`, `writeQueue`) and `:120-160` (`enqueueDerive`: the dedupe-by-resolved-roomDir shape, the `{ ok, queued, queuePath }` return contract, the never-throws `catch` returning `{ ok:false, reason:'write_failed' }`)
    - lib/core/intelligence-cascade.cjs at `:344-384` (the enqueue-then-detached-spawn block, its comment on the debounce being the queue dedupe, the `enq.queued` gate, and the `MOS_NO_DETACHED_DERIVE` test seam)
    - lib/core/eureka/tri-modal-index.cjs at `:181-188` (`tableExists`, now public per Plan 01), `:298-310` (`openIndex` DDL), `:330-345` (`indexNodes` corpus SELECT and the `opts.roomDir` body fallback)
    - .planning/phases/244-semantic-trigger-tier/244-RESEARCH.md Pitfall 2 and Pitfall 5
  </read_first>
  <behavior>
    - `ftsIndexState(db)` on a db with a `nodes` table and no `eureka_fts` returns `{ present: false, reason: 'index_absent', node_rows: N, fts_rows: 0, orphan_rows: 0, healthy: false }`.
    - `ftsIndexState(db)` after `openIndex(db)` but before any `indexNodes` returns `reason: 'index_empty'`, `present: true`, `fts_rows: 0`, `healthy: false`.
    - `ftsIndexState(db)` after a real `indexNodes` over a populated `nodes` table returns `reason: 'ok'`, `present: true`, `fts_rows > 0`, `orphan_rows: 0`, `healthy: true`.
    - `ftsIndexState(db)` when `eureka_fts` holds `node_id` values absent from `nodes` returns `reason: 'index_stale'`, `orphan_rows > 0`, `healthy: false`. This is the ghost-trigger condition Plan 03 fences.
    - `ftsIndexState(null)`, `ftsIndexState({})` and `ftsIndexState` against a closed handle return `{ present: false, reason: 'unavailable', healthy: false }` and never throw.
    - `requestFtsBuild(roomDir)` writes `<roomDir>/.mindrian/fts-index-queue.json` and returns `{ ok: true, queued: true, queuePath }` the first time; a second identical call returns `queued: false` and does NOT append a duplicate entry.
    - `requestFtsBuild` on an unwritable or nonexistent path returns `{ ok: false, reason: 'write_failed' }` and never throws.
    - `spawnFtsBuildDrain(roomDir, { queued: true })` with `MOS_NO_DETACHED_FTS_BUILD=1` set performs NO spawn and returns `{ spawned: false, reason: 'suppressed' }`.
    - `spawnFtsBuildDrain(roomDir, { queued: false })` performs NO spawn and returns `{ spawned: false, reason: 'already_pending' }`, because a drain is already owed.
    - Every exported function is synchronous. Nothing in this module is `async` and nothing awaits.
  </behavior>
  <action>
Create `lib/core/eureka/fts-index-lifecycle.cjs`, CJS, `'use strict'`, no new dependencies.

Header comment must state four things: (1) this is the CHEAP half of an enqueue-then-drain pair whose expensive half is `scripts/fts-index-drain.cjs`, structured exactly like the shipped `gsd-graph-derive-sweep.cjs` / `gsd-graph-derive-drain.cjs` pair; (2) Canon Part 9, this module never opens room.db, it takes a caller-owned handle, mirroring `sensor-expert-skill.cjs:146-148`; (3) Canon Part 8, every value it returns is a closed scalar or enum, never matched text and never a node id; (4) it is sync and zero-await by contract, because its only foreground caller is the ctx-assembly producer inside the 1200 ms NAV budget.

Constants: `FTS_QUEUE_RELATIVE` = `path.join('.mindrian', 'fts-index-queue.json')`; `FTS_BUILD_MAX_ATTEMPTS` = 3 (mirroring the drain analog's `MAX_DERIVE_ATTEMPTS` discipline); `FTS_STALE_ORPHAN_FLOOR` = 1.

`ftsIndexState(db)`: guard `db && typeof db.prepare === 'function'` else return the `unavailable` shape. Call the PUBLIC `tableExists(db, 'eureka_fts')` from `tri-modal-index.cjs` (Plan 01 promoted it; do NOT reach into `._test`, and do NOT hand-roll a second `sqlite_master` probe, which would be a Part 7 duplication of a seven-line function). Count `nodes` rows and `eureka_fts` rows using the failed-query-means-zero idiom from `room-graph-density-module.cjs:120-126`. Count orphans with `SELECT count(*) AS c FROM eureka_fts WHERE node_id NOT IN (SELECT id FROM nodes)`. Classify in this precedence: absent, then unavailable, then stale (orphans at or above the floor), then empty (`fts_rows === 0` while `node_rows > 0`), then ok. Wrap the whole body in one try/catch that soft-fails to the `unavailable` shape.

`queuePath`, `readQueue`, `writeQueue`: copy the shape from `gsd-graph-derive-sweep.cjs:93-118` against `FTS_QUEUE_RELATIVE`. `readQueue` returns `{ entries: [] }` on any read or parse failure. `writeQueue` creates `.mindrian/` if absent and writes JSON.

`requestFtsBuild(roomDir, opts)`: reject a non-string or empty `roomDir` with `{ ok: false, reason: 'no_room' }`. Resolve the path. Dedupe by resolved `roomDir` exactly as `enqueueDerive` does. An entry is `{ roomDir, enqueued_at, reason, attempts: 0 }` where `reason` is the caller-supplied enum from `ftsIndexState` (`index_absent` / `index_empty` / `index_stale`) and defaults to `index_absent`. Return `{ ok, queued, queuePath }`. Never throw.

`spawnFtsBuildDrain(roomDir, opts)`: no-op returning `{ spawned: false, reason: 'already_pending' }` when `opts.queued !== true`; no-op returning `{ spawned: false, reason: 'suppressed' }` when `process.env.MOS_NO_DETACHED_FTS_BUILD === '1'`. Otherwise `spawn(process.execPath, [<repo>/scripts/fts-index-drain.cjs, '--worker', '--room', roomDir], { detached: true, stdio: 'ignore', env: process.env })` then `.unref()`, returning `{ spawned: true }`. Wrap in try/catch returning `{ spawned: false, reason: 'spawn_failed' }`. Copy the `--worker` flag semantics from the analog: the spawn IS already the detached worker, so the drain must not re-detach a second copy of itself.

Export `ftsIndexState`, `requestFtsBuild`, `spawnFtsBuildDrain`, `readQueue`, `writeQueue`, `queuePath`, `FTS_BUILD_MAX_ATTEMPTS`, `FTS_QUEUE_RELATIVE`.
  </action>
  <verify>
    <automated>node -e "const l=require('./lib/core/eureka/fts-index-lifecycle.cjs'); ['ftsIndexState','requestFtsBuild','spawnFtsBuildDrain','readQueue','writeQueue','queuePath'].forEach(k=>{if(typeof l[k]!=='function')throw new Error('missing '+k)}); console.log('lifecycle surface ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `lifecycle surface ok`.
    - `node -e "const s=require('fs').readFileSync('lib/core/eureka/fts-index-lifecycle.cjs','utf8'); if(/\basync\b|\bawait\b/.test(s)) throw new Error('module must be sync'); console.log('sync-only ok')"` passes. The module is called inside the NAV latency budget and must contain no async surface.
    - `grep -c "_test" lib/core/eureka/fts-index-lifecycle.cjs` returns 0 (comment-stripped: `grep -v '^\s*[/*]' lib/core/eureka/fts-index-lifecycle.cjs | grep -c "_test"` returns 0). No test seam is imported into the runtime path.
    - `node scripts/check-substrate.cjs --diff` exits 0. This module issues no INSERT/UPDATE/DELETE against `nodes`, `edges` or `memory_event`.
    - `grep -lP '\x{2014}' lib/core/eureka/fts-index-lifecycle.cjs` returns nothing.
  </acceptance_criteria>
  <done>The lifecycle module exists, is sync-only, reaches `tableExists` through its public export, and classifies four distinguishable index states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author scripts/fts-index-drain.cjs, the expensive half</name>
  <files>scripts/fts-index-drain.cjs</files>
  <read_first>
    - scripts/gsd-graph-derive-drain.cjs IN FULL for the header contract, the `--worker` / `--room` argv shape, the exit-0-always discipline, and the keep-on-failure queue reconcile with a max-attempts drop
    - lib/core/eureka/tri-modal-index.cjs at `:318-380` (`indexNodes`: it is `async`, it does per-node DELETE-then-INSERT so it is idempotent, it degrades to a lexical-only index when the encoder is unavailable, and it threads `opts.roomDir` into the Artifact body-from-disk fallback)
    - lib/core/navigation.cjs at `:435-461` (`openRoomDbForCaller` is the WRITABLE door; `openRoomDbReadOnlyForCaller` is its read-only sibling; `closeRoomDbForCaller` closes either)
  </read_first>
  <behavior>
    - Run with `--worker --room <roomDir>` against a room whose queue holds one entry: `indexNodes` runs, `eureka_fts` becomes present and non-empty, the queue entry is CLEARED, and the process exits 0.
    - Run against a room with an EMPTY queue: no db work, exit 0, no queue file mutation.
    - Run where `indexNodes` throws: the queue entry is KEPT with `attempts` incremented, and the process still exits 0.
    - Run where the entry has already reached `FTS_BUILD_MAX_ATTEMPTS`: the entry is dropped AND a permanent-failure record is appended to `<roomDir>/.mindrian/fts-index-failures.json`, never a silent drop.
    - Running the drain twice in a row over the same room is idempotent: the second run finds an empty queue and does nothing, and the index row count is unchanged.
    - The process ALWAYS exits 0, on every non-fatal path. It must never block or fail a session-start or post-write hook.
  </behavior>
  <action>
Create `scripts/fts-index-drain.cjs`, executable, `#!/usr/bin/env node`, `'use strict'`.

Header comment, in the `gsd-graph-derive-drain.cjs` register, must state: this is the EXPENSIVE half of the enqueue-then-drain debounce whose cheap half is `lib/core/eureka/fts-index-lifecycle.cjs`; the division of labor (the lifecycle module enqueues and spawns, this file builds and reconciles); and the failure discipline, exit-0-never-block but NEVER SILENT ROT, a failed build keeps its entry and only a permanent failure after `FTS_BUILD_MAX_ATTEMPTS` drops it with a written failure record.

Argv parsing: a `process.argv` switch-case router matching the repo convention (no Commander, no yargs). Accept `--worker` (informational: this process IS the detached worker, so it must never re-spawn) and `--room <dir>`. When `--room` is absent, read the queue for the active room resolved the same way `gsd-graph-derive-drain.cjs` does; if no room resolves, exit 0 silently.

Body: read the queue via `readQueue` from the lifecycle module. For each entry, open room.db through `openRoomDbForCaller` from `lib/core/navigation.cjs` (the WRITABLE door: this process writes the index, so `openRoomDbReadOnlyForCaller` is wrong here) and always close via `closeRoomDbForCaller` in a `finally`. Call `await indexNodes(db, { roomDir })` and thread `roomDir` explicitly; without it the corpus collapses to claim text plus artifact TITLES only, because the Artifact/memory_artifact path-body fallback at `tri-modal-index.cjs:138` is what makes the corpus substantive (measured: 1229 claim rows carry 267,597 searchable chars, 28 Artifact rows carry 1,143 chars of titles alone).

Reconcile: clear an entry only on success. On failure increment `attempts` and keep it. At or past `FTS_BUILD_MAX_ATTEMPTS`, drop it AND append `{ roomDir, failed_at, attempts, message }` to `<roomDir>/.mindrian/fts-index-failures.json` so a doctor check can surface it (Plan 06 reads this file).

Do NOT index `fragments`. `indexNodes` reads `SELECT id, type, properties FROM nodes` and never touches `fragments`, so the correct behavior is the default. State this as an explicit NON-GOAL in the header with the measurement behind it: with `fragments` included, the turn "what is the weather in paris today" produced 3 confident hits; with `fragments` excluded, that turn and "my cat needs a vet appointment tomorrow" and "order pizza for dinner tonight" all produced 0 hits while two genuinely relevant turns produced 5 each. The corpus choice does almost all the work and requires no threshold tuning. A future edit that "helpfully" widens the corpus destroys the phase.

Wrap `main()` in a top-level try/catch that exits 0 on any fault.
  </action>
  <verify>
    <automated>node scripts/fts-index-drain.cjs --worker --room /nonexistent-room-244 && echo "exit-0-always ok"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `exit-0-always ok` (a nonexistent room must not fail the drain).
    - `node --check scripts/fts-index-drain.cjs` exits 0.
    - `grep -c "fragments" scripts/fts-index-drain.cjs` is at least 1 AND every occurrence is inside the NON-GOAL comment. Confirm with `grep -n fragments scripts/fts-index-drain.cjs` and transcribe the lines: there must be no SQL statement naming `fragments`.
    - `grep -v '^\s*[/*#]' scripts/fts-index-drain.cjs | grep -c "openRoomDbReadOnlyForCaller"` returns 0. The drain writes, so the read-only door would be wrong.
    - `grep -lP '\x{2014}' scripts/fts-index-drain.cjs` returns nothing.
  </acceptance_criteria>
  <done>The drain exists, always exits 0, keeps failing entries for retry, writes a permanent-failure record instead of dropping silently, and threads `roomDir` so the corpus includes artifact bodies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the lifecycle round-trip end to end</name>
  <files>tests/test-244-fts-index-lifecycle.cjs</files>
  <read_first>
    - lib/core/eureka/fts-index-lifecycle.cjs (as written in Task 1)
    - scripts/fts-index-drain.cjs (as written in Task 2)
    - tests/test-219-fts5-degrade.cjs at `:33-34` (offline preload), `:84-107` (fixture db + `hasTable`), `:110-125` (the `PASS`/`FAIL`/`FAILURES` async harness)
    - tests/helpers/fixture-room-219.cjs (exports `buildFixtureRoom`; reuse it rather than authoring a second FTS fixture, per Canon Part 7)
  </read_first>
  <behavior>
    - Every line of Task 1's `<behavior>` block, asserted directly.
    - Every line of Task 2's `<behavior>` block, asserted by invoking the drain IN-PROCESS (require and call its exported main, or spawn it synchronously with `MOS_NO_DETACHED_FTS_BUILD=1` already set) so timing is deterministic and no detached process outlives the test.
    - The full round trip: start from a room whose `eureka_fts` does not exist; call `ftsIndexState` and observe `index_absent`; call `requestFtsBuild` and observe `queued: true`; run the drain; call `ftsIndexState` again and observe `ok` with `fts_rows > 0`; confirm the queue is empty; confirm `lexicalSearch` against a relevant query now returns at least one hit where it previously returned `[]`.
    - The anti-vacuity leg: BEFORE the drain runs, `lexicalSearch` on the same relevant query returns `[]`. Without this assertion the round-trip test could pass against an index that was already built.
    - No orphan process survives: after the test, `pgrep -af "^[^ ]*node .*fts-index-drain"` returns nothing. Anchor the pattern on the interpreter, not on the bare script name; a bare `pgrep -f fts-index-drain` matches the invoking shell's own command line and reports a phantom PID (observed in Phase 236-02).
  </behavior>
  <action>
Create `tests/test-244-fts-index-lifecycle.cjs`. First executable line: `require('./eureka-offline-preload.cjs');`.

Set `process.env.MOS_NO_DETACHED_FTS_BUILD = '1'` at the top of the file and restore the prior value in a `finally`, so no leg of this test can spawn a detached process. Assert the suppression explicitly in one leg rather than relying on it silently.

Build fixtures with `buildFixtureRoom` from `tests/helpers/fixture-room-219.cjs`. Where a bare db is enough, use the `makeFixtureDb` shape from `tests/test-219-fts5-degrade.cjs:84-97`: plain `DatabaseSync`, no `allowExtension`, a `nodes` table with two semantically separated clusters.

Construct the `index_stale` case by inserting a row into `eureka_fts` whose `node_id` has no matching `nodes.id`, then asserting `orphan_rows > 0` and `reason === 'index_stale'`. Do not construct it by deleting from `nodes` inside a rebuild; that is Plan 03's fence and must stay independently provable.

Use the `PASS`/`FAIL`/`FAILURES` async harness shape. Clean every mkdtemp dir in a `finally`. End the file with the orphan-process check described in `<behavior>`.
  </action>
  <verify>
    <automated>node tests/test-244-fts-index-lifecycle.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-244-fts-index-lifecycle.cjs` exits 0 with `FAIL=0`.
    - MUTATION PROOF 1, executed live and reverted: make `ftsIndexState` return `reason: 'ok'` unconditionally; confirm the `index_absent`, `index_empty` and `index_stale` assertions all turn RED; restore; confirm green.
    - MUTATION PROOF 2, executed live and reverted: remove the dedupe check from `requestFtsBuild` so every call appends; confirm the second-call `queued: false` assertion turns RED; restore; confirm green.
    - MUTATION PROOF 3, executed live and reverted: change the drain's reconcile to clear the entry on FAILURE as well as success; confirm the keep-on-failure assertion turns RED; restore; confirm green. This is the RCA-graph-derive-silent-clear failure shape and must be provably fenced.
    - MUTATION PROOF 4, executed live and reverted: drop the `{ roomDir }` argument from the drain's `indexNodes` call; confirm the corpus assertion (a query matching artifact BODY text, not a title) turns RED; restore; confirm green. This proves the body-from-disk fallback is actually threaded rather than assumed.
    - `pgrep -af "^[^ ]*node .*fts-index-drain"` returns nothing after the run.
    - `bash tests/run-all-244.sh` discovers three files and exits 0.
    - `bash tests/run-all-219.sh` matches the baseline from Plan 01.
    - `grep -lP '\x{2014}' tests/test-244-fts-index-lifecycle.cjs` returns nothing.
  </acceptance_criteria>
  <done>The absent-to-built round trip is proven end to end with an anti-vacuity pre-assertion, four mutations observed red and restored, and no orphan process left behind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| foreground turn -> detached child process | A spawn crossing the process boundary carries argv and inherited env |
| queue file -> drain executor | A JSON file on disk drives what the drain opens and indexes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-244-04 | Denial of service | `spawnFtsBuildDrain` stampede | mitigate | Spawn is gated on `enq.queued === true`, so rapid repeat misses collapse to one drain, copying the analog's stated debounce-is-the-dedupe contract at `intelligence-cascade.cjs:350-356`. |
| T-244-05 | Denial of service | `indexNodes` on the foreground turn | mitigate | The lifecycle module is asserted sync-only by a source gate; `indexNodes` runs only inside the drain, never inside `decide()`. Measured: `lexicalSearch` 0-1 ms, `indexNodes` 53 ms for 4403 docs plus encoder time. |
| T-244-06 | Repudiation / silent rot | drain queue reconcile | mitigate | Keep-on-failure with a max-attempts drop plus a written `fts-index-failures.json` record, mutation-proven in Task 3. |
| T-244-07 | Tampering | argv passed to the detached spawn | mitigate | `roomDir` is passed as a discrete argv element to `spawn` with `shell: false` (the default), never interpolated into a shell string. `stdio: 'ignore'` prevents output-channel injection. |
| T-244-08 | Information disclosure | queue file contents | accept | The queue holds a LOCAL absolute path, an ISO timestamp, a closed reason enum and an attempt count. No turn prose, no matched text, no node ids. Canon Part 8 clean. |
| T-244-SC | Tampering | npm/pip/cargo installs | mitigate | NONE. Zero packages recommended by 244-RESEARCH.md's Package Legitimacy Audit; any install is out of scope and must be refused. |
</threat_model>

<verification>
- `bash tests/run-all-244.sh` exits 0 with three discovered files.
- `bash tests/run-all-219.sh` and `bash tests/run-all-236.sh` both match their pre-plan baselines (this plan does not touch `lazygraph-ops.cjs`, so 236 must be provably unchanged).
- `node scripts/check-substrate.cjs --diff` exits 0.
- `node scripts/doctor.cjs --acceptance` is NOT expected to change in this plan; capture its pre-plan and post-plan result and confirm they match. Plan 06 is the only plan permitted to move that number.
- `git diff --stat` shows exactly the three files in `files_modified`.
</verification>

<success_criteria>
- `ftsIndexState` distinguishes `index_absent`, `index_empty`, `index_stale`, `ok` and `unavailable`, and never throws.
- `requestFtsBuild` dedupes by resolved roomDir and never throws.
- `spawnFtsBuildDrain` spawns detached-and-unref'd only when a build is genuinely owed, and is suppressible by `MOS_NO_DETACHED_FTS_BUILD=1`.
- `scripts/fts-index-drain.cjs` builds the index, is idempotent, keeps failing entries for retry, writes a permanent-failure record, and always exits 0.
- The seam is live at BOTH ends: something enqueues and something drains. Neither half ships alone.
- Four mutation proofs executed live and reverted, each transcribed with its observed red output.
- `fragments` is never indexed; the exclusion is stated as a non-goal with its measurement.
</success_criteria>

<artifacts_this_phase_produces>
Symbols created by THIS plan:

| Symbol | Kind | Location |
|--------|------|----------|
| `ftsIndexState(db)` | exported function | `lib/core/eureka/fts-index-lifecycle.cjs` |
| `requestFtsBuild(roomDir, opts)` | exported function | `lib/core/eureka/fts-index-lifecycle.cjs` |
| `spawnFtsBuildDrain(roomDir, opts)` | exported function | `lib/core/eureka/fts-index-lifecycle.cjs` |
| `readQueue` / `writeQueue` / `queuePath` | exported functions | `lib/core/eureka/fts-index-lifecycle.cjs` |
| `FTS_BUILD_MAX_ATTEMPTS` / `FTS_QUEUE_RELATIVE` | exported constants | `lib/core/eureka/fts-index-lifecycle.cjs` |
| `scripts/fts-index-drain.cjs` | new CLI script (`--worker`, `--room <dir>`) | `scripts/` |
| `<roomDir>/.mindrian/fts-index-queue.json` | new runtime state file | per-room |
| `<roomDir>/.mindrian/fts-index-failures.json` | new runtime state file | per-room |
| `MOS_NO_DETACHED_FTS_BUILD` | new env test seam | process env |
| `tests/test-244-fts-index-lifecycle.cjs` | new file | `tests/` |

Consumed downstream by: Plan 05 (the producer calls `ftsIndexState` + `requestFtsBuild` + `spawnFtsBuildDrain`), Plan 06 (the doctor module calls `ftsIndexState` and reads `fts-index-failures.json`).

CIRS note: `scripts/fts-index-drain.cjs` is NOT one of the four invocable-surface classes. `scripts/check-shape-declaration.cjs:695-726` enumerates only `commands/`, `agents/`, `pipelines/*/CHAIN.md` and `skills/*/SKILL.md`. Its three sibling drains (`gsd-graph-derive-drain.cjs`, `gsd-graph-derive-sweep.cjs`, `brain-derivation-drain.cjs`) carry no HITL shape declaration either. No declaration is required and none must be invented.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/244-semantic-trigger-tier/244-02-SUMMARY.md` when done. It MUST transcribe all four mutation proofs with observed red output, the pre-plan and post-plan `doctor --acceptance` results, and the orphan-process check result.
</output>
