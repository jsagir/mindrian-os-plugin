---
phase: 95.5-post-compact-memory-pipeline-consumer
plan: 03
type: execute
wave: 2
depends_on:
  - 95.5-02
files_modified:
  - hooks/hooks.json
autonomous: true
requirements:
  - POSTCOMPACT-95.5-02
canon_parts: [Part 7]

must_haves:
  truths:
    - "hooks/hooks.json SessionStart array length goes from 8 to 9 after this plan ships."
    - "9th entry invokes scripts/restore-post-compact-context.cjs with timeout 3000ms (matching PostCompact 88-09 invariant)."
    - "9th entry's matcher is `startup|clear|compact` (matches all SessionStart trigger sources, including post-compact resume)."
    - "Edit is performed via Node JSON.parse + push + JSON.stringify(data, null, 2) + '\\n'; NEVER Edit-tool string surgery (95.2 B4 fix precedent)."
    - "Edit is idempotent: re-running the wiring task does NOT add a duplicate entry."
    - "Whitespace drift is zero: `git diff hooks/hooks.json` shows ONLY the inserted entry (no formatting churn outside the new array element)."
    - "The other 8 SessionStart entries are byte-stable (none reordered, none modified)."
  artifacts:
    - path: "hooks/hooks.json"
      provides: "SessionStart hook registry with 9 entries (added scripts/restore-post-compact-context.cjs)"
      contains: "scripts/restore-post-compact-context.cjs"
  key_links:
    - from: "hooks/hooks.json"
      to: "scripts/restore-post-compact-context.cjs"
      via: "SessionStart command entry"
      pattern: "restore-post-compact-context.cjs"
---

<objective>
Wire the consumer (Plan 95.5-02) into Claude Code's SessionStart hook chain by appending a 9th entry to `hooks/hooks.json` SessionStart array.

Current SessionStart array (length 8) per `hooks/hooks.json:3-86`:
1. session-start (Bash banner via run-hook.cmd)
2. operator-update.cjs
3. memory-resume-nudge.cjs
4. migrate-stale-user-settings.cjs (--auto --quiet)
5. statusline-fallback-echo.cjs
6. check-onboard-statusline.cjs
7. preflight-tension-surface.cjs
8. preflight-doctor.cjs (95.2 Finding C precedent -- exact template for D-01)

After this plan: length 9, with `scripts/restore-post-compact-context.cjs` as the 9th entry.

Per D-01b (95.2 B4 fix precedent): the edit MUST use Node JSON.parse + push + JSON.stringify, NOT Edit-tool string surgery. A one-shot Node script performs the read-modify-write atomically and idempotently (skip if already present).

Purpose: Activate the post-compact memory pipeline consumer. Without this entry, the consumer ships but never fires. Once shipped, every SessionStart fires the hook chain top-to-bottom; the 9th entry runs the consumer; if a fresh + room-matched side-channel file exists, the consumer emits `hookSpecificOutput.additionalContext` re-injecting TRIPLE_CONTEXT into Claude's context.
Output: `hooks/hooks.json` SessionStart array length 9; idempotent re-run produces no diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/includes/release-process.md
@.planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-CONTEXT.md
@.planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-RESEARCH.md
@hooks/hooks.json
@scripts/preflight-doctor.cjs
@scripts/restore-post-compact-context.cjs

<interfaces>
<!-- Existing 8th SessionStart entry (preflight-doctor.cjs, 95.2 Finding C) at hooks/hooks.json:76-85 -->
{
  "matcher": "startup|clear|compact",
  "hooks": [
    {
      "type": "command",
      "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/preflight-doctor.cjs\"",
      "timeout": 2000
    }
  ]
}

<!-- 9th SessionStart entry (TARGET shape) -->
{
  "matcher": "startup|clear|compact",
  "hooks": [
    {
      "type": "command",
      "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/restore-post-compact-context.cjs\"",
      "timeout": 3000
    }
  ]
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append SessionStart entry #9 via idempotent Node read-modify-write</name>
  <files>hooks/hooks.json</files>

  <read_first>
    - Read hooks/hooks.json:1-86 (SessionStart array -- confirm length 8 before edit).
    - Read 95.5-CONTEXT.md D-01b (idempotency check + Node JSON.stringify re-serialization, NOT Edit-tool string surgery).
    - Read 95.5-RESEARCH.md "Pattern 3: hooks.json Idempotent JSON.stringify Re-Serialization (95.2 B4 fix)" -- full Node code block.
    - Read 95.5-RESEARCH.md "Pitfall 6: hooks.json Re-Serialization Whitespace Drift" -- guards against the 95.2 B4 regression.
  </read_first>

  <behavior>
    - Pre-condition check: SessionStart array length === 8 (no duplicate insertion safeguard).
    - Idempotency: if any existing entry's command contains `restore-post-compact-context.cjs`, SKIP append + exit 0 with status message "already wired".
    - If not present: push the 9th entry with shape `{ matcher: 'startup|clear|compact', hooks: [{ type: 'command', command: 'node "${CLAUDE_PLUGIN_ROOT}/scripts/restore-post-compact-context.cjs"', timeout: 3000 }] }`.
    - Re-serialize: `JSON.stringify(data, null, 2) + '\n'` (matches existing 2-space indent + trailing newline).
    - Atomic write: write to temp file in same dir, then `fs.renameSync` (atomic on POSIX).
    - Post-condition: SessionStart array length === 9.
  </behavior>

  <action>
    Run a one-shot Node script (NOT a persisted file -- a single command-line invocation via `node -e '...'`) that performs the idempotent read-modify-write. This avoids creating a one-off helper script that would clutter the repo.

    Reference RESEARCH §"hooks.json Idempotent JSON.stringify Re-Serialization (95.2 B4 fix)" for the exact pattern.

    Inline command:

    ```bash
    node -e '
    const fs = require("node:fs");
    const path = require("node:path");
    const HOOKS_PATH = path.join(process.cwd(), "hooks", "hooks.json");
    const data = JSON.parse(fs.readFileSync(HOOKS_PATH, "utf8"));
    const arr = data.hooks && data.hooks.SessionStart;
    if (!Array.isArray(arr)) {
      console.error("FAIL: hooks.SessionStart is not an array");
      process.exit(1);
    }
    const NEW_CMD = "node \"${CLAUDE_PLUGIN_ROOT}/scripts/restore-post-compact-context.cjs\"";
    const already = arr.some(entry =>
      entry && Array.isArray(entry.hooks) &&
      entry.hooks.some(h => h && typeof h.command === "string" &&
        h.command.indexOf("restore-post-compact-context.cjs") !== -1));
    if (already) {
      console.log("idempotent: entry already present (length=" + arr.length + ")");
      process.exit(0);
    }
    arr.push({
      matcher: "startup|clear|compact",
      hooks: [{
        type: "command",
        command: NEW_CMD,
        timeout: 3000
      }]
    });
    const tmp = HOOKS_PATH + ".tmp.95.5-03";
    fs.writeFileSync(tmp, JSON.stringify(data, null, 2) + "\n");
    fs.renameSync(tmp, HOOKS_PATH);
    console.log("appended: SessionStart length=" + arr.length);
    '
    ```

    Re-run the command immediately to verify idempotency: second run MUST log `idempotent: entry already present (length=9)` and exit 0 without modifying the file (verify by checking mtime is unchanged across the second invocation, or by inspecting `git diff` after the second run shows no change).

    Do NOT use Edit / MultiEdit / sed / awk. The 95.2 B4 incident was caused by Edit-tool string surgery on the structurally-precise hooks.json; this rule is non-negotiable.

    Use ASCII `--` only. NO Unicode em-dashes (the file content is JSON; em-dash check applies to any inline narrative).
  </action>

  <acceptance_criteria>
    - [ ] `[[ "$PWD" == "/home/jsagi/MindrianOS-Plugin" ]]` (workspace guard).
    - [ ] `node -e "console.log(JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8')).hooks.SessionStart.length)"` returns `9` (post-edit).
    - [ ] `grep -c "restore-post-compact-context.cjs" hooks/hooks.json` returns >= 1.
    - [ ] `grep -c "preflight-doctor.cjs" hooks/hooks.json` returns >= 1 (8th entry preserved -- not overwritten).
    - [ ] All other 7 prior entries preserved: `grep -c "session-start\|operator-update\|memory-resume-nudge\|migrate-stale-user-settings\|statusline-fallback-echo\|check-onboard-statusline\|preflight-tension-surface" hooks/hooks.json` returns >= 7 (each entry has at least one match).
    - [ ] Timeout matches PostCompact 88-09 invariant (3000ms): `grep -B 1 "restore-post-compact-context.cjs" hooks/hooks.json | grep -A 2 restore | grep -c '"timeout": 3000'` returns >= 1.
    - [ ] Matcher matches existing convention: `grep -B 4 "restore-post-compact-context.cjs" hooks/hooks.json | grep -c '"matcher": "startup|clear|compact"'` returns >= 1.
    - [ ] JSON validity: `node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))"` exits 0.
    - [ ] Idempotency: re-running the inline command produces no diff (`git diff --exit-code hooks/hooks.json` after second run returns 0).
    - [ ] Whitespace integrity: `git diff hooks/hooks.json | grep -c '^+'` returns ~10-12 (the new entry block) -- NOT hundreds (which would indicate whitespace churn).
    - [ ] No em-dashes anywhere in hooks.json: `grep -P '[\x{2014}\x{2013}]' hooks/hooks.json | wc -l` returns 0.
    - [ ] R1 byte-equal preserved.
  </acceptance_criteria>

  <verify>
    <automated>
    [[ "$PWD" == "/home/jsagi/MindrianOS-Plugin" ]] || { echo "WORKSPACE GUARD FAILED"; exit 1; }
    LEN=$(node -e "console.log(JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8')).hooks.SessionStart.length)")
    [ "$LEN" = "9" ] &&
    [ "$(grep -c 'restore-post-compact-context.cjs' hooks/hooks.json)" -ge 1 ] &&
    [ "$(grep -c 'preflight-doctor.cjs' hooks/hooks.json)" -ge 1 ] &&
    [ "$(grep -c 'session-start\|operator-update\|memory-resume-nudge\|migrate-stale-user-settings\|statusline-fallback-echo\|check-onboard-statusline\|preflight-tension-surface' hooks/hooks.json)" -ge 7 ] &&
    node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))" &&
    [ "$(grep -F -e '--' -e '--' hooks/hooks.json | wc -l)" -eq 0 ] &&
    [ "$(sha256sum lib/hmi/shape-f6-renderer.cjs | cut -d' ' -f1)" = "1792535860abc791222bf0ecf59599d66e49ad9cc1606b3d8679fca2922150cf" ]
    </automated>
  </verify>

  <done>SessionStart array length 9, 9th entry wires `scripts/restore-post-compact-context.cjs` with matcher `startup|clear|compact` and timeout 3000ms, idempotent, JSON valid, no whitespace drift, R1 invariant intact.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end smoke -- Claude Code SessionStart simulation invokes consumer</name>
  <files>(no file modifications -- verification only)</files>

  <read_first>
    - Read hooks/hooks.json (post-Task 1) to confirm structure.
    - Read scripts/restore-post-compact-context.cjs (Plan 95.5-02 ship) to verify entry-point.
  </read_first>

  <behavior>
    - Smoke parses hooks/hooks.json, finds the 9th SessionStart entry, extracts the command string, expands `${CLAUDE_PLUGIN_ROOT}` to the current working directory, runs the resulting command, asserts exit 0 and silent envelope `{"continue":true}` (because no fixture room is set up in this smoke -- Tier 0 path).
    - Validates that the command-string-as-shipped is actually invokable (catches typos in path, env-var name, etc.).
  </behavior>

  <action>
    Run the inline smoke. Note: this validates ONLY that the wired command string is invokable end-to-end. The full happy-path consume + cross-room HARD SKIP + forensic preserve smokes are covered in Plan 95.5-04 (test suite GREEN).

    Pre-flight check: confirm the 9th entry parses cleanly.
    Then: extract the command, expand `${CLAUDE_PLUGIN_ROOT}` to `$PWD`, run via `bash -c`, capture stdout, assert it parses as JSON and has `continue: true`.
  </action>

  <acceptance_criteria>
    - [ ] Workspace guard passes.
    - [ ] 9th SessionStart entry's command string contains `restore-post-compact-context.cjs`.
    - [ ] Expanded command (with `${CLAUDE_PLUGIN_ROOT}` -> `$PWD`) is invokable from bash and exits 0.
    - [ ] Stdout parses as JSON.
    - [ ] Parsed JSON has `continue === true` (Tier 0 silent path is fine -- no fixture room set up).
    - [ ] R1 byte-equal preserved.
  </acceptance_criteria>

  <verify>
    <automated>
    [[ "$PWD" == "/home/jsagi/MindrianOS-Plugin" ]] || { echo "WORKSPACE GUARD FAILED"; exit 1; }
    set -e
    CMD=$(node -e "
      const data = JSON.parse(require('fs').readFileSync('hooks/hooks.json', 'utf8'));
      const arr = data.hooks.SessionStart;
      const idx = arr.findIndex(e => e.hooks && e.hooks.some(h => h.command && h.command.indexOf('restore-post-compact-context.cjs') !== -1));
      if (idx < 0) { console.error('not found'); process.exit(1); }
      console.log(arr[idx].hooks[0].command);
    ")
    EXPANDED="${CMD//\$\{CLAUDE_PLUGIN_ROOT\}/$PWD}"
    OUT=$(eval "$EXPANDED" </dev/null 2>/dev/null)
    node -e "
      const env = JSON.parse(process.argv[1]);
      if (env.continue !== true) { console.error('continue not true:', env); process.exit(1); }
      console.log('ok');
    " "$OUT" | grep -q '^ok$' &&
    [ "$(sha256sum lib/hmi/shape-f6-renderer.cjs | cut -d' ' -f1)" = "1792535860abc791222bf0ecf59599d66e49ad9cc1606b3d8679fca2922150cf" ]
    </automated>
  </verify>

  <done>Wired command string is shipped correctly: hook chain entry-point invokes consumer end-to-end, R1 invariant intact.</done>
</task>

</tasks>

<verification>
Plan 95.5-03 ships when:
1. `hooks/hooks.json` SessionStart array length === 9.
2. 9th entry wires `node "${CLAUDE_PLUGIN_ROOT}/scripts/restore-post-compact-context.cjs"` with matcher `startup|clear|compact` and timeout 3000ms.
3. JSON validity intact (no parse errors).
4. Idempotency: re-running the wiring command produces no diff.
5. Whitespace integrity: only the new entry appears in `git diff` (no formatting churn).
6. End-to-end smoke (Task 2 verify): expanded command invokes consumer, returns silent envelope.
7. R1 byte-equal preserved.
8. Zero Unicode em-dashes.
</verification>

<success_criteria>
- POSTCOMPACT-95.5-02 (consumer wired into hook chain): SessionStart array length 9, 9th entry calls `scripts/restore-post-compact-context.cjs`.
- D-01b: idempotent Node JSON.stringify re-serialization (no Edit-tool string surgery).
- 95.2 B4 fix precedent honored: zero whitespace drift; second run produces no diff.
- Other 8 entries byte-stable.
- R1 byte-equal preserved.
- Zero Unicode em-dashes.
</success_criteria>

<output>
After completion, create `.planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-03-SUMMARY.md` documenting:
- SessionStart array length pre-edit (8) and post-edit (9).
- 9th entry shape (matcher + command + timeout).
- Idempotency test result (second run produces no diff).
- End-to-end command invocation result.
- R1 invariant pre+post sha256 match.
</output>
