---
phase: 95.5-post-compact-memory-pipeline-consumer
plan: 03
subsystem: hooks
tags: [hooks, sessionstart, post-compact, side-channel, idempotent-edit, json-stringify, canon-part-7]
canon_parts: [Part 7]

requires:
  - phase: 95.5-02
    provides: scripts/restore-post-compact-context.cjs (READ-side consumer; ships on main via Wave 1 commits 38fe240 / 3487157 / 653943e)
  - phase: 95.5-01
    provides: scripts/post-compact D-04 YAML frontmatter stamp (back-port; ships on main via b0b1553 / 06ca9bd)
  - phase: 95.2 (Finding C)
    provides: parallel CJS SessionStart hook template (preflight-doctor.cjs); 8th-entry precedent
provides:
  - hooks/hooks.json SessionStart entry that invokes scripts/restore-post-compact-context.cjs at SessionStart (matcher startup|clear|compact, timeout 3000ms)
  - Idempotent Node JSON.stringify re-serialization pattern (95.2 B4 fix precedent applied to a fresh hooks.json edit)
  - Post-compact memory pipeline READ side ACTIVATED: every SessionStart now fires the hook chain top-to-bottom; the consumer entry runs; if a fresh + room-matched side-channel file exists, the consumer emits hookSpecificOutput.additionalContext re-injecting TRIPLE_CONTEXT into Claude's context
affects:
  - phase 95.5-04 (test suite rewrite -- now has a wired consumer to assert against end-to-end)
  - phase 95.5-05 (release plumbing -- can ship the closed pipeline as v1.13.0-beta.7 increment)
  - All future SessionStart-fired memory recovery work (the activation surface is now a proven idempotent edit pattern)

tech-stack:
  added: []
  patterns:
    - Idempotent hooks.json edit via Node JSON.parse + push + JSON.stringify(data, null, 2) + chr(10) (NEVER Edit-tool string surgery -- 95.2 B4 fix precedent)
    - Pre-edit duplicate-detection scan: arr.some(entry => entry.hooks.some(h => h.command.indexOf('restore-post-compact-context.cjs') !== -1))
    - Atomic write via fs.writeFileSync(tmp) + fs.renameSync(tmp, target) (POSIX atomic rename)

key-files:
  created: []
  modified:
    - hooks/hooks.json (+10 lines / -0 lines; new SessionStart entry block; zero whitespace drift outside the inserted block)

key-decisions:
  - "Substantive interpretation over literal length assertion: plan said length 8 -> 9; actual is 9 -> 10 because intervening commit 8752e3b (preflight-release-drift.cjs) landed between plan authoring (Wave-1 snapshot) and Wave-2 execution. Substance preserved: consumer is wired exactly once, idempotent, JSON valid, all 8 prior entries byte-stable."
  - "Smoke (Task 2) executed against the consumer artifact fetched from main into a tmp directory (verification only, NOT committed to worktree). Returned {continue:true} on Tier 0 cold start as designed. The wired command string + the actual consumer behavior together prove the hook chain entry-point is invokable end-to-end."
  - "No Edit / MultiEdit / sed / awk used on hooks.json. Pattern 3 (RESEARCH §189) honored: read-modify-write through Node only. Diff verified +10/-0 with no formatting churn outside the inserted block (Pitfall 6 §278 not triggered)."

patterns-established:
  - "Idempotent SessionStart entry append: scan-then-push-then-re-serialize; second invocation is a stable no-op (hash + mtime preserved)."
  - "Post-merge smoke deferral with fetch-from-main offline simulation: when a parallel-execution worktree lacks a load-bearing artifact that exists on main, fetch the artifact via 'git show main:path' into a tmp directory and run the smoke against it without mutating the worktree. Captures end-to-end signal without conflicting with the parallel branch's merge contract."

requirements-completed:
  - POSTCOMPACT-95.5-02

# Metrics
duration: ~2.5 min
completed: 2026-05-06
---

# Phase 95.5 Plan 03: Wire Post-Compact Memory Pipeline Consumer Summary

**hooks/hooks.json SessionStart array gains a parallel CJS entry invoking scripts/restore-post-compact-context.cjs (matcher `startup|clear|compact`, timeout 3000ms), activating the post-compact memory pipeline READ side; edit performed via idempotent Node JSON.parse + push + JSON.stringify(data, null, 2) + newline (95.2 B4 fix precedent), zero whitespace drift outside the +10-line insertion block.**

## Performance

- **Duration:** ~2.5 min (148 s)
- **Started:** 2026-05-06T22:23:28Z
- **Completed:** 2026-05-06T22:25:56Z
- **Tasks:** 2 (1 with file modification, 1 verification-only)
- **Files modified:** 1 (hooks/hooks.json; +10/-0)

## Accomplishments

- Activated the post-compact memory pipeline READ side (Wave 1 consumer was shipped to main but never fired -- this plan flips the switch).
- Honored Pattern 3 + Pitfall 6 strictly: zero Edit-tool string surgery, zero whitespace drift outside the new entry block, idempotent on re-run (hash + mtime stable).
- Substance preserved against a planner snapshot drift: plan asserted length 8 -> 9; intervening commit 8752e3b had bumped the array to length 9 between plan authoring and Wave 2 execution. Consumer is now wired exactly once at the canonical shape.

## Task Commits

Each task was committed atomically (parallel-executor `--no-verify` per orchestrator contract):

1. **Task 1: Append SessionStart entry via idempotent Node read-modify-write** -- `eb59744` (feat)
2. **Task 2: End-to-end SessionStart simulation** -- no commit (verification only; offline simulation against `git show main:scripts/restore-post-compact-context.cjs` returned `{"continue":true}` on Tier 0 cold start as designed)

**Plan metadata:** [final commit will follow this SUMMARY] (docs: complete plan)

## Files Created/Modified

- `hooks/hooks.json` -- appended SessionStart entry that invokes `scripts/restore-post-compact-context.cjs` (consumer ships on main via Wave 1; activation here completes the READ side of the post-compact memory pipeline). Diff is +10 / -0; the 8 prior entries are byte-stable (none reordered, none modified). JSON validity preserved; trailing newline preserved; 2-space indent preserved.

## Decisions Made

1. **Smoke routed through fetch-from-main when consumer absent in worktree.** This worktree was forked off `origin/main` BEFORE Wave 1 (95.5-00 / 95.5-01 / 95.5-02) landed on local main. So `scripts/restore-post-compact-context.cjs` exists on main but is NOT in this worktree branch. Rather than cherry-picking the consumer (which would create a duplicate on merge), Task 2 ran the smoke against the consumer fetched via `git show main:scripts/restore-post-compact-context.cjs` into `/tmp` with `CLAUDE_PLUGIN_ROOT` redirected to the tmp dir. Result: `{"continue":true}` on Tier 0 cold start as designed. End-to-end signal captured without conflicting with the parallel-branch merge contract.

2. **Length 9 -> 10 (not 8 -> 9 per stale plan).** Plan was authored against a Wave-1-pre snapshot. Between plan authoring and Wave-2 execution, commit `8752e3b feat(hooks): add SessionStart preflight-release-drift.cjs (9th entry)` landed. Substance preserved: consumer is wired exactly once with the canonical shape. Documented as deviation; orchestrator/verifier can read the +10/-0 diff to confirm zero drift outside the inserted block.

## Deviations from Plan

### Documented Issues (no auto-fix needed; substance honored)

**1. [Rule 3 -- Blocking, contextual] Length assertion drift (8 -> 9 vs actual 9 -> 10)**
- **Found during:** Task 1 pre-edit length check
- **Issue:** Plan's `<must_haves><truths>` clause asserts "SessionStart array length goes from 8 to 9 after this plan ships." Worktree pre-edit length was 9, not 8, due to intervening commit `8752e3b` that added `scripts/preflight-release-drift.cjs` between Wave-1 plan authoring and Wave-2 execution.
- **Fix:** Honored the SUBSTANTIVE intent (append the consumer entry exactly once, idempotent, JSON-clean, no whitespace churn). Documented the literal-vs-substantive split in the commit body and in this Summary.
- **Files modified:** hooks/hooks.json (+10 / -0; the +10 lines are exclusively the new entry block)
- **Verification:** Diff shows +10/-0 lines, zero formatting churn outside the inserted block. JSON.parse round-trips. Idempotency proven (re-run preserved hash + mtime). All 8 prior entries byte-stable.
- **Committed in:** `eb59744`

**2. [Rule 3 -- Blocking, contextual] Task 2 smoke routed through main snapshot**
- **Found during:** Task 2 consumer-presence guard
- **Issue:** `scripts/restore-post-compact-context.cjs` is on main (Wave 1 commits `38fe240` / `3487157` / `653943e`) but NOT in this worktree branch. Task 2 verify (per plan literal text) requires invoking the consumer end-to-end.
- **Fix:** Ran the smoke against the consumer fetched via `git show main:scripts/restore-post-compact-context.cjs` into a tmp directory with `CLAUDE_PLUGIN_ROOT` redirected to that tmp dir. NO file modifications committed to the worktree. Result: exit 0, stdout `{"continue":true}` (Tier 0 cold start envelope as designed by 95.5-02-SUMMARY's defensive posture: "any error path emits `{continue:true}`").
- **Files modified:** None (verification only)
- **Verification:** Smoke exit code 0; stdout JSON.parse succeeded; `env.continue === true` confirmed. The wired command string in `hooks/hooks.json` resolves to the consumer artifact-as-shipped-on-main, and that artifact returns the silent envelope as designed. End-to-end activation chain verified.
- **Committed in:** N/A (verification only)

**3. [Rule 4 candidate -- documented, not blocked] Plan verify regex chain bug (timeout grep)**
- **Found during:** Task 1 acceptance criteria run
- **Issue:** Plan's verify clause `grep -B 1 "restore-post-compact-context.cjs" hooks/hooks.json | grep -A 2 restore | grep -c '"timeout": 3000'` returns 0 because the `-A 2 restore` filter narrows the grep window such that the `"timeout": 3000` line is no longer captured. The regex chain is fragile across pipe stages.
- **Fix:** Substantive check via Node: extracted `arr[idx].hooks[0].timeout` directly, asserted `=== 3000`. PASS. Same for matcher (extracted `arr[idx].matcher`, asserted `=== "startup|clear|compact"`).
- **Files modified:** None (verify substitution)
- **Verification:** Substantive Node check returned the correct values; the `git diff hooks/hooks.json` shows `"timeout": 3000` and `"matcher": "startup|clear|compact"` in the inserted block.
- **Recommendation:** Future plans authoring grep-pipe verify chains over JSON should prefer Node node -e extraction (less brittle, more deterministic). Logged as a planner-discipline note; not an architectural change requiring user approval.

---

**Total deviations:** 3 documented (2 contextual blocking auto-fixed; 1 verify-script-discipline note).
**Impact on plan:** Substance fully honored. POSTCOMPACT-95.5-02 satisfied: consumer is wired into hook chain, idempotent, JSON valid, R1 byte-equal preserved, zero whitespace drift outside the inserted block, zero em-dashes, zero new dependencies.

## Issues Encountered

None. The drift between the plan's literal length assertion and the worktree's actual state was caught at Task 1's pre-edit check and routed through documented deviation handling (Rule 3 contextual blocking) rather than blocking the plan.

## Verification Trail

| Check | Plan literal | Actual | Status |
|------|------|------|--------|
| SessionStart length post-edit | 9 | 10 | PASS substantively (consumer wired exactly once; intervening preflight-release-drift.cjs accounts for the +1 baseline drift) |
| Consumer entry present (>=1) | yes | yes (count=1) | PASS |
| preflight-doctor.cjs preserved | yes | yes (count=1) | PASS |
| Other 7 prior entries preserved | each >=1 | each >=1 (7/7) | PASS |
| Timeout = 3000ms | yes | 3000 (Node-extracted) | PASS |
| Matcher = startup\|clear\|compact | yes | startup\|clear\|compact (Node-extracted) | PASS |
| JSON validity | yes | parse round-trips | PASS |
| Idempotency (no diff on re-run) | yes | hash + mtime stable across re-run | PASS |
| Whitespace integrity (~10-12 +lines) | yes | +10 / -0 | PASS |
| No em-dashes in hooks.json | yes | 0 occurrences | PASS |
| R1 byte-equal preserved (lib/hmi/shape-f6-renderer.cjs) | yes | sha256=1792535860abc791222bf0ecf59599d66e49ad9cc1606b3d8679fca2922150cf | PASS |
| Smoke: expanded command exits 0 + emits {continue:true} | yes (in-worktree) | yes (offline simulation against main snapshot; consumer absent in worktree, on main via Wave 1) | PASS deferred |

## Next Phase Readiness

- **95.5-04** (test suite rewrite): consumer is now wired in hooks.json AND ships in main; the rewritten 9-scenario regression suite has a real activation surface to assert against.
- **95.5-05** (release plumbing): the closed READ + WRITE post-compact memory pipeline can ship as a v1.13.0-beta.7 increment (or rolled into a beta.6 hotfix follow-up per CONTEXT.md milestone scheduling).
- **Merge contract:** This worktree branch will merge onto local main. Main currently has 10 SessionStart entries (8th preflight-doctor.cjs, 9th preflight-release-drift.cjs, 10th preflight-auto-explore.cjs). After merge: array gains the 11th entry (this plan's consumer wiring). The merge is conflict-free on `hooks/hooks.json` because the inserted block is appended at the end of the SessionStart array and main's tail differs (main has preflight-auto-explore.cjs at position 10 which this branch lacks). Conflict resolution will need to retain BOTH preflight-auto-explore.cjs (from main) AND restore-post-compact-context.cjs (from this branch) -- both are independent SessionStart entries that should coexist. Orchestrator should merge by union with array length 11.

## Self-Check: PASSED

- hooks/hooks.json modified: VERIFIED (`git diff` shows +10/-0)
- Commit eb59744 exists: VERIFIED (`git log --oneline | grep eb59744`)
- R1 invariant intact: VERIFIED (sha256 matches expected)
- POSTCOMPACT-95.5-02 satisfied: consumer wired, idempotent, JSON valid, end-to-end smoke against main snapshot returned {continue:true} on Tier 0 cold start

---
*Phase: 95.5-post-compact-memory-pipeline-consumer*
*Plan: 03*
*Completed: 2026-05-06*
