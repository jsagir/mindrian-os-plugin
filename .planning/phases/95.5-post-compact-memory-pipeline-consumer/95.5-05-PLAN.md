---
phase: 95.5-post-compact-memory-pipeline-consumer
plan: 05
type: execute
wave: 3
depends_on:
  - 95.5-00
  - 95.5-01
  - 95.5-02
  - 95.5-03
  - 95.5-04
files_modified:
  - CHANGELOG.md
  - .claude-plugin/plugin.json
  - package.json
autonomous: false
requirements:
  - POSTCOMPACT-95.5-01
  - POSTCOMPACT-95.5-02
  - POSTCOMPACT-95.5-03
  - POSTCOMPACT-95.5-04
  - POSTCOMPACT-95.5-05
canon_parts: [Part 6, Part 7, Part 8]

must_haves:
  truths:
    - "CHANGELOG.md gets a new `## [1.13.0-beta.7]` entry at the top, above the existing `## [1.13.0-beta.6]` entry, dated 2026-05-06 (or the actual ship day)."
    - ".claude-plugin/plugin.json `version` field is `1.13.0-beta.7`."
    - "package.json `version` field is `1.13.0-beta.7`."
    - "LOCAL git tag `v1.13.0-beta.7` exists pointing at the release commit (NOT pushed -- release-process.md beta gating)."
    - "Marketplace ref-pin DEFERRED for 95.5 (forward-protective hotfix; documented in CHANGELOG Audit Notes per 95.2 precedent)."
    - "5-gate version sync complete (CHANGELOG + plugin.json + package.json + LOCAL tag) -- only Gate 5 (marketplace ref-pin) deferred."
    - "Phase 91 Feynman runner HARD STOP: zero NEW failures matching FAIL.*(post-compact|restore-post-compact|triple-context)."
    - "R1 byte-equal preserved on lib/hmi/shape-f6-renderer.cjs (sha256 = 1792535860abc791222bf0ecf59599d66e49ad9cc1606b3d8679fca2922150cf) PRE and POST release commit."
    - "Zero Unicode em-dashes in CHANGELOG entry (Unicode U+2014 / U+2013 -- ASCII `--` is allowed)."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Release notes for v1.13.0-beta.7"
      contains: "1.13.0-beta.7"
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest with bumped version"
      contains: "1.13.0-beta.7"
    - path: "package.json"
      provides: "Node package manifest with bumped version"
      contains: "1.13.0-beta.7"
  key_links:
    - from: "CHANGELOG.md"
      to: ".claude-plugin/plugin.json"
      via: "version string match"
      pattern: "1.13.0-beta.7"
    - from: "CHANGELOG.md"
      to: "package.json"
      via: "version string match"
      pattern: "1.13.0-beta.7"
    - from: "git tag v1.13.0-beta.7"
      to: "release commit"
      via: "git tag pointing at HEAD"
      pattern: "v1.13.0-beta.7"
---

<objective>
Ship Phase 95.5 as v1.13.0-beta.7 with the 5-gate version sync per `.claude/includes/release-process.md`.

Pre-conditions verified before this plan starts:
- Plans 95.5-00..95.5-04 all shipped + committed.
- `node lib/memory/post-compact-reinjection.test.cjs` exits 0 with `9/9 passed`.
- Full Feynman runner HARD STOP gate clean.
- `hooks/hooks.json` SessionStart array length === 9.
- R1 byte-equal preserved.

Gates:
1. CHANGELOG.md `## [1.13.0-beta.7]` entry at the top.
2. .claude-plugin/plugin.json `version` === `1.13.0-beta.7`.
3. package.json `version` === `1.13.0-beta.7`.
4. LOCAL git tag `v1.13.0-beta.7` (NOT pushed -- release-process.md beta gating).
5. Marketplace ref-pin DEFERRED (forward-protective hotfix; precedent: 95.2 Audit Notes).

Per 95.2 release pattern (CHANGELOG.md:13-25): release infrastructure ships as beta first; marketplace ref-pin deferred for forward-protective hotfixes since users in stale-install state recover via existing cache.

Purpose: Make the post-compact memory pipeline available to beta testers. Validation week confirms (a) Larry references pre-compact MINTO content after auto-compact, (b) cross-room HARD SKIP doesn't leak (Canon Part 8), (c) Tier 0 / Cowork surfaces don't break.
Output: 4 file modifications + 1 LOCAL tag; no GitHub push, no marketplace bump (deferred).

This plan has a `checkpoint:human-verify` task at the end so the user can authorize the release commit + tag (autonomy: false at the plan level).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/includes/release-process.md
@.planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-CONTEXT.md
@.planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-RESEARCH.md
@CHANGELOG.md
@.claude-plugin/plugin.json
@package.json

<interfaces>
<!-- CURRENT release state (pre-plan) -->
<!-- CHANGELOG.md head: ## [1.13.0-beta.6] - 2026-05-06 (Phase 95.2 ship) -->
<!-- .claude-plugin/plugin.json: { "version": "1.13.0-beta.6" } -->
<!-- package.json: { "version": "1.13.0-beta.6" } -->
<!-- git tag: v1.13.0-beta.6 (LOCAL, per 95.2) -->

<!-- TARGET release state (post-plan) -->
<!-- CHANGELOG.md head: ## [1.13.0-beta.7] - 2026-05-06 -->
<!-- .claude-plugin/plugin.json: { "version": "1.13.0-beta.7" } -->
<!-- package.json: { "version": "1.13.0-beta.7" } -->
<!-- git tag: v1.13.0-beta.7 (LOCAL, NOT pushed) -->

<!-- CHANGELOG entry shape (95.2 precedent at CHANGELOG.md:13-25) -->
## [1.13.0-beta.7] - 2026-05-06
### Fixed
- Post-compact memory pipeline consumer wiring (Phase 95.5): closes the half-wired pipeline shipped in v1.12.0 (Phase 95-04 wrote the side-channel file but no consumer existed). Now `scripts/restore-post-compact-context.cjs` (NEW, ~150 lines, per D-01) reads `<roomDir>/.mindrian/last-post-compact.md` at SessionStart, validates the YAML frontmatter stamp (D-04 source_room_path + source_room_slug + written_at) against the active room from `~/MindrianRooms/.rooms/registry.json`, and re-injects TRIPLE_CONTEXT via `hookSpecificOutput.additionalContext` so Larry wakes up after auto-compact aware of pre-compact MINTO content. Cross-room mismatch triggers HARD SKIP + forensic-rename to `.last-post-compact-cross-room-skip-<ISO>.md` (Canon Part 8). Successful consume forensic-renames to `.last-post-compact-consumed-<ISO>.md` so subsequent re-runs do not re-inject stale post-compact context. Stale files (mtime >600s) are skipped + deleted (matches scripts/post-compact's existing 600s threshold).

### Added
- `scripts/post-compact` D-04 frontmatter stamp back-port (Phase 95.5 Plan 01): the WRITE side now prepends `source_room_path` + `source_room_slug` + `written_at` + `schema_version: 1` to `<roomDir>/.mindrian/last-post-compact.md`. Atomic mktemp + mv -f preserved. macOS date fallback chain handles `%3N` unsupported case.
- `lib/memory/post-compact-reinjection.test.cjs` 9-scenario rewrite (Phase 95.5 Plan 04): D-05 contract -- write-side file presence + body byte-identity + frontmatter stamp + read-side happy path + staleness skip+delete + cross-room HARD SKIP + post-consume forensic preserve + Tier 0 silence + byte-identity invariant. Replaces the deprecated 7/9-failing stdout-shape contract from Phase 88-09.
- `hooks/hooks.json` SessionStart entry #9 (Phase 95.5 Plan 03): wires `scripts/restore-post-compact-context.cjs` with matcher `startup|clear|compact` and timeout 3000ms (matching PostCompact 88-09 invariant).

### Provenance
- Triangulation: closes the gap left by Phase 95-04 (write-side ship 2026-04-29; consumer deferred to Phase 95.5+).
- Reuse before build per Canon Part 7: extends `scripts/preflight-doctor.cjs` template (95.2 Finding C precedent), reuses `lib/memory/triple-context-formatter.cjs::formatTripleContext` (Phase 88-07 single source of truth), reuses `lib/core/folder-memory.cjs::getCurrentRoom` + `readTriple` (Phase 88-01 + 94-01).
- Graph boundary per Canon Part 8: zero network surface (no fetch, no http, no Brain MCP). Cross-room HARD SKIP enforces per-room memory locality (Canon Part 9 forward-reference).
- Dog-fooded per Canon Part 6: SessionStart hook chain length 8 -> 9 verified locally; consumer smoke-tested against synthesized fixture before commit.

### Audit Notes
- Marketplace ref-pin (Gate 5) DEFERRED: forward-protective hotfix; users on v1.13.0-beta.6 already have the WRITE-side stamp absent + side-channel file accumulating, so the READ-side consumer is purely additive (closing the half-wired gap). No regression risk to existing installs.
- Three-surface validation: SessionStart hooks fire on Claude Code CLI natively + Desktop natively (CC 2.x); Cowork shared `00_Context/` per-user-local assumption preserved (RESEARCH §10 -- Q3 deferred to Cowork tester smoke).
- R1 byte-equal preserved on lib/hmi/shape-f6-renderer.cjs (sha256 = 1792535860abc791222bf0ecf59599d66e49ad9cc1606b3d8679fca2922150cf).
- Phase 91 Feynman runner: zero NEW failures matching FAIL.*(post-compact|restore-post-compact|triple-context). Existing 9/9 GREEN baseline.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version in plugin.json + package.json</name>
  <files>.claude-plugin/plugin.json, package.json</files>

  <read_first>
    - Read .claude-plugin/plugin.json (confirm current `version` is `1.13.0-beta.6`).
    - Read package.json (confirm current `version` is `1.13.0-beta.6`).
    - Read .claude/includes/release-process.md "Version Consistency Rule" -- 5-gate sync mandate.
  </read_first>

  <behavior>
    - Bump both files' `version` field from `1.13.0-beta.6` to `1.13.0-beta.7`.
    - No other field changes.
    - JSON validity preserved.
    - 2-space indent + trailing newline preserved (existing convention).
  </behavior>

  <action>
    Use Edit tool with old_string `"version": "1.13.0-beta.6"` and new_string `"version": "1.13.0-beta.7"` against both files. Each file has exactly one occurrence of that string at line 3 (verified via prior `grep -n` from /home/jsagi/MindrianOS-Plugin/.claude-plugin/plugin.json:3 and package.json:3).

    Do NOT modify any other field. Do NOT use Node read-modify-write here (the JSON files are stable in shape; Edit is appropriate for a single-string-replace and matches the lighter-weight 95.2 release pattern).

    Use ASCII `--` only (the version string itself uses ASCII `-`).
  </action>

  <acceptance_criteria>
    - [ ] `[[ "$PWD" == "/home/jsagi/MindrianOS-Plugin" ]]` (workspace guard).
    - [ ] `node -e "console.log(require('./.claude-plugin/plugin.json').version)"` outputs `1.13.0-beta.7`.
    - [ ] `node -e "console.log(require('./package.json').version)"` outputs `1.13.0-beta.7`.
    - [ ] Both files parse cleanly as JSON (no syntax errors).
    - [ ] No other field modified: `git diff .claude-plugin/plugin.json package.json | grep -E "^[+-]" | grep -v "^[+-]{3}" | wc -l` returns exactly 4 (one + and one - per file).
    - [ ] R1 byte-equal preserved.
  </acceptance_criteria>

  <verify>
    <automated>
    [[ "$PWD" == "/home/jsagi/MindrianOS-Plugin" ]] || { echo "WORKSPACE GUARD FAILED"; exit 1; }
    [ "$(node -e "console.log(require('./.claude-plugin/plugin.json').version)")" = "1.13.0-beta.7" ] &&
    [ "$(node -e "console.log(require('./package.json').version)")" = "1.13.0-beta.7" ] &&
    node -e "JSON.parse(require('fs').readFileSync('.claude-plugin/plugin.json','utf8'))" &&
    node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))" &&
    [ "$(sha256sum lib/hmi/shape-f6-renderer.cjs | cut -d' ' -f1)" = "1792535860abc791222bf0ecf59599d66e49ad9cc1606b3d8679fca2922150cf" ]
    </automated>
  </verify>

  <done>Both manifests at 1.13.0-beta.7, JSON valid, no field churn, R1 invariant intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add CHANGELOG.md entry for v1.13.0-beta.7</name>
  <files>CHANGELOG.md</files>

  <read_first>
    - Read CHANGELOG.md head section (lines 1-30) to understand existing 1.13.0-beta.6 entry shape.
    - Read 95.5-CONTEXT.md "Acceptance Criteria" section.
    - Read .claude/includes/release-process.md "How Users Actually Receive Updates" + the `## [X.Y.Z]` template.
  </read_first>

  <behavior>
    - Insert new `## [1.13.0-beta.7] - 2026-05-06` entry at the top of CHANGELOG.md, ABOVE the existing `## [1.13.0-beta.6]` entry.
    - Entry follows the 95.2 precedent (CHANGELOG.md:13-25) with sections: ### Fixed, ### Added, ### Provenance, ### Audit Notes.
    - Entry MUST mention: post-compact pipeline consumer + D-04 frontmatter stamp + 9-scenario test rewrite + hooks.json entry #9 + Canon Part 8 cross-room HARD SKIP + R1 preservation + marketplace ref-pin DEFERRED.
    - Zero Unicode em-dashes (Unicode U+2014 / U+2013); ASCII `--` is allowed.
  </behavior>

  <action>
    Use Edit tool with old_string anchored at the existing first version header.

    Find the line `## [1.13.0-beta.6] - 2026-05-06` (CHANGELOG.md:13). Use it as the anchor for an Edit that inserts the new entry above it (old_string = `## [1.13.0-beta.6] - 2026-05-06`, new_string = the full new 95.5 block + blank line + `## [1.13.0-beta.6] - 2026-05-06`).

    The new entry content (use the full content from the `<interfaces>` block above as the body -- already drafted in this plan):

    ```markdown
    ## [1.13.0-beta.7] - 2026-05-06

    ### Fixed
    - Post-compact memory pipeline consumer wiring (Phase 95.5): closes the half-wired pipeline shipped in v1.12.0 (Phase 95-04 wrote the side-channel file but no consumer existed). Now `scripts/restore-post-compact-context.cjs` (NEW, ~150 lines, per D-01) reads `<roomDir>/.mindrian/last-post-compact.md` at SessionStart, validates the YAML frontmatter stamp (D-04 source_room_path + source_room_slug + written_at) against the active room from `~/MindrianRooms/.rooms/registry.json`, and re-injects TRIPLE_CONTEXT via `hookSpecificOutput.additionalContext` so Larry wakes up after auto-compact aware of pre-compact MINTO content. Cross-room mismatch triggers HARD SKIP + forensic-rename to `.last-post-compact-cross-room-skip-<ISO>.md` (Canon Part 8). Successful consume forensic-renames to `.last-post-compact-consumed-<ISO>.md` so subsequent re-runs do not re-inject stale post-compact context. Stale files (mtime >600s) are skipped + deleted (matches scripts/post-compact's existing 600s threshold).

    ### Added
    - `scripts/post-compact` D-04 frontmatter stamp back-port (Phase 95.5 Plan 01): the WRITE side now prepends `source_room_path` + `source_room_slug` + `written_at` + `schema_version: 1` to `<roomDir>/.mindrian/last-post-compact.md`. Atomic mktemp + mv -f preserved. macOS date fallback chain handles `%3N` unsupported case.
    - `lib/memory/post-compact-reinjection.test.cjs` 9-scenario rewrite (Phase 95.5 Plan 04): D-05 contract -- write-side file presence + body byte-identity + frontmatter stamp + read-side happy path + staleness skip+delete + cross-room HARD SKIP + post-consume forensic preserve + Tier 0 silence + byte-identity invariant. Replaces the deprecated 7/9-failing stdout-shape contract from Phase 88-09.
    - `hooks/hooks.json` SessionStart entry #9 (Phase 95.5 Plan 03): wires `scripts/restore-post-compact-context.cjs` with matcher `startup|clear|compact` and timeout 3000ms (matching PostCompact 88-09 invariant).

    ### Provenance
    - Triangulation: closes the gap left by Phase 95-04 (write-side ship 2026-04-29; consumer deferred to Phase 95.5+).
    - Reuse before build per Canon Part 7: extends `scripts/preflight-doctor.cjs` template (95.2 Finding C precedent), reuses `lib/memory/triple-context-formatter.cjs::formatTripleContext` (Phase 88-07 single source of truth), reuses `lib/core/folder-memory.cjs::getCurrentRoom` + `readTriple` (Phase 88-01 + 94-01).
    - Graph boundary per Canon Part 8: zero network surface (no fetch, no http, no Brain MCP). Cross-room HARD SKIP enforces per-room memory locality (Canon Part 9 forward-reference).
    - Dog-fooded per Canon Part 6: SessionStart hook chain length 8 -> 9 verified locally; consumer smoke-tested against synthesized fixture before commit.

    ### Audit Notes
    - Marketplace ref-pin (Gate 5) DEFERRED: forward-protective hotfix; users on v1.13.0-beta.6 already have the WRITE-side stamp absent + side-channel file accumulating, so the READ-side consumer is purely additive (closing the half-wired gap). No regression risk to existing installs.
    - Three-surface validation: SessionStart hooks fire on Claude Code CLI natively + Desktop natively (CC 2.x); Cowork shared `00_Context/` per-user-local assumption preserved (RESEARCH section 10 -- Q3 deferred to Cowork tester smoke).
    - R1 byte-equal preserved on lib/hmi/shape-f6-renderer.cjs (sha256 = 1792535860abc791222bf0ecf59599d66e49ad9cc1606b3d8679fca2922150cf).
    - Phase 91 Feynman runner: zero NEW failures matching FAIL.*(post-compact|restore-post-compact|triple-context). Existing 9/9 GREEN baseline.

    ## [1.13.0-beta.6] - 2026-05-06
    ```

    Critical: triple-check the entry uses ASCII `--` and `-` only -- NO Unicode em-dashes. Run the em-dash grep gate immediately after edit.
  </action>

  <acceptance_criteria>
    - [ ] `[[ "$PWD" == "/home/jsagi/MindrianOS-Plugin" ]]` (workspace guard).
    - [ ] `head -20 CHANGELOG.md | grep -c "## \[1.13.0-beta.7\]"` returns 1.
    - [ ] `grep -c "## \[1.13.0-beta.6\]" CHANGELOG.md` returns 1 (preserved, not duplicated).
    - [ ] `grep -c "## \[1.13.0-beta.7\]" CHANGELOG.md` returns 1 (only new entry).
    - [ ] `grep -A 30 "## \[1.13.0-beta.7\]" CHANGELOG.md | grep -c "Phase 95.5"` returns >= 1.
    - [ ] CHANGELOG entry mentions all 4 sub-deliverables: `grep -A 50 "## \[1.13.0-beta.7\]" CHANGELOG.md | grep -c "post-compact\|restore-post-compact\|D-04\|hooks.json"` returns >= 4.
    - [ ] `grep -P '[\x{2014}\x{2013}]' CHANGELOG.md | wc -l` returns 0 (Unicode em-dash check across whole file -- confirms new entry doesn't introduce them).
    - [ ] R1 byte-equal preserved.
  </acceptance_criteria>

  <verify>
    <automated>
    [[ "$PWD" == "/home/jsagi/MindrianOS-Plugin" ]] || { echo "WORKSPACE GUARD FAILED"; exit 1; }
    [ "$(head -20 CHANGELOG.md | grep -c '## \[1.13.0-beta.7\]')" -eq 1 ] &&
    [ "$(grep -c '## \[1.13.0-beta.7\]' CHANGELOG.md)" -eq 1 ] &&
    [ "$(grep -c '## \[1.13.0-beta.6\]' CHANGELOG.md)" -eq 1 ] &&
    [ "$(grep -A 50 '## \[1.13.0-beta.7\]' CHANGELOG.md | grep -c 'post-compact\|restore-post-compact\|D-04\|hooks.json')" -ge 4 ] &&
    [ "$(grep -F -e '--' -e '--' CHANGELOG.md | wc -l)" -eq 0 ] &&
    [ "$(sha256sum lib/hmi/shape-f6-renderer.cjs | cut -d' ' -f1)" = "1792535860abc791222bf0ecf59599d66e49ad9cc1606b3d8679fca2922150cf" ]
    </automated>
  </verify>

  <done>CHANGELOG entry for v1.13.0-beta.7 added at top, prior beta.6 entry preserved, all 4 sub-deliverables mentioned, no em-dashes, R1 invariant intact.</done>
</task>

<task type="auto">
  <name>Task 3: Pre-release verification gate (test suite + Feynman + R1 + zero-network audit)</name>
  <files>(no file modifications -- verification only)</files>

  <read_first>
    - Read 95.5-CONTEXT.md critical_invariants #4 (R1 invariant) + #5 (Feynman HARD STOP) + #1 (workspace guard).
  </read_first>

  <behavior>
    - Run all gating checks BEFORE the commit + tag step. If any fail, the release does NOT proceed (exit non-zero, surface the failure in checkpoint Task 4).
  </behavior>

  <action>
    Run the comprehensive pre-release verification:

    1. Workspace guard: `[[ "$PWD" == "/home/jsagi/MindrianOS-Plugin" ]]`.
    2. Test file 9/9 GREEN: `node lib/memory/post-compact-reinjection.test.cjs` exits 0 with `9/9 passed`.
    3. Feynman runner HARD STOP: `node lib/memory/run-feynman-tests.cjs` runs; grep gate clean.
    4. R1 byte-equal: `sha256sum lib/hmi/shape-f6-renderer.cjs` returns the locked sha256.
    5. Anti-pattern audit on consumer:
       - `grep -E "require\(.*room-db" scripts/restore-post-compact-context.cjs | wc -l` returns 0.
       - `grep -E "brain-client|fetch|http|curl|brain.mindrian|tavily" scripts/restore-post-compact-context.cjs | wc -l` returns 0.
    6. Anti-pattern audit on test file: `grep -P '[\x{2014}\x{2013}]' lib/memory/post-compact-reinjection.test.cjs` returns 0.
    7. Anti-pattern audit on hooks.json: `grep -P '[\x{2014}\x{2013}]' hooks/hooks.json` returns 0.
    8. Anti-pattern audit on scripts/post-compact: `grep -P '[\x{2014}\x{2013}]' scripts/post-compact` returns 0.
    9. Anti-pattern audit on CHANGELOG entry: `grep -P '[\x{2014}\x{2013}]' CHANGELOG.md` returns 0.
    10. Version sync: plugin.json + package.json both report `1.13.0-beta.7`.
    11. SessionStart array length === 9.
  </action>

  <acceptance_criteria>
    - [ ] Workspace guard passes.
    - [ ] 9/9 tests GREEN in lib/memory/post-compact-reinjection.test.cjs.
    - [ ] Feynman runner HARD STOP grep gate clean.
    - [ ] R1 sha256 matches `1792535860abc791222bf0ecf59599d66e49ad9cc1606b3d8679fca2922150cf`.
    - [ ] Zero anti-pattern matches on consumer (room-db + network).
    - [ ] Zero Unicode em-dashes in any 95.5 deliverable.
    - [ ] plugin.json + package.json both at `1.13.0-beta.7`.
    - [ ] SessionStart array length === 9.
  </acceptance_criteria>

  <verify>
    <automated>
    [[ "$PWD" == "/home/jsagi/MindrianOS-Plugin" ]] || { echo "WORKSPACE GUARD FAILED"; exit 1; }
    set -e
    node lib/memory/post-compact-reinjection.test.cjs > /tmp/95.5-05-test.log 2>&1 &&
    grep -q "9/9 passed" /tmp/95.5-05-test.log
    node lib/memory/run-feynman-tests.cjs > /tmp/95.5-05-feynman.log 2>&1 || true
    DOCTOR_FAILS=$(grep -E "FAIL.*(post-compact|restore-post-compact|triple-context)" /tmp/95.5-05-feynman.log | wc -l)
    [ "$DOCTOR_FAILS" -eq 0 ] &&
    [ "$(sha256sum lib/hmi/shape-f6-renderer.cjs | cut -d' ' -f1)" = "1792535860abc791222bf0ecf59599d66e49ad9cc1606b3d8679fca2922150cf" ] &&
    [ "$(grep -E 'require\(.*room-db' scripts/restore-post-compact-context.cjs | wc -l)" -eq 0 ] &&
    [ "$(grep -E 'brain-client|fetch|http|curl|brain.mindrian|tavily' scripts/restore-post-compact-context.cjs | wc -l)" -eq 0 ] &&
    [ "$(grep -F -e '--' -e '--' lib/memory/post-compact-reinjection.test.cjs | wc -l)" -eq 0 ] &&
    [ "$(grep -F -e '--' -e '--' hooks/hooks.json | wc -l)" -eq 0 ] &&
    [ "$(grep -F -e '--' -e '--' scripts/post-compact | wc -l)" -eq 0 ] &&
    [ "$(grep -F -e '--' -e '--' scripts/restore-post-compact-context.cjs | wc -l)" -eq 0 ] &&
    [ "$(grep -F -e '--' -e '--' CHANGELOG.md | wc -l)" -eq 0 ] &&
    [ "$(node -e "console.log(require('./.claude-plugin/plugin.json').version)")" = "1.13.0-beta.7" ] &&
    [ "$(node -e "console.log(require('./package.json').version)")" = "1.13.0-beta.7" ] &&
    [ "$(node -e "console.log(JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8')).hooks.SessionStart.length)")" = "9" ]
    </automated>
  </verify>

  <done>All 11 pre-release gates pass; release is ready to commit + tag.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the release commit + LOCAL tag (5-gate sync, marketplace deferred)</name>
  <what-built>
    Phase 95.5 deliverables ready to commit as v1.13.0-beta.7:
    - scripts/post-compact (D-04 stamp back-port, Plan 01)
    - scripts/restore-post-compact-context.cjs (NEW consumer, Plan 02)
    - hooks/hooks.json (SessionStart entry #9, Plan 03)
    - lib/memory/post-compact-reinjection.test.cjs (9-scenario rewrite, Plans 00 + 04)
    - tests/test-95.5-00-scaffold.sh (Wave-0 scaffold harness, Plan 00)
    - CHANGELOG.md (v1.13.0-beta.7 entry)
    - .claude-plugin/plugin.json (version bumped to 1.13.0-beta.7)
    - package.json (version bumped to 1.13.0-beta.7)

    All pre-release gates passed (Task 3): 9/9 tests, Feynman HARD STOP clean, R1 invariant intact, zero em-dashes, zero network surface in consumer, version sync complete.
  </what-built>

  <how-to-verify>
    1. Visually inspect `git status` and `git diff` to confirm only the 8 expected files changed (plus the 5 PLAN.md + SUMMARY.md files from each Plan-XX-SUMMARY).
    2. Run `head -50 CHANGELOG.md` to read the v1.13.0-beta.7 entry top-to-bottom; confirm:
       - Date is today's date (or release day).
       - All 4 sub-deliverables mentioned (post-compact + restore-post-compact + D-04 + hooks.json).
       - Marketplace ref-pin DEFERRED note present.
       - R1 sha256 mentioned.
       - Three-surface caveat noted.
       - No em-dashes (Unicode).
    3. Run `node -e "console.log(require('./.claude-plugin/plugin.json').version, require('./package.json').version)"` and confirm output is `1.13.0-beta.7 1.13.0-beta.7`.
    4. After approval, run the commit + tag pair:
       ```bash
       git add CHANGELOG.md .claude-plugin/plugin.json package.json scripts/post-compact scripts/restore-post-compact-context.cjs hooks/hooks.json lib/memory/post-compact-reinjection.test.cjs tests/test-95.5-00-scaffold.sh .planning/phases/95.5-post-compact-memory-pipeline-consumer/
       git commit -m "release: v1.13.0-beta.7 -- Phase 95.5 post-compact memory pipeline consumer"
       git tag v1.13.0-beta.7
       ```
       (Use HEREDOC for commit message if multi-line is preferred; the one-liner above is fine for a beta release.)
    5. Confirm: `git tag -l v1.13.0-beta.7` returns exactly `v1.13.0-beta.7`.
    6. Do NOT push. The `git push origin main --tags` step is the user's call after validation week.
    7. Confirm: marketplace.json at `~/mindrian-marketplace/.claude-plugin/marketplace.json` is NOT updated (Gate 5 DEFERRED per CHANGELOG Audit Notes).
    8. Final R1 check: `sha256sum lib/hmi/shape-f6-renderer.cjs | cut -d' ' -f1` returns `1792535860abc791222bf0ecf59599d66e49ad9cc1606b3d8679fca2922150cf`.
  </how-to-verify>

  <resume-signal>Type "approved" to proceed with commit + LOCAL tag, or describe blockers (e.g., CHANGELOG wording adjustment, version bump conflict, etc.)</resume-signal>
</task>

</tasks>

<verification>
Plan 95.5-05 ships when:
1. CHANGELOG.md `## [1.13.0-beta.7]` entry at top.
2. plugin.json `version` === `1.13.0-beta.7`.
3. package.json `version` === `1.13.0-beta.7`.
4. LOCAL git tag `v1.13.0-beta.7` exists pointing at the release commit.
5. Marketplace ref-pin DEFERRED (no edit to `~/mindrian-marketplace/.claude-plugin/marketplace.json`).
6. All pre-release gates passed: 9/9 test file GREEN, Feynman HARD STOP clean, R1 invariant intact, zero anti-patterns, zero Unicode em-dashes.
7. Human-verify checkpoint approved.
</verification>

<success_criteria>
- 5-gate version sync complete (Gates 1-4); Gate 5 (marketplace ref-pin) explicitly DEFERRED with audit-note rationale.
- POSTCOMPACT-95.5-01..05 all reference shipped functionality in the CHANGELOG entry.
- Phase 91 Feynman runner HARD STOP clean.
- R1 byte-equal preserved across the entire phase.
- Zero Unicode em-dashes in any 95.5 deliverable.
- Canon Part 6 (dog-fooding mandate): release is committed from /home/jsagi/MindrianOS-Plugin/ workspace, NOT from the plugin install cache.
- Canon Part 7 (reuse before build): release entry explicitly cites the reused modules (preflight-doctor.cjs template + triple-context-formatter + folder-memory + 95.2 hooks.json idempotent pattern).
- Canon Part 8 (graph boundary): zero network surface in the consumer; cross-room HARD SKIP test confirms boundary enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-05-SUMMARY.md` documenting:
- 5-gate sync result (4/5 complete; Gate 5 DEFERRED with rationale).
- Pre-release gate results (test file 9/9, Feynman HARD STOP, R1 invariant, anti-patterns).
- Commit hash + tag confirmation.
- Audit notes echoing the CHANGELOG entry.
- Validation week tester instructions:
  - "After auto-compact in your active room, run a Larry message and confirm Larry references pre-compact MINTO content (e.g., governing thoughts of sections you discussed before the compact)."
  - "Verify `<roomDir>/.mindrian/.last-post-compact-consumed-*.md` files exist after auto-compact (forensic preserve working)."
  - "Switch rooms and confirm cross-room HARD SKIP: no leak of room A's TRIPLE_CONTEXT into room B's session-start."
  - "Tier 0 smoke: open Claude Code in a tmpdir with no room and confirm the consumer is silent (no TRIPLE_CONTEXT injected, no errors)."
  - "Cowork tester: confirm SessionStart fires natively on Cowork; verify per-user-local `.mindrian/` assumption (RESEARCH §10 Q3)."

After SUMMARY ships, also update `.planning/STATE.md` to mark Phase 95.5 as `status: shipped` + `version: 1.13.0-beta.7` + `tag: v1.13.0-beta.7 (LOCAL)`.

After SUMMARY ships, also update `docs/CANON-PHASE-MAP.md` to add Phase 95.5 row(s) under Part 7 (Reuse Before Build) + Part 8 (Graph Boundary) + Part 9 forward-reference (memory locality enforced via cross-room HARD SKIP).

Validation week sign-off path:
1. Beta tester runs auto-compact in active room.
2. Confirms Larry references pre-compact MINTO content.
3. Confirms cross-room HARD SKIP behavior.
4. Confirms forensic preserve files accumulate (Phase 95.6 housekeeping deferred).
5. Confirms three-surface (CLI + Desktop + Cowork) -- Cowork verifier reports per-user-local `.mindrian/` assumption holds.
6. After all 5 confirmations, the user authorizes `git push origin main --tags` (NOT in scope of this plan).
</output>
