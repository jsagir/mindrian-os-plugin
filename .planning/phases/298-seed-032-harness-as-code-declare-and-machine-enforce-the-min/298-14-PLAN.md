---
phase: 298
plan: 14
type: execute
wave: 5
depends_on: [298-09, 298-12]
files_modified:
  - scripts/hooks/pre-commit-room-minto-guard.sh
  - scripts/hooks/pre-commit
  - tests/test-harness-manifest-precommit-wiring.cjs
  - tests/test-harness-167-verdict.cjs
  - lib/core/recipe-maps.cjs
autonomous: true
requirements: [R-10, R-11]

must_haves:
  truths:
    - "Staging a file under data/harness-policies/ or data/harness-fixtures/ now fires the pre-commit drift guard, so the manifest's policies digest cannot silently rot (Pitfall 6)"
    - "The two byte-identical hook files stay byte-identical after the widening, proven with cmp -s"
    - "R-11: the two pre-existing red manifest-cluster tests are repaired by repointing them at the live canonical hook, so requirement 11 becomes literally true rather than aspirational"
    - "R-10: lib/core/recipe-maps.cjs exposes policies through loadManifest() and its three read-joins are byte-unchanged"
    - "R-10: data/command-registry.json is byte-unchanged, so the T-side sync payload is untouched"
  artifacts:
    - path: "scripts/hooks/pre-commit-room-minto-guard.sh"
      provides: "The canonical hook with the widened drift trigger"
      contains: "harness-policies"
    - path: "lib/core/recipe-maps.cjs"
      provides: "loadManifest() exposing the policies key with a matching catch degrade"
      contains: "policies"
  key_links:
    - from: "scripts/hooks/pre-commit-room-minto-guard.sh"
      to: "scripts/build-harness-manifest.cjs --check"
      via: "the widened staged-path alternation"
      pattern: "harness-policies"
    - from: "tests/test-harness-manifest-precommit-wiring.cjs"
      to: "scripts/hooks/pre-commit-room-minto-guard.sh"
      via: "the repointed template path constant"
      pattern: "pre-commit-room-minto-guard"
---

<objective>
Close the drift gap the phase exists to close: widen the pre-commit trigger to fire on policy and
fixture edits, repoint the two manifest-cluster tests at the hook file that actually carries the
guard, and expose `policies` through `recipe-maps.cjs`.

Purpose: Pitfall 6 plus R-10 plus R-11. Today the drift guard's path alternation lists five exact
paths and none is the policy directory, so a policy could land with a stale manifest. Meanwhile
two tests assert the guard lives in a file Phase 235-01 emptied, so they prove nothing and are red.

Output: two byte-identical hooks with a widened trigger, two repaired tests, and one exposed key.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-SPEC.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-PATTERNS.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-VALIDATION.md
@lib/core/recipe-maps.cjs
</context>

<locked_constraints>
- Never `git stash`. Explicit `git add <paths>` only. No em-dashes or en-dashes.
- `scripts/hooks/pre-commit` and `scripts/hooks/pre-commit-room-minto-guard.sh` are BYTE-IDENTICAL (31,044 bytes at HEAD, `cmp -s` clean). Edit BOTH and re-verify with `cmp -s`, or the reinstall check in `setup-hooks.sh` overwrites whichever one was edited.
- `scripts/install-pre-commit.sh` byte-copies the canonical guard file and authors no hook content of its own (Phase 235-01). Do not add hook content back into it.
- `data/command-registry.json` must be byte-unchanged. `lib/core/recipe-maps.cjs` may change ONLY inside `_loadManifest` and, if needed, its exports block; the three read-joins stay untouched.
- `_loadManifest` is an explicit-allowlist rebuild, not a spread. Adding `policies` needs a line in the try block AND a matching line in the catch degrade, or the failure path returns an object shaped differently from the success path.
</locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Widen the pre-commit drift trigger in both byte-identical hook files</name>
  <files>scripts/hooks/pre-commit-room-minto-guard.sh, scripts/hooks/pre-commit</files>
  <read_first>
    - `scripts/hooks/pre-commit-room-minto-guard.sh` lines 425-445: the comment block at 428-437 explaining what the guard catches, the anchored staged-path alternation at 439, the node invocation at 440, and the exit 2 on drift
    - `scripts/hooks/pre-commit` at the same line range, to confirm the two files are identical there
    - `scripts/install-pre-commit.sh` header (it byte-copies the canonical file and authors nothing)
    - `.planning/phases/298-.../298-PATTERNS.md` section on the pre-commit guard and its byte-identical twin
    - `.planning/phases/298-.../298-RESEARCH.md` Pitfall 6
  </read_first>
  <action>
    Take a baseline first: `cmp -s scripts/hooks/pre-commit scripts/hooks/pre-commit-room-minto-guard.sh`
    must succeed before any edit, and record both byte sizes with `wc -c`.

    The current trigger is an anchored exact-path alternation: a caret, an open parenthesis, five
    escaped exact paths separated by pipes, a close parenthesis, then a dollar. An exact-path
    alternation cannot match a directory, so add two PREFIX branches inside the same group, before
    the closing dollar: one matching any JSON file under `data/harness-policies/`, and one
    matching any path under `data/harness-fixtures/`. Keep the existing five branches unchanged,
    keep the anchoring, and keep the trailing dollar closing the group.

    Verify the resulting regex against a sample staged-path list with `grep -qE` before
    committing. It must match all five original paths plus
    `data/harness-policies/gate-card-fire.json` and
    `data/harness-fixtures/converged-room/STATE.md`, and it must NOT match
    `data/doctor-modules.json` or `data/harness-policies/CONTEXT.md.bak`.

    Update the comment block at 428-437 to name the two new trigger paths and to state why: the
    manifest digests the policy directory and the fixture, so an edit to either without a
    regeneration leaves a stale digest, which is exactly the drift class this phase exists to
    close.

    Apply the identical edit to BOTH files, then re-run `cmp -s` and confirm it still succeeds.
    Record both new byte sizes. Do not touch `scripts/install-pre-commit.sh`.
  </action>
  <verify>
    <automated>cmp -s scripts/hooks/pre-commit scripts/hooks/pre-commit-room-minto-guard.sh && echo hooks-identical; wc -c scripts/hooks/pre-commit scripts/hooks/pre-commit-room-minto-guard.sh; grep -c harness-policies scripts/hooks/pre-commit-room-minto-guard.sh; grep -c harness-fixtures scripts/hooks/pre-commit</automated>
  </verify>
  <acceptance_criteria>
    - `cmp -s` on the two hook files succeeds after the edit.
    - Both files report the same byte size from `wc -c`.
    - `grep -c harness-policies` returns at least 1 in each file, and the same for `harness-fixtures`.
    - The extracted trigger regex, tested with `grep -qE` against a sample list, matches all five original paths plus `data/harness-policies/gate-card-fire.json` and `data/harness-fixtures/converged-room/STATE.md`.
    - The same regex does NOT match `data/doctor-modules.json` and does NOT match `data/harness-policies/CONTEXT.md.bak`.
    - `git diff --stat HEAD -- scripts/install-pre-commit.sh` reports zero changed files.
    - `grep -cP '\x{2014}|\x{2013}'` reports 0 for both hook files.
  </acceptance_criteria>
  <done>Both hooks carry the widened trigger, remain byte-identical, and the regex matches the new directories without over-matching.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the two red manifest-cluster tests at the live canonical hook</name>
  <files>tests/test-harness-manifest-precommit-wiring.cjs, tests/test-harness-167-verdict.cjs</files>
  <read_first>
    - `tests/test-harness-manifest-precommit-wiring.cjs` in full, especially line 53 where `TEMPLATE_PATH` names `scripts/install-pre-commit.sh`, and the six checks it runs
    - `tests/test-harness-167-verdict.cjs` in full, especially line 58 where `INSTALL_PRECOMMIT` is set, the D-167-03 check that uses it, and the separate older D-167-06 Part 9 failure about `build-new-surface.cjs` writing a confirmed review status
    - `.planning/phases/298-.../298-RESEARCH.md` section "The live manifest-cluster baseline at HEAD" (the diagnosed root cause) and Open Question 2 (repair is the recommendation)
    - `.planning/phases/298-.../298-TEST-BASELINE.md` from plan 298-01 (the recorded failing-check counts for both files)
  </read_first>
  <action>
    In `tests/test-harness-manifest-precommit-wiring.cjs`, change the path constant at line 53
    from `scripts/install-pre-commit.sh` to `scripts/hooks/pre-commit-room-minto-guard.sh`, and
    add a header note recording why: Phase 235-01 (commits 7409a69f and 43565da3) rewrote
    `install-pre-commit.sh` to byte-copy the canonical guard and to author no hook content of its
    own, so the guard is live and correct while the test pointed at the wrong file.

    Add two new checks to that file. First, the two hook files are byte-identical, since that
    invariant is now load-bearing for this phase. Second, the drift trigger fires on
    `data/harness-policies/` and `data/harness-fixtures/`, asserted by extracting the trigger line
    from the hook and testing it against a sample staged-path list, so the widening from Task 1 is
    pinned rather than assumed.

    In `tests/test-harness-167-verdict.cjs`, change the `INSTALL_PRECOMMIT` constant at line 58
    the same way and add the same header note. Do NOT attempt to fix the separate D-167-06 Part 9
    failure about `build-new-surface.cjs` writing a confirmed review status: it is older, unrelated
    to this phase, and stays a recorded baseline item.

    Re-run both tests and compare against the recorded baseline. `precommit-wiring` must go from
    five failing checks of six to zero failing, and `167-verdict` must go from two failing of nine
    to one failing. If either does not, stop and report rather than widening scope.
  </action>
  <verify>
    <automated>node tests/test-harness-manifest-precommit-wiring.cjs; echo "wiring-exit=$?"; node tests/test-harness-167-verdict.cjs; echo "verdict-exit=$?"; bash tests/run-all-167.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-harness-manifest-precommit-wiring.cjs` exits 0 with all six original checks passing plus the two new ones.
    - `node tests/test-harness-167-verdict.cjs` reports exactly one failing check, down from the two recorded in `298-TEST-BASELINE.md`, and that remaining failure is the D-167-06 Part 9 item about `build-new-surface.cjs`.
    - Both test files name `scripts/hooks/pre-commit-room-minto-guard.sh` and no longer assert hook content inside `scripts/install-pre-commit.sh`.
    - `grep -c install-pre-commit tests/test-harness-manifest-precommit-wiring.cjs` returns 0, or returns only occurrences inside the explanatory header note.
    - The new trigger-widening check passes and would fail if the alternation were reverted.
    - `bash tests/run-all-167.sh` improves against its recorded baseline and introduces no new failure.
    - `grep -cP '\x{2014}|\x{2013}'` reports 0 for both test files.
  </acceptance_criteria>
  <done>Both tests point at the file that actually carries the guard, the wiring test is fully green, and the verdict test drops to its one unrelated pre-existing failure.</done>
</task>

<task type="auto">
  <name>Task 3: Expose policies through recipe-maps loadManifest without touching the read-joins</name>
  <files>lib/core/recipe-maps.cjs</files>
  <read_first>
    - `lib/core/recipe-maps.cjs` lines 125-185: `_loadManifest()` at 138-162 (the explicit four-key allowlist rebuild in the try block and the documented empty binding in the catch at 158-161), `postureForCommand` at 177, and `__reset()` at 417 with the exports block
    - `.planning/phases/298-.../298-PATTERNS.md` section on `_loadManifest()` (the comment style each new key gets)
    - `tests/test-201-harness-manifest.cjs` Task 3 (the read-join regression guard that must stay green)
    - `.planning/coordination/2026-09-07-M-TO-T-harness-v2-compatibility-brief.md` (what the T side was told about the sync payload)
  </read_first>
  <action>
    Add exactly two lines to `lib/core/recipe-maps.cjs`. In the try block of `_loadManifest()`,
    after the `runtime_surfaces` line, add a tolerant read of `policies`: the parsed value when it
    is an object, otherwise null. In the catch degrade, add the matching `policies: null` so the
    failure path returns an object with the same key set as the success path.

    Give each new line a one-line comment in the file's established style: name Phase 298, say it
    is a tolerant read, and state that the three read-joins ignore it. Do not spread the parsed
    JSON, do not reorder the existing keys, and do not touch `postureForCommand`, which reads
    nothing from the manifest.

    Confirm `__reset()` still clears the cache so the tests can force a reload.

    Verify the T-side payload is untouched: `git diff --stat HEAD -- data/command-registry.json`
    must report zero changed files, and `git diff HEAD -- lib/core/recipe-maps.cjs` must show
    changes confined to `_loadManifest` and, if needed, the exports block.

    Commit with an explicit `git add` naming the five files this plan modified.
  </action>
  <verify>
    <automated>node -e "const rm=require('./lib/core/recipe-maps.cjs'); if (typeof rm.__reset === 'function') rm.__reset(); const m=rm.loadManifest(); if (!m.policies || typeof m.policies !== 'object') throw new Error('policies not exposed'); if (!m.policies.digest || !m.policies.path) throw new Error('policies object incomplete'); if (!Array.isArray(m.maps) || m.maps.length !== 3) throw new Error('maps read-join changed'); console.log('policies exposed ok');"</automated>
  </verify>
  <acceptance_criteria>
    - The probe above exits 0 and prints `policies exposed ok`.
    - `node tests/test-201-harness-manifest.cjs` exits 0, including its Task 3 read-join regression guard.
    - `git diff --stat HEAD -- data/command-registry.json` reports zero changed files.
    - `git diff HEAD -- lib/core/recipe-maps.cjs` shows changes only inside `_loadManifest` and, if present, the exports block; no read-join hunk appears.
    - The catch degrade returns the same key set as the success path, provable by pointing the manifest path at a nonexistent file in a temporary process and comparing `Object.keys` of both returns.
    - `grep -c "Phase 298" lib/core/recipe-maps.cjs` returns at least 1 (the tolerant-read comments).
    - `grep -cP '\x{2014}|\x{2013}' lib/core/recipe-maps.cjs` returns 0.
  </acceptance_criteria>
  <done>`loadManifest()` exposes `policies` on both the success and the degrade path, and the T-side sync payload is byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged files -> the pre-commit guard | The guard decides whether a manifest drift check runs at all |
| manifest -> `recipe-maps.cjs` consumers | The T side reads this module's sync payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-298-28 | Tampering | the drift guard | mitigate | The alternation is widened by two prefix branches and the widening is pinned by a new test check, so the policy digest cannot silently rot. Both byte-identical hook files are edited and `cmp -s` is an acceptance criterion, so a reinstall cannot overwrite the fix |
| T-298-29 | Denial of service | the widened regex | mitigate | The regex is verified against a positive and a negative sample list before commit; over-matching would fire the manifest check on unrelated commits, so `data/doctor-modules.json` and a stray backup file are explicit negatives |
| T-298-26 | Tampering | the T-side sync payload | mitigate | `data/command-registry.json` is asserted byte-unchanged, and the `recipe-maps.cjs` diff is asserted confined to `_loadManifest`; the three read-joins are untouched and `test-201` Task 3 is the regression guard |
| T-298-30 | Repudiation | the two red tests | mitigate | Repair rather than record: the tests exist to prove a fresh clone inherits the guard, and today they prove nothing. Their before and after failing-check counts are compared against the recorded baseline, so the repair is measured rather than asserted |
| T-298-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase; Node built-ins only |
</threat_model>

<verification>
- `cmp -s` on the two hook files succeeds; both report the same byte size.
- The trigger regex matches the two new paths and the five originals, and rejects the two negatives.
- `precommit-wiring` is fully green; `167-verdict` drops to one pre-existing failure.
- `loadManifest().policies` is an object; `test-201` exits 0; `command-registry.json` is unchanged.
</verification>

<success_criteria>
- The drift class this phase exists to close is closed for its own artifacts.
- R-11 becomes literally true for the manifest cluster rather than aspirational.
- R-10 delivered: `policies` exposed, read-joins and the T-side payload untouched.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 298 across all plans. The plan-review-convergence source-grounding
pass reads this section to exclude newly created symbols from drift verification.

**New CLI scripts**
- `scripts/run-harness.cjs` - flags `--check`, `--tier <t>`, `--room <dir>`, `--policy <id>`, `--json`, `--help`; exported `loadPolicies`, `classifyPolicy`, `countEntries`, `scanRoom`, `main`
- `scripts/check-graph-derive-health.cjs` - flags `--room <dir>`, `--all`, `--json`, `--strict`, `--check`, `--help`
- `scripts/check-voice-style.cjs` - Stop hook, stdin envelope, no flags

**New library modules and exported functions**
- `lib/hmi/turn-text.cjs` - `readTurnText`, `readTranscriptTurn`, `readTranscriptTail`, `extractAssistantText`, `classifyPrecedingUserContentSource`, `scanContentForAskUserQuestion`, `TRANSCRIPT_TAIL_BYTES`
- `lib/hmi/voice-style-log.cjs` - `appendVoiceStyleRow`, `readVoiceStyleRows`, `evaluatePromotion`, `mindrianHome`, `voiceStyleLogPath`
- `lib/core/doctor/voice-style-log-module.cjs` - `check`, `voiceStyleLogPath`

**New generator symbols in `scripts/build-harness-manifest.cjs`**
- `LARRY_SURFACE_BINDINGS`, `POLICIES_DIR`, `FIXTURE_DIR`, `digestDirectory`, `loadPolicies`, `validatePolicyFile`, `runContractChecks`; `MANIFEST_VERSION` becomes 2

**New manifest JSON keys** in `data/harness-manifest.json`: `policies` with `path`, `digest`, `count`; `larry_surfaces` as an array of `role`, `path`, `digest`; `fixture_ref` with `path`, `digest`

**New policy schema keys** in `data/harness-policies/_schema.json` under `_doc`: `purpose`, `rung_vocabulary`, `rung_meaning`, `kind_vocabulary`, `applies_to_vocabulary`, `policy_fields`, `runner_path_rule`, `promotion_rule_doc`, `gate_count_principle`, `validation_rule`, `default_on_miss`.
**Per-policy file keys**: `id`, `kind`, `runner`, `args`, `rung`, `evidence_log`, `promotion_rule` with `window_runs`, `max_false_positive_rate`, `min_true_positives`; `owner`, `pinned_by`, `applies_to`, `notes`.

**New data paths**
- `data/harness-policies/` with `_schema.json`, `CONTEXT.md`, and 11 policy files
- `data/harness-fixtures/converged-room/` (33 committed text files)
- `<room>/.mindrian/harness-run.json` (runtime output, never committed)
- `$MINDRIAN_HOME/voice-style.jsonl` and its rotation `voice-style.jsonl.1` (runtime, never committed)

**New registry ids**
- doctor module id `voice-style-log` in `data/doctor-modules.json`
- doctor acceptance point id `harness-policies` in `scripts/doctor.cjs` `buildAcceptanceChecklist`
- one `hooks/hooks.json` Stop entry invoking `scripts/check-voice-style.cjs`

**New test files**
- `tests/test-298-policies-schema.cjs`, `tests/test-298-runner-idempotent.cjs`, `tests/test-298-derive-health.cjs`, `tests/test-298-voice-log.cjs`, `tests/test-298-contract-parity.cjs`, `tests/run-all-298.sh`

**New key on the `lib/core/recipe-maps.cjs` `loadManifest()` return**: `policies`

<output>
Create `.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-14-SUMMARY.md` when done, recording the before and after trigger regex verbatim, the before and after byte sizes of both hook files, and the before and after failing-check counts of both repaired tests.
</output>
