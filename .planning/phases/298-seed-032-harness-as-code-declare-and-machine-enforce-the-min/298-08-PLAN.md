---
phase: 298
plan: 08
type: execute
wave: 3
depends_on: [298-07]
files_modified:
  - lib/hmi/voice-style-log.cjs
  - lib/core/doctor/voice-style-log-module.cjs
  - data/doctor-modules.json
  - tests/test-298-voice-log.cjs
  - .planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-RESIDUALS.md
autonomous: true
requirements: [R-03]

must_haves:
  truths:
    - "D-02: one exported evaluatePromotion(policy, lines) computes the promotion verdict, and both printers (the runner and the doctor module) format it and compute nothing, so a printer can never disagree with the counter"
    - "D-02: the verdict object carries the counts, the last findings, MET or NOT MET against the policy promotion_rule, and the literal one-line rung edit that promotes"
    - "R-03: evaluatePromotion is side-effect free, never writes, and never changes a rung; promotion is a human edit to one policy file"
    - "Pitfall 5: max_false_positive_rate is computed over human-labeled rows only, and an unreviewed log returns MET false with a stated reason rather than a fake zero percent"
    - "D-04: one doctor MODULE line reports the count since the last release and returns status ok on every path, including a missing log, malformed lines, and a met promotion rule"
    - "D-04a: the cockpit statusline chip is filed as a co-design proposal only and is NOT built; nothing under lib/statusline/ changes in this phase"
  artifacts:
    - path: "lib/hmi/voice-style-log.cjs"
      provides: "The single promotion evaluator, added beside the log it reads"
      exports: ["evaluatePromotion"]
    - path: "lib/core/doctor/voice-style-log-module.cjs"
      provides: "The never-warn informational doctor module that prints the evaluator's verdict"
      exports: ["check", "voiceStyleLogPath"]
      min_lines: 60
    - path: "data/doctor-modules.json"
      provides: "Registry-only wiring for the voice-style-log module"
      contains: "voice-style-log"
    - path: ".planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-RESIDUALS.md"
      provides: "The D-04a co-design proposal and the other recorded residuals, filed not built"
  key_links:
    - from: "lib/core/doctor/voice-style-log-module.cjs"
      to: "lib/hmi/voice-style-log.cjs evaluatePromotion"
      via: "require and format, never recompute"
      pattern: "evaluatePromotion"
    - from: "data/doctor-modules.json"
      to: "lib/core/doctor/voice-style-log-module.cjs"
      via: "the runner field of a 7-key registry entry"
      pattern: "voice-style-log-module"
---

<objective>
Add the one promotion evaluator (D-02) and the one informational doctor line that prints its
verdict (D-04), and file the cockpit chip as a proposal rather than building it (D-04a).

Purpose: a rung is promoted by a human editing one line in one policy file after reading the
evidence. That human needs a verdict they can trust. The 2026-07-11 printer-must-match-counter
lesson says the verdict must be computed once and merely printed twice, so this plan writes one
evaluator and gives it its first printer.

Output: `evaluatePromotion`, a never-warn doctor module, one registry row, extended assertions,
and one residuals record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-CONTEXT.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-PATTERNS.md
@lib/core/doctor/card-fire-health-module.cjs
@data/doctor-modules.json
</context>

<locked_constraints>
- Never `git stash`. Explicit `git add <paths>` only. No em-dashes or en-dashes.
- CJS only, no new dependencies.
- The evaluator NEVER writes, never touches a policy file, and never promotes. R-03 is proven by a source grep for a rung assignment plus a log-mtime-unchanged assertion.
- The doctor module NEVER returns `warn` and NEVER returns `error`. It cannot ride an acceptance point: every `buildAcceptanceChecklist` entry is `severity: 'blocker'` (`scripts/doctor.cjs:1605-1612`), which is exactly why D-04 puts this in a MODULE.
- D-04a: do not build the cockpit chip. `lib/statusline/` is not in `files_modified` and must show a zero-file diff. The statusline is co-designed with the navigator, never a solo pick.
- Registry-only wiring: one entry in `data/doctor-modules.json`, no doctor engine change.
</locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add evaluatePromotion to lib/hmi/voice-style-log.cjs and pin it</name>
  <files>lib/hmi/voice-style-log.cjs, tests/test-298-voice-log.cjs</files>
  <read_first>
    - `lib/hmi/voice-style-log.cjs` as landed by plan 298-07 (the row shape and `readVoiceStyleRows`)
    - `lib/core/doctor/card-fire-health-module.cjs` lines 1-15 (the verbatim 2026-07-11 printer-must-match-counter lesson this evaluator enforces) and line 79 (the `ts` freshness read)
    - `.planning/phases/298-.../298-PATTERNS.md` section `lib/hmi/voice-style-log.cjs`, the `evaluatePromotion` return shape and the load-bearing counting rule
    - `.planning/phases/298-.../298-RESEARCH.md` Pitfall 5 and the LangTalks planner-facing takeaway 2 (the only sourced figure for human-labeled traces is 50 to 100, from Vanishing Gradients Ep. 57; the navigator sets the number, and `min_true_positives` is where it lands)
    - `tests/test-298-voice-log.cjs` as landed by plan 298-07, including the one PENDING entry left for this plan
  </read_first>
  <action>
    Add one exported function `evaluatePromotion(policy, lines)` to `lib/hmi/voice-style-log.cjs`.
    `policy` is a parsed policy object carrying `id`, `rung` and `promotion_rule`; `lines` is an
    array of parsed rows (the caller passes `readVoiceStyleRows()` output, filtered to that
    policy id). The function is pure: it reads its two arguments and returns an object. It opens
    no file, writes no file, and mutates neither argument.

    Return exactly these keys: `window_runs`, `fires`, `true_positives`, `false_positives`,
    `false_positive_rate`, `met` (boolean), `reasons` (array of strings), `next_rung` (the string
    one rung up, or null when already at `blocking` or when `met` is false), and `edit_line` (the
    literal one-line diff a human applies, formatted as the JSON key and value pair that sets the
    new rung).

    The counting rule is load-bearing and must be implemented exactly: `false_positive_rate` is
    `false_positives` divided by the sum of `true_positives` and `false_positives`, over LABELED
    rows only. A row whose `result` is still the default `fire` counts as neither a true positive
    nor a false positive; it counts only toward `fires`. When the labeled count is below
    `promotion_rule.min_true_positives`, return `met: false` with a reason naming the shortfall,
    so an unreviewed log can never read as a zero percent false-positive rate and auto-qualify
    (Pitfall 5, the same false-success shape as T-233-04 and T-217-01).

    Also return `met: false` with a stated reason when `promotion_rule` is missing or malformed,
    when `lines` is empty, and when the policy is already at `blocking`. Every reason string
    names the specific shortfall, never a generic failure.

    Then extend `tests/test-298-voice-log.cjs` with the remaining PENDING entry and four new
    cases: an unreviewed log of 50 `fire` rows returns `met: false` with a reason naming the
    labeled-row shortfall and a `false_positive_rate` that is not zero-by-omission; a labeled log
    meeting the rule returns `met: true` with a `next_rung` and a non-empty `edit_line`; a
    labeled log exceeding `max_false_positive_rate` returns `met: false`; and calling
    `evaluatePromotion` twice leaves the log file mtime unchanged and the input array unmutated.
    Remove the PENDING entry once implemented.
  </action>
  <verify>
    <automated>node tests/test-298-voice-log.cjs; echo "test-exit=$?"; grep -cE "^[^/]*\brung\s*=" lib/hmi/voice-style-log.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-298-voice-log.cjs` exits 0 with the four new `ok` lines present.
    - `evaluatePromotion` returns all nine named keys for every input, including empty input.
    - An all-`fire` log of any size returns `met: false` with a reason naming the labeled-row shortfall.
    - Calling `evaluatePromotion` twice leaves `fs.statSync(voiceStyleLogPath()).mtimeMs` unchanged and does not mutate the passed array.
    - `grep -cE "\brung\s*=[^=]" lib/hmi/voice-style-log.cjs` returns 0 (no assignment to a rung anywhere in the evaluator).
    - `grep -c "writeFileSync\|appendFileSync" lib/hmi/voice-style-log.cjs` is unchanged from its value after plan 298-07 (the evaluator adds no write).
    - The PENDING array in `tests/test-298-voice-log.cjs` is empty or absent.
    - `grep -cP '\x{2014}|\x{2013}' lib/hmi/voice-style-log.cjs tests/test-298-voice-log.cjs` reports 0 for both files.
  </acceptance_criteria>
  <done>One side-effect-free evaluator exists, refuses MET on unlabeled evidence, and returns the literal one-line rung edit a human applies.</done>
</task>

<task type="auto">
  <name>Task 2: Add the never-warn voice-style-log doctor module and its registry row</name>
  <files>lib/core/doctor/voice-style-log-module.cjs, data/doctor-modules.json</files>
  <read_first>
    - `lib/core/doctor/card-fire-health-module.cjs` in full: the header contract at 1-28 (what it does, what it deliberately does not do, the Canon Part 8 line, the `check(ctx)` contract line), the `mindrianHome`/`interceptLogPath` pair at 41-46, the `ctx.log_path` test seam at 51, the JSONL parse at 76-90, the freshness read at 79, the return discipline at 138-152 (every path carries a non-empty `detail`), and the export block at 150-165
    - `data/doctor-modules.json` lines 30-45 (the `card-fire-health` entry and the exact 7-key shape)
    - `.planning/phases/298-.../298-RESEARCH.md` section "The doctor module (D-04)" and section "The 21 doctor acceptance MODULES"
    - `.planning/phases/298-.../298-RESEARCH.md` LangTalks planner-facing takeaway 9 (report a windowed rate over the log, never a pass or fail on the last turn)
    - `lib/core/repo-version.cjs` (the version stamp the since-last-release window keys off)
  </read_first>
  <action>
    Create `lib/core/doctor/voice-style-log-module.cjs` mirroring
    `lib/core/doctor/card-fire-health-module.cjs` one for one. Header comment states: what it
    reports (the voice-style evidence count since the last release plus the promotion verdict),
    what it deliberately does NOT do (it never fails, never warns, never promotes, and never
    repairs), the Canon Part 8 line (local file reads only, zero network), and the contract line
    `check(ctx) -> { status, detail, action_lines? }`.

    `check(ctx)` reads the log through `require('../../hmi/voice-style-log.cjs')`, honoring a
    `ctx.log_path` test seam exactly as the analog does at line 51, then formats the object
    `evaluatePromotion` returned for each voice policy it finds rows for. It computes nothing
    itself: no counting, no rate, no verdict. It reports the count since the last release by
    filtering rows on the `version` stamp against `readRepoVersion().version`, and it reports a
    windowed rate rather than a pass or fail on the most recent turn.

    Return discipline: `status` is the string `ok` on EVERY path, including a missing log,
    malformed lines, zero rows, and a met promotion rule. Every return path carries a non-empty
    `detail` string. When a promotion rule is met, put the one-line rung edit into `action_lines`
    so the human sees exactly what to change; do not raise the status.

    Export `check` and the log-path helper under a `// exported for hermetic tests:` comment.

    Wire it registry-only by adding ONE entry to `data/doctor-modules.json` beside the
    `card-fire-health` entry, with exactly the 7 keys of that shape: `id` `voice-style-log`,
    `introduced_version` set to the version this ships in (read it with
    `node lib/core/repo-version.cjs`, do not guess), `cadence` `always`, `flag` null,
    `fix_supported` false, `runner` `lib/core/doctor/voice-style-log-module.cjs`, and a
    `description` naming what it reports. Do not modify any other entry and do not touch the
    doctor engine.
  </action>
  <verify>
    <automated>MINDRIAN_HOME=$(mktemp -d) node -e "const m=require('./lib/core/doctor/voice-style-log-module.cjs'); return Promise.resolve(m.check({flags:{}})).then(function(r){ if(r.status!=='ok') throw new Error('status must always be ok, got '+r.status); if(!r.detail||!r.detail.length) throw new Error('empty detail'); console.log('module ok on missing log:', r.detail); });" && node -e "const reg=require('./data/doctor-modules.json'); const list=Array.isArray(reg)?reg:reg.modules; const e=list.find(function(x){return x.id==='voice-style-log';}); if(!e) throw new Error('registry entry missing'); if(Object.keys(e).length!==7) throw new Error('entry must have exactly 7 keys, has '+Object.keys(e).length); if(e.flag!==null||e.fix_supported!==false||e.cadence!=='always') throw new Error('entry fields wrong'); console.log('registry entry ok');"</automated>
  </verify>
  <acceptance_criteria>
    - With `MINDRIAN_HOME` pointed at an empty scratch directory (no log at all), `check` returns `status` equal to the string `ok` and a non-empty `detail`.
    - With a log containing only malformed lines, `check` still returns `status` `ok` and a non-empty `detail`.
    - With a log whose promotion rule is met, `check` returns `status` `ok` and an `action_lines` array carrying the one-line rung edit.
    - `grep -c "'warn'" lib/core/doctor/voice-style-log-module.cjs` returns 0 and `grep -c "'error'" lib/core/doctor/voice-style-log-module.cjs` returns 0.
    - `grep -c "evaluatePromotion" lib/core/doctor/voice-style-log-module.cjs` returns at least 1, and the module contains no arithmetic on `true_positives` or `false_positives` of its own.
    - The `voice-style-log` registry entry exists with exactly 7 keys, `flag` null, `fix_supported` false, `cadence` `always`.
    - `node scripts/doctor.cjs --acceptance --pre-tag` matches its recorded baseline exit code (this module is informational and must not change any acceptance verdict).
    - `grep -cP '\x{2014}|\x{2013}' lib/core/doctor/voice-style-log-module.cjs` returns 0.
  </acceptance_criteria>
  <done>The informational module reports the evaluator's verdict, returns ok on every path, and is wired by a single 7-key registry row.</done>
</task>

<task type="auto">
  <name>Task 3: File the D-04a cockpit-chip proposal and the phase residuals without building anything</name>
  <files>.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-RESIDUALS.md</files>
  <read_first>
    - `lib/statusline/cockpit-signals.cjs` lines 20-40 and around line 129 (the orphan `~/.mindrian/voice-mark.json` side-channel it reads with no writer; this is the cheap hook the proposal names)
    - `lib/statusline/cockpit-renderer.cjs` lines 70-85 (the cockpit's own rule that static fields earn no space)
    - `.planning/phases/298-.../298-CONTEXT.md` D-04a and the Deferred Ideas section
    - `.planning/phases/298-.../298-RESEARCH.md` section "State of the Art", the Deprecated and outdated list, and section 16 of the design doc
  </read_first>
  <action>
    Create `298-RESIDUALS.md` in the phase directory with these sections, each one short and
    actionable, and each explicitly marked as filed rather than built:

    1. `## D-04a: cockpit voice chip (co-design proposal, NOT built)`. State the proposal: the
       orphan `~/.mindrian/voice-mark.json` side-channel that `lib/statusline/cockpit-signals.cjs`
       already reads with no writer makes a chip cheap, and the voice-style log now has a writer
       that could fill it. State the hard rule that blocks building it here: the statusline is
       co-designed with the navigator, never a solo pick. State the one open design question the
       navigator would need to answer (what the chip displays, given the cockpit's own rule that
       static fields earn no space).
    2. `## Filed for a separate /gsd-quick`. The `lib/core/visual-ops.cjs` U+2014 in the zero-entry
       EMPTY marker, which lands an em-dash in every generated STATE.md everywhere, not just in
       this phase's fixture (research Open Question 4).
    3. `## Recorded, out of scope`. The INV-1 validator defect; SEED-037 4c room healing; the
       CHANGELOG's 107 pre-existing em-dashes; `scripts/doctor.cjs --point <id>` as the slice-3
       prerequisite; `lib/mcp/larry-server-instructions.md` as a de-facto fourth Larry surface
       loaded by nothing at runtime and deliberately NOT declared in `contract-parity-larry`;
       the hardcoded `8 insight sensors` line in `agents/larry-extended.md`; and the stale header
       comment in `lib/core/room-skeleton-scaffold.cjs` that says 8 sections and 5 identity
       directories where the frozen constants say 11 and 6.

    Change no source file. Confirm with a diff that nothing under `lib/statusline/` moved.

    Commit with an explicit `git add lib/hmi/voice-style-log.cjs lib/core/doctor/voice-style-log-module.cjs data/doctor-modules.json tests/test-298-voice-log.cjs` (the residuals file lives under gitignored `.planning/` and is not committed).
  </action>
  <verify>
    <automated>test -f .planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-RESIDUALS.md && grep -c "D-04a" .planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-RESIDUALS.md; git diff --stat HEAD -- lib/statusline/ | wc -l; bash tests/run-all-298.sh</automated>
  </verify>
  <acceptance_criteria>
    - `298-RESIDUALS.md` exists with all three sections and names D-04a explicitly.
    - `git diff --stat HEAD -- lib/statusline/` reports zero changed files.
    - `git status --porcelain lib/statusline/` is empty.
    - `bash tests/run-all-298.sh` exits 0.
    - `grep -cP '\x{2014}|\x{2013}' .planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-RESIDUALS.md` returns 0.
  </acceptance_criteria>
  <done>D-04a is honored as a filed proposal with the statusline untouched, and every recorded residual has a named home.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence log rows -> promotion verdict | Rows are human-editable; a mislabeled or unlabeled log must not manufacture a promotion |
| doctor module -> release train | A module that returned `warn` or `error` would change a release verdict it has no business changing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-298-06 | Repudiation | `evaluatePromotion` | mitigate | The rate is computed over labeled rows only and `met` is false with a named reason when the labeled count is short, so an unreviewed log cannot read as zero percent false positives and auto-qualify (Pitfall 5) |
| T-298-18 | Elevation of privilege | `evaluatePromotion` | mitigate | The evaluator is pure: no file open, no write, no rung assignment. Proven by a source grep for a rung assignment and by a log-mtime-unchanged assertion. Promotion stays a human edit to one policy file, reviewable in a diff (D8) |
| T-298-19 | Denial of service | `voice-style-log-module.cjs` | mitigate | Status is the string `ok` on every path including missing and malformed logs, so a corrupt local log can never block a release. Grep asserts the module contains no `warn` and no `error` literal |
| T-298-20 | Tampering | `data/doctor-modules.json` | mitigate | Exactly one 7-key entry appended; the test asserts the key count and the field values, and no doctor engine file is touched |
| T-298-02 | Information disclosure | doctor `detail` string | mitigate | The detail reports counts, a rate and a verdict; it never echoes a logged turn excerpt, and the module makes no network call |
| T-298-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase; Node built-ins only |
</threat_model>

<verification>
- `node tests/test-298-voice-log.cjs` exits 0 with the evaluator cases green.
- The module returns `status` `ok` for missing, malformed, zero-row and rule-met logs.
- The `voice-style-log` registry entry has exactly 7 keys with `flag` null.
- `git diff --stat HEAD -- lib/statusline/` reports zero changed files.
- `node scripts/doctor.cjs --acceptance --pre-tag` matches its recorded baseline.
</verification>

<success_criteria>
- One evaluator, two printers: this plan ships the evaluator and the doctor printer; plan 298-10 adds the runner printer and neither recomputes.
- The evaluator refuses MET on unlabeled evidence and never promotes.
- The doctor line never fails, and the cockpit chip is filed as a proposal, not built.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 298 across all plans. The plan-review-convergence source-grounding
pass reads this section to exclude newly created symbols from drift verification.

**New CLI scripts**
- `scripts/run-harness.cjs` - flags `--check`, `--tier <t>`, `--room <dir>`, `--policy <id>`, `--json`, `--help`
- `scripts/check-graph-derive-health.cjs` - flags `--room <dir>`, `--all`, `--json`, `--strict`, `--check`, `--help`
- `scripts/check-voice-style.cjs` - Stop hook, stdin envelope, no flags

**New library modules and exported functions**
- `lib/hmi/turn-text.cjs` - `readTurnText`, `readTranscriptTurn`, `readTranscriptTail`, `extractAssistantText`, `classifyPrecedingUserContentSource`, `scanContentForAskUserQuestion`, `TRANSCRIPT_TAIL_BYTES`
- `lib/hmi/voice-style-log.cjs` - `appendVoiceStyleRow`, `readVoiceStyleRows`, `evaluatePromotion`, `mindrianHome`, `voiceStyleLogPath`
- `lib/core/doctor/voice-style-log-module.cjs` - `check`, `voiceStyleLogPath`

**`evaluatePromotion` return keys**: `window_runs`, `fires`, `true_positives`, `false_positives`, `false_positive_rate`, `met`, `reasons`, `next_rung`, `edit_line`

**New JSONL row keys** in `$MINDRIAN_HOME/voice-style.jsonl`: `ts`, `timestamp`, `policy_id`, `version`, `result` (one of `fire`, `true_positive`, `false_positive`), `detail`, `session_id`

**New generator symbols in `scripts/build-harness-manifest.cjs`**
- `LARRY_SURFACE_BINDINGS`, `POLICIES_DIR`, `FIXTURE_DIR`, `digestDirectory`, `loadPolicies`, `validatePolicyFile`, `runContractChecks`; `MANIFEST_VERSION` becomes 2

**New manifest JSON keys** in `data/harness-manifest.json`: `policies` with `path`, `digest`, `count`; `larry_surfaces` as an array of `role`, `path`, `digest`; `fixture_ref` with `path`, `digest`

**New policy schema keys** in `data/harness-policies/_schema.json` under `_doc`: `purpose`, `rung_vocabulary`, `rung_meaning`, `kind_vocabulary`, `applies_to_vocabulary`, `policy_fields`, `runner_path_rule`, `promotion_rule_doc`, `gate_count_principle`, `validation_rule`, `default_on_miss`.
**Per-policy file keys**: `id`, `kind`, `runner`, `args`, `rung`, `evidence_log`, `promotion_rule` with `window_runs`, `max_false_positive_rate`, `min_true_positives`; `owner`, `pinned_by`, `applies_to`, `notes`.

**New data paths**
- `data/harness-policies/` with `_schema.json`, `CONTEXT.md`, and 11 policy files: `gate-worktree-hygiene.json`, `gate-shape-declaration.json`, `gate-tool-honesty.json`, `gate-card-fire.json`, `gate-graph-derive-health.json`, `voice-hyphens-only.json`, `voice-glyph-present.json`, `voice-backend-noun-free.json`, `voice-fork-as-card.json`, `memory-write-policy.json`, `contract-parity-larry.json`
- `data/harness-fixtures/converged-room/` (33 committed text files)
- `<room>/.mindrian/harness-run.json` (runtime output, never committed)
- `$MINDRIAN_HOME/voice-style.jsonl` and its rotation `voice-style.jsonl.1` (runtime, never committed)

**New registry ids**
- doctor module id `voice-style-log` in `data/doctor-modules.json`
- doctor acceptance point id `harness-policies` in `scripts/doctor.cjs` `buildAcceptanceChecklist`
- one `hooks/hooks.json` Stop entry invoking `scripts/check-voice-style.cjs`

**New test files**
- `tests/test-298-policies-schema.cjs`, `tests/test-298-runner-idempotent.cjs`, `tests/test-298-derive-health.cjs`, `tests/test-298-voice-log.cjs`, `tests/test-298-contract-parity.cjs`, `tests/run-all-298.sh`

**New key on the `lib/core/recipe-maps.cjs` `loadManifest()` return**: `policies`

<output>
Create `.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-08-SUMMARY.md` when done, recording the final `evaluatePromotion` signature and return keys, the exact registry row added, and the D-04a proposal text so the navigator can act on it later.
</output>
