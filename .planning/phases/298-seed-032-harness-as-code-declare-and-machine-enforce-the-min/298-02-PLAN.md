---
phase: 298
plan: 02
type: execute
wave: 1
depends_on: [298-01]
files_modified:
  - data/harness-fixtures/converged-room/
autonomous: true
requirements: [R-04]

must_haves:
  truths:
    - "D-03: a scaffold-born, text-only converged-room fixture exists at data/harness-fixtures/converged-room/ with exactly 33 committed text files"
    - "D-03: the fixture carries no room.db and no .mindrian/ directory, so an absent derive queue reads empty, an absent decision-traces reads healthy, and an absent database reads as zero proposed claims"
    - "D-03a: nothing in the fixture can be created by a read-only opener, so the runner cannot dirty it on run one"
    - "The committed STATE.md reads total_entries: 13 and venture_stage: Investment, and carries zero U+2014 and zero raw ANSI escape bytes (research Corrections 1 to 3, Pitfall 3)"
    - "No machine-specific current_room: line is baked into the committed STATE.md"
  artifacts:
    - path: "data/harness-fixtures/converged-room/STATE.md"
      provides: "Layer 0 identity the runner scans for convergence"
      contains: "total_entries: 13"
    - path: "data/harness-fixtures/converged-room/MINTO.md"
      provides: "scaffold identity file"
    - path: "data/harness-fixtures/converged-room/USER.md"
      provides: "scaffold identity file"
    - path: "data/harness-fixtures/converged-room/references/ROOM.md"
      provides: "one of the 11 section ROOM.md identity files (ICM Layer 0)"
  key_links:
    - from: "data/harness-fixtures/converged-room/STATE.md"
      to: "the on-disk section entry count"
      via: "the compute-state counting rule (top-level non-hidden dirs, maxdepth 1, *.md, minus ROOM.md)"
      pattern: "total_entries: 13"
---

<objective>
Build the committed converged-room fixture the runner is measured against (D-03), once, by hand,
and never again. `scaffoldRoomSkeleton()` writes the 33 text files; `scripts/compute-state`
writes STATE.md once; then the output is scrubbed of the two literal em-dashes and two raw ANSI
escape sequences that `lib/core/visual-ops.cjs:529` emits for zero-entry sections.

Purpose: R-04's acceptance is "a second run on the committed fixture is a no-op: zero writes,
empty `git status --porcelain`, identical report". That is only provable against a committed
fixture whose contents are stable, text-only, and free of machine-specific lines.

Output: `data/harness-fixtures/converged-room/` with exactly 33 files across 17 directories plus
the root, committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-SPEC.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-CONTEXT.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-PATTERNS.md
@lib/core/room-skeleton-scaffold.cjs
</context>

<locked_constraints>
- Never `git stash`. Explicit `git add <paths>` only.
- No em-dashes, no en-dashes, anywhere in the committed fixture.
- The runner never regenerates this fixture. It is built once here and read forever after.
- Do not add a `ROOM.md` at `data/harness-fixtures/` or `data/harness-fixtures/converged-room/` root beyond what the scaffold itself writes: the every-directory-gets-a-ROOM.md rule governs user Data Rooms, and the three sibling fixture directories (`data/hitl-stages-fixtures/`, `data/grant-rubric-fixtures/`, `data/hitl-shape-declaration-fixtures/`) carry none.
</locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the fixture and regenerate STATE.md under a scratch rooms home</name>
  <files>data/harness-fixtures/converged-room/</files>
  <read_first>
    - `lib/core/room-skeleton-scaffold.cjs` lines 55-100 (the `scaffoldRoomSkeleton` signature, `SECTION_NAMES` at 63-75, `IDENTITY_DIRECTORIES` at 90-97). Trust the frozen constants, not the header comment at 184-186, which is stale (it says 8 sections and 5 identity dirs; the constants say 11 and 6).
    - `scripts/compute-state` lines 30-100 (the `MINDRIAN_ROOMS_HOME` / `current_room` resolver and the per-section counter at line 96) and lines 250-265 (the frontmatter emitter: `computed:` is an ISO timestamp at 257, `venture_stage` at 258, `total_entries` at 259, the CONDITIONAL `current_room` at 260).
    - `.planning/phases/298-.../298-RESEARCH.md` section "Layer-0 convergence reads", Corrections 1 to 3 and "The fixture is exactly 33 files".
    - `.planning/phases/298-.../298-PATTERNS.md` section `data/harness-fixtures/converged-room/` (the build recipe and the three mandatory scrubs).
  </read_first>
  <action>
    Create `data/harness-fixtures/` if absent. Run `scaffoldRoomSkeleton` in-process against
    `data/harness-fixtures/converged-room` with `{placeholder_slug: 'converged-room'}` via
    `node -e` requiring `lib/core/room-skeleton-scaffold.cjs`. Print and record the returned
    object: expect `ok: true`, `sections_created` length 11, `identity_files_created` length 6,
    `state_written: true`, `minto_written: true`, `user_written: true`, `contracts_created`
    length 11, `reference_docs_created` length 2, `errors: []`.

    Then regenerate STATE.md once with `MINDRIAN_ROOMS_HOME` pointed at an empty scratch
    directory created with `mktemp -d`, so the conditional `current_room:` line at
    `compute-state:260` is not emitted. Disable color for the run (set `NO_COLOR=1` and, if the
    renderer honors it, `MINDRIAN_NO_COLOR=1`; verify by grepping the output for `\x1b`). Write
    the output to `data/harness-fixtures/converged-room/STATE.md`.

    Do not create `.mindrian/`, do not create `room.db`, do not create `decision-traces/`. If the
    scaffold or compute-state creates any of those, delete them and record it in the SUMMARY.

    Record the observed `computed:`, `venture_stage:` and `total_entries:` values. `computed:` is
    an ISO timestamp and is expected to differ from any prior run; `total_entries` must read 13
    and `venture_stage` must read `Investment` (research Corrections 2 and 3). If either differs,
    stop and report rather than hand-editing the value.

    Do not commit in this task. Task 2 scrubs and commits.
  </action>
  <verify>
    <automated>find data/harness-fixtures/converged-room -type f | wc -l; test ! -d data/harness-fixtures/converged-room/.mindrian && echo "no-mindrian-ok"; grep -E "^(total_entries|venture_stage):" data/harness-fixtures/converged-room/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `find data/harness-fixtures/converged-room -type f | wc -l` returns 33.
    - `find data/harness-fixtures/converged-room -type d | wc -l` returns 18 (17 subdirectories plus the root).
    - `test ! -d data/harness-fixtures/converged-room/.mindrian` succeeds.
    - `test ! -f data/harness-fixtures/converged-room/.mindrian/room.db` succeeds.
    - `grep -c "^total_entries: 13$" data/harness-fixtures/converged-room/STATE.md` returns 1.
    - `grep -c "^venture_stage: Investment$" data/harness-fixtures/converged-room/STATE.md` returns 1.
    - `grep -c "^current_room:" data/harness-fixtures/converged-room/STATE.md` returns 0.
    - `grep -c "^computed: " data/harness-fixtures/converged-room/STATE.md` returns 1.
  </acceptance_criteria>
  <done>33 files exist under the fixture, no `.mindrian/`, and STATE.md carries total_entries 13, venture_stage Investment, no current_room line.</done>
</task>

<task type="auto">
  <name>Task 2: Scrub em-dashes and ANSI escapes, add the fixture note, commit</name>
  <files>data/harness-fixtures/converged-room/</files>
  <read_first>
    - `lib/core/visual-ops.cjs` around line 529 (the zero-entry EMPTY marker rendered with a U+2014 separator, wrapped in `c.red` and `c.reset`; this is the source of both defects)
    - `.planning/phases/298-.../298-RESEARCH.md` Pitfall 3 (the three options; option (a) is the chosen one) and Open Question 4
    - `data/hitl-shape-declaration-fixtures/` (naming precedent; note it carries no ROOM.md)
  </read_first>
  <action>
    Scrub the generated STATE.md in place, option (a) from Pitfall 3:
    1. Replace every U+2014 (em-dash) and U+2013 (en-dash) with a plain hyphen. The two known
       occurrences are the `EMPTY - GAP` markers for the two zero-entry sections (`assets/` and
       `team/`).
    2. Strip every raw ANSI escape byte (`\x1b[...m`) from the file.
    Do the scrub with a `node -e` read/replace/write, not by hand, so it is reproducible, and
    print the before/after counts.

    Sweep the other 32 files for the same two defects and scrub any hit the same way.

    Add a `data/harness-fixtures/converged-room/FIXTURE-NOTE.md`? No. Do NOT add a 34th file.
    Instead, record the build recipe and the scrub in the plan SUMMARY and in the `notes` field
    of `data/harness-policies/gate-graph-derive-health.json` when plan 298-09 authors it; this
    plan's SUMMARY is the durable record of how the fixture was produced.

    Confirm nothing under the fixture is gitignored: run `git check-ignore -v` against each of
    the 33 paths and assert zero matches (the hidden identity directories `.context/`,
    `.snapshots/`, `.intelligence/` each carry a `ROOM.md` and must commit).

    Commit with an explicit `git add data/harness-fixtures/converged-room` and nothing else.
    After committing, confirm `git status --porcelain` is empty.
  </action>
  <verify>
    <automated>grep -rlP '\x{2014}|\x{2013}' data/harness-fixtures/converged-room | wc -l; grep -rlP '\x1b' data/harness-fixtures/converged-room | wc -l; git status --porcelain; find data/harness-fixtures/converged-room -type f | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rlP '\x{2014}' data/harness-fixtures/converged-room | wc -l` returns 0.
    - `grep -rlP '\x{2013}' data/harness-fixtures/converged-room | wc -l` returns 0.
    - `grep -rlP '\x1b' data/harness-fixtures/converged-room | wc -l` returns 0.
    - `git check-ignore data/harness-fixtures/converged-room/.context/ROOM.md` exits non-zero (the path is NOT ignored).
    - `git status --porcelain` is empty after the commit.
    - `git ls-files data/harness-fixtures/converged-room | wc -l` returns 33.
    - `find data/harness-fixtures/converged-room -name room.db | wc -l` returns 0.
  </acceptance_criteria>
  <done>The 33-file fixture is committed, free of em-dashes, en-dashes and ANSI escapes, with an empty porcelain status afterwards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator machine -> committed repo | `compute-state` reads machine state (`MINDRIAN_ROOMS_HOME`, terminal color) and can bake it into a committed file |
| fixture -> the runner | The runner reads the fixture and must never write to it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-298-02 | Information disclosure | `data/harness-fixtures/converged-room/` | mitigate | The fixture is scaffold-born, so it contains only template text: no user content, no room content, no machine identity. The `current_room:` line is suppressed by pointing `MINDRIAN_ROOMS_HOME` at a scratch dir, and the acceptance criteria assert its absence |
| T-298-04 | Tampering | the fixture | mitigate | No `.mindrian/` and no `room.db` are committed, so `openRoomDbReadOnlyForCaller` returns null and the read-only door cannot create anything (D-03a; `openGraph` is forbidden because `lib/core/lazygraph-ops.cjs` calls `fs.mkdirSync(dbDir, {recursive:true})` on first touch) |
| T-298-01 | Tampering | committed text | mitigate | The em-dash and ANSI scrub is a scripted node replace with printed before/after counts, not a hand edit, and the acceptance criteria re-grep the whole tree |
| T-298-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase; Node built-ins only |
</threat_model>

<verification>
- 33 files, 18 directories, no `.mindrian/`, no `room.db`.
- STATE.md: `total_entries: 13`, `venture_stage: Investment`, one `computed:` line, no `current_room:` line.
- Zero U+2014, zero U+2013, zero `\x1b` bytes anywhere under the fixture.
- `git ls-files data/harness-fixtures/converged-room | wc -l` returns 33 and `git status --porcelain` is empty.
</verification>

<success_criteria>
- The committed fixture satisfies D-03 exactly: scaffold-born, text-only, 33 files, no database, no `.mindrian/`.
- The runner (plan 298-11) can later prove idempotence against it with `git status --porcelain` alone.
- No em-dash, en-dash or ANSI escape survives anywhere in the committed tree.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 298 across all plans. The plan-review-convergence source-grounding
pass reads this section to exclude newly created symbols from drift verification.

**New CLI scripts**
- `scripts/run-harness.cjs` - flags `--check`, `--tier <t>`, `--room <dir>`, `--policy <id>`, `--json`, `--help`
- `scripts/check-graph-derive-health.cjs` - flags `--room <dir>`, `--all`, `--json`, `--strict`, `--help`
- `scripts/check-voice-style.cjs` - Stop hook, stdin envelope, no flags

**New library modules and exported functions**
- `lib/hmi/turn-text.cjs` - `readTurnText`, `readTranscriptTurn`, `readTranscriptTail`, `extractAssistantText`, `classifyPrecedingUserContentSource`, `scanContentForAskUserQuestion`, `TRANSCRIPT_TAIL_BYTES`
- `lib/hmi/voice-style-log.cjs` - `appendVoiceStyleRow`, `readVoiceStyleRows`, `evaluatePromotion`, `mindrianHome`, `voiceStyleLogPath`
- `lib/core/doctor/voice-style-log-module.cjs` - `check`, `voiceStyleLogPath`

**New generator symbols in `scripts/build-harness-manifest.cjs`**
- `LARRY_SURFACE_BINDINGS`, `POLICIES_DIR`, `FIXTURE_DIR`, `digestDirectory`, `loadPolicies`, `validatePolicyFile`, `runContractChecks`; `MANIFEST_VERSION` becomes 2

**New manifest JSON keys** in `data/harness-manifest.json`: `policies` `{path, digest, count}`, `larry_surfaces` `[{role, path, digest}]`, `fixture_ref` `{path, digest}`

**New policy schema keys** in `data/harness-policies/_schema.json` under `_doc`: `purpose`, `rung_vocabulary`, `rung_meaning`, `kind_vocabulary`, `applies_to_vocabulary`, `policy_fields`, `runner_path_rule`, `promotion_rule_doc`, `gate_count_principle`, `validation_rule`, `default_on_miss`.
**Per-policy file keys**: `id`, `kind`, `runner`, `args`, `rung`, `evidence_log`, `promotion_rule` `{window_runs, max_false_positive_rate, min_true_positives}`, `owner`, `pinned_by`, `applies_to`, `notes`.

**New data paths**
- `data/harness-policies/` with `_schema.json`, `CONTEXT.md`, and 11 policy files: `gate-worktree-hygiene.json`, `gate-shape-declaration.json`, `gate-tool-honesty.json`, `gate-card-fire.json`, `gate-graph-derive-health.json`, `voice-hyphens-only.json`, `voice-glyph-present.json`, `voice-backend-noun-free.json`, `voice-fork-as-card.json`, `memory-write-policy.json`, `contract-parity-larry.json`
- `data/harness-fixtures/converged-room/` (33 committed text files)
- `<room>/.mindrian/harness-run.json` (runtime output, never committed)
- `$MINDRIAN_HOME/voice-style.jsonl` (runtime evidence log, never committed)

**New registry ids**
- doctor module id `voice-style-log` in `data/doctor-modules.json`
- doctor acceptance point id `harness-policies` in `scripts/doctor.cjs` `buildAcceptanceChecklist`
- one `hooks/hooks.json` Stop entry invoking `scripts/check-voice-style.cjs`

**New test files**
- `tests/test-298-policies-schema.cjs`, `tests/test-298-runner-idempotent.cjs`, `tests/test-298-derive-health.cjs`, `tests/test-298-voice-log.cjs`, `tests/test-298-contract-parity.cjs`, `tests/run-all-298.sh`

**New key on the `lib/core/recipe-maps.cjs` `loadManifest()` return**: `policies`

<output>
Create `.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-02-SUMMARY.md` when done, recording the exact scaffold return object, the compute-state invocation with its environment, the before/after scrub counts, and the 33-path file listing.
</output>
