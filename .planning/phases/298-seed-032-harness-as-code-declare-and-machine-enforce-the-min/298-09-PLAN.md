---
phase: 298
plan: 09
type: execute
wave: 3
depends_on: [298-01, 298-05, 298-06, 298-07]
files_modified:
  - data/harness-policies/_schema.json
  - data/harness-policies/CONTEXT.md
  - data/harness-policies/gate-worktree-hygiene.json
  - data/harness-policies/gate-shape-declaration.json
  - data/harness-policies/gate-tool-honesty.json
  - data/harness-policies/gate-card-fire.json
  - data/harness-policies/gate-graph-derive-health.json
  - data/harness-policies/voice-hyphens-only.json
  - data/harness-policies/voice-glyph-present.json
  - data/harness-policies/voice-backend-noun-free.json
  - data/harness-policies/voice-fork-as-card.json
  - data/harness-policies/memory-write-policy.json
  - data/harness-policies/contract-parity-larry.json
  - tests/test-298-policies-schema.cjs
autonomous: true
requirements: [R-02, R-03, R-07]

must_haves:
  truths:
    - "R-02: a hand-authored, human-diffable JSON file declares each policy, and a closed _schema.json fixes the vocabulary; every slice-1 gate is reachable from exactly one policy entry that names its runner, its rung, its evidence log and its pinning test"
    - "R-03: every policy carries an enforcement rung from the closed set declared, logged, blocking, and a promotion_rule decided BEFORE its evidence log is read"
    - "R-07: memory-write-policy.json declares D3, D4 and D5, and every phrase it names is present in skills/larry-personality/SKILL.md"
    - "A runner path is either null (an honest declared ghost) or a repo-relative path under scripts/ that resolves inside the repository root; nothing else validates"
    - "The 34 of 35 gates that have no evidence log today enter at declared and say so in their notes, because D8 promotes only on logged evidence"
  artifacts:
    - path: "data/harness-policies/_schema.json"
      provides: "The closed policy vocabulary the generator validates against"
      contains: "rung_vocabulary"
    - path: "data/harness-policies/CONTEXT.md"
      provides: "The Layer 2 contract doc: reads, does, writes, human check, change-impact"
    - path: "data/harness-policies/contract-parity-larry.json"
      provides: "The phrase set every Larry surface must carry plus the byte budget"
      contains: "byte_budget"
    - path: "data/harness-policies/memory-write-policy.json"
      provides: "The D3/D4/D5 declaration the persona states in prose"
      contains: "basket_fires_at"
    - path: "tests/test-298-policies-schema.cjs"
      provides: "R-02 and R-03 proof: closed vocabulary, no traversal, no unintentional ghosts"
  key_links:
    - from: "data/harness-policies/gate-graph-derive-health.json"
      to: "scripts/check-graph-derive-health.cjs"
      via: "the runner field, resolved inside the repository root"
      pattern: "check-graph-derive-health"
    - from: "data/harness-policies/memory-write-policy.json"
      to: "skills/larry-personality/SKILL.md"
      via: "every phrase the policy names is present in the skill"
      pattern: "NOT_REMEMBERED_BECAUSE"
    - from: "data/harness-policies/voice-hyphens-only.json"
      to: "$MINDRIAN_HOME/voice-style.jsonl"
      via: "the evidence_log field"
      pattern: "voice-style"
---

<objective>
Author the policy directory: one closed `_schema.json`, one Layer 2 contract doc, and the eleven
slice-1 policy files that make every slice-1 gate, every voice rule, the memory write policy and
the Larry contract reachable from one declared entry.

Purpose: R-02 and R-03. This is the declaration half of harness-as-code. Declaring what already
runs, at the rung it already runs at, is a transcription with no behavior change; the value is
that it becomes diffable, digestible and promotable by a stated rule.

Output: 13 new files under `data/harness-policies/` and one green schema test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-SPEC.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-PATTERNS.md
@docs/superpowers/specs/2026-09-07-harness-manifest-v2-design.md
@data/hitl-shape-declaration-schema.json
</context>

<locked_constraints>
- Never `git stash`. Explicit `git add <paths>` only. No em-dashes or en-dashes in any policy file, in the schema, or in the CONTEXT doc.
- JSON only. No YAML. Hand-rolled validation, never ajv and never zod, even though zod is already a dependency for the MCP SDK: this is a build-time artifact with no MCP context and the precedent is `data/hitl-shape-declaration-schema.json`.
- Do NOT hardcode the gate count anywhere. The gate set is enumerated from disk (`scripts/check-*.cjs` minus `*.test.cjs`, which is 35 today, not the 37 the glob returns). State the principle in `_schema.json`, never a frozen number.
- Do NOT add a `ROOM.md` to `data/harness-policies/`. The sibling fixture and schema directories under `data/` carry none; the design's own tree calls for `CONTEXT.md`, the Layer 2 contract idiom.
- The declared rung must match what the gate ALREADY does today. Declaring a gate as `blocking` that is currently advisory is a behavior change and a scope breach. `gate-shape-declaration` and `gate-tool-honesty` are advisory today and enter at `logged`.
- Slice 2, 3 and 4 policies are NOT authored here. Only the eleven named below.
</locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Author _schema.json and the CONTEXT.md Layer 2 contract</name>
  <files>data/harness-policies/_schema.json, data/harness-policies/CONTEXT.md</files>
  <read_first>
    - `data/hitl-shape-declaration-schema.json` in full: the single top-level `_doc` object holding the purpose paragraph, the closed vocabulary arrays inline, the per-member meaning table, the declaration forms, the trigger, the `surface_count_principle`, the `validation_rule`, and the explicit `default_on_miss`
    - `scripts/check-shape-declaration.cjs` (how a hand-rolled reader consumes that schema; no ajv, no zod)
    - `docs/superpowers/specs/2026-09-07-harness-manifest-v2-design.md` section 6 (the eleven policy keys and their meanings) and section 13 (error handling and honesty)
    - `.planning/phases/298-.../298-PATTERNS.md` section `data/harness-policies/_schema.json`
    - `.planning/phases/298-.../298-RESEARCH.md` Pitfall 5 (the labeled-rows-only rule that must be documented in the schema) and the Security Domain threat table row on `runner` paths
  </read_first>
  <action>
    Create `data/harness-policies/_schema.json` as a single top-level `_doc` object, mirroring the
    hitl-shape-declaration schema's registry-is-the-table idiom. It must carry:
    - `purpose`: one paragraph naming Phase 298, SEED-032, and the fact that this file IS the
      schema, read by `scripts/build-harness-manifest.cjs` at `--check` time and by
      `scripts/run-harness.cjs` when it loads the directory.
    - `rung_vocabulary`: the closed array `declared`, `logged`, `blocking`, plus a `rung_meaning`
      table giving each member its runtime semantics: declared is listed in the report only;
      logged is run, one JSONL line appended to `evidence_log`, never fails; blocking is run and
      fails the tier on a non-zero exit.
    - `kind_vocabulary`: the closed array `gate`, `voice`, `memory`, `contract`, with a meaning
      table.
    - `applies_to_vocabulary`: the closed array `pre-flight`, `pre-tag`, `full`, `stop-hook`,
      `room`.
    - `policy_fields`: the eleven field names with type and meaning: `id`, `kind`, `runner`,
      `args`, `rung`, `evidence_log`, `promotion_rule`, `owner`, `pinned_by`, `applies_to`,
      `notes`.
    - `runner_path_rule`: `runner` is either null (an honest declared ghost) or a repo-relative
      path beginning `scripts/` that must resolve inside the repository root; a path that escapes
      the root, an absolute path, or a path containing a parent-directory segment is rejected. The
      runner is always spawned with an argv array, never a shell string, and `args` is passed
      verbatim with no interpolation.
    - `promotion_rule_doc`: the object shape `window_runs`, `max_false_positive_rate`,
      `min_true_positives`, plus the load-bearing counting rule stated explicitly:
      `max_false_positive_rate` is computed over HUMAN-LABELED rows only; a row whose `result` is
      still the default `fire` counts as neither a true positive nor a false positive, because
      counting unlabeled rows would make an unreviewed log read as a zero percent false-positive
      rate and auto-satisfy any threshold. State that the rule is decided BEFORE the log is read
      and that promotion is a human edit to one line, never something the runner does.
    - `gate_count_principle`: the gate set is enumerated from disk at check time
      (`scripts/check-*.cjs` excluding `*.test.cjs`) and NEVER hardcoded, mirroring the shape
      schema's `surface_count_principle`.
    - `validation_rule`: an unknown top-level key, an unknown `rung`, an unknown `kind`, an
      unknown `applies_to` member, a missing required field, or a `runner` that fails
      `runner_path_rule` is a hard `--check` failure naming the file and the failing key.
    - `default_on_miss`: reject, fail closed, with the reason.

    Create `data/harness-policies/CONTEXT.md` as the Layer 2 contract doc, using the reads / does
    / writes / human check idiom: what this directory reads (nothing; it is read BY the generator
    and the runner), what it does (declares policies), what it writes (nothing), and the human
    check (a rung change is a one-line human edit reviewed in a diff). Include a change-impact
    table naming every consumer that must be re-run when a policy file changes: the generator
    `--check`, the pre-commit drift guard, the runner, and the `harness-policies` doctor
    acceptance point. State the migration slices and that slice 1 is what this phase lands.
  </action>
  <verify>
    <automated>node -e "const s=require('./data/harness-policies/_schema.json'); const d=s._doc; ['purpose','rung_vocabulary','rung_meaning','kind_vocabulary','applies_to_vocabulary','policy_fields','runner_path_rule','promotion_rule_doc','gate_count_principle','validation_rule','default_on_miss'].forEach(function(k){ if(!(k in d)) throw new Error('missing _doc key '+k); }); const rv=d.rung_vocabulary.join(','); if(rv!=='declared,logged,blocking') throw new Error('rung vocabulary wrong: '+rv); console.log('schema ok');" && test -f data/harness-policies/CONTEXT.md && echo "context ok"</automated>
  </verify>
  <acceptance_criteria>
    - `data/harness-policies/_schema.json` parses as JSON and its `_doc` carries all eleven named keys.
    - `_doc.rung_vocabulary` equals exactly the array declared, logged, blocking, in that order.
    - `_doc.kind_vocabulary` equals exactly gate, voice, memory, contract.
    - `_doc.applies_to_vocabulary` equals exactly pre-flight, pre-tag, full, stop-hook, room.
    - `_doc.promotion_rule_doc` text contains the words labeled and unlabeled and states that unlabeled rows count as neither.
    - `_doc.runner_path_rule` text names both the repo-relative-under-scripts constraint and the argv-array spawn rule.
    - `grep -cE "\b3[57]\b" data/harness-policies/_schema.json` returns 0 (no hardcoded gate count).
    - `data/harness-policies/CONTEXT.md` contains a change-impact table naming the generator, the pre-commit guard, the runner and the doctor point.
    - `grep -cP '\x{2014}|\x{2013}' data/harness-policies/_schema.json data/harness-policies/CONTEXT.md` reports 0 for both.
  </acceptance_criteria>
  <done>The closed schema and the Layer 2 contract doc exist, with the labeled-rows rule and the runner path rule stated in the schema itself.</done>
</task>

<task type="auto">
  <name>Task 2: Author the eleven slice-1 policy files</name>
  <files>data/harness-policies/gate-worktree-hygiene.json, data/harness-policies/gate-shape-declaration.json, data/harness-policies/gate-tool-honesty.json, data/harness-policies/gate-card-fire.json, data/harness-policies/gate-graph-derive-health.json, data/harness-policies/voice-hyphens-only.json, data/harness-policies/voice-glyph-present.json, data/harness-policies/voice-backend-noun-free.json, data/harness-policies/voice-fork-as-card.json, data/harness-policies/memory-write-policy.json, data/harness-policies/contract-parity-larry.json</files>
  <read_first>
    - `data/doctor-modules.json` (the flat 7-key per-entry model these files take as their shape reference)
    - `.planning/phases/298-.../298-RESEARCH.md` section "Gate and module inventory", the per-script flag and exit-code table (for `args` and for the natural rung of each of the four slice-1 gates), and the sentence recording that 34 of 35 gates have no evidence log today
    - `scripts/check-worktree-hygiene.cjs`, `scripts/check-shape-declaration.cjs`, `scripts/check-tool-honesty.cjs`, `scripts/check-card-fire.cjs`, `scripts/check-graph-derive-health.cjs` - confirm the actual parsed flags for each before writing its `args`; the research's flag column is a literal grep and is a strong but imperfect proxy
    - `.planning/phases/298-.../298-RESEARCH.md` section "The core-contract phrase set, per surface, with the test that already pins each one" (this table IS the `contract-parity-larry` payload) and section "The byte budget, measured now"
    - `.planning/phases/298-.../298-SUMMARY` for plan 298-05 (the exact identifier strings the skill now carries), or `skills/larry-personality/SKILL.md` directly
    - `docs/superpowers/specs/2026-09-07-harness-manifest-v2-design.md` section 10 (the four voice policies and their rungs) and section 12 (the slice-1 membership)
  </read_first>
  <action>
    Write eleven JSON files, one policy each, every one carrying all eleven schema fields. Keep
    them flat and diffable, one object per file, scalars and short arrays only.

    Rungs, set to what each gate does TODAY, never to what it should do:
    - `gate-worktree-hygiene`: kind gate, runner `scripts/check-worktree-hygiene.cjs`, args the
      flags that script actually parses for a check run, rung `blocking` (it is already an
      acceptance point), applies_to pre-tag and full, pinned_by `tests/test-260906-t3s-worktree-hygiene.cjs`.
    - `gate-shape-declaration`: runner `scripts/check-shape-declaration.cjs`, rung `logged` (it is
      advisory WARN since Phase 210; `--strict` restores hard fail), applies_to pre-tag and full.
    - `gate-tool-honesty`: runner `scripts/check-tool-honesty.cjs`, rung `logged` (advisory in
      release.sh today), applies_to pre-tag and full.
    - `gate-card-fire`: runner `scripts/check-card-fire.cjs`, rung `logged`, applies_to stop-hook.
      Its `evidence_log` is the existing `card-fire-intercepts.log` under `MINDRIAN_HOME`. Its
      `notes` must record that this is the D8 precedent: it is the only one of the 35 gates that
      already writes an evidence log, and its own 86 percent false-positive history is why the
      ladder exists.
    - `gate-graph-derive-health`: runner `scripts/check-graph-derive-health.cjs`, rung `logged`
      for one release per requirement 5, applies_to room and full. Its `notes` must name the
      module it wraps (`lib/core/doctor/graph-derive-health-module.cjs`) as the Canon Part 7
      reuse justification, and must state that the fixture reports `skip` because a Tier 0 room
      has no `room.db` and that `skip` is the green case.
    - `voice-hyphens-only`, `voice-glyph-present`: kind voice, runner
      `scripts/check-voice-style.cjs`, rung `declared` (rung 1 stated, rung 2 now logging),
      applies_to stop-hook, evidence_log the `voice-style.jsonl` path under `MINDRIAN_HOME`.
    - `voice-backend-noun-free`: kind voice, runner null (an honest declared ghost; no scanner
      exists for it), rung `declared`, applies_to stop-hook. Its `notes` must say plainly that it
      has no runner yet and is therefore a declared ghost, never counted as passing.
    - `voice-fork-as-card`: kind voice, runner `scripts/check-card-fire.cjs`, rung `logged`
      (card-fire already logs this), applies_to stop-hook.
    - `memory-write-policy`: kind memory, runner null (a declaration, not an executable check),
      rung `declared`, applies_to room. Beyond the eleven common fields it carries the D3, D4 and
      D5 declaration: a `channels` object mapping Belief to the room graph through
      `navigation.cjs`, Progress to `STATE.md`, Experience to `.planning/seeds/` plus
      `memory_event`; a `verbs` object mapping track to `context_assemble`, commit to
      `graph_write` and `claim_write`, recall to `graph_query` and `graph_reason`, note to
      `memory_event`; `basket_fires_at` set to 2; `claim_review_status` set to `proposed`;
      `toggled_off_writes` set to `NOT_REMEMBERED_BECAUSE`; `narrated` set to false with a Canon
      Part 12 reference. Every identifier string here must be one the skill already carries; the
      test asserts that both ways.
    - `contract-parity-larry`: kind contract, runner null (the generator runs it inline at
      `--check` because it is a pure file read), rung `blocking`, applies_to pre-flight, pre-tag
      and full. It carries a `surfaces` array of the three Larry surfaces with a `phrases` array
      each, and a `byte_budget` object naming `lib/mcp/runtime-instructions.cjs`, the exported
      constant `RUNTIME_INSTRUCTIONS`, and the limit 1950. Populate `phrases` ONLY with phrases a
      test already pins, taken from the research's per-surface table, PLUS the two genuine
      unpinned beta.27 gaps: the `## If asked about Theo by name` heading in
      `agents/larry-extended.md` and the `THEO:` clause in `RUNTIME_INSTRUCTIONS`. Adding a phrase
      no test pins manufactures a second source of truth the design forbids. Do NOT declare
      `lib/mcp/larry-server-instructions.md`; it is an orphan loaded by nothing at runtime.

    Every `notes` field is one paragraph, hyphens only, that says which rung the policy is on and
    WHY, in the presence-and-salience terms this phase uses. For any gate with no evidence log
    today, say so in the notes: it enters at its current rung and cannot be promoted until a log
    exists, because D8 promotes only on logged evidence.

    Every `promotion_rule` is decided now, before any log is read, and carries all three keys.
    For the voice policies use a `min_true_positives` grounded in the only sourced figure
    available (the 50 to 100 human-reviewed traces from Vanishing Gradients Ep. 57, recorded in
    the research's LangTalks section); state the chosen number and its source in the notes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),path=require('path'); const dir='data/harness-policies'; const files=fs.readdirSync(dir).filter(function(f){return f.endsWith('.json') && f!=='_schema.json';}); if(files.length!==11) throw new Error('expected 11 policy files, found '+files.length); const req=['id','kind','runner','args','rung','evidence_log','promotion_rule','owner','pinned_by','applies_to','notes']; const ids=new Set(); files.forEach(function(f){ const p=JSON.parse(fs.readFileSync(path.join(dir,f),'utf8')); req.forEach(function(k){ if(!(k in p)) throw new Error(f+' missing '+k); }); if(['declared','logged','blocking'].indexOf(p.rung)<0) throw new Error(f+' bad rung'); if(['gate','voice','memory','contract'].indexOf(p.kind)<0) throw new Error(f+' bad kind'); if(p.id+'.json'!==f) throw new Error(f+' id does not match filename'); if(ids.has(p.id)) throw new Error('duplicate id '+p.id); ids.add(p.id); if(p.runner!==null){ if(p.runner.indexOf('scripts/')!==0||p.runner.indexOf('..')>=0||path.isAbsolute(p.runner)) throw new Error(f+' runner path rule violated'); if(!fs.existsSync(path.resolve(p.runner))) throw new Error(f+' runner missing on disk: '+p.runner); } }); console.log('11 policies ok');"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly eleven policy JSON files exist beside `_schema.json`, each parsing as JSON with all eleven required fields.
    - Every `id` equals its filename without the extension, and no id repeats.
    - Every `rung` is a member of declared, logged, blocking; every `kind` is a member of gate, voice, memory, contract; every `applies_to` member is in the declared vocabulary.
    - Every non-null `runner` begins with `scripts/`, contains no parent-directory segment, is not absolute, and exists on disk.
    - The only null runners are `voice-backend-noun-free`, `memory-write-policy` and `contract-parity-larry`, and each of the three states in `notes` why it has no runner.
    - `gate-shape-declaration` and `gate-tool-honesty` are at rung `logged`, not `blocking` (they are advisory today; declaring them blocking would be a behavior change).
    - `memory-write-policy.json` `basket_fires_at` equals 2 and its channel and verb identifier strings all appear in `skills/larry-personality/SKILL.md`.
    - `contract-parity-larry.json` `byte_budget` names `lib/mcp/runtime-instructions.cjs`, `RUNTIME_INSTRUCTIONS` and 1950, and its `surfaces` array has exactly three entries.
    - `grep -c "larry-server-instructions" data/harness-policies/contract-parity-larry.json` returns 0.
    - `grep -rcP '\x{2014}|\x{2013}' data/harness-policies/` reports 0 for every file.
  </acceptance_criteria>
  <done>Eleven policy files declare the slice-1 harness at the rungs it already runs at, with runner paths that resolve and notes that state the rung rationale.</done>
</task>

<task type="auto">
  <name>Task 3: Turn tests/test-298-policies-schema.cjs green</name>
  <files>tests/test-298-policies-schema.cjs</files>
  <read_first>
    - `tests/test-298-policies-schema.cjs` (the stub from plan 298-01 with its PENDING list)
    - `tests/test-harness-manifest-check.cjs` (the mirror: in-process validation against an exported allowlist, the lookup helper, the sha256 hex assertion style)
    - `data/harness-policies/_schema.json` as written in Task 1
    - `skills/larry-personality/SKILL.md` (for the memory-write-policy cross-check)
  </read_first>
  <action>
    Flip `ASSERTIONS_IMPLEMENTED` to `true` and implement the PENDING list with the `record`
    counter idiom.

    Assertions, one `record` each:
    1. Every file in `data/harness-policies/` other than `_schema.json` and `CONTEXT.md` parses
       as JSON and carries exactly the eleven schema fields, plus only the extra keys the schema
       explicitly permits for `memory-write-policy` and `contract-parity-larry`.
    2. Closed vocabularies: every `rung`, `kind` and `applies_to` member is drawn from the arrays
       in `_schema.json`, read from the file rather than hardcoded in the test.
    3. Unknown-key rejection: write a temporary policy file carrying an extra key `xyz` into the
       real policy directory inside a try-finally, spawn
       `node scripts/build-harness-manifest.cjs --check`, assert the exit status is non-zero and
       that the combined stdout and stderr names both the temporary file name and the key `xyz`,
       then delete the temporary file and assert `--check` returns to its prior exit status. If
       plan 298-12 has not yet landed the generator's policy validation, guard this one assertion
       on a `grep` of `scripts/build-harness-manifest.cjs` for `harness-policies` and record it
       as skipped with a stated reason rather than failing.
    4. Runner path safety: every non-null `runner` begins with `scripts/`, contains no
       parent-directory segment, is not absolute, resolves inside the repository root, and exists
       on disk. Assert the negative too, in-process against a synthetic policy object: a runner of
       an absolute path, a runner containing a parent-directory segment, and a runner outside
       `scripts/` each fail the rule.
    5. Ghost accounting: exactly three policies have a null runner, they are the three named ones,
       and each of the three carries a `notes` string explaining why.
    6. Promotion rule shape: every `promotion_rule` carries `window_runs`,
       `max_false_positive_rate` and `min_true_positives`, with numeric values and
       `max_false_positive_rate` between 0 and 1.
    7. R-07 cross-check: every identifier string named in `memory-write-policy.json` channels and
       verbs appears in `skills/larry-personality/SKILL.md`, and `basket_fires_at` is at least 2.
    8. R-03 side-effect check: `require` the evaluator from `lib/hmi/voice-style-log.cjs`, call
       `evaluatePromotion` twice against a synthetic policy and a synthetic row array, and assert
       no file under `data/harness-policies/` changed mtime.

    Commit with an explicit `git add data/harness-policies tests/test-298-policies-schema.cjs`.
  </action>
  <verify>
    <automated>node tests/test-298-policies-schema.cjs; echo "test-exit=$?"; bash tests/run-all-298.sh; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-298-policies-schema.cjs` exits 0 with at least eight `ok` lines.
    - `grep -c "ASSERTIONS_IMPLEMENTED = true" tests/test-298-policies-schema.cjs` returns 1.
    - The vocabularies asserted in the test are READ from `_schema.json`, not hardcoded: `grep -c "_schema.json" tests/test-298-policies-schema.cjs` returns at least 1.
    - The unknown-key case cleans up after itself: after the test, `ls data/harness-policies/*.json | wc -l` returns 12 (eleven policies plus the schema) and `git status --porcelain data/harness-policies` is empty.
    - `bash tests/run-all-298.sh` exits 0 with the policies-schema leg PASSED.
    - `grep -cP '\x{2014}|\x{2013}' tests/test-298-policies-schema.cjs` returns 0.
  </acceptance_criteria>
  <done>The schema test proves the closed vocabulary, the runner path rule with its three negatives, the ghost accounting, the promotion-rule shape, and the skill cross-check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy JSON -> spawned process | A policy's `runner` and `args` become a spawned command; this is the one real new attack surface in the phase |
| policy JSON -> release train | A wrongly declared rung could turn an advisory gate into a release blocker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-298-03 | Elevation of privilege | the `runner` field | mitigate | `_schema.json` constrains `runner` to null or a repo-relative path under `scripts/`; the test asserts the rule plus three negatives (absolute path, parent-directory segment, path outside `scripts/`); the runner (plan 298-10) resolves against the repository root and rejects an escape, and spawns with an argv array so `args` is never interpolated into a shell string |
| T-298-05 | Denial of service | a malformed policy file | mitigate | Per-file try/catch: one bad file is reported by name and skipped from digesting while `--check` still fails, the shipped T-233-01 and T-217-01 self-DoS pattern |
| T-298-06 | Repudiation | `promotion_rule` | mitigate | The labeled-rows-only counting rule is documented in `_schema.json` itself and asserted by the test, so an unreviewed log cannot auto-qualify |
| T-298-21 | Tampering | declared rungs | mitigate | Every rung must match what the gate does today; the acceptance criteria name the two advisory gates that must enter at `logged`, so no gate silently becomes a release blocker |
| T-298-02 | Information disclosure | policy files | accept | Policies carry machinery metadata only: script paths, rungs, test names and rationale. No room content, no user bytes, no secrets. `methodology_tier` is mindrian-operation |
| T-298-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase; Node built-ins only, and the hand-rolled validation deliberately avoids ajv and zod |
</threat_model>

<verification>
- `node tests/test-298-policies-schema.cjs` exits 0.
- Eleven policy files plus `_schema.json` and `CONTEXT.md` exist; every non-null runner resolves.
- `bash tests/run-all-298.sh` exits 0.
- `git status --porcelain data/harness-policies` is empty after the test run.
</verification>

<success_criteria>
- R-02 delivered for slice 1: every slice-1 gate, every voice rule, the memory write policy and the Larry contract are reachable from one declared entry.
- R-03 delivered: a closed rung set, a promotion rule decided before the log is read, and a documented rule that unlabeled evidence never counts.
- R-07's declaration half delivered: `memory-write-policy.json` declares D3, D4 and D5 and every phrase it names exists in the skill.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 298 across all plans. The plan-review-convergence source-grounding
pass reads this section to exclude newly created symbols from drift verification.

**New CLI scripts**
- `scripts/run-harness.cjs` - flags `--check`, `--tier <t>`, `--room <dir>`, `--policy <id>`, `--json`, `--help`
- `scripts/check-graph-derive-health.cjs` - flags `--room <dir>`, `--all`, `--json`, `--strict`, `--check`, `--help`
- `scripts/check-voice-style.cjs` - Stop hook, stdin envelope, no flags

**New library modules and exported functions**
- `lib/hmi/turn-text.cjs` - `readTurnText`, `readTranscriptTurn`, `readTranscriptTail`, `extractAssistantText`, `classifyPrecedingUserContentSource`, `scanContentForAskUserQuestion`, `TRANSCRIPT_TAIL_BYTES`
- `lib/hmi/voice-style-log.cjs` - `appendVoiceStyleRow`, `readVoiceStyleRows`, `evaluatePromotion`, `mindrianHome`, `voiceStyleLogPath`
- `lib/core/doctor/voice-style-log-module.cjs` - `check`, `voiceStyleLogPath`

**New generator symbols in `scripts/build-harness-manifest.cjs`**
- `LARRY_SURFACE_BINDINGS`, `POLICIES_DIR`, `FIXTURE_DIR`, `digestDirectory`, `loadPolicies`, `validatePolicyFile`, `runContractChecks`; `MANIFEST_VERSION` becomes 2

**New manifest JSON keys** in `data/harness-manifest.json`: `policies` with `path`, `digest`, `count`; `larry_surfaces` as an array of `role`, `path`, `digest`; `fixture_ref` with `path`, `digest`

**New policy schema keys** in `data/harness-policies/_schema.json` under `_doc`: `purpose`, `rung_vocabulary`, `rung_meaning`, `kind_vocabulary`, `applies_to_vocabulary`, `policy_fields`, `runner_path_rule`, `promotion_rule_doc`, `gate_count_principle`, `validation_rule`, `default_on_miss`.
**Per-policy file keys**: `id`, `kind`, `runner`, `args`, `rung`, `evidence_log`, `promotion_rule` with `window_runs`, `max_false_positive_rate`, `min_true_positives`; `owner`, `pinned_by`, `applies_to`, `notes`.
**Extra keys permitted on two policies**: `memory-write-policy` carries `channels`, `verbs`, `basket_fires_at`, `claim_review_status`, `toggled_off_writes`, `narrated`; `contract-parity-larry` carries `surfaces` (each with `role`, `path`, `phrases`) and `byte_budget` (with `path`, `constant`, `limit`).

**New data paths**
- `data/harness-policies/` with `_schema.json`, `CONTEXT.md`, and 11 policy files: `gate-worktree-hygiene.json`, `gate-shape-declaration.json`, `gate-tool-honesty.json`, `gate-card-fire.json`, `gate-graph-derive-health.json`, `voice-hyphens-only.json`, `voice-glyph-present.json`, `voice-backend-noun-free.json`, `voice-fork-as-card.json`, `memory-write-policy.json`, `contract-parity-larry.json`
- `data/harness-fixtures/converged-room/` (33 committed text files)
- `<room>/.mindrian/harness-run.json` (runtime output, never committed)
- `$MINDRIAN_HOME/voice-style.jsonl` and its rotation `voice-style.jsonl.1` (runtime, never committed)

**New registry ids**
- doctor module id `voice-style-log` in `data/doctor-modules.json`
- doctor acceptance point id `harness-policies` in `scripts/doctor.cjs` `buildAcceptanceChecklist`
- one `hooks/hooks.json` Stop entry invoking `scripts/check-voice-style.cjs`

**New test files**
- `tests/test-298-policies-schema.cjs`, `tests/test-298-runner-idempotent.cjs`, `tests/test-298-derive-health.cjs`, `tests/test-298-voice-log.cjs`, `tests/test-298-contract-parity.cjs`, `tests/run-all-298.sh`

**New key on the `lib/core/recipe-maps.cjs` `loadManifest()` return**: `policies`

<output>
Create `.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-09-SUMMARY.md` when done, listing every policy with its id, kind, runner, rung and applies_to in one table, and recording the chosen `min_true_positives` value with its source.
</output>
