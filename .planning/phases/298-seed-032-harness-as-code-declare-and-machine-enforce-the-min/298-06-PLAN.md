---
phase: 298
plan: 06
type: execute
wave: 2
depends_on: [298-01, 298-02]
files_modified:
  - scripts/check-graph-derive-health.cjs
  - tests/test-298-derive-health.cjs
autonomous: true
requirements: [R-05]

must_haves:
  truths:
    - "SEED-037 4d ships as a gate: a room whose semantic derivation never ran is detectable from the command line"
    - "The gate is a thin CLI wrapper over the shipped detectRoomHealth, never a second detector (its module header declares itself the ONE shared signal with two consumers)"
    - "The committed converged-room fixture has no room.db, so the gate reports status skip and exits 0, which is the green case"
    - "A room with BELONGS_TO edges and zero cascade edges reports status fail and the gate exits 1"
    - "The gate follows the repo exit convention: 0 clean, 1 finding, 2 scanner or usage fault"
  artifacts:
    - path: "scripts/check-graph-derive-health.cjs"
      provides: "The SEED-037 4d CLI gate, wrapping detectRoomHealth"
      exports: ["main"]
      min_lines: 60
    - path: "tests/test-298-derive-health.cjs"
      provides: "R-05 proof: red on a stale unhealthy room, green on the fixture"
  key_links:
    - from: "scripts/check-graph-derive-health.cjs"
      to: "lib/core/doctor/graph-derive-health-module.cjs"
      via: "require of detectRoomHealth and check"
      pattern: "graph-derive-health-module"
---

<objective>
Build `scripts/check-graph-derive-health.cjs` (R-05, SEED-037 4d) as a thin CLI wrapper over the
detector that already ships, and turn `tests/test-298-derive-health.cjs` green.

Purpose: SEED-037 showed Larry must not write into graphs nothing downstream derives. The
detection signal shipped in Phase 233 as `detectRoomHealth()`; what is missing is a command-line
gate the runner can spawn and a policy can point at. This phase ships the monitor, not the
repair: healing the roughly 16 already-damaged rooms (SEED-037 4c) stays a separate, human-gated
action.

Output: one new check script and one green test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-SPEC.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-PATTERNS.md
@lib/core/doctor/graph-derive-health-module.cjs
@scripts/check-worktree-hygiene.cjs
</context>

<locked_constraints>
- Never `git stash`. Explicit `git add <paths>` only. No em-dashes or en-dashes.
- CJS only, `process.argv` switch-case routing, no Commander and no yargs, no new dependencies.
- Do NOT reimplement any part of `detectRoomHealth`. Its module header declares it the ONE shared detection signal with two consumers and never a second copy. A third consumer is a wrapper.
- The gate never repairs a room. Healing is SEED-037 4c and is out of scope.
- The `governance.cjs` proposed-node throwaway-database unit test that D-03 asks for is NOT built here. Plan 298-04 discharges it by extending `tests/test-189-governance-candidates.cjs`, which already owns that harness. Do not build a second one.
</locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write the check-graph-derive-health CLI wrapper</name>
  <files>scripts/check-graph-derive-health.cjs</files>
  <read_first>
    - `lib/core/doctor/graph-derive-health-module.cjs` in full: the header that declares it the one shared signal, `detectRoomHealth` at line 203 and its return shape, the status derivation at 244-262, the Tier-0 skip reason at 224, `check(ctx)` at 307 (registry-scoped, honors `ctx.flags.cascadeRooms`), `resolveRoomPath`, `QUEUE_STUCK_DAYS`, and the read-only opener use at line 219
    - `scripts/check-worktree-hygiene.cjs` lines 330-480: the argv loop with the `--root` value pattern and the unknown-flag exit 2, the scanner-fault wrapping that prints a script-name-prefixed stderr message and exits 2, and the `--json` report shape
    - `.planning/phases/298-.../298-PATTERNS.md` section `scripts/check-graph-derive-health.cjs` (the wrapper contract table and the exit map)
    - `.planning/phases/298-.../298-RESEARCH.md` section "SEED-037 4d: the derive-health gate" including the live probe result against a scaffolded fixture
  </read_first>
  <action>
    Create `scripts/check-graph-derive-health.cjs` with a header comment stating: what it is (the
    SEED-037 4d gate), that it is a WRAPPER over `lib/core/doctor/graph-derive-health-module.cjs`
    and names that module explicitly as the Canon Part 7 reuse justification, that it never
    repairs (4c is separate and human-gated), the exit map, and the Canon Part 8 line (local file
    and local database reads only, zero network).

    Copy the argv loop from `scripts/check-worktree-hygiene.cjs` verbatim in shape. Flags:
    `--room <dir>` takes a value and follows the `--root` pattern exactly (a missing value prints
    a script-name-prefixed error and exits 2, then `i++`); `--all`; `--json`; `--strict`;
    `--check` accepted as an explicit no-op default; `--help` prints usage and exits 0; anything
    else prints `check-graph-derive-health: unknown flag <a> (see --help)` on stderr and exits 2.

    Dispatch: with `--room <dir>`, call `detectRoomHealth(dir)` on that one path. With `--all`,
    call `check({flags: {cascadeRooms: true}})`. With neither, call `check({flags: {}})` for the
    registry-active room.

    Exit map, exactly: `fail` maps to 1; `warn` maps to 1 only under `--strict` and to 0
    otherwise; `ok` maps to 0; `skip` maps to 0. The `skip` to 0 mapping is load-bearing: the
    committed fixture has no `room.db` and therefore reports `skip`, and that is the green case.

    `--json` prints one `console.log(JSON.stringify(obj, null, 2))` of the object the module
    returned, verbatim, with no added or renamed keys. Without `--json`, print a short human line
    naming the status, the room path, the queue count and the reasons.

    Wrap every non-trivial step so a scanner fault prints
    `check-graph-derive-health: <message>` on stderr and exits 2, never a stack trace.

    Split `main()` from the module body with the `require.main === module` pattern and export the
    pure pieces (at minimum `main` and the exit-mapping function) so the test can exercise the
    mapping in-process without spawning.

    Do not add a CIRS or HITL shape declaration. No sibling `scripts/check-*.cjs` carries one; the
    precedent is unambiguous. Confirm with `node scripts/check-cirs-declaration.cjs --check` after
    the file lands.
  </action>
  <verify>
    <automated>node scripts/check-graph-derive-health.cjs --room data/harness-fixtures/converged-room --json; echo "fixture-exit=$?"; node scripts/check-graph-derive-health.cjs --bogus-flag; echo "unknown-flag-exit=$?"; node scripts/check-cirs-declaration.cjs --check; echo "cirs-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-graph-derive-health.cjs --room data/harness-fixtures/converged-room --json` exits 0 and its JSON carries `"status": "skip"` with a reason mentioning no room.db.
    - `node scripts/check-graph-derive-health.cjs --bogus-flag` exits 2 and prints a message beginning `check-graph-derive-health:` on stderr.
    - `node scripts/check-graph-derive-health.cjs --room` with no value exits 2.
    - `node scripts/check-graph-derive-health.cjs --help` exits 0 and lists `--room`, `--all`, `--json`, `--strict`.
    - `grep -c "graph-derive-health-module" scripts/check-graph-derive-health.cjs` returns at least 1.
    - `grep -c "detectRoomHealth" scripts/check-graph-derive-health.cjs` returns at least 1 and there is no local reimplementation: `grep -c "BELONGS_TO" scripts/check-graph-derive-health.cjs` returns 0.
    - `node scripts/check-cirs-declaration.cjs --check` matches its recorded baseline exit code.
    - `grep -cP '\x{2014}|\x{2013}' scripts/check-graph-derive-health.cjs` returns 0.
  </acceptance_criteria>
  <done>The gate exists, wraps the shipped detector, honors the four flags and the exit map, and reports skip and exit 0 on the fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Turn tests/test-298-derive-health.cjs green</name>
  <files>tests/test-298-derive-health.cjs</files>
  <read_first>
    - `tests/test-298-derive-health.cjs` (the stub from plan 298-01, including its PENDING list and its `ASSERTIONS_IMPLEMENTED` flag)
    - `tests/test-260906-t3s-worktree-hygiene.cjs` lines 1-60 (the `record` counter, `assertEqual`, and the disposable-fixture principle stated in its header)
    - `tests/test-189-governance-candidates.cjs` lines 18-40 (the `node:sqlite` availability SKIP guard idiom and how it builds a throwaway database)
    - `lib/core/doctor/graph-derive-health-module.cjs` lines 203-265 (which counts drive which status, so the red fixture is built to hit `needsHeal` rather than a stuck queue)
  </read_first>
  <action>
    Flip `ASSERTIONS_IMPLEMENTED` to `true` and implement the PENDING list.

    Keep the `node:sqlite` availability SKIP guard at the top so a runtime without it prints a
    SKIP line and exits 0 rather than failing.

    Red case: build a throwaway room directory with `fs.mkdtempSync`, create `.mindrian/room.db`
    inside it, and populate it so `detectRoomHealth` derives `fail`: insert nodes and at least one
    BELONGS_TO edge and zero cascade edges, matching the `needsHeal` branch. Assert
    `detectRoomHealth(dir).status` equals the string `fail`, then spawn
    `node scripts/check-graph-derive-health.cjs --room <dir> --json` with `child_process.spawnSync`
    and assert the process exit status is 1 and the parsed stdout carries the same status. Remove
    the temp directory in a `finally`.

    Green case: run the same spawn against `data/harness-fixtures/converged-room` and assert exit
    status 0 and `status` equal to the string `skip`. Then assert the fixture was not mutated:
    `data/harness-fixtures/converged-room/.mindrian` still does not exist, and
    `git status --porcelain data/harness-fixtures/converged-room` is empty.

    Flag cases: `--json` output parses as JSON and its key set equals the key set
    `detectRoomHealth` returned in-process for the same room; an unknown flag exits 2.

    Use the `record(name, fn)` counter and exit 1 if `failCount` is non-zero. Build the throwaway
    database with `node:sqlite` directly, in this file only, so `node:sqlite` is required by
    exactly one new 298 test.

    Commit with an explicit `git add scripts/check-graph-derive-health.cjs tests/test-298-derive-health.cjs`.
  </action>
  <verify>
    <automated>node tests/test-298-derive-health.cjs; echo "test-exit=$?"; git status --porcelain data/harness-fixtures/converged-room; bash tests/run-all-298.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-298-derive-health.cjs` exits 0 and reports at least four `ok` lines.
    - `grep -c "ASSERTIONS_IMPLEMENTED = true" tests/test-298-derive-health.cjs` returns 1.
    - After the test run, `git status --porcelain data/harness-fixtures/converged-room` is empty and `data/harness-fixtures/converged-room/.mindrian` does not exist.
    - `bash tests/run-all-298.sh` exits 0 with the derive-health leg reporting PASSED rather than SKIPPED.
    - The test creates and removes its throwaway room under an `fs.mkdtempSync` path and never writes inside the repository tree: `grep -c "mkdtempSync" tests/test-298-derive-health.cjs` returns at least 1.
    - `grep -cP '\x{2014}|\x{2013}' tests/test-298-derive-health.cjs` returns 0.
  </acceptance_criteria>
  <done>The derive-health test is green on both the red and the green case and proves the fixture was not mutated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--room <dir>` argument -> filesystem | A caller-supplied directory path is opened for reading |
| room.db -> gate process | The database is opened through the module's read-only door; the gate never writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-298-04 | Tampering | `scripts/check-graph-derive-health.cjs` | mitigate | The wrapper never opens a database itself. `detectRoomHealth` uses the read-only door at `lib/core/doctor/graph-derive-health-module.cjs:219`; the acceptance criteria assert the fixture is unmutated and `.mindrian` is still absent after a run |
| T-298-14 | Denial of service | the wrapper | mitigate | Every non-trivial step is wrapped to print a script-name-prefixed stderr message and exit 2 rather than throwing; a single bad room cannot abort a sweep |
| T-298-15 | Repudiation | status derivation | mitigate | Status is derived mechanically from counts inside the shipped module (the T-233-04 false-success guard) and the wrapper only maps it to an exit code; the wrapper contains no BELONGS_TO logic of its own, asserted by grep |
| T-298-02 | Information disclosure | `--json` output | mitigate | The printed object is the module's return verbatim: counts, statuses and reasons, no node bodies and no room content, and no network call |
| T-298-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase; Node built-ins only |
</threat_model>

<verification>
- The fixture run exits 0 with status skip; the throwaway unhealthy room exits 1 with status fail.
- An unknown flag exits 2; `--room` with no value exits 2.
- `git status --porcelain data/harness-fixtures/converged-room` is empty after the test.
- `bash tests/run-all-298.sh` exits 0.
</verification>

<success_criteria>
- R-05's gate exists as a wrapper, with the shipped detector named in its header as the reuse justification.
- The exit map is exactly fail 1, warn 1 under strict and 0 otherwise, ok 0, skip 0.
- `tests/test-298-derive-health.cjs` is green and proves the fixture is untouched.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 298 across all plans. The plan-review-convergence source-grounding
pass reads this section to exclude newly created symbols from drift verification.

**New CLI scripts**
- `scripts/run-harness.cjs` - flags `--check`, `--tier <t>`, `--room <dir>`, `--policy <id>`, `--json`, `--help`
- `scripts/check-graph-derive-health.cjs` - flags `--room <dir>`, `--all`, `--json`, `--strict`, `--check`, `--help`; exported `main` and the exit-mapping function
- `scripts/check-voice-style.cjs` - Stop hook, stdin envelope, no flags

**New library modules and exported functions**
- `lib/hmi/turn-text.cjs` - `readTurnText`, `readTranscriptTurn`, `readTranscriptTail`, `extractAssistantText`, `classifyPrecedingUserContentSource`, `scanContentForAskUserQuestion`, `TRANSCRIPT_TAIL_BYTES`
- `lib/hmi/voice-style-log.cjs` - `appendVoiceStyleRow`, `readVoiceStyleRows`, `evaluatePromotion`, `mindrianHome`, `voiceStyleLogPath`
- `lib/core/doctor/voice-style-log-module.cjs` - `check`, `voiceStyleLogPath`

**New generator symbols in `scripts/build-harness-manifest.cjs`**
- `LARRY_SURFACE_BINDINGS`, `POLICIES_DIR`, `FIXTURE_DIR`, `digestDirectory`, `loadPolicies`, `validatePolicyFile`, `runContractChecks`; `MANIFEST_VERSION` becomes 2

**New manifest JSON keys** in `data/harness-manifest.json`: `policies` with `path`, `digest`, `count`; `larry_surfaces` as an array of `role`, `path`, `digest`; `fixture_ref` with `path`, `digest`

**New policy schema keys** in `data/harness-policies/_schema.json` under `_doc`: `purpose`, `rung_vocabulary`, `rung_meaning`, `kind_vocabulary`, `applies_to_vocabulary`, `policy_fields`, `runner_path_rule`, `promotion_rule_doc`, `gate_count_principle`, `validation_rule`, `default_on_miss`.
**Per-policy file keys**: `id`, `kind`, `runner`, `args`, `rung`, `evidence_log`, `promotion_rule` with `window_runs`, `max_false_positive_rate`, `min_true_positives`; `owner`, `pinned_by`, `applies_to`, `notes`.

**New data paths**
- `data/harness-policies/` with `_schema.json`, `CONTEXT.md`, and 11 policy files: `gate-worktree-hygiene.json`, `gate-shape-declaration.json`, `gate-tool-honesty.json`, `gate-card-fire.json`, `gate-graph-derive-health.json`, `voice-hyphens-only.json`, `voice-glyph-present.json`, `voice-backend-noun-free.json`, `voice-fork-as-card.json`, `memory-write-policy.json`, `contract-parity-larry.json`
- `data/harness-fixtures/converged-room/` (33 committed text files)
- `<room>/.mindrian/harness-run.json` (runtime output, never committed)
- `$MINDRIAN_HOME/voice-style.jsonl` (runtime evidence log, never committed)

**New registry ids**
- doctor module id `voice-style-log` in `data/doctor-modules.json`
- doctor acceptance point id `harness-policies` in `scripts/doctor.cjs` `buildAcceptanceChecklist`
- one `hooks/hooks.json` Stop entry invoking `scripts/check-voice-style.cjs`

**New test files**
- `tests/test-298-policies-schema.cjs`, `tests/test-298-runner-idempotent.cjs`, `tests/test-298-derive-health.cjs`, `tests/test-298-voice-log.cjs`, `tests/test-298-contract-parity.cjs`, `tests/run-all-298.sh`

**New key on the `lib/core/recipe-maps.cjs` `loadManifest()` return**: `policies`

<output>
Create `.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-06-SUMMARY.md` when done, recording the exit map as implemented, the observed fixture JSON, and the shape of the throwaway unhealthy room the red case builds.
</output>
