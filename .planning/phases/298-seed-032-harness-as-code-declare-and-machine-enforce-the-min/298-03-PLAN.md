---
phase: 298
plan: 03
type: execute
wave: 1
depends_on: [298-01]
files_modified:
  - lib/hmi/turn-text.cjs
  - scripts/check-card-fire.cjs
autonomous: true
requirements: [R-08]

must_haves:
  truths:
    - "One transcript reader exists in the repo, not two: lib/hmi/turn-text.cjs owns it and scripts/check-card-fire.cjs calls it"
    - "check-card-fire.cjs behavior is unchanged after the lift: its ten pinned export names are still functions and its own tests still pass"
    - "gateSignature stays in check-card-fire.cjs, so turn-text.cjs never requires lib/core/gate-relevance.cjs"
    - "The 2 MB transcript tail cap (WR-08) travels with the reader, so the Stop hook cannot stall on a pathological transcript"
  artifacts:
    - path: "lib/hmi/turn-text.cjs"
      provides: "The single transcript reader, exported for check-card-fire.cjs and check-voice-style.cjs"
      exports: ["readTurnText", "readTranscriptTurn", "readTranscriptTail", "extractAssistantText", "classifyPrecedingUserContentSource", "scanContentForAskUserQuestion", "TRANSCRIPT_TAIL_BYTES"]
      min_lines: 80
  key_links:
    - from: "scripts/check-card-fire.cjs"
      to: "lib/hmi/turn-text.cjs"
      via: "require of the lifted reader"
      pattern: "require\\(.*turn-text\\.cjs"
---

<objective>
Lift the transcript reader out of `scripts/check-card-fire.cjs` into `lib/hmi/turn-text.cjs` and
repoint card-fire at it, so the rung-2 voice hook (plan 298-07) reads the last assistant turn
through the same code path rather than growing a second copy.

Purpose: R-08 first half. The reader encodes four fixes for real live defects (WR-06 turn-scoped
OR, WR-08 2 MB tail cap, CR-03 user records bound the window only, the tool_result versus
task-notification distinction). A second copy would drift away from all four.

Output: one new library module and one repointed consumer, with card-fire's observable behavior
byte-equivalent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-SPEC.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-PATTERNS.md
@lib/core/doctor/card-fire-health-module.cjs
</context>

<locked_constraints>
- Never `git stash`. Explicit `git add <paths>` only. No em-dashes or en-dashes.
- CJS only, no new dependencies.
- This is a LIFT, not a rewrite. Move the function bodies unchanged; do not improve them.
</locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/hmi/turn-text.cjs with the five clean pieces plus readTurnText</name>
  <files>lib/hmi/turn-text.cjs</files>
  <read_first>
    - `scripts/check-card-fire.cjs` line 250 (the `TRANSCRIPT_TAIL_BYTES` constant), lines 1411-1502 (`readTranscriptTurn`), 1507 onward (`readTranscriptTail`), the `extractAssistantText` body near 1533, `classifyPrecedingUserContentSource` at 1567, `scanContentForAskUserQuestion` at 1595, and the exports block at 1917-1964
    - `scripts/check-card-fire.cjs` line 386 (`gateSignature`) and line 486 (`matchedGlyphSpan`) - read them only to confirm they stay behind
    - `.planning/phases/298-.../298-PATTERNS.md` section `lib/hmi/turn-text.cjs` (the lift table with line numbers and the recommended export boundary)
    - `.planning/phases/298-.../298-RESEARCH.md` section "The transcript reader is a six-function closure, not one function"
    - `lib/hmi/voice-color-mark.cjs` lines 1-40 for the house header style in this directory
  </read_first>
  <action>
    Create `lib/hmi/turn-text.cjs` as a CJS module with a header comment that states: what it is
    (the ONE transcript reader), where it was lifted from (`scripts/check-card-fire.cjs`, naming
    the line ranges), the two consumers it will have (`scripts/check-card-fire.cjs` and
    `scripts/check-voice-style.cjs`), the Canon Part 8 line (local file reads only, zero
    network), and the explicit statement that `gateSignature` deliberately did NOT come along
    because it drags `lib/core/gate-relevance.cjs` and the glyph regexes into a module whose job
    is reading text.

    Move these five pieces verbatim from `check-card-fire.cjs`: the constant
    `TRANSCRIPT_TAIL_BYTES` (2 times 1024 times 1024), `readTranscriptTail(transcriptPath)`,
    `extractAssistantText(content)`, `classifyPrecedingUserContentSource(content)`, and
    `scanContentForAskUserQuestion(content)`. Do not rename them and do not alter their bodies.

    Add one new exported boundary function `readTurnText(transcriptPath)` returning
    `{output_text, preceding_user_text, preceding_user_text_source, assistant_contents}`. Its
    body is `readTranscriptTurn`'s body with the `askuserquestion_fired` and `gate_signature`
    computations removed, and with `assistant_contents` (the raw content array the caller needs
    to recompute `askuserquestion_fired`) added to the return.

    Also export `readTranscriptTurn(transcriptPath)` from this module as a thin wrapper that
    calls `readTurnText` and adds `askuserquestion_fired` via `scanContentForAskUserQuestion`,
    leaving `gate_signature` undefined; card-fire supplies its own.

    The `module.exports` object must list exactly these seven names: `readTurnText`,
    `readTranscriptTurn`, `readTranscriptTail`, `extractAssistantText`,
    `classifyPrecedingUserContentSource`, `scanContentForAskUserQuestion`,
    `TRANSCRIPT_TAIL_BYTES`.

    Every read degrades rather than throws: a missing or unreadable transcript path yields
    `output_text` empty string, `preceding_user_text` empty string,
    `preceding_user_text_source` the string `none`, and `assistant_contents` an empty array,
    matching the source's existing behavior. Per-line JSON parsing stays wrapped in try/catch so
    a partial head line produced by the tail cap is dropped rather than fatal.

    Do not modify `scripts/check-card-fire.cjs` in this task.
  </action>
  <verify>
    <automated>node -e "const m=require('./lib/hmi/turn-text.cjs'); ['readTurnText','readTranscriptTurn','readTranscriptTail','extractAssistantText','classifyPrecedingUserContentSource','scanContentForAskUserQuestion'].forEach(function(n){ if (typeof m[n] !== 'function') throw new Error('missing '+n); }); if (m.TRANSCRIPT_TAIL_BYTES !== 2097152) throw new Error('tail cap wrong'); console.log('turn-text exports ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('./lib/hmi/turn-text.cjs')"` exits 0.
    - All six named functions report `typeof` function and `TRANSCRIPT_TAIL_BYTES` equals 2097152.
    - `grep -c "gate-relevance" lib/hmi/turn-text.cjs` returns 0.
    - `grep -c "gateSignature" lib/hmi/turn-text.cjs` returns 0.
    - Calling `readTurnText` on a nonexistent path returns an empty `output_text` and a `preceding_user_text_source` of `none` without throwing.
    - `grep -v "^ *[*/]" lib/hmi/turn-text.cjs | grep -c "[emdash-or-endash]"` returns 0, where the bracketed class is the literal U+2014 and U+2013 characters.
  </acceptance_criteria>
  <done>lib/hmi/turn-text.cjs exists, exports exactly the seven names, degrades on a missing path, and pulls in no gate-relevance dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint check-card-fire.cjs at the lifted reader without changing its export surface</name>
  <files>scripts/check-card-fire.cjs</files>
  <read_first>
    - `scripts/check-card-fire.cjs` exports block at 1917-1964 (note `readTranscriptTurn` at 1933 and `readTranscriptTail` at 1934)
    - `lib/core/doctor/card-fire-health-module.cjs` line 105 (the four names it asserts are functions: `classifyCardFire`, `gateReachingEntries`, `computeBackstopHit`, `loadRegistry`) and line 79 (it reads `ts` off each intercept-log row for freshness)
    - `lib/mcp/stop-gate-handler.cjs` - grep its requires of `check-card-fire.cjs` to enumerate the six retry-store accessors it drives
    - `.planning/phases/298-.../298-RESEARCH.md` Pitfall 8, "breaking check-card-fire.cjs's pinned export surface"
    - the card-fire tests, found with `grep -rln "check-card-fire" tests/`
  </read_first>
  <action>
    In `scripts/check-card-fire.cjs`, delete the five lifted bodies and the
    `TRANSCRIPT_TAIL_BYTES` constant, and add one require near the existing requires:
    `const turnText = require('../lib/hmi/turn-text.cjs');`.

    Rewrite the local `readTranscriptTurn(transcriptPath)` so it calls
    `turnText.readTurnText(transcriptPath)` and then computes the two card-fire-domain fields
    locally: `askuserquestion_fired` from `turnText.scanContentForAskUserQuestion` over the
    returned `assistant_contents`, and `gate_signature` from the existing local `gateSignature`
    applied to `output_text`. Its return shape must remain exactly `output_text`,
    `askuserquestion_fired`, `gate_signature`, `preceding_user_text`,
    `preceding_user_text_source`.

    Keep `readTranscriptTail` available under its existing exported name by re-exporting
    `turnText.readTranscriptTail`. Grep the file for remaining references to
    `TRANSCRIPT_TAIL_BYTES`; alias it from `turnText` if it is still referenced, and drop the
    constant entirely if it is not.

    Do NOT rename, remove or reorder any other export. The four names `classifyCardFire`,
    `gateReachingEntries`, `computeBackstopHit`, `loadRegistry` and the six retry-store accessors
    `lib/mcp/stop-gate-handler.cjs` drives are frozen. `gateSignature` and `matchedGlyphSpan`
    stay defined in this file.

    Do not change `appendInterceptLog`, `readInterceptLogLines`, `RETRY_TTL_MS` or the intercept
    log record shape. The health module parses that JSONL and reads `ts` for freshness; a shape
    change would start counting malformed lines on every developer machine.

    Commit with an explicit `git add lib/hmi/turn-text.cjs scripts/check-card-fire.cjs`.
  </action>
  <verify>
    <automated>node -e "const m=require('./scripts/check-card-fire.cjs'); ['classifyCardFire','gateReachingEntries','computeBackstopHit','loadRegistry','readTranscriptTurn','readTranscriptTail'].forEach(function(n){ if (typeof m[n] !== 'function') throw new Error('pinned export lost: '+n); }); console.log('card-fire export surface intact');"</automated>
  </verify>
  <acceptance_criteria>
    - The export-surface probe above exits 0 and prints `card-fire export surface intact`.
    - Every retry-store accessor named in `lib/mcp/stop-gate-handler.cjs` still resolves to a function on the `check-card-fire.cjs` exports object.
    - `node scripts/check-hook-schema-compatibility.cjs` exits 0.
    - Every test file found by `grep -rln "check-card-fire" tests/` exits 0 when run, or matches its recorded baseline exit code from `298-TEST-BASELINE.md`.
    - `grep -c "require(.*turn-text" scripts/check-card-fire.cjs` returns 1.
    - `grep -c "function readTranscriptTail" scripts/check-card-fire.cjs` returns 0 (the body moved out).
    - `grep -c "function gateSignature" scripts/check-card-fire.cjs` returns 1 (the body stayed).
    - `git status --porcelain` is empty after the commit.
  </acceptance_criteria>
  <done>card-fire calls the lifted reader, its ten pinned export names are intact, and the hook schema gate is still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code transcript file -> hook process | `transcript_path` is attacker-influenced content in the sense that a model turn can contain anything; the reader parses it |
| repo modules -> Stop hook runtime | A throw inside the reader would break every turn for a real user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-298-05 | Denial of service | `lib/hmi/turn-text.cjs` | mitigate | `TRANSCRIPT_TAIL_BYTES` of 2 MB (WR-08) travels with the lift and caps the read; per-line JSON parse stays in try/catch so a partial head line is dropped, not fatal |
| T-298-07 | Denial of service | `scripts/check-card-fire.cjs` | mitigate | Every read degrades to the documented empty state and never throws, preserving the never-block-the-hook contract |
| T-298-02 | Information disclosure | turn text in memory | mitigate | The module reads and returns text; it never writes a log and never makes a network call. Canon Part 8 line stated in the header |
| T-298-08 | Tampering | card-fire's pinned export surface | mitigate | Ten frozen export names are asserted by an automated probe in the acceptance criteria (Pitfall 8) |
| T-298-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase; Node built-ins only |
</threat_model>

<verification>
- The export probe prints `card-fire export surface intact`.
- `node scripts/check-hook-schema-compatibility.cjs` exits 0.
- Card-fire's own tests match their recorded baseline exit codes.
- `bash tests/run-all-298.sh` still exits 0.
</verification>

<success_criteria>
- One transcript reader in the repo, owned by `lib/hmi/turn-text.cjs`, consumed by card-fire.
- Card-fire's observable behavior and export surface unchanged.
- `gateSignature` stayed behind; `turn-text.cjs` has no `gate-relevance.cjs` dependency.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 298 across all plans. The plan-review-convergence source-grounding
pass reads this section to exclude newly created symbols from drift verification.

**New CLI scripts**
- `scripts/run-harness.cjs` - flags `--check`, `--tier <t>`, `--room <dir>`, `--policy <id>`, `--json`, `--help`
- `scripts/check-graph-derive-health.cjs` - flags `--room <dir>`, `--all`, `--json`, `--strict`, `--help`
- `scripts/check-voice-style.cjs` - Stop hook, stdin envelope, no flags

**New library modules and exported functions**
- `lib/hmi/turn-text.cjs` - `readTurnText`, `readTranscriptTurn`, `readTranscriptTail`, `extractAssistantText`, `classifyPrecedingUserContentSource`, `scanContentForAskUserQuestion`, `TRANSCRIPT_TAIL_BYTES`
- `lib/hmi/voice-style-log.cjs` - `appendVoiceStyleRow`, `readVoiceStyleRows`, `evaluatePromotion`, `mindrianHome`, `voiceStyleLogPath`
- `lib/core/doctor/voice-style-log-module.cjs` - `check`, `voiceStyleLogPath`

**New generator symbols in `scripts/build-harness-manifest.cjs`**
- `LARRY_SURFACE_BINDINGS`, `POLICIES_DIR`, `FIXTURE_DIR`, `digestDirectory`, `loadPolicies`, `validatePolicyFile`, `runContractChecks`; `MANIFEST_VERSION` becomes 2

**New manifest JSON keys** in `data/harness-manifest.json`: `policies` with `path`, `digest`, `count`; `larry_surfaces` as an array of `role`, `path`, `digest`; `fixture_ref` with `path`, `digest`

**New policy schema keys** in `data/harness-policies/_schema.json` under `_doc`: `purpose`, `rung_vocabulary`, `rung_meaning`, `kind_vocabulary`, `applies_to_vocabulary`, `policy_fields`, `runner_path_rule`, `promotion_rule_doc`, `gate_count_principle`, `validation_rule`, `default_on_miss`.
**Per-policy file keys**: `id`, `kind`, `runner`, `args`, `rung`, `evidence_log`, `promotion_rule` with `window_runs`, `max_false_positive_rate`, `min_true_positives`; `owner`, `pinned_by`, `applies_to`, `notes`.

**New data paths**
- `data/harness-policies/` with `_schema.json`, `CONTEXT.md`, and 11 policy files: `gate-worktree-hygiene.json`, `gate-shape-declaration.json`, `gate-tool-honesty.json`, `gate-card-fire.json`, `gate-graph-derive-health.json`, `voice-hyphens-only.json`, `voice-glyph-present.json`, `voice-backend-noun-free.json`, `voice-fork-as-card.json`, `memory-write-policy.json`, `contract-parity-larry.json`
- `data/harness-fixtures/converged-room/` (33 committed text files)
- `<room>/.mindrian/harness-run.json` (runtime output, never committed)
- `$MINDRIAN_HOME/voice-style.jsonl` (runtime evidence log, never committed)

**New registry ids**
- doctor module id `voice-style-log` in `data/doctor-modules.json`
- doctor acceptance point id `harness-policies` in `scripts/doctor.cjs` `buildAcceptanceChecklist`
- one `hooks/hooks.json` Stop entry invoking `scripts/check-voice-style.cjs`

**New test files**
- `tests/test-298-policies-schema.cjs`, `tests/test-298-runner-idempotent.cjs`, `tests/test-298-derive-health.cjs`, `tests/test-298-voice-log.cjs`, `tests/test-298-contract-parity.cjs`, `tests/run-all-298.sh`

**New key on the `lib/core/recipe-maps.cjs` `loadManifest()` return**: `policies`

<output>
Create `.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-03-SUMMARY.md` when done, recording the exact line ranges lifted, the final export list of both files, and the observed exit codes of every card-fire test.
</output>
