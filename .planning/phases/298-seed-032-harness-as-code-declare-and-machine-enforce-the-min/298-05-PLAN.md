---
phase: 298
plan: 05
type: execute
wave: 1
depends_on: [298-01]
files_modified:
  - skills/larry-personality/SKILL.md
  - agents/larry-extended.md
autonomous: true
requirements: [R-07]

must_haves:
  truths:
    - "The persona states the D3 operating policy: Larry reads context and intent and then engages the right component (ICM Layer 1 Routing), silent for reads and context, a gate for any write that becomes a truth claim"
    - "The persona states the D5 memory channels: Belief is the room graph, Progress is STATE.md, Experience is seeds plus memory_event; track is context_assemble, commit is graph_write and claim_write, recall is graph_query and graph_reason, note is memory_event"
    - "The persona states the D4 write policy: memory_event fires silently on every substantive turn, claims land only as proposed after an F.8 governance basket that fires at two or more candidates, toggled-off candidates write NOT_REMEMBERED_BECAUSE, context_assemble runs at turn start, and none of it is ever narrated (Canon Part 12)"
    - "The Desktop wire lib/mcp/runtime-instructions.cjs is NOT modified in this phase; the decision and its measured byte reason are recorded"
    - "All nine frozen-phrase tests and lib/mcp/no-instructions.test.cjs hold their recorded baseline results"
  artifacts:
    - path: "skills/larry-personality/SKILL.md"
      provides: "The full D3/D4/D5 operating contract in prose, the surface the manifest points at"
      contains: "NOT_REMEMBERED_BECAUSE"
    - path: "agents/larry-extended.md"
      provides: "Short operating statements plus the pointer to the skill for the contract"
      contains: "larry-personality"
  key_links:
    - from: "agents/larry-extended.md"
      to: "skills/larry-personality/SKILL.md"
      via: "the established defer-to-the-skill pointer for contracts"
      pattern: "larry-personality"
    - from: "skills/larry-personality/SKILL.md"
      to: "data/harness-policies/memory-write-policy.json"
      via: "the policy declares what the skill states; the phrases the policy names must exist here"
      pattern: "memory_event"
---

<objective>
Land the D3, D4 and D5 operating policy as persona prose, in the two surfaces that have byte
headroom, and record the measured decision not to touch the Desktop wire.

Purpose: R-07. Requirement 7 says the persona STATES the operating policy and
`memory-write-policy.json` DECLARES it. Plan 298-09 authors the declaration; this plan authors
the statement, and the declaration's test asserts the skill carries the phrases the policy names.

Output: the operating contract in `skills/larry-personality/SKILL.md`, short statements plus a
pointer in `agents/larry-extended.md`, and a recorded byte measurement of the untouched Desktop
wire.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-SPEC.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-CONTEXT.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-PATTERNS.md
@docs/superpowers/specs/2026-09-07-harness-manifest-v2-design.md
</context>

<locked_constraints>
- Never `git stash`. Explicit `git add <paths>` only. No em-dashes or en-dashes.
- `lib/mcp/runtime-instructions.cjs` is NOT in this plan's `files_modified` and must not be edited. Its evaluated constant measures 1,944 of a 1,950-byte budget, so a new clause there would need a measured tightening first, and the BOUNDARIES paragraph is byte-frozen and must remain the last thing in the string.
- Do not rewrite Larry's voice. Prose stays prose; the manifest holds invariants, not sentences.
- Canon Part 12: nothing added here may make Larry narrate a memory operation.
- Do not touch the hardcoded "8 insight sensors" line in `agents/larry-extended.md`. It contradicts the skill's own never-hardcode-a-count rule and is a recorded residual, not 298 scope.
- Do not add `lib/mcp/larry-server-instructions.md` to anything. It is a de-facto fourth Larry surface loaded by nothing at runtime; declaring it would legitimize an orphan.
</locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: State the D3/D4/D5 operating contract in the larry-personality skill</name>
  <files>skills/larry-personality/SKILL.md</files>
  <read_first>
    - `skills/larry-personality/SKILL.md` - its section structure, the Elevation section (which `tests/test-205-elevation-doctrine-floor.cjs` requires to be LONGER than the agent's), and lines 177 and 183 (the never-hardcode-a-count rule this file states about SENSOR_REGISTRY)
    - `docs/superpowers/specs/2026-09-07-harness-manifest-v2-design.md` section 10 "Larry as first consumer", the four sub-points (surfaces, operating by intent, memory channels, write policy) and the "Where prose lands" paragraph
    - `.planning/seeds/SEED-040-hitl-memory-governance.md` for the basket, WHAT/HOW/WHO and `NOT_REMEMBERED_BECAUSE` vocabulary
    - `.planning/seeds/SEED-085-evoharness-rl-cost-aware-learned-harness-access.md` for the Belief, Progress and Experience channel names (RL and the four meta-actions are out of scope)
    - `.planning/phases/298-.../298-RESEARCH.md` section "Where the D3/D4/D5 prose lands (byte economics)"
    - `.planning/phases/298-.../298-CONTEXT.md` D1-D9 carried-forward block (D3, D4, D5 are named there)
  </read_first>
  <action>
    Add one new top-level section to `skills/larry-personality/SKILL.md` titled
    `## Operating the components (ICM Layer 1 Routing)` covering D3, and a second titled
    `## Memory channels and the write policy` covering D5 and D4. Place them where they read
    naturally against the existing section order; do not reorder or shorten any existing section,
    and do not shorten the Elevation section (a test floors its length against the agent's).

    The D3 section must state, in Larry's own register and without narrating anything to a user:
    read the context and the intent first, then engage the right component - the remote graph for
    methodology, the local graph for memory and context, the room layers for filing, the sensors
    and the dial for navigation. Silent for reads and for context assembly. A gate for any write
    that becomes a truth claim.

    The D5 section must state the three channels and the four verbs as an explicit mapping:
    Belief is the room graph reached through `navigation.cjs`; Progress is `STATE.md`; Experience
    is `.planning/seeds/` plus `memory_event`. Track is `context_assemble`; commit is
    `graph_write` and `claim_write`; recall is `graph_query` and `graph_reason`; note is
    `memory_event`.

    The D4 section must state, as a numbered contract: `context_assemble` runs at turn start;
    `memory_event` fires silently on every substantive turn; `claim_write` and `graph_write` land
    a node only at `review_status` proposed; a claim is proposed only after an F.8 governance
    basket, and the basket fires when two or more candidate writes exist; a candidate the human
    toggles off writes a `NOT_REMEMBERED_BECAUSE` record carrying the reason; none of this is
    ever narrated (Canon Part 12), and after a basket confirm Larry says nothing about the write
    and the next line is a next-move offer.

    Use the exact identifier strings `context_assemble`, `memory_event`, `graph_write`,
    `claim_write`, `graph_query`, `graph_reason`, `NOT_REMEMBERED_BECAUSE` and the literal word
    `proposed`, because plan 298-09's `memory-write-policy.json` test asserts the skill carries
    the phrases the policy names. Do not paraphrase them.

    Do not add a count of anything that is enumerated from disk (sensors, surfaces, tools). State
    where the live count is read from instead, matching this file's own rule at lines 177 and 183.

    Commit with an explicit `git add skills/larry-personality/SKILL.md`.
  </action>
  <verify>
    <automated>for s in context_assemble memory_event graph_write claim_write graph_query graph_reason NOT_REMEMBERED_BECAUSE proposed; do c=$(grep -c "$s" skills/larry-personality/SKILL.md); echo "$s=$c"; [ "$c" -ge 1 ] || exit 1; done; node tests/test-205-elevation-doctrine-floor.cjs; echo "elevation-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the eight identifier strings above appears at least once in `skills/larry-personality/SKILL.md`.
    - `grep -c "Operating the components" skills/larry-personality/SKILL.md` returns 1 and `grep -c "Memory channels" skills/larry-personality/SKILL.md` returns 1.
    - `grep -c "two or more" skills/larry-personality/SKILL.md` returns at least 1 (the basket threshold is stated in prose, not only as a number).
    - `node tests/test-205-elevation-doctrine-floor.cjs` matches its recorded exit code in `298-TEST-BASELINE.md` (it is expected red at HEAD for an unrelated canon-version reason; it must not get worse).
    - `grep -cP '\x{2014}|\x{2013}' skills/larry-personality/SKILL.md` returns 0.
    - No new hardcoded count of sensors, surfaces or tools is introduced: `grep -nE "[0-9]+ (insight sensors|surfaces|tools)" skills/larry-personality/SKILL.md` returns no line that did not exist before this task.
  </acceptance_criteria>
  <done>The skill carries the full D3, D5 and D4 operating contract using the exact identifier strings the policy file will name.</done>
</task>

<task type="auto">
  <name>Task 2: Add the short operating statements to the agent body and record the Desktop-wire decision</name>
  <files>agents/larry-extended.md</files>
  <read_first>
    - `agents/larry-extended.md` in full, especially its frontmatter (`initialPrompt`, the 10 `persona_variants` keys, the `skills:` list), the `## Operating the machinery` section, the `## Decision Gates` section, `## Elevation (Part 12`, and `## If asked about Theo by name`
    - `.planning/phases/298-.../298-RESEARCH.md` section "The core-contract phrase set, per surface, with the test that already pins each one" (Surface 1 table). Every phrase in that table is pinned by a named test and must survive this edit verbatim
    - `lib/mcp/no-instructions.test.cjs` lines 85-100 and 235-270 (the `SERVED_BUDGET_BYTES` constant, the `PART8_BOUNDARIES_FROZEN` copy and the two assertions that pin it: contains, and ends-with)
    - `.planning/phases/298-.../298-RESEARCH.md` Open Question 3 (does anything new belong on the Desktop wire at all)
  </read_first>
  <action>
    Add ONE short section to `agents/larry-extended.md` titled
    `## Operating policy (routing, channels, writes)`. It carries three or four sentences only:
    the D3 one-liner (read context and intent, engage the right component, silent for reads, a
    gate for a write that becomes a truth claim), the D5 channel names in one sentence (Belief is
    the room graph, Progress is STATE.md, Experience is seeds plus `memory_event`), the D4
    one-liner (`memory_event` silently every substantive turn, claims only as `proposed` after an
    F.8 basket at two or more candidates, `NOT_REMEMBERED_BECAUSE` on a toggle-off,
    `context_assemble` at turn start, never narrated), and then the pointer sentence: the full
    contract lives in the `larry-personality` skill. That defer-to-the-skill pattern is this
    file's established convention; do not restate the contract here.

    Do NOT touch any phrase the research's Surface 1 table lists. In particular leave intact:
    `Operating the machinery`, `ZERO user-content egress`, the absence of
    `What are you working on`, `chain-executor` and `runChain`, the sourced-or-absent and hedge
    word lines, the `## Decision Gates` heading with `AskUserQuestion` and the no-card-no-picture
    clause, the `## Elevation (Part 12` heading with vertical, horizontal and lateral, the
    `larry-personality skill` mention, the frontmatter `initialPrompt`, the 10 `persona_variants`
    keys and the four-entry `skills:` list, and `## If asked about Theo by name`. Add nothing
    matching the Part 8 leak pattern (`mcp__brain_`, `fetch(`, `writeBrain`, `sendToBrain`,
    `ingestToBrain`).

    Then measure the Desktop wire WITHOUT editing it: run
    `node -e "console.log(Buffer.byteLength(require('./lib/mcp/runtime-instructions.cjs').RUNTIME_INSTRUCTIONS,'utf8'))"`
    and record the number. Record the decision in the plan SUMMARY in one paragraph: R-07 is
    satisfied by the agent body plus the skill; the Desktop wire is not touched in Phase 298
    because the measured headroom is the budget minus the reading (6 bytes at research time) and
    the design's own estimate for a comparable clause is 120 to 180 bytes, and because the
    BOUNDARIES paragraph is byte-frozen and must remain the last thing in the string. Note that a
    future clause there is fundable only by a measured tightening of the RUNTIME LOOP prose, as
    its own task with a before and after byte reading.

    Commit with an explicit `git add agents/larry-extended.md`.
  </action>
  <verify>
    <automated>node tests/test-143.2-doctrine-presence.cjs && node tests/test-larry-handoff-seam.cjs && node tests/test-canon-entry-38-sourced-claims-floor.cjs && node tests/test-gate-native-fire-w1.cjs && node tests/test-chain-executor-part8-leak.cjs && bash tests/test-115-persona-variants.sh && bash tests/test-114-substrate-preload.sh && node lib/mcp/no-instructions.test.cjs && node -e "console.log('served bytes', Buffer.byteLength(require('./lib/mcp/runtime-instructions.cjs').RUNTIME_INSTRUCTIONS,'utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - All seven frozen-phrase commands in the verify block exit 0.
    - `node lib/mcp/no-instructions.test.cjs` reports 9 of 9 passing.
    - The served byte reading is unchanged from the value recorded in `298-TEST-BASELINE.md`, and is less than or equal to 1950.
    - `git diff --stat HEAD -- lib/mcp/runtime-instructions.cjs` reports zero changed files.
    - `grep -c "Operating policy (routing, channels, writes)" agents/larry-extended.md` returns 1.
    - `grep -c "larry-personality" agents/larry-extended.md` returns at least 1.
    - `grep -cE "mcp__brain_[a-z]+\(|fetch\s*\(|writeBrain|sendToBrain|ingestToBrain" agents/larry-extended.md` returns 0.
    - `grep -cP '\x{2014}|\x{2013}' agents/larry-extended.md` returns 0.
    - The plan SUMMARY contains the recorded Desktop-wire decision paragraph with the measured byte number.
  </acceptance_criteria>
  <done>The agent body carries the short operating statements and the skill pointer, every frozen-phrase test holds, and the untouched Desktop wire's byte reading is recorded with the reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persona prose -> Brain | Canon Part 8: nothing in the persona may instruct or enable user-content egress |
| persona prose -> served Desktop wire | A byte-budgeted constant with a frozen trailing paragraph; overflow silently truncates the served contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-298-02 | Information disclosure | `agents/larry-extended.md` | mitigate | `tests/test-chain-executor-part8-leak.cjs` pins the absence of the five egress patterns; the acceptance criteria re-grep for them directly |
| T-298-12 | Denial of service | `lib/mcp/runtime-instructions.cjs` | mitigate | The file is out of this plan's `files_modified` and the acceptance criteria assert a zero-file diff plus an unchanged byte reading, so the 6-byte headroom cannot be consumed by accident |
| T-298-13 | Tampering | the nine frozen-phrase tests | mitigate | All seven runnable frozen-phrase commands are in the verify block and must exit 0; the two known-red persona tests are compared against the recorded baseline rather than assumed green |
| T-298-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase; Node built-ins only |
</threat_model>

<verification>
- The seven frozen-phrase commands exit 0; `no-instructions.test.cjs` is 9 of 9.
- The Desktop wire byte reading is unchanged and the file has a zero-file diff.
- `test-205-elevation-doctrine-floor.cjs` and `test-115-surfaces-grep.sh` match their recorded baseline exit codes.
- `grep -cP` for U+2014 and U+2013 returns 0 on both edited files.
</verification>

<success_criteria>
- R-07's persona half is delivered: the skill states the full D3/D4/D5 contract with the exact identifier strings, and the agent carries the short statements plus the pointer.
- The Desktop wire is untouched and the decision is recorded with its measured byte reason.
- No frozen phrase is lost and no new hardcoded count is introduced.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 298 across all plans. The plan-review-convergence source-grounding
pass reads this section to exclude newly created symbols from drift verification.

**New CLI scripts**
- `scripts/run-harness.cjs` - flags `--check`, `--tier <t>`, `--room <dir>`, `--policy <id>`, `--json`, `--help`
- `scripts/check-graph-derive-health.cjs` - flags `--room <dir>`, `--all`, `--json`, `--strict`, `--help`
- `scripts/check-voice-style.cjs` - Stop hook, stdin envelope, no flags

**New library modules and exported functions**
- `lib/hmi/turn-text.cjs` - `readTurnText`, `readTranscriptTurn`, `readTranscriptTail`, `extractAssistantText`, `classifyPrecedingUserContentSource`, `scanContentForAskUserQuestion`, `TRANSCRIPT_TAIL_BYTES`
- `lib/hmi/voice-style-log.cjs` - `appendVoiceStyleRow`, `readVoiceStyleRows`, `evaluatePromotion`, `mindrianHome`, `voiceStyleLogPath`
- `lib/core/doctor/voice-style-log-module.cjs` - `check`, `voiceStyleLogPath`

**New persona sections created by this plan**
- `skills/larry-personality/SKILL.md` headings `## Operating the components (ICM Layer 1 Routing)` and `## Memory channels and the write policy`
- `agents/larry-extended.md` heading `## Operating policy (routing, channels, writes)`

**New generator symbols in `scripts/build-harness-manifest.cjs`**
- `LARRY_SURFACE_BINDINGS`, `POLICIES_DIR`, `FIXTURE_DIR`, `digestDirectory`, `loadPolicies`, `validatePolicyFile`, `runContractChecks`; `MANIFEST_VERSION` becomes 2

**New manifest JSON keys** in `data/harness-manifest.json`: `policies` with `path`, `digest`, `count`; `larry_surfaces` as an array of `role`, `path`, `digest`; `fixture_ref` with `path`, `digest`

**New policy schema keys** in `data/harness-policies/_schema.json` under `_doc`: `purpose`, `rung_vocabulary`, `rung_meaning`, `kind_vocabulary`, `applies_to_vocabulary`, `policy_fields`, `runner_path_rule`, `promotion_rule_doc`, `gate_count_principle`, `validation_rule`, `default_on_miss`.
**Per-policy file keys**: `id`, `kind`, `runner`, `args`, `rung`, `evidence_log`, `promotion_rule` with `window_runs`, `max_false_positive_rate`, `min_true_positives`; `owner`, `pinned_by`, `applies_to`, `notes`.

**New data paths**
- `data/harness-policies/` with `_schema.json`, `CONTEXT.md`, and 11 policy files: `gate-worktree-hygiene.json`, `gate-shape-declaration.json`, `gate-tool-honesty.json`, `gate-card-fire.json`, `gate-graph-derive-health.json`, `voice-hyphens-only.json`, `voice-glyph-present.json`, `voice-backend-noun-free.json`, `voice-fork-as-card.json`, `memory-write-policy.json`, `contract-parity-larry.json`
- `data/harness-fixtures/converged-room/` (33 committed text files)
- `<room>/.mindrian/harness-run.json` (runtime output, never committed)
- `$MINDRIAN_HOME/voice-style.jsonl` (runtime evidence log, never committed)

**New registry ids**
- doctor module id `voice-style-log` in `data/doctor-modules.json`
- doctor acceptance point id `harness-policies` in `scripts/doctor.cjs` `buildAcceptanceChecklist`
- one `hooks/hooks.json` Stop entry invoking `scripts/check-voice-style.cjs`

**New test files**
- `tests/test-298-policies-schema.cjs`, `tests/test-298-runner-idempotent.cjs`, `tests/test-298-derive-health.cjs`, `tests/test-298-voice-log.cjs`, `tests/test-298-contract-parity.cjs`, `tests/run-all-298.sh`

**New key on the `lib/core/recipe-maps.cjs` `loadManifest()` return**: `policies`

<output>
Create `.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-05-SUMMARY.md` when done, including the measured Desktop-wire byte reading, the recorded decision paragraph, and the exact list of identifier strings the skill now carries so plan 298-09 can assert against it.
</output>
