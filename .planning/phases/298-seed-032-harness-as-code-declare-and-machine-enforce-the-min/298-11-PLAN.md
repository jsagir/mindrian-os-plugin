---
phase: 298
plan: 11
type: execute
wave: 5
depends_on: [298-02, 298-06, 298-10]
files_modified:
  - scripts/run-harness.cjs
  - tests/test-298-runner-idempotent.cjs
autonomous: true
requirements: [R-04, R-05]

must_haves:
  truths:
    - "R-04: node scripts/run-harness.cjs --room <dir> decides convergence from Layer 0 alone: a ROOM.md in every section, STATE.md total_entries matching the on-disk entry count, an empty derive queue, and zero proposed truth-claim nodes"
    - "R-04: a run against the committed converged-room fixture exits 0, reports converged true, writes nothing, and leaves git status --porcelain empty; a second run produces a byte-identical report"
    - "D-03a: the runner reads the room database only through navigation.openRoomDbReadOnlyForCaller and never through openGraph, so a fixture with no .mindrian gains none"
    - "D-03: the runner ignores venture_stage on the fixture, because a scaffold-born room writes Pre-Opportunity and compute-state re-derives Investment from directory presence alone"
    - "R-05: the runner refuses converged true while gate-graph-derive-health is a ghost or the derive queue is non-empty"
  artifacts:
    - path: "scripts/run-harness.cjs"
      provides: "The --room convergence branch, the entry counter, and the single report write"
      exports: ["countEntries", "scanRoom"]
  key_links:
    - from: "scripts/run-harness.cjs"
      to: "lib/core/navigation.cjs openRoomDbReadOnlyForCaller"
      via: "the only database opener the runner may use"
      pattern: "openRoomDbReadOnlyForCaller"
    - from: "scripts/run-harness.cjs"
      to: "scripts/gsd-graph-derive-sweep.cjs readQueue"
      via: "reuse of the missing-is-empty derive-queue contract"
      pattern: "readQueue"
---

<objective>
Add the `--room <dir>` convergence branch to `scripts/run-harness.cjs` and prove idempotence
against the committed fixture.

Purpose: R-04's headline truth. One idempotent runner can say, from files alone, whether a room
is converged, and a second run on the fixture is a no-op with an empty `git status --porcelain`.
Convergence is a scan, not a memory.

Output: the convergence branch, the single conditional report write, and a green
`tests/test-298-runner-idempotent.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-SPEC.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-CONTEXT.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-PATTERNS.md
@docs/superpowers/specs/2026-09-07-harness-manifest-v2-design.md
</context>

<locked_constraints>
- Never `git stash`. Explicit `git add <paths>` only. No em-dashes or en-dashes.
- The ONLY write the runner ever makes in this branch is the room report at `<room>/.mindrian/harness-run.json`. Nothing else, anywhere, ever.
- D-03a is absolute: `navigation.openRoomDbReadOnlyForCaller(roomDir)` is the only database opener. Requiring `lib/core/lazygraph-ops.cjs` or calling `openGraph` is forbidden, because `openGraph` runs a recursive `fs.mkdirSync` of the `.mindrian` directory on first touch and would create `.mindrian/` and `room.db` inside the committed fixture on run one.
- The runner never repairs a room. Fixing is a human-gated action elsewhere (SEED-037 4c).
- `computed:` is an ISO TIMESTAMP, not a count. Never compare it. The comparable key is `total_entries`.
- The report carries no timestamp and no clock-derived field, so two consecutive runs serialize byte-identically.
- The fixture has no `.mindrian/` on purpose, so a converged fixture run must write NOTHING AT ALL. The report is written only when a `.mindrian` directory already exists in the room; the runner never creates it.
</locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Layer 0 convergence scan and the conditional report write</name>
  <files>scripts/run-harness.cjs</files>
  <read_first>
    - `scripts/compute-state` line 96 (the per-section counter: find at maxdepth 1, name `*.md`, excluding `ROOM.md`), line 91 (hidden directories are skipped), and lines 250-265 (the frontmatter emitter: `computed:` at 257 is an ISO timestamp, `venture_stage` at 258, `total_entries` at 259)
    - `scripts/gsd-graph-derive-sweep.cjs` lines 40-50 and 95-110 (`QUEUE_RELATIVE` and `readQueue`, which returns an empty entries array on ANY failure including a missing file) plus its exports around line 191
    - `lib/core/navigation.cjs` line 486 (the re-export of `openRoomDbReadOnlyForCaller`) and `lib/core/navigation/spine-events.cjs` lines 520-548 (the null-on-absent contract and the read-only URI form)
    - `lib/core/navigation/governance.cjs` line 44 (`findGovernanceCandidates` returns an empty array when the handle is falsy, so passing a null handle straight through is safe)
    - `lib/core/room-skeleton-scaffold.cjs` lines 60-100 (`SECTION_NAMES` and `IDENTITY_DIRECTORIES`, for reference only; the runner enumerates directories from disk so a room that grew a section outside the frozen eleven is still checked)
    - `lib/core/lazygraph-ops.cjs` around line 426 (confirm the recursive mkdir on first touch; this one read discharges the research's assumption A1)
    - `.planning/phases/298-.../298-RESEARCH.md` "Layer-0 convergence reads" in full, especially Corrections 1 to 3, plus Pitfalls 1, 2 and 10
  </read_first>
  <action>
    Add `countEntries(roomDir)` to `scripts/run-harness.cjs`, reproducing the `compute-state`
    counting rule exactly: enumerate the room's top-level directories, skip any whose name starts
    with a dot, and for each count files matching `*.md` at depth 1 only, excluding `ROOM.md`.
    Sum across sections. A scaffold-born room returns 13. Do not recurse, do not include
    `ROOM.md`, do not include hidden directories, and do not count files at the room root.

    Add `scanRoom(roomDir, policies)` returning a report object with these named checks:
    - `room_md_present`: every non-hidden top-level directory holds a `ROOM.md`. Enumerate from
      disk; do not import the frozen section list.
    - `entry_count_matches`: the `total_entries` value parsed from the room's `STATE.md`
      frontmatter equals `countEntries(roomDir)`. Read `computed:` only to report it as a
      freshness stamp and never compare it. Ignore `venture_stage` entirely and say why in a
      source comment: a scaffold writes Pre-Opportunity while compute-state re-derives Investment
      from directory presence alone, so the two structurally disagree on any full scaffold.
    - `derive_queue_empty`: call `readQueue` from `scripts/gsd-graph-derive-sweep.cjs` rather than
      reading the file, so the missing-is-empty contract is inherited rather than re-implemented.
    - `zero_proposed_claims`: open the database with
      `require('../lib/core/navigation.cjs').openRoomDbReadOnlyForCaller(roomDir)` inside a
      try/catch that yields null, pass the handle straight to `findGovernanceCandidates` (a null
      handle returns an empty array), count the result, and close the handle with
      `closeRoomDbForCaller` in a `finally` when it is non-null.
    - `derive_health_declared`: the `gate-graph-derive-health` policy exists in the loaded set and
      is not a ghost.

    `converged` is true only when every check passed AND `derive_health_declared` is true AND the
    derive queue is empty. A ghost derive-health gate is the missing-information error class:
    refuse `converged: true`, report the ghost by name, and never retry.

    Print the report; with `--json`, print only the report. Write
    `<room>/.mindrian/harness-run.json` ONLY when a `.mindrian` directory already exists in the
    room, and never create that directory. State that rule in a source comment naming the fixture:
    the committed fixture deliberately has no `.mindrian/`, so a converged fixture run writes
    nothing at all, which is what makes `git status --porcelain` empty.

    Exit 0 when converged, 1 when not converged, 2 on a scanner fault. The report object contains
    no timestamp, no duration and no clock-derived value, and its keys are emitted in a
    deterministic order.

    Remove the not-yet-implemented notice the `--room` flag printed after plan 298-10, and export
    `countEntries` and `scanRoom` alongside the existing exports.
  </action>
  <verify>
    <automated>node scripts/run-harness.cjs --room data/harness-fixtures/converged-room --json > /tmp/hr1.json; echo "run1-exit=$?"; node scripts/run-harness.cjs --room data/harness-fixtures/converged-room --json > /tmp/hr2.json; echo "run2-exit=$?"; cmp -s /tmp/hr1.json /tmp/hr2.json && echo reports-identical; test ! -d data/harness-fixtures/converged-room/.mindrian && echo no-mindrian-created; git status --porcelain data/harness-fixtures/converged-room</automated>
  </verify>
  <acceptance_criteria>
    - Both fixture runs exit 0 and their JSON reports carry `converged` true.
    - The two report files are byte-identical (`cmp -s` succeeds), proving no timestamp leaked into the report.
    - `data/harness-fixtures/converged-room/.mindrian` does not exist after either run.
    - `git status --porcelain data/harness-fixtures/converged-room` is empty after both runs.
    - `node -e` calling the exported `countEntries` against the fixture returns 13.
    - `grep -c "openGraph\|openRoomDbForCaller\|room-db.cjs\|lazygraph-ops" scripts/run-harness.cjs` returns 0.
    - `grep -c "openRoomDbReadOnlyForCaller" scripts/run-harness.cjs` returns at least 1 and `grep -c "closeRoomDbForCaller" scripts/run-harness.cjs` returns at least 1.
    - `grep -c "readQueue" scripts/run-harness.cjs` returns at least 1 (the derive queue is read through the shipped contract, not re-implemented).
    - `grep -c "venture_stage" scripts/run-harness.cjs` returns at most 1, and if present it appears only inside the comment explaining why it is ignored.
    - `grep -cE "computed[^a-z]*==|computed[^a-z]*===" scripts/run-harness.cjs` returns 0 (the timestamp is never compared).
    - `grep -cP '\x{2014}|\x{2013}' scripts/run-harness.cjs` returns 0.
  </acceptance_criteria>
  <done>The convergence branch scans Layer 0 only, refuses converged on a ghost gate, writes nothing on the fixture, and produces byte-identical reports across runs.</done>
</task>

<task type="auto">
  <name>Task 2: Complete tests/test-298-runner-idempotent.cjs</name>
  <files>tests/test-298-runner-idempotent.cjs</files>
  <read_first>
    - `tests/test-298-runner-idempotent.cjs` as left by plan 298-10 (the implemented tier, rung-exit, ghost and never-promote cases, and the two PENDING entries this task owns)
    - `tests/test-260906-t3s-worktree-hygiene.cjs` (the `record` counter, `assertEqual`, and its header principle that fixtures are disposable while the real repo is never touched; the one exception here is deliberate and stated below)
    - `.planning/phases/298-.../298-VALIDATION.md` rows T-298-08, T-298-09 and T-298-11
    - `.planning/phases/298-.../298-RESEARCH.md` Pitfall 10 (the ghost-ordering deadlock and why the zero-writes assertion is kept independent of the converged verdict)
  </read_first>
  <action>
    Implement the two remaining PENDING entries and flip the PENDING array to empty.

    Convergence and idempotence, run against the COMMITTED fixture and not a temp copy, because
    the requirement is an empty `git status --porcelain` in the real repository after two runs and
    a temp copy cannot prove that. Record in the test header why this one file breaks the
    disposable-fixture convention.
    - Run `node scripts/run-harness.cjs --room data/harness-fixtures/converged-room --json` twice
      with `spawnSync`. Assert both exit statuses are 0 and both reports parse with `converged`
      true.
    - Assert the two stdout strings are byte-identical.
    - Assert `data/harness-fixtures/converged-room/.mindrian` does not exist after the runs.
    - Assert `git status --porcelain data/harness-fixtures/converged-room` produces empty output.
    - Keep the zero-writes assertion in its own `record` entry, independent of the converged
      verdict, so a ghost-ordering regression fails one assertion rather than masking the other
      (Pitfall 10).
    - Assert `countEntries` against the fixture returns 13, exercising the exported function
      in-process.

    Ghost refusal: copy the eleven policy files plus `_schema.json` into an `fs.mkdtempSync`
    directory, rewrite the copied `gate-graph-derive-health.json` to set `runner` to null, point
    the runner at that temporary policy directory, run `--room` against the fixture, and assert
    the report names the policy as a ghost and reports `converged` false. Restore nothing in the
    real repository: the real policy directory is never touched. Assert afterwards that
    `git status --porcelain data/harness-policies` is empty.

    Non-converged room: build a throwaway room with `fs.mkdtempSync` that is missing one section's
    `ROOM.md`, and assert the runner exits 1, reports `converged` false, and names
    `room_md_present` as the failing check. Build a second throwaway room whose `.mindrian`
    directory exists and whose derive queue is non-empty, and assert the report is written to
    `<room>/.mindrian/harness-run.json` and that `converged` is false.

    Commit with an explicit `git add scripts/run-harness.cjs tests/test-298-runner-idempotent.cjs`.
  </action>
  <verify>
    <automated>node tests/test-298-runner-idempotent.cjs; echo "test-exit=$?"; git status --porcelain; bash tests/run-all-298.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-298-runner-idempotent.cjs` exits 0 with every PENDING entry implemented and the PENDING array empty.
    - `git status --porcelain` is empty after the test run, covering both the fixture and the policy directory.
    - The zero-writes assertion is a separate `record` entry from the converged-verdict assertion.
    - The ghost case reports `converged` false and names `gate-graph-derive-health` in the report.
    - The missing-ROOM.md room exits 1 and names `room_md_present`.
    - The non-empty-derive-queue room with an existing `.mindrian` gets a written `harness-run.json` and reports `converged` false.
    - `bash tests/run-all-298.sh` exits 0 with the runner-idempotence leg and the runner fixture leg both PASSED.
    - `grep -cP '\x{2014}|\x{2013}' tests/test-298-runner-idempotent.cjs` returns 0.
  </acceptance_criteria>
  <done>Idempotence is proven against the committed fixture with an empty porcelain status, and the ghost, missing-ROOM.md and dirty-queue paths all report honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--room <dir>` -> filesystem and room database | A caller-supplied path is scanned and its database opened |
| runner -> the committed repository | The runner must observe the fixture, never mutate it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-298-04 | Tampering | the `--room` branch | mitigate | Three independent proofs, as requirement 4 demands: the read-only URI opener rejects writes (probe-confirmed, `ERR_SQLITE_ERROR` on INSERT) and cannot create a missing file; `.mindrian` is asserted absent under the fixture after a run; `git status --porcelain` is asserted empty. Source greps assert the absence of `openGraph`, `openRoomDbForCaller`, `room-db.cjs` and `lazygraph-ops` |
| T-298-23 | Tampering | the report write | mitigate | The single write target is `<room>/.mindrian/harness-run.json` and it happens only when `.mindrian` already exists; the runner never creates that directory, so it cannot manufacture state in a Tier 0 room |
| T-298-24 | Repudiation | the converged verdict | mitigate | A ghost derive-health gate forces `converged` false (the missing-information error class) rather than a silent pass; the assertion is a separate `record` entry so a ghost-ordering regression cannot mask the zero-writes proof (Pitfall 10) |
| T-298-25 | Spoofing | report determinism | mitigate | The report carries no timestamp or duration, and the test asserts byte-identical stdout across two runs; a leaked clock value would fail that assertion immediately |
| T-298-02 | Information disclosure | the report | mitigate | The report carries counts, boolean check results and policy ids; no room content, no claim text, no node bodies. Zero network |
| T-298-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase; Node built-ins only |
</threat_model>

<verification>
- Two consecutive fixture runs exit 0, report converged true, and produce byte-identical JSON.
- `data/harness-fixtures/converged-room/.mindrian` is still absent; `git status --porcelain` is empty.
- The exported `countEntries` returns 13 against the fixture.
- `node tests/test-298-runner-idempotent.cjs` exits 0 and `bash tests/run-all-298.sh` exits 0.
</verification>

<success_criteria>
- R-04's headline truth holds: the runner is a no-op on the committed fixture, twice.
- Convergence is decided from Layer 0 alone, with `total_entries` compared and `computed:` never compared.
- D-03a is mechanically enforced, not merely intended.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 298 across all plans. The plan-review-convergence source-grounding
pass reads this section to exclude newly created symbols from drift verification.

**New CLI scripts**
- `scripts/run-harness.cjs` - flags `--check`, `--tier <t>`, `--room <dir>`, `--policy <id>`, `--json`, `--help`; exported `loadPolicies`, `classifyPolicy`, `countEntries`, `scanRoom`, the tier filter, the report builder and `main`
- `scripts/check-graph-derive-health.cjs` - flags `--room <dir>`, `--all`, `--json`, `--strict`, `--check`, `--help`
- `scripts/check-voice-style.cjs` - Stop hook, stdin envelope, no flags

**New library modules and exported functions**
- `lib/hmi/turn-text.cjs` - `readTurnText`, `readTranscriptTurn`, `readTranscriptTail`, `extractAssistantText`, `classifyPrecedingUserContentSource`, `scanContentForAskUserQuestion`, `TRANSCRIPT_TAIL_BYTES`
- `lib/hmi/voice-style-log.cjs` - `appendVoiceStyleRow`, `readVoiceStyleRows`, `evaluatePromotion`, `mindrianHome`, `voiceStyleLogPath`
- `lib/core/doctor/voice-style-log-module.cjs` - `check`, `voiceStyleLogPath`

**Room report keys** in `<room>/.mindrian/harness-run.json`: `room`, `converged`, `checks` (with `room_md_present`, `entry_count_matches`, `derive_queue_empty`, `zero_proposed_claims`, `derive_health_declared`), `total_entries_declared`, `total_entries_on_disk`, `computed_stamp`, `findings`

**New generator symbols in `scripts/build-harness-manifest.cjs`**
- `LARRY_SURFACE_BINDINGS`, `POLICIES_DIR`, `FIXTURE_DIR`, `digestDirectory`, `loadPolicies`, `validatePolicyFile`, `runContractChecks`; `MANIFEST_VERSION` becomes 2

**New manifest JSON keys** in `data/harness-manifest.json`: `policies` with `path`, `digest`, `count`; `larry_surfaces` as an array of `role`, `path`, `digest`; `fixture_ref` with `path`, `digest`

**New policy schema keys** in `data/harness-policies/_schema.json` under `_doc`: `purpose`, `rung_vocabulary`, `rung_meaning`, `kind_vocabulary`, `applies_to_vocabulary`, `policy_fields`, `runner_path_rule`, `promotion_rule_doc`, `gate_count_principle`, `validation_rule`, `default_on_miss`.
**Per-policy file keys**: `id`, `kind`, `runner`, `args`, `rung`, `evidence_log`, `promotion_rule` with `window_runs`, `max_false_positive_rate`, `min_true_positives`; `owner`, `pinned_by`, `applies_to`, `notes`.

**New data paths**
- `data/harness-policies/` with `_schema.json`, `CONTEXT.md`, and 11 policy files
- `data/harness-fixtures/converged-room/` (33 committed text files)
- `<room>/.mindrian/harness-run.json` (runtime output, never committed, written only when `.mindrian` already exists)
- `$MINDRIAN_HOME/voice-style.jsonl` and its rotation `voice-style.jsonl.1` (runtime, never committed)

**New registry ids**
- doctor module id `voice-style-log` in `data/doctor-modules.json`
- doctor acceptance point id `harness-policies` in `scripts/doctor.cjs` `buildAcceptanceChecklist`
- one `hooks/hooks.json` Stop entry invoking `scripts/check-voice-style.cjs`

**New test files**
- `tests/test-298-policies-schema.cjs`, `tests/test-298-runner-idempotent.cjs`, `tests/test-298-derive-health.cjs`, `tests/test-298-voice-log.cjs`, `tests/test-298-contract-parity.cjs`, `tests/run-all-298.sh`

**New key on the `lib/core/recipe-maps.cjs` `loadManifest()` return**: `policies`

<output>
Create `.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-11-SUMMARY.md` when done, recording the final report key order, the observed fixture report verbatim, and the confirmation read of the lazygraph-ops first-touch mkdir that discharges research assumption A1.
</output>
