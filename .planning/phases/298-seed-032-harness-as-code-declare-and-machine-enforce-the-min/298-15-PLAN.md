---
phase: 298
plan: 15
type: execute
wave: 6
depends_on: [298-10, 298-11, 298-12, 298-13, 298-14]
files_modified:
  - scripts/doctor.cjs
  - CLAUDE.md
  - tests/run-all-298.sh
autonomous: true
requirements: [R-09, R-11]

must_haves:
  truths:
    - "R-09: node scripts/doctor.cjs --acceptance --pre-tag carries one harness-policies point that spawns the runner --check --json and blocks on failure"
    - "R-09: the point is wired LAST, only after slice 1 is green, because every acceptance entry is severity blocker and --acceptance hard-aborts with no override; a blocker's first run must be a true green (the D-4 precedent from quick 260906-t3s)"
    - "D-02: the acceptance point's finding echoes the same verdict string the runner printed, because both come from one evaluatePromotion return"
    - "CLAUDE.md's Verification section names the runner and the acceptance point, so a future session does not have to infer the commands (LangTalks takeaway 7)"
    - "R-11: the phase closes with no new test failures anywhere, proven by rerunning the 20 baseline commands and diffing against the recorded set"
  artifacts:
    - path: "scripts/doctor.cjs"
      provides: "The harness-policies acceptance point in the shape of worktree-hygiene"
      contains: "harness-policies"
    - path: "CLAUDE.md"
      provides: "Two Verification lines naming the runner and the acceptance point"
      contains: "run-harness"
  key_links:
    - from: "scripts/doctor.cjs"
      to: "scripts/run-harness.cjs"
      via: "spawnSync with an argv array and a JSON parse of stdout"
      pattern: "run-harness"
---

<objective>
Wire the release train to the harness, name the commands in CLAUDE.md, and close the phase with a
measured no-new-failures proof.

Purpose: R-09 and R-11. This is the embedded rung on the placement ladder: standalone
(`run-harness --check`) became embedded (a doctor acceptance point) and chained (the widened
pre-commit guard). It is deliberately last, because every acceptance entry is a blocker and a
blocker's first run must be a true green.

Output: one acceptance point, two CLAUDE.md lines, a finalized aggregator, and the phase-close
baseline diff.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-SPEC.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-PATTERNS.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-VALIDATION.md
@CLAUDE.md
</context>

<locked_constraints>
- Never `git stash`. Explicit `git add <paths>` only. No em-dashes or en-dashes.
- Wire the acceptance point ONLY after plans 298-10 through 298-14 are green. Confirm that first; if `node scripts/run-harness.cjs --check --json` does not already exit 0 on a clean tree, stop and report rather than wiring a blocker that fails on its first run.
- Every `buildAcceptanceChecklist` entry is `severity: 'blocker'` and `--acceptance` hard-aborts with no allow override. Do not attempt to make this point advisory; informational reporting already lives in the `voice-style-log` doctor MODULE from plan 298-08.
- Copy the `worktree-hygiene` structure verbatim, preserving all four features: the `DOCTOR_TEST_FAIL_POINT` test seam, the script-missing guard BEFORE the spawn, the parse-failure branch that truncates stdout and stderr to the last 500 characters, and `applies_to: ['pre-tag', 'full']`.
- Do not regenerate `dist/` mirrors as a side effect.
</locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add the harness-policies acceptance point to doctor.cjs</name>
  <files>scripts/doctor.cjs</files>
  <read_first>
    - `scripts/doctor.cjs` lines 1313-1365: the `worktree-hygiene` entry in full, which is the structural template
    - `scripts/doctor.cjs` lines 1600-1615: the `eureka-fts-index-visible` comment explaining that every acceptance entry is a blocker and that logic, not severity, is the lever
    - `scripts/doctor.cjs` lines 2988-3012: the hard-abort with no allow override, and the PASS or FAIL tag plus finding-suffix printer the D-02 verdict must echo
    - `scripts/run-harness.cjs` as landed by plans 298-10 and 298-11 (the exact `--json` report key names and the verdict string format, recorded in the 298-10 SUMMARY)
    - `tests/test-doctor-acceptance-self-coverage.cjs` (what it requires of a new point)
    - `.planning/phases/298-.../298-RESEARCH.md` section "`harness-policies` acceptance point: the exact template"
  </read_first>
  <action>
    Before editing, confirm the precondition: run `node scripts/run-harness.cjs --check --json` on
    a clean tree and require exit 0. If it is non-zero, stop and report; do not wire a blocker
    whose first run is red.

    Add one entry to `buildAcceptanceChecklist` in `scripts/doctor.cjs`, placed beside
    `worktree-hygiene` and copied from it structurally. Fields: `id` `harness-policies`; a `label`
    naming what it proves (every declared harness policy runs and no blocking policy failed);
    `severity: 'blocker'`; `applies_to: ['pre-tag', 'full']`.

    The `run` function, in order: the `inTestMode` and `DOCTOR_TEST_FAIL_POINT` seam returning a
    synthesized failure for the id `harness-policies`; a `fs.existsSync` guard on
    `path.join(pluginRoot, 'scripts', 'run-harness.cjs')` returning a script-missing finding
    BEFORE any spawn; a `spawnSync('node', [scriptPath, '--check', '--json'], {encoding: 'utf8',
    timeout: 30000, cwd: pluginRoot})`; a `JSON.parse` of stdout inside a try/catch whose failure
    branch returns a finding naming the parse error with `stdout` and `stderr` each truncated to
    the last 500 characters; and an outer try/catch returning a threw finding.

    `ok` is the spawn's exit status equal to 0. The `finding` string on failure names the failing
    policy ids and the exact recovery command (`node scripts/run-harness.cjs --check`), and echoes
    the runner's own verdict string verbatim so the doctor line and the runner can never disagree
    (the 2026-07-11 printer-must-match-counter lesson). The `detail` object carries the counts
    parsed from the report.

    Add a comment block above the entry in the house style: what it catches, WHY it is a blocker
    and not advisory (a declared policy that stops running is a silent governance hole, and every
    acceptance entry is a blocker by construction), and the Canon Part 8 line (local reads and a
    local spawn only, zero network).
  </action>
  <verify>
    <automated>node scripts/doctor.cjs --acceptance --pre-tag; echo "acceptance-exit=$?"; node tests/test-doctor-acceptance-self-coverage.cjs; echo "self-coverage-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/doctor.cjs --acceptance --pre-tag` exits 0 and its output contains a line beginning `PASS  harness-policies:`.
    - `DOCTOR_TEST_FAIL_POINT=harness-policies node scripts/doctor.cjs --acceptance --pre-tag` exits 1 and names the synthesized failure.
    - `node tests/test-doctor-acceptance-self-coverage.cjs` exits 0.
    - Temporarily renaming `scripts/run-harness.cjs` makes the point return the script-missing finding rather than throwing; restore the name afterwards and confirm the point passes again.
    - `grep -c "harness-policies" scripts/doctor.cjs` returns at least 1 and the entry carries `severity: 'blocker'` and `applies_to: ['pre-tag', 'full']`.
    - `grep -c "slice(-500)" scripts/doctor.cjs` is one greater than its value at HEAD (the parse-failure truncation branch was copied).
    - The failing-path `finding` string contains the literal recovery command `node scripts/run-harness.cjs --check`.
    - `grep -cP '\x{2014}|\x{2013}' scripts/doctor.cjs` returns 0.
  </acceptance_criteria>
  <done>The acceptance point spawns the runner, blocks on a blocking-policy failure, honors the test seam and the script-missing guard, and its first real run is a true green.</done>
</task>

<task type="auto">
  <name>Task 2: Name the runner and the acceptance point in CLAUDE.md's Verification section</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` the `## Verification` section (currently four bullets: phase tests, the release gate, the born-wired and projection and render gates, and the acceptance roll-up)
    - `.planning/phases/298-.../298-RESEARCH.md` LangTalks planner-facing takeaway 7 (list your exact build and test commands in CLAUDE.md so Claude does not have to infer them)
  </read_first>
  <action>
    Add exactly two lines to the `## Verification` section of `CLAUDE.md`, matching the existing
    bullet style and placement.

    First line: the harness runner. Name `node scripts/run-harness.cjs --check` as the standalone
    policy check, `--tier pre-tag` as the release-tier form, `--room <dir>` as the convergence
    check, and `--policy <id>` as the promotion review.

    Second line: the acceptance point. Name that `node scripts/doctor.cjs --acceptance` now
    includes a `harness-policies` blocker that spawns the runner, so a declared policy that stops
    running fails the release train.

    Keep both lines short and in the imperative register the section already uses. Do not restructure
    the section, do not touch any other part of `CLAUDE.md`, and do not add an em-dash.
  </action>
  <verify>
    <automated>grep -n "run-harness" CLAUDE.md; grep -n "harness-policies" CLAUDE.md; grep -cP '\x{2014}|\x{2013}' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "run-harness" CLAUDE.md` returns at least 1 and the match sits inside the `## Verification` section.
    - `grep -c "harness-policies" CLAUDE.md` returns at least 1.
    - The runner line names all four flags: `--check`, `--tier`, `--room`, `--policy`.
    - `git diff HEAD -- CLAUDE.md` shows exactly two added lines and no other change.
    - `grep -cP '\x{2014}|\x{2013}' CLAUDE.md` returns 0.
  </acceptance_criteria>
  <done>CLAUDE.md's Verification section names the runner's four flags and the new acceptance point in two lines.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize the aggregator and prove no new failures against the recorded baseline</name>
  <files>tests/run-all-298.sh</files>
  <read_first>
    - `tests/run-all-298.sh` as landed by plan 298-01 (the seven `run_if` legs)
    - `.planning/phases/298-.../298-TEST-BASELINE.md` from plan 298-01 (the 20 commands with their recorded exit codes and the named expected-red set)
    - `.planning/phases/298-.../298-VALIDATION.md` "Sampling Rate" (the phase-gate list) and the Per-Task Verification Map, whose Status column is filled in here
  </read_first>
  <action>
    Add two legs to `tests/run-all-298.sh`, keeping the `run_if` idiom: one for
    `node scripts/check-graph-derive-health.cjs --room data/harness-fixtures/converged-room`,
    guarded on `scripts/check-graph-derive-health.cjs`; and one for
    `node scripts/run-harness.cjs --check --tier pre-tag`, guarded on `scripts/run-harness.cjs`.
    Every leg must now RUN rather than SKIP, since every guard file exists. Confirm the summary
    line reports `SKIP=0`.

    Then run the phase-close gate, in this order, recording every exit code:
    `bash tests/run-all-298.sh`, `bash tests/run-all-167.sh`, `bash tests/run-all-201.sh`,
    `bash tests/run-all-189.sh`, `node lib/mcp/no-instructions.test.cjs`,
    `node scripts/build-harness-manifest.cjs --check`,
    `node scripts/doctor.cjs --acceptance --pre-tag`, and `scripts/verify-release`.

    Then rerun all 20 baseline commands from `298-TEST-BASELINE.md` and produce a comparison table
    in the plan SUMMARY with columns Command, Baseline exit, Now exit, Verdict. The verdict is
    `unchanged`, `repaired` (was red, now green) or `REGRESSION` (was green, now red). A single
    REGRESSION row means the phase is not done: stop and report rather than closing.

    Expect exactly these movements and assert them: `test-harness-manifest-precommit-wiring.cjs`
    repaired to green, and `test-harness-167-verdict.cjs` improved from two failing checks to one
    while still exiting non-zero for the unrelated D-167-06 Part 9 item. The four persona-cluster
    reds stay unchanged.

    Finally, fill in the Task ID, Plan and Status columns of the Per-Task Verification Map in
    `298-VALIDATION.md`, set `nyquist_compliant: true`, `wave_0_complete: true` and
    `status: final` in its frontmatter, tick the remaining Wave 0 box (the fixture, landed by
    298-02) and every Validation Sign-Off box that now holds, and record the approval line as
    `approved 2026-MM-DD` with the real date.

    Commit with an explicit `git add scripts/doctor.cjs CLAUDE.md tests/run-all-298.sh`.
  </action>
  <verify>
    <automated>bash tests/run-all-298.sh; echo "298-exit=$?"; bash tests/run-all-167.sh; echo "167-exit=$?"; bash tests/run-all-201.sh; echo "201-exit=$?"; node scripts/build-harness-manifest.cjs --check; echo "manifest-exit=$?"; node scripts/doctor.cjs --acceptance --pre-tag; echo "acceptance-exit=$?"; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-298.sh` exits 0 with `FAIL=0` and `SKIP=0`.
    - `bash tests/run-all-167.sh` and `bash tests/run-all-201.sh` exit 0.
    - `node scripts/build-harness-manifest.cjs --check` exits 0.
    - `node scripts/doctor.cjs --acceptance --pre-tag` exits 0 with `PASS  harness-policies`.
    - `scripts/verify-release` matches or improves on its recorded baseline.
    - The 20-command comparison table in the SUMMARY contains zero REGRESSION rows.
    - `test-harness-manifest-precommit-wiring.cjs` is marked repaired and `test-harness-167-verdict.cjs` is marked improved with one remaining failing check.
    - `git status --porcelain` is empty after the commit, and `git status --porcelain dist/` is empty.
    - `298-VALIDATION.md` has `nyquist_compliant: true`, `wave_0_complete: true`, `status: final`, an `**Approval:** approved` line with a date, and every row of the Per-Task Verification Map carries a Task ID, a Plan and a Status.
  </acceptance_criteria>
  <done>Every 298 leg runs and passes, the release gates are green, and the 20-command comparison against the recorded baseline shows zero regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the runner's exit status -> the release train | A new blocker can stop a release |
| the runner's stdout -> the doctor point | Parsed JSON drives a blocking verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-298-31 | Denial of service | the `harness-policies` blocker | mitigate | Wired LAST, only after a confirmed clean `run-harness --check`, per the D-4 precedent that a blocker's first run must be a true green. The 30000ms spawn timeout bounds it, the script-missing guard precedes the spawn, and the parse-failure branch truncates output to 500 characters rather than dumping a whole report into a release log |
| T-298-03 | Elevation of privilege | the doctor spawn | mitigate | `spawnSync('node', [scriptPath, '--check', '--json'])` is an argv array with a repo-resolved script path, never a shell string; `pluginRoot` is the cwd |
| T-298-06 | Repudiation | the acceptance finding | mitigate | The finding echoes the runner's own verdict string verbatim, so the doctor line and the runner cannot disagree; the 2026-07-11 printer-must-match-counter lesson |
| T-298-32 | Repudiation | the no-new-failures claim | mitigate | The claim is measured, not asserted: all 20 baseline commands are rerun and diffed against the recorded exit codes, and a single REGRESSION row stops the phase (Pitfall 7) |
| T-298-02 | Information disclosure | the truncated stdout in a finding | mitigate | The runner's report carries counts, policy ids and verdicts only; no room content can reach a release log through it |
| T-298-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase; Node built-ins only |
</threat_model>

<verification>
- `node scripts/doctor.cjs --acceptance --pre-tag` exits 0 with a PASS on `harness-policies`; the `DOCTOR_TEST_FAIL_POINT` negative exits 1.
- `node tests/test-doctor-acceptance-self-coverage.cjs` exits 0.
- `bash tests/run-all-298.sh` reports FAIL=0 and SKIP=0; the 167 and 201 suites exit 0.
- The 20-command baseline comparison shows zero REGRESSION rows.
- `CLAUDE.md` names the runner's four flags and the acceptance point.
</verification>

<success_criteria>
- R-09 delivered: the release train runs the harness, and it went green on its first blocking run.
- R-11 delivered honestly: no new failures anywhere, two pre-existing manifest-cluster reds repaired, the four persona reds unchanged and named.
- The four success truths of the design are all live: `--check` catches a dropped phrase or a busted budget; every slice-1 gate is reachable from one policy entry; the runner is a no-op on the fixture; and the persona states the operating policy the manifest declares.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 298 across all plans. The plan-review-convergence source-grounding
pass reads this section to exclude newly created symbols from drift verification.

**New CLI scripts**
- `scripts/run-harness.cjs` - flags `--check`, `--tier <t>`, `--room <dir>`, `--policy <id>`, `--json`, `--help`; exported `loadPolicies`, `classifyPolicy`, `countEntries`, `scanRoom`, `main`
- `scripts/check-graph-derive-health.cjs` - flags `--room <dir>`, `--all`, `--json`, `--strict`, `--check`, `--help`
- `scripts/check-voice-style.cjs` - Stop hook, stdin envelope, no flags

**New library modules and exported functions**
- `lib/hmi/turn-text.cjs` - `readTurnText`, `readTranscriptTurn`, `readTranscriptTail`, `extractAssistantText`, `classifyPrecedingUserContentSource`, `scanContentForAskUserQuestion`, `TRANSCRIPT_TAIL_BYTES`
- `lib/hmi/voice-style-log.cjs` - `appendVoiceStyleRow`, `readVoiceStyleRows`, `evaluatePromotion`, `mindrianHome`, `voiceStyleLogPath`
- `lib/core/doctor/voice-style-log-module.cjs` - `check`, `voiceStyleLogPath`

**New generator symbols in `scripts/build-harness-manifest.cjs`**
- `LARRY_SURFACE_BINDINGS`, `POLICIES_DIR`, `FIXTURE_DIR`, `digestDirectory`, `loadPolicies`, `validatePolicyFile`, `runContractChecks`; `MANIFEST_VERSION` becomes 2

**New manifest JSON keys** in `data/harness-manifest.json`: `policies` with `path`, `digest`, `count`; `larry_surfaces` as an array of `role`, `path`, `digest`; `fixture_ref` with `path`, `digest`

**New policy schema keys** in `data/harness-policies/_schema.json` under `_doc`: `purpose`, `rung_vocabulary`, `rung_meaning`, `kind_vocabulary`, `applies_to_vocabulary`, `policy_fields`, `runner_path_rule`, `promotion_rule_doc`, `gate_count_principle`, `validation_rule`, `default_on_miss`.
**Per-policy file keys**: `id`, `kind`, `runner`, `args`, `rung`, `evidence_log`, `promotion_rule` with `window_runs`, `max_false_positive_rate`, `min_true_positives`; `owner`, `pinned_by`, `applies_to`, `notes`.

**New data paths**
- `data/harness-policies/` with `_schema.json`, `CONTEXT.md`, and 11 policy files
- `data/harness-fixtures/converged-room/` (33 committed text files)
- `<room>/.mindrian/harness-run.json` (runtime output, never committed)
- `$MINDRIAN_HOME/voice-style.jsonl` and its rotation `voice-style.jsonl.1` (runtime, never committed)

**New registry ids**
- doctor module id `voice-style-log` in `data/doctor-modules.json`
- doctor acceptance point id `harness-policies` in `scripts/doctor.cjs` `buildAcceptanceChecklist`
- one `hooks/hooks.json` Stop entry invoking `scripts/check-voice-style.cjs`

**New test files**
- `tests/test-298-policies-schema.cjs`, `tests/test-298-runner-idempotent.cjs`, `tests/test-298-derive-health.cjs`, `tests/test-298-voice-log.cjs`, `tests/test-298-contract-parity.cjs`, `tests/run-all-298.sh`

**New key on the `lib/core/recipe-maps.cjs` `loadManifest()` return**: `policies`

<output>
Create `.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-15-SUMMARY.md` when done, containing the 20-command baseline comparison table, the final acceptance-point output line, and a phase-level statement of the four design success truths with the command that proves each.
</output>
