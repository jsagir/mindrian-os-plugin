---
phase: 298
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-TEST-BASELINE.md
  - tests/run-all-298.sh
  - tests/test-298-policies-schema.cjs
  - tests/test-298-runner-idempotent.cjs
  - tests/test-298-derive-health.cjs
  - tests/test-298-voice-log.cjs
  - tests/test-298-contract-parity.cjs
autonomous: true
requirements: [R-11]

must_haves:
  truths:
    - "The six pre-existing red tests at HEAD are recorded verbatim before any 298 edit, so R-11's no-new-failures promise is provable (Pitfall 7)"
    - "bash tests/run-all-298.sh runs at HEAD and exits 0 with every leg SKIPPED, because run_if guards each leg on a file that does not exist yet"
    - "Each of the five test-298-*.cjs stubs exits 0 while its subject artifact is absent and exits 1 the moment the subject lands with no assertions implemented, so no later plan can pass vacuously (T-233-04 false-success class)"
    - "The runner-idempotent stub already encodes D-03a as a source grep so the read-only-door rule cannot be forgotten later"
  artifacts:
    - path: ".planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-TEST-BASELINE.md"
      provides: "The verbatim pre-edit pass/fail baseline for 20 named commands"
      contains: "test-harness-manifest-precommit-wiring"
    - path: "tests/run-all-298.sh"
      provides: "Phase 298 aggregator with run_if guards"
      contains: "run_if"
    - path: "tests/test-298-policies-schema.cjs"
      provides: "R-02 and R-03 proof surface (stub)"
    - path: "tests/test-298-runner-idempotent.cjs"
      provides: "R-04 proof surface (stub)"
    - path: "tests/test-298-derive-health.cjs"
      provides: "R-05 proof surface (stub)"
    - path: "tests/test-298-voice-log.cjs"
      provides: "R-08 proof surface (stub)"
    - path: "tests/test-298-contract-parity.cjs"
      provides: "R-06 proof surface (stub)"
  key_links:
    - from: "tests/run-all-298.sh"
      to: "tests/test-298-*.cjs"
      via: "run_if leg guarded on the test file path"
      pattern: "run_if .*test-298-"
    - from: "tests/test-298-runner-idempotent.cjs"
      to: "scripts/run-harness.cjs"
      via: "PENDING assertion list naming the D-03a source grep"
      pattern: "openGraph"
---

<objective>
Wave 0 for Phase 298. Capture the pre-existing red-test baseline verbatim BEFORE any file in
this phase is edited, create the phase aggregator `tests/run-all-298.sh` from the shipped
`tests/run-all-201.sh` idiom, and create the five `tests/test-298-*.cjs` stubs that later plans
turn green.

Purpose: R-11 says "existing tests stay green", and six tests are already red at HEAD for
reasons that predate this phase (research Pitfall 7). Without a recorded baseline the phase gets
blamed for them. The stubs exist so that every later task has an owning automated test on day
one, and so a stub can never pass vacuously once its subject lands.

Output: one baseline record, one aggregator, five test stubs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-SPEC.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-VALIDATION.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-PATTERNS.md
</context>

<locked_constraints>
Binding on every task in this plan and every other 298 plan:
- Never run `git stash`. Take baselines with `git show HEAD:<path>` or by copying to the phase directory.
- Commit with explicit `git add <paths>` only. Never `git add -A` or `git add .`.
- No em-dashes and no en-dashes anywhere: code, JSON, markdown, test names, commit messages.
- CJS only. No TypeScript. No new dependencies. Node built-ins only. No YAML.
- Do not regenerate `dist/` mirrors as a side effect. `test-234-dist-bundle.cjs` is already red on stale mirrors and stays out of scope.
- `.planning/` is gitignored in this repo. Phase artifacts under `.planning/` are working records; do not force-add them unless the navigator asks.
</locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Record the pre-edit test baseline verbatim</name>
  <files>.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-TEST-BASELINE.md</files>
  <read_first>
    - `.planning/phases/298-.../298-RESEARCH.md` section "Tests: conventions and the live baseline" (the manifest-cluster table) and section "The live test baseline for the persona cluster" (the four persona failures)
    - `.planning/phases/298-.../298-RESEARCH.md` Pitfall 7 ("promising all existing tests green")
    - `tests/run-all-201.sh` (the aggregator this phase copies in Task 2)
  </read_first>
  <action>
    Confirm the working tree is clean first with `git status --porcelain` (expect empty output).
    If it is not empty, stop and report; do NOT stash.

    Run each of these 20 commands from the repo root, one at a time, capturing the exit code and
    the last 5 lines of stdout for each. Do not fix anything.

    Manifest cluster (6): `node scripts/build-harness-manifest.cjs --check`,
    `node tests/test-harness-manifest-check.cjs`,
    `node tests/test-harness-manifest-part8-boundary.cjs`,
    `node tests/test-201-harness-manifest.cjs`,
    `node tests/test-harness-167-verdict.cjs`,
    `node tests/test-harness-manifest-precommit-wiring.cjs`.

    Persona cluster (10): `node lib/mcp/no-instructions.test.cjs`,
    `node tests/test-143.2-doctrine-presence.cjs`, `node tests/test-larry-handoff-seam.cjs`,
    `node tests/test-canon-entry-38-sourced-claims-floor.cjs`,
    `node tests/test-gate-native-fire-w1.cjs`, `node tests/test-chain-executor-part8-leak.cjs`,
    `bash tests/test-115-persona-variants.sh`, `bash tests/test-114-substrate-preload.sh`,
    `node tests/test-205-elevation-doctrine-floor.cjs`, `bash tests/test-115-surfaces-grep.sh`.

    Gate cluster (4): `node scripts/check-hook-schema-compatibility.cjs`,
    `node tests/test-doctor-acceptance-self-coverage.cjs`,
    `node tests/test-209-declared-implies-wired.cjs`,
    `node tests/test-connector-exhaustive-coverage.cjs`.

    Write `298-TEST-BASELINE.md` with: a `captured_at` line, the HEAD sha from
    `git rev-parse HEAD`, a table with columns Command, Exit code, Verdict (PASS or FAIL), Note,
    and a final section `## Expected red at HEAD (do not attribute to Phase 298)` listing the
    tests that exited non-zero with the one-line reason from RESEARCH.md for each. The research
    predicts these six as red: `test-harness-manifest-precommit-wiring.cjs`,
    `test-harness-167-verdict.cjs`, `test-205-elevation-doctrine-floor.cjs`,
    `test-209-declared-implies-wired.cjs`, `test-connector-exhaustive-coverage.cjs`,
    `test-115-surfaces-grep.sh`. Record what you actually observe, not what the research
    predicted; if the observed set differs, say so explicitly in a `## Divergence from research`
    section.

    Do not commit this file (`.planning/` is gitignored). It is the phase's working record and
    the phase-close comparison reruns the same 20 commands.
  </action>
  <verify>
    <automated>test -f .planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-TEST-BASELINE.md && grep -c "test-harness-manifest-precommit-wiring" .planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-TEST-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `298-TEST-BASELINE.md` exists and contains one table row per each of the 20 commands above.
    - Every row carries a numeric exit code, not a prose verdict alone.
    - The file contains a `## Expected red at HEAD` section naming each observed failure with a reason.
    - `git status --porcelain` lists no tracked-file modification caused by this task.
    - The file contains zero U+2014 and zero U+2013 characters: `grep -cP '\x{2014}|\x{2013}' <file>` returns 0.
  </acceptance_criteria>
  <done>The pre-edit baseline is on disk with observed exit codes for all 20 commands and a named expected-red set.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/run-all-298.sh from the run-all-201.sh idiom</name>
  <files>tests/run-all-298.sh</files>
  <read_first>
    - `tests/run-all-201.sh` in full (the `run`, `run_if`, PASS/FAIL/SKIP counters, the trailing `[ "$FAIL" -eq 0 ]`)
    - `.planning/phases/298-.../298-PATTERNS.md` section "The five tests/test-298-*.cjs files" -> the `tests/run-all-298.sh` excerpt and its leg list
  </read_first>
  <action>
    Copy the structure of `tests/run-all-201.sh` into `tests/run-all-298.sh`: `set -uo pipefail`,
    the `ROOT` resolution and `cd "$ROOT"`, the `PASS`/`FAIL`/`SKIP` counters, the `run()` and
    `run_if()` functions verbatim, and the trailing summary line plus `[ "$FAIL" -eq 0 ]`.

    Rewrite the header comment for Phase 298: name what the phase lands (manifest v2, the policy
    directory, the runner, the derive-health gate, the voice rung-2 trio, the F.8 readable rows)
    and state the `run_if` rationale (a four-slice phase must exit with SKIPs, not FAILs, while
    partially landed). Change the final echo to `Phase 298: PASS=$PASS FAIL=$FAIL SKIP=$SKIP`.

    Legs, in this order, each `run_if <label> <guard-file> <cmd>`:
    1. "298-01 policies schema" guard `tests/test-298-policies-schema.cjs` -> `node tests/test-298-policies-schema.cjs`
    2. "298-01 derive health gate" guard `tests/test-298-derive-health.cjs` -> `node tests/test-298-derive-health.cjs`
    3. "298-01 voice-style log" guard `tests/test-298-voice-log.cjs` -> `node tests/test-298-voice-log.cjs`
    4. "298-01 contract parity" guard `tests/test-298-contract-parity.cjs` -> `node tests/test-298-contract-parity.cjs`
    5. "298-01 runner idempotence" guard `tests/test-298-runner-idempotent.cjs` -> `node tests/test-298-runner-idempotent.cjs`
    6. "298 harness-manifest --check" guard `data/harness-manifest.json` -> `node scripts/build-harness-manifest.cjs --check`
    7. "298 runner converged fixture" guard `scripts/run-harness.cjs` -> `node scripts/run-harness.cjs --room data/harness-fixtures/converged-room`

    Make the file executable (`chmod +x tests/run-all-298.sh`). Do not add a leg that runs any
    existing 167/201 suite; those are run separately at wave boundaries.

    Commit with `git add tests/run-all-298.sh` only.
  </action>
  <verify>
    <automated>bash tests/run-all-298.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-298.sh` exits 0 at this point in the phase.
    - Its final line matches `Phase 298: PASS=0 FAIL=0 SKIP=7` before Task 3 lands, and `SKIP=2` or lower after Task 3 lands the five stubs (legs 6 and 7 still skip until the manifest is v2 and the runner exists; leg 6's guard `data/harness-manifest.json` already exists at HEAD so leg 6 runs and passes).
    - `grep -c "run_if" tests/run-all-298.sh` returns 7.
    - `grep -cP '\x{2014}|\x{2013}' tests/run-all-298.sh` returns 0.
    - The file is executable: `test -x tests/run-all-298.sh`.
  </acceptance_criteria>
  <done>The aggregator exists, is executable, exits 0, and reports SKIP for every leg whose guard file is absent.</done>
</task>

<task type="auto">
  <name>Task 3: Create the five test-298 stubs with an anti-vacuous-pass guard</name>
  <files>tests/test-298-policies-schema.cjs, tests/test-298-runner-idempotent.cjs, tests/test-298-derive-health.cjs, tests/test-298-voice-log.cjs, tests/test-298-contract-parity.cjs</files>
  <read_first>
    - `tests/test-260906-t3s-worktree-hygiene.cjs` lines 1-60 (the `record(name, fn)` counter and the local `assertEqual`; this is the idiom to copy)
    - `.planning/phases/298-.../298-VALIDATION.md` "Per-Task Verification Map" (the T-298-xx rows each stub will eventually prove)
    - `.planning/phases/298-.../298-PATTERNS.md` section "The five tests/test-298-*.cjs files" (which existing test each mirrors)
  </read_first>
  <action>
    Create five plain node scripts under `tests/`, each with: `'use strict';`, a header comment
    naming the phase, the requirement ids it proves, and the T-298-xx rows from
    `298-VALIDATION.md` it will carry; the `record(name, fn)` / `assertEqual(actual, expected,
    msg)` pair copied from `tests/test-260906-t3s-worktree-hygiene.cjs`; and a
    `SUBJECT` constant naming the artifact the test measures.

    Every stub uses this exact three-state contract at the top of `main()`:
    (a) if the SUBJECT path does not exist, print `SKIP <test name> (missing <SUBJECT>)` and
    `process.exit(0)`;
    (b) if the SUBJECT path exists and the module-level constant `ASSERTIONS_IMPLEMENTED` is
    still `false`, print `FAIL <test name>: subject landed but assertions are still a stub` plus
    the PENDING list, and `process.exit(1)`;
    (c) otherwise run the recorded assertions and exit 1 if `failCount > 0`.
    Declare `const ASSERTIONS_IMPLEMENTED = false;` in every stub. The plan that lands each
    SUBJECT flips it to `true` and fills the assertions. This is the anti-false-success shape
    (T-233-04): a stub can never pass once its subject exists.

    Per-file SUBJECT and PENDING list (write the PENDING entries as a string array printed in
    state (b), one line per row, so the owning plan has the checklist in the file):

    `tests/test-298-policies-schema.cjs` - SUBJECT `data/harness-policies/_schema.json`.
    PENDING: every file in `data/harness-policies/` except `_schema.json` and `CONTEXT.md`
    validates against the schema; unknown key rejected; `rung` in
    `['declared','logged','blocking']`; `kind` in `['gate','voice','memory','contract']`;
    every `runner` is either `null` or a repo-relative path that resolves inside REPO_ROOT and
    exists; `promotion_rule` carries `window_runs`, `max_false_positive_rate`,
    `min_true_positives`; `applies_to` drawn from
    `['pre-flight','pre-tag','full','stop-hook','room']`; zero unintentional ghosts among the
    `gate-*` policies.

    `tests/test-298-runner-idempotent.cjs` - SUBJECT `scripts/run-harness.cjs`.
    PENDING: `--check --tier pre-tag --json` reports only policies whose `applies_to` contains
    `pre-tag`; a temp `logged` policy failing exits 0 and appends exactly one JSONL row; the same
    policy at `blocking` exits 1; a `runner: null` policy reports `ghost` and forces
    `converged: false`; two consecutive `--room data/harness-fixtures/converged-room` runs both
    exit 0 with `converged: true` and leave `git status --porcelain` empty; the two reports are
    byte-identical; `data/harness-fixtures/converged-room/.mindrian` does not exist afterwards;
    the source grep `openGraph|openRoomDbForCaller|room-db.cjs` over `scripts/run-harness.cjs`
    returns 0 matches (D-03a); the source grep for an assignment to `rung` in
    `scripts/run-harness.cjs` returns 0 matches (R-03, the runner never promotes).

    `tests/test-298-derive-health.cjs` - SUBJECT `scripts/check-graph-derive-health.cjs`.
    PENDING: a throwaway `node:sqlite` room.db carrying BELONGS_TO edges and zero cascade edges
    yields `detectRoomHealth().status === 'fail'` and the wrapper exits 1; the committed fixture
    yields `status === 'skip'` and the wrapper exits 0; `--json` prints the returned object
    verbatim; an unknown flag exits 2. Guard the whole file on `node:sqlite` availability with
    the `try { require('node:sqlite') } catch` SKIP pattern used at
    `tests/test-189-governance-candidates.cjs:21-27`.

    `tests/test-298-voice-log.cjs` - SUBJECT `scripts/check-voice-style.cjs`.
    PENDING: with `process.env.MINDRIAN_HOME` pointed at an `fs.mkdtempSync` directory, a Stop
    envelope whose turn text carries U+2014 produces stdout that parses to an object with
    `continue === true`, exit 0, and exactly one new line in
    `$MINDRIAN_HOME/voice-style.jsonl`; a clean glyph-bearing turn produces `continue === true`,
    exit 0, and zero new lines; a malformed stdin payload still produces `continue === true` and
    exit 0; the emitted envelope contains no `hookSpecificOutput` key; `evaluatePromotion` called
    twice leaves the log file mtime unchanged.

    `tests/test-298-contract-parity.cjs` - SUBJECT `data/harness-policies/contract-parity-larry.json`.
    PENDING: deleting one pinned phrase from `agents/larry-extended.md` inside a try-finally makes
    `node scripts/build-harness-manifest.cjs --check` exit 1 with the surface name and the phrase
    in its output; restoring the file makes it exit 0;
    `Buffer.byteLength(require('./lib/mcp/runtime-instructions.cjs').RUNTIME_INSTRUCTIONS,'utf8')`
    is less than or equal to 1950.

    Commit with an explicit `git add` naming the five files.

    Then record Wave 0 as landed in the validation contract: in
    `.planning/phases/298-.../298-VALIDATION.md` change the frontmatter line
    `wave_0_complete: false` to `wave_0_complete: true` and tick the seven Wave 0 checkboxes
    that this plan delivered (baseline capture, the aggregator, the five stubs; the fixture box
    stays unticked for plan 298-02). Leave `nyquist_compliant` and `status` alone; plan 298-15
    flips those at phase close.
  </action>
  <verify>
    <automated>for t in policies-schema runner-idempotent derive-health voice-log contract-parity; do node tests/test-298-$t.cjs || exit 1; done; bash tests/run-all-298.sh</automated>
  </verify>
  <acceptance_criteria>
    - Each of the five files exists and `node tests/test-298-<name>.cjs` exits 0 at HEAD (state (a): SUBJECT absent, prints a SKIP line naming the missing SUBJECT).
    - Each file declares `const ASSERTIONS_IMPLEMENTED = false;` exactly once: `grep -c "ASSERTIONS_IMPLEMENTED = false" tests/test-298-*.cjs` totals 5.
    - Each file contains a PENDING array with at least three entries drawn from the lists above.
    - `bash tests/run-all-298.sh` exits 0 and its summary line shows `FAIL=0`.
    - `grep -cP '\x{2014}|\x{2013}' tests/test-298-*.cjs` reports 0 for every file.
    - No stub requires a package that is not a Node built-in: `grep -h "require(" tests/test-298-*.cjs | grep -vc "node:\|\.\./\|\./"` returns 0.
    - `grep -c "^wave_0_complete: true" .planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-VALIDATION.md` returns 1, and the Wave 0 list shows seven ticked boxes and one unticked (the fixture).
  </acceptance_criteria>
  <done>Five stubs exist, all exit 0 while their subjects are absent, and each carries the PENDING checklist the owning plan must implement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree -> test process | Test scripts spawn `node` and `bash` against repo-relative paths only |
| developer machine -> `.planning/` | The baseline record is a local working file; it never leaves the machine and carries no room content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-298-05 | Denial of service | `tests/run-all-298.sh` | mitigate | `run_if` guards every leg on a file path, so a partially landed phase SKIPs instead of hanging or failing; no watch-mode flag anywhere |
| T-298-06 | Repudiation | the five stubs | mitigate | The `ASSERTIONS_IMPLEMENTED` three-state contract makes a vacuous pass impossible once a subject lands (T-233-04 false-success class) |
| T-298-02 | Information disclosure | `298-TEST-BASELINE.md` | accept | Local `.planning/` file, gitignored, contains only command names and exit codes; no room content and no secrets |
| T-298-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero external packages (298-RESEARCH.md "Package Legitimacy Audit": not applicable, Node built-ins only). Any plan that adds a dependency is a scope breach, not a vetting problem |
</threat_model>

<verification>
- `git status --porcelain` shows only the six intended new files (the baseline lives under gitignored `.planning/`).
- `bash tests/run-all-298.sh` exits 0.
- Each of the five stubs exits 0 standalone.
- Re-running the 20 baseline commands produces the same exit codes recorded in Task 1.
</verification>

<success_criteria>
- `298-TEST-BASELINE.md` records 20 commands with observed exit codes and names the expected-red set.
- `tests/run-all-298.sh` exists, is executable, uses `run_if` for all 7 legs, and exits 0.
- Five `tests/test-298-*.cjs` stubs exist, each exiting 0 while its SUBJECT is absent and exiting 1 the moment its SUBJECT exists without implemented assertions.
- Zero em-dashes and zero en-dashes in every file this plan creates.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 298 across all plans. The plan-review-convergence source-grounding
pass reads this section to exclude newly created symbols from drift verification.

**New CLI scripts**
- `scripts/run-harness.cjs` - flags `--check`, `--tier <t>`, `--room <dir>`, `--policy <id>`, `--json`, `--help`
- `scripts/check-graph-derive-health.cjs` - flags `--room <dir>`, `--all`, `--json`, `--strict`, `--help`
- `scripts/check-voice-style.cjs` - Stop hook, stdin envelope, no flags

**New library modules and exported functions**
- `lib/hmi/turn-text.cjs` - `readTurnText`, `readTranscriptTurn`, `readTranscriptTail`, `extractAssistantText`, `classifyPrecedingUserContentSource`, `scanContentForAskUserQuestion`, `TRANSCRIPT_TAIL_BYTES`
- `lib/hmi/voice-style-log.cjs` - `appendVoiceStyleRow`, `readVoiceStyleRows`, `evaluatePromotion`, `mindrianHome`, `voiceStyleLogPath`
- `lib/core/doctor/voice-style-log-module.cjs` - `check`, `voiceStyleLogPath`

**New generator symbols in `scripts/build-harness-manifest.cjs`**
- `LARRY_SURFACE_BINDINGS`, `POLICIES_DIR`, `FIXTURE_DIR`, `digestDirectory`, `loadPolicies`, `validatePolicyFile`, `runContractChecks`; `MANIFEST_VERSION` becomes 2

**New manifest JSON keys** in `data/harness-manifest.json`: `policies` `{path, digest, count}`, `larry_surfaces` `[{role, path, digest}]`, `fixture_ref` `{path, digest}`

**New policy schema keys** in `data/harness-policies/_schema.json` under `_doc`: `purpose`, `rung_vocabulary`, `rung_meaning`, `kind_vocabulary`, `applies_to_vocabulary`, `policy_fields`, `runner_path_rule`, `promotion_rule_doc`, `gate_count_principle`, `validation_rule`, `default_on_miss`.
**Per-policy file keys**: `id`, `kind`, `runner`, `args`, `rung`, `evidence_log`, `promotion_rule` `{window_runs, max_false_positive_rate, min_true_positives}`, `owner`, `pinned_by`, `applies_to`, `notes`.

**New data paths**
- `data/harness-policies/` with `_schema.json`, `CONTEXT.md`, and 11 policy files: `gate-worktree-hygiene.json`, `gate-shape-declaration.json`, `gate-tool-honesty.json`, `gate-card-fire.json`, `gate-graph-derive-health.json`, `voice-hyphens-only.json`, `voice-glyph-present.json`, `voice-backend-noun-free.json`, `voice-fork-as-card.json`, `memory-write-policy.json`, `contract-parity-larry.json`
- `data/harness-fixtures/converged-room/` (33 committed text files)
- `<room>/.mindrian/harness-run.json` (runtime output, never committed)
- `$MINDRIAN_HOME/voice-style.jsonl` (runtime evidence log, never committed)

**New registry ids**
- doctor module id `voice-style-log` in `data/doctor-modules.json`
- doctor acceptance point id `harness-policies` in `scripts/doctor.cjs` `buildAcceptanceChecklist`
- one `hooks/hooks.json` Stop entry invoking `scripts/check-voice-style.cjs`

**New test files**
- `tests/test-298-policies-schema.cjs`, `tests/test-298-runner-idempotent.cjs`, `tests/test-298-derive-health.cjs`, `tests/test-298-voice-log.cjs`, `tests/test-298-contract-parity.cjs`, `tests/run-all-298.sh`

**New key on the `lib/core/recipe-maps.cjs` `loadManifest()` return**: `policies`

<output>
Create `.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-01-SUMMARY.md` when done, including the observed baseline table so later plans can diff against it without re-running the 20 commands.
</output>
