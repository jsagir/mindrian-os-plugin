---
phase: 298
plan: 13
type: execute
wave: 5
depends_on: [298-05, 298-09, 298-12]
files_modified:
  - scripts/build-harness-manifest.cjs
  - tests/test-298-contract-parity.cjs
autonomous: true
requirements: [R-06]

must_haves:
  truths:
    - "R-06: node scripts/build-harness-manifest.cjs --check fails when any of Larry's three speaking surfaces stops carrying a core-contract phrase, naming the surface and the phrase"
    - "R-06: --check fails when the Desktop wire's evaluated constant exceeds its 1,950-byte served budget, measured with Buffer.byteLength on the evaluated constant rather than on the file"
    - "R-06: the nine frozen-phrase tests and the byte-frozen BOUNDARIES paragraph stay intact; --check is a faster feedback path onto the same truths, never a second source of truth"
    - "The contract checks are pure file reads run inline by the generator, so they need no runner and no spawn"
  artifacts:
    - path: "scripts/build-harness-manifest.cjs"
      provides: "The inline contract-* checks inside --check"
      exports: ["runContractChecks"]
    - path: "tests/test-298-contract-parity.cjs"
      provides: "R-06 proof: tamper one phrase, --check goes red naming it; restore, green"
  key_links:
    - from: "scripts/build-harness-manifest.cjs"
      to: "data/harness-policies/contract-parity-larry.json"
      via: "runContractChecks reading the declared phrase set and byte budget"
      pattern: "contract-parity-larry"
    - from: "scripts/build-harness-manifest.cjs"
      to: "lib/mcp/runtime-instructions.cjs"
      via: "Buffer.byteLength on the evaluated RUNTIME_INSTRUCTIONS constant"
      pattern: "RUNTIME_INSTRUCTIONS"
---

<objective>
Make `--check` enforce the Larry contract: every declared phrase present in every declared
surface, and the Desktop wire within its byte budget.

Purpose: R-06 and the first of the design's four success truths. The three surfaces Larry speaks
through cannot drift apart silently once a single command fails on a dropped phrase.

Output: the inline contract checks in the generator and a green
`tests/test-298-contract-parity.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-SPEC.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-PATTERNS.md
@docs/superpowers/specs/2026-09-07-harness-manifest-v2-design.md
@lib/mcp/no-instructions.test.cjs
</context>

<locked_constraints>
- Never `git stash`. Explicit `git add <paths>` only. No em-dashes or en-dashes.
- `contract-parity-larry.json` is the single declaration; the generator READS it and hardcodes no phrase of its own.
- Declare and check ONLY phrases a test already pins, plus the two genuine unpinned beta.27 gaps. Adding a phrase no test pins manufactures the second source of truth the design forbids.
- The byte budget is measured with `Buffer.byteLength` on the EVALUATED constant, exactly as `lib/mcp/no-instructions.test.cjs` does at its `SERVED_BUDGET_BYTES` assertion. Never measure the file size.
- Do not modify `lib/mcp/runtime-instructions.cjs`, `agents/larry-extended.md` or `skills/larry-personality/SKILL.md` in this plan. The tamper case restores them in a `finally`.
- Do not declare `lib/mcp/larry-server-instructions.md`. It is an orphan loaded by nothing at runtime; declaring it would legitimize it.
</locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Run the contract-* policies inline inside --check</name>
  <files>scripts/build-harness-manifest.cjs</files>
  <read_first>
    - `scripts/build-harness-manifest.cjs` `runCheck()` and `validateManifest()` as left by plan 298-12, including the three-array finding taxonomy and the `RECOVERY` string
    - `data/harness-policies/contract-parity-larry.json` as landed by plan 298-09 (the `surfaces` array with per-surface `phrases`, and the `byte_budget` object with `path`, `constant` and `limit`)
    - `lib/mcp/no-instructions.test.cjs` lines 85-100 (`SERVED_BUDGET_BYTES` is 1950 and `PART8_BOUNDARIES_FROZEN` is a byte-identical copy of the whole paragraph) and lines 235-270 (the byte assertion and the two frozen assertions: the served string contains the paragraph and ends with it)
    - `.planning/phases/298-.../298-RESEARCH.md` section "The core-contract phrase set, per surface, with the test that already pins each one" and section "The byte budget, measured now"
  </read_first>
  <action>
    Add `runContractChecks(rootDir)` to `scripts/build-harness-manifest.cjs`. It loads every
    policy whose `kind` is `contract` from `data/harness-policies/`, and for each:

    - For every entry in the policy's `surfaces` array, read the surface file's bytes and assert
      each declared phrase is present as a literal substring. A missing phrase produces one
      finding naming the surface role, the surface path, and the exact phrase that is missing.
      Read the file bytes directly; do not require the module.
    - For the `byte_budget` object, require the named module, read the named exported constant,
      compute `Buffer.byteLength(value, 'utf8')`, and compare against the declared limit. Exceeding
      it produces one finding naming the constant, the measured byte count and the limit. Measure
      the evaluated constant, never the file size, because the file contains comments and code the
      served string does not.

    Wire the findings into `runCheck()` as a distinct finding class beside `stale`, `unresolved`
    and `malformed`, keeping the three existing arrays intact so no existing test that reads the
    return shape breaks; add the contract findings as a fourth array and include them in the
    non-zero exit decision and in the printed report. Every finding line names the surface and the
    phrase or the constant and the byte counts, so the failure is actionable without reading the
    policy.

    These are pure file reads. Do not spawn anything, do not add a runner to the contract policies,
    and do not make the runner responsible for them.

    Update the generator's header comment to record that `--check` now also runs the `contract-*`
    policies inline, and why: they are pure file reads and a faster feedback path onto truths the
    nine frozen-phrase tests already pin.
  </action>
  <verify>
    <automated>node scripts/build-harness-manifest.cjs --check; echo "check-exit=$?"; node -e "const g=require('./scripts/build-harness-manifest.cjs'); if(typeof g.runContractChecks!=='function') throw new Error('runContractChecks not exported'); const f=g.runContractChecks(process.cwd()); if(!Array.isArray(f)) throw new Error('expected an array of findings'); if(f.length!==0) throw new Error('unexpected findings at HEAD: '+JSON.stringify(f)); console.log('contract checks clean');"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-harness-manifest.cjs --check` exits 0 on a clean tree.
    - `runContractChecks` is exported and returns an empty array on a clean tree.
    - Temporarily removing one declared phrase from `agents/larry-extended.md` makes `--check` exit non-zero and its output names both the surface path and the missing phrase; restoring the file returns it to exit 0.
    - Temporarily lowering the declared `byte_budget.limit` below the measured value makes `--check` exit non-zero and its output names the constant, the measured bytes and the limit; restoring returns exit 0.
    - The byte measurement uses `Buffer.byteLength` on the required constant: `grep -c "Buffer.byteLength" scripts/build-harness-manifest.cjs` returns at least 1 and `grep -c "statSync.*runtime-instructions" scripts/build-harness-manifest.cjs` returns 0.
    - No phrase is hardcoded in the generator: `grep -c "Operating the machinery\|ZERO user-content egress" scripts/build-harness-manifest.cjs` returns 0.
    - `node lib/mcp/no-instructions.test.cjs` reports 9 of 9.
    - `grep -cP '\x{2014}|\x{2013}' scripts/build-harness-manifest.cjs` returns 0.
  </acceptance_criteria>
  <done>`--check` reads the declared contract policy and fails on a dropped phrase or a busted byte budget, naming exactly what broke.</done>
</task>

<task type="auto">
  <name>Task 2: Turn tests/test-298-contract-parity.cjs green</name>
  <files>tests/test-298-contract-parity.cjs</files>
  <read_first>
    - `tests/test-298-contract-parity.cjs` (the stub from plan 298-01 and its PENDING list)
    - `tests/test-201-harness-manifest.cjs` Task 2 (the tamper, spawn `--check`, assert non-zero and a named finding, restore, assert green dance; copy the try-finally exactly)
    - `data/harness-policies/contract-parity-larry.json` (pick the tampered phrase from its declared set, read at run time rather than hardcoded in the test)
    - `.planning/phases/298-.../298-VALIDATION.md` rows T-298-12, T-298-13 and T-298-14
  </read_first>
  <action>
    Flip `ASSERTIONS_IMPLEMENTED` to `true` and implement the PENDING list.

    Tamper and restore, in a try-finally that restores from an in-memory copy of the original
    bytes taken before the edit. Never use `git stash` and never use `git checkout` to restore:
    read the original bytes with `fs.readFileSync`, write the tampered version, run the check, and
    write the original bytes back in the `finally`. Assert in the `finally` that the restored file
    is byte-identical to the original.

    Cases:
    - Pick the first declared phrase for `agents/larry-extended.md` from
      `contract-parity-larry.json` at run time. Remove that exact substring from the file, spawn
      `node scripts/build-harness-manifest.cjs --check` with `spawnSync`, assert the exit status
      is non-zero and the combined stdout and stderr contains both the surface path and the
      phrase. Restore, re-run, assert exit 0.
    - Repeat once for a phrase declared on `skills/larry-personality/SKILL.md`, so the check is
      proven on more than one surface.
    - Byte budget: assert
      `Buffer.byteLength(require('./lib/mcp/runtime-instructions.cjs').RUNTIME_INSTRUCTIONS, 'utf8')`
      is less than or equal to the `byte_budget.limit` declared in the policy, and that the limit
      is 1950. Do not tamper with the wire file; tamper with a copy of the policy limit instead if
      a red case is wanted, and restore it the same way.
    - Assert `node lib/mcp/no-instructions.test.cjs` exits 0, so the generator's byte check and
      the shipped test agree on the same number.
    - Assert `git status --porcelain` is empty at the end of the test, proving every tamper was
      restored.

    Use the `record` counter and exit 1 if `failCount` is non-zero.

    Commit with an explicit `git add scripts/build-harness-manifest.cjs tests/test-298-contract-parity.cjs`.
  </action>
  <verify>
    <automated>node tests/test-298-contract-parity.cjs; echo "test-exit=$?"; git status --porcelain; node scripts/build-harness-manifest.cjs --check; bash tests/run-all-298.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-298-contract-parity.cjs` exits 0 with at least five `ok` lines.
    - `grep -c "ASSERTIONS_IMPLEMENTED = true" tests/test-298-contract-parity.cjs` returns 1.
    - `git status --porcelain` is empty after the test run, on both tampered surfaces and the policy file.
    - `grep -c "git stash\|git checkout" tests/test-298-contract-parity.cjs` returns 0 (restoration is from in-memory bytes only).
    - The tampered phrase is read from `contract-parity-larry.json` at run time: `grep -c "contract-parity-larry" tests/test-298-contract-parity.cjs` returns at least 1.
    - `node lib/mcp/no-instructions.test.cjs` reports 9 of 9 after the test run.
    - `bash tests/run-all-298.sh` exits 0 with the contract-parity leg PASSED.
    - `grep -cP '\x{2014}|\x{2013}' tests/test-298-contract-parity.cjs` returns 0.
  </acceptance_criteria>
  <done>The parity test proves a dropped phrase turns `--check` red on two surfaces and that every tamper is restored byte-for-byte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declared phrase set -> the three Larry surfaces | A declaration now gates a build |
| the test's tamper step -> the working tree | A test that fails mid-tamper could leave a persona surface damaged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-298-13 | Tampering | the three Larry surfaces | mitigate | The tamper runs inside a try-finally that restores from in-memory bytes taken before the edit, never with `git stash` (a popped foreign stash cost a recovery on 2026-09-07) and never with `git checkout`. The `finally` asserts byte-identical restoration and the test ends by asserting an empty `git status --porcelain` |
| T-298-12 | Denial of service | the Desktop wire byte budget | mitigate | The budget is measured on the evaluated constant, matching `no-instructions.test.cjs`, so the generator and the shipped test can never disagree about the number; the test asserts both |
| T-298-27 | Repudiation | a second source of truth | mitigate | The generator hardcodes no phrase; a grep for two of the pinned phrases in the generator source must return 0. The policy is the single declaration and it declares only phrases a test already pins plus the two named beta.27 gaps |
| T-298-02 | Information disclosure | contract findings | accept | Findings name a surface path and a contract phrase, both of which are repo content, not user content. Zero network |
| T-298-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase; Node built-ins only |
</threat_model>

<verification>
- `node scripts/build-harness-manifest.cjs --check` exits 0 clean and non-zero on a dropped phrase, naming it.
- `node tests/test-298-contract-parity.cjs` exits 0 and leaves an empty porcelain status.
- `node lib/mcp/no-instructions.test.cjs` is 9 of 9.
- `bash tests/run-all-298.sh` exits 0.
</verification>

<success_criteria>
- R-06 delivered: the first of the design's four success truths is live, and one command catches a silent persona drift.
- The check adds no second source of truth: it re-declares what tests already pin, plus the two named unpinned gaps.
- Every tamper is restored without touching git state.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 298 across all plans. The plan-review-convergence source-grounding
pass reads this section to exclude newly created symbols from drift verification.

**New CLI scripts**
- `scripts/run-harness.cjs` - flags `--check`, `--tier <t>`, `--room <dir>`, `--policy <id>`, `--json`, `--help`; exported `loadPolicies`, `classifyPolicy`, `countEntries`, `scanRoom`, `main`
- `scripts/check-graph-derive-health.cjs` - flags `--room <dir>`, `--all`, `--json`, `--strict`, `--check`, `--help`
- `scripts/check-voice-style.cjs` - Stop hook, stdin envelope, no flags

**New library modules and exported functions**
- `lib/hmi/turn-text.cjs` - `readTurnText`, `readTranscriptTurn`, `readTranscriptTail`, `extractAssistantText`, `classifyPrecedingUserContentSource`, `scanContentForAskUserQuestion`, `TRANSCRIPT_TAIL_BYTES`
- `lib/hmi/voice-style-log.cjs` - `appendVoiceStyleRow`, `readVoiceStyleRows`, `evaluatePromotion`, `mindrianHome`, `voiceStyleLogPath`
- `lib/core/doctor/voice-style-log-module.cjs` - `check`, `voiceStyleLogPath`

**New generator symbols in `scripts/build-harness-manifest.cjs`**
- `LARRY_SURFACE_BINDINGS`, `POLICIES_DIR`, `FIXTURE_DIR`, `digestDirectory`, `loadPolicies`, `validatePolicyFile`, `runContractChecks`; `MANIFEST_VERSION` becomes 2

**New manifest JSON keys** in `data/harness-manifest.json`: `policies` with `path`, `digest`, `count`; `larry_surfaces` as an array of `role`, `path`, `digest`; `fixture_ref` with `path`, `digest`

**New policy schema keys** in `data/harness-policies/_schema.json` under `_doc`: `purpose`, `rung_vocabulary`, `rung_meaning`, `kind_vocabulary`, `applies_to_vocabulary`, `policy_fields`, `runner_path_rule`, `promotion_rule_doc`, `gate_count_principle`, `validation_rule`, `default_on_miss`.
**Per-policy file keys**: `id`, `kind`, `runner`, `args`, `rung`, `evidence_log`, `promotion_rule` with `window_runs`, `max_false_positive_rate`, `min_true_positives`; `owner`, `pinned_by`, `applies_to`, `notes`.
**`contract-parity-larry` extra keys**: `surfaces` (each with `role`, `path`, `phrases`) and `byte_budget` (with `path`, `constant`, `limit`).

**New data paths**
- `data/harness-policies/` with `_schema.json`, `CONTEXT.md`, and 11 policy files
- `data/harness-fixtures/converged-room/` (33 committed text files)
- `<room>/.mindrian/harness-run.json` (runtime output, never committed)
- `$MINDRIAN_HOME/voice-style.jsonl` and its rotation `voice-style.jsonl.1` (runtime, never committed)

**New registry ids**
- doctor module id `voice-style-log` in `data/doctor-modules.json`
- doctor acceptance point id `harness-policies` in `scripts/doctor.cjs` `buildAcceptanceChecklist`
- one `hooks/hooks.json` Stop entry invoking `scripts/check-voice-style.cjs`

**New test files**
- `tests/test-298-policies-schema.cjs`, `tests/test-298-runner-idempotent.cjs`, `tests/test-298-derive-health.cjs`, `tests/test-298-voice-log.cjs`, `tests/test-298-contract-parity.cjs`, `tests/run-all-298.sh`

**New key on the `lib/core/recipe-maps.cjs` `loadManifest()` return**: `policies`

<output>
Create `.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-13-SUMMARY.md` when done, recording the finding format for a dropped phrase, the measured byte reading, and confirmation that the generator hardcodes no phrase.
</output>
