---
phase: 298
plan: 04
type: execute
wave: 1
depends_on: [298-01]
files_modified:
  - lib/core/navigation/governance.cjs
  - lib/core/memory/governance-candidate-raiser.cjs
  - tests/test-189-governance-candidates.cjs
  - tests/test-189-f8-governance-gate.cjs
autonomous: true
requirements: [R-07]

must_haves:
  truths:
    - "D-01: a navigator confirming a truth claim reads the claim text as the row label, and the row description carries knowledge_type to target_section, the confidence, and the source_path"
    - "D-01: no new scalar, no new shape and no new edge is minted; the 0.70 pre-check and MAX_TOGGLE_N of 4 stay exactly as shipped"
    - "D-01: the description actually reaches the rendered envelope, because renderGovernanceBasket folds contract.superset_options itself rather than relying on renderShapeF8, which has no description channel (Pitfall 9)"
    - "D-01a: the two stale Part 8 comments in governance-candidate-raiser.cjs (lines 8-9 and line 18) are corrected; Part 8 fences Brain egress and the card renders locally with zero Brain tokens"
    - "findGovernanceCandidates still never throws and still returns an empty array on any query failure"
  artifacts:
    - path: "lib/core/navigation/governance.cjs"
      provides: "The proposed truth-claim candidate SELECT, now carrying n.properties"
      contains: "n.properties"
    - path: "lib/core/memory/governance-candidate-raiser.cjs"
      provides: "Readable F.8 basket rows plus the superset_options fold"
      contains: "superset_options"
  key_links:
    - from: "lib/core/memory/governance-candidate-raiser.cjs"
      to: "rendered.contract.superset_options"
      via: "an explicit fold mirroring lib/mcp/gate-render.cjs:353-363"
      pattern: "contract\\.superset_options"
    - from: "lib/core/navigation/governance.cjs"
      to: "the claim text and knowledge_type inside the node properties blob"
      via: "JSON.parse of n.properties inside a per-row try/catch"
      pattern: "JSON\\.parse\\(.*properties"
---

<objective>
Make the F.8 governance basket readable (D-01) and correct the two stale Part 8 comments that
justify the current unreadable form (D-01a).

Purpose: Canon Part 9 says only a human confirms a truth claim. A basket whose rows are opaque
structural handles turns that confirmation into a rubber stamp. D-01 fixes it inside the shipped
superset card with no new shape, scalar or edge.

Output: two source files changed plus assertions added to the two existing Phase 189 tests that
already own this surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-CONTEXT.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-ADVISOR-basket-card.md
@.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-PATTERNS.md
@lib/mcp/gate-render.cjs
@lib/hmi/shape-f8-renderer.cjs
</context>

<locked_constraints>
- Never `git stash`. Explicit `git add <paths>` only. No em-dashes or en-dashes.
- No new HITL shape, no new scalar, no new edge type. `PRE_CHECK_THRESHOLD` (0.70) and `MAX_TOGGLE_N` (4) are frozen and re-exported, never re-minted.
- `findGovernanceCandidates` must keep its contract: NEVER throws, returns an empty array on any query failure.
- Canon Part 8: the card renders locally. No claim text, no node body and no candidate description may enter any `brain_*` call. Assert that in the tests.
- Canon Part 12: Larry stays silent after a basket confirm; the post-confirm path re-enters `decide()`. Do not add any narration string in this plan.
</locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Carry n.properties through findGovernanceCandidates</name>
  <files>lib/core/navigation/governance.cjs, tests/test-189-governance-candidates.cjs</files>
  <read_first>
    - `lib/core/navigation/governance.cjs` lines 1-80 in full: the guard at line 44, the SELECT at 56-66, the projection at 68-74, the outer `catch` that returns an empty array
    - `lib/core/navigation/typed-claim.cjs` lines 45-95: `KNOWLEDGE_TYPES` (the closed 6-member set at 53-55), `KNOWLEDGE_TYPE_TO_EPISTEMIC_TYPE` at 73-80, `PROTECTED_CLAIM_KEYS` at 83-90 (this is where `writeClaimNode` stores `knowledge_type` and `text` inside the properties blob)
    - `lib/core/node-insert.cjs` lines 55-70 (the `properties` column is a JSON blob on both schemas)
    - `tests/test-189-governance-candidates.cjs` in full: the `node:sqlite` SKIP guard at 21-27 and the four existing cases (a) to (d)
    - `.planning/phases/298-.../298-PATTERNS.md` section `lib/core/navigation/governance.cjs - findGovernanceCandidates (lines 53-76)`
  </read_first>
  <action>
    Add `n.properties` to the SELECT column list in `findGovernanceCandidates` and to the row
    projection. Each projected candidate gains three fields derived from the blob: `claim_text`
    (the `text` key), `knowledge_type` (the `knowledge_type` key), and keep the existing
    `candidate_id`, `kind`, `confidence`, `source_path` untouched and in their current order.

    The `JSON.parse` of `row.properties` must be wrapped in its own try/catch INSIDE the map, so a
    single malformed blob yields `claim_text: null` and `knowledge_type: null` for that row rather
    than dropping the whole result set. The outer catch that returns an empty array stays exactly
    as it is.

    Do not filter on `knowledge_type`. Do not change the `WHERE` clause, the type placeholders,
    the `DECIDED_EDGE_TYPES` NOT EXISTS subquery, or the `memory_event` carve-out. The candidate
    set is unchanged; only the columns carried are wider.

    Extend `tests/test-189-governance-candidates.cjs` with two new cases against the existing
    throwaway `node:sqlite` database it already builds. This also discharges D-03's ask for a
    throwaway-db unit test of the proposed-node SQL path: that harness already exists here, so
    reuse it rather than building a second one (Canon Part 7).
    Case (e): a proposed claim node whose `properties` blob carries `text` and `knowledge_type`
    surfaces with `claim_text` and `knowledge_type` populated, and `knowledge_type` is a member of
    the closed `KNOWLEDGE_TYPES` set imported from `lib/core/navigation/typed-claim.cjs`.
    Case (f): a proposed claim node whose `properties` column holds a malformed string still
    surfaces, with `claim_text` null and `knowledge_type` null, and the call does not throw.

    Commit with an explicit `git add lib/core/navigation/governance.cjs tests/test-189-governance-candidates.cjs`.
  </action>
  <verify>
    <automated>node tests/test-189-governance-candidates.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-189-governance-candidates.cjs` exits 0 and its output names the two new cases (e) and (f).
    - `grep -c "n.properties" lib/core/navigation/governance.cjs` returns at least 1.
    - `grep -c "JSON.parse" lib/core/navigation/governance.cjs` returns at least 1 and that parse sits inside a `try` block within the row map.
    - The projection still emits `candidate_id`, `kind`, `confidence`, `source_path` with their existing names and semantics.
    - A malformed properties blob produces `claim_text: null` without throwing (case (f) proves it).
    - `grep -cP '\x{2014}|\x{2013}' lib/core/navigation/governance.cjs` returns 0.
  </acceptance_criteria>
  <done>The candidate projection carries the claim text and the DIKW kind, degrading to nulls on a malformed blob, with two new cases pinning both paths.</done>
</task>

<task type="auto">
  <name>Task 2: Render readable rows and fold superset_options in the raiser, and correct the D-01a comments</name>
  <files>lib/core/memory/governance-candidate-raiser.cjs</files>
  <read_first>
    - `lib/core/memory/governance-candidate-raiser.cjs` in full, especially the header at lines 1-30 (the stale Part 8 premise at 8-9 and the stale comment at 18), the `PRE_CHECK_THRESHOLD` re-export at line 29, the `_enrich` and `target_section` fill at 42-56, and `renderGovernanceBasket` at 69-95
    - `lib/hmi/shape-f8-renderer.cjs` lines 36-110: `MAX_TOGGLE_N` at 40 (with its explicit MUST NOT equal 3 comment), `PRE_CHECK_THRESHOLD` at 42-44, `_normalizeOption` at 56-67 (it accepts only a string or a label/confidence pair and returns only those two fields), and the paging behavior at 88-95
    - `lib/mcp/gate-render.cjs` lines 60-120 (`SUPERSET_SCHEMA` option fields `id`, `label`, `description`, `rank`, `preview`; `_slug()` at 108) and lines 346-377 (the `base.contract.superset_options` fold at 353-363 and the text fallback at 401)
    - `lib/core/memory/governance-candidate.cjs` (`validateCandidate`, so the malformed-drop path is preserved)
    - `.planning/phases/298-.../298-PATTERNS.md` section `lib/core/memory/governance-candidate-raiser.cjs - renderGovernanceBasket (69-95)` and `.planning/phases/298-.../298-RESEARCH.md` Pitfall 9
    - `docs/MINDRIAN-CANON.md` Part 8 and Part 9 (load on demand; the correction text must be accurate)
  </read_first>
  <action>
    In `renderGovernanceBasket`, change the per-candidate option build:
    - `label` becomes the claim text, truncated. Pick one truncation length, define it as a named
      module-level constant `CLAIM_LABEL_MAX` with a one-line comment explaining the choice
      against the elicitation rung's label-only constraint, and truncate with a trailing ellipsis
      written as three ASCII periods, never a Unicode ellipsis. When `claim_text` is null or
      empty, fall back to `cand.candidate_id` so a malformed row still renders a handle.
    - keep `confidence` exactly as it is (the 0.70 pre-check reads it and is display-only).
    - add `description`, composed as the knowledge_type, then a space-hyphen-greater-than-space
      arrow, then the target_section, then `, conf 0.xx, from ` and the source_path. Format the
      confidence to two decimal places. When a part is missing, omit that part rather than
      printing the word null.
    - add `id: cand.candidate_id` so the structural handle survives as the option id and
      `gate-render.cjs`'s `_slug()` never derives a long slug from prose.

    Then, immediately beside the existing `rendered.contract.hitl_shape = HITL_SHAPE` line, set
    `rendered.contract.superset_options` yourself by mapping the option list to
    `{id, label, description, rank, preview}`, mirroring the fold at `lib/mcp/gate-render.cjs`
    lines 353-363. This is mandatory: `renderShapeF8` is called directly here and never passes
    through `gate-render.cjs`, and its `_normalizeOption` returns only label and confidence, so
    without this fold the description is silently dropped (Pitfall 9). Set `rank` from the array
    index and leave `preview` undefined unless a preview already exists on the candidate.

    Do not change `MAX_TOGGLE_N`, the paging behavior past four candidates, the
    `PRE_CHECK_THRESHOLD` re-export, the header default string, or the malformed-candidate drop
    via `validateCandidate`.

    D-01a: correct BOTH stale comment sites. Lines 8-9 currently assert the label is a structural
    id only and that no candidate body ever renders; line 18 repeats it as a Part 8 claim.
    Replace both with an accurate statement: Canon Part 8 fences Brain EGRESS, and this card is
    composed and rendered locally with zero Brain tokens (cite `lib/mcp/gate-render.cjs:39`), so
    the row may carry the claim text; what Part 8 forbids is the claim reaching a `brain_*` call,
    which nothing in this path does. Add a Canon Part 9 line stating why the row must be readable:
    only a human confirms a truth claim, and a human cannot confirm what they cannot read.

    Commit with an explicit `git add lib/core/memory/governance-candidate-raiser.cjs`.
  </action>
  <verify>
    <automated>node -e "const r=require('./lib/core/memory/governance-candidate-raiser.cjs'); const out=r.renderGovernanceBasket([{candidate_id:'n1',kind:'claim',confidence:0.82,source_path:'02_problem/CONTEXT.md',claim_text:'Cold start is the real bottleneck',knowledge_type:'causal',target_section:'02_problem'},{candidate_id:'n2',kind:'claim',confidence:0.55,source_path:'03_solution/CONTEXT.md',claim_text:'Users will pay for triage',knowledge_type:'assumption',target_section:'03_solution'}],{}); const so=out.contract.superset_options; if(!Array.isArray(so)||so.length!==2) throw new Error('superset_options not folded'); if(!so[0].description||!/conf 0\.\d\d/.test(so[0].description)) throw new Error('description missing or malformed: '+so[0].description); if(so[0].label==='n1') throw new Error('label is still the candidate id'); if(so[0].id!=='n1') throw new Error('candidate id lost'); console.log('readable rows ok:', so[0].label, '|', so[0].description);"</automated>
  </verify>
  <acceptance_criteria>
    - The probe above exits 0 and prints a label that is claim prose and a description matching the pattern kind, arrow, section, `conf 0.` plus two digits, `from ` plus a path.
    - `grep -c "contract.superset_options" lib/core/memory/governance-candidate-raiser.cjs` returns 1.
    - `grep -c "CLAIM_LABEL_MAX" lib/core/memory/governance-candidate-raiser.cjs` returns at least 2 (the definition and the use).
    - `grep -c "structural id only" lib/core/memory/governance-candidate-raiser.cjs` returns 0 (the stale line 18 premise is gone).
    - `grep -c "no candidate body ever renders" lib/core/memory/governance-candidate-raiser.cjs` returns 0 (the stale lines 8-9 premise is gone).
    - `grep -c "Part 9" lib/core/memory/governance-candidate-raiser.cjs` returns at least 1 (the new why-readable rationale).
    - `grep -c "MAX_TOGGLE_N" lib/hmi/shape-f8-renderer.cjs` is unchanged and that file is not in this plan's `files_modified`.
    - `grep -c "brain_" lib/core/memory/governance-candidate-raiser.cjs` returns 0.
    - `grep -cP '\x{2014}|\x{2013}' lib/core/memory/governance-candidate-raiser.cjs` returns 0.
  </acceptance_criteria>
  <done>Rows carry readable claim text and a composed description that reaches the envelope through an explicit superset_options fold, and both stale Part 8 comments are corrected.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the readable rows in the existing F.8 governance gate test</name>
  <files>tests/test-189-f8-governance-gate.cjs</files>
  <read_first>
    - `tests/test-189-f8-governance-gate.cjs` in full, especially line 54 where it calls `raiser.renderGovernanceBasket(renderCandidates, {})` and every assertion that currently reads the option label
    - `.planning/phases/298-.../298-VALIDATION.md` row T-298-16 (the exact description pattern to assert)
    - `lib/hmi/shape-f8-renderer.cjs` lines 88-103 (paging past four candidates and the 0.70 pre-check marking) so the new assertions do not contradict the frozen behavior
  </read_first>
  <action>
    Extend `tests/test-189-f8-governance-gate.cjs` with four assertions, keeping every existing
    assertion intact. Where an existing assertion compares an option label to a candidate id,
    update it to compare `contract.superset_options[i].id` instead, and record that change in the
    test's header comment with the phase number.

    New assertions:
    1. `rendered.contract.superset_options` is an array with one entry per rendered candidate,
       each carrying `id`, `label`, `description` and `rank`.
    2. `rendered.contract.superset_options[0].description` matches the regular expression
       `^\w+ -> .+, conf 0\.\d+, from `.
    3. `rendered.options[0].label` is NOT equal to the candidate id, and equals the truncated
       claim text.
    4. Part 8 assertion: the serialized rendered object contains no substring `brain_` and the
       module under test makes no network call. Assert it by grepping
       `JSON.stringify(rendered)` for `brain_` and by asserting the raiser module's source
       contains no `require` of `lib/core/brain-client.cjs`.
    Also assert the frozen scalars still hold from this call site: with five candidates the
    rendered option count reflects the `MAX_TOGGLE_N` paging behavior rather than a truncation
    to four, and a candidate at confidence 0.82 is pre-checked while one at 0.55 is not.

    Commit with an explicit `git add tests/test-189-f8-governance-gate.cjs`.
  </action>
  <verify>
    <automated>node tests/test-189-f8-governance-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-189-f8-governance-gate.cjs` exits 0.
    - `node tests/test-189-governance-candidates.cjs` still exits 0.
    - `grep -c "superset_options" tests/test-189-f8-governance-gate.cjs` returns at least 3.
    - `bash tests/run-all-189.sh` exits 0, or every leg that fails matches the recorded baseline in `298-TEST-BASELINE.md`.
    - `grep -cP '\x{2014}|\x{2013}' tests/test-189-f8-governance-gate.cjs` returns 0.
  </acceptance_criteria>
  <done>The F.8 gate test pins readable labels, the folded superset_options descriptions, the frozen paging and pre-check scalars, and the Part 8 no-egress property.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room.db node properties -> HMI card | Claim text originating in a user's room now crosses into a rendered card |
| HMI card -> Brain | The Canon Part 8 boundary: nothing from this card may reach a `brain_*` call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-298-02 | Information disclosure | `governance-candidate-raiser.cjs` | mitigate | Rendering is local composition with zero Brain tokens (`lib/mcp/gate-render.cjs:39`). Task 3 asserts the serialized card contains no `brain_` substring and the raiser requires no brain client. The corrected D-01a comments state the actual boundary so a future reader does not re-tighten the wrong rule |
| T-298-09 | Tampering | `findGovernanceCandidates` | mitigate | A malformed `properties` blob is contained by a per-row try/catch; the outer never-throw contract and the empty-array degrade are preserved and pinned by new case (f) |
| T-298-10 | Spoofing | the confirm surface | mitigate | Canon Part 9: only a human confirms. The row is readable precisely so the human is confirming the claim rather than an opaque handle. No auto-confirm path is added |
| T-298-11 | Repudiation | dropped description | mitigate | Pitfall 9: a description that renders nowhere would let the phase claim a readable basket it did not ship. The test asserts on `contract.superset_options[0].description`, not on the label alone |
| T-298-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase; Node built-ins only |
</threat_model>

<verification>
- `node tests/test-189-governance-candidates.cjs` exits 0 with cases (e) and (f) present.
- `node tests/test-189-f8-governance-gate.cjs` exits 0 with the four new assertions.
- `bash tests/run-all-189.sh` matches or improves on the recorded baseline.
- `grep -rn "brain_" lib/core/memory/governance-candidate-raiser.cjs` returns nothing.
</verification>

<success_criteria>
- D-01 delivered: label is the truncated claim text, description carries kind, target section, confidence and source path, and the description reaches the envelope.
- D-01a delivered: both stale Part 8 comment sites corrected with an accurate Part 8 and Part 9 statement.
- No new shape, scalar or edge; `MAX_TOGGLE_N` and the 0.70 pre-check untouched.
- Larry says nothing new; no narration string is added anywhere in this plan.
</success_criteria>

## Artifacts this phase produces

New symbols created by Phase 298 across all plans. The plan-review-convergence source-grounding
pass reads this section to exclude newly created symbols from drift verification.

**New CLI scripts**
- `scripts/run-harness.cjs` - flags `--check`, `--tier <t>`, `--room <dir>`, `--policy <id>`, `--json`, `--help`
- `scripts/check-graph-derive-health.cjs` - flags `--room <dir>`, `--all`, `--json`, `--strict`, `--help`
- `scripts/check-voice-style.cjs` - Stop hook, stdin envelope, no flags

**New library modules and exported functions**
- `lib/hmi/turn-text.cjs` - `readTurnText`, `readTranscriptTurn`, `readTranscriptTail`, `extractAssistantText`, `classifyPrecedingUserContentSource`, `scanContentForAskUserQuestion`, `TRANSCRIPT_TAIL_BYTES`
- `lib/hmi/voice-style-log.cjs` - `appendVoiceStyleRow`, `readVoiceStyleRows`, `evaluatePromotion`, `mindrianHome`, `voiceStyleLogPath`
- `lib/core/doctor/voice-style-log-module.cjs` - `check`, `voiceStyleLogPath`

**New symbols added to existing files by this plan**
- `lib/core/memory/governance-candidate-raiser.cjs` - `CLAIM_LABEL_MAX`
- `lib/core/navigation/governance.cjs` candidate projection fields - `claim_text`, `knowledge_type`

**New generator symbols in `scripts/build-harness-manifest.cjs`**
- `LARRY_SURFACE_BINDINGS`, `POLICIES_DIR`, `FIXTURE_DIR`, `digestDirectory`, `loadPolicies`, `validatePolicyFile`, `runContractChecks`; `MANIFEST_VERSION` becomes 2

**New manifest JSON keys** in `data/harness-manifest.json`: `policies` with `path`, `digest`, `count`; `larry_surfaces` as an array of `role`, `path`, `digest`; `fixture_ref` with `path`, `digest`

**New policy schema keys** in `data/harness-policies/_schema.json` under `_doc`: `purpose`, `rung_vocabulary`, `rung_meaning`, `kind_vocabulary`, `applies_to_vocabulary`, `policy_fields`, `runner_path_rule`, `promotion_rule_doc`, `gate_count_principle`, `validation_rule`, `default_on_miss`.
**Per-policy file keys**: `id`, `kind`, `runner`, `args`, `rung`, `evidence_log`, `promotion_rule` with `window_runs`, `max_false_positive_rate`, `min_true_positives`; `owner`, `pinned_by`, `applies_to`, `notes`.

**New data paths**
- `data/harness-policies/` with `_schema.json`, `CONTEXT.md`, and 11 policy files: `gate-worktree-hygiene.json`, `gate-shape-declaration.json`, `gate-tool-honesty.json`, `gate-card-fire.json`, `gate-graph-derive-health.json`, `voice-hyphens-only.json`, `voice-glyph-present.json`, `voice-backend-noun-free.json`, `voice-fork-as-card.json`, `memory-write-policy.json`, `contract-parity-larry.json`
- `data/harness-fixtures/converged-room/` (33 committed text files)
- `<room>/.mindrian/harness-run.json` (runtime output, never committed)
- `$MINDRIAN_HOME/voice-style.jsonl` (runtime evidence log, never committed)

**New registry ids**
- doctor module id `voice-style-log` in `data/doctor-modules.json`
- doctor acceptance point id `harness-policies` in `scripts/doctor.cjs` `buildAcceptanceChecklist`
- one `hooks/hooks.json` Stop entry invoking `scripts/check-voice-style.cjs`

**New test files**
- `tests/test-298-policies-schema.cjs`, `tests/test-298-runner-idempotent.cjs`, `tests/test-298-derive-health.cjs`, `tests/test-298-voice-log.cjs`, `tests/test-298-contract-parity.cjs`, `tests/run-all-298.sh`

**New key on the `lib/core/recipe-maps.cjs` `loadManifest()` return**: `policies`

<output>
Create `.planning/phases/298-seed-032-harness-as-code-declare-and-machine-enforce-the-min/298-04-SUMMARY.md` when done, recording the chosen `CLAIM_LABEL_MAX` value with its rationale, the exact description format string, and the before/after text of both corrected D-01a comments.
</output>
