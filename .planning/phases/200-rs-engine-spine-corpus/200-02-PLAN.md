---
phase: 200-rs-engine-spine-corpus
plan: 02
type: execute
wave: 2
depends_on: [200-01]
files_modified:
  - lib/core/rs-sqlite-mirror.cjs
  - lib/core/navigation.cjs
  - lib/core/rs-expert-brain-projection.cjs
  - commands/rs-experts.md
  - commands/rs-thesis.md
  - tests/test-200-graph-chokepoint.cjs
  - tests/test-200-brain-projection.cjs
autonomous: false
requirements: [SEED-030-item2, D-200-2-b, CANON-Part9, CANON-Part8]
must_haves:
  truths:
    - "RS Tier-0 graph writes go THROUGH lib/core/navigation.cjs, never a direct room.db open (Canon Part 9: navigation is the single SQL chokepoint)"
    - "The RS node/edge schema (RSDiscovery, ReverseSalient, Innovation, Paper, Author, Institution / DISCOVERED, DERIVED_FROM, ENABLES, AUTHORED_BY, AFFILIATED_WITH) is preserved byte-for-byte after the reroute"
    - "The dual-tier parity holds: what Tier-0 SQLite writes still mirrors what Tier-1 Aura writes (same node/edge identities)"
    - "The expert-graph base is LOCAL-only Tier-0 (Author/Paper/Institution live in room.db per Part 8; the shipped rs-experts/rs-thesis local-only path is unchanged)"
    - "NAVIGATOR DECISION (b): Phase 200 ADDS a Brain generic-handle projection as an additive Mode-A reader. It reads the Brain expert-network as GENERIC framework/enum handles ONLY (framework names, enum tiers) - never a person's name, affiliation, or bytes - and it rides the Phase 196 boundary guard"
    - "The projection is fail-closed on Part 8: if answering would require a local person-byte to cross the wire, it returns NOTHING rather than leak. It degrades to the pure Tier-0 result when Brain is absent or the 196 guard rejects the call"
  artifacts:
    - path: "lib/core/rs-sqlite-mirror.cjs"
      provides: "writeDiscovery routed through navigation.cjs (writeNode/writeEdge chokepoint helpers) instead of a direct better-sqlite3 open"
      exports: ["writeDiscovery"]
    - path: "lib/core/rs-expert-brain-projection.cjs"
      provides: "projectExpertHandles(localExpertNode, opts): reads the Brain expert-network as generic framework/enum handles only, riding the Phase 196 guard; returns [] when Brain absent or the guard rejects (fail-closed, degrades to Tier-0)"
      exports: ["projectExpertHandles"]
  key_links:
    - from: "lib/core/rs-sqlite-mirror.cjs"
      to: "lib/core/navigation.cjs"
      via: "require + the closed chokepoint surface (openRoomDbForCaller / writeEdge / memory_event), NOT a fresh Database() handle"
      pattern: "part9-chokepoint"
---

<objective>
Reconcile the RS graph writes onto the Part-9 chokepoint and resolve the expert-graph's
Brain boundary. Today `rs-sqlite-mirror.cjs` opens room.db directly and does
`INSERT OR REPLACE` into nodes/edges - it works, but it bypasses `navigation.cjs`, so RS
writes are invisible to the memory_event stream and the typed-edge invariants the rest of
the system relies on (Canon Part 9: SQL room.db is the local mind, and navigation.cjs is
the ONE door). This plan routes RS through that door without changing the RS schema.

Expert-graph boundary - NAVIGATOR DECISION (b), overriding the earlier D-200-2 recommendation
of local-only ratification: the LOCAL-only Tier-0 stays the base (Author/Paper/Institution
are user artifacts and MUST live only in room.db per Part 8; the shipped rs-experts/rs-thesis
local-only path is unchanged), and Phase 200 ADDITIONALLY lands a Brain generic-handle
projection as a Mode-A reader. The projection reads the Brain expert-network as generic
framework/enum HANDLES only (framework names, enum tiers), never a person's name/affiliation/bytes,
riding the Phase 196 boundary guard. This is the substrate the Phase 203 synthetic-expert reader
consumes; 203 no longer needs to be the Brain reader (knock-on to D-200-3).

This plan is `autonomous: false` because it changes a write path AND lands a Brain-reading
projection whose Part-8 line is load-bearing - it halts at a material gate for navigator approval
before the reroute and the projection land. The D-200-2 boundary question is already resolved to
(b) by the navigator this session; the remaining material gate is the live write-path reroute.
</objective>

## Task 1: Expose the RS write need through the navigation chokepoint

**Files:**
- Modify: `lib/core/navigation.cjs` (confirm the closed surface can express node+edge upserts for RS types; if a typed helper is missing, add it INSIDE the closed surface, not a 14th ad-hoc export)
- Test: `tests/test-200-graph-chokepoint.cjs`

**Interfaces:**
- Consumes: the existing navigation closed surface (`openRoomDbForCaller`, `writeEdge`, memory_event helpers).
- Produces: a documented path for writing an RSDiscovery node + its typed edges through navigation.cjs.

- [x] Step 1: Write a test asserting that after an RS write, `navigation.getNeighborhood` returns the RSDiscovery node and its DISCOVERED/DERIVED_FROM edges, AND a memory_event row exists for the write (proving it went through the chokepoint, not around it).
- [x] Step 2: Run it, verify it fails (RS still writes directly; no memory_event).
- [x] Step 3: If navigation.cjs lacks a node-upsert path for arbitrary typed nodes, add it within the closed surface (preserve the "closed 13-function surface" invariant; document the addition per CONTEXT D-05 convention).
- [x] Step 4: Run the test, verify the neighborhood + memory_event assertions pass against a temp room.db.
- [x] Step 5: Commit `feat(200-02): navigation.cjs path for RS typed node+edge writes`.

## Task 2: Reroute rs-sqlite-mirror through the chokepoint

**Files:**
- Modify: `lib/core/rs-sqlite-mirror.cjs` (`writeDiscovery` uses navigation.cjs instead of `new Database()`)
- Test: `tests/test-200-graph-chokepoint.cjs`

**Interfaces:**
- Consumes: the navigation path from Task 1.
- Produces: `writeDiscovery(pair, opts)` with identical external behavior (same nodes/edges land) but through the chokepoint.

- [x] Step 1: Write a parity test: run `writeDiscovery` on a fixture pair, assert the exact node ids/types and edge triples match the pre-reroute golden (captured from the current direct-write path as a fixture), AND that no direct `Database()` open occurs (spy/guard on the module).
- [x] Step 2: Run it, verify it fails.
- [x] Step 3: Replace the direct room.db open in `rs-sqlite-mirror.cjs` with the navigation.cjs path. Keep the schema and idempotent MERGE semantics identical.
- [x] Step 4: Run the parity test, verify pass. Run the existing RS mirror tests to confirm no regression.
- [x] Step 5: Commit `fix(200-02): route RS Tier-0 writes through navigation.cjs (Part 9)`.

## Task 3: Confirm the LOCAL-only Tier-0 base (D-200-2 (b), the unchanged half)

**Files:**
- Modify: `commands/rs-experts.md`, `commands/rs-thesis.md` (document that Tier-0 resolution is LOCAL-only; the frontmatter already removed Brain MCP)
- Test: `tests/test-200-graph-chokepoint.cjs`

The navigator resolved D-200-2 to option (b) this session: keep local-only Tier-0 as the base
AND add the Brain generic-handle projection (Task 4). This task locks the base half; Task 4 adds
the projection. No separate D-200-2 material gate is needed here - the decision is made; the
material gate that remains is the write-path reroute (Task 2) plus the projection landing (Task 4),
both covered by this plan's `autonomous: false`.

- [x] Step 1: Write a test asserting rs-experts/rs-thesis resolve Author/Paper/Institution from the LOCAL room.db (Tier 0) with NO Brain call required, and that a missing Brain key degrades cleanly (no throw).
- [x] Step 2: Run it, verify pass/fail; adjust the command bodies to state the local-only base contract explicitly (Part 8: people/paper data is LOCAL and never egresses).
- [x] Step 3: Run the test, verify pass.
- [x] Step 4: Commit `docs(200-02): lock RS expert-graph LOCAL-only Tier-0 base (D-200-2 (b), Part 8)`.

## Task 4: Add the Brain generic-handle projection (D-200-2 (b), the additive half)

**Files:**
- Create: `lib/core/rs-expert-brain-projection.cjs` (`projectExpertHandles(localExpertNode, opts)`)
- Modify: `commands/rs-experts.md` (document the optional Mode-A projection and its Part-8 contract)
- Test: `tests/test-200-brain-projection.cjs`

**Interfaces:**
- Consumes: a LOCAL expert node (from Tier-0 room.db) and the Phase 196 boundary guard (the `classify()` rules-gate + the PreToolUse hook on `mcp__brain_.*`).
- Produces: a list of GENERIC framework/enum handles (framework names, enum tiers) drawn from the Brain expert-network - never person bytes. Returns `[]` when Brain is absent or the 196 guard rejects the call (fail-closed, degrades to the pure Tier-0 result).

**Part-8 contract (load-bearing - this is the whole point of (b)):**
- The projection may send to Brain ONLY generic methodology handles (a domain/framework enum already generic in the room). It MUST NOT send a local person's name, affiliation, or any Author/Institution byte.
- The projection may read from Brain ONLY generic framework/enum handles. If the answer it needs can only be formed by echoing back a specific person's identity, it returns nothing.
- Every Brain call routes through the Phase 196 guard; a guard REJECT is treated as "degrade to Tier-0", not an error.

- [x] Step 1: MATERIAL GATE - the projection reads Brain; confirm the Part-8 contract above with the navigator before landing. (Decision (b) is already made; this gate is the Part-8 wording + the guard wiring, not the go/no-go.)
- [x] Step 2: Write a test asserting: (a) `projectExpertHandles` returns only generic handles (assert no local person name/affiliation string appears in the outbound Brain query - spy on the Brain call), (b) a 196-guard REJECT yields `[]` with no throw, (c) Brain-absent yields `[]` (pure Tier-0 degrade), (d) a synthetic person-byte in the input never appears in the outbound payload.
- [x] Step 3: Run it, verify it fails (module absent).
- [x] Step 4: Implement `rs-expert-brain-projection.cjs` to satisfy the contract, routing every Brain call through the 196 guard.
- [x] Step 5: Run the test, verify pass. Confirm the outbound-payload assertion is a hard fail on any person-byte leak.
- [x] Step 6: Commit `feat(200-02): Brain generic-handle expert projection, Part-8 fail-closed (D-200-2 (b))`.

## Verification

- Run: `node tests/test-200-graph-chokepoint.cjs` -> green.
- Run: `node tests/test-200-brain-projection.cjs` -> green (esp. the no-person-byte-egress assertion).
- Run the existing RS suite (rs-sqlite-mirror + rs-neo4j-writer tests) -> no regression.
- Run the Phase 196 guard suite (`bash tests/run-all-196.sh`) -> still green (the projection must not weaken the guard).
- Confirm `git grep -n "new Database(" lib/core/rs-sqlite-mirror.cjs` returns nothing.

## Self-review

- SEED-030 item 2 ("repoint RS vectors at the embedding layer") is NOT in this plan - it is deferred per D-200-1 (the embedding layer does not exist). This plan does item 3 (chokepoint) + the full expert-graph boundary (b).
- Part 9 satisfied: RS writes now flow through the one door.
- Part 8 satisfied: the LOCAL-only Tier-0 base never egresses; the Brain projection reads generic handles only, rides the 196 guard, and is fail-closed on any person-byte. Absent Brain = clean Tier-0 degrade.
- Part 7 satisfied: the projection reuses the shipped 196 guard and the rs-experts substrate; it adds one reader module, no new engine.
- Knock-on to D-200-3: 203 no longer needs to be the Brain reader (200 lands the projection); 203 becomes purely the synthetic-expert persona layer on top. Not built here.
