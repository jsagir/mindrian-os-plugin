---
phase: 260-pipeline-fixes-brain-repo-one-pass-one-push
plan: 01
type: execute
wave: 1
depends_on: []
target_repo: ProblemsWorthSolving-Brain
files_modified:
  - ProblemsWorthSolving-Brain/scripts/probe-alias-self-loops.mjs
  - ProblemsWorthSolving-Brain/docs/2026-08-20-RCA-alias-self-loop-minting.md
  - ProblemsWorthSolving-Brain/src/ingest/dedup.mjs
  - ProblemsWorthSolving-Brain/tests/ingest-dedup-selfloop.test.mjs
  - ProblemsWorthSolving-Brain/tests/red-proof.sh
autonomous: true
requirements: [FIX-02]

must_haves:
  truths:
    - "resolveFramework's alias branch can never emit a statement capable of minting (a)-[:ALIAS_OF]->(a)"
    - "The alias statement binds exactly ONE canonical node, so a single alias can never fan out N edges across a duplicate-name cluster"
    - "The rename (SET a.name) sits BEHIND the guard, so a self-pair can never rename the canonical node to the alias's name"
    - "The 42214 minting path is reproduced as a fixture and the fixture goes RED when the guard is disabled"
    - "The live ALIAS_OF self-loop population is recorded as a MEASURED number, replacing the stale 41 the source comment asserts"
    - "A canonical candidate carrying no id prop is FLAGGED rather than silently accepted"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-alias-self-loops.mjs"
      provides: "Read-only HTTPS probe measuring the live self-loop population and reproducing the 42214 identity shape"
      min_lines: 80
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-RCA-alias-self-loop-minting.md"
      provides: "The FIX-02 RCA: two minting paths, measured populations, residuals not fixed here"
      contains: "id(a) <> id(canon)"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/dedup.mjs"
      provides: "buildAliasStatements pure builder carrying the statement-level guard"
      contains: "id(a) <> id(canon)"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/ingest-dedup-selfloop.test.mjs"
      provides: "Hermetic guard proof, no graph, no network"
      min_lines: 70
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/red-proof.sh"
      provides: "alias-self-loop registered in the sabotage matrix"
      contains: "alias-self-loop"
  key_links:
    - from: "src/ingest/dedup.mjs resolveFramework alias branch"
      to: "buildAliasStatements"
      via: "the alias branch calls the pure builder instead of inlining the Cypher string"
      pattern: "buildAliasStatements\\("
    - from: "src/ingest/dedup.mjs buildAliasStatements"
      to: "tests/helpers/red-proof.mjs sabotaged"
      via: "sabotage seam returning the pre-fix statement shape"
      pattern: "sabotaged\\('alias-self-loop'\\)"
    - from: "tests/red-proof.sh guard matrix"
      to: "tests/ingest-dedup-selfloop.test.mjs"
      via: "name:test-file row driving CONTRACT_SABOTAGE"
      pattern: "alias-self-loop:tests/ingest-dedup-selfloop.test.mjs"
---

<objective>
Close FIX-02: the dedup write path cannot mint `ALIAS_OF` self-loops. Reproduce the 42214
minting path as a fixture, then kill it with a statement-level `id(a) <> id(canon)` guard,
and record the RCA against MEASURED live numbers rather than the stale figures the source
comments carry.

Purpose: this guard is a hard precondition on other work already sitting on disk.
`payloads/relabel-fix-260820/manifest.json` declares
`blocking_preconditions_NOT_MET.fix_02_self_loop_check` in its own words: 16 new `ALIAS_OF`
edges must not be minted until FIX-02's fixture has been run against them. 260-RESEARCH.md
reaches the same ordering independently ("FIX-02's guard should land before anything authors
alias edges for those 18"). So this plan is wave 1 and nothing that authors an alias edge
runs before it.

Output: `scripts/probe-alias-self-loops.mjs`, `docs/2026-08-20-RCA-alias-self-loop-minting.md`,
a guarded `buildAliasStatements` exported from `src/ingest/dedup.mjs`,
`tests/ingest-dedup-selfloop.test.mjs`, and an `alias-self-loop` row in `tests/red-proof.sh`.

**Measured ground truth this plan is built on (read-only HTTPS probes against canon,
2026-08-20, during planning).** Re-measure in Task 1; do not inherit these as facts, treat
them as the expected shape:

| Probe | Measured | Why it matters |
|---|---|---|
| `MATCH (a)-[r:ALIAS_OF]->(b) WHERE id(a)=id(b) RETURN count(r)` | **165** | The `dedup.mjs` comment says 41 and the v2.1.0 roadmap (CER-05) speaks of "the 42214 self-loop" in the singular. The real population is 4x the comment. |
| node 42214 | name `Nested Hierarchies`, labels `[DictionaryTerm, Tool, Concept, Framework, Person, Product]`, `n.id = 'Nested Hierarchies'`, ALIAS_OF target `[42214]` | The `id` PROP equals the NAME. That is the identity collision that mints the loop. |
| `MATCH (f:Framework) RETURN count(f), sum(CASE WHEN f.id IS NULL THEN 1 ELSE 0 END)` | **186 total, 103 with no id** | The comment says 99 of 181. A null `canon.id` makes the JS `sameId` check unconditionally false, which is what routes an identical node into the alias branch. |
| guard clause-chain, read-only rehearsal, non-self pair | `pairs: 1` | `WITH a, canon WHERE id(a) <> id(canon)` parses and passes a legitimate pair on this Memgraph build. |
| guard clause-chain, read-only rehearsal, self pair (`canon.name='Nested Hierarchies'`, `a.id='Nested Hierarchies'`) | `pairs: 0` | The guard filters the exact 42214 shape to zero. The fix is dialect-valid and behaviourally correct on canon. |

**Canon Part 8 check, stated not assumed:** every artifact here is Brain-repo maintenance code
and Brain-graph structural metadata. No LOCAL room data is read, written, or referenced. The
probe is read-tier and reads only node ids, labels, names and `ALIAS_OF` edges of the Brain's
own methodology corpus. Part-8-safe by construction.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/260-pipeline-fixes-brain-repo-one-pass-one-push/260-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<cross_repo_contract>
**Read this before running any command in this plan.**

| Concern | Repo | Absolute path |
|---|---|---|
| This PLAN.md and all GSD tracking | MindrianOS-Plugin | `/home/jsagi/dev/MindrianOS-Plugin/.planning/` (gitignored, `git add -f`) |
| Every file this plan creates or edits | ProblemsWorthSolving-Brain | `/home/jsagi/dev/ProblemsWorthSolving-Brain/` (normal tracked repo, plain `git add`) |

Every `git add` / `git commit` in this plan runs with
`/home/jsagi/dev/ProblemsWorthSolving-Brain` as the working directory. Nothing in this plan
commits to MindrianOS-Plugin. Both repos forbid em-dashes; hyphens only.

**PUSH FREEZE, FIX-04, NON-NEGOTIABLE.** `render.yaml` autodeploys `pws-brain-mcp` on every
commit to `main`. FIX-04 requires ALL of this phase's fixes to ship in ONE batched push.
Therefore: **commit locally, never `git push`.** Plan 260-05 owns the single push. Running
`git push` from this plan deploys a half-fixed ingest pipeline to production and breaks
FIX-04. This is a deliberate, time-boxed deviation from the Brain repo's standing
"commit directly to main" convention, scoped to this phase and discharged by 260-05 in the
same session.

**No MCP tools required anywhere in this plan.** Every live measurement runs as
`node scripts/<probe>.mjs` over HTTPS using the read-tier key resolved from
`MINDRIAN_BRAIN_KEY` or `~/.mindrian.env`. Bash only. Safe for subagent dispatch.
</cross_repo_contract>

<tasks>

<task type="auto">
  <name>Task 1: Measure the live self-loop population and write the RCA</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-alias-self-loops.mjs (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-RCA-alias-self-loop-minting.md (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-order-collisions.mjs` lines 1-60 and its `main()`: the established read-tier probe shape. It imports `resolveReadKey, call, q, initHandshake` from `probe-wave-attribution.mjs` rather than minting a second HTTP client (Canon Part 7). Copy that import, the banner-comment idiom, and the `fmtRow` markdown-table helper.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-wave-attribution.mjs` lines 40-100: `resolveReadKey` (env then `~/.mindrian.env`), `call`, `q`, `initHandshake`. Do not reimplement any of them.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/dedup.mjs` lines 79-145 in full: the alias branch, the `sameId` computation, the `canonRows` query with its `LIMIT 1` and no `ORDER BY`, and the 2026-08-07 incident comment block whose numbers this task corrects.
    - `/home/jsagi/dev/MindrianOS-Plugin/docs/RCA-TEMPLATE.md`: the Brain repo has no RCA template of its own, so follow the plugin repo's structure (symptom, root cause, evidence, fix, residuals) in prose form.
    - 260-RESEARCH.md, the "What this means for FIX-02" section and the live-update section's `blocking_preconditions_NOT_MET` paragraph.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/relabel-fix-260820/manifest.json`, the `blocking_preconditions_NOT_MET` block.
  </read_first>

  <action>
**`scripts/probe-alias-self-loops.mjs`.** A read-only probe. Zero writes, no admin key. Open
with the banner idiom from `probe-order-collisions.mjs` (a `scripts/probe-alias-self-loops.mjs`
header line, a `====` rule, a statement that this is FIX-02 pre-flight, READ-ONLY, zero writes,
no admin key, and a `Usage:` line naming the read-tier key resolution). Close the banner with
the literal line `No em-dashes.`.

Import `resolveReadKey, call, q, initHandshake` from `./probe-wave-attribution.mjs`. Do not
write a second fetch client.

Run these probes and print each result as a labelled markdown table:

- **[S-1] Self-loop population.** `MATCH (a)-[r:ALIAS_OF]->(b) WHERE id(a)=id(b) RETURN count(r) AS selfloops`
- **[S-2] Self-loop label census.** `MATCH (a)-[r:ALIAS_OF]->(b) WHERE id(a)=id(b) RETURN labels(a) AS labels, count(*) AS c ORDER BY c DESC LIMIT 15` -- how many are `:Framework`-bearing, which is what decides whether FIX-02's guard alone covers the minting surface or whether a non-Framework minting path also exists.
- **[S-3] Node 42214 identity shape.** `MATCH (n) WHERE id(n)=42214 OPTIONAL MATCH (n)-[r:ALIAS_OF]->(m) RETURN id(n) AS nid, n.name AS name, labels(n) AS labels, n.id AS prop_id, collect(id(m)) AS targets`
- **[S-4] The id-prop-equals-name population.** `MATCH (f:Framework) WHERE f.id IS NOT NULL AND toLower(f.id) = toLower(f.name) RETURN count(f) AS id_equals_name` -- the size of the class 42214 belongs to.
- **[S-5] Null-id framework population.** `MATCH (f:Framework) RETURN count(f) AS total, sum(CASE WHEN f.id IS NULL THEN 1 ELSE 0 END) AS no_id`
- **[S-6] Case-insensitive name clusters inside :Framework.** `MATCH (f:Framework) WHERE f.name IS NOT NULL WITH toLower(trim(f.name)) AS k, count(*) AS c WHERE c > 1 RETURN count(k) AS ci_clusters, sum(c) AS nodes` -- this decides whether the `canonRows ... LIMIT 1` non-deterministic pick can land on a different node than the one carrying the payload id.
- **[S-7] Guard rehearsal, non-self pair, READ-ONLY.**
  `MATCH (canon:Framework) WHERE canon.name = 'Scenario Planning' WITH canon ORDER BY id(canon) LIMIT 1 MATCH (a:Framework) WHERE a.id = 'Nested Hierarchies' WITH a, canon WHERE id(a) <> id(canon) RETURN count(*) AS pairs`
- **[S-8] Guard rehearsal, SELF pair, READ-ONLY.**
  `MATCH (canon:Framework) WHERE canon.name = 'Nested Hierarchies' WITH canon ORDER BY id(canon) LIMIT 1 MATCH (a:Framework) WHERE a.id = 'Nested Hierarchies' WITH a, canon WHERE id(a) <> id(canon) RETURN count(*) AS pairs`
- **[S-9] Fan-out surface.** `MATCH (f:Framework) WHERE f.name IS NOT NULL WITH f.name AS nm, count(*) AS c WHERE c > 1 RETURN count(nm) AS exact_name_groups, sum(c) AS nodes` -- exact-case duplicates are what the unbounded `MATCH (canon:Framework) WHERE canon.name = $canonName` would fan out across.

Print every measured number to stdout AND write the same tables into the RCA doc, so the doc's
numbers are transcribed from a run rather than typed from memory. If any probe errors, print
`[ERR] <label>: <message>` and keep going; a probe failure is a recorded row, never a silent skip.

**`docs/2026-08-20-RCA-alias-self-loop-minting.md`.** Write it AFTER the probe runs, from the
probe's own output. Sections:

1. **Symptom.** `ALIAS_OF` self-loops on the canon graph. State the MEASURED [S-1] count and
   name explicitly that `src/ingest/dedup.mjs` line 93 claims 41 and the v2.1.0 roadmap
   (`CER-05`) speaks of "the 42214 self-loop" in the singular. Both understate the population.
   Say which number is now authoritative and that the correction is measured, not estimated.
   This correction is an INPUT to Phase 261 CER-05, whose cleanup scope is the [S-1] number,
   not one edge.

2. **Why the existing JS check does not stop it.** `resolveFramework` compares
   `fw.id` against `canonRows[0].id`. Three measured reasons that comparison can be false for
   a node that IS the same node:
   - `canon.id` is NULL for [S-5]'s `no_id` of [S-5]'s `total` frameworks. `sameId` requires
     `canon?.id` truthy, so a null-id canonical makes `sameId` false unconditionally.
   - `canonRows` is `MATCH (f:Framework) WHERE toLower(f.name) = toLower($canonName) ... LIMIT 1`
     with NO `ORDER BY`. With [S-6]'s `ci_clusters` case-insensitive clusters live, the row it
     returns is not guaranteed to be the node carrying `fw.id`.
   - Branch (1)'s `MATCH (:Framework {id: $id})` is exact and case-sensitive, so a payload whose
     id differs only in case misses branch (1) entirely and arrives at branch (2).

3. **The minting path.** Once the alias branch is entered, the emitted statement re-resolves
   `canon` INDEPENDENTLY of the JS pick:
   `MATCH (canon:Framework) WHERE canon.name = $canonName MERGE (a:Framework {id: $aliasId}) SET a.name = $aliasName MERGE (a)-[:ALIAS_OF]->(canon)`.
   Two defects in one statement: (i) the `MATCH` is UNBOUNDED, so it binds every exact-name
   node ([S-9]) and mints one edge per row; (ii) nothing prevents the node `MERGE (a ...)`
   resolves to from being one of the `canon` bindings. Walk node 42214 through it concretely
   using [S-3]'s measured shape: `n.id` equals `n.name`, so `MERGE (a:Framework {id: 'Nested Hierarchies'})`
   and `MATCH (canon:Framework) WHERE canon.name = 'Nested Hierarchies'` resolve to the SAME
   node and the MERGE writes `(42214)-[:ALIAS_OF]->(42214)`. [S-4] gives the size of that class.

4. **A third defect the same statement carries.** `SET a.name = $aliasName` executes before any
   identity check, so in the self-pair case the canonical node would be RENAMED to the alias's
   surface form. Record this as part of the same root cause; the fix must place the SET behind
   the guard, not merely the final MERGE.

5. **The fix.** The statement-level `id(a) <> id(canon)` guard plus a deterministic single-canon
   binding. Quote the target statement verbatim (the form in Task 2's behavior block). Record
   [S-7] and [S-8] as the read-only rehearsal proving the clause chain parses on this Memgraph
   build AND filters the self pair to zero.

6. **What this fix does NOT do (residuals, named not hidden).**
   - It does not delete the [S-1] existing self-loops. That is Phase 261 CER-05, and this doc is
     the corrected scope input for it.
   - `MERGE (a:Framework {id: $aliasId})` still cannot match a node that carries that id without
     the `:Framework` label, so it would CREATE a second node with a duplicate id prop. Flag it,
     do not fix it here; it is a distinct defect with a distinct blast radius.
   - The `canonRows ... LIMIT 1` pick stays non-deterministic. FIX-02's guard makes that harmless
     for self-loop purposes; making it deterministic has its own blast radius across FIX-01's
     branch and is deliberately out of scope for this plan.
   - The guard prevents MINTING. It does not make a stranded 2-hop alias chain resolvable; that
     is FIX-03's matrix question (plan 260-02).

7. **Downstream unblock.** State plainly that
   `payloads/relabel-fix-260820/manifest.json`'s `blocking_preconditions_NOT_MET.fix_02_self_loop_check`
   is discharged by this fix landing, and that its OTHER blocking precondition
   (`contested_survivor_picks`, the 2 pairs where `origin/audit/2026-08-20-brain-heal`'s
   `alias-review.md` rules a different survivor) is NOT discharged by this plan and remains open
   for Phase 261.

No em-dashes in either file.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node scripts/probe-alias-self-loops.mjs && grep -q 'id(a) <> id(canon)' docs/2026-08-20-RCA-alias-self-loop-minting.md</automated>
  </verify>

  <acceptance_criteria>
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node scripts/probe-alias-self-loops.mjs` exits 0 and prints a labelled result line for all nine probes S-1 through S-9.
    - The printed `[S-1] selfloops` value is a positive integer and the RCA doc contains that SAME integer as a literal. (Transcribed, not predicted.)
    - The printed `[S-8]` value is `0` and the printed `[S-7]` value is greater than `0`. If S-8 is non-zero the guard form is wrong and this task fails: stop and re-derive before touching Task 2.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c '^| ' docs/2026-08-20-RCA-alias-self-loop-minting.md` outputs a number >= 9 (the measured tables are actually in the doc).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -q 'relabel-fix-260820' docs/2026-08-20-RCA-alias-self-loop-minting.md` exits 0 (section 7 names the downstream unblock).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -q 'CER-05' docs/2026-08-20-RCA-alias-self-loop-minting.md` exits 0 (the corrected cleanup scope reaches its consumer).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -Pn '\x{2014}' scripts/probe-alias-self-loops.mjs docs/2026-08-20-RCA-alias-self-loop-minting.md` exits 1 with no output.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'fetch(' scripts/probe-alias-self-loops.mjs` outputs `0` (no second HTTP client; the shared `call`/`q` seam is used).
    - Commit lands in the Brain repo. `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && git log origin/main..HEAD --oneline | wc -l` is >= 1 (committed, NOT pushed).
  </acceptance_criteria>

  <done>
    The self-loop population is measured live and recorded, the two minting paths and the
    rename defect are documented with node 42214 walked through concretely, the guard form is
    proven to parse and to filter the self pair on canon, and the residuals plus the downstream
    unblock are named. Committed locally, not pushed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard the alias statement (FIX-02) behind a hermetic RED proof</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/ingest-dedup-selfloop.test.mjs (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/dedup.mjs (modify)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/dedup.mjs` lines 79-145 (already read in Task 1; re-read only if the range is not in context).
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/contracts/e5-identity.mjs` lines 1-35: the exact import form for `sabotaged` from the `tests/helpers/red-proof.mjs` seam and the comment idiom explaining WHY a guard consults it. Copy both, adjusting the relative path for `src/ingest/`.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/pipeline.mjs` lines 88-140: the repo's "Pure. Exported for the hermetic test." idiom (`droppedNodePropKeys`, `nullEdgeEndpoints`, `pickMentionsProps`). `buildAliasStatements` follows this idiom exactly.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/ingest-edge-props.test.mjs` first 30 lines: a hermetic ingest test that imports from a module which itself imports a backend. Proof the import is safe with no live graph.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/ingest-dedup.test.mjs` lines 69-99: the existing self-loop incident tests. Your new tests must not duplicate them; theirs are LIVE (`guard = onMemgraph ? test : test.skip`) and skip without a backend, which is exactly why a hermetic sibling is required.
  </read_first>

  <behavior>
    New file `tests/ingest-dedup-selfloop.test.mjs`, flat `test('...', () => {})`, no
    `describe`, hermetic (no graph, no network, no env). It imports `buildAliasStatements`
    from `../src/ingest/dedup.mjs` and asserts on the emitted statement STRING:

    - Test 1 `the alias statement carries the statement-level identity guard`:
      the emitted `stmt` contains the literal substring `id(a) <> id(canon)`.
    - Test 2 `the canonical binding is deterministic and single`:
      the emitted `stmt` contains `ORDER BY id(canon)` and `LIMIT 1`, and the index of
      `LIMIT 1` is greater than the index of `MATCH (canon:Framework)`.
    - Test 3 `the rename sits BEHIND the guard`:
      `indexOf('SET a.name')` is greater than `indexOf('id(a) <> id(canon)')`. A rename in
      front of the guard would let a self-pair rename the canonical node.
    - Test 4 `the ALIAS_OF merge sits BEHIND the guard`:
      `indexOf('MERGE (a)-[:ALIAS_OF]->(canon)')` is greater than `indexOf('id(a) <> id(canon)')`.
    - Test 5 `params carry exactly the three keys the statement interpolates`:
      the returned params object has exactly `canonName`, `aliasId`, `aliasName`, and every
      dollar-prefixed token appearing in the statement is one of those three.
    - Test 6 `RED: the guard is disabled by the sabotage seam`:
      with `process.env.CONTRACT_SABOTAGE = 'alias-self-loop'` and
      `process.env.CONTRACT_SABOTAGE_ALLOW = '1'` set inside the test, `buildAliasStatements`
      returns the PRE-FIX statement shape: it does NOT contain `id(a) <> id(canon)`. Restore
      both env vars in a `finally`. This test asserts the seam works; `tests/red-proof.sh`
      is what proves tests 1-4 actually go red under it.
    - Test 7 `an empty or missing aliasId never produces a statement`:
      `buildAliasStatements({ canonName: 'X', aliasId: undefined, aliasName: 'Y' })` returns
      an empty array. A MERGE key of `undefined` is the driver-throw shape the 2026-08-07
      incident recorded.

    Write the file first and watch tests 1-4 and 7 fail against the current inline string.
  </behavior>

  <action>
Write `tests/ingest-dedup-selfloop.test.mjs` FIRST, run it, confirm it is RED, then edit
`src/ingest/dedup.mjs`.

**Test file.** Header banner in the repo idiom: name the file, state that it is hermetic (no
graph, no network), state the defect it pins (FIX-02, the 42214 minting path, cross-reference
`docs/2026-08-20-RCA-alias-self-loop-minting.md`), and close the banner with `// No em-dashes.`.
Imports: `test` from `node:test`, `assert` from `node:assert/strict`, and
`{ buildAliasStatements }` from `../src/ingest/dedup.mjs`. Write the seven tests named in
the behavior block. Prefix test 6's name with `RED:` per the repo convention.

**`src/ingest/dedup.mjs` edit 1: import the sabotage seam.** Add an import of `sabotaged` from
the `tests/helpers/red-proof.mjs` seam next to the existing imports, resolving the relative
path correctly from `src/ingest/`. Copy the explanatory comment form used at the top of
`src/contracts/e5-identity.mjs`: one short block saying the guard consults the seam so
`tests/red-proof.sh` can prove it is non-vacuous, and that nothing but that script sets the
env pair.

**`src/ingest/dedup.mjs` edit 2: add the pure builder.** Insert `buildAliasStatements` as a
module-level exported function ABOVE `resolveFramework`, with the repo's
"Pure. Exported for the hermetic test." comment idiom plus a short paragraph naming the two
minting paths from the RCA. Signature:
`export function buildAliasStatements({ canonName, aliasId, aliasName })`.

Behaviour:
- If `canonName` or `aliasId` is undefined, null, or a blank string, return `[]`. No MERGE key,
  no statement. (This mirrors the existing `!fw.id` reasoning already in the file: a MERGE on
  `undefined` is a driver throw, not a write.)
- If `sabotaged('alias-self-loop')` returns true, return the PRE-FIX statement verbatim (the
  current inline string at lines 137-143) so the RED proof has something to fail against.
- Otherwise return a single-element array whose `stmt` is exactly this clause chain, newline
  separated, in this order:

  line 1: `MATCH (canon:Framework) WHERE canon.name = $canonName`
  line 2: `WITH canon ORDER BY id(canon) LIMIT 1`
  line 3: `MERGE (a:Framework {id: $aliasId})`
  line 4: `WITH a, canon WHERE id(a) <> id(canon)`
  line 5: `SET a.name = $aliasName`
  line 6: `MERGE (a)-[:ALIAS_OF]->(canon)`

  and whose `params` is `{ canonName, aliasId, aliasName }`.

  Three things about that order are load-bearing and must be commented inline, one short line
  each: line 2 makes the canonical binding deterministic and SINGLE so one alias cannot fan out
  across a duplicate-name cluster; line 4 is the FIX-02 guard, placed after the MERGE because
  `a` does not exist as a binding before it; lines 5 and 6 sit behind the guard so a self-pair
  neither renames the canonical node nor mints the loop.

  Do NOT reorder line 3 in front of line 1. `MERGE` before `MATCH` would create the alias node
  even when no canonical exists, turning a safe zero-row no-op into a write.

**`src/ingest/dedup.mjs` edit 3: call the builder.** Replace the inline statement literal in the
alias branch (currently lines 137-143) with
`const statements = buildAliasStatements({ canonName, aliasId: fw.id, aliasName: fw.name });`.
Immediately after, if `statements.length === 0`, return a `noop` decision carrying a flag
`{ kind: 'alias_not_mintable', candidate: canonName, name: fw.name, reason: ... }` rather than
returning `decision: 'alias'` with zero statements. A decision that claims to alias while
staging nothing is the silent-success shape this repo exists to remove.

**`src/ingest/dedup.mjs` edit 4: flag the null-id canonical.** In the alias branch, when
`canon` exists and `canon.id` is null or undefined, append a flag with
`kind: 'alias_canon_has_no_id'`, `candidate: canonName`, `name: fw.name`, and a `reason`
stating that the canonical candidate carries no id prop, so the JS identity check cannot
confirm or deny sameness and the statement-level `id(a) <> id(canon)` guard is the only control
on this path. The RCA measured this as the majority of live frameworks, so it must be visible
in the plan rather than inferred. Change the alias branch's `flag: []` to carry this array.

**Do NOT** touch the `canonRows` query's `LIMIT 1` in this task. Making that pick deterministic
is a separate change with its own blast radius across FIX-01's branch, and FIX-02's guard makes
the non-determinism harmless for self-loop purposes. It is recorded as a residual in the RCA
(Task 1 section 6) instead.

Run the hermetic test, then run the two ingest suites that import this module to confirm the
edits did not break their import or their skip behaviour.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/ingest-dedup-selfloop.test.mjs && node --test tests/ingest-dedup.test.mjs tests/ingest-admin-gate.test.mjs</automated>
  </verify>

  <acceptance_criteria>
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/ingest-dedup-selfloop.test.mjs` exits 0 with 7 pass, 0 fail, and runs with NO `GRAPH_BACKEND` set (hermetic).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node -e "import('./src/ingest/dedup.mjs').then(m=>{const s=m.buildAliasStatements({canonName:'X',aliasId:'y',aliasName:'Y'});process.exit(s.length===1 && s[0].stmt.includes('id(a) <> id(canon)') ? 0 : 1)})"` exits 0.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node -e "import('./src/ingest/dedup.mjs').then(m=>process.exit(m.buildAliasStatements({canonName:'X',aliasId:undefined,aliasName:'Y'}).length===0?0:1))"` exits 0 (no MERGE key means no statement).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node -e "import('./src/ingest/dedup.mjs').then(m=>{const s=m.buildAliasStatements({canonName:'X',aliasId:'y',aliasName:'Y'})[0].stmt;const g=s.indexOf('id(a) <> id(canon)');process.exit(g>0 && s.indexOf('SET a.name')>g && s.indexOf('MERGE (a)-[:ALIAS_OF]->(canon)')>g ? 0 : 1)})"` exits 0 (both the rename and the merge sit behind the guard).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && CONTRACT_SABOTAGE=alias-self-loop CONTRACT_SABOTAGE_ALLOW=1 node -e "import('./src/ingest/dedup.mjs').then(m=>process.exit(m.buildAliasStatements({canonName:'X',aliasId:'y',aliasName:'Y'})[0].stmt.includes('id(a) <> id(canon)') ? 1 : 0))"` exits 0 (the seam genuinely disables the guard).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -v '^\s*//' src/ingest/dedup.mjs | grep -c "alias_canon_has_no_id"` outputs `1`.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -v '^\s*//' src/ingest/dedup.mjs | grep -c 'MERGE (a)-\[:ALIAS_OF\]->(canon)'` outputs `2` (the guarded form plus the sabotage fallback, and nothing else).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/ingest-dedup.test.mjs tests/ingest-admin-gate.test.mjs` exits 0 (they skip without a backend; a non-zero exit means the edit broke the import, not the assertions).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -Pn '\x{2014}' tests/ingest-dedup-selfloop.test.mjs` exits 1 with no output.
    - Committed locally, NOT pushed: `git log origin/main..HEAD --oneline | wc -l` is >= 2.
  </acceptance_criteria>

  <done>
    `buildAliasStatements` is exported, pure, sabotage-seamed, and emits a statement whose
    canonical binding is deterministic and single and whose rename and ALIAS_OF merge both sit
    behind an `id(a) <> id(canon)` guard. The hermetic suite passes 7/7 with no graph. A null-id
    canonical and an unmintable alias are both flagged rather than silently accepted.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register alias-self-loop in the RED-proof matrix</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/red-proof.sh (modify)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/red-proof.sh` in full: the banner explaining the two-run protocol, the per-guard explanatory comment block (each guard gets a short paragraph naming what sabotage disables and which assertion therefore fails), the `export GRAPH_BACKEND="${GRAPH_BACKEND:-memgraph}"` line and WHY it is there, and the `name:test-file` list plus the loop that consumes it.
  </read_first>

  <action>
Add one row to the guard matrix: `alias-self-loop:tests/ingest-dedup-selfloop.test.mjs`.

Add the matching explanatory comment paragraph in the same style as the existing ones, above
the list. It must say: `CONTRACT_SABOTAGE=alias-self-loop` makes `buildAliasStatements` in
`src/ingest/dedup.mjs` return the PRE-FIX inline statement, so tests 1 through 4 of
`tests/ingest-dedup-selfloop.test.mjs` fail (no `id(a) <> id(canon)`, no deterministic single
canon binding, and both the rename and the ALIAS_OF merge back in front of the guard). Name the
RCA doc as the source. Add one sentence recording that this suite is HERMETIC and therefore,
unlike the `moat` and `e5` rows, does not depend on the script's `GRAPH_BACKEND=memgraph` pin,
so it is the only ingest-side guard in this matrix that is provable on a machine with no Bolt
credential. That fact is load-bearing for this phase: no local Bolt exists.

Do NOT remove or reorder any existing row. Do NOT change the `GRAPH_BACKEND` pin.

Then run the full RED proof for this one guard, both directions, exactly as the script's own
protocol describes: sabotaged must exit NON-ZERO, unsabotaged must exit ZERO.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && bash -c 'CONTRACT_SABOTAGE=alias-self-loop CONTRACT_SABOTAGE_ALLOW=1 node --test tests/ingest-dedup-selfloop.test.mjs >/dev/null 2>&1; s=$?; node --test tests/ingest-dedup-selfloop.test.mjs >/dev/null 2>&1; u=$?; echo "sabotaged=$s unsabotaged=$u"; [ $s -ne 0 ] && [ $u -eq 0 ]'</automated>
  </verify>

  <acceptance_criteria>
    - The verify command prints `sabotaged=<non-zero> unsabotaged=0` and exits 0. A `sabotaged=0` is the vacuous-assertion failure this whole harness exists to catch: stop and fix the test, do not proceed.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'alias-self-loop:tests/ingest-dedup-selfloop.test.mjs' tests/red-proof.sh` outputs `1`.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'moat:tests/ingest-admin-gate.test.mjs' tests/red-proof.sh` outputs `1` and `grep -c 'e5:tests/ingest-embed.test.mjs' tests/red-proof.sh` outputs `1` (no existing row was removed).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'export GRAPH_BACKEND=' tests/red-proof.sh` outputs `1` (the pin is untouched).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -q 'HERMETIC' tests/red-proof.sh` exits 0 (the no-Bolt-credential fact is recorded where the next session reads it).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -Pn '\x{2014}' tests/red-proof.sh` exits 1 with no output for the lines this task added.
    - Committed locally, NOT pushed: `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && git log origin/main..HEAD --oneline | wc -l` is >= 3, and `git status --porcelain` shows no unstaged Brain-repo changes.
  </acceptance_criteria>

  <done>
    `alias-self-loop` is a first-class row in the RED-proof matrix with its own explanatory
    paragraph, the guard is demonstrably able to fail, and the fact that it is the only
    ingest-side guard provable without a Bolt credential is written where the next session
    will read it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest payload -> emitted Cypher statement | A payload-supplied `id` and `name` are interpolated as MERGE keys. The identity decision made in JS is re-made independently by the statement, and the two can disagree. This is the boundary FIX-02 hardens. |
| repo code -> live canon graph | `pws-brain-mcp` autodeploys on push to `main`. Code committed here becomes production ingest behaviour the moment plan 260-05 pushes. |
| read-tier HTTPS probe -> canon | `brain_query` with a read key. Read-only by service contract; this plan issues zero writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260-01 | Tampering | `resolveFramework` alias branch renaming the CANONICAL node via `SET a.name` on a self-pair | mitigate | the `SET` is placed behind the `id(a) <> id(canon)` guard (Task 2 behavior test 3, asserted by index comparison, not by reading) |
| T-260-02 | Tampering | unbounded `MATCH (canon:Framework) WHERE canon.name = $canonName` fanning one alias into N `ALIAS_OF` edges | mitigate | `WITH canon ORDER BY id(canon) LIMIT 1` makes the binding deterministic and single; asserted in Task 2 test 2 and measured in Task 1 [S-9] |
| T-260-03 | Denial of Service | `ALIAS_OF` self-loop noise degrading every alias traversal, including `find_connections` and the FIX-03 reader | mitigate (minting) / transfer (cleanup) | this plan stops NEW loops; deleting the measured [S-1] population is transferred to Phase 261 CER-05, and Task 1 section 1 hands CER-05 the corrected scope so the transfer is explicit rather than assumed |
| T-260-04 | Repudiation | a guard that is present in source but cannot be shown to fail | mitigate | `tests/red-proof.sh` registration plus the `sabotaged('alias-self-loop')` seam; Task 3's verify asserts the sabotaged run exits non-zero |
| T-260-05 | Information Disclosure | user room data reaching the Brain (Canon Part 8) | accept, with a stated control | this plan reads and writes only Brain-owned methodology structure. The probe returns node ids, labels, names and `ALIAS_OF` edges of the Brain's own corpus. There is no LOCAL side to any artifact here. |
| T-260-06 | Elevation of Privilege | premature deploy of half-fixed ingest code to production | mitigate | the push freeze in the cross-repo contract: this plan commits and never pushes; plan 260-05 owns the single batched push behind a navigator checkpoint |
| T-260-SC | Tampering | npm/pip/cargo installs | mitigate | not applicable: zero new packages. `node:test` and `node:assert` are built in; the probe reuses the repo's existing fetch-based `call`/`q` seam. No package-manager install task exists in this plan, so no legitimacy checkpoint is required. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

Every symbol and file below is NEW in Phase 260 or is an existing symbol this phase modifies.
A source-grounding or drift pass must treat these as phase-local, not as upstream drift.

**New files in `/home/jsagi/dev/ProblemsWorthSolving-Brain` (this plan):**

| Path | Created by | What it is |
|---|---|---|
| `scripts/probe-alias-self-loops.mjs` | 260-01 Task 1 | Read-only HTTPS probe, nine measurements S-1..S-9 |
| `docs/2026-08-20-RCA-alias-self-loop-minting.md` | 260-01 Task 1 | The FIX-02 RCA with measured populations and named residuals |
| `tests/ingest-dedup-selfloop.test.mjs` | 260-01 Task 2 | Hermetic 7-test guard proof, no graph, no network |

**New exported symbols:**

| Symbol | Module | Signature |
|---|---|---|
| `buildAliasStatements` | `src/ingest/dedup.mjs` | `({ canonName, aliasId, aliasName })` returns `[{ stmt, params }]` or `[]` |

**New flag kinds emitted by `resolveFramework`:**

| Kind | Meaning |
|---|---|
| `alias_not_mintable` | a name match resolved but no MERGE key exists, so no alias statement was staged |
| `alias_canon_has_no_id` | the canonical candidate carries no `id` prop, so the JS identity check is structurally unable to confirm sameness |

**Modified existing files:** `src/ingest/dedup.mjs` (new import, new pure builder, alias branch
now calls it, two new flags), `tests/red-proof.sh` (one new guard row plus its paragraph).

**New sabotage key:** `alias-self-loop`, consumed by `buildAliasStatements`, driven by
`tests/red-proof.sh`.

**No graph write occurs in this plan.** Every live interaction is a read-tier `brain_query`.
No `GRAPH-WRITE-LOG.md` row is owed, because that ledger records writes against the canon and
this plan makes none. No admin window is opened or required.

**No MindrianOS-Plugin code is modified by this phase.**
</artifacts_this_phase_produces>

<verification>
- `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/ingest-dedup-selfloop.test.mjs` passes 7/7 with no `GRAPH_BACKEND` and no network.
- The RED proof is two-directional: sabotaged exits non-zero, unsabotaged exits zero.
- `node scripts/probe-alias-self-loops.mjs` exits 0, prints S-1 through S-9, and its [S-8] guard rehearsal returns 0 while [S-7] returns a positive count.
- `grep -rn 'id(a) <> id(canon)' src/ tests/ docs/ --include='*.mjs' --include='*.md'` lists `src/ingest/dedup.mjs`, `tests/ingest-dedup-selfloop.test.mjs`, and `docs/2026-08-20-RCA-alias-self-loop-minting.md` and nothing else.
- No `git push` was executed. `git log origin/main..HEAD --oneline` shows this plan's commits sitting local, which is the FIX-04 batched-push precondition.
- `grep -Pn '\x{2014}'` across every file this plan touched exits 1 with no output.
</verification>

<success_criteria>
- The dedup write path is structurally incapable of emitting a self-`ALIAS_OF` statement, and
  that incapacity is proven by a hermetic test that has been SEEN to fail when the guard is
  disabled.
- One alias resolves to exactly one canonical edge, never N across a duplicate-name cluster.
- The live self-loop population is a measured number in a tracked doc, replacing the stale 41
  in the source comment and the singular framing in the roadmap, and that correction is
  addressed to its consumer (Phase 261 CER-05) by name.
- `payloads/relabel-fix-260820`'s `fix_02_self_loop_check` precondition is discharged in writing,
  and its second precondition (`contested_survivor_picks`) is explicitly recorded as still open.
- Three commits sit in the Brain repo, unpushed, ready for the FIX-04 batch.
</success_criteria>

<output>
Create `.planning/phases/260-pipeline-fixes-brain-repo-one-pass-one-push/260-01-SUMMARY.md` when done.
</output>
