---
phase: 260-pipeline-fixes-brain-repo-one-pass-one-push
plan: 04
type: execute
wave: 2
depends_on: [260-01]
target_repo: ProblemsWorthSolving-Brain
files_modified:
  - ProblemsWorthSolving-Brain/scripts/probe-framework-prop-coverage.mjs
  - ProblemsWorthSolving-Brain/docs/2026-08-20-MEASURE-additive-prop-coverage.md
  - ProblemsWorthSolving-Brain/src/ingest/dedup.mjs
  - ProblemsWorthSolving-Brain/tests/ingest-dedup-additive-props.test.mjs
  - ProblemsWorthSolving-Brain/tests/ingest-dedup.test.mjs
  - ProblemsWorthSolving-Brain/src/ingest/pipeline.mjs
  - ProblemsWorthSolving-Brain/docs/2026-08-20-CARRY-fix01-live-roundtrip.md
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "A framework already in the graph receives framework-level props the stored node LACKS, instead of the ingest silently accepting the payload and writing nothing"
    - "A prop that CONFLICTS with the stored value is still never overwritten; additive means absent-only"
    - "The ingest result reports, per prop, whether it was applied or skipped and why; no prop is silently accepted"
    - "A framework with no MERGE key reports every supplied prop as skipped with a stated reason, and never resolves identity by name to write anyway"
    - "The byId projection returns every key FRAMEWORK_PROP_KEYS compares, so a key missing from the projection can never be mistaken for a key missing from the node"
    - "The two existing live dedup tests are deliberately amended with the measurement that forced it, never deleted and never left to break silently in the admin window"
    - "The live round-trip FIX-01 requires is declared as an executable, numbered pre-item for the Phase 261 admin window, with the reason it cannot run from this machine stated"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-framework-prop-coverage.mjs"
      provides: "Read-only HTTPS probe measuring per-prop absence across the live :Framework population"
      min_lines: 70
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-MEASURE-additive-prop-coverage.md"
      provides: "The measured surface FIX-01 acts on, and the test-amendment justification"
      contains: "framework_type"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/dedup.mjs"
      provides: "additivePropPlan plus the three noop branches wired to it"
      contains: "additivePropPlan"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/ingest-dedup-additive-props.test.mjs"
      provides: "Hermetic proof of additive-vs-conflict-vs-no-key, no graph, no network"
      min_lines: 80
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-CARRY-fix01-live-roundtrip.md"
      provides: "The FIX-01 live round-trip, written as a runnable admin-window pre-item for Phase 261"
      contains: "Phase 261"
  key_links:
    - from: "src/ingest/dedup.mjs resolveFramework noop branches"
      to: "additivePropPlan"
      via: "each noop branch computes its prop report from the pure planner"
      pattern: "additivePropPlan\\("
    - from: "src/ingest/dedup.mjs resolveFramework"
      to: "src/ingest/pipeline.mjs buildPlan"
      via: "propReport travels out on the dedup result and is surfaced on the plan"
      pattern: "propReport"
    - from: "docs/2026-08-20-CARRY-fix01-live-roundtrip.md"
      to: "Phase 261 admin window runbook"
      via: "a numbered pre-item with a payload path and a verify query"
      pattern: "Phase 261"
---

<objective>
Close FIX-01: `ingestFramework` applies additive framework-level props to live nodes through the
`dedup.mjs` `resolveFramework` noop branch, and the ingest result reports applied or skipped per
prop so nothing is silently accepted.

Purpose: the roadmap states the stakes as "silent prop drops at 18x scale". That is measured, not
rhetorical. On canon right now, of 186 `:Framework` nodes: **157 have no `framework_type`, 139 have
no `pattern_type`, 35 have no `applicable_stages`, 185 have no `provenance_note`.** A re-ingest that
supplies any of those today resolves to `noop`, stages zero statements, reports zero flags, and the
prop is lost with the pipeline reporting success. `detectConflicts` even names the case out loud and
then does nothing with it: `if (cur === undefined || cur === null) continue; // additive: stored lacks it -> not a conflict`.

Output: a read-tier coverage probe and its measurement doc, a pure `additivePropPlan` wired into all
three `resolveFramework` noop branches, `propReport` surfaced through `buildPlan`, a hermetic test
suite, a deliberate amendment of the two existing live dedup tests, and the live round-trip declared
as a Phase 261 admin-window pre-item.

**The break this plan exists to catch before the admin window does.** Measured live during planning:
both ingest fixtures (`BIAS_BQ_V1`, `well-defined-problem-framework`) SUPPLY `framework_type`, and
both live nodes LACK it. So the moment FIX-01 lands, `resolveFramework(fx('bias-bq-v1').framework)`
stages a real statement and these two existing assertions become false:

```
assert.equal(r.decision, 'noop', 'already-canon must resolve to noop');
assert.equal(r.statements.length, 0, 'noop stages zero framework statements (node_delta 0)');
```

Those tests are LIVE-only (`guard = onMemgraph ? test : test.skip`) and this machine has no Bolt
credential, so they will SKIP here and BREAK in the admin window if left alone. Amending them is
therefore not optional tidying; it is the difference between a planned contract change and a
mid-ceremony surprise. Amend with the measurement, per the project's standing doctrine: coverage
kept, assertion updated, never deleted.

**A second defect FIX-01 would otherwise convert into a data-loss bug.** `resolveFramework`'s branch
(1) `byId` query projects `name, description, category, framework_type, applicable_stages,
pattern_type` and **omits `provenance_note`**, even though `provenance_note` is in
`FRAMEWORK_PROP_KEYS` and was added specifically by the 2026-08-07 incident to stop a graph losing
the note that an edge was a navigator decision rather than a sourced claim. Today the omission is
merely a missed conflict check. After FIX-01 it becomes: "stored value looks absent, therefore
additive, therefore write it" -- a silent OVERWRITE of exactly the provenance the key exists to
protect. The projection must be fixed in the same commit as the additive apply, not after it.

**Canon Part 8 check, stated not assumed:** framework-level props (`name`, `description`,
`category`, `framework_type`, `applicable_stages`, `pattern_type`, `provenance_note`) are generic
methodology metadata in the Brain's own corpus. No LOCAL room data is read, written, or referenced.
The probe is read-tier.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/260-pipeline-fixes-brain-repo-one-pass-one-push/260-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<cross_repo_contract>
**Read this before running any command in this plan.**

| Concern | Repo | Absolute path |
|---|---|---|
| This PLAN.md and all GSD tracking | MindrianOS-Plugin | `/home/jsagi/dev/MindrianOS-Plugin/.planning/` (gitignored, `git add -f`) |
| Every file this plan creates or edits | ProblemsWorthSolving-Brain | `/home/jsagi/dev/ProblemsWorthSolving-Brain/` (normal tracked repo, plain `git add`) |

Every `git add` / `git commit` in this plan runs with
`/home/jsagi/dev/ProblemsWorthSolving-Brain` as the working directory. Nothing in this plan
commits to MindrianOS-Plugin. Both repos forbid em-dashes; hyphens only.

**PUSH FREEZE, FIX-04, NON-NEGOTIABLE.** Commit locally, never `git push`. Plan 260-05 owns the
single batched push.

**Wave 2 ordering.** This plan depends on 260-01 because both edit `src/ingest/dedup.mjs`. Run it
only after 260-01's three commits are in. `git log origin/main..HEAD --oneline | grep -c FIX-02`
must be >= 1 before starting, and `buildAliasStatements` must already be exported.

**No MCP tools required.** All live measurement is `node scripts/probe-framework-prop-coverage.mjs`
over HTTPS with the read-tier key. Bash only. Safe for subagent dispatch.

**What CANNOT be done from this machine, and why that is not a scope reduction.** FIX-01's wording
ends "proven by a live round-trip on the deployed surface". A real round-trip means calling
`ingestFramework` against canon. Three independent facts block that here, all verifiable:
1. `ingestFramework` calls `enforceMoat({ rawCypher: 'INGEST', isAdmin })` and is deliberately NOT
   registered on the MCP surface (`src/ingest/pipeline.mjs` header: "there is no generic write tool
   anywhere; runIngestTx is not on the MCP surface"). So it is unreachable over HTTPS by design.
2. `resolveFramework` calls `cypher(...)` through `src/graph-client.mjs`, which needs a Bolt
   connection. There is no `.env` in this repo and no local Memgraph container is running.
3. Canon Memgraph (`pws-brain-db`) is on Render's private network, reachable only from
   `pws-brain-mcp`.
This is a dependency conflict, one of the three legitimate reasons to defer, and the deferral is
handled by DELIVERING the round-trip as an executable pre-item for the Phase 261 admin window
(Task 3), not by dropping it. Do not soften FIX-01's implementation to make it verifiable here.
</cross_repo_contract>

<tasks>

<task type="auto">
  <name>Task 1: Measure the additive surface and the test break it forces</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-framework-prop-coverage.mjs (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-MEASURE-additive-prop-coverage.md (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/dedup.mjs` lines 20-77: `FRAMEWORK_PROP_KEYS`, the 2026-08-07 `provenance_note` incident comment, `frameworkProps`, `detectConflicts` (note the `// additive: stored lacks it -> not a conflict` line), and branch (1)'s `byId` projection.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/ingest-dedup.test.mjs` lines 22-37 and 60-67: the four assertions this measurement decides the fate of.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/bias-bq-v1.payload.json` and `tests/fixtures/wdpf.payload.json`: read only the `framework` object of each, to list which `FRAMEWORK_PROP_KEYS` they supply.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-wave-attribution.mjs` lines 52-100 for `resolveReadKey`, `call`, `q`, `initHandshake` (skip if already in context).
  </read_first>

  <action>
**`scripts/probe-framework-prop-coverage.mjs`.** Read-only. Reuse the shared transport; no second
HTTP client. Banner in the repo idiom, closing with `No em-dashes.`.

Import `FRAMEWORK_PROP_KEYS`-equivalent knowledge by hardcoding the seven keys as a local constant
and asserting at startup that it matches the source list (read `src/ingest/dedup.mjs` as text and
check each key appears in the `FRAMEWORK_PROP_KEYS` array literal). A probe measuring a key set
that has drifted from the code is worse than no probe.

Probes:
- **[A-1] Corpus-wide per-prop absence.** One query returning `count(f) AS total` plus, for each of
  the six non-`name` keys, `sum(CASE WHEN f.<key> IS NULL THEN 1 ELSE 0 END)`. This is the size of
  the surface FIX-01 acts on.
- **[A-2] Fixture node state.** For `f.id IN ['BIAS_BQ_V1','well-defined-problem-framework']`,
  return per-key `IS NOT NULL` booleans.
- **[A-3] Fixture payload state.** Not a graph query: read both fixture JSON files and list which
  of the seven keys each `framework` object supplies.
- **[A-4] The cross-product.** For each fixture, compute and print the keys that are SUPPLIED by
  the payload and ABSENT on the node. That set is exactly what FIX-01 will newly stage, and its
  size decides whether the existing assertions survive.
- **[A-5] Provenance exposure.** `MATCH (f:Framework) WHERE f.provenance_note IS NOT NULL RETURN count(f) AS with_prov, collect(f.id)[..10] AS sample`. These are the nodes at risk from the missing `byId` projection: a payload supplying a different `provenance_note` would, after a naive FIX-01, overwrite theirs.

**`docs/2026-08-20-MEASURE-additive-prop-coverage.md`.** Written from the probe output. A
`Measured:` line with the real run date. Sections:

1. **The surface.** [A-1] as a table, one row per key: `key | nodes missing it | percent`. One
   sentence naming this as the concrete meaning of the roadmap's "silent prop drops at 18x scale".
2. **The forced test amendment.** [A-4] for both fixtures. If the supplied-and-absent set is
   non-empty for either fixture, state plainly that
   `tests/ingest-dedup.test.mjs`'s `decision === 'noop'` and `statements.length === 0` assertions
   become false when FIX-01 lands, name the exact key(s) that cause it, and record that Task 2
   amends those assertions with this measurement as the justification. If the set IS empty for
   both, say so and record that no amendment is needed. Measure; do not assume either way.
3. **The provenance_note projection gap.** State that branch (1)'s `byId` query omits
   `provenance_note` while `FRAMEWORK_PROP_KEYS` includes it, quote both, and explain the
   escalation: today a missed conflict check, after FIX-01 a silent overwrite. Give [A-5]'s count
   as the population exposed. Record that Task 2 fixes the projection in the same commit.
4. **What additive does NOT mean.** One short section restating the file's own re-ingest default:
   a prop that CONFLICTS is a NO-OP plus a flag, never an overwrite. FIX-01 widens the ABSENT case
   only. This section exists so a later reader cannot mistake FIX-01 for a licence to overwrite.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node scripts/probe-framework-prop-coverage.mjs && grep -q 'provenance_note' docs/2026-08-20-MEASURE-additive-prop-coverage.md</automated>
  </verify>

  <acceptance_criteria>
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node scripts/probe-framework-prop-coverage.mjs` exits 0 and prints labelled results for A-1 through A-5.
    - The probe's startup key-set check passes. If any of the seven keys the probe hardcodes is absent from the source `FRAMEWORK_PROP_KEYS` array literal (that is, the two lists have drifted), the probe exits non-zero and names the offending key. Confirm this guard is real by temporarily probing with a bogus key locally and seeing it fail, then removing the bogus key.
    - Section 1's table has one row per non-`name` key with a measured absence count.
    - Section 2 names a concrete verdict: either the exact key(s) that break the two assertions, or an explicit statement that the supplied-and-absent set is empty for both fixtures. A hedge is a task failure.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -q 'byId' docs/2026-08-20-MEASURE-additive-prop-coverage.md` exits 0 and section 3 quotes both the projection and `FRAMEWORK_PROP_KEYS`.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -qi 'never an overwrite\|never overwrite' docs/2026-08-20-MEASURE-additive-prop-coverage.md` exits 0 (section 4 exists).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'fetch(' scripts/probe-framework-prop-coverage.mjs` outputs `0`.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -Pn '\x{2014}' scripts/probe-framework-prop-coverage.mjs docs/2026-08-20-MEASURE-additive-prop-coverage.md` exits 1 with no output.
    - Committed locally, NOT pushed.
  </acceptance_criteria>

  <done>
    The additive surface is measured per prop across the live corpus, the fixture cross-product
    gives a yes-or-no verdict on whether the two existing assertions break, the `provenance_note`
    projection gap is documented with its exposed population, and the additive-is-not-overwrite
    boundary is restated so FIX-01 cannot be misread later.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: additivePropPlan, the three noop branches, and the deliberate test amendment</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/ingest-dedup-additive-props.test.mjs (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/dedup.mjs (modify)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/ingest-dedup.test.mjs (modify)
  </files>

  <read_first>
    - `docs/2026-08-20-MEASURE-additive-prop-coverage.md` sections 2 and 3 (written by Task 1). These decide what you amend and why.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/dedup.mjs` lines 27-164 in full, INCLUDING plan 260-01's `buildAliasStatements` addition. You are editing the same file 260-01 changed; read the current state, not the pre-260-01 state.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/pipeline.mjs` lines 88-102 (`droppedNodePropKeys`): the exact idiom for a pure exported reporter that makes a silent drop visible. `additivePropPlan` is its sibling and should read like it.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/ingest-dedup.test.mjs` in full.
  </read_first>

  <behavior>
    New file `tests/ingest-dedup-additive-props.test.mjs`, hermetic (no graph, no network),
    flat `test()`, importing `{ additivePropPlan }` from `../src/ingest/dedup.mjs`.

    `additivePropPlan(fw, stored)` returns `{ props, applied, skipped }` where `props` is the
    object to `SET`, `applied` is an array of key names, and `skipped` is an array of
    `{ prop, reason }`.

    - Test 1 `a key the payload supplies and the node lacks is APPLIED`:
      `additivePropPlan({ framework_type: 'process' }, { name: 'X' })` has `applied` equal to
      `['framework_type']` and `props.framework_type === 'process'`.
    - Test 2 `a key with an identical stored value is SKIPPED as identical, not applied`:
      reason is `identical`, `applied` is empty, `props` is empty.
    - Test 3 `a key with a DIFFERENT stored value is SKIPPED as conflict and never enters props`:
      reason is `conflict`, and `props` does not have the key. This is the additive-only contract:
      widening the absent case must not widen the conflict case.
    - Test 4 `an array-valued key compares by JSON, matching detectConflicts`:
      `applicable_stages: ['a','b']` against stored `['a','b']` skips as `identical`;
      against stored `['a']` skips as `conflict`.
    - Test 5 `a key the payload does not supply appears nowhere`:
      it is neither in `applied` nor in `skipped`. The report describes what the payload asked
      for, not the whole key space.
    - Test 6 `a key outside FRAMEWORK_PROP_KEYS is ignored entirely`:
      a payload key like `secret_note` is absent from `props`, `applied` and `skipped`. The
      allowlist stays closed.
    - Test 7 `null and undefined incoming values are not applied`:
      supplying `category: null` yields no apply and no props entry.
    - Test 8 `RED: an empty applied list must produce an empty props object`:
      assert `Object.keys(props).length === 0` whenever `applied.length === 0`, across a table of
      inputs. A `SET f += {}` staged on every noop would turn every re-ingest into a write.

    Write the file first; every test fails on a missing export.
  </behavior>

  <action>
Write the test file FIRST, confirm RED, then edit the source.

**`src/ingest/dedup.mjs` edit 1: fix the byId projection.** Add `f.provenance_note AS provenance_note`
to branch (1)'s `RETURN` clause so every key in `FRAMEWORK_PROP_KEYS` is projected. Add a one-line
comment naming the escalation from Task 1 section 3: before FIX-01 this omission was a missed
conflict check, after FIX-01 it would be a silent overwrite of the exact provenance the key exists
to protect. Do this edit FIRST so the additive logic is never live against an incomplete projection,
even for one commit.

**`src/ingest/dedup.mjs` edit 2: add `additivePropPlan`.** A pure exported function above
`resolveFramework`, in the `droppedNodePropKeys` idiom ("Pure. Exported for the hermetic test."),
with a short comment stating the rule: additive means ABSENT-ONLY. Iterate `FRAMEWORK_PROP_KEYS`;
for each key the payload supplies non-null:
- stored value is `undefined` or `null` -> push to `applied`, copy into `props`
- stored value equals incoming (arrays compared by `JSON.stringify`, scalars by `String`, exactly
  matching `detectConflicts`) -> push `{ prop, reason: 'identical' }` to `skipped`
- otherwise -> push `{ prop, reason: 'conflict' }` to `skipped`, and do NOT copy into `props`

Reuse the comparison logic rather than writing a second one. If that means extracting the
same-value test out of `detectConflicts` into a small local helper both call, do that: two
independently-written comparisons that must agree is how they stop agreeing.

**`src/ingest/dedup.mjs` edit 3: wire the three noop branches.** All three must produce a
`propReport`, and the return object of `resolveFramework` gains `propReport` on every path.

- **Branch (1), exact-id hit.** Compute `additivePropPlan(fw, byId[0])`. If `applied.length > 0`,
  stage exactly one statement: `MATCH (f:Framework {id: $id}) SET f += $props` with
  `{ id: fw.id, props }`. Use `MATCH`, not `MERGE`: the node is known to exist and a `MERGE` would
  create one on a race or a case-mismatch. Set `decision` to `'enrich'` when a statement is staged
  and keep `'noop'` when none is. Do NOT reuse `'noop'` for a branch that writes; a decision name
  that lies is the same class of defect as a description that lies.
- **Branch (2), name-resolved same-id.** Same treatment, keyed on `canon.id`, using `canon` as the
  stored object. Same `'enrich'` / `'noop'` rule.
- **Branch (3), `alias_needs_id` (no id at all).** Compute the plan for reporting only, then move
  EVERY entry from `applied` into `skipped` with reason `no_merge_key`, and stage NO statement.
  Add one comment line: resolving identity by name to write props is exactly Pitfall 5 (two
  distinct nodes share the name `Dominant Design`), and the honest resolution is to report the
  loss, not to guess the node. This branch keeps `decision: 'noop'` and keeps its existing
  `alias_needs_id` flag.

Also give `new_framework_needs_id` and the genuinely-new branch a `propReport`: for the new-node
branch every supplied key is `applied` (the `SET f += $props` already writes them), and for the
no-id-no-match branch every supplied key is `skipped` with reason `no_merge_key`. `propReport` must
be present on every return path so a consumer never has to test for its absence.

**`tests/ingest-dedup.test.mjs` edit: the deliberate amendment.** Driven by Task 1 section 2, not by
guesswork. Above the two affected tests add a dated comment block in the file's existing INCIDENT
style, recording: the measurement (which key(s) the fixtures supply that the live nodes lack), that
FIX-01 makes the previous assertion false BY DESIGN, and that the coverage is kept with the
assertion updated rather than the test deleted. Then update the assertions to the new contract:
- `decision` is `'noop'` OR `'enrich'`, and the test asserts WHICH one it expects and why, based on
  the measured node state.
- `statements.length` is asserted against the measured `applied` count, not a bare `0`.
- Add one assertion that no staged statement contains the string `ALIAS_OF` (an enrich must never
  become an alias) and one that `r.propReport.skipped` contains no entry with reason `conflict`
  for a clean re-ingest of an unmodified fixture.
The existing test at line 60 (`a conflicting prop on re-ingest is a NO-OP + flag`) must KEEP
asserting that a conflict stages no overwrite. Extend it to also assert the conflicting key appears
in `propReport.skipped` with reason `conflict`. That test is the guard on the boundary FIX-01 must
not cross.

These tests are live-only and will SKIP on this machine. Say so in the SUMMARY, and record that
their first real execution is the Phase 261 admin window per Task 3's carry document. Do not mark
them verified.

Run the hermetic suite and confirm the live suites still import cleanly.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/ingest-dedup-additive-props.test.mjs && node --test tests/ingest-dedup.test.mjs tests/ingest-dedup-selfloop.test.mjs</automated>
  </verify>

  <acceptance_criteria>
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/ingest-dedup-additive-props.test.mjs` exits 0 with 8 pass, 0 fail, no `GRAPH_BACKEND`.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node -e "import('./src/ingest/dedup.mjs').then(m=>{const r=m.additivePropPlan({framework_type:'process',category:'x'},{category:'y'});process.exit(r.applied.join()==='framework_type' && r.skipped.some(s=>s.prop==='category'&&s.reason==='conflict') && !('category' in r.props) ? 0 : 1)})"` exits 0.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node -e "import('./src/ingest/dedup.mjs').then(m=>{const r=m.additivePropPlan({secret_note:'x'},{});process.exit(r.applied.length===0 && r.skipped.length===0 && Object.keys(r.props).length===0 ? 0 : 1)})"` exits 0 (the allowlist stays closed).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'f.provenance_note AS provenance_note' src/ingest/dedup.mjs` outputs `2` (branch 1's projection now matches branch 2's, which already had it).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -v '^\s*//' src/ingest/dedup.mjs | grep -c "decision: 'enrich'"` outputs a number >= 1.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -v '^\s*//' src/ingest/dedup.mjs | grep -c 'MERGE (f:Framework {id: \$id}) SET f'` outputs `1` (the pre-existing genuinely-new branch only). The enrich statement uses `MATCH`, not `MERGE`: `grep -c 'MATCH (f:Framework {id: \$id}) SET f += \$props' src/ingest/dedup.mjs` outputs `1`.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -v '^\s*//' src/ingest/dedup.mjs | grep -c 'no_merge_key'` outputs a number >= 2 (the alias-needs-id and new-framework-needs-id branches both report).
    - Every `return` in `resolveFramework` carries `propReport`. Verify: the count of `propReport` occurrences in `src/ingest/dedup.mjs` (comments excluded) is >= the count of `return {` occurrences inside `resolveFramework`.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'INCIDENT\|AMENDED' tests/ingest-dedup.test.mjs` outputs a number >= 2 (the amendment carries a dated justification block, not a silent edit).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && git diff HEAD~1 --stat -- tests/ingest-dedup.test.mjs | grep -q 'deletion'` may be true, but `grep -c "^guard(" tests/ingest-dedup.test.mjs` must be unchanged from before this task: no test was removed, only assertions updated.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/ingest-dedup.test.mjs tests/ingest-dedup-selfloop.test.mjs` exits 0 (imports clean; live tests skip).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -Pn '\x{2014}' src/ingest/dedup.mjs tests/ingest-dedup-additive-props.test.mjs tests/ingest-dedup.test.mjs` exits 1 with no output for added lines.
    - Committed locally, NOT pushed.
  </acceptance_criteria>

  <done>
    `additivePropPlan` is pure, exported, closed to the allowlist, and applies absent-only while
    routing identical and conflicting values to `skipped` with distinct reasons. All three noop
    branches carry a `propReport`, the two with a MERGE key stage a `MATCH ... SET f += $props` and
    report `decision: 'enrich'`, the keyless branch reports every supplied prop as `no_merge_key`
    and stages nothing. The `byId` projection covers every allowlisted key. The two existing live
    assertions are amended with a dated justification and zero tests were deleted.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface propReport on the ingest result, and hand the live round-trip to Phase 261</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/pipeline.mjs (modify)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/ingest-dedup-additive-props.test.mjs (modify, append)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-CARRY-fix01-live-roundtrip.md (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/pipeline.mjs` lines 204-258 (`buildPlan` and `ingestFramework`): how `dedup.flag` is merged into `flag`, how `dedup.statements` enter `statements`, and what `buildPlan` returns.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/run-ingest.mjs`: how a payload is actually executed against the graph, which is the shape the carry document must be runnable as.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/order-collision-dishare-2026-08-20/` file layout (manifest, dry-run, verify, undo, close-window): the established admin-window payload convention from Phase 258. The carry document points at this convention rather than inventing one.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/mentions-write-tx.test.mjs` lines 50-90: how a hermetic test drives `ingestFramework` with `dryRun: true` and injected seams, to see whether a plan-shape assertion can be made without a graph.
  </read_first>

  <behavior>
    Appended to `tests/ingest-dedup-additive-props.test.mjs`, still hermetic:

    - Test 9 `buildPlan surfaces propReport`: assert by SOURCE INSPECTION, not execution, since
      `buildPlan` calls `resolveFramework` which needs a graph. Read `src/ingest/pipeline.mjs` as
      text and assert that the object literal returned by `buildPlan` contains `propReport`.
      Follow the source-scanning idiom from `tests/bounded-read-single-seam.test.mjs`: a comment
      claiming the field is surfaced is not a control; this scan is.
    - Test 10 `the plan's warn channel carries a prop skip that loses data`: source-scan assert
      that `buildPlan`'s `warn` array includes a term derived from `dedup.propReport`, so a
      `no_merge_key` skip reaches the operator through the same channel `droppedNodePropKeys`
      already uses for silent prop loss.
    - Test 11 `RED: the scan can fail`: run the same scanner against a fixture string with the
      field removed and assert it reports absent. Prefix with `RED:`.
  </behavior>

  <action>
**`src/ingest/pipeline.mjs` edit.** In `buildPlan`, add `propReport: dedup.propReport ?? { applied: [], skipped: [] }` to the returned object, next to the existing `dedup` field. Then fold the
data-losing subset of the report into `warn`: for every `skipped` entry whose reason is
`no_merge_key`, push `{ kind: 'framework_prop_dropped', prop, reason }`. Do NOT fold `conflict` or
`identical` skips into `warn`: `conflict` already reaches the operator through `dedup.flag` as
`prop_conflict`, and `identical` is not a loss. Add a two-line comment stating that split and why,
mirroring the existing reasoning above `droppedNodePropKeys` ("Prop loss is a WARN, not a reject").

Do not change `ingestFramework`'s signature or its five-step order. It already returns
`{ plan, committed }` and `plan` now carries `propReport`, which satisfies "the ingest result
reports applied/skipped per prop" without a new return shape for callers to adopt.

**`docs/2026-08-20-CARRY-fix01-live-roundtrip.md`.** This is the deliverable that keeps FIX-01's
last clause honest. It is a runbook item, not a note. A `Measured:` line, then:

1. **What is proven and what is not.** State exactly: the additive logic is proven hermetically
   (name the test file and count), the surface it acts on is measured live (name the measurement
   doc), and the round-trip through `ingestFramework` against canon is NOT proven from this
   machine. Give the three blocking facts from this plan's cross-repo contract, each as a command
   or a file citation a reader can check, not as an assertion.
2. **The round-trip, as an executable procedure.** Numbered steps for the Phase 261 admin window:
   - pick a target framework measured in `docs/2026-08-20-MEASURE-additive-prop-coverage.md` as
     missing exactly one allowlisted key;
   - record its BEFORE prop state with a read-tier query (give the query);
   - run `node payloads/run-ingest.mjs <payload>` WITHOUT `--commit` first and capture
     `plan.propReport`, asserting `applied` contains exactly that one key and `skipped` contains
     no `conflict`;
   - re-run WITH commit;
   - verify with a read-tier query that the key is now non-null and that NO other allowlisted key
     changed (give that query too: it must compare all seven, not just the one, because the whole
     point is that additive did not become overwrite);
   - record the result in `docs/GRAPH-WRITE-LOG.md` using the 8-column row shape Phase 258
     established, because this IS a write against canon.
3. **The undo.** Name the reversal for the one staged key and state that the payload directory must
   carry a `99-undo.cypher` keyed on `batch_id`, per the Phase 258 payload convention. Point at
   `payloads/order-collision-dishare-2026-08-20/` as the shape to copy.
4. **Ordering inside Phase 261.** This round-trip must run BEFORE the CER-01 through CER-04 bulk
   payloads, because those payloads flow through the same `resolveFramework` path and a defect
   found after 20 frameworks have been enriched is a defect found too late. State that explicitly
   as a sequencing requirement for 261's plan, and note that it is a small, single-key, reversible
   write chosen precisely so it can be the first thing the window does.
5. **What would falsify the fix.** Name the observable that would mean FIX-01 is wrong: any
   allowlisted key OTHER than the target changing value, or `propReport.applied` disagreeing with
   what actually landed on the node. A carry item that cannot fail is not a verification.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/ingest-dedup-additive-props.test.mjs && node --test tests/mentions-write-tx.test.mjs tests/ingest-admin-gate.test.mjs</automated>
  </verify>

  <acceptance_criteria>
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/ingest-dedup-additive-props.test.mjs` exits 0 with 11 pass, 0 fail.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'propReport' src/ingest/pipeline.mjs` outputs a number >= 2 (returned on the plan and consumed for `warn`).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'framework_prop_dropped' src/ingest/pipeline.mjs` outputs `1`.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c "reason === 'conflict'" src/ingest/pipeline.mjs` outputs `0` (conflicts are not double-reported into `warn`; they stay on `flag`).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/mentions-write-tx.test.mjs tests/ingest-admin-gate.test.mjs` exits 0 (the `buildPlan` shape change did not break the two suites that assert on plan structure).
    - The carry doc contains at least two runnable Cypher queries (BEFORE state and the all-seven-key AFTER comparison) and at least one shell command. Verify: `grep -c 'MATCH (f:Framework' docs/2026-08-20-CARRY-fix01-live-roundtrip.md` is >= 2 and `grep -c 'run-ingest.mjs' docs/2026-08-20-CARRY-fix01-live-roundtrip.md` is >= 1.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -q 'GRAPH-WRITE-LOG' docs/2026-08-20-CARRY-fix01-live-roundtrip.md` exits 0 (the write is ledgered, per the Phase 258 convention).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -qi 'falsif' docs/2026-08-20-CARRY-fix01-live-roundtrip.md` exits 0 (section 5 exists; the carry item can fail).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -q 'CER-01' docs/2026-08-20-CARRY-fix01-live-roundtrip.md` exits 0 (the sequencing requirement reaches Phase 261 by name).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -Pn '\x{2014}' src/ingest/pipeline.mjs docs/2026-08-20-CARRY-fix01-live-roundtrip.md` exits 1 with no output for added lines.
    - Committed locally, NOT pushed.
  </acceptance_criteria>

  <done>
    `buildPlan` returns `propReport` and routes only the data-losing skips into `warn`, with the
    split reasoned in a comment. The live round-trip FIX-01 names is delivered as a numbered,
    reversible, falsifiable admin-window procedure with its own BEFORE and AFTER queries, a ledger
    row, an undo, and an explicit ordering requirement placing it before Phase 261's bulk payloads.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest payload props -> stored framework node | A payload can now cause a `SET` on a live canonical node. Before FIX-01 the noop branch could not write at all, so this is a NEW write capability and the widest boundary this phase opens. |
| stored-value projection -> additive decision | The decision "this prop is absent" is made from a query projection. A key missing from the projection is indistinguishable from a key missing from the node. |
| repo code -> live canon graph | autodeploy on push; the enrich statement becomes production ingest behaviour when 260-05 pushes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260-18 | Tampering | additive apply silently OVERWRITING `provenance_note` because branch (1)'s `byId` projection omits it | mitigate | the projection is fixed FIRST, in the same commit, before the additive logic goes live; asserted by a `grep -c` on the projection line and measured against the [A-5] exposed population |
| T-260-19 | Tampering | additive widening into conflict-overwrite | mitigate | `additivePropPlan` never copies a conflicting key into `props` (hermetic test 3), and the pre-existing live conflict test is extended rather than relaxed |
| T-260-20 | Spoofing | writing props onto a node identified by NAME when no id exists (Pitfall 5, two distinct `Dominant Design` nodes) | mitigate | branch (3) stages nothing and reports every supplied prop as `no_merge_key`; the prohibition is stated as a code comment so a later session does not "improve" it into a name lookup |
| T-260-21 | Repudiation | an ingest reporting success while dropping props | mitigate | `propReport` on every return path, `framework_prop_dropped` folded into `warn`, and source-scanning tests 9-11 that prove the field is actually surfaced rather than merely intended |
| T-260-22 | Denial of Service | a `SET f += {}` staged on every re-ingest, turning read-only noops into writes at scale | mitigate | hermetic test 8 asserts empty `applied` implies empty `props`, and the statement is staged only when `applied.length > 0` |
| T-260-23 | Information Disclosure | user room data reaching the Brain (Canon Part 8) | accept, with a stated control | the seven allowlisted keys are generic methodology metadata; the allowlist is closed and hermetic test 6 proves an out-of-allowlist key is discarded entirely |
| T-260-24 | Elevation of Privilege | a new write capability shipped without ever being exercised against canon | mitigate (declared, not hidden) | the round-trip is delivered as a Phase 261 admin-window pre-item that must run BEFORE the bulk payloads, is single-key and reversible, is ledgered, and names its own falsification condition |
| T-260-SC | Tampering | npm/pip/cargo installs | mitigate | not applicable: zero new packages. No package-manager install task exists in this plan, so no legitimacy checkpoint is required. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

**New files in `/home/jsagi/dev/ProblemsWorthSolving-Brain` (this plan):**

| Path | Created by | What it is |
|---|---|---|
| `scripts/probe-framework-prop-coverage.mjs` | 260-04 Task 1 | Read-only HTTPS probe, A-1..A-5, with a key-set drift guard |
| `docs/2026-08-20-MEASURE-additive-prop-coverage.md` | 260-04 Task 1 | The measured additive surface and the test-amendment justification |
| `tests/ingest-dedup-additive-props.test.mjs` | 260-04 Tasks 2, 3 | Hermetic 11-test suite: 8 logic tests plus 3 source-scan tests |
| `docs/2026-08-20-CARRY-fix01-live-roundtrip.md` | 260-04 Task 3 | The FIX-01 round-trip as a Phase 261 admin-window pre-item |

**New exported symbols:**

| Symbol | Module | Shape |
|---|---|---|
| `additivePropPlan` | `src/ingest/dedup.mjs` | `(fw, stored)` returns `{ props, applied, skipped }` |

**New decision value:** `resolveFramework` may now return `decision: 'enrich'`. Existing values
`noop`, `alias`, `new` are unchanged. Any consumer switching on `decision` must handle `enrich`.

**New result field:** `propReport` on every `resolveFramework` return path, surfaced as
`plan.propReport` by `buildPlan`.

**New flag and warn kinds:** `framework_prop_dropped` (warn, from `buildPlan`), and the
`no_merge_key` / `conflict` / `identical` skip reasons inside `propReport.skipped`.

**Modified existing files:** `src/ingest/dedup.mjs` (byId projection completed, `additivePropPlan`
added, three noop branches wired, `propReport` on all return paths), `src/ingest/pipeline.mjs`
(`propReport` returned and its data-losing subset folded into `warn`), `tests/ingest-dedup.test.mjs`
(two assertions deliberately amended with a dated justification block; zero tests removed).

**No graph write occurs in this plan.** Every live interaction is a read-tier `brain_query`. The
first write this code enables happens in the Phase 261 admin window per the carry document, and
that write owes a `GRAPH-WRITE-LOG.md` row. No admin window is opened by this plan.

**No MindrianOS-Plugin code is modified by this phase.**
</artifacts_this_phase_produces>

<verification>
- `node --test tests/ingest-dedup-additive-props.test.mjs` passes 11/11 hermetically.
- `node --test tests/ingest-dedup.test.mjs tests/ingest-dedup-selfloop.test.mjs tests/mentions-write-tx.test.mjs tests/ingest-admin-gate.test.mjs` exits 0 (imports clean, live tests skip, plan-shape suites unbroken).
- `node scripts/probe-framework-prop-coverage.mjs` exits 0 with A-1..A-5 and a passing key-set drift guard.
- `grep -c 'f.provenance_note AS provenance_note' src/ingest/dedup.mjs` outputs `2`.
- The enrich statement uses `MATCH`, never `MERGE`, verified by grep.
- The count of `guard(` in `tests/ingest-dedup.test.mjs` is unchanged: no test was deleted.
- `grep -Pn '\x{2014}'` across every file this plan touched exits 1 with no output for added lines.
- No `git push` was executed; commits sit local for the FIX-04 batch.
</verification>

<success_criteria>
- A re-ingest of a framework already in the graph now lands the props the stored node lacks, and
  the pipeline says per prop what it did and why.
- Additive did not become overwrite: conflicting values are still flagged and never written, and
  the `provenance_note` projection gap that would have turned FIX-01 into a data-loss bug is closed
  in the same commit.
- A framework with no MERGE key reports its loss rather than guessing a node by name.
- The two live assertions FIX-01 invalidates are amended on the record, with the measurement that
  forced the change, before the admin window meets them.
- FIX-01's live round-trip is not claimed and not dropped: it ships as a numbered, reversible,
  falsifiable, ledgered pre-item that Phase 261 must run before its bulk payloads.
</success_criteria>

<output>
Create `.planning/phases/260-pipeline-fixes-brain-repo-one-pass-one-push/260-04-SUMMARY.md` when done.
</output>
