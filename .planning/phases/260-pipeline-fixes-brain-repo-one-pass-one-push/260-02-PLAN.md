---
phase: 260-pipeline-fixes-brain-repo-one-pass-one-push
plan: 02
type: execute
wave: 1
depends_on: []
target_repo: ProblemsWorthSolving-Brain
files_modified:
  - ProblemsWorthSolving-Brain/scripts/probe-name-matching-readers.mjs
  - ProblemsWorthSolving-Brain/docs/2026-08-20-MATRIX-name-matching-readers.md
autonomous: true
requirements: [FIX-03]

must_haves:
  truths:
    - "Every name-matching reader in the Brain is enumerated from source by a stated inclusion rule, not by an inherited count"
    - "The roadmap's 'four name-matching readers' figure is either confirmed or corrected against the census, in writing"
    - "For each reader the matrix records whether it traverses ALIAS_OF today, as a MEASURED answer, not an inferred one"
    - "For each reader the matrix records an explicit ruling: change in Phase 260, or deliberately unchanged with a reason"
    - "The matrix covers the no-canonical-copy topology (the majority case), not only the split-cluster minority"
    - "The 2-hop alias-chain question is answered with a live example, not treated as hypothetical"
    - "A BEFORE measurement exists for a fixed raw-fragment corpus, so plan 260-03's AFTER leg is a diff and not a claim"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-name-matching-readers.mjs"
      provides: "Read-only HTTPS probe producing the BEFORE half of the matrix, re-runnable as the AFTER half"
      min_lines: 120
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-MATRIX-name-matching-readers.md"
      provides: "The FIX-03 gate: reader census, per-reader ruling, BEFORE corpus, topology coverage"
      contains: "ALIAS_OF"
  key_links:
    - from: "docs/2026-08-20-MATRIX-name-matching-readers.md reader census"
      to: "src/arm1-orchestrator.mjs, src/brain-tools.mjs, src/community-summary.mjs, src/ingest/dedup.mjs, src/ingest/pipeline.mjs, src/ingest/validator.mjs"
      via: "every census row cites file plus line number"
      pattern: "src/.*\\.mjs:[0-9]+"
    - from: "scripts/probe-name-matching-readers.mjs"
      to: "scripts/probe-wave-attribution.mjs"
      via: "shared read-tier transport, no second HTTP client"
      pattern: "from './probe-wave-attribution.mjs'"
    - from: "docs/2026-08-20-MATRIX-name-matching-readers.md BEFORE table"
      to: "plan 260-03's AFTER re-run"
      via: "the same probe with --after, diffed row by row"
      pattern: "--after"
---

<objective>
Build the before/after matrix that gates FIX-03. This is the one sub-plan in the whole 258-263
set the roadmap flagged as needing deeper research during planning, and 260-RESEARCH.md sharpens
why: **the alias layer is roughly 78 percent applied on the split set and it currently buys the
product nothing, because no consumer follows the edge.** FIX-03 is not a tidy-up. It activates
work already paid for by the 2026-08-11 alias-collapse session.

Purpose: FIX-03 says the fix is "gated by a before/after matrix across all name-matching readers
AND the dedup write-path consumer". A gate that is authored after the change is not a gate. This
plan produces the BEFORE half and the rulings; plan 260-03 makes the change and re-runs the same
probe for the AFTER half.

Output: `scripts/probe-name-matching-readers.mjs` (re-runnable, `--after` flag switches the
output slot) and `docs/2026-08-20-MATRIX-name-matching-readers.md`.

**Scope discipline, stated up front.** 260-RESEARCH.md says: "Scope generously, not minimally.
The direct-match branch being alias-unaware is one symptom. The general shape is: every
name-matching reader must decide, EXPLICITLY, whether it resolves through `ALIAS_OF` or not, and
the before/after matrix should record that decision per reader rather than only measuring
match-count deltas." A reader left unchanged is a legitimate outcome. A reader left unexamined
is not. Research open question 3 is exactly this: "Does any reader intentionally NOT traverse
`ALIAS_OF`, and if so, is that recorded as a decision or is it incidental?" This plan converts
every incidental into a decision.

**Measured ground truth from planning-time read-only probes (2026-08-20). Re-measure; do not
inherit as fact.**

| Measurement | Value | Consequence |
|---|---|---|
| `:Framework` nodes with an outgoing `ALIAS_OF` to a `:Framework` | **42** | `normalizeName`'s DIRECT branch returns these 42 names inside `canonical_matches` as if they were canonical. They are aliases. |
| `:Framework` nodes with an outgoing `ALIAS_OF` to ANY node | **50** | 8 point at non-Framework targets, so a target-typed exclusion and an untyped one differ by 8. |
| NON-`:Framework` nodes that are `ALIAS_OF` sources pointing at a `:Framework` | **47** | Completely invisible to the current alias branch, which is typed `(a:Framework)` on the SOURCE side. This is the "buys the product nothing" population, quantified. |
| `normalizeName({raw:'scenario'})`, current query, live | 8 entries, `Shell Scenario Planning Method` appearing **twice** | The cross-branch duplicate defect, reproduced. `collect(DISTINCT ...) + collect(DISTINCT ...)` dedups within each branch, never across them. |
| Same fragment, proposed query, live | 4 entries, no duplicate | The delta FIX-03 produces on one fragment. |
| `Scenario planning methodology` | is BOTH an `ALIAS_OF` source AND still surfaces as a canonical in the proposed query | A live 2-hop chain. The research called the 46099 / 34086 case "a live precedent, not a hypothetical". This is a second one, found during planning. |
| Clusters with no `:Framework`-bearing copy | **772 of 823** (from 260-RESEARCH.md) | The majority topology. A matrix built only from the 51 split clusters tests the minority. |

**Canon Part 8 check, stated not assumed:** read-only measurement of the Brain's own methodology
corpus. Node names, labels and `ALIAS_OF` edges only. No LOCAL room data is read or written.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/260-pipeline-fixes-brain-repo-one-pass-one-push/260-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<cross_repo_contract>
**Read this before running any command in this plan.**

| Concern | Repo | Absolute path |
|---|---|---|
| This PLAN.md and all GSD tracking | MindrianOS-Plugin | `/home/jsagi/dev/MindrianOS-Plugin/.planning/` (gitignored, `git add -f`) |
| Every file this plan creates or edits | ProblemsWorthSolving-Brain | `/home/jsagi/dev/ProblemsWorthSolving-Brain/` (normal tracked repo, plain `git add`) |

Every `git add` / `git commit` in this plan runs with
`/home/jsagi/dev/ProblemsWorthSolving-Brain` as the working directory. Nothing in this plan
commits to MindrianOS-Plugin. Both repos forbid em-dashes; hyphens only.

**PUSH FREEZE, FIX-04, NON-NEGOTIABLE.** Commit locally, never `git push`. Plan 260-05 owns the
single batched push. See plan 260-01's cross-repo contract for the full reasoning.

**No MCP tools required.** All live measurement is `node scripts/probe-name-matching-readers.mjs`
over HTTPS with the read-tier key from `MINDRIAN_BRAIN_KEY` or `~/.mindrian.env`. Bash only.
Safe for subagent dispatch.

**Every doc this plan writes carries a `Measured:` line with the ACTUAL run date.** The filename
keeps the `2026-08-20` stem so it files alongside `payloads/relabel-fix-260820/` and
`260-RESEARCH.md`, but the date inside the doc is the date the probe ran. Do not backdate a
measurement to match a filename.
</cross_repo_contract>

<tasks>

<task type="auto">
  <name>Task 1: Census every name-matching reader from source</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-MATRIX-name-matching-readers.md (new, census section only)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/arm1-orchestrator.mjs` lines 59-83 (T1 `normalizeName`, both branches) and lines 89-112 (T2 `loadFramework`). Then use Grep, not Read, to locate the remaining name predicates in that file: `grep -n "f.name = \$framework_name\|toLower(f.name)\|toLower(cs.name)\|toLower(pt.name)" src/arm1-orchestrator.mjs`. Read only the specific line ranges that grep returns and that you have not already read.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/brain-tools.mjs` lines 190-210 and lines 320-335: the untyped `MATCH (a) WHERE toLower(a.name) = toLower($from)` traversal anchors.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/community-summary.mjs` lines 35-50.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/validator.mjs` lines 95-120.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/pipeline.mjs` lines 146-202 (`buildStructureStatements`: the `to_framework`, `from_framework`, MENTIONS and ProblemType name-resolved endpoints).
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/dedup.mjs` lines 79-145 (the `canonRows` query and the alias branch). Note plan 260-01 modifies this file in the same wave; the census records the reader's SHAPE, which 260-01 does not change.
    - 260-RESEARCH.md, "What this means for FIX-03 scoping" and "Open questions for the planner".
  </read_first>

  <action>
Create `docs/2026-08-20-MATRIX-name-matching-readers.md` with the census sections only. Plan
tasks 2 and 3 append the measurements and the rulings to the same file.

**Header.** Title, a `Measured:` line carrying the real run date, a `Phase:` line naming
260 / FIX-03, and a `Status:` line saying this document is the GATE on the FIX-03 change and
that the change (plan 260-03) must not land before the rulings section is complete.

**Section 1: the inclusion rule, stated before the list.** A "name-matching reader" is any
call site that (a) takes a NAME STRING supplied by a model, a user, or an ingest payload, (b)
resolves it to graph node(s) through a predicate on `.name`, and (c) returns the result to a
consumer or uses it to anchor a write. Write the rule out, then state its two deliberate
exclusions and why: a predicate on `.id` is identity resolution, not name matching; a hardcoded
literal name inside a diagnostic script is not a reader because no caller supplies the string.
Record that the rule is stated FIRST so the census is reproducible by someone who disagrees
with your judgement calls.

**Section 2: the census table.** One row per call site, with these columns:

| # | Reader | File:line | Predicate shape | Typed? | Name comes from | Result consumed by |

Enumerate from the grep and the reads. At minimum these must appear, and you must verify each
against source rather than trusting this list:

- `normalizeName` direct branch, `src/arm1-orchestrator.mjs` line 67, `CONTAINS`, typed `:Framework`
- `normalizeName` alias branch, `src/arm1-orchestrator.mjs` line 68, `CONTAINS`, typed `:Framework` on BOTH the alias source and the canon target
- `loadFramework`, `src/arm1-orchestrator.mjs` line 93, exact OR `CONTAINS`, typed `:Framework`
- the arm1 exact-name family (`f.name = $framework_name OR toLower(f.name) = toLower($framework_name)`) at every line grep returns, typed `:Framework`. If this shape appears N times, decide and STATE whether you count it as N readers or as one shape with N call sites, then be consistent. The roadmap's "four" almost certainly counts shapes, not call sites; say so if that is what you conclude.
- `brain-tools.mjs` traversal anchors, lines 196, 197, 203, 204, 326, exact `toLower` equality, UNTYPED (`MATCH (a)` with no label)
- `community-summary.mjs` line 41, exact `toLower` equality, untyped
- `validator.mjs` line 101 and line 113, the ingest existence checks
- `dedup.mjs` line 104 `canonRows`, exact `toLower` equality, typed `:Framework`, `LIMIT 1` with no `ORDER BY`
- `pipeline.mjs` lines 156, 167, 185, 197, the four name-resolved write endpoints

**Section 3: reconcile against the roadmap.** The Phase 260 roadmap entry and FIX-03 both say
"all four name-matching readers + the dedup write-path consumer". State the number the census
actually found. If it is not four, say so plainly and explain what the four most likely referred
to (a shape count, or the arm1 catalogue only). Do NOT silently adopt four, and do NOT silently
adopt your own number without naming the discrepancy. This reconciliation is itself a
deliverable: the roadmap's figure was never derived from a census, which is exactly why the
phase carried a research flag.

**Section 4: which of these are the "dedup write-path consumer".** The requirement names one
consumer. Identify it precisely: `resolveFramework`'s `canonRows` query plus the alias statement
it feeds, consumed by `buildPlan` in `src/ingest/pipeline.mjs`, which merges `dedup.statements`
into `plan.statements`. Note that `buildStructureStatements`'s four name-resolved endpoints are a
SECOND write-path name consumer in the same file and decide, explicitly, whether they are in
scope for FIX-03's matrix. Whatever you decide, record it as a decision with a reason.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'src/.*\.mjs:[0-9]' docs/2026-08-20-MATRIX-name-matching-readers.md</automated>
  </verify>

  <acceptance_criteria>
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'src/.*\.mjs:[0-9]' docs/2026-08-20-MATRIX-name-matching-readers.md` outputs a number >= 12 (every census row cites file plus line).
    - Every file:line citation resolves. Verify mechanically: for each cited `path:line`, `sed -n '<line>p' <path>` prints a line containing `name`. Run this as a loop and report any citation that fails; a stale citation is a census error, not a formatting nit.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -q 'four' docs/2026-08-20-MATRIX-name-matching-readers.md` exits 0 AND the surrounding text explicitly confirms or corrects the roadmap figure (section 3 exists and names a number).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -q 'Measured:' docs/2026-08-20-MATRIX-name-matching-readers.md` exits 0 and the date on that line equals `date +%F` at run time.
    - The inclusion rule (section 1) appears BEFORE the census table in the file. Verify: `grep -n 'inclusion rule' docs/2026-08-20-MATRIX-name-matching-readers.md` returns a line number lower than `grep -n '| # | Reader' docs/2026-08-20-MATRIX-name-matching-readers.md`.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -q 'buildStructureStatements' docs/2026-08-20-MATRIX-name-matching-readers.md` exits 0 (section 4 makes the second write-path consumer an explicit in-or-out decision).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -Pn '\x{2014}' docs/2026-08-20-MATRIX-name-matching-readers.md` exits 1 with no output.
    - Committed locally, NOT pushed.
  </acceptance_criteria>

  <done>
    Every name-matching reader in the Brain is listed with a file:line citation that resolves, the
    inclusion rule that produced the list is stated before the list, the roadmap's "four" is
    confirmed or corrected in writing, and the dedup write-path consumer is identified precisely
    with the second write-path consumer ruled in or out on the record.
  </done>
</task>

<task type="auto">
  <name>Task 2: Measure the BEFORE half against a fixed fragment corpus</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-name-matching-readers.mjs (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-MATRIX-name-matching-readers.md (modify, append sections 5-7)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-order-collisions.mjs`: the probe skeleton, argument handling, and the pattern of writing a markdown artifact plus printing to stdout. If already in context from plan 260-01, do not re-read.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-wave-attribution.mjs` lines 52-100: `resolveReadKey`, `call`, `q`, `initHandshake`.
    - The census table this plan's Task 1 wrote (sections 2 and 4 of the matrix doc).
  </read_first>

  <action>
**`scripts/probe-name-matching-readers.mjs`.** Read-only. Reuse the shared transport; mint no
second HTTP client. Accept one optional argument `--after`. With `--after` it writes into the
matrix doc's AFTER slot instead of the BEFORE slot, so plan 260-03 re-runs the exact same script
rather than authoring a second measurement instrument. Both modes print to stdout.

Define a FIXED fragment corpus as a module-level exported constant `FRAGMENTS` so the AFTER run
provably uses the same inputs. Choose fragments that exercise all four topologies, and say in a
comment which topology each one is for:
- an alias-heavy fragment: `scenario`
- a fragment whose canonical is a parenthetical expansion: `mece`
- a fragment with multiple `:Framework`-labelled variants: `jobs to be done`
- a fragment whose alias source is NOT `:Framework`-labelled (pick one from probe [N-4] below)
- a fragment matching a cluster with NO `:Framework`-bearing copy (pick one from probe [N-5])
- a control fragment that matches nothing: `zzzz-no-such-thing-v1`
- the FLOOR-03 anchor: `Scenario Planning`

Probes:

- **[N-1] Current `normalizeName` output per fragment.** Run the EXACT query body from
  `src/arm1-orchestrator.mjs` lines 67-71, parameterised. Record, per fragment: the returned
  array, its length, and the count of entries that appear more than once (the cross-branch
  duplicate count). Do NOT call the deployed `normalize_framework_name` tool for this leg: the
  deployed build is the pre-fix build either way, and running the query body directly keeps the
  BEFORE and AFTER legs on the same instrument.
- **[N-2] How many returned entries are themselves aliases.** For each fragment's [N-1] result,
  `MATCH (f:Framework) WHERE f.name IN $names AND exists((f)-[:ALIAS_OF]->(:Framework)) RETURN collect(f.name) AS alias_entries`.
  This is the direct-branch defect measured per fragment.
- **[N-3] Alias-population baselines.**
  `MATCH (f:Framework) WHERE exists((f)-[:ALIAS_OF]->(:Framework)) RETURN count(f) AS fw_alias_to_fw`, and
  `MATCH (f:Framework) WHERE exists((f)-[:ALIAS_OF]->()) RETURN count(f) AS fw_alias_to_any`.
  The difference is the exact blast-radius delta between a target-typed exclusion and an untyped one.
- **[N-4] The invisible population.**
  `MATCH (a)-[:ALIAS_OF]->(canon:Framework) WHERE NOT a:Framework RETURN count(DISTINCT a) AS nonfw_alias_sources`
  plus a `LIMIT 20` sample of `a.name, labels(a), canon.name`. These are alias surface forms the
  current alias branch cannot see because it types the SOURCE as `:Framework`.
- **[N-5] The majority topology.**
  `MATCH (n) WHERE n.name IS NOT NULL AND size(n.name) < 60 WITH toLower(trim(n.name)) AS key, count(*) AS c, sum(CASE WHEN n:Framework THEN 1 ELSE 0 END) AS fw WHERE c > 1 RETURN count(key) AS clusters, sum(CASE WHEN fw = 0 THEN 1 ELSE 0 END) AS no_framework_copy`
  plus a `LIMIT 10` sample of no-canonical-copy cluster keys. This is 260-RESEARCH.md's open
  question 1 answered with live data.
- **[N-6] Alias chains longer than one hop.**
  `MATCH (a)-[:ALIAS_OF]->(b)-[:ALIAS_OF]->(c) WHERE id(a) <> id(b) AND id(b) <> id(c) RETURN count(*) AS two_hop_chains` and a `LIMIT 10` sample of `a.name, b.name, c.name, labels(c)`. Then, restricted to chains whose FINAL node is `:Framework`, the count. This decides the hop-depth ruling.
- **[N-7] Self-loop interference with a multi-hop traversal.**
  `MATCH (a)-[:ALIAS_OF*1..2]->(c:Framework) WHERE id(a) = id(c) RETURN count(*) AS loops_reached` -- how much noise a `*1..2` form would traverse given the self-loop population plan 260-01 measured. A non-zero result is the reason the hop-depth ruling may need to be gated on Phase 261 CER-05.
- **[N-8] Proposed-query rehearsal per fragment, READ-ONLY.** Run the candidate replacement body
  (exactly the six-clause form named in plan 260-03's behavior block) against every fragment and
  record its output length and duplicate count alongside [N-1]'s. This is the AFTER prediction
  measured on live data BEFORE the code changes, which is what makes plan 260-03's real AFTER run
  a confirmation rather than a discovery.

Write sections 5, 6 and 7 into the matrix doc:
- **Section 5: BEFORE / rehearsal table**, one row per fragment: `fragment | before_len | before_dupes | before_alias_entries | rehearsal_len | rehearsal_dupes | delta`.
- **Section 6: population baselines**, the [N-3] through [N-7] numbers as a table with one
  sentence each on what the number means for the ruling.
- **Section 7: AFTER (populated by plan 260-03)**, a placeholder heading plus the literal line
  `Not yet measured. Populated by re-running this probe with --after after the FIX-03 change lands.`
  so the empty slot is visible rather than absent.

If any probe fails, record `[ERR] <label>: <message>` as a row. A failed probe is a recorded
result; a skipped probe is not.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node scripts/probe-name-matching-readers.mjs && grep -q 'Not yet measured' docs/2026-08-20-MATRIX-name-matching-readers.md</automated>
  </verify>

  <acceptance_criteria>
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node scripts/probe-name-matching-readers.mjs` exits 0 and prints labelled results for N-1 through N-8.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node -e "import('./scripts/probe-name-matching-readers.mjs').then(m=>process.exit(Array.isArray(m.FRAGMENTS) && m.FRAGMENTS.length>=7 ? 0 : 1))"` exits 0 (the corpus is exported and fixed, so the AFTER run cannot quietly use different inputs).
    - The section 5 table has one row per fragment in `FRAGMENTS` and no more. Verify the row count mechanically against `FRAGMENTS.length`.
    - For the `scenario` fragment, `before_dupes` is >= 1. If it is 0, the cross-branch duplicate defect did not reproduce: stop and re-derive before writing any ruling, because the whole FIX-03 case rests on it.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -q 'nonfw_alias_sources' docs/2026-08-20-MATRIX-name-matching-readers.md` exits 0 and the recorded value is a positive integer (the invisible population is measured, not asserted).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -q 'no_framework_copy' docs/2026-08-20-MATRIX-name-matching-readers.md` exits 0 (research open question 1 has a live answer).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -q 'two_hop_chains' docs/2026-08-20-MATRIX-name-matching-readers.md` exits 0 and the sample rows are present (the 2-hop question has a live example, not a hypothetical).
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'fetch(' scripts/probe-name-matching-readers.mjs` outputs `0`.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -Pn '\x{2014}' scripts/probe-name-matching-readers.mjs docs/2026-08-20-MATRIX-name-matching-readers.md` exits 1 with no output.
    - Committed locally, NOT pushed.
  </acceptance_criteria>

  <done>
    A re-runnable probe exists with a fixed, exported fragment corpus; the BEFORE numbers and a
    read-only rehearsal of the proposed query are both recorded per fragment; the four population
    baselines that decide the rulings are measured; and the AFTER slot exists as a visible,
    explicitly-empty placeholder.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rule on every reader, including the ones that will not change</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-MATRIX-name-matching-readers.md (modify, append sections 8-10)
  </files>

  <read_first>
    - Sections 1 through 7 of the matrix doc as written by Tasks 1 and 2. Do not re-read the source files; the census already carries what you need.
    - 260-RESEARCH.md, "What this means for FIX-03 scoping", items 1 through 4, and "Open questions for the planner", questions 1 and 3.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-RCA-alias-self-loop-minting.md` section 6 (written by plan 260-01, same wave). If it does not exist yet because 260-01 is still running, note the dependency in the ruling and proceed; the hop-depth ruling references its self-loop count, so record which number you used and where it came from.
  </read_first>

  <action>
Append the rulings. This is the section that makes the matrix a gate rather than a report.

**Section 8: per-reader ruling table.** One row per census row from section 2, with these
columns, and NO row left blank:

| # | Reader | Traverses ALIAS_OF today (measured) | Should it | Hop depth | Behaviour on a no-canonical-copy cluster | Ruling |

`Ruling` is one of exactly three values, and nothing else:
- `CHANGE-260` -- plan 260-03 modifies this reader in this phase.
- `UNCHANGED-DELIBERATE` -- examined, ruled to stay as it is. Requires a reason in the row.
- `DEFERRED-<gate>` -- should change but cannot yet. Requires the named gate, for example
  `DEFERRED-CER-05` for anything blocked on the self-loop cleanup.

`UNCHANGED-INCIDENTAL` is not an allowed value. That is the state this document exists to
eliminate (research open question 3).

Guidance for the rulings, which you must confirm or overturn against the measurements rather
than adopt:
- `normalizeName` DIRECT branch: `CHANGE-260`. It returns [N-2]'s alias entries as canonical.
  This is the requirement's named target.
- `normalizeName` ALIAS branch: rule on whether the SOURCE stays typed `:Framework`. FIX-03's
  wording is "typed `:Framework` target", which reads as a ruling that the TARGET is typed and
  therefore the SOURCE need not be. [N-4]'s `nonfw_alias_sources` is the value of untyping it.
  If you rule `CHANGE-260`, say what new class of node can now influence a result and why that
  is correct rather than merely bigger.
- `brain-tools.mjs` anchors: these are UNTYPED and they anchor traversals, not name resolution
  for the user. Rule explicitly. If `UNCHANGED-DELIBERATE`, the reason must be about what the
  reader is FOR, not about effort.
- `community-summary.mjs`: rule explicitly.
- `validator.mjs`: this decides whether an ingest is accepted. An alias-aware existence check
  changes accept/reject behaviour, which is a different risk class from a read. Rule explicitly
  and say which risk class you weighed.
- `dedup.mjs canonRows`: this is the dedup write-path consumer the requirement names. Rule
  explicitly, and note the interaction with plan 260-01's guard (260-01 deliberately left the
  `LIMIT 1` non-determinism in place; if your ruling here is `CHANGE-260` you are contradicting
  that, so either reconcile it in writing or rule `DEFERRED`).
- `pipeline.mjs buildStructureStatements` endpoints: rule per section 4's in-or-out decision.

**Section 9: the four topology questions, answered.** Answer 260-RESEARCH.md's four scoping
questions in order, each with the measurement that supports it:
1. Does it currently traverse `ALIAS_OF`? (per reader, from section 8)
2. Should it, given what it is for?
3. Hop depth, and the behaviour on a 2-hop chain whose intermediate node is itself an alias.
   Use [N-6]'s live sample. Name the concrete chain. Then state the hop-depth ruling for
   `normalizeName` and, if it is 1 hop, state the gate that would justify moving to 2 and cite
   [N-7]'s self-loop interference number as the reason it is gated rather than declined.
4. Behaviour on a cluster with NO `:Framework`-bearing copy, which [N-5] measures as the
   MAJORITY case. State what each `CHANGE-260` reader does on such a cluster. If the answer is
   "returns nothing, same as before", say that plainly: an unchanged behaviour on the majority
   topology is a finding, not a gap.

**Section 10: the gate.** A short section stating the exit condition for this document, in
imperative form: plan 260-03 may implement ONLY the readers marked `CHANGE-260`; each
`CHANGE-260` row names the file and line it authorises; and plan 260-03's AFTER run must fill
section 7 with the same probe, same fragments. Add one sentence recording that any reader whose
ruling changes later needs a new dated section here, never an edit in place, because the
BEFORE numbers are evidence.

**One more thing that must not be skipped.** If the rulings imply a change to a reader that
plan 260-03's files_modified does not cover, say so explicitly at the end of section 10 and stop
rather than quietly narrowing the ruling to fit the plan. Widening the phase is a navigator
decision, not a planner or executor one.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'CHANGE-260\|UNCHANGED-DELIBERATE\|DEFERRED-' docs/2026-08-20-MATRIX-name-matching-readers.md</automated>
  </verify>

  <acceptance_criteria>
    - Every row in section 8 carries exactly one of `CHANGE-260`, `UNCHANGED-DELIBERATE`, `DEFERRED-<gate>`. Verify: the count of ruling tokens in section 8 equals the number of census rows in section 2.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'UNCHANGED-INCIDENTAL' docs/2026-08-20-MATRIX-name-matching-readers.md` outputs `0` outside of the sentence that forbids it. (Check the one permitted occurrence is the prohibition itself.)
    - Every `UNCHANGED-DELIBERATE` row has a non-empty reason cell. Verify by eye and state the count in the summary; an empty reason is the incidental state wearing a deliberate label.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -c 'CHANGE-260' docs/2026-08-20-MATRIX-name-matching-readers.md` outputs a number >= 1 (at least `normalizeName`'s direct branch is authorised, or FIX-03 has no target).
    - Section 9 answers all four numbered questions. Verify: `grep -c '^[0-9]\. ' ` within section 9 returns >= 4, and question 3's answer names a concrete chain from the [N-6] sample by node name.
    - Section 9 question 4 explicitly references the [N-5] `no_framework_copy` number (the majority topology is addressed, not skipped).
    - Section 10 names the files and lines each `CHANGE-260` row authorises, and those files are a subset of plan 260-03's `files_modified` (`src/arm1-orchestrator.mjs`, `src/server.mjs`). If they are not, the discrepancy is stated in writing at the end of section 10 and the task reports it rather than narrowing the ruling.
    - `cd /home/jsagi/dev/ProblemsWorthSolving-Brain && grep -Pn '\x{2014}' docs/2026-08-20-MATRIX-name-matching-readers.md` exits 1 with no output.
    - Committed locally, NOT pushed.
  </acceptance_criteria>

  <done>
    Every enumerated reader carries an explicit ruling from a closed three-value vocabulary, no
    reader is left incidental, the four research scoping questions are answered against measured
    data with the majority topology covered, the 2-hop question cites a real chain by name, and
    the gate for plan 260-03 is written as an authorisation list rather than a suggestion.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-supplied name fragment -> graph query | `normalizeName`, `loadFramework` and the brain-tools anchors all take a string a model chose. What they return is what the model then treats as canonical methodology. |
| alias layer -> retrieval result | An `ALIAS_OF` edge is an assertion that one name refers to another concept. Traversing it widens what a reader can return; not traversing it silently narrows the answer. |
| read-tier HTTPS probe -> canon | `brain_query` with a read key. Read-only by service contract; this plan issues zero writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260-07 | Spoofing | an alias surface form returned inside `canonical_matches` as if it were a canonical framework name | mitigate | measured per fragment as [N-2]; the direct-branch exclusion is ruled `CHANGE-260` in section 8 and implemented in plan 260-03 |
| T-260-08 | Tampering | untyping the alias SOURCE lets a `:Person` or `:Book` node influence a framework resolution | mitigate | the target stays typed `:Framework`, so only nodes that assert "this name refers to that framework" can contribute; [N-4]'s sample is inspected before the ruling, and the ruling states what new class can now influence a result |
| T-260-09 | Repudiation | a matrix authored after the change, so the "gate" documents the outcome instead of constraining it | mitigate | the BEFORE half and all rulings land in wave 1; the AFTER slot ships as a visible empty placeholder; plan 260-03 depends on this plan |
| T-260-10 | Information Disclosure | user room data reaching the Brain (Canon Part 8) | accept, with a stated control | read-only measurement of the Brain's own corpus. The probe returns node names, labels and `ALIAS_OF` edges. There is no LOCAL side. |
| T-260-11 | Denial of Service | a `*1..2` alias traversal amplified by the live self-loop population | mitigate | [N-7] measures the interference before any hop-depth ruling is made, and the ruling is allowed to be `DEFERRED-CER-05` rather than forced |
| T-260-SC | Tampering | npm/pip/cargo installs | mitigate | not applicable: zero new packages. The probe reuses the repo's existing fetch-based `call`/`q` seam. No package-manager install task exists in this plan, so no legitimacy checkpoint is required. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

**New files in `/home/jsagi/dev/ProblemsWorthSolving-Brain` (this plan):**

| Path | Created by | What it is |
|---|---|---|
| `scripts/probe-name-matching-readers.mjs` | 260-02 Task 2 | Read-only HTTPS probe, N-1..N-8, `--after` flag switches the output slot |
| `docs/2026-08-20-MATRIX-name-matching-readers.md` | 260-02 Tasks 1, 2, 3 | The FIX-03 gate: census, BEFORE, rehearsal, rulings, AFTER placeholder |

**New exported symbols:**

| Symbol | Module | Shape |
|---|---|---|
| `FRAGMENTS` | `scripts/probe-name-matching-readers.mjs` | frozen array of raw name fragments, one per topology, shared by the BEFORE and AFTER runs |

**New closed vocabulary introduced by this phase:** the ruling values `CHANGE-260`,
`UNCHANGED-DELIBERATE`, `DEFERRED-<gate>`. Downstream phases reading the matrix should treat
these three as the complete set; `UNCHANGED-INCIDENTAL` is explicitly forbidden.

**Modified existing files:** none. This plan is additive only.

**No graph write occurs in this plan.** Every live interaction is a read-tier `brain_query`. No
`GRAPH-WRITE-LOG.md` row is owed. No admin window is opened or required.

**No MindrianOS-Plugin code is modified by this phase.**
</artifacts_this_phase_produces>

<verification>
- `node scripts/probe-name-matching-readers.mjs` exits 0 and prints N-1 through N-8 with no `[ERR]` rows.
- The matrix doc contains all ten sections in order, with section 7 explicitly empty and labelled as such.
- Every census file:line citation resolves to a line containing `name`.
- The ruling-token count in section 8 equals the census row count in section 2, and zero rows are unruled.
- `grep -Pn '\x{2014}'` across both files exits 1 with no output.
- No `git push` was executed; commits sit local for the FIX-04 batch.
</verification>

<success_criteria>
- FIX-03's gate exists BEFORE FIX-03's change, with a BEFORE measurement plan 260-03 can only
  confirm or contradict, never quietly redefine.
- The roadmap's unexamined "four name-matching readers" figure is replaced by a census with
  resolvable citations, and the discrepancy (if any) is stated rather than absorbed.
- Every reader carries an explicit ruling; no reader remains incidentally alias-unaware.
- The majority topology (clusters with no canonical copy) and the 2-hop chain case are both
  answered with live data and named examples, which is the specific gap 260-RESEARCH.md warned
  a minimally-scoped matrix would miss.
</success_criteria>

<output>
Create `.planning/phases/260-pipeline-fixes-brain-repo-one-pass-one-push/260-02-SUMMARY.md` when done.
</output>
