---
phase: 179-ignite-b1-starting-point-fix
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - lib/core/scratchpad-ops.cjs
  - tests/test-scratchpad-birth-whitelist.cjs
autonomous: true
requirements: [REQ-09]
canon_parts: [8, 9]
cirs_relationship:
  surfaces_added: []
  surfaces_modified:
    - scratchpad-ops
  surfaces_removed: []
  spine_consumed:
    - lib/core/scratchpad-ops.cjs
    - lib/core/navigation/room-birth.cjs
  gate_impact: "Widens the birth-answer journal whitelist so the B1 signal survives to B2. No invocable surface added/removed; modifies an existing LOCAL persistence chokepoint. Opens NO Brain wire."

must_haves:
  truths:
    - "writeScratchpadBirthAnswer persists role_blend + blueprint_family + hypothesis_text alongside the existing free_text + arrival_asset"
    - "drainBirthGateAnswers recovers all three new fields intact across a session boundary"
    - "absent new fields leave the entry shape byte-identical to today (additive, not breaking)"
  artifacts:
    - path: "lib/core/scratchpad-ops.cjs"
      provides: "widened writeScratchpadBirthAnswer whitelist"
      contains: "role_blend"
    - path: "tests/test-scratchpad-birth-whitelist.cjs"
      provides: "round-trip proof across a session boundary"
  key_links:
    - from: "writeScratchpadBirthAnswer"
      to: "birth_gate_answers entry"
      via: "additive optional-field whitelist"
      pattern: "role_blend|blueprint_family|hypothesis_text"
---

<objective>
Wave 2. Widen the scratchpad birth-answer whitelist so the B1 persona/hypothesis signal survives across a session boundary to B2. Today writeScratchpadBirthAnswer (scratchpad-ops.cjs:225-226) persists ONLY free_text + arrival_asset; ignite.md already PASSES role_blend + blueprint_family but they are SILENTLY DROPPED, so the B1 signal dies before birth (SPEC Req 9; CONTEXT code-context highlight).

Purpose: this unblocks every door -- the {role_blend, blueprintFamily, arrival_asset, hypothesis_text} tuple the 4-door B1 (Wave 3) captures must round-trip through this bus to reach the untouched birthRoom contract. It must land before the doors consume it.
Output: a widened whitelist in scratchpad-ops.cjs (+role_blend, +blueprint_family, +hypothesis_text) + a round-trip test across a session boundary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/179-ignite-b1-starting-point-fix/179-SPEC.md
@.planning/phases/179-ignite-b1-starting-point-fix/179-CONTEXT.md
@.planning/phases/179-ignite-b1-starting-point-fix/179-RESEARCH.md
@docs/MINDRIAN-CANON.md
@lib/core/scratchpad-ops.cjs
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (excluded from source-grounding drift verification):
- The three new optional whitelist fields on the birth_gate_answers entry: `role_blend`, `blueprint_family`, `hypothesis_text`
- `tests/test-scratchpad-birth-whitelist.cjs` -- the round-trip proof suite
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen writeScratchpadBirthAnswer + prove the round-trip across a session boundary</name>
  <read_first>
    - lib/core/scratchpad-ops.cjs:214-235 (writeScratchpadBirthAnswer: the entry object built at :217-223 with gate_id/option_key/canonical_verb/alias_label/ts always-present, then the optional-field block at :225-226 that whitelists ONLY free_text + arrival_asset via `if (typeof answer.X === 'string') entry.X = answer.X`)
    - lib/core/scratchpad-ops.cjs:237-260 (drainBirthGateAnswers JSDoc: it delegates to room-birth.cjs and replays each entry as a typed edge; the new fields must survive the read side, not just the write side)
    - lib/core/navigation/room-birth.cjs:144-248 (drainBirthGateAnswers internal impl that reads the entries back -- confirm the new fields pass through unmolested; do NOT change the 7-step txn or the edge-write logic, only confirm the read recovers the additive fields)
    - docs/MINDRIAN-CANON.md Part 8 (birth_gate_answers is LOCAL-only; the widened fields never cross to Brain) + Part 9 (the journal/replay bus)
  </read_first>
  <behavior>
    - Write then read: writeScratchpadBirthAnswer({gate_id:'B1', role_blend:{founder:1.0}, blueprint_family:'venture', hypothesis_text:'I believe X drives Y', ...}) persists all three; a fresh readScratchpad (simulating a new session) recovers role_blend deep-equal, blueprint_family ===, hypothesis_text === intact.
    - Additive shape: writeScratchpadBirthAnswer with none of the three new fields produces an entry byte-identical to today (the new keys are absent, not present-as-null).
    - Type discipline: role_blend persists when it is a plain object (the {key:1.0} blend); blueprint_family + hypothesis_text persist when strings; malformed types are dropped (mirror the existing `if (typeof answer.X === 'string')` guard idiom, with role_blend guarded by an isPlainObject-style check).
  </behavior>
  <action>
    In lib/core/scratchpad-ops.cjs writeScratchpadBirthAnswer, extend the optional-field block (currently free_text + arrival_asset at :225-226) additively: persist `blueprint_family` and `hypothesis_text` with the same `if (typeof answer.X === 'string') entry.X = answer.X` guard, and persist `role_blend` with an object-shape guard (only when answer.role_blend is a plain non-null non-array object, mirroring the existing defensive idiom). Do NOT touch the always-present fields (:217-223) or _writeAtomic. Confirm (read-side, no code change unless a drop is found) that room-birth.cjs drainBirthGateAnswers recovers the additive fields -- the drain reads the whole entry object, so additive keys ride for free; assert this in the test rather than mutating the drain. Author tests/test-scratchpad-birth-whitelist.cjs proving the round-trip across a simulated session boundary (write, then re-read the scratchpad fresh). Part 8: LOCAL-only; the widened fields are journaled to the local scratchpad, never to Brain. No em-dashes; CJS.
  </action>
  <verify>
    <automated>node tests/test-scratchpad-birth-whitelist.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-scratchpad-birth-whitelist.cjs` exits 0 and asserts: role_blend (object) + blueprint_family (string) + hypothesis_text (string) written by writeScratchpadBirthAnswer are recovered intact by a fresh scratchpad read (the session-boundary round-trip).
    - The test asserts an entry written with NONE of the three new fields is byte-identical to the pre-change shape (additive, not breaking): the three keys are absent on the entry, not present-as-null.
    - `grep -n 'role_blend' lib/core/scratchpad-ops.cjs` and `grep -n 'blueprint_family' lib/core/scratchpad-ops.cjs` and `grep -n 'hypothesis_text' lib/core/scratchpad-ops.cjs` each return at least one match.
    - `grep -nE 'fetch|http|curl|brain\.mindrian|tavily|mcp__brain' lib/core/scratchpad-ops.cjs` returns zero NEW matches versus baseline (Part 8 LOCAL sweep over the touched surface).
    - `grep -nP '\x{2014}|\x{2013}' lib/core/scratchpad-ops.cjs tests/test-scratchpad-birth-whitelist.cjs` returns zero matches.
  </acceptance_criteria>
  <done>writeScratchpadBirthAnswer persists role_blend + blueprint_family + hypothesis_text additively, drainBirthGateAnswers recovers them across a session boundary, the entry shape is byte-identical when the fields are absent, and the round-trip test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| B1 answer -> scratchpad journal | LOCAL persistence; the role_blend/hypothesis_text bytes never egress to Brain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-04 | Information Disclosure | hypothesis_text + role_blend to Brain | mitigate | LOCAL-only journal; Part 8 grep gate over scratchpad-ops.cjs finds zero new network/Brain symbols |
| T-179-05 | Tampering | malformed role_blend persisted | mitigate | object-shape guard mirrors the existing typeof-string guard; the test exercises a malformed input |
| T-179-SC | Tampering | npm/pip/cargo installs | mitigate | zero new deps (Node built-ins only); no install task -- no legitimacy checkpoint required |
</threat_model>

<verification>
- The whitelist persists three new fields additively; the round-trip across a session boundary is proven.
- The entry shape is unchanged when the new fields are absent.
- Part 8 LOCAL sweep over scratchpad-ops.cjs is clean.
</verification>

<success_criteria>
- `node tests/test-scratchpad-birth-whitelist.cjs` green (round-trip + additive-shape).
- Three new fields grep-present in scratchpad-ops.cjs.
- Part 8 grep gate clean; no em-dashes.
</success_criteria>

<output>
Create `.planning/phases/179-ignite-b1-starting-point-fix/179-02-SUMMARY.md` when done.
</output>
