---
phase: 179-ignite-b1-starting-point-fix
plan: 03
type: execute
wave: 3
depends_on: [179-02]
files_modified:
  - commands/ignite.md
  - tests/test-b1-four-door-contract.cjs
autonomous: true
requirements: [REQ-02, REQ-03, REQ-12]
canon_parts: [2, 2a, 3, 8, 9]
cirs_relationship:
  surfaces_added: []
  surfaces_modified:
    - ignite
  surfaces_removed: []
  spine_consumed:
    - commands/ignite.md
    - lib/core/persona-override.cjs
    - lib/core/shallow-doc-parser.cjs
    - lib/core/scratchpad-ops.cjs
    - lib/core/navigation/room-birth.cjs
  gate_impact: "Rewrites the ignite B1 surface as ONE persona-first 4-door card fired via the AskUserQuestion primitive (Phase 88.2 invariant; honored by the Wave-1 GA-4 interceptor). Single-axis role_blend threads through the UNTOUCHED room-birth.cjs:420-433 txn. Opens NO Brain wire."

must_haves:
  truths:
    - "B1 fires AskUserQuestion 'Who are you arriving as?' as one card with four doors (Persona / CV / Hypothesis / Free-Text)"
    - "Each door resolves to a valid {role_blend, blueprintFamily, arrival_asset} tuple threaded into birthRoom opts"
    - "Door 1 sets role_blend from the frozen ROLE_BLEND_KEYS and derives blueprintFamily (researcher/student/domain_expert->exploration; founder/operator/investor->venture)"
    - "Single-pick gates render arrow-key single-select; the keyboard contract routes through the AskUserQuestion primitive, never an ASCII box only"
    - "The single-axis role_blend is written exactly once at birth; the 7-step txn + approvedBy gate stay byte-unchanged"
  artifacts:
    - path: "commands/ignite.md"
      provides: "the ONE canonical persona-first 4-door B1"
      contains: "Who are you arriving as"
    - path: "tests/test-b1-four-door-contract.cjs"
      provides: "four-door tuple + ROLE_BLEND_KEYS import + birth-txn-byte-unchanged proof"
  key_links:
    - from: "commands/ignite.md B1"
      to: "lib/core/persona-override.cjs ROLE_BLEND_KEYS"
      via: "frozen vocab import (never redefine)"
      pattern: "ROLE_BLEND_KEYS|role_blend"
    - from: "commands/ignite.md B1"
      to: "writeScratchpadBirthAnswer"
      via: "thread {role_blend, blueprint_family, arrival_asset} through the Wave-2 widened whitelist"
      pattern: "writeScratchpadBirthAnswer"
---

<objective>
Wave 3. Make ignite B1 ONE persona-first 4-door card -- Door 1 Persona pick, Door 2 CV, Door 3 Hypothesis, Door 4 Free-Text -- each resolving to {role_blend, blueprintFamily, arrival_asset}, threaded through the Wave-2 widened scratchpad whitelist into the UNTOUCHED birthRoom contract. Every gate routes through the AskUserQuestion primitive (single-pick gates arrow-key single-select); no ASCII-box-only render (SPEC Req 2, 3, 12; CONTEXT wave-order step "4-door persona-first B1").

Purpose: B1 today ships the prose stopgap (e22b9ea4) but the doors are not the engineered feature, and there is no Door 3 (hypothesis); this lands the persona-first card whose answer is the {role_blend, blueprintFamily, arrival_asset} tuple every downstream wave consumes. The Wave-1 interceptor enforces that the card actually fires; this plan defines what fires.
Output: a rewritten B1 block in commands/ignite.md (one card, four doors, keyboard contract, single-axis role_blend) + a contract test proving the four-door tuple, the frozen-vocab import, and the byte-unchanged birth txn.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/179-ignite-b1-starting-point-fix/179-SPEC.md
@.planning/phases/179-ignite-b1-starting-point-fix/179-CONTEXT.md
@.planning/phases/179-ignite-b1-starting-point-fix/179-RESEARCH.md
@.planning/phases/174-hypothesis-based-ignite/174-RESEARCH.md
@.planning/phases/174-hypothesis-based-ignite/174-HANDOFF.md
@docs/MINDRIAN-CANON.md
@commands/ignite.md
@lib/core/persona-override.cjs
@lib/core/shallow-doc-parser.cjs
@lib/core/navigation/room-birth.cjs
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (excluded from source-grounding drift verification):
- The Door 3 (Hypothesis) and Door 4 (Free-Text) B1 doors in commands/ignite.md (Doors 1+2 already exist as the prose stopgap; this consolidates four into one canonical card)
- The `hypothesis-arrival` arrival_asset value
- `tests/test-b1-four-door-contract.cjs` -- the four-door contract proof
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ignite B1 as the canonical persona-first 4-door card (keyboard + checkbox contract)</name>
  <read_first>
    - commands/ignite.md (the "Gate B1 -- Starting Point" block, ~lines 75-118: the FIRE-THE-CARD doctrine, the persona-first options stopgap with the 6 personas + Paste-my-CV, the writeScratchpadBirthAnswer record-the-answer call, the Tri-Polar degradation line; THIS is the surface to rewrite into the canonical 4-door form)
    - lib/core/persona-override.cjs:80-94 (ROLE_BLEND_KEYS frozen 7-key vocab + normalizeRoleKey -- the B1 doctrine cites these by name; the card sets role_blend from this frozen set, never redefines it)
    - lib/core/shallow-doc-parser.cjs:72-86 blendFromCanonicalRole (single-axis {key:1.0} -- Door 1 + Door 2 resolve role_blend to a single-axis blend; the weighted computer is OUT of scope per SPEC boundary) + parseRoleHints + extractShallow (Door 2 CV reuse, verbatim)
    - lib/core/navigation/room-birth.cjs:420-433 (the role_blend write at birth STEP 1 via writeUserMdAtomic -- the B1 doctrine passes opts.roleBlend through; the 7-step txn + approvedBy gate at :319-326 stay BYTE-UNCHANGED; this plan does NOT edit room-birth.cjs)
    - docs/MINDRIAN-CANON.md Part 3 (Shape F via the AskUserQuestion primitive, the F.1 keyboard contract), Part 2/2a (persona = role-blend x journey-stage; journey-stage stays inert at Ordinary World per SPEC boundary), Part 8 (CV/hypothesis handles via auditQueryString; role_blend weights never cross to Brain)
  </read_first>
  <action>
    Rewrite the commands/ignite.md B1 block into ONE canonical persona-first 4-door card. Door 1 (Persona pick, default): AskUserQuestion header "Arrival", question "Who are you arriving as?", single-select arrow-key, options = the 6 personas resolving role_blend from ROLE_BLEND_KEYS (researcher/student/founder/operator/investor/domain_expert) and deriving blueprintFamily (researcher/student/domain_expert -> exploration; founder/operator/investor -> venture). Door 2 (CV): arrival_asset=cv-upload; run detect_dual_path -> extract_shallow (reuse verbatim), resolve role_blend via blendFromCanonicalRole (single-axis), venture -> blueprintFamily. Door 3 (Hypothesis): arrival_asset=hypothesis-arrival; capture one falsifiable "I believe ___" (the full hypothesis family + abstraction gate land in Waves 4-5; here Door 3 captures the statement + sets arrival_asset and routes role_blend if known). Door 4 (Free-Text): the AskUserQuestion Other/free-text row; Larry interprets and routes to one of the above. State explicitly that every single-pick gate (Door 1 persona, Door 4 routing) renders as an arrow-key-navigable single-select AskUserQuestion card -- never an ASCII box only (the Wave-1 GA-4 interceptor catches a violation). Thread the answer through writeScratchpadBirthAnswer with role_blend + blueprint_family + arrival_asset + (for Door 3) hypothesis_text populated -- the Wave-2 widened whitelist now persists them. Keep the FIRE-THE-CARD doctrine and the Tri-Polar card-incapable-surface line. Do NOT use prohibited scope-reduction language ("v1", "static for now", "placeholder"); each door delivers its full {role_blend, blueprintFamily, arrival_asset} tuple per D-02/D-03. No em-dashes; preserve the B2/B3 blocks below unchanged.
  </action>
  <verify>
    <automated>node tests/test-b1-four-door-contract.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-b1-four-door-contract.cjs` exits 0 and asserts the ignite.md B1 block names all four doors (Persona, CV, Hypothesis, Free-Text) and, for each, an arrival_asset + a path to a {role_blend, blueprintFamily, arrival_asset} tuple.
    - The test asserts the B1 block references `ROLE_BLEND_KEYS` (or the 6 named persona keys) and does NOT redefine the frozen vocabulary inline.
    - The test asserts the blueprintFamily derivation mapping is present: researcher/student/domain_expert -> exploration; founder/operator/investor -> venture.
    - The test asserts the B1 block routes the answer through `writeScratchpadBirthAnswer` with role_blend + blueprint_family + arrival_asset (the Wave-2 whitelist consumers).
    - The test asserts the keyboard contract prose: single-pick gates render as arrow-key single-select via AskUserQuestion, no ASCII-box-only render.
    - `git diff --quiet HEAD -- lib/core/navigation/room-birth.cjs` exits 0 (the 7-step birth txn + approvedBy gate are byte-unchanged this plan).
    - `grep -nP '\x{2014}|\x{2013}' commands/ignite.md tests/test-b1-four-door-contract.cjs` returns zero matches.
  </acceptance_criteria>
  <done>commands/ignite.md carries one canonical persona-first 4-door B1 card (single-select keyboard contract via AskUserQuestion), each door resolves a {role_blend, blueprintFamily, arrival_asset} tuple threaded through the Wave-2 whitelist, the frozen ROLE_BLEND_KEYS are imported not redefined, room-birth.cjs is byte-unchanged, and the contract test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator B1 answer -> ignite flow | the persona pick / CV text / hypothesis text is untrusted; CV/hypothesis handles audited via auditQueryString before any egress |
| ignite B1 -> Brain | role_blend weights + user_id never cross; only the Larry/Brain scalar via translateLarryToBrain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-06 | Information Disclosure | role_blend weights / CV text to Brain | mitigate | CV/hypothesis handles via auditQueryString; role_blend never egresses; Part 8 doctrine stated in the B1 block |
| T-179-07 | Tampering | redefining the frozen ROLE_BLEND_KEYS | mitigate | B1 imports the frozen vocab; the test asserts no inline redefinition |
| T-179-08 | Tampering | breaking the byte-unchanged birth txn | mitigate | git-diff-quiet acceptance on room-birth.cjs |
| T-179-SC | Tampering | npm/pip/cargo installs | mitigate | zero new deps (doctrine + test only); no install task -- no legitimacy checkpoint required |
</threat_model>

<verification>
- B1 is one persona-first 4-door card via the AskUserQuestion primitive.
- Each door yields a valid tuple; the frozen vocab is imported not redefined.
- The single-axis role_blend threads through the byte-unchanged birth txn.
- Part 8 doctrine present; no Brain egress of role_blend.
</verification>

<success_criteria>
- `node tests/test-b1-four-door-contract.cjs` green.
- `git diff --quiet HEAD -- lib/core/navigation/room-birth.cjs` exits 0.
- ROLE_BLEND_KEYS imported; four doors + tuple + keyboard contract present; no em-dashes.
</success_criteria>

<output>
Create `.planning/phases/179-ignite-b1-starting-point-fix/179-03-SUMMARY.md` when done.
</output>
