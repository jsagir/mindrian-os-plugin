---
phase: 179-ignite-b1-starting-point-fix
plan: 06
type: execute
wave: 6
depends_on: [179-03]
files_modified:
  - commands/ignite.md
  - tests/test-cv-multiselect-and-engine1.cjs
autonomous: true
requirements: [REQ-04, REQ-08]
canon_parts: [2, 3, 8, 10]
cirs_relationship:
  surfaces_added:
    - cv-second-select (the Shape F multiSelect domain gate)
  surfaces_modified:
    - ignite
  surfaces_removed: []
  spine_consumed:
    - commands/ignite.md
    - lib/core/shallow-doc-parser.cjs
  gate_impact: "Adds the CV-second-select multiSelect:true CHECKBOX gate over extractDomains() output + auto-fires the Act 1 triple-filter (Engine 1) on arrival, surfacing results at a Decision Gate (APPROVE/REJECT/DEFER, Part 3), never silently cascaded. Opens NO Brain wire; CV/domain handles via auditQueryString."

must_haves:
  truths:
    - "After a CV parse, a Shape F multiSelect:true CHECKBOX card asks which 2-3 domains pull the navigator, consuming extractDomains() output"
    - "The multiSelect card is arrow-key navigable and records the navigator's 2-3 picks to the scratchpad"
    - "Arrival (persona/CV/hypothesis) auto-fires the Act 1 triple-filter math (Part 10 sub-claim 5) without an explicit command"
    - "Engine 1 findings surface at the next Decision Gate for APPROVE/REJECT/DEFER (Part 3), never auto-written to the room"
  artifacts:
    - path: "commands/ignite.md"
      provides: "Door 2 CV-second-select multiSelect + arrival auto-fire Engine 1 doctrine"
      contains: "multiSelect"
    - path: "tests/test-cv-multiselect-and-engine1.cjs"
      provides: "multiSelect-checkbox + extractDomains-consumed + auto-fire + gate-results proof"
  key_links:
    - from: "commands/ignite.md Door 2"
      to: "lib/core/shallow-doc-parser.cjs extractDomains"
      via: "multiSelect over extracted domain handles"
      pattern: "extractDomains|multiSelect"
    - from: "commands/ignite.md arrival"
      to: "Engine 1 (/mos:explore-domains) -> Decision Gate"
      via: "auto-fire the math, gate the results"
      pattern: "explore-domains|Decision Gate"
---

<objective>
Wave 6. Add the CV-second-select multiSelect domain gate + auto-fire Engine 1. Door 2 (CV), after the dual-path parse, fires a Shape F multiSelect:true CHECKBOX card over extractDomains() output ("which 2-3 domains pull you?"); and arrival (persona/CV/hypothesis) auto-fires the Act 1 triple-filter math (decomposition/whitespace/reverse-salient, Part 10 sub-claim 5) in the background, surfacing findings at the next Decision Gate for APPROVE/REJECT/DEFER (Part 3), never silently cascaded (SPEC Req 4, 8; CONTEXT decision: auto-fire the math, gate the results).

Purpose: the CV path produces domain handles but has no multiSelect gate (grep-confirmed zero code); and Engine 1 is shipped but not auto-fired on arrival. This wave wires both -- the checkbox over extractDomains and the auto-fire-then-gate flow.
Output: the Door 2 multiSelect doctrine + the arrival auto-fire-then-gate doctrine in ignite.md + the proof suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/179-ignite-b1-starting-point-fix/179-SPEC.md
@.planning/phases/179-ignite-b1-starting-point-fix/179-CONTEXT.md
@.planning/phases/179-ignite-b1-starting-point-fix/179-RESEARCH.md
@docs/MINDRIAN-CANON.md
@commands/ignite.md
@lib/core/shallow-doc-parser.cjs
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (excluded from source-grounding drift verification):
- The CV-second-select multiSelect:true checkbox gate doctrine in commands/ignite.md Door 2
- The arrival auto-fire-Engine-1-then-gate doctrine in commands/ignite.md
- `tests/test-cv-multiselect-and-engine1.cjs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: CV-second-select multiSelect over extractDomains + auto-fire Engine 1, gate the results</name>
  <read_first>
    - commands/ignite.md Door 2 (the CV door from Wave 3: arrival_asset=cv-upload, detect_dual_path -> extract_shallow; THIS task adds the second-select multiSelect after the parse) + the existing extractDomains reference near the B3/sweepDomainInsights block (ignite.md already calls extractDomains for the domain-insight sweep)
    - lib/core/shallow-doc-parser.cjs:303-344 extractDomains (produces up to 8 audited domain handles via auditQueryString; this is the multiSelect input -- reuse verbatim)
    - docs/MINDRIAN-CANON.md Part 3 (the multiSelect:true checkbox is a Shape F variant via the AskUserQuestion primitive; the Decision Gate returns APPROVE/REJECT/DEFER; Engine 1 results surface at the gate, never auto-cascaded) + Part 2 Engine 1 (the Act 1 triple-filter: decomposition + whitespace + reverse salient; /mos:explore-domains is the decomposition first move) + Part 10 sub-claim 5 (the triple-filter fires automatically on first material)
    - 179-RESEARCH.md reuse map (CV-second-select = NEW Shape F multiSelect consuming extractDomains; Engine 1 first move = REUSE /mos:explore-domains, CV/hypothesis arrival auto-fires it)
  </read_first>
  <action>
    In commands/ignite.md Door 2, add the CV-second-select doctrine: after detect_dual_path -> extract_shallow, fire a Shape F multiSelect:true CHECKBOX AskUserQuestion card titled "which 2-3 domains pull you?" populated from extractDomains() output (the audited handles), arrow-key navigable, multiple selections allowed; record the navigator's 2-3 picks to the scratchpad (via writeScratchpadBirthAnswer / the birth journal). Add the arrival auto-fire doctrine (applies to persona/CV/hypothesis arrival): on arrival, auto-fire the Act 1 triple-filter math (the decomposition/whitespace/reverse-salient sweep, Part 10 sub-claim 5) in the background WITHOUT an explicit command; the findings surface at the NEXT Decision Gate for APPROVE/REJECT/DEFER (Part 3), NEVER auto-written to the room. State the multiSelect:true contract explicitly (it is a CHECKBOX, distinct from the single-select doors). Part 8: CV/domain handles via auditQueryString (extractDomains already audits); role_blend weights never cross to Brain. Author tests/test-cv-multiselect-and-engine1.cjs. Do NOT use scope-reduction language. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-cv-multiselect-and-engine1.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-cv-multiselect-and-engine1.cjs` exits 0 and asserts the ignite.md Door 2 block renders the domain gate as a `multiSelect: true` (or "multiSelect:true") CHECKBOX card consuming `extractDomains()` output, arrow-key navigable, recording 2-3 picks to the scratchpad.
    - The test asserts the arrival auto-fire doctrine: arrival auto-fires the Act 1 triple-filter (Engine 1 / explore-domains) WITHOUT an explicit command, and the findings surface at a Decision Gate (APPROVE/REJECT/DEFER) rather than being auto-written.
    - The test exercises `extractDomains` against a generic CV-like fixture string and asserts it returns >= 1 audited handle (the multiSelect input is real, not stubbed).
    - `grep -n 'multiSelect' commands/ignite.md` returns at least one match; `grep -n 'extractDomains' commands/ignite.md` returns at least one match in the Door 2 region.
    - `grep -nP '\x{2014}|\x{2013}' commands/ignite.md tests/test-cv-multiselect-and-engine1.cjs` returns zero matches.
  </acceptance_criteria>
  <done>Door 2 fires a multiSelect:true checkbox over extractDomains() output recording 2-3 domain picks, arrival auto-fires the Engine 1 triple-filter math with results surfaced at a Decision Gate (never auto-cascaded), and the proof suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CV domain handles -> multiSelect | extractDomains audits each handle via auditQueryString before surfacing |
| Engine 1 findings -> room | findings gate at a Decision Gate, never silently written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-15 | Information Disclosure | domain handles to Brain | mitigate | extractDomains audits via auditQueryString; the handles stay LOCAL; Part 8 doctrine in Door 2 |
| T-179-16 | Repudiation | Engine 1 findings silently cascaded | mitigate | findings surface at a Decision Gate for APPROVE/REJECT/DEFER; the test asserts the gate, not auto-write |
| T-179-SC | Tampering | npm/pip/cargo installs | mitigate | zero new deps (doctrine + test only); no install task -- no legitimacy checkpoint required |
</threat_model>

<verification>
- Door 2 fires a multiSelect:true checkbox over extractDomains output.
- Arrival auto-fires Engine 1; findings gate at a Decision Gate.
- CV/domain handles audited; no Brain egress.
</verification>

<success_criteria>
- `node tests/test-cv-multiselect-and-engine1.cjs` green (multiSelect + extractDomains + auto-fire + gate).
- multiSelect + extractDomains grep-present in ignite.md; no em-dashes.
</success_criteria>

<output>
Create `.planning/phases/179-ignite-b1-starting-point-fix/179-06-SUMMARY.md` when done.
</output>
