---
phase: 179-ignite-b1-starting-point-fix
plan: 05
type: execute
wave: 5
depends_on: [179-04]
files_modified:
  - lib/core/abstraction-gate.cjs
  - tests/fixtures/abstraction-gate-neutral.json
  - commands/ignite.md
  - scripts/check-abstraction-fixture-neutral.cjs
  - tests/test-abstraction-gate.cjs
autonomous: true
requirements: [REQ-06]
canon_parts: [3, 8, 9]
cirs_relationship:
  surfaces_added:
    - abstraction-gate (the instances-vs-structures 3-option Shape F selector)
  surfaces_modified:
    - ignite
  surfaces_removed: []
  spine_consumed:
    - commands/ignite.md
    - lib/core/navigation/typed-claim.cjs
  gate_impact: "Adds the instances-vs-structures 3-option Shape F selector, fired ALWAYS for every Door 3 hypothesis (CONTEXT decision 2, Brain-grounded Systems-Thinking iceberg move; kills the net-new classifier risk). Committed domain-neutral fixture + a grep gate proving neutrality. The abstraction value persists with the hypothesis node. Opens NO Brain wire; NO AION-specific content enters the repo."

must_haves:
  truths:
    - "A 3-option Shape F selector renders INSTANCES / STRUCTURE / unsure and fires ALWAYS for every Door 3 hypothesis (not conditional)"
    - "The selector renders as an arrow-key single-select AskUserQuestion card (the keyboard contract)"
    - "The committed fixture is domain-neutral (a generic 'I believe X drives Y'), proven by a grep gate that finds zero user/venture-specific strings"
    - "The abstraction-level value persists with the hypothesis node"
  artifacts:
    - path: "lib/core/abstraction-gate.cjs"
      provides: "the 3-option instances-vs-structures selector + persistence helper"
      contains: "INSTANCES"
    - path: "tests/fixtures/abstraction-gate-neutral.json"
      provides: "the committed domain-neutral fixture (zero venture content)"
    - path: "scripts/check-abstraction-fixture-neutral.cjs"
      provides: "the domain-neutrality grep gate"
    - path: "tests/test-abstraction-gate.cjs"
      provides: "3-option render + always-fire + persist + neutrality proof"
  key_links:
    - from: "commands/ignite.md Door 3"
      to: "lib/core/abstraction-gate.cjs"
      via: "always-fire the 3-option selector after capturing the hypothesis"
      pattern: "abstraction"
    - from: "lib/core/abstraction-gate.cjs"
      to: "the hypothesis node"
      via: "persist the abstraction-level value with the claim"
      pattern: "abstraction_level|persist"
---

<objective>
Wave 5 (the riskiest net-new surface). Add the instances-vs-structures abstraction gate: a 3-option Shape F selector ("Are you testing specific INSTANCES, the general STRUCTURE, or unsure?") that fires ALWAYS for every Door 3 hypothesis (not conditional), with a committed domain-neutral fixture and a grep gate proving no venture-specific content entered the plugin repo (SPEC Req 6; CONTEXT decision 2: always-fire is Brain-grounded -- the Systems-Thinking iceberg lift to structure must be deliberately surfaced because navigators default to instances; always-fire also kills the net-new classifier risk).

Purpose: the expert's correction in the precedent was that the hypothesis anchored on the wrong abstraction layer (instances, not structures), so it optimized the wrong layer with confidence. This gate lets the navigator SET the abstraction level deliberately. The 3rd option ("unsure") absorbs the genuinely-undecided navigator. NO AION-specific content; the fixture is generic.
Output: lib/core/abstraction-gate.cjs (the selector + persistence) + a committed domain-neutral fixture + a neutrality grep gate + the always-fire wiring in ignite.md Door 3 + the proof suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/179-ignite-b1-starting-point-fix/179-SPEC.md
@.planning/phases/179-ignite-b1-starting-point-fix/179-CONTEXT.md
@.planning/phases/179-ignite-b1-starting-point-fix/179-RESEARCH.md
@.planning/phases/174-hypothesis-based-ignite/174-RESEARCH.md
@docs/MINDRIAN-CANON.md
@commands/ignite.md
@lib/core/navigation/typed-claim.cjs
@scripts/check-room-blueprints.cjs
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (excluded from source-grounding drift verification):
- `lib/core/abstraction-gate.cjs` -- the 3-option INSTANCES/STRUCTURE/unsure selector + persistence helper
- `tests/fixtures/abstraction-gate-neutral.json` -- the committed domain-neutral fixture
- `scripts/check-abstraction-fixture-neutral.cjs` -- the domain-neutrality grep gate
- `tests/test-abstraction-gate.cjs`
- The always-fire abstraction-gate wiring in commands/ignite.md Door 3
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the 3-option abstraction selector + the domain-neutral fixture + the neutrality grep gate</name>
  <read_first>
    - docs/MINDRIAN-CANON.md Part 3 (Shape F selectors via the AskUserQuestion primitive; a 3-option single-select is an F.x family member with arrow-key keyboard) + CONTEXT decision 2 (always-fire, Brain-grounded; the 3rd "unsure" option)
    - .planning/phases/174-hypothesis-based-ignite/174-RESEARCH.md section B.1 (the abstraction-level trap: instances vs structures; the highest-leverage AND highest-risk net-new input; domain-neutral fixture required; AION specifics stay user-local) + section E.2 (the gate shape question)
    - scripts/check-room-blueprints.cjs (the --check grep-gate idiom to mirror for the neutrality gate: pure filesystem read, exit non-zero on violation, no network)
    - lib/core/navigation/typed-claim.cjs (the hypothesis node the abstraction value persists with -- the abstraction_level rides as an additive property on the claim, mirroring the props blob idiom)
    - docs/MINDRIAN-CANON.md Part 8 (LOCAL-only; the gate opens no Brain wire) + the no-real-names HARD RULE (the fixture must carry zero user/venture/AION-specific strings)
  </read_first>
  <behavior>
    - The selector exposes exactly 3 options: INSTANCES, STRUCTURE, unsure; renders as an arrow-key single-select AskUserQuestion card.
    - It fires ALWAYS for a Door 3 hypothesis (the helper is unconditional; there is no ambiguity-classifier branch that could skip it).
    - The recorded pick persists as an abstraction_level value on the hypothesis node (one of instances|structure|unsure).
    - The committed fixture is a generic "I believe X drives Y" with zero venture content; the neutrality grep gate FAILS (non-zero exit) if the fixture contains any banned domain/venture token (the gate carries an explicit denylist of AION/pharma/oncology/target/drug-style strings plus a heuristic for proper-noun venture names).
  </behavior>
  <action>
    Author lib/core/abstraction-gate.cjs exporting (a) the 3-option selector spec (INSTANCES / STRUCTURE / unsure; a Shape F single-select via the AskUserQuestion primitive) as a pure function returning the option set + header/question, and (b) a persistence helper that writes the chosen abstraction_level (instances|structure|unsure) as an additive property on the hypothesis claim node (mirror the typed-claim props-blob additive idiom; do NOT add a new node type or edge type -- Part 11 / SPEC boundary). The helper is UNCONDITIONAL (always-fire): no ambiguity classifier, no skip branch. Author tests/fixtures/abstraction-gate-neutral.json as the committed domain-neutral fixture: a generic "I believe X drives Y" example with ZERO venture/AION content. Author scripts/check-abstraction-fixture-neutral.cjs as the domain-neutrality grep gate -- a pure filesystem read over the fixture (and the abstraction-gate sources) that exits non-zero if any banned domain-specific token appears (carry an explicit denylist: aion, oncology, drug, target-pair, eureka, synergy + the venture-specific terms named in 174-RESEARCH; mirror the check-room-blueprints --check exit-code idiom). In commands/ignite.md Door 3, wire the abstraction gate to fire ALWAYS after the hypothesis is captured (before the path-forward), citing always-fire per CONTEXT decision 2. Part 8: LOCAL-only; no Brain, no network. No em-dashes; CJS.
  </action>
  <verify>
    <automated>node scripts/check-abstraction-fixture-neutral.cjs && node tests/test-abstraction-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-abstraction-gate.cjs` exits 0 and asserts the selector returns exactly 3 options (INSTANCES, STRUCTURE, unsure) rendered as an arrow-key single-select AskUserQuestion card.
    - The test asserts the gate is always-fire: the abstraction-gate helper has no conditional/classifier skip branch (e.g. a grep over abstraction-gate.cjs finds no ambiguity-classifier gating, or the helper signature has no "shouldFire" predicate).
    - The test asserts the chosen abstraction_level persists as an additive property on the hypothesis claim node (one of instances|structure|unsure) and that NO new node type or edge type is minted.
    - `node scripts/check-abstraction-fixture-neutral.cjs` exits 0 over the committed neutral fixture (domain-neutrality gate passes).
    - The test asserts the neutrality gate FAILS (non-zero) over an adversarial fixture containing a banned domain token (the gate actually rejects venture content -- prove it with a temp adversarial fixture).
    - `grep -niE 'aion|oncology|eureka|target-pair' tests/fixtures/abstraction-gate-neutral.json lib/core/abstraction-gate.cjs` returns zero matches in the FIXTURE and the gate source body (the denylist tokens may appear only inside the gate's denylist array).
    - `git diff --quiet HEAD -- lib/core/navigation/edges.cjs lib/core/navigation/transitions.cjs` exits 0 (no new edge/node type minted).
    - `grep -nP '\x{2014}|\x{2013}' lib/core/abstraction-gate.cjs scripts/check-abstraction-fixture-neutral.cjs tests/test-abstraction-gate.cjs commands/ignite.md` returns zero matches.
  </acceptance_criteria>
  <done>The 3-option instances-vs-structures selector fires always for every Door 3 hypothesis, renders via the AskUserQuestion keyboard contract, persists the abstraction_level on the hypothesis node without minting a new edge/node type, and the committed fixture is proven domain-neutral by a grep gate that actually rejects venture content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture -> plugin repo | the fixture is tracked, public-leaning; it must carry zero venture/AION content (no-real-names HARD RULE) |
| abstraction pick -> hypothesis node | LOCAL persistence; never to Brain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-12 | Information Disclosure | AION/venture content committed to the repo | mitigate | the domain-neutrality grep gate (check-abstraction-fixture-neutral.cjs) fails CLOSED on banned tokens; tested against an adversarial fixture |
| T-179-13 | Spoofing | a conditional classifier silently skips the gate | mitigate | always-fire is unconditional; the test asserts no skip branch (the net-new classifier risk is designed out) |
| T-179-14 | Tampering | minting a new edge/node type for abstraction | mitigate | abstraction_level rides as an additive claim property; git-diff-quiet on edges.cjs/transitions.cjs |
| T-179-SC | Tampering | npm/pip/cargo installs | mitigate | zero new deps (CJS + fixture + test only); no install task -- no legitimacy checkpoint required |
</threat_model>

<verification>
- The 3-option selector always-fires and renders via the keyboard contract.
- The fixture is domain-neutral, proven by a grep gate that rejects venture content.
- The abstraction value persists on the hypothesis node; no new edge/node type.
- Part 8 clean.
</verification>

<success_criteria>
- `node tests/test-abstraction-gate.cjs` green (3-option + always-fire + persist + adversarial-reject).
- `node scripts/check-abstraction-fixture-neutral.cjs` exits 0 over the neutral fixture.
- No new edge/node type; no AION content in the repo; no em-dashes.
</success_criteria>

<output>
Create `.planning/phases/179-ignite-b1-starting-point-fix/179-05-SUMMARY.md` when done.
</output>
