---
phase: 179-ignite-b1-starting-point-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/hooks.json
  - scripts/check-card-fire.cjs
  - tests/test-ga4-card-fire-interceptor.cjs
  - tests/run-all-179.sh
autonomous: true
requirements: [REQ-01]
canon_parts: [3, 8, 11]
cirs_relationship:
  surfaces_added:
    - check-card-fire (GA-4 Stop-hook turn-scan interceptor)
  surfaces_modified:
    - hooks (Stop block registration)
  surfaces_removed: []
  spine_consumed:
    - hooks/hooks.json
    - data/render-coverage-registry.json
    - scripts/check-render-coverage.cjs
  gate_impact: "The R-1 cure. R15 (Phase 178) proves a gate surface is WIRED to emit a card but cannot force the model to FIRE it at runtime. This adds a Stop-hook turn-scan interceptor: registry-keyed PRIMARY (a render-coverage-registry gate-reaching entry ran this turn) + ASCII-box output-text BACKSTOP, exit-2 block + additionalContext re-prompt with bounded retries then graceful degrade. Opens NO Brain wire."

must_haves:
  truths:
    - "A reached-gate turn with no fired AskUserQuestion card is intercepted and the card is forced (not merely logged)"
    - "A non-gate turn produces zero forced cards"
    - "The bounded escape releases after N retries with no infinite loop"
    - "Coverage enumerates from the Phase 178 R15 render-coverage registry, not a hand-maintained gate list"
  artifacts:
    - path: "scripts/check-card-fire.cjs"
      provides: "GA-4 Stop-hook turn-scan card-fire interceptor"
      contains: "render-coverage-registry"
    - path: "tests/test-ga4-card-fire-interceptor.cjs"
      provides: "positive intercept + negative non-gate + bounded-escape proof"
    - path: "tests/run-all-179.sh"
      provides: "Phase 179 single PASS/FAIL aggregator"
  key_links:
    - from: "hooks/hooks.json"
      to: "scripts/check-card-fire.cjs"
      via: "Stop block command entry"
      pattern: "check-card-fire"
    - from: "scripts/check-card-fire.cjs"
      to: "data/render-coverage-registry.json"
      via: "require + gate-reaching entries enumeration"
      pattern: "render-coverage-registry"
---

<objective>
Wave 1 (the load-bearing R-1 cure). Ship the GA-4 card-fire interceptor: a Stop-hook-class turn-scan that detects a reached-Decision-Gate turn with no fired AskUserQuestion card and FORCES the card via an exit-2 block + additionalContext re-prompt, degrading gracefully after N bounded retries. This MUST land first because every door in Waves 3-6 rides on a card that actually fires (SPEC Req 1; CONTEXT decision 1: registry-keyed PRIMARY + output-text BACKSTOP).

Purpose: R15 (Phase 178) proved a gate surface is WIRED to emit a card; it cannot force the model to FIRE the card at runtime (the named R-1 residual). A prose fence shipped and the agent ignored it. The structural cure moves enforcement below the agent: a deterministic turn-scan interceptor the LLM has no say in.
Output: scripts/check-card-fire.cjs + its Stop-block registration in hooks/hooks.json + tests/test-ga4-card-fire-interceptor.cjs + tests/run-all-179.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/179-ignite-b1-starting-point-fix/179-SPEC.md
@.planning/phases/179-ignite-b1-starting-point-fix/179-CONTEXT.md
@.planning/phases/179-ignite-b1-starting-point-fix/179-RESEARCH.md
@.planning/phases/179-ignite-b1-starting-point-fix/fanout/render-slip-investigation.json
@docs/MINDRIAN-CANON.md
@hooks/hooks.json
@data/render-coverage-registry.json
@scripts/operator-update.cjs
@tests/test-r1-posttooluse-interceptor-spike.cjs
@tests/run-all-178.sh
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (excluded from source-grounding drift verification):
- `scripts/check-card-fire.cjs` -- the GA-4 Stop-hook turn-scan interceptor
- The `check-card-fire` Stop-block entry in `hooks/hooks.json`
- `tests/test-ga4-card-fire-interceptor.cjs` -- the interceptor proof suite
- `tests/run-all-179.sh` -- the Phase 179 aggregator
- `~/.mindrian/card-fire-retries.json` (or equivalent LOCAL retry-count side-file the interceptor maintains for bounded escape)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the GA-4 turn-scan interceptor (registry-keyed PRIMARY + ASCII-box BACKSTOP)</name>
  <read_first>
    - scripts/operator-update.cjs (the Stop-hook stdin envelope idiom: read hook JSON from stdin, branch on hook_event_name, emit hookSpecificOutput.additionalContext, defensive exit-0 on any error; the active-room guard)
    - data/render-coverage-registry.json (the Phase 178 R15 substrate: `entries[]` with `render_coverage: 'card-emission'`; this is the gate-reaching enumeration to key PRIMARY detection off -- DO NOT hand-maintain a parallel list)
    - tests/test-r1-posttooluse-interceptor-spike.cjs (the PARTIAL verdict this builds on: the CLI hook substrate CAN observe an AskUserQuestion tool-call; the reached-gate marker is concatenated as opaque turn TEXT -- so the ASCII-box output scan is the BACKSTOP signal)
    - scripts/check-render-coverage.cjs (the deterministic code-evaluated predicate idiom to mirror; reuse its registry read, do not re-implement enumeration)
    - docs/MINDRIAN-CANON.md Part 11 R15 + Part 8 (boundary: the interceptor opens NO Brain wire; LOCAL-only)
  </read_first>
  <behavior>
    - Positive: given a transcript turn where a render-coverage-registry gate-reaching surface ran AND no AskUserQuestion tool-call appears in that turn, the predicate returns intercept=true.
    - Backstop: given a turn whose output text contains the ASCII-box gate glyphs (the `■ ... [1] [2] [3]` / "type 1, 2, or 3" anti-pattern) with no AskUserQuestion tool-call, intercept=true even for an off-registry surface.
    - Negative: given a turn with NO gate-reaching signal and no ASCII-box glyphs, intercept=false (zero forced cards on an ordinary turn).
    - Negative: given a reached-gate turn where an AskUserQuestion tool-call DID fire, intercept=false (the card fired; nothing to force).
    - Bounded escape: after N consecutive intercepts on the same turn-context (read N from the LOCAL retry side-file), the interceptor degrades to log+allow (returns degrade=true, continue=true) so a genuinely card-incapable surface cannot trap the navigator. N is a named constant (e.g. MAX_FORCE_RETRIES).
  </behavior>
  <action>
    Author scripts/check-card-fire.cjs as a Stop-hook entry script. Read the hook JSON from stdin (the on-stop / operator-update.cjs idiom: read once, parse transcript_path, tolerate empty stdin). Implement a deterministic, exported predicate (e.g. classifyCardFire(turn, registry)) that takes the latest turn signals and the render-coverage registry and returns { intercept, reason, degrade }. PRIMARY signal: require data/render-coverage-registry.json and treat any `entries[]` member with render_coverage 'card-emission' as a gate-reaching surface; a turn that ran such a surface with no AskUserQuestion tool-call observed is a candidate intercept. BACKSTOP signal: scan the turn output text for the ASCII-box gate glyphs and the literal "type 1, 2, or 3" phrasing; presence with no fired card is an intercept even off-registry. ENFORCEMENT: on intercept, emit the exit-2 block envelope with hookSpecificOutput.additionalContext re-prompting the agent to fire the AskUserQuestion card this turn (mirror operator-update.cjs's envelope shape; additionalContext ONLY inside hookSpecificOutput). BOUNDED ESCAPE: maintain a LOCAL retry counter (a ~/.mindrian/card-fire-retries.json side-file keyed by turn-context hash); after MAX_FORCE_RETRIES consecutive intercepts, return degrade=true and emit { continue: true, suppressOutput: true } (log to stderr, allow the turn). Part 8: LOCAL-only -- no fetch/http/curl, no Brain module require; the Part-8 grep gate over this file must find zero network/Brain symbols. Defensive: any internal error -> stderr + { continue: true, suppressOutput: true } + exit 0 (NEVER block the hook chain on a bug). No em-dashes; CJS module.
  </action>
  <verify>
    <automated>node tests/test-ga4-card-fire-interceptor.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-ga4-card-fire-interceptor.cjs` exits 0 and asserts: (a) a reached-gate-no-card fixture turn yields intercept=true and the emitted envelope carries hookSpecificOutput.additionalContext re-prompting a card fire; (b) a non-gate turn yields intercept=false (zero forced cards); (c) a reached-gate turn WITH a fired AskUserQuestion yields intercept=false; (d) after MAX_FORCE_RETRIES the predicate returns degrade=true and the envelope is { continue: true } (no infinite loop).
    - `node -e "const m=require('./scripts/check-card-fire.cjs'); if(typeof m.classifyCardFire!=='function') process.exit(1)"` exits 0 (predicate is exported and deterministic).
    - `grep -nE 'fetch|http|curl|brain\.mindrian|tavily|mcp__brain' scripts/check-card-fire.cjs` returns zero matches (Part 8 LOCAL sweep over the touched surface).
    - `grep -n 'render-coverage-registry' scripts/check-card-fire.cjs` returns at least one match (coverage keyed off the R15 registry, not a hand list).
    - `grep -nP '\x{2014}|\x{2013}' scripts/check-card-fire.cjs` returns zero matches (no em-dashes).
  </acceptance_criteria>
  <done>The interceptor predicate is deterministic, registry-keyed with an ASCII-box backstop, exit-2 blocks on a reached-gate-no-card turn, fires zero cards on ordinary turns, and bounded-escapes after N retries; the test proves all four behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: Register the interceptor in the hooks.json Stop block + ship the phase aggregator</name>
  <read_first>
    - hooks/hooks.json (the Stop block, lines 125-171: each entry is a `{ hooks: [ { type: 'command', command: 'node "${CLAUDE_PLUGIN_ROOT}/scripts/...cjs"', timeout } ] }`; append a new entry for check-card-fire mirroring the operator-update.cjs / gsd-graph-derive-sweep.cjs entries)
    - tests/run-all-178.sh (the phase-aggregator idiom: a bash script composing the phase suites + carried frozen-set drift fences, single PASS/FAIL)
    - scripts/check-card-fire.cjs (the script registered here, authored in Task 1)
  </read_first>
  <action>
    Add a new Stop-block entry to hooks/hooks.json invoking `node "${CLAUDE_PLUGIN_ROOT}/scripts/check-card-fire.cjs"` with a 3000ms timeout, placed in the Stop array alongside the existing on-stop / operator-update / jtbd-update entries (additive: do NOT modify or reorder the existing Stop entries; the JSON stays valid). Author tests/run-all-179.sh as the Phase 179 single PASS/FAIL aggregator: it runs tests/test-ga4-card-fire-interceptor.cjs (Wave 1) and is structured to compose the later-wave suites (Waves 2-7) as they land, each guarded by a file-existence SKIP so missing later suites do not FAIL the aggregator (mirror run-all-178.sh's scaffold-state guard). Carry the frozen-bank regression call-throughs as the other phases do (REACH_IDS length 6 unchanged is a Wave-3 concern; in Wave 1 the aggregator asserts the interceptor suite green + hooks.json parses). No em-dashes.
  </action>
  <verify>
    <automated>bash tests/run-all-179.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))"` exits 0 (hooks.json is still valid JSON after the additive Stop entry).
    - `node -e "const h=JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8')); const s=h.hooks.Stop; const hit=s.some(b=>b.hooks.some(x=>/check-card-fire/.test(x.command))); process.exit(hit?0:1)"` exits 0 (the interceptor is registered in the Stop block).
    - `node -e "const h=JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8')); process.exit(h.hooks.Stop.length>=6?0:1)"` exits 0 (the existing Stop entries are preserved; the new one is additive).
    - `bash tests/run-all-179.sh` exits 0 with the GA-4 suite green and missing later-wave suites SKIPPED (not FAILED).
    - `grep -nP '\x{2014}|\x{2013}' tests/run-all-179.sh` returns zero matches.
  </acceptance_criteria>
  <done>check-card-fire.cjs is wired into the hooks.json Stop block additively (existing entries byte-preserved), hooks.json is valid JSON, and tests/run-all-179.sh is the green Phase 179 aggregator that SKIPs not-yet-landed later-wave suites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent turn output -> Stop hook | The interceptor reads the model's turn (transcript) -- the turn is untrusted; the interceptor must not echo turn content to Brain |
| Stop hook -> ~/.mindrian retry side-file | LOCAL-only write; no network egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-01 | Denial of Service | check-card-fire.cjs infinite re-prompt | mitigate | bounded escape: MAX_FORCE_RETRIES then degrade=true + continue:true; tested by the bounded-escape assertion |
| T-179-02 | Information Disclosure | turn content to Brain | mitigate | LOCAL-only; Part 8 grep gate over check-card-fire.cjs finds zero network/Brain symbols |
| T-179-03 | Tampering | hooks.json corruption on edit | mitigate | additive Stop entry only; JSON.parse acceptance gate; existing entries preserved (>=6 assertion) |
| T-179-SC | Tampering | npm/pip/cargo installs | mitigate | zero new deps this plan (Node built-ins only); no install task -- no legitimacy checkpoint required |
</threat_model>

<verification>
- The GA-4 interceptor exists, is deterministic, registry-keyed, ASCII-box-backstopped, and bounded.
- It is registered in the hooks.json Stop block additively.
- Part 8 LOCAL sweep over scripts/check-card-fire.cjs is clean (zero Brain/network).
- tests/run-all-179.sh is the green phase aggregator.
</verification>

<success_criteria>
- `node tests/test-ga4-card-fire-interceptor.cjs` green (positive intercept + negative non-gate + fired-card negative + bounded escape).
- `bash tests/run-all-179.sh` green.
- hooks.json valid JSON with the check-card-fire Stop entry; existing Stop entries preserved.
- Part 8 grep gate clean; no em-dashes.
</success_criteria>

<output>
Create `.planning/phases/179-ignite-b1-starting-point-fix/179-01-SUMMARY.md` when done.
</output>
