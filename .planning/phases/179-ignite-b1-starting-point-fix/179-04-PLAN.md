---
phase: 179-ignite-b1-starting-point-fix
plan: 04
type: execute
wave: 4
depends_on: [179-03]
files_modified:
  - data/room-blueprints.json
  - scripts/check-room-blueprints.cjs
  - commands/ignite.md
  - tests/test-hypothesis-family-and-claim.cjs
autonomous: true
requirements: [REQ-05, REQ-07]
canon_parts: [2a, 5, 8, 9]
cirs_relationship:
  surfaces_added:
    - hypothesis-arrival-door (the hypothesis family + truth-claim filing + per-role framing)
  surfaces_modified:
    - room-blueprints
    - ignite
  surfaces_removed: []
  spine_consumed:
    - data/room-blueprints.json
    - scripts/check-room-blueprints.cjs
    - lib/core/navigation/typed-claim.cjs
    - commands/ignite.md
  gate_impact: "Adds the hypothesis blueprint family (data, not a frozen-set move per Part 11) + files the I-believe statement as a truth-claim node at review_status:proposed (Part 9) + per-role hypothesis framing. The CI gate (check-room-blueprints.cjs) moves 8->9 families in lockstep. Opens NO Brain wire."

must_haves:
  truths:
    - "data/room-blueprints.json carries a hypothesis family (sections problem-definition-seeded + assumptions + opportunity-bank; default_methodologies structure-argument/challenge-assumptions/validate/research)"
    - "check-room-blueprints.cjs stays green with the new family (family count 8->9 in lockstep; assumptions accepted as a valid section slug)"
    - "the scaffold resolves the hypothesis family section set"
    - "the I-believe hypothesis files as a truth-claim node at review_status:proposed, never confirmed without human byUser"
    - "Door 3 hypothesis framing auto-selects per role (researcher=testable claim / founder=market bet / investor=thesis precondition); empty role_blend -> generic 'I believe ___ because ___'"
  artifacts:
    - path: "data/room-blueprints.json"
      provides: "the hypothesis blueprint family"
      contains: "hypothesis"
    - path: "scripts/check-room-blueprints.cjs"
      provides: "9-family CI gate accepting the hypothesis family + assumptions slug"
      contains: "hypothesis"
    - path: "tests/test-hypothesis-family-and-claim.cjs"
      provides: "family CI-green + scaffold-resolves + claim-proposed + per-role-framing proof"
  key_links:
    - from: "data/room-blueprints.json hypothesis family"
      to: "scripts/check-room-blueprints.cjs"
      via: "9-family count + valid-section-slug acceptance"
      pattern: "hypothesis"
    - from: "commands/ignite.md Door 3"
      to: "lib/core/navigation/typed-claim.cjs writeClaimNode"
      via: "file I-believe as review_status:proposed truth-claim"
      pattern: "writeClaimNode|review_status"
---

<objective>
Wave 4. Add the `hypothesis` blueprint family + file the captured "I believe ___" as a truth-claim node at review_status:proposed + auto-select Door 3's hypothesis framing per role. The CI gate (check-room-blueprints.cjs) currently asserts EXACTLY 8 families and rejects the `assumptions` slug; this plan moves it to 9 in lockstep and accepts `assumptions` (the same extension already granted to `opportunity-bank`), so CI never goes RED (SPEC Req 5, 7; CONTEXT decision 3: section set LOCKED as specced).

Purpose: Door 3 (Wave 3) captures the hypothesis statement; this wave gives it a home (the family) and legitimacy (the proposed truth-claim, Part 9 role 5: only a human byUser confirms) and frames the prompt per the captured role_blend.
Output: the hypothesis family in room-blueprints.json + the 9-family CI gate + the writeClaimNode filing doctrine + the per-role framing map in ignite.md Door 3 + the proof suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/179-ignite-b1-starting-point-fix/179-SPEC.md
@.planning/phases/179-ignite-b1-starting-point-fix/179-CONTEXT.md
@.planning/phases/179-ignite-b1-starting-point-fix/179-RESEARCH.md
@.planning/phases/174-hypothesis-based-ignite/174-RESEARCH.md
@docs/MINDRIAN-CANON.md
@data/room-blueprints.json
@scripts/check-room-blueprints.cjs
@lib/core/navigation/typed-claim.cjs
@lib/core/room-skeleton-scaffold.cjs
@commands/ignite.md
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (excluded from source-grounding drift verification):
- The `hypothesis` blueprint family key in data/room-blueprints.json
- The 9-family `EXPECTED_FAMILY_COUNT` + `EXPECTED_FAMILIES` `hypothesis` member + the `assumptions` valid-slug extension in scripts/check-room-blueprints.cjs
- The per-role hypothesis framing map in commands/ignite.md Door 3 (researcher/founder/investor + generic fallback)
- `tests/test-hypothesis-family-and-claim.cjs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the hypothesis blueprint family + move the CI gate 8->9 in lockstep</name>
  <read_first>
    - data/room-blueprints.json (the 8 families: exploration/solution-first/problem-first/business-first/portfolio/venture/program/case-study; each carries description + arrival_assets[] + sections[] + default_methodologies[]; the exploration family is the closest analog -- it already uses problem-definition + opportunity-bank)
    - scripts/check-room-blueprints.cjs (EXPECTED_FAMILIES set of 8 + EXPECTED_FAMILY_COUNT=8 at ~:34-43; the three invariants; loadValidSectionSlugs reads SECTION_NAMES; EXTENDED_VALID_SLUGS_FOR_CHECK = new Set(['opportunity-bank']) at ~:88 -- the pattern for accepting a non-frozen slug)
    - lib/core/room-skeleton-scaffold.cjs SECTION_NAMES (the frozen 8: problem-definition is valid; assumptions + opportunity-bank are NOT in the frozen set -- the scaffold skips non-frozen slugs gracefully; the CI gate needs the extension)
    - docs/MINDRIAN-CANON.md Part 11 (a data family is NOT a frozen-set move -- no canon amendment needed; CONTEXT decision 3 confirms no dedicated `hypotheses` section, no Appendix-D note)
  </read_first>
  <behavior>
    - The hypothesis family validates: sections = [problem-definition, assumptions, opportunity-bank], default_methodologies = [structure-argument, challenge-assumptions, validate, research], arrival_assets includes hypothesis-arrival.
    - check-room-blueprints.cjs --check exits 0 with 9 families (no drift, no extras) and accepts `assumptions` + `opportunity-bank` as valid section slugs.
    - scaffoldRoomSkeleton('hypothesis') resolves a section set (problem-definition is scaffold-creatable; non-frozen slugs skip gracefully without error).
  </behavior>
  <action>
    Add a `hypothesis` family to data/room-blueprints.json mirroring the exploration family's shape: description (a domain-neutral hypothesis/falsify-a-belief job), arrival_assets including `hypothesis-arrival`, sections = ["problem-definition", "assumptions", "opportunity-bank"], default_methodologies = ["structure-argument", "challenge-assumptions", "validate", "research"]. In scripts/check-room-blueprints.cjs, move EXPECTED_FAMILY_COUNT 8 -> 9 and add `hypothesis` to EXPECTED_FAMILIES; extend EXTENDED_VALID_SLUGS_FOR_CHECK to also include `assumptions` (mirroring the opportunity-bank precedent and its explanatory comment) so the gate accepts the hypothesis family's non-frozen sections. These two edits MUST land together (the family addition without the gate update breaks CI; the gate update without the family breaks the count) -- one atomic lockstep wave, CI never RED. Author tests/test-hypothesis-family-and-claim.cjs Task-1 assertions (family shape + CI-green + scaffold-resolves). No em-dashes; do NOT touch the frozen SECTION_NAMES table in room-skeleton-scaffold.cjs.
  </action>
  <verify>
    <automated>node scripts/check-room-blueprints.cjs --check && node tests/test-hypothesis-family-and-claim.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-room-blueprints.cjs --check` exits 0 with the new hypothesis family present (9 families, no drift).
    - `node -e "const b=require('./data/room-blueprints.json'); const h=b.hypothesis; const ok=h && Array.isArray(h.sections) && h.sections.includes('problem-definition') && h.sections.includes('assumptions') && h.sections.includes('opportunity-bank') && ['structure-argument','challenge-assumptions','validate','research'].every(m=>h.default_methodologies.includes(m)); process.exit(ok?0:1)"` exits 0.
    - `node -e "const s=require('./lib/core/room-skeleton-scaffold.cjs'); const r=s.resolveBlueprint? s.resolveBlueprint('hypothesis'): s.scaffoldRoomSkeleton; if(!r) process.exit(1)"` exits 0 (the scaffold resolves the family; use whichever resolver the scaffold exports).
    - `grep -n 'assumptions' scripts/check-room-blueprints.cjs` returns at least one match (the valid-slug extension).
    - `git diff --quiet HEAD -- lib/core/room-skeleton-scaffold.cjs` exits 0 (frozen SECTION_NAMES untouched).
    - `grep -nP '\x{2014}|\x{2013}' data/room-blueprints.json scripts/check-room-blueprints.cjs` returns zero matches.
  </acceptance_criteria>
  <done>The hypothesis family exists with the locked section set + methodologies, the CI gate is green at 9 families and accepts the assumptions slug, the scaffold resolves the family, and the frozen SECTION_NAMES table is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: File the I-believe hypothesis as a proposed truth-claim + per-role Door 3 framing</name>
  <read_first>
    - lib/core/navigation/typed-claim.cjs:79-141 writeClaimNode (params = { knowledge_type, text, sessionId, ... }; type 'claim', created_by 'system', review_status 'proposed' on INSERT, NEVER auto-confirmed; KNOWLEDGE_TYPES = {fact,causal,heuristic,anomaly_cue,mental_model,assumption} -- an "I believe ___" hypothesis files as knowledge_type 'assumption')
    - commands/ignite.md Door 3 block (authored in Wave 3: arrival_asset=hypothesis-arrival, captures the falsifiable statement -- this task adds the writeClaimNode filing doctrine + the per-role framing map)
    - docs/MINDRIAN-CANON.md Part 9 (the proposed->confirmed gate: only a human byUser promotes a truth-claim; the hypothesis node is proposed) + Part 5 (evidence tier None/Practitioner initially) + Part 2a (per-role framing from role_blend)
    - .planning/phases/174-hypothesis-based-ignite/174-RESEARCH.md section E.3 (per-role framing: researcher=testable claim / founder=market bet / investor=thesis precondition)
  </read_first>
  <action>
    In commands/ignite.md Door 3, add (a) the truth-claim filing doctrine: file the captured "I believe ___" via writeClaimNode with knowledge_type 'assumption' and review_status proposed (cite Part 9 -- the node is NEVER confirmed without a human byUser; cite Part 5 -- initial evidence tier None/Practitioner); the hypothesis_text also rides the Wave-2 scratchpad whitelist for B2 replay. (b) the per-role framing map: the Door 3 prompt auto-selects framing from the captured role_blend -- researcher -> "state your testable claim", founder -> "state your market bet", investor -> "state your thesis precondition"; when role_blend is empty/unknown, fall back to the generic "I believe ___ because ___". Add Task-2 assertions to tests/test-hypothesis-family-and-claim.cjs proving the writeClaimNode-proposed doctrine + the four framing branches (3 named roles + generic). Part 8: the hypothesis text is LOCAL (filed to room.db via writeClaimNode), never to Brain; do NOT use scope-reduction language. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-hypothesis-family-and-claim.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-hypothesis-family-and-claim.cjs` exits 0 and asserts the ignite.md Door 3 block cites writeClaimNode with review_status proposed (the hypothesis files as a proposed truth-claim, never auto-confirmed).
    - The test asserts the Door 3 framing map names all three role framings (researcher=testable claim, founder=market bet, investor=thesis precondition) AND the generic "I believe ___ because ___" fallback for an empty role_blend.
    - The test (or a sibling) exercises writeClaimNode against an in-memory room.db with knowledge_type 'assumption' and asserts the written node has review_status 'proposed' (Part 9: not confirmed).
    - `grep -nP '\x{2014}|\x{2013}' commands/ignite.md tests/test-hypothesis-family-and-claim.cjs` returns zero matches.
  </acceptance_criteria>
  <done>Door 3 files the I-believe statement as a proposed truth-claim via writeClaimNode (never confirmed without human byUser), the hypothesis framing auto-selects per role with a generic fallback, and the proof suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hypothesis text -> room.db | LOCAL truth-claim filing; the I-believe bytes never egress to Brain |
| Door 3 framing -> role_blend | the captured role_blend selects framing locally; no Brain call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-09 | Elevation of Privilege | a hypothesis confirmed without a human | mitigate | writeClaimNode writes review_status proposed; the proposed->confirmed gate requires a human byUser (Part 9); the test asserts proposed |
| T-179-10 | Tampering | CI gate drift (family count mismatch) | mitigate | the 8->9 family count + slug extension land in one atomic wave; check-room-blueprints --check is the gate |
| T-179-11 | Information Disclosure | hypothesis text to Brain | mitigate | LOCAL writeClaimNode only; Part 8 doctrine in Door 3 |
| T-179-SC | Tampering | npm/pip/cargo installs | mitigate | zero new deps (data + doctrine + test only); no install task -- no legitimacy checkpoint required |
</threat_model>

<verification>
- The hypothesis family exists; the CI gate is green at 9 families.
- The hypothesis files as a proposed truth-claim; per-role framing auto-selects with a generic fallback.
- Frozen SECTION_NAMES untouched; Part 8 clean.
</verification>

<success_criteria>
- `node scripts/check-room-blueprints.cjs --check` exits 0 (9 families).
- `node tests/test-hypothesis-family-and-claim.cjs` green (family + scaffold + claim-proposed + per-role framing).
- Frozen SECTION_NAMES byte-unchanged; no em-dashes.
</success_criteria>

<output>
Create `.planning/phases/179-ignite-b1-starting-point-fix/179-04-SUMMARY.md` when done.
</output>
