---
phase: 179-ignite-b1-starting-point-fix
plan: 07
type: execute
wave: 7
depends_on: [179-03, 179-06]
files_modified:
  - commands/new-project.md
  - tests/test-b1-reconcile-canonical.cjs
autonomous: true
requirements: [REQ-10, REQ-11]
canon_parts: [3, 8, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified:
    - new-project
  surfaces_removed: []
  spine_consumed:
    - commands/new-project.md
    - commands/ignite.md
  gate_impact: "Reconciles the two B1 specs: ignite.md persona-first is the ONE canonical B1; new-project.md demotes to a pure B2 scaffold backend (its competing B1 gate prose removed). Closes the phase with the cross-cutting Part 8 leak sweep + the CIRS R12 conformance check over all touched surfaces. Opens NO Brain wire."

must_haves:
  truths:
    - "new-project.md carries no competing persona-first B1 gate prose; it keeps only the B2 scaffold backend"
    - "ignite.md remains the ONE canonical B1; one canonical B1 remains across the two files"
    - "A Part 8 sweep over all touched surfaces this phase returns zero user-content egress"
    - "The CIRS born-wired gate passes (every new/modified surface WIRED or EXCLUDED); the 179-CONTEXT cirs_relationship is conformant"
  artifacts:
    - path: "commands/new-project.md"
      provides: "the demoted-to-B2-backend new-project (no B1 gate)"
    - path: "tests/test-b1-reconcile-canonical.cjs"
      provides: "no-competing-B1 + one-canonical-B1 + Part-8-sweep + CIRS-conformance proof"
  key_links:
    - from: "commands/ignite.md"
      to: "commands/new-project.md B2"
      via: "ignite delegates to new-project's B2 scaffold backend (already wired); new-project no longer renders B1"
      pattern: "scaffoldRoomSkeleton|B2"
---

<objective>
Wave 7 (reconcile last). Collapse the two divergent B1 specs to ONE: ratify ignite.md persona-first as the single canonical B1, and demote new-project.md to a pure B2 scaffold backend by removing its competing B1 gate prose (ignite.md:80-98 persona-first vs new-project.md:147-188 arriving-with). Then close the phase with the cross-cutting Part 8 leak sweep + CIRS R12 conformance over every surface this phase touched (SPEC Req 10, 11; CONTEXT wave-order step "reconcile the two B1 specs", last).

Purpose: today both files describe a B1 (ignite persona-first with role_blend; new-project arriving-with, no role_blend), which is a self-CONTRADICTS the dog-fooding mandate (Part 6). new-project.md should keep only the B2 scaffold backend that ignite delegates to. This must land last so the canonical ignite B1 (Waves 3-6) is fully built before its competitor is removed.
Output: new-project.md with its B1 gate removed (B2 backend retained) + the reconciliation + Part-8-sweep + CIRS-conformance proof suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/179-ignite-b1-starting-point-fix/179-SPEC.md
@.planning/phases/179-ignite-b1-starting-point-fix/179-CONTEXT.md
@.planning/phases/179-ignite-b1-starting-point-fix/179-RESEARCH.md
@docs/MINDRIAN-CANON.md
@commands/new-project.md
@commands/ignite.md
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (excluded from source-grounding drift verification):
- `tests/test-b1-reconcile-canonical.cjs` -- the reconciliation + Part-8-sweep + CIRS-conformance proof
- (this plan REMOVES the competing B1 gate prose from new-project.md; it adds no new runtime symbol)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Demote new-project.md to a pure B2 scaffold backend (remove its competing B1 gate)</name>
  <read_first>
    - commands/new-project.md:140-200 (the B1 GATE block: the "B1: STARTING POINT GATE" header, the pickShape('F.1', ...) call with the solution-first/exploration/venture verbs, the writeScratchpadBirthAnswer journal, the arrival-asset+blueprintFamily mapping, the Tri-Polar degradation script -- THIS is the competing B1 to remove) and the B2 GATE block immediately after (the scaffold backend to RETAIN)
    - commands/ignite.md Gate B2 block (ignite delegates to new-project's B2 scaffold backend by passing blueprintFamily into scaffoldRoomSkeleton -- this delegation must keep working; new-project keeps the B2 backend, loses only the B1 gate)
    - docs/MINDRIAN-CANON.md Part 6 (dog-fooding: two B1 specs is a self-CONTRADICTS; the plugin must honor its own canon) + Part 11 (CIRS: new-project stays a WIRED surface; removing its B1 gate does not change its connector wiring)
  </read_first>
  <action>
    Remove the competing B1 STARTING POINT GATE block from commands/new-project.md (the persona/arriving-with gate prose, its pickShape F.1 call, its B1 writeScratchpadBirthAnswer journal, the B1 arrival-asset+blueprintFamily mapping, and the B1 Tri-Polar degradation script). RETAIN the B2 scaffold backend (the blueprint gate + scaffoldRoomSkeleton call + birthRoom delegation) that ignite.md's Gate B2 delegates to -- new-project becomes a pure B2 scaffold backend. Replace the removed B1 block with a one-line pointer noting that the canonical B1 lives in commands/ignite.md (the single persona-first 4-door card) and new-project is entered AT B2 with blueprintFamily already resolved. Do NOT touch the new-project frontmatter connector block (it stays a WIRED surface; CIRS coverage unchanged). Author tests/test-b1-reconcile-canonical.cjs Task-1 assertions. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-b1-reconcile-canonical.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-b1-reconcile-canonical.cjs` exits 0 and asserts commands/new-project.md carries NO persona-first / arriving-with B1 starting-point gate prose (grep for the removed markers returns zero in new-project.md).
    - The test asserts commands/new-project.md RETAINS its B2 scaffold backend (the scaffoldRoomSkeleton / blueprint gate / birthRoom delegation is still present).
    - The test asserts ignite.md remains the ONE canonical B1 (the "Who are you arriving as?" persona-first card is present in ignite.md and absent from new-project.md) -- one canonical B1 across the two files.
    - `git diff --quiet HEAD -- commands/ignite.md` exits 0 (this plan does NOT modify ignite.md; the canonical B1 was built in Waves 3-6).
    - `grep -nP '\x{2014}|\x{2013}' commands/new-project.md tests/test-b1-reconcile-canonical.cjs` returns zero matches.
  </acceptance_criteria>
  <done>new-project.md has its competing B1 gate removed (B2 scaffold backend retained), ignite.md is the single canonical B1, the delegation from ignite Gate B2 still resolves, and the reconciliation test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-cutting Part 8 leak sweep + CIRS R12 conformance over all touched surfaces</name>
  <read_first>
    - 179-CONTEXT.md frontmatter cirs_relationship block (surfaces_added: check-card-fire, cv-second-select, hypothesis-arrival-door; surfaces_modified: ignite, new-project, scratchpad-ops, shallow-doc-parser, room-blueprints, persona-variants; the conformant block the phase declares)
    - docs/MINDRIAN-CANON.md Part 8 (the LOCAL->BRAIN: NO invariant; the breach rule: any code path writing user-specific bytes to Brain or embedding user strings in a Brain query) + Part 11 R12 (every phase touching an invocable surface declares a conformant cirs_relationship block or the gate FAILS)
    - scripts/check-render-coverage.cjs + the CIRS born-wired generators (build-connector-registry.cjs --check, build-orchestration-projection.cjs --check) -- the gates that must stay green over the phase's touched surfaces
    - the phase's touched runtime surfaces: scripts/check-card-fire.cjs, lib/core/scratchpad-ops.cjs, lib/core/abstraction-gate.cjs, scripts/check-abstraction-fixture-neutral.cjs, commands/ignite.md, commands/new-project.md
  </read_first>
  <action>
    Add Task-2 assertions to tests/test-b1-reconcile-canonical.cjs that perform the cross-cutting Part 8 leak sweep + the CIRS conformance check over EVERY surface this phase touched. Part 8 sweep: grep each phase-touched runtime source (check-card-fire.cjs, scratchpad-ops.cjs, abstraction-gate.cjs, check-abstraction-fixture-neutral.cjs) for forbidden Brain/network egress symbols (fetch/http/curl/brain.mindrian/tavily/mcp__brain) and assert zero user-content egress; assert role_blend weights + user_id + hypothesis_text never appear in a Brain query payload across the touched surfaces. CIRS conformance: assert the 179-CONTEXT cirs_relationship block declares 11 in canon_parts (R12 auto-derivation) and that the born-wired gates (run the connector + render-coverage --check) exit 0 over the touched surfaces (every new/modified surface WIRED or EXCLUDED). This is the phase's closing gate. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-b1-reconcile-canonical.cjs && node scripts/check-render-coverage.cjs --check && bash tests/run-all-179.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-b1-reconcile-canonical.cjs` exits 0 and asserts a Part 8 sweep over every phase-touched runtime surface (check-card-fire.cjs, scratchpad-ops.cjs, abstraction-gate.cjs, check-abstraction-fixture-neutral.cjs) finds zero forbidden Brain/network egress symbols and zero user-content (role_blend weights / user_id / hypothesis_text) in any Brain query payload.
    - The test asserts the 179-CONTEXT cirs_relationship block implies canon_parts 11 (R12 conformance).
    - `node scripts/check-render-coverage.cjs --check` exits 0 (R15 render coverage green over the phase's surfaces).
    - `node scripts/build-connector-registry.cjs --check` exits 0 (CIRS born-wired gate green: every new/modified surface WIRED or EXCLUDED).
    - `bash tests/run-all-179.sh` exits 0 with ALL seven waves' suites green (the phase aggregator is fully GREEN, no SKIPs remaining).
    - `grep -nP '\x{2014}|\x{2013}' tests/test-b1-reconcile-canonical.cjs` returns zero matches.
  </acceptance_criteria>
  <done>A Part 8 leak sweep over all touched surfaces returns zero user-content egress, the CIRS born-wired + render-coverage gates pass, the 179-CONTEXT cirs_relationship is conformant, and tests/run-all-179.sh is fully green across all seven waves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| every phase-touched surface -> Brain | the cross-cutting sweep proves no user-content egress across the whole phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-17 | Information Disclosure | any phase surface leaking user content to Brain | mitigate | the closing Part 8 grep sweep over every touched runtime surface; zero forbidden symbols |
| T-179-18 | Tampering | a new/modified surface neither WIRED nor EXCLUDED (dark) | mitigate | the CIRS born-wired --check gate exits 0; the cirs_relationship block is conformant (R12) |
| T-179-19 | Repudiation | two competing B1 specs (self-CONTRADICTS) | mitigate | new-project B1 removed; one canonical B1 asserted by the test |
| T-179-SC | Tampering | npm/pip/cargo installs | mitigate | zero new deps (doctrine + test only); no install task -- no legitimacy checkpoint required |
</threat_model>

<verification>
- new-project.md has no competing B1; ignite.md is the one canonical B1.
- The cross-cutting Part 8 sweep is clean over all touched surfaces.
- CIRS born-wired + render-coverage gates green; run-all-179.sh fully green.
</verification>

<success_criteria>
- `node tests/test-b1-reconcile-canonical.cjs` green (reconcile + Part-8-sweep + CIRS-conformance).
- `node scripts/check-render-coverage.cjs --check` and `node scripts/build-connector-registry.cjs --check` exit 0.
- `bash tests/run-all-179.sh` fully green across all seven waves; no em-dashes.
</success_criteria>

<output>
Create `.planning/phases/179-ignite-b1-starting-point-fix/179-07-SUMMARY.md` when done.
</output>
