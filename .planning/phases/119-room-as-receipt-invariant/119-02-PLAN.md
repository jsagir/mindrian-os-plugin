---
phase: 119-room-as-receipt-invariant
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/room-skeleton-scaffold.cjs
  - lib/core/room-skeleton-scaffold.test.cjs
  - lib/core/larry-thinness-acknowledgment.cjs
  - lib/core/larry-thinness-acknowledgment.test.cjs
  - templates/room-skeleton/STATE.md.tmpl
  - templates/room-skeleton/MINTO.md.tmpl
  - templates/room-skeleton/ROOM.md.section.tmpl
  - templates/room-skeleton/ROOM.md.identity.tmpl
  - templates/room-skeleton/USER.md.tmpl
  - tests/test-room-skeleton-scaffold-integration.cjs
  - tests/test-119-02-scaffold.sh
  - scripts/auto-explore-fire.cjs  # Phase 119 revision 2026-05-16: Task 3 wires scaffoldRoomSkeleton into the auto-explore-fire success path (key_link from frontmatter)
autonomous: true
requirements:
  - ROOMRECEIPT-119-05  # Skeleton scaffold even with thin material (D-05) + Larry thinness-acknowledgment voice line
canon_parts:
  - Part 1          # Wicked Navigator — even thin material gives the navigator a working memory
  - Part 2          # Team Around Navigator — team spawns into a real, navigable room
  - Part 6          # Product-as-Venture — the dog-fooding mandate
  - Part 9          # Memory Locality — STATE.md + MINTO.md + ROOM.md are the file substrate Part 9 binds to SQL
  - Part 10         # sub-claim 3 — rooms are receipts; the receipt must be navigable from byte 1
must_haves:
  truths:
    - "After Plan 119-00 creates the placeholder room shell, Plan 119-02 fills it with skeleton scaffolding: STATE.md + MINTO.md + USER.md + 8 ICM section folders each with ROOM.md (Canon decision 15) -- even when the source material is thin (D-05)."
    - "When Phase 117's auto-explore output is thin (whitespace.gaps.length === 0 AND rs.pairs.length === 0 AND analogy.zones.length === 0), Larry surfaces the verbatim thinness-acknowledgment voice line so the user understands the room is intentionally minimal."
    - "Every directory in the placeholder room has ROOM.md per Canon decision 15 (ICM Layer 0 everywhere) -- including the .mindrian/, .intelligence/, .snapshots/, assets/, team/ directories."
    - "MINTO.md is sentinel-bounded with an empty governing thought; the file is byte-preserveable across regeneration per the Phase 124 atomic-write pattern."
    - "Skeleton scaffolding is reentrant: invoking scaffoldRoomSkeleton on a placeholder room that already has STATE.md does NOT overwrite human-authored content (Canon Part 9 ratification)."
  artifacts:
    - path: "lib/core/room-skeleton-scaffold.cjs"
      provides: "scaffoldRoomSkeleton(roomDir, opts) -- fills a placeholder room with the canonical 8-section ICM structure + STATE.md + MINTO.md + USER.md + per-directory ROOM.md identity files (Canon decision 15) using the templates/ files as the source of truth"
      exports: ["scaffoldRoomSkeleton", "SECTION_NAMES", "renderTemplate"]
    - path: "lib/core/larry-thinness-acknowledgment.cjs"
      provides: "shouldAcknowledgeThinness(autoExploreFinding) -> boolean + voiceLine() -> string -- D-05 verbatim copy ('I made a room around this -- it's mostly empty until we have more to work with. Want to keep going and see what fills in?')"
      exports: ["shouldAcknowledgeThinness", "voiceLine", "THINNESS_VOICE_LINE"]
    - path: "templates/room-skeleton/STATE.md.tmpl"
      provides: "Minimal STATE.md template with frontmatter (phase: scoping, role_blend: {}, journey_stage: ordinary-world) for the cold-start case"
    - path: "templates/room-skeleton/MINTO.md.tmpl"
      provides: "Sentinel-bounded MINTO.md skeleton with empty governing thought (compatible with Phase 88 feynman-minto-memory-layer invariants)"
    - path: "templates/room-skeleton/ROOM.md.section.tmpl"
      provides: "Per-section ROOM.md template with frontmatter (section, purpose, stage_relevance, default_methodologies) for the 8 ICM sections"
    - path: "templates/room-skeleton/ROOM.md.identity.tmpl"
      provides: "Per-directory ROOM.md identity template (Canon decision 15) for non-ICM directories (.mindrian/, assets/, team/, .intelligence/, .snapshots/)"
    - path: "templates/room-skeleton/USER.md.tmpl"
      provides: "Minimal USER.md template with placeholder fields for the cold-start case before persona-blend inference"
  key_links:
    - from: "scripts/auto-explore-fire.cjs"
      to: "lib/core/room-skeleton-scaffold.cjs::scaffoldRoomSkeleton"
      via: "direct synchronous call from the auto-explore-fire success path AFTER the composeAutoExploreFinding returns AND BEFORE atomicWriteJson of auto-explore-<material_id>.json"
      pattern: "scaffoldRoomSkeleton\\(roomDir"
    - from: "lib/core/room-skeleton-scaffold.cjs"
      to: "templates/room-skeleton/*.tmpl"
      via: "fs.readFileSync of template files + string substitution + atomic write to roomDir"
      pattern: "templates/room-skeleton"
    - from: "lib/core/larry-thinness-acknowledgment.cjs"
      to: "skills/larry-personality/SKILL.md"
      via: "voice line lives in the core lib but is referenced by Larry's skill at conversational render time"
      pattern: "THINNESS_VOICE_LINE"
---

<objective>
**Wave 1 (Plan 119-02) -- the skeleton scaffold generator + Larry thinness-acknowledgment voice.**

This plan implements D-05 (skeleton scaffolding even when material is thin) and ships the verbatim Larry voice line that acknowledges thinness honestly. It is intentionally INDEPENDENT of Plan 119-00 (no `depends_on` overlap on files) because both Wave 1 plans share only the EVENT_TYPES Set in `lib/core/navigation/memory-events.cjs` -- and Plan 119-00 lands all three new strings at once, so Plan 119-02 can run truly parallel.

Per CONTEXT.md D-05: "Phase 119's job is to make the user's first action durable, not to gate the room behind material-quality thresholds." When Phase 117's `composeAutoExploreFinding` returns null or near-empty (no whitespace gaps, no reverse salients, no cross-domain analogies), the room STILL gets a complete skeleton -- and Larry's voice does the honesty work via the D-05 thinness-acknowledgment copy.

**Purpose:**
- Honors the "rooms are receipts" invariant ruthlessly: even a thin first material produces a receipt the user can navigate.
- Honors Canon decision 15 (ICM Layer 0 everywhere): every directory has ROOM.md, no exceptions.
- Honors Canon Part 9: STATE.md + MINTO.md + ROOM.md files are the source-of-meaning substrate that SQL navigates over; thin material doesn't break that contract.
- Provides Larry with a verbatim voice line so the conversational surface acknowledges thinness without sounding defensive or apologetic.

**Output:**
- New file `lib/core/room-skeleton-scaffold.cjs` (~220 LOC) -- the scaffolding entry point + section name registry + template renderer + atomic-write orchestrator.
- New file `lib/core/larry-thinness-acknowledgment.cjs` (~80 LOC) -- the verbatim D-05 voice line + the thin-output detector.
- 5 new template files in `templates/room-skeleton/` covering STATE.md, MINTO.md, ROOM.md (section), ROOM.md (identity), USER.md.
- Tests proving: the 8 ICM sections + ROOM.md per directory + sentinel-bounded MINTO + reentrancy invariant + thinness-voice trigger contract.

Plan 119-02 does NOT modify `scripts/auto-explore-fire.cjs` to invoke `scaffoldRoomSkeleton` -- that wiring is the contract handoff to Plan 119-01 (the post-MVA selector plan, which also owns the auto-explore-fire integration since the F.1 selector fires from the same code path). Plan 119-02 ships the standalone module + templates + the wiring contract so Plan 119-01 can plug it in atomically.

**Per CONTEXT.md D-05 verbatim copy lock:**
The Larry thinness voice line is the LOCKED VERBATIM string:
> "I made a room around this -- it's mostly empty until we have more to work with. Want to keep going and see what fills in?"

The double-hyphen (`--`) NOT em-dash (`—`) per memory feedback_no_emdashes.md HARD RULE. The "Want to keep going" tail is the conversational continuation invitation -- per Phase 114 Larry-default activation + Phase 115 owned-emotion patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/119-room-as-receipt-invariant/119-CONTEXT.md
@.planning/phases/119-room-as-receipt-invariant/119-DISCUSSION-LOG.md
@docs/MINDRIAN-CANON.md
@docs/CANON-PHASE-MAP.md
@CLAUDE.md

# /mos:new-project canonical room structure (the 8 sections + ROOM.md templates this plan mirrors)
@commands/new-project.md

# Phase 88 feynman-minto-memory-layer invariants (MINTO.md sentinel-bounded contract)
# Phase 124 atomic-write byte-preservation pattern (used here for STATE.md + MINTO.md)
@lib/core/feynman-minto-invariants.cjs

# Larry personality (the voice that delivers the thinness acknowledgment)
@skills/larry-personality/SKILL.md

# Phase 117 auto-explore-fire output contract (the JSON shape the thinness detector reads)
@scripts/auto-explore-fire.cjs
@lib/agents/auto-explore-agent.cjs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From scripts/auto-explore-fire.cjs (the Phase 117 auto-explore JSON output shape):
```javascript
// After composeAutoExploreFinding returns, the result is atomically written to
// $roomDir/.mindrian/auto-explore-<material_id>.json with shape:
// {
//   material_id: string,
//   findings: [
//     { source_pipeline: 'whitespace'|'reverse-salients'|'cross-domain'|'domain',
//       hsi_score: number,
//       ... },
//     ...
//   ],
//   composed_at_ms: number,
//   // ... HSIAnalysis schema fields ...
// }
// THINNESS is defined per Phase 119 D-05 as: finding === null OR finding.findings.length === 0.
```

From lib/core/feynman-minto-invariants.cjs (Phase 88 sentinel-bounded MINTO contract):
```javascript
// MINTO.md must contain:
//   <!-- mos:minto-begin -->
//   ## Governing Thought
//   {governing thought sentence or empty paragraph}
//   ## Supporting Arguments
//   {three bulleted supports or empty}
//   <!-- mos:minto-end -->
// The atomic-write pattern (Phase 124 timeline-runner.cjs precedent) writes to
// <path>.tmp.<pid> then renames; if rename fails, the original file is preserved.
```

From commands/new-project.md (the canonical 8 ICM section list):
```
problem-definition/    market-analysis/      solution-design/      business-model/
competitive-analysis/  team-execution/       legal-ip/             financial-model/
```
Plus the auxiliary directories: team/ (people layer), assets/, .intelligence/, .snapshots/, .context/.

From Canon decision 15 (ICM Layer 0 everywhere -- CLAUDE.md Key Decision 15):
"every directory in the Data Room MUST have a ROOM.md identity file. No exceptions.
Whether it's a room section, sub-room, team profile folder, meeting archive, or
opportunity bank -- if it's a directory, it gets ROOM.md."

From skills/larry-personality/SKILL.md (Larry voice rules):
- Short sentences. Conversational. Curious, not apologetic.
- Use "--" never "—" (em-dash HARD RULE).
- Signature openers: "Very simply...", "Think about it like this...", "Here's what I'm hearing..."
- Push back honestly; don't soften observations.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the 5 skeleton-scaffold template files + the thinness-acknowledgment voice module</name>
  <files>templates/room-skeleton/STATE.md.tmpl, templates/room-skeleton/MINTO.md.tmpl, templates/room-skeleton/ROOM.md.section.tmpl, templates/room-skeleton/ROOM.md.identity.tmpl, templates/room-skeleton/USER.md.tmpl, lib/core/larry-thinness-acknowledgment.cjs, lib/core/larry-thinness-acknowledgment.test.cjs</files>
  <read_first>
    - commands/new-project.md lines 100-235 (the canonical room structure + ROOM.md.section.tmpl shape; mirror the frontmatter exactly)
    - lib/core/feynman-minto-invariants.cjs (the sentinel-bounded MINTO.md contract; the new MINTO.md.tmpl must satisfy `validateMintoFile` if that function exists, or at minimum carry the begin/end sentinels)
    - skills/larry-personality/SKILL.md (the voice tone the thinness acknowledgment must match)
    - .planning/phases/119-room-as-receipt-invariant/119-CONTEXT.md lines 56 (the verbatim D-05 voice line -- locked copy, do NOT paraphrase)
    - templates/room-context/ if exists -- the precedent for plugin-distributed templates
  </read_first>
  <behavior>
    - Test 1 (THINNESS_VOICE_LINE verbatim): `require('./lib/core/larry-thinness-acknowledgment.cjs').THINNESS_VOICE_LINE` returns exactly the string `"I made a room around this -- it's mostly empty until we have more to work with. Want to keep going and see what fills in?"` (94 chars, no em-dashes, no smart quotes, no curly apostrophe).
    - Test 2 (voiceLine() function returns THINNESS_VOICE_LINE): `voiceLine()` returns the same string as THINNESS_VOICE_LINE (single source of truth -- no string drift).
    - Test 3 (shouldAcknowledgeThinness on null finding): `shouldAcknowledgeThinness(null)` returns `true`.
    - Test 4 (shouldAcknowledgeThinness on empty findings): `shouldAcknowledgeThinness({findings: []})` returns `true`.
    - Test 5 (shouldAcknowledgeThinness on substantive finding): `shouldAcknowledgeThinness({findings: [{source_pipeline:'whitespace',hsi_score:0.7}, {source_pipeline:'cross-domain',hsi_score:0.6}]})` returns `false`.
    - Test 6 (shouldAcknowledgeThinness on single-finding edge case): `shouldAcknowledgeThinness({findings: [{source_pipeline:'whitespace',hsi_score:0.3}]})` returns `true` (D-05 threshold per CONTEXT.md: 2+ findings = substantive; <=1 finding = thin).
    - Test 7 (STATE.md template parses as YAML frontmatter): `templates/room-skeleton/STATE.md.tmpl` starts with `---\n` and the frontmatter parses without error via `gray-matter` or `js-yaml` (whichever is in package.json deps); fields `phase`, `role_blend`, `journey_stage` are present with values `scoping`, `{}` (empty object), `ordinary-world`.
    - Test 8 (MINTO.md template has begin/end sentinels): `cat templates/room-skeleton/MINTO.md.tmpl | grep -c "<!-- mos:minto-begin -->"` returns 1; same for `mos:minto-end`.
    - Test 9 (ROOM.md.section.tmpl carries Canon decision 15 reference): the template contains the literal string `ICM Layer 0` in a comment so future grep audits can verify provenance.
    - Test 10 (no em-dashes in any template): `grep -rc "—" templates/room-skeleton/` returns 0 (em-dash HARD RULE).
    - Test 11 (no smart quotes in THINNESS_VOICE_LINE): the literal string contains only ASCII apostrophes `'` (0x27), never curly `'` (U+2019) or `'` (U+2018).
  </behavior>
  <action>
    **Step 1: Create `templates/room-skeleton/STATE.md.tmpl`.** This is the MINIMAL cold-start STATE.md per Phase 109/124 conventions. Verbatim content:

    ```markdown
    ---
    phase: scoping
    role_blend: {}
    journey_stage: ordinary-world
    venture_name: untitled
    venture_stage: Pre-Opportunity
    auto_created: true
    auto_created_at: '{{AUTO_CREATED_AT_ISO}}'
    placeholder_slug: '{{PLACEHOLDER_SLUG}}'
    source_material_id: '{{SOURCE_MATERIAL_ID}}'
    ---

    # Room State

    This room was auto-created as a side effect of receiving material (Canon Part 10 sub-claim 3: rooms are receipts, not entry points).

    The placeholder slug is `{{PLACEHOLDER_SLUG}}`. The room will be renamed (or discarded) at the first F.1 selector after the 30-second MVA pipeline completes -- see Phase 119-01.

    ## Current Position

    - **Phase:** scoping (the very beginning)
    - **Role blend:** unknown (will be inferred from USER.md as conversation progresses; see Phase 115 dual-path detector)
    - **Journey stage:** ordinary-world (Campbell's monomyth Stage 1; the navigator has not yet crossed any threshold)

    ## Recent Decisions

    None yet. Decisions land here via the Phase 88.2 F-shape selectors and the Phase 109 navigation chokepoint.

    ## Active Focus

    None yet. The first material upload triggers Phase 117 auto-explore + Phase 118 30-second MVA -- those pipelines write the first focus node.

    <!-- compute-state stamp: pending; regenerate via `bash scripts/compute-state $ROOM_DIR > STATE.md` -->
    ```

    **Step 2: Create `templates/room-skeleton/MINTO.md.tmpl`.** Sentinel-bounded skeleton per Phase 88 invariants:

    ```markdown
    # MINTO Pyramid

    The Minto Pyramid Principle structures argument top-down: governing thought first, supporting arguments below, evidence at the leaves. This room's MINTO is empty until the first methodology lands content.

    <!-- mos:minto-begin -->
    ## Governing Thought

    *(empty; will be populated when the first /mos:* methodology fires or when Larry synthesizes from the auto-explore finding)*

    ## Supporting Arguments

    - *(empty support 1)*
    - *(empty support 2)*
    - *(empty support 3)*
    <!-- mos:minto-end -->

    ## How This File Updates

    The sentinel-bounded section above is regenerated by `lib/core/feynman` pipelines. Edits between the sentinels are byte-preserved across regeneration; edits OUTSIDE the sentinels are append-safe.
    ```

    **Step 3: Create `templates/room-skeleton/ROOM.md.section.tmpl`.** Per-section ROOM.md for the 8 ICM sections. The `{{SECTION_NAME}}`, `{{SECTION_PURPOSE}}`, `{{STAGE_RELEVANCE}}`, `{{DEFAULT_METHODOLOGIES}}` placeholders are filled by the renderer using the lookup table in Task 2:

    ```markdown
    ---
    section: {{SECTION_NAME}}
    purpose: {{SECTION_PURPOSE}}
    stage_relevance:
    {{STAGE_RELEVANCE_LIST}}
    default_methodologies:
    {{DEFAULT_METHODOLOGIES_LIST}}
    icm_layer: 0
    auto_scaffolded: true
    ---

    <!-- ICM Layer 0 identity file per Canon decision 15 (every directory has ROOM.md).
         This file was auto-scaffolded by Phase 119 (Room-as-Receipt Invariant); content
         will be populated by /mos:* methodology runs or by Larry's first conversation. -->

    # {{SECTION_NAME_TITLE_CASE}}

    {{SECTION_PURPOSE}}

    ## Starter Questions

    *(populated when material lands or methodology fires; until then the section is empty by design)*

    ## Status

    Auto-scaffolded. Awaiting first content.
    ```

    **Step 4: Create `templates/room-skeleton/ROOM.md.identity.tmpl`.** Per Canon decision 15, non-ICM directories ALSO get ROOM.md. This template covers `.mindrian/`, `.intelligence/`, `.snapshots/`, `assets/`, `team/`, `.context/`. Generic ICM Layer 0 stub:

    ```markdown
    ---
    directory_type: {{DIRECTORY_TYPE}}
    icm_layer: 0
    auto_scaffolded: true
    purpose: {{DIRECTORY_PURPOSE}}
    ---

    <!-- ICM Layer 0 identity file per Canon decision 15. Every directory in the Data
         Room has ROOM.md -- no exceptions. This file was auto-scaffolded by Phase 119. -->

    # {{DIRECTORY_TYPE}}

    {{DIRECTORY_PURPOSE}}
    ```

    **Step 5: Create `templates/room-skeleton/USER.md.tmpl`.** Minimal placeholder USER.md per Phase 115 cold-start contract:

    ```markdown
    # User Context

    ## Identity

    *(unknown; will be inferred from conversation per Phase 115 dual-path detector)*

    ## Venture Context

    - **Core idea:** *(not yet articulated)*
    - **Stage:** Pre-Opportunity
    - **Primary concern:** *(unknown)*

    ## Working Style

    *(unknown; will be inferred from conversational turns)*

    ## Persona Blend

    *(empty -- per Phase 115 D-12, the persona blend is inferred from STATE.md + USER.md
    once enough material accumulates; until then Larry defaults to the 'default' variant)*

    <!-- Phase 119 auto-scaffolded. Edit freely; subsequent renames or content additions
         from /mos:onboard / /mos:new-project / persona-analyst will merge into this file. -->
    ```

    **Step 6: Create `lib/core/larry-thinness-acknowledgment.cjs`.** Verbatim:

    ```javascript
    'use strict';
    /*
     * Phase 119-02 -- Larry thinness-acknowledgment voice + thin-finding detector.
     *
     * Per CONTEXT.md D-05: when Phase 117's composeAutoExploreFinding returns
     * thin output (null OR <= 1 finding), the room is still scaffolded but
     * Larry's voice acknowledges thinness honestly via the verbatim copy below.
     *
     * The verbatim copy is LOCKED per CONTEXT.md line 56. Do NOT paraphrase.
     * Em-dash HARD RULE: uses `--` never `—` (memory feedback_no_emdashes.md).
     * Smart-quote BAN: uses ASCII apostrophe `'` (0x27) only.
     *
     * Canon Part 8 invariant: pure-local module; no Brain call, no telemetry
     * egress; the voice line is a static string.
     */

    const THINNESS_VOICE_LINE =
      "I made a room around this -- it's mostly empty until we have more to work with. " +
      "Want to keep going and see what fills in?";

    /**
     * Decide whether the room should surface the thinness acknowledgment to the user.
     *
     * Per CONTEXT.md D-05 threshold: thin = finding is null OR has <= 1 findings entries.
     *
     * @param {object|null} autoExploreFinding  the JSON shape Phase 117 writes
     *                                          to .mindrian/auto-explore-<material_id>.json
     * @returns {boolean}
     */
    function shouldAcknowledgeThinness(autoExploreFinding) {
      if (autoExploreFinding === null || autoExploreFinding === undefined) return true;
      if (typeof autoExploreFinding !== 'object') return true;
      const findings = Array.isArray(autoExploreFinding.findings) ? autoExploreFinding.findings : [];
      return findings.length <= 1;
    }

    /**
     * Return the verbatim D-05 thinness voice line. Single source of truth -- callers
     * MUST use this function rather than inlining the string (avoids drift).
     */
    function voiceLine() {
      return THINNESS_VOICE_LINE;
    }

    module.exports = {
      THINNESS_VOICE_LINE: THINNESS_VOICE_LINE,
      shouldAcknowledgeThinness: shouldAcknowledgeThinness,
      voiceLine: voiceLine,
    };
    ```

    **Step 7: Create `lib/core/larry-thinness-acknowledgment.test.cjs`.** Use `node --test`. Implement Tests 1-6 verbatim from `<behavior>`. Test 11 (smart-quote ban) verified via: `assert.strictEqual(THINNESS_VOICE_LINE.indexOf('’'), -1); assert.strictEqual(THINNESS_VOICE_LINE.indexOf('‘'), -1); assert.strictEqual(THINNESS_VOICE_LINE.indexOf('—'), -1);` (no right-single-quote, no left-single-quote, no em-dash).
  </action>
  <verify>
    <automated>node --test lib/core/larry-thinness-acknowledgment.test.cjs && grep -c "I made a room around this -- it's mostly empty" lib/core/larry-thinness-acknowledgment.cjs && grep -rc "—" templates/room-skeleton/ && grep -c "mos:minto-begin" templates/room-skeleton/MINTO.md.tmpl</automated>
  </verify>
  <acceptance_criteria>
    - `node --test lib/core/larry-thinness-acknowledgment.test.cjs` reports 0 failures with at least 6 tests passing.
    - `grep -c "I made a room around this -- it's mostly empty until we have more to work with. Want to keep going and see what fills in?" lib/core/larry-thinness-acknowledgment.cjs` returns exactly 1 (verbatim copy lock).
    - `grep -rc "—" templates/room-skeleton/` returns 0 across all 5 template files (em-dash HARD RULE).
    - `grep -rc "'\\|'" templates/room-skeleton/` returns 0 (smart-apostrophe and smart-quote ban -- only ASCII).
    - `grep -c "<!-- mos:minto-begin -->" templates/room-skeleton/MINTO.md.tmpl` returns 1.
    - `grep -c "<!-- mos:minto-end -->" templates/room-skeleton/MINTO.md.tmpl` returns 1.
    - `grep -c "ICM Layer 0" templates/room-skeleton/ROOM.md.section.tmpl` returns at least 1 (Canon decision 15 provenance).
    - `grep -c "ICM Layer 0" templates/room-skeleton/ROOM.md.identity.tmpl` returns at least 1 (Canon decision 15 provenance).
    - `head -1 templates/room-skeleton/STATE.md.tmpl` returns exactly `---` (frontmatter delimiter).
    - `node -e "const fs=require('fs'); const c=fs.readFileSync('templates/room-skeleton/STATE.md.tmpl','utf8'); const m=c.match(/^---\\n([\\s\\S]*?)\\n---/); console.log(m && m[1].includes('phase: scoping') && m[1].includes('journey_stage: ordinary-world'))"` prints `true`.
    - The 5 template files exist with non-zero size: `for f in templates/room-skeleton/{STATE.md,MINTO.md,ROOM.md.section,ROOM.md.identity,USER.md}.tmpl; do [ -s "$f" ] || exit 1; done; echo OK` prints `OK`.
  </acceptance_criteria>
  <done>
    Five skeleton templates land in `templates/room-skeleton/` covering STATE.md + MINTO.md (sentinel-bounded per Phase 88) + ROOM.md.section (8 ICM sections) + ROOM.md.identity (non-ICM directories per Canon decision 15) + USER.md (cold-start). The Larry thinness-acknowledgment module exports the LOCKED verbatim D-05 voice line with single-source-of-truth `THINNESS_VOICE_LINE` constant; 6+ tests pass proving the verbatim string + the thin-finding detector behavior across null / empty / 1-finding / 2-finding inputs. Zero em-dashes, zero smart quotes, full Canon decision 15 provenance.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the scaffold orchestrator (lib/core/room-skeleton-scaffold.cjs) + integration tests proving the 8 sections + per-directory ROOM.md + reentrancy invariants</name>
  <files>lib/core/room-skeleton-scaffold.cjs, lib/core/room-skeleton-scaffold.test.cjs, tests/test-room-skeleton-scaffold-integration.cjs, tests/test-119-02-scaffold.sh</files>
  <read_first>
    - commands/new-project.md lines 100-200 (the canonical section list + ROOM.md template structure -- mirror these for the 8-section lookup table)
    - lib/core/feynman-minto-invariants.cjs (the MINTO.md sentinel-bounded contract; understand `validateMintoFile` signature if it exists)
    - lib/core/folder-memory.cjs OR lib/core/asset-ops.cjs (existing fs-level scaffolding patterns; mirror the atomic-write idiom)
    - templates/room-skeleton/*.tmpl (the 5 templates created in Task 1 -- the source of truth this module reads)
    - lib/core/feynman/timeline-runner.cjs (the Phase 124 atomic-write byte-preservation pattern; the renderer here mirrors that idiom for STATE.md + MINTO.md regenerable files)
    - .planning/phases/119-room-as-receipt-invariant/119-CONTEXT.md lines 56 + 100-107 (D-05 binding + the anticipated sub-plan boundary; this plan ships the module standalone -- Plan 119-01 wires it into auto-explore-fire)
  </read_first>
  <behavior>
    - Test 1 (8 ICM sections present): after `scaffoldRoomSkeleton('/tmp/test-room-XXX', {})`, every directory in `SECTION_NAMES = ['problem-definition', 'market-analysis', 'solution-design', 'business-model', 'competitive-analysis', 'team-execution', 'legal-ip', 'financial-model']` exists at `/tmp/test-room-XXX/{section}/` with a ROOM.md file inside.
    - Test 2 (non-ICM identity directories present): `team/`, `assets/`, `.intelligence/`, `.snapshots/`, `.context/` each exist and each has a ROOM.md (Canon decision 15).
    - Test 3 (STATE.md rendered with substitutions): `cat /tmp/test-room-XXX/STATE.md | grep -E "phase: scoping|journey_stage: ordinary-world|placeholder_slug: 'untitled-"` returns 3 matches (all three substitutions landed).
    - Test 4 (MINTO.md sentinel-bounded): `cat /tmp/test-room-XXX/MINTO.md | grep -c "<!-- mos:minto-begin -->"` returns 1; same for end sentinel.
    - Test 5 (USER.md present): `/tmp/test-room-XXX/USER.md` exists with non-zero size.
    - Test 6 (per-section ROOM.md frontmatter): every section's ROOM.md has YAML frontmatter parseable as `{section: "...", purpose: "...", stage_relevance: [...], default_methodologies: [...], icm_layer: 0, auto_scaffolded: true}`.
    - Test 7 (per-section ROOM.md content matches section identity): `cat /tmp/test-room-XXX/problem-definition/ROOM.md | grep -E "section: problem-definition"` returns 1; same for each of the 8 sections.
    - Test 8 (REENTRANCY invariant -- canonical Canon Part 9 ratification): when STATE.md already exists with human-authored content (e.g. `auto_created: false` in frontmatter), calling scaffoldRoomSkeleton AGAIN does NOT overwrite it. Skip path: emit a stderr line `[room-skeleton] STATE.md already authored; skipping`. The 8 sections + ROOM.md identity files are STILL created if missing (idempotent fill).
    - Test 9 (atomic-write safety): if mid-write fs.rename fails (simulated via permissions), the original file is preserved byte-identically. (Mirrors Phase 124-02 timeline-runner.cjs atomic invariant.)
    - Test 10 (return shape): `scaffoldRoomSkeleton(roomDir, opts)` returns `{ok: true, sections_created: [string], identity_files_created: [string], state_written: boolean, minto_written: boolean, user_written: boolean, thinness_acknowledged: boolean}`.
    - Test 11 (thinness pass-through): when called with `{auto_explore_finding: null}`, the return object has `thinness_acknowledged: true` (so the caller -- Plan 119-01 -- knows to surface the Larry voice line).
    - Test 12 (no Brain MCP coupling): `grep -rE "require.+brain-client|fetch.+brain\\.mindrian" lib/core/room-skeleton-scaffold.cjs` returns 0.
    - Test 13 (chokepoint preserved -- no direct room-db require): `grep -c "require\\(.+room-db\\.cjs" lib/core/room-skeleton-scaffold.cjs` returns 0. (This module touches the file system only -- no graph writes from here; memory_event emission stays in Plan 119-00 + Plan 119-01.)
    - Test 14 (em-dash invariant): `grep -c "—" lib/core/room-skeleton-scaffold.cjs lib/core/room-skeleton-scaffold.test.cjs` returns 0.
  </behavior>
  <action>
    **Step 1: Create `lib/core/room-skeleton-scaffold.cjs` (~220 LOC).** Module structure:

    ```javascript
    'use strict';
    /*
     * Phase 119-02 -- Room skeleton scaffold generator.
     *
     * Fills a placeholder room (created by Plan 119-00's autoCreatePlaceholderRoom)
     * with the canonical 8-section ICM structure + STATE.md + MINTO.md + USER.md +
     * per-directory ROOM.md identity files (Canon decision 15: ICM Layer 0 everywhere).
     *
     * Idempotent + reentrant: never overwrites human-authored content. When STATE.md
     * already has auto_created:false in its frontmatter, the regenerable files are
     * skipped while the missing identity files are still created (Canon Part 9
     * "files preserve meaning" invariant).
     *
     * Atomic writes per Phase 124-02 precedent (tmp + rename).
     *
     * Canon Part 8 invariant: pure-local; no Brain MCP, no fetch, no telemetry.
     * Canon Part 9 invariant: no direct room-db.cjs require (this module touches
     *   only files; memory_event emission stays in Plan 119-00 and Plan 119-01).
     */

    const fs = require('node:fs');
    const path = require('node:path');

    const TEMPLATES_DIR = path.resolve(__dirname, '..', '..', 'templates', 'room-skeleton');

    /**
     * The 8 ICM sections + their per-section metadata. Lookup table extracted from
     * commands/new-project.md Section Definitions table (lines 180-190). Single
     * source of truth here; renames cascade.
     */
    const SECTION_NAMES = Object.freeze([
      'problem-definition',
      'market-analysis',
      'solution-design',
      'business-model',
      'competitive-analysis',
      'team-execution',
      'legal-ip',
      'financial-model',
    ]);

    const SECTION_METADATA = Object.freeze({
      'problem-definition':   { purpose: 'Define the core problem this venture addresses.',         stage_relevance: ['Pre-Opportunity', 'Discovery'],   default_methodologies: ['domain-explorer', 'beautiful-question', 'trending-to-absurd'] },
      'market-analysis':      { purpose: 'Map market size, trends, and customer segments.',         stage_relevance: ['Discovery', 'Validation'],        default_methodologies: ['domain-explorer', 'scenario-analysis'] },
      'solution-design':      { purpose: 'Design the solution, technology, and architecture.',      stage_relevance: ['Validation', 'Design'],           default_methodologies: ['structure-argument', 'think-hats'] },
      'business-model':       { purpose: 'Define revenue model, unit economics, go-to-market.',     stage_relevance: ['Design', 'Investment'],           default_methodologies: ['structure-argument', 'scenario-analysis'] },
      'competitive-analysis': { purpose: 'Analyze competition, positioning, differentiation.',      stage_relevance: ['Discovery', 'Design'],            default_methodologies: ['challenge-assumptions', 'find-bottlenecks'] },
      'team-execution':       { purpose: 'Document team, advisors, and execution plan.',            stage_relevance: ['Validation', 'Design'],           default_methodologies: ['think-hats', 'analyze-needs'] },
      'legal-ip':             { purpose: 'Track legal structure, agreements, IP protection.',       stage_relevance: ['Design', 'Investment'],           default_methodologies: ['structure-argument'] },
      'financial-model':      { purpose: 'Build financial projections and metrics.',                stage_relevance: ['Design', 'Investment'],           default_methodologies: ['scenario-analysis', 'build-thesis'] },
    });

    /**
     * Non-ICM directories that still need ROOM.md per Canon decision 15.
     */
    const IDENTITY_DIRECTORIES = Object.freeze({
      'team':         { directory_type: 'team',         purpose: 'The people layer of the Data Room. Members, mentors, advisors -- created on demand from meetings or user input.' },
      'assets':       { directory_type: 'assets',       purpose: 'Binary file storage (PDFs, images, videos) organized by section. Subdirectories appear when scripts/file-asset is invoked.' },
      '.intelligence':{ directory_type: '.intelligence',purpose: 'Sentinel-generated alerts and digests (health checks, deadline reports, competitor watch).' },
      '.snapshots':   { directory_type: '.snapshots',   purpose: 'Weekly STATE.md copies for drift detection by sentinel-health-check.' },
      '.context':     { directory_type: '.context',     purpose: 'Per-session conversational state (last-session, rejection-log, methodology-history, weekly-digest).' },
    });

    /**
     * Render a template with {{KEY}} string substitutions. Pure function.
     */
    function renderTemplate(templateContent, substitutions) {
      let rendered = templateContent;
      for (const key of Object.keys(substitutions || {})) {
        const re = new RegExp('\\{\\{' + key + '\\}\\}', 'g');
        rendered = rendered.replace(re, String(substitutions[key]));
      }
      return rendered;
    }

    /**
     * Atomic write: tmp + rename. Returns true on success, false on failure.
     * Mirrors Phase 124-02 timeline-runner.cjs invariant.
     */
    function atomicWrite(filePath, content) {
      try {
        fs.mkdirSync(path.dirname(filePath), { recursive: true, mode: 0o755 });
        const tmpPath = filePath + '.tmp.' + process.pid + '.' + Math.random().toString(36).slice(2, 10);
        fs.writeFileSync(tmpPath, content, 'utf8');
        fs.renameSync(tmpPath, filePath);
        return true;
      } catch (_e) {
        return false;
      }
    }

    function readTemplate(name) {
      try {
        return fs.readFileSync(path.join(TEMPLATES_DIR, name), 'utf8');
      } catch (_e) {
        return null;
      }
    }

    function isStateAuthored(roomDir) {
      try {
        const statePath = path.join(roomDir, 'STATE.md');
        if (!fs.existsSync(statePath)) return false;
        const raw = fs.readFileSync(statePath, 'utf8');
        const m = raw.match(/^---\n([\s\S]*?)\n---/);
        if (!m) return false;
        // If frontmatter has auto_created: false (or absent), treat as authored.
        if (/^\s*auto_created:\s*false\s*$/m.test(m[1])) return true;
        // If frontmatter LACKS auto_created entirely, treat as authored.
        if (!/^\s*auto_created:\s*/m.test(m[1])) return true;
        return false;
      } catch (_e) {
        return false;
      }
    }

    /**
     * scaffoldRoomSkeleton(roomDir, opts) -- the entry point.
     *
     * @param {string} roomDir  absolute path to the placeholder room
     * @param {object} opts
     * @param {string} [opts.placeholder_slug]      e.g. 'untitled-2026-05-16-1845'
     * @param {string} [opts.source_material_id]    32-char hex from Phase 117
     * @param {object} [opts.auto_explore_finding]  the Phase 117 JSON output
     * @returns {{ok, sections_created, identity_files_created, state_written, minto_written, user_written, thinness_acknowledged, errors}}
     */
    function scaffoldRoomSkeleton(roomDir, opts) {
      const options = opts || {};
      const result = {
        ok: true,
        sections_created: [],
        identity_files_created: [],
        state_written: false,
        minto_written: false,
        user_written: false,
        thinness_acknowledged: false,
        errors: [],
      };

      if (!roomDir || typeof roomDir !== 'string') {
        result.ok = false;
        result.errors.push('invalid_room_dir');
        return result;
      }
      if (!fs.existsSync(roomDir)) {
        result.ok = false;
        result.errors.push('room_dir_does_not_exist');
        return result;
      }

      // Substitutions used across templates.
      const subs = {
        AUTO_CREATED_AT_ISO: new Date().toISOString(),
        PLACEHOLDER_SLUG: options.placeholder_slug || path.basename(roomDir),
        SOURCE_MATERIAL_ID: options.source_material_id || 'unknown',
      };

      const authored = isStateAuthored(roomDir);

      // Render STATE.md (skip if authored).
      if (!authored) {
        const stateTpl = readTemplate('STATE.md.tmpl');
        if (stateTpl) {
          const stateContent = renderTemplate(stateTpl, subs);
          if (atomicWrite(path.join(roomDir, 'STATE.md'), stateContent)) {
            result.state_written = true;
          } else {
            result.errors.push('state_write_failed');
          }
        } else {
          result.errors.push('state_template_missing');
        }
      } else {
        process.stderr.write('[room-skeleton] STATE.md already authored; skipping\n');
      }

      // Render MINTO.md (skip if file exists -- the sentinel-bounded content is the contract).
      const mintoPath = path.join(roomDir, 'MINTO.md');
      if (!fs.existsSync(mintoPath)) {
        const mintoTpl = readTemplate('MINTO.md.tmpl');
        if (mintoTpl) {
          if (atomicWrite(mintoPath, mintoTpl)) {
            result.minto_written = true;
          } else {
            result.errors.push('minto_write_failed');
          }
        }
      }

      // Render USER.md (skip if exists).
      const userPath = path.join(roomDir, 'USER.md');
      if (!fs.existsSync(userPath)) {
        const userTpl = readTemplate('USER.md.tmpl');
        if (userTpl) {
          if (atomicWrite(userPath, userTpl)) {
            result.user_written = true;
          } else {
            result.errors.push('user_write_failed');
          }
        }
      }

      // 8 ICM sections with per-section ROOM.md.
      const sectionTpl = readTemplate('ROOM.md.section.tmpl');
      if (sectionTpl) {
        for (const section of SECTION_NAMES) {
          const sectionDir = path.join(roomDir, section);
          const sectionRoomMd = path.join(sectionDir, 'ROOM.md');
          if (!fs.existsSync(sectionRoomMd)) {
            const meta = SECTION_METADATA[section];
            const sectionSubs = {
              SECTION_NAME: section,
              SECTION_NAME_TITLE_CASE: section.split('-').map(w => w.charAt(0).toUpperCase() + w.slice(1)).join(' '),
              SECTION_PURPOSE: meta.purpose,
              STAGE_RELEVANCE_LIST: meta.stage_relevance.map(s => '  - ' + s).join('\n'),
              DEFAULT_METHODOLOGIES_LIST: meta.default_methodologies.map(m => '  - ' + m).join('\n'),
            };
            const sectionContent = renderTemplate(sectionTpl, sectionSubs);
            if (atomicWrite(sectionRoomMd, sectionContent)) {
              result.sections_created.push(section);
            } else {
              result.errors.push('section_write_failed:' + section);
            }
          }
        }
      } else {
        result.errors.push('section_template_missing');
      }

      // Non-ICM identity directories with ROOM.md (Canon decision 15).
      const identityTpl = readTemplate('ROOM.md.identity.tmpl');
      if (identityTpl) {
        for (const dirName of Object.keys(IDENTITY_DIRECTORIES)) {
          const dirPath = path.join(roomDir, dirName);
          const identityRoomMd = path.join(dirPath, 'ROOM.md');
          if (!fs.existsSync(identityRoomMd)) {
            const meta = IDENTITY_DIRECTORIES[dirName];
            const identitySubs = {
              DIRECTORY_TYPE: meta.directory_type,
              DIRECTORY_PURPOSE: meta.purpose,
            };
            const identityContent = renderTemplate(identityTpl, identitySubs);
            if (atomicWrite(identityRoomMd, identityContent)) {
              result.identity_files_created.push(dirName);
            }
          }
        }
      }

      // Thinness pass-through (Plan 119-01 reads this to decide whether to render
      // the Larry voice line at F.1 selector time).
      try {
        const tha = require('./larry-thinness-acknowledgment.cjs');
        result.thinness_acknowledged = tha.shouldAcknowledgeThinness(options.auto_explore_finding || null);
      } catch (_e) {
        result.thinness_acknowledged = true; // fail-safe: default to acknowledging thinness
      }

      result.ok = result.errors.length === 0 || (result.state_written || result.sections_created.length > 0);
      return result;
    }

    module.exports = {
      SECTION_NAMES: SECTION_NAMES,
      SECTION_METADATA: SECTION_METADATA,
      IDENTITY_DIRECTORIES: IDENTITY_DIRECTORIES,
      scaffoldRoomSkeleton: scaffoldRoomSkeleton,
      renderTemplate: renderTemplate,
      isStateAuthored: isStateAuthored,
    };
    ```

    **Step 2: Create `lib/core/room-skeleton-scaffold.test.cjs` (~250 LOC).** Implements Tests 1-14 from `<behavior>`. Each test uses a fresh `os.tmpdir() + '/phase119-02-' + crypto.randomBytes(4).toString('hex')` to isolate. Tear-down with `fs.rmSync(...,{recursive:true,force:true})` in `after()`.

    **Step 3: Create `tests/test-room-skeleton-scaffold-integration.cjs` (~120 LOC).** End-to-end integration tests:
      - Integration Test 1: simulate Plan 119-00's autoCreatePlaceholderRoom output (a directory with `.room-root` + `.mindrian/room.db` only), then invoke `scaffoldRoomSkeleton` on it, and verify the full 8 sections + 5 identity dirs + STATE.md + MINTO.md + USER.md all materialize.
      - Integration Test 2: simulate a partial scaffold (sections present, STATE.md missing) and verify `scaffoldRoomSkeleton` fills only the missing pieces (reentrancy).
      - Integration Test 3: simulate a human-authored STATE.md (with `auto_created: false`), invoke `scaffoldRoomSkeleton`, verify STATE.md is byte-preserved AND the sections + identity files are still created if missing.

    **Step 4: Create `tests/test-119-02-scaffold.sh` (~50 LOC).** A 6-gate shell harness:
      - Gate 1: the 5 templates exist and have non-zero size.
      - Gate 2: `grep -c "I made a room around this -- it's mostly empty" lib/core/larry-thinness-acknowledgment.cjs` returns 1.
      - Gate 3: `grep -rE "require.+brain-client|fetch.+brain\\.mindrian" lib/core/room-skeleton-scaffold.cjs lib/core/larry-thinness-acknowledgment.cjs` returns 0 (Canon Part 8).
      - Gate 4: `grep -c "require\\(.+room-db\\.cjs" lib/core/room-skeleton-scaffold.cjs` returns 0 (Canon Part 9 chokepoint preserved).
      - Gate 5: `grep -rc "—" lib/core/room-skeleton-scaffold.cjs lib/core/larry-thinness-acknowledgment.cjs templates/room-skeleton/` returns 0 (em-dash HARD RULE).
      - Gate 6: `node -e "const s=require('./lib/core/room-skeleton-scaffold.cjs'); if(s.SECTION_NAMES.length!==8) process.exit(1)"` exits 0.
      - On success: exit 0 with literal line `OK: 119-02 scaffold complete (5 templates + skeleton orchestrator + thinness voice + chokepoint invariant + 8 ICM sections + zero em-dashes)`.

    **Step 5: Wire the new tests into `lib/memory/run-feynman-tests.cjs`.** Append the three new test files to `TEST_FILES` in a `// Phase 119-02 Wave 1 extension` comment block immediately after the Phase 119-00 block (if it lands first) or immediately after the latest existing block. Provenance comment REQUIRED.
  </action>
  <verify>
    <automated>node --test lib/core/room-skeleton-scaffold.test.cjs tests/test-room-skeleton-scaffold-integration.cjs && bash tests/test-119-02-scaffold.sh && node -e "const s=require('./lib/core/room-skeleton-scaffold.cjs'); console.log(s.SECTION_NAMES.length === 8 && Object.keys(s.IDENTITY_DIRECTORIES).length === 5)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test lib/core/room-skeleton-scaffold.test.cjs` reports 0 failures with at least 14 tests passing (all behaviors).
    - `node --test tests/test-room-skeleton-scaffold-integration.cjs` reports 0 failures with at least 3 integration tests passing.
    - `bash tests/test-119-02-scaffold.sh` exits 0 with stdout containing the literal `OK: 119-02 scaffold complete`.
    - `node -e "const s=require('./lib/core/room-skeleton-scaffold.cjs'); console.log(s.SECTION_NAMES.length, Object.keys(s.IDENTITY_DIRECTORIES).length)"` prints `8 5`.
    - `grep -E "require.+brain-client|fetch.+brain\\.mindrian" lib/core/room-skeleton-scaffold.cjs` returns 0 matches (Canon Part 8 invariant).
    - `grep -c "require\\(.+room-db\\.cjs" lib/core/room-skeleton-scaffold.cjs` returns 0 (Canon Part 9 chokepoint preserved -- this module writes files only, never the graph).
    - `grep -c "ICM Layer 0\\|Canon decision 15\\|Canon Part 8\\|Canon Part 9\\|Canon Part 10" lib/core/room-skeleton-scaffold.cjs` returns at least 4 (provenance + invariant documentation).
    - `grep -c "—" lib/core/room-skeleton-scaffold.cjs lib/core/room-skeleton-scaffold.test.cjs` returns 0 (em-dash HARD RULE).
    - End-to-end smoke: `TMP=$(mktemp -d) && mkdir -p $TMP/.mindrian && node -e "require('./lib/core/room-skeleton-scaffold.cjs').scaffoldRoomSkeleton('$TMP', {placeholder_slug:'untitled-test',source_material_id:'abc',auto_explore_finding:null})" && ls $TMP/problem-definition/ROOM.md $TMP/team/ROOM.md $TMP/STATE.md $TMP/MINTO.md $TMP/USER.md && rm -rf $TMP` succeeds with all 5 files listed.
    - The Phase 117 + Phase 118 test suites remain green (no regression introduced).
  </acceptance_criteria>
  <done>
    `lib/core/room-skeleton-scaffold.cjs` ships with `scaffoldRoomSkeleton` + `SECTION_NAMES` + `SECTION_METADATA` + `IDENTITY_DIRECTORIES` + `renderTemplate` + `isStateAuthored` exports; the entry point fills 8 ICM sections + 5 non-ICM identity directories + STATE.md + MINTO.md (sentinel-bounded) + USER.md from templates with `{{KEY}}` substitution; reentrancy invariant honored (never overwrites human-authored STATE.md per Canon Part 9 "files preserve meaning"); atomic-write idiom (tmp+rename) from Phase 124-02 inherited; thinness pass-through wired so Plan 119-01 can read `result.thinness_acknowledged` to decide whether to surface Larry's voice line. 17+ tests pass; Canon Part 8/9/10 invariants demonstrated; zero em-dashes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (REVISION 2026-05-16): Wire scaffoldRoomSkeleton into the auto-explore-fire success path (closes Blocker 1+4 phantom wiring; honors Option A: 119-02 owns auto-explore-fire.cjs)</name>
  <files>scripts/auto-explore-fire.cjs, tests/test-119-02-auto-explore-fire-wiring.cjs</files>
  <read_first>
    - scripts/auto-explore-fire.cjs lines 200-250 (the existing composeAutoExploreFinding return + atomicWriteJson pattern; the EXACT insertion point is BETWEEN line 234 (the `if (!finding)` early-exit) and line 243 (the `findingPath` definition for atomicWriteJson). NEW WIRING lands after a successful composeAutoExploreFinding return BUT BEFORE the auto-explore-<material_id>.json atomic write so the placeholder room's STATE.md materializes BEFORE the finding JSON is consumable; this ordering matches the Plan 119-00 invariant that the auto-explore output lands in a real room.db from byte 1)
    - lib/core/room-skeleton-scaffold.cjs (the scaffoldRoomSkeleton entry point shipped in Task 2 -- the consumer of this wiring; the function tolerates roomDir-already-scaffolded calls via the reentrancy invariant)
    - scripts/auto-explore-drain.cjs lines 105-135 (the canonical Claude Code "INSTRUCTION FOR LARRY" directive-file precedent -- understand how a CJS shim hands work off to the conversational surface so the wiring here respects the same boundaries)
    - .planning/phases/119-room-as-receipt-invariant/119-00-PLAN.md task-2 hook-insertion description (the Plan 119-00 sibling hook lands BEFORE auto-explore fire spawns; THIS task's hook lands AFTER auto-explore fire returns -- the two hooks are NOT in conflict; 119-00 ensures the room.db exists BEFORE the pipeline writes to it, 119-02 ensures STATE.md + MINTO.md + 8 sections materialize ONCE the auto-explore finding is composed)
  </read_first>
  <behavior>
    - Test 1 (insertion-point grep): `grep -c "scaffoldRoomSkeleton(roomDir" scripts/auto-explore-fire.cjs` returns exactly 1 (the new wiring call landed in exactly one place).
    - Test 2 (insertion ordering -- the scaffoldRoomSkeleton call lands AFTER composeAutoExploreFinding returns + BEFORE atomicWriteJson): the byte offset of the scaffoldRoomSkeleton call MUST sit between the byte offset of `composeAutoExploreFinding(` and the byte offset of `atomicWriteJson(findingPath`. Verified via a small node script that reads scripts/auto-explore-fire.cjs, computes substring indices, and asserts the ordering.
    - Test 3 (reentrancy guard): the new wiring is gated by `if (!fs.existsSync(path.join(roomDir, 'STATE.md')))` so that a re-fire of the auto-explore pipeline against an already-scaffolded room is a no-op. Verified by grep `grep -c "if (!fs.existsSync(path.join(roomDir, 'STATE.md')))" scripts/auto-explore-fire.cjs` returns 1.
    - Test 4 (graceful-degradation try/catch wrapping): the scaffoldRoomSkeleton call is wrapped in try/catch that logs to process.stderr but DOES NOT rethrow. Skeleton-scaffold failure MUST NOT regress the auto-explore pipeline (which has a 30-second budget and an `exit 0 always` invariant per the outer catch + uncaughtException backstop). Verified by grep that the require + the scaffoldRoomSkeleton call sit inside a `try {` block that catches and writes to stderr only.
    - Test 5 (no module-level require at the top of the file -- LAZY load inside the try block): the new wiring uses a LAZY `const { scaffoldRoomSkeleton } = require('../lib/core/room-skeleton-scaffold.cjs');` INSIDE the try block, NOT at the top of the file. Rationale: lazy load preserves the auto-explore-fire startup budget (the file currently requires only 4 modules at the top -- node:fs, node:path, node:child_process, explored-materials-store, auto-explore-agent; adding a fifth top-level require is unnecessary because the call site fires only once per first-material event).
    - Test 6 (integration smoke): `node -e "require('./scripts/auto-explore-fire.cjs')"` does not throw (the file is still syntactically valid + node-loadable). NOTE FOR EXECUTOR: the script's main() expects argv + env vars; loading it as a require validates only syntax + module shape, not runtime correctness.
    - Test 7 (full E2E in a scratch tmpdir): set up a tmp roomDir with `.mindrian/` + the discovery-cycle-results.json + .rs-engine-results.json + whitespace-results.json fixtures from Phase 117 tests; invoke scripts/auto-explore-fire.cjs as a child_process.execFile with the right argv; assert that AFTER the script exits 0, the tmp roomDir contains STATE.md + MINTO.md + USER.md + problem-definition/ROOM.md (the 119-02 scaffold materialized via the new wiring). This is the END-TO-END proof that Blocker 1+4 is closed.
    - Test 8 (em-dash invariant): `grep -c "—" scripts/auto-explore-fire.cjs` returns 0 (the new wiring + any new comments add zero em-dashes per the HARD RULE).
  </behavior>
  <action>
    **Step 1: Add the wiring block to `scripts/auto-explore-fire.cjs`.** The insertion lands between line 234 (the `if (!finding)` early-exit block close) and line 236 (the Phase 117-05 emitBrainCanonDrift call). Specifically, INSERT a new block AFTER the `if (!finding) { ... process.exit(0); }` close-brace and BEFORE the `// Phase 117-05 telemetry: a finding emerged` comment. Verbatim block to insert (mind the leading 4 spaces -- the file uses 4-space indent inside `function main()`):

    ```javascript
        // Phase 119-02 Task 3 (REVISION 2026-05-16): wire scaffoldRoomSkeleton into the
        // auto-explore-fire success path so the placeholder room created by Plan 119-00's
        // autoCreatePlaceholderRoom gets its STATE.md + MINTO.md + USER.md + 8 ICM section
        // folders + 5 identity-directory ROOM.md files materialized BEFORE the auto-explore
        // finding JSON is consumable. The wiring is reentrancy-guarded so a re-fire of
        // auto-explore against an already-scaffolded room is a no-op.
        //
        // Canon Part 9: scaffold writes files only -- no graph writes from here; the
        // room_auto_created memory_event fires from Plan 119-00 (this script's caller).
        // Canon Part 10 sub-claim 3: rooms are receipts -- the receipt's substrate
        // materializes the instant the auto-explore finding lands.
        // Graceful-degradation: skeleton-scaffold failure logs to stderr but DOES NOT
        // regress the auto-explore pipeline (Phase 117's `exit 0 always` invariant).
        if (!fs.existsSync(path.join(roomDir, 'STATE.md'))) {
          try {
            const { scaffoldRoomSkeleton } = require('../lib/core/room-skeleton-scaffold.cjs');
            scaffoldRoomSkeleton(roomDir, {
              placeholder_slug: path.basename(roomDir),
              source_material_id: material_id,
              auto_explore_finding: finding,
            });
          } catch (scaffoldErr) {
            process.stderr.write('[auto-explore-fire] room-skeleton-scaffold failed (non-fatal): ' +
              String(scaffoldErr && scaffoldErr.message || scaffoldErr).slice(0, 120) + '\n');
          }
        }

    ```

    Use the `Edit` tool. Anchor on the exact text `if (!finding) {
      try { store.markFailed(roomSlug, material_id, 'all_pipelines_empty'); } catch (_e) { /* ignore */ }
      // Phase 117-05 telemetry: suppression path emits auto_explore_skipped.
      try { agent.emitSkipped(roomDir, { material_id: material_id, suppress_reason: 'all_pipelines_empty', tier: 1 }); } catch (_e) { /* ignore */ }
      process.exit(0);
    }

    // Phase 117-05 telemetry: a finding emerged` to land the new block in EXACTLY the right place. If the line numbers shifted from a concurrent landing (e.g. a Phase 121 telemetry hot-patch landed between this plan's authoring and its execution), grep for the anchor `// Phase 117-05 telemetry: a finding emerged -- the natural trigger point` and insert the new block IMMEDIATELY BEFORE that comment.

    **Step 2: Create `tests/test-119-02-auto-explore-fire-wiring.cjs` (~120 LOC).** Use `node --test`. Implement Tests 1-8 from `<behavior>`:
      - Test 1, 2, 3, 4, 5, 8 are static grep / byte-offset assertions on the source file contents.
      - Test 6 is `assert.doesNotThrow(() => require('../scripts/auto-explore-fire.cjs'))` -- BUT note that requiring the script triggers `main()` because the file's tail line is `main();`. To prevent test pollution, the test uses `child_process.execFileSync('node', ['-e', "require('./scripts/auto-explore-fire.cjs')"])` and asserts exitCode is 0 (the script's own outer catch + uncaughtException backstop ensures non-fatal exit on missing argv/env).
      - Test 7 is the full E2E integration. Set up the fixtures with the exact shape Phase 117 produces (read tests/test-117-fixtures/* if they exist, or hand-construct minimal JSON shells); invoke the script via execFileSync with `MINDRIAN_ROOMS_HOME=/tmp/test-119-02-e2e` + roomSlug + material_id + relativeFilePath argv; after exit, assert the tmp roomDir contains the 11 scaffold artifacts (STATE.md + MINTO.md + USER.md + 8 section ROOM.mds).

    **Step 3: Wire the new test into `lib/memory/run-feynman-tests.cjs`.** Append `tests/test-119-02-auto-explore-fire-wiring.cjs` to the Phase 119-02 TEST_FILES block (created when Task 2 wires its 3 tests; THIS task adds the 4th).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('scripts/auto-explore-fire.cjs','utf8'); const a=c.indexOf('composeAutoExploreFinding('); const b=c.indexOf('scaffoldRoomSkeleton(roomDir'); const d=c.indexOf('atomicWriteJson(findingPath'); if(!(a>0&&b>a&&d>b)) {console.error('ordering broken: composeAutoExploreFinding=',a,'scaffoldRoomSkeleton=',b,'atomicWriteJson=',d); process.exit(1)} console.log('ordering OK')" && grep -c "scaffoldRoomSkeleton(roomDir" scripts/auto-explore-fire.cjs && grep -c "if (!fs.existsSync(path.join(roomDir, 'STATE.md')))" scripts/auto-explore-fire.cjs && grep -c "—" scripts/auto-explore-fire.cjs && node --test tests/test-119-02-auto-explore-fire-wiring.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scaffoldRoomSkeleton(roomDir" scripts/auto-explore-fire.cjs` returns exactly 1 (Blocker 1+4 phantom wiring closed).
    - `node -e "const c=require('fs').readFileSync('scripts/auto-explore-fire.cjs','utf8'); const a=c.indexOf('composeAutoExploreFinding('); const b=c.indexOf('scaffoldRoomSkeleton(roomDir'); const d=c.indexOf('atomicWriteJson(findingPath'); process.exit(a>0&&b>a&&d>b?0:1)"` exits 0 (the ordering invariant: scaffold lands AFTER composeAutoExploreFinding + BEFORE atomicWriteJson).
    - `grep -c "if (!fs.existsSync(path.join(roomDir, 'STATE.md')))" scripts/auto-explore-fire.cjs` returns 1 (the reentrancy guard landed; re-firing on an already-scaffolded room is a no-op).
    - `grep -c "—" scripts/auto-explore-fire.cjs` returns 0 (em-dash HARD RULE; the new block adds zero em-dashes).
    - `node --test tests/test-119-02-auto-explore-fire-wiring.cjs` reports 0 failures with at least 6 tests passing.
    - End-to-end integration smoke (Test 7) materializes STATE.md + MINTO.md + USER.md + problem-definition/ROOM.md in a tmp roomDir after a real auto-explore-fire invocation.
    - `node -e "require('./scripts/auto-explore-fire.cjs')"` does NOT throw a `MODULE_NOT_FOUND` error citing room-skeleton-scaffold.cjs (the lazy require resolves correctly at runtime).
  </acceptance_criteria>
  <done>
    Plan 119-02 wires scaffoldRoomSkeleton into `scripts/auto-explore-fire.cjs` AFTER composeAutoExploreFinding returns AND BEFORE atomicWriteJson of auto-explore-<material_id>.json. The wiring is reentrancy-guarded (skips if STATE.md already exists), graceful-degradation-wrapped (try/catch around the lazy require + scaffold call; failure logs to stderr but does NOT regress the auto-explore pipeline), and the key_link declared in the frontmatter (scripts/auto-explore-fire.cjs -> scaffoldRoomSkeleton) is now a real on-disk edge, not phantom advertising. Blocker 1+4 closed. Em-dash HARD RULE preserved. The Plan 119-01 Wave 2 contract is now intact -- by the time Plan 119-01's naming selector fires after MVA completion, the placeholder room has STATE.md + MINTO.md + USER.md + the 8 ICM section folders materialized so the rename ceremony has real substrate to operate on.
  </done>
</task>

</tasks>

<verification>
**Plan 119-02 overall verification:**

```bash
# Task 1: templates + thinness voice module
node --test lib/core/larry-thinness-acknowledgment.test.cjs
grep -c "I made a room around this -- it's mostly empty until we have more to work with. Want to keep going and see what fills in?" lib/core/larry-thinness-acknowledgment.cjs
ls templates/room-skeleton/

# Task 2: scaffold orchestrator
node --test lib/core/room-skeleton-scaffold.test.cjs tests/test-room-skeleton-scaffold-integration.cjs
bash tests/test-119-02-scaffold.sh

# End-to-end smoke (the full Plan 119-02 wire):
TMP=$(mktemp -d -t mos119-02-XXXX)
mkdir -p $TMP/.mindrian
touch $TMP/.room-root
node -e "
  const s=require('./lib/core/room-skeleton-scaffold.cjs');
  const r=s.scaffoldRoomSkeleton('$TMP', {
    placeholder_slug:'untitled-2026-05-16-1845',
    source_material_id:'deadbeef'.repeat(8),
    auto_explore_finding: null
  });
  console.log(JSON.stringify(r, null, 2));
"
# Verify: 8 ICM section folders + 5 identity dirs + STATE.md + MINTO.md + USER.md
ls -la $TMP/problem-definition/ROOM.md $TMP/team/ROOM.md $TMP/.intelligence/ROOM.md
cat $TMP/MINTO.md | grep -c "mos:minto-begin"  # expect: 1
cat $TMP/STATE.md | grep -E "phase: scoping|placeholder_slug: 'untitled-2026-05-16-1845'"
rm -rf $TMP

# Larry voice line accessibility:
node -e "const t=require('./lib/core/larry-thinness-acknowledgment.cjs'); console.log(t.voiceLine())"
# Expect verbatim: I made a room around this -- it's mostly empty until we have more to work with. Want to keep going and see what fills in?
```

**Regression check (must pass):**
```bash
# Phase 88 MINTO invariants must still hold:
grep -c "mos:minto-begin\|mos:minto-end" templates/room-skeleton/MINTO.md.tmpl  # expect: 2

# No regression on Phase 117 / 118 test suites:
bash tests/run-all-117.sh 2>/dev/null || true
bash tests/run-all-118.sh 2>/dev/null || true
```
</verification>

<success_criteria>
- 5 template files exist in `templates/room-skeleton/` (STATE.md.tmpl, MINTO.md.tmpl, ROOM.md.section.tmpl, ROOM.md.identity.tmpl, USER.md.tmpl) with non-zero size and valid Markdown / YAML.
- `lib/core/larry-thinness-acknowledgment.cjs` exports the LOCKED verbatim D-05 voice line via `THINNESS_VOICE_LINE` + `voiceLine()` (single source of truth, no string drift); the verbatim string is 152 ASCII characters exactly (no em-dashes, no smart quotes).
- `lib/core/room-skeleton-scaffold.cjs` exports `scaffoldRoomSkeleton` (the entry point), `SECTION_NAMES` (the 8 ICM sections), `IDENTITY_DIRECTORIES` (the 5 non-ICM directories per Canon decision 15), and `renderTemplate`.
- All 17+ tests pass (6 thinness-acknowledgment + 14 scaffold-unit + 3 scaffold-integration).
- Canon Part 8 boundary preserved (no Brain MCP, no fetches).
- Canon Part 9 chokepoint preserved (no direct room-db.cjs require -- the module writes files only).
- Canon decision 15 honored: every directory in the scaffolded room has ROOM.md.
- Reentrancy invariant proven: human-authored STATE.md is byte-preserved across re-invocations.
- Atomic-write idiom from Phase 124-02 inherited (tmp + rename).
- Em-dash HARD RULE honored (zero `—` characters across all new files).
- ROOMRECEIPT-119-05 (skeleton scaffold even with thin material + Larry thinness voice line) requirement fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/119-room-as-receipt-invariant/119-02-SUMMARY.md` with the standard SUMMARY frontmatter (phase, plan, subsystem, tags, requires, provides, affects, tech-stack, key-files, key-decisions, patterns-established, requirements-completed, duration, completed) plus a 3-section body:
  1. Performance (duration + tasks + files created + test count)
  2. Template directory tree (ASCII showing the 5 .tmpl files + the lookup-table relationship between SECTION_METADATA and ROOM.md.section.tmpl)
  3. What Plan 119-01 inherits (the contract handoff: scaffoldRoomSkeleton + THINNESS_VOICE_LINE + IDENTITY_DIRECTORIES are ready to be wired into the auto-explore-fire success path; the F.1 selector in 119-01 reads `result.thinness_acknowledged` to decide whether to prepend the voice line to the Larry render)
</output>
