---
phase: "83"
plan_number: "08"
name: "Honesty layer in larry-personality + CHANGELOG + v1.10.7 5-gate release"
status: draft
depends_on: ["83-07"]
parallel_safe: false
estimated_size: large
---

# Plan 83-08: Honesty layer in larry-personality + CHANGELOG + v1.10.7 5-gate release

## Goal

Two parts in one plan:

**Part A: Honesty layer.** Update skills/larry-personality/SKILL.md to add an explicit no-fake-recall rule that forbids the phrase "I do not have that in working memory" and any variant that implies stored state MindrianOS does not actually have today. Ship 3 CORRECT examples and 1 INCORRECT example. Add a markdown-level test that asserts the rule and at least one CORRECT example are present. Closes leak vector 8 from 83-CONTEXT.md and SCOPE-INJ-10.

**Part B: 5-gate release as v1.10.7.** CHANGELOG [1.10.7] entry crediting Jonathan, naming all 8 leak vectors, explaining Tier 1 + 1.5 + 2 + Honesty Layer, deferring Tier 3 to v1.10.8. Version bumps in plugin.json and package.json from 1.10.5 to 1.10.7. ROADMAP/TODO/PROJECT.md slot shifts (smart-notebook v1.10.6 -> v1.10.7 -> v1.10.8, sixth shift). Commit, tag v1.10.7, push, marketplace update. Closes SCOPE-INJ-07 (CHANGELOG half) and SCOPE-INJ-11 (Honesty test half).

## Scope

### Part A: Honesty layer

1. Edit `skills/larry-personality/SKILL.md`. Add a new top-level section titled `## Honesty about memory` placed after `## Integration Offers` and before `## Onboarding: Invoked + Provoked`. Exact content:

   ```markdown
   ## Honesty about memory

   ### No fake recall

   When a user asks "do you remember X" and X is not in your current session context, the correct response language is:
   - "Let me search for that"
   - "I do not have that loaded, looking now"
   - "I have no record of that in this session, searching"

   The phrase "I do not have that in working memory" is FORBIDDEN because:
   1. MindrianOS does not have a working memory layer today. The SQLite memory layer at lib/core/memory-ops.cjs exists but is unwired.
   2. The phrase implies stored state that does not exist.
   3. After a successful filesystem search recovery, the prior denial reads as a lie.
   4. Users trust "I do not remember" for one second, then watch detailed recall, and that exact moment is when trust collapses.

   Examples:

   CORRECT:
   User: "Do you remember the rashut conversation?"
   Larry: "Let me search. I do not have rashut loaded in this session."
   [searches]
   Larry: "Found it at ~/MindrianRooms/rashut-hadshanut-ai. Created 2026-04-13. 21 entries. Want a quick recall?"

   CORRECT:
   User: "What did we decide last time?"
   Larry: "I do not have last session loaded. Searching the room now."
   [searches]
   Larry: "Found the decision in STATE.md: you chose option B. Want the context?"

   CORRECT:
   User: "You mentioned the Milken connection earlier."
   Larry: "Not in this session. Let me check the room."
   [searches]
   Larry: "Yes, it is in align-x-milken. Pulling the context now."

   INCORRECT:
   User: "Do you remember the rashut conversation?"
   Larry: "I do not have that in working memory right now."
   [searches]
   Larry: "Found it, here are 21 details I did not actually remember but am pretending to."

   The honest version takes 4 extra words. The dishonest version takes user trust.
   ```

2. Add a markdown-level test. File: `test/83-honesty-layer.test.cjs` (or registered with the same runner as 83-04). Assertions:
   - SKILL.md contains the `## Honesty about memory` heading
   - SKILL.md contains the `### No fake recall` sub-heading
   - SKILL.md contains the literal string `"I do not have that in working memory"` (documented as forbidden, not used)
   - SKILL.md contains at least one `CORRECT:` example block
   - SKILL.md contains at least one `INCORRECT:` example block
   - The CORRECT example block appears lexically before the INCORRECT block (forbidden example comes after)

### Part B: 5-gate release

3. Update `CHANGELOG.md`. Add a new entry at the top:

   ```markdown
   ## [1.10.7] - 2026-04-14

   ### Added
   - Cross-session scope injection: session-start now injects ACTIVE ROOM CONTEXT and Cross-Room Policy into every Claude session, reading the active room from ~/MindrianRooms/.rooms/registry.json (Tier 1)
   - Sealed room walker: session-start walks ~/MindrianRooms/ for any subdirectory containing GUARDRAIL.md and surfaces sealed rooms with their first 3 hard-rule lines quoted (Tier 1)
   - Filesystem write interception: a new PreToolUse hook blocks Write/Edit/MultiEdit operations that target a non-active room under ~/MindrianRooms/, with actionable /mos:rooms switch hints. Sealed rooms block unconditionally. (Tier 1.5)
   - Mid-session intent classifier: a new UserPromptSubmit hook scores the user message against every room in the registry plus every sealed room on the machine and warns when the highest-scoring room is not the active one (Tier 2)
   - Honesty layer in larry-personality: new "## Honesty about memory" section forbids the phrase "I do not have that in working memory" and requires "let me search" language before filesystem recall

   ### Fixed
   - Statusline wrapper bundle: scripts/statusline-mos now ships as a plugin file. session-start auto-installs it to ~/.claude/statusline-mos and migrates settings.json from the hardcoded context-monitor path to the wrapper path. Detection-driven, idempotent, non-clobbering for users who hand-fixed their config.
   - Cross-session leak (8 vectors): Jonathan Sagir caught a witnessed failure on 2026-04-14 where a single Claude Code session leaked content from the sealed rashut-hadshanut-ai room across recall, drafting, methodology execution, filesystem writes, recovery pivots, Hebrew translation filing, mid-session topic recognition, and honesty-layer collapse. v1.10.7 closes vectors 1 through 8 via Tier 1 + Tier 1.5 + Tier 2 + Honesty Layer. See .planning/research/cross-session-memory-and-room-intent.md and .planning/phases/83-cross-session-scope-injection/83-CONTEXT.md for the full analysis.

   ### Changed
   - Smart-notebook milestone slot shifts v1.10.6 -> v1.10.7 -> v1.10.8. Sixth shift in this v1.10.x patch line. Smart-notebook in v1.10.8 will promote the SQLite memory layer at lib/core/memory-ops.cjs to load-bearing and deliver real persistent cross-session memory (Tier 3), voice-log per room, and synthesis voice room-scoping.
   - This release acknowledges openly: MindrianOS does not yet have real cross-session memory. What ships here is read-time scope injection, write-time scope interception, message-time intent classification, and a language rule preventing the assistant from calling filesystem search "memory". Real memory wiring is v1.10.8.
   ```

   Zero em-dashes. Zero emoji. Use hyphens only.

4. Bump `.claude-plugin/plugin.json` `version` from 1.10.5 to 1.10.7.

5. Bump `package.json` `version` from 1.10.5 to 1.10.7. (If package.json does not exist or is at a different version, adjust accordingly and document in commit message.)

6. Update PROJECT.md, TODO.md, ROADMAP.md slot shifts:
   - Smart-notebook moves from v1.10.6 slot to v1.10.8 slot (sixth shift)
   - v1.10.7 slot filled with "Cross-session scope + write interception + intent classifier + honesty layer"
   - Note the slot-shift count in ROADMAP.md

7. Commit:
   ```
   release: v1.10.7 -- cross-session scope + write interception + intent classifier + honesty layer (Jonathan witnessed 8 leak vectors)

   Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
   ```

8. Tag:
   ```
   git tag -a v1.10.7 -m "v1.10.7 - Cross-session leak fix (8 vectors)"
   ```

9. Push:
   ```
   git push origin main --tags
   ```

10. Marketplace update (cross-repo, pre-authorized):
    ```
    cd ~/mindrian-marketplace/
    # sed or python3 update .claude-plugin/marketplace.json: version -> 1.10.7, source.ref -> v1.10.7
    git add .claude-plugin/marketplace.json
    git commit -m "chore: pin mos@mindrian-marketplace to v1.10.7"
    git push origin main
    cd /home/jsagi/MindrianOS-Plugin
    ```

11. Verify all 5 gates green per `.claude/includes/release-process.md`:
    - CHANGELOG [1.10.7] entry present at top
    - plugin.json version = 1.10.7
    - package.json version = 1.10.7
    - git tag v1.10.7 exists pointing at the release commit
    - marketplace.json version = 1.10.7 AND source.ref = v1.10.7

## Out of Scope

- Wiring lib/core/memory-ops.cjs (v1.10.8 smart-notebook scope)
- Adding a beta suffix (this is a direct-to-stable bug fix, not release infrastructure)
- Rewriting the wrapper (unchanged from 83-01)
- Re-cutting smart-notebook research (separate task for v1.10.8 plan-phase)

## Task Breakdown

1. Edit skills/larry-personality/SKILL.md, add "## Honesty about memory" section.
2. Write test/83-honesty-layer.test.cjs with assertions listed above.
3. Run full test runner (all of 83-04, 83-06, 83-07, 83-08 tests) and confirm all pass.
4. Write CHANGELOG [1.10.7] entry.
5. Bump plugin.json and package.json versions to 1.10.7.
6. Update PROJECT.md / TODO.md / ROADMAP.md slot shifts.
7. Grep zero em-dashes across the full diff.
8. Commit with the release message.
9. Tag v1.10.7.
10. Push with --tags.
11. Marketplace update in the cross-repo.
12. Verify all 5 gates.

## Tests

- Honesty layer markdown test: see Part A step 2
- All Tier 1, Tier 1.5, Tier 2 tests from 83-04, 83-06, 83-07 must still pass
- No new tests for the release steps themselves (release is verified by the 5-gate checklist)

## Verification

- `node <test-runner>` exits 0, all test suites pass
- `grep -n "em dash or unicode 2014" CHANGELOG.md skills/larry-personality/SKILL.md` returns nothing
- `git log --oneline -1` shows the release commit
- `git tag --list v1.10.7` returns v1.10.7
- `cat .claude-plugin/plugin.json | python3 -c 'import json,sys; print(json.load(sys.stdin)["version"])'` returns 1.10.7
- `cat package.json | python3 -c 'import json,sys; print(json.load(sys.stdin)["version"])'` returns 1.10.7
- `cat ~/mindrian-marketplace/.claude-plugin/marketplace.json` shows version 1.10.7 and source.ref v1.10.7

## Requirements Covered

- SCOPE-INJ-07 (CHANGELOG + slot shift half)
- SCOPE-INJ-10 (honesty layer in larry-personality)
- SCOPE-INJ-11 (test coverage Honesty Layer, jointly with 83-06 and 83-07)

## Dependencies

- 83-07 (all Tier 1, 1.5, 2 implementation must be on disk and passing before shipping)

## Risks

- Risk: SKILL.md insertion point may collide with a later skill edit. Mitigation: insert after "## Integration Offers" and before "## Onboarding: Invoked + Provoked", a stable pair confirmed by reading the current file.
- Risk: package.json may not exist in this repo or may be at a different version than plugin.json. Mitigation: inspect both, fix both, document in commit message if they drift.
- Risk: Marketplace repo push fails or requires auth. Mitigation: pre-authorized per user statement. On failure, escalate immediately and do not proceed.
- Risk: Pre-push hook or pre-commit hook rejects the commit. Mitigation: fix the cited issue and create a new commit (never --no-verify unless user says so).
- Risk: Tag exists already (from a prior aborted release). Mitigation: check with `git tag --list v1.10.7`; if present, confirm it points at HEAD; if it points at a different commit, escalate.
- Risk: Slot-shift update to ROADMAP.md is manual and error-prone. Mitigation: grep for "v1.10.6" and "smart-notebook" before editing; double-check after.
- Risk: Honesty layer test is brittle to whitespace changes in SKILL.md. Mitigation: use normalized reads (collapse whitespace, lowercase) for the assertions.
- Risk: Release ships but a user's cache still serves the old version because marketplace auto-update is OFF by default. Mitigation: this is correct-by-design per release-process.md; users opt in or run the two-command manual upgrade.

## Deviation Notes

- This plan combines the honesty layer and the release in a single plan to keep the plan count at exactly 8 as locked by the user. The release is the last step because it depends on every prior plan shipping its code.
- No beta gating. This is a direct-to-stable bug fix for a witnessed active failure. Release infrastructure beta gating does not apply.
- Slot shift language is the sixth shift; if subsequent audit reveals a different count, correct in the commit message but do not change the plan's intent.
