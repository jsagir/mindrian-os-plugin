---
phase: 213-eureka-reach-wiring
plan: 04
type: execute
wave: 2
depends_on: [213-02]
files_modified:
  - lib/core/eureka/eureka-offer.cjs
  - tests/test-213-eureka-offer.cjs
autonomous: true
requirements: [EUREKA-03, EUREKA-07]
tags: [eureka, larryreacts, shape-f, offer]

must_haves:
  truths:
    - "composeEurekaOffer emits exactly ONE hedged offer object or null - never a list, never a command execution (the single-offer contract, navigation-engine-offer idiom)"
    - "The offer text names the two bridged node HANDLES and the direction enum (structural_transfer vs semantic_implementation) and NOTHING from artifact content (Part 8, RESEARCH V6 property d)"
    - "The recommended next command comes from commands/*.md frontmatter via data/command-registry.json through composeWorkflow - the SINGLE canonical command-truth source; no dossier/sub-room/room.db read at runtime (LOCKED)"
    - "The optional Brain leg rides recommendFrameworkChain with its isAvailable() sync degrade - generic framework handles only, and the offer works fully offline"
    - "Confidence is passed through from the side-channel guard confidence band - never re-derived or re-weighted downstream (the D-02 offer discipline)"
    - "Larry voices it HEDGED per Canon Part 12: an opportunity plus a question, never a grade, never a directive"
  artifacts:
    - path: "lib/core/eureka/eureka-offer.cjs"
      provides: "The LarryReacts eureka offer composer (Shape-F option CONTENT, not a new selector)"
      exports: ["composeEurekaOffer", "DIRECTION_ENUM"]
      min_lines: 80
  key_links:
    - from: "lib/core/eureka/eureka-offer.cjs"
      to: "lib/brain/chain-recommender.cjs"
      via: "recommendFrameworkChain (FEEDS_INTO, degrade-safe)"
      pattern: "recommendFrameworkChain"
    - from: "lib/core/eureka/eureka-offer.cjs"
      to: "lib/workflow/command-resolver.cjs"
      via: "composeWorkflow attaches commands to the framework chain"
      pattern: "composeWorkflow"
    - from: "lib/core/eureka/eureka-offer.cjs"
      to: ".mindrian/last-eureka.json"
      via: "reads the plan-02 side-channel handles/enums (never room content)"
      pattern: "last-eureka"
---

<objective>
Build the LarryReacts leg: the pure composer that turns a guard-cleared eureka side-channel into Shape-F offer CONTENT - the hedged Larry line naming the two bridged node handles + transfer direction, plus the recommended next command chain drawn from the registry. This is the "recommend the right next command as a natural conversational segue" goal the 2026-07-05 addendum names as Phase 213's real intent, built on the ~85%-shipped substrate (offer composer, chain recommender, resolver) - wired into, never rebuilt.

Purpose: RESEARCH V4's M4 caution - the 213 failure mode is a SENS-13 that registers but whose offer never renders. Plan 03 proves the reach surfaces; this plan makes what surfaces WORTH confirming: concrete, hedged, actionable content with the next-command segue attached.

Output: eureka-offer.cjs + its test suite. Consumption by the F.7 dial happens through the existing fired-reach render path (deep_research maps to the closed 'Spawn Sub-Agent' shape); this module produces the option content object those surfaces read - it adds NO new selector and NO render fork (SEED-020 single door respected).

**EXECUTION PRECONDITIONS:** same two phase gates ((a) curing-track verdict, (b) 212-05 checkpoint) plus 213-02 SUMMARY exists.

Acceptance criteria (objective, grep-able / exit codes):
- `node tests/test-213-eureka-offer.cjs` exits 0
- `grep -c "recommendFrameworkChain" lib/core/eureka/eureka-offer.cjs` >= 1 and `grep -c "composeWorkflow" lib/core/eureka/eureka-offer.cjs` >= 1 (wired into, not rebuilt)
- `grep -cE "command-research|room\.db|RELATED_TO|dossier" lib/core/eureka/eureka-offer.cjs` returns 0 (single command-truth source; enrichment inputs never become a runtime path)
- `grep -cE "require\(.*chain-executor" lib/core/eureka/eureka-offer.cjs` returns 0 (composes an offer, never executes)
- `grep -cP '\x{2014}' lib/core/eureka/eureka-offer.cjs tests/test-213-eureka-offer.cjs` returns 0 per file
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-RESEARCH.md (V4, V6, Architectural Responsibility Map rows 3-5)
@lib/core/navigation-engine-offer.cjs (:1-120 - the single-offer/abstain/confidence contract to mirror; D-02: consumers never re-derive confidence)
@lib/brain/chain-recommender.cjs (:1-90 - recommendFrameworkChain contract + isAvailable degrade + Part-8 comment discipline)
@lib/workflow/command-resolver.cjs (:104-143 - composeWorkflow + validateChainAutonomy shapes)
@lib/core/eureka/eureka-reach-runner.cjs (the side-channel schema this consumes)
@.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/core/eureka/eureka-offer.cjs - composeEurekaOffer</name>
  <files>lib/core/eureka/eureka-offer.cjs</files>
  <read_first>
    - lib/core/navigation-engine-offer.cjs :85-125 (resolveOffer return shape { command, framework, jtbd, confidence, reason, scope })
    - lib/brain/chain-recommender.cjs :6-35 (chain contract: [seed, ...successors] max 4, never a command name)
    - lib/workflow/command-resolver.cjs :110-143 (composeWorkflow step shape + validateChainAutonomy)
    - tests/fixtures/213/last-eureka.json (input schema)
  </read_first>
  <behavior>
    - Test 1: a valid side-channel payload (handles n042/n317, surprise_type structural_transfer, guard confidence 'high') -> exactly one offer object { text, a_handle, b_handle, direction, confidence, next: { chain, workflow } } where direction is a DIRECTION_ENUM member and confidence === the input guard confidence (passthrough, never recomputed)
    - Test 2: offer.text contains both handles and a hedged question mark, contains the direction enum wording, and contains NO string from any bridge content field (the fixture carries no content fields by schema; the test asserts text is composed ONLY from handles + enums + fixed hedged phrasing)
    - Test 3: guard verdict not 'transferable' or payload absent/stale-shaped -> null (abstain; a wrong offer trains the ignore reflex)
    - Test 4: Brain unavailable (injected recommendFn returning the seed-only chain) -> offer still emits with next.chain length 1 (offline-full-function)
    - Test 5: a framework with no registry command -> the workflow step degrades to { command: null, optional: true } and the offer survives (composeWorkflow contract honored, not re-implemented)
    - Test 6: the module never requires chain-executor and composeEurekaOffer never invokes any command (pure composition)
  </behavior>
  <action>
    Create lib/core/eureka/eureka-offer.cjs (CJS, pure-composition, sync except the injected recommend seam kept sync-shaped per chain-recommender's sync degrade). Export DIRECTION_ENUM = Object.freeze(['structural_transfer','semantic_implementation']) and composeEurekaOffer({ payload, recommendFn?, composeWorkflowFn?, problemType? }) -> offer | null. Logic: validate payload is the plan-02 closed schema with guard.verdict 'transferable' (else null); map surprise_type to the direction enum; build the hedged Part-12 line from a fixed template - name the two node HANDLES and the direction, offer-as-question, no grade, no imperative (the RESEARCH architecture-diagram wording: "X and Y look like the same structure - here is the opportunity + transfer direction. Want to pursue it?" adapted with handles); thread confidence STRAIGHT THROUGH from payload.guard.confidence (D-02: never re-derived). Next-command segue: chain = (recommendFn || recommendFrameworkChain)({ problemType }) with try/catch degrade to the default seed; workflow = (composeWorkflowFn || composeWorkflow)(chain); attach { chain, workflow } as offer.next; note in the header that runChain consumption of a CONFIRMED offer is the existing act-command path (halt-at-material, EXEC budgets) - this module stops at content, the navigator verb is the only bridge to execution. Header also states: Shape-F option content only, no new selector, no second scoring of reaches (decide() is the one selection brain); command truth = registry/frontmatter ONLY.
  </action>
  <verify>
    <automated>node -e "const o=require('./lib/core/eureka/eureka-offer.cjs'); if(o.DIRECTION_ENUM.length!==2) throw new Error('enum'); if(o.composeEurekaOffer({payload:null})!==null) throw new Error('abstain'); console.log('composer loads + abstains ok')"</automated>
  </verify>
  <done>Composer exists, single-offer-or-null, handles+enums-only text, registry-only command truth, degrade paths built.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test-213-eureka-offer.cjs - the offer contract suite</name>
  <files>tests/test-213-eureka-offer.cjs</files>
  <read_first>
    - tests/test-213-sensor-eureka.cjs (plan 02's fixture handling idiom)
    - lib/workflow/command-resolver.cjs :145-155 (__reset test seam for registry re-pointing if needed)
  </read_first>
  <action>
    Write the suite covering all 6 Task-1 behaviors with bare node assert, hermetic: recommendFn/composeWorkflowFn injected everywhere (no live Brain, no registry dependence for the degrade arms; one arm MAY use the real composeWorkflow against the live data/command-registry.json to pin the integration - guard it with fs.existsSync on the registry and a SKIP log). Add the Part-8 text-composition arm as a strict property: build the offer from a payload whose handles are sentinel strings, then assert every non-fixed-template token in offer.text is either a sentinel handle or a DIRECTION_ENUM member - nothing else survives into the text. Add a voice arm: text matches the hedged shape (ends with a question), contains no em-dash character, no grading vocabulary (/\b(great|excellent|well done)\b/i zero hits) per Canon Part 12.
  </action>
  <verify>
    <automated>node tests/test-213-eureka-offer.cjs</automated>
  </verify>
  <done>Suite green hermetically; the token-whitelist arm mechanically proves handles+enums-only text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| side-channel -> offer text | handles + enums only may enter the rendered line (the token-whitelist test is the fence) |
| offer -> Brain leg | recommendFrameworkChain carries framework names + problem-type enums only (its own Part-8 guard) |
| offer -> execution | none: navigator confirm is the only bridge; module never requires chain-executor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-11 | Information Disclosure | offer text composition | mitigate | token-whitelist test arm; fixed template + handles + enums only |
| T-213-12 | Elevation of Privilege | offer -> runChain | mitigate | no chain-executor require (grep gate); plan-03 no-force suite covers this file once present |
| T-213-13 | Spoofing (fabricated confidence) | confidence passthrough | mitigate | D-02 passthrough pinned by Test 1; no local re-derivation code path exists |
</threat_model>

## Artifacts this phase produces (plan 04 contribution)

| Artifact | Kind | Names/symbols |
|----------|------|---------------|
| lib/core/eureka/eureka-offer.cjs | module | composeEurekaOffer, DIRECTION_ENUM=['structural_transfer','semantic_implementation'], offer shape { text, a_handle, b_handle, direction, confidence, next: { chain, workflow } } |
| tests/test-213-eureka-offer.cjs | test | 6 behaviors + token-whitelist + voice arms |
| New env vars | none | - |

<verification>
- `node tests/test-213-eureka-offer.cjs` exit 0
- objective grep gates pass (wired-into strings present; forbidden runtime sources absent; no chain-executor; no em-dashes)
</verification>

<success_criteria>
A guard-cleared eureka becomes one hedged, handle-and-enum-only Shape-F offer with a registry-derived next-command segue, fully offline-capable, structurally incapable of executing anything - LarryReacts recommends; the navigator triggers.
</success_criteria>

<output>
Create `.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-04-SUMMARY.md` when done.
</output>
