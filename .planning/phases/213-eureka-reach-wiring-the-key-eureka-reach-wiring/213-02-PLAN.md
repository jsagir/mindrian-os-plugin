---
phase: 213-eureka-reach-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/sensors/sensor-eureka.cjs
  - lib/core/eureka/eureka-reach-runner.cjs
  - scripts/auto-explore-fire.cjs
  - tests/test-213-sensor-eureka.cjs
  - tests/fixtures/213/last-eureka.json
autonomous: true
requirements: [EUREKA-01, EUREKA-07]
tags: [eureka, sensor, sens-13, guard-gate]

must_haves:
  truths:
    - "SENS-13 fires the FROZEN deep_research reach and no other; no 7th reach id is minted (CIRS R3, REACH_IDS frozen six)"
    - "No guard verdict = no fire, NEVER no guard = unguarded fire (the honest degrade); guard availability is PROBED on the live module, never inferred from a file existing on disk (Pattern 3)"
    - "The side-channel <roomDir>/.mindrian/last-eureka.json carries a CLOSED key schema of enums, opaque node handles, and quantized scalars only - zero user prose (Canon Part 8)"
    - "A stale side-channel never re-fires the sensor (30-minute freshness window, the SIGNAL_FRESHNESS_MS idiom incl. the WR-01 future-mtime guard)"
    - "The runner is born WIRED: invoked from the shipped auto-explore fire path in this same plan, not left as an orphan module"
  artifacts:
    - path: "lib/core/sensors/sensor-eureka.cjs"
      provides: "SENS-13 detector (pure, side-channel read, deep_research reach)"
      exports: ["sensorEureka"]
      min_lines: 60
    - path: "lib/core/eureka/eureka-reach-runner.cjs"
      provides: "Async side-channel producer: bridge scan + Stage-A guard gate + closed-schema writer"
      exports: ["runEurekaScan", "SIDE_CHANNEL_FILE", "SIDE_CHANNEL_SCHEMA_VERSION", "probeGuard"]
      min_lines: 100
  key_links:
    - from: "lib/core/eureka/eureka-reach-runner.cjs"
      to: "lib/core/eureka-critic.cjs"
      via: "probe-based guard availability + Stage-A verdict (consume the 212 contract, never rebuild)"
      pattern: "eureka-critic"
    - from: "lib/core/eureka/eureka-reach-runner.cjs"
      to: "lib/core/rs-differential-scorer.cjs"
      via: "scoreMeasured for the measured bridge differential"
      pattern: "scoreMeasured"
    - from: "lib/core/sensors/sensor-eureka.cjs"
      to: ".mindrian/last-eureka.json"
      via: "fresh side-channel read (the sensorArtifactFiled idiom)"
      pattern: "last-eureka"
    - from: "scripts/auto-explore-fire.cjs"
      to: "lib/core/eureka/eureka-reach-runner.cjs"
      via: "opt-in fire-and-forget invocation after the Step-5 finding write"
      pattern: "eureka-reach-runner"
---

<objective>
Build the KEY's detector half: the SENS-13 eureka sensor (riding the FROZEN deep_research reach) plus its side-channel producer (the runner that scans the 211 substrate for a guard-cleared bridge and writes the enum/handle-only side-channel), wired into the shipped auto-explore fire path so the producer is born invoked. Registration into SENSOR_REGISTRY and the decide() reachability proof are plan 03 (which owns lib/core/insight-sensors.cjs).

Purpose: SEED-049 THE KEY - "a command-triggered eureka engine is dead on arrival; students do not type commands." The sensor is the key. This plan produces the candidate reach; decide() remains the ONE selection brain and the navigator remains the only trigger.

Output: sensor-eureka.cjs, eureka-reach-runner.cjs, the auto-explore opt-in seam, unit tests + fixture.

**DECISION-GATE ITEM, ENCODED WITH RECOMMENDATION AND FLAGGED (do not silently re-decide):** SENS-13 fires POST-GuardGate. Encoded per the researched recommendation (RESEARCH Open Question 2): the guard verdict is a FIRING PRECONDITION - a restatement never becomes an offer; guard availability is a probe on the live eureka-critic module; the degrade is no-guard -> no-fire, never no-guard -> unguarded-fire. Refinement made explicit here for navigator sign-off: the RUNTIME gate is the 212 critic's Stage A (deterministic gates + criticRule confidence), because Stage B's two rubric passes are session-run and eval-side; a candidate Stage A cannot clear does NOT fire. If the navigator wants Stage B in the runtime path instead, that is a scope change - flag at review, do not improvise.

Verified at plan time against live code (per the ROADMAP:2933 instruction): lib/core/insight-sensors.cjs enumerates SENS-01..SENS-12 (SENS-11 = expert-skill :111, SENS-12 = room-pick :120); lib/core/sensors/ has no SENS-13. SENS-13 is free. REACH_IDS is the frozen six (sensor-types.cjs:43-50); deep_research is member 5 and maps to 'Spawn Sub-Agent' -> 'subagent-dispatcher' (navigation-engine.cjs:429,:660).

**EXECUTION PRECONDITIONS (same two as plan 01 - verify, then proceed):**
(a) curing-sequence debug track verdict recorded (status no longer `gathering`); (b) 212-05 >=0.85 human-verify checkpoint passed. The runner's guard probe additionally requires lib/core/eureka-critic.cjs to exist ON DISK from executed 212 - the probe (not disk existence) decides liveness, and all tests here are hermetic via injection seams so the suite is green even where the critic is absent.

Acceptance criteria (objective, grep-able / exit codes):
- `node tests/test-213-sensor-eureka.cjs` exits 0
- `grep -c "deep_research" lib/core/sensors/sensor-eureka.cjs` >= 1 AND `grep -E "reach_id: *'(context_block|contradiction|cross_room|brain_consult|hats)'" lib/core/sensors/sensor-eureka.cjs` exits 1 (fires deep_research only)
- `grep -c "SENS-13" lib/core/sensors/sensor-eureka.cjs` >= 1
- `grep -cE "require\(.*(decide|navigation-engine|chain-executor)" lib/core/sensors/sensor-eureka.cjs` returns 0 (Phase 144 fence: sensors produce, never route or execute)
- `grep -c "eureka-reach-runner" scripts/auto-explore-fire.cjs` >= 1 (born wired)
- `node -e "const r=require('./lib/core/eureka/eureka-reach-runner.cjs'); r.probeGuard({criticProbeFn:()=>null}).then(g=>{if(g.available!==false)throw new Error('degrade');console.log('no-guard-no-fire ok')})"` prints ok
- `grep -cP '\x{2014}'` returns 0 for every file touched (no em-dashes)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-RESEARCH.md
@lib/core/sensors/sensor-types.cjs
@lib/core/sensors/sensor-lagging-component.cjs (the SENS-02 substrate SEED-049 names as the reuse base)
@lib/core/insight-sensors.cjs (sensorArtifactFiled side-channel idiom :539-575; SIGNAL_FRESHNESS_MS + isFreshFile :180-199 - REPLICATE these two small helpers locally in sensor-eureka.cjs, do NOT import insight-sensors.cjs from a sensors/ file, that would be circular)
@lib/core/rs-differential-scorer.cjs (scoreMeasured :483, bandFor :465, EUREKA_DIFF_FLOOR :450-462)
@.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-01-PLAN.md and 212-02-PLAN.md (the critic contract this plan CONSUMES: loadCriticTags, assembleCriticPayload, classifyCandidate, criticRule, VERDICTS)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/core/sensors/sensor-eureka.cjs - the SENS-13 detector</name>
  <files>lib/core/sensors/sensor-eureka.cjs, tests/fixtures/213/last-eureka.json</files>
  <read_first>
    - lib/core/insight-sensors.cjs :539-575 (sensorArtifactFiled: the roomDir side-channel read idiom) and :180-199 (isFreshFile incl. WR-01 non-negative-age guard)
    - lib/core/sensors/sensor-room-pick.cjs (most recent sensor file, current house header style)
    - lib/core/sensors/sensor-types.cjs (makeReach contract; evidence = flat scalar bag)
  </read_first>
  <behavior>
    - Test (in task 3's file): fresh valid side-channel with guard.verdict 'transferable' -> reach { reach_id: 'deep_research', posture: 'hold', signal: 'eureka_bridge' }
    - guard.verdict 'restatement' / 'pseudoscience' / 'general_shallow' -> null (a restatement never becomes an offer)
    - guard.available false or guard key absent -> null (no guard = no fire)
    - stale file (mtime older than 30 min) or future-dated mtime -> null
    - band 'low' or 'moderate' -> null (fire only on band in {opportunity, high, breakthrough} - the strong-gating leg of the Pitfall-2 rank-starvation mitigation)
    - malformed JSON / missing roomDir / schema_version !== 1 -> null, never throws
  </behavior>
  <action>
    Create lib/core/sensors/sensor-eureka.cjs exporting sensorEureka(turn, tuple, ctx) -> Readonly reach | null. Pure, sync, LOCAL-first, soft-fail-to-null. Read <ctx.roomDir>/.mindrian/last-eureka.json via local readJsonSafe + isFreshFile helpers (30-minute window const EUREKA_SIGNAL_FRESHNESS_MS = 30*60*1000, replicated with the WR-01 future-mtime guard; do not import insight-sensors.cjs - circular). Firing preconditions, ALL required: payload.schema_version === 1; payload.guard && payload.guard.available === true && payload.guard.verdict === 'transferable' (the flagged POST-GuardGate lock); payload.bridge.band is one of ['opportunity','high','breakthrough']; payload.bridge.surprise_type is one of ['structural_transfer','semantic_implementation']. Return makeReach({ reach_id: 'deep_research', posture: 'hold', dispatch: 'eureka-bridge-offer (211 tri-modal substrate)', companions: [], signal: 'eureka_bridge', evidence: { band, surprise_type, guard_verdict, guard_confidence, a_handle, b_handle, differential: payload.bridge.differential_quantized } }). Evidence values are enums/opaque node-id handles/quantized scalars ONLY - the header documents that a_handle/b_handle are room.db node ids (opaque), NEVER titles or artifact text, and that this evidence stays on the LOCAL decision trace (Part 8). Header comments: SENS-13 id (SENS-11/12 taken, verified 2026-07-06); rides the FROZEN deep_research reach, no 7th reach (CIRS R3); posture 'hold' = a standing suggestion at the Decision Gate, never auto-opens (the SENS-SHOW precedent; the Brain RECOMMENDS, never TRIGGERS); Phase 144 fence (produces a candidate reach, never assigns routing_source, never requires decide()/chain-executor); the routing fence + Part-8 sweep tests already span every lib/core/sensors/*.cjs so this file is auto-covered. Also create tests/fixtures/213/last-eureka.json - a valid schema_version-1 fixture payload (guard transferable, band breakthrough, handles 'n042'/'n317') used by task 3 and plan 03.
  </action>
  <verify>
    <automated>node -e "const {sensorEureka}=require('./lib/core/sensors/sensor-eureka.cjs'); const r=sensorEureka({},{},{}); if(r!==null) throw new Error('must soft-fail null'); console.log('loads + null-degrades ok')"</automated>
  </verify>
  <done>Module loads standalone, degrades to null on every malformed input, fires deep_research/hold only under the full precondition set; fixture exists.</done>
</task>

<task type="auto">
  <name>Task 2: lib/core/eureka/eureka-reach-runner.cjs - side-channel producer with the Stage-A guard gate</name>
  <files>lib/core/eureka/eureka-reach-runner.cjs</files>
  <read_first>
    - lib/core/rs-differential-scorer.cjs :409-560 (scoreMeasured contract: opts.encodeFn/vectors/lexicalFn injection seams, bandFor, provenance stamping, Part 8 dual-layer audit ALREADY inside - do not re-audit inputs)
    - lib/core/eureka/hybrid-retrieve.cjs and tri-modal-index.cjs exports (the 211 candidate-pair substrate)
    - .planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-02-PLAN.md Task 2 (criticRule payload contract + confidence bands)
  </read_first>
  <action>
    Create lib/core/eureka/eureka-reach-runner.cjs (async, CJS). Export:
    - probeGuard({ criticProbeFn? }) -> Promise<{ available, reason }>. Default probe: lazy require('../eureka-critic.cjs') inside a try/catch, then assert typeof criticRule === 'function' AND loadCriticTags().schema_version === 1. PROBE THE LIVE MODULE, never fs.existsSync (Pattern 3, the vec0 lesson). criticProbeFn is the hermetic test seam.
    - runEurekaScan({ roomDir, pair?, encodeFn?, vectors?, criticProbeFn?, scoreFn?, now? }) -> Promise<{ fired, reason, sideChannelPath? }>. Steps: (1) probeGuard; unavailable -> { fired:false, reason:'guard_unavailable' } and write NOTHING (no guard = no fire, the flagged lock). (2) Resolve the candidate pair: opts.pair (two { handle, text } objects, the test seam) or derive the top cross-community pair from the 211 substrate (hybrid-retrieve over roomDir's room.db; if the substrate/db is absent -> { fired:false, reason:'substrate_unavailable' }). (3) scoreMeasured(a.text, b.text, { encodeFn, vectors }) - it self-audits Part 8 and self-degrades; abs差 below resolveEurekaDiffFloor() or band in {low, moderate} -> { fired:false, reason:'below_floor' }. (4) Stage-A guard: build the critic feature payload via the 212 contract (assembleCriticPayload with quantized differential/semantic/lexical + surprise_type + generic domain tags; domain tags default 'unknown' when the caller has no classification) then classifyCandidate + criticRule. Verdict not 'transferable' OR criticRule confidence 'unknown' -> { fired:false, reason:'guard_not_cleared' }. (5) Atomic-write <roomDir>/.mindrian/last-eureka.json with the CLOSED schema: { schema_version: 1, scanned_at (ISO date), guard: { available: true, verdict, confidence, tags (string array from the closed reasoning_tags enum) }, bridge: { a_handle, b_handle, surprise_type, band, differential_quantized (2dp) }, provenance: { model, method } }. The writer REJECTS (returns fired:false, reason:'schema_violation') any extra key and any value that is not an enum member / opaque handle / quantized number - the closed-key schema IS the Part-8 fence for this file; handles are node ids, never text. Constants: SIDE_CHANNEL_FILE = 'last-eureka.json', SIDE_CHANNEL_SCHEMA_VERSION = 1. Honor EUREKA_DIFF_FLOOR (existing env var) via resolveEurekaDiffFloor - no new env vars. Never throws; every failure is a structured { fired:false, reason } (closed reason enum: guard_unavailable, substrate_unavailable, below_floor, guard_not_cleared, schema_violation, room_dir_missing).
  </action>
  <verify>
    <automated>node -e "const r=require('./lib/core/eureka/eureka-reach-runner.cjs'); if(r.SIDE_CHANNEL_FILE!=='last-eureka.json'||r.SIDE_CHANNEL_SCHEMA_VERSION!==1) throw new Error('consts'); r.runEurekaScan({roomDir:'/nonexistent-213',criticProbeFn:()=>null}).then(o=>{if(o.fired!==false) throw new Error('must not fire'); console.log('runner degrade ok: '+o.reason)})"</automated>
  </verify>
  <done>Runner exists with probe-based guard, closed-schema writer, closed failure-reason enum, zero new env vars, and consumes (never rebuilds) scoreMeasured + the 212 critic contract.</done>
</task>

<task type="auto">
  <name>Task 3: born-wired invocation seam + the SENS-13 unit suite</name>
  <files>scripts/auto-explore-fire.cjs, tests/test-213-sensor-eureka.cjs</files>
  <read_first>
    - scripts/auto-explore-fire.cjs (full file, 307 lines - the Step-5 finding write + exit discipline: every path exits 0, uncaughtException backstop)
    - tests/test-212-critic-stage-a.cjs if present, else tests/test-211-case-cards.cjs (house bare-assert test style)
  </read_first>
  <action>
    (1) Wire the runner into scripts/auto-explore-fire.cjs as an ADDITIVE, OPT-IN, fire-and-forget step AFTER the Step-5 finding write succeeds (the ui4 seam style: default behavior byte-identical when the eureka substrate is absent): lazy-require lib/core/eureka/eureka-reach-runner.cjs inside try/catch, call runEurekaScan({ roomDir }) with .catch(()=>{}) swallow - the auto-explore exit-0 discipline must hold on every path; a runner fault can never change auto-explore's exit code or its own finding write. Comment the seam: "Phase 213 SENS-13 producer - material filed -> bridge scan -> side-channel; the sensor (not this script) decides firing; decide() (not the sensor) decides surfacing; the navigator (not decide()) decides acting."
    (2) Write tests/test-213-sensor-eureka.cjs covering the Task-1 behavior list end to end using a temp room dir + the tests/fixtures/213/last-eureka.json fixture (copy in, touch mtime for fresh/stale/future arms), plus two runner arms: probeGuard degrade (criticProbeFn -> null => available:false) and the closed-schema writer arm via runEurekaScan with injected pair + criticProbeFn + scoreFn/encodeFn stubs asserting (a) the written file has EXACTLY the closed key set (deep key enumeration, sorted-keys compare), (b) guard_not_cleared writes nothing, (c) reason enum membership. Bare node assert, hermetic (no network, no model download, no live critic needed).
  </action>
  <verify>
    <automated>node tests/test-213-sensor-eureka.cjs</automated>
  </verify>
  <done>Suite green hermetically; auto-explore-fire byte-identical in behavior when substrate absent (its own existing flow untouched); seam comment present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room.db content -> side-channel | user graph content is read by the runner; ONLY enums/handles/quantized scalars may cross into last-eureka.json |
| side-channel -> sensor evidence -> decision trace | LOCAL only; nothing here egresses |
| runner -> eureka-critic payload | assembleCriticPayload + auditQueryObject (212 D1) already fence this leg |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-04 | Information Disclosure | last-eureka.json writer | mitigate | closed-key schema writer rejects unknown keys/non-enum values; test enumerates exact key set |
| T-213-05 | Elevation of Privilege (machine over navigator) | sensor firing path | mitigate | posture 'hold'; sensor produces a candidate reach only; Phase 144 fence grep gate; no-force suite in plan 03 |
| T-213-06 | Spoofing (stale/planted side-channel) | sensor freshness gate | mitigate | 30-min window + WR-01 future-mtime guard + schema_version check |
| T-213-07 | DoS (runner cost in a hook path) | auto-explore seam | mitigate | fire-and-forget with catch-swallow; auto-explore exit-0 discipline unchanged |
</threat_model>

## Artifacts this phase produces (plan 02 contribution)

| Artifact | Kind | Names/symbols |
|----------|------|---------------|
| lib/core/sensors/sensor-eureka.cjs | module | sensorEureka (SENS-13), EUREKA_SIGNAL_FRESHNESS_MS, signal kind 'eureka_bridge', dispatch handle 'eureka-bridge-offer (211 tri-modal substrate)' |
| lib/core/eureka/eureka-reach-runner.cjs | module | runEurekaScan, probeGuard, SIDE_CHANNEL_FILE='last-eureka.json', SIDE_CHANNEL_SCHEMA_VERSION=1, reason enum {guard_unavailable, substrate_unavailable, below_floor, guard_not_cleared, schema_violation, room_dir_missing} |
| scripts/auto-explore-fire.cjs | edit | opt-in fire-and-forget seam after Step 5 |
| tests/test-213-sensor-eureka.cjs | test | sensor behaviors + runner degrade + closed-schema arms |
| tests/fixtures/213/last-eureka.json | fixture | valid schema_version-1 payload |
| New env vars | none | reuses EUREKA_DIFF_FLOOR |

<verification>
- `node tests/test-213-sensor-eureka.cjs` exit 0
- objective grep gates all pass
- `bash tests/run-all-117.sh` (if present) or the auto-explore test leg still green - the seam is additive
</verification>

<success_criteria>
SENS-13 exists, fires deep_research only, POST-GuardGate (flagged), stale-proof, Part-8-closed at the side-channel, and its producer is born invoked from the shipped auto-explore path - with plan 03 left exactly two edits (registry + reachability proof).
</success_criteria>

<output>
Create `.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-02-SUMMARY.md` when done.
</output>
