---
phase: 213-eureka-reach-wiring
plan: 03
type: execute
wave: 2
depends_on: [213-02]
files_modified:
  - lib/core/insight-sensors.cjs
  - tests/test-213-reach-wired.cjs
  - tests/test-213-no-force.cjs
autonomous: true
requirements: [EUREKA-02, EUREKA-03]
tags: [eureka, born-wired, decide, reachability]

must_haves:
  truths:
    - "The eureka reach is REACHABLE through the REAL decide() at runtime: a fresh guard-cleared side-channel makes decide() return fire_skill 'subagent-dispatcher' via deep_research -> 'Spawn Sub-Agent' (the born-wired proof, THE phase-defining acceptance criterion per RESEARCH V2)"
    - "The test FAILS if the reach is unreachable (registered-but-never-surfaces is the M4 placeholder disease; this is the countermeasure)"
    - "decide() itself is BYTE-UNCHANGED: the wiring is one SENSOR_REGISTRY entry + exports; no engine edit, no second selection brain"
    - "Recommend-never-trigger holds structurally: no path from a fired sensor to runChain without the navigator; sensors never require decide()/chain-executor (Phase 144 fence)"
    - "Co-fire behavior is PINNED empirically: a same-turn context_block sensor outranks the eureka reach (canonical order), and on a quiet turn the eureka reach surfaces (Pitfall 2 verified, not assumed)"
  artifacts:
    - path: "lib/core/insight-sensors.cjs"
      provides: "SENS-13 registered in SENSOR_REGISTRY canonical order + sensorEureka export"
      contains: "sensorEureka"
    - path: "tests/test-213-reach-wired.cjs"
      provides: "The born-wired runtime reachability proof (Phase 185 predicate pattern, applied at feature time)"
      min_lines: 80
    - path: "tests/test-213-no-force.cjs"
      provides: "Adversarial recommend-never-trigger invariant suite"
      min_lines: 50
  key_links:
    - from: "lib/core/insight-sensors.cjs"
      to: "lib/core/sensors/sensor-eureka.cjs"
      via: "require + SENSOR_REGISTRY entry"
      pattern: "sensor-eureka"
    - from: "tests/test-213-reach-wired.cjs"
      to: "lib/core/navigation-engine.cjs"
      via: "calls the REAL decide() (no parallel re-implementation - the Phase 185 lesson)"
      pattern: "decide\\("
---

<objective>
Register SENS-13 into the sensor spine and prove, with a test that fails otherwise, that the eureka reach is reachable through decide() at runtime. This is the room's #1-leverage structural change (04-synthesis Step 2): Phase 213 is the FIRST born-wired-at-feature-time proof - the sensor is wired into dispatchSensors -> decide() in the same phase that builds it, verified by a runtime test, not left as a registered-but-unreachable orphan.

Purpose: 01-cli-canon-audit 3d used the empty 213 directory as exhibit-A of "engine first, spine-wiring never." This plan is the refutation, and the precedent for making spine-routing a build-time gate after one more proof phase.

Output: SENSOR_REGISTRY registration, the reach-wired proof suite, the no-force adversarial suite.

**EXECUTION PRECONDITIONS:** same two phase gates as plan 01 ((a) curing-track verdict, (b) 212-05 >=0.85 checkpoint) plus 213-02 SUMMARY exists.

Acceptance criteria (objective, grep-able / exit codes):
- `node tests/test-213-reach-wired.cjs` exits 0
- `node tests/test-213-no-force.cjs` exits 0
- `grep -c "sensorEureka" lib/core/insight-sensors.cjs` >= 3 (require, registry entry, export)
- `grep -c "SENS-13" lib/core/insight-sensors.cjs` >= 1 (header roster updated)
- `node -e "const s=require('./lib/core/insight-sensors.cjs'); if(s.SENSOR_REGISTRY.length!==15) throw new Error('expected 15 sensors, got '+s.SENSOR_REGISTRY.length); if(typeof s.sensorEureka!=='function') throw new Error('export'); console.log('registry ok')"` prints ok (14 current entries + SENS-13)
- Existing spine suites stay green: `node tests/test-148-engine-reaches.cjs` exits 0 (frozen-six unchanged)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-RESEARCH.md (V2, Pitfall 2, Pattern 2)
@lib/core/insight-sensors.cjs (registry :582-606, exports :668-719, header roster :100-128)
@lib/core/navigation-engine.cjs (decide() :817, sensor consumption :918-928, resolveFireSkill :588-612, verbToSkillFamily :654-670, TURN_STAGE suppression via insight-sensors :362-408)
@lib/core/drift-runtime-reachability.cjs (the Phase 185 predicate PATTERN: assert through the real read path, name precise failure reasons, never a parallel re-implementation)
@.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-02-SUMMARY.md
@tests/fixtures/213/last-eureka.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register SENS-13 in the sensor spine (the one-edit wiring)</name>
  <files>lib/core/insight-sensors.cjs</files>
  <read_first>
    - lib/core/insight-sensors.cjs :120-128 (SENS-12 require block - the newest registration idiom to mirror), :582-606 (SENSOR_REGISTRY), :668-719 (module.exports)
  </read_first>
  <action>
    Three additive edits to lib/core/insight-sensors.cjs, nothing else: (1) a require block for { sensorEureka } from './sensors/sensor-eureka.cjs' with the house comment style: "Phase 213 detector - SENS-13 eureka. Fires the EXISTING deep_research reach (no 7th reach minted, CIRS R3) when the LOCAL side-channel <roomDir>/.mindrian/last-eureka.json carries a FRESH, guard-cleared (POST-GuardGate: verdict transferable, no guard = no fire) bridge from the 211 tri-modal substrate. posture 'hold' - a standing suggestion at the Decision Gate; the Brain RECOMMENDS, never TRIGGERS (Phase 210 doctrine). Lives under lib/core/sensors/ so the routing fence + Part-8 sweep span it." (2) append sensorEureka LAST in SENSOR_REGISTRY (canonical order: after sensorRoomPick) with the entry comment "Phase 213 detector (SENS-13 - eureka bridge -> deep_research offer)". (3) add sensorEureka to module.exports with the matching comment. Update the header roster comment enumeration to include SENS-13. Zero changes to dispatchSensors, normalizeTurn, eligibility gating, or any other body code - decide() and the dispatch chokepoint stay byte-identical (the frozen-six and turn-stage gates already handle deep_research).
  </action>
  <verify>
    <automated>node -e "const s=require('./lib/core/insight-sensors.cjs'); if(s.SENSOR_REGISTRY.indexOf(s.sensorEureka)!==s.SENSOR_REGISTRY.length-1) throw new Error('SENS-13 must be last (canonical order)'); console.log('registered ok')"</automated>
  </verify>
  <done>SENS-13 registered last, exported, roster comment updated; no other line of insight-sensors.cjs behavior changed; test-148-engine-reaches + the sensors Part-8 sweep still green.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test-213-reach-wired.cjs - the born-wired runtime reachability proof</name>
  <files>tests/test-213-reach-wired.cjs</files>
  <read_first>
    - lib/core/navigation-engine.cjs :817-930 (decide() entry: sensorTuple/sensorCtx assembly - note sensorCtx.roomDir comes from context.roomDir) and :588-612 + :654-670 (deep_research -> 'Spawn Sub-Agent' -> 'subagent-dispatcher')
    - lib/core/insight-sensors.cjs :362-408 (TURN_STAGE_GATED_REACHES: deep_research suppressed turns 1-2, unlocked at turn_count >= 3)
    - lib/core/drift-runtime-reachability.cjs header (the predicate philosophy: "reachable here means reachable by the real decide() read")
  </read_first>
  <action>
    Write the proof suite calling the REAL decide() (require lib/core/navigation-engine.cjs; no mocks of the engine, no parallel ranker). Arms, each with a precise failure message naming the broken link:
    1. REACHABLE (the phase gate): temp room dir; copy tests/fixtures/213/last-eureka.json into <dir>/.mindrian/ with a fresh mtime; call decide({ text: 'quiet turn', turn_count: 5 }, { roomDir: dir }); assert decision.fire_skill === 'subagent-dispatcher' AND the decision trace's sensor reaches include reach_id 'deep_research' with signal 'eureka_bridge'. This arm FAILING means the eureka reach is unreachable through decide() - the exact drift class Phase 185 doctor --drift catches for commands/agents, asserted here at feature time for the sensor plane.
    2. TURN-STAGE HONEST: same fixture, turn_count 1 -> deep_research absent from the emitted reaches (suppressed turns 1-2); turn_count 3 -> present. Pins that the born-wired path respects the shipped BCH-S5 gate rather than bypassing it.
    3. CO-FIRE PINNED (Pitfall 2, empirical): same fixture PLUS a turn that fires a context_block sensor (e.g. text 'X is the bottleneck' firing SENS-02); assert sensorReaches[0].reach_id === 'context_block' (canonical order outranks) AND 'deep_research' still present in the full reach list. Add a comment block recording the verdict for the SUMMARY: the eureka offer yields to same-turn context sensors and surfaces on quiet turns; if this arm shows the reach NEVER surfaces in realistic mixes, the fix belongs in SENS-13's firing condition, never in reordering the frozen REACH_IDS or bypassing decide() (RESEARCH Pitfall 2 doctrine).
    4. STALE/UNGUARDED NEGATIVE: stale mtime -> fire_skill falls through to the non-eureka path; fixture with guard.verdict 'restatement' -> same. The negative arms prove the reachability arm is load-bearing, not vacuously green.
  </action>
  <verify>
    <automated>node tests/test-213-reach-wired.cjs</automated>
  </verify>
  <done>All 4 arms green against the real decide(); arm-1 failure message names the born-wired breach explicitly; co-fire verdict recorded in comments.</done>
</task>

<task type="auto">
  <name>Task 3: tests/test-213-no-force.cjs - the recommend-never-trigger adversarial suite</name>
  <files>tests/test-213-no-force.cjs</files>
  <read_first>
    - lab/apo/apo-loop.cjs :46-68 (Phase 210-C signal-not-veto comments - the doctrine wording the assertions encode)
    - lib/core/chain-executor.cjs header (runChain is invoked by act/pipeline/ignite wrappers AFTER a navigator verb, never by the sensor plane)
  </read_first>
  <action>
    Write the adversarial suite asserting the LOCKED constraint ("the Brain RECOMMENDS, never TRIGGERS") structurally and behaviorally:
    1. STRUCTURAL FENCES (source scans, the house grep-gate style with comment lines filtered via grep -v on comment prefixes): lib/core/sensors/sensor-eureka.cjs and lib/core/eureka/eureka-reach-runner.cjs contain zero require() of chain-executor, command-resolver, or navigation-engine (producers never route/execute); lib/core/eureka/eureka-offer.cjs (once plan 04 lands - guard the arm with fs.existsSync and SKIP-log when absent so wave order cannot break this suite) contains zero require() of chain-executor.
    2. BEHAVIORAL: decide() with the fresh eureka fixture returns a DECISION OBJECT and provably invokes no chain: monkey-patch require.cache entry for chain-executor.cjs with a runChain that throws 'FORCED', call decide(), assert no throw and fire_skill set - the fired reach surfaces an offer, it executes nothing.
    3. VOCABULARY FENCE (the plan-language lock made mechanical): scan lib/core/sensors/sensor-eureka.cjs + lib/core/eureka/eureka-reach-runner.cjs sources for the forcing vocabulary /\b(auto[-_]?fire|must[-_]?answer|disqualif|force[-_]?fire)\b/i in non-comment lines; zero hits. (The words may appear in comments quoting the anti-constraint; filter comment lines first - the grep hygiene rule.)
    4. POSTURE PIN: sensorEureka's fired reach has posture 'hold' exactly (a regression here is the Stop-hook force-fire incident class, feedback_1_15_enforcement_regression_watch).
  </action>
  <verify>
    <automated>node tests/test-213-no-force.cjs</automated>
  </verify>
  <done>All 4 invariants green; the suite SKIP-logs (not fails) the plan-04 file arm when absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sensor plane -> decide() | candidate reaches only; routing_source never mutated by sensors (Phase 144 fence) |
| decide() -> navigator | offers only; the navigator verb is the sole trigger for runChain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-08 | Elevation of Privilege (machine over navigator) | fire path end to end | mitigate | no-force suite arms 1-4; posture 'hold' pinned |
| T-213-09 | Tampering (silent unreachability regression) | SENSOR_REGISTRY / decide() | mitigate | reach-wired arm 1 fails the suite on any future edit that starves the reach |
| T-213-10 | Repudiation (unverifiable wiring claim) | born-wired claim | mitigate | the claim IS a runtime test, not prose (Phase 185 discipline) |
</threat_model>

## Artifacts this phase produces (plan 03 contribution)

| Artifact | Kind | Names/symbols |
|----------|------|---------------|
| lib/core/insight-sensors.cjs | edit | sensorEureka require + registry entry (position 15, last) + export; header roster names SENS-13 |
| tests/test-213-reach-wired.cjs | test | 4 arms; the phase-defining born-wired proof |
| tests/test-213-no-force.cjs | test | 4 invariants incl. the vocabulary fence |
| New env vars | none | - |

<verification>
- `node tests/test-213-reach-wired.cjs` exit 0
- `node tests/test-213-no-force.cjs` exit 0
- `node tests/test-148-engine-reaches.cjs` exit 0 and the lib/core/sensors Part-8 sweep + routing fence suites green (they auto-span the new file)
- objective grep/registry gates pass
</verification>

<success_criteria>
The eureka reach is provably reachable through the real decide() at runtime, the proof fails closed on regression, decide() is byte-unchanged, and the recommend-never-trigger constraint is mechanically asserted - the first born-wired-at-feature-time proof is on disk.
</success_criteria>

<output>
Create `.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-03-SUMMARY.md` when done. In the SUMMARY, name the recommended SECOND born-wired proof phase (RESEARCH Open Question 3): Phase 214's find-analogies leg riding the same deep_research reach is the natural candidate; after it, mint the build-time gate (04-synthesis Step 2).
</output>
