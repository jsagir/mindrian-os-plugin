---
phase: 213-eureka-reach-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/eureka/compression-meter.cjs
  - lab/eureka-graders/arrival-grader.cjs
  - lab/eureka-graders/status-quo-judge.cjs
  - tests/test-213-compression-meter.cjs
  - tests/test-213-graders.cjs
autonomous: true
requirements: [EUREKA-04, EUREKA-05]
tags: [eureka, compression, graders, critic]

must_haves:
  truths:
    - "Score = CompressionDelta x GuardGate x StatusQuoGate is computed by deterministic CODE; no LLM sits anywhere in the formula path (SEED-050 THE METRIC)"
    - "A Lured arrival scores NEGATIVE, not zero; an arrival-without-compression input (the nichefoods-null shape) scores ~0"
    - "Arrival verdicts (Full/Partial/Missed/Lured) and status-quo labels (status_quo_stuck/redirect_ok) are computed BY CODE from item patterns; the LLM never picks the class (the 212 D2 verdict-by-code idiom)"
    - "The meter stays LOCAL: zero network, zero Brain, zero MCP imports"
  artifacts:
    - path: "lib/core/eureka/compression-meter.cjs"
      provides: "The deterministic COMPRESSION meter (runtime-side home)"
      exports: ["computeCompressionDelta", "guardGate", "statusQuoGate", "compressionScore", "LURED_PENALTY", "GUARD_ZERO_LABEL", "STATUS_QUO_ZERO_LABEL"]
      min_lines: 80
    - path: "lab/eureka-graders/arrival-grader.cjs"
      provides: "Arrival grader, verdict-by-code (eval-side home)"
      exports: ["ARRIVAL_VERDICTS", "gradeArrival"]
    - path: "lab/eureka-graders/status-quo-judge.cjs"
      provides: "Status-quo label judge, verdict-by-code (eval-side home)"
      exports: ["STATUS_QUO_LABELS", "judgeStatusQuo"]
  key_links:
    - from: "lib/core/eureka/compression-meter.cjs"
      to: "evals/eureka/cases/*.md frontmatter"
      via: "human_baseline_effort as the compression denominator"
      pattern: "human_baseline_effort"
    - from: "lab/eureka-graders/arrival-grader.cjs"
      to: "lib/core/eureka/compression-meter.cjs"
      via: "Lured verdict feeds the negative-score leg"
      pattern: "[Ll]ured"
    - from: "tests/test-213-compression-meter.cjs"
      to: "evals/eureka/README.md rubric"
      via: "regression fixture reproducing the hand-scoring rubric arithmetic"
      pattern: "CompressionDelta"
---

<objective>
Build the three graders Phase 212 explicitly DEFERRED to 213 (212-RESEARCH.md:62-63): the deterministic COMPRESSION meter, the Arrival grader (Full/Partial/Missed/Lured with Lured = NEGATIVE), and the status-quo label judge (status_quo_stuck vs redirect_ok). Per SEED-050: the meter is CODE, not an LLM judge, and stays local. The 211 gold set (evals/eureka/) is the calibration baseline.

Purpose: without the critic legs, the eureka reach in plans 02-05 has nothing honest to gate or tune against. This plan is the CRITIC half's local arithmetic (the two-in-a-box, generator + critic).

Output: compression-meter.cjs (runtime-side), two grader modules (eval-side), two test files.

**DECISION-GATE ITEM, ENCODED WITH RECOMMENDATION AND FLAGGED (do not silently re-decide):** grader/meter home. RESEARCH Open Question 1 recommendation is encoded here: the deterministic COMPRESSION meter lives RUNTIME-SIDE under lib/core/eureka/ (it is consumed by the plan-05 APO signal at runtime), the LLM-protocol graders live EVAL-SIDE under lab/eureka-graders/ (mirroring the 202 lab/apo precedent), and MCP-servable packaging of the judge family (SEED-050:102-105) is deferred to the consolidation arc's critic-MCP step. If the navigator wants a different home, say so at plan review; the executor must not relocate on its own.

Requirements note: ROADMAP.md:2942 says "Requirements: TBD" and no REQUIREMENTS.md exists. The phase-local requirement IDs EUREKA-01..EUREKA-07 are derived from the goal sentence + navigator locks; the derivation table is the Multi-Source Coverage Audit below.

**EXECUTION PRECONDITIONS (state, verify, then proceed - PLAN NOW, EXECUTE ONLY AFTER BOTH):**
(a) The curing-sequence debug track has a verdict: `.planning/debug/beta13-curing-sequence-persona-and-commands-bisect.md` frontmatter `status:` is no longer `gathering`.
(b) Phase 212 plan 05's blocking human-verify checkpoint passed: `evals/eureka/212-calibration-report.md` exists and the >=0.85 gold-card accuracy leg was human-approved (or honestly deferred WITH navigator approval recorded).
If either fails at execution start, STOP and report; do not partially execute.

Acceptance criteria (objective, grep-able / exit codes):
- `node tests/test-213-compression-meter.cjs` exits 0
- `node tests/test-213-graders.cjs` exits 0
- `grep -c "CompressionDelta x GuardGate x StatusQuoGate" lib/core/eureka/compression-meter.cjs` >= 1 (formula documented verbatim)
- `grep -riE "modelcontextprotocol|server\.tool|fetch\(|https?://" lib/core/eureka/compression-meter.cjs lab/eureka-graders/*.cjs` exits 1 (zero hits: local, no network, no MCP)
- `grep -cP '\x{2014}' lib/core/eureka/compression-meter.cjs lab/eureka-graders/arrival-grader.cjs lab/eureka-graders/status-quo-judge.cjs` returns 0 per file (no em-dashes)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 213 section, lines ~2931-2949)
@.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-RESEARCH.md
@.planning/seeds/SEED-050-eureka-eval-salient-verifier-judge-synthetic-trust.md
@evals/eureka/README.md
@evals/eureka/211-manual-baseline.md
@.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-02-PLAN.md (the verdict-by-code idiom to mirror)
</context>

## Multi-Source Coverage Audit (phase-wide, lives in plan 01)

| Source | Item | Covered by |
|--------|------|-----------|
| GOAL (ROADMAP:2933) | eureka-reach + LarryReacts wired into decide()/F.7/runChain | 213-02, 213-03, 213-04 (EUREKA-01/02/03) |
| GOAL | 190 touchpoint, post-210 softened (born-declared, advisory lint) | 213-06 (EUREKA-06) |
| GOAL | 202 touchpoint, post-210 softened (compression as APO reward SIGNAL) | 213-05 (EUREKA-06) |
| GOAL | 205 touchpoint, post-210 softened (lateralEngine socket + grill suggest-quality input) | 213-05 (EUREKA-06) |
| GOAL | Arrival grader, status-quo label, deterministic COMPRESSION meter | 213-01 (EUREKA-04/05) |
| GOAL | Gated by 212 Grounding Guard >=0.85 calibration | execution precondition (b) in every plan |
| REQ | "TBD" in ROADMAP; EUREKA-01..07 derived (this table is the derivation record) | all plans |
| RESEARCH V2 | born-wired reachability test through decide() (Phase 185 predicate pattern) | 213-03 (EUREKA-02) |
| RESEARCH V3 | softened wordings only; no plan verb "block/disqualify/force" | all plans (checked in 213-06) |
| RESEARCH V6 | ui4 additive opt-in seam style; Part 8 handles/enums only | 213-04, 213-05, 213-06 (EUREKA-07) |
| RESEARCH Pitfall 2 | deep_research rank starvation: pin co-fire behavior empirically | 213-03 Task 2 |
| RESEARCH Pitfall 4 | arrival-without-compression scores ~0; Lured negative | 213-01 |
| RESEARCH OQ2 / directive | SENS-13 fires POST-GuardGate (recommend + FLAG) | 213-02 (flagged) |
| RESEARCH OQ1 / directive | meter runtime-side, graders eval-side (recommend + FLAG) | 213-01 (flagged, above) |
| RESEARCH OQ3 | name the second born-wired proof candidate in the phase summary | 213-06 Task 3 |
| CONTEXT locks L1-L6 (RESEARCH user_constraints; no CONTEXT.md exists) | recommend-never-trigger; SENS-13 id; wire-into-not-rebuild; single command-truth source; 212 gate; curing-track gate | all plans; preconditions |
| Deferred (OUT) | 191-03/191-05 + visibility filter; SEED-052; Phase 214/215; Phase 212.5 whitespace | excluded everywhere |

No MISSING items. Excluded items are scoped to other phases by the ROADMAP itself.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/core/eureka/compression-meter.cjs - the deterministic COMPRESSION meter</name>
  <files>lib/core/eureka/compression-meter.cjs, tests/test-213-compression-meter.cjs</files>
  <read_first>
    - evals/eureka/README.md (the frozen formula + rubric, lines 41-108)
    - evals/eureka/211-manual-baseline.md (expected shapes; the simulated table is LOW-TRUST fixture input only, never gold)
    - evals/eureka/cases/nichefoods-null.md and cases/archimedes-uq.md (frontmatter schema)
    - lib/core/eureka/lexical-overlap.cjs (house style for a small pure lib/core/eureka module)
  </read_first>
  <behavior>
    - Test 1: compressionScore({ compressionDelta: 0.85, guardEvents: [], statusQuoEvents: [], arrival: 'Full' }) === 0.85
    - Test 2: any guardEvents entry === 'pseudoscience' -> guardGate 0 -> score 0
    - Test 3: any statusQuoEvents entry === 'status_quo_stuck' -> statusQuoGate 0 -> score 0
    - Test 4: arrival 'Lured' -> score === LURED_PENALTY (a NEGATIVE constant, e.g. -1 x compressionDelta floor'd at -1), strictly < 0 even when gates are open
    - Test 5: nichefoods-null shape (arrival Full, compressionDelta 0) -> score 0 (arrival without compression = null result)
    - Test 6: computeCompressionDelta clamps to [0,1]; non-finite/absent inputs -> 0 (defensive, never throws)
    - Test 7: the module loads standalone with require() and has zero network/MCP imports (string scan)
  </behavior>
  <action>
    Create lib/core/eureka/compression-meter.cjs (CJS, 'use strict', BSL header, hyphens only). Pure, sync, zero-I/O module. Document the formula verbatim in the header: Score = CompressionDelta x GuardGate x StatusQuoGate (SEED-050:47, evals/eureka/README.md:50). Exports:
    - computeCompressionDelta(humanBaselineTurns, observedTurns) -> 0..1 clamped; when the caller has qualitative human_baseline_effort (strings like "~2 years"), the caller maps to a turns-equivalent scalar BEFORE calling; this function takes numbers only (keep the qualitative mapping OUT of the meter - it is a hand-scoring judgment per the README rubric, per D-locked "the meter is CODE").
    - guardGate(guardEvents) -> 0 when any event equals GUARD_ZERO_LABEL ('pseudoscience'), else 1. Labels are the 212 critic verdict enums (data/eureka-critic-tags.json verdicts); do NOT re-mint the enum here, name the string constants and note their source.
    - statusQuoGate(statusQuoEvents) -> 0 when any event equals STATUS_QUO_ZERO_LABEL ('status_quo_stuck'), else 1.
    - compressionScore({ compressionDelta, guardEvents, statusQuoEvents, arrival }) -> the composite. arrival 'Lured' returns a NEGATIVE score (LURED_PENALTY leg: -(max(compressionDelta, MIN_LURED_MAGNITUDE)) with MIN_LURED_MAGNITUDE 0.25 so a Lured run is never a soft zero); document that Lured-negative is SEED-050 verbatim ("Lured = negative, not zero").
    All inputs defensive (non-array -> [], non-finite -> 0); never throws. This module is the RUNTIME-SIDE home per the flagged Decision-Gate recommendation in the objective. Write tests/test-213-compression-meter.cjs FIRST (bare node assert, house pattern), watch it fail, then implement.
  </action>
  <verify>
    <automated>node tests/test-213-compression-meter.cjs</automated>
  </verify>
  <done>All 7 behaviors pass; formula string present in header; zero network/MCP import strings; no em-dashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lab/eureka-graders/ - Arrival grader + status-quo judge, verdict-by-code</name>
  <files>lab/eureka-graders/arrival-grader.cjs, lab/eureka-graders/status-quo-judge.cjs, tests/test-213-graders.cjs</files>
  <read_first>
    - .planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-02-PLAN.md Task 1 (the runRubric two-pass + verdictFromRubric-by-code idiom to MIRROR, not import - eureka-critic.cjs may not exist on disk yet at execution start of this wave-1 plan if 212 execution reordered; graders must load standalone)
    - lab/apo/voice-contract-gate.cjs (house style for a lab-side judge module)
    - SEED-050 sections "The judge design" (Judges 1 and 3) and "Mode-blindness" (the mode-conditioned skeptic rule)
  </read_first>
  <behavior>
    - Test 1: gradeArrival({ subClaimsReached: 3, subClaimsTotal: 3, luredDistractor: false }) -> { verdict: 'Full', credit: 1 }
    - Test 2: partial credit = subClaimsReached / subClaimsTotal (e.g. 1/3 -> verdict 'Partial', credit ~0.333)
    - Test 3: luredDistractor true -> verdict 'Lured' regardless of sub-claims (falling for a seeded distractor dominates)
    - Test 4: zero reached, no lure -> 'Missed', credit 0
    - Test 5: judgeStatusQuo({ innovationIntentSignalled: true, statusQuoRejected: true, turnRelitigatesStatusQuo: true }) -> 'status_quo_stuck'
    - Test 6: same but turnRelitigatesStatusQuo false -> 'redirect_ok'; and when innovationIntentSignalled false the label is 'redirect_ok' (no intent = no stuck verdict; the judge is mode-conditioned, SEED-050)
    - Test 7: ARRIVAL_VERDICTS is exactly ['Full','Partial','Missed','Lured'] and STATUS_QUO_LABELS exactly ['status_quo_stuck','redirect_ok'] (frozen closed enums)
  </behavior>
  <action>
    Create lab/eureka-graders/ with the two modules (CJS, pure, sync, zero-I/O, never throws). The design rule carried from 212 D2: the verdict enum is computed BY CODE from boolean/scalar item inputs; any LLM pass (the session judge that produces those booleans from a transcript) happens OUTSIDE these modules and its per-item outputs are the inputs here. Header comments state this split explicitly and cite SEED-050 Judge 1 (Arrival: Full/Partial with credit = sub-claims reached/total, Missed, Lured = negative downstream) and Judge 3 (status-quo: stuck only when intent was signalled AND the status quo explicitly rejected AND the turn re-defends it; otherwise redirect_ok). These are EVAL-SIDE homes per the flagged Decision-Gate recommendation; MCP-servable packaging deferred (SEED-050:102-105 noted in the header as a future lift, not built now). Wire the Lured link: arrival-grader exports nothing meter-specific, but its header names compression-meter.cjs as the consumer of the Lured verdict (negative leg). Calibration anchor: header cites evals/eureka/ as the gold baseline (211-04). Write tests/test-213-graders.cjs FIRST, watch it fail, implement both modules.
  </action>
  <verify>
    <automated>node tests/test-213-graders.cjs</automated>
  </verify>
  <done>All 7 behaviors pass; both modules load standalone; closed enums frozen; header documents the code-computes-verdict split and the eval-side home decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval transcript -> grader inputs | judged booleans/scalars cross; raw transcript text does NOT enter these modules |
| meter output -> APO signal (plan 05) | scalar score only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-01 | Information Disclosure | grader/meter modules | mitigate | pure scalar/enum inputs only; grep gate proves zero network/MCP imports |
| T-213-02 | Tampering (self-graded gold) | calibration baseline | mitigate | 211-manual-baseline simulated table used as ARITHMETIC fixture only, never as gold labels (test comments state this); Gate B stays human |
| T-213-03 | Repudiation (score provenance) | compressionScore | accept | provenance stamping is the runner's job (plan 02); the pure meter carries none by design |
</threat_model>

## Artifacts this phase produces (plan 01 contribution)

| Artifact | Kind | Names/symbols |
|----------|------|---------------|
| lib/core/eureka/compression-meter.cjs | module | computeCompressionDelta, guardGate, statusQuoGate, compressionScore, LURED_PENALTY (negative const leg), MIN_LURED_MAGNITUDE=0.25, GUARD_ZERO_LABEL='pseudoscience', STATUS_QUO_ZERO_LABEL='status_quo_stuck' |
| lab/eureka-graders/arrival-grader.cjs | module | ARRIVAL_VERDICTS=['Full','Partial','Missed','Lured'], gradeArrival |
| lab/eureka-graders/status-quo-judge.cjs | module | STATUS_QUO_LABELS=['status_quo_stuck','redirect_ok'], judgeStatusQuo |
| tests/test-213-compression-meter.cjs | test | 7 assertions |
| tests/test-213-graders.cjs | test | 7 assertions |
| New env vars | none | - |

<verification>
- `node tests/test-213-compression-meter.cjs` exit 0
- `node tests/test-213-graders.cjs` exit 0
- grep gates from the objective (formula string present; zero network/MCP; zero em-dashes)
</verification>

<success_criteria>
Both grader legs + the meter exist as deterministic code with frozen closed enums, Lured scores negative, arrival-without-compression scores ~0, and the flagged home decision (meter runtime-side, graders eval-side) is implemented as recommended and visibly flagged for the navigator.
</success_criteria>

<output>
Create `.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-01-SUMMARY.md` when done.
</output>
