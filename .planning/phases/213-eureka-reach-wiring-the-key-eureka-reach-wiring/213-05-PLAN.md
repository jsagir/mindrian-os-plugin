---
phase: 213-eureka-reach-wiring
plan: 05
type: execute
wave: 2
depends_on: [213-01]
files_modified:
  - lab/apo/apo-loop.cjs
  - lib/core/eureka/lateral-engine-adapter.cjs
  - lib/core/grill-engine.cjs
  - tests/test-213-touchpoints.cjs
autonomous: true
requirements: [EUREKA-06]
tags: [eureka, touchpoints, apo, fusion, grill, post-210]

must_haves:
  truths:
    - "All three touchpoints are wired in their POST-Phase-210 SOFTENED forms: 202 = SIGNAL joining the reward blend, 205 = SUGGEST-grade inputs through the pre-drilled sockets; no wording or code path re-introduces a veto, a mandatory card, or an auto-elevation (the LOCKED recommend-never-trigger constraint + the room's consolidation verdict)"
    - "apo-loop with no compression ctx is byte-identical in behavior: every existing 202/210 test stays green untouched (the ui4 opt-in seam property)"
    - "The compression term is bounded by a frozen weight and selection remains quality-lexicographic: selection sees EVERY candidate; a zero-compression candidate stays selectable (signal, never a veto - the 210-C structural guarantee extended, not modified)"
    - "The lateralEngine adapter satisfies the fusion-router socket contract ({ score } function) and runLateralPath routes to 'rs_sideways_engine' with it injected; without it, the shipped blocked_until_phase_200_rs degrade is byte-unchanged"
    - "grill-engine's BLOCKED_UNTIL_200 constant is NOT flipped in this phase (flagged below); the eureka input to the grill is an optional suggest-quality annotation only"
  artifacts:
    - path: "lib/core/eureka/lateral-engine-adapter.cjs"
      provides: "The RS discriminator adapter for the 205 ctx.lateralEngine socket"
      exports: ["makeLateralEngine"]
    - path: "lab/apo/apo-loop.cjs"
      provides: "Additive optional compression signal in the reward blend"
      contains: "COMPRESSION_SIGNAL_WEIGHT"
    - path: "lib/core/grill-engine.cjs"
      provides: "Optional eureka suggest-quality annotation (enum-only) on the grill result"
      contains: "eurekaSignal"
  key_links:
    - from: "lab/apo/apo-loop.cjs"
      to: "lib/core/eureka/compression-meter.cjs"
      via: "the meter score is the reward signal APO tunes fire-rate against (SEED-050)"
      pattern: "compression"
    - from: "lib/core/eureka/lateral-engine-adapter.cjs"
      to: "lib/core/rs-differential-scorer.cjs"
      via: "scoreMeasured backs the adapter's score function"
      pattern: "scoreMeasured"
    - from: "lib/core/fusion-router.cjs"
      to: "lib/core/eureka/lateral-engine-adapter.cjs"
      via: "ctx.lateralEngine injection (pre-drilled socket :283, NOT edited)"
      pattern: "lateralEngine"
---

<objective>
Wire the three 190/202/205 touchpoints in their post-210 softened semantics (RESEARCH V3 resolution table): the COMPRESSION score joins the APO reward blend as a bounded SIGNAL (202); the eureka substrate becomes injectable through the pre-drilled ctx.lateralEngine socket and an optional enum-only suggest-quality annotation on the grill (205). The 190 touchpoint (born-declared, advisory lint green) is asserted in plan 06's gate since it is a verification property, not a code edit here.

The goal sentence's vocabulary ("build-gate", "disqualifier", "elevation tree") names PRE-210 binding forms; Phase 210 softened all three and the room classified the binding forms as enforcement overshoot. Every task below is additive, opt-in, and suggest/signal-grade. No task in this plan may use the verbs the LOCKED constraint forbids, in code, comments, or test names.

**FLAGGED FORK (encoded with recommendation - navigator confirms, executor does not decide):** grill-engine's BLOCKED_UNTIL_200 (:210) is a NAVIGATOR DEFERRAL ("the navigator DEFERRED live-wiring Arm B to a separate gated plan", grill-engine.cjs:207). Recommendation encoded here: do NOT flip it in 213 - un-deferring a navigator lock inside a wiring phase would repeat the over-reach class 210 reverted. The eureka contribution to the grill is instead an optional, enum-only suggest-quality input (ctx.eurekaSignal) that rides the EXISTING result shape, live regardless of the Arm-B seam, and Arm-B-compatible whenever the navigator separately approves the flip. If the navigator wants the flip inside 213, that is a scope addition to raise at review.

**EXECUTION PRECONDITIONS:** same two phase gates ((a) curing-track verdict - NOTE this plan touches the exact 202/205 mechanisms the curing bisect is testing, so the verdict gate is load-bearing here, re-verify the seams against the verdict before editing; (b) 212-05 checkpoint) plus 213-01 SUMMARY exists (the meter is the signal source).

Acceptance criteria (objective, grep-able / exit codes):
- `node tests/test-213-touchpoints.cjs` exits 0
- Existing APO suite green after the edit: the 202/210 apo test files (locate via `ls tests/ | grep -iE "202|apo"`) all exit 0 unmodified
- `grep -c "COMPRESSION_SIGNAL_WEIGHT" lab/apo/apo-loop.cjs` >= 2 (const + use)
- `grep -cE "\b(disqualif|veto|block(ed)?_candidate|must_elevate|force)\b" lib/core/eureka/lateral-engine-adapter.cjs tests/test-213-touchpoints.cjs` returns 0 per file on non-comment lines (softened vocabulary only)
- `grep -c "BLOCKED_UNTIL_200 = true" lib/core/grill-engine.cjs` returns 1 (the deferral is untouched)
- `grep -cP '\x{2014}'` returns 0 for every touched file
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-RESEARCH.md (V3 resolution table, V8 thin-seam inference, State-of-the-Art table)
@lab/apo/apo-loop.cjs (full file - TELEMETRY_WEIGHT/VOICE_SIGNAL_WEIGHT idiom :58-68, scoreCandidate :139-146, selectBest :155-165)
@lib/core/fusion-router.cjs (:270-300 runFusion ctx contract incl. lateralEngine :283; :408-458 runLateralPath socket contract - READ ONLY, this plan does not edit fusion-router)
@lib/core/grill-engine.cjs (:180-300 - the result shape with egress.guard_verdict :196, the BLOCKED_UNTIL_200 seam :202-230)
@lib/core/rs-differential-scorer.cjs (:473-520 scoreMeasured)
@.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 202 touchpoint - compression joins the APO reward blend as a bounded signal</name>
  <files>lab/apo/apo-loop.cjs</files>
  <read_first>
    - lab/apo/apo-loop.cjs :58-68 (the two existing weight consts + their cap-discipline comments - the pattern to extend), :127-146 (scoreCandidate), :155-165 (selectBest quality-lexicographic)
  </read_first>
  <behavior>
    - Test (in task 3's file): scoreCandidate with NO ctx.compression === scoreCandidate pre-edit for identical inputs (byte-identical no-opts behavior)
    - ctx.compression = { activated: true, scoreFor: (c) => 0.8 } adds exactly COMPRESSION_SIGNAL_WEIGHT * 0.8 to the blended score
    - activated false or absent -> zero contribution even when scoreFor is present
    - a candidate with compression score 0 (or a negative Lured score clamped to 0 for the blend) is STILL returned by selectBest when its quality leads - selection sees every candidate (signal, never removal)
    - scoreFor throwing -> contribution 0, no exception escapes (soft-fail)
  </behavior>
  <action>
    Additive edit to lab/apo/apo-loop.cjs mirroring the TELEMETRY_WEIGHT idiom exactly: const COMPRESSION_SIGNAL_WEIGHT = 0.15 (the canon frozen signal-weight cap; comment: bounds the eureka COMPRESSION meter's influence exactly as telemetry is bounded - a quality lead larger than 0.15 can never be overturned, and quality-lexicographic selectBest makes primacy structural, so this term is a SIGNAL by construction, Phase 210-C doctrine extended to the 213 eureka loop per SEED-050 "APO tunes fire-rate against the compression score"). Add compressionTerm(candidate, compression) -> clamp01(compression.scoreFor(candidate)) with try/catch to 0; negative meter scores (Lured) clamp to 0 in the BLEND (the negative verdict lives in the eval report, not as a selection wound - comment this). Extend scoreCandidate: + (ctx.compression && ctx.compression.activated === true ? COMPRESSION_SIGNAL_WEIGHT * compressionTerm(candidate, ctx.compression) : 0). Thread ctx.compression from runApo opts (opts.compression || null) alongside telemetry. Export COMPRESSION_SIGNAL_WEIGHT and compressionTerm. Touch NOTHING in selectBest, qualityTerm, telemetryTerm, or the voice-signal path.
  </action>
  <verify>
    <automated>node -e "const a=require('./lab/apo/apo-loop.cjs'); if(a.COMPRESSION_SIGNAL_WEIGHT!==0.15) throw new Error('weight'); const s1=a.scoreCandidate({},{qualityScoreFn:()=>0.5}); const s2=a.scoreCandidate({},{qualityScoreFn:()=>0.5,compression:{activated:true,scoreFor:()=>0.8}}); if(Math.abs(s2-s1-0.12)>1e-9) throw new Error('blend math'); console.log('202 signal ok')"</automated>
  </verify>
  <done>Blend math exact; no-opts byte-identical; existing APO tests green unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: 205 touchpoint - lateralEngine adapter + grill suggest-quality annotation</name>
  <files>lib/core/eureka/lateral-engine-adapter.cjs, lib/core/grill-engine.cjs</files>
  <read_first>
    - lib/core/fusion-router.cjs :418-458 (the socket contract: an object with a score function; injected -> routed_to 'rs_sideways_engine', differential_score stays null at the router, computed downstream, NEVER fabricated)
    - lib/core/grill-engine.cjs :180-200 (the Arm-A result shape carrying egress.guard_verdict) and :202-230 (the deferral seam - READ, do not flip)
  </read_first>
  <action>
    (1) Create lib/core/eureka/lateral-engine-adapter.cjs exporting makeLateralEngine({ scoreFn?, encodeFn? } = {}) -> { score: async (pair, structure) => result }. The score function extracts the two frames' text via the caller-owned pair objects, delegates to (scoreFn || scoreMeasured) - which self-audits Part 8 and self-degrades - and returns { differential_score: result.signed_diff ?? null, band, surprise_type, provenance } with null (never fabricated) on any degrade. Header: this is the live occupant of the Phase 205 pre-drilled ctx.lateralEngine socket (fusion-router.cjs:283); injection is the CALLER's choice per call (fresh resolution, never registration-frozen - the ui4/MCP-staleness lesson); its output feeds the quorum's SUGGEST path only (Phase 210-D: the quorum suggests, nothing downstream treats it as mandatory). fusion-router.cjs itself is NOT edited - the socket already exists.
    (2) Additive edit to lib/core/grill-engine.cjs: where the Arm-A result object is assembled (:180-200 region), accept an OPTIONAL ctx.eurekaSignal = { guard_verdict, band, surprise_type } (closed enums only; validate membership, drop anything else) and, when present and valid, attach it to the result as eureka_signal (annotation field, default absent). One comment line: "Phase 213 (205 touchpoint, post-210 semantics): an optional suggest-quality annotation from the eureka critic; informational for the navigator-facing render, never a gate on any arm; the BLOCKED_UNTIL_200 deferral (:210) is deliberately untouched - flipping it is a separately-gated navigator decision." No other grill line changes; BLOCKED_UNTIL_200 stays true.
  </action>
  <verify>
    <automated>node -e "const m=require('./lib/core/eureka/lateral-engine-adapter.cjs'); const e=m.makeLateralEngine({scoreFn:async()=>({signed_diff:0.4,band:'high',surprise_type:'structural_transfer'})}); if(typeof e.score!=='function') throw new Error('socket shape'); const {runLateralPath}=require('./lib/core/fusion-router.cjs'); const r=runLateralPath({lateralEngine:e},[{frameKey:'a'},{frameKey:'b'}],'s'); if(r.available!==true||r.routed_to!=='rs_sideways_engine') throw new Error('socket routing'); console.log('205 socket ok')"</automated>
  </verify>
  <done>Adapter satisfies the socket contract against the REAL runLateralPath; grill annotation optional and enum-validated; deferral constant untouched.</done>
</task>

<task type="auto">
  <name>Task 3: tests/test-213-touchpoints.cjs - softened-semantics proof suite</name>
  <files>tests/test-213-touchpoints.cjs</files>
  <read_first>
    - lab/apo tests located via ls tests/ | grep -iE "202|apo" (the no-regression baseline arms)
    - lib/core/fusion-router.cjs :435-445 (the absent-socket degrade shape to pin byte-stable)
  </read_first>
  <action>
    Write the suite: (A) the five Task-1 APO behaviors incl. the byte-identical no-opts arm (compute score pre/post shape by calling with and without compression ctx) and the selection-sees-every-candidate arm; (B) socket arms: runLateralPath WITHOUT lateralEngine returns the exact shipped degrade { available:false, reason:'blocked_until_phase_200_rs', differential_score:null } byte-stable; WITH the adapter -> routed, differential_score null at the router; adapter degrade (scoreFn rejecting) -> score resolves with differential_score null, never throws, never fabricates; (C) grill arms: no ctx.eurekaSignal -> result has no eureka_signal key (absent, not null); valid enums -> attached verbatim; invalid member (e.g. band 'amazing') -> dropped entirely; assert BLOCKED_UNTIL_200 still exported true; (D) the vocabulary fence over the two new/edited surfaces (non-comment lines, forbidden-verbs regex from plan 03 Task 3 arm 3). Hermetic, bare assert.
  </action>
  <verify>
    <automated>node tests/test-213-touchpoints.cjs</automated>
  </verify>
  <done>All arms green; existing APO + fusion suites green unmodified; the softened semantics are pinned mechanically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meter score -> APO blend | scalar in [0,1] only; clamped, bounded by frozen weight |
| eureka enums -> grill result | closed-enum membership validation; anything else dropped |
| adapter -> scoreMeasured | Part-8 dual-layer audit lives inside scoreMeasured (reused, not duplicated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-14 | Elevation of Privilege (signal creep back into a veto) | APO blend / quorum / grill | mitigate | quality-lexicographic selection untouched; bounded weight; suite arms A+C prove non-removal; vocabulary fence |
| T-213-15 | Tampering (fabricated differential) | lateral adapter | mitigate | null-never-fabricated pinned (T-205-07-R inherited); degrade arm in suite |
| T-213-16 | Information Disclosure | grill annotation | mitigate | closed-enum membership validation drops any non-enum value |
</threat_model>

## Artifacts this phase produces (plan 05 contribution)

| Artifact | Kind | Names/symbols |
|----------|------|---------------|
| lab/apo/apo-loop.cjs | edit | COMPRESSION_SIGNAL_WEIGHT=0.15, compressionTerm, ctx.compression { activated, scoreFor } seam |
| lib/core/eureka/lateral-engine-adapter.cjs | module | makeLateralEngine |
| lib/core/grill-engine.cjs | edit | optional ctx.eurekaSignal -> result.eureka_signal annotation (closed enums) |
| tests/test-213-touchpoints.cjs | test | arms A-D |
| New env vars | none | - |

<verification>
- `node tests/test-213-touchpoints.cjs` exit 0
- every pre-existing 202/205/210 apo + fusion + grill test file exits 0 UNMODIFIED
- objective grep gates pass (weight present, deferral untouched, vocabulary clean, no em-dashes)
</verification>

<success_criteria>
All three researched touchpoint seams live in their softened forms: compression is a bounded reward signal, the lateral socket has a real occupant, the grill carries an optional suggest-quality annotation - and the one genuine fork (the Arm-B deferral flip) is flagged to the navigator, not silently decided.
</success_criteria>

<output>
Create `.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-05-SUMMARY.md` when done.
</output>
