---
phase: 213-eureka-reach-wiring
plan: 06
type: execute
wave: 3
depends_on: [213-01, 213-02, 213-03, 213-04, 213-05]
files_modified:
  - tests/run-all-213.sh
  - tests/test-213-part8-boundary.cjs
autonomous: false
requirements: [EUREKA-06, EUREKA-07]
tags: [eureka, phase-gate, part8, regression, human-verify]

must_haves:
  truths:
    - "bash tests/run-all-213.sh aggregates every 213 leg with the house PASS/FAIL/SKIP + run_if-file-guard discipline and exits non-zero on any FAIL"
    - "The Part-8 boundary suite proves handles/enums-only across every 213 surface: the side-channel file, the offer text, the APO signal, the grill annotation (EUREKA-07 closed)"
    - "The 190 touchpoint holds in its post-210 form: check-shape-declaration --check exits 0 (born-declared, advisory lint green) - declare anyway, never lean on the advisory downgrade"
    - "Prior aggregators are unchanged: run-all-211 shows PASS=9 with at most the one pre-existing env-dependent rerank FAIL (proven pre-existing by the room's HEAD~1 repro - NOT a 213 regression), run-all-205 and run-all-144 green"
    - "A HUMAN ran the real-conversation Larry probe and approved; the phase does not self-certify (the 211/212 honest-deferral pattern)"
    - "The Dev-Research Compositing mandate is satisfied: the 213 findings are mirrored to the rethinking-mindrianos room, cross-linked both ways"
  artifacts:
    - path: "tests/run-all-213.sh"
      provides: "The single phase gate aggregator"
      min_lines: 40
    - path: "tests/test-213-part8-boundary.cjs"
      provides: "The handles/enums-only boundary sweep over all 213 surfaces"
      min_lines: 60
  key_links:
    - from: "tests/run-all-213.sh"
      to: "tests/test-213-*.cjs"
      via: "run_if legs (guarded on file existence, SKIP not FAIL on absence)"
      pattern: "test-213-"
    - from: "tests/test-213-part8-boundary.cjs"
      to: "lib/core/part8-egress-guard.cjs"
      via: "reuse the Phase 196 classify() guard, never a second auditor"
      pattern: "part8-egress-guard"
---

<objective>
Close the phase: the run-all-213 aggregator (the RESEARCH Wave-0 gap), the cross-surface Part-8 boundary sweep, the 190-touchpoint born-declared assertion, the full regression sweep over the touched prior suites, the blocking human real-conversation probe, and the Dev-Research room mirror. This plan is the phase gate; nothing in 213 is "done" until it is green AND human-approved.

Purpose: RESEARCH V2 - the room treats 213 as the precedent-setting proof. A proof phase that self-certifies proves nothing; the human probe + the mechanical gates are the credibility.

Output: aggregator, boundary suite, regression evidence in the SUMMARY, room mirror entry, navigator approval on record.

**EXECUTION PRECONDITIONS:** same two phase gates ((a) curing-track verdict, (b) 212-05 checkpoint) plus SUMMARYs for 213-01 through 213-05 all exist.

Acceptance criteria (objective, grep-able / exit codes):
- `bash tests/run-all-213.sh` exits 0 with FAIL=0
- `node tests/test-213-part8-boundary.cjs` exits 0
- `node scripts/check-shape-declaration.cjs --check` exits 0 (the 190 touchpoint, post-210 advisory form, green)
- `node scripts/check-render-coverage.cjs` exits 0 and `node scripts/build-connector-registry.cjs --check` exits 0
- `bash tests/run-all-211.sh` output shows PASS=9 and FAIL is 0 or 1 where the 1 is ONLY the documented env-dependent rerank leg; `bash tests/run-all-205.sh` and `bash tests/run-all-144.sh` exit 0
- `node scripts/doctor.cjs --acceptance` exits 0
- `grep -rlP '\x{2014}' lib/core/eureka/ lib/core/sensors/sensor-eureka.cjs lab/eureka-graders/ tests/test-213-*.cjs tests/run-all-213.sh` exits 1 (zero em-dash files across every 213 artifact)
- `grep -rniE "\b(disqualif|auto[-_]?fire|must[-_]?answer|force[-_]?fire)\b" lib/core/eureka/ lib/core/sensors/sensor-eureka.cjs lab/eureka-graders/` on non-comment lines returns 0 hits (phase-wide softened-vocabulary sweep)
- Room mirror file exists: `ls ~/MindrianRooms/rethinking-mindrianos/research/2026-*-phase213-born-wired-eureka-reach*/` succeeds
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-RESEARCH.md (Validation Architecture, V7 - the pre-existing 211 rerank FAIL is NOT a 213 regression)
@tests/run-all-211.sh (the aggregator idiom: run_if file-guarded legs, PASS/FAIL/SKIP counters, offline preload)
@lib/core/part8-egress-guard.cjs (the Phase 196 classify() guard to REUSE)
@.planning/debug/beta13-curing-sequence-persona-and-commands-bisect.md (the QA-protocol style for the human probe)
@CLAUDE.md (Dev-Research Compositing section - the two-homes mandate)
@.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-01-SUMMARY.md through 213-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: run-all-213.sh + the Part-8 boundary sweep</name>
  <files>tests/run-all-213.sh, tests/test-213-part8-boundary.cjs</files>
  <read_first>
    - tests/run-all-211.sh (full - the run_if guard, NODE_OPTIONS offline preload, counter + exit discipline)
    - lib/core/part8-egress-guard.cjs header (classify() contract)
    - tests/fixtures/213/last-eureka.json
  </read_first>
  <action>
    (1) Create tests/run-all-213.sh on the run-all-211.sh model (bash, set -uo pipefail, run_if legs guarded on file existence so a partially-landed phase SKIPs cleanly, PASS/FAIL/SKIP counters, exit 1 on FAIL>0, reuse the eureka-offline-preload NODE_OPTIONS line so no leg can download a model). Legs in order: test-213-compression-meter, test-213-graders, test-213-sensor-eureka, test-213-reach-wired, test-213-no-force, test-213-eureka-offer, test-213-touchpoints, test-213-part8-boundary. Header comment names the phase gate contract: this script green + the plan-06 human probe = the 213 gate.
    (2) Create tests/test-213-part8-boundary.cjs - the cross-surface handles/enums sweep REUSING the Phase 196 guard (require lib/core/part8-egress-guard.cjs classify(); never a second auditor): (a) SIDE-CHANNEL: run the plan-02 writer with a poisoned injected pair (sentinel prose in text fields) and assert the written last-eureka.json contains no sentinel byte anywhere (content never crosses; only handles/enums/quantized scalars); (b) OFFER: composeEurekaOffer over sentinel handles - classify() the offer.text and the full offer object as if Brain-bound and assert no violation, plus the token-whitelist re-check; (c) APO SIGNAL: assert the ctx.compression seam carries only { activated, scoreFor } and the blend consumes a scalar (shape-level assertion); (d) GRILL ANNOTATION: assert eureka_signal attaches only closed-enum members (re-drive the plan-05 drop arm through classify()). Every arm hermetic (injected fns, temp dirs).
  </action>
  <verify>
    <automated>bash tests/run-all-213.sh</automated>
  </verify>
  <done>Aggregator green end to end with FAIL=0; boundary suite green; SKIP discipline verified by renaming one leg temporarily is NOT required (the run_if guard is inspected, not exercised destructively).</done>
</task>

<task type="auto">
  <name>Task 2: full regression + 190-touchpoint assertion + room mirror</name>
  <files>none (evidence gathered into the SUMMARY; mirror written outside the repo)</files>
  <read_first>
    - CLAUDE.md Verification section (the four gate commands) + Dev-Research Compositing section
    - ~/MindrianRooms/rethinking-mindrianos/research/2026-07-05-statusline-ratification-cue/ (the mirror-entry format to follow)
  </read_first>
  <action>
    (1) Run and record verbatim in the SUMMARY: bash tests/run-all-213.sh; bash tests/run-all-211.sh (expect PASS=9, FAIL 0 or the one documented env rerank leg - if ANY OTHER leg fails, that IS a 213 regression, stop and fix); bash tests/run-all-205.sh; bash tests/run-all-144.sh; node scripts/check-shape-declaration.cjs --check (the 190 touchpoint: 213 minted no new command/agent/pipeline/skill surface, so the declared count is unchanged and the advisory lint is green - record the enumerated count); node scripts/check-render-coverage.cjs; node scripts/build-connector-registry.cjs --check; node scripts/doctor.cjs --acceptance. Also run node scripts/doctor.cjs --drift and record the class-R runtime-reachability line (green context for the born-wired claim).
    (2) Run the phase-wide vocabulary + em-dash sweeps from the acceptance criteria and record zero-hit evidence.
    (3) Dev-Research Compositing (CLAUDE.md mandate - this phase is architecture work): write the room mirror entry at ~/MindrianRooms/rethinking-mindrianos/research/2026-MM-DD-phase213-born-wired-eureka-reach/2026-MM-DD-phase213-born-wired-eureka-reach.md (today's date) summarizing: the born-wired proof shipped (first spine-at-feature-time instance, 04-synthesis Step 2 change #1), the co-fire verdict from test-213-reach-wired arm 3, the three softened-touchpoint seams, the two flagged Decision-Gate encodings (POST-GuardGate Stage-A firing; meter/grader homes) and the third flag (BLOCKED_UNTIL_200 left deferred), and the named second-proof candidate (Phase 214 find-analogies). Cross-link: the room entry cites this phase dir; the 213-06 SUMMARY cites the room entry path. Mirror to mindrianOS/research/ per the mandate if that sibling exists; otherwise note the single-mirror waiver explicitly in the SUMMARY.
  </action>
  <verify>
    <automated>node scripts/doctor.cjs --acceptance</automated>
  </verify>
  <done>All regression evidence recorded verbatim; 190 assertion green with counts; mirror entry exists and is cross-linked both ways.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: the real-conversation Larry probe (the phase gate's human leg)</name>
  <what-built>The eureka reach is wired end to end: material filed -> auto-explore fires the bridge scan -> guard-cleared side-channel -> SENS-13 -> decide() -> the hedged Shape-F eureka offer with a next-command segue -> (only on your confirm) the existing act/runChain path. All mechanical gates are green (Tasks 1-2 evidence in the draft SUMMARY).</what-built>
  <how-to-verify>
    1. Open a FRESH Larry session in a test room that has the 211 substrate populated (the JHTV room used for the 211 room report is suitable), with a fresh guard-cleared side-channel present (the executor stages it via runEurekaScan or the fixture if the live scan finds no bridge - the staging method is recorded honestly either way).
    2. Converse past turn 3 on a quiet turn (no bottleneck/contradiction phrasing). EXPECTED: Larry surfaces ONE hedged eureka offer naming two node handles and a transfer direction, ending in a question. It must feel like a suggestion, not a card shoved at you (the Stop-hook force-fire class is the regression to smell for - feedback_1_15_enforcement_regression_watch).
    3. DECLINE it. EXPECTED: nothing executes, no re-prompt pressure, the conversation continues normally.
    4. Re-trigger and CONFIRM it. EXPECTED: the recommended next command runs through the existing chain path and HALTS at the first material step for you.
    5. Tri-Polar sanity: confirm the offer content is surface-agnostic (the same composeEurekaOffer object; CLI card vs Desktop conversational rendering differ, content identical).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 213, or describe what felt wrong (especially any forcing pressure or content leakage in the offer text) - a forcing-pressure report reopens plan 03/04, not a patch here.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| all 213 surfaces -> anything shared/remote | the boundary suite is the fence: handles/enums/quantized scalars only |
| machine -> navigator | the human probe explicitly tests decline-without-pressure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-17 | Information Disclosure | any 213 surface | mitigate | test-213-part8-boundary reuses the 196 classify() guard across all four surfaces |
| T-213-18 | Elevation of Privilege | end-to-end UX | mitigate | human probe steps 2-3 (hedged, declinable, no re-prompt pressure); blocking checkpoint, never self-certified |
| T-213-19 | Repudiation (regression blame) | prior suites | mitigate | verbatim regression evidence in SUMMARY; the 211 env leg pre-documented so it cannot mask a real 213 break |
</threat_model>

## Artifacts this phase produces (plan 06 contribution + phase roll-up)

| Artifact | Kind | Names/symbols |
|----------|------|---------------|
| tests/run-all-213.sh | aggregator | 8 legs, PASS/FAIL/SKIP discipline |
| tests/test-213-part8-boundary.cjs | test | 4 boundary arms via part8-egress-guard classify() |
| room mirror entry | doc (outside repo) | 2026-MM-DD-phase213-born-wired-eureka-reach |
| PHASE ROLL-UP (all plans) | - | SENS-13 sensor + registry entry; eureka-reach-runner (last-eureka.json side-channel, schema v1); compression-meter + 2 graders (enums: ARRIVAL_VERDICTS, STATUS_QUO_LABELS); eureka-offer (DIRECTION_ENUM); COMPRESSION_SIGNAL_WEIGHT in apo-loop; lateral-engine-adapter; grill eureka_signal annotation; 8 test files + aggregator + 1 fixture; ZERO new commands/agents/pipelines/skills; ZERO new env vars; ZERO new packages |

<verification>
- `bash tests/run-all-213.sh` FAIL=0
- all acceptance-criteria commands from the objective pass
- navigator approval recorded at the Task-3 checkpoint
</verification>

<success_criteria>
The phase gate is green mechanically AND human-approved: born-wired proof on disk and in doctor --drift context, Part-8 closed across every surface, prior suites unregressed, the room carries the durable reasoning trail, and Phase 213 stands as the first spine-at-feature-time precedent with the second proof candidate named.
</success_criteria>

<output>
Create `.planning/phases/213-eureka-reach-wiring-the-key-eureka-reach-wiring/213-06-SUMMARY.md` when done, including: the verbatim regression evidence, the co-fire verdict, the three flags for the navigator (POST-GuardGate Stage-A firing; meter/grader homes; BLOCKED_UNTIL_200 left deferred), and the named second born-wired proof candidate (Phase 214 find-analogies riding deep_research).
</output>
