---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 11
type: execute
wave: 3
depends_on: ["355-02"]
files_modified:
  - lib/core/intelligence-cascade.cjs
  - scripts/scout-cadence-runner.cjs
  - tests/test-scout-cadence-fires.cjs
  - commands/scout.md
  - skills/scout/SKILL.md
  - scripts/detect-reverse-salients.py
  - scripts/compute-hsi.py
autonomous: true
requirements: [HIPS-01]
tags: [python-retirement, scout, cascade, d-52]

must_haves:
  truths:
    - "D-05 / D-52: the Python reverse-salient detector (scripts/detect-reverse-salients.py) is removed from all four live callers: lib/core/intelligence-cascade.cjs, scripts/scout-cadence-runner.cjs, commands/scout.md and skills/scout/SKILL.md; no Python is ported and no Python file's logic changes"
    - "D-52: scout's step 2 now names and runs the CJS RS engine (lib/core/rs-engine.cjs runModeInternal, the same engine /mos:find-bottlenecks uses); the cadence runner's SCHED-02 reverse-salient sensor is the CJS engine step, so the scheduled run still carries a reverse-salient step"
    - "D-52: scripts/detect-reverse-salients.py and scripts/compute-hsi.py each gain a header note: offline reference only (compute-hsi.py also remains the MINDRIAN_RS_BACKEND=python fallback and scout's HSI step 1); since Phase 355 no reader trusts their direction strings, which are re-derived from the stored pair (D-07, plan 355-12)"
    - "D-06 / D-58: tests/test-scout-cadence-fires.cjs's SCHED-02 reverse-salient assertion is amended with a Phase 355 D-52 citation to accept the CJS step name"
    - "Runtime state (RESEARCH inventory b): Python-sourced REVERSE_SALIENT section edges stop being rewritten and disappear at the next hsi-to-graph run; rs-engine-sourced edges (source 'rs-engine', Convention A) are unaffected; the SUMMARY records every unfiltered REVERSE_SALIENT reader found (assumption A2) for plan 355-12"
    - "RED-to-GREEN: leg G of tests/test-355-direction-agreement.cjs passes"
  artifacts:
    - path: "scripts/scout-cadence-runner.cjs"
      provides: "SCHED-02 reverse-salient step on the CJS engine"
      contains: "rs-engine"
  key_links:
    - from: "commands/scout.md step 2"
      to: "lib/core/rs-engine.cjs runModeInternal"
      via: "node invocation in the documented bash block"
      pattern: "runModeInternal"
---

<objective>
HIPS-01 leg G (D-05, D-52): the Python reverse-salient detector writes the opposite direction convention and still runs unconditionally from four places. Take it off every live path, give scout's step 2 the CJS RS engine instead, and label the two Python scripts as reference so nobody mistakes them for the live rule.

Purpose: after this plan no live code path produces a Convention B direction label for a reverse salient.
Output: two code callers edited, two docs edited, two header notes, one amended test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@tests/test-355-direction-agreement.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the detector spawn from the cascade and the cadence runner; CJS RS step for SCHED-02</name>
  <files>lib/core/intelligence-cascade.cjs, scripts/scout-cadence-runner.cjs, tests/test-scout-cadence-fires.cjs</files>
  <read_first>
    - lib/core/intelligence-cascade.cjs lines 280-310 and 420-500 (stepsResult shape, the HSI backend dispatch, the unconditional detect-reverse-salients execFileSync, the hsi-to-graph step)
    - scripts/scout-cadence-runner.cjs lines 360-420 (runStep, bin(), hsiDepsAvailable gating, the detect-reverse-salients step and its skipped twin)
    - lib/core/rs-engine.cjs lines 470-600 (runModeInternal(roomDir, opts) signature, what it writes: .rs-engine-results.json and REVERSE_SALIENT edges with source 'rs-engine')
    - tests/test-scout-cadence-fires.cjs lines 140-190 (SCHED-02 assertions)
  </read_first>
  <action>intelligence-cascade.cjs: delete the execFileSync block that runs detect-reverse-salients.py (and its now-stale header comment about staying unchanged); set `stepsResult.reverseSalients = { status: 'retired', reason: 'Phase 355 D-52: Python detector off the live path; reverse salients come from lib/core/rs-engine.cjs' }` so the result shape keeps its key; leave the HSI backend dispatch and hsi-to-graph step untouched. scout-cadence-runner.cjs: remove both the live and the skipped `detect-reverse-salients` steps and add one step named `rs-engine-cjs` that runs `process.execPath` with `-e` and a two-line script calling `require(<abs lib/core/rs-engine.cjs>).runModeInternal(roomDir, <the opts that match /mos:find-bottlenecks internal mode, read from rs-engine.cjs>)`, placed where the detector step was and NOT gated on scikit-learn (the CJS engine has no Python dependency); keep runStep's non-fatal error handling. tests/test-scout-cadence-fires.cjs: amend the SCHED-02 reverse-salient assertion to `names.includes('rs-engine-cjs')` with a comment "Amended Phase 355 D-52: the Python detector is retired; SCHED-02's reverse-salient sensor is the CJS RS engine". Grep and record in the SUMMARY every reader of REVERSE_SALIENT edges that does not filter on source 'rs-engine' (`grep -rn "REVERSE_SALIENT" lib scripts --include=*.cjs`), for plan 355-12. Commit `fix(355-11): Python reverse-salient detector off the cascade and cadence paths (HIPS-01, D-52)`.</action>
  <verify>
    <automated>node tests/test-scout-cadence-fires.cjs && ! (node tests/test-355-direction-agreement.cjs 2>&1 | grep -E "^FAIL: G " | grep -qE "intelligence-cascade|scout-cadence-runner")</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-scout-cadence-fires.cjs` exits 0
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/intelligence-cascade.cjs scripts/scout-cadence-runner.cjs | grep -c "detect-reverse-salients"` prints 0
    - `grep -c "rs-engine-cjs" scripts/scout-cadence-runner.cjs` >= 1 and `grep -c "Phase 355 D-52" tests/test-scout-cadence-fires.cjs` >= 1
    - `grep -c "status: 'retired'" lib/core/intelligence-cascade.cjs` >= 1
  </acceptance_criteria>
  <done>No background path spawns the Python detector; the scheduled run still carries a reverse-salient step.</done>
</task>

<task type="auto">
  <name>Task 2: Scout docs name the CJS engine; Python scripts marked as reference; registry gates green</name>
  <files>commands/scout.md, skills/scout/SKILL.md, scripts/detect-reverse-salients.py, scripts/compute-hsi.py</files>
  <read_first>
    - commands/scout.md lines 250-300 (5b HSI pipeline bash block and the Report list)
    - skills/scout/SKILL.md lines 245-290 (the mirrored block)
    - scripts/detect-reverse-salients.py and scripts/compute-hsi.py first 20 lines
    - scripts/hooks/pre-commit legs that fire on commands/*.md and skills/*/SKILL.md (build-command-registry --check, build-connector-registry --check, orchestration projection, check-shape-declaration --check, check-help-coverage, command-registration-check, check-reward-before-investment --staged)
  </read_first>
  <action>In commands/scout.md and skills/scout/SKILL.md replace "Step 2: Detect reverse salients" and its python3 line with "Step 2: Reverse salients from the CJS RS engine (the engine /mos:find-bottlenecks uses)" and a `node -e` line invoking `${PLUGIN_ROOT}/lib/core/rs-engine.cjs` runModeInternal on "$ROOM_DIR" (same opts as Task 1); add one sentence under the block: "Phase 355: the Python detector is reference only; direction labels in .hsi-results.json are re-derived from the stored similarity pair, never trusted as written." Keep step 1 and step 3 byte-unchanged except the comment. Add a header comment to scripts/detect-reverse-salients.py ("Offline reference only since Phase 355 D-52; no live caller. It writes the retired Convention B direction; do not reintroduce it on a live path.") and to scripts/compute-hsi.py ("Reference implementation, the MINDRIAN_RS_BACKEND=python fallback and scout's HSI step 1. Its surprise_type strings use the retired Convention B; since Phase 355 D-07 no reader trusts them: hsi-to-graph re-derives from lsa_sim / semantic_sim through lib/core/direction-convention.cjs."). Comments only in the .py files; no logic change. Run every pre-commit gate listed in read_first plus the agreement test. Commit `docs(355-11): scout step 2 runs the CJS RS engine; Python scripts marked reference (D-52)`.</action>
  <verify>
    <automated>node scripts/build-command-registry.cjs --check && node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check && node scripts/check-shape-declaration.cjs --check && ! (node tests/test-355-direction-agreement.cjs 2>&1 | grep -qE "^FAIL: G ")</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command passes (agreement leg G has no FAIL line)
    - `grep -cE "python3?\s+[^\n]*detect-reverse-salients\.py" commands/scout.md skills/scout/SKILL.md` prints 0 for each file
    - `git diff -U0 scripts/detect-reverse-salients.py scripts/compute-hsi.py | grep '^[-+]' | grep -v '^[-+][-+]' | grep -vcE '^[-+]\s*#'` prints 0 (comment lines only)
    - `grep -c "runModeInternal" commands/scout.md skills/scout/SKILL.md` shows >= 1 in each file
  </acceptance_criteria>
  <done>The Python detector exists only as labeled reference; scout documents the live CJS engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduled / cascade background runs -> room graph | Unattended writers of REVERSE_SALIENT edges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-51 | Tampering | inverted labels written by a background path | mitigate | Detector removed from all four callers; agreement leg G static scan |
| T-355-52 | Denial of service | SCHED-02 losing its reverse-salient sensor | mitigate | CJS rs-engine step replaces it; cadence test amended and green |
| T-355-53 | Repudiation | stale Convention B edges in existing rooms | accept | Python-sourced edges vanish at the next hsi-to-graph run; readers that surface them are fixed in 355-12 (list recorded here) |
| T-355-54 | Tampering | Python logic edited under a "comment" commit | mitigate | Acceptance asserts .py diffs are comment lines only |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Cadence step name `rs-engine-cjs` (`scripts/scout-cadence-runner.cjs`)
- Cascade result `stepsResult.reverseSalients = { status: 'retired', reason }` (`lib/core/intelligence-cascade.cjs`)
- Header notes in `scripts/detect-reverse-salients.py` and `scripts/compute-hsi.py`

<verification>
- Cadence test and registry gates green; agreement leg G passes
</verification>

<success_criteria>
- The Python detector is off every live path; the scheduled run still has a reverse-salient sensor
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-11-SUMMARY.md` when done.
</output>
