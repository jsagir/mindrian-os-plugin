---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 10
type: tdd
wave: 3
depends_on: ["355-02"]
files_modified:
  - tests/fixtures/355/eureka-ranking-pin.json
  - tests/test-355-eureka-ranking-pin.cjs
  - lib/core/rs-differential-scorer.cjs
  - lib/core/eureka/portfolio-dimensions.cjs
  - tests/test-211-measured-differential.cjs
  - tests/test-215-score.cjs
autonomous: true
requirements: [HIPS-01]
tags: [direction-convention, eureka, scoreMeasured, ranking-pin, atomic]

must_haves:
  truths:
    - "D-47 (C1): lib/core/rs-differential-scorer.cjs scoreMeasured is the FIFTH direction site; its label becomes classify(lexical, semantic) from the one module; highLeg stays sign-keyed and byte-unchanged"
    - "D-47 (C2): in the SAME commit, portfolio-dimensions.cjs feasibilityFromRs swaps its two label branches so the meaning-to-feasibility map is unchanged (the pairs that scored 1.0 / 0.7 still do; the paraphrase-risk 0.4 pairs still do); a ranking-pin test captured on pre-flip code proves eureka's feasibility values and rank order do not move"
    - "D-06 / D-58: tests/test-211-measured-differential.cjs (Tests 5-6 directions) and tests/test-215-score.cjs (feasibility by label) are amended in place with a Phase 355 D-47 header line; only expected labels change"
    - "RESEARCH Open Question 1 (recorded tension): today's ranking rewards shared-words pairs; this plan preserves it byte-for-byte and the tension is written into 355-VERIFICATION.md by 355-25 for the next engine phase"
    - "RED-to-GREEN: after this plan, leg E (scoreMeasured) of tests/test-355-direction-agreement.cjs passes"
  artifacts:
    - path: "tests/test-355-eureka-ranking-pin.cjs"
      provides: "Pre-flip feasibility + rank order pinned across the flip"
      min_lines: 60
    - path: "tests/fixtures/355/eureka-ranking-pin.json"
      provides: ">= 12 (lexical, semantic) pairs with pre-flip feasibility and rank recorded"
  key_links:
    - from: "lib/core/rs-differential-scorer.cjs scoreMeasured"
      to: "lib/core/direction-convention.cjs classify"
      via: "require + call replacing the inverted ternary"
      pattern: "classify\\(lexical, semantic\\)"
    - from: "lib/core/eureka/portfolio-dimensions.cjs feasibilityFromRs"
      to: "rs.direction"
      via: "swapped branches: semantic_implementation -> bridge / transfer; structural_transfer -> semantic floor"
      pattern: "semantic_implementation"
---

<objective>
HIPS-01, the fifth producer (D-47, research C1/C2): `scoreMeasured` feeds the live eureka path and uses the inverted rule. Flipping it alone would silently reorder `/mos:eureka`, because `portfolio-dimensions.cjs` scores feasibility by label. This plan pins today's ranking first (on unchanged code), then flips the label and swaps the two feasibility branches in ONE commit, so the pin proves nothing a user sees moves except the direction's meaning becoming correct.

Purpose: an honesty fix that does not smuggle in a ranking change.
Output: ranking-pin fixture and test, the atomic flip, two amended tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@lib/core/direction-convention.cjs
@tests/test-355-direction-agreement.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin today's eureka feasibility and rank order on UNCHANGED code</name>
  <files>tests/fixtures/355/eureka-ranking-pin.json, tests/test-355-eureka-ranking-pin.cjs</files>
  <read_first>
    - lib/core/rs-differential-scorer.cjs lines 440-445 and 560-600 (scoreMeasured, opts.lexicalFn / opts.encodeFn seams, passes, highLeg)
    - lib/core/eureka/portfolio-dimensions.cjs lines 1-60, 170-240 (FEASIBILITY, BRIDGE_BAND_LO / HI, feasibilityFromRs, scorePairDimensions, exports and _test)
    - scripts/eureka-portfolio-report.cjs: the function that composes each pair's composite `score` and sorts `ranked` (grep `ranked` and `scorePairDimensions`)
    - tests/test-215-score.cjs lines 1-40 and 80-110 (how existing tests build a pair-scoring input)
    - 355-RESEARCH.md "Code Examples > Driving scoreMeasured to an exact (lexical, semantic) pair offline"
  </read_first>
  <action>Write tests/test-355-eureka-ranking-pin.cjs (hygiene-355 preamble). For each fixture pair it computes rs = await scoreMeasured('pin a','pin b',{ lexicalFn: () => lexical, encodeFn: () => vecsFor(semantic) }), then feasibility = feasibilityFromRs(rs) (through portfolio-dimensions' exported path or _test), then the composite score and rank through the same function eureka-portfolio-report.cjs uses (holding every other dimension at the fixture's fixed values), and asserts feasibility, composite and the sorted order equal the fixture's recorded values. Author tests/fixtures/355/eureka-ranking-pin.json with at least 12 pairs spanning: lexical high / semantic low inside and outside BRIDGE_BAND, semantic high / lexical low, exact ties, one pair failing `passes`, and two pairs with equal composites (tie order recorded as produced); record expected_feasibility, expected_score and expected_rank by running the CURRENT, unflipped code once (a generator block inside the test behind `--record`, run once, then committed output; the normal run never rewrites the fixture). Run the test green on unchanged code. Commit `test(355-10): pin pre-flip eureka feasibility and rank (D-47)`.</action>
  <verify>
    <automated>node tests/test-355-eureka-ranking-pin.cjs && git diff --quiet lib/core/rs-differential-scorer.cjs lib/core/eureka/portfolio-dimensions.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-355-eureka-ranking-pin.cjs` exits 0 while rs-differential-scorer.cjs and portfolio-dimensions.cjs are unmodified (git diff --quiet on both succeeds)
    - `node -e "const f=require('./tests/fixtures/355/eureka-ranking-pin.json');if((f.pairs||f).length<12)process.exit(1)"` exits 0
    - The pin commit precedes the flip commit in `git log`
  </acceptance_criteria>
  <done>Today's eureka ranking is frozen in a test before anything moves.</done>
</task>

<task type="auto">
  <name>Task 2: ATOMIC - flip scoreMeasured and swap the feasibility branches in one commit; amend 211 and 215 (D-47)</name>
  <files>lib/core/rs-differential-scorer.cjs, lib/core/eureka/portfolio-dimensions.cjs, tests/test-211-measured-differential.cjs, tests/test-215-score.cjs</files>
  <read_first>
    - lib/core/rs-differential-scorer.cjs lines 436-450 (header documenting the inverted meaning) and 560-600
    - lib/core/eureka/portfolio-dimensions.cjs lines 170-190
    - tests/test-211-measured-differential.cjs lines 95-130 (Tests 5-6)
    - tests/test-215-score.cjs lines 75-115
    - Every other scoreMeasured consumer, read to confirm none branches on the label value: lib/core/eureka/eureka-reach-runner.cjs (:256-260, :380-402), lib/core/eureka/lateral-engine-adapter.cjs (:90-100), lib/core/intel-pipeline.cjs, lib/core/graph-backfill.cjs, lib/core/graph-derive-classifier.cjs, lib/core/intelligence-cascade.cjs, lib/core/eureka/lexical-overlap.cjs
    - data/floor-ledger.json rows for rs-differential-scorer (highLeg and bandFor anchors must still resolve)
  </read_first>
  <action>rs-differential-scorer.cjs: require `./direction-convention.cjs`; in scoreMeasured replace the inverted ternary with `const direction = classify(lexical, semantic);` and keep `signed_diff`, `abs_diff`, `highLeg` (sign-keyed), `passes` and every band/floor line byte-unchanged; rewrite the header lines that documented the inverted meaning to say the direction now follows direction-convention.cjs (Phase 355 D-47) and that an exact tie is semantic_implementation. portfolio-dimensions.cjs: swap the two label branches of feasibilityFromRs (semantic_implementation -> BRIDGE_BAND check -> FEASIBILITY.bridge else FEASIBILITY.transfer; structural_transfer -> FEASIBILITY.semantic; unknown / 'none' -> FEASIBILITY.semantic unchanged) with a comment citing Phase 355 D-47: "the label meaning flipped; the pair-to-feasibility map did not; the tension (today's ranking favors shared-words pairs) is recorded for the next engine phase". If any consumer read in read_first branches on the direction value, apply the same meaning-preserving swap in this commit and add it to the ranking pin; otherwise record "no other label-keyed consumer" in the SUMMARY. Amend tests/test-211-measured-differential.cjs Tests 5-6 and tests/test-215-score.cjs label-keyed expectations with a header line "Amended Phase 355 D-47" (only labels change; feasibility numbers in 215 stay identical because the branches swapped with the labels). Run: the ranking pin (must stay green), 211, 215, the agreement test (leg E green), the floor sweep, tests/test-213-sensor-eureka.cjs. Commit ALL FOUR files in ONE commit: `fix(355-10): scoreMeasured takes the one direction rule; feasibility branches swapped so eureka ranking is unchanged (HIPS-01, D-47)`.</action>
  <verify>
    <automated>node tests/test-355-eureka-ranking-pin.cjs && node tests/test-211-measured-differential.cjs && node tests/test-215-score.cjs && node tests/test-355-floor-sweep.cjs && node tests/test-213-sensor-eureka.cjs && ! (node tests/test-355-direction-agreement.cjs 2>&1 | grep -qE "^FAIL: E ")</automated>
  </verify>
  <acceptance_criteria>
    - All verify commands pass; `tests/fixtures/355/eureka-ranking-pin.json` is byte-unchanged by this task (`git diff --quiet tests/fixtures/355/eureka-ranking-pin.json`)
    - `git show --stat HEAD` lists exactly lib/core/rs-differential-scorer.cjs, lib/core/eureka/portfolio-dimensions.cjs, tests/test-211-measured-differential.cjs and tests/test-215-score.cjs (plus any consumer swapped under the read_first rule)
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/rs-differential-scorer.cjs | grep -cE "\?\s*'semantic_implementation'\s*:\s*'structural_transfer'"` prints 0
    - `grep -c "Phase 355 D-47" tests/test-211-measured-differential.cjs tests/test-215-score.cjs lib/core/eureka/portfolio-dimensions.cjs` shows >= 1 in each file
  </acceptance_criteria>
  <done>Eureka's direction is honest and its ranking is provably unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scoreMeasured label -> eureka ranking, side channel, reach card | The label drives what the user is offered first |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-47 | Tampering | silent ranking change | mitigate | Ranking pin captured on unchanged code, then flip + swap in one commit; pin must stay green |
| T-355-48 | Repudiation | half-applied flip across commits | mitigate | Acceptance asserts the flip commit contains both source files and both test amendments |
| T-355-49 | Tampering | a second label-keyed consumer missed | mitigate | read_first enumerates every scoreMeasured consumer; any label branch is swapped and pinned in the same commit |
| T-355-50 | Spoofing | fixture regenerated after the flip | mitigate | Normal run never writes the fixture; acceptance asserts it is byte-unchanged by Task 2 |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Test `tests/test-355-eureka-ranking-pin.cjs` (flag `--record`, run once pre-flip)
- Fixture `tests/fixtures/355/eureka-ranking-pin.json` (`pairs[{lexical, semantic, dims, expected_feasibility, expected_score, expected_rank}]`)

<verification>
- Ranking pin, 211, 215, floor sweep, 213 sensor green; agreement leg E passes
</verification>

<success_criteria>
- The fifth direction site follows the module and eureka's order is unchanged
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-10-SUMMARY.md` when done.
</output>
