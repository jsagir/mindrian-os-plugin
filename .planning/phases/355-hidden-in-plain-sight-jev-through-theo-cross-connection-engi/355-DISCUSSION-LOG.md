# Phase 355: Hidden in Plain Sight - Jev-through-Theo Cross-Connection Engines - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md - this log preserves the alternatives considered.

**Date:** 2026-09-23
**Phase:** 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
**Mode:** advisor (USER-PROFILE.md present; calibration tier `minimal_decisive` from the profile's "opinionated" vendor philosophy, no config override; owner framing resolved to outcome language from the profile's guided learning style with no `technical_background` override, so labels were reframed while the technical decision stayed intact)
**Areas discussed:** Direction names; Stamp placement; Floors; Naming honesty; Rendering; Labeling session and fixture rooms; Filing and the Larry touchpoint; plus three follow-ups and one cross-repo gate
**Researchers:** seven `gsd-advisor-researcher` agents on `claude-opus-5-5`, one per area, in parallel; two were re-briefed mid-run with the T-side session's measured `find_connections` shape.

---

## Direction names

| Option | Description | Selected |
|--------|-------------|----------|
| Keep ids, one module, retire Python | New `lib/core/direction-convention.cjs`; rs-math delegates unchanged; hsi-lsa, hsi-engine, rs-innovation-classifier flip; Python spawn removed; eureka_critic enum byte-identical | ✓ |
| Rename to meaning_bridge / term_collision | 65 files, wire enum and SEED-014 contract break, stored labels unknown | |

**User's choice:** Keep ids, one module, retire Python.
**Notes:** Convention A (rs-math) matches the deck; a fourth inverted producer (rs-innovation-classifier) surfaced during research.

## Stamp placement

| Option | Description | Selected |
|--------|-------------|----------|
| One shared verification-stamp.cjs at the output layer | `callTool('find_connections', {from, to})`; local exact-name endpoint resolution; per-run memo; seven-case degradation; judge literal 'none'; eureka side-channel schema bump 1 to 2 | ✓ |
| Stamp inside each engine at finding time | Touches Python compute, duplicates the Theo call five times | |

**User's choice:** One shared module at the output layer.
**Notes:** Re-briefed with the live Theo shape (no `backend` field; success + `refusals` for unresolvable endpoints; `limit` rejected by our guard, not Theo).

## Floors

| Option | Description | Selected |
|--------|-------------|----------|
| Disclose all; ship data/floor-ledger.json | One row per literal, const-name anchors, sweep test with negative control; compute untouched | ✓ |
| Calibrate RS/HSI/eureka floors on the fixture rooms now | Circular with the hit rate; most floors have no env seam | |

**User's choice:** Disclose all this phase.

## Naming honesty

| Option | Description | Selected |
|--------|-------------|----------|
| A2 + B2: descriptions + fixture test | scout-hsi keeps name and NOT EXECUTED banner with a "reference only" line; whitespace_scan description leads with what it returns; fixture test is the real gate | ✓ |
| Wire scout-hsi to compute; rename whitespace_scan | Half-pipeline plus a write path and a model download; a rename is a silent contract break (drift gate covers slash commands only) | |

**User's choice:** A2 + B2.

## Rendering

| Option | Description | Selected |
|--------|-------------|----------|
| Stamp line + evidence block | One shared formatter; glyphs from the existing 12; ASCII path hops; hop counts as words; judge on the last evidence line | ✓ |
| Stamp line only; evidence behind a flag | Fails the AI-SPEC "Bad" row (label with no evidence line) | |

**User's choice:** Stamp line + evidence block.

## Labeling session and fixture rooms

| Option | Description | Selected |
|--------|-------------|----------|
| Keypress CLI + 3 new fixture rooms | scripts/label-355-gold.cjs; seeded shuffle; hash-bound gold; blinding tripwires; three Claude-authored cross-domain rooms | ✓ |
| Markdown sheet + parser | Order effects, no timing, pre-commit hook trips on room-like folders | |

**User's choice:** Keypress CLI + 3 new fixture rooms.
**Notes:** None of the existing fixture rooms can yield 20 cross-domain pairings.

## Filing and the Larry touchpoint

| Option | Description | Selected |
|--------|-------------|----------|
| Stamp on the opportunity node + SOURCED_FROM | writeOpportunityNode with extraProps; writeEdge sourced_from; born proposed; SENS-13 stamped card variant + fire-once dedup | ✓ |
| Separate memory_event; node unchanged | Breaks D10; separates the stamp from the claim the human confirms | |

**User's choice:** Stamp on the opportunity node + SOURCED_FROM.

## Follow-up: gate_answer promotion gap

| Option | Description | Selected |
|--------|-------------|----------|
| Widen _promoteCardSubject to 'opportunity' | Already in TRUTH_CLAIM_TYPES; Phase 348 guard still applies; coordinate with 354-02 | ✓ |
| Route opportunity subjects to qualifyCandidate | Reuses the qualification path; one more hop | |

**User's choice:** Widen `_promoteCardSubject`.

## Follow-up: classifier fallback

| Option | Description | Selected |
|--------|-------------|----------|
| No default label: emit 'none' | Honest; third classifier output value; not in the eureka_critic enum | ✓ |
| Fallback to semantic_implementation | Deterministic default, less honest | |
| Keep structural_transfer | The inverted convention's default | |

**User's choice:** Emit `none`.

## Follow-up: SENS-14 double surfacing

| Option | Description | Selected |
|--------|-------------|----------|
| No: SENS-14 is the harvest lane | Fire-once test constrains SENS-13 only | ✓ |
| Yes: suppress SENS-14 for stamped nodes | Changes SENS-14 behavior this phase | |

**User's choice:** No suppression.

## Cross-repo gate: Jev keyholder vs Theo rule 1

| Option | Description | Selected |
|--------|-------------|----------|
| Amend rule 1: proxied typed judgment is not judgment | Theo passes a stated policy plus Part-8-clean state to Jev, interprets nothing; keeps the 2026-09-17 keyholder ruling | ✓ |
| Keep Theo pure; dev-time Jev only, indefinitely | Caps the "Jev preferred" ruling at dev time | |
| A separate judgment proxy, not Theo | A fourth hosted service | |
| Decide later; record as open | Phase 355 ships judge: none regardless | |

**User's choice:** Amend rule 1. Relayed to the T-side session; the amendment text lands only through Theo's GSD with the navigator's direct approval in that session (T-side's own condition).

## Todos folded

| Todo | Disposition |
|------|-------------|
| Registry-drift gate (2026-07-03) | Folded as D-26 |
| Audit autonomous_safe posture vs the reversibility x consequence grid (2026-09-08) | Folded as D-17 |
| Mirror the gate_render description fix into Theo (2026-09-07) | Closed as stale (T-side verified byte-identical) |

## Claude's Discretion

File names for the stamp formatter and labeling script's terminal fallback; ledger row ids; fixture rooms' domains and texts within D-34; fixture placement for the direction-agreement set; wave ordering honoring D-35 and D-39.

## Deferred Ideas

See CONTEXT.md `<deferred>`: per-user Jev through Theo (SEED-015), canon coverage (SEED-009 / Phase 20.1), OQ-4, the later-phase capabilities, the stage-driven trigger, the Part 9 writer cleanup, the whitespace_scan migrate-on-open door, a compute-claim honesty rule, the F.1 brain-chip percent, the gate_answer description drift, the ROADMAP interleave.
