---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 02
type: tdd
wave: 2
depends_on: ["355-01"]
files_modified:
  - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PHRASE-CONFIRMATION.md
  - lib/core/direction-convention.cjs
  - tests/test-355-direction-convention.cjs
  - tests/fixtures/355/direction-pairs.json
  - tests/test-355-direction-agreement.cjs
autonomous: false
requirements: [HIPS-01]
tags: [direction-convention, pws-author-checkpoint, red-test, d-35]

must_haves:
  truths:
    - "D-35 step (2): the PWS author confirms (or replaces) the two direction phrases BEFORE any fixture room, citation pair, or direction fixture is authored; the confirmation, the date and the phrase hash are recorded in 355-PHRASE-CONFIRMATION.md and in the module's PHRASES_CONFIRMED export"
    - "AI-SPEC D1 / D-06: tests/fixtures/355/direction-pairs.json holds >= 30 (lsa, semantic) pairs (exact ties, near ties, both near 0, both near 1, missing values, out-of-range values, the real 272 HSI row) whose expected labels were computed from classify() after confirmation, never hand-typed"
    - "D-06 / D-47 / D-52 RED-first: tests/test-355-direction-agreement.cjs asserts identical labels from classify, rs-math, hsi-lsa, hsi-engine, scoreMeasured and the classifier's single-axis branch, a proof that the Python detector is off all four live callers, and a comment-stripped repo grep that finds the comparison-to-label rule ONLY in lib/core/direction-convention.cjs (with a planted negative control); at the end of this plan it FAILS on exactly the legs plans 355-09, 355-10 and 355-11 turn green"
  artifacts:
    - path: "tests/test-355-direction-agreement.cjs"
      provides: "The cross-producer agreement proof (HIPS-01, SPEC AC1 + AC2)"
      min_lines: 120
    - path: "tests/fixtures/355/direction-pairs.json"
      provides: ">= 30 direction pairs with module-computed expected labels"
      contains: "phrase_hash"
    - path: ".planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PHRASE-CONFIRMATION.md"
      provides: "The PWS author's ruling on the two phrases"
  key_links:
    - from: "tests/fixtures/355/direction-pairs.json"
      to: "lib/core/direction-convention.cjs"
      via: "phrase_hash equals phraseHash() at test time"
      pattern: "phrase_hash"
    - from: "tests/test-355-direction-agreement.cjs"
      to: "lib/core/rs-differential-scorer.cjs scoreMeasured"
      via: "opts.lexicalFn + opts.encodeFn seams"
      pattern: "scoreMeasured"
---

<objective>
Carry D-35 step (2): the PWS author confirms the two direction phrases ("same meaning in different words", "same words with different meaning") before anything is built on top of them. Then write the RED cross-producer agreement test (HIPS-01; SPEC AC1 and AC2) against a fixture whose expected labels are computed from the confirmed module, so plans 355-09 (hsi-lsa, hsi-engine, rs-math, classifier), 355-10 (scoreMeasured, D-47) and 355-11 (Python detector removal, D-52) each turn specific legs green.

Purpose: the repo's test-first rule (the Phase 354 rule): the failing test asserts the exact label every downstream consumer reads, before any producer flips.
Output: the confirmation record, the fixture, the RED agreement test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-ORIGIN-CONCEPT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-01-SUMMARY.md
@lib/core/direction-convention.cjs
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: The PWS author confirms the two direction phrases (D-35 step 2, AI-SPEC D17)</name>
  <files>(none; human ruling)</files>
  <read_first>
    - lib/core/direction-convention.cjs (DIRECTION_MEANING and NONE_MEANING as landed by 355-01)
    - 355-ORIGIN-CONCEPT.md section 3 (the deck text the phrases paraphrase)
  </read_first>
  <action>Present to the navigator, for relay to the PWS author: (1) the deck's own section 3 wording, quoted; (2) the two phrases exactly as shipped in DIRECTION_MEANING; (3) the none phrase "no wording signal measured" used for whitespace zones and find-connections bridges (D-49); (4) the one-line meaning of each wire id (structural_transfer = semantic similarity higher than lexical; semantic_implementation = lexical higher or equal). Ask for "confirmed" or replacement wording for any of the three phrases. Do not author any fixture before the answer.</action>
  <what-built>lib/core/direction-convention.cjs (plan 355-01) with three user-facing phrases</what-built>
  <how-to-verify>
    1. Read the two phrases and the none phrase printed by `node -e "const d=require('./lib/core/direction-convention.cjs');console.log(d.DIRECTION_MEANING, d.NONE_MEANING)"`.
    2. Compare each against the deck's section 3 meaning (same meaning in different words vs same words with different meaning).
    3. Answer "confirmed" or give the exact replacement wording.
  </how-to-verify>
  <verify><automated>node -e "const d=require('./lib/core/direction-convention.cjs');console.log(JSON.stringify(d.DIRECTION_MEANING), d.NONE_MEANING, d.phraseHash())"</automated></verify>
  <acceptance_criteria>
    - The resume signal carries either the word "confirmed" or explicit replacement wording, attributed to the PWS author by role
  </acceptance_criteria>
  <resume-signal>Type "confirmed" (PWS author approves all three phrases) or give replacement wording per phrase</resume-signal>
  <done>The PWS author's ruling on the phrases is in hand.</done>
</task>

<task type="auto">
  <name>Task 2: Record the ruling and stamp PHRASES_CONFIRMED into the module</name>
  <files>.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PHRASE-CONFIRMATION.md, lib/core/direction-convention.cjs, tests/test-355-direction-convention.cjs</files>
  <read_first>
    - lib/core/direction-convention.cjs (whole file)
    - tests/test-355-direction-convention.cjs (the phrase assertions)
    - Task 1's resume signal
  </read_first>
  <action>If the PWS author replaced any phrase, update DIRECTION_MEANING / NONE_MEANING to the exact replacement text and update the matching assertions in tests/test-355-direction-convention.cjs (cite "Phase 355-02, PWS author ruling <date>" in a comment). Then add the export `PHRASES_CONFIRMED` = Object.freeze({ by: 'pws-author', at: '<ISO date of the ruling>', phrase_hash: '<phraseHash() value after any edit>' }) and a test assertion that `PHRASES_CONFIRMED.phrase_hash === phraseHash()` (so any later phrase edit without a new ruling fails the test; D-35 "if the phrases change after judging, direction-ok labels are re-run"). Write 355-PHRASE-CONFIRMATION.md: who (role only, never a name), date, the three phrases verbatim, the phrase hash, the deck quote they paraphrase, and a line stating that any change to these phrases requires a new ruling and invalidates `direction_ok` labels.</action>
  <verify>
    <automated>node tests/test-355-direction-convention.cjs && node -e "const d=require('./lib/core/direction-convention.cjs');if(d.PHRASES_CONFIRMED.phrase_hash!==d.phraseHash())process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-355-direction-convention.cjs` exits 0 with the PHRASES_CONFIRMED equality leg present (`grep -c "PHRASES_CONFIRMED" tests/test-355-direction-convention.cjs` >= 1)
    - 355-PHRASE-CONFIRMATION.md contains the literal hash printed by `node -e "console.log(require('./lib/core/direction-convention.cjs').phraseHash())"`
    - `grep -ciE "pws author|pws-author" .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PHRASE-CONFIRMATION.md` >= 1 and the file names no person
  </acceptance_criteria>
  <done>The ruling is durable and machine-checked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: direction-pairs fixture and the RED cross-producer agreement test (SPEC AC1, AC2)</name>
  <files>tests/fixtures/355/direction-pairs.json, tests/test-355-direction-agreement.cjs</files>
  <read_first>
    - lib/core/rs-math.cjs lines 390-405 (classifyDirection(signedDiff))
    - lib/core/hsi-lsa.cjs lines 110-140 (classifyDirectionB and its exports)
    - lib/core/hsi-engine.cjs lines 250-290 and its module.exports (the :272 call site; which matrix function is exported)
    - lib/core/rs-differential-scorer.cjs lines 440-445 and 560-600 (scoreMeasured, opts.lexicalFn / opts.encodeFn seams)
    - lib/core/rs-innovation-classifier.cjs lines 1-20, 55-70, 140-160 (floors LSA_HIGH / BERT_HIGH 0.3 and the rule)
    - lib/core/intelligence-cascade.cjs lines 460-490, scripts/scout-cadence-runner.cjs around the `detect-reverse-salients` step, commands/scout.md around :273, skills/scout/SKILL.md around :264
    - 355-RESEARCH.md "Code Examples > Driving scoreMeasured to an exact (lexical, semantic) pair offline", "Pitfall 1", "Pitfall 4"
    - tests/fixtures/272/room/.hsi-results.json (one real row, lsa 0.2456 / semantic 0.4828)
    - tests/helpers/hygiene-355.cjs
  </read_first>
  <behavior>
    - fixture: >= 30 pairs; each `{ id, lsa, semantic, expected, classifier_single_axis, note }`; missing values encoded as JSON null, an absent key, or the string "NaN"; expected computed by classify() after Task 2
    - leg A (module): classify(pair) === expected for every pair
    - leg B (rs-math): for pairs with both values finite, rs-math.classifyDirection(semantic - lsa) === expected
    - leg C (hsi-lsa): classifyDirectionB(lsa, semantic) === expected (RED until 355-09)
    - leg D (hsi-engine): the engine's pair label for a crafted 2-artifact input equals expected (RED until 355-09)
    - leg E (scoreMeasured): r.direction === expected with lexicalFn -> lsa and encodeFn -> vecsFor(semantic), for pairs with semantic in [0, 1] (RED until 355-10)
    - leg F (classifier): for pairs flagged classifier_single_axis, the classifier's label === expected (RED until 355-09)
    - leg G (Python off): no invocation of detect-reverse-salients.py remains in the four live callers (RED until 355-11)
    - leg H (one rule): comment-stripped scan of lib/**/*.cjs and scripts/*.cjs finds comparison-to-label code only in lib/core/direction-convention.cjs; a planted scratch rule file under lib/core/ is caught, then removed (RED until 355-09 and 355-10)
    - fixture guard: fixture.phrase_hash === phraseHash()
  </behavior>
  <action>
FIXTURE: write tests/fixtures/355/direction-pairs.json as `{ "_source": "expected labels computed by lib/core/direction-convention.cjs classify() after the PWS author's phrase ruling (355-PHRASE-CONFIRMATION.md); never hand-edited", "phrase_hash": "<phraseHash()>", "generated_at": "<ISO>", "pairs": [...] }` with at least 30 pairs covering: 6 clear meaning bridges (lsa low, semantic high), 6 clear term collisions (lsa high, semantic low), 3 exact ties (0.4/0.4, 0/0, 1/1), 4 near ties (difference +-0.001 and +-0.0001), 2 both-near-zero, 2 both-near-one, 3 missing (null lsa, absent semantic key, "NaN"), 2 out-of-range finite (1.2/0.3, -0.2/0.1; expected by the two-way rule), the real 272 HSI row (id `real-272-hsi-1`), and at least 6 pairs flagged `classifier_single_axis: true` where exactly one of lsa / semantic is >= 0.3 and the other < 0.3. Generate expected labels with a one-off node command that requires the module; do not type them.

RED TEST: write tests/test-355-direction-agreement.cjs (hygiene-355 preamble; `makeChecker`) implementing legs A-H exactly as the behavior list states, each check label prefixed with its leg letter and, for RED legs, the plan that turns it green (for example `E scoreMeasured (turns green in 355-10)`). Leg D: drive the hsi-engine function that the :272 call site sits in with a crafted 2-artifact LSA matrix and semantic matrix whose single off-diagonal cells equal the pair's values; if no such function is exported, leg D fails with the message `hsi-engine seam missing: export the pair-label path (355-09)`. Leg E: `vecsFor = (c) => [[1,0],[c, Math.sqrt(Math.max(0, 1 - c*c))]]`, `await scorer.scoreMeasured('probe a','probe b',{ lexicalFn: () => lsa, encodeFn: () => vecsFor(semantic) })`. Leg G: regex `/python3?\s+[^\n]*detect-reverse-salients\.py/` over the two .md files and a non-comment-line `detect-reverse-salients` search over the two .cjs files, emitting ONE check per caller file labelled `G python off: <repo-relative path>` (four checks). Leg H patterns: (1) a ternary holding both wire-id literals `/['"](structural_transfer|semantic_implementation)['"]\s*:\s*['"](structural_transfer|semantic_implementation)['"]/`; (2) a numeric comparison and a wire-id literal on one line `/[<>]=?[^=].*['"](structural_transfer|semantic_implementation)['"]/`; (3) `/return\s+['"](structural_transfer|semantic_implementation)['"]/`; hits are allowed only in lib/core/direction-convention.cjs plus an explicit `ALLOWED_HITS` array (empty now; any entry needs a one-line reason); print the scanned-file count and fail if it is 0; negative control writes `lib/core/__scratch_direction_rule_355.cjs` containing `const x = d > 0 ? 'structural_transfer' : 'semantic_implementation';`, asserts it is caught, deletes it in a finally block. End with `attempts() === 0`.
Run it. It must FAIL, and the failing check labels must be exactly within legs C, D, E, F, G, H (legs A and B pass). Commit `test(355-02): add failing cross-producer direction agreement test (HIPS-01)`.
  </action>
  <verify>
    <automated>node -e "const f=require('./tests/fixtures/355/direction-pairs.json');const d=require('./lib/core/direction-convention.cjs');if(f.pairs.length<30||f.phrase_hash!==d.phraseHash())process.exit(1)" && (node tests/test-355-direction-agreement.cjs; test $? -ne 0)</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./tests/fixtures/355/direction-pairs.json').pairs.length)"` prints a number >= 30
    - `node tests/test-355-direction-agreement.cjs` exits non-zero, and every `FAIL:` line it prints starts with `C `, `D `, `E `, `F `, `G ` or `H ` (no A or B failure)
    - The output contains a line reporting the scanned-file count greater than 0 and a PASS line for the negative control
    - `ls lib/core/__scratch_direction_rule_355.cjs` fails after the run (scratch removed)
    - `grep -c "turns green in 355-" tests/test-355-direction-agreement.cjs` >= 5
  </acceptance_criteria>
  <done>A failing test states exactly what each producer flip must deliver.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWS author ruling -> shipped phrases | Human wording becomes the rendered meaning of every finding |
| test scratch file -> lib/core tree | The negative control writes into the real tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-06 | Tampering | DIRECTION_MEANING edited after judging | mitigate | PHRASES_CONFIRMED.phrase_hash pinned by test; label CLI (355-03) records phrase_module_hash and refuses resume on mismatch |
| T-355-07 | Repudiation | who confirmed the phrases | mitigate | 355-PHRASE-CONFIRMATION.md records role, date, hash; no personal name (repo naming rule) |
| T-355-08 | Tampering | scratch rule file left in lib/core | mitigate | Deleted in a finally block; acceptance asserts it is gone |
| T-355-09 | Spoofing | hand-typed expected labels | mitigate | Expected labels generated by requiring the module; fixture carries phrase_hash checked at test time |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Export `PHRASES_CONFIRMED` `{ by, at, phrase_hash }` in `lib/core/direction-convention.cjs`
- Record `.planning/phases/355-.../355-PHRASE-CONFIRMATION.md`
- Fixture `tests/fixtures/355/direction-pairs.json` (`_source`, `phrase_hash`, `generated_at`, `pairs[]` of `{id, lsa, semantic, expected, classifier_single_axis, note}`)
- Test `tests/test-355-direction-agreement.cjs` (legs A-H, `ALLOWED_HITS` array, scratch control path `lib/core/__scratch_direction_rule_355.cjs`)

<verification>
- `node tests/test-355-direction-convention.cjs` exits 0
- `node tests/test-355-direction-agreement.cjs` fails only on legs C-H
</verification>

<success_criteria>
- The PWS author's ruling is recorded before any fixture authoring (D-35)
- The agreement test is committed RED with named hand-offs to 355-09, 355-10, 355-11
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-02-SUMMARY.md` when done (`git add -f` for phase-dir files).
</output>
