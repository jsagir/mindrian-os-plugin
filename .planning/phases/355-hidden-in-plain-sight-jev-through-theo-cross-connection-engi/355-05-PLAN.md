---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 05
type: tdd
wave: 2
depends_on: ["355-01"]
files_modified:
  - tests/test-355-naming-honesty.cjs
  - lib/mcp/tool-router.cjs
  - lib/mcp/tools/sensors.cjs
  - data/mcp-tool-connectors.json
  - data/connector-registry.json
autonomous: true
requirements: [HIPS-03]
tags: [naming-honesty, mcp, cirs, d-57-file]

must_haves:
  truths:
    - "D-23: scout-hsi keeps its name and its place in UNIMPLEMENTED_MUTATING_ORCHESTRATION; its description sentence and its NOT EXECUTED banner say 'reference only, no compute; run /mos:scout hsi in Claude Code'; it is not wired to hsiEngine.runTier1"
    - "D-24: MCP whitespace_scan keeps its name; its description leads with 'Returns open questions and unsupported claims. This is NOT the /mos:whitespace HSI engine; run it in Claude Code'"
    - "D-25: the gate is a fixture test, not the advisory scanner: scout-hsi returns the banner and writes no .hsi-results.json in a tmp room; whitespace_scan's description names open questions and unsupported claims and never claims to be the HSI or whitespace engine; check-tool-honesty --report lists no finding for either surface"
    - "D-26: no rename (the registry-drift gate compares only slash commands and would pass a silent MCP rename); a test asserts both MCP names are unchanged"
    - "D-57: lib/mcp/tool-router.cjs is re-read at execution time from 355-BASELINE.md's post-354 anchors, never from the research's line numbers"
    - "SPEC Req 3 / CIRS Part 11: build-connector-registry --check, build-orchestration-projection --check and check-shape-declaration --check exit 0 after the edit"
  artifacts:
    - path: "tests/test-355-naming-honesty.cjs"
      provides: "The D-25 fixture gate for scout-hsi and whitespace_scan"
      min_lines: 60
  key_links:
    - from: "tests/test-355-naming-honesty.cjs"
      to: "lib/mcp/tool-router.cjs orchestration handler"
      via: "direct handler call against a tmp room"
      pattern: "scout-hsi"
    - from: "tests/test-355-naming-honesty.cjs"
      to: "lib/mcp/tools/sensors.cjs whitespace_scan description"
      via: "registered description string"
      pattern: "whitespace_scan"
---

<objective>
SPEC Req 3 (HIPS-03): every intelligence surface says what it does. `scout-hsi` is reference text under a compute-shaped name; MCP `whitespace_scan` is open questions plus unsupported claims under a whitespace-engine-shaped description. Neither is renamed (D-23, D-24, D-26); both descriptions become true, and a fixture test (D-25) is the real gate because `check-tool-honesty.cjs` only checks write claims and runs advisory.

D-57: `lib/mcp/tool-router.cjs` is a shared file edited by pending 354 plans; this plan starts from 355-BASELINE.md's post-354 anchors and re-greps before editing.
Output: `tests/test-355-naming-honesty.cjs`, edited descriptions, gates green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-BASELINE.md
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED fixture gate for the two misleading surfaces (D-25, D-26)</name>
  <files>tests/test-355-naming-honesty.cjs</files>
  <read_first>
    - 355-BASELINE.md "D-57 file anchors (post-354)" block for tool-router.cjs (then re-run `grep -n "scout-hsi\|UNIMPLEMENTED_MUTATING_ORCHESTRATION\|NOT EXECUTED" lib/mcp/tool-router.cjs` yourself; the file may have moved since)
    - lib/mcp/tool-router.cjs: the UNIMPLEMENTED_MUTATING_ORCHESTRATION set, the NOT EXECUTED banner builder, the `orchestration` tool's description sentence naming scout-hsi, and the exported handler used by existing orchestration tests (`grep -rln "UNIMPLEMENTED_MUTATING_ORCHESTRATION\|scout-hsi" tests | head`)
    - lib/mcp/tools/sensors.cjs lines 285-330 (whitespace_scan registration and handler)
    - scripts/check-tool-honesty.cjs lines 1090-1115 and its --report output format
    - tests/helpers/fixture-room-354.cjs (makeScratchRoom for the tmp room)
  </read_first>
  <behavior>
    - calling the orchestration handler with command 'scout-hsi' on a tmp room returns text containing 'NOT EXECUTED' and 'reference only, no compute' and '/mos:scout hsi', and afterwards no `.hsi-results.json` exists anywhere under the tmp room
    - 'scout-hsi' is still a member of UNIMPLEMENTED_MUTATING_ORCHESTRATION (D-23: no rename, no wiring)
    - the orchestration tool's description sentence that names scout-hsi contains 'reference only, no compute'
    - whitespace_scan is still registered under that exact name (D-26)
    - whitespace_scan's description starts with 'Returns open questions and unsupported claims.' and contains 'NOT the `/mos:whitespace` HSI engine'
    - neither description matches `/\b(computes?|runs?|scans?)\b[^.]{0,40}\b(HSI|whitespace)\b[^.]{0,20}\bengine\b/i` outside the NOT sentence
    - `node scripts/check-tool-honesty.cjs --report` output has no finding line naming scout-hsi or whitespace_scan
  </behavior>
  <action>Write tests/test-355-naming-honesty.cjs (hygiene-355 preamble, makeChecker) implementing the behavior list. Require the tool-router and sensors modules the way the existing orchestration and sensor-tool tests do (reuse their registration or handler-export path; do not invent a new export if one exists). For the description checks read the registered description strings (from the tool definition objects the modules export or register), not from source text. For check-tool-honesty use `spawnSync(process.execPath, ['scripts/check-tool-honesty.cjs','--report'])` and scan stdout. Run it; the description assertions fail today. Commit `test(355-05): add failing naming-honesty gate (D-25)`.</action>
  <verify>
    <automated>node tests/test-355-naming-honesty.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The test exits non-zero before Task 2, and its FAIL lines are only the description / banner wording checks (the no-rename and no-write checks already PASS)
    - `grep -c "hsi-results.json" tests/test-355-naming-honesty.cjs` >= 1
  </acceptance_criteria>
  <done>A failing test pins what each surface must say.</done>
</task>

<task type="auto">
  <name>Task 2: Make the two descriptions true, keep both names, keep CIRS gates green (D-23, D-24, D-26)</name>
  <files>lib/mcp/tool-router.cjs, lib/mcp/tools/sensors.cjs, data/mcp-tool-connectors.json, data/connector-registry.json</files>
  <read_first>
    - lib/mcp/tool-router.cjs (the three sites found in Task 1, re-grepped now)
    - lib/mcp/tools/sensors.cjs lines 285-330
    - data/mcp-tool-connectors.json (the whitespace_scan row's hitl_why; `grep -n "whitespace_scan" data/mcp-tool-connectors.json`)
    - Theo mirror check, READ ONLY: `grep -rn "scout-hsi\|whitespace_scan" ~/Theo/src | head` (never edit Theo; M-T protocol)
  </read_first>
  <action>
tool-router.cjs: in the orchestration tool's description, replace the scout-hsi sentence with one stating "scout-hsi: reference only, no compute; run `/mos:scout hsi` in Claude Code" (cite "Phase 355 D-23" in an adjacent code comment); make the NOT EXECUTED banner text for scout-hsi carry the same words. Leave UNIMPLEMENTED_MUTATING_ORCHESTRATION membership, the command id and every other description byte-unchanged. Do not wire scout-hsi to hsiEngine.runTier1 (D-23: it would cover one of three pipeline steps, add a write path and a model download on Desktop/Cowork).
sensors.cjs: make the whitespace_scan description lead with exactly "Returns open questions and unsupported claims. This is NOT the `/mos:whitespace` HSI engine; run it in Claude Code." followed by the existing accurate remainder (drop any clause that claims gap detection or the whitespace engine). Keep the tool name and handler unchanged.
mcp-tool-connectors.json: only if the whitespace_scan row's hitl_why claims the whitespace/HSI engine, rewrite that string truthfully; then regenerate with `node scripts/build-connector-registry.cjs` (never hand-edit data/connector-registry.json). If hitl_why is already accurate, touch neither data file.
If the Theo read shows Theo mirrors either description, record it in the SUMMARY as an item for the 355-27 outbound note to Theo; never edit Theo.
Run the gates: `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-shape-declaration.cjs --check`, `node scripts/check-render-coverage.cjs --check`.
Commit `fix(355-05): scout-hsi and whitespace_scan describe what they do (HIPS-03, D-23, D-24)`.
  </action>
  <verify>
    <automated>node tests/test-355-naming-honesty.cjs && node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check && node scripts/check-shape-declaration.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-355-naming-honesty.cjs` exits 0 with `FAIL: 0`
    - `grep -c "reference only, no compute" lib/mcp/tool-router.cjs` >= 2 (description + banner)
    - `grep -c "Returns open questions and unsupported claims. This is NOT the" lib/mcp/tools/sensors.cjs` = 1
    - `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-shape-declaration.cjs --check` and `node scripts/check-render-coverage.cjs --check` each exit 0
    - `git diff --stat` shows no change to data/command-registry.json (no rename, D-26)
  </acceptance_criteria>
  <done>Both surfaces tell a Desktop or Cowork user what they actually return, and the born-wired gates stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool description -> host model / user | Desktop and Cowork read these strings to decide what to call |
| shared file tool-router.cjs -> peer phases | Edited by 354 plans and by 355-12 later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-21 | Spoofing | compute-shaped names that do not compute | mitigate | Descriptions and banner state the truth; fixture test is the gate (D-25) |
| T-355-22 | Denial of service | silent MCP rename breaking Desktop/Cowork | mitigate | No rename (D-26); test asserts both names still registered |
| T-355-23 | Tampering | editing tool-router.cjs over a peer's uncommitted diff | mitigate | Re-grep from 355-BASELINE.md; `git status --short lib/mcp/tool-router.cjs` empty before edit (D-57 gate already proved 354 closed) |
| T-355-24 | Tampering | hand-edited connector registry | mitigate | Regenerate only via build-connector-registry.cjs; --check gate |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Test `tests/test-355-naming-honesty.cjs`
- Description text "scout-hsi: reference only, no compute; run `/mos:scout hsi` in Claude Code" (tool-router orchestration description + NOT EXECUTED banner)
- Description text "Returns open questions and unsupported claims. This is NOT the `/mos:whitespace` HSI engine; run it in Claude Code." (sensors.cjs whitespace_scan)

<verification>
- `node tests/test-355-naming-honesty.cjs` exits 0
- Connector, projection, shape and render-coverage gates exit 0
</verification>

<success_criteria>
- scout-hsi and whitespace_scan describe their real behavior; names unchanged; CIRS gates green
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-05-SUMMARY.md` when done.
</output>
