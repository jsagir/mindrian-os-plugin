---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 23
type: tdd
wave: 7
depends_on: ["355-04", "355-14", "355-16", "355-17", "355-18", "355-22"]
files_modified:
  - skills/larry-personality/SKILL.md
  - tests/fixtures/355/prose/desktop-verified.md
  - tests/fixtures/355/prose/desktop-unverified.md
  - tests/fixtures/355/prose/desktop-unchecked.md
  - tests/fixtures/355/prose/cowork-verified.md
  - tests/test-355-no-decimal.cjs
  - tests/test-355-tri-polar.cjs
  - tests/test-355-stamp-coverage.cjs
  - tests/test-355-floor-sweep.cjs
autonomous: true
requirements: [HIPS-05, HIPS-04, HIPS-02]
tags: [render-gate, no-decimal, tri-polar, larry, stamp-coverage, floor-sweep]

must_haves:
  truths:
    - "D-29 / D-41 / D-50: skills/larry-personality/SKILL.md gains the stamp phrasing rule for Desktop / Cowork and reach narration: open with the De Stijl glyph, hedge ('might be one argument'), state the direction phrase, reproduce formatStampLines(stamp, 'desktop') sentences verbatim ('Checked: strong. The methodology graph links them in two steps: ...', 'Nobody has judged the citation yet; this is the path alone.'), never a number or percentage, narrate only stamps a CLI run stored, and otherwise say exactly 'Not yet checked; run the CLI to verify.'; end with the gate question"
    - "D-30 / SPEC AC4: tests/test-355-no-decimal.cjs runs the D-30 regex `(^|[^0-9A-Za-z.])(0?\\.[0-9]+|1\\.0+)([^0-9.]|$)` plus a `[0-9]+%` check on stamp and evidence lines over: captured stdout of all five producers' render seams (Theo up via replay and Theo down via null), the rendered F.1 zones.body, the reach card text and every prose fixture; a planted 0.87 is caught on each surface; the F.1 brain-chip `<conf>%` is out of scope and only reported"
    - "AI-SPEC D12: the same test fails on 'breakthrough', 'convergent', 'validated' or 'proven' attached to a finding in any template, render or prose fixture"
    - "AI-SPEC D13 / Tri-Polar: tests/test-355-tri-polar.cjs proves that for the same fixture finding the tier word, the path hop by hop, the backend, the direction phrase and the judge disclosure appear verbatim in the CLI block, the Desktop / Cowork prose fixture and the reach card"
    - "SPEC AC5 / AC7 / AI-SPEC D4 / D5 / D7: tests/test-355-stamp-coverage.cjs aggregates all five producers: findings produced == rendered == Stamp.parse ok (100%, no rounding); every stamp carries verification, backend, direction and judge 'none' and every render says 'judge  none, path check only'; replaying the REAL captured Theo file through stamp-connections, every verified stamp's path is byte-equal to the recorded shortest path (non-Framework nodes as [Label]) and its tier equals the tier recomputed from the recorded edges"
    - "D-22: tests/test-355-floor-sweep.cjs gains leg 4b: for every disclosed ledger row, each dependent producer's captured render contains disclosureLine(producer) and contains none of the row's values as a bare decimal"
  artifacts:
    - path: "tests/test-355-no-decimal.cjs"
      provides: "SPEC AC4 render guard across five producers, F.1, card and prose"
      min_lines: 120
    - path: "tests/test-355-tri-polar.cjs"
      provides: "AI-SPEC D13 same-stamp-everywhere proof"
    - path: "tests/test-355-stamp-coverage.cjs"
      provides: "SPEC AC5 / AC7 aggregate coverage and byte-true replay"
    - path: "tests/fixtures/355/prose/desktop-verified.md"
      provides: "Larry Desktop prose fixture with its stamp in frontmatter"
  key_links:
    - from: "tests/fixtures/355/prose/*.md"
      to: "lib/core/verification-stamp-format.cjs formatStampLines(stamp, 'desktop')"
      via: "fixture frontmatter holds the stamp; body must contain the formatter's sentences verbatim"
      pattern: "desktop"
    - from: "tests/test-355-stamp-coverage.cjs"
      to: "tests/fixtures/355-theo-find-connections-responses.json"
      via: "makeReplayCallTool over the real capture"
      pattern: "355-theo-find-connections-responses"
---

<objective>
The render gate for the whole phase (HIPS-05, HIPS-04, HIPS-02): write Larry's Desktop / Cowork stamp rule, give it recorded prose fixtures, and prove across all five producers and all three surfaces that every shown finding carries the same honest stamp and that no score, similarity or percentage reaches a user (SPEC AC4, AC5, AC7; AI-SPEC D4, D5, D7, D8, D12, D13; D-22's render leg).

Output: the SKILL rule, four prose fixtures, three new tests, one extended sweep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-AI-SPEC.md
@lib/core/verification-stamp-format.cjs
@lib/core/floor-disclosure.cjs
@tests/helpers/theo-replay-355.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Larry's stamp phrasing rule and the recorded prose fixtures (D-29, D-41, D-50)</name>
  <files>skills/larry-personality/SKILL.md, tests/fixtures/355/prose/desktop-verified.md, tests/fixtures/355/prose/desktop-unverified.md, tests/fixtures/355/prose/desktop-unchecked.md, tests/fixtures/355/prose/cowork-verified.md</files>
  <read_first>
    - skills/larry-personality/SKILL.md (whole file: voice rules "Hedged always", no numbers, De Stijl glyph opening, the reach narration section)
    - 355-CONTEXT.md `<specifics>` (the Desktop prose shape) and D-41, D-50
    - lib/core/verification-stamp-format.cjs formatStampLines(stamp, 'desktop') and formatUncheckedDesktop()
    - pre-commit legs for skills/*/SKILL.md (connector registry, orchestration projection, shape declaration)
  </read_first>
  <action>Add a section `## Cross-connection stamps (Phase 355)` to skills/larry-personality/SKILL.md: when narrating a finding that carries a stored stamp (opportunity node props, a report's stamp, the reach evidence), open with the De Stijl glyph for the move, hedge, state the direction phrase, reproduce the Desktop stamp sentences verbatim (quote the formatter's exact templates with placeholders), never add a score, similarity, differential or percentage, never say a judge or model checked it, never call a finding a breakthrough / validated / proven, and end with the gate question ("Want the first one filed as a proposed opportunity?"); when no stored stamp exists (Desktop and Cowork cannot compute one this phase, D-50) say exactly "Not yet checked; run the CLI to verify."; the SENS-13 reach narration mirrors the reach card line. Hyphens only. Author the four prose fixtures: each has YAML frontmatter `surface:` (desktop | cowork), `stamp:` (a JSON-serializable stamp object built from the stub fixture: verified strong for two fixtures, unverified co_sourced_only for one, null for desktop-unchecked) and `finding:` (the two names); the body is a short Larry turn written to the new rule (verbatim formatter sentences inside hedged prose). Run the SKILL gates. Commit `docs(355-23): Larry narrates stored stamps verbatim, never numbers (HIPS-05, D-41, D-50)`.</action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check && node scripts/check-shape-declaration.cjs --check && grep -c "## Cross-connection stamps (Phase 355)" skills/larry-personality/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Gates exit 0; the section heading appears once
    - `grep -c "Not yet checked; run the CLI to verify." skills/larry-personality/SKILL.md tests/fixtures/355/prose/desktop-unchecked.md` shows >= 1 in each
    - `ls tests/fixtures/355/prose/*.md | wc -l` prints 4
  </acceptance_criteria>
  <done>Larry has one rule for stamps on hookless surfaces, and four fixtures to test it against.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: No-decimal / banned-claim render guard and the Tri-Polar same-stamp test (AC4, D12, D13)</name>
  <files>tests/test-355-no-decimal.cjs, tests/test-355-tri-polar.cjs</files>
  <read_first>
    - the render seams: scripts/whitespace-command.cjs (renderScanLines, renderAnalyzeLines, renderNoveltyLines), scripts/hsi-to-graph.cjs (renderHsiFindings), lib/agents/reverse-salient-agent.cjs (renderBottleneckFinding), scripts/stamp-connections.cjs (main with --pair), scripts/eureka-portfolio-report.cjs Markdown writer + lib/core/eureka/report-html.cjs, lib/core/eureka/qualify-opportunity.cjs F.1 render, lib/hmi/dial-label-composer.cjs composeLabel
    - the four producer fixtures under tests/fixtures/355/producers/ and tests/fixtures/355/prose/
    - skills/ui-system/SKILL.md :149 (the F.1 brain-chip `<conf>%`, out of scope)
  </read_first>
  <behavior>
    - for each of five producers x {Theo up (replay), Theo down (null)}: captured lines contain no D-30 decimal; stamp and evidence lines contain no `[0-9]+%`; planting '0.87' into one input title per surface makes that surface's check fail inside a sub-assertion (negative control reported as PASS when caught)
    - F.1 zones.body for a stamped candidate, the reach card text for a verified and an unverified stamp, and all four prose fixtures pass the same checks
    - no template, render or prose fixture contains breakthrough, convergent, validated or proven (case-insensitive, word boundary) next to a finding; the internal band enum never renders
    - tri-polar: for the verified prose fixture's stamp, the CLI block (formatStampLines 'cli'), the prose body and the card text all contain the tier word, formatPathText(nodes, edges), the direction phrase and the judge disclosure ('judge  none, path check only' on CLI, 'Nobody has judged the citation yet' in prose); the backend appears on CLI and is implied in prose ('The methodology graph'); for the unverified fixture the CLI advice line and the card's unverified sentence both appear
    - any F.1 brain-chip `<conf>%` occurrence is printed as an out-of-scope note, never a failure
  </behavior>
  <action>Write tests/test-355-no-decimal.cjs and tests/test-355-tri-polar.cjs (hygiene-355 preamble each; mkdtemp for anything that writes) per the behavior list, driving only the exported render seams and fixtures (no encoders, no network; Pitfall 11). Run both; fix any real leak they find in the seam that owns it only if that file is already in this phase (otherwise record it as a finding in the SUMMARY and stop for a decision). Commit `test(355-23): render guard - no decimals, no banned claims, same stamp on every surface (HIPS-05, D-30, D-12, D-13)`.</action>
  <verify>
    <automated>node tests/test-355-no-decimal.cjs && node tests/test-355-tri-polar.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both tests exit 0 with `FAIL: 0` and one caught negative control per surface (>= 8 PASS lines containing "negative control")
    - `grep -c "brain-chip" tests/test-355-no-decimal.cjs` >= 1 (the out-of-scope note exists)
  </acceptance_criteria>
  <done>No number and no over-claim reaches a user on any surface; the stamp reads the same everywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Aggregate stamp coverage with byte-true replay, and the floor sweep's render leg (AC5, AC7, D-22)</name>
  <files>tests/test-355-stamp-coverage.cjs, tests/test-355-floor-sweep.cjs</files>
  <read_first>
    - tests/fixtures/355-theo-find-connections-responses.json (the real capture from 355-14) and scripts/capture-355-theo-responses.cjs validateCapture
    - lib/core/verification-stamp.cjs (Stamp, choosePath, tierFromHops, LATERAL_EDGE_TYPES) and scripts/stamp-connections.cjs
    - tests/test-355-floor-sweep.cjs (existing legs) and data/floor-ledger.json dependent_outputs
  </read_first>
  <behavior>
    - per producer (eureka, find-connections, find-bottlenecks, hsi, whitespace), over its fixture: produced findings == rendered stamp blocks == stamps passing Stamp.parse; the ratio is asserted as integer equality, never a rounded percentage
    - every stamp has judge 'none'; every rendered block ends with 'judge  none, path check only'; no lib/ or hooks/ file references jev-question-ceilings or the citation question (AC7)
    - replaying the REAL capture through stamp-connections for every captured pair: each verified stamp's path.nodes equals the recorded chosen path with non-Framework nodes as '[' + label + ']', its path.edges equal the recorded edges, and its tier equals tierFromHops(recorded edges length); no stamp carries a path that is not in the capture; unverified stamps carry no path
    - floor sweep leg 4b: for every disclosed ledger row and each producer in its dependent_outputs, that producer's captured render (from the same seams) contains disclosureLine(producer) and contains none of the row's numeric values as a bare decimal token
  </behavior>
  <action>Write tests/test-355-stamp-coverage.cjs (hygiene-355 preamble) per the behavior list; the capture replay leg exits 77 with a loud message only if the capture file is absent (it exists after 355-14). Add leg 4b to tests/test-355-floor-sweep.cjs with a comment "Phase 355-23: D-22 dependent-output render leg". Run both and the full `bash tests/run-all-355.sh`. Commit `test(355-23): aggregate stamp coverage with byte-true Theo replay; floor render leg (HIPS-04, HIPS-02, D-22)`.</action>
  <verify>
    <automated>node tests/test-355-stamp-coverage.cjs && node tests/test-355-floor-sweep.cjs && bash tests/run-all-355.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both tests exit 0 with `FAIL: 0`; `bash tests/run-all-355.sh` exits 0
    - `grep -c "Phase 355-23" tests/test-355-floor-sweep.cjs` >= 1
  </acceptance_criteria>
  <done>Coverage is 100% by integer equality, and every verified path is byte-true to what Theo returned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host-generated Larry prose -> user | Plugin code cannot filter it at runtime |
| rendered surfaces -> user trust | The final place a number or over-claim could leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-112 | Spoofing | Larry implying a judge ran or adding a number | mitigate | SKILL rule with verbatim formatter sentences; prose fixtures checked offline (the stated limit: runtime prose is host-generated) |
| T-355-113 | Spoofing | a fabricated path in any render | mitigate | Byte-true replay against the real capture; unverified forbids a path |
| T-355-114 | Repudiation | coverage rounded to look complete | mitigate | Integer equality of produced / rendered / parsed counts |
| T-355-115 | Tampering | a new decimal leak added later | mitigate | Five-producer, three-surface D-30 guard with per-surface negative controls in run-all-355 |
</threat_model>

## Artifacts this phase produces

This plan's share:
- SKILL section `## Cross-connection stamps (Phase 355)` in `skills/larry-personality/SKILL.md`
- Prose fixtures `tests/fixtures/355/prose/desktop-verified.md`, `desktop-unverified.md`, `desktop-unchecked.md`, `cowork-verified.md` (frontmatter keys `surface`, `stamp`, `finding`)
- Tests `tests/test-355-no-decimal.cjs`, `tests/test-355-tri-polar.cjs`, `tests/test-355-stamp-coverage.cjs`; leg 4b in `tests/test-355-floor-sweep.cjs`

<verification>
- `bash tests/run-all-355.sh` exits 0
</verification>

<success_criteria>
- SPEC AC4, AC5, AC7 hold across five producers and three surfaces
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-23-SUMMARY.md` when done.
</output>
