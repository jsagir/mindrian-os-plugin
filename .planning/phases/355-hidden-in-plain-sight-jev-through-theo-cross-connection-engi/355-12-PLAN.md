---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 12
type: tdd
wave: 3
depends_on: ["355-02", "355-05"]
files_modified:
  - tests/test-355-direction-readers.cjs
  - lib/core/eureka-critic.cjs
  - lib/core/eureka/eureka-offer.cjs
  - lib/core/grill-engine.cjs
  - lib/mcp/tool-router.cjs
  - scripts/hsi-to-graph.cjs
  - lib/core/nl-graph-queries.cjs
  - lib/chat/fabric-chat.cjs
  - scripts/generate-chat-embed.cjs
autonomous: true
requirements: [HIPS-01]
tags: [direction-convention, readers, enums, stored-labels, d-57-file]

must_haves:
  truths:
    - "D-07: the duplicate direction enums in eureka-critic.cjs, eureka-offer.cjs and grill-engine.cjs import DIRECTIONS from lib/core/direction-convention.cjs; the eureka_critic zod enum in lib/mcp/tool-router.cjs becomes z.enum(DIRECTIONS) and its schema_version stays 1 (the remaining two duplicates, eureka-reach-runner.cjs and sensor-eureka.cjs, move in 355-19 with the schema bump)"
    - "D-07: stored labels are never trusted: scripts/hsi-to-graph.cjs writes HSI_CONNECTION surprise_type as classify(lsa_sim, semantic_sim), ignoring the string in .hsi-results.json (which compute-hsi.py still writes under the retired convention); lib/core/nl-graph-queries.cjs re-derives surprise_type from lsa_sim / semantic_sim in JS and returns innovation_type only for REVERSE_SALIENT edges whose source is 'rs-engine'"
    - "D-07: LLM-facing schema descriptions that define surprise_type's meaning (lib/chat/fabric-chat.cjs, scripts/generate-chat-embed.cjs) state the module's meaning and that pre-355 stored strings may carry the retired convention"
    - "D-01: no wire id renamed; data/*.json gains no direction enum"
    - "D-57: lib/mcp/tool-router.cjs is re-read from 355-BASELINE.md anchors plus a fresh grep; 355-05 edited it one wave earlier"
    - "Leg H of tests/test-355-direction-agreement.cjs gains no new hits (the re-derivation calls classify, never a second comparison)"
  artifacts:
    - path: "tests/test-355-direction-readers.cjs"
      provides: "Enum identity, write-time and read-time re-derivation proofs"
      min_lines: 80
  key_links:
    - from: "scripts/hsi-to-graph.cjs"
      to: "lib/core/direction-convention.cjs classify"
      via: "surprise_type = classify(pair.lsa_sim, pair.semantic_sim) at the HSI_CONNECTION write"
      pattern: "classify\\("
    - from: "lib/mcp/tool-router.cjs eureka_critic schema"
      to: "lib/core/direction-convention.cjs DIRECTIONS"
      via: "z.enum(DIRECTIONS)"
      pattern: "z\\.enum\\(DIRECTIONS\\)"
---

<objective>
HIPS-01 readers and duplicates (D-07): after the producer flips, three places still hard-code the two ids, one zod schema pins them by hand, and several readers trust direction strings stored in rooms under the retired convention. Point every duplicate at the one module and make every reader re-derive the label from the stored similarity pair.

D-57: `lib/mcp/tool-router.cjs` is a shared file; re-read it first.
Output: one new test, seven small source edits, two schema-description edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-BASELINE.md
@lib/core/direction-convention.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Duplicate enums and the eureka_critic zod enum take DIRECTIONS (D-07)</name>
  <files>tests/test-355-direction-readers.cjs, lib/core/eureka-critic.cjs, lib/core/eureka/eureka-offer.cjs, lib/core/grill-engine.cjs, lib/mcp/tool-router.cjs</files>
  <read_first>
    - lib/core/eureka-critic.cjs lines 515-530 and 660-690 (SURPRISE_TYPES and the `|| 'structural_transfer'` default near :676)
    - lib/core/eureka/eureka-offer.cjs lines 55-70
    - lib/core/grill-engine.cjs lines 55-75 (EUREKA_SURPRISE_TYPES)
    - lib/mcp/tool-router.cjs: re-grep `grep -n "z.enum(\['structural_transfer'\|eureka_critic\|schema_version" lib/mcp/tool-router.cjs` (research put the enum near :2211)
    - tests/test-212-critic-rubric.cjs, tests/test-213-eureka-offer.cjs, tests/test-212-part8-boundary.cjs (must stay green)
  </read_first>
  <behavior>
    - eureka-critic SURPRISE_TYPES, eureka-offer's direction list and grill-engine EUREKA_SURPRISE_TYPES each deep-equal DIRECTIONS and are sourced from the module (static leg: each file requires direction-convention and has no two-literal array of the ids in non-comment code)
    - the tool-router eureka_critic input schema's direction field accepts exactly DIRECTIONS (safeParse of 'structural_transfer' and 'semantic_implementation' succeeds; 'none' and 'meaning_bridge' fail) and its schema_version literal is still 1
    - static exception list: the only remaining non-comment two-literal id arrays under lib/ are in lib/core/eureka/eureka-reach-runner.cjs and lib/core/sensors/sensor-eureka.cjs, each tagged `(moves in 355-19)`
  </behavior>
  <action>RED: write tests/test-355-direction-readers.cjs (hygiene-355 preamble) with the enum legs above; run; fail. GREEN: in each of the three lib files replace the local frozen array with the module's DIRECTIONS (keep the local constant NAME as an alias so every in-file reference still works, e.g. `const SURPRISE_TYPES = DIRECTIONS;`), cite Phase 355 D-07. eureka-critic.cjs near :676: read the code path; if the candidate carries a similarity pair, set surprise_type from `classify(...)` of that pair; if it does not and the value is missing, keep the existing default but add a comment that the critic enum is two-valued by design (D-04: the critic sees only pairs that passed the floors) and record this as a disclosed limitation in the SUMMARY. tool-router.cjs: replace the hand-written `z.enum(['structural_transfer','semantic_implementation'])` for eureka_critic with `z.enum(DIRECTIONS)` (require the module at the top with the other lib requires), leave schema_version and every other schema byte-unchanged. Run the new test, tests/test-212-critic-rubric.cjs, tests/test-213-eureka-offer.cjs, tests/test-212-part8-boundary.cjs and `node scripts/check-tool-honesty.cjs --check`. Commit `refactor(355-12): duplicate direction enums import the one module (HIPS-01, D-07)`.</action>
  <verify>
    <automated>node tests/test-355-direction-readers.cjs && node tests/test-212-critic-rubric.cjs && node tests/test-213-eureka-offer.cjs && node tests/test-212-part8-boundary.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All four tests exit 0
    - `grep -c "z.enum(DIRECTIONS)" lib/mcp/tool-router.cjs` = 1
    - `grep -l "direction-convention" lib/core/eureka-critic.cjs lib/core/eureka/eureka-offer.cjs lib/core/grill-engine.cjs lib/mcp/tool-router.cjs | wc -l` prints 4
  </acceptance_criteria>
  <done>Every surviving id list outside the side channel is the module's list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Readers re-derive stored labels from the stored pair (D-07)</name>
  <files>tests/test-355-direction-readers.cjs, scripts/hsi-to-graph.cjs, lib/core/nl-graph-queries.cjs, lib/chat/fabric-chat.cjs, scripts/generate-chat-embed.cjs</files>
  <read_first>
    - scripts/hsi-to-graph.cjs lines 40-150 (how .hsi-results.json pairs become HSI_CONNECTION edges; which props are stored: lsa_sim, semantic_sim, surprise_type; the MOAT-01 transaction; the REVERSE_SALIENT delete/rewrite)
    - lib/core/nl-graph-queries.cjs lines 55-100 and the code that executes a query template and returns rows
    - lib/chat/fabric-chat.cjs lines 30-55 and scripts/generate-chat-embed.cjs lines 35-45 (schema property descriptions)
    - tests/fixtures/272/room/.hsi-results.json (Convention B strings with lsa/semantic pairs)
    - tests/helpers/fixture-room-354.cjs (makeScratchRoom) and tests/test-reverse-salient-telemetry.cjs (running hsi-to-graph on a tmp room)
  </read_first>
  <behavior>
    - running `node scripts/hsi-to-graph.cjs <tmpRoom>` on a copy of tests/fixtures/272/room/.hsi-results.json writes HSI_CONNECTION edges whose surprise_type equals classify(lsa_sim, semantic_sim) for every edge, including rows whose stored string says the opposite
    - a pair with a missing lsa_sim or semantic_sim is written with surprise_type 'none' (never a default direction)
    - the nl-graph-queries HSI query returns surprise_type re-derived from lsa_sim / semantic_sim for an edge seeded with the wrong stored string
    - the nl-graph-queries REVERSE_SALIENT query returns innovation_type null for an edge without source 'rs-engine' and the stored value for an rs-engine edge
    - leg H of tests/test-355-direction-agreement.cjs reports no hit in any file this task touches
  </behavior>
  <action>RED: add the behavior legs to tests/test-355-direction-readers.cjs (tmp room via makeScratchRoom; copy the 272 fixture; seed edges for the query legs through the same writer hsi-to-graph uses; never run in place); run; fail. GREEN: hsi-to-graph.cjs: at the HSI_CONNECTION write, compute `surprise_type: classify(pair.lsa_sim ?? pair.lsa, pair.semantic_sim ?? pair.semantic)` using the field names the file actually reads, cite Phase 355 D-07 ("the stored string from compute-hsi.py is not trusted"); change nothing else (the stamp is added in 355-16). nl-graph-queries.cjs: add lsa_sim and semantic_sim to the HSI query's SELECT and, in the JS that returns that query's rows, overwrite surprise_type with classify(row.lsa_sim, row.semantic_sim); change the REVERSE_SALIENT query to return innovation_type through `CASE WHEN json_extract(e.properties, '$.source') = 'rs-engine' THEN json_extract(e.properties, '$.innovation_type') ELSE NULL END` (no label literal in the SQL). fabric-chat.cjs and generate-chat-embed.cjs: where a schema description defines surprise_type or innovation_type, add "(Phase 355: structural_transfer = same meaning in different words, semantic_implementation = same words with different meaning; re-derive from lsa_sim / semantic_sim; strings stored before the room's next HSI run may use the retired convention)". Run the new test, tests/test-reverse-salient-telemetry.cjs, any existing nl-graph-queries / fabric-chat test (`ls tests | grep -E "nl-graph|fabric-chat"`), the floor sweep and the agreement test. Commit `fix(355-12): readers re-derive direction from the stored pair (HIPS-01, D-07)`.</action>
  <verify>
    <automated>node tests/test-355-direction-readers.cjs && node tests/test-reverse-salient-telemetry.cjs && node tests/test-355-floor-sweep.cjs && ! (node tests/test-355-direction-agreement.cjs 2>&1 | grep -E "^FAIL: H " | grep -qE "hsi-to-graph|nl-graph-queries|fabric-chat|generate-chat-embed")</automated>
  </verify>
  <acceptance_criteria>
    - Verify commands pass
    - `grep -c "D-07" scripts/hsi-to-graph.cjs lib/core/nl-graph-queries.cjs` shows >= 1 in each
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/nl-graph-queries.cjs | grep -cE "THEN 'structural_transfer'|THEN 'semantic_implementation'"` prints 0
    - The tmp room is removed after the test (`ls /tmp | grep -c mos-355` does not grow across two runs)
  </acceptance_criteria>
  <done>No reader shows a user a direction string it did not re-derive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored room data (.hsi-results.json, room.db edges) -> rendered answers | Strings written under the retired convention |
| MCP eureka_critic input -> critic | Host-supplied direction values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-55 | Spoofing | inverted stored labels shown as current | mitigate | Write-time and read-time re-derivation through classify; legacy REVERSE_SALIENT innovation_type suppressed unless source 'rs-engine' |
| T-355-56 | Tampering | MCP input outside the closed id set | mitigate | z.enum(DIRECTIONS) rejects anything else ('none', placeholder names) |
| T-355-57 | Tampering | a second rule hidden in SQL | mitigate | No label literal in SQL; leg H scan covers the touched files |
| T-355-58 | Tampering | tests writing into the committed 272 fixture room | mitigate | Copy to a mkdtemp scratch room; never run in place |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Test `tests/test-355-direction-readers.cjs`
- `lib/mcp/tool-router.cjs` eureka_critic direction schema `z.enum(DIRECTIONS)`
- Write-time re-derived `surprise_type` on `HSI_CONNECTION` edges (`scripts/hsi-to-graph.cjs`)
- nl-graph-queries HSI rows carry `lsa_sim`, `semantic_sim`; REVERSE_SALIENT `innovation_type` null unless `source = 'rs-engine'`

<verification>
- `node tests/test-355-direction-readers.cjs` exits 0; 212/213 tests and the floor sweep green
</verification>

<success_criteria>
- No duplicate id list outside the module except the two that move with the schema bump; no reader trusts a stored direction string
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-12-SUMMARY.md` when done.
</output>
