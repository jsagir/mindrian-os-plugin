---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 06
type: tdd
wave: 2
depends_on: ["355-01"]
files_modified:
  - tests/fixtures/355/theo-stub-responses.json
  - tests/helpers/theo-replay-355.cjs
  - tests/test-355-theo-unreachable.cjs
  - tests/test-355-stamp-truth.cjs
  - tests/test-355-stamp-format.cjs
  - lib/core/verification-stamp.cjs
  - lib/core/verification-stamp-format.cjs
autonomous: true
requirements: [HIPS-04, HIPS-05]
tags: [verification-stamp, theo, find_connections, zod, degradation, formatter]

must_haves:
  truths:
    - "D-08: one shared module lib/core/verification-stamp.cjs builds every stamp; nothing else builds a stamp by hand; it is called only at producers' output or filing layers"
    - "D-09 / THEO-04: the only wire call is brain-client callTool('find_connections', { from, to }) with exactly those two keys; the raw theo server is never called; limit and maxHops are never sent"
    - "D-10 / D-48: endpoint resolution is local and exact against data/framework-names.json (framework_names + curated_extras), in the order frontmatter framework:, then methodology: through data/command-registry.json frameworks[0], then the finding's title; a miss stamps unverified / not_called / handle_unresolved; no fuzzy match, no case folding, no Theo lookup"
    - "D-11: tier = min hops across paths[], cross-checked against edges.length, path.length - 1 and pathLabels.length - 1; 1-2 = strong, 3 = indirect; backend 'theo' is set by the plugin; Theo's missing backend field is never invented"
    - "D-49: strong / indirect require at least one lateral edge type (EXTENDS, CONTRASTS_WITH, COMPLEMENTS, PRODUCES_INPUT_FOR, FEEDS_INTO, ADDRESSES, SUPPORTS, PRECEDES, REQUIRES_KNOWLEDGE_OF, GUARDED_BY) by exact match; an anchor-only path (SOURCED_FROM, PART_OF, INSTANCE_OF, HAS_CHUNK, EXTRACTED_FROM, MENTIONS) stamps unverified / co_sourced_only; any other non-lateral path stamps unverified / no_lateral_relation (ADDRESSES_PROBLEM_TYPE is not ADDRESSES); non-Framework nodes render as [Label], never by internal id; hops render with the undirected ' -- EDGE -- ' joiner; findings without a word-versus-meaning measurement carry direction 'none'"
    - "D-12: a per-run Map memo keyed from + '\\u0000' + to, deduped pairs through a local pool of 4, all awaited before return; no cache across runs"
    - "D-13 / D-54: the degradation matrix maps null / throw -> unavailable backend_unavailable; egress_blocked -> unavailable egress_refused; tier_denied, rate_limited, invalid_key -> unavailable with that reason; an unlisted-tool error -> unavailable tool_not_listed; a text-only reply -> unavailable text_reply; refusals without paths -> theo endpoint_unresolved; paths [] -> theo no_path_within_3_hops; hop disagreement or a bad shape -> theo malformed_response; no finding is dropped and no path is invented"
    - "D-14: judge is the zod literal 'none'; the Jev citation branch is a documented seam comment only, no code"
    - "D-17: the module is read-only (one Theo read, zero writes, zero Jev, zero key) and exports POSTURE { autonomous_safe: true, reversibility: 'n/a (read-only)', consequence: 'low', writes: 'none' }"
    - "D-27 / D-28: lib/core/verification-stamp-format.cjs formatStampLines(stamp, surface) renders a one-line stamp plus a short evidence block from the existing glyphs only (✓ strong, • indirect, bare ⚠ unverified never followed by U+FE0F, · separator), hop counts as words, the judge on the last line ('judge  none, path check only'), never '→', never '-->', never a tree glyph, never a decimal"
    - "D-50: formatUncheckedDesktop() returns 'Not yet checked; run the CLI to verify.' for Desktop/Cowork findings with no stored stamp"
  artifacts:
    - path: "lib/core/verification-stamp.cjs"
      provides: "Stamp zod schema, resolver, tier, path choice, degradation, memo + pool, node-prop converters"
      exports: ["Stamp", "REASONS", "BACKENDS", "TIERS", "LATERAL_EDGE_TYPES", "ANCHOR_EDGE_TYPES", "POSTURE", "extractCarried", "resolveEndpoint", "tierFromHops", "choosePath", "stampFinding", "stampFindingDetailed", "stampFindings", "toNodeProps", "fromNodeProps"]
      contains: "find_connections"
    - path: "lib/core/verification-stamp-format.cjs"
      provides: "zod-free renderer shared by CLI, Desktop prose rules and the reach card"
      exports: ["formatStampLines", "formatPathText", "formatUncheckedDesktop", "assertNoScalar", "GLYPH", "HOP_WORDS", "REASON_WORDS", "BACKEND_WORDS", "UNVERIFIED_ADVICE"]
    - path: "tests/helpers/theo-replay-355.cjs"
      provides: "makeReplayCallTool(fixture), makeNullCallTool(), makeThrowCallTool(), makeCountingCallTool()"
  key_links:
    - from: "lib/core/verification-stamp.cjs"
      to: "lib/core/brain-client.cjs callTool"
      via: "lazy require inside stampFinding when deps.callTool is absent"
      pattern: "callTool\\('find_connections'"
    - from: "lib/core/verification-stamp-format.cjs"
      to: "lib/core/direction-convention.cjs"
      via: "DIRECTION_MEANING / NONE_MEANING for the direction phrase"
      pattern: "direction-convention"
---

<objective>
SPEC Req 4 (HIPS-04) and the render half of HIPS-05: the one verification adapter and the one formatter. Code resolves a finding's two ends to canon Framework names locally (Part 8), asks Theo `find_connections` once per distinct pair through `brain-client.callTool`, computes the tier in code, maps every failure to an honest `unverified` stamp with a named backend and reason, and renders it with the existing glyphs and no number. Producers are wired in 355-16, 355-17, 355-18 and 355-20; this plan builds and proves the adapter against recorded and stubbed Theo answers only (zero network in tests).

Purpose: a stamp that cannot lie: a lying stamp (fabricated path, invented tier, dropped finding, snapped name, hidden outage) is unrepresentable in the schema or fails a test.
Output: two modules, a replay helper, a stub fixture, three tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-AI-SPEC.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PATTERNS.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-BASELINE.md
@lib/core/direction-convention.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests, stub fixture and replay helper for the adapter and the formatter</name>
  <files>tests/fixtures/355/theo-stub-responses.json, tests/helpers/theo-replay-355.cjs, tests/test-355-theo-unreachable.cjs, tests/test-355-stamp-truth.cjs, tests/test-355-stamp-format.cjs</files>
  <read_first>
    - 355-RESEARCH.md "Live Theo Contract (probe record)" (the exact success, multi-path and refusal shapes), "Critical Corrections" C5 and C6, "Code Examples > Degradation mapping"
    - 355-AI-SPEC.md Section 4 item (2) and Section 4b Stamp schema (drafts; direction names there are superseded by D-01, reasons superseded by D-54)
    - lib/core/brain-client.cjs callTool: the null outage return, the egress_blocked / tier_denied / rate_limited / invalid_key error returns, the `{text}` non-JSON body return, the JSON-RPC-error-with-no-result null (re-grep via 355-BASELINE.md anchors; D-57 file, READ ONLY here)
    - data/framework-names.json (pick every stub endpoint name from framework_names or curated_extras)
    - data/command-registry.json (one command whose frameworks[0] is a snapshot name, for the methodology rung)
    - skills/ui-system/SKILL.md lines 140-160 and 310-330; lib/core/doctor/ui-compliance-module.cjs lines 30-52 (glyph vocabulary and forbidden forms)
  </read_first>
  <behavior>
    - fixture: `{ "_note": "synthetic, shaped exactly like the 2026-09-23 live probes; internal Theo record ids replaced by *-fixture-* ids", "responses": { "<from>\u0000<to>": <Theo object or {"$null":true} or {"$throw":"msg"} or {"$string":"..."}> } }` covering: strong 1-hop lateral, strong 2-hop lateral with all-Framework interior, indirect 3-hop lateral, co-sourced-only 2-hop (SOURCED_FROM x2 via BrainRecord), no-lateral 2-hop (ADDRESSES_PROBLEM_TYPE x2 via DomainConcept), five equal 2-hop paths (deterministic choice), refusals (FRAMEWORK_NOT_FOUND), paths [], hop mismatch, pathLabels length mismatch
    - unreachable test: for each of null, throw, {error:'egress_blocked'}, {error:'tier_denied'}, {error:'rate_limited'}, {error:'invalid_key'}, {error:'unknown_tool'}, {error:'something else'}, a bare string, {text:'Error ...'}, {coverage, refusals}, {coverage} alone, {paths: []}, hop mismatch: verification 'unverified', no 'path' key, backend/reason exactly as the D-13 / D-54 matrix names; stampFindings over three findings with a null callTool returns three stamps, all backend 'unavailable', zero path keys (finding count unchanged); every formatted CLI block for these contains 'may be novel or hallucinated - verify with a domain expert'
    - truth test: tier recomputed from the recorded edges equals stamp.verification; stamp.path.nodes equals the recorded path with non-Framework entries replaced by '[' + label + ']' (byte-equal); stamp.path.edges equals the recorded edges; no internal record id appears in the stamp or in any formatted line; among five equal paths the choice prefers a lateral edge, then an all-Framework interior, then lexical order, and is identical across 10 runs
    - truth test, resolution: frontmatter framework: hit -> via 'framework'; methodology: slug -> registry frameworks[0] -> via 'methodology'; title hit -> via 'title'; a case variant of a real name misses (no case folding); a miss yields not_called / handle_unresolved and ZERO callTool calls
    - truth test, wire: every captured callTool call is exactly ('find_connections', {from, to}) with Object.keys sorted equal to ['from','to']
    - truth test, memo + pool: 6 findings over 3 distinct pairs call callTool 3 times; max in-flight <= 4 (counting stub); a second stampFindings call re-calls Theo (no cross-run cache)
    - truth test, schema: Stamp.parse rejects an extra 'score' or 'confidence' key, a path on an unverified stamp, a verified stamp with a reason, strong with 3 hops, judge other than 'none', direction outside ['structural_transfer','semantic_implementation','none']
    - truth test, POSTURE deep-equals { autonomous_safe: true, reversibility: 'n/a (read-only)', consequence: 'low', writes: 'none' }; toNodeProps -> fromNodeProps round-trips through Stamp.parse
    - format test: ✓ strong, • indirect, ⚠ unverified (never followed by U+FE0F); separator ' · '; path text joined by ' -- ' only (no '-->', no '→', no '└' or other tree glyphs); hop words 'one step' / 'two steps' / 'three steps'; the last CLI line is 'judge  none, path check only'; card verified line starts 'verified through ', card unverified line equals 'unverified - novel or hallucinated, verify with an expert'; formatUncheckedDesktop() equals 'Not yet checked; run the CLI to verify.'; for every stub case and every surface, no line matches `(^|[^0-9A-Za-z.])(0?\.[0-9]+|1\.0+)([^0-9.]|$)` or `[0-9]+%`; assertNoScalar(['x 0.87 y']) returns a line without '0.87' and a count of 1
    - every test ends with installNetGuard attempts() === 0
  </behavior>
  <action>Create the fixture and tests/helpers/theo-replay-355.cjs (`makeReplayCallTool(fixture)` returning an async (tool, args) => response by `args.from + '\u0000' + args.to`, honoring the $null / $throw / $string sentinels, recording `calls[]`; `makeNullCallTool()`; `makeThrowCallTool()`; `makeCountingCallTool(inner)` tracking in-flight max). Write the three tests exactly per the behavior list (hygiene-355 preamble in each; every stamp call passes `deps.callTool`, `deps.names` (a Set built from data/framework-names.json) and `deps.registry` so nothing reads the network). Run them; they fail (modules absent). Commit `test(355-06): add failing stamp truth, degradation and format tests`.</action>
  <verify>
    <automated>node tests/test-355-stamp-truth.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The three tests exist and exit non-zero with "Cannot find module" or equivalent for the two new modules
    - `node -e "const f=require('./tests/fixtures/355/theo-stub-responses.json');if(Object.keys(f.responses).length<10)process.exit(1)"` exits 0
    - `grep -c "brainrecord-export" tests/fixtures/355/theo-stub-responses.json` prints 0 (no real internal Theo ids committed)
  </acceptance_criteria>
  <done>Failing tests pin every stamp promise before the adapter exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/core/verification-stamp.cjs (D-08..D-14, D-17, D-48, D-49, D-54)</name>
  <files>lib/core/verification-stamp.cjs</files>
  <read_first>
    - lib/core/strategy/taxonomy-climb.cjs lines 1-40, around :157 and :166 (the "ONE wire door" header, the callTool call inside try/catch, the exports style)
    - scripts/jev-devtime-client.cjs `pool` (around :381): COPY its body; D-55 forbids requiring scripts/ from lib/
    - lib/core/direction-convention.cjs (DIRECTIONS, NONE)
    - tests/test-355-stamp-truth.cjs and tests/test-355-theo-unreachable.cjs (the contract)
  </read_first>
  <action>Create lib/core/verification-stamp.cjs ('use strict', BSL header modeled on taxonomy-climb.cjs: ONE wire door = brainClient.callTool('find_connections', {from, to}) behind the Part 8 belt; read-only posture (D-17); Theo rule 3 no cross-run cache (D-12); a `JEV SEAM (D-14)` comment block describing the future citation Choice through the Theo seam, with no code; Canon Part 8: only exact canon names cross; hyphens only). Requires: zod, node:fs, node:path, ./direction-convention.cjs; brain-client only lazily inside stampFinding when deps.callTool is absent. Implement: `TIERS`, `BACKENDS`, `REASONS` = ['handle_unresolved','backend_unavailable','tool_not_listed','egress_refused','tier_denied','rate_limited','invalid_key','text_reply','endpoint_unresolved','no_path_within_3_hops','co_sourced_only','no_lateral_relation','malformed_response'] (UNAVAILABLE_REASONS = the second through eighth; THEO_REASONS = the last five); `LATERAL_EDGE_TYPES` and `ANCHOR_EDGE_TYPES` as frozen Sets with the D-49 members; `Stamp` = z.object({ verification: z.enum(TIERS), backend: z.enum(BACKENDS), direction: z.enum([...DIRECTIONS, NONE]), judge: z.literal('none'), path: z.object({ nodes: z.array(z.string().min(1).max(128)).min(2).max(4), labels: z.array(z.string().min(1).max(64)).min(2).max(4), edges: z.array(z.string().regex(/^[A-Z][A-Z_]{0,63}$/)).min(1).max(3) }).strict().optional(), reason: z.enum(REASONS).optional() }).strict().superRefine(...) enforcing: edges.length === nodes.length - 1 === labels.length - 1; strong -> 1-2 hops, indirect -> 3 hops, verified -> backend 'theo', no reason, path present and at least one lateral edge; unverified -> reason present and no path; backend 'unavailable' -> reason in UNAVAILABLE_REASONS; backend 'theo' with unverified -> reason in THEO_REASONS; backend 'not_called' -> reason 'handle_unresolved'. `POSTURE` frozen per the must-have. `extractCarried(text, title)` reads only a leading `---` frontmatter block and returns { framework, methodology, title } (trimmed, surrounding quotes stripped; `framework:` key only, per D-48). `resolveEndpoint(carried, ctx)` tries framework, then methodology (normalize to '/mos:' + slug, look up frameworks[0] in the registry map), then title; exact `Set.has` against ctx.names; returns { name, via } or { name: null, via: null }. Default ctx loaders `loadFrameworkNames()` / `loadCommandFrameworks()` read the two data files once per call site (no module-level result cache). `tierFromHops(h)`. `choosePath(paths)`: validate each with a passthrough zod object (path, pathLabels, edges string arrays, hops int), take the min-hop set, prefer lateral, then all-Framework interior, then lexical order of path + edges joined. `stampFindingDetailed({ fromHandle, toHandle, direction }, deps)` implements the degradation order: missing handle -> not_called / handle_unresolved (no call); try callTool, catch -> null; null/undefined -> backend_unavailable; string or array -> text_reply; error 'egress_blocked' -> egress_refused; error in tier_denied / rate_limited / invalid_key -> same reason; error matching /unknown[_ ]tool|not (found|listed)/i -> tool_not_listed; other string error -> backend_unavailable; string `text` with no paths/refusals -> text_reply; no `paths` key: refusals array -> theo endpoint_unresolved, else theo malformed_response; paths not an array or zero valid -> malformed_response; empty -> no_path_within_3_hops; best path hop cross-check fails -> malformed_response; tier 'unverified' from hops -> malformed_response; no lateral edge -> co_sourced_only when every edge is an anchor type, else no_lateral_relation; else strong/indirect with path { nodes: display-safe (Framework -> name, else '[' + label + ']'), labels, edges }. Parse with Stamp; on a parse failure return unverified / theo / malformed_response. Return { stamp, detail: { from_via, to_via, paths_count, chosen: { labels, edges, hops } | null, ms } } (detail never contains internal ids or room text). `stampFinding` returns only the stamp. `stampFindings(findings, deps)`: per-run Map memo keyed from + '\u0000' + to, local copied `pool(items, 4, fn)`, returns results aligned to input order. `toNodeProps(stamp)` -> flat { verification, backend, direction, judge, reason (if any), path (nodes array), path_labels, path_edges, path_len (edges.length or 0) }; `fromNodeProps(props)` rebuilds and Stamp.parse-s. Run the truth and unreachable tests green. Commit `feat(355-06): verification stamp adapter (HIPS-04, D-08..D-14, D-17, D-48, D-49, D-54)`.</action>
  <verify>
    <automated>node tests/test-355-stamp-truth.cjs && node tests/test-355-theo-unreachable.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both tests exit 0 with `FAIL: 0`
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/verification-stamp.cjs | grep -cE "fetch\(|https?://|node:https?|api\.typesafe\.ai|TYPESAFE_API_KEY|jev-|writeFileSync|renameSync|appendFileSync"` prints 0
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/verification-stamp.cjs | grep -c "callTool('find_connections'"` prints 1
    - `grep -c "JEV SEAM (D-14)" lib/core/verification-stamp.cjs` prints 1
  </acceptance_criteria>
  <done>The adapter exists, reads Theo once per distinct pair, and can only produce honest stamps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lib/core/verification-stamp-format.cjs (D-27, D-28, D-49, D-50)</name>
  <files>lib/core/verification-stamp-format.cjs</files>
  <read_first>
    - tests/test-355-stamp-format.cjs (the contract)
    - 355-CONTEXT.md `<specifics>` (the CLI stamp mock and the Desktop prose shape) and D-27, D-28, D-41, D-49, D-50
    - lib/hmi/dial-label-composer.cjs lines 95-115 (template family style the card line must fit)
    - lib/core/verification-stamp.cjs (REASONS, TIERS, BACKENDS)
  </read_first>
  <action>Create lib/core/verification-stamp-format.cjs (zod-free, requires only ./direction-convention.cjs, so the hook-path sensor and lib/hmi can require it). Exports: `GLYPH` { strong: '✓', indirect: '•', unverified: '⚠' }; `SEP` ' · '; `HOP_WORDS` { 1: 'one step', 2: 'two steps', 3: 'three steps' }; `BACKEND_WORDS` { theo: 'theo', unavailable: 'theo unavailable', not_called: 'not checked' }; `REASON_WORDS` with one plain phrase per REASONS entry (handle_unresolved 'endpoint not a known framework'; endpoint_unresolved 'the methodology graph does not know one endpoint'; no_path_within_3_hops 'no link within three steps in the methodology graph'; co_sourced_only 'shares a source record'; no_lateral_relation 'linked only through a broad shared node'; backend_unavailable 'methodology graph unreachable'; tool_not_listed 'path check not offered by the methodology graph'; egress_refused 'check refused by the privacy guard'; tier_denied 'path check not available on this plan'; rate_limited 'methodology graph busy, not checked'; invalid_key 'methodology graph key rejected'; text_reply 'methodology graph gave no structured answer'; malformed_response 'methodology graph answer did not add up'); `UNVERIFIED_ADVICE` 'may be novel or hallucinated - verify with a domain expert'; `formatPathText(nodes, edges)` joining node, ' -- ' + EDGE + ' -- ', node (undirected, D-49); `formatStampLines(stamp, surface)` for surface 'cli' and 'cowork' (identical): verified -> [glyph + ' ' + tier + SEP + phrase + SEP + backend word, 'path   ' + pathText, 'tier   ' + tier + ', ' + hop words + ' in the methodology graph', 'judge  none, path check only']; unverified -> ['⚠ unverified' + SEP + phrase + SEP + backend word, 'reason ' + reason words, UNVERIFIED_ADVICE, 'judge  none, path check only']; surface 'desktop' -> verified ['Checked: ' + tier + '. The methodology graph links them in ' + hop words + ': ' + pathText + '.', 'Direction: ' + phrase + '.', 'Nobody has judged the citation yet; this is the path alone.'], unverified ['Not verified: ' + reason words + '.', 'Direction: ' + phrase + '.', 'It ' + UNVERIFIED_ADVICE + '.']; surface 'card' -> verified ['verified through ' + pathText], unverified ['unverified - novel or hallucinated, verify with an expert']; phrase = DIRECTION_MEANING[direction] or NONE_MEANING; unknown tier or surface throws TypeError. Every return passes through `assertNoScalar(lines)` (replaces any token matching the D-30 decimal regex or `\d+%` with '[withheld]' and returns { lines, withheld }; formatStampLines returns the lines). `formatUncheckedDesktop()` returns 'Not yet checked; run the CLI to verify.' Header cites D-27, D-28 (glyph overload flagged for the UI checker: the evidence line, not the glyph, carries the meaning), D-49 and Canon Part 12. Run the format test green, then `node scripts/check-render-coverage.cjs --check`; if it reports a new render entry point, run `node scripts/build-render-coverage.cjs` and re-check. Commit `feat(355-06): stamp formatter, existing glyphs only, no numbers (HIPS-05, D-27, D-28, D-50)`.</action>
  <verify>
    <automated>node tests/test-355-stamp-format.cjs && node tests/test-355-stamp-truth.cjs && node scripts/check-render-coverage.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-355-stamp-format.cjs` exits 0 with `FAIL: 0`
    - `grep -P '\x{26A0}\x{FE0F}' lib/core/verification-stamp-format.cjs` prints nothing; `grep -cE -- "-->|→|└|├" lib/core/verification-stamp-format.cjs` prints 0
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/verification-stamp-format.cjs | grep -c "require("` prints 1 and `grep -c "zod" lib/core/verification-stamp-format.cjs` prints 0
    - `node scripts/check-render-coverage.cjs --check` exits 0
  </acceptance_criteria>
  <done>One formatter renders the same honest stamp on every surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin -> Theo (brain-client callTool) | The only network egress this module causes: two canon names |
| Theo response -> stamp | Untrusted remote shape becomes a user-visible verdict |
| stamp -> rendered text | The rendered line is what the user trusts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-25 | Information disclosure | from/to carrying room text or node ids | mitigate | Only exact snapshot names reach callTool (resolveEndpoint returns a Set member or null); a miss never calls; the Part 8 belt blocks content (egress_refused) as a second wall |
| T-355-26 | Spoofing / Tampering | fabricated or snapped path, invented tier | mitigate | Tier recomputed from returned edges with three cross-checks; unverified forbids a path by schema; no fuzzy or case-folded match; replay test byte-compares |
| T-355-27 | Repudiation | a rate limit reported as "Theo says unknown" | mitigate | D-54 matrix: tier_denied / rate_limited / invalid_key / text_reply are their own reasons under backend unavailable |
| T-355-28 | Spoofing (false assurance) | provenance-hub or problem-type-hub "strong" | mitigate | D-49 lateral-edge requirement by exact type; co_sourced_only and no_lateral_relation reasons |
| T-355-29 | Information disclosure (Brain IP) | internal Theo record ids rendered | mitigate | Non-Framework nodes rendered as [Label]; test asserts no fixture-internal id reaches a stamp or line |
| T-355-30 | Denial of service | Theo latency | mitigate | pool of 4, per-run memo, brain-client's own 20 s timeout; callers await stamps before any BEGIN (enforced in 355-20) |
| T-355-31 | Tampering | a score smuggled into a stamp | mitigate | Stamp is .strict(); formatter runs assertNoScalar on every line |
| T-355-32 | Elevation of privilege | Jev reached from runtime | mitigate | No Jev code, key or endpoint in the module (grep acceptance); D-14 seam is a comment |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Module `lib/core/verification-stamp.cjs`: `Stamp` (zod), `TIERS`, `BACKENDS`, `REASONS` (13 reason enums incl. `co_sourced_only`, `no_lateral_relation`, `text_reply`, `tool_not_listed`), `LATERAL_EDGE_TYPES`, `ANCHOR_EDGE_TYPES`, `POSTURE`, `extractCarried`, `resolveEndpoint`, `loadFrameworkNames`, `loadCommandFrameworks`, `tierFromHops`, `choosePath`, `stampFinding`, `stampFindingDetailed`, `stampFindings`, `toNodeProps`, `fromNodeProps`
- Node property names (D-37 via toNodeProps): `verification`, `backend`, `direction`, `judge`, `reason`, `path`, `path_labels`, `path_edges`, `path_len`
- Module `lib/core/verification-stamp-format.cjs`: `formatStampLines(stamp, surface)` (surfaces `cli`, `cowork`, `desktop`, `card`), `formatPathText`, `formatUncheckedDesktop`, `assertNoScalar`, `GLYPH`, `SEP`, `HOP_WORDS`, `REASON_WORDS`, `BACKEND_WORDS`, `UNVERIFIED_ADVICE`
- Test helper `tests/helpers/theo-replay-355.cjs`: `makeReplayCallTool`, `makeNullCallTool`, `makeThrowCallTool`, `makeCountingCallTool`
- Fixture `tests/fixtures/355/theo-stub-responses.json`
- Tests `tests/test-355-stamp-truth.cjs`, `tests/test-355-theo-unreachable.cjs`, `tests/test-355-stamp-format.cjs`

<verification>
- The three 355-06 tests exit 0
- `node scripts/check-render-coverage.cjs --check` exits 0
</verification>

<success_criteria>
- Every D-13 / D-54 degradation case yields an honest unverified stamp; verified stamps carry a byte-true path with a lateral edge
- The formatter renders every case with the existing glyphs and no number
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-06-SUMMARY.md` when done.
</output>
