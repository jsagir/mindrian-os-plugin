---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 01
type: tdd
wave: 1
depends_on: []
execution_gate: "D-57 - Phase 354 must be CLOSED (all 354-NN plans [x] in ROADMAP.md, 354-16-SUMMARY.md present, no uncommitted peer diff on the shared files) before ANY task of ANY 355 plan runs"
files_modified:
  - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-BASELINE.md
  - tests/helpers/hygiene-355.cjs
  - tests/run-all-355.sh
  - lib/core/direction-convention.cjs
  - tests/test-355-direction-convention.cjs
autonomous: true
requirements: [HIPS-01, HIPS-10]
tags: [phase-gate, direction-convention, baseline, run-all]

must_haves:
  truths:
    - "D-57: execution of Phase 355 starts only after Phase 354 closes; Task 1 refuses to proceed (and stops the phase) while any 354-NN plan is unchecked in ROADMAP.md or any shared file (gate.cjs, tool-router.cjs, brain-client.cjs, part8-egress-guard.cjs, doctor.cjs, scripts/jev-devtime-client.cjs) carries an uncommitted diff this session did not make"
    - "HIPS-10: the doctor --acceptance failing-point set is captured ONCE at the 355 base commit and written into both 355-BASELINE.md and the tests/run-all-355.sh header with the date and the commit; later plans never re-capture it"
    - "D-01: the wire ids stay structural_transfer / semantic_implementation; no rename anywhere"
    - "D-02: lib/core/direction-convention.cjs is the ONE module that owns the direction rule, cites 355-ORIGIN-CONCEPT.md section 3, and exports classify(lsa, semantic), classifyDiff(signedDiff), DIRECTIONS, DIRECTION_MEANING, NONE, NONE_MEANING and phraseHash()"
    - "D-02: classify(0.2, 0.6) === 'structural_transfer' (semantic higher = same meaning in different words, Convention A); classify(0.6, 0.2) === 'semantic_implementation'; an exact tie returns 'semantic_implementation'; null, undefined, NaN or a non-number on either side returns 'none'"
    - "The module is zod-free and requires nothing but node:crypto, so the hook-path sensor can import it without a load-time throw"
  artifacts:
    - path: "lib/core/direction-convention.cjs"
      provides: "The one direction rule and the two confirmed-to-be phrases"
      exports: ["classify", "classifyDiff", "DIRECTIONS", "DIRECTION_MEANING", "NONE", "NONE_MEANING", "phraseHash"]
      contains: "355-ORIGIN-CONCEPT.md"
    - path: "tests/run-all-355.sh"
      provides: "Phase aggregator mirroring tests/run-all-224.sh with the baseline in its header"
      contains: "doctor_acceptance_no_new_regression"
    - path: "tests/helpers/hygiene-355.cjs"
      provides: "installNetGuard, makeChecker, scrubVendorKey shared by every 355 test"
    - path: ".planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-BASELINE.md"
      provides: "Base commit, re-read anchors of the six D-57 files, doctor baseline set"
  key_links:
    - from: "tests/test-355-direction-convention.cjs"
      to: "lib/core/direction-convention.cjs"
      via: "require + pinned input/output table"
      pattern: "direction-convention"
    - from: "tests/run-all-355.sh"
      to: "tests/test-355-*.cjs"
      via: "glob loop with run_if"
      pattern: "test-355-"
---

<objective>
EXECUTION GATE (D-57): nothing in Phase 355 runs until Phase 354 has closed. Plans 354-11 through 354-18 edit `lib/mcp/tools/gate.cjs`, `lib/mcp/tool-router.cjs`, `lib/core/brain-client.cjs`, `lib/core/part8-egress-guard.cjs`, `scripts/doctor.cjs` and `scripts/jev-devtime-client.cjs`, all of which later 355 plans edit. Task 1 proves the gate holds, records the base commit, re-reads those six files' current anchors, and captures the `doctor --acceptance` baseline that the phase gate (HIPS-10) compares against.

Then land the single direction rule (HIPS-01, D-01, D-02): `lib/core/direction-convention.cjs`, test-first. This is step (1) of D-35's fixed order; plan 355-02 carries step (2), the PWS author's confirmation of the two phrases.

Purpose: every later plan builds on a known-clean tree, a known doctor baseline, and one definition of what `structural_transfer` means.
Output: 355-BASELINE.md, `tests/run-all-355.sh`, `tests/helpers/hygiene-355.cjs`, `lib/core/direction-convention.cjs`, `tests/test-355-direction-convention.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-SPEC.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PATTERNS.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-ORIGIN-CONCEPT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-57 gate check, base commit, D-57 file anchors, doctor baseline, hygiene helper and run-all-355.sh skeleton</name>
  <files>.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-BASELINE.md, tests/helpers/hygiene-355.cjs, tests/run-all-355.sh</files>
  <read_first>
    - .planning/ROADMAP.md (only the `### Phase 354` card: every `- [ ] 354-NN-PLAN.md` line)
    - tests/run-all-224.sh (whole file: run / run_if counters, strip_comments, doctor_acceptance_no_new_regression at :182-209, the dependency-diff leg)
    - tests/test-356-tripwires.cjs lines 1-40 (the fetch-counter + key-scrub hygiene idiom this helper generalizes)
    - tests/test-353-tripwires.cjs lines 25-100 (check(), isPureLineComment, nonCommentContains, listFilesRecursive; COPY, never require - the 353 tests are peer-owned history)
    - 355-RESEARCH.md sections "Validation Architecture" (run-all legs 1-9) and "Pitfall 16" (the key is exported in the navigator's shell)
  </read_first>
  <action>
GATE (D-57), run first and STOP the whole phase on any failure:
(a) `grep -nE '^- \[ \] 354-[0-9]+-PLAN\.md' .planning/ROADMAP.md` must print nothing, and `ls .planning/phases/354-*/354-16-SUMMARY.md` must succeed. If either fails, do not continue: report "Phase 354 not closed; D-57 holds 355" and stop.
(b) `git status --short lib/mcp/tools/gate.cjs lib/mcp/tool-router.cjs lib/core/brain-client.cjs lib/core/part8-egress-guard.cjs scripts/doctor.cjs scripts/jev-devtime-client.cjs scripts/eval-icm-writers.cjs tests/test-353-*.cjs .planning/REQUIREMENTS.md` must be empty. A non-empty line is a peer's uncommitted work (two-session collision rule): never revert or stage it; stop and report which paths are dirty.

BASELINE RECORD: create 355-BASELINE.md (Feynman prose, hyphens only) holding: BASE_355 = `git rev-parse --short HEAD`; the date; the output of these re-read greps, one fenced block each, so every later plan that edits a D-57 file starts from the post-354 anchors instead of the research's line numbers:
`grep -n "_promoteCardSubject\|row.type !== 'claim'\|subject_not_claim" lib/mcp/tools/gate.cjs`;
`grep -n "scout-hsi\|UNIMPLEMENTED_MUTATING_ORCHESTRATION\|NOT EXECUTED\|z.enum(\['structural_transfer'" lib/mcp/tool-router.cjs`;
`grep -n "^const BRAIN_URL\|^async function callTool\|egress_blocked\|tier_denied\|rate_limited" lib/core/brain-client.cjs`;
`grep -n "find_connections\|known_tool_shape" lib/core/part8-egress-guard.cjs`;
`grep -n "id: '" scripts/doctor.cjs`;
`grep -n "EGRESS_PROFILES\|kind: '" scripts/jev-devtime-client.cjs`.
DOCTOR BASELINE (HIPS-10): run `node scripts/doctor.cjs --acceptance` once (stdout+stderr to a file under the session scratch directory, never inside the repo), record its exit code and the sorted set of failing point ids (parse the same way `doctor_acceptance_no_new_regression` in tests/run-all-224.sh parses them) into 355-BASELINE.md under `## Doctor acceptance baseline (BASE_355)`.

HELPER: create tests/helpers/hygiene-355.cjs ('use strict', BSL header, Phase 355 citation, hyphens only) exporting: `scrubVendorKey()` (deletes process.env.TYPESAFE_API_KEY and returns whether it was present, never its value); `installNetGuard()` (replaces globalThis.fetch with a thrower that increments a counter, returns `{ attempts: () => n, restore }`); `makeChecker(label)` returning `{ check(name, cond, detail), summary() }` where summary prints `PASS: x FAIL: y` and returns the exit code (0 all pass, 1 any fail); `isPureLineComment(line)`, `nonCommentLines(fileAbs)` and `listFilesRecursive(dirAbs)` COPIED from tests/test-356-tripwires.cjs / tests/test-353-tripwires.cjs (URL-safe `//` handling kept). Every 355 test must call scrubVendorKey and installNetGuard BEFORE requiring any repo module, and assert `attempts() === 0` as its last check.

SKELETON: create tests/run-all-355.sh (chmod +x) mirroring tests/run-all-224.sh: header prose with BASE_355, the date and the baseline failing-point set as a literal list; `set -uo pipefail`; ROOT cd; PASS/FAIL/SKIP counters; `run` and `run_if`; `strip_comments`. Legs:
(1) a glob loop `for f in tests/test-355-*.cjs; do run_if "$(basename "$f")" "$f" node "$f"; done` treating exit 77 as SKIP;
(2) Part 8 sweep: `PART8_RE="fetch\(|https?://|require\(['\"]node:https?|\b(curl|wget)\b"` over the target list `lib/core/direction-convention.cjs lib/core/verification-stamp.cjs lib/core/verification-stamp-format.cjs lib/core/floor-disclosure.cjs scripts/stamp-connections.cjs`, each target run_if-on-existence for now (plan 355-27 flips missing targets to FAIL);
(3) Part 9 sweep: no `node:sqlite`, `DatabaseSync` or `INSERT INTO` in the same target list (comment-stripped);
(4) `git diff --quiet package.json package-lock.json` (zero new dependencies);
(5) `--check` replays, each run_if on the script existing: `node scripts/check-floor-ledger.cjs --check`, `node scripts/refresh-framework-names.cjs --check`, `node scripts/measure-hsi-thinking-mode.cjs --check`, `node scripts/calibrate-citation-check.cjs --check`, `node scripts/judge-355-usefulness.cjs --check`, `node scripts/capture-355-theo-responses.cjs --check`;
(6) structural gates: `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-shape-declaration.cjs --check` (no --strict), `node scripts/check-render-coverage.cjs --check`;
(7) `doctor_acceptance_no_new_regression` copied from run-all-224.sh with THIS phase's baseline set;
(8) no-regression legs, each run_if: `bash tests/run-all-356.sh`, `bash tests/run-all-272.sh`, `node tests/272-direction-convention.test.cjs`, `node tests/test-213-sensor-eureka.cjs`, `node tests/test-219-harvest-sensor.cjs`, `node tests/test-354-gate-subject-promotion.cjs`, `node lib/core/part8-egress-guard.test.cjs`, `node tests/test-354-egress-typed-question.cjs`, `node tests/test-reverse-salient-telemetry.cjs`;
(9) em-dash leg: a function `emdash_scan` that greps U+2014 over every NEW file this phase adds (the glob `lib/core/direction-convention.cjs lib/core/verification-stamp*.cjs lib/core/floor-disclosure.cjs scripts/*355* scripts/stamp-connections.cjs tests/test-355-*.cjs tests/helpers/*355*.cjs data/floor-ledger.json`) and over ADDED lines only of modified files (`git diff "$BASE_355" -- lib scripts commands skills data docs tests | grep '^+' | grep -v '^+++'`), zero hits required.
End with the run-all-224 PASS/FAIL/SKIP summary and `exit $FAIL`-style status.
  </action>
  <verify>
    <automated>bash -n tests/run-all-355.sh && node -e "const h=require('./tests/helpers/hygiene-355.cjs');for(const k of ['scrubVendorKey','installNetGuard','makeChecker','isPureLineComment','nonCommentLines','listFilesRecursive'])if(typeof h[k]!=='function')throw new Error(k)" && grep -c "BASE_355" .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^- \[ \] 354-[0-9]+-PLAN\.md' .planning/ROADMAP.md` prints nothing at the moment Task 1 ran (recorded in 355-BASELINE.md as the gate result)
    - 355-BASELINE.md contains the literal headings `## Gate`, `## D-57 file anchors (post-354)`, `## Doctor acceptance baseline (BASE_355)` and a 7-to-12 hex-char commit id after `BASE_355 =`
    - `grep -c "doctor_acceptance_no_new_regression" tests/run-all-355.sh` prints at least 2 (definition + call)
    - `grep -c 'for f in tests/test-355-\*.cjs' tests/run-all-355.sh` prints 1
    - `bash tests/run-all-355.sh` exits 0 on the base tree (every 355-specific leg SKIPs, the structural and no-regression legs pass, the doctor leg reports no new failing point)
    - `grep -P '\x{2014}' tests/run-all-355.sh tests/helpers/hygiene-355.cjs` prints nothing
  </acceptance_criteria>
  <done>The phase has a proven post-354 starting point, a recorded doctor baseline, one shared test hygiene helper and an aggregator that SKIPs cleanly until each 355 artifact lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/core/direction-convention.cjs, test first (D-01, D-02)</name>
  <files>tests/test-355-direction-convention.cjs, lib/core/direction-convention.cjs</files>
  <read_first>
    - lib/core/rs-math.cjs lines 20-35 and 390-405 (Convention A doc and classifyDirection: the rule this module adopts; the :395-400 comment forbids a third branch for finite values)
    - lib/core/hsi-lsa.cjs lines 1-25 and 115-135 (Convention B and the MUST-NOT-unify warning, which protects the algorithms, not the label rule)
    - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-ORIGIN-CONCEPT.md section 3 (the deck's two directions)
    - 355-CONTEXT.md D-01, D-02, D-07 and the `<specifics>` block (the two phrases the PWS author confirms: "same meaning in different words" and "same words with different meaning")
    - 355-RESEARCH.md "Code Examples > Direction module (shape)" and "Pitfall 6" (Number(null) === 0 trap)
    - tests/272-direction-convention.test.cjs lines 40-50 (tie behavior that must stay green)
  </read_first>
  <behavior>
    - classify(0.2, 0.6) -> 'structural_transfer'; classify(0.6, 0.2) -> 'semantic_implementation'
    - classify(0.4, 0.4) -> 'semantic_implementation' (exact tie, D-02); classify(0, 0) -> 'semantic_implementation'
    - classify(null, 0.5), classify(0.5, undefined), classify(NaN, 0.1), classify('abc', 0.1), classify(0.1, Infinity) -> 'none'
    - classify('0.2', '0.6') -> 'structural_transfer' (numeric strings coerce, matching rs-math's Number() handling)
    - classifyDiff(0.01) -> 'structural_transfer'; classifyDiff(0) -> 'semantic_implementation'; classifyDiff(-0.3) -> 'semantic_implementation'; classifyDiff(null) -> 'none'
    - DIRECTIONS deep-equals ['structural_transfer','semantic_implementation'] and Object.isFrozen(DIRECTIONS) === true
    - DIRECTION_MEANING.structural_transfer === 'same meaning in different words'; DIRECTION_MEANING.semantic_implementation === 'same words with different meaning'; NONE === 'none'; NONE_MEANING === 'no wording signal measured'
    - phraseHash() returns a 64-char hex sha256 of JSON.stringify({DIRECTIONS, DIRECTION_MEANING, NONE_MEANING}) and changes when any phrase changes
    - the module source requires only node:crypto (no zod, no lib/ require)
  </behavior>
  <action>
RED: write tests/test-355-direction-convention.cjs first using tests/helpers/hygiene-355.cjs (scrubVendorKey + installNetGuard before the require), asserting every behavior line above plus a static leg that reads the module source and asserts it contains `355-ORIGIN-CONCEPT.md` and that its only `require(` call is `require('node:crypto')`. Run it; it must fail (module absent). Commit `test(355-01): add failing direction-convention test`.

GREEN: create lib/core/direction-convention.cjs ('use strict', BSL copyright, a header that (1) cites `355-ORIGIN-CONCEPT.md section 3` as the source, (2) states Convention A: signed_diff = semantic - lsa, positive means the deep encoder sees shared meaning the shallow one misses, i.e. same meaning in different words, (3) states D-01: wire ids unchanged, (4) states the tie rule and the none sentinel, (5) hyphens only). Exports: `DIRECTIONS` = Object.freeze(['structural_transfer','semantic_implementation']); `NONE` = 'none'; `DIRECTION_MEANING` = Object.freeze({ structural_transfer: 'same meaning in different words', semantic_implementation: 'same words with different meaning' }) (D-02 gives the gist "same meaning, different words"; the exact rendered phrases are the CONTEXT `<specifics>` wording the PWS author confirms in plan 355-02); `NONE_MEANING` = 'no wording signal measured' (the D-49 phrase for findings with no word-versus-meaning measurement); `classifyDiff(signedDiff)`: null/undefined -> NONE, `Number(x)` not finite -> NONE, `> 0` -> 'structural_transfer', else 'semantic_implementation' (this is the ONLY place in the repo the comparison-to-label rule lives); `classify(lsa, semantic)`: null/undefined on either side -> NONE, non-finite Number on either side -> NONE, else `classifyDiff(Number(semantic) - Number(lsa))`; `phraseHash()` via node:crypto createHash('sha256'). No range clamp for finite values (a third finite branch would change rs-math behavior, forbidden by rs-math.cjs:395-400).
Run the test green. Commit `feat(355-01): one direction convention module (HIPS-01, D-01, D-02)`.
  </action>
  <verify>
    <automated>node tests/test-355-direction-convention.cjs && node tests/272-direction-convention.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-355-direction-convention.cjs` exits 0 and prints `FAIL: 0`
    - `node -e "const d=require('./lib/core/direction-convention.cjs');if(d.classify(0.2,0.6)!=='structural_transfer'||d.classify(0.4,0.4)!=='semantic_implementation'||d.classify(null,0.5)!=='none')process.exit(1)"` exits 0
    - `grep -c "355-ORIGIN-CONCEPT.md" lib/core/direction-convention.cjs` prints at least 1
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/direction-convention.cjs | grep -c "require("` prints 1
    - `node tests/272-direction-convention.test.cjs` still exits 0 (unchanged Convention A pins)
    - git log shows the test commit before the feat commit for this task
  </acceptance_criteria>
  <done>One module defines the rule and the phrases, pinned by a test that was red before it was green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer session -> shared working tree | A concurrent Phase 354 session may still hold uncommitted edits on six shared files |
| doctor --acceptance -> run-all-355 baseline | The baseline decides what counts as a "new" regression for the whole phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-01 | Tampering | shared files owned by a peer | mitigate | Task 1 gate (b): `git status --short` on the six D-57 files plus the peer's 353 files must be empty; on any hit stop and report, never stage or revert |
| T-355-02 | Repudiation | doctor baseline | mitigate | Captured once at BASE_355, written with commit + date into two places; later plans read it, never re-capture; a changed baseline needs a new dated section, not an overwrite |
| T-355-03 | Information disclosure | tests reading TYPESAFE_API_KEY | mitigate | hygiene-355.cjs scrubVendorKey returns presence only, never the value; installNetGuard proves zero network per test |
| T-355-04 | Tampering | direction rule duplicated elsewhere | mitigate | Module is the only comparison-to-label site; plan 355-02 adds the repo-wide grep leg with a negative control |
| T-355-05 | Denial of service | hook-path import of the module | mitigate | Module requires only node:crypto (static leg in the test), so the sensor chain cannot throw at load |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Module `lib/core/direction-convention.cjs` with exports `classify(lsa, semantic)`, `classifyDiff(signedDiff)`, `DIRECTIONS`, `DIRECTION_MEANING`, `NONE`, `NONE_MEANING`, `phraseHash()`
- Test `tests/test-355-direction-convention.cjs`
- Test helper `tests/helpers/hygiene-355.cjs` exports `scrubVendorKey`, `installNetGuard`, `makeChecker`, `isPureLineComment`, `nonCommentLines`, `listFilesRecursive`
- Aggregator `tests/run-all-355.sh` with shell functions `run`, `run_if`, `strip_comments`, `doctor_acceptance_no_new_regression`, `emdash_scan` and the constant `BASE_355`
- Record `.planning/phases/355-.../355-BASELINE.md` (sections Gate, D-57 file anchors (post-354), Doctor acceptance baseline (BASE_355))

<verification>
- `bash tests/run-all-355.sh` exits 0 with 355-specific legs SKIPPED except the direction-convention test, which PASSES
- `node tests/272-direction-convention.test.cjs` exits 0
</verification>

<success_criteria>
- Phase 354 proven closed before any 355 edit; baseline recorded with commit and date
- `lib/core/direction-convention.cjs` exists, cites the origin deck, and passes its pinned table
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-01-SUMMARY.md` when done (new phase-dir files need `git add -f`).
</output>
