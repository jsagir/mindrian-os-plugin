---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 24
type: tdd
wave: 8
depends_on: ["355-03", "355-13", "355-14", "355-23"]
files_modified:
  - tests/test-355-hit-rate-record.cjs
  - scripts/measure-355-hit-rate.cjs
  - tests/fixtures/355-rooms/pairings.items.json
  - tests/fixtures/355-rooms/judgments.json
  - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/labeling-session-pairings-unstamped.json
autonomous: false
requirements: [HIPS-07]
tags: [hit-rate, fixture-rooms, blind-judging, baseline, checkpoint]

must_haves:
  truths:
    - "D-34 / SPEC AC12: scripts/measure-355-hit-rate.cjs refuses any --room that does not resolve (realpath + path.sep) inside tests/fixtures/355-rooms/, always copies the room to an os.tmpdir() mkdtemp and runs the engines there, and never resolves ~/MindrianRooms or any os.homedir() room path"
    - "D-32 / D-35 step (4): the UNSTAMPED baseline export runs before any stamp exists for the fixture rooms; the item file carries only pair_id, room, a_excerpt, b_excerpt and direction_phrase for display (producer kept for the later join, never shown); the navigator judges it blind in the first sitting"
    - "SPEC Req 7: the shown set per room is what the producers actually show (HSI top 10, find-bottlenecks run with topk 10, eureka's ranked list when its substrate resolves), deduped by artifact pair; the run fails loudly (never pads) when a room has fewer than 20 shown pairings, and records the top-K it used"
    - "Pitfall 11: the dev-time export uses the real local encoder (MongoDB/mdbr-leaf-ir from ~/.mindrian/model-cache); its id and the engines' versions are recorded; tests never run an encoder"
    - "D-33: the navigator's sitting-1 judgments {pair_id, useful, direction_ok, already_known, at} are emitted to tests/fixtures/355-rooms/judgments.json with labeled_at and fixture_sha256 and committed before any stamped pairing file exists"
  artifacts:
    - path: "scripts/measure-355-hit-rate.cjs"
      provides: "export --unstamped / stamp / record / --check, room guard, Wilson interval"
      exports: ["guardRoomPath", "exportUnstamped", "wilson95", "joinJudgments", "computeRates"]
    - path: "tests/test-355-hit-rate-record.cjs"
      provides: "Guard refusals, no-live-room scan, join and rate math, record shape (exit 77 until the record exists)"
      min_lines: 100
    - path: "tests/fixtures/355-rooms/pairings.items.json"
      provides: "Unstamped shown pairings for all three rooms"
    - path: "tests/fixtures/355-rooms/judgments.json"
      provides: "Navigator's blind sitting-1 judgments"
  key_links:
    - from: "scripts/measure-355-hit-rate.cjs"
      to: "tests/fixtures/355-rooms/"
      via: "guardRoomPath realpath prefix + cpSync to mkdtemp"
      pattern: "355-rooms"
    - from: "tests/fixtures/355-rooms/judgments.json"
      to: "tests/fixtures/355-rooms/pairings.items.json"
      via: "fixture_sha256 and pair_id join"
      pattern: "pair_id"
---

<objective>
SPEC Req 7 first half (HIPS-07): measure usefulness for the first time in the engine's history. Build the fixture-only measurement script, export what the producers actually show on the three fixture rooms WITHOUT stamps, and let the navigator judge that baseline blind (D-32, D-35 step 4). Plan 355-25 stamps the same pairings, runs the second sitting and writes 355-VERIFICATION.md.

Output: `scripts/measure-355-hit-rate.cjs`, its record test, the unstamped item file, the sitting-1 judgments.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-AI-SPEC.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@tests/fixtures/355-rooms/README.md
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/measure-355-hit-rate.cjs with the room guard, the export, the join and the rate math (test first)</name>
  <files>tests/test-355-hit-rate-record.cjs, scripts/measure-355-hit-rate.cjs</files>
  <read_first>
    - scripts/eval-icm-writers.cjs lines 75-105 (realpath + separator prefix guard; READ ONLY) and tests/test-353-tripwires.cjs lines 115-140 (the fixture-only refusal leg; READ ONLY)
    - lib/core/rs-engine.cjs runModeInternal, lib/core/hsi-engine.cjs runTier1, lib/agents/reverse-salient-agent.cjs detectAndSurface (topk), scripts/hsi-to-graph.cjs --top, scripts/eureka-command.cjs (offline run path) - how each writes its results into a room
    - lib/core/direction-convention.cjs DIRECTION_MEANING / NONE_MEANING
    - scripts/label-355-gold.cjs SETS['pairings-unstamped'] (the item fields it displays)
  </read_first>
  <behavior>
    - guardRoomPath('<repo>/tests/fixtures/355-rooms/room-control') returns the realpath; a tmp dir outside the tree, a sibling-prefix path ('tests/fixtures/355-rooms-evil'), a symlink pointing outside, and any path under os.homedir() + '/MindrianRooms' each throw; the CLI exits non-zero for them
    - the script's non-comment source contains no 'MindrianRooms' and no os.homedir() room resolution (static leg)
    - exportUnstamped(rooms, { runProducers }) (with an injected runProducers returning recorded pair lists) writes items [{ pair_id, room, producer, a_excerpt, b_excerpt, direction_phrase }] where pair_id = first 12 hex of sha256(room + '\u0000' + sorted artifact paths), excerpts are the artifact title plus its first sentences capped at 600 chars, direction_phrase comes from DIRECTION_MEANING / NONE_MEANING, pairs are deduped across producers, and it throws 'fewer than 20 shown pairings in <room>' instead of padding
    - wilson95(k, n) returns [lo, hi] matching hand-computed values for (0,0 -> [0,0] with n 0 flagged), (5,20), (20,20); no dependency
    - computeRates(judgments, stamps) returns per room, pooled and per tier (strong / indirect / unverified) { k, n, rate, wilson } using the BLIND judgments joined by pair_id, plus unverified share, reason mix, not_called share, already_known x tier cross-tab and the false-friend count per tier from planted-cases.json
    - `--check` exits 77 while tests/fixtures/355-rooms/hit-rate-record.json is absent and otherwise recomputes the record exactly
  </behavior>
  <action>RED: write tests/test-355-hit-rate-record.cjs (hygiene-355 preamble; mkdtemp; injected producers; no encoder) per the behavior list; run; fail. GREEN: create scripts/measure-355-hit-rate.cjs ('use strict', BSL header "dev-time only; fixture rooms only; never a live room (SPEC AC12)"; argv switch-case: `export --unstamped [--top <n>]`, `stamp` (355-25), `record` (355-25), `--check`, `--help`). exportUnstamped copies each room (cpSync) into a mkdtemp, runs, in that copy only: hsi-engine runTier1 then hsi-to-graph (no --stamp) and takes the top-n pairs; rs-engine runModeInternal and takes the top-n pairs; eureka through its offline run path if its substrate resolves (record `eureka: substrate_unavailable` otherwise); collects excerpts from the copy's artifact files; deletes the mkdtemp; writes tests/fixtures/355-rooms/pairings.items.json with { generated_at, encoder: <model id>, top_k, phrase_hash, rooms: {<room>: {producers_ran: [...], shown: n}}, items: [...] } (temp then rename). Export guardRoomPath, exportUnstamped, wilson95, joinJudgments, computeRates. Run green. Commit `feat(355-24): fixture-only hit-rate measurement script (HIPS-07)`.</action>
  <verify>
    <automated>node tests/test-355-hit-rate-record.cjs; test $? -eq 0 -o $? -eq 77</automated>
  </verify>
  <acceptance_criteria>
    - The test exits 0 or 77 (77 only for the record legs) with every guard / export / Wilson / rate leg PASS
    - `node scripts/measure-355-hit-rate.cjs export --unstamped --room "$(mktemp -d)"; test $? -ne 0` succeeds (refused)
    - `grep -vE '^\s*(//|\*|/\*)' scripts/measure-355-hit-rate.cjs | grep -cE "MindrianRooms|homedir\(\)"` prints 0
  </acceptance_criteria>
  <done>A measurement tool that can only ever touch temp copies of the three fixture rooms.</done>
</task>

<task type="auto">
  <name>Task 2: Export the unstamped shown pairings for all three rooms (real encoder, dev machine)</name>
  <files>tests/fixtures/355-rooms/pairings.items.json</files>
  <read_first>
    - scripts/measure-355-hit-rate.cjs (Task 1)
    - `ls ~/.mindrian/model-cache/MongoDB/mdbr-leaf-ir` (encoder present) and `git ls-files tests/fixtures/355-rooms | grep -c stamp` (must be 0: no stamped output exists yet, D-32)
  </read_first>
  <action>Run `node scripts/measure-355-hit-rate.cjs export --unstamped --top 10` once. If a room yields fewer than 20 shown pairings, re-run with a larger --top (the same K for every producer and room) and record the K used; never add pairs by hand. If the encoder is missing, stop and report BLOCKED (an identity fallback would make every HSI pair the same direction and the measurement meaningless). Verify `git status --short tests/fixtures/355-rooms` shows only the new items file (engines ran on temp copies). Record in the SUMMARY: producers that ran per room, shown counts, K, encoder id, eureka substrate status. Commit `data(355-24): unstamped shown pairings for the three fixture rooms (HIPS-07, D-32)`.</action>
  <verify>
    <automated>node -e "const f=require('./tests/fixtures/355-rooms/pairings.items.json');const by={};for(const i of f.items)by[i.room]=(by[i.room]||0)+1;const r=Object.keys(by);if(r.length!==3||r.some(k=>by[k]<20)||f.items.some(i=>'stamp' in i||'stamp_lines' in i||'verification' in i))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 (three rooms, >= 20 items each, no stamp field)
    - `git status --short tests/fixtures/355-rooms/room-*` is empty after the run
  </acceptance_criteria>
  <done>The baseline the navigator judges is exactly today's raw output, unstamped.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Sitting 1 - the navigator judges the unstamped pairings blind</name>
  <files>tests/fixtures/355-rooms/judgments.json, .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/labeling-session-pairings-unstamped.json</files>
  <read_first>
    - scripts/label-355-gold.cjs usage (`--help`)
  </read_first>
  <action>Ask the navigator to judge in a real terminal. On resume, verify and commit the judgments with the session file as `docs(355-24): navigator blind sitting-1 judgments (unstamped baseline)` (git add -f for the phase-dir session), confirming no pairings-stamped file exists at that commit.</action>
  <what-built>tests/fixtures/355-rooms/pairings.items.json (>= 60 unstamped pairings across three rooms)</what-built>
  <how-to-verify>
    1. Do NOT open tests/fixtures/355-rooms/planted-cases.json first.
    2. Run `node scripts/label-355-gold.cjs start --set pairings-unstamped` (resume with `resume --set pairings-unstamped`).
    3. For each pairing answer three y/n prompts: useful to someone working this room? direction phrase right? already known?
    4. When `status --set pairings-unstamped` shows all labeled, run `node scripts/label-355-gold.cjs emit --set pairings-unstamped` and reply "judged".
  </how-to-verify>
  <verify><automated>node -e "const j=require('./tests/fixtures/355-rooms/judgments.json');const i=require('./tests/fixtures/355-rooms/pairings.items.json');const h=require('node:crypto').createHash('sha256').update(require('node:fs').readFileSync('tests/fixtures/355-rooms/pairings.items.json')).digest('hex');if(!j.labeled_at||j.fixture_sha256!==h||j.judgments.length!==i.items.length||j.judgments.some(x=>typeof x.useful!=='boolean'||typeof x.direction_ok!=='boolean'||typeof x.already_known!=='boolean'))process.exit(1)"</automated></verify>
  <acceptance_criteria>
    - The verify command exits 0
    - `git ls-files tests/fixtures/355-rooms | grep -c "pairings-stamped"` prints 0 at the judgments commit
  </acceptance_criteria>
  <resume-signal>Type "judged" once emit has written tests/fixtures/355-rooms/judgments.json</resume-signal>
  <done>The first human-judged baseline in the engine's history exists, taken blind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measurement script -> filesystem | Engines write into whatever room they run on |
| navigator judgment -> the phase's headline number | Blindness decides whether the number means anything |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-116 | Elevation of privilege / Tampering | --room escaping to a real room | mitigate | realpath + separator prefix guard; symlink, sibling-prefix, tmp and MindrianRooms paths refused (tested); static scan bans MindrianRooms / homedir |
| T-355-117 | Tampering | engines writing into the committed fixture tree | mitigate | cpSync to mkdtemp; git status acceptance on the room trees |
| T-355-118 | Tampering | stamp anchoring the baseline judgment | mitigate | Unstamped export and sitting 1 happen before any stamped file exists (git-order checks) |
| T-355-119 | Repudiation | padded pairings to reach 20 | mitigate | Script throws instead of padding; K recorded and uniform |
| T-355-120 | Information disclosure | room excerpts leaving the machine | accept | Fixture rooms are synthetic dev content; no egress in this plan |
</threat_model>

## Artifacts this phase produces

This plan's share:
- CLI `scripts/measure-355-hit-rate.cjs` (`export --unstamped`, `--top`, `stamp`, `record`, `--check`, `--help`; exports `guardRoomPath`, `exportUnstamped`, `wilson95`, `joinJudgments`, `computeRates`)
- Fixtures `tests/fixtures/355-rooms/pairings.items.json` (`generated_at`, `encoder`, `top_k`, `phrase_hash`, `rooms`, `items[{pair_id, room, producer, a_excerpt, b_excerpt, direction_phrase}]`), `tests/fixtures/355-rooms/judgments.json` (`labeled_at`, `fixture_sha256`, `judgments[{pair_id, useful, direction_ok, already_known, at}]`)
- Test `tests/test-355-hit-rate-record.cjs`

<verification>
- `node tests/test-355-hit-rate-record.cjs` exits 0 or 77; the Task 3 verify exits 0
</verification>

<success_criteria>
- >= 20 shown pairings per room judged blind, unstamped, before any stamp exists
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-24-SUMMARY.md` when done.
</output>
