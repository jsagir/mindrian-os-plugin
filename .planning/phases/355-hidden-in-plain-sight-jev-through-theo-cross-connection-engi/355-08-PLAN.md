---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 08
type: tdd
wave: 2
depends_on: ["355-01"]
files_modified:
  - tests/test-355-framework-names.cjs
  - scripts/refresh-framework-names.cjs
  - data/framework-names.json
autonomous: true
requirements: [HIPS-04]
tags: [framework-names, theo, snapshot, part8, d-51]

must_haves:
  truths:
    - "D-51: data/framework-names.json is regenerated this phase from ONE live Theo list_frameworks read through lib/core/brain-client.cjs (askOp, generic handles only; never the raw theo server), as a dated, hashed snapshot in the SAME shape (framework_names + curated_extras) with snapshot_date and source_sha256 recorded"
    - "D-51: the three stale entries (Falsifiability, Safe fail culture, Seven Da Vincian Principles) are each reviewed and either dropped or kept with a written note in a stale_review array; a name any commands/*.md frontmatter still references is kept with a note, never silently dropped"
    - "D-51: --check is offline: it reads only the committed snapshot, recomputes source_sha256 over the sorted framework_names, validates the shape, and never requires brain-client"
    - "Spike-findings IP ruling: only framework NAMES are written to the repo; no description, category text or internal record id is committed"
    - "D-10: the regenerated snapshot is what verification-stamp.cjs resolves against (framework_names UNION curated_extras), so resolution coverage rises from about a quarter of canon to the live list; staleness remains a disclosed limitation"
    - "node scripts/build-command-registry.cjs --check and node scripts/build-connector-registry.cjs --check exit 0 after the snapshot changes (the pre-commit legs this file triggers)"
  artifacts:
    - path: "scripts/refresh-framework-names.cjs"
      provides: "--live (one brain-client read) / --check (offline) snapshot builder"
      exports: ["buildSnapshot", "validateSnapshot", "namesSha256"]
    - path: "data/framework-names.json"
      provides: "Dated, hashed canon-name snapshot"
      contains: "source_sha256"
    - path: "tests/test-355-framework-names.cjs"
      provides: "Offline proof of shape, hash, stale review and registry compatibility"
  key_links:
    - from: "scripts/refresh-framework-names.cjs --live"
      to: "lib/core/brain-client.cjs askOp('list_frameworks')"
      via: "lazy require inside the live branch only"
      pattern: "askOp\\('list_frameworks'"
    - from: "lib/core/verification-stamp.cjs loadFrameworkNames"
      to: "data/framework-names.json"
      via: "framework_names UNION curated_extras"
      pattern: "curated_extras"
---

<objective>
D-51 (HIPS-04 resolution coverage): the stamp resolves a finding's ends against `data/framework-names.json`, which today holds 112 names from 2026-05-12 (3 stale, about a quarter of canon). Regenerate it once from a live Theo `list_frameworks` read through `brain-client.cjs`, keep its shape, date and hash it, review the stale entries, and make the build offline-checkable.

Purpose: an `unverified / handle_unresolved` stamp should mean "not a canon framework", not "our list was four months old".
Output: `scripts/refresh-framework-names.cjs`, the regenerated snapshot, `tests/test-355-framework-names.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.claude/skills/spike-findings-MindrianOS-Plugin/SKILL.md
@.claude/skills/spike-findings-MindrianOS-Plugin/references/section-framework-ledger.md
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/refresh-framework-names.cjs with offline --check, test first</name>
  <files>tests/test-355-framework-names.cjs, scripts/refresh-framework-names.cjs</files>
  <read_first>
    - data/framework-names.json (whole file: snapshot_note, snapshot_date, framework_names, curated_extras_note, curated_extras)
    - scripts/build-command-registry.cjs lines 20-60 and 430-520 (how --refresh-names and the unresolvable-framework validation read this file; the union rule)
    - lib/core/brain-client.cjs askOp doc comment near :1535-1560 (list_frameworks params { limit? } -> rows { name, description, category }; degraded shape)
    - .claude/skills/spike-findings-MindrianOS-Plugin/references/section-framework-ledger.md (ROW_CAP returns a text-only notice with zero rows; page by bucket, never SKIP)
    - scripts/build-section-command-ledger.cjs pullTheoFrameworks (READ ONLY; the bucketed fallback pattern if list_frameworks is capped)
  </read_first>
  <behavior>
    - buildSnapshot({ rows, prior, date }) returns { snapshot_note, snapshot_date, source: 'theo list_frameworks via lib/core/brain-client.cjs askOp', source_sha256, framework_names (sorted, unique, trimmed, each 1-128 chars), curated_extras_note, curated_extras (prior curated_extras minus any name now in framework_names), stale_review }
    - namesSha256(names) === sha256 of JSON.stringify(sorted names)
    - buildSnapshot never copies description, category or any key other than name from the rows
    - validateSnapshot(snapshot) passes on a well-formed snapshot and fails on: unsorted names, a duplicate, a name over 128 chars, a source_sha256 mismatch, a missing snapshot_date, a stale_review entry without decision in {'dropped','kept'} or without note
    - `--check` exits 0 on the committed file and the check path never loads brain-client (test asserts require.cache has no brain-client entry after running validate in-process)
    - `--live` with an injected askOp stub returning { degraded: true } or fewer than 105 rows writes nothing and exits non-zero with a message naming the reason
  </behavior>
  <action>RED: write tests/test-355-framework-names.cjs (hygiene-355 preamble) per the behavior list, driving the exported functions with in-memory rows and an injected `deps.askOp`; the `--check` leg runs `spawnSync(process.execPath, ['scripts/refresh-framework-names.cjs','--check'])`, and a registry leg runs `node scripts/build-command-registry.cjs --check` and `node scripts/build-connector-registry.cjs --check`. Run; it fails. GREEN: create scripts/refresh-framework-names.cjs ('use strict', BSL header, Phase 355 D-51 citation, "names only: never commit a Theo description (IP ruling)", hyphens only; argv switch-case `--check` / `--live` / `--help`). `--live`: lazily require lib/core/brain-client.cjs, call `askOp('list_frameworks', { limit: 1000 })`; if the result is degraded, text-only or returns fewer than 105 names, fall back to the bucketed read pattern from build-section-command-ledger.cjs pullTheoFrameworks (require it if exported, else copy the Cypher pattern; names only); if that also fails, write nothing and exit 2. Build the snapshot with the stale_review computed as: for each name in the prior snapshot that is not in the live set, `grep`-equivalent scan of commands/*.md frontmatter `frameworks:` values; referenced -> { name, decision: 'kept', note: 'still referenced by <command>; not a live Framework as of <date>' } and the name stays in curated_extras; unreferenced -> { name, decision: 'dropped', note: 'not a live Framework as of <date>' }. Write atomically (temp then rename). Update snapshot_note to say the list is every live :Framework name (not only FEEDS_INTO-linked) and how to refresh. Run the test green (the --check leg is expected to fail until Task 2 writes the new-shape file; mark it with `(green after Task 2)`). Commit `feat(355-08): framework-names snapshot builder with offline --check (D-51)`.</action>
  <verify>
    <automated>node -e "const r=require('./scripts/refresh-framework-names.cjs');for(const k of ['buildSnapshot','validateSnapshot','namesSha256'])if(typeof r[k]!=='function')process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify one-liner exits 0
    - `node tests/test-355-framework-names.cjs` passes every leg except those labelled `(green after Task 2)`
    - `grep -vE '^\s*(//|\*|/\*)' scripts/refresh-framework-names.cjs | grep -c "description"` prints 0 outside the never-copy assertion comment
  </acceptance_criteria>
  <done>The builder exists and cannot commit anything but names.</done>
</task>

<task type="auto">
  <name>Task 2: One live refresh through brain-client, stale review, registries still green</name>
  <files>data/framework-names.json</files>
  <read_first>
    - scripts/refresh-framework-names.cjs (Task 1)
    - `git status --short data/framework-names.json data/command-registry.json data/connector-registry.json` (must be empty; collision rule)
  </read_first>
  <action>Run `node scripts/refresh-framework-names.cjs --live` once (Theo through brain-client only; the call carries no room data). If it exits non-zero because Theo is unreachable or degraded, do NOT hand-write the file: record the failure (time, reason) in the SUMMARY and stop this task as BLOCKED so the orchestrator can retry; 355-06 and later plans keep working against the old snapshot. On success run `node scripts/refresh-framework-names.cjs --check`, `node scripts/build-command-registry.cjs --check`, `node scripts/build-connector-registry.cjs --check` and the full test. Record in the SUMMARY: live name count, how many old names stayed, how many were added, the three stale decisions, snapshot_date and source_sha256. Commit `data(355-08): regenerate framework-names from live Theo list_frameworks (D-51)`.</action>
  <verify>
    <automated>node scripts/refresh-framework-names.cjs --check && node tests/test-355-framework-names.cjs && node scripts/build-command-registry.cjs --check && node scripts/build-connector-registry.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - All four verify commands exit 0
    - `node -e "const s=require('./data/framework-names.json');if(!s.source_sha256||!s.snapshot_date||s.framework_names.length<105||!Array.isArray(s.stale_review)||s.stale_review.length!==3)process.exit(1)"` exits 0
    - `node -e "const s=JSON.stringify(require('./data/framework-names.json'));if(/brainrecord|description/i.test(s))process.exit(1)"` exits 0
  </acceptance_criteria>
  <done>The resolver works against a dated, hashed, live list of canon names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin -> Theo (askOp list_frameworks) | A read-only generic op with an integer limit; no room data |
| Theo response -> committed repo data | Brain content could leak into a shipped file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-38 | Information disclosure (Brain IP) | descriptions committed to data/ | mitigate | buildSnapshot copies `name` only; acceptance greps the file for description/internal ids |
| T-355-39 | Tampering | partial list written after a capped read | mitigate | Degraded / text-only / fewer than 105 names -> write nothing, exit 2; bucketed fallback never uses SKIP |
| T-355-40 | Repudiation | which snapshot a stamp resolved against | mitigate | snapshot_date + source_sha256 recorded; --check recomputes the hash |
| T-355-41 | Denial of service | registry validation broken by dropped names | mitigate | Referenced stale names are kept with a note; build-command-registry --check gate |
| T-355-42 | Information disclosure | room data on the wire | accept | The op carries only `limit`; brain-client's Part 8 belt still applies |
</threat_model>

## Artifacts this phase produces

This plan's share:
- CLI `scripts/refresh-framework-names.cjs` (`--live`, `--check`, `--help`; exports `buildSnapshot`, `validateSnapshot`, `namesSha256`)
- Snapshot fields in `data/framework-names.json`: `snapshot_date`, `source`, `source_sha256`, `framework_names`, `curated_extras`, `stale_review[{name, decision, note}]`
- Test `tests/test-355-framework-names.cjs`

<verification>
- `node scripts/refresh-framework-names.cjs --check` exits 0; registry gates exit 0
</verification>

<success_criteria>
- A live, dated, hashed, names-only snapshot; stale names decided in writing
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-08-SUMMARY.md` when done.
</output>
