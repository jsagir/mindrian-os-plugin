---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 16
type: execute
wave: 4
depends_on: ["355-04", "355-06", "355-09", "355-11", "355-12"]
files_modified:
  - tests/test-355-producer-whitespace-hsi.cjs
  - tests/fixtures/355/producers/whitespace-gaps.json
  - tests/fixtures/355/producers/whitespace-novelty.json
  - scripts/whitespace-command.cjs
  - scripts/whitespace-to-graph.cjs
  - commands/whitespace.md
  - skills/whitespace/SKILL.md
  - scripts/hsi-to-graph.cjs
  - commands/scout.md
  - skills/scout/SKILL.md
  - data/floor-ledger.json
autonomous: true
requirements: [HIPS-04, HIPS-05, HIPS-02]
tags: [stamp-wiring, whitespace, hsi, no-decimal, output-layer]

must_haves:
  truths:
    - "D-08 / SPEC Req 4: whitespace and HSI are stamped at the OUTPUT layer only (whitespace-command.cjs, whitespace-to-graph.cjs, hsi-to-graph.cjs); the Python whitespace compute and the HSI engine compute are untouched"
    - "D-48 / D-49: whitespace zone endpoints are the zone's nearest_frameworks[0] and [1]; a novelty row's endpoints are the artifact's carried name (frontmatter framework:, methodology:, title) and its nearest_concept; HSI pair endpoints are the two artifacts' carried names; whitespace findings carry direction 'none'; HSI findings carry classify(lsa_sim, semantic_sim)"
    - "D-29: every decimal the whitespace CLI prints is replaced: the zone density column and footer become a sparsity rank (1 = sparsest, an integer, no new threshold), zone analyze's Density line becomes 'sparsity rank n of N', novelty scores become the band word novel / moderate / covered from the existing (ledgered) 0.8 / 0.4 bands, and the 'Novel (>0.8)' / 'Covered (<0.4)' label text becomes words; the novelty math is unchanged"
    - "D-27 / D-30: each shown finding prints formatStampLines(stamp, 'cli') and each render ends with disclosureLine(producer); captured stdout of every whitespace subcommand that renders findings, and of hsi-to-graph --stamp, contains no match for the D-30 decimal regex and no percentage on stamp or evidence lines, with Theo up (replay) and Theo down (null)"
    - "D-16: stamp fields ride the existing lazygraph-ops / node-insert writes as flat props (toNodeProps) on WhitespaceZone nodes and on the shown HSI_CONNECTION edges; the Part 9 cleanup of those writers is NOT widened here; no new raw INSERT line is added (check-substrate --diff clean)"
    - "Pitfall 7 / D-12: Theo is awaited BEFORE any BEGIN in whitespace-to-graph and hsi-to-graph; stamping is opt-in via --stamp so background callers (the cascade and the cadence runner) make no Theo call; the interactive /mos:scout and /mos:whitespace docs pass --stamp"
    - "100% of shown findings carry a stamp: for each render, rendered rows == findings == stamp blocks, and unresolved ends still render under their original names with the unverified advice"
  artifacts:
    - path: "tests/test-355-producer-whitespace-hsi.cjs"
      provides: "Coverage, no-decimal, disclosure and node-prop proofs for two producers, Theo up and down"
      min_lines: 120
    - path: "scripts/whitespace-command.cjs"
      provides: "Pure render seams renderScanLines / renderAnalyzeLines / renderNoveltyLines taking stamps"
      exports: ["renderScanLines", "renderAnalyzeLines", "renderNoveltyLines", "whitespaceEndpoints"]
    - path: "scripts/hsi-to-graph.cjs"
      provides: "--stamp: stamp the top shown pairs before the transaction, print stamped findings"
      exports: ["main", "renderHsiFindings", "hsiEndpoints"]
  key_links:
    - from: "scripts/whitespace-command.cjs"
      to: "lib/core/verification-stamp.cjs stampFindings"
      via: "await before render"
      pattern: "stampFindings"
    - from: "scripts/hsi-to-graph.cjs"
      to: "lib/core/verification-stamp.cjs toNodeProps"
      via: "stamp props merged into the shown HSI_CONNECTION edge properties"
      pattern: "toNodeProps"
---

<objective>
Wire the stamp and the floor disclosure into two of the five producers (HIPS-04, HIPS-05, HIPS-02): whitespace and HSI. Both get the stamp at their output layer only (D-08), lose every rendered decimal (D-29, D-30), and store the stamp on the nodes/edges they already write (D-16, D-37 props). Theo is called only on interactive runs and always before a transaction opens.

Output: render seams and --stamp paths in three scripts, four doc updates, two recorded producer fixtures, one test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@lib/core/verification-stamp.cjs
@lib/core/verification-stamp-format.cjs
@lib/core/floor-disclosure.cjs
@tests/helpers/theo-replay-355.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: whitespace-command render seams, stamps, band words, disclosure; the producer test</name>
  <files>tests/test-355-producer-whitespace-hsi.cjs, tests/fixtures/355/producers/whitespace-gaps.json, tests/fixtures/355/producers/whitespace-novelty.json, scripts/whitespace-command.cjs, data/floor-ledger.json</files>
  <read_first>
    - scripts/whitespace-command.cjs (whole file: every console.log that prints a number; the map / analyze / score / tree / external / discover subcommands; main() at the bottom; `grep -n "toFixed\|density\|novelty" scripts/whitespace-command.cjs`)
    - scripts/compute-whitespace-gaps.py output shape (READ ONLY): gap objects (zone_id / gap_id, density_score, problem_type, nearest_frameworks, validated) and novelty rows (artifact, section, novelty_score, nearest_concept)
    - tests/fixtures/355/theo-stub-responses.json (use its name pairs for resolvable endpoints)
    - data/floor-ledger.json whitespace rows (anchors must still resolve after the label text goes)
  </read_first>
  <action>Author tests/fixtures/355/producers/whitespace-gaps.json (>= 5 zones, nearest_frameworks drawn from the stub fixture's pairs for 3 zones, unresolvable names for 1, a single name for 1) and whitespace-novelty.json (>= 6 rows spanning the three novelty bands, some nearest_concept values that are canon names in the stub fixture). Write tests/test-355-producer-whitespace-hsi.cjs (hygiene-355 preamble) with a whitespace section (every check label starts with `whitespace `; the Task 3 HSI section's labels start with `hsi `): for each render seam, with makeReplayCallTool(stub fixture) and with makeNullCallTool, assert rendered rows == findings == stamp blocks (every block from formatStampLines), direction phrase is NONE_MEANING, disclosureLine('whitespace') is the last line, no line matches `(^|[^0-9A-Za-z.])(0?\.[0-9]+|1\.0+)([^0-9.]|$)`, no `[0-9]+%` on stamp or evidence lines, Theo-down rows all say backend unavailable and carry the advice line, a planted 0.87 in a zone_id is caught by the leg (negative control). Then edit scripts/whitespace-command.cjs: add pure exported seams `whitespaceEndpoints(finding, kind)` (zone -> nearest_frameworks[0..1]; novelty -> extractCarried(artifact text) resolved, and nearest_concept), `renderScanLines(gaps, stamps)`, `renderAnalyzeLines(zone, stamp, rank, total)`, `renderNoveltyLines(scores, stamps)`; make the map / analyze / score subcommands async: resolve endpoints, `await stampFindings(...)` (per-run memo, pool 4), then print the seam's lines; replace every decimal print per the D-29 truth (sparsity rank, band words novel / moderate / covered, summary counts in words); inventory tree / external / discover: any decimal they print is replaced by a word or rank, and any of them that renders pair findings gets stamps the same way (record the inventory in the SUMMARY); make main() await the async subcommands; export the seams under `require.main === module` guarding. Keep every compute line and the 0.8 / 0.4 comparisons unchanged. Run the floor sweep; if a whitespace row's anchor went stale, update only that row's line_anchor in data/floor-ledger.json to a regex matching the surviving comparison. Commit `feat(355-16): whitespace findings stamped, no decimals, floors disclosed (HIPS-04, HIPS-05, D-29)`.</action>
  <verify>
    <automated>! (node tests/test-355-producer-whitespace-hsi.cjs 2>&1 | grep -qE "^FAIL: whitespace ") && node tests/test-355-floor-sweep.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Every whitespace-section check in tests/test-355-producer-whitespace-hsi.cjs passes (HSI section may still fail until Task 3)
    - `grep -vE '^\s*(//|\*|/\*)' scripts/whitespace-command.cjs | grep -c "toFixed("` prints 0
    - `grep -cE "Novel \(>0\.8\)|Covered \(<0\.4\)" scripts/whitespace-command.cjs` prints 0
    - `node tests/test-355-floor-sweep.cjs` exits 0
  </acceptance_criteria>
  <done>Every whitespace finding a user sees carries a stamp and no number.</done>
</task>

<task type="auto">
  <name>Task 2: whitespace-to-graph --stamp and the whitespace docs (Tri-Polar)</name>
  <files>scripts/whitespace-to-graph.cjs, commands/whitespace.md, skills/whitespace/SKILL.md, tests/test-355-producer-whitespace-hsi.cjs</files>
  <read_first>
    - scripts/whitespace-to-graph.cjs (whole file: main(), the lazygraph-ops addWhitespaceZone write path near :132 / :184, any BEGIN)
    - commands/whitespace.md lines 120-420 and skills/whitespace/SKILL.md (render rules for map / analyze / score; any instruction to show density or novelty numbers)
    - skills/larry-personality/SKILL.md (voice rules: hedged, no numbers)
    - lib/core/verification-stamp-format.cjs formatUncheckedDesktop (D-50)
  </read_first>
  <action>whitespace-to-graph.cjs: add a `--stamp` flag; when set, resolve zone endpoints with whitespaceEndpoints, `await stampFindings` BEFORE any write or BEGIN, and merge toNodeProps(stamp) into each WhitespaceZone's existing properties through the SAME lazygraph-ops call (no new writer, no raw SQL, cite D-16); without the flag behavior is byte-identical (background callers unchanged). Export `main(argv, deps)` with deps.callTool for tests. Add a test leg: run main on a mkdtemp room built from the recorded gaps with --stamp and a replay callTool, then read the zone nodes back and assert each carries verification / backend / direction / judge props that re-parse through fromNodeProps; without --stamp no zone carries them. commands/whitespace.md and skills/whitespace/SKILL.md: in the map / analyze / score render rules replace the numeric columns with the sparsity rank and band words, instruct Larry to reproduce the stamp block lines verbatim and the disclosure line, never to add a number (Canon Part 12), and on Desktop / Cowork when no stored stamp exists to say exactly "Not yet checked; run the CLI to verify." (D-50); where these docs invoke whitespace-to-graph interactively add `--stamp`. Run the pre-commit gates for commands/ and skills/ (build-command-registry --check, build-connector-registry --check, build-orchestration-projection --check, check-shape-declaration --check, check-help-coverage). Commit `feat(355-16): whitespace zones store their stamp; docs render stamps, no numbers (HIPS-05, D-16, D-50)`.</action>
  <verify>
    <automated>node scripts/build-command-registry.cjs --check && node scripts/build-connector-registry.cjs --check && node scripts/check-shape-declaration.cjs --check && ! (node tests/test-355-producer-whitespace-hsi.cjs 2>&1 | grep -qE "^FAIL: whitespace ")</automated>
  </verify>
  <acceptance_criteria>
    - Registry and shape gates exit 0; no whitespace FAIL remains in the producer test
    - `grep -c "Not yet checked; run the CLI to verify." commands/whitespace.md skills/whitespace/SKILL.md` shows >= 1 in each file
    - `git diff -U0 scripts/whitespace-to-graph.cjs | grep '^+' | grep -cE "INSERT INTO|openGraph\(|DatabaseSync"` prints 0
  </acceptance_criteria>
  <done>Whitespace zones carry their stamp in the graph, and every surface renders it the same way.</done>
</task>

<task type="auto">
  <name>Task 3: hsi-to-graph --stamp prints stamped HSI findings; scout docs pass --stamp</name>
  <files>scripts/hsi-to-graph.cjs, commands/scout.md, skills/scout/SKILL.md, tests/test-355-producer-whitespace-hsi.cjs</files>
  <read_first>
    - scripts/hsi-to-graph.cjs (whole file after 355-12: main(), the MOAT-01 BEGIN, the HSI_CONNECTION write with the re-derived surprise_type, any stdout)
    - tests/fixtures/272/room/.hsi-results.json (input shape: pair ids, lsa_sim, semantic_sim, hsi_score; how pair ids map to artifact paths)
    - commands/scout.md and skills/scout/SKILL.md 5b block and "Report:" list (after 355-11)
  </read_first>
  <action>hsi-to-graph.cjs: add `--stamp` and `--top <n>` (default 10). With --stamp: choose the shown set = the top-n pairs by the existing hsi_score order (no new threshold), resolve each end's carried name from the artifact file (extractCarried; D-48), direction = classify(lsa_sim, semantic_sim), `await stampFindings` BEFORE the MOAT-01 BEGIN, merge toNodeProps(stamp) into those pairs' HSI_CONNECTION edge properties in the SAME existing write (cite D-16), and after COMMIT print, per shown pair, `<A title> and <B title>` followed by formatStampLines(stamp, 'cli'), then disclosureLine('hsi'); print no hsi_score or similarity number. Without --stamp: byte-identical behavior (the cascade and cadence callers stay Theo-free). Export `main(argv, deps)`, `renderHsiFindings(pairs, stamps)` and `hsiEndpoints(pair, roomDir)`. Add the HSI section to the producer test: run main on a mkdtemp copy of the 272 room with --stamp and replay / null callTools; assert shown pairs == stamp blocks == min(top, pairs), no decimal / percent on any stdout line, the disclosure line last, and the shown edges' props re-parse with fromNodeProps while non-shown edges carry none. scout.md and skills/scout/SKILL.md: step 3 becomes `node "${PLUGIN_ROOT}/scripts/hsi-to-graph.cjs" "$ROOM_DIR" --stamp` (keep the D-03 advisory wrapper), and the Report list tells Larry to report hsi-to-graph's stamped lines verbatim, never numbers from .hsi-results.json, and on Desktop / Cowork without a stored stamp to say "Not yet checked; run the CLI to verify." Run the producer test, tests/test-reverse-salient-telemetry.cjs, tests/test-355-direction-readers.cjs, the registry / shape gates and `node scripts/check-substrate.cjs --diff`. Commit `feat(355-16): HSI findings stamped at the output layer (HIPS-04, HIPS-05, D-08, D-16)`.</action>
  <verify>
    <automated>node tests/test-355-producer-whitespace-hsi.cjs && node tests/test-reverse-salient-telemetry.cjs && node tests/test-355-direction-readers.cjs && node scripts/build-command-registry.cjs --check && node scripts/check-shape-declaration.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-355-producer-whitespace-hsi.cjs` exits 0 with `FAIL: 0`
    - `grep -c "hsi-to-graph.cjs\" \"\$ROOM_DIR\" --stamp" commands/scout.md skills/scout/SKILL.md` shows >= 1 in each file
    - In scripts/hsi-to-graph.cjs the stampFindings call appears on a line before the first `BEGIN` (`grep -n "stampFindings\|BEGIN" scripts/hsi-to-graph.cjs` shows the stamp line number lower)
    - `git diff -U0 scripts/hsi-to-graph.cjs | grep '^+' | grep -cE "INSERT INTO|openGraph\("` prints 0
  </acceptance_criteria>
  <done>HSI findings a user reads through scout carry a stamp and no number; background runs stay offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| producer output -> user (CLI, Larry prose) | What the user reads and trusts |
| interactive CLI -> Theo | Canon names only, only with --stamp |
| stamp -> room graph writes | Stored stamps later narrated on Desktop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-75 | Information disclosure | artifact text reaching Theo | mitigate | Only resolveEndpoint results (snapshot members) reach stampFindings; artifact text is read locally for frontmatter only |
| T-355-76 | Denial of service | Theo latency in background runs or inside a transaction | mitigate | --stamp opt-in (background callers unchanged); stamps awaited before BEGIN (acceptance line-order check) |
| T-355-77 | Spoofing | a number read as a verdict | mitigate | All decimals replaced by rank / band words; D-30 regex leg with a planted negative control |
| T-355-78 | Tampering | new raw SQL in legacy writers | mitigate | Stamp props ride the existing write; check-substrate --diff and added-line greps |
| T-355-79 | Repudiation | a finding dropped because it could not be checked | mitigate | rows == findings == stamp blocks, Theo up and down |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Seams in `scripts/whitespace-command.cjs`: `whitespaceEndpoints`, `renderScanLines`, `renderAnalyzeLines`, `renderNoveltyLines` (subcommands map / analyze / score now async)
- Flags `--stamp` on `scripts/whitespace-to-graph.cjs` and `scripts/hsi-to-graph.cjs`; `--top <n>` on hsi-to-graph; exports `main(argv, deps)`, `renderHsiFindings`, `hsiEndpoints`
- Stamp props (`verification`, `backend`, `direction`, `judge`, `reason`, `path`, `path_labels`, `path_edges`, `path_len`) on `WhitespaceZone` nodes and shown `HSI_CONNECTION` edges
- Fixtures `tests/fixtures/355/producers/whitespace-gaps.json`, `tests/fixtures/355/producers/whitespace-novelty.json`
- Test `tests/test-355-producer-whitespace-hsi.cjs`

<verification>
- Producer test, readers test, telemetry test, floor sweep and registry gates green
</verification>

<success_criteria>
- Whitespace and HSI findings carry stamps on every surface, no decimals, floors disclosed, compute untouched
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-16-SUMMARY.md` when done.
</output>
