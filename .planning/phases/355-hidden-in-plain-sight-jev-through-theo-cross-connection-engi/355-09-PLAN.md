---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 09
type: execute
wave: 3
depends_on: ["355-02"]
files_modified:
  - lib/core/rs-math.cjs
  - lib/core/hsi-lsa.cjs
  - lib/core/hsi-engine.cjs
  - tests/272-hsi-lsa-algorithm.test.cjs
  - lib/core/rs-innovation-classifier.cjs
  - lib/core/rs-thesis-generator.cjs
  - scripts/rs-discovery-engine.cjs
  - lib/memory/test-rs-innovation-classifier.cjs
autonomous: true
requirements: [HIPS-01]
tags: [direction-convention, hsi, rs-math, classifier, test-amendment]

must_haves:
  truths:
    - "D-03: rs-math.classifyDirection delegates to direction-convention.classifyDiff with no behavior change for finite inputs; hsi-lsa and hsi-engine flip to classify(lsa, semantic) (their output inverts); the two similarity ALGORITHMS stay separate (the hsi-lsa MUST-NOT-unify warning is kept and re-scoped to the algorithms)"
    - "D-04: rs-innovation-classifier's single-axis branches both come from classify(lsa, bert) (same answer by construction when exactly one axis clears its floor), 'hybrid' is unchanged, and below-floor pairs emit 'none' instead of defaulting to structural_transfer; 'none' is a classifier-only value and never enters the eureka_critic enum"
    - "D-04: downstream classifier consumers are amended with a Phase 355 citation: rs-discovery-engine drops its structural_transfer defaults in favor of 'none' and routes 'none' pairs past thesis generation with skipped_reason 'no_direction' (the pair is kept, never dropped); rs-thesis-generator returns its existing invalid-classification envelope unchanged for anything else"
    - "D-06 / D-58: tests/272-hsi-lsa-algorithm.test.cjs and lib/memory/test-rs-innovation-classifier.cjs are amended in place with a header line citing Phase 355 D-03 / D-04, never silently; tests/272-direction-convention.test.cjs stays green unchanged"
    - "RED-to-GREEN: after this plan, legs B, C, D and F of tests/test-355-direction-agreement.cjs pass"
  artifacts:
    - path: "lib/core/hsi-engine.cjs"
      provides: "HSI pair direction through the one module"
      contains: "direction-convention"
    - path: "lib/core/rs-innovation-classifier.cjs"
      provides: "classify() with 'none' for below-floor pairs"
      contains: "'none'"
  key_links:
    - from: "lib/core/hsi-engine.cjs computeHsiMatrix"
      to: "lib/core/direction-convention.cjs classify"
      via: "require + call at the former classifyDirectionB site"
      pattern: "classify\\(lsaSim, semSim\\)"
    - from: "lib/core/rs-math.cjs classifyDirection"
      to: "lib/core/direction-convention.cjs classifyDiff"
      via: "one-line delegate"
      pattern: "classifyDiff"
---

<objective>
HIPS-01, first producer flip (D-03, D-04): make rs-math, hsi-lsa, hsi-engine and the innovation classifier take their label from `lib/core/direction-convention.cjs`. After this plan the HSI pipeline and the classifier say "structural_transfer" only when meaning is more similar than wording, which is what the origin deck meant and what rs-math always said.

Purpose: turn legs B, C, D and F of the RED agreement test green without touching any similarity algorithm.
Output: four flipped producers, two amended consumers, two amended tests, each amendment cited.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-02-SUMMARY.md
@lib/core/direction-convention.cjs
@tests/test-355-direction-agreement.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: rs-math delegates; hsi-lsa and hsi-engine flip to classify (D-03)</name>
  <files>lib/core/rs-math.cjs, lib/core/hsi-lsa.cjs, lib/core/hsi-engine.cjs, tests/272-hsi-lsa-algorithm.test.cjs</files>
  <read_first>
    - lib/core/rs-math.cjs lines 20-35 and 390-405
    - lib/core/hsi-lsa.cjs lines 1-25, 95-135 and its module.exports (~:240-250)
    - lib/core/hsi-engine.cjs lines 35-50, 85-95, 240-300 (the :272 call site inside computeHsiMatrix, the 0.6/0.4 and 0.7/0.3 weights that stay byte-unchanged)
    - tests/272-hsi-lsa-algorithm.test.cjs lines 60-100 (the Convention B pins)
    - tests/test-355-direction-agreement.cjs legs B, C, D
    - data/floor-ledger.json rows for hsi-engine (anchors must still resolve after the edit)
  </read_first>
  <action>rs-math.cjs: require `./direction-convention.cjs` and make classifyDirection(signedDiff) return `directionConvention.classifyDiff(signedDiff)`; update its doc comment to say the rule now lives in direction-convention.cjs (Phase 355 D-03) and that finite behavior is unchanged. hsi-lsa.cjs: keep computeLsaSimilarity and every algorithm line unchanged; replace classifyDirectionB's body with `return classify(lsaSim, semSim)` and rewrite its doc comment: Convention B is retired (Phase 355 D-03), the name is kept only so existing importers do not break, the direction now matches rs-math; re-scope the header MUST-NOT-unify warning to the two similarity algorithms. hsi-engine.cjs: import `classify` from `./direction-convention.cjs`, replace `classifyDirectionB(lsaSim, semSim)` at the pair-label site with `classify(lsaSim, semSim)`, drop classifyDirectionB from its hsi-lsa import, and update the header lines that described the B check; leave both weight pairs, the threshold and every other expression byte-unchanged. tests/272-hsi-lsa-algorithm.test.cjs: add a header comment "Amended Phase 355 D-03: Convention B retired; classifyDirectionB now delegates to lib/core/direction-convention.cjs (semantic higher = structural_transfer)" and flip each pinned expected label accordingly (for example classifyDirectionB(0.8, 0.3) now expects 'semantic_implementation'); change nothing else in that test. Run the four affected tests and the floor sweep. Commit `fix(355-09): hsi-lsa, hsi-engine and rs-math take the direction from one module (HIPS-01, D-03)`.</action>
  <verify>
    <automated>node tests/272-hsi-lsa-algorithm.test.cjs && node tests/272-direction-convention.test.cjs && node tests/272-rs-engine-contract.test.cjs && node tests/test-355-floor-sweep.cjs && ! (node tests/test-355-direction-agreement.cjs 2>&1 | grep -qE "^FAIL: (B|C|D) ")</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/272-hsi-lsa-algorithm.test.cjs`, `node tests/272-direction-convention.test.cjs`, `node tests/272-rs-engine-contract.test.cjs` and `node tests/test-355-floor-sweep.cjs` exit 0
    - `node tests/test-355-direction-agreement.cjs` prints no FAIL line starting with `B `, `C ` or `D `
    - `grep -c "Phase 355" tests/272-hsi-lsa-algorithm.test.cjs` >= 1
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/hsi-engine.cjs | grep -c "classifyDirectionB"` prints 0
    - `git diff -U0 lib/core/hsi-engine.cjs | grep '^[-+]' | grep -cE "0\.6 \*|0\.4 \*|0\.7 \*|0\.3 \*"` prints 0 (weights untouched)
  </acceptance_criteria>
  <done>The HSI pipeline and rs-math agree with the module on every fixture pair.</done>
</task>

<task type="auto">
  <name>Task 2: The classifier emits 'none' below floor; consumers amended (D-04)</name>
  <files>lib/core/rs-innovation-classifier.cjs, lib/core/rs-thesis-generator.cjs, scripts/rs-discovery-engine.cjs, lib/memory/test-rs-innovation-classifier.cjs</files>
  <read_first>
    - lib/core/rs-innovation-classifier.cjs (whole file: floors, CLASSIFICATIONS, BRIDGE_CONCEPT_BY_CLASSIFICATION, computeBridgeConcept, the rule block, exports)
    - scripts/rs-discovery-engine.cjs around :117, :520-590 and :640-660 (the classifier call, the hard-coded 'structural_transfer' at :580 and the :648 fallback)
    - lib/core/rs-thesis-generator.cjs lines 20-50 (invalid_classification envelope) and lib/core/rs-commercial-assessor.cjs lines 55-70 and 165-180 ('_fallback' template: unchanged, already safe)
    - lib/memory/test-rs-innovation-classifier.cjs (the pins at :95-170)
    - tests/test-355-direction-agreement.cjs leg F
  </read_first>
  <action>rs-innovation-classifier.cjs: import classify from ../core path `./direction-convention.cjs`; keep LSA_HIGH / BERT_HIGH and the strict `>` tests byte-unchanged; rule: both high -> 'hybrid'; exactly one high -> `classify(scored_pair.lsa, scored_pair.bert)`; neither -> 'none'. Add 'none' to CLASSIFICATIONS (4 values) and give BRIDGE_CONCEPT_BY_CLASSIFICATION a 'none' entry that yields null bridge_concept; update the header doc (lines 9-15) to cite Phase 355 D-04 and to state 'none' is classifier-only and never enters the eureka_critic enum. rs-discovery-engine.cjs: replace the structural_transfer defaults (:580 literal and the :648 fallback) with 'none' where they stand for "no classification", and route a 'none' pair past thesis generation with `skipped_reason: 'no_direction'` while keeping it in the output list (no drop); cite Phase 355 D-04 at each edit. rs-thesis-generator.cjs: add only a doc line that 'none' pairs are routed around it by rs-discovery-engine (Phase 355 D-04); do not add a mechanism for 'none'. lib/memory/test-rs-innovation-classifier.cjs: add a header comment "Amended Phase 355 D-04" and flip the pins: lsa=0.5 bert=0.1 -> 'semantic_implementation'; lsa=0.1 bert=0.5 -> 'structural_transfer'; both below floor and the exactly-0.3 case -> 'none'; hybrid unchanged. Run the classifier test, the discovery-engine and thesis tests under lib/memory (`ls lib/memory/test-rs-*.cjs`, run each), and the agreement test. Commit `fix(355-09): innovation classifier emits none below floor; consumers amended (HIPS-01, D-04)`.</action>
  <verify>
    <automated>node lib/memory/test-rs-innovation-classifier.cjs && for f in lib/memory/test-rs-*.cjs; do node "$f" || exit 1; done && node tests/test-355-floor-sweep.cjs && ! (node tests/test-355-direction-agreement.cjs 2>&1 | grep -qE "^FAIL: (B|C|D|F) ")</automated>
  </verify>
  <acceptance_criteria>
    - Every `lib/memory/test-rs-*.cjs` exits 0
    - `node tests/test-355-direction-agreement.cjs` prints no FAIL line starting with `B `, `C `, `D ` or `F `
    - `node -e "const c=require('./lib/core/rs-innovation-classifier.cjs');const o=c.classify({lsa:0.1,bert:0.1});if(o.classification!=='none')process.exit(1)"` exits 0 (adapt the argument to the classifier's real input shape if it wraps scored_pair)
    - `grep -c "Phase 355" lib/memory/test-rs-innovation-classifier.cjs` >= 1 and `grep -c "D-04" scripts/rs-discovery-engine.cjs` >= 1
  </acceptance_criteria>
  <done>No producer invents a direction for a pair that cleared neither floor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine label -> downstream consumers and stored edges | A label change alters what users read and what rooms store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-43 | Tampering | silent test rewrites | mitigate | Every amended test carries a Phase 355 D-03/D-04 header line; only expected labels change |
| T-355-44 | Spoofing (false direction) | below-floor default to structural_transfer | mitigate | 'none' emitted instead; consumers keep the pair and skip the thesis |
| T-355-45 | Denial of service | 'none' crashing thesis generation | mitigate | rs-discovery-engine routes 'none' around the thesis step; lib/memory rs tests run as the gate |
| T-355-46 | Tampering | similarity algorithm drift during the flip | mitigate | Acceptance asserts weights and algorithm lines untouched; 272 contract tests stay green |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Classifier output value `'none'` in `CLASSIFICATIONS` (`lib/core/rs-innovation-classifier.cjs`), bridge_concept null for it
- Discovery-engine field `skipped_reason: 'no_direction'` (`scripts/rs-discovery-engine.cjs`)
- `hsi-lsa.classifyDirectionB` retained as a thin delegate to `classify` (name kept for importers)

<verification>
- 272 tests, lib/memory rs tests, floor sweep green; agreement legs B, C, D, F pass
</verification>

<success_criteria>
- rs-math, hsi-lsa, hsi-engine and the classifier all emit the module's label for the same input
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-09-SUMMARY.md` when done.
</output>
