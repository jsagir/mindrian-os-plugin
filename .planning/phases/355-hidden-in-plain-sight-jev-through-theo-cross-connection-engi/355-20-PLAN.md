---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 20
type: tdd
wave: 5
depends_on: ["355-18", "355-19"]
files_modified:
  - tests/helpers/fixture-room-355.cjs
  - tests/test-355-filing.cjs
  - scripts/eureka-portfolio-report.cjs
autonomous: true
requirements: [HIPS-06]
tags: [filing, opportunity, navigation, sourced-from, part9, telemetry]

must_haves:
  truths:
    - "D-36 / D-56 (C10): the stamp rides the EXISTING opportunity-minting call in bankStatements (navigation.writeOpportunityNode, never graph-ops.indexOpportunity, never a second writer); with a stamp present the call carries reason 'eureka stamped finding' and formula_version 'stamp-v1', never passes review_status (born 'proposed'), and stays off STATE_KEYS"
    - "D-37: stamp fields ride extraProps as flat enums and arrays (toNodeProps: verification, backend, direction, judge, reason, path, path_labels, path_edges, path_len) plus pws_stage (ill_defined | extend_opportunity from the room root ROOM.md frontmatter, omitted when absent) and engine_mode (the report run_mode); no DDL"
    - "D-38: after the node is minted, navigation.writeEdge writes SOURCED_FROM { relation: 'sourced_from', origin: 'eureka-355' } from the opportunity node to each end's source artifact node (falling back to the entity node when no artifact node exists, counted in the SUMMARY), beside the existing DERIVED_FROM edges; linkOpportunityEvidence and writeReasoningNode are not used for it"
    - "D-40: section is the real domain slug from deriveBankSection; the opportunity-bank/ file is still written only by qualifyCandidate after a human confirms"
    - "D-56 / Pitfall 7: every Theo call has completed (stamps come from stampRankedPairs, 355-18) before db.exec('BEGIN'); no await sits between BEGIN and COMMIT"
    - "D-53 (C4): after COMMIT the filing layer writes the v2 side channel through writeStampedSideChannel for the highest-ranked banked stamped opportunity whose guard verdict is transferable and band fires, carrying opportunity_handle = the node id, so SENS-13 has a real producer"
    - "D-43: on a tmp room, one banking run yields exactly one row type='opportunity' with review_status 'proposed' after close and reopen; its stamp props re-parse with Stamp; it has >= 2 SOURCED_FROM edges; only pre-existing node types appear; an agent-attributed promoteNodeStatus returns agent_attribution_forbidden; the added lines hold no raw INSERT INTO (Part 9)"
    - "AI-SPEC Section 7: one local memory_event 'cross_connection_stamped' { producer, finding_count, tier_counts, reason_counts, backend, judge, theo_ms_bucket } is written through navigation.cjs per banking run, exact key set, no content strings, silent under MINDRIAN_DISABLE_MEMORY_EVENT"
  artifacts:
    - path: "tests/helpers/fixture-room-355.cjs"
      provides: "buildFilingRoom(label) -> { root, roomDir, db paths, statements, stampsByKey, cleanup } built only through navigation writers"
      exports: ["buildFilingRoom"]
    - path: "tests/test-355-filing.cjs"
      provides: "The D-43 filing proof (reopen, proposed, props, SOURCED_FROM, side channel, attribution guard, Part 9, telemetry)"
      min_lines: 120
  key_links:
    - from: "scripts/eureka-portfolio-report.cjs bankStatements"
      to: "navigation.writeOpportunityNode + navigation.writeEdge SOURCED_FROM"
      via: "extraProps merge, then edges, inside the existing BEGIN/COMMIT"
      pattern: "SOURCED_FROM"
    - from: "scripts/eureka-portfolio-report.cjs (after COMMIT)"
      to: "lib/core/eureka/eureka-reach-runner.cjs writeStampedSideChannel"
      via: "opportunity_handle = minted node id"
      pattern: "writeStampedSideChannel"
---

<objective>
SPEC Req 6 filing (HIPS-06): an accepted stamped eureka finding files as a `proposed` opportunity through the writer the banking path already uses, with the stamp on the node, SOURCED_FROM provenance to the room nodes, and a real SENS-13 producer written after the transaction commits. Nothing is promoted here; 355-22 wires promotion and fire-once surfacing.

Output: a filing fixture helper, the D-43 test, the bankStatements changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PATTERNS.md
@lib/core/verification-stamp.cjs
@lib/core/navigation/CONTEXT.md
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filing fixture helper and the RED D-43 test</name>
  <files>tests/helpers/fixture-room-355.cjs, tests/test-355-filing.cjs</files>
  <read_first>
    - tests/helpers/fixture-room-219.cjs (buildFixtureRoom: seeding room.db only through navigation writers) and tests/helpers/fixture-room-354.cjs (makeScratchRoom, SKIP_EXIT_CODE)
    - scripts/eureka-portfolio-report.cjs lines 1425-1660 (BANK_SESSION_ID, the bank predicate and MINDRIAN_OPPORTUNITY_BANK_PREDICATE, deriveBankSection, bankStatements(db, sessionId, statements, opts), the writeOpportunityNode and DERIVED_FROM calls)
    - lib/core/navigation/typed-opportunity.cjs lines 90-125 and 205-290 (writeOpportunityNode params, STATE_KEYS, OPPORTUNITY_EVIDENCE_EDGE_SUBSET, born proposed)
    - lib/core/navigation/edges.cjs lines 1040-1140 (writeEdge params) and ALLOWED_EDGE_TYPES (SOURCED_FROM present)
    - lib/core/navigation/transitions.cjs lines 35-50 and 230-250 (TRUTH_CLAIM_TYPES, promoteNodeStatus agent guard)
    - tests/test-reverse-salient-telemetry.cjs (memory_event assertion idiom) and the navigation memory_event writer (`grep -n "memory_event" lib/core/navigation.cjs | head`)
  </read_first>
  <behavior>
    - buildFilingRoom('filing') returns a mkdtemp room whose room.db holds two artifact nodes, two entity nodes linked to them the way extraction links them, and a recorded statement set in which EXACTLY ONE statement passes the default bank predicate; stampsByKey maps that statement's pair key to a verified Stamp (from makeReplayCallTool over the stub fixture)
    - after bankStatements(db, BANK_SESSION_ID, statements, { stampsByKey, roomDir, runMode }) plus close and reopen: `SELECT count(*) FROM nodes WHERE type='opportunity' AND review_status='proposed'` is 1 and no other opportunity row exists
    - fromNodeProps(props of that node) parses; props.formula_version === 'stamp-v1'; props.pws_stage equals the fixture ROOM.md value; props.engine_mode equals runMode
    - >= 2 SOURCED_FROM edges have source = the node id, properties relation 'sourced_from' and origin 'eureka-355'; the pre-existing DERIVED_FROM edges are still present
    - the set of node types after banking equals the set before plus nothing new
    - <room>/.mindrian/last-eureka.json exists, validates as v2, and its opportunity_handle equals the node id
    - navigation promoteNodeStatus on that node with an agent actor returns agent_attribution_forbidden and the row still reads proposed
    - a memory_event row 'cross_connection_stamped' exists with exactly the AI-SPEC key set and no string value longer than 64 chars; with MINDRIAN_DISABLE_MEMORY_EVENT=1 banking still succeeds and writes no such event
    - static Part 9 leg: `git diff -U0 <BASE_355> -- scripts/eureka-portfolio-report.cjs` added lines contain no INSERT INTO, openGraph( or DatabaseSync
    - static order leg: inside bankStatements no `await` appears between the BEGIN line and the COMMIT line
  </behavior>
  <action>Create tests/helpers/fixture-room-355.cjs ('use strict', "seeds only through navigation writers; never raw SQL", cleanup removes only its own mkdtemp root) exporting buildFilingRoom(label). Write tests/test-355-filing.cjs (hygiene-355 preamble; exit 77 with a loud message if node:sqlite is unavailable) implementing the behavior list. Run it; it fails. Commit `test(355-20): add failing proposed-opportunity filing test (D-43)`.</action>
  <verify>
    <automated>node tests/test-355-filing.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The test runs and fails on the stamp / SOURCED_FROM / side-channel legs (banking itself already works)
    - `grep -vE '^\s*(//|\*|/\*)' tests/helpers/fixture-room-355.cjs | grep -cE "INSERT INTO|\.prepare\("` prints 0
  </acceptance_criteria>
  <done>A failing test pins the whole filing contract.</done>
</task>

<task type="auto">
  <name>Task 2: bankStatements carries the stamp, writes SOURCED_FROM, emits telemetry; side channel after COMMIT</name>
  <files>scripts/eureka-portfolio-report.cjs</files>
  <read_first>
    - scripts/eureka-portfolio-report.cjs lines 1540-1660 and the main() call site near :1258 (after 355-18, stampRankedPairs runs before it)
    - lib/core/eureka/eureka-reach-runner.cjs buildSideChannelPayload / writeStampedSideChannel param shapes (score, guard, a, b) and sensor-eureka FIRING_BANDS
    - tests/test-355-filing.cjs
  </read_first>
  <action>bankStatements: accept `opts.stampsByKey` (Map pair key -> Stamp), `opts.roomDir` and `opts.runMode`; for a statement with a stamp, extend its EXISTING writeOpportunityNode params with reason 'eureka stamped finding', formula_version 'stamp-v1' and extraProps { ...toNodeProps(stamp), pws_stage (read once from the room root ROOM.md frontmatter `pws_stage:`, only 'ill_defined' or 'extend_opportunity', else omitted), engine_mode: opts.runMode } merged with whatever extraProps it already passes (never review_status, never a STATE_KEYS key); after the node write and the existing DERIVED_FROM edges, call navigation.writeEdge({ source_id: nodeId, target_id: <each end's source artifact node id, else the entity node id>, edge_type: 'SOURCED_FROM', properties: { relation: 'sourced_from', origin: 'eureka-355' } }) for both ends; keep everything inside the existing BEGIN/COMMIT with no await inside; cite D-36..D-38, D-40, D-56. After COMMIT: pick the highest-ranked banked statement with a stamp whose critic verdict is 'transferable' and whose band is one the sensor fires on, and call writeStampedSideChannel(roomDir, { score: <its scoreMeasured output>, guard: <the guard object built from its critic verdict and confidence band in the shape probeGuard returns>, a, b, stamp, opportunityHandle: nodeId }); log (stderr, no content) when none qualifies. Then write one memory_event 'cross_connection_stamped' through navigation's memory_event writer with { producer: 'eureka', finding_count, tier_counts, reason_counts, backend (the most common backend), judge: 'none', theo_ms_bucket (one of '<1s','1-3s','3-10s','>10s' from the stamping step's elapsed time passed in opts) }, honoring MINDRIAN_DISABLE_MEMORY_EVENT. main(): pass stampsByKey (from 355-18's stampRankedPairs), roomDir and run_mode into bankStatements. Run the filing test, tests/test-355-producer-eureka.cjs, tests/test-215-score.cjs, the ranking pin, tests/test-213-sensor-eureka.cjs, tests/test-219-harvest-sensor.cjs and `node scripts/check-substrate.cjs --diff`. Commit `feat(355-20): stamped eureka findings file as proposed opportunities with SOURCED_FROM provenance (HIPS-06, D-36..D-40, D-56)`.</action>
  <verify>
    <automated>node tests/test-355-filing.cjs && node tests/test-355-producer-eureka.cjs && node tests/test-215-score.cjs && node tests/test-355-eureka-ranking-pin.cjs && node tests/test-219-harvest-sensor.cjs && node scripts/check-substrate.cjs --diff</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-355-filing.cjs` exits 0 with `FAIL: 0`; every other verify command exits 0
    - `grep -c "origin: 'eureka-355'" scripts/eureka-portfolio-report.cjs` >= 1 and `grep -c "formula_version: 'stamp-v1'" scripts/eureka-portfolio-report.cjs` >= 1
    - `grep -vE '^\s*(//|\*|/\*)' scripts/eureka-portfolio-report.cjs | grep -cE "indexOpportunity|review_status:"` prints 0 in the lines this task added (check with `git diff -U0 HEAD~1 -- scripts/eureka-portfolio-report.cjs`)
  </acceptance_criteria>
  <done>A stamped eureka finding lands as a proposed opportunity with provenance, and SENS-13 finally has a producer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine output -> room.db (Part 9 chokepoint) | Machine findings become graph nodes |
| filing layer -> side channel -> hook sensor | Local file that drives a reach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-95 | Elevation of privilege | a machine finding born confirmed | mitigate | review_status never passed (born proposed); agent-attributed promote returns agent_attribution_forbidden (tested) |
| T-355-96 | Tampering | raw SQL bypassing the chokepoint | mitigate | Only writeOpportunityNode / writeEdge; added-line Part 9 grep and check-substrate --diff |
| T-355-97 | Denial of service | Theo awaited inside the write transaction | mitigate | Stamps computed before BEGIN (355-18); static no-await-between-BEGIN-and-COMMIT leg |
| T-355-98 | Information disclosure | content in telemetry | mitigate | Exact key set, enum / count values only, 64-char cap leg, local only |
| T-355-99 | Tampering | a new node type minted | mitigate | Node-type set before == after (tested); only existing 'opportunity' type used |
| T-355-100 | Spoofing | side channel naming the wrong opportunity | mitigate | opportunity_handle = the node id just minted; validated v2 schema; written after COMMIT only |
</threat_model>

## Artifacts this phase produces

This plan's share:
- `bankStatements(db, sessionId, statements, { stampsByKey, roomDir, runMode, stampElapsedMs })` in `scripts/eureka-portfolio-report.cjs`
- Opportunity node props: `formula_version: 'stamp-v1'`, `pws_stage`, `engine_mode` plus the toNodeProps stamp fields
- Edge `SOURCED_FROM` with properties `{ relation: 'sourced_from', origin: 'eureka-355' }`
- memory_event `cross_connection_stamped` (`producer`, `finding_count`, `tier_counts`, `reason_counts`, `backend`, `judge`, `theo_ms_bucket`)
- Test helper `tests/helpers/fixture-room-355.cjs` (`buildFilingRoom`); test `tests/test-355-filing.cjs`

<verification>
- Filing test green; eureka, 215, ranking-pin, 219 tests green; check-substrate --diff clean
</verification>

<success_criteria>
- One banking run files exactly one proposed, stamped, provenance-linked opportunity and feeds SENS-13
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-20-SUMMARY.md` when done.
</output>
