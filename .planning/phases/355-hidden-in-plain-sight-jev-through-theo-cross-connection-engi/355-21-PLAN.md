---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 21
type: tdd
wave: 5
depends_on: ["355-06", "355-07", "355-15", "355-16", "355-17", "355-18"]
files_modified:
  - tests/test-355-part8-egress.cjs
  - tests/test-355-cirs-wiring.cjs
  - .planning/todos/pending/2026-09-08-autonomous-safe-posture-reversibility-audit.md
  - scripts/check-cross-connection-honesty.cjs
  - scripts/doctor.cjs
  - tests/test-355-doctor-point.cjs
autonomous: true
requirements: [HIPS-04, HIPS-08, HIPS-09, HIPS-10]
tags: [part8, egress-sweep, cirs, doctor-blocker, tripwire, d-57-file]

must_haves:
  truths:
    - "SPEC AC8 / AI-SPEC D9: tests/test-355-part8-egress.cjs proves (a) no fetch(, URL, node:http(s), curl or wget in the stamp module, the formatter, the direction module, floor-disclosure and stamp-connections (a missing target FAILS); (b) every callTool captured while stamping all recorded producer fixtures is ('find_connections', {from, to}) with exactly two keys whose values are snapshot names; (c) a planted room sentence is classified as blocked by the REAL part8-egress-guard classify() and the resulting stamp is unverified / unavailable / egress_refused; (d) no non-comment TYPESAFE_API_KEY, api.typesafe.ai, jev-devtime-client or 355 dev-script name appears under lib/ or hooks/ (scratch negative control); (e) hooks/hooks.json references no 355 script; (f) zero fetch attempts"
    - "D-44 / Pitfall 15: 355's own banned-script list lives in tests/test-355-part8-egress.cjs; tests/test-353-* and tests/test-356-tripwires.cjs are not edited"
    - "D-17: tests/test-355-cirs-wiring.cjs proves the stamp module declares POSTURE { autonomous_safe: true, reversibility 'n/a (read-only)', consequence 'low', writes 'none' }, no reach id was added (SENS-13 still rides deep_research), chain-executor.cjs never references the stamp module or stamp-connections (a stamp is never a material step, coordinated with Phase 356's Jev-seat policy), stamp-connections is not a registry command, find-connections keeps hitl_shape F.8, and the connector / projection / shape gates exit 0"
    - "D-17 folded todo: the reversibility x consequence grid gets a dated Phase 355 data point (read-only, consequence low -> the no-gate quadrant) appended to .planning/todos/pending/2026-09-08-autonomous-safe-posture-reversibility-audit.md"
    - "HIPS-10 / AI-SPEC Section 5: scripts/doctor.cjs gains the offline, zero-network blocker 'cross-connection-honesty' (applies_to ['pre-tag','full'], a DOCTOR_TEST_FAIL_POINT arm, a DOCTOR_SKIP_CROSS_CONNECTION=1 escape) that runs the Theo-unreachable self-test, the measurement --check replays, and (only if data/hsi-thinking-mode-rules.json exists) the rule-table provenance match; a missing record degrades to ok with detail not_run and never reports a number it does not have; THEO-04's advisory theo-mcp-exposure point stays advisory and is not duplicated"
    - "Open question ruled: the new blocker needs NO row in data/doctor-modules.json (that file registers the accumulative engine's organ modules; acceptance points live inline in doctor.cjs, and the icm-ruling-eval-fresh precedent has none)"
    - "D-57: scripts/doctor.cjs is re-read from 355-BASELINE.md anchors plus a fresh grep (354-13/17/18 edited it); the edit is one appended point object"
  artifacts:
    - path: "tests/test-355-part8-egress.cjs"
      provides: "Part 8 sweep + wire-shape capture + real-guard refusal + tripwires"
      min_lines: 120
    - path: "tests/test-355-cirs-wiring.cjs"
      provides: "D-17 posture and born-wired proof"
    - path: "scripts/check-cross-connection-honesty.cjs"
      provides: "checkCrossConnectionHonesty({ repoRoot, spawnImpl }) used by the doctor point and its test"
      exports: ["checkCrossConnectionHonesty"]
    - path: "tests/test-355-doctor-point.cjs"
      provides: "In-process proof of the point's pass / degrade / fail paths"
  key_links:
    - from: "scripts/doctor.cjs cross-connection-honesty"
      to: "scripts/check-cross-connection-honesty.cjs"
      via: "require + in-process call, no network"
      pattern: "cross-connection-honesty"
    - from: "tests/test-355-part8-egress.cjs"
      to: "lib/core/part8-egress-guard.cjs classify"
      via: "real guard classification of a planted sentence"
      pattern: "part8-egress-guard"
---

<objective>
The phase's standing gates (HIPS-04, HIPS-08, HIPS-09, HIPS-10): a Part 8 egress sweep over everything 355 added (SPEC AC8, D-17 "register it in the Part 8 egress sweep"), a CIRS wiring test for the stamp's posture (D-17), the folded posture-grid log, and one offline doctor blocker that fails a release if the honesty machinery stops working.

D-57: `scripts/doctor.cjs` is a shared file; re-read it first.
Output: two tests, a posture-grid entry, one checker script, one doctor point, one point test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-AI-SPEC.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-BASELINE.md
@tests/helpers/hygiene-355.cjs
@tests/helpers/theo-replay-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Part 8 egress sweep, CIRS wiring test, posture-grid entry (D-17, AC8)</name>
  <files>tests/test-355-part8-egress.cjs, tests/test-355-cirs-wiring.cjs, .planning/todos/pending/2026-09-08-autonomous-safe-posture-reversibility-audit.md</files>
  <read_first>
    - tests/run-all-224.sh Part 8 / Part 9 sweep legs and tests/test-356-tripwires.cjs (BANNED_356 idiom; READ ONLY)
    - lib/core/part8-egress-guard.cjs classify (exported) and _proveKnownToolShape (the find_connections {from, to} recognizer)
    - lib/core/verification-stamp.cjs (POSTURE, stampFindings), the four producer fixtures under tests/fixtures/355/producers/ and the seams that stamp them (whitespaceEndpoints, hsiEndpoints, eurekaEndpoints, stamp-connections parsePairs, the bottlenecks stampFn)
    - lib/core/chain-executor.cjs around the autonomous_safe default (~:526), lib/core/sensors/sensor-types.cjs REACH_IDS, commands/find-connections.md frontmatter
    - .planning/todos/pending/2026-09-08-autonomous-safe-posture-reversibility-audit.md
  </read_first>
  <action>Write tests/test-355-part8-egress.cjs (hygiene-355 preamble) with legs (a)-(f) of the first must-have truth. Leg (b): run each producer's endpoint seam and stampFindings over its recorded fixture with a capturing replay callTool; assert every captured call. Leg (c): a deps.callTool wrapper that runs the real `classify({ tool: 'find_connections', args })` (use the guard's real exported signature) and returns `{ error: 'egress_blocked' }` when it blocks; feed stampFinding a planted sentence as fromHandle (bypassing resolution on purpose, to prove the second wall) and assert egress_refused; also assert every snapshot name pairs with a fixed canon name as allow / known_tool_shape for a sample of 50 names. Leg (d): BANNED_355 = ['TYPESAFE_API_KEY', 'api.typesafe.ai', 'jev-devtime-client', 'jev-question-ceilings', 'jev-response-schema', 'label-355-gold', 'measure-hsi-thinking-mode', 'calibrate-citation-check', 'judge-355-usefulness', 'capture-355-theo-responses', 'refresh-framework-names', 'measure-355-hit-rate'] over non-comment lines of every file under lib/ and hooks/; negative control writes lib/core/__scratch_355_egress.cjs requiring '../../scripts/jev-question-ceilings.cjs', asserts it is caught, deletes it in finally. Write tests/test-355-cirs-wiring.cjs per the D-17 truth (spawn the three gates; read REACH_IDS and assert none contains 'stamp', 'verification' or 'cross_connection' and sensor-eureka REACH_ID === 'deep_research'; grep chain-executor.cjs non-comment lines for 'verification-stamp' and 'stamp-connections' = 0; data/command-registry.json has no command containing 'stamp-connections'; commands/find-connections.md frontmatter hitl_shape === 'F.8'; the todo file contains '## Data point: Phase 355 verification stamp'). Append that section to the todo file: the module makes one read-only Theo call per distinct pair, writes nothing, sends canon names only; reversibility n/a (read), consequence low, so the grid puts it in the no-gate quadrant; declared POSTURE autonomous_safe true; it is not a chain step and chain-executor never sees it (Phase 356 coordination); date and commit. Run both tests (the posture test's todo leg passes after the append). Commit `test(355-21): Part 8 egress sweep and CIRS wiring proof for the stamp (HIPS-04, HIPS-08, HIPS-09, D-17)` (git add -f the todo file).</action>
  <verify>
    <automated>node tests/test-355-part8-egress.cjs && node tests/test-355-cirs-wiring.cjs && ! ls lib/core/__scratch_355_egress.cjs 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Both tests exit 0 with `FAIL: 0` and a PASS line for the scratch negative control
    - `grep -c "Data point: Phase 355 verification stamp" .planning/todos/pending/2026-09-08-autonomous-safe-posture-reversibility-audit.md` = 1
    - `git diff --stat -- tests/test-353-tripwires.cjs tests/test-356-tripwires.cjs` is empty
  </acceptance_criteria>
  <done>The stamp's wire shape, its posture and the ban on runtime Jev are all machine-checked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The cross-connection-honesty doctor blocker (HIPS-10)</name>
  <files>scripts/check-cross-connection-honesty.cjs, scripts/doctor.cjs, tests/test-355-doctor-point.cjs</files>
  <read_first>
    - scripts/doctor.cjs: re-grep `grep -n "id: '" scripts/doctor.cjs` and read the icm-ruling-eval-fresh point in full (DOCTOR_TEST_FAIL_POINT arm, DOCTOR_SKIP_* escape, degrade rules, applies_to) plus runAcceptance near :2043 and inTestMode near :819
    - data/doctor-modules.json $schema_note (why no row is needed)
    - tests/test-doctor-acceptance-self-coverage.cjs and tests/test-doctor-acceptance.cjs (must stay green)
    - scripts/measure-hsi-thinking-mode.cjs --check exit codes (0 / 77 / 1); scripts/calibrate-citation-check.cjs may not exist yet (355-26): treat a missing script like a missing record (not_run)
  </read_first>
  <behavior>
    - checkCrossConnectionHonesty({ repoRoot, spawnImpl }) returns { ok: true, finding: null, detail } when: the Theo-unreachable self-test stamps a three-finding fixture all unverified / unavailable with no path; each --check replay exits 0 or 77 (77 and a missing script -> detail entry status 'not_run'); and, when data/hsi-thinking-mode-rules.json exists, its jev_model and fixture_sha256 equal tests/fixtures/355-hsi-measurement-record.json's
    - it returns { ok: false, finding: 'cross-connection-honesty: <which check> ...' } when the self-test yields any path or any backend other than unavailable, when a replay exits 1, or when the rule table's provenance mismatches
    - it never requires brain-client, never calls fetch (installNetGuard proves zero attempts) and never reads a vendor key
    - the doctor point with DOCTOR_TEST_MODE=1 and DOCTOR_TEST_FAIL_POINT=cross-connection-honesty reports ok false; with DOCTOR_SKIP_CROSS_CONNECTION=1 it reports ok true with detail skipped
  </behavior>
  <action>RED: write tests/test-355-doctor-point.cjs (hygiene-355 preamble) calling checkCrossConnectionHonesty in-process with an injected spawnImpl (exit 0 / 77 / 1 cases) and tmp copies for the rule-table provenance cases; plus one leg that spawns `node scripts/doctor.cjs --acceptance --json --pre-tag` with DOCTOR_TEST_MODE=1 and DOCTOR_TEST_FAIL_POINT=cross-connection-honesty (hermetic HOME per the self-coverage test's envelope, timeout 300000) and asserts the checklist entry for 'cross-connection-honesty' has ok false. Run; fail. GREEN: create scripts/check-cross-connection-honesty.cjs ('use strict', BSL header, "offline, zero network, never Jev or Theo"; the self-test uses verification-stamp stampFindings with deps.callTool returning null over three findings with snapshot names). Append one point object to doctor.cjs's acceptance list after icm-ruling-eval-fresh: id 'cross-connection-honesty', label 'Cross-connection honesty: stamps degrade honestly and the 355 measurement records replay offline', severity 'blocker', applies_to ['pre-tag','full'], run() with the DOCTOR_TEST_FAIL_POINT arm, the DOCTOR_SKIP_CROSS_CONNECTION escape and a call to checkCrossConnectionHonesty({ repoRoot }) wrapped in try/catch (a throw is ok false with the message); a comment citing HIPS-10, AI-SPEC Section 5 and the no-row ruling for data/doctor-modules.json. Run the point test, tests/test-doctor-acceptance-self-coverage.cjs, tests/test-doctor-acceptance.cjs and tests/test-doctor-acceptance-preflight-checks.cjs. Commit `feat(355-21): cross-connection-honesty acceptance blocker (HIPS-10)`.</action>
  <verify>
    <automated>node tests/test-355-doctor-point.cjs && node tests/test-doctor-acceptance-self-coverage.cjs && node tests/test-doctor-acceptance.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All three tests exit 0
    - `grep -c "id: 'cross-connection-honesty'" scripts/doctor.cjs` = 1 and `grep -c "DOCTOR_SKIP_CROSS_CONNECTION" scripts/doctor.cjs` >= 1
    - `grep -c "cross-connection-honesty" data/doctor-modules.json` prints 0 (ruled: no row)
    - `grep -vE '^\s*(//|\*|/\*)' scripts/check-cross-connection-honesty.cjs | grep -cE "brain-client|fetch\(|TYPESAFE_API_KEY"` prints 0
  </acceptance_criteria>
  <done>A release cannot ship if the stamp stops degrading honestly or a 355 record stops replaying.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin runtime -> Theo / TypeSafe | The two egress surfaces this phase touches |
| doctor acceptance -> release train | A false green would ship a lying stamp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-101 | Information disclosure | a new egress path in 355 code | mitigate | Comment-stripped sweep over every 355 runtime file; missing target fails; captured-call shape assertion |
| T-355-102 | Elevation of privilege | Jev reachable from lib/ or hooks/ | mitigate | BANNED_355 tripwire with scratch negative control; hooks.json scan |
| T-355-103 | Spoofing | the Part 8 guard not actually applied | mitigate | Leg (c) runs the REAL classify() on a planted sentence and checks the egress_refused mapping |
| T-355-104 | Repudiation | doctor point green when it measured nothing | mitigate | Degrade only with a named not_run detail; DOCTOR_TEST_FAIL_POINT arm tested |
| T-355-105 | Tampering | a stamp treated as a material chain step | mitigate | chain-executor reference scan; POSTURE declared; posture-grid entry |
| T-355-106 | Tampering | editing peer-owned tripwire files | mitigate | 355 ban list lives in its own test; acceptance asserts 353/356 tripwires untouched |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Tests `tests/test-355-part8-egress.cjs` (constant `BANNED_355`, scratch path `lib/core/__scratch_355_egress.cjs`), `tests/test-355-cirs-wiring.cjs`, `tests/test-355-doctor-point.cjs`
- Checker `scripts/check-cross-connection-honesty.cjs` (export `checkCrossConnectionHonesty`)
- Doctor blocker id `cross-connection-honesty` in `scripts/doctor.cjs`; env `DOCTOR_SKIP_CROSS_CONNECTION`, `DOCTOR_TEST_FAIL_POINT=cross-connection-honesty`
- Posture-grid entry "## Data point: Phase 355 verification stamp" in the 2026-09-08 todo

<verification>
- Part 8, CIRS and doctor-point tests plus the doctor self-coverage tests green
</verification>

<success_criteria>
- The egress surface, the posture and the release gate for 355 are all enforced by code
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-21-SUMMARY.md` when done.
</output>
