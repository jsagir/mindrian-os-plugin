---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 07
type: tdd
wave: 2
depends_on: ["355-01"]
files_modified:
  - tests/test-355-jev-ceilings.cjs
  - scripts/jev-devtime-client.cjs
  - scripts/jev-question-ceilings.cjs
  - scripts/jev-response-schema.cjs
autonomous: true
requirements: [HIPS-08, HIPS-09]
tags: [jev, dev-time, egress-ceiling, zod, d-57-file]

must_haves:
  truths:
    - "D-55 / C8: all 355 Jev work goes through the shared scripts/jev-devtime-client.cjs; this plan ADDS three frozen profiles (hsi_thinking_mode, citation_check, usefulness_judge) to EGRESS_PROFILES and never touches another builder's profile, the 353 builder's jev(), or build-section-command-ledger.cjs"
    - "D-57: scripts/jev-devtime-client.cjs is re-read at execution time (354-17 edited it); the edit is additive; tests/test-356-jev-client.cjs stays green"
    - "D-44: every question has its own per-question ceiling = the profile guard composed with a closure check (fixture membership for sentences and pairings, exact templated claim + exact Theo-returned hops for citations); the shared allow-list is never widened; the pinned model 'jev-1.13.0' is required by every profile (jev-latest refused)"
    - "AI-SPEC pitfall 9: the shared client gains two additive, default-off options, timeoutMs (AbortSignal.timeout) and honorRetryAfter, so a hung socket cannot stall a measurement run; existing callers see identical behavior"
    - "D-46 / AI-SPEC Section 3: the citation claim is templated from two canon names and the confirmed DIRECTION_MEANING phrase only; path hops reach Jev as named {from, relation, to} objects with non-Framework nodes as [Label], never an internal Theo id, never a number"
    - "AI-SPEC Section 4b: parseJevResponse rejects a non-200, a schema miss, a choice outside the criteria, probability keys that differ from the criteria, probabilities not summing to 1 within 0.02, and any model other than jev-1.13.0"
    - "D-44: nothing under lib/ or hooks/ requires these scripts (tripwire lives in 355-21's part8 test; this plan's scripts carry the 'dev-time only' header)"
  artifacts:
    - path: "scripts/jev-question-ceilings.cjs"
      provides: "PINNED_MODEL, frozen questions, renderClaim, hopsFromTheoPath, closure ceilings, composeGuard"
      exports: ["PINNED_MODEL", "THINKING_MODE_LABELS", "THINKING_MODE_QUESTIONS", "CITATION_QUESTIONS_STATED", "CITATION_QUESTIONS_WITHHELD", "USEFULNESS_QUESTIONS", "renderClaim", "hopsFromTheoPath", "makeSentenceCeiling", "makeCitationCeiling", "makeUsefulnessCeiling", "composeGuard", "questionSha256"]
    - path: "scripts/jev-response-schema.cjs"
      provides: "zod JevResponse + parseJevResponse(res, expected)"
      exports: ["JevResponse", "parseJevResponse"]
    - path: "tests/test-355-jev-ceilings.cjs"
      provides: "Every 355 ceiling proven to refuse each mutation with zero fetch"
      min_lines: 100
  key_links:
    - from: "scripts/jev-question-ceilings.cjs"
      to: "scripts/jev-devtime-client.cjs EGRESS_PROFILES"
      via: "makeEgressGuard(EGRESS_PROFILES.<id>) composed with a closure"
      pattern: "makeEgressGuard"
    - from: "scripts/jev-question-ceilings.cjs renderClaim"
      to: "lib/core/direction-convention.cjs DIRECTION_MEANING"
      via: "confirmed phrase, never a placeholder name"
      pattern: "DIRECTION_MEANING"
---

<objective>
Dev-time Jev infrastructure for SPEC Req 5 and the AI-SPEC's D15/D18 measurements (HIPS-08, HIPS-09): three additive profiles in the shared client (D-55), one file of frozen questions and closure ceilings, one response parser. No Jev call is made in this plan; every test runs with a counting fetch stub and zero network. The live runs happen in 355-15 (thinking mode) and 355-26 (citation check, usefulness judge).

D-57: `scripts/jev-devtime-client.cjs` is a shared file edited by 354-17; start from 355-BASELINE.md and re-read it whole before the additive edit.
Output: the profiles, `scripts/jev-question-ceilings.cjs`, `scripts/jev-response-schema.cjs`, `tests/test-355-jev-ceilings.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-AI-SPEC.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-BASELINE.md
@.claude/skills/spike-findings-MindrianOS-Plugin/SKILL.md
@.claude/skills/spike-findings-MindrianOS-Plugin/references/jev-typed-decisions-api.md
@.claude/skills/spike-findings-MindrianOS-Plugin/references/policy-execution-parity.md
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three additive profiles and two default-off client options in scripts/jev-devtime-client.cjs (D-55, D-57)</name>
  <files>tests/test-355-jev-ceilings.cjs, scripts/jev-devtime-client.cjs</files>
  <read_first>
    - scripts/jev-devtime-client.cjs (WHOLE file, re-read now: 354-17 added framework_command_ledger; exact_state_v1 checks at _checkExactStateV1 and _applyOptionalFields; jev(); pool(); EGRESS_PROFILES; the header's "one profile per builder, never a merged union")
    - tests/test-356-jev-client.cjs (the existing client contract that must stay green)
    - `git status --short scripts/jev-devtime-client.cjs` (must be empty before editing; otherwise stop, collision rule)
  </read_first>
  <behavior>
    - EGRESS_PROFILES.hsi_thinking_mode: kind exact_state_v1, model 'jev-1.13.0', state_keys ['sentence'], string_keys ['sentence'], max_len_by_key { sentence: 300 }, question_ids ['mode'], question_keys ['type','instructions','criteria'], question_type 'choice', instructions_keys ['question','rule'], criteria_keys ['analytical','integrative','descriptive','evaluative','creative','none'], question_max_len 200
    - EGRESS_PROFILES.citation_check: model 'jev-1.13.0', state_keys ['claim','path'], string_keys ['claim'], max_len_by_key { claim: 400 }, question_ids ['relation'], question_type 'choice', instructions_keys ['question','rule'], criteria_keys ['supports','contradicts','says_nothing'], question_max_len 200
    - EGRESS_PROFILES.usefulness_judge: model 'jev-1.13.0', state_keys ['a_excerpt','b_excerpt','direction_phrase','verification'], string_keys the same four, max_len_by_key { a_excerpt: 2400, b_excerpt: 2400, direction_phrase: 80, verification: 16 }, question_ids ['usefulness'], question_type 'choice', instructions_keys ['question','rule'], criteria_keys ['useful','not_useful','already_known','none'], question_max_len 200
    - each new profile's guard throws on: model 'jev-latest', an extra state key, a missing state key, an extra question id, a criteria key outside its list; the pre-existing profiles are deep-equal to their pre-edit values (snapshot taken at the top of the test from a git show of BASE_355's file)
    - jev(body, { guard, key, fetchImpl, timeoutMs: 50 }) passes an AbortSignal to fetchImpl; without timeoutMs no signal is passed (unchanged behavior)
    - with honorRetryAfter: true, a 429 carrying retry-after '1' sleeps 1000 ms via sleepImpl; without it the existing 400 * 2 ** attempt backoff is used
    - tests/test-356-jev-client.cjs still exits 0
  </behavior>
  <action>RED: write tests/test-355-jev-ceilings.cjs (hygiene-355 preamble; `installNetGuard` plus an injected counting fetchImpl; never a real key) with the profile legs above. Run; it fails. GREEN: in scripts/jev-devtime-client.cjs add the three profiles inside EGRESS_PROFILES after the existing ones, each Object.freeze-d with an `id` equal to its key and a one-line comment "355 (D-44, D-55): <question>; closure check composed in scripts/jev-question-ceilings.cjs"; update the header comment's profile list. In jev(): if `o.timeoutMs` is a positive number pass `signal: AbortSignal.timeout(o.timeoutMs)` in the fetch init; if `o.honorRetryAfter === true` and the 429/529 response carries a numeric positive `retry-after`, sleep that many seconds instead of the exponential backoff. Change nothing else. Run tests/test-355-jev-ceilings.cjs (profile legs) and tests/test-356-jev-client.cjs green. Commit `feat(355-07): hsi_thinking_mode, citation_check, usefulness_judge egress profiles + default-off timeout (D-55)`.</action>
  <verify>
    <automated>node tests/test-356-jev-client.cjs && node -e "const c=require('./scripts/jev-devtime-client.cjs');for(const k of ['hsi_thinking_mode','citation_check','usefulness_judge']){const p=c.EGRESS_PROFILES[k];if(!p||p.model!=='jev-1.13.0'||!Object.isFrozen(p))process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-356-jev-client.cjs` exits 0
    - The verify node one-liner exits 0 (three frozen profiles pinned to jev-1.13.0)
    - `git diff "$(grep -oP 'BASE_355 = \K[0-9a-f]+' .planning/phases/355-*/355-BASELINE.md)" -- scripts/jev-devtime-client.cjs | grep '^-' | grep -v '^---' | grep -vcE "^-\s*(//|$)"` prints 0 or only the replaced header-comment lines (no removed code line)
  </acceptance_criteria>
  <done>The shared client can carry 355's three questions and nothing wider.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/jev-question-ceilings.cjs and scripts/jev-response-schema.cjs (D-44, D-46, AI-SPEC Sections 3-4b)</name>
  <files>scripts/jev-question-ceilings.cjs, scripts/jev-response-schema.cjs, tests/test-355-jev-ceilings.cjs</files>
  <read_first>
    - 355-AI-SPEC.md Section 3 "Entry Point Pattern" (the drafted questions, criteria, checkTop, makeCitationCeiling, makeSentenceCeiling) and Section 4b parseJevResponse; its placeholder direction names are superseded by D-01 and DIRECTION_MEANING
    - lib/core/direction-convention.cjs (DIRECTIONS, DIRECTION_MEANING)
    - scripts/jev-devtime-client.cjs (makeEgressGuard, EGRESS_PROFILES from Task 1)
    - .claude/skills/spike-findings-MindrianOS-Plugin/references/policy-execution-parity.md (why the rule is stated in instructions.rule)
  </read_first>
  <behavior>
    - PINNED_MODEL === 'jev-1.13.0'; THINKING_MODE_LABELS deep-equals ['analytical','integrative','descriptive','evaluative','creative','none']
    - THINKING_MODE_QUESTIONS.mode has instructions { question, rule } and criteria keyed exactly by THINKING_MODE_LABELS, each option { what, not_for?, examples } with examples written for the criteria (never taken from the gold items file; a leg asserts no example string equals any sentence in tests/fixtures/355-hsi-thinking-mode-sentences.items.json or the tuning set; plan 355-03 writes those files in the same wave, so when either file is absent this leg prints `SKIP (items not yet authored; re-asserted by 355-15)` instead of failing)
    - renderClaim('A','B','structural_transfer') === 'A and B: ' + DIRECTION_MEANING.structural_transfer + '.'; renderClaim with direction 'none' or an unknown direction throws
    - hopsFromTheoPath({ path, pathLabels, edges }) returns [{ from, relation, to }] with relation = edge lower-cased, underscores to spaces, and any non-Framework node as '[' + label + ']'
    - makeSentenceCeiling(Set) throws on a non-fixture sentence, an extra state key, an edited question object, or model 'jev-latest'; returns true on the exact frozen shape
    - makeCitationCeiling({ from, to, direction, theoPath }) throws on a free-text claim, a path not deep-equal to hopsFromTheoPath(theoPath), an extra state key, a question set other than STATED or WITHHELD; returns true on both frozen sets
    - makeUsefulnessCeiling(Map pair_id -> {a_excerpt, b_excerpt, direction_phrase}) throws when a_excerpt / b_excerpt / direction_phrase are not byte-equal to the pairing item, or verification is not one of 'strong','indirect','unverified'
    - composeGuard(profileGuard, closure) runs both and returns true; used with jev() and a counting fetchImpl, a refused payload produces zero fetch calls
    - questionSha256(questions) is a stable 64-hex sha256 of the canonical JSON
    - parseJevResponse({status:200, json: valid}) -> { ok: true, model, answers, usage }; each of non-200, model 'jev-1.14.0', choice outside criteria, probability keys differing, sum off by more than 0.02, missing usage -> { ok: false, reason } with a reason naming the failure
  </behavior>
  <action>RED: extend tests/test-355-jev-ceilings.cjs with the behavior legs; run; fails. GREEN: create scripts/jev-question-ceilings.cjs ('use strict', header "dev-time only; never required from lib/ or hooks/; Phase 355 D-44, D-46, D-55"; requires node:util, node:crypto, ./jev-devtime-client.cjs and ../lib/core/direction-convention.cjs). Port the AI-SPEC Section 3 questions with these changes: direction phrases from DIRECTION_MEANING (no placeholder names); the thinking-mode criteria examples written fresh (not from the gold or tuning files); CITATION_RULE text as drafted (priority order contradicts > supports > says_nothing; a broad shared node says nothing); USEFULNESS_QUESTIONS.usefulness with instructions.question 'Would a domain reader act on this pairing to extend the opportunity?' and a stated rule in priority order (already_known when the link is standard practice in both fields; useful when one side offers a mechanism the other side's problem could borrow; not_useful when the two excerpts only share vocabulary or the pairing suggests no action; none when the excerpts are not a pairing), criteria keyed useful / not_useful / already_known / none, each { what, not_for }. Build each ceiling as composeGuard(makeEgressGuard(EGRESS_PROFILES.<id>), closure) where the closure runs the AI-SPEC checkTop plus the membership or equality check. Create scripts/jev-response-schema.cjs porting the AI-SPEC Section 4b JevResponse and parseJevResponse, importing PINNED_MODEL from ./jev-question-ceilings.cjs. Run green. Commit `feat(355-07): frozen questions, per-question closure ceilings, response parser (HIPS-08, HIPS-09)`.</action>
  <verify>
    <automated>node tests/test-355-jev-ceilings.cjs && node tests/test-356-jev-client.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-355-jev-ceilings.cjs` exits 0 with `FAIL: 0` and its final check reports zero fetch attempts
    - `grep -cE "meaning_bridge|term_collision" scripts/jev-question-ceilings.cjs` prints 0
    - `grep -c "jev-latest" scripts/jev-question-ceilings.cjs` prints 0 outside a refusal test string (`grep -vE '^\s*(//|\*)' scripts/jev-question-ceilings.cjs | grep -c "jev-latest"` prints 0)
    - `grep -rlE "jev-question-ceilings|jev-response-schema|jev-devtime-client" lib hooks` prints nothing
  </acceptance_criteria>
  <done>Every 355 Jev payload has exactly one allowed shape and is refused before fetch() otherwise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-time script -> api.typesafe.ai | Only fixture sentences, canon names, Theo edge types and enum phrases may cross |
| shared client -> other phases' builders | 353/354/356/357 builders depend on the same file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-33 | Information disclosure | room text or internal ids reaching Jev | mitigate | Closure ceilings: sentence must be a fixture member, claim must equal the template, path must equal Theo's hops with [Label] for non-Framework nodes, excerpts must be byte-equal to committed fixture pairings; refuse-don't-strip |
| T-355-34 | Tampering | widening another builder's limits | mitigate | One profile per question, never a union; test snapshots pre-existing profiles and asserts deep equality |
| T-355-35 | Repudiation | model drift across a run | mitigate | Every profile pins jev-1.13.0; parseJevResponse rejects any other model |
| T-355-36 | Denial of service | hung vendor socket | mitigate | Default-off timeoutMs option used by 355 scripts; retry-after honored when opted in |
| T-355-37 | Information disclosure | vendor key printed | mitigate | loadKey unchanged; tests never set a key; installNetGuard proves zero network |
| T-355-SC | Tampering | npm installs | accept | No package installed (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Profiles `EGRESS_PROFILES.hsi_thinking_mode`, `EGRESS_PROFILES.citation_check`, `EGRESS_PROFILES.usefulness_judge`; `jev()` options `timeoutMs`, `honorRetryAfter` (in `scripts/jev-devtime-client.cjs`)
- Module `scripts/jev-question-ceilings.cjs`: `PINNED_MODEL`, `THINKING_MODE_LABELS`, `THINKING_MODE_QUESTIONS`, `CITATION_RULE`, `CITATION_QUESTIONS_STATED`, `CITATION_QUESTIONS_WITHHELD`, `USEFULNESS_QUESTIONS`, `renderClaim`, `hopsFromTheoPath`, `makeSentenceCeiling`, `makeCitationCeiling`, `makeUsefulnessCeiling`, `composeGuard`, `questionSha256`
- Module `scripts/jev-response-schema.cjs`: `JevResponse`, `parseJevResponse`
- Test `tests/test-355-jev-ceilings.cjs`

<verification>
- `node tests/test-355-jev-ceilings.cjs` and `node tests/test-356-jev-client.cjs` exit 0
</verification>

<success_criteria>
- Three questions, three ceilings, one parser, zero network, no other builder affected
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-07-SUMMARY.md` when done.
</output>
