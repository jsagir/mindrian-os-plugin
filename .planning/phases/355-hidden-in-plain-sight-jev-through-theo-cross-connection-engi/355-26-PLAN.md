---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 26
type: tdd
wave: 10
depends_on: ["355-14", "355-15", "355-25", "355-28"]
files_modified:
  - tests/test-355-jev-calibration-record.cjs
  - scripts/calibrate-citation-check.cjs
  - scripts/judge-355-usefulness.cjs
  - tests/fixtures/355-jev-citation-responses.json
  - tests/fixtures/355-jev-usefulness-responses.json
  - tests/fixtures/355-jev-calibration-record.json
  - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-JEV-MEASUREMENT.md
autonomous: true
requirements: [HIPS-09]
tags: [jev, dev-time, citation-check, usefulness-judge, calibration]

must_haves:
  truths:
    - "D-46 / AI-SPEC D15: scripts/calibrate-citation-check.cjs scores every templated citation item with the rule STATED and with the rule WITHHELD (one Jev Choice supports / contradicts / says_nothing per item per variant, through the citation_check profile composed with makeCitationCeiling); no-path items make ZERO Jev calls (code decides unverified)"
    - "D-46: the record reports, per variant, exact agreement with the navigator's blind gold overall and on the auto-verdict slice (confidence >= 0.8), that slice's coverage share and the human-routed share; the stated-rule slice's band comes from confidenceFromBucket({ correct, n }) over MEASURED accuracy (>= 0.9 high, >= 0.7 medium, else low, empty unknown; eureka-critic.cjs), never from Jev's own confidence number; the future-seam rule is written down (high: auto-verdict at >= 0.8; medium: every verdict to a human; low or unknown: rewrite the question first)"
    - "D-46 / AI-SPEC D18: scripts/judge-355-usefulness.cjs asks one Jev Choice (useful / not_useful / already_known / none) per sitting-1 pairing over { a_excerpt, b_excerpt, direction_phrase, verification } (fixture text is synthetic dev content; verification is a tier word, never a number) through the usefulness_judge profile and makeUsefulnessCeiling; the record reports exact agreement with the navigator's blind useful / not labels overall and per tier, and the already_known rate against the stamp tier; Jev's answers never enter the hit rate (computed from human judgments alone in 355-25)"
    - "D-14 / D-44: no Jev verdict reaches a runtime stamp this phase (judge stays 'none'); both scripts are dev-time only with --check offline replay (sha256-keyed recorded responses), pinned jev-1.13.0, the denominator guard and the 8,000 input-token tripwire"
    - "D-32 / D-33: both scripts refuse to run unless their gold (citation gold from 355-14, sitting-1 judgments from 355-24) carries labeled_at and a fixture_sha256 matching the current item file; the record test asserts each gold commit is an ancestor of the first commit adding its Jev response file"
  artifacts:
    - path: "scripts/calibrate-citation-check.cjs"
      provides: "Stated vs withheld citation calibration with --check"
      exports: ["runCalibration", "summarizeVariant", "bandFromMeasured"]
    - path: "scripts/judge-355-usefulness.cjs"
      provides: "D18 usefulness judge vs navigator gold with --check"
      exports: ["runJudge", "agreement"]
    - path: "tests/fixtures/355-jev-calibration-record.json"
      provides: "Machine record replayed by both --check modes"
    - path: "tests/test-355-jev-calibration-record.cjs"
      provides: "Offline replay, gold-first git order, pinned model, zero-call no-path proof"
  key_links:
    - from: "scripts/calibrate-citation-check.cjs"
      to: "scripts/jev-question-ceilings.cjs CITATION_QUESTIONS_STATED / WITHHELD + makeCitationCeiling"
      via: "composeGuard(makeEgressGuard(EGRESS_PROFILES.citation_check), closure)"
      pattern: "CITATION_QUESTIONS_WITHHELD"
    - from: "scripts/calibrate-citation-check.cjs bandFromMeasured"
      to: "lib/core/eureka-critic.cjs confidenceFromBucket"
      via: "require + call on measured { correct, n }"
      pattern: "confidenceFromBucket"
---

<objective>
The two remaining dev-time Jev measurements (HIPS-09; AI-SPEC D15, D18): calibrate the future citation-check judge (rule stated vs withheld) against the navigator's blind citation gold, and measure how far a Jev usefulness judge sits from the navigator's blind pairing judgments. Both results feed the Theo T-2 outbound note (355-27); neither touches a runtime stamp.

Output: two scripts, recorded responses, one machine record, two new sections in 355-JEV-MEASUREMENT.md, one record test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-AI-SPEC.md
@.claude/skills/spike-findings-MindrianOS-Plugin/references/policy-execution-parity.md
@scripts/jev-question-ceilings.cjs
@scripts/jev-response-schema.cjs
@scripts/measure-hsi-thinking-mode.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Both calibration scripts with offline replay, and the record test</name>
  <files>tests/test-355-jev-calibration-record.cjs, scripts/calibrate-citation-check.cjs, scripts/judge-355-usefulness.cjs</files>
  <read_first>
    - scripts/measure-hsi-thinking-mode.cjs (355-15: the live / --jev-fixture / --check skeleton, response keying, guards, abort rules; copy its structure)
    - lib/core/eureka-critic.cjs lines 575-595 (confidenceFromBucket)
    - tests/fixtures/355-citation-pairs.items.json, tests/fixtures/355-citation-pairs.json, tests/fixtures/355-rooms/pairings.items.json, tests/fixtures/355-rooms/judgments.json, tests/fixtures/355-rooms/stamps.json
  </read_first>
  <behavior>
    - summarizeVariant(rows) over rows { pair_id, gold, choice, confidence } returns { n, exact_agreement, auto_slice: { n, agreement, coverage_share }, human_routed_share } using the 0.8 cut
    - bandFromMeasured({ correct, n }) equals confidenceFromBucket's answer ('high' / 'medium' / 'low' / 'unknown')
    - a run over items that include no_path entries sends zero requests for them (counting fetchImpl)
    - both scripts refuse to run when the gold lacks labeled_at or its fixture_sha256 mismatches (exit non-zero naming the field)
    - usefulness agreement maps navigator useful true <-> Jev 'useful' and navigator useful false <-> any other choice; per-tier agreement uses the stamp tier from stamps.json; already_known rate per tier is reported
    - `--check` for each script exits 77 while its record is absent, otherwise recomputes the record exactly with zero network and no key, and fails if any stored response model is not jev-1.13.0
    - record legs (when present): the citation gold commit and the sitting-1 judgments commit are ancestors of the first commits that added tests/fixtures/355-jev-citation-responses.json and tests/fixtures/355-jev-usefulness-responses.json respectively
  </behavior>
  <action>RED: write tests/test-355-jev-calibration-record.cjs (hygiene-355 preamble; injected fetchImpl; fake key passed explicitly) per the behavior list; run; fail. GREEN: create scripts/calibrate-citation-check.cjs and scripts/judge-355-usefulness.cjs ('use strict', dev-time-only headers, Phase 355 HIPS-09 / D-46 citation, hyphens only), each with argv switch-case default live / --jev-fixture <path> / --check / --help, pool(4), timeoutMs 15000, honorRetryAfter, parseJevResponse with the matching expected options, abort on 401 / model drift / >8000 input tokens / any failure after one retry, responses keyed by sha256 of canonical { model, state, questions } (the citation script stores stated and withheld under their own keys since the questions differ), writing tests/fixtures/355-jev-citation-responses.json, tests/fixtures/355-jev-usefulness-responses.json and the shared tests/fixtures/355-jev-calibration-record.json (sections citation: { stated, withheld, band_stated, seam_rule, rule_text_sha256, question_sha256s } and usefulness: { n, agreement, per_tier, already_known_by_tier }), temp then rename, each script touching only its own section. Run green except the record legs (77). Commit `feat(355-26): citation-check calibration and usefulness judge, dev-time with offline replay (HIPS-09, D-46)`.</action>
  <verify>
    <automated>node tests/test-355-jev-calibration-record.cjs; test $? -eq 0 -o $? -eq 77</automated>
  </verify>
  <acceptance_criteria>
    - The test exits 0 or 77 (77 only for the record legs) with every math / refusal / zero-call leg PASS
    - `grep -rlE "calibrate-citation-check|judge-355-usefulness" lib hooks` prints nothing
  </acceptance_criteria>
  <done>Both measurements exist, blind-guarded and replayable.</done>
</task>

<task type="auto">
  <name>Task 2: Live runs, the machine record and the 355-JEV-MEASUREMENT.md sections</name>
  <files>tests/fixtures/355-jev-citation-responses.json, tests/fixtures/355-jev-usefulness-responses.json, tests/fixtures/355-jev-calibration-record.json, .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-JEV-MEASUREMENT.md</files>
  <read_first>
    - 355-JEV-MEASUREMENT.md (355-15's sections and, if present, 355-28's distilled-table section; append, never rewrite them)
    - scripts/calibrate-citation-check.cjs and scripts/judge-355-usefulness.cjs (Task 1)
  </read_first>
  <action>Run `node scripts/calibrate-citation-check.cjs` then `node scripts/judge-355-usefulness.cjs` once each (dev machine key; never printed). A missing key (exit 3) or an abort: record the reason in the SUMMARY and stop BLOCKED; never fabricate. Then run both `--check` modes. Append to 355-JEV-MEASUREMENT.md two sections, Feynman prose, hyphens only, no claim that anything "works": `## Citation-check calibration (D-46, AI-SPEC D15)` (items, strata, synthetic count, calls, tokens, cost, model, the stated rule text verbatim, per-variant exact agreement overall and on the auto slice, coverage, human-routed share, the measured band for the stated rule and the future-seam rule; a line that no verdict reaches a runtime stamp this phase) and `## Usefulness judge vs navigator (AI-SPEC D18)` (n, overall and per-tier agreement, already_known by tier, and a line that the hit rate in 355-VERIFICATION.md uses human judgments only). Run the record test (all legs). Commit `data(355-26): citation-check calibration + usefulness-judge agreement recorded (HIPS-09)`.</action>
  <verify>
    <automated>node scripts/calibrate-citation-check.cjs --check && node scripts/judge-355-usefulness.cjs --check && node tests/test-355-jev-calibration-record.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All three commands exit 0 (no SKIP in the record test)
    - 355-JEV-MEASUREMENT.md contains both new headings and still contains 355-15's `## Decision` section unchanged (`git diff HEAD~1 -- <file> | grep '^-' | grep -v '^---' | wc -l` prints 0)
    - `node -e "const r=require('./tests/fixtures/355-jev-calibration-record.json');if(!r.citation||!r.usefulness||!['high','medium','low','unknown'].includes(r.citation.band_stated))process.exit(1)"` exits 0
  </acceptance_criteria>
  <done>Theo's future judgment seam can be minted from a measured policy, not a draft.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> api.typesafe.ai | Templated claims, Theo edge types, canon names, synthetic fixture excerpts |
| Jev verdicts -> phase records | Must never flow into a runtime stamp or the human hit rate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-126 | Information disclosure | non-template or non-fixture text to Jev | mitigate | citation_check / usefulness_judge profiles + closure ceilings (exact claim, exact Theo hops, byte-equal fixture excerpts) before fetch |
| T-355-127 | Spoofing | band computed from the model's self-report | mitigate | bandFromMeasured uses measured { correct, n } through confidenceFromBucket (tested) |
| T-355-128 | Tampering | Jev answers leaking into the human hit rate | mitigate | 355-25 computes rates from judgments.json only; D18 lives in a separate record section |
| T-355-129 | Repudiation | gold labeled after seeing Jev | mitigate | Refusal without labeled_at; git-ancestry legs |
| T-355-130 | Elevation of privilege | a verdict reaching a runtime stamp | mitigate | judge stays z.literal('none'); lib/hooks tripwire in 355-21 |
</threat_model>

## Artifacts this phase produces

This plan's share:
- CLI `scripts/calibrate-citation-check.cjs` (live / `--jev-fixture` / `--check`; exports `runCalibration`, `summarizeVariant`, `bandFromMeasured`)
- CLI `scripts/judge-355-usefulness.cjs` (live / `--jev-fixture` / `--check`; exports `runJudge`, `agreement`)
- Fixtures `tests/fixtures/355-jev-citation-responses.json`, `tests/fixtures/355-jev-usefulness-responses.json`, `tests/fixtures/355-jev-calibration-record.json` (`citation{stated, withheld, band_stated, seam_rule, rule_text_sha256, question_sha256s}`, `usefulness{n, agreement, per_tier, already_known_by_tier}`)
- Sections in `355-JEV-MEASUREMENT.md`: Citation-check calibration; Usefulness judge vs navigator
- Test `tests/test-355-jev-calibration-record.cjs`

<verification>
- Both `--check` modes and the record test exit 0
</verification>

<success_criteria>
- Stated-vs-withheld citation agreement and the usefulness-judge agreement are measured, recorded and replayable
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-26-SUMMARY.md` when done.
</output>
