---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 17
type: execute
wave: 4
depends_on: ["355-04", "355-06", "355-08", "355-09"]
files_modified:
  - tests/test-355-producer-rs-connections.cjs
  - tests/fixtures/355/producers/rs-pairs.json
  - lib/core/rs-engine.cjs
  - lib/agents/reverse-salient-agent.cjs
  - commands/find-bottlenecks.md
  - skills/find-bottlenecks/SKILL.md
  - scripts/stamp-connections.cjs
  - commands/find-connections.md
  - skills/find-connections/SKILL.md
autonomous: true
requirements: [HIPS-04, HIPS-05]
tags: [stamp-wiring, find-bottlenecks, find-connections, rs-engine, d-18]

must_haves:
  truths:
    - "D-08 / D-16: find-bottlenecks is stamped at rs-engine's write boundary, not in its compute: runModeInternal accepts an optional async opts.stampFn that the caller supplies; it is awaited on the computed pairs BEFORE writeReverseSalientEdges opens the graph, and the returned stamps ride the SAME upsertEdge properties (toNodeProps); rs-engine never requires the stamp module and behaves byte-identically when no stampFn is passed"
    - "D-48: an RS pair's endpoints are the two artifacts' carried names (frontmatter framework:, then methodology: via the registry, then title); direction is the pair's rs-math direction (Convention A); the agent stamps exactly the pairs it will show (topk, engine order)"
    - "D-18: find-connections gets a thin CJS entry, scripts/stamp-connections.cjs (not an MCP tool; the Phase 268 ruling stands): it takes framework-name pairs, resolves them exactly, stamps them through verification-stamp.cjs, and prints formatStampLines blocks plus disclosureLine('find-connections'); find-connections findings carry direction 'none' (D-49)"
    - "D-29: commands/find-connections.md stops promising 'confidence scores'; Larry pastes the stamp blocks verbatim under each bridge; commands/find-bottlenecks.md and the agent's F.0 body show the stamp block and no differential or similarity number"
    - "D-50 / C9: on Desktop and Cowork find-connections cannot be stamped this phase (no governed find_connections on the Brain shim, no new MCP tool); the docs make Larry say exactly 'Not yet checked; run the CLI to verify.' for every unstamped bridge; a guarded shim passthrough is a deferred phase"
    - "D-17: the stamp rides the callers' existing reach ids (brain_consult for find-connections); no new reach id; commands/find-connections.md keeps its hitl_shape and gains Bash in allowed-tools only for stamp-connections.cjs"
    - "100% of shown findings carry a stamp, Theo up (replay) and Theo down (null); no D-30 decimal on any rendered line"
  artifacts:
    - path: "scripts/stamp-connections.cjs"
      provides: "find-connections CLI stamp entry (D-18)"
      exports: ["main", "parsePairs"]
    - path: "tests/test-355-producer-rs-connections.cjs"
      provides: "Stamp coverage, no-decimal and stored-prop proofs for find-bottlenecks and find-connections"
      min_lines: 100
  key_links:
    - from: "lib/agents/reverse-salient-agent.cjs"
      to: "lib/core/rs-engine.cjs runModeInternal opts.stampFn"
      via: "agent-supplied async stampFn built on verification-stamp stampFindings"
      pattern: "stampFn"
    - from: "commands/find-connections.md"
      to: "scripts/stamp-connections.cjs"
      via: "Bash invocation per surfaced framework pair"
      pattern: "stamp-connections.cjs"
---

<objective>
Wire the stamp into the two remaining pair producers that are not eureka (HIPS-04, HIPS-05): `/mos:find-bottlenecks` (RS engine through the reverse-salient agent) and `/mos:find-connections` (prompt-driven, so it gets a thin CJS stamp entry, D-18). Both show stamps and no numbers; find-bottlenecks stores the stamp on the REVERSE_SALIENT edges it already writes (D-16).

Output: rs-engine write-boundary hook, agent wiring, `scripts/stamp-connections.cjs`, four doc updates, one recorded fixture, one test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@lib/core/verification-stamp.cjs
@lib/core/verification-stamp-format.cjs
@lib/core/floor-disclosure.cjs
@tests/helpers/theo-replay-355.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: find-bottlenecks - rs-engine write-boundary stampFn, agent wiring, F.0 body, docs</name>
  <files>tests/test-355-producer-rs-connections.cjs, tests/fixtures/355/producers/rs-pairs.json, lib/core/rs-engine.cjs, lib/agents/reverse-salient-agent.cjs, commands/find-bottlenecks.md, skills/find-bottlenecks/SKILL.md</files>
  <read_first>
    - lib/core/rs-engine.cjs lines 380-600 (writeReverseSalientEdges(roomDir, pairs), the upsertEdge properties, runModeInternal(roomDir, opts) and where it calls the writer; any embeddings / encoder seam in opts)
    - lib/agents/reverse-salient-agent.cjs lines 150-260 (readPairField, runRsEngine cjs / python branches) and 378-660 (detectAndSurface, surfaceFinding F.0 payload, zones.body)
    - commands/find-bottlenecks.md lines 55-95 and skills/find-bottlenecks/SKILL.md (render rules)
    - tests/test-reverse-salient-agent.cjs and tests/test-reverse-salient-f0-integration.cjs (must stay green)
  </read_first>
  <action>Author tests/fixtures/355/producers/rs-pairs.json: >= 6 recorded rs-engine pair dicts (source/target artifact ids, titles, sections, lsa_score, semantic_score, signed_diff, abs_diff, direction) with titles chosen so some resolve to stub-fixture canon names and some do not. Write tests/test-355-producer-rs-connections.cjs (hygiene-355 preamble; labels prefixed `bottlenecks ` / `connections `) with a bottlenecks section: writeReverseSalientEdges(tmpRoom, pairs, { stampsByPairKey }) stores stamp props that re-parse via fromNodeProps on exactly the stamped pairs and none on the others; without the option the edge properties are byte-identical to today's; the agent's render seam on a recorded finding + stamp prints the stamp block and no D-30 decimal, Theo up and down; if runModeInternal exposes an embeddings seam, drive it with a stubbed stampFn and assert the stampFn is awaited before the write, otherwise assert the source order statically (the `await opts.stampFn` line precedes the writeReverseSalientEdges call). rs-engine.cjs: add optional `opts.stampFn` to runModeInternal (awaited with the pair list before writeReverseSalientEdges; its result is a Map keyed `source_artifact_id + '\u0000' + target_artifact_id` -> stamp props) and an optional third parameter `{ stampsByPairKey }` to writeReverseSalientEdges merged into the existing upsertEdge properties; no require of the stamp module in rs-engine (cite D-08 / D-16). reverse-salient-agent.cjs: in the cjs branch pass a stampFn that resolves each of the first `topk` pairs' endpoints (extractCarried on the artifact file, resolveEndpoint) and calls stampFindings; attach `stamp` and `stamp_lines` (formatStampLines 'cli') plus `disclosure` (disclosureLine('find-bottlenecks')) to each surfaced finding; put the stamp lines in the F.0 zones.body evidence and drop any rendered differential / similarity number; for the python branch stamp the surfaced finding after reading results (no stored props; say so in a comment). Export a pure `renderBottleneckFinding(finding, stamp)` seam. Docs: commands/find-bottlenecks.md and skills/find-bottlenecks/SKILL.md tell Larry to reproduce the stamp block verbatim, never a number, and on Desktop / Cowork without a stored stamp to say "Not yet checked; run the CLI to verify." Run the new test, the two reverse-salient tests, the floor sweep, the registry / shape gates. Commit `feat(355-17): find-bottlenecks findings stamped at the rs-engine write boundary (HIPS-04, HIPS-05, D-08, D-16)`.</action>
  <verify>
    <automated>! (node tests/test-355-producer-rs-connections.cjs 2>&1 | grep -qE "^FAIL: bottlenecks ") && node tests/test-reverse-salient-agent.cjs && node tests/test-reverse-salient-f0-integration.cjs && node tests/test-355-floor-sweep.cjs && node scripts/build-command-registry.cjs --check && node scripts/check-shape-declaration.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - Verify commands pass
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/rs-engine.cjs | grep -c "verification-stamp"` prints 0 (the engine never requires the stamp module)
    - `grep -c "stampFn" lib/core/rs-engine.cjs lib/agents/reverse-salient-agent.cjs` shows >= 1 in each
    - `git diff -U0 lib/core/rs-engine.cjs | grep '^+' | grep -cE "INSERT INTO|DatabaseSync"` prints 0
  </acceptance_criteria>
  <done>The bottleneck finding a user sees carries its stamp, and the stored edge carries the same stamp.</done>
</task>

<task type="auto">
  <name>Task 2: find-connections - scripts/stamp-connections.cjs and the docs (D-18, D-50)</name>
  <files>scripts/stamp-connections.cjs, commands/find-connections.md, skills/find-connections/SKILL.md, tests/test-355-producer-rs-connections.cjs</files>
  <read_first>
    - commands/find-connections.md (whole file: frontmatter allowed-tools, hitl_shape, steps 2-5, ":79 confidence scores")
    - skills/find-connections/SKILL.md (mirror)
    - scripts/build-section-command-ledger.cjs lines 670-710 (argv switch-case router shape)
    - lib/core/verification-stamp.cjs (resolveEndpoint, stampFindings) and lib/core/verification-stamp-format.cjs
  </read_first>
  <action>Create scripts/stamp-connections.cjs ('use strict', BSL header "find-connections CLI stamp entry, Phase 355 D-18; not an MCP tool; Theo only through verification-stamp -> brain-client; canon names only", hyphens only). argv: `--pair "<Framework A>|<Framework B>"` (repeatable), `--pairs-json <path>` ([{from, to}]), `--json` (print the stamp objects instead of lines), `--help`. Resolve each side exactly (resolveEndpoint with { framework: name }); an unresolved side stamps unverified / not_called / handle_unresolved and is still printed under its original name; direction 'none'; `await stampFindings`; print per pair `<A> and <B>` then formatStampLines(stamp, 'cli'), then disclosureLine('find-connections'). Export `main(argv, deps)` and `parsePairs(argv)`; `require.main === module` runs main with process.argv. Add the connections section to the test: replay and null callTools; exact-name refusal (a case variant stamps handle_unresolved with zero calls); every pair printed with a stamp block; no D-30 decimal; `--json` output re-parses through Stamp. Docs: in commands/find-connections.md add `Bash` to allowed-tools (keep hitl_shape F.8 and everything else), replace "with relationship types and confidence scores" with "with relationship types", add a step after cross-domain discovery: "On the CLI, for each framework pair you surface, run `node \"${CLAUDE_PLUGIN_ROOT}/scripts/stamp-connections.cjs\" --pair \"<A>|<B>\"` and paste its lines verbatim under that bridge; never add a number" and "On Desktop or Cowork, write exactly: Not yet checked; run the CLI to verify." (D-50); mirror in skills/find-connections/SKILL.md (use the MINDRIAN_OS_ROOT / CLAUDE_PLUGIN_ROOT root expression that file already uses). Run every pre-commit gate for commands/ and skills/ (build-command-registry --check, build-connector-registry --check, build-orchestration-projection --check, check-shape-declaration --check, check-help-coverage, command-registration-check) and the test. Commit `feat(355-17): find-connections stamp entry + docs (HIPS-04, HIPS-05, D-18, D-50)`.</action>
  <verify>
    <automated>node tests/test-355-producer-rs-connections.cjs && node scripts/build-command-registry.cjs --check && node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check && node scripts/check-shape-declaration.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-355-producer-rs-connections.cjs` exits 0 with `FAIL: 0`
    - `grep -c "confidence scores" commands/find-connections.md skills/find-connections/SKILL.md` prints 0 for each
    - `grep -c "stamp-connections.cjs" commands/find-connections.md skills/find-connections/SKILL.md` shows >= 1 in each; `grep -c "Not yet checked; run the CLI to verify." commands/find-connections.md skills/find-connections/SKILL.md` shows >= 1 in each
    - `grep -vE '^\s*(//|\*|/\*)' scripts/stamp-connections.cjs | grep -cE "fetch\(|https?://|registerTool|server\.tool\("` prints 0 (not an MCP tool, no direct network)
  </acceptance_criteria>
  <done>Every framework bridge Larry surfaces on the CLI carries a stamp; Desktop says honestly that it is not checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Larry-supplied framework names -> stamp-connections -> Theo | Names typed by the host model reach the wire |
| engine pairs -> REVERSE_SALIENT edges | Stored stamps later narrated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-80 | Information disclosure | room text passed as a --pair name | mitigate | Exact snapshot membership before any call; anything else stamps handle_unresolved with zero calls; Part 8 belt blocks content as a second wall |
| T-355-81 | Tampering | stamping inside the engine compute | mitigate | rs-engine only calls a caller-supplied stampFn at the write boundary and never requires the stamp module (grep acceptance) |
| T-355-82 | Spoofing | 'confidence scores' promised to the user | mitigate | Doc text removed; stamp blocks pasted verbatim; no-decimal legs |
| T-355-83 | Elevation of privilege | a new MCP surface for verification | mitigate | stamp-connections is a script only (D-18; SPEC out of scope); grep acceptance for tool registration |
| T-355-84 | Repudiation | Desktop implying a check happened | mitigate | Exact "Not yet checked; run the CLI to verify." sentence required on Desktop / Cowork (D-50) |
</threat_model>

## Artifacts this phase produces

This plan's share:
- `lib/core/rs-engine.cjs`: `runModeInternal` option `opts.stampFn`; `writeReverseSalientEdges(roomDir, pairs, { stampsByPairKey })`
- `lib/agents/reverse-salient-agent.cjs`: surfaced finding fields `stamp`, `stamp_lines`, `disclosure`; export `renderBottleneckFinding`
- CLI `scripts/stamp-connections.cjs` (`--pair`, `--pairs-json`, `--json`, `--help`; exports `main`, `parsePairs`)
- Fixture `tests/fixtures/355/producers/rs-pairs.json`; test `tests/test-355-producer-rs-connections.cjs`

<verification>
- Producer test, reverse-salient tests, floor sweep and all registry / shape gates green
</verification>

<success_criteria>
- find-bottlenecks and find-connections show stamps on every surfaced finding; no numbers; Desktop honest
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-17-SUMMARY.md` when done.
</output>
