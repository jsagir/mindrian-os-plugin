---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 13
type: execute
wave: 3
depends_on: ["355-02", "355-06", "355-08"]
files_modified:
  - tests/test-355-fixture-rooms.cjs
  - tests/fixtures/355-rooms/README.md
  - tests/fixtures/355-rooms/planted-cases.json
  - tests/fixtures/355-rooms/room-ill-defined/
  - tests/fixtures/355-rooms/room-extend/
  - tests/fixtures/355-rooms/room-control/
autonomous: true
requirements: [HIPS-07]
tags: [fixture-rooms, hit-rate, d-34, d-35, d-48]

must_haves:
  truths:
    - "D-35 step (3): the three fixture rooms are authored only after the PWS author's phrase ruling (355-02) and before any navigator judging"
    - "D-34: three Claude-authored rooms under tests/fixtures/355-rooms/ follow the Phase 353 icm-rooms pattern (.room-root at each room root; ROOM.md and MINTO.md in EVERY directory, required by the pre-commit guard); each has 4 sections in distinct domains and 3-4 artifacts of 150-300 words per section (12-16 documents, 66-120 candidate pairs); no real names"
    - "D-34: room-ill-defined is an ill-defined problem; room-extend sits at the extend-the-opportunity step; room-control plants meaning bridges (same idea, different words) and false friends (shared words, different meanings, e.g. 'virus', 'channel') so the direction-fidelity judgment (AI-SPEC D17) has real cases"
    - "D-48: a deliberate subset of artifacts carries frontmatter `framework:` (an exact name from the regenerated data/framework-names.json) or `methodology:` (a command slug whose registry frameworks[0] resolves), so per-tier hit rates are not empty; the rest carry neither, so handle_unresolved is also exercised"
    - "D-40: each room's root ROOM.md frontmatter carries pws_stage (ill_defined or extend_opportunity; omitted for the control room), the value the filing layer records on the opportunity node"
    - "D-32: the planted-case list lives in planted-cases.json (joined only after judging), never in ROOM.md, MINTO.md or artifact text; the README tells the labeler not to open it before judging"
    - "Rooms are never run in place: tests and the measurement script copy them to os.tmpdir() (the icm-rooms README rule)"
  artifacts:
    - path: "tests/test-355-fixture-rooms.cjs"
      provides: "Structure, size, frontmatter-resolution and discovery proof"
      min_lines: 80
    - path: "tests/fixtures/355-rooms/README.md"
      provides: "Case index and the never-in-place / no-peeking rules"
    - path: "tests/fixtures/355-rooms/planted-cases.json"
      provides: "Planted bridges and false friends for room-control (post-judging join only)"
  key_links:
    - from: "artifact frontmatter framework: / methodology:"
      to: "lib/core/verification-stamp.cjs resolveEndpoint"
      via: "exact match against data/framework-names.json and data/command-registry.json"
      pattern: "framework:"
---

<objective>
SPEC Req 7 (HIPS-07) needs rooms to measure on, and none of the existing fixture rooms qualify (D-34). Author three small, realistic, synthetic Data Rooms that make RS, HSI and whitespace fire, carry resolvable framework names on part of their artifacts (D-48), and plant true bridges and false friends in a control room. This is D-35 step (3), after the PWS author's phrase ruling.

Output: `tests/fixtures/355-rooms/` (three rooms, README, planted-cases.json) and `tests/test-355-fixture-rooms.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-INTENT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PHRASE-CONFIRMATION.md
@tests/fixtures/icm-rooms/README.md
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structure test (RED), README, planted-cases.json and room-ill-defined</name>
  <files>tests/test-355-fixture-rooms.cjs, tests/fixtures/355-rooms/README.md, tests/fixtures/355-rooms/planted-cases.json, tests/fixtures/355-rooms/room-ill-defined/</files>
  <read_first>
    - tests/fixtures/icm-rooms/alpha-room/ (ROOM.md, MINTO.md, STATE.md, .room-root and one section's ROOM.md / MINTO.md: copy their shape)
    - scripts/hooks/pre-commit lines 100-150 (ROOM.md + MINTO.md in every staged dir under a .room-root) and lines 330-390 (feynman-minto-guardian pre-commit validators)
    - lib/core/rs-engine.cjs discoverArtifacts / _walkDir (which files count as artifacts; ROOM.md / MINTO.md / STATE.md exclusions; flat section/*.md vs section/name/name.md)
    - lib/core/verification-stamp.cjs extractCarried / resolveEndpoint and data/framework-names.json (pick framework: values that exist in the snapshot)
    - data/command-registry.json (pick methodology: slugs whose frameworks[0] is in the snapshot)
  </read_first>
  <action>RED: write tests/test-355-fixture-rooms.cjs (hygiene-355 preamble) asserting, for each of room-ill-defined, room-extend, room-control: `.room-root` exists; every directory under the room carries ROOM.md and MINTO.md; exactly 4 section directories; each section has 3 or 4 artifacts; every artifact body (frontmatter stripped) is 150-300 words; `discoverArtifacts(<tmp copy>)` returns 12-16 artifacts; at least 4 artifacts per room resolve through resolveEndpoint via 'framework' or 'methodology' and at least 4 resolve through nothing; room-ill-defined root ROOM.md has `pws_stage: ill_defined`, room-extend has `pws_stage: extend_opportunity`, room-control has no pws_stage; planted-cases.json lists >= 4 meaning bridges and >= 4 false friends, each naming two artifact paths that exist in room-control; no artifact, ROOM.md or MINTO.md contains the words 'planted', 'false friend' or 'bridge case'. All discovery runs on an `fs.cpSync` copy under `fs.mkdtempSync(os.tmpdir())`, removed afterwards. Run; fail. Then write tests/fixtures/355-rooms/README.md (case index; "copy to a temp dir, never run in place"; "labelers: do not open planted-cases.json before judging"; no real names; hyphens only), planted-cases.json (filled in Task 3), and author room-ill-defined: an ill-defined problem (for example why a network of rural clinics loses patients between referral and follow-up), four sections in distinct domains (for example patient-flow, transport-logistics, community-trust, staffing), 3-4 plain-prose artifacts per section of 150-300 words written as a team's working notes (pseudonymous roles only, no person, company or place names), root ROOM.md frontmatter `pws_stage: ill_defined`, per-directory ROOM.md and MINTO.md in the icm-rooms format. Run `node scripts/feynman-minto-guardian.cjs pre-commit tests/fixtures/355-rooms/room-ill-defined` and fix every critical or error finding before committing. Commit `test(355-13): fixture-room structure test + room-ill-defined (HIPS-07, D-34)`.</action>
  <verify>
    <automated>node scripts/feynman-minto-guardian.cjs pre-commit tests/fixtures/355-rooms/room-ill-defined && node tests/test-355-fixture-rooms.cjs 2>&1 | grep -c "PASS: room-ill-defined"</automated>
  </verify>
  <acceptance_criteria>
    - The guardian exits 0 for room-ill-defined
    - Every room-ill-defined check in tests/test-355-fixture-rooms.cjs passes (the other two rooms' checks fail until Tasks 2-3)
    - `git status --short tests/fixtures/355-rooms/room-ill-defined` shows no file outside the room tree changed by running the test (tests ran on a tmp copy)
  </acceptance_criteria>
  <done>One realistic ill-defined room exists and the structure test pins what all three must satisfy.</done>
</task>

<task type="auto">
  <name>Task 2: room-extend (the extend-the-opportunity step)</name>
  <files>tests/fixtures/355-rooms/room-extend/</files>
  <read_first>
    - tests/fixtures/355-rooms/room-ill-defined/ (Task 1's format)
    - 355-INTENT.md section 2 (the "how can this opportunity be extended" step, Medici Effect grounding)
  </read_first>
  <action>Author room-extend: a room reviewing an existing opportunity and asking how to extend it (for example a cold-chain delivery service looking for adjacent uses), four sections in distinct domains (for example operations, materials-science, retail-demand, regulation), 3-4 artifacts of 150-300 words each, root ROOM.md frontmatter `pws_stage: extend_opportunity`, ROOM.md + MINTO.md in every directory, the D-48 frontmatter mix (at least 4 artifacts with a resolvable framework: or methodology:, at least 4 with neither). Include a few cross-section passages where one domain's mechanism could plausibly transfer to another's problem (so the engines have real candidates) without saying so. Run the guardian and the structure test. Commit `test(355-13): fixture room-extend (HIPS-07, D-34)`.</action>
  <verify>
    <automated>node scripts/feynman-minto-guardian.cjs pre-commit tests/fixtures/355-rooms/room-extend && node tests/test-355-fixture-rooms.cjs 2>&1 | grep -c "PASS: room-extend"</automated>
  </verify>
  <acceptance_criteria>
    - The guardian exits 0 for room-extend and every room-extend check in the structure test passes
  </acceptance_criteria>
  <done>The second measurement room exists.</done>
</task>

<task type="auto">
  <name>Task 3: room-control with planted bridges and false friends; all structure checks green</name>
  <files>tests/fixtures/355-rooms/room-control/, tests/fixtures/355-rooms/planted-cases.json</files>
  <read_first>
    - tests/fixtures/355-rooms/room-ill-defined/ and room-extend/ (format)
    - 355-ORIGIN-CONCEPT.md section 3 and the deck's false-friend note ("automobile means something else in biology")
    - 355-PHRASE-CONFIRMATION.md (the two phrases the navigator will judge against)
  </read_first>
  <action>Author room-control: four sections in deliberately distant domains (for example cell-biology, computer-security, retail-marketing, river-hydrology). Plant at least 4 meaning bridges (two artifacts in different sections describing the same mechanism in different vocabulary, e.g. immune memory and signature-based intrusion detection) and at least 4 false friends (two artifacts sharing a salient word with different meanings, e.g. 'virus' in cell-biology vs computer-security, 'channel' in retail-marketing vs river-hydrology vs ion channels, 'bank' in river-hydrology vs a merchandising bank); keep every artifact natural working-notes prose with no hint that it is planted. D-48 frontmatter mix as in the other rooms. Write planted-cases.json as `{ "_note": "Claude-authored ground truth for the D17 direction-fidelity analysis; joined after judging only; labelers must not open this before judging", "meaning_bridges": [ { "a": "<room-relative path>", "b": "<path>", "mechanism": "<one line>" } ], "false_friends": [ { "a": "...", "b": "...", "shared_word": "...", "meanings": ["...", "..."] } ] }`. Run the guardian and the full structure test. Commit `test(355-13): fixture room-control with planted bridges and false friends (HIPS-07, D-34)`.</action>
  <verify>
    <automated>node scripts/feynman-minto-guardian.cjs pre-commit tests/fixtures/355-rooms/room-control && node tests/test-355-fixture-rooms.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-355-fixture-rooms.cjs` exits 0 with `FAIL: 0`
    - `node -e "const p=require('./tests/fixtures/355-rooms/planted-cases.json');if(p.meaning_bridges.length<4||p.false_friends.length<4)process.exit(1)"` exits 0
    - `grep -rliE "planted|false friend|bridge case" tests/fixtures/355-rooms/room-*` prints nothing
  </acceptance_criteria>
  <done>Three rooms exist that make the engines fire and give the navigator real bridges and real false friends to judge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture rooms -> engines that write into rooms | Engines write .hsi-results.json, room.db, .mindrian/ into whatever room they run on |
| planted ground truth -> blind labeler | Hints could bias the navigator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-59 | Tampering | engines writing into the committed fixture tree | mitigate | Tests copy rooms to mkdtemp and delete the copy; git status acceptance check |
| T-355-60 | Tampering | label leakage via planted markers | mitigate | Planted list only in planted-cases.json; grep acceptance forbids hint words in room text; README no-peeking rule |
| T-355-61 | Information disclosure | real names in fixtures | mitigate | Roles and pseudonyms only; authoring rule restated in README |
| T-355-62 | Denial of service | pre-commit guard refusing the rooms | mitigate | ROOM.md + MINTO.md in every directory; guardian run locally before each commit |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Fixture rooms `tests/fixtures/355-rooms/room-ill-defined/`, `room-extend/`, `room-control/` (root ROOM.md frontmatter key `pws_stage`)
- Case files `tests/fixtures/355-rooms/README.md`, `tests/fixtures/355-rooms/planted-cases.json` (`meaning_bridges[]`, `false_friends[]`)
- Test `tests/test-355-fixture-rooms.cjs`

<verification>
- `node tests/test-355-fixture-rooms.cjs` exits 0; guardian green for all three rooms
</verification>

<success_criteria>
- Three measurement rooms with resolvable and unresolvable ends, and planted cases for the direction-fidelity judgment
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-13-SUMMARY.md` when done.
</output>
