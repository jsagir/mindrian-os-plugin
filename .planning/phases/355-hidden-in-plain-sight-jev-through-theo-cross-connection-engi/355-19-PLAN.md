---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 19
type: tdd
wave: 4
depends_on: ["355-06", "355-10", "355-12"]
files_modified:
  - tests/test-355-side-channel-v2.cjs
  - lib/core/eureka/eureka-reach-runner.cjs
  - lib/core/sensors/sensor-eureka.cjs
  - tests/fixtures/213/last-eureka.json
  - tests/test-213-sensor-eureka.cjs
  - tests/test-213-part8-boundary.cjs
  - tests/test-355-direction-readers.cjs
autonomous: true
requirements: [HIPS-06, HIPS-01]
tags: [side-channel, schema-bump, sens-13, dedup-ledger, atomic]

must_haves:
  truths:
    - "D-53 (C3): ONE atomic commit moves every lockstep site: eureka-reach-runner.cjs gains CRITIC_TAGS_SCHEMA_VERSION = 1 for the critic-tags probe and bumps SIDE_CHANNEL_SCHEMA_VERSION to 2 (builder and validator); sensor-eureka.cjs SCHEMA_VERSION becomes 2; tests/fixtures/213/last-eureka.json becomes v2; tests/test-213-sensor-eureka.cjs moves its mismatch case from 2 to 3; tests/test-213-part8-boundary.cjs builds a v2 payload; probeGuard() still returns available true (no guard_unavailable regression)"
    - "D-15: the v2 closed schema's exact key set is v1 plus 'stamp' and 'opportunity_handle' (both present; null on the hook-path scan producer, which cannot call Theo); validateClosedSchema checks stamp's own closed keys (verification, backend, direction, judge, reason, path_nodes, path_edges) by enum, charset and length, so no room text and no number can ride in it"
    - "D-53 (C4): the runner exports writeStampedSideChannel(roomDir, {...}) (validate, then temp-then-rename) for the filing layer (355-20) to call, because the only existing producer never completes in production"
    - "D-42: fire-once is a dedup ledger at <room>/.mindrian/eureka-reach-ledger.json keyed on opportunity_handle; the sensor only READS it (a present key or a corrupt ledger returns null, fail closed) and writes nothing; the runner exports markEurekaReachSurfaced(roomDir, handle) (atomic write) for the surfacing seam (355-22); SENS-14 is untouched"
    - "D-41: reach_id stays deep_research and the signal stays eureka_bridge (no seven-place sensor lockstep); the evidence bag gains opportunity_handle, stamp_verification, stamp_backend, stamp_direction, stamp_judge, stamp_reason and stamp_path (formatPathText of canon names), no decimals, no banned words; the existing differential stays off-render"
    - "D-07: SURPRISE_TYPES in the runner and FIRING_SURPRISE_TYPES in the sensor are the module's DIRECTIONS; the sensor stays zod-free (hook path, loaded by insight-sensors.cjs)"
  artifacts:
    - path: "tests/test-355-side-channel-v2.cjs"
      provides: "v2 schema, probe, writer, dedup and evidence-bag proofs"
      min_lines: 100
    - path: "lib/core/eureka/eureka-reach-runner.cjs"
      provides: "v2 side channel + writeStampedSideChannel + markEurekaReachSurfaced"
      exports: ["SIDE_CHANNEL_SCHEMA_VERSION", "CRITIC_TAGS_SCHEMA_VERSION", "buildSideChannelPayload", "validateClosedSchema", "writeStampedSideChannel", "markEurekaReachSurfaced", "EUREKA_REACH_LEDGER_RELPATH"]
  key_links:
    - from: "lib/core/sensors/sensor-eureka.cjs"
      to: "<room>/.mindrian/eureka-reach-ledger.json"
      via: "read-only dedup check on opportunity_handle"
      pattern: "eureka-reach-ledger"
    - from: "lib/core/eureka/eureka-reach-runner.cjs probeGuard"
      to: "data/eureka-critic-tags.json schema_version 1"
      via: "CRITIC_TAGS_SCHEMA_VERSION, decoupled from the side-channel version"
      pattern: "CRITIC_TAGS_SCHEMA_VERSION"
---

<objective>
HIPS-06 plumbing (D-15, D-41, D-42, D-53): the eureka side channel's closed schema rejects any new key, so the stamp needs a version bump, and the bump must move every lockstep site in one commit or the eureka guard silently reports guard_unavailable on every scan (research C3). Add the writer the filing layer will call (C4) and the read-only fire-once ledger check. SENS-13 keeps its reach id and signal.

Output: the RED test, the atomic bump, the dedup and evidence-bag change, the enum imports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PATTERNS.md
@lib/core/verification-stamp-format.cjs
@lib/core/direction-convention.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED test for the v2 side channel, the probe, the writer, the dedup ledger and the evidence bag</name>
  <files>tests/test-355-side-channel-v2.cjs</files>
  <read_first>
    - lib/core/eureka/eureka-reach-runner.cjs lines 60-135 (constants, SURPRISE_TYPES, probeGuard and the :127 coupling), 240-310 (buildSideChannelPayload, validateClosedSchema, sortedKeysEqual), 410-435 (exports)
    - lib/core/sensors/sensor-eureka.cjs (whole file)
    - lib/core/sensors/sensor-url-ingest.cjs lines 85-95 and 180-215 (the read-only ledger dedup precedent, fail-closed on corrupt)
    - data/eureka-critic-tags.json (schema_version 1) and lib/core/eureka-critic.cjs around :78
    - tests/test-213-sensor-eureka.cjs and tests/test-213-part8-boundary.cjs (current fixtures and cases)
  </read_first>
  <behavior>
    - probeGuard() returns available true after the bump (critic tags still v1)
    - buildSideChannelPayload(...) with a stamp and an opportunity handle yields schema_version 2 and exactly the v1 key set plus stamp and opportunity_handle; with no stamp both are null
    - validateClosedSchema accepts both v2 shapes and rejects: a v1 payload, an extra top-level key, an extra stamp key, a stamp verification outside TIERS, a stamp field holding a number, a path_nodes entry longer than 128 chars or containing a newline, a path_edges entry not matching /^[A-Z][A-Z_]{0,63}$/
    - writeStampedSideChannel(tmpRoom, {...}) writes .mindrian/last-eureka.json atomically only when validation passes (an invalid input writes nothing and returns { ok: false, reason })
    - sensorEureka on a fresh valid v2 payload (guard transferable, band in FIRING_BANDS, surprise_type in DIRECTIONS) returns a reach with reach_id 'deep_research', signal 'eureka_bridge' and evidence containing opportunity_handle and the six stamp_* fields; stamp_path equals formatPathText(nodes, edges) for a verified stamp and '' otherwise; no evidence value except the pre-existing differential is a non-integer number
    - with the handle present in .mindrian/eureka-reach-ledger.json the sensor returns null; with a corrupt ledger it returns null; with no ledger it fires
    - markEurekaReachSurfaced(tmpRoom, handle) creates or updates the ledger atomically ({ schema_version: 1, entries: { <handle>: { at } } }) and is idempotent
    - the sensor's source requires neither zod nor verification-stamp.cjs (static leg) and writes no file during a call (fs write spy)
  </behavior>
  <action>Write tests/test-355-side-channel-v2.cjs (hygiene-355 preamble; mkdtemp rooms; fixed clock for freshness) implementing the behavior list. Run it; it fails. Commit `test(355-19): add failing v2 side-channel + fire-once tests (D-15, D-42, D-53)`.</action>
  <verify>
    <automated>node tests/test-355-side-channel-v2.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The test exists, runs, and fails (current code is v1)
    - `grep -c "probeGuard" tests/test-355-side-channel-v2.cjs` >= 1 and `grep -c "eureka-reach-ledger" tests/test-355-side-channel-v2.cjs` >= 1
  </acceptance_criteria>
  <done>A failing test pins every v2 promise.</done>
</task>

<task type="auto">
  <name>Task 2: ATOMIC schema bump across the runner, the sensor, the 213 fixture and the two 213 tests (D-53)</name>
  <files>lib/core/eureka/eureka-reach-runner.cjs, lib/core/sensors/sensor-eureka.cjs, tests/fixtures/213/last-eureka.json, tests/test-213-sensor-eureka.cjs, tests/test-213-part8-boundary.cjs</files>
  <read_first>
    - the five files, whole, as they stand now
    - lib/core/direction-convention.cjs (DIRECTIONS) and lib/core/verification-stamp.cjs (TIERS, BACKENDS, REASONS: copy the enum VALUES into the runner's validator as frozen arrays required from the stamp module, since the runner is not on the hook path; the sensor must not require it)
  </read_first>
  <action>eureka-reach-runner.cjs: add `const CRITIC_TAGS_SCHEMA_VERSION = 1;` and use it in probeGuard's critic-tags comparison (cite research C3); set SIDE_CHANNEL_SCHEMA_VERSION = 2; `const SURPRISE_TYPES = DIRECTIONS;` from the module; extend buildSideChannelPayload with `opts.stamp` (mapped to the closed side-channel stamp object { verification, backend, direction, judge, reason, path_nodes, path_edges }, null when absent) and `opts.opportunityHandle` (string or null); extend validateClosedSchema to the v2 key set with sortedKeysEqual and the stamp sub-checks from Task 1's behavior; update the header block (:14-40) describing the v2 contract (Phase 355 D-15 / D-53). sensor-eureka.cjs: SCHEMA_VERSION = 2; `const FIRING_SURPRISE_TYPES = DIRECTIONS;` (require ../direction-convention.cjs, zod-free). tests/fixtures/213/last-eureka.json: rewrite as a valid v2 payload (stamp null, opportunity_handle null). tests/test-213-sensor-eureka.cjs: the version-mismatch case uses 3 (header note "Amended Phase 355 D-53"); tests/test-213-part8-boundary.cjs: build a v2 payload (same note). Run: tests/test-213-sensor-eureka.cjs, tests/test-213-part8-boundary.cjs, tests/test-213-eureka-offer.cjs, tests/test-213-touchpoints.cjs, tests/test-213-reach-wired.cjs, `bash tests/run-all-213.sh` if present, and the new test's schema / probe legs. Commit ALL FIVE files in ONE commit: `feat(355-19): eureka side channel v2 in lockstep, critic-tags probe decoupled (HIPS-06, D-15, D-53)`.</action>
  <verify>
    <automated>node tests/test-213-sensor-eureka.cjs && node tests/test-213-part8-boundary.cjs && node tests/test-213-eureka-offer.cjs && node tests/test-213-reach-wired.cjs && node -e "(async()=>{const r=require('./lib/core/eureka/eureka-reach-runner.cjs');const g=await r.probeGuard({});if(!g||g.available!==true){console.error(g);process.exit(1)}})()"</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command exits 0 (adapt probeGuard's argument to its real signature; the assertion is available === true)
    - `git show --stat HEAD` lists exactly the five files
    - `grep -c "CRITIC_TAGS_SCHEMA_VERSION" lib/core/eureka/eureka-reach-runner.cjs` >= 2 and `grep -c "SIDE_CHANNEL_SCHEMA_VERSION = 2" lib/core/eureka/eureka-reach-runner.cjs` = 1 and `grep -c "SCHEMA_VERSION = 2" lib/core/sensors/sensor-eureka.cjs` = 1
  </acceptance_criteria>
  <done>The side channel can carry a stamp and nothing downstream silently stopped working.</done>
</task>

<task type="auto">
  <name>Task 3: writeStampedSideChannel, markEurekaReachSurfaced, read-only dedup and the stamp evidence bag (D-41, D-42)</name>
  <files>lib/core/eureka/eureka-reach-runner.cjs, lib/core/sensors/sensor-eureka.cjs, tests/test-355-direction-readers.cjs</files>
  <read_first>
    - tests/test-355-side-channel-v2.cjs (remaining RED legs)
    - lib/core/sensors/sensor-url-ingest.cjs lines 180-215
    - tests/test-355-direction-readers.cjs (the `(moves in 355-19)` exception entries)
  </read_first>
  <action>Runner: export `EUREKA_REACH_LEDGER_RELPATH = path.join('.mindrian','eureka-reach-ledger.json')`; `writeStampedSideChannel(roomDir, { score, guard, a, b, stamp, opportunityHandle })` builds via buildSideChannelPayload, validates, writes `<room>/.mindrian/last-eureka.json` temp-then-rename, returns { ok, reason? }; `markEurekaReachSurfaced(roomDir, handle)` read-modify-writes the ledger atomically, idempotent, returns { ok }. Neither is called in this plan (355-20 and 355-22 wire them). Sensor: after the existing guard / band / surprise gates, if `payload.opportunity_handle` is a non-empty string, read the ledger at the replicated relpath (do not require the runner: replicate the relpath constant like the url-ingest sensor does) and return null when the key exists or the ledger is corrupt; add the evidence fields opportunity_handle, stamp_verification, stamp_backend, stamp_direction, stamp_judge, stamp_reason ('' when absent) and stamp_path (require ../verification-stamp-format.cjs formatPathText only; '' for unverified or null stamps); keep every existing evidence field. tests/test-355-direction-readers.cjs: remove the two `(moves in 355-19)` exception entries so the static leg now allows zero duplicates outside the module. Run the full new test, the readers test, all 213 tests, tests/test-219-harvest-sensor.cjs (SENS-14 untouched) and tests/test-dial-label-bank-drift.cjs. Commit `feat(355-19): stamped side-channel writer, fire-once ledger, stamp evidence bag (HIPS-06, D-41, D-42)`.</action>
  <verify>
    <automated>node tests/test-355-side-channel-v2.cjs && node tests/test-355-direction-readers.cjs && node tests/test-213-sensor-eureka.cjs && node tests/test-219-harvest-sensor.cjs && node tests/test-213-reach-wired.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All five commands exit 0; the side-channel test prints `FAIL: 0`
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/sensors/sensor-eureka.cjs | grep -cE "require\('zod'\)|verification-stamp\.cjs|writeFileSync|renameSync"` prints 0
    - `grep -c "(moves in 355-19)" tests/test-355-direction-readers.cjs` prints 0
  </acceptance_criteria>
  <done>SENS-13 can carry a stamped finding once, and the sensor still writes nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| side-channel file -> hook-path sensor | A local file drives what Larry is offered |
| filing layer -> side channel | The new producer seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-89 | Tampering | forged side channel forcing a fire | mitigate | Closed v2 schema with enum / charset / length checks, freshness window, guard verdict gate, dedup ledger |
| T-355-90 | Denial of service | schema bump silently disabling the guard | mitigate | CRITIC_TAGS_SCHEMA_VERSION decoupled; probeGuard available asserted in the atomic commit |
| T-355-91 | Denial of service | zod load failure in the hook chain | mitigate | Sensor stays zod-free and does not require verification-stamp.cjs (static leg) |
| T-355-92 | Information disclosure | room text in the side channel or evidence bag | mitigate | stamp fields are enums and canon names only; opportunity_handle is an opaque local id; the bag stays on the local trace (Part 8) |
| T-355-93 | Tampering | corrupt dedup ledger causing repeat fires | mitigate | Corrupt ledger fails closed (null) as in sensor-url-ingest |
| T-355-94 | Repudiation | a partial lockstep across commits | mitigate | Acceptance asserts the bump commit holds exactly the five lockstep files |
</threat_model>

## Artifacts this phase produces

This plan's share:
- `lib/core/eureka/eureka-reach-runner.cjs`: `CRITIC_TAGS_SCHEMA_VERSION = 1`, `SIDE_CHANNEL_SCHEMA_VERSION = 2`, v2 keys `stamp` (`verification`, `backend`, `direction`, `judge`, `reason`, `path_nodes`, `path_edges`) and `opportunity_handle`; exports `writeStampedSideChannel`, `markEurekaReachSurfaced`, `EUREKA_REACH_LEDGER_RELPATH`
- Ledger file `<room>/.mindrian/eureka-reach-ledger.json` (`schema_version`, `entries{<handle>: {at}}`)
- `lib/core/sensors/sensor-eureka.cjs`: `SCHEMA_VERSION = 2`; evidence fields `opportunity_handle`, `stamp_verification`, `stamp_backend`, `stamp_direction`, `stamp_judge`, `stamp_reason`, `stamp_path`
- Test `tests/test-355-side-channel-v2.cjs`

<verification>
- New test, readers test, all 213 tests and the 219 harvest-sensor test green
</verification>

<success_criteria>
- Side channel v2 carries the stamp; guard probe unaffected; fire-once ledger ready; sensor pure
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-19-SUMMARY.md` when done.
</output>
