---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 22
type: tdd
wave: 6
depends_on: ["355-20"]
files_modified:
  - tests/test-355-gate-opportunity-promotion.cjs
  - lib/mcp/tools/gate.cjs
  - tests/test-355-sens13-fire-once.cjs
  - lib/core/navigation-engine.cjs
  - lib/hmi/dial-label-composer.cjs
  - tests/test-dial-label-bank-drift.cjs
autonomous: true
requirements: [HIPS-06, HIPS-05]
tags: [gate-answer, promotion, sens-13, fire-once, reach-card, d-57-file]

must_haves:
  truths:
    - "D-39: _promoteCardSubject in lib/mcp/tools/gate.cjs promotes a card subject whose row.type is 'claim' OR 'opportunity'; the Phase 348 human-attribution guard (TRUTH_CLAIM_TYPES includes opportunity) still applies; a regression test proves the exact opportunity node id reads proposed before and confirmed after gate_answer approve with card.subjectNodeId = that id and kind 'general'; SYS-08's tests/test-354-gate-subject-promotion.cjs stays green"
    - "SPEC Req 6 / D-43: without gate_answer the opportunity stays proposed (no other path confirms it); an agent-attributed promotion is refused"
    - "D-42: SENS-13 fires exactly once for a filed opportunity across two consecutive decide() dispatches: the surfacing seam in navigation-engine.cjs (after the Phase 222 ranking, when the winning reach carries signal 'eureka_bridge' and an evidence.opportunity_handle) calls markEurekaReachSurfaced(roomDir, handle), soft-fail; the sensor stays read-only; SENS-14 is not suppressed"
    - "D-41: the deep_research reach card gains a signal-aware stamped-finding variant in lib/hmi/dial-label-composer.cjs: 'verified through <stamp_path>' for strong / indirect and exactly 'unverified - novel or hallucinated, verify with an expert' otherwise, identical to formatStampLines(stamp, 'card'); reach_id and signal unchanged; tests/test-dial-label-bank-drift.cjs is amended with a Phase 355 citation"
    - "AI-SPEC Section 7: gate.cjs writes one local memory_event 'cross_connection_gate_outcome' { finding_id, tier, direction, response } when the promoted or rejected subject is an opportunity carrying a verification prop; exact key set; no content"
    - "D-57: gate.cjs is re-read from 355-BASELINE.md anchors plus a fresh grep (354-02/12/16 changed it); only the type check line and the outcome event are added"
  artifacts:
    - path: "tests/test-355-gate-opportunity-promotion.cjs"
      provides: "proposed -> confirmed only through gate_answer, exact node id"
    - path: "tests/test-355-sens13-fire-once.cjs"
      provides: "exactly one SENS-13 fire across two dispatches + card text"
  key_links:
    - from: "lib/mcp/tools/gate.cjs _promoteCardSubject"
      to: "lib/core/navigation confirmNode / transitions human guard"
      via: "widened type check claim|opportunity"
      pattern: "'opportunity'"
    - from: "lib/core/navigation-engine.cjs decide()"
      to: "lib/core/eureka/eureka-reach-runner.cjs markEurekaReachSurfaced"
      via: "lazy require after ranking, soft-fail"
      pattern: "markEurekaReachSurfaced"
---

<objective>
Close the Larry touchpoint of SPEC Req 6 (HIPS-06, HIPS-05): the filed opportunity surfaces exactly once as a SENS-13 reach whose card carries the stamp, and only a human `gate_answer` promotes it from proposed to confirmed (D-39, D-41, D-42).

D-57: `lib/mcp/tools/gate.cjs` is a shared file; re-read it first.
Output: gate widen + outcome event, surfacing mark, card variant, two tests, one amended drift test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-BASELINE.md
@tests/helpers/fixture-room-355.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gate_answer promotes an opportunity subject (D-39) and records the outcome</name>
  <files>tests/test-355-gate-opportunity-promotion.cjs, lib/mcp/tools/gate.cjs</files>
  <read_first>
    - lib/mcp/tools/gate.cjs: re-grep `grep -n "_promoteCardSubject\|subject_not_claim\|row.type" lib/mcp/tools/gate.cjs`, then read _promoteCardSubject and its call site in gate_answer in full
    - lib/core/navigation/transitions.cjs lines 35-50 and 225-250 (TRUTH_CLAIM_TYPES, agent_attribution_forbidden)
    - tests/test-354-gate-subject-promotion.cjs (SYS-08's harness: how it builds a card with subjectNodeId and calls gate_answer; copy the approach)
    - tests/helpers/fixture-room-355.cjs (buildFilingRoom from 355-20)
  </read_first>
  <behavior>
    - after buildFilingRoom + bankStatements, the opportunity row reads proposed; a gate_answer approve on a card { kind: 'general', subjectNodeId: <that id> } makes a fresh read (close + reopen) return confirmed for exactly that id and changes no other node's review_status
    - a gate_answer reject leaves it proposed (or the existing reject semantics for claims, unchanged)
    - a card whose subject is a 'decision' or other non-truth type still reports subject_not_claim-style skip (behavior for non-claim, non-opportunity types unchanged)
    - navigation promoteNodeStatus with an agent actor on the opportunity returns agent_attribution_forbidden
    - a memory_event 'cross_connection_gate_outcome' with exactly { finding_id, tier, direction, response } is written for the opportunity outcome; none for a plain claim
  </behavior>
  <action>RED: write tests/test-355-gate-opportunity-promotion.cjs (hygiene-355 preamble; reuse the SYS-08 harness approach); run; fail at the promotion leg. GREEN: in _promoteCardSubject change only the type check so 'opportunity' passes alongside 'claim' (keep the skip reason for other types; cite "Phase 355 D-39: opportunity is a TRUTH_CLAIM_TYPE; the Phase 348 human-attribution guard applies"); after the promote / reject outcome for an opportunity subject that has a `verification` prop, write the outcome memory_event through navigation's memory_event writer honoring MINDRIAN_DISABLE_MEMORY_EVENT. Run the new test, tests/test-354-gate-subject-promotion.cjs and every other gate test (`ls tests | grep -i gate`, run each). Commit `feat(355-22): gate_answer promotes a proposed opportunity; outcome recorded locally (HIPS-06, D-39)`.</action>
  <verify>
    <automated>node tests/test-355-gate-opportunity-promotion.cjs && node tests/test-354-gate-subject-promotion.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both tests exit 0; every `tests/*gate*.cjs` exits 0
    - `git diff -U0 HEAD~1 -- lib/mcp/tools/gate.cjs | grep '^[-+]' | grep -v '^[-+][-+]' | grep -c "row.type"` shows the single changed type-check line (1 removed, 1 added)
    - `grep -c "D-39" lib/mcp/tools/gate.cjs` >= 1
  </acceptance_criteria>
  <done>Only a human gate turns a stamped finding into a confirmed opportunity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fire-once surfacing mark and the stamped reach card (D-41, D-42)</name>
  <files>tests/test-355-sens13-fire-once.cjs, lib/core/navigation-engine.cjs, lib/hmi/dial-label-composer.cjs, tests/test-dial-label-bank-drift.cjs</files>
  <read_first>
    - lib/core/navigation-engine.cjs lines 940-960 (decide signature) and 1270-1310 (dispatchSensors call, the Phase 222 rankFiredCandidates block, sensorReaches[0])
    - lib/core/insight-sensors.cjs isReachEligibleForTurn and turnCountOf (deep_research is gated in turns 1-2)
    - an existing test that calls decide() with a real roomDir (`grep -rln "navigation-engine" tests | head`) for the minimal context shape
    - lib/hmi/dial-label-composer.cjs lines 95-120 (TEMPLATE_FAMILIES.deep_research) and composeLabel (~:347) and who builds its slotContext from a reach
    - tests/test-dial-label-bank-drift.cjs
    - lib/core/eureka/eureka-reach-runner.cjs writeStampedSideChannel / markEurekaReachSurfaced (355-19)
  </read_first>
  <behavior>
    - on a tmp room holding a fresh v2 side channel written by writeStampedSideChannel for a filed opportunity (guard transferable, firing band, direction in DIRECTIONS), two consecutive decide() calls with turn_count 6: the first result's fired sensor reaches include exactly one reach with signal 'eureka_bridge' (SENS-13); the second's include none; .mindrian/eureka-reach-ledger.json holds the handle
    - calling dispatchSensors twice WITHOUT decide fires SENS-13 twice (the sensor itself is pure; the dedup lives at the surfacing seam) - asserted and documented
    - a harvest (SENS-14) reach is unaffected by the ledger (tests/test-219-harvest-sensor.cjs green)
    - composeLabel('deep_research', slotContext from that reach) returns 'verified through <stamp_path>' for a verified stamp and exactly 'unverified - novel or hallucinated, verify with an expert' for an unverified one; the text equals formatStampLines(stamp, 'card')[0] for the same stamp; no D-30 decimal
    - a markEurekaReachSurfaced failure (read-only ledger dir) never throws out of decide()
  </behavior>
  <action>RED: write tests/test-355-sens13-fire-once.cjs (hygiene-355 preamble; tmp room; fixed clock) per the behavior list; run; fail. GREEN: navigation-engine.cjs: immediately after the Phase 222 ranking try block, add a guarded block: if `ctx.roomDir` is a string and `sensorReaches[0]` has signal 'eureka_bridge' and a non-empty `evidence.opportunity_handle`, lazy-require `./eureka/eureka-reach-runner.cjs` and call markEurekaReachSurfaced(ctx.roomDir, handle) inside try/catch (soft-fail, no other side effect), with a comment citing Phase 355 D-42 ("the sensor stays pure; the seam that surfaces the reach records it"). dial-label-composer.cjs: add the stamped-finding variant to the deep_research family, selected when slotContext.signal === 'eureka_bridge' and slotContext.stamp_verification is present; verified -> 'verified through ' + slotContext.stamp_path; otherwise the exact unverified sentence; make the slotContext builder pass the reach's stamp_* evidence fields and signal through (read the builder first). Amend tests/test-dial-label-bank-drift.cjs to include the new variant with a header note "Amended Phase 355 D-41". Run the new test, the drift test, tests/test-213-sensor-eureka.cjs, tests/test-213-touchpoints.cjs, tests/test-213-no-force.cjs, tests/test-219-harvest-sensor.cjs, tests/test-237-session-scope.cjs and `node scripts/check-render-coverage.cjs --check`. Commit `feat(355-22): SENS-13 surfaces a filed opportunity once; stamped reach card (HIPS-06, HIPS-05, D-41, D-42)`.</action>
  <verify>
    <automated>node tests/test-355-sens13-fire-once.cjs && node tests/test-dial-label-bank-drift.cjs && node tests/test-213-sensor-eureka.cjs && node tests/test-213-touchpoints.cjs && node tests/test-219-harvest-sensor.cjs && node tests/test-237-session-scope.cjs && node scripts/check-render-coverage.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command exits 0
    - `grep -c "markEurekaReachSurfaced" lib/core/navigation-engine.cjs` = 1 and `grep -c "D-42" lib/core/navigation-engine.cjs` >= 1
    - `grep -c "unverified - novel or hallucinated, verify with an expert" lib/hmi/dial-label-composer.cjs` = 1
    - `grep -c "Phase 355 D-41" tests/test-dial-label-bank-drift.cjs` >= 1
  </acceptance_criteria>
  <done>Larry's next turn offers the filed opportunity once, with its stamp on the card.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP gate_answer (host) -> truth-claim promotion | The only door from proposed to confirmed |
| decide() hot path -> local ledger write | A side effect in the selection path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-107 | Elevation of privilege | opportunity confirmed without a human | mitigate | Widened check still flows through confirmNode and the Phase 348 human-attribution guard; agent promotion refused (tested) |
| T-355-108 | Tampering | widening beyond opportunity | mitigate | Only 'opportunity' added; other types keep the skip reason (tested) |
| T-355-109 | Denial of service | a ledger write failure breaking decide() | mitigate | Lazy require + try/catch soft-fail; tested with an unwritable ledger dir |
| T-355-110 | Repudiation | repeated reach nagging the user | mitigate | Handle recorded at the surfacing seam; second dispatch silent (tested) |
| T-355-111 | Information disclosure | content in the gate-outcome event | mitigate | Exact key set: opaque local id, tier enum, direction enum, response enum |
</threat_model>

## Artifacts this phase produces

This plan's share:
- `lib/mcp/tools/gate.cjs`: `_promoteCardSubject` accepts `'opportunity'`; memory_event `cross_connection_gate_outcome` (`finding_id`, `tier`, `direction`, `response`)
- `lib/core/navigation-engine.cjs`: surfacing mark via `markEurekaReachSurfaced` after ranking
- `lib/hmi/dial-label-composer.cjs`: deep_research stamped-finding variant (slotContext keys `signal`, `stamp_verification`, `stamp_path`)
- Tests `tests/test-355-gate-opportunity-promotion.cjs`, `tests/test-355-sens13-fire-once.cjs`

<verification>
- Promotion, fire-once, drift, 213, 219, 237 and gate tests green; render coverage green
</verification>

<success_criteria>
- SENS-13 fires once per filed opportunity; only gate_answer confirms it
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-22-SUMMARY.md` when done.
</output>
