---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 25
type: execute
wave: 9
depends_on: ["355-24"]
files_modified:
  - scripts/measure-355-hit-rate.cjs
  - tests/fixtures/355-theo-find-connections-responses.json
  - tests/fixtures/355-rooms/stamps.json
  - tests/fixtures/355-rooms/pairings-stamped.items.json
  - tests/fixtures/355-rooms/judgments-stamped.json
  - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/labeling-session-pairings-stamped.json
  - tests/fixtures/355-rooms/hit-rate-record.json
  - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-VERIFICATION.md
autonomous: false
requirements: [HIPS-07, HIPS-04]
tags: [hit-rate, stamping, as-shown, hub-inflation, verification-record, checkpoint]

must_haves:
  truths:
    - "D-32: stamps for the fixture-room pairings are computed only AFTER the sitting-1 judgments are committed; Theo is reached through capture-355-theo-responses.cjs --live --append (brain-client, canon names only) and every stamp is computed by replaying that capture, so the record replays offline"
    - "AI-SPEC Labeling: sitting 2 shows the same pairings shuffled WITH their stamp lines (the as-shown pass); per-tier rates are computed from the BLIND sitting-1 judgments joined by pair_id; the as-shown rate sits beside them and the gap is recorded as the stamp's influence on the reader, not as usefulness"
    - "SPEC Req 7 / AC12: 355-VERIFICATION.md records, for each of the three rooms, every shown pairing's judgment, >= 20 judged pairings per room, the hit rate per room and pooled with n and a 95% Wilson interval, the unstamped baseline rate, and the per-tier rates (strong / indirect / unverified) with n; no target rate is promised and the text says this is the first calibration point, taken on dev-repo fixture rooms, not real ventures; no live room was read or written"
    - "AI-SPEC Section 6 (recorded with no target): unverified rate and reason mix, the not_called (handle_unresolved) share from D-48, the hub-inflation share (in-sample proxy: strong paths whose interior crosses a top-decile-frequency node), the provenance-routed strong count (C6), diversity (distinct interior nodes / strong stamps), the false-friend count per tier from planted-cases.json (D17), the verified-vs-already-known cross-tab, and Theo find_connections latency p50 / p95 (its first plugin-side measurement)"
    - "RESEARCH C2 / D-47: the record states the portfolio ranking tension (today's feasibility map rewards shared-words pairs, preserved byte-for-byte this phase) with the per-direction hit rate as the evidence for the next engine phase"
    - "Disclosed limitations: Desktop / Cowork cannot compute a stamp (D-50); compute-hsi.py still writes retired-convention strings that no reader trusts (D-07); the name snapshot's date (D-51); one labeler unless a second was available"
    - "tests/fixtures/355-rooms/hit-rate-record.json is the machine source of every number; `node scripts/measure-355-hit-rate.cjs --check` recomputes it exactly and regenerates the 355-VERIFICATION.md hit-rate section"
  artifacts:
    - path: ".planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-VERIFICATION.md"
      provides: "SPEC Req 7 hit-rate record (section '## Hit-rate record (SPEC Req 7)')"
      contains: "Wilson"
    - path: "tests/fixtures/355-rooms/hit-rate-record.json"
      provides: "Machine record replayed by --check"
    - path: "tests/fixtures/355-rooms/stamps.json"
      provides: "pair_id -> { stamp, detail } computed from the replayed capture"
  key_links:
    - from: "scripts/measure-355-hit-rate.cjs record"
      to: "tests/fixtures/355-rooms/judgments.json (blind) + stamps.json"
      via: "joinJudgments by pair_id; per-tier rates from blind labels only"
      pattern: "joinJudgments"
---

<objective>
SPEC Req 7 second half (HIPS-07; HIPS-04's hub and coverage measurements): stamp the already-judged pairings through one live Theo capture, run the as-shown sitting, and write the first hit-rate record in the engine's history into 355-VERIFICATION.md, with every number replayable offline and nothing presented as a target or a win.

Output: stamping and record subcommands, stamps and stamped items, sitting-2 judgments, the machine record, the verification record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-AI-SPEC.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-24-SUMMARY.md
@scripts/measure-355-hit-rate.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stamp the judged pairings through one live capture; build the stamped item file</name>
  <files>scripts/measure-355-hit-rate.cjs, tests/fixtures/355-theo-find-connections-responses.json, tests/fixtures/355-rooms/stamps.json, tests/fixtures/355-rooms/pairings-stamped.items.json</files>
  <read_first>
    - scripts/measure-355-hit-rate.cjs (355-24), scripts/capture-355-theo-responses.cjs (--live --append), lib/core/verification-stamp.cjs (extractCarried, resolveEndpoint, stampFindingsDetailed), lib/core/verification-stamp-format.cjs
    - `git log --oneline -1 -- tests/fixtures/355-rooms/judgments.json` (must exist: stamps only after the blind sitting)
  </read_first>
  <action>Implement the `stamp` subcommand: refuse unless judgments.json exists and its fixture_sha256 matches pairings.items.json; for each item re-derive the two artifact paths from a fresh temp copy of its room, resolve carried names (extractCarried + resolveEndpoint against the snapshot), write the distinct resolvable name pairs to a scratch pairs file, run `capture-355-theo-responses.cjs --live --append --pairs <file>` once (canon names only; if Theo is unreachable stop and report BLOCKED rather than recording a whole-run outage as findings), then stamp every item with stampFindingsDetailed using a local replay function over the capture file (no network during stamping); direction = the item's recorded direction (none for whitespace / find-connections items); write tests/fixtures/355-rooms/stamps.json { generated_at, capture_sha256, snapshot_sha256, by_pair: { <pair_id>: { stamp, detail } } } and tests/fixtures/355-rooms/pairings-stamped.items.json (same items plus `stamp_lines` = formatStampLines(stamp, 'cli') joined with newlines; no other new field). Commit `data(355-25): stamps for the judged fixture pairings via one live Theo capture (HIPS-04, HIPS-07)`.</action>
  <verify>
    <automated>node scripts/capture-355-theo-responses.cjs --check && node -e "const s=require('./tests/fixtures/355-rooms/stamps.json');const i=require('./tests/fixtures/355-rooms/pairings.items.json');const {Stamp}=require('./lib/core/verification-stamp.cjs');if(Object.keys(s.by_pair).length!==i.items.length)process.exit(1);for(const v of Object.values(s.by_pair))Stamp.parse(v.stamp)"</automated>
  </verify>
  <acceptance_criteria>
    - Both verify commands exit 0 (every judged pairing has a parseable stamp; the appended capture still validates)
    - The stamps commit is a descendant of the judgments commit (`git merge-base --is-ancestor <judgments commit> HEAD`)
  </acceptance_criteria>
  <done>Every judged pairing now has a byte-replayable stamp.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Sitting 2 - the navigator judges the same pairings as shown (with stamps)</name>
  <files>tests/fixtures/355-rooms/judgments-stamped.json, .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/labeling-session-pairings-stamped.json</files>
  <read_first>
    - scripts/label-355-gold.cjs usage (`--help`)
  </read_first>
  <action>Ask for a later sitting (not the same session as sitting 1). On resume, verify and commit the judgments with the session file as `docs(355-25): navigator as-shown sitting-2 judgments`.</action>
  <what-built>tests/fixtures/355-rooms/pairings-stamped.items.json (the sitting-1 pairings, shuffled by the CLI, each with its stamp lines)</what-built>
  <how-to-verify>
    1. Run `node scripts/label-355-gold.cjs start --set pairings-stamped` (resume with `resume --set pairings-stamped`).
    2. Answer the same three y/n prompts per pairing, now reading the stamp lines too.
    3. When all are labeled, run `node scripts/label-355-gold.cjs emit --set pairings-stamped` and reply "judged".
  </how-to-verify>
  <verify><automated>node -e "const j=require('./tests/fixtures/355-rooms/judgments-stamped.json');const i=require('./tests/fixtures/355-rooms/pairings-stamped.items.json');if(!j.labeled_at||j.judgments.length!==i.items.length)process.exit(1)"</automated></verify>
  <acceptance_criteria>
    - The verify command exits 0
  </acceptance_criteria>
  <resume-signal>Type "judged" once emit has written tests/fixtures/355-rooms/judgments-stamped.json</resume-signal>
  <done>The as-shown judgments exist beside the blind ones.</done>
</task>

<task type="auto">
  <name>Task 3: Compute the record and write 355-VERIFICATION.md (SPEC Req 7)</name>
  <files>scripts/measure-355-hit-rate.cjs, tests/fixtures/355-rooms/hit-rate-record.json, .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-VERIFICATION.md</files>
  <read_first>
    - 355-AI-SPEC.md Section 5 D16, D17 and "Reference Dataset" labeling protocol; Section 6 flywheel table; Section 7 key metrics
    - tests/fixtures/355-rooms/planted-cases.json (joined now, after both sittings)
    - tests/fixtures/355-theo-find-connections-responses.json latency_ms
    - tests/test-355-hit-rate-record.cjs (the record legs that must turn green)
  </read_first>
  <action>Implement `record` and make `--check` recompute it: computeRates over the blind judgments + stamps (per room, pooled, per tier, each { k, n, rate, wilson }), the unstamped baseline rate (sitting 1 over all shown pairings), the as-shown rate (sitting 2) and the gap, unverified share and reason mix, not_called share, hub-inflation share (in-sample proxy: interior nodes of all strong paths, top-decile by frequency; name the source as the proxy), provenance-routed strong count (a lateral path that also crosses a BrainRecord node or a SOURCED_FROM edge), diversity, false-friend count per tier (planted false_friends whose direction_ok was false, joined by artifact paths), the already_known x tier cross-tab, Theo latency p50 / p95; write tests/fixtures/355-rooms/hit-rate-record.json (rates as fractions in data only). Write 355-VERIFICATION.md (Feynman prose, hyphens only, numbers allowed here because this is a dev record, not a user surface) with the section `## Hit-rate record (SPEC Req 7)` and subsections Rooms and producers, Hit rate (per room, pooled, per tier, each with n and Wilson), Unstamped baseline, As-shown rate and stamp influence, Stamp mix and not_called share, Hub inflation / provenance routing / diversity, Direction fidelity (false friends per tier), Verified versus already known, Theo latency, Portfolio ranking tension (D-47), Disclosed limitations, and a closing paragraph: first calibration point, dev fixture rooms not real ventures, no target; the briefing's "50%+ of top-10" quoted only as its own reference point; a high unverified rate is a canon-coverage finding for Theo (T-3), not a phase failure. Add an HTML comment at the top of the section: "Regenerate with node scripts/measure-355-hit-rate.cjs record; verifiers append below, never overwrite this section." Run `node scripts/measure-355-hit-rate.cjs --check` and `node tests/test-355-hit-rate-record.cjs` (all legs now green). Commit `docs(355-25): first human-judged hit rate recorded (HIPS-07)`.</action>
  <verify>
    <automated>node scripts/measure-355-hit-rate.cjs --check && node tests/test-355-hit-rate-record.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both commands exit 0; the record test has no SKIP
    - `grep -c "## Hit-rate record (SPEC Req 7)" .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-VERIFICATION.md` = 1 and the section contains "Wilson", "unstamped baseline" and "first calibration point"
    - `grep -ciE "\b(works|improves discovery|validated|proven)\b" .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-VERIFICATION.md` prints 0
    - `node -e "const r=require('./tests/fixtures/355-rooms/hit-rate-record.json');for(const room of Object.values(r.per_room))if(room.n<20)process.exit(1)"` exits 0
  </acceptance_criteria>
  <done>The engine's first measured usefulness is on record, honest about what it is.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> Theo (capture append) | Canon names only |
| judgments -> the recorded rate | The number later phases calibrate against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-121 | Tampering | stamps anchoring the blind judgment | mitigate | Stamps computed only after the sitting-1 commit (refusal + ancestry check); per-tier rates use blind labels only |
| T-355-122 | Repudiation | a rate without its n or interval | mitigate | Every rate stored as { k, n, rate, wilson }; record test requires n and Wilson |
| T-355-123 | Spoofing | the record claiming success | mitigate | Banned-claim grep; "first calibration point, no target" paragraph required |
| T-355-124 | Information disclosure | non-canon strings to Theo | mitigate | Only resolveEndpoint results go to the capture; capture script refuses non-members |
| T-355-125 | Tampering | verifier overwriting the record | mitigate | Machine record is the source; section regenerable by `record`; header comment instructs append-only |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Subcommands `stamp`, `record` and a full `--check` in `scripts/measure-355-hit-rate.cjs`
- Fixtures `tests/fixtures/355-rooms/stamps.json` (`capture_sha256`, `snapshot_sha256`, `by_pair`), `pairings-stamped.items.json` (`stamp_lines`), `judgments-stamped.json`, `hit-rate-record.json` (`per_room`, `pooled`, `per_tier`, `baseline`, `as_shown`, `unverified_share`, `reason_mix`, `not_called_share`, `hub_inflation_share`, `provenance_routed_strong`, `diversity`, `false_friends_by_tier`, `already_known_by_tier`, `theo_latency_ms {p50, p95}`)
- Record section "## Hit-rate record (SPEC Req 7)" in `.planning/phases/355-.../355-VERIFICATION.md`

<verification>
- `node scripts/measure-355-hit-rate.cjs --check` and `node tests/test-355-hit-rate-record.cjs` exit 0
</verification>

<success_criteria>
- >= 20 judged pairings per room, hit rate + baseline + per-tier with n and Wilson, first Theo latency figures, all replayable
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-25-SUMMARY.md` when done.
</output>
