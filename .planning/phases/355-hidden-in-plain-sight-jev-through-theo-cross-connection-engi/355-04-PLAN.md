---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 04
type: tdd
wave: 2
depends_on: ["355-01"]
files_modified:
  - tests/test-355-floor-sweep.cjs
  - data/floor-ledger.json
  - scripts/check-floor-ledger.cjs
  - lib/core/floor-disclosure.cjs
  - docs/ENV-TUNING.md
autonomous: true
requirements: [HIPS-02]
tags: [floors, ledger, disclosure, sweep, negative-control]

must_haves:
  truths:
    - "D-19: every floor, band, weight, rubric, policy and definitional literal is recorded with status 'disclosed' this phase; none is calibrated (calibrating on the fixture rooms would be circular with the hit rate)"
    - "D-20: data/floor-ledger.json holds one row per literal { id, file, line_anchor (const name or 're:' regex, never a bare line number), value, gates, kind: floor|band|weight|rubric|policy|definitional, env_override, env_read: load|call|null, status: disclosed|calibrated, provenance: null|{fixture, encoder, date, method, gold, n}, dependent_outputs: [producer ids] }; 'calibrated' without provenance.gold and provenance.n fails --check"
    - "D-21 + RESEARCH HIPS-02 row: the ledger covers every D-21 inventory item plus hsi-to-graph.cjs's 0.3 literal and the whitespace band label text, plus every other hit the sweep's pattern pass finds in the engine families"
    - "D-22: tests/test-355-floor-sweep.cjs strips comments, prints its scanned-file count and fails on zero, resolves every hit to a ledger row by file + anchor, fails on a stale anchor, catches a planted `const FAKE_FLOOR = 0.42` (negative control), and proves every disclosed row's dependent producers get an 'unverified' disclosure through lib/core/floor-disclosure.cjs"
    - "D-20: docs/ENV-TUNING.md stays operator env docs and gains prose entries for EUREKA_DIFF_FLOOR, the eureka-critic Stage A gate env names and the analogy-fitness env names (whatever the code actually reads)"
  artifacts:
    - path: "data/floor-ledger.json"
      provides: "Floor ledger (D-20 row shape), all rows disclosed"
      contains: "\"status\": \"disclosed\""
    - path: "scripts/check-floor-ledger.cjs"
      provides: "--check offline validator: shape, anchors resolve, calibrated needs provenance"
    - path: "lib/core/floor-disclosure.cjs"
      provides: "PRODUCER_IDS, disclosureFor(producerId), disclosureLine(producerId)"
      exports: ["PRODUCER_IDS", "disclosureFor", "disclosureLine", "loadLedger"]
    - path: "tests/test-355-floor-sweep.cjs"
      provides: "The D-22 sweep with a negative control"
      min_lines: 120
  key_links:
    - from: "lib/core/floor-disclosure.cjs"
      to: "data/floor-ledger.json"
      via: "fs read, cached, rows filtered by dependent_outputs"
      pattern: "floor-ledger.json"
    - from: "tests/test-355-floor-sweep.cjs"
      to: "scripts/check-floor-ledger.cjs"
      via: "require of the exported validateLedger / resolveHits"
      pattern: "check-floor-ledger"
---

<objective>
SPEC Req 2 (HIPS-02): no threshold literal is presented as a verdict. Ship the floor ledger (D-20), disclose every row (D-19, D-21), give producers one helper that turns the ledger into the devpkg-shaped `unverified` block and one rendered line, and prove it with a comment-stripped sweep that catches a planted literal (D-22). Plan 355-23 adds the sweep's render leg once all five producers print their disclosure line.

Purpose: a user never reads "0.3" as if someone had measured it; the honest statement "this cut-off is a default" rides every output that depends on one.
Output: `data/floor-ledger.json`, `scripts/check-floor-ledger.cjs`, `lib/core/floor-disclosure.cjs`, `tests/test-355-floor-sweep.cjs`, ENV-TUNING prose.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PATTERNS.md
@docs/ENV-TUNING.md
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED sweep test, then the ledger and its --check validator (D-20, D-21, D-22)</name>
  <files>tests/test-355-floor-sweep.cjs, data/floor-ledger.json, scripts/check-floor-ledger.cjs</files>
  <read_first>
    - data/section-command-ledger.json first 30 lines and scripts/build-section-command-ledger.cjs lines 600-620 (house ledger top-level shape and the offline --check discipline: zero network, never requires brain-client)
    - tests/test-353-tripwires.cjs lines 79-100 (negative-control idiom; READ ONLY)
    - Every D-21 file at its literal: lib/core/rs-engine.cjs (DEFAULT_THRESHOLD), lib/core/rs-differential-scorer.cjs (DIFF_FLOOR, LSA_FLOOR, BERT_FLOOR, SEMANTIC_FLOOR IIFE, EUREKA_DIFF_FLOOR resolver, bandFor, the highLeg 0.2 test in scoreMeasured), lib/core/rs-innovation-classifier.cjs (LSA_HIGH, BERT_HIGH), lib/core/rs-breakthrough-scorer.cjs (the five rubrics), lib/core/hsi-engine.cjs (the 0.30 threshold and the two weight pairs), scripts/hsi-to-graph.cjs (the 0.3 literal near :100), scripts/compute-whitespace-gaps.py (10th percentile near :250), scripts/whitespace-command.cjs (novelty bands 0.8 / 0.4 and the `Novel (>0.8)` / `Covered (<0.4)` label text), scripts/interpret-whitespace.cjs (0.6 near :45, 1.0 near :273), lib/core/eureka-critic.cjs (Stage A gates, confidenceFromBucket bands), lib/core/eureka/tail-quadrant.cjs (quadrant constants), lib/core/eureka/analogy-fitness.cjs (LAYER_THRESHOLD, TEXT_WEIGHT, RESTATEMENT_FLOOR, bands)
    - 355-CONTEXT.md D-19..D-22 and 355-RESEARCH.md HIPS-02 row (the two literals D-21 missed)
  </read_first>
  <behavior>
    - scope: FLOOR_SCAN_FILES (every D-21 file plus scripts/hsi-to-graph.cjs) UNION a pattern pass over the engine families lib/core/rs-*.cjs, lib/core/hsi-*.cjs, lib/core/eureka/*.cjs, lib/core/eureka-critic.cjs, scripts/*whitespace*.cjs, scripts/hsi-*.cjs, scripts/compute-whitespace-gaps.py; the test prints `scanned N files` and fails when N === 0
    - hit patterns on comment-stripped lines (JS `//`, `/*`, `*` lines; Python `#` lines): `const [A-Z_]+ = 0?\.\d+`, `envFloat\('`, `resolveFloat\('`, and a numeric comparison `[<>]=?\s*(0?\.\d+|1\.0+)\b`
    - every hit line resolves to a row with the same file whose line_anchor matches it (a const name matches as `\bNAME\b`; an anchor starting `re:` is a regex); any unresolved hit fails with file:line and the line text
    - every row's anchor matches at least one comment-stripped line of its file; a stale anchor fails
    - every row has status 'disclosed' (D-19); a row with status 'calibrated' and provenance missing gold or n fails validateLedger
    - negative control: writes lib/core/rs-__scratch-floor-355.cjs with `const FAKE_FLOOR = 0.42;`, asserts resolveHits reports it unresolved, deletes it in finally
    - the ledger contains a row for each D-21 item and the two research additions (asserted by an EXPECTED_IDS list in the test)
  </behavior>
  <action>
RED: write tests/test-355-floor-sweep.cjs (hygiene-355 preamble) implementing the behavior list by requiring `validateLedger(ledger, repoRoot)` and `resolveHits(ledger, repoRoot, extraFiles)` from scripts/check-floor-ledger.cjs, plus an EXPECTED_IDS list with at least these ids: `rs-engine.DEFAULT_THRESHOLD`, `rs-differential-scorer.DIFF_FLOOR`, `rs-differential-scorer.LSA_FLOOR`, `rs-differential-scorer.BERT_FLOOR`, `rs-differential-scorer.SEMANTIC_FLOOR`, `rs-differential-scorer.EUREKA_DIFF_FLOOR`, `rs-differential-scorer.bandFor`, `rs-differential-scorer.highLeg`, `rs-innovation-classifier.LSA_HIGH`, `rs-innovation-classifier.BERT_HIGH`, `rs-breakthrough-scorer.rubrics`, `hsi-engine.threshold`, `hsi-engine.weights`, `hsi-to-graph.threshold`, `compute-whitespace-gaps.percentile`, `whitespace-command.novelty-bands`, `interpret-whitespace.thresholds`, `eureka-critic.stage-a-gates`, `eureka-critic.confidence-bands`, `tail-quadrant.constants`, `analogy-fitness.thresholds`, `analogy-fitness.bands` (split a multi-literal item into more rows if one anchor cannot match every line; keep the listed ids as the primary rows). Run it; it fails (no ledger). Commit `test(355-04): add failing floor sweep`.

GREEN: create scripts/check-floor-ledger.cjs (argv switch-case: `--check` validates data/floor-ledger.json offline and exits 0/1 with each problem printed; exports `validateLedger`, `resolveHits`, `stripComments`, `SCAN_FAMILIES`, `FLOOR_SCAN_FILES`; zero network, never requires brain-client). Create data/floor-ledger.json with top-level `{ "built_at", "phase": "355", "floor_basis": "disclosed, not calibrated: every row is a default cut-off; calibrating on the 355 fixture rooms would be circular with the hit rate (D-19)", "rows": [ ... ] }`. For each row fill: `id`, `file`, `line_anchor` (prefer the const name; else `re:` + a regex that matches the literal's line, never a line number), `value` (number or array of numbers), `gates` (one plain sentence: what the literal decides), `kind`, `env_override` (the env var name the code actually reads, or null), `env_read` ('load' for module-load IIFEs, 'call' for per-call reads, null), `status: 'disclosed'`, `provenance: null`, `dependent_outputs` (subset of ['eureka','find-connections','find-bottlenecks','hsi','whitespace']; [] for a definitional constant no producer's shown output depends on). Then add a row for every other hit the pattern pass finds in the engine families until the sweep has zero unresolved hits. ANCHOR DURABILITY: later plans edit render lines in these files (355-16 removes the whitespace `Novel (>0.8)` / `Covered (<0.4)` label text; 355-09/355-10 touch the hsi-engine and scoreMeasured label lines), so anchor on the constant or the comparison, and when a render-text literal repeats a comparison literal cover both with ONE `re:` anchor (for example `re:(>=?\\s*0?\\.8|>=?\\s*0?\\.4|<\\s*0?\\.4)` for the whitespace novelty bands) so removing the render text never stales the row.
Run the test; all legs except the dependent-output leg (Task 2) pass. Commit `feat(355-04): floor ledger + offline --check (HIPS-02, D-19..D-21)`.
  </action>
  <verify>
    <automated>node scripts/check-floor-ledger.cjs --check && node tests/test-355-floor-sweep.cjs 2>&1 | grep -E "scanned [1-9][0-9]* files"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-floor-ledger.cjs --check` exits 0
    - `node -e "const l=require('./data/floor-ledger.json');if(!l.rows.length||l.rows.some(r=>r.status!=='disclosed'||/^\d+$/.test(String(r.line_anchor))))process.exit(1)"` exits 0
    - test output contains `scanned N files` with N > 0 and a PASS line for the FAKE_FLOOR negative control; `ls lib/core/rs-__scratch-floor-355.cjs` fails afterwards
    - `grep -vE '^\s*(//|\*|/\*)' scripts/check-floor-ledger.cjs | grep -cE "brain-client|fetch\("` prints 0
  </acceptance_criteria>
  <done>Every threshold literal in the engine families is on a ledger row, and the sweep proves nothing new slips in unrecorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/core/floor-disclosure.cjs and the ENV-TUNING prose</name>
  <files>lib/core/floor-disclosure.cjs, tests/test-355-floor-sweep.cjs, docs/ENV-TUNING.md</files>
  <read_first>
    - lib/core/floor-disclosure.cjs does not exist yet; read data/floor-ledger.json (Task 1 output)
    - docs/ENV-TUNING.md sections DERIVE_CONVERGES_FLOOR / DERIVE_INFORMS_FLOOR (the Phase 224 provenance idiom)
    - skills/larry-personality/SKILL.md lines mentioning "Hedged" and numbers (voice rule for the disclosure wording)
  </read_first>
  <behavior>
    - PRODUCER_IDS deep-equals ['eureka','find-connections','find-bottlenecks','hsi','whitespace'] and is frozen
    - disclosureFor('eureka') returns { observation: null, suggests: null, unverified: [..one plain sentence per disclosed row whose dependent_outputs includes 'eureka'..], evidence_used: [..those row ids..] }
    - for every disclosed row R and every p in R.dependent_outputs, disclosureFor(p).evidence_used includes R.id
    - disclosureLine(p) for every producer id contains the word 'unverified', names data/floor-ledger.json, and matches neither the D-30 decimal regex `(^|[^0-9A-Za-z.])(0?\.[0-9]+|1\.0+)([^0-9.]|$)` nor `[0-9]+%`
    - disclosureFor('not-a-producer') throws
    - the module requires only node:fs and node:path
  </behavior>
  <action>
RED: add a `dependent outputs` leg to tests/test-355-floor-sweep.cjs asserting the behavior list; run; it fails. GREEN: create lib/core/floor-disclosure.cjs ('use strict', BSL header, Phase 355 HIPS-02 / D-19 / D-22 citation, hyphens only): `loadLedger()` (lazy fs read of data/floor-ledger.json, cached, returns {rows: []} on a read failure so a render never crashes), `PRODUCER_IDS`, `disclosureFor(producerId)` building the devpkg shape (observation / suggests / unverified / evidence_used; each unverified sentence reads "<gates, lower-cased> uses a default cut-off that has not been calibrated", never the value), `disclosureLine(producerId)` returning exactly `unverified: the cut-offs behind these findings are defaults, not calibrated yet (data/floor-ledger.json)`. No zod, no network, no writes.
DOCS: append to docs/ENV-TUNING.md a `## Phase 355 disclosed floors` section: one short paragraph stating every engine floor is disclosed, not calibrated (link data/floor-ledger.json and D-19's circularity reason), then one entry per env seam the ledger names (EUREKA_DIFF_FLOOR and the actual Stage A gate and analogy env names read from the code), each saying what it overrides, when it is read (load or call) and that an override is an operator seam, not calibration provenance. Hyphens only.
Commit `feat(355-04): floor disclosure helper + ENV-TUNING entries (HIPS-02)`.
  </action>
  <verify>
    <automated>node tests/test-355-floor-sweep.cjs && node -e "const f=require('./lib/core/floor-disclosure.cjs');for(const p of f.PRODUCER_IDS){const l=f.disclosureLine(p);if(!/unverified/.test(l)||/(^|[^0-9A-Za-z.])(0?\.[0-9]+|1\.0+)([^0-9.]|$)/.test(l))process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-355-floor-sweep.cjs` exits 0 with `FAIL: 0`
    - `grep -c "## Phase 355 disclosed floors" docs/ENV-TUNING.md` prints 1 and `grep -c "EUREKA_DIFF_FLOOR" docs/ENV-TUNING.md` >= 1
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/floor-disclosure.cjs | grep -c "require("` prints 2
    - `grep -P '\x{2014}' lib/core/floor-disclosure.cjs data/floor-ledger.json scripts/check-floor-ledger.cjs tests/test-355-floor-sweep.cjs` prints nothing and `git diff -U0 docs/ENV-TUNING.md | grep '^+' | grep -cP '\x{2014}'` prints 0
  </acceptance_criteria>
  <done>Producers have one call that turns the ledger into an honest "unverified" line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine literals -> rendered output | A default cut-off could be read by a user as a measured verdict |
| sweep test -> lib/core tree | Negative control writes a scratch file into lib/core |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-16 | Repudiation | a floor presented as calibrated | mitigate | validateLedger rejects 'calibrated' without provenance.gold and provenance.n; all rows disclosed this phase |
| T-355-17 | Tampering | a new literal added without a row | mitigate | Pattern pass over the engine families plus FAKE_FLOOR negative control; unresolved hit fails the sweep |
| T-355-18 | Tampering | stale anchor after refactor | mitigate | Anchors are const names or regexes, never line numbers; a row whose anchor matches nothing fails |
| T-355-19 | Information disclosure | floor values rendered in disclosure text | mitigate | disclosureLine carries no value; D-30 regex asserted per producer |
| T-355-20 | Denial of service | missing ledger crashes a render | accept | loadLedger degrades to {rows: []}; the sweep test catches a missing ledger in CI |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Ledger `data/floor-ledger.json` (top-level `built_at`, `phase`, `floor_basis`, `rows[]` with `id`, `file`, `line_anchor`, `value`, `gates`, `kind`, `env_override`, `env_read`, `status`, `provenance`, `dependent_outputs`)
- Validator `scripts/check-floor-ledger.cjs` (`--check`; exports `validateLedger`, `resolveHits`, `stripComments`, `SCAN_FAMILIES`, `FLOOR_SCAN_FILES`)
- Module `lib/core/floor-disclosure.cjs` (exports `PRODUCER_IDS`, `loadLedger`, `disclosureFor`, `disclosureLine`)
- Test `tests/test-355-floor-sweep.cjs` (constant `EXPECTED_IDS`; scratch path `lib/core/rs-__scratch-floor-355.cjs`)
- Docs section `docs/ENV-TUNING.md` "## Phase 355 disclosed floors"

<verification>
- `node scripts/check-floor-ledger.cjs --check` exits 0
- `node tests/test-355-floor-sweep.cjs` exits 0
</verification>

<success_criteria>
- Every floor literal in the engine families is ledgered and disclosed; the sweep catches a planted literal
- Producers can render one honest disclosure line with no number in it
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-04-SUMMARY.md` when done.
</output>
