---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 28
type: tdd
wave: 5
depends_on: ["355-15", "355-16"]
files_modified:
  - tests/fixtures/355-hsi-distillation-sentences.json
  - scripts/distill-hsi-thinking-mode-rules.cjs
  - tests/fixtures/355-jev-hsi-distillation-responses.json
  - data/hsi-thinking-mode-rules.json
  - data/floor-ledger.json
  - lib/core/hsi-spectral.cjs
  - tests/test-355-hsi-distillation.cjs
  - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-JEV-MEASUREMENT.md
autonomous: true
requirements: [HIPS-08]
tags: [conditional, hsi, distillation, rule-table, shipped-as-data]

must_haves:
  truths:
    - "CONDITIONAL (D-45): this plan does work ONLY if tests/fixtures/355-hsi-measurement-record.json says decision 'adopted' (signed by the navigator in 355-15); on 'not_adopted' Task 1 records SKIPPED with that reason, Tasks 2-3 are skipped, and no file listed above is created or changed"
    - "D-45 / AI-SPEC Finding 3: 'adopt' means distilling Jev's labels into a rule table under data/ from a SEPARATE distillation set (never the gold sentences, never the tuning set), shipped as data, read at runtime with zero vendor calls; runtime classification of user sentences through Jev stays blocked by Part 8"
    - "D-45: the table replaces the regex ONLY if, re-measured offline on the gold with the same three cuts, it also clears the fixed bar against the post-fix regex; otherwise the table is not shipped and the record says so"
    - "AI-SPEC pitfall 7: the record states where 'none' sentences go in HSI's Markov chain (THINKING_MODES_v1 feeds buildTransitionMatrix); the five-state chain is unchanged and 'none' sentences are dropped from the transition sequence"
    - "D-19 / D-22: any threshold the distillation procedure uses is a disclosed floor-ledger row"
  artifacts:
    - path: "data/hsi-thinking-mode-rules.json"
      provides: "Distilled rule table with jev_model, distillation fixture sha256, gold fixture sha256 (only if adopted and cleared)"
    - path: "tests/test-355-hsi-distillation.cjs"
      provides: "Disjointness, offline re-measure, provenance and runtime zero-vendor proof"
  key_links:
    - from: "lib/core/hsi-spectral.cjs classifySentenceMode"
      to: "data/hsi-thinking-mode-rules.json"
      via: "local read when present and cleared; regex fallback otherwise"
      pattern: "hsi-thinking-mode-rules"
---

<objective>
Ordering note: runs after 355-16 (both edit data/floor-ledger.json) and before 355-26 (both append to 355-JEV-MEASUREMENT.md). The adoption branch of D-45 (HIPS-08), executed only if the navigator signed "adopted" in 355-15: distill Jev's thinking-mode labels from a separate sentence set into a shipped rule table, re-measure it offline on the blind gold against the same pre-fixed bar, and wire it into hsi-spectral only if it clears. On "not_adopted" this plan is a recorded no-op.

Output (adopted path only): distillation set, distiller, recorded responses, rule table, one ledger update, the hsi-spectral read, one test, a record section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-AI-SPEC.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-JEV-MEASUREMENT.md
@scripts/measure-hsi-thinking-mode.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decision gate; if adopted, author the separate distillation set</name>
  <files>tests/fixtures/355-hsi-distillation-sentences.json</files>
  <read_first>
    - tests/fixtures/355-hsi-measurement-record.json (decision, signed_off_by)
    - tests/fixtures/355-hsi-thinking-mode-sentences.items.json and tests/fixtures/355-hsi-tuning-sentences.json (the sets the distillation set must not overlap)
  </read_first>
  <action>Read the record. If decision is not 'adopted', write nothing, state "SKIPPED: navigator decision not_adopted (355-15)" in the SUMMARY for this and the next two tasks, and finish the plan. If adopted: write tests/fixtures/355-hsi-distillation-sentences.json with at least 120 new Claude-written sentences in the same shape as the items file (id prefix d, boundary cases >= 30%, no mode names, no real names), disjoint from the gold items and the tuning set. Commit `test(355-28): separate HSI distillation set (D-45)`.</action>
  <verify>
    <automated>node -e "const r=require('./tests/fixtures/355-hsi-measurement-record.json');if(r.decision!=='adopted'){console.log('SKIPPED not_adopted');process.exit(0)}const d=require('./tests/fixtures/355-hsi-distillation-sentences.json').items;const g=new Set(require('./tests/fixtures/355-hsi-thinking-mode-sentences.items.json').items.map(x=>x.sentence));const t=new Set(require('./tests/fixtures/355-hsi-tuning-sentences.json').items.map(x=>x.sentence));if(d.length<120||d.some(x=>g.has(x.sentence)||t.has(x.sentence)))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 (prints SKIPPED on the not_adopted path)
  </acceptance_criteria>
  <done>Either a recorded skip, or a disjoint distillation set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (adopted only): Distiller, Jev labels on the distillation set, the rule table</name>
  <files>tests/test-355-hsi-distillation.cjs, scripts/distill-hsi-thinking-mode-rules.cjs, tests/fixtures/355-jev-hsi-distillation-responses.json, data/hsi-thinking-mode-rules.json, data/floor-ledger.json</files>
  <read_first>
    - scripts/measure-hsi-thinking-mode.cjs (live / --jev-fixture / --check skeleton, guards)
    - lib/core/hsi-spectral.cjs MODE_PATTERNS and classifySentenceMode (the shape a table must replace)
    - data/floor-ledger.json and scripts/check-floor-ledger.cjs (any distillation threshold becomes a disclosed row)
  </read_first>
  <behavior>
    - distill(rows) over Jev-labeled distillation rows is deterministic: the same input yields a byte-identical table
    - the table holds { version, jev_model, distillation_fixture_sha256, gold_fixture_sha256, built_at, modes: { <mode>: { tokens: [...], priority } }, none_rule, markov_none_policy: 'drop_from_chain' }
    - the distiller's makeSentenceCeiling set is the distillation set only (a gold sentence is refused before fetch)
    - `--check` rebuilds the table from the recorded responses with zero network and byte-compares it
  </behavior>
  <action>Skip with the Task 1 reason if not adopted. Otherwise: RED test first (determinism, disjointness, refusal of a gold sentence, --check byte-compare, table shape); then create scripts/distill-hsi-thinking-mode-rules.cjs (dev-time only; live labels the distillation set once through the hsi_thinking_mode profile + makeSentenceCeiling(distillation set), pinned jev-1.13.0; the distillation procedure keeps a token for a mode when it appears in at least a minimum number of that mode's sentences with a minimum precision; both minimums become `disclosed` rows in data/floor-ledger.json with kind 'rubric'); run live once, write the responses and the table, run --check, the floor sweep and the test. Commit `data(355-28): distilled HSI thinking-mode rule table (D-45)`.</action>
  <verify>
    <automated>node -e "const r=require('./tests/fixtures/355-hsi-measurement-record.json');process.exit(r.decision==='adopted'?1:0)" || (node scripts/distill-hsi-thinking-mode-rules.cjs --check && node tests/test-355-hsi-distillation.cjs && node tests/test-355-floor-sweep.cjs)</automated>
  </verify>
  <acceptance_criteria>
    - On the adopted path: --check, the distillation test and the floor sweep exit 0
    - On the not_adopted path: none of this task's files exist (`ls data/hsi-thinking-mode-rules.json` fails)
  </acceptance_criteria>
  <done>A shipped-as-data table built only from sentences the gold never saw.</done>
</task>

<task type="auto">
  <name>Task 3 (adopted only): Offline re-measure on the gold; wire the table only if it clears the bar</name>
  <files>lib/core/hsi-spectral.cjs, data/hsi-thinking-mode-rules.json, tests/test-355-hsi-distillation.cjs, .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-JEV-MEASUREMENT.md</files>
  <read_first>
    - scripts/measure-hsi-thinking-mode.cjs computeMetrics / applyAdoptionBar
    - lib/core/hsi-spectral.cjs classifySentenceMode, buildTransitionMatrix and their callers
    - scripts/check-cross-connection-honesty.cjs (the doctor point checks this table's provenance)
  </read_first>
  <action>Skip with the Task 1 reason if not adopted. Otherwise classify every gold sentence with the table (offline), compute the same three cuts and apply the same bar against regex_postfix. If it clears: in hsi-spectral.cjs make classifySentenceMode read data/hsi-thinking-mode-rules.json once (local fs, lazy, cached; regex fallback when absent or unparseable), map 'none' sentences out of the Markov transition sequence per markov_none_policy, cite Phase 355 D-45; add test legs proving the runtime path makes zero network calls, reads no key, and falls back to the regex when the file is removed; run tests/272-* HSI tests and amend any pin that the table legitimately moves with a Phase 355 D-45 header note. If it does not clear: delete data/hsi-thinking-mode-rules.json and its ledger rows, leave hsi-spectral.cjs untouched. Either way append `## Distilled rule table (D-45)` to 355-JEV-MEASUREMENT.md with the three cuts, the bar result and the Markov policy. Commit `feat(355-28): distilled thinking-mode table wired only after clearing the bar (HIPS-08, D-45)` or `docs(355-28): distilled table did not clear the bar; not shipped`.</action>
  <verify>
    <automated>node -e "const r=require('./tests/fixtures/355-hsi-measurement-record.json');process.exit(r.decision==='adopted'?1:0)" || (node tests/test-355-hsi-distillation.cjs && node tests/272-hsi-lsa-algorithm.test.cjs && grep -c "## Distilled rule table (D-45)" .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-JEV-MEASUREMENT.md)</automated>
  </verify>
  <acceptance_criteria>
    - On the adopted path: the test and 272 tests exit 0 and the record section exists; `grep -rlE "api\.typesafe\.ai|TYPESAFE_API_KEY|jev-" lib/core/hsi-spectral.cjs` prints nothing
    - On the not_adopted path: `git diff --quiet lib/core/hsi-spectral.cjs` relative to the plan's start
  </acceptance_criteria>
  <done>Adoption, if it happens, ships as data that proved itself offline; otherwise nothing changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> api.typesafe.ai | Claude-written distillation sentences only |
| shipped data -> runtime classifier | A table that changes what users are told about their room's thinking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-131 | Tampering | table tuned on the gold (leakage) | mitigate | Distillation set disjoint from gold and tuning (tested); ceiling refuses gold sentences |
| T-355-132 | Elevation of privilege | runtime Jev call | mitigate | Table read from local data only; zero-network and no-key legs; tripwire in 355-21 |
| T-355-133 | Repudiation | shipping a table that did not clear the bar | mitigate | Same pre-fixed bar applied offline; failing table deleted |
| T-355-134 | Tampering | hidden thresholds in the distiller | mitigate | Every distillation threshold is a disclosed floor-ledger row |
</threat_model>

## Artifacts this phase produces

This plan's share (adopted path only; none on not_adopted):
- Fixture `tests/fixtures/355-hsi-distillation-sentences.json`; responses `tests/fixtures/355-jev-hsi-distillation-responses.json`
- CLI `scripts/distill-hsi-thinking-mode-rules.cjs` (live / `--check`; export `distill`)
- Data `data/hsi-thinking-mode-rules.json` (`version`, `jev_model`, `distillation_fixture_sha256`, `gold_fixture_sha256`, `built_at`, `modes`, `none_rule`, `markov_none_policy`)
- Test `tests/test-355-hsi-distillation.cjs`; record section "## Distilled rule table (D-45)"

<verification>
- Adopted path: distillation --check and test green; not_adopted path: no listed file changed
</verification>

<success_criteria>
- The D-45 adoption branch is executed exactly as locked, or skipped with the navigator's recorded reason
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-28-SUMMARY.md` when done.
</output>
