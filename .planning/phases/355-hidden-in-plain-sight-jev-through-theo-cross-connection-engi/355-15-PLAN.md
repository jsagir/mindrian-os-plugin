---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 15
type: tdd
wave: 3
depends_on: ["355-03", "355-07"]
files_modified:
  - tests/test-355-hsi-measurement-record.cjs
  - scripts/measure-hsi-thinking-mode.cjs
  - tests/fixtures/355-jev-hsi-responses.json
  - tests/fixtures/355-hsi-measurement-record.json
  - lib/core/hsi-spectral.cjs
  - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-JEV-MEASUREMENT.md
autonomous: false
requirements: [HIPS-08]
tags: [jev, dev-time, hsi, thinking-mode, measurement, adoption-bar]

must_haves:
  truths:
    - "SPEC Req 5 / D-44: scripts/measure-hsi-thinking-mode.cjs asks ONE Jev Choice over the five modes plus none per gold sentence, dev-time only, through scripts/jev-devtime-client.cjs with the hsi_thinking_mode profile composed with makeSentenceCeiling; modes: live (default), --jev-fixture <path> (replay) and --check (offline, no key, no network)"
    - "D-32: the script refuses to run unless tests/fixtures/355-hsi-thinking-mode-sentences.json has labeled_at and its fixture_sha256 equals the current items file hash"
    - "AI-SPEC guardrails: pinned model jev-1.13.0 (any other model aborts the run), 8,000 input-token tripwire per call, zero tolerated failures (a failed item fails the run; the denominator never shrinks), 401 aborts without printing the key, pool of 4, timeoutMs 15000, honorRetryAfter"
    - "D-45 (bar fixed before results): adopt only if Jev's full-set accuracy (none counted) minus the regex's is >= 10 points AND no mode's Jev recall is more than 5 points below the regex's, on EACH of 3 repeat runs; otherwise the record says not cleared (and 'inside run-to-run noise' when it clears on some repeats but not all, with the per-item flip count)"
    - "D-45: the record reports full-set accuracy, 5-mode-subset accuracy, per-mode recall, the 6x6 confusion matrix, Jev accuracy at confidence >= 0.9 vs below with counts, and the regex's descriptive share and zero-match fallback count; confidence never filters the primary metric"
    - "D-58: the regex's 'anolog' typo in the integrative pattern is measured as shipped first, then fixed in lib/core/hsi-spectral.cjs, and both regex accuracies are recorded (pre-fix at BASE_355, post-fix = the baseline the bar is applied against); --check recomputes the post-fix regex from the current file, so a later regex change makes the record stale and fails"
    - "SPEC AC11: the fixture set (>= 100 sentences), both accuracies and the navigator's decision are in 355-JEV-MEASUREMENT.md and tests/fixtures/355-hsi-measurement-record.json; the decision is recorded either way; adoption work, if any, is plan 355-28"
    - "D-44: nothing under lib/ or hooks/ references the script, the key or api.typesafe.ai (the hsi-spectral edit is the typo fix only)"
  artifacts:
    - path: "scripts/measure-hsi-thinking-mode.cjs"
      provides: "Dev-time measurement with live / --jev-fixture / --check / --repeats"
      exports: ["computeMetrics", "applyAdoptionBar", "buildBody", "main"]
    - path: "tests/fixtures/355-hsi-measurement-record.json"
      provides: "Machine-readable record the --check replay must reproduce"
    - path: "tests/test-355-hsi-measurement-record.cjs"
      provides: "Offline proof the record replays and was taken blind"
  key_links:
    - from: "scripts/measure-hsi-thinking-mode.cjs"
      to: "scripts/jev-devtime-client.cjs jev()"
      via: "composeGuard(makeEgressGuard(EGRESS_PROFILES.hsi_thinking_mode), makeSentenceCeiling(set))"
      pattern: "hsi_thinking_mode"
    - from: "scripts/measure-hsi-thinking-mode.cjs --check"
      to: "tests/fixtures/355-jev-hsi-responses.json"
      via: "sha256 of canonical {model, state, questions} lookups, repeat index"
      pattern: "355-jev-hsi-responses"
---

<objective>
The phase's first Jev question (SPEC Req 5, HIPS-08; AI-SPEC D14): measure the HSI thinking-mode regex against one Jev Choice on the navigator's blind gold, with the adoption bar fixed before any result (D-45), three repeats, every number replayable offline, and the regex typo handled the way D-58 rules. The navigator signs the mechanically applied decision at the end. If the decision is "adopt", plan 355-28 does the distillation; nothing here ships a runtime Jev call.

Output: the measurement script, recorded responses, the machine record, 355-JEV-MEASUREMENT.md, the typo fix, the record test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-AI-SPEC.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-BASELINE.md
@.claude/skills/spike-findings-MindrianOS-Plugin/references/jev-typed-decisions-api.md
@scripts/jev-question-ceilings.cjs
@scripts/jev-response-schema.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/measure-hsi-thinking-mode.cjs and its offline record test</name>
  <files>tests/test-355-hsi-measurement-record.cjs, scripts/measure-hsi-thinking-mode.cjs</files>
  <read_first>
    - 355-AI-SPEC.md Section 4 item (3) (the measurement loop draft), Section 4b retry/failure table, Section 5 D11 and D14 rows
    - scripts/jev-devtime-client.cjs (loadKey, jev with timeoutMs / honorRetryAfter, pool, EGRESS_PROFILES.hsi_thinking_mode)
    - scripts/jev-question-ceilings.cjs (THINKING_MODE_QUESTIONS, THINKING_MODE_LABELS, makeSentenceCeiling, composeGuard, questionSha256, PINNED_MODEL) and scripts/jev-response-schema.cjs
    - lib/core/hsi-spectral.cjs lines 95-150 and its exports (classifySentenceMode, THINKING_MODES_v1)
    - scripts/build-section-command-ledger.cjs lines 1-40 and 600-700 (the DIVERGENCE header note, --jev-fixture, --check discipline; READ ONLY)
  </read_first>
  <behavior>
    - computeMetrics(rows) over rows {id, gold, regex, jev, confidence} returns { full_accuracy, subset5_accuracy, recall_by_mode: {jev, regex}, confusion: {jev: 6x6, regex: 6x6}, confidence_slices: { ge_0_9: {n, accuracy}, lt_0_9: {n, accuracy} }, regex_descriptive_share, regex_zero_match_fallbacks, n } with accuracies as integers of basis points or fractions (the record is data, never rendered to a user)
    - applyAdoptionBar(perRepeatMetrics, regexMetrics) returns { cleared: bool, per_repeat: [...], reason } using exactly the D-45 rule; a repeat set that clears once and fails once returns cleared false with reason 'not cleared: inside run-to-run noise'
    - the script exits non-zero with a message naming labeled_at when the gold lacks it or its fixture_sha256 differs from the items hash (D-32)
    - `--check` with no record file exits 77 (SKIP) with a loud message; with a record it recomputes every metric from tests/fixtures/355-jev-hsi-responses.json plus the CURRENT classifySentenceMode and exits 0 only if every number equals the record
    - `--check` makes zero network calls, never calls loadKey, and fails if any recorded response model is not jev-1.13.0
    - a live run with a fetchImpl stub returning one non-200 fails the whole run (no shrunken denominator); a response with usage.input_tokens > 8000 aborts; a 401 aborts and the key never appears in output
    - the record test (when the record exists) additionally asserts: >= 100 gold sentences; the gold's commit is an ancestor of the first commit that added tests/fixtures/355-jev-hsi-responses.json (git merge-base --is-ancestor); no THINKING_MODE_QUESTIONS example equals a gold or tuning sentence; the record's question_sha256 equals questionSha256(THINKING_MODE_QUESTIONS); decision is 'adopted' or 'not_adopted' with signed_off_by 'navigator'
  </behavior>
  <action>RED: write tests/test-355-hsi-measurement-record.cjs (hygiene-355 preamble) testing computeMetrics / applyAdoptionBar on hand-built rows, the gold-refusal path, a stubbed live run (injected fetchImpl, fake key string passed explicitly, never from env), and the record legs (exit 77 while the record is absent, printing why); run; fail. GREEN: create scripts/measure-hsi-thinking-mode.cjs ('use strict', header "dev-time only; never required from lib/ or hooks/; Phase 355 HIPS-08, D-44, D-45, D-58", the DIVERGENCE note idiom, hyphens only; argv switch-case: default live, `--repeats <n>` (default 3), `--jev-fixture <path>`, `--check`, `--regex-only` (offline: recompute the regex side and rewrite only the regex fields of the record), `--help`). Live: loadKey() or exit 3 with the sibling's no-key message; for each repeat build one body per gold sentence `{ model: PINNED_MODEL, state: { sentence }, questions: THINKING_MODE_QUESTIONS }`, call jev(body, { guard, key, timeoutMs: 15000, honorRetryAfter: true }) through pool(4), parse with parseJevResponse(res, { mode: THINKING_MODE_LABELS }), retry a parse failure once, abort on 401 / model drift / >8000 input tokens / any failure after retry; store responses keyed by sha256 of canonical JSON {model, state, questions} as arrays indexed by repeat; compute metrics per repeat plus the regex metrics; write tests/fixtures/355-jev-hsi-responses.json and tests/fixtures/355-hsi-measurement-record.json (temp then rename) with { measured_at, jev_model, calls, non_200: 0, input_tokens, cost_usd_estimate (at the documented 0.042 USD per million input tokens, labelled vendor-documented), question_sha256, fixture_sha256, gold_labeled_at, repeats: [metrics], regex_prefix, regex_postfix, bar: applyAdoptionBar(...), decision: 'pending_signoff' }. Run the record test green except its decision leg. Commit `feat(355-15): HSI thinking-mode measurement with offline replay (HIPS-08, D-45)`.</action>
  <verify>
    <automated>node tests/test-355-hsi-measurement-record.cjs; test $? -eq 0 -o $? -eq 77</automated>
  </verify>
  <acceptance_criteria>
    - The record test exits 0 or 77 (77 only because the record does not exist yet), with every computeMetrics / applyAdoptionBar / refusal / stub leg PASS
    - `grep -rlE "measure-hsi-thinking-mode" lib hooks` prints nothing
    - `grep -vE '^\s*(//|\*|/\*)' scripts/measure-hsi-thinking-mode.cjs | grep -c "jev-latest"` prints 0
  </acceptance_criteria>
  <done>A measurement that can only ask the frozen question about fixture sentences, and that replays offline to the same numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Three live repeats on the shipped regex, then the D-58 typo fix and the post-fix regex; write 355-JEV-MEASUREMENT.md</name>
  <files>tests/fixtures/355-jev-hsi-responses.json, tests/fixtures/355-hsi-measurement-record.json, lib/core/hsi-spectral.cjs, .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-JEV-MEASUREMENT.md</files>
  <read_first>
    - lib/core/hsi-spectral.cjs line with the integrative MODE_PATTERNS (`anolog`)
    - `grep -rn "classifySentenceMode\|MODE_PATTERNS\|THINKING_MODES_v1" tests lib/memory | head` (any parity test pinning the Python regex that the fix would move)
    - scripts/compute-hsi.py (READ ONLY; its copy of the pattern stays as-is, D-05)
  </read_first>
  <action>Precondition: `test -f tests/fixtures/355-hsi-thinking-mode-sentences.json` and its labeled_at present (from 355-03). Run `node scripts/measure-hsi-thinking-mode.cjs --repeats 3` once (navigator's machine, key from the env or ~/.secrets/typesafe.env; never printed). If it exits 3 (no key) or aborts, record the reason in the SUMMARY and stop this task as BLOCKED; never fabricate a record. On success the record holds regex_prefix = the regex as shipped at BASE_355. Then fix the typo in lib/core/hsi-spectral.cjs (`anolog` -> `analog` in the integrative pattern only) with a comment "Phase 355 D-58: typo fix; pre-fix accuracy recorded in 355-JEV-MEASUREMENT.md; scripts/compute-hsi.py keeps the original pattern as reference", run any parity test found in read_first and, if one pins the Python pattern's output, amend it with a Phase 355 D-58 header note stating CJS and the Python reference now diverge on the word 'analog' by design. Run `node scripts/measure-hsi-thinking-mode.cjs --regex-only` to write regex_postfix and re-apply the bar against regex_postfix (the baseline users get after this phase). Run `--check` (must exit 0). Write 355-JEV-MEASUREMENT.md (Feynman prose, hyphens only, no claim that anything "works"): date, model id per repeat, calls, non-200 count, input tokens, cost estimate, question sha256, fixture sha256, gold labeled_at, a table per repeat of the three accuracy cuts for Jev and for both regexes, per-mode recall, the confusion matrices, the confidence slices, the regex descriptive share and zero-match fallbacks, the typo's effect (pre-fix minus post-fix), the bar result per repeat and the flip count, and a `## Decision` section reading "pending navigator sign-off". Commit `data(355-15): HSI thinking-mode Jev vs regex, 3 repeats; regex typo fixed (HIPS-08, D-45, D-58)`.</action>
  <verify>
    <automated>node scripts/measure-hsi-thinking-mode.cjs --check && node -e "const r=require('./tests/fixtures/355-hsi-measurement-record.json');if(r.jev_model!=='jev-1.13.0'||r.non_200!==0||r.repeats.length!==3||!r.regex_prefix||!r.regex_postfix)process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Both verify commands exit 0
    - `grep -c "anolog" lib/core/hsi-spectral.cjs` prints 0 in non-comment code and `git diff -U0 lib/core/hsi-spectral.cjs | grep '^[-+]' | grep -v '^[-+][-+]' | wc -l` is small (the pattern line plus the comment)
    - 355-JEV-MEASUREMENT.md contains the headings `## Regex vs Jev`, `## Adoption bar`, `## Regex typo (D-58)` and `## Decision`
    - `grep -ciE "\b(works|validated|proven)\b" .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-JEV-MEASUREMENT.md` prints 0
  </acceptance_criteria>
  <done>The phase has its first measured Jev-vs-code comparison, replayable offline.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: The navigator signs the mechanically applied adoption decision</name>
  <files>tests/fixtures/355-hsi-measurement-record.json, .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-JEV-MEASUREMENT.md</files>
  <read_first>
    - 355-JEV-MEASUREMENT.md sections Adoption bar and Decision
  </read_first>
  <action>Present the bar result exactly as computed. If the bar did not clear on all three repeats, the only valid option is not_adopted (the bar is never relaxed after results, D-45). On the navigator's answer, set the record's decision ('adopted' | 'not_adopted'), signed_off_by 'navigator', signed_off_at, rewrite the Decision section (and, for not_adopted, one line on what a future attempt would need), re-run the record test green, and commit `docs(355-15): navigator decision on HSI thinking-mode adoption`.</action>
  <decision>Adopt Jev's thinking-mode labels (via a distilled rule table, plan 355-28) or keep the regex</decision>
  <context>D-45 fixed the bar before any result: >= 10 points on the full set with none counted, no mode dropping more than 5, on every one of 3 repeats. Adoption never means a runtime Jev call; it means a data/ rule table distilled from a separate set that must clear the same bar offline.</context>
  <options>
    <option id="not_adopted">
      <name>Keep the regex (the only option if the bar did not clear on all repeats)</name>
      <pros>No new runtime data; the record stands as the first calibration point</pros>
      <cons>The regex's known bias (descriptive fallback, no none) stays, disclosed</cons>
    </option>
    <option id="adopted">
      <name>Adopt: run plan 355-28 (distillation set, rule table, offline re-measure)</name>
      <pros>Better thinking-mode labels shipped as data, zero runtime vendor calls</pros>
      <cons>More dev-time work; the table must clear the bar again on its own</cons>
    </option>
  </options>
  <verify><automated>node tests/test-355-hsi-measurement-record.cjs</automated></verify>
  <acceptance_criteria>
    - `node tests/test-355-hsi-measurement-record.cjs` exits 0 with the decision leg PASS
    - If `bar.cleared` is false in the record, decision is 'not_adopted'
  </acceptance_criteria>
  <resume-signal>Select: not_adopted or adopted</resume-signal>
  <done>The decision is recorded either way with the navigator's sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> api.typesafe.ai | Claude-written fixture sentences cross, nothing else |
| vendor key in the navigator's shell -> script | Secret handling |
| measurement record -> adoption decision | A number that could be gamed after the fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-68 | Information disclosure | a non-fixture sentence sent to Jev | mitigate | Profile guard + makeSentenceCeiling membership check before fetch; refuse-don't-strip |
| T-355-69 | Information disclosure | key printed on 401 or in logs | mitigate | loadKey never logged; 401 aborts with a key-free message (tested) |
| T-355-70 | Repudiation | accuracy on a shrunken denominator | mitigate | Any failure after one retry fails the run; non_200 must be 0 in the record |
| T-355-71 | Tampering | bar relaxed after results | mitigate | Bar code written and committed before the live run; applyAdoptionBar is pure and tested; not_adopted forced when the bar fails |
| T-355-72 | Repudiation | model drift inside a run | mitigate | Pinned jev-1.13.0 in body and profile; parse rejects any other model; --check verifies every stored model |
| T-355-73 | Tampering | gold labeled after seeing machine answers | mitigate | Refuses without labeled_at; git-ancestry leg asserts gold precedes the first response file |
| T-355-74 | Elevation of privilege | Jev reachable from runtime | mitigate | Only the typo line changes under lib/; tripwire grep in 355-21 |
</threat_model>

## Artifacts this phase produces

This plan's share:
- CLI `scripts/measure-hsi-thinking-mode.cjs` (default live, `--repeats`, `--jev-fixture`, `--check`, `--regex-only`, `--help`; exports `computeMetrics`, `applyAdoptionBar`, `buildBody`, `main`)
- Fixtures `tests/fixtures/355-jev-hsi-responses.json` (sha256-keyed, arrays per repeat), `tests/fixtures/355-hsi-measurement-record.json` (`measured_at`, `jev_model`, `calls`, `non_200`, `input_tokens`, `cost_usd_estimate`, `question_sha256`, `fixture_sha256`, `gold_labeled_at`, `repeats`, `regex_prefix`, `regex_postfix`, `bar`, `decision`, `signed_off_by`, `signed_off_at`)
- Record `.planning/phases/355-.../355-JEV-MEASUREMENT.md`
- Typo fix `anolog` -> `analog` in `lib/core/hsi-spectral.cjs` MODE_PATTERNS.integrative
- Test `tests/test-355-hsi-measurement-record.cjs`

<verification>
- `node scripts/measure-hsi-thinking-mode.cjs --check` and `node tests/test-355-hsi-measurement-record.cjs` exit 0
</verification>

<success_criteria>
- Regex and Jev accuracies (three cuts, three repeats) recorded, replayable, decision signed
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-15-SUMMARY.md` when done.
</output>
