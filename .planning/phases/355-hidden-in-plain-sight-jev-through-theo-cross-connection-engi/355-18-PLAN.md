---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 18
type: execute
wave: 4
depends_on: ["355-04", "355-06", "355-10"]
files_modified:
  - tests/test-355-producer-eureka.cjs
  - tests/fixtures/355/producers/eureka-report.json
  - scripts/eureka-portfolio-report.cjs
  - lib/core/eureka/report-html.cjs
  - lib/core/eureka/qualify-opportunity.cjs
  - commands/eureka.md
  - skills/eureka/SKILL.md
autonomous: true
requirements: [HIPS-04, HIPS-05]
tags: [stamp-wiring, eureka, report, f1, no-decimal]

must_haves:
  truths:
    - "D-08 / D-56: eureka's stamps are computed in eureka-portfolio-report.cjs main() after the ranked list is final and BEFORE bankStatements opens its transaction; every ranked pair carries `stamp` in portfolio-report.json (the filing wiring that consumes them is 355-20)"
    - "D-48: a eureka pair's endpoints are resolved locally and exactly: the entity's source artifact frontmatter framework:, then methodology: via the registry, then the entity title; the opaque a_handle / b_handle never leave the machine (D-10); direction is the pair's scoreMeasured direction (post 355-10), or 'none' in reasoning mode"
    - "D-29: the Score column and every rendered decimal leave commands/eureka.md, skills/eureka/SKILL.md, portfolio-report.md and report-html.cjs's ranked table (the composite score may stay in the JSON as data, never rendered); reasoning mode's lsa_similarity column is removed; qualify-opportunity.cjs componentValue renders a number as the word 'measured' (value withheld) and the F.1 WHY lines lead with the stamp block, so the rendered F.1 zones.body carries no decimal"
    - "D-12 (AI-SPEC D12): no rendered eureka template calls a finding a 'breakthrough', 'convergent', 'validated' or 'proven'; the internal band enum 'breakthrough' is never rendered"
    - "D-30: captured portfolio-report.md, the HTML ranked table text and the F.1 zones.body contain no D-30 decimal and no percentage on stamp or evidence lines, Theo up (replay) and Theo down (null); 100% of ranked rows carry a stamp block"
  artifacts:
    - path: "tests/test-355-producer-eureka.cjs"
      provides: "Coverage, no-decimal and banned-claim proofs for eureka's three renders"
      min_lines: 100
    - path: "scripts/eureka-portfolio-report.cjs"
      provides: "stampRankedPairs(ranked, ctx, deps) and stamp-bearing md render"
      exports: ["stampRankedPairs", "eurekaEndpoints"]
  key_links:
    - from: "scripts/eureka-portfolio-report.cjs main()"
      to: "lib/core/verification-stamp.cjs stampFindings"
      via: "await before the bankStatements call"
      pattern: "stampRankedPairs"
    - from: "lib/core/eureka/qualify-opportunity.cjs formatComponentLines"
      to: "lib/core/verification-stamp-format.cjs formatStampLines"
      via: "stamp block as the first F.1 WHY lines"
      pattern: "formatStampLines"
---

<objective>
Wire the stamp into eureka's three renders (HIPS-04, HIPS-05): the portfolio report (Markdown and HTML), the `/mos:eureka` command's table rules, and the F.1 qualify card. Replace every rendered number with the stamp's evidence (D-27, D-29), keep the internal composite as data, and compute stamps before the banking transaction so 355-20 can file them (D-56).

Output: stamping step + render changes in four source files, two doc updates, a recorded report fixture, one test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@lib/core/verification-stamp.cjs
@lib/core/verification-stamp-format.cjs
@lib/core/floor-disclosure.cjs
@tests/helpers/theo-replay-355.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stamp the ranked pairs before banking; report Markdown and HTML render stamps, not scores</name>
  <files>tests/test-355-producer-eureka.cjs, tests/fixtures/355/producers/eureka-report.json, scripts/eureka-portfolio-report.cjs, lib/core/eureka/report-html.cjs</files>
  <read_first>
    - scripts/eureka-portfolio-report.cjs lines 800-1300 (main(argv): run modes, how `ranked` is built, where portfolio-report.json / .md are written, the bankStatements call near :1258) and the Markdown writer (grep `toFixed\|score\|lsa_similarity` in the file)
    - lib/core/eureka/report-html.cjs lines 50-70 (fmtNum) and 195-300 (rankedTable, reasoning branch)
    - how an entity node records its source artifact (grep `EXTRACTED_FROM\|source_path` in lib/core/eureka/*.cjs and scripts/eureka-portfolio-report.cjs)
    - tests/test-215-score.cjs and any tests/test-226-*.cjs that pin the report render (must stay green or be amended with a Phase 355 D-29 citation where they pin a rendered number)
  </read_first>
  <action>Author tests/fixtures/355/producers/eureka-report.json: a recorded report object (embedded mode and one reasoning-mode variant) with >= 6 ranked pairs whose titles include stub-fixture canon names for some ends and non-canon titles for others, each with rs { direction, abs_diff, passes } and a composite score. Write tests/test-355-producer-eureka.cjs (hygiene-355 preamble; labels prefixed `eureka `): with replay and null callTools, stampRankedPairs attaches a Stamp-parseable `stamp` to every ranked row (rows == stamps), the Markdown render of the fixture shows a stamp block per row and disclosureLine('eureka') once, the HTML ranked-table text (tags stripped) shows the tier word and path text per row, neither contains a D-30 decimal nor a percentage on stamp/evidence lines, neither contains the words breakthrough / convergent / validated / proven, reasoning mode renders no lsa_similarity cell, a planted 0.87 title is caught (negative control). Then in scripts/eureka-portfolio-report.cjs add `eurekaEndpoints(pair, ctx)` (D-48 resolution through the entity's source artifact frontmatter, then the title; never the handle) and `async stampRankedPairs(ranked, ctx, deps)` (direction = pair.rs.direction or 'none' in reasoning mode; stampFindings with per-run memo); call it in main() after `ranked` is final and BEFORE the bankStatements call, attach `stamp` to each ranked row in portfolio-report.json, and change the Markdown writer to print per row: rank, A title, B title, weak dimensions, tail glyph, mode, then the stamp block; remove the Score column and every decimal print (the composite stays in the JSON). In report-html.cjs replace the score cell with the stamp's tier word plus formatPathText (or the unverified reason words), drop fmtNum from rendered cells, remove any reasoning-mode similarity number cell. Amend any pinned render test with a Phase 355 D-29 header note. Commit `feat(355-18): eureka pairs stamped before banking; report renders stamps, not scores (HIPS-04, HIPS-05, D-29, D-56)`.</action>
  <verify>
    <automated>! (node tests/test-355-producer-eureka.cjs 2>&1 | grep -qE "^FAIL: eureka (report|html|stamp)") && node tests/test-215-score.cjs && node tests/test-355-eureka-ranking-pin.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Every `eureka report`, `eureka html` and `eureka stamp` check passes; tests/test-215-score.cjs and the ranking pin stay green
    - In scripts/eureka-portfolio-report.cjs, `grep -n "stampRankedPairs(\|bankStatements(db" scripts/eureka-portfolio-report.cjs` shows the stampRankedPairs call on a lower line number than the bankStatements call inside main()
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/eureka/report-html.cjs | grep -c "fmtNum(r.score"` prints 0
  </acceptance_criteria>
  <done>Every ranked eureka pair carries its stamp in the report and nowhere shows a number.</done>
</task>

<task type="auto">
  <name>Task 2: F.1 qualify card and the eureka docs render stamps, never numbers</name>
  <files>lib/core/eureka/qualify-opportunity.cjs, commands/eureka.md, skills/eureka/SKILL.md, tests/test-355-producer-eureka.cjs</files>
  <read_first>
    - lib/core/eureka/qualify-opportunity.cjs lines 340-420 (componentValue, formatComponentLines) and 480-520 (the WHY lines above verb rows, renderShapeF1 / appendAskUserQuestionTrailer usage)
    - commands/eureka.md lines 220-300 (ranked table rules, the Score column, reasoning-mode `lsa_similarity 0.11` example) and skills/eureka/SKILL.md (mirror)
    - skills/ui-system/SKILL.md F.1 section (zones.body, WHY lines)
    - existing qualify tests (`ls tests | grep -i qualify`)
  </read_first>
  <action>qualify-opportunity.cjs: componentValue renders a finite number as the word 'measured' (strings and booleans unchanged, 'unknown' unchanged), cite Phase 355 D-29 / Canon Part 12; formatComponentLines accepts an optional `stamp` argument and, when present, prepends formatStampLines(stamp, 'cli') as the first WHY lines (the path replaces the number, D-27); the F.1 render path passes the candidate's stored stamp (the stamp props on the opportunity node re-parsed through fromNodeProps, when present). Add test legs: the rendered F.1 zones.body for a fixture candidate with a stamp contains the stamp block and no D-30 decimal; without a stamp it contains no decimal either. commands/eureka.md and skills/eureka/SKILL.md: replace both table examples: embedded columns become Rank, A, B, Stamp, Weak dims, Tail, Mode with the stamp block rendered under each row; reasoning mode shows no similarity number (remove the lsa_similarity column and the `0.11` example); add the rule "never render a score, similarity, differential or percentage; reproduce the stamp block verbatim"; add the D-50 Desktop / Cowork rule (narrate only stamps a CLI run stored; otherwise say exactly "Not yet checked; run the CLI to verify."). Run the producer test, the existing qualify tests, check-render-coverage --check and the registry / shape / help gates. Commit `feat(355-18): F.1 card and eureka docs carry the stamp, not numbers (HIPS-05, D-27, D-29, D-50)`.</action>
  <verify>
    <automated>node tests/test-355-producer-eureka.cjs && node scripts/check-render-coverage.cjs --check && node scripts/build-command-registry.cjs --check && node scripts/check-shape-declaration.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-355-producer-eureka.cjs` exits 0 with `FAIL: 0`; every existing qualify test (`for f in tests/*qualify*.cjs; do node "$f" || exit 1; done`) exits 0
    - `grep -cE "\| *Score *\||0\.11|lsa_similarity" commands/eureka.md skills/eureka/SKILL.md` prints 0 for each file
    - `grep -c "Not yet checked; run the CLI to verify." commands/eureka.md skills/eureka/SKILL.md` shows >= 1 in each
  </acceptance_criteria>
  <done>The eureka card and docs show the evidence path and never a number.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eureka entity handles (room.db ids) -> stamp endpoints | Opaque ids must never cross to Theo |
| report / card -> user | The rendered verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-85 | Information disclosure | a_handle / b_handle sent to Theo | mitigate | eurekaEndpoints resolves only snapshot names from frontmatter or titles; handles are never passed to stampFindings (test asserts captured callTool args are snapshot names) |
| T-355-86 | Spoofing | a composite score read as a verdict | mitigate | Score column removed from every render; numbers render as 'measured'; D-30 legs with negative control |
| T-355-87 | Spoofing | 'breakthrough' language on a one-signal finding | mitigate | Banned-claim leg over templates and renders (AI-SPEC D12) |
| T-355-88 | Denial of service | Theo awaited inside the banking transaction | mitigate | stampRankedPairs runs before bankStatements (line-order acceptance) |
</threat_model>

## Artifacts this phase produces

This plan's share:
- `scripts/eureka-portfolio-report.cjs`: exports `stampRankedPairs`, `eurekaEndpoints`; `portfolio-report.json` ranked rows gain `stamp`
- `lib/core/eureka/qualify-opportunity.cjs`: `formatComponentLines(components, harvestIndex, stamp)`; numbers render as 'measured'
- `lib/core/eureka/report-html.cjs`: ranked-table stamp cell
- Fixture `tests/fixtures/355/producers/eureka-report.json`; test `tests/test-355-producer-eureka.cjs`

<verification>
- Producer test, 215, ranking pin, qualify tests, render-coverage and registry gates green
</verification>

<success_criteria>
- Every eureka surface shows stamps and no numbers; stamps exist before banking
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-18-SUMMARY.md` when done.
</output>
