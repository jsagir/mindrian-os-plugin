---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 14
type: tdd
wave: 3
depends_on: ["355-02", "355-03", "355-06", "355-07", "355-08"]
files_modified:
  - tests/test-355-theo-capture.cjs
  - scripts/capture-355-theo-responses.cjs
  - tests/fixtures/355-theo-find-connections-responses.json
  - tests/fixtures/355-citation-pairs.items.json
  - tests/fixtures/355-citation-pairs.json
  - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/labeling-session-citations.json
autonomous: false
requirements: [HIPS-04, HIPS-09]
tags: [theo, capture, replay, citation-pairs, gold, blind, checkpoint]

must_haves:
  truths:
    - "D-09 / THEO-04: scripts/capture-355-theo-responses.cjs reaches Theo only through lib/core/brain-client.cjs callTool('find_connections', { from, to }); it refuses, before any call, any pair whose two names are not exact members of data/framework-names.json (Part 8)"
    - "D-11 / AI-SPEC Eval Tooling: one live capture records Theo's answers (path, pathLabels, edges, hops, refusals, coverage) keyed from + '\\u0000' + to, with per-call latency, so every later test replays offline; internal Theo record ids are replaced by synthetic '<label>-captured-<n>' ids before the file is written (Brain IP)"
    - "D-11: the first successful parsed answer is recorded as the find_connections-is-served precondition; if Theo is unreachable the capture writes nothing and the plan stops with the honest outage recorded"
    - "D-35 step (3) / D-46: >= 40 citation pairs are templated from canon names only (claim = renderClaim(from, to, direction); path = hopsFromTheoPath of the captured Theo path) across strata (1-2 hops direct lateral, 1-2 hops through a broad hub, 3 hops, contradicting or synthetic-contradicting, same path under both direction claims) with >= 8 per stratum where Theo's canon allows, plus a separate no-path list that must cost zero Jev calls"
    - "D-32 / D-33: the navigator labels the citation pairs blind (supports / says nothing / contradicts) through scripts/label-355-gold.cjs before any Jev citation response exists; the gold carries labeled_at and fixture_sha256"
  artifacts:
    - path: "scripts/capture-355-theo-responses.cjs"
      provides: "--live capture through brain-client; --check offline validation"
      exports: ["capturePairs", "sanitizeResponse", "validateCapture"]
    - path: "tests/fixtures/355-theo-find-connections-responses.json"
      provides: "Recorded, sanitized Theo find_connections answers with latency"
    - path: "tests/fixtures/355-citation-pairs.items.json"
      provides: ">= 40 templated, Part-8-clean {claim, path} items, no labels"
    - path: "tests/fixtures/355-citation-pairs.json"
      provides: "Navigator's blind citation gold"
  key_links:
    - from: "scripts/capture-355-theo-responses.cjs"
      to: "lib/core/brain-client.cjs callTool"
      via: "lazy require in the --live branch only"
      pattern: "callTool\\('find_connections'"
    - from: "tests/fixtures/355-citation-pairs.items.json"
      to: "scripts/jev-question-ceilings.cjs renderClaim / hopsFromTheoPath"
      via: "items generated by those two functions from captured paths"
      pattern: "renderClaim"
---

<objective>
Two dev-time inputs the rest of the phase replays offline (HIPS-04, HIPS-09): a recorded, sanitized set of live Theo `find_connections` answers, and the >= 40 templated citation pairs the dev-time citation-check calibration (D-46, AI-SPEC D15) is scored against, labeled blind by the navigator. D-35 step (3): authored after the phrase ruling; step (4): judged before any Jev citation answer exists.

Output: `scripts/capture-355-theo-responses.cjs` + test, the capture file, the citation items and the navigator's gold.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-AI-SPEC.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PHRASE-CONFIRMATION.md
@lib/core/verification-stamp.cjs
@scripts/jev-question-ceilings.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/capture-355-theo-responses.cjs with offline --check (test first)</name>
  <files>tests/test-355-theo-capture.cjs, scripts/capture-355-theo-responses.cjs</files>
  <read_first>
    - lib/core/brain-client.cjs callTool (re-grep from 355-BASELINE.md; D-57 file, READ ONLY)
    - lib/core/verification-stamp.cjs (loadFrameworkNames, choosePath) and tests/helpers/theo-replay-355.cjs (the replay key format the capture must match)
    - 355-RESEARCH.md "Live Theo Contract (probe record)" (shapes and latencies)
    - scripts/build-section-command-ledger.cjs lines 600-700 (dev-time --check discipline, READ ONLY)
  </read_first>
  <behavior>
    - capturePairs(pairs, { callTool, names, now }) calls callTool('find_connections', { from, to }) exactly once per distinct pair, only when both names are in `names`; a non-member pair is refused with reason 'not_canon_name' and never called
    - sanitizeResponse(res) replaces every non-Framework path entry that is not a canon name with '<lowercased label>-captured-<n>' consistently within the file and keeps pathLabels, edges, hops, coverage and refusals unchanged; a null / error / text response is recorded as { "$null": true } / { "$error": "<code>" } / { "$text": true } (never the text body)
    - the written file is { captured_at, theo_host (hostname only), snapshot_sha256 (from data/framework-names.json source_sha256), served: true|false, calls: n, latency_ms: [..], responses: { "<from>\u0000<to>": ... } }, written temp-then-rename, and its responses replay through makeReplayCallTool to the same stamps as the raw responses
    - validateCapture(file) fails on: a key whose two names are not snapshot members, any string matching /brainrecord-export-\d+/ or another raw internal id pattern, a missing captured_at / snapshot_sha256, a path whose hops disagree with edges.length
    - `--check` exits 0 on a valid committed file, exits 77 (SKIP) when the file does not exist yet, and never loads brain-client
  </behavior>
  <action>RED: write tests/test-355-theo-capture.cjs (hygiene-355 preamble; injected callTool stubs from tests/helpers/theo-replay-355.cjs; no network) per the behavior list; run; fail. GREEN: create scripts/capture-355-theo-responses.cjs ('use strict', BSL header, "dev-time only; Theo via brain-client callTool only (THEO-04); canon names only (Part 8); internal ids replaced before write (Brain IP)", hyphens only; argv switch-case `--live --pairs <json> [--out <path>] [--append]`, `--check [--file <path>]`, `--help`). Default out: tests/fixtures/355-theo-find-connections-responses.json; `--append` merges new keys into an existing file without touching existing ones (355-24 appends room pairs). Out path must resolve (realpath + path.sep) under <repo>/tests/fixtures/. Run green. Commit `feat(355-14): Theo find_connections capture with offline --check (HIPS-04)`.</action>
  <verify>
    <automated>node tests/test-355-theo-capture.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-355-theo-capture.cjs` exits 0 with `FAIL: 0`
    - `grep -vE '^\s*(//|\*|/\*)' scripts/capture-355-theo-responses.cjs | grep -cE "fetch\(|https?://"` prints 0 (the only wire is brain-client)
    - `node scripts/capture-355-theo-responses.cjs --check; test $? -eq 77` succeeds before Task 2 writes the file
  </acceptance_criteria>
  <done>A capture tool that can only send canon names and can only write sanitized answers.</done>
</task>

<task type="auto">
  <name>Task 2: One live capture and the >= 40 templated citation items (D-46)</name>
  <files>tests/fixtures/355-theo-find-connections-responses.json, tests/fixtures/355-citation-pairs.items.json</files>
  <read_first>
    - data/framework-names.json (candidate endpoints)
    - scripts/jev-question-ceilings.cjs renderClaim and hopsFromTheoPath
    - lib/core/verification-stamp.cjs choosePath, LATERAL_EDGE_TYPES, ANCHOR_EDGE_TYPES
    - 355-AI-SPEC.md Section 5 "Reference Dataset" row (iii) (strata)
  </read_first>
  <action>Build a candidate list of 80-120 canon-name pairs (mix of methodologically related and distant frameworks, chosen from the snapshot by name only), write it to the session scratch directory, and run `node scripts/capture-355-theo-responses.cjs --live --pairs <scratch file>` once. If `served` is false or every response is `$null`, stop: record the outage time and reason in the SUMMARY, write nothing, and report the task BLOCKED (the stamps stay honestly unavailable; judging cannot proceed). Otherwise run `--check`. Then generate tests/fixtures/355-citation-pairs.items.json from the captured answers with a one-off node script that requires jev-question-ceilings and verification-stamp: `{ "_note": "templated from canon names and captured Theo paths only; no labels", "phrase_hash": "<phraseHash()>", "capture_sha256": "<sha256 of the capture file>", "items": [ { "pair_id", "from", "to", "direction", "claim": renderClaim(...), "path": hopsFromTheoPath(chosen path), "stratum": "direct_lateral|hub|three_hop|contradicting|both_directions", "synthetic": false } ], "no_path": [ { "pair_id", "from", "to", "direction" } ] }` with at least 40 items and at least 8 per stratum where the canon provides them; for the contradicting stratum, if Theo's edge vocabulary has no opposite-relation type on any captured path, build the items from a CONTRASTS_WITH-bearing path when one exists, else mark them `synthetic: true` and record why; add at least 5 no_path entries. Record in the SUMMARY: calls, served, p50 / p95 latency, strata counts, synthetic count. Commit `data(355-14): live Theo capture + 40+ templated citation pairs (HIPS-04, HIPS-09, D-46)`.</action>
  <verify>
    <automated>node scripts/capture-355-theo-responses.cjs --check && node -e "const f=require('./tests/fixtures/355-citation-pairs.items.json');if(f.items.length<40||f.no_path.length<5||f.items.some(i=>typeof i.claim!=='string'||!Array.isArray(i.path)||'gold' in i))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Both verify commands exit 0
    - `grep -cE "brainrecord-export-[0-9]+" tests/fixtures/355-theo-find-connections-responses.json tests/fixtures/355-citation-pairs.items.json` prints 0 for each file
    - `git ls-files tests/fixtures | grep -c "355-jev-citation"` prints 0 (no Jev citation answer exists yet)
  </acceptance_criteria>
  <done>Offline replays have real Theo answers, and the citation calibration has a Part-8-clean item set.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: The navigator labels the citation pairs blind; the executor commits the gold</name>
  <files>tests/fixtures/355-citation-pairs.json, .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/labeling-session-citations.json</files>
  <read_first>
    - scripts/label-355-gold.cjs usage (`--help`)
  </read_first>
  <action>Ask the navigator to label in a real terminal. On resume, verify the gold (hash, labeled_at, one verdict per item) and commit it with the session file as `docs(355-14): navigator blind citation gold` (git add -f for the phase-dir session), confirming no tests/fixtures/355-jev-citation-*.json exists at that commit.</action>
  <what-built>tests/fixtures/355-citation-pairs.items.json (>= 40 claim + path items) and the labeling CLI</what-built>
  <how-to-verify>
    1. Run `node scripts/label-355-gold.cjs start --set citations` (resume with `resume --set citations`).
    2. For each item read the claim and the numbered hops; press `s` if the hops show the claimed relation, `n` if they say nothing about it, `c` if a hop states the opposite; `u` undoes, `q` saves.
    3. When `status --set citations` shows all labeled, run `node scripts/label-355-gold.cjs emit --set citations`.
    4. Reply "labeled".
  </how-to-verify>
  <verify><automated>node -e "const g=require('./tests/fixtures/355-citation-pairs.json');const i=require('./tests/fixtures/355-citation-pairs.items.json');const c=require('node:crypto');const h=c.createHash('sha256').update(require('node:fs').readFileSync('tests/fixtures/355-citation-pairs.items.json')).digest('hex');if(!g.labeled_at||g.fixture_sha256!==h||g.items.length!==i.items.length||g.items.some(x=>!['supports','says_nothing','contradicts'].includes(x.gold)))process.exit(1)"</automated></verify>
  <acceptance_criteria>
    - The verify command exits 0
    - The gold commit precedes any tests/fixtures/355-jev-citation-responses.json in git history
  </acceptance_criteria>
  <resume-signal>Type "labeled" once emit has written tests/fixtures/355-citation-pairs.json</resume-signal>
  <done>The citation calibration has blind human gold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> Theo (brain-client) | Live read with canon names |
| Theo response -> committed fixture | Brain internals could be committed |
| navigator keystrokes -> citation gold | Human truth for calibration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-63 | Information disclosure | non-canon strings sent to Theo | mitigate | Exact snapshot membership check before any call; refused pairs never called (tested) |
| T-355-64 | Information disclosure (Brain IP) | internal record ids committed | mitigate | sanitizeResponse replaces them; validateCapture and acceptance grep reject raw ids |
| T-355-65 | Repudiation | outage recorded as "no path" | mitigate | Outage writes nothing and blocks the task; served flag recorded |
| T-355-66 | Tampering | gold labeled after seeing Jev | mitigate | Gold committed before any 355-jev-citation file; git-order check in 355-26's record test |
| T-355-67 | Elevation of privilege | --out path escape | mitigate | realpath + separator prefix under tests/fixtures/ |
</threat_model>

## Artifacts this phase produces

This plan's share:
- CLI `scripts/capture-355-theo-responses.cjs` (`--live`, `--pairs`, `--out`, `--append`, `--check`, `--file`; exports `capturePairs`, `sanitizeResponse`, `validateCapture`)
- Fixture `tests/fixtures/355-theo-find-connections-responses.json` (`captured_at`, `theo_host`, `snapshot_sha256`, `served`, `calls`, `latency_ms`, `responses`)
- Fixtures `tests/fixtures/355-citation-pairs.items.json` (`phrase_hash`, `capture_sha256`, `items[{pair_id, from, to, direction, claim, path, stratum, synthetic}]`, `no_path[]`) and gold `tests/fixtures/355-citation-pairs.json`
- Test `tests/test-355-theo-capture.cjs`

<verification>
- `node tests/test-355-theo-capture.cjs` and `node scripts/capture-355-theo-responses.cjs --check` exit 0; gold verify exits 0
</verification>

<success_criteria>
- Real Theo answers recorded once, sanitized; >= 40 citation items labeled blind before any Jev citation run
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-14-SUMMARY.md` when done.
</output>
