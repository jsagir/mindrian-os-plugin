---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 02
subsystem: testing
tags: [direction-convention, pws-author-checkpoint, red-test, d-35, cross-producer-agreement]

requires:
  - phase: 355-01
    provides: "lib/core/direction-convention.cjs (classify, classifyDiff, DIRECTIONS, DIRECTION_MEANING, NONE, NONE_MEANING, phraseHash), tests/helpers/hygiene-355.cjs, tests/run-all-355.sh skeleton"
provides:
  - "PWS author's ruling on the three direction phrases (D-35 step 2, AI-SPEC D17), recorded in 355-PHRASE-CONFIRMATION.md and machine-pinned via PHRASES_CONFIRMED.phrase_hash"
  - "tests/fixtures/355/direction-pairs.json: 35 (lsa, semantic) pairs with module-computed expected labels, phrase_hash-guarded"
  - "tests/test-355-direction-agreement.cjs: the RED cross-producer agreement test (legs A-H) naming exactly which later plan turns each leg green"
affects: [355-09, 355-10, 355-11, 355-12, 355-13, 355-27]

tech-stack:
  added: []
  patterns:
    - "PHRASES_CONFIRMED = { by: role-only, at: ISO date, phrase_hash } frozen export, pinned by an equality test against phraseHash() so a later unratified phrase edit fails loudly (D-35)"
    - "Fixture expected labels generated by a one-off node script that requires the module under test, never hand-typed (T-355-09 mitigation)"
    - "Leg-lettered RED test: each check name carries its leg letter (A-H) and, for RED legs, the exact plan number that turns it green, so a FAIL: line is self-documenting"

key-files:
  created:
    - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PHRASE-CONFIRMATION.md
    - tests/fixtures/355/direction-pairs.json
    - tests/test-355-direction-agreement.cjs
  modified:
    - lib/core/direction-convention.cjs
    - tests/test-355-direction-convention.cjs

key-decisions:
  - "The PWS author confirmed all three phrases exactly as shipped by 355-01 (no replacement wording); DIRECTION_MEANING and NONE_MEANING stay byte-identical, recorded by role only, never a personal name"
  - "PHRASES_CONFIRMED.phrase_hash stamped with the value recomputed live at Task 2 execution time (fba76597996550770774995f22602eb220054e2615a4d6e77212c01bc09cfe16), confirmed identical to the hash printed at the moment the checkpoint answer was given -- zero drift between confirmation and stamping"
  - "Leg B eligibility (both values finite) mirrors direction-convention.cjs classify()'s own null-check-first-then-Number.isFinite gate, not a naive Number.isFinite(Number(v)) -- naive coercion would have wrongly counted null as finite (Number(null) === 0) and broken the 'legs A and B always pass' requirement"
  - "Leg D drives the already-exported lib/core/hsi-engine.cjs::computeHsiMatrix directly with a crafted 2-artifact, 2x2 matrix pair (threshold=0 to guarantee inclusion) rather than falling back to the 'seam missing' failure message, since the function IS exported; the RED comes from classifyDirectionB's opposite bucketing, not a missing seam"
  - "Leg E restricts to pairs with a finite semantic in [0, 1] per the plan's literal scope; pairs with a missing/non-finite lsa but valid semantic are still driven (scoreMeasured never returns 'none', so those remain RED past 355-10 too -- noted here for the 355-10 executor, not fixed by this plan)"
  - "Leg H's three literal patterns (ternary-holding-both-literals, comparison-adjacent-to-literal, bare return-of-literal) do not catch rs-innovation-classifier.cjs's assignment-form rule (classification = 'structural_transfer';); left as specified by the plan text rather than widening the patterns, since leg F already independently proves that file's rule is inverted"

patterns-established:
  - "Fixture-guard pattern: every 355 fixture consumed by a phrase-dependent test carries its own phrase_hash, checked against the live module's phraseHash() at test time (not just at generation time)"

requirements-completed: [HIPS-01]

duration: 55min
completed: 2026-09-23
---

# Phase 355 Plan 02: PWS Author Phrase Ruling and RED Cross-Producer Agreement Test Summary

**Recorded the PWS author's confirmation of all three direction phrases (no replacement wording), stamped it into `lib/core/direction-convention.cjs` as a hash-pinned `PHRASES_CONFIRMED` export, then shipped a 35-pair fixture and an 8-leg (A-H) cross-producer agreement test that fails on exactly legs C through H (109 FAIL / 79 PASS) -- each FAIL line naming the specific 355 plan (09, 10, or 11) that will turn it green.**

## Performance

- **Duration:** 55 min
- **Started:** 2026-09-23T20:45:00Z (approx, continuation agent resume)
- **Completed:** 2026-09-23T21:40:00Z (approx)
- **Tasks:** 2 completed this session (Task 1, the PWS author checkpoint, was resolved by the orchestrator before this continuation started)
- **Files modified:** 3 created, 2 modified

## Accomplishments
- `355-PHRASE-CONFIRMATION.md` records the PWS author's ruling (role only, never a personal name): all three phrases confirmed exactly as shipped by 355-01, no replacement wording, the deck quote they paraphrase, and the binding-effect note that any future phrase edit invalidates every `direction_ok` label under the old hash
- `lib/core/direction-convention.cjs` gains the frozen `PHRASES_CONFIRMED = { by: 'pws-author', at: '2026-09-23', phrase_hash: 'fba765...' }` export; `tests/test-355-direction-convention.cjs` gains the `PHRASES_CONFIRMED.phrase_hash === phraseHash()` equality leg plus a role-only assertion (31/31 assertions pass)
- `tests/fixtures/355/direction-pairs.json` ships 35 pairs (6 clear meaning bridges, 6 clear term collisions, 3 exact ties, 4 near ties at +-0.001/+-0.0001, 2 both-near-zero, 2 both-near-one, 3 missing-value encodings (null, absent key, `"NaN"` string), 2 out-of-range finite, the real 272 HSI row `real-272-hsi-1` (lsa 0.2456 / semantic 0.4828), and 6 `classifier_single_axis` pairs), every `expected` label computed by requiring `direction-convention.cjs` and calling `classify()` in a one-off generator script -- never hand-typed
- `tests/test-355-direction-agreement.cjs` drives all 8 legs: A (module classify, 35/35 PASS) and B (`rs-math.classifyDirection`, 32/32 PASS on finite pairs) hold now; C (`hsi-lsa.classifyDirectionB`), D (`hsi-engine.computeHsiMatrix`, crafted 2-artifact matrices), E (`rs-differential-scorer.scoreMeasured` via `lexicalFn`/`encodeFn` seams), F (`rs-innovation-classifier`'s single-axis branch), G (four live `detect-reverse-salients.py` callers), and H (the "one rule" repo sweep, comment-stripped, with a planted-and-removed negative control) all fail, each FAIL line naming the exact plan (355-09, 355-10, or 355-11) that turns it green
- Leg H's sweep scanned 2089 `lib/**/*.cjs` + `scripts/*.cjs` files and found the comparison-to-label rule duplicated in five files outside the module (`lib/core/hsi-lsa.cjs`, `lib/core/rs-chain-feeder.cjs`, `lib/core/rs-differential-scorer.cjs`, `lib/core/rs-math.cjs`, `lib/memory/test-rs-discovery-engine.cjs`) -- the exact "no second rule" proof the plan's must-haves require, plus a negative control (a planted scratch file) proven caught and cleanly removed

## Task Commits

Each task was committed atomically:

1. **Task 1: The PWS author confirms the two direction phrases** - checkpoint; human ruling relayed via the orchestrator's `<checkpoint_resolution>` (no commit; nothing to build)
2. **Task 2: Record the ruling and stamp PHRASES_CONFIRMED into the module** - `2b0b8bbdc` (feat)
3. **Task 3: direction-pairs fixture and the RED cross-producer agreement test** - `ed8c4090d` (test)

**Plan metadata:** committed separately below (docs: complete plan)

## Files Created/Modified
- `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PHRASE-CONFIRMATION.md` - the durable, machine-checked record of the PWS author's ruling
- `lib/core/direction-convention.cjs` - `PHRASES_CONFIRMED` frozen export
- `tests/test-355-direction-convention.cjs` - `PHRASES_CONFIRMED` presence/role/hash-equality legs (31/31 PASS)
- `tests/fixtures/355/direction-pairs.json` - 35 module-computed (lsa, semantic, expected) pairs, phrase-hash guarded
- `tests/test-355-direction-agreement.cjs` - legs A-H cross-producer agreement test, 79 PASS / 109 FAIL (RED as designed)

## Decisions Made
- All three phrases confirmed as-is; no wording changed, so `DIRECTION_MEANING`/`NONE_MEANING` needed zero edits (see key-decisions above for the finer implementation calls: leg B's finite-eligibility gate, leg D's direct seam use, leg E's scope restriction, leg H's pattern-vs-classifier-file gap)

## Deviations from Plan

### Auto-fixed Issues

None - both tasks' action steps were followed as written. No Rule 1/2/3 auto-fixes were needed.

### Documented departures from literal task wording (no functional impact)

**1. [Documented scope note] Leg E remains RED for missing-lsa pairs even conceptually past 355-10**
- **Found during:** Writing leg E of `tests/test-355-direction-agreement.cjs`
- **Issue:** The plan's behavior list scopes leg E to "pairs with semantic in [0, 1]" with no lsa restriction. `scoreMeasured` never returns the `'none'` sentinel, so pairs with a missing/non-finite lsa (e.g. `missing-null-lsa`, `missing-nan-string-lsa`) but a valid semantic will still mismatch the fixture's `'none'` expectation even after 355-10 flips the direction ternary, unless 355-10 also teaches `scoreMeasured` to emit `'none'` on a non-finite lexical leg.
- **Why not restructured:** The plan's own behavior text does not ask for an lsa filter on leg E, and narrowing the filter myself would silently under-test the exact case 355-10 needs to see. Left as specified; flagged here so the 355-10 executor reads it before assuming "leg E fully green" is possible without a `'none'`-aware direction assignment.
- **Files modified:** none beyond the planned `tests/test-355-direction-agreement.cjs`
- **Committed in:** `ed8c4090d` (test)

---

**Total deviations:** 0 auto-fixed; 1 documented scope note (no functional impact on this plan's own deliverables, forward-looking note for 355-10).
**Impact on plan:** None on this plan's own acceptance criteria -- all Task 2 and Task 3 acceptance lines pass exactly as specified (see Issues Encountered for the full verification transcript summary).

## Issues Encountered

None. Both tasks' automated verification commands from the plan ran clean:
- `node tests/test-355-direction-convention.cjs` exits 0 (31/31 PASS)
- `node -e "const d=require('./lib/core/direction-convention.cjs');if(d.PHRASES_CONFIRMED.phrase_hash!==d.phraseHash())process.exit(1)"` exits 0
- `node -e "const f=require('./tests/fixtures/355/direction-pairs.json');const d=require('./lib/core/direction-convention.cjs');if(f.pairs.length<30||f.phrase_hash!==d.phraseHash())process.exit(1)"` exits 0
- `node tests/test-355-direction-agreement.cjs` exits 1 (RED as designed); every `FAIL:` line starts with `C `, `D `, `E `, `F `, `G `, or `H ` (verified: zero `A `/`B ` failures across 35 A-checks and 32 B-checks)
- `H scanned file count > 0 (2089 files)` line present; `H negative control: planted scratch rule file caught` PASSes; `ls lib/core/__scratch_direction_rule_355.cjs` fails after the run (scratch cleanly removed in the `finally` block)
- `grep -c "turns green in 355-" tests/test-355-direction-agreement.cjs` returns 7 (>= 5 required)

Per the orchestrator's `<shared_tree_rules>` briefing: `tests/run-all-355.sh` was at `PASS=20 FAIL=3 SKIP=11` before this plan (the three pre-existing FAILs are documented external issues in `deferred-items.md`: Phase 356's own em-dash guard on its own file, an `@huggingface/transformers` version gap in `272-cache-probe.test.cjs`, and `PB8-03` in `part8-egress-guard.cjs` -- none of the three touches a file this plan created or modified). `run-all-355.sh`'s glob-discovery leg (`tests/test-355-*.cjs`) auto-picked up `tests/test-355-direction-agreement.cjs` with zero changes to `run-all-355.sh` itself (not in this plan's `files_modified` list). A clean, isolated re-run (captured in full, not truncated by a 120-second tool timeout) confirmed `PASS=20 FAIL=4 SKIP=11` -- exactly the pre-existing baseline plus one new FAIL, and that one new FAIL is `test-355-direction-agreement.cjs` itself (RED by design). An earlier, timeout-truncated run in this same session showed `PASS=19 FAIL=5`; re-running cleanly attributed that extra swing to the concurrently-executing shared tree (Phases 356, 357, 358, 361 committing in parallel), not to this plan's own files -- confirmed by grepping the full leg-by-leg output for every non-PASSED line and finding only the 3 documented pre-existing failures plus the 1 designed-RED leg, nothing new. `tests/test-355-direction-agreement.cjs` will keep showing red until 355-09/355-10/355-11 land; that is the expected, documented state, not a regression.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `355-PHRASE-CONFIRMATION.md` and `PHRASES_CONFIRMED` are ready for every later 355 plan that authors a fixture, citation pair, or direction fixture (D-35's own gate)
- `tests/fixtures/355/direction-pairs.json` and `tests/test-355-direction-agreement.cjs` are ready for 355-09 (flips `hsi-lsa.cjs`, `hsi-engine.cjs`, `rs-innovation-classifier.cjs` onto the module -- legs C, D, F, and part of H turn green), 355-10 (flips `rs-differential-scorer.scoreMeasured` -- leg E and the rest of H turn green), and 355-11 (removes the four live Python callers -- leg G turns green)
- Blocker/concern carried forward: none blocking 355-03 or any other 355-02-independent plan; the leg-E `'none'`-on-missing-lsa scope note above is informational for whoever executes 355-10, not a blocker for that plan's own scope

---
*Phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi*
*Completed: 2026-09-23*

## Self-Check: PASSED

All 5 created/modified files verified present on disk
(`355-PHRASE-CONFIRMATION.md`, `lib/core/direction-convention.cjs`,
`tests/test-355-direction-convention.cjs`,
`tests/fixtures/355/direction-pairs.json`,
`tests/test-355-direction-agreement.cjs`); both commits (`2b0b8bbdc`,
`ed8c4090d`) verified present in `git log`.
