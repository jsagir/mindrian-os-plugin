---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 03
type: tdd
wave: 2
depends_on: ["355-01"]
files_modified:
  - tests/fixtures/355-hsi-thinking-mode-sentences.items.json
  - tests/fixtures/355-hsi-tuning-sentences.json
  - scripts/label-355-gold.cjs
  - tests/test-355-label-cli.cjs
  - tests/fixtures/355-hsi-thinking-mode-sentences.json
  - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/labeling-session-sentences.json
autonomous: false
requirements: [HIPS-08, HIPS-07]
tags: [labeling-cli, gold, blind, hsi, keypress]

must_haves:
  truths:
    - "D-31: scripts/label-355-gold.cjs is an argv switch-case CLI with subcommands start / resume / status / emit over four sets (sentences, citations, pairings-unstamped, pairings-stamped); keys 1-6 label the five thinking modes plus none under a fixed on-screen legend; y/n x3 per pairing (useful, direction ok, already known); s / n / c per citation pair (supports / says nothing / contradicts); u undoes; q (and Ctrl+C) saves and quits; the session is saved after every key through temp-file-then-rename; raw keypress via readline.emitKeypressEvents + setRawMode with a line-mode fallback when stdin is not a TTY"
    - "D-32: the script never requires hsi-spectral.cjs, verification-stamp*.cjs, jev-devtime-client.cjs or any 355 Jev script (tripwire leg), never prints boundary_tag, and never shows a stamp on the unstamped pairing set"
    - "D-33: item files hold no labels; the session file {set, fixture_sha256, order_seed, phrase_module_hash, started_at, entries:{id:{label, at, ms}}} is keyed by id, never by position; emit writes the gold with _labeling_note, labeler 'navigator', fixture_sha256 and labeled_at; resume refuses a changed item-file hash or a changed phrase-module hash (D-35)"
    - "D-35: sentence labeling shows no direction and runs now; pairing and citation sets refuse to start unless PHRASES_CONFIRMED.phrase_hash === phraseHash()"
    - "AI-SPEC Reference Dataset (ii): >= 120 Claude-written sentences, >= 30% boundary cases, no sentence names its own mode, no real names; a separate ~20-sentence tuning set shares no sentence with the gold items"
    - "SPEC Req 5 / HIPS-08: the navigator labels the sentences blind and the gold file is committed BEFORE any tests/fixtures/355-jev-*-responses.json exists"
  artifacts:
    - path: "scripts/label-355-gold.cjs"
      provides: "Blind labeling CLI (D-31..D-33)"
      exports: ["run", "LEGENDS", "SETS", "mulberry32"]
    - path: "tests/test-355-label-cli.cjs"
      provides: "PassThrough-driven CLI proof + blinding tripwires"
      min_lines: 120
    - path: "tests/fixtures/355-hsi-thinking-mode-sentences.items.json"
      provides: ">= 120 unlabeled sentences with boundary_tag"
    - path: "tests/fixtures/355-hsi-thinking-mode-sentences.json"
      provides: "Navigator's blind gold (labeled_at, fixture_sha256)"
  key_links:
    - from: "scripts/label-355-gold.cjs"
      to: "lib/core/direction-convention.cjs"
      via: "phraseHash() recorded as phrase_module_hash; PHRASES_CONFIRMED gate for pairing/citation sets"
      pattern: "phraseHash"
    - from: "tests/fixtures/355-hsi-thinking-mode-sentences.json"
      to: "tests/fixtures/355-hsi-thinking-mode-sentences.items.json"
      via: "fixture_sha256 equals sha256 of the items file bytes"
      pattern: "fixture_sha256"
---

<objective>
Build the blind labeling CLI the whole phase uses (D-31..D-33) and start the one labeling job that shows no direction and can run early (D-35): the navigator's blind gold labels for the HSI thinking-mode sentences (SPEC Req 5, HIPS-08). The same CLI later serves the citation pairs (355-14) and the two fixture-room pairing sittings (355-24, 355-25, HIPS-07).

Purpose: a human is the judge; the CLI only records, and it is built so the judge cannot see what would bias the label.
Output: item and tuning fixtures, `scripts/label-355-gold.cjs`, its test, and the committed sentence gold.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-AI-SPEC.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PATTERNS.md
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the thinking-mode sentence items and the separate tuning set (no labels)</name>
  <files>tests/fixtures/355-hsi-thinking-mode-sentences.items.json, tests/fixtures/355-hsi-tuning-sentences.json</files>
  <read_first>
    - lib/core/hsi-spectral.cjs lines 95-140 (THINKING_MODES_v1 and MODE_PATTERNS, so boundary cases target the real regex traps: "is/has" judgments, "important" in neutral text, "issue", "island", "hash", "could" in plain description)
    - 355-AI-SPEC.md Section 5 "Reference Dataset" row (ii) and Section 3 "Common Pitfalls" items 1, 4 and 7
  </read_first>
  <action>Write tests/fixtures/355-hsi-thinking-mode-sentences.items.json as `{ "_note": "Claude-written dev fixture, Phase 355. Items carry NO labels; gold is the navigator's blind judgment (355-03 Task 3).", "items": [ { "id": "s001", "sentence": "...", "boundary_tag": null | "<tag>" }, ... ] }` with at least 120 sentences, written to cover the five modes plus none roughly evenly (about 20 each; the intended mode is NOT recorded anywhere), each sentence 6-40 words and at most 300 characters, domains varied (clinical operations, logistics, education, manufacturing, finance, public policy, software), no real person, company or place names. At least 36 items carry a boundary_tag from this closed set: `is-judgment` ("This is the biggest risk in the plan"), `important-neutral`, `integrative-vs-creative`, `analytical-vs-evaluative`, `regex-trap` (issue / island / hash / could-as-description), `none-fragment` (greetings, headings, logistics). No sentence names its own mode ("This is an analytical point" is forbidden). Write tests/fixtures/355-hsi-tuning-sentences.json the same shape with about 20 different sentences (ids t001..t020) for criteria iteration only; no sentence may appear in both files.</action>
  <verify>
    <automated>node -e "const a=require('./tests/fixtures/355-hsi-thinking-mode-sentences.items.json').items;const t=require('./tests/fixtures/355-hsi-tuning-sentences.json').items;const S=new Set(a.map(x=>x.sentence));if(a.length<120)throw 1;if(a.filter(x=>x.boundary_tag).length/a.length<0.3)throw 2;if(t.some(x=>S.has(x.sentence)))throw 3;if(a.some(x=>'label' in x||'gold' in x||x.sentence.length>300))throw 4;if(/\b(analytical|integrative|descriptive|evaluative|creative)\b/i.test(a.map(x=>x.sentence).join(' ')))throw 5"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 (>= 120 items, >= 30% boundary-tagged, disjoint tuning set, no label keys, <= 300 chars, no sentence contains a mode name)
    - Every boundary_tag value is one of the six tags listed in the action
  </acceptance_criteria>
  <done>A blind-ready item set exists that tests the regex where it is known to fail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/label-355-gold.cjs and its PassThrough-driven test (D-31, D-32, D-33)</name>
  <files>tests/test-355-label-cli.cjs, scripts/label-355-gold.cjs</files>
  <read_first>
    - scripts/build-section-command-ledger.cjs lines 660-710 (the argv switch-case router shape to copy)
    - 355-RESEARCH.md "Code Examples > Keypress with fallback" and "Pitfall 12" (WSL raw mode, Ctrl+C in raw mode, setRawMode(false) in finally / process.on('exit'))
    - scripts/eval-icm-writers.cjs lines 75-105 (realpath + separator prefix guard idiom; READ ONLY, peer history)
    - lib/core/direction-convention.cjs (phraseHash, PHRASES_CONFIRMED)
    - tests/helpers/hygiene-355.cjs
  </read_first>
  <behavior>
    - `start --set sentences --items <p> --session-dir <d>` shows one sentence at a time with the fixed legend `1 analytical  2 integrative  3 descriptive  4 evaluative  5 creative  6 none   u undo   q save+quit`; writing '1' then '6' via a PassThrough records two entries keyed by item id with label, at, ms
    - after every key the session file exists and parses (atomic write: a `.tmp` sibling renamed over the target)
    - 'u' removes the most recent entry and re-shows that item; 'q' saves and exits 0; Ctrl+C (key.ctrl && name 'c') saves and exits 0
    - `resume` with an edited items file (hash changed) exits non-zero with a message naming fixture_sha256; `resume` for a pairing or citation set with a changed phrase hash exits non-zero naming phrase_module_hash
    - `start --set citations` or a pairings set refuses (non-zero) when PHRASES_CONFIRMED is missing or PHRASES_CONFIRMED.phrase_hash !== phraseHash()
    - the order is a seeded shuffle (mulberry32); the seed is stored as order_seed; the same seed gives the same order
    - `status` prints labeled/total counts and nothing else about labels
    - `emit --set sentences --out <p>` writes {_labeling_note, labeler:'navigator', fixture_sha256, labeled_at, items:[{id, sentence, gold}]} only when every item is labeled; otherwise exits non-zero
    - captured output never contains any boundary_tag value; the unstamped pairing display never contains the substrings 'strong', 'indirect', 'unverified' or a stamp glyph
    - `--out` must resolve (realpath + path.sep) inside <repo>/tests/fixtures/ or os.tmpdir(); `--session-dir` inside the phase dir or os.tmpdir(); anything else exits non-zero
    - static tripwire: the script's non-comment lines contain none of hsi-spectral, verification-stamp, jev-devtime-client, jev-question-ceilings, jev-response-schema, measure-hsi, calibrate-citation, judge-355
  </behavior>
  <action>
RED: write tests/test-355-label-cli.cjs (hygiene-355 preamble) that drives the exported `run({ argv, input, output, now, repoRoot })` with a `stream.PassThrough` as input (line mode, since a PassThrough is not a TTY) and a string-collecting writable as output, over tmp copies of tiny item files (3 sentences, 2 citation pairs, 2 pairings), asserting every behavior line above. Run it; it fails. Commit `test(355-03): add failing labeling CLI test`.

GREEN: create scripts/label-355-gold.cjs ('use strict', BSL header, "dev-time only, never required from lib/ or hooks/", hyphens only). Exports `run`, `SETS`, `LEGENDS`, `mulberry32`. SETS maps set id to defaults: sentences -> items `tests/fixtures/355-hsi-thinking-mode-sentences.items.json`, out `tests/fixtures/355-hsi-thinking-mode-sentences.json`, keys 1-6 -> ['analytical','integrative','descriptive','evaluative','creative','none']; citations -> items `tests/fixtures/355-citation-pairs.items.json`, out `tests/fixtures/355-citation-pairs.json`, keys s/n/c -> supports / says_nothing / contradicts; pairings-unstamped -> items `tests/fixtures/355-rooms/pairings.items.json`, out `tests/fixtures/355-rooms/judgments.json`, three sequential y/n prompts (useful, direction ok, already known); pairings-stamped -> items `tests/fixtures/355-rooms/pairings-stamped.items.json`, out `tests/fixtures/355-rooms/judgments-stamped.json`, same prompts plus the item's pre-rendered `stamp_lines` text shown verbatim. Default session path `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/labeling-session-<set>.json` (the D-33 shape, one file per set). Display only whitelisted fields per set: sentences -> `sentence`; citations -> `claim` + numbered `path` hops; pairings -> `room`, `a_excerpt`, `b_excerpt`, `direction_phrase` (+ `stamp_lines` for the stamped set only). Keypress: raw mode when `input.isTTY && typeof input.setRawMode === 'function'` (restore in finally and on process exit), else line mode (one token per line). The CLI main wires process.stdin/stdout and `process.exitCode`. On completion of the last item in a set, print a one-line hint to run emit (do not auto-emit).
Run the test green. Commit `feat(355-03): blind labeling CLI (D-31, D-32, D-33)`.
  </action>
  <verify>
    <automated>node tests/test-355-label-cli.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-355-label-cli.cjs` exits 0 and prints `FAIL: 0`
    - `grep -vE '^\s*(//|\*|/\*)' scripts/label-355-gold.cjs | grep -cE "hsi-spectral|verification-stamp|jev-devtime-client|jev-question-ceilings|jev-response-schema|measure-hsi|calibrate-citation|judge-355"` prints 0
    - `grep -c "renameSync" scripts/label-355-gold.cjs` >= 1 and `grep -c "setRawMode(false)" scripts/label-355-gold.cjs` >= 1
    - `node scripts/label-355-gold.cjs status --set sentences` exits 0 and prints a `0/<N>` style count before any labeling
  </acceptance_criteria>
  <done>A tested, blinded, crash-safe labeling CLI exists.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: The navigator labels the >= 120 sentences blind; the executor commits the gold</name>
  <files>tests/fixtures/355-hsi-thinking-mode-sentences.json, .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/labeling-session-sentences.json</files>
  <read_first>
    - scripts/label-355-gold.cjs usage text (`node scripts/label-355-gold.cjs --help`)
  </read_first>
  <action>Ask the navigator to label in their own terminal (a real TTY). Nothing in this plan runs any regex, Jev or stamp code before the gold exists. On resume, the executor verifies the gold and commits both files together as `docs(355-03): navigator blind gold for HSI thinking-mode sentences` (git add -f for the phase-dir session file). Confirm `ls tests/fixtures/355-jev-*-responses.json` fails at commit time (gold precedes every Jev response file, D-32).</action>
  <what-built>scripts/label-355-gold.cjs and the unlabeled item file</what-built>
  <how-to-verify>
    1. In a terminal at the repo root run `node scripts/label-355-gold.cjs start --set sentences` (or `resume --set sentences` after a break).
    2. For each sentence press 1-6 by what the sentence DOES (1 analytical, 2 integrative, 3 descriptive, 4 evaluative, 5 creative, 6 none); `u` undoes, `q` saves and quits.
    3. When `status --set sentences` shows all items labeled, run `node scripts/label-355-gold.cjs emit --set sentences`.
    4. Reply "labeled".
  </how-to-verify>
  <verify><automated>node -e "const g=require('./tests/fixtures/355-hsi-thinking-mode-sentences.json');const c=require('node:crypto');const fs=require('node:fs');const h=c.createHash('sha256').update(fs.readFileSync('tests/fixtures/355-hsi-thinking-mode-sentences.items.json')).digest('hex');if(!g.labeled_at||g.labeler!=='navigator'||g.fixture_sha256!==h||g.items.length<120||g.items.some(i=>!['analytical','integrative','descriptive','evaluative','creative','none'].includes(i.gold)))process.exit(1)"</automated></verify>
  <acceptance_criteria>
    - The verify command exits 0 (labeled_at present, labeler 'navigator', hash matches the items file, >= 120 items, every gold in the six-label set)
    - `git log --oneline -1 -- tests/fixtures/355-hsi-thinking-mode-sentences.json` shows the docs(355-03) commit and `git ls-files tests/fixtures | grep -c "355-jev-"` prints 0 at that commit
  </acceptance_criteria>
  <resume-signal>Type "labeled" once emit has written the gold file</resume-signal>
  <done>The blind sentence gold is committed before any machine answer exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator keystrokes -> gold files | Human judgment becomes the measurement truth |
| CLI argv paths -> filesystem writes | --out / --session-dir / --items come from the command line |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-10 | Tampering | session edited between sittings | mitigate | fixture_sha256 + phrase_module_hash checked on resume; mismatch exits non-zero (tested) |
| T-355-11 | Tampering | label leakage biasing the judge | mitigate | Whitelisted display fields; boundary_tag never printed; no stamp on the unstamped set; static tripwire bans hsi-spectral / stamp / Jev requires |
| T-355-12 | Elevation of privilege | --out / --session-dir path escape | mitigate | realpath + path.sep prefix guard to tests/fixtures/ or os.tmpdir() (and the phase dir for sessions); anything else exits non-zero (tested) |
| T-355-13 | Denial of service | crash mid-session loses labels | mitigate | temp-then-rename save after every key; Ctrl+C in raw mode saves and quits; setRawMode(false) restored in finally |
| T-355-14 | Repudiation | gold produced after machine answers | mitigate | Gold committed before any 355-jev-*-responses.json exists; git-order asserted in 355-15's record test |
| T-355-15 | Information disclosure | real names in fixtures | mitigate | Claude-written sentences, no real names; labeler recorded by role 'navigator' |
</threat_model>

## Artifacts this phase produces

This plan's share:
- CLI `scripts/label-355-gold.cjs`: subcommands `start`, `resume`, `status`, `emit`; flags `--set`, `--items`, `--out`, `--session-dir`, `--seed`; exports `run`, `SETS` (`sentences`, `citations`, `pairings-unstamped`, `pairings-stamped`), `LEGENDS`, `mulberry32`
- Session files `.planning/phases/355-.../labeling-session-<set>.json` (`set`, `fixture_sha256`, `order_seed`, `phrase_module_hash`, `started_at`, `entries`)
- Fixtures `tests/fixtures/355-hsi-thinking-mode-sentences.items.json` (`id`, `sentence`, `boundary_tag`), `tests/fixtures/355-hsi-tuning-sentences.json`, gold `tests/fixtures/355-hsi-thinking-mode-sentences.json` (`_labeling_note`, `labeler`, `fixture_sha256`, `labeled_at`, `items[{id, sentence, gold}]`)
- Test `tests/test-355-label-cli.cjs`

<verification>
- `node tests/test-355-label-cli.cjs` exits 0
- Gold file verify command in Task 3 exits 0
</verification>

<success_criteria>
- Sentences labeled blind by the navigator, gold committed before any Jev response file
- CLI ready for the citation and pairing sittings
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-03-SUMMARY.md` when done.
</output>
