---
phase: 355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi
plan: 27
type: execute
wave: 11
depends_on: ["355-21", "355-23", "355-25", "355-26", "355-28"]
files_modified:
  - tests/run-all-355.sh
  - lib/memory/run-feynman-tests.cjs
  - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/2026-07-03-registry-drift-gate-prevent-silent-command-disappearance-key.md
  - docs/2026-09-2x-HANDOFF-theo-phase-355-results.md
  - docs/OPEN-HANDOFFS.md
autonomous: true
requirements: [HIPS-01, HIPS-02, HIPS-03, HIPS-04, HIPS-05, HIPS-06, HIPS-07, HIPS-08, HIPS-09, HIPS-10]
tags: [close-out, phase-gate, requirements, theo-handoff, rethinking-room]

must_haves:
  truths:
    - "HIPS-10 / SPEC AC13: tests/run-all-355.sh is final (every Part 8 / Part 9 sweep target is a hard `run`, a missing target FAILS; run-all-353 joins the no-regression legs if it was green at BASE_355) and `bash tests/run-all-355.sh` exits 0, including the doctor --acceptance leg whose failing-point set is a subset of the BASE_355 baseline"
    - "HIPS-10: every offline tests/test-355-*.cjs is appended to TEST_FILES in lib/memory/run-feynman-tests.cjs (the registry is not under tests/), each with a one-line Phase 355 plan/ID comment; a registered file that is missing counts FAIL and exit 77 counts SKIP"
    - "Registration mechanism (RESEARCH Phase Requirements, the Phase 272/274/276/275/340 phase-close precedent): only when `git status --short .planning/REQUIREMENTS.md .planning/ROADMAP.md` is empty, add `### Phase 355 - Hidden in Plain Sight (HIPS family)` after the last phase section and before `## Traceability`, with the house provenance paragraph and one row per HIPS-01..HIPS-10, each flipped to `- [x]` with an indented `**Measured:** (date) <command and number>` line, or left `- [ ]` with a stated reason; update the Traceability active-requirement count, the family list and the caveat family list; commit immediately"
    - "ROADMAP: the Phase 355 card's `**Requirements**: TBD (minted at spec time)` line becomes `HIPS-01..HIPS-10 (phase-local, minted in the 355 plan set; registered in REQUIREMENTS.md)` ONLY if the orchestrator has not already changed it; nothing else in ROADMAP.md is touched (the Phase 267/354 interleave stays out of scope)"
    - "Handoff Section 5 (M-T protocol): a durable dated entry addressed to T-3 records the measured unverified rate, reason mix, hub-inflation share, provenance-routed strong count, per-tier hit rate and the fixture rooms used (counts and buckets only, no room content), plus, for T-2, the citation-check policy text and the measured stated-vs-withheld agreement; docs/OPEN-HANDOFFS.md gains an entry; a live ping is sent to a running T-side session if one exists, otherwise the entry says the ping is pending for the navigator; no Theo file is edited"
    - "Dev-Research Compositing (CLAUDE.md): a dated research entry under ~/MindrianRooms/rethinking-mindrianos/research/ (markdown only, the room's own convention) summarizes the phase's findings with citations back to 355-VERIFICATION.md, 355-JEV-MEASUREMENT.md and this plan, mirrored to ~/MindrianOS/research/, committed in the repository that tracks the room; no room.db or graph write"
    - "D-26 folded todo: the registry-drift-gate todo is annotated with the Phase 355 ruling (no MCP rename; any future rename ships a deprecated alias); D-17's posture entry already landed in 355-21"
    - "Release note item: the next release's CHANGELOG must state that the direction convention is unified and stored HSI labels are re-derived from their similarity pair (research Runtime State Inventory); recorded in the handoff entry because release is out of this phase's scope"
    - "355-VALIDATION.md is finalized: every row's status reflects the real run, nyquist_compliant true, approval dated"
  artifacts:
    - path: "tests/run-all-355.sh"
      provides: "Final phase gate"
      contains: "run-all-353"
    - path: "docs/2026-09-2x-HANDOFF-theo-phase-355-results.md"
      provides: "Durable M-side entry for Theo T-3 and T-2 (the real date replaces 2x)"
  key_links:
    - from: ".planning/REQUIREMENTS.md Phase 355 section"
      to: "the plan SUMMARYs and the two records"
      via: "Measured lines citing the command and number"
      pattern: "HIPS-10"
---

<objective>
Close Phase 355 (all HIPS IDs; SPEC AC13): finalize the phase gate, register the offline tests, register and close the HIPS-01..10 rows in REQUIREMENTS.md with measured proof, file the research trail in the rethinking-mindrianos room, and send Theo the measurements the handoff promised. Every claim in this plan's outputs cites a command and a number from the SUMMARYs, 355-VERIFICATION.md or 355-JEV-MEASUREMENT.md; none is inferred.

Output: final run-all-355.sh, registry entries, REQUIREMENTS rows, ROADMAP line (only if still TBD), validation sign-off, the Theo handoff entry, the room filing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-SPEC.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-BASELINE.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-VERIFICATION.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-JEV-MEASUREMENT.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-VALIDATION.md
@docs/2026-09-23-HANDOFF-theo-phase-355-seams.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final phase gate, test registration, validation sign-off (HIPS-10, SPEC AC13)</name>
  <files>tests/run-all-355.sh, lib/memory/run-feynman-tests.cjs, .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-VALIDATION.md</files>
  <read_first>
    - tests/run-all-355.sh (the 355-01 skeleton) and 355-BASELINE.md (BASE_355, baseline failing set)
    - lib/memory/run-feynman-tests.cjs TEST_FILES tail (the commented-path entry shape; missing = FAIL, 77 = SKIP)
    - every 355 SUMMARY (for which tests exist and which are offline)
    - `git status --short lib/memory/run-feynman-tests.cjs tests/run-all-355.sh` (must be empty; collision rule)
  </read_first>
  <action>run-all-355.sh: convert the Part 8 and Part 9 sweep legs from run_if-on-existence to hard `run` over the final target list (lib/core/direction-convention.cjs, lib/core/verification-stamp.cjs, lib/core/verification-stamp-format.cjs, lib/core/floor-disclosure.cjs, scripts/stamp-connections.cjs, scripts/check-cross-connection-honesty.cjs), so a missing target fails; add the Part 9 check that scripts/eureka-portfolio-report.cjs and lib/core/eureka/opportunity-harvest.cjs still require navigation; add `bash tests/run-all-353.sh` to the no-regression legs only if `git stash`-free evidence shows it was green at BASE_355 (else record why it is excluded in the header); add the amended tests (272-hsi-lsa-algorithm, test-211, test-215, test-213-sensor-eureka, test-213-part8-boundary, test-scout-cadence-fires, test-dial-label-bank-drift, lib/memory/test-rs-innovation-classifier) as explicit no-regression legs; keep the doctor leg comparing against the BASE_355 set (never re-baseline). lib/memory/run-feynman-tests.cjs: append every offline tests/test-355-*.cjs after the current tail, each with `// Phase 355 Plan-NN, HIPS-xx: <one-line why>`; do not register anything that needs a key or the network (all 355 tests are offline by design; confirm each exits 0 or 77 with the key unset). Run `bash tests/run-all-355.sh` and `node lib/memory/run-feynman-tests.cjs`. Fill 355-VALIDATION.md's Status column from the real runs, set `nyquist_compliant: true` and `wave_0_complete: true`, tick the sign-off list, write `**Approval:** approved <date>`. Commit `test(355-27): final phase gate and test registration (HIPS-10)` (git add -f the validation file).</action>
  <verify>
    <automated>bash tests/run-all-355.sh && node lib/memory/run-feynman-tests.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both commands exit 0; the run-all output's doctor leg reports no failing point outside the BASE_355 set
    - `grep -c "tests', 'test-355-" lib/memory/run-feynman-tests.cjs` equals `ls tests/test-355-*.cjs | wc -l`
    - `grep -c "run_if \"Part 8" tests/run-all-355.sh` prints 0 (the sweeps are hard legs)
    - `grep -c "nyquist_compliant: true" .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-VALIDATION.md` = 1
  </acceptance_criteria>
  <done>The phase gate is final, green and registered where the release train will run it.</done>
</task>

<task type="auto">
  <name>Task 2: Register and close HIPS-01..10; ROADMAP requirements line; D-26 todo annotation</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/todos/pending/2026-07-03-registry-drift-gate-prevent-silent-command-disappearance-key.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md: the last phase section before `## Traceability` (grep `^### Phase`) and the Phase 354 and 357 sections (row format, provenance paragraph wording) and the Traceability paragraph (active count, family list, caveat list)
    - .planning/ROADMAP.md: only the `### Phase 355` card (grep the Requirements line)
    - 355-RESEARCH.md "Phase Requirements" (the HIPS table and registration steps 1-7)
    - every 355 SUMMARY, 355-VERIFICATION.md and 355-JEV-MEASUREMENT.md (the numbers the Measured lines cite)
  </read_first>
  <action>First run `git status --short .planning/REQUIREMENTS.md .planning/ROADMAP.md`; if either is dirty (a peer is editing), wait and retry; never stage or revert a peer's change. Then add the section `### Phase 355 - Hidden in Plain Sight (HIPS family)` immediately after the last phase section and before `## Traceability`, opening with: "HIPS-01..10 were minted in the Phase 355 plan set (2026-09-23), ratifying `355-RESEARCH.md`'s proposed IDs for SPEC Requirements 1-7 plus the AI-SPEC's D15/D18 dev-time measurements and the phase gate, scoped to Phase 355 only, and are registered here at phase close by `355-27-PLAN.md` Task 2, per the Phase 272/274/276/275/340 precedent. `355-CONTEXT.md` is the scope contract." Then one row per ID in the SYS/THEO row format (about 100 columns, 6-space continuation), each ending with the plan list, followed by an indented `**Measured:** (<date>) <command> -> <number or result>` line: HIPS-01 one direction convention (agreement test legs A-H, ranking pin, readers test; plans 01, 02, 09, 10, 11, 12, 19); HIPS-02 floors disclosed (ledger row count, floor sweep incl. leg 4b; plans 04, 16, 23); HIPS-03 naming honesty (naming test + CIRS gates; plan 05); HIPS-04 verification stamp (stamp truth / unreachable / coverage / part8 tests, capture replay, snapshot count; plans 06, 08, 14, 16, 17, 18, 21, 23, 25); HIPS-05 stamp on five producers and three surfaces with no decimal (no-decimal + tri-polar tests; plans 06, 16, 17, 18, 22, 23); HIPS-06 proposed filing, SENS-13 once, gate-only promotion (filing, side-channel, fire-once, promotion tests; plans 19, 20, 22); HIPS-07 hit rate recorded (per-room n, pooled rate with Wilson, baseline; plans 03, 13, 24, 25); HIPS-08 HSI Jev vs regex and the decision (both accuracies, bar result, decision; plans 03, 07, 15, 28); HIPS-09 citation calibration and usefulness agreement (stated vs withheld agreement, band, D18 agreement; plans 07, 14, 26); HIPS-10 phase gate (run-all-355, doctor blocker, registry; plans 01, 21, 27). A row whose proof is missing (for example a BLOCKED live run) stays `- [ ]` with the stated reason. In `## Traceability`: add HIPS-01..10 to the active count (read the current number, add 10), append "plus HIPS-01..10 (Phase 355)" to the family list with a one-sentence mint note in the house style, and add HIPS to the caveat family list. ROADMAP.md: only if the Phase 355 card still reads `**Requirements**: TBD (minted at spec time)`, replace it with `**Requirements**: HIPS-01..HIPS-10 (phase-local, minted in the 355 plan set; registered in REQUIREMENTS.md)`; touch nothing else. Append to the registry-drift todo a dated "Phase 355 ruling (D-26)" paragraph: the drift gate covers slash commands only, so 355 shipped description fixes plus a fixture test and no rename; any future MCP rename must ship a deprecated alias. Commit REQUIREMENTS.md (and ROADMAP.md if changed, and the todo with git add -f) immediately: `docs(355-27): register and close HIPS-01..10 with measured proof`.</action>
  <verify>
    <automated>grep -c "^### Phase 355 - Hidden in Plain Sight (HIPS family)" .planning/REQUIREMENTS.md && test "$(grep -cE '^- \[[ x]\] \*\*HIPS-(0[1-9]|10)\*\*' .planning/REQUIREMENTS.md)" -eq 10</automated>
  </verify>
  <acceptance_criteria>
    - The section heading appears once and exactly ten HIPS rows exist
    - Every `- [x]` HIPS row is followed by a `**Measured:**` line; every `- [ ]` HIPS row carries a stated reason
    - `grep -c "HIPS-01..10 (Phase 355)" .planning/REQUIREMENTS.md` >= 1
    - `git diff HEAD~1 -- .planning/ROADMAP.md | grep '^[-+]' | grep -v '^[-+][-+]' | wc -l` prints 0 or 2 (only the Requirements line, if it was still TBD)
  </acceptance_criteria>
  <done>Every HIPS ID is registered with the proof that closes it, or the honest reason it stays open.</done>
</task>

<task type="auto">
  <name>Task 3: Theo handoff entry (Section 5) and the rethinking-mindrianos research filing</name>
  <files>docs/2026-09-2x-HANDOFF-theo-phase-355-results.md, docs/OPEN-HANDOFFS.md</files>
  <read_first>
    - docs/2026-09-23-HANDOFF-theo-phase-355-seams.md sections 4-6 (what M-side promised to send back, and the boundaries)
    - ~/Theo/docs/M-T-COORDINATION-PROTOCOL.md (READ ONLY: durable entry plus live ping, no cross-edits)
    - docs/OPEN-HANDOFFS.md (entry format)
    - tests/fixtures/355-rooms/hit-rate-record.json, tests/fixtures/355-jev-calibration-record.json, tests/fixtures/355-theo-find-connections-responses.json (latency), data/framework-names.json (snapshot date, count)
    - The most recent entry under ~/MindrianRooms/rethinking-mindrianos/research/ (folder and frontmatter convention) and `git -C ~ log --oneline -5` (the 'rethinking-mindrianos: file ...' message convention)
  </read_first>
  <action>Write docs/2026-09-<DD>-HANDOFF-theo-phase-355-results.md (replace the 2x in the filename with the real day) in the style of the seams handoff, Feynman prose, hyphens only, no room content, counts and buckets only: to T-3 - fixture rooms used (names and sizes only), unverified rate and reason mix, not_called share, hub-inflation share and its proxy source, provenance-routed strong count (paths through BrainRecord / SOURCED_FROM), per-tier hit rate with n and Wilson, Theo find_connections latency p50 / p95, snapshot coverage (live names count, date, source hash), and the observation that ADDRESSES_PROBLEM_TYPE-only and SOURCED_FROM-only paths stamp unverified under the D-49 lateral rule (a canon-coverage input, not a request); to T-2 - the citation-check policy text verbatim, the stated-vs-withheld agreement overall and on the auto slice, the measured band and the future-seam rule, and that runtime stamps still say judge none; to T-5 - that the plugin sets backend 'theo' itself until Theo Phase 20 adds its literal; any Theo description mirror found by 355-05; a release-note item for the next plugin release (direction convention unified; stored HSI labels re-derived from their similarity pair; find-connections on Desktop / Cowork says not yet checked). Add a dated entry at the top of docs/OPEN-HANDOFFS.md pointing to it. If a T-side session is visible to this session (list running peer sessions), send it a one-line ping naming the file; otherwise add "live ping pending for the navigator" to the entry. Never edit a file under ~/Theo. Then file the research trail: create a dated folder under ~/MindrianRooms/rethinking-mindrianos/research/ following the most recent entry's convention (markdown only; summary of the direction ruling, the stamp contract and its degradation matrix, the floor disclosure, the hit-rate record, the Jev measurements and decision, the Theo findings, each citing the phase files and commits), mirror it to ~/MindrianOS/research/, and commit it in the repository that tracks the room with the 'rethinking-mindrianos: file ...' message convention; no room.db or graph write. Commit the two docs files in the dev repo: `docs(355-27): Theo results handoff (T-3, T-2) and open-handoffs entry`.</action>
  <verify>
    <automated>ls docs/2026-09-*-HANDOFF-theo-phase-355-results.md && grep -c "phase-355-results" docs/OPEN-HANDOFFS.md && grep -cP '\x{2014}' docs/2026-09-*-HANDOFF-theo-phase-355-results.md | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - The handoff file exists, names T-3 and T-2 sections, cites the unverified rate, hub-inflation share and the stated-vs-withheld agreement with numbers taken from the records, and contains no em-dash
    - docs/OPEN-HANDOFFS.md references it
    - A new dated research folder exists under ~/MindrianRooms/rethinking-mindrianos/research/ and a mirror under ~/MindrianOS/research/, committed with the room repository's message convention
    - `git -C ~/Theo status --short` shows nothing this session created
  </acceptance_criteria>
  <done>Theo has the evidence it asked for, and the reasoning trail lives in the room as the compositing rule requires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this session -> shared tracked files (REQUIREMENTS.md, ROADMAP.md, OPEN-HANDOFFS.md) | Peers edit the same files |
| M-side -> T-side | Durable entry only; no cross-edit |
| dev repo -> rethinking-mindrianos room | Research trail filed into a real room |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-135 | Tampering | clobbering a peer's REQUIREMENTS / ROADMAP edit | mitigate | Clean-file check before editing; commit immediately; only the 355 section, the traceability lines and one ROADMAP line change |
| T-355-136 | Repudiation | a requirement closed without proof | mitigate | Every `- [x]` carries a Measured line with a command and a number; unproven rows stay open with a reason |
| T-355-137 | Information disclosure | room content in the Theo handoff | mitigate | Counts, buckets, canon names and edge types only; fixture rooms named by folder, never quoted |
| T-355-138 | Tampering | editing Theo's repository | mitigate | M-T protocol: durable M-side file plus ping; acceptance checks ~/Theo status |
| T-355-139 | Tampering | room graph writes during filing | mitigate | Markdown files only in a new dated folder; no room.db or graph write |
| T-355-140 | Repudiation | a silently re-baselined doctor gate | mitigate | The doctor leg keeps the BASE_355 set; a new failing point fails the gate |
</threat_model>

## Artifacts this phase produces

Full phase inventory (every symbol Phase 355 creates):
- Modules: `lib/core/direction-convention.cjs` (`classify`, `classifyDiff`, `DIRECTIONS`, `DIRECTION_MEANING`, `NONE`, `NONE_MEANING`, `phraseHash`, `PHRASES_CONFIRMED`); `lib/core/verification-stamp.cjs` (`Stamp`, `TIERS`, `BACKENDS`, `REASONS`, `LATERAL_EDGE_TYPES`, `ANCHOR_EDGE_TYPES`, `POSTURE`, `extractCarried`, `resolveEndpoint`, `loadFrameworkNames`, `loadCommandFrameworks`, `tierFromHops`, `choosePath`, `stampFinding`, `stampFindingDetailed`, `stampFindings`, `toNodeProps`, `fromNodeProps`); `lib/core/verification-stamp-format.cjs` (`formatStampLines`, `formatPathText`, `formatUncheckedDesktop`, `assertNoScalar`, `GLYPH`, `SEP`, `HOP_WORDS`, `REASON_WORDS`, `BACKEND_WORDS`, `UNVERIFIED_ADVICE`); `lib/core/floor-disclosure.cjs` (`PRODUCER_IDS`, `loadLedger`, `disclosureFor`, `disclosureLine`)
- Reason enums: `handle_unresolved`, `backend_unavailable`, `tool_not_listed`, `egress_refused`, `tier_denied`, `rate_limited`, `invalid_key`, `text_reply`, `endpoint_unresolved`, `no_path_within_3_hops`, `co_sourced_only`, `no_lateral_relation`, `malformed_response`; classifier output value `none`
- Scripts / CLIs: `scripts/label-355-gold.cjs`, `scripts/check-floor-ledger.cjs`, `scripts/refresh-framework-names.cjs`, `scripts/capture-355-theo-responses.cjs`, `scripts/jev-question-ceilings.cjs`, `scripts/jev-response-schema.cjs`, `scripts/measure-hsi-thinking-mode.cjs`, `scripts/calibrate-citation-check.cjs`, `scripts/judge-355-usefulness.cjs`, `scripts/measure-355-hit-rate.cjs`, `scripts/stamp-connections.cjs`, `scripts/check-cross-connection-honesty.cjs`, `scripts/distill-hsi-thinking-mode-rules.cjs` (only if adopted)
- CLI flags: `--stamp` (whitespace-to-graph, hsi-to-graph), `--top` (hsi-to-graph, measure-355-hit-rate), `--check` / `--live` / `--jev-fixture` / `--repeats` / `--regex-only` / `--append` as listed per script; `jev()` options `timeoutMs`, `honorRetryAfter`; `runModeInternal` option `stampFn`; `writeReverseSalientEdges` option `stampsByPairKey`; `bankStatements` options `stampsByKey`, `roomDir`, `runMode`
- Jev profiles: `EGRESS_PROFILES.hsi_thinking_mode`, `citation_check`, `usefulness_judge`
- Data / ledgers: `data/floor-ledger.json`, regenerated `data/framework-names.json` (`source_sha256`, `stale_review`), `data/hsi-thinking-mode-rules.json` (only if adopted and cleared)
- Side channel v2: `SIDE_CHANNEL_SCHEMA_VERSION = 2`, `CRITIC_TAGS_SCHEMA_VERSION = 1`, keys `stamp`, `opportunity_handle`; `writeStampedSideChannel`, `markEurekaReachSurfaced`, `EUREKA_REACH_LEDGER_RELPATH` -> `<room>/.mindrian/eureka-reach-ledger.json`
- Sensor evidence-bag fields: `opportunity_handle`, `stamp_verification`, `stamp_backend`, `stamp_direction`, `stamp_judge`, `stamp_reason`, `stamp_path`
- Node / edge property names: `verification`, `backend`, `direction`, `judge`, `reason`, `path`, `path_labels`, `path_edges`, `path_len`, `pws_stage`, `engine_mode`, `formula_version: 'stamp-v1'`; SOURCED_FROM properties `relation: 'sourced_from'`, `origin: 'eureka-355'`
- memory_events: `cross_connection_stamped`, `cross_connection_gate_outcome`
- Doctor blocker id: `cross-connection-honesty` (env `DOCTOR_SKIP_CROSS_CONNECTION`)
- Fixtures: `tests/fixtures/355/{direction-pairs.json, eureka-ranking-pin.json, theo-stub-responses.json, producers/*, prose/*}`, `tests/fixtures/355-rooms/{room-ill-defined, room-extend, room-control, README.md, planted-cases.json, pairings.items.json, pairings-stamped.items.json, judgments.json, judgments-stamped.json, stamps.json, hit-rate-record.json}`, `tests/fixtures/355-hsi-*.json`, `tests/fixtures/355-citation-pairs*.json`, `tests/fixtures/355-theo-find-connections-responses.json`, `tests/fixtures/355-jev-*.json`
- Tests: `tests/test-355-*.cjs` (direction-convention, direction-agreement, direction-readers, eureka-ranking-pin, label-cli, floor-sweep, naming-honesty, stamp-truth, theo-unreachable, stamp-format, jev-ceilings, framework-names, fixture-rooms, theo-capture, hsi-measurement-record, producer-whitespace-hsi, producer-rs-connections, producer-eureka, side-channel-v2, filing, part8-egress, cirs-wiring, doctor-point, gate-opportunity-promotion, sens13-fire-once, no-decimal, tri-polar, stamp-coverage, hit-rate-record, jev-calibration-record, hsi-distillation if adopted); helpers `tests/helpers/hygiene-355.cjs`, `theo-replay-355.cjs`, `fixture-room-355.cjs`; aggregator `tests/run-all-355.sh`
- Records: `355-BASELINE.md`, `355-PHRASE-CONFIRMATION.md`, `355-JEV-MEASUREMENT.md`, `355-VERIFICATION.md` (hit-rate section), labeling session files, `docs/2026-09-<DD>-HANDOFF-theo-phase-355-results.md`

<verification>
- `bash tests/run-all-355.sh` and `node lib/memory/run-feynman-tests.cjs` exit 0; ten HIPS rows registered; handoff and room filing exist
</verification>

<success_criteria>
- SPEC AC13 holds; every HIPS ID is closed with proof or left open with a reason; Theo and the rethinking room have the phase's evidence
</success_criteria>

<output>
Create `.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-27-SUMMARY.md` when done.
</output>
