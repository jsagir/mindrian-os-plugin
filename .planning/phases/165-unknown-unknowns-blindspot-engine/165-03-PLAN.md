---
phase: 165-unknown-unknowns-blindspot-engine
plan: 03
type: execute
wave: 2
depends_on: ["165-01"]
files_modified:
  - lib/core/unknowns/corpus-adapter.cjs
autonomous: true
requirements: [D-165-01, D-165-03, D-165-04, D-165-10]
must_haves:
  truths:
    - "findConfidentClaims reads the LOCAL room.db and returns the UNION over EvidenceClaim/claim/CausalClaim WHERE review_status=confirmed AND evidence_tier IN (Academic,Operational)"
    - "ungraded-confirmed, proposed, None-tier, and Practitioner-tier claims are excluded by construction (the graded-confirmed reading)"
    - "the corpus rows are recast into the shared instance-feature schema (the recast)"
    - "the proxy oracle labels every probed instance by the weighted blend of contradiction density + tier/confidence mismatch + staleness, thresholded"
    - "the proxy reads only LOCAL room.db scalars; zero Brain egress; deterministic via the injected now seam"
  artifacts:
    - path: "lib/core/unknowns/corpus-adapter.cjs"
      provides: "findConfidentClaims (the recast search-space) + the proxy oracle"
      exports: ["findConfidentClaims", "proxyOracle"]
  key_links:
    - from: "lib/core/unknowns/corpus-adapter.cjs"
      to: "room.db nodes/edges"
      via: "json_extract(properties,'$.evidence_tier') UNION query (insights.cjs reader idiom)"
      pattern: "evidence_tier"
    - from: "lib/core/unknowns/corpus-adapter.cjs"
      to: "lib/core/unknowns/iface.cjs"
      via: "INSTANCE_FEATURES + TIER_FLOOR + DEFAULT_CONFIG.proxy import"
      pattern: "require.*unknowns/iface"
---

<objective>
Build the recast: the corpus-adapter that reads the confident-claim corpus out of the LOCAL room.db (the load-bearing D-165-03 UNION, NOT a naive writeClaimNode query), recasts rows into the shared instance-feature schema, and owns the proxy oracle (the 3 LOCAL scalars, weighted and thresholded). Turns the corpus-adapter and proxy-oracle test stubs GREEN.

Purpose: D-165-01 (proxy oracle) + D-165-03 (the graded-confirmed corpus) + D-165-04 (room-local) + D-165-10 (LOCAL-only, zero Brain). This is the genuinely net-new recast code the engine is built around.
Output: lib/core/unknowns/corpus-adapter.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-unknown-unknowns-blindspot-engine/165-RESEARCH.md
@lib/core/unknowns/iface.cjs
@lib/core/navigation/insights.cjs
</context>

<rules>
- THE CORPUS (D-165-03), load-bearing: the corpus is the UNION over EvidenceClaim/claim/CausalClaim WHERE review_status='confirmed' AND json_extract(properties,'$.evidence_tier') IN ('Academic','Operational') -- the insights.cjs graded-confirmed reader idiom. writeClaimNode writes knowledge_type + hardcoded confidence:1.0, NOT evidence_tier; the tier lives in properties JSON on EvidenceClaim / graded claims. Do NOT write a naive writeClaimNode query -- it would return an empty corpus and the bandit would hunt nothing.
- Clone the insights.cjs reader idiom (findStaleClaims:146 / findUnsupportedClaims:68 / the json_extract(properties,'$.field') pattern at :226-232). Use the opts.now seam for the staleness math; NO raw Date.now in the math path. D-165-09.
- The 3 proxy scalars (section 2.1): (a) contradiction density = incident CONTRADICTS|INVALIDATES edges / (1 + total incident edges); (b) tier mismatch = max(0, confidence - TIER_FLOOR[evidence_tier]); (c) staleness = clamp01((now - last_modified_at)/STALE_WINDOW_MS). Weights w_contra 0.5 / w_mismatch 0.3 / w_stale 0.2; proxyThreshold 0.5; proxyCost 0.0 (proxy tier). All from DEFAULT_CONFIG.proxy in iface.
- Part-8 LOCAL only (D-165-10): zero Brain require; the proxy reads only room.db scalars. Room-local v1 (D-165-04): scan the active room.db only; NO cross-room rollup (rollupSubRooms is the deferred OPT-IN hook, not a v1 default).
- All room.db reads route through the navigation chokepoint / its insights readers; never a raw room.db open outside the navigation allow-list (the substrate guard rejects it).
- The proxy LABELS only; it returns {trueLabel, cost}. It does NOT write findings (the orchestrator/finding-writer in plan 165-04 writes proposed nodes). The engine HALTS at the gate.
- NO em-dashes anywhere (hyphens only).
</rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: findConfidentClaims (the recast search-space)</name>
  <read_first>165-RESEARCH.md section 3 (the EXACT UNION query: SELECT n.id,n.type,n.confidence,n.source_path,n.last_modified_at, json_extract(properties,'$.evidence_tier'), json_extract(properties,'$.knowledge_type') FROM nodes WHERE type IN (EvidenceClaim,claim,CausalClaim) AND review_status='confirmed' AND evidence_tier IN (Academic,Operational); + section 3.2 the return shape + deriving section/domain from source_path + domain edges as enum handles); lib/core/navigation/insights.cjs (the json_extract reader idiom + how readers open the db); lib/core/navigation/typed-claim.cjs + evidence-claim.cjs (where evidence_tier actually lives); lib/core/unknowns/iface.cjs (INSTANCE_FEATURES)</read_first>
  <behavior>
    - findConfidentClaims(db, opts) returns rows for EXACTLY the graded-confirmed Academic/Operational claims in the fixture; the None/Practitioner/proposed/ungraded-confirmed rows are absent.
    - each returned instance carries the INSTANCE_FEATURES schema: claimId, section, domain, evidenceTier, confidence, governingHash, ageDays (section/domain derived from source_path + taxonomy edges as enum handles).
    - on a room with confirmed claims but no graded tier, the corpus is empty (the graded-confirmed reading, Pitfall 1) -- not an error.
  </behavior>
  <action>Add findConfidentClaims(db, opts) to lib/core/unknowns/corpus-adapter.cjs running the section-3 UNION query through the navigation/insights reader path (clone the json_extract idiom; never raw INSERT/INSERT-bypass). Map each row into the INSTANCE_FEATURES schema: confidence from the column, evidenceTier from json_extract, section/domain derived from source_path plus any PART_OF/TAGGED_WITH domain edges (enum handles only, Part 8), governingHash + ageDays from last_modified_at via the opts.now seam. Return the recast instance array. Room-local only; no rollupSubRooms. Document the corpus as "graded-confirmed" in the header so a reader understands why ungraded-confirmed claims are excluded.</action>
  <verify>
    <automated>node tests/test-unknowns-corpus-adapter.cjs</automated>
  </verify>
  <done>corpus-adapter stub GREEN: findConfidentClaims returns exactly the fixture's graded-confirmed Academic/Operational set as INSTANCE_FEATURES instances; excludes None/Practitioner/proposed/ungraded; room-local.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The proxy oracle (the 3 LOCAL scalars)</name>
  <read_first>165-RESEARCH.md section 2.1 (the 3 scalars with exact SQL sources: contradiction density over incident CONTRADICTS|INVALIDATES edges; tier mismatch = confidence - TIER_FLOOR(evidence_tier); staleness = (now - last_modified_at)/STALE_WINDOW via the findStaleClaims window + opts.now seam) + section 2.2 (weights 0.5/0.3/0.2, threshold 0.5, proxyCost 0.0) + the Code Examples proxyOracle sketch; lib/core/navigation/edges.cjs (CONTRADICTS:153, INVALIDATES:405 are frozen members); lib/core/navigation/insights.cjs (the edge-count SELECT idiom + the findStaleClaims window/opts.now seam); lib/core/unknowns/iface.cjs (TIER_FLOOR, DEFAULT_CONFIG.proxy)</read_first>
  <behavior>
    - proxyOracle(db, claimId, cfg, nowFn) returns {trueLabel, cost}: trueLabel = (w_contra*contradictionDensity + w_mismatch*tierMismatch + w_stale*staleness >= proxyThreshold) ? 1 : 0; cost = proxyCost (0.0 for the proxy tier).
    - the fixture's contradicted claim and the stale claim score >= threshold (labeled 1); a clean, fresh, well-tiered claim scores 0.
    - the staleness scalar is deterministic under the injected nowFn (no raw Date.now).
    - contradiction density counts only CONTRADICTS|INVALIDATES incident edges, normalized by total incident edges.
  </behavior>
  <action>Add proxyOracle(db, claimId, cfg, nowFn) plus the three scalar readers (readContradictionDensity, readTierMismatch, readStaleness) to corpus-adapter.cjs. contradictionDensity: count incident edges where edge_type IN (CONTRADICTS, INVALIDATES) on claimId, over (1 + total incident edges), via the insights.cjs edge SELECT idiom. tierMismatch: max(0, confidence - TIER_FLOOR[evidenceTier]) from iface. staleness: clamp01((nowFn() - last_modified_at)/(staleWindowDays*86400000)) using the DEFAULT_CONFIG.proxy.staleWindowDays window and the opts.now seam. Blend with cfg weights (default from DEFAULT_CONFIG.proxy), threshold at cfg.proxyThreshold, return {trueLabel, cost: cfg.proxyCost}. Zero Brain require; all reads LOCAL room.db; deterministic via nowFn.</action>
  <verify>
    <automated>node tests/test-unknowns-proxy-oracle.cjs</automated>
  </verify>
  <done>proxy-oracle stub GREEN: the 3-scalar weighted blend labels the contradicted/stale fixture claim 1 and a clean claim 0; deterministic via the injected now; zero Brain egress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus-adapter -> room.db | the adapter is a LOCAL reader; it must route through the navigation/insights read path, never raw SQL, never Brain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-06 | Information Disclosure | corpus-adapter reads claim props | mitigate | LOCAL room.db reads only; zero Brain require; the Part-8 sweep (plan 165-06) greps the module for forbidden Brain substrings |
| T-165-07 | Tampering | corpus query correctness | mitigate | the corpus test asserts the UNION filter returns the graded-confirmed set and excludes the others; the load-bearing reconciliation is encoded as a test |
| T-165-08 | Elevation of Privilege | proxy auto-labeling | mitigate | the proxy returns a label only; it never writes a confirmed node; finding-writes (plan 165-04) land proposed; only confirmNode (human) promotes |
</threat_model>

<verification>
- node tests/test-unknowns-corpus-adapter.cjs and test-unknowns-proxy-oracle.cjs GREEN.
- grep for a Brain require (brain-client / mcp__brain) over corpus-adapter.cjs is 0.
- grep for a raw "INSERT INTO" or direct room.db open outside the navigation path over corpus-adapter.cjs is 0.
</verification>

<success_criteria>
- findConfidentClaims implements the load-bearing graded-confirmed UNION and recasts to INSTANCE_FEATURES.
- The proxy oracle blends the 3 LOCAL scalars with the locked default weights/threshold and is deterministic.
- Room-local, zero Brain egress.
</success_criteria>

<output>
Create `.planning/phases/165-unknown-unknowns-blindspot-engine/165-03-SUMMARY.md` when done.
</output>
