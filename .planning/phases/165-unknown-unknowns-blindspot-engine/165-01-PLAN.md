---
phase: 165-unknown-unknowns-blindspot-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/unknowns/iface.cjs
  - tests/run-all-165.sh
  - tests/fixtures/unknowns/build-fixture-room.cjs
  - tests/test-unknowns-corpus-adapter.cjs
  - tests/test-unknowns-dsp.cjs
  - tests/test-unknowns-dsp-goodness.cjs
  - tests/test-unknowns-bandit.cjs
  - tests/test-unknowns-resume.cjs
  - tests/test-unknowns-proxy-oracle.cjs
  - tests/test-unknowns-frozen-edges.cjs
  - tests/test-unknowns-part8-boundary.cjs
  - tests/test-unknowns-rank-in.cjs
  - tests/test-unknowns-verdict.cjs
autonomous: true
requirements: [D-165-02, D-165-03, D-165-09, D-165-10]
must_haves:
  truths:
    - "The single shared instance-feature schema (the recast IFACE) exists and is the one contract every downstream engine module imports"
    - "A fixture room.db seeds confirmed/proposed claims across Academic/Operational/Practitioner/None tiers so the corpus filter is testable"
    - "Every Wave-0 test file exists as a RED stub that names the behavior it will assert and fails until its module ships"
    - "run-all-165.sh runs every test-unknowns-*.cjs, prints a per-suite PASS/FAIL tally, exits 1 on any failure, and runs an em-dash sweep"
  artifacts:
    - path: "lib/core/unknowns/iface.cjs"
      provides: "The shared instance/partition/checkpoint/config IFACE block cited by all downstream plans"
      contains: "INSTANCE_FEATURES"
    - path: "tests/run-all-165.sh"
      provides: "The one-command phase gate (clone of run-all-164.sh)"
    - path: "tests/fixtures/unknowns/build-fixture-room.cjs"
      provides: "A seeded room.db with graded-confirmed + proposed + None/Practitioner claims"
  key_links:
    - from: "tests/run-all-165.sh"
      to: "tests/test-unknowns-*.cjs"
      via: "CJS_SUITES array enumeration"
      pattern: "test-unknowns-"
---

<objective>
Lay the harness-as-code Wave-0 foundation: the ONE shared IFACE block, the seeded fixture room.db, the full set of RED test stubs (contracts-on-disk), and the run-all-165.sh phase gate. Nothing in this plan implements engine logic; it freezes the contracts every later wave builds against and the test surface every later wave turns green.

Purpose: harness property 3 (contracts-on-disk) + property 5 (one shared IFACE) + property 2 (the phase gate that later waves must keep green). The IFACE is the recast made concrete so no downstream executor invents its own instance shape.
Output: lib/core/unknowns/iface.cjs, tests/run-all-165.sh, the fixture builder, and 11 RED test stubs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-unknown-unknowns-blindspot-engine/165-CONTEXT.md
@.planning/phases/165-unknown-unknowns-blindspot-engine/165-RESEARCH.md
@.planning/phases/165-unknown-unknowns-blindspot-engine/165-VALIDATION.md
@.planning/research/reference/unknown-unknowns-horvitz/README.md
</context>

<rules>
- THE CORPUS (D-165-03): the confident-claim corpus is a UNION over EvidenceClaim/claim/CausalClaim WHERE review_status='confirmed' AND json_extract(properties,'$.evidence_tier') IN ('Academic','Operational'). The fixture MUST seed rows that pass AND rows that fail this filter (proposed, None, Practitioner, ungraded-confirmed) so the corpus test has both sides.
- NO Math.random anywhere; NO unseeded Date.now in any math path (caller-injected opts.now seam only). D-165-09.
- Frozen edges, remap-only (D-165-08): only INVALIDATES / ROOT_CAUSES / ENABLES / FEEDS_INTO. No new edge type, no canon amendment this phase.
- Part-8 LOCAL only (D-165-10): the fixture and IFACE carry enum/scalar handles only, never venture prose; zero Brain require.
- Clone, do not fork: run-all-165.sh clones tests/run-all-164.sh; the fixture clones the shipped navigation.writeClaimNode / writeEvidenceClaim write path, never raw INSERT INTO.
- The engine HALTS at the F.1 gate; proxy writes proposed only; only a human confirms.
- NO em-dashes anywhere (hyphens only); the run-all sweep enforces this on the phase artifacts.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: Define the shared IFACE block (the recast contract)</name>
  <read_first>.planning/research/reference/unknown-unknowns-horvitz/README.md (class roster + Eq.2 goodness); 165-RESEARCH.md section 1.4 (the instance := { claimId, section, domain, evidenceTier, confidence, governingHash, ageDays } schema + the NUMERIC vs CATEGORICAL feature split); section 1.6 (the checkpoint shape); section 1.1 reference config defaults</read_first>
  <files>lib/core/unknowns/iface.cjs</files>
  <action>Create lib/core/unknowns/iface.cjs as a pure constants/contract module (zero I/O, zero require of navigation or Brain). Export: (1) INSTANCE_FEATURES, the frozen ordered feature schema from 165-RESEARCH section 1.4 -- claimId, section, domain, evidenceTier, confidence, governingHash, ageDays -- each tagged NUMERIC or CATEGORICAL (confidence/ageDays/evidenceTier-mapped numeric; section/domain categorical); (2) TIER_NUMERIC, the Academic=4/Operational=3/Practitioner=2/None=1 map used by the numeric feature distance, AND TIER_FLOOR, the Academic=1.0/Operational=0.75/Practitioner=0.4/None=0.1 map used by the proxy tier-mismatch scalar (these are two distinct maps per section 1.4 vs section 2.1b; name them so downstream modules do not conflate them); (3) DEFAULT_CONFIG, the reference defaults reconciled with the LOCKED discretion: confidenceThreshold 0.65, budget 0.2, gamma 0.2, dspWeights {l1:1,l2:1,l3:1,l4:1,l5:0.1}, minSupport 0.1, maxPatternLength 3, plus the proxy block w_contra 0.5 / w_mismatch 0.3 / w_stale 0.2 / proxyThreshold 0.5 / proxyCost 0.0 / humanConfirmBudget 3 / staleWindowDays 30; (4) CHECKPOINT_SHAPE, a documented JSDoc-described shape for the per-pull resumable checkpoint (scanId, corpusHash, pull, budget, arms[], cumulativeUtility, done) per section 1.6; (5) FROZEN_ENGINE_EDGES = Object.freeze({ INVALIDATES, ROOT_CAUSES, ENABLES, FEEDS_INTO }) mirroring issue-tree.cjs:49. Document at the top that this is the single recast contract every unknowns module imports. NO fenced implementation beyond the constant objects. Add a module-load self-check (clone issue-tree.cjs:67) that requires ./navigation/edges.cjs ALLOWED_EDGE_TYPES via the navigation path used elsewhere -- but since iface is pure, instead re-export FROZEN_ENGINE_EDGES and leave the live frozen-set assertion to the engine modules that already require navigation; document this delegation in a comment so the frozen-edges test knows where the assertion lives.</action>
  <verify>
    <automated>node -e "const i=require('./lib/core/unknowns/iface.cjs'); if(!i.INSTANCE_FEATURES||!i.TIER_FLOOR||!i.DEFAULT_CONFIG||!i.FROZEN_ENGINE_EDGES) process.exit(1); console.log('iface ok')"</automated>
  </verify>
  <done>iface.cjs loads, exports INSTANCE_FEATURES / TIER_NUMERIC / TIER_FLOOR / DEFAULT_CONFIG / CHECKPOINT_SHAPE / FROZEN_ENGINE_EDGES; no Brain require; no Math.random.</done>
</task>

<task type="auto">
  <name>Task 2: Build the seeded fixture room.db</name>
  <read_first>lib/core/navigation.cjs (writeClaimNode:177, writeEvidenceClaim, writeEdge:114 -- the chokepoint write path); lib/core/navigation/typed-claim.cjs (writeClaimNode shape: knowledge_type, confidence=1.0, NO evidence_tier); lib/core/navigation/evidence-claim.cjs (EVIDENCE_TIERS:48, writeEvidenceClaim sets evidence_tier in properties); 165-RESEARCH.md section 3.1 (the corpus UNION query the fixture must satisfy); how existing tests open a temp room.db (grep an existing tests/test-navigation-*.cjs for the openRoomDb / tmp room pattern)</read_first>
  <files>tests/fixtures/unknowns/build-fixture-room.cjs</files>
  <action>Create tests/fixtures/unknowns/build-fixture-room.cjs exporting buildFixtureRoom(roomDir) that creates a temp room.db via the SHIPPED navigation chokepoint write path (require navigation.cjs; NEVER raw INSERT INTO -- the substrate guard forbids it). Seed a deliberate mix: (a) at least 5 EvidenceClaim/CausalClaim nodes review_status confirmed with evidence_tier Academic or Operational (these ARE the corpus); (b) confirmed claims with evidence_tier None and Practitioner (excluded by the filter); (c) confirmed plain writeClaimNode claims with NO evidence_tier (ungraded-confirmed -- excluded, per Pitfall 1); (d) proposed claims (excluded by status); (e) seed at least one CONTRADICTS or INVALIDATES edge incident on a corpus claim (so the proxy contradiction-density scalar has signal) and at least one corpus claim with an old last_modified_at (so the staleness scalar has signal). Vary section/domain enum handles across the corpus claims so DSP partitioning has feature variance and the inter-partition distance has something to discriminate. Accept an opts.now-style fixed timestamp for the stale rows so the staleness test is deterministic. Return { dbPath, corpusIds, nonCorpusIds, contradictedId, staleId }. Keep all node text as generic enum-like handles (Part 8), never realistic venture prose.</action>
  <verify>
    <automated>node -e "const os=require('os'),fs=require('fs'),path=require('path');const d=fs.mkdtempSync(path.join(os.tmpdir(),'uufix-'));const {buildFixtureRoom}=require('./tests/fixtures/unknowns/build-fixture-room.cjs');const r=buildFixtureRoom(d);if(!r.dbPath||!Array.isArray(r.corpusIds)||r.corpusIds.length<5)process.exit(1);console.log('fixture ok',r.corpusIds.length)"</automated>
  </verify>
  <done>buildFixtureRoom returns a real room.db path with >=5 graded-confirmed corpus claims plus the excluded mix; writes route through navigation.cjs; deterministic stale timestamps via injected now.</done>
</task>

<task type="auto">
  <name>Task 3: Write the RED test stubs + run-all-165.sh phase gate</name>
  <read_first>tests/run-all-164.sh (the aggregator to clone: SCRIPT_DIR/REPO_ROOT setup, CJS_SUITES array, per-suite PASS/FAIL tally, exit 1 on failure, the U+2014 em-dash sweep written via codepoint escape); 165-VALIDATION.md (the per-req test map -- each stub's required assertion); 165-RESEARCH.md Validation Architecture (the Wave-0 gaps list)</read_first>
  <files>tests/test-unknowns-corpus-adapter.cjs, tests/test-unknowns-dsp.cjs, tests/test-unknowns-dsp-goodness.cjs, tests/test-unknowns-bandit.cjs, tests/test-unknowns-resume.cjs, tests/test-unknowns-proxy-oracle.cjs, tests/test-unknowns-frozen-edges.cjs, tests/test-unknowns-part8-boundary.cjs, tests/test-unknowns-rank-in.cjs, tests/test-unknowns-verdict.cjs, tests/run-all-165.sh</files>
  <action>Create each test-unknowns-*.cjs as a RED stub: a plain Node CJS assert file that REQUIRES the module it will test (which does not exist yet) inside a try/catch, and on require-failure prints "RED: <module> not yet implemented (plan NN)" and process.exit(1). Each stub must carry, as a header comment, the EXACT assertion it will make once GREEN, sourced from 165-VALIDATION.md: corpus-adapter asserts the UNION filter returns exactly the graded-confirmed Academic/Operational set from the fixture (not the None/Practitioner/proposed/ungraded rows); dsp asserts partition() returns partitions each carrying {pattern, instances, centroid, meanConfidence}; dsp-goodness asserts interPartitionFeatureDistance + interPartitionConfidenceDistance discriminate (two differently-centroided partitions yield distinct, NON-1.0 distances) AND a lone partition returns 0.0 (the stub-leak regression); bandit asserts selectNextInstance produces pull records and respects budget=floor(N*budget); resume asserts an interrupted-then-resumed scan is byte-identical (same corpusHash, same pull order, same findings); proxy-oracle asserts the 3-scalar blend with weights 0.5/0.3/0.2 and threshold 0.5 labels the contradicted/stale fixture claim isUnknownUnknown=1 and a clean claim 0; frozen-edges asserts only INVALIDATES/ROOT_CAUSES/ENABLES/FEEDS_INTO are emittable and the module-load self-check throws on a drifted type; part8-boundary is the forbidden-substring sweep over lib/core/unknowns/*.cjs (no Brain require, no raw INSERT INTO, no Math.random); rank-in asserts engine output becomes a candidate the f-selector-ranker scores into the F.1 set; verdict asserts the adversarial {passed, findings[]} structured verdict shape. Then create tests/run-all-165.sh by cloning run-all-164.sh: set CJS_SUITES to the 10 test-unknowns-*.cjs files in wave order, keep the per-suite tally + exit-1-on-fail + the em-dash sweep over the 165 phase artifacts (lib/core/unknowns/*.cjs + tests/test-unknowns-*.cjs), written so the script carries no literal em-dash itself (U+2014 codepoint escape). chmod the script executable. At Wave-0 close the suite is intentionally RED (every stub exits 1); later waves turn each green.</action>
  <verify>
    <automated>bash -n tests/run-all-165.sh && node tests/test-unknowns-corpus-adapter.cjs; test $? -eq 1 && echo "RED stub correctly fails"</automated>
  </verify>
  <done>All 10 stubs exist and exit 1 with a RED message; run-all-165.sh parses (bash -n clean), enumerates every stub, and carries the em-dash sweep; the script is executable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture -> room.db | the fixture is the only writer here; it must route through navigation.cjs, never raw SQL |
| iface module -> downstream engine | the IFACE is a pure contract; a leak of venture prose into it would breach Part 8 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-01 | Tampering | fixture room.db writes | mitigate | fixture uses navigation.writeClaimNode/writeEvidenceClaim; the substrate guard (scripts/check-substrate.cjs) rejects a raw room.db open outside the navigation allow-list |
| T-165-02 | Information Disclosure | iface.cjs / fixture node text | mitigate | enum/scalar handles only; the Wave-0 part8-boundary stub will sweep lib/core/unknowns/* for forbidden substrings once modules land |
| T-165-SC | Tampering | npm installs | accept | zero new packages this phase (pure CJS clone of shipped siblings); no install task, no slopcheck target |
</threat_model>

<verification>
- bash tests/run-all-165.sh runs to completion and reports every stub RED (expected at Wave-0 close).
- node loads lib/core/unknowns/iface.cjs without a Brain require.
- The fixture builder produces a room.db whose corpus UNION returns >=5 graded-confirmed claims.
</verification>

<success_criteria>
- The shared IFACE exists and is the single recast contract (INSTANCE_FEATURES, TIER maps, DEFAULT_CONFIG, CHECKPOINT_SHAPE, FROZEN_ENGINE_EDGES).
- The fixture seeds both sides of the D-165-03 filter.
- All 10 RED stubs exist with their exact GREEN assertion documented; run-all-165.sh is the phase gate.
</success_criteria>

<output>
Create `.planning/phases/165-unknown-unknowns-blindspot-engine/165-01-SUMMARY.md` when done.
</output>
