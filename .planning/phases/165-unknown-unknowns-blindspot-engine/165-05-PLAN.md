---
phase: 165-unknown-unknowns-blindspot-engine
plan: 05
type: execute
wave: 3
depends_on: ["165-01"]
files_modified:
  - commands/file-meeting.md
  - commands/map-unknowns.md
  - data/connector-registry.json
autonomous: true
requirements: [D-165-05, D-165-06, D-165-07]
must_haves:
  truths:
    - "commands/file-meeting.md carries a connector block riding the EXISTING contradiction reach (no 7th reach), with sensor_triggers [SENS-06, SENS-08]"
    - "commands/map-unknowns.md sensor_triggers is upgraded from [] to [SENS-06, SENS-08] so the engine auto-fires at the next F.1 gate"
    - "data/connector-registry.json is REGENERATED (not hand-edited) and the --check tripwire is clean"
    - "no new reach_id is minted and no new edge type is introduced"
  artifacts:
    - path: "commands/file-meeting.md"
      provides: "the file-meeting connector block (closes the orphan)"
      contains: "connector:"
    - path: "data/connector-registry.json"
      provides: "the regenerated registry carrying file-meeting + the upgraded map-unknowns triggers"
  key_links:
    - from: "commands/file-meeting.md"
      to: "the contradiction reach + SENS-06/SENS-08"
      via: "connector frontmatter (rides an existing reach)"
      pattern: "reach_id: contradiction"
    - from: "data/connector-registry.json"
      to: "the connector frontmatter on file-meeting + map-unknowns"
      via: "scripts/build-connector-registry.cjs regen"
      pattern: "file-meeting"
---

<objective>
Wire the trigger spine: close the /mos:file-meeting orphan by adding a connector block riding the EXISTING contradiction reach, upgrade /mos:map-unknowns sensor_triggers so the engine auto-fires, and regenerate the connector registry. This is the "meetings are the oracle" wiring (harness Surfaces wave). The filing event dispatches the blind-spot trigger sensors; the engine surfaces at the next F.1 gate, freshness-gated.

Purpose: D-165-05 (SENS-06 + SENS-08 trigger conditions) + D-165-06 (auto-fire at the F.1 gate) + D-165-07 (wire file-meeting onto the connector spine).
Output: connector blocks on file-meeting + map-unknowns, regenerated data/connector-registry.json.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-unknown-unknowns-blindspot-engine/165-RESEARCH.md
@docs/CONNECTOR-CONTRACT.md
@commands/reanalyze.md
@commands/map-unknowns.md
</context>

<rules>
- file-meeting orphan (D-165-07): add a connector block riding an EXISTING reach (the contradiction reach), sensor_triggers [SENS-06, SENS-08] -- NOT a 7th reach. The frozen reach bank is 6 (context_block / contradiction / cross_room / brain_consult / deep_research / hats). NO new reach_id.
- The registry is GENERATED: edit ONLY the connector: frontmatter on the two command files, then regen data/connector-registry.json via scripts/build-connector-registry.cjs. NEVER hand-edit the JSON. The --check tripwire must be clean after regen.
- Frozen edges, remap-only (D-165-08): this plan touches no edge vocabulary; it is connector frontmatter + registry only.
- The connector block conforms to docs/CONNECTOR-CONTRACT.md schema (connects_to_spine, sensor_triggers, reach_id, sub_mode, framework, posture, hierarchy_rank, filing, plan_gated, web_scope, surface). framework null / web_scope null for file-meeting (not a single-framework methodology, no web).
- map-unknowns keeps its existing reach_id context_block / sub_mode unknowns-matrix / framework "Knowns and Unknowns Matrix Framework"; ONLY sensor_triggers changes [] -> [SENS-06, SENS-08].
- Part-8 LOCAL: the connector carries generic enum/handle fields only (Part 8 is structurally safe per the connector contract section 5); no user content.
- The engine HALTS at the F.1 gate (auto-fire surfaces a probe offer, never an auto-confirm).
- NO em-dashes anywhere (hyphens only) in the YAML or any added prose.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: Add the file-meeting connector block + upgrade map-unknowns triggers</name>
  <read_first>commands/file-meeting.md (confirm NO connector block exists -- the orphan); commands/reanalyze.md (the SENS-06/contradiction/reanalyze precedent the file-meeting block shares the lane with: reach_id contradiction, hierarchy_rank 38); commands/map-unknowns.md (the current connector with sensor_triggers []); docs/CONNECTOR-CONTRACT.md (the frontmatter schema + the worked examples); 165-RESEARCH.md section 7.2 (the EXACT file-meeting block to add: connects_to_spine true, sensor_triggers [SENS-06, SENS-08], reach_id contradiction, sub_mode file-meeting, framework null, posture pull_back, hierarchy_rank 37, filing fileEvidenceWithReadback, plan_gated false, web_scope null, surface F.1) + section 7.3 (the map-unknowns upgrade)</read_first>
  <files>commands/file-meeting.md, commands/map-unknowns.md</files>
  <action>Add the Phase-165 connector frontmatter block to commands/file-meeting.md per 165-RESEARCH section 7.2 EXACTLY: connects_to_spine true, sensor_triggers [SENS-06, SENS-08], reach_id contradiction (rides the EXISTING reach, the same lane as /mos:reanalyze -- no 7th reach), sub_mode file-meeting, framework null, posture pull_back, hierarchy_rank 37 (one tighter than reanalyze's 38), filing fileEvidenceWithReadback, plan_gated false, web_scope null, surface F.1. Add a one-line comment header naming the rationale (the meeting-filing event dispatches the blind-spot trigger sensors; rides the existing contradiction reach). Then edit commands/map-unknowns.md: change sensor_triggers from [] to [SENS-06, SENS-08]; leave reach_id context_block, sub_mode unknowns-matrix, framework, posture, hierarchy_rank, filing all UNCHANGED. Touch nothing else in either file.</action>
  <verify>
    <automated>grep -q "reach_id: contradiction" commands/file-meeting.md && grep -q "sub_mode: file-meeting" commands/file-meeting.md && grep -A2 "connector:" commands/map-unknowns.md | grep -q "SENS-06" && echo "connector frontmatter ok"</automated>
  </verify>
  <done>file-meeting.md carries the connector block riding the contradiction reach with [SENS-06, SENS-08]; map-unknowns.md sensor_triggers is [SENS-06, SENS-08]; no 7th reach; no other fields changed.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the connector registry + verify the tripwire</name>
  <read_first>scripts/build-connector-registry.cjs (the regen entry + the --check tripwire flags); data/connector-registry.json (the current generated artifact -- confirm map-unknowns/unknowns-matrix already present; file-meeting absent); 165-RESEARCH.md section 7.3 (regen after both frontmatter edits, never hand-edit)</read_first>
  <files>data/connector-registry.json</files>
  <action>Run scripts/build-connector-registry.cjs to regenerate data/connector-registry.json from the updated frontmatter (the generator walks commands/*.md). Do NOT hand-edit the JSON. After regen, run scripts/build-connector-registry.cjs --check and confirm it is clean (no STALE / UN-WIRED / UN-RANKED). Confirm the regenerated registry now carries the file-meeting connector (reach_id contradiction, sub_mode file-meeting) and the upgraded map-unknowns sensor_triggers. If --check reports the registry is stale before regen, that is expected (the frontmatter changed); the regen resolves it. Commit only the regenerated JSON, not a hand-edit.</action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs && node scripts/build-connector-registry.cjs --check && grep -q "file-meeting" data/connector-registry.json && echo "registry regen + check clean"</automated>
  </verify>
  <done>data/connector-registry.json is regenerated (not hand-edited), carries the file-meeting connector + the upgraded map-unknowns triggers, and the --check tripwire is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connector frontmatter -> registry | the generator is the only writer of the JSON; a hand-edit would drift |
| connector -> Brain | the connector contract is structurally Part-8-safe (generic handles only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-13 | Tampering (constitutional) | a 7th reach minted | mitigate | the block rides the EXISTING contradiction reach; the connector --check + the frozen REACH_IDS bank reject a non-frozen reach_id |
| T-165-14 | Tampering | a hand-edited registry | mitigate | the JSON is regenerated via the generator; --check asserts the registry matches the frontmatter |
| T-165-15 | Information Disclosure | connector carries user content | accept | per the connector contract section 5 the block carries only generic enum/handle fields; no user-content path exists |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check is clean after regen.
- data/connector-registry.json carries file-meeting (contradiction reach) + map-unknowns [SENS-06, SENS-08].
- grep confirms no 7th reach_id introduced (the file-meeting block reuses contradiction).
</verification>

<success_criteria>
- The file-meeting orphan is closed (connector block riding the existing contradiction reach).
- map-unknowns auto-fires (sensor_triggers upgraded).
- The registry is regenerated and the --check tripwire is clean; no 7th reach, no edge-vocabulary change.
</success_criteria>

<output>
Create `.planning/phases/165-unknown-unknowns-blindspot-engine/165-05-SUMMARY.md` when done.
</output>
