---
phase: 165-unknown-unknowns-blindspot-engine
plan: 02
type: execute
wave: 2
depends_on: ["165-01"]
files_modified:
  - lib/core/unknowns/pattern-miner.cjs
  - lib/core/unknowns/dsp.cjs
  - lib/core/unknowns/bandit.cjs
  - lib/core/unknowns/rumsfeld-matrix.cjs
autonomous: true
requirements: [D-165-02, D-165-08, D-165-09]
must_haves:
  truths:
    - "PatternMiner produces support-bounded descriptive patterns over claim features (quartile numeric, equality categorical, AND-combined to maxPatternLength)"
    - "DSP partitions the corpus by the goodness function with the REAL inter-partition distance (NOT the reference stub return 1.0)"
    - "interPartitionFeatureDistance and interPartitionConfidenceDistance discriminate; a lone partition returns 0.0"
    - "The UCB-with-discount bandit selects arms and instances index-deterministically (zero Math.random) and checkpoints per pull so a resume is byte-identical"
    - "The Rumsfeld 2x2 categorizes items into KK/KU/UK/UU and routes each quadrant to its frozen FEEDS_INTO downstream pipeline handles"
  artifacts:
    - path: "lib/core/unknowns/dsp.cjs"
      provides: "Algorithm 1 partition + the real interPartition{Feature,Confidence}Distance"
      contains: "interPartitionFeatureDistance"
    - path: "lib/core/unknowns/bandit.cjs"
      provides: "Algorithm 2 UCB+discount, index-deterministic, per-pull checkpoint"
      contains: "selectNextInstance"
    - path: "lib/core/unknowns/pattern-miner.cjs"
      provides: "support-bounded descriptive pattern mining"
    - path: "lib/core/unknowns/rumsfeld-matrix.cjs"
      provides: "2x2 categorize + quadrant->pipeline routing table"
  key_links:
    - from: "lib/core/unknowns/dsp.cjs"
      to: "lib/core/unknowns/iface.cjs"
      via: "INSTANCE_FEATURES + TIER_NUMERIC + DEFAULT_CONFIG import"
      pattern: "require.*unknowns/iface"
    - from: "lib/core/unknowns/rumsfeld-matrix.cjs"
      to: "FROZEN_ENGINE_EDGES (FEEDS_INTO)"
      via: "quadrant routing emits FEEDS_INTO handles"
      pattern: "FEEDS_INTO"
---

<objective>
Build the deterministic engine core: the pattern-miner, the DSP partitioner WITH the real inter-partition distance (closing the reference stub leak), the UCB-with-discount bandit (index-deterministic, per-pull checkpoint), and the Rumsfeld 2x2 router. This is harness Wave-0/Foundation math: pure, zero-I/O, deterministic, cloning the issue-tree.cjs purity model (NOT the futures async shell). Turns the dsp, dsp-goodness, bandit, and resume test stubs GREEN.

Purpose: D-165-02 (FULL DSP + UCB bandit, complete-to-spec) + D-165-09 (no Math.random, resumable) + the interPartitionDistance discretion deliverable.
Output: lib/core/unknowns/{pattern-miner,dsp,bandit,rumsfeld-matrix}.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-unknown-unknowns-blindspot-engine/165-RESEARCH.md
@.planning/research/reference/unknown-unknowns-horvitz/README.md
@lib/core/unknowns/iface.cjs
</context>

<rules>
- NO Math.random anywhere; NO unseeded Date.now in any math path. D-165-09. Arm order = stable deterministic (HSI/priority desc, tie-break ascending partition id); within-arm instance pick = ascending claimId cursor into a sorted array. No reservoir sampling, no shuffling.
- The interPartition*Distance MUST be the REAL computation (section 1.4), NOT the reference stub return 1.0. A lone partition returns 0.0, NOT 1.0.
- Clone the issue-tree.cjs deterministic single-build purity model (no async, no I/O, no Brain). Do NOT clone the 56KB futures orchestrator async shell.
- Import the shared IFACE from lib/core/unknowns/iface.cjs (plan 165-01); do not redefine INSTANCE_FEATURES, the tier maps, or DEFAULT_CONFIG.
- Frozen edges, remap-only (D-165-08): rumsfeld-matrix emits ONLY frozen FEEDS_INTO handles for chain routing; no new edge type.
- Part-8 LOCAL only (D-165-10): zero Brain require in any of these four modules; pure math over the instance set passed in.
- The bandit checkpoint key carries NO Date.now (deterministic scanId over roomDir+corpusHash).
- NO em-dashes anywhere (hyphens only).
</rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PatternMiner + DSP with the REAL inter-partition distance</name>
  <read_first>.planning/research/reference/unknown-unknowns-horvitz/README.md (PatternMiner contract, DSP Algorithm 1, the goodness Eq.2 g1..g5); 165-RESEARCH.md section 1.4 (the EXACT interPartitionFeatureDistance spec -- normalized-numeric |a-b|/range over corpus min-max + Hamming categorical, mean across feature dims then mean across siblings; interPartitionConfidenceDistance = mean |meanConf_i - meanConf_j|; lone-partition 0.0; stable id-sorted sibling iteration for fixed FP summation order); lib/core/issue-tree.cjs (the pure-build determinism doctrine to mirror); lib/core/unknowns/iface.cjs (INSTANCE_FEATURES, TIER_NUMERIC, DEFAULT_CONFIG.dspWeights)</read_first>
  <behavior>
    - minePatterns(instances, features) returns patterns each shaped {conditions:[{feature,operator,value}]}; numeric features yield quartile <=/>= conditions, categorical yield = conditions, AND-combined up to maxPatternLength; only patterns above minSupport are kept.
    - partition(instances, confidenceScores, patterns) returns partitions each {pattern, instances, centroid, meanConfidence}; greedy set-cover picks the pattern maximizing coveredUncovered.size/goodness until the space is covered.
    - interPartitionFeatureDistance(P_i, partitions, corpusRanges) returns the mean pairwise centroid distance to siblings; two well-separated partitions yield a distinct NON-1.0 value; a lone partition returns 0.0.
    - interPartitionConfidenceDistance(P_i, partitions) returns mean |meanConf_i - meanConf_j|; lone partition 0.0.
    - goodness uses l1*g1 - l2*g2 + l3*g3 - l4*g4 + l5*g5 with the real g2/g4 (NOT 1.0), so the same partition set scored with the real distance differs from the stubbed score.
  </behavior>
  <action>Create lib/core/unknowns/pattern-miner.cjs implementing the PatternMiner contract (minePatterns over the INSTANCE_FEATURES schema; quartile bucketing for numeric, equality for categorical; AND-combine to maxPatternLength; support-bounded by minSupport). Create lib/core/unknowns/dsp.cjs implementing DSP Algorithm 1 partition() PLUS the REAL interPartitionFeatureDistance and interPartitionConfidenceDistance per 165-RESEARCH section 1.4: compute corpusRanges (per-numeric-feature max-min) ONCE over the corpus so the metric is scale-free and deterministic; numeric per-feature distance = |a-b|/range (range 0 yields 0), categorical = Hamming mismatch; aggregate as unweighted mean across feature dims then mean across stable-id-sorted siblings; lone partition returns 0.0. Implement the goodness function exactly as Eq.2 with the real g2/g4 wired in (use DEFAULT_CONFIG.dspWeights). All pure, deterministic, no Math.random, no Date.now, no Brain require. Mirror the issue-tree.cjs module-doc determinism doctrine in the header comment.</action>
  <verify>
    <automated>node tests/test-unknowns-dsp.cjs && node tests/test-unknowns-dsp-goodness.cjs</automated>
  </verify>
  <done>dsp + dsp-goodness stubs GREEN: partition returns the {pattern,instances,centroid,meanConfidence} shape; the real inter-partition distances discriminate (NON-1.0) and a lone partition is 0.0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The UCB-with-discount bandit (index-deterministic, resumable)</name>
  <read_first>.planning/research/reference/unknown-unknowns-horvitz/README.md (BanditForUnknownUnknowns Algorithm 2: initialize/selectNextInstance, Eq.1 utility, Eq.3 discount currentArmSize/sizeAtPull, the UCB bound sqrt(2*ln(sumEffCounts)/N_t), first-K try-each-arm-once); 165-RESEARCH.md section 1.5 (index-deterministic arm order = priority desc tie-break partition-id asc; within-arm instance pick = ascending claimId cursor) + section 1.6 (the per-pull checkpoint shape: scanId, corpusHash, pull, budget, arms[], cumulativeUtility, done; resume rule: matching corpusHash continues from pull, mismatch starts fresh); lib/core/unknowns/iface.cjs (CHECKPOINT_SHAPE, DEFAULT_CONFIG gamma/budget)</read_first>
  <behavior>
    - initialize(partitions) seeds one arm per partition with effectiveCount/discountedMean/currentArmSize/sizeAtPull and a sorted unprobed-instance list (ascending claimId).
    - selectNextInstance(oracleFn, budget) runs first-K try-each-arm-once in stable arm order, then UCB-with-discount selection; returns pull records {time,arm,instance,utility,isUnknownUnknown,cumulativeUtility}.
    - utility = (isUnknownUnknown?1:0) - gamma*cost (Eq.1); discount = currentArmSize/sizeAtPull (Eq.3).
    - a scan run, interrupted at pull P (checkpoint emitted), then resumed from that checkpoint produces a byte-identical full record sequence to an uninterrupted run (zero Math.random; matching corpusHash continues).
    - a checkpoint whose corpusHash differs from the current corpus starts a fresh scan.
  </behavior>
  <action>Create lib/core/unknowns/bandit.cjs implementing Algorithm 2 with index-deterministic arm + instance selection (section 1.5) and a per-pull resumable checkpoint (section 1.6). The arm-priority input is an injected per-partition scalar (default: arm priority from a caller-supplied priorityFn or, absent it, descending mean intra-partition proxy-readiness; the orchestrator in plan 165-04 supplies the real priority -- here accept it as an arg so HSI-or-LOCAL-fallback is the orchestrator's choice, NOT hardcoded in the bandit). Emit a checkpoint object (the CHECKPOINT_SHAPE) the caller persists; expose a resumeFrom(checkpoint, ...) entry that validates corpusHash and either continues from pull or signals a fresh-scan-required. scanId derives deterministically from (roomDir, corpusHash) with NO Date.now. The "time" field in a pull record is the monotone pull counter, not a wall clock. Zero Math.random, zero Brain require.</action>
  <verify>
    <automated>node tests/test-unknowns-bandit.cjs && node tests/test-unknowns-resume.cjs</automated>
  </verify>
  <done>bandit + resume stubs GREEN: pull records carry the Eq.1 utility, budget=floor(N*budget) is respected, and an interrupt-then-resume run is byte-identical; grep confirms zero Math.random in bandit.cjs.</done>
</task>

<task type="auto">
  <name>Task 3: The Rumsfeld 2x2 router</name>
  <read_first>.planning/research/reference/unknown-unknowns-horvitz/README.md (KnownsUnknownsMatrix.categorizeItem KK/KU/UK/UU); 165-CONTEXT.md specifics (the 2x2 IS the router: KU -> whitespace/research/expert-fill/find-analogies; UK -> meetings/graph-walk/build-knowledge; UU -> challenge/issue-tree/validate-mvp; KK = the bandit hunting ground); 165-RESEARCH.md section 4.1 (the NOW-SHIPPED down-targets: issue-tree -> /mos:diagnose sub_mode issue-tree, challenge -> /mos:challenge-assumptions, expert-fill -> /mos:bono, validate-mvp -> /mos:validate, find-analogies -> /mos:find-analogies); lib/core/unknowns/iface.cjs (FROZEN_ENGINE_EDGES.FEEDS_INTO)</read_first>
  <files>lib/core/unknowns/rumsfeld-matrix.cjs</files>
  <action>Create lib/core/unknowns/rumsfeld-matrix.cjs porting KnownsUnknownsMatrix.categorizeItem(item, awareness, knowledge) -> KK/KU/UK/UU and adding the quadrant->pipeline routing table. The routing table maps each quadrant to its frozen downstream consumer HANDLES (the shipped /mos: command slugs from section 4.1), declared as FEEDS_INTO chain targets (use FROZEN_ENGINE_EDGES.FEEDS_INTO from iface). KK is the bandit hunting ground (no downstream route; it is the input the engine probes). The module RETURNS the routing intent (quadrant + the FEEDS_INTO target handles); it does NOT invoke the commands and does NOT write edges (the orchestrator in plan 165-04 writes the FEEDS_INTO edges through the navigation chokepoint). Pure, deterministic, no Brain require, no new edge type. Provide getMatrixSummary()/exportMatrix() equivalents over a categorized set for the engine's analyze step.</action>
  <verify>
    <automated>node -e "const m=require('./lib/core/unknowns/rumsfeld-matrix.cjs'); const q=m.categorizeItem({},true,false); if(!q) process.exit(1); console.log('rumsfeld',q)"</automated>
  </verify>
  <done>rumsfeld-matrix categorizes into KK/KU/UK/UU and returns the quadrant->FEEDS_INTO routing intent for non-KK quadrants; no command invocation, no edge write, no Brain require.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine math -> graph | these four modules never touch room.db or Brain; they are pure functions over the instance set passed in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-03 | Tampering | bandit determinism | mitigate | index-deterministic arm + instance selection; the resume test asserts byte-identical; the Part-8 sweep (plan 165-06) greps zero Math.random over lib/core/unknowns/* |
| T-165-04 | Information Disclosure | engine math | accept | the modules take an instance set in memory and return scalars/handles; no Brain require, no egress path exists |
| T-165-05 | Tampering (constitutional) | rumsfeld routing | mitigate | routing emits only the frozen FEEDS_INTO handle; no new edge type; the frozen-edges self-check lives in the engine modules that require navigation (plan 165-04) |
</threat_model>

<verification>
- node tests/test-unknowns-dsp.cjs, test-unknowns-dsp-goodness.cjs, test-unknowns-bandit.cjs, test-unknowns-resume.cjs all GREEN.
- grep -c "Math.random" over the four new modules is 0.
- grep for a Brain require (brain-client / mcp__brain) over the four modules is 0.
</verification>

<success_criteria>
- PatternMiner, DSP (with the real inter-partition distance), the deterministic resumable bandit, and the Rumsfeld router all ship and turn their stubs GREEN.
- The stub-leak regression is closed: interPartition distances discriminate and a lone partition is 0.0.
- Zero Math.random, zero Brain egress.
</success_criteria>

<output>
Create `.planning/phases/165-unknown-unknowns-blindspot-engine/165-02-SUMMARY.md` when done.
</output>
