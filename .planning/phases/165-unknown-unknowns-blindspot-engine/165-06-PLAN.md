---
phase: 165-unknown-unknowns-blindspot-engine
plan: 06
type: execute
wave: 4
depends_on: ["165-01", "165-02", "165-03", "165-04", "165-05"]
files_modified:
  - lib/core/unknowns/verdict.cjs
  - tests/test-unknowns-verdict.cjs
  - tests/test-unknowns-part8-boundary.cjs
  - tests/run-all-165.sh
autonomous: true
requirements: [D-165-08, D-165-09, D-165-10]
must_haves:
  truths:
    - "An adversarial {passed, findings[]} verdict proves the engine BY INSTRUMENTATION (it runs the pipeline on the fixture and asserts the blind-spot it should find)"
    - "The Part-8 boundary sweep proves zero Brain egress, zero raw room.db write, zero Math.random over lib/core/unknowns/*"
    - "run-all-165.sh is the single green phase gate: every test-unknowns-*.cjs passes, plus the frozen-edge / no-random / Part-8 / em-dash sweeps"
  artifacts:
    - path: "lib/core/unknowns/verdict.cjs"
      provides: "the adversarial structured verdict (harness property 6)"
      exports: ["runVerdict"]
    - path: "tests/run-all-165.sh"
      provides: "the finalized one-command phase gate (Failed 0, exit 0)"
  key_links:
    - from: "lib/core/unknowns/verdict.cjs"
      to: "lib/core/unknowns/orchestrator.cjs"
      via: "runs discoverUnknownUnknowns on the fixture and inspects the result"
      pattern: "discoverUnknownUnknowns"
    - from: "tests/run-all-165.sh"
      to: "the Part-8 + frozen-edge + no-random sweeps"
      via: "grep gates over lib/core/unknowns/*"
      pattern: "Math.random"
---

<objective>
Close the harness with the Verify wave: an adversarial structured {passed, findings[]} verdict that proves the engine by instrumentation (mirroring 164 W6 / 169 W6), the Part-8 forbidden-substring boundary sweep, and the finalized run-all-165.sh phase gate that locks the whole suite GREEN. No later wave starts before this one is green; this IS the green.

Purpose: harness property 6 (adversarial structured verdict) + the Part-8 sweep (D-165-10) + the no-Math.random gate (D-165-09) + the frozen-edge assertion (D-165-08) + the run-all-165.sh phase gate the validation contract requires.
Output: lib/core/unknowns/verdict.cjs, the verdict + part8-boundary tests GREEN, the finalized run-all-165.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-unknown-unknowns-blindspot-engine/165-RESEARCH.md
@.planning/phases/165-unknown-unknowns-blindspot-engine/165-VALIDATION.md
@lib/core/unknowns/orchestrator.cjs
@tests/run-all-164.sh
</context>

<rules>
- The adversarial verdict proves the engine BY INSTRUMENTATION (harness property 6): it runs discoverUnknownUnknowns on the seeded fixture and asserts the engine actually surfaces the planted blind spot (the contradicted + stale graded-confirmed claim), the findings landed PROPOSED (never confirmed), and only frozen edges were emitted. The verdict returns {passed, findings[]} (each finding a structured {check, ok, detail}).
- The Part-8 sweep is a forbidden-substring scan over lib/core/unknowns/*.cjs: no Brain require (brain-client / mcp__brain / brain-md), no raw room.db open / INSERT INTO outside the navigation path, no Math.random, no unseeded Date.now in a math path. Mirror the Phase 90 5-tripwire + the orchestrator.cjs:978 grep-gate precedent.
- Frozen edges, remap-only (D-165-08): the verdict + the run-all assert only INVALIDATES / ROOT_CAUSES / ENABLES / FEEDS_INTO are emitted and the module-load self-check throws on drift; no canon amendment, no edges.cjs change.
- run-all-165.sh is the SINGLE phase gate (clone the run-all-164.sh finalization shape): every test-unknowns-*.cjs registered + the frozen-edge / no-random / Part-8 grep checks + the em-dash sweep (written via U+2014 codepoint escape so the script carries no literal em-dash). Failed 0, exit 0.
- The engine HALTS at the gate; the verdict asserts the engine never auto-confirmed (no confirmed truth-claim node written by the engine).
- Part-8 LOCAL only; the verdict runs against the LOCAL fixture room.db; zero Brain require in verdict.cjs.
- NO em-dashes anywhere (hyphens only).
</rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The adversarial structured verdict + the Part-8 boundary sweep</name>
  <read_first>tests/run-all-164.sh + the 164 verdict test pattern (test-bono-verdict.cjs + test-bono-part8-leak.cjs -- the {passed, findings[]} shape + the leak scan to mirror); lib/core/unknowns/orchestrator.cjs (discoverUnknownUnknowns -- the engine the verdict instruments); tests/fixtures/unknowns/build-fixture-room.cjs (the planted contradicted + stale claim the verdict must prove is surfaced); 165-RESEARCH.md section 8 (the constitutional assertions for run-all-165.sh: Part-8 boundary, frozen-edge, no-Math.random, no-raw-INSERT) + the Security Domain threat table</read_first>
  <behavior>
    - runVerdict({roomDir, db}) runs discoverUnknownUnknowns on the seeded fixture and returns {passed, findings[]}.
    - it asserts (each a structured finding): the planted contradicted/stale graded-confirmed claim is surfaced as a proxy-labeled UU candidate; every written finding is review_status proposed (zero confirmed written by the engine); only the 4 frozen edge types were emitted; the corpus matched the graded-confirmed UNION (not empty, not the excluded rows); the scan checkpoint is present and resumable.
    - the part8-boundary test sweeps lib/core/unknowns/*.cjs and fails on any Brain require, raw room.db write, Math.random, or unseeded Date.now.
    - passed is true only when every structured finding ok is true.
  </behavior>
  <action>Create lib/core/unknowns/verdict.cjs exporting runVerdict({roomDir, db}) that builds (or accepts) the seeded fixture, runs discoverUnknownUnknowns, and returns a structured {passed, findings[]} verdict where each finding is {check, ok, detail}: (1) blind-spot-surfaced (the planted contradicted+stale claim is in the proxy-labeled UU set), (2) proposed-only (no confirmed truth-claim node written by the engine), (3) frozen-edges-only (the emitted edge types are a subset of INVALIDATES/ROOT_CAUSES/ENABLES/FEEDS_INTO), (4) corpus-graded-confirmed (the corpus equals the fixture's Academic/Operational confirmed set), (5) resumable (the checkpoint is present + a resume is byte-identical). passed = AND over findings. Then implement tests/test-unknowns-verdict.cjs to assert runVerdict on the fixture returns passed true with all five findings ok. Then implement tests/test-unknowns-part8-boundary.cjs as the forbidden-substring sweep over lib/core/unknowns/*.cjs (Brain require, raw room.db open / INSERT INTO outside navigation, Math.random, unseeded Date.now) -- mirror the Phase 90 tripwire idiom; assert each forbidden count is 0. verdict.cjs has zero Brain require.</action>
  <verify>
    <automated>node tests/test-unknowns-verdict.cjs && node tests/test-unknowns-part8-boundary.cjs</automated>
  </verify>
  <done>verdict + part8-boundary stubs GREEN: runVerdict proves the engine by instrumentation (planted blind spot surfaced, proposed-only, frozen-edges-only, graded-confirmed corpus, resumable); the Part-8 sweep confirms zero forbidden substrings over lib/core/unknowns/*.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize run-all-165.sh as the single green phase gate</name>
  <read_first>tests/run-all-165.sh (the Wave-0 scaffold from plan 165-01); tests/run-all-164.sh (the finalized-gate shape: CJS_SUITES + the frozen-contract grep checks + the connector --check + the Part-8 grep sweep + the em-dash sweep, Failed 0 exit 0); 165-RESEARCH.md section 8 (the constitutional assertions) + the Validation Architecture sampling-rate (the phase gate before /gsd-verify-work)</read_first>
  <files>tests/run-all-165.sh</files>
  <action>Finalize tests/run-all-165.sh (extend the plan-01 scaffold to the run-all-164.sh finalized shape): ensure CJS_SUITES registers all 10 test-unknowns-*.cjs (corpus-adapter, dsp, dsp-goodness, bandit, resume, proxy-oracle, frozen-edges, part8-boundary, rank-in, verdict); add the FROZEN-CONTRACT grep checks (a grep that the only emittable edge handles in lib/core/unknowns/*.cjs are the 4 frozen types; a grep that no 7th reach_id appears; the no-Math.random grep over lib/core/unknowns/*; the no-raw-INSERT grep); add the connector --check (node scripts/build-connector-registry.cjs --check) so the file-meeting/map-unknowns wiring is gated; keep the per-suite PASS/FAIL tally + the U+2014-escaped em-dash sweep over the phase artifacts. The gate exits 0 with Failed 0 only when every suite + every grep gate passes. This is the single PASS/FAIL phase contract for /gsd-verify-work.</action>
  <verify>
    <automated>bash tests/run-all-165.sh; test $? -eq 0 && echo "PHASE GATE GREEN"</automated>
  </verify>
  <done>run-all-165.sh runs every test-unknowns-*.cjs GREEN plus the frozen-edge / no-random / Part-8 / connector-check / em-dash sweeps; Failed 0, exit 0; it is the single phase gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verdict -> engine | the verdict instruments the engine against the LOCAL fixture; it is the adversarial proof, not a happy-path smoke test |
| run-all gate -> CI | the gate is the single PASS/FAIL contract; a silently-skipped suite would hide a regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-16 | Information Disclosure | claim content egress to Brain | mitigate | the part8-boundary sweep over lib/core/unknowns/* asserts zero Brain require / egress; the verdict runs LOCAL-only |
| T-165-17 | Tampering | a regression slips past the gate | mitigate | run-all-165.sh runs to completion even on failure, tallies Failed, exits 1 on any fail; the verdict proves by instrumentation, not by promise |
| T-165-18 | Elevation of Privilege | engine auto-confirms | mitigate | the verdict's proposed-only finding asserts the engine wrote zero confirmed truth-claim nodes |
| T-165-SC | Tampering | npm installs | accept | zero new packages this phase; no install task, no slopcheck target |
</threat_model>

<verification>
- bash tests/run-all-165.sh exits 0, Failed 0, every test-unknowns-*.cjs PASS.
- node tests/test-unknowns-verdict.cjs returns passed true with five ok findings.
- node tests/test-unknowns-part8-boundary.cjs confirms zero forbidden substrings.
- node scripts/build-connector-registry.cjs --check is clean.
</verification>

<success_criteria>
- The adversarial {passed, findings[]} verdict proves the engine by instrumentation.
- The Part-8 sweep + frozen-edge + no-random gates pass.
- run-all-165.sh is the single green phase gate (Failed 0, exit 0).
</success_criteria>

<output>
Create `.planning/phases/165-unknown-unknowns-blindspot-engine/165-06-SUMMARY.md` when done.
</output>
