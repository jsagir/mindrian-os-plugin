---
phase: 165-unknown-unknowns-blindspot-engine
plan: 04
type: execute
wave: 3
depends_on: ["165-01", "165-02", "165-03"]
files_modified:
  - lib/core/unknowns/orchestrator.cjs
autonomous: true
requirements: [D-165-01, D-165-02, D-165-06, D-165-08, D-165-09, D-165-10]
must_haves:
  truths:
    - "The orchestrator runs the full pipeline: corpus -> mine -> DSP -> bandit(proxy,budget) -> categorize/route -> write proposed findings -> rank into F.1"
    - "Proxy-labeled findings are written as proposed truth-claim nodes plus frozen cascade edges through the navigation chokepoint (cloning 169 candidateToFinding)"
    - "Only the 4 frozen edges (INVALIDATES/ROOT_CAUSES/ENABLES/FEEDS_INTO) are emittable; a module-load self-check throws on a drifted type"
    - "Engine output ranks into the F.1 selector via the f-selector-ranker; the engine HALTS at the gate and never auto-confirms a blind spot"
    - "The full scan is resumable byte-identically via the per-pull checkpoint persisted to the LOCAL room sidecar"
  artifacts:
    - path: "lib/core/unknowns/orchestrator.cjs"
      provides: "discoverUnknownUnknowns pipeline + the proposed-finding writer + the F.1 rank-in"
      exports: ["discoverUnknownUnknowns"]
  key_links:
    - from: "lib/core/unknowns/orchestrator.cjs"
      to: "navigation.writeClaimNode + navigation.writeEdge"
      via: "the candidateToFinding-cloned proposed-node + frozen-edge writer"
      pattern: "writeClaimNode|writeEdge"
    - from: "lib/core/unknowns/orchestrator.cjs"
      to: "lib/workflow/f-selector-ranker.cjs"
      via: "rankForSelector candidate rank-in"
      pattern: "rankForSelector|f-selector-ranker"
    - from: "lib/core/unknowns/orchestrator.cjs"
      to: "navigation/edges.cjs ALLOWED_EDGE_TYPES"
      via: "module-load frozen-edge self-check"
      pattern: "ALLOWED_EDGE_TYPES"
---

<objective>
Wire the surface: the orchestrator that sequences the deterministic engine (corpus -> mine -> DSP -> bandit -> categorize/route), writes proxy-labeled findings as PROPOSED truth-claim nodes plus frozen cascade edges through the navigation chokepoint (cloning the shipped 169 candidateToFinding writer), and ranks engine output into the F.1 selector. The engine halts at the gate (orchestrator-in-loop, harness property 9). Turns the rank-in and frozen-edges stubs GREEN.

Purpose: D-165-01 (proxy findings land proposed) + D-165-02 (the full pipeline) + D-165-06 (rank into F.1, auto-fire) + D-165-08 (frozen edges, remap-only) + D-165-09 (resumable) + D-165-10 (writes via the chokepoint).
Output: lib/core/unknowns/orchestrator.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-unknown-unknowns-blindspot-engine/165-RESEARCH.md
@lib/core/unknowns/iface.cjs
@lib/core/graph-derivation.cjs
@lib/core/issue-tree.cjs
@lib/workflow/f-selector-ranker.cjs
</context>

<rules>
- Clone, do not fork: clone graph-derivation.cjs candidateToFinding / runDerivation's write step (PROPOSED node via navigation.writeClaimNode + frozen edge via navigation.writeEdge, _candidateHash stable id so a re-run does not re-mint) -- do NOT roll your own raw SQL. Clone the issue-tree.cjs single-build discipline + writeIssueTreeEdges chokepoint pattern + the module-load frozen-edge self-check (issue-tree.cjs:67). Do NOT clone the 56KB futures async orchestrator; use it only as the outer stage-sequencing reference.
- Frozen edges, remap-only (D-165-08): emit ONLY INVALIDATES / ROOT_CAUSES / ENABLES / FEEDS_INTO. The module-load self-check requires navigation/edges.cjs ALLOWED_EDGE_TYPES and throws if any emittable type drifted out of the frozen set. NO new edge type, NO canon amendment. If you discover a genuine frozen-set gap, STOP and flag it; do not invent.
- Proxy findings land review_status proposed ONLY. Only navigation.confirmNode (human byUser) promotes to confirmed (Part 9 role 5). The engine HALTS at the F.1 gate; it never auto-confirms a blind spot (harness property 9; orchestrator-in-loop). humanConfirmBudget default 3 (the top-N surfaced at the gate).
- NO Math.random; the bandit checkpoint is persisted to the LOCAL room sidecar (<roomDir>/.mindrian/uu-scan.json, the last-cascade.json side-channel idiom) so a scan is resumable byte-identically. D-165-09.
- Part-8 LOCAL only (D-165-10): all writes through navigation.cjs; the claim BODY lives on the NODE, edge properties are enum/scalar only; zero Brain egress. HSI arm-priority: per open-question 1, use the LOCAL proxy-score arm priority as the v1 default (HSI optional cached enrichment, outside the bandit tight loop, Part-8-clean) -- do NOT add a Brain/Pinecone call inside the bandit loop.
- The rank-in produces a candidate REACH scored into the F.1 set; the engine does NOT invoke the downstream commands and does NOT auto-commit (D-165-06).
- NO em-dashes anywhere (hyphens only).
</rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The pipeline orchestrator + the proposed-finding writer</name>
  <read_first>.planning/research/reference/unknown-unknowns-horvitz/README.md (UnknownUnknownsOrchestrator.discoverUnknownUnknowns stage order: createSearchSpace -> extractFeatures -> minePatterns -> dsp.partition -> bandit.initialize -> bandit.selectNextInstance(budget=floor(N*budget)) -> analyzeDiscoveries -> generateRecommendations); lib/core/graph-derivation.cjs (candidateToFinding:95 -> the proposed-node + edge intent shape; runDerivation:154 the write step -- writeClaimNode never downgrades a confirmed node; _candidateHash:79 stable id); lib/core/issue-tree.cjs (the single-build discipline + writeIssueTreeEdges:242 chokepoint + the module-load self-check:67); lib/core/navigation.cjs (writeClaimNode:177, writeEdge:114); lib/core/unknowns/{corpus-adapter,pattern-miner,dsp,bandit,rumsfeld-matrix,iface}.cjs (the modules this orchestrator sequences); 165-RESEARCH.md the system diagram (lines 442-488) + section 1.6 (the checkpoint sidecar)</read_first>
  <behavior>
    - discoverUnknownUnknowns({roomDir, db, config, now, priorityFn}) runs the full stage pipeline over the fixture corpus and returns {findings, recommendations, checkpoint, ranked}.
    - the bandit uses the LOCAL proxy-score arm priority (v1 default) and the corpus-adapter proxyOracle as oracleFn; budget = floor(N*config.budget).
    - each proxy-labeled UU instance is written as a PROPOSED truth-claim node + a frozen cascade edge through navigation.writeClaimNode/writeEdge (cloning candidateToFinding); _candidateHash gives a stable id so a re-run does not re-mint; a confirmed node is never downgraded.
    - the module-load self-check throws if an emittable edge type is not in ALLOWED_EDGE_TYPES.
    - the per-pull checkpoint persists to <roomDir>/.mindrian/uu-scan.json; an interrupted-then-resumed run is byte-identical.
    - the engine writes proposed only; no confirmed node is written by the engine.
  </behavior>
  <action>Create lib/core/unknowns/orchestrator.cjs cloning the issue-tree single-build discipline. Sequence the stages by composing the plan-02/03 modules: findConfidentClaims (createSearchSpace + extractFeatures, the recast) -> pattern-miner.minePatterns -> dsp.partition -> bandit.initialize (arm priority = LOCAL proxy-score mean per partition, the v1 default; HSI enrichment is an OPTIONAL cached read outside the loop, NOT wired into the tight loop) -> bandit.selectNextInstance(corpusAdapter.proxyOracle, floor(N*budget)) with the checkpoint persisted to <roomDir>/.mindrian/uu-scan.json -> rumsfeld-matrix categorize/route -> analyzeDiscoveries + generateRecommendations (the HIGH_RISK_PATTERN/MODEL_CONFIDENCE/EXPLORATION_NEEDED enum from the reference). For each proxy-labeled UU instance, clone candidateToFinding to produce the {knowledge_type, text, node_seed, edge} intent, then write the PROPOSED node via navigation.writeClaimNode and the frozen cascade edge via navigation.writeEdge (clone writeIssueTreeEdges: route every edge, count written/rejected, never swallow a rejection). Add the module-load frozen-edge self-check (clone issue-tree.cjs:67) over FROZEN_ENGINE_EDGES vs ALLOWED_EDGE_TYPES. Persist + resume the bandit checkpoint (corpusHash guard: matching continues, mismatch starts fresh). Zero Math.random, zero Brain egress, all writes via the chokepoint, findings PROPOSED only.</action>
  <verify>
    <automated>node tests/test-unknowns-frozen-edges.cjs && node tests/test-unknowns-resume.cjs</automated>
  </verify>
  <done>orchestrator runs the full pipeline; proxy findings land PROPOSED via the chokepoint with frozen edges; the module-load self-check throws on a drifted type; the scan resumes byte-identically; frozen-edges stub GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rank engine output into the F.1 selector (the gate)</name>
  <read_first>lib/workflow/f-selector-ranker.cjs (rankForSelector:359 signature -- args {jtbd, problemType, focusNodeId, roomState, packetOptional, k}; MAX_K=3 clamp; module.exports); lib/core/sensors/sensor-types.cjs (makeReach:80 -- the candidate-reach factory, reach_id must be one of the frozen 6, evidence = LOCAL scalars only); 165-RESEARCH.md section 7.4 (the gate sequence: engine output -> candidate reach -> f-selector-ranker -> F.1 Decision Gate -> APPROVE top-N -> confirmNode); 165-CONTEXT specifics (Larry reacts at the gate: "where you are most confident is where I bet you are wrong -- probe?")</read_first>
  <behavior>
    - the orchestrator surfaces its top-N (humanConfirmBudget, default 3) proxy-ranked UU candidates as a ranked F.1 set via the f-selector-ranker rank-in.
    - the candidate carries LOCAL scalars only (proxy score, partition id, corpus size) -- never venture prose -- on the reach evidence.
    - the engine returns the ranked set; it does NOT call confirmNode and does NOT invoke any downstream command (the human decides at the gate).
    - the corpus size is surfaced so the navigator sees if the corpus is thin (open-question recommendation).
  </behavior>
  <action>Add the rank-in step to orchestrator.cjs: build a candidate-reach (makeReach, reach_id context_block riding the map-unknowns front door, posture hold, evidence = LOCAL scalars: top-N proxy scores + partition ids + corpus size) and score the top-N (humanConfirmBudget) into the F.1 set via lib/workflow/f-selector-ranker.cjs rankForSelector (clamped at MAX_K). Return the ranked set in the orchestrator result as {ranked, corpusSize}. Do NOT call confirmNode; do NOT invoke a downstream command. Document that APPROVE at the gate (a human byUser) is what promotes a proposed blind spot to confirmed via navigation.confirmNode -- the engine stops here (orchestrator-in-loop). Surface corpusSize so a thin corpus is visible at the gate.</action>
  <verify>
    <automated>node tests/test-unknowns-rank-in.cjs</automated>
  </verify>
  <done>rank-in stub GREEN: engine output becomes a candidate the f-selector-ranker scores into the F.1 set, clamped at MAX_K; the engine halts at the gate (no confirmNode call, no command invocation); corpus size is surfaced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator -> room.db graph | all node/edge writes cross here; must route through navigation.cjs, frozen edges only |
| engine -> F.1 gate | the engine proposes; the human disposes; no auto-confirm crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-09 | Elevation of Privilege | a proxy proposal auto-promoted | mitigate | the orchestrator writes proposed only; only confirmNode (human byUser, Part 9 role 5) promotes; the truth-machine guard rejects an agent-attributed confirm |
| T-165-10 | Tampering (constitutional) | a new edge type / 7th reach | mitigate | the module-load frozen-edge self-check throws on drift; makeReach rejects a non-frozen reach_id; no canon amendment this phase |
| T-165-11 | Information Disclosure | venture prose on an edge or a reach | mitigate | the claim body lives on the NODE; edge properties + reach evidence are enum/scalar only (clone candidateToFinding's properties.reason.slice(0,64) idiom); the Part-8 sweep (plan 165-06) enforces |
| T-165-12 | Tampering | non-deterministic resume | mitigate | the checkpoint corpusHash guard + index-deterministic selection; the resume test asserts byte-identical |
</threat_model>

<verification>
- node tests/test-unknowns-frozen-edges.cjs, test-unknowns-resume.cjs, test-unknowns-rank-in.cjs all GREEN.
- grep over orchestrator.cjs: zero Math.random, zero Brain require, zero raw INSERT INTO.
- The orchestrator requires graph-derivation/navigation (the chokepoint), not node:sqlite directly.
</verification>

<success_criteria>
- The full pipeline runs and writes proposed findings + frozen edges through the chokepoint.
- Engine output ranks into F.1; the engine halts at the gate (no auto-confirm).
- The scan is resumable byte-identically; the frozen-edge self-check guards the vocabulary.
</success_criteria>

<output>
Create `.planning/phases/165-unknown-unknowns-blindspot-engine/165-04-SUMMARY.md` when done.
</output>
