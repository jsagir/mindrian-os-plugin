---
phase: 230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur
plan: 05
type: execute
wave: 2
depends_on: [230-01]
files_modified:
  - .planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/smoke-labels.json
autonomous: false
requirements: [D7]
must_haves:
  truths:
    - "A hand-labeled smoke reference set of 10-15 already-known skills exists BEFORE any full-fleet spend, pre-labeled by Jonathan, never labeled after the fact from pipeline output (AI-SPEC Section 5 Labeling rule)"
    - "The set covers every composition slot from the AI-SPEC: clear-fire, near-miss sibling pairs, weak+good description guards, the WS2 defect/clean pair, and a not_evaluated probe"
    - "The WS2 defect slot targets the real logged check-card-fire.cjs over-enforcement bug (CONTEXT.md Specifics: the concrete worked example WS2 exists to catch)"
  artifacts:
    - path: .planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/smoke-labels.json
      provides: "SmokeLabelSchema-valid array: the D7 calibration reference dataset with Jonathan's approved expected verdicts"
  key_links:
    - from: smoke-labels.json
      to: lib/core/skillopt-schemas.cjs
      via: "every record validates against SmokeLabelSchema"
      pattern: "SmokeLabelSchema"
    - from: smoke-labels.json
      to: "out/inventory.json"
      via: "every named skill exists in the real inventory; the WS2 pair comes from the real backed subset"
      pattern: "inventory"
---

<objective>
Assemble and human-approve the smoke-test calibration set: the hand-labeled reference dataset that gates everything downstream (AI-SPEC D7 is a hard run gate -- the funnel judge is never trusted blind, and the full 124-skill spend is blocked until agreement with these labels clears).

Purpose: CONTEXT.md Specifics locks smoke-testing on a small handful of known skills before spending the full budget. The AI-SPEC (Section 5, Reference Dataset) specifies the exact composition and mandates labeling by Jonathan DURING harness implementation, before the full run. Claude drafts from real inventory data; Jonathan approves or edits at a checkpoint -- automation first, human judgment where it is the actual point.

Output: smoke-labels.json, schema-valid and human-approved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-AI-SPEC.md
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-CONTEXT.md
@lib/core/skillopt-schemas.cjs
</context>

## Artifacts this phase produces (Plan 05 slice)

New file: `.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/smoke-labels.json` -- an array of SmokeLabelSchema records (roles: clear_fire, near_miss_pair, weak_description, good_description, ws2_defect, ws2_clean, not_evaluated_probe). This file is a hand-labeled INPUT to the pipeline, not a pipeline product -- it lives beside the plan files, deliberately outside out/, so no pipeline run can ever overwrite it.

<tasks>

<task type="auto">
  <name>Task 1: draft the smoke set from real inventory data</name>
  <files>.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/smoke-labels.json</files>
  <read_first>
    - 230-AI-SPEC.md Section 5 "Reference Dataset" (the composition contract this task fills slot by slot: 10-15 skills; 1-2 clear-fire; 2-3 near-miss sibling pairs from mos:find-* / mos:explore-*; 1 known-weak + 1 known-good description; 1 script-backed skill with the real logged defect + 1 clean .cjs; 1 induced-failure probe)
    - out/inventory.json (run node scripts/skillopt-inventory.cjs first if absent -- selection MUST come from real records: real family keys, real backed flags, real descriptions)
    - .planning/todos/pending/2026-07-05-fix-stop-hook-forcing-irrelevant-decision-gate-cards.md (the logged over-enforcement bug the ws2_defect slot must reference)
    - lib/core/skillopt-schemas.cjs (SmokeLabelSchema field vocabulary -- roles and expected_* enums)
  </read_first>
  <action>
    Generate out/inventory.json if missing (node scripts/skillopt-inventory.cjs). Then draft smoke-labels.json with 10-15 records filling EVERY composition slot:

    - clear_fire (1-2): skills whose descriptions name concrete distinctive trigger phrases; expected_funnel 'pass'.
    - near_miss_pair (4-6 records = 2-3 pairs): real siblings from the mos:find-* and mos:explore-* families (CONTEXT.md Specifics names both); expected_funnel per Jonathan's expectation drafted from reading the actual descriptions -- where two sibling descriptions genuinely overlap, draft 'flagged' honestly rather than optimistically.
    - weak_description (1): a skill whose description reads as a vague capability blurb (pick by reading real descriptions from the inventory; state WHY in notes); expected_funnel 'flagged'.
    - good_description (1): a clearly-routed skill; expected_funnel 'pass' -- this is the over-edit guard (AI-SPEC offline metric: the pipeline proposing diffs on known-good skills is the WS1 overcorrection analogue).
    - ws2_defect (1): the skill from the backed subset whose backing_refs include scripts/check-card-fire.cjs; if NO inventory record references it, still include a record with the skill field set to the backing script path 'scripts/check-card-fire.cjs' and notes explaining the direct --files targeting (Plan 04 supports it); expected_ws2 'finding_expected', notes citing the 2026-07-05 todo (over-enforcement: gate cards forced on plain prose).
    - ws2_clean (1): a small, well-tested backed .cjs from the real backed subset; expected_ws2 'no_finding_expected' (the D4 false-positive guard).
    - not_evaluated_probe (1): any skill, expected_funnel 'not_evaluated', notes: induced via the funnel's --probe-timeout-unit hook (Plan 02); the probe MUST land as not_evaluated reason induced_probe, never as a pass (D5).

    Every record: SmokeLabelSchema-valid; skill names verified to exist in inventory (except the direct-path ws2 fallback); notes field carries the WHY for every draft label so Jonathan reviews reasoning, not bare verdicts. Write the file with the Write tool (this is a hand-file, not a pipeline write).
  </action>
  <verify>
    <automated>node -e "const {SmokeLabelSchema}=require('./lib/core/skillopt-schemas.cjs'); const labels=require('./.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/smoke-labels.json'); if(labels.length<10||labels.length>15){console.error('size '+labels.length+' outside 10-15');process.exit(1)}; const roles=new Set(labels.map(l=>l.role)); ['clear_fire','near_miss_pair','weak_description','good_description','ws2_defect','ws2_clean','not_evaluated_probe'].forEach(r=>{if(!roles.has(r)){console.error('missing slot '+r);process.exit(1)}}); labels.forEach((l,i)=>{const p=SmokeLabelSchema.safeParse(l); if(!p.success){console.error('record '+i+' invalid: '+JSON.stringify(p.error.issues));process.exit(1)}}); console.log('smoke set OK: '+labels.length+' records, all slots filled')"</automated>
  </verify>
  <acceptance_criteria>
    - 10-15 records, all seven roles present, every record SmokeLabelSchema-valid (the node -e gate above).
    - Near-miss pairs are REAL siblings sharing a family key in out/inventory.json.
    - ws2_defect record cites the 2026-07-05 todo in notes; ws2_clean names a file from the real backed subset.
    - Every record's notes explains the draft label's reasoning.
  </acceptance_criteria>
  <done>A complete, schema-valid draft smoke set grounded in real inventory data awaits Jonathan's review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Jonathan pre-labels -- approve or edit the smoke expectations</name>
  <action>Present the drafted smoke-labels.json to Jonathan for pre-label review. Apply any edits he dictates directly to the file, re-run the Task 1 SmokeLabelSchema validation gate after every edit, and record his explicit approval verbatim in the SUMMARY. Do not proceed to any live spend without it.</action>
  <what-built>A drafted 10-15 skill smoke calibration set (smoke-labels.json) with reasoning per label, filling every AI-SPEC composition slot: clear-fire, near-miss sibling pairs, weak/good description guards, the check-card-fire defect + clean-control WS2 pair, and the induced not_evaluated probe.</what-built>
  <how-to-verify>
    1. Open .planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/smoke-labels.json
    2. For each record, check the expected_funnel / expected_ws2 label against your own expectation as the skill author + code reviewer (the two hats in AI-SPEC 1b). The notes field carries the draft reasoning.
    3. Pay closest attention to: the near-miss pairs (would the right sibling really win?), the good_description pick (this guards against over-editing), and the ws2_clean pick (this guards against false-positive findings).
    4. Edit any label or swap any skill directly in the file, or dictate changes.
  </how-to-verify>
  <resume-signal>Type "approved" to lock the smoke set, or list the records to change. These labels become the D7 calibration bar: the full 124-skill spend is gated on the funnel agreeing with them.</resume-signal>
  <done>Jonathan's explicit approval (or his applied-and-revalidated edits plus approval) is recorded; the smoke set is locked as the D7 reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft labels -> calibration authority | labels drafted by Claude become the measuring stick for the judge; unreviewed drafts would make the pipeline grade itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-16 | Spoofing (self-calibration) | smoke-labels.json | mitigate | blocking human-verify checkpoint: Jonathan approves every label before it gains gate authority; labels are drafted from real inventory data with reasoning attached (AI-SPEC: never labeled after the fact from pipeline output) |
| T-230-17 | Tampering | pipeline overwriting the reference set | mitigate | file lives OUTSIDE out/ so assertUnderOut-guarded pipeline writes structurally cannot target it |
| T-230-SC | Tampering | package installs | accept (N/A) | zero installs this phase |
</threat_model>

<verification>
- node -e SmokeLabelSchema validation gate exits 0 (size, slots, per-record validity).
- Checkpoint resolved with explicit approval recorded in the SUMMARY.
</verification>

<success_criteria>
- Human-approved, schema-valid smoke set on disk before any live smoke spend (Plan 07 hard-depends on this plan).
- Every AI-SPEC composition slot filled from real repo data.
</success_criteria>

<output>
Create `.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-05-SUMMARY.md` when done.
</output>
