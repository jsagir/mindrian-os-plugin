---
phase: 230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur
plan: 04
type: execute
wave: 2
depends_on: [230-01]
files_modified:
  - scripts/skillopt-codereview.cjs
  - references/methodology/skillopt-review-rubric.md
  - references/methodology/skillopt-refute-rubric.md
autonomous: true
requirements: [D4]
must_haves:
  truths:
    - "Every reported finding survived a second, independent adversarial pass that genuinely tried to refute it -- refuted findings are dropped from the report, never silently downgraded (CONTEXT.md Safety decision)"
    - "Every CONFIRMED/PLAUSIBLE finding's evidence_quote is verified as a verbatim substring of the real .cjs file by deterministic code; a failed anchor drops the finding as fabricated and records WHY"
    - "The reviewer reports findings only -- it is never asked to also suggest fixes (the systematic-overcorrection literature in AI-SPEC 1b: asking for fixes RAISES the misjudgment rate)"
    - "Review subagents can read and run scripts but can never Write or Edit (review, never rewrite -- WS2 scope)"
  artifacts:
    - path: scripts/skillopt-codereview.cjs
      provides: "WS2 two-pass adversarial code review over the .cjs-backed skill subset (Refute-or-Promote)"
      exports: [reviewOneFile, refuteFindings, anchorEvidence, chunkFile, runCodeReview]
      min_lines: 220
    - path: references/methodology/skillopt-review-rubric.md
      provides: "frozen reviewer instruction (findings-only, verbatim-quote anchored, severity-ranked)"
    - path: references/methodology/skillopt-refute-rubric.md
      provides: "frozen adversarial refutation instruction (Refute-or-Promote skeptical stance)"
  key_links:
    - from: scripts/skillopt-codereview.cjs
      to: lib/core/skillopt-schemas.cjs
      via: "require -- ReviewFindingSchema, CodeFindingSchema, inlineSchemaJson, assertUnderOut"
      pattern: "require\\(.*skillopt-schemas"
    - from: scripts/skillopt-codereview.cjs
      to: "out/inventory.json"
      via: "backed subset + backing_refs drive the file list under review"
      pattern: "inventory\\.json"
    - from: scripts/skillopt-codereview.cjs
      to: "the real reviewed .cjs file"
      via: "anchorEvidence verbatim-substring check (deterministic, not a model call)"
      pattern: "anchorEvidence"
---

<objective>
Build Workstream 2: adversarially-verified code review over the ~10-20 script/workflow-backed skills identified by the inventory (CONTEXT.md Scope decision). Two isolated opus passes per file -- review, then an independent refutation pass -- with a deterministic evidence-quote anchor so a fabricated finding structurally cannot reach the report (D4/CFM2, the check-card-fire.cjs over-enforcement class).

Purpose: PATTERNS.md flagged the discrepancy -- there is NO discrete in-repo code-review skill file to reuse; per the flagged guidance, the reviewer is built as a spawned-subagent rubric modeled on the huji-eval.cjs judge leg (pinned model, fail-closed, calibrated), not a copied skill. WS2 runs independent of any skill's trigger-flag status (locked Rigor decision).

Output: scripts/skillopt-codereview.cjs + two frozen rubric files, selftested deterministically; live opus spend happens only in Plan 07's smoke.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-AI-SPEC.md
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-PATTERNS.md
@lib/core/skillopt-schemas.cjs
@scripts/huji-eval.cjs
@scripts/huji-run-one.cjs
</context>

## Artifacts this phase produces (Plan 04 slice)

New files: `scripts/skillopt-codereview.cjs`, `references/methodology/skillopt-review-rubric.md`, `references/methodology/skillopt-refute-rubric.md`.
New exported symbols: `reviewOneFile({file, skill, config})`, `refuteFindings({file, findings, config})`, `anchorEvidence(finding, fileText)`, `chunkFile(fileText, maxLines)`, `runCodeReview({inventory, config})`.
New CLI flags: `--skills <csv>`, `--files <csv>` (direct file targets, used by the smoke run for check-card-fire.cjs), `--out <dir>`, `--selftest`, `--dry-run`.
New artifact shapes on disk: `out/review/units/<unit_id>.json` (UnitRecord per pass), `out/review/findings-raw.json` (ALL findings incl. refuted, for the survival-rate metric), `out/review/findings.json` (CONFIRMED/PLAUSIBLE survivors only).

<tasks>

<task type="auto">
  <name>Task 1: the two frozen rubrics -- findings-only reviewer + skeptical refuter</name>
  <files>references/methodology/skillopt-review-rubric.md, references/methodology/skillopt-refute-rubric.md</files>
  <read_first>
    - 230-AI-SPEC.md Section 1b Dimension 4 (what a good vs bad finding looks like, in domain language) and "Known Failure Modes > Systematic overcorrection" (hallucinated requirements, overweighted edge cases, and the explicit warning that asking for fixes raises misjudgment) and Section 6 "Evidence-quote anchor" guardrail
    - .planning/todos/pending/2026-07-05-fix-stop-hook-forcing-irrelevant-decision-gate-cards.md (the concrete worked example of the finding class WS2 exists to catch -- read it so the rubric's examples are grounded in the real logged bug)
    - 230-PATTERNS.md "No Analog Found" (build as judge-leg rubric, not a copied skill)
  </read_first>
  <action>
    Create references/methodology/skillopt-review-rubric.md, the FROZEN reviewer instruction: you review ONE CJS file (or one chunk of it) that backs a MindrianOS skill; report defects as findings ranked critical/high/medium/low; every claim must be grounded in what the code ACTUALLY does, never in a requirement you assume it should meet; every finding carries evidence_quote, a VERBATIM line copied exactly from the file (it will be machine-checked as a substring -- a paraphrase gets the finding dropped as fabricated); report findings ONLY, do not propose fixes or rewrites; prefer fewer well-grounded findings over volume; an unreachable edge case is not a defect; if the file is sound say so with zero findings. Include one grounded positive example shaped like the check-card-fire over-enforcement class (a gate that fires on plain prose with no real decision present -- over-enforcement is a defect class, cite the shape not the file) and one negative example (asserting a bug because the code lacks a feature no caller needs). Feynman-plain, JTBD-oriented, no em-dashes.

    Create references/methodology/skillopt-refute-rubric.md, the FROZEN refutation instruction (Refute-or-Promote): you receive findings another reviewer produced about a CJS file you can read yourself; your job is to REFUTE each one -- actively look for why the claim is wrong: a requirement that was assumed not real, an edge case that cannot be reached, a misread of control flow, a behavior that is actually intended per the file's own header contract; verdict per finding: refuted (you found a genuine hole), confirmed (you tried and the defect stands), plausible (you could not refute it but could not fully confirm either); document your refutation_attempt for every finding including confirmed ones (what you tried and why it failed to refute); you earn nothing by agreeing -- rubber-stamp confirmation is the failure mode you exist to prevent. No em-dashes.
  </action>
  <verify>
    <automated>test -s references/methodology/skillopt-review-rubric.md && test -s references/methodology/skillopt-refute-rubric.md && ! grep -P '\x{2014}' references/methodology/skillopt-review-rubric.md references/methodology/skillopt-refute-rubric.md && grep -qi 'verbatim' references/methodology/skillopt-review-rubric.md && grep -qi 'refut' references/methodology/skillopt-refute-rubric.md && ! grep -qi 'suggest a fix\|propose a fix\|how to fix' references/methodology/skillopt-review-rubric.md</automated>
  </verify>
  <acceptance_criteria>
    - Review rubric: findings-only mandate present (no fix-suggestion request anywhere), verbatim evidence_quote requirement stated with the machine-check consequence, over-enforcement example present, zero-findings-is-valid stated.
    - Refute rubric: skeptical stance, three-verdict vocabulary matching CodeFindingSchema enum exactly (confirmed/plausible/refuted), refutation_attempt required on every finding, anti-rubber-stamp framing present.
    - Both files: no em-dashes, plain prose, stable frozen wording (these bytes are the prompt-cache prefix).
  </acceptance_criteria>
  <done>Both rubrics exist as frozen standalone system-prompt files encoding the anti-overcorrection and Refute-or-Promote disciplines from the AI-SPEC research base.</done>
</task>

<task type="auto">
  <name>Task 2: skillopt-codereview.cjs -- two-pass orchestration with deterministic evidence anchor</name>
  <files>scripts/skillopt-codereview.cjs</files>
  <read_first>
    - scripts/huji-eval.cjs header lines 1-40 and the judge leg (pinned-different-model, fail-closed, skip-cleanly-without-live-flag design -- the shape this reviewer mirrors per PATTERNS.md No-Analog guidance)
    - scripts/huji-run-one.cjs lines 289-337 (buildStageBArgs spawn shape + parseEnvelope)
    - 230-AI-SPEC.md Section 4 Model Configuration (WS2 = opus tier, higher --max-turns) and Tool Use (allowlist: Read, Glob, Bash(node scripts/*), NO Write/Edit) and Section 4 Context Window Strategy (one .cjs at a time, chunk oversized files by function/export)
    - lib/core/skillopt-schemas.cjs (ReviewFindingSchema, CodeFindingSchema, UnitRecordSchema, inlineSchemaJson, assertUnderOut, assertPinnedModelId)
  </read_first>
  <action>
    Create scripts/skillopt-codereview.cjs.

    File list: from out/inventory.json backed records, union of backing_refs resolved to real repo paths (dedupe; skip with an explicit not_evaluated record any ref that does not exist on disk -- reason 'spawn_failed' is wrong here, add nothing new: use 'empty_envelope'? No: nonexistent target is recorded with reason 'schema_fail'? Neither fits -- use the existing 'fabricated_evidence' for anchor failures only and record missing files under 'empty_envelope'? NO. Correct approach: a missing backing file is NOT a spawn class at all; record it as UnitRecord status not_evaluated with reason 'spawn_failed' and detail 'backing_ref_missing:<path>' in payload -- the reason enum stays closed, the detail carries specificity). --files <csv> overrides for direct targets (smoke run reviews scripts/check-card-fire.cjs directly regardless of which SKILL.md references it).

    chunkFile(fileText, maxLines=800): split on top-level function/const boundaries (regex on /^(function |const [A-Za-z_$]+\s*=|class )/m), pack chunks up to maxLines, never split inside a function; each chunk carries its start-line offset so evidence anchors resolve.

    Pass 1 reviewOneFile: spawn per file (or per chunk), pinned FULL opus id (copy the pinned opus id from the huji config surface, e.g. the claude-opus-4-8 form STATE records; assertPinnedModelId preflights), --append-system-prompt-file skillopt-review-rubric.md, -p carries the FILE PATH (the session Reads it -- path-not-inline, AI-SPEC Core Pattern 1) + the skill name + chunk line range, --output-format json, --json-schema inlineSchemaJson(array-of-ReviewFindingSchema wrapper), --allowedTools 'Read,Glob,Bash(node scripts/*)', --max-turns 6, --max-budget-usd config.reviewBudgetUsd (default 2.00), --permission-mode dontAsk, --no-session-persistence, timeout 600000.

    anchorEvidence(finding, fileText): DETERMINISTIC -- finding.evidence_quote must be a verbatim substring of the real file text (exact bytes, no normalization); on failure the finding is dropped with a UnitRecord not_evaluated reason 'fabricated_evidence' naming the finding -- dropped findings are logged, never silently vanished (D4 + D5 together).

    Pass 2 refuteFindings: only anchor-surviving findings; a SEPARATE isolated spawn (same opus pinning, refute rubric frozen file, -p carries file path + findings JSON path written under out/review/, --json-schema inlineSchemaJson(array-of-CodeFindingSchema wrapper), same allowlist/fuses). Output merge: every input finding must come back with a verdict; a missing verdict for a submitted finding is a schema_fail for the whole refutation unit (retry once, then the FILE lands not_evaluated -- the pipeline never promotes an unrefuted finding by default).

    runCodeReview: sequential over files (the subset is ~10-20; opus calls are heavy -- concurrency 1-2 via runPool copied from huji-batch, cap 2), .done sentinel per file, one retry per pass on transport/schema failure then not_evaluated. Write findings-raw.json (all verdicts, feeds the survival-rate metric confirmed/(confirmed+refuted)) and findings.json (confirmed + plausible only). Both via assertUnderOut + writeAtomic.

    Selftest (zero spawn): anchorEvidence drops a paraphrased-quote fixture and passes an exact-substring fixture (build the fixture from a real line of scripts/check-card-fire.cjs read at selftest time, proving the anchor runs against real file bytes); refuted fixture excluded from findings.json but present in findings-raw.json; missing-verdict refutation fixture -> file not_evaluated; chunker fixture: an oversized synthetic file splits on function boundaries with correct line offsets; dry-run arg vector contains no Write and no Edit in --allowedTools.
  </action>
  <verify>
    <automated>node scripts/skillopt-codereview.cjs --selftest && node scripts/skillopt-codereview.cjs --dry-run --files scripts/check-card-fire.cjs | grep -q -- '--no-session-persistence' && ! node scripts/skillopt-codereview.cjs --dry-run --files scripts/check-card-fire.cjs | grep -qE 'allowedTools.*(Write|Edit)'</automated>
  </verify>
  <acceptance_criteria>
    - --selftest exits 0 on all five fixtures (anchor pass, anchor drop, refuted exclusion, missing-verdict not_evaluated, chunker offsets).
    - findings.json fixture contains only confirmed/plausible; findings-raw.json retains the refuted one (behavior assertion via selftest).
    - Dry-run arg vectors: review pass cites skillopt-review-rubric.md, refute pass cites skillopt-refute-rubric.md, both carry inline --json-schema without '$schema', allowlist exactly Read,Glob,Bash(node scripts/*).
    - anchorEvidence is called on every finding before refutation (grep -v '^\s*//' scripts/skillopt-codereview.cjs | grep -c 'anchorEvidence' returns >= 2 -- definition + call site).
  </acceptance_criteria>
  <done>WS2 machinery complete: two hermetic opus passes, deterministic anti-fabrication anchor, refuted findings dropped-but-logged, review-only tool surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewed .cjs content -> reviewer/refuter subagents | first-party but 72KB-class files with arbitrary strings flow into review sessions -- prompt-injection surface (a reviewed file could contain text addressed to the reviewer) |
| reviewer allowlist includes Bash(node scripts/*) | the subagent may EXECUTE first-party scripts while reviewing them |
| findings -> report | a confident false positive that reaches the report burns maintainer trust (the curl/HackerOne DoS class from AI-SPEC 1b) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-12 | Tampering (prompt injection) | reviewed file text inside review session | mitigate | file passed as PATH not inline; rubric instructs code-is-data; output schema-forced; no Write/Edit in allowlist so injected instructions cannot mutate anything; evidence anchor rejects invented content |
| T-230-13 | Elevation of privilege | Bash(node scripts/*) in reviewer allowlist | accept | AI-SPEC Section 4 prescribes it (running a script can ground a finding); scripts are first-party repo code already executed by CI; bounded by --max-budget-usd, --max-turns 6, 600s timeout, no Write/Edit; residual risk accepted and named |
| T-230-14 | Spoofing | fabricated evidence_quote | mitigate | deterministic verbatim-substring anchor BEFORE refutation; failures dropped as fabricated_evidence and logged (D4) |
| T-230-15 | Repudiation | refuted findings silently downgraded or vanished | mitigate | findings-raw.json retains every verdict for the survival-rate metric; report file carries survivors only; missing verdict = unit not_evaluated (D5) |
| T-230-SC | Tampering | package installs | accept (N/A) | zero installs this phase |
</threat_model>

<verification>
- --selftest exits 0 with zero API spend; anchor fixture proven against real scripts/check-card-fire.cjs bytes.
- Dry-run arg vectors match the AI-SPEC tool-use contract for WS2.
- git status shows only the three new files.
</verification>

<success_criteria>
- Refute-or-Promote pipeline exists with the evidence anchor as deterministic code, not a model judgment.
- Reviewer is structurally findings-only (rubric) and write-incapable (allowlist).
- All failure paths land as explicit not_evaluated units; nothing is absorbed.
</success_criteria>

<output>
Create `.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-04-SUMMARY.md` when done.
</output>
