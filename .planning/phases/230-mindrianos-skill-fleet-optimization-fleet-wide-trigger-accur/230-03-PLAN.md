---
phase: 230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur
plan: 03
type: execute
wave: 2
depends_on: [230-01]
files_modified:
  - scripts/skillopt-triggerloop.cjs
  - references/methodology/skillopt-revise-rubric.md
autonomous: true
requirements: [D2, D3, D7]
must_haves:
  truths:
    - "A REAL Skill-tool fire is observed ONLY via --output-format stream-json --verbose JSONL scanning; any attempt to read triggers off the --output-format json envelope is blocked as unverified (the single most load-bearing detail in the phase, AI-SPEC Pitfall 1)"
    - "The Skill tool_use input field name is pinned from a LIVE captured JSONL, never assumed from documentation"
    - "Description candidates are tested inside a scratch plugin checkout under out/ -- the real skills/ tree is never touched by the loop (CFM4)"
    - "Best description is selected by validation pass-rate, never the last iteration; any candidate that regresses >= 1 previously-passing validation query is blocked (regressed_query_count == 0 hard gate, D3/CFM1)"
  artifacts:
    - path: scripts/skillopt-triggerloop.cjs
      provides: "WS1.5 flagged-only real trigger-test loop: preflight, stream-json Skill-fire detector, scratch-checkout revision loop, best-by-validation selection"
      exports: [preflightPluginLoad, detectSkillFire, runTriggerQuery, runLoopForSkill, SKILL_INPUT_FIELD]
      min_lines: 260
    - path: references/methodology/skillopt-revise-rubric.md
      provides: "frozen instruction for the description-revision subagent (train failures only, never validation results)"
  key_links:
    - from: scripts/skillopt-triggerloop.cjs
      to: "spawned claude subagent (stream-json)"
      via: "spawnSync('claude', [...'--output-format','stream-json','--verbose'...]) + JSONL line scan"
      pattern: "stream-json"
    - from: scripts/skillopt-triggerloop.cjs
      to: lib/core/skillopt-schemas.cjs
      via: "require -- TriggerResultSchema, LoopSummarySchema, DescriptionRevisionSchema, assertUnderOut"
      pattern: "require\\(.*skillopt-schemas"
    - from: scripts/skillopt-triggerloop.cjs
      to: "out/scratch/<skill>/ plugin checkout"
      via: "--plugin-dir pointed at the scratch copy carrying the candidate description"
      pattern: "plugin-dir"
---

<objective>
Build Workstream 1 step 5: the literal real trigger-test loop for flagged skills only (CONTEXT.md Rigor decision) -- actual Skill-tool invocation observed via stream-json, train/validation split, up to 5 description-revision iterations, best-by-validation selection with the regressed_query_count == 0 do-no-harm gate.

Purpose: the funnel (Plan 02) predicts; this loop MEASURES. AI-SPEC Pitfall 1 makes the detection mechanism the phase's most load-bearing detail: the json envelope has no reliable .messages[] tool trace, so a genuine fire is only observable in the stream-json JSONL. This plan pins that mechanism against a LIVE capture (one cheap spawn) before any loop logic trusts it.

Output: scripts/skillopt-triggerloop.cjs + the revision rubric, with a live-captured JSONL fixture proving the detector on both a firing and a non-firing case.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-AI-SPEC.md
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-PATTERNS.md
@lib/core/skillopt-schemas.cjs
@scripts/huji-batch.cjs
</context>

## Artifacts this phase produces (Plan 03 slice)

New files: `scripts/skillopt-triggerloop.cjs`, `references/methodology/skillopt-revise-rubric.md`.
New exported symbols: `preflightPluginLoad(config)`, `detectSkillFire(jsonlText, expectedSkill)`, `runTriggerQuery({skill, query, scratchDir, config})`, `runLoopForSkill({skill, queries, config})`, `SKILL_INPUT_FIELD` (the live-verified input field constant).
New CLI flags: `--capture <skill> <query>` (live JSONL capture), `--skills <csv>`, `--out <dir>`, `--max-iterations <n>` (default 5, hard cap 5), `--runs-per-query <n>` (default 3), `--selftest`, `--dry-run`.
New artifact shapes on disk: `out/captures/<name>.jsonl` (live stream-json captures), `out/scratch/<skill>/` (scratch plugin checkout per flagged skill), `out/loop/<skill>/iteration-<n>.json` (TriggerResult array per iteration), `out/loop/<skill>/summary.json` (LoopSummary).

<tasks>

<task type="auto">
  <name>Task 1: preflight + live stream-json capture + Skill-fire detector</name>
  <files>scripts/skillopt-triggerloop.cjs</files>
  <read_first>
    - scripts/huji-batch.cjs lines 162-192 (preflightPluginLoad -- the ONLY stream-json JSONL-parsing precedent in the repo; copy the line-split + JSON.parse-per-line + system/init assertion shape near-verbatim)
    - 230-AI-SPEC.md Section 3 Common Pitfalls 1 (json envelope has NO .messages[] trace -- stream-json --verbose is mandatory) and Section 4 Core Pattern final paragraph (the tool_use block shape { type:'tool_use', name:'Skill', input:{...} } and the explicit instruction: verify the input field name against a live capture before hard-coding)
    - 230-PATTERNS.md "Pattern Assignments > scripts/skillopt-triggerloop.cjs" (the JSONL scan skeleton and the preflight abort-closed rule)
    - lib/core/skillopt-schemas.cjs (TriggerResultSchema, UnitRecordSchema, assertUnderOut, PHASE_OUT_DIR)
  </read_first>
  <action>
    Create scripts/skillopt-triggerloop.cjs part 1 of 2 (Task 2 adds the loop).

    preflightPluginLoad(config): copy the huji-batch 162-192 shape -- spawn claude with --output-format stream-json --verbose plus a trivial -p, scan JSONL for the system/init event, assert plugins contains the mos plugin AND plugin_errors is empty; on failure return { ok:false, reason:'plugin_preflight_failed' } and the caller ABORTS the whole run closed before spending anything (AI-SPEC Section 6 Plugin preflight guardrail, D7).

    detectSkillFire(jsonlText, expectedSkill): split on newline, JSON.parse per line (skip unparseable lines but COUNT them -- more than 20% unparseable lines returns { ok:false, reason:'jsonl_corrupt' }, never a silent zero-fire); scan assistant events for message.content[] blocks with type 'tool_use' and name 'Skill'; extract the invoked skill identifier from the block's input object via the SKILL_INPUT_FIELD constant; return { fired:boolean, invoked:string|null, blocks:[raw matched blocks] }. Match tolerance: compare both the bare skill name and the namespaced command form (mos:find-connections) since the AI-SPEC explicitly warns the field may surface either.

    LIVE CAPTURE (the one live step in this plan, cheap by design): implement --capture <skill> <query> which runs preflightPluginLoad, then spawns ONE subagent with --output-format stream-json --verbose, --plugin-dir <repo root>, --permission-mode dontAsk, --max-turns 4, --max-budget-usd 0.50, --no-session-persistence, DEFAULT tool set (no --allowedTools restriction -- real progressive-disclosure conditions per AI-SPEC Section 4 Tool Use), -p carrying the query text only. Write raw JSONL to out/captures/<skill>.jsonl (assertUnderOut first). THEN execute it for real, twice: (a) a query hand-written to clearly fire a well-known skill (pick one obvious candidate from skills/, e.g. a distinctive mos: skill whose description names concrete trigger phrases -- choose from the real inventory, record the choice in the SUMMARY), (b) an off-topic query (weather-smalltalk style) expected to fire nothing. Inspect capture (a), pin SKILL_INPUT_FIELD to the actual field name observed in the real tool_use input object, and record the capture filename + the raw block in a header comment beside the constant. If capture (a) shows NO Skill tool_use block, STOP and record the blocker in the SUMMARY (the detector cannot be pinned blind -- do not guess the field name); try one alternative obviously-triggering skill first before declaring the blocker.

    Selftest (--selftest, zero spend): run detectSkillFire against the two REAL captured JSONL files committed as fixtures under out/captures/ -- (a) must return fired:true with the expected skill, (b) must return fired:false; plus a synthetic corrupt-JSONL fixture returning jsonl_corrupt; plus an integrity guard test: a helper assertStreamJsonArgs(args) must reject any trigger-run arg vector whose --output-format is not stream-json (the D7 guardrail: trigger-rate computation from a json envelope is blocked as unverified).
  </action>
  <verify>
    <automated>node scripts/skillopt-triggerloop.cjs --selftest && ls .planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/out/captures/*.jsonl | wc -l | grep -qv '^0$' && grep -v '^\s*//' scripts/skillopt-triggerloop.cjs | grep -q 'stream-json'</automated>
  </verify>
  <acceptance_criteria>
    - Two real captures exist under out/captures/ (one firing, one non-firing), produced by live spawns during this task.
    - SKILL_INPUT_FIELD is a named constant whose value was read from the firing capture, with the source capture filename in an adjacent comment (source assertion: grep -A2 'SKILL_INPUT_FIELD' names a .jsonl file).
    - --selftest exits 0: firing fixture -> fired:true + correct skill; non-firing fixture -> fired:false; corrupt fixture -> jsonl_corrupt (not fired:false); assertStreamJsonArgs rejects an --output-format json arg vector.
    - preflightPluginLoad failure path returns ok:false and no capture spawn happens after a failed preflight (fixture-tested via an injected bad init event).
  </acceptance_criteria>
  <done>The Skill-fire detection mechanism is pinned against reality: live captures on disk, field name verified not assumed, json-envelope path structurally blocked.</done>
</task>

<task type="auto">
  <name>Task 2: scratch-checkout revision loop with best-by-validation selection</name>
  <files>scripts/skillopt-triggerloop.cjs, references/methodology/skillopt-revise-rubric.md</files>
  <read_first>
    - 230-AI-SPEC.md Section 1b Dimension 3 (do-no-harm: best-by-validation, zero regressed queries) and Section 5 D3 row (regressed_query_count == 0 is a hard gate) and Section 4 "State Management" (scratch artifacts under out/, .done sentinels)
    - docs/superpowers/specs/2026-07-17-mindrian-skill-optimization-design.md "Workstream 1 -- 5. Full trigger-test loop" (the authoritative loop definition: ~20 queries, 3 runs each, revise against train failures only, up to 5 iterations, best-by-validation) and "Error handling" (non-convergence marking)
    - scripts/huji-batch.cjs lines 60-64 (writeAtomic) + 200-229 (runPool for the 3-runs-per-query fan-out)
    - lib/core/skillopt-schemas.cjs (LoopSummarySchema, DescriptionRevisionSchema, TriggerResultSchema, inlineSchemaJson)
  </read_first>
  <action>
    Create references/methodology/skillopt-revise-rubric.md: frozen instruction for the revision subagent -- you receive one skill's current description plus its TRAIN-set failures only (query, expected, what actually fired); rewrite the description as a routing rule: WHAT the skill does AND WHEN to invoke it (concrete phrases/situations), distinct from the named sibling stubs provided; never see or optimize against validation results (overfitting guard, AI-SPEC 1b Dimension 3); output JSON per the provided schema. No em-dashes.

    Extend scripts/skillopt-triggerloop.cjs with the loop:

    Scratch checkout: buildScratchCheckout(skill, description, scratchDir) copies the plugin's skill-loading surface (the skills/ tree plus .claude-plugin/plugin.json and whatever minimal root files claude --plugin-dir needs -- mirror what scripts/huji-pin-smoketest.cjs proved sufficient for --plugin-dir resolution) into out/scratch/<skill>/, then rewrites ONLY that skill's SKILL.md description frontmatter inside the scratch copy. assertUnderOut on scratchDir before any copy; the REAL skills/ tree is never written (CFM4). Full roster present in the scratch copy so progressive disclosure runs under real conditions (AI-SPEC Section 4 Tool Use).

    runTriggerQuery: one query, config.runsPerQuery (3) spawns via runPool (concurrency from config, cap 4), each spawn stream-json --verbose with --plugin-dir <scratch checkout>, default tool set, --max-turns 4, --max-budget-usd config.triggerBudgetUsd (default 0.25), --no-session-persistence, timeout 180000; assertStreamJsonArgs preflights the arg vector (Task 1 guard); per run detectSkillFire; trigger_rate = fires/runs; spawn/timeout/corrupt-JSONL failures land as UnitRecord not_evaluated (kind 'trigger'), and a query with any not_evaluated run reports its rate over completed runs only with completed_runs recorded -- never inflated.

    runLoopForSkill: iteration 0 = baseline description measured on train + validation; then up to config.maxIterations (5, hard cap): spawn the revision subagent (pinned sonnet, revise rubric frozen file, --json-schema inlineSchemaJson(DescriptionRevisionSchema), --allowedTools Read, --max-turns 2, train failures ONLY in -p), rebuild scratch checkout with the candidate, re-measure train + validation. Selection: best iteration by VALIDATION pass-rate (a query passes when trigger_rate >= 2/3 for should_trigger, and fires 0 for should_not_trigger); compute regressed_query_count = count of validation queries that passed at iteration 0 but fail in the candidate; if the best candidate has regressed_query_count > 0 the proposed diff is BLOCKED and the summary records blocked_by_regression with the query list (D3 hard gate -- never silently propose it). converged=false when 5 iterations run without a validation improvement; keep best-by-validation anyway and mark did-not-converge (design spec Error handling). Write out/loop/<skill>/iteration-<n>.json + summary.json (LoopSummarySchema-valid, writeAtomic).

    Selftest additions (zero spawn, fixture-driven with canned TriggerResult sets): best-by-validation picks iteration 2 when rates are [0.5, 0.7, 0.9, 0.8] even though iteration 3 is last; a candidate with higher aggregate rate but one regressed validation query is blocked; 5-iteration no-improvement fixture yields converged:false with baseline kept; scratch-checkout path targeting skills/ (simulated) is rejected by assertUnderOut.
  </action>
  <verify>
    <automated>node scripts/skillopt-triggerloop.cjs --selftest && node scripts/skillopt-triggerloop.cjs --dry-run --skills mos:find-connections | grep -q 'stream-json' && grep -v '^\s*//' scripts/skillopt-triggerloop.cjs | grep -c 'regressed_query_count' | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Selftest exits 0 on all four loop fixtures (best-by-validation not-last, regression block, non-convergence, scratch containment).
    - LoopSummary fixture output validates against LoopSummarySchema with selected_by 'validation_pass_rate'.
    - Revision spawn arg vector (dry-run) shows --allowedTools Read, --max-turns 2, inline --json-schema, and the -p payload builder provably excludes validation results (selftest asserts the built prompt for a fixture contains train queries and none of the validation query strings).
    - Trigger spawn arg vector (dry-run) shows --output-format stream-json --verbose and a --plugin-dir under out/scratch/ (never the repo root skills/ for candidate testing).
  </acceptance_criteria>
  <done>The full flagged-skill loop exists and is fixture-proven: real-fire measurement, train-only revision, best-by-validation selection, do-no-harm regression block, honest non-convergence marking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated queries + revised descriptions -> live subagents with DEFAULT tool set | trigger-test subagents run un-allowlisted (real conditions) against the user's keychain -- the widest subagent surface in the phase |
| candidate descriptions -> scratch plugin checkout | pipeline-written SKILL.md variants must stay inside out/scratch/, never the shipped skills/ tree |
| documented field names -> detector logic | agentskills.io's input.skill claim is untrusted until a live capture confirms it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-08 | Elevation of privilege | trigger-test subagent (default tools, dontAsk) | mitigate | --max-turns 4 + --max-budget-usd 0.25 + 180s timeout + --no-session-persistence per run; the -p payload is a single short eval query; runs execute against a scratch checkout, and this plan only ever runs the two capture spawns live (the flagged-loop volume waits for the deferred opt-in) |
| T-230-09 | Tampering | scratch checkout writes | mitigate | assertUnderOut fail-closed before every copy/rewrite; real skills/ never a write target; selftest includes the containment rejection fixture (CFM4/D6) |
| T-230-10 | Spoofing | trigger reads off the json envelope (unverifiable .messages[]) | mitigate | assertStreamJsonArgs structurally blocks non-stream-json trigger runs; detector only accepts JSONL input (D7 integrity guardrail) |
| T-230-11 | Repudiation | partial runs inflating trigger rates | mitigate | not_evaluated runs excluded from the numerator AND denominator with completed_runs recorded; corrupt JSONL is jsonl_corrupt, never fired:false (D5) |
| T-230-SC | Tampering | package installs | accept (N/A) | zero installs this phase |
</threat_model>

<verification>
- --selftest exits 0 (detector fixtures from REAL captures + loop fixtures, zero further spend).
- Two live captures on disk under out/captures/ with the pinned SKILL_INPUT_FIELD traceable to them.
- git status shows only the two new files (plus out/ artifacts, which are phase-dir contents).
</verification>

<success_criteria>
- Skill-fire detection is live-verified, not doc-assumed (D7's detector leg satisfied ahead of Plan 07's smoke).
- Do-no-harm gate (regressed_query_count == 0) and best-by-validation selection are enforced in code with reject fixtures.
- Nothing in this plan can write to skills/ or scripts/ -- containment fixture proves it.
</success_criteria>

<output>
Create `.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-03-SUMMARY.md` when done.
</output>
