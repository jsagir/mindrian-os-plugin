---
phase: 230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur
plan: 06
type: execute
wave: 3
depends_on: [230-02, 230-03, 230-04]
files_modified:
  - scripts/skillopt-merge.cjs
  - scripts/skillopt-eval.cjs
  - tests/run-all-230.sh
autonomous: true
requirements: [D3, D4, D5, D6]
must_haves:
  truths:
    - "One merged report carries per-skill trigger verdicts (unchanged / proposed diff / not_evaluated / did-not-converge) plus CONFIRMED-and-PLAUSIBLE-only WS2 findings, then STOPs -- no apply mode exists anywhere in this phase (CONTEXT.md Safety decision, CFM4)"
    - "The merge exits nonzero when spawned != ok + not_evaluated across the unit ledgers (D5 reconciliation identity)"
    - "bash tests/run-all-230.sh is the deterministic phase gate: zero API spend, every D-dimension code leg asserted, file-guarded SKIPs, nonzero exit on any FAIL (run-all-229 doctrine)"
    - "A proposed diff with regressed_query_count != 0 anywhere in the artifacts fails the eval harness (D3 hard gate re-checked at the aggregation layer, independent of the loop's own gate)"
  artifacts:
    - path: scripts/skillopt-merge.cjs
      provides: "report aggregator + terminal human-approval STOP (CFM4)"
      exports: [runMerge, buildReport]
      min_lines: 160
    - path: scripts/skillopt-eval.cjs
      provides: "deterministic eval harness: schemas roundtrip, reconciliation, do-no-harm, write-path, evidence-quote, smoke-replay agreement"
      exports: [checkReconciliation, checkDoNoHarm, checkWritePath, checkEvidenceQuotes, checkSmokeAgreement]
      min_lines: 200
    - path: tests/run-all-230.sh
      provides: "the phase's single PASS/FAIL/SKIP aggregator gate wired into repo test convention"
  key_links:
    - from: scripts/skillopt-merge.cjs
      to: "out/funnel/funnel-results.json + out/loop/*/summary.json + out/review/findings.json"
      via: "aggregation inputs"
      pattern: "funnel-results"
    - from: scripts/skillopt-eval.cjs
      to: lib/core/skillopt-schemas.cjs
      via: "require -- every schema + assertUnderOut re-used as the D6 assertion"
      pattern: "require\\(.*skillopt-schemas"
    - from: tests/run-all-230.sh
      to: "all skillopt selftests + skillopt-eval --suite code"
      via: "run/run_if legs"
      pattern: "skillopt-eval"
---

<objective>
Close the harness: the merge step that produces the ONE human-gated report (and stops), the deterministic eval harness that re-checks every critical gate over the on-disk artifacts, and the phase's run-all-230.sh aggregator wired into repo test convention.

Purpose: the guardrails built inside Plans 02-04 fire per-unit at run time; this plan adds the INDEPENDENT after-the-fact layer -- skillopt-eval re-derives D3/D4/D5/D6 from the artifacts alone, so a bug in a pipeline script cannot silently vouch for itself (the same two-layer discipline as huji-eval over huji-run-one).

Output: scripts/skillopt-merge.cjs, scripts/skillopt-eval.cjs, tests/run-all-230.sh -- all runnable green on fixture artifacts with zero API spend.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-AI-SPEC.md
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-PATTERNS.md
@lib/core/skillopt-schemas.cjs
@scripts/huji-eval.cjs
@scripts/huji-batch.cjs
@tests/run-all-229.sh
</context>

## Artifacts this phase produces (Plan 06 slice)

New files: `scripts/skillopt-merge.cjs`, `scripts/skillopt-eval.cjs`, `tests/run-all-230.sh`.
New exported symbols: `runMerge({outDir})`, `buildReport(artifacts)` (merge); `checkReconciliation(outDir)`, `checkDoNoHarm(outDir)`, `checkWritePath(outDir)`, `checkEvidenceQuotes(outDir)`, `checkSmokeAgreement(outDir, labelsPath, tolerance)` (eval).
New CLI flags: merge gets `--out <dir>`, `--selftest`; eval gets the huji-eval-style switch-case router `--check <name>`, `--suite code`, `--selftest`, `--tolerance <0..1>` (smoke agreement bar, default 0.85).
New artifact shapes on disk: `out/report/skillopt-report.md` (the human-readable merged report), `out/report/report.json` (machine form), both terminal -- the merge prints the itemized approval checklist and exits.

<tasks>

<task type="auto">
  <name>Task 1: skillopt-merge.cjs -- aggregate, reconcile, STOP at the human gate</name>
  <files>scripts/skillopt-merge.cjs</files>
  <read_first>
    - scripts/huji-batch.cjs lines 60-64 (writeAtomic), 272-278 (haltBatch STOP-and-page shape), 381-386 (runAggregation)
    - 230-AI-SPEC.md Section 1 "Output Consequence" (what the report is FOR: approvable without re-verifying every claim), Section 6 "Human-approval gate (terminal)", Section 7 "Key Metrics to Track" (the per-run metrics the report must surface)
    - 230-PATTERNS.md "Pattern Assignments > scripts/skillopt-merge.cjs"
    - lib/core/skillopt-schemas.cjs (LoopSummarySchema, CodeFindingSchema, UnitRecordSchema, assertUnderOut)
  </read_first>
  <action>
    Create scripts/skillopt-merge.cjs. Inputs (all optional-with-explicit-absence: a missing input renders as a NOT RUN section, never an empty-implies-clean section): out/funnel/funnel-results.json, out/loop/*/summary.json, out/review/findings.json + findings-raw.json, out/funnel/units/ + out/review/units/ ledgers.

    buildReport produces report.json + skillopt-report.md with these sections: (1) run metrics header per AI-SPEC Section 7 -- fleet trigger-accuracy rate, flag rate, not-evaluated rate, CONFIRMED finding count + survival rate confirmed/(confirmed+refuted), per-flagged-skill validation lift, total cost vs projection when present; (2) per-skill WS1 verdicts: unchanged (pass) / proposed description diff (old + new description, train and validation rates before/after, regressed_query_count rendered and equal to 0, converged flag with did-not-converge marked honestly) / not_evaluated with reason / blocked_by_regression when the loop blocked a diff; (3) WS2 findings: CONFIRMED and PLAUSIBLE only, each with severity, claim, verbatim evidence_quote, refutation_attempt; refuted count shown as a metric only; (4) EVERY not_evaluated unit listed with its reason (AI-SPEC sampling rule: every not_evaluated unit gets eyes); (5) the terminal ITEMIZED APPROVAL CHECKLIST: one checkbox line per proposed description diff and per surviving finding, then a STOP banner stating that real SKILL.md / script writes happen only after explicit itemized human sign-off, outside this pipeline.

    Hard behaviors: reconciliation assertion spawned === ok + not_evaluated across every units ledger, exit 1 with the delta named on violation (D5); report writes go through assertUnderOut + writeAtomic; the script contains NO code path that writes to skills/ or scripts/ and NO apply or write-back flag at all -- applying approved changes is future post-approval work outside this phase (CONTEXT.md Deferred Ideas). Report prose: Feynman-simplified, JTBD-oriented, LTR, no em-dashes (house style, AI-SPEC 1b Regulatory).

    --selftest with canned artifact fixtures: (a) a not_evaluated funnel unit appears in section 4 AND is excluded from the accuracy-rate numerator and denominator; (b) a refuted finding appears in no findings section but is counted in the survival-rate metric; (c) a broken-reconciliation fixture exits 1; (d) a missing loop dir renders NOT RUN; (e) a LoopSummary fixture with blocked_by_regression renders as blocked, not as a proposed diff.
  </action>
  <verify>
    <automated>node scripts/skillopt-merge.cjs --selftest && grep -v '^\s*//' scripts/skillopt-merge.cjs | grep -qc 'assertUnderOut' && ! grep -v '^\s*//' scripts/skillopt-merge.cjs | grep -qE -- '--apply|writeBack|apply-approved'</automated>
  </verify>
  <acceptance_criteria>
    - --selftest exits 0 on all five fixtures.
    - No apply/write-back CLI flag or code path exists (grep gate above); the only write targets are under out/report/.
    - The STOP banner and itemized checklist render in the fixture report (selftest asserts both substrings).
    - Reconciliation violation exits 1 naming the delta.
  </acceptance_criteria>
  <done>The terminal aggregation step exists: one honest report, one human gate, zero write capability toward real skills or scripts.</done>
</task>

<task type="auto">
  <name>Task 2: skillopt-eval.cjs -- the independent deterministic gate layer</name>
  <files>scripts/skillopt-eval.cjs</files>
  <read_first>
    - scripts/huji-eval.cjs (the exact analog: switch-case --check/--suite/--selftest argv router, deterministic code legs, file-guarded clean SKIPs -- header lines 1-40 plus the router)
    - 230-AI-SPEC.md Section 5 "CI/CD Integration" (the six deterministic checks this file implements, verbatim list) and Section 5 "Eval Tooling" (code layer definition)
    - lib/core/skillopt-schemas.cjs (every schema, NOT_EVALUATED_REASONS, assertUnderOut)
    - 230-PATTERNS.md "Pattern Assignments > scripts/skillopt-eval.cjs"
  </read_first>
  <action>
    Create scripts/skillopt-eval.cjs with the huji-eval switch-case router. Checks (each independently runnable via --check <name>, all bundled under --suite code; each SKIPs cleanly with an explicit SKIP line when its input artifact does not exist yet -- file-guarded SKIP doctrine, never a fake PASS):

    1. schemas-roundtrip: node lib/core/skillopt-schemas.cjs selftest exits 0 AND every emitted JSON Schema file (toJsonSchemas output) contains no '$schema' key.
    2. reconciliation (D5): re-derive from disk -- count unit files under out/funnel/units/ and out/review/units/, group by status, assert spawned === ok + not_evaluated per ledger AND every not_evaluated unit carries a reason from NOT_EVALUATED_REASONS.
    3. do-no-harm (D3): every out/loop/*/summary.json parses as LoopSummarySchema; any summary carrying a proposed_description has regressed_query_count === 0; any nonzero regression count must appear only with blocked_by_regression semantics (no proposed_description). FAIL loudly otherwise.
    4. write-path (D6): walk every file under out/ recording realpaths; assert none resolves under skills/ or scripts/ or lib/ (symlink-aware via fs.realpathSync); additionally assert out/scratch/ checkouts contain no symlink pointing back into the repo skills/ tree.
    5. evidence-quote (D4): for every finding in out/review/findings.json, re-read the REAL named .cjs file and assert evidence_quote is a verbatim substring -- an independent re-check of Plan 04's anchor, running against current disk bytes.
    6. smoke-replay (D7): checkSmokeAgreement -- join out/funnel/funnel-results.json per-skill verdicts against smoke-labels.json expected_funnel (records with a non-null expected_funnel); agreement = matches/comparable; PASS at >= tolerance (default 0.85, --tolerance overridable); print the per-skill agreement table; the not_evaluated_probe record must have landed not_evaluated for the check to pass (probe verification is part of D7, wired to D5).

    --selftest: fixture out-dir under a temp path exercising every check both PASS and FAIL directions (broken reconciliation, nonzero regression with proposed_description, an out file symlinked into skills/ simulated via a crafted path record, a paraphrased evidence quote, an agreement below tolerance).
  </action>
  <verify>
    <automated>node scripts/skillopt-eval.cjs --selftest && node scripts/skillopt-eval.cjs --suite code; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - --selftest exits 0 with both PASS and FAIL directions exercised per check (six checks, twelve fixture directions).
    - --suite code on the current repo state exits 0 with explicit SKIP lines for artifacts that do not exist yet (no live artifacts before Plan 07) -- SKIP is printed per missing input, never silent.
    - Every check is pure deterministic code: zero spawnSync('claude') call sites in this file (grep -v '^\s*//' scripts/skillopt-eval.cjs | grep -c "spawnSync('claude'" returns 0).
    - checkSmokeAgreement prints a per-skill table naming each mismatch.
  </acceptance_criteria>
  <done>The independent verification layer exists: every critical failure mode re-derived from artifacts on disk, runnable free on every push.</done>
</task>

<task type="auto">
  <name>Task 3: tests/run-all-230.sh -- the phase aggregator gate</name>
  <files>tests/run-all-230.sh</files>
  <read_first>
    - tests/run-all-229.sh (FULL read -- the run/run_if counters, non-zero-exit-on-FAIL convention, file-guarded SKIP doctrine, the PHASE GATE PART 2 human-checkpoint announcement block at the end; copy the conventions, drop the 229-specific legs)
    - 230-AI-SPEC.md Section 5 "CI/CD Integration" (the deterministic-only mandate: the LLM-judge layer costs money and is explicitly NOT wired here)
  </read_first>
  <action>
    Create tests/run-all-230.sh modeled on run-all-229.sh conventions (same counters, same exit discipline, bash, executable bit). Legs, each a run or file-guarded run_if:

    - 230-01a schemas selftest: node lib/core/skillopt-schemas.cjs
    - 230-01b inventory: node scripts/skillopt-inventory.cjs --selftest, then --check asserting 124 records
    - 230-02a genqueries selftest
    - 230-02b funnel selftest
    - 230-03 triggerloop selftest (run_if guarded on out/captures/ fixtures existing)
    - 230-04 codereview selftest
    - 230-06a merge selftest
    - 230-06b eval suite: node scripts/skillopt-eval.cjs --suite code

    Header comment: this script is the AUTOMATED half of the phase gate, deterministic and zero-API-spend by design (AI-SPEC: the funnel/loop/review LLM layer runs on demand inside a pipeline run, never on push). End block announces PHASE GATE PART 2, the human legs: Jonathan's smoke pre-labels (Plan 05 checkpoint) + the smoke calibration review (Plan 07 checkpoint) + the future full-fleet opt-in that stays OUTSIDE this phase (CONTEXT.md Execution mechanism decision). chmod +x. No em-dashes.
  </action>
  <verify>
    <automated>bash tests/run-all-230.sh; test $? -eq 0 && test -x tests/run-all-230.sh && ! grep -P '\x{2014}' tests/run-all-230.sh</automated>
  </verify>
  <acceptance_criteria>
    - bash tests/run-all-230.sh exits 0 at this point in the phase (selftest legs PASS, artifact-dependent legs SKIP with explicit lines).
    - FAIL in any leg produces nonzero exit (assert by temporarily injecting a false leg during development, then removing it).
    - The PART 2 human-gate announcement block is present and names the deferred full-fleet opt-in.
    - Zero spawnSync/claude invocations anywhere in the legs (grep for "claude -p" and "spawnSync" over the script returns nothing outside comments).
  </acceptance_criteria>
  <done>One command gates the whole phase deterministically; the paid layers stay on-demand behind the smoke calibration and the future opt-in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline artifacts -> merged report -> maintainer trust | the report is approved FROM; a self-vouching pipeline (guardrails only inside the scripts being graded) would let one bug fabricate a clean report |
| out/ artifacts -> real repo tree | the last structural line where pipeline output could leak into skills/ or scripts/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-18 | Repudiation | report absorbing missing/failed workstreams | mitigate | missing inputs render NOT RUN sections; reconciliation identity exits 1; every not_evaluated unit listed by name (D5/CFM3) |
| T-230-19 | Tampering | write-back path from merge | mitigate | no apply flag or code path exists in this phase at all; grep-gated in verify; report writes assertUnderOut-contained (CFM4/D6) |
| T-230-20 | Tampering | out/ escape via symlink | mitigate | checkWritePath resolves realpaths (fs.realpathSync) and rejects any resolution under skills/, scripts/, lib/ |
| T-230-21 | Spoofing | pipeline scripts vouching for themselves | mitigate | skillopt-eval re-derives D3/D4/D5/D6 from disk artifacts independently of the scripts that produced them (two-layer huji-eval discipline) |
| T-230-SC | Tampering | package installs | accept (N/A) | zero installs this phase |
</threat_model>

<verification>
- All three selftests + bash tests/run-all-230.sh exit 0 with zero API spend.
- node scripts/doctor.cjs --acceptance still green (repo-wide roll-up, CLAUDE.md Verification).
- git status shows only the three new files.
</verification>

<success_criteria>
- The merged-report STOP gate, the independent eval layer, and the phase aggregator all exist and are green on fixtures.
- D3, D4, D5, D6 each enforced twice: once inside the pipeline scripts (Plans 02-04), once independently over artifacts (this plan).
</success_criteria>

<output>
Create `.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-06-SUMMARY.md` when done.
</output>
