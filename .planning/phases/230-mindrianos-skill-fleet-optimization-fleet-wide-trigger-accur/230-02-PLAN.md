---
phase: 230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur
plan: 02
type: execute
wave: 2
depends_on: [230-01]
files_modified:
  - scripts/skillopt-genqueries.cjs
  - scripts/skillopt-funnel.cjs
  - references/methodology/skillopt-queries-rubric.md
  - references/methodology/skillopt-judge-rubric.md
autonomous: true
requirements: [D1, D2, D5]
must_haves:
  truths:
    - "One sonnet subagent per family sees every sibling stub at once, so negative eval queries are real near-misses (mos:find-connections vs mos:find-analogies), not generic noise"
    - "The funnel judges ONE query per isolated spawn against the FULL 124-skill roster (name+description stubs only, never bodies), mirroring real progressive disclosure"
    - "A skill flags on any train-set miss OR any low/medium-confidence verdict -- fail open toward scrutiny, never a silent pass"
    - "A spawn error, timeout, nonzero exit, empty envelope, or safeParse failure after one retry is recorded not_evaluated with its reason; spawned == ok + not_evaluated always reconciles"
  artifacts:
    - path: scripts/skillopt-genqueries.cjs
      provides: "WS1.2 per-family eval-query generation with deterministic stratified train/validation split"
      exports: [generateQueriesForFamily, splitQueries]
      min_lines: 180
    - path: scripts/skillopt-funnel.cjs
      provides: "WS1.3-4 roster-wide judge funnel with runPool concurrency, flagging, unit ledger, .done sentinels"
      exports: [judgeOneQuery, buildRosterStubs, runFunnel]
      min_lines: 220
    - path: references/methodology/skillopt-queries-rubric.md
      provides: "frozen system-prompt instruction for the per-family query generator (prompt-cache stable)"
    - path: references/methodology/skillopt-judge-rubric.md
      provides: "frozen system-prompt instruction for the roster-wide funnel judge (prompt-cache stable)"
  key_links:
    - from: scripts/skillopt-genqueries.cjs
      to: lib/core/skillopt-schemas.cjs
      via: "require -- EvalQuerySetSchema + inlineSchemaJson + UnitRecordSchema"
      pattern: "require\\(.*skillopt-schemas"
    - from: scripts/skillopt-funnel.cjs
      to: "out/inventory.json"
      via: "buildRosterStubs reads name+description per skill"
      pattern: "inventory\\.json"
    - from: scripts/skillopt-funnel.cjs
      to: "spawned claude subagent"
      via: "spawnSync('claude', args) with --json-schema inline + --no-session-persistence"
      pattern: "spawnSync\\('claude'"
---

<objective>
Build Workstream 1's generation and funnel legs: per-family eval-query generation (CONTEXT.md Specifics: near-miss queries exploit REAL sibling confusion, generated per-family so one agent sees every sibling at once) and the cheap roster-wide judge funnel (CONTEXT.md Rigor decision: judge-funnel first, one call per query against the full 124-skill roster, only flagged skills escalate).

Purpose: this is the pipeline's cost-defining design move -- ~800-1,000 sonnet calls at fleet scale instead of agentskills.io's ~7,500-call literal per-skill loop. This plan builds and selftests the machinery deterministically; live spend happens only in Plan 07's smoke run.

Output: two spawn-orchestrating scripts + two frozen rubric files, all selftested with a zero-spend fixture path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-AI-SPEC.md
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-PATTERNS.md
@lib/core/skillopt-schemas.cjs
@scripts/huji-run-one.cjs
@scripts/huji-batch.cjs
</context>

## Artifacts this phase produces (Plan 02 slice)

New files: `scripts/skillopt-genqueries.cjs`, `scripts/skillopt-funnel.cjs`, `references/methodology/skillopt-queries-rubric.md`, `references/methodology/skillopt-judge-rubric.md`.
New exported symbols: `generateQueriesForFamily({family, skills, config})`, `splitQueries(queries, seed)` (genqueries); `judgeOneQuery({query, expected, rosterText, config})`, `buildRosterStubs(inventory)`, `runFunnel({inventory, queriesDir, config})` (funnel).
New CLI flags: both scripts get `--out <dir>`, `--selftest`, `--dry-run`; genqueries adds `--families <csv>` (smoke scoping), funnel adds `--skills <csv>` (smoke scoping), `--concurrency <n>` (default 3, hard cap 4), `--probe-timeout-unit <skill>` (Plan 07's induced not_evaluated probe).
New artifact shapes on disk: `out/queries/<skill>.json` (EvalQuerySet), `out/funnel/units/<unit_id>.json` + `.done` sentinels (UnitRecord), `out/funnel/funnel-results.json` (per-skill verdict: pass | flagged | not_evaluated, plus reconciliation counts).

<tasks>

<task type="auto">
  <name>Task 1: skillopt-genqueries.cjs + queries rubric -- per-family near-miss generation with deterministic split</name>
  <files>scripts/skillopt-genqueries.cjs, references/methodology/skillopt-queries-rubric.md</files>
  <read_first>
    - scripts/huji-run-one.cjs lines 219-269 (buildStageAPrompt + buildStageAArgs -- the frozen-rubric-via---append-system-prompt-file + variable-in--p separation this task copies) and 311-337 (parseEnvelope) and 485-496 (spawnSync with fuses + explicit failure envelopes)
    - lib/core/skillopt-schemas.cjs (EvalQuerySchema, EvalQuerySetSchema, UnitRecordSchema, inlineSchemaJson, PHASE_OUT_DIR, assertUnderOut, assertPinnedModelId, NOT_EVALUATED_REASONS)
    - 230-AI-SPEC.md Section 4b "Prompt Engineering Discipline" (system vs user separation, few-shot = sibling stubs inline) and Section 5 "Reference Dataset" (6-8 queries per skill, train ~60% / validation ~40%, stratified should-trigger / should-not-trigger)
    - 230-PATTERNS.md "Pattern Assignments > scripts/skillopt-genqueries.cjs"
  </read_first>
  <action>
    Create references/methodology/skillopt-queries-rubric.md: the FROZEN generator instruction (stable bytes = prompt-cache bite across every family call, AI-SPEC 4b). Content: you see every skill in ONE namespace family (name + description each); write 6-8 eval queries PER SKILL; each query labeled expected_skill (or null for should-not-trigger); the highest-value negatives are near-misses that share keywords with a SIBLING but need a different skill (agentskills.io guidance, quoted in AI-SPEC 1b Dimension 2); every skill must get at least 2 should_trigger and at least 2 should_not_trigger queries; singleton families get generic-plausible negatives instead of sibling negatives; output ONLY the JSON matching the provided schema. Feynman-plain prose, no em-dashes.

    Create scripts/skillopt-genqueries.cjs. generateQueriesForFamily builds the spawn arg array exactly on the buildStageAArgs shape (discrete args, no shell): -p carries the per-family variable (all sibling stubs as 'name: description' lines + the family name), --append-system-prompt-file references/methodology/skillopt-queries-rubric.md, --plugin-dir <repo root> (keychain auth, NEVER --bare, AI-SPEC Pitfall 4), --model from config (pinned FULL sonnet id -- copy the exact pinned sonnet id from the huji-eval.cjs judge config; assertPinnedModelId preflights it), --output-format json, --json-schema inlineSchemaJson(a families-output wrapper schema: array of EvalQuerySetSchema WITHOUT the split field -- the model labels queries, it never assigns splits), --allowedTools Read, --max-turns 2, --max-budget-usd config.genBudgetUsd (default 0.50), --permission-mode dontAsk, --no-session-persistence. spawnSync with encoding utf8, maxBuffer 8MB, timeout 180000.

    splitQueries(queries, seed): DETERMINISTIC stratified 60/40 train/validation split done in code, never by the model: partition by kind (should_trigger / should_not_trigger), sort each partition by query text, interleave with a seeded index stride so both splits carry a proportional kind mix; ~60% train (round so validation gets at least 2 queries when total >= 5).

    Per-family orchestration: parseEnvelope (copy near-verbatim from huji-run-one 311-337), safeParse against the wrapper schema, ONE retry on spawn error / nonzero / timeout / schema_fail, then record a UnitRecord status not_evaluated with the matching NOT_EVALUATED_REASONS entry (CONTEXT.md Safety decision: never absorbed). On success: apply splitQueries, write out/queries/<skill>.json per skill (assertUnderOut + writeAtomic), write the family UnitRecord (kind 'genqueries', model, session_id, total_cost_usd from the envelope).

    argv router: --families <csv> (default: all families from out/inventory.json), --out, --dry-run (build + print arg vectors, spawn nothing), --selftest (fixtures only, no spawn): splitQueries stratification fixture (10 queries, 6/4 split, both kinds in both splits); a canned bad-envelope fixture must produce status not_evaluated reason schema_fail after the simulated retry; a canned good-envelope fixture must yield per-skill files with split fields present.
  </action>
  <verify>
    <automated>node scripts/skillopt-genqueries.cjs --selftest && node scripts/skillopt-genqueries.cjs --dry-run --families mos:find | grep -q -- '--no-session-persistence' && node scripts/skillopt-genqueries.cjs --dry-run --families mos:find | grep -q -- '--append-system-prompt-file'</automated>
  </verify>
  <acceptance_criteria>
    - --selftest exits 0: split is 60/40 (plus-minus one query), both splits contain both kinds, identical output for identical seed (run twice, deep-equal).
    - --dry-run arg vector contains --json-schema followed by an inline JSON string with no '$schema' substring and no filesystem path (AI-SPEC Pitfalls 2-3), plus --no-session-persistence, --plugin-dir, and NO --bare.
    - Bad-envelope fixture lands as UnitRecord { status:'not_evaluated', not_evaluated_reason:'schema_fail' }, never as a written queries file.
    - references/methodology/skillopt-queries-rubric.md exists, instructs sibling near-miss negatives + per-skill minimum 2/2 kind mix, and contains no em-dashes (grep -P '\x{2014}' empty).
  </acceptance_criteria>
  <done>Per-family generation machinery complete and selftested; splits are deterministic code, the model only ever labels queries; failures land as explicit not_evaluated units.</done>
</task>

<task type="auto">
  <name>Task 2: skillopt-funnel.cjs + judge rubric -- one isolated judge spawn per query against the full roster</name>
  <files>scripts/skillopt-funnel.cjs, references/methodology/skillopt-judge-rubric.md</files>
  <read_first>
    - 230-AI-SPEC.md Section 3 "Entry Point Pattern" (the judgeOneQuery reference implementation -- copy its arg list and unwrap flow) and Section 4 "Model Configuration" table (funnel = sonnet, --max-turns 2, --max-budget-usd 0.05) and Section 4 "Context Window Strategy" (roster = name+description stubs ONLY, never bodies)
    - scripts/huji-batch.cjs lines 200-229 (runPool -- copy verbatim) + 459-478 (getLimit re-read each iteration, onSettle failure recording) + 60-64 (writeAtomic) + 83-86 (isDone sentinel resume predicate)
    - lib/core/skillopt-schemas.cjs (JudgeVerdictSchema, UnitRecordSchema, inlineSchemaJson, assertUnderOut, assertPinnedModelId)
    - 230-PATTERNS.md "Shared Patterns" (all five: spawn args, envelope, inlining, no-silent-skip, bounded pool) and "Pattern Assignments > scripts/skillopt-funnel.cjs"
  </read_first>
  <action>
    Create references/methodology/skillopt-judge-rubric.md: the FROZEN judge instruction: you receive the full MindrianOS skill roster (name + description per skill -- the exact bytes progressive disclosure shows) and ONE user query; predict which ONE skill would fire, or null if none; be honest about confidence -- low or medium confidence sends the skill to a deeper real-invocation test, it never counts as a pass; do not reward yourself for matching the expected label, report what the roster text actually routes. No em-dashes.

    Create scripts/skillopt-funnel.cjs. buildRosterStubs(inventory): 'name: description' lines for ALL 124 records (never bodies -- AI-SPEC Context Window Strategy; even a smoke-scoped run judges against the FULL roster, only the evaluated query set shrinks). judgeOneQuery: exact AI-SPEC Section 3 reference shape -- -p carries roster stubs + query + expected label, --append-system-prompt-file the judge rubric, --model pinned sonnet (assertPinnedModelId preflight), --output-format json, --json-schema inlineSchemaJson(JudgeVerdictSchema), --allowedTools Read, --max-turns 2, --max-budget-usd 0.05, --permission-mode dontAsk, --no-session-persistence, --plugin-dir repo root; spawnSync timeout 120000, maxBuffer 8MB. Unwrap via parseEnvelope shape (structured_output first), double-validate with JudgeVerdictSchema.safeParse (belt + suspenders, AI-SPEC 4b).

    runFunnel: enumerate every query from out/queries/*.json filtered by --skills; fan out through runPool (copied verbatim from huji-batch 200-229) with getLimit reading config.concurrency (default 3, hard cap 4 -- clamp and warn on higher values; AI-SPEC Pitfall 5). Per unit: skip if .done sentinel exists (resume); ONE retry on spawn/timeout/nonzero/schema_fail; then UnitRecord status not_evaluated with reason -- NEVER counted as pass. --probe-timeout-unit <skill> forces timeout 1ms on that skill's first unit and expects it to land not_evaluated reason 'timeout' with not_evaluated_reason overridable to 'induced_probe' via --probe-mark (Plan 07's D5 probe hook). Write out/funnel/units/<unit_id>.json + .done after guardrails pass.

    Flag rule (CONTEXT.md Rigor decision, verbatim): a skill is FLAGGED when any train-split query has predicted_skill !== expected_skill, OR any of its verdicts has confidence 'low' or 'medium'. Validation-split queries are judged too but NEVER drive description revision (they exist for Plan 03's do-no-harm gate). funnel-results.json per skill: { skill, verdict: 'pass'|'flagged'|'not_evaluated', train_miss_count, low_conf_count, unit_ids }, plus a top-level reconciliation block { spawned, ok, not_evaluated } where spawned === ok + not_evaluated MUST hold or the script exits 1 (D5 identity).

    argv router: --skills <csv>, --out, --concurrency, --dry-run, --selftest, --probe-timeout-unit, --probe-mark. --selftest (no spawn): fixture verdict with predicted !== expected on a train query -> flagged; fixture verdict confidence 'medium' with correct prediction -> flagged; all-high-correct fixture -> pass; unparseable-envelope fixture -> not_evaluated + reconciliation identity still holds; .done fixture skipped on re-run (resume).
  </action>
  <verify>
    <automated>node scripts/skillopt-funnel.cjs --selftest && node scripts/skillopt-funnel.cjs --dry-run --skills mos:find-connections | grep -q -- '--max-budget-usd' && grep -v '^\s*//' scripts/skillopt-funnel.cjs | grep -c 'runPool' | grep -qv '^0$' && ! grep -v '^\s*//' scripts/skillopt-funnel.cjs | grep -q 'Promise\.all('</automated>
  </verify>
  <acceptance_criteria>
    - --selftest exits 0 covering all five fixtures (miss->flag, medium-conf->flag, clean->pass, unparseable->not_evaluated, .done resume skip).
    - Reconciliation identity spawned === ok + not_evaluated enforced with exit 1 on violation (assert via a deliberately broken fixture in selftest).
    - Concurrency clamped to <= 4 (passing --concurrency 10 warns and runs at 4 -- assert in selftest); no bare Promise.all( over units anywhere outside comments.
    - Dry-run arg vector: --max-turns 2, --max-budget-usd 0.05, --allowedTools Read, --no-session-persistence, inline --json-schema with no '$schema'.
    - Roster stubs builder output contains exactly one line per inventory record and no skill body text (assert stub line count === inventory length in selftest fixture).
  </acceptance_criteria>
  <done>Funnel machinery complete: hermetic per-query judge spawns, bounded pool, resume sentinels, flag rule per the locked Rigor decision, and the D5 reconciliation identity enforced in code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill descriptions + generated queries -> subagent prompts | semi-trusted text (SKILL.md descriptions, model-generated queries) is embedded in -p for judge/generator spawns -- prompt-injection surface |
| orchestrator -> ~800-1,000 spawned subagents (fleet scale) | each spawn spends real API budget against the user's keychain session |
| pipeline -> filesystem | all queries/units/results must land under .planning/.../out/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-04 | Tampering (prompt injection) | judge/generator -p payload | mitigate | descriptions are short frontmatter stubs, never full bodies; subagent locked to --allowedTools Read + --max-turns 2 + schema-forced output (a hijacked turn cannot write, spawn, or exceed 2 turns; worst case = one wrong verdict, caught by smoke calibration D7) |
| T-230-05 | Denial of service (spend/429) | runFunnel fan-out | mitigate | runPool hard-capped at 4 (clamp + warn), --max-budget-usd 0.05/query, --max-turns 2, spawnSync timeout 120s, --no-session-persistence; fleet-scale run itself deferred to the user's explicit future opt-in (CONTEXT.md Execution mechanism) |
| T-230-06 | Repudiation | killed/errored units | mitigate | one retry then UnitRecord not_evaluated with reason; spawned == ok + not_evaluated identity exits 1 on violation (D5/CFM3) |
| T-230-07 | Information disclosure | -p roster text to subagents | accept | roster stubs are this repo's own shipped frontmatter (public plugin content); no user data, no Brain data crosses (Canon Part 8 untouched -- no Brain call anywhere in this pipeline) |
| T-230-SC | Tampering | package installs | accept (N/A) | zero installs this phase |
</threat_model>

<verification>
- Both selftests exit 0 with zero API spend.
- Dry-run arg vectors match the AI-SPEC Section 3 reference (inline schema, keychain path, fuses).
- git status shows only the four new files.
</verification>

<success_criteria>
- Deterministic split machinery + flag rule + reconciliation identity all proven by fixtures before any live call.
- Every spawn call site carries all five fuses (--max-turns, --max-budget-usd, timeout, allowlist, --no-session-persistence).
- Rubrics are frozen standalone files so the prompt cache bites across the fleet run.
</success_criteria>

<output>
Create `.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-02-SUMMARY.md` when done.
</output>
