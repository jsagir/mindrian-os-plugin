---
phase: 230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/skillopt-schemas.cjs
  - scripts/skillopt-inventory.cjs
autonomous: true
requirements: [D5, D6]
must_haves:
  truths:
    - "Every downstream skillopt script can require lib/core/skillopt-schemas.cjs and get every verdict/artifact contract from one module (interface-first: Wave 2 plans build against these contracts, never invent their own)"
    - "node lib/core/skillopt-schemas.cjs runs its own valid+reject fixtures and exits 0"
    - "node scripts/skillopt-inventory.cjs writes out/inventory.json covering all 124 skills with family grouping and .cjs backing detection, with zero model calls"
    - "A write targeted outside the phase out/ directory is rejected fail-closed by assertUnderOut (CFM4 / D6)"
  artifacts:
    - path: lib/core/skillopt-schemas.cjs
      provides: "zod/v4 verdict + artifact contracts, inlineSchemaJson chokepoint, assertUnderOut containment guard, PHASE_OUT_DIR constant, NOT_EVALUATED_REASONS vocabulary"
      exports: [JudgeVerdictSchema, ReviewFindingSchema, CodeFindingSchema, TriggerResultSchema, LoopSummarySchema, DescriptionRevisionSchema, EvalQuerySchema, EvalQuerySetSchema, InventoryRecordSchema, UnitRecordSchema, SmokeLabelSchema, NOT_EVALUATED_REASONS, PHASE_OUT_DIR, inlineSchemaJson, toJsonSchemas, assertUnderOut, assertPinnedModelId]
      min_lines: 250
    - path: scripts/skillopt-inventory.cjs
      provides: "WS1.1 deterministic inventory: gray-matter parse of skills/*/SKILL.md, family grouping, script/workflow backing detection"
      min_lines: 120
  key_links:
    - from: scripts/skillopt-inventory.cjs
      to: lib/core/skillopt-schemas.cjs
      via: require
      pattern: "require\\(.*skillopt-schemas"
    - from: scripts/skillopt-inventory.cjs
      to: skills/
      via: "readdirSync walk + gray-matter parse"
      pattern: "matter\\("
---

<objective>
Lay the contract floor for the whole Phase 230 pipeline: one schema module that is the single source of truth for every structured verdict and on-disk artifact shape (the pitch-feedback-schemas.cjs discipline), plus the WS1.1 inventory script that enumerates all 124 skills, groups them into namespace families, and detects the script/workflow-backed subset for WS2.

Purpose: interface-first ordering. Plans 02 (genqueries+funnel), 03 (triggerloop), and 04 (codereview) run in parallel in Wave 2 and must build against these contracts instead of exploring the codebase or inventing shapes. Per CONTEXT.md Execution-mechanism decision: Claude Agent SDK / Claude Code native primitives only, zero new dependency (gray-matter@^4.0.3 and zod@^3.25.76 are already pinned in package.json).

Output: lib/core/skillopt-schemas.cjs + scripts/skillopt-inventory.cjs, both with deterministic selftests (zero API spend).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-CONTEXT.md
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-AI-SPEC.md
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-PATTERNS.md
@lib/core/pitch-feedback-schemas.cjs
</context>

## Artifacts this phase produces (Plan 01 slice)

New files: `lib/core/skillopt-schemas.cjs`, `scripts/skillopt-inventory.cjs`.
New exported symbols (skillopt-schemas.cjs): `JudgeVerdictSchema`, `ReviewFindingSchema`, `CodeFindingSchema`, `TriggerResultSchema`, `LoopSummarySchema`, `DescriptionRevisionSchema`, `EvalQuerySchema`, `EvalQuerySetSchema`, `InventoryRecordSchema`, `UnitRecordSchema`, `SmokeLabelSchema`, `NOT_EVALUATED_REASONS`, `PHASE_OUT_DIR`, `inlineSchemaJson(schema)`, `toJsonSchemas(outDir)`, `assertUnderOut(targetPath, outRoot)`, `assertPinnedModelId(id)`.
New CLI surface (skillopt-inventory.cjs): argv flags `--out <dir>`, `--check`, `--selftest` (switch-case router, no Commander/yargs, per repo convention).
New artifact shape on disk: `out/inventory.json` (array of InventoryRecord).
No new command/agent/pipeline/skill surface is added anywhere in Phase 230, so Canon Part 11 (CIRS born-wired + HITL shape declaration) does not trigger; `scripts/*.cjs` and `lib/core/*.cjs` are not one of the four declared classes (Phase 229 huji-*.cjs precedent).

<tasks>

<task type="auto">
  <name>Task 1: lib/core/skillopt-schemas.cjs -- every pipeline contract in one module</name>
  <files>lib/core/skillopt-schemas.cjs</files>
  <read_first>
    - lib/core/pitch-feedback-schemas.cjs (FULL read -- the exact analog: module shape, zod/v4 discipline, bounds-as-contract constants at lines 44-49, enum-forced verdicts at 78/128, toJsonSchemas + inlineSchemaJson at 140-186, require.main selftest block at 208-296)
    - 230-AI-SPEC.md Section 4b "Structured Outputs with zod" (the verbatim JudgeVerdictSchema + CodeFindingSchema field lists) and Section 3 "Common Pitfalls" 2-3 (inline schema, strip $schema)
    - 230-PATTERNS.md "Pattern Assignments > lib/core/skillopt-schemas.cjs" and "Shared Patterns > zod v4 -> JSON Schema inlining"
    - scripts/huji-batch.cjs lines 135-154 (assertPinnedModelId + assertOutsideRepo -- the two guards to adapt)
  </read_first>
  <action>
    Create lib/core/skillopt-schemas.cjs mirroring pitch-feedback-schemas.cjs module shape ('use strict', const { z } = require('zod/v4') -- NEVER bare 'zod'). Export exactly these contracts:

    1. JudgeVerdictSchema -- copy verbatim from AI-SPEC 4b: query (min 1), predicted_skill (string nullable), expected_skill (string nullable), confidence z.enum(['high','medium','low']), reasoning z.string().max(600). Comment: low/medium NEVER passes -- it flags (fail open toward scrutiny, CONTEXT.md Rigor decision).
    2. ReviewFindingSchema -- WS2 pass-1 output (no verdict yet): skill (min 1), file (min 1), severity z.enum(['critical','high','medium','low']), claim max 800, evidence_quote max 800.
    3. CodeFindingSchema -- copy verbatim from AI-SPEC 4b: ReviewFindingSchema fields PLUS verdict z.enum(['confirmed','plausible','refuted']) and refutation_attempt max 800. Comment: refuted is dropped from the report, never silently downgraded.
    4. TriggerResultSchema -- per-query real-invocation observation: skill, query, split z.enum(['train','validation']), iteration int min 0, runs int (3), fires int, trigger_rate number 0..1.
    5. LoopSummarySchema -- per-flagged-skill loop outcome: skill, iterations_run int max 5, best_iteration int, selected_by z.literal('validation_pass_rate'), baseline_validation_rate, best_validation_rate, regressed_query_count int (comment: MUST be 0 for a diff to be proposed -- D3/CFM1 hard gate), converged boolean, proposed_description string max 1024 nullable.
    6. DescriptionRevisionSchema -- revision subagent output: skill, iteration, description max 1024, rationale max 400.
    7. EvalQuerySchema -- query (min 1), expected_skill (string nullable, null means no skill should fire), family, split z.enum(['train','validation']), kind z.enum(['should_trigger','should_not_trigger']).
    8. EvalQuerySetSchema -- { skill, family, queries: array of EvalQuerySchema min 4 }.
    9. InventoryRecordSchema -- { skill, dir, description max 2048, family, backed boolean, backing_refs: array of string (matched scripts/*.cjs, bin/*.cjs, Workflow( references), body_lines int }.
    10. UnitRecordSchema -- the per-spawn trace record (AI-SPEC Section 7): unit_id, kind z.enum(['genqueries','funnel','trigger','revise','review','refute']), model, session_id nullable, total_cost_usd nullable, exit nullable, status z.enum(['ok','not_evaluated']), not_evaluated_reason (z.enum(NOT_EVALUATED_REASONS) nullable), error_issues (unknown array nullable), payload (unknown nullable).
    11. SmokeLabelSchema -- { skill, role z.enum(['clear_fire','near_miss_pair','weak_description','good_description','ws2_defect','ws2_clean','not_evaluated_probe']), expected_funnel z.enum(['pass','flagged','not_evaluated']) nullable, expected_ws2 z.enum(['finding_expected','no_finding_expected']) nullable, notes max 500 }.

    Shared constants + helpers:
    - NOT_EVALUATED_REASONS frozen array: ['spawn_failed','nonzero_exit','timeout','schema_fail','empty_envelope','budget_exceeded','fabricated_evidence','induced_probe'].
    - PHASE_OUT_DIR = path.join('.planning','phases','230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur','out') -- the single canonical out root every script defaults to.
    - inlineSchemaJson(schema): z.toJSONSchema(schema), delete obj.$schema (CLI 2.1.211 rejects the draft/2020-12 meta-ref -- AI-SPEC Pitfall 3), return JSON.stringify(obj). Draft-07-compatible keywords only. This is the SINGLE inlining chokepoint for every spawn call site.
    - toJsonSchemas(outDir): emit one .json file per schema with $schema stripped (mirrors pitch-feedback-schemas lines 140-186).
    - assertUnderOut(targetPath, outRoot): resolve both; return { ok:false, reason:'outside_out' } unless resolved target starts with resolved outRoot + path.sep AND resolved outRoot contains path.sep + '.planning' + path.sep. This is the D6/CFM4 write-path containment guard adapted from huji-batch assertOutsideRepo (147-154), inverted: writes must land INSIDE the phase out sandbox. Fail closed.
    - assertPinnedModelId(id): reject bare aliases; require /^claude-[a-z]+-[0-9]/ (copy from huji-batch 135-141).

    Selftest under require.main === module (mirror pitch-feedback-schemas 208-296): for EVERY schema one valid fixture that parses and one reject fixture that fails (e.g. JudgeVerdictSchema rejects confidence 'certain'; CodeFindingSchema rejects a missing evidence_quote; LoopSummarySchema rejects iterations_run 6); assert inlineSchemaJson output contains no '$schema' substring; assert assertUnderOut rejects a target under skills/ and accepts a target under PHASE_OUT_DIR; assert assertPinnedModelId rejects 'opus' and accepts 'claude-opus-4-8'. Exit 1 on any failure with the failing fixture named. No em-dashes anywhere in the file.
  </action>
  <verify>
    <automated>node lib/core/skillopt-schemas.cjs && node -e "const s=require('./lib/core/skillopt-schemas.cjs'); ['JudgeVerdictSchema','ReviewFindingSchema','CodeFindingSchema','TriggerResultSchema','LoopSummarySchema','DescriptionRevisionSchema','EvalQuerySchema','EvalQuerySetSchema','InventoryRecordSchema','UnitRecordSchema','SmokeLabelSchema','inlineSchemaJson','toJsonSchemas','assertUnderOut','assertPinnedModelId','NOT_EVALUATED_REASONS','PHASE_OUT_DIR'].forEach(k=>{ if(s[k]===undefined) { console.error('MISSING '+k); process.exit(1);} }); console.log('exports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Selftest exits 0; every schema has at least one valid and one reject fixture exercised.
    - node -e export check above prints "exports ok" (all 17 symbols present).
    - inlineSchemaJson(JudgeVerdictSchema) output string contains no "$schema" substring (assert in selftest).
    - assertUnderOut('skills/act/SKILL.md', PHASE_OUT_DIR).ok === false; assertUnderOut(path under PHASE_OUT_DIR, PHASE_OUT_DIR).ok === true.
    - grep -c "require('zod/v4')" lib/core/skillopt-schemas.cjs returns 1 and grep -c "require('zod')" (exact bare form) returns 0.
  </acceptance_criteria>
  <done>Single contract module exists; every Wave 2 plan can require it and get every schema, the inlining chokepoint, the containment guard, and the out-dir constant without reading any other file.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/skillopt-inventory.cjs -- deterministic 124-skill inventory (WS1.1)</name>
  <files>scripts/skillopt-inventory.cjs</files>
  <read_first>
    - lib/core/skillopt-schemas.cjs (the module Task 1 just created -- InventoryRecordSchema, PHASE_OUT_DIR, assertUnderOut)
    - scripts/huji-batch.cjs lines 60-64 (writeAtomic: write-temp-then-rename) and 89-104 (listSubmissions readdirSync withFileTypes enumeration walk)
    - scripts/frontmatter-schema-validator.cjs (header + gray-matter usage: matter(fileText) -> { data, content })
    - 230-PATTERNS.md "Pattern Assignments > scripts/skillopt-inventory.cjs" (family grouping is net-new, no analog)
    - docs/superpowers/specs/2026-07-17-mindrian-skill-optimization-design.md "Workstream 1 -- 1. Inventory" (the authoritative step definition)
  </read_first>
  <action>
    Create scripts/skillopt-inventory.cjs, fully deterministic, ZERO model calls (the AI-SPEC model table lists haiku as the cost ceiling for this step; pure code is cheaper, reviewable, and reproducible -- record this as the implementation choice in the file header, it is within Claude's Discretion per CONTEXT.md).

    Behavior: walk skills/ with readdirSync({ withFileTypes:true }).filter(isDirectory) (huji-batch 89-104 shape); for each dir read SKILL.md, parse with matter() from gray-matter; extract data.name and data.description (fall back to dir name when name missing, and record description '' -- never crash on a malformed file, record it with backed:false and a body_lines of 0 plus a stderr warning line naming the file); grep the BODY content (not frontmatter) with non-comment-aware regexes for three backing signals: /scripts\/[A-Za-z0-9._-]+\.cjs/g, /bin\/[A-Za-z0-9._-]+\.cjs/g, /Workflow\(/g; backed = any match; backing_refs = unique matched strings.

    Family grouping (net-new logic, no analog): family key = for names containing ':', the namespace prefix plus the first hyphen-delimited verb segment of the remainder (mos:find-connections -> 'mos:find', mos:explore-domains -> 'mos:explore'); for names without ':', the first hyphen segment prefixed 'plain:' ('build-thesis' -> 'plain:build'). Singleton families are fine -- the generator (Plan 02) treats a singleton family as a skill with no sibling near-misses.

    Output: validate every record against InventoryRecordSchema (safeParse; a validation failure is a HARD exit 1 naming the record -- inventory is deterministic, so a schema failure is a bug, not a not_evaluated); write out/inventory.json (array) via writeAtomic after assertUnderOut passes on the target. argv switch-case router: --out <dir> (default PHASE_OUT_DIR), --check (parse + validate + print counts, write nothing), --selftest (run embedded fixtures: a fixture SKILL.md string with a scripts/foo.cjs body ref must yield backed:true; a name 'mos:find-connections' must yield family 'mos:find'; an assertUnderOut violation must exit nonzero). Print a summary line: total skills, family count, backed count, backed skill names.
  </action>
  <verify>
    <automated>node scripts/skillopt-inventory.cjs --selftest && node scripts/skillopt-inventory.cjs --out .planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/out && node -e "const inv=require('./.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/out/inventory.json'); if(inv.length!==124){console.error('expected 124 skills, got '+inv.length);process.exit(1)}; const backed=inv.filter(r=>r.backed); if(backed.length<5||backed.length>40){console.error('backed count suspicious: '+backed.length);process.exit(1)}; const fams=new Set(inv.map(r=>r.family)); console.log('OK 124 skills, '+fams.size+' families, '+backed.length+' backed')"</automated>
  </verify>
  <acceptance_criteria>
    - --selftest exits 0 (backing-detection fixture, family-key fixture, containment-violation fixture all pass).
    - Real run writes out/inventory.json with exactly 124 records, every record InventoryRecordSchema-valid.
    - Backed subset is nonempty and plausible (5-40 records; design expects ~10-20 -- print the actual names so Plan 05 can pick the WS2 smoke pair from real data).
    - mos:find-connections, mos:find-analogies, mos:find-bottlenecks share one family key; mos:explore-domains, mos:explore-trends, mos:explore-futures share another (assert in selftest via fixture names, confirm on real output in the summary line).
    - No write lands outside the out dir (assertUnderOut called before writeAtomic; grep -v '^\s*//' scripts/skillopt-inventory.cjs | grep -c 'assertUnderOut' returns >= 1).
  </acceptance_criteria>
  <done>out/inventory.json exists with 124 family-grouped, backing-classified records; Plans 02-05 consume it as their ground truth for rosters, families, and the WS2 subset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skills/*/SKILL.md -> inventory parser | 124 semi-trusted markdown bodies flow into gray-matter + regex; content is data, never executed |
| pipeline -> filesystem | every generated byte must resolve under .planning/.../out/ only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-01 | Tampering | skillopt-inventory.cjs regex scan | mitigate | SKILL.md bodies are parsed as data only (gray-matter + regex); no eval, no require of skill content, no subagent spawn in this plan at all |
| T-230-02 | Tampering | out/inventory.json write path | mitigate | assertUnderOut fail-closed before writeAtomic; target must resolve under .planning/.../out/ (D6/CFM4) |
| T-230-03 | Repudiation | malformed SKILL.md absorbed silently | mitigate | malformed file recorded with explicit stderr warning + degraded record, never dropped from the 124 count (D5 no-silent-skip discipline starts here) |
| T-230-SC | Tampering | npm/pip/cargo installs | accept (N/A) | zero package-manager installs in this plan or phase; gray-matter + zod already pinned in package.json (AI-SPEC Section 2 zero-new-dependency decision) |
</threat_model>

<verification>
- node lib/core/skillopt-schemas.cjs exits 0 (selftest).
- node scripts/skillopt-inventory.cjs --selftest exits 0; real run yields 124 records.
- Both files: zero em-dashes (grep -P '\x{2014}' returns nothing), 'use strict' present, CJS only.
- No file under skills/ or scripts/huji-* modified: git status shows only the two new files.
</verification>

<success_criteria>
- Contract module + inventory both green on their deterministic selftests with zero API spend.
- All 17 exported symbols present and consumed-ready for Wave 2.
- Family grouping proven on the real dense families named in CONTEXT.md Specifics (mos:find-*, mos:explore-*).
</success_criteria>

<output>
Create `.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-01-SUMMARY.md` when done.
</output>
