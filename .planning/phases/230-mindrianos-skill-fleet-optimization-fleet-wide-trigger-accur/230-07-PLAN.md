---
phase: 230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur
plan: 07
type: execute
wave: 4
depends_on: [230-02, 230-03, 230-04, 230-05, 230-06]
files_modified:
  - .planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-07-CALIBRATION.md
autonomous: false
requirements: [D2, D5, D7]
must_haves:
  truths:
    - "The harness ran LIVE end to end on the approved smoke set only -- never the full 124-skill budget, never a Workflow-tool call (CONTEXT.md Execution mechanism decision, both locked)"
    - "Funnel verdicts agree with Jonathan's pre-labels at or above tolerance, computed by code (checkSmokeAgreement), before any full-fleet spend is ever considered (D7 hard run gate)"
    - "The induced probe landed as not_evaluated reason induced_probe -- never absorbed as a pass (D5 live proof)"
    - "WS2 caught the real check-card-fire over-enforcement class on the defect target AND produced zero CONFIRMED findings on the clean control (D4 live proof, both directions)"
    - "A full-fleet cost projection exists, extrapolated from real smoke total_cost_usd envelopes, so the future opt-in decision is priced (AI-SPEC Section 4b cost discipline)"
  artifacts:
    - path: .planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-07-CALIBRATION.md
      provides: "the calibration verdict: agreement table, detector proof, WS2 both-directions result, probe proof, cost projection, and the explicit deferred-run statement"
    - path: .planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/out/report/skillopt-report.md
      provides: "the first real merged report, smoke-scoped, ending at the STOP gate"
  key_links:
    - from: "smoke run artifacts under out/"
      to: scripts/skillopt-eval.cjs
      via: "--suite code + checkSmokeAgreement over the live artifacts"
      pattern: "skillopt-eval"
    - from: 230-07-CALIBRATION.md
      to: smoke-labels.json
      via: "agreement computed against the human-approved labels, never re-labeled from output"
      pattern: "smoke-labels"
---

<objective>
Run the harness live, small, and honestly: the smoke calibration that the whole phase exists to reach. Both workstreams execute over the approved 10-15 skill smoke set, every guardrail fires for real, agreement against Jonathan's pre-labels is computed by code, and a full-fleet cost projection is produced -- then the phase STOPs. The 124-skill run and any Workflow-tool orchestration remain explicitly deferred to the user's own future opt-in.

Purpose: AI-SPEC D7 is the run gate -- an uncalibrated judge, an unproven detector, or a silently-absorbed failure at fleet scale would burn ~800-1,000 calls producing a report no one can trust. The smoke run buys that trust for a few dollars.

Output: live smoke artifacts under out/, the first real merged report, and 230-07-CALIBRATION.md with the calibration verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-AI-SPEC.md
@.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/smoke-labels.json
@lib/core/skillopt-schemas.cjs
</context>

## Artifacts this phase produces (Plan 07 slice)

New file: `230-07-CALIBRATION.md` (the calibration verdict document).
New live artifacts under out/: `out/queries/<skill>.json` for smoke families, `out/funnel/units/` + `out/funnel/funnel-results.json`, `out/review/` findings for the defect + clean pair, `out/report/skillopt-report.md` + `report.json`, `out/smoke/projection.json` (full-fleet cost extrapolation).
No new code symbols -- this plan exercises Plans 01-06's machinery; any code change discovered necessary here is a deviation to report, not silently patch in.

<tasks>

<task type="auto">
  <name>Task 1: live WS1 smoke -- generation, funnel, probe, detector re-proof</name>
  <files>.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/out/</files>
  <read_first>
    - smoke-labels.json (the approved set -- defines exactly which families and skills run)
    - 230-AI-SPEC.md Section 6 "Online" guardrail table (every per-unit fuse that must be observed firing or holding during this run) and Section 7 "Alert Thresholds" (not-evaluated rate > ~5% halts; preflight failure aborts before spend)
    - scripts/skillopt-funnel.cjs header (the --skills / --probe-timeout-unit / --probe-mark flags Plan 02 built)
    - scripts/skillopt-triggerloop.cjs header (preflightPluginLoad + --capture usage from Plan 03)
  </read_first>
  <action>
    Execute in order, recording every command and cost:

    1. Preflight: node scripts/skillopt-triggerloop.cjs invoking preflightPluginLoad -- mos plugin loaded, plugin_errors empty; ABORT the whole task on failure (AI-SPEC plugin-preflight guardrail, zero spend on a broken preflight).
    2. Fresh inventory: node scripts/skillopt-inventory.cjs (124 records).
    3. Generation, smoke families only: node scripts/skillopt-genqueries.cjs --families <the family keys covering every smoke-labels skill> -- expect per-skill eval_queries files with train/validation splits.
    4. Funnel, smoke skills only, FULL 124-roster context (the roster always stays full -- that is the design): node scripts/skillopt-funnel.cjs --skills <csv of smoke skills> --concurrency 3, PLUS --probe-timeout-unit <the not_evaluated_probe skill> --probe-mark induced_probe. Expected: verdicts for every smoke skill, the probe unit landing not_evaluated reason induced_probe, reconciliation identity holding.
    5. Detector re-proof on the live roster (D7's detector leg, both directions): node scripts/skillopt-triggerloop.cjs --capture runs against one clear_fire smoke skill (expect fired:true naming it) and one off-topic query (expect fired:false). Uses the Plan 03 pinned SKILL_INPUT_FIELD against fresh captures.
    6. Write out/smoke/projection.json: per-bucket mean total_cost_usd from the actual unit envelopes (genqueries per family, funnel per query, trigger per run) extrapolated to fleet volume per AI-SPEC Section 4b (funnel ~800-1,000; loop ~600-1,200 flagged-only; review ~20-40) -- the priced input for the future opt-in.

    Live-run discipline: if the not-evaluated rate (excluding the deliberate probe) exceeds ~5%, HALT and record the transport diagnosis instead of re-running blind (AI-SPEC alert threshold). Any harness bug discovered mid-run is a deviation: STOP the affected leg, record it, fix only under the executor deviation rules, never hand-edit artifacts to green.
  </action>
  <verify>
    <automated>node scripts/skillopt-eval.cjs --check reconciliation && node -e "const u=require('fs').readdirSync('.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/out/funnel/units').filter(f=>f.endsWith('.json')); const recs=u.map(f=>require(require('path').resolve('.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/out/funnel/units',f))); const probe=recs.find(r=>r.not_evaluated_reason==='induced_probe'); if(!probe){console.error('induced probe missing or absorbed');process.exit(1)}; console.log('probe honest: '+probe.unit_id)" && test -s .planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/out/smoke/projection.json</automated>
  </verify>
  <acceptance_criteria>
    - Preflight passed before any spend (command output recorded in the SUMMARY).
    - Every smoke skill has an eval_queries file and a funnel verdict; reconciliation check passes on live artifacts.
    - The probe unit exists with status not_evaluated reason induced_probe (the node -e gate above) and is listed in funnel-results as not_evaluated.
    - Fresh captures show fired:true on the clear_fire skill and fired:false on the off-topic query.
    - projection.json carries per-bucket real mean costs and fleet-scale extrapolations.
  </acceptance_criteria>
  <done>WS1 machinery proven live on the smoke set with every guardrail observed, at smoke-scale spend only.</done>
</task>

<task type="auto">
  <name>Task 2: live WS2 smoke + merge + calibration computation</name>
  <files>.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/out/, .planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-07-CALIBRATION.md</files>
  <read_first>
    - smoke-labels.json (the ws2_defect and ws2_clean targets + expected_ws2 labels)
    - .planning/todos/pending/2026-07-05-fix-stop-hook-forcing-irrelevant-decision-gate-cards.md (what the defect finding should substantively describe -- the ground truth for judging whether WS2 caught the real class)
    - 230-AI-SPEC.md Section 6 "Offline" metrics (survival rate anomaly: 0% refuted across many findings suggests rubber-stamping) and Section 5 D7 row (the PASS definition this task computes)
  </read_first>
  <action>
    1. WS2 live: node scripts/skillopt-codereview.cjs --files scripts/check-card-fire.cjs plus the ws2_clean target from smoke-labels. Expected per the approved labels: at least one CONFIRMED or PLAUSIBLE finding on check-card-fire.cjs whose claim substantively describes the over-enforcement class (gate/card firing on content with no real decision present -- compare against the 2026-07-05 todo), with a verbatim evidence_quote; ZERO CONFIRMED findings on the clean control. If the defect is MISSED or the control is flagged, that is a calibration FAIL to report honestly in 230-07-CALIBRATION.md -- do not re-run until it passes, do not tune the rubric mid-run (rubric changes are a recorded deviation with re-run, keeping the calibration honest).
    2. Merge: node scripts/skillopt-merge.cjs -- the first real report; confirm it renders all sections, lists every not_evaluated unit (including the probe), and STOPs at the itemized approval checklist with zero real-file writes.
    3. Full deterministic gate: bash tests/run-all-230.sh -- now with live artifacts present, the previously-SKIPped legs run, including checkSmokeAgreement against smoke-labels.json at the default 0.85 tolerance.
    4. Write 230-07-CALIBRATION.md: the agreement table (per-skill expected vs actual, mismatches named), detector both-directions proof (capture filenames), WS2 both-directions outcome, probe proof, survival-rate note (flag a 0%-refuted anomaly if it occurs), cost projection summary, and the closing statement: the full 124-skill run and any Workflow-tool orchestration are DEFERRED pending the user's explicit multi-agent-orchestration opt-in (CONTEXT.md Execution mechanism + Deferred Ideas -- restate it so the next session cannot miss it). No em-dashes, Feynman prose.
  </action>
  <verify>
    <automated>bash tests/run-all-230.sh && node scripts/skillopt-eval.cjs --check smoke-replay && test -s .planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/out/report/skillopt-report.md && test -s .planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-07-CALIBRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - run-all-230.sh fully green with live artifacts (agreement leg RUNS, not SKIPs).
    - checkSmokeAgreement >= tolerance with the per-skill table in output; every mismatch also appears in 230-07-CALIBRATION.md.
    - findings.json contains a check-card-fire finding matching the over-enforcement class and no CONFIRMED finding on the clean control (or the honest FAIL is documented -- fabricating agreement is the one unacceptable outcome).
    - The merged report ends at the STOP checklist; git status confirms zero modifications under skills/ or scripts/ from the run.
  </acceptance_criteria>
  <done>Calibration computed honestly from live artifacts against human pre-labels; the report and verdict document exist; nothing real was written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Jonathan reviews the calibration verdict</name>
  <action>Present 230-07-CALIBRATION.md and the merged report to Jonathan for the calibration verdict. Record his verdict verbatim in the SUMMARY. Whatever the verdict, do NOT launch the full 124-skill run or any Workflow-tool orchestration -- that remains a separate future step behind his explicit multi-agent-orchestration opt-in.</action>
  <what-built>The harness ran live on your approved smoke set: per-family query generation, roster-wide funnel, real Skill-fire captures, the induced not_evaluated probe, adversarial code review of check-card-fire.cjs plus the clean control, and the merged STOP-gated report. 230-07-CALIBRATION.md holds the verdict: agreement vs your pre-labels, both detector directions, both WS2 directions, the probe proof, and the full-fleet cost projection.</what-built>
  <how-to-verify>
    1. Read .planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-07-CALIBRATION.md -- start with the agreement table and any named mismatches.
    2. Spot-check the concerning-signal units per the AI-SPEC sampling rule: every not_evaluated unit, any low/medium-confidence verdict, the check-card-fire finding (does it describe the real bug you logged three times?).
    3. Open out/report/skillopt-report.md -- confirm it reads approvable (Feynman prose, verbatim quotes, itemized checklist) and that it STOPped without touching any real file.
    4. Check out/smoke/projection.json -- the priced estimate for the future 124-skill run.
  </how-to-verify>
  <resume-signal>Type "calibrated" if the harness earns the future full-fleet run, or name what disagrees. Either way the full run does NOT launch now -- it stays behind your explicit multi-agent-orchestration opt-in as a separate future step.</resume-signal>
  <done>Jonathan's calibration verdict is recorded; the phase closes with the harness proven (or the honest gap named) and the fleet run still deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live subagent spawns -> user's keychain session + real spend | the only plan that runs the pipeline hot; smoke-scoped by construction |
| calibration results -> future full-fleet go/no-go | inflated agreement here would green-light an untrustworthy ~800-1,000-call run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-22 | Denial of service (spend) | live smoke run | mitigate | scope locked to smoke-labels skills (--families/--skills flags); every unit carries the Plan 02/03/04 fuses; not-evaluated rate > ~5% halts; no full-fleet flag exists in any script invocation in this plan |
| T-230-23 | Spoofing (self-grading) | calibration computation | mitigate | agreement computed by deterministic checkSmokeAgreement against pre-approved labels; misses reported as FAIL, mid-run rubric tuning banned (recorded deviation + re-run only); terminal human review of the verdict |
| T-230-24 | Repudiation | probe or failed units absorbed | mitigate | the induced_probe unit is asserted present-as-not_evaluated by code in Task 1's verify; reconciliation re-checked on live artifacts |
| T-230-25 | Tampering | live run writing outside out/ | mitigate | checkWritePath over the live tree + git status assertion of zero skills/ or scripts/ modifications in Task 2 acceptance |
| T-230-SC | Tampering | package installs | accept (N/A) | zero installs this phase |
</threat_model>

<verification>
- bash tests/run-all-230.sh fully green with live artifacts.
- node scripts/doctor.cjs --acceptance green (repo roll-up before declaring the phase done, CLAUDE.md Verification).
- git status: no modifications under skills/ or scripts/ from the run; only the calibration doc and phase-dir artifacts are new.
</verification>

<success_criteria>
- Live smoke calibration complete and human-reviewed: agreement at tolerance (or the honest miss documented with its diagnosis), detector and WS2 proven in both directions, probe honest, run priced.
- The full 124-skill spend and Workflow-tool orchestration remain un-launched and explicitly deferred in writing.
</success_criteria>

<output>
Create `.planning/phases/230-mindrianos-skill-fleet-optimization-fleet-wide-trigger-accur/230-07-SUMMARY.md` when done.
</output>
