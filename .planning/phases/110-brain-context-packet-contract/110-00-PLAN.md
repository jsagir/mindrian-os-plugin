---
phase: 110-brain-context-packet-contract
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - tests/test-brain-packet-schema-check.cjs
  - tests/test-brain-packet-validation-per-job.cjs
  - tests/test-brain-packet-part8-invariant-per-job.cjs
  - tests/test-brain-packet-precommit-hook.cjs
  - tests/run-all-110.sh
  - lib/memory/run-feynman-tests.cjs
autonomous: true
requirements:
  - PACKET-110-01
  - PACKET-110-02
  - PACKET-110-03
  - PACKET-110-04
  - PACKET-110-05
  - PACKET-110-06
  - PACKET-110-07
  - PACKET-110-08
  - PACKET-110-09
canon_parts:
  - "Part 8 (Graph Boundary -- this Wave 0 substrate registers the 9 PACKET-110-XX requirement IDs that Plans 110-01 through 110-05 implement; it ships ZERO behavior, only the 4 RED test stubs + the scoped runner + the REQUIREMENTS/ROADMAP lock that the wire-hardening work hangs off, mirroring 109-00)"
  - "Part 9 (Memory Locality -- Phase 110 is the wire-level hardening of Part 8 that Canon Part 9 names as the third member of the 108+109+110 implementing cluster; this plan locks the contract; no code yet)"
  - "Part 7 (Reuse Before Build -- the 4 test stubs follow the existing tests/test-*.cjs CJS pattern verified against tests/test-navigation-packet-part8-leak.cjs and tests/test-navigation-chokepoint-hook.cjs; tests/run-all-110.sh is a verbatim mirror of tests/run-all-122.sh; zero new npm dependencies)"
must_haves:
  truths:
    - "REQUIREMENTS.md gains a `## Brain Context Packet Contract (PACKET-110)` block with 9 checkbox entries PACKET-110-01 through PACKET-110-09 (goal-shaped descriptions, each citing the source CONTEXT D-NN), plus 9 `| PACKET-110-0N | Phase 110 | Pending |` rows appended to the bottom Status table"
    - "ROADMAP.md `### Phase 110` block: `**Requirements**: TBD` line replaced with `**Requirements**: PACKET-110-01..09 (see .planning/REQUIREMENTS.md)`; `**Plans:** TBD -- phase is stubbed; ...` line replaced with a `**Plans:** 6 plans across 4 waves.` line plus an enumerated `Plans:` checkbox list 110-00 through 110-05; the stale 9-job vocabulary in the Goal paragraph is corrected to the D-02 12-job vocabulary"
    - "4 RED test stub files exist: tests/test-brain-packet-schema-check.cjs, tests/test-brain-packet-validation-per-job.cjs, tests/test-brain-packet-part8-invariant-per-job.cjs, tests/test-brain-packet-precommit-hook.cjs"
    - "Each test stub exits 1 with a stderr line `MISSING - Wave N must implement <deliverable> (Plan 110-NN)` so the runner records RED status without a false-positive PASS"
    - "tests/run-all-110.sh exists, is a verbatim structural mirror of tests/run-all-122.sh (bash only, no emoji, no em-dashes, SHELL_SUITES empty, CJS_SUITES = the 4 new suites, per-suite PASS/FAIL loop, exit non-zero if any failed), runs to completion, and exits 1 today (4 RED stubs)"
    - "lib/memory/run-feynman-tests.cjs TEST_FILES[] registers all 4 new test paths in a Phase 110 block with a comment header mapping each file to its owning plan"
    - "Every file this plan ships contains zero em-dashes (U+2014) and zero en-dashes (U+2013) per the CLAUDE.md hard rule"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "The 9 PACKET-110-XX requirement IDs registered with Status: Pending; downstream plans (110-01..05) reference these IDs in their requirements frontmatter"
      contains: "PACKET-110-01"
    - path: "tests/test-brain-packet-schema-check.cjs"
      provides: "RED stub filled by Plan 110-01 (the --check schema tripwire test: malformed JSON / missing job in-out def / unknown-job def -> non-zero, plus the recursive additionalProperties:false assertion)"
      contains: "MISSING"
    - path: "tests/test-brain-packet-validation-per-job.cjs"
      provides: "RED stub filled by Plan 110-05 (the 12-job in/out validation suite + the privacy-mode sub-block + the dual-path-warning sub-block)"
      contains: "MISSING"
    - path: "tests/test-brain-packet-part8-invariant-per-job.cjs"
      provides: "RED stub filled by Plan 110-05 (the 12-job round-trip build->validate test + the adversarial forbidden-substring sweep over JSON.stringify(buildBrainPacket(...)))"
      contains: "MISSING"
    - path: "tests/test-brain-packet-precommit-hook.cjs"
      provides: "RED stub filled by Plan 110-04 (the D-08 layer-2 hook test: a staged fixture diff introducing a bare sendPacket( not preceded by buildBrainPacket( -> hook exit non-zero)"
      contains: "MISSING"
    - path: "tests/run-all-110.sh"
      provides: "Scoped Phase 110 runner; CJS_SUITES = the 4 new suites; header documents RED-by-design-until-owning-plan-lands; mirrors tests/run-all-122.sh"
      contains: "Phase 110 scoped test runner"
    - path: "lib/memory/run-feynman-tests.cjs"
      provides: "Existing Feynman test registry extended with 4 new TEST_FILES entries so bash tests/run-all-110.sh and the whole-repo Feynman runner both pick them up"
      contains: "test-brain-packet-validation-per-job.cjs"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 110 block: Requirements line + Plans checkbox list enumerating 110-00 through 110-05 so /gsd:progress reports plan state correctly; 12-job vocabulary corrected"
      contains: "110-00-PLAN.md"
  key_links:
    - from: "lib/memory/run-feynman-tests.cjs TEST_FILES array"
      to: "tests/test-brain-packet-schema-check.cjs and tests/test-brain-packet-validation-per-job.cjs and tests/test-brain-packet-part8-invariant-per-job.cjs and tests/test-brain-packet-precommit-hook.cjs"
      via: "path.join(REPO_ROOT, 'tests', '<filename>') entries appended to TEST_FILES in a Phase 110 block"
      pattern: "test-brain-packet-validation-per-job\\.cjs"
    - from: "tests/run-all-110.sh CJS_SUITES array"
      to: "the 4 new tests/test-brain-packet-*.cjs files"
      via: "CJS_SUITES=( test-brain-packet-schema-check.cjs test-brain-packet-validation-per-job.cjs test-brain-packet-part8-invariant-per-job.cjs test-brain-packet-precommit-hook.cjs )"
      pattern: "test-brain-packet-schema-check\\.cjs"
    - from: ".planning/REQUIREMENTS.md PACKET-110-01 through PACKET-110-09 rows"
      to: "Status table at the bottom of REQUIREMENTS.md"
      via: "9 rows appended to the per-phase status table mirroring the NAV-109-01 through NAV-109-09 pattern"
      pattern: "PACKET-110-0[1-9].*Pending"
---

<objective>
Wave 0 substrate plan. Ships ZERO behavior. Ships the test infrastructure plus the requirement and roadmap lock that Waves 1 through 3 of Phase 110 require: 4 RED test stubs, the scoped `tests/run-all-110.sh` runner, the Feynman-runner registration, the 9 PACKET-110-XX requirement IDs in `.planning/REQUIREMENTS.md`, and the `### Phase 110` block update in `.planning/ROADMAP.md` (plan list + Requirements line + 12-job-vocabulary correction).

Modelled verbatim on `109-00-PLAN.md` (the Wave-0 substrate-plan template). The 4 test stubs each exit 1 with stderr `MISSING - Wave N must implement ... (Plan 110-NN)` so the test runner records RED status; later plans (110-01, 110-04, 110-05) replace the stub bodies with real assertions without changing the registered paths. The Wave 0 deliverables ARE the validation contract Phase 110 ships against (`110-VALIDATION.md` mirrors this).

Per RESEARCH section "Validation Architecture -> Wave 0 substrate plan" and CONTEXT D-11, the four suites are: `test-brain-packet-schema-check.cjs` (covers PACKET-110-01 + -02), `test-brain-packet-validation-per-job.cjs` (covers PACKET-110-03 + -04 + -07 + -08), `test-brain-packet-part8-invariant-per-job.cjs` (covers PACKET-110-06 round-trip + the D-11(d) adversarial sweep), `test-brain-packet-precommit-hook.cjs` (covers PACKET-110-05 D-08 layer-2 hook).

This plan has zero dependencies and runs first. It writes nothing executable -- no schema, no `sendPacket`, no hook block. Those are Waves 1 through 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-brain-context-packet-contract/110-CONTEXT.md
@.planning/phases/110-brain-context-packet-contract/110-RESEARCH.md
@.planning/phases/110-brain-context-packet-contract/110-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@lib/memory/run-feynman-tests.cjs
@tests/run-all-122.sh
@tests/test-navigation-packet-part8-leak.cjs
@tests/test-navigation-chokepoint-hook.cjs

<interfaces>
<!-- Test stub pattern (verbatim from 109-00-PLAN.md Task 2 / tests/test-navigation-*.cjs Wave-0 stubs):

```javascript
'use strict';
// Phase 110 Wave 0 stub. Plan 110-00 ships this stub; Plan 110-NN replaces with real assertions.
// Requirement: PACKET-110-NN
// Deliverable: <one-line deliverable>
// Pattern: matches tests/test-navigation-packet-part8-leak.cjs CJS direct-CJS approach (no Mocha, no Jest, zero new npm dependencies).

process.stderr.write('MISSING - Wave N must implement <deliverable> (Plan 110-NN)\n');
process.exit(1);
```
-->

<!-- tests/run-all-110.sh = verbatim structural mirror of tests/run-all-122.sh:
   - shebang #!/usr/bin/env bash
   - header comment explaining the suites are RED-by-design until their owning plan lands (map each suite -> plan)
   - `set -uo pipefail`
   - SHELL_SUITES=( )  (empty)
   - CJS_SUITES=( test-brain-packet-schema-check.cjs test-brain-packet-validation-per-job.cjs test-brain-packet-part8-invariant-per-job.cjs test-brain-packet-precommit-hook.cjs )
   - the same TOTAL/PASSED/FAILED loop, the same Summary block, exit 1 if any failed
   - bash only, no emoji, no em-dashes
-->

<!-- run-feynman-tests.cjs registry pattern (lib/memory/run-feynman-tests.cjs lines 33+, and the Phase 109 block around line 1197):
const TEST_FILES = [
  path.join(REPO_ROOT, 'lib', 'memory', 'feynman-prompts.test.cjs'),
  ...
  // Phase 110-00: Brain Context Packet Contract Wave 0 substrate (4 stubs filled by Plans 110-01 / 110-04 / 110-05).
  //   test-brain-packet-schema-check.cjs              -> 110-01 (PACKET-110-01 + -02: the --check schema tripwire)
  //   test-brain-packet-validation-per-job.cjs        -> 110-05 (PACKET-110-03 + -04 + -07 + -08: 12-job in/out + privacy + dual-path)
  //   test-brain-packet-part8-invariant-per-job.cjs   -> 110-05 (PACKET-110-06 round-trip + D-11(d) adversarial sweep)
  //   test-brain-packet-precommit-hook.cjs            -> 110-04 (PACKET-110-05 D-08 layer-2 hook)
  path.join(REPO_ROOT, 'tests', 'test-brain-packet-schema-check.cjs'),
  path.join(REPO_ROOT, 'tests', 'test-brain-packet-validation-per-job.cjs'),
  path.join(REPO_ROOT, 'tests', 'test-brain-packet-part8-invariant-per-job.cjs'),
  path.join(REPO_ROOT, 'tests', 'test-brain-packet-precommit-hook.cjs'),
];
Append the 4 entries with the Phase comment markers matching the in-file convention.
-->

<!-- REQUIREMENTS.md format (the NAV-109 block is the verbatim precedent; the new block sits immediately before the Status table at the bottom of the file):
## Brain Context Packet Contract (PACKET-110)

- [ ] **PACKET-110-01**: ...one block per PACKET-110-XX requirement; the Status table at the bottom gains 9 `| PACKET-110-0N | Phase 110 | Pending |` rows.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 9 PACKET-110-XX requirement IDs in REQUIREMENTS.md and update ROADMAP.md Phase 110 block</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the NAV-109-01 through NAV-109-09 block plus the 9 `| NAV-109-0N | Phase 109 | Complete |` Status-table rows -- format the 9 PACKET-110-XX entries and rows identically; the new block goes immediately BEFORE the Status table at the bottom of the file)
    - .planning/phases/110-brain-context-packet-contract/110-CONTEXT.md (the LOCKED decisions D-00 through D-11 -- each PACKET-110-XX description must trace to a D-NN)
    - .planning/phases/110-brain-context-packet-contract/110-RESEARCH.md section "Phase Requirements -> the proposed PACKET-110-01..09 breakdown" (the requirement table with verbatim descriptions matched to RESEARCH support) and section "Open Questions" 1 / 5 (the dispositions: the Brain-side `brain_packet` tool is OUT of plugin scope and `sendPacket` degrades soft when absent; the "legacy free-form Brain job path" is forward-looking with NO current call site)
    - .planning/ROADMAP.md `### Phase 110` block (lines ~1045-1058 -- the `**Goal:**` paragraph lists the STALE 9-job vocabulary; `**Requirements**: TBD`; `**Plans:** TBD -- phase is stubbed`)
  </read_first>
  <action>
    STEP 1 -- REQUIREMENTS.md. Append a new section to `.planning/REQUIREMENTS.md` immediately before the bottom Status table, with this exact content (replace any em-dash or en-dash with a hyphen before writing -- the file already has legacy em-dashes elsewhere; touch ONLY the new PACKET-110 section):

    ```markdown

    ## Brain Context Packet Contract (PACKET-110)

    - [ ] **PACKET-110-01**: Brain packet schema artifact. `data/brain-packet-schema.json` ships: draft 2020-12; root `$schema` + `$id` (`https://mindrian-os.com/schemas/brain-packet/1.0`) + `$defs`; shared `$defs` for `PrivacyMode` (enum of D-03's 3 values), `Origin` (enum `["navigation_api","test_fixture"]`), `BankedOpportunities` (object: `count` integer >= 0, `items` array `maxItems: 3` of hashed-id + tags + hsi_band enum + composite_score), `LocalGraphSummary` (the `buildBrainPacket` local_graph_summary shape with `banked_opportunities` `$ref`), `ActiveContext` (jtbd / operator / focus_node), and the per-projection sub-shapes matching `packet.cjs`'s safe-projection mappers; a `$def` per ALL 12 D-02 jobs (`select_methodology`, `suggest_next_move`, `detect_contradiction`, `summarize_neighborhood`, `classify_room_budding`, `rank_assumptions`, `generate_feynman_explanation`, `strengthen_minto`, `prepare_investor_brief`, `opportunity_react`, `opportunity_reflect`, `opportunity_rank`), each `{ "in": {...}, "out": {...} }` with `in.properties.packet_version: { "const": "1.0" }`, `in.properties.job: { "const": "<jobname>" }`, `in.properties.origin: { "$ref": "#/$defs/Origin" }`, `in.properties.privacy_mode: { "const": "local_summary_only" }`, `in.properties.local_graph_summary: { "$ref": "#/$defs/LocalGraphSummary" }`; `additionalProperties: false` on EVERY object node in the schema (root, every shared `$def`, every job `in` and `out`, every nested object) -- this is the leak-prevention teeth. `data/ROOM.md` "Files in this section" table gains a `brain-packet-schema.json` row. Per CONTEXT D-04 + D-05 + D-06 + D-08; RESEARCH "Implementation Approach" 1-2.

    - [ ] **PACKET-110-02**: Schema generator + `--check` drift tripwire + scoped test. `scripts/build-brain-packet-schema.cjs` mirrors `scripts/build-command-registry.cjs`: default run validates the schema is well-formed (ajv `strict: true` compile -- surfaces schema-cleanliness warnings at build time; runtime uses `strict: false`), every D-02 job has an `in` AND an `out` `$def`, each job's `in.properties.job` is `{ const: <jobname> }`, no `$def` references a job outside the D-02 closed vocabulary (a small SHARED_DEFS name allow-list ignores the non-job `$defs`), and recursively asserts `additionalProperties: false` on every `{ type:'object', properties:{...} }` node; then exit 0. `--check` re-runs the same structural assertions in memory and exits 1 on malformed JSON / a job missing `in` or `out` / a job-const mismatch / an unknown-job `$def` / a missing `additionalProperties: false`, printing a one-line recovery command on stderr (`Recovery: fix data/brain-packet-schema.json (the hand-maintained source of truth), then re-run: node scripts/build-brain-packet-schema.cjs --check`). `tests/test-brain-packet-schema-check.cjs` (the Wave-0 stub) becomes a real `child_process` suite asserting: `--check` exits 0 on the shipped schema; exits non-zero on (a) `echo {} > <tmp copy>` malformed (b) a tmp copy with a job's `in` `$def` deleted (c) a tmp copy with a `$def` named `not_a_real_job` added; stderr names the failing condition + the recovery line. Per CONTEXT D-06 + D-11; RESEARCH "Implementation Approach" 3 + "Architecture Patterns -> Pattern 2".

    - [ ] **PACKET-110-03**: `brain-client.sendPacket()` + ajv `in`-validator middleware + D-08 layer-3 origin allowlist. `lib/core/brain-client.cjs` gains a brand-new `sendPacket(packet, opts)` function (NOT a `callTool()` branch). Module scope: lazy compile of `data/brain-packet-schema.json` once via `require('ajv').default || require('ajv')` + `new Ajv({ allErrors: true, strict: false })` + `addSchema`; per-job validator memoized via `_ajv.compile({ $ref: schemaId + '#/$defs/' + job + '/properties/in' })`. `sendPacket` order: (1) D-08 layer 3 -- if `typeof packet.origin !== 'string'` throw; if `packet.origin === 'test_fixture'` and `process.env.MINDRIAN_TEST_MODE !== '1'` throw `origin "test_fixture" only valid when MINDRIAN_TEST_MODE=1`; if `packet.origin !== 'navigation_api' && packet.origin !== 'test_fixture'` throw; (2) unknown-job guard (`packet.job` not a D-02 job -> throw); (3) ajv-validate `packet` against `$defs[job].in` -- on failure: snapshot `(fn.errors||[]).slice()` immediately, log `brain_packet_rejected` via `navigation.cjs`'s re-exported `logMemoryEvent(opts.db, ...)` (skip the log gracefully when no `opts.db`), then `throw new Error('brain packet rejected for job "'+packet.job+'": '+formatErrs)`; (4) build the wire envelope and POST a `tools/call` with `name: 'brain_packet'`, `arguments: { packet }` to `${BRAIN_URL}/mcp` reusing `_ensureSession`/`getApiKey`/the SSE-parse -- if the Brain returns "unknown tool" / -32602 / a 404-ish error treat it exactly like D-04's "Brain doesn't recognize packet_version": reject, DEGRADE SOFT (return the no-advice sentinel), log NOTHING (it is not a leak). `navigation.cjs` re-exports `logMemoryEvent(db, eventType, payload)` (a 14th export -- acceptable per the Phase 109 navigation.cjs header; it re-exports internal helpers as needed). `sendPacket` exported; the compiled validators + a `_resetSchema` test hook added to the `module.exports` `_test`-style block. Per CONTEXT D-04 + D-07 (`in` half) + D-08 layer 3; RESEARCH "Implementation Approach" 4 + "Open Question" 1 + 2 + 6.

    - [ ] **PACKET-110-04**: Response-validation-then-degrade gate (`out` half). Inside `brain-client.sendPacket`, after the wire POST returns: parse the Brain result; ajv-validate it against `$defs[job].out`; on failure -- do NOT ingest, log `brain_response_rejected` via `navigation.cjs`'s `logMemoryEvent` (skip gracefully without `opts.db`), return `{ advice: null, reason: 'response_schema_invalid' }` -- NEVER throw on a bad response, NEVER partial-ingest (no strip-extra-and-keep-the-rest). On success return the validated `out` (the caller -- typically code that then calls `storeBrainSuggestions` -- receives a known-good shape). `lib/core/navigation/ingestion.cjs` `storeBrainSuggestions` is UNCHANGED (each suggestion still lands `brain_insight` / `created_by:'brain'` / `review_status:'proposed'` / one `brain_suggestion_received` event per call / BEGIN/COMMIT/ROLLBACK / Phase 108 invariant SQL = 0 rows) -- the gate sits in front of it, in `sendPacket`. The validator stance phrase to honor: "reject hard, degrade soft." Per CONTEXT D-07 (`out` half) + Canon Part 9; RESEARCH "Implementation Approach" 5 + "Anti-Patterns to Avoid".

    - [ ] **PACKET-110-05**: Pre-commit hook wiring (D-08 layer 2 + the schema-drift block). `scripts/check-schema-aliases.cjs` gains a `--check-sendpacket` subcommand mirroring its existing `--check-chokepoint` subcommand (same `MINDRIAN_HOOK_STAGED_FILES` / `MINDRIAN_HOOK_STAGED_CONTENT_DIR` hermetic-test seams): it scans staged `*.cjs`/`*.js`/`*.mjs` for a `\bsendPacket\s*\(` call site NOT lexically preceded (same file, within a small window or the same function body) by a `\bbuildBrainPacket\s*\(` call, exiting 1 with a structured message pointing at D-08; allow-list: `lib/core/brain-client.cjs` (the definition), `lib/core/navigation*.cjs`, `lib/core/navigation/`, `tests/`, `scripts/`. `scripts/hooks/pre-commit-room-minto-guard.sh` AND the byte-identical `scripts/hooks/pre-commit` gain TWO new blocks placed right after the Phase-122 command-registry block: (a) `if git diff --cached --name-only | grep -qE '^(data/brain-packet-schema\.json|scripts/build-brain-packet-schema\.cjs)$'; then node "$REPO_ROOT/scripts/build-brain-packet-schema.cjs" --check || { echo "brain-packet-schema drift -- run: node scripts/build-brain-packet-schema.cjs --check" >&2; exit 2; } fi`; (b) `if command -v node >/dev/null 2>&1 && [ -f "$REPO_ROOT/scripts/check-schema-aliases.cjs" ]; then node "$REPO_ROOT/scripts/check-schema-aliases.cjs" --check-sendpacket || { echo "bare sendPacket( introduced -- per D-08 it must be lexically preceded by buildBrainPacket(" >&2; exit 2; } fi`. Then re-run `scripts/setup-hooks.sh` so `.git/hooks/pre-commit` (the live byte-copy) matches the source. `tests/test-brain-packet-precommit-hook.cjs` (the Wave-0 stub) becomes a real `child_process` suite mirroring `tests/test-navigation-chokepoint-hook.cjs`: a staged-fixture diff introducing a bare `sendPacket(` not preceded by `buildBrainPacket(` -> `--check-sendpacket` exits non-zero with a D-08 message; a diff where `sendPacket(` IS preceded by `buildBrainPacket(` -> exit 0. Per CONTEXT D-08 layer 2 + D-11; RESEARCH "Implementation Approach" 3 + "Open Question" 4 + "Common Pitfall 4".

    - [ ] **PACKET-110-06**: `origin` field in the packet builder + the `EVENT_TYPES` extension. `lib/core/navigation/packet.cjs` `buildBrainPacket` return object gains `origin: 'navigation_api',` as a one-line top-level addition next to `packet_version: '1.0',`. `lib/core/navigation/memory-events.cjs` `EVENT_TYPES` frozen Set gains `'brain_packet_rejected'`, `'brain_response_rejected'`, `'brain_legacy_path_used'` (3 new strings; additive, same pattern as Phase 116-00's 5 tension strings -- size grows by 3) with a `// Phase 110-00 / 110-02 extension (Brain Context Packet Contract; D-07 + D-10 telemetry mirror)` comment. The produced packet round-trips: `buildBrainPacket(db, job, focusNodeId, ...)` validates against `$defs[job].in` for every D-02 job. Regression touches: `tests/test-navigation-packet-builder.cjs` (assert `packet.origin === 'navigation_api'`), `tests/test-navigation-packet-part8-leak.cjs` (the new field does not change the leak assertions; the top-level-key enumeration is updated if present), `tests/test-navigation-memory-events.cjs` (the closed-enum size assertion is bumped by 3). Per CONTEXT D-08 layer 1 + D-07 + D-10 + the "Claude's Discretion" enum-extension note; RESEARCH "Implementation Approach" 6.

    - [ ] **PACKET-110-07**: Privacy-mode opt-up wiring. A `resolvePrivacyMode(roomDir, opts)` helper in `lib/core/navigation/packet.cjs`: `opts.privacyMode` (per-call) beats `roomDir/.config.json > preferences.brain_privacy_mode` (project-level; note the leading dot in `.config.json`, the established room-config path per `lib/core/model-profiles.cjs`) beats the default `'local_summary_only'`; an unrecognized value falls back to `'local_summary_only'`; `'allow_excerpts'` additionally requires a Part-3 Decision Gate APPROVE-with-reason edge on the room node (a `brain_excerpts`-tagged decision, written via `navigation.cjs` when the user picks APPROVE on the "excerpts of your room content will reach the Brain" F-shape selector; the F.0 primitive at `lib/hmi/shape-f0-renderer.cjs` already exists) -- absent that edge it caps down to the config mode (or `local_summary_only`). `buildBrainPacket` adds a new top-level `privacy_mode` field carrying `resolvePrivacyMode(...)` (one of D-03's 3 enum values); `constraints.privacy: 'no_raw_artifact_text'` stays as a separate human-readable note. Because NO shipped job declares above the default, every shipped `$def.in.properties.privacy_mode` is `{ "const": "local_summary_only" }` -- so a packet whose resolved mode is `allow_filenames` for a job that only allows `local_summary_only` FAILS ajv validation: that is the "config caps, never raises" enforcement, for free, via the schema. `allow_excerpts` has NO shipped consumer -- it is a defined-but-unconsumed escape hatch; no shipped job is wired to it. Per CONTEXT D-03 + D-09 + Canon Part 3; RESEARCH "Implementation Approach" 8 + "Open Question" 6 + "Anti-Patterns to Avoid".

    - [ ] **PACKET-110-08**: Dual-path + once-per-session deprecation warning. A module-level `let _legacyPathWarned = false;` in `lib/core/brain-client.cjs` plus a helper `function _warnLegacyOnce(db) { if (_legacyPathWarned) return; _legacyPathWarned = true; console.warn('[mindrian-os] legacy free-form Brain job call detected. Migrate to brain-client.sendPacket() -- the legacy job path is removed in v1.14.0.'); try { require('./navigation.cjs').logMemoryEvent(db, 'brain_legacy_path_used', { source_path: 'system:brain-legacy' }); } catch (_) {} }`. Per RESEARCH "Open Question" 5: there is NO existing legacy free-form Brain *job* call site today (every Brain caller uses `query()` / `search()` / `schema()` / `write()` / `callTool('brain_query'...)`, all of which D-10 explicitly says are NOT "legacy" and are untouched forever). So this is the forward-looking deprecation guard + the `brain_legacy_path_used` event type as the CONTRACT, with a documented comment in `brain-client.cjs`: "as of v1.13.0-beta.3 there is no legacy free-form Brain job call site -- new job-style work goes through `sendPacket()`; if a free-form job helper is ever added before v1.14.0 it must call `_warnLegacyOnce()` first; in v1.14.0 both the helper and this guard are deleted." A test (in `test-brain-packet-validation-per-job.cjs`) exercises `_warnLegacyOnce` directly: calling it twice -> `console.warn` fires exactly once, `brain_legacy_path_used` logged exactly once. Per CONTEXT D-10; RESEARCH "Implementation Approach" 7 + "Open Question" 5.

    - [ ] **PACKET-110-09**: The D-11 test suite + scoped runner + Feynman registration. Four `tests/test-brain-packet-*.cjs` suites (created RED in Wave 0, filled by 110-01 / 110-04 / 110-05), framework = node `assert` / `node:assert/strict` + `node:child_process` (no jest/mocha/vitest/zod; CJS): (a) `test-brain-packet-schema-check.cjs` (the `--check` tripwire test -- filled by 110-01); (b) `test-brain-packet-validation-per-job.cjs` (12 D-02 jobs x {valid `in` validates, malformed `in` -> throws naming the job + logs `brain_packet_rejected`, off-spec `out` -> NOT ingested + logs `brain_response_rejected` + never throws + never partial-ingests, `origin` not in the allowed enum (in the relevant env) -> refused} + a privacy-mode sub-block ("config caps, never raises") + a dual-path sub-block (`_warnLegacyOnce` once-per-process) -- uses the `require.cache` mock-the-wire-POST idiom from `lib/memory/brain-derivation.test.cjs`; filled by 110-05); (c) `test-brain-packet-part8-invariant-per-job.cjs` (12 jobs x {round-trip build->validate passes} + the adversarial forbidden-substring sweep over `JSON.stringify(buildBrainPacket(...))` -- seed `SECRET RAW BODY` / `leak@example.com` / `/home/jsagi/secret/` / an 800-char transcript / a `${...}` injection into node properties, build the packet, assert none reach the serialized packet; mirrors `tests/test-navigation-packet-part8-leak.cjs` but loops all 12 jobs; filled by 110-05); (d) `test-brain-packet-precommit-hook.cjs` (the D-08 layer-2 hook test -- filled by 110-04). All 4 registered in `lib/memory/run-feynman-tests.cjs` `TEST_FILES[]` (Phase 110 block) plus a scoped `tests/run-all-110.sh` (verbatim mirror of `tests/run-all-122.sh`; `CJS_SUITES` = the 4 suites; header documents RED-by-design-until-owning-plan-lands). Per CONTEXT D-11; RESEARCH "Validation Architecture" + "Phase Requirements -> Test Map".
    ```

    Then append these 9 rows to the Status table at the bottom of `.planning/REQUIREMENTS.md` (immediately after the last existing row, identical format -- pipe-delimited, no em-dashes):

    ```markdown
    | PACKET-110-01 | Phase 110 | Pending |
    | PACKET-110-02 | Phase 110 | Pending |
    | PACKET-110-03 | Phase 110 | Pending |
    | PACKET-110-04 | Phase 110 | Pending |
    | PACKET-110-05 | Phase 110 | Pending |
    | PACKET-110-06 | Phase 110 | Pending |
    | PACKET-110-07 | Phase 110 | Pending |
    | PACKET-110-08 | Phase 110 | Pending |
    | PACKET-110-09 | Phase 110 | Pending |
    ```

    STEP 2 -- ROADMAP.md. Open `.planning/ROADMAP.md`, find the `### Phase 110: Brain Context Packet Contract (REGISTERED 2026-05-03 -- STUB)` block. Make three edits (use hyphens, never em-dashes, in everything you write):
      (a) In the `**Goal:**` paragraph, replace the stale 9-job list in parentheses (`select_methodology`, `suggest_next_move`, `detect_contradiction`, `summarize_neighborhood`, `classify_room_budding`, `rank_assumptions`, `generate_feynman_explanation`, `strengthen_minto`, `prepare_investor_brief`) with the D-02 12-job vocabulary by appending the three Bank jobs: add `, opportunity_react, opportunity_reflect, opportunity_rank` so the parenthetical lists all 12.
      (b) Replace `**Requirements**: TBD (PACKET-110-01..NN -- defined when CONTEXT.md is expanded post Phase 109)` with `**Requirements**: PACKET-110-01..09 (registered 2026-05-12 -- see .planning/REQUIREMENTS.md "## Brain Context Packet Contract (PACKET-110)")`.
      (c) Replace the line `**Plans:** TBD -- phase is stubbed; full CONTEXT to be expanded after Phase 109 navigation API is reviewed.` with this block:

    ```markdown
    **Plans:** 6 plans across 4 waves (planned 2026-05-12 via /gsd:plan-phase 110). Target band: v1.13.0-beta.3 (Path C re-route).

    Plans:
    - [ ] 110-00-PLAN.md - Wave 0 substrate (register PACKET-110-01..09 in REQUIREMENTS.md + ROADMAP; 4 RED test stubs; tests/run-all-110.sh; Feynman-runner registration)
    - [ ] 110-01-PLAN.md - Wave 1 schema artifact + generator + --check tripwire (data/brain-packet-schema.json + scripts/build-brain-packet-schema.cjs; data/ROOM.md row; fill test-brain-packet-schema-check.cjs)
    - [ ] 110-02-PLAN.md - Wave 1 origin field + EVENT_TYPES extension + privacy-mode resolver (packet.cjs origin/privacy_mode + resolvePrivacyMode; memory-events.cjs +3; regression touches)
    - [ ] 110-03-PLAN.md - Wave 2 sendPacket() validator middleware + response-degrade gate + dual-path warn (brain-client.cjs sendPacket; navigation.cjs logMemoryEvent re-export; reject-hard-degrade-soft)
    - [ ] 110-04-PLAN.md - Wave 3 D-08 layer-2 + pre-commit hook wiring (check-schema-aliases.cjs --check-sendpacket; both hook blocks; setup-hooks re-run; fill test-brain-packet-precommit-hook.cjs)
    - [ ] 110-05-PLAN.md - Wave 3 the D-11 validation suite (fill test-brain-packet-validation-per-job.cjs + test-brain-packet-part8-invariant-per-job.cjs: 12-job in/out + privacy + dual-path + round-trip + adversarial Part-8 sweep)
    ```

    After writing both files, scan the new PACKET-110 section in REQUIREMENTS.md and the modified Phase 110 block in ROADMAP.md for em-dashes and en-dashes; replace any with hyphens. Do NOT touch any pre-existing content outside those two regions (legacy em-dashes elsewhere are out of scope).
  </action>
  <verify>
    <automated>grep -c "PACKET-110-0" .planning/REQUIREMENTS.md | awk '$1 == 18 { exit 0 } { exit 1 }' && grep -q "## Brain Context Packet Contract (PACKET-110)" .planning/REQUIREMENTS.md && grep -q "| PACKET-110-09 | Phase 110 | Pending |" .planning/REQUIREMENTS.md && grep -q "110-00-PLAN.md" .planning/ROADMAP.md && grep -q "110-05-PLAN.md" .planning/ROADMAP.md && grep -q "opportunity_rank" .planning/ROADMAP.md && grep -q "PACKET-110-01..09" .planning/ROADMAP.md && awk '/## Brain Context Packet Contract \(PACKET-110\)/,/^\| Requirement/' .planning/REQUIREMENTS.md | grep -P "[\x{2014}\x{2013}]" && exit 1 || awk '/### Phase 110:/,/### Phase 112:/' .planning/ROADMAP.md | sed -n '/Plans:$/,/110-05-PLAN/p' | grep -P "[\x{2014}\x{2013}]" && exit 1 || exit 0</automated>
  </verify>
  <done>
    REQUIREMENTS.md has a `## Brain Context Packet Contract (PACKET-110)` block with 9 goal-shaped PACKET-110-01..09 entries each citing a D-NN, plus 9 `Pending` Status-table rows; ROADMAP.md `### Phase 110` block has the Requirements line, the 6-plan checkbox list, and the corrected 12-job vocabulary; zero em-dashes or en-dashes in the new REQUIREMENTS section or the modified ROADMAP block.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the 4 RED test stubs, tests/run-all-110.sh, and register the 4 suites in the Feynman runner</name>
  <files>tests/test-brain-packet-schema-check.cjs, tests/test-brain-packet-validation-per-job.cjs, tests/test-brain-packet-part8-invariant-per-job.cjs, tests/test-brain-packet-precommit-hook.cjs, tests/run-all-110.sh, lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - tests/test-navigation-packet-part8-leak.cjs (the canonical CJS test pattern this phase's part-8 suite mirrors; and the Wave-0 stub it once was -- the `MISSING - Wave N must implement ...` idiom)
    - tests/test-navigation-chokepoint-hook.cjs (the hook-test idiom the precommit-hook suite mirrors; the `MINDRIAN_HOOK_STAGED_FILES` / `MINDRIAN_HOOK_STAGED_CONTENT_DIR` env seams)
    - tests/run-all-122.sh (the scoped-runner template -- mirror its shebang, header structure, `set -uo pipefail`, SHELL_SUITES/CJS_SUITES arrays, the TOTAL/PASSED/FAILED loop, the Summary block, the exit-1-if-any-failed tail; bash only, no emoji, no em-dashes)
    - lib/memory/run-feynman-tests.cjs (the `TEST_FILES` const array near line 33 and the Phase 109 block around line 1197 -- copy the `path.join(REPO_ROOT, 'tests', ...)` pattern and the Phase-comment-marker convention)
    - .planning/phases/110-brain-context-packet-contract/110-VALIDATION.md "Wave 0 Gaps" (the 4 test files + their owning plans -- the canonical assignment)
  </read_first>
  <action>
    STEP 1 -- create the 4 RED test stub files. Each follows this template (vary the WAVE / DELIVERABLE / PACKET-ID / TARGET-PLAN per the table below; HYPHEN never em-dash):

    ```javascript
    'use strict';
    // Phase 110 Wave 0 stub. Plan 110-00 ships this stub; Plan <TARGET-PLAN> replaces it with real assertions.
    // Requirement: <PACKET-ID>
    // Deliverable: <DELIVERABLE>
    // Pattern: matches tests/test-navigation-packet-part8-leak.cjs (CJS, node:assert, node:child_process; no Mocha, no Jest, zero new npm dependencies).

    process.stderr.write('MISSING - Wave <WAVE> must implement <DELIVERABLE> (Plan <TARGET-PLAN>)\n');
    process.exit(1);
    ```

    Assignment table:

    | File | WAVE | DELIVERABLE | PACKET-ID | TARGET-PLAN |
    |---|---|---|---|---|
    | tests/test-brain-packet-schema-check.cjs | 1 | scripts/build-brain-packet-schema.cjs --check tripwire (malformed JSON / missing job in-out def / unknown-job def -> non-zero; recursive additionalProperties:false assertion) | PACKET-110-01 + PACKET-110-02 | 110-01 |
    | tests/test-brain-packet-validation-per-job.cjs | 3 | the 12-job sendPacket in/out validation suite + privacy-mode sub-block + dual-path-warning sub-block (require.cache mock of the wire POST) | PACKET-110-03 + PACKET-110-04 + PACKET-110-07 + PACKET-110-08 | 110-05 |
    | tests/test-brain-packet-part8-invariant-per-job.cjs | 3 | the 12-job round-trip build->validate test + the adversarial forbidden-substring sweep over JSON.stringify(buildBrainPacket(...)) | PACKET-110-06 + PACKET-110-09 | 110-05 |
    | tests/test-brain-packet-precommit-hook.cjs | 3 | the D-08 layer-2 hook test (a staged fixture diff introducing a bare sendPacket( not preceded by buildBrainPacket( -> hook exit non-zero) | PACKET-110-05 | 110-04 |

    STEP 2 -- create `tests/run-all-110.sh`. Copy `tests/run-all-122.sh` verbatim as the structural template, then change ONLY: the header comment block (Phase 110 framing -- "Phase 110 scoped runner -- run before each 110 task commit. Expect RED on the suites whose owning plan has not landed yet:" then a per-suite -> plan map: `test-brain-packet-schema-check.cjs -> 110-01`, `test-brain-packet-validation-per-job.cjs -> 110-05`, `test-brain-packet-part8-invariant-per-job.cjs -> 110-05`, `test-brain-packet-precommit-hook.cjs -> 110-04`; note all 4 are RED-by-design until their owning plan lands; bash only, no emoji, no em-dashes); the title strings ("Phase 110 scoped test runner" / "Summary (110 scoped)"); and `CJS_SUITES=( test-brain-packet-schema-check.cjs test-brain-packet-validation-per-job.cjs test-brain-packet-part8-invariant-per-job.cjs test-brain-packet-precommit-hook.cjs )` (SHELL_SUITES stays empty). Keep the `set -uo pipefail`, the loop, the Summary block, and the exit-1-if-any-failed tail byte-for-byte the same shape. Make the file executable (`chmod +x tests/run-all-110.sh`) if the harness preserves the bit; if not, leave it -- the runner is invoked via `bash tests/run-all-110.sh`.

    STEP 3 -- register the 4 suites in `lib/memory/run-feynman-tests.cjs`. Find the `TEST_FILES` array. Append, immediately before the closing `]` (or after the last existing entry), a Phase 110 block with a comment header mapping each file to its owning plan, then the 4 `path.join` entries:

    ```javascript
      // Phase 110-00: Brain Context Packet Contract Wave 0 substrate (4 stubs filled by Plans 110-01 / 110-04 / 110-05).
      //   test-brain-packet-schema-check.cjs              -> 110-01 (PACKET-110-01 + -02: the --check schema tripwire)
      //   test-brain-packet-validation-per-job.cjs        -> 110-05 (PACKET-110-03 + -04 + -07 + -08: 12-job in/out + privacy + dual-path)
      //   test-brain-packet-part8-invariant-per-job.cjs   -> 110-05 (PACKET-110-06 round-trip + D-11(d) adversarial sweep)
      //   test-brain-packet-precommit-hook.cjs            -> 110-04 (PACKET-110-05 D-08 layer-2 hook)
      path.join(REPO_ROOT, 'tests', 'test-brain-packet-schema-check.cjs'),
      path.join(REPO_ROOT, 'tests', 'test-brain-packet-validation-per-job.cjs'),
      path.join(REPO_ROOT, 'tests', 'test-brain-packet-part8-invariant-per-job.cjs'),
      path.join(REPO_ROOT, 'tests', 'test-brain-packet-precommit-hook.cjs'),
    ```

    NO em-dashes anywhere (hyphens only). NO emoji. The 4 stubs are stubs -- they exit 1 and do nothing else. `tests/run-all-110.sh` is shippable and complete (it will exit 1 today because the 4 CJS_SUITES are RED stubs; that is correct-by-design).
  </action>
  <verify>
    <automated>for f in tests/test-brain-packet-schema-check.cjs tests/test-brain-packet-validation-per-job.cjs tests/test-brain-packet-part8-invariant-per-job.cjs tests/test-brain-packet-precommit-hook.cjs tests/run-all-110.sh; do test -f "$f" || { echo "missing: $f"; exit 1; }; done && for f in tests/test-brain-packet-schema-check.cjs tests/test-brain-packet-validation-per-job.cjs tests/test-brain-packet-part8-invariant-per-job.cjs tests/test-brain-packet-precommit-hook.cjs; do node "$f" >/dev/null 2>&1 && { echo "stub did not exit 1: $f"; exit 1; }; node "$f" 2>&1 | grep -q "MISSING - Wave" || { echo "stub missing MISSING line: $f"; exit 1; }; done && grep -c "test-brain-packet-schema-check.cjs\|test-brain-packet-validation-per-job.cjs\|test-brain-packet-part8-invariant-per-job.cjs\|test-brain-packet-precommit-hook.cjs" lib/memory/run-feynman-tests.cjs | awk '$1 >= 4 { exit 0 } { exit 1 }' && bash tests/run-all-110.sh; rc=$?; [ "$rc" -eq 1 ] || { echo "run-all-110.sh expected exit 1 with 4 RED stubs, got $rc"; exit 1; }; grep -q "CJS_SUITES" tests/run-all-110.sh && grep -q "test-brain-packet-schema-check.cjs" tests/run-all-110.sh && grep -lP "[\x{2014}\x{2013}]" tests/test-brain-packet-schema-check.cjs tests/test-brain-packet-validation-per-job.cjs tests/test-brain-packet-part8-invariant-per-job.cjs tests/test-brain-packet-precommit-hook.cjs tests/run-all-110.sh && exit 1 || exit 0</automated>
  </verify>
  <done>
    4 RED stub files exist; each exits 1 on direct invocation with a `MISSING - Wave N must implement ...` stderr line; `tests/run-all-110.sh` exists, mirrors `tests/run-all-122.sh`, runs to completion and exits 1 (4 RED stubs); `lib/memory/run-feynman-tests.cjs` registers all 4 new test paths in a Phase 110 block; zero em-dashes or en-dashes in any file written.
  </done>
</task>

</tasks>

<verification>
- All 6 file paths in `files_modified` exist on disk.
- `node tests/test-brain-packet-schema-check.cjs` exits 1 with stderr containing `MISSING - Wave 1 must implement` (proves the stub is RED, not silently passing); the other 3 stubs exit 1 with their `MISSING - Wave 3 ...` lines.
- `bash tests/run-all-110.sh` runs to completion and exits 1 (4 RED CJS_SUITES); the per-suite output shows 4 FAILED entries.
- `node lib/memory/run-feynman-tests.cjs` runs to completion (the 4 new entries are picked up; they fail RED, which is expected at Wave 0 -- the existing suite invocation pattern is not broken).
- `grep -c "PACKET-110-0" .planning/REQUIREMENTS.md` returns 18 (9 requirement entries + 9 Status-table rows).
- `grep -P "[\x{2014}\x{2013}]" tests/test-brain-packet-*.cjs tests/run-all-110.sh` returns zero matches; the new `## Brain Context Packet Contract (PACKET-110)` section of REQUIREMENTS.md and the modified `### Phase 110` block of ROADMAP.md both pass the same em-dash/en-dash check restricted to those regions.
</verification>

<success_criteria>
Wave 0 substrate is shippable when:
1. The 9 PACKET-110-XX requirement IDs are registered in `.planning/REQUIREMENTS.md` with goal-shaped descriptions tracing to D-00..D-11, plus 9 `Pending` Status-table rows.
2. The `### Phase 110` block of `.planning/ROADMAP.md` lists Requirements `PACKET-110-01..09`, enumerates plans 110-00 through 110-05 as a checkbox list, and uses the corrected D-02 12-job vocabulary.
3. The 4 RED test stubs exist; each exits 1 with the canonical `MISSING - Wave N must implement ... (Plan 110-NN)` message.
4. `tests/run-all-110.sh` exists, mirrors `tests/run-all-122.sh`, and runs to completion (exits 1 today because the 4 CJS_SUITES are RED stubs -- correct-by-design).
5. `lib/memory/run-feynman-tests.cjs` registers the 4 new test files in `TEST_FILES[]` in a Phase 110 block.
6. Zero em-dashes or en-dashes in any file written by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/110-brain-context-packet-contract/110-00-SUMMARY.md` recording: artifacts created, the REQUIREMENTS table state (9 Pending rows added), the ROADMAP Phase 110 block diff, the Feynman-runner registry diff, and the Wave 1 plans (110-01, 110-02) now ready to run in parallel. Note any deviations from the plan (none expected; the substrate is fully specified).
</output>
