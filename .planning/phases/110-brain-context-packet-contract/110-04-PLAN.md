---
phase: 110-brain-context-packet-contract
plan: "04"
type: execute
wave: 3
depends_on: ["110-01", "110-03"]
files_modified:
  - scripts/check-schema-aliases.cjs
  - scripts/hooks/pre-commit-room-minto-guard.sh
  - scripts/hooks/pre-commit
  - .git/hooks/pre-commit
  - tests/test-brain-packet-precommit-hook.cjs
autonomous: true
requirements:
  - PACKET-110-05
canon_parts:
  - "Part 8 (Graph Boundary -- D-08 layer 2: the pre-commit hook is the real teeth of the three-layer origin guarantee; the string is in-process-forgeable but the hook + review catch a packet built outside buildBrainPacket)"
  - "Part 7 (Reuse Before Build -- the --check-sendpacket subcommand extends the existing scripts/check-schema-aliases.cjs mega-script, mirroring its --check-chokepoint subcommand and reusing the MINDRIAN_HOOK_STAGED_FILES / MINDRIAN_HOOK_STAGED_CONTENT_DIR seams; the schema-drift block clones the Phase 122 command-registry block)"
  - "Part 9 (Memory Locality -- the chokepoint enforcement: a packet must be built through the navigation API, never assembled directly from files/shell/transcript)"
must_haves:
  truths:
    - "scripts/check-schema-aliases.cjs gains a --check-sendpacket subcommand mirroring its existing --check-chokepoint subcommand: it scans staged *.cjs/*.js/*.mjs (via the existing getStagedFiles / readStagedContent seams -- MINDRIAN_HOOK_STAGED_FILES newline-separated, MINDRIAN_HOOK_STAGED_CONTENT_DIR for content) for a `\\bsendPacket\\s*\\(` call site NOT lexically preceded (same file, within a small line window or the same function body) by a `\\bbuildBrainPacket\\s*\\(` call; on a violation it exits 1 with a structured message naming the file:line + pointing at D-08; allow-list (always allowed, never flagged): lib/core/brain-client.cjs (the definition), lib/core/navigation.cjs, lib/core/navigation/, tests/, scripts/"
    - "scripts/hooks/pre-commit-room-minto-guard.sh AND the byte-identical scripts/hooks/pre-commit gain TWO new blocks placed right after the Phase-122 command-registry block: (a) a schema-drift block -- if git diff --cached --name-only matches ^(data/brain-packet-schema\\.json|scripts/build-brain-packet-schema\\.cjs)$ then node \"$REPO_ROOT/scripts/build-brain-packet-schema.cjs\" --check || { echo recovery >&2; exit 2; }; (b) a D-08 layer-2 block -- if command -v node && [ -f \"$REPO_ROOT/scripts/check-schema-aliases.cjs\" ] then node \"$REPO_ROOT/scripts/check-schema-aliases.cjs\" --check-sendpacket || { echo recovery >&2; exit 2; }"
    - ".git/hooks/pre-commit (the live byte-copy) matches scripts/hooks/pre-commit-room-minto-guard.sh after scripts/setup-hooks.sh is re-run (cmp -s) -- so the installed hook actually runs the two new blocks"
    - "tests/test-brain-packet-precommit-hook.cjs is a real child_process suite (no longer the MISSING stub) mirroring tests/test-navigation-chokepoint-hook.cjs: a staged-fixture diff introducing a bare sendPacket( not preceded by buildBrainPacket( -> `node scripts/check-schema-aliases.cjs --check-sendpacket` exits non-zero with a D-08 message; a diff where sendPacket( IS preceded by buildBrainPacket( -> exit 0; an empty staged set -> exit 0; an allow-listed file (lib/core/brain-client.cjs) with a bare sendPacket( -> exit 0"
    - "scripts/check-schema-aliases.cjs's existing --check-chokepoint subcommand + the default schema-aliases scan + the buildAllowedTableSet / scanSql exports are UNCHANGED -- the new subcommand is purely additive"
    - "Every file this plan ships contains zero em-dashes (U+2014) and zero en-dashes (U+2013)"
  artifacts:
    - path: "scripts/check-schema-aliases.cjs"
      provides: "A new --check-sendpacket subcommand (D-08 layer 2): scans staged JS for a bare sendPacket( not lexically preceded by buildBrainPacket(; allow-list; exits 1 with a D-08 message on a violation"
      contains: "check-sendpacket"
    - path: "scripts/hooks/pre-commit-room-minto-guard.sh"
      provides: "Two new blocks after the Phase-122 command-registry block: the brain-packet-schema --check drift block + the --check-sendpacket D-08 layer-2 block"
      contains: "check-sendpacket"
    - path: "tests/test-brain-packet-precommit-hook.cjs"
      provides: "Real child_process suite around `node scripts/check-schema-aliases.cjs --check-sendpacket`: bare-sendPacket-without-buildBrainPacket -> non-zero; preceded-by-buildBrainPacket -> 0; empty staged set -> 0; allow-listed file -> 0; mirrors tests/test-navigation-chokepoint-hook.cjs"
      min_lines: 40
  key_links:
    - from: "scripts/hooks/pre-commit-room-minto-guard.sh"
      to: "scripts/check-schema-aliases.cjs --check-sendpacket"
      via: "a new `node \"$REPO_ROOT/scripts/check-schema-aliases.cjs\" --check-sendpacket || exit 2` block after the Phase-122 command-registry block"
      pattern: "check-schema-aliases\\.cjs --check-sendpacket"
    - from: "scripts/hooks/pre-commit-room-minto-guard.sh"
      to: "scripts/build-brain-packet-schema.cjs --check"
      via: "a new `git diff --cached --name-only | grep -qE ... && node ... --check || exit 2` block"
      pattern: "build-brain-packet-schema\\.cjs --check"
    - from: ".git/hooks/pre-commit"
      to: "scripts/hooks/pre-commit-room-minto-guard.sh"
      via: "scripts/setup-hooks.sh byte-copies the source over the installed hook (cmp -s + atomic rename)"
      pattern: "check-sendpacket"
    - from: "tests/test-brain-packet-precommit-hook.cjs"
      to: "scripts/check-schema-aliases.cjs"
      via: "spawnSync('node', [SCRIPT, '--check-sendpacket'], { env: { MINDRIAN_HOOK_STAGED_FILES, MINDRIAN_HOOK_STAGED_CONTENT_DIR } })"
      pattern: "--check-sendpacket"
---

<objective>
Wire the D-08 layer-2 enforcement (Wave 3; depends on 110-01's `scripts/build-brain-packet-schema.cjs` and 110-03's `sendPacket`): add a `--check-sendpacket` subcommand to `scripts/check-schema-aliases.cjs` (mirroring its existing `--check-chokepoint` subcommand) that fails any commit introducing a bare `sendPacket(` call not lexically preceded by a `buildBrainPacket(` call, add two new blocks to the pre-commit hook source (`scripts/hooks/pre-commit-room-minto-guard.sh` + the byte-identical `scripts/hooks/pre-commit`) -- the `brain-packet-schema.json` `--check` drift block (clone of the Phase 122 command-registry block) and the `--check-sendpacket` D-08-layer-2 block -- re-run `scripts/setup-hooks.sh` so the live `.git/hooks/pre-commit` matches, and fill the Wave-0 `tests/test-brain-packet-precommit-hook.cjs` stub with a real `child_process` suite.

Per CONTEXT D-08 layer 2 (the pre-commit hook is the real teeth -- the origin string is in-process-forgeable, but the hook + review catch a packet built outside `buildBrainPacket`; "three layers, no crypto") and the "Claude's Discretion" note (extend `check-schema-aliases.cjs` vs a sibling script -- this plan extends, per Canon Part 7 reuse). Per RESEARCH "Implementation Approach" 3 (the `--check-sendpacket` subcommand sketch + the two pre-commit blocks), "Open Question" 4 (the installed hook does NOT currently invoke `check-schema-aliases.cjs` at all -- so wiring the new block into the hook is a real task, not just adding a subcommand), and "Common Pitfall" 4 (edit the source AND re-run `setup-hooks.sh` -- the source and the live `.git/hooks/pre-commit` are byte-identical today; the test invokes the subcommand directly via the env seams, not the installed copy).

This plan touches only `scripts/check-schema-aliases.cjs`, the two hook source files, the live `.git/hooks/pre-commit`, and `tests/test-brain-packet-precommit-hook.cjs`. It does NOT touch `data/brain-packet-schema.json` (110-01), `packet.cjs` / `memory-events.cjs` (110-02), `brain-client.cjs` / `navigation.cjs` (110-03), or the per-job validation suites (110-05). It depends on 110-01 (the `--check` script the schema-drift block invokes) and 110-03 (`sendPacket` exists -- though the hook only greps for `sendPacket(` call sites, not the implementation, so the dependency is really "the contract is in place").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-brain-context-packet-contract/110-CONTEXT.md
@.planning/phases/110-brain-context-packet-contract/110-RESEARCH.md
@.planning/phases/110-brain-context-packet-contract/110-VALIDATION.md
@CLAUDE.md
@scripts/check-schema-aliases.cjs
@scripts/hooks/pre-commit-room-minto-guard.sh
@scripts/setup-hooks.sh
@tests/test-navigation-chokepoint-hook.cjs

<interfaces>
<!--
THE --check-sendpacket SUBCOMMAND (scripts/check-schema-aliases.cjs). Mirror the existing --check-chokepoint subcommand verbatim in structure. Source: RESEARCH "Architecture Patterns -> Recommended File Layout" + "Implementation Approach" 3 + the existing checkChokepoint() in the file.
The existing seams to reuse (already in check-schema-aliases.cjs):
  - getStagedFiles() -> reads MINDRIAN_HOOK_STAGED_FILES (newline-separated) for hermetic tests; else `git diff --cached --name-only --diff-filter=ACM`.
  - readStagedContent(filePath) -> reads MINDRIAN_HOOK_STAGED_CONTENT_DIR/<path> if set; else <REPO_ROOT>/<path>; else `git show :<file>`.
  - isAllowedPath(p) / ALLOWED_DIRECT_IMPORT -- the --check-chokepoint allow-list. For --check-sendpacket we need a SEPARATE allow-list (the files where a bare sendPacket( is fine):
      const ALLOWED_SENDPACKET_FILES = [
        /^lib\/core\/brain-client\.cjs$/,        // the definition
        /^lib\/core\/navigation\.cjs$/,
        /^lib\/core\/navigation\//,
        /^tests\//,
        /^scripts\//,
      ];
      function isAllowedSendpacketPath(p) { const n = p.replace(/\\/g,'/'); return ALLOWED_SENDPACKET_FILES.some(rx => rx.test(n)); }
The check logic:
  function checkSendpacket() {
    const staged = getStagedFiles();
    const violations = [];
    for (const filePath of staged) {
      if (!/\.(cjs|js|mjs)$/.test(filePath)) continue;
      if (isAllowedSendpacketPath(filePath)) continue;
      const content = readStagedContent(filePath);
      if (!content) continue;
      const lines = content.split('\n');
      // A bare sendPacket( call is OK only if a buildBrainPacket( call appears earlier in the SAME file
      // (a coarse lexical proximity check -- the hook is the teeth; precise data-flow is not the point).
      // Walk the file: track whether we have seen a buildBrainPacket( yet. The first sendPacket( with no
      // prior buildBrainPacket( in the file is a violation. (Window: same file is enough -- if buildBrainPacket
      // is called anywhere above the sendPacket call site in the file, the packet plausibly came from it.)
      let sawBuild = false;
      for (let i = 0; i < lines.length; i++) {
        if (/\bbuildBrainPacket\s*\(/.test(lines[i])) sawBuild = true;
        const m = lines[i].match(/\bsendPacket\s*\(/);
        if (m && !sawBuild) violations.push({ file: filePath, line: i + 1, match: lines[i].trim().slice(0, 120) });
      }
    }
    if (violations.length === 0) process.exit(0);
    process.stderr.write('[check-sendpacket] FAIL (D-08 layer 2): a `sendPacket(` call site is not lexically preceded by a `buildBrainPacket(` call. Brain Context Packets must be built ONLY through lib/core/navigation.cjs::buildBrainPacket (D-01 chokepoint).\n');
    for (const v of violations) process.stderr.write('  ' + v.file + ':' + v.line + ': ' + v.match + '\n');
    process.stderr.write('Build the packet via navigation.buildBrainPacket(...) before calling brain-client.sendPacket(...), or add the path to ALLOWED_SENDPACKET_FILES in scripts/check-schema-aliases.cjs.\n');
    process.exit(1);
  }
  module.exports.checkSendpacket = checkSendpacket;
  module.exports.ALLOWED_SENDPACKET_FILES = ALLOWED_SENDPACKET_FILES;
And in the CLI dispatch (the `if (require.main === module)` block, alongside the existing `if (args.includes('--check-chokepoint')) { checkChokepoint(); return; }`):
  if (args.includes('--check-sendpacket')) { checkSendpacket(); return; }
Place the --check-sendpacket dispatch line BEFORE the --check-chokepoint one or after it -- order does not matter; both short-circuit.

THE TWO PRE-COMMIT HOOK BLOCKS (scripts/hooks/pre-commit-room-minto-guard.sh + scripts/hooks/pre-commit -- byte-identical; edit BOTH; then re-run scripts/setup-hooks.sh so .git/hooks/pre-commit matches). Place RIGHT AFTER the Phase-122 command-registry block (which is around lines 132-146, ending with the `fi` after the `node "$REPO_ROOT/scripts/build-command-registry.cjs" --check` line) and BEFORE the feynman-minto-guardian block. Source: RESEARCH "Code Examples -> The Phase 122 --check pre-commit block" (the template) + "Architecture Patterns -> Recommended File Layout":
  # ---------------------------------------------------------------------------
  # Phase 110 guardian: brain-packet-schema drift / malformed-schema tripwire.
  # ---------------------------------------------------------------------------
  if git diff --cached --name-only | grep -qE '^(data/brain-packet-schema\.json|scripts/build-brain-packet-schema\.cjs)$'; then
    if command -v node >/dev/null 2>&1 && [ -f "$REPO_ROOT/scripts/build-brain-packet-schema.cjs" ]; then
      node "$REPO_ROOT/scripts/build-brain-packet-schema.cjs" --check || { echo "brain-packet-schema drift -- run: node scripts/build-brain-packet-schema.cjs --check" >&2; exit 2; }
    fi
  fi
  # ---------------------------------------------------------------------------
  # Phase 110 guardian (D-08 layer 2): refuse a bare sendPacket( not preceded by buildBrainPacket(.
  # ---------------------------------------------------------------------------
  if command -v node >/dev/null 2>&1 && [ -f "$REPO_ROOT/scripts/check-schema-aliases.cjs" ]; then
    node "$REPO_ROOT/scripts/check-schema-aliases.cjs" --check-sendpacket || { echo "bare sendPacket( introduced -- per D-08 it must be lexically preceded by buildBrainPacket(" >&2; exit 2; }
  fi
NOTE: scripts/hooks/pre-commit-room-minto-guard.sh and scripts/hooks/pre-commit are byte-identical 8588-byte files today (and .git/hooks/pre-commit matches both). The fix: edit BOTH source files identically, then run `bash scripts/setup-hooks.sh` (it cmp -s-compares the guard source against the installed hook and byte-copies on mismatch -- so after the edit, setup-hooks.sh re-installs the new source over .git/hooks/pre-commit). The Phase-110 test (tests/test-brain-packet-precommit-hook.cjs) invokes `node scripts/check-schema-aliases.cjs --check-sendpacket` DIRECTLY (with the env seams) -- it tests the tracked source, not the installed copy.

THE TEST (tests/test-brain-packet-precommit-hook.cjs). Mirror tests/test-navigation-chokepoint-hook.cjs verbatim in structure (the spawnSync + the MINDRIAN_HOOK_STAGED_FILES / MINDRIAN_HOOK_STAGED_CONTENT_DIR fixture pattern). Source: RESEARCH "Validation Architecture -> Phase Requirements -> Test Map" row PACKET-110-05 (D-08 layer 2):
  const REPO_ROOT = path.resolve(__dirname, '..');
  const HOOK = path.join(REPO_ROOT, 'scripts', 'check-schema-aliases.cjs');
  function runCheck(files /* [{ path, content }] */) {
    const tmp = fs.mkdtempSync(path.join(os.tmpdir(), 'phase-110-sendpacket-hook-'));
    const stagedPaths = [];
    for (const f of files) { const full = path.join(tmp, f.path); fs.mkdirSync(path.dirname(full), { recursive: true }); fs.writeFileSync(full, f.content); stagedPaths.push(f.path); }
    const env = Object.assign({}, process.env, { MINDRIAN_HOOK_STAGED_FILES: stagedPaths.join('\n'), MINDRIAN_HOOK_STAGED_CONTENT_DIR: tmp });
    const r = spawnSync('node', [HOOK, '--check-sendpacket'], { env, encoding: 'utf8' });
    fs.rmSync(tmp, { recursive: true, force: true });
    return { code: r.status, stdout: r.stdout || '', stderr: r.stderr || '' };
  }
  Cases:
   1. empty staged set: runCheck([]) -> equal(code, 0).
   2. a NEW file lib/feature/new-brain-caller.cjs with a bare `const r = await sendPacket(packet);` and NO buildBrainPacket( above it -> notEqual(code, 0); ok(stderr.includes('D-08')); ok(stderr.includes('new-brain-caller.cjs')).
   3. the same file but with `const packet = navigation.buildBrainPacket(db, 'suggest_next_move', focusId);` on a line ABOVE the sendPacket( call -> equal(code, 0).
   4. an allow-listed file lib/core/brain-client.cjs with a bare `function sendPacket(packet, opts) { ... }` (the definition) -> equal(code, 0) (allow-listed, never flagged).
   5. a non-JS file commands/foo.md containing the literal text 'sendPacket(' -> equal(code, 0) (only *.cjs/*.js/*.mjs are scanned).
  Final: console.log('test-brain-packet-precommit-hook: PASS (N assertions)'); process.exit(0).
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the --check-sendpacket subcommand to scripts/check-schema-aliases.cjs</name>
  <files>scripts/check-schema-aliases.cjs</files>
  <read_first>
    - scripts/check-schema-aliases.cjs (the WHOLE file: the `--check-chokepoint` subcommand -- `checkChokepoint()`, `ALLOWED_DIRECT_IMPORT`, `BANNED_PATTERNS`, `isAllowedPath`, `getStagedFiles`, `readStagedContent` -- and the CLI dispatch `if (require.main === module) { ... if (args.includes('--check-chokepoint')) { checkChokepoint(); return; } ... }`; the `module.exports` block; the existing default-scan path -- do NOT change it)
    - .planning/phases/110-brain-context-packet-contract/110-CONTEXT.md D-08 layer 2 (the lexical guard: fail any commit that introduces a `brain-client...sendPacket(` call site not lexically preceded by a `buildBrainPacket(`; "same shape as the Phase 109 navigation-chokepoint pre-commit check")
    - .planning/phases/110-brain-context-packet-contract/110-RESEARCH.md "Architecture Patterns -> Recommended File Layout" (the `--check-sendpacket` allow-list + scan sketch) + "Implementation Approach" 3 + "Open Question" 4 + "Common Pitfall" 4
  </read_first>
  <action>
    In `scripts/check-schema-aliases.cjs`, after the existing `checkChokepoint` block (and its `module.exports.checkChokepoint = ...` / `module.exports.ALLOWED_DIRECT_IMPORT = ...` lines), add a new section (mirroring the `--check-chokepoint` section's structure):
      - `const ALLOWED_SENDPACKET_FILES = [ /^lib\/core\/brain-client\.cjs$/, /^lib\/core\/navigation\.cjs$/, /^lib\/core\/navigation\//, /^tests\//, /^scripts\// ];`
      - `function isAllowedSendpacketPath(p) { const n = p.replace(/\\/g, '/'); return ALLOWED_SENDPACKET_FILES.some(function (rx) { return rx.test(n); }); }`
      - `function checkSendpacket()` -- per the interfaces sketch above: `getStagedFiles()`, skip non-`.cjs`/`.js`/`.mjs`, skip `isAllowedSendpacketPath`, `readStagedContent`, split into lines, walk: set `sawBuild = true` on a `/\bbuildBrainPacket\s*\(/` line, push a violation on the first `/\bsendPacket\s*\(/` line with no prior `sawBuild` in the same file; on `violations.length === 0` -> `process.exit(0)`; else write the structured `[check-sendpacket] FAIL (D-08 layer 2): ...` message + each `file:line: <trimmed line>` + the recovery line ("Build the packet via navigation.buildBrainPacket(...) before calling brain-client.sendPacket(...), or add the path to ALLOWED_SENDPACKET_FILES in scripts/check-schema-aliases.cjs.") -> `process.exit(1)`.
      - `module.exports.checkSendpacket = checkSendpacket; module.exports.ALLOWED_SENDPACKET_FILES = ALLOWED_SENDPACKET_FILES; module.exports.isAllowedSendpacketPath = isAllowedSendpacketPath;`
    In the CLI dispatch block (`if (require.main === module) { const args = process.argv.slice(2); ... }`), add -- alongside the existing `if (args.includes('--check-chokepoint')) { checkChokepoint(); return; }` -- a new line: `if (args.includes('--check-sendpacket')) { checkSendpacket(); return; }`. (Order relative to `--check-chokepoint` does not matter; both short-circuit before the default scan.)
    Do NOT change `checkChokepoint`, `ALLOWED_DIRECT_IMPORT`, `BANNED_PATTERNS`, `getStagedFiles`, `readStagedContent`, the default schema-aliases scan, `buildAllowedTableSet`, `scanSql`, `checkSqlAgainstAliases`, `parseAliasesYaml`, `formatViolation`, or `ALLOWED_EXISTING_TABLES`. NO em-dashes (hyphens only). NO emoji.
  </action>
  <verify>
    <automated>node -e "const m=require('./scripts/check-schema-aliases.cjs'); if(typeof m.checkSendpacket!=='function'){console.error('checkSendpacket not exported');process.exit(1)} if(!Array.isArray(m.ALLOWED_SENDPACKET_FILES)||!m.ALLOWED_SENDPACKET_FILES.some(rx=>rx.test('lib/core/brain-client.cjs'))){console.error('allow-list missing brain-client');process.exit(1)} if(typeof m.checkChokepoint!=='function'){console.error('checkChokepoint regressed');process.exit(1)} console.log('check-schema-aliases exports OK');" && MINDRIAN_HOOK_STAGED_FILES="" node scripts/check-schema-aliases.cjs --check-sendpacket && echo "empty staged set: pass" && grep -q "check-sendpacket" scripts/check-schema-aliases.cjs && grep -q "checkSendpacket" scripts/check-schema-aliases.cjs && ! grep -lP "[\x{2014}\x{2013}]" scripts/check-schema-aliases.cjs</automated>
  </verify>
  <done>
    `scripts/check-schema-aliases.cjs` has a `--check-sendpacket` subcommand: `checkSendpacket()` scans staged `*.cjs`/`*.js`/`*.mjs` (via the existing seams) for a bare `sendPacket(` not lexically preceded by `buildBrainPacket(` in the same file, exits 1 with a D-08 message on a violation, exits 0 otherwise; the allow-list (`brain-client.cjs`, `navigation.cjs`, `navigation/`, `tests/`, `scripts/`) is exported; the `--check-chokepoint` subcommand and the default scan are unchanged; an empty staged set passes; zero em-dashes or en-dashes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the two pre-commit hook blocks (both source files) and re-run setup-hooks.sh</name>
  <files>scripts/hooks/pre-commit-room-minto-guard.sh, scripts/hooks/pre-commit, .git/hooks/pre-commit</files>
  <read_first>
    - scripts/hooks/pre-commit-room-minto-guard.sh (the WHOLE file: `$REPO_ROOT` resolution; the ROOM.md/MINTO.md guard; the Phase-122 command-registry block around lines 132-146 ending with the `fi` after `node "$REPO_ROOT/scripts/build-command-registry.cjs" --check`; the feynman-minto-guardian block -- the two new Phase-110 blocks go BETWEEN the command-registry block and the feynman-minto-guardian block)
    - scripts/hooks/pre-commit (byte-identical to the .sh file today -- the same edit, applied identically)
    - scripts/setup-hooks.sh (the installer: `cmp -s` the guard source against the installed `.git/hooks/pre-commit`; on mismatch byte-copies via a tmp file + atomic rename -- so re-running it after editing the source re-installs the new source over `.git/hooks/pre-commit`)
    - .planning/phases/110-brain-context-packet-contract/110-RESEARCH.md "Code Examples -> The Phase 122 --check pre-commit block" (the template the schema-drift block clones) + "Architecture Patterns -> Recommended File Layout" (the two-block layout) + "Common Pitfall" 4 (edit the source AND re-run setup-hooks.sh; the test invokes the subcommand directly, not the installed copy)
    - .planning/phases/110-brain-context-packet-contract/110-CONTEXT.md D-08 layer 2
  </read_first>
  <action>
    STEP 1 -- edit `scripts/hooks/pre-commit-room-minto-guard.sh`. Find the Phase-122 command-registry block (it ends with the `fi` that closes the `if git diff --cached --name-only | grep -qE '^(commands/.*\.md|data/command-registry\.json|data/framework-names\.json)$'; then ... fi`). Immediately after that `fi`, before the feynman-minto-guardian block, insert the two new blocks from the interfaces sketch above VERBATIM:
    ```bash
    # ---------------------------------------------------------------------------
    # Phase 110 guardian: brain-packet-schema drift / malformed-schema tripwire.
    # ---------------------------------------------------------------------------
    if git diff --cached --name-only | grep -qE '^(data/brain-packet-schema\.json|scripts/build-brain-packet-schema\.cjs)$'; then
      if command -v node >/dev/null 2>&1 && [ -f "$REPO_ROOT/scripts/build-brain-packet-schema.cjs" ]; then
        node "$REPO_ROOT/scripts/build-brain-packet-schema.cjs" --check || { echo "brain-packet-schema drift -- run: node scripts/build-brain-packet-schema.cjs --check" >&2; exit 2; }
      fi
    fi
    # ---------------------------------------------------------------------------
    # Phase 110 guardian (D-08 layer 2): refuse a bare sendPacket( not preceded by buildBrainPacket(.
    # ---------------------------------------------------------------------------
    if command -v node >/dev/null 2>&1 && [ -f "$REPO_ROOT/scripts/check-schema-aliases.cjs" ]; then
      node "$REPO_ROOT/scripts/check-schema-aliases.cjs" --check-sendpacket || { echo "bare sendPacket( introduced -- per D-08 it must be lexically preceded by buildBrainPacket(" >&2; exit 2; }
    fi
    ```
    STEP 2 -- apply the EXACT SAME insertion to `scripts/hooks/pre-commit` (the byte-identical sibling -- find the same Phase-122 command-registry block, insert the same two blocks in the same place). After both edits, `cmp -s scripts/hooks/pre-commit scripts/hooks/pre-commit-room-minto-guard.sh` must still succeed (they stay byte-identical).
    STEP 3 -- run `bash scripts/setup-hooks.sh` so `.git/hooks/pre-commit` (the live byte-copy) is re-installed from the now-updated source. (If `setup-hooks.sh` reports "no-op" because the live hook already matches, that means the live hook was somehow already updated -- in that case just verify `grep -q "check-sendpacket" .git/hooks/pre-commit`; if it is NOT there, manually `cp scripts/hooks/pre-commit-room-minto-guard.sh .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit` and re-run `setup-hooks.sh` to confirm it is now a no-op.)
    Do NOT change any other part of the hook scripts (the ROOM.md/MINTO.md guard, the command-registry block, the feynman-minto-guardian block). NO em-dashes (hyphens only) in the added bash. NO emoji.
  </action>
  <verify>
    <automated>grep -q "build-brain-packet-schema.cjs --check" scripts/hooks/pre-commit-room-minto-guard.sh && grep -q "check-schema-aliases.cjs --check-sendpacket" scripts/hooks/pre-commit-room-minto-guard.sh && grep -q "build-brain-packet-schema.cjs --check" scripts/hooks/pre-commit && grep -q "check-schema-aliases.cjs --check-sendpacket" scripts/hooks/pre-commit && cmp -s scripts/hooks/pre-commit scripts/hooks/pre-commit-room-minto-guard.sh && bash scripts/setup-hooks.sh >/dev/null 2>&1; grep -q "check-schema-aliases.cjs --check-sendpacket" .git/hooks/pre-commit && grep -q "build-brain-packet-schema.cjs --check" .git/hooks/pre-commit && cmp -s .git/hooks/pre-commit scripts/hooks/pre-commit-room-minto-guard.sh && ! grep -lP "[\x{2014}\x{2013}]" scripts/hooks/pre-commit-room-minto-guard.sh scripts/hooks/pre-commit</automated>
  </verify>
  <done>
    `scripts/hooks/pre-commit-room-minto-guard.sh` and the byte-identical `scripts/hooks/pre-commit` both have the two new Phase-110 blocks (the `brain-packet-schema.json` `--check` drift block + the `--check-sendpacket` D-08-layer-2 block) inserted right after the Phase-122 command-registry block; `.git/hooks/pre-commit` matches the source after `setup-hooks.sh` is re-run; zero em-dashes or en-dashes in the added bash.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fill tests/test-brain-packet-precommit-hook.cjs -- the D-08 layer-2 hook test</name>
  <files>tests/test-brain-packet-precommit-hook.cjs</files>
  <read_first>
    - tests/test-brain-packet-precommit-hook.cjs (the Wave-0 MISSING stub from 110-00 -- replace its body; keep the path)
    - tests/test-navigation-chokepoint-hook.cjs (the canonical hook-test idiom: `spawnSync('node', [HOOK, '--check-chokepoint'], { env: { ...process.env, MINDRIAN_HOOK_STAGED_FILES, MINDRIAN_HOOK_STAGED_CONTENT_DIR } })`; `makeStagedContent(files)` writes paths+content into a tmp dir; assert `r.status` + `r.stderr`; cleanup in a `finally`)
    - scripts/check-schema-aliases.cjs (just edited -- the `--check-sendpacket` subcommand; the error fragments it emits: `[check-sendpacket] FAIL (D-08 layer 2)`, the file:line lines, the recovery line; the allow-list `ALLOWED_SENDPACKET_FILES`)
    - lib/memory/run-feynman-tests.cjs (the exit-code convention: 0 = PASS, 77 = SKIPPED, other = FAIL -- this test exits 0 on all-pass, 1 on any-fail; never 77)
    - .planning/phases/110-brain-context-packet-contract/110-VALIDATION.md "Phase Requirements -> Test Map" row PACKET-110-05 (D-08 layer 2 -- what this suite must cover)
  </read_first>
  <action>
    Replace the body of `tests/test-brain-packet-precommit-hook.cjs` (keep the registered path; it stays in `lib/memory/run-feynman-tests.cjs` `TEST_FILES[]` from 110-00). Write a real `node:assert/strict` + `node:child_process` suite mirroring `tests/test-navigation-chokepoint-hook.cjs`:
      1. `const REPO_ROOT = path.resolve(__dirname, '..'); const HOOK = path.join(REPO_ROOT, 'scripts', 'check-schema-aliases.cjs');`
      2. `function runCheck(files /* [{ path, content }] */) { const tmp = fs.mkdtempSync(path.join(os.tmpdir(), 'phase-110-sendpacket-hook-')); const stagedPaths = []; for (const f of files) { const full = path.join(tmp, f.path); fs.mkdirSync(path.dirname(full), { recursive: true }); fs.writeFileSync(full, f.content); stagedPaths.push(f.path); } const env = Object.assign({}, process.env, { MINDRIAN_HOOK_STAGED_FILES: stagedPaths.join('\n'), MINDRIAN_HOOK_STAGED_CONTENT_DIR: tmp }); const r = spawnSync('node', [HOOK, '--check-sendpacket'], { env, encoding: 'utf8' }); fs.rmSync(tmp, { recursive: true, force: true }); return { code: r.status, stdout: r.stdout || '', stderr: r.stderr || '' }; }`
      3. Case 1 -- empty staged set: `const r = runCheck([]); equal(r.code, 0, 'empty staged set passes; stderr=' + r.stderr);`
      4. Case 2 -- a new caller with a bare `sendPacket(` and no `buildBrainPacket(` above it -> violation: `const r = runCheck([{ path: 'lib/feature/new-brain-caller.cjs', content: "'use strict';\nconst bc = require('../core/brain-client.cjs');\nasync function go(packet) {\n  const r = await bc.sendPacket(packet);\n  return r;\n}\nmodule.exports = { go };\n" }]); notEqual(r.code, 0, 'bare sendPacket without buildBrainPacket must fail'); ok(r.stderr.includes('D-08'), 'stderr cites D-08'); ok(r.stderr.includes('new-brain-caller.cjs'), 'stderr names the file');`
      5. Case 3 -- the same caller but with `buildBrainPacket(` on a line above the `sendPacket(` call -> OK: `const r = runCheck([{ path: 'lib/feature/new-brain-caller.cjs', content: "'use strict';\nconst nav = require('../core/navigation.cjs');\nconst bc = require('../core/brain-client.cjs');\nasync function go(db, focusId) {\n  const packet = nav.buildBrainPacket(db, 'suggest_next_move', focusId);\n  const r = await bc.sendPacket(packet, { db });\n  return r;\n}\nmodule.exports = { go };\n" }]); equal(r.code, 0, 'sendPacket preceded by buildBrainPacket passes; stderr=' + r.stderr);`
      6. Case 4 -- an allow-listed file with a bare `sendPacket(` (the definition) -> OK: `const r = runCheck([{ path: 'lib/core/brain-client.cjs', content: "'use strict';\nfunction sendPacket(packet, opts) { return packet; }\nmodule.exports = { sendPacket };\n" }]); equal(r.code, 0, 'brain-client.cjs is allow-listed; stderr=' + r.stderr);`
      7. Case 5 -- a non-JS file containing the literal text `sendPacket(` -> OK (only `*.cjs`/`*.js`/`*.mjs` scanned): `const r = runCheck([{ path: 'commands/foo.md', content: "Call sendPacket( with a packet.\n" }]); equal(r.code, 0, 'non-JS files are not scanned; stderr=' + r.stderr);`
      8. Final: `console.log('test-brain-packet-precommit-hook: PASS (N assertions)'); process.exit(0);` -- only reached if every assertion passes (they throw on failure -> non-zero exit naturally).
    NO em-dashes (hyphens only). NO emoji. Do NOT change `tests/run-all-110.sh` or `lib/memory/run-feynman-tests.cjs` -- they already reference this path from 110-00; this plan only fills the body.
  </action>
  <verify>
    <automated>node tests/test-brain-packet-precommit-hook.cjs && node tests/test-brain-packet-precommit-hook.cjs 2>&1 | grep -q "test-brain-packet-precommit-hook: PASS" && ! node tests/test-brain-packet-precommit-hook.cjs 2>&1 | grep -q "MISSING - Wave" && grep -q "spawnSync" tests/test-brain-packet-precommit-hook.cjs && grep -q "MINDRIAN_HOOK_STAGED_FILES" tests/test-brain-packet-precommit-hook.cjs && grep -q "check-sendpacket" tests/test-brain-packet-precommit-hook.cjs && ! grep -lP "[\x{2014}\x{2013}]" tests/test-brain-packet-precommit-hook.cjs</automated>
  </verify>
  <done>
    `tests/test-brain-packet-precommit-hook.cjs` is a real `child_process` suite (no longer the MISSING stub): empty staged set -> exit 0; bare `sendPacket(` without `buildBrainPacket(` -> exit non-zero with a D-08 message naming the file; `sendPacket(` preceded by `buildBrainPacket(` -> exit 0; an allow-listed file -> exit 0; a non-JS file -> exit 0; the suite exits 0 only on all-pass and prints a PASS summary; uses the `MINDRIAN_HOOK_STAGED_FILES` / `MINDRIAN_HOOK_STAGED_CONTENT_DIR` seams; zero em-dashes or en-dashes.
  </done>
</task>

</tasks>

<verification>
- `node scripts/check-schema-aliases.cjs --check-sendpacket` with `MINDRIAN_HOOK_STAGED_FILES=""` exits 0; the `--check-chokepoint` subcommand and the default scan are unchanged (`node scripts/check-schema-aliases.cjs --check-chokepoint` with empty staged set still exits 0).
- `scripts/hooks/pre-commit-room-minto-guard.sh` and `scripts/hooks/pre-commit` both contain the two new Phase-110 blocks and are still byte-identical (`cmp -s` succeeds); `.git/hooks/pre-commit` matches them after `bash scripts/setup-hooks.sh`.
- `node tests/test-brain-packet-precommit-hook.cjs` exits 0 and prints a PASS summary (no longer the MISSING stub).
- `bash tests/run-all-110.sh` now shows `test-brain-packet-precommit-hook.cjs: PASSED` (and `test-brain-packet-schema-check.cjs: PASSED` from 110-01; the remaining 2 stubs are filled by 110-05).
- `node lib/memory/run-feynman-tests.cjs` runs to completion; the `test-brain-packet-precommit-hook.cjs` entry passes.
- `grep -P "[\x{2014}\x{2013}]" scripts/check-schema-aliases.cjs scripts/hooks/pre-commit-room-minto-guard.sh scripts/hooks/pre-commit tests/test-brain-packet-precommit-hook.cjs` returns zero matches.
</verification>

<success_criteria>
1. `scripts/check-schema-aliases.cjs` has a `--check-sendpacket` subcommand (`checkSendpacket()` + `ALLOWED_SENDPACKET_FILES` + `isAllowedSendpacketPath`, exported) mirroring its `--check-chokepoint` subcommand: scans staged `*.cjs`/`*.js`/`*.mjs` (via the existing `getStagedFiles` / `readStagedContent` seams) for a bare `sendPacket(` not lexically preceded by `buildBrainPacket(` in the same file; allow-list = `brain-client.cjs` / `navigation.cjs` / `navigation/` / `tests/` / `scripts/`; exits 1 with a D-08 message on a violation, 0 otherwise; the `--check-chokepoint` subcommand and the default scan are unchanged.
2. `scripts/hooks/pre-commit-room-minto-guard.sh` and the byte-identical `scripts/hooks/pre-commit` both have two new blocks after the Phase-122 command-registry block: (a) the `brain-packet-schema.json` `--check` drift block (clone of the Phase 122 pattern); (b) the `--check-sendpacket` D-08-layer-2 block. `.git/hooks/pre-commit` matches the source after `setup-hooks.sh` is re-run.
3. `tests/test-brain-packet-precommit-hook.cjs` is a real `child_process` suite filling the Wave-0 stub: the 5 cases above (empty / bare-sendPacket-violation / preceded-by-buildBrainPacket-OK / allow-listed-OK / non-JS-OK).
4. Zero em-dashes or en-dashes in any file written.
</success_criteria>

<output>
After completion, create `.planning/phases/110-brain-context-packet-contract/110-04-SUMMARY.md` recording: the `--check-sendpacket` subcommand logic + allow-list, the two new pre-commit hook blocks (where they sit relative to the command-registry and feynman-minto-guardian blocks), the `setup-hooks.sh` re-run outcome (installed / no-op), the test stub now filled. Note that 110-05 fills the last two stubs (`test-brain-packet-validation-per-job.cjs`, `test-brain-packet-part8-invariant-per-job.cjs`) -- after which `bash tests/run-all-110.sh` is fully GREEN. Note any deviations (e.g. whether `setup-hooks.sh` had to be forced because the live hook was already drifted).
</output>
