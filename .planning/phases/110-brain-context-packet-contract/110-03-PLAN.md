---
phase: 110-brain-context-packet-contract
plan: "03"
type: execute
wave: 2
depends_on: ["110-01", "110-02"]
files_modified:
  - lib/core/brain-client.cjs
  - lib/core/navigation.cjs
autonomous: true
requirements:
  - PACKET-110-03
  - PACKET-110-04
  - PACKET-110-08
canon_parts:
  - "Part 8 (Graph Boundary -- the schema validator middleware refuses to send a leaking packet; the D-08 layer-3 origin allowlist is the belt to the schema's suspenders)"
  - "Part 9 (Memory Locality -- reject hard, degrade soft: a bad Brain response is no response; never partial-ingest; the turn proceeds with no Brain advice)"
  - "Part 4 (Every Choice Is Graph Data -- brain_packet_rejected / brain_response_rejected / brain_legacy_path_used memory_event rows record the wire decisions)"
  - "Part 7 (Reuse Before Build -- sendPacket reuses _ensureSession / getApiKey / the SSE-parse from callTool; the once-per-session warning reuses the checkFilePermissions._warned module-flag idiom; ajv is bundled via the MCP SDK -- NOT a direct dep)"
must_haves:
  truths:
    - "lib/core/brain-client.cjs gains a brand-new sendPacket(packet, opts) function (NOT a callTool branch); module scope lazily compiles data/brain-packet-schema.json once via require('ajv').default || require('ajv') + new Ajv({ allErrors:true, strict:false }) + addSchema, with per-job in/out validators memoized via _ajv.compile({ $ref: schemaId + '#/$defs/' + job + '/properties/in' or '/properties/out' })"
    - "sendPacket order: (1) D-08 layer 3 -- typeof packet.origin !== 'string' -> throw; packet.origin === 'test_fixture' && process.env.MINDRIAN_TEST_MODE !== '1' -> throw; packet.origin not in {navigation_api, test_fixture} -> throw; (2) unknown-job guard (packet.job not a D-02 job) -> throw; (3) ajv-validate packet against $defs[job].in -- on failure snapshot (fn.errors||[]).slice() immediately, log brain_packet_rejected via navigation.cjs's logMemoryEvent(opts.db, ...) when opts.db is present (skip gracefully when absent), then throw new Error('brain packet rejected for job \"'+packet.job+'\": '+formatErrs); (4) build the wire envelope (tools/call name:'brain_packet' arguments:{packet}) and POST it reusing _ensureSession/getApiKey/the SSE-parse; if the Brain returns 'unknown tool' / -32602 / a 404-ish error -> reject, DEGRADE SOFT (return the no-advice sentinel { advice: null, reason: 'brain_packet_tool_absent' }), log NOTHING (not a leak); (5) parse the Brain result, ajv-validate against $defs[job].out -- on failure do NOT ingest, log brain_response_rejected via logMemoryEvent (skip gracefully without opts.db), return { advice: null, reason: 'response_schema_invalid' } -- NEVER throw on a bad response, NEVER partial-ingest; (6) on success return the validated out (the caller -- typically code that then calls storeBrainSuggestions -- receives a known-good shape)"
    - "lib/core/navigation.cjs re-exports logMemoryEvent(db, eventType, payload) (a 14th export -- delegates to navigation/memory-events.cjs's logEvent; acceptable per the Phase 109 navigation.cjs header which states the closed surface is the documented 13-function API and the implementation re-exports internal helpers as needed)"
    - "lib/core/brain-client.cjs gains a module-level `let _legacyPathWarned = false;` plus a `_warnLegacyOnce(db)` helper: first call -> console.warn('[mindrian-os] legacy free-form Brain job call detected. Migrate to brain-client.sendPacket() -- the legacy job path is removed in v1.14.0.') + best-effort logMemoryEvent(db, 'brain_legacy_path_used', { source_path:'system:brain-legacy' }) wrapped in try/catch; subsequent calls -> no-op. A documented comment states: as of v1.13.0-beta.3 there is NO legacy free-form Brain job call site; new job-style work goes through sendPacket(); if a free-form job helper is ever added before v1.14.0 it must call _warnLegacyOnce() first; in v1.14.0 both the helper and this guard are deleted"
    - "sendPacket and _warnLegacyOnce are exported; the compiled validators + a _resetSchema() test hook + a way to reset _legacyPathWarned are added to the module.exports _test block; query() / write() / search() / schema() / callTool() / ask() / stats() are UNCHANGED (raw-Cypher methodology lookups are Part-8-clean by construction and permanent -- NOT 'legacy')"
    - "ajv is NOT added to package.json dependencies; the schema is read from data/brain-packet-schema.json by path (no JSON require so the test-mode env override stays consistent with 110-01's script)"
    - "Every file this plan ships contains zero em-dashes (U+2014) and zero en-dashes (U+2013)"
  artifacts:
    - path: "lib/core/brain-client.cjs"
      provides: "sendPacket(packet, opts) + the lazy ajv validator middleware + the D-08 layer-3 origin allowlist + the response-validate-then-degrade gate + the _warnLegacyOnce dual-path guard; query/write/search/schema/callTool unchanged"
      contains: "sendPacket"
    - path: "lib/core/navigation.cjs"
      provides: "A 14th re-export: logMemoryEvent(db, eventType, payload) -> navigation/memory-events.cjs::logEvent (so brain-client.cjs can log the brain_* events without requiring the internal module directly)"
      contains: "logMemoryEvent"
  key_links:
    - from: "lib/core/brain-client.cjs sendPacket"
      to: "data/brain-packet-schema.json"
      via: "fs.readFileSync(path.join(__dirname, '..', '..', 'data', 'brain-packet-schema.json')) + ajv compile at module scope (lazy)"
      pattern: "brain-packet-schema\\.json"
    - from: "lib/core/brain-client.cjs sendPacket"
      to: "lib/core/navigation.cjs logMemoryEvent"
      via: "require('./navigation.cjs').logMemoryEvent(opts.db, 'brain_packet_rejected'|'brain_response_rejected', payload) wrapped in try/catch"
      pattern: "logMemoryEvent"
    - from: "lib/core/brain-client.cjs sendPacket"
      to: "lib/core/brain-client.cjs callTool wire path"
      via: "reuses _ensureSession / getApiKey / the SSE-parse to POST tools/call name:'brain_packet'"
      pattern: "brain_packet"
    - from: "lib/core/navigation.cjs logMemoryEvent"
      to: "lib/core/navigation/memory-events.cjs logEvent"
      via: "module.exports.logMemoryEvent = memoryEvents.logEvent (or a thin wrapper)"
      pattern: "memoryEvents\\.logEvent"
---

<objective>
Build the wire-level enforcement (Wave 2; depends on 110-01's schema file and 110-02's `origin`/EVENT_TYPES additions): `lib/core/brain-client.cjs::sendPacket(packet, opts)` -- a brand-new validate-then-route function (NOT a `callTool()` branch) that (1) checks the D-08 layer-3 origin allowlist before anything else, (2) ajv-validates the outbound packet against `data/brain-packet-schema.json`'s `$defs[job].in` and **refuses to send** (throws + logs `brain_packet_rejected`) on failure, (3) POSTs a `tools/call name:'brain_packet'` reusing the existing `callTool` wire path and **degrades soft** (no advice, no log) when the Brain has no `brain_packet` tool, (4) ajv-validates the Brain response against `$defs[job].out` and **does NOT ingest / never throws / never partial-ingests** (logs `brain_response_rejected`, returns a no-advice sentinel) on failure, (5) returns the validated `out` on success. Plus: `lib/core/navigation.cjs` gains a 14th re-export `logMemoryEvent` (so `brain-client.cjs` can log the `brain_*` events without reaching into the internal `memory-events.cjs`), and `brain-client.cjs` gains the forward-looking `_warnLegacyOnce(db)` deprecation guard + the `_legacyPathWarned` module flag (D-10 -- documented as having no current call site).

Per CONTEXT D-04 (degrade-gracefully when the Brain doesn't recognize the contract) + D-07 ("reject hard, degrade soft" -- the verbatim stance phrase) + D-08 layer 3 (the brain-client allowlist) + D-10 (the dual-path warning). Per RESEARCH "Implementation Approach" 4 + 5 + 7, "Open Question" 1 + 2 + 5, and "Common Pitfall" 1 (snapshot `validate.errors` immediately), 2 (`strict: false` for runtime), 5 (a missing `brain_packet` tool is not a bad response), 6 (`opts.db` for the event log; skip gracefully when absent).

This plan touches only `lib/core/brain-client.cjs` and `lib/core/navigation.cjs`. It does NOT touch `data/brain-packet-schema.json` (110-01), `packet.cjs` / `memory-events.cjs` (110-02), `ingestion.cjs` (UNCHANGED -- the `out` gate sits in `sendPacket`, not in `storeBrainSuggestions`), the pre-commit hook (110-04), or the new `tests/test-brain-packet-*.cjs` suites (110-05). It depends on 110-01 (the schema file `sendPacket` compiles) and 110-02 (the `brain_*` EVENT_TYPES `logMemoryEvent` needs + the `origin` field a valid packet carries).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-brain-context-packet-contract/110-CONTEXT.md
@.planning/phases/110-brain-context-packet-contract/110-RESEARCH.md
@.planning/phases/110-brain-context-packet-contract/110-VALIDATION.md
@CLAUDE.md
@lib/core/brain-client.cjs
@lib/core/navigation.cjs
@lib/core/navigation/memory-events.cjs
@lib/core/navigation/ingestion.cjs
@data/brain-packet-schema.json
@lib/memory/brain-derivation.test.cjs

<interfaces>
<!--
THE AJV MIDDLEWARE (module scope in brain-client.cjs). Source: RESEARCH "Architecture Patterns -> Pattern 1: Compile the schema once, validate per job" (verbatim sketch, verified against ajv@8.18.0 on disk):
  const Ajv = require('ajv').default || require('ajv'); // ajv@8.18 exports a function that also has .default
  let _ajv = null, _rootValidator = null;
  const _jobValidators = new Map(); // job -> { in: fn, out: fn }
  const _BRAIN_PACKET_SCHEMA_PATH = process.env.MINDRIAN_BRAIN_PACKET_SCHEMA || path.join(__dirname, '..', '..', 'data', 'brain-packet-schema.json');
  function _schema() {
    if (_rootValidator) return;
    const schema = JSON.parse(fs.readFileSync(_BRAIN_PACKET_SCHEMA_PATH, 'utf8'));
    _ajv = new Ajv({ allErrors: true, strict: false }); // strict:false -> a hand-maintained schema may have benign constructs strict-mode would throw on at compile time; do NOT set removeAdditional (we REJECT extras, not strip them)
    _ajv.addSchema(schema);                              // registers under schema.$id
    _rootValidator = _ajv.compile(schema);
    _schema._schemaId = schema.$id;
  }
  function _validatorFor(job, half /* 'in' | 'out' */) {
    _schema();
    let pair = _jobValidators.get(job);
    if (!pair) { pair = {}; _jobValidators.set(job, pair); }
    if (!pair[half]) pair[half] = _ajv.compile({ $ref: _schema._schemaId + '#/$defs/' + job + '/properties/' + half });
    return pair[half];
  }
  function _resetSchema() { _ajv = null; _rootValidator = null; _jobValidators.clear(); _schema._schemaId = null; }  // test hook
  const SHIPPED_JOBS = new Set([ ...the 12 D-02 jobnames... ]);
GOTCHA (Pitfall 1): validate.errors is overwritten on every validate() call -- snapshot (fn.errors||[]).slice() immediately after a failing fn(data).
GOTCHA (Pitfall 2): strict:false for runtime; the BUILD-time strict:true gate is in scripts/build-brain-packet-schema.cjs (110-01) -- do NOT change that.

THE sendPacket FUNCTION (brain-client.cjs). Source: RESEARCH "Implementation Approach" 4 (the step-by-step sketch):
  /**
   * Validate-then-route a Brain Context Packet. The ONLY door for typed Brain job calls.
   * @param {object} packet  - MUST come from lib/core/navigation.cjs::buildBrainPacket (D-01 chokepoint; D-08 layer 2 enforces lexically).
   * @param {object} [opts]  - { db?, roomDir? } for the memory_event log; if absent the event log is skipped (Brain calls are best-effort).
   * @returns {Promise<object>} the validated Brain `out` on success; { advice: null, reason: '...' } on a soft-degrade (brain_packet tool absent | response schema invalid).
   * @throws on a bad `in` packet (a programmer error in OUR code) or a bad `origin` (D-08 layer 3). NEVER throws on a bad Brain response.
   */
  async function sendPacket(packet, opts) {
    const o = opts || {};
    // (1) D-08 layer 3 -- the belt to the schema's suspenders. Runs FIRST so a caller who bypassed ajv still gets caught.
    if (!packet || typeof packet.origin !== 'string') throw new Error('brain packet missing origin (D-08): packets must come from buildBrainPacket');
    if (packet.origin === 'test_fixture' && process.env.MINDRIAN_TEST_MODE !== '1') throw new Error('brain packet origin "test_fixture" only valid when MINDRIAN_TEST_MODE=1');
    if (packet.origin !== 'navigation_api' && packet.origin !== 'test_fixture') throw new Error('brain packet origin "' + packet.origin + '" not in the closed allowlist (D-08): navigation_api | test_fixture');
    // (2) unknown-job guard.
    if (!SHIPPED_JOBS.has(packet.job)) throw new Error('brain packet: unknown job "' + packet.job + '" (not in the D-02 closed vocabulary)');
    // (3) `in` validation -- reject hard.
    const inFn = _validatorFor(packet.job, 'in');
    if (!inFn(packet)) {
      const errs = (inFn.errors || []).slice().map(function (e) { return (e.instancePath || '(root)') + ' ' + e.message; }).join('; ');
      _logEventBestEffort(o.db, 'brain_packet_rejected', { job: packet.job, errors: (inFn.errors || []).length, source_path: 'system:brain-packet' });
      throw new Error('brain packet rejected for job "' + packet.job + '": ' + errs);
    }
    // (4) build the wire envelope + POST. Reuse callTool's transport (it does tools/call over Streamable HTTP + SSE-parse).
    let result;
    try { result = await callTool('brain_packet', { packet: packet }); }
    catch (e) { /* network/transport error -- treat like the Brain being unreachable: degrade soft, no log (not a leak) */ return { advice: null, reason: 'brain_unreachable' }; }
    if (_looksLikeUnknownToolError(result)) { /* the live Brain has no `brain_packet` tool yet (D-04 generalized) -> degrade soft, NO log */ return { advice: null, reason: 'brain_packet_tool_absent' }; }
    // (5) `out` validation -- reject hard but degrade soft (a half-trusted response is no response).
    const parsed = _parseBrainResult(result); // unwrap the MCP tools/call result envelope to the response object
    const outFn = _validatorFor(packet.job, 'out');
    if (!outFn(parsed)) {
      _logEventBestEffort(o.db, 'brain_response_rejected', { job: packet.job, errors: (outFn.errors || []).length, source_path: 'system:brain-packet' });
      return { advice: null, reason: 'response_schema_invalid' };  // NEVER throw on a bad response; NEVER partial-ingest
    }
    // (6) success -- the caller gets a known-good shape (typically then calls navigation.storeBrainSuggestions).
    return parsed;
  }
HELPERS:
  function _logEventBestEffort(db, eventType, payload) { if (!db) return; try { require('./navigation.cjs').logMemoryEvent(db, eventType, payload); } catch (_) { /* best-effort */ } }
  function _looksLikeUnknownToolError(result) { // callTool returns { text: 'Error: ...' } on error per the existing code (see the JSDoc at line ~329); also handle a -32602 / 'unknown tool' / 'method not found' / 'No such tool' string + a 404-ish marker. Be liberal: any error string mentioning the tool name not existing.
    if (!result) return false; const t = (result && (result.text || result.error || JSON.stringify(result))) || ''; const s = String(t).toLowerCase();
    return s.includes('unknown tool') || s.includes('no such tool') || s.includes('method not found') || s.includes('-32602') || (s.includes('brain_packet') && s.includes('not')) || s.includes('not found'); }
  function _parseBrainResult(result) { // callTool returns the tools/call result content array directly (or { text: ... }); unwrap to the response object. If it is an array of { type:'text', text: '<json>' } parse the JSON; if it is already an object return it.
    if (Array.isArray(result)) { for (const item of result) { if (item && item.type === 'text' && typeof item.text === 'string') { try { return JSON.parse(item.text); } catch (_) { /* fall through */ } } } return { suggestions: [] }; }
    if (result && typeof result === 'object' && !result.text) return result;
    if (result && typeof result.text === 'string') { try { return JSON.parse(result.text); } catch (_) { return { suggestions: [] }; } }
    return { suggestions: [] }; }
NOTE: callTool / _ensureSession / getApiKey are already in brain-client.cjs -- reuse them; do NOT add a second HTTP path. The exact unwrap shape (_parseBrainResult) should match how callTool returns results in this codebase -- read the callTool body + its JSDoc (~line 329) to get it right.

THE DUAL-PATH GUARD (brain-client.cjs). Source: RESEARCH "Implementation Approach" 7 + "Open Question" 5 + "Code Examples -> Module-level once-per-process warning flag":
  let _legacyPathWarned = false;
  /**
   * Deprecation guard for any free-form Brain *job* call. As of v1.13.0-beta.3 there is NO such call site --
   * new job-style work goes through brain-client.sendPacket(). If a free-form job helper is ever added before
   * v1.14.0, it MUST call _warnLegacyOnce() first; in v1.14.0 both the helper and this guard are deleted.
   * (query() / write() / search() / schema() / ask() / stats() / callTool() raw-Cypher methodology lookups are
   * NOT "legacy" -- they carry only generic handles and are Part-8-clean by construction; never touch them here.)
   */
  function _warnLegacyOnce(db) {
    if (_legacyPathWarned) return;
    _legacyPathWarned = true;
    console.warn('[mindrian-os] legacy free-form Brain job call detected. Migrate to brain-client.sendPacket() -- the legacy job path is removed in v1.14.0.');
    try { require('./navigation.cjs').logMemoryEvent(db, 'brain_legacy_path_used', { source_path: 'system:brain-legacy' }); } catch (_) { /* best-effort */ }
  }

THE navigation.cjs RE-EXPORT (lib/core/navigation.cjs). The file currently re-exports exactly 13 documented functions + has a `notImplementedYet` helper. Add a 14th export:
  // Memory-event logging (Phase 110-03 -- a thin re-export so brain-client.cjs can log brain_* events
  //   without reaching into the internal navigation/memory-events.cjs; the closed surface is the DOCUMENTED
  //   13-function API, the implementation re-exports internal helpers as needed -- see this file's header).
  logMemoryEvent: memoryEvents.logEvent,
(memoryEvents is already required at the top: `const memoryEvents = require('./navigation/memory-events.cjs');`. logEvent(db, eventType, payload) is its signature -- it returns { ok: true, ... } on success, { ok:false, reason:'invalid_event_type' } if the type is not in EVENT_TYPES. Just re-export it.)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 14th re-export logMemoryEvent to navigation.cjs</name>
  <files>lib/core/navigation.cjs</files>
  <read_first>
    - lib/core/navigation.cjs (the whole `module.exports` block -- the 13 documented functions + the `notImplementedYet` helper + the header comment about the closed surface; `memoryEvents` is already required)
    - lib/core/navigation/memory-events.cjs (the `logEvent(db, eventType, payload)` signature + its return shape `{ ok: true, ... }` / `{ ok: false, reason: 'invalid_event_type' }`)
    - .planning/phases/110-brain-context-packet-contract/110-RESEARCH.md "Open Question" 2 (the disposition: a 14th re-export is fine; the closed surface is the DOCUMENTED 13-function API and the implementation re-exports internal helpers as needed)
  </read_first>
  <action>
    In `lib/core/navigation.cjs`, add one line to the `module.exports` object (after the existing 13 entries, e.g. after `promoteNodeStatus`), with a short comment:
    ```javascript
      // Memory-event logging (Phase 110-03 -- a thin re-export so brain-client.cjs can log the
      //   brain_packet_rejected / brain_response_rejected / brain_legacy_path_used events without
      //   reaching into the internal navigation/memory-events.cjs. The closed navigation surface is the
      //   DOCUMENTED 13-function API; the implementation re-exports internal helpers as needed.)
      logMemoryEvent: memoryEvents.logEvent,
    ```
    Do NOT change any of the 13 existing exports. Do NOT change `notImplementedYet` or the header. NO em-dashes (hyphens only). NO emoji.
  </action>
  <verify>
    <automated>node -e "const nav=require('./lib/core/navigation.cjs'); if(typeof nav.logMemoryEvent!=='function'){console.error('logMemoryEvent not exported');process.exit(1)} const ev=require('./lib/core/navigation/memory-events.cjs'); if(nav.logMemoryEvent!==ev.logEvent){console.error('logMemoryEvent should re-export memory-events.logEvent');process.exit(1)} console.log('logMemoryEvent OK');" && grep -q "logMemoryEvent" lib/core/navigation.cjs && ! grep -lP "[\x{2014}\x{2013}]" lib/core/navigation.cjs</automated>
  </verify>
  <done>
    `lib/core/navigation.cjs` exports a 14th function `logMemoryEvent(db, eventType, payload)` re-exporting `navigation/memory-events.cjs::logEvent`; the 13 documented exports are unchanged; zero em-dashes or en-dashes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add sendPacket + the ajv validator middleware + the D-08 layer-3 allowlist + the response-degrade gate + _warnLegacyOnce to brain-client.cjs</name>
  <files>lib/core/brain-client.cjs</files>
  <read_first>
    - lib/core/brain-client.cjs (the WHOLE file: BRAIN_URL; getApiKey / isAvailable; _ensureSession + the sessionCache + _hashKey; callTool(toolName, args) -- the JSON-RPC-over-Streamable-HTTP + SSE-parse + the return shape, and its JSDoc around line 329; the existing module-level `checkFilePermissions._warned` flag idiom; query / write / search / smartSearch / ask / schema / stats / enrichCausalEdges / hatAwareRecommend / suggestValidationSteps / getFrameworkChain; the `module.exports` block + the `_test` block)
    - .planning/phases/110-brain-context-packet-contract/110-CONTEXT.md D-04 (Brain doesn't recognize packet_version -> reject -> degrade gracefully) + D-07 (the full "reject hard, degrade soft" stance: bad `in` -> throw + refuse to send + log brain_packet_rejected; bad `out` -> do NOT ingest + log brain_response_rejected + degrade gracefully; NEVER throw on a bad response; NEVER partial-ingest) + D-08 layer 3 (brain-client allowlist: origin must be navigation_api, or test_fixture only when MINDRIAN_TEST_MODE=1) + D-10 (the dual-path warning) + D-02 (the 12 jobnames)
    - .planning/phases/110-brain-context-packet-contract/110-RESEARCH.md "Architecture Patterns -> Pattern 1" (the verbatim ajv module-scope sketch) + "Implementation Approach" 4 (the verbatim sendPacket step-by-step) + 7 (the _warnLegacyOnce idiom) + "Open Question" 1 (the wire envelope: POST tools/call name:'brain_packet' arguments:{packet}; degrade soft when the tool is absent) + 5 (no current legacy call site) + 6 (opts.db for the event log; skip gracefully when absent) + "Common Pitfalls" 1 (snapshot validate.errors), 2 (strict:false runtime), 5 (a missing brain_packet tool is not a bad response -> no brain_response_rejected log), 6 (no db handle -> skip the event log)
    - lib/memory/brain-derivation.test.cjs (the `require.cache` mock-the-Brain idiom -- read it so the sendPacket implementation is mockable: 110-05's tests will install a fake brain-client wire path; keep callTool/the transport overridable -- e.g. have sendPacket call the module's own `callTool` (not a captured local) so a test can monkeypatch `module.exports.callTool` ... actually the cleaner seam: export `_test: { _resetSchema, _setLegacyWarned, _parseBrainResult, _looksLikeUnknownToolError, _validatorFor }` and have 110-05 stub `callTool` via require.cache OR a `__sendPacketTransport` override hook -- RECOMMENDED: add an optional `opts.__transport` (a function `(toolName, args) => Promise<result>`) that defaults to `callTool`, so a test can inject a fake without require.cache surgery)
    - data/brain-packet-schema.json (the 110-01 schema -- $id `https://mindrian.ai/schemas/brain-packet/1.0`; $defs[job].properties.in / .properties.out; the BrainResponse out shape)
    - lib/core/navigation/memory-events.cjs EVENT_TYPES (the 3 new strings from 110-02: brain_packet_rejected / brain_response_rejected / brain_legacy_path_used must already be in the Set -- 110-03 depends on 110-02 for exactly this)
    - CLAUDE.md "What NOT to Use" (ajv bundled-never-direct-dep -> require('ajv').default || require('ajv'); CJS not ESM; NO em-dashes)
  </read_first>
  <behavior>
    - Test 1: `sendPacket(packet)` where `packet.origin` is missing/undefined -> throws an Error whose message mentions origin / D-08.
    - Test 2: `sendPacket({ ...validPacket, origin: 'test_fixture' })` with `process.env.MINDRIAN_TEST_MODE !== '1'` -> throws; with `MINDRIAN_TEST_MODE === '1'` -> proceeds (no origin throw).
    - Test 3: `sendPacket({ ...validPacket, origin: 'forged' })` -> throws (origin not in the closed allowlist).
    - Test 4: `sendPacket({ ...validPacket, job: 'not_a_real_job' })` -> throws (unknown job).
    - Test 5: `sendPacket(malformedInPacket)` (e.g. an extra top-level `transcript` field, or a missing `local_graph_summary`) -> throws an Error whose message starts `brain packet rejected for job "..."`; if `opts.db` is passed, a `brain_packet_rejected` `memory_event` row is logged.
    - Test 6: `sendPacket(validPacket, { __transport: async () => ({ text: 'Error: unknown tool brain_packet' }) })` -> resolves to `{ advice: null, reason: 'brain_packet_tool_absent' }`; NO `brain_response_rejected` row is logged (a missing tool is not a bad response).
    - Test 7: `sendPacket(validPacket, { __transport: async () => [{ type:'text', text: JSON.stringify({ totally: 'off-spec', extra: 'field' }) }] })` -> resolves to `{ advice: null, reason: 'response_schema_invalid' }`, does NOT throw, does NOT partial-ingest; if `opts.db` is passed a `brain_response_rejected` row is logged.
    - Test 8: `sendPacket(validPacket, { __transport: async () => [{ type:'text', text: JSON.stringify({ job_id:'x', suggestions:[{ summary:'do X', methodology:'JTBD', confidence: 0.8 }] }) }] })` -> resolves to the parsed `{ job_id:'x', suggestions:[...] }` object (a known-good shape the caller can hand to `storeBrainSuggestions`).
    - Test 9: `_warnLegacyOnce(db)` called twice -> `console.warn` fires exactly once; a `brain_legacy_path_used` row is logged exactly once. (A test resets the flag via `_test._setLegacyWarned(false)` between cases.)
  </behavior>
  <action>
    In `lib/core/brain-client.cjs`:
      1. Near the top (after the existing `require`s -- `fs`/`path` are likely already required for `checkFilePermissions`; add them if not): add the ajv module-scope block from the interfaces sketch -- `const Ajv = require('ajv').default || require('ajv');`, `let _ajv = null; let _rootValidator = null; const _jobValidators = new Map();`, `const _BRAIN_PACKET_SCHEMA_PATH = process.env.MINDRIAN_BRAIN_PACKET_SCHEMA || path.join(__dirname, '..', '..', 'data', 'brain-packet-schema.json');`, `function _schema()` (lazy: read+parse the schema file, `new Ajv({ allErrors:true, strict:false })`, `addSchema`, `compile`, stash `_schema._schemaId = schema.$id`), `function _validatorFor(job, half)` (memoize `_ajv.compile({ $ref: _schema._schemaId + '#/$defs/' + job + '/properties/' + half })`), `function _resetSchema()` (clear all of the above -- the test hook), and `const SHIPPED_JOBS = new Set([ ... the 12 D-02 jobnames ... ]);`. A comment: "ajv@8.18.0 is transitive via @modelcontextprotocol/sdk -- do NOT add it to package.json (CLAUDE.md 'What NOT to Use'). strict:false at runtime; the strict:true build gate lives in scripts/build-brain-packet-schema.cjs."
      2. Add the `_logEventBestEffort(db, eventType, payload)`, `_looksLikeUnknownToolError(result)`, and `_parseBrainResult(result)` helpers from the interfaces sketch. Make `_parseBrainResult` match how `callTool` actually returns results in this codebase (read the `callTool` body + its JSDoc around line ~329 -- it returns the content array, or `{ text: 'Error: ...' }` on error; unwrap accordingly).
      3. Add the `_legacyPathWarned` module flag + the `_warnLegacyOnce(db)` helper from the interfaces sketch, with the documented JSDoc (no current call site; new job-style work goes through `sendPacket`; if a free-form job helper is ever added before v1.14.0 it must call `_warnLegacyOnce()` first; in v1.14.0 both are deleted; `query`/`write`/`search`/`schema`/`ask`/`stats`/`callTool` are NOT "legacy").
      4. Add `async function sendPacket(packet, opts)` exactly per the interfaces step-by-step: (1) D-08 layer-3 origin checks (missing -> throw; test_fixture without MINDRIAN_TEST_MODE=1 -> throw; not in {navigation_api, test_fixture} -> throw); (2) unknown-job guard (`!SHIPPED_JOBS.has(packet.job)` -> throw); (3) `in` validation -- `const inFn = _validatorFor(packet.job, 'in'); if (!inFn(packet)) { const errs = (inFn.errors||[]).slice().map(...).join('; '); _logEventBestEffort((opts||{}).db, 'brain_packet_rejected', { job: packet.job, errors: (inFn.errors||[]).length, source_path: 'system:brain-packet' }); throw new Error('brain packet rejected for job "'+packet.job+'": '+errs); }`; (4) build + POST -- `const transport = (opts && opts.__transport) || callTool; let result; try { result = await transport('brain_packet', { packet }); } catch (e) { return { advice: null, reason: 'brain_unreachable' }; } if (_looksLikeUnknownToolError(result)) return { advice: null, reason: 'brain_packet_tool_absent' };` (NO log -- it is not a leak); (5) `out` validation -- `const parsed = _parseBrainResult(result); const outFn = _validatorFor(packet.job, 'out'); if (!outFn(parsed)) { _logEventBestEffort((opts||{}).db, 'brain_response_rejected', { job: packet.job, errors: (outFn.errors||[]).length, source_path: 'system:brain-packet' }); return { advice: null, reason: 'response_schema_invalid' }; }` (NEVER throw, NEVER partial-ingest); (6) `return parsed;`.
      5. Update `module.exports`: add `sendPacket` and `_warnLegacyOnce` to the public exports (alongside `query`, `write`, etc.); add `_resetSchema`, `_validatorFor`, `_parseBrainResult`, `_looksLikeUnknownToolError`, `_schema`, and a `_setLegacyWarned: function (v) { _legacyPathWarned = !!v; }` to the existing `_test` block (the not-part-of-the-public-API test surface). Do NOT remove or change `query` / `write` / `search` / `smartSearch` / `ask` / `schema` / `stats` / `callTool` / `getApiKey` / `isAvailable` / `enrichCausalEdges` / `hatAwareRecommend` / `suggestValidationSteps` / `getFrameworkChain`.
    Do NOT add `ajv` to `package.json`. Do NOT touch `ingestion.cjs` (the `out` gate is here in `sendPacket`, not in `storeBrainSuggestions` -- `storeBrainSuggestions` stays exactly as Phase 109 ships it). NO em-dashes (hyphens only). NO emoji.
  </action>
  <verify>
    <automated>node -e "
      const path=require('node:path'), fs=require('node:fs'), os=require('node:os');
      const REPO=process.cwd();
      const bc = require(path.join(REPO,'lib','core','brain-client.cjs'));
      if (typeof bc.sendPacket!=='function'){console.error('sendPacket not exported');process.exit(1)}
      if (typeof bc._warnLegacyOnce!=='function'){console.error('_warnLegacyOnce not exported');process.exit(1)}
      if (!bc._test || typeof bc._test._resetSchema!=='function' || typeof bc._test._setLegacyWarned!=='function'){console.error('test hooks missing');process.exit(1)}
      // build a minimally-valid packet via buildBrainPacket so the schema accepts it.
      const { openRoomDb } = require(path.join(REPO,'lib','core','room-db.cjs'));
      const nav = require(path.join(REPO,'lib','core','navigation.cjs'));
      const tmp=fs.mkdtempSync(path.join(os.tmpdir(),'p110-03-')); fs.mkdirSync(path.join(tmp,'.mindrian'),{recursive:true});
      const db=openRoomDb(tmp); const now=Date.now();
      const insN=db.prepare(\"INSERT OR IGNORE INTO nodes (id,type,properties,source_path,created_by,confidence,review_status,created_at,last_seen_at,source_section) VALUES (?,?,?,?,'user',?,?,?,?,?)\");
      insN.run('room:t','room','{}','fx',1.0,'confirmed',now,now,null);
      insN.run('decision:f','decision',JSON.stringify({summary:'s'}),'fx',0.7,'confirmed',now,now,'d');
      const mocks={jtbd:{getCurrent:()=>({current:null})},operator:{getCurrent:()=>({current:null})}};
      const pkt = nav.buildBrainPacket(db,'suggest_next_move','decision:f',{_mocks:mocks,roomId:'t'});
      (async ()=>{
        // bad origin -> throws
        let threw=false; try { await bc.sendPacket(Object.assign({},pkt,{origin:'forged'})); } catch(_) { threw=true; }
        if(!threw){console.error('forged origin did not throw');process.exit(1)}
        // missing origin -> throws
        threw=false; const noOrigin=Object.assign({},pkt); delete noOrigin.origin; try { await bc.sendPacket(noOrigin); } catch(_) { threw=true; }
        if(!threw){console.error('missing origin did not throw');process.exit(1)}
        // unknown job -> throws
        threw=false; try { await bc.sendPacket(Object.assign({},pkt,{job:'not_a_real_job'})); } catch(_) { threw=true; }
        if(!threw){console.error('unknown job did not throw');process.exit(1)}
        // malformed in (extra field) -> throws with 'brain packet rejected'
        threw=false; let msg=''; try { await bc.sendPacket(Object.assign({},pkt,{transcript:'leak'})); } catch(e) { threw=true; msg=String(e.message); }
        if(!threw||!/brain packet rejected/.test(msg)){console.error('malformed in not rejected: '+msg);process.exit(1)}
        // brain_packet tool absent -> degrade soft, reason brain_packet_tool_absent
        let r = await bc.sendPacket(pkt, { __transport: async ()=>({ text:'Error: unknown tool brain_packet' }) });
        if(!r||r.reason!=='brain_packet_tool_absent'){console.error('tool-absent not degraded: '+JSON.stringify(r));process.exit(1)}
        // off-spec out -> degrade soft, reason response_schema_invalid, no throw
        r = await bc.sendPacket(pkt, { __transport: async ()=>([{ type:'text', text: JSON.stringify({ totally:'off-spec', extra:'x' }) }]) });
        if(!r||r.reason!=='response_schema_invalid'){console.error('off-spec out not degraded: '+JSON.stringify(r));process.exit(1)}
        // good out -> returns the parsed object
        r = await bc.sendPacket(pkt, { __transport: async ()=>([{ type:'text', text: JSON.stringify({ job_id:'x', suggestions:[{ summary:'do X', methodology:'JTBD', confidence:0.8 }] }) }]) });
        if(!r||!Array.isArray(r.suggestions)||r.suggestions[0].summary!=='do X'){console.error('good out not returned: '+JSON.stringify(r));process.exit(1)}
        // _warnLegacyOnce twice -> warn once
        bc._test._setLegacyWarned(false);
        let warnCount=0; const origWarn=console.warn; console.warn=()=>{warnCount++;};
        bc._warnLegacyOnce(); bc._warnLegacyOnce(); console.warn=origWarn;
        if(warnCount!==1){console.error('_warnLegacyOnce fired '+warnCount+' times, expected 1');process.exit(1)}
        fs.rmSync(tmp,{recursive:true,force:true});
        console.log('sendPacket OK');
      })().catch(e=>{console.error('async fail: '+e.message);process.exit(1)});
    " && grep -q "sendPacket" lib/core/brain-client.cjs && grep -q "brain_packet_rejected" lib/core/brain-client.cjs && grep -q "_warnLegacyOnce" lib/core/brain-client.cjs && grep -q "require('ajv')" lib/core/brain-client.cjs && ! grep -q '"ajv"' package.json && ! grep -lP "[\x{2014}\x{2013}]" lib/core/brain-client.cjs</automated>
  </verify>
  <done>
    `lib/core/brain-client.cjs` has `sendPacket(packet, opts)` (D-08 layer-3 origin allowlist -> unknown-job guard -> ajv `in` validation reject-hard + `brain_packet_rejected` log -> POST `tools/call name:'brain_packet'` via the existing wire path with soft-degrade when the tool is absent -> ajv `out` validation reject-hard-degrade-soft + `brain_response_rejected` log, never throw, never partial-ingest -> return the validated `out`), the module-scope lazy ajv middleware (`strict:false`), the `_warnLegacyOnce(db)` dual-path guard + `_legacyPathWarned` flag (no current call site, documented), and the `_test` block with `_resetSchema` / `_setLegacyWarned` / the helpers; `query`/`write`/`search`/`schema`/`callTool` are unchanged; `package.json` does not list `ajv`; zero em-dashes or en-dashes.
  </done>
</task>

</tasks>

<verification>
- `lib/core/navigation.cjs` exports `logMemoryEvent` (a 14th re-export of `navigation/memory-events.cjs::logEvent`); the 13 documented exports are unchanged.
- `lib/core/brain-client.cjs` exports `sendPacket` and `_warnLegacyOnce`; the `_test` block exposes `_resetSchema`, `_setLegacyWarned`, `_validatorFor`, `_parseBrainResult`, `_looksLikeUnknownToolError`, `_schema`.
- `sendPacket`: bad/missing/forged `origin` -> throws (D-08 layer 3); unknown job -> throws; malformed `in` -> throws `brain packet rejected for job "..."` + (with `opts.db`) logs `brain_packet_rejected`; `brain_packet` tool absent -> `{ advice: null, reason: 'brain_packet_tool_absent' }` and NO `brain_response_rejected` log; off-spec `out` -> `{ advice: null, reason: 'response_schema_invalid' }`, no throw, no partial-ingest, + (with `opts.db`) logs `brain_response_rejected`; good `out` -> returns the parsed object.
- `_warnLegacyOnce` fires `console.warn` exactly once per process; `query`/`write`/`search`/`schema`/`callTool`/`ask`/`stats` are byte-unchanged.
- `node lib/memory/run-feynman-tests.cjs` runs to completion; existing brain-client / navigation tests still pass; the new Phase-110 stubs are still RED (correct -- 110-04 / 110-05 fill them).
- `package.json` does not list `ajv` as a dependency.
- `grep -P "[\x{2014}\x{2013}]" lib/core/brain-client.cjs lib/core/navigation.cjs` returns zero matches.
</verification>

<success_criteria>
1. `lib/core/navigation.cjs` has a 14th re-export `logMemoryEvent(db, eventType, payload)` -> `navigation/memory-events.cjs::logEvent`.
2. `lib/core/brain-client.cjs::sendPacket(packet, opts)` is a brand-new function (NOT a `callTool` branch) implementing: D-08 layer-3 origin allowlist (runs first) -> unknown-job guard -> ajv `in` validation (reject hard: throw + log `brain_packet_rejected`) -> POST `tools/call name:'brain_packet'` reusing `callTool`'s transport (soft-degrade with `{ advice:null, reason:'brain_packet_tool_absent' }` and NO log when the tool is absent; `brain_unreachable` on a transport error) -> ajv `out` validation (reject hard, degrade soft: log `brain_response_rejected`, return `{ advice:null, reason:'response_schema_invalid' }`, never throw, never partial-ingest) -> return the validated `out` on success.
3. The module-scope ajv middleware compiles `data/brain-packet-schema.json` once with `new Ajv({ allErrors:true, strict:false })` and memoizes per-job `in`/`out` validators; `validate.errors` is snapshotted immediately on a failing call; `ajv` is `require('ajv').default || require('ajv')` and is NOT a direct dependency.
4. `_warnLegacyOnce(db)` + `_legacyPathWarned` ship as the forward-looking D-10 deprecation guard with a documented "no current call site" comment; `query`/`write`/`search`/`schema`/`callTool` are explicitly untouched.
5. `storeBrainSuggestions` (in `ingestion.cjs`) is NOT touched -- the `out` gate sits in `sendPacket`.
6. Zero em-dashes or en-dashes in any file written.
</success_criteria>

<output>
After completion, create `.planning/phases/110-brain-context-packet-contract/110-03-SUMMARY.md` recording: the `sendPacket` flow shipped, the ajv middleware module-scope structure, the soft-degrade reasons (`brain_packet_tool_absent` | `brain_unreachable` | `response_schema_invalid`), the `navigation.cjs` `logMemoryEvent` re-export, the `_warnLegacyOnce` guard, the `__transport` test seam, and the `_test` hooks added. Note that 110-04 wires the D-08 layer-2 pre-commit hook (which greps for `sendPacket(` call sites) and 110-05 fills the per-job validation suite. Note any deviations (e.g. the exact `_parseBrainResult` unwrap shape, if it differed from the sketch because `callTool` returns results differently).
</output>
