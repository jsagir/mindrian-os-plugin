---
phase: 110-brain-context-packet-contract
plan: "01"
type: execute
wave: 1
depends_on: ["110-00"]
files_modified:
  - data/brain-packet-schema.json
  - scripts/build-brain-packet-schema.cjs
  - data/ROOM.md
  - tests/test-brain-packet-schema-check.cjs
autonomous: true
requirements:
  - PACKET-110-01
  - PACKET-110-02
  - PACKET-110-09
canon_parts:
  - "Part 8 (Graph Boundary -- the machine-readable wire contract that makes leaks structurally hard; additionalProperties:false on every object node is the teeth)"
  - "Part 9 (Memory Locality -- Brain reasons over typed packets, never raw memory; this plan defines the typed shape)"
  - "Part 7 (Reuse Before Build -- scripts/build-brain-packet-schema.cjs mirrors scripts/build-command-registry.cjs verbatim; ajv@8.18.0 is bundled via the MCP SDK -- NOT a direct dep)"
must_haves:
  truths:
    - "data/brain-packet-schema.json exists, is valid JSON draft 2020-12, compiles under ajv@8.18.0 with strict:true, has a $def for all 12 D-02 jobs each carrying an in and an out sub-schema, and has additionalProperties:false on every object node (root, every shared $def, every job in/out, every nested object)"
    - "Every shipped job $def.in carries packet_version: { const: '1.0' }, job: { const: '<jobname>' }, origin: { $ref: '#/$defs/Origin' }, privacy_mode: { const: 'local_summary_only' }, and local_graph_summary: { $ref: '#/$defs/LocalGraphSummary' }"
    - "scripts/build-brain-packet-schema.cjs --check exits 0 on the shipped schema and exits 1 on a malformed JSON copy / a copy with a job missing in or out / a copy with a $def naming a non-D-02 job / a copy missing additionalProperties:false on some object node, printing a one-line recovery command on stderr"
    - "data/ROOM.md gains a brain-packet-schema.json row in the Files in this section table (generated-checked, not generated-from; bound to Phase 110; canon parts 8 + 9)"
    - "tests/test-brain-packet-schema-check.cjs is a real child_process suite (no longer the MISSING stub): --check exits 0 on the shipped schema; exits non-zero on the 4 mangled copies above; stderr names the failing condition and the recovery line; the test exits 0 only when every assertion passes"
    - "ajv is NOT added to package.json dependencies (it stays transitive via @modelcontextprotocol/sdk); the script uses require('ajv').default || require('ajv')"
    - "Every file this plan ships contains zero em-dashes (U+2014) and zero en-dashes (U+2013)"
  artifacts:
    - path: "data/brain-packet-schema.json"
      provides: "The single source of truth for the Brain Context Packet wire format: $defs per job (in + out), shared $defs, privacy_mode enum, packet_version const, origin enum, additionalProperties:false everywhere"
      contains: "select_methodology"
    - path: "scripts/build-brain-packet-schema.cjs"
      provides: "The generated-checked tripwire: default run validates well-formedness + 12-job coverage + no-unknown-job + recursive additionalProperties:false; --check re-runs in memory and exits 1 on any failure with a recovery line"
      min_lines: 70
    - path: "tests/test-brain-packet-schema-check.cjs"
      provides: "Real child_process suite around scripts/build-brain-packet-schema.cjs --check: exit 0 on the good schema; exit non-zero on 4 deliberately-mangled copies; mirrors the Phase 122 command-registry --check test idiom"
      min_lines: 40
  key_links:
    - from: "scripts/build-brain-packet-schema.cjs"
      to: "data/brain-packet-schema.json"
      via: "fs.readFileSync at default-run and --check time; ajv compile"
      pattern: "brain-packet-schema\\.json"
    - from: "tests/test-brain-packet-schema-check.cjs"
      to: "scripts/build-brain-packet-schema.cjs"
      via: "spawnSync('node', ['scripts/build-brain-packet-schema.cjs', '--check'], ...)"
      pattern: "build-brain-packet-schema\\.cjs"
    - from: "scripts/build-brain-packet-schema.cjs SHIPPED_JOBS const"
      to: "the 12 D-02 job $defs in data/brain-packet-schema.json"
      via: "for (const job of SHIPPED_JOBS) assert defs[job].in && defs[job].out"
      pattern: "SHIPPED_JOBS"
---

<objective>
Ship the wire-format contract artifact and its drift tripwire (Wave 1, parallel with 110-02): `data/brain-packet-schema.json` (the hand-maintained single source of truth -- a JSON Schema with a `$def` per all 12 D-02 jobs carrying an `in` and an `out` shape, shared `$defs` for `LocalGraphSummary` / `BankedOpportunities` / the privacy enum / the origin enum / the `packet_version` const, and `additionalProperties: false` on every object node), `scripts/build-brain-packet-schema.cjs` (a verbatim structural clone of `scripts/build-command-registry.cjs`'s `--check` tripwire), the `data/ROOM.md` row for the new artifact, and the real `tests/test-brain-packet-schema-check.cjs` body filling the Wave-0 stub.

Per CONTEXT D-04 + D-05 + D-06 + D-08 and RESEARCH "Implementation Approach" 1-3: the schema lives at `data/brain-packet-schema.json`, the validator is `ajv@8.18.0` (already on disk via the MCP SDK -- NEVER a direct dependency, per CLAUDE.md "What NOT to Use"), the `--check` script is the generated-checked-with-pre-commit-tripwire pattern Phase 122 established, and `additionalProperties: false` everywhere is the leak-prevention teeth ("a packet that carries an extra `transcript` or `body` field is refused at the wire, not stripped-and-sent").

This plan creates `data/brain-packet-schema.json` and `scripts/build-brain-packet-schema.cjs` from scratch (no imports beyond Node built-ins + the transitive ajv) and fills one Wave-0 test stub. It depends only on 110-00 (the requirement IDs + the stub paths). It does NOT touch `packet.cjs` (origin field is 110-02), `brain-client.cjs` (sendPacket is 110-03), or the pre-commit hook (D-08 layer 2 is 110-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-brain-context-packet-contract/110-CONTEXT.md
@.planning/phases/110-brain-context-packet-contract/110-RESEARCH.md
@.planning/phases/110-brain-context-packet-contract/110-VALIDATION.md
@CLAUDE.md
@scripts/build-command-registry.cjs
@lib/core/navigation/packet.cjs
@data/ROOM.md
@tests/test-navigation-packet-part8-leak.cjs

<interfaces>
<!--
THE SCHEMA SHAPE (data/brain-packet-schema.json). Source: RESEARCH "Architecture Patterns -> Recommended File Layout" + "Implementation Approach" 1-2 + lib/core/navigation/packet.cjs's buildBrainPacket return shape. draft 2020-12. ONE root schema, $defs + $ref (NOT separate files).

Top level:
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mindrian-os.com/schemas/brain-packet/1.0",
  "$defs": { ... }

Shared $defs (factored once, $ref'd from every job's `in`):
  "PrivacyMode":   { "enum": ["local_summary_only", "allow_filenames", "allow_excerpts"] }
  "Origin":        { "enum": ["navigation_api", "test_fixture"] }
  "FocusNode":     { "type":"object", "additionalProperties":false, "required":["id","type","summary"],
                     "properties":{ "id":{"type":"string"}, "type":{"type":["string","null"]}, "summary":{"type":"string"} } }
  "ActiveContext": { "type":"object", "additionalProperties":false, "required":["jtbd","operator","focus_node"],
                     "properties":{ "jtbd":{"type":["string","null"]}, "operator":{"type":"string"},
                       "focus_node":{"$ref":"#/$defs/FocusNode"} } }
  "BankedOpportunities": { "type":"object", "additionalProperties":false, "required":["count","items"],
                     "properties":{ "count":{"type":"integer","minimum":0},
                       "items":{ "type":"array", "maxItems":3, "items":{ "type":"object", "additionalProperties":false,
                         "properties":{ "id_hash":{"type":"string","maxLength":64},
                           "tags":{"type":"array","items":{"type":"string","maxLength":40}},
                           "hsi_band":{"enum":["high","medium","low"]},
                           "composite_score":{"type":"number"} } } } } }
  "Constraints":   { "type":"object", "additionalProperties":false,
                     "properties":{ "privacy":{"type":"string"}, "max_tokens":{"type":"integer"} } }
  -- per-projection $defs for the safe-projection mapper outputs in packet.cjs:
  "ClaimProjection":         { "type":"object", "additionalProperties":false, ... id / type / summary / confidence / review_status / source_section ... }
  "ContradictionProjection": { "type":"object", "additionalProperties":false, ... }
  "UnsupportedProjection":   { "type":"object", "additionalProperties":false, ... }
  "RecentChangeProjection":  { "type":"object", "additionalProperties":false, ... }
  "LocalGraphSummary": { "type":"object", "additionalProperties":false,
                     "required":["nearest_claims","nearest_assumptions","contradictions","unsupported_claims","recent_changes","banked_opportunities"],
                     "properties":{ "nearest_claims":{"type":"array","items":{"$ref":"#/$defs/ClaimProjection"}},
                       "nearest_assumptions":{"type":"array","items":{"$ref":"#/$defs/ClaimProjection"}},
                       "contradictions":{"type":"array","items":{"$ref":"#/$defs/ContradictionProjection"}},
                       "unsupported_claims":{"type":"array","items":{"$ref":"#/$defs/UnsupportedProjection"}},
                       "recent_changes":{"type":"array","items":{"$ref":"#/$defs/RecentChangeProjection"}},
                       "banked_opportunities":{"$ref":"#/$defs/BankedOpportunities"} } }
  -- a uniform `out` shape (each job may tighten later; v1.0 ships uniform):
  "BrainResponse": { "type":"object", "additionalProperties":false,
                     "properties":{ "job_id":{"type":"string"},
                       "suggestions":{"type":"array","items":{ "type":"object","additionalProperties":false,
                         "properties":{ "suggestion_index":{"type":"integer"}, "summary":{"type":"string"},
                           "methodology":{"type":"string"}, "body":{"type":"string"}, "confidence":{"type":"number"},
                           "graph_updates_proposed":{"type":"array","items":{ "type":"object","additionalProperties":false,
                             "properties":{ "source":{"type":"string"}, "target":{"type":"string"}, "type":{"type":"string"}, "confidence":{"type":"number"} } } } } } } } }

Per-job $defs (12 of them; names = the D-02 vocabulary exactly):
  "<jobname>": {
    "in":  { "type":"object", "additionalProperties":false,
             "required":["packet_version","job","room_stage","origin","privacy_mode","active_context","local_graph_summary","constraints"],
             "properties":{ "packet_version":{"const":"1.0"}, "job":{"const":"<jobname>"},
               "room_stage":{"type":["string","null"]}, "origin":{"$ref":"#/$defs/Origin"},
               "privacy_mode":{"const":"local_summary_only"}, "active_context":{"$ref":"#/$defs/ActiveContext"},
               "local_graph_summary":{"$ref":"#/$defs/LocalGraphSummary"}, "constraints":{"$ref":"#/$defs/Constraints"} } },
    "out": { "$ref":"#/$defs/BrainResponse" }
  }
The 12 jobnames (D-02): select_methodology, suggest_next_move, detect_contradiction, summarize_neighborhood,
  classify_room_budding, rank_assumptions, generate_feynman_explanation, strengthen_minto, prepare_investor_brief,
  opportunity_react, opportunity_reflect, opportunity_rank.

NOTE: packet.cjs (110-02) adds `origin` + `privacy_mode` as new top-level fields next to `packet_version`. This schema's `in.required` already lists them. The round-trip test in 110-05 catches any shape mismatch between this schema and what buildBrainPacket actually returns.

THE GENERATOR (scripts/build-brain-packet-schema.cjs). Source: RESEARCH "Architecture Patterns -> Pattern 2" (the verbatim sketch). Structure mirrors scripts/build-command-registry.cjs:
  'use strict';
  const fs = require('node:fs'); const path = require('node:path');
  const REPO_ROOT = path.resolve(__dirname, '..');
  const SCHEMA_PATH = path.join(REPO_ROOT, 'data', 'brain-packet-schema.json');
  // ajv is transitive via @modelcontextprotocol/sdk; if a future SDK bump removes it, this is the canary.
  const SHIPPED_JOBS = Object.freeze([ ...the 12 D-02 jobnames... ]);
  const SHARED_DEFS = new Set(['PrivacyMode','Origin','FocusNode','ActiveContext','BankedOpportunities','Constraints',
    'ClaimProjection','ContradictionProjection','UnsupportedProjection','RecentChangeProjection','LocalGraphSummary','BrainResponse']);
  function assertAdditionalPropsFalse(node, pathStr, errs) { // recurse: any { type:'object', properties:{...} } missing additionalProperties:false -> errs.push(...)
    ... walks $defs, properties, items ... }
  function checkSchema(strictBuild) {
    const errs = []; let schema;
    try { schema = JSON.parse(fs.readFileSync(SCHEMA_PATH,'utf8')); }
    catch (e) { return ['data/brain-packet-schema.json is missing or not valid JSON: ' + e.message]; }
    try { const Ajv = require('ajv').default || require('ajv'); new Ajv({ allErrors:true, strict: strictBuild ? true : false }).compile(schema); }
    catch (e) { errs.push('schema does not compile under ajv: ' + e.message); }
    const defs = (schema && schema.$defs) || {};
    for (const job of SHIPPED_JOBS) {
      if (!defs[job]) { errs.push('missing $def for shipped job: ' + job); continue; }
      if (!defs[job].in)  errs.push('$def "' + job + '" has no "in" sub-schema');
      if (!defs[job].out) errs.push('$def "' + job + '" has no "out" sub-schema');
      const jc = defs[job].in && defs[job].in.properties && defs[job].in.properties.job;
      if (!jc || jc.const !== job) errs.push('$def "' + job + '" in.properties.job must be { const: "' + job + '" }');
    }
    for (const name of Object.keys(defs)) {
      if (SHIPPED_JOBS.includes(name)) continue;
      if (SHARED_DEFS.has(name)) continue;
      errs.push('$def "' + name + '" is neither a D-02 job nor a known shared def -- closed vocabulary violation');
    }
    assertAdditionalPropsFalse(schema, '#', errs);
    return errs;
  }
  function main() {
    const isCheck = process.argv.slice(2).includes('--check');
    const errs = checkSchema(true); // build always uses strict to surface schema-cleanliness warnings
    if (errs.length) { console.error(errs.join('\n'));
      console.error('Recovery: fix data/brain-packet-schema.json (the hand-maintained source of truth), then re-run: node scripts/build-brain-packet-schema.cjs --check');
      process.exit(1); }
    console.log(isCheck ? 'brain-packet-schema: OK' : 'brain-packet-schema: OK (validated; nothing to regenerate -- the schema is hand-maintained)');
  }
  if (require.main === module) main(); else module.exports = { checkSchema, SHIPPED_JOBS, SHARED_DEFS };

THE TEST (tests/test-brain-packet-schema-check.cjs). Mirror the Phase 122 command-registry --check test idiom + tests/test-navigation-chokepoint-hook.cjs's spawnSync style:
  - spawnSync('node', [SCRIPT, '--check'], { cwd: REPO_ROOT }) -> assert status === 0 on the shipped schema.
  - write 4 mangled copies of data/brain-packet-schema.json into a tmp dir, point the script at each via a SCHEMA_PATH override (the script reads a fixed path -- so instead: copy the repo schema to a tmp REPO_ROOT-shaped dir, mangle, run the script with cwd=that dir; OR add a MINDRIAN_BRAIN_PACKET_SCHEMA env override to the script for the test seam -- RECOMMENDED: add the env seam, like check-schema-aliases.cjs's MINDRIAN_HOOK_STAGED_CONTENT_DIR). Mangles: (a) echo '{}' -- malformed/empty; (b) JSON.parse, delete defs[someJob].in, re-stringify; (c) JSON.parse, defs.not_a_real_job = {in:{},out:{}}, re-stringify; (d) JSON.parse, delete additionalProperties from some nested object, re-stringify. For each: spawnSync --check with the env override -> assert status !== 0 AND stderr contains the expected fragment ('missing $def' / 'no "in"' / 'closed vocabulary' / 'additionalProperties') AND stderr contains 'Recovery:'.
  - exit 0 only if every assertion passes; print a PASS summary line.
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship data/brain-packet-schema.json + scripts/build-brain-packet-schema.cjs + the data/ROOM.md row</name>
  <files>data/brain-packet-schema.json, scripts/build-brain-packet-schema.cjs, data/ROOM.md</files>
  <read_first>
    - .planning/phases/110-brain-context-packet-contract/110-RESEARCH.md "## Architecture Patterns -> Recommended File Layout" + "Pattern 1: Compile the schema once, validate per job" + "Pattern 2: The --check tripwire script" (the verbatim schema sketch + the verbatim generator sketch)
    - .planning/phases/110-brain-context-packet-contract/110-CONTEXT.md decisions D-02 (the 12-job closed vocabulary -- the EXACT jobnames), D-04 (packet_version 1.0), D-05 (ajv, no direct dep), D-06 (data/brain-packet-schema.json, generated-checked), D-08 (the origin enum: navigation_api in prod, test_fixture only when MINDRIAN_TEST_MODE=1)
    - lib/core/navigation/packet.cjs buildBrainPacket return object (the live shape: packet_version, job, room_stage, active_context.{jtbd,operator,focus_node.{id,type,summary}}, local_graph_summary.{nearest_claims,nearest_assumptions,contradictions,unsupported_claims,recent_changes,banked_opportunities}, constraints.{privacy,max_tokens}) + the safe-projection mappers (safeNodeProjection / safeContradictionProjection / safeUnsupportedProjection / safeRecentChangeProjection / surface_banked_opportunities / hsiBand) -- the schema's per-projection $defs must match their outputs
    - scripts/build-command-registry.cjs (the verbatim structural template: 'use strict'; REPO_ROOT; the const arrays; the checkSchema-style function; the main() process.argv switch; the require.main === module guard; the recovery-line-on-stderr + process.exit(1) on failure)
    - lib/core/navigation/ingestion.cjs storeBrainSuggestions (the reader of the Brain `out` -- the BrainResponse $def's suggestions[] shape must match what it reads: each suggestion -> brain_insight node; graph_updates_proposed if present)
    - data/ROOM.md (the "Files in this section" table -- add a row for brain-packet-schema.json mirroring the command-registry.json row's format)
    - CLAUDE.md "What NOT to Use" (ajv bundled-never-direct-dep; CJS not ESM; node assert + child_process is the test framework; no zod/jest/mocha/vitest; no commander/yargs -- process.argv switch; NO em-dashes)
  </read_first>
  <behavior>
    - Test 1: `JSON.parse(fs.readFileSync('data/brain-packet-schema.json','utf8'))` succeeds (valid JSON) and has `$schema`, `$id`, `$defs`.
    - Test 2: `new (require('ajv').default || require('ajv'))({ allErrors:true, strict:true }).compile(schema)` does not throw (the schema compiles clean under strict mode -- the build gate).
    - Test 3: for each of the 12 D-02 jobnames, `schema.$defs[job]` exists and has both `.in` and `.out`; `schema.$defs[job].in.properties.job.const === job`; `schema.$defs[job].in.properties.packet_version.const === '1.0'`; `schema.$defs[job].in.properties.privacy_mode.const === 'local_summary_only'`; `schema.$defs[job].in.properties.origin.$ref === '#/$defs/Origin'`; `schema.$defs[job].in.properties.local_graph_summary.$ref === '#/$defs/LocalGraphSummary'`; `schema.$defs[job].in.required` includes `origin` and `privacy_mode`.
    - Test 4: recursively walking the schema, every `{ type:'object', properties:{...} }` node (root, every shared $def, every job in/out, every nested object) carries `additionalProperties: false`.
    - Test 5: `schema.$defs.Origin.enum` deep-equals `['navigation_api','test_fixture']`; `schema.$defs.PrivacyMode.enum` deep-equals `['local_summary_only','allow_filenames','allow_excerpts']`.
    - Test 6: `node scripts/build-brain-packet-schema.cjs` exits 0 on the shipped schema; `node scripts/build-brain-packet-schema.cjs --check` exits 0; stdout contains `brain-packet-schema: OK`.
  </behavior>
  <action>
    STEP 1 -- write `data/brain-packet-schema.json`. Use the schema shape in the interfaces block above as the spec. Concretely:
      - draft 2020-12; `"$schema": "https://json-schema.org/draft/2020-12/schema"`; `"$id": "https://mindrian-os.com/schemas/brain-packet/1.0"`.
      - The shared `$defs`: `PrivacyMode` (enum of D-03's 3), `Origin` (enum `["navigation_api","test_fixture"]`), `FocusNode`, `ActiveContext`, `BankedOpportunities` (count integer >= 0; items array maxItems 3 of {id_hash string maxLength 64, tags array of string maxLength 40, hsi_band enum [high,medium,low], composite_score number}), `Constraints` ({privacy string, max_tokens integer}), `ClaimProjection` / `ContradictionProjection` / `UnsupportedProjection` / `RecentChangeProjection` (match the safe-projection mapper outputs in packet.cjs -- read those functions; keep the projections permissive on optional fields but `additionalProperties: false`), `LocalGraphSummary` (the 6-key object with `banked_opportunities: { "$ref": "#/$defs/BankedOpportunities" }`), `BrainResponse` (the uniform `out` shape -- `job_id` optional, `suggestions` array of {suggestion_index?, summary?, methodology?, body?, confidence?, graph_updates_proposed?: array of {source, target, type, confidence?}}; `additionalProperties: false` at every level -- matches what `ingestion.cjs` reads).
      - The 12 per-job `$defs` (names = the D-02 vocabulary EXACTLY: `select_methodology`, `suggest_next_move`, `detect_contradiction`, `summarize_neighborhood`, `classify_room_budding`, `rank_assumptions`, `generate_feynman_explanation`, `strengthen_minto`, `prepare_investor_brief`, `opportunity_react`, `opportunity_reflect`, `opportunity_rank`). Each `{ "in": {...}, "out": {"$ref":"#/$defs/BrainResponse"} }`. The `in` is `{ "type":"object", "additionalProperties":false, "required":["packet_version","job","room_stage","origin","privacy_mode","active_context","local_graph_summary","constraints"], "properties":{ "packet_version":{"const":"1.0"}, "job":{"const":"<jobname>"}, "room_stage":{"type":["string","null"]}, "origin":{"$ref":"#/$defs/Origin"}, "privacy_mode":{"const":"local_summary_only"}, "active_context":{"$ref":"#/$defs/ActiveContext"}, "local_graph_summary":{"$ref":"#/$defs/LocalGraphSummary"}, "constraints":{"$ref":"#/$defs/Constraints"} } }`. The 12 `in` sub-schemas differ ONLY in the `job` const -- so author one, then clone+swap the jobname 12 times. (Per RESEARCH "Implementation Approach" 2: `buildBrainPacket` builds the same `local_graph_summary` regardless of job; only `job` and the `out` shape differ; the `out` is uniform in v1.0.)
      - `additionalProperties: false` on EVERY object node -- root (if root is `{type:'object', properties:...}` -- the root here is just `{$schema,$id,$defs}`, which is itself an object but not a JSON-Schema-validation node; put `additionalProperties:false` only on the validation-schema objects, i.e. inside `$defs`), every shared `$def` that is an object, every job `in` and `out`, every nested object inside them. This is the leak teeth -- the `--check` script asserts it recursively.
    STEP 2 -- write `scripts/build-brain-packet-schema.cjs`. Use the generator sketch in the interfaces block above as the spec. Concretely:
      - `'use strict';` + `require('node:fs')` + `require('node:path')`. `REPO_ROOT = path.resolve(__dirname, '..')`. `SCHEMA_PATH = process.env.MINDRIAN_BRAIN_PACKET_SCHEMA || path.join(REPO_ROOT, 'data', 'brain-packet-schema.json')` (the env override is the test seam -- mirrors `check-schema-aliases.cjs`'s `MINDRIAN_HOOK_STAGED_CONTENT_DIR`). A header comment: "ajv is transitive via @modelcontextprotocol/sdk; if a future SDK bump removes it, this is the canary -- do NOT add ajv to package.json (CLAUDE.md 'What NOT to Use')."
      - `SHIPPED_JOBS = Object.freeze([ ... the 12 D-02 jobnames ... ])`.
      - `SHARED_DEFS = new Set([ 'PrivacyMode','Origin','FocusNode','ActiveContext','BankedOpportunities','Constraints','ClaimProjection','ContradictionProjection','UnsupportedProjection','RecentChangeProjection','LocalGraphSummary','BrainResponse' ])` (the non-job `$def` names allow-list; keep it in sync with the schema's shared `$defs`).
      - `function assertAdditionalPropsFalse(node, pathStr, errs)` -- recurse: if `node && typeof node === 'object'`: if `node.type === 'object' && node.properties && typeof node.properties === 'object'` and `node.additionalProperties !== false` -> `errs.push('object schema at ' + pathStr + ' is missing "additionalProperties": false (leak-prevention requirement)')`; then recurse into `node.$defs`, `node.properties` (each value), `node.items` (object or array), `node.allOf`/`node.anyOf`/`node.oneOf` arrays. (Skip pure `$ref` nodes -- they have no `type`/`properties`.) Build `pathStr` as a JSON-pointer-ish string for the error message.
      - `function checkSchema(strictBuild)` -- exactly the sketch: read+parse (return early on JSON error); ajv compile under `strict: strictBuild ? true : false` (catch -> push); for each `SHIPPED_JOBS` job assert `defs[job]` exists and has `.in`+`.out` and `in.properties.job.const === job`; for each `Object.keys(defs)` not in `SHIPPED_JOBS` and not in `SHARED_DEFS` push the closed-vocabulary violation; `assertAdditionalPropsFalse(schema, '#', errs)`; return `errs`.
      - `function main()` -- `const isCheck = process.argv.slice(2).includes('--check'); const errs = checkSchema(true);` (build always strict); on `errs.length` -> `console.error(errs.join('\n')); console.error('Recovery: fix data/brain-packet-schema.json (the hand-maintained source of truth), then re-run: node scripts/build-brain-packet-schema.cjs --check'); process.exit(1);` -- else `console.log(isCheck ? 'brain-packet-schema: OK' : 'brain-packet-schema: OK (validated; nothing to regenerate -- the schema is hand-maintained)');`.
      - `if (require.main === module) main(); else module.exports = { checkSchema, SHIPPED_JOBS, SHARED_DEFS, assertAdditionalPropsFalse };`
    STEP 3 -- update `data/ROOM.md`. Add a row to the "Files in this section" table for `brain-packet-schema.json`, mirroring the `command-registry.json` row's format. Suggested cell text: "`brain-packet-schema.json` | `scripts/build-brain-packet-schema.cjs` (validated, not generated -- hand-maintained source of truth) | The Brain Context Packet wire format: `$defs` per all 12 closed-vocabulary Brain jobs (each carrying an `in` and an `out` shape), shared `$defs` for `LocalGraphSummary` / `BankedOpportunities` / the `privacy_mode` enum / the `packet_version` const / the `origin` enum, `additionalProperties: false` on every object node (the leak-prevention teeth). `lib/core/brain-client.cjs::sendPacket` compiles it once and validates every outbound packet and every Brain response against the job's `in`/`out`. DO NOT hand-edit carelessly -- the pre-commit hook and the Feynman runner reject a malformed schema / a job missing `in`+`out` / an unknown-job `$def` / a missing `additionalProperties: false`. Re-validate: `node scripts/build-brain-packet-schema.cjs --check`. Bound to Phase 110; canon parts 8 + 9." Also add `scripts/build-brain-packet-schema.cjs` to the "Cross-references" list. If `data/ROOM.md` frontmatter lists `founding_phase`/`phase`/`canon_parts`, leave those AS-IS (122 is still the founding phase); just append the row + cross-ref.
    NO em-dashes (hyphens only) anywhere. NO emoji. Do NOT touch `package.json` -- ajv stays transitive.
  </action>
  <verify>
    <automated>node -e "const s=require('./data/brain-packet-schema.json'); const Ajv=require('ajv').default||require('ajv'); new Ajv({allErrors:true,strict:true}).compile(s); const jobs=['select_methodology','suggest_next_move','detect_contradiction','summarize_neighborhood','classify_room_budding','rank_assumptions','generate_feynman_explanation','strengthen_minto','prepare_investor_brief','opportunity_react','opportunity_reflect','opportunity_rank']; for(const j of jobs){const d=s.\$defs[j]; if(!d||!d.in||!d.out){console.error('bad def '+j);process.exit(1)} if(d.in.properties.job.const!==j||d.in.properties.packet_version.const!=='1.0'||d.in.properties.privacy_mode.const!=='local_summary_only'){console.error('bad in const '+j);process.exit(1)} if(!d.in.required.includes('origin')||!d.in.required.includes('privacy_mode')){console.error('missing required '+j);process.exit(1)}} if(JSON.stringify(s.\$defs.Origin.enum)!=='[\"navigation_api\",\"test_fixture\"]'){console.error('bad origin enum');process.exit(1)} (function walk(n,p){ if(!n||typeof n!=='object')return; if(n.type==='object'&&n.properties&&typeof n.properties==='object'&&n.additionalProperties!==false){console.error('missing additionalProperties:false at '+p);process.exit(1)} if(n.\$defs)for(const k of Object.keys(n.\$defs))walk(n.\$defs[k],p+'/\$defs/'+k); if(n.properties)for(const k of Object.keys(n.properties))walk(n.properties[k],p+'/properties/'+k); if(n.items)walk(n.items,p+'/items'); for(const kw of ['allOf','anyOf','oneOf'])if(Array.isArray(n[kw]))n[kw].forEach((x,i)=>walk(x,p+'/'+kw+'/'+i)); })(s,'#'); console.log('schema OK');" && node scripts/build-brain-packet-schema.cjs && node scripts/build-brain-packet-schema.cjs --check && grep -q "brain-packet-schema.json" data/ROOM.md && grep -q "build-brain-packet-schema.cjs" data/ROOM.md && ! grep -q '"ajv"' package.json && ! grep -lP "[\x{2014}\x{2013}]" data/brain-packet-schema.json scripts/build-brain-packet-schema.cjs data/ROOM.md</automated>
  </verify>
  <done>
    `data/brain-packet-schema.json` is valid JSON, compiles under ajv@8.18.0 strict, has all 12 D-02 job `$defs` with `in`+`out` and the right `const`s and `required` fields, and has `additionalProperties: false` on every object node. `scripts/build-brain-packet-schema.cjs` exists, default-run and `--check` both exit 0 on the shipped schema and print `brain-packet-schema: OK`. `data/ROOM.md` has the new row + cross-ref. `package.json` does not list `ajv`. Zero em-dashes or en-dashes in any file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fill tests/test-brain-packet-schema-check.cjs -- the --check tripwire suite</name>
  <files>tests/test-brain-packet-schema-check.cjs</files>
  <read_first>
    - tests/test-brain-packet-schema-check.cjs (the Wave-0 MISSING stub from 110-00 -- replace its body; keep the path)
    - tests/test-navigation-chokepoint-hook.cjs (the canonical spawnSync child_process test idiom: `spawnSync('node', [SCRIPT, ...args], { env, encoding:'utf8' })`; assert `r.status` and `r.stderr`; tmp-dir fixtures via `fs.mkdtempSync`; cleanup in a `finally`)
    - scripts/build-brain-packet-schema.cjs (just written -- the `MINDRIAN_BRAIN_PACKET_SCHEMA` env override is the seam this test uses to point the script at a mangled copy; the error fragments it emits: 'missing $def for shipped job', '$def "<job>" has no "in"', 'closed vocabulary violation', 'missing "additionalProperties": false', 'Recovery:')
    - lib/memory/run-feynman-tests.cjs (the exit-code convention: 0 = PASS, 77 = SKIPPED, other = FAIL -- this test exits 0 on all-pass, 1 on any-fail; never 77)
    - .planning/phases/110-brain-context-packet-contract/110-VALIDATION.md "Phase Requirements -> Test Map" rows PACKET-110-01 + PACKET-110-02 (what this suite must cover)
  </read_first>
  <action>
    Replace the body of `tests/test-brain-packet-schema-check.cjs` (keep the registered path; it stays in `lib/memory/run-feynman-tests.cjs` `TEST_FILES[]` from 110-00). Write a real `node:assert/strict` + `node:child_process` suite:
      1. `const REPO_ROOT = path.resolve(__dirname, '..'); const SCRIPT = path.join(REPO_ROOT, 'scripts', 'build-brain-packet-schema.cjs'); const REAL_SCHEMA = path.join(REPO_ROOT, 'data', 'brain-packet-schema.json');`
      2. Sanity: `spawnSync('node', [SCRIPT, '--check'], { cwd: REPO_ROOT, encoding:'utf8' })` -> `equal(r.status, 0, 'shipped schema must pass --check; stderr=' + r.stderr)`; `ok(/brain-packet-schema: OK/.test(r.stdout), 'OK line printed')`.
      3. Mangle helper: `function runWithSchema(schemaObjOrText) { const tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'phase-110-schema-')); const p = path.join(tmpDir, 'mangled-schema.json'); fs.writeFileSync(p, typeof schemaObjOrText === 'string' ? schemaObjOrText : JSON.stringify(schemaObjOrText, null, 2)); const env = Object.assign({}, process.env, { MINDRIAN_BRAIN_PACKET_SCHEMA: p }); const r = spawnSync('node', [SCRIPT, '--check'], { env, cwd: REPO_ROOT, encoding:'utf8' }); fs.rmSync(tmpDir, { recursive:true, force:true }); return r; }`
      4. Mangle (a) -- malformed JSON: `const r = runWithSchema('{');` (or `'{}'` for empty-but-valid -> still fails because no job `$defs`). Assert `notEqual(r.status, 0)` AND `ok(r.stderr.includes('Recovery:'))`. (For `'{'` the message is "not valid JSON"; for `'{}'` it is "missing $def for shipped job". Test BOTH: a literal `'{'` -> stderr includes "not valid JSON"; `'{}'` -> stderr includes "missing $def for shipped job".)
      5. Mangle (b) -- a job missing `in`: `const base = JSON.parse(fs.readFileSync(REAL_SCHEMA,'utf8')); delete base.$defs.select_methodology.in; const r = runWithSchema(base);` -> `notEqual(r.status, 0)`; `ok(r.stderr.includes('has no "in"') || r.stderr.includes('select_methodology'), 'names the broken job')`; `ok(r.stderr.includes('Recovery:'))`.
      6. Mangle (c) -- unknown-job `$def`: `const base = JSON.parse(fs.readFileSync(REAL_SCHEMA,'utf8')); base.$defs.not_a_real_job = { in: { type:'object', additionalProperties:false, properties:{ job:{const:'not_a_real_job'} } }, out: { type:'object', additionalProperties:false } }; const r = runWithSchema(base);` -> `notEqual(r.status, 0)`; `ok(r.stderr.includes('closed vocabulary') || r.stderr.includes('not_a_real_job'), 'flags the unknown job')`; `ok(r.stderr.includes('Recovery:'))`.
      7. Mangle (d) -- a nested object missing `additionalProperties:false`: `const base = JSON.parse(fs.readFileSync(REAL_SCHEMA,'utf8')); delete base.$defs.LocalGraphSummary.additionalProperties; const r = runWithSchema(base);` -> `notEqual(r.status, 0)`; `ok(r.stderr.includes('additionalProperties') && r.stderr.includes('LocalGraphSummary'), 'pinpoints the hole')`; `ok(r.stderr.includes('Recovery:'))`.
      8. Final: `console.log('test-brain-packet-schema-check: PASS (N assertions)'); process.exit(0);` -- only reached if every `assert`/`ok`/`equal` above passed (they throw on failure, which surfaces a non-zero exit naturally).
    NO em-dashes (hyphens only). NO emoji. Do NOT change `tests/run-all-110.sh` or `lib/memory/run-feynman-tests.cjs` -- they already reference this path from 110-00; this plan only fills the body.
  </action>
  <verify>
    <automated>node tests/test-brain-packet-schema-check.cjs && node tests/test-brain-packet-schema-check.cjs 2>&1 | grep -q "test-brain-packet-schema-check: PASS" && ! node tests/test-brain-packet-schema-check.cjs 2>&1 | grep -q "MISSING - Wave" && grep -c "spawnSync" tests/test-brain-packet-schema-check.cjs | awk '$1 >= 1 { exit 0 } { exit 1 }' && grep -q "MINDRIAN_BRAIN_PACKET_SCHEMA" tests/test-brain-packet-schema-check.cjs && ! grep -lP "[\x{2014}\x{2013}]" tests/test-brain-packet-schema-check.cjs</automated>
  </verify>
  <done>
    `tests/test-brain-packet-schema-check.cjs` is a real `child_process` suite (no longer the MISSING stub): `--check` exits 0 on the shipped schema; exits non-zero with the expected stderr fragments + `Recovery:` line on each of the 4 mangled copies; the suite exits 0 only on all-pass and prints a PASS summary; uses the `MINDRIAN_BRAIN_PACKET_SCHEMA` env seam; zero em-dashes or en-dashes.
  </done>
</task>

</tasks>

<verification>
- `data/brain-packet-schema.json` is valid JSON, compiles under ajv@8.18.0 with `strict: true`, has a `$def` for all 12 D-02 jobs each carrying `in`+`out`, and has `additionalProperties: false` on every object node.
- `node scripts/build-brain-packet-schema.cjs` exits 0; `node scripts/build-brain-packet-schema.cjs --check` exits 0; both print `brain-packet-schema: OK`.
- `node tests/test-brain-packet-schema-check.cjs` exits 0 and prints a PASS summary (no longer the MISSING stub).
- `bash tests/run-all-110.sh` now shows `test-brain-packet-schema-check.cjs: PASSED` (the other 3 stubs still FAILED -- correct, they are filled by 110-04 / 110-05).
- `node lib/memory/run-feynman-tests.cjs` runs to completion; the `test-brain-packet-schema-check.cjs` entry passes.
- `package.json` does not list `ajv` as a dependency.
- `grep -P "[\x{2014}\x{2013}]" data/brain-packet-schema.json scripts/build-brain-packet-schema.cjs data/ROOM.md tests/test-brain-packet-schema-check.cjs` returns zero matches.
</verification>

<success_criteria>
1. `data/brain-packet-schema.json` is the single source of truth: draft 2020-12, `$id` `https://mindrian-os.com/schemas/brain-packet/1.0`, `$defs` per all 12 D-02 jobs (each `in`+`out`), shared `$defs` (`PrivacyMode`/`Origin`/`LocalGraphSummary`/`BankedOpportunities`/`Constraints`/`BrainResponse`/the 4 projections/`FocusNode`/`ActiveContext`), `additionalProperties: false` everywhere, every job `in` carries `packet_version: { const:'1.0' }` / `job: { const:'<jobname>' }` / `origin: { $ref:'#/$defs/Origin' }` / `privacy_mode: { const:'local_summary_only' }` / `local_graph_summary: { $ref:'#/$defs/LocalGraphSummary' }`.
2. `scripts/build-brain-packet-schema.cjs` mirrors `scripts/build-command-registry.cjs`: default run validates well-formedness + 12-job coverage + job-const match + no-unknown-job + recursive `additionalProperties: false`; `--check` re-runs in memory and exits 1 on any failure with a one-line recovery command on stderr; build always uses ajv `strict: true`.
3. `data/ROOM.md` has the `brain-packet-schema.json` row + the `scripts/build-brain-packet-schema.cjs` cross-reference.
4. `tests/test-brain-packet-schema-check.cjs` is a real `child_process` suite filling the Wave-0 stub (the 4-mangled-copies tripwire test via the `MINDRIAN_BRAIN_PACKET_SCHEMA` env seam).
5. `ajv` is NOT a direct dependency in `package.json`; the script uses `require('ajv').default || require('ajv')`.
6. Zero em-dashes or en-dashes in any file written.
</success_criteria>

<output>
After completion, create `.planning/phases/110-brain-context-packet-contract/110-01-SUMMARY.md` recording: the schema file structure shipped (the 12 job `$defs` + the shared `$defs` factored), the `--check` tripwire behavior, the `data/ROOM.md` diff, and the test stub now filled. Note that 110-03 (sendPacket) compiles this schema at module scope and that the round-trip test in 110-05 cross-checks it against `buildBrainPacket`'s actual output (origin/privacy_mode fields added by 110-02). Note any deviations.
</output>
