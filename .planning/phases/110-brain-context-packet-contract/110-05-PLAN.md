---
phase: 110-brain-context-packet-contract
plan: "05"
type: execute
wave: 3
depends_on: ["110-03"]
files_modified:
  - tests/test-brain-packet-validation-per-job.cjs
  - tests/test-brain-packet-part8-invariant-per-job.cjs
autonomous: true
requirements:
  - PACKET-110-06
  - PACKET-110-07
  - PACKET-110-08
  - PACKET-110-09
canon_parts:
  - "Part 8 (Graph Boundary -- the D-11 suite proves, per shipped job, that the Canon Part 8 invariant holds: no body / transcript / shell text / absolute path / email reaches the serialized packet)"
  - "Part 9 (Memory Locality -- reject hard, degrade soft: the suite proves a malformed in throws + logs brain_packet_rejected, an off-spec out is refused + logs brain_response_rejected + never throws + never partial-ingests)"
  - "Part 7 (Reuse Before Build -- the part-8 invariant suite mirrors tests/test-navigation-packet-part8-leak.cjs's adversarial forbidden-substring sweep but loops all 12 D-02 jobs; the validation suite reuses the require.cache / __transport mock idiom; no new dependency)"
must_haves:
  truths:
    - "tests/test-brain-packet-validation-per-job.cjs is a real node:assert/strict suite (no longer the MISSING stub) covering, for ALL 12 D-02 jobs: (a) a well-formed `in` packet (built via navigation.buildBrainPacket) validates and sendPacket proceeds past the in-gate (using the __transport seam to inject a known-good out); (b) a malformed `in` packet (an extra top-level field e.g. transcript, or a missing required field) -> sendPacket throws an Error whose message starts `brain packet rejected for job \"<job>\"` AND when opts.db is passed a `brain_packet_rejected` memory_event row is written; (c) an off-spec Brain `out` (totally-wrong shape via __transport) -> sendPacket returns { advice: null, reason: 'response_schema_invalid' }, does NOT throw, does NOT partial-ingest, AND when opts.db is passed a `brain_response_rejected` row is written; (d) a packet with `origin` other than the allowed enum (in the relevant env) -> sendPacket throws (and `origin: 'test_fixture'` is accepted only when MINDRIAN_TEST_MODE=1)"
    - "tests/test-brain-packet-validation-per-job.cjs also covers: a privacy-mode sub-block proving 'config caps, never raises' -- a packet whose resolved privacy_mode is 'allow_filenames' for a job whose $def.in.properties.privacy_mode is { const: 'local_summary_only' } FAILS ajv validation in sendPacket (i.e. sendPacket throws); and a dual-path sub-block proving _warnLegacyOnce() called twice -> console.warn fires exactly once and `brain_legacy_path_used` is logged exactly once (resetting the flag via brain-client._test._setLegacyWarned between cases)"
    - "tests/test-brain-packet-part8-invariant-per-job.cjs is a real node:assert/strict suite (no longer the MISSING stub) covering, for ALL 12 D-02 jobs: (a) round-trip -- navigation.buildBrainPacket(db, job, focusId, ...) returns a packet that validates against data/brain-packet-schema.json's $defs[job].in (compile the schema with ajv, validate each produced packet); (b) the adversarial Part-8 invariant -- seed forbidden content (a long SECRET RAW BODY string, leak@example.com, /home/jsagi/secret/path, an 800-char transcript, a ${injection} string) into node properties, build the packet for that job, assert JSON.stringify(packet) contains NONE of the seeded substrings and no string field longer than ~500 chars and no absolute /home/ path and no email; mirrors tests/test-navigation-packet-part8-leak.cjs's 8-tripwire idiom but loops all 12 jobs"
    - "Both suites exit 0 only when every assertion passes; each prints a PASS summary line; neither exits 77; both are already registered in lib/memory/run-feynman-tests.cjs TEST_FILES[] and tests/run-all-110.sh CJS_SUITES (from 110-00) -- this plan only fills the bodies, it does NOT change those registrations"
    - "Every file this plan ships contains zero em-dashes (U+2014) and zero en-dashes (U+2013)"
  artifacts:
    - path: "tests/test-brain-packet-validation-per-job.cjs"
      provides: "The 12-job sendPacket in/out validation suite + the privacy-mode 'config caps' sub-block + the dual-path once-per-session sub-block; uses navigation.buildBrainPacket to build valid packets and brain-client.sendPacket's __transport seam to inject good/bad Brain responses"
      min_lines: 80
    - path: "tests/test-brain-packet-part8-invariant-per-job.cjs"
      provides: "The 12-job round-trip (buildBrainPacket -> validate against the 110-01 schema) + the adversarial forbidden-substring sweep over JSON.stringify(buildBrainPacket(...)); mirrors test-navigation-packet-part8-leak.cjs but loops all 12 jobs"
      min_lines: 60
  key_links:
    - from: "tests/test-brain-packet-validation-per-job.cjs"
      to: "lib/core/brain-client.cjs sendPacket + lib/core/navigation.cjs buildBrainPacket"
      via: "require both; build valid packets with buildBrainPacket; drive sendPacket with the __transport seam; assert throws / soft-degrade / event logs"
      pattern: "sendPacket"
    - from: "tests/test-brain-packet-part8-invariant-per-job.cjs"
      to: "data/brain-packet-schema.json"
      via: "JSON.parse + ajv.compile + validate each produced packet against $defs[job].in"
      pattern: "brain-packet-schema\\.json"
    - from: "tests/test-brain-packet-part8-invariant-per-job.cjs"
      to: "lib/core/navigation/packet.cjs buildBrainPacket"
      via: "navigation.buildBrainPacket(db, job, focusId, { _mocks, roomId }) for each of the 12 jobs; seed forbidden content into node properties first"
      pattern: "buildBrainPacket"
---

<objective>
Fill the last two Wave-0 test stubs with the D-11 validation suite (Wave 3; depends on 110-03's `sendPacket` -- which itself pulls in 110-01's schema and 110-02's `origin`/EVENT_TYPES): `tests/test-brain-packet-validation-per-job.cjs` (the 12-job `in`/`out` validation suite -- valid `in` validates, malformed `in` throws + logs `brain_packet_rejected`, off-spec `out` is refused-and-degraded + logs `brain_response_rejected` + never throws + never partial-ingests, bad `origin` is refused; plus a privacy-mode "config caps, never raises" sub-block and a dual-path `_warnLegacyOnce` once-per-session sub-block) and `tests/test-brain-packet-part8-invariant-per-job.cjs` (the 12-job round-trip `buildBrainPacket` -> validate-against-the-110-01-schema, plus the adversarial forbidden-substring sweep over `JSON.stringify(buildBrainPacket(...))` -- mirroring `tests/test-navigation-packet-part8-leak.cjs`'s 8-tripwire idiom but looping all 12 D-02 jobs).

Per CONTEXT D-11 (the suite must prove, per shipped job (all 12): (a) well-formed `in` validates, malformed is refused with the expected `brain_packet_rejected` event; (b) off-spec `out` is refused-and-degraded with the expected `brain_response_rejected` event, no partial ingest; (c) a packet with `origin` other than the allowed enum (in the relevant env) is refused; (d) the Canon Part 8 invariant -- no body/transcript/shell text -- holds for the packet `buildBrainPacket` actually produces). Per CONTEXT D-06 (the `--check` schema tripwire test -- that one is filled by 110-01, not here) and D-08 layer 2 (the pre-commit-hook test -- filled by 110-04, not here). Per RESEARCH "Validation Architecture", "Phase Requirements -> Test Map" (PACKET-110-03 / -04 / -06 / -07 / -08 / -09 rows), and "Code Examples -> The adversarial leak-test idiom" (the verbatim pattern to copy + "loop this over all 12 D-02 jobs").

This plan touches only `tests/test-brain-packet-validation-per-job.cjs` and `tests/test-brain-packet-part8-invariant-per-job.cjs`. It does NOT touch `data/brain-packet-schema.json` (110-01), `scripts/build-brain-packet-schema.cjs` (110-01), `packet.cjs` / `memory-events.cjs` (110-02), `brain-client.cjs` / `navigation.cjs` (110-03), the pre-commit hook (110-04), `tests/test-brain-packet-schema-check.cjs` (110-01 fills it), `tests/test-brain-packet-precommit-hook.cjs` (110-04 fills it), `tests/run-all-110.sh`, or `lib/memory/run-feynman-tests.cjs` (both already reference these two paths from 110-00). It depends on 110-03 (`sendPacket`, the `__transport` seam, the `_test` hooks) which transitively requires 110-01 + 110-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-brain-context-packet-contract/110-CONTEXT.md
@.planning/phases/110-brain-context-packet-contract/110-RESEARCH.md
@.planning/phases/110-brain-context-packet-contract/110-VALIDATION.md
@CLAUDE.md
@lib/core/brain-client.cjs
@lib/core/navigation.cjs
@lib/core/navigation/packet.cjs
@lib/core/navigation/memory-events.cjs
@data/brain-packet-schema.json
@tests/test-navigation-packet-part8-leak.cjs
@tests/test-navigation-packet-builder.cjs
@lib/memory/brain-derivation.test.cjs

<interfaces>
<!--
THE 12 D-02 JOBS (loop over these in both suites):
  select_methodology, suggest_next_move, detect_contradiction, summarize_neighborhood, classify_room_budding,
  rank_assumptions, generate_feynman_explanation, strengthen_minto, prepare_investor_brief,
  opportunity_react, opportunity_reflect, opportunity_rank.

BUILDING A VALID PACKET (both suites). buildBrainPacket needs a tmp room.db with at least a room node + a focus node + the _mocks for jtbd/operator (see tests/test-navigation-packet-part8-leak.cjs's makeRoom/defaultMocks). Pattern:
  const { openRoomDb } = require(path.join(REPO_ROOT, 'lib', 'core', 'room-db.cjs'));
  const navigation = require(path.join(REPO_ROOT, 'lib', 'core', 'navigation.cjs'));
  function makeRoom() {
    const tmp = fs.mkdtempSync(path.join(os.tmpdir(), 'phase-110-validation-'));
    fs.mkdirSync(path.join(tmp, '.mindrian'), { recursive: true });
    const db = openRoomDb(tmp); const now = Date.now();
    const insN = db.prepare("INSERT OR IGNORE INTO nodes (id,type,properties,source_path,created_by,confidence,review_status,created_at,last_seen_at,source_section) VALUES (?,?,?,?,'user',?,?,?,?,?)");
    insN.run('room:test','room','{}','fixture',1.0,'confirmed',now,now,null);
    insN.run('decision:focus','decision',JSON.stringify({summary:'short'}),'fixture',0.7,'confirmed',now,now,'design');
    // seed a few neighbors so local_graph_summary has content (claim/assumption/opportunity + edges) -- for the part-8 suite seed FORBIDDEN content too (see below).
    return { tmp, db };
  }
  function defaultMocks() { return { jtbd: { getCurrent: () => ({ current: null }) }, operator: { getCurrent: () => ({ current: null }) } }; }
  // then: const packet = navigation.buildBrainPacket(db, job, 'decision:focus', { _mocks: defaultMocks(), roomId: 'test' });
  // packet.origin === 'navigation_api', packet.privacy_mode === 'local_summary_only' (from 110-02).

THE __transport SEAM (sendPacket, from 110-03). sendPacket(packet, { __transport, db }) where __transport is `async (toolName, args) => result` -- replaces callTool so the test never hits the network. Inject:
  - a known-good out:  __transport: async () => ([{ type:'text', text: JSON.stringify({ job_id:'j1', suggestions:[{ summary:'do X', methodology:'JTBD', confidence: 0.8 }] }) }])  -> sendPacket returns the parsed object.
  - an off-spec out:   __transport: async () => ([{ type:'text', text: JSON.stringify({ totally:'off-spec', extra:'field' }) }])  -> sendPacket returns { advice: null, reason: 'response_schema_invalid' }.
  - a tool-absent err: __transport: async () => ({ text: 'Error: unknown tool brain_packet' })  -> sendPacket returns { advice: null, reason: 'brain_packet_tool_absent' }, no brain_response_rejected log.
The _test hooks on brain-client.cjs (from 110-03): _test._resetSchema(), _test._setLegacyWarned(false), _test._validatorFor, _test._parseBrainResult, _test._looksLikeUnknownToolError, _test._schema.

COUNTING memory_event ROWS. After sendPacket logs a brain_* event via navigation.logMemoryEvent(db, ...), query the db:
  const n = db.prepare("SELECT COUNT(*) AS c FROM nodes WHERE type='memory_event' AND instr(properties, '<event_type>') > 0").get().c;
  (logEvent writes a memory_event node whose properties JSON includes the event_type; OR check the node id which is `memory_event:<eventType>:...` -- so `SELECT COUNT(*) FROM nodes WHERE type='memory_event' AND id LIKE 'memory_event:brain_packet_rejected:%'` is the cleaner query. Read lib/core/navigation/memory-events.cjs logEvent to get the exact id/properties shape.)

TEST A -- tests/test-brain-packet-validation-per-job.cjs (mirror lib/memory/brain-derivation.test.cjs's mock idiom + the per-job loop):
  'use strict';
  const { ok, equal, notEqual } = require('node:assert/strict');
  const fs = require('node:fs'), path = require('node:path'), os = require('node:os');
  const REPO_ROOT = path.resolve(__dirname, '..');
  const navigation = require(path.join(REPO_ROOT, 'lib', 'core', 'navigation.cjs'));
  const brainClient = require(path.join(REPO_ROOT, 'lib', 'core', 'brain-client.cjs'));
  const D02_JOBS = ['select_methodology','suggest_next_move','detect_contradiction','summarize_neighborhood','classify_room_budding','rank_assumptions','generate_feynman_explanation','strengthen_minto','prepare_investor_brief','opportunity_react','opportunity_reflect','opportunity_rank'];
  // makeRoom / defaultMocks as above.
  async function run() {
    let assertions = 0;
    for (const job of D02_JOBS) {
      const { tmp, db } = makeRoom();
      try {
        const goodPacket = navigation.buildBrainPacket(db, job, 'decision:focus', { _mocks: defaultMocks(), roomId: 'test' });
        // (a) valid in -> sendPacket proceeds past the in-gate (inject a good out, assert it comes back parsed).
        const r1 = await brainClient.sendPacket(goodPacket, { db, __transport: async () => ([{ type:'text', text: JSON.stringify({ job_id:'j', suggestions:[{ summary:'ok', confidence:0.7 }] }) }]) });
        ok(r1 && Array.isArray(r1.suggestions), 'job ' + job + ': valid in -> good out returned'); assertions++;
        // (b) malformed in -> throws `brain packet rejected for job "<job>"` AND logs brain_packet_rejected.
        const badPacket = Object.assign({}, goodPacket, { transcript: 'SECRET RAW LEAK ' + 'x'.repeat(200) }); // extra top-level field -> additionalProperties:false fails
        let threw = false, msg = '';
        try { await brainClient.sendPacket(badPacket, { db }); } catch (e) { threw = true; msg = String(e.message); }
        ok(threw && msg.indexOf('brain packet rejected for job "' + job + '"') === 0, 'job ' + job + ': malformed in throws with the right message; got: ' + msg); assertions++;
        const rejCount = db.prepare("SELECT COUNT(*) AS c FROM nodes WHERE type='memory_event' AND id LIKE 'memory_event:brain_packet_rejected:%'").get().c;
        ok(rejCount >= 1, 'job ' + job + ': brain_packet_rejected logged'); assertions++;
        // (c) off-spec out -> { advice:null, reason:'response_schema_invalid' }, no throw, logs brain_response_rejected.
        const r3 = await brainClient.sendPacket(goodPacket, { db, __transport: async () => ([{ type:'text', text: JSON.stringify({ totally:'off-spec', extra:'field' }) }]) });
        ok(r3 && r3.advice === null && r3.reason === 'response_schema_invalid', 'job ' + job + ': off-spec out degraded'); assertions++;
        const respCount = db.prepare("SELECT COUNT(*) AS c FROM nodes WHERE type='memory_event' AND id LIKE 'memory_event:brain_response_rejected:%'").get().c;
        ok(respCount >= 1, 'job ' + job + ': brain_response_rejected logged'); assertions++;
        // (d) bad origin -> throws.
        let threwOrigin = false; try { await brainClient.sendPacket(Object.assign({}, goodPacket, { origin: 'forged' }), { db }); } catch (_) { threwOrigin = true; }
        ok(threwOrigin, 'job ' + job + ': forged origin throws'); assertions++;
      } finally { try { fs.rmSync(tmp, { recursive:true, force:true }); } catch (_) {} }
    }
    // origin: 'test_fixture' env gate (one check, not per-job).
    { const { tmp, db } = makeRoom(); try {
        const p = navigation.buildBrainPacket(db, 'suggest_next_move', 'decision:focus', { _mocks: defaultMocks(), roomId:'test' });
        const tf = Object.assign({}, p, { origin: 'test_fixture' });
        const prev = process.env.MINDRIAN_TEST_MODE;
        delete process.env.MINDRIAN_TEST_MODE; let threw1 = false; try { await brainClient.sendPacket(tf, { db }); } catch (_) { threw1 = true; } ok(threw1, 'test_fixture origin throws without MINDRIAN_TEST_MODE=1'); assertions++;
        process.env.MINDRIAN_TEST_MODE = '1'; let threwOnOrigin = false; try { await brainClient.sendPacket(tf, { db, __transport: async () => ([{ type:'text', text: JSON.stringify({ suggestions: [] }) }]) }); } catch (e) { if (/origin/.test(String(e.message))) threwOnOrigin = true; } ok(!threwOnOrigin, 'test_fixture origin accepted when MINDRIAN_TEST_MODE=1'); assertions++;
        if (prev === undefined) delete process.env.MINDRIAN_TEST_MODE; else process.env.MINDRIAN_TEST_MODE = prev;
      } finally { try { fs.rmSync(tmp, { recursive:true, force:true }); } catch (_) {} } }
    // privacy-mode 'config caps, never raises': a packet whose resolved privacy_mode is allow_filenames for a job whose $def.in is const local_summary_only -> sendPacket throws (ajv const mismatch).
    { const { tmp, db } = makeRoom(); try {
        const p = navigation.buildBrainPacket(db, 'suggest_next_move', 'decision:focus', { _mocks: defaultMocks(), roomId:'test', privacyMode: 'allow_filenames' });
        // buildBrainPacket honors per-call -> p.privacy_mode === 'allow_filenames'; but the schema's $defs.suggest_next_move.in.properties.privacy_mode is { const: 'local_summary_only' } -> sendPacket's ajv in-validation fails -> throws.
        ok(p.privacy_mode === 'allow_filenames', 'buildBrainPacket honored the per-call privacy mode'); assertions++;
        let threwCap = false, capMsg = ''; try { await brainClient.sendPacket(p, { db }); } catch (e) { threwCap = true; capMsg = String(e.message); }
        ok(threwCap && /brain packet rejected/.test(capMsg), 'config-caps: a packet exceeding the job-declared privacy mode is refused at the wire; got: ' + capMsg); assertions++;
      } finally { try { fs.rmSync(tmp, { recursive:true, force:true }); } catch (_) {} } }
    // dual-path: _warnLegacyOnce twice -> warn once, brain_legacy_path_used logged once.
    { const { tmp, db } = makeRoom(); try {
        brainClient._test._setLegacyWarned(false);
        let warnCount = 0; const origWarn = console.warn; console.warn = function () { warnCount++; };
        brainClient._warnLegacyOnce(db); brainClient._warnLegacyOnce(db);
        console.warn = origWarn;
        equal(warnCount, 1, '_warnLegacyOnce fires console.warn exactly once'); assertions++;
        const legacyCount = db.prepare("SELECT COUNT(*) AS c FROM nodes WHERE type='memory_event' AND id LIKE 'memory_event:brain_legacy_path_used:%'").get().c;
        equal(legacyCount, 1, 'brain_legacy_path_used logged exactly once'); assertions++;
        brainClient._test._setLegacyWarned(false); // restore for any later test in the process
      } finally { try { fs.rmSync(tmp, { recursive:true, force:true }); } catch (_) {} } }
    console.log('test-brain-packet-validation-per-job: PASS (' + assertions + ' assertions)');
  }
  run().catch(function (e) { process.stderr.write('FAIL: ' + (e && e.stack ? e.stack : e) + '\n'); process.exit(1); });
NOTE: brainClient._test._resetSchema() should be called once at the top if a prior test in the same process compiled the schema with a different MINDRIAN_BRAIN_PACKET_SCHEMA env -- defensively call it before the loop.

TEST B -- tests/test-brain-packet-part8-invariant-per-job.cjs (mirror tests/test-navigation-packet-part8-leak.cjs's 8-tripwire sweep, looped over the 12 jobs + add the round-trip):
  'use strict';
  const { ok } = require('node:assert/strict');
  const fs = require('node:fs'), path = require('node:path'), os = require('node:os');
  const REPO_ROOT = path.resolve(__dirname, '..');
  const { openRoomDb } = require(path.join(REPO_ROOT, 'lib', 'core', 'room-db.cjs'));
  const navigation = require(path.join(REPO_ROOT, 'lib', 'core', 'navigation.cjs'));
  const Ajv = require('ajv').default || require('ajv');
  const D02_JOBS = [ ...the 12... ];
  function makeRoomWithForbidden() { // like test-navigation-packet-part8-leak.cjs's makeRoom: seed SECRET RAW BODY, leak@example.com, /home/jsagi/secret/, an 800-char transcript, a ${injection} into node properties + a couple edges so local_graph_summary is non-trivial.
    const tmp = fs.mkdtempSync(path.join(os.tmpdir(), 'phase-110-part8-')); fs.mkdirSync(path.join(tmp,'.mindrian'),{recursive:true});
    const db = openRoomDb(tmp); const now = Date.now();
    const insN = db.prepare("INSERT OR IGNORE INTO nodes (id,type,properties,source_path,created_by,confidence,review_status,created_at,last_seen_at,source_section) VALUES (?,?,?,?,'user',?,?,?,?,?)");
    insN.run('room:test','room','{}','fixture',1.0,'confirmed',now,now,null);
    const transcript = 'B'.repeat(800);
    insN.run('decision:focus','decision',JSON.stringify({summary:'short',body:'SECRET RAW DECISION BODY ${injection}',transcript}),'/home/jsagi/secret/path/decision.md',0.7,'confirmed',now,now,'design');
    insN.run('claim:c1','claim',JSON.stringify({summary:'short',body:'SECRET RAW CLAIM BODY',email:'leak@example.com'}),'/home/jsagi/secret/c1.md',0.6,'confirmed',now,now,'design');
    insN.run('assumption:a1','assumption',JSON.stringify({claim:'short',body:'SECRET RAW ASSUMPTION ${x}'}),'/home/jsagi/a1.md',0.5,'proposed',now,now,'design');
    insN.run('opportunity:hi','opportunity',JSON.stringify({hsi_score:85,tags:['fintech','b2b'],body:'SECRET RAW OPP BODY '+'y'.repeat(300),mirror_solution:'SECRET MIRROR'}),'/home/jsagi/oh.md',0.6,'confirmed',now,now,'design');
    const insE = db.prepare("INSERT OR IGNORE INTO edges (source,target,type,properties) VALUES (?,?,?,'{}')");
    insE.run('decision:focus','claim:c1','INFORMS'); insE.run('decision:focus','assumption:a1','ASSUMES'); insE.run('decision:focus','opportunity:hi','BANKED_BY');
    return { tmp, db };
  }
  function defaultMocks() { return { jtbd: { getCurrent: () => ({ current: null }) }, operator: { getCurrent: () => ({ current: null }) } }; }
  function run() {
    let assertions = 0;
    // compile the 110-01 schema once for the round-trip check.
    const schema = JSON.parse(fs.readFileSync(path.join(REPO_ROOT, 'data', 'brain-packet-schema.json'), 'utf8'));
    const ajv = new Ajv({ allErrors: true, strict: false }); ajv.addSchema(schema);
    for (const job of D02_JOBS) {
      const { tmp, db } = makeRoomWithForbidden();
      try {
        const packet = navigation.buildBrainPacket(db, job, 'decision:focus', { _mocks: defaultMocks(), roomId: 'test' });
        // round-trip: the produced packet validates against $defs[job].in.
        const validateIn = ajv.compile({ $ref: schema.$id + '#/$defs/' + job + '/properties/in' });
        const okIn = validateIn(packet);
        ok(okIn, 'job ' + job + ': buildBrainPacket output validates against $defs[' + job + '].in; errors=' + JSON.stringify(validateIn.errors)); assertions++;
        // adversarial Part-8 sweep (mirror test-navigation-packet-part8-leak.cjs's 8 tripwires).
        const s = JSON.stringify(packet);
        ok(!/SECRET RAW DECISION BODY/.test(s), 'job ' + job + ': no decision body leak'); assertions++;
        ok(!/SECRET RAW CLAIM BODY/.test(s), 'job ' + job + ': no claim body leak'); assertions++;
        ok(!/SECRET RAW ASSUMPTION/.test(s), 'job ' + job + ': no assumption body leak'); assertions++;
        ok(!/SECRET RAW OPP BODY/.test(s), 'job ' + job + ': no opportunity body leak'); assertions++;
        ok(!/SECRET MIRROR/.test(s), 'job ' + job + ': no mirror_solution leak'); assertions++;
        ok(!/\/home\/jsagi\//.test(s), 'job ' + job + ': no absolute /home path leak'); assertions++;
        ok(!/\/secret\//.test(s), 'job ' + job + ': no /secret/ path component leak'); assertions++;
        ok(!/[\w.+-]+@[\w-]+\.[\w.-]+/.test(s), 'job ' + job + ': no email leak'); assertions++;
        ok(!s.split('"').some(function (x) { return x.length > 500; }), 'job ' + job + ': no >500-char string field (transcript-length leak)'); assertions++;
        ok(!/\$\{[^}]+\}/.test(s), 'job ' + job + ': no template-injection string leak'); assertions++;
      } finally { try { fs.rmSync(tmp, { recursive:true, force:true }); } catch (_) {} }
    }
    console.log('test-brain-packet-part8-invariant-per-job: PASS (' + assertions + ' assertions)');
  }
  try { run(); } catch (e) { process.stderr.write('FAIL: ' + (e && e.stack ? e.stack : e) + '\n'); process.exit(1); }
NOTE: if buildBrainPacket's safe-projection mappers legitimately emit a short generic path slug like `sample-room/design` (a source_section, not an absolute path), the `/home/jsagi/` and `/secret/` tripwires still pass (those test for the ABSOLUTE path, not a relative slug) -- but if the round-trip OR a tripwire fails, that is a real Part-8 leak or a schema/shape mismatch and the suite SHOULD fail. Do NOT weaken the tripwires to make the test pass; fix the leak (a follow-up fix to packet.cjs's projection mappers would be a separate gap-closure plan, surfaced by this test).
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill tests/test-brain-packet-validation-per-job.cjs -- the 12-job in/out + privacy + dual-path suite</name>
  <files>tests/test-brain-packet-validation-per-job.cjs</files>
  <read_first>
    - tests/test-brain-packet-validation-per-job.cjs (the Wave-0 MISSING stub from 110-00 -- replace its body; keep the path)
    - lib/core/brain-client.cjs (the 110-03 `sendPacket(packet, opts)` function -- the `__transport` seam, the throw-on-bad-`in` / soft-degrade-on-bad-`out` behavior, the `_test` block: `_resetSchema` / `_setLegacyWarned` / `_validatorFor` / `_parseBrainResult` / `_looksLikeUnknownToolError` / `_schema`; the `_warnLegacyOnce(db)` helper; the soft-degrade reasons `brain_packet_tool_absent` / `brain_unreachable` / `response_schema_invalid`)
    - lib/core/navigation/packet.cjs (the 110-02 `buildBrainPacket` -- `origin: 'navigation_api'`, `privacy_mode` resolved via per-call > config > default; the `_mocks` seam for jtbd/operator)
    - lib/core/navigation/memory-events.cjs (the 110-02 EVENT_TYPES with `brain_packet_rejected` / `brain_response_rejected` / `brain_legacy_path_used`; `logEvent`'s memory_event id/properties shape so the count-query is right: the id is `memory_event:<eventType>:<ms>:<hex>`)
    - tests/test-navigation-packet-part8-leak.cjs (the `makeRoom` / `defaultMocks` helpers + the tmp-room.db + the `_mocks` injection -- copy the room-building idiom)
    - lib/memory/brain-derivation.test.cjs (the test idiom for mocking the Brain wire path without hitting the network -- 110-03's `__transport` seam is the modern equivalent; use it)
    - data/brain-packet-schema.json (the 110-01 schema -- so you know which top-level field to add to make a packet malformed: any extra top-level field, e.g. `transcript`, trips `additionalProperties: false`; and `$defs[job].in.properties.privacy_mode` is `{ const: 'local_summary_only' }` for every shipped job -> a packet with `privacy_mode: 'allow_filenames'` fails the `in` validation)
    - CLAUDE.md "What NOT to Use" (node `assert` + `child_process`; CJS; no jest/mocha/vitest/zod; NO em-dashes)
  </read_first>
  <action>
    Replace the body of `tests/test-brain-packet-validation-per-job.cjs` (keep the registered path) with the suite sketched in the interfaces block above. Concretely:
      - `'use strict';` + `require('node:assert/strict')` (`ok`, `equal`, `notEqual`) + `node:fs`/`node:path`/`node:os`. `REPO_ROOT = path.resolve(__dirname, '..')`. Require `navigation` (`lib/core/navigation.cjs`) and `brainClient` (`lib/core/brain-client.cjs`). `D02_JOBS = [ ...the 12 D-02 jobnames... ]`.
      - `makeRoom()` and `defaultMocks()` helpers (copy the idiom from `tests/test-navigation-packet-part8-leak.cjs`: tmp dir + `.mindrian` + `openRoomDb` + insert a `room:test` node + a `decision:focus` node + a couple neighbor nodes/edges so `local_graph_summary` has content; `defaultMocks` returns `{ jtbd:{getCurrent:()=>({current:null})}, operator:{getCurrent:()=>({current:null})} }`).
      - `async function run()` -- defensively call `brainClient._test._resetSchema()` at the top; then the per-job loop (12 jobs): build a valid packet via `navigation.buildBrainPacket(db, job, 'decision:focus', { _mocks: defaultMocks(), roomId: 'test' })`; assert (a) valid `in` -> `sendPacket(goodPacket, { db, __transport: async () => ([{type:'text',text:JSON.stringify({job_id:'j',suggestions:[{summary:'ok',confidence:0.7}]})}]) })` returns an object with `Array.isArray(r.suggestions)`; (b) malformed `in` (a copy with an extra top-level `transcript` field) -> `sendPacket(badPacket, { db })` throws an Error whose message *starts with* `brain packet rejected for job "<job>"` AND the db now has >= 1 `memory_event` row with `id LIKE 'memory_event:brain_packet_rejected:%'`; (c) off-spec `out` -> `sendPacket(goodPacket, { db, __transport: async () => ([{type:'text',text:JSON.stringify({totally:'off-spec',extra:'field'})}]) })` returns `{ advice: null, reason: 'response_schema_invalid' }`, does NOT throw, AND the db now has >= 1 `memory_event` row with `id LIKE 'memory_event:brain_response_rejected:%'`; (d) bad `origin` -> `sendPacket(Object.assign({},goodPacket,{origin:'forged'}), { db })` throws. Clean up the tmp room in a `finally` after each job.
      - After the loop: the `origin: 'test_fixture'` env-gate check (not per-job, one room): `test_fixture` origin throws when `process.env.MINDRIAN_TEST_MODE !== '1'`; is accepted (no origin-related throw) when `MINDRIAN_TEST_MODE === '1'` (save+restore the env var); the privacy-mode "config caps, never raises" check: `buildBrainPacket(..., { privacyMode: 'allow_filenames' })` returns a packet with `privacy_mode === 'allow_filenames'`, and `sendPacket(thatPacket, { db })` throws `brain packet rejected` (because the schema's `$defs[job].in.properties.privacy_mode` is `{ const: 'local_summary_only' }`); the dual-path check: `brainClient._test._setLegacyWarned(false)`, capture `console.warn`, call `brainClient._warnLegacyOnce(db)` twice, restore `console.warn`, assert it fired exactly once, assert the db has exactly 1 `memory_event` row with `id LIKE 'memory_event:brain_legacy_path_used:%'`, then `_setLegacyWarned(false)` to restore.
      - End: `console.log('test-brain-packet-validation-per-job: PASS (' + assertions + ' assertions)');` and let the `run().catch(...)` (which writes the stack to stderr + `process.exit(1)`) surface any failure.
    NO em-dashes (hyphens only). NO emoji. Do NOT change `tests/run-all-110.sh` or `lib/memory/run-feynman-tests.cjs`.
  </action>
  <verify>
    <automated>node tests/test-brain-packet-validation-per-job.cjs && node tests/test-brain-packet-validation-per-job.cjs 2>&1 | grep -q "test-brain-packet-validation-per-job: PASS" && ! node tests/test-brain-packet-validation-per-job.cjs 2>&1 | grep -q "MISSING - Wave" && grep -q "sendPacket" tests/test-brain-packet-validation-per-job.cjs && grep -q "brain_packet_rejected" tests/test-brain-packet-validation-per-job.cjs && grep -q "brain_response_rejected" tests/test-brain-packet-validation-per-job.cjs && grep -q "brain_legacy_path_used" tests/test-brain-packet-validation-per-job.cjs && grep -q "_warnLegacyOnce" tests/test-brain-packet-validation-per-job.cjs && grep -q "allow_filenames" tests/test-brain-packet-validation-per-job.cjs && grep -c "select_methodology" tests/test-brain-packet-validation-per-job.cjs | awk '$1 >= 1 { exit 0 } { exit 1 }' && ! grep -lP "[\x{2014}\x{2013}]" tests/test-brain-packet-validation-per-job.cjs</automated>
  </verify>
  <done>
    `tests/test-brain-packet-validation-per-job.cjs` is a real suite (no longer the MISSING stub): for all 12 D-02 jobs it proves valid `in` validates + malformed `in` throws `brain packet rejected for job "<job>"` and logs `brain_packet_rejected` + off-spec `out` is degraded (`{advice:null,reason:'response_schema_invalid'}`, no throw) and logs `brain_response_rejected` + bad `origin` throws; plus the `test_fixture` env-gate, the privacy-mode "config caps" check, and the `_warnLegacyOnce` once-per-session check; it exits 0 only on all-pass and prints a PASS summary; zero em-dashes or en-dashes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fill tests/test-brain-packet-part8-invariant-per-job.cjs -- the 12-job round-trip + adversarial Part-8 sweep</name>
  <files>tests/test-brain-packet-part8-invariant-per-job.cjs</files>
  <read_first>
    - tests/test-brain-packet-part8-invariant-per-job.cjs (the Wave-0 MISSING stub from 110-00 -- replace its body; keep the path)
    - tests/test-navigation-packet-part8-leak.cjs (the VERBATIM template: the 8-tripwire sweep over `JSON.stringify(packet)` -- seed forbidden content into node properties, build the packet, assert no leak; copy `makeRoom`/`defaultMocks` and the tripwire regexes; this plan loops them over the 12 D-02 jobs and adds the round-trip)
    - lib/core/navigation/packet.cjs (the 110-02 `buildBrainPacket` + the safe-projection mappers `safeNodeProjection` / `safeContradictionProjection` / `safeUnsupportedProjection` / `safeRecentChangeProjection` / `surface_banked_opportunities` / `hsiBand` -- these are what strip the forbidden content; the test proves they hold for all 12 jobs; `origin: 'navigation_api'` + `privacy_mode: 'local_summary_only'` are on the produced packet)
    - data/brain-packet-schema.json (the 110-01 schema -- `$id` `https://mindrian-os.com/schemas/brain-packet/1.0`; `$defs[job].properties.in` -- the round-trip compiles `{ $ref: schema.$id + '#/$defs/' + job + '/properties/in' }` and validates each produced packet)
    - .planning/phases/110-brain-context-packet-contract/110-RESEARCH.md "Code Examples -> The adversarial leak-test idiom" (the verbatim pattern + "For Phase 110: loop this over all 12 D-02 jobs") + "Common Pitfall" 3 (`additionalProperties: false` on nested objects -- a seeded `SECRET MIRROR` reaching `JSON.stringify(packet)` is the failure signal)
    - CLAUDE.md "What NOT to Use" (node `assert`; CJS; ajv `require('ajv').default || require('ajv')`; NO em-dashes)
  </read_first>
  <action>
    Replace the body of `tests/test-brain-packet-part8-invariant-per-job.cjs` (keep the registered path) with the suite sketched in the interfaces block above. Concretely:
      - `'use strict';` + `require('node:assert/strict')` (`ok`) + `node:fs`/`node:path`/`node:os`. `REPO_ROOT = path.resolve(__dirname, '..')`. Require `{ openRoomDb }` from `lib/core/room-db.cjs` and `navigation` from `lib/core/navigation.cjs`. `const Ajv = require('ajv').default || require('ajv');`. `D02_JOBS = [ ...the 12 D-02 jobnames... ]`.
      - `makeRoomWithForbidden()` -- copy `tests/test-navigation-packet-part8-leak.cjs`'s `makeRoom` and ALSO seed: a `decision:focus` node with `body: 'SECRET RAW DECISION BODY ${injection}'` + an 800-char `transcript` + `source_path: '/home/jsagi/secret/path/decision.md'`; a `claim:c1` node with `body: 'SECRET RAW CLAIM BODY'` + `email: 'leak@example.com'` + `source_path: '/home/jsagi/secret/c1.md'`; an `assumption:a1` node with `body: 'SECRET RAW ASSUMPTION ${x}'`; an `opportunity:hi` node with `body: 'SECRET RAW OPP BODY ' + 'y'.repeat(300)` + `mirror_solution: 'SECRET MIRROR'` + `tags: ['fintech','b2b']` + `hsi_score: 85`; and edges `decision:focus -INFORMS-> claim:c1`, `decision:focus -ASSUMES-> assumption:a1`, `decision:focus -BANKED_BY-> opportunity:hi`. `defaultMocks()` as in the other suite.
      - `function run()` -- compile the 110-01 schema once: `const schema = JSON.parse(fs.readFileSync(path.join(REPO_ROOT, 'data', 'brain-packet-schema.json'), 'utf8')); const ajv = new Ajv({ allErrors:true, strict:false }); ajv.addSchema(schema);`. Then the per-job loop (12 jobs): `makeRoomWithForbidden()`; `const packet = navigation.buildBrainPacket(db, job, 'decision:focus', { _mocks: defaultMocks(), roomId: 'test' });`. Round-trip: `const validateIn = ajv.compile({ $ref: schema.$id + '#/$defs/' + job + '/properties/in' }); ok(validateIn(packet), 'job ' + job + ': buildBrainPacket output validates against $defs[' + job + '].in; errors=' + JSON.stringify(validateIn.errors));`. Adversarial sweep over `const s = JSON.stringify(packet);` -- the tripwires from `tests/test-navigation-packet-part8-leak.cjs` (no `SECRET RAW DECISION BODY` / `SECRET RAW CLAIM BODY` / `SECRET RAW ASSUMPTION` / `SECRET RAW OPP BODY` / `SECRET MIRROR`; no `/home/jsagi/`; no `/secret/`; no email regex match; no string field > 500 chars; no `${...}` template-injection string) -- each as a `ok(!regex.test(s), 'job ' + job + ': ...')`. Clean up the tmp room in a `finally` after each job.
      - End: `console.log('test-brain-packet-part8-invariant-per-job: PASS (' + assertions + ' assertions)');` (count the assertions); wrap `run()` in `try { run(); } catch (e) { process.stderr.write('FAIL: ' + (e && e.stack ? e.stack : e) + '\n'); process.exit(1); }`.
      - IMPORTANT: do NOT weaken the tripwires to make the test pass. If a tripwire OR the round-trip fails, that is a real Part-8 leak or a schema/shape mismatch -- the suite SHOULD fail, and the fix (a follow-up touch to `packet.cjs`'s projection mappers or to the 110-01 schema) is a separate gap-closure plan surfaced by this test. (The relative source-section slugs like `sample-room/design` that `packet.cjs` legitimately emits are NOT absolute `/home/` paths and do NOT trip the tripwires.)
    NO em-dashes (hyphens only). NO emoji. Do NOT change `tests/run-all-110.sh` or `lib/memory/run-feynman-tests.cjs`.
  </action>
  <verify>
    <automated>node tests/test-brain-packet-part8-invariant-per-job.cjs && node tests/test-brain-packet-part8-invariant-per-job.cjs 2>&1 | grep -q "test-brain-packet-part8-invariant-per-job: PASS" && ! node tests/test-brain-packet-part8-invariant-per-job.cjs 2>&1 | grep -q "MISSING - Wave" && grep -q "buildBrainPacket" tests/test-brain-packet-part8-invariant-per-job.cjs && grep -q "brain-packet-schema.json" tests/test-brain-packet-part8-invariant-per-job.cjs && grep -q "SECRET RAW" tests/test-brain-packet-part8-invariant-per-job.cjs && grep -q "leak@example.com" tests/test-brain-packet-part8-invariant-per-job.cjs && grep -c "select_methodology" tests/test-brain-packet-part8-invariant-per-job.cjs | awk '$1 >= 1 { exit 0 } { exit 1 }' && ! grep -lP "[\x{2014}\x{2013}]" tests/test-brain-packet-part8-invariant-per-job.cjs</automated>
  </verify>
  <done>
    `tests/test-brain-packet-part8-invariant-per-job.cjs` is a real suite (no longer the MISSING stub): for all 12 D-02 jobs it proves (a) the packet `buildBrainPacket` produces validates against `data/brain-packet-schema.json`'s `$defs[job].in` (round-trip), and (b) the adversarial Part-8 sweep -- with forbidden content seeded into node properties -- finds zero leaks in `JSON.stringify(packet)` (no body / transcript / `${injection}` / `/home/` / `/secret/` / email / >500-char string); it exits 0 only on all-pass and prints a PASS summary; zero em-dashes or en-dashes.
  </done>
</task>

</tasks>

<verification>
- `node tests/test-brain-packet-validation-per-job.cjs` exits 0 and prints `test-brain-packet-validation-per-job: PASS (N assertions)`; for all 12 D-02 jobs it covers valid-`in` / malformed-`in`-throws-and-logs / off-spec-`out`-degrades-and-logs / bad-`origin`-throws, plus the `test_fixture` env-gate, the privacy "config caps" check, and the `_warnLegacyOnce` once-per-session check.
- `node tests/test-brain-packet-part8-invariant-per-job.cjs` exits 0 and prints `test-brain-packet-part8-invariant-per-job: PASS (N assertions)`; for all 12 D-02 jobs it covers the round-trip (produced packet validates against the 110-01 schema's `$defs[job].in`) and the adversarial forbidden-substring sweep (no leak in `JSON.stringify(packet)`).
- Neither suite is the MISSING stub anymore; neither exits 77.
- `bash tests/run-all-110.sh` exits 0 -- all 4 CJS_SUITES PASSED (`test-brain-packet-schema-check.cjs` from 110-01, `test-brain-packet-precommit-hook.cjs` from 110-04, and these two).
- `node lib/memory/run-feynman-tests.cjs` runs to completion; both new entries pass.
- `grep -P "[\x{2014}\x{2013}]" tests/test-brain-packet-validation-per-job.cjs tests/test-brain-packet-part8-invariant-per-job.cjs` returns zero matches.
</verification>

<success_criteria>
1. `tests/test-brain-packet-validation-per-job.cjs` is a real `node:assert/strict` suite filling the Wave-0 stub: for all 12 D-02 jobs -- (a) well-formed `in` validates and `sendPacket` proceeds (good `out` via `__transport` comes back parsed); (b) malformed `in` -> `sendPacket` throws `brain packet rejected for job "<job>"` + (with `opts.db`) logs `brain_packet_rejected`; (c) off-spec `out` -> `sendPacket` returns `{ advice:null, reason:'response_schema_invalid' }`, never throws, never partial-ingests, + (with `opts.db`) logs `brain_response_rejected`; (d) bad `origin` -> throws; plus the `test_fixture` env-gate (`MINDRIAN_TEST_MODE=1`), the privacy-mode "config caps, never raises" check (`allow_filenames` packet for a `local_summary_only`-const job -> `sendPacket` throws), and the dual-path `_warnLegacyOnce` once-per-session check.
2. `tests/test-brain-packet-part8-invariant-per-job.cjs` is a real `node:assert/strict` suite filling the Wave-0 stub: for all 12 D-02 jobs -- (a) round-trip: `buildBrainPacket(...)` output validates against `data/brain-packet-schema.json`'s `$defs[job].in`; (b) adversarial Part-8 sweep: with forbidden content seeded into node properties, `JSON.stringify(buildBrainPacket(...))` contains no body / transcript / `${injection}` / absolute `/home/` path / `/secret/` path / email / >500-char string; mirrors `tests/test-navigation-packet-part8-leak.cjs`'s 8-tripwire idiom.
3. Both suites exit 0 only on all-pass, print a PASS summary, never exit 77; they are already registered (110-00) in `lib/memory/run-feynman-tests.cjs` `TEST_FILES[]` and `tests/run-all-110.sh` `CJS_SUITES` -- this plan only fills the bodies.
4. After this plan, `bash tests/run-all-110.sh` is fully GREEN (all 4 CJS_SUITES PASSED).
5. Zero em-dashes or en-dashes in any file written.
</success_criteria>

<output>
After completion, create `.planning/phases/110-brain-context-packet-contract/110-05-SUMMARY.md` recording: the assertion counts in each suite, the per-job coverage matrix (12 jobs x {valid in / malformed in / off-spec out / bad origin / round-trip / adversarial sweep}), confirmation that `bash tests/run-all-110.sh` is now fully GREEN, and confirmation that `node lib/memory/run-feynman-tests.cjs` runs to completion with both new Phase-110 suites passing. If any tripwire or round-trip surfaced a real Part-8 leak or a schema/shape mismatch (a packet field not stripped, a `$def` shape not matching `buildBrainPacket`'s output), DO NOT weaken the test -- record it as a gap that needs a follow-up `/gsd:plan-phase 110 --gaps` plan touching `packet.cjs` or `data/brain-packet-schema.json`, and note it in the SUMMARY. Note any other deviations.
</output>
