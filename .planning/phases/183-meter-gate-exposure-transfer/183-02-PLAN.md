---
phase: 183-meter-gate-exposure-transfer
plan: 02
type: execute
wave: 2
depends_on: ["183-01"]
files_modified:
  - lib/core/meter/transfer-reader.cjs
  - lib/core/meter/two-gauge.cjs
  - lib/core/navigation.cjs
autonomous: true
requirements: [METER-01, METER-02]

must_haves:
  truths:
    - "The three METER-02 proxies (reject-reason-capture rate, insight-to-validated-decision latency, independence trend) are computable LOCALLY and are labeled NAMED-DEBT proxies (not transfer deltas) in the code and the artifact."
    - "readTwoGauge returns the PAIR {gauge1, gauge2} together or throws - there is NO exported bare-density function whose return type is a number."
    - "The welded read returns BOTH a volume_direction and a quality_direction; volume-up-quality-flat AND quality-up-by-starving-volume are BOTH labeled regression; a win is volume-up AND quality-holds-or-climbs."
    - "The meter makes zero Brain calls; transfer payloads carry only counts, reach_ids, timestamps, and enum statuses - never the reject reason string, never prose."
  artifacts:
    - path: "lib/core/meter/transfer-reader.cjs"
      provides: "Gauge 2 source - reject-capture-rate over f_selector_decision, insight-to-status_promoted latency, per-cycle independence trend; each labeled named-debt"
      min_lines: 60
    - path: "lib/core/meter/two-gauge.cjs"
      provides: "the WELDED read - readTwoGauge(db, sinceEpochMs) returns {gauge1, gauge2, volume_direction, quality_direction} together or throws; no bare-density export"
      min_lines: 50
    - path: "lib/core/navigation.cjs"
      provides: "thin additive re-export of the welded read (readTwoGauge) so callers reach the meter through the one Part 9 door"
      contains: "readTwoGauge"
  key_links:
    - from: "lib/core/meter/two-gauge.cjs"
      to: "lib/core/meter/gate-density-reader.cjs + lib/core/meter/transfer-reader.cjs"
      via: "computes gauge1 and gauge2 in the same call, returns one frozen object"
      pattern: "computeInvocationDensity"
    - from: "lib/core/navigation.cjs"
      to: "lib/core/meter/two-gauge.cjs"
      via: "additive re-export readTwoGauge"
      pattern: "readTwoGauge"
---

<objective>
METER-02: build the Gauge-2 transfer source (three named-debt proxies) and the WELDED two-gauge read module whose only export returns the density+transfer PAIR together or throws. This is the telemetry-layer enforcement of the v1.19 welded contract: invocation density is structurally un-returnable without the transfer denominator beside it.

Purpose: produce the instrument the entry-31 self-binding clause waits on and that Phase 184 READER's R1 A/B test consumes - a real two-gauge reading from a live navigator on the gate. METER is the Gauge-2 SOURCE, NOT a finished transfer measurement; the proxies are named-debt and the code must say so.

Output: lib/core/meter/transfer-reader.cjs, lib/core/meter/two-gauge.cjs, the navigation.cjs re-export, and the two green floor tests (transfer + two-gauge weld).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/183-meter-gate-exposure-transfer/183-CONTEXT.md
@.planning/phases/183-meter-gate-exposure-transfer/183-RESEARCH.md
@.planning/phases/183-meter-gate-exposure-transfer/183-VALIDATION.md
@.planning/phases/183-meter-gate-exposure-transfer/183-01-SUMMARY.md
@docs/MINDRIAN-CANON.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by Plan 02 (Phase 184 READER consumes the welded read):

- **Module:** `lib/core/meter/transfer-reader.cjs` exporting three named-debt proxy readers, each a pure reader over the chokepoint, each carrying an explicit `NAMED_DEBT` label:
  - `rejectReasonCaptureRate(db, sinceEpochMs, roomState)` -> `{ reject_total, reject_with_reason, capture_rate, proxy_class: 'named-debt' }` over `f_selector_decision` rows (reads the PRESENCE/absence of `reason`, never the string).
  - `insightToValidatedDecisionLatency(db, sinceEpochMs, roomState)` -> `{ sample_count, median_latency_ms, proxy_class: 'named-debt' }` pairing an insight event `createdAt` (reach_presented / auto_explore_finding_surfaced) to the next `status_promoted` `createdAt`.
  - `independenceTrend(db, sinceEpochMs, roomState)` -> `{ direction, delta, proxy_class: 'named-debt', caveat }` per-cycle invocation-density delta, with the explicit "direction is not self-interpreting" caveat.
- **Module:** `lib/core/meter/two-gauge.cjs` exporting EXACTLY `readTwoGauge(db, sinceEpochMs, roomState)` -> frozen `{ gauge1_density, gauge2_transfer, volume_direction, quality_direction, verdict }` (returns the pair or throws; NO bare-density export). `verdict` is one of `win | regression_dealer | regression_starved | flat`.
- **Re-export:** `navigation.readTwoGauge` added to `lib/core/navigation.cjs` via the thin additive-re-export idiom (mirrors logMemoryEvent / writeEdge / getRoomContext).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: METER-02 Gauge 2 source - the three named-debt transfer proxies</name>
  <files>lib/core/meter/transfer-reader.cjs</files>
  <read_first>
    - lib/workflow/reach-reject-reader.cjs (whole - the reach-keyed reject/presentation reader idiom: _injected seam, findRecentChanges reads, enums-only, never throws; reuse for the reject-capture substrate)
    - lib/workflow/selector-decisions.cjs:102-260 (the f_selector_decision row: decision 'defer'|'reject', reason string|null at :130/:248, payload key set; the reject-reason SUBSTRATE - read its presence, never egress the string)
    - lib/core/navigation/transitions.cjs:73-179 (status_promoted is the validated-decision end-event, emitted by confirmNode; this is the latency end anchor)
    - lib/core/navigation/memory-events.cjs:597-625 (findRecentChanges returns rows with .createdAt + .properties - the latency timestamps)
    - 183-RESEARCH.md "Transfer Proxies: real vs named-debt (honesty table)" + Common Pitfall 2 (a proxy must NEVER be framed as a transfer measurement) + Open Question 3 (latency end-event = status_promoted)
    - tests/test-meter-transfer.cjs (the pin this task turns green)
  </read_first>
  <behavior>
    - rejectReasonCaptureRate computes COUNT(reject AND reason!=null) / COUNT(reject) over f_selector_decision rows; on zero reject rows it returns a cold-start (capture_rate null), never a divide-by-zero or throw. (Note: f_selector_decision is not yet written from the dial in production per ROADMAP:2846, so cold-start is the expected reading on most rooms today - this is honest and correct.)
    - insightToValidatedDecisionLatency pairs each insight event createdAt to the next status_promoted createdAt and reports a median; cold-starts to sample_count 0 when no pair exists.
    - independenceTrend reports a per-cycle density delta with direction (rising|falling|flat) AND an explicit caveat string that falling density is not self-interpreting (could be internalized method OR disengagement).
    - Every returned object carries proxy_class: 'named-debt'; the module header states none of the three is a real transfer delta - METER is the Gauge-2 SOURCE.
    - No reader egresses the reject reason STRING; capture-rate reads presence/absence only.
  </behavior>
  <action>
    Create lib/core/meter/transfer-reader.cjs as three pure readers mirroring reach-reject-reader.cjs in shape (require ../navigation.cjs, _injected roomState seams, read ONLY via navigation.findRecentChanges, return cold-start on null db, never throw). rejectReasonCaptureRate reads f_selector_decision rows and divides reason-present rejects by total rejects (read the boolean presence of properties.reason, NEVER the string value). insightToValidatedDecisionLatency reads reach_presented (and auto_explore_finding_surfaced) createdAt timestamps and pairs each to the next status_promoted createdAt, returning a median latency and a sample_count. independenceTrend computes the per-cycle delta of the invocation-density basis (reuse the Plan 01 gate-density-reader counts) and reports direction + delta + the non-self-interpreting caveat. Stamp every return object with proxy_class 'named-debt' and add a module-header comment carrying the Canon Part 5 honesty line verbatim in spirit: these are NAMED-DEBT proxies, NOT real transfer deltas; METER is the Gauge-2 SOURCE/instrument that Phase 184 READER's R1 A/B test consumes, not a finished transfer measurement. Open no db, make no Brain call, import no packet.cjs / brain-client. No em-dashes.
  </action>
  <acceptance_criteria>
    - `node tests/test-meter-transfer.cjs` exits 0.
    - `node -e "const t=require('./lib/core/meter/transfer-reader.cjs'); const r=t.rejectReasonCaptureRate(null,0,{}); process.exit(r && r.proxy_class==='named-debt' && r.capture_rate===null ? 0 : 1)"` exits 0 (cold-start + named-debt label).
    - `grep -c "named-debt" lib/core/meter/transfer-reader.cjs` is >= 4 (header line + one per proxy return).
    - `grep -rE 'fetch|http|curl|brain.mindrian|tavily|brain-client' lib/core/meter/transfer-reader.cjs | grep -v '^[^:]*:[0-9]*: *//' | grep -c .` returns 0 (Part 8 sweep, comment-stripped).
    - `grep -c "properties.reason" lib/core/meter/transfer-reader.cjs` shows reason is read as a presence check; `grep -nE "reason\s*===|reason\s*!==|reason !=" lib/core/meter/transfer-reader.cjs` confirms no echo of the reason value into a returned field.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-meter-transfer.cjs</automated>
  </verify>
  <done>The three named-debt transfer proxies read LOCALLY via the chokepoint, cold-start safely, carry the named-debt label, never egress the reject reason string; the transfer pin is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the WELDED two-gauge read + the navigation.cjs re-export</name>
  <files>lib/core/meter/two-gauge.cjs, lib/core/navigation.cjs</files>
  <read_first>
    - lib/core/meter/gate-density-reader.cjs (Plan 01 - computeInvocationDensity, the Gauge 1 input)
    - lib/core/meter/transfer-reader.cjs (Task 1 - the three Gauge 2 proxies)
    - lib/core/navigation.cjs:60-130 + :285-340 (the module.exports surface + the thin additive-re-export idiom: logMemoryEvent / writeEdge / getRoomContext / openRoomDbForCaller - mirror this exact shape for readTwoGauge)
    - docs/MINDRIAN-CANON.md Part 5 "The welded two-gauge metric" (un-reportable without the transfer denominator beside it - welded, not a clause; both regression directions; win = volume up AND quality holds-or-climbs)
    - 183-RESEARCH.md "The Welded Two-Gauge Emission Contract" (one read function, structurally welded; refuse the lone-density; the two-directional regression guard D-180-02)
    - tests/test-meter-two-gauge-weld.cjs (the pin this task turns green)
  </read_first>
  <behavior>
    - readTwoGauge(db, sinceEpochMs, roomState) computes gauge1_density (from gate-density-reader) AND gauge2_transfer (from transfer-reader) in the SAME call and returns them in ONE frozen object; a caller CANNOT obtain density without the transfer reading beside it.
    - There is NO exported function whose return type is a bare density number; the module exports ONLY readTwoGauge (plus internal helpers).
    - The return carries volume_direction and quality_direction scalars and a verdict: win only when volume-up AND quality-holds-or-climbs; regression_dealer for volume-up-quality-flat; regression_starved for quality-up-by-starving-volume; flat otherwise.
    - If an internal compute cannot produce a gauge it THROWS (or returns the welded object with an explicit unavailable marker on BOTH halves) - it never returns a half-pair.
    - navigation.readTwoGauge is the single sanctioned door; it re-exports two-gauge.readTwoGauge via the thin additive idiom and adds no new db open and no Brain call.
  </behavior>
  <action>
    Create lib/core/meter/two-gauge.cjs exporting EXACTLY readTwoGauge(db, sinceEpochMs, roomState): call gate-density-reader.computeInvocationDensity for gauge1_density and the three transfer-reader proxies for gauge2_transfer, assemble both into one Object.freeze({gauge1_density, gauge2_transfer, volume_direction, quality_direction, verdict}), and return it; if either half cannot compute, throw (the lone-density write is forbidden - mirror the research's refuse-the-unwelded-payload rule). Derive volume_direction from the gauge1 density trend and quality_direction from the gauge2 transfer trend, then set verdict via the two-directional guard: win = volume up AND quality holds-or-climbs; regression_dealer = volume up + quality flat (the Hooked Dealer quadrant); regression_starved = quality up bought by suppressed volume; flat otherwise. Export NOTHING that returns a bare number. Then in lib/core/navigation.cjs add a thin additive re-export readTwoGauge: two-gauge.readTwoGauge to module.exports, mirroring the logMemoryEvent / writeEdge / getRoomContext re-export comment+member shape exactly, so callers reach the welded meter through the one Part 9 door. Make zero Brain calls; import no packet.cjs / brain-client. No em-dashes.
  </action>
  <acceptance_criteria>
    - `node tests/test-meter-two-gauge-weld.cjs` exits 0.
    - `node -e "const m=require('./lib/core/meter/two-gauge.cjs'); const keys=Object.keys(m); const bare=keys.filter(k=>/density/i.test(k)&&k!=='readTwoGauge'); process.exit(keys.includes('readTwoGauge')&&bare.length===0?0:1)"` exits 0 (only readTwoGauge exported; no bare-density export).
    - `node -e "const n=require('./lib/core/navigation.cjs'); process.exit(typeof n.readTwoGauge==='function'?0:1)"` exits 0 (the one-door re-export).
    - `node -e "const m=require('./lib/core/meter/two-gauge.cjs'); const r=m.readTwoGauge(null,0,{}); process.exit(r&&'gauge1_density'in r&&'gauge2_transfer'in r&&'verdict'in r?0:1)"` exits 0 (the PAIR returned together, never a half).
    - `grep -rE 'fetch|http|curl|brain.mindrian|tavily|brain-client' lib/core/meter/two-gauge.cjs | grep -v '^[^:]*:[0-9]*: *//' | grep -c .` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-meter-two-gauge-weld.cjs && node -e "const n=require('./lib/core/navigation.cjs'); process.exit(typeof n.readTwoGauge==='function'?0:1)"</automated>
  </verify>
  <done>readTwoGauge returns the density+transfer pair together or throws; no bare-density export exists; the two-directional regression guard labels both single-direction readings as regressions; navigation.readTwoGauge is the single door; the weld pin is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transfer-reader -> f_selector_decision rows | The reader reads the PRESENCE of the reject reason field; the reason STRING must never cross into a returned/egressable field. |
| meter reader -> network | MUST NOT EXIST. The grep-sweep over lib/core/meter/ proves zero network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-01 | Information disclosure (Part 8 breach) | transfer-reader.cjs, two-gauge.cjs, navigation re-export | mitigate | LOCAL-only via navigation.findRecentChanges; zero Brain calls; the run-all-183.sh grep-sweep over lib/core/meter/ asserts 0 network tokens; no packet.cjs / brain-client import. |
| T-183-02 | Information disclosure | transfer payloads + the reject reason string | mitigate | Capture-rate reads reason PRESENCE/absence only; the string is never echoed into a returned field (grep asserts no reason-value echo); payloads carry counts, reach_ids, timestamps, enum statuses only. |
| T-183-05 | Tampering (the engagement machine) | two-gauge.cjs | mitigate | Structural weld: readTwoGauge returns the pair or throws; NO exported bare-density function (asserted by test-meter-two-gauge-weld.cjs); both regression directions labeled. |
| T-183-06 | Repudiation / honesty | transfer-reader.cjs + the artifact | mitigate | Every proxy carries proxy_class 'named-debt' and the module + SUMMARY state METER is the Gauge-2 SOURCE, not a transfer measurement (Canon Part 5 / Pitfall 2). |
| T-183-SC | Tampering (supply chain) | n/a | accept | No external packages installed (183-RESEARCH Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- `bash tests/run-all-183.sh` is FULLY GREEN: all five meter tests pass + the Part 8 grep-sweep + the reach-ids/posture-ids drift fences (METER mints no reach/posture).
- `node -e "const n=require('./lib/core/navigation.cjs'); n.readTwoGauge"` resolves to a function (the one-door welded read).
- No exported symbol in lib/core/meter/ returns a bare density number (asserted by the weld floor test).
- Part 8: `grep -rE 'fetch|http|curl|brain.mindrian|tavily|brain-client' lib/core/meter/` (comment-stripped) returns 0.
</verification>

<success_criteria>
- The three named-debt transfer proxies are computable LOCALLY, cold-start safely, carry the named-debt label, and never egress the reject reason string.
- readTwoGauge returns {gauge1_density, gauge2_transfer, volume_direction, quality_direction, verdict} together or throws; no bare-density export; navigation.readTwoGauge is the single sanctioned door.
- The two-directional regression guard labels volume-up-quality-flat AND quality-up-by-starving-volume as regressions; a win is volume-up AND quality-holds-or-climbs.
- The full tests/run-all-183.sh is green; LOCAL-only sweep clean; no em-dashes; no reach/posture/edge/node minted.
- The instrument is ready for Phase 184 READER's R1 A/B test (a real two-gauge reading from a live navigator on the gate satisfies the entry-31 self-binding clause).
</success_criteria>

<output>
Create `.planning/phases/183-meter-gate-exposure-transfer/183-02-SUMMARY.md` when done.
</output>
