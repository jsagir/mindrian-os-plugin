---
phase: 183-meter-gate-exposure-transfer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/memory-events.cjs
  - scripts/intent-classifier.cjs
  - lib/core/meter/gate-density-reader.cjs
  - tests/test-meter-gate-reach.cjs
  - tests/test-meter-density.cjs
  - tests/test-meter-event-types-floor.cjs
  - tests/test-meter-transfer.cjs
  - tests/test-meter-two-gauge-weld.cjs
  - tests/run-all-183.sh
autonomous: true
requirements: [METER-01]

must_haves:
  truths:
    - "When the engine arm renders a decide() gate, exactly one gate_reached memory_event is written LOCALLY (guarded by offered.length>0, deduped on the turn/correlation id)."
    - "The gate-reach count per session is readable via navigation.findRecentChanges filtered on eventType gate_reached - a real number, not an inference from Brain request count."
    - "Gauge 1 invocation density is derivable from the events that actually fire (reach_presented + gate_reached), with framework_invoked as an additive term that reads ~0 today."
    - "The gate_reached emit sits on the surface-shared engine-arm resolve path (the same seam as the live reach_presented loop), so Desktop/Cowork gate-reaches are counted, not only CLI."
  artifacts:
    - path: "lib/core/navigation/memory-events.cjs"
      provides: "gate_reached added to the frozen EVENT_TYPES Set via the verbatim additive idiom"
      contains: "gate_reached"
    - path: "scripts/intent-classifier.cjs"
      provides: "the single gate_reached emit at the existing engine-arm seam (~:1544), guarded by offered.length>0, dedupe_key on the turn id"
      contains: "gate_reached"
    - path: "lib/core/meter/gate-density-reader.cjs"
      provides: "Gauge 1 invocation-density derivation over reach_presented + gate_reached (+ additive framework_invoked) via findRecentChanges"
      min_lines: 40
    - path: "tests/run-all-183.sh"
      provides: "phase aggregator carrying the Part 8 grep-sweep + the frozen-set drift fences (mirror run-all-180.sh)"
      contains: "fetch"
  key_links:
    - from: "scripts/intent-classifier.cjs"
      to: "navigation.logMemoryEvent"
      via: "gate_reached emit on the open roomDb handle"
      pattern: "logMemoryEvent\\(roomDb, 'gate_reached'"
    - from: "lib/core/meter/gate-density-reader.cjs"
      to: "navigation.findRecentChanges"
      via: "eventType-filtered read of gate_reached + reach_presented"
      pattern: "findRecentChanges"
---

<objective>
METER-01: emit the gate-reach signal and derive Gauge 1 (invocation density) over the Part 9 memory_event chokepoint, LOCAL-only. This plan also lays the phase-wide Wave 0 test scaffold (the RED pins for both plans) so every subsequent task has an automated verify.

Purpose: answer "does a navigator reach the decide() gate" with a REAL number written precisely when the LOCAL engine renders the gate (surface-agnostic, zero Brain dependence). This is the entry-31 self-binding-clause precondition - METER must confirm a gate subject exists before Phase 184 READER may open.

Output: one additive EVENT_TYPES member (gate_reached), one emit line at the existing engine-arm seam, the Gauge-1 density reader, and the full tests/run-all-183.sh scaffold (all five meter test stubs + the aggregator).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/183-meter-gate-exposure-transfer/183-CONTEXT.md
@.planning/phases/183-meter-gate-exposure-transfer/183-RESEARCH.md
@.planning/phases/183-meter-gate-exposure-transfer/183-VALIDATION.md
@docs/MINDRIAN-CANON.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by Plan 01 (downstream consumers depend on these exact names):

- **EVENT_TYPES member:** `gate_reached` (string) added to the frozen Set in `lib/core/navigation/memory-events.cjs` (86 -> 87). Payload contract (enum/scalar only): `{ reach_count: <integer>, routing_source: 'engine', source_path: 'gate:reached', created_by: 'system', dedupe_key: <turn-id string> }`.
- **Emit call site:** one `navigation.logMemoryEvent(roomDb, 'gate_reached', {...})` at `scripts/intent-classifier.cjs` ~:1544 (immediately beside the live `reach_presented` loop, guarded by `offered.length > 0`).
- **Module:** `lib/core/meter/gate-density-reader.cjs` exporting `computeInvocationDensity(db, sinceEpochMs, roomState)` -> `{ gate_reaches, reach_presentations, framework_invocations, density, denominator_unit }` (a pure reader; opens no db; makes no Brain call; returns a cold-start zero-object on a null db).
- **Test stubs (phase-wide Wave 0, RED pins):** `tests/test-meter-gate-reach.cjs`, `tests/test-meter-density.cjs`, `tests/test-meter-event-types-floor.cjs`, `tests/test-meter-transfer.cjs`, `tests/test-meter-two-gauge-weld.cjs`.
- **Aggregator:** `tests/run-all-183.sh` (mirror `tests/run-all-180.sh`; carries the Part 8 grep-sweep over `lib/core/meter/` + the reach-ids / posture-ids drift fences).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 - lay the phase test scaffold (RED pins) + the aggregator</name>
  <files>tests/test-meter-gate-reach.cjs, tests/test-meter-density.cjs, tests/test-meter-event-types-floor.cjs, tests/test-meter-transfer.cjs, tests/test-meter-two-gauge-weld.cjs, tests/run-all-183.sh</files>
  <read_first>
    - tests/test-canon-entry-31-two-gauge-floor.cjs (the FLOOR-test idiom: read the artifact, assert membership + the welded pair, NEVER assert a raw .size/count so a future addition cannot false-fail)
    - tests/run-all-180.sh (the aggregator idiom + the carried reach-ids/posture-ids drift fences + the run() helper)
    - tests/test-95.5-00-scaffold.sh:52 (the Part 8 grep-sweep fence: grep -cE over the new file asserting 0 network tokens) - reference via 183-RESEARCH.md Part 8 table
    - lib/workflow/reach-reject-reader.cjs:142-164 (presentationsCount - the roomState injection seam so floor tests run db-free)
    - lib/core/navigation/memory-events.cjs:10-130 (the EVENT_TYPES Set + the additive-idiom comments to mirror in the event-types floor test)
  </read_first>
  <action>
    Create the five meter test stubs as RED pins (each asserts the not-yet-built artifact and EXITS NON-ZERO until its target ships): (a) test-meter-gate-reach.cjs requires lib/core/navigation/memory-events.cjs and asserts EVENT_TYPES.has('gate_reached'), plus a static assertion that scripts/intent-classifier.cjs contains the literal logMemoryEvent(roomDb, 'gate_reached' on the engine-arm seam; (b) test-meter-density.cjs requires lib/core/meter/gate-density-reader.cjs and asserts computeInvocationDensity returns an object carrying gate_reaches, reach_presentations, density, and denominator_unit, exercising the roomState injection seam (no live db); (c) test-meter-event-types-floor.cjs mirrors the entry-31 floor idiom - asserts gate_reached membership AND the full prior named floor (reach_presented, framework_invoked, f_selector_decision, status_promoted, feynman_timeline_refreshed) is preserved, NEVER asserting .size; (d) test-meter-transfer.cjs requires lib/core/meter/transfer-reader.cjs and asserts the three named-debt proxy readers exist and each returns a cold-start (null or zero) on an empty/null db, AND asserts the source carries an explicit named-debt label string (Canon Part 5 honesty); (e) test-meter-two-gauge-weld.cjs requires lib/core/meter/two-gauge.cjs and asserts readTwoGauge returns BOTH gauge1 and gauge2 keys or throws, that there is NO exported bare-density function returning a number, and that volume-up-quality-flat AND quality-up-by-starving-volume are BOTH labeled regression. Then write tests/run-all-183.sh mirroring run-all-180.sh: run each of the five node tests, carry the Part 8 grep-sweep (grep -rE over lib/core/meter/ and the intent-classifier emit seam for fetch, http, curl, brain.mindrian, tavily, brain-client asserting zero matches - use a comment-stripping filter per the grep-gate-hygiene rule so header prose cannot self-invalidate), and carry the reach-ids-drift + posture-ids-drift fences (frozen 6 / frozen 3) GREEN to prove METER mints no reach/posture. bash only, no emoji, no em-dashes. Tasks 2 and 3 of this plan turn pins (a)(b)(c) green; Plan 02 turns (d)(e) green.
  </action>
  <acceptance_criteria>
    - `ls tests/test-meter-gate-reach.cjs tests/test-meter-density.cjs tests/test-meter-event-types-floor.cjs tests/test-meter-transfer.cjs tests/test-meter-two-gauge-weld.cjs tests/run-all-183.sh` lists all six files.
    - `node tests/test-meter-gate-reach.cjs; echo $?` is NON-ZERO before Task 2 (RED pin proven).
    - `grep -c "\.size" tests/test-meter-event-types-floor.cjs` returns 0 (floor test never asserts a raw size).
    - `bash tests/run-all-183.sh; echo $?` is NON-ZERO at end of Task 1 (the meter targets do not exist yet) AND the grep-sweep + drift-fence sections are present (`grep -E "fetch|http|curl|brain.mindrian|tavily" tests/run-all-183.sh` matches, `grep -E "reach-ids-drift|posture-ids-drift" tests/run-all-183.sh` matches).
  </acceptance_criteria>
  <verify>
    <automated>bash tests/run-all-183.sh; test $? -ne 0 && grep -qE "fetch|http|curl|brain.mindrian|tavily" tests/run-all-183.sh && echo SCAFFOLD_OK</automated>
  </verify>
  <done>All five meter test stubs + run-all-183.sh exist; the suite is RED (targets unbuilt); the Part 8 grep-sweep and reach/posture drift fences are wired into the aggregator; no test asserts a raw .size.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: METER-01 emit - add gate_reached to EVENT_TYPES and emit it at the engine-arm seam</name>
  <files>lib/core/navigation/memory-events.cjs, scripts/intent-classifier.cjs</files>
  <read_first>
    - lib/core/navigation/memory-events.cjs:10-130 (the frozen EVENT_TYPES Set + the verbatim additive idiom at the feynman_timeline_refreshed/_failed and framework_invoked blocks - mirror that comment+member shape exactly)
    - lib/core/navigation/memory-events.cjs:536-575 (logEvent signature + the dedupe_key 60s idempotency contract: payload.dedupe_key keys the idempotency)
    - scripts/intent-classifier.cjs:1495-1558 (the live engine arm: cortexNodes assembled, reachList computed via buildReachList, the offered = reachList.reaches.slice(0, offered_count), the per-reach reach_presented emit loop at :1545-1553 while roomDb is OPEN, the best-effort try/catch that never blocks the turn)
    - tests/test-meter-gate-reach.cjs + tests/test-meter-event-types-floor.cjs (the pins this task turns green)
    - docs/MINDRIAN-CANON.md Part 8 (LOCAL->BRAIN: NO; enum/scalar payloads) + Part 9 v1.5 audit-node carve-out (memory_event is system-bookkeeping; created_by=system review_status=confirmed is canon-legal)
  </read_first>
  <behavior>
    - EVENT_TYPES.has('gate_reached') is true; the full prior named floor is preserved (event-types-floor pin green).
    - On the engine arm, when offered.length > 0, exactly ONE gate_reached memory_event is written per gate render (not one-per-reach); reach_count equals offered.length.
    - The emit is deduped on the turn/correlation id so a re-entrant arm cannot double-count one gate (gate counts never exceed turn counts).
    - The payload carries only counts, enums, and the dedupe handle - never prose, never a reach framework string, never user content.
    - The emit lives inside the same surface-shared engine-arm resolve block as the reach_presented loop (not a CLI-only fork), so Desktop/Cowork gate-reaches are counted.
  </behavior>
  <action>
    Add the string member gate_reached to the frozen EVENT_TYPES Set in memory-events.cjs using the verbatim additive idiom (a Phase 183 METER-01 comment block mirroring the feynman_timeline_refreshed precedent, then the bare 'gate_reached', entry); the Set grows 86 -> 87, additive only. Then at scripts/intent-classifier.cjs, immediately AFTER the offered top-3 array is computed (~:1544, before or after the per-reach reach_presented loop, inside the SAME guarded best-effort try block that holds the open roomDb handle) add a single navigation.logMemoryEvent(roomDb, 'gate_reached', {...}) guarded by offered.length > 0. Payload: reach_count set to offered.length, routing_source 'engine', source_path 'gate:reached', created_by 'system', and dedupe_key keyed on the turn/correlation id available in the decision trace (reuse the existing correlation id seam; if none is in hand, key on decision.decision_trace correlation/turn field) so the 60s dedupe window de-duplicates a retried turn. Keep it best-effort: a faulting emit must never block the turn, never throw out of the .then, never leak the db handle - mirror the existing reach_presented try/catch exactly. Do NOT touch the frozen render contract (MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15 gate, the 6-reach bank, appendAskUserQuestionTrailer) - this is an OBSERVATION beside the render, it changes nothing about what is offered. No em-dashes in any comment.
  </action>
  <acceptance_criteria>
    - `node tests/test-meter-gate-reach.cjs` exits 0.
    - `node tests/test-meter-event-types-floor.cjs` exits 0.
    - `grep -c "gate_reached" lib/core/navigation/memory-events.cjs` is >= 1.
    - `grep -c "logMemoryEvent(roomDb, 'gate_reached'" scripts/intent-classifier.cjs` is exactly 1 (single hook, not per-reach).
    - `grep -n "dedupe_key" scripts/intent-classifier.cjs` shows the dedupe handle on the gate_reached payload.
    - `node tests/test-navigation-acceptance.cjs` (if present) still passes - the additive member breaks no existing memory_event consumer.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-meter-gate-reach.cjs && node tests/test-meter-event-types-floor.cjs</automated>
  </verify>
  <done>gate_reached is a frozen EVENT_TYPES member; exactly one gate_reached event is emitted per gate render on the surface-shared engine arm, deduped on turn id, enum/scalar payload only; the two RED pins are green; the frozen render contract is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: METER-01 Gauge 1 - the invocation-density reader</name>
  <files>lib/core/meter/gate-density-reader.cjs</files>
  <read_first>
    - lib/workflow/reach-reject-reader.cjs:115-184 (the exact pure-reader pattern to mirror: the _injected roomState seam, read ONLY via navigation.findRecentChanges, return 0 on a null db / read fault, never throw out of the read)
    - lib/core/navigation/memory-events.cjs:597-625 (findRecentChanges signature: (db, sinceEpochMs, {eventType, limit}) -> rows with .properties and .createdAt)
    - 183-RESEARCH.md "Pitfall 5" + Open Question 1 (framework_invoked is UNDER-EMITTED - verified this session: it fires at NO production site, only in test files - so the density basis leans on reach_presented + gate_reached; framework_invoked is an additive term that reads ~0 today) and Open Question 2 (denominator unit = gate_reached, the cleanest per-turn unit)
    - tests/test-meter-density.cjs (the pin this task turns green)
  </read_first>
  <behavior>
    - computeInvocationDensity(db, sinceEpochMs, roomState) returns an object carrying gate_reaches, reach_presentations, framework_invocations, density, and denominator_unit.
    - The density basis is reach_presented + gate_reached counts (the events that actually fire); framework_invocations is included as an additive term and reads 0 on rooms where it never fires.
    - denominator_unit is the string 'gate_reached' (the per-turn unit; documented in a code comment citing Open Question 2).
    - On a null db with no injected counters, every count is 0 and density is the cold-start value (never a throw).
    - The reader uses the roomState injection seam so the floor test runs db-free.
  </behavior>
  <action>
    Create lib/core/meter/gate-density-reader.cjs as a pure reader mirroring reach-reject-reader.cjs verbatim in shape: require ../navigation.cjs, expose an _injected(roomState, key) seam, and export computeInvocationDensity(db, sinceEpochMs, roomState). Count gate_reached rows and reach_presented rows via navigation.findRecentChanges(db, sinceEpochMs, {eventType, limit}); also count framework_invoked as an additive term (it reads ~0 today - state in a comment that the production emission site is unwired per Open Question 1). Compute density as the invocation volume relative to the gate_reached denominator unit (set denominator_unit to 'gate_reached'); guard division by zero (cold-start density is 0). Open NO db (caller-owned handle, the writeEdge/getRoomContext contract), make NO Brain call, return a zeroed cold-start object on a null db, and never throw out of the read. Add a header comment naming the Part 8 fence (enum/scalar reads only; no prose). This module is Gauge 1 ONLY - it must NOT be the surface a caller uses to read density in isolation downstream; Plan 02's two-gauge weld is the only sanctioned read path. No em-dashes.
  </action>
  <acceptance_criteria>
    - `node tests/test-meter-density.cjs` exits 0.
    - `node -e "const m=require('./lib/core/meter/gate-density-reader.cjs'); const r=m.computeInvocationDensity(null,0,{}); process.exit(r && r.denominator_unit==='gate_reached' && r.gate_reaches===0 ? 0 : 1)"` exits 0 (cold-start contract).
    - `grep -rE 'fetch|http|curl|brain.mindrian|tavily|brain-client' lib/core/meter/gate-density-reader.cjs | grep -v '^[^:]*:[0-9]*: *//' | grep -c .` returns 0 (Part 8 sweep, comment-stripped).
    - `grep -c "findRecentChanges" lib/core/meter/gate-density-reader.cjs` is >= 1 (reads via the chokepoint, not raw SQL).
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-meter-density.cjs</automated>
  </verify>
  <done>Gauge 1 is a pure reader over reach_presented + gate_reached (framework_invoked additive) via the navigation chokepoint; cold-starts safely on a null db; carries the Part 8 fence; the density pin is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine-arm -> room.db | The gate_reached emit crosses from the resolve path into LOCAL SQLite via the navigation.cjs chokepoint. Only enum/scalar/timestamp payloads may cross. |
| meter reader -> network | MUST NOT EXIST. The meter readers make zero outbound calls; the grep-sweep proves no network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-01 | Information disclosure | gate_reached emit + gate-density-reader.cjs | mitigate | LOCAL-only via navigation.logMemoryEvent / findRecentChanges; the run-all-183.sh grep-sweep over lib/core/meter/ + the emit seam for fetch/http/curl/brain.mindrian/tavily/brain-client asserts 0; no packet.cjs / brain-client import. |
| T-183-02 | Information disclosure | gate_reached payload | mitigate | Payload carries reach_count (integer), routing_source/source_path/created_by enums, and a dedupe handle ONLY - never prose, never a framework string, never user content. The emit mirrors the reach_presented enum/scalar precedent; a test asserts the payload key set. |
| T-183-04 | Tampering (metric meaning) | gate-density-reader.cjs | mitigate | Gauge 1 must never be the downstream read path in isolation - the bare-density export is forbidden; Plan 02's welded two-gauge read is the only sanctioned consumer (deferred to Plan 02, asserted by test-meter-two-gauge-weld.cjs). |
| T-183-SC | Tampering (supply chain) | n/a | accept | This phase installs NO external packages (183-RESEARCH Package Legitimacy Audit = N/A); zero registry surface, no slopcheck target. |
</threat_model>

<verification>
- `bash tests/run-all-183.sh` is GREEN for the Task-2/Task-3 targets (gate-reach, density, event-types-floor pins) and the Part 8 grep-sweep + reach/posture drift fences pass. The transfer + two-gauge-weld pins remain RED until Plan 02.
- `grep -c "gate_reached" lib/core/navigation/memory-events.cjs` >= 1 and the single emit at scripts/intent-classifier.cjs is present exactly once.
- Part 8: `grep -rE 'fetch|http|curl|brain.mindrian|tavily|brain-client' lib/core/meter/` (comment-stripped) returns 0.
</verification>

<success_criteria>
- gate_reached is a frozen EVENT_TYPES member (86 -> 87, additive idiom); the prior floor is preserved.
- One surface-shared, deduped gate_reached emit fires per gate render; reach_count = offered.length; enum/scalar payload only.
- Gauge 1 invocation density is readable via navigation.findRecentChanges, leaning on the events that fire (reach_presented + gate_reached), framework_invoked additive, denominator_unit = gate_reached.
- The phase test scaffold (5 stubs + run-all-183.sh) exists; METER-01 pins are green; METER-02 pins are RED for Plan 02.
- LOCAL-only sweep clean; no em-dashes; frozen render contract untouched.
</success_criteria>

<output>
Create `.planning/phases/183-meter-gate-exposure-transfer/183-01-SUMMARY.md` when done.
</output>
