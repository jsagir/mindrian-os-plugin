---
phase: 360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8
plan: 07
type: execute
wave: 4
depends_on: ["360-04", "360-05", "360-06"]
files_modified:
  - scripts/intent-classifier.cjs
  - lib/core/room-bind-picker-policy.cjs
  - lib/core/session-binding.cjs
autonomous: true
requirements: [BIND360-01, BIND360-02, BIND360-03, BIND360-04, BIND360-06, BIND360-10, BIND360-11]
tags: [runtime-fix, userpromptsubmit, room-bind, cwd, session-binding, tri-polar]

must_haves:
  truths:
    - "TURN_IS_HARNESS is computed once at module init from STDIN_MESSAGE through a helper that lazy-requires lib/hmi/turn-text.cjs and returns true only on an exact 'harness' from classifyUserPromptText; empty text, a missing module, a missing export or a throw returns false (D-06, SPEC R6, PSB-06)"
    - "One early guard, `if (TURN_IS_HARNESS) return 0;` directly after `if (!message) return 0;` in main(), skips all four room-resolution outputs and every side write they make; main() (:477-748) holds only room-resolution work, so nothing else is skipped (D-07, RESEARCH Finding 5, SPEC R1)"
    - "The binding-answer consumer call is wrapped in `if (!TURN_IS_HARNESS)`, so a harness turn leaves a pending payload for the next human turn; consumePriorF1Pick, the NAV engine block and injectGraphFindings are untouched (D-08, D-09, SPEC R2)"
    - "CONTEXT D-04 is superseded by RESEARCH Finding 4: the hook reads no transcript_path; the verdict judges exactly the prompt text the gate scores, and intent-classifier holds no lead literal and reads no isMeta or origin (SPEC R4)"
    - "In the same early-guard block, after the rooms root resolves, a second guard returns 0 when lib/core/room-bind-picker-policy.cjs says the session is unbound with a cwd that resolves outside the rooms home (N-1, SPEC R10) or already chose 'dev repo / no room', stored as the __no_room__ sentinel in the existing session binding store (N-2, SPEC R11); any fault in that guard returns false so today's picker fires"
    - "The policy module composes the existing realpath helper (room-path-containment realRoomRoot) and the existing binding store's sentinel (session-binding NO_ROOM_SLUG, newly exported); it adds no store, no network and no text inspection (Part 7 reuse, Canon Part 8, N-1 'deterministic realpath prefix check')"
    - "Planner decision P-1: an ancestor-of-rooms-home cwd is ambiguous and fires; P-3: the policy guard's early return also silences the strict-mode override and the legacy advisory for the two session classes it covers, because they are the same room-bind nag family and would otherwise write into the machine-wide active room (same single-guard rationale as D-07)"
    - "No lib/mcp file changes, and the MCP room_bind / gate_render binding path behaves exactly as before (SPEC R9, D-18, Tri-Polar: the defect needs a UserPromptSubmit hook, so it is CLI-only)"
  artifacts:
    - path: "scripts/intent-classifier.cjs"
      provides: "harnessVerdict, TURN_IS_HARNESS, extractCwd, STDIN_CWD, unboundPickerSuppressed, the two main() guards, the consumer guard"
      contains: "TURN_IS_HARNESS"
    - path: "lib/core/room-bind-picker-policy.cjs"
      provides: "cwdRoomsHomeVerdict, unboundPickerSuppression"
      exports: ["cwdRoomsHomeVerdict", "unboundPickerSuppression"]
      min_lines: 60
    - path: "lib/core/session-binding.cjs"
      provides: "NO_ROOM_SLUG added to module.exports"
      contains: "NO_ROOM_SLUG"
  key_links:
    - from: "scripts/intent-classifier.cjs"
      to: "lib/hmi/turn-text.cjs"
      via: "lazy require inside harnessVerdict, classifyUserPromptText(STDIN_MESSAGE) === 'harness'"
      pattern: "classifyUserPromptText"
    - from: "scripts/intent-classifier.cjs"
      to: "lib/core/room-bind-picker-policy.cjs"
      via: "lazy require inside unboundPickerSuppressed(root)"
      pattern: "unboundPickerSuppression"
    - from: "lib/core/room-bind-picker-policy.cjs"
      to: "lib/core/room-path-containment.cjs"
      via: "realRoomRoot for the canonical rooms root"
      pattern: "realRoomRoot"
---

<objective>
Wire the fix into the one hook that injects the picker: harness turns emit and consume nothing (SPEC R1, R2, R6), and the unbound picker stays quiet in a dev repo and after a remembered "dev repo / no room" answer (SPEC R10, R11), with every fault failing toward today's picker.

Purpose: D-06, D-07, D-08, D-09; N-1, N-2; SPEC R1-R4, R6, R9-R11. Every RED leg from 360-02 (r4c), 360-03 (layer c), 360-04 and 360-05 goes GREEN here.
Output: guards in `scripts/intent-classifier.cjs`, new `lib/core/room-bind-picker-policy.cjs`, one additive export in `lib/core/session-binding.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-SPEC.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-CONTEXT.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-RESEARCH.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-04-SUMMARY.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-05-SUMMARY.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-06-SUMMARY.md
@tests/fixtures/ups-harness-360/pre-phase.json

Parallel-session rules (every task, D-20): never write `.planning/STATE.md`. Run `git status --short -- <file>` before editing and STOP on a diff you did not make. Commit by explicit path only (`git add <new paths>` when new, then `git commit --only -m "<type>(360-07): ..." -- <paths>`); never `git add -A` / `git commit -a` / `git stash` / revert an unowned diff. Never edit: `lib/mcp/**`, `lib/core/write-lock.cjs`, `lib/core/part8-egress-guard.cjs`, `scripts/doctor.cjs`, `lib/core/graph-ops.cjs`, `lib/core/navigation.cjs`, `scripts/eval-icm-writers.cjs`, `tests/test-353-grader-agreement.cjs`, `tests/test-353-ledger-shape.cjs`, `docs/OPEN-HANDOFFS.md`. No em-dashes. The R4 tripwire scans ALL of `scripts/intent-classifier.cjs`, comments included: never write a harness lead literal, the word `isMeta`, `originKind` or `origin.kind` in that file, not even in a comment.

Why R11 needs code (root cause, verified at plan time in a scratch fixture): a typed "dev repo / no room" answer is already stored as `{bound: ['__no_room__'], primary: '__no_room__'}`, but the next scoring turn still renders the UNBOUND header, because `_runBindingGate` -> `resolveSessionScope` tests `bound.indexOf(topRoom)` (the sentinel is never a scored room, so every turn is off-scope) and the F.8 path maps a sentinel primary to `boundPrimary = null`, which selects the unbound header. The zero-score path already honors the sentinel (:618); the F.8 path does not. The guard closes that gap in the hook without touching the shared scope resolver that MCP also uses.

Budget (SPEC constraint, `BUDGET_MS` 200): the harness check is one module load (measured 0.35 ms) and a few startsWith calls with zero data-file reads; the policy guard adds one small binding-file read plus two realpath calls on the human path, the input it needs.

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/ups-harness-360/pre-phase.json`, `tests/test-360-r3-suites.cjs`, `tests/run-all-360.sh` | 01 |
| `tests/fixtures/ups-harness-360/cases.json`, `tests/fixtures/ups-harness-360/spawn-kit.cjs`, `tests/test-360-tripwire.cjs` | 02 |
| `tests/test-360-leads.cjs`, `tests/test-360-snapshot-replay.cjs` | 03 |
| `tests/test-360-harness-picker.cjs` plus preload stubs `capture-spy.cjs`, `throwing-turn-text.cjs`, `missing-export-turn-text.cjs` | 04 |
| `tests/test-360-picker-policy.cjs` plus preload stub `throwing-picker-policy.cjs` | 05 |
| `lib/hmi/turn-text.cjs` (HARNESS_LEADS widened, `classifyUserPromptText`), conditional 357 dev-tool imports | 06 |
| `scripts/intent-classifier.cjs` guards, `lib/core/room-bind-picker-policy.cjs`, `NO_ROOM_SLUG` export in `lib/core/session-binding.cjs` | 07 (this plan) |
| BIND360 requirement closure, VALIDATION sign-off, dual research filing | 08 |
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: harness verdict constant, the main() harness guard and the consumer guard (D-06, D-07, D-08)</name>
  <files>scripts/intent-classifier.cjs</files>
  <read_first>
    - scripts/intent-classifier.cjs (lines 395-420 module-init STDIN constants; 477-490 the top of main(); 3505-3580 the entry block with consumePriorF1Pick and consumePriorBindingAnswer; 3457-3500 module.exports)
    - lib/hmi/turn-text.cjs (classifyUserPromptText as 360-06 shipped it)
    - tests/test-360-harness-picker.cjs and tests/test-360-tripwire.cjs (the legs this task turns GREEN)
    - .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-RESEARCH.md (Pattern 2; Finding 5; Pitfall 8)
  </read_first>
  <behavior>
    - node tests/test-360-harness-picker.cjs passes r1, r2, r3 and r6
    - node tests/test-360-tripwire.cjs passes r4a, r4b, r4c and r4d
    - A require() of the classifier with empty stdin does not load turn-text (harnessVerdict returns false before requiring)
  </behavior>
  <action>
    1. `git status --short -- scripts/intent-classifier.cjs` must be empty; STOP otherwise.
    2. Directly after `const STDIN_SESSION_ID = extractSessionId(STDIN_RAW);` (:411) add `function harnessVerdict(text)`: return false when `text` is not a non-empty string; otherwise inside try/catch lazy-require `path.join(__dirname, '..', 'lib', 'hmi', 'turn-text.cjs')`, return false unless `typeof tt.classifyUserPromptText === 'function'`, and return `tt.classifyUserPromptText(text) === 'harness'` (the property is read at call time, so the 360-04 preload stubs reach it); the catch returns false. Then `const TURN_IS_HARNESS = harnessVerdict(STDIN_MESSAGE);`. Comment: Phase 360 D-06; the verdict comes from the ONE shared classifier (SPEC R4); it judges the same text the gate scores (D-01 text signature); only an exact harness verdict suppresses and every fault leaves today's human path (SPEC R6, PSB-06); no transcript read (D-04 superseded by RESEARCH Finding 4). Name no lead, no record flag.
    3. In `main()`, directly after `if (!message) return 0;` (:482), add `if (TURN_IS_HARNESS) return 0;` with a comment: D-07, one guard before the strict-mode override, the zero-score gate, the F.8 binding gate and the legacy advisory, so no header, systemMessage, side-channel F.8 record, trace payload or offered marker is produced on a harness turn (SPEC R1; this is the upstream minter of the 2026-09-23 anchor false block, 357 RESEARCH Finding 3).
    4. In the entry block, wrap only the `consumePriorBindingAnswer(roomDir, sessionId, STDIN_MESSAGE)` try/catch in `if (!TURN_IS_HARNESS) { ... }` with a comment citing D-08 and SPEC R2 (a peer or subagent's text is never read as the navigator's binding answer). Leave `consumePriorF1Pick`, `emitEngineDecisionBlock` and `injectGraphFindings` exactly as they are (D-09; their harness-turn behavior is a deferred item).
    5. Run `node tests/test-360-harness-picker.cjs </dev/null`, `node tests/test-360-tripwire.cjs --only r4 </dev/null`, `node tests/test-360-leads.cjs </dev/null`, `node tests/test-360-r3-suites.cjs --group r3 </dev/null` and `node tests/test-209-engine-arm-contract.cjs </dev/null`.
    6. Commit: `git commit --only -m "fix(360-07): harness turns mint no room-bind output and consume no binding answer (D-06, D-07, D-08; R1, R2, R6)" -- scripts/intent-classifier.cjs`.
  </action>
  <verify>
    <automated>node tests/test-360-harness-picker.cjs </dev/null && node tests/test-360-tripwire.cjs --only r4 </dev/null && node tests/test-360-r3-suites.cjs --group r3 </dev/null</automated>
  </verify>
  <acceptance_criteria>
    - All three verify commands exit 0
    - `grep -v '^\s*//' scripts/intent-classifier.cjs | grep -c "if (TURN_IS_HARNESS) return 0;"` is 1 and `grep -v '^\s*//' scripts/intent-classifier.cjs | grep -c "if (!TURN_IS_HARNESS)"` is 1
    - `grep -c "isMeta\|originKind\|origin\.kind\|transcript_path" scripts/intent-classifier.cjs` is 0 (all four were 0 at plan time; the hook reads no record flag and no transcript, D-04 superseded)
    - D-09 untouched: `grep -c "consumePriorF1Pick(roomDir, sessionId" scripts/intent-classifier.cjs` and `grep -c "emitEngineDecisionBlock(roomDir, sessionId)" scripts/intent-classifier.cjs` equal their PLAN_BASE counts
    - `node -e "require('./scripts/intent-classifier.cjs');const k=Object.keys(require.cache).some(p=>p.endsWith('lib/hmi/turn-text.cjs'));process.exit(k?1:0)" </dev/null` exits 0 (empty stdin loads no turn-text)
    - `! grep -l "$(printf '\xe2\x80\x94')" scripts/intent-classifier.cjs`
  </acceptance_criteria>
  <done>Harness turns produce no room-bind output and cannot answer the binding card, human turns are byte-identical, and a broken classifier degrades to today's picker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: picker-policy module, NO_ROOM_SLUG export, and the cwd / no-room guard (N-1, N-2)</name>
  <files>lib/core/room-bind-picker-policy.cjs, lib/core/session-binding.cjs, scripts/intent-classifier.cjs</files>
  <read_first>
    - lib/core/room-path-containment.cjs (resolveExistingRealpath and realRoomRoot, lines 30-104; module.exports at :194)
    - lib/core/session-binding.cjs (NO_ROOM_SLUG const at :78; readSessionBinding; module.exports at :208)
    - scripts/intent-classifier.cjs (lines 73-100 resolveMindrianRoomsRoot; 378-411 extractSessionId and the STDIN constants; 477-490 main() top; 599-633 the zero-score path's sentinel handling; 635-697 the F.8 path, resolveSessionId(resolveActiveRoomDir()) and readSessionBinding(sessionId, {home: root}); 991-1016 resolveSessionId)
    - tests/test-360-picker-policy.cjs (the module contract and every leg this task turns GREEN; P-1, P-2)
    - lib/core/resolve-active-room.cjs (lines 543-559 resolveSessionScope: read only, do not change)
  </read_first>
  <behavior>
    - node tests/test-360-picker-policy.cjs passes unit, r10, r11 and fault
    - node tests/test-360-harness-picker.cjs still passes every leg
    - node tests/test-360-r3-suites.cjs (r3, mcp and wider groups) is no worse than the pre-phase baseline
    - with the snapshot present, node tests/test-360-snapshot-replay.cjs (both layers) exits 0
  </behavior>
  <action>
    1. `git status --short -- lib/core/session-binding.cjs lib/core/room-path-containment.cjs scripts/intent-classifier.cjs` must be empty (except this plan's own Task 1 commit being already in); STOP on a peer diff.
    2. `lib/core/session-binding.cjs`: add `NO_ROOM_SLUG` to `module.exports` only (additive; comment: exported for the Phase 360 picker policy so the sentinel has one definition, N-2). No other line changes.
    3. Create `lib/core/room-bind-picker-policy.cjs` (CJS, `'use strict'`, node:fs and node:path plus `./room-path-containment.cjs` and `./session-binding.cjs`; header comment: Phase 360, N-1 and N-2, SPEC R10 / R11, Canon Part 8 local-only with no network and no text inspection, never throws, CLI-only because only the UserPromptSubmit hook injects the picker, no em-dashes). `cwdRoomsHomeVerdict(cwd, roomsRoot)`: `'unresolvable'` unless cwd is a non-empty absolute string whose `fs.realpathSync` succeeds and whose `fs.statSync` is a directory, and `realRoomRoot(roomsRoot)` succeeds; `'inside'` when the real cwd equals the real root or starts with root plus `path.sep`; `'ancestor'` when `path.relative(realCwd, realRoot)` is non-empty, not absolute and does not start with `..` (P-1: ambiguous, fires); else `'outside'`; the whole body in try/catch returning `'unresolvable'`. `unboundPickerSuppression({binding, cwd, roomsRoot})`: read `bound` (array or empty) and `primary` defensively; `realPrimary` = primary when it is a non-empty string other than `NO_ROOM_SLUG`; `realBound` = bound entries other than `NO_ROOM_SLUG`; return `'no-room-remembered'` when there is no realPrimary and (primary equals `NO_ROOM_SLUG` or bound includes it) (P-2, N-2); return `'cwd-outside-unbound'` when there is no realPrimary, realBound is empty, and `cwdRoomsHomeVerdict(cwd, roomsRoot) === 'outside'` (N-1); else null; the whole body in try/catch returning null. Export both.
    4. `scripts/intent-classifier.cjs`: after `extractSessionId` add `function extractCwd(raw)` mirroring its parse shape (a non-empty string at `cwd` or `payload.cwd`, else undefined; never throws), and after `TURN_IS_HARNESS` add `const STDIN_CWD = extractCwd(STDIN_RAW);`. Add `function unboundPickerSuppressed(root)`: inside try/catch, `sessionId = resolveSessionId(resolveActiveRoomDir())` (the same key the F.8 path reads), `binding = require(<lib/core/session-binding.cjs>).readSessionBinding(sessionId, {home: root})`, lazy-require `<lib/core/room-bind-picker-policy.cjs>`, return `policy.unboundPickerSuppression({binding, cwd: STDIN_CWD, roomsRoot: root}) !== null` when the export is a function, else false; the catch returns false (fail toward today's picker). In `main()`, directly after `const root = resolveMindrianRoomsRoot(); if (!root) return 0;`, add `if (unboundPickerSuppressed(root)) return 0;` with a comment block: N-1 (an unbound session outside the rooms home gets no picker, no F.8 mint, no marker write; inside it nothing changes; an unreadable or ambiguous cwd fires, P-1), N-2 (a stored "dev repo / no room" choice is honored for the rest of the session; a new session asks again; an explicit room binding restores gating), the verified root cause from this plan's context, P-3 (this early return also silences the strict-mode override and the legacy advisory for these two session classes), SPEC R10 / R11. Keep it in the same early-guard block as the harness guard. Do not touch `lib/core/resolve-active-room.cjs`, `lib/workflow/session-binding-consumer.cjs` or anything under `lib/mcp/`.
    5. Run `node tests/test-360-picker-policy.cjs </dev/null`, `node tests/test-360-harness-picker.cjs </dev/null`, `node tests/test-360-tripwire.cjs </dev/null`, `node tests/test-360-leads.cjs </dev/null`, `node tests/test-360-r3-suites.cjs </dev/null` (all three groups) and `node tests/test-360-snapshot-replay.cjs </dev/null` (0 or 77; record layer c counts, anchor human post 0).
    6. Commit: `git add lib/core/room-bind-picker-policy.cjs` then `git commit --only -m "feat(360-07): unbound picker quiet outside the rooms home and after a dev repo / no room answer (N-1, N-2; R10, R11)" -- lib/core/room-bind-picker-policy.cjs lib/core/session-binding.cjs scripts/intent-classifier.cjs`.
  </action>
  <verify>
    <automated>node tests/test-360-picker-policy.cjs </dev/null && node tests/test-360-harness-picker.cjs </dev/null && node tests/test-360-tripwire.cjs </dev/null && node tests/test-360-r3-suites.cjs </dev/null</automated>
  </verify>
  <acceptance_criteria>
    - All four verify commands exit 0
    - `node tests/test-360-snapshot-replay.cjs </dev/null` exits 0 with the snapshot present (77 when absent) and prints anchor human post 0
    - `node -e "const s=require('./lib/core/session-binding.cjs');process.exit(s.NO_ROOM_SLUG==='__no_room__'?0:1)"` exits 0
    - `grep -c "'__no_room__'\|\"__no_room__\"" lib/core/room-bind-picker-policy.cjs` is 0 (the sentinel comes from session-binding)
    - `grep -c "realRoomRoot" lib/core/room-bind-picker-policy.cjs` is at least 1 (reuse, Part 7)
    - `grep -cE "require\(['\"](node:)?(https?|net|http2|dgram)['\"]\)|fetch\(" lib/core/room-bind-picker-policy.cjs` is 0 (no network, Canon Part 8)
    - `git diff <plan_base_sha> -- lib/core/session-binding.cjs | grep '^[-+]' | grep -v '^[-+][-+]' | grep -vc '^[-+]\s*//'` is at most 2 (additive export only)
    - Minimal hook diff: `git diff <plan_base_sha> -- scripts/intent-classifier.cjs | grep '^[-+]' | grep -v '^[-+][-+]' | grep -v '^[-+]\s*//' | grep -vc '^[-+]\s*$'` is at most 45
    - `git diff --name-only <plan_base_sha> -- lib/mcp lib/core/resolve-active-room.cjs lib/workflow/session-binding-consumer.cjs` restricted to 360 commits is empty (`node tests/test-360-tripwire.cjs --only r9 </dev/null` exits 0)
    - `! grep -l "$(printf '\xe2\x80\x94')" lib/core/room-bind-picker-policy.cjs lib/core/session-binding.cjs scripts/intent-classifier.cjs`
    - `git log --format=%s -30 | grep -c '(360-07)'` is at least 2
  </acceptance_criteria>
  <done>A dev-repo session is no longer nagged by the picker, a remembered "dev repo / no room" answer holds for the session, every fault falls back to today's picker, and no MCP file changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code UserPromptSubmit stdin -> intent-classifier | prompt, session_id and cwd are untrusted input from the host; the hook reads only their structure |
| peer / subagent text -> this session's binding store and the Stop hook's side channel | a harness turn must neither set write scope nor mint a gate the Stop hook enforces |
| intent-classifier -> shared libs (turn-text, session-binding, room-path-containment) | shared modules also serve the Stop hook and MCP; changes must be additive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-360-01 | Spoofing / Elevation of privilege | harness text consumed as the binding answer | mitigate | `if (!TURN_IS_HARNESS)` around the consumer call (D-08); 360-04 r2 spy leg green |
| T-360-02 | Denial of service (of the conversation) | a harness turn minting an F.8 gate the Stop hook force-blocks on | mitigate | `if (TURN_IS_HARNESS) return 0;` before every emitter (D-07); 360-04 r1 green with 0 side-channel records |
| T-360-03 | Spoofing (inverse) | a human turn misclassified, so a real picker is missed | mitigate | exact 'harness' only; lead-only verdict with 0 of 2,154 false positives; r3 byte-identity; the cwd rule fails toward the picker for unresolvable and ancestor cwds |
| T-360-06 | Denial of service | a guard fault crashing or blocking the prompt | mitigate | both helpers in try/catch returning false; 360-04 r6 and 360-05 fault legs green; exit 0 |
| T-360-08 | Tampering | the cwd rule silencing legitimate room work | mitigate | realpath containment against the same rooms root the gate scores; unbound-only; P-1 ancestor fires |
| T-360-09 | Spoofing | a harness turn writing the no-room sentinel to silence pickers | mitigate | harness turns never reach the consumer; the store's writers are unchanged; explicit rebind restores gating |
| T-360-07 | Information disclosure / Part 8 | a network dependency in the hook path | mitigate | the policy module requires only node:fs, node:path and two local modules; acceptance greps for network requires |
| T-360-11 | Tampering | editing MCP or the shared scope resolver | mitigate | never-edit list; r9 tripwire scoped to 360 commits; resolve-active-room and session-binding-consumer untouched |
</threat_model>

<verification>
- `node tests/test-360-harness-picker.cjs </dev/null && node tests/test-360-picker-policy.cjs </dev/null && node tests/test-360-tripwire.cjs </dev/null && node tests/test-360-leads.cjs </dev/null` all exit 0
- `node tests/test-360-r3-suites.cjs </dev/null` exits 0 (r3, mcp, wider no worse than baseline)
- `bash tests/run-all-360.sh </dev/null` exits 0
</verification>

<success_criteria>
In the CLI hook, harness turns never mint, inject or consume room-bind state; unbound dev-repo sessions and sessions that chose "dev repo / no room" are not re-asked; human turns inside the rooms home are byte-identical to before; MCP binding is untouched.
</success_criteria>

<output>
Create `.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-07-SUMMARY.md` when done (`git add -f`, then `git commit --only -- <path>`). Record: both fix commit shas, the hook diff line count, the r3 / mcp / wider comparison, the replay counts for both layers, and the P-1 / P-2 / P-3 decisions as shipped.
</output>
