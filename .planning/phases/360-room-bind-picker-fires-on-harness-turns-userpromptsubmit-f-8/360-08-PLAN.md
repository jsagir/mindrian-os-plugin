---
phase: 360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8
plan: 08
type: execute
wave: 5
depends_on: ["360-07"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-VALIDATION.md
  - /home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-phase-360-room-bind-picker-harness-turns-and-dev-repo.md
  - /home/jsagi/MindrianOS/research/2026-09-23-phase-360-room-bind-picker-harness-turns-and-dev-repo.md
autonomous: true
requirements: [BIND360-01, BIND360-02, BIND360-03, BIND360-04, BIND360-05, BIND360-06, BIND360-07, BIND360-08, BIND360-09, BIND360-10, BIND360-11]
tags: [close-out, verification, requirements, dual-filing, tri-polar]

must_haves:
  truths:
    - "The full phase runner is green with known reds unchanged, and each BIND360 row is closed with its measured proof (the test leg and its result) or left open with a stated reason; no row is ticked without a passing leg (D-15, D-17)"
    - "The local snapshot replay result is recorded as numbers: layer h harness 33 -> 0, human 2 -> 2, 0 human with a harness verdict (SPEC R7); layer c human pickers with an outside cwd 0 and the dev-repo anchor session 0 (N-1, SPEC R10); or SKIP with the stated reason when the snapshot is absent"
    - "Tri-Polar parity is proven, not argued: 360's own commits touch no lib/mcp path and the four MCP room-bind suites match the pre-phase baseline (SPEC R9, D-18)"
    - "VALIDATION.md is signed off with rows for BIND360-10 and BIND360-11 added and nyquist_compliant true"
    - "The research trail and outcome are filed in both research homes, cross-linked to 360-CONTEXT, with no transcript text, no names and no session ids (CLAUDE.md Dev-Research Compositing)"
    - "The close-out states plainly that the fix is on main but not live until a release ships and the installed plugin picks it up, and lists the follow-on candidates: capturing an AskUserQuestion card answer into the binding store (R11 card path), the harness-turn behavior of consumePriorF1Pick / the NAV block / injectGraphFindings (D-09 deferred), auto-continuation turns, and the P-1 / P-3 decisions for navigator review"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "BIND360-01..11 closed with proof or left open with a reason"
      contains: "BIND360-11"
    - path: ".planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-VALIDATION.md"
      provides: "sign-off, BIND360-10 / -11 rows"
      contains: "nyquist_compliant: true"
    - path: "/home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-phase-360-room-bind-picker-harness-turns-and-dev-repo.md"
      provides: "room-side research trail and outcome"
      contains: "Phase 360"
  key_links:
    - from: "/home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-phase-360-room-bind-picker-harness-turns-and-dev-repo.md"
      to: ".planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-CONTEXT.md"
      via: "cross-link line"
      pattern: "360-CONTEXT.md"
---

<objective>
Close Phase 360 with measured proof: run everything once more, record the replay numbers, close the BIND360 rows honestly, sign off validation, and file the reasoning trail in both research homes.

Purpose: D-15, D-17, D-18; SPEC R7, R8, R9, R10 acceptance evidence; CLAUDE.md Verification and Dev-Research Compositing. No runtime file changes in this plan.
Output: REQUIREMENTS and VALIDATION updates, two research files, the phase SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-SPEC.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-CONTEXT.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-RESEARCH.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-VALIDATION.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-07-SUMMARY.md

Parallel-session rules (every task, D-20): never write `.planning/STATE.md`. Run `git status --short -- <file>` before editing and STOP on a diff you did not make (REQUIREMENTS.md is shared with peer phases: edit only the BIND360 rows). Commit by explicit path only (`git add -f` for `.planning/**`, then `git commit --only -m "docs(360-08): ..." -- <paths>`); never `git add -A` / `git commit -a` / `git stash` / revert an unowned diff. Never edit: `lib/mcp/**`, `lib/core/write-lock.cjs`, `lib/core/part8-egress-guard.cjs`, `scripts/doctor.cjs`, `lib/core/graph-ops.cjs`, `lib/core/navigation.cjs`, `scripts/eval-icm-writers.cjs`, `tests/test-353-grader-agreement.cjs`, `tests/test-353-ledger-shape.cjs`, `docs/OPEN-HANDOFFS.md`. No em-dashes. The two research files live in the home repo (`/home/jsagi`, a separate git repo also used by peer sessions): commit them there by explicit path only. The research files carry no transcript text, no names, no session ids or prefixes, no cwd paths from the snapshot.

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/ups-harness-360/pre-phase.json`, `tests/test-360-r3-suites.cjs`, `tests/run-all-360.sh` | 01 |
| `tests/fixtures/ups-harness-360/cases.json`, `tests/fixtures/ups-harness-360/spawn-kit.cjs`, `tests/test-360-tripwire.cjs` | 02 |
| `tests/test-360-leads.cjs`, `tests/test-360-snapshot-replay.cjs` | 03 |
| `tests/test-360-harness-picker.cjs` plus preload stubs `capture-spy.cjs`, `throwing-turn-text.cjs`, `missing-export-turn-text.cjs` | 04 |
| `tests/test-360-picker-policy.cjs` plus preload stub `throwing-picker-policy.cjs` | 05 |
| `lib/hmi/turn-text.cjs` (HARNESS_LEADS widened, `classifyUserPromptText`), conditional 357 dev-tool imports | 06 |
| `scripts/intent-classifier.cjs` guards, `lib/core/room-bind-picker-policy.cjs`, `NO_ROOM_SLUG` export in `lib/core/session-binding.cjs` | 07 |
| BIND360 requirement closure, VALIDATION sign-off, dual research filing | 08 (this plan) |
</context>

<tasks>

<task type="auto">
  <name>Task 1: full evidence run, BIND360 row closure and VALIDATION sign-off</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-VALIDATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the "Phase 360 - Room-bind picker on harness turns and in dev repos (BIND360 family)" section only)
    - .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-VALIDATION.md (frontmatter, the Phase Requirements -> Test Map, Wave 0 Gaps, Validation Sign-Off)
    - .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-01-SUMMARY.md through 360-07-SUMMARY.md (per-leg results, known reds)
    - tests/fixtures/ups-harness-360/pre-phase.json (plan_base_sha, suites baseline)
  </read_first>
  <action>
    1. Evidence run (record every number in the SUMMARY): `bash tests/run-all-360.sh </dev/null` (must exit 0 with FAIL 0; the replay leg PASSED when the snapshot is present, SKIPPED otherwise); `node tests/test-360-snapshot-replay.cjs </dev/null` with its printed table (layer h: harness pre 33 / post 0, human pre 2 / post 2, human with a harness verdict 0; layer c: outside-cwd human post 0, anchor session human post 0); `node tests/test-360-tripwire.cjs --only r9 </dev/null` and `--only r8`; `node tests/test-360-r3-suites.cjs </dev/null` (r3, mcp and wider, known reds unchanged); and the CLAUDE.md gates `node scripts/build-connector-registry.cjs --check` and `node scripts/check-render-coverage.cjs` (expected unaffected: no command, agent, skill or pipeline surface changed). Also list 360's commits with `git log --format='%h %s' <plan_base_sha>..HEAD --grep='(360-'`.
    2. `.planning/REQUIREMENTS.md`: `git status --short -- .planning/REQUIREMENTS.md` must be empty. In the BIND360 section only, flip each row whose legs passed to `- [x]` and append one proof clause to its text (for example "Proof: test-360-harness-picker r1 green, 0 side writes across N harness entries; commit <sha>"). A row whose proof is missing stays `- [ ]` with "Open: <reason>". Do not touch any other family's rows or the Traceability counts.
    3. `360-VALIDATION.md`: set frontmatter `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`; add two rows to the Phase Requirements -> Test Map: BIND360-10 (cwd rule; `node tests/test-360-picker-policy.cjs --only r10` plus `node tests/test-360-snapshot-replay.cjs --layer c`) and BIND360-11 (no-room session memory; `node tests/test-360-picker-policy.cjs --only r11`); set "File Exists?" to yes for every row; add `tests/test-360-picker-policy.cjs` and `tests/test-360-r3-suites.cjs` to the Wave 0 list and tick it; tick every Validation Sign-Off box and set Approval to "approved <date> (360-08)".
    4. Commit: `git add -f .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-VALIDATION.md` then `git commit --only -m "docs(360-08): close BIND360-01..11 with measured proof, validation sign-off" -- .planning/REQUIREMENTS.md .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-VALIDATION.md`.
  </action>
  <verify>
    <automated>bash tests/run-all-360.sh </dev/null && node tests/test-360-tripwire.cjs --only r9 </dev/null && grep -q "nyquist_compliant: true" .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-360.sh </dev/null` exits 0 and its totals line shows FAIL 0
    - `grep -c "^- \[x\] \*\*BIND360-" .planning/REQUIREMENTS.md` plus `grep -c "^- \[ \] \*\*BIND360-" .planning/REQUIREMENTS.md` equals 11, and every `- [ ]` BIND360 row contains "Open:"
    - `grep -c "BIND360-10\|BIND360-11" .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-VALIDATION.md` is at least 2
    - Only the BIND360 section changed: `C=$(git log -1 --format=%H --grep='(360-08)' -- .planning/REQUIREMENTS.md); S=$(grep -n '^### Phase 360 - Room-bind picker' .planning/REQUIREMENTS.md | cut -d: -f1); E=$(grep -n '^## Traceability' .planning/REQUIREMENTS.md | cut -d: -f1); git diff -U0 "$C^" "$C" -- .planning/REQUIREMENTS.md | grep '^@@' | sed -E 's/^@@ [^+]*\+([0-9]+).*/\1/' | awk -v s="$S" -v e="$E" '$1<s||$1>e{bad=1} END{exit bad}'` exits 0
    - The SUMMARY records the replay numbers for both layers (or the SKIP reason) and the 360 commit list
    - `! grep -l "$(printf '\xe2\x80\x94')" .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-VALIDATION.md`
  </acceptance_criteria>
  <done>Every BIND360 row is either closed by a named passing leg or openly marked with its reason, and validation is signed off on measured results.</done>
</task>

<task type="auto">
  <name>Task 2: dual research filing, live-status statement and follow-on record</name>
  <files>/home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-phase-360-room-bind-picker-harness-turns-and-dev-repo.md, /home/jsagi/MindrianOS/research/2026-09-23-phase-360-room-bind-picker-harness-turns-and-dev-repo.md</files>
  <read_first>
    - /home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-larry-extended-x-jev-gate-triad-replay.md (the house format of a research entry and its cross-link line)
    - CLAUDE.md (section "Dev-Research Compositing (Rethinking Room)")
    - .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-RESEARCH.md (Findings 1-7) and 360-07-SUMMARY.md (shipped decisions P-1, P-2, P-3)
  </read_first>
  <action>
    1. Write the research entry (same content in both homes; Feynman-simple, JTBD-oriented, hyphens only, no em-dashes, no names, no session ids or prefixes, no transcript text, no snapshot cwd paths). Sections: "What was wrong" (the UserPromptSubmit hook treated every turn as human, so peer hand-backs, queued peer messages, task notifications and idle notices re-injected the room picker, 33 of 35 fires on 2026-09-23, and one of them minted the gate behind that day's anchor false block; and in dev repos the picker nagged every scoring turn, even after a stored "dev repo / no room" answer); "Why" (the hook stdin carries no origin or isMeta, so the only honest signal is the prompt's leading tag; the stored no-room sentinel was never a scored room, so the scope test called every turn off-scope and the gate rendered the unbound header); "What changed" (one shared lead list and one rule body in turn-text; one harness guard and one consumer guard in the hook; a realpath cwd rule and the remembered no-room choice in a small policy module; nothing in MCP); "Measured" (the replay numbers from Task 1, the 30-day census 0 of 2,154 human prompts, the 357 replay unchanged per entry); "Decisions for the navigator" (P-1 ancestor cwd fires; P-3 the early return also silences the strict-mode override and legacy advisory for the two covered session classes; the pasted-lead accepted limit); "Follow-on candidates" (capture an AskUserQuestion card answer into the binding store so the R11 memory also works when the answer comes through the card, not only as a typed label; the harness-turn behavior of consumePriorF1Pick, the NAV block and injectGraphFindings, D-09 deferred; auto-continuation turns; the Theo-side analog: none, this is plugin-local hook policy); "Live status" (on main, not live until a release ships and the installed plugin picks it up); and a final cross-link line naming `.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-CONTEXT.md` and `360-SPEC.md`.
    2. Commit in the home repo by explicit path: `git -C /home/jsagi status --short -- MindrianRooms/rethinking-mindrianos/research/2026-09-23-phase-360-room-bind-picker-harness-turns-and-dev-repo.md MindrianOS/research/2026-09-23-phase-360-room-bind-picker-harness-turns-and-dev-repo.md` shows only these new files; then `git -C /home/jsagi add <both paths>` and `git -C /home/jsagi commit --only -m "rethinking-mindrianos: Phase 360 room-bind picker research trail and outcome (harness turns, dev repo, no-room memory)" -- <both paths>`.
    3. Write `360-08-SUMMARY.md` with the Task 1 evidence, the live-status sentence, and the follow-on list; `git add -f` it and commit with `git commit --only -- <path>`.
  </action>
  <verify>
    <automated>grep -c "360-CONTEXT.md" /home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-phase-360-room-bind-picker-harness-turns-and-dev-repo.md /home/jsagi/MindrianOS/research/2026-09-23-phase-360-room-bind-picker-harness-turns-and-dev-repo.md</automated>
  </verify>
  <acceptance_criteria>
    - Both research files exist, are identical (`cmp` exits 0), and each contains `360-CONTEXT.md`, "not live until" and "Follow-on"
    - `grep -cE '[0-9a-f]{8}-[0-9a-f]{4}-|56924067|0208790f|21829408|0f86dd63|uds:' <both files>` reports 0 for each
    - `! grep -l "$(printf '\xe2\x80\x94')" <both files>`
    - `git -C /home/jsagi log --format=%s -10 -- MindrianRooms/rethinking-mindrianos/research/2026-09-23-phase-360-room-bind-picker-harness-turns-and-dev-repo.md | grep -c "Phase 360"` is at least 1
    - `360-08-SUMMARY.md` states the fix is on main and not live until released and picked up, and lists the four follow-on candidates
  </acceptance_criteria>
  <done>The reasoning trail lives in both research homes, cross-linked to the phase, and the navigator has the open decisions and follow-ons in one place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev repo -> home repo research files | findings cross into a separate, shared git repo; evidence must stay sanitized |
| phase close -> requirement ledger | a ticked row is a claim other sessions rely on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-360-04 | Information disclosure | research files | mitigate | no names, ids, prefixes, socket paths, cwd paths or transcript text; acceptance greps both files |
| T-360-10 | Repudiation | a BIND360 row ticked without proof | mitigate | every `- [x]` row carries its passing leg; unproven rows stay open with a reason |
| T-360-11 | Tampering | editing peer rows in REQUIREMENTS.md or peer files in the home repo | mitigate | edit only the BIND360 section; explicit-path commits in both repos; git status checks first |
</threat_model>

<verification>
- `bash tests/run-all-360.sh </dev/null` exits 0
- `node tests/test-360-tripwire.cjs --only r9 </dev/null` exits 0
- both research files present and identical
</verification>

<success_criteria>
Phase 360 closes on measured evidence: runner green, replay numbers recorded, Tri-Polar parity proven, requirements closed honestly, validation signed off, and the reasoning trail filed in both research homes.
</success_criteria>

<output>
Create `.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-08-SUMMARY.md` when done (`git add -f`, then `git commit --only -- <path>`). Record: the runner totals, both replay layers, the 360 commit list, the suite comparison, the REQUIREMENTS row states, the live-status sentence and the follow-on list.
</output>
