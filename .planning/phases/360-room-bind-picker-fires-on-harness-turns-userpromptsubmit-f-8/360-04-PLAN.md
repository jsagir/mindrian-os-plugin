---
phase: 360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8
plan: 04
type: execute
wave: 3
depends_on: ["360-02"]
files_modified:
  - tests/test-360-harness-picker.cjs
  - tests/fixtures/ups-harness-360/capture-spy.cjs
  - tests/fixtures/ups-harness-360/throwing-turn-text.cjs
  - tests/fixtures/ups-harness-360/missing-export-turn-text.cjs
autonomous: true
requirements: [BIND360-01, BIND360-02, BIND360-03, BIND360-06]
tags: [integration, spawn, preload, fault-injection, byte-identical, room-bind]

must_haves:
  truths:
    - "For every harness entry, the hook on HEAD emits no room-resolution marker and makes no F.8 side-channel record, no binding_gate / zero_score_gate payload, no strict-mode trace and no offered marker, while the same exact prompt on the PLAN_BASE code root fires (the SPEC R1 control proving the fixture scores) and the entry's control_prompt fires on HEAD (SPEC R1, D-07)"
    - "The bound off-scope, bound zero-score and strict-mode harness entries are suppressed too, so all four room-resolution outputs are covered by one guard (SPEC R1, D-07)"
    - "R2's RED leg is a call spy, not a bind outcome: on a harness turn after a pending payload, captureCliActionSet is invoked 0 times (1 before the fix), the binding file is unchanged and no binding_gate_consumed entry exists, and the next human turn with an exact label binds and writes binding_gate_consumed (SPEC R2, D-08, RESEARCH Finding 6)"
    - "Human-origin entries (cwd absent) produce byte-identical stdout on the PLAN_BASE code root and HEAD at the same fixture path, after HEAD is shown deterministic against itself (SPEC R3, D-17)"
    - "With turn-text's export made to throw or to be missing by a --require preload, the human turn's stdout is unchanged, the harness prompt fires (fail toward today's behavior), the process exits 0, and the stub records that it was actually reached (SPEC R6, D-16, PSB-06)"
    - "The Skill-body carve-out, mid-text quote, image lead and queued human entries fire; the pasted-lead accepted-limit entry is pinned as suppressed because CONTEXT D-04 is superseded by RESEARCH Finding 4 (lead-only verdict, 0 observed cases)"
  artifacts:
    - path: "tests/test-360-harness-picker.cjs"
      provides: "legs r1, r2, r3, r6 selectable by --only"
      min_lines: 200
    - path: "tests/fixtures/ups-harness-360/capture-spy.cjs"
      provides: "--require spy on f8-action-capture-cli captureCliActionSet"
      contains: "SPY_OUT_360"
    - path: "tests/fixtures/ups-harness-360/throwing-turn-text.cjs"
      provides: "--require stub: classifyUserPromptText throws after recording the call"
      contains: "SPY_OUT_360"
    - path: "tests/fixtures/ups-harness-360/missing-export-turn-text.cjs"
      provides: "--require stub: classifyUserPromptText getter records access and returns undefined"
      contains: "defineProperty"
  key_links:
    - from: "tests/fixtures/ups-harness-360/capture-spy.cjs"
      to: "lib/hmi/f8-action-capture-cli.cjs"
      via: "require cache: the same resolved module object consumePriorBindingAnswer lazy-requires"
      pattern: "captureCliActionSet"
    - from: "tests/test-360-harness-picker.cjs"
      to: "tests/fixtures/ups-harness-360/spawn-kit.cjs"
      via: "makeFixture / spawnClassifier / buildCodeRoot / sideWrites"
      pattern: "spawn-kit"
---

<objective>
Write the spawn-level proof for SPEC R1, R2, R3 and R6: harness prompts are suppressed with no side writes, a harness turn never reaches the binding-answer consumer, human turns are byte-identical to the pre-phase code, and classifier faults fail toward today's behavior.

Purpose: SPEC R1, R2, R3, R6; D-07, D-08, D-16, D-17; RESEARCH Finding 6 (spy, not bind outcome) and Pattern 3 (preload semantics). Tests are RED where the fix is missing (r1, r2, r6) and GREEN where they guard existing behavior (r3).
Output: `tests/test-360-harness-picker.cjs` and three preload stubs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-SPEC.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-CONTEXT.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-RESEARCH.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-02-SUMMARY.md
@tests/fixtures/ups-harness-360/spawn-kit.cjs
@tests/fixtures/ups-harness-360/cases.json

Parallel-session rules (every task, D-20): never write `.planning/STATE.md`. Run `git status --short -- <file>` before editing and STOP on a diff you did not make. Commit by explicit path only (`git add <new paths>`, then `git commit --only -m "test(360-04): ..." -- <paths>`); never `git add -A` / `git commit -a` / `git stash` / revert an unowned diff. Never edit: `lib/mcp/**`, `lib/core/write-lock.cjs`, `lib/core/part8-egress-guard.cjs`, `scripts/doctor.cjs`, `lib/core/graph-ops.cjs`, `lib/core/navigation.cjs`, `scripts/eval-icm-writers.cjs`, `tests/test-353-grader-agreement.cjs`, `tests/test-353-ledger-shape.cjs`, `docs/OPEN-HANDOFFS.md`. No em-dashes. Every spawn goes through `spawn-kit.cjs` (minimal env, fixture-root cwd), never a hand-built env.

Preload semantics (RESEARCH Pattern 3, D-16): Node caches `module.exports` per resolved path. A `--require` stub that requires the real module by the same absolute path and replaces a property changes what the classifier sees later, because the classifier looks the property up at call time through a lazy require of the same path. Each stub records every call or access by appending one line to `process.env.SPY_OUT_360` (a temp file path) when set, so a leg can prove the stub was reached instead of passing vacuously. No production env seam is added (D-16).

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/ups-harness-360/pre-phase.json`, `tests/test-360-r3-suites.cjs`, `tests/run-all-360.sh` | 01 |
| `tests/fixtures/ups-harness-360/cases.json`, `tests/fixtures/ups-harness-360/spawn-kit.cjs`, `tests/test-360-tripwire.cjs` | 02 |
| `tests/test-360-leads.cjs`, `tests/test-360-snapshot-replay.cjs` | 03 |
| `tests/test-360-harness-picker.cjs` plus preload stubs `capture-spy.cjs`, `throwing-turn-text.cjs`, `missing-export-turn-text.cjs` | 04 (this plan) |
| `tests/test-360-picker-policy.cjs` plus preload stub `throwing-picker-policy.cjs` | 05 |
| `lib/hmi/turn-text.cjs` (HARNESS_LEADS widened, `classifyUserPromptText`), conditional 357 dev-tool imports | 06 |
| `scripts/intent-classifier.cjs` guards, `lib/core/room-bind-picker-policy.cjs`, `NO_ROOM_SLUG` export in `lib/core/session-binding.cjs` | 07 |
| BIND360 requirement closure, VALIDATION sign-off, dual research filing | 08 |
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: r1 (suppression with no side writes, with controls) and r2 (consumer spy) legs</name>
  <files>tests/test-360-harness-picker.cjs, tests/fixtures/ups-harness-360/capture-spy.cjs</files>
  <read_first>
    - tests/fixtures/ups-harness-360/spawn-kit.cjs (every export; sideWrites semantics)
    - tests/fixtures/ups-harness-360/cases.json (entries with expect / control_prompt / setup; the r2-pending-answer sequence)
    - scripts/intent-classifier.cjs (lines 3293-3452 consumePriorBindingAnswer: backward scan, the lazy require of lib/hmi/f8-action-capture-cli.cjs at :3340, the binding_gate_consumed write; lines 3510-3580 the entry block call order)
    - lib/hmi/f8-action-capture-cli.cjs (lines 40-140: captureCliActionSet, _matchVerb exact whole-string match, module.exports at :140)
    - .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-RESEARCH.md (Finding 5 main() content; Finding 6 consumer semantics)
  </read_first>
  <behavior>
    - r1, every entry with expect 'suppress' and no accepted_limit: HEAD run exits 0, markersIn(stdout) is empty, and sideWrites shows 0 F.8 records, 0 binding_gate_payload, 0 zero_score_gate, 0 strict-mode traces and 0 offered markers for the session
    - r1 control A: the same exact prompt on buildCodeRoot(plan_base_sha) prints the entry's marker (or the unbound header by default)
    - r1 control B: the entry's control_prompt on HEAD prints that marker
    - r1, entries with expect 'fire' (carve-out, human-direct, human-queued, mid-text quote, image lead): HEAD prints the unbound header
    - r1, the accepted-limit entry: HEAD prints no marker (the recorded limit, not a defect)
    - r2: turn 1 (human) prints the unbound header and persists a binding_gate_payload; turn 2 (harness, containing a real option label verbatim, spawned with --require capture-spy.cjs) leaves the spy file with 0 captureCliActionSet lines, the binding file absent or unchanged, and no binding_gate_consumed entry; turn 3 (human, exactly that label) writes a binding whose bound set contains the label's slug and appends binding_gate_consumed
  </behavior>
  <action>
    1. Create `tests/fixtures/ups-harness-360/capture-spy.cjs`: resolve the repo root as three levels above the stub's dir, require `lib/hmi/f8-action-capture-cli.cjs` by absolute path, keep the original `captureCliActionSet`, and replace the export with a wrapper that appends the line `captureCliActionSet` to `process.env.SPY_OUT_360` (when set; any append error is swallowed) and then returns the original's result for the same arguments. CJS, `'use strict'`, header comment citing D-16 and RESEARCH Finding 6, no em-dashes.
    2. Create `tests/test-360-harness-picker.cjs` (node:assert; repo style `check(name, fn)`; `--only r1|r2|r3|r6`; prints `PASS test-360-harness-picker.cjs (N checks)`; exits 1 on failure; every fixture removed in a finally block). r1 iterates `CASES.entries` per the behavior list, using a fresh fixture per entry and `setup.binding` through `writeBinding` before the run; for the strict-mode entry follow what 360-02-SUMMARY recorded about control reachability. r2 follows the `r2-pending-answer` sequence: read the persisted payload after turn 1 from the fixture's decision-trace file, pick a real room label from `options` (never guessed), substitute it for `{{LABEL}}`, and set `SPY_OUT_360` to a temp file for turn 2 only.
    3. Run `node tests/test-360-harness-picker.cjs --only r1 </dev/null` and `--only r2`: both are expected RED on pre-fix code (harness entries fire; the spy records 1 call). Record the failing leg names and counts in the SUMMARY. Commit after Task 2.
  </action>
  <verify>
    <automated>node tests/test-360-harness-picker.cjs --only r2 </dev/null; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Before 360-07, `--only r1` and `--only r2` exit non-zero, and the r2 failure message names the spy count 1 (RED recorded in the SUMMARY)
    - `grep -c "buildCodeRoot" tests/test-360-harness-picker.cjs` is at least 1 (the R1 control runs the exact prompt on PLAN_BASE)
    - `grep -c "SPY_OUT_360" tests/fixtures/ups-harness-360/capture-spy.cjs` is at least 1
    - `grep -c "spawnSync\|execSync" tests/test-360-harness-picker.cjs` is 0 (all spawns go through spawn-kit)
    - `stat -c %Y ~/.mindrian/card-fire-reached.json 2>/dev/null` prints the same value before and after running the file (no real side-channel write)
    - `! grep -l "$(printf '\xe2\x80\x94')" tests/test-360-harness-picker.cjs tests/fixtures/ups-harness-360/capture-spy.cjs`
  </acceptance_criteria>
  <done>R1 and R2 are pinned by legs that fail today for the right reason and cannot pass vacuously.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: r3 byte-identical legs and r6 fault-injection legs with the two turn-text stubs</name>
  <files>tests/test-360-harness-picker.cjs, tests/fixtures/ups-harness-360/throwing-turn-text.cjs, tests/fixtures/ups-harness-360/missing-export-turn-text.cjs</files>
  <read_first>
    - tests/fixtures/ups-harness-360/spawn-kit.cjs (makeFixture opts.at, buildCodeRoot)
    - tests/fixtures/ups-harness-360/pre-phase.json (plan_base_sha)
    - lib/hmi/turn-text.cjs (module.exports is a plain unfrozen object, RESEARCH Pattern 3)
    - .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-RESEARCH.md (Finding 6 determinism measurement: 1530 B equal across fresh homes at the same path; Pitfall 8 stdin)
  </read_first>
  <behavior>
    - r3: for each human entry with expect 'fire' or 'unchanged' (no cwd in stdin), HEAD run twice at the same fixture path gives equal stdout bytes (else the leg fails with "non-deterministic output; byte identity cannot be proven", never normalizing); then the PLAN_BASE code root and HEAD at the same freshly rebuilt fixture path give equal stdout bytes; on a mismatch print only the first differing byte offset and both lengths
    - r6 throwing: with --require throwing-turn-text.cjs, the human-direct entry's stdout equals the no-stub HEAD stdout byte for byte, exit 0, and the spy file holds exactly 1 invocation line; the harness-task-notification entry prints the unbound header (fail toward today's behavior) and exits 0
    - r6 missing: with --require missing-export-turn-text.cjs, the same two assertions hold and the spy file holds at least 1 access line
  </behavior>
  <action>
    1. Create `tests/fixtures/ups-harness-360/throwing-turn-text.cjs`: require `lib/hmi/turn-text.cjs` by absolute path (repo root three levels up) and replace `classifyUserPromptText` with a function that appends `classifyUserPromptText invoked` to `process.env.SPY_OUT_360` when set, then throws `new Error('injected fault (R6)')`. The replacement is installed even when the real export does not exist yet, so the RED leg is meaningful before 360-06.
    2. Create `tests/fixtures/ups-harness-360/missing-export-turn-text.cjs`: require the same module and `Object.defineProperty(exports object, 'classifyUserPromptText', {configurable: true, enumerable: true, get() { append 'classifyUserPromptText accessed' to SPY_OUT_360 when set; return undefined; }})`.
    3. Add r3 and r6 to `tests/test-360-harness-picker.cjs` per the behavior list, using `makeFixture({at: <fixed temp path>})` so both code roots see the same absolute fixture path, and `buildCodeRoot(PRE_PHASE.plan_base_sha)`. If an r3 mismatch appears, diagnose before touching anything: run `git log --format='%h %s' <plan_base_sha>..HEAD -- scripts/intent-classifier.cjs lib/` and report non-360 commits that could explain it; do not normalize bytes.
    4. Run `--only r3` (expected GREEN on pre-fix code: HEAD equals PLAN_BASE) and `--only r6` (expected RED: the stubs record 0 invocations because the classifier does not call turn-text yet). Record both in the SUMMARY.
    5. Commit: `git add tests/test-360-harness-picker.cjs tests/fixtures/ups-harness-360/capture-spy.cjs tests/fixtures/ups-harness-360/throwing-turn-text.cjs tests/fixtures/ups-harness-360/missing-export-turn-text.cjs` then `git commit --only -m "test(360-04): harness picker legs R1/R2/R3/R6 with consumer spy and fault stubs - RED until 360-07" -- tests/test-360-harness-picker.cjs tests/fixtures/ups-harness-360/capture-spy.cjs tests/fixtures/ups-harness-360/throwing-turn-text.cjs tests/fixtures/ups-harness-360/missing-export-turn-text.cjs`.
  </action>
  <verify>
    <automated>node tests/test-360-harness-picker.cjs --only r3 </dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-360-harness-picker.cjs --only r3 </dev/null` exits 0 on pre-fix code
    - Before 360-07, `node tests/test-360-harness-picker.cjs --only r6 </dev/null` exits non-zero and names an invocation count of 0 (RED recorded)
    - `grep -c "injected fault (R6)" tests/fixtures/ups-harness-360/throwing-turn-text.cjs` is 1 and `grep -c "defineProperty" tests/fixtures/ups-harness-360/missing-export-turn-text.cjs` is 1
    - `grep -c "non-deterministic output" tests/test-360-harness-picker.cjs` is at least 1 (no silent normalization)
    - `node tests/test-360-tripwire.cjs --only r8 </dev/null` still exits 0 with the new stub files in the fixture dir
    - `! grep -l "$(printf '\xe2\x80\x94')" tests/fixtures/ups-harness-360/throwing-turn-text.cjs tests/fixtures/ups-harness-360/missing-export-turn-text.cjs tests/test-360-harness-picker.cjs`
    - `git log --format=%s -30 | grep -c '(360-04)'` is at least 1
  </acceptance_criteria>
  <done>Human-path byte identity is guarded against the real pre-phase code, and the fault path is proven reachable, so R6 cannot pass by accident.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer / subagent text -> this session's binding store | a harness turn must not be read as the navigator's answer |
| test process -> navigator's real state | spawned hooks must stay inside the fixture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-360-01 | Spoofing / Elevation of privilege | a harness turn consumed as the binding answer (write scope set by a peer) | mitigate | r2 call-spy leg (0 captureCliActionSet calls on a harness turn), binding file and consumed-marker assertions, exact-label human follow-up still binds |
| T-360-02 | Denial of service (of the conversation) | a harness turn minting an F.8 gate that force-blocks the next Stop | mitigate | r1 asserts 0 F.8 side-channel records and 0 payloads on every harness entry |
| T-360-06 | Denial of service | a classifier fault blocking or crashing the prompt | mitigate | r6 throwing and missing-export legs: exit 0, human output unchanged, stub reach proven |
| T-360-05 | Tampering | test writes to the real side channel | mitigate | spawn-kit minimal env and fixture cwd; acceptance compares the real side-channel mtime before and after |
</threat_model>

<verification>
- `node tests/test-360-harness-picker.cjs --only r3 </dev/null` exits 0
- `--only r1`, `--only r2`, `--only r6` exit non-zero before 360-07 (RED recorded with reasons)
</verification>

<success_criteria>
SPEC R1, R2, R3 and R6 each have a spawn-level leg that fails today for the stated reason (or guards today's behavior, for R3), using sanitized fixtures and no production test seam.
</success_criteria>

<output>
Create `.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-04-SUMMARY.md` when done (`git add -f`, then `git commit --only -- <path>`). Record: per-leg RED / GREEN status with the failing entry ids, the r2 spy count before the fix, the r3 byte lengths per entry, and any buildCodeRoot directory widening.
</output>
