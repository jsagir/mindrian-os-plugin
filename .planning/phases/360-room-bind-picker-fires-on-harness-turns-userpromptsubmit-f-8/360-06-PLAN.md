---
phase: 360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8
plan: 06
type: execute
wave: 3
depends_on: ["360-01", "360-03"]
files_modified:
  - lib/hmi/turn-text.cjs
  - tests/test-357-harness-source.cjs
  - scripts/extract-dogfood-stop-events.cjs
  - scripts/replay-card-fire.cjs
autonomous: true
requirements: [BIND360-04, BIND360-05, BIND360-07]
tags: [turn-text, harness-leads, shared-classifier, runtime-fix, 357-compat]

must_haves:
  truths:
    - "The ONE HARNESS_LEADS in lib/hmi/turn-text.cjs gains the two queued-peer tags '<agent-message' and '<cross-session-message', and its peer entry becomes the 37-char stem 'Another Claude session sent a message' (a prefix of 357-07's 40-char LCP, so every prior match still matches), all as structural leading-tag startsWith matches, never meaning (SPEC R5, D-10, RESEARCH Finding 7, Pitfall 6)"
    - "classifyUserPromptText(text) is the UserPromptSubmit entry point: it calls classifyPrecedingUserContentSource(text, lead-only rec) and carries no rule body or tag list of its own, so the Stop hook and the UserPromptSubmit hook share one rule body and one list (SPEC R4, D-01 signature under Claude's Discretion, D-02)"
    - "CONTEXT D-04 (a bounded transcript tail read) is superseded by RESEARCH Finding 4 (A3 unresolved; the docs say the transcript may lag): the verdict is lead-only with zero file reads, and a human who pastes a harness lead as the very first characters is an accepted limit with 0 observed cases in 2,154 human prompts; D-03 is moot because the UserPromptSubmit stdin carries no origin or isMeta (RESEARCH Finding 1)"
    - "classifyPrecedingUserContentSource's body is byte-identical to PLAN_BASE, its 1-arg contract is unchanged, and the 357 replay gives identical per-entry outcomes to the plan-01 reference, so the list change flips no Stop verdict (0 pass->block, 0 human-upstream flips) (D-11, SPEC R5)"
    - "If a 357 leg pins the absence of the two tags, it is updated in the same commit citing R5, and if the plan-01 census found a harness lead literal in a 357 dev tool, that tool imports HARNESS_LEADS instead, so the list is defined in exactly one file; any other extra file STOPS the plan (D-11, SPEC R4, RESEARCH Pitfalls 3 and 7)"
  artifacts:
    - path: "lib/hmi/turn-text.cjs"
      provides: "HARNESS_LEADS (5 frozen entries), classifyUserPromptText"
      exports: ["HARNESS_LEADS", "classifyUserPromptText", "classifyPrecedingUserContentSource"]
  key_links:
    - from: "lib/hmi/turn-text.cjs classifyUserPromptText"
      to: "lib/hmi/turn-text.cjs classifyPrecedingUserContentSource"
      via: "direct call with the lead-only rec"
      pattern: "classifyPrecedingUserContentSource\\("
---

<objective>
Make the one shared classifier cover every harness prompt the UserPromptSubmit hook actually sees, and give that hook its entry point, without changing a single Stop-hook verdict.

Purpose: SPEC R4, R5, R7 (layer h of the replay goes green here); D-01, D-02, D-05, D-10, D-11; RESEARCH Findings 1, 4, 7 and Pitfalls 3, 6, 7. `tests/test-360-leads.cjs` goes GREEN; intent-classifier is untouched here (360-07 wires it).
Output: `lib/hmi/turn-text.cjs` edited; conditional edits to one 357 test and up to two 357 dev tools.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-SPEC.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-CONTEXT.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-RESEARCH.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-01-SUMMARY.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-03-SUMMARY.md
@tests/fixtures/ups-harness-360/pre-phase.json
@lib/hmi/turn-text.cjs

Parallel-session rules (every task, D-20): never write `.planning/STATE.md`. Run `git status --short -- <file>` before editing and STOP on a diff you did not make. Commit by explicit path only (`git add <new paths>` when new, then `git commit --only -m "<type>(360-06): ..." -- <paths>`); never `git add -A` / `git commit -a` / `git stash` / revert an unowned diff. Never edit: `lib/mcp/**`, `lib/core/write-lock.cjs`, `lib/core/part8-egress-guard.cjs`, `scripts/doctor.cjs`, `lib/core/graph-ops.cjs`, `lib/core/navigation.cjs`, `scripts/eval-icm-writers.cjs`, `tests/test-353-grader-agreement.cjs`, `tests/test-353-ledger-shape.cjs`, `docs/OPEN-HANDOFFS.md`. No em-dashes. `lib/hmi/turn-text.cjs` stays pure local CJS (only `node:fs` today): no new require, no network, no ledger, no transcript read added.

The three conditional files in `files_modified` are touched ONLY when `tests/fixtures/ups-harness-360/pre-phase.json` says so: `tests/test-357-harness-source.cjs` when `absence_pin_legs` is non-empty; `scripts/extract-dogfood-stop-events.cjs` and `scripts/replay-card-fire.cjs` when they appear in `lead_literal_files`. When a condition is false the file is not opened for writing, and the SUMMARY says "not needed" for it.

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/ups-harness-360/pre-phase.json`, `tests/test-360-r3-suites.cjs`, `tests/run-all-360.sh` | 01 |
| `tests/fixtures/ups-harness-360/cases.json`, `tests/fixtures/ups-harness-360/spawn-kit.cjs`, `tests/test-360-tripwire.cjs` | 02 |
| `tests/test-360-leads.cjs`, `tests/test-360-snapshot-replay.cjs` | 03 |
| `tests/test-360-harness-picker.cjs` plus preload stubs `capture-spy.cjs`, `throwing-turn-text.cjs`, `missing-export-turn-text.cjs` | 04 |
| `tests/test-360-picker-policy.cjs` plus preload stub `throwing-picker-policy.cjs` | 05 |
| `lib/hmi/turn-text.cjs` (HARNESS_LEADS widened, `classifyUserPromptText`), conditional 357 dev-tool imports | 06 (this plan) |
| `scripts/intent-classifier.cjs` guards, `lib/core/room-bind-picker-policy.cjs`, `NO_ROOM_SLUG` export in `lib/core/session-binding.cjs` | 07 |
| BIND360 requirement closure, VALIDATION sign-off, dual research filing | 08 |
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: widen HARNESS_LEADS and add classifyUserPromptText (tests from 360-03 go GREEN)</name>
  <files>lib/hmi/turn-text.cjs, tests/test-357-harness-source.cjs</files>
  <read_first>
    - lib/hmi/turn-text.cjs (whole file as 357-07 shipped it: HARNESS_LEADS and its comment, classifyPrecedingUserContentSource and its rec fields, readTurnText's use of the rule, module.exports)
    - tests/fixtures/ups-harness-360/pre-phase.json (rec_field_names, lead_only_reduction, lead_only_rec, harness_leads_at_plan_base, absence_pin_legs)
    - tests/test-360-leads.cjs (every behavior this task must satisfy)
    - tests/test-357-harness-source.cjs (only when absence_pin_legs is non-empty: the named legs)
    - tests/test-209-primary-sidechannel.cjs (Behavior 14, the 1-arg contract)
    - .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-RESEARCH.md (Pattern 1; Findings 1, 4, 7; Pitfalls 6, 7)
  </read_first>
  <behavior>
    - tests/test-360-leads.cjs passes every check
    - HARNESS_LEADS is frozen with exactly: '<task-notification', '[Cross-session idle notice]', 'Another Claude session sent a message', '<agent-message', '<cross-session-message'
    - classifyPrecedingUserContentSource's function source text equals PLAN_BASE's, and its 1-arg calls return typed,tool_result,none,none
    - classifyUserPromptText never throws; non-string input returns 'none'
  </behavior>
  <action>
    1. `git status --short -- lib/hmi/turn-text.cjs tests/test-357-harness-source.cjs` must be empty; STOP otherwise.
    2. HARNESS_LEADS (D-10, SPEC R5): keep 357-07's `<task-notification` and `[Cross-session idle notice]` entries in place; replace the peer-framing entry (whatever prefix 357-07 measured, for example the 40-char LCP ending in a newline and `<`) with the stem `Another Claude session sent a message` in the same position, or add it when 357-07 added no peer entry; append `<agent-message` and `<cross-session-message`. Keep `Object.freeze`. Rewrite the comment above it: the five structural leads, why the two queued-peer tags are back (SPEC R5: they lead the prompt text of 15 queued peer deliveries at UserPromptSubmit, 7 of which fired the picker, even though they have 0 stored occurrences as user text on the Stop path), why the stem (RESEARCH Finding 7 / Pitfall 6: a prefix of the measured LCP, covers the binary's while-you-were-working variant and CRLF), and "structural startsWith on the trimmed lead only, never meaning".
    3. Add `classifyUserPromptText(text)` directly after `classifyPrecedingUserContentSource`: inside try/catch, return `'none'` for a non-string, else return `classifyPrecedingUserContentSource(text, LEAD_ONLY_REC)` where LEAD_ONLY_REC is `{isMeta: false, originKind: undefined, prevHumanUpstream: true}` under the shipped field names, or the `lead_only_rec` recorded in pre-phase.json when `lead_only_reduction` was false (adapt to 357, never edit its rule body, D-19); the catch returns `'none'`. Doc comment: Phase 360 UserPromptSubmit entry point (SPEC R4, R5); D-01's working name `classifyUserPromptPayload(payload, opts)` was replaced under Claude's Discretion by a text signature, because the hook already extracts the prompt (`STDIN_MESSAGE`) and judging exactly that text needs no second JSON parse; D-02 one rule body; D-03 moot and D-04 superseded (RESEARCH Findings 1 and 4: the stdin has no origin or isMeta and the transcript may lag, so the verdict is lead-only with zero file reads); isMeta stays false here because forwarding it without a provable previous record would break the R-A carve-out; accepted limit: a human prompt whose first characters are a harness lead is treated as harness (0 of 2,154 in the 30-day census). Add `classifyUserPromptText` to `module.exports`.
    4. Only when `absence_pin_legs` is non-empty: in `tests/test-357-harness-source.cjs` change each named leg so it asserts the two tags ARE present as leads and that a Stop-path record with originKind 'human' and such a lead still returns 'typed'; the leg comment cites SPEC R5 and D-11. Touch nothing else in that file.
    5. Run `node tests/test-360-leads.cjs </dev/null` (GREEN), `node tests/test-357-harness-source.cjs </dev/null`, `node tests/test-209-primary-sidechannel.cjs </dev/null`, and the rule-body identity check from the acceptance list.
    6. Commit: `git commit --only -m "fix(360-06): harness leads cover queued peer tags; UPS entry classifyUserPromptText (R4, R5; 15 queued-peer runs, D-10)" -- lib/hmi/turn-text.cjs` plus `tests/test-357-harness-source.cjs` when step 4 ran.
  </action>
  <verify>
    <automated>node tests/test-360-leads.cjs </dev/null && node tests/test-357-harness-source.cjs </dev/null && node tests/test-209-primary-sidechannel.cjs </dev/null</automated>
  </verify>
  <acceptance_criteria>
    - All three verify commands exit 0
    - `node -e "const t=require('./lib/hmi/turn-text.cjs');const L=t.HARNESS_LEADS;const w=['<task-notification','[Cross-session idle notice]','Another Claude session sent a message','<agent-message','<cross-session-message'];if(!Object.isFrozen(L)||L.length!==5||w.some(x=>!L.includes(x)))process.exit(1);if(typeof t.classifyUserPromptText!=='function')process.exit(2)"` exits 0
    - `node -e "const t=require('./lib/hmi/turn-text.cjs');const r=[t.classifyPrecedingUserContentSource('hi'),t.classifyPrecedingUserContentSource([{type:'tool_result',tool_use_id:'x'}]),t.classifyPrecedingUserContentSource([]),t.classifyPrecedingUserContentSource(null)].join();process.exit(r==='typed,tool_result,none,none'?0:1)"` exits 0
    - Rule body unchanged: `B=$(node -p "require('./tests/fixtures/ups-harness-360/pre-phase.json').plan_base_sha"); git show "$B":lib/hmi/turn-text.cjs > "$TMPDIR/tt-base.cjs" && node -e "const a=require(process.env.TMPDIR+'/tt-base.cjs').classifyPrecedingUserContentSource.toString();const b=require('./lib/hmi/turn-text.cjs').classifyPrecedingUserContentSource.toString();process.exit(a===b?0:1)"` exits 0 (TMPDIR set to a scratch dir)
    - `grep -c "transcript_path\|readFileSync" lib/hmi/turn-text.cjs` equals its PLAN_BASE count (no new file read; D-04 superseded)
    - `grep -c "D-04" lib/hmi/turn-text.cjs` is at least 1 and `grep -c "RESEARCH Finding 4\|Finding 4" lib/hmi/turn-text.cjs` is at least 1
    - `! grep -l "$(printf '\xe2\x80\x94')" lib/hmi/turn-text.cjs tests/test-357-harness-source.cjs`
  </acceptance_criteria>
  <done>The shared list covers every UserPromptSubmit harness lead, the UserPromptSubmit entry point reuses the 357 rule body unchanged, and the 360 lead legs pass.</done>
</task>

<task type="auto">
  <name>Task 2: R4 one-definition closure (conditional) and 357 compatibility proof</name>
  <files>scripts/extract-dogfood-stop-events.cjs, scripts/replay-card-fire.cjs</files>
  <read_first>
    - tests/fixtures/ups-harness-360/pre-phase.json (lead_literal_files, replay_357_reference)
    - scripts/extract-dogfood-stop-events.cjs and scripts/replay-card-fire.cjs (only those listed in lead_literal_files: every non-comment line carrying a harness lead literal, and how the literal is used)
    - tests/run-all-357.sh (which 357 legs exercise those tools)
    - .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-RESEARCH.md (Pitfall 3)
  </read_first>
  <action>
    1. Read `lead_literal_files` from pre-phase.json and remove `lib/hmi/turn-text.cjs`. If the remainder is empty, write "R4 closure not needed: census = turn-text only" in the SUMMARY and go to step 3. If it holds any path other than `scripts/extract-dogfood-stop-events.cjs` or `scripts/replay-card-fire.cjs`, STOP and report the path (never allowlist, never edit an unplanned file; SPEC R4 says exactly one definition).
    2. For each listed 357 dev tool: `git status --short -- <file>` must be empty (STOP otherwise). Replace each harness lead literal on a non-comment line with a reference into `require('../lib/hmi/turn-text.cjs').HARNESS_LEADS` (a `HARNESS_LEADS.some(...)` / `HARNESS_LEADS.find(...)` startsWith test where the tool classifies a lead, or an indexed lookup of the matching entry where the tool needs the tag text), preserving the tool's observable behavior; add a one-line comment citing SPEC R4 (one definition). If a literal cannot be expressed through HARNESS_LEADS without new logic, STOP and report instead of inventing a rule. Run that tool's own 357 legs from `tests/run-all-357.sh`. Commit: `git commit --only -m "refactor(360-06): 357 dev tools import HARNESS_LEADS (R4 one definition)" -- <edited tool paths>`.
    3. 357 compatibility proof (D-11), under the isolation rule (temp HOME / MINDRIAN_HOME / CARD_FIRE_SIDECHANNEL_PATH / TMPDIR): `bash tests/run-all-357.sh </dev/null` exits 0; run the 357 replay in compare mode with JSON output into the scratch dir and compare it with `replay_357_reference` from pre-phase.json: identical counts and an identical outcome for every entry id (0 class changes means 0 pass->block flips and 0 human-upstream flips). Record the comparison in the SUMMARY. A non-empty diff STOPS the plan with the entry ids (the list change must not move a Stop verdict).
    4. Run `node tests/test-360-tripwire.cjs --only r4d </dev/null` (GREEN now) and, with the snapshot present, `node tests/test-360-snapshot-replay.cjs --layer h </dev/null` (GREEN: harness 33 -> 0, human 2 -> 2, 0 human with a harness verdict; or 77 when absent). Record the printed counts.
  </action>
  <verify>
    <automated>node tests/test-360-tripwire.cjs --only r4d </dev/null && node tests/test-360-snapshot-replay.cjs --layer h </dev/null; s=$?; test $s -eq 0 -o $s -eq 77</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-360-tripwire.cjs --only r4d </dev/null` exits 0 (lead list defined in exactly one file)
    - The replay comparison in step 3 reports 0 changed entry outcomes and equal counts (written in the SUMMARY with the entry count)
    - An isolated `bash tests/run-all-357.sh </dev/null` exits 0
    - `node tests/test-360-snapshot-replay.cjs --layer h </dev/null` exits 0 when the snapshot is present (77 when absent), printing harness post 0 and human post 2
    - When a 357 tool was edited: `grep -v '^\s*//' <tool> | grep -c "<task-notification\|<agent-message\|<cross-session-message\|Cross-session idle notice\|Another Claude session sent"` is 0 and `grep -c "HARNESS_LEADS" <tool>` is at least 1
    - `git log --format=%s -30 | grep -c '(360-06)'` is at least 1
  </acceptance_criteria>
  <done>There is exactly one harness lead list in lib/ and scripts/, and the 357 replay proves the list change moved no Stop-hook verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code prompt text -> shared classifier | untrusted text; the verdict reads only its leading characters, never its meaning |
| 360 change -> 357 Stop-hook verdicts | the shared list serves both hooks; a Stop verdict must not move |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-360-03 | Spoofing (inverse) | a human prompt misread as harness, so a real picker is missed | mitigate | lead-only startsWith on the trimmed text; mid-text, Skill body, image and slash-command legs stay typed; 0 of 2,154 human prompts lead with a harness lead; pasted-lead case recorded as an accepted limit |
| T-360-10 | Repudiation | a silent Stop-hook behavior change from the shared list | mitigate | per-entry 357 replay outcomes compared with the plan-01 reference; rule-body source identity check; 1-arg contract check |
| T-360-11 | Tampering | forking the 357 rule or editing 357 files outside the stated R4 / R5 reasons | mitigate | rule body byte-identical; conditional edits only as recorded by the plan-01 census, cited in the commit; any other file STOPS the plan |
| T-360-07 | Information disclosure / Part 8 | a new dependency or file read in a shared lib | mitigate | no new require in turn-text; readFileSync / transcript_path counts unchanged |
</threat_model>

<verification>
- `node tests/test-360-leads.cjs </dev/null && node tests/test-357-harness-source.cjs </dev/null && node tests/test-209-primary-sidechannel.cjs </dev/null` exit 0
- isolated `bash tests/run-all-357.sh </dev/null` exits 0
- `node tests/test-360-tripwire.cjs --only r4d </dev/null` exits 0
</verification>

<success_criteria>
The shared classifier recognizes every harness lead the UserPromptSubmit hook sees, the hook has a one-line entry point into the 357 rule body, and no Stop-hook verdict changed.
</success_criteria>

<output>
Create `.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-06-SUMMARY.md` when done (`git add -f`, then `git commit --only -- <path>`). Record: the final HARNESS_LEADS, the rec used by classifyUserPromptText, which conditional files were touched (or "not needed"), the 357 replay comparison, and the layer-h replay counts.
</output>
