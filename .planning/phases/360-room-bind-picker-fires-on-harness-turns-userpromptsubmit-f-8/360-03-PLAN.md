---
phase: 360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8
plan: 03
type: execute
wave: 2
depends_on: ["360-01"]
files_modified:
  - tests/test-360-leads.cjs
  - tests/test-360-snapshot-replay.cjs
autonomous: true
requirements: [BIND360-05, BIND360-07, BIND360-10]
tags: [unit, replay, local-only, turn-text, harness-leads, cwd]

must_haves:
  truths:
    - "Each of the five UserPromptSubmit harness leads (task notification, queued cross-session tag, queued agent-message tag, the stored peer framing, the idle notice) plus the while-you-were-working framing variant, a CRLF framing and a whitespace-prefixed lead returns 'harness' from classifyUserPromptText; a human prompt quoting a tag mid-text, a Skill body, an image lead and a slash-command lead return 'typed' (SPEC R5, D-05, D-12, RESEARCH Pitfall 6, R-A carve-out by construction)"
    - "classifyUserPromptText(t) equals classifyPrecedingUserContentSource(t, lead-only rec) for every case, proving one rule body shared by the Stop hook and the UserPromptSubmit hook (D-02, SPEC R4)"
    - "Non-string and empty inputs return a non-harness class and never throw, so a fault can only fail toward today's behavior (SPEC R6)"
    - "The local replay attributes every intent-classifier UserPromptSubmit run to its trigger by the parentUuid walk, derives ground truth from origin.kind / promptSource / commandMode only (no lead literals), and under the harness verdict reports unbound picker fires harness 33 -> 0 and human 2 -> 2 with 0 human runs given a harness verdict (SPEC R7)"
    - "The same replay layered with the cwd rule reports 0 human-turn pickers where cwd resolves outside the rooms home, including the dev-repo anchor session 56924067 identified by the sha256 of its id, never by the id itself (N-1, SPEC R10)"
    - "The replay reads the snapshot only when present (sha256sum -c first), skips with exit 77 and a stated reason when absent, and prints counts, booleans and cwd classes only (R-D, SPEC R7, R8)"
  artifacts:
    - path: "tests/test-360-leads.cjs"
      provides: "R5 lead legs, D-12 mid-text negative, carve-out by construction, one-rule-body equivalence, fault-shape legs"
      min_lines: 100
    - path: "tests/test-360-snapshot-replay.cjs"
      provides: "local-only counterfactual replay, layers h and c"
      min_lines: 150
  key_links:
    - from: "tests/test-360-leads.cjs"
      to: "lib/hmi/turn-text.cjs"
      via: "classifyUserPromptText and classifyPrecedingUserContentSource"
      pattern: "classifyUserPromptText"
    - from: "tests/test-360-snapshot-replay.cjs"
      to: "lib/core/room-bind-picker-policy.cjs"
      via: "cwdRoomsHomeVerdict for the layer-c cwd class"
      pattern: "cwdRoomsHomeVerdict"
---

<objective>
Write the two tests that judge the verdict functions directly: the lead unit legs (RED until 360-06 ships `classifyUserPromptText`) and the local-only snapshot replay that proves the 2026-09-23 evidence moves as SPEC R7 and R10 require (RED until 360-06 / 360-07, SKIP when the snapshot is absent).

Purpose: SPEC R5, R6 (shape faults), R7, R10 acceptance; D-02, D-05, D-12; RESEARCH Pattern 4 and Finding 3.
Output: `tests/test-360-leads.cjs`, `tests/test-360-snapshot-replay.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-SPEC.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-CONTEXT.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-RESEARCH.md
@.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-01-SUMMARY.md
@tests/fixtures/ups-harness-360/pre-phase.json

Parallel-session rules (every task, D-20): never write `.planning/STATE.md`. Run `git status --short -- <file>` before editing and STOP on a diff you did not make. Commit by explicit path only (`git add <new paths>`, then `git commit --only -m "test(360-03): ..." -- <paths>`); never `git add -A` / `git commit -a` / `git stash` / revert an unowned diff. Never edit: `lib/mcp/**`, `lib/core/write-lock.cjs`, `lib/core/part8-egress-guard.cjs`, `scripts/doctor.cjs`, `lib/core/graph-ops.cjs`, `lib/core/navigation.cjs`, `scripts/eval-icm-writers.cjs`, `tests/test-353-grader-agreement.cjs`, `tests/test-353-ledger-shape.cjs`, `docs/OPEN-HANDOFFS.md`. No em-dashes. The snapshot `~/.cache/mindrian-dev/357-raw/` is local-only: the replay reads it in place, never copies it, and never echoes snapshot prompt bodies, peer names, session ids or cwd paths (it prints classes and counts).

Measured expectations (RESEARCH Finding 3, re-measured at plan time 2026-09-23 with the cwd class added): 122 intent-classifier runs = 38 human, 77 harness, 7 other. Unbound-header fires: harness 33, human 2. Both human fires ran with a cwd outside the rooms home (one in the dev-repo anchor session, one in another session); 0 human fires ran with a cwd equal to an ancestor of the rooms home or inside it. So layer c (harness verdict plus the cwd rule) takes human 2 -> 0, and the anchor session's human pickers 1 -> 0.

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/ups-harness-360/pre-phase.json`, `tests/test-360-r3-suites.cjs`, `tests/run-all-360.sh` | 01 |
| `tests/fixtures/ups-harness-360/cases.json`, `tests/fixtures/ups-harness-360/spawn-kit.cjs`, `tests/test-360-tripwire.cjs` | 02 |
| `tests/test-360-leads.cjs`, `tests/test-360-snapshot-replay.cjs` | 03 (this plan) |
| `tests/test-360-harness-picker.cjs` plus preload stubs `capture-spy.cjs`, `throwing-turn-text.cjs`, `missing-export-turn-text.cjs` | 04 |
| `tests/test-360-picker-policy.cjs` plus preload stub `throwing-picker-policy.cjs` | 05 |
| `lib/hmi/turn-text.cjs` (HARNESS_LEADS widened, `classifyUserPromptText`), conditional 357 dev-tool imports | 06 |
| `scripts/intent-classifier.cjs` guards, `lib/core/room-bind-picker-policy.cjs`, `NO_ROOM_SLUG` export in `lib/core/session-binding.cjs` | 07 |
| BIND360 requirement closure, VALIDATION sign-off, dual research filing | 08 |
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lead unit legs against the shared turn-text export (RED until 360-06)</name>
  <files>tests/test-360-leads.cjs</files>
  <read_first>
    - lib/hmi/turn-text.cjs (HARNESS_LEADS, classifyPrecedingUserContentSource and its rec fields as shipped by 357-07)
    - tests/fixtures/ups-harness-360/pre-phase.json (rec_field_names, lead_only_reduction, lead_only_rec)
    - .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-RESEARCH.md (Pattern 1; Finding 2 prompt forms; Finding 7 and Pitfall 6 on the 37-char peer stem)
  </read_first>
  <behavior>
    - classifyUserPromptText returns 'harness' for a prompt leading with: a task-notification tag; a cross-session-message tag with from="sample-peer"; an agent-message tag with from="sample-peer"; "Another Claude session sent a message:" then a newline and a tag; "Another Claude session sent a message while you were working:" then a newline and a tag; the same framing with CRLF; the bracketed cross-session idle notice; any of these preceded by spaces or newlines
    - It returns 'typed' for: a human sentence that quotes a task-notification tag in its middle (D-12); a Skill body leading "Base directory for this skill:"; "[Image #1]" then text; a slash-command lead "<command-message>"; a plain human sentence
    - It returns 'none' (never throws) for '', undefined, null, 42, {} and []
    - For every string case above, classifyUserPromptText(t) === classifyPrecedingUserContentSource(t, LEAD_ONLY_REC), where LEAD_ONLY_REC is {isMeta: false, originKind: undefined, prevHumanUpstream: true} under the shipped field names, or pre-phase.json lead_only_rec when recorded (D-02)
    - HARNESS_LEADS is frozen, has exactly five entries, and equals as a set the three R-A leads with the peer entry being the 37-char stem "Another Claude session sent a message", plus the two queued-peer tags (R5, D-10)
    - Stop-path invariants: classifyPrecedingUserContentSource(an agent-message-led text, {isMeta: false, originKind: 'human', prevHumanUpstream: true}) is 'typed' (a human-origin record with a tag lead stays typed); the 1-arg contract still returns typed,tool_result,none,none
    - 1000 calls of classifyUserPromptText on a 4 KB human prompt take under 200 ms total (hook budget, no file read)
  </behavior>
  <action>
    1. Create `tests/test-360-leads.cjs` (node:assert, CJS, repo test style: `check(name, fn)` printing `  ok - <name>`, final `PASS test-360-leads.cjs (N checks)`, exit 1 with the stack on failure). Build every prompt from placeholders (from="sample-peer", fingerprint-free body text). Read the rec field names and any `lead_only_rec` from `tests/fixtures/ups-harness-360/pre-phase.json`, so the test adapts to what 357-07 shipped (D-19). If `require('../lib/hmi/turn-text.cjs').classifyUserPromptText` is not a function, fail with the single message "classifyUserPromptText missing (RED until 360-06)".
    2. Implement every behavior above as its own check.
    3. Run it and record RED in the SUMMARY (expected: the missing-export failure). Commit with Task 2.
  </action>
  <verify>
    <automated>node tests/test-360-leads.cjs </dev/null; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Before 360-06, `node tests/test-360-leads.cjs </dev/null` exits non-zero and its output contains `classifyUserPromptText missing`
    - `grep -c "check(" tests/test-360-leads.cjs` is at least 20
    - `grep -c "Another Claude session sent a message while you were working" tests/test-360-leads.cjs` is at least 1 (Pitfall 6 variant leg)
    - `grep -c "pre-phase.json" tests/test-360-leads.cjs` is at least 1 (adapts to 357's shipped rec names)
    - `grep -cE '[0-9a-f]{8}-[0-9a-f]{4}-' tests/test-360-leads.cjs` is 0 and `grep -c "uds:" tests/test-360-leads.cjs` is 0
    - `! grep -l "$(printf '\xe2\x80\x94')" tests/test-360-leads.cjs`
  </acceptance_criteria>
  <done>The lead contract of the one shared classifier is pinned leg by leg, and it fails for exactly one reason until 360-06 lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: local-only snapshot replay with layer h (harness verdict) and layer c (plus the cwd rule)</name>
  <files>tests/test-360-snapshot-replay.cjs</files>
  <read_first>
    - .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-RESEARCH.md (Finding 3 attribution; Pattern 4 counterfactual replay; Pitfall 5 off-scope state dependence)
    - .planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-SPEC.md (R7 and Amendment R10 acceptance)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md (R-D snapshot location and SHA256SUMS)
    - tests/run-all-360.sh (MOS360_SNAPSHOT_DIR and MOS360_REPLAY_ROOMS_HOME exports)
  </read_first>
  <behavior>
    - Snapshot dir absent: prints one line starting "SKIP:" naming the dir and "local-only evidence (R-D)", exits 77
    - `sha256sum -c SHA256SUMS` fails in the snapshot dir: exits 1 with "snapshot integrity failed"
    - classifyUserPromptText missing: exits 1 with "RED until 360-06"; cwdRoomsHomeVerdict missing when layer c runs: exits 1 with "RED until 360-07"
    - Attribution reproduces the SPEC baseline before any verdict is applied: pre unbound fires harness 33, human 2, other 0; any drift exits 1 with "attribution drift"
    - Layer h: post harness 0, post human 2, human runs with a harness verdict 0, other unchanged
    - Layer c: human post fires with cwd class 'outside' is 0; the anchor session (the session whose id's sha256 equals the frozen ANCHOR_SESSION_SHA256) has 0 human post fires; human fires with cwd class inside, ancestor or unresolvable are unchanged (all 0 in this snapshot)
    - Output lines carry counts, booleans and cwd classes only; sessions are labeled s1..s4 in filename order plus an anchor flag
  </behavior>
  <action>
    1. Create `tests/test-360-snapshot-replay.cjs` (CJS, Node built-ins, `--layer h|c|all`, default all). Snapshot dir = `process.env.MOS360_SNAPSHOT_DIR` or `<os.homedir()>/.cache/mindrian-dev/357-raw`; rooms home for layer c = `process.env.MOS360_REPLAY_ROOMS_HOME` or `<os.homedir()>/MindrianRooms`. Absent dir: SKIP line, exit 77. Present: `spawnSync('sha256sum', ['-c', '--quiet', 'SHA256SUMS'], {cwd: dir})`, non-zero exits 1.
    2. For each `*.jsonl` (sorted), parse records, index by uuid, and select attachment records with `attachment.hookEvent === 'UserPromptSubmit'` whose `attachment.command` names intent-classifier. Attribute each to its trigger by walking `parentUuid` over every attachment that is not `queued_command` until reaching a `user` record or a `queued_command` attachment (RESEARCH Finding 3). Ground truth without any lead literal: harness when the trigger's `origin.kind` is `peer` or `task-notification`, or `promptSource === 'system'`, or a queued attachment's `commandMode === 'task-notification'`; human when `origin.kind === 'human'`; else other. Trigger text: a user record's string content or first text block; a queued attachment's `prompt`. For stored peer triggers also evaluate the second-line form (text after the first newline); the verdict is harness only when BOTH forms are harness (A2, RESEARCH Finding 2). Pre fire (unbound-header metric) = the attachment's recorded stdout contains the unbound header `-- mindrianOS -- bind session -- select rooms --`; the all-outputs row (unbound header, off-scope header, no room matched, Intent mismatch, strict-mode override) is printed as information only with the Pitfall 5 caveat.
    3. Layer h: verdict from the real `require('../lib/hmi/turn-text.cjs').classifyUserPromptText`; post_h = pre && verdict !== 'harness'. Layer c: cwd class from the real `require('../lib/core/room-bind-picker-policy.cjs').cwdRoomsHomeVerdict(attachmentRecord.cwd, roomsHome)`; post_c = post_h && class !== 'outside' (the replay has no binding state, so it models the unbound picker only; R11 is covered by fixtures in 360-05, not here).
    4. ANCHOR_SESSION_SHA256 is a frozen hex constant: compute sha256 of the anchor session's file stem locally while authoring (the stem whose 8-char prefix the SPEC table labels "this dev session") and write only the hash; the replay hashes each stem at run time to set the anchor flag. The file must contain no session id or prefix.
    5. Assertions per the behavior list; print a small table (per class: runs, pre, post_h, post_c; per session s1..s4: human pre / post_c, anchor flag); exit 0 only when every assertion holds.
    6. Run `node tests/test-360-snapshot-replay.cjs </dev/null` (expected RED: classifyUserPromptText missing) and `MOS360_SNAPSHOT_DIR=/nonexistent node tests/test-360-snapshot-replay.cjs </dev/null` (expected exit 77). Record both in the SUMMARY.
    7. Commit both files: `git add tests/test-360-leads.cjs tests/test-360-snapshot-replay.cjs` then `git commit --only -m "test(360-03): lead legs and local snapshot replay, layers h and c (R5, R7, R10) - RED until 360-06/07" -- tests/test-360-leads.cjs tests/test-360-snapshot-replay.cjs`.
  </action>
  <verify>
    <automated>MOS360_SNAPSHOT_DIR=/nonexistent node tests/test-360-snapshot-replay.cjs </dev/null; test $? -eq 77</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 (absent snapshot SKIPs with 77)
    - With the snapshot present and before 360-06, `node tests/test-360-snapshot-replay.cjs --layer h </dev/null` exits 1 and prints `RED until 360-06`
    - `grep -c "<task-notification\|<agent-message\|<cross-session-message\|Cross-session idle notice\|Another Claude session" tests/test-360-snapshot-replay.cjs` is 0 (ground truth never uses lead literals; verdicts come from the real export)
    - `grep -cE '56924067|0208790f|21829408|0f86dd63' tests/test-360-snapshot-replay.cjs` is 0 and `grep -c "ANCHOR_SESSION_SHA256" tests/test-360-snapshot-replay.cjs` is at least 2
    - `grep -c "sha256sum" tests/test-360-snapshot-replay.cjs` is at least 1
    - `! grep -l "$(printf '\xe2\x80\x94')" tests/test-360-snapshot-replay.cjs`
    - `git log --format=%s -30 | grep -c '(360-03)'` is at least 1
  </acceptance_criteria>
  <done>The 2026-09-23 evidence is replayable on demand, locally only, with the SPEC R7 numbers and the R10 anchor assertion encoded, and the file leaks no id, path or prompt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local snapshot -> test output and committed test file | raw session content, ids, peer names and cwd paths must stay local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-360-04 | Information disclosure | tests/test-360-snapshot-replay.cjs | mitigate | reads the snapshot in place after sha256sum; prints counts, booleans and cwd classes only; the anchor is a sha256 constant, never an id; acceptance greps for ids and lead literals |
| T-360-03 | Spoofing (inverse) | a human prompt misread as harness | mitigate | replay asserts 0 human runs with a harness verdict; unit legs pin the mid-text quote, Skill body, image and slash-command leads as typed |
| T-360-10 | Repudiation | a replay that silently re-derives a different baseline | mitigate | attribution-drift assertion on the pre counts (33 / 2 / 0) before any verdict applies |
</threat_model>

<verification>
- `MOS360_SNAPSHOT_DIR=/nonexistent node tests/test-360-snapshot-replay.cjs </dev/null` exits 77
- `node tests/test-360-leads.cjs </dev/null` exits non-zero with `classifyUserPromptText missing` (RED recorded)
</verification>

<success_criteria>
The verdict functions have direct, id-free tests, and the snapshot replay encodes SPEC R7 (33 -> 0, 2 -> 2) and SPEC R10 (anchor session 0 human pickers) as exit codes.
</success_criteria>

<output>
Create `.planning/phases/360-room-bind-picker-fires-on-harness-turns-userpromptsubmit-f-8/360-03-SUMMARY.md` when done (`git add -f`, then `git commit --only -- <path>`). Record: RED outputs of both tests, whether the snapshot was present, the pre counts the attribution produced, and how ANCHOR_SESSION_SHA256 was derived (without the id).
</output>
