---
phase: 130.7-correlation-id-contract-dual-graph-ci-gates
plan: 03
type: execute
wave: 3
depends_on:
  - 130.7-01-correlation-id-contract-dual-graph-ci-gates
  - 130.7-02-correlation-id-contract-dual-graph-ci-gates
files_modified:
  - scripts/check-dual-graph-health.cjs
  - lib/memory/dual-graph-health.test.cjs
  - scripts/brain-derive-command.cjs
  - commands/brain-derive.md
  - lib/memory/brain-derive-curation.test.cjs
  - tests/run-all-130.7.sh
  - lib/memory/run-feynman-tests.cjs
autonomous: true
requirements:
  - ACCEPT-06  # the dual-graph health check fails the build on a REGRESSION vs baseline (report-only / baseline mode in 130.7; flips to absolute hard-fail post-132)
  - ACCEPT-07  # brain-boundary-scan passes on every new code path (Part 8)
  - CURATION-01  # /mos:brain-derive --review-anchors
  - CURATION-02  # /mos:brain-derive --orphan-census
  - CURATION-03  # /mos:brain-derive --cross-label-dups

must_haves:
  truths:
    - "The dual-graph CI health check runs in REPORT-ONLY / BASELINE mode in 130.7: it RECORDS the current live measured values as a baseline and exits non-zero ONLY on a REGRESSION (a metric getting worse than the recorded baseline), never on the absolute pre-existing debt. This is useful from day one (guards against backsliding) without bricking the v1.13.1 release train while Phase 132 still owns the cleanup."
    - "Metric M3 (no-fork) measures the RECOMMENDER OUTPUT, not raw-graph storage: the chain-recommender returns exactly ONE canonical target per logical query (the no-fork contract Plan 02 Task 1 Test 2 proves, the thing Phase 136's LazyGraph slot consumes). The raw-graph cross-label-dup COUNT is REPORTED as an informational baseline number only; it is NOT a hard-fail threshold in 130.7 (Phase 132 owns reducing it)."
    - "The CI gate fails-CLOSED on an INCONCLUSIVE read (Brain unreachable, malformed result, or no recorded baseline to compare against): inconclusive is a FAIL, never a false-green pass."
    - "A documented flip-to-blocking trigger exists: when Phase 132 completes (the debt is cleaned), the report-only baseline mode flips to absolute hard-fail thresholds. The absolute thresholds are carried as a POST-132 TARGET owner-decision needing sign-off, NOT claimed as verbatim from the curation audit."
    - "/mos:brain-derive --review-anchors surfaces the REVIEW_REQUIRED queue (curation-audit Q1)."
    - "/mos:brain-derive --orphan-census surfaces the all-label orphan scan (curation-audit section 7)."
    - "/mos:brain-derive --cross-label-dups surfaces same-name-different-label collisions (curation-audit section 13), keyed by correlation_id collisions across primary_label -- a REPORTING surface that feeds Phase 132 dedup, not a 130.7 hard-fail."
    - "Every new code path passes the Part 8 boundary check: the gate + curation surfaces query methodology nodes only; zero user content reaches Brain."
  artifacts:
    - path: "scripts/check-dual-graph-health.cjs"
      provides: "the dual-graph release-candidate CI gate in REPORT-ONLY/BASELINE mode: --check records/compares a baseline and exits non-zero on a REGRESSION (or inconclusive read), never on pre-existing absolute debt; a --flip-blocking path (post-132) is documented + stubbed for the absolute-threshold mode"
      min_lines: 90
      contains: "process.exit"
    - path: "lib/memory/dual-graph-health.test.cjs"
      provides: "planted-fixture tests proving the gate exits ZERO on pre-existing debt (no regression) and NON-ZERO only on a planted REGRESSION (a metric worse than baseline) + fails-closed on inconclusive; plus the M3 no-fork test asserting recommender output (one canonical per query), not raw-graph dup count"
      min_lines: 70
    - path: "scripts/brain-derive-command.cjs"
      provides: "three new curation modes: --review-anchors, --orphan-census, --cross-label-dups (additive to the existing single/--all/--cross-room/--dry-run argv parser)"
      contains: "review-anchors"
  key_links:
    - from: "scripts/check-dual-graph-health.cjs"
      to: "lib/core/brain-client.cjs"
      via: "read-only metric Cypher via query(); isAvailable() pre-flight; inconclusive -> fail-closed"
      pattern: "brain-client|isAvailable"
    - from: "scripts/check-dual-graph-health.cjs"
      to: "lib/brain/chain-recommender.cjs"
      via: "M3 calls recommendCanonicalTargets and asserts ONE canonical target per logical query (recommender output, not raw-graph storage)"
      pattern: "recommendCanonicalTargets|chain-recommender"
    - from: "scripts/brain-derive-command.cjs"
      to: "lib/core/correlation.cjs"
      via: "--cross-label-dups groups by correlation_id collision across primary_label (reporting only)"
      pattern: "correlation"
---

<objective>
Ship the measurement layer: (1) `scripts/check-dual-graph-health.cjs` -- the dual-graph release-candidate CI gate that ships in 130.7 as REPORT-ONLY / BASELINE mode (records the live measured values as a baseline and FAILS THE BUILD only on a REGRESSION versus that baseline, plus fails-CLOSED on an inconclusive read), with a documented flip-to-blocking trigger gated on Phase 132 completion; (2) the three `/mos:brain-derive` curation surfaces (`--review-anchors`, `--orphan-census`, `--cross-label-dups`) that pair with the gate so curation debt becomes visible within days, not months.

Why report-only/baseline (not absolute hard-fail) in 130.7: the absolute thresholds breach against the LIVE Brain today (REVIEW_REQUIRED ~23-28, ~50 cross-label dup groups, orphan mass far above any small per-label percent). Phase 132 runs AFTER 130.7 and is the cleanup that brings these under threshold. An absolute hard-fail gate shipped in 130.7 would be dead-on-arrival -- it would block every v1.13.1 release candidate until 132 lands. The baseline mode is useful from day one (it guards against backsliding -- any metric getting WORSE fails the build) without bricking the release train. When Phase 132 cleans the debt, the absolute thresholds (carried as a post-132 owner-decision target) can be turned on via the documented flip.

Provenance correction: the curation audit (2026-05-17) establishes that "the Brain has no health metric" and defines the QUERIES (Q1 REVIEW_REQUIRED, Q5 JTBD cohort, section 7 orphan census, section 13 cross-label dups). It does NOT specify the absolute numeric thresholds (< 3 / > 10 / > 5% / > 1). Those thresholds are an OWNER DECISION (post-132 target) needing sign-off, NOT "verbatim from the curation audit." The day-one gate derives its breach line from the MEASURED baseline, not from those un-ratified absolutes.

Output: the report-only/baseline CI gate (fail-closed on inconclusive, fail-on-regression, with a documented post-132 flip), the three curation modes (additive to the existing brain-derive argv), the asserting suites, and the wired aggregator + Feynman registration that closes Phase 130.7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-CONTEXT.md
@.planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-01-PLAN.md
@.planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-02-PLAN.md
@.planning/phases/132-dual-graph-correlation-hypergraph-reformat/132-CONTEXT.md
@scripts/brain-derive-command.cjs
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Extracted from the codebase. Executor: use these directly. -->

The CI-gate idiom is scripts/build-command-registry.cjs:
  - main() reads argv; `--check` regenerates in memory and process.exit(1) on mismatch, exit(0) on match.
  - registered in the pre-commit hook + lib/memory/run-feynman-tests.cjs as a tripwire.

lib/brain/chain-recommender.cjs (Plan 02): recommendCanonicalTargets({problemType?, currentFramework?, roomState?}) -> [{correlation_id, canonical_name, primary_label}] -- returns ONE canonical target per logical query (no fork). M3 measures THIS output.

scripts/brain-derive-command.cjs (CURRENT -- EXTEND its argv parser additively):
  parseArgv(argv) -> {section, all, crossRoom, dryRun}   (extend with reviewAnchors, orphanCensus, crossLabelDups)
  main() validates: either --all OR a section token must be present (the curation modes are standalone, like --all).
  Renders via SYM action-report helpers; NEVER throws (every failure becomes a structured result).

lib/core/brain-client.cjs: isAvailable() (sync bool); query(cypher, params) ($-bound; read-only metric Cypher here); _test.sanitizeCypherInput.

lib/core/correlation.cjs (Plan 01): computeCorrelationId(name, label); CORRELATION_VERSION.
lib/core/correlation-label-index.cjs (Plan 01): parseLabelIndex / serializeLabelIndex.

The 4 metrics' QUERIES (NOT their absolute thresholds) are defined in:
  ~/MindrianRooms/mindrianOS/methodology/2026-05-17-brain-curation-audit.md
  - Q1 REVIEW_REQUIRED count (review-anchors surface)
  - Q5 JTBD cohort framework count (cohort-size surface)
  - section 7 all-label orphan census (orphan-census surface)
  - section 13 cross-label duplicates (cross-label-dups surface)
  The audit says "the Brain has no health metric"; it does NOT specify the numeric thresholds < 3 / > 10 / > 5% / > 1. Those are a POST-132 owner-decision target, carried in this plan for the flip, NOT claimed as audit-verbatim.

Phase 132 (the cleanup that brings the debt under threshold) is @.planning/phases/132-dual-graph-correlation-hypergraph-reformat/132-CONTEXT.md -- it owns the ~50 cross-label dedup-group collapses + the orphan/REVIEW_REQUIRED reduction. 130.7 does NOT clean any of that; it only measures + guards against regression.

Aggregator: tests/run-all-130.7.sh (created Plan 01, extended Plan 02); Feynman block: lib/memory/run-feynman-tests.cjs Phase 130.7.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/check-dual-graph-health.cjs -- the report-only/baseline fail-closed CI gate (fails on regression, not on pre-existing debt)</name>
  <read_first>
    - scripts/build-command-registry.cjs (the --check -> process.exit(non-zero)-on-breach CI-gate idiom to mirror exactly)
    - lib/core/brain-client.cjs (isAvailable + query -- the read-only metric Cypher path; the inconclusive-result shapes to treat as fail-closed)
    - lib/brain/chain-recommender.cjs (Plan 02 -- recommendCanonicalTargets; M3 asserts its OUTPUT is one canonical target per query)
    - lib/core/correlation.cjs (Plan 01 -- the cross-label baseline count groups by correlation_id collision across primary_label, REPORTED informationally)
    - .planning/phases/132-dual-graph-correlation-hypergraph-reformat/132-CONTEXT.md (the debt 130.7 must NOT gate against absolutely yet; 132 owns the cleanup -> the post-132 flip target)
    - ~/MindrianRooms/mindrianOS/methodology/2026-05-17-brain-curation-audit.md sections 2 (Q1/Q5), 7 (orphan census), 13 (cross-label) -- the metric QUERIES (NOT the absolute thresholds; those are the post-132 owner-decision target)
  </read_first>
  <behavior>
    - Test 1 (records a baseline): on first run against a fixture metric set, the gate RECORDS the measured values as a baseline (a small JSON baseline artifact, e.g. data/dual-graph-health-baseline.json or a documented path) and exits 0. No prior baseline + a successful read is "baseline recorded", not a fail.
    - Test 2 (pre-existing debt does NOT fail): given a recorded baseline equal to the (high, debt-laden) live values, a re-read returning the SAME debt-laden values exits 0 -- the absolute pre-existing debt (REVIEW_REQUIRED ~25, ~50 cross-label dup groups, high orphan mass) never trips the gate in report-only mode.
    - Test 3 (regression FAILS): given a recorded baseline, a planted re-read where ANY metric is WORSE than baseline (e.g. REVIEW_REQUIRED count rose, or orphan-rate rose, or a NEW cross-label dup group appeared, or a JTBD cohort SHRANK below its baseline) exits NON-ZERO naming the regressed metric + the baseline-vs-current delta. This is the day-one value: backsliding is blocked.
    - Test 4 (improvement updates baseline, never fails): a re-read where metrics are BETTER than baseline exits 0 and ratchets the baseline DOWN (so the next run guards against regressing past the improvement). Improvement is never a failure.
    - Test 5 (M3 measures RECOMMENDER OUTPUT): the no-fork metric calls recommendCanonicalTargets over a fixture and asserts it returns exactly ONE canonical target per logical query (the Plan 02 contract). A raw-graph cross-label-dup COUNT is also computed and REPORTED as an informational baseline number, but a high raw dup count alone does NOT breach (Phase 132 owns reducing it). Assert: recommender returns 1 target for the cross-label-duplicate fixture name -> M3 PASS even though the raw graph still holds 2 un-deduped variants.
    - Test 6 (fail-closed on inconclusive): an inconclusive read (Brain unreachable / null / malformed result / baseline file unreadable when a comparison is required) exits NON-ZERO with reason 'inconclusive' -- NEVER exit 0 on a read or comparison it could not complete (no false-green).
    - Test 7 (flip-to-blocking is documented + stubbed): a --flip-blocking (or equivalent) mode exists that, when invoked, compares against the POST-132 ABSOLUTE thresholds (< 3 / > 10 / > 5% / > 1) carried as an owner-decision target; in 130.7 this mode is OFF by default and the test asserts the default mode is baseline/report-only. The absolute thresholds are tagged in-source as 'POST-132 TARGET -- owner sign-off required', NOT as audit-verbatim.
  </behavior>
  <action>
    Create scripts/check-dual-graph-health.cjs mirroring the build-command-registry.cjs --check idiom, but in REPORT-ONLY / BASELINE mode for 130.7. It computes 4 metrics:
      (M1) min framework count per JTBD cohort (Q5 query);
      (M2) REVIEW_REQUIRED node count (Q1 query);
      (M3) NO-FORK -- calls lib/brain/chain-recommender.cjs recommendCanonicalTargets and asserts it returns exactly ONE canonical target per logical query (the recommender-output contract Plan 02 proves). M3 does NOT gate on raw-graph cross-label-dup existence. SEPARATELY it computes a raw cross-label-dup group COUNT (group node names with > 1 distinct primary_label / correlation_id) and REPORTS it as an informational baseline number ONLY -- this raw count is tracked for regression (a NEW dup group appearing is a regression) but its absolute pre-existing value is never a hard-fail (Phase 132 owns reducing it);
      (M4) per-label orphan-rate (zero-edge nodes / total per label, section 7 query).
    Each metric is a named function returning {metric, value, direction ('lower_is_better'|'higher_is_better'), breached_on_regression:bool, reason}. Read-only Cypher through brain-client.query ($-bound, zero user content) for M1/M2/M4 and the raw M3 baseline count; M3's no-fork assertion runs recommendCanonicalTargets over the room/fixture.

    Baseline contract: the gate reads a baseline artifact (documented path, e.g. data/dual-graph-health-baseline.json). If absent + the read succeeds -> RECORD the baseline, exit 0. If present -> COMPARE: exit NON-ZERO (process.exit(1)) printing every metric that REGRESSED (got worse than baseline by direction); on improvement, ratchet the baseline toward the better value and exit 0; on no change, exit 0. Pre-existing absolute debt (baseline == current) is NEVER a failure.

    Fail-closed: isAvailable() false, OR any metric query returns null/malformed, OR a required baseline comparison cannot be completed -> process.exit(1) reason 'inconclusive' (a gate that cannot read MUST NOT pass).

    Flip-to-blocking (post-132): include a documented mode (e.g. --flip-blocking or MODE constant) that compares against the POST-132 ABSOLUTE thresholds (M1 < 3, M2 > 10, M3 raw dup groups > 1 per canonical name, M4 > 5%). Tag these in-source: `// POST-132 TARGET -- absolute thresholds; owner sign-off required before flipping the default; NOT verbatim from the 2026-05-17 curation audit (which defines the queries, not the numbers)`. In 130.7 this mode is OFF by default; the default --check is baseline/report-only.

    The gate accepts an injectable metric-reader + baseline-store (so tests plant fixture metric values + baselines without a live Brain). Part 8: every Cypher is read-only aggregate over methodology nodes; no SET/MERGE/DELETE; no user-content params (grep-gated). Add lib/memory/dual-graph-health.test.cjs with the seven behaviors using the injectable reader/store. Zero em-dashes; no fenced code blocks in the source.
  </action>
  <verify>
    <automated>node lib/memory/dual-graph-health.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - lib/memory/dual-graph-health.test.cjs passes all seven behaviors: records a baseline; pre-existing debt exits 0; a planted REGRESSION exits non-zero; an improvement ratchets the baseline + exits 0; M3 asserts recommender output (one canonical per query) PASSES even with raw graph un-deduped; inconclusive exits non-zero (fail-closed); the default mode is baseline/report-only with the absolute thresholds tagged as a post-132 target.
    - The default gate does NOT hard-fail on absolute pre-existing debt: `node scripts/check-dual-graph-health.cjs --check` with a planted baseline equal to debt-laden current values returns exit 0 (asserted inside the test via the injectable reader/store).
    - M3 keys on the recommender output, not raw storage: `grep -qE "recommendCanonicalTargets|chain-recommender" scripts/check-dual-graph-health.cjs`, and the raw cross-label count is labeled informational/baseline in-source (not a hard-fail threshold in the default mode).
    - Provenance is correct: `grep -n "verbatim" scripts/check-dual-graph-health.cjs` returns nothing claiming the thresholds are audit-verbatim; the absolute thresholds carry a 'POST-132 TARGET -- owner sign-off required' comment.
    - Part 8 clean: `grep -nE "\\b(SET|MERGE|DELETE|DETACH|CREATE)\\b" scripts/check-dual-graph-health.cjs` returns nothing (read-only); `grep -nE "artifact|MINTO|roomDir|MindrianRooms|transcript" scripts/check-dual-graph-health.cjs` returns nothing.
  </acceptance_criteria>
  <done>The dual-graph health gate ships in report-only/baseline mode: it records a baseline, fails the build on a REGRESSION (never on pre-existing absolute debt), fails-CLOSED on an inconclusive read, measures M3 as recommender output (not raw-graph dup count), and documents the post-132 flip-to-blocking with the absolute thresholds tagged as an un-ratified owner target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the three /mos:brain-derive curation surfaces (--review-anchors, --orphan-census, --cross-label-dups)</name>
  <read_first>
    - scripts/brain-derive-command.cjs (the FULL current module -- parseArgv {section, all, crossRoom, dryRun}, main() mode validation, the SYM action-report render helpers; extend additively)
    - commands/brain-derive.md (the command doc -- add the three new flags to the argument-hint + usage)
    - lib/core/brain-client.cjs (isAvailable + query -- the read-only curation Cypher path)
    - lib/core/correlation.cjs (Plan 01 -- --cross-label-dups groups by correlation_id collision)
    - ~/MindrianRooms/mindrianOS/methodology/2026-05-17-brain-curation-audit.md sections 2 (Q1 review-anchors), 7 (orphan census), 13 + 16 (cross-label dups) -- the exact queries + output shapes
  </read_first>
  <behavior>
    - Test 1 (argv): parseArgv recognizes --review-anchors, --orphan-census, --cross-label-dups as standalone modes (each sets its flag; each is valid WITHOUT a section token, like --all); the three are mutually exclusive with each other and with derive modes (a clear error if combined).
    - Test 2 (review-anchors output): the mode produces a structured digest of REVIEW_REQUIRED nodes (count + sample list) -- the curation-audit Q1 shape.
    - Test 3 (orphan-census output): the mode produces a per-label orphan count table (curation-audit section 7 shape).
    - Test 4 (cross-label-dups output): the mode produces same-name-different-label collision groups, grouped by canonical_name where > 1 distinct (name,label)/correlation_id exists -- the section 13 shape; each group lists the variant labels. This is a REPORTING surface that feeds Phase 132 dedup (it does NOT itself dedup or fail a build).
    - Test 5 (degrade + Part 8): with Brain unavailable each mode returns a structured 'brain_offline' result (never throws); the Cypher is read-only methodology-node-only (no user content, no writes).
  </behavior>
  <action>
    Extend scripts/brain-derive-command.cjs additively: parseArgv gains reviewAnchors / orphanCensus / crossLabelDups booleans; main() routes each to a dedicated handler BEFORE the existing single/--all derive path (each curation mode is standalone like --all -- it does not require a section). Each handler runs the curation-audit query via brain-client.query (read-only): --review-anchors = Q1 (REVIEW_REQUIRED nodes: count + sample); --orphan-census = section 7 (per-label zero-edge counts); --cross-label-dups = section 13 (group node names with > 1 distinct primary_label, surfaced as correlation_id-collision groups -- two variants of the same name under different labels produce two correlation_ids, and the surface flags the group for Phase 132 dedup). Render each via the existing SYM action-report helpers (mirror the existing dry-run summary render). Each handler NEVER throws and returns a structured 'brain_offline' result when isAvailable() is false. Update commands/brain-derive.md: add the three flags to the argument-hint + a usage line each. EVERY existing brain-derive mode (single / --all / --cross-room / --dry-run) stays byte-stable in behavior. Part 8: read-only methodology Cypher only; zero user content; zero writes. Add lib/memory/brain-derive-curation.test.cjs with the five behaviors. Zero em-dashes; no fenced code blocks in the source.
  </action>
  <verify>
    <automated>node lib/memory/brain-derive-curation.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - lib/memory/brain-derive-curation.test.cjs passes all five behaviors (argv recognition + the three output shapes + Brain-offline degrade).
    - The three flags are wired: `grep -q "review-anchors\|reviewAnchors" scripts/brain-derive-command.cjs && grep -q "orphan-census\|orphanCensus" scripts/brain-derive-command.cjs && grep -q "cross-label-dups\|crossLabelDups" scripts/brain-derive-command.cjs`.
    - commands/brain-derive.md documents the three flags: `grep -q "review-anchors" commands/brain-derive.md && grep -q "orphan-census" commands/brain-derive.md && grep -q "cross-label-dups" commands/brain-derive.md`.
    - Part 8 clean: `grep -nE "\\b(SET|MERGE|DELETE|DETACH|CREATE)\\b" scripts/brain-derive-command.cjs` shows no NEW write Cypher in the curation handlers (existing derive behavior unchanged); no user-content params.
    - The existing brain-derive modes still behave identically (run the existing brain-derive test suite if present in the regression gate).
  </acceptance_criteria>
  <done>The three curation surfaces exist as standalone /mos:brain-derive modes, produce the curation-audit digest shapes (reporting surfaces that feed Phase 132 dedup), degrade Brain-offline, are documented, and are Part 8 read-only-clean.</done>
</task>

<task type="auto">
  <name>Task 3: phase close-out -- Part 8 boundary sweep over all 130.7 code paths + aggregator + Feynman registration + zero-regression</name>
  <read_first>
    - .planning/research/2026-05-24-brain-boundary-scan-pr-gate-spec.md (the 5 forbidden-pattern classes the Part 8 sweep asserts: room paths, artifact bodies, transcripts, PII, proprietary numbers)
    - scripts/check-substrate.cjs (the shipped runtime tripwire -- run it across every 130.7-touched file)
    - tests/run-all-130.7.sh + lib/memory/run-feynman-tests.cjs (the aggregator + the additive Phase 130.7 block to finalize)
    - tests/run-all-130.sh + tests/test-navigation-acceptance.cjs (the zero-regression surfaces)
  </read_first>
  <action>
    Add a Part 8 boundary-sweep test (lib/memory/dual-graph-health.test.cjs or a dedicated assertion block) that scans every NEW/CHANGED 130.7 code path (lib/core/correlation.cjs, lib/core/correlation-label-index.cjs, scripts/backfill-correlation-id.cjs, lib/brain/chain-recommender.cjs, bin/local-chain-recommender.cjs, lib/core/navigation/spine-events.cjs, scripts/check-dual-graph-health.cjs, scripts/brain-derive-command.cjs) for the 5 forbidden-pattern classes from the boundary-scan spec (room paths ~/MindrianRooms, artifact/MINTO/ROOM.md bodies, Speaker-N transcript shapes, PII email/phone, financial $N proprietary patterns) and asserts zero matches in any outbound-Brain payload construction -- mirroring the Phase 90 5-tripwire forbidden-substring sweep. Note: the correlation_labels index is a LOCAL artifact (Plan 01); the sweep confirms it is never placed in an outbound-Brain payload. Finalize tests/run-all-130.7.sh so it runs ALL Phase 130.7 suites (correlation + correlation-label-index + chain-recommender + local-chain-recommender + spine-events-correlation + dual-graph-health + brain-derive-curation) and the Part 8 sweep, printing N/N. Finalize the additive Phase 130.7 block in lib/memory/run-feynman-tests.cjs registering every 130.7 suite, prior blocks byte-unchanged. Run scripts/check-substrate.cjs across the touched files. Run the prior-phase aggregators for zero-regression. Zero em-dashes.
  </action>
  <verify>
    <automated>bash tests/run-all-130.7.sh && bash tests/run-all-130.sh && node tests/test-navigation-acceptance.cjs && node scripts/check-substrate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - The Part 8 boundary sweep asserts zero forbidden-pattern matches across all eight 130.7 code paths (the 5 classes from the boundary-scan spec); the correlation_labels LOCAL index is confirmed never in an outbound-Brain payload.
    - `bash tests/run-all-130.7.sh` runs every 130.7 suite + the Part 8 sweep GREEN and exits non-zero on any failure.
    - The Phase 130.7 Feynman block is additive: `git diff lib/memory/run-feynman-tests.cjs` shows only added lines.
    - Zero regression: run-all-130.sh + test-navigation-acceptance.cjs pass; `node scripts/check-substrate.cjs` reports no new violation.
  </acceptance_criteria>
  <done>Every 130.7 code path is proven Part 8-clean by a forbidden-pattern sweep; the phase aggregator + CI registration are complete; zero regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| check-dual-graph-health -> Brain | Read-only metric Cypher crosses; an inconclusive read MUST fail-closed (a gate that passes on a read it could not complete is a false-green -- the worst failure for a release gate). |
| check-dual-graph-health -> baseline artifact | A LOCAL baseline JSON; the gate compares current-vs-baseline. A missing baseline when a comparison is required is inconclusive (fail-closed), not a silent pass. |
| brain-derive curation modes -> Brain | Read-only curation Cypher over methodology nodes only; zero user content, zero writes. |
| the 8 code paths -> any outbound Brain payload | The Part 8 forbidden-pattern sweep is the static-analysis floor proving no user-specific bytes are constructed for Brain anywhere in the phase; the correlation_labels index is LOCAL and confirmed never in an outbound payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130.7-03-01 | Repudiation / false-green | check-dual-graph-health.cjs exit contract | mitigate | The gate FAILS-CLOSED: isAvailable() false OR null/malformed result OR an incompletable baseline comparison -> process.exit(1) reason 'inconclusive'; Test 6 asserts a read it cannot complete NEVER exits 0. |
| T-130.7-03-02 | Repudiation (dead-on-arrival gate) | gate mode in 130.7 | mitigate | Report-only/baseline mode means the gate fails ONLY on regression vs the recorded baseline, never on pre-existing absolute debt (Tests 1-4). It is therefore live + useful from day one (blocks backsliding) without bricking the v1.13.1 release train while Phase 132 still owns the cleanup. The absolute hard-fail mode is gated behind a documented post-132 flip. |
| T-130.7-03-03 | Tampering (metric correctness / wrong layer) | M3 no-fork metric | mitigate | M3 measures the RECOMMENDER OUTPUT (recommendCanonicalTargets returns one canonical per query, Test 5), NOT raw-graph cross-label-dup existence. The raw dup count is reported informationally only, so M3 cannot falsely fire on the ~50-group debt Phase 132 owns. |
| T-130.7-03-04 | Tampering (false provenance) | absolute thresholds | mitigate | The absolute thresholds (< 3 / > 10 / > 5% / > 1) are tagged in-source as a POST-132 TARGET needing owner sign-off, NOT claimed as audit-verbatim; the grep gate asserts no 'verbatim from the curation audit' claim. The day-one breach line derives from the measured baseline. |
| T-130.7-03-05 | Information Disclosure | the gate + curation Cypher | mitigate | All Cypher is read-only aggregate over methodology nodes; grep gates prove zero SET/MERGE/DELETE in the new paths and zero user-content params. Canon Part 8. |
| T-130.7-03-06 | Information Disclosure (phase-wide) | all 8 130.7 code paths | mitigate | Task 3's Part 8 forbidden-pattern sweep (the boundary-scan spec's 5 classes) + scripts/check-substrate.cjs across every touched file is the phase-closing structural proof that no user data reaches Brain on any new path; the LOCAL correlation_labels index is confirmed never in an outbound payload. |
| T-130.7-03-07 | Denial of Service (regression) | brain-derive additive modes | mitigate | The three curation modes are additive standalone routes; existing single/--all/--cross-room/--dry-run behavior is byte-stable (asserted) and the curation handlers never throw (Brain-offline -> structured result). |
| T-130.7-03-SC | Tampering | npm installs | mitigate | Zero new packages (node built-ins + existing lib only). No install task; assert via `git diff package.json` showing no dependency change. |
</threat_model>

<verification>
- `node lib/memory/dual-graph-health.test.cjs` -- records a baseline; pre-existing debt exits 0; a planted regression exits non-zero; improvement ratchets baseline + exits 0; M3 asserts recommender output (one canonical per query) not raw dup count; inconclusive fails-closed; default mode is report-only with absolute thresholds tagged post-132.
- `node lib/memory/brain-derive-curation.test.cjs` -- argv + three output shapes + Brain-offline degrade.
- Part 8 sweep: zero forbidden-pattern matches across all 8 130.7 code paths; correlation_labels index confirmed LOCAL-only.
- `bash tests/run-all-130.7.sh` GREEN (all 130.7 suites); run-all-130.sh + test-navigation-acceptance.cjs GREEN (zero regression); `node scripts/check-substrate.cjs` clean.
- Read-only grep gates + no-user-content grep gates pass on the gate + curation scripts; no 'verbatim from the curation audit' threshold claim.
- `git diff package.json` shows no new dependency.
</verification>

<success_criteria>
- The dual-graph CI gate ships in report-only/baseline mode: fails the build on a REGRESSION vs baseline (live + useful day one), never on pre-existing absolute debt, and fails-CLOSED on an inconclusive read; the post-132 flip-to-blocking is documented with absolute thresholds tagged as an un-ratified owner target.
- M3 measures recommender output (one canonical target per query, the Phase-136 contract), with the raw cross-label-dup count reported informationally only (Phase 132 owns reducing it).
- The three /mos:brain-derive curation surfaces ship as reporting surfaces (feed Phase 132 dedup), produce the curation-audit digest shapes, and degrade Brain-offline.
- Every 130.7 code path is proven Part 8-clean.
- The phase aggregator + Feynman registration are complete; zero regression. Phase 130.7 is closed.
</success_criteria>

<output>
Create `.planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-03-SUMMARY.md` when done.
</output>
</content>
