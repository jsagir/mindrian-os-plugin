# Plan 130.7-02 SUMMARY -- chain-recommender canonical tuple + memory_event correlation refs

Status: COMPLETE (code + tests green at HEAD; closed out by the consolidating session 2026-06-01).

## What shipped

The chain-recommender and the local recommender now return ONE canonical
correlation tuple per query (no cross-label fork), and navigation memory_event
references carry the correlation_id hash scalar. Every surface REUSES the
Plan 01 `computeCorrelationId` chokepoint (lib/core/correlation.cjs) -- none
re-hashes -- so the LOCKED contract-of-record fix applied to correlation.cjs in
the 130.7-01 corrective conformance propagated through this entire plan by
construction.

## Commits (concurrent 130.7-02 session, then consolidated)

- 4438dcdc test(130.7-02): failing recommendCanonicalTargets no-fork canonical-tuple test (RED)
- a91acf63 feat(130.7-02): recommendCanonicalTargets -- one canonical tuple per query, no cross-label fork (GREEN)
- 38ca7a1e test(130.7-02): failing spine-events correlation_id reference test (RED)
- 4fd045e9 feat(130.7-02): thread correlation_id onto memory_event references (additive; EVENT_TYPES unchanged)
- b79e6b60 test(130.7-02): failing local-chain-recommender aggregate-by-correlation_id test (RED)
- (GREEN impl for bin/local-chain-recommender.cjs landed in the 130.7-01 corrective commit 3f0cd06c during consolidation)
- 33c9cf88 docs(130.7-02): cosmetic comment reword in local-chain-recommender (substrate allow-list note)

## Contract conformance (post-correction verification)

- lib/brain/chain-recommender.cjs:59 requires correlation.cjs; line 323 calls correlation.computeCorrelationId (REUSED, never re-hashed).
- bin/local-chain-recommender.cjs:51 requires correlation.cjs; line 131 calls computeCorrelationId.
- lib/core/navigation/spine-events.cjs:80 calls computeCorrelationId for the memory_event reference; EVENT_TYPES set unchanged (delta 0; additive payload field only).
- No live-code 'c1:' scheme remains (only comment/test-doc strings describing the replaced scheme).

## Verification

- tests/run-all-130.7.sh: 5/5 GREEN at HEAD (correlation, correlation-label-index, chain-recommender-canonical, local-chain-recommender, spine-events-correlation).
- Zero regression: run-all-130.sh 4/4; test-navigation-acceptance.cjs 1/1.
- Substrate guard + brain-boundary-scan passed on every commit (no --no-verify).
- ZERO live Brain writes in this plan.

## Acceptance criteria

- [x] chain-recommender.cjs returns one canonical {correlation_id, canonical_name, primary_label} per query (no duplicate-target fork)
- [x] bin/local-chain-recommender.cjs references correlation_id, not name
- [x] navigation.cjs memory_event references carry correlation_id (additive; EVENT_TYPES delta 0)
- [x] all surfaces reuse computeCorrelationId (the locked contract propagates by construction)

## Note (consolidation)

This plan was built by a concurrent session that stopped before writing this
SUMMARY or flipping the ROADMAP checkbox. The consolidating session verified
the code is green and contract-conformant, committed the trailing cosmetic
cleanup, and authored this close-out. Wave 3 (130.7-03: the 4-metric dual-graph
CI gate + 3 curation surfaces + Part 8 phase-wide sweep) is next.
