---
phase: 130.7-correlation-id-contract-dual-graph-ci-gates
plan: 01
subsystem: dual-graph-correlation-contract
tags: [correlation-id, embedding-independent, brain-backfill, canon-part-8, label-index, tdd]
requires:
  - Phase 122 workflow-layer (chain-recommender is the Plan 02 consumer)
  - Phase 90 brain-derivation-layer (BRAIN.md per-section convention the label index mirrors)
  - lib/core/brain-client.cjs (query / isAvailable / sanitizeCypherInput -- the only Brain door)
  - lib/core/framework-chain-composer.cjs (parseFrameworkChainSection -- the named-section idiom)
provides:
  - computeCorrelationId (the single deterministic, embedding-INDEPENDENT hashing chokepoint)
  - scripts/backfill-correlation-id.cjs (--dry-run preview; --execute gated, NOT run here)
  - serializeLabelIndex / parseLabelIndex / LABEL_INDEX_SECTION_KEY ('correlation_labels')
  - the LOCAL correlation_labels index data source Plan 02's no-fork canonical pick reads
affects:
  - Plan 02 (chain-recommender + local recommender consume computeCorrelationId + correlation_labels)
  - Plan 03 (CI gates measure cross-label-dups + orphan-rate against this contract)
  - Phase 131 (cascade edges land on canonical correlation_ids)
  - Phase 136 (LazyGraph slot relies on one canonical target per query)
tech-stack:
  added: []  # zero new npm deps -- node:crypto built-in only
  patterns:
    - name-based deterministic hash (NUL-delimited sha256, version-prefixed 'c1:')
    - embedding-independence by construction (pure 2-arg function, arity asserted)
    - dry-run/--execute gate mirrored from seed-brain-commands.cjs
    - named LOCAL section producer/parser mirrored from framework-chain-composer.cjs
key-files:
  created:
    - lib/core/correlation.cjs
    - lib/core/correlation-label-index.cjs
    - lib/memory/correlation.test.cjs       # RED in prior session (6420a297)
    - lib/memory/correlation-label-index.test.cjs
    - scripts/backfill-correlation-id.cjs
    - tests/run-all-130.7.sh
    - .planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-01-BACKFILL-DRYRUN.cypher
  modified:
    - lib/memory/run-feynman-tests.cjs       # additive Phase 130.7 block (14 insertions, 0 deletions)
decisions:
  - "correlation_id = 'c1:' + sha256(trim(name) + NUL + trim(label)); name-based, embedding-INDEPENDENT, safe under Phase 134 / 127.1 substrate swaps"
  - "case + internal whitespace PRESERVED (trim only); merging value-distinct names is Phase 132's dedup job, not the hash's -- the hash must SURFACE cross-label/near-dup problems the Phase 03 gates measure, not hide them"
  - "the correlation_labels index is written to a dedicated LOCAL sibling file (data/correlation-labels-index.txt) on --execute; the room loader folds it into roomState.brainSections.correlation_labels; chosen over a per-room BRAIN.md section so the index is one machine-parseable artifact the recommender reads synchronously"
  - "dry-run preview is built from a LOCAL fixture / built-in representative set (zero Brain touch, not even a read); --read-brain opt-in previews against a READ-ONLY live enumeration but still writes nothing"
  - "multi-label tie-break prefers the methodology label (Framework > Concept) per curation-audit section 13; :Archived excluded per section 9"
metrics:
  duration_min: 10
  completed: 2026-06-01
  tasks: 3
  files_created: 7
  files_modified: 1
  commits: 4   # 1 RED (prior session) + 3 this session
---

# Phase 130.7 Plan 01: Correlation-ID Contract + LOCAL Label Index Summary

Shipped the name-based, embedding-INDEPENDENT `computeCorrelationId` hashing chokepoint, the one-time idempotent `correlation_id` Brain backfill (dry-run only here; live `--execute` gated to the orchestrator), and the LOCAL `correlation_labels` index (`canonical_name -> {primary_label, edge_degree}`) that Plan 02's no-fork canonical pick reads as a real data source. ZERO live Brain writes; zero new npm deps.

## What shipped

- **`lib/core/correlation.cjs`** -- the single hashing chokepoint. `computeCorrelationId(canonicalName, primaryLabel)` returns `'c1:' + sha256(trim(name) + NUL + trim(label))`. Pure 2-arg function: no fs/net/db/embedding require (the forbidden-require grep returns nothing beyond `node:crypto`). The NUL delimiter prevents the `'AB'+'C'` vs `'A'+'BC'` boundary collision. Exports `computeCorrelationId`, `CORRELATION_ALGO` ('sha256'), `CORRELATION_VERSION` (1). The Brain backfill, the Plan 02 chain-recommender, and the local recommender all reuse this one function, so Brain and local agree BY CONSTRUCTION.

- **`scripts/backfill-correlation-id.cjs`** -- the one-time backfill. `--dry-run` (default) prints the idempotent MERGE/SET Cypher + the serialized `correlation_labels` index + a summary, then `exit(0)` WITHOUT touching the Brain. `--execute` requires `isAvailable()` (a write-capable `MINDRIAN_BRAIN_KEY`, the same gate `seed-brain-commands.cjs --execute` uses) and runs each write via `brain-client.query` with `$`-bound params; it was NOT run during this plan. The `cid` is computed via `lib/core/correlation.cjs` (not a second inline hash). The read Cypher excludes `:Archived` and selects only `n.name` + the label enum + `size((n)--())` degree -- the most-edged signal Plan 02 needs is produced here, not invented downstream. No destructive verbs; reversible via the `correlation_backfill_version` + `correlation_backfilled_at` stamp.

- **`lib/core/correlation-label-index.cjs`** -- `serializeLabelIndex(rows)` / `parseLabelIndex(sectionBody)` / `LABEL_INDEX_SECTION_KEY` ('correlation_labels'). Pure, total (returns `{}` on empty/malformed, never throws), escape-safe (pipe-bearing names round-trip), and deterministic (shuffled input serializes byte-identically). Mirrors `framework-chain-composer.parseFrameworkChainSection` so Plan 02 reads it via `roomState.brainSections.correlation_labels` exactly as it reads `framework_chain_predictions` today -- no live Cypher in the synchronous recommender path.

- **`lib/memory/correlation.test.cjs`** (RED authored in prior session 6420a297) -- 12 assertions GREEN: determinism, embedding-independence (arity-2 + ignored-3rd-arg, the key_invariant), label-sensitivity, trim normalization (case + inner whitespace preserved), 28-name REVIEW_REQUIRED collision fixture, golden version pin, fail-loud on non-string.

- **`lib/memory/correlation-label-index.test.cjs`** -- 11 assertions GREEN, including the cross-label-duplicate round-trip ('HEART Framework' under :Framework deg 8 AND :Product deg 2) that is exactly the disambiguation data Plan 02 consumes.

- **`tests/run-all-130.7.sh`** (executable) + additive Phase 130.7 block in `run-feynman-tests.cjs` (14 insertions, 0 deletions; every prior block byte-unchanged).

## ZERO live Brain writes

Per the HARD_BRAIN_WRITE_GATE: only `--dry-run` was run. No `--execute`, no `mcp__my-neo4j__write_neo4j_cypher`, no `brain_write`. The execute-only LOCAL artifact `data/correlation-labels-index.txt` is absent, confirming `--execute` never ran. The dry-run preview is built from a LOCAL built-in representative set (not even a read of the Brain). The exact MERGE/SET Cypher the dry-run prints is captured at `.planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-01-BACKFILL-DRYRUN.cypher` (git add -f'd) for the orchestrator to show the user before any `--execute` go-ahead.

## Deviations from Plan

### Auto-fixed Issues

None requiring code change. One acceptance-gate compliance fix during Task 2:

**1. [Rule 3 - Blocking issue] Comment text tripped the plan's own no-destructive-Cypher grep gate**
- **Found during:** Task 2 verification
- **Issue:** An explanatory comment in `backfill-correlation-id.cjs` listed the literal words "DELETE / REMOVE / DETACH / DROP" to document that none are used; this matched the plan's acceptance grep `grep -nE "\b(DELETE|REMOVE|DETACH|DROP)\b"` (which must return nothing) and the `!/DELETE|REMOVE|DETACH/` execute-gate node check.
- **Fix:** Reworded the comment to "No destructive verbs anywhere ... it never removes a node" without the literal keywords. The grep gate now returns clean and the execute-gate node check prints OK.
- **Files modified:** scripts/backfill-correlation-id.cjs (comment only; no logic change)
- **Commit:** 6ac8bb54

### Verification note (not a deviation)

The acceptance criterion "idempotency assertable via the dry-run printing identical Cypher on repeat" holds at the Cypher-body level: the MERGE/SET statement is byte-identical on every run. The inline `$params` comment carries a per-run `correlation_backfilled_at` ISO timestamp (an intentional audit stamp), so the params comment differs run-to-run while the executable Cypher and the computed `correlation_id` scalars are stable. Structural idempotency is unchanged: SET to a value-equal `cid` is a no-op.

## Threat surface scan

No new security-relevant surface beyond the plan's `<threat_model>`. The only egress surface (`backfill --execute` toward Brain) carries exactly `{name (handle), label (enum), cid (hex), version (int), ts (timestamp)}` as `$`-bound params; the `correlation_labels` index is a LOCAL artifact that never egresses. Part 8 grep gates on user-content surfaces (artifact/MINTO/meeting/transcript/roomDir/MindrianRooms) return nothing. No npm install task (zero new deps; `git diff package.json` empty).

## Self-Check: PASSED

- correlation.cjs FOUND; correlation-label-index.cjs FOUND; backfill-correlation-id.cjs FOUND; both test files FOUND; run-all-130.7.sh FOUND; 130.7-01-BACKFILL-DRYRUN.cypher FOUND.
- Commits 2555f38d, 6ac8bb54, bb08a014 (this session) + 6420a297 (prior RED) all present in git log.
- All commits made WITH hooks (no --no-verify): substrate guard + brain-boundary-scan passed on every commit.
