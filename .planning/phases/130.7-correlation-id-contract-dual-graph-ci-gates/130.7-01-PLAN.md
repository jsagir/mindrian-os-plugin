---
phase: 130.7-correlation-id-contract-dual-graph-ci-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/correlation.cjs
  - lib/memory/correlation.test.cjs
  - scripts/backfill-correlation-id.cjs
  - tests/run-all-130.7.sh
  - lib/memory/run-feynman-tests.cjs
autonomous: true
requirements:
  - ACCEPT-01  # every active teaching-graph node has a stable deterministic correlation_id
  - ACCEPT-05  # correlation_id documented as embedding-independent (safe under 134 / 127.1)
  - INVARIANT-EMBED  # the key_invariant: name-based, NOT vector-based; asserted by test
user_setup:
  - service: mindrian-brain
    why: "The one-time correlation_id Cypher backfill writes the correlation_id property onto teaching-graph methodology nodes via brain_query. Read-only by default (--dry-run prints the Cypher); --execute requires a brain_write/admin-plan MINDRIAN_BRAIN_KEY (same gate as scripts/seed-brain-commands.cjs)."
    env_vars:
      - name: MINDRIAN_BRAIN_KEY
        source: "Brain MCP admin/brain_write key (held by the maintainer; same key seed-brain-commands.cjs --execute uses)"

must_haves:
  truths:
    - "The same teaching-graph node yields the identical correlation_id across two independent runs (deterministic)."
    - "correlation_id is computed from (canonical_name, primary_label) ONLY; passing any embedding/vector input does not change the output (embedding-independent)."
    - "Running the backfill twice leaves the graph in the identical state (idempotent: MERGE/SET on a value-equal hash is a no-op)."
    - "The backfill writes ONLY the correlation_id scalar onto methodology nodes; it sends zero user content to the Brain (Canon Part 8)."
  artifacts:
    - path: "lib/core/correlation.cjs"
      provides: "computeCorrelationId(canonicalName, primaryLabel) -> stable hex string; the single hashing chokepoint reused by Brain backfill, chain-recommender, and the local recommender"
      min_lines: 40
      exports: ["computeCorrelationId", "CORRELATION_ALGO", "CORRELATION_VERSION"]
    - path: "scripts/backfill-correlation-id.cjs"
      provides: "one-time idempotent Cypher backfill: --dry-run prints the MERGE/SET Cypher; --execute runs it via brain-client.query; enum/scalar projection only"
      min_lines: 60
    - path: "lib/memory/correlation.test.cjs"
      provides: "the embedding-independence + determinism + collision-resistance asserting suite (the key_invariant test)"
      min_lines: 50
  key_links:
    - from: "scripts/backfill-correlation-id.cjs"
      to: "lib/core/correlation.cjs"
      via: "require + computeCorrelationId per node (so Brain and local agree by construction)"
      pattern: "computeCorrelationId"
    - from: "scripts/backfill-correlation-id.cjs"
      to: "lib/core/brain-client.cjs"
      via: "query() with $-bound params, gated on isAvailable() + --execute"
      pattern: "brain-client"
---

<objective>
Ship the correlation primitive: a single deterministic, embedding-INDEPENDENT hashing function and the one-time idempotent Cypher backfill that stamps `correlation_id` onto every active teaching-graph methodology node. This is Wave 1 -- the foundation Plan 02 (consumers) and Plan 03 (CI gates) both build on.

Purpose: 131's cascade edges and 136's LazyGraph slot must land on canonical targets. A name-based correlation_id (NOT vector-based) gives every node a stable handle that survives any Phase 134 (`@huggingface/transformers` embedding swap) or Phase 127.1 (Pinecone -> Neo4j HNSW) substrate change. Stating and TESTING this independence turns a latent 134 worry into a proven invariant (132-REVIEW-4.8 section 3).

Output: `lib/core/correlation.cjs` (the hashing chokepoint), `scripts/backfill-correlation-id.cjs` (the backfill), the embedding-independence asserting test, and the phase test aggregator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-CONTEXT.md
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Extracted from the codebase. Executor: use these directly, no exploration needed. -->

The Cypher-write-to-Brain analog is scripts/seed-brain-commands.cjs:
  - --dry-run / default prints generated Cypher to stdout (process.exit(0))
  - --execute requires MINDRIAN_BRAIN_KEY with brain_write plan
  - Cypher uses MERGE (n {name: '...'}) SET n.prop = '...' form

The Brain write path is lib/core/brain-client.cjs:
  - async query(cypher, params)  -> { records: [...] } | { error|text } | null
  - isAvailable()                -> sync boolean (the ONLY pre-flight)
  - sanitizeCypherInput(value)   -> whitelist-sanitized scalar (exposed on _test.sanitizeCypherInput)
  query() forwards params as $-bound params (never string-interpolated into the Cypher body).

The phase test-aggregator idiom is tests/run-all-130.sh (mirror it as tests/run-all-130.7.sh):
  - runs each tests/test-130.7-*.cjs / lib/memory/*.test.cjs, prints N/N, exits non-zero on any failure.
lib/memory/run-feynman-tests.cjs carries an ADDITIVE per-phase registration block; add a Phase 130.7 block, every prior block byte-unchanged.

Node built-in crypto is available: require('node:crypto').createHash('sha256').
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: correlation.cjs hashing chokepoint + embedding-independence asserting test</name>
  <read_first>
    - lib/core/brain-client.cjs (the sanitizeCypherInput whitelist + _test surface -- the scalar-projection discipline to mirror)
    - lib/core/navigation/spine-events.cjs (the deterministic dedupe_key derivation idiom -- a stable-hash-from-scalars precedent already shipped)
    - tests/run-all-130.sh + lib/memory/chain-recommender.test.cjs (the direct-CJS node:assert/strict test idiom to mirror)
    - docs/MINDRIAN-CANON.md Part 8 (enum/scalar projection only; no user content) and the 130.7-CONTEXT key_invariant block
  </read_first>
  <behavior>
    - Test 1 (determinism): computeCorrelationId('Beautiful Question Framework', 'Framework') called twice returns the identical string.
    - Test 2 (embedding-independence, the key_invariant): the function signature accepts ONLY (canonicalName, primaryLabel); a test constructs two calls for the SAME (name,label) where any hypothetical embedding/vector context differs (e.g. a third arg, or a global embedding fixture) and asserts the output is byte-identical -- proving the id keys on name+label, never on a vector. Assert computeCorrelationId.length (arity) === 2.
    - Test 3 (label-sensitivity): the SAME canonical_name under two different primary_labels (e.g. 'HEART Framework' as :Framework vs :Product) yields DIFFERENT correlation_ids -- so cross-label duplicates are distinguishable (this is the data the Plan 03 cross-label-dups metric needs).
    - Test 4 (normalization is explicit + minimal): define and assert the canonicalization rule (trim; the rule is documented in-source and tested). Do NOT lowercase silently if that would collapse intended-distinct names -- pick the rule, assert it, document why.
    - Test 5 (collision-resistance shape): over a fixture list of the ~28 REVIEW_REQUIRED framework names from the curation audit, all correlation_ids are distinct (no accidental collisions in the real name set).
    - Test 6 (stability across versions): CORRELATION_VERSION is exported and the test pins the algorithm output for one known input as a golden value, so a future accidental hash-input change fails loudly.
  </behavior>
  <action>
    Create lib/core/correlation.cjs exporting computeCorrelationId(canonicalName, primaryLabel), CORRELATION_ALGO ('sha256'), and CORRELATION_VERSION (integer, start at 1). Implementation: require('node:crypto'); the hash input is the canonicalized tuple joined by a fixed non-name separator (use a NUL or '' delimiter so 'AB'+'C' cannot collide with 'A'+'BC'); canonicalization is trim() on both inputs (document in-source that case + internal whitespace are preserved deliberately so 'HEART Framework' and 'Heart framework' stay distinct unless a later curation pass merges them -- the id reflects the node AS STORED, dedup is Phase 132's job not the hash's). Return the hex digest, optionally prefixed with the version (e.g. 'c1:' + hex) so a version bump is self-describing. The function takes EXACTLY two parameters and reads no global state, no embedding, no fs, no db, no network -- it is a pure function of its two string args (this is what makes it embedding-independent by construction). Throw a TypeError on non-string input (fail loud, never silently hash undefined). Add the test file lib/memory/correlation.test.cjs implementing the six behaviors above (direct-CJS, node:assert/strict, zero new deps), using the curation-audit REVIEW_REQUIRED names as the collision fixture. NO fenced code blocks anywhere in the module beyond what node requires. Zero em-dashes.
  </action>
  <verify>
    <automated>node lib/memory/correlation.test.cjs && node -e "const c=require('./lib/core/correlation.cjs'); const a=c.computeCorrelationId('X Framework','Framework'); const b=c.computeCorrelationId('X Framework','Framework'); if(a!==b) process.exit(1); if(c.computeCorrelationId('X Framework','Framework')===c.computeCorrelationId('X Framework','Product')) process.exit(2); if(c.computeCorrelationId.length!==2) process.exit(3); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - lib/memory/correlation.test.cjs passes all six behaviors (determinism, embedding-independence arity assert, label-sensitivity, normalization rule, collision-resistance over the real name set, golden-value version pin).
    - computeCorrelationId is a pure 2-arg function: `grep -nE "require\\(.*(fs|net|http|brain|sqlite|pinecone)" lib/core/correlation.cjs | grep -v "node:crypto"` returns nothing (no fs/net/db/embedding require).
    - The same node yields the same id across two runs; the same name under two labels yields two ids.
  </acceptance_criteria>
  <done>The embedding-independent hashing chokepoint exists, is pure, and the key_invariant has an explicit passing asserting test.</done>
</task>

<task type="auto">
  <name>Task 2: one-time idempotent correlation_id Cypher backfill (dry-run prints, --execute writes, Part 8 clean)</name>
  <read_first>
    - scripts/seed-brain-commands.cjs (the canonical Cypher-emit + --dry-run/--execute + MINDRIAN_BRAIN_KEY gate pattern to mirror)
    - lib/core/brain-client.cjs (query / isAvailable / sanitizeCypherInput -- the only Brain door)
    - lib/core/correlation.cjs (Task 1 -- the SAME hash the local side will use, so Brain and local agree by construction)
    - ~/MindrianRooms/mindrianOS/methodology/2026-05-17-brain-curation-audit.md (the active-node universe: ~155 Frameworks + the all-label orphan census; the backfill targets ACTIVE methodology nodes and MUST exclude :Archived per section 9)
  </read_first>
  <action>
    Create scripts/backfill-correlation-id.cjs. It reads the active teaching-graph node set via brain-client.query (a read Cypher: MATCH (n) WHERE NOT 'Archived' IN labels(n) AND n.name IS NOT NULL RETURN n.name AS canonical_name, head(labels(n)) AS primary_label -- naming primary_label as the FIRST/most-specific label, with a documented tie-break rule for multi-label nodes that prefers the methodology label, e.g. Framework over Concept, per the curation audit cross-label finding section 13). For each row it computes correlation_id = correlation.computeCorrelationId(canonical_name, primary_label) IN NODE.JS (not in Cypher -- the hash is owned by lib/core/correlation.cjs so the local recommender produces the identical value), then emits an idempotent write: MATCH (n {name: $name}) WHERE head(labels(n)) = $label SET n.correlation_id = $cid (MERGE-on-property semantics: SET to a value-equal scalar is a no-op, so a second run changes nothing). --dry-run (default) prints the generated Cypher + a count summary and process.exit(0) WITHOUT touching the Brain. --execute requires isAvailable() AND MINDRIAN_BRAIN_KEY; it runs each write via brain-client.query with $-bound params (name, label, cid all bound, never interpolated). The ONLY values that ever leave the box toward Brain are: a generic framework/methodology name (a handle, already public in the teaching graph), its label enum, and the computed hex scalar -- zero user artifact bodies, zero room content, zero PII (Canon Part 8). Add a stamping property correlation_backfill_version = correlation.CORRELATION_VERSION + a correlation_backfilled_at timestamp so the run is auditable + reversible (the curation-audit reversibility discipline). The script NEVER deletes, NEVER relabels, NEVER touches edges -- it only adds the scalar property. Print a final summary line (nodes scanned / ids computed / writes emitted-or-executed). Zero em-dashes; no fenced code blocks in the source beyond Cypher template strings.
  </action>
  <verify>
    <automated>node scripts/backfill-correlation-id.cjs --dry-run 2>&1 | grep -qE "correlation_id" && grep -qE "computeCorrelationId" scripts/backfill-correlation-id.cjs && node -e "const s=require('fs').readFileSync('scripts/backfill-correlation-id.cjs','utf8'); if(/--execute/.test(s) && /MINDRIAN_BRAIN_KEY/.test(s) && !/DELETE|REMOVE|DETACH/.test(s)) {console.log('OK')} else process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/backfill-correlation-id.cjs --dry-run` prints the MERGE/SET Cypher + summary and exits 0 WITHOUT any Brain write (isAvailable not required for dry-run; if it short-circuits when Brain is offline, dry-run still prints the would-be Cypher).
    - Part 8 egress is clean: the only $-bound params are {name (handle), label (enum), cid (hex), version, timestamp}. `grep -nE "artifact|MINTO|ROOM\\.md|meeting|transcript|roomDir|MindrianRooms" scripts/backfill-correlation-id.cjs` returns nothing (no user-content surface).
    - The backfill computes the id via lib/core/correlation.cjs (not a second inline hash) -- so the Brain value and the local value are identical BY CONSTRUCTION.
    - No destructive Cypher: `grep -nE "\\b(DELETE|REMOVE|DETACH|DROP)\\b" scripts/backfill-correlation-id.cjs` returns nothing.
    - Idempotency is documented + structural: SET to a value-equal scalar; a second run is a no-op (assert via the dry-run printing identical Cypher on repeat).
  </acceptance_criteria>
  <done>The one-time backfill exists, is idempotent + reversible + Part 8-clean, computes ids via the shared chokepoint, and dry-runs without touching the Brain.</done>
</task>

<task type="auto">
  <name>Task 3: phase test aggregator + Feynman-runner registration + zero-regression gate</name>
  <read_first>
    - tests/run-all-130.sh (the exact aggregator shape to mirror as tests/run-all-130.7.sh)
    - lib/memory/run-feynman-tests.cjs (the additive per-phase registration block pattern -- add Phase 130.7, leave every prior block byte-unchanged)
  </read_first>
  <action>
    Create tests/run-all-130.7.sh mirroring run-all-130.sh: it runs lib/memory/correlation.test.cjs (Task 1) and is structured so Plan 02 + Plan 03 append their suites later; prints N/N and exits non-zero on any failure; chmod +x. Add an additive Phase 130.7 registration block to lib/memory/run-feynman-tests.cjs registering the correlation suite in CI, with every existing block (including the Phase 130 block) byte-unchanged. Run the prior-phase aggregators (run-all-130.sh, the navigation acceptance test) to prove zero regression from the additive correlation.cjs + run-feynman-tests.cjs edits. Zero em-dashes.
  </action>
  <verify>
    <automated>bash tests/run-all-130.7.sh && bash tests/run-all-130.sh && node tests/test-navigation-acceptance.cjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/run-all-130.7.sh exists, is executable, runs the correlation suite GREEN, and exits non-zero on failure.
    - The Phase 130.7 block in run-feynman-tests.cjs is additive: `git diff lib/memory/run-feynman-tests.cjs` shows only added lines (no prior block modified).
    - Zero regression: run-all-130.sh and test-navigation-acceptance.cjs still pass.
  </acceptance_criteria>
  <done>The phase has a wired aggregator + CI registration; the correlation primitive runs green with zero regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backfill script -> Brain (Neo4j, remote) | The ONLY new egress surface in this plan. Methodology-node names + label enums + computed hex scalar cross here. Untrusted-direction concern is REVERSE: nothing user-specific must cross LOCAL -> BRAIN (Canon Part 8). |
| correlation.cjs -> callers | A pure function; the contract is determinism + embedding-independence. A non-deterministic or collision-prone hash would silently fork chains downstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130.7-01-01 | Information Disclosure | scripts/backfill-correlation-id.cjs Cypher payload | mitigate | Only {name (public handle), label enum, hex cid, version int, timestamp} are $-bound; Task 2 grep gate proves zero artifact/MINTO/ROOM.md/meeting/PII surface; the read Cypher excludes :Archived and selects only n.name + label (never properties carrying user content). Canon Part 8 LOCAL->BRAIN: NO. |
| T-130.7-01-02 | Tampering | lib/core/correlation.cjs hash inputs | mitigate | Pure 2-arg function (arity asserted), NUL-delimited input (no boundary-collision), golden-value version pin (Test 6) fails loudly on any accidental input-shape change; CORRELATION_VERSION makes a deliberate change self-describing. |
| T-130.7-01-03 | Tampering (determinism) | correlation_id stability under substrate swap | mitigate | The key_invariant test (Test 2) asserts the id keys on (name,label) ONLY and is byte-identical regardless of any embedding context -- so a Phase 134 / 127.1 vector swap cannot invalidate it. This IS the embedding-independence invariant the CONTEXT requires asserting. |
| T-130.7-01-04 | Elevation of Privilege | backfill --execute write gate | mitigate | --execute requires isAvailable() AND MINDRIAN_BRAIN_KEY (brain_write/admin plan); --dry-run (default) is read-emit-only and never writes. No destructive Cypher (DELETE/REMOVE/DETACH/DROP grep-gated to zero). Reversible by the correlation_backfill_version stamp. |
| T-130.7-01-SC | Tampering | npm installs | mitigate | This plan adds ZERO new packages (node:crypto built-in only). No package-manager install task, so the legitimacy gate is N/A; assert via `git diff package.json` showing no dependency change. |
</threat_model>

<verification>
- `node lib/memory/correlation.test.cjs` -- all six behaviors pass (determinism + embedding-independence arity + label-sensitivity + normalization + collision-resistance + golden version pin).
- `node scripts/backfill-correlation-id.cjs --dry-run` prints idempotent MERGE/SET Cypher + summary, exits 0, touches nothing.
- Part 8 grep gates on the backfill return zero user-content surface.
- `bash tests/run-all-130.7.sh` GREEN; prior-phase aggregators GREEN (zero regression).
- `git diff package.json` shows no new dependency.
</verification>

<success_criteria>
- correlation_id is deterministic, embedding-INDEPENDENT (asserted by test), and label-sensitive.
- The one-time backfill is idempotent, reversible, Part 8-clean, and computes ids via the shared chokepoint so Brain and local agree by construction.
- Phase aggregator + Feynman registration wired; zero regression.
</success_criteria>

<output>
Create `.planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-01-SUMMARY.md` when done.
</output>
