---
phase: 130.7-correlation-id-contract-dual-graph-ci-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 130.7-01-correlation-id-contract-dual-graph-ci-gates
files_modified:
  - lib/brain/chain-recommender.cjs
  - lib/memory/chain-recommender.test.cjs
  - bin/local-chain-recommender.cjs
  - lib/memory/local-chain-recommender.test.cjs
  - lib/core/navigation/spine-events.cjs
  - lib/memory/spine-events-correlation.test.cjs
  - tests/run-all-130.7.sh
autonomous: true
requirements:
  - ACCEPT-02  # chain-recommender returns one canonical {correlation_id, canonical_name, primary_label} per query (no fork) -- consumed by Phase 136 LazyGraph slot
  - ACCEPT-03  # bin/local-chain-recommender.cjs references correlation_id, not name
  - ACCEPT-04  # navigation.cjs memory_event references carry correlation_id
  - ACCEPT-07  # brain-boundary-scan passes on every new code path (Part 8)

must_haves:
  truths:
    - "chain-recommender returns EXACTLY ONE canonical target per query for a known cross-label-duplicate name (no duplicate-target fork) -- this is the contract Phase 136's LazyGraph slot consumes."
    - "Each returned element is a {correlation_id, canonical_name, primary_label} tuple, not a raw node and not a bare name string."
    - "bin/local-chain-recommender.cjs aggregates by correlation_id (not name) and resolves the identical canonical correlation_id the Brain side produces."
    - "navigation.cjs memory_event references carry correlation_id (not a raw framework name)."
    - "Every new/changed Brain-touching code path passes the Part 8 boundary check: only generic handles + enums + hashes cross to Brain."
  artifacts:
    - path: "lib/brain/chain-recommender.cjs"
      provides: "recommendCanonicalTargets (new tuple-returning API; dedups cross-label duplicates to one canonical target per query); recommendFrameworkChain stays byte-stable for back-compat"
      contains: "correlation_id"
    - path: "bin/local-chain-recommender.cjs"
      provides: "Tier-LOCAL recommender that walks memory_event aggregates by correlation_id and resolves the same canonical the Brain version does"
      min_lines: 60
      contains: "correlation_id"
    - path: "lib/core/navigation/spine-events.cjs"
      provides: "memory_event reference payloads carrying correlation_id"
      contains: "correlation_id"
  key_links:
    - from: "lib/brain/chain-recommender.cjs"
      to: "lib/core/correlation.cjs"
      via: "require + computeCorrelationId to tag each returned tuple with its canonical correlation_id"
      pattern: "computeCorrelationId|correlation_id"
    - from: "bin/local-chain-recommender.cjs"
      to: "lib/core/correlation.cjs"
      via: "the SAME hash, so the local aggregate key == the Brain node key"
      pattern: "correlation"
    - from: "bin/local-chain-recommender.cjs"
      to: "lib/core/navigation.cjs"
      via: "findRecentChanges / spine event reads through the chokepoint (never room-db directly)"
      pattern: "navigation"
---

<objective>
Make the correlation contract live on the consumers: (1) `chain-recommender.cjs` returns ONE canonical `{correlation_id, canonical_name, primary_label}` tuple per query with NO duplicate-target fork across cross-label duplicates; (2) the new `bin/local-chain-recommender.cjs` walks local memory_event aggregates BY correlation_id and resolves the identical canonical the Brain side produces; (3) navigation.cjs memory_event references carry correlation_id, not a raw name.

Purpose: 131 writes INFORMS/CONTRADICTS/SUPERSEDES cascade edges that must land on canonical targets, and 136's LazyGraph suggestion slot shows a suggestion per query -- if the teaching graph still forks across cross-label duplicates (HEART as :Framework AND :Product; Four Lenses with 12+ variant nodes -- curation audit section 13), 131's edges fork and 136 shows duplicate/wrong targets. The "one canonical target, no fork" is locked as the Phase-136-consumed contract (132-REVIEW-4.8 section 4).

Output: the extended recommender (back-compat preserved), the net-new local recommender, the correlation_id-threaded memory_event reference, and three asserting suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-CONTEXT.md
@.planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-01-PLAN.md
@lib/brain/chain-recommender.cjs
@lib/core/navigation/spine-events.cjs
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Extracted from the codebase. Executor: use these directly. -->

lib/core/correlation.cjs (Plan 01, Wave 1 -- SHIPPED before this plan runs):
  computeCorrelationId(canonicalName, primaryLabel) -> stable hex string (pure, embedding-independent)
  CORRELATION_VERSION (int)

lib/brain/chain-recommender.cjs (CURRENT, to EXTEND -- keep recommendFrameworkChain byte-stable):
  recommendFrameworkChain({problemType?, currentFramework?, roomState?}) -> string[]   (length 1..4, seed is element 0)
  DEFAULT_SEED, MAX_CHAIN_LENGTH, FEEDS_INTO_CYPHER (constants kept for the Part 8 test)
  Internals reusable: _pickSeed, _collectEdges, _walkChain (FEEDS_INTO walk via framework-chain-composer).
  Brain touch is ONLY brain-client.isAvailable() + the $seed-bound FEEDS_INTO question; NO command literals (122-02 grep guard).

lib/core/navigation.cjs chokepoint (the ONLY door to room.db):
  findRecentChanges(db, sinceEpochMs, opts) -> [{id, eventType, targetNodeId, createdAt, sourcePath, properties}]
  logSpineRead / logSuggestionSurfaced (roomDir-taking; memory_event writers)
  openRoomDbForCaller(roomDir) / closeRoomDbForCaller(db)  (caller-owned handle; close in finally; null when Tier 0)

lib/core/navigation/spine-events.cjs logSuggestionSurfaced: properties.commands is an array of {command, score}; this is the reference payload to extend with correlation_id.
lib/core/navigation/memory-events.cjs logEvent: references already use a `target_node_id` key; findRecentChanges projects it as targetNodeId.

lib/core/brain-client.cjs: isAvailable() (sync bool); query(cypher, params) ($-bound); _test.sanitizeCypherInput.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chain-recommender returns one canonical {correlation_id, canonical_name, primary_label} per query (no fork)</name>
  <read_first>
    - lib/brain/chain-recommender.cjs (the full current module -- recommendFrameworkChain + _pickSeed + _walkChain + the Part 8 boundary comment + FEEDS_INTO_CYPHER constant)
    - lib/memory/chain-recommender.test.cjs (the existing suite -- the new tuple API must not break it; recommendFrameworkChain stays byte-stable)
    - lib/core/correlation.cjs (Plan 01 -- computeCorrelationId)
    - ~/MindrianRooms/mindrianOS/methodology/2026-05-17-brain-curation-audit.md sections 13 + 22 (the cross-label duplicate reality: HEART :Framework + :Product; Four Lenses 12+ variants; the most-edged + primary-label-preferred canonical-pick heuristic)
  </read_first>
  <behavior>
    - Test 1 (tuple shape): recommendCanonicalTargets returns an array where every element is exactly {correlation_id, canonical_name, primary_label} (three string keys, no raw node guts, no bare strings).
    - Test 2 (no cross-label fork -- THE contract): for a seed whose name exists under two labels in the fixture (HEART Framework as :Framework AND :Product), the result contains EXACTLY ONE target for that logical work -- the canonical pick (most-edged, primary-label-preferred: Framework over Product). Assert array length 1 for that name AND its primary_label is the preferred one.
    - Test 3 (correlation_id matches the canonical tuple): the returned correlation_id equals computeCorrelationId(canonical_name, primary_label) for the CHOSEN canonical (agrees with the Brain backfill value by construction).
    - Test 4 (back-compat): recommendFrameworkChain is byte-stable -- the existing suite still passes; the new function is additive.
    - Test 5 (degrade): no edges / Brain unavailable / unknown seed -> a single-element array of the seed-as-canonical tuple (never throws, never null, never an empty fork).
    - Test 6 (no duplicate correlation_id): the output array has no two elements sharing a correlation_id.
  </behavior>
  <action>
    Add recommendCanonicalTargets({problemType?, currentFramework?, roomState?}) to lib/brain/chain-recommender.cjs WITHOUT altering recommendFrameworkChain (keep it and its constants byte-stable; the new function internally reuses _pickSeed / _collectEdges / _walkChain). The new function produces the same ordered framework-name walk, then PROJECTS each name to a canonical tuple: resolve the name's primary_label and dedup cross-label duplicates to ONE canonical node per logical work using the curation-audit heuristic -- prefer the methodology label (Framework over Product / Concept / DictionaryTerm), then most-edged. The label and edge-degree come from roomState's pre-parsed Brain projection (e.g. roomState.labelIndex / roomState.feedsIntoEdges), NOT a new live Cypher in this synchronous path (mirror how _collectEdges already takes pre-parsed edges so the function stays sync and Brain-touch-minimal). Tag each chosen canonical with correlation_id = correlation.computeCorrelationId(canonical_name, primary_label). Return the array deduped so no logical work appears twice -- dedup KEY is correlation_id (two cross-label variants collapse to the same chosen name+label and thus the same id). Degrade to a single seed tuple on any error; never throw, never null. The function names NO command (122-02 grep guard holds); the only Brain-bound value stays a framework handle (Part 8). Update lib/memory/chain-recommender.test.cjs with the six behaviors using a fixture that includes a cross-label-duplicate name. Zero em-dashes; no fenced code blocks in the source.
  </action>
  <verify>
    <automated>node lib/memory/chain-recommender.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - lib/memory/chain-recommender.test.cjs passes all six behaviors, including Test 2 (exactly one canonical target for the cross-label-duplicate fixture name) and Test 4 (recommendFrameworkChain byte-stable / existing assertions intact).
    - Both functions are exported: `grep -q "recommendCanonicalTargets" lib/brain/chain-recommender.cjs && grep -q "recommendFrameworkChain" lib/brain/chain-recommender.cjs`.
    - No command literals introduced (Part 8 / 122-02 guard): `grep -cE "['\"]mos:" lib/brain/chain-recommender.cjs` returns 0.
    - The returned correlation_id is computed via lib/core/correlation.cjs (not a second inline hash): `grep -q "correlation" lib/brain/chain-recommender.cjs`.
  </acceptance_criteria>
  <done>chain-recommender exposes a tuple-returning canonical API that emits exactly one target per logical work (no cross-label fork), agreeing with the Brain backfill correlation_id by construction; back-compat preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bin/local-chain-recommender.cjs -- Tier-LOCAL aggregates by correlation_id; resolves identical canonical to Brain</name>
  <read_first>
    - lib/brain/chain-recommender.cjs (Task 1 -- recommendCanonicalTargets is the Brain-side shape this local sibling must MATCH so a downstream consumer gets the same {correlation_id, canonical_name, primary_label} whether Brain is up or down)
    - lib/core/correlation.cjs (Plan 01 -- the SAME computeCorrelationId so the local aggregate key equals the Brain node key)
    - lib/core/navigation.cjs (findRecentChanges / openRoomDbForCaller / closeRoomDbForCaller -- the ONLY door to room.db; bin/ scripts are NOT substrate-allow-listed so MUST go through navigation, never require room-db.cjs directly)
    - scripts/suggest-next-command.cjs (a bin/scripts consumer of the recommender + the process.argv switch idiom + the Tier-0 graceful-degrade pattern to mirror)
  </read_first>
  <behavior>
    - Test 1 (aggregate-by-correlation_id): given a room with memory_event references carrying correlation_id (seeded), the local recommender groups/aggregates by correlation_id (NOT by name) -- two events whose references share a correlation_id count as one logical target; assert the aggregate key set is correlation_ids.
    - Test 2 (canonical agreement): for a (canonical_name, primary_label) present both locally and as a Brain canonical, bin/local-chain-recommender resolves the IDENTICAL correlation_id that recommendCanonicalTargets produces (assert equality against computeCorrelationId) -- the dual-graph join works by construction.
    - Test 3 (Tier-LOCAL / Brain-down): when Brain isAvailable() is false, the local recommender still returns canonical tuples from local aggregates alone (no Brain call), never throws.
    - Test 4 (chokepoint): the module reaches room.db ONLY through navigation.cjs (no direct room-db.cjs / node:sqlite require) -- source-grep assertion.
    - Test 5 (Tier-0): an absent room.db yields an empty result, not a crash.
  </behavior>
  <action>
    Create bin/local-chain-recommender.cjs. It opens room.db via navigation.openRoomDbForCaller(roomDir) (close in a finally via closeRoomDbForCaller), reads recent memory_event references via navigation.findRecentChanges, and AGGREGATES them by correlation_id (the reference payload carries correlation_id after Task 3; for events that predate the threading, fall back to computing correlation.computeCorrelationId(name, label) from the reference's name+label so old events still join). It returns the same {correlation_id, canonical_name, primary_label} tuple shape recommendCanonicalTargets returns, ranked by local aggregate weight (e.g. recency-weighted count). When Brain is available it MAY reconcile against the Brain canonical (the resolveByCorrelationId join is a value-equality check on correlation_id -- no fork because the id is canonical), but it NEVER requires Brain (Tier-LOCAL is the whole point). It reaches room.db ONLY through navigation.cjs (bin/ is not substrate-allow-listed; a direct room-db.cjs require would trip scripts/check-substrate.cjs). process.argv switch mirrors scripts/suggest-next-command.cjs; degrades to [] on absent room.db (Tier 0). Add lib/memory/local-chain-recommender.test.cjs with the five behaviors (direct-CJS, node:assert/strict, a seeded fixture room). Zero em-dashes; no fenced code blocks in the source.
  </action>
  <verify>
    <automated>node lib/memory/local-chain-recommender.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - lib/memory/local-chain-recommender.test.cjs passes all five behaviors, including Test 2 (local correlation_id equals the Brain-side value via computeCorrelationId) and Test 3 (works Brain-down).
    - Substrate-clean: `node scripts/check-substrate.cjs` reports no violation for bin/local-chain-recommender.cjs (it reaches room.db only via navigation.cjs). Confirm with `grep -E "require\\(.*room-db|node:sqlite" bin/local-chain-recommender.cjs` returning nothing.
    - The aggregate key is correlation_id, not name: `grep -q "correlation_id" bin/local-chain-recommender.cjs`.
  </acceptance_criteria>
  <done>The Tier-LOCAL recommender exists, aggregates by correlation_id, resolves the identical canonical the Brain side produces, works Brain-down, and never bypasses the navigation chokepoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: navigation.cjs memory_event references carry correlation_id (not raw name)</name>
  <read_first>
    - lib/core/navigation/spine-events.cjs (logSuggestionSurfaced -- properties.commands {command, score}; the reference write path to thread correlation_id through)
    - lib/core/navigation/memory-events.cjs (logEvent + the target_node_id reference key + findRecentChanges projection)
    - lib/core/correlation.cjs (Plan 01 -- computeCorrelationId for deriving the id when only name+label are known at write time)
    - lib/memory/run-feynman-tests.cjs Phase 129 block (the additive memory_event-emission test idiom + the dedupe_key TTL behavior to preserve)
  </read_first>
  <behavior>
    - Test 1 (reference carries correlation_id): a memory_event reference written through the spine path carries a correlation_id field on the reference payload (alongside or instead of the raw name); assert findRecentChanges surfaces it.
    - Test 2 (derivation): when the writer is given name+label but no precomputed id, the reference's correlation_id equals computeCorrelationId(name, label) -- so it joins to the same Brain/local canonical.
    - Test 3 (back-compat additive): EVENT_TYPES is unchanged (no net-new event type); the dedupe_key 60s TTL still works; every pre-existing spine emission test is byte-stable (the change is an ADDITIVE payload field, not a schema break).
    - Test 4 (Part 8): the correlation_id on the reference is a hash scalar -- no user content; a Brain-bound packet built from these references would carry only the hash + enum, never the raw name if the name is user-specific (for methodology references the name is a public handle, but the contract is correlation_id-first).
  </behavior>
  <action>
    Thread correlation_id through the memory_event reference write path in lib/core/navigation/spine-events.cjs (and any reference-emitting helper that today carries a raw framework name): add a correlation_id field to the reference payload, derived via correlation.computeCorrelationId(name, primary_label) when not supplied by the caller. This is ADDITIVE -- do NOT add a new EVENT_TYPE, do NOT break the dedupe_key TTL, do NOT change findRecentChanges' existing target_node_id projection (correlation_id rides alongside in properties). Keep every prior spine emission byte-stable in behavior (the reference simply gains a correlation_id key). Add lib/memory/spine-events-correlation.test.cjs with the four behaviors. The change routes through the navigation chokepoint only; spine-events.cjs is already allow-listed. Zero em-dashes; no fenced code blocks in the source.
  </action>
  <verify>
    <automated>node lib/memory/spine-events-correlation.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - lib/memory/spine-events-correlation.test.cjs passes all four behaviors, including Test 2 (reference correlation_id equals computeCorrelationId) and Test 3 (additive; no EVENT_TYPES delta, dedupe TTL intact).
    - EVENT_TYPES is unchanged: the existing Phase 129/130 event-type-count assertions still pass (run them in Task 4's regression gate).
    - The reference payload carries correlation_id: `grep -q "correlation_id" lib/core/navigation/spine-events.cjs`.
    - Substrate-clean: `node scripts/check-substrate.cjs` flags no violation on the edited spine-events.cjs.
  </acceptance_criteria>
  <done>memory_event references carry a correlation_id (derived via the shared chokepoint), additively, with no schema break and no chokepoint bypass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chain-recommender -> Brain | Only a generic framework handle ($seed) crosses; the canonical projection happens over pre-parsed roomState data, not a new live user-bearing Cypher. |
| bin/local-chain-recommender -> room.db | Crosses ONLY via the navigation.cjs chokepoint; bin/ is not substrate-allow-listed so a direct room-db.cjs require would be a Part 9 bypass. |
| spine-events reference payload -> (future) Brain packet | The reference now carries a correlation_id hash; the contract is correlation_id-first so a downstream Brain packet joins on the hash, never a user-specific raw string. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130.7-02-01 | Information Disclosure | chain-recommender recommendCanonicalTargets Brain touch | mitigate | The canonical projection runs over roomState pre-parsed edges/labels (no new live Cypher); the only Brain-bound value stays $seed (a framework handle); Task 1 grep gate proves zero command literals. Canon Part 8. |
| T-130.7-02-02 | Spoofing / Tampering (false join) | dual-graph correlation_id join (local vs Brain) | mitigate | Local and Brain compute the id from the SAME lib/core/correlation.cjs over the same (name,label); Test 2 in Tasks 1+2 asserts byte-equality, so the join cannot silently mismatch or fork. A collision-prone id would surface in Plan 01 Test 5 + Plan 03's cross-label-dups metric. |
| T-130.7-02-03 | Tampering (chokepoint bypass) | bin/local-chain-recommender.cjs room.db access | mitigate | Reaches room.db ONLY through navigation.openRoomDbForCaller/findRecentChanges; check-substrate.cjs flags any direct room-db.cjs/node:sqlite require; Task 2 grep gate + the substrate scan enforce it. Canon Part 9: navigation is the only door. |
| T-130.7-02-04 | Denial of Service (schema break) | spine-events reference payload change | mitigate | Strictly ADDITIVE: no new EVENT_TYPE, dedupe_key TTL preserved, target_node_id projection unchanged; Task 3 Test 3 + Task 4 regression gate (Phase 129/130 suites) prove byte-stable behavior. |
| T-130.7-02-05 | Information Disclosure (false-green Part 8) | the new Brain-touching code paths | mitigate | check-substrate.cjs runs over all three edited/new files; the chain-recommender command-literal grep + the spine reference being a hash scalar keep the boundary structurally clean. The dedicated Part 8 forbidden-pattern sweep is Plan 03's gate; here the per-file substrate scan is the floor. |
| T-130.7-02-SC | Tampering | npm installs | mitigate | Zero new packages (node built-ins + existing lib only). No install task; assert via `git diff package.json` showing no dependency change. |
</threat_model>

<verification>
- `node lib/memory/chain-recommender.test.cjs` -- six behaviors incl. no-fork + back-compat.
- `node lib/memory/local-chain-recommender.test.cjs` -- five behaviors incl. canonical agreement + Brain-down.
- `node lib/memory/spine-events-correlation.test.cjs` -- four behaviors incl. additive / no schema break.
- `node scripts/check-substrate.cjs` -- clean on chain-recommender.cjs, bin/local-chain-recommender.cjs, spine-events.cjs.
- Command-literal grep on chain-recommender returns 0; correlation_id present in all three artifacts.
- `git diff package.json` shows no new dependency.
</verification>

<success_criteria>
- One canonical {correlation_id, canonical_name, primary_label} per query, no cross-label fork (the Phase-136 contract).
- The Tier-LOCAL recommender aggregates by correlation_id and resolves the identical canonical the Brain side produces (the dual-graph join works by construction).
- memory_event references carry correlation_id, additively, with no schema break.
- Every new/changed Brain-touching path is Part 8 / Part 9 clean.
</success_criteria>

<output>
Append the three new suites to `tests/run-all-130.7.sh` (created in Plan 01) and the Phase 130.7 Feynman block; create `.planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-02-SUMMARY.md` when done.
</output>
