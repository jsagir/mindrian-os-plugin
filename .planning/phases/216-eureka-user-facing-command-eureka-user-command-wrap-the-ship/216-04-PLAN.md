---
phase: 216-eureka-user-command
plan: 04
type: execute
wave: 4
depends_on: [216-01, 216-02, 216-03]
files_modified:
  - tests/run-all-216.sh
autonomous: false
requirements: [216-R5, 216-R6]
tags: [eureka, phase-gate, regression, aggregator, human-verify]
must_haves:
  truths:
    - "bash tests/run-all-216.sh is a single PASS/FAIL/SKIP phase gate that runs hermetically offline with zero network (Canon Part 8)"
    - "The JHU/215 path is UNREGRESSED: run-all-215.sh and run-all-211.sh both green after every 216 change (this phase edited scripts/eureka-portfolio-report.cjs)"
    - "A human navigator has run /mos:eureka against a real room and confirmed the 4-zone render, the honest tail behavior, and the fire-and-return flow (the 215-style human-verify leg; real-room content never reaches a network judge)"
  artifacts:
    - path: "tests/run-all-216.sh"
      provides: "Phase 216 aggregator: substrate + e2e + six gates + 215/211 regression legs"
      contains: "eureka-offline-preload"
      min_lines: 50
  key_links:
    - from: "tests/run-all-216.sh"
      to: "tests/eureka-offline-preload.cjs"
      via: "NODE_OPTIONS --require (the zero-network guard inherited by every leg and child)"
      pattern: "NODE_OPTIONS"
    - from: "tests/run-all-216.sh"
      to: "tests/run-all-215.sh"
      via: "regression leg (216-R6)"
      pattern: "run-all-215"
---

<objective>
Close Phase 216 with the two-leg gate the 211/215 phases established: (1) a hermetic offline aggregator `tests/run-all-216.sh` covering the adapter, the room-mode runner, the dispatcher, the six governance gates, and the 215/211 regression legs; (2) the navigator's human spot-check of a real `/mos:eureka` run (real-room content is verified by a HUMAN, never a network judge - the 215 rule restated in 216-RESEARCH.md).

Covers D-01 through D-05 end to end at the acceptance level. 216-R5 (hermetic suite) and 216-R6 (211/215 unregressed).

Purpose: this phase touched the shipped runner that Phase 215's navigator-confirmed acceptance (DG-2 graph-canonical) rides on; the phase cannot be called done on unit tests alone.
Output: tests/run-all-216.sh green, run-all-215 + run-all-211 green, navigator verdict recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/216-eureka-user-facing-command-eureka-user-command-wrap-the-ship/216-CONTEXT.md
@.planning/phases/216-eureka-user-facing-command-eureka-user-command-wrap-the-ship/216-02-SUMMARY.md
@.planning/phases/216-eureka-user-facing-command-eureka-user-command-wrap-the-ship/216-03-SUMMARY.md
@tests/run-all-215.sh
@.planning/phases/215-eureka-portfolio-scale-fusion-eureka-portfolio-fusion/215-05-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 04 slice)

| Artifact | Kind | Symbol / shape |
|----------|------|----------------|
| `tests/run-all-216.sh` | new bash aggregator | `run` / `run_if` legs, PASS/FAIL/SKIP counters, exit nonzero on any FAIL (the run-all-215.sh scaffold) |

<tasks>

<task type="auto">
  <name>Task 1: tests/run-all-216.sh + the full verification sweep</name>
  <files>tests/run-all-216.sh</files>
  <read_first>
    - tests/run-all-215.sh (READ END TO END: the set -uo pipefail scaffold, the NODE_OPTIONS offline-preload export at line 28, run/run_if helpers, the PASS/FAIL/SKIP summary + exit contract - copy this structure verbatim)
    - tests/eureka-offline-preload.cjs (why the preload exists: transformers.js v4 only honors env.allowRemoteModels; the guard must wrap every leg AND its children - the dispatcher's detached-start leg spawns children)
  </read_first>
  <action>
    Create `tests/run-all-216.sh` (bash only, set -uo pipefail, NO em-dashes) on the run-all-215.sh scaffold, exporting `NODE_OPTIONS="${NODE_OPTIONS:-} --require ${ROOT}/tests/eureka-offline-preload.cjs"` before any leg. Legs in order:

    1. `run "216-01 room-native substrate" node tests/test-216-room-substrate.cjs`
    2. `run "216-02 room mode + dispatcher (offline e2e)" node tests/test-216-eureka-command.cjs`
    3. `run "216-03 gate: connector registry" node scripts/build-connector-registry.cjs --check`
    4. `run "216-03 gate: shape declaration (strict)" node scripts/check-shape-declaration.cjs --check --strict`
    5. `run "216-03 gate: command registration" node lib/core/command-registration-check.cjs`
    6. `run "216-03 gate: help coverage" node scripts/check-help-coverage.cjs`
    7. `run "216-03 gate: skill mirror" node scripts/build-skill-mirrors.cjs --check`
    8. `run "216-03 gate: render coverage" node scripts/check-render-coverage.cjs`
    9. `run_if "215 engine no-regression" "tests/run-all-215.sh" bash tests/run-all-215.sh` (216-R6: this phase edited scripts/eureka-portfolio-report.cjs; the 215 aggregator itself chains run-all-211 as its own leg 7, so 211 coverage rides along - do NOT add a duplicate standalone 211 leg)

    Header comment states the two-leg phase-gate contract (aggregator green + the Task 2 navigator spot-check) and restates the Part 8 egress rule, matching the run-all-215.sh header.

    Then execute the full sweep and record the counts in the SUMMARY:
    - `bash tests/run-all-216.sh` (must end FAIL=0)
    - `bash tests/run-all-211.sh` (belt-and-suspenders direct run; must end FAIL=0)
  </action>
  <verify>
    <automated>bash tests/run-all-216.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-216.sh` prints a final `Phase 216: PASS=N FAIL=0 SKIP=M` line and exits 0 (the 215 leg may PASS or SKIP-if-file-absent; FAIL anywhere is a hard stop)
    - `bash tests/run-all-211.sh` exits 0
    - `grep -c "eureka-offline-preload" tests/run-all-216.sh` >= 1 (zero-network guard wired)
    - `grep -c "run-all-215" tests/run-all-216.sh` >= 1 (regression leg present)
    - `bash -n tests/run-all-216.sh` exits 0 (syntax-clean)
  </acceptance_criteria>
  <done>One command gates the whole phase offline, the six governance gates are legs of it, and the 215/211 engines are proven unregressed after the runner edit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Navigator spot-check - /mos:eureka on a real room</name>
  <what-built>The full /mos:eureka surface: room-native substrate adapter (D-01), --pairs room runner mode, fire-and-return dispatcher (D-05), born-wired command with F.8 close (D-04), report-only rendering (D-03), all gates and regression suites green.</what-built>
  <how-to-verify>
    In a normal Claude Code session (not a dev shell):
    1. Open a session with a real room active (a typical tens-of-entries room is the RIGHT test - that is the cohort the JHU-scale engine never saw).
    2. Type `/mos:eureka`.
    3. Confirm the fire-and-return shape (D-05): the command confirms the scan started and either renders within a few polls or tells you where the report will land without blocking the conversation. First run may note the one-time local model download.
    4. When the report renders, check the 4-zone anatomy: header with the room name, ranked pairs table, the tail read, Opportunity Statements, an action footer with exactly one primary command. NO emoji, only the 12 glyphs.
    5. Tail honesty (D-01): if your room has fewer than 30 entries, the tail section must say "Not enough entries for a tail read" - not a crash, not a fabricated gem list. If 30+, a real tail or an honest empty-quadrant line.
    6. Statement honesty (D-03): every Opportunity Statement shows NOT YET BANKED (critic pending) - nothing claims to be banked or written to your graph.
    7. At the close, if a genuine fork exists you should get a native AskUserQuestion card (F.8), never an ASCII numbered menu.
    8. Run `/mos:eureka report` - the same report re-renders from disk without a rescan.
    Nothing from your room should have gone anywhere: the scan is fully local (Canon Part 8).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 216, or describe what rendered wrong (zone, tail verdict, statement state, or flow) and it becomes a gap-closure item.</resume-signal>
  <acceptance_criteria>
    - Navigator verdict recorded verbatim in the SUMMARY (the 215-05 precedent)
    - Any deviation the navigator names is logged as a gap for /gsd-plan-phase 216 --gaps rather than hot-patched past the checkpoint
  </acceptance_criteria>
  <read_first>
    - .planning/phases/215-eureka-portfolio-scale-fusion-eureka-portfolio-fusion/215-05-SUMMARY.md (how the 215 navigator verdict was captured - mirror that recording format)
  </read_first>
  <action>Present the how-to-verify steps to the navigator, pause for the spot-check, and record the verdict verbatim in the SUMMARY (the 215-05 recording format). Do not proceed, hot-patch, or re-run past this gate without the navigator's signal; a described defect becomes a gap-closure item for /gsd-plan-phase 216 --gaps.</action>
  <verify>
    <automated>MISSING - human checkpoint by design: real-room content never reaches an automated judge (Canon Part 8, the 215 rule); the automated legs live in Task 1's aggregator</automated>
    <human-check>Navigator runs /mos:eureka on a real room and confirms steps 1-8 of how-to-verify</human-check>
  </verify>
  <done>Navigator verdict recorded verbatim in 216-04-SUMMARY.md; either "approved" closes the phase or every named deviation is logged as a gap item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test legs -> network | forbidden; the offline preload guards every leg and child process |
| real room -> verification | human eyes only; no automated judge sees room content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-216-13 | Information disclosure | test suite reaching the network for model weights | mitigate | NODE_OPTIONS --require tests/eureka-offline-preload.cjs (allowRemoteModels=false) inherited by all legs and spawned children |
| T-216-14 | Information disclosure | real-room report content in the repo or a judge | mitigate | spot-check is human-only in the navigator's own session; no report file enters the repo (the 215 gitignore precedent) |
| T-216-SC | Tampering | package installs | accept | N/A - zero new packages this phase |
</threat_model>

<verification>
- `bash tests/run-all-216.sh` -> FAIL=0
- `bash tests/run-all-215.sh` -> FAIL=0 (216-R6)
- `bash tests/run-all-211.sh` -> FAIL=0 (216-R6)
- Navigator verdict recorded (Task 2)
</verification>

<success_criteria>
- 216-R5 satisfied: one hermetic aggregator gates the adapter, e2e, and all six governance gates with zero network
- 216-R6 satisfied: the 215 and 211 suites are green post-change, preserving the DG-1/DG-2-confirmed JHU behavior
- Phase 216 closes only after the human spot-check verdict is recorded
</success_criteria>

<output>
Create `.planning/phases/216-eureka-user-facing-command-eureka-user-command-wrap-the-ship/216-04-SUMMARY.md` when done
</output>
