---
phase: 216-eureka-user-command
plan: 03
type: execute
wave: 3
depends_on: [216-02]
files_modified:
  - commands/eureka.md
  - data/help-groups.json
  - skills/eureka/SKILL.md
  - data/connector-registry.json
autonomous: true
requirements: [216-R3, 216-R4]
tags: [eureka, command-authoring, cirs, hitl-shape, four-zone-ui]
must_haves:
  truths:
    - "/mos:eureka exists as a born-wired command surface: connector block enumerable by the registry gate, hitl_shape F.8 declared with hitl_why (D-02, D-04, Canon Part 11 CIRS)"
    - "The command body implements fire-and-return (D-05): start the scan, confirm running, render the report as the durable artifact, never hold the conversation hostage"
    - "The rendered output follows the 4-zone anatomy with the 12-glyph vocabulary, no emoji, footer never omitted, and fires AskUserQuestion at the close instead of drawing a selector box"
    - "All six governance gates named in 216-RESEARCH.md pass at author time"
    - "v1 is report-only (D-03): connector filing is none, the body never banks statements, pending critic state renders honestly"
  artifacts:
    - path: "commands/eureka.md"
      provides: "The born-wired /mos:eureka command surface"
      contains: "hitl_shape"
      min_lines: 120
    - path: "skills/eureka/SKILL.md"
      provides: "Generated byte-mirror (Windows registration parity)"
    - path: "data/help-groups.json"
      provides: "eureka in the intelligence-research family"
      contains: "eureka"
  key_links:
    - from: "commands/eureka.md"
      to: "scripts/eureka-command.cjs"
      via: "Bash invocation of the dispatcher subcommands in the body"
      pattern: "eureka-command\\.cjs"
    - from: "commands/eureka.md"
      to: "scripts/resolve-room"
      via: "pre-flight active-room resolution (the whitespace pattern, SEED-034 one door)"
      pattern: "resolve-room"
    - from: "commands/eureka.md"
      to: "data/connector-registry.json"
      via: "connector frontmatter enumerated by build-connector-registry.cjs"
      pattern: "sub_mode: eureka-portfolio"
---

<objective>
Author `commands/eureka.md` - the user-facing surface for the whole 211-216 arc - and clear the six governance gates so `/mos:eureka` is born WIRED (Canon Part 11 CIRS, R1/R2/R16).

Implements D-02 (name: /mos:eureka, collision-free), D-03 (report-only: connector `filing: none`, no banking anywhere in the body), D-04 (hitl_shape F.8 with the discovery-command precedent of find-connections/whitespace/find-analogies/opportunities), and D-05 (fire-and-return body flow over the Plan 02 dispatcher's start/status/report contract).

Purpose: today only a developer with a shell can run the Eureka engine; after this plan, any navigator on any of the three surfaces types /mos:eureka and gets the ranked + tail + Opportunity-Statement read against their OWN active room.
Output: commands/eureka.md, the eureka entry in data/help-groups.json, the generated skills/eureka/SKILL.md byte-mirror, the regenerated data/connector-registry.json, all six gates green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/216-eureka-user-facing-command-eureka-user-command-wrap-the-ship/216-CONTEXT.md
@.planning/phases/216-eureka-user-facing-command-eureka-user-command-wrap-the-ship/216-RESEARCH.md
@.planning/phases/216-eureka-user-facing-command-eureka-user-command-wrap-the-ship/216-02-SUMMARY.md
@commands/whitespace.md
@commands/opportunities.md
@skills/ui-system/SKILL.md
@docs/HITL-SHAPE-DECLARATION-CONTRACT.md
</context>

## Artifacts this phase produces (Plan 03 slice)

| Artifact | Kind | Symbol / shape |
|----------|------|----------------|
| `commands/eureka.md` | new command surface | frontmatter fields: `name: eureka`, `description`, `help_jtbd`, `argument-hint: "[run\|status\|report]"`, `body_shape: E (Action Report)`, `hitl_shape: "F.8"`, `hitl_why`, `serves_jtbd: ["connect-domains", "explore"]`, `teaching`, `ui_reference: skills/ui-system/SKILL.md`, `allowed-tools: [Read, Bash, AskUserQuestion]` |
| connector block | frontmatter contract | `connects_to_spine: true`, `sensor_triggers: [SENS-13]`, `reach_id: context_block`, `sub_mode: eureka-portfolio`, `framework: null`, `posture: hold`, `hierarchy_rank: 3`, `filing: none`, `plan_gated: false`, `web_scope: null`, `surface: F.1` |
| `<!-- mos:firing-block v2 -->` | body stamp | copied verbatim from commands/whitespace.md (shape-gate predicate 7) |
| `skills/eureka/SKILL.md` | generated file | byte-mirror via build-skill-mirrors.cjs --write (never hand-written) |
| help-groups entry | JSON edit | `"eureka"` appended to the `intelligence-research` group's commands array |
| `data/connector-registry.json` | regenerated file | includes the eureka connector tuple |

<tasks>

<task type="auto">
  <name>Task 1: Author commands/eureka.md (frontmatter + fire-and-return body + 4-zone render)</name>
  <files>commands/eureka.md</files>
  <read_first>
    - commands/whitespace.md (READ END TO END: the frontmatter axes, the verbatim firing-block v2 text at lines 36-48, the voice rules, pre-flight resolve-room + 3-line error, per-subcommand Step 1 dispatcher call + Step 2 4-zone render structure, Cross-Surface Adaptation section - eureka.md mirrors this architecture)
    - commands/opportunities.md (frontmatter lines 1-31: the `body_shape: E (Action Report)` literal, F.8 hitl_why phrasing, filing: none precedent)
    - skills/ui-system/SKILL.md (Sections 1-3: 4-zone anatomy, Shape E contract, the 12 glyphs, NO EMOJI, fire-the-card rule)
    - scripts/eureka-command.cjs (the Plan 02 subcommand + status.json + report-JSON contracts the body drives)
    - .planning/phases/216-eureka-user-facing-command-eureka-user-command-wrap-the-ship/216-RESEARCH.md (Pattern 2 - the distilled frontmatter; Anti-Patterns; the 4-zone render contract code block)
  </read_first>
  <action>
    Create `commands/eureka.md`. NO em-dashes, NO emoji, hyphens only.

    FRONTMATTER (exact fields, exact values - the two born-wired gates parse these):
    - name: eureka (D-02; lowercase-hyphen rule)
    - description: Surface cross-domain opportunity candidates from your room at portfolio scale
    - help_jtbd: "Rank cross-domain opportunity pairs and surface the weak-signal tail." (help-coverage gate rule (a))
    - argument-hint: "[run|status|report]"
    - body_shape: E (Action Report) (the opportunities.md literal form)
    - hitl_shape: "F.8" (D-04; quoted, the closed-vocabulary form)
    - hitl_why: "Ranked opportunity candidates are surfaced as an independent any-order set to review and act on in any order."
    - serves_jtbd: ["connect-domains", "explore"] (both exist in the help-groups jtbd taxonomy)
    - teaching: one sentence in the analog commands' voice - when you want to see where your room's ideas cross-pollinate into fundable opportunities, /mos:eureka ranks cross-domain pairs and flags the weak-signal tail the top-N sort buries
    - ui_reference: skills/ui-system/SKILL.md
    - allowed-tools: Read, Bash, AskUserQuestion (AskUserQuestion is REQUIRED - shape-gate predicate 8 for restrictive allowed-tools)
    - connector block (Canon Part 11 R1): connects_to_spine: true; sensor_triggers: [SENS-13] (the Phase 213 eureka detector in lib/core/sensors/sensor-eureka.cjs - the semantically-correct trigger; the gate does not restrict sensor ids, uniqueness is on the tuple); reach_id: context_block (one of the 6 frozen); sub_mode: eureka-portfolio (verified free 2026-07-10 - RE-VERIFY at author time, step below); framework: null (never invent a framework name); posture: hold (one of the 3 frozen); hierarchy_rank: 3; filing: none (D-03 report-only); plan_gated: false; web_scope: null; surface: F.1.

    BODY, in this order:
    1. The `<!-- mos:firing-block v2 -->` block copied VERBATIM from commands/whitespace.md lines 36-48 (predicate 7; all four analogs carry it byte-identically).
    2. `# /mos:eureka` + Larry voice rules block (copy the LOCKED voice-rules list from whitespace.md: 12 glyphs only, 3-line error pattern, no emoji, no "I" openers).
    3. Subcommand routing table: default/run -> full scan (Shape E), status -> scan state (Shape E), report -> re-render last report (Shape E). No-args behaves as run (the primary JTBD is the scan, unlike whitespace's multi-tool help).
    4. Pre-flight: resolve the active room via `bash "${CLAUDE_PLUGIN_ROOT}/scripts/resolve-room"` (fallback ./scripts/resolve-room when CLAUDE_PLUGIN_ROOT unset); on exit 1 render the 3-line error (What: No Data Room found / Why: no room under ~/MindrianRooms/ / Fix: /mos:new-project) and STOP. Never re-guess the room (SEED-034).
    5. Subcommand run - the D-05 fire-and-return flow, spelled as numbered steps for Larry:
       Step 1: `node "${CLAUDE_PLUGIN_ROOT}/scripts/eureka-command.cjs" ROOM_DIR start` - then TELL the navigator the scan is running and name the report path. Include the first-run honesty note: the first scan downloads the local embedding model once (model id only crosses the wire, no room bytes - Canon Part 8); offline machines degrade to an honest empty report, never a crash.
       Step 2: poll `node ... ROOM_DIR status` up to 3 times over ~15 seconds. If state becomes done, proceed to Step 3. If still running, STOP with: the scan is running in the background, the report will land at the named path, run /mos:eureka again in a minute to render it. Never busy-wait past the third poll (a large room must not hold the conversation hostage - D-05).
       Step 3: `node ... ROOM_DIR report` - read the JSON and render the 4-zone output (below).
    6. Subcommand status: run the status call, render one Shape E block translating state (none -> suggest run; running -> say so + path; failed -> 3-line error quoting status.json error; done -> suggest report).
    7. Subcommand report: skip straight to Step 3 rendering.
    8. The 4-zone render spec (Zone 2 reads the report JSON fields by name):
       - Zone 1 Header: `-- [Room Name] -- Eureka Portfolio Scan -- [Stage] --`
       - Zone 2 Body (Shape E): (a) Provenance one-liner: pairs mode, encoder, N pairs scored; (b) ranked table from json.ranked - rank, A title, B title, score, weak dims, tail flag glyph; (c) Tail read: when json.tail.insufficient_structure is true render EXACTLY the honest line - Not enough entries for a tail read (below the 30-entry floor); otherwise the tail items table; when suspect_noise flag it; (d) Opportunity Statements from json.statements - text plus honest state: pending renders as NOT YET BANKED (critic pending), never claimed as banked (D-03, Pitfall 4 invariant)
       - Zone 3 Intelligence Strip: max 3 signals, only when real (e.g. warn on suspect_noise, lightning on a tail-flagged statement); omit when none
       - Zone 4 Action Footer (NEVER omit): exactly one primary - `> /mos:eureka report` or the strongest follow-up; plus `> /mos:find-connections` and `> /mos:whitespace map` as grounded alternates
    9. The Decision Gate close (D-04): after rendering, IF there is a genuine unanswered fork (which candidate to pursue), FIRE AskUserQuestion in F.8 form - options built from the top statements plus the standard discovery next-steps; never draw a numbered box. Skip the card when the navigator already said what they want.
    10. Report-only note (D-03): one sentence stating statements are not written to the room graph in v1; banking is a later governed phase.
    11. Error Handling section (3-line patterns: no room, no report yet, scan failed, encoder unavailable) and Cross-Surface Adaptation section (CLI full power; Desktop conversational render of the same JSON; Cowork same as CLI, report file shareable via 00_Context/) - the whitespace.md closing structure.

    AUTHOR-TIME VERIFICATION STEP (inside this task, before declaring done): run `grep -rn "sub_mode: eureka-portfolio" commands/` and confirm exactly ONE hit (this file); run `grep -rn "name: eureka" commands/*.md` and confirm exactly one command claims the name. If either collides (another phase landed meanwhile - 216-RESEARCH.md validity note), pick `sub_mode: eureka-portfolio-216` and record the deviation in the SUMMARY.
  </action>
  <verify>
    <automated>node lib/core/command-registration-check.cjs && node scripts/check-shape-declaration.cjs --check --strict</automated>
  </verify>
  <acceptance_criteria>
    - `node lib/core/command-registration-check.cjs` exits 0 (no fence/tab/name faults)
    - `node scripts/check-shape-declaration.cjs --check --strict` exits 0 (F.8 + hitl_why + firing block + AskUserQuestion in allowed-tools all satisfied)
    - `grep -c "mos:firing-block v2" commands/eureka.md` outputs 1
    - `grep -c "AskUserQuestion" commands/eureka.md` >= 2 (allowed-tools + body)
    - `grep -c "filing: none" commands/eureka.md` outputs 1 (D-03)
    - `grep -c "Not enough entries for a tail read" commands/eureka.md` >= 1 (the directive phrase reaches the render spec)
    - `grep -c $'—' commands/eureka.md` outputs 0 (no em-dashes)
    - `grep -cE "(start|status|report)" commands/eureka.md` >= 6 (the fire-and-return flow drives all three dispatcher subcommands)
  </acceptance_criteria>
  <done>commands/eureka.md exists with gate-correct frontmatter, the verbatim firing block, the D-05 fire-and-return body over the Plan 02 dispatcher, the 4-zone Shape E render spec with honest tail/critic language, and both author-time gates pass strict.</done>
</task>

<task type="auto">
  <name>Task 2: Registration surfaces - help-groups, skill mirror, connector registry, all six gates</name>
  <files>data/help-groups.json, skills/eureka/SKILL.md, data/connector-registry.json</files>
  <read_first>
    - data/help-groups.json (the groups array shape; the intelligence-research group currently holds find-connections, whitespace, find-analogies among its commands - eureka joins this family per the phase's JTBD fit)
    - scripts/build-skill-mirrors.cjs (header only: the --write/--check semantics; the mirror is GENERATED, never hand-edited)
    - scripts/build-connector-registry.cjs (header + the --check semantics; regeneration writes data/connector-registry.json)
  </read_first>
  <action>
    1. Edit `data/help-groups.json`: append `"eureka"` to the `commands` array of the group with id `intelligence-research` (the family holding find-connections/whitespace/find-analogies, jtbd connect-domains + explore - the "portfolio-scale opportunity discovery" fit named in planning guidance). Touch nothing else in the file; do not renumber, do not hardcode counts anywhere (both gates enumerate from disk).
    2. Regenerate the skill mirror: `node scripts/build-skill-mirrors.cjs --write` (creates skills/eureka/SKILL.md as a byte-mirror; never author it by hand - a hand mirror drifts, Pitfall 4).
    3. Regenerate the connector registry: `node scripts/build-connector-registry.cjs` (writes data/connector-registry.json including the new (SENS-13, context_block, eureka-portfolio) tuple).
    4. Run ALL SIX gates and fix any fault at its source (the frontmatter or the JSON edit, never the gate):
       - `node scripts/build-connector-registry.cjs --check`
       - `node scripts/check-shape-declaration.cjs --check --strict`
       - `node lib/core/command-registration-check.cjs`
       - `node scripts/check-help-coverage.cjs`
       - `node scripts/build-skill-mirrors.cjs --check`
       - `node scripts/check-render-coverage.cjs`
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check && node scripts/check-shape-declaration.cjs --check --strict && node lib/core/command-registration-check.cjs && node scripts/check-help-coverage.cjs && node scripts/build-skill-mirrors.cjs --check && node scripts/check-render-coverage.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All six gate commands exit 0 in one chained invocation (216-R4)
    - `python3 -c "import json; d=json.load(open('data/help-groups.json')); g=[x for x in d['groups'] if x['id']=='intelligence-research'][0]; assert 'eureka' in g['commands']"` exits 0
    - `test -f skills/eureka/SKILL.md` exits 0 and the file was produced by the generator (git diff shows it born in the same commit as the --write run)
    - `grep -c "eureka-portfolio" data/connector-registry.json` >= 1 (the tuple is enumerated)
  </acceptance_criteria>
  <done>/mos:eureka is registered in a help family, mirrored for Windows registration parity, enumerated in the connector registry, and all six governance gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator input -> command body | subcommand text parsed by Larry per the routing table |
| command body -> dispatcher | Bash invocation with resolved ROOM_DIR only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-216-09 | Spoofing | a fifth room resolver | mitigate | pre-flight uses scripts/resolve-room only (SEED-034); the body never guesses paths |
| T-216-10 | Elevation of privilege | over-broad allowed-tools | mitigate | restrictive set: Read, Bash, AskUserQuestion only (no Write, no WebSearch) |
| T-216-11 | Information disclosure | report content spoken to a network judge | mitigate | render is local prose over local JSON; no MCP/web tools in allowed-tools |
| T-216-12 | Tampering | banking statements despite D-03 | mitigate | filing: none; body states report-only; render-coverage + shape gates pin the single governed door |
| T-216-SC | Tampering | package installs | accept | N/A - zero new packages this phase |
</threat_model>

<verification>
- All six gates green (the Task 2 chained command)
- `node tests/test-216-eureka-command.cjs` still green (no dispatcher drift while authoring)
- `grep -c $'—' commands/eureka.md` outputs 0; no emoji anywhere in the new surface
</verification>

<success_criteria>
- 216-R3 satisfied: the command surface exists, born wired, F.8-declared, fire-and-return, 4-zone-rendered, report-only
- 216-R4 satisfied: connector-registry --check, shape-declaration --check --strict, command-registration-check, help-coverage, skill-mirror --check, render-coverage all exit 0
</success_criteria>

<output>
Create `.planning/phases/216-eureka-user-facing-command-eureka-user-command-wrap-the-ship/216-03-SUMMARY.md` when done
</output>
