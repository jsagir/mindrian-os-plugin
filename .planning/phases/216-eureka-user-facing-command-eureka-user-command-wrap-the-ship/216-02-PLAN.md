---
phase: 216-eureka-user-command
plan: 02
type: execute
wave: 2
depends_on: [216-01]
files_modified:
  - scripts/eureka-portfolio-report.cjs
  - scripts/eureka-command.cjs
  - tests/test-216-eureka-command.cjs
autonomous: true
requirements: [216-R2]
tags: [eureka, runner, dispatcher, room-mode, fire-and-return]
must_haves:
  truths:
    - "The shipped runner gains an additive --pairs room mode that composes the Plan 01 adapter with the SAME four Wave-1 modules; graph and full modes behave byte-identically to Phase 215 (D-01, additive not replacement)"
    - "Room mode never reads any idea-graph file: a nonexistent --graph path plus --pairs room still exits 0 (the loadGraph hard-throw is resolved BY the adapter, per the navigator directive)"
    - "A sub-30-entry room produces a report whose tail section says 'Not enough entries for a tail read' and the run exits 0 (graceful degradation, D-01)"
    - "A dispatcher exists that resolves substrate per room (explicit --graph > room-local idea-graph > room-native), runs the engine, and supports fire-and-return via start/status/report subcommands (D-05)"
    - "The JHU fixture evals/eureka/jhtv-idea-graph.json is NEVER the default on a user room (216-RESEARCH.md anti-pattern)"
  artifacts:
    - path: "scripts/eureka-portfolio-report.cjs"
      provides: "--pairs room mode composing buildRoomNativeSubstrate"
      contains: "buildRoomNativeSubstrate"
    - path: "scripts/eureka-command.cjs"
      provides: "Thin /mos:eureka dispatcher: run|start|status|report|help subcommands"
      exports: ["main"]
      min_lines: 120
    - path: "tests/test-216-eureka-command.cjs"
      provides: "Hermetic offline e2e for room mode + dispatcher"
      min_lines: 120
  key_links:
    - from: "scripts/eureka-portfolio-report.cjs"
      to: "lib/core/eureka/room-native-substrate.cjs"
      via: "require + buildRoomNativeSubstrate(db, { canonicalId: catalogId }) in the room-mode branch"
      pattern: "room-native-substrate"
    - from: "scripts/eureka-command.cjs"
      to: "scripts/eureka-portfolio-report.cjs"
      via: "require + await runner.main([...]) in-process"
      pattern: "eureka-portfolio-report"
    - from: "scripts/eureka-command.cjs"
      to: "<ROOM_DIR>/.mindrian/eureka/status.json"
      via: "status file written at run start and finish (fire-and-return state)"
      pattern: "status\\.json"
---

<objective>
Wire the Plan 01 adapter into the shipped runner as an ADDITIVE `--pairs room` mode, and author the thin command-facing dispatcher `scripts/eureka-command.cjs` that Plan 03's `commands/eureka.md` body will shell.

Implements D-01 (room-native substrate is the command path when no idea-graph exists; the CSV-derived graph/full path from 211-215 is UNCHANGED - the DG-2 "graph is canonical" navigator call for the JHU flow stays intact) and D-05 (fire-and-return: the dispatcher can kick off a scan detached, report status, and hand back the rendered report as the durable artifact). Report-only per D-03: nothing here writes nodes/edges/memory_event; the runner already confines writes to derived eureka_* tables + report files (Canon Part 9).

Purpose: after this plan, a normal room with only room.db produces the SAME kind of ranked, tail-flagged, Opportunity-Statement report the JHU acceptance run proved out in Phase 215 - from one dispatcher call, no dev flags.
Output: room mode in the runner, the dispatcher, and a hermetic offline e2e covering both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/216-eureka-user-facing-command-eureka-user-command-wrap-the-ship/216-CONTEXT.md
@.planning/phases/216-eureka-user-facing-command-eureka-user-command-wrap-the-ship/216-01-SUMMARY.md
@scripts/eureka-portfolio-report.cjs
@lib/core/eureka/room-native-substrate.cjs
@tests/test-215-portfolio-report.cjs
@scripts/whitespace-command.cjs
@lib/core/resolve-active-room.cjs
</context>

## Artifacts this phase produces (Plan 02 slice)

| Artifact | Kind | Symbol / shape |
|----------|------|----------------|
| `--pairs room` | new CLI flag value on `scripts/eureka-portfolio-report.cjs` | third pairs mode: `graph|full|room` |
| room-mode provenance | data contract | `pairs_mode: 'room'`; `growth_proxy` overridden to `'created_at-recency (room-native)'`; `honest_nouns` from adapter meta; Graph provenance row reads `(room-native: no idea-graph)` |
| tail degrade string | report contract | literal `Not enough entries for a tail read` in the room-mode markdown when `insufficient_structure` is true |
| `scripts/eureka-command.cjs` | new CJS dispatcher | `main(argv)` exported; argv contract `ROOM_DIR SUBCOMMAND [--top N] [--offline] [--graph <path>]`; subcommands `run`, `start`, `status`, `report`, `help` |
| report file paths | filesystem contract | `<ROOM_DIR>/.mindrian/eureka/portfolio-report.md`, `<ROOM_DIR>/.mindrian/eureka/portfolio-report.json`, `<ROOM_DIR>/.mindrian/eureka/status.json` |
| status.json shape | data contract | `{ state: 'running'\|'done'\|'failed', started_at, finished_at?, pid, out, json, error? }` |
| `tests/test-216-eureka-command.cjs` | new test script | hermetic e2e: room-mode runner legs + dispatcher legs |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: --pairs room mode in the shipped runner</name>
  <files>scripts/eureka-portfolio-report.cjs, tests/test-216-eureka-command.cjs</files>
  <read_first>
    - scripts/eureka-portfolio-report.cjs (READ END TO END before editing: parseArgv 92-122, HELP 124-141, loadGraph 220-310, main 526-834 - especially the loadGraph call at 555, the pair-enumeration branch at 600-619, the tail axes at 666-675, provenance at 737-761, renderReport tail section at 419-451)
    - lib/core/eureka/room-native-substrate.cjs (the Plan 01 contract: buildRoomNativeSubstrate(db, { canonicalId }) -> {meta, techMap, convergesPairs})
    - .planning/phases/215-eureka-portfolio-scale-fusion-eureka-portfolio-fusion/215-05-SUMMARY.md (DG-2: graph mode is the canonical JHU acceptance substrate - this task must not alter graph/full behavior in any way)
    - tests/test-215-portfolio-report.cjs (fixture patterns to copy; run it before AND after the edit)
  </read_first>
  <behavior>
    - Test 1: main(['--db', fixtureRoomDir, '--pairs', 'room', '--offline', '--top', '25', '--out', md, '--json', json]) exits 0 on a 36-node two-domain fixture room WITH typed edges and NO graph file anywhere; JSON provenance has pairs_mode 'room', ranked.length >= 1, tail.insufficient_structure === false
    - Test 2: the same call with '--graph', '/nonexistent/path.json' STILL exits 0 (room mode never touches the graph path - the D-01 consequence)
    - Test 3: an edge between two SAME-type, SAME-domain nodes still appears among scored pairs in room mode (the union rule: the room's own citations always score, the DG-2 cited-signal spirit), while cross-boundary enumeration covers the rest
    - Test 4: a 10-node fixture room in room mode exits 0, JSON tail.insufficient_structure === true, and the markdown contains the literal string 'Not enough entries for a tail read'
    - Test 5: room-mode JSON provenance growth proxy value is 'created_at-recency (room-native)' and the markdown Graph provenance row contains '(room-native: no idea-graph)'
    - Test 6 (no-regression pin): node tests/test-215-portfolio-report.cjs still exits green after the edit (graph + full modes untouched)
  </behavior>
  <action>
    Edit `scripts/eureka-portfolio-report.cjs` - ADDITIVE changes only, per D-01 ("an ADDITIONAL substrate for rooms without an idea-graph, not a replacement of the existing graph/full modes"):

    1. parseArgv: change the normalization line 119 to `if (opts.pairs !== 'full' && opts.pairs !== 'room') opts.pairs = 'graph';`. Update HELP's --pairs line to name all three modes, with room described as "room = room-native substrate from room.db nodes+edges (no idea-graph needed)".
    2. Require the adapter at the top with the other lib requires: `const roomNative = require(path.join(REPO_ROOT, 'lib/core/eureka/room-native-substrate.cjs'));`.
    3. main() substrate branch: when `opts.pairs === 'room'`, do NOT call loadGraph and do NOT read graphPath at all (Test 2 pins this). Instead, after `db = openRoomDb(...)` at line 561, call `roomNative.buildRoomNativeSubstrate(db, { canonicalId: catalogId })` and use its techMap/convergesPairs/meta where the graph-mode path used loadGraph's. Restructure minimally: hoist `let graph/techMap/convergesPairs` declarations above the openRoomDb call, populate them from loadGraph BEFORE the open (graph/full modes, exactly as today) or from the adapter AFTER the open (room mode). The graph/full code path must remain semantically identical - same call order, same values.
    4. Room-mode pair enumeration (new branch beside the existing two at lines 600-619): pairsToScore = the UNION of (a) the existing full-mode cross-boundary rule over `indexed` values (copy the loop verbatim - differ in root OR differ in type), and (b) the adapter's convergesPairs where BOTH endpoints are in `indexed`. Dedupe by the unordered `a < b ? a+'|'+b : b+'|'+a` key, edge pairs first so their shared_problems survive the dedupe. Rationale comment (one sentence in code): the room's own typed edges are its cited convergences and must always score even same-type/same-root (DG-2 spirit); cross-boundary enumeration guarantees non-empty pairs on edge-sparse rooms.
    5. Provenance (additive, no renames): when room mode - `growth_proxy: 'created_at-recency (room-native)'` (override the tailResult value in the provenance object only; do NOT touch lib/core/eureka/tail-quadrant.cjs), `honest_nouns` from the adapter's meta.honest_nouns, and pass a ctx.graphRel of '(room-native: no idea-graph)' to renderReport.
    6. renderReport honesty (two conditional additions, both gated on `ctx.provenance.pairs_mode === 'room'`): (a) inside the insufficient_structure block, append the line `Not enough entries for a tail read (the cohort is below the MIN_COHORT 30 floor). The ranked pairs and Opportunity Statements above still stand.` - the literal phrase the navigator directive requires; (b) in the tail section intro, one line noting the room-native axes: attention is room-graph node degree, growth is created_at recency (still a proxy, still UNCALIBRATED).
    7. Do NOT change: DEFAULT_GRAPH, the graph/full enumeration branches, scoring, tail thresholds, statement building, JSON shape keys (room mode fills the same keys), exports. The module.exports list stays as-is.

    Create `tests/test-216-eureka-command.cjs` (plain-node ok() scaffold copied from tests/test-215-portfolio-report.cjs) with behaviors 1-5 as its first legs. Fixture builder: copy makeFixtureRoom, extend it to (a) accept a node count, (b) spread created_at across distinct days, (c) INSERT typed edges (including one same-type same-domain edge for Test 3 and an (A,B)/(B,A) duplicate to confirm union dedupe). All legs run --offline; zero network (the run-all aggregator preload enforces it in Plan 04; this file must also pass standalone).
  </action>
  <verify>
    <automated>node tests/test-216-eureka-command.cjs && node tests/test-215-portfolio-report.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-216-eureka-command.cjs` exits 0 (behaviors 1-5)
    - `node tests/test-215-portfolio-report.cjs` exits 0 (behavior 6, graph/full unregressed)
    - `grep -c "pairs !== 'room'" scripts/eureka-portfolio-report.cjs` >= 1 (the argv normalization admits room)
    - `grep -c "room-native-substrate" scripts/eureka-portfolio-report.cjs` >= 1 (the adapter is required)
    - `grep -c "Not enough entries for a tail read" scripts/eureka-portfolio-report.cjs` >= 1 (the directive phrase is in the renderer)
    - `git diff --stat scripts/eureka-room-report.cjs lib/core/eureka/` shows zero changes to eureka-room-report.cjs, ahp-weights.cjs, portfolio-dimensions.cjs, tail-quadrant.cjs, opportunity-statement.cjs (Part 7: same four modules, composed not modified; only room-native-substrate.cjs may show if Task hardening was needed)
  </acceptance_criteria>
  <done>Room mode runs the full composed pipeline (AHP + 3-dim + tail + statements) against a plain room.db with no idea-graph, degrades honestly below MIN_COHORT with the directive's exact phrase, and graph/full modes are proven unchanged by the 215 test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/eureka-command.cjs - the fire-and-return dispatcher</name>
  <files>scripts/eureka-command.cjs, tests/test-216-eureka-command.cjs</files>
  <read_first>
    - scripts/whitespace-command.cjs (lines 1-110: the dispatcher header/usage convention, ROOM_DIR-first argv contract, SYM glyph map, 3-line error pattern; this is the structural analog - but note this dispatcher calls the runner IN-PROCESS, not via execSync)
    - scripts/eureka-portfolio-report.cjs (exported main(argv) contract and exit codes; the --db flag takes a room DIRECTORY)
    - lib/core/resolve-active-room.cjs (exports + CLI entry at lines 304-315: the dispatcher does NOT resolve rooms itself - commands/eureka.md resolves via scripts/resolve-room and passes ROOM_DIR in, the whitespace pattern, SEED-034 one-door rule)
    - .planning/phases/216-eureka-user-facing-command-eureka-user-command-wrap-the-ship/216-CONTEXT.md (D-05: fire-and-return, not block-and-wait; must not assume room size)
  </read_first>
  <behavior>
    - Test 7: `run` on a fixture room (offline) exits 0; `<ROOM_DIR>/.mindrian/eureka/portfolio-report.md` and `.json` exist; `status.json` reads state 'done' with started_at, finished_at, out, json fields
    - Test 8: `report` prints the report JSON to stdout (JSON.parse succeeds, has provenance + ranked + statements keys); `status` prints one JSON line with state 'done'
    - Test 9: `status` on a room never scanned prints `{"state":"none"}` and exits 0; `report` on that room exits 1 with a single-line 3-line-pattern stderr naming the fix (run /mos:eureka)
    - Test 10: substrate resolution - a fixture room WITH `<ROOM_DIR>/.mindrian/idea-graph.json` (copy the 215 fixture-graph builder) runs in pairs_mode 'graph' against THAT file; the same room without it runs in pairs_mode 'room'; an explicit `--graph <path>` beats both. In no case does the dispatcher pass the JHU default (assert provenance graph row never mentions jhtv)
    - Test 11: a nonexistent ROOM_DIR exits 1 with a single-line stderr (never a stack trace)
    - Test 12 (fire-and-return, D-05): `start` exits 0 in well under the full scan time and prints the status.json + report paths; polling status.json (bounded loop, <= 30s) reaches state 'done' and the report files exist - the detached child completed independently
  </behavior>
  <action>
    Create `scripts/eureka-command.cjs` (CJS, 'use strict', BSL header, process.argv switch-case router - no Commander/yargs; NO em-dashes; NO emoji; zero network - the header states Canon Part 8 and Part 9 posture: writes only under <ROOM_DIR>/.mindrian/eureka/ plus the runner's derived eureka_* tables; NO nodes/edges/memory_event writes, banking is deferred per D-03).

    Argv contract: `node scripts/eureka-command.cjs ROOM_DIR SUBCOMMAND [flags]`; `--help` without ROOM_DIR prints usage. Flags for run/start: `--top <n>` (default 25), `--offline` (passthrough), `--graph <path>` (explicit substrate override).

    Paths module-level: REPORT_DIR = path.join(roomDir, '.mindrian', 'eureka'); OUT_MD = portfolio-report.md; OUT_JSON = portfolio-report.json; STATUS = status.json. mkdirSync recursive before writes.

    Subcommands:
    - `run`: (1) validate ROOM_DIR exists and is a directory, else the 3-line error to stderr + exit 1; (2) resolve substrate: explicit --graph (must exist, else 3-line error) -> pairs graph with that path; else `<ROOM_DIR>/.mindrian/idea-graph.json` exists -> pairs graph with it; else -> pairs room with NO --graph argument passed. NEVER fall through to the runner's DEFAULT_GRAPH (the JHU fixture is someone else's data - 216-RESEARCH.md anti-pattern 5); (3) write STATUS {state:'running', started_at: ISO, pid: process.pid, out, json}; (4) `const runner = require('./eureka-portfolio-report.cjs')` and `const code = await runner.main(argvArray)` in-process with ['--db', roomDir, '--pairs', mode, '--top', String(top), '--out', OUT_MD, '--json', OUT_JSON] plus optional --graph/--offline; (5) on code 0 write STATUS state 'done' + finished_at, exit 0; on nonzero or throw write STATUS state 'failed' + error (single line), single-line stderr, exit 1.
    - `start` (D-05 fire-and-return): spawn(process.execPath, [__filename, roomDir, 'run', ...forwardedFlags], { detached: true, stdio: 'ignore' }).unref(); print two lines to stdout - 'eureka scan started (background)' and the OUT_MD + STATUS paths - and exit 0 immediately. The child's own `run` maintains status.json, so `start` writes nothing itself beyond ensuring REPORT_DIR exists.
    - `status`: print the parsed STATUS file as one JSON line, or `{"state":"none"}` when absent; always exit 0.
    - `report`: print the OUT_JSON contents to stdout when present (exit 0); else the 3-line error (What: no eureka report yet / Why: no completed scan for this room / Fix: /mos:eureka) and exit 1.
    - `help` / unknown / missing subcommand: usage text, exit 0 for help, 1 for unknown.

    Export `module.exports = { main }` taking argv (the testable seam, the runner's own pattern) with the require.main gate for CLI execution.

    Extend `tests/test-216-eureka-command.cjs` with behaviors 7-12 as dispatcher legs (call the exported main() in-process for 7-11; Test 12 spawns the real CLI via child_process.spawnSync for `start` then polls status.json with a bounded loop). All offline. Clean up tmp rooms in finally.
  </action>
  <verify>
    <automated>node tests/test-216-eureka-command.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-216-eureka-command.cjs` exits 0 (behaviors 1-12 all green, >= 25 assertions)
    - `grep -v '^\s*[*/]' scripts/eureka-command.cjs | grep -cE "(https?://|require\(.(node:)?(http|https|net|dns)|fetch\()"` outputs 0 (zero egress, Canon Part 8)
    - `grep -c "jhtv-idea-graph" scripts/eureka-command.cjs` outputs 0 (the JHU fixture is never referenced by the dispatcher)
    - `grep -v '^\s*[*/]' scripts/eureka-command.cjs | grep -cE "INSERT INTO (nodes|edges|memory_event)"` outputs 0 (Part 9: report-only, D-03)
    - `node scripts/eureka-command.cjs --help` exits 0 and names all five subcommands
  </acceptance_criteria>
  <done>The dispatcher runs the engine against a passed ROOM_DIR with correct substrate resolution, supports detached fire-and-return with a polling status contract, prints machine-readable report JSON for the command body to render, and fails with 3-line errors - all proven offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv -> dispatcher | ROOM_DIR and flag values are untrusted strings |
| dispatcher -> room filesystem | writes confined to <ROOM_DIR>/.mindrian/eureka/ |
| detached child -> status.json | crash visibility depends on the status file contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-216-04 | Information disclosure | dispatcher egress (Part 8) | mitigate | zero network requires; grep gate in Task 2 acceptance; runner already egress-free |
| T-216-05 | Information disclosure | JHU fixture data on a user machine | mitigate | dispatcher never passes DEFAULT_GRAPH; Test 10 + grep gate pin it |
| T-216-06 | Tampering | unexpected room-graph writes (Part 9) | mitigate | report-only v1 (D-03); no INSERT into nodes/edges/memory_event; grep gate |
| T-216-07 | Repudiation | detached child dies silently | mitigate | status.json transitions running->done/failed with error text; `status` subcommand surfaces it (Test 12) |
| T-216-08 | Denial of service | O(n^2) room-mode enumeration on a huge room | accept | D-05 fire-and-return keeps the conversation unblocked; room-scale n is small; the 215 run proved 2M pairs completes |
| T-216-SC | Tampering | package installs | accept | N/A - zero new packages this phase |
</threat_model>

<verification>
- `node tests/test-216-eureka-command.cjs` green (12 behaviors)
- `node tests/test-216-room-substrate.cjs` green (Plan 01 unregressed)
- `bash tests/run-all-215.sh` green (the runner edit did not regress the 215 gate; leg 6 may SKIP if the real-room JSON is absent - PASS/SKIP both acceptable, FAIL is not)
</verification>

<success_criteria>
- 216-R2 satisfied: `--pairs room` composes the adapter through the SAME shipped modules; graph/full modes unchanged; the dispatcher exposes run/start/status/report with room-correct output paths and fire-and-return
- The 215/JHU path is untouched at the behavior level (test-215-portfolio-report.cjs green without edits to that test)
</success_criteria>

<output>
Create `.planning/phases/216-eureka-user-facing-command-eureka-user-command-wrap-the-ship/216-02-SUMMARY.md` when done
</output>
