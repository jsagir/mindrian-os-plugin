---
phase: 240.1-context-layer-drift-detection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-240.1-state-version-preserved.cjs
  - tests/test-240.1-state-version-drift-gate.cjs
  - lib/core/state-version.cjs
  - lib/core/frontmatter-schemas.cjs
  - lib/core/state-ops.cjs
autonomous: true
requirements: [CTXL-01]
canon_parts: [6, 7]

must_haves:
  truths:
    - "A room whose STATE.md was seeded by scripts/room-registry still carries gsd_state_version after a real computeState() regeneration"
    - "A STATE.md carrying a version NEWER than the plugin understands is never overwritten; a notification is surfaced instead"
    - "A STATE.md with no version stamp is stamped forward, not rejected"
    - "This repo's own frontmatter validator recognises the keys this repo's own writers emit"
    - "current_room is never preserved across a regeneration, so a parked room cannot resurrect a stale active-room marker"
  artifacts:
    - path: "lib/core/state-version.cjs"
      provides: "preserve-or-notify persistence for the per-room STATE.md"
      exports: ["persistState", "readStateFrontmatter", "classifyVersion", "spliceFrontmatterKeys", "STATE_SCHEMA_VERSION", "STATE_SCHEMA_VERSION_LITERAL", "PRESERVED_KEYS"]
      min_lines: 140
    - path: "tests/test-240.1-state-version-preserved.cjs"
      provides: "the empirical repro from 240.1-RESEARCH.md 1B, turned into a standing test"
      min_lines: 80
    - path: "tests/test-240.1-state-version-drift-gate.cjs"
      provides: "must_catch / must_not_catch probe table for the four version classifications"
      min_lines: 100
  key_links:
    - from: "lib/core/state-ops.cjs"
      to: "lib/core/state-version.cjs"
      via: "persistState replaces the blind fs.writeFileSync"
      pattern: "persistState\\("
    - from: "lib/core/state-version.cjs"
      to: "schema-violations.jsonl"
      via: "appendStateVersionViolation JSONL sink"
      pattern: "schema-violations\\.jsonl"
---

<objective>
Close the in-repo half of CTXL-01 at its root: build `lib/core/state-version.cjs`,
the preserve-or-notify module every per-room `STATE.md` write site must call, wire
the first write site (`lib/core/state-ops.cjs::computeState`), and teach this
repo's own frontmatter validator the keys this repo's own writers emit.

Purpose: `scripts/compute-state` renders a fresh frontmatter block and every caller
blind-overwrites the room's `STATE.md` with it, destroying the `gsd_state_version`
stamp that `scripts/room-registry:127-131` seeds at room birth. This was reproduced
empirically during research (240.1-RESEARCH.md, Finding 1B): BEFORE
`gsd_state_version: 1.0 / status: active`, AFTER `computed: ... / venture_stage: ...
/ total_entries: ...`, both seeded keys DESTROYED. Per locked decision D-01 this is
the CTXL-01 target; `.planning/STATE.md` is explicitly out of scope.

Output: one new module, two new tests, three modified files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/240.1-context-layer-drift-detection/240.1-RESEARCH.md
@.planning/phases/240.1-context-layer-drift-detection/240.1-PATTERNS.md
@lib/core/install-state.cjs
@lib/core/user-md-ops.cjs
@lib/core/state-ops.cjs
@scripts/room-registry
</context>

<locked_decisions>
D-01 (LOCKED): CTXL-01 targets the PER-ROOM `STATE.md` generated by
`scripts/compute-state`. `.planning/STATE.md` is `gsd-core` territory, external,
already root-caused by two RCAs, and explicitly OUT OF SCOPE. No task in this plan
may name `~/.claude/gsd-core` as an edit target. Plan 240.1-01's scope-escape
tripwire enforces this by machine.
</locked_decisions>

<planner_corrections>
Three corrections to the census in `240.1-RESEARCH.md`, all measured on disk at
HEAD `3d8aa34a` while planning. Honor these over the research text where they
conflict.

CORRECTION A (new, load-bearing). `scripts/room-registry` contains a SECOND
STATE.md writer the six-site census missed: `_write_current_room()` at
`scripts/room-registry:190-280`, an embedded Python frontmatter UPSERT that
already preserves every sibling key. It is a PRESERVING writer, not a clobbering
one, and `lib/memory/statusline-active-room-write.test.cjs` test 3 already asserts
`gsd_state_version: 1.0` and `status: active` survive it. Do NOT touch it and do
NOT count it as a defect. It is prior art for the merge semantics, in bash plus
Python, so it is not requirable from Node.

CORRECTION B (new, load-bearing, and a real trap). `PRESERVED_KEYS` MUST NOT
include `current_room`. `scripts/compute-state:29-45` deliberately RE-DERIVES
`current_room` from the registry and emits the line ONLY for the registry-active
room, so a parked room gains no line ("this never bulk-backfills the existing
rooms"). If `persistState` preserved `current_room`, every parked room would
resurrect a stale active-room marker on its next regeneration and the no-backfill
contract that `lib/memory/statusline-active-room-write.test.cjs` test 6 pins would
break in production. `PRESERVED_KEYS` is exactly `['gsd_state_version', 'status']`.

CORRECTION C (new). Two more absent-guarded seeders exist beyond `room-registry`:
`scripts/vault-import.cjs:143-158` (`scaffoldRoom`, emits `gsd_state_version: 1.0`,
`status: imported`, `created_by: phase-80-vault-import`) and
`lib/core/room-skeleton-scaffold.cjs:300-310` (writes
`templates/room-skeleton/STATE.md.tmpl`, which carries NO version stamp, guarded by
`isStateAuthored` at `:120-136` so it never clobbers a registry-seeded room). A
scaffold-born room legitimately has no stamp until its first regeneration stamps it
forward via the legacy branch. Both are REVIEWED-NO-CHANGE for this plan; neither
is a defect. Record them in the summary's census.
</planner_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author both CTXL-01 tests RED-first and prove they fail for the right reason</name>
  <files>tests/test-240.1-state-version-preserved.cjs, tests/test-240.1-state-version-drift-gate.cjs</files>
  <read_first>
    - .planning/phases/240.1-context-layer-drift-detection/240.1-RESEARCH.md (Finding 1B, the empirical repro this test encodes; Pitfall 2, why a computeState-only test is insufficient)
    - .planning/phases/240.1-context-layer-drift-detection/240.1-PATTERNS.md (section "NEW tests/test-240.1-state-version-preserved.cjs + ...state-version-drift-gate.cjs")
    - scripts/room-registry (lines 118-140, `_seed_room_bootstrap`: the exact seeded STATE.md bytes the fixture must reproduce)
    - lib/core/state-ops.cjs (lines 31-46, `computeState`, the function under test)
    - tests/test-236-rebuild-preserves-journal.cjs (the `ok` / `fail` / scenario harness shape and the MUTATION THAT TURNS THIS RED header idiom)
    - tests/test-across-session-memory.cjs (lines 100-111, `withTmpRoot`: save and restore `MINDRIAN_ROOMS_HOME` in a `finally`)
  </read_first>
  <behavior>
`tests/test-240.1-state-version-preserved.cjs`
  - Scenario 1: a room seeded exactly as `scripts/room-registry:127-131` seeds it
    (`gsd_state_version: 1.0` and `status: active`), put through the REAL
    `computeState(roomDir)` from `lib/core/state-ops.cjs`, still carries
    `gsd_state_version: 1.0` afterwards.
  - Scenario 2: the same run still carries `status: active`.
  - Scenario 3: the same run DID regenerate the body: the frontmatter now also
    carries a `computed:` line and a `total_entries:` line, so the test cannot pass
    by the write having been skipped entirely.
  - Scenario 4 (anti-vacuity): the emitted `gsd_state_version` value is the
    byte-identical string `1.0`, not a re-rendered `1`.
  - Scenario 5: a room whose STATE.md carries NO frontmatter at all is stamped
    forward and gains `gsd_state_version: 1.0`.

`tests/test-240.1-state-version-drift-gate.cjs` (must_catch / must_not_catch table)
  - must_catch: on-disk `gsd_state_version: 2.0` (newer than the module
    understands). `persistState` returns `status === 'version-mismatch'`, the file
    bytes are UNCHANGED (byte comparison against the pre-call read), and one JSON
    line is appended to the violation sink.
  - must_not_catch: on-disk `1.0` (equal). The write proceeds; the stamp is
    re-emitted byte-identically.
  - must_not_catch: no version key present (legacy). The write proceeds and stamps
    forward.
  - must_not_catch: STATE.md absent entirely. The file is created carrying the stamp.
  - must_not_catch: a non-numeric version value (`gsd_state_version: banana`).
    Safety-net branch: stamped forward, matching `install-state.cjs`'s
    lower-than-v1 coercion, never a throw.
  - CORRECTION B fence: a room whose prior STATE.md carries `current_room: stale-room`
    and which is NOT the registry-active room does NOT carry `current_room` after
    the regeneration. `current_room` is not in `PRESERVED_KEYS`.
  - Never-throw fence: `persistState` called against an unwritable path returns
    `{ status: 'error' }` and writes one line to stderr; it does not throw.
  </behavior>
  <action>
Create both test files. Node built-ins only (`node:test` is not used by this
repo's phase tests; use `node:assert/strict` plus the plain scenario-runner shape
in `tests/test-236-rebuild-preserves-journal.cjs`). Print a final
`PASS test-240.1-<name>.cjs (<n> scenarios)` line and `process.exit(0)`.

HERMETICITY IS MANDATORY. Every scenario owns a `fs.mkdtempSync` root under
`os.tmpdir()`, sets `MINDRIAN_ROOMS_HOME` to a path inside it, and restores the
prior value in a `finally`. Phase 240 Plan 02 just fixed a live-store leak of
exactly this class (MEM-03); do not reintroduce it. Nothing under
`~/MindrianRooms/` may be read or written.

Seed the fixture STATE.md by writing the byte-identical block
`scripts/room-registry:127-131` writes, including the `# State` body and the
`## Decisions` heading, so a body-preservation regression is visible too. Also
create `.room-root` and `.mindrian/` in the fixture room so `compute-state`'s room
detection succeeds.

`tests/test-240.1-state-version-preserved.cjs` drives the REAL `computeState()`.
`tests/test-240.1-state-version-drift-gate.cjs` drives `persistState()` directly
with a fixed synthetic rendered body (no `compute-state` spawn) so the version
branches are isolated from the renderer.

Write the MUTATION header in both files, modeled on
`tests/test-236-rebuild-preserves-journal.cjs:18-31`, stating BOTH directions:
what turns the file RED (restoring the blind
`fs.writeFileSync(path.join(resolved, 'STATE.md'), result)` at
`lib/core/state-ops.cjs:44` with no preceding version read), AND, honestly, what
does NOT: this mutation does not redden a test which only exercises
`computeState()` if the other five write sites are unguarded, which is exactly why
`tests/test-240.1-hook-write-site.sh` exists in plan 240.1-03 (Pitfall 2).

Both files must run RED at the end of this task, because
`lib/core/state-version.cjs` does not exist yet. Capture and transcribe the exact
RED output in the summary: it must fail on the missing module or the destroyed
stamp, NOT on a syntax error or a bad fixture path. A test that is red for the
wrong reason is not a RED-first proof.

No em-dashes and no en-dashes in either file. Where a forbidden code point must be
named, use a `\u` escape only, never a literal character (the house rule from
`tests/test-143.2-doctrine-presence.cjs:20-23`).
  </action>
  <verify>
    <automated>node tests/test-240.1-state-version-preserved.cjs; test $? -ne 0 &amp;&amp; node tests/test-240.1-state-version-drift-gate.cjs; test $? -ne 0</automated>
  </verify>
  <done>
Both test files exist, are syntactically valid, and both exit nonzero. The failure
message names either the missing `lib/core/state-version.cjs` module or a destroyed
`gsd_state_version` stamp. No file under `~/MindrianRooms/` was touched.
  </done>
  <acceptance_criteria>
    - behavior: `node tests/test-240.1-state-version-preserved.cjs` exits nonzero with a message naming `gsd_state_version` or `state-version.cjs`
    - behavior: `node tests/test-240.1-state-version-drift-gate.cjs` exits nonzero for the same class of reason
    - source: both files call `fs.mkdtempSync` and set `MINDRIAN_ROOMS_HOME`, restoring it in a `finally`
    - source: neither file contains the literal string `MindrianRooms` outside a temp-root construction
    - source: both files carry a MUTATION header naming `lib/core/state-ops.cjs:44` and stating what does NOT turn them red
    - source: zero U+2014 and zero U+2013 characters in either file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build lib/core/state-version.cjs and close the frontmatter-schema drift</name>
  <files>lib/core/state-version.cjs, lib/core/frontmatter-schemas.cjs, tests/test-240.1-state-version-drift-gate.cjs</files>
  <read_first>
    - lib/core/install-state.cjs (lines 54 and 184-231: `SCHEMA_VERSION` and `migrateIfNeeded`, the four-branch mismatch contract to port verbatim in structure)
    - lib/core/user-md-ops.cjs (lines 62, 299-300, 386: the preserve-across-regeneration idiom and the line-push frontmatter writer)
    - lib/core/room-skeleton-scaffold.cjs (lines 90-100: `atomicWrite`, the in-repo same-dir tmp plus renameSync idiom for exactly this file)
    - scripts/frontmatter-schema-validator.cjs (lines 1-30 and 197-219: the ADVISORY-NOT-BLOCKING contract and the `resolveLogPath` / `appendViolationLog` sink shape)
    - lib/core/frontmatter-schemas.cjs (the `'STATE.md'` key and the `'USER.md'` key immediately below it, for the commented-group convention and the exported validation function's signature)
    - scripts/compute-state (lines 238-246: the exact frontmatter block this module receives as its `rendered` argument)
  </read_first>
  <action>
Create `lib/core/state-version.cjs`. CJS, `'use strict'`, Node built-ins only, zero
new dependencies, no YAML dependency (Security Domain V5).

Declare and export two constants, and document in a comment why there are two:
`STATE_SCHEMA_VERSION = 1` is the numeric comparison authority (the
`install-state.cjs:54` idiom), and `STATE_SCHEMA_VERSION_LITERAL = '1.0'` is the
emitted rendering, kept byte-identical to what `scripts/room-registry:130` writes,
because `String(1.0)` in JavaScript is `'1'` and a silent `1.0` to `1` rewrite
would be a new drift introduced by the fix itself.

Declare and export `PRESERVED_KEYS = Object.freeze(['gsd_state_version', 'status'])`.
Write CORRECTION B's reasoning into a comment directly above it, naming
`scripts/compute-state:29-45` and
`lib/memory/statusline-active-room-write.test.cjs` test 6, so a future reader
cannot "helpfully" add `current_room` to the list.

Export `readStateFrontmatter(roomDir)` returning
`{ present, keys, raw }`. Parse with the line-walk idiom used in
`scripts/frontmatter-schema-validator.cjs` (split on the leading `---` fence, walk
lines, split each on the FIRST colon, trim). Do not add a YAML dependency and do
not require `brain-md-schema.cjs` for this. Return `{ present: false, keys: {},
raw: null }` for an absent file, an unparseable file, or an EMPTY file. The empty
case is load-bearing: `scripts/on-agent-complete:133` currently truncates STATE.md
to zero before `compute-state` runs (Pitfall 3), so an empty read is a real and
expected input, not a corruption signal.

Export `classifyVersion(value)` returning one of the literal strings `'absent'`,
`'current'`, `'future'`, `'legacy'`. Parse with `parseFloat` so `'1'` and `'1.0'`
both compare equal to `STATE_SCHEMA_VERSION`. Mapping: undefined or null to
`'absent'`; NaN or a value below `STATE_SCHEMA_VERSION` to `'legacy'` (the
`install-state.cjs` lower-than-v1 safety net); equal to `'current'`; greater to
`'future'`.

Export `spliceFrontmatterKeys(rendered, keys)`: insert each key from
`PRESERVED_KEYS` (in that order) immediately after the opening `---` of the
rendered block, SKIPPING any key already present in the rendered block, so a future
change to `scripts/compute-state` cannot produce a duplicate key. If `rendered` has
no opening `---` fence, return it unchanged and let the caller record the anomaly.

Export `persistState(roomDir, rendered, opts)`, the four-branch contract ported
structurally from `install-state.cjs:184-214`:
  - `'future'` -> DO NOT WRITE. Call the notification sink, write one line to
    stderr, and return `{ status: 'version-mismatch', onDiskVersion, moduleVersion:
    STATE_SCHEMA_VERSION, written: false, advice: 'run /mos:doctor --fix' }`. This
    is CTXL-01's success criterion: detect mismatch, notify, refuse to overwrite.
  - `'current'` -> preserve. Re-emit the on-disk RAW literal byte-for-byte (so
    `1.0` stays `1.0`), splice `status` if the prior file carried one, write.
    Return `{ status: 'ok', classification: 'current', stamped: false }`.
  - `'absent'` or `'legacy'` -> stamp forward using
    `STATE_SCHEMA_VERSION_LITERAL`, preserve `status` if present, write. Return
    `{ status: 'ok', classification, stamped: true }`.
  - any thrown error -> return `{ status: 'error', message }` and write one line to
    stderr. NEVER throw and NEVER swallow silently. This is the never-throw
    envelope for hook-adjacent code, with the counter-lesson from
    `lib/core/navigation/room-birth.cjs:992-995` (a bare `catch (_e)` with a
    "Tolerate" comment is why that site's compute-state has never once succeeded):
    a soft failure must still WRITE A LINE.

The write itself must be ATOMIC: same-dir tmp plus `fs.renameSync`. Prefer
requiring `atomicWrite` from `lib/core/room-skeleton-scaffold.cjs` if that module
exports it; if it does not, implement the identical shape here and cite
`lib/core/room-skeleton-scaffold.cjs:90` as the source idiom in a comment. The
read-then-write version compare opens a TOCTOU window (Security Domain
T-240.1-05); tmp-and-rename is the in-repo mitigation.

The notification sink: mirror `scripts/frontmatter-schema-validator.cjs:197-219`.
That file is a hook script with NO `module.exports`, so it cannot be required;
re-implement `resolveLogPath()` (prefer `process.env.CLAUDE_PLUGIN_DATA`, else
`$HOME/.mindrian/`) and an append helper writing ONE JSON line to
`schema-violations.jsonl`, and say in a comment that the shape is deliberately
identical so `/mos:doctor --acceptance` and `/mos:admin` pick it up with no new
surface (Canon Part 7). Use a new entry `kind: 'state-version-mismatch'` carrying
`file`, `onDiskVersion`, `moduleVersion`, and an ISO timestamp. The append is
soft-fail wrapped, matching the source.

Write into the module header WHY the existing notification surface could not
simply be reused: `hooks/hooks.json:258-263` registers
`frontmatter-schema-validator.cjs` on the matcher `Write|Edit|MultiEdit`, which
fires only on CLAUDE TOOL CALLS, while every STATE.md write site is a programmatic
`fs.writeFileSync` or a shell redirect. The existing surface is structurally blind
to the writer that causes the drift. This module reuses the LOG SINK and the
ADVISORY SEMANTICS and does not attempt to route detection through the PostToolUse
hook.

Then edit `lib/core/frontmatter-schemas.cjs`. Add FOUR keys to the `'STATE.md'`
key's `optional` list under a new commented group. State in the comment that this
is a Canon Part 6 dog-fooding fix: these are keys this repo's OWN writers emit that
this repo's OWN validator did not recognise, and `CHANGELOG.md:822` records the
same self-violation class happening once already. The four keys and the writer
that emits each:
  `gsd_state_version`  seeded by `scripts/room-registry:130`, `scripts/vault-import.cjs:149`; now also emitted by `lib/core/state-version.cjs`
  `status`             seeded by `scripts/room-registry:131` (`active`), `scripts/vault-import.cjs:150` (`imported`)
  `created_by`         emitted by `scripts/vault-import.cjs:151`
  `current_room`       emitted by `scripts/compute-state:244` for the registry-active room only

Add a scenario to `tests/test-240.1-state-version-drift-gate.cjs` asserting all
four keys validate cleanly through the pure validation function
`lib/core/frontmatter-schemas.cjs` exports (read the file for the exact exported
name and signature), so the schema fix is fenced rather than assumed.

No em-dashes and no en-dashes in any file this task touches.
  </action>
  <verify>
    <automated>node tests/test-240.1-state-version-drift-gate.cjs</automated>
  </verify>
  <done>
`lib/core/state-version.cjs` exists and exports all seven named symbols.
`tests/test-240.1-state-version-drift-gate.cjs` passes fully green, including all
five version-branch probes, the CORRECTION B `current_room` fence, the never-throw
fence, and the four-key schema scenario.
`tests/test-240.1-state-version-preserved.cjs` is still RED, because write site 1
is not wired until Task 3.
  </done>
  <acceptance_criteria>
    - source: `lib/core/state-version.cjs` exports `persistState`, `readStateFrontmatter`, `classifyVersion`, `spliceFrontmatterKeys`, `STATE_SCHEMA_VERSION`, `STATE_SCHEMA_VERSION_LITERAL`, `PRESERVED_KEYS`
    - source: `PRESERVED_KEYS` is exactly `['gsd_state_version', 'status']` and the file contains no code path adding `current_room` to it
    - source: the module contains `renameSync` (atomic write) and `schema-violations.jsonl` (the shared sink)
    - source: `lib/core/frontmatter-schemas.cjs` `'STATE.md'` optional list contains all four of `gsd_state_version`, `status`, `created_by`, `current_room`
    - behavior: `persistState` on a room carrying `gsd_state_version: 2.0` returns `status === 'version-mismatch'` and leaves the file bytes byte-identical
    - behavior: `persistState` on an unwritable path returns `{ status: 'error' }` and does not throw
    - test-command: `node tests/test-240.1-state-version-drift-gate.cjs` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire write site 1 in state-ops.cjs, correct its false chokepoint claim, and run the mutation proof</name>
  <files>lib/core/state-ops.cjs</files>
  <read_first>
    - lib/core/state-ops.cjs (the WHOLE file: line 2 carries a literal em-dash, lines 19-26 carry the false "single Node chokepoint" claim, lines 31-46 are `computeState`)
    - lib/core/state-version.cjs (the module built in Task 2, for `persistState`'s signature and verdict envelope)
    - .planning/phases/240.1-context-layer-drift-detection/240.1-RESEARCH.md (Finding 1C, the six-site census that makes the chokepoint claim measurably false)
    - lib/memory/statusline-active-room-write.test.cjs (the 7-behavior regression fence this change must not break; behaviors 5 and 6 drive a real compute-state regeneration)
  </read_first>
  <action>
Edit `lib/core/state-ops.cjs`, three changes in one commit.

(1) Line 2 currently reads `MindrianOS Plugin  -  State Operations` with a
literal em-dash (U+2014). Replace it with a hyphen. This is a CLAUDE.md HARD RULE
violation sitting in a file this plan must edit anyway; it is called out here as an
explicit task line rather than being left as a surprise at commit time.

(2) Replace the blind `fs.writeFileSync(path.join(resolved, 'STATE.md'), result)`
at line 44 with a call to `persistState(resolved, result)` from
`lib/core/state-version.cjs`. Change the signature to `computeState(roomDir, opts)`.
Default behavior is UNCHANGED: return the raw string, so every existing caller is
byte-compatible. When `opts && opts.withVerdict === true`, return
`{ content, verdict }` instead. Document the option in the JSDoc block.

(3) Correct the doc comment at lines 19-26. It currently claims persistence at
"the single Node chokepoint," which `240.1-RESEARCH.md` Finding 1C measured as
false: five other sites bypass this function (`lib/core/intelligence-cascade.cjs:492-498`,
`scripts/on-stop:144-145`, `scripts/on-task-complete:51-55`,
`scripts/on-agent-complete:133`, `lib/core/navigation/room-birth.cjs:986-990`).
`CHANGELOG.md:806` carries the same false claim. Rewrite the comment to name the
census, state that the version-preservation authority is
`lib/core/state-version.cjs` and not this function, and point at plan 240.1-03 as
where the remaining sites are wired. Leaving the false claim in place is what
produces Pitfall 2 in the next reader.

Then run the MUTATION PROOF and transcribe both directions in the summary. This
milestone requires a real, executed mutation for every gate the phase claims to
close, not an asserted one:
  RED:  restore line 44 to the blind `fs.writeFileSync(path.join(resolved,
        'STATE.md'), result)`. Run `node tests/test-240.1-state-version-preserved.cjs`
        and confirm it fails, quoting the failing scenario and its message.
  GREEN: restore the `persistState` call. Re-run and confirm it passes.
Confirm the file is byte-identical to the fixed state afterwards (`git diff` shows
only the three intended changes).

REGRESSION FENCE, mandatory before commit:
`node lib/memory/statusline-active-room-write.test.cjs` must still report
`statusline-active-room-write: 7/7 tests passed`. That suite is registered in
`lib/memory/run-feynman-tests.cjs:926`, its behavior 3 already asserts
`gsd_state_version: 1.0` and `status: active` survive the `room-registry` upsert
path, and its behaviors 5 and 6 drive a real `compute-state` regeneration. It is
the closest existing fence to this change and the single most likely thing to
break. Measured baseline before this plan: 7/7 passing.
  </action>
  <verify>
    <automated>node tests/test-240.1-state-version-preserved.cjs &amp;&amp; node tests/test-240.1-state-version-drift-gate.cjs &amp;&amp; node lib/memory/statusline-active-room-write.test.cjs</automated>
  </verify>
  <done>
`computeState()` persists through `persistState`. Both Phase 240.1 CTXL-01 tests
are green. `lib/memory/statusline-active-room-write.test.cjs` still reports 7/7.
The mutation proof was executed in both directions and transcribed. `grep -c
$'\u2014' lib/core/state-ops.cjs` returns 0.
  </done>
  <acceptance_criteria>
    - source: `lib/core/state-ops.cjs` line 2 contains no U+2014; the whole file contains zero U+2014 and zero U+2013
    - source: `lib/core/state-ops.cjs` contains `persistState(` and no longer contains `fs.writeFileSync(path.join(resolved, 'STATE.md')`
    - source: the `computeState` JSDoc no longer claims "the single Node chokepoint" and names the five bypassing sites
    - behavior: `computeState(roomDir)` with no opts returns a string (unchanged contract); with `{ withVerdict: true }` returns an object carrying `content` and `verdict`
    - test-command: `node tests/test-240.1-state-version-preserved.cjs` exits 0
    - test-command: `node lib/memory/statusline-active-room-write.test.cjs` prints `7/7 tests passed`
    - behavior: with line 44 mutated back to the blind write, `tests/test-240.1-state-version-preserved.cjs` exits nonzero (transcribed in the summary)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room filesystem -> `readStateFrontmatter` | Untrusted-shaped file content (possibly empty, truncated, or hand-edited) crosses here |
| `persistState` -> room `STATE.md` | A write into user-owned room data |
| `persistState` -> `schema-violations.jsonl` | An append into the plugin data dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-240.1-04 | Tampering | `persistState` path handling | mitigate | `path.resolve` on `roomDir` before any join; never accept a caller-supplied filename |
| T-240.1-05 | Tampering | TOCTOU between the version read and the write | mitigate | Same-dir tmp plus `fs.renameSync` (the `room-skeleton-scaffold.cjs:90` idiom); the compute-state sites have no lock at all |
| T-240.1-06 | Denial of service | A throw inside hook-adjacent code | mitigate | Never-throw envelope returning `{ status: 'error' }` plus one stderr line; a throw here would break Larry's turn on all three surfaces |
| T-240.1-07 | Information disclosure | Violation-log contents | mitigate | The JSONL entry carries only `file`, `onDiskVersion`, `moduleVersion`, `kind`, timestamp; never file body, never room content |
| T-240.1-08 | Spoofing | Data loss disguised as success | mitigate | The `'future'` branch refuses the write and returns `written: false`; the drift gate asserts the file bytes are unchanged, not merely that a verdict was returned |
| T-240.1-SC | Tampering | npm/pip/cargo installs | accept | Zero package-manager operations; `240.1-RESEARCH.md` Package Legitimacy Audit records N/A for the phase |
</threat_model>

<verification>
- `node tests/test-240.1-state-version-preserved.cjs` exits 0.
- `node tests/test-240.1-state-version-drift-gate.cjs` exits 0.
- `node lib/memory/statusline-active-room-write.test.cjs` prints `7/7 tests passed`.
- `bash tests/run-all-2401.sh` now discovers two files and no longer trips the empty-discovery guard (the `.sh` leg and the other three test files land in later plans).
- Zero U+2014 and U+2013 across all five files in `files_modified`.
- `git diff --stat` shows changes confined to the five files in `files_modified`.
</verification>

<success_criteria>
1. A room seeded by `scripts/room-registry` survives a real `computeState()` with its `gsd_state_version` and `status` intact, proven by an executed test (ROADMAP SC1, first half).
2. A newer-than-understood version stamp produces a notification and NO overwrite, proven by a must_catch probe asserting byte-identical file contents (ROADMAP SC1, second half).
3. The blind-overwrite mutation was reintroduced, observed RED, and restored, with both directions transcribed.
4. This repo's frontmatter validator now recognises the four keys this repo's writers emit (Canon Part 6).
5. `current_room` is provably NOT preserved across a regeneration (CORRECTION B).
6. The pre-existing 7/7 `statusline-active-room-write` fence is still green.
</success_criteria>

## Artifacts this phase produces

This plan's contribution. Full phase index is in `240.1-01-PLAN.md`.

| Symbol / path | Kind |
|---------------|------|
| `lib/core/state-version.cjs` | new module |
| `STATE_SCHEMA_VERSION` (number, 1) | exported const |
| `STATE_SCHEMA_VERSION_LITERAL` (string, `1.0`) | exported const |
| `PRESERVED_KEYS` (frozen `['gsd_state_version','status']`) | exported const |
| `readStateFrontmatter(roomDir)` | exported function |
| `classifyVersion(value)` | exported function |
| `spliceFrontmatterKeys(rendered, keys)` | exported function |
| `persistState(roomDir, rendered, opts)` | exported function |
| `computeState(roomDir, opts)` with `opts.withVerdict` | new option on an existing export |
| `kind: 'state-version-mismatch'` | new JSONL entry kind in `schema-violations.jsonl` |
| `gsd_state_version`, `status`, `created_by`, `current_room` | new keys in the `'STATE.md'` frontmatter schema optional list |
| `tests/test-240.1-state-version-preserved.cjs` | new test |
| `tests/test-240.1-state-version-drift-gate.cjs` | new test |

<output>
Create `.planning/phases/240.1-context-layer-drift-detection/240.1-02-SUMMARY.md` when done.
</output>
