---
phase: 240.1-context-layer-drift-detection
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-240.1-layer-doctrine-presence.cjs
  - docs/MWP-SPECIFICATION.md
  - docs/BRAIN-MD-SCHEMA.md
autonomous: true
requirements: [CTXL-02]
canon_parts: [7, 8, 9]

must_haves:
  truths:
    - "docs/MWP-SPECIFICATION.md names and distinguishes the SEMANTIC layer from the CONTEXT layer, pinned to INDEXER_OWNED_NODE_TYPES as the operational boundary"
    - "docs/BRAIN-MD-SCHEMA.md carries the same distinction for BRAIN.md's own derived vs institutional content"
    - "A doctrine-presence gate asserts both terms are present AND distinguished, and was observed RED before the doc edits landed"
    - "The new prose introduces no KuzuDB claim, and the gate's KuzuDB exclusion is scoped to the new sections only"
    - "docs/BRAIN-MD-SCHEMA.md's Canon Part 8 ALLOWED/FORBIDDEN contract is not weakened by the insertion"
  artifacts:
    - path: "tests/test-240.1-layer-doctrine-presence.cjs"
      provides: "the 143.2 doctrine-presence idiom applied to the semantic/context boundary"
      min_lines: 110
    - path: "docs/MWP-SPECIFICATION.md"
      provides: "section 2.8, the layer distinction in the canonical graph-schema doc"
      contains: "INDEXER_OWNED_NODE_TYPES"
    - path: "docs/BRAIN-MD-SCHEMA.md"
      provides: "section 5.1, the layer distinction for BRAIN.md"
      contains: "INDEXER_OWNED_NODE_TYPES"
  key_links:
    - from: "docs/MWP-SPECIFICATION.md section 2.8"
      to: "lib/core/lazygraph-ops.cjs:81"
      via: "cited file:line for the enforcing allowlist"
      pattern: "lazygraph-ops\\.cjs"
    - from: "tests/test-240.1-layer-doctrine-presence.cjs"
      to: "the new doc sections"
      via: "heading-anchored slice then token assertions against the slice only"
      pattern: "Semantic Layer vs Context Layer"
---

<objective>
Name the boundary the code already enforces. Phase 236 (GRAPHDB-01) shipped
`INDEXER_OWNED_NODE_TYPES`, an ownership allowlist that IS the line between
derivable structure (safe to wipe and re-derive on every `rebuildGraph`) and
irreplaceable institutional knowledge (must survive it). Nothing in the docs says
so. CTXL-02's honest deliverable is to write that boundary down in the two docs
that currently conflate the two layers, and to fence it with a doctrine-presence
gate that cannot pass vacuously.

Purpose: `240.1-RESEARCH.md` Finding 2B established that room.db needs NO schema
change and NO migration, so this requirement is documentation over an existing,
unnamed, already-load-bearing distinction. Phase 236 existed because the indexer
treated context-layer rows as semantic-layer rows and truncated them; naming the
boundary makes the doctrine load-bearing rather than decorative, and gives the next
reader the vocabulary Phase 236 had to discover the hard way.

Output: one new test, two modified docs. No code, no schema, no migration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/240.1-context-layer-drift-detection/240.1-RESEARCH.md
@.planning/phases/240.1-context-layer-drift-detection/240.1-PATTERNS.md
@lib/core/lazygraph-ops.cjs
@tests/test-143.2-doctrine-presence.cjs
@~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-motherduck-context-layer/2026-07-30-motherduck-context-layer.md
</context>

<locked_decisions>
D-02 (LOCKED): MotherDuck's DABStep figures are VERIFIED, not assumed. Confirmed
2026-07-30 against the primary source and filed per Dev-Research Compositing at
`~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-motherduck-context-layer/`.
Both may be cited as fact in the new doc sections, WITH their source URL:
  - `https://motherduck.com/blog/context-belongs-in-the-warehouse` (2026-07-29):
    Guides improve an agent's accuracy by 72 percentage points on the DABStep
    agentic analytics benchmark and lower costs by about 55 percent versus agents
    discovering context on their own.
  - `https://motherduck.com/blog/oops-maybe-we-do-need-semantic-layers`: an A/B
    result where baking domain knowledge INTO the warehouse capped at 93 percent
    accuracy while moving it to a separate curated layer reached 100 percent.
Cite them as MotherDuck's measurements with the URL attached. Never present either
as a MindrianOS measurement. The second is the stronger, falsifiable framing and
should carry the argumentative weight.
</locked_decisions>

<planner_resolutions>
RESOLUTION 1 (locked). Insert NEW numbered sub-sections; do NOT renumber the
existing sections. `docs/MWP-SPECIFICATION.md` gets `## 2.8 Semantic Layer vs
Context Layer` immediately before `## 3. The 9 KuzuDB Edge Types` (currently line
343). `docs/BRAIN-MD-SCHEMA.md` gets `## 5.1 Semantic Layer vs Context Layer`
immediately before `## 6. Version History` (currently line 196). Renumbering would
touch every downstream cross-reference and would make the mutation proof ambiguous.

RESOLUTION 2 (locked). Do NOT rename the existing `## 3. The 9 KuzuDB Edge Types`
heading, even though the correction banner at `docs/MWP-SPECIFICATION.md:3` already
declares KuzuDB retired and the heading is itself a stale claim. That rename is a
separate doc-hygiene change; folding it in here makes this plan's mutation proof
ambiguous. Plan 240.1-07 registers it as a deferred item.

RESOLUTION 3 (locked). The em-dash and en-dash sweep in this plan's test covers
CTXL-02's OWN surfaces only: `docs/MWP-SPECIFICATION.md`,
`docs/BRAIN-MD-SCHEMA.md`, and the test file itself. It deliberately does NOT
include `lib/core/state-ops.cjs`, which carries a literal em-dash on line 2 that
plan 240.1-02 fixes under its own gate. Including it here would make this plan's
gate red for a reason it does not own and would couple two otherwise independent
wave-1 plans. State this in the test header.
</planner_resolutions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the doctrine-presence gate RED-first and prove it is red for the right reason</name>
  <files>tests/test-240.1-layer-doctrine-presence.cjs</files>
  <read_first>
    - tests/test-143.2-doctrine-presence.cjs (the WHOLE file: the PRESENCE table at :51-59, the assertion loop, the focused-region grep idiom at :181-193, the em-dash sweep at :216-219, the false-clean-illusion header at :14-19, and the house rule at :20-23 about \u escapes)
    - .planning/phases/240.1-context-layer-drift-detection/240.1-RESEARCH.md (Pitfall 4, the vacuous-pass failure mode this gate must not have; section 2D, the measured hit counts)
    - .planning/phases/240.1-context-layer-drift-detection/240.1-PATTERNS.md (section "NEW tests/test-240.1-layer-doctrine-presence.cjs")
    - docs/MWP-SPECIFICATION.md (line 3 correction banner, the `## ` heading list, line 343)
    - docs/BRAIN-MD-SCHEMA.md (the `## ` heading list, section 5 at :153-195)
  </read_first>
  <behavior>
  - The gate locates each new section by its EXACT heading literal, slices from
    that heading to the next `\n## `, and runs every token assertion against THAT
    SLICE ONLY. A token appearing anywhere else in the file does not satisfy it.
  - CTXL-02a (`docs/MWP-SPECIFICATION.md`, slice `## 2.8 Semantic Layer vs Context
    Layer`): the slice must contain all of `SEMANTIC layer`, `CONTEXT layer`,
    `INDEXER_OWNED_NODE_TYPES`, `INDEXER_OWNED_EDGE_TYPES`, `rebuildGraph`,
    `lazygraph-ops.cjs`, `GRAPHDB-01`, `room.db`, and `SQLite`. It must NOT contain
    `KuzuDB`.
  - CTXL-02b (`docs/BRAIN-MD-SCHEMA.md`, slice `## 5.1 Semantic Layer vs Context
    Layer`): the same nine must-contain tokens plus `BRAIN.md`, and must NOT
    contain `KuzuDB`.
  - CTXL-02c (non-weakening fence): the slice `## 5. Canon Part 8 Contract
    (CRITICAL)` in `docs/BRAIN-MD-SCHEMA.md` still contains its ALLOWED and
    FORBIDDEN contract tokens, so inserting 5.1 cannot have eaten or diluted it.
  - CTXL-02d (source-truth fence): `lib/core/lazygraph-ops.cjs` still declares both
    `INDEXER_OWNED_NODE_TYPES` and `INDEXER_OWNED_EDGE_TYPES`, so the doctrine
    cannot outlive the code it names. If Phase 236's allowlist is ever renamed, this
    gate reddens and the docs get corrected with it.
  - Dash sweep: zero U+2014 and zero U+2013 across the three CTXL-02 surfaces only.
  </behavior>
  <action>
Create `tests/test-240.1-layer-doctrine-presence.cjs`, modeled structurally on
`tests/test-143.2-doctrine-presence.cjs`. `node:assert/strict`, CJS, no
dependencies.

Implement `sliceSection(body, headingLiteral)`: find the exact heading literal,
assert it was found with a message naming the file and the heading, then return the
substring from that index to the next occurrence of `\n## ` (or end of file). This
is the direct answer to Pitfall 4: the bare words `semantic` and `context` already
appear 9 and 11 times in `docs/MWP-SPECIFICATION.md`, so a naive whole-file
`mustContain` on them would be GREEN before any work is done. No `mustContain`
token in this gate may be a bare common word; anchor only on distinguishing tokens.

Build a declarative `PRESENCE` table with `id`, `file`, `section`, `mustContain`,
and `mustNotContain`, and a single assertion loop over it, exactly as the 143.2
analog does.

Port the false-clean-illusion header warning in 240.1's own words: this gate
asserts the layer distinction is PRESENT and DISTINGUISHED, and deliberately does
NOT assert that `KuzuDB` is absent from `docs/MWP-SPECIFICATION.md` as a whole. The
9 surviving mentions below the correction banner at `:3` are out of scope to rip
out here, and a too-eager whole-file `mustNotContain` would produce a gate that
fights unrelated legacy prose while saying nothing about whether the doctrine
landed. The `mustNotContain: ['KuzuDB']` is scoped to the NEW section slices only.
Note also that `scripts/check-kuzu-reintroduction.cjs` scans only `.cjs`, `.js`,
and `.mjs` and explicitly excludes `docs/*.md`, so the machine gate will NOT catch
a KuzuDB slip in this prose. This test is the only thing that will.

Port the dash sweep, scoped per Resolution 3 to
`['docs/MWP-SPECIFICATION.md', 'docs/BRAIN-MD-SCHEMA.md',
'tests/test-240.1-layer-doctrine-presence.cjs']`. Follow the house rule from
`tests/test-143.2-doctrine-presence.cjs:20-23`: the forbidden code points appear in
this file ONLY as the six-character escape sequences backslash-u-2014 and
backslash-u-2013, never as literal characters.

End with a `PASS test-240.1-layer-doctrine-presence.cjs (<n> assertions)` line and
`process.exit(0)`.

Run it. It MUST be RED at the end of this task, because neither new section exists.
Transcribe the exact RED output in the summary: the failure must name the missing
heading literal, not a syntax error and not a missing file. Pitfall 4's explicit
warning sign is a gate that passes on its first run; a gate that is red for the
wrong reason is not a proof either.
  </action>
  <verify>
    <automated>node tests/test-240.1-layer-doctrine-presence.cjs; test $? -ne 0</automated>
  </verify>
  <done>
The gate exists, is syntactically valid, and exits nonzero with a message naming
`## 2.8 Semantic Layer vs Context Layer` (or the BRAIN.md equivalent) as not found
in its target file. Its own dash sweep passes for the test file itself.
  </done>
  <acceptance_criteria>
    - behavior: `node tests/test-240.1-layer-doctrine-presence.cjs` exits nonzero and the message names a missing heading literal
    - source: the file contains a `sliceSection` function and every `mustContain` assertion runs against a slice, never against the whole file body
    - source: no `mustContain` entry is a bare common word; `semantic` and `context` appear only inside the multi-word anchors `SEMANTIC layer` and `CONTEXT layer`
    - source: `mustNotContain: ['KuzuDB']` appears only in slice-scoped entries; no whole-file KuzuDB assertion exists
    - source: the dash sweep list contains exactly the three CTXL-02 surfaces and does NOT contain `lib/core/state-ops.cjs`
    - source: the file names the forbidden code points only via backslash-u escape sequences and contains zero literal U+2014 or U+2013 characters
    - source: the header states the false-clean-illusion mitigation in 240.1's own words and notes that `check-kuzu-reintroduction.cjs` excludes `.md`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write both layer-distinction sections, turn the gate green, and mutation-prove it</name>
  <files>docs/MWP-SPECIFICATION.md, docs/BRAIN-MD-SCHEMA.md</files>
  <read_first>
    - lib/core/lazygraph-ops.cjs (lines 75-135: `INDEXER_OWNED_NODE_TYPES`, `INDEXER_OWNED_EDGE_TYPES`, and the scoped DELETE at :131 that enforces the boundary; line 31 for `EDGE_TYPES`)
    - docs/BRAIN-MD-SCHEMA.md (section 5 at :153-195: the prose shape to match, an ALLOWED/FORBIDDEN table pinned to the enforcing code)
    - docs/MWP-SPECIFICATION.md (line 3 correction banner; `## 2. The Seven MWP Layers` at :45; `## 3. The 9 KuzuDB Edge Types` at :343)
    - ~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-motherduck-context-layer/2026-07-30-motherduck-context-layer.md (the verified figures and the exact source URLs)
    - .planning/REQUIREMENTS.md (line 21: the GRAPHDB-01 prose that states the fix "was DELETE SCOPE and not atomicity", which is the layer-conflation statement this doctrine names)
  </read_first>
  <action>
Write `## 2.8 Semantic Layer vs Context Layer` into `docs/MWP-SPECIFICATION.md`,
inserted immediately before `## 3. The 9 KuzuDB Edge Types`. Do not rename or
renumber anything (Resolutions 1 and 2).

Match the register of `docs/BRAIN-MD-SCHEMA.md:153-195`: a short statement of the
rule, then a two-column table, then the file:line that enforces it. Content:

  - The rule: room.db is one generic property graph (`nodes(id, type, properties)`,
    `edges(source, target, type, properties)`), and the layer separation is by
    `type` string discrimination, never by table or column. The boundary is
    therefore not visible in the DDL; it lives in an ownership allowlist.
  - The table. SEMANTIC layer (schema and structure): `Artifact`, `Section`,
    `BELONGS_TO`; derivable from the filesystem; safe to wipe and re-derive on every
    `rebuildGraph`. CONTEXT layer (institutional knowledge): `memory_event`,
    human-confirmed truth-claim nodes, decisions, opportunity `stage_history`, and
    the other 22 of the 23 `EDGE_TYPES`; irreplaceable, human-confirmed or
    session-accumulated; must survive `rebuildGraph`.
  - The enforcement: cite `lib/core/lazygraph-ops.cjs:81` (the
    `INDEXER_OWNED_NODE_TYPES` declaration), `:84`
    (`INDEXER_OWNED_EDGE_TYPES`), and `:131` (the ownership-scoped DELETE) by
    file:line.
  - The incident that proves it is load-bearing: Phase 236 / GRAPHDB-01 existed
    because the indexer treated context-layer rows as semantic-layer rows and
    truncated them. The fix was DELETE SCOPE, not atomicity
    (`.planning/REQUIREMENTS.md:21`). Phase 240 then pinned survival with a join
    test.
  - Canon linkage: this is the operational expression of Canon Part 9 Memory
    Locality ("only a human confirms a truth-claim node").
  - External corroboration, per D-02, with the URLs attached and attributed to
    MotherDuck, never to MindrianOS: the Guides announcement's +72pp accuracy and
    about -55 percent cost-per-run DABStep result, and, carrying the argumentative
    weight, the A/B result where baking domain knowledge into the warehouse capped
    at 93 percent accuracy while moving it to a separate curated layer reached 100
    percent. The point to draw: mixing the two layers costs measurable accuracy,
    not just conceptual tidiness.

HARD CONSTRAINT: write the new prose against SQLite and `room/.room-graph/room.db`.
The word `KuzuDB` must not appear anywhere in the new section. The body of this doc
still says KuzuDB in nine places below its own correction banner, so the surrounding
prose is a trap; do not copy its vocabulary.

Then write `## 5.1 Semantic Layer vs Context Layer` into `docs/BRAIN-MD-SCHEMA.md`,
inserted after section 5 and immediately before `## 6. Version History`. Same rule,
same enforcement citations, plus the BRAIN.md-specific half: which BRAIN.md content
is DERIVED and regenerable by `/mos:brain-derive` (the nine optional analysis
section headings, re-derivable from the graph) versus which frontmatter and content
carries institutional state that must not be silently regenerated. Note that
BRAIN.md is already ahead of `STATE.md` here: it carries `brain_graph_version` in
its required frontmatter, a graph-version stamp `STATE.md` lacked until CTXL-01.
Do not weaken, reword, or move section 5's ALLOWED/FORBIDDEN Canon Part 8 contract;
the gate fences it.

Run `node tests/test-240.1-layer-doctrine-presence.cjs`. It must now be GREEN.

Then run the MUTATION PROOF and transcribe both directions in the summary:
  RED:   delete the `INDEXER_OWNED_NODE_TYPES` mention from the
         `docs/MWP-SPECIFICATION.md` 2.8 slice ONLY. The gate must go red naming
         that token and that file.
  GREEN: restore it, re-run, confirm green.
Then a second, sharper direction proving the slice scoping is real rather than
incidental:
  RED:   move the `INDEXER_OWNED_NODE_TYPES` mention OUT of the 2.8 slice and into
         an unrelated section of the same file. The gate must STILL go red,
         because the token must live inside the slice. Restore.
This second direction is what proves the gate cannot pass vacuously on a token that
merely exists somewhere in a 534-line document.
  </action>
  <verify>
    <automated>node tests/test-240.1-layer-doctrine-presence.cjs</automated>
  </verify>
  <done>
Both sections exist at their specified insertion points, both name and distinguish
the two layers, both cite `lazygraph-ops.cjs` by file:line, and neither contains
`KuzuDB`. The gate is green. Both mutation directions were executed and
transcribed. No existing heading was renamed or renumbered.
  </done>
  <acceptance_criteria>
    - test-command: `node tests/test-240.1-layer-doctrine-presence.cjs` exits 0
    - source: `docs/MWP-SPECIFICATION.md` contains `## 2.8 Semantic Layer vs Context Layer` positioned before `## 3. The 9 KuzuDB Edge Types`
    - source: `docs/BRAIN-MD-SCHEMA.md` contains `## 5.1 Semantic Layer vs Context Layer` positioned after `## 5. Canon Part 8 Contract (CRITICAL)` and before `## 6. Version History`
    - source: the section-2.8 slice contains `INDEXER_OWNED_NODE_TYPES`, `INDEXER_OWNED_EDGE_TYPES`, `rebuildGraph`, `lazygraph-ops.cjs`, `GRAPHDB-01`, `SQLite`, `room.db` and does not contain `KuzuDB`
    - source: both new sections cite `motherduck.com` URLs and attribute the figures to MotherDuck, not to MindrianOS
    - source: `grep -c '^## 3. The 9 KuzuDB Edge Types' docs/MWP-SPECIFICATION.md` returns 1 (heading untouched, Resolution 2)
    - source: `docs/BRAIN-MD-SCHEMA.md` section 5's ALLOWED and FORBIDDEN contract text is byte-unchanged
    - behavior: deleting `INDEXER_OWNED_NODE_TYPES` from the 2.8 slice turns the gate red (transcribed)
    - behavior: moving `INDEXER_OWNED_NODE_TYPES` out of the 2.8 slice into another section of the same file ALSO turns the gate red (transcribed; this proves the slice scoping)
    - source: zero U+2014 and zero U+2013 in both docs
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> future implementer | Doc prose that misstates the enforcing code produces a wrong implementation later |
| doc file -> doctrine gate | The gate reads first-party doc bytes; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-240.1-13 | Spoofing | A doctrine gate that passes vacuously | mitigate | Heading-anchored slicing plus distinguishing-token-only anchors; the RED-first observation and the move-the-token-out mutation both prove the scoping bites |
| T-240.1-14 | Tampering | Reintroducing KuzuDB as a current claim | mitigate | `mustNotContain: ['KuzuDB']` scoped to the new slices; noted in the test header that `check-kuzu-reintroduction.cjs` excludes `.md` and cannot catch this |
| T-240.1-15 | Repudiation | Restating MotherDuck's figures as MindrianOS measurements | mitigate | D-02 requires the source URL inline and explicit attribution to MotherDuck |
| T-240.1-16 | Information disclosure | Weakening the Canon Part 8 ALLOWED/FORBIDDEN contract while inserting nearby | mitigate | The gate's CTXL-02c leg fences section 5's contract tokens; the acceptance criteria require it byte-unchanged |
| T-240.1-SC | Tampering | npm/pip/cargo installs | accept | Zero package-manager operations; audit recorded N/A for the phase |
</threat_model>

<verification>
- `node tests/test-240.1-layer-doctrine-presence.cjs` exits 0.
- `node scripts/check-kuzu-reintroduction.cjs` still exits 0 (this plan touches no `.cjs`, `.js`, or `.mjs` source, so the gate's scan scope is unchanged).
- `git diff --stat` shows changes confined to the three files in `files_modified`.
- Zero U+2014 and U+2013 across all three files.
- `bash tests/run-all-2401.sh` discovers this test file and runs it.

</verification>

<success_criteria>
1. Both docs carry an explicit, documented SEMANTIC-vs-CONTEXT layer distinction naming `INDEXER_OWNED_NODE_TYPES` as the concrete operational boundary (ROADMAP SC2).
2. The doctrine-presence gate exists in the `143.2-doctrine-presence` idiom and asserts both terms are present AND distinguished.
3. The gate was observed RED before the doc edits and GREEN after, with both mutation directions executed and transcribed.
4. No room.db schema change and no migration, as Finding 2B established none is needed.
5. No KuzuDB claim introduced; no existing heading renamed or renumbered.
</success_criteria>

## Artifacts this phase produces

This plan's contribution. Full phase index is in `240.1-01-PLAN.md`.

| Symbol / path | Kind |
|---------------|------|
| `tests/test-240.1-layer-doctrine-presence.cjs` | new test (doctrine-presence gate) |
| `sliceSection(body, headingLiteral)` | function inside that test (heading-anchored region slicer) |
| `## 2.8 Semantic Layer vs Context Layer` | new section in `docs/MWP-SPECIFICATION.md` |
| `## 5.1 Semantic Layer vs Context Layer` | new section in `docs/BRAIN-MD-SCHEMA.md` |

<output>
Create `.planning/phases/240.1-context-layer-drift-detection/240.1-04-SUMMARY.md` when done.
</output>
