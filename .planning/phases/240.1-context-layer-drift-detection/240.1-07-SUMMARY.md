---
phase: 240.1-context-layer-drift-detection
plan: 07
subsystem: phase close-out / residual register / Dev-Research Compositing
tags: [ctxl-01, ctxl-02, ctxl-03, deferred-items, rca, dev-research-compositing, phase-gate]

dependency-graph:
  requires:
    - phase: 240.1-context-layer-drift-detection
      plan: 03
      provides: "the six-plus-site STATE.md write-site census this register's Item 2/5/6 cite"
    - phase: 240.1-context-layer-drift-detection
      plan: 04
      provides: "the SEMANTIC-vs-CONTEXT doctrine this register's Item 3 and the research entry's section 4 cite"
    - phase: 240.1-context-layer-drift-detection
      plan: 06
      provides: "the ctxl-eval.cjs null-delta design this register's Item 8 and the research entry's section 5 cite"
  provides:
    - "the phase residual register (deferred-items.md, 8 items)"
    - "the room-birth compute-state RCA (.planning/debug/room-birth-compute-state-node-on-bash.md)"
    - "the Dev-Research Compositing trail at ~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-context-layer-drift-detection/, cross-linked both directions with the MotherDuck entry"
    - "an executed, fully-recorded phase gate: tests/run-all-2401.sh, statusline-active-room-write regression fence, doctor --acceptance, tests/run-all-240.sh, check-kuzu-reintroduction.cjs, and a repo-wide dash sweep"
  affects: []

tech-stack:
  added: []
  patterns:
    - "Phase residual register (the 240-memory deferred-items.md format): every out-of-scope item gets what/why/evidence/what-closing-it-would-take"
    - "docs/RCA-TEMPLATE.md kind:rca filing for a defect found but deliberately not fixed in-scope"
    - "Dev-Research Compositing: same finding, two homes, cross-linked in both directions (the phase dir gets the actionable record, the rethinking-mindrianos room gets the durable reasoning trail)"

key-files:
  created:
    - .planning/phases/240.1-context-layer-drift-detection/deferred-items.md
    - .planning/debug/room-birth-compute-state-node-on-bash.md
  modified: []

decisions:
  - "Item 1 of the residual register states plainly and with double RCA citation that .planning/STATE.md's own drift is NOT fixed by this phase -- it lives in gsd-core, an external tool, and both existing RCAs already concluded no in-repo code change is possible."
  - "The room-birth compute-state defect (node invoking a bash script) is filed as its own RCA with status: investigating (root cause confirmed, fix deferred), not status: resolved, since no fix was applied in this session."
  - "The Dev-Research Compositing entry is a NEW dated room entry recording this phase's own measured outcome, not a duplicate of the already-filed MotherDuck source-verification entry; both now carry reciprocal cross-links."
  - "package-lock.json's regeneration and the .mindrian-npm-install.lock file, both silently produced by node scripts/doctor.cjs --acceptance's own package-lock-sync check (the same D-240-14 class Phase 240's own close-out plan hit), were discarded before the final commit per this plan's own KNOWN REPO QUIRKS instruction."

metrics:
  duration: "~90 minutes"
  completed: "2026-07-30"
---

# Phase 240.1 Plan 07: Close-out Summary

**Registered the phase's own honest residual (8 items, led by the explicit non-fix of `.planning/STATE.md`), filed the room-birth compute-state-on-node defect as its own RCA, composited this phase's outcome into the rethinking-mindrianos Data Room cross-linked in both directions with the MotherDuck source entry, and ran the full phase gate green end to end.**

## What Was Built

**Task 1 -- the phase residual register.** `.planning/phases/240.1-context-layer-drift-detection/deferred-items.md` (8 items, matching the `240-memory/deferred-items.md` format). Item 1 is the load-bearing one: it states, quoting both existing RCAs verbatim (`gsd-tools-state-resync-clobbers-stopped-at-frontmatter.md`'s "None in this repo" verdict and `gsd-phase-complete-cross-phase-corruption.md`'s "Not filed -- root cause unconfirmed, and the tool lives outside this repo" verdict), that the `.planning/STATE.md` symptom that motivated this phase is NOT fixed here, and traces the upstream mechanism live against the installed `~/.claude/gsd-core/bin/lib/state.cjs`: `writeStateMd` derives frontmatter via `syncStateFrontmatter` (a disk-scan-derived read) BEFORE `acquireStateLock` runs (`state.cjs:1091-1092`), and `gsd_state_version` is hardcoded to the literal `'1.0'` on every regeneration (`state.cjs:918`), preserved at exactly one call site (`:1485`, the milestone-switch path). Items 2-8 cover the room-birth defect (previewed here, filed properly in Task 2), the stale `## 3. The 9 KuzuDB Edge Types` heading in `docs/MWP-SPECIFICATION.md` (confirmed `scripts/check-kuzu-reintroduction.cjs` cannot catch it -- its own source excludes `.md` files), the `opportunity-ops.cjs` STATE.md variant (a different artifact shape, out of scope by 240.1-03's Resolution 4), the no-backfill legacy-forward policy for existing rooms, a reviewed-no-change confirmation of `room-skeleton-scaffold.cjs`'s template writer, the stale `CHANGELOG.md:806` "single Node chokepoint" claim (historical record, not rewritten), and a re-confirmation that the three pre-existing test failures Phase 240 already tracked (`test-memory-command.cjs`, `test-hmi-compliance-e2e.cjs`, `test-graph-derivation-verdict.cjs`) have not changed shape.

**Task 2 -- the room-birth compute-state RCA.** `.planning/debug/room-birth-compute-state-node-on-bash.md`, filed per `docs/RCA-TEMPLATE.md` with `kind: rca`, `status: investigating`. Documents that `lib/core/navigation/room-birth.cjs:986-990` spawns `scripts/compute-state` (a bash script) with the `node` interpreter, producing a deterministic `SyntaxError` on the shebang comment line, silently swallowed by a bare `catch (_e)` with a "Tolerate" comment at `:991-994`. Reproduced live this session: `node scripts/compute-state /tmp` raises `SyntaxError: Invalid or unexpected token` pointing at line 2. Cites `lib/core/intelligence-cascade.cjs:492-503`'s `execFileSync('bash', [...])` shape as the correct in-repo analog and the recommended fix direction. `lib/core/navigation/room-birth.cjs` itself is confirmed untouched (`git diff` empty).

**Task 3 -- Dev-Research Compositing and the full phase gate.** A new dated entry, `~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-context-layer-drift-detection/2026-07-30-context-layer-drift-detection.md` (195 lines), records this phase's own measured outcome: the empirical version-destruction repro, the six-plus-site write census with its three planning-time corrections (the `room-registry`'s preserving `_write_current_room` upsert being prior art rather than a defect; the `current_room` preservation trap that would have broken the no-backfill contract; the two absent-guarded seeders in `vault-import.cjs` and `room-skeleton-scaffold.cjs` reviewed-no-change), the structurally-blind-validator finding (the most transferable lesson: a PostToolUse hook matched on `Write|Edit|MultiEdit` cannot see any of the seven programmatic/shell STATE.md writers by construction), the semantic-vs-context boundary naming (`INDEXER_OWNED_NODE_TYPES` was already the operational boundary before Phase 240.1 named it), and CTXL-03's honest result (the committed gate proves the measurement apparatus is sound via mutation testing; the live A/B leg was never exercised in this repo's own CI in this phase, stated as a limitation, not hidden; the 8-task synthetic corpus is sized for a deterministic CI-safe gate, not for statistical power at production scale). Cross-linked in both directions with `2026-07-30-motherduck-context-layer/2026-07-30-motherduck-context-layer.md` (that entry now carries a reciprocal "Reciprocal cross-link (added at phase close-out)" paragraph) and with the phase directory. Neither research-room file is tracked by this repo's git (they live entirely under `~/MindrianRooms/`, outside the worktree); per the worktree-isolation guard's restriction on complex commands targeting external paths, both were authored as scratch files inside the worktree via the `Write` tool, then placed at their external destinations with a single `cp` command (the same workaround Phase 240's own close-out plan used), and the scratch files were deleted immediately after.

## Phase Gate Results

Every command run and its exact result, per the plan's `<verification>` list:

1. **`bash tests/run-all-2401.sh`** -- `Phase 240.1: PASS=8 FAIL=0 SKIP=0`. All eight Phase 240.1 test files discovered by glob (`test-240.1-state-version-preserved.cjs`, `test-240.1-state-version-drift-gate.cjs`, `test-240.1-hook-write-site.sh`, `test-240.1-layer-doctrine-presence.cjs`, `test-240.1-ctxl-fixture.cjs`, `test-240.1-ctxl-eval-gate.cjs`, plus the glob-discipline and scope-escape self-tests), zero failures.
2. **`node lib/memory/statusline-active-room-write.test.cjs`** -- `statusline-active-room-write: 7/7 tests passed`, matching the plan's stated baseline exactly.
3. **`node scripts/doctor.cjs --acceptance`** -- `Acceptance full: 15/15 points passed.`
4. **`bash tests/run-all-240.sh`** -- `Phase 240: PASS=8 FAIL=0 SKIP=0`, matching Phase 240's own close-out baseline (no regression introduced by Phase 240.1).
5. **`node scripts/check-kuzu-reintroduction.cjs`** -- `check-kuzu-reintroduction: PASS`. "Scanned 2 dependency manifest(s) and 1961 source file(s). No live kuzu dependency and no live kuzu require/import found." Exit 0.
6. **Repo-wide dash sweep over every Phase 240.1-touched file** -- all 24 production/test/doc files this phase's own commits created or modified (`docs/BRAIN-MD-SCHEMA.md`, `docs/MWP-SPECIFICATION.md`, `lib/core/frontmatter-schemas.cjs`, `lib/core/intelligence-cascade.cjs`, `lib/core/state-ops.cjs`, `lib/core/state-version.cjs`, `scripts/compute-state`, `scripts/ctxl-eval.cjs`, `scripts/on-agent-complete`, `scripts/on-stop`, `scripts/on-task-complete`, `scripts/state-write.cjs`, every `tests/test-240.1-*` and `tests/run-all-2401.sh` file, plus this plan's own two new files) confirmed zero U+2014 and zero U+2013 via a direct Python codepoint scan. Both new Dev-Research Compositing room entries also confirmed zero U+2014 and zero U+2013.

**Classified exceptions to the dash sweep, none blocking.** Four files carry literal em/en-dash characters, all pre-existing and all out of this plan's edit scope:
- `.planning/STATE.md` lines 1589/1593 ("Phase 240.1 -- context-layer-drift-detection") -- written by the orchestrator's own wave-tracking commits, not by any Task in this plan; this plan is explicitly instructed not to touch `STATE.md`.
- `.planning/phases/240.1-context-layer-drift-detection/240.1-01-SUMMARY.md:91`, `240.1-06-SUMMARY.md:97-98`, and `240.1-PATTERNS.md:25` -- each is prose ALREADY COMMITTED by a prior plan's executor, quoting a literal em/en-dash character as part of describing a dash-related bug that was found and fixed elsewhere (the `state-ops.cjs` header em-dash Correction 2 named, and `ctxl-eval.cjs`'s own em-dash constants before they were converted to `\u` escapes). None of these three files is in this plan's `files_modified` list, and retroactively editing another plan's already-committed SUMMARY is out of this plan's authority.

## Task Commits

1. **Task 1: Write the phase residual register** -- `3c8feb05` (docs)
2. **Task 2: File the room-birth compute-state RCA** -- `151ccec7` (docs)
3. **Task 3: Dev-Research Compositing and the full phase gate** -- no new commit in this worktree. Both room-entry files live entirely outside the repo (`~/MindrianRooms/rethinking-mindrianos/research/`) and are not tracked by this repo's git; the phase gate itself changed no tracked file (the one transient side effect, `package-lock.json`'s silent regeneration by `doctor --acceptance`'s own package-lock-sync check plus a `.mindrian-npm-install.lock` artifact, was reverted/removed before this SUMMARY was written, per this plan's own KNOWN REPO QUIRKS instruction). `git status --short` is empty at the time of this SUMMARY.

**Plan metadata:** per this plan's own objective, `.planning/STATE.md`, `.planning/ROADMAP.md`, and `.planning/REQUIREMENTS.md` are NOT touched by this executor. `git diff --stat .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md` is empty. The orchestrator owns those writes after this wave completes.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking issue] `doctor --acceptance` silently regenerated `package-lock.json` and left a `.mindrian-npm-install.lock` artifact**
- **Found during:** Task 3, immediately after running `node scripts/doctor.cjs --acceptance` for the phase gate.
- **Issue:** the acceptance suite's own "Package-lock sync (vendored-deps lockstep)" check silently updated `package-lock.json`'s `version` and root-package `version` fields in place from a stale `1.15.3-beta.11` to the current `1.15.3-beta.51`, and left a new untracked `.mindrian-npm-install.lock` file. This is the same class the plan's own KNOWN REPO QUIRKS section warned about, and the same D-240-14 pattern Phase 240's own close-out plan hit.
- **Fix:** `git checkout -- package-lock.json` to discard the regeneration; `rm -f .mindrian-npm-install.lock` to remove the lock artifact. Confirmed `git status --short` empty afterward.
- **Verification:** re-checked `git status --short` before writing this SUMMARY -- empty.
- **Files modified:** none (the reversion restored the pre-existing state; nothing was committed).

**2. [Rule 3 - Blocking issue, self-corrected before commit] RCA filed with `status: resolved` initially, corrected to `status: investigating`**
- **Found during:** Task 2, immediately after the initial `Write` of the RCA file.
- **Issue:** the RCA's frontmatter was first written as `status: resolved`, which per `docs/RCA-TEMPLATE.md`'s lifecycle (`gathering -> investigating -> fixing -> resolved`) implies a fix was implemented and verified, and that the file should be moved to `.planning/debug/resolved/`. No fix was applied in this session (deliberately, per this phase's own scope boundary).
- **Fix:** corrected `status` to `investigating` (root cause confirmed by live reproduction, fix not yet applied) before committing, and reworded the "Process note" in Non-Code Follow-ups to match, so the file's own lifecycle claim is honest and the file correctly stays in `.planning/debug/` (not `.planning/debug/resolved/`) for a future session to pick up.
- **Verification:** re-read the file after the edit; frontmatter and prose now agree.
- **Files modified:** `.planning/debug/room-birth-compute-state-node-on-bash.md` (corrected before its only commit, `151ccec7` -- no separate fix commit needed).

No other deviations. Both fixes were caught and corrected within the same task, before any commit landed the incorrect state.

## Known Stubs

None. Both new files (the residual register and the RCA) are fully authored with real file:line citations, live reproductions, and verbatim quotes, not placeholders. Both Dev-Research Compositing room entries are complete prose with cross-links confirmed by grep in both directions.

## Threat Flags

None. This plan's own threat model (T-240.1-26 through T-240.1-29, T-240.1-SC) is fully addressed by construction: Item 1 of the residual register mitigates T-240.1-26 (the spoofing risk of appearing to have fixed `.planning/STATE.md`) by stating plainly it was not fixed; the RCA mitigates T-240.1-27 (repudiation) with a live reproduction and file:line citations; every classified pre-existing test failure was re-confirmed live against the current tree rather than assumed (T-240.1-28); and the Dev-Research Compositing entries carry no secrets or credentials, consistent with T-240.1-29's accepted disposition. No new trust-boundary surface was introduced -- this plan is documentation, an RCA filing, and a read-only phase-gate run.

## Self-Check: PASSED

- FOUND: `.planning/phases/240.1-context-layer-drift-detection/deferred-items.md`
- FOUND: `.planning/debug/room-birth-compute-state-node-on-bash.md`
- FOUND: `~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-context-layer-drift-detection/2026-07-30-context-layer-drift-detection.md` (195 lines, 0 em-dashes, 0 en-dashes)
- FOUND: reciprocal cross-link in `~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-motherduck-context-layer/2026-07-30-motherduck-context-layer.md`
- FOUND: commit `3c8feb05` (Task 1)
- FOUND: commit `151ccec7` (Task 2)
- FOUND: `git status --short` empty at time of this check
- FOUND: `git diff --stat .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md` empty
