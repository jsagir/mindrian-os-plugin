---
phase: 240.1-context-layer-drift-detection
plan: 07
type: execute
wave: 3
depends_on: [240.1-03, 240.1-04, 240.1-06]
files_modified:
  - .planning/phases/240.1-context-layer-drift-detection/deferred-items.md
  - .planning/debug/room-birth-compute-state-node-on-bash.md
autonomous: true
requirements: [CTXL-01, CTXL-02, CTXL-03]
canon_parts: [6, 7, 8]

must_haves:
  truths:
    - "The phase does not appear to have fixed the .planning/STATE.md symptom that motivated it, because it did not"
    - "The room-birth compute-state invocation that has never once succeeded is filed as its own RCA, not silently folded in"
    - "The Dev-Research Compositing mandate is satisfied: findings live in the phase dir AND the rethinking-mindrianos room, cross-linked"
    - "The full phase gate is green and the regression battery matches its documented baseline"
  artifacts:
    - path: ".planning/phases/240.1-context-layer-drift-detection/deferred-items.md"
      provides: "the phase residual register: every out-of-scope item with a written disposition"
      min_lines: 60
    - path: ".planning/debug/room-birth-compute-state-node-on-bash.md"
      provides: "RCA for a silent-skip instance found during research and deliberately not folded into this phase"
      min_lines: 40
  key_links:
    - from: ".planning/phases/240.1-context-layer-drift-detection/deferred-items.md"
      to: ".planning/debug/gsd-tools-state-resync-clobbers-stopped-at-frontmatter.md"
      via: "cited RCA for the deferred external half"
      pattern: "gsd-tools-state-resync"
    - from: "~/MindrianRooms/rethinking-mindrianos/research/"
      to: ".planning/phases/240.1-context-layer-drift-detection/"
      via: "cross-linked dated research entry"
      pattern: "240.1"
---

<objective>
Close the phase honestly. Three of this phase's most valuable outputs are things it
deliberately did NOT do, and if they are not written down the phase reads as having
fixed a symptom it never touched. Register every residual with a written disposition,
file the one genuine new defect research found as its own RCA, satisfy the
Dev-Research Compositing mandate, and run the full phase gate.

Purpose: `240.1-RESEARCH.md`'s single most important planning warning is that the
requirement text named two different files both called `STATE.md`, and that the
symptom the ROADMAP describes lives in the half this repo cannot fix. Locked decision
D-01 scoped the phase to the fixable half. That decision is only safe if it is
recorded where the next reader will find it, citing both existing RCAs, so nobody
later reads "CTXL-01 closed" and concludes the `.planning/STATE.md` corruption is
gone. It is not.

Output: one residual register, one new RCA, one cross-linked research entry, one
green phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/240.1-context-layer-drift-detection/240.1-RESEARCH.md
@.planning/phases/240.1-context-layer-drift-detection/240.1-03-SUMMARY.md
@.planning/phases/240.1-context-layer-drift-detection/240.1-04-SUMMARY.md
@.planning/phases/240.1-context-layer-drift-detection/240.1-06-SUMMARY.md
@.planning/phases/240-memory/deferred-items.md
@docs/RCA-TEMPLATE.md
</context>

<locked_decisions>
D-01 (LOCKED): CTXL-01 targeted the per-room `STATE.md`. `.planning/STATE.md` is
`gsd-core` territory, external, already root-caused by two RCAs with the verdict
"Required Code Changes: None in this repo." This plan's Task 1 is where that
decision becomes a permanent, discoverable record rather than a planning-time
conversation.

D-02 (LOCKED): MotherDuck's DABStep figures are VERIFIED and were filed at
`~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-motherduck-context-layer/`
before planning. Task 3 extends that trail with this phase's own outcome rather than
creating a competing entry for the same source material.
</locked_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Write the phase residual register with a disposition for every out-of-scope item</name>
  <files>.planning/phases/240.1-context-layer-drift-detection/deferred-items.md</files>
  <read_first>
    - .planning/phases/240-memory/deferred-items.md (the in-repo format and register register to match)
    - .planning/phases/240.1-context-layer-drift-detection/240.1-RESEARCH.md (Finding 1A for the external half and both RCA verdicts; Finding 1C for the census and the room-birth bug; Open Questions 2 and 4)
    - .planning/debug/gsd-tools-state-resync-clobbers-stopped-at-frontmatter.md (its explicit "Required Code Changes: None in this repo" verdict, to quote accurately)
    - .planning/debug/gsd-phase-complete-cross-phase-corruption.md (its "Not filed - root cause unconfirmed, and the tool lives outside this repo" verdict)
    - the three plan summaries this plan depends on (for the census dispositions each executor recorded)
  </read_first>
  <action>
Create `.planning/phases/240.1-context-layer-drift-detection/deferred-items.md`. Match
the format of `.planning/phases/240-memory/deferred-items.md`. Every item gets: what
it is, why it was out of scope, the evidence, and what would have to be true to pick
it up.

Item 1, the most important one. THE `.planning/STATE.md` HALF IS NOT FIXED. State
plainly that the symptom that motivated this phase (a `stopped_at` versus Current
Position self-contradiction observed while two concurrent sessions were writing) lives
in `.planning/STATE.md`, which is generated by `gsd-core` at `~/.claude/gsd-core/`, a
separately versioned tool this repo does not track. Quote both existing RCAs' verdicts
verbatim with their filenames. Record the traced upstream mechanism so the trail is not
lost: `gsd-core`'s `writeStateMd` derives frontmatter via `syncStateFrontmatter` from a
disk-scan cache BEFORE `acquireStateLock` runs (`state.cjs:1091-1092`), so two
concurrent sessions can each derive from the same pre-write state and then serialize
their writes. Note that `gsd_state_version` already exists in that file and is
completely inert, hardcoded on every regeneration at `state.cjs:918` and preserved at
only one call site, `:1485`. State what this phase DID do instead per D-01, and state
that any future fix belongs upstream, not here.

Item 2. `lib/core/navigation/room-birth.cjs:986-990` invokes a bash script with `node`
and has therefore never once succeeded, silently, wrapped in a bare `catch (_e)` with a
"Tolerate" comment at `:992-995`. Filed as its own RCA by Task 2. Not fixed here because
folding an unrelated fix into this phase would blur its mutation proofs
(`240.1-RESEARCH.md` Open Question 4). Note it is an instance of the standing
silent-skip and false-success watch item.

Item 3. `docs/MWP-SPECIFICATION.md`'s heading `## 3. The 9 KuzuDB Edge Types` is itself
a stale-KuzuDB claim, contradicted by that file's own correction banner at `:3`, and the
body carries nine more KuzuDB mentions. Deliberately not renamed by plan 240.1-04
(its Resolution 2) because it would make that plan's mutation proof ambiguous. Note that
`scripts/check-kuzu-reintroduction.cjs` cannot catch these: it scans only `.cjs`, `.js`,
and `.mjs` and explicitly excludes `docs/*.md`.

Item 4. `lib/core/opportunity-ops.cjs:1020` and `:1109` write a `STATE.md` for fund and
opportunity sub-directories. A different artifact shape, not a room-root `STATE.md`.
Declared out of scope by plan 240.1-03's Resolution 4 and confirmed untouched.

Item 5. Existing rooms on disk under `~/MindrianRooms/` carry no version stamp today,
because any prior `compute-state` run destroyed it. Per `240.1-RESEARCH.md` Open
Question 2 the chosen policy is treat-absent-as-legacy and stamp forward on the next
regeneration, which is idempotent and needs no migration script. Record that no bulk
backfill was run and none is needed.

Item 6. `lib/core/room-skeleton-scaffold.cjs:300-310` writes
`templates/room-skeleton/STATE.md.tmpl`, which carries no version stamp. Reviewed, no
change: `isStateAuthored` at `:120-136` returns true for any frontmatter lacking
`auto_created`, so a registry-seeded room is never clobbered, and a scaffold-born room
is stamped forward on its first regeneration by the legacy branch. Record it so the
census is complete.

Item 7. `CHANGELOG.md:806` carries the same false "single Node chokepoint" claim that
plan 240.1-02 corrected in `lib/core/state-ops.cjs`. Historical changelog entries are
not rewritten; record the discrepancy so a future reader who greps the changelog is not
misled.

Item 8. Any pre-existing unrelated test failures observed while running this phase's
regression battery, with the evidence that they are pre-existing (confirmed against a
clean checkout, not assumed). If there are none, say so explicitly rather than omitting
the section.
  </action>
  <verify>
    <automated>test -f .planning/phases/240.1-context-layer-drift-detection/deferred-items.md &amp;&amp; grep -q "None in this repo" .planning/phases/240.1-context-layer-drift-detection/deferred-items.md</automated>
  </verify>
  <done>
The register exists with all eight items, each carrying a written disposition. Item 1
quotes both RCA verdicts verbatim with their filenames and states plainly that the
`.planning/STATE.md` symptom is NOT fixed by this phase.
  </done>
  <acceptance_criteria>
    - source: the file contains all eight items, each with a disposition
    - source: item 1 names both `gsd-tools-state-resync-clobbers-stopped-at-frontmatter.md` and `gsd-phase-complete-cross-phase-corruption.md` and quotes their verdicts
    - source: item 1 states explicitly that this phase did NOT fix `.planning/STATE.md`
    - source: item 1 records the `state.cjs:1091-1092` lock-ordering mechanism and the inert stamp at `:918`
    - source: item 3 notes that `check-kuzu-reintroduction.cjs` excludes `.md` and cannot catch the stale heading
    - source: zero U+2014 and zero U+2013
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: File the room-birth compute-state RCA</name>
  <files>.planning/debug/room-birth-compute-state-node-on-bash.md</files>
  <read_first>
    - docs/RCA-TEMPLATE.md (the required RCA structure; do not improvise a bug report)
    - lib/core/navigation/room-birth.cjs (lines 975-1000: STEP 3, the `execSync('node ' + computeStateScript + ...)` invocation, the `_pathSafetyReason` guard above it, and the bare `catch (_e)` with the "Tolerate" comment below it)
    - lib/core/intelligence-cascade.cjs (lines 492-498: the correct `execFileSync('bash', [...])` shape, for the recommended fix)
    - scripts/compute-state (line 1: the `#!/usr/bin/env bash` shebang that makes the node invocation impossible)
  </read_first>
  <action>
Create `.planning/debug/room-birth-compute-state-node-on-bash.md` following
`docs/RCA-TEMPLATE.md` with `kind: rca`.

The defect: `lib/core/navigation/room-birth.cjs:986-990` invokes
`scripts/compute-state` with `node`. That script's shebang is `#!/usr/bin/env bash`
(`scripts/compute-state:1`) and its second line is a bash comment, so Node raises a
SyntaxError immediately. Verified live during research:
`node scripts/compute-state /tmp` fails parsing a bash comment. The call is wrapped in
a bare `catch (_e)` with a "Tolerate" comment at `:992-995`, so room birth's STEP 3
compute-state has never once succeeded and fails completely silently.

Include: the reproduction command and its exact output; the blast radius (a newly born
room's `STATE.md` is never computed at birth, so it stays at whatever the seeder wrote
until the first hook-driven regeneration); the contrast with the correct shape at
`lib/core/intelligence-cascade.cjs:492`, which uses `execFileSync('bash', [scriptPath,
roomDir], {...})` with an args array; and the recommended fix (convert to the
`execFileSync('bash', [...])` shape and replace the bare catch with one that writes a
line, per the never-throw-but-never-silent rule this phase adopted).

Classify it against the standing watch item: this is a silent-skip and false-success
instance, the class `feedback_false_success_silent_skip_gates_academy_testers` tracks.

State clearly why it was NOT fixed in Phase 240.1: it is not CTXL-01, CTXL-02, or
CTXL-03, and folding an unrelated fix in would blur this phase's mutation proofs
(`240.1-RESEARCH.md` Open Question 4's recommendation, followed).

`.planning/` is gitignored except `.planning/debug/`, so confirm the file is tracked;
use `git add -f` if needed.
  </action>
  <verify>
    <automated>test -f .planning/debug/room-birth-compute-state-node-on-bash.md &amp;&amp; grep -q "room-birth.cjs:986" .planning/debug/room-birth-compute-state-node-on-bash.md</automated>
  </verify>
  <done>
The RCA exists, follows `docs/RCA-TEMPLATE.md`, carries the live reproduction and its
output, names the recommended fix and the correct in-repo analog, and is tracked by git.
`lib/core/navigation/room-birth.cjs` itself is untouched.
  </done>
  <acceptance_criteria>
    - source: the RCA follows `docs/RCA-TEMPLATE.md`'s structure with `kind: rca`
    - source: it contains the reproduction command `node scripts/compute-state` and the observed SyntaxError
    - source: it cites `lib/core/intelligence-cascade.cjs:492` as the correct analog
    - source: it states why the fix was deferred out of Phase 240.1
    - behavior: `git ls-files .planning/debug/room-birth-compute-state-node-on-bash.md` returns the path (the file is tracked)
    - source: `git diff lib/core/navigation/room-birth.cjs` is empty
    - source: zero U+2014 and zero U+2013
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Satisfy Dev-Research Compositing and run the full phase gate</name>
  <files>~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-context-layer-drift-detection/2026-07-30-context-layer-drift-detection.md, ~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-motherduck-context-layer/2026-07-30-motherduck-context-layer.md</files>
  <read_first>
    - CLAUDE.md (the Dev-Research Compositing section: findings go in BOTH the phase dir AND the rethinking-mindrianos room, cross-linked)
    - ~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-motherduck-context-layer/2026-07-30-motherduck-context-layer.md (the existing entry to extend and cross-link, not duplicate)
    - the three plan summaries this plan depends on (for the measured outcomes to record)
    - .planning/phases/240-memory/240-06-SUMMARY.md (the precedent for how a phase close-out plan recorded its compositing and gate run)
  </read_first>
  <action>
Dev-Research Compositing. The MotherDuck source material was already filed at
`~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-motherduck-context-layer/`
before planning (that is what closed research BLOCKED #1 and produced D-02). Do NOT
create a competing entry for the same source. Instead create a NEW dated entry,
`~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-context-layer-drift-detection/`,
recording this phase's OWN outcome, and cross-link the two in both directions.

The new entry records what the phase actually measured, which is the durable
MindrianOS-dev finding:
  - the empirical version-destruction repro and the six-plus-site write census, including
    the three census corrections found at planning time (the preserving
    `_write_current_room` upsert in `scripts/room-registry`; the `current_room`
    preservation trap that would have broken the no-backfill contract; the two additional
    absent-guarded seeders in `scripts/vault-import.cjs` and
    `lib/core/room-skeleton-scaffold.cjs`)
  - the finding that the repo's existing drift-notification surface
    (`scripts/frontmatter-schema-validator.cjs`, matcher `Write|Edit|MultiEdit`) is
    STRUCTURALLY BLIND to programmatic and shell writers, which is why the drift was
    invisible for so long. This is the most transferable lesson in the phase: a hook-based
    validator only sees tool calls, so any artifact written by code is unvalidated by
    construction.
  - the semantic-versus-context boundary as named in the two docs, and the observation that
    `INDEXER_OWNED_NODE_TYPES` was already the boundary before anyone named it
  - the CTXL-03 result: whether a delta was measurable at MindrianOS's scale, stated
    honestly including a null result, with the sample-size limitation attached
  - a cross-link back to `.planning/phases/240.1-context-layer-drift-detection/` and to the
    MotherDuck entry
Add the reciprocal cross-link into the existing MotherDuck entry so the trail is
navigable from either end.

Then run the FULL PHASE GATE and record every command and its exact output in the
summary:
  - `bash tests/run-all-2401.sh` (expect all six Phase 240.1 test files discovered, zero
    FAIL; the `.sh` leg tallied through `run_may_skip`)
  - `node lib/memory/statusline-active-room-write.test.cjs` (expect `7/7 tests passed`;
    this is the closest pre-existing fence to the CTXL-01 change, baseline measured at
    planning time as 7/7)
  - `node scripts/doctor.cjs --acceptance`
  - `bash tests/run-all-240.sh` (expect unchanged from its Phase 240 close-out baseline,
    proving Phase 240.1 regressed nothing)
  - `node scripts/check-kuzu-reintroduction.cjs` (expect exit 0)
  - a repo-wide dash sweep over every file this phase touched: zero U+2014 and zero U+2013

Any failure in the battery must be classified before the phase can close: WORKING, a
known tracked bug, ENV GAP, or NEW FAILURE (the CLAUDE.md QA classification rule). A
pre-existing failure must be CONFIRMED pre-existing against a clean checkout, never
assumed. Record the classification and the evidence for each.

Do NOT update `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, or
`.planning/STATE.md` checkboxes in this plan. Phase-level closure is the orchestrator's
step after `gsd-verifier` runs, and this milestone's standing discipline is that
tracking writes are hand-edited and diff-reviewed at that step, not by an executor.
  </action>
  <verify>
    <automated>bash tests/run-all-2401.sh &amp;&amp; node lib/memory/statusline-active-room-write.test.cjs &amp;&amp; node scripts/check-kuzu-reintroduction.cjs</automated>
  </verify>
  <done>
The new dated research entry exists and is cross-linked in both directions with the
MotherDuck entry and with the phase directory. Every phase-gate command was run and its
exact output transcribed. Any failure is classified with evidence. No ROADMAP,
REQUIREMENTS, or STATE checkbox was touched.
  </done>
  <acceptance_criteria>
    - behavior: `~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-context-layer-drift-detection/` exists and contains a dated entry file
    - source: the new entry cross-links to `.planning/phases/240.1-context-layer-drift-detection/` and to the MotherDuck entry
    - source: the existing MotherDuck entry carries a reciprocal cross-link to the new entry
    - source: the new entry records the structurally-blind-validator finding and all three planning-time census corrections
    - test-command: `bash tests/run-all-2401.sh` exits 0 with six Phase 240.1 test files discovered and FAIL=0
    - test-command: `node lib/memory/statusline-active-room-write.test.cjs` prints `7/7 tests passed`
    - test-command: `bash tests/run-all-240.sh` matches its Phase 240 close-out baseline
    - test-command: `node scripts/check-kuzu-reintroduction.cjs` exits 0
    - behavior: `git diff .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md` is empty
    - source: zero U+2014 and zero U+2013 across every file this phase touched
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase record -> future reader | A close-out that omits what was not done produces a false belief that a symptom is fixed |
| repo -> rethinking-mindrianos room | Dev-Research Compositing writes outside the repo, into a user room |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-240.1-26 | Spoofing | The phase appearing to have fixed `.planning/STATE.md` | mitigate | Item 1 of the residual register states plainly that it did not, quoting both existing RCA verdicts |
| T-240.1-27 | Repudiation | A silently swallowed defect losing its evidence trail | mitigate | The room-birth defect is filed as its own RCA with a live reproduction, per `docs/RCA-TEMPLATE.md` |
| T-240.1-28 | Spoofing | A pre-existing failure assumed rather than confirmed | mitigate | The CLAUDE.md QA classification rule is applied per failure, with confirmation against a clean checkout required |
| T-240.1-29 | Information disclosure | Writing repo internals into a user room | accept | The rethinking-mindrianos room is the designated MindrianOS-dev consultant room and this is a mandated CLAUDE.md workflow; the entry carries no secrets and no credentials |
| T-240.1-SC | Tampering | npm/pip/cargo installs | accept | Zero package-manager operations; audit recorded N/A for the phase |
</threat_model>

<verification>
- `bash tests/run-all-2401.sh` exits 0, six Phase 240.1 test files discovered, FAIL=0.
- `node lib/memory/statusline-active-room-write.test.cjs` prints `7/7 tests passed`.
- `node scripts/doctor.cjs --acceptance` run and its output recorded.
- `bash tests/run-all-240.sh` matches its Phase 240 baseline.
- `node scripts/check-kuzu-reintroduction.cjs` exits 0.
- Both `.planning/debug/room-birth-compute-state-node-on-bash.md` and the deferred register exist; the RCA is git-tracked.
- `git diff .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md` is empty.
</verification>

<success_criteria>
1. Every out-of-scope item carries a written disposition, and the `.planning/STATE.md` half is explicitly recorded as NOT fixed, citing both existing RCAs.
2. The room-birth silent-skip defect is filed as its own RCA with a live reproduction, and its production file is untouched.
3. Dev-Research Compositing is satisfied: a new dated entry in the rethinking-mindrianos room, cross-linked in both directions.
4. The full phase gate is green, every command's output transcribed, and any failure classified with evidence.
5. No tracking-file checkbox was touched by an executor.
</success_criteria>

## Artifacts this phase produces

This plan's contribution. Full phase index is in `240.1-01-PLAN.md`.

| Symbol / path | Kind |
|---------------|------|
| `.planning/phases/240.1-context-layer-drift-detection/deferred-items.md` | new phase residual register (8 items) |
| `.planning/debug/room-birth-compute-state-node-on-bash.md` | new RCA (git-tracked via `git add -f`) |
| `~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-context-layer-drift-detection/` | new dated research entry, cross-linked both ways |

<output>
Create `.planning/phases/240.1-context-layer-drift-detection/240.1-07-SUMMARY.md` when done.
</output>
