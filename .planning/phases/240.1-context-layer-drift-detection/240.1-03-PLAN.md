---
phase: 240.1-context-layer-drift-detection
plan: 03
type: execute
wave: 2
depends_on: [240.1-02]
files_modified:
  - scripts/state-write.cjs
  - scripts/compute-state
  - scripts/on-stop
  - scripts/on-task-complete
  - scripts/on-agent-complete
  - lib/core/intelligence-cascade.cjs
  - tests/test-240.1-hook-write-site.sh
autonomous: true
requirements: [CTXL-01]
canon_parts: [7, 11]

must_haves:
  truths:
    - "The version stamp survives a regeneration driven by a REAL hook write site, not only by a direct computeState() call"
    - "scripts/on-agent-complete no longer truncates STATE.md to zero before compute-state runs"
    - "A version mismatch reaching a bash hook produces a durable record even when the hook's stdout is discarded"
    - "The cascade's Step 8 can report version-mismatch as a distinct outcome from ok and error"
  artifacts:
    - path: "scripts/state-write.cjs"
      provides: "the bash-to-Node persistence entry point; reads the rendered body from stdin, always exits 0"
      min_lines: 45
    - path: "tests/test-240.1-hook-write-site.sh"
      provides: "Pitfall 2's antidote: end-to-end proof at a real hook write site"
      min_lines: 90
  key_links:
    - from: "scripts/on-stop"
      to: "scripts/state-write.cjs"
      via: "printf piped into node"
      pattern: "state-write\\.cjs"
    - from: "scripts/state-write.cjs"
      to: "lib/core/state-version.cjs"
      via: "require + persistState"
      pattern: "persistState"
    - from: "lib/core/intelligence-cascade.cjs"
      to: "lib/core/state-version.cjs"
      via: "persistState verdict routed into stepsResult.computeState"
      pattern: "persistState\\("
---

<objective>
Finish CTXL-01's coverage across the write-site census. Plan 240.1-02 wired ONE of
six sites. Five bypass it. This plan builds the bash-to-Node bridge
(`scripts/state-write.cjs`), converts the three hook scripts and the cascade step
to use it, corrects the false assurance in `scripts/compute-state`'s own comment,
and proves coverage END TO END at a real hook site rather than at
`computeState()` alone.

Purpose: `240.1-RESEARCH.md` Finding 1C measured that `CHANGELOG.md:806`'s "single
Node chokepoint" claim is false, and Pitfall 2 names the exact failure this plan
prevents: the stamp preserved on one path and silently destroyed on the other five,
with the phase gate green while the bug persists in the hook-driven paths users
actually hit. A test that only calls `computeState()` is the documented warning
sign.

Output: one new script, five modified files, one new integration test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/240.1-context-layer-drift-detection/240.1-RESEARCH.md
@.planning/phases/240.1-context-layer-drift-detection/240.1-PATTERNS.md
@.planning/phases/240.1-context-layer-drift-detection/240.1-02-SUMMARY.md
@lib/core/state-version.cjs
@scripts/on-stop
@scripts/on-agent-complete
</context>

<locked_decisions>
D-01 (LOCKED): CTXL-01 targets the PER-ROOM `STATE.md`. `.planning/STATE.md` is
out of scope; no file in this plan may reference `~/.claude/gsd-core`. Plan
240.1-01's scope-escape tripwire covers `scripts/state-write.cjs` and
`tests/test-240.1-hook-write-site.sh` by name.
</locked_decisions>

<planner_resolutions>
RESOLUTION 1 (design, locked before execution). The bash sites pipe the rendered
body into a small Node CLI rather than having the CLI re-spawn `compute-state`
itself. Each hook keeps its existing capture and its existing failure fallback
prose byte-for-byte; only the PERSISTENCE line changes. This is the smallest
correct diff and it preserves the capture-then-write shape `240.1-RESEARCH.md`
calls canonical.

RESOLUTION 2 (locked). Do NOT implement the version compare inside
`scripts/compute-state`. That script is a pure render-to-stdout and never reads
the target file, and `scripts/on-agent-complete`'s redirect truncates STATE.md to
zero BEFORE it runs. A comparison there would read an empty file. This is Pitfall
3's explicit warning sign. `scripts/compute-state` gets a comment correction only,
no logic change.

RESOLUTION 3 (locked). Write site 6, `lib/core/navigation/room-birth.cjs:986-990`,
is NOT fixed in this phase. It invokes a bash script with `node` and has therefore
never once succeeded (proven live: `node scripts/compute-state /tmp` raises a
SyntaxError on a bash comment), swallowed by a bare `catch (_e)` at `:992-995`. It
is a real "silent skip / false success" instance but it is not CTXL-01, and folding
an unrelated fix in here would blur this plan's mutation proofs. Plan 240.1-07
files it as its own RCA and registers it as a deferred item. Do not touch
`room-birth.cjs` in this plan; an empty `git diff` on that file is an acceptance
criterion.

RESOLUTION 4 (locked). `lib/core/opportunity-ops.cjs:1020` and `:1109` write a
`STATE.md` for fund and opportunity SUB-directories. Those are a different artifact
shape, not a room-root STATE.md, and are declared OUT OF SCOPE. Do not touch them;
record the disposition in the summary census.
</planner_resolutions>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/state-write.cjs and correct scripts/compute-state's false assurance</name>
  <files>scripts/state-write.cjs, scripts/compute-state</files>
  <read_first>
    - lib/core/state-version.cjs (the module from plan 240.1-02: `persistState`'s signature and verdict envelope)
    - scripts/skillopt-eval.cjs (lines 1-40 and the `main(argv)` tail: the repo's switch-case argv router convention, no Commander and no yargs)
    - scripts/compute-state (lines 1-45 for the shebang and the truncation warning already documented there, lines 238-246 for the frontmatter emit and its "this cannot clobber siblings" comment)
    - scripts/frontmatter-schema-validator.cjs (lines 1-30: the ALWAYS-exits-0 advisory contract this script must match)
  </read_first>
  <action>
Create `scripts/state-write.cjs` (executable, `chmod +x`, `#!/usr/bin/env node`,
`'use strict'`, CJS, zero new dependencies).

Contract, stated in its own header: `Usage: <rendered body on stdin> | node
scripts/state-write.cjs <roomDir>`. It reads the rendered STATE.md body from stdin
to EOF, calls `persistState(roomDir, body)` from `lib/core/state-version.cjs`, and
ALWAYS exits 0. It is ADVISORY, NOT BLOCKING, exactly like
`scripts/frontmatter-schema-validator.cjs`: a version-mismatch check that throws or
exits nonzero inside `on-stop` would break Larry's turn on all three surfaces.

Output discipline. On `status === 'ok'` print nothing (hooks are quiet on the happy
path). On `status === 'version-mismatch'` write ONE line to stderr naming the file,
the on-disk version, the module version, and the advice `run /mos:doctor --fix`. On
`status === 'error'` write ONE line to stderr naming the message. Never silent on a
non-ok verdict: the counter-lesson at `lib/core/navigation/room-birth.cjs:992-995`
is that a bare swallow is how a write site fails invisibly for months. The durable
record is the `schema-violations.jsonl` append `persistState` already performs, so
the signal survives even where a caller discards stderr.

Argument handling: exactly one positional argument, the room dir. Missing or empty
argument prints one usage line to stderr and still exits 0 (advisory contract). Do
not add flags; this script has one job.

Then edit `scripts/compute-state`, comment-only, no logic change. The block at
`:238-240` currently reads "Because the block is emitted fresh, this cannot clobber
siblings." That is the exact false assurance that hid this bug: it is true WITHIN
the frontmatter block and false ABOUT the block as a whole, because the whole prior
frontmatter is discarded by whatever caller redirects the output. Replace it with a
correction that states: this script is a PURE RENDER TO STDOUT and never reads the
target file; the emitted frontmatter deliberately carries NO version stamp; version
preservation and persistence belong to `lib/core/state-version.cjs::persistState`,
which every write site must call; and a manual `bash scripts/compute-state <room> >
STATE.md` bypasses that and will drop the stamp.
  </action>
  <verify>
    <automated>printf -- '---\ncomputed: x\n---\n\n# Data Room State\n' | node scripts/state-write.cjs "$(mktemp -d)"; test $? -eq 0</automated>
  </verify>
  <done>
`scripts/state-write.cjs` exists, is executable, persists a body from stdin through
`persistState`, and exits 0 on every input including a missing argument and an
unwritable room dir. `scripts/compute-state`'s comment no longer carries the false
"cannot clobber siblings" assurance and names `lib/core/state-version.cjs` as the
persistence authority. `bash -n scripts/compute-state` exits 0.
  </done>
  <acceptance_criteria>
    - source: `scripts/state-write.cjs` requires `lib/core/state-version.cjs` and calls `persistState`
    - source: `scripts/state-write.cjs` contains no `process.exit(1)` and no `process.exit(2)`
    - behavior: piping a rendered body into it against a temp room dir writes `STATE.md` and exits 0
    - behavior: invoked with no argument it prints one usage line to stderr and exits 0
    - behavior: against a room whose STATE.md carries `gsd_state_version: 2.0`, it prints one stderr line containing `/mos:doctor --fix`, exits 0, and leaves the file bytes unchanged
    - source: `scripts/compute-state` no longer contains the string `this cannot clobber siblings` and does contain `state-version.cjs`
    - source: `scripts/compute-state` has no new logic; `git diff scripts/compute-state` shows comment lines only
    - test-command: `bash -n scripts/compute-state` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Convert the three hook write sites and the cascade step</name>
  <files>scripts/on-stop, scripts/on-task-complete, scripts/on-agent-complete, lib/core/intelligence-cascade.cjs</files>
  <read_first>
    - scripts/on-stop (lines 138-150: the canonical capture-then-write shape)
    - scripts/on-task-complete (lines 45-60: the same shape with an empty-guard)
    - scripts/on-agent-complete (lines 126-140: the odd one out, a truncating redirect inside a background subshell)
    - lib/core/intelligence-cascade.cjs (lines 484-505: Step 8, the `execFileSync('bash', [...])` spawn and its `{ status: 'ok' | 'error' }` result envelope)
    - scripts/state-write.cjs (the bridge built in Task 1)
  </read_first>
  <action>
Four edits. In every bash edit, keep the existing capture and the existing failure
fallback prose byte-for-byte; change ONLY the persistence line (Resolution 1).

Site 3, `scripts/on-stop:144-145`. Replace
`printf '%s\n' "$state_content" > "${ROOM_DIR}/STATE.md"` with a pipe of the same
`printf` into `node "${SCRIPT_DIR}/state-write.cjs" "$ROOM_DIR"`, followed by
`|| true`. Do NOT redirect stderr to `/dev/null` here: the advisory line is the
deliverable, and the script already always exits 0.

Site 4, `scripts/on-task-complete:51-55`. Same conversion inside the existing
`if [ -n "$state_content" ]` empty-guard. Keep the guard.

Site 5, `scripts/on-agent-complete:131-134`. This one changes SHAPE, not just the
final line. It currently runs
`"${SCRIPT_DIR}/compute-state" "$ROOM_DIR" > "${ROOM_DIR}/STATE.md"`, and the shell
truncates STATE.md to zero BEFORE compute-state runs, which is documented at
`scripts/compute-state:29-33` and is Pitfall 3. Convert it to the capture-then-pipe
shape used at `on-stop`: capture into a variable first, then pipe into
`state-write.cjs`. Keep the surrounding `( sleep 1 ; ... ) &` background subshell
and keep its `2>/dev/null || true`, and add a comment stating WHY the stderr
discard is acceptable at this one site: a background subshell's stderr is not
user-visible anyway, and the durable signal for this site is the
`schema-violations.jsonl` append that `persistState` performs, not stdout.

Site 2, `lib/core/intelligence-cascade.cjs:499-503`. Keep the existing
`execFileSync('bash', [scriptPath, roomDir], {...})` spawn exactly as it is: an
args array is the Security-Domain-approved spawn shape and this site is already
correct. Replace only the `fs.writeFileSync(path.join(roomDir, 'STATE.md'),
computedState)` plus `stepsResult.computeState = { status: 'ok' }` pair with a
`persistState(roomDir, computedState)` call whose verdict is routed into the
existing envelope, so `{ status: 'version-mismatch' }` becomes a third tellable
outcome alongside `ok` and `error` without inventing a new envelope shape. Keep the
outer `catch` writing `{ status: 'error', message: e.message }`.

Run `bash -n` on all three bash scripts and `node --check` on the cascade module
before committing.
  </action>
  <verify>
    <automated>bash -n scripts/on-stop &amp;&amp; bash -n scripts/on-task-complete &amp;&amp; bash -n scripts/on-agent-complete &amp;&amp; node --check lib/core/intelligence-cascade.cjs</automated>
  </verify>
  <done>
All four sites route persistence through `persistState`. `scripts/on-agent-complete`
no longer contains a `> "${ROOM_DIR}/STATE.md"` redirect. All three bash scripts
parse. The cascade module parses and its Step 8 can emit a `version-mismatch`
status.
  </done>
  <acceptance_criteria>
    - source: `scripts/on-stop`, `scripts/on-task-complete`, and `scripts/on-agent-complete` each contain `state-write.cjs`
    - source: no bash file under `scripts/` contains the substring `> "${ROOM_DIR}/STATE.md"`
    - source: `scripts/on-agent-complete` retains its `( ... ) &` background subshell and carries the comment explaining the stderr discard at that one site
    - source: `lib/core/intelligence-cascade.cjs` contains `persistState(` and no longer contains `fs.writeFileSync(path.join(roomDir, 'STATE.md')`
    - source: `lib/core/intelligence-cascade.cjs` still contains `execFileSync('bash'` (the approved spawn shape is unchanged)
    - source: `git diff lib/core/navigation/room-birth.cjs` is empty (Resolution 3)
    - source: `git diff lib/core/opportunity-ops.cjs` is empty (Resolution 4)
    - test-command: `bash -n` passes on all three hook scripts; `node --check lib/core/intelligence-cascade.cjs` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Prove coverage at a real hook write site and run the mutation proofs</name>
  <files>tests/test-240.1-hook-write-site.sh</files>
  <read_first>
    - tests/test-127-03-acceptance-gates.sh (lines 25-45: the sandboxed `HOME` plus `env -u` shape, and `mktemp -d -t`)
    - tests/test-jtbd-auto-anchor-empirical.sh (the `set -euo pipefail` single-suite convention and its cleanup trap)
    - scripts/compute-state (line 37: `ROOMS_HOME="${MINDRIAN_ROOMS_HOME:-$HOME/MindrianRooms}"`, which is why a sandboxed HOME alone is sufficient)
    - scripts/room-registry (lines 118-140: the exact seeded STATE.md bytes to reproduce in the sandbox)
    - scripts/on-agent-complete (the converted site, for the background-subshell timing this test must wait out)
  </read_first>
  <action>
Create `tests/test-240.1-hook-write-site.sh` with `set -euo pipefail` and a cleanup
`trap` removing every path it creates.

This test exists for one reason, stated in its header: `240.1-RESEARCH.md` Pitfall
2's documented warning sign is "a test that only calls `computeState()`." Plan
240.1-02's tests are exactly that. This one drives REAL hook scripts end to end.

Sandbox: `TMPROOT="$(mktemp -d -t ctxl2401-XXXXXX)"`, then run every leg with
`HOME="$TMPROOT"` and `env -u MINDRIAN_ROOMS_HOME`, so
`scripts/compute-state:37`'s `ROOMS_HOME` default resolves inside the sandbox.
Nothing under the real `~/MindrianRooms/` may be read or written; assert that
`$HOME` inside each leg is the sandbox before running anything.

Seed `$TMPROOT/MindrianRooms/<slug>/` with `.room-root`, `.mindrian/`, `ROOM.md`,
a `.rooms/registry.json` naming the room active, and a `STATE.md` carrying the
byte-identical `room-registry:127-131` block (`gsd_state_version: 1.0`,
`status: active`).

Three scenarios:
  1. `scripts/on-task-complete` driven against the seeded room. After it returns,
     `STATE.md` still carries `gsd_state_version: 1.0` AND `status: active` AND a
     freshly regenerated `computed:` line.
  2. `scripts/on-stop` driven against the same room. Same three assertions.
  3. `scripts/on-agent-complete` driven against the same room. This is the hardest
     leg: the write happens inside a `( sleep 1 ; ... ) &` background subshell, so
     poll for the regenerated `computed:` line with a bounded wait (poll every
     200ms up to 10 seconds) rather than a fixed sleep, then assert the same three
     conditions. If the bounded wait expires, FAIL with a message naming the
     background subshell, never SKIP.

Environment SKIP discipline: if a leg genuinely cannot run in this environment
(for example a required interpreter is absent), print a line beginning with the
literal `SKIP` and exit 0, so `tests/run-all-2401.sh`'s `run_may_skip` tallies it
as SKIP and never as a pass it did not earn. A missing assertion is a FAIL, not a
SKIP. Do not use SKIP to paper over a real failure.

Then run TWO mutation proofs and transcribe both directions in the summary:
  M1: revert `scripts/on-agent-complete` to the truncating
      `"${SCRIPT_DIR}/compute-state" "$ROOM_DIR" > "${ROOM_DIR}/STATE.md"` redirect.
      Scenario 3 must go RED. Restore and confirm GREEN.
  M2: revert `scripts/on-task-complete`'s pipe to the bare
      `printf '%s\n' "$state_content" > "${ROOM_DIR}/STATE.md"`. Scenario 1 must go
      RED while `tests/test-240.1-state-version-preserved.cjs` stays GREEN. That
      divergence is the whole point of this file and must be stated explicitly in
      the summary: it is the measured proof that Pitfall 2 is real and that a
      computeState-only test would have passed over a live bug.

Confirm both files are byte-identical to their fixed state afterwards.
  </action>
  <verify>
    <automated>bash tests/test-240.1-hook-write-site.sh</automated>
  </verify>
  <done>
All three scenarios pass against real hook scripts in a sandboxed HOME. Both
mutation proofs were executed and transcribed, including the M2 divergence showing
the plan 240.1-02 test staying green while this one reddens. `bash
tests/run-all-2401.sh` discovers three Phase 240.1 files and the `.sh` leg runs
through `run_may_skip`.
  </done>
  <acceptance_criteria>
    - test-command: `bash tests/test-240.1-hook-write-site.sh` exits 0 and prints no line beginning with `SKIP`
    - source: the test contains `mktemp -d`, `HOME="$TMPROOT"`, `env -u MINDRIAN_ROOMS_HOME`, and a cleanup `trap`
    - source: the test contains zero occurrences of the literal path `~/MindrianRooms` outside a comment
    - behavior: scenario 3 uses a bounded poll (not a fixed sleep) and FAILS rather than SKIPs when the bounded wait expires
    - behavior: with `scripts/on-agent-complete` reverted to the truncating redirect, this test exits nonzero (transcribed)
    - behavior: with `scripts/on-task-complete` reverted to the bare redirect, this test exits nonzero WHILE `node tests/test-240.1-state-version-preserved.cjs` still exits 0 (transcribed; this is the Pitfall 2 divergence proof)
    - test-command: `bash tests/run-all-2401.sh` reports the `.sh` leg through `run_may_skip`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook event -> bash script -> Node CLI | Room path and rendered body cross a process boundary |
| stdin -> `scripts/state-write.cjs` | Untrusted-shaped text of arbitrary length |
| sandboxed HOME -> real HOME | The integration test must never cross back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-240.1-09 | Tampering | Command injection via room path | mitigate | The bash sites pass `"$ROOM_DIR"` as a quoted single argv entry; the cascade keeps `execFileSync('bash', [args])` with an args array, never string concatenation |
| T-240.1-10 | Denial of service | A nonzero exit inside a Stop or SubagentStop hook | mitigate | `scripts/state-write.cjs` ALWAYS exits 0; every call site keeps `|| true` |
| T-240.1-11 | Tampering | The truncating redirect destroying data before it can be read | mitigate | `scripts/on-agent-complete` converted to capture-then-pipe; scenario 3 proves it and M1 proves the gate reddens on revert |
| T-240.1-12 | Information disclosure | Test leaking into the live rooms store | mitigate | Sandboxed `HOME` plus `env -u MINDRIAN_ROOMS_HOME`; an assertion that `$HOME` is the sandbox runs before any leg |
| T-240.1-SC | Tampering | npm/pip/cargo installs | accept | Zero package-manager operations; audit recorded N/A for the phase |
</threat_model>

<verification>
- `bash tests/test-240.1-hook-write-site.sh` exits 0.
- `node tests/test-240.1-state-version-preserved.cjs` and `node tests/test-240.1-state-version-drift-gate.cjs` still exit 0 (plan 240.1-02 not regressed).
- `node lib/memory/statusline-active-room-write.test.cjs` still prints `7/7 tests passed`.
- `bash -n` passes on `scripts/on-stop`, `scripts/on-task-complete`, `scripts/on-agent-complete`, `scripts/compute-state`.
- `git diff lib/core/navigation/room-birth.cjs` and `git diff lib/core/opportunity-ops.cjs` are both empty.
- Zero U+2014 and U+2013 across all seven files in `files_modified`.

</verification>

<success_criteria>
1. The version stamp survives a regeneration driven by each of the three real hook scripts, proven by an executed integration test (ROADMAP SC1's coverage half).
2. `scripts/on-agent-complete` no longer truncates STATE.md before rendering.
3. Two mutation proofs executed in both directions, including the M2 divergence that measures Pitfall 2 rather than merely citing it.
4. `scripts/compute-state` remains a pure renderer; its false "cannot clobber siblings" assurance is gone.
5. Sites 6 (`room-birth.cjs`) and the opportunity sub-artifact writers are provably untouched, with written dispositions.
</success_criteria>

## Artifacts this phase produces

This plan's contribution. Full phase index is in `240.1-01-PLAN.md`.

| Symbol / path | Kind |
|---------------|------|
| `scripts/state-write.cjs` | new CLI script (stdin body plus roomDir argv, always exits 0) |
| `tests/test-240.1-hook-write-site.sh` | new integration test |
| `{ status: 'version-mismatch' }` on `stepsResult.computeState` | new outcome on an existing cascade envelope |

<output>
Create `.planning/phases/240.1-context-layer-drift-detection/240.1-03-SUMMARY.md` when done.
</output>
