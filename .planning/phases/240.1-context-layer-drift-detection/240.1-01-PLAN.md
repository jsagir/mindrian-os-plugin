---
phase: 240.1-context-layer-drift-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/run-all-2401.sh
autonomous: true
requirements: [CTXL-01, CTXL-02, CTXL-03]
canon_parts: [6, 7]

must_haves:
  truths:
    - "A single command gates all three CTXL requirements: bash tests/run-all-2401.sh"
    - "The aggregator discovers Phase 240.1 tests by glob, never by a hand-maintained list"
    - "An empty discovery FAILS loudly instead of printing a green summary"
    - "The aggregator provably cannot silently re-run Phase 240's suite and report it as 240.1 coverage"
    - "The locked CTXL-01 scope decision is machine-checked, not just written in prose"
  artifacts:
    - path: "tests/run-all-2401.sh"
      provides: "Phase 240.1 glob-discovery aggregator plus the glob-discipline and scope-escape legs"
      min_lines: 120
      contains: "found -eq 0"
  key_links:
    - from: "tests/run-all-2401.sh"
      to: "tests/test-240.1-*.cjs"
      via: "shopt -s nullglob for loop"
      pattern: "tests/test-240\\.1-\\*"
    - from: "tests/run-all-2401.sh"
      to: "the CTXL-01 locked scope decision"
      via: "grep tripwire over this phase's own surfaces"
      pattern: "gsd-core"
---

<objective>
Build the Phase 240.1 verification aggregator RED-first, before any of the three
requirements' fixes exist, and give it two legs the other six plans cannot give
themselves: a glob-discipline self-test that proves the runner cannot mistake
Phase 240's suite for Phase 240.1 coverage, and a scope-escape tripwire that
turns the phase's single locked scope decision (D-01) into a standing machine
check instead of prose in a ROADMAP.

Purpose: every other plan in this phase adds test files. Glob discovery means
none of them has to edit this runner, so this file is written once, first, and
never touched again. Authoring it RED (it exits 1 until the first
tests/test-240.1-* file lands) is the Nyquist discipline this milestone has used
for every phase: the harness that discovers nothing must FAIL, not print green.

Output: tests/run-all-2401.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/240.1-context-layer-drift-detection/240.1-RESEARCH.md
@.planning/phases/240.1-context-layer-drift-detection/240.1-PATTERNS.md
@tests/run-all-240.sh
</context>

<locked_decisions>
D-01 (LOCKED, do not re-litigate): CTXL-01 targets the PER-ROOM `STATE.md`
generated by `scripts/compute-state`, NOT `.planning/STATE.md`. `.planning/STATE.md`
is written by `gsd-core`, an external tool this repo does not own; two prior RCAs
already concluded "Required Code Changes: None in this repo." Any task naming
`~/.claude/gsd-core` as an edit target is a scope escape. This plan's Task 2
makes that decision machine-checked.

D-02 (LOCKED): MotherDuck's DABStep figures (+72pp accuracy, -55% cost per run)
are VERIFIED against the primary source and filed at
`~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-motherduck-context-layer/`.
They may be cited as fact with their source URL. D-02 is consumed by plans 04
and 06, not by this plan.
</locked_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/run-all-2401.sh RED-first with glob discovery and the found-eq-0 guard</name>
  <files>tests/run-all-2401.sh</files>
  <read_first>
    - tests/run-all-240.sh (the structural analog to copy: header clauses, run / run_may_skip, strip_comments, the nullglob loop, the found-eq-0 guard)
    - .planning/phases/240.1-context-layer-drift-detection/240.1-PATTERNS.md (section "NEW tests/run-all-2401.sh")
    - .planning/phases/240.1-context-layer-drift-detection/240.1-RESEARCH.md (section "Validation Architecture", the aggregator naming rule)
  </read_first>
  <action>
Create `tests/run-all-2401.sh` (executable, `chmod +x`) by copying the structure of
`tests/run-all-240.sh` and changing only the phase token. Keep `set -uo pipefail`
(the looser form, because an aggregator must survive a failing leg), the `ROOT`
resolution via `BASH_SOURCE`, the `PASS`/`FAIL`/`SKIP` counters, the `run` helper,
the `run_may_skip` helper (REQUIRED: this phase ships
`tests/test-240.1-hook-write-site.sh`, a bash leg that may legitimately self-SKIP
on environment), and the `strip_comments` helper.

Filename discipline, stated in the header in the runner's own words: the
aggregator filename strips the dot (`run-all-2401.sh`, matching the existing
`tests/run-all-1432.sh` and `tests/run-all-1441.sh`), while the TEST filenames keep
it (`tests/test-240.1-*.cjs`). The globs are therefore `tests/test-240.1-*.cjs` and
`tests/test-240.1-*.sh`.

Port the three load-bearing header clauses from `tests/run-all-240.sh:4-33`,
rewritten for 240.1: (1) DISCOVERY IS BY GLOB, NOT BY LIST, so adding a Phase 240.1
test requires no edit to this runner; (2) the header enumerates the mandatory test
filenames by name so a missing one is visible by READING, since a file that does
not exist cannot be globbed; (3) THE found-eq-0 GUARD IS LOAD-BEARING AND MUST NOT
BE SOFTENED, because a clean summary over an empty discovery is the same
false-success shape this milestone exists to close.

Enumerate these six mandatory filenames in the header, each tagged with the
requirement it proves and the plan that lands it:
  test-240.1-state-version-preserved.cjs      CTXL-01   plan 240.1-02
  test-240.1-state-version-drift-gate.cjs     CTXL-01   plan 240.1-02
  test-240.1-hook-write-site.sh               CTXL-01   plan 240.1-03
  test-240.1-layer-doctrine-presence.cjs      CTXL-02   plan 240.1-04
  test-240.1-ctxl-fixture.cjs                 CTXL-03   plan 240.1-05
  test-240.1-ctxl-eval-gate.cjs               CTXL-03   plan 240.1-06

State in the header that until the first `tests/test-240.1-*` file lands this
harness legitimately exits 1, and that this is the guard working exactly as
designed, not a defect in this file.

Add the GLOB-DISCIPLINE SELF-TEST leg. There are already four `tests/test-240-*`
files on disk from Phase 240; a sloppy glob would silently re-run Phase 240's
suite and report it as 240.1 coverage. Two probes in a `mktemp -d` scratch dir
with a `trap ... EXIT` cleanup:
  - must_not_match: create `test-240-decoy.cjs` in the scratch dir and assert the
    240.1 glob pattern does NOT expand to it.
  - must_match: create `test-240.1-decoy.cjs` in the scratch dir and assert the
    pattern DOES expand to it.
Then assert over the REAL discovered set that every discovered basename begins
with the literal `test-240.1-`. A leg failure prints the offending basename.

End with the standard summary block printing `PASS=`, `FAIL=`, `SKIP=` and exiting
nonzero when `FAIL` is greater than 0.

No emoji. No em-dashes and no en-dashes anywhere in this file (CLAUDE.md HARD RULE).
Hyphens only. Every leg fully hermetic, zero network reach.
  </action>
  <verify>
    <automated>bash tests/run-all-2401.sh; test $? -eq 1 &amp;&amp; bash tests/run-all-2401.sh 2>&amp;1 | grep -q "no tests/test-240.1-\* files discovered"</automated>
  </verify>
  <done>
`tests/run-all-2401.sh` exists, is executable, and exits 1 with the explicit
"no tests/test-240.1-* files discovered" message because no Phase 240.1 test file
exists yet. The glob-discipline self-test's two probes both report correctly. The
header enumerates all six mandatory filenames. `grep -c $'\u2014' tests/run-all-2401.sh`
returns 0.
  </done>
  <acceptance_criteria>
    - source: `tests/run-all-2401.sh` contains the literal `found -eq 0` and the literal `tests/test-240.1-*`
    - source: `tests/run-all-2401.sh` contains `run_may_skip` (the .sh leg lands in plan 240.1-03)
    - source: `tests/run-all-2401.sh` contains zero U+2014 and zero U+2013 characters
    - behavior: with no `tests/test-240.1-*` file on disk, the runner exits 1 and prints the empty-discovery message; it never prints a green summary
    - behavior: the glob-discipline self-test reports `test-240-decoy.cjs` as NOT matched and `test-240.1-decoy.cjs` as matched
    - test-command: `bash -n tests/run-all-2401.sh` exits 0 (syntax valid)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the CTXL-01 scope-escape tripwire leg and prove it bites</name>
  <files>tests/run-all-2401.sh</files>
  <read_first>
    - tests/run-all-2401.sh (the file just created in Task 1)
    - tests/run-all-240.sh (lines 120-200, the tri-polar parity gate: the must_catch / must_not_catch probe shape to copy)
    - .planning/ROADMAP.md (Phase 240.1 SC1, the "Explicitly OUT OF SCOPE" clause this leg enforces)
  </read_first>
  <action>
Append a SCOPE-ESCAPE TRIPWIRE leg to `tests/run-all-2401.sh` that turns locked
decision D-01 into a standing machine check.

The rule it enforces: no surface THIS PHASE creates or edits under `lib/`,
`scripts/`, or `tests/` may reference `~/.claude/gsd-core` or write to
`.planning/STATE.md`. Both are the documented warning signs from
`240.1-RESEARCH.md` Pitfall 1.

Scope the grep to this phase's OWN surfaces by an explicit frozen path list
inside the runner, not by a repo-wide sweep. A repo-wide sweep would be a false
positive by construction: `scripts/doctor.cjs:2293-2299` legitimately probes for a
gsd-core install and `tests/test-doctor-class-q.cjs:13` legitimately names it. Say
that in a comment beside the list so the next reader does not "fix" the scoping.

The frozen list (files that do not exist yet are skipped, not failed, since they
land in later waves):
  lib/core/state-version.cjs
  scripts/state-write.cjs
  tests/test-240.1-state-version-preserved.cjs
  tests/test-240.1-state-version-drift-gate.cjs
  tests/test-240.1-hook-write-site.sh

For each existing file, run the phase's forbidden-token grep through
`strip_comments` first, so a comment that legitimately EXPLAINS the out-of-scope
decision (for example "gsd-core is external and out of scope") cannot itself trip
the gate. Forbidden tokens: `gsd-core` and `.planning/STATE.md`.

Prove the leg bites with a must_catch / must_not_catch pair in the same scratch
`mktemp -d` used by Task 1's self-test:
  - must_catch: a probe file whose non-comment body contains `gsd-core` MUST be
    caught.
  - must_not_catch: a probe file whose ONLY `gsd-core` occurrence is inside a
    comment line MUST NOT be caught (this is what makes the strip_comments pass
    load-bearing rather than decorative).
A self-test that skipped the comment-stripping would prove the pattern bites
while saying nothing about whether the stripping then swallows the bite. Both
directions run the exact same pipeline the real leg runs.

Increment `PASS`/`FAIL` for the tripwire leg through the same counters, so it
appears in the final summary.
  </action>
  <verify>
    <automated>bash tests/run-all-2401.sh 2>&amp;1 | grep -q "scope-escape tripwire" &amp;&amp; bash -n tests/run-all-2401.sh</automated>
  </verify>
  <done>
The tripwire leg runs on every invocation, reports both self-test directions, and
reports zero scope escapes because none of the five listed files exists yet (each
recorded as skipped-absent, never as a pass it did not earn). The runner still
exits 1 overall on the empty test-file discovery.
  </done>
  <acceptance_criteria>
    - source: `tests/run-all-2401.sh` contains the frozen path list including `lib/core/state-version.cjs` and `scripts/state-write.cjs`
    - source: the tripwire leg pipes each candidate file through `strip_comments` before grepping
    - behavior: the must_catch probe (a non-comment `gsd-core` reference) is reported CAUGHT
    - behavior: the must_not_catch probe (a `gsd-core` reference on a comment line only) is reported NOT caught
    - behavior: a file on the frozen list that does not exist on disk is reported as skipped-absent, never as a pass
    - CLI: `bash tests/run-all-2401.sh 2>&amp;1` prints a line naming the scope-escape tripwire leg and its self-test result
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo tree -> test harness | The harness reads and greps first-party source; no untrusted input crosses |
| harness -> filesystem | `mktemp -d` scratch dirs are created and removed by a trap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-240.1-01 | Tampering | `tests/run-all-2401.sh` scratch dir | mitigate | `mktemp -d` plus `trap 'rm -rf "$TMP"' EXIT`; never write outside the scratch root |
| T-240.1-02 | Spoofing | Empty-discovery false green | mitigate | The `found -eq 0` guard exits 1; the self-test proves the guard is reachable |
| T-240.1-03 | Spoofing | Phase 240 suite masquerading as 240.1 coverage | mitigate | Glob-discipline self-test asserts every discovered basename begins with `test-240.1-` |
| T-240.1-SC | Tampering | npm/pip/cargo installs | accept | Zero package-manager operations this phase; `240.1-RESEARCH.md` Package Legitimacy Audit records N/A for the whole phase |
</threat_model>

<verification>
- `bash -n tests/run-all-2401.sh` exits 0.
- `bash tests/run-all-2401.sh` exits 1 with the empty-discovery message (the RED-first state).
- `grep -c $'\u2014' tests/run-all-2401.sh` and `grep -c $'\u2013' tests/run-all-2401.sh` both return 0.
- `bash tests/run-all-240.sh` still passes unchanged (this plan touches no Phase 240 file).
</verification>

<success_criteria>
1. `tests/run-all-2401.sh` exists, is executable, discovers by glob, and never by a hand-maintained list.
2. The found-eq-0 guard is present and demonstrably reachable.
3. The glob-discipline self-test proves the runner cannot pick up `tests/test-240-*`.
4. The scope-escape tripwire proves locked decision D-01 by machine, with both must_catch and must_not_catch directions exercised.
5. Zero em-dashes and en-dashes.
</success_criteria>

## Artifacts this phase produces

Full phase roll-up. Plans 02 through 07 each list their own subset under the same
heading; this is the authoritative index.

| Symbol / path | Kind | Plan | Requirement |
|---------------|------|------|-------------|
| `tests/run-all-2401.sh` | test aggregator (new file) | 240.1-01 | all three |
| `lib/core/state-version.cjs` | new module | 240.1-02 | CTXL-01 |
| `STATE_SCHEMA_VERSION` | exported const (number, 1) | 240.1-02 | CTXL-01 |
| `STATE_SCHEMA_VERSION_LITERAL` | exported const (string, `1.0`) | 240.1-02 | CTXL-01 |
| `PRESERVED_KEYS` | exported frozen array | 240.1-02 | CTXL-01 |
| `readStateFrontmatter(roomDir)` | exported function | 240.1-02 | CTXL-01 |
| `classifyVersion(value)` | exported function | 240.1-02 | CTXL-01 |
| `persistState(roomDir, rendered, opts)` | exported function | 240.1-02 | CTXL-01 |
| `spliceFrontmatterKeys(rendered, keys)` | exported function | 240.1-02 | CTXL-01 |
| `computeState(roomDir, opts)` `opts.withVerdict` | new option on an existing export | 240.1-02 | CTXL-01 |
| `gsd_state_version` / `status` / `created_by` / `current_room` | new keys in the `'STATE.md'` schema optional list | 240.1-02 | CTXL-01 |
| `tests/test-240.1-state-version-preserved.cjs` | test (new file) | 240.1-02 | CTXL-01 |
| `tests/test-240.1-state-version-drift-gate.cjs` | test (new file) | 240.1-02 | CTXL-01 |
| `scripts/state-write.cjs` | new CLI script (stdin -> persistState) | 240.1-03 | CTXL-01 |
| `tests/test-240.1-hook-write-site.sh` | test (new file) | 240.1-03 | CTXL-01 |
| `## 2.8 Semantic Layer vs Context Layer` | new doc section in `docs/MWP-SPECIFICATION.md` | 240.1-04 | CTXL-02 |
| `## 5.1 Semantic Layer vs Context Layer` | new doc section in `docs/BRAIN-MD-SCHEMA.md` | 240.1-04 | CTXL-02 |
| `tests/test-240.1-layer-doctrine-presence.cjs` | test (new file) | 240.1-04 | CTXL-02 |
| `tests/helpers/fixture-room-2401.cjs` | test fixture builder (new file) | 240.1-05 | CTXL-03 |
| `buildFixtureRoom2401(rootDir)` | exported function | 240.1-05 | CTXL-03 |
| `FIXTURE_ROOM_SLUG` / `FIXTURE_SECTIONS` | exported consts | 240.1-05 | CTXL-03 |
| `tests/fixtures/240.1-ctxl-tasks.json` | fixed task corpus (new file) | 240.1-05 | CTXL-03 |
| `tests/test-240.1-ctxl-fixture.cjs` | test (new file) | 240.1-05 | CTXL-03 |
| `scripts/ctxl-eval.cjs` | new eval harness CLI | 240.1-06 | CTXL-03 |
| `--selftest` / `--check <name>` / `--suite code` / `--suite ab` / `--report` / `--out DIR` | CLI flags on `scripts/ctxl-eval.cjs` | 240.1-06 | CTXL-03 |
| `CTXL_EVAL_LIVE` | new env var (opt-in live model leg) | 240.1-06 | CTXL-03 |
| `checkCorpusShape` / `checkContextDiscriminates` / `checkAnchorGrounded` / `checkPart8Hygiene` / `checkCostLedger` / `checkDeltaRecord` / `scoreAnswer` / `buildBundle` | exported functions on `scripts/ctxl-eval.cjs` | 240.1-06 | CTXL-03 |
| `tests/test-240.1-ctxl-eval-gate.cjs` | test (new file) | 240.1-06 | CTXL-03 |
| `.planning/phases/240.1-context-layer-drift-detection/deferred-items.md` | phase residual register | 240.1-07 | all three |
| `.planning/debug/room-birth-compute-state-node-on-bash.md` | new RCA | 240.1-07 | CTXL-01 |

<output>
Create `.planning/phases/240.1-context-layer-drift-detection/240.1-01-SUMMARY.md` when done.
</output>
