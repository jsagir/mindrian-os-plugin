---
phase: 250-honesty-rail-doctrine-amendment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/tier0-messaging.cjs
  - bin/mindrian-brain-mcp-client.cjs
  - lib/core/doctor/class-m-brain-smoke.cjs
  - lib/core/brain-client.cjs
  - skills/brain-connector/SKILL.md
  - docs/install/BRAIN-SETUP.md
  - dist/generic-claude-dir/.claude/skills/brain-connector/SKILL.md
  - dist/zed/.agents/skills/brain-connector/SKILL.md
  - tests/test-250-refusal-shapes.cjs
  - tests/test-250-transport-retry.cjs
  - tests/test-250-refusal-queue.cjs
  - tests/test-250-doctrine-fence.cjs
  - tests/run-all-250.sh
autonomous: true
execution_model: sonnet
requirements: [HONEST-01, AVAIL-02]
must_haves:
  truths:
    - "A methodology consult with a VALID key and an unreachable Brain reports kind unreachable, and its reason string never contains 'MINDRIAN_BRAIN_KEY not set' (the shim conflation bug is dead)"
    - "A transient transport failure retries with bounded backoff at the transport layer BEFORE any refusal fires; 401/403 never retry; the null contract is unchanged - null now means transport failure AFTER the retry budget (AVAIL-02, navigator ruling 2026-08-10)"
    - "A keyless session still gets the byte-locked DIRECTOR_NOT_AVAILABLE sentinel (5 keys, exact wire string), but FALLBACK_ADVICE now carries refusal framing, not graceful-degradation framing"
    - "A not_ready refusal auto-queues an enrichment entry with source refusal via enqueue() directly, never via captureReadinessMiss, and merges idempotently with an existing live_reach entry (hit_count increments, no downgrade)"
    - "The doctrine phrases (the silent-fallback clause and the never-mention-failures clause) appear NOWHERE in skills/, commands/, agents/, or dist/ - proven by a scoped grep fence that was demonstrably RED before the rewrite"
    - "The SKILL.md refusal section instructs the four kinds in Larry's voice, fires the F.1 card, says the queue disclosure on refusal, and carries the four anti-nagging rules"
    - "No refusal or queue code is reachable from sensors/ or decide() - the 249 hot-path fence stays green"
  artifacts:
    - path: "lib/core/tier0-messaging.cjs"
      provides: "REFUSAL_KINDS, refusalResponse, larryRefusalLine, renderRefusal, refuseNotReady exports; byte-locked tier0Response unchanged"
      exports: ["REFUSAL_KINDS", "refusalResponse", "larryRefusalLine", "renderRefusal", "refuseNotReady", "DIRECTOR_NOT_AVAILABLE", "tier0Response"]
    - path: "lib/core/brain-client.cjs"
      provides: "Bounded retry-with-backoff around the single HTTP dispatch; transport-class failures only; null contract preserved"
    - path: "bin/mindrian-brain-mcp-client.cjs"
      provides: "All 6 tool handlers split transport-null (unreachable refusal) from no-key (existing sentinel)"
      contains: "refusalResponse('unreachable'"
    - path: "tests/test-250-refusal-shapes.cjs"
      provides: "Kind shapes, status mapping, conflation red proof, wire-lock guard, shim source assertion"
    - path: "tests/test-250-transport-retry.cjs"
      provides: "Loopback-server retry budget proof: transient retries, 403 zero-retry, null-after-budget"
    - path: "tests/test-250-refusal-queue.cjs"
      provides: "refuseNotReady enqueue with source refusal, idempotent merge with live_reach, never-throws"
    - path: "tests/test-250-doctrine-fence.cjs"
      provides: "Scoped grep fence over skills/ commands/ agents/ dist/ for the two doctrine phrase patterns"
    - path: "tests/run-all-250.sh"
      provides: "Glob-discovery phase runner (tests/test-250-*), found-eq-0 guard, em-dash fence"
  key_links:
    - from: "bin/mindrian-brain-mcp-client.cjs"
      to: "lib/core/tier0-messaging.cjs"
      via: "require destructure of refusalResponse next to the existing chokepointTier0"
      pattern: "refusalResponse\\('unreachable'"
    - from: "lib/core/brain-client.cjs HTTP dispatch"
      to: "the retry wrapper"
      via: "bounded backoff around the one transport seam all tools flow through"
      pattern: "RETRY"
    - from: "lib/core/tier0-messaging.cjs (refuseNotReady)"
      to: "lib/core/enrichment-queue.cjs"
      via: "enqueue() with source refusal"
      pattern: "source: 'refusal'"
    - from: "skills/brain-connector/SKILL.md"
      to: "scripts/enrichment-queue-append.cjs"
      via: "the Larry-direct append block extended with --source refusal"
      pattern: "--source refusal"
---

<objective>
Kill the silent-fallback doctrine at its executable and instruction homes and replace it
with a typed, visible refusal rail (HONEST-01), with client resilience matched to the
refusal taxonomy (AVAIL-02, navigator ruling 2026-08-10): transient transport failures
retry with bounded backoff at the transport layer BEFORE the unreachable refusal fires,
so a blip never becomes a user-facing refusal. Four refusal kinds (no_key, unreachable,
tier_denied, not_ready) land in the tier0-messaging chokepoint; the shim's live dishonesty
bug (transport-null conflated with the no-key sentinel) is fixed; the SKILL.md doctrine
sections are rewritten with the refusal copy, the F.1 card, and the anti-nagging rules;
the committed dist mirrors are regenerated; and a scoped doctrine grep fence proves all
enumerated kill-list sites are dead.

Purpose: the honesty invariant ("a user is NEVER served methodology that did not come from
the Brain, without being told") gets its rail BEFORE the 252 hard-require sweep. This plan
only ADDS honesty - per the research sequencing table it is releasable in a v2.0.0-beta
before Phase 252 with no lockstep risk (no doc claims Brain-required; the amendment rows
are untouched here).

Output: refusal kinds + render helpers in lib/core/tier0-messaging.cjs, the transport
retry budget in lib/core/brain-client.cjs, the fixed shim, the rewritten
skills/brain-connector/SKILL.md + regenerated dist mirrors, the honest BRAIN-SETUP.md
sentence, four new born-RED test files, and tests/run-all-250.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/250-honesty-rail-doctrine-amendment/250-RESEARCH.md
@lib/core/tier0-messaging.cjs
@lib/core/tier0-messaging.test.cjs
@bin/mindrian-brain-mcp-client.cjs
@skills/brain-connector/SKILL.md
@lib/core/enrichment-queue.cjs
@tests/run-all-249.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refusal kinds in the chokepoint + shim conflation fix + transport retry budget + phase runner</name>
  <files>tests/test-250-refusal-shapes.cjs, tests/test-250-transport-retry.cjs, tests/run-all-250.sh, lib/core/tier0-messaging.cjs, lib/core/brain-client.cjs, bin/mindrian-brain-mcp-client.cjs, lib/core/doctor/class-m-brain-smoke.cjs</files>
  <behavior>
    Write tests/test-250-refusal-shapes.cjs FIRST (node:test CJS, node:assert/strict, the
    tests/test-249-capture-seam.cjs style), run it, record the RED output. Tests:
    - Test 1: REFUSAL_KINDS is frozen and equals ['no_key','unreachable','tier_denied','not_ready'].
    - Test 2: refusalResponse('no_key', {tool:'brain_query'}).status === 'DIRECTOR_NOT_AVAILABLE'
      (locked wire string, downstream compat) and carries upgrade_hint; refusalResponse
      statuses for the other kinds are 'BRAIN_UNREACHABLE', 'BRAIN_TIER_DENIED',
      'GRAPH_NOT_READY' respectively. Shape: {status, kind, reason, command_context,
      next_moves, upgrade_hint?}.
    - Test 3 (the conflation red proof): refusalResponse('unreachable', {tool:'brain_query'})
      .reason does NOT contain 'MINDRIAN_BRAIN_KEY not set' and DOES name unreachability.
    - Test 4: unknown kind coerces to 'unreachable'; non-object/absent ctx coerces
      command_context to 'unknown' (the existing tier0Response defensive pattern).
    - Test 5: tier0Response byte-lock unchanged - same 5 keys, status DIRECTOR_NOT_AVAILABLE,
      FALLBACK_ADVICE value matches /Larry/ (the existing loose regex) AND no longer contains
      the old graceful phrase 'Larry can still talk with you'.
    - Test 6: larryRefusalLine(kind, detail) returns a string under 120 chars for every kind
      (statusline-safe, the larryTier0Hint precedent).
    - Test 7 (shim source assertion, avoids a full MCP harness): read
      bin/mindrian-brain-mcp-client.cjs as text; assert the pattern
      /r == null \? tier0Response/ appears ZERO times and /refusalResponse\('unreachable'/
      appears at least 5 times (the 5 raw tools) with brain_ask's envelope variant present.

    Then write tests/test-250-transport-retry.cjs FIRST as well (AVAIL-02; loopback
    node:http server, the test-249-capture-seam.cjs precedent - no fetch mocking), run it,
    record the RED output. Tests (use tiny backoff values via the env overrides so the
    suite stays under 60s):
    - Test A (transient recovers): server scripted to fail twice (503) then 200 - the
      wrapped call returns the payload, never null; server-side attempt counter === 3.
    - Test B (budget exhausted): server always 503 - the call returns null after exactly
      1 + RETRY_MAX attempts, and only then (a blip never becomes a refusal; a real outage
      still resolves to null for the unreachable refusal).
    - Test C (zero-retry classes): server returns 403 - the tier_denied sentinel comes back
      with exactly ONE attempt (401/403 are validation-class, never transient; retrying
      them is taxonomy-wrong and hammers auth).
    - Test D (env override): MINDRIAN_BRAIN_RETRY_MAX=0 disables retries (one attempt);
      an invalid value falls back to the default (the repo's defensive numeric-env
      convention, which is a parsing convention, not failure concealment - research's
      non-doctrine list).
  </behavior>
  <action>
    1. Create tests/run-all-250.sh mirroring tests/run-all-249.sh EXACTLY in mechanism:
       glob discovery of tests/test-250-* (.cjs via node --test, .sh via bash), the
       found-eq-0 guard (a harness that discovers nothing FAILS), and the em-dash fence
       (grep -P for the U+2014 codepoint escape over the files this phase touches - never
       a literal em-dash in the runner itself). Header enumerates the mandatory 250 test
       files by name. bash only, set -uo pipefail.
    2. Write both test files per the behavior block. Run
       `node --test tests/test-250-refusal-shapes.cjs tests/test-250-transport-retry.cjs`
       and RECORD the red output in the SUMMARY (refusalResponse not exported yet; shim
       still conflates; no retry wrapper exists).
    3. Extend lib/core/tier0-messaging.cjs (research Pattern 2 - this edit IS the sanctioned
       value amendment its header names):
       - KEEP: DIRECTOR_NOT_AVAILABLE constant, the five sentinel keys, tier0Response(),
         larryTier0Hint(), isAvailable() passthrough. The wire shape is byte-locked.
       - REWRITE VALUE: FALLBACK_ADVICE becomes refusal framing, e.g. "Larry does not
         improvise methodology. Methodology comes from the Brain or it is refused, visibly.
         Conversation and room context remain available." (must still match /Larry/ for
         the existing loose test regex).
       - ADD: const REFUSAL_KINDS = Object.freeze(['no_key','unreachable','tier_denied','not_ready']);
         KIND_STATUS map (no_key -> DIRECTOR_NOT_AVAILABLE, unreachable -> 'BRAIN_UNREACHABLE',
         tier_denied -> 'BRAIN_TIER_DENIED', not_ready -> 'GRAPH_NOT_READY');
         refusalResponse(kind, ctx) per the research sketch (unknown kind coerces to
         'unreachable', honest per-kind REASONS, per-kind NEXT_MOVES arrays of short option
         handles, upgrade_hint only for no_key); larryRefusalLine(kind, detail) one-liners
         under 120 chars. Export all three.
       - V5 rule (threat model): refusal reasons interpolate ONLY closed-enum kind, coerced
         tool name, canonical framework name, and the server message already sliced to 300
         chars upstream in brain-client. Never render unbounded server bodies. Never echo
         user turn text. Never print a key value or key-file CONTENTS (path NAMES like
         ~/.mindrian.env are fine and already shipped in the hint).
    4. AVAIL-02 transport retry in lib/core/brain-client.cjs (the retry lives at the
       transport layer around the call, NOT in the refusal renderer, NOT in the shim):
       - Locate the single HTTP dispatch seam all Brain tools flow through (brain-client
         is the single source of the default URL at line ~24; grep for the shared request
         function the wrappers call). Wrap THAT seam only.
       - Retry ONLY transport-class outcomes: network errors (ECONNRESET, ENOTFOUND,
         timeout) and 5xx responses. NEVER retry 401/403 (they map to the invalid_key /
         tier_denied sentinels - validation class, not transient), never 2xx, never
         other 4xx.
       - Budget: RETRY_MAX = 2 retries (3 attempts total), backoff 300ms then 900ms.
         Env overrides MINDRIAN_BRAIN_RETRY_MAX and MINDRIAN_BRAIN_RETRY_BASE_MS parsed
         with the repo's defensive numeric convention (invalid input falls back to the
         default). Worst-case added latency ~1.2s on a genuinely dead Brain - bounded,
         and only on the failing path.
       - NULL CONTRACT PRESERVED (the 247-02 do-not-widen note binds - 82 degradation
         tests key on it): after the budget, return null exactly as today. The retry
         changes WHEN null returns, never WHAT returns null, and never which statuses
         map to which sentinels. Say this in a comment at the wrapper.
    5. Fix the shim bin/mindrian-brain-mcp-client.cjs (site #9, the live dishonesty bug):
       destructure refusalResponse from ../lib/core/tier0-messaging.cjs next to the existing
       chokepointTier0 require (line ~46). In each of the 5 raw tool handlers
       (brain_query/brain_schema/brain_search/brain_stats/brain_write) the null branch
       `r == null ? tier0Response(name) : r` becomes
       `r == null ? refusalResponse('unreachable', { tool: name }) : r`.
       The `!brainClient.isAvailable()` branch KEEPS tier0Response(name) unchanged
       (tests/test-127-00-shim-handshake.sh Test 8 asserts the no-key live call returns
       DIRECTOR_NOT_AVAILABLE - that behavior must not move). brain_ask wraps its sentinel
       in a DirectiveEnvelope: read its handler, keep the keyless envelope-wrapped tier0
       sentinel as-is, and wrap refusalResponse('unreachable', { tool: 'brain_ask' }) in
       the SAME envelope for its transport-null case.
    6. Update the Class-M consumer lib/core/doctor/class-m-brain-smoke.cjs (found at plan
       time per research A1 - it lives HERE, not in scripts/doctor.cjs): the recognizer at
       line ~189 currently accepts only parsed.status === 'DIRECTOR_NOT_AVAILABLE' as a
       structured sentinel. Extend it to recognize all four statuses
       (DIRECTOR_NOT_AVAILABLE, BRAIN_UNREACHABLE, BRAIN_TIER_DENIED, GRAPH_NOT_READY) as
       STRUCTURED results (not crashes), reporting parsed.kind in the smoke output line.
       Keep the smoke's pass/fail semantics otherwise unchanged - a structured refusal
       counts exactly the way DIRECTOR_NOT_AVAILABLE counts today, now honestly named.
    7. Run both suites to green. Run the pre-existing lock test
       `node lib/core/tier0-messaging.test.cjs` (plain assert script) - it must stay green
       with the rewritten FALLBACK_ADVICE (its value assertion is the loose /Larry/ regex).
    No npm installs (hard convention; research Package Legitimacy Audit: not applicable).
  </action>
  <verify>
    <automated>node --test tests/test-250-refusal-shapes.cjs tests/test-250-transport-retry.cjs && node lib/core/tier0-messaging.test.cjs && bash tests/run-all-250.sh</automated>
  </verify>
  <done>Red outputs recorded, then: all shape + retry tests green; existing tier0-messaging lock test green; shim null branches call refusalResponse('unreachable') in all 6 handlers while no-key branches are untouched; transient failures retry with bounded backoff before any null surfaces, 403 never retries; Class-M smoke recognizes the four statuses; runner discovers and passes the 250 suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI-path render helper + refusal auto-queue seam</name>
  <files>tests/test-250-refusal-queue.cjs, lib/core/tier0-messaging.cjs</files>
  <behavior>
    Write tests/test-250-refusal-queue.cjs FIRST (node:test CJS, temp room dirs like
    tests/test-249-enrichment-queue.cjs), run it, record the RED output. Tests:
    - Test 1: refuseNotReady(roomDir, {framework, readiness_score, missing_dimensions,
      context_class, probe_provenance}) lands ONE entry in
      roomDir/.mindrian/enrichment-queue.json with source 'refusal', and returns a
      refusalResponse whose kind is 'not_ready', whose status is 'GRAPH_NOT_READY', and
      whose reason names the framework, the readiness N/4, the missing dimensions, and
      the queue disclosure ("Queued for enrichment" phrasing).
    - Test 2 (idempotent merge): pre-seed the queue via enqueue() with source 'live_reach'
      for the same canonical framework, then refuseNotReady the same framework: exactly ONE
      entry remains, hit_count incremented, and the source does not downgrade (both are
      live per _SOURCE_IS_LIVE - assert the merged entry still reports a live source).
    - Test 3 (never throws): refuseNotReady with an unwritable/absent roomDir still RETURNS
      the refusalResponse and never throws into the caller (the enrichment-queue module's
      own never-throw discipline).
    - Test 4: renderRefusal(kind, ctx) returns a non-empty multi-line string for each of
      the four kinds; the not_ready render contains the queue disclosure sentence; the
      unreachable render never contains 'MINDRIAN_BRAIN_KEY not set'.
    - Test 5 (binding scope guard): grep lib/core/sensors/ and the decide() module source
      for require of tier0-messaging refusal symbols - zero matches (the 249 Pitfall-6
      hot-path fence, extended to the refusal seam).
  </behavior>
  <action>
    Extend lib/core/tier0-messaging.cjs with the two exported helpers (research Open
    Question 1 recommendation: the helper ships in 250, per-command adoption rides 252's
    sweep):
    1. renderRefusal(kind, ctx): returns the full Larry-voice refusal copy block (research
       Pattern 3 copy, one honest sentence naming the kind + the queue disclosure when
       applicable + the next-move handles). This is the documented CLI-path render contract
       that /mos: commands adopt in 252 - add a short header comment saying exactly that.
    2. refuseNotReady(roomDir, miss): requires ./enrichment-queue.cjs lazily inside the
       function (not at module top - keeps the module load-light for the shim path), calls
       enqueue(roomDir, { framework, normalized, readiness_score, missing_dimensions,
       context_class, source: 'refusal', probe_provenance }) inside a try/catch that never
       throws into the caller, then returns refusalResponse('not_ready', ...) with the
       reason format: "The graph doesn't have <Framework> structured yet (readiness <N>/4;
       missing: <dims>). Queued for enrichment." Explicitly NOT captureReadinessMiss - that
       helper pins source 'live_reach' (enrichment-queue.cjs line ~495).
    3. Binding scope (research Open Question 2 recommendation, locked by the planning
       constraints): the not_ready refusal binds to the two readiness-shaped wrappers only
       (orchestrationReadiness / discoverStructure consumers and their MCP twins) - exactly
       where 249's capture seams sit. Do NOT wire refuseNotReady into any sensor, decide(),
       or per-turn hook path. captureReadinessMiss stays live and untouched for the
       silent-capture-on-successful-serve leg.
    Run the suite to green. Then run the existing hot-path fence
    `node --test tests/test-249-capture-seam.cjs` - it must stay green (no new probe or
    enqueue reachable from sensors/decide).
  </action>
  <verify>
    <automated>node --test tests/test-250-refusal-queue.cjs && node --test tests/test-249-capture-seam.cjs && bash tests/run-all-250.sh</automated>
  </verify>
  <done>Red output recorded, then: queue seam tests green; an existing live_reach entry and a refusal render for the same framework merge to one entry with hit_count incremented; refuseNotReady never throws; the 249 hot-path fence stays green; renderRefusal ships as the documented 252-adoption contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Doctrine kill - SKILL.md rewrite, BRAIN-SETUP sentence, site-11 mark, dist rebuild, grep fence</name>
  <files>tests/test-250-doctrine-fence.cjs, skills/brain-connector/SKILL.md, docs/install/BRAIN-SETUP.md, lib/core/brain-client.cjs, dist/generic-claude-dir/.claude/skills/brain-connector/SKILL.md, dist/zed/.agents/skills/brain-connector/SKILL.md</files>
  <behavior>
    Write tests/test-250-doctrine-fence.cjs FIRST. The fence walks ONLY skills/, commands/,
    agents/, dist/ (NOT lib/, NOT docs/ - those red-flood on legitimate env-parse comments
    and ride 252's sweep; NOT .claude/worktrees). Grep hygiene: match content lines, print
    file:line for every hit, exit non-zero on ANY hit. The closed pattern list (research
    Pitfall 6 - exactly these two, no widening):
    - /silent fallback/i
    - /never mention (failures|this bookkeeping)/i
    Run it BEFORE any edit and RECORD the red output: it must report the known kill-list
    hits (skills/brain-connector/SKILL.md lines 31, 100, 126 and both dist mirrors at
    lines 27, 96) - that red run is the fence's own can-fail proof.
  </behavior>
  <action>
    After recording the red fence run, execute the kill list (research Doctrine Site
    Enumeration, sites 1-8 text legs; sites 8-9 code legs already landed in Task 1):
    1. skills/brain-connector/SKILL.md rewrite (KEEP the section short - the Anthropic
       instruction-bloat finding: rules, not pages of scenarios):
       - Site 1 (line ~31): replace "All fail = silent fallback. Never mention failures to
         user." with "Any success = Brain active. All fail = visible refusal - see the
         Refusal section below."
       - Site 2 (line ~100 gating rule): replace the silent-fallback bullet with "Visible
         refusal on all failures; render once per consult, never repeat ambient".
       - Site 3 (Offer-to-Setup, lines ~37-38): the answer-locally-first-then-offer posture
         becomes the F.1 refusal fork for METHODOLOGY asks (refuse first, offer the key
         path as a next move); chat/context asks stay unaffected - say so explicitly.
         (Plan 250-04 reframes the no_key leg to the registration failure edge once silent
         registration lands - write this copy as true TODAY, not as permanent.)
       - Site 4 (line ~107 unreachable-fallback instruction): new text - local Room
         heuristics may drive CONVERSATION and command resolution, never presented as
         methodology; methodology asks refuse visibly. Command resolution still goes
         through lib/workflow/command-resolver.cjs (keep that existing rule).
       - Site 5 (line ~126, the 249-01 bookkeeping clause): reconcile - capture stays
         silent when it accompanies a SUCCESSFUL serve (score 0-2 but material served with
         provenance); when it accompanies a REFUSAL the queueing is SAID ("I've queued it
         for enrichment" is part of the refusal copy). Extend the 249 append block with the
         refusal leg: same CLI, --source refusal instead of --source live_reach ('refusal'
         is pre-validated in ALLOWED_SOURCES; the CLI takes --source).
       - NEW "## Refusal (the honesty rail)" section: the four kinds with the research
         Pattern 3 copy (no_key / unreachable / tier_denied / not_ready - one honest
         sentence naming the kind, the queue disclosure for not_ready, then fire the F.1
         AskUserQuestion card with the kind's next moves; SEED-021 binding: fire the card,
         never draw the box). Note that unreachable means unreachable AFTER the bounded
         transport retry budget (AVAIL-02) - Larry never narrates the retries themselves.
         The not_ready refusal refuses the ORCHESTRATION claim then OFFERS the
         disclosed-partial path (Pitfall 7 - refusing to fake structure is not refusing to
         serve what exists; the partial MARKING contract itself lands in plan 250-03).
         Then the four anti-nagging rules verbatim from research Pattern 3:
         consult-scoped only (never ambient, never per-turn); first refusal of a kind per
         session full, repeats compress to larryRefusalLine; key-setup pitch at most once
         per session; refusal never interrupts a non-methodology conversation.
       - FENCE CAUTION: the new text must NOT contain the two fence phrases verbatim. Refer
         to the dead doctrine as "the silent-fallback clause" (hyphenated compound) or
         describe it without quoting; the verbatim quotes belong in plan 250-02's amendment
         doc under docs/, which is out of fence scope by design.
    2. docs/install/BRAIN-SETUP.md (site 10 - the wording drifted from the research's line
       citation; target by content): find the sentence block "Without a key, every Brain
       command returns a structured \"Brain unavailable\" response (DIRECTOR_NOT_AVAILABLE)
       and Larry surfaces a one-line upgrade hint. The plugin remains fully functional at
       Tier-0." Rewrite to honest-refusal framing: without a key, methodology consults are
       visibly refused with a path to a key; conversation and room context remain available.
       Kill the "fully functional at Tier-0" claim in THIS sentence only - the doc's broader
       Tier-0 prose rides 252's 121-file sweep (and plan 250-04 updates the ceremony
       paragraph when silent registration lands).
    3. lib/core/brain-client.cjs (site 11, MARK only - the behavior flip is SWEEP-01/252
       territory): add a comment above getTier0Chain()/getFrameworkChain() (lines ~1137-1220)
       stating these hardcoded chains are served with source 'tier0' and are NOT
       graph-grounded; per the Phase 250 provenance contract they must never be presented
       as methodology without disclosure, and the guard-site flip lands in Phase 252
       (SWEEP-01). No behavior change - the 247-02 do-not-widen note on the null branch
       binds (82 degradation tests key on it).
    4. Regenerate the committed dist mirrors (sites 6-7, Pitfall 1):
       `node scripts/build-dist-bundles.cjs`, then confirm freshness with
       `node scripts/build-dist-bundles.cjs --check-stale` (exit 0).
    5. Re-run the fence to GREEN and the full runner.
  </action>
  <verify>
    <automated>node --test tests/test-250-doctrine-fence.cjs && node scripts/build-dist-bundles.cjs --check-stale && bash tests/run-all-250.sh</automated>
  </verify>
  <done>Fence red run recorded (7 known hits), then green after rewrite + dist rebuild; SKILL.md carries the Refusal section, the reconciled bookkeeping clause, and the --source refusal leg; BRAIN-SETUP.md sentence is honest; site 11 is marked for the 252 flip; dist mirrors are fresh; full 250 runner green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Brain server -> refusal copy | server-controlled 403/error bodies cross into user-visible refusal text |
| user session -> enrichment queue | refusal path writes to the room queue file (Part 8 boundary) |
| transport retry -> Brain server | client-originated repeat traffic on failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-250-01 | Tampering | refusalResponse tier_denied reason | mitigate | keep brain-client's existing 300-char slice; render the server message as quoted plain text, never as instructions (Task 1 step 3, V5 rule) |
| T-250-02 | Information disclosure | refusal copy | mitigate | never print key values or key-file contents; interpolate only closed-enum kind, coerced tool name, canonical framework name (Task 1 step 3) |
| T-250-03 | Information disclosure (Part 8) | refuseNotReady queue write | mitigate | enrichment-queue's closed allowlist validator already rejects prose; refusal path passes only handles - Tests 1/2 of test-250-refusal-queue.cjs exercise the validator path |
| T-250-04 | Denial of service | refusal render in hot path | mitigate | consult-scoped only; test-250-refusal-queue.cjs Test 5 + the existing test-249-capture-seam.cjs fence keep sensors/decide clean |
| T-250-08 | Denial of service | transport retry amplification | mitigate | bounded budget (2 retries, backoff), transport-class only, 401/403 zero-retry (Test C) - the client never hammers auth failures |

No package installs this phase (zero-npm-deps hard convention; research Package Legitimacy Audit: not applicable).
</threat_model>

<verification>
- `bash tests/run-all-250.sh` green (glob discovers all four 250 test files; found-eq-0 guard live)
- `node lib/core/tier0-messaging.test.cjs` green (byte-lock intact)
- `node --test tests/test-249-capture-seam.cjs` green (hot-path fence)
- `node scripts/build-dist-bundles.cjs --check-stale` exit 0
- Every test file's RED run recorded in the SUMMARY before its GREEN (eval-honesty rule: a test that cannot fail is not evidence)
- No em-dashes in any touched file (runner fence)
</verification>

<success_criteria>
- HONEST-01 executable legs complete: four typed refusal kinds at the chokepoint, shim
  conflation fixed (unreachable-with-key never claims a missing key), refusal auto-queue
  live with source refusal, doctrine phrases dead across skills/commands/agents/dist with
  a demonstrably-red-then-green fence.
- AVAIL-02 complete: transient transport failures retry with bounded backoff before the
  unreachable refusal fires; 401/403 never retry; the null contract and sentinel mapping
  are byte-for-byte what the 82 degradation tests expect.
- The wire contract survived: DIRECTOR_NOT_AVAILABLE constant, five sentinel keys,
  tier0Response shape, test-127-00 Test 8 semantics (no-key path) all unchanged.
- This plan's tree is releasable in a v2.0.0-beta before Phase 252 (it only adds honesty
  and resilience; decisions.md rows untouched).
</success_criteria>

<output>
Create `.planning/phases/250-honesty-rail-doctrine-amendment/250-01-SUMMARY.md` when done,
including every recorded RED output, the doctrine fence's before/after hit counts, and the
retry budget's test-observed attempt counts.
</output>
