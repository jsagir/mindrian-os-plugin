---
phase: 250-honesty-rail-doctrine-amendment
plan: 03
type: execute
wave: 3
depends_on: [250-01, 250-02]
files_modified:
  - skills/brain-connector/SKILL.md
  - dist/generic-claude-dir/.claude/skills/brain-connector/SKILL.md
  - dist/zed/.agents/skills/brain-connector/SKILL.md
  - tests/test-250-provenance-fence.cjs
autonomous: true
execution_model: sonnet
requirements: [HONEST-03]
must_haves:
  truths:
    - "A graph-grounded answer carries exactly ONE '■ BRAIN:' source line at the end of the grounded block; a Larry-voice conversation turn carries none - absence IS the signal (the voice-mark design, second plane)"
    - "Partial graph material serves only as '■ BRAIN (partial):' and only after the navigator picks the use-partial fork at the F.1 refusal card - the disclosed-degraded state"
    - "Anything derived from a source:'tier0' hardcoded chain carries NO mark and, on a methodology ask, refuses instead (until 252 flips the chains)"
    - "The U+25A0 mark does not collide with the frozen 5-glyph voice-mark set: countDeStijlGlyphs ignores it and the exactly-one-color-mark contract is untouched"
  artifacts:
    - path: "skills/brain-connector/SKILL.md"
      provides: "The Provenance section: mark forms, absence-is-signal rule, partial marking, tier0-chain disclosure, anti-nagging, HTML export rule"
      contains: "■ BRAIN"
    - path: "tests/test-250-provenance-fence.cjs"
      provides: "Contract-string fence over SKILL.md + both dist mirrors + live collision guard against voice-color-mark.cjs"
  key_links:
    - from: "skills/brain-connector/SKILL.md Provenance section"
      to: "skills/ui-system/SKILL.md [■ BRAIN] chip vocabulary"
      via: "chip reuse - no new glyph, no sixth color (the 12-glyph set is frozen)"
      pattern: "■ BRAIN"
    - from: "tests/test-250-provenance-fence.cjs"
      to: "lib/hmi/voice-color-mark.cjs"
      via: "countDeStijlGlyphs called live on a sample grounded block"
      pattern: "countDeStijlGlyphs"
---

<objective>
Give Larry-served methodology its provenance mark (the HONEST-03 provenance leg):
graph-grounded answers carry one "■ BRAIN" source line reusing the existing ui-system
chip vocabulary, absence of the line IS the Larry-voice signal (the same
absence-is-the-signal design the voice-color marks already ship), and partial material
is served only as a disclosed "■ BRAIN (partial)" state after the navigator picks that
fork. No new glyph, no ambient nagging.

NOTE - SEED-011 is NOT decided here: the key ceremony was RULED 2026-08-10 (Option A,
per-install silent registration, baked in by default - REQUIREMENTS.md HONEST-03). The
registration BEHAVIOR ships in plan 250-04; this plan is purely the provenance contract.

Purpose: the honesty invariant needs answers to be distinguishable, not just failures to
be visible - a user must always know WHERE methodology came from, without Larry narrating
Brain status every turn.

Output: the SKILL.md Provenance section, regenerated dist mirrors, and the born-RED
provenance fence test with a live voice-mark collision guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/250-honesty-rail-doctrine-amendment/250-RESEARCH.md
@skills/brain-connector/SKILL.md
@skills/ui-system/SKILL.md
@skills/larry-personality/SKILL.md
@lib/hmi/voice-color-mark.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The provenance contract - SKILL.md section + collision guard + dist rebuild</name>
  <files>tests/test-250-provenance-fence.cjs, skills/brain-connector/SKILL.md, dist/generic-claude-dir/.claude/skills/brain-connector/SKILL.md, dist/zed/.agents/skills/brain-connector/SKILL.md</files>
  <behavior>
    Write tests/test-250-provenance-fence.cjs FIRST (node:test CJS), run it, record the
    RED output (the section does not exist yet). Tests:
    - Test 1 (contract strings): skills/brain-connector/SKILL.md contains a Provenance
      section with ALL of: the terminal/Cowork mark form ("■ BRAIN:" with framework, tool,
      and "readiness" tokens), the Desktop degrade form ("**■ Brain:**"), the partial form
      ("■ BRAIN (partial):"), an absence-is-the-signal statement, the source-'tier0'
      not-graph-grounded rule, and the one-line-per-answer anti-nagging rule.
    - Test 2 (dist parity): both dist mirrors
      (dist/generic-claude-dir/.claude/skills/brain-connector/SKILL.md and
      dist/zed/.agents/skills/brain-connector/SKILL.md) contain the same contract strings.
    - Test 3 (live collision guard, not a source grep): require lib/hmi/voice-color-mark.cjs
      and call countDeStijlGlyphs on a sample turn that opens with one legitimate color
      mark and ends with a "■ BRAIN: Jobs to Be Done · brain_search · readiness 3/4" line:
      the count must be EXACTLY 1 (U+25A0 is invisible to the 5-glyph detector; the
      exactly-one-color-mark contract survives the source line). Also assert
      countDeStijlGlyphs('■ BRAIN: x') === 0.
    - Test 4 (no em-dash in the section).
  </behavior>
  <action>
    After recording the red run, write the "## Provenance (where methodology came from)"
    section in skills/brain-connector/SKILL.md - SHORT (the Anthropic instruction-bloat
    finding binds; rules, not scenario pages). The contract (research Pattern 7, all
    constraints verified against frozen contracts):
    1. THE MARK: one source line per graph-grounded answer, form
       "■ BRAIN: <framework> · <tool> · readiness <N>/4" (terminal/Cowork), rendered at
       the END of the methodology content it grounds - never turn-anchored (the voice
       color mark owns the turn start). Desktop degrades to a bold markdown line
       ("**■ Brain:** ...") per the existing ui-system degrade table; U+25A0 is a plain
       glyph that survives every surface.
    2. ABSENCE IS THE SIGNAL: no source line = Larry-voice conversation. Mirror the
       voice-mark design explicitly (larry-personality line ~209 precedent). One mechanism
       family, two planes: color square = WHO is speaking; ■ BRAIN line = WHERE the
       methodology came from.
    3. PARTIAL: a not_ready refusal answered with "use what the graph does hold" serves
       prose search results marked "■ BRAIN (partial): <framework> · readiness <N>/4" -
       the disclosed-degraded state, which passes the honesty invariant BECAUSE it is
       told. Partial is only served after the navigator picks that fork at the F.1 card
       (cross-reference the Refusal section from plan 250-01) - never by default.
    4. TIER0 CHAINS (site 11, marked in 250-01): any output derived from a source:'tier0'
       hardcoded chain is NOT graph-grounded - no ■ BRAIN line ever, and on a methodology
       ask that path refuses instead. The chains flip in Phase 252 (SWEEP-01).
    5. ANTI-NAGGING: one source line per answer, never per fact, never repeated within a
       turn. Provenance is a mark, not a narration.
    6. HTML EXPORTS: pages obey M:OS Design System v1.1 (the ui-system rule "if it renders
       as a page, it obeys M:OS") - a source-line component, not the terminal chip.
    7. FONT CAVEAT (research A5): some fonts render U+25A0 close to U+2B1B; position
       disambiguates (the source line is never turn-anchored). Fallback if plan 250-04's
       live three-surface check shows real confusion: the literal word chip "[BRAIN]".
    Do NOT touch lib/hmi/voice-color-mark.cjs, the ui-system 12-glyph vocabulary, or any
    color-mark contract - the chip vocabulary is reused, never extended (a new glyph is a
    canon amendment; research Don't-Hand-Roll table).
    FENCE CAUTION: keep the two 250-01 doctrine phrases out of the new text (the doctrine
    fence scans skills/ and dist/).
    Then regenerate dist (`node scripts/build-dist-bundles.cjs`), confirm `--check-stale`
    exit 0, and run the provenance fence to green, the doctrine fence (still green), and
    the full runner.
  </action>
  <verify>
    <automated>node --test tests/test-250-provenance-fence.cjs && node --test tests/test-250-doctrine-fence.cjs && node scripts/build-dist-bundles.cjs --check-stale && bash tests/run-all-250.sh</automated>
  </verify>
  <done>Red output recorded, then: the Provenance section ships all seven contract points in SKILL.md and both dist mirrors; the live collision guard proves U+25A0 is invisible to countDeStijlGlyphs and the exactly-one-color-mark contract survives; doctrine fence still green; full runner green. Live rendering proof rides plan 250-04's released-build checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill instruction -> user perception | the mark is the user's only signal of methodology origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-250-09 | Repudiation | spoofed provenance (a turn faking "■ BRAIN:" on ungrounded content) | mitigate | instruction-layer risk inherent to skill contracts; mitigations: the mark's closed fence-tested form, Part 6 dog-fooding (a violation surfaces as a CONTRADICTS edge), and 252's sweep making ungrounded methodology structurally unavailable |
| T-250-10 | Spoofing | glyph confusion (U+25A0 vs the voice marks) | mitigate | live collision guard (Test 3) + position anchoring (end of block, never turn start) + the A5 fallback recorded in the section |

No package installs this phase (zero-npm-deps hard convention).
</threat_model>

<verification>
- `node --test tests/test-250-provenance-fence.cjs` green (RED run recorded first)
- `node --test tests/test-250-doctrine-fence.cjs` still green after the SKILL.md edit + dist rebuild
- `node scripts/build-dist-bundles.cjs --check-stale` exit 0
- `bash tests/run-all-250.sh` green
</verification>

<success_criteria>
- The HONEST-03 provenance leg is contract-complete: graph-grounded, partial, and
  Larry-voice states are all distinguishable by a fence-testable closed form; the frozen
  voice-mark contract is untouched (proven live, not by inspection); nagging is
  structurally prevented (one line per answer, consult-scoped refusals, absence as
  default signal).
- Live three-surface rendering proof is explicitly deferred to plan 250-04's
  released-build checkpoint (restart-to-apply rule) - this plan claims contract, not
  live behavior.
</success_criteria>

<output>
Create `.planning/phases/250-honesty-rail-doctrine-amendment/250-03-SUMMARY.md` when done,
including the provenance fence RED output and the collision-guard result.
</output>
