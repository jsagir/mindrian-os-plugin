---
phase: 250-honesty-rail-doctrine-amendment
plan: 02
type: execute
wave: 2
depends_on: [250-01]
files_modified:
  - docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md
  - skills/brain-connector/SKILL.md
  - docs/HITL-SHAPE-DECLARATION-CONTRACT.md
  - dist/generic-claude-dir/.claude/skills/brain-connector/SKILL.md
  - dist/zed/.agents/skills/brain-connector/SKILL.md
  - tests/test-250-amendment-unit.cjs
autonomous: false
execution_model: sonnet
requirements: [HONEST-02]
must_haves:
  truths:
    - "One dated amendment document carries BOTH replacement decision rows verbatim, the causal record of the weeks-long invisible outage with its citations, and a STATUS + EFFECTIVE clause tying force to the release that completes SWEEP-01..03"
    - ".claude/includes/decisions.md rows 1 and 8 are UNCHANGED by this plan (application rides Phase 252's release cut) - asserted by a negative test, unless the navigator explicitly rules inline-now at the Task 1 checkpoint"
    - "brain-connector declares Form B hitl_stages (stage brain-refusal-fork, shape F.1, mode gate) while connector.excluded stays true; the shape gate shows no NEW warns vs baseline and the R1 connector ledger passes"
    - "The doc-now/rows-at-sweep interpretation of 'rewrites Decisions #1 and #8 TOGETHER' is explicitly flagged to and ratified by the navigator - never auto-picked"
  artifacts:
    - path: "docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md"
      provides: "The single reviewable amendment unit: preamble + effective clause, causal record (a-e), verbatim replacement rows #1 and #8, Decision #5 wording touch, consequential-edits ledger, ratification block"
      min_lines: 80
    - path: "skills/brain-connector/SKILL.md"
      provides: "Form B hitl_stages + hitl_why frontmatter; connector.excluded:true retained"
      contains: "hitl_stages"
    - path: "docs/HITL-SHAPE-DECLARATION-CONTRACT.md"
      provides: "Exempt-skills list updated (brain-connector moves to declared; four exempt remain)"
    - path: "tests/test-250-amendment-unit.cjs"
      provides: "Verbatim-rows assertions, citation presence, effective clause, decisions.md negative assertion, no-em-dash check"
  key_links:
    - from: "docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md"
      to: ".claude/includes/decisions.md"
      via: "verbatim replacement text for rows 1 and 8 (applied mechanically in 252's release)"
      pattern: "Honest refusal everywhere"
    - from: "skills/brain-connector/SKILL.md frontmatter"
      to: "scripts/check-shape-declaration.cjs"
      via: "R16 gate reads hitl_stages from the surface file itself"
      pattern: "brain-refusal-fork"
---

<objective>
Draft and ratify the doctrine amendment as ONE reviewable unit (HONEST-02): a dated
document rewriting Decisions #1 and #8 together, carrying the causal record of the
weeks-long invisible outage inside the amendment text itself, with an effective clause
bound to Phase 252's release (the amendment-sweep lockstep, HARD). Declare the refusal
fork's HITL shape via Form B hitl_stages on brain-connector (Part 11 / CIRS). Two
navigator gates: the application-timing decision (before drafting) and the ratification
sign-off (after).

Purpose: the constitution stops contradicting itself ON THE RECORD, not in a chat log,
while no intermediate release cut can ever carry rewritten rows over silently-degrading
guards (ROADMAP Progress lockstep rule, verbatim: "Phases 250 and 252 are never split
across releases").

Output: docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md (ratified), the Form B declaration
on brain-connector with clean gates, the updated declaration-contract doc, regenerated
dist mirrors, and the born-RED amendment unit test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/250-honesty-rail-doctrine-amendment/250-RESEARCH.md
@.claude/includes/decisions.md
@skills/brain-connector/SKILL.md
@skills/larry-personality/SKILL.md
@docs/HITL-SHAPE-DECLARATION-CONTRACT.md
@docs/2026-08-09-HANDOFF-brain-envelope-and-egress-guard.md
@docs/2026-08-09-HANDOFF-tier0-removal-milestone.md
@docs/2026-08-10-HANDOFF-build-the-loop-milestone.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Navigator call - amendment application timing</name>
  <decision>When do the rewritten Decisions #1/#8 rows land in .claude/includes/decisions.md?</decision>
  <context>
    The amendment-sweep lockstep (ROADMAP Progress, HARD) forbids any release cut carrying
    the rewritten rows while guards silently degrade. Betas may be cut between Phases 250
    and 252 (Phase 251 sits between). Research A2/A3 flag this as the ONE judgment call in
    this phase's sequencing - it is the navigator's, never auto-picked.
  </context>
  <options>
    <option id="option-doc-now">
      <name>Doc-now / rows-at-sweep (RESEARCH RECOMMENDATION)</name>
      <pros>Mechanically safe under ANY cut cadence: the ratified document's own effective clause ("in force with the release that completes SWEEP-01..03") makes every intermediate cut self-describing. decisions.md rows apply mechanically in 252's release. HONEST-02's "rewrites #1 and #8 TOGETHER as one reviewable unit" is satisfied by the doc carrying both replacement rows verbatim, reviewed and ratified in 250.</pros>
      <cons>decisions.md keeps the stale "graceful degradation" row text until 252 ships (mitigated: the amendment doc is tracked, ratified, and cross-referenced).</cons>
    </option>
    <option id="option-inline-now">
      <name>Inline rows in 250 with an effective-gate caveat in the row text</name>
      <pros>decisions.md reads the new doctrine immediately.</pros>
      <cons>Transiently honest at best: puts "Brain required" words on main before guards flip; any beta cut between 250 and 252 then carries the exact contradiction the lockstep rule names as "worse than either state" (research Pitfall 4). Requires freezing release cuts until 252 to be safe.</cons>
    </option>
  </options>
  <resume-signal>Select: option-doc-now or option-inline-now (one word is enough)</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The amendment unit - doc + Form B declaration + contract doc + gates</name>
  <files>tests/test-250-amendment-unit.cjs, docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md, skills/brain-connector/SKILL.md, docs/HITL-SHAPE-DECLARATION-CONTRACT.md, dist/generic-claude-dir/.claude/skills/brain-connector/SKILL.md, dist/zed/.agents/skills/brain-connector/SKILL.md</files>
  <behavior>
    Write tests/test-250-amendment-unit.cjs FIRST (node:test CJS), run it, record the RED
    output (doc absent). Tests:
    - Test 1: docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md exists and contains the new
      Decision #1 row VERBATIM and the new Decision #8 row VERBATIM (assert the exact
      strings fixed in the action below; if the navigator amends wording at the Task 3
      ratification, update doc and test string constants in the same commit).
    - Test 2: the STATUS + EFFECTIVE clause is present ("Ratified on merge" and "in force
      with the release that completes SWEEP-01..03" phrasing, per the Task 1 ruling).
    - Test 3: all five causal-record legs are cited - assert presence of the five anchor
      strings: the old clause quote, "brain-response-sanitize-hook", "e.reduce is not a
      function", the inert "activation:" frontmatter mention, and the 2026-08-10
      stale-plugin-cache reproduction reference.
    - Test 4 (lockstep guard, guards research Pitfall 4): .claude/includes/decisions.md
      row 1 still contains "Zero config; Larry works immediately." and row 8 still
      contains "graceful degradation everywhere" - i.e. rows NOT yet rewritten. If Task 1
      ruled option-inline-now, INVERT this assertion to match the ruling and say so in a
      test comment citing the checkpoint.
    - Test 5: the amendment doc contains no em-dash (U+2014).
  </behavior>
  <action>
    1. Draft docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md with the research Pattern 6
       structure IN ORDER (assuming option-doc-now; adapt the effective clause if the
       navigator ruled otherwise):
       (1) Preamble: amends .claude/includes/decisions.md rows 1 and 8 (row 5 wording
       touched); authority: navigator escalation gates 2026-08-09, Build-the-Loop
       ratification 2026-08-10; STATUS + EFFECTIVE clause: "Ratified on merge; IN FORCE
       with the release that completes SWEEP-01..03. No release cut carries the rewritten
       rows while any guard silently degrades (ROADMAP amendment-sweep lockstep, HARD)."
       (2) The causal record, inside the amendment text itself (navigator direction: "on
       the record, not in a chat log"): (a) the removed doctrine clause quoted verbatim
       with its historical file:line (skills/brain-connector/SKILL.md:31 as it stood
       before plan 250-01; quoting it HERE is safe - docs/ is outside the 250-01 fence
       scope by design); (b) the weeks-long outage: scripts/brain-response-sanitize-hook.cjs
       blanked 100 percent of Brain responses, "e.reduce is not a function" on every call,
       invisible precisely because the doctrine instructed silence (cite
       docs/2026-08-09-HANDOFF-brain-envelope-and-egress-guard.md); (c) the inert
       "activation:" frontmatter no-op - two clauses in one file, both pretending, neither
       doing (cite the tier0 handoff section 2); (d) the counterfeit framing: "can a user
       ever be served methodology that did not come from the Brain, without being told?
       The answer must be no"; (e) the live 2026-08-10 reproduction on a stale plugin
       cache (cite the build-the-loop handoff section 4). Include the filed data4sci rule
       "truncation is explicit rather than silent", and cite the MotherDuck
       refuse-rather-than-guess note as a FIRST-PARTY position (the navigator's own panel
       appearance - the handoff is explicit that it is not external validation). Note the
       corpus whitespace honestly: the refusal doctrine has no external precedent in the
       44-source corpus; the amendment owns it as the navigator's call.
       (3) New Decision #1 row, VERBATIM (this exact string, also asserted by Test 1):
       "One-command install; the Brain is part of what installs. Larry's methodology comes
       from the Brain and says so; a keyless session gets an honest refusal and a visible
       path to a key, never an imitation." Note the coupling is now RESOLVED in this row's
       favor: SEED-011 is DECIDED - Option A per-install silent registration, baked in by
       default (NAVIGATOR RULING 2026-08-10, recorded in REQUIREMENTS.md HONEST-03; the
       behavior ships in plan 250-04). Cite the ruling; the row's "one-command install"
       claim is strengthened, not threatened - a fresh install registers silently and the
       honest refusal remains for the failure edge.
       (4) New Decision #8 row, VERBATIM (this exact string, also asserted by Test 1):
       "Honest refusal everywhere. A Brain failure or readiness miss surfaces in-turn and
       auto-queues enrichment; no surface conceals a failure or serves methodology the
       graph did not give."
       (5) Decision #5 wording touch: Brain stays remote MCP; drop the implied optionality
       (per the tier0 handoff section 3); give the replacement rationale text.
       (6) Consequential-edits ledger for the SWEEP release: the decisions.md rows
       themselves, CLAUDE.md lines 19 and 84, docs/install/BRAIN-SETUP.md's broader Tier-0
       prose, the tier-0-no-key fixture inversion (SWEEP-02), the 121-doc list (252
       re-measures), the source:'tier0' hardcoded chains in brain-client.cjs (site 11,
       marked in 250-01, flipped in SWEEP-01), and the SEED-011 registration behavior
       (ruled 2026-08-10: ships INSIDE this milestone via plan 250-04's cross-repo work +
       operator deploy; the cross-repo definition of done applies - not done until the
       user-reached surface works).
       (7) Ratification block: navigator sign-off line, left "pending" until Task 3.
       House rules: hyphens only, no em-dashes, Feynman-plain prose.
    2. Add the Form B declaration to skills/brain-connector/SKILL.md frontmatter (research
       Pattern 5 - Form B, NOT Form A: scalar hitl_shape + connector.excluded:true trips
       gate predicate 2b, while hitl_stages + excluded passes clean, the larry-personality
       precedent). Insert exactly:
       hitl_stages:
         - stage: "brain-refusal-fork"
           shapes: ["F.1"]
           mode: "gate"
       hitl_why: "A Brain failure or readiness miss is a genuine Decision-Gate fork: the navigator picks the next move (connect the key, retry, use partial graph material with provenance, or continue without methodology) - never silently degraded."
       connector.excluded: true STAYS with its existing reason - removing it makes the
       surface a gap in the R1 connector-coverage ledger and HARD-FAILS
       build-connector-registry --check. FENCE NOTE: the hitl_why tail deliberately reads
       "never silently degraded", NOT the two fenced doctrine phrases - the 250-01 fence
       scans skills/ including frontmatter and must stay green.
    3. Update docs/HITL-SHAPE-DECLARATION-CONTRACT.md: the exempt-skills list (line ~123)
       currently names brain-connector among the five exempt skills; move brain-connector
       to declared, leaving four exempt - otherwise the contract doc contradicts the
       gate's reality. Do NOT touch the conversation-mode field-reuse collision (accepted
       WARN, out of scope, follow-up already filed).
    4. Gates, in order, with the baseline captured BEFORE the frontmatter edit:
       a. `node scripts/check-shape-declaration.cjs --check` before the edit - save the
          warn list to the scratchpad as baseline.
       b. After the edit: re-run; assert NO NEW warns vs baseline and brain-connector
          absent from the warn list (predicate 2b clean via Form B).
       c. `node scripts/build-connector-registry.cjs --check` - R1 ledger unbroken.
    5. Regenerate dist mirrors (`node scripts/build-dist-bundles.cjs`), confirm
       `--check-stale` exit 0, re-run `node --test tests/test-250-doctrine-fence.cjs`
       (the frontmatter edit propagates to dist; the fence must stay green), then the
       amendment unit test to green and the full runner.
  </action>
  <verify>
    <automated>node --test tests/test-250-amendment-unit.cjs && node scripts/check-shape-declaration.cjs --check && node scripts/build-connector-registry.cjs --check && node scripts/build-dist-bundles.cjs --check-stale && node --test tests/test-250-doctrine-fence.cjs && bash tests/run-all-250.sh</automated>
  </verify>
  <done>Red output recorded (doc absent), then: amendment doc complete with all seven parts and both verbatim rows; decisions.md rows verified UNCHANGED (or per Task 1 ruling); Form B declaration live with zero new gate warns; R1 ledger green; contract doc lists four exempt skills; dist fresh; doctrine fence still green; full runner green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Navigator ratification of the amendment unit</name>
  <what-built>
    The complete amendment unit: docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md (both
    replacement rows verbatim, causal record a-e with citations, effective clause,
    consequential-edits ledger, pending sign-off line), the Form B hitl_stages
    declaration on brain-connector with clean R16/R1 gates, and the updated
    declaration-contract doc.
  </what-built>
  <how-to-verify>
    1. Read docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md top to bottom.
    2. Confirm the causal record is the record you want ON THE RECORD: the verbatim old
       clause, the sanitize-hook outage, the inert activation key, the counterfeit
       framing question, the 2026-08-10 stale-cache reproduction.
    3. EXPLICIT INTERPRETATION FLAG (research A3 - the one judgment call): HONEST-02 says
       "rewrites Decisions #1 and #8 TOGETHER as one reviewable unit". This plan satisfies
       that with the ratified document carrying both replacement rows verbatim; the
       decisions.md row application is mechanical in Phase 252's release per your Task 1
       ruling. Confirm this interpretation, or direct the inline variant.
    4. Confirm the two verbatim replacement rows read exactly as you want them applied in
       252 (any wording change now updates doc + test constants in the same commit).
    5. On approval: the executor fills the ratification block (navigator, date 2026-08-10
       or the actual date), re-runs `node --test tests/test-250-amendment-unit.cjs` and
       `bash tests/run-all-250.sh` to confirm green with the signed doc.
  </how-to-verify>
  <resume-signal>Type "ratified" to sign off, or describe the changes required (wording edits loop back through Task 2's test constants).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| amendment doc -> constitution | replacement row text becomes binding doctrine at 252's release |
| SKILL.md frontmatter -> R16/R1 gates | declaration changes feed two build gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-250-05 | Repudiation | amendment ratification | mitigate | causal record + authority + dated sign-off live INSIDE the tracked doc; ratification is a blocking human checkpoint, never auto-approved |
| T-250-06 | Tampering | premature row application | mitigate | Test 4's negative assertion on decisions.md rows 1/8 fails the suite if anyone rewrites the rows before 252 (lockstep guard) |
| T-250-07 | Elevation of privilege | dropping connector.excluded | mitigate | build-connector-registry --check hard-fails on the R1 gap; the task keeps excluded:true explicitly |

No package installs this phase (zero-npm-deps hard convention).
</threat_model>

<verification>
- `node --test tests/test-250-amendment-unit.cjs` green after ratification (RED run recorded first)
- `node scripts/check-shape-declaration.cjs --check` - no new warns vs the pre-edit baseline; brain-connector clean via Form B
- `node scripts/build-connector-registry.cjs --check` green (excluded retained)
- `node scripts/build-dist-bundles.cjs --check-stale` exit 0 and `node --test tests/test-250-doctrine-fence.cjs` green (frontmatter propagated to dist without re-introducing fence phrases)
- `.claude/includes/decisions.md` rows 1/8 unchanged (unless navigator ruled inline-now)
</verification>

<success_criteria>
- HONEST-02 complete: one dated, ratified amendment document rewriting Decisions #1 and #8
  together with the causal record inside it; the refusal fork's HITL shape declared Form B
  with clean gates; application timing decided by the navigator at a blocking gate; no
  possible release cut between 250 and 252 carries the contradiction the lockstep rule
  names.
</success_criteria>

<output>
Create `.planning/phases/250-honesty-rail-doctrine-amendment/250-02-SUMMARY.md` when done,
recording the Task 1 ruling, the ratification outcome, the gate baseline diff, and the
amendment unit test's RED output.
</output>
