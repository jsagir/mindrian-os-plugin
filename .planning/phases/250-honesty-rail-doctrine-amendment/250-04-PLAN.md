---
phase: 250-honesty-rail-doctrine-amendment
plan: 04
type: execute
wave: 4
depends_on: [250-01, 250-02, 250-03]
notes: "DEVIATION (coordinator-authorized 2026-08-10): 4-plan set where research recommended 3. NAVIGATOR RULING 2026-08-10 (REQUIREMENTS.md HONEST-03) converted SEED-011 from a decision doc into baked-in BEHAVIOR - per-install silent registration ships inside this milestone, adding cross-repo work that did not fit the 3-plan shape."
files_modified:
  - lib/core/resolve-brain-key.cjs
  - lib/core/brain-client.cjs
  - lib/core/tier0-messaging.cjs
  - skills/brain-connector/SKILL.md
  - docs/install/BRAIN-SETUP.md
  - docs/BRAIN-IDENTITY-DESIGN.md
  - dist/generic-claude-dir/.claude/skills/brain-connector/SKILL.md
  - dist/zed/.agents/skills/brain-connector/SKILL.md
  - tests/test-250-silent-registration.cjs
  - tests/test-127-00-shim-handshake.sh
  - "cross-repo /home/jsagi/dev/ProblemsWorthSolving-Brain: src/http/app.mjs mount + new src/http/register.mjs + tests/register-endpoint.test.mjs (exact files per in-task recon)"
autonomous: false
execution_model: sonnet
requirements: [HONEST-03]
user_setup:
  - service: "ProblemsWorthSolving-Brain on Render (pws-brain-mcp.onrender.com)"
    why: "The /register endpoint deploy is an operator act: push + Render redeploy; the admin key is not on this machine by design (246-02/247-03 precedent)"
    dashboard_config:
      - task: "Confirm Render auto-deploy picked up the brain repo push (or trigger a manual deploy)"
        location: "Render dashboard -> pws-brain-mcp service"
must_haves:
  truths:
    - "A fresh install with NO key silently registers (UUID -> POST /register -> cached install token) at first Brain consult and gets methodology with ZERO ceremony - baked in by default (NAVIGATOR RULING 2026-08-10, REQUIREMENTS.md HONEST-03)"
    - "Existing keyed users are untouched: env and ~/.mindrian.env and CWD .env all WIN over the install token in the resolver ladder - the first three legs resolve byte-identically to today"
    - "The no_key refusal is the FAILURE EDGE only (registration failed / offline), never the default experience; its copy says so and still offers the manual key path as the override"
    - "The /register endpoint validates a strict UUIDv4-only payload, is idempotent per install_id, rate-limited on the keyless path, and mints READ-tier tokens only; the threat model is on the record in docs/BRAIN-IDENTITY-DESIGN.md"
    - "Refusal, provenance, queue disclosure, and silent registration are verified live on all three surfaces on a RELEASED build after restart - never claimed from the dev session that edited the files"
  artifacts:
    - path: "docs/BRAIN-IDENTITY-DESIGN.md"
      provides: "Option A design: endpoint contract, threat model, rate/WAF hardening, revocation + telemetry notes, opt-out var"
      min_lines: 60
    - path: "lib/core/resolve-brain-key.cjs"
      provides: "Fourth ladder leg: install-token cache at ~/.mindrian-install.json (0600), source 'install-token', lowest precedence"
    - path: "tests/test-250-silent-registration.cjs"
      provides: "Loopback /register mock: ladder precedence, mint+cache, failure edge, opt-out var, once-per-process cap"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/register-endpoint.test.mjs"
      provides: "Endpoint contract born RED: UUID validation, idempotency, rate limit, read tier"
  key_links:
    - from: "lib/core/resolve-brain-key.cjs"
      to: "~/.mindrian-install.json"
      via: "install-token leg after the three existing legs"
      pattern: "install-token"
    - from: "lib/core/brain-client.cjs first consult"
      to: "POST /register"
      via: "lazy one-shot registration when the ladder resolves nothing"
      pattern: "register"
    - from: "src/http/app.mjs (brain repo)"
      to: "src/http/register.mjs"
      via: "express route mount ahead of the MCP handler"
      pattern: "/register"
---

<objective>
Ship SEED-011 as BEHAVIOR (the HONEST-03 registration leg): per-install silent
registration, Option A, baked in by default - the decision is ALREADY MADE (NAVIGATOR
RULING 2026-08-10, recorded in REQUIREMENTS.md HONEST-03; no decision checkpoint exists
in this plan). A fresh install mints a UUID, calls the brain repo's new /register
endpoint, caches a READ-tier install token, and Larry serves methodology with no API-key
ceremony. Existing keyed users win over the install token at every ladder leg. The no_key
refusal becomes the failure edge (registration failed / offline), never the default
experience. Cross-repo rule binds: not done until the user-reached surface works - so the
plan ends with the operator deploy ceremony plus the phase's single three-surface
human-verify on a RELEASED build after restart.

Purpose: honesty without nagging completes HONEST-03 - provenance (250-03) tells the user
where methodology came from; silent identity removes the ceremony that would have made
the honest refusal a recurring nag.

Output: brain-repo /register endpoint (born-RED tests, local commits; deploy at the
checkpoint), the plugin-side registration leg in the resolve-brain-key ladder, the
reframed no_key failure-edge copy, docs/BRAIN-IDENTITY-DESIGN.md, and the released-build
three-surface verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/250-honesty-rail-doctrine-amendment/250-RESEARCH.md
@.planning/seeds/SEED-011-brain-silent-identity.md
@lib/core/resolve-brain-key.cjs
@lib/core/brain-client.cjs
@lib/core/tier0-messaging.cjs
@docs/install/BRAIN-SETUP.md
@/home/jsagi/dev/ProblemsWorthSolving-Brain/CLAUDE.md
@/home/jsagi/dev/ProblemsWorthSolving-Brain/src/http/app.mjs
@/home/jsagi/dev/ProblemsWorthSolving-Brain/src/http/rate-limit.mjs
@/home/jsagi/dev/ProblemsWorthSolving-Brain/src/http/supabase-keys.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Brain-repo /register endpoint + identity design doc (local commits, no deploy)</name>
  <files>/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/register-endpoint.test.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/src/http/register.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/src/http/app.mjs, docs/BRAIN-IDENTITY-DESIGN.md, .planning/seeds/SEED-011-brain-silent-identity.md</files>
  <behavior>
    RECON FIRST (single pass, no re-reads): read the brain repo's CLAUDE.md,
    src/http/app.mjs (express + createMcpHandler, 256kb body cap), src/http/auth.mjs,
    src/http/rate-limit.mjs (custom limiter: sha256-of-Authorization keying, socket
    address fallback for keyless requests, DEFAULT_MAX 120/window), and
    src/http/supabase-keys.mjs (the existing key-minting machinery). Then write
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/register-endpoint.test.mjs FIRST
    (ESM, node --test, the repo's tests/*.test.mjs convention), run it, record the RED
    output. Contract tests:
    - Test 1: POST /register with body {"install_id":"<valid UUIDv4>"} returns 200 with
      {token, tier:'read'}; the token authenticates a subsequent read-tier call.
    - Test 2 (idempotency): the SAME install_id POSTed twice yields ONE identity - the
      second call returns the same (or refreshed-for-the-same-identity) token, never a
      second unrelated key (no unbounded minting).
    - Test 3 (strict input): missing install_id, non-UUID string, extra fields in the
      body, or a non-JSON body -> 400. The schema is closed: install_id ONLY (Part 8
      posture: nothing else ever crosses in the registration payload).
    - Test 4 (rate limit): a keyless burst beyond the register-specific cap from one
      client -> 429 (the limiter's socket-address fallback path).
    - Test 5 (tier ceiling): a registered token can NEVER reach an admin/write tool -
      assert the auth layer scopes it to the read tier.
  </behavior>
  <action>
    1. Implement src/http/register.mjs mounted in src/http/app.mjs AHEAD of the MCP
       handler: POST /register, unauthenticated BY DESIGN (it mints credentials), body
       parsed under the existing 256kb express.json cap. Validate install_id against a
       strict UUIDv4 regex; reject any extra body fields. Mint through the EXISTING
       supabase-keys.mjs machinery at READ tier (reuse before build - never a second
       minting path); persist install_id -> key identity so re-registration is idempotent.
       Never log the token value (log the sha256-16 prefix, the repo's existing hashing
       posture).
    2. Rate limiting: give /register its own tighter cap (recommend 5 per window per
       socket address - registration is a once-per-install event; DEFAULT_MAX 120 is for
       authenticated traffic). Reuse the existing rate-limit.mjs mechanism; do not vendor
       a new limiter. Cloudflare/WAF at the Render edge stays the outer bound - record
       the hardening posture in the design doc, do not attempt WAF config from code.
    3. Write docs/BRAIN-IDENTITY-DESIGN.md in the PLUGIN repo (the tracked, user-reachable
       home): the ruling citation (NAVIGATOR RULING 2026-08-10, REQUIREMENTS.md HONEST-03:
       Option A baked in by default; B rejected - extractable embedded key; C dead by
       navigator ruling - the keyless-serves-degraded gradient SWEEP-02 inverts against);
       the endpoint contract (request/response, idempotency, rate caps, tier ceiling);
       the threat model (see this plan's STRIDE register - token farming, replay,
       enumeration, extraction); revocation path (per-install identity makes revocation
       and rate-limiting per install possible - the seed's own rationale); the telemetry
       note the seed asks for; the MINDRIAN_DISABLE_AUTO_REGISTER opt-out contract; and
       the ceremony statement: manual keys (mindrian-os.com/brain-access,
       ~/.mindrian.env, chmod 600) remain the OVERRIDE path and always win the ladder.
    4. Update .planning/seeds/SEED-011-brain-silent-identity.md: status resolved, pointer
       to the ruling (REQUIREMENTS.md HONEST-03, 2026-08-10) and to the design doc
       (gitignored local record; no git add -f needed).
    5. Commit LOCALLY in the brain repo (its own conventions). NO push, NO deploy - the
       live surface changes only at Task 3's operator ceremony. Run the endpoint suite
       to green plus the brain repo's full `node --test tests/` (no regressions).
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/register-endpoint.test.mjs && node --test tests/</automated>
  </verify>
  <done>Red output recorded, then: endpoint contract green (UUID-strict, idempotent, rate-limited, read-tier-ceilinged, token never logged); brain repo suite green; design doc on the record in the plugin repo; seed marked resolved; zero deploys performed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plugin-side silent registration + no_key failure-edge copy</name>
  <files>tests/test-250-silent-registration.cjs, lib/core/resolve-brain-key.cjs, lib/core/brain-client.cjs, lib/core/tier0-messaging.cjs, skills/brain-connector/SKILL.md, docs/install/BRAIN-SETUP.md, tests/test-127-00-shim-handshake.sh, dist/generic-claude-dir/.claude/skills/brain-connector/SKILL.md, dist/zed/.agents/skills/brain-connector/SKILL.md</files>
  <behavior>
    Write tests/test-250-silent-registration.cjs FIRST (node:test CJS, loopback node:http
    /register mock + temp HOME, the test-249-capture-seam.cjs precedent - never live in
    unit tests), run it, record the RED output. Tests:
    - Test 1 (ladder precedence FROZEN): with MINDRIAN_BRAIN_KEY set, resolveBrainKey()
      returns source 'env' even when a cached install token exists; same for
      ~/.mindrian.env ('mindrian-env-file') and CWD .env ('cwd-env-file'). The first
      three legs resolve byte-identically to today - existing keyed users untouched.
    - Test 2 (mint + cache): no key anywhere, no cache: the first Brain consult mints a
      UUID (crypto.randomUUID), POSTs {install_id} to the mock /register, writes
      ~/.mindrian-install.json (temp HOME) with {install_id, token, minted_at} at mode
      0600, and the resolver then returns the token with source 'install-token'.
      Subsequent resolves read the cache with ZERO further POSTs (assert the mock's
      request counter).
    - Test 3 (failure edge): mock returns 500 / connection refused: the resolver reports
      available:false with an honest registration-failed reason; the no_key refusal copy
      renders the FAILURE-EDGE framing (contains "registration" or "offline", does NOT
      lead with a demand to go get a key); the registration attempt is capped at once
      per process (the _reasonLoggedThisProcess precedent) and NEVER throws or blocks.
    - Test 4 (opt-out): MINDRIAN_DISABLE_AUTO_REGISTER=1 suppresses the leg entirely -
      no UUID minted, no POST, keyless resolves exactly as today (deterministic for
      harnesses and the 82 degradation tests).
    - Test 5 (identity hygiene, Part 8): the registration POST body contains ONLY
      install_id - assert the mock never receives room paths, user prose, or env
      contents.
  </behavior>
  <action>
    1. lib/core/resolve-brain-key.cjs: add the FOURTH ladder leg (after env,
       ~/.mindrian.env, CWD .env): read ~/.mindrian-install.json, stat-check mode 0600
       (the SEC-02 posture the key file already gets), return
       {key: token, source: 'install-token', available: true}. The leg is read-only here;
       minting lives in brain-client (the resolver stays a pure resolver).
    2. lib/core/brain-client.cjs: lazy one-shot registration at the first Brain consult
       when the ladder resolves nothing AND MINDRIAN_DISABLE_AUTO_REGISTER is unset:
       mint crypto.randomUUID(), POST /register to the configured Brain base URL
       (brain-client line ~24 single source), on 200 write the cache file 0600 and
       proceed with the consult; on any failure record the honest reason, cap the attempt
       once per process, and fall through to the no_key refusal path. NEVER at session
       start, NEVER in sensors/decide (the hot-path fence binds), NEVER blocking - the
       registration rides the consult that needed it. The AVAIL-02 retry budget from
       plan 250-01 does NOT apply to /register (one attempt per process is the cap;
       a failed registration is the failure edge, not a transient to hammer).
    3. lib/core/tier0-messaging.cjs: reframe the no_key strings for the failure edge -
       REASONS.no_key, NEXT_MOVES.no_key, UPGRADE_HINT: silent registration is the
       default; the refusal fires when registration failed or the Brain is offline;
       the manual key (~/.mindrian.env, mindrian-os.com/brain-access) is offered as the
       OVERRIDE path, not the ceremony. Keep the byte-locked wire keys and
       DIRECTOR_NOT_AVAILABLE status untouched (values evolve, keys never - and keep
       /Larry/ matching FALLBACK_ADVICE). Update the SKILL.md Refusal section's no_key
       copy to match (plan 250-01 wrote it ceremony-first as true-then; this task makes
       it true-now), and update BRAIN-SETUP.md's key-request paragraph: fresh installs
       register silently; the manual key path remains for operators and overrides
       (broader Tier-0 prose still rides 252).
    4. tests/test-127-00-shim-handshake.sh: export MINDRIAN_DISABLE_AUTO_REGISTER=1 in
       its harness environment so Test 8's no-key DIRECTOR_NOT_AVAILABLE assertion stays
       deterministic once the live endpoint exists (otherwise a live harness run would
       silently register and break the fixture). Same guard for any other keyless fixture
       the executor finds keying on DIRECTOR_NOT_AVAILABLE (grep tests/ for the constant;
       the full inversion rides SWEEP-02, this is only determinism hygiene).
    5. Regenerate dist, `--check-stale` exit 0, doctrine fence + provenance fence still
       green, full runner green, tier0 lock test green.
  </action>
  <verify>
    <automated>node --test tests/test-250-silent-registration.cjs && node lib/core/tier0-messaging.test.cjs && node --test tests/test-250-doctrine-fence.cjs tests/test-250-provenance-fence.cjs && node scripts/build-dist-bundles.cjs --check-stale && bash tests/run-all-250.sh</automated>
  </verify>
  <done>Red output recorded, then: ladder precedence frozen for keyed users; fresh-install mint+cache proven against the loopback mock with a request-counted single POST; failure edge renders the reframed no_key copy and never throws, blocks, or repeats past once per process; opt-out var suppresses everything; Part 8 payload audit green; all fences and the full runner green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator deploy + three-surface verify on a RELEASED build (the restart-to-apply rule)</name>
  <what-built>
    The complete Phase 250 surface: the refusal rail with transport retry (250-01), the
    ratified amendment + Form B declaration (250-02), the provenance contract (250-03),
    and silent registration end to end (250-04 Tasks 1-2, endpoint not yet live).
  </what-built>
  <how-to-verify>
    NAMED RULE: a commit is NOT live until a release ships AND is picked up; skills load
    from the plugin cache and a running session never hot-reloads (four independent filed
    occurrences). Nothing below counts from the dev session that edited the files.

    PART A - operator deploy ceremony (brain repo; admin act, 246-02/247-03 precedent):
    1. Push the brain repo commits; confirm Render redeploys pws-brain-mcp (dashboard or
       deploy hook).
    2. Live probe: POST https://pws-brain-mcp.onrender.com/register with a scratch UUID
       returns 200 {token, tier:'read'}; an immediate keyless burst returns 429; a
       malformed body returns 400. Record the responses (token value redacted).

    PART B - released plugin build:
    3. Cut rides the release train per ROADMAP Progress (v2.0.0-beta.N once Gates 0/1
       clear - this checkpoint PAUSES here if the train is not open; that pause is the
       honest state, say so rather than verifying on dev).
    4. `/plugin marketplace update` then `claude plugin update mos@mindrian-marketplace`,
       then RESTART the session.

    PART C - the three-surface matrix (CLI, Desktop, Cowork - Tri-Polar; the
    test-245 claim-(b) precedent for plugin-scoped vs project-scoped tool names):
    5. FRESH-INSTALL LEG: no key set, delete ~/.mindrian-install.json, restart: a
       methodology ask WORKS with zero ceremony (silent registration), and the answer
       ends with one "■ BRAIN: ..." source line.
    6. KEYED-USER LEG: with MINDRIAN_BRAIN_KEY set: behavior identical to before this
       phase (source env wins; no registration traffic).
    7. UNREACHABLE LEG: valid key, Brain blocked (or use a wrong URL override): the
       refusal names unreachability AFTER the bounded retry, and NEVER says the key is
       not set (the dead conflation bug).
    8. NOT_READY LEG: consult a framework known below 3/4: the refusal says the graph
       does not have it structured AND says "queued for enrichment"; confirm the entry
       in <room>/.mindrian/enrichment-queue.json with source refusal.
    9. PROVENANCE ABSENCE LEG: a plain conversation turn carries NO ■ BRAIN line (absence
       is the signal); the F.1 card FIRES on card-capable surfaces (a card, never a drawn
       box).
    10. LOCKSTEP GLANCE: in the released build, .claude/includes/decisions.md rows 1 and
        8 still read the OLD text (the amendment is ratified-not-in-force; rows flip with
        252's release).
  </how-to-verify>
  <resume-signal>Type "verified" with the surface matrix results, or describe failures per leg (a U+25A0 glyph-confusion report triggers the [BRAIN] fallback recorded in 250-03).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous internet -> /register | unauthenticated endpoint that mints credentials |
| plugin first-run -> Brain server | install UUID crosses the wire (the ONLY thing that does) |
| install-token cache -> resolver ladder | a file on disk becomes a credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-250-11 | Spoofing / abuse | /register token farming | mitigate | strict UUIDv4-only closed schema, idempotency per install_id, register-specific rate cap (~5/window/address) inside the existing limiter, Cloudflare/WAF outer bound (design doc) |
| T-250-12 | Elevation of privilege | registered token scope | mitigate | READ tier ceiling minted through the existing supabase-keys machinery; Test 5 asserts admin/write is unreachable |
| T-250-13 | Information disclosure | registration payload (Part 8) | mitigate | body carries install_id ONLY; Test 5 (plugin side) audits the outbound payload; extra fields rejected server-side |
| T-250-14 | Information disclosure | token at rest / in logs | mitigate | cache file mode 0600 (SEC-02 posture), token never logged (sha256-16 prefix only, both repos) |
| T-250-15 | Denial of service | registration retries hammering the endpoint | mitigate | once-per-process attempt cap; AVAIL-02 retry budget explicitly does NOT apply to /register |
| T-250-16 | Repudiation | untracked identity decisions | mitigate | docs/BRAIN-IDENTITY-DESIGN.md carries the ruling citation, contract, threat model, and revocation path on the record |

No package installs in either repo (zero-npm-deps plugin convention; brain repo reuses its vendored stack).
</threat_model>

<verification>
- Brain repo: `node --test tests/register-endpoint.test.mjs` + full `node --test tests/` green (RED runs recorded first)
- Plugin: `node --test tests/test-250-silent-registration.cjs` green, ladder precedence + Part 8 payload audit included
- `bash tests/run-all-250.sh` + both fences + `node lib/core/tier0-messaging.test.cjs` + `node scripts/build-dist-bundles.cjs --check-stale` green
- Live: /register probed on Render (200/429/400 legs) at the checkpoint, and the ten-step three-surface matrix completed on a released, restarted build
</verification>

<success_criteria>
- HONEST-03 complete per the 2026-08-10 ruling: provenance (250-03) + silent identity as
  default BEHAVIOR on the user-reached surface (cross-repo definition of done), with the
  no_key refusal reduced to an honest failure edge - honesty never became nagging.
- Existing keyed users provably untouched; keyless fixtures kept deterministic via the
  opt-out var (full inversion remains SWEEP-02's).
- The phase's live claims rest ONLY on the released-build checkpoint, never the dev
  session.
</success_criteria>

<output>
Create `.planning/phases/250-honesty-rail-doctrine-amendment/250-04-SUMMARY.md` when done,
recording both repos' RED outputs, the live probe responses (token redacted), the
three-surface matrix results, and the release version verified against.
</output>
