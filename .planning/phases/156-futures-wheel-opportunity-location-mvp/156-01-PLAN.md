---
phase: 156-futures-wheel-opportunity-location-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/futures.md
  - lib/core/futures/orchestrator.cjs
  - lib/core/futures/causal-cue.cjs
  - tests/test-futures-causal-cue.cjs
  - tests/test-futures-frontmatter.cjs
autonomous: true
requirements: [FW-01, FW-03, FW-04]
canon_parts: [Part 2, Part 3, Part 4, Part 7, Part 8]
must_haves:
  truths:
    - "A navigator can invoke /mos:futures [concept] and the command file resolves with connector frontmatter"
    - "Each proposed consequence is flagged cue-supported or cue-thin; neither is auto-dropped"
    - "A consequence frontmatter object validates horizon (enum), confidence (0-1 float), and PESTEL domain (enum)"
  artifacts:
    - path: "commands/futures.md"
      provides: "The /mos:futures command surface + Part 7 chain-not-duplicate justification + connector frontmatter"
      contains: "connector:"
    - path: "lib/core/futures/causal-cue.cjs"
      provides: "Advisory causal-cue lexicon flagger (cue-supported / cue-thin), never auto-drops"
      exports: ["flagCausalCue", "CAUSAL_CUE_LEXICON"]
    - path: "lib/core/futures/orchestrator.cjs"
      provides: "Orchestrator scaffold exporting the consequence frontmatter validators + the depth/fan-out cap constants"
      exports: ["validateConsequenceFrontmatter", "FUTURES_DEPTH_CAP", "FUTURES_FANOUT_CAP", "HORIZON_ENUM", "PESTEL_DOMAIN_ENUM"]
  key_links:
    - from: "commands/futures.md"
      to: "lib/core/futures/orchestrator.cjs"
      via: "command body instructs Larry to call the orchestrator"
      pattern: "lib/core/futures/orchestrator"
    - from: "lib/core/futures/orchestrator.cjs"
      to: "lib/core/futures/causal-cue.cjs"
      via: "require + flagCausalCue call"
      pattern: "require.*causal-cue"
---

<objective>
Scaffold the net-new /mos:futures command surface, the orchestrator module shell (exporting the frontmatter validators + the bounded depth/fan-out cap constants the later waves consume), and the advisory causal-cue flagger. This is the interface-first wave: it defines the consequence frontmatter contract (horizon / confidence / PESTEL domain) and the cap constants that Wave 2's generation loop builds against, so Wave 2 receives the contract in-hand and never explores for it.

Purpose: FW-01 (the command exists, spine-wired, Part 7-justified), FW-03 (advisory causal-cue flag, reuse-not-ML, never drops), FW-04 (consequence frontmatter deltas validated). No graph writes, no HSI, no Larry generation loop yet (those are Wave 2).
Output: commands/futures.md, lib/core/futures/orchestrator.cjs (scaffold + validators + caps), lib/core/futures/causal-cue.cjs, and two unit test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-SPEC.md
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-CONTEXT.md
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-RESEARCH.md
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-VALIDATION.md
@docs/MINDRIAN-CANON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold /mos:futures command + orchestrator shell with frontmatter validators and caps</name>
  <read_first>
    - commands/systems-thinking.md (lines 1-80: the F-selector + connector frontmatter analog -- connects_to_spine, sensor_triggers, reach_id, sub_mode, framework, posture, hierarchy_rank, filing, surface)
    - commands/whitespace.md (lines 1-35: the connector frontmatter shape for an HSI/Python-wrapping methodology command + frameworks/produces/inputs/autonomous_safe keys)
    - lib/core/opportunity-ops.cjs (the parseFrontmatter helper near line 24 -- reuse, do NOT hand-roll a YAML parser)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-SPEC.md (FW-01, FW-04 acceptance bars)
  </read_first>
  <action>
    Reuse-before-build (Part 7): commands/futures.md is net-new because none of /mos:explore-futures, /mos:scenario-plan, /mos:explore-trends build a living multi-ring consequence graph with HSI bridge scan + opportunity banking; the command body MUST carry a Part 7 justification block that names each of those existing foresight commands and states chain-not-duplicate for each (this is the FW-01 acceptance). lib/core/futures/orchestrator.cjs is net-new thin orchestration over shipped engines (navigation.writeEdge, compute-hsi.py, hsi-to-graph.cjs, opportunity-ops.bankOpportunity, command-resolver) -- repointing is insufficient because no shipped module owns the guided-by-ring file-then-scan sequence.

    Create commands/futures.md with frontmatter mirroring the systems-thinking.md + whitespace.md analogs: name futures, description, body_shape methodology, kind methodology, frameworks ["Futures Wheel"], produces "room/opportunity-bank/futures-*/**", autonomous_safe false (it has HITL gates), ui_reference skills/ui-system/SKILL.md, allowed-tools (Read, Write, Bash, Glob, AskUserQuestion), and a connector: block with connects_to_spine true, reach_id context_block (do NOT mint a new reach), sub_mode futures-wheel, framework "Futures Wheel", posture hold, filing fileEvidenceWithReadback, surface F.1. The body declares D-01 guided-by-ring as the literal command loop, D-03 subsystem-PESTEL-map-default render, and instructs Larry to drive the orchestrator at lib/core/futures/orchestrator.cjs. NO em-dashes; hyphens only.

    Create lib/core/futures/orchestrator.cjs as the module shell. Export the bounded caps as named constants FUTURES_DEPTH_CAP (default 3) and FUTURES_FANOUT_CAP (default 5), both navigator-overridable (accept an opts override but clamp to these maxima). Export HORIZON_ENUM = the frozen set {near, mid, long} and PESTEL_DOMAIN_ENUM = the frozen set {Political, Economic, Social, Technological, Environmental, Legal}. Export validateConsequenceFrontmatter(fm) returning { valid, errors } that checks: horizon is in HORIZON_ENUM, confidence is a float in the inclusive range 0.0-1.0, domain is in PESTEL_DOMAIN_ENUM (FW-04). Reuse opportunity-ops parseFrontmatter for any frontmatter parsing. Stub the generation/HSI/gate functions Wave 2-4 will fill with a clearly-labeled "implemented in Wave N" comment so this wave has zero graph/HSI surface. NO em-dashes.
  </action>
  <acceptance_criteria>
    - SOURCE: `test -f commands/futures.md && grep -q 'connector:' commands/futures.md` exits 0
    - SOURCE: commands/futures.md body contains a Part 7 block naming explore-futures, scenario-plan, and explore-trends with a chain-not-duplicate statement (grep finds all three command names)
    - SOURCE: `grep -c 'reach_id: context_block' commands/futures.md` is 1 (no new reach minted)
    - BEHAVIOR: require('lib/core/futures/orchestrator.cjs') exposes FUTURES_DEPTH_CAP===3, FUTURES_FANOUT_CAP===5, HORIZON_ENUM and PESTEL_DOMAIN_ENUM as the exact frozen sets, and validateConsequenceFrontmatter as a function
    - TEST-COMMAND: `node tests/test-futures-frontmatter.cjs` passes (created in Task 3)
    - NO em-dashes: `grep -c $'\u2014' commands/futures.md lib/core/futures/orchestrator.cjs` is 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "const o=require('./lib/core/futures/orchestrator.cjs'); if(o.FUTURES_DEPTH_CAP!==3||o.FUTURES_FANOUT_CAP!==5||typeof o.validateConsequenceFrontmatter!=='function') process.exit(1)" && test -f commands/futures.md && grep -q 'connector:' commands/futures.md</automated>
  </verify>
  <done>commands/futures.md exists with connector frontmatter + Part 7 chain-not-duplicate block; orchestrator.cjs exports the caps, the two enums, and validateConsequenceFrontmatter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Advisory causal-cue flagger (lexicon, never auto-drops)</name>
  <read_first>
    - lib/core/opportunity-ops.cjs (parseFrontmatter helper near line 24 -- reuse for any frontmatter access)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-RESEARCH.md (section 9: causal-cue advisory pass -- static lexicon, no ML, no new dependency, reuse regex/parse helpers, advisory only)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-SPEC.md (FW-03 acceptance: cue-phrased flagged cue-supported, non-cue flagged cue-thin, BOTH still presented)
  </read_first>
  <behavior>
    - flagCausalCue("Cars led to gas stations") returns { flag: 'cue-supported', matched: ['led to'], dropped: false }
    - flagCausalCue("Cars and gas stations") returns { flag: 'cue-thin', matched: [], dropped: false }
    - Every return has dropped === false (advisory only; the gate decides, never the flagger)
    - The displayed-confidence adjustment is returned as a delta/multiplier field, never as a mutation of the input
  </behavior>
  <action>
    Create lib/core/futures/causal-cue.cjs. Export CAUSAL_CUE_LEXICON as a frozen list of cue patterns: "leads to", "led to", "because", "enables", "results in", "causes", "drives", "forces", "triggers", "gives rise to". Export flagCausalCue(text) which runs a case-insensitive regex pass over the consequence text, returns { flag: 'cue-supported' | 'cue-thin', matched: string[], confidence_adjust: number, dropped: false }. confidence_adjust is a small advisory multiplier (cue-supported slightly raises displayed confidence, cue-thin slightly lowers it) but NEVER drops or removes the consequence (FW-03: advisory only; dropped is always false). Use ONLY built-in regex; no ML, no new runtime dependency (Part 7 constraint). NO em-dashes.
  </action>
  <acceptance_criteria>
    - BEHAVIOR: flagCausalCue on a cue-phrased string returns flag 'cue-supported' with a non-empty matched array
    - BEHAVIOR: flagCausalCue on a non-cue string returns flag 'cue-thin' with an empty matched array
    - BEHAVIOR: every flagCausalCue return has dropped === false (assert across both cases)
    - SOURCE: `grep -ciE "require\\(.*(transformers|onnx|tensorflow|ml)" lib/core/futures/causal-cue.cjs` is 0 (no ML dependency)
    - TEST-COMMAND: `node tests/test-futures-causal-cue.cjs` passes
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-futures-causal-cue.cjs</automated>
  </verify>
  <done>flagCausalCue flags cue-supported vs cue-thin, never drops, adds no ML dependency; CAUSAL_CUE_LEXICON exported.</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for causal-cue and frontmatter validators (Nyquist)</name>
  <read_first>
    - tests/run-all-150.10.sh (lines 1-30: the node-assert + bash-aggregator repo test convention)
    - lib/core/futures/causal-cue.cjs (the module under test, from Task 2)
    - lib/core/futures/orchestrator.cjs (validateConsequenceFrontmatter, from Task 1)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-VALIDATION.md (the FW-03 / FW-04 test rows + commands)
  </read_first>
  <action>
    Create tests/test-futures-causal-cue.cjs using node built-in assert (no framework): assert flagCausalCue flags a cue-phrased consequence cue-supported, a non-cue one cue-thin, and that dropped is false in both cases (FW-03 never-drops). Create tests/test-futures-frontmatter.cjs: assert validateConsequenceFrontmatter accepts { horizon:'mid', confidence:0.7, domain:'Economic' }; rejects an out-of-enum horizon, a confidence of 1.5 and of -0.1, and an out-of-enum domain (FW-04 enum/range). Each file prints a PASS/FAIL line and exits non-zero on failure (mirror run-all-150.10.sh's per-suite convention). NO em-dashes.
  </action>
  <acceptance_criteria>
    - TEST-COMMAND: `node tests/test-futures-causal-cue.cjs` exits 0
    - TEST-COMMAND: `node tests/test-futures-frontmatter.cjs` exits 0
    - BEHAVIOR: test-futures-frontmatter asserts BOTH the confidence>1 and confidence<0 rejections (range, not just enum)
    - SOURCE: neither test file uses a watch flag or an external test framework (grep for "watch" returns 0; only node:assert required)
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-futures-causal-cue.cjs && node tests/test-futures-frontmatter.cjs</automated>
  </verify>
  <done>Both unit tests green; FW-03 never-drops and FW-04 enum+range are asserted automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator concept -> command | The seed concept string is navigator-supplied input crossing into the orchestrator |
| orchestrator -> filesystem | Consequence frontmatter is written locally; no network surface in this wave |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-02 | Tampering | causal-cue flagger | mitigate | flagCausalCue is ADVISORY: dropped is always false; it never promotes or confirms a consequence. Truth-claim confirmation is deferred to the Wave 3 confirmNode gate (Part 9). Asserted by the never-drops test. |
| T-156-03 | Information Disclosure | this wave's modules | accept | This wave has ZERO network surface (no fetch, no Brain call). Part 8 egress risk enters only at FW-13 (Wave 4). Grep confirms no fetch/brain token in causal-cue.cjs or orchestrator.cjs scaffold. |
| T-156-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this wave (Part 7 constraint: reuse existing helpers, no new dependency). Asserted by the no-ML-dependency grep. |
</threat_model>

<verification>
- `node tests/test-futures-causal-cue.cjs` green (FW-03)
- `node tests/test-futures-frontmatter.cjs` green (FW-04)
- `test -f commands/futures.md && grep -q 'connector:' commands/futures.md` (FW-01)
- No em-dashes anywhere in the wave's files
</verification>

<success_criteria>
- /mos:futures command surface exists, spine-wired (connector frontmatter, reach_id context_block, no new reach), Part 7-justified (FW-01)
- Causal-cue flagger flags cue-supported / cue-thin and never drops (FW-03)
- Consequence frontmatter validators enforce horizon enum + confidence 0-1 + PESTEL domain enum (FW-04)
- The orchestrator shell exports the caps + enums + validators that Wave 2 consumes
</success_criteria>

<output>
Create `.planning/phases/156-futures-wheel-opportunity-location-mvp/156-01-SUMMARY.md` when done.
</output>
