---
phase: 156-futures-wheel-opportunity-location-mvp
plan: 02
type: execute
wave: 2
depends_on: ["156-01"]
files_modified:
  - lib/core/futures/orchestrator.cjs
  - tests/test-futures-generator.cjs
  - tests/test-futures-edges.cjs
  - tests/test-futures-hsi-integration.cjs
  - tests/fixtures/futures-seed-room/README.md
autonomous: true
requirements: [FW-02, FW-05, FW-06]
canon_parts: [Part 4, Part 7, Part 8, Part 9]
must_haves:
  truths:
    - "A seed produces consequence artifacts tagged ring 1|2|3, bounded by depth (3) x fan-out (5); ring-2 artifacts reference a ring-1 parent"
    - "room.db carries ROOT_CAUSES edges from ring N-1 artifacts to their ring N children; zero non-frozen edge types are written by the command"
    - "On a seed with >=4 filed consequences, the run registers them as Artifact nodes, runs compute-hsi.py then hsi-to-graph.cjs, and produces >=1 HSI_CONNECTION edge in one run"
  artifacts:
    - path: "lib/core/futures/orchestrator.cjs"
      provides: "Guided-by-ring generation loop (cap-bounded), the cascade-edge writer, the Artifact-node registrar, and the file-then-scan HSI sequencer"
      exports: ["generateRing", "writeCascadeEdges", "registerConsequenceArtifacts", "runHsiScan", "assertArtifactCountMatchesFiled"]
    - path: "tests/test-futures-hsi-integration.cjs"
      provides: "Integration test asserting Artifact count == filed count BEFORE the scan, and >=1 HSI_CONNECTION edge after"
      contains: "HSI_CONNECTION"
    - path: "tests/fixtures/futures-seed-room"
      provides: "A small seed room with >=4 consequence .md files for the compute-hsi + hsi-to-graph leg (Wave 0 requirement)"
  key_links:
    - from: "lib/core/futures/orchestrator.cjs"
      to: "lib/core/navigation.cjs writeEdge"
      via: "writeCascadeEdges calls navigation.writeEdge with edge_type ROOT_CAUSES / ENABLES"
      pattern: "writeEdge\\("
    - from: "lib/core/futures/orchestrator.cjs"
      to: "lib/core/node-insert.cjs insertNode (via lazygraph-ops _indexArtifactBody)"
      via: "registerConsequenceArtifacts registers each consequence as a type='Artifact' node in room.db"
      pattern: "insertNode|_indexArtifactBody"
    - from: "lib/core/futures/orchestrator.cjs"
      to: "scripts/compute-hsi.py + scripts/hsi-to-graph.cjs"
      via: "runHsiScan invokes python3 compute-hsi.py <room> --tier 1 then node hsi-to-graph.cjs <room>"
      pattern: "compute-hsi.py|hsi-to-graph.cjs"
---

<objective>
Build the bounded guided-by-ring generation loop (D-01), the ROOT_CAUSES / ENABLES cascade-edge writer through the navigation chokepoint, the load-bearing Artifact-node registrar, and the ordered file-then-register-then-scan HSI sequencer. This wave closes the single highest-risk landmine in the phase: if consequences are filed as .md but never registered as Artifact nodes, hsi-to-graph silently writes ZERO edges (a false-success). A task asserts Artifact-node count == filed-consequence count BEFORE compute-hsi runs.

Purpose: FW-02 (bounded multi-ring generation, depth 3 x fan-out 5, ring-N references ring-N-1 parent), FW-05 (ROOT_CAUSES / ENABLES via navigation.writeEdge, zero non-frozen edge types), FW-06 (the explicit file -> register-as-Artifact -> compute-hsi.py -> hsi-to-graph.cjs -> read-back HSI_CONNECTION sequence).
Output: orchestrator.cjs generation + edge + registration + HSI functions, three test files, and the HSI integration fixture room.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-SPEC.md
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-RESEARCH.md
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-VALIDATION.md
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-01-SUMMARY.md
@docs/MINDRIAN-CANON.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bounded guided-by-ring generation loop + cascade-edge writer</name>
  <read_first>
    - lib/core/futures/orchestrator.cjs (the Wave-1 shell: FUTURES_DEPTH_CAP, FUTURES_FANOUT_CAP, validateConsequenceFrontmatter, HORIZON_ENUM, PESTEL_DOMAIN_ENUM)
    - lib/core/futures/causal-cue.cjs (flagCausalCue -- the generator annotates each consequence)
    - lib/core/navigation.cjs (lines 53-103: the closed surface; writeEdge re-export at line 103)
    - lib/core/navigation/edges.cjs (lines 300-372: ALLOWED_EDGE_TYPES frozen set incl. ROOT_CAUSES line 320 + ENABLES; writeEdge(db, {source_id, target_id, edge_type, properties}) signature returning {ok} -- never throws; rejects non-frozen types with {ok:false, reason:'invalid_edge_type'})
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-SPEC.md (FW-02, FW-05 acceptance)
  </read_first>
  <behavior>
    - generateRing(seed, ringNumber, parents, opts) returns at most FUTURES_FANOUT_CAP consequence objects per parent; ringNumber never exceeds FUTURES_DEPTH_CAP (caps clamp, not error)
    - Each ring-2 consequence object carries a parent_id referencing a ring-1 consequence; each consequence carries ring, horizon, confidence, domain, and a causal_cue flag
    - writeCascadeEdges(db, consequences) writes a ROOT_CAUSES edge (source_id=parent cause, target_id=child effect) for each parent->child link via navigation.writeEdge; an enabling relation uses ENABLES
    - writeCascadeEdges NEVER attempts LEADS_TO / CAUSES (not frozen); a writeEdge {ok:false} on a non-frozen type is treated as a programming error the test catches
  </behavior>
  <action>
    Implement generateRing in lib/core/futures/orchestrator.cjs: the literal D-01 "and then what?" recursion -- generate ring 1 from the seed, and (when called for ring N>1) expand ONLY the approved parents into at most FUTURES_FANOUT_CAP children each, stamping ring N. Clamp ringNumber to FUTURES_DEPTH_CAP and per-parent children to FUTURES_FANOUT_CAP so the graph cannot balloon (FW-02 "mathematically unmanageable" guard; this MUST happen before any HSI O(n^2) pairing in Task 3). Each consequence object carries ring, parent_id (null for ring 1), horizon, confidence, domain (validated via validateConsequenceFrontmatter), and the flagCausalCue annotation. The actual prose generation is Larry-driven on Desktop/Cowork; on CLI the function accepts a pre-supplied consequence batch so it is unit-testable headlessly.

    Implement writeCascadeEdges(db, consequences): for each consequence with a non-null parent_id, call navigation.writeEdge(db, { source_id: parent_id, target_id: consequence_id, edge_type: 'ROOT_CAUSES', properties: { ring, confidence } }) -- source is the CAUSE (the parent), target is the EFFECT (the child), per edges.cjs ROOT_CAUSES semantics. Use ENABLES only for an explicit enabling relation. Route EVERY edge write through navigation.writeEdge (Part 9 chokepoint); never raw-SQL a cascade edge. Properties are enum/scalar ONLY (ring number + confidence float) -- never the consequence body (Part 8). NO em-dashes.
  </action>
  <acceptance_criteria>
    - BEHAVIOR: generateRing clamps to FUTURES_DEPTH_CAP rings and FUTURES_FANOUT_CAP children/parent (a request for ring 4 or 7 children is bounded)
    - BEHAVIOR: a ring-2 consequence object has a parent_id pointing at a ring-1 consequence id
    - BEHAVIOR: after writeCascadeEdges, a SELECT on room.db edges WHERE type='ROOT_CAUSES' returns one row per parent->child link, source=parent target=child
    - SOURCE: `grep -ciE "LEADS_TO|'CAUSES'" lib/core/futures/orchestrator.cjs` is 0 (forbidden non-frozen types never referenced)
    - SOURCE: every edge write in orchestrator.cjs goes through navigation.writeEdge (no raw `INSERT INTO edges` for cascade edges); grep finds zero `INSERT INTO edges` in orchestrator.cjs
    - TEST-COMMAND: `node tests/test-futures-generator.cjs` and `node tests/test-futures-edges.cjs` pass
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-futures-generator.cjs && node tests/test-futures-edges.cjs</automated>
  </verify>
  <done>generateRing is cap-bounded with parent links; writeCascadeEdges writes ROOT_CAUSES/ENABLES via the chokepoint only; no non-frozen edge type is referenced.</done>
</task>

<task type="auto">
  <name>Task 2: Artifact-node registrar + the count-match precondition assertion (LANDMINE #1)</name>
  <read_first>
    - scripts/hsi-to-graph.cjs (lines 69-101: findArtifact.get(id) gate -- writes an HSI_CONNECTION edge ONLY when BOTH endpoints exist as type='Artifact' nodes; it `continue`s past a missing endpoint, silently writing zero edges)
    - lib/core/node-insert.cjs (lines 95-122: insertNode(conn, id, type, properties, overrides) -- the NOT-NULL-safe Artifact-node writer; created_by='system' is canon-legal for these system-written nodes per the Part 9 audit-node carve-out)
    - lib/core/lazygraph-ops.cjs (openGraph / closeGraph + _indexArtifactBody -- the Artifact + Section upsert path the header of node-insert.cjs documents)
    - scripts/compute-hsi.py (lines 16, 133-172: CLI contract `python3 scripts/compute-hsi.py <room> [--tier 1]` + discover_artifacts walks room/*.md)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-RESEARCH.md (sections 1+2: the Artifact-node registration gap = the highest landmine)
  </read_first>
  <action>
    Implement registerConsequenceArtifacts(roomDir, consequences) in orchestrator.cjs: file each consequence as a nested .md under opportunity-bank/futures-<seed-slug>/<consequence-slug>/<consequence-slug>.md (Obsidian nested rule, ICM Layer 0 each folder gets a ROOM.md), THEN register each consequence as a type='Artifact' node in room.db keyed by its artifact id -- via the lazygraph-ops _indexArtifactBody indexing path OR a direct node-insert.insertNode(conn, id, 'Artifact', properties) when indexing is unavailable. Both registrations (the .md on disk for compute-hsi discover_artifacts, AND the Artifact node in room.db for hsi-to-graph's findArtifact gate) are required; the function must produce both.

    Implement assertArtifactCountMatchesFiled(db, filedConsequenceIds) returning { ok, artifactCount, filedCount }: it SELECTs COUNT(*) FROM nodes WHERE type='Artifact' AND id IN (filedConsequenceIds) and asserts it equals filedConsequenceIds.length. This is the load-bearing precondition (RESEARCH landmine #1): the HSI sequencer in Task 3 MUST call this and HARD-FAIL (return a clear error, do NOT proceed to compute-hsi) if the counts differ -- preventing the silent zero-edge false-success. NO em-dashes.
  </action>
  <acceptance_criteria>
    - BEHAVIOR: after registerConsequenceArtifacts on N consequences, room.db has N nodes WHERE type='Artifact' matching the consequence ids, AND N consequence .md files exist under opportunity-bank/futures-<seed>/
    - BEHAVIOR: assertArtifactCountMatchesFiled returns ok:true when counts match and ok:false (with artifactCount/filedCount) when an Artifact node is missing
    - SOURCE: each consequence folder has a ROOM.md (ICM Layer 0); grep/test confirms presence
    - SOURCE: the Artifact-node write uses insertNode or _indexArtifactBody (grep finds the require), never a bare 3-column INSERT that breaks on a migrated room.db
    - TEST-COMMAND: covered by `node tests/test-futures-hsi-integration.cjs` (Task 3)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const o=require('./lib/core/futures/orchestrator.cjs'); if(typeof o.registerConsequenceArtifacts!=='function'||typeof o.assertArtifactCountMatchesFiled!=='function') process.exit(1)"</automated>
  </verify>
  <done>Consequences register as both .md (compute-hsi input) and Artifact nodes (hsi-to-graph endpoints); the count-match precondition hard-fails before the scan if any Artifact node is missing.</done>
</task>

<task type="auto">
  <name>Task 3: HSI file-then-scan sequencer + integration fixture (FW-06)</name>
  <read_first>
    - scripts/compute-hsi.py (lines 16-19: CLI `python3 scripts/compute-hsi.py <room> --tier 1` writes .hsi-results.json; Tier 1 LSA+MiniLM is the MVP default; Tier 0 keyword fallback if python3 absent)
    - scripts/hsi-to-graph.cjs (lines 21-101: `node scripts/hsi-to-graph.cjs <room>` reads .hsi-results.json, writes HSI_CONNECTION edges, hsi_score>0.3 threshold)
    - lib/core/futures/orchestrator.cjs (registerConsequenceArtifacts + assertArtifactCountMatchesFiled from Task 2)
    - tests/run-all-150.10.sh (the node-assert + bash-aggregator test convention; the Part-8 grep-sweep pattern)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-VALIDATION.md (FW-06 row: assert >=1 HSI_CONNECTION; Wave 0 fixture requirement)
  </read_first>
  <action>
    Implement runHsiScan(roomDir, filedConsequenceIds) in orchestrator.cjs enforcing the FW-06 deterministic order: (1) assertArtifactCountMatchesFiled -- hard-fail with a clear error if counts differ (landmine guard, Task 2), (2) shell `python3 scripts/compute-hsi.py <roomDir> --tier 1` via child_process to produce .hsi-results.json, (3) shell `node scripts/hsi-to-graph.cjs <roomDir>` to write HSI_CONNECTION edges, (4) read back the HSI_CONNECTION edges from room.db and return the top cross-domain bridges (ranked by hsi_score; cross-domain = the two endpoints carry different PESTEL domain frontmatter). Tri-Polar: detect python3 absence and degrade to the Tier 0 keyword fallback path the existing whitespace/HSI commands use (do not crash on Desktop/Cowork without python3). The command OWNS this sequence; no hidden async. NO em-dashes.

    Create the integration fixture tests/fixtures/futures-seed-room/ -- a small seed room with >=4 consequence .md files spanning >=2 PESTEL domains (so a cross-domain HSI_CONNECTION can form) plus a README.md describing the fixture. Create tests/test-futures-hsi-integration.cjs: register the fixture consequences as Artifact nodes, assert assertArtifactCountMatchesFiled ok:true, run runHsiScan, and assert .hsi-results.json exists AND room.db has >=1 HSI_CONNECTION edge after the run (FW-06 acceptance). If python3 is unavailable in CI, the test asserts the Tier 0 degradation path returns cleanly rather than failing hard.
  </action>
  <acceptance_criteria>
    - BEHAVIOR: runHsiScan refuses to call compute-hsi and returns a clear error when assertArtifactCountMatchesFiled is ok:false (the false-success guard fires)
    - BEHAVIOR: on the fixture (>=4 consequences, >=2 domains, python3 present) the run produces .hsi-results.json and >=1 HSI_CONNECTION edge in room.db in a single call
    - BEHAVIOR: HSI_CONNECTION edges are written by hsi-to-graph.cjs (raw path), NOT routed through navigation.writeEdge (which would reject them as non-frozen) -- the orchestrator never calls writeEdge with edge_type HSI_CONNECTION
    - SOURCE: `grep -ciE "writeEdge.*HSI_CONNECTION|writeEdge.*REVERSE_SALIENT" lib/core/futures/orchestrator.cjs` is 0 (RESEARCH landmine #2)
    - SOURCE: runHsiScan has a python3-absent degradation branch (grep finds the Tier 0 / fallback handling)
    - TEST-COMMAND: `node tests/test-futures-hsi-integration.cjs` passes (green with python3; clean degradation without)
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-futures-hsi-integration.cjs</automated>
  </verify>
  <done>runHsiScan enforces register->assert->compute-hsi->hsi-to-graph->read-back in order, hard-fails on count mismatch, never routes HSI edges through writeEdge, degrades without python3; the fixture proves >=1 HSI_CONNECTION edge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consequence .md -> room.db Artifact node | The registration step; a gap here causes the HSI false-success |
| orchestrator -> compute-hsi.py (child process) | Local subprocess; reads only the local room dir |
| orchestrator -> navigation.writeEdge | The only legal cascade-edge write door (Part 9 chokepoint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-01 | Spoofing (false-success) | runHsiScan / registerConsequenceArtifacts | mitigate | assertArtifactCountMatchesFiled HARD-FAILS before compute-hsi when Artifact count != filed count; runHsiScan refuses to proceed. Asserted by test-futures-hsi-integration. This is the phase's #1 landmine (RESEARCH risk 1). |
| T-156-02 | Tampering | cascade-edge writer | mitigate | All cascade edges route through navigation.writeEdge (Part 9 chokepoint), which rejects non-frozen types; HSI edges use the hsi-to-graph raw path by design and are NEVER routed through writeEdge. Consequences land review_status='proposed' (system-written Artifacts per the Part 9 audit-node carve-out); truth-claim confirmation is the Wave 3 gate. |
| T-156-03 | Information Disclosure | edge properties + HSI scan | mitigate | Edge properties are enum/scalar ONLY (ring, confidence) -- the consequence body NEVER lands on an edge (Part 8). compute-hsi.py + hsi-to-graph.cjs operate purely on the LOCAL room dir + room.db; zero Brain egress. Grep confirms no fetch/Brain token in orchestrator.cjs. |
| T-156-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependency (Part 7); compute-hsi.py uses the shipped Python (sentence-transformers + LSA already in the stack). No install task in this wave. |
</threat_model>

<verification>
- `node tests/test-futures-generator.cjs` (FW-02 caps + parent links)
- `node tests/test-futures-edges.cjs` (FW-05 ROOT_CAUSES via chokepoint, zero non-frozen)
- `node tests/test-futures-hsi-integration.cjs` (FW-06 file->register->scan->>=1 HSI_CONNECTION + count-match guard)
- No em-dashes anywhere in the wave's files
</verification>

<success_criteria>
- Bounded multi-ring generation: ring 1|2|3 tagged, depth 3 x fan-out 5 clamped, ring-N references ring-N-1 parent (FW-02)
- ROOT_CAUSES / ENABLES cascade edges via navigation.writeEdge only; zero non-frozen edge types written (FW-05)
- The file -> register-as-Artifact -> compute-hsi.py -> hsi-to-graph.cjs -> read-back sequence runs in one command; >=1 HSI_CONNECTION edge; count-match precondition hard-fails before the scan (FW-06)
</success_criteria>

<output>
Create `.planning/phases/156-futures-wheel-opportunity-location-mvp/156-02-SUMMARY.md` when done.
</output>
