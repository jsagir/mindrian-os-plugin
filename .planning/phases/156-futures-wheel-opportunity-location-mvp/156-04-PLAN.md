---
phase: 156-futures-wheel-opportunity-location-mvp
plan: 04
type: execute
wave: 4
depends_on: ["156-03"]
files_modified:
  - lib/core/futures/orchestrator.cjs
  - commands/futures.md
  - tests/test-futures-chaining.cjs
  - tests/test-futures-signal.cjs
  - tests/test-futures-part8-leak.cjs
  - tests/run-all-156.sh
autonomous: true
requirements: [FW-11, FW-12, FW-13]
canon_parts: [Part 3, Part 4, Part 5, Part 7, Part 8]
must_haves:
  truths:
    - "On a seed whose consequences span >=2 domains, the command footer offers top-N ranked handoffs to the foresight web partners, each resolved through the Phase 122 command-resolver (never a hardcoded command string)"
    - "Accepting the RS pass writes >=1 REVERSE_SALIENT edge / bridge artifact; the RS-finding -> open-as-futures-wheel reverse handoff exists from the RS and systems-thinking surfaces"
    - "Seed grounding fetches >=1 public source (cache hit or live); a per-ring research pass corroborates a confidence OR proposes a signal-derived consequence; queries carry only generic domain handles"
    - "A Part 8 boundary scan over the new command + lib code returns zero user-content-to-Brain paths"
  artifacts:
    - path: "lib/core/futures/orchestrator.cjs"
      provides: "The top-N ranked chaining-handoff surfacer (via command-resolver) + the two-fire-point SIGNAL research integration"
      exports: ["surfaceChainingHandoffs", "runSignalResearch"]
    - path: "tests/test-futures-part8-leak.cjs"
      provides: "Adversarial egress tripwire (mirrors test-navigation-packet-part8-leak.cjs); zero room-content-to-Brain"
      contains: "Part 8"
    - path: "tests/run-all-156.sh"
      provides: "The single phase-gate aggregator running all test-futures-*.cjs + the Part 8 scan"
      contains: "run-all"
  key_links:
    - from: "lib/core/futures/orchestrator.cjs"
      to: "lib/workflow/command-resolver.cjs"
      via: "surfaceChainingHandoffs resolves each handoff via commandsForFramework / composeWorkflow (no hardcoded command string)"
      pattern: "commandsForFramework|composeWorkflow"
    - from: "lib/core/futures/orchestrator.cjs"
      to: "lib/core/research-corpus.cjs + lib/core/research-cache.cjs"
      via: "runSignalResearch uses getCached/isFresh then fetchCorpus with a generic domain handle, putCached"
      pattern: "fetchCorpus|getCached"
    - from: "lib/core/futures/orchestrator.cjs"
      to: "scripts/rs-engine.py + lib/core/bridge-writer.cjs"
      via: "the RS handoff invokes the shipped reverse-salient engine; REVERSE_SALIENT edges written by hsi-to-graph (raw path)"
      pattern: "rs-engine|bridge-writer|REVERSE_SALIENT"
---

<objective>
Wire the foresight meta-lens chaining web (top-N ranked handoffs resolved through the Phase 122 command-resolver, mutual RS invocation), the bounded two-fire-point SIGNAL research step (seed grounding + per-ring on-demand, 30-day cached, generic handles only), the adversarial Part 8 boundary tripwire, and the run-all-156.sh phase-gate aggregator. This wave proves the "watch the water" thesis end-to-end and locks the locality constitution.

Purpose: FW-12 (top-N ranked foresight-web handoffs via command-resolver, no hardcoded strings; mutual RS + open-as-futures-wheel reverse handoff), FW-13 (bounded SIGNAL research at two fire points, reusing research-corpus + cache + Phase 131 workflow, generic handles only), FW-11 (Part 8 zero room-content-to-Brain over all new code).
Output: orchestrator chaining + SIGNAL functions, footer handoffs in commands/futures.md, three test files, the aggregator.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-SPEC.md
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-RESEARCH.md
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-VALIDATION.md
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-03-SUMMARY.md
@docs/MINDRIAN-CANON.md
@docs/RESEARCH-AS-WORKFLOW-STEP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Top-N ranked foresight-web chaining handoffs via command-resolver + mutual RS</name>
  <read_first>
    - lib/workflow/command-resolver.cjs (lines 80-156: commandsForFramework(name), frameworksForCommand(cmd), composeWorkflow(frameworkChain), validateChainAutonomy -- the ONLY door; never a hardcoded command string; degrades to [] on missing registry)
    - commands/systems-thinking.md (lines 19-39 + 67-75: the connector frontmatter + the top-1-2 capped cross-framework chain surfacing pattern -- mirror the dial top-N)
    - lib/hmi/reach-component-map.json (the established top-N-of-K ranked surfacing the dial uses; D-04 top-3-of-N)
    - scripts/rs-engine.py + lib/core/bridge-writer.cjs (the Phase 89 reverse-salient engine; RS writes REVERSE_SALIENT edges via the hsi-to-graph raw path)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-SPEC.md (FW-12: 8 partners; RS mutual + reverse open-as-futures-wheel handoff; resolve via Phase 122 resolver)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-CONTEXT.md (D-04: top-N ranked, mirror the 150.x dial top-3-of-N)
  </read_first>
  <action>
    Implement surfaceChainingHandoffs(roomDir, consequences) in orchestrator.cjs: rank the 8 foresight-web partners (RS, /mos:systems-thinking, /mos:scenario-plan, /mos:explore-trends, /mos:analyze-timing + /mos:dominant-designs, /mos:diagnose, /mos:mullins, /mos:explore-futures) by a detected trigger (cross-domain bridge -> RS; causal loop -> systems-thinking; branch co-occurrence -> scenario-plan; far-horizon consequence -> explore-trends/analyze-timing; banked candidate -> mullins) and surface the TOP-3-of-N at the F.1 gate (D-04, mirror the dial). Resolve EACH handoff target through command-resolver.commandsForFramework / composeWorkflow against data/command-registry.json -- NEVER a hardcoded command string (FW-12 acceptance). For the RS handoff: when accepted, invoke the shipped scripts/rs-engine.py + bridge-writer.cjs over the consequence set so >=1 REVERSE_SALIENT edge / bridge artifact is written (via the hsi-to-graph raw path, NOT writeEdge). Add the mutual reverse handoff -- the "open as a futures wheel?" hook -- declared in the connector surface so it is reachable from at least the RS and systems-thinking surfaces. Update commands/futures.md footer to offer these handoffs. NO em-dashes.

    Reuse-before-build (Part 7): all 8 partners are INVOKED via the resolver, never duplicated; the only net-new is the ranking + surfacing logic. The RS engine, command-resolver, and the dial top-N pattern are all repointed.
  </action>
  <acceptance_criteria>
    - BEHAVIOR: surfaceChainingHandoffs returns at most 3 ranked handoffs (top-3-of-N, D-04); on a >=2-domain seed the set includes the foresight-web partners
    - BEHAVIOR: every handoff target resolves through command-resolver (commandsForFramework / composeWorkflow); a registry miss degrades to a manual "run X" line, not a crash
    - SOURCE: `grep -ciE "['\"]/mos:(systems-thinking|scenario-plan|explore-trends|mullins|diagnose|analyze-timing|dominant-designs|explore-futures)['\"]" lib/core/futures/orchestrator.cjs` is 0 (no hardcoded command strings; all via resolver)
    - BEHAVIOR: accepting the RS handoff writes >=1 REVERSE_SALIENT edge / bridge artifact; the open-as-futures-wheel reverse hook is declared for the RS + systems-thinking surfaces
    - TEST-COMMAND: `node tests/test-futures-chaining.cjs` passes
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-futures-chaining.cjs</automated>
  </verify>
  <done>Top-3-of-N handoffs resolve via command-resolver (zero hardcoded strings); RS is mutually invocable and writes a REVERSE_SALIENT edge; the reverse open-as-futures-wheel hook exists.</done>
</task>

<task type="auto">
  <name>Task 2: Bounded two-fire-point SIGNAL research (seed grounding + per-ring), generic handles only</name>
  <read_first>
    - lib/core/research-corpus.cjs (lines 294-353: fetchCorpus(args) + SOURCES registry + the _adapters; academic/Tavily/Brain-cypher/sciBot adapters; Brain-cypher sanitizes to generic handles)
    - lib/core/research-cache.cjs (line 165 exports: cacheKey, cachePath, getCached(roomDir,source,query,opts), putCached, isFresh(fetchedAtIso,opts) -- the 30-day TTL cache)
    - docs/RESEARCH-AS-WORKFLOW-STEP.md (the Phase 131 research-as-graph-aware-workflow; findings wire as typed evidence)
    - lib/core/navigation/evidence-claim.cjs (the evidence-claim path that wires a finding as typed graph evidence + adjusts confidence + evidence tier per Part 5)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-CONTEXT.md (D-05: two fire points -- seed grounding up front + per-ring on-demand; 30-day cached; generic handles; NOT always-on)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-SPEC.md (FW-13 acceptance: >=1 public source up front; per-ring raises/lowers a confidence with a cited source OR proposes >=1 signal-derived consequence; generic handles only)
  </read_first>
  <action>
    Implement runSignalResearch(roomDir, query, opts) in orchestrator.cjs: the SIGNAL leg with the cache-first pattern -- getCached(roomDir, source, query) -> if not isFresh -> fetchCorpus({ source, query, limit }) -> putCached. The query MUST be a generic domain/concept handle (the seed concept or a ring's domain keyword), NEVER a consequence body or any room artifact text (Part 8). Two fire points (D-05): (a) seedGrounding -- called once up front to ground ring-1 generation; (b) perRingResearch -- the navigator fires it over a ring's consequences on demand. Each pass either corroborates a consequence's confidence + evidence tier (wire the finding via the evidence-claim path, Part 5) OR surfaces a weak signal that proposes >=1 additional signal-derived consequence (tag it signal-derived). The 30-day cache bounds external calls; this is NOT always-on. NO em-dashes.
  </action>
  <acceptance_criteria>
    - BEHAVIOR: seedGrounding fetches >=1 public source (cache hit or live) for the seed concept before ring-1 generation
    - BEHAVIOR: a per-ring pass either adjusts >=1 consequence confidence with a cited public source OR proposes >=1 consequence tagged signal-derived
    - SOURCE: the fetchCorpus query argument is a generic handle; `grep` confirms runSignalResearch never passes a consequence .md body / room artifact text to fetchCorpus (the query is the seed/domain handle)
    - BEHAVIOR: a second call within the TTL window is a cache hit (isFresh true), bounding external calls
    - TEST-COMMAND: `node tests/test-futures-signal.cjs` passes (asserts generic-handle-only + two fire points)
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-futures-signal.cjs</automated>
  </verify>
  <done>SIGNAL research runs at two fire points, cache-first (30-day TTL), with generic domain handles only; findings adjust confidence or propose signal-derived consequences.</done>
</task>

<task type="auto">
  <name>Task 3: Part 8 adversarial egress tripwire + run-all-156.sh phase-gate aggregator</name>
  <read_first>
    - tests/test-navigation-packet-part8-leak.cjs (the adversarial egress tripwire pattern to mirror: forbidden-substring sweep, 9 tripwires registered in run-all-110.sh)
    - tests/run-all-150.10.sh (the bash PASS/FAIL aggregator convention: per-suite line, final tally, exit 1 on any failure, Part-8 grep sweep over local artifacts)
    - lib/core/futures/orchestrator.cjs + lib/core/futures/causal-cue.cjs + lib/core/futures/subsystem-render.cjs + commands/futures.md (all new code -- the surface the Part 8 scan covers)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-VALIDATION.md (FW-11 row + Wave 0 requirements: run-all-156.sh aggregator, test-futures-part8-leak adversarial stub)
    - docs/MINDRIAN-CANON.md (Part 8: LOCAL data -> BRAIN: NO; a breach is any code path writing user bytes to Brain or querying Brain with user-specific strings)
  </read_first>
  <action>
    Create tests/test-futures-part8-leak.cjs mirroring test-navigation-packet-part8-leak.cjs: an adversarial boundary scan over all new code (lib/core/futures/*.cjs + commands/futures.md) asserting ZERO user-content-to-Brain paths -- no fetch/Brain-write carrying a consequence body, room artifact text, or personal identifier; the only external call (the FW-13 SIGNAL fetch) carries a generic domain handle. Assert the SIGNAL query path cannot pass a room body. This is the FW-11 floor.

    Create tests/run-all-156.sh as the single phase-gate aggregator (mirror run-all-150.10.sh): run every tests/test-futures-*.cjs (generator, causal-cue, frontmatter, edges, hsi-integration, render, confirm, bank, chaining, signal, part8-leak) to completion, print a per-suite PASS/FAIL line + a final tally, exit 1 if any failed, and append a Part-8 grep sweep over lib/core/futures/ + commands/futures.md for forbidden user-content-to-Brain tokens (returns 0). bash only, no emoji, no em-dashes.
  </action>
  <acceptance_criteria>
    - TEST-COMMAND: `node tests/test-futures-part8-leak.cjs` passes (zero room-content-to-Brain; SIGNAL query is generic-handle-only)
    - SOURCE: a boundary grep over lib/core/futures/ + commands/futures.md for Brain-write / Brain-query-with-user-content paths returns 0 (FW-11)
    - TEST-COMMAND: `bash tests/run-all-156.sh` runs every test-futures-*.cjs, prints per-suite PASS/FAIL + tally, and exits 0 with all green
    - SOURCE: run-all-156.sh references all 11 test-futures-*.cjs files and the Part-8 grep sweep (grep confirms)
    - NO em-dashes: `grep -c $'\u2014' tests/run-all-156.sh tests/test-futures-part8-leak.cjs` is 0
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-futures-part8-leak.cjs && bash tests/run-all-156.sh</automated>
  </verify>
  <done>The adversarial Part 8 tripwire confirms zero room-content egress; run-all-156.sh is the one-command phase gate aggregating all FW tests + the boundary scan, green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator -> fetchCorpus (external web) | The ONLY external call in the phase; must carry generic handles only |
| orchestrator -> command-resolver | Handoff resolution; the resolver reads a plugin-local registry, makes zero Brain calls |
| SIGNAL finding -> evidence-claim | A public finding wires as typed LOCAL evidence; no user content leaves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-03 | Information Disclosure (Part 8 egress) | runSignalResearch / fetchCorpus | mitigate | The fetchCorpus query is a generic domain/concept handle ONLY; never a consequence body or room artifact text. The adversarial test-futures-part8-leak tripwire + the run-all-156 Part-8 grep sweep assert zero user-content-to-Brain/external paths. This is the phase's locality floor (FW-11). |
| T-156-02 | Tampering | chaining handoffs | mitigate | Handoffs resolve through command-resolver (no hardcoded strings); the resolver makes zero Brain calls and reads a plugin-local registry. RS REVERSE_SALIENT edges use the hsi-to-graph raw path, never writeEdge. |
| T-156-01 | Spoofing (false-success) | run-all-156.sh | mitigate | The aggregator runs the Wave-2 HSI integration test (the count-match landmine guard) as part of the phase gate, so a regression that reintroduces the silent zero-edge path turns the gate RED. |
| T-156-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependency (Part 7); reuses research-corpus + cache + command-resolver + rs-engine, all shipped. No install task. |
</threat_model>

<verification>
- `node tests/test-futures-chaining.cjs` (FW-12 top-N via resolver, RS mutual)
- `node tests/test-futures-signal.cjs` (FW-13 two fire points, generic handles)
- `node tests/test-futures-part8-leak.cjs` (FW-11 zero egress)
- `bash tests/run-all-156.sh` green (the full phase gate)
- No em-dashes anywhere in the wave's files
</verification>

<success_criteria>
- Top-3-of-N ranked foresight-web handoffs resolve through the Phase 122 command-resolver (zero hardcoded strings); RS mutually invocable, writes >=1 REVERSE_SALIENT edge; reverse open-as-futures-wheel hook exists (FW-12)
- Bounded SIGNAL research at two fire points (seed grounding + per-ring), 30-day cached, generic handles only; corroborates a confidence or proposes a signal-derived consequence (FW-13)
- Part 8 boundary scan over all new code returns zero user-content-to-Brain paths (FW-11)
- run-all-156.sh is the single green phase gate
</success_criteria>

<output>
Create `.planning/phases/156-futures-wheel-opportunity-location-mvp/156-04-SUMMARY.md` when done.
</output>
