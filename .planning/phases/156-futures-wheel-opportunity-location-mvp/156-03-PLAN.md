---
phase: 156-futures-wheel-opportunity-location-mvp
plan: 03
type: execute
wave: 3
depends_on: ["156-02"]
files_modified:
  - lib/core/futures/subsystem-render.cjs
  - lib/core/futures/orchestrator.cjs
  - tests/test-futures-render.cjs
  - tests/test-futures-confirm.cjs
  - tests/test-futures-bank.cjs
autonomous: false
requirements: [FW-07, FW-08, FW-09, FW-10]
canon_parts: [Part 3, Part 4, Part 5, Part 9]
user_setup: []
must_haves:
  truths:
    - "For a seed with consequences across >=2 PESTEL domains, the subsystem impact map groups them by domain and is invocable from the command footer (default view per D-03)"
    - "The top cross-domain HSI_CONNECTION bridges the navigator did NOT explicitly draw are surfaced at a tri-context Decision Gate with APPROVE / REJECT(reason) / DEFER (per-ring batch, D-02)"
    - "A freshly generated consequence node is review_status proposed; after navigator APPROVE it is confirmed with a byUser attribution; a REJECT writes a reason edge"
    - "An approved candidate banks via bankOpportunity with provenance tracing to an HSI_CONNECTION, REVERSE_SALIENT, or ROOT_CAUSES edge; dedup still functions"
  artifacts:
    - path: "lib/core/futures/subsystem-render.cjs"
      provides: "The PESTEL-domain subsystem impact map render (default) + the concentric ring view (on demand), via the ui-system De Stijl 4-zone render -- NOT hand-rolled HTML"
      exports: ["renderSubsystemMap", "renderRingView"]
    - path: "lib/core/futures/orchestrator.cjs"
      provides: "The per-ring batch Decision Gate (surface bridges + APPROVE/REJECT/DEFER), the confirmNode promotion, and bankOpportunity-with-provenance wiring"
      exports: ["surfaceBridgesAtGate", "confirmRingDecisions", "bankCandidateWithProvenance"]
  key_links:
    - from: "lib/core/futures/orchestrator.cjs"
      to: "lib/core/navigation.cjs confirmNode"
      via: "confirmRingDecisions calls navigation.confirmNode(db, id, byUser) on approved nodes; REJECT writes a reason edge via writeEdge"
      pattern: "confirmNode\\("
    - from: "lib/core/futures/orchestrator.cjs"
      to: "lib/core/opportunity-ops.cjs bankOpportunity"
      via: "bankCandidateWithProvenance calls bankOpportunity(roomDir, opportunity) with a provenance field naming the source edge"
      pattern: "bankOpportunity\\("
    - from: "lib/core/futures/subsystem-render.cjs"
      to: "skills/ui-system/SKILL.md"
      via: "render reuses the De Stijl 4-zone + Shape F.1 selector, not hand-rolled HTML"
      pattern: "ui-system"
---

<objective>
Render the consequence graph as the PESTEL subsystem impact map (the D-03 default view) plus the on-demand ring view, surface the hidden cross-domain HSI bridges at a per-ring batch tri-context Decision Gate (D-02), promote approved consequences proposed->confirmed via the confirmNode chokepoint (Part 9), and bank approved opportunity candidates via the shipped bankOpportunity engine with provenance tracing to a source edge.

Purpose: FW-07 (subsystem PESTEL render, reuse ui-system), FW-08/FW-09 (banking with edge provenance via opportunity-ops), FW-10 (HITL proposed->confirmed via confirmNode with byUser; REJECT writes a reason edge). This wave has HITL Decision Gates, so it is NOT autonomous.
Output: subsystem-render.cjs, orchestrator gate + confirm + bank functions, three test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-SPEC.md
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-RESEARCH.md
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-VALIDATION.md
@.planning/phases/156-futures-wheel-opportunity-location-mvp/156-02-SUMMARY.md
@docs/MINDRIAN-CANON.md
@skills/ui-system/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PESTEL subsystem impact map render (default) + ring view (on demand)</name>
  <read_first>
    - skills/ui-system/SKILL.md (the De Stijl 4-zone render + Shape F.1 selector; Dashboard Export Integrity rule -- do NOT hand-roll HTML)
    - lib/core/futures/orchestrator.cjs (the consequence objects carry ring + domain frontmatter from Wave 2)
    - lib/core/opportunity-ops.cjs (parseFrontmatter -- reuse to read each consequence's domain: tag)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-CONTEXT.md (D-03: subsystem PESTEL map is the DEFAULT; ring view on demand)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-SPEC.md (FW-07 acceptance: groups by domain for >=2 domains, invocable from footer)
  </read_first>
  <action>
    Create lib/core/futures/subsystem-render.cjs. Implement renderSubsystemMap(consequences) -- the D-03 DEFAULT view -- grouping consequences by their PESTEL domain: frontmatter (Political/Economic/Social/Technological/Environmental/Legal) into the ui-system De Stijl 4-zone layout. Reuse the skills/ui-system render primitives; do NOT hand-roll HTML (CLAUDE.md Dashboard Export Integrity). Implement renderRingView(consequences) -- the on-demand concentric ring view grouping by ring 1|2|3. The command output footer offers both views (subsystem map default, ring view on demand). Text/De Stijl render so it works across CLI/Desktop/Cowork (Tri-Polar; not web-only). NO em-dashes.
  </action>
  <acceptance_criteria>
    - BEHAVIOR: renderSubsystemMap on consequences spanning >=2 domains produces output with a distinct group per present PESTEL domain (grep the output for the domain headers)
    - BEHAVIOR: renderRingView groups by ring 1|2|3
    - SOURCE: subsystem-render.cjs reuses ui-system render primitives (grep finds the ui-system reference/require), no hand-rolled `<html>`/`<div>` string literals
    - TEST-COMMAND: `node tests/test-futures-render.cjs` passes (asserts PESTEL grouping)
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-futures-render.cjs</automated>
  </verify>
  <done>renderSubsystemMap groups by PESTEL domain (default), renderRingView by ring (on demand), both via ui-system render, no hand-rolled HTML.</done>
</task>

<task type="auto">
  <name>Task 2: Per-ring batch Decision Gate -- bridge surfacing + proposed->confirmed + REJECT reason edge</name>
  <read_first>
    - lib/core/navigation/confirm-node.cjs (lines 32-70: confirmNode(db, id, byUser, reason) -- the single APPROVE-to-promote door; rejects agent attribution; resolveByUser reads USER.md so byUser is a human navigator, never larry/brain/system)
    - lib/core/navigation.cjs (confirmNode + writeEdge re-exports on the closed surface)
    - lib/core/navigation/edges.cjs (REJECTED / REJECTED_BECAUSE / DEFERRED are frozen-legal cascade edges for REJECT reason + DEFER)
    - skills/ui-system/SKILL.md (Shape F.1 selector / tri-context Decision Gate -- the approval surface; AskUserQuestion primitive)
    - lib/core/futures/orchestrator.cjs (runHsiScan returns the top cross-domain bridges from Wave 2)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-CONTEXT.md (D-02: per-ring batch APPROVE/REJECT-reason/DEFER; pruned nodes do not expand)
    - docs/MINDRIAN-CANON.md (Part 3 tri-context gate; Part 9 truth-claim confirmation requires human byUser)
  </read_first>
  <action>
    Implement surfaceBridgesAtGate(roomDir, bridges, ringConsequences) in orchestrator.cjs: present (a) the per-ring batch of proposed consequences and (b) the top-N cross-domain HSI_CONNECTION bridges from runHsiScan -- specifically the bridges whose endpoints span different PESTEL domains and that are NOT an explicit ring parent->child link (the "do what a human can't" surfaced ripple) -- at a tri-context Decision Gate via the Shape F.1 AskUserQuestion selector (LOCAL room state + BRAIN methodology handles + SIGNAL). Each option is APPROVE / REJECT-with-reason / DEFER (D-02 per-ring batch; pruned nodes do not expand to the next ring).

    Implement confirmRingDecisions(db, roomDir, decisions): for each APPROVE, resolve the navigator identity via resolveByUser(roomDir) and call navigation.confirmNode(db, id, byUser) to promote the consequence from proposed to confirmed (Part 9 -- never agent-attributed). For each REJECT, write a REJECTED_BECAUSE edge via navigation.writeEdge carrying the reason as an enum/scalar reason-code property (Part 4 reason becomes graph data; Part 8 no body text on the edge). For each DEFER, write a DEFERRED edge. NO em-dashes.
  </action>
  <acceptance_criteria>
    - BEHAVIOR: a freshly generated consequence node has review_status='proposed'; after confirmRingDecisions APPROVE it is 'confirmed' with a byUser attribution that is NOT an agent identity (not larry/brain/system)
    - BEHAVIOR: a REJECT writes a REJECTED_BECAUSE edge (frozen-legal) with a reason property; a DEFER writes a DEFERRED edge
    - BEHAVIOR: surfaceBridgesAtGate includes at least one bridge whose endpoints span two different PESTEL domains and is not a direct ROOT_CAUSES ring link (the do-what-a-human-cant surfaced ripple)
    - SOURCE: promotion routes through navigation.confirmNode (grep finds confirmNode, NOT a direct promoteNodeStatus or raw UPDATE on review_status)
    - TEST-COMMAND: `node tests/test-futures-confirm.cjs` passes
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-futures-confirm.cjs</automated>
  </verify>
  <done>The per-ring batch gate surfaces cross-domain bridges; APPROVE promotes proposed->confirmed via confirmNode with a human byUser; REJECT writes a REJECTED_BECAUSE reason edge; DEFER writes DEFERRED.</done>
</task>

<task type="auto">
  <name>Task 3: Opportunity banking with edge provenance</name>
  <read_first>
    - lib/core/opportunity-ops.cjs (lines 1115-1189: bankOpportunity(roomDir, opportunity) -- requires opportunity.problem; supports confidence + evidence; dedups by problem_hash 8-char prefix; updates confidence if higher; appends evidence; passes frontmatter through)
    - lib/core/futures/orchestrator.cjs (the confirmed bridges/consequences from Task 2 are the bank candidates)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-SPEC.md (FW-08/FW-09: provenance tracing to an HSI_CONNECTION / REVERSE_SALIENT / ROOT_CAUSES edge id/pair; dedup still functions)
    - .planning/phases/156-futures-wheel-opportunity-location-mvp/156-RESEARCH.md (section 5: extend the opportunity object with an additive provenance field; bankOpportunity passes frontmatter through)
  </read_first>
  <action>
    Implement bankCandidateWithProvenance(roomDir, candidate) in orchestrator.cjs: build the opportunity object (problem required, confidence, evidence) and add an ADDITIVE provenance field naming the source edge -- the HSI_CONNECTION / REVERSE_SALIENT / ROOT_CAUSES edge id or its source-target pair that the candidate traces to (FW-09). Call opportunity-ops.bankOpportunity(roomDir, opportunity); the shipped engine passes the provenance frontmatter through and preserves problem_hash dedup. A candidate without a traceable source edge is NOT bankable (refuse with a clear error). NO em-dashes.
  </action>
  <acceptance_criteria>
    - BEHAVIOR: bankCandidateWithProvenance writes a banked .md under opportunity-bank/ whose frontmatter has a provenance field naming the source edge (HSI_CONNECTION / REVERSE_SALIENT / ROOT_CAUSES)
    - BEHAVIOR: banking the same problem twice dedups (problem_hash) and updates confidence if higher, rather than creating a duplicate
    - BEHAVIOR: a candidate with no source-edge provenance is refused (bankCandidateWithProvenance returns an error; nothing is banked)
    - SOURCE: banking goes through opportunity-ops.bankOpportunity (grep finds the require + call), not a hand-rolled file writer
    - TEST-COMMAND: `node tests/test-futures-bank.cjs` passes
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-futures-bank.cjs</automated>
  </verify>
  <done>Approved candidates bank via bankOpportunity with an additive provenance field tracing to a source edge; dedup preserved; un-traceable candidates refused.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator decision -> confirmNode | The human-confirms-truth boundary (Part 9); only a human byUser may promote a truth-claim |
| gate render -> AskUserQuestion | The tri-context Decision Gate surface; the only choice primitive |
| candidate -> bankOpportunity | Local bank write; provenance must trace to a real edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-02 | Elevation of Privilege (agent-confirmed truth) | confirmRingDecisions | mitigate | Promotion routes ONLY through navigation.confirmNode; resolveByUser coerces any agent identity (larry/brain/system) to the navigator default, so no agent can smuggle a confirmed truth-claim (Part 9 role 5). Asserted by test-futures-confirm (byUser is not an agent). |
| T-156-03 | Information Disclosure | gate render + reason edges + banking | mitigate | The tri-context BRAIN panel carries only generic methodology handles (framework names), never room content (Part 8). REJECT reason rides the edge as an enum/scalar reason-code, never the consequence body. Banking is a LOCAL filesystem write. No Brain egress in this wave. |
| T-156-01 | Spoofing (false provenance) | bankCandidateWithProvenance | mitigate | A candidate with no traceable source edge is refused; provenance MUST name a real HSI_CONNECTION / REVERSE_SALIENT / ROOT_CAUSES edge (FW-09). Prevents banking an opportunity with fabricated lineage. |
| T-156-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependency (Part 7); reuses shipped confirmNode + bankOpportunity + ui-system. No install task. |
</threat_model>

<verification>
- `node tests/test-futures-render.cjs` (FW-07 PESTEL grouping)
- `node tests/test-futures-confirm.cjs` (FW-10 proposed->confirmed byUser + REJECT reason edge)
- `node tests/test-futures-bank.cjs` (FW-08/FW-09 provenance + dedup)
- No em-dashes anywhere in the wave's files

## Human Verification (this wave is NOT autonomous)

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The per-ring batch Decision Gate surfaces the top cross-domain HSI bridges; the subsystem PESTEL map is the default render.</what-built>
  <how-to-verify>
    1. Run `/mos:futures "automobile adoption"` and walk the guided-by-ring loop to ring 2-3.
    2. Confirm the gate surfaces at least one cross-domain bridge the navigator did NOT explicitly draw (e.g. automobile -> middle-manager) -- the do-what-a-human-cant test.
    3. APPROVE a consequence; confirm it reads confirmed with your navigator identity. REJECT one with a reason; confirm the reason is captured.
    4. Confirm the subsystem PESTEL map renders as the default view and the ring view is offered on demand.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</verification>

<success_criteria>
- Subsystem PESTEL impact map is the default render; ring view on demand; groups by domain for >=2 domains (FW-07)
- Cross-domain HSI bridges surfaced at a per-ring batch tri-context gate; >=1 is a bridge the navigator did not draw (FW-07 + Goal test)
- Consequences proposed->confirmed via confirmNode with a human byUser; REJECT writes a reason edge (FW-10)
- Approved candidates bank via bankOpportunity with edge provenance; dedup preserved (FW-08/FW-09)
</success_criteria>

<output>
Create `.planning/phases/156-futures-wheel-opportunity-location-mvp/156-03-SUMMARY.md` when done.
</output>
