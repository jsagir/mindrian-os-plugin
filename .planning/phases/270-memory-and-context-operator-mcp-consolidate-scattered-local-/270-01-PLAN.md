---
phase: 270-memory-and-context-operator-mcp-consolidate-scattered-local-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/270-memory-and-context-operator-mcp-consolidate-scattered-local-/270-DECISIONS.md
autonomous: false
requirements: [MEMOP-15]
canon_parts: [3, 7, 9, 11]

open_questions:
  OQ-1: "DEPTH_CAP reconciliation. BLOCKING navigator decision, asked in THIS plan, Task 1. Conservative default if deferred: keep DEPTH_CAP=3 imported from lib/core/coverage-rollup.cjs and expose structure beyond it (RESEARCH.md option a). ZOOM re-rooting is follow-up, not built here."
  OQ-2: "Identity-write trigger. BLOCKING navigator decision, asked in THIS plan, Task 2. This phase ships only the mechanism-reuse half RESEARCH.md 4.3 already settled (a caller plus a hitl_shape declaration); the trigger itself is jointly owned with Phase 267.2 W2 and is NOT re-decided here."
  OQ-3: "Whether MCP-tool hitl_shape is actually R16-mandated is UNRESOLVED. Every new tool this phase adds declares connector plus hitl_shape anyway, which is correct under either answer. Flagged to the navigator in Task 3, no code task."
  OQ-4: "Cross-surface memory consistency has no external grounding (langtalks returned nothing). Treated as a DESIGNED INVARIANT with a test (the parity leg of tests/test-270-resource-session-room.cjs), never cited as a researched pattern."
  OQ-5: "The two undeclared MCP tools (detect_dual_path, extract_shallow). CLOSED IN THIS PHASE at plan 270-06, RED-pinned at 270-02 Task 3 in a NEW test file so the Wave-0 RED state cannot break tests/run-all-266.sh."
  OQ-6: "Retiring room_state_bound. BLOCKING navigator gate at 270-12 Task 1, a manual foreign-host Resource parity check per Assumption A2. No retirement without it."
  OQ-7: "Canonical section-set expansion, INCLUDING the team-execution within-section structure gap. SURFACED ONLY, explicitly out of this phase's scope to build. The 4.1a schema-driven constraint is exactly what makes a future 9th section cost zero operator rewrite."

must_haves:
  truths:
    - "The navigator has answered OQ-1 with a named option (a, b or c), and the answer is written down where every later plan can read it"
    - "The navigator has answered OQ-2's mechanism-half question, and the answer explicitly does NOT re-decide Phase 267.2 W2's own trigger choice"
    - "OQ-3, OQ-4, OQ-5 and OQ-7 each carry a one-line disposition of record that a later reader can check without re-reading RESEARCH.md"
    - "No code, test, or generated file is touched by this plan"
  artifacts:
    - path: ".planning/phases/270-memory-and-context-operator-mcp-consolidate-scattered-local-/270-DECISIONS.md"
      provides: "The navigator's answers to OQ-1 and OQ-2, plus the four dispositions of record"
      min_lines: 40
      contains: "OQ-1 ANSWER"
  key_links:
    - from: ".planning/phases/270-memory-and-context-operator-mcp-consolidate-scattered-local-/270-DECISIONS.md"
      to: "lib/core/coverage-rollup.cjs"
      via: "the OQ-1 answer names the DEPTH_CAP constant it is ruling on, by file"
      pattern: "coverage-rollup\\.cjs"
---

<objective>
Answer the two blocking navigator questions before any code in this phase depends on them, and
record the four non-blocking dispositions so no later plan silently resolves them.

This plan exists as its own plan, ahead of every implementation plan, for one reason: RESEARCH.md
section 5 names OQ-1 as blocking the discovery wave and OQ-2 as blocking the identity-write wave.
Both are structural choices with frozen-constant and cross-phase consequences. Asking them once,
early, unblocks waves 3 and 6 simultaneously and keeps the decision out of the same context window
as the code it governs.

**This plan writes no code and touches no test.** Its only output is a decisions file.

Purpose: RESEARCH.md's own provenance note is binding here. `.planning/config.json` sets
`workflow.skip_discuss: true`, so there is no CONTEXT.md and there are no locked navigator
decisions for Phase 270. Every design recommendation in RESEARCH.md section 4 needs navigator
ratification, not assumption. Two of them cannot be safely defaulted.

Output: `270-DECISIONS.md` in this phase directory, carrying two answered questions and four
dispositions of record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/270-memory-and-context-operator-mcp-consolidate-scattered-local-/270-RESEARCH.md
@.planning/phases/270-memory-and-context-operator-mcp-consolidate-scattered-local-/270-VALIDATION.md

Do NOT read `.planning/STATE.md` in full. It is 1.3MB and will consume the whole context window.
If you need it, grep it for a specific literal instead.
</context>

<artifacts_this_phase_produces>

This plan (270-01) produces:

| Artifact | Kind | Path |
|---|---|---|
| `270-DECISIONS.md` | new markdown decisions record | `.planning/phases/270-memory-and-context-operator-mcp-consolidate-scattered-local-/270-DECISIONS.md` |

Symbols produced by LATER plans in this phase, listed here once so the whole phase surface is
visible in one place:

| Symbol | Kind | Home | Plan |
|---|---|---|---|
| `tests/run-all-270.sh` | phase test aggregator | `tests/` | 270-02 |
| `tests/test-270-resource-session-room.cjs` | test file | `tests/` | 270-02 |
| `tests/test-270-connector-coverage.cjs` | test file | `tests/` | 270-02 |
| `tests/test-270-no-second-walker.cjs` | test file | `tests/` | 270-03 |
| `tests/test-270-baseline-schema-driven.cjs` | test file | `tests/` | 270-03 |
| `tests/test-270-tree-classification.cjs` | test file | `tests/` | 270-03 |
| `tests/test-270-dynamic-tree.cjs` | test file | `tests/` | 270-03 |
| `tests/test-270-cross-room-fence.cjs` | test file | `tests/` | 270-04 |
| `tests/test-270-identity-write.cjs` | test file | `tests/` | 270-04 |
| `registerResources(server, ctx)` | changed signature (was `(server, roomDir)`) | `lib/mcp/resources.cjs` | 270-05 |
| `lib/mcp/tools/dual-path.cjs` + its `connectors` export | new tool module | `lib/mcp/tools/` | 270-06 |
| `tests/test-270-tool-schema-budget.cjs` | test file carrying the measured baseline | `tests/` | 270-06 |
| `discoverIcmForest(opts)` | new exported function | `lib/core/icm-forest.cjs` | 270-07 |
| `DIRECTORY_CLASSES` | new frozen enum | `lib/core/icm-forest.cjs` | 270-07 |
| `mos://tree` and `mos://room/{slug}/tree` | new MCP Resources | `lib/mcp/resources.cjs` | 270-08 |
| `startTreeWatcher(server, opts)` / `stopTreeWatcher()` | new exported functions | `lib/mcp/tree-watcher.cjs` | 270-08 |
| `context_assemble` | new MCP Tool | `lib/mcp/tools/context.cjs` | 270-09 |
| `_meta.legCostChars` / `_meta.legCostTokensApprox` / `opts.estimateOnly` | new `getRoomContext` fields | `lib/core/navigation/room-context.cjs` | 270-09 |
| `findTransitiveSupport(db, nodeId, opts)` | new exported function | `lib/core/navigation/insights.cjs` | 270-10 |
| `findNearestSubRoomDecisions(parentRoomDir, opts)` | new exported function | `lib/core/icm-forest.cjs` | 270-10 |
| `graph_reason` | new MCP Tool | `lib/mcp/tools/graph-reason.cjs` | 270-10 |
| `identity_write` | new MCP Tool | `lib/mcp/tools/identity.cjs` | 270-11 |
| retirement of `room_state_bound` | removed MCP Tool | `lib/mcp/tools/room.cjs` | 270-12 |

</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: OQ-1 navigator decision - DEPTH_CAP reconciliation for the ICM forest walk</name>

  <files>none (decision only)</files>

  <read_first>
    - `.planning/phases/270-.../270-RESEARCH.md` section 3.1.1 (the two walkers and their limits) and section 5 OQ-1. Read both in full before presenting the options.
    - `lib/core/coverage-rollup.cjs` lines 38-45 - the frozen `DEPTH_CAP = 3` constant and its in-source rationale.
    - `lib/core/memory/reconcile-memory-runner.cjs` lines 141-195 - `walkFractalMemory`, its "NO hand-rolled walker" header, and lines 171-173 where the source names "ZOOM re-rooting beyond DEPTH_CAP" as intentionally deferred.
    - `lib/core/graph-derivation.cjs` lines 25-60 - `rollupSubRooms`'s arbitrary-depth, cycle-guarded, cross-room ATTACH header.
  </read_first>

  <decision>
    Walker A (`walkFractalMemory`) caps its memory-file scan at `DEPTH_CAP = 3`, a frozen constant
    in `lib/core/coverage-rollup.cjs:41` that carries an explicit "do NOT mint a second depth
    constant" instruction. Walker B (`rollupSubRooms`) is arbitrary depth. The navigator's original
    scope ask was "full dynamic awareness of the entire ICM folder tree at runtime". Those two
    facts have never been reconciled. `discoverIcmForest` (plan 270-07) cannot be written until
    this is settled.
  </decision>

  <context>
    Why this cannot be defaulted silently: `DEPTH_CAP` is a frozen constant with a stated Part 11
    R11 rationale, and `reconcile-memory-runner.cjs:171-173` names the exact extension the
    navigator is asking for as deliberately OUT of scope for the phase that wrote it. Picking
    option (b) without a ruling would quietly overturn a prior phase's recorded deferral. Picking
    option (c) would mint a second effective depth policy. Both are the planner overreaching.

    What is NOT in question: `discoverIcmForest` will delegate descent to the existing walkers
    under every option. It never hand-rolls a recursive `readdirSync`. That is Canon Part 7 and is
    pinned by `tests/test-270-no-second-walker.cjs`.
  </context>

  <options>
    <option id="oq1-a">
      <name>(a) Keep the cap, expose structure beyond it - RECOMMENDED DEFAULT</name>
      <pros>Zero change to a frozen constant. Honors the prior phase's recorded deferral. `discoverIcmForest` still answers "what exists, at what depth, in what class" to arbitrary depth via Walker B's `NESTED_WITHIN` traversal, while memory-file READS stay capped at 3 exactly as today. Smallest blast radius; nothing existing changes behaviour.</pros>
      <cons>A depth-4 sub-room is visible as structure but its memory artifacts are still not scanned or projected. The navigator's "full awareness" ask is answered for structure, not for content.</cons>
    </option>
    <option id="oq1-b">
      <name>(b) Implement ZOOM re-rooting</name>
      <pros>Fully answers the original ask: a depth-4 sub-room is treated as a fresh depth-0 root of its own recursion, so its memory artifacts get scanned. `reconcile-memory-runner.cjs:171-173` already names this as the designed extension point, so the design is pre-thought.</pros>
      <cons>Materially larger. Touches the memory reconcile runner and the `MEMORY_ARTIFACT_NODE_ID(section, kind)` uniqueness scheme that depth-qualification currently guarantees. Overturns a prior phase's explicit deferral. Realistically its own phase, not a task inside this one.</cons>
    </option>
    <option id="oq1-c">
      <name>(c) Make the cap a parameter, default 3</name>
      <pros>Caller controls reach without a code change per call site.</pros>
      <cons>Effectively mints a second depth policy while leaving the constant nominally frozen, which is the exact failure Pitfall P5 names. The parameter becomes the real contract and the constant becomes decoration.</cons>
    </option>
  </options>

  <acceptance_criteria>
    - The navigator has selected exactly one of `oq1-a`, `oq1-b`, `oq1-c`, and that literal option id is written into `270-DECISIONS.md` under a heading containing the exact string `OQ-1 ANSWER`.
    - The recorded answer names `lib/core/coverage-rollup.cjs` explicitly, so a later reader can find the constant being ruled on without re-deriving it.
    - If the answer is `oq1-b`, the decisions file ALSO records that plan 270-07's scope grows and that `reconcile-memory-runner.cjs` enters this phase's `files_modified`, and flags that the phase may need a split (a `## PHASE SPLIT RECOMMENDED` note back to the orchestrator).
    - No file under `lib/`, `bin/`, `scripts/`, `tests/` or `data/` is modified by this task. `git status --porcelain lib bin scripts tests data` returns empty.
  </acceptance_criteria>

  <action>
    Present the three options above to the navigator EXACTLY as written, with the option ids visible.
    Do not add a fourth option and do not editorialize past the recorded pros and cons.

    Before presenting, run these two reads so the navigator sees ground truth rather than a summary:
    `sed -n '38,45p' lib/core/coverage-rollup.cjs` (the frozen constant and its rationale) and
    `sed -n '169,175p' lib/core/memory/reconcile-memory-runner.cjs` (the source naming ZOOM
    re-rooting as intentionally deferred). Quote both outputs in the presentation.

    State the recommended default (`oq1-a`) once and stop. Do not implement anything. Do not write
    `270-DECISIONS.md` here; Task 3 writes it after both decisions are in hand.

    Capture the navigator's reply verbatim, including any reasoning, and carry it to Task 3.
  </action>

  <verify>
    <automated>git status --porcelain lib bin scripts tests data | wc -l | grep -qx 0 && echo "ok: no code touched by the decision gate"</automated>
  </verify>

  <done>The navigator has named one of `oq1-a`, `oq1-b`, `oq1-c`, the reply is captured verbatim for Task 3, and no file under lib/, bin/, scripts/, tests/ or data/ was modified.</done>

  <resume-signal>Reply with one of: `oq1-a`, `oq1-b`, `oq1-c` (optionally with a sentence of reasoning to record verbatim).</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: OQ-2 navigator decision - scope of the cross-room identity write in THIS phase</name>

  <files>none (decision only)</files>

  <read_first>
    - `.planning/phases/270-.../270-RESEARCH.md` section 4.3 in full (the correction that collapses the task) and section 5 OQ-2.
    - `lib/core/user-md-ops.cjs` lines 420-480 - `writeUserMdAtomic(userMdPath, data)`, the absolute-path-taking atomic writer with zero room coupling.
    - `lib/core/user-archetype.cjs` line 64 - the already-wired READ of `path.join(os.homedir(), '.mindrian-user.md')`.
    - `.planning/ROADMAP.md`, the `### Phase 267.2` entry - so the answer does not duplicate or contradict that phase's own W2 scope.
  </read_first>

  <decision>
    RESEARCH.md 4.3 proved the MECHANISM already exists: `writeUserMdAtomic(path.join(os.homedir(),
    '.mindrian-user.md'), data)` works today, unmodified, with zero room coupling. What is missing
    is a caller and a trigger. This phase can safely ship the caller. The TRIGGER is jointly owned
    with Phase 267.2 W2 and Phase 267.3 and must be decided once, across all three phases, not
    twice.

    Question for the navigator: does Phase 270 ship the `identity_write` MCP Tool (the caller plus
    its `F.1` hitl_shape declaration) now, leaving the trigger to Phase 267.2, or does Phase 270
    stop at recording the 4.3 correction and hand the whole item to Phase 267.2?
  </decision>

  <context>
    The caveat RESEARCH.md 4.3 raises and this decision must not paper over: a first install may
    have no MCP session at all, and the FIRST_INSTALL surface is a bash hook injecting prose
    (`scripts/session-start`). An MCP tool the model must CHOOSE to call is model-compliance
    dependent, which is precisely the fragility Phase 267.2's own goal text names about
    `check-onboard --write`. Making it a tool does not by itself make it deterministic. So
    `oq2-ship-caller` is honest only if it is recorded as "the mechanism half, deliberately not the
    determinism half".
  </context>

  <options>
    <option id="oq2-ship-caller">
      <name>Ship the caller in Phase 270, leave the trigger to Phase 267.2 - RECOMMENDED</name>
      <pros>Closes the "zero writers anywhere in the repo" finding that originated this phase. Reuses `writeUserMdAtomic` verbatim, so it is genuinely a two-line caller plus a connector declaration, not a new mechanism. Gives Phase 267.2 something concrete to trigger instead of asking it to build both halves.</pros>
      <cons>An MCP tool alone is not deterministic on first install. Must be recorded as such, not sold as a fix for the determinism problem.</cons>
    </option>
    <option id="oq2-defer-whole">
      <name>Record the 4.3 correction only; hand the whole item to Phase 267.2</name>
      <pros>One phase owns both halves, so the trigger and the caller ship together and cannot drift.</pros>
      <cons>Phase 270 drops plan 270-11 and the `tests/test-270-identity-write.cjs` pin, and the ROADMAP question 1 for this phase ("should the cross-room identity write be built as a first tool under this operator") goes unanswered in code.</cons>
    </option>
  </options>

  <acceptance_criteria>
    - The navigator has selected exactly one of `oq2-ship-caller`, `oq2-defer-whole`, and that literal option id is written into `270-DECISIONS.md` under a heading containing the exact string `OQ-2 ANSWER`.
    - The recorded answer contains a sentence stating explicitly that Phase 270 does NOT decide the identity-write TRIGGER, and names Phase 267.2 W2 as the owner.
    - If the answer is `oq2-defer-whole`, the decisions file records that plan 270-11 is dropped and that `tests/test-270-identity-write.cjs` (plan 270-04 Task 2) must be written as a MECHANISM pin against `writeUserMdAtomic` directly rather than against an `identity_write` tool handler.
    - No file under `lib/`, `bin/`, `scripts/`, `tests/` or `data/` is modified by this task.
  </acceptance_criteria>

  <action>
    Present the two options above to the navigator EXACTLY as written, with the option ids visible.

    Before presenting, run
    `grep -n "roomDir\|room_dir\|ROOM.md\|resolveActiveRoom\|MindrianRooms" lib/core/user-md-ops.cjs`
    and show the navigator that it returns NOTHING. That empty result is the entire basis for the
    claim that the mechanism already exists, and it is more convincing shown than asserted.
    Also show `sed -n '440,448p' lib/core/user-md-ops.cjs` (the absolute-path signature) and
    `sed -n '60,68p' lib/core/user-archetype.cjs` (the already-wired read).

    State the recommended default (`oq2-ship-caller`) once, together with the honest caveat that a
    model-invoked MCP tool is not by itself deterministic on first install. Do not implement
    anything. Do not write `270-DECISIONS.md` here; Task 3 writes it.

    Capture the navigator's reply verbatim and carry it to Task 3.
  </action>

  <verify>
    <automated>git status --porcelain lib bin scripts tests data | wc -l | grep -qx 0 && echo "ok: no code touched by the decision gate"</automated>
  </verify>

  <done>The navigator has named one of `oq2-ship-caller`, `oq2-defer-whole`, the reply is captured verbatim for Task 3, and no file under lib/, bin/, scripts/, tests/ or data/ was modified.</done>

  <resume-signal>Reply with one of: `oq2-ship-caller`, `oq2-defer-whole`.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 270-DECISIONS.md - the two answers plus the four dispositions of record</name>

  <files>.planning/phases/270-memory-and-context-operator-mcp-consolidate-scattered-local-/270-DECISIONS.md</files>

  <read_first>
    - The navigator's verbatim replies from Task 1 and Task 2 in this same session. Record them verbatim; do not paraphrase into a different meaning.
    - `.planning/phases/270-.../270-RESEARCH.md` section 5 (all seven open questions) and section 2.4 (the Part 8 correction) and section 4.4 (the Theo analogy rejection).
    - `.planning/REQUIREMENTS.md` - read the `## Traceability` section at the tail so the MEMOP caveat wording matches the Phase 266/269 precedent (working IDs minted at plan time, not yet formally registered).
  </read_first>

  <action>
    Create `.planning/phases/270-memory-and-context-operator-mcp-consolidate-scattered-local-/270-DECISIONS.md`.

    Frontmatter: `phase: 270`, `created: <today>`, `status: ratified`.

    Then these sections, in this order, with these exact headings:

    1. `## OQ-1 ANSWER` - the selected option id verbatim, the navigator's reasoning verbatim if
       given, and one line naming `lib/core/coverage-rollup.cjs` as the home of the `DEPTH_CAP = 3`
       constant this rules on. If the answer is `oq1-a`, add the sentence: "ZOOM re-rooting beyond
       DEPTH_CAP stays deferred, exactly as `lib/core/memory/reconcile-memory-runner.cjs:171-173`
       already recorded; Phase 270 does not overturn that deferral."

    2. `## OQ-2 ANSWER` - the selected option id verbatim, plus the mandatory sentence that Phase
       270 does not decide the identity-write TRIGGER and that Phase 267.2 W2 owns it.

    3. `## Dispositions of record (not re-decided by any later plan)` - a four-row table with
       columns `OQ | Disposition | Where it is handled`. The four rows, worded as follows:

       - OQ-3: "UNRESOLVED and deliberately so. `docs/HITL-SHAPE-DECLARATION-CONTRACT.md` names four
         R16 surface classes and MCP tools are not among them, so per-operation hitl_shape on MCP
         tools may be a parallel MCP-specific convention rather than a constitutional mandate.
         Phase 270 behaves as if it IS mandated - every new tool declares `connector` plus
         `hitl_shape` plus `hitl_why` - which is correct under either answer. Flagged, no code
         task." / "Flag only; carried to the navigator."

       - OQ-4: "No external grounding exists (the langtalks corpus returned nothing on multi-surface
         memory consistency). Phase 270 treats CLI/Desktop/Cowork room-resolution parity as a
         DESIGNED INVARIANT with a test, never as a researched pattern, and cites no external
         source for it." / "Pinned by the parity leg of `tests/test-270-resource-session-room.cjs`
         (plan 270-02), fixed by plan 270-05."

       - OQ-5: "CLOSED IN THIS PHASE. `detect_dual_path` and `extract_shallow` are registered inline
         at `bin/mindrian-mcp-server.cjs:187` and `:199` with no `connectors` export, so they appear
         in neither `data/mcp-tool-connectors.json` nor any hitl_shape declaration - a Part 11 R1
         born-wired gap. Fixed by extracting them into a `lib/mcp/tools/*.cjs` module, which is the
         only shape `scripts/build-connector-registry.cjs` discovers." / "RED-pinned at plan 270-02
         Task 3, closed at plan 270-06."

       - OQ-7: "SURFACED ONLY, explicitly out of Phase 270's scope to build. Two distinct sub-points,
         kept separate because they are different failure modes: (i) five candidate MISSING sections
         (Meetings, Value Proposition, Marketing and Sales, Funding Options, Research Documents),
         navigator-cited against the pre-MindrianOS Notion Data Room template; (ii) a WITHIN-SECTION
         structure gap - `team-execution` is correctly canonical and hyper-critical, but its
         `SECTION_METADATA` entry is thin prose with no role / domain-expertise / availability /
         cross-linking fields against real Mentor Profiles usage. Phase 270 decides neither. The
         4.1a schema-driven constraint is what makes a future 9th section, or a richer
         `team-execution` schema, cost zero operator rewrite." / "Carried to the navigator as a
         flag; enforced indirectly by `tests/test-270-baseline-schema-driven.cjs` (plan 270-03)."

    4. `## Corrections this phase carries forward (do not re-litigate)` - three bullets:
       - Part 8 is ALREADY triple-enforced (import-graph isolation in `lib/core/navigation.cjs`;
         the per-job `privacy_mode` const in `data/brain-packet-schema.json` via
         `lib/core/navigation/packet.cjs:29-36`; the runtime PreToolUse block in
         `scripts/part8-egress-guard-hook.cjs`). The ROADMAP's premise that it is "a documented
         convention, not a schema-level guarantee" is factually wrong. NO task in this phase may
         claim to newly enforce Part 8.
       - "Mirror Theo" is EXPLICITLY REJECTED as an architecture for the room side. Theo
         consolidates over one already-remote, read-mostly store; the room side is N room.db files
         across a nested forest with a hard cross-room-aggregation fence
         (`lib/core/navigation/edges.cjs:45`), human-gated promotion moments, and a never-leaves
         constraint. Different architecture, not the same one wearing a different hat.
       - `alwaysLoad` is a SERVER-level flag in `.mcp.json`, not a per-tool one. Phase 268's "make
         an explicit alwaysLoad true/false call per surviving tool" rubric is not expressible
         against this codebase and must not be carried forward. Reducing the number and size of
         registered tool descriptions is the only token lever this repo has.

    5. `## Requirement IDs minted by this phase` - a `MEMOP-01` through `MEMOP-15` table with the
       one-line behaviour each names (copy them from the plan frontmatter `requirements` fields
       across plans 270-02 through 270-12), followed by the verbatim caveat sentence: "These are
       phase-local working IDs minted at plan time and are NOT yet formally registered in
       `.planning/REQUIREMENTS.md`, matching the Phase 266 and Phase 269 precedent."

    No em-dashes anywhere in the file. Use hyphens.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/270-memory-and-context-operator-mcp-consolidate-scattered-local-/270-DECISIONS.md';const s=fs.readFileSync(p,'utf8');const need=['OQ-1 ANSWER','OQ-2 ANSWER','Dispositions of record','OQ-3','OQ-4','OQ-5','OQ-7','coverage-rollup.cjs','267.2','part8-egress-guard-hook.cjs','MEMOP-01','MEMOP-15','not yet formally registered'];for(const t of need){if(s.indexOf(t)===-1)throw new Error('missing: '+t);}if(s.indexOf(String.fromCharCode(8212))!==-1)throw new Error('em-dash found');if(s.split('\n').length<40)throw new Error('too short');console.log('ok '+s.split('\n').length+' lines')"</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/270-.../270-DECISIONS.md` exists and is at least 40 lines.
    - It contains the literal strings `OQ-1 ANSWER` and `OQ-2 ANSWER`, each followed by one of the option ids offered in Tasks 1 and 2.
    - It contains one-line dispositions for OQ-3, OQ-4, OQ-5 and OQ-7, and the OQ-7 disposition mentions BOTH the five candidate sections AND the `team-execution` within-section structure gap as separate sub-points.
    - It contains the literal strings `coverage-rollup.cjs`, `267.2`, `part8-egress-guard-hook.cjs`, `MEMOP-01`, `MEMOP-15`, and `not yet formally registered`.
    - The file contains zero U+2014 characters (no em-dashes; house rule). Verified by the node one-liner above, which tests the raw codepoint via `String.fromCharCode(8212)` rather than embedding the literal, so this plan file does not itself contain the character it forbids.
    - `git status --porcelain lib bin scripts tests data` returns empty (this plan touched no code).
  </acceptance_criteria>

  <done>Every later plan in this phase can read one file to learn what the navigator ruled, and no later plan has to re-open OQ-1, OQ-2, OQ-3, OQ-4, OQ-5 or OQ-7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| (none) | This plan writes one markdown file under `.planning/` and touches no executable surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-270-09 | Repudiation | the navigator's ruling | mitigate | Answers are recorded verbatim in `270-DECISIONS.md`, with the option id, so a later plan cannot silently reinterpret them |
| T-270-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase. RESEARCH.md's Package Legitimacy Audit records "No new packages recommended" and every capability is buildable on already-vendored deps. If any later plan introduces a package, the slopcheck legitimacy gate runs at that time. |
</threat_model>

<verification>
`git status --porcelain lib bin scripts tests data` is empty, and the decisions file passes the
Task 3 node gate.
</verification>

<success_criteria>
OQ-1 and OQ-2 are answered by the navigator with named option ids; OQ-3, OQ-4, OQ-5 and OQ-7 each
carry a one-line disposition; the three carried-forward corrections (Part 8 already triple-enforced,
Theo analogy rejected, `alwaysLoad` is server-level) are written down; MEMOP-01 through MEMOP-15 are
minted with the not-yet-registered caveat.
</success_criteria>

<output>
Create `.planning/phases/270-memory-and-context-operator-mcp-consolidate-scattered-local-/270-01-SUMMARY.md` when done.
</output>
