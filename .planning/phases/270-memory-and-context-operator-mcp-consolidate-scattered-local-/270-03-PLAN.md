---
phase: 270-memory-and-context-operator-mcp-consolidate-scattered-local-
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-270-no-second-walker.cjs
  - tests/test-270-baseline-schema-driven.cjs
  - tests/test-270-tree-classification.cjs
  - tests/test-270-dynamic-tree.cjs
autonomous: true
requirements: [MEMOP-03, MEMOP-04, MEMOP-05, MEMOP-06]
canon_parts: [3, 7, 9]

open_questions:
  OQ-1: "DEPTH_CAP reconciliation. Answered in plan 270-01 Task 1. These tests assert DELEGATION and CLASSIFICATION, which hold under every OQ-1 option, so they are written before the answer lands and stay valid after it."
  OQ-2: "Identity-write trigger. Answered in plan 270-01 Task 2. Not touched here."
  OQ-3: "MCP-tool hitl_shape R16 mandate is UNRESOLVED. Not touched here."
  OQ-4: "Cross-surface consistency is a designed invariant. Task 3's mos://tree pin is the Desktop-facing half of it."
  OQ-5: "Two undeclared MCP tools. RED-pinned at plan 270-02 Task 3, fixed at 270-06. Not touched here."
  OQ-6: "room_state_bound retirement. Gated at plan 270-12. Not touched here."
  OQ-7: "Canonical section-set expansion, INCLUDING the team-execution within-section structure gap. SURFACED ONLY, out of scope to build. Task 1's schema-driven tripwire is precisely what makes a future 9th section, or a richer team-execution schema, cost zero operator rewrite."

must_haves:
  truths:
    - "A source tripwire turns RED the moment operator code hand-rolls a recursive directory descent instead of delegating to walkFractalMemory, discoverSections or rollupSubRooms"
    - "A source tripwire turns RED the moment operator code hardcodes a canonical section name, a length === 8, or a length === 5, and it derives its own forbidden-literal list from the frozen table rather than restating it"
    - "A test asserts every discovered directory lands in exactly one of four classes, and that a blueprint-family room carrying fewer than 8 sections is a normal room, not a defect"
    - "A test asserts the tree Resource reflects a folder created AFTER the server registered its resources, with no re-registration step"
  artifacts:
    - path: "tests/test-270-no-second-walker.cjs"
      provides: "Canon Part 7 delegation census over the operator's own files, mirroring test-248-resolver-census.cjs"
      min_lines: 80
      contains: "reconcile-memory-runner"
    - path: "tests/test-270-baseline-schema-driven.cjs"
      provides: "The 4.1a tripwire: no hardcoded 8, no hardcoded 5, no inlined section-name literal in operator files"
      min_lines: 70
      contains: "SECTION_METADATA"
    - path: "tests/test-270-tree-classification.cjs"
      provides: "Four-class classification plus the blueprint-subset tolerance, over a real temp room fixture"
      min_lines: 100
      contains: "DIRECTORY_CLASSES"
    - path: "tests/test-270-dynamic-tree.cjs"
      provides: "Live discovery pin: a folder created after registration shows up on the next read"
      min_lines: 80
      contains: "mos://tree"
  key_links:
    - from: "tests/test-270-baseline-schema-driven.cjs"
      to: "lib/core/room-skeleton-scaffold.cjs"
      via: "imports SECTION_NAMES at runtime to BUILD its own forbidden-literal list, never restating the names"
      pattern: "require\\(.*room-skeleton-scaffold\\.cjs.*\\)"
    - from: "tests/test-270-no-second-walker.cjs"
      to: "lib/core/icm-forest.cjs"
      via: "comment-stripped source census over a declared OPERATOR_FILES array"
      pattern: "OPERATOR_FILES"
    - from: "tests/test-270-tree-classification.cjs"
      to: "lib/core/section-registry.cjs"
      via: "asserts STRUCTURAL_DIRS members land in the structural class, imported not restated"
      pattern: "STRUCTURAL_DIRS"
---

<objective>
Land the four Wave-0 pins that govern the discovery half of this phase: two source tripwires that
enforce Canon Part 7 and the 4.1a schema-driven constraint, and two behavioural pins that define
what `discoverIcmForest` and `mos://tree` must actually do.

The two tripwires exist because RESEARCH.md spent 74 tool calls proving the capabilities already
ship under different names, and the single most likely failure mode for this phase is an executor
re-deriving them from scratch:

- **P1/Part 7:** `walkFractalMemory` (`lib/core/memory/reconcile-memory-runner.cjs:174-195`) and
  `rollupSubRooms` (`lib/core/graph-derivation.cjs`) already walk. `discoverSections`
  (`lib/core/section-registry.cjs:63-108`) already qualifies. The reconcile runner's own header at
  `:144-145` says it verbatim: "NO hand-rolled walker - Part 7 reuse". The genuine gap is only a
  forest ROOT that composes them.
- **P4b/4.1a:** `SECTION_NAMES`, `SECTION_METADATA` and `IDENTITY_DIRECTORIES` are `Object.freeze`d
  and exported at `lib/core/room-skeleton-scaffold.cjs:430-437` under an in-source FROZEN TABLE
  CONTRACT at `:351`. The existing `length === 8` / `=== 5` assertions live in
  `room-skeleton-scaffold.test.cjs` and are THAT module's own contract test. The operator must not
  add a second copy, or it silently diverges the day OQ-7 lands.

**All four tests are RED when this plan finishes.** `lib/core/icm-forest.cjs` and the `mos://tree`
Resource do not exist yet. That is the Wave-0 contract. Plan 270-07 greens the first three, plan
270-08 greens the fourth.

Purpose: turn RESEARCH.md's reuse mapping and its four-class directory finding from prose into
gates an executor cannot walk past.
Output: four RED test files, discovered automatically by `tests/run-all-270.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/270-memory-and-context-operator-mcp-consolidate-scattered-local-/270-RESEARCH.md
@.planning/phases/270-memory-and-context-operator-mcp-consolidate-scattered-local-/270-VALIDATION.md

Do NOT read `.planning/STATE.md` in full. It is 1.3MB. Grep it for a literal if you need it.
</context>

<artifacts_this_phase_produces>

This plan (270-03) produces:

| Artifact | Kind | Path |
|---|---|---|
| `test-270-no-second-walker.cjs` | new source-census test, RED by design | `tests/test-270-no-second-walker.cjs` |
| `test-270-baseline-schema-driven.cjs` | new source-census test, RED by design | `tests/test-270-baseline-schema-driven.cjs` |
| `test-270-tree-classification.cjs` | new behavioural test, RED by design | `tests/test-270-tree-classification.cjs` |
| `test-270-dynamic-tree.cjs` | new behavioural test, RED by design | `tests/test-270-dynamic-tree.cjs` |
| `OPERATOR_FILES` | shared constant, declared independently in the two census tests | both census test files |

The full phase symbol inventory lives in plan 270-01's `<artifacts_this_phase_produces>` table.

</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The two source tripwires - no second walker, and no hardcoded baseline</name>

  <files>tests/test-270-no-second-walker.cjs, tests/test-270-baseline-schema-driven.cjs</files>

  <read_first>
    - `tests/test-248-resolver-census.cjs` IN FULL - THE donor for both files. Port its
      comment-stripping helper (the `tests/test-224-resolver-fallback.cjs:39` `stripComments`
      pattern it names), its census-rule numbering convention (`census.1`, `census.2`, ...), and
      its explicit "grep gate hygiene, NEVER grep raw source, a header prose mention
      self-invalidates the gate" rule. That rule is load-bearing here: both of these tests must
      mention the very literals they forbid, in their own headers.
    - `lib/core/memory/reconcile-memory-runner.cjs` lines 141-225 - `walkFractalMemory` at `:174-195`,
      `discoverMemoryFiles` at `:209-222`, the `BASENAME_TO_KIND` table at `:73-87`, the
      "NO hand-rolled walker - Part 7 reuse" header at `:144-145`, the depth-qualified section-key
      rule at `:153-156`, and the never-open-a-sub-room's-db rule at `:158-163`.
    - `lib/core/graph-derivation.cjs` lines 25-60 and 395-470 - `rollupSubRooms`'s header,
      `_directChildSlugs` at `:427-441`, `_childDirForSlug` at `:402-425`, and
      `_readChildEdgesViaAttach` at `:447+`.
    - `lib/core/section-registry.cjs` lines 39 and 63-108 - `STRUCTURAL_DIRS = ['meetings','team']`,
      the `continue` skip at `:83`, the hidden-dir skip at `:80`, and the fact that
      `discoverSections` qualifies on `STATE.md` OR any `.md` OR a Decision-16 nested artifact -
      NOT on `ROOM.md`. The walker's "ROOM.md-bearing" comment is looser than the code.
    - `lib/core/room-skeleton-scaffold.cjs` lines 30-67, 202-254, 345-360, 425-437 - the three frozen
      tables, `resolveBlueprint`'s validated-subset path at `:254`, the scaffold loop at `:353`
      iterating `sectionList` not `SECTION_NAMES`, the FROZEN TABLE CONTRACT comment at `:351`, and
      the exports block.
    - `lib/core/room-skeleton-scaffold.test.cjs` around `:133` and `:251` - the EXISTING `=== 8` and
      `=== 5` assertions. Read them so you can state in your header that they are that module's own
      contract test and that this phase deliberately does not add a second copy.
    - `lib/core/coverage-rollup.cjs` lines 38-45 - `DEPTH_CAP = 3` and the "do NOT mint a second
      depth constant" instruction.
  </read_first>

  <action>
    Both files declare the SAME operator-file list, independently, as a top constant:

    `const OPERATOR_FILES = ['lib/core/icm-forest.cjs', 'lib/mcp/tree-watcher.cjs', 'lib/mcp/tools/context.cjs', 'lib/mcp/tools/graph-reason.cjs', 'lib/mcp/tools/identity.cjs', 'lib/mcp/tools/dual-path.cjs'];`

    Both files use the same `readStripped(rel)` helper: read the file if it exists, strip `//` line
    comments and `/* */` block comments with the `test-248` idiom, return the stripped source; return
    `null` when the file does not exist. A `null` from `lib/core/icm-forest.cjs` specifically is a
    HARD FAIL with the message "operator core not created yet (plan 270-07) - RED by design", so
    Wave 0 is red for a stated reason rather than an accident. A `null` from any other target is a
    SKIP counted into a printed `skipped` tally, because those files legitimately arrive in later
    waves.

    ---

    **File A: `tests/test-270-no-second-walker.cjs`** - Canon Part 7 delegation census.

    Header comment states the rule and cites `reconcile-memory-runner.cjs:144-145` verbatim in
    substance, plus the grep-hygiene warning that this header itself contains the forbidden tokens
    and is therefore never scanned (only `OPERATOR_FILES` are).

    Four rules, numbered `walker.1` through `walker.4`:

    - `walker.1` DELEGATION PRESENT. `lib/core/icm-forest.cjs`'s stripped source must contain a
      require of `./section-registry.cjs`, a require of `./graph-derivation.cjs`, a require of
      `./memory/reconcile-memory-runner.cjs`, a require of `./room-skeleton-scaffold.cjs`, and a
      require of `./coverage-rollup.cjs`. Failure detail lists which requires are missing by name.
    - `walker.2` NO SECOND DESCENT. Across every existing `OPERATOR_FILES` entry, the stripped
      source must contain zero self-recursive directory descent. Detect it as: zero occurrences of
      a `readdirSync` call inside a function that also calls itself by name. Implement this
      structurally, not with a single regex: extract each `function <name>(` body by brace matching,
      and fail if a body contains BOTH `readdirSync` and a call to its own `<name>(`. Report the
      offending function name. This is stricter and less brittle than grepping for `readdirSync`
      outright, which would wrongly flag the legitimate single-level `listRooms()`-style enumeration
      the forest root is allowed to do.
    - `walker.3` NO SECOND DEPTH CONSTANT (Pitfall P5). Across every existing `OPERATOR_FILES`
      entry, the stripped source must contain zero assignment matching
      `/(?:const|let|var)\s+\w*DEPTH_CAP\w*\s*=/` and zero numeric literal `3` bound to an
      identifier whose name contains `depth` case-insensitively. `DEPTH_CAP` may only appear as an
      imported reference. Failure message names `lib/core/coverage-rollup.cjs:41` as the one place
      the constant lives.
    - `walker.4` NO PROMOTION FROM DISCOVERY (Pitfall P4, Canon Part 3 / Part 9 role 5). Across
      every existing `OPERATOR_FILES` entry, the stripped source must contain zero occurrence of
      `healRoom`, `approvedBy`, `birthRoom`, or `confirmNode`. Discovery surfaces a folder as
      "found, unregistered"; only a human-approved `healRoom` call promotes it, and that call lives
      in `lib/core/graph-backfill.cjs:292-297`, not here. Failure message quotes `healRoom`'s step
      (0) refusal shape `{ok:false, reason:'no_approval'}`.

    ---

    **File B: `tests/test-270-baseline-schema-driven.cjs`** - the 4.1a tripwire.

    The elegant bit, and it is mandatory: this test IMPORTS `SECTION_NAMES` and
    `IDENTITY_DIRECTORIES` from `lib/core/room-skeleton-scaffold.cjs` at runtime and BUILDS its
    forbidden-literal list from them. It never restates a section name. If OQ-7 later adds a 9th
    section, this test picks it up with zero edit - which is the same property it is enforcing on
    the operator.

    Four rules, numbered `baseline.1` through `baseline.4`:

    - `baseline.1` IMPORT PRESENT. `lib/core/icm-forest.cjs`'s stripped source contains a require of
      `./room-skeleton-scaffold.cjs` AND references at least one of `SECTION_NAMES`,
      `SECTION_METADATA`, `IDENTITY_DIRECTORIES` by identifier.
    - `baseline.2` NO INLINED SECTION LITERAL. For each name in the imported `SECTION_NAMES`, assert
      zero occurrences of that name as a quoted string literal (`'name'` or `"name"`) in any existing
      `OPERATOR_FILES` stripped source. Same for each entry of `IDENTITY_DIRECTORIES`, with ONE
      documented exception: `'team'` also appears in `section-registry.cjs`'s `STRUCTURAL_DIRS`, so
      if the operator needs to disambiguate it, it must do so by importing `STRUCTURAL_DIRS`, not by
      writing `'team'`. State that in a comment and enforce it: `'team'` is forbidden as a literal
      like every other name; the disambiguation must be `STRUCTURAL_DIRS.includes(name)`.
    - `baseline.3` NO FROZEN-COUNT ASSERTION. Across every existing `OPERATOR_FILES` entry, zero
      matches for `=== 8`, `=== 5`, `!== 8`, `!== 5`, `.length === 8`, `.length === 5`. Failure
      message names `lib/core/room-skeleton-scaffold.test.cjs:251` as the ONE legitimate home of
      those assertions and says a second copy silently diverges when OQ-7 lands.
    - `baseline.4` SUBSET IS NOT AN ERROR (Pitfall P4b, second half). Across every existing
      `OPERATOR_FILES` entry, zero matches for an error/throw/reject path whose adjacent text
      contains `missing section` or `incomplete room` or `section_missing`. Implement as: split the
      stripped source into statements, and for any statement containing `throw` or
      `ok: false` or `ok:false`, assert its text does not contain any of those three phrases.
      Header comment cites `room-skeleton-scaffold.cjs:202-254` (`resolveBlueprint` returns a
      validated SUBSET) and `:353` (the scaffold loop iterates `sectionList`, not `SECTION_NAMES`).

    Both files print a summary line stating how many `OPERATOR_FILES` entries existed, how many were
    skipped, and how many rules ran - so a green run can never be misread as covering files that
    were not there yet.

    No em-dashes. CJS only. `node:assert`, `node:fs`, `node:path` only.
  </action>

  <verify>
    <automated>node tests/test-270-no-second-walker.cjs; a=$?; node tests/test-270-baseline-schema-driven.cjs; b=$?; test $a -ne 0 -a $b -ne 0 && echo "both RED as designed (plan 270-07 greens them)"</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-270-no-second-walker.cjs` exits NON-ZERO today, and its output contains the literal `operator core not created yet`.
    - `node tests/test-270-baseline-schema-driven.cjs` exits NON-ZERO today, with the same message.
    - Neither failure is `Cannot find module`: `node tests/test-270-no-second-walker.cjs 2>&1 | grep -c 'Cannot find module'` returns 0, and the same for the other file.
    - `grep -c 'OPERATOR_FILES' tests/test-270-no-second-walker.cjs` returns at least 2, and the same for `tests/test-270-baseline-schema-driven.cjs`.
    - `tests/test-270-baseline-schema-driven.cjs` derives its forbidden list at runtime: `grep -c "room-skeleton-scaffold" tests/test-270-baseline-schema-driven.cjs` returns at least 1, AND `node -e "const s=require('fs').readFileSync('tests/test-270-baseline-schema-driven.cjs','utf8');const {SECTION_NAMES}=require('./lib/core/room-skeleton-scaffold.cjs');const inlined=SECTION_NAMES.filter(n=>s.indexOf(\"'\"+n+\"'\")!==-1||s.indexOf('\"'+n+'\"')!==-1);if(inlined.length)throw new Error('test restates section names: '+inlined.join(','));console.log('ok')"` exits 0. The test must not contain the very literals it forbids.
    - Both files contain the string `stripComments` or an equivalent comment-stripping helper, and neither greps raw source: `grep -c 'readFileSync' tests/test-270-no-second-walker.cjs` is at least 1 and every scan path goes through the stripper (verify by reading, then assert `grep -c "readStripped(" tests/test-270-no-second-walker.cjs` is at least 3).
    - Neither file contains U+2014, verified with the `String.fromCharCode(8212)` node check.
  </acceptance_criteria>

  <done>An executor who hand-rolls a walker, mints a second depth constant, auto-promotes a discovered folder, or types a section name as a literal gets a red test before review sees it.</done>
</task>

<task type="auto">
  <name>Task 2: RED pin - tests/test-270-tree-classification.cjs (four classes, subset tolerance, structure-only payload)</name>

  <files>tests/test-270-tree-classification.cjs</files>

  <read_first>
    - `.planning/phases/270-.../270-RESEARCH.md` section 3.1.0 IN FULL - the frozen baseline, the
      SUBSET nuance (a room may legitimately have FEWER than 8 sections), and the third directory
      class finding: `STRUCTURAL_DIRS = ['meetings','team']` is skipped by `discoverSections` at
      `section-registry.cjs:83`, `meetings` is populated and has its own Resource
      (`resources.cjs:136`) yet is invisible to section discovery BY DESIGN, and `team` appears in
      BOTH `IDENTITY_DIRECTORIES` and `STRUCTURAL_DIRS`. Also read Pitfall P4c.
    - `lib/core/room-skeleton-scaffold.cjs` lines 30-67 and 169-254 - `SECTION_NAMES`,
      `SECTION_METADATA`, `IDENTITY_DIRECTORIES`, and `resolveBlueprint(blueprintFamily)` returning
      either `SECTION_NAMES` (`:204`) or a validated subset (`:254`).
    - `lib/core/section-registry.cjs` lines 39 and 63-108 - `STRUCTURAL_DIRS` and the qualification
      predicate `discoverSections` actually uses.
    - `lib/core/graph-self-heal.cjs` lines 1-60 - `detectUnsentineledArtifactFolder(roomDir)` and
      `healRoom`'s step (0) `approvedBy` refusal. The tree must SURFACE an unsentineled folder and
      must never promote it.
    - `lib/core/room-root.cjs` - how a `.room-root` sentinel is written and what `slug` field it
      carries, since `_childDirForSlug` (`graph-derivation.cjs:402-425`) resolves children by
      matching it.
    - `lib/mcp/tools/room.cjs` lines 38-60 - `listRooms()` and the `MINDRIAN_ROOMS_HOME` env
      resolution, since the forest root enumerates through this logic.
    - `tests/test-209-session-start-exemplar.cjs` - the `ok(desc, fn)` harness. Port it.
  </read_first>

  <action>
    Create `tests/test-270-tree-classification.cjs`. Header states the four classes by name and
    cites P4c: "a flat directory list misrepresents the room; at least four classes exist with
    different discovery semantics."

    Define the expected class enum ONCE at the top as the contract this test pins:
    `const DIRECTORY_CLASSES = ['canonical_section', 'identity_directory', 'structural_directory', 'discovered'];`
    Plan 270-07 must export this exact frozen array from `lib/core/icm-forest.cjs`; the test asserts
    the module's export deep-equals this local copy, so the contract is stated in exactly two places
    and drift is caught.

    `makeFixtureForest()` helper. Build under `fs.mkdtempSync(path.join(os.tmpdir(),'270-forest-'))`
    and point `process.env.MINDRIAN_ROOMS_HOME` at it for the duration (restore in `finally`):

    - Room `alpha`: a FULL room. Every name in the imported `SECTION_NAMES` as a directory, each
      with a `ROOM.md` and a `STATE.md`. Every name in `IDENTITY_DIRECTORIES` as a directory. A
      `meetings/` directory containing one `.md` file. A `.room-root` sentinel with
      `{"slug":"alpha"}`. A `room.db`-less room is fine for this test; do not open SQLite here.
    - Room `beta`: a SUBSET room. Only three of the imported `SECTION_NAMES` on disk, chosen by
      index (`SECTION_NAMES[0]`, `[2]`, `[5]`) so the fixture never restates a name. Plus a
      `.room-root` with `{"slug":"beta"}` and a `STATE.md`.
    - Under `beta`, a nested `custom-lab/` directory with a `ROOM.md` and two `.md` artifacts and
      NO `.room-root` sentinel. This is the "discovered beyond baseline, unregistered" case that
      `detectUnsentineledArtifactFolder` finds and that `healRoom` refuses to promote without
      `approvedBy`.
    - Under `alpha`, a hidden `.scratch/` directory with a `.md` inside. It must never appear.

    Six `ok()` assertions:

    1. `'DIRECTORY_CLASSES is exported frozen and matches the pinned four-class contract'`
       `const forest = require('../lib/core/icm-forest.cjs');` assert
       `Object.isFrozen(forest.DIRECTORY_CLASSES)` and `deepStrictEqual` against the local copy.
    2. `'every returned node carries exactly one class, drawn from DIRECTORY_CLASSES'`
       Call `discoverIcmForest({ home: fixtureRoot })`. Flatten every node. Assert each has a
       `class` property, that `DIRECTORY_CLASSES.includes(node.class)`, and that no node carries two
       class-ish fields.
    3. `'the canonical / identity / structural split is imported, not guessed'`
       For room `alpha`: every directory whose basename is in `SECTION_NAMES` classifies as
       `canonical_section`; every directory whose basename is in `IDENTITY_DIRECTORIES` AND NOT in
       `STRUCTURAL_DIRS` classifies as `identity_directory`; `meetings` classifies as
       `structural_directory`. All three membership sets are IMPORTED at test runtime from
       `room-skeleton-scaffold.cjs` and `section-registry.cjs`, never written out.
    4. `'team resolves to exactly one class, deterministically, and the rule is stated'`
       `team` is in BOTH `IDENTITY_DIRECTORIES` and `STRUCTURAL_DIRS`. Assert it lands in
       `structural_directory` (the precedence rule this phase pins: `STRUCTURAL_DIRS` wins, because
       `discoverSections` skipping it is the stronger, behaviour-visible fact). Assert it appears
       exactly ONCE in the flattened node list. A comment above this leg states the precedence rule
       so plan 270-07 implements it rather than inventing its own.
    5. `'a blueprint-subset room is a NORMAL room, never an error'`
       For room `beta`: assert the top-level result has `ok: true`; assert the room's own record has
       no `error`, no `defect`, no `missing_sections` field, and no `ok: false`; assert the count of
       `canonical_section` nodes under `beta` equals 3 and NOT `SECTION_NAMES.length`. Comment cites
       `room-skeleton-scaffold.cjs:202-254` and `:353`.
    6. `'the payload is identity and structure only, never file bodies, and hidden dirs never appear'`
       Assert every node's key set is a subset of the allow-list
       `['slug','path','relPath','depth','class','hasRoomDb','registered','name','children']`.
       Assert no node value anywhere in the returned object is a string longer than 512 characters
       (a cheap, honest proxy for "no file body leaked"). Assert `custom-lab` appears with
       `registered: false`, and that no `.scratch` node exists at any depth. Comment cites
       `room-context.cjs:14-18`'s must-not-cross-the-wire discipline as the reason structure-only is
       the contract.

    Sanity guard before grading: assert at least 2 rooms and at least 10 total nodes were returned,
    and throw "harness never reached real data" otherwise.

    Clean up the fixture and restore `MINDRIAN_ROOMS_HOME` in a `finally`.

    No em-dashes. CJS only. No new deps. Do NOT write a section-name literal anywhere in this file;
    derive every name from the imported tables (the `tests/test-270-baseline-schema-driven.cjs` gate
    does not scan test files, but restating them here would still rot the same way).
  </action>

  <verify>
    <automated>node tests/test-270-tree-classification.cjs; test $? -ne 0 && echo "RED as designed (plan 270-07 greens this)"</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-270-tree-classification.cjs` exits NON-ZERO today, failing on the missing `lib/core/icm-forest.cjs` with a message naming plan 270-07.
    - `grep -c 'DIRECTORY_CLASSES' tests/test-270-tree-classification.cjs` returns at least 3.
    - The file imports every membership set rather than restating it: `grep -c 'SECTION_NAMES' tests/test-270-tree-classification.cjs` is at least 3, `grep -c 'IDENTITY_DIRECTORIES' ...` is at least 2, `grep -c 'STRUCTURAL_DIRS' ...` is at least 2.
    - The file contains no canonical section-name literal: `node -e "const s=require('fs').readFileSync('tests/test-270-tree-classification.cjs','utf8');const {SECTION_NAMES}=require('./lib/core/room-skeleton-scaffold.cjs');const bad=SECTION_NAMES.filter(n=>s.indexOf(n)!==-1);if(bad.length)throw new Error('restates: '+bad.join(','));console.log('ok')"` exits 0.
    - `grep -c "ok('" tests/test-270-tree-classification.cjs` returns 6.
    - The file contains the literals `custom-lab`, `registered`, and `.scratch` (the three fixture cases legs 5 and 6 depend on).
    - No U+2014, verified with the `String.fromCharCode(8212)` node check.
    - No fixture survives: `ls /tmp | grep -c '^270-forest-'` returns 0 after the run, and `git status --porcelain` shows no unexpected files.
  </acceptance_criteria>

  <done>The shape of `discoverIcmForest`'s return value is now a contract, including the two nuances (subset rooms, the double-classed `team`) that a naive implementation gets wrong.</done>
</task>

<task type="auto">
  <name>Task 3: RED pin - tests/test-270-dynamic-tree.cjs (a folder created after registration shows up on the next read)</name>

  <files>tests/test-270-dynamic-tree.cjs</files>

  <read_first>
    - `.planning/phases/270-.../270-RESEARCH.md` section 3.4(a) and 3.4(b) - the SDK's
      `sendResourceListChanged()` on `resource()` / `registerResource()` / `update()`, and the fact
      that a `ResourceTemplate`'s `list` callback ALREADY re-evaluates per `resources/list`
      (`resources.cjs:69-81`). Dynamic discovery with no registration step is the SHIPPED pattern
      for sections; the forest is a direct analogue, not an invention.
    - `node_modules/@modelcontextprotocol/sdk/dist/cjs/server/mcp.js` lines 380-400 and 451-524 -
      confirm the read-callback arity first hand (`readCallback(uri, extra)` for static,
      `readCallback(uri, variables, extra)` for templates) and the `sendResourceListChanged` call
      sites. Do not assume.
    - `lib/mcp/resources.cjs` lines 60-90 and 200-227 - the `room-sections` static resource and the
      `sectionTemplate` `list` callback, the two nearest analogues to what `mos://tree` must do.
    - `docs/research/RESEARCH_19_MCP_PERSISTENT_AGENTS.md` lines 126-198 - the notification model
      (`notifications/resources/list_changed`, per-URI `resources/subscribe`), its decoupling
      insight at `:159`, and its recommendation at `:198` to watch DIRECTORIES not files, one
      watcher per room root, with `awaitWriteFinish` debouncing.
    - `tests/test-270-tree-classification.cjs` from Task 2 of this plan - reuse its
      `makeFixtureForest()` shape rather than writing a second fixture builder. If you extract it,
      extract it into this file as a local copy with a comment naming the sibling, since these two
      tests must stay independently runnable.
  </read_first>

  <action>
    Create `tests/test-270-dynamic-tree.cjs`. Header states the requirement: "a sub-room created
    AFTER `registerResources` ran must appear on the next `mos://tree` read, with no
    re-registration. RED until plan 270-08."

    Reuse the `makeStubServer()` shape from `tests/test-270-resource-session-room.cjs` (plan 270-02
    Task 2) - capture `resource()`, `tool()`, and a `sendResourceListChanged()` counter. Copy it
    locally with a comment naming the sibling file; do not require across test files.

    Four `ok()` assertions:

    1. `'mos://tree is registered as a Resource, not as a Tool'`
       After `registerResources(stub, ctx)`, assert `stub.resources` (or `stub.templates`) has an
       entry whose URI is exactly `mos://tree`, and assert `stub.tools` has NO entry named
       `tree_read` or `mos_tree` or `context_tree`. Comment cites RESEARCH.md 4.1 item 1 and the
       Tool-vs-Resource decision rule at 3.4: pure read, browsable, no fork, URI-expressible
       therefore Resource; Resources cost nothing per turn against the always-loaded tool schema
       budget.
    2. `'the first mos://tree read reflects the fixture as built'`
       Invoke the `mos://tree` handler with `extra = { sessionId: 'sess-270-tree' }`. Parse the JSON
       body. Assert the fixture's two room slugs are both present. Sanity guard: throw "harness
       never reached real data" if fewer than 2 rooms come back.
    3. `'a sub-room created after registration appears on the NEXT read, with no re-registration'`
       `fs.mkdirSync` a new directory `gamma-lab` under room `beta`, write a `ROOM.md` and a
       `STATE.md` into it. Do NOT call `registerResources` again and do NOT touch the stub. Invoke
       the SAME captured handler a second time. Assert `gamma-lab` is present in the second response
       and was absent from the first. This is the whole point of the scope item: dynamic by
       discovery, not by a registration step.
    4. `'creating a room root fires sendResourceListChanged at most once per debounce window'`
       Start the watcher (`require('../lib/mcp/tree-watcher.cjs').startTreeWatcher(stub, { home: fixtureRoot, debounceMs: 50 })`),
       record `stub.listChangedCount`, create THREE directories in quick succession under the
       fixture root, `await` a sleep of roughly 4x the debounce window, then assert the counter
       increased by at least 1 and by no more than 2. Stop the watcher in a `finally` and assert
       `stopTreeWatcher()` leaves no open handle (assert the process would exit: check
       `process._getActiveHandles` length does not grow, or simply assert `stopTreeWatcher()`
       returns truthy and the test process terminates without a forced exit). Comment cites
       RESEARCH_19 `:198` (watch directories not files, one watcher per room root,
       `awaitWriteFinish` debouncing) and records Assumption A4 verbatim in substance: "SDK-side
       behaviour is confirmed by reading `mcp.js`; whether the notification usefully reaches Claude
       Code / Desktop / Cowork clients is NOT exercised here and is a manual verification
       (270-VALIDATION.md MEMOP-12)."

    Clean up the fixture and stop the watcher in a `finally`. Never leave a chokidar handle open;
    a hanging test is a false green in a CI harness.

    No em-dashes. CJS only. `chokidar` is already a dependency (`package.json` `^4.0.3`); do not add
    a package.
  </action>

  <verify>
    <automated>timeout 60 node tests/test-270-dynamic-tree.cjs; c=$?; test $c -ne 0 -a $c -ne 124 && echo "RED as designed (plan 270-08 greens this)"</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-270-dynamic-tree.cjs` exits NON-ZERO today, failing because `mos://tree` is not registered and/or `lib/mcp/tree-watcher.cjs` does not exist. The message names plan 270-08.
    - The run TERMINATES on its own: `timeout 60 node tests/test-270-dynamic-tree.cjs; test $? -ne 124` succeeds. A hung watcher handle is a failure of this task, not an acceptable RED.
    - `grep -c 'mos://tree' tests/test-270-dynamic-tree.cjs` returns at least 2.
    - `grep -c 'sendResourceListChanged' tests/test-270-dynamic-tree.cjs` returns at least 2.
    - `grep -c 'gamma-lab' tests/test-270-dynamic-tree.cjs` returns at least 2 (the create and the two assertions).
    - `grep -c "ok('" tests/test-270-dynamic-tree.cjs` returns 4.
    - The file does NOT add a dependency: `git diff --stat package.json` is empty.
    - No U+2014, verified with the `String.fromCharCode(8212)` node check.
    - No fixture survives: `ls /tmp | grep -c '^270-forest-'` returns 0 after the run.
  </acceptance_criteria>

  <done>"Dynamic by discovery, no registration step" is now a test, and the notification half carries its own honest Assumption A4 caveat instead of an untested claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test process -> `$MINDRIAN_ROOMS_HOME` | Tasks 2 and 3 override this env var to point at a temp fixture. The real `~/MindrianRooms` must never be read or written. |
| filesystem watcher -> process lifetime | Task 3 starts a chokidar watcher. An unclosed handle hangs the suite and reads as a timeout rather than a verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-270-04 | Information Disclosure | tree/forest read payload | mitigate | Task 2 leg 6 pins an allow-listed key set and a 512-character ceiling on every returned string, so a future implementation cannot start returning file bodies without turning the test red |
| T-270-05 | Tampering | path traversal via a slug or section name | mitigate | Task 2's fixture includes a hidden `.scratch` dir and an unsentineled `custom-lab`; leg 6 asserts hidden dirs never surface. Plan 270-07 must reuse `safeResolveSection` / `SECTION_RE` from `lib/mcp/tool-router.cjs` rather than joining raw strings |
| T-270-06 | Elevation of Privilege | auto-promotion of a discovered folder | mitigate | Task 1 rule `walker.4` forbids `healRoom` / `approvedBy` / `birthRoom` / `confirmNode` in operator source; Task 2 leg 6 asserts `custom-lab` comes back `registered: false` rather than promoted |
| T-270-12 | Denial of Service | an unclosed chokidar handle hanging the suite | mitigate | Task 3 requires the run to terminate under a 60s timeout and asserts `stopTreeWatcher()` in a `finally` |
| T-270-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs. `chokidar@^4.0.3` is already vendored; `git diff --stat package.json` is asserted empty. |
</threat_model>

<verification>
`bash tests/run-all-270.sh` discovers at least 7 files and reports `FAIL=6` at the end of wave 1
(the two pins from 270-02 plus the four from this plan). Every failure message names the plan that
greens it. No test hangs; no fixture survives; `git status --porcelain` is clean apart from the four
new test files.
</verification>

<success_criteria>
Canon Part 7 delegation and the 4.1a schema-driven constraint are enforced by source tripwires that
derive their own forbidden lists from the frozen tables; the four-class contract, the
blueprint-subset tolerance, the structure-only payload rule, and live discovery-without-registration
are each pinned by a behavioural test that fails today for a stated reason.
</success_criteria>

<output>
Create `.planning/phases/270-memory-and-context-operator-mcp-consolidate-scattered-local-/270-03-SUMMARY.md` when done.
</output>
