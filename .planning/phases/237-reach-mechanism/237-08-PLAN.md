---
phase: 237-reach-mechanism
plan: 08
type: execute
wave: 4
depends_on: ["237-02", "237-07"]
files_modified:
  - lib/mcp/tools/chain.cjs
  - tests/test-237-approve-executes.cjs
autonomous: true
requirements: [REACH-01]

must_haves:
  truths:
    - "On a seeded room, approving the Decision Gate for a material chain step causes that step's actual resolved command to run"
    - "The step's declared output artifact exists on disk after the approve, and did not exist before it"
    - "The chain trace records real execution with an exit code and an artifact path, not only a log line"
    - "A mutation restoring the log-only onStep turns the gate red because the artifact never appears"
    - "chain_run still halts at the first material step and still mints the gate through the same gate-render ladder"
    - "A non-approve verdict still does not execute the step"
    - "The Phase 237 findings are filed in both the phase directory and the rethinking-mindrianos room, or the failure to file is stated plainly rather than claimed done"
  artifacts:
    - path: "lib/mcp/tools/chain.cjs"
      provides: "chain_run wired to the real two-tier dispatcher as its onStep default, with the log-only stub deleted"
      contains: "chain-step-dispatcher"
    - path: "tests/test-237-approve-executes.cjs"
      provides: "end-to-end approve-to-execute proof through the real mint / answer / resume path, plus the log-only mutation"
      min_lines: 140
  key_links:
    - from: "lib/mcp/tools/chain.cjs::chainRun"
      to: "lib/core/chain-step-dispatcher.cjs::makeChainStepDispatcher"
      via: "the wired onStep default, replacing makeDefaultOnStep"
      pattern: "makeChainStepDispatcher"
    - from: "lib/mcp/tools/chain.cjs::_resumeFromGateAnswer"
      to: "entry.onStepFn"
      via: "the approve path that now reaches a real executor"
      pattern: "onStepFn"
---

<objective>
Close the approve-to-execute seam: make the thing that runs when a navigator approves a Decision Gate be the real dispatcher, and prove it with an artifact on disk.

Purpose: `chain.cjs:293` installs `makeDefaultOnStep(roomDir)` as the `onStep` default, and the MCP tool handler at line 470 supplies no `onStep`, so every real `chain_run` call through MCP uses the log-only stub. The approve path at line 395 calls it. Plan 07 built the honest replacement; this plan wires it and proves the whole loop with the one thing that cannot be faked, a file that exists afterward and did not exist before.

The fixture is `/mos:snapshot` because it is uniquely suitable: it is MATERIAL under BOTH authorities (so the Plan 02 collapse cannot have reclassified it and invalidated the test), it is genuinely executable server-side through `scripts/generate-hub.cjs` with zero npm dependencies and node built-ins only, its behavior is a pure filesystem read of the room followed by a single write, and its artifact `<roomDir>/exports/hub.html` is a one-line existence assertion.

This plan also carries the phase's close-out obligations: the CLAUDE.md Dev-Research Compositing mirror, and the three forward-routed observations this phase surfaced but must not fix.

Output: `chain.cjs` wired to the dispatcher with the stub deleted, an end-to-end gate that would go red if the stub came back, and the phase research filed in both homes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/237-reach-mechanism/237-RESEARCH.md
@.planning/phases/237-reach-mechanism/237-PATTERNS.md
@.planning/phases/237-reach-mechanism/237-VALIDATION.md
@.planning/phases/237-reach-mechanism/237-02-SUMMARY.md
@.planning/phases/237-reach-mechanism/237-07-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols and files created across all eight Phase 237 plans. Newly created here means the source-grounding pass must not flag them as drift.

**New source files**
- `lib/core/chain-step-dispatcher.cjs` (Plan 07) exporting `dispatchStep`, `makeChainStepDispatcher`, `resolveExecutable`, `executableCommands`, `scriptPathFor`, and the constants `TIER_EXECUTABLE`, `TIER_HOST_DISPATCH`, `DISPATCH_TIMEOUT_MS`

**New generated-data fields**
- `data/command-registry.json` -> `commands[].executable` shaped `{ script, args, produces }`, `null` when the command declares none (Plan 05)
- command markdown frontmatter key `executable:` with sub-keys `script`, `args`, `produces` (Plan 05, populated on `commands/snapshot.md`)

**New runtime values**
- `memory_event` label `chain_step_dispatched` (Plan 07), deliberately distinct from the legacy `chain_step_executed` label so historical false rows stay distinguishable by inspection with no data migration
- `chain_output` fields `tier`, `executed`, `artifact`, `exit_code`, `timed_out`, `requires_host_dispatch`, `dispatch` (`{ agent, command, room_path, target_section }`) (Plan 07)

**New / widened exported functions**
- `lib/core/insight-sensors.cjs` -> `deriveTurnSignals(ctx, sessionId)` (widened from `deriveTurnSignals(ctx)`) and new exported helper `isMarkerOwnedByCaller(markerSessionId, callerSessionId)` (Plan 04)

**New writer fields**
- `scripts/post-write` -> shell variable `SESSION_ID` and top-level `session_id` key in the `last-cascade.json` payload (Plan 06)
- `scripts/auto-explore-fingerprint.cjs` -> `session_id` extracted from hook stdin and appended as a fifth element on the detached `auto-explore-fire.cjs` spawn argv (Plan 06)
- `scripts/auto-explore-fire.cjs` -> `session_id` read from `process.argv[5]` and stamped as a top-level key in the auto-explore finding JSON (Plan 06)

**Removed symbols (deletions, not creations, listed so a reader does not hunt for them)**
- `lib/mcp/tools/chain.cjs`: `postureForCommand`, `__resetPostureCache`, `_loadPostureIndex`, `_postureIndexCache`, `CONNECTOR_REGISTRY_PATH`, `PUSH_FORWARD` (Plan 02); `makeDefaultOnStep` (Plan 08)
- `lib/core/chain-executor.cjs`: `_loadDecide` (Plan 03)

**New test artifacts**
- `tests/run-all-237.sh` (Plan 01)
- `tests/test-237-autonomy-parity.cjs`, `tests/test-237-one-authority-fence.cjs` (Plan 02)
- `tests/test-237-decide-census.cjs` (Plan 03)
- `tests/test-237-session-scope.cjs`, `tests/test-237-session-scope.worker.cjs`, `tests/test-237-session-scope-degrade.cjs` (Plan 04)
- `tests/test-237-executable-seam.cjs` (Plan 05)
- `tests/test-237-post-write-session-stamp.cjs` (Plan 06)
- `tests/test-237-dispatcher-tiers.cjs` (Plan 07)
- `tests/test-237-approve-executes.cjs` (Plan 08)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the end-to-end approve-to-execute gate driving the real mint, answer and resume path</name>
  <read_first>
    - `lib/mcp/tools/chain.cjs` lines 250-310 (`chainRun`'s signature, the `gateAnswer` short-circuit at lines 287-289 that routes to `_resumeFromGateAnswer`, the `onStepFn` and `postureFn` defaults, and the `roomDir !== undefined` note forcing the resilient async path), lines 330-360 (the resume-ledger mint and what it stores on the entry), lines 370-430 (`_resumeFromGateAnswer`: the verdict check, the `entry.onStepFn(entry.haltedStep, entry.previousOutput)` call at line 395, and the response assembly), and lines 430-500 (the MCP tool handler, its zod schema, and the `connectors` export with `hitl_shape: 'F.1'`)
    - `lib/mcp/gate-render.cjs` (`renderGate` and `normalizeGateAnswer`: the exact `gate_id` and answer shapes the test must construct, since the test must answer the gate the way the real ladder does and not by poking the ledger)
    - `lib/core/chain-step-dispatcher.cjs` (the Plan 07 module: `makeChainStepDispatcher`'s signature and the `chain_output` fields the trace assertion will read)
    - `scripts/generate-hub.cjs` (its header stating zero npm dependencies, and the region around its final write confirming the output path `<roomDir>/exports/hub.html`)
    - `tests/test-198-chain-run-halt.test.cjs` (post-Plan-02 state: the working halt-and-mint drive this test extends into the approve leg, and its SKIP-safe module guard)
    - `tests/test-241-guardian-tripolar-parity.cjs` lines 77-114 and 160-215 (tmpdir lifecycle, `seedRoom`, the mutated-tmp-copy harness with its needle assertions and require-graph pin)
    - `tests/test-198-concurrency-mcp.test.cjs` lines 31-56 (hermetic env save/delete/restore)
  </read_first>
  <files>tests/test-237-approve-executes.cjs</files>
  <behavior>
    - Leg 1 PRE-STATE: on a freshly seeded room, `<roomDir>/exports/hub.html` does NOT exist. Asserted explicitly, because a test that only checks the post-state can pass on a pre-existing file.
    - Leg 2 HALT AND MINT: `chainRun` over a chain whose first step is `/mos:snapshot` halts at that step and mints a gate through `gate-render.renderGate`, returning a usable `gate_id`. This confirms the Plan 02 collapse left `/mos:snapshot` MATERIAL, which is the assumption the whole fixture rests on.
    - Leg 3 APPROVE EXECUTES: answering that gate with an approve verdict, through `chainRun`'s own `gateAnswer` path and a `normalizeGateAnswer`-shaped answer, causes `scripts/generate-hub.cjs` to run. `<roomDir>/exports/hub.html` exists afterward and is non-empty.
    - Leg 4 TRACE IS REAL: the returned result records `executed === true`, an `exit_code` of 0, and an `artifact` path that equals the resolved `<roomDir>/exports/hub.html`. Assert on those fields, not merely on truthiness, because "the trace records real execution, not only a log line" is the actual success criterion.
    - Leg 5 NON-APPROVE DOES NOT EXECUTE: answering the same gate with a non-approve verdict leaves `<roomDir>/exports/hub.html` absent and returns the existing not-executed note. The gate must still gate.
    - Leg 6 SINGLE USE: the resume ledger's single-use contract survives, so a second answer against the same `gate_id` does not execute a second time.
    - Leg 7 MUTATION: a tmp copy of `lib/mcp/tools/chain.cjs` with the dispatcher default textually replaced by a log-only `onStep` returning `quality: 'high'` reproduces the defect: the approve returns green and `<roomDir>/exports/hub.html` is ABSENT. This is the mutation the ROADMAP success criterion names by hand ("a mutation restoring log-only execution turns the gate red").
  </behavior>
  <action>
Create `tests/test-237-approve-executes.cjs`, a standalone `node:assert` executable exiting 0 or 1, with the repo's standard test header block naming Phase 237-08 and mapping the seven legs.

Seed the room with the `seedRoom` shape from `tests/test-241-guardian-tripolar-parity.cjs:94-114`, with enough section directories and `ROOM.md` content that `scripts/generate-hub.cjs` produces a non-trivial `hub.html`. Assert Leg 1 immediately after seeding.

Drive the real path end to end. Call `chainRun(steps, { roomDir, ... })` with no `onStep` and no `postureFn` supplied, so the test proves the WIRED DEFAULT and not an injected one. This is the whole point: `chain_run`'s MCP handler supplies neither, so a test that injects them proves nothing about production. Capture the `gate_id` from the halt result, build the answer through `gate-render.normalizeGateAnswer` (or the exact shape it produces), and pass it back as `chainRun(null, { gateAnswer })`. Do not reach into `_internal._resumeLedger` to fabricate an entry; the mint-then-consume ledger is the seam being proven live.

Wrap everything in the hermetic env block from `tests/test-198-concurrency-mcp.test.cjs:31-45`, deleting `CLAUDE_ACTIVE_ROOM` and saving/restoring `MINDRIAN_ROOMS_HOME` and `MINDRIAN_MCP_FIRST`, plus `mkTmp` and `process.on('exit')` cleanup.

Allow a generous but bounded wall-clock budget for the `generate-hub.cjs` spawn (a `timeout` on any spawn the test itself makes, and a clear failure message if the dispatcher's own timeout fires) so a slow machine produces a legible failure rather than a hang.

For Leg 7 follow `tests/test-241-guardian-tripolar-parity.cjs:160-215`: read `lib/mcp/tools/chain.cjs`, pin every relative require and any `PLUGIN_ROOT` computation to absolute repo paths with a per-needle `assert.ok` so a source drift fails loudly, apply the textual mutation, `assert.notEqual(mutated, src)`, write to a temp path, `require(dest)`, and assert the artifact is ABSENT while the returned quality reads green. Never mutate the working tree.

Run the test now, before Task 2. Legs 3, 4 and 7 are expected to FAIL: today the approve returns `quality: 'high'` and no artifact appears. Capture that RED verbatim. That capture IS the reproduction of REACH-01's headline defect and belongs in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `node tests/test-237-approve-executes.cjs` exits non-zero when run before Task 2, with Legs 3 and 4 named in the failure output and the failure showing `quality: 'high'` alongside an absent `exports/hub.html`
    - The captured pre-fix RED output is pasted verbatim into `237-08-SUMMARY.md`
    - `grep -c '_resumeLedger' tests/test-237-approve-executes.cjs` returns 0 (the test answers the gate through the real ladder, it does not fabricate a ledger entry)
    - `grep -c 'onStep' tests/test-237-approve-executes.cjs` shows no injection of an `onStep` into the primary `chainRun` call; verify by reading the call site that neither `onStep` nor `postureFn` is passed for Legs 2 through 6
    - `grep -c 'assert.notEqual(mutated' tests/test-237-approve-executes.cjs` returns at least 1
    - `grep -cE "require\('(jest|vitest|mocha|chai|sinon)" tests/test-237-approve-executes.cjs` returns 0
    - `grep -Pc '\x{2014}' tests/test-237-approve-executes.cjs` returns 0
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-237-approve-executes.cjs; test $? -ne 0 &amp;&amp; echo "EXPECTED RED before Task 2"</automated>
  </verify>
  <done>An end-to-end gate exists that drives the real mint / answer / resume path with the wired defaults, asserts both the pre-state and the post-state of the artifact, asserts the trace fields rather than truthiness, and has captured the live reproduction of the false-success defect.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the dispatcher as chain_run's onStep default and delete the log-only stub</name>
  <read_first>
    - `lib/mcp/tools/chain.cjs` in full (post-Plan-02 state: the stub at lines 184-210, the default installation at line 293, the ledger carry around lines 340-360 and 405-420, the module header comment block, and `module.exports` with its `_internal` bag)
    - `lib/core/chain-step-dispatcher.cjs` (the Plan 07 module: `makeChainStepDispatcher(roomDir, opts)`'s exact signature and returned closure shape)
    - `tests/test-237-approve-executes.cjs` (the file authored in Task 1, so the rewire targets the exact contract it asserts)
    - `tests/test-198-chain-run-halt.test.cjs` (post-Plan-02: confirm which `_internal` members it still references before removing any)
    - `.planning/phases/237-reach-mechanism/237-07-SUMMARY.md` (the dispatcher's final exported names and any deviation from the plan)
  </read_first>
  <files>lib/mcp/tools/chain.cjs</files>
  <action>
Delete `makeDefaultOnStep` entirely (currently lines 184-210). It is the log-only stub whose fabricated `quality: 'high'` is REACH-01's headline defect. Remove it from the `_internal` export bag as well.

Require the Plan 07 module and install it as the `onStep` default. Change line 293 from `const onStepFn = (typeof o.onStep === 'function') ? o.onStep : makeDefaultOnStep(roomDir);` to install `makeChainStepDispatcher(roomDir, { sessionId, targetSection })` instead, sourcing `sessionId` from whatever `chainRun` already has in scope for the call (pass `null` when it has none; the dispatcher tolerates it) and `targetSection` from the existing options if one is available, otherwise `null`. Keep `opts.onStep` as an injectable seam so tests and other callers that supply their own executor still win.

Confirm the resume-ledger carry still works end to end: the entry stored around line 350 records `onStepFn` and `_resumeFromGateAnswer` calls `entry.onStepFn(entry.haltedStep, entry.previousOutput)` at line 395. The stored value must be the dispatcher closure, so a resume after a mint reaches the real executor. Read those two regions and verify rather than assuming.

Update the module header comment so it documents the two-tier dispatch contract: a script-backed step genuinely spawns and its artifact is verified; a prompt-backed step returns `quality: null` with a `requires_host_dispatch` directive because the MCP server cannot invoke a Claude Code subagent or slash command; and `quality: 'high'` is only ever returned on a verified real execution. Delete any surviving prose describing the log-only behavior.

Verify the CIRS Part 11 contract is still consistent. `chain.cjs`'s `connectors` export around lines 483-498 declares `hitl_shape: 'F.1'` with an `hitl_why` describing the halt-at-first-material-step behavior. That behavior is unchanged by this rewire (the gate still mints, the gate still halts), so the declaration should still be accurate; read it and confirm, and if the `hitl_why` prose describes the executor's behavior in a way that is now wrong, correct the prose without changing the shape. Then re-run `node scripts/build-connector-registry.cjs --check`.

Do NOT change `chainRun`'s signature, the zod input schema at lines 440-449, the `_resumeLedger` single-use or TTL semantics, or the `gateAnswer` short-circuit. Do NOT adopt the Phase 234-05 registration-time write gate: `chain.cjs` lines 25-43 documents at length why `chain_run` deliberately does not, and that is not this phase's decision to revisit.

Do NOT touch `lib/mcp/tools/chain.cjs` lines 83-96 (the room-resolver copy), `isMcpFirst`, `resolveWriteRoom`, `resolveActiveRoom`, `fallbackRoomDir`, or `lib/mcp/mcp-first-flag.cjs`. Those belong to the v1.17.0 MCP-First milestone and touching them here creates a merge conflict with a milestone that has not started.
  </action>
  <acceptance_criteria>
    - `node tests/test-237-approve-executes.cjs` exits 0 with all seven legs passing
    - `node tests/test-237-dispatcher-tiers.cjs` exits 0
    - `node tests/test-237-autonomy-parity.cjs` exits 0
    - `node tests/test-237-one-authority-fence.cjs` exits 0
    - `node tests/test-198-chain-run-halt.test.cjs` exits 0
    - `node tests/test-act-on-runchain.cjs` exits 0
    - `grep -c 'makeDefaultOnStep' lib/mcp/tools/chain.cjs` returns 0
    - `grep -c 'chain-step-dispatcher' lib/mcp/tools/chain.cjs` returns at least 1
    - `grep -v '^ \*' lib/mcp/tools/chain.cjs | grep -c "quality: 'high'"` returns 0 (no fabricated quality survives anywhere in the file)
    - `git diff lib/mcp/tools/chain.cjs | grep -cE 'mcp-first-flag|isMcpFirst|resolveWriteRoom|resolveActiveRoom|fallbackRoomDir'` returns 0 (the v1.17.0 fence held)
    - `git diff lib/mcp/tools/chain.cjs | grep -cE '_resumeLedger|GATE_TTL|single-use'` shows no semantic change to the ledger's single-use or TTL behavior; verify by reading the diff
    - `node scripts/build-connector-registry.cjs --check` exits 0
    - `node scripts/build-orchestration-projection.cjs --check` exits 0
    - `node scripts/check-render-coverage.cjs` exits 0
    - `node tests/test-198-local-only.test.cjs` exits 0
    - `bash tests/run-all-237.sh` exits 0 with `Failed: 0` and `Skipped: 0`
    - Mutation re-check, demonstrated live not asserted: restore a log-only `onStep` returning `quality: 'high'` in the working tree, run `node tests/test-237-approve-executes.cjs`, capture the non-zero exit showing green quality with an absent `exports/hub.html`, restore byte-identically, confirm `git status --porcelain lib/mcp/tools/chain.cjs` is empty, and paste both captures into the SUMMARY
    - `grep -Pc '\x{2014}' lib/mcp/tools/chain.cjs` returns 0
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-237-approve-executes.cjs &amp;&amp; node tests/test-237-dispatcher-tiers.cjs &amp;&amp; node tests/test-198-chain-run-halt.test.cjs &amp;&amp; bash tests/run-all-237.sh</automated>
  </verify>
  <done>Approving a Decision Gate for a material chain step runs that step's real resolved command; `<roomDir>/exports/hub.html` exists afterward and did not before; the trace carries a real exit code and artifact path; the log-only stub is gone from the file; and restoring it has been demonstrated to turn the gate red.</done>
</task>

<task type="auto">
  <name>Task 3: File the phase findings in both required homes and record the forward-routed observations</name>
  <read_first>
    - `CLAUDE.md` section "Dev-Research Compositing (Rethinking Room)" (the rule that every GSD phase touching MindrianOS's own architecture files findings in BOTH the phase directory AND `~/MindrianRooms/rethinking-mindrianos/research/<dated-entry>/`, cross-linked, and that either half alone counts as incomplete)
    - `.planning/STATE.md` (the 2026-07-28 Phase 243 entry, which records that this exact mirror write was REFUSED by the Write tool under session worktree isolation and was stated plainly in the SUMMARY rather than claimed done; that is the precedent for the honest-degrade branch below)
    - `.planning/debug/room-bind-mcp-first-off-falls-back-to-stale-global-active-room.md` (its front-matter `status` and `next_action` fields; this phase closes only its signal-staleness leg and the file must NOT be moved to `.planning/debug/resolved/`)
    - `.planning/phases/237-reach-mechanism/237-RESEARCH.md` sections "Open Questions" and "Assumptions Log" (the five open questions and seven assumptions this phase either resolved or forward-routed)
  </read_first>
  <files>.planning/phases/237-reach-mechanism/237-08-SUMMARY.md</files>
  <action>
Part A, the Dev-Research Compositing mirror. Compose a dated research entry summarizing Phase 237's three findings and their fixes (the log-only executor and its fabricated quality, the two-authority category error with its measured 48-of-112 disagreement and 12 dangerous cases, and the freshness-as-ownership signal bleed), cross-linked to `.planning/phases/237-reach-mechanism/237-RESEARCH.md`. Attempt to write it under `~/MindrianRooms/rethinking-mindrianos/research/<YYYY-MM-DD>-phase-237-reach-mechanism/`.

If the write is REFUSED by the tool because this session is worktree-isolated or otherwise scope-restricted, do NOT retry with a workaround and do NOT claim it was filed. State plainly in the SUMMARY that the mirror was attempted and refused, name the exact refusal, and record the composed content inline in the SUMMARY so a future session with broader filesystem access can complete the filing without recomposing it. Phase 243 hit exactly this and handled it this way; matching that precedent is the honest outcome, and Canon Part 11's CIRS discipline forbids reporting a success about an effect that did not apply.

Part B, update the routed-in RCA's forward pointer. In `.planning/debug/room-bind-mcp-first-off-falls-back-to-stale-global-active-room.md`, update the `next_action` field to record that Phase 237 closed the signal-staleness leg (its Test 2 text, which is ROADMAP SC3's wording) and that the structural eight-copy room-resolver collapse remains open and is carried to the v1.17.0 MCP-First milestone. Do NOT change `status` away from `diagnosed` and do NOT move the file to `.planning/debug/resolved/`; the defect it names is not resolved. `.planning/debug/` is tracked, so this is an ordinary edit and commit, not a force-add.

Part C, record the forward-routed observations in the SUMMARY so they are not lost between phases. All four are OBSERVATIONS, not work items for this phase, and no task may act on them:
1. To Phase 238 (GATE-03): `lib/mcp/tools/chain.cjs`'s `_resumeLedger` is a process-global `Map`, not session-keyed, so two sessions sharing one MCP server process share it.
2. To Phase 239 (BRAIN-01): for a PLUGIN-BUNDLED MCP server the full hook-matcher tool-name form is `mcp__plugin_<plugin-name>_<server-name>__<tool-name>`; a matcher written against the bare `mcp__<server>__.*` form never fires. `lib/core/seam-liveness.cjs`'s `checkHookMatcherLiveness` header cites a bare form that may therefore not fire in production.
3. To a future phase: `scripts/act-command.cjs:262` feeds the real `decide()` a synthetic `sessionId: 'act-chain-' + idx` rather than a real session id.
4. To a future phase: four room-scoped freshness markers remain unscoped (`last-eureka.json`, `last-opportunity-harvest.json`, `url-ingest-ledger.json`, and the diffusion marker scan), all sharing the identical shape REACH-03 fixed, and `isMarkerOwnedByCaller` is exported so they can adopt it without a second mechanism.

Part D, record the phase-level decisions in the SUMMARY: the SC3 numbering resolution (built to ROADMAP.md's SC3, the stale-marker leg, which matches REQUIREMENTS.md REACH-03 and the RCA's Test 2; the RCA's Test 1 room-binding leg is v1.17.0's and was not built), the no-migration decision on the historical `chain_step_executed` rows, and the resolution of research assumptions A2, A3 and A6 with their evidence.
  </action>
  <acceptance_criteria>
    - `237-08-SUMMARY.md` contains either a path to the written `~/MindrianRooms/rethinking-mindrianos/research/<dated-entry>/` file OR an explicit statement that the mirror was attempted and refused, naming the refusal, with the composed content recorded inline
    - The SUMMARY does not claim the mirror was filed if it was not
    - `grep -c 'v1.17.0' .planning/debug/room-bind-mcp-first-off-falls-back-to-stale-global-active-room.md` returns at least 1
    - `grep -c '^status: diagnosed' .planning/debug/room-bind-mcp-first-off-falls-back-to-stale-global-active-room.md` returns 1 (status unchanged)
    - `ls .planning/debug/resolved/ | grep -c 'room-bind-mcp-first-off'` returns 0 (the file was not moved)
    - The SUMMARY contains all four forward-routed observations, each naming its destination phase
    - The SUMMARY contains the SC3 numbering resolution, the no-migration decision, and the A2 / A3 / A6 resolutions
    - `bash tests/run-all-237.sh` exits 0 with `Failed: 0` and `Skipped: 0`
    - `node scripts/doctor.cjs --acceptance` exits 0
    - `grep -Pc '\x{2014}' .planning/phases/237-reach-mechanism/237-08-SUMMARY.md` returns 0
  </acceptance_criteria>
  <verify>
    <automated>bash tests/run-all-237.sh &amp;&amp; node scripts/doctor.cjs --acceptance</automated>
  </verify>
  <done>The phase's findings are filed in both homes or the failure to mirror is stated plainly with the content preserved; the routed-in RCA carries an accurate forward pointer without being falsely resolved; and all four forward-routed observations plus the three phase-level decisions are on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> `chain_run` tool | Untrusted framework-chain input, zod-validated at `chain.cjs` lines 440-449 |
| navigator approve verdict -> resume ledger -> executor | The human-authorization boundary: a step executes only after an approve verdict is consumed from a single-use ledger |
| `chain_run` -> `lib/core/chain-step-dispatcher.cjs` | Where the MCP server's new spawn capability is actually reached in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-237-08-01 | Elevation of Privilege | a malformed or hostile chain step reaching execution | mitigate | The step's command is only a lookup key into the closed build-time-generated registry (Plan 05 / Plan 07 controls). Anything not in that registry routes to tier 2 and spawns nothing. The zod schema at `chain.cjs` lines 440-449 is unchanged and still validates the tool input. |
| T-237-08-02 | Spoofing | gate replay causing a second execution | mitigate | `chain.cjs`'s resume ledger is single-use with a 30-minute TTL (T-198-12). This plan does not weaken either property, asserted by Leg 6 and by a diff review acceptance criterion. The dispatcher runs strictly inside `_resumeFromGateAnswer`'s post-consume path. |
| T-237-08-03 | Elevation of Privilege | execution on a non-approve verdict | mitigate | Leg 5 asserts a non-approve verdict leaves the artifact absent. The verdict check at `chain.cjs` lines 380-389 is unchanged. |
| T-237-08-04 | Repudiation | a false-success trace surviving the rewire | mitigate | `grep -c "quality: 'high'"` over the comment-stripped file must return 0, so no fabricated quality can remain anywhere in `chain.cjs`. Leg 4 asserts the trace's `executed`, `exit_code` and `artifact` fields by value rather than truthiness. Leg 7 demonstrates that restoring the stub turns the gate red. |
| T-237-08-05 | Denial of Service | a slow or hanging spawn blocking the MCP server | mitigate | `DISPATCH_TIMEOUT_MS` is enforced inside the dispatcher (Plan 07, T-237-07-05) and the EXEC-06 `maxSteps` brake bounds the step count. This plan adds a bounded wall-clock budget to the test itself so a slow machine fails legibly rather than hanging. |
| T-237-08-06 | Spoofing | cross-session gate consumption | accept | The `_resumeLedger` is a process-global `Map` shared by every session on one MCP server process. Out of scope here and explicitly routed to Phase 238 GATE-03 in Task 3's forward-routed observations. Recorded as an accepted, disclosed gap rather than silently carried. |
| T-237-08-07 | Tampering | scope creep into the v1.17.0 room-resolver collapse | mitigate | `lib/mcp/tools/chain.cjs` lines 83-96 hold one of the eight resolver copies and are visibly wrong while editing this file. A grep acceptance criterion over `git diff` for `mcp-first-flag`, `isMcpFirst`, `resolveWriteRoom`, `resolveActiveRoom` and `fallbackRoomDir` must return 0. |
| T-237-08-08 | Repudiation | claiming a research mirror that was refused | mitigate | Task 3 Part A's honest-degrade branch: state the refusal, name it, preserve the content inline, do not claim the filing. Canon Part 11 CIRS forbids reporting success about an effect that did not apply, and Phase 243 set the precedent. |
| T-237-08-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. `node:assert` is built in and `scripts/generate-hub.cjs` declares zero npm dependencies; no dependency manifest is touched. |
</threat_model>

<verification>
Phase-closing verification. Run all of it, in order:

1. `node tests/test-237-approve-executes.cjs` -> exit 0, 7/7 legs
2. `node tests/test-237-dispatcher-tiers.cjs` -> exit 0
3. `node tests/test-237-decide-census.cjs` -> exit 0
4. `node tests/test-237-executable-seam.cjs` -> exit 0
5. `node tests/test-237-autonomy-parity.cjs` -> exit 0
6. `node tests/test-237-one-authority-fence.cjs` -> exit 0
7. `node tests/test-237-session-scope.cjs` -> exit 0
8. `node tests/test-237-session-scope-degrade.cjs` -> exit 0
9. `node tests/test-237-post-write-session-stamp.cjs` -> exit 0
10. `node tests/test-198-chain-run-halt.test.cjs` -> exit 0
11. `node tests/test-act-on-runchain.cjs` -> exit 0
12. `node tests/test-recipe-maps-authority.cjs` -> exit 0
13. `node tests/test-198-local-only.test.cjs` -> exit 0
14. `node tests/test-sensors-part8-sweep.cjs` -> exit 0
15. `bash tests/run-all-237.sh` -> exit 0, `Failed: 0`, `Skipped: 0`
16. `node scripts/build-connector-registry.cjs --check` -> exit 0
17. `node scripts/build-orchestration-projection.cjs --check` -> exit 0
18. `node scripts/check-render-coverage.cjs` -> exit 0
19. `node scripts/doctor.cjs --acceptance` -> exit 0
20. `git diff --stat` against the phase base commit shows zero touches to `lib/core/lazygraph-ops.cjs`, `scripts/build-ecosystem-graph.cjs`, `tests/test-236-*` and `.planning/phases/236-room-db-data-loss-fixes/`

Out of scope, do not touch: `lib/mcp/tools/chain.cjs` lines 83-96, `lib/mcp/mcp-first-flag.cjs`, every room-resolver copy across `lib/mcp/tools/*.cjs` and `lib/mcp/tool-router.cjs`, `room_bind`'s response contract, the `_resumeLedger` session-keying (Phase 238), the Phase 234-05 registration-time write gate, anything under `.planning/phases/236-room-db-data-loss-fixes/`, `lib/core/lazygraph-ops.cjs`, `scripts/build-ecosystem-graph.cjs`, `tests/test-236-*`.

Do NOT write a server-side methodology executor. If a leg seems to demand an artifact from a prompt-backed command, that is the Pitfall-1 trap and the answer is tier 2's honest refusal.
</verification>

<success_criteria>
- ROADMAP SC1 is satisfied and demonstrated: approving a material step's Decision Gate on a seeded room runs the real resolved command, the artifact exists afterward and did not before, the trace carries a real exit code and artifact path, and a mutation restoring log-only execution turns the gate red
- The log-only stub and every `quality: 'high'` fabrication are gone from `lib/mcp/tools/chain.cjs`
- The single-use, TTL-bounded resume ledger is unchanged and a non-approve verdict still does not execute
- `bash tests/run-all-237.sh` exits 0 with zero failures and zero skips: every leg authored in Wave 1 is now live
- `node scripts/doctor.cjs --acceptance` and all three build check gates are green
- The Dev-Research Compositing mirror is either filed or its refusal is stated plainly with the content preserved
- The routed-in RCA carries an accurate forward pointer and was not falsely resolved
- All four forward-routed observations and all three phase-level decisions are recorded
- Zero em-dashes in every touched file
- Zero diff against Phase 236's territory and against the v1.17.0 room-resolver fence
</success_criteria>

<output>
Create `.planning/phases/237-reach-mechanism/237-08-SUMMARY.md` when done.

`.planning/` is gitignored: commit the SUMMARY with `git add -f .planning/phases/237-reach-mechanism/237-08-SUMMARY.md`. `.planning/debug/` IS tracked, so the RCA edit from Task 3 Part B commits normally.

Record in the SUMMARY: the pre-fix RED showing `quality: 'high'` with an absent artifact, the post-fix GREEN with the real exit code and artifact path, the live mutation re-check captures, the full `bash tests/run-all-237.sh` output showing zero failures and zero skips, the research-mirror outcome (filed or refused, with content preserved on refusal), all four forward-routed observations, and all three phase-level decisions.

Commit messages: `test(237-08): add end-to-end approve-to-execute gate with log-only mutation`, `fix(237-08): wire chain_run to the real two-tier dispatcher and delete the log-only stub`, `docs(237-08): file Phase 237 findings and update the routed-in RCA forward pointer`.
</output>
