---
phase: 122-workflow-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/score-innovation.md
  - commands/whitespace.md
  - commands/explore-domains.md
  - commands/explore-trends.md
  - commands/macro-trends.md
  - commands/research.md
  - commands/think-hats.md
  - commands/persona.md
  - commands/hat-briefing.md
  - commands/scenario-plan.md
  - commands/rs-fetch.md
  - commands/rs-experts.md
  - commands/rs-thesis.md
  - commands/rs-explain.md
  - commands/find-bottlenecks.md
  - commands/find-connections.md
  - commands/find-analogies.md
  - commands/compare-ventures.md
  - commands/diagnostics.md
  - commands/diagnose.md
  - commands/causal.md
  - commands/mos-reason.md
  - commands/root-cause.md
  - commands/analyze-needs.md
  - commands/user-needs.md
  - commands/validate.md
  - commands/value-proposition.md
  - commands/grade.md
  - commands/deep-grade.md
  - commands/mullins.md
  - commands/systems-thinking.md
  - commands/analyze-systems.md
  - commands/analyze-timing.md
  - commands/dominant-designs.md
  - commands/explore-futures.md
  - commands/structure-argument.md
  - commands/build-thesis.md
  - commands/build-knowledge.md
  - commands/map-unknowns.md
  - commands/beautiful-question.md
  - commands/lean-canvas.md
  - commands/pipeline.md
  - commands/act.md
  - commands/suggest-next.md
  - docs/COMMAND-FRONTMATTER.md
  - lib/workflow/command-resolver.test.cjs
  - lib/workflow/ROOM.md
  - lib/memory/command-registry.test.cjs
  - lib/memory/run-feynman-tests.cjs
  - tests/test-command-registry.cjs
  - tests/run-all-122.sh
autonomous: true
requirements: [WORKFLOW-122-01, WORKFLOW-122-03, WORKFLOW-122-10]
canon_parts: [7, 8]
must_haves:
  truths:
    - "Every algorithmic-cohort command file declares kind / frameworks / produces / inputs / autonomous_safe in frontmatter"
    - "Utility and meta commands declare kind and frameworks: [] (no false framework claims)"
    - "docs/COMMAND-FRONTMATTER.md documents the contract with the exact keys and the methodology|utility|meta enum"
    - "The Wave-0 test stubs exist, are registered in the Feynman runner, and exit 0 with the canonical stub line"
  artifacts:
    - path: "docs/COMMAND-FRONTMATTER.md"
      provides: "The /mos: command frontmatter contract (kind, frameworks[], produces, inputs[], autonomous_safe)"
      min_lines: 40
    - path: "commands/analyze-needs.md"
      provides: "Retrofitted frontmatter -- frameworks includes the Brain name for JTBD, kind methodology"
      contains: "frameworks:"
    - path: "lib/workflow/command-resolver.test.cjs"
      provides: "Wave-0 stub for the resolver test suite"
      contains: "Phase 122 Wave 0 stub"
    - path: "lib/memory/command-registry.test.cjs"
      provides: "Wave-0 stub for the registry generator + drift tripwire test suite"
      contains: "Phase 122 Wave 0 stub"
    - path: "tests/test-command-registry.cjs"
      provides: "Wave-0 stub for the scoped bash-suite registry test"
      contains: "Phase 122 Wave 0 stub"
  key_links:
    - from: "lib/memory/run-feynman-tests.cjs"
      to: "lib/workflow/command-resolver.test.cjs"
      via: "TEST_FILES[] entry"
      pattern: "command-resolver\\.test\\.cjs"
    - from: "lib/memory/run-feynman-tests.cjs"
      to: "lib/memory/command-registry.test.cjs"
      via: "TEST_FILES[] entry"
      pattern: "command-registry\\.test\\.cjs"
---

<objective>
Establish the single source of truth for the framework to command mapping: extend the `/mos:` command frontmatter schema with `kind`, `frameworks[]`, `produces`, `inputs[]`, `autonomous_safe`, document it in `docs/COMMAND-FRONTMATTER.md`, and retrofit the algorithmic command cohort FIRST (then the utility/meta tail so the registry generator in 122-02 has a complete corpus to scan). Also lands the Wave-0 test scaffold (registered in the Feynman runner plus a scoped 122 bash runner) so every downstream plan has a place to write tests.

Purpose: Reliability rule 1 -- the truth lives in ONE place (each command file's own `frameworks:` frontmatter); nothing else (no skill, no doc, no hardcoded map) may assert it. Per Canon Part 7 this EXTENDS the existing command frontmatter (the same `commands/*.md` files `lib/core/frontmatter-schemas.cjs` plus `scripts/frontmatter-schema-validator.cjs` already parse) -- it does not introduce a new metadata store.
Output: 43 retrofitted `commands/*.md` files, `docs/COMMAND-FRONTMATTER.md`, 3 Wave-0 test stubs registered, `tests/run-all-122.sh`, `lib/workflow/ROOM.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/WORKFLOW-LAYER-SPEC.md
@.planning/phases/122-workflow-layer/122-RESEARCH.md
@.planning/phases/122-workflow-layer/122-VALIDATION.md
@CLAUDE.md
@commands/analyze-needs.md
@lib/core/frontmatter-schemas.cjs
@lib/memory/run-feynman-tests.cjs

<interfaces>
The NEW frontmatter keys (Phase 122). Copy these verbatim into each retrofitted command. Source: 122-RESEARCH.md "## Code Examples -> The frontmatter contract".

Per command (added next to the existing name / description / serves_jtbd / allowed-tools / body_shape fields, NOT replacing them):
  kind: methodology | utility | meta          # methodology = real work over room graph + Brain; utility = /mos:status etc; meta = /mos:pipeline /mos:act /mos:suggest-next
  frameworks: ["exact Brain :Framework name", ...]   # [] for utility/meta. Copy the EXACT string from the Brain. "Jobs to Be Done (JTBD)" NOT "Jobs to be Done" NOT "JTBD".
  produces: "room artifact glob"              # e.g. "room/market-analysis/jtbd-analysis/*" ; null for utility/meta
  inputs: ["expected room state", ...]        # e.g. ["a customer segment defined"] ; [] is valid
  autonomous_safe: true | false               # may /mos:act run it unattended?

FEEDS_INTO-linked Brain framework names confirmed live (122-RESEARCH.md Lens 2; copy the EXACT string):
  "Beautiful Question Framework", "Domain Selection", "Jobs to Be Done (JTBD)", "Six Thinking Hats",
  "PWS Value Proposition", "Cynefin Framework", "Systems Thinking", "Root Cause Analysis",
  "S-Curve Analysis", "Lean Canvas", "Reverse Salient Analysis", "Wicked Problem Detection Framework",
  "Scenario Planning", "Red Teaming", "PWS Triple Validation Compass", "Knowns and Unknowns Matrix Framework",
  "The Pyramid Principle", "MECE", "HSI Semantic Surprise Analysis Assistant"
For any command whose framework is NOT in that short list, use the cleanest canonical guess -- the 122-02 generator `--check` mode plus the `data/framework-names.json` snapshot FAIL the build with the list of close matches, so wrong guesses are caught, not committed.

The Wave-0 stub contract (each of the 3 new test files):
  - exits 0
  - prints exactly:  Phase 122 Wave 0 stub - to be implemented by plan 122-NN   (HYPHEN, not em-dash)
  - the downstream plan (122-02 / 122-03 / 122-05) swaps the implementation in WITHOUT changing the registered path.

TEST_FILES[] registration target: lib/memory/run-feynman-tests.cjs (the const array near the top where every other *.test.cjs is listed as path.join(REPO_ROOT, ...)).
Scoped bash runner: tests/run-all-122.sh -- mirror tests/run-all-956.sh exactly (bash only, no emoji, no em-dashes, per-suite PASS/FAIL loop, exit non-zero if any failed). Leave tests/run-all.sh untouched (it auto-discovers tests/test-*.sh; the Feynman runner is the durable gate).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 test scaffold -- 3 stubs registered in the Feynman runner + a scoped 122 bash runner + lib/workflow/ROOM.md</name>
  <read_first>
    - lib/memory/run-feynman-tests.cjs (the TEST_FILES const array -- copy the path.join pattern)
    - .planning/REQUIREMENTS.md "JTBDCONS-104-05" (the canonical Wave-0 stub pattern: each stub exits 0 with the "Phase NNN Wave 0 stub - to be implemented by plan NNN-NN" line, HYPHEN not em-dash; downstream plans swap implementations without changing paths)
    - tests/run-all-956.sh (the scoped-runner template -- bash only, no emoji, no em-dashes; mirror its SHELL_SUITES/CJS_SUITES + PASS/FAIL loop structure)
    - .planning/phases/122-workflow-layer/122-VALIDATION.md "## Wave 0 Requirements"
    - lib/hmi/ROOM.md (the lib-level ICM Layer 0 identity-file pattern -- lib/workflow/ROOM.md is the analogue; MINTO.md not required at lib level)
  </read_first>
  <action>
    Create three stub test files, each a short CJS file that prints the line `Phase 122 Wave 0 stub - to be implemented by plan 122-NN` (HYPHEN, not em-dash) and `process.exit(0)`:
      - lib/workflow/command-resolver.test.cjs   (filled by plan 122-03)
      - lib/memory/command-registry.test.cjs     (filled by plan 122-02)
      - tests/test-command-registry.cjs          (filled by plan 122-02; the node entrypoint the bash runner targets)
    Register the two `lib/` stubs in `lib/memory/run-feynman-tests.cjs` TEST_FILES[]: append `path.join(REPO_ROOT, 'lib', 'workflow', 'command-resolver.test.cjs')` and `path.join(REPO_ROOT, 'lib', 'memory', 'command-registry.test.cjs')`, matching the existing formatting with a one-line comment each.
    Create `tests/run-all-122.sh` mirroring `tests/run-all-956.sh` exactly (bash only, `set -uo pipefail`, per-suite PASS/FAIL loop, summary, exit non-zero if any failed). Its CJS_SUITES list = `test-command-registry.cjs` (more are appended by 122-03 and 122-05). Header comment notes which suites are RED-by-design until their owning plan lands.
    Create `lib/workflow/ROOM.md` (ICM Layer 0 identity for the new dir per CLAUDE.md decision #15) -- a short identity file naming Phase 122 as the founding phase and the resolver as the contained surface (mirror lib/hmi/ROOM.md tone). MINTO.md is NOT required at lib level.
    NO em-dashes anywhere. NO emoji.
  </action>
  <verify>
    <automated>node lib/workflow/command-resolver.test.cjs && node lib/memory/command-registry.test.cjs && node tests/test-command-registry.cjs && grep -q "command-resolver.test.cjs" lib/memory/run-feynman-tests.cjs && grep -q "command-registry.test.cjs" lib/memory/run-feynman-tests.cjs && bash tests/run-all-122.sh && test -f lib/workflow/ROOM.md</automated>
  </verify>
  <acceptance_criteria>
    - `node lib/workflow/command-resolver.test.cjs` exits 0 and stdout contains "Phase 122 Wave 0 stub"
    - `node lib/memory/command-registry.test.cjs` exits 0 and stdout contains "Phase 122 Wave 0 stub"
    - `node tests/test-command-registry.cjs` exits 0 and stdout contains "Phase 122 Wave 0 stub"
    - `grep -c "command-resolver.test.cjs" lib/memory/run-feynman-tests.cjs` is at least 1 AND `grep -c "command-registry.test.cjs" lib/memory/run-feynman-tests.cjs` is at least 1
    - `bash tests/run-all-122.sh` exits 0
    - `test -f lib/workflow/ROOM.md` is true
    - `grep -rlP "\x{2014}" lib/workflow/ tests/run-all-122.sh tests/test-command-registry.cjs lib/memory/command-registry.test.cjs lib/workflow/command-resolver.test.cjs` returns nothing (no em-dash U+2014)
  </acceptance_criteria>
  <done>Three stub test files exist and exit 0 with the canonical line; the two lib stubs are in TEST_FILES[]; tests/run-all-122.sh passes; lib/workflow/ROOM.md exists.</done>
</task>

<task type="auto">
  <name>Task 2: Document the frontmatter contract -- docs/COMMAND-FRONTMATTER.md</name>
  <read_first>
    - .planning/WORKFLOW-LAYER-SPEC.md "### Phase 1 -- Frontmatter contract + retrofit" (the exact field list)
    - .planning/phases/122-workflow-layer/122-RESEARCH.md "## Code Examples -> The frontmatter contract (what each commands/*.md declares)" (the methodology + utility/meta YAML examples -- copy them verbatim)
    - skills/ui-system/SKILL.md (the doc this sits next to -- match its tone; this is the command-side analogue)
    - commands/analyze-needs.md + commands/pipeline.md (the before-state of a methodology vs a meta command)
  </read_first>
  <action>
    Create `docs/COMMAND-FRONTMATTER.md`. Sections:
      1. Why this exists -- one paragraph: `frameworks:` is the SOLE place the framework-to-command mapping is declared (reliability rule 1); `data/command-registry.json` is generated from it; the pre-commit / Feynman tripwire fails the build on drift or an unresolvable framework name. Commands NEVER enter the Brain (Canon Part 8) -- the registry is plugin-local, validated AGAINST Brain framework names, never written back.
      2. The five new keys -- a table: `kind` (enum methodology|utility|meta, required), `frameworks` (array of exact Brain :Framework names; [] for utility/meta; required), `produces` (room artifact glob string or null; required), `inputs` (array of expected-room-state strings; [] valid; required), `autonomous_safe` (boolean; required -- may /mos:act run it unattended).
      3. The two YAML examples (methodology = analyze-needs; meta = pipeline) -- verbatim from 122-RESEARCH.md.
      4. Picking the right framework name -- the FEEDS_INTO-linked-name rule: copy the EXACT string from the Brain (cross-reference `data/framework-names.json` once 122-02 ships it); near-duplicates exist ("JTBD" vs "Jobs to Be Done (JTBD)" vs "Jobs-to-be-Done") -- a wrong string FAILS the build with the list of close matches. Note `frameworks: ["Amazon"]` (a junk :Framework node) MUST fail the tripwire (the allowlist is the FEEDS_INTO-linked subset of about 105 names, not all 748 nodes).
      5. The cohort-first retrofit rule -- the algorithmic cohort (HSI / whitespace / explore-domains / research+think-hats / rs-* / find-* / diagnostics / scoring / systems / argument-structure) gets retrofitted FIRST; utility/meta get `frameworks: []` and are not on the critical path.
    Add a forward-reference: `docs/THE-BRAIN.md` and `skills/pws-methodology/SKILL.md` point here after Phase 122.
    NO em-dashes (hyphens only). NO emoji.
  </action>
  <verify>
    <automated>test -f docs/COMMAND-FRONTMATTER.md && grep -q "autonomous_safe" docs/COMMAND-FRONTMATTER.md && grep -q "frameworks" docs/COMMAND-FRONTMATTER.md && grep -q "Canon Part 8" docs/COMMAND-FRONTMATTER.md && grep -q "data/command-registry.json" docs/COMMAND-FRONTMATTER.md && ! grep -lP "\x{2014}" docs/COMMAND-FRONTMATTER.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/COMMAND-FRONTMATTER.md` exists and is at least 40 lines
    - It contains the literal strings `kind`, `frameworks`, `produces`, `inputs`, `autonomous_safe`, `methodology | utility | meta`, `Canon Part 8`, `data/command-registry.json`, `data/framework-names.json`
    - It contains both YAML examples (a `kind: methodology` block and a `kind: meta` block)
    - `grep -lP "\x{2014}" docs/COMMAND-FRONTMATTER.md` returns nothing (no em-dash U+2014)
  </acceptance_criteria>
  <done>docs/COMMAND-FRONTMATTER.md documents all five keys, the enum, the two examples, the exact-Brain-name rule, and the Canon Part 8 boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Retrofit the algorithmic cohort frontmatter FIRST, then the utility/meta tail</name>
  <read_first>
    - docs/COMMAND-FRONTMATTER.md (just written -- the contract)
    - .planning/WORKFLOW-LAYER-SPEC.md "## CRITICAL PATH -- the algorithmic commands go first" (the cohort list with canonical command names)
    - .planning/phases/122-workflow-layer/122-RESEARCH.md "## Lens 4: The Three Personas" (the per-persona cohort to framework-anchor mapping -- use these to fill `frameworks:` for the rs-*, find-*, scoring, diagnostics commands)
    - commands/analyze-needs.md, commands/value-proposition.md (note: `value-proposition.md` declares `name: validate-proposition` -- the command slug is `/mos:validate-proposition`, NOT `/mos:value-proposition`; the spec flags this name mismatch; the registry will surface the real slug; do NOT rename the command, just add the frontmatter)
    - lib/core/frontmatter-schemas.cjs + scripts/frontmatter-schema-validator.cjs (the hand-rolled parser the generator will use -- keep YAML simple: `key: value`, `key: ["a","b"]`, or `key:` plus dash list)
  </read_first>
  <action>
    For EACH command file listed in this plan's `files_modified` (the 43 algorithmic-cohort + utility/meta files), open it and ADD the five new keys to the YAML frontmatter (after the existing `serves_jtbd:` line, before `allowed-tools:` -- keep all existing keys byte-identical). Order: do the algorithmic cohort FIRST (the ~40 methodology files), then `pipeline.md`, `act.md`, `suggest-next.md` (the meta tail).

    Algorithmic cohort (kind: methodology) -- representative fills (use the persona-lens anchors for the rest; copy the EXACT Brain string; for a name not in the short list in 122-RESEARCH.md, pick the cleanest canonical form -- the 122-02 build `--check` will catch a wrong one):
      - analyze-needs.md      -> kind: methodology, frameworks: ["Jobs to Be Done (JTBD)"], produces: "room/market-analysis/jtbd-analysis/*", inputs: ["a customer segment defined"], autonomous_safe: true
      - beautiful-question.md -> kind: methodology, frameworks: ["Beautiful Question Framework"], produces: "room/problem-definition/beautiful-question/*", inputs: [], autonomous_safe: true
      - explore-domains.md    -> kind: methodology, frameworks: ["Domain Selection"], produces: "room/problem-definition/domain-decomposition/*", inputs: [], autonomous_safe: true
      - think-hats.md         -> kind: methodology, frameworks: ["Six Thinking Hats"], produces: "room/**/six-hats/*", inputs: [], autonomous_safe: true
      - persona.md            -> kind: methodology, frameworks: ["Six Thinking Hats"], produces: "room/team/ai-personas/*", inputs: [], autonomous_safe: true
      - hat-briefing.md       -> kind: methodology, frameworks: ["Six Thinking Hats"], produces: "room/**/hat-briefing/*", inputs: [], autonomous_safe: false
      - scenario-plan.md      -> kind: methodology, frameworks: ["Scenario Planning"], produces: "room/**/scenarios/*", inputs: [], autonomous_safe: true
      - research.md           -> kind: methodology, frameworks: ["Hypothesis-Driven Problem Solving"], produces: "room/**/research/*", inputs: [], autonomous_safe: true
      - whitespace.md         -> kind: methodology, frameworks: ["HSI Semantic Surprise Analysis Assistant"], produces: "room/opportunity-bank/whitespace/*", inputs: [], autonomous_safe: true
      - score-innovation.md   -> kind: methodology, frameworks: ["HSI Semantic Surprise Analysis Assistant"], produces: "room/opportunity-bank/hsi-scores/*", inputs: [], autonomous_safe: true
      - find-bottlenecks.md   -> kind: methodology, frameworks: ["Reverse Salient Analysis"], produces: "room/**/reverse-salients/*", inputs: [], autonomous_safe: true
      - rs-fetch.md / rs-experts.md / rs-thesis.md / rs-explain.md -> kind: methodology, frameworks: ["Reverse Salient Analysis"], produces: "room/**/rs-*/*", inputs: [], autonomous_safe: true  (rs-explain may be autonomous_safe: false if it is a synthesis step)
      - find-connections.md / find-analogies.md -> kind: methodology, frameworks: ["SAPPhIRE"] / ["TRIZ"] (cleanest canonical), produces: "room/**/analogies/*", inputs: [], autonomous_safe: true
      - compare-ventures.md   -> kind: methodology, frameworks: ["PWS Triple Validation Compass"], produces: "room/competitive-analysis/comparison/*", inputs: [], autonomous_safe: true
      - diagnostics.md        -> kind: methodology, frameworks: ["HSI Semantic Surprise Analysis Assistant"], produces: "room/**/diagnostics/*", inputs: [], autonomous_safe: true
      - diagnose.md           -> kind: methodology, frameworks: ["Problem Definition Transformation Framework"], produces: "room/problem-definition/diagnosis/*", inputs: [], autonomous_safe: true
      - causal.md             -> kind: methodology, frameworks: ["Root Cause Analysis"], produces: "room/**/causal/*", inputs: [], autonomous_safe: true
      - mos-reason.md         -> kind: methodology, frameworks: ["The Pyramid Principle"], produces: "room/**/reasoning/*", inputs: [], autonomous_safe: true
      - root-cause.md         -> kind: methodology, frameworks: ["Root Cause Analysis"], produces: "room/**/root-cause/*", inputs: [], autonomous_safe: true
      - user-needs.md         -> kind: methodology, frameworks: ["Jobs to Be Done (JTBD)"], produces: "room/market-analysis/user-needs/*", inputs: [], autonomous_safe: true
      - validate.md           -> kind: methodology, frameworks: ["Jobs to Be Done (JTBD)"], produces: "room/**/validation/*", inputs: ["a customer segment defined"], autonomous_safe: true
      - value-proposition.md  -> kind: methodology, frameworks: ["PWS Value Proposition"], produces: "room/business-model/value-proposition/*", inputs: [], autonomous_safe: true  (name: validate-proposition stays; the registry surfaces /mos:validate-proposition)
      - grade.md              -> kind: methodology, frameworks: ["PWS Triple Validation Compass"], produces: "room/**/grades/*", inputs: ["at least one room section populated"], autonomous_safe: true
      - deep-grade.md         -> kind: methodology, frameworks: ["PWS Triple Validation Compass"], produces: "room/**/deep-grades/*", inputs: ["3+ room sections populated"], autonomous_safe: true
      - mullins.md            -> kind: methodology, frameworks: ["Mullins 7-Domains"] (cleanest canonical), produces: "room/**/mullins/*", inputs: [], autonomous_safe: true
      - systems-thinking.md / analyze-systems.md -> kind: methodology, frameworks: ["Systems Thinking"], produces: "room/**/systems/*", inputs: [], autonomous_safe: true
      - analyze-timing.md     -> kind: methodology, frameworks: ["S-Curve Analysis"], produces: "room/**/timing/*", inputs: [], autonomous_safe: true
      - dominant-designs.md   -> kind: methodology, frameworks: ["Dominant Design"] (Utterback-Abernathy; cleanest canonical), produces: "room/**/dominant-designs/*", inputs: [], autonomous_safe: true
      - explore-futures.md / explore-trends.md / macro-trends.md -> kind: methodology, frameworks: ["Scenario Planning"] / ["S-Curve Analysis"] / ["PEST Analysis"] (cleanest canonical), produces: "room/**/trends/*", inputs: [], autonomous_safe: true
      - structure-argument.md -> kind: methodology, frameworks: ["The Pyramid Principle", "MECE"], produces: "room/**/argument/*", inputs: [], autonomous_safe: true
      - build-thesis.md       -> kind: methodology, frameworks: ["PWS Value Proposition"], produces: "room/**/thesis/*", inputs: [], autonomous_safe: true
      - build-knowledge.md    -> kind: methodology, frameworks: ["DIKW"] (cleanest canonical), produces: "room/**/knowledge/*", inputs: [], autonomous_safe: true
      - map-unknowns.md       -> kind: methodology, frameworks: ["Knowns and Unknowns Matrix Framework"], produces: "room/**/unknowns/*", inputs: [], autonomous_safe: true
      - lean-canvas.md        -> kind: methodology, frameworks: ["Lean Canvas"], produces: "room/business-model/lean-canvas/*", inputs: [], autonomous_safe: true

    Meta tail (kind: meta):
      - pipeline.md     -> kind: meta, frameworks: [], produces: null, inputs: [], autonomous_safe: false
      - act.md          -> kind: meta, frameworks: [], produces: null, inputs: [], autonomous_safe: false
      - suggest-next.md -> kind: meta, frameworks: [], produces: null, inputs: [], autonomous_safe: false

    Keep every existing frontmatter key byte-identical. Add ONLY the five new keys. NO em-dashes in any added text (hyphens only). NO emoji. Do NOT touch the command body below the `---`.
  </action>
  <verify>
    <automated>for f in commands/analyze-needs.md commands/beautiful-question.md commands/explore-domains.md commands/pipeline.md commands/act.md commands/suggest-next.md commands/score-innovation.md commands/whitespace.md commands/structure-argument.md commands/rs-fetch.md commands/value-proposition.md; do grep -q "^kind:" "$f" && grep -q "^frameworks:" "$f" && grep -q "^autonomous_safe:" "$f" || { echo "MISSING new keys in $f"; exit 1; }; done; grep -q 'kind: methodology' commands/analyze-needs.md && grep -q 'kind: meta' commands/pipeline.md && grep -q 'Jobs to Be Done (JTBD)' commands/analyze-needs.md && grep -q 'frameworks: \[\]' commands/pipeline.md</automated>
  </verify>
  <acceptance_criteria>
    - For each of analyze-needs / beautiful-question / explore-domains / pipeline / act / suggest-next / score-innovation / whitespace / structure-argument / rs-fetch / value-proposition: `grep -q "^kind:"` AND `grep -q "^frameworks:"` AND `grep -q "^autonomous_safe:"` all succeed
    - `grep -q 'kind: methodology' commands/analyze-needs.md` succeeds AND `grep -q 'kind: meta' commands/pipeline.md` succeeds
    - `grep -q 'Jobs to Be Done (JTBD)' commands/analyze-needs.md` succeeds
    - `grep -q 'frameworks: \[\]' commands/pipeline.md` succeeds (meta declares empty frameworks)
    - All pre-existing frontmatter keys in each touched file are unchanged (e.g. `grep -q "^name: analyze-needs" commands/analyze-needs.md` still succeeds)
    - `git diff --stat` shows exactly the 43 command files in `files_modified` touched (no others)
    - `grep -rlP "\x{2014}" commands/` returns nothing (no em-dash introduced)
  </acceptance_criteria>
  <done>All 43 command files in files_modified carry the five new frontmatter keys; the algorithmic cohort declares kind: methodology with a non-empty frameworks list; the meta tail declares kind: meta with frameworks: []; no command body changed.</done>
</task>

</tasks>

<verification>
- `node lib/memory/run-feynman-tests.cjs` runs to completion (the 2 new stubs pass).
- `bash tests/run-all-122.sh` exits 0.
- `node tests/test-command-registry.cjs` exits 0 (stub).
- Every command file in `files_modified` parses cleanly with the existing hand-rolled frontmatter parser (`node scripts/frontmatter-schema-validator.cjs` on a sampled file does not error).
- `docs/COMMAND-FRONTMATTER.md` exists and documents all five keys.
- `grep -rlP "\x{2014}" commands/ docs/COMMAND-FRONTMATTER.md lib/workflow/ tests/run-all-122.sh` returns nothing.
</verification>

<success_criteria>
- The five new frontmatter keys (`kind`, `frameworks`, `produces`, `inputs`, `autonomous_safe`) are present on all 43 command files in `files_modified`, with the algorithmic cohort retrofitted first (kind: methodology, non-empty frameworks) and the meta tail last (kind: meta, frameworks: []).
- `docs/COMMAND-FRONTMATTER.md` is the documented contract, referencing the Canon Part 8 boundary and the FEEDS_INTO-linked-name rule.
- The Wave-0 test scaffold exists: `lib/workflow/command-resolver.test.cjs`, `lib/memory/command-registry.test.cjs`, `tests/test-command-registry.cjs` (all passing stubs), the two `lib/` stubs registered in `lib/memory/run-feynman-tests.cjs` TEST_FILES[], and `tests/run-all-122.sh` mirroring `tests/run-all-956.sh`.
- `lib/workflow/ROOM.md` exists (ICM Layer 0 identity for the new dir).
- No command body changed; no em-dashes introduced; no new npm dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/122-workflow-layer/122-01-SUMMARY.md`
</output>
