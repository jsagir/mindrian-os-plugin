---
phase: 122-workflow-layer
plan: 04
type: execute
wave: 4
depends_on: ["122-03"]
files_modified:
  - lib/core/framework-chain-composer.cjs
  - commands/suggest-next.md
  - commands/pipeline.md
  - commands/act.md
  - scripts/suggest-next-command.cjs
  - scripts/pipeline-command.cjs
  - scripts/act-command.cjs
  - skills/pws-methodology/SKILL.md
  - skills/brain-connector/SKILL.md
  - lib/memory/suggest-next-workflow.test.cjs
  - lib/memory/navigation-hook-resolver.test.cjs
  - lib/memory/run-feynman-tests.cjs
  - tests/run-all-122.sh
autonomous: true
requirements: [WORKFLOW-122-06, WORKFLOW-122-07, WORKFLOW-122-08, WORKFLOW-122-10]
canon_parts: [3, 4, 7, 8]
must_haves:
  truths:
    - "framework-chain-composer.proposeNextFramework() resolves the command for the next framework via command-resolver.commandsForFramework(), not via mapFrameworkToCommandSlug -- and degrades to command:null (no /mos: yet) when there is none"
    - "proposeNextFramework gains a composeWorkflow multi-step path so a multi-hop FEEDS_INTO chain surfaces as offer_next_step.workflow, flowing unchanged into offer-presenter / shape-f1-renderer"
    - "/mos:suggest-next in a room with a known ProblemType returns a COMMAND SEQUENCE (resolver-composed), not just a framework list"
    - "/mos:pipeline gains --from-problem-type <x> / --from-framework <x>: Brain-derive the chain (recommender), compose commands (resolver), run in sequence"
    - "/mos:act --chain runs the composed workflow but calls validateChainAutonomy first and stops at the first non-autonomous_safe step with a needs-you-here gate"
    - "The pws-methodology and brain-connector skills route framework-to-command through the resolver -- no skill names a /mos: from memory"
  artifacts:
    - path: "lib/core/framework-chain-composer.cjs"
      provides: "proposeNextFramework() with the mapFrameworkToCommandSlug call swapped for command-resolver.commandsForFramework() + a composeWorkflow multi-step path"
      contains: "command-resolver"
    - path: "scripts/suggest-next-command.cjs"
      provides: "/mos:suggest-next renders the recommended framework chain AND its resolver-composed command sequence"
      contains: "command-resolver"
    - path: "lib/memory/suggest-next-workflow.test.cjs"
      provides: "Integration test: a fixture room with a ProblemType -> /mos:suggest-next returns a command sequence; /mos:act --chain stops at the first non-autonomous step"
      min_lines: 40
  key_links:
    - from: "lib/core/framework-chain-composer.cjs"
      to: "lib/workflow/command-resolver.cjs"
      via: "require + commandsForFramework / composeWorkflow inside proposeNextFramework"
      pattern: "command-resolver"
    - from: "scripts/act-command.cjs"
      to: "lib/workflow/command-resolver.cjs"
      via: "composeWorkflow + validateChainAutonomy in --chain mode"
      pattern: "validateChainAutonomy"
    - from: "scripts/pipeline-command.cjs"
      to: "lib/brain/chain-recommender.cjs"
      via: "recommendFrameworkChain in --from-problem-type / --from-framework mode"
      pattern: "chain-recommender"
---

<objective>
Route everything through the resolver: the navigation engine (the ONE surgical edit -- swap `mapFrameworkToCommandSlug()` in `lib/core/framework-chain-composer.cjs proposeNextFramework()` for `command-resolver.commandsForFramework()`, add a `composeWorkflow` multi-step path so a chain surfaces as `offer_next_step.workflow`), `/mos:suggest-next` (render the recommended framework chain AND its command sequence), `/mos:pipeline` (new `--from-problem-type <x>` / `--from-framework <x>` -> recommender -> resolver -> run in sequence), `/mos:act --chain` (run the composed workflow but `validateChainAutonomy` first and stop at the first non-`autonomous_safe` step with a "needs you here" gate), and the `pws-methodology` + `brain-connector` skills (route framework-to-command through the resolver -- no skill names a `/mos:` from memory).

Purpose: Reliability rule 3 (the resolver is the only door -- ALL of them go through it) + rule 4 (the trigger is the hook, not the model -- the navigation engine computes the invocation; Larry is the voice). Per Canon Part 7 this is the consolidation: it does NOT re-implement intent detection (that is `scripts/intent-classifier.cjs` on `UserPromptSubmit`, already there), does NOT add a new hook, does NOT touch the operator-gating (the engine already gates `offer_next_step` by operator -- quiet under JUST_TALK -- and that is preserved), does NOT touch the presenter format (`offer-presenter.cjs` / `shape-f1-renderer.cjs` -- the workflow flows into them unchanged); it swaps ONE internal call and adds ONE multi-step path. Per Canon Part 3 the resolver output is the `offer_next_step` that becomes the F.0/F.1 accept/reject/defer Decision Gate (the `--chain` autonomy gates are the "human confirms" clause made literal). Per Canon Part 4 the navigation hook's command suggestion is now a deterministic graph+registry lookup, not a model recall. Per Canon Part 8 nothing here writes a command to the Brain; the `brain-connector` skill text is updated but the recommender it calls carries names + enums only.
Output: the surgical edit to `framework-chain-composer.cjs`; `/mos:suggest-next`, `/mos:pipeline`, `/mos:act` (and their `scripts/*-command.cjs`) wired through the resolver/recommender; `pws-methodology` + `brain-connector` skill prose updated to point at the resolver; two new tests registered in the Feynman runner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/WORKFLOW-LAYER-SPEC.md
@.planning/phases/122-workflow-layer/122-RESEARCH.md
@.planning/phases/122-workflow-layer/122-VALIDATION.md
@CLAUDE.md
@lib/core/framework-chain-composer.cjs
@lib/core/navigation-engine.cjs
@lib/core/offer-presenter.cjs
@lib/core/skill-activation-router.cjs
@lib/workflow/command-resolver.cjs
@lib/brain/chain-recommender.cjs
@commands/suggest-next.md
@commands/pipeline.md
@commands/act.md
@skills/pws-methodology/SKILL.md
@skills/brain-connector/SKILL.md

<interfaces>
The navigation-hook plug-in point -- the ONE surgical edit (122-RESEARCH.md "## Code Examples -> The navigation-hook plug-in point"). In `lib/core/framework-chain-composer.cjs proposeNextFramework()`, the existing slug call:
```js
//   const slug = mapFrameworkToCommandSlug(top.to);
//   const command = '/mos:' + slug;
// becomes:
const resolver = require('../workflow/command-resolver.cjs');
const cmds = resolver.commandsForFramework(top.to);
const command = cmds.length ? cmds[0] : null;          // null -> "no /mos: for [framework] yet" (degrade, not fabricate)
// ... and proposeNextFramework gains an optional composeWorkflow path so a multi-hop FEEDS_INTO
//     chain surfaces as offer_next_step.workflow = resolver.composeWorkflow([completed, top.to, ...])
// The decision struct's offer_next_step then flows unchanged into offer-presenter.cjs / shape-f1-renderer.cjs.
```
- `mapFrameworkToCommandSlug` + `FRAMEWORK_TO_COMMAND_SLUG` + `KNOWN_FRAMEWORKS` stay EXPORTED for back-compat in THIS plan (callers other than proposeNextFramework may still use them); 122-05 prunes `FRAMEWORK_TO_COMMAND_SLUG` to a pass-through. In THIS plan: change ONLY the call inside `proposeNextFramework`, and have `mapFrameworkToCommandSlug` delegate to the resolver internally (so any remaining caller also gets the resolver answer). Do NOT delete the exports yet.
- The `proposeNextFramework` return struct currently is `{ next, confidence, source:'FEEDS_INTO', command, reason, recommended_eligible }`. Add `workflow` (the `composeWorkflow([completedFramework, next, ...successors])` array) when a multi-hop chain is available; keep `command` (the single first step) for back-compat. The engine's `offer_next_step` carries both; `offer-presenter.cjs` renders the single-line "Offer: Because <reason>, try <command>." (unchanged); `shape-f1-renderer.cjs` (Phase 88.2 / 101) can render the multi-step chain as an F-selector if it chooses -- but that rendering is NOT this plan's job; this plan only ensures the data is there.

DO NOT (per 122-RESEARCH.md Anti-Patterns + Pitfall 5):
- re-implement intent classification (the engine already classifies; consume `decide()` output)
- add a second operator check anywhere (the engine gates `offer_next_step` by operator; the resolver is pure)
- loosen the presenter's consecutive-ignore suppression or one-per-turn flag; a multi-step chain offer is HEAVIER than a single-step offer -- the noise budget stays the same or tighter
- emit a command suggestion from any code path OUTSIDE the engine's gated `offer_next_step` pipeline (that is the Peddler line)

/mos:suggest-next -- the wiring. `scripts/suggest-next-command.cjs` (the script behind `commands/suggest-next.md`): read the room's `ProblemType` (and active JTBD) -> `chain-recommender.recommendFrameworkChain({ problemType, roomState })` -> `command-resolver.composeWorkflow(thatChain)` -> render BOTH the framework chain AND the command sequence (with explicit "run <framework> manually -- no /mos: for it" for `command:null` steps). Reuse the room-state reading the command already does (it has `mcp__mindrian-brain__*` tools in its frontmatter -- the recommender's Brain query is the FEEDS_INTO traversal, names+enums only). Render via the command's declared `body_shape` (or Shape E if none) per skills/ui-system/SKILL.md -- do NOT invent a format.

/mos:pipeline -- the new args. `scripts/pipeline-command.cjs`: add `--from-problem-type <x>` and `--from-framework <x>` parsing (process.argv switch, mindrian-tools style). When given: `recommendFrameworkChain({ problemType })` (or `{ currentFramework }`) -> `composeWorkflow(...)` -> run the resulting `/mos:` commands in sequence (the existing pipeline-runner machinery; for a `command:null` step, print "no /mos: for <framework> -- skipping / run manually" and continue). The acceptance criterion: `/mos:pipeline --from-problem-type ill-defined` runs a Brain-derived command chain.

/mos:act --chain -- the autonomy gate. `scripts/act-command.cjs`: in `--chain` mode, pick the framework chain for the room state via `recommendFrameworkChain` -> `composeWorkflow(...)` -> `validateChainAutonomy(workflow)`; run the steps in order, BUT before each step check `autonomous_safe`; at the FIRST step whose command is not `autonomous_safe` (or `command:null`), STOP and render a "needs you here" gate (a Shape F.0 / E action report -- "Step N is /mos:x -- this one needs your eyes. [continue] [stop]") instead of running it. The acceptance criterion: `/mos:act --chain` stops at the first non-`autonomous_safe` step.

The skill cleanup (prose -- the resolver-routing pointers; the full deletion of the dead Brain-Command-node text is 122-05's job, but the resolver pointer goes in here):
- `skills/pws-methodology/SKILL.md` line ~13: "If Brain is connected, use it for framework suggestions ... Otherwise, load `references/methodology/index.md` for the command routing index." -> replace with: framework-to-command routing goes through `lib/workflow/command-resolver.cjs` (the generated `data/command-registry.json`); Larry never names a `/mos:` from memory -- every command came back from the resolver. (The `references/methodology/index.md` table is deleted/regenerated in 122-05.)
- `skills/brain-connector/SKILL.md`: add a line in the "Brain-Powered Command Suggestions" area: "...and the command for that framework is whatever `command-resolver.commandsForFramework(<framework>)` returns -- or, if none, 'run <framework> manually -- there is no /mos: for it'." (122-05 deletes the dead "Brain has Command nodes / FOLLOWS_FRAMEWORK -> Command" prose; this plan adds the correct pointer alongside it so the skill is never the source of truth.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The surgical edit -- route framework-chain-composer.proposeNextFramework through the resolver + add the composeWorkflow path</name>
  <read_first>
    - lib/core/framework-chain-composer.cjs (the WHOLE file -- proposeNextFramework, mapFrameworkToCommandSlug, FRAMEWORK_TO_COMMAND_SLUG, KNOWN_FRAMEWORKS, module.exports)
    - lib/core/navigation-engine.cjs (decide() -> decision struct with offer_next_step -- how proposeNextFramework's output reaches offer_next_step)
    - lib/core/offer-presenter.cjs (presentOffer -- the "Offer: Because <reason>, try <command>." format; consecutive-ignore suppression; one-per-turn flag -- all UNCHANGED)
    - lib/workflow/command-resolver.cjs (commandsForFramework / composeWorkflow -- from 122-03)
    - .planning/phases/122-workflow-layer/122-RESEARCH.md "## Code Examples -> The navigation-hook plug-in point" + "## Architecture Patterns -> Pattern 3" (the exact swap; do NOT re-implement intent detection; do NOT add an operator check)
  </read_first>
  <action>
    In `lib/core/framework-chain-composer.cjs`:
      1. Inside `proposeNextFramework(completedFramework, edges)`, where it currently does `const slug = mapFrameworkToCommandSlug(top.to); const command = '/mos:' + slug;`, replace with `const resolver = require('../workflow/command-resolver.cjs'); const cmds = resolver.commandsForFramework(top.to); const command = cmds.length ? cmds[0] : null;` (degrade to null -- the consumer/presenter prints "no /mos: for <framework> yet"). Adjust `reason` text if it assumed a command always exists.
      2. Add the multi-step path: when there is a multi-hop FEEDS_INTO chain available from `completedFramework` (the composer already parses `framework_chain_predictions` -- take `completedFramework`, then `top.to`, then any further FEEDS_INTO successors up to depth ~3), set `workflow = resolver.composeWorkflow([completedFramework, top.to, ...successors])` on the return struct (alongside the existing `command` for back-compat). When only a single next framework is available, `workflow` is the 2-element `composeWorkflow([completedFramework, top.to])` or omitted -- either is fine; the engine's `offer_next_step` carries whatever is there.
      3. Make `mapFrameworkToCommandSlug(name)` delegate internally to the resolver: `const cmds = require('../workflow/command-resolver.cjs').commandsForFramework(name); if (cmds.length) return cmds[0].replace(/^\/mos:/, ''); /* fall back to the old FRAMEWORK_TO_COMMAND_SLUG lookup for anything the registry does not know yet */`. Keep `FRAMEWORK_TO_COMMAND_SLUG` and `KNOWN_FRAMEWORKS` EXPORTED (122-05 prunes them) -- do NOT delete the exports here.
    Do NOT touch `navigation-engine.cjs`, `offer-presenter.cjs`, `skill-activation-router.cjs`, `intent-classifier.cjs`, `hooks/hooks.json`, or anything operator-related. Do NOT add a new hook.
    Create a new test `lib/memory/navigation-hook-resolver.test.cjs`: assert that `proposeNextFramework` (called with a known completed framework + synthetic FEEDS_INTO edges) returns a struct whose `command` is what `command-resolver.commandsForFramework(next)[0]` returns (or `null` if none), and whose `workflow` (when present) is a `composeWorkflow`-shaped array; assert that for a command-less next framework `command === null` (degrade, not fabricate); assert `framework-chain-composer.cjs` source now `require`s `../workflow/command-resolver`. Register it in `lib/memory/run-feynman-tests.cjs` TEST_FILES[]; add to `tests/run-all-122.sh`.
    NO em-dashes. NO emoji.
  </action>
  <verify>
    <automated>node lib/memory/navigation-hook-resolver.test.cjs && grep -q "command-resolver" lib/core/framework-chain-composer.cjs && grep -q "composeWorkflow" lib/core/framework-chain-composer.cjs && grep -q "navigation-hook-resolver.test.cjs" lib/memory/run-feynman-tests.cjs && node lib/memory/run-feynman-tests.cjs 2>&1 | grep -q "navigation-hook-resolver"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "command-resolver" lib/core/framework-chain-composer.cjs` is at least 1 (the require is added)
    - `grep -c "composeWorkflow" lib/core/framework-chain-composer.cjs` is at least 1 (the multi-step path)
    - `node -e "const c=require('./lib/core/framework-chain-composer.cjs'); process.exit(typeof c.proposeNextFramework==='function' && typeof c.mapFrameworkToCommandSlug==='function' && c.FRAMEWORK_TO_COMMAND_SLUG ? 0 : 1)"` exits 0 (exports preserved)
    - `node lib/memory/navigation-hook-resolver.test.cjs` exits 0
    - `grep -c "navigation-hook-resolver.test.cjs" lib/memory/run-feynman-tests.cjs` is at least 1
    - `git diff lib/core/navigation-engine.cjs lib/core/offer-presenter.cjs hooks/hooks.json` is empty (no change to the engine, presenter, or hooks)
    - `grep -lP "\x{2014}" lib/core/framework-chain-composer.cjs lib/memory/navigation-hook-resolver.test.cjs` returns nothing
  </acceptance_criteria>
  <done>proposeNextFramework resolves the command via the resolver (degrading to null), gains a composeWorkflow workflow field, and mapFrameworkToCommandSlug delegates to the resolver; the engine/presenter/hooks are untouched; the test is registered and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire /mos:suggest-next, /mos:pipeline --from-problem-type/--from-framework, /mos:act --chain through the resolver/recommender</name>
  <read_first>
    - commands/suggest-next.md + scripts/suggest-next-command.cjs (the current behavior -- it already reads room state + has brain MCP tools; add the recommender->resolver compose)
    - commands/pipeline.md + scripts/pipeline-command.cjs (the existing pipeline-runner -- add the --from-problem-type / --from-framework args + the recommend->compose->run-in-sequence path)
    - commands/act.md + scripts/act-command.cjs (the existing --chain mode if any -- add validateChainAutonomy + the stop-at-first-non-autonomous gate)
    - lib/workflow/command-resolver.cjs + lib/brain/chain-recommender.cjs (from 122-03 -- composeWorkflow / validateChainAutonomy / recommendFrameworkChain)
    - skills/ui-system/SKILL.md (the body_shape rendering contract -- /mos:suggest-next and /mos:act render via their declared body_shape, not a bespoke format)
    - .planning/phases/122-workflow-layer/122-RESEARCH.md "## Lens 4: The Three Personas" (the founder / researcher / investor chain examples -- the rendered command sequence should read like those)
    - .planning/phases/122-workflow-layer/122-VALIDATION.md "## Per-Task Verification Map" (the suggest-next-workflow / pipeline-from-problem-type / act --chain test rows)
  </read_first>
  <action>
    `scripts/suggest-next-command.cjs`: after reading the room's `ProblemType` (and active JTBD if available), call `require('../lib/brain/chain-recommender.cjs').recommendFrameworkChain({ problemType, roomState })` -> `require('../lib/workflow/command-resolver.cjs').composeWorkflow(thatChain)` -> render BOTH the framework chain AND the command sequence: for each `{step, framework, command, optional}`, show `step. <framework> -> <command>` and for `command===null` show `step. <framework> -> (no /mos: for this -- run it manually)`. Render via the command's `body_shape` (Shape E if none) per ui-system. The output MUST be a command SEQUENCE, not just a framework list. Keep the existing fallback behavior (no Brain / no ProblemType) but make it framework-only-advice (degrade) -- still go through the resolver for whatever frameworks it does have.
    `scripts/pipeline-command.cjs`: add `--from-problem-type <x>` and `--from-framework <x>` to the process.argv parsing. When `--from-problem-type` is given: `recommendFrameworkChain({ problemType: x })` -> `composeWorkflow(...)` -> iterate the workflow; for each step with a non-null `command`, invoke it via the existing pipeline-step machinery; for a `command:null` step, print "no /mos: for <framework> -- run it manually; continuing" and skip. When `--from-framework <x>` is given: `recommendFrameworkChain({ currentFramework: x })` -> same. Document the two new args in `commands/pipeline.md` (the `argument-hint` and the body). The acceptance check: `/mos:pipeline --from-problem-type ill-defined` runs a Brain-derived command chain (each resolved command exists -- because the resolver only returns registered commands).
    `scripts/act-command.cjs`: in `--chain` mode, `recommendFrameworkChain({ problemType: <room ProblemType>, roomState })` -> `composeWorkflow(...)` -> `validateChainAutonomy(workflow)`; then iterate: before running step N, if `workflow[N].command === null` OR that command's `autonomous_safe !== true`, STOP and render a "needs you here" gate (Shape F.0 / E: "Chain reached step N: <command> for <framework>. This step is not autonomous_safe -- it needs your eyes. [continue] [stop the chain]") and do NOT run it; otherwise run it and continue. Document `--chain` behavior in `commands/act.md` (the `argument-hint` already lists `--chain` -- expand the body). The acceptance check: `/mos:act --chain` stops at the first non-`autonomous_safe` step (e.g. if the chain includes `/mos:hat-briefing`).
    NO em-dashes. NO emoji. Render via body_shape; do not invent a format.
  </action>
  <verify>
    <automated>grep -q "command-resolver" scripts/suggest-next-command.cjs && grep -q "chain-recommender" scripts/suggest-next-command.cjs && grep -q "from-problem-type" scripts/pipeline-command.cjs && grep -q "chain-recommender" scripts/pipeline-command.cjs && grep -q "validateChainAutonomy" scripts/act-command.cjs && grep -q "from-problem-type" commands/pipeline.md && node -c scripts/suggest-next-command.cjs && node -c scripts/pipeline-command.cjs && node -c scripts/act-command.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "command-resolver" scripts/suggest-next-command.cjs` is at least 1 AND `grep -c "chain-recommender" scripts/suggest-next-command.cjs` is at least 1
    - `grep -c "from-problem-type" scripts/pipeline-command.cjs` is at least 1 AND `grep -c "from-framework" scripts/pipeline-command.cjs` is at least 1 AND `grep -c "chain-recommender" scripts/pipeline-command.cjs` is at least 1
    - `grep -c "from-problem-type" commands/pipeline.md` is at least 1 (the arg is documented)
    - `grep -c "validateChainAutonomy" scripts/act-command.cjs` is at least 1
    - `node -c scripts/suggest-next-command.cjs && node -c scripts/pipeline-command.cjs && node -c scripts/act-command.cjs` all exit 0 (syntax-valid)
    - `grep -c "/mos:" scripts/suggest-next-command.cjs` literals are all self-references or resolver-sourced (no hardcoded methodology-command map introduced)
    - `grep -lP "\x{2014}" scripts/suggest-next-command.cjs scripts/pipeline-command.cjs scripts/act-command.cjs commands/pipeline.md commands/act.md commands/suggest-next.md` returns nothing
  </acceptance_criteria>
  <done>/mos:suggest-next renders a resolver-composed command sequence (not just frameworks); /mos:pipeline has --from-problem-type / --from-framework that recommend->compose->run; /mos:act --chain validates autonomy and stops at the first non-autonomous step; all three scripts are syntax-valid and route through the resolver/recommender.</done>
</task>

<task type="auto">
  <name>Task 3: Skill prose -- point pws-methodology and brain-connector at the resolver + the suggest-next integration test</name>
  <read_first>
    - skills/pws-methodology/SKILL.md (the "If Brain is connected ... Otherwise load references/methodology/index.md" line ~13, and the "The index enables /mos:help to recommend commands" line ~21)
    - skills/brain-connector/SKILL.md (the "Brain-Powered Command Suggestions" section, lines ~91-97 -- "Brain has Command nodes linked to Frameworks ... query brain_proactive_command ... Multi-hop: Room frameworks -> FOLLOWS_FRAMEWORK -> Command -> TRIGGERED_BY_SIGNAL ..." -- this is the dead/Part-8-violating prose; THIS plan ADDS the correct resolver pointer alongside it; 122-05 DELETES the dead text)
    - .planning/phases/122-workflow-layer/122-RESEARCH.md "## State of the Art -> Deprecated/outdated by this phase" (the list of what 122-05 prunes -- so this plan's prose changes are forward-compatible with that)
    - .planning/phases/122-workflow-layer/122-VALIDATION.md "## Per-Task Verification Map" (the suggest-next-workflow test row + the manual-only "Larry actually routing" row)
  </read_first>
  <action>
    `skills/pws-methodology/SKILL.md`: replace the "If Brain is connected, use it for framework suggestions ... Otherwise, load `references/methodology/index.md` for the command routing index." line with: "Framework-to-command routing goes through `lib/workflow/command-resolver.cjs` (the generated `data/command-registry.json`, validated against the Brain's framework names). Larry NEVER names a `/mos:` command from memory -- every command surfaced came back from the resolver. If a framework has no command yet, say so ('run <framework> manually -- there is no /mos: for it'); never invent one." Update the "The index enables /mos:help to recommend commands" line to point at the resolver / registry instead of `references/methodology/index.md`.
    `skills/brain-connector/SKILL.md`: in the "Brain-Powered Command Suggestions" area, ADD (do NOT yet delete the dead "Brain has Command nodes" prose -- 122-05 does that): "The command for a framework is whatever `command-resolver.commandsForFramework(<framework>)` returns. Commands NEVER live in the Brain (Canon Part 8) -- the Brain holds methodology (FEEDS_INTO chains); the plugin-local registry holds the framework-to-command mapping. If the resolver returns nothing, the answer is 'run <framework> manually'."
    Create the integration test `lib/memory/suggest-next-workflow.test.cjs`: build a hermetic fixture room (a `.room-root` dir + a STATE.md with a `ProblemType: ill-defined` set, via `MINDRIAN_ROOMS_HOME` env override per the Phase 95.1 pattern) -> invoke `scripts/suggest-next-command.cjs` against it (or call the recommender+resolver path directly with that room state) -> assert the output is a COMMAND SEQUENCE (contains at least one `/mos:` token AND a step-numbered framework list) and that every `/mos:` it emits exists in `data/command-registry.json` (no hallucinated command). Add a second case: a synthetic chain that includes `/mos:hat-briefing` -> `validateChainAutonomy` reports it as a blocker (the `/mos:act --chain` stop-point). Register it in `lib/memory/run-feynman-tests.cjs` TEST_FILES[]; add to `tests/run-all-122.sh`.
    NO em-dashes. NO emoji.
  </action>
  <verify>
    <automated>node lib/memory/suggest-next-workflow.test.cjs && grep -q "command-resolver" skills/pws-methodology/SKILL.md && grep -q "command-resolver" skills/brain-connector/SKILL.md && grep -q "Canon Part 8" skills/brain-connector/SKILL.md && grep -q "suggest-next-workflow.test.cjs" lib/memory/run-feynman-tests.cjs && bash tests/run-all-122.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "command-resolver" skills/pws-methodology/SKILL.md` is at least 1
    - `grep -c "command-resolver" skills/brain-connector/SKILL.md` is at least 1 AND `grep -c "Canon Part 8" skills/brain-connector/SKILL.md` is at least 1
    - `node lib/memory/suggest-next-workflow.test.cjs` exits 0 and asserts every `/mos:` it produces exists in `data/command-registry.json`
    - `grep -c "suggest-next-workflow.test.cjs" lib/memory/run-feynman-tests.cjs` is at least 1
    - `bash tests/run-all-122.sh` exits 0
    - `grep -lP "\x{2014}" skills/pws-methodology/SKILL.md skills/brain-connector/SKILL.md lib/memory/suggest-next-workflow.test.cjs` returns nothing
  </acceptance_criteria>
  <done>pws-methodology and brain-connector skill prose point at the resolver (and brain-connector cites the Canon Part 8 boundary); the suggest-next integration test proves a command sequence is produced and every command in it is registered; the test is registered and the scoped runner passes.</done>
</task>

</tasks>

<verification>
- `node lib/memory/navigation-hook-resolver.test.cjs` exits 0; `node lib/memory/suggest-next-workflow.test.cjs` exits 0.
- `grep -c "command-resolver" lib/core/framework-chain-composer.cjs` is at least 1; `grep -c "composeWorkflow" lib/core/framework-chain-composer.cjs` is at least 1.
- `git diff lib/core/navigation-engine.cjs lib/core/offer-presenter.cjs hooks/hooks.json scripts/intent-classifier.cjs` is empty (no new hook, engine/presenter untouched).
- `scripts/suggest-next-command.cjs` / `scripts/pipeline-command.cjs` / `scripts/act-command.cjs` all `node -c` clean and all route through `command-resolver` / `chain-recommender`.
- `/mos:pipeline` documents `--from-problem-type` / `--from-framework`; `/mos:act` documents `--chain` autonomy gating.
- `pws-methodology` + `brain-connector` skill prose point at the resolver; brain-connector cites Canon Part 8.
- `node lib/memory/run-feynman-tests.cjs` runs to completion; `bash tests/run-all-122.sh` exits 0.
- `grep -lP "\x{2014}" lib/core/framework-chain-composer.cjs scripts/suggest-next-command.cjs scripts/pipeline-command.cjs scripts/act-command.cjs skills/pws-methodology/ skills/brain-connector/` returns nothing.
</verification>

<success_criteria>
- The navigation engine routes framework-to-command through the resolver (the ONE surgical edit in `framework-chain-composer.proposeNextFramework`), degrades to `command: null` when there is none, and gains a `composeWorkflow` `workflow` field that flows unchanged into the presenter/F-renderer; no new hook, no engine/presenter change, no operator-check added.
- `/mos:suggest-next` returns a resolver-composed command SEQUENCE in a room with a known `ProblemType`; `/mos:pipeline --from-problem-type <x>` / `--from-framework <x>` recommend->compose->run a Brain-derived command chain; `/mos:act --chain` runs the composed workflow but stops at the first non-`autonomous_safe` (or command-less) step with a "needs you here" gate.
- `pws-methodology` and `brain-connector` skills route framework-to-command through the resolver in their prose (no skill names a `/mos:` from memory); brain-connector cites the Canon Part 8 boundary alongside the (still-to-be-deleted-in-122-05) dead prose.
- Two new tests (`navigation-hook-resolver.test.cjs`, `suggest-next-workflow.test.cjs`) registered in the Feynman runner + `tests/run-all-122.sh`; both pass; the suggest-next test asserts every emitted `/mos:` is registered (no hallucinated command).
- No new npm dependency; no em-dashes; no command string written to the Brain.
</success_criteria>

<output>
After completion, create `.planning/phases/122-workflow-layer/122-04-SUMMARY.md`
</output>
