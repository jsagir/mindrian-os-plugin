# Phase 122 -- Deferred Items

> Out-of-scope discoveries logged during execution. NOT fixed here.

- [122-01 / Task 3] `commands/doctor.md` contains a U+2014 em-dash (pre-existing; NOT in this plan's files_modified). Out of scope per the GSD SCOPE BOUNDARY rule -- the no-em-dash project rule should be swept across all `commands/*.md` in a dedicated housekeeping pass, not as a side effect of the Phase 122 frontmatter retrofit.
- [122-02 / Task 1] Working tree carried an uncommitted `M docs/testers/REGISTRY.md` and an untracked `docs/testers/aniruddh-mohan/` directory at execution start -- pre-existing, unrelated to Phase 122, NOT touched by this plan. Left as-is for whoever owns the testers-hub work to commit.
- [122-02 / Task 2] `test/84-smart-notebook-copilot.test.cjs` (TEST_FILES line 59 in `lib/memory/run-feynman-tests.cjs` -- a Phase-84 file, NOT touched by Phase 122) HANGS when run standalone (`timeout 30 node test/84-smart-notebook-copilot.test.cjs` -> exit 124). Cases 06/07/12/13 fail with `error in view rs_discoveries: no such table: main.nodes` (from `lib/core/migrations/phase-109-nodes-provenance.cjs:280` via `room-db.cjs:110`), then the process prints "ok 14" and never exits -- a dangling SQLite handle keeps the event loop alive. Because `run-feynman-tests.cjs` iterates with a blocking `spawnSync` and no per-test timeout, this hang prevents the full Feynman suite from ever reaching the registered `lib/memory/command-registry.test.cjs` (line 1208) and `lib/workflow/command-resolver.test.cjs` (line 1207). The Phase-122 command-registry suite is verified GREEN directly (`node lib/memory/command-registry.test.cjs` -> exit 0) and via the scoped runner (`bash tests/run-all-122.sh` -> exit 0). Fix needed in a Phase-84/109 housekeeping pass: (a) make the migration not error on the fixture room, OR (b) `db.close()` in the test teardown, OR (c) give `run-feynman-tests.cjs` a per-test timeout so one hung suite cannot block the whole runner. Pre-existing, out of Phase-122 scope.
