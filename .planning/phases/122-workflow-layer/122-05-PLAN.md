---
phase: 122-workflow-layer
plan: 05
type: execute
wave: 5
depends_on: ["122-04"]
files_modified:
  - lib/core/framework-chain-composer.cjs
  - lib/hmi/jtbd-taxonomy.json
  - references/methodology/index.md
  - skills/brain-connector/SKILL.md
  - docs/COMMAND-FRONTMATTER.md
  - docs/WORKFLOWS.md
  - docs/THE-BRAIN.md
  - docs/CANON-PHASE-MAP.md
  - CHANGELOG.md
  - lib/memory/workflow-layer-e2e.test.cjs
  - lib/memory/run-feynman-tests.cjs
  - tests/run-all-122.sh
autonomous: true
requirements: [WORKFLOW-122-09, WORKFLOW-122-10, WORKFLOW-122-11]
canon_parts: [7, 8, 9, 10]
must_haves:
  truths:
    - "framework-chain-composer.FRAMEWORK_TO_COMMAND_SLUG is pruned to a pass-through (or removed) -- it no longer asserts a framework-to-command mapping; the resolver is the only door"
    - "lib/hmi/jtbd-taxonomy.json:methodology_hooks is either regenerated from the registry framework_index or marked informational-only (the resolver is authoritative)"
    - "references/methodology/index.md no longer carries a hand-maintained command-routing table (regenerated from the registry, or replaced by a pointer to docs/COMMAND-FRONTMATTER.md + docs/WORKFLOWS.md)"
    - "skills/brain-connector/SKILL.md has NO Brain-Command-node prose left (no 'Brain has Command nodes', no 'brain_proactive_command', no 'FOLLOWS_FRAMEWORK -> Command') -- the live Brain has no Command label; the dead Part-8-violating text is deleted"
    - "docs/WORKFLOWS.md documents the Brain<->registry<->Larry join and the Canon Part 8 boundary; docs/THE-BRAIN.md and the CANON-PHASE-MAP point at it"
    - "An end-to-end test walks frontmatter -> registry -> resolver -> a composed chain and asserts the spec's acceptance-example composition; the Canon Part 8 zero-Brain-mutation grep sweep passes (no /mos: in any Brain-query payload builder; no Command node assertions in skills/agents/references)"
  artifacts:
    - path: "docs/WORKFLOWS.md"
      provides: "The Brain<->registry<->Larry join + the Canon Part 8 boundary (commands never enter the Brain) + the 5 reliability rules + the resolver/recommender surface"
      min_lines: 60
    - path: "lib/memory/workflow-layer-e2e.test.cjs"
      provides: "End-to-end: frontmatter -> build-command-registry --check -> resolver.composeWorkflow(acceptance example) -> assert the command sequence + null markers; plus the Canon Part 8 grep sweep"
      min_lines: 50
  key_links:
    - from: "docs/THE-BRAIN.md"
      to: "docs/WORKFLOWS.md"
      via: "a See-also / forward-reference line"
      pattern: "WORKFLOWS\\.md"
    - from: "lib/memory/workflow-layer-e2e.test.cjs"
      to: "scripts/build-command-registry.cjs"
      via: "spawnSync ... --check (the full chain start)"
      pattern: "build-command-registry"
    - from: "lib/memory/workflow-layer-e2e.test.cjs"
      to: "lib/workflow/command-resolver.cjs"
      via: "composeWorkflow(the acceptance example)"
      pattern: "composeWorkflow"
---

<objective>
Close the loop: prune every remaining hand-maintained framework-to-command map so the resolver is the ONLY door (`framework-chain-composer.FRAMEWORK_TO_COMMAND_SLUG` -> pass-through; `lib/hmi/jtbd-taxonomy.json:methodology_hooks` -> regenerated-from-registry or informational-only; `references/methodology/index.md` table -> regenerated or replaced by a pointer); DELETE the dead/Part-8-violating "Brain has Command nodes / `brain_proactive_command` / `FOLLOWS_FRAMEWORK -> Command`" prose from `skills/brain-connector/SKILL.md` (the live Brain has no `Command` label -- it is dead text that breeds the breach); ship `docs/WORKFLOWS.md` (the Brain<->registry<->Larry join + the Canon Part 8 boundary + the 5 reliability rules) and point `docs/THE-BRAIN.md` + `docs/CANON-PHASE-MAP.md` at it; add an end-to-end test that walks frontmatter -> registry -> resolver -> a composed chain (asserting the spec's acceptance example) plus the Canon Part 8 zero-Brain-mutation grep sweep; and finalize the CHANGELOG `[Unreleased] -- v1.13.0-beta.10` entry (version is already bumped; the release tag / marketplace pin / npm publish are maintainer-gated -- this plan only finalizes the CHANGELOG text and flags the maintainer steps).

Purpose: Reliability rule 1 made total (NOTHING else asserts the mapping) + rule 5 (degrade, do not fabricate -- documented). Per Canon Part 7 this is pure consolidation -- it deletes drift-class surface (three hand-maintained maps) rather than adding new code; the one new file is the e2e test + the docs. Per Canon Part 8 the `brain-connector` "Command node" prose was a latent breach in the skill text (asserting commands live in the Brain) -- it is deleted; the e2e grep sweep proves zero `/mos:` in any Brain-query payload builder and zero `Command`-node assertions anywhere. Per Canon Part 9/10 the Workflow Layer is the "Brain reasons -> Larry proposes a workflow -> human confirms" exemplar -- `docs/WORKFLOWS.md` cites it as such and `docs/CANON-PHASE-MAP.md` records it.
Output: the three map-prunings; the brain-connector deletion; `docs/WORKFLOWS.md`; `docs/THE-BRAIN.md` + `docs/CANON-PHASE-MAP.md` updates; `docs/COMMAND-FRONTMATTER.md` cross-link; the e2e test (registered in the Feynman runner); the CHANGELOG `[1.13.0-beta.10]` entry expansion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/WORKFLOW-LAYER-SPEC.md
@.planning/phases/122-workflow-layer/122-RESEARCH.md
@.planning/phases/122-workflow-layer/122-VALIDATION.md
@CLAUDE.md
@docs/MINDRIAN-CANON.md
@docs/CANON-PHASE-MAP.md
@docs/THE-BRAIN.md
@docs/COMMAND-FRONTMATTER.md
@lib/core/framework-chain-composer.cjs
@lib/hmi/jtbd-taxonomy.json
@references/methodology/index.md
@skills/brain-connector/SKILL.md
@skills/pws-methodology/SKILL.md
@data/command-registry.json
@CHANGELOG.md

<interfaces>
The three hand-maintained maps to prune (122-RESEARCH.md "## State of the Art -> Deprecated/outdated by this phase"):
1. `lib/core/framework-chain-composer.cjs:FRAMEWORK_TO_COMMAND_SLUG` (frozen table) -> pruned to a PASS-THROUGH: either delete the const entirely (and have `mapFrameworkToCommandSlug` rely solely on `command-resolver.commandsForFramework`, returning the slug stripped of `/mos:`, or `null`), OR shrink it to `{}` with a comment that the resolver is authoritative and this is kept only as an empty back-compat export. `KNOWN_FRAMEWORKS` may stay (it is a name-recognition bootstrap, not a command map) but add a comment that it is NOT the framework-to-command source. `proposeNextFramework` already routes through the resolver (122-04) -- this plan removes the residual map.
2. `lib/hmi/jtbd-taxonomy.json:entries[].methodology_hooks` -> EITHER regenerate each entry's `methodology_hooks` from `data/command-registry.json` `framework_index` (so the JSON stays useful but is derived), OR keep the field but add a top-level note `"methodology_hooks_note": "informational only; lib/workflow/command-resolver.cjs is authoritative for framework-to-command"`. The existing `tests/test-jtbd-taxonomy.cjs` schema test MUST still pass after the change (additive note or regenerated values -- not a schema break). NOTE the live mismatch flagged in 122-RESEARCH.md: `jtbd-taxonomy.json` has `/mos:value-proposition` while the command file's `name:` is `validate-proposition` -- if regenerating from the registry, this self-corrects to `/mos:validate-proposition`; if keeping the field, fix that one entry by hand.
3. `references/methodology/index.md` (the 26-row hand-maintained command-routing table loaded by `skills/pws-methodology/SKILL.md`) -> EITHER regenerate the table from `data/command-registry.json` (a small script section, or a one-off regenerate noted in the file header "GENERATED from data/command-registry.json -- do not hand-edit; regenerate with ..."), OR replace the table with a pointer: "The authoritative framework-to-command mapping is `data/command-registry.json` (generated from `commands/*.md` frontmatter; see `docs/COMMAND-FRONTMATTER.md`). For routing in conversation, Larry uses `lib/workflow/command-resolver.cjs` -- he never names a `/mos:` from memory." The `pws-methodology` SKILL.md reference to `references/methodology/index.md` (already softened in 122-04) should now point at the registry / `docs/COMMAND-FRONTMATTER.md`.

The brain-connector deletion (122-RESEARCH.md item 3 + "## State of the Art"): in `skills/brain-connector/SKILL.md`, DELETE the entire "Brain-Powered Command Suggestions" block that says "Brain has Command nodes linked to Frameworks, VentureStages, SignalTypes ... Query `brain_proactive_command` ... Multi-hop: Room frameworks -> FOLLOWS_FRAMEWORK -> Command -> TRIGGERED_BY_SIGNAL -> Signals -> RELEVANT_AT_STAGE -> Stage" (lines ~91-97). The live Brain has NO `Command` label (verified in 122-RESEARCH.md) -- this is dead text, and it is the exact class of text Canon Part 8 forbids (asserting commands live in the Brain). Replace it with the resolver pointer added in 122-04 (keep that line; delete the dead prose around it). Net result: brain-connector SKILL.md mentions commands ONLY via "`command-resolver.commandsForFramework(<framework>)` returns the command; commands never live in the Brain (Canon Part 8)".

docs/WORKFLOWS.md -- the new doc. Sections:
  1. The closed loop (one paragraph) -- navigation engine routes -> operator state machine sets the mode -> SQL graph says where you are -> the cleaned Brain (FEEDS_INTO) says what is next -> the registry says which command does it -> Larry proposes it as an F-selector -> you confirm -> /mos:act runs it -> the artifact files -> the cascade fires -> the next nudge surfaces. The Workflow Layer (Phase 122) is the registry+resolver link in that sentence.
  2. The five reliability rules (verbatim summary from the spec): single source of truth (`frameworks:` in command frontmatter); generated never hand-written + CI tripwire (`data/command-registry.json` from `scripts/build-command-registry.cjs`, `--check` in pre-commit + the Feynman runner); the resolver is the only door (`lib/workflow/command-resolver.cjs`); the trigger is the hook not the model (the navigation engine computes the invocation); degrade do not fabricate (no command -> "run it manually"; no Brain -> registry still works; no registry -> framework-only advice).
  3. The Brain<->registry<->Larry join + the Canon Part 8 boundary -- a diagram/prose: the Brain holds methodology (`Framework -[:FEEDS_INTO]-> Framework`); the plugin-local `data/command-registry.json` holds the framework-to-command mapping (generated from frontmatter, VALIDATED against the Brain's framework names at build time via a read-only query, NEVER written back); `lib/workflow/command-resolver.cjs` joins them at runtime with zero Brain calls. Commands NEVER enter the Brain -- no `Command` node, ever. A command string in a Brain-query payload is a canonical breach (the brain-boundary-scan PR gate exists for it).
  4. The surfaces -- `/mos:suggest-next` (chain + command sequence), `/mos:pipeline --from-problem-type/--from-framework`, `/mos:act --chain` (autonomy gates = "human confirms" made literal), the navigation hook (the `offer_next_step` workflow), the `pws-methodology` + `brain-connector` skills.
  5. Canon citations -- Part 7 (Reuse Before Build -- this is ~90% wiring of existing code), Part 8 (the boundary), Part 3 (the resolver feeds the Decision Gate), Part 4 (the command suggestion is deterministic graph+registry, not model recall), Part 9/10 (the "Brain reasons -> Larry proposes -> human confirms" exemplar).
docs/THE-BRAIN.md: add a "See also: docs/WORKFLOWS.md (the framework-to-command layer; commands never enter the Brain)" line; if THE-BRAIN.md anywhere implies commands live in the Brain, correct it.
docs/CANON-PHASE-MAP.md: add a row under Part 7 (Reuse Before Build) AND under Part 8 (Graph Boundary) for Phase 122 workflow-layer (shipped at v1.13.0-beta.10): "Phase 122 routes all framework-to-command maps through one resolver (Part 7 consolidation); the registry is plugin-local, validated against Brain names, never written back (Part 8); the brain-connector SKILL.md Command-node prose deleted (latent Part 8 breach in prose removed)". Also add to the "v1.13.0 milestone phases" table.

The e2e test (`lib/memory/workflow-layer-e2e.test.cjs`) -- walks the FULL chain:
  1. `spawnSync('node', ['scripts/build-command-registry.cjs', '--check'])` -> `status === 0` (frontmatter -> registry is consistent).
  2. `require('lib/workflow/command-resolver.cjs').composeWorkflow(["Beautiful Question Framework","Domain Selection","Jobs to Be Done (JTBD)"])` -> assert a 3-step workflow where `command` is filled for all three (the spec's acceptance example -- the exact slugs come from the 122-01 retrofit; assert each `command` is a non-null `/mos:` string that exists in `data/command-registry.json`). `composeWorkflow(["Red Teaming"])` -> assert `[{step:1, framework:"Red Teaming", command:null, optional:true}]` (the command-less degrade).
  3. `validateChainAutonomy(composeWorkflow(["Six Thinking Hats", "<a framework whose command is autonomous_safe:false, e.g. the one mapping to /mos:hat-briefing>"]))` -> `runnable === false` (the `/mos:act --chain` stop-point).
  4. Canon Part 8 grep sweep: `grep -rEl "/mos:[a-z-]+" lib/brain/ lib/workflow/` -> for each file, no line matches `/mos:` within ~80 chars of `brain`/`query`/`fetch`/`http`; `grep -rEl "Brain has Command|brain_proactive_command|FOLLOWS_FRAMEWORK.*Command|:Command" skills/ agents/ references/` -> empty (no Command-node assertion left anywhere); `scripts/build-command-registry.cjs` -> no write-Cypher (`/CREATE |MERGE |SET |DELETE /i`); `lib/workflow/command-resolver.cjs` -> no `require(...brain-client...)`.

The CHANGELOG entry (CLAUDE.md release process -- but DO NOT push/tag/publish; the maintainer does that): the file already has `## [Unreleased] -- v1.13.0-beta.10 (in progress)` and `.claude-plugin/plugin.json` / `package.json` already say `1.13.0-beta.10`. Expand the Unreleased/beta.10 block with a "### Added" / "### Changed" / "### Fixed" set for Phase 122: Added -- `data/command-registry.json` + `scripts/build-command-registry.cjs` + `lib/workflow/command-resolver.cjs` + `lib/brain/chain-recommender.cjs` + `docs/COMMAND-FRONTMATTER.md` + `docs/WORKFLOWS.md` + the five new `/mos:` command frontmatter keys + `/mos:pipeline --from-problem-type/--from-framework` + `/mos:act --chain` autonomy gating + the pre-commit registry-drift tripwire. Changed -- `/mos:suggest-next` returns a command sequence; `framework-chain-composer` routes through the resolver; `pws-methodology` + `brain-connector` skills point at the resolver. Fixed -- the hallucinated-command failure mode (Larry naming a non-existent or wrong command, e.g. `/mos:jtbd` instead of `/mos:analyze-needs`); the latent Canon Part 8 breach in `brain-connector` SKILL.md prose ("Brain has Command nodes" deleted). End the block with a maintainer note: "Release steps (maintainer-gated, NOT done by this phase): cut the `v1.13.0-beta.10` tag, pin the marketplace `source.ref`, `npm publish @mindrian/os` with `@next` dist-tag -- per CLAUDE.md release process + the feedback_release_lockstep_npm memory rule." Do NOT bump any version (already at beta.10). Do NOT `git tag`. Do NOT `npm publish`. Do NOT edit `marketplace.json`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prune the three hand-maintained maps + delete the brain-connector Command-node prose</name>
  <read_first>
    - lib/core/framework-chain-composer.cjs (FRAMEWORK_TO_COMMAND_SLUG, KNOWN_FRAMEWORKS, mapFrameworkToCommandSlug -- 122-04 already made mapFrameworkToCommandSlug delegate to the resolver; this plan prunes the residual table)
    - lib/hmi/jtbd-taxonomy.json (the entries[].methodology_hooks fields -- and tests/test-jtbd-taxonomy.cjs which must still pass)
    - tests/test-jtbd-taxonomy.cjs (the schema test that constrains jtbd-taxonomy.json -- read it so the prune does not break it)
    - references/methodology/index.md (the 26-row table loaded by skills/pws-methodology/SKILL.md)
    - skills/pws-methodology/SKILL.md (already points at the resolver from 122-04 -- finish the index.md reference removal)
    - skills/brain-connector/SKILL.md (the "Brain-Powered Command Suggestions" block lines ~91-97 -- DELETE the dead "Brain has Command nodes / brain_proactive_command / FOLLOWS_FRAMEWORK -> Command" prose; keep the resolver pointer 122-04 added)
    - data/command-registry.json (the source of truth -- if regenerating index.md or methodology_hooks, derive from here)
    - .planning/phases/122-workflow-layer/122-RESEARCH.md "## State of the Art -> Deprecated/outdated by this phase" (the exact prune list)
  </read_first>
  <action>
    1. `lib/core/framework-chain-composer.cjs`: prune `FRAMEWORK_TO_COMMAND_SLUG` to `Object.freeze({})` (or delete it and update `mapFrameworkToCommandSlug` to rely solely on `command-resolver.commandsForFramework(name)` -> `cmds[0].replace(/^\/mos:/,'')` or `null`). Add a comment: "Framework-to-command mapping is OWNED by lib/workflow/command-resolver.cjs (the generated data/command-registry.json). This kept only as an empty back-compat export." `KNOWN_FRAMEWORKS` may stay; add a comment "name-recognition bootstrap only -- NOT the framework-to-command source". Keep all exports (callers may import the empty `FRAMEWORK_TO_COMMAND_SLUG`).
    2. `lib/hmi/jtbd-taxonomy.json`: EITHER regenerate every `entries[i].methodology_hooks` from `data/command-registry.json` `framework_index` (mapping each entry's frameworks/serves-jtbd hint to the registered commands) OR keep the field and add a top-level `"methodology_hooks_note": "informational only; lib/workflow/command-resolver.cjs is authoritative for framework-to-command"`. EITHER WAY: fix the `/mos:value-proposition` -> `/mos:validate-proposition` mismatch flagged in research. Verify `node tests/test-jtbd-taxonomy.cjs` still exits 0 after the change (additive note or regenerated values -- not a schema break).
    3. `references/methodology/index.md`: replace the 26-row hand-maintained command-routing table with EITHER a regenerated table (header: "GENERATED from data/command-registry.json -- do not hand-edit") OR a pointer block ("The authoritative framework-to-command mapping is `data/command-registry.json` (generated from `commands/*.md` frontmatter -- see `docs/COMMAND-FRONTMATTER.md` and `docs/WORKFLOWS.md`). In conversation, Larry routes via `lib/workflow/command-resolver.cjs` -- he never names a `/mos:` from memory."). Keep any non-routing content (the infra-commands table can stay if it is descriptive, not a framework map). Update `skills/pws-methodology/SKILL.md` so its `references/methodology/index.md` mention (already softened in 122-04) points at the registry / `docs/COMMAND-FRONTMATTER.md` instead.
    4. `skills/brain-connector/SKILL.md`: DELETE the entire "Brain-Powered Command Suggestions" dead block ("Brain has Command nodes linked to Frameworks ... Query `brain_proactive_command` ... Multi-hop: Room frameworks -> FOLLOWS_FRAMEWORK -> Command -> TRIGGERED_BY_SIGNAL -> Signals -> RELEVANT_AT_STAGE -> Stage"). Keep the resolver pointer line 122-04 added ("the command for a framework is whatever `command-resolver.commandsForFramework(<framework>)` returns; commands never live in the Brain -- Canon Part 8"). After this, `grep -E "Brain has Command|brain_proactive_command|FOLLOWS_FRAMEWORK.*Command|:Command" skills/brain-connector/SKILL.md` returns nothing.
    NO em-dashes (hyphens only). NO emoji.
  </action>
  <verify>
    <automated>node tests/test-jtbd-taxonomy.cjs && node -e "const c=require('./lib/core/framework-chain-composer.cjs'); const keys=Object.keys(c.FRAMEWORK_TO_COMMAND_SLUG||{}); process.exit(keys.length===0 ? 0 : 1)" && ! grep -E "Brain has Command|brain_proactive_command|FOLLOWS_FRAMEWORK.*Command|:Command" skills/brain-connector/SKILL.md && grep -q "command-resolver" skills/brain-connector/SKILL.md && ! grep -rE "Brain has Command|brain_proactive_command|FOLLOWS_FRAMEWORK.*Command|:Command" skills/ agents/ references/ && node lib/memory/run-feynman-tests.cjs 2>&1 | grep -q "."</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-jtbd-taxonomy.cjs` exits 0 (the prune did not break the schema test)
    - `node -e "const c=require('./lib/core/framework-chain-composer.cjs'); process.exit(Object.keys(c.FRAMEWORK_TO_COMMAND_SLUG||{}).length===0 ? 0 : 1)"` exits 0 (the table is empty/pruned, export preserved)
    - `grep -E "Brain has Command|brain_proactive_command|FOLLOWS_FRAMEWORK.*Command|:Command" skills/brain-connector/SKILL.md` returns nothing
    - `grep -rE "Brain has Command|brain_proactive_command|FOLLOWS_FRAMEWORK.*Command" skills/ agents/ references/` returns nothing (no Command-node assertion anywhere)
    - `grep -c "command-resolver" skills/brain-connector/SKILL.md` is at least 1 (the resolver pointer survives)
    - `references/methodology/index.md` either has a "GENERATED from data/command-registry.json" header OR a pointer to `docs/COMMAND-FRONTMATTER.md` / `docs/WORKFLOWS.md`; `grep -E "docs/COMMAND-FRONTMATTER.md|data/command-registry.json" references/methodology/index.md` returns at least one match
    - `lib/hmi/jtbd-taxonomy.json` either has `methodology_hooks_note` OR its `methodology_hooks` values all correspond to commands in `data/command-registry.json`; `grep -q "value-proposition" lib/hmi/jtbd-taxonomy.json` -- if present it is `validate-proposition` (the registry's slug), not the stale `value-proposition`
    - `node lib/memory/run-feynman-tests.cjs` runs to completion
    - `grep -lP "\x{2014}" lib/core/framework-chain-composer.cjs references/methodology/index.md skills/brain-connector/SKILL.md skills/pws-methodology/SKILL.md` returns nothing
  </acceptance_criteria>
  <done>FRAMEWORK_TO_COMMAND_SLUG is empty/pruned (export preserved); jtbd-taxonomy.json methodology_hooks is regenerated-or-noted (and the value-proposition slug fixed); references/methodology/index.md no longer hand-maintains a command map; brain-connector SKILL.md has no Command-node prose left; the jtbd-taxonomy schema test and the Feynman runner still pass.</done>
</task>

<task type="auto">
  <name>Task 2: docs/WORKFLOWS.md + THE-BRAIN.md / CANON-PHASE-MAP.md / COMMAND-FRONTMATTER.md cross-links</name>
  <read_first>
    - .planning/WORKFLOW-LAYER-SPEC.md (the "## The five reliability rules" + "## How this slots into the v1.13.0 roadmap" + "### What it leverages" sections -- docs/WORKFLOWS.md summarizes these)
    - docs/THE-BRAIN.md (the existing Brain doc -- add the See-also; correct anything implying commands live in the Brain)
    - docs/CANON-PHASE-MAP.md (the Part 7 + Part 8 tables + the "v1.13.0 milestone phases" table -- add Phase 122 rows; mirror the format of the existing rows e.g. the Phase 95.2 rows)
    - docs/COMMAND-FRONTMATTER.md (from 122-01 -- add a cross-link to docs/WORKFLOWS.md; it already references the Canon Part 8 boundary)
    - docs/MINDRIAN-CANON.md (Part 7 / Part 8 / Part 3 / Part 4 / Part 9 / Part 10 -- the citations docs/WORKFLOWS.md makes)
    - skills/ui-system/SKILL.md (the doc-tone neighbor)
  </read_first>
  <action>
    Create `docs/WORKFLOWS.md` with the 5 sections from the interfaces block above (the closed loop; the five reliability rules; the Brain<->registry<->Larry join + the Canon Part 8 boundary; the surfaces; the Canon citations). Cite `docs/COMMAND-FRONTMATTER.md` (the frontmatter contract), `data/command-registry.json` (the generated registry), `lib/workflow/command-resolver.cjs` (the resolver), `lib/brain/chain-recommender.cjs` (the recommender), `scripts/build-command-registry.cjs` (the generator + `--check` tripwire), `.git/hooks/pre-commit` + `lib/memory/run-feynman-tests.cjs` (the CI surface). Make the Canon Part 8 section explicit: commands never enter the Brain; no `Command` node ever; a command string in a Brain-query payload is a canonical breach. >= 60 lines.
    `docs/THE-BRAIN.md`: add "See also: `docs/WORKFLOWS.md` -- the framework-to-command layer; commands never enter the Brain (Canon Part 8)". If THE-BRAIN.md anywhere implies a command/`Command`-node lives in the Brain, correct it (the live Brain has no `Command` label).
    `docs/CANON-PHASE-MAP.md`: add a Phase 122 row under "### Part 7 - Reuse Before Build" (status: shipped, beta.10 -- "Phase 122 routes all three hand-maintained framework-to-command maps (`FRAMEWORK_TO_COMMAND_SLUG`, `jtbd-taxonomy.json:methodology_hooks`, `references/methodology/index.md`) through one resolver; ~90% wiring of existing code (navigation engine, Brain client, Feynman runner, pre-commit hook); reference `.planning/WORKFLOW-LAYER-SPEC.md`") and under "### Part 8 - The Graph Boundary" (status: shipped, beta.10 -- "Phase 122 registry is plugin-local, validated against Brain framework names at build time via a read-only query, never written back; the `brain-connector` SKILL.md 'Brain has Command nodes' prose deleted (latent Part 8 breach in prose removed); the resolver makes zero Brain calls; the recommender's Cypher carries names + enums only; verified by `grep -rE "Brain has Command|:Command" skills/ agents/ references/` returning 0 and the workflow-layer-e2e grep sweep"). Add a row to the "### v1.13.0 ... milestone phases" table: Phase 122 workflow-layer, canon parts 3/4/7/8, beta.10, "the capstone -- the registry+resolver link that makes 'Larry leads -> the right command' a CI-enforced guarantee".
    `docs/COMMAND-FRONTMATTER.md`: add a "See also: `docs/WORKFLOWS.md`" cross-link near the top.
    NO em-dashes (hyphens only). NO emoji.
  </action>
  <verify>
    <automated>test -f docs/WORKFLOWS.md && grep -q "Canon Part 8" docs/WORKFLOWS.md && grep -q "command-resolver" docs/WORKFLOWS.md && grep -q "data/command-registry.json" docs/WORKFLOWS.md && grep -q "WORKFLOWS.md" docs/THE-BRAIN.md && grep -q "Phase 122" docs/CANON-PHASE-MAP.md && grep -q "WORKFLOWS.md" docs/COMMAND-FRONTMATTER.md && ! grep -lP "\x{2014}" docs/WORKFLOWS.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/WORKFLOWS.md` exists and is at least 60 lines
    - It contains the literal strings `Canon Part 8`, `command-resolver`, `data/command-registry.json`, `scripts/build-command-registry.cjs`, `chain-recommender`, `FEEDS_INTO`, `degrade`, and the 5 reliability-rule names
    - `grep -c "WORKFLOWS.md" docs/THE-BRAIN.md` is at least 1
    - `grep -c "Phase 122" docs/CANON-PHASE-MAP.md` is at least 1 (rows added under Part 7, Part 8, and the v1.13.0 milestone table)
    - `grep -c "WORKFLOWS.md" docs/COMMAND-FRONTMATTER.md` is at least 1
    - `grep -lP "\x{2014}" docs/WORKFLOWS.md docs/THE-BRAIN.md docs/CANON-PHASE-MAP.md docs/COMMAND-FRONTMATTER.md` returns nothing
  </acceptance_criteria>
  <done>docs/WORKFLOWS.md documents the closed loop, the 5 reliability rules, the Brain-registry-Larry join, the Canon Part 8 boundary, the surfaces, and the Canon citations; THE-BRAIN.md / CANON-PHASE-MAP.md / COMMAND-FRONTMATTER.md all point at it; the CANON-PHASE-MAP records Phase 122 under Part 7, Part 8, and the v1.13.0 milestone table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: End-to-end test (frontmatter -> registry -> resolver -> composed chain) + Canon Part 8 grep sweep + CHANGELOG finalization</name>
  <read_first>
    - .planning/phases/122-workflow-layer/122-RESEARCH.md "## Code Examples -> The acceptance-criterion behavior" (the composeWorkflow([3 frameworks]) -> [3-step] / composeWorkflow(["Red Teaming"]) -> [null] example) + "## Common Pitfalls -> Pitfall 4" (the grep guard) + "## Validation Architecture -> Phase Requirements -> Test Map" (the e2e + zero-Brain-mutation rows)
    - scripts/build-command-registry.cjs (the --check entrypoint -- the e2e starts here)
    - lib/workflow/command-resolver.cjs (composeWorkflow / validateChainAutonomy)
    - data/command-registry.json (the generated registry the e2e asserts against)
    - CHANGELOG.md (the existing `## [Unreleased] -- v1.13.0-beta.10 (in progress)` block -- expand it; DO NOT bump version, DO NOT tag, DO NOT publish)
    - CLAUDE.md "## Release Process" + the feedback_release_lockstep_npm memory rule (the maintainer-gated steps to flag in the CHANGELOG note -- NOT to execute)
    - lib/memory/run-feynman-tests.cjs (register the new e2e test in TEST_FILES[])
  </read_first>
  <behavior>
    - Test 1 (frontmatter -> registry consistency): `spawnSync('node', ['scripts/build-command-registry.cjs', '--check'], {cwd: REPO_ROOT})` -> `status === 0`.
    - Test 2 (the acceptance example): `composeWorkflow(["Beautiful Question Framework","Domain Selection","Jobs to Be Done (JTBD)"])` -> a 3-step array; each step has `step` 1-indexed, a non-null `command` that is a `/mos:` string, `optional === false`; each `command` exists in `data/command-registry.json` `commands[].command`. (Assert the SHAPE + registered-ness, not hardcoded slugs -- the 122-01 retrofit owns the exact strings.)
    - Test 3 (the degrade case): `composeWorkflow(["Red Teaming"])` -> `[{step:1, framework:"Red Teaming", command:null, optional:true}]` (a deliberately command-less framework -- if "Red Teaming" ever gets a command, swap to any framework with no command in the registry; the test should pick one dynamically: find a `framework_index` key with an empty array, OR a known Brain framework not in `framework_index`).
    - Test 4 (the autonomy stop-point): build a workflow including a command whose registry entry has `autonomous_safe: false` (e.g. `/mos:hat-briefing`) -> `validateChainAutonomy` -> `runnable === false` AND `blockers` names that step.
    - Test 5 (Canon Part 8 grep sweep): for every `.cjs` under `lib/brain/` and `lib/workflow/`, no line matches `/mos:[a-z-]+` within ~80 chars of a `brain`/`query`/`fetch`/`http` token; `lib/workflow/command-resolver.cjs` has no `require(...brain-client...)`; `scripts/build-command-registry.cjs` has no write-Cypher (`/CREATE |MERGE |SET |DELETE /i`); `grep -rE "Brain has Command|brain_proactive_command|FOLLOWS_FRAMEWORK.*Command|:Command" skills/ agents/ references/` is empty.
  </behavior>
  <action>
    Create `lib/memory/workflow-layer-e2e.test.cjs` (CJS, `node:assert` + `node:child_process` + `node:fs`) covering behaviors 1-5. For Test 5, read each file's source with `fs.readFileSync` and run the regex checks (no shelling out needed; or use `spawnSync('grep', ...)` -- either is fine, keep it deterministic). Print a PASS summary; throw on any fail. Register it in `lib/memory/run-feynman-tests.cjs` TEST_FILES[] (`path.join(REPO_ROOT, 'lib', 'memory', 'workflow-layer-e2e.test.cjs')`); add it to `tests/run-all-122.sh` CJS_SUITES.
    Expand `CHANGELOG.md`'s `## [Unreleased] -- v1.13.0-beta.10 (in progress)` block (do NOT create a new version block -- the file already has this one; do NOT bump `.claude-plugin/plugin.json` or `package.json` -- already at beta.10) with `### Added` / `### Changed` / `### Fixed` lines for Phase 122 per the interfaces block (the registry + generator + resolver + recommender + docs + the 5 frontmatter keys + `/mos:pipeline --from-problem-type/--from-framework` + `/mos:act --chain` autonomy gating + the pre-commit drift tripwire; Changed: `/mos:suggest-next` returns a command sequence, `framework-chain-composer` routes through the resolver, skills point at the resolver; Fixed: the hallucinated-command failure mode, the latent Canon Part 8 breach in `brain-connector` prose). End the block with the maintainer note: "Release steps (maintainer-gated -- NOT performed in this phase): cut the `v1.13.0-beta.10` tag, pin `marketplace.json` `source.ref`, `npm publish @mindrian/os` with the `@next` dist-tag -- per CLAUDE.md release process + the feedback_release_lockstep_npm rule."
    DO NOT run `git tag`. DO NOT run `npm publish`. DO NOT edit `marketplace.json`. DO NOT bump any version.
    NO em-dashes (hyphens only). NO emoji.
  </action>
  <verify>
    <automated>node lib/memory/workflow-layer-e2e.test.cjs && grep -q "workflow-layer-e2e.test.cjs" lib/memory/run-feynman-tests.cjs && node lib/memory/run-feynman-tests.cjs 2>&1 | grep -q "workflow-layer-e2e" && bash tests/run-all-122.sh && grep -q "command-registry" CHANGELOG.md && grep -q "1.13.0-beta.10" CHANGELOG.md && ! grep -lP "\x{2014}" lib/memory/workflow-layer-e2e.test.cjs CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `node lib/memory/workflow-layer-e2e.test.cjs` exits 0 (real test -- starts with `build-command-registry.cjs --check`, asserts the acceptance-example composition, the degrade case, the autonomy stop-point, and the Canon Part 8 grep sweep)
    - `grep -c "workflow-layer-e2e.test.cjs" lib/memory/run-feynman-tests.cjs` is at least 1
    - `node lib/memory/run-feynman-tests.cjs` runs to completion with the e2e suite among those run
    - `bash tests/run-all-122.sh` exits 0
    - `CHANGELOG.md` `## [Unreleased] -- v1.13.0-beta.10` block contains the literal strings `command-registry`, `command-resolver`, `hallucinated`, `Canon Part 8`; the maintainer note mentions `npm publish` and `marketplace` as NOT-done-here steps
    - `git diff .claude-plugin/plugin.json package.json` is empty (no version bump); `git status` shows no change to `marketplace.json` (it is not in this repo) and no new tag
    - `grep -lP "\x{2014}" lib/memory/workflow-layer-e2e.test.cjs CHANGELOG.md` returns nothing
  </acceptance_criteria>
  <done>The end-to-end test walks frontmatter -> registry --check -> resolver.composeWorkflow(acceptance example) -> autonomy stop-point and runs the Canon Part 8 grep sweep, all passing and registered in the Feynman runner; the CHANGELOG beta.10 block is finalized for Phase 122 with the maintainer-gated release steps flagged (not executed); no version bump, no tag, no publish.</done>
</task>

</tasks>

<verification>
- `node lib/memory/workflow-layer-e2e.test.cjs` exits 0; `node tests/test-jtbd-taxonomy.cjs` exits 0 (the prune did not break it).
- `node -e "const c=require('./lib/core/framework-chain-composer.cjs'); process.exit(Object.keys(c.FRAMEWORK_TO_COMMAND_SLUG||{}).length===0 ? 0 : 1)"` exits 0 (the residual map is pruned, export preserved).
- `grep -rE "Brain has Command|brain_proactive_command|FOLLOWS_FRAMEWORK.*Command|:Command" skills/ agents/ references/` returns nothing (no Command-node assertion anywhere).
- `docs/WORKFLOWS.md` exists (>= 60 lines), documents the Canon Part 8 boundary; `docs/THE-BRAIN.md` + `docs/CANON-PHASE-MAP.md` + `docs/COMMAND-FRONTMATTER.md` point at it; the CANON-PHASE-MAP records Phase 122 under Part 7, Part 8, and the v1.13.0 milestone table.
- `CHANGELOG.md` beta.10 block finalized for Phase 122 with the maintainer-gated release steps flagged; no version bump; no tag; no `npm publish`; no `marketplace.json` edit.
- `node lib/memory/run-feynman-tests.cjs` runs to completion; `bash tests/run-all-122.sh` exits 0.
- `grep -rlP "\x{2014}" docs/WORKFLOWS.md docs/COMMAND-FRONTMATTER.md lib/memory/workflow-layer-e2e.test.cjs CHANGELOG.md references/methodology/index.md lib/core/framework-chain-composer.cjs skills/brain-connector/SKILL.md` returns nothing.
</verification>

<success_criteria>
- The three hand-maintained framework-to-command maps are pruned: `framework-chain-composer.FRAMEWORK_TO_COMMAND_SLUG` is empty/pass-through (export preserved), `lib/hmi/jtbd-taxonomy.json:methodology_hooks` is regenerated-from-registry-or-noted (and the `value-proposition` -> `validate-proposition` slug fixed), `references/methodology/index.md` no longer hand-maintains a command-routing table.
- `skills/brain-connector/SKILL.md` has NO Brain-Command-node prose left; `grep -rE "Brain has Command|brain_proactive_command|FOLLOWS_FRAMEWORK.*Command|:Command" skills/ agents/ references/` returns nothing; the resolver pointer survives.
- `docs/WORKFLOWS.md` ships (the Brain<->registry<->Larry join, the 5 reliability rules, the Canon Part 8 boundary, the surfaces, the Canon 3/4/7/8/9/10 citations); `docs/THE-BRAIN.md` + `docs/CANON-PHASE-MAP.md` + `docs/COMMAND-FRONTMATTER.md` point at it; the CANON-PHASE-MAP records Phase 122 under Part 7, Part 8, and the v1.13.0 milestone table.
- `lib/memory/workflow-layer-e2e.test.cjs` walks frontmatter -> registry `--check` -> `resolver.composeWorkflow(the spec's acceptance example)` -> the degrade case -> the `validateChainAutonomy` stop-point -> the Canon Part 8 grep sweep, all passing and registered in the Feynman runner + `tests/run-all-122.sh`.
- The CHANGELOG `[Unreleased] -- v1.13.0-beta.10` block is finalized for Phase 122 with the maintainer-gated release steps (tag / marketplace pin / `npm publish @next`) flagged as NOT performed here; no version bump; no `git tag`; no `npm publish`; no `marketplace.json` edit.
- No new npm dependency; no em-dashes; the Brain is never written to and no command string ever reaches a Brain-query payload.
</success_criteria>

<output>
After completion, create `.planning/phases/122-workflow-layer/122-05-SUMMARY.md`
</output>
