---
phase: 123-install-lifecycle-harness
plan: 06
type: execute
wave: 7
depends_on: [01, 02, 03, 04, 05, 07]
files_modified:
  - CHANGELOG.md
  - .claude-plugin/plugin.json
  - package.json
  - docs/CANON-PHASE-MAP.md
autonomous: false
requirements: [HARNESS-123-17]
canon_parts: [5, 6]

must_haves:
  truths:
    - "v1.13.0-beta.13 is cut via `bash scripts/release.sh --prerelease` (NOT by hand) -- the fixed release.sh handles the pre-release algebra via semver.inc, makes commit A + commit B (two-commit form), tags v1.13.0-beta.13 on commit A, pushes both repos, npm-publishes @mindrian_os/install@1.13.0-beta.13 with @next, and runs doctor --acceptance --pre-tag before tagging + full doctor --acceptance after the push"
    - "The beta.13 cut carries Plans 01-05 + 07: release.sh fixes, install-state record + manifest, doctor classes I+J + --acceptance, cache prune + @mindrian_os/cli sweep, Brain key resolver"
    - "It also carries the still-pending Phase 109 release commit (per the Phase 109 ledger note in ROADMAP.md), Phase 110 docs, and the brain-client fix that landed on main HEAD 4c586c8"
    - "docs/CANON-PHASE-MAP.md is updated with a Part-6 row (dog-fooding the install lifecycle) and a Part-7 row (reuse justification: ~90% extension of shipped code) for Phase 123, both marked shipped"
    - "`mindrian-os doctor --acceptance` runs all green on a real Windows box (Lawrence / operator manual verification) BEFORE promotion to a clean 1.13.0"
  artifacts:
    - path: CHANGELOG.md
      provides: "the v1.13.0-beta.13 entry (Plans 01-05 + 07 absorbed) + the [Unreleased] reset to v1.13.0-beta.14 (next pre-release, written by release.sh's commit B)"
      contains: "1.13.0-beta.13"
    - path: docs/CANON-PHASE-MAP.md
      provides: "Phase 123 mapped under Part 6 + Part 7 (shipped)"
      contains: "Phase 123"
  key_links:
    - from: scripts/release.sh
      to: "git tag v1.13.0-beta.13 + npm @mindrian_os/install@1.13.0-beta.13 + marketplace.json source.ref"
      via: "release.sh --prerelease (the fixed pipeline)"
      pattern: "release: v1.13.0-beta.13"
    - from: docs/CANON-PHASE-MAP.md
      to: "Phase 123"
      via: "Part 6 + Part 7 rows"
      pattern: "Phase 123|123-install-lifecycle-harness"
---

<objective>
Plan-6 of 7. The release cut. After Plans 01-05 + 07 have all landed (and their Wave-0 tests are green on `main`), this plan ships `v1.13.0-beta.13` via the FIXED `release.sh`, updates the canon-phase map, and gates promotion to a clean `1.13.0` on a Windows-box `--acceptance` run.

This plan is mostly OPERATIONAL -- but it has 3 non-trivial pieces: the CHANGELOG composition, the canon-phase-map update, and the manual Windows verification.

1. **Pre-flight (autonomous).**
   - Verify main is clean: `git status --porcelain` shows only expected drift (untracked OK).
   - Verify all 6 prior plans landed: `ls .planning/phases/123-install-lifecycle-harness/*-SUMMARY.md | wc -l` >= 6.
   - Verify the Wave-0 suite passes: `bash tests/run-all.sh` exits 0; all Phase-123 tests green.
   - Verify `node_modules/semver` exists. If absent: instruct the operator to run `npm install`. DO NOT auto-run.
   - Verify `node scripts/doctor.cjs --acceptance --pre-tag` exits 0.

2. **Compose the v1.13.0-beta.13 CHANGELOG entry (autonomous).** Write a `## [Unreleased]` entry at the top of `CHANGELOG.md` summarizing Plans 01-05 + 07. The entry shape is in <changelog_template> below. `release.sh`'s commit A renames `[Unreleased]` to `## [1.13.0-beta.13] - <today>` automatically.

3. **Cut the release (autonomous + a checkpoint).**
   - **Dry-run FIRST:** `MOS_TEST_DRY_RUN=1 bash scripts/release.sh --prerelease`.
   - If the dry-run is clean: **CHECKPOINT** the operator to confirm "ready to cut beta.13".
   - On confirm: `bash scripts/release.sh --prerelease` (REAL run). This:
     - Computes `NEW_VERSION = semver.inc('1.13.0-beta.12', 'prerelease', 'beta')` = `1.13.0-beta.13`.
     - Computes `NEXT_VERSION` for commit B = `1.13.0-beta.14`.
     - Bumps `plugin.json` + `package.json` to `1.13.0-beta.13`; bumps marketplace `marketplace.json` `version` to `1.13.0-beta.13` AND `source.ref` to `v1.13.0-beta.13`.
     - Runs `verify-release` (Step 2 + Step 6.5).
     - Renames CHANGELOG `[Unreleased]` to `## [1.13.0-beta.13] - <today>`.
     - **Runs `mindrian-os doctor --acceptance --pre-tag` (Step 6.6) -- HARD ABORT on failure.**
     - Makes **commit A**: `release: v1.13.0-beta.13 -- install-lifecycle-harness`; `git tag v1.13.0-beta.13` on commit A.
     - Npm-publish: `@mindrian_os/install@1.13.0-beta.13 --tag next`.
     - Makes **commit B**: bump to `1.13.0-beta.14`; reset CHANGELOG `## [Unreleased] -- v1.13.0-beta.14 (in progress)`.
     - Dirty-repo guard (Step 8): expect exactly 2 commits ahead (A + B).
     - Pushes plugin repo + marketplace repo (Step 9).
     - **Runs full `mindrian-os doctor --acceptance` (Step 9.6) -- HARD ABORT on failure.**
     - Updates the local plugin cache (Step 10).
   - **If anything fails:** follow the recovery instructions printed by `release.sh` (Plan-1's design is re-entrant where it can be).

4. **Update `docs/CANON-PHASE-MAP.md` (autonomous).** Add Part-6 + Part-7 rows + a version-history entry per <canon_phase_map_edits> below. RECOMMENDED commit shape: a SEPARATE commit AFTER the release lands (cleaner than mixing it into commit A). Document the choice in the SUMMARY.

5. **Manual Windows `--acceptance` validation (CHECKPOINT).** The operator (Lawrence or equivalent) on a Windows box:
   1. `/plugin marketplace update`
   2. `claude plugin update mos@mindrian-marketplace` (or `--version 1.13.0-beta.13` if not on the beta channel)
   3. Restart Claude Code.
   4. `mindrian-os doctor --acceptance` -- ALL GREEN expected.
   5. `mindrian-os doctor --install-state --json` -- inspect for sanity.
   6. Confirm `Brain: HTTP client active` or `Brain: not configured (Tier 0)` in the session-start banner -- the MCP-centric WARN is GONE.
   PASS gates promotion to clean `1.13.0` (a follow-up `bash scripts/release.sh --finalize` -- NOT this plan's work).

NOTE: This plan does NOT cut `1.13.0` (the clean release). That happens AFTER the Windows `--acceptance` is green, as a follow-up release action. Documented as the post-Phase-123 milestone.

Purpose: ship the harness. The phase only matters if the user's install actually upgrades cleanly to it.
Output: `v1.13.0-beta.13` cut + tagged + pushed + npm-published; CANON-PHASE-MAP updated; Windows operator manual-verification gate documented.
</objective>

<changelog_template>
The `[Unreleased]` body (release.sh renames the heading):

```markdown
## [Unreleased]

### Added
- **Install-state contract (Phase 123).** One record (`~/.mindrian/install-state.json`, written by session-start, read by everyone), one manifest (`data/deployment-surfaces.json`, 6 surfaces), two new doctor classes (I = install-state + topology + 6-way version-of-record; J = manifest reconciliation), one new release gate command (`mindrian-os doctor --acceptance` / `--pre-tag`). Bug 7 fixed: a marketplace-cache-only install is a healthy topology, not drift. Aggressive `doctor --fix` recovers missing records, drifted owned surfaces, wrong `~/.mindrian-last-version`, and (under hard guardrails: never a dev-clone, never with uncommitted/unpushed changes, always backup-then-verify-then-remove) a legacy clone migration.
- **release.sh owns ALL version bumps.** Pre-release algebra via the npm `semver` package as a devDep (no runtime-dep impact); two-commit form (commit A = release+tag at `vN` with `plugin.json == vN`; commit B = next-bump to `vN+1`; main HEAD on B; marketplace `source.ref` pinned to `vN`). Dirty-repo / ahead-of-origin guard. Step 9.5 publishes `@mindrian_os/install` (renamed from the stale `@mindrian_os/cli`). `mindrian-os doctor --acceptance --pre-tag` before tag; full `--acceptance` after push -- both hard aborts, no override.
- **Cache pruning on update.** `lib/core/cache-prune.cjs` keeps the active version + N=2 most recent under `~/.claude/plugins/cache/<marketplace>/mos/`, never deletes the active, skips entirely if `installed_plugins.json` is unreadable. Runs in session-start on version change AND in doctor --fix unconditionally.
- **Brain key resolver (Plan-7).** `lib/core/resolve-brain-key.cjs` is THE single resolver: `MINDRIAN_BRAIN_KEY` env -> `~/.mindrian.env` -> CWD `.env` -> not-found. `brain-client.cjs::getApiKey()` delegates. `session-start`'s Brain block emits positive status lines -- the MCP-centric WARN is gone. `skills/brain-connector/SKILL.md` has a new step-0 HTTP-path detection branch. SEC-02 `chmod 600 ~/.mindrian.env` on write (POSIX only) in `commands/setup.md`.

### Fixed
- **`scripts/release.sh:40`'s `IFS='.' read -r MAJOR MINOR PATCH`** mangled pre-release versions; replaced with `semver.inc()` calls. (Why beta.10/11/12/13 were hand-rolled.)
- **`scripts/doctor.cjs:40`'s hardcoded `INSTALL_DIR = ~/.claude/plugins/mindrian-os/`** was the `MODULE_NOT_FOUND` source; new code resolves via `resolveActivePluginRoot()`.
- **`scripts/session-start:419`'s `~/.mindrian-last-version` write inside the cold-start `else` branch** never fired on a session WITH an active room. The new install-state record block writes it unconditionally as the single writer.
- **`commands/setup.md:145`'s stale URL `mindrianos-jsagirs-projects.vercel.app/brain-access`** -> `mindrian-os.com/brain-access`.
- **CHANGELOG prose about the Brain client "calling `brain.mindrian.ai`"** softened to "currently `*.onrender.com`, moving to `brain.mindrian.ai`; override via `MINDRIAN_BRAIN_URL`".
- **`scripts/release-beta-smoke.sh`** retired (hard-pinned to v1.11.0-beta.1, stale Phase 89.6 artifact); `mindrian-os doctor --acceptance --pre-tag` supersedes it.

### Changed
- **`brain-client.cjs::getApiKey()` precedence**: was env -> CWD `.env` -> `~/.mindrian.env`; now env -> `~/.mindrian.env` -> CWD `.env` (per Phase 123 D-31).
- **`data/deployment-surfaces.json`** added (hand-maintained; 6 surfaces; the `data/` layout convention from Phase 122).
- **`docs/install/BRAIN-SETUP.md` + `.env.brain.template`**: state explicitly that auth is `Authorization: Bearer <key>` only (NOT `x-api-key`); surface `https://mindrian-os.com/brain-access` in the no-key fallback.
- **`docs/CANON-PHASE-MAP.md`**: Phase 123 mapped under Part 6 (dog-fooding the install lifecycle) and Part 7 (reuse justification).

### Notes
- **Path C re-route status:** v1.13.0-beta.13 carries Phase 123. Phase 110 implementation remains slated for beta.3 per the 2026-05-05 Path C re-route.
- **Promotion to clean 1.13.0** gated on a real-Windows `mindrian-os doctor --acceptance` run (Lawrence / operator), per Canon Part 5 + Part 6.
```

Em-dash-free. Verb-first bullets.
</changelog_template>

<canon_phase_map_edits>
1. **Part 6 table.** Append after the existing Part-6 rows:
   ```
   | shipped  | Phase 123 install-lifecycle-harness -- dog-fooding the install lifecycle: one record (`~/.mindrian/install-state.json`), one manifest (`data/deployment-surfaces.json`), one command (`doctor --acceptance`), one release script (`release.sh` with semver pre-release algebra + two-commit form + dirty-repo guard + Step 9.5 `@mindrian_os/install`). The plugin's own install state honors the plugin's canon. | Phase 123 install-lifecycle-harness (v1.13.0-beta.13) -- Plans 01-07 |
   ```

2. **Part 7 table.** Append:
   ```
   | shipped | Phase 123 install-lifecycle-harness extends shipped code (~90% reuse): `lib/core/active-plugin-root.cjs` (resolver, extended with topology), `scripts/doctor.cjs` (classes A-H roster + new I + J), `scripts/verify-release` (wrapped by `--acceptance`), `scripts/install-pre-commit.sh` (reused for the dev-clone manifest surface), `data/command-registry.json` layout convention (`data/deployment-surfaces.json` mirrors it), `performRecoveryAtomic` + `safeRename` (reused for legacy migration + `installed_plugins.json` repair). Net-new files: `data/deployment-surfaces.json`, `lib/core/resolve-brain-key.cjs`, `lib/core/cache-prune.cjs`, the per-class fixtures. | Phase 123 install-lifecycle-harness (v1.13.0-beta.13) |
   ```

3. **Version history table** (bottom):
   ```
   | v1.4 (kept)   | TBD      | 2026-05-13 | Phase 123 (v1.13.0-beta.13) install-lifecycle-harness shipped -- Part 6 + Part 7 map rows added; no canon text change. |
   ```
</canon_phase_map_edits>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-install-lifecycle-harness/123-CONTEXT.md
@.planning/phases/123-install-lifecycle-harness/123-RESEARCH.md
@.claude/includes/release-process.md
@docs/CANON-PHASE-MAP.md

<interfaces>
<!-- All prior Plan 01-05 + 07 SUMMARYs must exist and be on main HEAD. release.sh post-Plan-1 + 4 has: --prerelease, --finalize, --start-prerelease, --allow-ahead flags; semver bump algebra; two-commit form; dirty-repo guard; Step 6.6 (--acceptance --pre-tag) before tag; Step 9.6 (--acceptance full) after push. -->
<!-- Plan-4 deleted scripts/release-beta-smoke.sh; the release.sh no longer references it. -->
<!-- The Windows operator manual gate uses tests/manual/95.6-windows-cold-install-acceptance.md (Plan-5 added the mindrian-os doctor --acceptance callout at the top). -->
<!-- Promotion to clean 1.13.0: a follow-up `bash scripts/release.sh --finalize` after the Windows --acceptance is green. NOT this plan's work; just documented. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight + compose the v1.13.0-beta.13 CHANGELOG entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/phases/123-install-lifecycle-harness/ (confirm SUMMARYs for 01, 02, 03, 04, 05, 07 all exist)
    - CHANGELOG.md (find the current [Unreleased] heading + any prior entries' shape -- the new entry mirrors that style)
    - All 6 prior PLAN.md success_criteria + their SUMMARYs (compose FROM the SUMMARY content -- the SUMMARYs are the ground truth)
    - .planning/phases/123-install-lifecycle-harness/123-CONTEXT.md (the one-line contract -- the entry's opening line is a paraphrase of it)
    - docs/INSTALL-LIFECYCLE-HARNESS.md (the disease table -- the Fixed section references the exact bugs the spec named)
  </read_first>
  <action>
    1. **Pre-flight verification.** Run (and record outputs in the SUMMARY):
       - `ls .planning/phases/123-install-lifecycle-harness/*-SUMMARY.md | wc -l` -- expect >= 6.
       - `bash tests/run-all.sh` -- expect exit 0. (If any Phase-123 test fails, ABORT this task and fix the failing plan first.)
       - `node scripts/doctor.cjs --acceptance --pre-tag` -- expect exit 0. (If non-zero, Step 6.6 would also fail. Fix first.)
       - `test -d node_modules/semver` -- if missing, instruct operator to `npm install`. Do NOT auto-run.
       - `git fetch origin main && git log origin/main..HEAD --oneline` -- expect to show the still-pending Phase 109 release commit + the Phase 110 docs + the brain-client fix + any Phase 123 plan commits the operator wants to ship. Anything unexpected -> explicit blessing required.
       - Verify no `git status --porcelain` drift in tracked files (untracked is OK).

    2. **Compose CHANGELOG.md's new [Unreleased] entry** using the template in `<changelog_template>` above. The entry MUST:
       - Cite each plan by name + plan-number ("Plan 01", "Plan 02", ...).
       - Open with the one-line contract paraphrase.
       - Itemize Added / Fixed / Changed / Notes sections.
       - Mention the Path C re-route status.
       - Be em-dash-free.
       - State the promotion-gate (clean 1.13.0 gated on Windows --acceptance green).

    3. Stage the CHANGELOG edit (`git add -f CHANGELOG.md` if needed). DO NOT commit yet -- Task 2's release.sh will pick this up as the source for the `[1.13.0-beta.13] - <date>` rename.

    Note: release.sh does `[Unreleased]` -> `## [1.13.0-beta.13] - <today>` automatically. This task ONLY writes the BODY of the entry.
  </action>
  <verify>
    <automated>bash tests/run-all.sh</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/123-install-lifecycle-harness/*-SUMMARY.md | wc -l` is >= 6
    - `bash tests/run-all.sh` exits 0
    - `node scripts/doctor.cjs --acceptance --pre-tag` exits 0
    - `test -d node_modules/semver` succeeds OR the operator has been instructed to run `npm install`
    - `head -200 CHANGELOG.md | grep -q "Unreleased"` returns TRUE
    - `head -200 CHANGELOG.md | grep -q "Install-state contract (Phase 123)"` returns TRUE
    - `head -200 CHANGELOG.md | grep -q "release.sh owns ALL version bumps"` returns TRUE
    - `head -200 CHANGELOG.md | grep -q "Brain key resolver"` returns TRUE
    - `head -200 CHANGELOG.md | grep -nP "\xe2\x80\x94"` returns nothing (no em-dashes in the new entry)
  </acceptance_criteria>
  <done>Pre-flight green; the CHANGELOG body for v1.13.0-beta.13 is composed under [Unreleased]; ready for release.sh to rename the heading + commit it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint -- operator reviews CHANGELOG + commits-ahead + dry-run before the real cut</name>
  <action>
    Pre-flight checks pass; the v1.13.0-beta.13 CHANGELOG body is composed; main is in the expected state (Phase 109 release commit + Phase 110 docs + Phase 123 plans + brain-client fix ahead of origin). The operator now reviews + greenlights the release.

    **How to verify:**
    Operator reviews:
    1. `head -200 CHANGELOG.md` -- inspect the new [Unreleased] entry; confirm Plans 01-05 + 07 are all named; em-dash-free.
    2. `git log origin/main..HEAD --oneline` -- inspect the commits ahead of origin. Expected (in some order): the Phase 123 plan commits (one per plan -- 01, 02, 03, 04, 05, 07), the still-pending Phase 109 release commit, Phase 110 docs, the brain-client fix on `4c586c8`. If more is listed, confirm each one belongs in beta.13.
    3. `git status --porcelain` -- expect only the staged CHANGELOG + the pre-existing untracked working-tree drift (dashboard/graph.json, docs/testers/REGISTRY.md, docs/testers/aniruddh-mohan/) per the planning-context note.

    Then do a DRY-RUN of release.sh: `MOS_TEST_DRY_RUN=1 bash scripts/release.sh --prerelease 2>&1 | tee /tmp/release-dryrun-beta13.log`. Review the output. The dry-run prints what each step would do; confirm the version bump shows `1.13.0-beta.12 -> 1.13.0-beta.13`, the commit-A + commit-B plan is sketched, the dirty-repo guard passes, the doctor --acceptance --pre-tag would pass, the npm publish target is @mindrian_os/install@1.13.0-beta.13 --tag next.

    Reply "approved" if everything looks right. Reply with specific concerns otherwise.
  </action>
  <resume-signal>Type "approved" to proceed to the REAL release.sh run, OR describe issues to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run release.sh --prerelease (the real cut)</name>
  <files>scripts/release.sh, CHANGELOG.md, .claude-plugin/plugin.json, package.json</files>
  <read_first>
    - scripts/release.sh (post-Plan-1 + Plan-4 -- the full pipeline this task invokes)
    - The dry-run output from Task 2's checkpoint (/tmp/release-dryrun-beta13.log -- the operator's review summary)
    - .claude/includes/release-process.md (the 5-way + 1-npm consistency rule + the marketplace source.ref pinning rule)
  </read_first>
  <action>
    1. Run `bash scripts/release.sh --prerelease` (the REAL, non-dry-run pipeline).
    2. The script will:
       - Compute NEW_VERSION = 1.13.0-beta.13; NEXT_VERSION = 1.13.0-beta.14.
       - Bump plugin.json + package.json + marketplace.json (version AND source.ref).
       - Run verify-release (Step 2 + Step 6.5).
       - Rename CHANGELOG `[Unreleased]` to `## [1.13.0-beta.13] - <today>`.
       - Run `doctor --acceptance --pre-tag` (Step 6.6) -- HARD ABORT on failure.
       - Make commit A + tag v1.13.0-beta.13.
       - Npm-publish @mindrian_os/install@1.13.0-beta.13 --tag next.
       - Make commit B (1.13.0-beta.14 + [Unreleased] heading reset).
       - Dirty-repo / ahead-of-origin guard (Step 8).
       - Push plugin repo + marketplace repo (Step 9).
       - Run full `doctor --acceptance` (Step 9.6) -- HARD ABORT on failure.
       - Update local plugin cache (Step 10).
    3. If the script aborts at any step: capture the abort message; follow the recovery instructions printed by `release.sh`; if the npm publish landed but a later step failed, the recovery is to either (a) retry the failed step manually OR (b) yank the npm publish via `npm dist-tag rm @mindrian_os/install@1.13.0-beta.13 next` + npm-deprecate the version. Document the outcome.
    4. After a successful run: capture all output to a release log file (e.g. `/tmp/release-beta13.log`) and reference it in the SUMMARY.
  </action>
  <verify>
    <automated>git tag -l v1.13.0-beta.13</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l v1.13.0-beta.13` lists the tag (commit A succeeded)
    - `git log -1 --format='%s' v1.13.0-beta.13` shows "release: v1.13.0-beta.13" or a close variant
    - `head -50 CHANGELOG.md | grep -q "1.13.0-beta.13"` returns TRUE (commit A renamed [Unreleased])
    - `node -e "console.log(require('./.claude-plugin/plugin.json').version)"` prints `1.13.0-beta.14` (commit B bumped to NEXT_VERSION)
    - `node -e "console.log(require('./package.json').version)"` prints `1.13.0-beta.14`
    - `head -10 CHANGELOG.md | grep -q "Unreleased"` returns TRUE (commit B reset the heading)
    - `git log origin/main..HEAD --oneline` returns empty (push succeeded; commit A + commit B are on origin/main)
    - `git -C ~/mindrian-marketplace log -1 --format='%s'` shows "release: mos v1.13.0-beta.13" (marketplace pushed)
    - `node -e "console.log(require(process.env.HOME + '/mindrian-marketplace/.claude-plugin/marketplace.json').plugins[0].source.ref)"` prints `v1.13.0-beta.13`
  </acceptance_criteria>
  <done>v1.13.0-beta.13 is cut + tagged + npm-published + pushed; commit B (1.13.0-beta.14 next-bump) is on main HEAD; marketplace source.ref pinned to v1.13.0-beta.13; both --acceptance gates passed.</done>
</task>

<task type="auto">
  <name>Task 4: Update docs/CANON-PHASE-MAP.md + commit</name>
  <files>docs/CANON-PHASE-MAP.md</files>
  <read_first>
    - docs/CANON-PHASE-MAP.md (find the Part-6 + Part-7 tables; find the version-history table at the bottom)
    - Phase 122's existing Part-7 + Part-8 rows for the pattern shape (mirror that style)
    - .planning/phases/123-install-lifecycle-harness/123-CONTEXT.md (canon_parts: [5, 6])
  </read_first>
  <action>
    1. Edit `docs/CANON-PHASE-MAP.md` per `<canon_phase_map_edits>` above: 3 inserts (Part-6 row, Part-7 row, version-history row).
    2. Stage + commit as a SEPARATE commit (cleaner than mixing into commit A): `git add -f docs/CANON-PHASE-MAP.md && git commit -m "docs(123): map Phase 123 under Part 6 + Part 7"` ending with the standard `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` footer.
    3. Push: but per the planning context "NEVER git push" rule for THIS plan-phase invocation, the operator decides whether to push immediately or in the next session. Recommend NOT auto-pushing here; the canon-map is a small docs commit, and the planning-context rule says "DO NOT git push" for this overall session. NOTE in the SUMMARY: the canon-map commit is on local main; the operator can `git push origin main` separately.
    4. NO em-dashes.
  </action>
  <verify>
    <automated>grep -q "Phase 123" docs/CANON-PHASE-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Phase 123 install-lifecycle-harness" docs/CANON-PHASE-MAP.md` returns TRUE
    - `grep -q "dog-fooding the install lifecycle" docs/CANON-PHASE-MAP.md` returns TRUE (the Part-6 row's phrase)
    - `grep -q "~90% reuse" docs/CANON-PHASE-MAP.md` returns TRUE (the Part-7 row's phrase)
    - `git log -1 --format='%s' docs/CANON-PHASE-MAP.md` shows the canon-map commit message
    - `head -10 docs/CANON-PHASE-MAP.md | grep -nP "\xe2\x80\x94"` returns nothing on the new lines
  </acceptance_criteria>
  <done>docs/CANON-PHASE-MAP.md has Phase 123 mapped under Part 6 + Part 7 + the version-history note; committed as a separate clean docs commit; ready for the operator to push when they choose.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Checkpoint -- Windows operator manual mindrian-os doctor --acceptance gate (promotion-to-1.13.0 gate)</name>
  <action>
    v1.13.0-beta.13 is cut + pushed + npm-published + the canon-phase map is updated. The acceptance pipeline ran twice (--pre-tag before tag, full after push), both green on this box. NOW the Windows operator (Lawrence or equivalent) confirms on a real Windows install.

    **How to verify:**
    On a real WINDOWS box (Lawrence or equivalent operator):
    1. `/plugin marketplace update`
    2. `claude plugin update mos@mindrian-marketplace` (if not auto-updating beta channel: `--version 1.13.0-beta.13`)
    3. Restart Claude Code.
    4. Start a session. Confirm the session-start banner shows version 1.13.0-beta.13 AND `Brain: HTTP client active (mindrian-brain.onrender.com)` OR `Brain: not configured (Tier 0)` -- the MCP-centric WARN should be GONE.
    5. `mindrian-os doctor --acceptance` -- expect ALL GREEN (the 5-point checklist passes).
    6. `mindrian-os doctor --install-state --json` -- inspect: `active_version == "1.13.0-beta.13"`, `topology == "marketplace-cache"`, all 5 owned surfaces `ok: true` (the pre-commit-hook surface is skipped on a user box), the install-state record present, the 6-way version-of-record consistent.
    7. `mindrian-os doctor --all` -- expect exit 0.

    If ALL of the above pass: reply "windows-acceptance green; ready to promote to 1.13.0" -- this unblocks the promotion (a follow-up `bash scripts/release.sh --finalize` cut, NOT part of this phase).
    If ANY of the above fails: reply with the specific failures; the next session cuts a beta.14 with fixes.

    If you don't have a Windows box right now, reply "deferred-pending-windows-test" and the phase ships as beta.13 unpromoted; promotion to 1.13.0 waits.
  </how-to-verify>
  <resume-signal>Reply "windows-acceptance green; ready to promote to 1.13.0", OR "windows-acceptance failed: <findings>", OR "deferred-pending-windows-test".</resume-signal>
</task>

</tasks>

<verification>
- `git tag -l v1.13.0-beta.13` returns the tag.
- `node -e "console.log(require('./.claude-plugin/plugin.json').version)"` prints `1.13.0-beta.14` (commit B is HEAD).
- `git -C ~/mindrian-marketplace log -1 --format='%s'` mentions `v1.13.0-beta.13`.
- `npm view @mindrian_os/install@1.13.0-beta.13 version` returns `1.13.0-beta.13`.
- `head -50 CHANGELOG.md | grep -q "1.13.0-beta.13"` returns TRUE.
- `grep -q "Phase 123" docs/CANON-PHASE-MAP.md` returns TRUE.
- Windows operator manual checkpoint: response captured ("windows-acceptance green" OR specific findings OR deferred).
</verification>

<success_criteria>
- `v1.13.0-beta.13` cut via `bash scripts/release.sh --prerelease` (NOT hand-rolled). The two-commit form held: commit A on the tag, commit B on main HEAD. Marketplace `source.ref` pinned to `v1.13.0-beta.13`.
- The cut carries Plans 01-05 + 07 + the still-pending Phase 109 release commit + Phase 110 docs + the brain-client fix that was on main HEAD 4c586c8.
- Both `doctor --acceptance` gates (--pre-tag before tag + full after push) ran inside the release pipeline and exited 0.
- `npm view @mindrian_os/install@1.13.0-beta.13 version` returns `1.13.0-beta.13` (the publish landed with dist-tag `next`).
- `docs/CANON-PHASE-MAP.md` has Phase 123 mapped under Part 6 + Part 7 (both shipped); version-history note added.
- The Windows manual `--acceptance` checkpoint is documented (operator response captured) -- this gates promotion to clean 1.13.0 (a follow-up release, NOT this phase's work).
</success_criteria>

<output>
After completion, create `.planning/phases/123-install-lifecycle-harness/123-06-SUMMARY.md`. The SUMMARY must capture:
- The pre-flight verification outputs.
- The release.sh log (or its tail).
- The 5-way + 1-npm consistency check (plugin.json, package.json, CHANGELOG, git tag, marketplace.json + source.ref, npm view).
- The Windows operator response (green / failed / deferred).
- The promotion-to-1.13.0 status (gated on the Windows checkpoint).
</output>
