---
phase: 123-install-lifecycle-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/release.sh
  - package.json
  - tests/test-release-npm-gate.sh
  - tests/test-release-bump-algebra.cjs
  - tests/run-all.sh
  - lib/memory/run-feynman-tests.cjs
autonomous: true
requirements: [HARNESS-123-01, HARNESS-123-02, HARNESS-123-03, HARNESS-123-04]
canon_parts: [5, 6]

must_haves:
  truths:
    - "Running `bash scripts/release.sh --prerelease` on a clean repo at v1.13.0-beta.12 produces a v1.13.0-beta.13 release commit (commit A), a v1.13.0-beta.13 git tag on commit A, and a follow-up next-bump commit (commit B) with plugin.json/package.json at v1.13.0-beta.14 and the CHANGELOG `[Unreleased]` heading reset"
    - "release.sh refuses to push when the working tree has more than the release commit(s) ahead of origin/main (unless --allow-ahead) and refuses on dirty tracked files other than the bumped files"
    - "release.sh Step 9.5 publishes `@mindrian_os/install` (not `@mindrian_os/cli`) with @next for pre-release suffixes and @latest for clean X.Y.Z"
    - "release.sh refuses to operate when .claude-plugin/plugin.json version is not valid semver (e.g. a 4-component 1.12.5.1)"
  artifacts:
    - path: scripts/release.sh
      provides: "the version-owning release pipeline with semver bump algebra, two-commit form, dirty-repo guard, and the @mindrian_os/install Step 9.5"
      contains: "require('"
      min_lines: 250
    - path: package.json
      provides: "semver added to devDependencies (not dependencies, not files allowlist)"
      contains: "semver"
    - path: tests/test-release-bump-algebra.cjs
      provides: "semver inc cases (incl. the patch-finalizes correction + the 1.12.5.1 non-semver case) + release.sh structural assertions"
      min_lines: 60
    - path: tests/test-release-npm-gate.sh
      provides: "the 6-gate structural assertion on release.sh Step 9.5, updated to @mindrian_os/install"
      contains: "@mindrian_os/install"
  key_links:
    - from: scripts/release.sh
      to: node_modules/semver
      via: "node -e require('$PLUGIN_DIR/node_modules/semver') one-liner"
      pattern: "node_modules/semver"
    - from: scripts/release.sh
      to: marketplace.json source.ref
      via: "two-commit form pins source.ref == vN (commit A)"
      pattern: "source.ref|source\\[.ref.\\]|\\\"ref\\\""
    - from: tests/run-all.sh
      to: tests/test-release-bump-algebra.cjs
      via: "test-*.cjs glob picks it up"
      pattern: "test-release-bump-algebra"
---

<objective>
Plan-1 of 7. Make `scripts/release.sh` the ONLY thing that touches a version — including pre-releases. Today `release.sh:40` does `IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT"` which mangles `1.13.0-beta.11` into `PATCH=0-beta`, which is why beta.10/11/12/13 were all hand-rolled. This plan:

1. Adds the `semver` npm package as a **devDependency** (not `dependencies`, not the `files` allowlist — the published `@mindrian_os/install` tarball stays zero-runtime-dep). `release.sh` checks `node_modules/semver` exists and instructs `npm install` if missing (it does NOT auto-run `npm install`).
2. Replaces the `IFS='.' read` parse with `semver.inc()` calls via a `node -e` one-liner: `--prerelease` → `semver.inc(v,'prerelease','beta')`; `--finalize` (alias `release.sh stable`) → `semver.inc(v,'patch')` (CORRECTION to CONTEXT D-18: `semver.inc('1.13.0-beta.11','patch')` returns `1.13.0`, NOT `1.13.1`; `'minor'` also returns `1.13.0` — only `'major'` differs → `2.0.0`); `--start-prerelease` → `semver.inc(v,'preminor','beta')` then `semver.inc(...,'prerelease','beta')` → `1.14.0-beta.1`; bare `patch`/`minor`/`major` map to `semver.inc(v, mode)`. Refuses if `plugin.json` version is not valid semver.
3. Implements the **TWO-COMMIT next-bump form** (RESEARCH override 1, a verified fact not a judgment call): Claude Code resolves a plugin's installed version from `.claude-plugin/plugin.json` `version` FIRST, so the one-commit form (`vN` tag on a commit whose `plugin.json` already says `vN+1`) would make an install from `source.ref: vN` self-report `vN+1` — broken. Commit A = release commit (CHANGELOG `[vN] - date`, `plugin.json`/`package.json` == `vN`, marketplace `marketplace.json` `version` == `vN` AND `source.ref` == `vN`; `git tag vN` on commit A). Commit B = next-bump commit (`plugin.json`/`package.json` bumped to `vN+1`, CHANGELOG `[Unreleased] -- vN+1 (in progress)` heading reset). `main` HEAD ends on commit B. `release.sh` makes BOTH commits + the tag + pushes both repos.
4. Adds the dirty-repo / ahead-of-origin guard (D-20): before pushing, snapshot + print `git log origin/main..HEAD --oneline`; push only if the delta is exactly the release commit(s) (A and B); else abort: "N unpushed commits that aren't this release; push/stash them or pass `--allow-ahead`." Block on dirty *tracked* files except the ones `release.sh` itself bumped (`plugin.json`, `package.json`, `CHANGELOG.md`, marketplace's `marketplace.json`); untracked files OK.
5. Renames Step 9.5: `@mindrian_os/cli` → `@mindrian_os/install` everywhere (the publish, the dist-tag logic, the `npm pack --dry-run` payload-allowlist gate, the recovery instructions). Updates `tests/test-release-npm-gate.sh`'s 6 structural-gate expectations to match (still passes).
6. Wave-0 test: `tests/test-release-bump-algebra.cjs` — runs the actual `semver.inc` calls (incl. the patch-finalizes correction + the 1.12.5.1 non-semver case) + structural assertions on `release.sh` (no `IFS='.' read -r MAJOR MINOR PATCH`; contains `require('` for semver; has a `--prerelease` arm; has a `git log origin/main..HEAD` guard; has the two-commit form). Registered in `tests/run-all.sh` and `lib/memory/run-feynman-tests.cjs` `TEST_FILES[]`.

NOTE: `bin/cli.js` routing of `--acceptance` and wiring `doctor --acceptance --pre-tag` into `release.sh` is Plan-4's job, not this plan's. NOTE: Step 9.5's `@mindrian_os/cli` rename here is the `release.sh`-half of the override-8 sweep; the doc/test-file half is Plan-5.

Purpose: stop the version treadmill. Hard prerequisite for cutting `v1.13.0-beta.13` (Plan-6) via `release.sh` instead of by hand.
Output: a `release.sh` that handles pre-releases, refuses on a dirty repo, and publishes the right package; `semver` in `devDependencies`; the Wave-0 bump-algebra test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-install-lifecycle-harness/123-CONTEXT.md
@.planning/phases/123-install-lifecycle-harness/123-RESEARCH.md
@.claude/includes/release-process.md

<interfaces>
<!-- Verified semver 7.7.4 outputs (ran 2026-05-12; semver/package.json dependencies: {}). -->
semver.inc('1.13.0-beta.11','prerelease','beta')  -> '1.13.0-beta.12'
semver.inc('1.13.0-beta.11','patch')              -> '1.13.0'   (NOT 1.13.1; 'minor' also -> '1.13.0')
semver.inc('1.13.0-beta.11','major')              -> '2.0.0'
semver.inc('1.13.0','preminor','beta')            -> '1.14.0-beta.0'  (call inc once more with 'prerelease','beta' to get beta.1)
semver.inc('1.13.0','prepatch','beta')            -> '1.13.1-beta.0'
semver.valid('1.12.5.1')                          -> null   (4-component, NOT valid semver)
semver.coerce('1.12.5.1')                         -> '1.12.5'

<!-- Current release.sh structure (262 lines): Step 0 parse bump type; Step 1 get current version (line 40 IFS='.' read -- THE BUG); Step 2 verify-release; Step 3 bump plugin.json; Step 4 bump marketplace.json; Step 5/5b validate marketplace + reserved-name; Step 6 CHANGELOG entry check; Step 6.5 post-bump re-verify; Step 7 git commit plugin (specific files); Step 8 git commit marketplace; Step 9 push both; Step 9.5 npm publish @mindrian_os/cli (lines 172-221); Step 10 update local cache. -->
<!-- MARKETPLACE_DIR is ~/mindrian-marketplace; its main branch is `master` (release.sh:170 does `git push origin master`). -->
<!-- tests/test-release-npm-gate.sh is a 6-gate structural assertion on Step 9.5: (1) npm publish present, (2) ordering after Step 9 push, (3) dist-tag logic @next/@latest, (4) MOS_TEST_DRY_RUN hatch, (5) recovery message, (6) no em-dashes. It currently asserts @mindrian_os/cli -- change to @mindrian_os/install. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — add semver devDep + write tests/test-release-bump-algebra.cjs</name>
  <files>package.json, tests/test-release-bump-algebra.cjs, tests/run-all.sh, lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - package.json (current devDependencies block, the `files` allowlist, `dependencies` — confirm semver is not in any of them as a declared dep)
    - tests/test-release-npm-gate.sh (the structural-assertion style to mirror — grep-on-release.sh assertions, exit 0/1, no test framework)
    - tests/test-doctor-class-g.cjs (the .cjs test pattern: node:assert, spawnSync, exits 0 on all-pass)
    - tests/run-all.sh (the `test-*.cjs` glob — confirm a new tests/test-*.cjs is auto-picked up)
    - lib/memory/run-feynman-tests.cjs (lines ~33 TEST_FILES[] and ~1141 where the doctor-class tests register — add the new file there)
    - .planning/phases/123-install-lifecycle-harness/123-RESEARCH.md § "semver API, verified by running it" and § Pitfall 2 (the patch/minor correction) and § Pitfall 6 (the 1.12.5.1 case)
  </read_first>
  <action>
    1. In `package.json`: add `"semver": "^7.7.4"` to `devDependencies` (alphabetically sorted within the block). Do NOT add it to `dependencies`. Do NOT add `semver` to the `files` array. (semver@7.7.4 is already in `node_modules` transitively; this just declares it so a normal `npm install` keeps it.) Run `npm install` is NOT part of this task — the executor leaves `node_modules` alone; the test below requires `node_modules/semver` to exist (it does, transitively).
    2. Create `tests/test-release-bump-algebra.cjs` (CJS, `'use strict'`, node:assert, no test framework, `process.exit(0)` on all-pass / `process.exit(1)` on any fail, per-test PASS/FAIL prints). It must:
       - Test A: `const semver = require('semver')` (resolved from `node_modules`); assert `semver.inc('1.13.0-beta.11','prerelease','beta') === '1.13.0-beta.12'`.
       - Test B: assert `semver.inc('1.13.0-beta.11','patch') === '1.13.0'` AND `semver.inc('1.13.0-beta.11','minor') === '1.13.0'` (the CONTEXT-D-18 correction — patch finalizes, minor does NOT bump the minor).
       - Test C: assert `semver.inc('1.13.0-beta.11','major') === '2.0.0'`.
       - Test D: `let v = semver.inc('1.13.0','preminor','beta'); v = semver.inc(v,'prerelease','beta'); assert(v === '1.14.0-beta.1')`.
       - Test E: assert `semver.valid('1.12.5.1') === null` AND `semver.coerce('1.12.5.1').version === '1.12.5'` (the non-semver legacy version).
       - Test F (structural, on `scripts/release.sh` — read it with `fs.readFileSync`): assert it does NOT contain the literal `IFS='.' read -r MAJOR MINOR PATCH`; assert it contains `require(` (the semver one-liner); assert it contains `--prerelease`; assert it contains `git log origin/main..HEAD`; assert it contains `node_modules/semver` (the existence check); assert it contains `@mindrian_os/install` and does NOT contain `@mindrian_os/cli`.
       - Test G (structural, two-commit form): assert `scripts/release.sh` contains a marker comment for the two-commit form (e.g. `# Commit A (release commit)` and `# Commit B (next-bump commit)` — Task 2 will add these); assert it contains `--allow-ahead`. (This test will be RED until Task 2 lands — that is the expected RED→GREEN cycle.)
    3. In `tests/run-all.sh`: no edit needed if the `test-*.cjs` glob picks it up (verify by reading; if the glob is scoped, add the file explicitly).
    4. In `lib/memory/run-feynman-tests.cjs` `TEST_FILES[]`: add `path.join(REPO_ROOT, 'tests', 'test-release-bump-algebra.cjs')` near the other Phase-123/release tests (group it with a `// Phase 123` comment block — Plans 02/03/04/05/07 will add their tests to the same block).
  </action>
  <behavior>
    - Test A: semver.inc prerelease bumps beta.11 -> beta.12
    - Test B: semver.inc patch on a beta finalizes to X.Y.0 (not X.Y.1); minor also yields X.Y.0
    - Test C: semver.inc major on a beta -> (X+1).0.0
    - Test D: preminor+prerelease opens a fresh series at X.(Y+1).0-beta.1
    - Test E: 1.12.5.1 is not valid semver; coerce yields 1.12.5
    - Test F/G: release.sh structural shape (RED until Task 2, then GREEN)
  </behavior>
  <verify>
    <automated>node tests/test-release-bump-algebra.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); if(!p.devDependencies.semver) process.exit(1)"` exits 0
    - `node -e "const p=require('./package.json'); if(p.dependencies && p.dependencies.semver) process.exit(1)"` exits 0
    - `node -e "const p=require('./package.json'); if((p.files||[]).includes('semver')) process.exit(1)"` exits 0
    - `node -e "require('semver')"` exits 0 (semver resolvable from node_modules)
    - `test -f tests/test-release-bump-algebra.cjs` and the file contains the literals `semver.inc('1.13.0-beta.11','patch')` (or an equivalent assertion) and `1.12.5.1`
    - `grep -q "test-release-bump-algebra" lib/memory/run-feynman-tests.cjs`
    - `node tests/test-release-bump-algebra.cjs` Tests A-E pass; F/G are allowed to fail at this point (they go GREEN after Task 2)
  </acceptance_criteria>
  <done>semver in devDependencies only; tests/test-release-bump-algebra.cjs exists with the 7 tests; registered in the feynman runner; Tests A-E green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite release.sh — semver bump algebra + the two-commit form + the dirty-repo guard</name>
  <files>scripts/release.sh</files>
  <read_first>
    - scripts/release.sh (in full — the current Step 0..Step 10 structure; line 40's `IFS='.' read`; line 42-46's MAJOR/MINOR/PATCH math; Steps 7/8/9's git commit+tag+push; Step 9.5's npm block; MARKETPLACE_DIR / MARKETPLACE branch name)
    - .claude/includes/release-process.md (the 5-way version consistency rule + "marketplace source must be pinned to a ref")
    - .planning/phases/123-install-lifecycle-harness/123-RESEARCH.md § "Pattern 2: Two-commit next-bump" and § Pitfall 1 and § "The semver bump one-liner shape for release.sh"
    - tests/test-release-bump-algebra.cjs (the structural assertions Task 1 wrote — this task makes Tests F/G GREEN)
  </read_first>
  <action>
    Rewrite `scripts/release.sh` so it:

    **Step 0 — arg parse:** accept `--prerelease` | `--finalize` (alias: a bare `stable` first arg) | `--start-prerelease` | bare `patch`|`minor`|`major` | (default = `--prerelease` if the current version has a `-` suffix, else error "specify a bump type"); plus `--allow-ahead`. Reject unknown args with a usage block. Document the finalize semantics in the usage block (`--prerelease` -> beta.N+1; `--finalize` -> strips the suffix to X.Y.0; `--start-prerelease` -> X.(Y+1).0-beta.1).

    **Step 0.5 — semver preflight:** `if [ ! -d "$PLUGIN_DIR/node_modules/semver" ]; then echo "Run 'npm install' first -- release.sh needs the semver devDep."; exit 1; fi`. (Do NOT run `npm install` here — release.sh must not mutate the working tree's node_modules.)

    **Step 1 — current version + new version:** read `CURRENT` from `.claude-plugin/plugin.json` `version`. Compute `NEW_VERSION` via a `node -e` one-liner against `$PLUGIN_DIR/node_modules/semver`:
    ```bash
    NEW_VERSION="$(node -e "
      const semver = require('$PLUGIN_DIR/node_modules/semver');
      const cur = require('$PLUGIN_DIR/.claude-plugin/plugin.json').version;
      if (!semver.valid(cur)) { console.error('plugin.json version is not valid semver: ' + cur + ' -- fix it before releasing.'); process.exit(1); }
      const mode = process.argv[1];
      let out;
      if (mode === 'prerelease')           out = semver.inc(cur, 'prerelease', 'beta');
      else if (mode === 'finalize')        out = semver.inc(cur, 'patch');
      else if (mode === 'start-prerelease'){ out = semver.inc(cur, 'preminor', 'beta'); out = semver.inc(out, 'prerelease', 'beta'); }
      else if (mode === 'patch')           out = semver.inc(cur, 'patch');
      else if (mode === 'minor')           out = semver.inc(cur, 'minor');
      else if (mode === 'major')           out = semver.inc(cur, 'major');
      else { console.error('unknown bump mode: ' + mode); process.exit(1); }
      process.stdout.write(out);
    " "$BUMP_MODE")" || { echo 'version computation failed'; exit 1; }
    ```
    Delete the old `IFS='.' read -r MAJOR MINOR PATCH` and the `case "$BUMP" in ... esac` MAJOR/MINOR/PATCH math entirely.

    **Step 2 — verify-release** (keep as is — `bash "$PLUGIN_DIR/scripts/verify-release"`; Plan-4 will note the de-dup TODO).

    **Step 3-6 — the release commit (commit A) staging:** bump `.claude-plugin/plugin.json` and `package.json` to `NEW_VERSION`. Bump `~/mindrian-marketplace/.claude-plugin/marketplace.json` `plugins[0].version` to `NEW_VERSION` AND its `source.ref` to `v$NEW_VERSION` (currently Step 4 sets the version; ensure `source.ref` is also set/created). Keep Step 5/5b (validate marketplace + reserved-name). Keep Step 6 (CHANGELOG `## [NEW_VERSION] - $(date +%F)` finalized — change `[Unreleased]` to `[NEW_VERSION] - <date>`). Keep Step 6.5 (post-bump re-verify).

    **Step 7 — Commit A (release commit):** add comment `# --- Step 7: Commit A (release commit) -- finalizes vN, NEVER git add -A ---`. `git add .claude-plugin/plugin.json package.json CHANGELOG.md` (in $PLUGIN_DIR); `git commit -m "release: v$NEW_VERSION -- <one-line summary>"`; `git tag "v$NEW_VERSION"` (on commit A — the current `git tag` after the commit is correct, just under the new comment). Then in $MARKETPLACE_DIR: `git add .claude-plugin/marketplace.json && git commit -m "release: mos v$NEW_VERSION"` (this is the marketplace's commit A — there is no marketplace commit B).

    **Step 7.5 — Commit B (next-bump commit, plugin repo only):** add comment `# --- Step 7.5: Commit B (next-bump commit) -- plugin.json/package.json -> next pre-release ---`. Compute `NEXT_VERSION` via the same semver one-liner: if `NEW_VERSION` has a `-` suffix, `semver.inc(NEW_VERSION, 'prerelease', 'beta')`; if `NEW_VERSION` is clean `X.Y.Z`, use `--start-prerelease` semantics (`semver.inc` `prepatch`+`prerelease` -> `X.Y.(Z+1)-beta.1`) UNLESS the operator passed `--no-next-bump` (a new optional flag — if so, skip commit B; print a note that the repo is left at `NEW_VERSION` with no next-bump). Bump `.claude-plugin/plugin.json` + `package.json` to `NEXT_VERSION`. Reset the CHANGELOG: insert a new `## [Unreleased] -- v$NEXT_VERSION (in progress)` heading at the top (above the just-finalized `[NEW_VERSION]` entry). `git add .claude-plugin/plugin.json package.json CHANGELOG.md && git commit -m "chore: bump to v$NEXT_VERSION (next pre-release)"`. Do NOT tag commit B. (The marketplace repo gets NO commit B — `marketplace.json` only ever points at `vN`.)

    **Step 8 — Dirty-repo / ahead-of-origin guard (BEFORE the push):** add `# --- Step 8: dirty-repo / ahead-of-origin guard ---`. Run `git fetch origin main 2>/dev/null || true`. Then:
    ```bash
    AHEAD="$(git log origin/main..HEAD --oneline)"
    echo "Commits ahead of origin/main:"; echo "$AHEAD"
    AHEAD_COUNT="$(echo "$AHEAD" | grep -c . || true)"
    EXPECTED=2; [ "$NO_NEXT_BUMP" = "1" ] && EXPECTED=1
    if [ "$AHEAD_COUNT" -gt "$EXPECTED" ]; then
      if [ "$ALLOW_AHEAD" != "1" ]; then
        echo "Refusing to push: $AHEAD_COUNT commits ahead but only $EXPECTED are this release. Push/stash the rest, or pass --allow-ahead."; exit 1
      fi
      echo "  (--allow-ahead set; pushing $AHEAD_COUNT commits)"
    fi
    # dirty tracked files except the bumped ones:
    DIRTY="$(git status --porcelain --untracked-files=no | awk '{print $2}' | grep -vE '^(\.claude-plugin/plugin\.json|package\.json|CHANGELOG\.md)$' || true)"
    if [ -n "$DIRTY" ]; then echo "Refusing to push: dirty tracked files not part of this release:"; echo "$DIRTY"; exit 1; fi
    ```
    (Note: by Step 8, commits A and B are already committed, so the only "dirty" left would be a stray edit — the bumped files are clean.)

    **Step 9 — push both:** `cd "$PLUGIN_DIR" && git push origin main --tags` (pushes commit A, commit B, and the tag); `cd "$MARKETPLACE_DIR" && git push origin master` (pushes the marketplace's commit A).

    **Step 9.5 — npm publish:** RENAME `@mindrian_os/cli` → `@mindrian_os/install` everywhere in this block: the header comment (line 12), the `echo "=== Step 9.5: npm publish (@mindrian_os/install) ==="`, the `npm publish --tag "$NPM_TAG"` (unchanged command, but the printed package name in the success/failure messages becomes `@mindrian_os/install`), the recovery instructions ("the package.json name is '@mindrian_os/install'" / "Re-run JUST the publish: npm publish --tag $NPM_TAG" / "Verify: npm view @mindrian_os/install@$NPM_TAG version"). Keep the dist-tag logic (`-beta./-alpha./-rc./-next.` → `@next`; clean `X.Y.Z` → `@latest`), the `MOS_TEST_DRY_RUN=1` hatch, and the `npm pack --dry-run` payload-allowlist gate (the allowlist itself is the `package.json` `files` array — unchanged). NOTE: Step 9.5 publishes the package at the working-tree state, which after commit B is `NEXT_VERSION` — that is wrong. FIX: Step 9.5 must publish `NEW_VERSION` (the released version), not `NEXT_VERSION`. Either run Step 9.5 BEFORE Step 7.5 (publish from commit-A state, then make commit B), or `git stash`/checkout `plugin.json`+`package.json` to `NEW_VERSION` for the publish then restore. **Recommended:** move Step 9.5 to run between Step 9's push and Step 7.5's commit B — i.e. reorder: Step 7 (commit A + tag) → Step 9.5 (npm publish NEW_VERSION from the commit-A working tree) → Step 7.5 (commit B) → Step 8 (guard) → Step 9 (push both). Document the ordering with comments.

    **Step 10 — update local cache** (keep).

    Throughout: NO em-dashes (use hyphens) — `tests/test-release-npm-gate.sh` Gate 6 enforces this. Keep all `git add` calls explicit (NEVER `git add -A`).
  </action>
  <behavior>
    - `release.sh --prerelease` at v1.13.0-beta.12: commit A has plugin.json/package.json == 1.13.0-beta.13 + CHANGELOG `[1.13.0-beta.13] - <date>` + tag v1.13.0-beta.13; commit B has plugin.json/package.json == 1.13.0-beta.14 + CHANGELOG `[Unreleased] -- v1.13.0-beta.14 (in progress)`; main HEAD on commit B; marketplace.json version == 1.13.0-beta.13 AND source.ref == v1.13.0-beta.13
    - `release.sh` with 3+ commits ahead of origin/main and no --allow-ahead: aborts before pushing
    - `release.sh` with a dirty tracked file other than plugin.json/package.json/CHANGELOG.md: aborts
    - `release.sh` when plugin.json version is `1.12.5.1`: aborts with "not valid semver"
    - Step 9.5 publishes @mindrian_os/install at NEW_VERSION (not NEXT_VERSION)
  </behavior>
  <verify>
    <automated>node tests/test-release-bump-algebra.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "IFS='.' read -r MAJOR MINOR PATCH" scripts/release.sh` returns FALSE (the literal is gone)
    - `grep -q "require(" scripts/release.sh` returns TRUE; `grep -q "node_modules/semver" scripts/release.sh` returns TRUE
    - `grep -q -- "--prerelease" scripts/release.sh` and `grep -q -- "--start-prerelease" scripts/release.sh` and `grep -q -- "--allow-ahead" scripts/release.sh` all return TRUE
    - `grep -q "git log origin/main..HEAD" scripts/release.sh` returns TRUE
    - `grep -q "Commit A" scripts/release.sh` and `grep -q "Commit B" scripts/release.sh` return TRUE
    - `grep -q "@mindrian_os/install" scripts/release.sh` returns TRUE; `grep -q "@mindrian_os/cli" scripts/release.sh` returns FALSE
    - `bash -n scripts/release.sh` exits 0 (valid shell syntax)
    - `node tests/test-release-bump-algebra.cjs` exits 0 (all 7 tests green, including F and G)
    - `grep -nP "\xe2\x80\x94" scripts/release.sh` returns nothing (no em-dashes)
  </acceptance_criteria>
  <done>release.sh has the semver bump algebra, the two-commit form (A: release+tag, B: next-bump), the dirty-repo/ahead guard, and the @mindrian_os/install Step 9.5; `bash -n` clean; the bump-algebra test green.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests/test-release-npm-gate.sh expectations to @mindrian_os/install</name>
  <files>tests/test-release-npm-gate.sh</files>
  <read_first>
    - tests/test-release-npm-gate.sh (in full — the 6 structural gates, what each greps for in scripts/release.sh, the `@mindrian_os/cli` literals it asserts)
    - scripts/release.sh (the now-rewritten Step 9.5 — confirm the literals the test should expect)
  </read_first>
  <action>
    In `tests/test-release-npm-gate.sh`, change every `@mindrian_os/cli` literal to `@mindrian_os/install` (Gate 1 = the `npm publish` line / package name; Gate 5 = the recovery message; any others). Re-confirm the 6 gates still describe the rewritten `release.sh`:
    - Gate 1: `release.sh` contains `npm publish --tag` and references `@mindrian_os/install`
    - Gate 2: the `npm publish` block appears AFTER the `git push origin main` line (Step 9.5 is after Step 9 — note: per Task 2 the publish was moved to run between Step 9's push and commit B, so it's still after the push line for the plugin repo; if the structural ordering check is brittle, relax it to "after `git push origin main`")
    - Gate 3: the `@next` / `@latest` dist-tag conditional is present (`-beta./-alpha./-rc./-next.` → `@next`)
    - Gate 4: `MOS_TEST_DRY_RUN` hatch present
    - Gate 5: the recovery instructions block references `@mindrian_os/install` and `npm view @mindrian_os/install@`
    - Gate 6: `grep -nP "\xe2\x80\x94"` (em-dash) on `release.sh` returns nothing
    Run the test; it must exit 0.
  </action>
  <verify>
    <automated>bash tests/test-release-npm-gate.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@mindrian_os/install" tests/test-release-npm-gate.sh` returns TRUE
    - `grep -q "@mindrian_os/cli" tests/test-release-npm-gate.sh` returns FALSE
    - `bash tests/test-release-npm-gate.sh` exits 0 (all 6 gates pass against the rewritten release.sh)
  </acceptance_criteria>
  <done>test-release-npm-gate.sh asserts @mindrian_os/install and passes against the rewritten release.sh.</done>
</task>

</tasks>

<verification>
- `bash -n scripts/release.sh` clean.
- `node tests/test-release-bump-algebra.cjs` exits 0 (all 7 tests).
- `bash tests/test-release-npm-gate.sh` exits 0 (6 gates).
- `node -e "const p=require('./package.json'); process.exit(p.devDependencies.semver && !(p.dependencies||{}).semver && !(p.files||[]).includes('semver') ? 0 : 1)"` exits 0.
- `grep -rn "@mindrian_os/cli" scripts/release.sh tests/test-release-npm-gate.sh` returns nothing.
- Canon Part 8: no new network in `scripts/release.sh` beyond the pre-existing `npm publish` / `claude plugin marketplace update` (release-time only). `grep -E "fetch|http|curl|brain.mindrian|tavily" scripts/release.sh` should return only the pre-existing release-pipeline lines (none new from this plan).
</verification>

<success_criteria>
- `scripts/release.sh` no longer parses versions with `IFS='.' read`; uses `semver.inc()` via a `node -e` one-liner; supports `--prerelease` / `--finalize` / `--start-prerelease` / bare `patch|minor|major`; refuses on a non-semver `plugin.json` version.
- `release.sh` implements the TWO-COMMIT form (commit A: finalize + tag `vN` with `plugin.json == vN`; commit B: bump to `vN+1`; `main` HEAD on B); `marketplace.json` `version` AND `source.ref` pinned to `vN` on commit A.
- `release.sh` has the dirty-repo / ahead-of-origin guard (`git log origin/main..HEAD`; refuse if >release commits ahead unless `--allow-ahead`; block on dirty tracked files except the bumped ones).
- `release.sh` Step 9.5 publishes `@mindrian_os/install` at `NEW_VERSION` with `@next`/`@latest`; recovery instructions updated.
- `semver` in `package.json` `devDependencies` only (not `dependencies`, not `files`).
- `tests/test-release-bump-algebra.cjs` (7 tests) and `tests/test-release-npm-gate.sh` (6 gates) both green; the bump test registered in the feynman runner.
</success_criteria>

<output>
After completion, create `.planning/phases/123-install-lifecycle-harness/123-01-SUMMARY.md`
</output>
