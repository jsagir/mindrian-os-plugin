---
phase: 123-install-lifecycle-harness
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/core/active-plugin-root.cjs
  - data/deployment-surfaces.json
  - data/ROOM.md
  - scripts/session-start
  - tests/test-install-state-record.cjs
  - lib/memory/run-feynman-tests.cjs
autonomous: true
requirements: [HARNESS-123-05, HARNESS-123-06]
canon_parts: [5, 6]

must_haves:
  truths:
    - "After a session starts, `~/.mindrian/install-state.json` exists with active_version, active_root, topology, resolved_at, surfaces[], and the 4 version-of-record cross-check values (installed_plugins_version, statusline_renders_version, last_version_file_value, path_bin_version)"
    - "`~/.mindrian-last-version` is rewritten to match the resolved active_version on EVERY session start (incl. a session WITH an active room — the Pitfall-7 bug is fixed)"
    - "`scripts/session-start` is the single writer of both `~/.mindrian/install-state.json` and `~/.mindrian-last-version`; no other SessionStart hook writes either"
    - "`data/deployment-surfaces.json` is valid JSON with 6 surface entries; the pre-commit-hook entry has topology_scope: dev-clone; all paths use a literal $HOME token (no absolute paths)"
    - "`lib/core/active-plugin-root.cjs` returns a `topology` field (marketplace-cache | dev-clone | legacy | not-found)"
  artifacts:
    - path: data/deployment-surfaces.json
      provides: "the static deployment-surface manifest (6 surfaces, D-07 schema)"
      contains: "topology_scope"
    - path: lib/core/active-plugin-root.cjs
      provides: "the one resolver, now returning topology"
      contains: "topology"
      min_lines: 142
    - path: scripts/session-start
      provides: "the single writer of the install-state record + ~/.mindrian-last-version + the on-version-change owned-surface reconcile"
      contains: "install-state.json"
      min_lines: 1328
    - path: tests/test-install-state-record.cjs
      provides: "hermetic HOME test: record full-snapshot fields + single-writer + the Pitfall-7 no-room fix"
      min_lines: 60
  key_links:
    - from: scripts/session-start
      to: lib/core/active-plugin-root.cjs
      via: "node -e require(resolveActivePluginRoot) to get active_version/active_root/topology"
      pattern: "active-plugin-root"
    - from: scripts/session-start
      to: data/deployment-surfaces.json
      via: "walk the manifest to reconcile owned surfaces on version change"
      pattern: "deployment-surfaces"
    - from: scripts/session-start
      to: "~/.mindrian/install-state.json"
      via: "single-writer write in the earliest steps, unconditional"
      pattern: "install-state\\.json"
---

<objective>
Plan-2 of 7. Build the install-state record and the deployment-surface manifest — the two data artifacts the contract rests on.

1. **Extend `lib/core/active-plugin-root.cjs`** to add a `topology` field to its return shape (`marketplace-cache | dev-clone | legacy | not-found`). It already has the 4 cases as resolution branches; surface them. `dev-clone` detection MUST be robust per RESEARCH Pitfall 4: `dev-clone` = `MINDRIAN_OS_ROOT` is set AND points at the resolved root, OR the path is the canonical `~/MindrianOS-Plugin` dir with a `.git` AND an `install.sh` AND `git remote get-url origin` resolves to a mindrian-os-plugin repo. `legacy` is specifically `~/.claude/plugins/mindrian-os/` (the obsolete install-cache clone — note the live one on the dev box has its `origin` pointing at `mindrian-agno-backend.git`, so a naive "has an origin remote → dev-clone" test mis-classifies it). `marketplace-cache` = the path is under `~/.claude/plugins/cache/<mp>/mos/<v>/`. `not-found` = `root` is null. Keep the existing `{root, source}` fields byte-stable; this is additive.

2. **`data/deployment-surfaces.json`** — a static, hand-maintained JSON file (NOT generated, NO `--check` tripwire — it isn't derived from anything, unlike `command-registry.json`; reuse only the `data/` layout convention). The 6 surfaces from CONTEXT D-08 with the D-07 fields (`id, path, owner, topology_scope, check_kind ∈ {marker, exact-value, observed-only}, expected, reconcile ∈ {on-version-change, never}, remediation`). Paths use a literal `$HOME` token (expanded at read time via `os.homedir()` — never absolute). Surfaces: (1) `statusline-dispatch-shim` (`$HOME/.claude/statusline-mos`, owner session-start, scope all, marker `MINDRIAN-STATUSLINE-DISPATCH`, reconcile on-version-change); (2) `settings-statusline-command` (`$HOME/.claude/settings.json`, owner session-start, scope all, exact-value `bash "$HOME/.claude/statusline-mos"`, reconcile on-version-change); (3) `mindrian-last-version` (`$HOME/.mindrian-last-version`, owner session-start, scope all, exact-value `<active_version>`, reconcile on-version-change); (4) `install-state-record` (`$HOME/.mindrian/install-state.json`, owner session-start, scope all, observed-only, expected null, reconcile never — self, excluded from its own check); (5) `plugin-bin-path-entry` (`<active_root>/bin`, owner claude-code, scope all, observed-only, expected null, reconcile never — observed for the cross-check, never written); (6) `dev-clone-pre-commit-hook` (`<dev_clone_root>/.git/hooks/pre-commit`, owner session-start, scope `dev-clone`, marker = the ROOM.md/MINTO.md guard sentinel from `scripts/install-pre-commit.sh`, reconcile on-version-change — `session-start` idempotently installs/updates it via `scripts/install-pre-commit.sh`; skipped entirely on a user box). Use the exact sketch in 123-RESEARCH.md § "The deployment-surfaces manifest (sketch of the 6 surfaces, D-08)" as the starting point. Update `data/ROOM.md` to mention `deployment-surfaces.json` (keep the ICM Layer-0 rule — every dir gets a ROOM.md; `data/` already has one).

3. **`scripts/session-start` — the single writer.** In its EARLIEST steps (after the workspace guard / node preflight, before the stable-prefix render and before any downstream reader): (a) call `active-plugin-root.cjs` via a `node -e` to get `active_version` / `active_root` / `topology`; (b) collect the 4 version-of-record cross-check values (`installed_plugins_version` from `~/.claude/plugins/installed_plugins.json` `plugins["mos@mindrian-marketplace"][0].version`; `statusline_renders_version` from invoking `scripts/statusline-mos` and parsing the version it prints — defensive, default `unknown` on any failure; `last_version_file_value` = the *previous* `~/.mindrian-last-version` content read at line ~101, kept for the transition banner; `path_bin_version` = parse `$PATH` for a `.../mos/<v>/bin` entry, default `unknown`); (c) write `~/.mindrian/install-state.json` (mkdir `-p ~/.mindrian` first) as a full snapshot `{active_version, active_root, topology, resolved_at, surfaces:[...], installed_plugins_version, statusline_renders_version, last_version_file_value, path_bin_version}` — the `surfaces[]` array is `{id, path (expanded), check_kind, expected, observed, ok}` for the manifest's owned + observed-only surfaces; (d) write `~/.mindrian-last-version` = `active_version` (the new value) in the SAME step; (e) **REMOVE the existing `~/.mindrian-last-version` write at ~line 419** (the cold-start-branch-only write — Pitfall 7); (f) **KEEP the read of the previous `~/.mindrian-last-version` value at ~line 101** (used for the version-change transition banner) — read-before-write ordering: read at ~101, banner render at ~401-419, then the new record + new-value write happens AFTER the banner (the banner is still early, before the room-context fork; "before any reader" means before statusline/bin/cli.js/downstream consumers read the *record*, which they do much later or in a different process). Confirm no OTHER SessionStart hook in `hooks/hooks.json` (`check-onboard-statusline.cjs` / `preflight-doctor.cjs` / `preflight-release-drift.cjs` / `sessionstart-npm-reconcile.cjs`) writes `~/.mindrian-last-version` (RESEARCH says it's already the single writer — verify in `read_first` and note in the SUMMARY). (g) **On version change** (`active_version != last_version_file_value`; an absent previous value counts as a change so a fresh install gets stamped on first session): walk `data/deployment-surfaces.json` and reconcile the owned (`owner: session-start`) surfaces whose `reconcile == on-version-change` — re-stamp the statusline dispatch shim (the existing Step A logic already does this; tie it to the manifest), re-stamp the `settings.json` `statusLine.command` line (the existing Step B logic), rewrite `~/.mindrian-last-version` (already done in step d), and on a `dev-clone` topology, idempotently install/update the pre-commit hook via `scripts/install-pre-commit.sh` (the existing hooks block already calls this — tie it to the manifest's `dev-clone-pre-commit-hook` surface). Surfaces with `topology_scope: dev-clone` are skipped on a non-dev-clone box. Keep ALL of this Canon-Part-8 clean: zero `fetch`/`http`/`curl`/`brain.mindrian`/`tavily` in the new lines.

   NOTE: this plan does NOT add the doctor classes (Plan-3), the `--acceptance` gate (Plan-4), the cache prune (Plan-5), or the Brain status line (Plan-7). It writes the record + manifest and wires the on-version-change reconcile. The cache-prune call in `session-start` (Plan-5) and the Brain status line (Plan-7) both touch `session-start` and are sequenced AFTER this plan.

4. **Wave-0 test:** `tests/test-install-state-record.cjs` — hermetic `HOME`/`USERPROFILE` scratch dir with an active-room fixture; spawn `scripts/session-start` (or invoke the record-write path directly if `session-start` is too heavy to spawn in a test — mirror how `tests/test-doctor-class-g.cjs` spawns a child with a scratch HOME); assert `~/.mindrian/install-state.json` exists with all the D-04 fields; assert `~/.mindrian-last-version` was written to match `active_version` EVEN with an active room present (the Pitfall-7 regression); assert running it twice is idempotent (second run with the same version does not re-stamp owned surfaces beyond a no-op); a grep sweep that the new `session-start` lines contain no `fetch|http|curl|brain.mindrian|tavily`. Register in `lib/memory/run-feynman-tests.cjs` `TEST_FILES[]` (the Phase-123 block).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-install-lifecycle-harness/123-CONTEXT.md
@.planning/phases/123-install-lifecycle-harness/123-RESEARCH.md

<interfaces>
<!-- lib/core/active-plugin-root.cjs current shape (verified): module.exports = { resolveActivePluginRoot }; returns { root: <path|null>, source: 'MINDRIAN_OS_ROOT'|'installed_plugins.json'|'marketplace-cache'|'legacy-clone'|'not-found' }. Internal helpers: isPluginDir(dir), fromInstalledPlugins(home), fromMarketplaceCache(home), fromLegacyClone(home). EXTEND: add `topology` to the return object; add a classifyTopology(root, source) helper. -->
<!-- installed_plugins.json shape (live): { "version": 2, "plugins": { "mos@mindrian-marketplace": [ { "scope": "user", "installPath": ".../cache/mindrian-marketplace/mos/1.12.5.1", "version": "1.12.5.1", "gitCommitSha": "..." } ] } }. active version = entry.version; active root = entry.installPath; plugin-bin = entry.installPath + '/bin'. -->
<!-- session-start key line numbers (verified): :87 ROOM_DIR resolve; :92 PLUGIN_VERSION via platform.cjs.readPluginJsonVersion; :101-104 LAST_VERSION_FILE read; :112 stable_prefix; :149 if [ -d "$ROOM_DIR" ]; :401-419 transition-banner block (the :419 echo "$PLUGIN_VERSION" > "$LAST_VERSION_FILE" is INSIDE the cold-start else branch -- REMOVE it, fold into the new early write); :1078-1130 Step A (statusline dispatch shim) + Step B (settings.json statusLine migration); the hooks block (where install-pre-commit.sh is called) is further down. -->
<!-- scripts/install-pre-commit.sh (Phase 108): the dev-clone pre-commit-hook installer; session-start already calls it in the hooks block; reuse it for the manifest's dev-clone-pre-commit-hook surface. The marker sentinel it writes into .git/hooks/pre-commit is the value to put in the manifest's `expected` for that surface -- read install-pre-commit.sh to get the exact sentinel string. -->
<!-- data/command-registry.json + data/ROOM.md (Phase 122): the `data/` layout precedent -- a checked-in JSON both session-start and doctor read. ROOM.md per dir is the ICM Layer-0 rule. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — write tests/test-install-state-record.cjs (RED) + extend active-plugin-root.cjs with topology</name>
  <files>lib/core/active-plugin-root.cjs, tests/test-install-state-record.cjs, lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - lib/core/active-plugin-root.cjs (in full — the current return shape, the 4 resolution branches, isPluginDir/fromInstalledPlugins/fromMarketplaceCache/fromLegacyClone)
    - tests/test-doctor-class-g.cjs (the makeTmpHome / runDoctor / spawnSync-with-scratch-HOME hermetic pattern to mirror)
    - tests/test-doctor-atomic-swap.cjs (the MINDRIAN_PLUGIN_HOME scenario-builder style)
    - .planning/phases/123-install-lifecycle-harness/123-RESEARCH.md § Pitfall 4 (the dev-clone-vs-legacy distinction) and § "Resolving the active plugin root + topology" code example and § Pitfall 7 (the ~/.mindrian-last-version no-room bug)
    - lib/memory/run-feynman-tests.cjs (the Phase-123 TEST_FILES[] block Plan-1 started)
  </read_first>
  <action>
    1. Extend `lib/core/active-plugin-root.cjs`: add a `classifyTopology(root, source)` helper:
       - `if (!root) return 'not-found';`
       - `if (source === 'MINDRIAN_OS_ROOT') return 'dev-clone';` (the env-override path is always a dev clone)
       - if `root` is under `<home>/.claude/plugins/cache/` and matches `.../mos/<version>/` → `'marketplace-cache'`
       - if `path.resolve(root) === path.resolve(home, '.claude/plugins/mindrian-os')` → `'legacy'`
       - if `fs.existsSync(path.join(root, '.git'))` AND `fs.existsSync(path.join(root, 'install.sh'))` AND `git -C <root> remote get-url origin` (via `child_process.execSync`, swallow errors) contains `mindrian-os-plugin` (case-insensitive) → `'dev-clone'`
       - else: if `source === 'legacy-clone'` → `'legacy'`; else `'marketplace-cache'` (defensive default — a resolved plugin dir that isn't a recognized dev clone is treated as a cache install)
       Update `resolveActivePluginRoot()` to add `topology: classifyTopology(root, source)` to its return object (every return path). Keep `root` and `source` byte-stable. Keep `module.exports = { resolveActivePluginRoot }` — optionally also export `classifyTopology` for the doctor classes (Plan-3) to reuse.
    2. Create `tests/test-install-state-record.cjs` (CJS, `'use strict'`, node:assert, no framework, exit 0/1, per-test PASS/FAIL). It must:
       - Test 1 (topology): `const { resolveActivePluginRoot } = require('../lib/core/active-plugin-root.cjs'); const r = resolveActivePluginRoot(); assert(['marketplace-cache','dev-clone','legacy','not-found'].includes(r.topology))` — and with `MINDRIAN_OS_ROOT` set to a scratch dir containing `.git`+`install.sh`, assert `r.topology === 'dev-clone'`.
       - Test 2 (record write — hermetic): mkdtempSync a scratch HOME; create a minimal active-room fixture under it (`.room-root` + STATE.md + ROOM.md is enough — borrow the shape from `test/fixtures/cascade-surface-e2e/` if present); set `~/.mindrian-last-version` in the scratch HOME to a deliberately-wrong value (`0.0.0-stale`); spawn `scripts/session-start` with `HOME`/`USERPROFILE` = the scratch dir and a CLAUDE_PLUGIN_ROOT pointing at the repo (mirror how the doctor-class-g test spawns); after it exits, assert `<scratchHOME>/.mindrian/install-state.json` exists, parses as JSON, and has the keys `active_version, active_root, topology, resolved_at, surfaces, installed_plugins_version, statusline_renders_version, last_version_file_value, path_bin_version`; assert `surfaces` is an array; assert `<scratchHOME>/.mindrian-last-version` content is now `active_version` (NOT `0.0.0-stale` — the Pitfall-7 fix: even with an active room, it gets rewritten).
       - Test 3 (single-writer / idempotent): run `session-start` a second time against the same scratch HOME (same version); assert the record still parses and `~/.mindrian-last-version` is unchanged (no churn).
       - Test 4 (Canon Part 8 grep sweep): `assert(spawnSync('grep', ['-nE', 'fetch|http|curl|brain.mindrian|tavily', 'scripts/session-start']).status === 0 ? <only pre-existing matches> : ...)` — implement this as: read the git diff of `session-start` for this plan is not available in-test, so instead grep the *new* record-write block (between two new marker comments the executor adds, e.g. `# --- BEGIN install-state record (Phase 123) ---` ... `# --- END install-state record ---`) and assert that span contains none of `fetch|http|curl|brain.mindrian|tavily`. (Note: this test goes GREEN in Task 2 when the markers + block exist.)
       - Test 5 (manifest schema — folded here OR moved to its own assert): `const m = require('../data/deployment-surfaces.json'); assert(Array.isArray(m.surfaces) && m.surfaces.length === 6); for (const s of m.surfaces) { assert(s.id && s.path && s.owner && s.topology_scope && s.check_kind && ('expected' in s) && s.reconcile && s.remediation); assert(s.path.includes('$HOME') || s.path.includes('<active_root>') || s.path.includes('<dev_clone_root>')); } assert(m.surfaces.some(s => s.id.includes('pre-commit') && s.topology_scope === 'dev-clone'));` — (RED until Task 2/3 land the manifest).
       - Register `path.join(REPO_ROOT, 'tests', 'test-install-state-record.cjs')` in `lib/memory/run-feynman-tests.cjs` `TEST_FILES[]` (the Phase-123 block).
  </action>
  <behavior>
    - Test 1: resolveActivePluginRoot() returns a valid topology; MINDRIAN_OS_ROOT pointing at a dev-clone-shaped dir -> topology 'dev-clone'
    - Test 2: session-start writes ~/.mindrian/install-state.json with all 9 D-04 keys; ~/.mindrian-last-version is rewritten to active_version even with an active room
    - Test 3: a second session-start at the same version is idempotent (no churn)
    - Test 4: the new record-write block in session-start has no network calls
    - Test 5: data/deployment-surfaces.json has 6 schema-conformant entries with $HOME-token paths and the dev-clone-scoped pre-commit surface
  </behavior>
  <verify>
    <automated>node tests/test-install-state-record.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const {resolveActivePluginRoot}=require('./lib/core/active-plugin-root.cjs'); const r=resolveActivePluginRoot(); process.exit(typeof r.topology==='string' && ['marketplace-cache','dev-clone','legacy','not-found'].includes(r.topology) ? 0 : 1)"` exits 0
    - `grep -q "topology" lib/core/active-plugin-root.cjs` returns TRUE
    - `test -f tests/test-install-state-record.cjs`
    - `grep -q "test-install-state-record" lib/memory/run-feynman-tests.cjs`
    - `node tests/test-install-state-record.cjs` Test 1 passes (Tests 2-5 may be RED until Tasks 2-3 land)
  </acceptance_criteria>
  <done>active-plugin-root.cjs returns topology; the hermetic record test exists (Test 1 green, 2-5 RED pending); registered in the feynman runner.</done>
</task>

<task type="auto">
  <name>Task 2: Write the install-state record + the on-version-change reconcile into scripts/session-start (single writer)</name>
  <files>scripts/session-start</files>
  <read_first>
    - scripts/session-start (in full — the workspace guard, the node preflight, :92 PLUGIN_VERSION, :101-104 LAST_VERSION_FILE read, :149 the `if [ -d "$ROOM_DIR" ]` fork, :401-419 the transition-banner block incl. the :419 write to remove, :1078-1130 Step A/B statusline migration, the hooks block where install-pre-commit.sh is called)
    - data/deployment-surfaces.json (once Task 3 lands — the manifest the reconcile walks; if Task 3 runs after this task in the same plan execution, the executor coordinates ordering — recommend doing Task 3 before Task 2)
    - lib/core/active-plugin-root.cjs (the now-extended resolver with `topology`)
    - scripts/install-pre-commit.sh (the dev-clone hook installer + the sentinel marker)
    - hooks/hooks.json (the SessionStart hooks roster — confirm `check-onboard-statusline.cjs` / `preflight-doctor.cjs` / `preflight-release-drift.cjs` / `sessionstart-npm-reconcile.cjs` do NOT write `~/.mindrian-last-version`)
    - .planning/phases/123-install-lifecycle-harness/123-RESEARCH.md § Pitfall 7 + § Open Question 3 (the record-write vs banner ordering)
  </read_first>
  <action>
    Edit `scripts/session-start`:

    **A. The early record-write block.** Insert a new block AFTER the transition-banner render (i.e. after line ~419's old write, which you are removing) but still BEFORE the room-context fork's downstream consumers — practically: keep :101's read of `LAST_VERSION` (the *previous* value), keep the banner render at :401-407, then DELETE the `echo "$PLUGIN_VERSION" > "$LAST_VERSION_FILE"` at :419, and immediately after the banner block insert:
    ```bash
    # --- BEGIN install-state record (Phase 123, single writer) ---
    # Canon Part 8: LOCAL only -- no fetch/http/curl/brain/tavily here.
    mkdir -p "$HOME/.mindrian" 2>/dev/null || true
    _INSTALL_STATE_JSON="$HOME/.mindrian/install-state.json"
    node -e '
      const fs = require("fs"); const os = require("os"); const path = require("path");
      const home = os.homedir();
      let r = { root: null, source: "not-found", topology: "not-found" };
      try { r = require(process.env.PLUGIN_ROOT_FOR_RESOLVE + "/lib/core/active-plugin-root.cjs").resolveActivePluginRoot(); } catch (_) {}
      // version-of-record cross-checks
      let installedPluginsVersion = "unknown";
      try {
        const ip = JSON.parse(fs.readFileSync(path.join(home, ".claude/plugins/installed_plugins.json"), "utf8"));
        const arr = ip.plugins && ip.plugins["mos@mindrian-marketplace"];
        if (Array.isArray(arr) && arr[0] && arr[0].version) installedPluginsVersion = arr[0].version;
      } catch (_) {}
      const lastVersionFile = path.join(home, ".mindrian-last-version");
      let lastVersionFileValue = null;
      try { lastVersionFileValue = fs.readFileSync(lastVersionFile, "utf8").trim() || null; } catch (_) {}
      let pathBinVersion = "unknown";
      try { const m = (process.env.PATH || "").match(/\/mos\/([^\/]+)\/bin(?::|$)/); if (m) pathBinVersion = m[1]; } catch (_) {}
      const activeVersion = process.env.PLUGIN_VERSION_FOR_RESOLVE || installedPluginsVersion || "unknown";
      // surfaces[] -- walk the manifest, record observed/ok per surface
      let surfaces = [];
      try {
        const man = JSON.parse(fs.readFileSync(process.env.PLUGIN_ROOT_FOR_RESOLVE + "/data/deployment-surfaces.json", "utf8"));
        const expand = (p) => p.replace("$HOME", home).replace("<active_root>", r.root || "").replace("<dev_clone_root>", r.topology === "dev-clone" ? (r.root || "") : "");
        for (const s of (man.surfaces || [])) {
          if (s.topology_scope === "dev-clone" && r.topology !== "dev-clone") continue;
          const fp = expand(s.path);
          let observed = null, ok = null;
          try {
            if (s.check_kind === "marker") { const c = fs.readFileSync(fp, "utf8"); observed = c.includes(s.expected) ? s.expected : null; ok = !!observed; }
            else if (s.check_kind === "exact-value") { let c = ""; try { c = fs.readFileSync(fp, "utf8"); } catch (_) {} const exp = (s.expected === "<active_version>") ? activeVersion : s.expected.replace("$HOME", home); observed = c.trim(); ok = c.includes(exp); }
            else { observed = fs.existsSync(fp) ? "present" : "absent"; ok = null; } // observed-only
          } catch (_) { observed = "absent"; ok = (s.check_kind === "observed-only") ? null : false; }
          surfaces.push({ id: s.id, path: fp, check_kind: s.check_kind, expected: s.expected, observed, ok });
        }
      } catch (_) {}
      const rec = { active_version: activeVersion, active_root: r.root, topology: r.topology, resolved_at: new Date().toISOString(), surfaces, installed_plugins_version: installedPluginsVersion, statusline_renders_version: process.env.STATUSLINE_RENDERS_VERSION || "unknown", last_version_file_value: lastVersionFileValue, path_bin_version: pathBinVersion };
      fs.writeFileSync(process.env.INSTALL_STATE_JSON_PATH, JSON.stringify(rec, null, 2));
      fs.writeFileSync(lastVersionFile, activeVersion + "\n");
    ' PLUGIN_ROOT_FOR_RESOLVE="$PLUGIN_ROOT" PLUGIN_VERSION_FOR_RESOLVE="$PLUGIN_VERSION" STATUSLINE_RENDERS_VERSION="$_STATUSLINE_RENDERS_VERSION" INSTALL_STATE_JSON_PATH="$_INSTALL_STATE_JSON" 2>/dev/null || true
    # --- END install-state record ---
    ```
    (Pass env via the `node -e ... NAME=val` trailing-arg form OR an `env NAME=val node -e ...` wrapper — match the existing `session-start` style; the existing file uses `env ... node -e` for the BANNER_MINTO / TRIPLE_CONTEXT blocks, so use `env`.)

    For `_STATUSLINE_RENDERS_VERSION`: just before the record block, set `_STATUSLINE_RENDERS_VERSION=$(bash "${WRAPPER_DST}" 2>/dev/null | grep -oE 'v?[0-9]+\.[0-9]+\.[0-9]+[^ ]*' | head -1 | sed 's/^v//' || echo "unknown")` — defensive; `WRAPPER_DST` is the deployed statusline shim path (already a variable in `session-start` Step A/B). If `WRAPPER_DST` isn't set yet at the record-write point (it's set in Step A around :1079), either move the record block to after Step A/B, OR compute `_STATUSLINE_RENDERS_VERSION` lazily as `unknown` and accept it (the record is still useful; the doctor class can compute it live). **Recommended:** put the record-write block AFTER Step A/B (so `WRAPPER_DST` exists and the shim is fresh), which is still before `session-start`'s exit and before any *separate process* reads the record. Update the ordering note in the SUMMARY.

    **B. Remove the old write.** Delete the `echo "$PLUGIN_VERSION" > "$LAST_VERSION_FILE" 2>/dev/null || true` line at ~:419 (the cold-start-branch-only write). The new block above is now the only writer of `~/.mindrian-last-version`.

    **C. The on-version-change reconcile.** Wrap the manifest-driven owned-surface reconcile in a version-change guard: `if [ "$PLUGIN_VERSION" != "$LAST_VERSION" ] || [ -z "$LAST_VERSION" ]; then ... fi` — inside: the existing Step A (re-stamp the statusline dispatch shim) and Step B (re-stamp `settings.json` `statusLine.command`) already run unconditionally; leave them running unconditionally (re-stamping a marker shim is cheap and idempotent), but ADD a comment that they realize the manifest's `statusline-dispatch-shim` and `settings-statusline-command` surfaces. Inside the version-change guard, ADD: if the resolved topology is `dev-clone`, idempotently install/update the pre-commit hook via `bash "${PLUGIN_ROOT}/scripts/install-pre-commit.sh"` (the hooks block already does this — tie it to the manifest's `dev-clone-pre-commit-hook` surface; ensure it's only run when topology is `dev-clone`, never on a user box). The reconcile must NOT add any network. (The cache prune — Plan-5 — and the Brain status line — Plan-7 — will be added inside / near this same guard by their respective plans.)

    **D. Verify no other hook writes `~/.mindrian-last-version`.** Read `hooks/hooks.json` and the named SessionStart hook scripts; confirm none writes that file. If one does, consolidate it into this block and note it in the SUMMARY.

    Throughout: no em-dashes; `bash -n scripts/session-start` must stay clean; keep all `git`/file writes scoped (no `git add -A` — there's no git op here anyway).
  </action>
  <behavior>
    - On every session start (cold OR with an active room): ~/.mindrian/install-state.json is written with the 9 D-04 keys; ~/.mindrian-last-version is rewritten to active_version
    - On version change: the owned surfaces (statusline shim, settings.json statusLine, ~/.mindrian-last-version, dev-clone pre-commit hook on a dev clone) are reconciled
    - The old :419 write is gone
    - No other SessionStart hook writes ~/.mindrian-last-version
    - The new block has zero network calls
  </behavior>
  <verify>
    <automated>node tests/test-install-state-record.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "install-state.json" scripts/session-start` returns TRUE
    - `grep -q "BEGIN install-state record" scripts/session-start` and `grep -q "END install-state record" scripts/session-start` return TRUE
    - `grep -c 'echo "\$PLUGIN_VERSION" > "\$LAST_VERSION_FILE"' scripts/session-start` returns 0 (the old write is removed)
    - `awk '/BEGIN install-state record/,/END install-state record/' scripts/session-start | grep -E "fetch|http|curl|brain.mindrian|tavily"` returns nothing (the new block is Part-8 clean)
    - `grep -q "deployment-surfaces" scripts/session-start` returns TRUE
    - `bash -n scripts/session-start` exits 0
    - `node tests/test-install-state-record.cjs` Tests 1-4 pass (Test 5 needs Task 3's manifest)
  </acceptance_criteria>
  <done>session-start writes the install-state record + ~/.mindrian-last-version (single writer, after Step A/B, unconditional); the old :419 write removed; the on-version-change reconcile walks the manifest; Part-8 clean; bash -n clean.</done>
</task>

<task type="auto">
  <name>Task 3: Create data/deployment-surfaces.json (the manifest) + update data/ROOM.md</name>
  <files>data/deployment-surfaces.json, data/ROOM.md</files>
  <read_first>
    - .planning/phases/123-install-lifecycle-harness/123-RESEARCH.md § "The deployment-surfaces manifest (sketch of the 6 surfaces, D-08)" — the JSON sketch is the starting point; use it
    - scripts/install-pre-commit.sh (to get the EXACT sentinel marker string it writes into .git/hooks/pre-commit — that string is the `expected` value for the `dev-clone-pre-commit-hook` surface)
    - data/command-registry.json + data/ROOM.md (the `data/` layout convention + the current ROOM.md content)
    - .planning/phases/123-install-lifecycle-harness/123-CONTEXT.md D-07 (the schema fields) + D-08 (the 6 surfaces)
  </read_first>
  <action>
    1. Create `data/deployment-surfaces.json` — valid JSON, hand-maintained (no generator, no `--check`). Top-level `{ "$schema_note": "...", "surfaces": [ ... ] }` where `$schema_note` says: "Hand-maintained. NOT generated. New surface = one entry, no code change. session-start reconciles owner:'session-start' surfaces whose reconcile=='on-version-change' on version change; doctor class J flags. $HOME token expanded via os.homedir(); <active_root>/<dev_clone_root> resolved at read time. topology_scope 'dev-clone' surfaces skipped on a user box." The `surfaces` array has exactly 6 entries, each with `id, path, owner, topology_scope, check_kind, expected, reconcile, remediation`:
       - `statusline-dispatch-shim`: path `$HOME/.claude/statusline-mos`, owner `session-start`, topology_scope `all`, check_kind `marker`, expected `MINDRIAN-STATUSLINE-DISPATCH`, reconcile `on-version-change`, remediation "session-start re-stamps the dispatcher shim; if it stays stale, an antivirus may be quarantining it -- re-run a session or `mindrian-os doctor --fix`."
       - `settings-statusline-command`: path `$HOME/.claude/settings.json`, owner `session-start`, topology_scope `all`, check_kind `exact-value`, expected `bash "$HOME/.claude/statusline-mos"`, reconcile `on-version-change`, remediation "session-start rewrites statusLine.command; if a managed-settings layer overrides it, that is expected."
       - `mindrian-last-version`: path `$HOME/.mindrian-last-version`, owner `session-start`, topology_scope `all`, check_kind `exact-value`, expected `<active_version>`, reconcile `on-version-change`, remediation "session-start rewrites it to match installed_plugins.json on every session."
       - `install-state-record`: path `$HOME/.mindrian/install-state.json`, owner `session-start`, topology_scope `all`, check_kind `observed-only`, expected `null`, reconcile `never`, remediation "self -- excluded from its own check. Absent -> 'run mindrian-os doctor --fix' (class I)."
       - `plugin-bin-path-entry`: path `<active_root>/bin`, owner `claude-code`, topology_scope `all`, check_kind `observed-only`, expected `null`, reconcile `never`, remediation "Claude Code owns PATH; a wrong/vanished entry -> 'restart Claude Code' (never stamped)."
       - `dev-clone-pre-commit-hook`: path `<dev_clone_root>/.git/hooks/pre-commit`, owner `session-start`, topology_scope `dev-clone`, check_kind `marker`, expected `<the exact sentinel from scripts/install-pre-commit.sh>`, reconcile `on-version-change`, remediation "session-start idempotently installs/updates it via scripts/install-pre-commit.sh; skipped entirely on a user (non-dev-clone) box."
       Validate it parses: `node -e "JSON.parse(require('fs').readFileSync('data/deployment-surfaces.json','utf8'))"` must exit 0.
    2. Update `data/ROOM.md`: add a one-line bullet mentioning `deployment-surfaces.json` (the static deployment-surface manifest read by session-start + doctor class J) alongside the existing `command-registry.json` mention. Keep the ICM Layer-0 ROOM.md identity intact.
  </action>
  <verify>
    <automated>node tests/test-install-state-record.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const m=JSON.parse(require('fs').readFileSync('data/deployment-surfaces.json','utf8')); process.exit(Array.isArray(m.surfaces) && m.surfaces.length===6 ? 0 : 1)"` exits 0
    - `grep -q "topology_scope" data/deployment-surfaces.json` and `grep -q '"dev-clone"' data/deployment-surfaces.json` return TRUE
    - `node -e "const m=JSON.parse(require('fs').readFileSync('data/deployment-surfaces.json','utf8')); process.exit(m.surfaces.some(s=>s.id==='dev-clone-pre-commit-hook' && s.topology_scope==='dev-clone') ? 0 : 1)"` exits 0
    - `node -e "const m=JSON.parse(require('fs').readFileSync('data/deployment-surfaces.json','utf8')); process.exit(m.surfaces.every(s=>s.path.includes('\$HOME')||s.path.includes('<active_root>')||s.path.includes('<dev_clone_root>')) ? 0 : 1)"` exits 0 (no absolute paths)
    - `grep -q "deployment-surfaces" data/ROOM.md` returns TRUE
    - `node tests/test-install-state-record.cjs` exits 0 (all 5 tests now green)
  </acceptance_criteria>
  <done>data/deployment-surfaces.json is valid JSON with the 6 D-08 surfaces in the D-07 schema, $HOME-token paths, dev-clone-scoped pre-commit surface; data/ROOM.md mentions it; the full record test green.</done>
</task>

</tasks>

<verification>
- `node tests/test-install-state-record.cjs` exits 0 (5 tests).
- `node -e "const {resolveActivePluginRoot}=require('./lib/core/active-plugin-root.cjs'); process.exit(typeof resolveActivePluginRoot().topology==='string'?0:1)"` exits 0.
- `node -e "JSON.parse(require('fs').readFileSync('data/deployment-surfaces.json','utf8'))"` exits 0.
- `bash -n scripts/session-start` exits 0.
- Canon Part 8: `awk '/BEGIN install-state record/,/END install-state record/' scripts/session-start | grep -E "fetch|http|curl|brain.mindrian|tavily"` returns nothing.
- `grep -c 'echo "\$PLUGIN_VERSION" > "\$LAST_VERSION_FILE"' scripts/session-start` returns 0 (old write removed).
</verification>

<success_criteria>
- `lib/core/active-plugin-root.cjs` returns a `topology` field; `root`/`source` byte-stable.
- `data/deployment-surfaces.json` exists: valid JSON, 6 surfaces, D-07 schema, $HOME-token paths, `dev-clone`-scoped pre-commit surface; `data/ROOM.md` mentions it.
- `scripts/session-start` writes `~/.mindrian/install-state.json` (full D-04 snapshot) + `~/.mindrian-last-version` as the single writer in its early steps, unconditionally; the old cold-start-only `~/.mindrian-last-version` write at ~:419 is removed; the on-version-change reconcile walks the manifest's owned surfaces (incl. the dev-clone pre-commit hook on a dev clone); zero network in the new lines.
- `tests/test-install-state-record.cjs` (5 tests) green; registered in the feynman runner.
</success_criteria>

<output>
After completion, create `.planning/phases/123-install-lifecycle-harness/123-02-SUMMARY.md`
</output>
