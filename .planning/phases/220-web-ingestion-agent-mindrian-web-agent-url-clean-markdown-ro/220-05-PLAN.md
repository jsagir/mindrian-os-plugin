---
phase: 220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro
plan: 05
type: execute
wave: 4
depends_on: ["220-03", "220-04"]
files_modified:
  - .planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-RELEASE-STAGING.md
  - .planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-VERIFICATION.md
autonomous: false
requirements: [REQ-1, REQ-6]
must_haves:
  truths:
    - "One REAL URL went end-to-end live: cited artifact filed in research/, entities visible via graph_query - fixture-green alone did not close the phase (the 218 lesson, SPEC acceptance 2)"
    - "The navigator confirmed the live invocation surfaces on a real session (pasted-URL card + /mos:research URL mode), recorded verbatim in 220-VERIFICATION.md"
    - "220's release readiness is STAGED: gates green, regressions green, README/marketplace/website DRAFTS updated to include 220 features - and git diff --exit-code proves NO version bump happened in 220 (REQ-6 as re-amended: the cut transfers to Phase 221)"
    - "scripts/release.sh was NOT executed in this phase - the ONE joint 219+220+221 cut executes at Phase 221 completion (navigator decision 2026-07-13, FINAL)"
  artifacts:
    - path: ".planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-VERIFICATION.md"
      provides: "live evidence record (real-URL run + navigator confirmation verbatim) + readiness sweep table"
      contains: "graph_query"
    - path: ".planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-RELEASE-STAGING.md"
      provides: "220 content filled into the joint-cut drafts: CHANGELOG slot, README refresh, website fact-check additions, handoff to Phase 221"
      contains: "release.sh"
  key_links:
    - from: "220-RELEASE-STAGING.md"
      to: ".planning/phases/219-.../219-RELEASE-STAGING.md"
      via: "fills the marked 220 slot 219-07 left in the joint CHANGELOG draft (copy-applicable insert, 219's doc not edited by this plan)"
      pattern: "220"
    - from: "220-VERIFICATION.md"
      to: "the Phase 221 joint cut"
      via: "readiness recorded BEFORE the cut gate opens; 221's REQ-6 consumes it"
      pattern: "readiness"
---

<objective>
Close Phase 220: the live half of REQ-1 (SPEC acceptance: "live test on one real URL files a cited artifact whose entities are visible via graph_query") plus REQ-6 as RE-AMENDED 2026-07-13 - 220 STAGES its release readiness; scripts/release.sh does NOT run here. The ONE joint version cut (219+220+221) executes as Phase 221's final requirement (ROADMAP release note, navigator-pre-approved). D-09's execute-the-cut wording is superseded by that re-amendment - this plan implements the readiness-staging form, mirroring 219 Plan 07's structure (validation evidence -> blocking navigator checkpoint -> staged drafts + git-diff-clean proof).

Preconditions this plan verifies (honest blockers, never silent): all four prior 220 plans complete with run-all-220 fully green; 219's readiness is green (219-RELEASE-STAGING.md exists + 219-VERIFICATION.md carries the corepower confirmation) - if 219 readiness is not yet recorded, the checkpoint reports it and readiness staging HOLDS until it lands (SPEC REQ-6: "after 220's requirements pass + 219 readiness is green").

Purpose: the phase's finish line as re-sequenced - everything proven live, everything staged, nothing bumped.
Output: live evidence + navigator confirmation in 220-VERIFICATION.md, staged drafts in 220-RELEASE-STAGING.md, clean version files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-SPEC.md
@.planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-07-PLAN.md
@.claude/includes/release-process.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full offline gate sweep + LIVE real-URL end-to-end evidence run</name>
  <files>.planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/220-.../220-SPEC.md acceptance criteria (the 8-row checklist this evidence run must map onto, minus the re-amended cut row)
    - .planning/phases/219-.../219-VERIFICATION.md (the live-evidence recording format 219-06 established - clone the structure)
    - lib/core/url-ingest.cjs (the envelope fields to capture as evidence)
  </read_first>
  <action>
    (1) OFFLINE SWEEP, recorded as a table in a new 220-VERIFICATION.md (clone the 219-VERIFICATION structure): bash tests/run-all-220.sh (expect FAIL=0, all seven 220 legs PASS), the six governance gates chained (build-connector-registry --check, check-shape-declaration --check --strict, command registration, help coverage, build-skill-mirrors --check, check-render-coverage), node scripts/doctor.cjs --acceptance, and the regression set: bash tests/run-all-219.sh, run-all-218.sh, run-all-216.sh (chains 215+211). Record PASS/FAIL counts verbatim. (2) LIVE RUN (Part 8-clean by construction - the outbound is a public URL only): pick a stable public page relevant to a real room (default: a Tavily docs page or a mindrian-os.com page; ador-ip-test is the target room per the 218/219 live-room discipline). Precondition probe: TAVILY_API_KEY present in the environment. If PRESENT: run node -e ingestUrl against the real room with the real URL (origin 'on_demand'); then prove graph visibility - query room.db for the artifact node + its DERIVED_FROM-linked proposed entities via the graph_query surface (the shipped consumer REQ-1 names); re-run the SAME ingest and record the no_op envelope (live idempotency); record the artifact path, content_sha256, entity count, and the graph_query output snippet (counts + node types, never room prose beyond the public web content itself). If ABSENT: record the gap honestly in 220-VERIFICATION.md and fall through to the ladder live-leg at the Task 2 checkpoint (the navigator supplies the key or approves WebFetch-rung evidence) - never fake the live row. (3) Map every SPEC acceptance row to its evidence line in 220-VERIFICATION.md, leaving the navigator-confirmation and readiness rows open for Tasks 2-3.
  </action>
  <verify>
    <automated>bash tests/run-all-220.sh && node scripts/doctor.cjs --acceptance && test -s .planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-VERIFICATION.md && grep -q "graph_query" .planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 220-VERIFICATION.md exists with the offline sweep table (FAIL=0 everywhere) and the live-run section (real URL, artifact path, sha, entity count, graph_query evidence, live no_op re-run) OR the honest TAVILY_API_KEY-absent record routing the live leg to the checkpoint
    - Live evidence shows >=1 proposed entity DERIVED_FROM the live artifact node (SPEC REQ-1 live acceptance)
    - No room-content prose pasted into the verification record beyond counts/paths/enums (no-real-names + Part 8 record discipline)
  </acceptance_criteria>
  <done>Fixture-green is backed by live-green (or an honest, named gap at the human gate) - the 218 lesson honored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Navigator live verification - pasted-URL card + URL mode on a real session</name>
  <action>Pause execution (blocking gate, never auto-approvable). Hand the navigator the two-check script below; on confirmation, record the results verbatim in the 220-VERIFICATION.md navigator section. On a described defect, route it to gap-closure (/gsd-plan-phase 220 --gaps) - never hot-patch past the gate.</action>
  <what-built>The full web-ingestion agent: /mos:research URL mode (F.1 readback, three-rung ladder), the pasted-URL sensor firing an F.1 card offer, content-hash idempotency + SUPERSEDES versioning, cadence crawl loop, all offline-green (Task 1 table) plus the live single-URL evidence run.</what-built>
  <how-to-verify>
    1. In a normal Claude Code session in a real room (ador-ip-test or your working room), PASTE a bare public URL into conversation as plain text. Expect: an F.1 card offering [Ingest] [Ingest+Explore] [Skip] - and NOTHING filed until you pick a verb. Pick [Ingest].
    2. Expect a rendered result naming the filed artifact path under research/ and an entity count. Paste the same URL again in a new turn - expect NO new card (already-ingested dedup).
    3. Run /mos:research <that-url> explicitly. Expect the readback gate, then an honest "already ingested, unchanged" (no_op) - not a duplicate file.
    4. (If Task 1 ran key-absent) confirm the ladder honestly: the surface reports Tavily unavailable and offers the WebFetch rung rather than failing silent.
    5. Optional Desktop cross-check (Tri-Polar): repeat step 1 on Claude Desktop if available this session; report any surface divergence.
  </how-to-verify>
  <resume-signal>Type "confirmed" with any output snippets you want recorded - the confirmation lands verbatim in 220-VERIFICATION.md. If anything misbehaves (card missing, silent filing, duplicate artifacts, crash), describe it: it becomes a gap-closure item, and readiness staging (Task 3) holds until resolved.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Release readiness staging (REQ-6 re-amended) - drafts updated, nothing bumped, cut handed to 221</name>
  <files>.planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-RELEASE-STAGING.md, .planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/219-.../219-RELEASE-STAGING.md (AS LANDED by 219-07: the joint CHANGELOG draft with the MARKED 220 SLOT, the README refresh format, the marketplace pin instruction, the website fact-check checklist, the handoff note wording)
    - .planning/phases/219-.../219-VERIFICATION.md (the corepower confirmation - the 219-readiness precondition)
    - .claude/includes/release-process.md (the five-gate lockstep 221 will execute; the two-command user upgrade path for the README draft)
  </read_first>
  <action>
    PRECONDITION (honest blocker): verify 219 readiness is green - 219-RELEASE-STAGING.md exists AND 219-VERIFICATION.md contains the corepower confirmation. If either is missing, STOP and report "219 readiness not yet recorded - 220 staging holds" (SPEC REQ-6 ordering; do not stage around it). THEN write 220-RELEASE-STAGING.md containing, fully drafted and copy-applicable at the Phase 221 cut: (1) the 220 CHANGELOG content for the joint entry - the copy-applicable insert for the marked 220 slot in 219's draft (219's file is NOT edited by this plan; the insert is staged here and applied at cut time): web ingestion agent - /mos:research URL mode + pasted-URL card, Tavily Extract adapter behind the Part 8 chokepoint, content-hash idempotency + SUPERSEDES versioning, watched-sources cadence crawl under regulator caps, D-19 provider-honesty envelope consumption, adversarial safety fixtures; (2) the README.md content-refresh draft additions describing the URL-to-room-knowledge capability in the EXISTING Feynman + JTBD voice (content only, zero restyle - the D-15/219 discipline); (3) website update drafts: which mindrian-os.com surfaces gain the web-ingestion feature description + the hand-typed-version fact-check checklist additions (VERSION-BUMP-CHECKLIST discipline, navigator memory rule); (4) the marketplace note: no 220-specific action beyond the joint pin 219's doc already stages; (5) the HANDOFF NOTE verbatim: "The 219+220+221 version cut is ONE joint cut (navigator decision 2026-07-13, FINAL - ROADMAP release note). scripts/release.sh <version> executes as Phase 221's final requirement, covering all three phases. 220 stages readiness only. Never hand-bump." Do NOT run scripts/release.sh; do NOT modify package.json / .claude-plugin/plugin.json / CHANGELOG.md / README.md - everything lands as staged drafts in the phase dir. FINALLY complete 220-VERIFICATION.md: the readiness sweep row (re-run bash tests/run-all-220.sh + node scripts/doctor.cjs --acceptance one last time, record), the navigator confirmation from Task 2, the 219-readiness precondition evidence, and the pointer to 220-RELEASE-STAGING.md - the record Phase 221's cut gate consumes.
  </action>
  <verify>
    <automated>git diff --exit-code package.json .claude-plugin/plugin.json CHANGELOG.md README.md && grep -q "release.sh" .planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-RELEASE-STAGING.md && grep -q "readiness" .planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-VERIFICATION.md && bash tests/run-all-220.sh</automated>
  </verify>
  <acceptance_criteria>
    - git diff --exit-code proves package.json, plugin.json, CHANGELOG.md, README.md UNTOUCHED - no premature bump (REQ-6 acceptance verbatim)
    - 220-RELEASE-STAGING.md contains all five staged sections including the verbatim handoff note naming release.sh and Phase 221
    - 220-VERIFICATION.md complete: offline sweep, live evidence, navigator confirmation, 219-readiness precondition, readiness sweep - the joint-cut acceptance rows (npm publish, five gates, marketplace pin, README diff, website fact-check) explicitly marked "transfers to Phase 221 per SPEC REQ-6"
    - run-all-220.sh + doctor --acceptance green at phase close
  </acceptance_criteria>
  <done>Phase 220 is cut-ready and cut-clean: everything the joint release needs is staged, nothing it forbids has happened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live URL run -> real room | first production write of web content into a user room |
| staged drafts -> the Phase 221 joint cut | drift between staging and execution is a silent version mismatch |
| navigator session evidence -> repo record | human-run evidence crosses back into the tracked record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-220-23 | Tampering | hand-bumped versions / premature partial cut | mitigate | git diff --exit-code gate on all four version surfaces; release.sh explicitly NOT run; cut owned by Phase 221 (REQ-6 re-amendment) |
| T-220-24 | Repudiation | phase closing on fixture-green alone (the 218 failure class) | mitigate | live real-URL evidence + blocking navigator checkpoint BEFORE readiness closes; key-absent path recorded honestly, never faked |
| T-220-25 | Information Disclosure | room prose pasted into the verification/staging record | mitigate | evidence discipline: counts/paths/enums/public-web snippets only; no-real-names rule holds |
| T-220-26 | Repudiation | 220 readiness staged before 219 readiness exists | mitigate | hard precondition on 219-RELEASE-STAGING.md + corepower confirmation; honest hold, never staged around |
| T-220-SC | Tampering | package installs | accept (n/a) | zero installs this phase |
</threat_model>

<verification>
- 220-VERIFICATION.md complete (offline sweep, live evidence, navigator confirmation, readiness, 221 transfer rows)
- 220-RELEASE-STAGING.md staged; version files git-diff-clean
- run-all-220.sh + doctor --acceptance green at close
</verification>

<success_criteria>
- SPEC acceptance rows 1-7 evidenced (offline + live); row 8 (the cut) explicitly transferred to Phase 221 per the re-amended REQ-6
- The navigator has personally seen the two invocation surfaces work live; defects routed to gap-closure, not patched past the gate
- Phase 221 inherits a zero-friction joint cut: 219 + 220 both staged, both clean
</success_criteria>

## Artifacts this plan produces

- .planning/phases/220-.../220-VERIFICATION.md (offline sweep table, live real-URL evidence incl. graph_query output, live idempotency no_op, navigator confirmation verbatim, readiness record, 221-transfer rows)
- .planning/phases/220-.../220-RELEASE-STAGING.md (CHANGELOG 220-slot insert, README refresh draft, website update drafts + fact-check additions, marketplace note, verbatim 221 handoff)
- Explicitly NOT produced: a version bump, an npm publish, a git tag, any edit to 219's staging doc - the ONE joint cut executes at Phase 221 completion via scripts/release.sh

<output>
Create `.planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-05-SUMMARY.md` when done
</output>
