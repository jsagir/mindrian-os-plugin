---
phase: 220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro
plan: 03
type: execute
wave: 3
depends_on: ["220-02"]
files_modified:
  - lib/core/sensors/sensor-url-ingest.cjs
  - lib/core/insight-sensors.cjs
  - commands/research.md
  - skills/research/SKILL.md
  - tests/test-220-url-sensor.cjs
autonomous: true
requirements: [REQ-2, REQ-4]
must_haves:
  truths:
    - "A conversation turn containing a bare http(s) URL yields exactly one url-ingest reach (frozen deep_research, posture hold) ending at an F.1 card with verbs [Ingest] [Ingest+Explore] [Skip] - and NOTHING files without a navigator verb (Part 3, REQ-2)"
    - "A URL inside a fenced code block or quotes does NOT fire; an already-ingested URL (ledger dedup) does NOT fire (D-05)"
    - "/mos:research <url> routes to the ingest pipeline through a readback gate; a topic argument keeps the existing deep_research fan-out byte-unchanged (Part 7 - no new command)"
    - "The degrade ladder is surface-complete: Tavily -> WebFetch (Claude-orchestrated, content passed via opts.content) -> gate-OFFERED llm_manual_baseline, never default, never silent (D-01, D-10)"
    - "Both surfaces are born WIRED with hitl_shape declared; all six governance gates green (Part 11, REQ-2)"
    - "commands/research.md documents the REAL modes and ladder (research_mode enum, typed outcomes) - doc-parity with the shipped chain, no unshipped claims (D-07, REQ-4)"
  artifacts:
    - path: "lib/core/sensors/sensor-url-ingest.cjs"
      provides: "SENS-15 (build-time re-verified) pasted-URL detector, clone of sensor-eureka, frozen-reach fail-closed"
      exports: ["sensorUrlIngest"]
    - path: "lib/core/insight-sensors.cjs"
      provides: "sensor registered at the three touch points (require + SENSOR_REGISTRY + module.exports); dispatch loop untouched"
      contains: "sensorUrlIngest"
    - path: "commands/research.md"
      provides: "URL mode (readback gate -> ingestUrl -> honest envelope render + ladder + D-10 offer), SENS id added to sensor_triggers"
      contains: "url-ingest"
    - path: "skills/research/SKILL.md"
      provides: "regenerated byte-mirror (build-skill-mirrors.cjs --write, never hand-edited)"
    - path: "tests/test-220-url-sensor.cjs"
      provides: "REQ-2 fixture test: fires/suppresses/dedups/never-writes + doc-parity greps"
  key_links:
    - from: "lib/core/sensors/sensor-url-ingest.cjs"
      to: "lib/core/sensors/sensor-types.cjs"
      via: "makeReach({reach_id:'deep_research', posture:'hold', dispatch:'url-ingest-offer', signal:'pasted_url'}) - frozen bank membership fail-closed at load"
      pattern: "deep_research"
    - from: "lib/core/sensors/sensor-url-ingest.cjs"
      to: "<roomDir>/.mindrian/url-ingest-ledger.json"
      via: "read-only dedup lookup against Plan 02's ledger (readJsonSafe replicated locally, soft-fail)"
      pattern: "url-ingest-ledger"
    - from: "commands/research.md"
      to: "lib/core/url-ingest.cjs"
      via: "URL mode shells node -e ingestUrl(...) after the F.1 readback verb; renders the returned envelope honestly"
      pattern: "ingestUrl"
---

<objective>
Implement REQ-2: the invocation matrix. Explicit surface = /mos:research gains a URL mode (Part 7 reuse ruling - NO new command; the thin alias stays deferred per CONTEXT until the navigator asks). Contextual surface = the pasted-URL sensor on the insight-sensor rail (expected SENS-15 after 219's SENS-14 lands - free id re-verified at build time exactly as 219 Plan 03 mandates), firing the FROZEN deep_research reach into an F.1 card [Ingest] [Ingest+Explore] [Skip]. Every contextual fire ends at a Decision Gate; nothing files without a navigator verb (Part 3, Pitfall 3). Both surfaces born WIRED + hitl_shape declared (Part 11, D-05).

Also carries REQ-4's doc-parity leg (D-07): the URL-mode documentation must describe the REAL shipped chain - the research_mode enum, the typed outcomes, the actual three-rung ladder - mirroring 219-05's reality-to-docs discipline. The cache-purity and envelope-emission legs of REQ-4 landed in Plan 02.

D-10 surfaces here as the gate-OFFERED terminal rung: when Tavily AND WebFetch are unavailable, the command OFFERS (never defaults to) an LLM high-effort manual ingest on the frozen deep_research reach - the model reads the URL via native web tools, hand-normalizes to markdown, and calls ingestUrl with contentSource 'llm_manual'; the pipeline (Plan 02) stamps engine_mode 'llm_manual_baseline' and the providers bag tells the truth. Excluded from calibration sets; structurally unreachable from cadence.

Naming rule (binding): tokens are url-ingest / pasted_url - never bare 'harvest' (219 owns it).

Purpose: the two doors exist, both governed, both honest - explicit for intent, contextual for serendipity.
Output: SENS sensor + registration, the URL mode + card + ladder docs, skill mirror, sensor test, six gates green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-SPEC.md
@.planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-PATTERNS.md
@.planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-02-SUMMARY.md
@lib/core/sensors/sensor-eureka.cjs
@lib/core/sensors/sensor-types.cjs
@commands/research.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: sensor-url-ingest.cjs (SENS id re-verified) + three-touch-point registration</name>
  <files>lib/core/sensors/sensor-url-ingest.cjs, lib/core/insight-sensors.cjs</files>
  <read_first>
    - lib/core/sensors/sensor-eureka.cjs (:56-87 header/id-claim/frozen-reach fail-closed throw; :95-119 replicated readJsonSafe; :146-190 fire body + enum/handle-only evidence bag; the soft-fail-to-null wrapper - the exact clone template)
    - lib/core/insight-sensors.cjs (the LIVE require block, SENSOR_REGISTRY, and module.exports AS LANDED - 219's sensor-opportunity-harvest rows will sit there; the dispatch loop is FROZEN, Phase 144 fence, do not touch; normalizeTurn aliases turn.text)
    - lib/core/sensors/sensor-types.cjs (makeReach contract: reach_id/posture/dispatch/signal/evidence flat-scalar bag)
    - .planning/phases/220-.../220-PATTERNS.md "sensor-url-ingest" section (detection difference: this sensor reads the TURN text, not a freshness side-channel)
  </read_first>
  <action>
    BUILD-TIME FREE-ID RE-CHECK (mandated, the 219-03 discipline): run grep -oE "SENS-[0-9]+" lib/core/insight-sensors.cjs | sort -uV | tail -3 against the LIVE file and claim the next free id (expected SENS-15 with 219's SENS-14 landed; if 219 has not landed use the actual next free id and record the divergence in the SUMMARY). Create lib/core/sensors/sensor-url-ingest.cjs cloning sensor-eureka.cjs structurally: SENSOR_ID from the live check; REACH_ID 'deep_research' with the load-time fail-closed throw if it drifts off the frozen REACH_IDS bank (the reach is FROZEN - ride it, mint nothing; SPEC constraint). Detection (D-05, discretion documented in the module header): operate on turn.text; strip fenced code blocks (```...```) and inline code spans FIRST; then match a conservative bare http(s) URL regex; skip URLs inside straight or markdown-quote quoting; take the FIRST remaining candidate. Dedup: read <roomDir>/.mindrian/url-ingest-ledger.json via a LOCALLY replicated readJsonSafe (NEVER require('../insight-sensors.cjs') - circular; only ./sensor-types.cjs) - if the URL has a ledger entry, return null (already ingested; Plan 02's ledger is the contract). Fire: return makeReach({ reach_id:'deep_research', posture:'hold', dispatch:'url-ingest-offer', signal:'pasted_url', evidence:{ url_count, first_url_host, first_url_handle (sha256 hex of the url, first 12 chars), ledger_checked: true } }) - flat scalars only, HOST + HASH HANDLE, never the full URL and never prose in the evidence bag (Part 8 five-tripwire sweep spans lib/core/sensors/* automatically; the card host recovers the URL locally from the turn context). posture 'hold' = standing offer, never auto-open (Part 3). Soft-fail-to-null wrapper around the whole body. Register in insight-sensors.cjs at the three touch points exactly as the existing last sensor row sits (require with a "Phase 220 detector (SENS-NN -- pasted URL -> deep_research url-ingest offer)" comment, SENSOR_REGISTRY append LAST, module.exports re-export). Do NOT modify the dispatch loop. The dispatch slug 'url-ingest-offer' completes its reachability when Task 2's command surface lands - note the pairing in the SUMMARY (the 219-03 born-wired-at-feature-time precedent).
  </action>
  <verify>
    <automated>node -e "require('./lib/core/insight-sensors.cjs')" && node tests/test-220-url-sensor.cjs 2>/dev/null || node -e "require('./lib/core/sensors/sensor-url-ingest.cjs')"</automated>
  </verify>
  <acceptance_criteria>
    - node -e "require('./lib/core/insight-sensors.cjs')" loads clean (no circular require, no frozen-reach throw)
    - The claimed SENS id is verifiably free: the pre-edit grep output is recorded in the SUMMARY verbatim
    - Comment-filtered greps: zero require of insight-sensors from the sensor file; zero full-URL field in the evidence bag construction (host + handle only); zero 'harvest' token
    - The dispatch loop region of insight-sensors.cjs is byte-unchanged (git diff shows only the three touch points)
  </acceptance_criteria>
  <done>The contextual tripwire is live on the one governed rail: detects, dedups, offers - never acts.</done>
</task>

<task type="auto">
  <name>Task 2: /mos:research URL mode + F.1 card + D-10 offered manual rung + skill mirror</name>
  <files>commands/research.md, skills/research/SKILL.md</files>
  <read_first>
    - commands/research.md (AS LANDED post-219-05 - the drift-fixed body; frontmatter :1-40: connector block, sensor_triggers, allowed-tools already includes WebFetch; the frameworks/connector.framework match invariant; PRESERVE every existing field byte-wise except the named additions)
    - lib/hmi/selector-dispatcher.cjs (:198-230 - the ONLY AskUserQuestion payload builder; route the card through it, never bespoke)
    - lib/hmi/shape-f1-renderer.cjs (:20-22 verb-cap + Free-Text invariants; :134 renderShapeF1)
    - lib/core/url-ingest.cjs (the envelope enums to render honestly: outcome, research_mode, providers)
  </read_first>
  <action>
    Extend commands/research.md with the URL mode (Part 7: same command, new argument shape). FRONTMATTER (surgical, byte-preserving): argument-hint becomes "[topic | url]"; ADD the Task-1 SENS id to the sensor_triggers array (alongside SENS-04); leave reach_id/hitl_shape/filing/framework untouched. If the connector schema supports a second sub_mode, add 'url-ingest' alongside 'hat-scoped-research'; if the registry gate rejects a list-valued sub_mode, keep the single sub_mode and carry the URL mode in the body + the 'url-ingest-offer' dispatch slug instead - confirm against node scripts/build-connector-registry.cjs --check at build time and record which shape the gate accepted (the 219-03 confirm-at-build pattern). BODY: a new "URL mode" section after the existing invocation modes: (1) DETECT - a bare http(s) URL argument routes here; anything else keeps the existing deep_research fan-out unchanged. (2) READBACK GATE (F.1, Part 3) - before ANY fetch, render the card through the selector-dispatcher archetype path with verbs [Ingest] [Ingest+Explore] [Skip] (user verbs capped, Free-Text auto-appended - renderer invariant); the card names the URL host and the target section (research/). (3) On [Ingest]: run node -e with ingestUrl(roomDir, url, {sessionId, origin:'on_demand'}) via Bash; render the returned envelope HONESTLY per its enums: outcome filed -> artifact path + entity count; no_op -> "already ingested, unchanged" with the prior artifact path; superseded -> "content changed - new version filed, SUPERSEDES the prior"; provider_unavailable -> proceed to the ladder. (4) LADDER (D-01/D-10): rung 2 - Claude performs WebFetch on the URL (already an allowed-tool), normalizes to markdown, calls ingestUrl with opts.content + contentSource 'webfetch'; rung 3 - ONLY when rungs 1-2 both failed, OFFER at a Decision Gate the LLM high-effort manual ingest ("Engines are down. I can read and hand-normalize this page myself - slower, labeled llm_manual_baseline, excluded from calibration. Proceed?") - on explicit approval, fetch/read via the native web tools on the frozen deep_research reach, hand-normalize to clean markdown, call ingestUrl with contentSource 'llm_manual'; NEVER auto-selected, NEVER silent, the envelope's providers.llm_manual 'used' + frontmatter engine_mode stamp are non-negotiable (Plan 02 enforces). (5) On [Ingest+Explore]: after a successful ingest, OFFER the existing 219 explore surface (/mos:explore-opportunity) on the resulting knowledge at its own gate - never auto-run (chain fetches stay material, Part 3). (6) DOC-PARITY (REQ-4/D-07): document the research_mode enum values and the typed outcome vocabulary exactly as the code returns them; claim NOTHING unshipped (the 219-05 reality-to-docs rule). Also document the SENS contextual path in the body's sensor section. No em-dashes anywhere. Then regenerate the skill mirror: node scripts/build-skill-mirrors.cjs --write (byte-mirror; never hand-edit skills/research/SKILL.md).
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check && node scripts/check-shape-declaration.cjs --check --strict && node scripts/build-skill-mirrors.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - build-connector-registry --check green with the URL mode + sensor trigger registered; check-shape-declaration --check --strict clean (hitl_shape/hitl_why cover the new gate - extend hitl_why if the gate flags it)
    - grep -c "url-ingest" commands/research.md >= 2; grep -c "llm_manual_baseline" commands/research.md >= 1; grep -ci "never" the-manual-rung-section confirms the never-default/never-silent language present
    - Comment-filtered grep: zero em-dash characters in the modified files; the existing topic-mode sections are byte-unchanged (git diff confined to frontmatter additions + the new URL mode section)
    - skills/research/SKILL.md is a regenerated byte-mirror (build-skill-mirrors.cjs --check passes)
  </acceptance_criteria>
  <done>One command, two argument shapes, three honest rungs, every gate human-held.</done>
</task>

<task type="auto">
  <name>Task 3: REQ-2 sensor fixture test + six governance gates + doc-parity greps</name>
  <files>tests/test-220-url-sensor.cjs</files>
  <read_first>
    - .planning/phases/220-.../220-SPEC.md requirement 2 acceptance ("a turn containing a URL yields the card offer, no write occurs without the navigator verb; born-wired + shape-declaration gates green")
    - lib/core/insight-sensors.cjs (dispatchSensors + normalizeTurn call shapes for driving the fixture turn)
    - tests/test-220-ingest-e2e.cjs (the temp-room fixture idiom + ledger seeding from Plan 02)
  </read_first>
  <action>
    Write tests/test-220-url-sensor.cjs as a standalone node script (exit 0/1, temp room, zero network): (1) FIRE: a turn whose text contains a bare https URL yields exactly one reach from the url-ingest sensor with reach_id 'deep_research', posture 'hold', dispatch 'url-ingest-offer', signal 'pasted_url'. (2) EVIDENCE HYGIENE: walk every evidence field - flat scalars only, first_url_host is a bare hostname, first_url_handle matches ^[a-f0-9]{12}$, NO field contains the full URL or any turn prose. (3) SUPPRESS - code block: the same URL inside a fenced block yields null. (4) SUPPRESS - quoted: a quoted URL yields null. (5) SUPPRESS - dedup: seed the Plan 02 ledger with the URL (write the interfaces-schema entry), assert null. (6) NO WRITE WITHOUT VERB: snapshot the temp room (file list + room.db artifact/entity counts) before and after a full dispatchSensors pass over the URL turn - assert byte-identical state: the sensor NEVER files, NEVER touches the ledger, NEVER writes room.db (Part 3, Pitfall 3); plus comment-filtered grep: sensor-url-ingest.cjs requires neither url-ingest.cjs nor navigation (read-only detector by construction). (7) SOFT-FAIL: a corrupt ledger JSON yields null, not a throw. (8) DOC-PARITY GREPS (REQ-4): commands/research.md contains every research_mode enum value the driver/pipeline actually return (read the enum from lib/core/url-ingest.cjs source and assert each token present in the doc) and contains zero occurrences of the retired unshipped-ordering claim (the 219-05 fixed drift must not regress into the new section: comment-filtered grep "paid -> native" returns 0). Then run the full gate sweep and the harness.
  </action>
  <verify>
    <automated>node tests/test-220-url-sensor.cjs && bash tests/run-all-220.sh && node scripts/check-render-coverage.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All eight assertion groups green; test exits 0
    - bash tests/run-all-220.sh: url-sensor leg flips SKIP -> PASS, FAIL=0
    - Six governance gates green in one chained invocation (the 216-03 precedent): build-connector-registry --check, check-shape-declaration --check --strict, command registration, help coverage, build-skill-mirrors --check, check-render-coverage
    - node scripts/doctor.cjs --acceptance green
  </acceptance_criteria>
  <done>REQ-2 pinned offline: the card offers, the navigator decides, nothing writes uninvited; docs match the shipped truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator turn text -> sensor evidence -> reach telemetry | conversation content is scanned; only enums/handles may leave the scan |
| F.1 card verb -> fetch + file | the human gate between detection and action |
| manual rung (LLM web read) -> filed artifact | model-fetched content enters the room labeled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-220-13 | Information Disclosure | turn prose / full URL leaking via the evidence bag | mitigate | host + 12-hex hash handle only; mechanical evidence-walk test; Part 8 five-tripwire sweep spans lib/core/sensors/* |
| T-220-14 | Tampering | sensor auto-filing on fire (Pitfall 3) | mitigate | posture 'hold' + read-only detector (no url-ingest/navigation require, grep-gated); before/after room-state snapshot assertion |
| T-220-15 | Spoofing | manual-rung output passing as engine output | mitigate | D-10: OFFER at a gate only, explicit approval verb, engine_mode 'llm_manual_baseline' + providers.llm_manual 'used' stamped by the pipeline; excluded from calibration |
| T-220-16 | Denial of Service (cost) | URL mode auto-firing expensive fetches | mitigate | F.1 readback gate BEFORE any fetch; [Ingest+Explore] offers the 219 chain at its own gate, never auto-runs |
| T-220-17 | Repudiation | docs claiming an unshipped chain (the drift class 219-05 fixed) | mitigate | doc-parity greps pinned in the sensor test; reality-to-docs rule carried into the new section |
| T-220-SC | Tampering | package installs | accept (n/a) | zero installs this phase |
</threat_model>

<verification>
- node tests/test-220-url-sensor.cjs green; bash tests/run-all-220.sh FAIL=0
- Six governance gates green (connector registry, shape declaration --strict, command registration, help coverage, skill mirror, render coverage)
- node scripts/doctor.cjs --acceptance green
</verification>

<success_criteria>
- SPEC acceptance 3 satisfied offline: pasted URL -> card offer, zero writes without the verb, born-wired + shape gates green
- The invocation matrix is complete: explicit URL mode + contextual sensor, both riding the frozen deep_research reach, both ending at Decision Gates (D-05)
- D-10's terminal rung is gate-OFFERED and honestly labeled; REQ-4's doc-parity leg locked
</success_criteria>

## Artifacts this plan produces

- Functions: `sensorUrlIngest(turn, tuple, ctx)` (lib/core/sensors/sensor-url-ingest.cjs; registered at the build-time-verified SENS id, expected SENS-15)
- Reach usage: deep_research (FROZEN) + posture hold + dispatch 'url-ingest-offer' + signal 'pasted_url' (no new reach id)
- Surface: /mos:research URL mode (commands/research.md + regenerated skills/research/SKILL.md) with the F.1 card verbs {Ingest, Ingest+Explore, Skip} and the three-rung ladder (tavily-extract -> webfetch -> gate-OFFERED llm_manual_baseline)
- Evidence-bag fields: {url_count, first_url_host, first_url_handle, ledger_checked} (flat scalars, no full URL, no prose)
- Test files: tests/test-220-url-sensor.cjs (fire/suppress/dedup/no-write/soft-fail + REQ-4 doc-parity greps)

<output>
Create `.planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-03-SUMMARY.md` when done
</output>
