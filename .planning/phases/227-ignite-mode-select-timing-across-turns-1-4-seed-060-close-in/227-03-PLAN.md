---
phase: 227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/larry-personality/SKILL.md
autonomous: true
requirements: [REQ-4]

must_haves:
  truths:
    - "grep -ci ignite skills/larry-personality/SKILL.md is greater than zero (currently zero at planning time, a confirmed live gap named by SEED-056)."
    - "The new section states, in real Hooked-Model terms, why the mode-select gate fires at turn 1 and why that is correct, not a defect."
    - "The new section names the silent-skip failure mode explicitly as the actual defect Requirement 1's checkpoint exists to catch, not generic Hooked-Model summary text."
  artifacts:
    - path: "skills/larry-personality/SKILL.md"
      provides: "a new peer-level ## Ignite and the mode-select gate (Hooked-Model timing) section, placed adjacent to the existing Voice Signature section, containing the Prompt-not-Investment framing, the ambiguous-vs-signaled routing rule citing detect_dual_path, and the silent-skip failure mode"
      contains: "Ignite and the mode-select gate"
  key_links:
    - from: "skills/larry-personality/SKILL.md new section"
      to: "commands/ignite.md Gate B1 Door 2 (detect_dual_path -> extract_shallow)"
      via: "a citation naming detect_dual_path as the existing ambiguous-vs-signaled precedent"
      pattern: "detect_dual_path"
    - from: "skills/larry-personality/SKILL.md new section"
      to: "lib/core/doctor/mode-select-checkpoint-module.cjs (plan 227-01)"
      via: "naming the silent-skip failure mode as what Requirement 1's checkpoint catches"
      pattern: "silent"
---

<objective>
Name ignite and document real Hooked-Model (Fogg B=MAP / TARI) reasoning for the mode-select
gate's turn-1-through-4 timing in skills/larry-personality/SKILL.md (SPEC Requirement 4).
Currently a grep for "ignite" in this file returns zero matches: the gate that decides Just
Talk versus Explore+Capture versus Build a Room is never named in Larry's own personality
doctrine, so there is no documented reasoning anywhere for why it fires when it does. SEED-056's
persona-coverage audit confirmed and handed off this exact gap.

Purpose: this is not a generic Hooked-Model essay. The content is locked by this session's own
hooked-model audit finding (SPEC Background section): firing the gate at turn 1 is the RIGHT
timing, but only when framed correctly as a Fogg Prompt, never as a Hook Model Investment
(Investment always comes after Reward; a forced heavyweight ask before any value is delivered
is the anti-pattern this framing avoids). The actual defect the two source debug files describe
is not "asks too early", it is the third failure mode: the gate neither fires a card nor states
a default, a fully silent skip. Requirement 1's checkpoint (plan 227-01) targets exactly that
failure mode; this section's job is to state that framing in writing so it is never re-litigated
as "the gate fires too early" again.

Output: one new peer-level doctrine section in skills/larry-personality/SKILL.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-SPEC.md
@.planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-CONTEXT.md

# The exact section this new section sits beside (read the whole file once; it is 449 lines)
@skills/larry-personality/SKILL.md

# The detect_dual_path precedent this section must cite by name
@commands/ignite.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Ignite and the mode-select gate section to larry-personality.md (D-10)</name>
  <files>skills/larry-personality/SKILL.md</files>
  <action>
    Confirmed live at planning time: the file's "## Voice Signature (Part 12 HARD requirement)"
    section ends at line 212, immediately followed by "## Thinking Trace -- Show Your Work" at
    line 214. Insert the new section between these two, immediately after Voice Signature's
    closing paragraph and before Thinking Trace's heading. Both are HARD or binding doctrine
    sections with a similar citation-heavy register, so this slot matches D-10's placement
    instruction (a peer-level doctrine section near the existing Voice and Elevation sections,
    not buried in a sub-bullet).

    Heading: "## Ignite and the mode-select gate (Hooked-Model timing)".

    Required content, all four elements are locked by SPEC Requirement 4 and D-10, prose style
    and exact wording are Claude's discretion:
    (a) Name /mos:ignite as the front door every venture birth goes through (Canon Part 11
        CIRS, one governed path), and name the session-start mode-selection gate (Just Talk,
        Explore+Capture, Build a Room; skills/conversation-mode/SKILL.md; hitl_shape F.1) as
        ignite's own no-room entry point onto the same lane-picker doctrine.
    (b) State plainly that the gate is a Fogg Hook Model PROMPT (an external trigger cueing a
        next action), never an INVESTMENT (an ask for user effort, which the Hook Model's own
        sequencing rule requires to always land after a Reward, never before any value has been
        delivered). A single-click, three-to-four-option lane pick is friction-minimized enough
        to be a Prompt; firing it at turn 1 therefore does not violate Investment-after-Reward,
        the way a heavyweight forced ask would.
    (c) State the ambiguous-vs-signaled distinction: the gate fires ONLY when the user's opener
        does not already signal a lane. Cite, by exact name, this codebase's own detect_dual_path
        precedent (commands/ignite.md Gate B1 Door 2, the CV path: "run the Phase 115 dual-path:
        detect_dual_path -> extract_shallow", confirmed live at that file's own text, loaded via
        context above) as the existing infer-from-signal-ask-only-when-genuinely-ambiguous
        pattern this gate's timing follows. When a navigator opens with an already-determinable
        intent (for example "build me a room for X"), the lane is already signaled and Larry
        proceeds directly rather than re-asking a decided question.
    (d) Name the silent-skip failure mode explicitly, by name, as the actual defect: not early
        timing, but the gate neither firing a card nor stating a default. State that this is
        exactly what Requirement 1's mode-select-checkpoint doctor class (plan 227-01,
        lib/core/doctor/mode-select-checkpoint-module.cjs) exists to catch, as an advisory warn,
        never a hard-fail, never a re-fired card.

    Do not restate the Hook Model's four full phases (Trigger, Action, Variable Reward,
    Investment) as generic summary text; the section's job is this ONE gate's specific timing
    reasoning, not a Hook Model primer. Keep it as dense and citation-heavy as the adjacent
    Voice Signature section, not a long essay.

    No em-dashes anywhere in the new section; use the double-hyphen convention this file already
    uses elsewhere (" -- ").
  </action>
  <verify>
    <automated>grep -ci ignite skills/larry-personality/SKILL.md</automated>
  </verify>
  <done>grep -ci ignite skills/larry-personality/SKILL.md is greater than zero; the new section contains the Prompt-not-Investment framing, the detect_dual_path citation, and the silent-skip failure mode named explicitly; the section sits between Voice Signature and Thinking Trace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| skills/larry-personality/SKILL.md prose | a repo-controlled doctrine file read by Claude at runtime to shape its own behavior, not executed code, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-227-09 | Tampering (doctrine drift) | a future edit accidentally reintroducing the "gate fires too early" misreading this section exists to foreclose | accept | this is a documentation-only, prose-level risk with no code or security surface; the mitigation IS the section itself, plus this phase's own SWEEP-FINDINGS and doctor checkpoint giving future sessions a structural signal instead of only prose |

No new attack surface: this plan touches one markdown doctrine file, adds no code, opens no new
sidechannel, and makes no package-manager installs; the package legitimacy gate does not apply.
</threat_model>

<verification>
grep -ci ignite skills/larry-personality/SKILL.md returns a number greater than zero. A manual
read of the new section confirms all four required content elements (a-d) are present and the
section is placed between Voice Signature and Thinking Trace.
</verification>

<success_criteria>
- skills/larry-personality/SKILL.md contains a new "## Ignite and the mode-select gate
  (Hooked-Model timing)" section.
- grep -ci ignite skills/larry-personality/SKILL.md is greater than zero.
- The section states the Prompt-not-Investment framing, the ambiguous-vs-signaled distinction
  citing detect_dual_path, and the silent-skip failure mode by name.
- Zero em-dashes in the new section.
</success_criteria>

<output>
Create `.planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-03-SUMMARY.md` when done
</output>
