---
phase: 227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sweep-skill-descriptions.cjs
  - .planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-SWEEP-FINDINGS.md
autonomous: true
requirements: [REQ-2]

must_haves:
  truths:
    - "Every skills/*/SKILL.md in the repo (123 files, confirmed live at planning time) has been scanned and classified, none silently skipped."
    - "Every skill flagged with a genuinely trivial description or frontmatter looseness has either been fixed inline this phase or explicitly deferred with a one-line reason, never left unclassified."
    - "No new hard-fail or blocking gate is introduced by any trivial fix (frontmatter tightening or in-file restraint text only)."
  artifacts:
    - path: "scripts/sweep-skill-descriptions.cjs"
      provides: "a reusable, repeatable CJS scanner over skills/*/SKILL.md that mechanically extracts sensor_triggers and description text and applies the D-06 looseness heuristic, so this sweep can be re-run for the deferred-real-work backlog in a future phase"
      min_lines: 40
    - path: ".planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-SWEEP-FINDINGS.md"
      provides: "one row per skill scanned: skill, sensor_triggers, description-tightness, verdict (clean, fixed-trivial, or deferred-real-work), fix-commit when applicable (D-07)"
      min_lines: 123
  key_links:
    - from: "scripts/sweep-skill-descriptions.cjs"
      to: "skills/*/SKILL.md"
      via: "fs.readFileSync over every skills/*/SKILL.md, frontmatter parse of sensor_triggers and description"
      pattern: "skills"
    - from: "227-SWEEP-FINDINGS.md"
      to: "trending-to-absurd's own 2026-06-24 fix (commit 7868dfbb)"
      via: "the calibration reference row proving the heuristic catches the confirmed pre-fix shape and does not false-positive on the confirmed post-fix shape"
      pattern: "trending-to-absurd"
---

<objective>
Run the systemic sweep SPEC Requirement 2 calls for: scan every methodology skill for the same
loose-description auto-fire bypass pattern that let trending-to-absurd reach past ignite's F.1
front door before its 2026-06-24 fix (FIX 1 conversational restraint doctrine, FIX 2 trigger
tightening, commit 7868dfbb). Fix the trivial instances found inline. Report, do not silently
fix, everything else.

Purpose: close item 2 of ignite-frontdoor-bypassed-methodology-overfire.md's fix_remaining list
verbatim: "SYSTEMIC sweep: other methodology skills for the same loose-description bypass (CIRS
R4 no-second-selection-brain)". Mirrors the accepted precedent in intern-w1-mode-gate-skip.md,
where a related sweep found 55 pre-existing contradictions and only the one named instance was
resolved in-session: report everything, fix only what is genuinely a one-line description or
frontmatter edit or a simple in-file orchestrator restraint addition, defer the rest.

Output: a reusable sweep script plus the 227-SWEEP-FINDINGS.md report, and (as a side effect of
the trivial-fix pass) zero or more one-line SKILL.md edits, each cited by commit in the report.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-SPEC.md
@.planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-CONTEXT.md

# The confirmed-clean reference shape this sweep's heuristic is calibrated against (post-fix):
@skills/trending-to-absurd/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and run the sweep classifier over every skills/*/SKILL.md (D-06)</name>
  <files>scripts/sweep-skill-descriptions.cjs</files>
  <action>
    Confirmed live at planning time: 123 skills/*/SKILL.md files exist; every one of them
    currently declares sensor_triggers as an empty array (a repo-wide grep at planning time
    found zero non-empty sensor_triggers anywhere). This means D-06's first clause (non-empty
    sensor_triggers is auto-fire capable by construction) currently flags nothing; the entire
    live risk surface is D-06's second clause, description text that reads like a broad or
    casual invitation rather than an explicit-intent gate, the exact trending-to-absurd
    pre-fix failure mode.

    Write scripts/sweep-skill-descriptions.cjs as a standalone CJS script (glob skills/*/SKILL.md
    via fs.readdirSync over the skills directory, no new dependency, this repo has no glob
    package installed and none is needed for a flat one-level directory scan). For each
    SKILL.md: parse the frontmatter block (the text between the first two "---" lines), extract
    sensor_triggers (should currently be an empty array for all 123, record the literal value
    regardless so the column is real, not assumed) and the description field (a folded YAML
    block scalar starting "description: >" and running until the next top-level frontmatter
    key, mirror the exact extraction approach 227-PATTERNS.md's illustrative test snippet for
    trending-to-absurd already uses: a regex capturing everything between "description: >" and
    the next top-level key).

    Apply this classification heuristic per skill (the exact regex terms are Claude's
    discretion per 227-CONTEXT.md, as long as it demonstrably flags the trending-to-absurd
    pre-fix shape and does not false-positive on the confirmed post-fix shape read from
    skills/trending-to-absurd/SKILL.md, already loaded via context above):
    - sensor_triggers non-empty: always flag as needing review regardless of description
      (none currently exist, but the script must still check this, not assume it).
    - description-tightness "tight": the description text contains an explicit-intent gating
      marker, for example a case-insensitive match on "only when", "explicitly", "on request",
      "do not use for", or "never use for". skills/trending-to-absurd/SKILL.md's live
      description (loaded via context) is the calibration example: it contains "Use ONLY when
      the navigator EXPLICITLY asks" and "Do NOT use for general exploration", so it must
      classify as tight. Verify this in the script's own self-test before trusting it against
      the other 122 files.
    - description-tightness "loose": no such marker is present. This is the candidate set for
      manual review in task 2, not an automatic verdict; a "loose" classification alone does
      NOT mean the skill is broken, many skills are passive reference or utility skills whose
      description was never meant to gate an auto-reach and are correctly "clean by inspection"
      per D-06's own text ("skills with sensor_triggers: [] and an explicit-intent description
      ... are clean by inspection, still listed in the report" implies the converse is not
      automatically unclean, it is a candidate for a human look).

    Have the script write its raw classification directly to a scratch JSON file (for example
    under the phase directory or a temp path) rather than requiring you to manually transcribe
    123 rows; task 2 turns that raw data into the final markdown report. Run the script and
    confirm it produces exactly 123 rows (or the live count at execution time if the skill
    count has changed since planning; recount with the same directory scan, do not hardcode
    123 as a hard assertion, use it as a sanity expectation logged to the console).

    No em-dashes anywhere in the script.
  </action>
  <verify>
    <automated>node scripts/sweep-skill-descriptions.cjs</automated>
  </verify>
  <done>Script runs to completion, exits 0, prints a total-scanned count matching the live skills/*/SKILL.md count, and its self-test confirms trending-to-absurd classifies as tight.</done>
</task>

<task type="auto">
  <name>Task 2: Review the loose-flagged subset, apply trivial fixes, write the final report (D-07)</name>
  <files>.planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-SWEEP-FINDINGS.md</files>
  <action>
    Read task 1's raw classification output. For every skill flagged "loose", read that one
    skill's SKILL.md description in full (a targeted read, not a re-scan of all 123) and decide,
    per the SPEC boundary, which bucket it belongs in:
    - fixed-trivial: the fix is a one-line description or sensor_triggers tightening (the same
      shape as trending-to-absurd's own FIX 2: add an explicit-intent qualifier, add a "do not
      use for" exclusion clause) OR a simple in-file orchestrator-level restraint addition
      comparable to trending-to-absurd's own restraint fix, when the pattern is shallow and
      self-contained. Apply the fix directly to that skill's SKILL.md (or its paired
      orchestrator file when the restraint text lives there), commit it with a descriptive
      message naming the phase and the skill, and record the resulting short commit hash in
      the report's fix-commit column.
    - deferred-real-work: anything requiring new code, new gates, cross-file changes, or
      genuine design work. Do not fix it. Record a one-line reason in the report (for example
      "requires a new sensor_triggers wiring decision" or "restraint text would need to span
      the orchestrator's own multi-stage flow, not a single line").

    Defensive rule: if the sweep flags skills/conversation-mode/SKILL.md or
    skills/larry-personality/SKILL.md as loose, do NOT fix either inline in this task, mark it
    deferred-real-work with the reason "touched by a concurrent Phase 227 plan (227-03 or
    227-04), avoid a cross-plan file conflict"; this is a real, confirmed risk since both files
    are edited elsewhere in this phase. In practice neither is expected to be genuinely
    invocation-shaped in the CIRS R4 sense (conversation-mode is connector.excluded: true,
    ambient always-on infra, and larry-personality is core personality doctrine, not an
    invocable methodology), so this branch is a safety net, not the expected path.

    Write the final .planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-SWEEP-FINDINGS.md
    with exactly one markdown table row per skill scanned (skill, sensor_triggers,
    description-tightness, verdict, fix-commit), a short header paragraph naming the heuristic
    used and citing this task's calibration reference (trending-to-absurd post-fix), and a
    summary count line (N clean, N fixed-trivial, N deferred-real-work, total matching the live
    skill count). Every fixed-trivial row cites a real commit hash, no placeholder text. Zero
    skills silently missing from the table.

    No em-dashes anywhere in the report or in any trivial fix applied to a SKILL.md.
  </action>
  <verify>
    <automated>grep -c "|" .planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-SWEEP-FINDINGS.md</automated>
  </verify>
  <done>227-SWEEP-FINDINGS.md exists, has one row per scanned skill (matching the live skills/*/SKILL.md count), every row has a verdict of clean, fixed-trivial, or deferred-real-work, and every fixed-trivial row cites a real commit hash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| scripts/sweep-skill-descriptions.cjs reading skills/*/SKILL.md | reads repo-controlled markdown files only, no external or user-supplied input, no network |
| trivial-fix edits to SKILL.md description text | edits to repo-controlled prose files consumed later by Claude's own skill-selection judgment, not executed code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-227-06 | Elevation of Privilege | a loosely-worded skill description causing Larry to auto-invoke a heavyweight methodology without explicit user intent | mitigate | this is the exact defect class Requirement 2 exists to reduce: trivial instances get an explicit-intent gate added inline this phase; anything deeper is named and deferred, never silently left loose |
| T-227-07 | Tampering | the sweep script itself producing a false "clean" verdict that hides a genuinely loose description | accept | the heuristic is explicitly calibrated against one confirmed positive (trending-to-absurd pre-fix shape, described in the debug file) and one confirmed negative (trending-to-absurd post-fix, read live); residual false negatives across the other 122 skills are a known, disclosed limitation of a regex heuristic, consistent with this phase's own established discipline of naming coverage gaps rather than silently dropping them (D-08's own precedent) |
| T-227-08 | Repudiation | a fixed-trivial row with no real commit hash, making a claimed fix unverifiable later | mitigate | task 2 explicitly requires every fixed-trivial row to cite a real, resulting commit hash, no placeholder text permitted |

No package-manager installs occur in this plan (zero new npm, pip, or cargo dependencies); the
package legitimacy gate does not apply.
</threat_model>

<verification>
node scripts/sweep-skill-descriptions.cjs exits 0. 227-SWEEP-FINDINGS.md exists with a row count
matching the live skills/*/SKILL.md count and a summary line whose three verdict counts sum to
that same total. Every fixed-trivial row's cited commit exists in git log.
</verification>

<success_criteria>
- scripts/sweep-skill-descriptions.cjs exists, runs cleanly, is reusable for a future re-sweep
  of the deferred-real-work backlog.
- 227-SWEEP-FINDINGS.md exists, covers every methodology skill, zero silently skipped.
- Every fixed-trivial entry cites its fix commit.
- No new hard-fail or blocking gate introduced by any trivial fix.
- Zero em-dashes in any file this plan touches.
</success_criteria>

<output>
Create `.planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-02-SUMMARY.md` when done
</output>
