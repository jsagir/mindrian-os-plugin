---
phase: 227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in
plan: 05
type: execute
wave: 2
depends_on: ["227-01"]
files_modified:
  - tests/test-227-frontdoor-restraint.cjs
  - lib/memory/run-feynman-tests.cjs
autonomous: true
requirements: [REQ-3]

must_haves:
  truths:
    - "Tester Test 4's fix (the clean ignite-F.1 first-touch on an explore-invitation, commit 7868dfbb) is now provable without a live human tester session."
    - "The test is a permanent regression floor: it runs every time lib/memory/run-feynman-tests.cjs runs, not a one-time check."
    - "The one honestly unscriptable assertion (no opening compliment) is named as a known coverage gap in the test's own header, not silently dropped."
  artifacts:
    - path: "tests/test-227-frontdoor-restraint.cjs"
      provides: "four structural/static assertions replaying ignite-frontdoor-bypassed-methodology-overfire.md's Test 4 Section 5 scenario: (a) trending-to-absurd sensor_triggers regression floor, (b) trending-to-absurd description explicit-intent regression floor, (c) orchestrator.cjs single-horizon-honored regression floor, (d) conversation-mode Mode 2 restraint text regression floor"
      min_lines: 40
      exports: []
  key_links:
    - from: "tests/test-227-frontdoor-restraint.cjs"
      to: "lib/memory/run-feynman-tests.cjs TEST_FILES"
      via: "a path.join(REPO_ROOT, 'tests', 'test-227-frontdoor-restraint.cjs') array entry"
      pattern: "test-227-frontdoor-restraint.cjs"
    - from: "tests/test-227-frontdoor-restraint.cjs assertion (c)"
      to: "lib/core/trending-to-absurd/orchestrator.cjs generateAbsurdRings"
      via: "reading the horizon-clamp branch source text, not executing the function"
      pattern: "HORIZON_ENUM"
---

<objective>
Build the scripted regression fixture SPEC Requirement 3 calls for: a deterministic test that
replays ignite-frontdoor-bypassed-methodology-overfire.md's Test 4 scenario (an
explore-invitation in a fresh context) and asserts the 2026-06-24 fix (FIX 1 conversational
restraint doctrine, FIX 2 trigger tightening, commit 7868dfbb) still holds, without requiring a
live human tester session.

Purpose: close item 4 of that debug file's fix_remaining list verbatim: "re-run tester Test 4 to
confirm the clean ignite-F.1 first-touch is restored." The fix landed 2026-06-24 with zero
automated coverage; verification since then has only ever been a human tester re-run. This test
becomes the permanent regression floor so a future change cannot silently reopen the exact
over-fire this phase's own Requirement 2 sweep is hunting for elsewhere.

Output: tests/test-227-frontdoor-restraint.cjs, registered in
lib/memory/run-feynman-tests.cjs's TEST_FILES array.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-SPEC.md
@.planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-CONTEXT.md

# The structural/static test convention this file follows (read in full)
@tests/test-209-declared-implies-wired.cjs

# The four files this test's assertions read
@skills/trending-to-absurd/SKILL.md
@lib/core/trending-to-absurd/orchestrator.cjs
@skills/conversation-mode/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/test-227-frontdoor-restraint.cjs (D-08)</name>
  <files>tests/test-227-frontdoor-restraint.cjs</files>
  <behavior>
    - Assertion (a): skills/trending-to-absurd/SKILL.md's frontmatter sensor_triggers is still
      the empty array (the regression floor on the 2026-06-24 fix; confirmed live at planning
      time as "sensor_triggers: []" inside the connector block).
    - Assertion (b): that same file's description field still gates on explicit intent.
      Confirmed live at planning time, the description literally contains "Use ONLY when the
      navigator EXPLICITLY asks" and "Do NOT use for general exploration". Assert on the
      presence of this explicit-intent gating language, not a denylist of casual words: a naive
      denylist on words like "opportunities" would FALSE-POSITIVE on this exact file, since its
      own Do-NOT-use exclusion clause deliberately quotes casual phrasing as a negative example
      ("there have got to be some opportunities here" appears inside the description precisely
      BECAUSE it is listed as a phrase that must NOT trigger this skill). Assert the positive
      signal is present (an EXPLICITLY/only-when marker plus a "do not use for general
      exploration" clause), not the absence of casual words.
    - Assertion (c): lib/core/trending-to-absurd/orchestrator.cjs's horizon logic still honors
      a single requested horizon rather than unconditionally forcing all three. Confirmed live
      at planning time: generateAbsurdRings clamps via
      "const specHorizon = HORIZON_ENUM.includes(horizon) ? horizon : 'near';". Read the
      function's source text and assert this clamp-to-requested-horizon branch is still present
      (assert on the branching text itself, not on the function's runtime output, matching this
      repo's structural/static test convention).
    - Assertion (d): skills/conversation-mode/SKILL.md's Mode 2 restraint text is still present.
      Confirmed live at planning time, the exact live string in Mode 2 is "THE SCAFFOLD FOLLOWS
      THE LEARNER" (uppercase, the RCA citation line: "THE SCAFFOLD FOLLOWS THE LEARNER (RCA
      ignite-frontdoor-bypassed-methodology-overfire)"). Assert on this exact live string, not
      the lowercase "the scaffold follows the learner" phrasing that lives in
      trending-to-absurd's OWN description text (a different file); do not confuse the two,
      they are two separate occurrences of a related phrase in two different files with two
      different casings.
    - No opening-compliment assertion is scriptable, that is model behavior at inference time,
      not a static artifact. State this honestly in the test file's own header comment as a
      known coverage gap, matching this session's established discipline of naming gaps rather
      than silently dropping them (the exact same discipline D-08 itself documents).
  </behavior>
  <action>
    Write tests/test-227-frontdoor-restraint.cjs following the same structural and static test
    convention as tests/test-209-declared-implies-wired.cjs, already loaded via context above:
    a small micro-harness (a counter variable plus an ok(description, function) helper that
    runs the function, increments the counter, and logs a line), node:assert/strict for the
    assertions, a REPO constant resolved as the parent of __dirname, real fs.readFileSync calls
    against the real repo files (never simulate an LLM turn, never mock file contents), and a
    final PASS line printing the total assertion count.

    Open the file with a header comment naming the phase and Requirement 3, and explicitly
    stating the known coverage gap (no opening-compliment assertion is scriptable). Implement
    the four behaviors above as four ok(...) blocks in order (a) through (d). Prefer reading
    each source file once per assertion (small files, this is fine at this scale) over caching
    across assertions, matching the analog's own straightforward per-behavior read style.

    No em-dashes anywhere in the file.
  </action>
  <verify>
    <automated>node tests/test-227-frontdoor-restraint.cjs</automated>
  </verify>
  <done>node tests/test-227-frontdoor-restraint.cjs exits 0 and prints a PASS line with exactly 4 assertions, the header comment names the opening-compliment coverage gap.</done>
</task>

<task type="auto">
  <name>Task 2: Register the test in run-feynman-tests.cjs and confirm the full suite still passes</name>
  <files>lib/memory/run-feynman-tests.cjs</files>
  <action>
    Register tests/test-227-frontdoor-restraint.cjs in lib/memory/run-feynman-tests.cjs's
    TEST_FILES array (the REPO_ROOT constant is already resolved near the top of that file).
    Append immediately before the closing bracket of the array, following the exact same
    path.join(REPO_ROOT, 'tests', name) shape the existing Phase 226 entries use, the most
    recent entries in the array today (note: plan 227-01, if it has already landed by the time
    this task runs, will have appended its own test-227-mode-select-checkpoint.cjs entry
    immediately before this same closing bracket; append this new entry after that one, do not
    remove or reorder it). Add a short comment above the new line naming Phase 227 and
    Requirement 3, mirroring the existing per-phase comment convention in that file.

    No other change to run-feynman-tests.cjs is needed, its spawnSync loop already generically
    handles any file appended to TEST_FILES.

    No em-dashes anywhere in the file.
  </action>
  <verify>
    <automated>node tests/test-227-frontdoor-restraint.cjs</automated>
  </verify>
  <done>tests/test-227-frontdoor-restraint.cjs is registered exactly once in lib/memory/run-feynman-tests.cjs's TEST_FILES array, immediately after (or in place beside) plan 227-01's own entry if present, without disturbing any existing entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| tests/test-227-frontdoor-restraint.cjs reading repo-controlled source files | reads only files this repo already ships (skill docs, an orchestrator module), no external or user-supplied input, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-227-13 | Repudiation (a false-positive assertion that would pass even after a real regression) | assertion (b)'s denylist-vs-positive-signal design choice | mitigate | deliberately designed as a positive-signal assertion (explicit-intent marker present) rather than a denylist, specifically because a denylist was proven during planning to false-positive on the file's own legitimate Do-NOT-use quoted example; this is a genuine correctness fix over 227-PATTERNS.md's own illustrative denylist snippet |
| T-227-14 | Repudiation (a known, disclosed gap being mistaken for full coverage) | the unscriptable opening-compliment behavior | accept | named explicitly in the test file's own header comment as a known coverage gap, matching this session's established discipline (D-08); a future human tester re-run remains the only way to verify that specific behavior, this test does not claim to replace it |

No package-manager installs occur in this plan (zero new npm, pip, or cargo dependencies); the
package legitimacy gate does not apply.
</threat_model>

<verification>
node tests/test-227-frontdoor-restraint.cjs exits 0 with 4 assertions. grep -c
test-227-frontdoor-restraint.cjs lib/memory/run-feynman-tests.cjs equals 1.
</verification>

<success_criteria>
- tests/test-227-frontdoor-restraint.cjs exists, passes, covers all four D-08 behaviors, and
  names the opening-compliment coverage gap honestly in its header.
- Registered exactly once in lib/memory/run-feynman-tests.cjs's TEST_FILES array.
- Zero em-dashes in any file this plan touches.
</success_criteria>

<output>
Create `.planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-05-SUMMARY.md` when done
</output>
