---
phase: 227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in
plan: 04
type: execute
wave: 2
depends_on: ["227-01"]
files_modified:
  - lib/hmi/selector-dispatcher.cjs
  - skills/conversation-mode/SKILL.md
autonomous: true
requirements: [REQ-1, REQ-5]

must_haves:
  truths:
    - "When the F.1 mode-select lane-picker card actually renders, that firing is now recorded to the sidechannel plan 227-01 built (D-03 call site 1)."
    - "When Larry proceeds directly from an already-signaled opener without firing the F.1 card, that resolution is now recorded too (D-03 call site 2), so only the genuine silent-skip case leaves no record."
    - "Mode 3 (Build a Room) no longer invokes /mos:new-project directly; it routes through ignite's Directive/--express path and reaches Gate B2 without re-triggering Gate B1's four-door card."
  artifacts:
    - path: "lib/hmi/selector-dispatcher.cjs"
      provides: "an additive recordLanePick call inside pickShape's existing emitTelemetry trailer, scoped to the F.1 mode-select card specifically (not every F.1 gate in the app), wrapped in its own try/catch"
      contains: "mode-select-sidechannel"
    - path: "skills/conversation-mode/SKILL.md"
      provides: "(1) a Lane Picker section addition instructing Larry to record a default-stated resolution via recordLanePick when the lane is inferred without firing the card, and (2) Mode 3's body text routing through /mos:ignite --express instead of /mos:new-project directly"
      contains: "recordLanePick"
  key_links:
    - from: "lib/hmi/selector-dispatcher.cjs pickShape trailer"
      to: "lib/core/mode-select-sidechannel.cjs recordLanePick"
      via: "a require call inside the same emitTelemetry-gated block the existing card-fire-sidechannel producer already uses"
      pattern: "mode-select-sidechannel"
    - from: "skills/conversation-mode/SKILL.md Mode 3"
      to: "commands/ignite.md Entry Routing Directive path and Gate B2"
      via: "invoking /mos:ignite --express with the already-established conversational context as blueprint seed"
      pattern: "ignite"
---

<objective>
Close the second half of Requirement 1 (D-03's two call sites: the code-level card-fire site
and the prose-instructed default-stated site) and deliver Requirement 5 (conversation-mode Mode
3 routes through ignite instead of calling /mos:new-project directly). Both land in
skills/conversation-mode/SKILL.md, so they are grouped in one plan to avoid two plans editing
the same file in the same wave. This plan depends on 227-01 because both edits require and cite
lib/core/mode-select-sidechannel.cjs's recordLanePick export existing and being correct first.

Purpose: without this plan, plan 227-01's sidechannel and doctor check exist but nothing ever
calls recordLanePick in a real session, so the checkpoint would always report warn (or, if
has_user_turn defaults false outside a live session, silently ok) without ever reflecting real
gate resolutions. This plan is what makes Requirement 1 actually observe reality, not just be
capable of observing it in a test.

Output: an additive edit to lib/hmi/selector-dispatcher.cjs's pickShape trailer, and two prose
edits to skills/conversation-mode/SKILL.md (the Lane Picker section and Mode 3's body text).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-SPEC.md
@.planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-CONTEXT.md
@.planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-PATTERNS.md

# The module this plan wires (built in 227-01, must land first)
@lib/core/mode-select-sidechannel.cjs

# The file this plan edits, read in full first (already 170 lines, small)
@skills/conversation-mode/SKILL.md

# The confirmed live Entry Routing / Gate B1 / Gate B2 text this plan routes Mode 3 through
@commands/ignite.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the card-fire call site in selector-dispatcher.cjs's pickShape trailer (D-03 site 1)</name>
  <files>lib/hmi/selector-dispatcher.cjs</files>
  <action>
    Confirmed live at planning time: pickShape's existing telemetry trailer (inside the
    "if (payloadObj.emitTelemetry === true)" block, itself inside "if (isFShape)", roughly
    lines 1071-1096) already calls card-fire-sidechannel.cjs's recordReachedGate with a
    gateSubjectText built by joining result.rendered.zones.header and
    result.rendered.zones.body. Add an ADDITIVE sibling block immediately after that existing
    call (do not replace or modify the existing card-fire-sidechannel call), in its own
    try/catch so a fault in this new block never affects the existing block or the presentation
    result.

    Precision requirement: do NOT record a lane pick for every F.1 gate in the app. F.1 is used
    by other Decision Gates too (for example trending-to-absurd's own trend-selection stage
    also renders as F.1). Scope this call specifically to the mode-select lane-picker card by
    checking result.shape === 'F.1' AND the joined gateSubjectText (reuse the same header+body
    join the existing block already computes, do not recompute it a second time, just read the
    same local variable) contains both "brainstorming" and "building something"
    (case-insensitive), the two words unique to the Lane Picker's exact card text from
    skills/conversation-mode/SKILL.md's Lane Picker section: "Are we just chatting,
    brainstorming, or building something?" When both match, call
    require('../core/mode-select-sidechannel.cjs').recordLanePick with lane: 'card-fired' (this
    call site only knows a card was fired, not which of the three lanes the user later picks,
    since the trailer fires at render time before the answer; 'card-fired' presence is
    sufficient for the doctor checkpoint's purpose, which only needs to know the gate did not
    stay silent). sessionId is not in scope at this call site either, matching the existing
    card-fire-sidechannel producer's own documented gap at this exact location (it also files
    under NO_SESSION_KEY); do not attempt to thread one in, the sidechannel's own union-read
    already accounts for this.

    No em-dashes anywhere in the new block.
  </action>
  <verify>
    <automated>node -c lib/hmi/selector-dispatcher.cjs</automated>
  </verify>
  <done>The file parses cleanly, the new block sits inside the existing emitTelemetry trailer as an additive sibling to the card-fire-sidechannel call, wrapped in its own try/catch, scoped to F.1 mode-select cards specifically via the subject-text check.</done>
</task>

<task type="auto">
  <name>Task 2: Conversation-mode.md default-stated recording + Mode 3 routes through ignite (D-03 site 2, D-11, D-12)</name>
  <files>skills/conversation-mode/SKILL.md</files>
  <action>
    Two edits to the same file, apply both.

    Edit A, Lane Picker section (D-03's second call site). This gate is enforced by prose
    alone; there is no code hook for "the lane was inferred from an already-signaled opener
    without firing the card" because that resolution happens inside Larry's own prose-driven
    judgment, not a code path (confirmed during planning: neither selector-dispatcher.cjs nor
    scripts/intent-classifier.cjs's engine-arm or emitBindingGate seams are the right site,
    since the mode-select gate is connector.excluded: true, ambient infra that never flows
    through the sensor-triggered dispatchSensors -> decide() -> resolver pipeline those seams
    serve). Confirmed live: the Lane Picker section's existing bullet list ends with "One
    re-surface per turn-cluster. Do not nag the picker every turn." and "Never auto-switch
    lanes. A lane change is always a navigator pick at the Decision Gate, never a unilateral
    Larry decision." Add one new bullet immediately after those two, instructing Larry that
    whichever way the lane resolves this session (the F.1 card fires, or the user's opener
    already signals a lane clearly enough that Larry proceeds without firing the card, the
    ambiguous-vs-signaled distinction the larry-personality.md Hooked-Model section documents,
    plan 227-03) it gets recorded so the mode-select-checkpoint doctor class (plan 227-01) does
    not see a silent skip. When the card fires, selector-dispatcher.cjs's trailer records it
    automatically (task 1 above, this plan). When Larry proceeds directly from an
    already-signaled opener, record it explicitly with a one-line invocation:
    node -e require('<plugin_root>/lib/core/mode-select-sidechannel.cjs').recordLanePick with
    an object literal lane: 'default-stated' -- mirror this same file's own existing node -e
    convention already used above in the Scratchpad Persistence section (for example the
    scratchpad-ops.cjs require pattern with the <plugin_root> placeholder token), reuse that
    exact placeholder token verbatim, do not invent a new one.

    Edit B, Mode 3 (SPEC Requirement 5, D-11/D-12). Confirmed live: the "## Mode 3: Build a
    Room" section currently reads exactly:
    "- Immediately say: "Let us set up your Data Room." and invoke the /mos:new-project flow.
    - No exploratory conversation needed."
    Replace the first bullet (keep the second bullet, "No exploratory conversation needed.",
    unchanged) so Mode 3 invokes /mos:ignite --express instead of /mos:new-project directly,
    carrying the already-established conversational context (persona, problem, venture) forward
    as the blueprint seed. State that this is ignite's Entry Routing Directive/Imperative path
    (commands/ignite.md's "## Entry Routing" section, confirmed live), and that because
    conversation-mode's own Mode 2-to-Mode-3 transition already establishes the navigator's
    persona and intent, this Directive path has a determinable role/venture and therefore
    bypasses Gate B1 entirely per that gate's own documented rule (commands/ignite.md Gate B1:
    "Directive paths with a determinable role/venture ... bypass B1", confirmed live at that
    file's exact text), proceeding straight to Gate B2 (Blueprint), the actual room-creation
    step. Do not literally write the string "/mos:new-project" anywhere in the new text (the
    acceptance check is a grep for that exact string no longer appearing as a direct Mode-3
    invocation; simplest way to guarantee this is to not use the string at all in the
    replacement). Do not touch Mode 1's or Mode 2's own separate new-project references
    (Mode 1's "Do NOT suggest /mos:new-project unless..." line and Mode 2's "suggest
    transitioning to /mos:new-project..." line both stay exactly as they are; D-11 scopes this
    change to Mode 3's body text only, not the other two modes). Do not add a fifth Gate B1
    door, do not reference Phase 223 anywhere in the changed text (D-12): Gate B1's four-door
    structure in commands/ignite.md must remain byte-for-byte unmodified, this plan does not
    touch commands/ignite.md at all.

    No em-dashes anywhere in either edit.
  </action>
  <verify>
    <automated>grep -c "new-project" skills/conversation-mode/SKILL.md</automated>
  </verify>
  <done>The Lane Picker section contains the default-stated recording instruction citing recordLanePick; Mode 3's first bullet routes through /mos:ignite --express and cites Gate B1's bypass rule and Gate B2, with zero literal "/mos:new-project" occurrences inside the Mode 3 section specifically (Mode 1 and Mode 2's pre-existing references are untouched and still present elsewhere in the file); commands/ignite.md is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| lib/hmi/selector-dispatcher.cjs's new additive trailer block | reads only the gate's own already-rendered content (header/body text this same function just constructed), no external or user-typed input reaches this block directly |
| skills/conversation-mode/SKILL.md prose instructing a node -e shell invocation | a documented, repo-convention pattern already used elsewhere in this exact file (scratchpad-ops.cjs, bank-opportunity); the new instruction follows the same shape, invoking only this phase's own local, argument-free recordLanePick call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-227-10 | Denial of Service | the new trailer block throwing and taking down pickShape's presentation result | mitigate | wrapped in its own try/catch, isolated from the existing card-fire-sidechannel block's own try/catch, matching that block's own isolation discipline (T-209-24's never-block precedent) |
| T-227-11 | Tampering (over-broad recording defeating the checkpoint's purpose) | recording a lane pick on every F.1 gate rather than specifically the mode-select card, which would make the doctor checkpoint always report ok regardless of whether the real mode-select gate fired | mitigate | the subject-text scoping check (both "brainstorming" and "building something" present) narrows the recording to the mode-select card specifically, a deliberate design correction made during planning since 227-PATTERNS.md's own illustrative snippet did not include this scoping |
| T-227-12 | Elevation of Privilege | the prose-instructed node -e invocation being copy-pasted or misused to call an arbitrary function | accept | the instruction is a fixed, argument-free one-liner calling only recordLanePick with a fixed lane value, identical in shape and risk level to this same file's own pre-existing scratchpad-ops.cjs and bank-opportunity node -e instructions, LOCAL-only, zero Brain egress, zero network |

No package-manager installs occur in this plan (zero new npm, pip, or cargo dependencies); the
package legitimacy gate does not apply.
</threat_model>

<verification>
node -c lib/hmi/selector-dispatcher.cjs exits 0 (syntax check). grep -c "new-project"
skills/conversation-mode/SKILL.md shows the pre-existing Mode 1 and Mode 2 references only, none
inside the Mode 3 section. grep -c recordLanePick skills/conversation-mode/SKILL.md is greater
than zero. commands/ignite.md is byte-identical to its state before this plan (git diff shows no
changes to that file).
</verification>

<success_criteria>
- lib/hmi/selector-dispatcher.cjs's pickShape trailer records a mode-select card fire, scoped
  specifically to the F.1 lane-picker card, additively and without disturbing the existing
  card-fire-sidechannel call.
- skills/conversation-mode/SKILL.md's Lane Picker section instructs recording the
  default-stated resolution.
- skills/conversation-mode/SKILL.md's Mode 3 routes through /mos:ignite --express, reaching
  Gate B2 via the Directive-path bypass rule, without touching commands/ignite.md.
- Zero em-dashes in any file this plan touches.
- No new hard-fail or blocking gate introduced.
</success_criteria>

<output>
Create `.planning/phases/227-ignite-mode-select-timing-across-turns-1-4-seed-060-close-in/227-04-SUMMARY.md` when done
</output>
