---
phase: 222-reach-ranking-unification-replace-the-three-disagreeing-what
plan: 04
type: execute
wave: 4
depends_on: [222-03]
files_modified:
  - tests/test-222-zero-deps.cjs
  - tests/run-all-222.sh
  - docs/ENV-TUNING.md
autonomous: true
requirements: [4, 5, 6]
must_haves:
  truths:
    - "bash tests/run-all-222.sh exits PASS with FAIL=0 SKIP=0 on the fully-landed phase (SPEC phase-level acceptance)"
    - "The new module tree requires nothing outside node:* and this repo's lib/ and data/ trees; git diff package.json package-lock.json is empty (Req 4)"
    - "The Part 8 (no prose/egress) and Part 9 (chokepoint-only) grep sweeps over the new files run as harness legs with comment-strip hygiene, mirroring run-all-158.sh"
    - "MINDRIAN_HEDGE_UPDATE_N and MINDRIAN_HEDGE_ETA are documented in docs/ENV-TUNING.md with defaults and rationale"
  artifacts:
    - path: "tests/run-all-222.sh"
      provides: "The D-04 phase harness: 7 node legs + grep-sweep legs + the Req 4 dependency-diff leg, run_if-guarded"
      min_lines: 60
    - path: "tests/test-222-zero-deps.cjs"
      provides: "Req 4 require-tripwire over the three new source files"
      min_lines: 30
    - path: "docs/ENV-TUNING.md"
      provides: "Additive rows for the two Phase 222 env tunables"
      contains: "MINDRIAN_HEDGE_UPDATE_N"
  key_links:
    - from: "tests/run-all-222.sh"
      to: "tests/test-222-*.cjs"
      via: "run_if-guarded legs (partial-landing safe)"
      pattern: "run_if \"222-"
    - from: "tests/run-all-222.sh"
      to: "lib/workflow/reach-hedge-ranker.cjs"
      via: "comment-stripped Part 8 / Part 9 grep sweeps (the run-all-158.sh legs b/c idiom)"
      pattern: "strip_comments"
---

<objective>
Close the phase with the D-04 harness: `tests/run-all-222.sh` mirroring `run-all-209.sh`'s
run/run_if aggregator shape, carrying all seven node test legs (Reqs 1-3, 5-7, plus the
Wave 1 weight-state leg), the Req 4 zero-dependency tripwire (`test-222-zero-deps.cjs` +
a `git diff --quiet package.json package-lock.json` leg), and the run-all-158.sh-style
comment-stripped grep sweeps enforcing Part 8 (no prose/egress in the new surfaces) and
Part 9 (chokepoint-only db access in the new module). Document the two D-03 env tunables
in docs/ENV-TUNING.md. The full suite green at FAIL=0 SKIP=0 is the phase gate.

Purpose: turns seven per-plan proofs into one command (`bash tests/run-all-222.sh`) and
makes the phase's constitutional constraints permanent tripwires, not one-time reviews.
Output: harness + tripwire test + docs; the phase's acceptance checklist fully runnable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-SPEC.md
@.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-PATTERNS.md
@.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-03-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

- `tests/run-all-222.sh` (the phase aggregator; legs 222-01 through 222-07 + grep sweeps + the dependency-diff leg)
- `tests/test-222-zero-deps.cjs` (Req 4 require-tripwire)
- `docs/ENV-TUNING.md` additive entries: `MINDRIAN_HEDGE_UPDATE_N` (default 50), `MINDRIAN_HEDGE_ETA` (default 0.3)

<tasks>

<task type="auto">
  <name>Task 1: The zero-deps tripwire + run-all-222.sh with constitutional grep legs (Req 4, D-04)</name>
  <files>tests/test-222-zero-deps.cjs, tests/run-all-222.sh</files>
  <read_first>
    - tests/run-all-209.sh (the aggregator analog: set -uo pipefail, run/run_if helpers, PASS/FAIL/SKIP counters, exit-nonzero-on-FAIL tail - PATTERNS.md carries the verbatim scaffold to copy)
    - tests/run-all-158.sh (legs b/c: the strip_comments helper at :72-76 - grep -vE '^[[:space:]]*(//|\*|/\*)' - and the Part 8 reason-sweep + Part 9 chokepoint-sweep leg shapes to mirror; comment-stripping is MANDATORY so header prose never trips a gate)
    - lib/workflow/reach-hedge-ranker.cjs, lib/core/navigation/ranker-weights.cjs, lib/core/migrations/phase-222-ranker-weights.cjs (the three swept files - confirm their actual require lists before writing the tripwire's allowlist)
  </read_first>
  <action>
    Create tests/test-222-zero-deps.cjs (node:assert/strict): for each of the three new source
    files (lib/workflow/reach-hedge-ranker.cjs, lib/core/navigation/ranker-weights.cjs,
    lib/core/migrations/phase-222-ranker-weights.cjs), read the file, strip comment lines,
    extract every require('...') target with a regex, and assert each target either starts with
    'node:' or resolves to a relative path inside this repo's lib/ or data/ trees (path.resolve
    against the file's dirname, then startsWith check on the repo's lib/ and data/ roots). Any
    other target (a bare package name, zod, anything npm-shaped) fails with the offending file
    and target named (the Req 4 acceptance tripwire, the Phase-90 5-tripwire pattern).

    Create tests/run-all-222.sh copying PATTERNS.md's verbatim scaffold (run/run_if helpers,
    counters, ROOT cd, final [ "$FAIL" -eq 0 ]). Legs in order, all run_if-guarded on their file:
      222-01 weight-state (Req 3/7 substrate)  node tests/test-222-weight-state.cjs
      222-02 rank-fired (Req 1)                node tests/test-222-rank-fired.cjs
      222-03 reach-wired (Req 2+6)             node tests/test-222-reach-wired.cjs
      222-04 hedge-update (Req 3)              node tests/test-222-hedge-update.cjs
      222-05 frozen-scalars (Req 5)            node tests/test-222-frozen-scalars.cjs
      222-06 degrade (Req 7)                   node tests/test-222-degrade.cjs
      222-07 zero-deps (Req 4)                 node tests/test-222-zero-deps.cjs
    Then three inline sweep legs (mirror run-all-158.sh's inline-leg style, each counted in
    PASS/FAIL):
      (a) Part 8 sweep: define strip_comments exactly as run-all-158.sh:72-76; over
          lib/workflow/reach-hedge-ranker.cjs AND lib/core/navigation/ranker-weights.cjs assert
          zero comment-stripped matches for the egress/prose pattern
          "fetch\(|https?://|require\(['\"]node:http|\.reason\b" (no network, no freeform reason
          reads/writes in the new surfaces);
      (b) Part 9 sweep: over lib/workflow/reach-hedge-ranker.cjs assert zero comment-stripped
          matches for "require\(['\"]node:sqlite|better-sqlite3|\bDatabaseSync\b|\bfs\.(read|write)"
          AND at least one match for "require\(['\"]\.\./core/navigation\.cjs['\"]\)" (reads ONLY
          via the chokepoint - the 158 leg-c shape);
      (c) Req 4 dependency-diff leg: git diff --quiet package.json package-lock.json (fails the
          harness if either file drifted).
    Header comment states the two-part contract (all legs green = phase gate) and the Part 8
    no-egress rule. bash only, no network, no model downloads. No em-dashes.
  </action>
  <verify>
    <automated>bash -n tests/run-all-222.sh && bash tests/run-all-222.sh</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: bash tests/run-all-222.sh prints "Phase 222: PASS=10 FAIL=0 SKIP=0" (7 node legs + 3 sweep legs) and exits 0
    - Behavior: node tests/test-222-zero-deps.cjs exits 0; temporarily appending require('zod') to a scratch copy makes it exit nonzero (self-check the tripwire bites, then discard the scratch copy)
    - Source: grep -c "strip_comments" tests/run-all-222.sh >= 2 (helper defined and used - the grep-gate-hygiene idiom; bare unfiltered == 0 gates are forbidden)
    - Source: grep -c "run_if" tests/run-all-222.sh >= 7 (every node leg partial-landing safe)
    - Behavior: git diff --quiet package.json package-lock.json exits 0 (Req 4 acceptance)
  </acceptance_criteria>
  <done>One command proves the whole phase; the zero-dependency, no-egress, and chokepoint-only constraints are permanent harness legs, not review-time promises.</done>
</task>

<task type="auto">
  <name>Task 2: ENV-TUNING docs + the full phase gate run (D-03 documentation, phase acceptance)</name>
  <files>docs/ENV-TUNING.md</files>
  <read_first>
    - docs/ENV-TUNING.md (the existing entry format - table/section shape, how defaults and rationale are stated; additive edit only)
    - lib/workflow/reach-hedge-ranker.cjs (the shipped defaults and TUNABLE-LATER rationale comments - the doc must state the SAME numbers the code reads, no hand-typed drift)
    - .planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-SPEC.md (the phase-level Acceptance Criteria checklist this task closes out)
  </read_first>
  <action>
    Additively document in docs/ENV-TUNING.md, following the file's existing entry format:
    MINDRIAN_HEDGE_UPDATE_N (default 50 - the Hedge weight-update debounce window in qualifying
    f_selector_decision events; D-03, SEED-009's own precedent number; per-event updates rejected
    for thrash risk) and MINDRIAN_HEDGE_ETA (default 0.3 - the Hedge/MWU learning rate; bounds a
    single fold's multiplicative swing; Arora-Hazan-Kale 2012). Both read defensively with numeric
    fallbacks in lib/workflow/reach-hedge-ranker.cjs. State that both are room-local ranking
    tunables with zero egress (Part 8). No em-dashes.

    Then run the phase gate end to end and record results in the SUMMARY: bash
    tests/run-all-222.sh (must exit PASS FAIL=0 SKIP=0 - the SPEC's final acceptance line);
    git diff --quiet package.json package-lock.json; node scripts/build-connector-registry.cjs
    --check; and re-run the two adjacent-suite regressions node tests/test-213-reach-wired.cjs
    and node tests/test-198-contract-schema.test.cjs. Walk SPEC.md's 8-item Acceptance Criteria
    checklist and record each as met with its proving command - any unmet item BLOCKS the phase
    summary (surface it, do not paper over it).
  </action>
  <verify>
    <automated>bash tests/run-all-222.sh && git diff --quiet package.json package-lock.json && node scripts/build-connector-registry.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: bash tests/run-all-222.sh exits 0 with FAIL=0 SKIP=0
    - Source: grep -c "MINDRIAN_HEDGE_UPDATE_N\|MINDRIAN_HEDGE_ETA" docs/ENV-TUNING.md == 2 (one entry each)
    - Behavior: the documented defaults byte-match the code's HEDGE_UPDATE_N_DEFAULT / HEDGE_ETA_DEFAULT values (grep both files, compare)
    - Behavior: node scripts/build-connector-registry.cjs --check exits 0 (Part 11 stays green)
    - Behavior: all 8 SPEC.md Acceptance Criteria lines are individually checked off with proving commands in the plan SUMMARY
  </acceptance_criteria>
  <done>The two tunables are documented at their shipped defaults, every SPEC acceptance line has a green proving command, and the phase is gate-ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
This phase is room-local only (Canon Part 8, no egress). This plan ships no runtime
behavior - it ships the tripwires that make the phase's security posture permanent.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future edits -> new modules | the harness legs are the standing gate against regression re-introducing egress, prose leaks, chokepoint bypasses, or npm deps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-222-02 | Information disclosure (Part 8) | prose/egress creeping into the new surfaces later | mitigate | Harness leg (a): comment-stripped fetch/http/.reason sweep over both new modules, runs on every bash tests/run-all-222.sh |
| T-222-03 | Tampering (Part 9 breach) | future direct-SQL bypass in the ranker | mitigate | Harness leg (b): comment-stripped node:sqlite/DatabaseSync/fs sweep + mandatory navigation.cjs require assertion |
| T-222-SC | Tampering (supply chain) | a dependency sneaking into the new tree | mitigate | test-222-zero-deps.cjs require-allowlist (node:* + repo lib/ and data/ only) + the git-diff harness leg on package.json/package-lock.json; Req 4 locked |
| T-222-04 | Repudiation | phase declared done without runnable evidence | mitigate | The SPEC acceptance checklist is closed item-by-item with proving commands in the SUMMARY; FAIL=0 SKIP=0 is a hard exit-code gate, not prose |
</threat_model>

<verification>
- bash tests/run-all-222.sh exits 0, PASS=10 FAIL=0 SKIP=0
- git diff package.json package-lock.json empty (Req 4)
- node scripts/build-connector-registry.cjs --check exits 0
- All 8 SPEC.md Acceptance Criteria recorded green with commands in the SUMMARY
</verification>

<success_criteria>
- One command proves the phase; the harness is partial-landing safe (run_if) and hygiene-correct (strip_comments)
- Req 4 provably holds: zero new dependencies, tripwired forever
- The two D-03 tunables are documented at their shipped defaults with rationale
</success_criteria>

<output>
Create `.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-04-SUMMARY.md` when done
</output>
