---
phase: 222-reach-ranking-unification-replace-the-three-disagreeing-what
plan: 03
type: execute
wave: 3
depends_on: [222-02]
files_modified:
  - lib/core/navigation-engine.cjs
  - lib/mcp/tools/sensors.cjs
  - tests/test-222-reach-wired.cjs
autonomous: true
requirements: [1, 2, 6]
must_haves:
  truths:
    - "A live decide() call on a fixture turn where the registry-order-first and highest-scored candidates differ produces a fire_skill verb matching the HIGHEST-SCORED candidate (Req 2)"
    - "A live suggest_next / reach_candidates call through the REAL MCP tool registration returns candidates in combined-score order, not SENSOR_REGISTRY order (Req 1, Req 6)"
    - "The Wicked-escalation precedence (outranks any sensor reach) and the dead-Brain / empty-array degrade path in resolveFireSkill are byte-untouched and regression-proven"
    - "The scored path is proven live-reachable via real registration, not a bypassed internal function call (Req 6, the Phase 150.5 anti-pattern)"
    - "A negative arm proves registry-first != score-first for the fixture, so the positive assertions are load-bearing, not tautological"
  artifacts:
    - path: "tests/test-222-reach-wired.cjs"
      provides: "Born-wired reachability proof: real decide() + real sensors.register, negative arm, regression arms"
      min_lines: 100
  key_links:
    - from: "lib/core/navigation-engine.cjs"
      to: "lib/workflow/reach-hedge-ranker.cjs"
      via: "lazy require inside decide() (the :510 f-selector-ranker idiom) re-ordering sensorReaches after the dispatch block, before any [0] read"
      pattern: "rankFiredCandidates"
    - from: "lib/mcp/tools/sensors.cjs"
      to: "lib/workflow/reach-hedge-ranker.cjs"
      via: "dispatchCandidateReaches ranks the fired array before suggest_next/reach_candidates consume it"
      pattern: "rankFiredCandidates"
    - from: "lib/mcp/tools/sensors.cjs"
      to: "lib/core/navigation.cjs"
      via: "openRoomDbForCaller(roomDir) so the MCP path gets the Hedge differentiator (RESEARCH A4)"
      pattern: "openRoomDbForCaller"
---

<objective>
Wire both consumers of the unscored path onto Plan 02's shared ranker (D-01): one insertion
in `decide()` covers `resolveFireSkill` and every other `sensorReaches[0]` read on the engine
arm (Req 2), and one insertion in `dispatchCandidateReaches` covers both `suggest_next` and
`reach_candidates` on the MCP arm (Req 1). Then prove reachability the way this codebase
mandates after the Phase 150.5 dead-sensor incident (D-04): `tests/test-222-reach-wired.cjs`
drives the REAL `decide()` and the REAL MCP tool registration with a negative arm - never a
bypassed internal call (Req 6). `resolveFireSkill`'s body, signature, Wicked-escalation
precedence, and dead-Brain degrade are untouched; `insight-sensors.cjs` detection is
untouched (SPEC boundary).

Purpose: this is the wiring-fix half of the phase goal - three consumers, one scored pick.
Output: two surgical call-site edits + the born-wired proof suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-SPEC.md
@.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-CONTEXT.md
@.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-RESEARCH.md
@.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-PATTERNS.md
@.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-02-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

- Engine wiring: `decide()` in `lib/core/navigation-engine.cjs` re-orders `sensorReaches` via `rankFiredCandidates` after the dispatch block (no new exported symbol; one lazy require + one call)
- MCP wiring: `dispatchCandidateReaches` in `lib/mcp/tools/sensors.cjs` returns the ranked array (no new exported symbol; one top require + one call + the db open/close)
- `tests/test-222-reach-wired.cjs` (Req 2 + Req 6 born-wired proof)

<tasks>

<task type="auto">
  <name>Task 1: Engine arm - decide() re-orders sensorReaches before any [0] read (D-01, Req 2)</name>
  <files>lib/core/navigation-engine.cjs</files>
  <read_first>
    - lib/core/navigation-engine.cjs (the file being edited: the dispatch block inside decide() at ~:918-928 where sensorReaches is assigned inside the dispatchStartMs timing scope; the lazy-require idiom at :510 - require('../workflow/f-selector-ranker.cjs') INSIDE the function, the shape to copy; the legD cortexNodes read at :848-860 - ctx.roomContext.cortexNodes with Array.isArray guard; the ctx.roomDb caller-threaded handle convention documented at :878; resolveFireSkill at :588-652 - READ ONLY, do not edit; the other sensorReaches[0] reads the reorder transparently covers: trace :357, :962-963, rationale :1222)
    - lib/workflow/reach-hedge-ranker.cjs (rankFiredCandidates contract: same objects resorted, 0/1 passthrough, soft-fail returns input)
  </read_first>
  <action>
    In lib/core/navigation-engine.cjs, immediately AFTER the dispatch timing block in decide()
    (the block that sets sensorReaches and dispatchSensorsMs, ~:918-928) and BEFORE any consumer
    reads sensorReaches, insert the Phase 222 re-order:

    Lazy-require the ranker INSIDE decide() (mirror the :510 f-selector-ranker idiom exactly -
    engine-to-workflow requires in this file are function-local, avoiding module-load cycles):
    const reachHedgeRanker = require('../workflow/reach-hedge-ranker.cjs'); then
    sensorReaches = reachHedgeRanker.rankFiredCandidates(sensorReaches, {
      cortexNodes: (ctx.roomContext && Array.isArray(ctx.roomContext.cortexNodes)) ? ctx.roomContext.cortexNodes : [],
      db: ctx.roomDb || null,
    });
    Wrap the require + call in try/catch that leaves sensorReaches untouched on any error (the
    same soft-fail discipline as the dispatch block itself; the ranker also self-guards, this is
    belt-and-suspenders on the hot path). Comment the insertion with: "Phase 222 (D-01): re-order
    the fired subset by the shared scored selection BEFORE any [0] read - resolveFireSkill, the
    trace, and the rationale clause all read [0] and now transparently see the scored winner."

    Do NOT touch resolveFireSkill's signature or body (:588-652). Do NOT touch the Wicked-
    escalation branch (:591-597) or the dead-Brain/empty-array degrade (:582-585 region). Do NOT
    touch insight-sensors.cjs or SENSOR_REGISTRY order (SPEC boundary; Phase 213-03 doctrine:
    selection fixes live in scoring, never in reordering the frozen canonical list). The engine
    never OPENS room.db - it passes the caller-threaded ctx.roomDb handle through, which may be
    null (ranker degrades to equal weights, no event).
  </action>
  <verify>
    <automated>node -e "const nav=require('./lib/core/navigation-engine.cjs');const d=nav.decide({},{});if(!d||!d.decision_trace)throw new Error('decide() broke on empty input');console.log('OK: decide() soft-path intact')" && node tests/test-213-reach-wired.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: decide({}, {}) still returns a decision with decision_trace (empty-input soft path intact)
    - Behavior: node tests/test-213-reach-wired.cjs still exits 0 (the SENS-13 born-wired suite - the strongest existing live-decide() regression)
    - Source: grep -c "rankFiredCandidates" lib/core/navigation-engine.cjs == 1 (exactly one insertion point)
    - Source: git diff shows ZERO changes inside the resolveFireSkill function body (:588-652 region) - the reorder supersedes nothing there
    - Behavior: node tests/test-222-frozen-scalars.cjs exits 0
  </acceptance_criteria>
  <done>Every [0] read of sensorReaches on the engine arm (resolveFireSkill, trace, rationale) sees the scored winner via one insertion; empty/single-candidate turns are byte-identical to before.</done>
</task>

<task type="auto">
  <name>Task 2: MCP arm - dispatchCandidateReaches ranks the fired array for both tools (D-01, Req 1)</name>
  <files>lib/mcp/tools/sensors.cjs</files>
  <read_first>
    - lib/mcp/tools/sensors.cjs (the file being edited: dispatchCandidateReaches at :97-105; buildSensorInputs at :81-90 - ctx is { roomDir, lowFillSections: null } only, NO cortexNodes/roomState on this path, deliberately, per the SPEC boundary; the neighboring tool's navigation.openRoomDbForCaller(roomDir) usage at ~:178-195 including its null-db guard and its db lifecycle/close discipline - mirror it exactly; suggest_next :133-154 and reach_candidates :157-172 - READ ONLY, they consume dispatchCandidateReaches' return and need zero edits)
    - lib/workflow/reach-hedge-ranker.cjs (rankFiredCandidates tolerates absent cortexNodes: flat 0.5 floor, Hedge as sole differentiator - Pitfall 2)
  </read_first>
  <action>
    In lib/mcp/tools/sensors.cjs: add the top-level require beside the existing workflow require
    (:36 command-resolver idiom): const reachHedgeRanker =
    require('../../workflow/reach-hedge-ranker.cjs');

    Rewire dispatchCandidateReaches (:97-105) per the PATTERNS.md target: keep buildSensorInputs
    and dispatchSensors exactly as-is; capture fired = Array.isArray(reaches) ? reaches : [];
    when fired.length <= 1 return fired directly (skip the db open entirely - the 0/1 path stays
    byte-identical AND cost-identical); otherwise open db = navigation.openRoomDbForCaller(roomDir)
    (nullable - RESEARCH A4's sanctioned seam, the same call the neighboring sensors tool makes),
    return reachHedgeRanker.rankFiredCandidates(fired, { roomDir, db }), and release the handle
    following EXACTLY the db lifecycle the neighboring tool at :178-195 uses (close in finally if
    it closes; leave open if the chokepoint caches - mirror, do not invent). Keep the existing
    outer try/catch returning [] on error. Comment: "Phase 222 (D-01/Req 1): no cortexNodes are
    threaded on this path (deferred, SPEC boundary) - every candidate sits on the flat 0.5 D4
    floor and the Hedge outcome layer is the ONLY differentiator here (Req 3's MCP-path role)."

    suggest_next and reach_candidates get ZERO edits - the single-insertion/many-[0]-readers
    pattern, same as the engine arm. Do not touch the zod schemas, connector blocks, or
    hitl_shape declarations (:313-325 - re-ordering output does not change fork status, per
    RESEARCH's Part 11 analysis).
  </action>
  <verify>
    <automated>node tests/test-198-contract-schema.test.cjs && node -e "require('./lib/mcp/tools/sensors.cjs');console.log('OK: sensors.cjs loads clean')"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: node tests/test-198-contract-schema.test.cjs exits 0 (MCP contract schemas unregressed)
    - Source: grep -c "rankFiredCandidates" lib/mcp/tools/sensors.cjs == 1 (one insertion; suggest_next/reach_candidates untouched)
    - Source: git diff on the suggest_next and reach_candidates registration blocks (:133-172) is empty
    - Source: grep -c "openRoomDbForCaller" lib/mcp/tools/sensors.cjs increased by exactly 1 vs pre-edit
    - Behavior: node scripts/build-connector-registry.cjs --check exits 0 (Part 11: fork status unchanged, registry green)
  </acceptance_criteria>
  <done>Both MCP tools consume the shared scored selection through one rewired function; 0/1-candidate calls are byte- and cost-identical to today; the db seam gives the flat-floor path its Hedge differentiator.</done>
</task>

<task type="auto">
  <name>Task 3: test-222-reach-wired.cjs - the born-wired proof with a negative arm (D-04, Req 2 + Req 6)</name>
  <files>tests/test-222-reach-wired.cjs</files>
  <read_first>
    - tests/test-213-reach-wired.cjs (read in FULL - THE pattern: header naming the disease it prevents, makeRoom mkdtempSync fixture, check(name,fn) runner, real nav.decide() calls, assertions against shipped maps like nav.reachIdToSkillFamily never hand-typed verbs, the ARM 4a/4b STALE/UNGUARDED negative-arm pattern at :198-215, and HOW it makes >=2 sensors fire deterministically from ctx/turn fixtures)
    - tests/test-198-contract-schema.test.cjs (the makeFakeServer() seam: capture handlers from the REAL sensors.register(fakeServer, ctx), then invoke them)
    - lib/core/navigation-engine.cjs (reachIdToSkillFamily at :423; the Wicked-escalation inputs :591-597 and dead-Brain degrade :582-585 the regression arms must exercise)
    - lib/mcp/tools/sensors.cjs (register signature + how suggest_next/reach_candidates parse params and shape responses)
    - lib/workflow/reach-hedge-ranker.cjs (REACH_IDS order + weight semantics, to build a fixture where score-first != registry-first)
  </read_first>
  <action>
    Create tests/test-222-reach-wired.cjs, header stating: "Prevents the Phase 150.5 disease:
    registered-but-unreachable. NO mock of the engine, NO parallel ranker, NO re-implementation
    of the canonical order." Six arms:

    ARM 1 (fixture): makeRoom() builds a temp room via the real openRoomDb, seeds weight state
    via navigation.upsertHedgeWeightState with weights heavily favoring d4_blend, and constructs
    a turn/ctx fixture where >=2 sensors fire (copy test-213's firing recipe) AND the scored
    winner differs from the registry-first candidate (achieve the score split by threading
    ctx.roomContext.cortexNodes that give the LATER-registry reach a high D4 prior via
    buildReachScoresFromCortex, and/or seeding reject history against the registry-first reach
    so the composed 158 discount sinks it on the flat floor).

    ARM 2 (NEGATIVE, load-bearing proof): assert the fixture's registry-order-first fired
    candidate reach_id !== the expected score-first reach_id. If this arm fails the whole suite
    is tautological - it exists so the positive arms cannot pass vacuously (the 213 ARM 4
    pattern).

    ARM 3 (engine reachability, Req 2): call the REAL nav.decide(turn, context) with the fixture
    (ctx.roomDb threaded). Assert the decision's fire_skill verb strictly equals
    nav.reachIdToSkillFamily(<expected score-first reach_id>) - the SHIPPED map, never a
    hand-typed verb literal - and NOT reachIdToSkillFamily(<registry-first reach_id>).

    ARM 4 (MCP reachability, Req 6): makeFakeServer() captures handlers from the REAL
    sensors.register(fakeServer, ctx); invoke the captured suggest_next handler and assert the
    returned top reach_id is the score-first candidate; invoke reach_candidates and assert the
    full returned order matches the combined-score order (registry order would differ - covered
    by ARM 2).

    ARM 5 (Wicked precedence regression): a fixture where the Wicked-escalation condition holds
    alongside >=2 fired sensor reaches - assert the escalation verb still outranks ANY sensor
    reach (resolveFireSkill :591-597 untouched).

    ARM 6 (dead-Brain/empty degrade regression): the zero-fired-sensor fixture returns today's
    exact degrade output (:582-585 path), proving the insertion is invisible when nothing fires.

    node:assert/strict + check-runner only, no framework. Clean up temp rooms in finally.
  </action>
  <verify>
    <automated>node tests/test-222-reach-wired.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: node tests/test-222-reach-wired.cjs exits 0, all six arms green
    - Source: grep -c "require(" tests/test-222-reach-wired.cjs shows requires of BOTH lib/core/navigation-engine.cjs and lib/mcp/tools/sensors.cjs (the anti-vacuous-green signature; Pitfall 3's warning sign inverted)
    - Source: grep -vE '^[[:space:]]*(//|\*|/\*)' tests/test-222-reach-wired.cjs | grep -cE "reachIdToSkillFamily" >= 2 (shipped-map assertions, no hand-typed verb literals)
    - Behavior: the negative arm (ARM 2) fails the suite if the fixture degenerates to registry-first == score-first
    - Behavior: node tests/test-213-reach-wired.cjs and node tests/test-222-frozen-scalars.cjs still exit 0
  </acceptance_criteria>
  <done>Both live surfaces (real decide(), real MCP registration) provably return the shared scored pick for a fixture where it differs from registry order; Wicked precedence and dead-Brain degrade are regression-pinned; the proof is load-bearing via the negative arm.</done>
</task>

</tasks>

<threat_model>
This phase is room-local only (Canon Part 8, no egress): both wired arms read the caller's
own room.db handle at most, thread no data to the Brain, and add no network surface. The
MCP tools' zod input validation (user_text max 4000, section regex) is untouched (ASVS V5).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> suggest_next/reach_candidates | untrusted tool params cross here; existing zod schemas remain the gate (zero schema edits this plan) |
| ctx.roomDb / openRoomDbForCaller -> ranker | the db handle carries user-writable weight/outcome rows; validation lives in Plan 02's readHedgeWeights |
| ctx.roomContext.cortexNodes -> D4 scores | caller-threaded projected cortex; buildReachScoresFromCortex reads presence/enum signals only (shipped, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-222-01 | Tampering | poisoned weight rows steering auto-fire (resolveFireSkill now consumes the ranked order) | mitigate | Plan 02's scalar validation + Req 7 disclosure apply unchanged on this path; ARM 3/4 prove the ranked path, ARM 5/6 prove the precedence/degrade guards still cap what a skewed rank can cause |
| T-222-02 | Elevation of privilege | a ranking bug promoting a reach past the Wicked-escalation precedence | mitigate | The insertion is BEFORE resolveFireSkill, which keeps its own precedence logic byte-untouched; ARM 5 regression-pins it |
| T-222-03 | Denial of service | per-call db open on the MCP path | mitigate | 0/1-candidate calls skip the open entirely; multi-candidate calls mirror the neighboring tool's existing open/close lifecycle; ranker soft-fails to the unranked array |
| T-222-04 | Tampering (Part 9) | call sites reaching room.db directly | mitigate | Both arms touch the db ONLY via navigation.cjs (ctx.roomDb passthrough / openRoomDbForCaller); no new SQL anywhere in this plan |
| T-222-SC | Tampering (supply chain) | npm installs | accept (N/A) | Zero new packages (Req 4); this plan adds two requires of repo-internal modules only |
</threat_model>

<verification>
- node tests/test-222-reach-wired.cjs exits 0 (six arms)
- node tests/test-213-reach-wired.cjs, node tests/test-198-contract-schema.test.cjs exit 0 (adjacent-suite regression)
- node tests/test-222-frozen-scalars.cjs exits 0 (Req 5 tripwire)
- node scripts/build-connector-registry.cjs --check exits 0 (Part 11)
- git diff lib/core/insight-sensors.cjs is empty (detection boundary held)
</verification>

<success_criteria>
- One selection logic, three consumers: suggest_next, reach_candidates, and resolveFireSkill all return/act on the same scored pick (Reqs 1-2, phase goal)
- Reachability proven via real registration and real decide(), with a negative arm (Req 6, D-04)
- Wicked precedence + dead-Brain degrade byte-untouched and regression-pinned
</success_criteria>

<output>
Create `.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-03-SUMMARY.md` when done
</output>
