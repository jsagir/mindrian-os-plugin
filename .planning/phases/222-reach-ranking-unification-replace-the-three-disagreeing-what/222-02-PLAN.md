---
phase: 222-reach-ranking-unification-replace-the-three-disagreeing-what
plan: 02
type: execute
wave: 2
depends_on: [222-01]
files_modified:
  - lib/workflow/reach-hedge-ranker.cjs
  - tests/test-222-hedge-update.cjs
  - tests/test-222-rank-fired.cjs
  - tests/test-222-degrade.cjs
autonomous: true
requirements: [1, 3, 7]
must_haves:
  truths:
    - "Given >=2 fired candidates with distinct D4 scores (cortex nodes supplied), rankFiredCandidates returns them in combined-score order, not registry order"
    - "Given 0 or 1 fired candidates, rankFiredCandidates returns its input unchanged - byte-identical to today's output, no db read, no update trigger"
    - "Given no cortex nodes (the real MCP-call condition), every candidate sits on the flat 0.5 D4 floor and the outcome-learned layer (Hedge weights + composed Phase-158 discount) is the only differentiator - never a silent revert to registry order"
    - "A synthetic outcome sequence where one expert is consistently right leaves that expert's weight strictly greater after >= N folds, and the combined argmax matches its pick on a held-out fixture turn"
    - "A missing/corrupt weight-state read degrades to the D4-alone blend AND emits reach_weight_state_unavailable; a healthy or merely cold (zero-row) state emits NO event"
  artifacts:
    - path: "lib/workflow/reach-hedge-ranker.cjs"
      provides: "rankFiredCandidates + Hedge combiner + weight read/update, the single shared selection layer (D-01)"
      exports: ["rankFiredCandidates", "readHedgeWeights", "maybeUpdateHedgeWeights", "hedgeUpdate", "deriveExpertLosses", "EXPERT_IDS", "REACH_IDS", "HEDGE_UPDATE_N_DEFAULT", "HEDGE_ETA_DEFAULT"]
      min_lines: 120
    - path: "tests/test-222-hedge-update.cjs"
      provides: "Req 3 synthetic-outcome convergence proof"
      min_lines: 50
    - path: "tests/test-222-rank-fired.cjs"
      provides: "Req 1 ordering semantics: score order, 0/1 passthrough identity, flat-floor outcome tie-break"
      min_lines: 50
    - path: "tests/test-222-degrade.cjs"
      provides: "Req 7 disclosed-degrade honesty against a real room.db"
      min_lines: 50
  key_links:
    - from: "lib/workflow/reach-hedge-ranker.cjs"
      to: "lib/hmi/cortex-reach-adapter.cjs"
      via: "buildReachScoresFromCortex(roomState.cortexNodes || []) - the SAME reusable call the CLI path makes (SPEC Req 1 target)"
      pattern: "buildReachScoresFromCortex"
    - from: "lib/workflow/reach-hedge-ranker.cjs"
      to: "lib/core/navigation.cjs"
      via: "readHedgeWeightState / upsertHedgeWeightState / findRecentChanges / logMemoryEvent - chokepoint only (Part 9)"
      pattern: "require\\(['\"]\\.\\./core/navigation\\.cjs['\"]\\)"
    - from: "lib/workflow/reach-hedge-ranker.cjs"
      to: "lib/workflow/reach-reject-reader.cjs"
      via: "countPenalty discount composed INTO the D4 expert before the Hedge blend (OQ-2: one coordinated adjustment, not two)"
      pattern: "countPenalty"
---

<objective>
Build `lib/workflow/reach-hedge-ranker.cjs`, the one shared scored-selection module both
call sites adopt in Plan 03. Per D-01 it exports a pure-shaped `rankFiredCandidates(
sensorReaches, roomState)` that re-orders `dispatchSensors`' already-fired subset (same
objects, resorted, so every downstream `[0]` read transparently sees the scored winner);
per Requirement 3 it layers a hand-rolled two-expert Hedge/MWU adjustment (Arora-Hazan-Kale
2012) learned from the Phase 159 outcome log; per D-03 weight updates are debounced
(env-tunable N, default 50); per Requirement 7 a failed weight-state read degrades to
D4-alone and emits `reach_weight_state_unavailable`, while cold start stays silent.
Detection logic in insight-sensors.cjs is untouched (SPEC boundary). No second selection
brain (Connector Spine): this module re-orders the ONE dispatch output.

Purpose: everything behavioral in this phase lives here; Plan 03 is then a two-line wiring.
Output: the module + three unit/integration test files (Reqs 1, 3, 7 at module level).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-SPEC.md
@.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-CONTEXT.md
@.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-RESEARCH.md
@.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-PATTERNS.md
@.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-01-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

- `lib/workflow/reach-hedge-ranker.cjs` exporting: `rankFiredCandidates(sensorReaches, roomState)`, `readHedgeWeights(db, roomState)`, `maybeUpdateHedgeWeights(db, opts)`, `hedgeUpdate(weights, losses, eta)`, `deriveExpertLosses(row, opts)`, `EXPERT_IDS` (`['d4_blend','registry_order']`), `REACH_IDS` (local frozen copy of the canonical 6), `HEDGE_UPDATE_N_DEFAULT` (50), `HEDGE_ETA_DEFAULT` (0.3)
- Env vars consumed (documented in Plan 04): `MINDRIAN_HEDGE_UPDATE_N`, `MINDRIAN_HEDGE_ETA`
- `tests/test-222-hedge-update.cjs`, `tests/test-222-rank-fired.cjs`, `tests/test-222-degrade.cjs`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reach-hedge-ranker.cjs - the shared selection + Hedge layer (D-01, D-03, Req 3)</name>
  <files>lib/workflow/reach-hedge-ranker.cjs, tests/test-222-hedge-update.cjs</files>
  <read_first>
    - lib/workflow/reach-reject-reader.cjs (THE module analog, read in full: BSL header + invariants doc-comment shape, single navigation require, local frozen REACH_IDS const at lines 41-48, ALL-CAPS tunables with low-data rationale comments at 93-98, the _injected injection seam at 120-132, non-throwing chokepoint reads throughout, flat module.exports of functions + named constants; also the countPenalty export this task composes)
    - lib/hmi/cortex-reach-adapter.cjs (buildReachScoresFromCortex at :194 - signature, empty-array degrade to {}, exported at :262)
    - lib/hmi/dial-reach-orchestrator.cjs (_resolveReachScore at :199-207 - the reachScores read pattern to MIRROR locally: supplied finite number else 0.5 floor; do NOT require this module, copy the read shape only)
    - lib/core/navigation/memory-events.cjs (findRecentChanges signature + opts: eventType filter; logEvent payload rules)
    - lib/workflow/offer-closer.cjs (closeOffer's decisionArgs: decision accept/defer/reject, reach_id optional, score_at_decision - the exact row shape the loss fold reads)
    - .planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-RESEARCH.md (Pattern 2 d4For, Pattern 3 hedgeUpdate, the readHedgeWeights worked example, Pitfalls 1/2/5)
  </read_first>
  <action>
    Create lib/workflow/reach-hedge-ranker.cjs mirroring reach-reject-reader.cjs's module
    disciplines (BSL 1.1 header, phase-222 doc comment naming the three invariants: chokepoint-only
    db access, enum/scalar-only reads/writes, injection seam for db-free tests). Sole imports:
    ../core/navigation.cjs, ../hmi/cortex-reach-adapter.cjs, ./reach-reject-reader.cjs. Never
    require node:sqlite, better-sqlite3, fs, dial-reach-orchestrator, or insight-sensors.

    Constants block (each with the TUNABLE-LATER rationale comment discipline from
    reach-reject-reader lines 93-98): EXPERT_IDS = Object.freeze(['d4_blend','registry_order']);
    REACH_IDS = a local frozen copy of the canonical 6 (context_block, contradiction, cross_room,
    brain_consult, deep_research, hats - mirror reach-reject-reader:41-48, never imported from the
    orchestrator); HEDGE_UPDATE_N_DEFAULT = 50 (D-03, SEED-009's own precedent number, env override
    MINDRIAN_HEDGE_UPDATE_N read defensively with numeric fallback); HEDGE_ETA_DEFAULT = 0.3 (env
    override MINDRIAN_HEDGE_ETA; rationale comment: bounds a single fold's multiplicative swing to
    exp(0.3) ~ 1.35, conservative for a 2-expert Hedge per Arora-Hazan-Kale 2012, horizon-optimal
    eta is T-dependent and deliberately NOT computed at this data scale).

    Functions:

    1. hedgeUpdate(weights, losses, eta) - GENERAL over expert-id-keyed objects, never hardcoded
       two fields (SEED-057 generality note in CONTEXT.md): for each key, w = w * Math.exp(-eta *
       loss); renormalize to sum 1; if the sum is 0 or non-finite return equal weights over the
       keys. Pure, ~15 lines.

    2. deriveExpertLosses(row, opts) - maps one f_selector_decision row to { d4_blend: loss,
       registry_order: loss } per SPEC Req 3's convention (accept = low loss, reject = high loss
       for the endorsing expert), made computable from stored row data: endorsement_registry =
       1 / (REACH_IDS.indexOf(row.reach_id) + 1) (the registry expert endorses front-of-registry
       reaches most; A2's 1/(rank+1) signal); endorsement_d4 = d4For(row.reach_id,
       opts.reachScores) when opts.reachScores is supplied, else the 0.5 neutral (historical
       reachScores are not persisted - documented limitation, TUNABLE-LATER). loss = (decision ===
       'reject') ? endorsement : (1 - endorsement). Rows with decision 'defer', a missing/off-set
       reach_id, or no decision are SKIPPED by the caller (return null). The endorsing-expert
       reading: an expert that fully endorsed a rejected pick takes loss 1; one that fully
       endorsed an accepted pick takes loss 0 - exactly SPEC's sentence, generalized to fractional
       endorsement.

    3. readHedgeWeights(db, roomState) - follow PATTERNS.md's "Non-throwing degrade, never silent"
       worked example EXACTLY: (a) injection seam first - roomState.hedgeWeights object wins
       (db-free unit tests, the _injected discipline); (b) no db -> equal weights over EXPERT_IDS,
       NO event (the MCP/no-room cold path); (c) navigation.readHedgeWeightState(db) inside try -
       a THROW is a real fault: emit the degrade event and return equal weights; (d) null result =
       cold start: equal weights, NO event (Pitfall 5 - the signal must stay honest); (e) validate
       every stored weight is finite and non-negative with a positive sum - corrupt scalars emit
       the degrade event and return equal weights; (f) healthy values renormalize to sum 1.
       emitUnavailable(db) wraps navigation.logMemoryEvent(db, 'reach_weight_state_unavailable',
       { fault_kind: 'read_fault' | 'corrupt_scalar', source: 'reach-hedge-ranker' }) in its own
       try/catch - the degrade path itself never throws. Payload is enum tokens only, never prose
       and never a field named reason (Part 8 + the run-all-158 reason-sweep convention).

    4. maybeUpdateHedgeWeights(db, opts) - the D-03 debounced fold. No db -> return
       { updated: false }. Read current state via readHedgeWeights semantics (reuse the accessor
       read + its updatedAt/updateCount via navigation.readHedgeWeightState, tolerating null);
       since = state ? state.updatedAt : 0; rows = navigation.findRecentChanges(db, since,
       { eventType: 'f_selector_decision' }); keep rows where deriveExpertLosses returns non-null;
       if kept.length < N return { updated: false }; else fold chronologically (oldest first)
       through hedgeUpdate with eta, then navigation.upsertHedgeWeightState(db, foldedWeights,
       { updateCount: (state ? state.updateCount : 0) + 1, updatedAt: newest row timestamp }).
       Entire body inside try/catch returning { updated: false } on any error - the update trigger
       may never break a turn.

    5. rankFiredCandidates(sensorReaches, roomState) - the D-01 shared selection. Contract:
       (a) if sensorReaches is not an array or length <= 1, return the INPUT untouched - same
       reference, zero reads, zero side effects (SPEC Req 1 acceptance: 0/1 candidates are
       byte-identical to today); (b) rs = roomState || {}; reachScores = rs.reachScores when it is
       an object, else buildReachScoresFromCortex(rs.cortexNodes || []) - the SAME reusable call
       the CLI path makes at intent-classifier.cjs:1036 (SPEC Req 1 target; the adapter already
       returns {} on empty input, reuse not rebuild); (c) local d4For(reachId): supplied finite
       reachScores value clamped to 0..1, else 0.5 (mirror _resolveReachScore, Pattern 2);
       (d) compose Phase 158: cp = reachRejectReader.countPenalty(db, reach_id, rs) guarded
       try/catch to 0 when db absent or the call throws; d4Adjusted = d4For(reach_id) * (1 - cp)
       (OQ-2 resolution: COMPOSE the shipped discount into the D4 expert BEFORE the blend - one
       coordinated adjustment, not two; isHardSuppressed stays a dial-path concern, deliberately
       NOT applied here); (e) registrySignal_i = 1 / (originalIndex + 1) over the fired array's
       own order (A2); (f) weights = readHedgeWeights(db, rs) where db = rs.db || null;
       (g) combined_i = weights.d4_blend * d4Adjusted_i + weights.registry_order *
       registrySignal_i; (h) STABLE sort descending by combined - ties preserve original fired
       order (determinism); return the SAME objects re-sorted, shape untouched (the in-place
       re-order pattern: every downstream [0] read sees the winner); (i) fire-and-forget
       maybeUpdateHedgeWeights(db) after ranking, only when db is present, inside try/catch;
       (j) the WHOLE function body is wrapped so any unexpected throw returns the ORIGINAL input
       array unchanged (hot-path soft-fail, the engine's own dispatch idiom). roomState shape
       accepted: { cortexNodes?, reachScores?, hedgeWeights?, db?, roomDir? }. Hot-path budget
       honored: at most one weight-state SELECT + one bounded findRecentChanges per multi-candidate
       turn, no file reads, no network, no embedding calls (SPEC Constraints).

    module.exports (flat, functions + named constants, per the reach-reject-reader export shape):
    rankFiredCandidates, readHedgeWeights, maybeUpdateHedgeWeights, hedgeUpdate,
    deriveExpertLosses, EXPERT_IDS, REACH_IDS, HEDGE_UPDATE_N_DEFAULT, HEDGE_ETA_DEFAULT.

    No em-dashes anywhere. CJS only.
  </action>
  <behavior>
    - Test 1 (pure convergence, SPEC Req 3 acceptance): fold 50 synthetic loss vectors where
      d4_blend loss = 0.1 and registry_order loss = 0.9 through hedgeUpdate at default eta;
      final weights.d4_blend is STRICTLY greater than weights.registry_order; weights sum to ~1.
    - Test 2 (held-out argmax): with the converged weights, a fixture turn where the d4 expert
      prefers candidate B (d4 0.9 vs 0.4) and registry order prefers A (index 0) yields combined
      argmax B.
    - Test 3 (row-driven fold via maybeUpdateHedgeWeights, real temp room.db): seed >= 50
      f_selector_decision rows (via navigation.logMemoryEvent) whose reach_id is always
      REACH_IDS[0] with decision 'reject' (the registry expert consistently wrong); after
      maybeUpdateHedgeWeights, readHedgeWeightState shows weights.registry_order strictly less
      than weights.d4_blend and updateCount 1.
    - Test 4 (debounce, D-03): with only N-1 qualifying rows, maybeUpdateHedgeWeights returns
      { updated: false } and writes nothing.
    - Test 5 (loss skip rules): deriveExpertLosses returns null for decision 'defer', for a
      missing reach_id, and for an off-set reach_id.
    - Test 6 (hedgeUpdate guards): zero/non-finite weight sum returns equal weights over the keys.
  </behavior>
  <verify>
    <automated>node tests/test-222-hedge-update.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: node tests/test-222-hedge-update.cjs exits 0, all 6 behaviors green (Test 3 drives the REAL openRoomDb + REAL navigation accessors on a temp room - the no-mock leg of this wave)
    - Source (Part 9): grep -vE '^[[:space:]]*(//|\*|/\*)' lib/workflow/reach-hedge-ranker.cjs | grep -cE "require\(['\"](node:sqlite|better-sqlite3)|\bDatabaseSync\b|\bfs\.(read|write)" == 0
    - Source (Part 9): grep -vE '^[[:space:]]*(//|\*|/\*)' lib/workflow/reach-hedge-ranker.cjs | grep -cE "require\(['\"]\.\./core/navigation\.cjs['\"]\)" == 1
    - Source (decoupling): grep -vE '^[[:space:]]*(//|\*|/\*)' lib/workflow/reach-hedge-ranker.cjs | grep -cE "dial-reach-orchestrator|insight-sensors" == 0
    - Source (D-03): grep -c "MINDRIAN_HEDGE_UPDATE_N" lib/workflow/reach-hedge-ranker.cjs >= 1 and grep -c "MINDRIAN_HEDGE_ETA" lib/workflow/reach-hedge-ranker.cjs >= 1
    - Behavior: node tests/test-222-frozen-scalars.cjs still exits 0 (the Wave 1 tripwire stays green)
  </acceptance_criteria>
  <done>The module exports all nine named symbols, the synthetic-outcome convergence proof passes end to end (pure fold AND row-driven fold against a real room.db), and the debounce boundary is proven at N-1 vs N.</done>
</task>

<task type="auto">
  <name>Task 2: Ordering semantics (Req 1) + disclosed-degrade honesty (Req 7) tests</name>
  <files>tests/test-222-rank-fired.cjs, tests/test-222-degrade.cjs</files>
  <read_first>
    - lib/workflow/reach-hedge-ranker.cjs (the module Task 1 just built - test against its real exports)
    - tests/test-213-reach-wired.cjs (makeRoom mkdtempSync fixture helper + check(name, fn) runner shape)
    - lib/core/room-db.cjs (openRoomDb - the degrade test opens a real temp room, then corrupts the table)
    - .planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-SPEC.md (Req 1 and Req 7 acceptance wording - the fixtures below implement it literally)
  </read_first>
  <action>
    Create tests/test-222-rank-fired.cjs (node:assert/strict, check-runner shape). Fixture
    candidates are plain reach objects { reach_id, posture } matching dispatchSensors' output
    shape. Checks: (1) SCORE ORDER - two fired candidates [deep_research, context_block] with
    injected roomState.reachScores { context_block: 0.9, deep_research: 0.3 } and
    roomState.hedgeWeights { d4_blend: 1, registry_order: 0 } return context_block first (score
    order, not input order); (2) ZERO/ONE PASSTHROUGH - [] and [one] return the SAME array
    reference, deep-equal to input (assert.strictEqual on the reference - byte-identity per
    SPEC Req 1 acceptance); (3) FLAT-FLOOR OUTCOME TIE-BREAK - no cortexNodes and no reachScores
    (the real MCP condition, every d4 = 0.5): with injected hedgeWeights { d4_blend: 0.9,
    registry_order: 0.1 } and an injected Phase-158 discount seam (roomState carrying the
    reject-history injection reach-reject-reader honors, or a directly-injected penalties map if
    the module exposes the d4Adjusted seam) making the registry-FIRST candidate carry a heavy
    reject discount, the SECOND candidate wins - proving the outcome layer breaks the flat-floor
    tie, no silent registry-order revert; (4) STABILITY - identical combined scores preserve
    input order; (5) SHAPE - returned objects are the same references with keys untouched;
    (6) SOFT-FAIL - a roomState whose cortexNodes getter throws still returns the input array
    unchanged (the hot-path guard).

    Create tests/test-222-degrade.cjs (Req 7 acceptance, implemented literally against a REAL
    temp room.db - no mocks): (a) HEALTHY - open a temp room, upsert valid weights via
    navigation.upsertHedgeWeightState, run rankFiredCandidates with 2 fired candidates and
    roomState.db set; assert a valid ranked result AND findRecentChanges(db, 0, { eventType:
    'reach_weight_state_unavailable' }) returns 0 rows (the signal is honest, not unconditional);
    (b) COLD START - a fresh room (table exists, zero rows): valid ranked result, equal-weight
    blend, and STILL 0 degrade events (Pitfall 5); (c) CORRUPT SCALAR - db.prepare UPDATE
    ranker_weights SET weight = -1 (test-side direct SQL is the sanctioned fixture-corruption
    seam, mirroring how phase tests seed rooms): rankFiredCandidates still returns a valid
    D4-only ranked result (no throw, no hang) AND exactly 1 reach_weight_state_unavailable event
    with fault_kind 'corrupt_scalar' is readable back; (d) MISSING TABLE - db.exec('DROP TABLE
    ranker_weights'): valid ranked result, fault_kind 'read_fault' event emitted; (e) the emitted
    event payload contains ONLY enum/scalar values (assert no property value longer than 64 chars
    and no property named reason - Part 8 discipline). Clean up temp dirs in finally.
  </action>
  <verify>
    <automated>node tests/test-222-rank-fired.cjs && node tests/test-222-degrade.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: node tests/test-222-rank-fired.cjs exits 0 (6 checks incl. the strictEqual same-reference passthrough)
    - Behavior: node tests/test-222-degrade.cjs exits 0 (5 arms; corrupt AND missing-table both return valid rankings AND emit exactly one event each; healthy AND cold emit zero)
    - Behavior: SPEC Req 7 acceptance sentence holds verbatim: "a fixture that deletes/corrupts the weight-state table before a call still returns a valid ranked result... a fixture with a healthy table asserts NO such event fires"
    - Behavior: node tests/test-222-weight-state.cjs and node tests/test-222-frozen-scalars.cjs still exit 0 (no regression)
  </acceptance_criteria>
  <done>Req 1's ordering semantics and Req 7's degrade honesty are pinned at module level against real room.db fixtures, ready for Plan 03 to wire the live call sites against the same behaviors.</done>
</task>

</tasks>

<threat_model>
This phase is room-local only (Canon Part 8, no egress): the module makes zero network
calls, reads only the passed room's db handle, and weight state never crosses rooms or
enters a Brain packet. Plan 04's grep sweeps gate this permanently.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ranker_weights rows -> readHedgeWeights | stored scalars are untrusted until validated (finite, non-negative, positive sum) |
| f_selector_decision rows -> loss fold | outcome rows are user-generated history; only enum/scalar fields (decision, reach_id, timestamps) are read |
| roomState injection seam -> ranking | test/caller-injected hedgeWeights bypass the db read by design; production callers (Plan 03) never populate it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-222-01 | Tampering | corrupt/poisoned ranker_weights scalars skew ranking | mitigate | readHedgeWeights validates finite non-negative positive-sum; corrupt -> equal weights + reach_weight_state_unavailable (Req 7, tested in test-222-degrade arms c/d) |
| T-222-02 | Information disclosure (Part 8) | degrade-event payload | mitigate | Payload = { fault_kind: enum, source: enum }, no prose, no field named reason; test-222-degrade arm (e) asserts it; Plan 04 grep sweep gates it |
| T-222-03 | Tampering (Part 9 breach) | module bypassing the navigation chokepoint | mitigate | Sole db surface is navigation.cjs exports; acceptance grep proves zero node:sqlite/DatabaseSync/fs requires (comment-stripped, the grep-hygiene idiom) |
| T-222-04 | Denial of service | hot-path stall (per-turn reads) or a throwing ranker killing decide() | mitigate | 0/1-candidate short-circuit does zero reads; multi-candidate turn costs one SELECT + one bounded findRecentChanges; whole body soft-fails to the input array (tested: rank-fired check 6) |
| T-222-05 | Information disclosure (Part 8) | cross-room weight aggregation | mitigate | Module only ever touches the caller-passed db handle; no room enumeration surface exists; room-local by construction |
| T-222-SC | Tampering (supply chain) | npm installs | accept (N/A) | Zero new packages (Req 4); the module's require list is pure node-free repo-internal, gated by Plan 04's tripwire |
</threat_model>

<verification>
- node tests/test-222-hedge-update.cjs exits 0
- node tests/test-222-rank-fired.cjs exits 0
- node tests/test-222-degrade.cjs exits 0
- node tests/test-222-weight-state.cjs and node tests/test-222-frozen-scalars.cjs still exit 0
- git diff package.json package-lock.json empty; git diff on lib/hmi/dial-reach-orchestrator.cjs, lib/workflow/f-selector-ranker.cjs, lib/core/insight-sensors.cjs empty (boundaries held)
</verification>

<success_criteria>
- One shared selection module exists with all nine exports; detection untouched; no second selection brain (D-01)
- Hedge convergence proven on synthetic outcomes, pure AND row-driven, debounce boundary at N (Req 3, D-03)
- Ordering semantics pinned: score order with real D4, byte-identical 0/1 passthrough, outcome-broken flat-floor ties (Req 1 module level)
- Degrade honesty pinned: fault/corrupt -> D4-alone + event; healthy/cold -> silence (Req 7)
</success_criteria>

<output>
Create `.planning/phases/222-reach-ranking-unification-replace-the-three-disagreeing-what/222-02-SUMMARY.md` when done
</output>
