---
phase: 357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re
plan: 10
type: execute
wave: 7
depends_on: ["357-09"]
files_modified:
  - agents/larry-extended.md
  - skills/larry-personality/SKILL.md
  - data/harness-manifest.json
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - /home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-larry-extended-x-jev-gate-triad-replay.md
  - /home/jsagi/MindrianOS/research/2026-09-23-larry-extended-x-jev-gate-triad-replay.md
autonomous: true
requirements: [GATE357-07, GATE357-01, GATE357-02, GATE357-03, GATE357-04, GATE357-05, GATE357-06, GATE357-08, GATE357-09]
tags: [larry, prose-shrink, metric-gated, close-out, dual-filing]

must_haves:
  truths:
    - "The prose shrink runs only when the replay re-run on HEAD proves R4 (exit 0: 0 false blocks, 0 new misses); otherwise no Larry file is touched and the skip reason is recorded, which is a legal outcome (SPEC R6)"
    - "The shrink set is the two real card spans only: agents/larry-extended.md '## Decision Gates -- fire the card, never draw the box (SEED-021)' (1751 B) and skills/larry-personality/SKILL.md item 5 'Never hand-draw the dial glyphs' (479 B), 2230 B combined, target at most 1115 B; SKILL :244 (the Voice Signature residual) is NOT touched (R-B)"
    - "The shrunk text keeps: '## Decision Gates', 'no card, no picture (SEED-021)', 'AskUserQuestion', fire the card on a genuine unanswered relevant fork, never draw the ASCII box, the FIRE-IF-FORK trailer is judgment-gated, 'type a/b/c' only on a surface that cannot fire the tool (D-16, R-B)"
    - "'## Post-Gate Handoff' is untouched and still says runChain halts at the first material step; tests/test-larry-handoff-seam.cjs (and tests/test-356-larry-contract.cjs when present) stay green (D-16, 356 D-12)"
    - "data/harness-manifest.json is regenerated after the edit and the before/after byte delta is recorded (R-B, SPEC R6)"
    - "Close-out closes each GATE357 row with measured proof or leaves it open with a stated reason, files the outcome in both research homes (D-17), and opens a follow-on phase when R-C was ruled known_false_block (R-C)"
  artifacts:
    - path: "agents/larry-extended.md"
      provides: "shrunk Decision Gates section deferring to the code verdict"
      contains: "no card, no picture (SEED-021)"
    - path: "data/harness-manifest.json"
      provides: "regenerated larry_surfaces digests"
  key_links:
    - from: "agents/larry-extended.md"
      to: "scripts/check-card-fire.cjs"
      via: "the prose defers to the Stop-hook card gate's verdict"
      pattern: "card gate"
---

<objective>
Shrink Larry's card-rule prose only if the measured bar holds, then close the phase: requirement rows, dual research filing, and the R-C follow-on.

Purpose: SPEC R6 (metric-gated shrink, >= 50% on the two card spans per R-B, skip reason if R4 failed), D-16, D-17, R-B, R-C, and the requirement close-out for GATE357-01..09.
Output: two smaller prose spans (or a recorded skip), a regenerated manifest, closed or annotated requirement rows, the dual-filed outcome.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-SPEC.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-09-SUMMARY.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-08-SUMMARY.md
@data/harness-policies/contract-parity-larry.json

Parallel-session rules (every task): explicit-path commits only (`git add <path>`, `git add -f` under `.planning/`, then `git commit --only -m "<msg>" -- <paths>`; peers keep files staged); record PLAN_BASE; `git status --short -- <file>` before editing and STOP on a peer diff; never `git add -A` / `git commit -a` / `git stash` / revert unowned diffs. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, lib/core/navigation.cjs, docs/OPEN-HANDOFFS.md, the '## Post-Gate Handoff' section of agents/larry-extended.md, SKILL.md line ~244 (Voice Signature residual), and the user-level `~/.claude/agents/larry-extended.md`. No em-dashes.

Span measurement (identical before and after): larry-extended bytes = `awk '/^## Decision Gates/{f=1} /^## The Cardinal Sin/{f=0} f' agents/larry-extended.md | wc -c` (1751 at planning); SKILL bytes = the single line starting `5. Never hand-draw the dial glyphs` measured with `grep '^5\. Never hand-draw the dial glyphs' skills/larry-personality/SKILL.md | wc -c` (479 at planning). Target: combined at most 1115.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| Corpus loader, `pre-phase.json`, fixture files, loader test, `run-all-357.sh` | 01 |
| `scripts/replay-card-fire.cjs`, `tests/test-357-replay.cjs` | 02 |
| `card_fire_replay` egress profile, `data/jev-policies/card-fire-replay.json` | 03 |
| `scripts/label-card-fire-replay.cjs`, labeler refusal test, tripwire list entry | 04 |
| Authored debug and live entries | 05 |
| Dogfood extractor, sanitized dogfood entries, `357-DOGFOOD-LABELS.md` | 06 |
| D-07 and D-08a deterministic fixes with tests | 07 |
| `357-JEV-LABEL-REPORT.md`, ratified labels (human checkpoint) | 08 |
| `baseline.json`, standing-gate wiring, mutation leg | 09 |
| Larry prose shrink, regenerated `data/harness-manifest.json`, D-17 filing, requirement closure | 10 (this plan) |

<tasks>

<task type="auto">
  <name>Task 1: R4 gate re-check, then the two-span prose shrink and manifest regeneration (or the recorded skip)</name>
  <files>agents/larry-extended.md, skills/larry-personality/SKILL.md, data/harness-manifest.json</files>
  <read_first>
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-09-SUMMARY.md (first line R4: MET or NOT MET)
    - agents/larry-extended.md (`grep -n '^## ' agents/larry-extended.md`; read '## Post-Gate Handoff' (read only) and '## Decision Gates -- fire the card, never draw the box (SEED-021)' in full; line ~154 "see Decision Gates above")
    - skills/larry-personality/SKILL.md (the '### Operating the Dial (shipped)' list, items 1-5, and the paragraph after it)
    - tests/test-gate-native-fire-w1.cjs (lines 105-125: pinned phrases), tests/test-larry-voice-mark-182.cjs (lines 280-290), tests/test-larry-handoff-seam.cjs
    - data/harness-policies/contract-parity-larry.json (phrases for agents/larry-extended.md)
    - scripts/build-harness-manifest.cjs (`--check` and regenerate usage; `node scripts/build-harness-manifest.cjs --check` output BEFORE any edit, recorded)
  </read_first>
  <action>
    1. Gate (SPEC R6): run `node scripts/replay-card-fire.cjs --surface both --baseline compare`. If it exits non-zero, or 357-09-SUMMARY says `R4: NOT MET`, do NOT edit any Larry file: record in the SUMMARY `R6: SKIPPED - R4 not met` with the residual ids and counts, and go to Task 2. This is a legal outcome.
    2. Record the before bytes of both spans (context measurement) and `node scripts/build-harness-manifest.cjs --check` output (drift attributable to other phases is recorded, not fixed here).
    3. Rewrite the larry-extended '## Decision Gates -- fire the card, never draw the box (SEED-021)' section, keeping that exact header line, as a short rule (about 4 sentences) that says: at a genuine, unanswered fork relevant to the conversation, fire the AskUserQuestion tool in that same turn with the gate's options; never render the gate as an ASCII box or ask the navigator to type 1, 2 or 3, no card, no picture (SEED-021); the `[FIRE-IF-FORK]` trailer is judgment-gated: when the navigator already answered or the gate is unrelated, proceed in prose and never reproduce the block as text; the Stop-hook card gate checks this in code and its verdict wins; the "type a/b/c" form is only for a surface that cannot fire the tool (never the CLI). Keep the literal strings `## Decision Gates`, `no card, no picture (SEED-021)` and `AskUserQuestion`. Do not touch any other section (Post-Gate Handoff stays byte-identical).
    4. Rewrite SKILL item 5 as one short line that says: never hand-draw the dial glyphs or the "Choose next reach:" block, they arrive only from the engine arm's render; when that render carries the [AskUserQuestion contract: ...] trailer on a genuine fork, fire the card in the same response; no card, no picture (SEED-021). Do not touch the Voice Signature paragraph near :244 (R-B) or any other line.
    5. Measure after bytes; combined must be at most 1115 (>= 50% of 2230). Iterate on wording only if above. Avoid any sentence that equates material with irreversible (356 D-12 scan).
    6. Regenerate `node scripts/build-harness-manifest.cjs`, then `node scripts/build-harness-manifest.cjs --check`. Run: `node tests/test-gate-native-fire-w1.cjs`, `node tests/test-larry-voice-mark-182.cjs`, `node tests/test-larry-handoff-seam.cjs`, `node tests/test-298-contract-parity.cjs` (only when `git status --short` shows no peer diff on Larry surfaces or harness policies, per RESEARCH Pitfall 9; else record it as not run with the reason), `node scripts/check-first-touch-drift.cjs`, and `node tests/test-356-larry-contract.cjs` when that file exists.
    7. Commit: `git commit --only -m "refactor(357-10): Larry card-rule prose defers to the card gate's code verdict (R6, R-B; <before>B -> <after>B)" -- agents/larry-extended.md skills/larry-personality/SKILL.md data/harness-manifest.json`.
  </action>
  <verify>
    <automated>A=$(awk '/^## Decision Gates/{f=1} /^## The Cardinal Sin/{f=0} f' agents/larry-extended.md | wc -c); B=$(grep '^5\. Never hand-draw the dial glyphs' skills/larry-personality/SKILL.md | wc -c); echo "spans=$((A+B))"; node tests/test-gate-native-fire-w1.cjs && node tests/test-larry-voice-mark-182.cjs && node tests/test-larry-handoff-seam.cjs && node scripts/build-harness-manifest.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - When R4 is MET: `spans` printed by verify is at most 1115, and all four commands exit 0
    - When R4 is NOT MET: `git diff $PLAN_BASE -- agents/larry-extended.md skills/larry-personality/SKILL.md` is empty and the SUMMARY records the skip reason
    - `awk '/^## Post-Gate Handoff/{f=1} /^## Decision Gates/{f=0} f' agents/larry-extended.md | sha256sum` equals the same command run at PLAN_BASE (`git show $PLAN_BASE:agents/larry-extended.md | awk ... | sha256sum`)
    - `grep -c "DECLARED CONVENTION" skills/larry-personality/SKILL.md` and `grep -c "not a per-token runtime guarantee" skills/larry-personality/SKILL.md` are unchanged from PLAN_BASE (SKILL :244 untouched)
    - `grep -c "no card, no picture (SEED-021)" agents/larry-extended.md` is at least 1 and `grep -c "## Decision Gates" agents/larry-extended.md` is 1
    - `grep -c "$(printf '\xe2\x80\x94')" agents/larry-extended.md skills/larry-personality/SKILL.md` reports 0 for both
  </acceptance_criteria>
  <done>Either the two card spans are at most half their former size with every pinned phrase and test green, or no prose changed and the skip reason is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Close-out (requirement rows, R-C follow-on, D-17 dual filing, SUMMARY notes)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, /home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-larry-extended-x-jev-gate-triad-replay.md, /home/jsagi/MindrianOS/research/2026-09-23-larry-extended-x-jev-gate-triad-replay.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the "Phase 357 - Gate-triad replay harness (GATE357 family)" section registered at plan time) and `git status --short -- .planning/REQUIREMENTS.md .planning/ROADMAP.md`
    - Every 357-0N-SUMMARY.md in the phase dir (measured proof per requirement)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-08-SUMMARY.md (R-C outcome)
    - Both research-trail files (tracked in the home repo `/home/jsagi`, commit ea37fa6c5)
  </read_first>
  <action>
    1. REQUIREMENTS.md: for each GATE357-01..09 row, tick `- [x]` only with measured proof named in one clause (test command plus result, or SUMMARY reference); otherwise leave `- [ ]` and append `Open: <reason>` (for example GATE357-07 when R6 was skipped, GATE357-04/05/08 when R4 was not met). Change nothing outside the Phase 357 section.
    2. R-C follow-on: if plan 08 ruled the R-C case `known_false_block`, run `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" phase add "Card gate: text-dependent relevance false block (R-C follow-on from Phase 357, dogfood entry <id>)"` and record the new phase number; otherwise record `R-C: ruled block, no follow-on`.
    3. D-17 dual filing: append the same `## Execution outcome (Phase 357 close, <date>)` section to both research files: bar counts (false blocks, new misses, parity), the known_miss and known_false_block ids with reasons, the two fixes and their cited entries, the Jev labeler run status (labeled or unlabeled, disagreement count, how ruled), the R6 byte delta or skip reason, and a link line to `.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md`. No transcript text, no names. Commit in the home repo by explicit path only: `git -C /home/jsagi commit --only -m "rethinking-mindrianos: Phase 357 execution outcome (replay bar, fixes, R6)" -- MindrianOS/research/2026-09-23-larry-extended-x-jev-gate-triad-replay.md MindrianRooms/rethinking-mindrianos/research/2026-09-23-larry-extended-x-jev-gate-triad-replay.md`.
    4. Commit the repo docs: `git commit --only -m "docs(357-10): close GATE357 rows with measured proof" -- .planning/REQUIREMENTS.md .planning/ROADMAP.md` (ROADMAP only when step 2 added a phase).
    5. SUMMARY notes (required sections): `## Byte delta` (before, after, percent, or the skip reason); `## Post-Gate Handoff (356 D-12)` stating the section was not touched, runChain still halts at the first material step, the handoff seam and the 356 contract test results, and whether larry-extended mentions forced_material (a note for jsagi-a7); `## Known pre-existing reds (R-J)` listing run-all-179 E2E-1, run-all-209 209-03, run-all-238 238-03 and the 5 test-card-fire-relevance-gate legs as known, not fixed in 357; `## Deferred` restating the CONTEXT deferred items (room-bind picker on harness turns may reuse the 'harness' class; intent-classifier unrelated F.1 mints; the other 13 Larry judgments; prose forks with 0 labels).
  </action>
  <verify>
    <automated>grep -c "GATE357-0" .planning/REQUIREMENTS.md && git -C /home/jsagi log -1 --format=%s -- MindrianOS/research/2026-09-23-larry-extended-x-jev-gate-triad-replay.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GATE357-0" .planning/REQUIREMENTS.md` is at least 9 and every GATE357 row is either `- [x]` or carries `Open:`
    - `grep -c "Execution outcome (Phase 357 close" /home/jsagi/MindrianOS/research/2026-09-23-larry-extended-x-jev-gate-triad-replay.md /home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-larry-extended-x-jev-gate-triad-replay.md` reports 1 for each
    - `git -C /home/jsagi show --stat HEAD --format=` lists only the two research files (explicit-path commit in the home repo)
    - The SUMMARY contains the four required sections named in step 5
    - `grep -c "$(printf '\xe2\x80\x94')" .planning/REQUIREMENTS.md` is unchanged from PLAN_BASE, and the two research files report 0
  </acceptance_criteria>
  <done>Every GATE357 requirement is closed with proof or left open with a reason, the outcome is filed in both research homes, and the R-C follow-on exists when it was ruled a text-dependent false block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Larry prose -> model behavior on every surface | a shrink that drops the fire mandate would re-open the silent-degrade (Canon Part 11 R15) |
| home repo (`/home/jsagi`) -> research trail | a very dirty tree; a plain commit would sweep unrelated work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-24 | Tampering | Larry card-rule prose | mitigate | shrink only after the replay proves R4; pinned phrases kept; gate-native-fire-w1, voice-mark-182, handoff-seam, 298 parity and manifest --check run; Post-Gate Handoff hash compared |
| T-357-25 | Repudiation | a skipped R6 reported as done | mitigate | skip reason recorded; GATE357-07 left open with the reason |
| T-357-05 | Information disclosure | research trail filing | mitigate | counts and ids only, no transcript text or names |
| T-357-08 | Tampering | home repo and shared planning files | mitigate | `git -C /home/jsagi commit --only -- <two paths>`; REQUIREMENTS edits limited to the Phase 357 section |
</threat_model>

<verification>
- R4 MET path: `node tests/test-gate-native-fire-w1.cjs && node tests/test-larry-voice-mark-182.cjs && node tests/test-larry-handoff-seam.cjs && node scripts/build-harness-manifest.cjs --check && bash tests/run-all-357.sh` all exit 0
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
</verification>

<success_criteria>
Larry's card-rule prose is at most half its former size and defers to the code verdict (or the skip is recorded because the bar was not met), every GATE357 row is closed with proof or annotated, and the outcome lives in both research homes.
</success_criteria>

<output>
Create `.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-10-SUMMARY.md` when done (`git add -f`, then `git commit --only -- <path>`), with the sections required by Task 2 step 5.
</output>
