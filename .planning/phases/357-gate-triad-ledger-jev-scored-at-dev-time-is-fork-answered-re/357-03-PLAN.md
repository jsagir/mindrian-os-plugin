---
phase: 357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re
plan: 03
type: execute
wave: 2
depends_on: ["357-01"]
files_modified:
  - scripts/jev-devtime-client.cjs
  - data/jev-policies/card-fire-replay.json
  - data/ROOM.md
  - scripts/build-section-command-ledger.cjs
  - tests/fixtures/356-section-guard-parity.json
  - tests/test-356-jev-client.cjs
autonomous: true
requirements: [GATE357-03]
tags: [jev, egress-guard, part8, shared-client, policy]

must_haves:
  truths:
    - "Phase 357 uses ONE shared dev-time Jev client, scripts/jev-devtime-client.cjs, imported when 356-02 or 354-17 already extracted it; only if it is absent does 357 extract it, and then by executing 356-02-PLAN.md Task 1 verbatim (D-10, 356 D-06/D-07, orchestrator ruling 2026-09-23)"
    - "357 does not redefine the guard schema: card_fire_replay is declared with the exact_state_v1 kind and the optional fields 356 defined (max_len_by_key, must_equal_file, string_keys, question_max_len, question_strings_from_file_key), and the guard is built as makeEgressGuard(profile, {root}) (R-G, 356-02, 356-07)"
    - "The shared EGRESS_PROFILES map gains exactly one new profile, card_fire_replay; no other profile changes (D-10, 356 D-08: one profile per builder, never a union)"
    - "card_fire_replay allows top keys {model, state, questions}, model jev-latest, exactly six string state keys (output_text, preceding_user_text, gate_subject_text, gate_shape, turns_since_gate, policy), caps on output_text, preceding_user_text and gate_subject_text, state.policy byte-identical to data/jev-policies/card-fire-replay.json, exactly three Noul questions (is_fork, already_answered, relevant) whose rule, boundary cases and criteria must be strings of the policy file (D-10, D-12, R-G)"
    - "Every refusal throws EGRESS_REFUSED naming the key before any fetch; nothing is stripped (R-G, 356 D-09)"
    - "data/jev-policies/card-fire-replay.json states three independent Noul policies in the Noul-native shape {policy_id, version, instructions, criteria: {true, false}, boundary_cases[]}, criteria an OBJECT, boundary_cases an array of strings (D-12, 356 D-01 amended)"
  artifacts:
    - path: "scripts/jev-devtime-client.cjs"
      provides: "EGRESS_PROFILES.card_fire_replay (exact_state_v1)"
      contains: "card_fire_replay"
    - path: "data/jev-policies/card-fire-replay.json"
      provides: "three Noul policies with string boundary cases"
      contains: "already_answered"
  key_links:
    - from: "scripts/jev-devtime-client.cjs"
      to: "data/jev-policies/card-fire-replay.json"
      via: "EGRESS_PROFILES.card_fire_replay.must_equal_file.policy + question_strings_from_file_key 'policy'"
      pattern: "card-fire-replay\\.json"
---

<objective>
Give Phase 357 its Part-8-clean path to Jev: the `card_fire_replay` profile on the one shared dev-time Jev client, declared in 356's `exact_state_v1` schema, plus the written policy for the three Noul questions (is-fork, already-answered, relevant).

Purpose: SPEC R3 (the dev-time labeler uses the shared client and allow-list, no forked guard), D-10, D-12, R-G, and the 2026-09-23 orchestrator ruling that 357 declares its profile with 356's fields and never redefines the schema. The labeler itself lands in plan 04.
Output: one profile entry in `scripts/jev-devtime-client.cjs`, the policy JSON, one `data/ROOM.md` row. The last three `files_modified` paths are touched ONLY in the ABSENT branch, where 357 executes 356-02-PLAN.md's extraction verbatim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-RESEARCH.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-02-PLAN.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-07-PLAN.md
@.claude/skills/spike-findings-MindrianOS-Plugin/SKILL.md
@.claude/skills/spike-findings-MindrianOS-Plugin/references/policy-execution-parity.md

Parallel-session rules (every task): explicit-path commits only (`git add <path>`, then `git commit --only -m "<msg>" -- <paths>`; peers keep files staged); record PLAN_BASE; `git status --short -- <file>` before editing and STOP on a peer diff; never `git add -A` / `git commit -a` / `git stash` / revert unowned diffs. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs (read-only), scripts/doctor.cjs, lib/core/graph-ops.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, lib/core/navigation.cjs, docs/OPEN-HANDOFFS.md. No em-dashes. Nothing in this plan is required from lib/ or hooks/.

Shared-file coordination (356 D-06, D-11; R-G): `scripts/jev-devtime-client.cjs` and `data/ROOM.md` are shared with 356 (jsagi-a7) and 354-17 (jsagi-25). Re-read each from disk immediately before editing. Add only the 357 entry. Never edit or reorder another phase's profile or row.

The client contract 357 consumes (356-02-PLAN.md context, 356-07-PLAN.md Task 1), stated as signatures:
- `makeEgressGuard(profile, opts)` -> `guard(payload)` returning true or throwing; `opts.root` is required when the profile declares `must_equal_file` (read once at construction, byte comparison, a path escaping root is refused).
- Kind `exact_state_v1` fields: `top_keys` (exact), `model` (exact value), `state_keys` (exact set, every key required), `question_ids` (exact set), `question_keys`, `question_type`, `instructions_keys`, `criteria_keys`; optional `max_len_by_key`, `must_equal_file`, `string_keys`, `question_max_len` (caps `instructions.question`), `question_strings_from_file_key` (every `instructions.rule`, each `instructions.boundary_cases` element, `criteria.true` and `criteria.false` must be a string value found in that file's JSON).
- Guard errors: `err.code = 'EGRESS_REFUSED'`, `err.profile`, `err.key`, message naming the key.
- `jev(body, {key, guard, endpoint, fetchImpl, sleepImpl})`, `pool(items, n, fn)`, `loadKey({env, secretsPath})`, frozen `EGRESS_PROFILES`.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| Corpus loader, `pre-phase.json`, fixture files, loader test, `run-all-357.sh` | 01 |
| `scripts/replay-card-fire.cjs`, `tests/test-357-replay.cjs` | 02 |
| `card_fire_replay` egress profile, `data/jev-policies/card-fire-replay.json` | 03 (this plan) |
| `scripts/label-card-fire-replay.cjs`, labeler refusal test, tripwire list entry | 04 |
| Authored debug and live entries | 05 |
| Dogfood extractor, sanitized dogfood entries, `357-DOGFOOD-LABELS.md` | 06 |
| D-07 and D-08a deterministic fixes with tests | 07 |
| `357-JEV-LABEL-REPORT.md`, ratified labels (human checkpoint) | 08 |
| `baseline.json`, standing-gate wiring, mutation leg | 09 |
| Larry prose shrink, manifest regen, D-17 filing, requirement closure | 10 |

<tasks>

<task type="auto">
  <name>Task 1: The three Noul policies (data/jev-policies/card-fire-replay.json) and the data/ROOM.md row</name>
  <files>data/jev-policies/card-fire-replay.json, data/ROOM.md</files>
  <read_first>
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md (D-10, D-12, the "Root cause" section, R-C, R-E, R-F)
    - .claude/skills/spike-findings-MindrianOS-Plugin/references/policy-execution-parity.md (state the policy, never let it be inferred)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-07-PLAN.md (Task 2 readPolicy shape checks: string policy_id, version, instructions, criteria {true, false} strings, boundary_cases a non-empty array of STRINGS; mirror that per policy entry)
    - lib/core/gate-relevance.cjs (lines 60-141 and 267-358: what the code calls answered, relevant and yes/no-shaped, so the policy states the gate's intent rather than its regex)
    - agents/larry-extended.md section "## Decision Gates -- fire the card, never draw the box (SEED-021)" (read only: genuine, unanswered, relevant fork)
    - `ls data/jev-policies/ 2>/dev/null` (356 may already have its own policy file; mirror its top-level field style)
    - data/ROOM.md (the "Files in this section" table) and `git status --short -- data/ROOM.md`
  </read_first>
  <action>
    1. Create `data/jev-policies/card-fire-replay.json` (create the folder if absent). Top level: `{ "policy_set": "card-fire-replay", "version": "1", "phase": "357", "used_by": "scripts/label-card-fire-replay.cjs (dev time only, never a hook)", "noul_mapping": {"yes_at_or_above": 0.8, "no_at_or_below": 0.2, "otherwise": "uncertain"}, "policies": [ three entries ] }`. Each entry has the Noul-native shape `{ "policy_id": "<id>", "version": "1", "instructions": "<the rule, one paragraph>", "criteria": {"true": "<gloss>", "false": "<gloss>"}, "boundary_cases": ["<case> -> <true|false>: <why>", ...] }` with `criteria` an OBJECT and `boundary_cases` an array of STRINGS (the guard's question_strings_from_file_key check matches each boundary case string exactly, so they must be plain strings). Plain language, hyphens only, no room content, no real names. Each policy stands alone; the three are never compared (D-12). Each `instructions` names the state keys it reads and states that `gate_shape` is one of `F.1`, `F.8`, `backstop` and `turns_since_gate` is `0` (reached this turn) or `1+` (an older reach):
       - `is_fork`: true when the assistant turn at `output_text` (or the gate at `gate_subject_text`) leaves the navigator a genuine structural choice between two or more distinct next moves that the turn itself cannot resolve; false for a status report, a holding message, a numbered step list, a yes/no confirmation closer, a footnote or citation list, a relayed background-agent report with no question, or prose that merely mentions options. At least six boundary cases covering: benign numbered action footer (false), footnote reference list (false), "want me to do X?" closer (false), a two-way strategic fork in prose such as run research vs build the plan (true), a three-option bracket-box menu (true), a relayed subagent report with no question (false).
       - `already_answered`: true when the text at `preceding_user_text` already picks one of the options of that same gate (by label, ordinal or an unambiguous paraphrase), so asking again repeats an answered question; false when it asks something new, changes topic, or is a harness message rather than a human reply. At least four boundary cases: "option 2 please" against a three-option gate (true), "yes" against a yes/no gate (true), a new question on another topic (false), a background task notification (false).
       - `relevant`: true when the gate at `gate_subject_text` is about what the human is working on in `preceding_user_text`; false when the only shared words are the gate's own interface wording (for example gate, reach, decision, session, room), when the gate names an unrelated artifact, or when the preceding record is a harness message (a subagent hand-back, a task notification, a peer or idle notice) rather than a human turn. At least four boundary cases: a human question about the gate code against a dial listing an unrelated research artifact (false, the 09:40 shape, R-F), a subagent hand-back against a room-bind gate (false, the 08:50 shape, R-E), a human asking to resume the named room (true), a human reply naming one of the dial's listed reaches (true).
    2. Validate with `node -e`: the file parses; exactly three policies with ids `is_fork`, `already_answered`, `relevant`; every `criteria` is a non-array object whose keys are exactly `true` and `false` with string values of at most 400 chars; every `boundary_cases` is a non-empty array of strings; each `instructions` is a string of at most 1200 chars; the whole file is at most 12000 bytes; no U+2014.
    3. `data/ROOM.md`: if `git status --short -- data/ROOM.md` is clean, add ONE row to the "Files in this section" table for `jev-policies/card-fire-replay.json` (hand-maintained, dev-time only; the three Noul policies the Phase 357 labeler states in every request; byte equality enforced by the `card_fire_replay` egress profile; never read at runtime; bound to Phase 357, canon parts 8 and 11). If 356 already added a `jev-policies/` row, add the 357 file as its own row without editing 356's. If the file has a peer diff, skip this step and record it in the SUMMARY.
    4. Commit: `git add data/jev-policies/card-fire-replay.json` then `git commit --only -m "feat(357-03): card-fire-replay Noul policies (is_fork, already_answered, relevant)" -- data/jev-policies/card-fire-replay.json data/ROOM.md` (drop data/ROOM.md from the list when step 3 was skipped).
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('data/jev-policies/card-fire-replay.json','utf8');const p=JSON.parse(t);if(p.policies.map(x=>x.policy_id).sort().join()!=='already_answered,is_fork,relevant')process.exit(1);for(const x of p.policies){if(Array.isArray(x.criteria)||Object.keys(x.criteria).sort().join()!=='false,true')process.exit(2);if(!Array.isArray(x.boundary_cases)||!x.boundary_cases.length||x.boundary_cases.some(b=>typeof b!=='string'))process.exit(3)}if(Buffer.byteLength(t)>12000)process.exit(4);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ok`
    - `node -e "const p=require('./data/jev-policies/card-fire-replay.json');const n=Object.fromEntries(p.policies.map(x=>[x.policy_id,x.boundary_cases.length]));process.exit(n.is_fork>=6&&n.already_answered>=4&&n.relevant>=4?0:1)"` exits 0
    - `grep -c "$(printf '\xe2\x80\x94')" data/jev-policies/card-fire-replay.json` is 0
    - When data/ROOM.md was edited: `grep -c "card-fire-replay.json" data/ROOM.md` is 1 and `git diff $PLAN_BASE -- data/ROOM.md | grep '^-' | grep -v '^---' | wc -l` is 0 (one added row, nothing removed)
  </acceptance_criteria>
  <done>The three Noul policies exist as standalone stated policy text with string boundary cases, and data/ROOM.md names the file.</done>
</task>

<task type="auto">
  <name>Task 2: Shared client (import-if-present; extraction only per 356-02 verbatim) and the card_fire_replay profile</name>
  <files>scripts/jev-devtime-client.cjs</files>
  <read_first>
    - Run `test -f scripts/jev-devtime-client.cjs && echo PRESENT || echo ABSENT` FIRST, then `git status --short -- scripts/jev-devtime-client.cjs` and `git log -5 --format='%h %s' -- scripts/jev-devtime-client.cjs` (record all three in the SUMMARY).
    - PRESENT: scripts/jev-devtime-client.cjs in full (the authoritative interface): confirm the `exact_state_v1` kind and which of its optional fields exist (`max_len_by_key`, `must_equal_file`, `string_keys`, `question_max_len`, `question_strings_from_file_key`).
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-02-PLAN.md (Task 1 STEP 0 to STEP 3 and Task 2: the extraction spec 357 must follow verbatim if ABSENT, including its parity corpus, its test and its DEFER rule for build-section-command-ledger.cjs)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-07-PLAN.md (Task 1 step 1: the three optional exact_state_v1 fields string_keys, question_max_len, question_strings_from_file_key; and step 2: the material_step_ledger profile, the model for card_fire_replay)
    - data/jev-policies/card-fire-replay.json (Task 1 output)
  </read_first>
  <action>
    Branch on existence (D-10 import-if-present; orchestrator ruling: never redefine the schema).
    PRESENT branch (expected, since 356 and 354-17 run before or alongside 357):
    1. If the client has an uncommitted diff you did not make, STOP and report (a peer is mid-edit).
    2. If the `exact_state_v1` kind or any of the five optional fields card_fire_replay needs is missing, add ONLY the missing piece by following 356-02-PLAN.md Task 1 STEP 2 (kind, max_len_by_key, must_equal_file) and 356-07-PLAN.md Task 1 step 1 (string_keys, question_max_len, question_strings_from_file_key) verbatim, additively, touching no existing profile; before editing, message the 356 owner (jsagi-a7) that 357 is adding exactly those pieces to their spec (if no messaging tool is available, write the message under `## Peer messages (to relay)` in the SUMMARY). Record in the SUMMARY every line added.
    3. Add one frozen entry, touching no other profile: `card_fire_replay: Object.freeze({ id: 'card_fire_replay', kind: 'exact_state_v1', top_keys: ['model','state','questions'], model: 'jev-latest', state_keys: ['output_text','preceding_user_text','gate_subject_text','gate_shape','turns_since_gate','policy'], string_keys: ['output_text','preceding_user_text','gate_subject_text','gate_shape','turns_since_gate','policy'], max_len_by_key: { output_text: 4000, preceding_user_text: 2000, gate_subject_text: 300 }, must_equal_file: { policy: 'data/jev-policies/card-fire-replay.json' }, question_ids: ['is_fork','already_answered','relevant'], question_keys: ['type','instructions','criteria'], question_type: 'noul', instructions_keys: ['question','rule','boundary_cases'], criteria_keys: ['true','false'], question_max_len: 400, question_strings_from_file_key: 'policy' })` with every nested array and object frozen. The `must_equal_file` path is also the single source of the policy path for the labeler (it reads `EGRESS_PROFILES.card_fire_replay.must_equal_file.policy`).
    ABSENT branch (only if neither 356-02 nor 354-17 has landed):
    4. Execute 356-02-PLAN.md Task 1 (STEP 0 preflight with its 353 baselines and peer notice, STEP 1 parity corpus `tests/fixtures/356-section-guard-parity.json`, STEP 2 ABSENT extraction, STEP 3 commit) and Task 2 (its `tests/test-356-jev-client.cjs` and its REFACTOR-or-DEFER rule for scripts/build-section-command-ledger.cjs) exactly as written there, attributing the commits to `357-03` and naming 356-02 as the spec in each commit message. Then add the three 356-07 optional fields per step 2 above. Then add card_fire_replay per step 3. Record in the SUMMARY that 357 executed 356-02's extraction so the 356 session takes its PRESENT branch, and message jsagi-a7 and jsagi-25 (or record the message for relay).
    Both branches:
    5. Guard smoke check with `node -e`, building the guard as `makeEgressGuard(EGRESS_PROFILES.card_fire_replay, { root: process.cwd() })` and a body `{model: 'jev-latest', state: {output_text: 'a', preceding_user_text: 'b', gate_subject_text: '', gate_shape: 'backstop', turns_since_gate: '0', policy: <policy file text>}, questions: {<each of the three ids>: {type: 'noul', instructions: {question: 'Q?', rule: <that policy's instructions>, boundary_cases: <that policy's boundary_cases>}, criteria: <that policy's criteria>}}}`: it returns true; a one-byte policy change throws with `err.code === 'EGRESS_REFUSED'` and `err.key` naming policy; an extra state key `room` throws naming `room`; `output_text` of 4001 chars throws naming `output_text`; a `rule` string not in the file throws. Record the five outcomes in the SUMMARY (plan 04 turns them into a test).
    6. Commit: `git add scripts/jev-devtime-client.cjs` then `git commit --only -m "feat(357-03): card_fire_replay egress profile on the shared Jev client (exact_state_v1)" -- scripts/jev-devtime-client.cjs` (in the ABSENT branch, the 356-02 files are committed by their own step-4 commits).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=require('./scripts/jev-devtime-client.cjs');const pr=c.EGRESS_PROFILES.card_fire_replay;if(!pr||pr.kind!=='exact_state_v1'||!Object.isFrozen(pr))process.exit(1);const t=fs.readFileSync(pr.must_equal_file.policy,'utf8');const P=JSON.parse(t).policies;const q={};for(const x of P)q[x.policy_id]={type:'noul',instructions:{question:'Q?',rule:x.instructions,boundary_cases:x.boundary_cases},criteria:x.criteria};const g=c.makeEgressGuard(pr,{root:process.cwd()});const s={output_text:'a',preceding_user_text:'b',gate_subject_text:'',gate_shape:'backstop',turns_since_gate:'0',policy:t};if(g({model:'jev-latest',state:s,questions:q})!==true)process.exit(2);try{g({model:'jev-latest',state:Object.assign({},s,{policy:t+' '}),questions:q});process.exit(3)}catch(e){if(e.code!=='EGRESS_REFUSED')process.exit(4)}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ok`
    - In the PRESENT branch without missing fields: `git diff $PLAN_BASE -- scripts/jev-devtime-client.cjs | grep '^-' | grep -v '^---' | wc -l` is 0 (purely additive)
    - `node -e "const c=require('./scripts/jev-devtime-client.cjs');const n=Object.keys(c.EGRESS_PROFILES);process.exit(n.includes('card_fire_replay')&&n.includes('section_command_ledger')?0:1)"` exits 0 and the SUMMARY lists the full profile key list before and after (only card_fire_replay added)
    - `grep -v '^\s*//' scripts/jev-devtime-client.cjs | grep -c "require(.*lib/"` is 0
    - `node tests/test-353-tripwires.cjs` exits 0; when `tests/test-356-jev-client.cjs` exists, `env -u TYPESAFE_API_KEY node tests/test-356-jev-client.cjs` exits 0
    - `git status --short -- scripts/eval-icm-writers.cjs tests/test-353-grader-agreement.cjs tests/test-353-ledger-shape.cjs` is unchanged from the plan-start record (unowned files untouched)
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/jev-devtime-client.cjs` is 0
  </acceptance_criteria>
  <done>The one shared client carries a card_fire_replay profile declared in 356's exact_state_v1 schema, built with {root}, refusing with EGRESS_REFUSED, and no other profile's behavior moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-time labeler -> TypeSafe API (api.typesafe.ai) | the only place any 357 text leaves the machine; only sources (a), (b), (c) may cross |
| shared client -> other phases' builders | a profile or schema change could widen 353's or 356's limits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-02 | Information disclosure | state.policy and question text smuggling room text | mitigate | must_equal_file pins state.policy to the file bytes (read once, byte compare); question_strings_from_file_key forces rule, boundary cases and criteria to be policy-file strings; question_max_len caps the fixed question sentence; max_len_by_key caps the three text keys; every refusal is EGRESS_REFUSED naming the key |
| T-357-16 | Elevation of privilege | EGRESS_PROFILES union or schema redefinition | mitigate | one new profile only; 357 adds schema pieces only verbatim from 356-02 / 356-07 when missing, with a peer message; additive-diff acceptance |
| T-357-17 | Tampering | unowned dirty 353 consumers | mitigate | ABSENT branch follows 356-02's REFACTOR-or-DEFER rule verbatim; unowned files never edited; git status compared before and after |
| T-357-07 | Information disclosure | TYPESAFE_API_KEY | mitigate | loadKey never prints or logs the key; nothing in this plan calls the network |
| T-357-SC | Tampering | package installs | accept | none (Node built-ins only) |
</threat_model>

<verification>
- The Task 1 and Task 2 verify commands print `ok`
- `node tests/test-353-tripwires.cjs` exits 0
- `! grep -rn "api.typesafe.ai" lib/ hooks/`
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
</verification>

<success_criteria>
Phase 357 reaches Jev only through the one shared client, with one dedicated exact_state_v1 profile in 356's schema and one stated policy file, refusing (never stripping) on any key, cap, policy-byte or foreign-question-text violation.
</success_criteria>

<output>
Create `.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-03-SUMMARY.md` when done (`git add -f`, then `git commit --only -- <path>`). Record: PLAN_BASE, the branch taken (PRESENT or ABSENT), any schema pieces added and from which 356 plan, peer messages sent or to relay, the profile key list before and after, the five guard smoke outcomes, and whether data/ROOM.md was edited.
</output>
