---
phase: 357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re
plan: 02
type: execute
wave: 2
depends_on: ["357-01"]
files_modified:
  - scripts/replay-card-fire.cjs
  - tests/test-357-replay.cjs
autonomous: true
requirements: [GATE357-02, GATE357-06]
tags: [card-fire, replay, tri-polar, hermetic, mcp]

must_haves:
  truths:
    - "`node scripts/replay-card-fire.cjs [--surface cli|mcp|both] [--baseline write|compare] [--json] [--code-root <dir>|pre-phase] [--only <ids>] [--source <src>] [--corpus-dir <dir>]` replays every corpus entry through the REAL deriveTurnSignals -> classifyCardFire (CLI) and handleStopEvent (MCP), with no mocks of either (SPEC R2, D-13)"
    - "The replay makes zero network calls: it replaces globalThis.fetch with a thrower for the whole run, and a test proves no fetch is attempted (D-13)"
    - "Every entry and every surface runs in a fresh temp MINDRIAN_HOME, CARD_FIRE_SIDECHANNEL_PATH, MINDRIAN_ROOMS_HOME and MINDRIAN_ROOMS_ROOT with a unique session id and stopGateHandler._resetForTest(), so the navigator's real active room and ~/.mindrian are never read or written (R-H, RESEARCH Pitfall 4)"
    - "Live and dogfood entries replay in transcript mode (a temp jsonl plus seeded side-channel records), so the turn-text code that D-07 changes is actually exercised (R-I)"
    - "`--code-root pre-phase` runs the same corpus against a git archive of pre_phase_sha, so the pre-phase false block reproduces as FALSE_BLOCK (R-I, SPEC R2)"
    - "Exit is 1 when false_blocks > 0 or new_misses > 0 (known_miss and known_false_block excluded per R-C); exit 2 on a harness error or a CLI/MCP class mismatch in --surface both (SPEC R2, R5)"
    - "CLI and MCP are compared by verdict CLASS only (block vs pass), since an MCP block carries no reason; dedup-already-fired-this-session is ignored (R-H, D-14)"
  artifacts:
    - path: "scripts/replay-card-fire.cjs"
      provides: "replay harness: runReplay, runEntry, prepareCodeRoot, classifyOutcome, main"
      exports: ["runReplay", "runEntry", "prepareCodeRoot", "classifyOutcome", "main"]
      min_lines: 250
    - path: "tests/test-357-replay.cjs"
      provides: "GATE357-02 / -06 legs: network ban, exit semantics, hermeticity negative control, parity, code-root, live anchors, bar"
      min_lines: 180
  key_links:
    - from: "scripts/replay-card-fire.cjs"
      to: "scripts/check-card-fire.cjs"
      via: "require from the code root; deriveTurnSignals + classifyCardFire + exported retry accessors + consumeReachedGatesForVerdict"
      pattern: "deriveTurnSignals"
    - from: "scripts/replay-card-fire.cjs"
      to: "lib/mcp/stop-gate-handler.cjs"
      via: "handleStopEvent(sessionId, stopContext) after _resetForTest()"
      pattern: "handleStopEvent"
    - from: "scripts/replay-card-fire.cjs"
      to: "scripts/card-fire-replay-corpus.cjs"
      via: "loadCorpus / readPrePhase"
      pattern: "loadCorpus"
---

<objective>
Build the replay harness: one command that runs the whole labeled corpus through the real Stop-hook card gate on both Tri-Polar surfaces, hermetically, with no network, against HEAD or against a git archive of the pre-phase commit, and reports false blocks, missed forks, known misses and new misses.

Purpose: SPEC R2 (replay harness), SPEC R5 (CLI vs MCP parity), D-13, R-H, R-I. This is the measuring instrument every later plan (corpus authoring, fixes, bar gate) uses.
Output: `scripts/replay-card-fire.cjs` and `tests/test-357-replay.cjs`. No runtime (lib/, hooks/, check-card-fire) change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-SPEC.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-RESEARCH.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-PATTERNS.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-01-SUMMARY.md
@scripts/card-fire-replay-corpus.cjs

Parallel-session rules (every task): explicit-path commits only (`git add <path>`, `git add -f` under `.planning/`, then `git commit --only -m "<msg>" -- <paths>`; peers keep files staged, a plain commit sweeps them in); record PLAN_BASE; `git status --short -- <file>` before editing and STOP on a peer diff; never `git add -A` / `git commit -a` / `git stash` / revert unowned diffs. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, lib/core/navigation.cjs, docs/OPEN-HANDOFFS.md. Scripts must not require anything under tests/ (RESEARCH anti-pattern). No em-dashes.

Interface this plan consumes (from 357-01): `loadCorpus({corpusDir, include238, sources}) -> {entries, files, errors}`, `readPrePhase(corpusDir) -> {pre_phase_sha, runtime_files} | null`, `validateEntry`, `CORPUS_DIR`. Entry envelope modes: `direct` (fields go straight to deriveTurnSignals), `transcript` (`envelope.transcript` records -> temp jsonl -> `transcript_path`), `sidechannel` (`envelope.sidechannel_records` `{entry, shape, age_ms, subject}` seeded into the side file), `transcript+sidechannel` (the live Stop contract). Optional `envelope.steps` (prior Stops replayed first in the same session) and `envelope.counters` `{retry, session}`.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `scripts/card-fire-replay-corpus.cjs`, `pre-phase.json`, fixture files, loader test, `run-all-357.sh` | 01 |
| `scripts/replay-card-fire.cjs`, `tests/test-357-replay.cjs` | 02 (this plan; mutation leg: 09) |
| `card_fire_replay` egress profile, `data/jev-policies/card-fire-replay.json` | 03 |
| `scripts/label-card-fire-replay.cjs`, labeler refusal test, tripwire list entry | 04 |
| Authored debug and live entries | 05 |
| Dogfood extractor, sanitized dogfood entries, `357-DOGFOOD-LABELS.md` | 06 |
| D-07 and D-08a deterministic fixes with tests | 07 |
| `357-JEV-LABEL-REPORT.md`, ratified labels (human checkpoint) | 08 |
| `baseline.json`, standing-gate wiring, mutation leg | 09 |
| Larry prose shrink, manifest regen, D-17 filing, requirement closure | 10 |

<tasks>

<task type="auto">
  <name>Task 1: Replay core (hermetic env, envelope modes, CLI and MCP surfaces, code root, metrics, exit codes)</name>
  <files>scripts/replay-card-fire.cjs</files>
  <read_first>
    - scripts/check-card-fire.cjs (lines 1482-1665 deriveTurnSignals; lines 1669-1740 main(), whose bookkeeping order the CLI surface mirrors; lines 1753-1800 exports: deriveTurnSignals, classifyCardFire, loadRegistry, turnContextHash, consumeReachedGatesForVerdict, readRetryCount, bumpRetryCount, clearRetryCount, readSessionCount, bumpSessionCount, clearSessionCount)
    - lib/mcp/stop-gate-handler.cjs (lines 380-400 _resetForTest; lines 445-589 handleStopEvent, its return shapes {fire:false, reason, business}, {fire:true, business, ...}, and 'handler-error' / 'dedup-already-fired-this-session')
    - lib/core/card-fire-sidechannel.cjs (lines 60-160 TTL_MS, TURN_FRESH_MS, sideFilePath env override; lines 266-332 recordReachedGate({sessionId, surface, shape, subjectText, filePath}) and the stored record shape with `ts`)
    - lib/hmi/turn-text.cjs (readTurnText: which jsonl record shapes it reads, role from message.role or type)
    - tests/helpers/cardfire-hermetic-238.cjs (the MINDRIAN_HOME + CARD_FIRE_SIDECHANNEL_PATH pair; copy the idea, do not require it)
    - tests/test-198-stop-gate-retry-ceiling.test.cjs (lines 60-80: MINDRIAN_ROOMS_HOME / MINDRIAN_ROOMS_ROOT for the MCP path)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-RESEARCH.md (Finding 7, Patterns 1-3, "Side-channel mode seeding" example, Pitfalls 2 and 4)
  </read_first>
  <action>
    Create `scripts/replay-card-fire.cjs` ('use strict'; header comment: dev-time only, never a hook, never required from lib/ or hooks/, zero network, hyphens only; switch-case argv router like the repo's other CLIs). Implement:
    1. Network ban (D-13): at module load of the CLI entry (inside `main`, and inside `runReplay` for in-process callers) save `globalThis.fetch` and replace it with a function that throws `new Error('network forbidden in replay-card-fire')`; restore it in a `finally`.
    2. `prepareCodeRoot(spec)`: `spec` undefined or the repo path returns `{dir: <repo root>, cleanup: noop}`. `spec === 'pre-phase'` reads `readPrePhase()`; if null, throw `pre-phase.json missing`. Then mkdtemp under os.tmpdir() (prefix `replay-357-root-`), run `git archive <pre_phase_sha> scripts lib data package.json .claude-plugin | tar -x -C <tmp>` via child_process.execFileSync with a shell pipe (or `git archive -o <tmp>/a.tar` then `tar -xf`), verify each `runtime_files` digest against the extracted file's sha256 (throw naming the path on a mismatch), and symlink `<tmp>/node_modules` to the repo's `node_modules` when that directory exists. Any other string is treated as an existing directory. Returns `{dir, cleanup}` where cleanup `fs.rmSync(tmp, {recursive:true, force:true})`.
    3. `loadCodeRootModules(dir)`: require `<dir>/scripts/check-card-fire.cjs` and `<dir>/lib/mcp/stop-gate-handler.cjs` and `<dir>/lib/core/card-fire-sidechannel.cjs` by absolute path; cache per dir.
    4. `withHermeticEnv(label, fn)`: mkdtemp a dir; set `MINDRIAN_HOME=<dir>/home`, `CARD_FIRE_SIDECHANNEL_PATH=<dir>/card-fire-reached.json`, `MINDRIAN_ROOMS_HOME=<dir>/rooms` and `MINDRIAN_ROOMS_ROOT=<dir>/rooms` (created empty); remember and restore each previous value (delete when previously unset); rmSync the dir in `finally`. Env vars are read at call time by these modules (retryFilePath, sideFilePath, session-room), so a fresh dir per entry per surface isolates everything; if a leg in Task 2 shows state leaking between entries, fall back to one dir per run plus deleting the two side files between entries, and record that in the SUMMARY.
    5. `materializeEnvelope(env, dir, sessionId, mods)`: for modes containing `transcript`, write `env.transcript` records as JSON lines to `<dir>/transcript.jsonl` and build a stop context `{session_id, transcript_path}` with NO output_text / preceding_user_text / preceding_user_text_source; for modes containing `sidechannel`, call `mods.sidechannel.recordReachedGate({sessionId, surface: rec.entry, shape: rec.shape, subjectText: rec.subject, filePath: <side file>})` per record, then read the side file JSON, set each seeded record's `ts = Date.now() - rec.age_ms`, write it back; for `direct`, copy the direct fields plus `session_id`. Seed `env.counters` by calling `bumpSessionCount(sessionId)` `session` times, and `bumpRetryCount(turnContextHash(deriveTurnSignals(ctx)))` `retry` times.
    6. `runCliStop(ctx, mods)`: mirror `main()` exactly minus stdin, stdout and the intercept log: `registry = loadRegistry()`, `turn = deriveTurnSignals(ctx)`, `ctxHash = turnContextHash(turn)`, `turn.retry_count = readRetryCount(ctxHash)`, `turn.session_count = readSessionCount(turn.session_id)`, `verdict = classifyCardFire(turn, registry)`; then on degrade clear both counters and `consumeReachedGatesForVerdict(turn, verdict)`; on intercept bump both counters; otherwise clear both and consume. Return `{class: verdict.intercept === true ? 'block' : 'pass', reason: verdict.reason, degrade: verdict.degrade === true, preceding_user_text_source: turn.preceding_user_text_source, preceding_user_is_meta: turn.preceding_user_is_meta}`. A `predicate-error` reason is returned as `{class: 'error'}`.
    7. `runMcpStop(ctx, mods)`: `mods.stopGateHandler._resetForTest()` is called once per entry before the first step; `r = await handleStopEvent(ctx.session_id, ctx)`; class `block` when `r.fire === true`, `pass` otherwise; reason `r.reason`; `room_dir = r.business ? r.business.room_dir : null`; `handler-error` returns `{class: 'error'}`; `dedup-already-fired-this-session` sets `dedup: true`.
    8. `runEntry(entry, {codeRoot, surface})`: for each surface requested run in its own `withHermeticEnv`, with session id `replay357-<surface>-<index>-<random hex>`: replay `envelope.steps` in order (each materialized then run), then the final envelope; the entry's result is the final step's result. Returns `{cli?, mcp?}`.
    9. `classifyOutcome(entry, cls, baselineClass)`: `known_false_block` present -> `KNOWN_FALSE_BLOCK` when cls is block, else OK; `envelope_partial === true` -> `EXCLUDED_PARTIAL`; expected pass and cls block -> `FALSE_BLOCK`; expected block and cls pass -> `NEW_MISS` when baselineClass === 'block', else `KNOWN_MISS` when `known_miss` is set, else `MISSED_FORK`; cls error -> `ERROR`; otherwise `OK`.
    10. `runReplay(opts)`: load the corpus (`corpusDir` default CORPUS_DIR), exit 2 on loader errors; filter by `--only` (comma list of ids) and `--source`; prepare the code root; run every entry; the outcome uses the CLI class (or the MCP class for `--surface mcp`); in `both`, a CLI/MCP class difference that is not an MCP dedup is a `PARITY_MISMATCH` (counted, listed). Baseline: `--baseline write` writes `<corpusDir>/baseline.json` as `{meta: {pre_phase_sha, code_root: 'pre-phase'|'HEAD'|<dir>, written_at, entry_count}, verdicts: {<id>: 'block'|'pass'}}` and refuses (exit 2) unless the code root is `pre-phase` (D-03); `--baseline compare` (and the default when baseline.json exists) reads it for new_misses and lists corpus ids missing from the baseline. Counts: `entries, evaluated, false_blocks, missed_forks, known_misses, known_false_blocks, new_misses, excluded_partial, parity_mismatches, errors, unmarked_misses` (unmarked = MISSED_FORK). Always clean up the code root in `finally`.
    11. Output: `--json` prints one JSON object `{code_root, pre_phase_sha, surface, counts, entries: [{id, source, expected, cli, mcp, baseline, outcome}]}`; otherwise a compact human table with the counts and every non-OK entry, plus `known misses:` and `known false blocks:` id lists. Exit 2 on errors or parity mismatches, else 1 when `false_blocks > 0 || new_misses > 0`, else 0.
    12. Export `runReplay, runEntry, prepareCodeRoot, classifyOutcome, main`; run `main()` only under `require.main === module`.
    Commit: `git add scripts/replay-card-fire.cjs` then `git commit --only -m "feat(357-02): card-fire replay harness (CLI + MCP, hermetic, code-root)" -- scripts/replay-card-fire.cjs`.
  </action>
  <verify>
    <automated>node scripts/replay-card-fire.cjs --surface both --source 238 --json > /tmp/r357.json; echo "exit=$?"; node -e "const r=require('/tmp/r357.json');if(r.counts.entries!==18||r.counts.errors||r.counts.parity_mismatches||r.counts.false_blocks)process.exit(1);if(r.entries.some(e=>e.mcp&&e.mcp.room_dir!==null&&e.mcp.room_dir!==undefined))process.exit(1);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/replay-card-fire.cjs --surface both --source 238 --json` exits 0 and reports 18 entries, 0 errors, 0 parity mismatches, 0 false blocks (the 238 corpus is green at HEAD)
    - `node scripts/replay-card-fire.cjs --code-root pre-phase --surface cli --source 238` exits 0 and `ls -d /tmp/replay-357-root-* 2>/dev/null | wc -l` is 0 afterwards (archive cleaned up)
    - `grep -v '^\s*//' scripts/replay-card-fire.cjs | grep -c "require(.*tests/"` is 0
    - `grep -v '^\s*//' scripts/replay-card-fire.cjs | grep -c "MINDRIAN_ROOMS_HOME"` is at least 1 and `grep -v '^\s*//' scripts/replay-card-fire.cjs | grep -c "_resetForTest"` is at least 1
    - `grep -v '^\s*//' scripts/replay-card-fire.cjs | grep -c "network forbidden in replay-card-fire"` is 1
    - `stat -c %Y ~/.mindrian/card-fire-retries.json 2>/dev/null` printed before and after one `--surface both` replay run gives the same value (or the file is absent both times); both values are recorded in the SUMMARY
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/replay-card-fire.cjs` is 0
  </acceptance_criteria>
  <done>The harness replays the 238 corpus green on both surfaces, hermetically, and can archive and replay the pre-phase commit.</done>
</task>

<task type="auto">
  <name>Task 2: Replay test (network ban, exit semantics, hermeticity negative control, modes, parity, live anchors, bar)</name>
  <files>tests/test-357-replay.cjs</files>
  <read_first>
    - scripts/replay-card-fire.cjs (Task 1 output: CLI flags, JSON shape, exit codes, exports)
    - scripts/card-fire-replay-corpus.cjs (validateEntry rules the temp corpora must satisfy)
    - tests/test-238-card-fire-corpus.cjs (style: ok(desc, fn), header comment)
    - lib/core/resolve-active-room.cjs (lines 1-40 and ~240-330: MINDRIAN_ROOMS_HOME/.rooms/registry.json `active` field, to build the negative-control room)
    - tests/test-209-primary-sidechannel.cjs (lines ~470-540 writeTranscriptAndDerive: minimal jsonl records for a typed vs tool_result preceding turn)
  </read_first>
  <action>
    Create `tests/test-357-replay.cjs` ('use strict', node:assert/strict, ok counter, header naming GATE357-02 and GATE357-06). Test hygiene: at top `delete process.env.TYPESAFE_API_KEY`; every spawned child gets an env copy without TYPESAFE_API_KEY, `HOME` set to a fresh mkdtemp dir, and `NODE_OPTIONS=--require <abs path of a preload written by this test into a mkdtemp dir>` whose body replaces `globalThis.fetch` with a function that writes `NETWORK_ATTEMPT_357` to stderr and throws. Helper `replay(args, extraEnv)` spawns `node scripts/replay-card-fire.cjs ...args --json` and returns `{status, json, stderr}`. Temp corpora are written with `--corpus-dir <mkdtemp>` and valid meta (`sanitization_statement`) and must pass `validateEntry`. Legs:
    - L1 network ban: a full `--surface both` run has no `NETWORK_ATTEMPT_357` in stderr.
    - L2 exit semantics (SPEC R2 acceptance): (a) temp corpus with one direct entry whose output is a genuine bracket box (`[1] ... [2] ... [3] ...` plus a choice cue, as in the 238 Half B texts), `sidechannel_health: 'healthy'`, `reach_corroborated: true`, labeled pass -> outcome FALSE_BLOCK, `counts.false_blocks === 1`, exit 1; (b) temp corpus with one ordinary no-gate direct entry labeled block plus a temp `baseline.json` saying `block` for it -> NEW_MISS, exit 1; (c) the same entry with `known_miss: {reason: 'text-dependent prose fork'}` and no baseline -> KNOWN_MISS, exit 0; (d) entry (a) with `known_false_block: {reason: 'text-dependent'}` -> KNOWN_FALSE_BLOCK, exit 0.
    - L3 hermeticity negative control (R-H, Pitfall 4): build a decoy rooms home in mkdtemp with `.rooms/registry.json` whose `active` names a decoy room dir containing a `STATE.md`; run `--surface mcp --source 238` with `MINDRIAN_ROOMS_HOME` and `MINDRIAN_ROOMS_ROOT` pointing at the decoy in the child env; assert every `entries[].mcp.room_dir` is null or undefined and the decoy `STATE.md` mtime and bytes are unchanged.
    - L4 envelope modes and bookkeeping: temp corpus with (a) a `transcript+sidechannel` entry whose preceding user record is a tool_result block and whose side channel holds a fresh (age_ms 30000) F.1 record with a relevant subject -> pass with cli reason `preceding-turn-synthetic-no-user-engagement`; (b) the same with a human-typed preceding record (`origin: {kind: 'human'}`) sharing a 4+ char content token with the subject -> block `reached-registry-gate-no-card`; (c) a two-step entry: step 1 has an assistant record carrying an AskUserQuestion tool_use block and a fresh relevant record (card fired -> pass, record consumed), final step the same human turn with no card and no new record -> pass (proves the CLI mirrors main()'s consumption); assert (a)-(c) on both surfaces with 0 parity mismatches.
    - L5 code root: `--code-root pre-phase --source 238` gives the same per-entry classes as the HEAD run and leaves no `replay-357-root-*` dir in os.tmpdir().
    - L6 live anchors (SPEC R2 acceptance, active when `live-2026-09-23.json` has >= 2 entries, otherwise print `SKIP L6: live entries land in 357-05` and do not fail): `--code-root pre-phase --surface both --source live` reports `live-2026-09-23-01` and `live-2026-09-23-02` with outcome FALSE_BLOCK and cli reason `reached-registry-gate-no-card`, and exits 1.
    - L7 bar (active when `tests/fixtures/card-fire-replay/baseline.json` exists, otherwise print `SKIP L7: baseline lands in 357-09`): HEAD `--surface both --baseline compare` exits 0 with `false_blocks === 0`, `new_misses === 0`, `parity_mismatches === 0`, `unmarked_misses === 0`, and the baseline covers every corpus id.
    - A `--mutation` argv flag is reserved: when passed and baseline.json is absent, print `SKIP mutation: baseline lands in 357-09` and exit 0 (plan 09 implements the leg).
    Exit 1 on any failed leg.
    Commit: `git add tests/test-357-replay.cjs` then `git commit --only -m "test(357-02): replay harness legs (network ban, exit, hermeticity, parity)" -- tests/test-357-replay.cjs`.
  </action>
  <verify>
    <automated>node tests/test-357-replay.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-357-replay.cjs` exits 0, printing L1-L5 as passing and SKIP lines for L6 and L7
    - `grep -c "NETWORK_ATTEMPT_357" tests/test-357-replay.cjs` is at least 2
    - `grep -c "known_false_block" tests/test-357-replay.cjs` is at least 1 and `grep -c "NEW_MISS" tests/test-357-replay.cjs` is at least 1
    - `grep -c "live-2026-09-23-01" tests/test-357-replay.cjs` is at least 1
    - `git status --short` after the run shows no new file outside tests/ and scripts/ changed by this plan (the tests write only to mkdtemp dirs)
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-357-replay.cjs` is 0
  </acceptance_criteria>
  <done>The replay's exit semantics, hermeticity, envelope modes, main()-equivalent bookkeeping and CLI/MCP parity are proven by tests, and the live-anchor and bar legs are already written and waiting on their inputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay process -> navigator's real rooms and `~/.mindrian` | handleStopEvent resolves the machine-wide active room and runs closeOutRoom when MINDRIAN_ROOMS_HOME is unset |
| replay process -> network | the harness must be provably offline |
| git archive temp tree -> repo | the pre-phase code root must never write into the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-03 | Tampering | handleStopEvent closeOutRoom on the real active room | mitigate | per-entry temp MINDRIAN_ROOMS_HOME and MINDRIAN_ROOMS_ROOT; L3 decoy-room negative control asserts room_dir null and decoy STATE.md untouched |
| T-357-04 | Tampering | `~/.mindrian` retries, side channel, intercept log | mitigate | per-entry temp MINDRIAN_HOME and CARD_FIRE_SIDECHANNEL_PATH; the intercept log is never written; real retries mtime recorded unchanged |
| T-357-12 | Denial of service | mkdtemp code roots and hermetic dirs | mitigate | cleanup in finally; L5 asserts no leftover replay-357-root-* dir |
| T-357-14 | Information disclosure | network egress from the replay | mitigate | globalThis.fetch replaced with a thrower for the run; L1 spawns with a NETWORK_ATTEMPT_357 preload |
| T-357-15 | Repudiation | false parity (vacuous MCP pass) | mitigate | handler-error and predicate-error are ERROR outcomes (exit 2), never pass; parity compares classes per entry |
</threat_model>

<verification>
- `node tests/test-357-replay.cjs` exits 0
- `node scripts/replay-card-fire.cjs --surface both --source 238` exits 0
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
- `git diff --name-only $PLAN_BASE..HEAD -- lib hooks scripts/check-card-fire.cjs` prints nothing (no runtime change in this plan)
</verification>

<success_criteria>
One command replays the labeled corpus on the CLI and MCP surfaces with zero network and zero side effects on the navigator's machine state, runs against HEAD or the archived pre-phase commit, and exits non-zero exactly when false blocks or new misses exist.
</success_criteria>

<output>
Create `.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-02-SUMMARY.md` when done (`git add -f`, then `git commit --only -- <path>`). Record: PLAN_BASE, the 238 replay counts on both surfaces, the real retries-file mtime before and after, whether per-entry env isolation held (Task 1 step 4 fallback used or not), and the exact CLI usage line.
</output>
