---
phase: 357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re
plan: 09
type: execute
wave: 6
depends_on: ["357-08"]
files_modified:
  - tests/fixtures/card-fire-replay/baseline.json
  - tests/test-357-replay.cjs
  - tests/run-all-238.sh
  - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-VALIDATION.md
autonomous: true
requirements: [GATE357-02, GATE357-04, GATE357-05, GATE357-06, GATE357-08]
tags: [bar-gate, baseline, parity, standing-gate, mutation]

must_haves:
  truths:
    - "baseline.json is written by replay-card-fire.cjs --code-root pre-phase --baseline write against the recorded pre-phase commit and committed (D-03)"
    - "On HEAD, `replay-card-fire.cjs --surface both --baseline compare` reports false_blocks = 0 (known_false_block excluded per R-C), new_misses = 0, parity_mismatches = 0, errors = 0, and every missed fork is a listed known_miss with a text-dependence reason (SPEC R4, R5, acceptance)"
    - "The R4 result is recorded as MET or NOT MET; NOT MET writes an RCA and routes to gap closure instead of improvising runtime changes (D-08: every extra change must cite an entry id)"
    - "The replay is a standing gate: it runs in tests/run-all-357.sh and as a new run_if leg in tests/run-all-238.sh (R-J, D-14)"
    - "Reverting the R4 fix fails the standing leg: `node tests/test-357-replay.cjs --mutation` rebuilds HEAD with the three runtime files restored to their pre-phase bytes and requires a non-zero replay exit naming the live entries as FALSE_BLOCK, and also checks each fix separately (SPEC R7, R-I, R-J)"
    - "Pre-existing reds (run-all-179 E2E-1, run-all-209 209-03, run-all-238 238-03, test-card-fire-relevance-gate 5 legs) are recorded as known and not fixed in 357 (R-J)"
  artifacts:
    - path: "tests/fixtures/card-fire-replay/baseline.json"
      provides: "pre-phase verdict per corpus id"
      contains: "pre_phase_sha"
    - path: "tests/test-357-replay.cjs"
      provides: "--mutation leg"
      contains: "--mutation"
    - path: "tests/run-all-238.sh"
      provides: "replay leg in the GATE-04 card-fire runner"
      contains: "test-357-replay"
  key_links:
    - from: "tests/run-all-238.sh"
      to: "tests/test-357-replay.cjs"
      via: "run_if leg"
      pattern: "run_if .*test-357-replay"
    - from: "tests/test-357-replay.cjs"
      to: "tests/fixtures/card-fire-replay/pre-phase.json"
      via: "git show <pre_phase_sha>:<runtime file> into a HEAD archive"
      pattern: "pre_phase_sha"
---

<objective>
Prove the phase bar and make it permanent: write the pre-phase baseline, run the full corpus on HEAD on both surfaces against it (0 false blocks, 0 new misses, parity), wire the replay into the existing card-fire runner, and prove that reverting the fix makes the standing leg fail.

Purpose: SPEC R4 bar, R5 parity, R7 regression permanence, D-03, D-13, D-14, R-I, R-J. This plan's R4 result gates plan 10's prose shrink (SPEC R6).
Output: baseline.json, the mutation leg, the run-all-238 leg, the VALIDATION sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-SPEC.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-VALIDATION.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-07-SUMMARY.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-08-SUMMARY.md
@scripts/replay-card-fire.cjs
@tests/run-all-238.sh

Parallel-session rules (every task): explicit-path commits only (`git add <path>`, `git add -f` under `.planning/`, then `git commit --only -m "<msg>" -- <paths>`; peers keep files staged); record PLAN_BASE; `git status --short -- <file>` before editing and STOP on a peer diff; never `git add -A` / `git commit -a` / `git stash` / revert unowned diffs. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, lib/core/navigation.cjs, docs/OPEN-HANDOFFS.md. No em-dashes. This plan changes NO runtime file (lib/, hooks/, scripts/check-card-fire.cjs).
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| Corpus loader, `pre-phase.json`, fixture files, loader test, `run-all-357.sh` | 01 |
| `scripts/replay-card-fire.cjs`, `tests/test-357-replay.cjs` | 02 |
| `card_fire_replay` egress profile, `data/jev-policies/card-fire-replay.json` | 03 |
| `scripts/label-card-fire-replay.cjs`, labeler refusal test, tripwire list entry | 04 |
| Authored debug and live entries | 05 |
| Dogfood extractor, sanitized dogfood entries, `357-DOGFOOD-LABELS.md` | 06 |
| D-07 and D-08a deterministic fixes with tests | 07 |
| `357-JEV-LABEL-REPORT.md`, ratified labels (human checkpoint) | 08 |
| `baseline.json`, `run-all-238.sh` replay leg, mutation leg, VALIDATION sign-off | 09 (this plan) |
| Larry prose shrink, manifest regen, D-17 filing, requirement closure | 10 |

<tasks>

<task type="auto">
  <name>Task 1: Baseline on the pre-phase commit and the R4 / R5 bar on HEAD</name>
  <files>tests/fixtures/card-fire-replay/baseline.json</files>
  <read_first>
    - scripts/replay-card-fire.cjs (`--baseline write|compare`, `--code-root pre-phase`, JSON counts)
    - tests/fixtures/card-fire-replay/pre-phase.json
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-08-SUMMARY.md (post-ruling counts, R-C outcome)
    - docs/RCA-TEMPLATE.md (only if the bar fails)
  </read_first>
  <action>
    1. `node scripts/replay-card-fire.cjs --code-root pre-phase --surface cli --baseline write` (D-03). Confirm the file's `meta.pre_phase_sha` equals pre-phase.json's and `verdicts` covers every corpus id.
    2. `node scripts/replay-card-fire.cjs --surface both --baseline compare --json > <scratchpad>/bar.json`; record exit code and counts. R4 is MET when the exit is 0 with `false_blocks === 0`, `new_misses === 0`, `parity_mismatches === 0`, `errors === 0` and `unmarked_misses === 0`. List `known_misses` and `known_false_blocks` ids with their reasons in the SUMMARY (SPEC acceptance: known misses listed by id with text-dependence reasons).
    3. If R4 is NOT MET: do not edit any runtime file. Write `.planning/debug/357-residual-false-blocks.md` per docs/RCA-TEMPLATE.md (`kind: rca`, one section per residual id: outcome, pre-phase and HEAD reason, whether a structural signal distinguishes it or it is text-dependent), `git add -f` it, record `R4: NOT MET` with the ids in the SUMMARY, and continue with Task 2 (the standing leg must exist and fail honestly). The remedy is `/gsd-plan-phase 357 --gaps` citing those ids (D-08), not improvisation here.
    4. Commit: `git add tests/fixtures/card-fire-replay/baseline.json` then `git commit --only -m "test(357-09): pre-phase replay baseline (D-03)" -- tests/fixtures/card-fire-replay/baseline.json` (plus the RCA path when written).
  </action>
  <verify>
    <automated>node scripts/replay-card-fire.cjs --surface both --baseline compare; echo "bar-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const b=require('./tests/fixtures/card-fire-replay/baseline.json'),p=require('./tests/fixtures/card-fire-replay/pre-phase.json');const c=require('./scripts/card-fire-replay-corpus.cjs').loadCorpus({});process.exit(b.meta.pre_phase_sha===p.pre_phase_sha&&c.entries.every(e=>e.id in b.verdicts)?0:1)"` exits 0
    - `node -e "const b=require('./tests/fixtures/card-fire-replay/baseline.json');process.exit(b.verdicts['live-2026-09-23-01']==='block'&&b.verdicts['live-2026-09-23-02']==='block'?0:1)"` exits 0 (the pre-phase code blocked both live turns)
    - The SUMMARY states `R4: MET` with bar-exit 0 and all five counts at 0, or `R4: NOT MET` with the residual ids and the committed RCA path
  </acceptance_criteria>
  <done>The baseline is committed and the R4 / R5 bar result on HEAD is measured and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Standing gate (run-all-238 leg), the mutation leg, and VALIDATION sign-off</name>
  <files>tests/test-357-replay.cjs, tests/run-all-238.sh, .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-VALIDATION.md</files>
  <read_first>
    - tests/test-357-replay.cjs (the reserved `--mutation` flag and legs L6, L7)
    - tests/run-all-238.sh (run_if legs, lines 44-62)
    - tests/fixtures/card-fire-replay/pre-phase.json and .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-07-SUMMARY.md (the two fix commit shas)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-VALIDATION.md
  </read_first>
  <action>
    1. Implement `--mutation` in tests/test-357-replay.cjs (replace the reserved SKIP when baseline.json exists): build a code root by `git archive HEAD scripts lib data package.json .claude-plugin` into mkdtemp (symlink node_modules as the harness does); then three mutants, each a fresh copy: (M1) restore `lib/hmi/turn-text.cjs` and `scripts/check-card-fire.cjs` from `git show <pre_phase_sha>:<path>` (reverts D-07); (M2) restore `lib/core/gate-relevance.cjs` (reverts D-08a); (M3) restore all three. For each, run `node scripts/replay-card-fire.cjs --code-root <mutant> --surface both --baseline compare --json` and require exit 1 with: M1 reporting `live-2026-09-23-01` as FALSE_BLOCK; M2 reporting `live-2026-09-23-02` as FALSE_BLOCK; M3 reporting both. Also require the unmutated HEAD run to exit 0 (so the failure is caused by the revert, not by the corpus). Clean every mkdtemp in finally. When R4 is NOT MET (the HEAD run exits non-zero), print `mutation leg: not provable, R4 NOT MET` and fail (an honest red).
    2. `tests/run-all-238.sh`: re-read it from disk; add ONE leg after the 238-07/08 corpus leg: `run_if "357 card-fire replay standing gate (GATE-04, R-J)" "tests/test-357-replay.cjs" node tests/test-357-replay.cjs`. Change nothing else in the file.
    3. Run `bash tests/run-all-357.sh` and `bash tests/run-all-238.sh`; record both summaries. In run-all-238, the only red leg allowed is the pre-existing 238-03 chosen-validation leg (R-J); record it as known. Also run `bash tests/run-all-179.sh` and `bash tests/run-all-209.sh` once and record their known reds (E2E-1, 209-03) unchanged. Do not fix any of them.
    4. 357-VALIDATION.md: set `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter, tick the Wave 0 and sign-off checkboxes that are now true, and set `**Approval:** approved <date> (357-09 bar: MET|NOT MET)`.
    5. Commit: `git add -f <VALIDATION>` then `git commit --only -m "test(357-09): replay standing gate in run-all-238 and the reverted-fix mutation leg (R7, R-J)" -- tests/test-357-replay.cjs tests/run-all-238.sh <VALIDATION path>`.
  </action>
  <verify>
    <automated>node tests/test-357-replay.cjs --mutation && bash tests/run-all-357.sh</automated>
  </verify>
  <acceptance_criteria>
    - With R4 MET: `node tests/test-357-replay.cjs --mutation` exits 0 and prints the three mutants as failing the replay with the named FALSE_BLOCK ids; `bash tests/run-all-357.sh` exits 0
    - `grep -v '^#' tests/run-all-238.sh | grep -c "test-357-replay"` is at least 1 and `git diff $PLAN_BASE -- tests/run-all-238.sh | grep '^-' | grep -v '^---' | wc -l` is 0
    - `bash tests/run-all-238.sh` output shows the 357 leg PASSED and no FAILED leg other than 238-03 (recorded as known)
    - `ls -d /tmp/replay-357-root-* 2>/dev/null | wc -l` is 0 after the mutation run
    - `grep -c "nyquist_compliant: true" .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-VALIDATION.md` is 1
    - `git diff --name-only $PLAN_BASE..HEAD -- lib hooks scripts` prints nothing (no runtime change)
  </acceptance_criteria>
  <done>The replay guards the card gate from the existing GATE-04 runner and its own phase runner, and reverting either fix is proven to fail it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future edits to check-card-fire / gate-relevance / turn-text -> user turns | a regression reintroduces force-blocks on harness or chrome-only turns |
| mutation temp trees -> repo | mutants must never write into the working tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-23 | Tampering | a silent revert of the fix | mitigate | run-all-238 and run-all-357 legs; mutation leg proves a revert of either fix fails the replay |
| T-357-12 | Denial of service | mkdtemp mutants and archives | mitigate | cleanup in finally; acceptance checks no leftover replay-357-root-* dir |
| T-357-20 | Repudiation | a bar claimed without proof | mitigate | counts recorded from JSON; NOT MET writes an RCA and routes to gap closure instead of improvised fixes |
| T-357-08 | Tampering | shared runner file | mitigate | one additive run_if line; zero removed lines; explicit-path commit |
</threat_model>

<verification>
- `node scripts/replay-card-fire.cjs --surface both --baseline compare` exits 0 (R4 MET) or the RCA exists (R4 NOT MET)
- `node tests/test-357-replay.cjs --mutation` exits 0 when R4 is MET
- `bash tests/run-all-357.sh` exits 0 when R4 is MET
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
</verification>

<success_criteria>
The phase bar (0 false blocks, 0 new misses, CLI/MCP parity) is measured on HEAD against a committed pre-phase baseline, and the replay is a standing gate whose failure on a reverted fix is proven.
</success_criteria>

<output>
Create `.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-09-SUMMARY.md` when done (`git add -f`, then `git commit --only -- <path>`). It MUST start with a line `R4: MET` or `R4: NOT MET` (plan 10 reads it), then: PLAN_BASE, the bar counts, known_miss and known_false_block ids with reasons, the three mutant results, the run-all-357 / 238 / 179 / 209 summaries, and the known pre-existing reds (R-J).
</output>
