---
phase: 357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re
plan: 06
type: execute
wave: 3
depends_on: ["357-01", "357-02"]
files_modified:
  - scripts/extract-dogfood-stop-events.cjs
  - tests/fixtures/card-fire-replay/dogfood.json
  - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md
autonomous: true
requirements: [GATE357-09, GATE357-01]
tags: [dogfood, part8, sanitization, extractor, local-only]

must_haves:
  truths:
    - "scripts/extract-dogfood-stop-events.cjs is dev-only and local-only: it reads the R-D snapshot (~/.cache/mindrian-dev/357-raw/, verified by sha256sum first), never the live transcript or intercept paths, never the network, and writes raw candidates only inside that snapshot dir (D-04, D-05, R-D)"
    - "Envelopes are reconstructed with the RESEARCH Finding 2 algorithm (UserPromptSubmit mints from plain-text and hookSpecificOutput JSON lines, transcript cut at the last assistant record before the Stop, 10-minute prune, 120-second freshness, consumption after non-block outcomes) and an entry whose reconstructed pre-phase verdict disagrees with the live outcome is marked envelope_partial and excluded from the metric (D-04)"
    - "Committed dogfood entries are sanitized paraphrases whose pre-phase verdict AND reason equal the raw envelope's, checked by the extractor before it writes dogfood.json; any mismatch or any leak-check hit blocks the write (D-05, RESEARCH Pitfall 5)"
    - "At least 20 dogfood entries are selected, excluding the two source (c) anchors, and include every other live block in the snapshot, the 09:20 human-typed block of session 0f86dd63 flagged for the R-C ruling, and a stratified set of passes (R-C, SPEC R1 (d))"
    - "Every dogfood entry is proposed locally with label_origin 'local' and listed in 357-DOGFOOD-LABELS.md (id, one-line sanitized gist, proposed verdict, why) for the plan-08 navigator review; dogfood text never goes to Jev (D-06, D-11)"
  artifacts:
    - path: "scripts/extract-dogfood-stop-events.cjs"
      provides: "--scan, --select, --apply-sanitized, --write-review-sheet [--with-head-verdicts]"
      min_lines: 250
    - path: "tests/fixtures/card-fire-replay/dogfood.json"
      provides: "source (d): >= 20 sanitized transcript-mode entries, label_origin local"
      contains: "verdict_preservation"
    - path: ".planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md"
      provides: "the D-06 review sheet"
      contains: "R-C"
  key_links:
    - from: "scripts/extract-dogfood-stop-events.cjs"
      to: "scripts/replay-card-fire.cjs"
      via: "runEntry(entry, {codeRoot: 'pre-phase'}) for fidelity and verdict preservation"
      pattern: "runEntry"
    - from: "scripts/extract-dogfood-stop-events.cjs"
      to: "lib/hmi/turn-text.cjs"
      via: "reconstruction reads transcript prefixes through the real reader (via the replay), never a second jsonl walker for turn text"
      pattern: "replay-card-fire"
---

<objective>
Build source (d): a local-only extractor that reconstructs real Stop events from the navigator's own dev-session transcripts in the R-D snapshot, a sanitization step that proves each paraphrase keeps its verdict, at least 20 committed dogfood entries with locally proposed labels, and the review sheet the navigator ratifies at the plan-08 checkpoint.

Purpose: SPEC R1 (d), D-04, D-05, D-06, R-C, R-D. Part 8: this text never leaves the machine and never goes to Jev.
Output: the extractor script, dogfood.json entries, 357-DOGFOOD-LABELS.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-RESEARCH.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-02-SUMMARY.md
@scripts/card-fire-replay-corpus.cjs
@scripts/replay-card-fire.cjs

Parallel-session rules (every task): explicit-path commits only (`git add <path>`, `git add -f` under `.planning/`, then `git commit --only -m "<msg>" -- <paths>`; peers keep files staged); record PLAN_BASE; `git status --short -- <file>` before editing and STOP on a peer diff; never `git add -A` / `git commit -a` / `git stash` / revert unowned diffs. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, lib/core/navigation.cjs, docs/OPEN-HANDOFFS.md. No em-dashes.

Part 8 rules (D-05, D-11, R-D): the raw dir is `~/.cache/mindrian-dev/357-raw/` (mode 700). Raw candidates, the selection, the sanitized map, the denylist and the verdict-preservation report all live inside that dir with mode 600 and are never committed or copied into the repo. The extractor prints counts only, never transcript text. Dogfood entries are never passed to the labeler (plan 04 refuses them) or any network call.

Snapshot facts (RESEARCH Findings 1-3): sessions 56924067, 0f86dd63, 0208790f, 21829408 plus `card-fire-intercepts.log`, all from 2026-09-23, all after the 2026-09-17T15:32Z window where the installed plugin equals the pre-phase code for every card-fire file. Stop outcome per check-card-fire hook command: `hook_success` stdout of length 39 = pass, length 71 = degrade, `hook_blocking_error` whose `blockingError.command` names `scripts/check-card-fire.cjs` = block. `system` subtype `stop_hook_summary` marks each Stop. The two source (c) anchors are session 56924067 at about 08:50:52Z and 09:40:16Z: exclude both from dogfood. The R-C case is the human-typed block in session 0f86dd63 at about 09:20Z.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| Corpus loader, `pre-phase.json`, fixture files, loader test, `run-all-357.sh` | 01 |
| `scripts/replay-card-fire.cjs`, `tests/test-357-replay.cjs` | 02 |
| `card_fire_replay` egress profile, `data/jev-policies/card-fire-replay.json` | 03 |
| `scripts/label-card-fire-replay.cjs`, labeler refusal test, tripwire list entry | 04 |
| Authored debug and live entries | 05 |
| `scripts/extract-dogfood-stop-events.cjs`, sanitized `dogfood.json`, `357-DOGFOOD-LABELS.md` | 06 (this plan) |
| D-07 and D-08a deterministic fixes with tests | 07 |
| `357-JEV-LABEL-REPORT.md`, ratified labels (human checkpoint) | 08 |
| `baseline.json`, standing-gate wiring, mutation leg | 09 |
| Larry prose shrink, manifest regen, D-17 filing, requirement closure | 10 |

<tasks>

<task type="auto">
  <name>Task 1: The local-only extractor (scan, select, apply-sanitized, review sheet)</name>
  <files>scripts/extract-dogfood-stop-events.cjs</files>
  <read_first>
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-RESEARCH.md (Finding 1 record shapes and subject recovery, Finding 2 reconstruction algorithm, Finding 3 anchor timeline, Pitfall 5)
    - scripts/replay-card-fire.cjs (exports runEntry, prepareCodeRoot; the envelope modes and the `counters` and `steps` fields)
    - scripts/card-fire-replay-corpus.cjs (validateEntry: the dogfood entry must pass it, mode must contain transcript)
    - scripts/intent-classifier.cjs (lines 1141 NAV-unchanged suppression line; lines 1626-1668 AskUserQuestion contract and payload lines, `rendered.text` subject; lines 2943-2950 and 3208-3215 F.8 subject)
    - lib/core/card-fire-sidechannel.cjs (lines 126-140 sanitizeSubjectText, 300-char cap)
    - scripts/label-topic-forest.cjs (lines 1-40 dev-script shell: requires, argv switch)
  </read_first>
  <action>
    Create `scripts/extract-dogfood-stop-events.cjs` ('use strict'; header: dev-time only, local only, reads the R-D snapshot, never the network, never the live paths, prints counts never text, raw artifacts stay in the raw dir, hyphens only). Switch-case argv. Global `--raw-dir <dir>` default `path.join(os.homedir(), '.cache', 'mindrian-dev', '357-raw')`. Every mode first runs `sha256sum -c SHA256SUMS` semantics in Node (recompute each listed file's sha256 and compare; refuse with exit 2 on any mismatch or a missing file) and refuses a raw dir inside the repo. Set `globalThis.fetch` to a thrower. Modes:
    1. `--scan`: for each session jsonl in the raw dir, walk records in order (per-line try/catch). Collect mints from UserPromptSubmit `hook_success` attachments: every `[AskUserQuestion contract: shape=F.x` found in the plain stdout OR inside a `{"hookSpecificOutput":...}` JSON line's `additionalContext` becomes `{entry: 'scripts/intent-classifier.cjs', shape, ts, subject}` with the subject recovered per Finding 1 and cut to 300 chars; a `[NAV DECISION unchanged` line mints nothing. At each check-card-fire Stop outcome (Finding 1 length and blockingError rules) build a candidate: the transcript prefix cut at the last `assistant` record before the Stop attachment, reduced to the records the gate reads (the previous user record, the preceding user record, and every assistant record of the current turn, each keeping `isMeta`, `origin.kind` and content blocks including AskUserQuestion tool_use); side-channel records = mints within 600000 ms before the Stop with `age_ms = stopTs - mintTs`; counters = the running retry and session counts (bump after a live block, reset after a live pass or degrade); after a live non-block outcome clear the mints (consumption). Candidate id `dogfood-<first 8 hex of session>-<HHMMSS of the Stop>`. Run `runEntry(candidate, {codeRoot: 'pre-phase', surface: 'cli'})` once per candidate (prepare the pre-phase code root once and reuse it) and store `raw_verdict {class, reason}` and `live_outcome`; mark `envelope_partial: true` when raw_verdict.class differs from live_outcome (degrade counts as pass). Write `<raw-dir>/candidates.json` with mode 600. Print only: sessions, stops, live blocks, degrades, agreement count, partial count.
    2. `--select`: read candidates.json; drop the two source (c) anchors (session 56924067 at 08:50:52Z and 09:40:16Z, matched by session prefix and Stop time within 5 s); take every remaining live block that is not partial, the 0f86dd63 ~09:20Z block flagged `r_c_case: true`, and a stratified sample of non-partial passes (card fired; relevance pass on a fresh primary gate; synthetic preceding record; no gate signal), until the total is at least 24 and at most 36; write `<raw-dir>/selected.json` (mode 600) and print the per-stratum counts.
    3. `--apply-sanitized <map.json>`: the map (authored by the executor in the raw dir) has one object per selected id: `{id, transcript: [sanitized records], sidechannel_records: [sanitized], proposed: 'block'|'pass', why, gist}`. For each id build the dogfood entry `{id, source: 'dogfood', envelope: {mode: 'transcript+sidechannel' (or 'transcript' when there are no records), transcript, sidechannel_records, counters}, expected_verdict_class: proposed, label_origin: 'local', why, r_c_case?}` and require: `validateEntry` passes; `runEntry(entry, {codeRoot: 'pre-phase', surface: 'both'})` gives the SAME cli class AND reason as the raw candidate's raw_verdict and the same class on MCP; the leak check passes (no email pattern, no `/home/`, no `MindrianRooms`, no UUID pattern, and no case-insensitive whole-word hit on any line of `<raw-dir>/denylist.txt`). Any failure is listed by id and nothing is written (exit 1). On success write `tests/fixtures/card-fire-replay/dogfood.json` keeping its existing meta plus `verdict_preservation: 'checked'`, `extracted_from: 'R-D snapshot (local only)'`, `entry_count`, and write `<raw-dir>/sanitize-report.json` (mode 600) with raw and sanitized verdicts per id.
    4. `--write-review-sheet [--with-head-verdicts]`: writes `.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md` from dogfood.json plus the map's `gist` values: header (D-06: navigator ratifies every row; confirmed rows flip to `human`; dogfood never goes to Jev), a table with columns id, gist, live outcome, pre-phase verdict, proposed verdict, why, and, with `--with-head-verdicts`, a HEAD verdict column computed by `runEntry(entry, {surface: 'cli'})` on the working tree; then a `## R-C case` section for the `r_c_case` entry (its overlap class: chrome token count and content token count, its pre-phase and HEAD verdicts, and the question: genuine relevant fork (block) or false block (pass); if pass with no deterministic rule, it becomes `known_false_block` with a text-dependence reason and a follow-on phase opens); then a `## How to answer` section (reply `approved`, or one line per correction `<id>: block|pass <reason>`, and for R-C `r-c: block` or `r-c: known_false_block <reason>`).
    5. `--leak-check <file> [<file> ...]`: runs the same leak check as `--apply-sanitized` (built-in patterns plus `<raw-dir>/denylist.txt`) over the named files; prints only the file name and the matched rule id (never the matched text) and exits 1 on any hit, 0 otherwise.
    Export the pure helpers used by the modes (`recoverSubject`, `classifyStopOutcome`, `buildCandidate`, `leakCheck`) for inspection; `main()` only under `require.main === module`.
    Commit: `git add scripts/extract-dogfood-stop-events.cjs` then `git commit --only -m "feat(357-06): local-only dogfood Stop-event extractor (R-D snapshot, verdict-preserving sanitization)" -- scripts/extract-dogfood-stop-events.cjs`.
  </action>
  <verify>
    <automated>node scripts/extract-dogfood-stop-events.cjs --scan && node scripts/extract-dogfood-stop-events.cjs --select && stat -c %a ~/.cache/mindrian-dev/357-raw/candidates.json</automated>
  </verify>
  <acceptance_criteria>
    - `--scan` exits 0 and prints counts where agreement plus partial equals stops, and the partial count is recorded in the SUMMARY (RESEARCH expected about 0 in this window)
    - `--select` exits 0 and reports between 24 and 36 selected entries including exactly one `r_c_case`
    - `stat -c %a ~/.cache/mindrian-dev/357-raw/candidates.json` prints 600
    - `git status --short` shows no candidates, selection, map or report file inside the repo
    - `grep -v '^\s*//' scripts/extract-dogfood-stop-events.cjs | grep -c "\.claude/projects"` is 0 (no live transcript path; the intercept log is read only as the snapshot copy under --raw-dir)
    - `grep -v '^\s*//' scripts/extract-dogfood-stop-events.cjs | grep -c "api.typesafe\|jev-devtime-client\|label-card-fire-replay"` is 0
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/extract-dogfood-stop-events.cjs` is 0
  </acceptance_criteria>
  <done>Real Stop events reconstruct from the snapshot with measured fidelity, and a local selection of at least 24 candidates with the R-C case exists outside the repo.</done>
</task>

<task type="auto">
  <name>Task 2: Sanitize, propose local labels, write dogfood.json and the review sheet</name>
  <files>tests/fixtures/card-fire-replay/dogfood.json, .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md</files>
  <read_first>
    - `~/.cache/mindrian-dev/357-raw/selected.json` (local only; read each candidate's records to author its paraphrase)
    - tests/fixtures/card-fire-replay/dogfood.json (meta and sanitization_statement)
    - lib/core/gate-relevance.cjs (lines 83-141 tokenizer rules and GATE_BOILERPLATE_TOKENS, so each paraphrase keeps its overlap class)
    - agents/larry-extended.md section "## Decision Gates -- fire the card, never draw the box (SEED-021)" (read only: the labeling standard for a genuine, unanswered, relevant fork)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md (D-05, D-06, R-C)
  </read_first>
  <action>
    1. Build `~/.cache/mindrian-dev/357-raw/denylist.txt` (mode 600): one term per line: every directory name under `~/MindrianRooms/`, every room name in `~/MindrianRooms/.rooms/registry.json` when present, and every person name, organization name, client or venture name you see while reading the selected records.
    2. Author `~/.cache/mindrian-dev/357-raw/sanitized-map.json` (mode 600): for every selected id, sanitized transcript records (same record count, order, `isMeta`, `origin.kind`, lead-tag class and tool_use blocks; text paraphrased with neutral stand-ins, keeping the same length class and the same chrome-vs-content overlap class and count against the gate subject), sanitized side-channel records (same shape and age_ms; subject paraphrased in the renderer layout with stand-ins), `gist` (one neutral line, no names), and `proposed` with `why`. Labeling rule (D-06, Claude proposes, navigator ratifies): `block` only when the final assistant turn leaves the navigator a genuine, unanswered structural fork AND the reached gate is relevant to the preceding HUMAN turn; `pass` for a harness-preceded turn (hand-back, task notification, peer or idle notice) unless the output itself poses a fork, for a turn whose gate the human already answered, for a gate unrelated to the human turn, and for a status or holding turn. The R-C case gets your honest proposal plus a why that names what the decision depends on.
    3. Run `node scripts/extract-dogfood-stop-events.cjs --apply-sanitized ~/.cache/mindrian-dev/357-raw/sanitized-map.json`. Iterate on the map (never on the rule or the raw verdict) until it exits 0. If an entry cannot keep its verdict after 3 paraphrase attempts, drop it from the map and record the id and the reason in the SUMMARY, as long as at least 20 entries remain (else STOP and report).
    4. Run `node scripts/extract-dogfood-stop-events.cjs --write-review-sheet` (the HEAD column is added at plan 08, after the fixes land).
    5. Verify: `node tests/test-357-corpus-loader.cjs` now passes L1-L6 (default mode fully green: four sources, at least 45 entries); `--dogfood-strict` still fails only on `label_origin` (expected until plan 08). `node scripts/replay-card-fire.cjs --code-root pre-phase --surface both --source dogfood` shows 0 errors and 0 parity mismatches.
    6. Commit: `git add tests/fixtures/card-fire-replay/dogfood.json` and `git add -f .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md`, then `git commit --only -m "test(357-06): sanitized dogfood corpus (local labels) and D-06 review sheet" -- tests/fixtures/card-fire-replay/dogfood.json .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md`.
  </action>
  <verify>
    <automated>node tests/test-357-corpus-loader.cjs && node -e "const d=require('./tests/fixtures/card-fire-replay/dogfood.json');if(d.meta.verdict_preservation!=='checked'||d.entries.length<20)process.exit(1);if(d.entries.some(e=>e.label_origin!=='local'))process.exit(2);if(d.entries.filter(e=>e.r_c_case).length!==1)process.exit(3);console.log('ok',d.entries.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-357-corpus-loader.cjs` exits 0 (default mode: four sources, at least 45 entries, every mode rule satisfied)
    - The verify node check prints `ok` with an entry count of at least 20
    - `grep -ciE "[A-Za-z0-9._%+-]+@|/home/|MindrianRooms|[0-9a-f]{8}-[0-9a-f]{4}-" tests/fixtures/card-fire-replay/dogfood.json .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md` reports 0 for both
    - `node scripts/extract-dogfood-stop-events.cjs --leak-check tests/fixtures/card-fire-replay/dogfood.json .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md` exits 0 (denylist of local room and person names plus built-in patterns)
    - `grep -c "R-C case" .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md` is at least 1
    - `git status --short` lists no file under `~/.cache` inside the repo and no raw file in the repo
    - `grep -c "$(printf '\xe2\x80\x94')" tests/fixtures/card-fire-replay/dogfood.json .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md` reports 0 for both
  </acceptance_criteria>
  <done>At least 20 sanitized, verdict-preserving dogfood entries with local label proposals are committed, and the review sheet with the R-C section is ready for the navigator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator's transcripts (R-D snapshot) -> tracked repo | real conversation text, room names and third-party names must not cross |
| dogfood corpus -> Jev | forbidden outright (Part 8, round 2 ruling) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-05 | Information disclosure | dogfood.json, 357-DOGFOOD-LABELS.md | mitigate | executor-authored paraphrases; extractor leak check (email, /home/, MindrianRooms, UUID, local denylist of room and person names) blocks the write; acceptance re-runs the denylist check |
| T-357-11 | Information disclosure | raw candidates, selection, map, denylist | mitigate | kept in the mode-700 raw dir with mode-600 files; the extractor refuses a raw dir inside the repo; counts-only stdout |
| T-357-01 | Information disclosure | dogfood text reaching Jev | mitigate | the extractor has no network (fetch thrower, no client import); the labeler refuses source dogfood (plan 04) |
| T-357-18 | Repudiation | sanitization that flips the verdict | mitigate | raw vs sanitized pre-phase verdict and reason must match on CLI and MCP class before writing; partial reconstructions are marked envelope_partial and excluded |
| T-357-04 | Tampering | real `~/.mindrian` state during reconstruction | mitigate | reconstruction runs through runEntry's hermetic env; live paths are never read |
</threat_model>

<verification>
- `node tests/test-357-corpus-loader.cjs` exits 0
- `node scripts/replay-card-fire.cjs --code-root pre-phase --surface both --source dogfood` reports 0 errors and 0 parity mismatches
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
</verification>

<success_criteria>
Source (d) exists as at least 20 sanitized real Stop events whose verdicts were proven unchanged by sanitization, with local label proposals and a review sheet ready for the single human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-06-SUMMARY.md` when done (`git add -f`, then `git commit --only -- <path>`). Record: PLAN_BASE, scan counts (stops, blocks, degrades, agreement, partial), selection counts per stratum, dropped ids with reasons, proposed block/pass counts, and the leak-check results. No transcript text.
</output>
