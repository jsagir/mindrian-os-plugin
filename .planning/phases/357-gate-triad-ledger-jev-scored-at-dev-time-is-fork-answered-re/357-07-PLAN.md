---
phase: 357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re
plan: 07
type: execute
wave: 4
depends_on: ["357-02", "357-05"]
files_modified:
  - lib/hmi/turn-text.cjs
  - scripts/check-card-fire.cjs
  - tests/test-357-harness-source.cjs
  - lib/core/gate-relevance.cjs
  - tests/test-357-f1-chrome.cjs
autonomous: true
requirements: [GATE357-04, GATE357-05]
tags: [card-fire, runtime-fix, turn-text, gate-relevance, deterministic, tri-polar]

must_haves:
  truths:
    - "A preceding user record is classified 'harness' when it has isMeta true and the previous user record is not human-upstream, OR when its origin.kind is not 'human' and its text leads with <task-notification, [Cross-session idle notice] or the observed peer framing; a meta record right after a human-typed record (Skill body, image) stays 'typed' (D-07 as amended by R-A, the V3 carve-out)"
    - "The match is structural only (isMeta flag, origin.kind, leading tag); <agent-message, <cross-session-message and [SYSTEM NOTIFICATION are NOT in the tag list (R-A: 0 stored occurrences)"
    - "classifyCardFire treats 'harness' exactly like 'tool_result' on the PRIMARY path (verdict preceding-turn-synthetic-no-user-engagement), the stale comments at the guard and at the consumption helper are corrected, and preceding_user_is_meta threads through readTurnText and deriveTurnSignals with direct-field precedence (D-07, D-02)"
    - "classifyPrecedingUserContentSource(content) called with one argument behaves byte-for-byte as before (test-209 Behavior 14)"
    - "gate-relevance strips a frozen F1_DIAL_CHROME_TOKENS set, derived only from lib/hmi/dial-presenter.cjs static template strings (never from frequency), from the GATE side of the overlap check, citing live-2026-09-23-02; GATE_BOILERPLATE_TOKENS is unchanged and the 238 corpus tuning is untouched (D-08, D-08a, D-09, R-F)"
    - "No rule of the form 'primary arm plus 0 option labels passes' is added (D-08); nothing is added beyond D-07 and the D-08a chrome strip; runtime stays pure local code with no ledger file and no network (SPEC R4)"
    - "After both fixes the replay reports live-2026-09-23-01, live-2026-09-23-02 and the three debug harness targets as OK on both surfaces, while the carve-out cases, the 238 Half B entries and every other block-labeled entry still block (SPEC R4, R5)"
  artifacts:
    - path: "lib/hmi/turn-text.cjs"
      provides: "'harness' source class, HARNESS_LEADS, preceding_user_is_meta"
      contains: "harness"
    - path: "scripts/check-card-fire.cjs"
      provides: "'harness' treated as synthetic at the PRIMARY guard; preceding_user_is_meta threaded"
      contains: "'harness'"
    - path: "lib/core/gate-relevance.cjs"
      provides: "F1_DIAL_CHROME_TOKENS (frozen, exported) stripped in gateSubjectTokens"
      exports: ["F1_DIAL_CHROME_TOKENS", "GATE_BOILERPLATE_TOKENS", "subjectTokens", "gateSubjectTokens", "gateTopicallyRelevant"]
    - path: "tests/test-357-harness-source.cjs"
      provides: "GATE357-04 legs"
      min_lines: 120
    - path: "tests/test-357-f1-chrome.cjs"
      provides: "GATE357-05 legs including the renderer drift test"
      min_lines: 100
  key_links:
    - from: "lib/hmi/turn-text.cjs"
      to: "scripts/check-card-fire.cjs"
      via: "preceding_user_text_source 'harness' consumed at the PRIMARY synthetic guard"
      pattern: "=== 'harness'"
    - from: "tests/test-357-f1-chrome.cjs"
      to: "lib/hmi/dial-presenter.cjs"
      via: "drift test re-derives chrome tokens from the renderer's static literals"
      pattern: "dial-presenter"
---

<objective>
Land the minimal deterministic runtime fix: (1) D-07 as amended by R-A, so a harness record (subagent hand-back, task notification, peer or idle notice) preceding a turn is treated as synthetic rather than as a human turn; (2) the D-08a fix per R-F, so the F.1 dial's own static chrome words can no longer satisfy topical relevance by themselves.

Purpose: SPEC R4 (the minimal deterministic change that brings the corpus false blocks to 0 with no new misses), D-07, D-08, D-08a, D-09, R-A, R-F. Each change cites the corpus entries it fixes. Both CLI (Stop hook) and MCP (stop_gate_check) inherit the change because they share classifyCardFire (Tri-Polar, SPEC R5).
Output: changes to three runtime files, two new tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-RESEARCH.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-05-SUMMARY.md
@lib/hmi/turn-text.cjs
@lib/core/gate-relevance.cjs

Parallel-session rules (every task): explicit-path commits only (`git add <path>`, then `git commit --only -m "<msg>" -- <paths>`; peers keep files staged); record PLAN_BASE; `git status --short -- <file>` before editing and STOP on a peer diff; never `git add -A` / `git commit -a` / `git stash` / revert unowned diffs. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, lib/core/navigation.cjs, docs/OPEN-HANDOFFS.md. No em-dashes. lib/ and scripts/check-card-fire.cjs stay pure local CJS: no new require of any network module, any ledger file or any scripts/ dev tool.

Known pre-existing red (R-J, do not fix, do not misattribute): tests/test-card-fire-relevance-gate.cjs has 5 red legs marked "expected until 210-05". Record its pass/fail counts before and after each task; they must not get worse.

Measured expectations (RESEARCH Finding 4, R-A): over 30 days of transcripts the V3 rule flips 65 blocks to pass, 0 passes to blocks, 0 human-upstream turns. Finding 5, R-F: the 09:40 block's only overlap was the chrome token `gate`; the 09:20 block (R-C) overlaps on `prior` plus one 10-char content token and is expected to STILL block after the chrome fix, which is correct behavior for this plan (its label is ruled at plan 08).
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| Corpus loader, `pre-phase.json`, fixture files, loader test, `run-all-357.sh` | 01 |
| `scripts/replay-card-fire.cjs`, `tests/test-357-replay.cjs` | 02 |
| `card_fire_replay` egress profile, `data/jev-policies/card-fire-replay.json` | 03 |
| `scripts/label-card-fire-replay.cjs`, labeler refusal test, tripwire list entry | 04 |
| Authored debug and live entries | 05 |
| Dogfood extractor, sanitized dogfood entries, `357-DOGFOOD-LABELS.md` | 06 |
| D-07 `'harness'` source class and D-08a F.1 chrome strip, `tests/test-357-harness-source.cjs`, `tests/test-357-f1-chrome.cjs` | 07 (this plan) |
| `357-JEV-LABEL-REPORT.md`, ratified labels (human checkpoint) | 08 |
| `baseline.json`, standing-gate wiring, mutation leg | 09 |
| Larry prose shrink, manifest regen, D-17 filing, requirement closure | 10 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-07 (R-A) harness source class in turn-text and the check-card-fire PRIMARY guard</name>
  <files>tests/test-357-harness-source.cjs, lib/hmi/turn-text.cjs, scripts/check-card-fire.cjs</files>
  <read_first>
    - lib/hmi/turn-text.cjs (whole file: classifyPrecedingUserContentSource :115-139, readTurnText's user-record branch :208-222, readTranscriptTurn, module.exports)
    - scripts/check-card-fire.cjs (lines 640-700 the PRIMARY guard and its block comment that says tool_result is the ONLY producer; lines 1320-1345 the stale comment "a background task-notification carrying a real text block classifies as 'typed'"; lines 1600-1665 the precedence rule for preceding_user_text_source and the returned turn object; exports :1753-1800)
    - tests/test-209-primary-sidechannel.cjs (lines ~555-585 Behavior 14: the 1-arg call contract that must stay green)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-RESEARCH.md (Finding 4 lead-tag table; Pattern 4; Pitfall 3)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md (D-07, R-A)
    - `cd ~/.cache/mindrian-dev/357-raw && sha256sum -c SHA256SUMS` (the peer-framing prefix in step 2 is measured from this snapshot)
  </read_first>
  <behavior>
    - classifyPrecedingUserContentSource(content) with 1 argument returns exactly what it returned before for a non-empty string, a text block array, a tool_result-only array, an empty array and null
    - With a record-meta second argument: isMeta true and prevHumanUpstream false returns 'harness'; isMeta true and prevHumanUpstream true returns the base class ('typed' for text)
    - originKind 'task-notification' with text starting '<task-notification>' returns 'harness'; originKind 'human' with the same leading text returns 'typed' (a pasted tag is still a human turn)
    - a non-human record starting '[Cross-session idle notice]' returns 'harness'; a human record starting '<command-message' or '[Image' returns 'typed'
    - readTurnText on a transcript (human prompt, assistant tool_use, tool_result, assistant, peer isMeta record) returns preceding_user_text_source 'harness' and preceding_user_is_meta true
    - readTurnText on (human prompt, isMeta Skill-body record) and on (human prompt, isMeta '[Image #1]' record) returns 'typed' with preceding_user_is_meta true
    - classifyCardFire on a PRIMARY-hit turn with a fresh relevant subject returns pass 'preceding-turn-synthetic-no-user-engagement' when the source is 'harness' and returns intercept 'reached-registry-gate-no-card' when the source is 'typed'
  </behavior>
  <action>
    1. Write `tests/test-357-harness-source.cjs` FIRST covering every behavior above (temp jsonl transcripts in mkdtemp, records shaped as in the 357-05 context: `{type, message: {role, content}, isMeta?, origin?}`), plus these corpus legs run through `node scripts/replay-card-fire.cjs --surface both --json --only <ids>` on HEAD: live-2026-09-23-01, debug-room-bind-task-notification, debug-harness-peer-after-tool-result and debug-harness-idle-notice have outcome OK; debug-carveout-skill-meta-after-human and debug-carveout-image-meta-after-human have cli class block; 0 parity mismatches. Test hygiene: delete TYPESAFE_API_KEY, fetch thrower, NET_ATTEMPTS === 0 at the end. Run it and record that the new-behavior legs fail before the change (RED).
    2. Measure the peer framing: with a scratch script (session scratchpad, not the repo), compute the longest common prefix of the text of every `origin.kind === 'peer'` user record across the four snapshot sessions; print only the prefix and the count. If the prefix is at least 8 chars and is harness framing (not conversation content), add it to the lead list; otherwise add nothing (peer records are all isMeta, so rule A covers them) and record why in the SUMMARY.
    3. `lib/hmi/turn-text.cjs` (D-07 per R-A): add frozen `HARNESS_LEADS` = `['<task-notification', '[Cross-session idle notice]']` plus the measured peer prefix when step 2 kept it (do NOT include `<agent-message`, `<cross-session-message` or `[SYSTEM NOTIFICATION`). Change `classifyPrecedingUserContentSource(content, rec)`: compute the existing result as `base` with the unchanged body; when `rec` is absent or not an object, return `base` (1-arg callers unchanged); lead = `extractAssistantText(content).trimStart()`; return `'harness'` when `rec.isMeta === true && rec.prevHumanUpstream !== true`, or when `rec.originKind !== 'human' && HARNESS_LEADS.some(t => lead.startsWith(t))`; else `base`. Keep the try/catch returning 'none'. In `readTurnText`, track `prevUserHumanUpstream` (false initially): for each role:user record compute `isMeta = obj.isMeta === true`, `originKind = obj.origin && typeof obj.origin === 'object' ? obj.origin.kind : undefined`, call `classifyPrecedingUserContentSource(userContent, {isMeta, originKind, prevHumanUpstream: prevUserHumanUpstream})`, record `lastPrecedingUserIsMeta = isMeta`, then set `prevUserHumanUpstream = originKind === 'human' || (!isMeta && /^(<command-name|<command-message)/.test(lead))`. Return `preceding_user_is_meta` from readTurnText and pass it through readTranscriptTurn. Export `HARNESS_LEADS`. Update the function's doc comment: the four classes typed / tool_result / harness / none, the R-A carve-out, structural only, never meaning.
    4. `scripts/check-card-fire.cjs`: at the PRIMARY guard change the condition to `t.preceding_user_text_source === 'tool_result' || t.preceding_user_text_source === 'harness'` (same verdict object); update the block comment above it (tool_result is no longer the only producer; name the 'harness' class, D-07/R-A, and the live anchor live-2026-09-23-01); rewrite the stale comment near :1333-1334 to say a task notification or other harness record is now classified 'harness' and exits through the synthetic guard; in deriveTurnSignals thread `preceding_user_is_meta` with direct-field precedence (`typeof e.preceding_user_is_meta === 'boolean'` wins, else the transcript value, else false) and add it to the returned object. Nothing else in the file changes.
    5. Run the new test (GREEN), `node tests/test-209-primary-sidechannel.cjs`, `node tests/test-238-card-fire-corpus.cjs`, `node tests/test-198-stop-gate-retry-ceiling.test.cjs`, and `node tests/test-card-fire-relevance-gate.cjs` (counts no worse than before). Commit: `git add tests/test-357-harness-source.cjs` then `git commit --only -m "fix(357-07): harness preceding record is synthetic, not a human turn (D-07, R-A; live-2026-09-23-01)" -- lib/hmi/turn-text.cjs scripts/check-card-fire.cjs tests/test-357-harness-source.cjs`.
  </action>
  <verify>
    <automated>node tests/test-357-harness-source.cjs && node tests/test-209-primary-sidechannel.cjs && node tests/test-238-card-fire-corpus.cjs && node tests/test-198-stop-gate-retry-ceiling.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All four commands in verify exit 0
    - `node tests/test-card-fire-relevance-gate.cjs` fail count is no higher than the count recorded before this task
    - `grep -v '^\s*//' lib/hmi/turn-text.cjs | grep -c "agent-message\|cross-session-message\|SYSTEM NOTIFICATION"` is 0 (R-A dropped tags)
    - `grep -v '^\s*//' scripts/check-card-fire.cjs | grep -c "=== 'harness'"` is 1
    - `node -e "const t=require('./lib/hmi/turn-text.cjs');const r=[t.classifyPrecedingUserContentSource('hi'),t.classifyPrecedingUserContentSource([{type:'tool_result',tool_use_id:'x'}]),t.classifyPrecedingUserContentSource([]),t.classifyPrecedingUserContentSource(null)].join();process.exit(r==='typed,tool_result,none,none'?0:1)"` exits 0 (1-arg contract)
    - `node scripts/replay-card-fire.cjs --surface both --only live-2026-09-23-01,debug-room-bind-task-notification,debug-harness-peer-after-tool-result,debug-harness-idle-notice --json` reports all four OK and 0 parity mismatches
    - `git diff $PLAN_BASE -- scripts/check-card-fire.cjs | grep '^[-+]' | grep -v '^[-+][-+]' | grep -vc '^\s*[-+]\s*//'` is at most 12 (a minimal code diff; comment lines excluded)
    - `grep -c "$(printf '\xe2\x80\x94')" lib/hmi/turn-text.cjs scripts/check-card-fire.cjs tests/test-357-harness-source.cjs` reports 0 for each
  </acceptance_criteria>
  <done>Harness records no longer read as human turns, the carve-out keeps human-initiated meta turns blocking, and the live anchor plus the three debug harness targets pass on both surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-08a (R-F) F.1 dial chrome strip in gate-relevance, with a renderer drift test</name>
  <files>tests/test-357-f1-chrome.cjs, lib/core/gate-relevance.cjs</files>
  <read_first>
    - lib/core/gate-relevance.cjs (lines 60-200: STOPWORDS, MIN_SUBJECT_TOKEN_LEN, GATE_BOILERPLATE_TOKENS and its header comment that predicts this F.1 extension and warns that F.1 canonical verbs are content; subjectTokens; gateSubjectTokens; lines 300-358 gateTopicallyRelevant and module.exports)
    - lib/hmi/dial-presenter.cjs (lines 115-175 PROMPT_LINE, FRAMING_MODE_B, FRAMING_TIER_0; lines 240-250 the gauge strings; lines 300-340 _buildDecisionGateHeader literals; lines 420-498 renderDial assembly)
    - scripts/intent-classifier.cjs (lines 1626-1668: the F.1 subject is rendered.text)
    - `git show 893cee043 --stat` and `git show 893cee043 -- lib/core/gate-relevance.cjs | head -80` (the GATE_BOILERPLATE_TOKENS precedent)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-RESEARCH.md (Finding 5, Pitfall 6 is unrelated; Finding 6 for the 238 Half B risk)
  </read_first>
  <behavior>
    - gateTopicallyRelevant(human text whose only token shared with an F.1 dial subject is 'gate', that subject) returns false (was true before)
    - gateTopicallyRelevant(human text sharing one non-chrome content token such as 'briefing' with the same subject) still returns true
    - A human text sharing 'prior' plus one 10-char content token with an F.1 subject still returns true (the R-C 09:20 shape stays blocked; its label is ruled at plan 08)
    - A subject made only of chrome words still returns true (gateTokens empty -> conservative true, unchanged)
    - The F.8 behaviors pinned by GATE_BOILERPLATE_TOKENS are unchanged (tests/test-209-primary-sidechannel.cjs Behaviors 12-13 and tests/test-card-fire-relevance-gate.cjs leg 3 keep their results)
    - The drift leg: every static literal listed in the test still appears verbatim in lib/hmi/dial-presenter.cjs, and the tokens derived from those literals minus GATE_BOILERPLATE_TOKENS equal F1_DIAL_CHROME_TOKENS exactly (no frequency-derived extras such as research, claim, back, bring, spin, worked)
  </behavior>
  <action>
    1. Write `tests/test-357-f1-chrome.cjs` FIRST covering the behaviors above (test hygiene block as in Task 1). The drift leg holds a frozen `DIAL_STATIC_LITERALS` array copied from dial-presenter.cjs: the PROMPT_LINE value, FRAMING_MODE_B, FRAMING_TIER_0, the header suffix ` - REACH - decision gate`, the context-line literals ` LOCAL `, ` / BRAIN `, ` / SIGNAL `, `ranked`, `offline`, `cold`, `prior`, `(offline)`, `(none this turn)`, and the gauge string `Investigate | Blend | Insight`; each must be found verbatim in the dial-presenter source text; derive tokens with the exported `subjectTokens`; assert the derived set minus GATE_BOILERPLATE_TOKENS deep-equals F1_DIAL_CHROME_TOKENS. Also exercise `renderDial` from dial-presenter for tier_mode `mode_a`, `mode_b` and `tier_0` with a minimal reach list (read renderDial's input contract; if a render needs data the test cannot supply, record SKIP for that tier with the reason) and assert that the rendered text's tokens, minus tokens of the context label and row labels the test supplied, are a subset of F1_DIAL_CHROME_TOKENS plus GATE_BOILERPLATE_TOKENS. Add corpus legs through the replay on HEAD: live-2026-09-23-02 outcome OK; every `238:*:s2` and `238:*:s3` entry cli class block; 0 parity mismatches. Run it and record RED.
    2. `lib/core/gate-relevance.cjs` (D-08a per R-F): add, directly after GATE_BOILERPLATE_TOKENS, a frozen `F1_DIAL_CHROME_TOKENS` Set containing exactly the tokens derived from DIAL_STATIC_LITERALS by subjectTokens minus those already in GATE_BOILERPLATE_TOKENS; its comment cites D-08a, R-F, the precedent commit 893cee043, the corpus entry `live-2026-09-23-02`, the rule "derived from dial-presenter static template strings, never from frequency", and the drift test file. In `gateSubjectTokens`, skip a token when it is in GATE_BOILERPLATE_TOKENS OR in F1_DIAL_CHROME_TOKENS. Leave GATE_BOILERPLATE_TOKENS, subjectTokens, STOPWORDS, gateAlreadyAnswered, extractOptionLabels and isYesNoShapedGate byte-identical. Export `F1_DIAL_CHROME_TOKENS`, `GATE_BOILERPLATE_TOKENS` and `subjectTokens` (additive exports).
    3. Run the new test (GREEN), `node tests/test-238-card-fire-corpus.cjs`, `node tests/test-209-primary-sidechannel.cjs`, `node tests/test-card-fire-relevance-gate.cjs` (counts no worse), `node tests/test-357-harness-source.cjs`. Then run the full replay on HEAD `node scripts/replay-card-fire.cjs --surface both --json` and record in the SUMMARY: every entry whose class changed versus `--code-root pre-phase`, which must be only pre-phase FALSE_BLOCK entries turning OK (no block-labeled entry may turn pass); list any remaining FALSE_BLOCK ids (expected: at most dogfood entries awaiting the plan-08 ruling, including the R-C case). If a block-labeled entry turned pass because of a specific chrome token, remove that token from F1_DIAL_CHROME_TOKENS only if it is also not a DIAL_STATIC_LITERALS token; otherwise STOP and report (do not invent a new rule, D-08).
    4. Commit: `git add tests/test-357-f1-chrome.cjs` then `git commit --only -m "fix(357-07): strip F.1 dial chrome from gate relevance (D-08a, R-F; live-2026-09-23-02)" -- lib/core/gate-relevance.cjs tests/test-357-f1-chrome.cjs`.
  </action>
  <verify>
    <automated>node tests/test-357-f1-chrome.cjs && node tests/test-238-card-fire-corpus.cjs && node tests/test-209-primary-sidechannel.cjs && node tests/test-357-harness-source.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All four commands in verify exit 0
    - `node tests/test-card-fire-relevance-gate.cjs` fail count is no higher than before this plan
    - `node -e "const g=require('./lib/core/gate-relevance.cjs');const f=g.F1_DIAL_CHROME_TOKENS;if(!(f instanceof Set)||!Object.isFrozen(f))process.exit(1);for(const w of ['research','claim','back','bring','spin','worked'])if(f.has(w))process.exit(2);if(!f.has('gate'))process.exit(3);console.log([...f].join(' '))"` exits 0
    - `git diff $PLAN_BASE -- lib/core/gate-relevance.cjs | grep "^-" | grep -v "^---" | grep -c "'bind', 'session', 'room', 'rooms', 'select', 'start', 'talk', 'new'"` is 0 (GATE_BOILERPLATE_TOKENS untouched)
    - `node scripts/replay-card-fire.cjs --surface both --only live-2026-09-23-02 --json` reports OK and 0 parity mismatches
    - The SUMMARY's HEAD-vs-pre-phase class-change list contains no entry labeled block that changed to pass
    - `grep -v '^\s*//' lib/core/gate-relevance.cjs | grep -c "zero-option\|labels.length === 0"` equals the count before this plan (no primary-arm-plus-0-labels rule, D-08)
    - `grep -c "$(printf '\xe2\x80\x94')" lib/core/gate-relevance.cjs tests/test-357-f1-chrome.cjs` reports 0 for each
  </acceptance_criteria>
  <done>F.1 dial chrome no longer counts as topical overlap, the list is pinned to the renderer by a drift test, live-2026-09-23-02 passes, and no block-labeled entry changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code transcript -> Stop hook | the preceding record's flags and text are untrusted structure; the fix must read structure, never meaning |
| hook runtime -> user's turn | a weakened gate lets a genuine fork go unrendered (Canon Part 11 R15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-09 | Spoofing | a human turn misread as harness (new missed fork) | mitigate | R-A carve-out: meta after a human-upstream record stays typed; human origin with a harness-looking lead stays typed; test legs for Skill-body, image and pasted-tag turns; new_misses gate at plan 09 |
| T-357-10 | Denial of service (of the R15 guarantee) | over-broad chrome strip | mitigate | chrome derived only from dial-presenter static literals, frozen, drift-tested; empty gate token set stays conservative true; 238 Half B and every block-labeled entry re-checked |
| T-357-19 | Tampering | runtime gaining a network or ledger dependency | mitigate | no new require in lib/ or check-card-fire beyond existing modules; `! grep -rn api.typesafe.ai lib/ hooks/`; SPEC R4 no ledger file |
| T-357-20 | Repudiation | a silent behavior change beyond the cited fixes | mitigate | commit messages cite live-2026-09-23-01 and -02; minimal-diff acceptance on check-card-fire; HEAD-vs-pre-phase class-change list in the SUMMARY |
</threat_model>

<verification>
- `node tests/test-357-harness-source.cjs && node tests/test-357-f1-chrome.cjs && node tests/test-209-primary-sidechannel.cjs && node tests/test-238-card-fire-corpus.cjs && node tests/test-198-stop-gate-retry-ceiling.test.cjs` all exit 0
- `node scripts/replay-card-fire.cjs --surface both --source live` exits 0 (both live entries OK on HEAD)
- `! grep -rn "api.typesafe.ai" lib/ hooks/`
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
</verification>

<success_criteria>
Both 2026-09-23 live false blocks and the authored harness cases pass on the CLI and MCP surfaces through two structural, deterministic, cited changes, while every block-labeled corpus entry still blocks.
</success_criteria>

<output>
Create `.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-07-SUMMARY.md` when done (`git add -f`, then `git commit --only -- <path>`). Record: PLAN_BASE, the two fix commit shas (plan 09's mutation leg reverts these), the peer-prefix measurement, the final F1_DIAL_CHROME_TOKENS list, relevance-gate counts before and after, and the HEAD-vs-pre-phase class-change list with the remaining FALSE_BLOCK ids.
</output>
