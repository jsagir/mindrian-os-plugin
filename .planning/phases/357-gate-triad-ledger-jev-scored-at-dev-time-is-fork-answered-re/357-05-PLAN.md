---
phase: 357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re
plan: 05
type: execute
wave: 3
depends_on: ["357-01", "357-02"]
files_modified:
  - tests/fixtures/card-fire-replay/debug-cases.json
  - tests/fixtures/card-fire-replay/live-2026-09-23.json
autonomous: true
requirements: [GATE357-01, GATE357-02]
tags: [card-fire, corpus, fixtures, debug-cases, live-evidence, sanitization]

must_haves:
  truths:
    - "Source (b) has at least one envelope for each of the 10 resolved debug cases check-card-fire owns, with reach-gate-stale-turn-input recorded only as a routed-in case (routed_in: intent-classifier), plus the two V3 carve-out cases (human then Skill-body meta, human then image meta, expected block) and the harness-preceded cases D-07 targets (SPEC R1 (b), R-A, RESEARCH Pitfall 3)"
    - "The intern-w1 prose fork with 0 extractable labels is labeled block and carries known_miss with a text-dependence reason (SPEC R4, Deferred)"
    - "Source (c) holds exactly two entries: live-2026-09-23-01 models the F.8 room-bind gate minted on the subagent hand-back turn, not the consumed F.1 fleet-census reach (R-E), and live-2026-09-23-02 models the human-typed turn whose only overlap with the unrelated F.1 dial subject is the chrome token 'gate' (D-08a, R-F)"
    - "Both live entries are transcript+sidechannel envelopes paraphrased from the R-D snapshot only (never the live paths), keep isMeta, origin.kind, record order, reach age and the token-overlap class, and carry no third-party names, room or venture content (D-05, R-D, R-I, RESEARCH Pitfall 5)"
    - "Replaying the corpus on the archived pre-phase commit reports both live entries as FALSE_BLOCK with reason reached-registry-gate-no-card and exits non-zero (SPEC R2 acceptance)"
  artifacts:
    - path: "tests/fixtures/card-fire-replay/debug-cases.json"
      provides: "source (b): >= 14 authored entries"
      contains: "debug-intern-w1-prose-fork"
    - path: "tests/fixtures/card-fire-replay/live-2026-09-23.json"
      provides: "source (c): live-2026-09-23-01 and live-2026-09-23-02"
      contains: "live-2026-09-23-02"
  key_links:
    - from: "tests/fixtures/card-fire-replay/live-2026-09-23.json"
      to: "scripts/replay-card-fire.cjs"
      via: "--code-root pre-phase --source live reproduces FALSE_BLOCK (test-357-replay L6)"
      pattern: "live-2026-09-23-01"
---

<objective>
Author the synthetic and sanitized corpus content: source (b), one or more envelopes per resolved card-fire RCA plus the carve-out and harness cases, and source (c), the two 2026-09-23 live false blocks reconstructed from the local R-D snapshot. Prove on the archived pre-phase code that the live false blocks reproduce.

Purpose: SPEC R1 (b) and (c), SPEC R2 acceptance (pre-phase run flags the live turn as FALSE_BLOCK), R-E (anchor correction), D-08a (second live case), D-05 (sanitization). These entries are the targets the D-07 and D-08a fixes (plan 07) must turn to pass without creating new misses.
Output: `debug-cases.json` and `live-2026-09-23.json` filled. No code change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-SPEC.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-RESEARCH.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-02-SUMMARY.md
@scripts/card-fire-replay-corpus.cjs
@tests/fixtures/card-fire-corpus-238.json

Parallel-session rules (every task): explicit-path commits only (`git add <path>`, then `git commit --only -m "<msg>" -- <paths>`; peers keep files staged); record PLAN_BASE; `git status --short -- <file>` before editing and STOP on a peer diff; never `git add -A` / `git commit -a` / `git stash` / revert unowned diffs. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, lib/core/navigation.cjs, docs/OPEN-HANDOFFS.md. No em-dashes.

Part 8 and sanitization rules for this plan (D-05, R-D): raw transcript text is read ONLY from `~/.cache/mindrian-dev/357-raw/` (never `~/.claude/projects/` or `~/.mindrian/` live paths). Scratch inspection scripts live in the session scratchpad or `~/.cache/mindrian-dev/357-raw/`, never in the repo. Committed text is authored or paraphrased: no real person name, no organization name, no email, no room name, no venture or client content, no absolute home path, no session UUID. Use neutral stand-in tokens such as ROOM_ALPHA, a neutral artifact name such as research/sample-briefing-mirror, and neutral framing text. Keep what decides the verdict: record order, `isMeta`, `origin.kind`, the leading tag class, gate shape, reach age, and whether the overlapping token between the preceding user text and the gate subject is chrome or content (RESEARCH Pitfall 5).

Record shapes the replay writes to a temp jsonl (readTurnText reads `message.role` or `type`): user text `{type: 'user', message: {role: 'user', content: '<text>' | [{type: 'text', text}]}, origin: {kind: 'human'}}`; harness records add `isMeta: true` and `origin: {kind: 'peer' | 'task-notification'}` as observed; a tool result `{type: 'user', message: {role: 'user', content: [{type: 'tool_result', tool_use_id: 'toolu_x', content: '<short text>'}]}}`; assistant `{type: 'assistant', message: {role: 'assistant', content: [{type: 'text', text}]}}` and, for a fired card, a content block `{type: 'tool_use', name: 'AskUserQuestion', id: 'toolu_y', input: {}}`. Side-channel records `{entry: 'scripts/intent-classifier.cjs', shape: 'F.1' | 'F.8', age_ms, subject}`; fresh means age_ms 30000 (far from the 120000 fresh window), stale means 300000 (inside the 600000 TTL). Subjects are at most 300 chars.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| Corpus loader, `pre-phase.json`, fixture files, loader test, `run-all-357.sh` | 01 |
| `scripts/replay-card-fire.cjs`, `tests/test-357-replay.cjs` | 02 |
| `card_fire_replay` egress profile, `data/jev-policies/card-fire-replay.json` | 03 |
| `scripts/label-card-fire-replay.cjs`, labeler refusal test, tripwire list entry | 04 |
| Authored `debug-cases.json` and `live-2026-09-23.json` entries | 05 (this plan) |
| Dogfood extractor, sanitized dogfood entries, `357-DOGFOOD-LABELS.md` | 06 |
| D-07 and D-08a deterministic fixes with tests | 07 |
| `357-JEV-LABEL-REPORT.md`, ratified labels (human checkpoint) | 08 |
| `baseline.json`, standing-gate wiring, mutation leg | 09 |
| Larry prose shrink, manifest regen, D-17 filing, requirement closure | 10 |

<tasks>

<task type="auto">
  <name>Task 1: Source (b), one or more entries per resolved RCA plus the carve-out and harness cases</name>
  <files>tests/fixtures/card-fire-replay/debug-cases.json</files>
  <read_first>
    - tests/fixtures/card-fire-replay/debug-cases.json (the empty-entries file and its meta)
    - The ten RCAs in .planning/debug/resolved/: backstop-benign-list-defeats-relevance-gate.md, card-fire-answered-gate-refires-within-ttl-window.md, card-fire-block-surface.md, card-fire-over-enforcement.md, card-fire-relevance-check-gap.md, card-fire-stale-f1-reach-suggestion-forces-block-regardless-of-relevance.md, stop-hook-fires-card-on-option-shaped-prose-sentence.md, room-bind-gate-fires-on-notification-only-turns.md, intern-w1-card-discipline-decay.md, reach-gate-stale-turn-input.md (read each file's frontmatter expected/actual/root_cause and its repro or evidence section only; `grep -n` for "repro\|evidence\|expected\|actual" first, then read those ranges)
    - scripts/check-card-fire.cjs (lines 523-786 classifyCardFire: the order of checks and every reason string)
    - lib/core/gate-relevance.cjs (lines 83-141 token rules and GATE_BOILERPLATE_TOKENS; lines 200-358 extractOptionLabels, isYesNoShapedGate, gateAlreadyAnswered, gateTopicallyRelevant)
    - scripts/card-fire-replay-corpus.cjs (validateEntry rules)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-RESEARCH.md (Pitfall 3 carve-out cases; Finding 4 lead-tag table)
  </read_first>
  <action>
    Fill `entries` of debug-cases.json with authored entries (source 'debug', label_origin 'hand', a one-sentence `why` citing the RCA file name). The optional informational field `routed_in` may be added where stated. Minimum set, ids as written:
    1. `debug-backstop-benign-list` (direct): a benign numbered action footer with no choice cue; `sidechannel_health: 'healthy'`, `reach_corroborated: false`; expected pass.
    2. `debug-answered-gate-refires` (transcript+sidechannel with `steps`): step 1 has a fresh F.8 record and an assistant record that fires AskUserQuestion (card fired, record consumed); the final envelope has the human's answer as the preceding user record, no card, no new record; expected pass (proves consumption).
    3. `debug-block-surface-simple-binary` (transcript+sidechannel): fresh relevant F.1 record, human turn sharing a content token with the subject, assistant output ending in a two-option yes/no box; expected pass (gate-is-simple-binary).
    4. `debug-over-enforcement-stale-terse` (transcript+sidechannel): stale F.1 record (age_ms 300000), terse human turn with fewer than 2 subject tokens; expected pass.
    5. `debug-relevance-check-gap-no-subject` (direct): `ran_entries: ['scripts/intent-classifier.cjs']`, no `gate_subject_text`, output a plain closing question; expected pass (primary-gate-existence-unconfirmed).
    6. `debug-stale-f1-irrelevant` (transcript+sidechannel): fresh F.1 subject naming an unrelated neutral artifact, a long human status question with zero shared 4+ char tokens with the subject; expected pass (gate-irrelevant-to-turn).
    7. `debug-option-shaped-prose-f8-chrome` (transcript+sidechannel): fresh F.8 subject in the emitBindingGate shape with ROOM_ALPHA as the candidate, a human turn that uses only session and room wording, output whose closing sentence names options in prose; expected pass.
    8. `debug-room-bind-tool-result` (transcript+sidechannel): preceding user record is a tool_result block, fresh F.8 record; expected pass (preceding-turn-synthetic-no-user-engagement).
    9. `debug-room-bind-task-notification` (transcript+sidechannel): preceding user record NOT isMeta, `origin: {kind: 'task-notification'}`, text starting `<task-notification>` and sharing one 4+ char content token with the fresh F.8 subject; expected pass. On pre-phase code this blocks (classified typed): a D-07 target.
    10. `debug-intern-w1-labeled-fork` (direct): a two-option fork whose labels are NOT yes/no-shaped, rendered as a bracket box with a choice cue, `healthy`, `reach_corroborated: true`; expected block.
    11. `debug-intern-w1-prose-fork` (direct): the same two-way fork written as prose with 0 extractable option labels and no gate signal, `healthy`, `reach_corroborated: false`; expected block with `known_miss: {reason: 'text-dependent: prose fork with 0 extractable option labels and no structural gate signal; needs text understanding (SPEC R4, Deferred)'}`.
    12. `debug-reach-gate-stale-turn-input` (transcript+sidechannel, `routed_in: 'intent-classifier'`): the card gate's view of the RCA repro turn: a fresh F.1 record and a terse human command turn with no card; expected block, with a `why` stating that the stale payload was intent-classifier's defect (fixed upstream, out of scope per SPEC) and that from the card gate's side a fresh unrendered gate after a terse human turn must still force (the WR-06 floor).
    13. `debug-carveout-skill-meta-after-human` (transcript+sidechannel): a human prompt (`origin: {kind: 'human'}`) followed immediately by an `isMeta: true` Skill-body user record (neutral text beginning `Base directory for this skill`) that shares one 4+ char content token with the fresh F.1 subject, then an assistant output with no card; expected block (R-A carve-out keeps it blocking).
    14. `debug-carveout-image-meta-after-human` (transcript+sidechannel): a human prompt followed by an `isMeta: true` record whose text is `[Image #1]`, fresh F.1 record, no card; expected block.
    15. `debug-harness-peer-after-tool-result` (transcript+sidechannel): tool_result user record, assistant record, then an `isMeta: true`, `origin: {kind: 'peer'}` plain-string record (neutral peer framing) sharing one content token with the fresh F.8 subject, then an assistant output with no options and no question; expected pass. Blocks on pre-phase code: a D-07 target.
    16. `debug-harness-idle-notice` (transcript+sidechannel): a tool_result record, then an `isMeta: true` record starting `[Cross-session idle notice]` sharing a content token with a fresh F.1 subject, no card; expected pass. A D-07 target.
    After writing, run `node scripts/replay-card-fire.cjs --code-root pre-phase --surface both --source debug --json` and require: 0 errors, 0 parity mismatches; outcomes FALSE_BLOCK only for ids 9, 15 and 16 (the D-07 targets); KNOWN_MISS for id 11; OK for every other id. If any other id is not OK, fix the fixture (not the code) so it reproduces the RCA's documented shape, and note each correction in the SUMMARY. Then run `node tests/test-357-corpus-loader.cjs` and confirm L1, L2, L5, L6 pass.
    Commit: `git commit --only -m "test(357-05): source (b) debug-case corpus entries (10 RCAs, carve-outs, harness cases)" -- tests/fixtures/card-fire-replay/debug-cases.json`.
  </action>
  <verify>
    <automated>node scripts/replay-card-fire.cjs --code-root pre-phase --surface both --source debug --json > /tmp/r357-debug.json; node -e "const r=require('/tmp/r357-debug.json');const o=Object.fromEntries(r.entries.map(e=>[e.id,e.outcome]));const fb=['debug-room-bind-task-notification','debug-harness-peer-after-tool-result','debug-harness-idle-notice'];if(r.counts.errors||r.counts.parity_mismatches)process.exit(1);if(r.counts.entries<16)process.exit(2);for(const [id,out] of Object.entries(o)){const want=fb.includes(id)?'FALSE_BLOCK':(id==='debug-intern-w1-prose-fork'?'KNOWN_MISS':'OK');if(out!==want){console.error(id,out,want);process.exit(3)}}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ok`
    - `node -e "const c=require('./scripts/card-fire-replay-corpus.cjs');const r=c.loadCorpus({});if(r.errors.length)process.exit(1);const d=r.entries.filter(e=>e.source==='debug');process.exit(d.length>=16?0:1)"` exits 0
    - `node -e "const d=require('./tests/fixtures/card-fire-replay/debug-cases.json').entries;const w=d.map(e=>e.why).join(' ');for(const f of ['backstop-benign-list','answered-gate-refires','block-surface','over-enforcement','relevance-check-gap','stale-f1-reach','option-shaped-prose','room-bind-gate-fires','intern-w1','reach-gate-stale-turn-input'])if(!w.includes(f)){console.error(f);process.exit(1)}"` exits 0 (every RCA cited)
    - `grep -cE "[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[a-z]{2,}|/home/|MindrianRooms" tests/fixtures/card-fire-replay/debug-cases.json` is 0
    - `grep -c "$(printf '\xe2\x80\x94')" tests/fixtures/card-fire-replay/debug-cases.json` is 0
  </acceptance_criteria>
  <done>Every resolved card-fire RCA has an envelope, the carve-out and harness cases exist, and on pre-phase code only the three D-07 targets are false blocks and the prose fork is a known miss.</done>
</task>

<task type="auto">
  <name>Task 2: Source (c), the two 2026-09-23 live false blocks, reconstructed from the R-D snapshot</name>
  <files>tests/fixtures/card-fire-replay/live-2026-09-23.json</files>
  <read_first>
    - tests/fixtures/card-fire-replay/live-2026-09-23.json (the empty-entries file and its meta)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md ("Root cause" section, D-08a, R-D, R-E, R-F)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-RESEARCH.md (Finding 1 transcript shapes and subject recovery; Finding 3 anchor timeline; Finding 5 D-08a overlap analysis; Pitfall 5)
    - scripts/intent-classifier.cjs (lines 1626-1668 the F.1 engine-arm subject `rendered.text`; lines 2925-2960 and 3195-3225 the F.8 subject `header + ' ' + body`)
    - lib/hmi/dial-presenter.cjs (lines 300-345 _buildDecisionGateHeader and lines 420-498 renderDial: the F.1 subject layout the paraphrase must keep)
    - `cd ~/.cache/mindrian-dev/357-raw && sha256sum -c SHA256SUMS` (must pass before any read)
  </read_first>
  <action>
    1. Inspect the snapshot locally with a scratch Node script kept in the session scratchpad (never in the repo). For session file `56924067-*.jsonl` print only structure for the window 08:47:00Z to 08:51:30Z and 09:38:00Z to 09:40:30Z: per record the timestamp, `type`, `isMeta`, `origin.kind`, a lead-tag class (tool_result, `<task-notification`, peer framing, plain), text length, and for UserPromptSubmit `hook_success` attachments whether an `[AskUserQuestion contract: shape=F.x` line appears (plain text or inside the `hookSpecificOutput` JSON line). Cross-check the two blocks against `card-fire-intercepts.log` in the same snapshot (verdict `reached-registry-gate-no-card`, empty `gate_signature`, `ran_entries: [scripts/intent-classifier.cjs]`). Recover each blocked Stop's gate subject per RESEARCH Finding 1 (F.8: header plus body from the JSON line; F.1: the text between the NAV block's Why paragraph and the AskUserQuestion contract line, trimmed to 300 chars) and compute, with gate-relevance's tokenizer rules (lowercase, split on non-alphanumerics, length >= 4, not a stopword, not numeric, GATE_BOILERPLATE_TOKENS stripped on the gate side), which tokens overlap between the preceding user text and the subject. Record only the overlap CLASS (chrome or content, and the count) in the SUMMARY, never the text.
    2. Author `live-2026-09-23-01` (mode transcript+sidechannel, expected pass, label_origin 'human', why citing the CONTEXT root cause and R-E): transcript = a human prompt, an assistant tool_use turn, a tool_result user record, an assistant record, then the hand-back record exactly as observed structurally (`isMeta: true`, `origin: {kind: 'peer'}`, a plain string with neutral paraphrased peer framing and report text), then the final assistant output with no options and no question (paraphrased, same length class); sidechannel_records = ONE fresh F.8 record (age_ms equal to the observed seconds between the F.8 mint and the Stop, times 1000, and under 120000) whose subject keeps the F.8 header plus body layout with ROOM_ALPHA-style stand-ins; the hand-back text must share with the subject exactly the same overlap class and count as measured in step 1. Set `envelope.counters` from the reconstructed retry state (0 and 0 when this was the first block).
    3. Author `live-2026-09-23-02` (mode transcript+sidechannel, expected pass, label_origin 'human', why citing D-08a and R-F): transcript = the prior assistant record, then a human-typed user record (`origin: {kind: 'human'}`, not meta) paraphrased so that it keeps at least 2 subject tokens and shares with the gate subject ONLY the token `gate`, then the final assistant output with no options and no question; sidechannel_records = ONE fresh F.1 record whose subject follows the renderDial layout (the header line ending `- REACH - decision gate`, the LOCAL / BRAIN / SIGNAL context line, the gauge line, the `Choose next reach:` prompt, body rows naming a neutral artifact such as `memory_artifact:research/sample-briefing-mirror`, the `top-N of M` footer), at most 300 chars.
    4. Replay both on the archived pre-phase commit: `node scripts/replay-card-fire.cjs --code-root pre-phase --surface both --source live --json`. Both entries must be FALSE_BLOCK with cli reason `reached-registry-gate-no-card`, 0 parity mismatches, and the command must exit 1. If a paraphrase changed the verdict or the reason, rework the paraphrase (keep the overlap class) until it reproduces; never change the expected label to fit.
    5. Leak check before commit: `grep -ciE "[A-Za-z0-9._%+-]+@|/home/|MindrianRooms|[0-9a-f]{8}-[0-9a-f]{4}-" tests/fixtures/card-fire-replay/live-2026-09-23.json` is 0; also grep the file for every room directory name under `~/MindrianRooms/` (case-insensitive, whole word) and for any person name you saw in the raw window; any hit blocks the commit until replaced.
    6. Run `node tests/test-357-replay.cjs` (its L6 live-anchor leg is now active and must pass). Commit: `git commit --only -m "test(357-05): source (c) live 2026-09-23 false blocks (F.8 hand-back, F.1 chrome overlap)" -- tests/fixtures/card-fire-replay/live-2026-09-23.json`.
  </action>
  <verify>
    <automated>node tests/test-357-replay.cjs && node scripts/replay-card-fire.cjs --code-root pre-phase --surface both --source live; test $? -eq 1 && echo "pre-phase live exit=1 as required"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-357-replay.cjs` exits 0 and its output shows L6 as passing (not SKIP)
    - `node scripts/replay-card-fire.cjs --code-root pre-phase --surface both --source live --json` reports exactly 2 entries, both FALSE_BLOCK with cli reason `reached-registry-gate-no-card`, and exits 1
    - `node -e "const e=require('./tests/fixtures/card-fire-replay/live-2026-09-23.json').entries;const a=e.find(x=>x.id==='live-2026-09-23-01'),b=e.find(x=>x.id==='live-2026-09-23-02');process.exit(a&&b&&a.envelope.sidechannel_records[0].shape==='F.8'&&b.envelope.sidechannel_records[0].shape==='F.1'?0:1)"` exits 0 (R-E: #1 is F.8)
    - `grep -ciE "[A-Za-z0-9._%+-]+@|/home/|MindrianRooms|[0-9a-f]{8}-[0-9a-f]{4}-" tests/fixtures/card-fire-replay/live-2026-09-23.json` is 0
    - `git status --short` shows no new file under the repo from the scratch inspection (scratch scripts stayed outside the repo)
    - `grep -c "$(printf '\xe2\x80\x94')" tests/fixtures/card-fire-replay/live-2026-09-23.json` is 0
  </acceptance_criteria>
  <done>The two live false blocks exist as sanitized transcript+sidechannel entries and reproduce as FALSE_BLOCK on the archived pre-phase code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R-D raw snapshot (outside the repo, mode 700) -> tracked fixture | the navigator's real transcript text must be paraphrased before it crosses |
| tracked fixtures -> Jev (plan 08 labeler run) | sources (b) and (c) are sent to Jev, so they must already be sanitized here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-05 | Information disclosure | live-2026-09-23.json, debug-cases.json | mitigate | paraphrase with neutral stand-ins; grep gates for email, home paths, MindrianRooms, UUIDs, local room names and seen person names; sanitization_statement in meta |
| T-357-11 | Information disclosure | raw snapshot inspection | mitigate | read only from `~/.cache/mindrian-dev/357-raw/` after `sha256sum -c`; scratch scripts outside the repo; SUMMARY records overlap classes, never text |
| T-357-18 | Repudiation | a paraphrase that silently changes the verdict | mitigate | pre-phase replay must reproduce FALSE_BLOCK with the same reason; the label is never changed to fit (RESEARCH Pitfall 5) |
</threat_model>

<verification>
- `node tests/test-357-replay.cjs` exits 0 with L6 active
- `node scripts/replay-card-fire.cjs --code-root pre-phase --surface both --source debug --json` shows FALSE_BLOCK only for the three D-07 targets
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
- `git diff --name-only $PLAN_BASE..HEAD -- lib hooks scripts` prints nothing (fixtures only)
</verification>

<success_criteria>
The corpus now carries every resolved RCA as a replayable envelope and the two live false blocks as sanitized, verdict-preserving entries that the pre-phase code flags as FALSE_BLOCK.
</success_criteria>

<output>
Create `.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-05-SUMMARY.md` when done (`git add -f`, then `git commit --only -- <path>`). Record: PLAN_BASE, the pre-phase outcome per debug id, any fixture corrections, the structural timeline of both live Stops (types, isMeta, origin.kind, ages, no text), the overlap class and count for each live entry, and the leak-check results.
</output>
