---
phase: 357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re
plan: 08
type: execute
wave: 5
depends_on: ["357-04", "357-05", "357-06", "357-07"]
files_modified:
  - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-JEV-LABEL-REPORT.md
  - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md
  - tests/fixtures/card-fire-replay/dogfood.json
  - tests/fixtures/card-fire-replay/debug-cases.json
  - tests/fixtures/card-fire-replay/live-2026-09-23.json
autonomous: false
requirements: [GATE357-09, GATE357-03, GATE357-04]
tags: [checkpoint, labels, navigator, part8, jev-report]

must_haves:
  truths:
    - "This is the phase's single human checkpoint: the navigator ratifies every dogfood label, rules on the R-C 09:20 case and rules on every Jev disagreement in one review (D-06, R-C, D-12)"
    - "The Jev labeler runs once, keyed, over sources (a), (b), (c) only, producing 357-JEV-LABEL-REPORT.md; a keyless run records 'unlabeled' and exits 0; no dogfood entry is sent (SPEC R3, D-11)"
    - "Jev labels are never auto-applied: a hand label changes only when the navigator rules so, and then carries label_origin 'jev' plus the ruling note (D-12)"
    - "Each confirmed dogfood row flips from label_origin 'local' to 'human'; corrected rows take the navigator's verdict (D-06)"
    - "If the navigator labels the R-C case a false block that no deterministic rule can clear, it is recorded as known_false_block with a text-dependence reason and excluded from the 0-false-block count, and a follow-on phase is opened at close-out (R-C)"
  artifacts:
    - path: ".planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-JEV-LABEL-REPORT.md"
      provides: "per-entry Noul labels (or unlabeled) and the disagreement list"
      contains: "never auto-applied"
    - path: "tests/fixtures/card-fire-replay/dogfood.json"
      provides: "ratified dogfood labels (label_origin human)"
      contains: "\"label_origin\": \"human\""
  key_links:
    - from: ".planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md"
      to: "tests/fixtures/card-fire-replay/dogfood.json"
      via: "navigator ruling applied row by row"
      pattern: "label_origin"
---

<objective>
Run the dev-time Jev labeler once over the sanitized sources, refresh the dogfood review sheet with post-fix verdicts, stop for the navigator's single review (dogfood labels, the R-C 09:20 case, Jev disagreements), then apply the rulings to the fixtures.

Purpose: D-06 (the single, non-autonomous human checkpoint), R-C (the 09:20 case labeled here), D-12 (Jev disagreements ruled, never auto-applied), SPEC R3 (labeler run), GATE357-09 (dogfood labels human-ratified).
Output: 357-JEV-LABEL-REPORT.md, ratified labels in the fixtures, a stamped review sheet.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-06-SUMMARY.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-07-SUMMARY.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md

Parallel-session rules (every task): explicit-path commits only (`git add <path>`, `git add -f` under `.planning/`, then `git commit --only -m "<msg>" -- <paths>`; peers keep files staged); record PLAN_BASE; `git status --short -- <file>` before editing and STOP on a peer diff. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, lib/core/navigation.cjs, docs/OPEN-HANDOFFS.md. No em-dashes. This plan changes no code: fixtures and phase documents only.

Part 8: the labeler is the only network call in the phase; it selects sources 238, debug and live only and refuses dogfood before building any request (plan 04). The key is read from the environment or `~/.secrets/typesafe.env` by loadKey and is never printed.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| Corpus loader, `pre-phase.json`, fixture files, loader test, `run-all-357.sh` | 01 |
| `scripts/replay-card-fire.cjs`, `tests/test-357-replay.cjs` | 02 |
| `card_fire_replay` egress profile, `data/jev-policies/card-fire-replay.json` | 03 |
| `scripts/label-card-fire-replay.cjs`, labeler refusal test, tripwire list entry | 04 |
| Authored debug and live entries | 05 |
| Dogfood extractor, sanitized dogfood entries, `357-DOGFOOD-LABELS.md` | 06 |
| D-07 and D-08a deterministic fixes with tests | 07 |
| `357-JEV-LABEL-REPORT.md`, ratified labels (the single human checkpoint) | 08 (this plan) |
| `baseline.json`, standing-gate wiring, mutation leg | 09 |
| Larry prose shrink, manifest regen, D-17 filing, requirement closure | 10 |

<tasks>

<task type="auto">
  <name>Task 1: Run the labeler once and refresh the review sheet with post-fix verdicts</name>
  <files>.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-JEV-LABEL-REPORT.md, .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md</files>
  <read_first>
    - scripts/label-card-fire-replay.cjs (flags, report path, keyless behavior)
    - scripts/extract-dogfood-stop-events.cjs (`--write-review-sheet --with-head-verdicts`)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md (current sheet)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-07-SUMMARY.md (remaining FALSE_BLOCK ids after the fixes)
  </read_first>
  <action>
    1. `node scripts/label-card-fire-replay.cjs --dry-run` first (every body built and guarded, nothing sent); it must exit 0 and report zero `refused` rows. Then run `node scripts/label-card-fire-replay.cjs` once (keyed when a key exists). Record the status counts (labeled, uncertain, error, structural, refused, unlabeled) and the disagreement ids in the SUMMARY. A keyless environment writes an `unlabeled` report and exits 0; that is legal (D-11) and the checkpoint proceeds with hand labels only.
    2. Regenerate the review sheet with `node scripts/extract-dogfood-stop-events.cjs --write-review-sheet --with-head-verdicts` so each dogfood row shows the HEAD verdict after the D-07 and D-08a fixes, and the R-C section shows the case's pre-phase and HEAD verdicts.
    3. Append to 357-DOGFOOD-LABELS.md a `## Jev disagreements (sources a, b, c)` section listing, per disagreement id from the report: source, hand label, derived Jev verdict, and the three Noul mappings, with the ruling options `keep-hand` or `accept-jev <verdict>`. No entry text.
    4. Run `node scripts/replay-card-fire.cjs --surface both` and paste only the counts line and the non-OK id list into the sheet under `## Replay on HEAD before ruling`.
    5. Commit both documents: `git add -f <report> <sheet>` then `git commit --only -m "docs(357-08): Jev label report and review sheet for the navigator checkpoint" -- <report> <sheet>`.
  </action>
  <verify>
    <automated>test -f .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-JEV-LABEL-REPORT.md && grep -c "never auto-applied" .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-JEV-LABEL-REPORT.md && grep -c "Jev disagreements" .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md</automated>
  </verify>
  <acceptance_criteria>
    - The report exists and contains `never auto-applied`; its per-entry table has no row whose id starts with `dogfood-`
    - The sheet contains a HEAD verdict column, the `R-C case` section and the `Jev disagreements` section
    - `grep -ciE "TYPESAFE_API_KEY=|Bearer " .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-JEV-LABEL-REPORT.md` is 0
    - `grep -c "$(printf '\xe2\x80\x94')" <report> <sheet>` reports 0 for both
  </acceptance_criteria>
  <done>The navigator has one sheet showing every dogfood proposal with post-fix verdicts, the R-C question and the Jev disagreements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Navigator review (D-06 dogfood labels, R-C 09:20 case, Jev disagreements)</name>
  <files>.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md (read by the navigator; not edited in this task)</files>
  <read_first>
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-JEV-LABEL-REPORT.md
  </read_first>
  <action>Stop and present the review to the navigator (D-06: this is the phase's single human checkpoint; it is never auto-approved, and workflow.auto_advance does not apply). Show the path of the review sheet, the counts (dogfood rows, proposed block/pass, R-C case HEAD verdict, Jev disagreement count or 'unlabeled'), and the how-to-answer lines. Do not edit any file and do not proceed to Task 3 until the navigator replies with the resume signal. Record the reply verbatim for Task 3.</action>
  <verify>
    <automated>grep -c "How to answer" .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md</automated>
  </verify>
  <acceptance_criteria>
    - The navigator's reply is recorded verbatim before Task 3 starts
    - No fixture file changed during this task (`git status --short -- tests/fixtures/card-fire-replay` is empty)
  </acceptance_criteria>
  <done>The navigator has ruled on the dogfood labels, the R-C case and the Jev disagreements.</done>
  <what-built>A sanitized dogfood corpus with locally proposed labels, a Jev label report for the synthetic and sanitized sources, and a review sheet that shows each dogfood row's live outcome, pre-phase verdict, post-fix HEAD verdict and proposed label, plus the R-C case and the Jev disagreements.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md`.
    2. For each dogfood row, confirm the proposed verdict (block = a genuine, unanswered, relevant fork the card should have fired; pass = no card was owed).
    3. In the R-C section, rule on the 09:20 case: `r-c: block` (a genuine relevant fork, the block was correct) or `r-c: known_false_block <reason>` (a false block that only text understanding could clear; excluded from the 0-false-block bar, follow-on phase opened).
    4. In the Jev disagreements section, rule each id `keep-hand` or `accept-jev <verdict>`.
    5. Optionally skim `357-JEV-LABEL-REPORT.md` for uncertain rows.
  </how-to-verify>
  <resume-signal>Type "approved" to ratify every proposal as written (R-C and Jev rows then default to the proposal and keep-hand), or list rulings one per line: "&lt;id&gt;: block|pass &lt;reason&gt;", "r-c: block" or "r-c: known_false_block &lt;reason&gt;", "jev &lt;id&gt;: keep-hand|accept-jev &lt;verdict&gt;".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply the rulings to the fixtures and stamp the sheet</name>
  <files>tests/fixtures/card-fire-replay/dogfood.json, tests/fixtures/card-fire-replay/debug-cases.json, tests/fixtures/card-fire-replay/live-2026-09-23.json, .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md</files>
  <read_first>
    - The navigator's resume message (the rulings, verbatim; record it in the SUMMARY)
    - tests/fixtures/card-fire-replay/dogfood.json, debug-cases.json, live-2026-09-23.json
    - scripts/card-fire-replay-corpus.cjs (validateEntry rules for known_false_block: expected pass and a non-empty reason)
  </read_first>
  <action>
    1. dogfood.json: every row the navigator confirmed (all rows on "approved") gets `label_origin: 'human'`; a corrected row gets the navigator's verdict in `expected_verdict_class`, `label_origin: 'human'` and `why` rewritten to the navigator's reason (sanitized, no names). R-C: on `known_false_block`, set `expected_verdict_class: 'pass'` and `known_false_block: {reason: '<navigator reason; must name the text dependence>'}`; on `block`, set `expected_verdict_class: 'block'`. Add `ratified_at` (ISO date) to the file meta.
    2. Jev rulings: `keep-hand` changes nothing in the fixture; `accept-jev <verdict>` sets that entry's `expected_verdict_class` to the verdict, `label_origin: 'jev'` and appends ` (navigator accepted the Jev label <date>)` to `why`. Only entries the navigator named change (D-12).
    3. Stamp the sheet: add `## Ratified` with the date, the verbatim rulings and the resulting counts (human-confirmed, corrected, R-C outcome, Jev rulings applied).
    4. Verify: `node tests/test-357-corpus-loader.cjs --dogfood-strict` exits 0; `node tests/test-357-corpus-loader.cjs` exits 0; `node scripts/replay-card-fire.cjs --surface both` prints the post-ruling counts (record them; the bar is proven in plan 09).
    5. Commit: `git add -f <sheet>` then `git commit --only -m "test(357-08): navigator-ratified dogfood labels, R-C ruling, Jev rulings (D-06, R-C, D-12)" -- tests/fixtures/card-fire-replay/dogfood.json <sheet>` plus debug-cases.json and live-2026-09-23.json only when a Jev ruling changed them.
  </action>
  <verify>
    <automated>node tests/test-357-corpus-loader.cjs --dogfood-strict && node tests/test-357-corpus-loader.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both verify commands exit 0
    - `node -e "const d=require('./tests/fixtures/card-fire-replay/dogfood.json');process.exit(d.entries.every(e=>e.label_origin==='human')&&d.meta.ratified_at?0:1)"` exits 0
    - `grep -c "## Ratified" .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-DOGFOOD-LABELS.md` is 1
    - When R-C was ruled known_false_block: `node -e "const d=require('./tests/fixtures/card-fire-replay/dogfood.json');const r=d.entries.find(e=>e.r_c_case);process.exit(r.known_false_block&&/text/i.test(r.known_false_block.reason)?0:1)"` exits 0
    - `git diff $PLAN_BASE -- tests/fixtures/card-fire-replay/debug-cases.json tests/fixtures/card-fire-replay/live-2026-09-23.json` changes only entries named in an `accept-jev` ruling
  </acceptance_criteria>
  <done>Every dogfood label is human-ratified, the R-C case carries the navigator's ruling, and only navigator-accepted Jev labels changed any hand label.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| labeler -> TypeSafe API | the phase's only network egress; sources (a)(b)(c) only |
| Jev labels -> fixture truth | a vendor model must not silently define the correct verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-01 | Information disclosure | dogfood reaching Jev | mitigate | labeler selects 238/debug/live only and refuses dogfood before request build (plan 04 test); `--dry-run` first; report has no dogfood rows (acceptance) |
| T-357-07 | Information disclosure | TYPESAFE_API_KEY in the report | mitigate | loadKey never prints; acceptance greps the report for key patterns |
| T-357-21 | Tampering | auto-applied vendor labels | mitigate | only navigator-named `accept-jev` rulings change a hand label, marked label_origin jev with the ruling note (D-12); diff acceptance on debug and live files |
| T-357-22 | Repudiation | unrecorded label decisions | mitigate | verbatim rulings in the sheet's Ratified section and the SUMMARY |
</threat_model>

<verification>
- `node tests/test-357-corpus-loader.cjs --dogfood-strict` exits 0
- `node tests/test-357-labeler-refusal.cjs` exits 0
- `git diff --name-only $PLAN_BASE..HEAD -- lib hooks scripts` prints nothing (no code change in the checkpoint plan)
</verification>

<success_criteria>
The corpus labels are final: dogfood human-ratified, the R-C case ruled, Jev disagreements ruled by the navigator, and a Jev report on file for the sanitized sources.
</success_criteria>

<output>
Create `.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-08-SUMMARY.md` when done (`git add -f`, then `git commit --only -- <path>`). Record: PLAN_BASE, labeler status counts and cost if keyed, the verbatim rulings, the R-C outcome, and the post-ruling replay counts.
</output>
