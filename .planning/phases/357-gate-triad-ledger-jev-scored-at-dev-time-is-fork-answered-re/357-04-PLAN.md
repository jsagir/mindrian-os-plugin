---
phase: 357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re
plan: 04
type: execute
wave: 3
depends_on: ["357-01", "357-03"]
files_modified:
  - scripts/label-card-fire-replay.cjs
  - tests/test-357-labeler-refusal.cjs
  - tests/test-353-tripwires.cjs
autonomous: true
requirements: [GATE357-03]
tags: [jev, labeler, part8, tripwire, dev-time]

must_haves:
  truths:
    - "scripts/label-card-fire-replay.cjs (dev-only, under scripts/, never lib/ or hooks/) asks three independent Nouls (is_fork, already_answered, relevant) per eligible entry, with the full policy stated in state.policy and each question's rule, boundary cases and criteria copied from the policy file, through the shared client and makeEgressGuard(EGRESS_PROFILES.card_fire_replay, {root}) only (SPEC R3, D-10, D-12, 356 exact_state_v1 schema)"
    - "The labeler refuses any entry whose source is dogfood or whose file lacks meta.sanitization_statement BEFORE it builds any request; dogfood entries are never loaded into the request builder at all (D-11, SPEC R3, Part 8)"
    - "Keyless, the labeler degrades to 'unlabeled' for every entry and exits 0 (D-11)"
    - "Noul >= 0.80 maps to yes, <= 0.20 to no, otherwise uncertain; the three Nouls are never compared to each other or mixed with any confidence; disagreements with hand labels are listed for a navigator ruling and never auto-applied (D-12)"
    - "tests/test-353-tripwires.cjs leg 2 bans label-card-fire-replay from hooks/ by appending exactly one entry to the existing named list constant HOOKS_BANNED_LEDGER_SCRIPTS (introduced by 356 in commit 10c69091d); 357 never creates the constant and never edits the regex (D-11, 356 D-19, orchestrator 2026-09-23)"
    - "No non-comment line under lib/ or hooks/ references api.typesafe.ai, TYPESAFE_API_KEY, jev-devtime-client or any 357 dev script (SPEC R3 acceptance)"
  artifacts:
    - path: "scripts/label-card-fire-replay.cjs"
      provides: "selectLabelable, assertLabelable, buildRequestBody, mapNoul, deriveVerdict, renderReport, main"
      exports: ["selectLabelable", "assertLabelable", "buildRequestBody", "mapNoul", "deriveVerdict", "renderReport", "main"]
      min_lines: 150
    - path: "tests/test-357-labeler-refusal.cjs"
      provides: "GATE357-03 legs: refusal before request, keyless exit 0, guard enforcement, vendor scan with negative control"
      min_lines: 120
    - path: "tests/test-353-tripwires.cjs"
      provides: "leg 2 list includes label-card-fire-replay"
      contains: "label-card-fire-replay"
  key_links:
    - from: "scripts/label-card-fire-replay.cjs"
      to: "scripts/jev-devtime-client.cjs"
      via: "require; makeEgressGuard(EGRESS_PROFILES.card_fire_replay, {root}); jev(body, {key, guard})"
      pattern: "card_fire_replay"
    - from: "scripts/label-card-fire-replay.cjs"
      to: "scripts/card-fire-replay-corpus.cjs"
      via: "loadCorpus({sources: ['238','debug','live']})"
      pattern: "loadCorpus"
---

<objective>
Build the dev-time Jev labeler for the synthetic and sanitized corpus sources (a), (b), (c), prove it refuses dogfood before any request and runs keyless with exit 0, and extend the shared tripwire so the labeler can never reach a hook.

Purpose: SPEC R3 and its acceptance (refusal test, tripwire green, no api.typesafe.ai under lib/ or hooks/, keyless exit 0), D-10, D-11, D-12. The live, keyed labeling run happens at the plan-08 checkpoint so its report reflects the final corpus.
Output: the labeler, its test, one tripwire list entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-RESEARCH.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-03-SUMMARY.md
@.claude/skills/spike-findings-MindrianOS-Plugin/references/jev-typed-decisions-api.md
@scripts/card-fire-replay-corpus.cjs
@scripts/jev-devtime-client.cjs
@data/jev-policies/card-fire-replay.json

Parallel-session rules (every task): explicit-path commits only (`git add <path>`, then `git commit --only -m "<msg>" -- <paths>`; peers keep files staged); record PLAN_BASE; `git status --short -- <file>` before editing and STOP on a peer diff; never `git add -A` / `git commit -a` / `git stash` / revert unowned diffs. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, lib/core/navigation.cjs, docs/OPEN-HANDOFFS.md. No em-dashes.

Test hygiene (the 356 pattern, restated): the developer shell exports TYPESAFE_API_KEY, so every 357 test starts with `delete process.env.TYPESAFE_API_KEY;`, a counter `let NET_ATTEMPTS = 0;` and `globalThis.fetch = function noNetwork357() { NET_ATTEMPTS += 1; throw new Error('no network in 357 tests'); };`, asserts `NET_ATTEMPTS === 0` last, and spawns children with TYPESAFE_API_KEY deleted, `HOME` = a fresh mkdtemp dir (so `~/.secrets/typesafe.env` is unreachable) and a mkdtemp preload that prints `NETWORK_ATTEMPT_357` on any fetch.

Jev contract (spike skill reference, 356-07 payload precedent): `POST https://api.typesafe.ai/v1/systemone`, body `{model: 'jev-latest', state, questions: {<id>: {type: 'noul', instructions: {question, rule, boundary_cases}, criteria: {true, false}}}}`; independent questions in ONE call run in parallel and cannot see each other; the answer is read at `json.answers[<id>]` with `type === 'noul'` and `noul` a finite number in 0..1 (no confidence); a malformed question returns 400.

The card_fire_replay profile (plan 03, 356 exact_state_v1 schema) requires: exactly the six string state keys; `question_ids` exactly is_fork, already_answered, relevant; each question exactly `{type: 'noul', instructions: {question, rule, boundary_cases}, criteria: {true, false}}`; `instructions.question` at most 400 chars; `rule`, every `boundary_cases` element and both criteria strings must be string values of the policy file; `state.policy` byte-identical to the file; the guard is built as `makeEgressGuard(EGRESS_PROFILES.card_fire_replay, { root: <repo root> })`; refusals throw with `err.code === 'EGRESS_REFUSED'` and `err.key`.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| Corpus loader, `pre-phase.json`, fixture files, loader test, `run-all-357.sh` | 01 |
| `scripts/replay-card-fire.cjs`, `tests/test-357-replay.cjs` | 02 |
| `card_fire_replay` egress profile, `data/jev-policies/card-fire-replay.json` | 03 |
| `scripts/label-card-fire-replay.cjs`, `tests/test-357-labeler-refusal.cjs`, tripwire list entry | 04 (this plan) |
| Authored debug and live entries | 05 |
| Dogfood extractor, sanitized dogfood entries, `357-DOGFOOD-LABELS.md` | 06 |
| D-07 and D-08a deterministic fixes with tests | 07 |
| `357-JEV-LABEL-REPORT.md`, ratified labels (human checkpoint) | 08 |
| `baseline.json`, standing-gate wiring, mutation leg | 09 |
| Larry prose shrink, manifest regen, D-17 filing, requirement closure | 10 |

<tasks>

<task type="auto">
  <name>Task 1: The dev-time labeler (scripts/label-card-fire-replay.cjs)</name>
  <files>scripts/label-card-fire-replay.cjs</files>
  <read_first>
    - scripts/jev-devtime-client.cjs (whole file: loadKey({env, secretsPath}), makeEgressGuard(profile, {root}), jev signature, pool, EGRESS_PROFILES.card_fire_replay fields and the exact_state_v1 checker)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-07-PLAN.md (Task 2 buildPayload, parseNoulAnswer and scoreAll: the payload and answer-parsing precedent this labeler mirrors)
    - data/jev-policies/card-fire-replay.json (policies[].policy_id, instructions, criteria)
    - scripts/card-fire-replay-corpus.cjs (loadCorpus options, entry shape, envelope modes)
    - scripts/build-section-command-ledger.cjs (lines 1-20 header wording for a dev-only, navigator-invoked, never-a-hook script; the CONC constant)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md (D-10, D-11, D-12)
  </read_first>
  <action>
    Create `scripts/label-card-fire-replay.cjs` ('use strict'; header: dev-time only, navigator-invoked, never a hook, never release.sh, never required from lib/ or hooks/; Part 8: sends only sanitized sources 238, debug and live; dogfood never leaves the machine; hyphens only). Requires only Node built-ins, `./jev-devtime-client.cjs` and `./card-fire-replay-corpus.cjs`. Implement and export:
    1. `LABELABLE_SOURCES` = frozen `['238', 'debug', 'live']`; `STRUCTURAL_REASONS` = frozen set of verdict reasons that no Noul can inform (`card-fired`, and every bounded-escape degrade reason used by classifyCardFire; read them from scripts/check-card-fire.cjs) - entries whose `expected_reason` is structural are reported as `structural (not asked)`.
    2. `assertLabelable(entry, fileMeta)`: throws an Error `refused <id>: dogfood never leaves the machine (Part 8, D-11)` when `entry.source === 'dogfood'`, and `refused <id>: file has no meta.sanitization_statement (D-11)` when `fileMeta` lacks a non-empty `sanitization_statement`. It is the FIRST statement of `buildRequestBody`.
    3. `selectLabelable(corpus)`: returns only entries whose source is in LABELABLE_SOURCES (dogfood is never passed on), each paired with its file meta (the 238 file's own meta for source 238).
    4. `projectState(entry)`: derives the six state keys from the entry and nothing else: `output_text` (direct: `envelope.output_text`; transcript modes: the text of the last assistant record), `preceding_user_text` (direct field or the text of the last user record), `gate_subject_text` (direct field, or the joined `subject` of the sidechannel records, or '' for a backstop entry), `gate_shape` (the sidechannel record shape, else `backstop`), `turns_since_gate` (the string `0` when the newest sidechannel `age_ms` <= 120000, `1+` otherwise, `0` for direct entries), `policy` (the policy file text read with `fs.readFileSync(<repo root>/<EGRESS_PROFILES.card_fire_replay.must_equal_file.policy>, 'utf8')`; that profile field is the single source of the policy path). All six values are strings (string_keys). Values longer than the profile caps are NOT truncated: the guard refuses them and the entry is reported as `refused: <guard message>` (refuse-don't-strip).
    5. `QUESTION_TEXT` = a frozen map of one fixed sentence per Noul, each at most 400 chars and naming the state keys it reads (for example is_fork: "Using the is_fork policy in `policy`, does the turn at `output_text` leave the navigator a genuine structural fork?"). `buildRequestBody(entry, fileMeta)`: `assertLabelable` first; then `{model: 'jev-latest', state: projectState(entry), questions: {is_fork, already_answered, relevant}}` where each question is `{type: 'noul', instructions: {question: QUESTION_TEXT[id], rule: <that policy's instructions>, boundary_cases: <that policy's boundary_cases array>}, criteria: <that policy's criteria object>}` copied from the parsed policy file (so every rule, boundary case and criteria string is a policy-file string, which the guard's question_strings_from_file_key check enforces).
    6. `mapNoul(v)`: number >= 0.80 -> `yes`, <= 0.20 -> `no`, other number -> `uncertain`, non-number -> `error`. Thresholds are read from the policy file's `noul_mapping`, not hardcoded twice.
    7. `deriveVerdict({is_fork, already_answered, relevant})`: `block` when is_fork is yes AND already_answered is no AND relevant is yes; `pass` when is_fork is no OR already_answered is yes OR relevant is no; otherwise `uncertain`. The three answers are only combined through the gate's own AND structure, never ranked or compared (D-12).
    8. `renderReport(rows, meta)`: markdown with a header (run timestamp, model, policy sha256, thresholds, counts per status, the statement "Jev labels are never auto-applied; every disagreement below needs a navigator ruling (D-12)"), a per-entry table (id, source, hand label, is_fork value and mapping, already_answered, relevant, derived verdict, agree yes/no/structural/unlabeled/refused), and a `## Disagreements for navigator ruling` section listing ids only. No entry text is written into the report.
    9. `main(argv)`: switch-case flags `--report <path>` (default `.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-JEV-LABEL-REPORT.md`), `--corpus-dir <dir>`, `--only <ids>`, `--dry-run` (build and guard every body, send nothing). Flow: load and select; `key = loadKey({ env: process.env, secretsPath: path.join(os.homedir(), '.secrets', 'typesafe.env') })`; with `--dry-run`, build every body and pass it through the guard (a refusal becomes a `refused: <key>` row), send nothing, write the report with the remaining rows `unlabeled`, and exit 0; when key is null, every row is `unlabeled`, write the report, print `unlabeled: no TYPESAFE key (keyless run)` and exit 0. Otherwise `guard = makeEgressGuard(EGRESS_PROFILES.card_fire_replay, { root: <repo root> })` built once; `pool(rows, 4, ...)`; one `jev(body, {key, guard})` call per entry carrying all three questions; read each answer at `json.answers[id]`, accepting it only when `type === 'noul'` and `noul` is a finite number in [0, 1] (stored exactly as returned); a non-200 status, a missing answer or an EGRESS_REFUSED throw maps that entry's questions to `error` (for a refusal, the row status is `refused: <err.key>`). Never print the key or any response text beyond the status code.
    Commit: `git add scripts/label-card-fire-replay.cjs` then `git commit --only -m "feat(357-04): dev-time Jev labeler for card-fire replay sources (a)(b)(c)" -- scripts/label-card-fire-replay.cjs`.
  </action>
  <verify>
    <automated>env -u TYPESAFE_API_KEY HOME=$(mktemp -d) node scripts/label-card-fire-replay.cjs --report "$(mktemp -d)/r.md"; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The keyless verify run prints `unlabeled: no TYPESAFE key (keyless run)` and `exit=0`
    - `git status --short -- .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-JEV-LABEL-REPORT.md` is empty after the verify run (tests write to temp paths; the real report is produced at plan 08)
    - `grep -v '^\s*//' scripts/label-card-fire-replay.cjs | grep -c "fetch("` is 0 (all network goes through the shared client's jev)
    - `grep -v '^\s*//' scripts/label-card-fire-replay.cjs | grep -c "card_fire_replay"` is at least 1
    - `grep -v '^\s*//' scripts/label-card-fire-replay.cjs | grep -c "require(.*lib/"` is 0
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/label-card-fire-replay.cjs` is 0
  </acceptance_criteria>
  <done>The labeler builds guarded three-Noul requests only for sources (a)(b)(c), refuses dogfood first, degrades to unlabeled with exit 0 when keyless, and reports disagreements without applying them.</done>
</task>

<task type="auto">
  <name>Task 2: Refusal test, guard enforcement legs, vendor scan, and the tripwire list entry</name>
  <files>tests/test-357-labeler-refusal.cjs, tests/test-353-tripwires.cjs</files>
  <read_first>
    - scripts/label-card-fire-replay.cjs (Task 1 exports)
    - scripts/jev-devtime-client.cjs (makeEgressGuard, EGRESS_PROFILES.card_fire_replay)
    - tests/test-353-tripwires.cjs (whole file: leg 2 and helpers isPureLineComment, nonCommentContains, listFilesRecursive)
    - `git status --short -- tests/test-353-tripwires.cjs` and `git log -3 --format='%h %s' -- tests/test-353-tripwires.cjs` (record both)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-01-PLAN.md (Task 2: the agreed constant name HOOKS_BANNED_LEDGER_SCRIPTS and its conversion recipe)
  </read_first>
  <action>
    1. Tripwire (D-11): run `node tests/test-353-tripwires.cjs` and record the PASS/FAIL counts first. If the file has an uncommitted diff you did not make, STOP this step, record it, and continue with step 2. `HOOKS_BANNED_LEDGER_SCRIPTS` already exists (356 introduced it in commit 10c69091d; confirm with `git log --format='%h %s' -- tests/test-353-tripwires.cjs`). Append exactly one line, `  'label-card-fire-replay',` (the only 357 script that touches Jev), after the last entry of that frozen array (currently `'irreversibility-answer-key',`), keeping the one-entry-per-line, trailing-comma style, skip if already present, and change nothing else: do NOT create the constant, do NOT touch the regex construction, the leg heading logic, legs 1 and 3, or the helpers. If the constant is somehow absent, STOP this step, record it in the SUMMARY and message jsagi-a7 (356) instead of creating it. Leg 1 writes and removes a scratch file under lib/core/; run it only when no peer session is editing lib/core/ (check `git status --short -- lib/core`).
    2. Create `tests/test-357-labeler-refusal.cjs` with the test hygiene block from the context section. Legs:
       - L1 refusal before request (SPEC R3 acceptance): build a temp corpus dir containing a dogfood file with one valid transcript-mode entry; call `buildRequestBody(dogfoodEntry, meta)` with a spy `fetchImpl` never reached; assert it throws with `refused` and the entry id in the message and the spy count is 0; call `selectLabelable(loadCorpus({corpusDir: temp}))` and assert no returned entry has source dogfood.
       - L2 missing statement: an entry whose file meta has an empty `sanitization_statement` throws `refused` naming the id.
       - L3 keyless exit 0: spawn `node scripts/label-card-fire-replay.cjs --report <tmp>/r.md` with the child env rules; exit 0, stdout contains `unlabeled`, the temp report exists and contains `unlabeled`, stderr has no `NETWORK_ATTEMPT_357`.
       - L4 guard enforcement through the real profile: `makeEgressGuard(EGRESS_PROFILES.card_fire_replay, { root: <repo root> })` returns true for a body built by `buildRequestBody` for a real 238 entry; each of these throws with `err.code === 'EGRESS_REFUSED'` and names the key in `err.key` or the message: state.policy one byte different (policy); an extra state key `room` (room); `output_text` of 4001 chars (output_text); `turns_since_gate` as the number 0 (turns_since_gate, string_keys); a question whose `type` is `choice`; a fourth question id; an `instructions.rule` edited by one character (question_strings_from_file_key). Routed through `jev(body, {key: 'k', guard, fetchImpl: spy})`, every refusal leaves the spy call count at 0.
       - L5 questions derive from the policy: for a built body, every `instructions.rule`, every `boundary_cases` element and both criteria strings deep-equal the corresponding values in the parsed policy file, and every `instructions.question` is at most 400 chars.
       - L6 mapping: `mapNoul(0.8) === 'yes'`, `mapNoul(0.2) === 'no'`, `mapNoul(0.5) === 'uncertain'`, `mapNoul(undefined) === 'error'`; `deriveVerdict` returns block only for (yes, no, yes) and pass whenever any of fork no, answered yes, relevant no.
       - L7 report hygiene: `renderReport` over two rows contains no entry text (feed rows whose envelopes carry a sentinel string and assert the sentinel is absent from the output) and contains `never auto-applied`.
       - L8 vendor scan with negative control (SPEC R3 acceptance, no runtime Jev): list every file under lib/ and hooks/ recursively (fail if 0 files); no non-comment line contains any of `api.typesafe.ai`, `TYPESAFE_API_KEY`, `jev-devtime-client`, `label-card-fire-replay`, `replay-card-fire`, `extract-dogfood-stop-events`, `card-fire-replay-corpus`; negative control: the same scan function applied to a mkdtemp file (NOT under lib/) whose code line contains `api.typesafe.ai` must report a hit.
       - Final `NET_ATTEMPTS === 0`; exit 1 on any failure.
    3. Run `node tests/test-353-tripwires.cjs` again: exit 0 with the same PASS count as step 1.
    4. Commit: `git add tests/test-357-labeler-refusal.cjs` then `git commit --only -m "test(357-04): labeler refusal, keyless exit, guard legs; tripwire list entry" -- tests/test-357-labeler-refusal.cjs tests/test-353-tripwires.cjs` (drop the tripwire path when step 1 was skipped).
  </action>
  <verify>
    <automated>node tests/test-357-labeler-refusal.cjs && node tests/test-353-tripwires.cjs && ! grep -rn "api.typesafe.ai" lib/ hooks/</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-357-labeler-refusal.cjs` exits 0 with L1-L8 passing
    - `node tests/test-353-tripwires.cjs` exits 0
    - `grep -v '^\s*//' tests/test-353-tripwires.cjs | grep -c "'label-card-fire-replay'"` is 1
    - `grep -v '^\s*//' tests/test-353-tripwires.cjs | grep -c "HOOKS_BANNED_LEDGER_SCRIPTS"` is at least 2, and `git diff $PLAN_BASE -- tests/test-353-tripwires.cjs | grep '^[-+]' | grep -v '^[-+][-+]' | wc -l` is exactly 1 and that line is `+  'label-card-fire-replay',` (the list is one entry per line with a trailing comma, so the append adds one line and removes none)
    - `grep -v '^\s*//' tests/test-353-tripwires.cjs | grep -c "/eval-icm-writers|build-section-command-ledger/"` is 0
    - `grep -rn "api.typesafe.ai" lib/ hooks/` prints nothing
    - `test ! -e lib/core/__scratch-353-tripwire-negctl.cjs` (the 353 leg-1 scratch file was removed)
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-357-labeler-refusal.cjs tests/test-353-tripwires.cjs` reports 0 for both
  </acceptance_criteria>
  <done>Refusal happens before any request, keyless runs exit 0, the guard's caps and policy pin are proven through the real profile, and the labeler is banned from hooks/ by the shared list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| labeler -> TypeSafe API | sanitized sources (a)(b)(c) only; dogfood must never cross |
| dev scripts -> user runtime (lib/, hooks/) | a vendor call in a hook would put Jev in every user's turn loop (2026-09-17 ruling) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-01 | Information disclosure | dogfood text reaching Jev | mitigate | selectLabelable drops source dogfood; assertLabelable is the first statement of buildRequestBody and throws naming the id; L1 proves zero fetch calls |
| T-357-02 | Information disclosure | policy or question smuggling | mitigate | questions copied from the policy file (L5 substring proof); state.policy pinned by must_equal_file (L4) |
| T-357-06 | Elevation of privilege | hooks/ or lib/ reaching vendor code | mitigate | HOOKS_BANNED_LEDGER_SCRIPTS gains label-card-fire-replay; L8 scans lib/ and hooks/ for seven tokens with a negative control; `! grep -rn api.typesafe.ai lib/ hooks/` |
| T-357-07 | Information disclosure | TYPESAFE_API_KEY in output or report | mitigate | key never printed; report holds ids and numbers only (L7 sentinel check); tests delete the key and point HOME at a temp dir |
| T-357-08 | Tampering | shared tree during tripwire leg 1 | mitigate | run leg 1 only with a clean `git status --short -- lib/core`; assert the scratch file is gone |
| T-357-SC | Tampering | package installs | accept | none (Node built-ins only) |
</threat_model>

<verification>
- `node tests/test-357-labeler-refusal.cjs && node tests/test-353-tripwires.cjs` exit 0
- `! grep -rn "api.typesafe.ai" lib/ hooks/`
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
</verification>

<success_criteria>
Jev is available to Phase 357 only as a dev-time teacher for sanitized sources, the refusal and keyless behaviors are tested, and the hooks/ ban covers the new script.
</success_criteria>

<output>
Create `.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-04-SUMMARY.md` when done (`git add -f`, then `git commit --only -- <path>`). Record: PLAN_BASE, the tripwire before/after counts, the commit that introduced HOOKS_BANNED_LEDGER_SCRIPTS (expected 10c69091d), and the list's final contents.
</output>
