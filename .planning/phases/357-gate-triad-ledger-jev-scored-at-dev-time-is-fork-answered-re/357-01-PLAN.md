---
phase: 357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/card-fire-replay-corpus.cjs
  - tests/fixtures/card-fire-replay/pre-phase.json
  - tests/fixtures/card-fire-replay/debug-cases.json
  - tests/fixtures/card-fire-replay/live-2026-09-23.json
  - tests/fixtures/card-fire-replay/dogfood.json
  - tests/test-357-corpus-loader.cjs
  - tests/run-all-357.sh
autonomous: true
requirements: [GATE357-01, GATE357-08]
tags: [card-fire, replay, corpus, fixtures, test-scaffolding]

must_haves:
  truths:
    - "Execution starts only after Phase 354 has closed (D-15): the first task stops with 'held for Phase 354' when 354-16-SUMMARY.md is absent"
    - "The pre-phase commit (the last commit before any 357 runtime change) is recorded once, with sha256 digests of the runtime files, so every later --code-root pre-phase run replays exactly the code that produced the 2026-09-23 false blocks (D-03, R-I)"
    - "One loader reads four corpus sources: the 238 corpus IN PLACE through an adapter (never copied, D-01) plus debug-cases.json, live-2026-09-23.json and dogfood.json, each carrying meta.sanitization_statement (D-01, D-05)"
    - "Every entry is validated against the D-02 shape (id, source, envelope, expected_verdict_class, label_origin, why, optional known_miss / known_false_block per R-C) and the R-I envelope-mode rule: live and dogfood entries must use a transcript mode, direct-mode entries must carry sidechannel_health and reach_corroborated and must not carry gate_is_fresh"
    - "The 238 adapter emits 18 per-state entries: Half A x state 1 (healthy, uncorroborated) expected pass, Half B x states 2 and 3 expected block (RESEARCH Finding 6)"
    - "tests/run-all-357.sh is written once with every 357 leg pre-declared behind run_if guards, so later plans only add files (D-14)"
  artifacts:
    - path: "scripts/card-fire-replay-corpus.cjs"
      provides: "loadCorpus, adapt238, validateEntry, readPrePhase, SOURCES, LABEL_ORIGINS, ENVELOPE_MODES"
      exports: ["loadCorpus", "adapt238", "validateEntry", "readPrePhase", "SOURCES", "LABEL_ORIGINS", "ENVELOPE_MODES", "CORPUS_DIR"]
    - path: "tests/fixtures/card-fire-replay/pre-phase.json"
      provides: "pre_phase_sha plus runtime file digests"
      contains: "pre_phase_sha"
    - path: "tests/test-357-corpus-loader.cjs"
      provides: "GATE357-01 loader assertions plus --dogfood-strict mode (GATE357-09)"
      min_lines: 120
    - path: "tests/run-all-357.sh"
      provides: "Phase 357 aggregator, written once"
      contains: "run_if \"357:"
  key_links:
    - from: "scripts/card-fire-replay-corpus.cjs"
      to: "tests/fixtures/card-fire-corpus-238.json"
      via: "adapt238 reads the 238 file in place"
      pattern: "card-fire-corpus-238\\.json"
    - from: "tests/test-357-corpus-loader.cjs"
      to: "scripts/card-fire-replay-corpus.cjs"
      via: "require + loadCorpus()"
      pattern: "card-fire-replay-corpus"
---

<objective>
Lay the Phase 357 corpus foundation: a single loader for the four-source replay corpus, the empty-entries fixture files with their sanitization meta, the recorded pre-phase commit, the loader test, and the phase aggregator.

Purpose: SPEC R1 (one versioned corpus, 4 sources, loader test) and the Wave 0 scaffolding in 357-VALIDATION.md. Every later plan (harness, labeler, extractor, fixes) consumes `scripts/card-fire-replay-corpus.cjs`; nothing re-parses fixture files on its own.
Output: one loader module, five fixture files, one test, one aggregator. No runtime (lib/, hooks/, check-card-fire) change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-SPEC.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-RESEARCH.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-PATTERNS.md
@.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-VALIDATION.md
@tests/fixtures/card-fire-corpus-238.json
@tests/test-238-card-fire-corpus.cjs
@tests/run-all-238.sh
@tests/run-all-354.sh

Parallel-session rules for EVERY task in this phase (D-11 of 356, D-15 of 357):
- Commit only by explicit path: `git add <new path>` (use `git add -f <path>` for anything under `.planning/`), then `git commit --only -m "<msg>" -- <path> [<path> ...]`. Peer sessions keep files staged in the shared index; a plain `git commit` would sweep them in. Never `git add -A`, never `git commit -a`, never `git stash`, never revert or checkout a diff you did not make.
- Record `PLAN_BASE=$(git rev-parse HEAD)` at plan start and write it into the SUMMARY.
- Before editing any file, run `git status --short -- <that file>`. If it shows a diff you did not make, STOP and report the peer diff instead of editing.
- Never edit (read-only at most): lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, lib/core/navigation.cjs, docs/OPEN-HANDOFFS.md.
- No em-dashes anywhere (hyphens only). CJS only, Node built-ins only, switch-case argv parsing.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `scripts/card-fire-replay-corpus.cjs` (loader, 238 adapter, validator) | 01 (this plan) |
| `tests/fixtures/card-fire-replay/pre-phase.json`, empty-entries `debug-cases.json` / `live-2026-09-23.json` / `dogfood.json` | 01 (this plan) |
| `tests/test-357-corpus-loader.cjs`, `tests/run-all-357.sh` | 01 (this plan) |
| `scripts/replay-card-fire.cjs`, `tests/test-357-replay.cjs` | 02 (mutation leg: 09) |
| `scripts/jev-devtime-client.cjs` `card_fire_replay` profile, `data/jev-policies/card-fire-replay.json` | 03 |
| `scripts/label-card-fire-replay.cjs`, `tests/test-357-labeler-refusal.cjs`, tripwire list entry | 04 |
| Authored `debug-cases.json` and `live-2026-09-23.json` entries | 05 |
| `scripts/extract-dogfood-stop-events.cjs`, sanitized `dogfood.json`, `357-DOGFOOD-LABELS.md` | 06 |
| D-07 `'harness'` source class and D-08a F.1 chrome strip, with their tests | 07 |
| `357-JEV-LABEL-REPORT.md`, ratified labels (the single human checkpoint) | 08 |
| `tests/fixtures/card-fire-replay/baseline.json`, `run-all-238.sh` replay leg, mutation leg | 09 |
| Larry prose shrink, regenerated `data/harness-manifest.json`, D-17 filing, requirement closure | 10 |

<tasks>

<task type="auto">
  <name>Task 1: 354 gate, pre-phase anchor, corpus loader and empty-entries fixture files</name>
  <files>scripts/card-fire-replay-corpus.cjs, tests/fixtures/card-fire-replay/pre-phase.json, tests/fixtures/card-fire-replay/debug-cases.json, tests/fixtures/card-fire-replay/live-2026-09-23.json, tests/fixtures/card-fire-replay/dogfood.json</files>
  <read_first>
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md (D-01, D-02, D-03, D-15 and the whole post_research_rulings block, especially R-C, R-D, R-I)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-RESEARCH.md (Finding 6, Pattern 1, Pitfall 2)
    - tests/fixtures/card-fire-corpus-238.json (top-level `{meta, entries}`, entry fields `id, source, expect_fire, why, text`, and the meta.sanitization_statement wording to mirror)
    - tests/test-238-card-fire-corpus.cjs (lines 60-130: the three side-channel states and `buildTurn`)
    - scripts/check-card-fire.cjs (lines 1482-1600: which envelope fields deriveTurnSignals accepts directly; note gate_is_fresh is NOT accepted)
  </read_first>
  <action>
    1. 354 gate (D-15): run `ls .planning/phases/354-*/354-16-SUMMARY.md`. If it does not exist, STOP the whole plan and report `held for Phase 354 (D-15): 354-16-SUMMARY.md absent`. Also run `cd ~/.cache/mindrian-dev/357-raw && sha256sum -c SHA256SUMS` (R-D snapshot); if it fails or the dir is missing, STOP and report it (the dogfood and live evidence would be gone).
    2. Pre-phase anchor (D-03, R-I): set `PRE=$(git rev-parse HEAD)`. Confirm `git status --short -- lib/hmi/turn-text.cjs scripts/check-card-fire.cjs lib/core/gate-relevance.cjs lib/core/card-fire-sidechannel.cjs lib/mcp/stop-gate-handler.cjs lib/hmi/dial-presenter.cjs data/render-coverage-registry.json` prints nothing, and `git log --format=%s $PRE -- lib/hmi/turn-text.cjs scripts/check-card-fire.cjs lib/core/gate-relevance.cjs | grep -c '357-'` is 0 (no 357 runtime change yet). Write `tests/fixtures/card-fire-replay/pre-phase.json` as `{ "pre_phase_sha": PRE, "recorded_at": <ISO now>, "why": "<one sentence: the last commit before any Phase 357 runtime change; --code-root pre-phase archives it>", "runtime_files": { "<path>": "<sha256 hex of git show PRE:path>" } }` for those seven paths (compute each digest with `git show "$PRE:<path>" | sha256sum`).
    3. Create `scripts/card-fire-replay-corpus.cjs` (`'use strict'`, header comment: dev-time only, never required from lib/ or hooks/, reads fixtures only, no network, hyphens only). Exports:
       - `CORPUS_DIR` = `path.join(__dirname, '..', 'tests', 'fixtures', 'card-fire-replay')`; `CORPUS_238_PATH` = the 238 file.
       - `SOURCES` = frozen `['238', 'debug', 'live', 'dogfood']`; `SOURCE_FILES` = frozen map `{debug: 'debug-cases.json', live: 'live-2026-09-23.json', dogfood: 'dogfood.json'}`; `LABEL_ORIGINS` = frozen `['hand', 'jev', 'local', 'human']`; `ENVELOPE_MODES` = frozen `['direct', 'transcript', 'sidechannel', 'transcript+sidechannel']`.
       - `adapt238(corpus)`: for every Half A entry (`expect_fire === false`) emit ONE entry `{id: '238:' + e.id + ':s1', source: '238', envelope: {mode: 'direct', output_text: e.text, ran_entries: [], sidechannel_health: 'healthy', reach_corroborated: false}, expected_verdict_class: 'pass', label_origin: 'hand', why: e.why}`; for every Half B entry emit TWO entries, `:s2` with `sidechannel_health: 'healthy', reach_corroborated: true` and `:s3` with `sidechannel_health: 'unavailable', reach_corroborated: false`, both `expected_verdict_class: 'block'`. Never copy or rewrite the 238 file (D-01).
       - `validateEntry(entry, fileMeta)` returns an array of error strings (empty = valid): id is a non-empty string; source in SOURCES; `expected_verdict_class` in {block, pass}; `label_origin` in LABEL_ORIGINS; `why` non-empty string; `envelope.mode` in ENVELOPE_MODES; if source is live or dogfood, mode must contain `transcript` (RESEARCH Pitfall 2, R-I); mode `direct` requires string `sidechannel_health` and boolean `reach_corroborated` and forbids `gate_is_fresh` (deriveTurnSignals ignores it, so the fixture would lie); modes containing `transcript` require a non-empty `envelope.transcript` array of records `{type: 'user'|'assistant', message: {role, content}, isMeta?, origin?}` and forbid `output_text`, `preceding_user_text`, `preceding_user_text_source`; modes containing `sidechannel` require a non-empty `envelope.sidechannel_records` array of `{entry, shape, age_ms, subject}` (age_ms a non-negative number) and forbid `ran_entries`; optional `envelope.steps` is an array of prior-step envelopes each validated by the same mode rules; optional `envelope.counters` is `{retry: int>=0, session: int>=0}`; `known_miss` (object with non-empty `reason`) only when expected is block; `known_false_block` (object with non-empty `reason`, R-C) only when expected is pass; `envelope_partial` boolean allowed only on dogfood; `fileMeta.sanitization_statement` is a non-empty string; no string anywhere in the entry contains U+2014.
       - `loadCorpus({corpusDir, include238, sources})` returns `{entries, files, errors}`: reads the 238 file through `adapt238` (when include238 is not false) and each SOURCE_FILES file present in corpusDir (default CORPUS_DIR); every file entry's `source` must equal its file's source key; duplicate ids are errors; `files[source] = {path, meta, count}`. It never throws on a malformed file: it records an error string and continues.
       - `readPrePhase(corpusDir)` returns the parsed pre-phase.json or null.
    4. Create the three empty-entries fixture files `debug-cases.json`, `live-2026-09-23.json`, `dogfood.json` as `{ "meta": { "phase": "357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re", "source": "<debug|live|dogfood>", "schema": "D-02 entry shape; see scripts/card-fire-replay-corpus.cjs validateEntry", "sanitization_statement": "<source-specific statement, below>" }, "entries": [] }`. Statements (hyphens only): debug = every value is authored for this corpus from the structural shape recorded in `.planning/debug/resolved/` RCAs, no real person or organization name, no room or venture content; live = the two 2026-09-23 entries are paraphrased from a local-only snapshot (`~/.cache/mindrian-dev/357-raw/`, R-D), keeping only record structure (isMeta, origin.kind, record order), gate shape, reach age and the token-overlap class that decides relevance, no third-party names, no room or venture specifics; dogfood = entries are built locally from the navigator's own dev-session transcripts (R-D snapshot), sanitized before commit with neutral stand-in tokens for any name, room, venture or path, verdict-preserving per RESEARCH Pitfall 5, and never sent to Jev (D-05, D-11).
    5. Commit by explicit path: `git add scripts/card-fire-replay-corpus.cjs tests/fixtures/card-fire-replay/*.json` then `git commit --only -m "feat(357-01): replay corpus loader, 238 adapter, pre-phase anchor" -- scripts/card-fire-replay-corpus.cjs tests/fixtures/card-fire-replay/pre-phase.json tests/fixtures/card-fire-replay/debug-cases.json tests/fixtures/card-fire-replay/live-2026-09-23.json tests/fixtures/card-fire-replay/dogfood.json`.
  </action>
  <verify>
    <automated>node -e "const c=require('./scripts/card-fire-replay-corpus.cjs');const r=c.loadCorpus({});if(r.errors.length){console.error(r.errors);process.exit(1)}const n=r.entries.filter(e=>e.source==='238').length;if(n!==18){console.error('238 entries',n);process.exit(1)}const p=c.readPrePhase();if(!p||!/^[0-9a-f]{40}$/.test(p.pre_phase_sha))process.exit(1);console.log('ok',n,p.pre_phase_sha)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ok 18 <40-hex sha>` and exits 0
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('tests/fixtures/card-fire-replay/pre-phase.json','utf8')).runtime_files['scripts/check-card-fire.cjs'].length)"` prints 64
    - `git diff --stat tests/fixtures/card-fire-corpus-238.json` is empty (the 238 file is read in place, D-01)
    - `grep -v '^\s*//' scripts/card-fire-replay-corpus.cjs | grep -c "require('\.\./lib\|require(path.join(.*'lib'"` is 0 (the loader does not load runtime code)
    - `node -e "const c=require('./scripts/card-fire-replay-corpus.cjs');const e=c.validateEntry({id:'x',source:'live',envelope:{mode:'direct',output_text:'a',sidechannel_health:'healthy',reach_corroborated:false},expected_verdict_class:'pass',label_origin:'hand',why:'w'},{sanitization_statement:'s'});process.exit(e.length>0?0:1)"` exits 0 (a direct-mode live entry is rejected)
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/card-fire-replay-corpus.cjs tests/fixtures/card-fire-replay/*.json` reports 0 for every file
  </acceptance_criteria>
  <done>The loader returns 18 adapted 238 entries with zero errors, the pre-phase sha and digests are on disk, and three empty-entries source files carry their sanitization statements.</done>
</task>

<task type="auto">
  <name>Task 2: Loader test and the run-all-357.sh aggregator (written once)</name>
  <files>tests/test-357-corpus-loader.cjs, tests/run-all-357.sh</files>
  <read_first>
    - scripts/card-fire-replay-corpus.cjs (the module Task 1 wrote; its exports are the test's only interface)
    - tests/test-238-card-fire-corpus.cjs (test file style: 'use strict', node:assert/strict, ok(desc, fn) counter, header comment)
    - tests/run-all-354.sh (run with exit 77 = SKIPPED ENV GAP, the em-dash guard loop, final exit) and tests/run-all-238.sh (run_if)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-VALIDATION.md (requirement -> test map, known pre-existing reds)
  </read_first>
  <action>
    1. Create `tests/test-357-corpus-loader.cjs` ('use strict', node:assert/strict, `ok(desc, fn)` counter, header comment naming GATE357-01 and GATE357-09 and stating the expected-red window below). Default mode legs:
       - L1 structure: `loadCorpus({})` returns zero errors; every entry passes `validateEntry` with its file meta; ids are unique.
       - L2 238 adapter: exactly 18 entries with source 238; 10 end in `:s1` with expected pass; 4 end in `:s2` and 4 in `:s3`, all expected block; the 238 file's own sha256 is unchanged by loading (read it before and after).
       - L3 sources and floor (SPEC R1 acceptance): all four sources have at least one entry; total entries >= 45; every non-238 file has a non-empty `meta.sanitization_statement`.
       - L4 mode rule: every live and dogfood entry has an `envelope.mode` containing `transcript`; no direct-mode entry carries `gate_is_fresh`.
       - L5 known lists: every `known_miss` entry has expected block and a `reason` that contains the word `text` (text-dependence, SPEC acceptance); every `known_false_block` entry has expected pass and a non-empty `reason`; print both id lists.
       - L6 no em-dash in any fixture file.
       `--dogfood-strict` mode (GATE357-09) adds: dogfood has >= 20 entries; every dogfood entry has `label_origin === 'human'`; every dogfood entry either lacks `envelope_partial` or has it false; the dogfood file meta carries `verdict_preservation: 'checked'` (written by the plan-06 extractor after its raw-vs-sanitized check).
       State in the header comment: L3 and L4 are RED until plan 357-06 lands dogfood entries, and `--dogfood-strict` is RED until the plan 357-08 checkpoint ratifies labels; this is the 238-07 tests-first precedent, not a defect.
    2. Create `tests/run-all-357.sh` modeled on run-all-354.sh (`set -uo pipefail`, ROOT cd, PASS/FAIL/SKIP, `run` honoring exit 77, `run_if label file cmd...`). Header comment: written ONCE in 357-01, no later 357 plan edits it; legs guarded on `tests/fixtures/card-fire-replay/baseline.json` activate at the plan-09 bar gate; hyphens only. Legs, in order:
       - `run_if "357: corpus loader (GATE357-01)" tests/test-357-corpus-loader.cjs node tests/test-357-corpus-loader.cjs`
       - `run_if "357: replay harness and parity (GATE357-02, GATE357-06)" tests/test-357-replay.cjs node tests/test-357-replay.cjs`
       - `run_if "357: labeler refusal and vendor tripwire (GATE357-03)" tests/test-357-labeler-refusal.cjs node tests/test-357-labeler-refusal.cjs`
       - `run_if "357: harness source class (GATE357-04)" tests/test-357-harness-source.cjs node tests/test-357-harness-source.cjs`
       - `run_if "357: F.1 dial chrome strip (GATE357-05)" tests/test-357-f1-chrome.cjs node tests/test-357-f1-chrome.cjs`
       - `run_if "357: dogfood strict (GATE357-09)" tests/fixtures/card-fire-replay/baseline.json node tests/test-357-corpus-loader.cjs --dogfood-strict`
       - `run_if "357: mutation, reverted fix fails (GATE357-08)" tests/fixtures/card-fire-replay/baseline.json node tests/test-357-replay.cjs --mutation`
       - `run_if "357: shared tripwires (D-11)" tests/test-353-tripwires.cjs node tests/test-353-tripwires.cjs`
       - regression legs with plain `run`: tests/test-209-primary-sidechannel.cjs, tests/test-238-card-fire-corpus.cjs, tests/test-198-stop-gate-retry-ceiling.test.cjs, tests/test-larry-handoff-seam.cjs, tests/test-gate-native-fire-w1.cjs, tests/test-larry-voice-mark-182.cjs (all six measured green at planning time under a hermetic env; do NOT add tests/test-card-fire-relevance-gate.cjs or tests/test-ga4-card-fire-e2e-179.cjs, which carry pre-existing reds per R-J)
       - vendor gate leg: `run "357: no api.typesafe.ai under lib/ or hooks/" bash -c '! grep -rn "api.typesafe.ai" lib/ hooks/'`
       - em-dash guard over `PHASE_357_SURFACES` = scripts/card-fire-replay-corpus.cjs, scripts/replay-card-fire.cjs, scripts/label-card-fire-replay.cjs, scripts/extract-dogfood-stop-events.cjs, scripts/jev-devtime-client.cjs, data/jev-policies/card-fire-replay.json, lib/hmi/turn-text.cjs, scripts/check-card-fire.cjs, lib/core/gate-relevance.cjs, agents/larry-extended.md, skills/larry-personality/SKILL.md, tests/test-353-tripwires.cjs, tests/run-all-238.sh, plus `find tests -maxdepth 1 -name 'test-357-*.cjs'`, `find tests/fixtures/card-fire-replay -maxdepth 1 -name '*.json'`, and `find .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re -maxdepth 1 -name '357-*.md'`; a missing file is skipped.
       - Final line `exit $(( FAIL > 0 ? 1 : 0 ))`. `chmod +x tests/run-all-357.sh`.
    3. Run `node tests/test-357-corpus-loader.cjs` and record in the SUMMARY that L1, L2, L5, L6 pass and L3 / L4 fail only on the missing debug, live and dogfood entries (expected red window). Run each of the six regression legs once and record PASS/FAIL; if any is red at this point, remove it from the aggregator, record it as a pre-existing red in the SUMMARY, and do not fix it (R-J).
    4. Commit: `git add tests/test-357-corpus-loader.cjs tests/run-all-357.sh` then `git commit --only -m "test(357-01): corpus loader test and run-all-357 aggregator" -- tests/test-357-corpus-loader.cjs tests/run-all-357.sh`.
  </action>
  <verify>
    <automated>bash -n tests/run-all-357.sh && node tests/test-357-corpus-loader.cjs; echo "loader-exit=$? (L3/L4 expected red until 357-06)"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/run-all-357.sh` exits 0 and `test -x tests/run-all-357.sh`
    - `grep -v '^#' tests/run-all-357.sh | grep -c 'run_if "357:'` is 8
    - `grep -v '^#' tests/run-all-357.sh | grep -c 'test-card-fire-relevance-gate\|test-ga4-card-fire-e2e-179'` is 0
    - `node tests/test-357-corpus-loader.cjs` output shows L1, L2, L5 and L6 as passing and names L3 or L4 as the only failing legs
    - `grep -c "dogfood-strict" tests/test-357-corpus-loader.cjs` is at least 2
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-357-corpus-loader.cjs tests/run-all-357.sh` reports 0 for both
  </acceptance_criteria>
  <done>The loader test encodes every SPEC R1 acceptance check plus the dogfood-strict mode, and the aggregator pre-declares every 357 leg once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local raw evidence (`~/.cache/mindrian-dev/357-raw/`) -> tracked repo fixtures | real transcript text must never cross into a committed file unsanitized |
| test process -> shared working tree | peer sessions hold staged and unstaged diffs in the same tree and index |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-05 | Information disclosure | tests/fixtures/card-fire-replay/*.json | mitigate | every source file must carry meta.sanitization_statement; validateEntry rejects a file without one; this plan commits zero entries, only statements |
| T-357-08 | Tampering | shared index and working tree | mitigate | `git commit --only -- <paths>` for every commit; preflight `git status --short` per file; the do-not-touch list is read-only |
| T-357-13 | Tampering | tests/fixtures/card-fire-corpus-238.json | mitigate | adapter reads it in place; L2 asserts its sha256 is unchanged by loading |
| T-357-SC | Tampering | package installs | accept | no packages installed in Phase 357 (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `node -e "require('./scripts/card-fire-replay-corpus.cjs').loadCorpus({})"` returns zero errors
- `bash -n tests/run-all-357.sh` exits 0
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
</verification>

<success_criteria>
The corpus has one loader that reads the 238 corpus in place and three sanitization-stamped source files; the pre-phase commit is pinned with digests; the loader test and the aggregator exist before any harness or fix code, with the red window documented.
</success_criteria>

<output>
Create `.planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-01-SUMMARY.md` when done and commit it with `git add -f <path>` then `git commit --only -- <path>`. Record: PLAN_BASE, pre_phase_sha, the 354 gate result, the R-D `sha256sum -c` result, the regression-leg PASS/FAIL results, and the loader test's red legs.
</output>
