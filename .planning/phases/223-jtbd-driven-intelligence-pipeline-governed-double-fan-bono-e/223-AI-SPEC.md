# AI-SPEC — Phase 223: jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e

> AI design contract generated by `/gsd-ai-integration-phase`. Consumed by `gsd-planner` and `gsd-eval-auditor`.
> Locks framework selection, implementation guidance, and evaluation strategy before planning begins.

---

## 1. System Classification

**System Type:** Hybrid (Multi-Agent governed debate + JTBD-oriented research/intelligence meta-orchestrator)

**Description:**
Two related surfaces sharing one research-ingestion + graph-close-the-loop spine. `/mos:bono`
(evolved from its shipped Phase-164 form) runs a governed, six-hat research debate with
per-persona web research and disconfirming-evidence discipline, concluding in a Minto/MECE
narrative synthesis version-tracked via a SUPERSEDES chain on re-run. `/mos:intel-pipeline`
(net-new) is a JTBD-oriented meta-orchestrator running calibrate -> decompose -> fan research
-> compute -> consolidate -> synthesize -> write-to-graph against any room. Used by the
navigator (Larry's human) to turn an ad-hoc, manually-stitched multi-command research
workflow into one governed, repeatable pass. "Good" = every claim/opportunity/open_question
node lands `proposed` (never auto-confirmed), every persona's assertions trace to its own
wired sources, re-runs version-chain rather than silently duplicate, and the navigator is
gated (F.1/F.5) at every point real judgment or real spend is committed.

**Critical Failure Modes:**
1. A claim, opportunity, or conclusion node gets auto-confirmed (bypasses `navigation.confirmNode(byUser)`) -- breaks Canon Part 9's human-only truth-promotion invariant.
2. Persona/hat research content, or any conclusion/claim text, egresses to the Brain -- breaks Canon Part 8 (only generic framework/domain handles may cross that boundary).
3. A bono re-run silently duplicates instead of version-chaining (zero or multiple `SUPERSEDES` edges where exactly one is expected), so the navigator can't tell which conclusion is current.
4. Intel-pipeline's calibrate-then-fan sequence reinforces JTBD state without disclosure -- the JTBD orients the search, the search's findings are never checked against JTBD for drift (see Section 6, Guardrail G-2; verified real, not hypothetical, in the 2026-07-15 causal-loop pass -- 223-SPEC.md Addendum).
5. A persona asserts a claim beyond its own wired `INFORMS` source set -- breaks the heterogeneity/disconfirming-evidence discipline Requirement 1 exists to enforce, silently degrading the debate into six voices agreeing with one lens.

---

## 1b. Domain Context

> Researched by `gsd-domain-researcher`. Grounds the evaluation strategy in domain expert knowledge.

**Industry Vertical:** <!-- healthcare | legal | finance | customer service | education | developer tooling | e-commerce | etc. -->

**User Population:** <!-- who uses this system and in what context -->

**Stakes Level:** <!-- Low | Medium | High | Critical -->

**Output Consequence:** <!-- what happens downstream when the AI output is acted on -->

### What Domain Experts Evaluate Against

<!-- Domain-specific rubric ingredients — in practitioner language, not AI jargon -->
<!-- Format: Dimension / Good (expert accepts) / Bad (expert flags) / Stakes / Source -->

### Known Failure Modes in This Domain

<!-- Domain-specific failure modes from research — not generic hallucination, but how it manifests here -->

### Regulatory / Compliance Context

<!-- Relevant regulations or constraints — or "None identified" if genuinely none apply -->

### Domain Expert Roles for Evaluation

| Role | Responsibility |
|------|---------------|
| <!-- e.g., Senior practitioner --> | <!-- Dataset labeling / rubric calibration / production sampling --> |

---

## 2. Framework Decision

**Selected Framework:** In-house extension of `lib/core/bono/*` (Claude-native orchestration on the shipped connector spine)

**Version:** `@modelcontextprotocol/sdk` ^1.29.0, `zod` ^3.25.76, CJS, Node >=22.5.0 (all already-pinned versions from `package.json` — zero new entries)

**Rationale:**
90% of the target architecture already ships and is verified on disk: all 11 reuse-map engines
(`runCellFanout`, `runDebate`, `graph-derivation.runDerivation`, `findings-wirer.wireAccept/wireReject`,
`expert-library.assembleTeam`, `research-context-extractor.extractContext`, `source-lens-driver.runSourceLens`,
`dispatch-optimizer.planDispatch`, `navigation.cjs` + `navigation/edges.cjs`, `narrative-schema.cjs`) exist
and are exercised by Phase 164's shipped `commands/bono.md` (COMPLETE, `run-all-164.sh` 20/20 green). This
phase's net-new work is exactly 4 small files (`hat-governance.cjs`, `persona-research.cjs`,
`commands/intel-pipeline.md`, `skills/intel-pipeline/SKILL.md`) extending that in-house spine — not a
system built from scratch. Every external framework in the matrix is eliminated three times over: (1) it
violates the zero-new-npm-dependency hard constraint (223-SPEC.md Constraints); (2) it introduces a second
egress path Canon Part 8's graph boundary forbids; (3) it duplicates the "one governed reach path / no
second selection brain" connector-spine rule this codebase already enforces structurally
(`dispatchSensors -> decide() -> resolver`).

**Alternatives Considered:**

| Framework | Ruled Out Because |
|-----------|------------------|
| Claude Agent SDK (Anthropic) | Only external option aligned on provider (Claude) + MCP-heavy + safety-first without a model swap — but still a net-new dependency that duplicates the connector spine this plugin already provides while running *inside* that same Agent runtime; circular. |
| LangChain / LlamaIndex / CrewAI / LangGraph / AutoGen / Haystack | All net-new npm dependencies (hard constraint violation); none exist anywhere in this repo's dependency manifest today; each imports its own orchestration/agent-loop brain that would compete with, not extend, the shipped connector spine. |

**Vendor Lock-In Accepted:** No — the in-house extension has zero framework lock-in beyond what's already shipped (MCP SDK is a protocol standard, not a vendor-specific agent framework); this was a factor favoring the selection, not a trade-off accepted despite it.

---

## 3. Framework Quick Reference

> Fetched from official docs by `gsd-ai-researcher`. Distilled for this specific use case.

### Installation
```bash
# Install command(s)
```

### Core Imports
```python
# Key imports for this use case
```

### Entry Point Pattern
```python
# Minimal working example for this system type
```

### Key Abstractions
<!-- Framework-specific concepts the developer must understand before coding -->
| Concept | What It Is | When You Use It |
|---------|-----------|-----------------|
| | | |

### Common Pitfalls
<!-- Gotchas specific to this framework and system type — from docs, issues, and community reports -->
1.
2.
3.

### Recommended Project Structure
```
project/
├── # Framework-specific folder layout
```

---

## 4. Implementation Guidance

**Model Configuration:**
<!-- Which model(s), temperature, max tokens, and other key parameters -->

**Core Pattern:**
<!-- The primary implementation pattern for this system type in this framework -->

**Tool Use:**
<!-- Tools/integrations needed and how to configure them -->

**State Management:**
<!-- How state is persisted, retrieved, and updated -->

**Context Window Strategy:**
<!-- How to manage context limits for this system type -->

---

## 4b. AI Systems Best Practices

> Written by `gsd-ai-researcher`. Cross-cutting patterns every developer building AI systems needs — independent of framework choice.

### Structured Outputs with Pydantic

<!-- Framework-specific Pydantic integration pattern for this use case -->
<!-- Include: output model definition, how the framework uses it, retry logic on validation failure -->

```python
# Pydantic output model for this system type
```

### Async-First Design

<!-- How async is handled in this framework, the one common mistake, and when to stream vs. await -->

### Prompt Engineering Discipline

<!-- System vs. user prompt separation, few-shot guidance, token budget strategy -->

### Context Window Management

<!-- Strategy specific to this system type: RAG chunking / conversation summarisation / agent compaction -->

### Cost and Latency Budget

<!-- Per-call cost estimate, caching strategy, sub-task model routing -->

---

## 5. Evaluation Strategy

### Dimensions

| Dimension | Rubric (Pass/Fail or 1-5) | Measurement Approach | Priority |
|-----------|--------------------------|---------------------|----------|
| | | Code / LLM Judge / Human | Critical / High / Medium |

### Eval Tooling

**Primary Tool:** <!-- e.g., RAGAS + Langfuse -->

**Setup:**
```bash
# Install and configure
```

**CI/CD Integration:**
```bash
# Command to run evals in CI/CD pipeline
```

### Reference Dataset

**Size:** <!-- e.g., 20 examples to start -->

**Composition:**
<!-- What scenario types the dataset covers: critical paths, edge cases, failure modes -->

**Labeling:**
<!-- Who labels examples and how (domain expert, LLM judge with calibration, etc.) -->

---

## 6. Guardrails

> Derived from a code-grounded causal-loop pass (2026-07-15, four loops traced between this
> phase's writes and already-shipped machinery -- see `223-SPEC.md` Addendum for the two that
> bear on locked Requirements). All four are reinforcing (R) loops with no existing counter-
> signal; none are balancing. This is the fifth instance of today's cross-session "silent
> degrade, no disclosure" pattern (`.planning/debug/intern-qa-silent-degrade-pattern-three-
> independent-sessions-2026-07-14.md`), found here inside a phase's own design before build,
> not after.

### Online (Real-Time)

| Guardrail | Trigger | Intervention |
|-----------|---------|--------------|
| G-1 Claim-provenance tag (Loop 1: claim-nudge into `context_block` reach score) | Any `writeClaimNode` call from bono/intel-pipeline | Flag -- tag the node with an AI-pipeline-authored provenance marker (not a new node type, an additive property) so downstream consumers (reach scoring, Eureka) CAN distinguish it from a human-filed claim, even though this phase does not require them to act on the distinction yet |
| G-2 JTBD-drift disclosure (Loop 2: calibrate-then-fan reinforcing loop, 223-SPEC.md Addendum) | Intel-pipeline's synthesize step produces findings that diverge from the JTBD cues `setCurrent` locked at calibrate time | Escalate -- surface the divergence at the F.5 synthesize gate rather than silently filing findings that no longer match the JTBD the fan was oriented by; navigator decides whether to re-calibrate |
| G-3 Opportunity-bridge existence check (Loop 3: `compute-opportunity-state` cannot see room.db writes, verified BLOCKING in 223-SPEC.md Addendum) | Either surface completes and Requirement 4's acceptance step runs | Block -- do not claim "opportunities surfaced in the bank rollup" as satisfied unless a bridge (223-SPEC.md Addendum options a/b/c) actually exists and is exercised; a green acceptance check against the wrong store is a false pass |
| G-4 Unreviewed-substrate disclosure (Loop 4: Eureka scores ALL nodes regardless of `review_status`) | A `/mos:eureka` run (or the 24h scout-cadence auto-run) scores a room containing `proposed` claims from this phase alongside `confirmed` ones | Flag -- Eureka's own output should distinguish (or at minimum not silently blend) proposed-AI-authored claims from confirmed-human ones in its ranked pairs, given a multi-persona fan can multiply proposed claims fast relative to a room's normal human-filing pace |

### Offline (Flywheel)

| Metric | Sampling Strategy | Action on Degradation |
|--------|------------------|----------------------|
| `SUPERSEDES` chain correctness (exactly-one edge per re-run, zero on first run -- Req 2 acceptance) | Every bono re-run on the same topic, 100% sampled (cheap, structural check) | If a re-run produces zero or multiple `SUPERSEDES` edges, block the release gate (`tests/run-all-223.sh`) -- this is a correctness bug, not a quality-degradation signal |
| Persona heterogeneity (no two hat-cells share an identical lens; disconfirming-evidence-first for Black) | Every bono debate run | If heterogeneity collapses (personas converging on one lens), flag for a governance-map review -- Requirement 1's whole point is failing |
| Proposed-claim volume vs. human-filed volume ratio (Loop 4's "multiplies fast" risk) | Weekly rollup per room that has run bono/intel-pipeline | If AI-proposed claims dominate a room's node count by a wide margin with a low confirm rate, flag for navigator review -- signals the pipeline is generating volume the human isn't actually reviewing, not real progress |

---

## 7. Production Monitoring

**Tracing Tool:** <!-- e.g., Langfuse self-hosted -->

**Key Metrics to Track:**
<!-- 3-5 metrics that will be monitored in production -->

**Alert Thresholds:**
<!-- When to page/alert -->

**Smart Sampling Strategy:**
<!-- How to select interactions for human review — signal-based filters -->

---

## Checklist

- [ ] System type classified
- [ ] Critical failure modes identified (≥ 3)
- [ ] Domain context researched (Section 1b: vertical, stakes, expert criteria, failure modes)
- [ ] Regulatory/compliance context identified or explicitly noted as none
- [ ] Domain expert roles defined for evaluation involvement
- [ ] Framework selected with rationale documented
- [ ] Alternatives considered and ruled out
- [ ] Framework quick reference written (install, imports, pattern, pitfalls)
- [ ] AI systems best practices written (Section 4b: Pydantic, async, prompt discipline, context)
- [ ] Evaluation dimensions grounded in domain rubric ingredients
- [ ] Each eval dimension has a concrete rubric (Good/Bad in domain language)
- [ ] Eval tooling selected — Arize Phoenix default confirmed or override noted
- [ ] Reference dataset spec written (size ≥ 10, composition + labeling defined)
- [ ] CI/CD eval integration specified
- [ ] Online guardrails defined
- [ ] Production monitoring configured (tracing tool + sampling strategy)
