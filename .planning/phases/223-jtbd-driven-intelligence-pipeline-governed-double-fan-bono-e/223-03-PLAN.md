---
phase: 223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono
plan: 03
type: execute
wave: 2
depends_on: ["223-01", "223-02"]
files_modified:
  - commands/bono.md
  - skills/bono/SKILL.md
  - data/connector-registry.json
autonomous: true
requirements: ["Req 1", "Req 2", "Req 6"]
must_haves:
  truths:
    - "commands/bono.md carries the 8-phase governed flow: topic-confirm/JTBD orientation -> domain decomposition -> governed team assembly -> per-persona research fan -> hypothesis-confirm + governed debate -> MECE-Minto synthesis + unknowns matrix -> close the loop -> version cut"
    - "bono declares hitl_stages (topic-confirm F.1, hypothesis-confirm F.1, ruling F.5) and keeps reach_id hats / sub_mode bono - never a 7th reach"
    - "The conclusion path emits narrative-schema JSON (governing_thought <=250, 3-5 key_claims) and a map-unknowns matrix, then terminates through writeCloseLoop (Req 2 + Req 4)"
    - "--version-log walks the SUPERSEDES chain via walkSupersedesChain and lists conclusions in chain order"
    - "skills/bono/SKILL.md is regenerated by build-skill-mirrors.cjs and keeps the DESENSITIZE asymmetry: command sensor_triggers [SENS-05], mirror [] - the two files are NEVER converged (RESEARCH staleness correction 1)"
    - "No file under commands/, skills/, or lib/core/bono/ references mindrian-designs (Req 6)"
    - "lib/memory/feynman-prompts.cjs is byte-identical after this plan (SPEC constraint)"
  artifacts:
    - path: "commands/bono.md"
      provides: "The evolved 8-phase governed research debate surface, drafted from SPEC Reqs 1-2 + BUILD-BRIEF Sections 5/6 per Requirement 6's fallback"
      contains: "hitl_stages"
      min_lines: 150
    - path: "skills/bono/SKILL.md"
      provides: "Regenerated mirror, byte-identical to the command except the desensitized sensor_triggers field"
  key_links:
    - from: "commands/bono.md"
      to: "lib/core/bono/hat-governance.cjs"
      via: "the debate step wires composeGovernedSeams into runDebate (Plan 01's exports)"
      pattern: "composeGovernedSeams|hat-governance"
    - from: "commands/bono.md"
      to: "lib/core/bono/persona-research.cjs"
      via: "the fan step injects personaDispatchCell as runCellFanout's dispatchCell"
      pattern: "personaDispatchCell|persona-research"
    - from: "commands/bono.md"
      to: "lib/core/close-loop-writer.cjs"
      via: "the terminal step routes conclusion/knowns/unknowns/opportunities through writeCloseLoop, then runs compute-opportunity-state"
      pattern: "writeCloseLoop|close-loop-writer"
    - from: "commands/bono.md"
      to: "lib/core/temporal/supersession.cjs"
      via: "version cut: findPriorConclusion feeds writeCloseLoop's priorConclusionId; --version-log renders walkSupersedesChain"
      pattern: "version-log|walkSupersedesChain"
---

<objective>
Evolve /mos:bono from its shipped Phase-164 form into the 8-phase governed research debate
(Req 1 consumption + Req 2 surface): the body is REPLACED (evolve, don't rebuild - every engine
call stays on the shipped substrate), the flow wires Plan 01's governance + persona research and
Plan 02's close-the-loop spine + version cut, and the prose is DRAFTED from this phase's SPEC and
the BUILD-BRIEF's self-contained Sections 5/6 per Requirement 6's fallback - the missing
~/mindrian-designs/ directory is not referenced anywhere.

Purpose: the first of the two born-wired surfaces. The mirror + registry regeneration here is
deliberately serialized BEFORE Plan 04 (both plans touch the generated mirror/registry artifacts;
running them in one wave would race the generators).

Output: the replaced commands/bono.md, the regenerated skills/bono/SKILL.md mirror, a clean
connector --check, and green regression suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-SPEC.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-CONTEXT.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-RESEARCH.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-BUILD-BRIEF.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-01-SUMMARY.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-02-SUMMARY.md
@commands/bono.md
@commands/act.md
@scripts/build-skill-mirrors.cjs
</context>

## Artifacts this phase produces (phase-wide contract)

| Artifact | Kind | Plan |
|----------|------|------|
| `lib/core/bono/hat-governance.cjs` (`HAT_GOVERNANCE`, `CROSS_CUTTING_RULES`, `governanceForHat`, `enforceGovernance`, `assertHeterogeneity`, `composeGovernedSeams`) | new module | 01 |
| `lib/core/bono/persona-research.cjs` (`personaDispatchCell`, `validateCitations`) | new module | 01 |
| `tests/helpers/fixture-room-223.cjs` (`buildFixtureRoom223`) | new test helper | 01 |
| `tests/test-223-hat-governance.cjs`, `tests/test-223-part8-egress.cjs` | test files | 01 |
| `lib/core/navigation/typed-open-question.cjs` (`writeOpenQuestionNode`) | new navigation submodule | 02 |
| `lib/core/navigation/typed-claim.cjs` optional `extraProps` bag | function contract extension | 02 |
| `lib/core/navigation.cjs` re-export of `writeOpenQuestionNode` | additive re-export | 02 |
| `lib/core/close-loop-writer.cjs` (`writeCloseLoop`, `findPriorConclusion`, `mintArtifactId`) | new shared-spine module | 02 |
| `lib/core/temporal/supersession.cjs::walkSupersedesChain` | function addition (net-new walker) | 02 |
| `tests/test-223-close-loop.cjs`, `tests/test-223-supersedes-chain.cjs` | test files | 02 |
| `commands/bono.md` 8-phase governed body + `hitl_stages` + `web_scope: green` | surface evolution | 03 |
| `skills/bono/SKILL.md` regenerated mirror (DESENSITIZE preserved) | generated mirror | 03 |
| bono `--version-log` (reads `walkSupersedesChain`) | command capability | 03 |
| `lib/core/intel-pipeline.cjs` (`runIntelPipeline`) | new composition module | 04 |
| `commands/intel-pipeline.md` (kind: meta connector) + `skills/intel-pipeline/SKILL.md` | new surface + generated mirror | 04 |
| `tests/test-223-intel-pipeline.cjs` | test file | 04 |
| `tests/run-all-223.sh` + `run-feynman-tests.cjs` registration | new harness + registration | 05 |
| `data/connector-registry.json`, `data/render-coverage-registry.json` regenerated | generated artifacts | 03/04/05 |
| `docs/CANON-PHASE-MAP.md` phase row + rethinking-mindrianos research entry | docs + compositing | 05 |
| Version cut via `scripts/release.sh` (navigator-gated) | release | 05 |

<tasks>

<task type="auto">
  <name>Task 1: Replace commands/bono.md's body with the 8-phase governed flow</name>
  <files>commands/bono.md</files>
  <read_first>
    - commands/bono.md (the FULL current file: the Phase-164 frontmatter incl. the connector-block comment discipline, the flow sections being replaced, the engine-call vocabulary to preserve)
    - commands/act.md lines 1-60 (the hitl_stages list format: stage/shapes/mode keys - bono's new hitl_stages follows this shape)
    - 223-01-SUMMARY.md + 223-02-SUMMARY.md (the exact exported seam shapes: composeGovernedSeams return, personaDispatchCell ctx contract, writeCloseLoop payload contract, findPriorConclusion/walkSupersedesChain signatures)
    - lib/memory/narrative-schema.cjs lines 34-120 (the conclusion JSON shape the prose instructs: governing_thought <=250 chars, key_claims 3-5)
    - commands/structure-argument.md and commands/map-unknowns.md (the Pyramid+MECE discipline and the unknowns-matrix discipline the synthesis step borrows - reuse their vocabulary, do not re-derive)
    - The repo's Larry voice reference (voice-dna) named by SPEC's carried style rules (no emoji, no em-dashes, 12-glyph vocabulary, 3-line errors)
  </read_first>
  <action>
    Replace the body of commands/bono.md (everything below the frontmatter) with the 8-phase
    governed flow, DRAFTED from SPEC Requirements 1-2 + BUILD-BRIEF Sections 5/6 (Req 6 fallback:
    the prose is an assumption to draft, per the SPEC's Constraint Clarity flag; zero references to
    mindrian-designs anywhere). Keep the frontmatter's Phase-164 fields and connector-comment
    discipline, with these frontmatter changes ONLY:
    - hitl_shape/hitl_why REPLACED by hitl_stages in act.md's list format: topic-confirm (F.1,
      gate), hypothesis-confirm (F.1, gate), ruling (F.5, gate) plus an updated hitl_why naming the
      three decision surfaces (BUILD-BRIEF SP-A.1).
    - connector.web_scope: null -> green (the per-persona web legs are now first-class; BUILD-BRIEF
      SP-A.1 + Section 8; direction stays SIGNAL->LOCAL, proven by Plan 01's egress test).
    - connector.reach_id stays hats, sub_mode stays bono, framework stays "Six Thinking Hats",
      sensor_triggers stays [SENS-05]. NEVER a 7th reach.

    The 8 phases (numbered sections, engine calls named, directive prose - no fenced full
    implementations):
    1. Topic-confirm + JTBD orientation - Shape F selector (scope/purpose/substrates/hypothesis
       seed); read the room's active JTBD via lib/hmi/jtbd-state.cjs getCurrent to orient scope;
       the F.1 topic-confirm gate.
    2. Domain decomposition - Engine 1 decomposition into the subdomain grid (existing substrate,
       unchanged).
    3. Governed team assembly - assembleTeam (anti-ossification guards unchanged) + hat-governance:
       each persona slot binds to HAT_GOVERNANCE[hat]'s discipline; assertHeterogeneity runs over
       the assembled cells and a duplicate-lens set is re-drawn before any research fires (Req 1).
    4. Per-persona research fan - runCellFanout with dispatchCell = personaDispatchCell (Plan 01):
       each (subdomain x hat) cell runs extractContext -> runSourceLens -> wireAccept, generic
       handle only (Part 8), building the persona's wired INFORMS world-of-knowledge; planDispatch
       budget + the fan cap stay the cost controls.
    5. Hypothesis-confirm + governed debate - the F.1 hypothesis-confirm gate, then runDebate with
       composeGovernedSeams injected (deriveFn stays synchronous over pre-resolved data - CR-01)
       and validateCitations riding the self-critique so no persona asserts beyond its wired
       sources; the F.5 ruling gate (verbs supported | rejected | refined | undecided; residual
       tensions filed).
    6. MECE-Minto synthesis + unknowns matrix - apply /mos:structure-argument's Pyramid+MECE
       discipline to emit the narrative JSON (validateNarrative-conformant) and /mos:map-unknowns'
       matrix for the unknowns base (Req 2). lib/memory/feynman-prompts.cjs is NOT touched
       (byte-checked SPEC constraint).
    7. Close the loop - findPriorConclusion(db, topic_hash) detects a re-run, then ONE
       writeCloseLoop call (Plan 02) carrying claims/relations/killed/conclusion/knowns/unknowns/
       opportunities plus priorConclusionId when a prior exists; then run
       bash scripts/compute-opportunity-state <roomDir> so the bank is aware (Req 4; D-01 dual
       write and D-02 proposed edges live INSIDE the writer - the command never bypasses it).
    8. Version cut + --version-log - the SUPERSEDES chain is written by step 7's supersede path
       (D-04: NULL review_status, mechanical bookkeeping; the new conclusion NODE is what the
       navigator ratifies). Document the --version-log flag: render walkSupersedesChain(db,
       newestConclusionId) as a chain-order list (newest first; one line per conclusion with
       created_at + a governing-thought snippet). A first run renders a single-entry log and zero
       SUPERSEDES edges.

    Preserve the existing constitutional callouts (Part 7 orchestration-not-atom, Part 8 generic
    handles, Part 9 proposed-only + confirmNode(byUser), the D-164 decisions), updating them to
    name the new modules. Voice: Larry, Feynman-simplified, no em-dashes, no emoji.
  </action>
  <verify>
    <automated>bash -c 'grep -q "hitl_stages" commands/bono.md && grep -q "web_scope: green" commands/bono.md && grep -q "reach_id: hats" commands/bono.md && grep -q "version-log" commands/bono.md && grep -q "writeCloseLoop" commands/bono.md && grep -q "personaDispatchCell" commands/bono.md && ! grep -q "mindrian-designs" commands/bono.md && git diff --exit-code lib/memory/feynman-prompts.cjs && echo BONO-BODY-OK'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints BONO-BODY-OK (all eight structural greps green)
    - grep -c "^### \|^## " commands/bono.md shows the 8 numbered flow phases present (exactly eight numbered phase sections in the body)
    - grep -q "SENS-05" commands/bono.md (the command KEEPS its sensor trigger; the mirror is what stays desensitized)
    - grep -q "composeGovernedSeams" commands/bono.md and grep -q "walkSupersedesChain" commands/bono.md (the new seams are named, not paraphrased)
    - No em-dash character anywhere in the file: grep -cP "\x{2014}" commands/bono.md returns 0
    - git diff --exit-code lib/memory/feynman-prompts.cjs exits 0 (byte-identical)
  </acceptance_criteria>
  <done>bono's shipped surface IS the 8-phase governed debate: governance + persona research wired by name, conclusion disciplined by narrative-schema, loop closed through the shared writer, version cut documented with --version-log.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the mirror + registry (DESENSITIZE preserved, never converged) and run regressions</name>
  <files>skills/bono/SKILL.md, data/connector-registry.json</files>
  <read_first>
    - scripts/build-skill-mirrors.cjs lines 25-52 (the DESENSITIZE convention: for a connects_to_spine command with non-empty sensor_triggers, the mirror's sensor_triggers is rewritten to [] ON PURPOSE - RESEARCH staleness correction 1; the task is RE-RUN, never converge)
    - skills/bono/SKILL.md (current mirror state: line 38 sensor_triggers [] - the correct asymmetry that must SURVIVE regeneration)
    - scripts/build-connector-registry.cjs (the --check contract: duplicate-tuple CONN-03 check, frozen reach validation)
  </read_first>
  <action>
    Run node scripts/build-skill-mirrors.cjs to regenerate skills/bono/SKILL.md from the new
    commands/bono.md body. Do NOT hand-edit the mirror and do NOT converge sensor_triggers: the
    command keeps [SENS-05], the mirror keeps [] (the enforced DESENSITIZE convention - converging
    them would trip the registry's CONN-03 duplicate-tuple check; this SUPERSEDES the SPEC/BRIEF
    "reconcile the drift" wording per RESEARCH staleness correction 1).

    Then run node scripts/build-connector-registry.cjs (regenerate) followed by
    node scripts/build-connector-registry.cjs --check. bono's registry tuple must be UNCHANGED
    except web_scope (reach_id hats, sub_mode bono, framework Six Thinking Hats, posture hold);
    zero new entries at this point (intel-pipeline does not exist yet - Plan 04 adds the 2 new
    entries).

    Regressions: bash tests/run-all-164.sh (bono base must stay 20/20 - the engines are untouched,
    only the command prose changed) and node tests/test-223-hat-governance.cjs +
    node tests/test-223-close-loop.cjs (Wave 1 modules still green).
  </action>
  <verify>
    <automated>bash -c 'node scripts/build-connector-registry.cjs --check && grep -q "sensor_triggers: \[\]" skills/bono/SKILL.md && grep -q "SENS-05" commands/bono.md && ! grep -rq "mindrian-designs" commands/ skills/ lib/core/bono/ && bash tests/run-all-164.sh && echo BONO-WIRING-OK'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints BONO-WIRING-OK
    - build-connector-registry.cjs --check exits 0 with NO duplicate (sensor, reach_id, sub_mode) tuple
    - The mirror differs from the command ONLY in the desensitized sensor_triggers field (spot-check: diff of the two frontmatter blocks shows exactly that one field differing, plus any generator-standard header)
    - bash tests/run-all-164.sh passes 20/20 (no engine regression from the prose evolution)
    - grep -rc "mindrian-designs" commands/ skills/ lib/core/bono/ sums to 0 (Req 6 holds after regeneration)
  </acceptance_criteria>
  <done>The evolved bono surface is born-wired and gate-clean: mirror regenerated with the correct asymmetry, registry tuple stable, base suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator input at 3 gates -> debate flow | untrusted free-text (topic, hypothesis edits) enters the governed flow |
| command prose -> connector registry | a hand-drifted connector block could mint a 7th reach or duplicate a tuple |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-10 | Elevation of Privilege | debate conclusions auto-filing | mitigate | three declared hitl_stages (topic-confirm F.1, hypothesis-confirm F.1, ruling F.5); synthesis files only on APPROVE; all writes proposed-only through writeCloseLoop |
| T-223-11 | Tampering | connector tuple drift (7th reach / converged mirror) | mitigate | connector block machine-regenerated, never hand-trusted (BRIEF Section 8); --check gate + CONN-03 duplicate check run in-plan |
| T-223-12 | Information Disclosure | web_scope green widening egress | mitigate | direction is SIGNAL->LOCAL only; Brain legs stay generic-handle-only behind classify (Plan 01's proven boundary); Plan 05's Part 8 sweep is the permanent tripwire |
| T-223-13 | Spoofing | injected web content steering the debate (prompt injection via research findings) | mitigate | injection-as-data: findings are wired EvidenceClaims a persona argues OVER, gated by citation boundary + governance self-critique; nothing from the web is executed |
| T-223-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new dependencies; Plan 05 git-diff leg fails on any drift |
</threat_model>

<verification>
- Task 1 + Task 2 verify commands print BONO-BODY-OK / BONO-WIRING-OK
- bash tests/run-all-164.sh 20/20; node tests/test-223-hat-governance.cjs, node tests/test-223-close-loop.cjs, node tests/test-223-supersedes-chain.cjs all exit 0
- Manual (deferred to phase verification, per 223-VALIDATION manual rows): read the 8-phase prose aloud against the Larry voice reference; one live bono run on a real topic checking personas cite only their wired sources
</verification>

<success_criteria>
- The 8-phase governed body ships with hitl_stages declared and the connector tuple stable (no 7th reach)
- Mirror regenerated with DESENSITIZE asymmetry intact - never converged
- Req 6 grep clean across commands/, skills/, lib/core/bono/
- feynman-prompts.cjs byte-identical; run-all-164 still 20/20
</success_criteria>

<output>
Create `.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-03-SUMMARY.md` when done, recording the 8-phase section map, the frontmatter diff (hitl_stages, web_scope), and confirmation that the mirror regeneration preserved the asymmetry.
</output>
