---
phase: 223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/typed-open-question.cjs
  - lib/core/navigation/typed-claim.cjs
  - lib/core/navigation.cjs
  - lib/core/close-loop-writer.cjs
  - lib/core/temporal/supersession.cjs
  - tests/test-223-close-loop.cjs
  - tests/test-223-supersedes-chain.cjs
autonomous: true
requirements: ["Req 2", "Req 4"]
must_haves:
  truths:
    - "One writeCloseLoop call lands claim + opportunity + open_question nodes in room.db, ALL review_status 'proposed', with every write routed through navigation.cjs (Part 9 chokepoint)"
    - "Every opportunity is a DUAL write per D-01: the bank .md is written FIRST (six reader-required frontmatter fields + shared artifact_id), the room.db node SECOND (same artifact_id in extraProps) - a mid-crash leaves a bank-visible artifact, never a dangling node"
    - "bash scripts/compute-opportunity-state <roomDir> surfaces the written-through opportunity in the bank rollup (G-3: the acceptance measures BOTH stores, never just room.db)"
    - "All semantic edges (SUPPORTS/CONTRADICTS/CONVERGES/INFORMS/REJECTED_BECAUSE) land review_status 'proposed' per D-02; the SUPERSEDES edge lands review_status NULL per D-04 (no review_status arg passed)"
    - "A 2-run same-topic fixture produces exactly one SUPERSEDES edge and walkSupersedesChain lists both conclusions in chain order; a 1-run fixture produces zero SUPERSEDES edges (no false chain)"
    - "confirmNode requires an explicit byUser argument - no write path in this plan can promote a node (Part 9)"
  artifacts:
    - path: "lib/core/close-loop-writer.cjs"
      provides: "The shared close-the-loop graph-write spine both surfaces call (BUILD-BRIEF Section 6 contract): claims, relations, killed claims, conclusion, knowns, unknowns, opportunities, supersession"
      exports: ["writeCloseLoop", "findPriorConclusion", "mintArtifactId"]
      min_lines: 150
    - path: "lib/core/navigation/typed-open-question.cjs"
      provides: "open_question node writer (type 'open_question', born proposed) - net-new: navigation ships no writer for the type insights.findOpenQuestions reads"
      exports: ["writeOpenQuestionNode", "OPEN_QUESTION_NODE_ID"]
    - path: "lib/core/temporal/supersession.cjs"
      provides: "walkSupersedesChain added next to supersede - the net-new read-side chain walker (RESEARCH finding 4: no chain-read helper ships)"
      exports: ["supersede", "walkSupersedesChain"]
    - path: "tests/test-223-close-loop.cjs"
      provides: "Req 4 proof: both stores, all proposed, .md-first ordering, byUser gate"
    - path: "tests/test-223-supersedes-chain.cjs"
      provides: "Req 2 chain proof: 2-run exactly-one edge + NULL review_status + chain order; 1-run zero"
  key_links:
    - from: "lib/core/close-loop-writer.cjs"
      to: "lib/core/navigation.cjs"
      via: "every node/edge write routes through navigation exports (writeClaimNode, writeOpportunityNode, writeOpenQuestionNode, writeEdge) - the Part 9 chokepoint"
      pattern: "require\\('.*navigation.cjs'\\)"
    - from: "lib/core/close-loop-writer.cjs"
      to: "lib/core/opportunity-ops.cjs"
      via: "D-01 write-through reuses bankOpportunity/fileOpportunity (Part 7 - the writers already emit the six reader fields)"
      pattern: "opportunity-ops"
    - from: "lib/core/close-loop-writer.cjs"
      to: "lib/memory/narrative-schema.cjs"
      via: "validateNarrative gates the conclusion payload before any conclusion node writes"
      pattern: "validateNarrative"
    - from: "lib/core/close-loop-writer.cjs"
      to: "lib/core/temporal/supersession.cjs"
      via: "priorConclusionId routes through supersede (chokepoint-routed SUPERSEDES write, D-04 NULL)"
      pattern: "supersede"
---

<objective>
Build the ONE close-the-loop graph-write spine both surfaces terminate through (Req 4, BUILD-BRIEF
Section 6 contract), resolving the Req-4 write-side gap exactly as D-01 locks it: every opportunity
is a dual write - bank .md FIRST via the shipped opportunity-ops writers, room.db node SECOND, one
shared artifact_id minted before either write. All 223 semantic edges land review_status 'proposed'
per D-02; SUPERSEDES edges carry NULL per D-04. Also ship the net-new SUPERSEDES chain WALKER
(supersession.cjs only writes today - RESEARCH finding 4) that Plan 03's --version-log reads.

Purpose: Plans 03 (bono) and 04 (intel-pipeline) both call writeCloseLoop; without this plan
neither surface can close the loop, and Req 4's acceptance (BOTH stores, G-3) cannot pass.

Output: the shared writer module, the open_question typed writer, the typed-claim extraProps
extension, the chain walker, and two green test legs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-SPEC.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-CONTEXT.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-RESEARCH.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-BUILD-BRIEF.md
@lib/core/navigation.cjs
@lib/core/navigation/typed-claim.cjs
@lib/core/navigation/typed-opportunity.cjs
@lib/core/navigation/edges.cjs
@lib/core/opportunity-ops.cjs
@lib/core/temporal/supersession.cjs
@lib/memory/narrative-schema.cjs
@tests/helpers/fixture-room-224.cjs
</context>

## Artifacts this phase produces (phase-wide contract)

| Artifact | Kind | Plan |
|----------|------|------|
| `lib/core/bono/hat-governance.cjs` (`HAT_GOVERNANCE`, `CROSS_CUTTING_RULES`, `governanceForHat`, `enforceGovernance`, `assertHeterogeneity`, `composeGovernedSeams`) | new module | 01 |
| `lib/core/bono/persona-research.cjs` (`personaDispatchCell`, `validateCitations`) | new module | 01 |
| `tests/helpers/fixture-room-223.cjs` (`buildFixtureRoom223`) | new test helper | 01 |
| `tests/test-223-hat-governance.cjs`, `tests/test-223-part8-egress.cjs` | test files | 01 |
| `lib/core/navigation/typed-open-question.cjs` (`writeOpenQuestionNode`) | new navigation submodule | 02 |
| `lib/core/navigation/typed-claim.cjs` optional `extraProps` bag | function contract extension | 02 |
| `lib/core/navigation.cjs` re-export of `writeOpenQuestionNode` | additive re-export | 02 |
| `lib/core/close-loop-writer.cjs` (`writeCloseLoop`, `findPriorConclusion`, `mintArtifactId`) | new shared-spine module | 02 |
| `lib/core/temporal/supersession.cjs::walkSupersedesChain` | function addition (net-new walker) | 02 |
| `tests/test-223-close-loop.cjs`, `tests/test-223-supersedes-chain.cjs` | test files | 02 |
| `commands/bono.md` 8-phase governed body + `hitl_stages` + `web_scope: green` | surface evolution | 03 |
| `skills/bono/SKILL.md` regenerated mirror (DESENSITIZE preserved) | generated mirror | 03 |
| bono `--version-log` (reads `walkSupersedesChain`) | command capability | 03 |
| `lib/core/intel-pipeline.cjs` (`runIntelPipeline`) | new composition module | 04 |
| `commands/intel-pipeline.md` (kind: meta connector) + `skills/intel-pipeline/SKILL.md` | new surface + generated mirror | 04 |
| `tests/test-223-intel-pipeline.cjs` | test file | 04 |
| `tests/run-all-223.sh` + `run-feynman-tests.cjs` registration | new harness + registration | 05 |
| `data/connector-registry.json`, `data/render-coverage-registry.json` regenerated | generated artifacts | 03/04/05 |
| `docs/CANON-PHASE-MAP.md` phase row + rethinking-mindrianos research entry | docs + compositing | 05 |
| Version cut via `scripts/release.sh` (navigator-gated) | release | 05 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: navigation additions - open_question typed writer + typed-claim extraProps bag</name>
  <files>lib/core/navigation/typed-open-question.cjs, lib/core/navigation/typed-claim.cjs, lib/core/navigation.cjs, tests/test-223-close-loop.cjs</files>
  <read_first>
    - lib/core/navigation/typed-claim.cjs (the FULL module: writeClaimNode at lines 82-145, KNOWLEDGE_TYPES, CLAIM_NODE_ID idempotency, the D-10 additive-JSON-props discipline, the ON CONFLICT clause that excludes review_status)
    - lib/core/navigation/typed-opportunity.cjs lines 100-130 and 177-220 (the extraProps precedent to copy: protected keys an extraProps bag may NEVER touch; the id-minter idiom)
    - lib/core/navigation/insights.cjs line 190 (the read side: findOpenQuestions selects n.type = 'open_question' - the exact type string the new writer must mint)
    - lib/core/navigation.cjs lines 195-345 (the additive-re-export pattern for typed writers: writeClaimNode / writeOpportunityNode / writeDomainNode comment blocks and export lines)
    - scripts/check-substrate.cjs (confirm the navigation allow-list regex ^lib/core/navigation/ covers the new submodule)
  </read_first>
  <behavior>
    - Test 1: writeOpenQuestionNode(db, {question, sessionId, extraProps}) lands a nodes row with type 'open_question', review_status 'proposed', properties carrying the question text; re-writing the same question UPSERTs (no duplicate) and never touches review_status on conflict
    - Test 2: insights.findOpenQuestions (via navigation) returns the node written in Test 1 - the writer and the shipped reader agree on the type string
    - Test 3: writeClaimNode with an extraProps bag {kind:'conclusion', topic_hash:'abc', pipeline:'bono-v2'} persists those keys in properties; extraProps attempting to override protected keys (knowledge_type, text, review_status-affecting anything) is silently filtered exactly like typed-opportunity's discipline; writeClaimNode WITHOUT extraProps is byte-identical in behavior to today (every existing caller unaffected)
    - Test 4: confirmNode without a byUser argument fails; with byUser it promotes - asserted once here for the nodes this plan writes (SPEC Req 4 acceptance)
  </behavior>
  <action>
    Create lib/core/navigation/typed-open-question.cjs modeled line-for-line on typed-claim.cjs's
    structure (caller-owned db handle, never opens room.db, never requires node:sqlite directly -
    stays inside the check-substrate allow-list): OPEN_QUESTION_NODE_ID(sessionId, questionKey)
    idempotent minter copying CLAIM_NODE_ID's idiom; writeOpenQuestionNode(db, params) with params
    {question (mandatory non-empty string), sessionId, source, extraProps} inserting type
    'open_question', created_by 'system', review_status 'proposed' at INSERT, ON CONFLICT updating
    properties + last_seen_at ONLY (review_status never touched - the no-downgrade idiom). Document
    in the header: an open_question is written by the 223 close-the-loop contract (SPEC Req 4:
    unknowns -> open_question nodes) and read by insights.findOpenQuestions; it is born proposed and
    only confirmNode(byUser) promotes it (Part 9).

    Extend lib/core/navigation/typed-claim.cjs ADDITIVELY: writeClaimNode's params gains an optional
    extraProps plain-object bag merged into the props blob AFTER the fixed keys, with a protected-key
    filter copying typed-opportunity's precedent (extraProps may never override knowledge_type, text,
    or any fixed provenance key; non-plain-object extraProps is ignored). This enables the conclusion
    marker (kind/topic/topic_hash) and the G-1 provenance tag (pipeline) WITHOUT a new node type and
    WITHOUT touching the schema. Zero behavior change when extraProps is absent.

    Re-export writeOpenQuestionNode (and OPEN_QUESTION_NODE_ID) from lib/core/navigation.cjs
    following the existing additive-re-export comment pattern next to writeClaimNode/
    writeOpportunityNode, with a header comment naming Phase 223 / Req 4.

    Start tests/test-223-close-loop.cjs with behaviors 1-4 as its first section (Task 2 appends the
    writer-level sections). Build throwaway rooms with buildFixtureRoom224 (Plan 01's fixture-223
    helper is a Wave-1 sibling and NOT a dependency of this plan - the 224 helper suffices for pure
    db legs; document this choice in the test header).
  </action>
  <verify>
    <automated>node tests/test-223-close-loop.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-223-close-loop.cjs exits 0 (behaviors 1-4 section)
    - grep -c "writeOpenQuestionNode" lib/core/navigation.cjs returns at least 2 (comment + export)
    - Source assertion: grep -c "node:sqlite" lib/core/navigation/typed-open-question.cjs returns 0 (caller-owned handle discipline)
    - Source assertion: grep -n "extraProps" lib/core/navigation/typed-claim.cjs shows the protected-key filter before the props merge
    - Regression: node tests/test-graph-derive-sweep.cjs exits 0 and bash tests/run-all-164.sh passes 20/20 (writeClaimNode's existing callers byte-compatible)
  </acceptance_criteria>
  <done>navigation can write all three Req-4 node kinds (claim / opportunity / open_question), the conclusion + provenance markers have a legal additive carrier, and the byUser gate is re-proven against this phase's nodes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: close-loop-writer.cjs - the shared spine implementing BUILD-BRIEF Section 6 with D-01 dual-write and D-02 proposed edges</name>
  <files>lib/core/close-loop-writer.cjs, tests/test-223-close-loop.cjs</files>
  <read_first>
    - 223-BUILD-BRIEF.md Section 6 (the authoritative output -> graph-write contract table this module implements row by row)
    - lib/core/opportunity-ops.cjs lines 158-188 (the reader's six required frontmatter fields: funder, program, deadline, relevance_score, status, created), lines 640-684 (fileOpportunity frontmatter emit), lines 1120-1230 (bankOpportunity problem_hash dedup)
    - lib/core/navigation/edges.cjs lines 695-774 (writeEdge params incl. review_status enum {'proposed','confirmed'}, absent binds NULL; ALLOWED_EDGE_TYPES frozen set; WR-06 clobber guard)
    - lib/core/navigation/typed-opportunity.cjs lines 177-279 (writeOpportunityNode params incl. extraProps; born proposed, NO review_status arg exists)
    - lib/memory/narrative-schema.cjs lines 34-120 (validateNarrative: governing_thought <=250 chars, key_claims 3-5)
    - lib/core/temporal/supersession.cjs lines 40-145 (supersede(db, oldNodeId, newNodeId, opts) - the ONLY legal SUPERSEDES write path)
  </read_first>
  <behavior>
    - Test 1: writeCloseLoop over a full payload on a scratch fixture room lands: one claim node per finding, one conclusion claim node (extraProps kind 'conclusion' + topic_hash), one claim + SUPPORTS edge per known, one open_question node per unknown, one opportunity node per opportunity, one REJECTED_BECAUSE edge per killed claim - ALL nodes review_status 'proposed', ALL semantic edges (SUPPORTS/CONTRADICTS/CONVERGES/INFORMS/REJECTED_BECAUSE) review_status 'proposed' per D-02, asserted by direct SELECT
    - Test 2 (D-01 ordering): for each opportunity the bank .md exists under room/opportunity-bank/ with the six reader-required frontmatter fields PLUS artifact_id, and the room.db opportunity node carries the SAME artifact_id in its properties - the pair is joinable from either store
    - Test 3 (D-01 crash ordering): with an injected node-writer that throws AFTER the .md write, the bank .md is on disk and NO opportunity node exists in room.db (bank-visible artifact, never a dangling node); the writer surfaces the partial-write disclosure, never swallows it
    - Test 4 (G-3, both stores): bash scripts/compute-opportunity-state <fixtureRoom> after a writeCloseLoop run surfaces the written-through opportunity in the bank rollup output - the acceptance measures the .md store, not just room.db (Pitfall 3 in 223-RESEARCH.md)
    - Test 5: an invalid conclusion payload (governing_thought >250 chars, or 2 key_claims) is REJECTED by validateNarrative before any conclusion node writes - zero conclusion rows land
    - Test 6: every claim node written carries the G-1 provenance marker (extraProps pipeline field naming the calling surface) so downstream consumers CAN distinguish AI-pipeline-authored claims
    - Test 7: a relations entry with an edge type outside SUPPORTS/CONTRADICTS/CONVERGES/INFORMS is rejected by the writer's own allow-list before writeEdge is even called (defense in depth over the frozen ALLOWED_EDGE_TYPES)
  </behavior>
  <action>
    Create lib/core/close-loop-writer.cjs - the shared spine BOTH surfaces call (Req 4: one
    graph-write contract). Requires navigation.cjs (the Part 9 chokepoint) plus opportunity-ops,
    narrative-schema, and temporal/supersession. Module header MUST document the D-01 write ordering
    decision verbatim intent: bank .md FIRST, room.db node SECOND, one shared artifact_id minted
    before either write, so a mid-crash leaves a bank-visible artifact without a dangling node
    (CONTEXT specifics). Exports:

    1. mintArtifactId(topic, name) - the D-01 cross-reference id (RESEARCH Open Question 1's
       recommendation, adopted): one id minted BEFORE either write, embedded in the .md frontmatter
       as artifact_id AND in writeOpportunityNode's extraProps.artifact_id. Deterministic-enough
       (hash of topic+name+date) so re-runs dedup sanely alongside bankOpportunity's problem_hash.
    2. writeCloseLoop(db, roomDir, payload, opts) implementing the Section 6 table:
       - payload.claims[] ({text, knowledge_type, evidence?}) -> navigation.writeClaimNode, each with
         extraProps {pipeline: opts.surface ('bono'|'intel-pipeline'), run_id} - the G-1 provenance
         marker (AI-SPEC Section 6), additive property, NOT a new node type.
       - payload.relations[] ({source_id, target_id, edge_type, reason}) -> navigation.writeEdge with
         review_status 'proposed' (D-02: 223's surfaces carry stance knowledge the 224 deriver cannot
         infer; one consistent proposal lifecycle). Writer-local allow-list restricted to
         SUPPORTS/CONTRADICTS/CONVERGES/INFORMS.
       - payload.killed[] -> exactly one REJECTED_BECAUSE edge per killed claim, review_status
         'proposed', reason as a short scalar (rejection IS data).
       - payload.conclusion ({governing_thought, key_claims, topic}) -> validateNarrative FIRST
         (reject the whole conclusion block on invalid, disclose, write nothing); then one conclusion
         node via writeClaimNode with extraProps {kind:'conclusion', topic, topic_hash, pipeline,
         run_id} - a claim-type node with a conclusion marker, NOT a new node type (the truth-claim
         closed set is frozen; insights/eureka consumers keep working). The node id must be
         run-unique (route the run_id into the sourceSegment key) so a re-run mints a NEW node -
         required for the SUPERSEDES chain.
       - payload.knowns[] -> claim nodes + SUPPORTS edge -> conclusion (review_status 'proposed').
       - payload.unknowns[] -> navigation.writeOpenQuestionNode each.
       - payload.opportunities[] ({name, problem, funder?, program?, deadline?, relevance_score?,
         supports_claim_ids[]}) -> D-01 dual write: (a) mintArtifactId; (b) bank .md FIRST via
         opportunity-ops bankOpportunity (chosen over fileOpportunity for its problem_hash dedup on
         re-runs - both emit the six reader fields; pass artifact_id through so it lands in the
         frontmatter - if bankOpportunity's current emit drops unknown fields, extend opportunity-ops
         ADDITIVELY with an optional passthrough rather than hand-rolling a writer, Part 7); (c)
         room.db node SECOND via navigation.writeOpportunityNode with extraProps {artifact_id,
         pipeline, run_id}; (d) SUPPORTS edges from the opportunity node to its claims, 'proposed'.
       - payload.priorConclusionId (optional) -> supersession.supersede(db, prior, newConclusionId) -
         D-04: supersede passes NO review_status (the SUPERSEDES edge binds NULL, a mechanical
         system fact, not a proposal). writeCloseLoop NEVER writes a SUPERSEDES edge on a first run
         (no priorConclusionId, no edge - Req 2's no-false-chain acceptance).
       Returns a scalar summary {claim_ids, conclusion_id, opportunity: [{node_id, md_path,
       artifact_id}], open_question_ids, edges_written, superseded} plus per-section disclosed
       failures (SEED-059: a degraded section is reported structurally, never silently dropped).
       Injectable opts seams for tests: bankWriteFn, nodeWriters, edgeWriter, supersedeFn.
    3. findPriorConclusion(db, topicHash) - SELECT the newest prior claim node whose properties
       carry kind 'conclusion' + the same topic_hash (read-only; excludes the current run's node by
       run_id). This is the lookup Plan 03's version cut uses to decide whether a prior exists.

    NOTE (files_modified): if bankOpportunity needs the additive artifact_id passthrough, add
    lib/core/opportunity-ops.cjs to this plan's actual touched files in the SUMMARY - the change is
    additive-only (a new optional field emitted into frontmatter; the reader and all six required
    fields untouched). Do NOT modify computeOpportunityBankState/listOpportunities (CONTEXT
    canonical_refs: do NOT modify the reader).

    Append behaviors 1-7 to tests/test-223-close-loop.cjs. Test 4 spawns the real
    scripts/compute-opportunity-state via child_process against the fixture room and asserts the
    rollup names the opportunity (integration leg, still hermetic - tmpdir room, no network).
  </action>
  <verify>
    <automated>node tests/test-223-close-loop.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-223-close-loop.cjs exits 0 with behaviors 1-7 asserted
    - Source assertion: grep -c "navigation.cjs" lib/core/close-loop-writer.cjs returns at least 1, and the comment-stripped grep for "INSERT INTO nodes\|INSERT INTO edges" over lib/core/close-loop-writer.cjs returns 0 (chokepoint only, no raw SQL writes)
    - Source assertion: grep -n "review_status" lib/core/close-loop-writer.cjs shows 'proposed' passed on semantic edge writes and NO review_status on the supersede path (D-02 + D-04)
    - The module header documents .md-FIRST node-SECOND ordering and names D-01
    - SELECT review_status FROM edges WHERE type='SUPERSEDES' on the 2-run fixture db returns NULL (asserted in test-223-supersedes-chain.cjs, Task 3)
    - grep -c "mindrian-designs" lib/core/close-loop-writer.cjs returns 0
  </acceptance_criteria>
  <done>Both surfaces have ONE writer to terminate through; Req 4's dual-store acceptance is provable; D-01/D-02/D-04 are implemented exactly as locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: walkSupersedesChain - the net-new read-side walker + the 2-run/1-run chain fixture</name>
  <files>lib/core/temporal/supersession.cjs, tests/test-223-supersedes-chain.cjs</files>
  <read_first>
    - lib/core/temporal/supersession.cjs (FULL file: supersede's write shape, the edge it mints, its validation idiom, module.exports = {supersede} at line 145 - the walker joins this module)
    - lib/core/navigation/edges.cjs lines 32-154 (edges table shape the walker SELECTs over: source, target, type, properties, review_status)
    - lib/core/close-loop-writer.cjs (Task 2's conclusion node shape: extraProps kind/topic_hash/run_id - what the fixture writes)
  </read_first>
  <behavior>
    - Test 1 (2-run fixture): simulate two same-topic runs at the lib level - run A writes a conclusion via writeCloseLoop; run B calls findPriorConclusion (finds A), then writeCloseLoop with priorConclusionId - the db holds exactly ONE SUPERSEDES edge (B -> A), its review_status column is NULL (D-04), and walkSupersedesChain(db, B) returns [B, A] in chain order (newest first)
    - Test 2 (1-run fixture): a single writeCloseLoop run produces ZERO SUPERSEDES edges (no false chain on a first run - SPEC Req 2 acceptance) and walkSupersedesChain on that conclusion returns a single-element chain
    - Test 3 (3-link chain): A <- B <- C walks as [C, B, A]; walking from the MIDDLE (B) still returns the full chain in order (the walker follows SUPERSEDES edges in both directions to the chain ends)
    - Test 4 (cycle guard): a manually-inserted cyclic SUPERSEDES pair terminates with a disclosed {ok:false, reason:'cycle_detected'} (or a truncated chain with a cycle flag), never an infinite loop
    - Test 5: the walk result is independent of node review_status (a confirmed conclusion and a proposed one appear identically in chain order) - D-04's "purely mechanical" semantics
  </behavior>
  <action>
    Extend lib/core/temporal/supersession.cjs ADDITIVELY with walkSupersedesChain(db, nodeId, opts):
    read-only SELECTs over the edges table (caller-owned handle, same discipline as supersede;
    reads are legal outside the write chokepoint). From nodeId, walk SUPERSEDES edges forward
    (edges where source = current: current supersedes target -> older) and backward (edges where
    target = current -> newer) to both chain ends, then return {ok:true, chain:[{node_id,
    superseded_at?}...]} ordered newest -> oldest. Guard with a visited set (cycle -> {ok:false,
    reason:'cycle_detected'} carrying the partial chain); cap depth via opts.maxDepth default 100.
    The walker ignores review_status entirely (D-04: the chain is bookkeeping, independent of
    confirmation state). Update module.exports to {supersede, walkSupersedesChain}. Do not touch
    supersede's body.

    Write tests/test-223-supersedes-chain.cjs covering behaviors 1-5, building throwaway rooms via
    buildFixtureRoom224 + openRoomDb and driving writeCloseLoop/findPriorConclusion from Task 2
    (this is the Req 2 lib-level proof; Plan 03 wires the same calls into bono's surface and
    --version-log renders this walker's output).
  </action>
  <verify>
    <automated>node tests/test-223-supersedes-chain.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-223-supersedes-chain.cjs exits 0 with behaviors 1-5 asserted
    - The 2-run leg asserts BOTH: exactly one row in SELECT COUNT(*) FROM edges WHERE type='SUPERSEDES', AND that row's review_status IS NULL (D-04)
    - The 1-run leg asserts zero SUPERSEDES rows
    - Source assertion: grep -c "walkSupersedesChain" lib/core/temporal/supersession.cjs returns at least 2 (definition + export)
    - Source assertion: comment-stripped grep for "INSERT\|UPDATE\|DELETE" over the walkSupersedesChain function body returns 0 (read-only walker)
  </acceptance_criteria>
  <done>The version-cut chain is provable end to end at the lib level: exactly one NULL-status SUPERSEDES edge per re-run, zero on first run, mechanical chain order for --version-log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI-composed payloads -> room.db typed nodes/edges | machine conclusions enter the human-trust surface; mislabeled status defeats Part 9 |
| close-loop writer -> opportunity-bank/*.md | the writer bridges two disconnected stores; inconsistency corrupts the bank's source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-05 | Elevation of Privilege | AI-proposed nodes auto-confirmed | mitigate | every node writer used is born-proposed with no status arg; confirmNode(byUser) is the only promotion path, re-proven in test (Part 9, WR-10) |
| T-223-06 | Tampering | dual-write inconsistency (node without .md or .md without node) | mitigate | D-01 ordering (.md FIRST) + shared artifact_id + crash-ordering test leg; a partial write is disclosed, never silent (SEED-059) |
| T-223-07 | Spoofing | AI-pipeline claims indistinguishable from human-filed claims | mitigate | G-1 provenance marker (extraProps pipeline) on every claim written by this spine (AI-SPEC Section 6) |
| T-223-08 | Tampering | SUPERSEDES misuse promoting/demoting proposals | mitigate | D-04: SUPERSEDES binds NULL review_status (not in the proposal lifecycle); WR-06 clobber guard upstream; walker is read-only |
| T-223-09 | Input Validation | oversized/unbounded conclusion payloads | mitigate | validateNarrative gates the conclusion (<=250 governing thought, 3-5 key claims) before any write; writer-local edge-type allow-list (ASVS V5) |
| T-223-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new dependencies (locked constraint); Plan 05's git-diff leg fails on any drift |
</threat_model>

<verification>
- node tests/test-223-close-loop.cjs exits 0
- node tests/test-223-supersedes-chain.cjs exits 0
- Regressions: node tests/test-graph-derive-sweep.cjs exits 0; bash tests/run-all-224.sh stays PASS=17 FAIL=0 (shared edges/derivation path); bash tests/run-all-164.sh stays 20/20 (writeClaimNode callers)
</verification>

<success_criteria>
- One writeCloseLoop call proves Req 4 against BOTH stores (room.db proposed nodes + bank rollup via the written-through .md)
- D-01 ordering and shared artifact_id are implemented and crash-tested; D-02 'proposed' on all semantic edges; D-04 NULL on SUPERSEDES
- The chain walker exists (net-new), cycle-guarded, mechanical, ready for Plan 03's --version-log
</success_criteria>

<output>
Create `.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-02-SUMMARY.md` when done, recording the artifact_id scheme chosen (Claude's discretion item), whether opportunity-ops needed the additive passthrough, and the writeCloseLoop payload contract Plans 03/04 consume.
</output>
