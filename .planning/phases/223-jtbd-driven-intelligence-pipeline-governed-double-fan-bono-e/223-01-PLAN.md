---
phase: 223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/bono/hat-governance.cjs
  - lib/core/bono/persona-research.cjs
  - tests/helpers/fixture-room-223.cjs
  - tests/test-223-hat-governance.cjs
  - tests/test-223-part8-egress.cjs
autonomous: true
requirements: ["Req 1"]
must_haves:
  truths:
    - "runDebate invoked with the governed seams produces measurably different behavior per hat: a Black argument that cites zero disconfirming evidence, or cites confirming before disconfirming, is caught by the governance layer before it folds forward"
    - "No two persona cells in one run share an identical lens (heterogeneity mandate), asserted by a fixture"
    - "A persona cannot cite a source outside its own wired INFORMS set - the citation-boundary check rejects the violation"
    - "A seeded LOCAL-content breach payload is rejected by part8-egress-guard.classify before any Brain-bound call; only the generic subdomain handle ever crosses"
    - "Hat governance is scoped to bono's debate logic only - nothing in the new modules hooks live conversation (the Phase 210 caution)"
  artifacts:
    - path: "lib/core/bono/hat-governance.cjs"
      provides: "Governance map (data) for 6 hats + 5 cross-cutting rules + enforcement selectors runDebate consumes via its existing injectable seams"
      exports: ["HAT_GOVERNANCE", "CROSS_CUTTING_RULES", "governanceForHat", "enforceGovernance", "assertHeterogeneity", "composeGovernedSeams"]
      min_lines: 120
    - path: "lib/core/bono/persona-research.cjs"
      provides: "Per-cell extractContext -> runSourceLens -> wireAccept research pipe injected as runCellFanout's dispatchCell; tracks each persona's wired source set"
      exports: ["personaDispatchCell", "validateCitations"]
      min_lines: 80
    - path: "tests/helpers/fixture-room-223.cjs"
      provides: "Phase-223 fixture room: buildFixtureRoom224 base + .mindrian/jtbd-state.json + MINTO.md + opportunity-bank/ dir"
      exports: ["buildFixtureRoom223"]
    - path: "tests/test-223-hat-governance.cjs"
      provides: "Req 1 proof: per-hat behavioral difference, heterogeneity mandate, citation boundary"
    - path: "tests/test-223-part8-egress.cjs"
      provides: "Part 8 proof: seeded breach rejected, generic handles only, classify wraps every Brain-bound path"
  key_links:
    - from: "lib/core/bono/hat-governance.cjs"
      to: "lib/core/bono/debate-composition.cjs"
      via: "composeGovernedSeams returns {deriveFn, selfCritiqueFn, onStep} matching runDebate's injectable option shape (verified seams, debate-composition.cjs:262-293)"
      pattern: "composeGovernedSeams"
    - from: "lib/core/bono/persona-research.cjs"
      to: "lib/core/bono/cell-fanout.cjs"
      via: "personaDispatchCell matches the dispatchCell(cell, ctx) contract (cell-fanout.cjs:237-256)"
      pattern: "personaDispatchCell"
    - from: "lib/core/bono/persona-research.cjs"
      to: "lib/core/findings-wirer.cjs"
      via: "accepted findings wired via wireAccept (EvidenceClaim proposed + INFORMS); returned node_ids become the persona's wired source set"
      pattern: "wireAccept"
    - from: "lib/core/bono/persona-research.cjs"
      to: "lib/core/part8-egress-guard.cjs"
      via: "classify() gates every Brain-bound payload, fail-closed"
      pattern: "part8-egress-guard"
---

<objective>
Ship the two net-new bono governance modules (Req 1): the hat-governance map encoding the
BUILD-BRIEF Section 5 scrutiny table plus the 5 cross-cutting rules, riding runDebate's EXISTING
injectable seams (deriveFn/onStep/gateFn/selfCritiqueFn - zero engine rebuild, per the verified
Part 7 seam map in 223-RESEARCH.md), and the persona-research wrapper that gives each
(subdomain x hat) cell its own world-of-knowledge via extractContext -> runSourceLens ->
wireAccept, injected as runCellFanout's dispatchCell seam.

Purpose: Requirement 1 is the phase's behavioral core - governed hats with per-persona web
research replace bono's undifferentiated hat behavior. Plans 03 (bono surface) and 04
(intel-pipeline) both stand on the modules and fixture built here.

Output: two new lib/core/bono/ modules, the shared phase fixture helper, and two green test legs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-SPEC.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-CONTEXT.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-RESEARCH.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-BUILD-BRIEF.md
@lib/core/bono/cell-fanout.cjs
@lib/core/bono/debate-composition.cjs
@lib/core/findings-wirer.cjs
@lib/core/part8-egress-guard.cjs
@tests/helpers/fixture-room-224.cjs
</context>

## Artifacts this phase produces (phase-wide contract)

| Artifact | Kind | Plan |
|----------|------|------|
| `lib/core/bono/hat-governance.cjs` (`HAT_GOVERNANCE`, `CROSS_CUTTING_RULES`, `governanceForHat`, `enforceGovernance`, `assertHeterogeneity`, `composeGovernedSeams`) | new module | 01 |
| `lib/core/bono/persona-research.cjs` (`personaDispatchCell`, `validateCitations`) | new module | 01 |
| `tests/helpers/fixture-room-223.cjs` (`buildFixtureRoom223`) | new test helper | 01 |
| `tests/test-223-hat-governance.cjs`, `tests/test-223-part8-egress.cjs` | test files | 01 |
| `lib/core/navigation/typed-open-question.cjs` (`writeOpenQuestionNode`) | new navigation submodule | 02 |
| `lib/core/navigation/typed-claim.cjs` optional `extraProps` bag | function contract extension | 02 |
| `lib/core/navigation.cjs` re-export of `writeOpenQuestionNode` | additive re-export | 02 |
| `lib/core/close-loop-writer.cjs` (`writeCloseLoop`, `findPriorConclusion`, `mintArtifactId`) | new shared-spine module | 02 |
| `lib/core/temporal/supersession.cjs::walkSupersedesChain` | function addition (net-new walker) | 02 |
| `tests/test-223-close-loop.cjs`, `tests/test-223-supersedes-chain.cjs` | test files | 02 |
| `commands/bono.md` 8-phase governed body + `hitl_stages` + `web_scope: green` | surface evolution | 03 |
| `skills/bono/SKILL.md` regenerated mirror (DESENSITIZE preserved) | generated mirror | 03 |
| bono `--version-log` (reads `walkSupersedesChain`) | command capability | 03 |
| `lib/core/intel-pipeline.cjs` (`runIntelPipeline`) | new composition module | 04 |
| `commands/intel-pipeline.md` (kind: meta connector) + `skills/intel-pipeline/SKILL.md` | new surface + generated mirror | 04 |
| `tests/test-223-intel-pipeline.cjs` | test file | 04 |
| `tests/run-all-223.sh` + `run-feynman-tests.cjs` registration | new harness + registration | 05 |
| `data/connector-registry.json`, `data/render-coverage-registry.json` regenerated | generated artifacts | 03/04/05 |
| `docs/CANON-PHASE-MAP.md` phase row + rethinking-mindrianos research entry | docs + compositing | 05 |
| Version cut via `scripts/release.sh` (navigator-gated) | release | 05 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hat-governance.cjs - the governance map + enforcement selectors riding runDebate's seams</name>
  <files>lib/core/bono/hat-governance.cjs, tests/test-223-hat-governance.cjs</files>
  <read_first>
    - lib/core/bono/debate-composition.cjs lines 247-322 (runDebate's injectable seams: deriveFn/onStep/gateFn/selfCritiqueFn/postureFn/onHalt; the default deriveFn shape a governed deriveFn must match - SYNC, array-returning)
    - lib/core/graph-derivation.cjs lines 148-191 (CR-01: runDerivation throws loudly on a Promise-returning deriveFn - the governed deriveFn must stay synchronous over pre-resolved data)
    - 223-BUILD-BRIEF.md Section 5 (the authoritative self-contained scrutiny table: per-hat governed behavior + discipline sources + the 5 cross-cutting rules)
    - .planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/ (skim the phase title/context: governance stays scoped to bono's debate logic, never a live-conversation hard-fail)
    - lib/core/bono/cell-fanout.cjs lines 195-260 (the cell/reading shape {stance, evidence, confidence} the enforcement functions validate)
  </read_first>
  <behavior>
    - Test 1: HAT_GOVERNANCE has exactly the six keys white/black/yellow/green/red/blue; each entry carries a discipline, a rules list, and a discipline_source; CROSS_CUTTING_RULES names all five (heterogeneity mandate, anti-premature-convergence, Key-Assumptions-Check-first, disconfirming-over-confirming weighting, strongest-model Blue judge)
    - Test 2: enforceGovernance('black', argument) returns {ok:false, violations:[...]} for an argument whose evidence cites confirming items before any disconfirming item, and for an argument with zero disconfirming evidence; returns {ok:true} for a disconfirming-first argument (per-hat measurable behavioral difference, SPEC Req 1 acceptance)
    - Test 3: enforceGovernance('white', argument) rejects an evidence item with no source/tier (cite-or-retract); enforceGovernance('red', argument) rejects an argument carrying a justification block (Red is gut-read, no justification); the same argument objects pass under other hats - proving the map differentiates behavior per hat
    - Test 4: assertHeterogeneity(cells) returns {ok:false} when two persona cells share an identical lens descriptor and {ok:true} on a heterogeneous set (SPEC Req 1 acceptance: no two persona cells in one run share an identical lens)
    - Test 5: composeGovernedSeams({hats}) returns {deriveFn, selfCritiqueFn, onStep} where deriveFn is SYNCHRONOUS (calling it returns an array, never a Promise - assert typeof result.then === 'undefined') and selfCritiqueFn routes each material step's output through enforceGovernance for that step's hat
    - Test 6: a runDebate invocation on an in-memory fixture (injected runChainFn/runDerivationFn/wireAcceptFn/wireRejectFn stubs, no db, no LLM) with composeGovernedSeams injected HALTS/flags a governance-violating black step and passes a compliant one - the seams verifiably change debate behavior
  </behavior>
  <action>
    Create lib/core/bono/hat-governance.cjs - DATA plus thin enforcement, not a runtime (223-RESEARCH:
    "the only genuinely net-new logic is the hat-governance map (data)"). Module header MUST state the
    Phase 210 caution verbatim intent: these rules govern bono's debate steps ONLY and are never wired
    into live-conversation enforcement; no hook, sensor, or command outside bono/intel-pipeline may
    require this module as a gate.

    Exports:
    1. HAT_GOVERNANCE - frozen object keyed white/black/yellow/green/red/blue, each entry
       {discipline, rules: [...], evidence_policy, discipline_source} encoding BUILD-BRIEF Section 5:
       white = sourced-only, evidence-tiered, no interpretation, cite-or-retract (De Bono; SAT);
       black = ACH disconfirming-evidence-first, steelman-then-attack, falsify-before-reject
       (Heuer/Pherson ACH; red-team); yellow = benefits/value, evidence-backed never hope (De Bono);
       green = provocations + alternatives, feeds hypotheses to Black (De Bono); red = gut read,
       timeboxed, no justification, surfaces the unspoken (De Bono); blue = strongest-model judge,
       MECE + parallel-thinking + anti-premature-convergence, bias-guarded (De Bono; multi-agent-debate
       research). Draft the prose from SPEC Requirement 1 + BUILD-BRIEF Section 5 per Requirement 6's
       fallback - the module must contain no reference to mindrian-designs.
    2. CROSS_CUTTING_RULES - frozen list of the five rules with ids: heterogeneity_mandate,
       anti_premature_convergence, key_assumptions_check_first, disconfirming_over_confirming,
       strongest_model_judge.
    3. governanceForHat(hat) - selector returning the entry or null (unknown hat never throws).
    4. enforceGovernance(hat, argument) - mechanical validator over the structured argument/reading
       shape ({stance, evidence:[{source, tier, disposition: 'disconfirming'|'confirming'|...}],
       confidence, justification?}): black requires at least one disconfirming item AND the first
       evidence citation to be disconfirming; white requires every evidence item to carry source+tier;
       yellow requires evidence-backed stance (non-empty evidence); red rejects justification prose;
       green requires at least one provocation/alternative marker; blue checks the anti-convergence
       flag (dissent recorded before ruling). Returns {ok, violations:[]} - violations are short
       scalar reason strings, never content bytes.
    5. assertHeterogeneity(cells) - fails when any two cells share an identical lens descriptor
       (the lens/persona identity string carried on assembled cells); returns {ok, duplicates:[]}.
    6. composeGovernedSeams(opts) - returns {deriveFn, selfCritiqueFn, onStep} matching runDebate's
       injectable option shapes exactly: deriveFn wraps the caller-supplied (or default per-hat
       CONVERGES) candidate producer and stays SYNCHRONOUS over pre-resolved cell data (CR-01:
       never return a Promise); selfCritiqueFn(stepResult) routes material per-hat steps through
       enforceGovernance and cross-cutting checks, returning the debate-composition critique contract
       (read the selfCritiqueFn consumption in debate-composition.cjs before shaping the return);
       onStep enforces Key-Assumptions-Check-first ordering (flags a debate whose first material step
       is not the assumptions surface). Accept an opts.deriveCandidateFn override so Plan 03's surface
       can supply stance-aware candidates.

    Write tests/test-223-hat-governance.cjs as a zero-deps node assert script (repo convention,
    mirror tests/test-224-classifier.cjs style) covering behaviors 1-6 with injected stubs only -
    no live LLM, no web, no db required for this task's legs (VALIDATION: debate legs use injected
    seams). Write the failing test legs FIRST, then implement until green.
  </action>
  <verify>
    <automated>node tests/test-223-hat-governance.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-223-hat-governance.cjs exits 0 with behaviors 1-6 asserted
    - grep -c "white\|black\|yellow\|green\|red\|blue" output aside: node -e "const g=require('./lib/core/bono/hat-governance.cjs'); const ks=Object.keys(g.HAT_GOVERNANCE); if(ks.length!==6) process.exit(1)" exits 0
    - node -e "const g=require('./lib/core/bono/hat-governance.cjs'); if(g.CROSS_CUTTING_RULES.length!==5) process.exit(1)" exits 0
    - Source assertion: grep -v '^\s*//' lib/core/bono/hat-governance.cjs | grep -c "mindrian-designs" returns 0 AND grep -c "mindrian-designs" lib/core/bono/hat-governance.cjs returns 0 (Req 6: not even in comments)
    - Source assertion: grep -E "fetch\(|https?://|node:http|require\('http" lib/core/bono/hat-governance.cjs returns nothing on executable lines (pure data + validation, zero network surface)
    - The module header names the Phase 210 scope caution (grep -c "debate" lib/core/bono/hat-governance.cjs returns at least 1 in the header block)
  </acceptance_criteria>
  <done>The governance map exists as frozen data + mechanical enforcement; a governed runDebate run behaves measurably differently per hat and the heterogeneity mandate is fixture-proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: persona-research.cjs - per-cell world-of-knowledge via the dispatchCell seam</name>
  <files>lib/core/bono/persona-research.cjs, tests/helpers/fixture-room-223.cjs</files>
  <read_first>
    - lib/core/bono/cell-fanout.cjs lines 195-260 (the dispatchCell(cell, ctx) seam this module fills; cell = {subdomain, hat, handle}; the default is researchCorpus.fetchCorpus - persona-research is genuinely NEW wiring per RESEARCH A2)
    - lib/core/research-context-extractor.cjs lines 253-283 (extractContext signature + computeLensSet)
    - lib/lens-engine/source-lens-driver.cjs lines 422-719 (runSourceLens: web-research ingestion -> ranked findings; identify the injectable/test seams it exposes)
    - lib/core/findings-wirer.cjs lines 133-175 (wireAccept(db, params): params.finding {title, source, url, retrieved_at, evidence_tier, summary}, params.decision {target_section, target_kind}, topic, sessionId -> {ok, node_id})
    - lib/core/part8-egress-guard.cjs lines 223-274 (classify(payload, {toolName}) -> {verdict:'allow'|'block'|'ambiguous'}; default-deny, reason carries no offending bytes)
    - tests/helpers/fixture-room-224.cjs lines 211-280 (buildFixtureRoom224 shape this phase's fixture wraps)
  </read_first>
  <behavior>
    - Test 1 (in test-223-hat-governance.cjs, persona section): personaDispatchCell run over a cell with injected extractContextFn/runSourceLensFn/wireAcceptFn stubs returns a normalized reading {stance, evidence, confidence, wired_sources} where wired_sources equals exactly the node_ids the wireAccept stub returned - the persona's world-of-knowledge is its own wired INFORMS set
    - Test 2: validateCitations(reading) returns {ok:false, violations} when reading.evidence cites a source id NOT in reading.wired_sources, and {ok:true} when every citation is inside the set (SPEC Req 1 acceptance: a persona may not assert beyond its own wired sources)
    - Test 3: two cells dispatched over the same subdomain with different hats produce DIFFERENT lens scopes (the hat feeds the lens selection), asserted via the injected extractContextFn call args
    - Test 4: a runSourceLensFn stub returning zero accepted findings produces a reading with empty wired_sources and a disclosed degraded marker (SEED-059: the thin-world state is structural, never silent)
  </behavior>
  <action>
    Create lib/core/bono/persona-research.cjs. Exports:

    1. personaDispatchCell(cell, ctx) - the runCellFanout dispatchCell-contract function (async is
       fine here; cell-fanout awaits dispatchCell). Per cell: (a) call extractContext scoped to
       cell.subdomain + hat-driven lens selection (injectable via ctx.extractContextFn, default
       research-context-extractor.extractContext); (b) run the web leg via ctx.runSourceLensFn
       (default source-lens-driver.runSourceLens) with the GENERIC subdomain handle cell.handle only -
       never room content - in any outbound query construction; (c) wire each accepted finding via
       ctx.wireAcceptFn (default findings-wirer.wireAccept) as EvidenceClaim proposed + INFORMS,
       collecting returned node_ids into wired_sources; (d) return the normalized reading
       {stance, evidence, confidence, wired_sources, degraded?}. ctx also carries db, roomDir, topic,
       sessionId for the wireAccept params. A missing db degrades to wire-skipped with a disclosed
       marker (SEED-059), never a throw - cell-fanout's defensive stub is the outer net.
    2. validateCitations(reading) - the citation-boundary check: every evidence citation's source id
       must be inside reading.wired_sources. Exported separately so hat-governance's selfCritiqueFn
       (Plan 03 wiring) and tests can call it directly.

    Part 8 (SIGNAL -> LOCAL only): web research INGESTS public content into LOCAL EvidenceClaims -
    legal direction. Any Brain-bound payload (e.g. generic dimension hints) MUST pass
    part8-egress-guard.classify first (injectable ctx.classifyFn, default the real classify) and
    proceed only on verdict 'allow'; 'block'/'ambiguous' means the Brain leg is skipped with a
    disclosed marker. The module must never place room content into an outbound query string -
    only cell.handle (the generic domain handle cell-fanout already mints).

    Create tests/helpers/fixture-room-223.cjs exporting buildFixtureRoom223(tmpDir, opts): wrap
    buildFixtureRoom224 (Part 7 reuse - do not duplicate its artifact-writing logic), then add the
    223-specific substrate: .mindrian/jtbd-state.json (a current JTBD entry shaped per
    lib/hmi/jtbd-state.cjs getCurrent's return contract), MINTO.md at the room root, and an empty
    opportunity-bank/ directory. Return the base fixture's fields plus {jtbdPath, bankDir}.
    Plans 03/04 consume this helper; keep opts pass-through (artifactCount).

    Add the persona-research behaviors as a clearly-labeled section of
    tests/test-223-hat-governance.cjs (Req 1 has ONE test file per 223-VALIDATION's map). All legs
    use injected seams - never live web (VALIDATION Wave 0 rule).
  </action>
  <verify>
    <automated>node tests/test-223-hat-governance.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-223-hat-governance.cjs exits 0 including the persona-research section (behaviors 1-4)
    - Source assertion: grep -c "part8-egress-guard" lib/core/bono/persona-research.cjs returns at least 1 (the guard is required, not optional)
    - Source assertion: grep -E "fetch\(|https?://|node:http|curl|wget" lib/core/bono/persona-research.cjs on executable lines (strip comment lines first: grep -v '^\s*//' then grep -v '^\s*\*') returns nothing - the web leg is DELEGATED to runSourceLens, never hand-rolled
    - node -e "const f=require('./tests/helpers/fixture-room-223.cjs'); if(typeof f.buildFixtureRoom223!=='function') process.exit(1)" exits 0
    - Source assertion: grep -c "fixture-room-224" tests/helpers/fixture-room-223.cjs returns at least 1 (wraps, never duplicates)
    - grep -c "mindrian-designs" lib/core/bono/persona-research.cjs returns 0
  </acceptance_criteria>
  <done>Each (subdomain x hat) cell can carry its own wired world-of-knowledge; the citation boundary is mechanically checkable; the shared phase fixture exists for Plans 03/04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Part 8 egress proof - seeded breach rejected, generic handles only</name>
  <files>tests/test-223-part8-egress.cjs</files>
  <read_first>
    - lib/core/part8-egress-guard.cjs lines 223-274 (classify verdicts; the default-deny content scan; reason carries no offending bytes)
    - lib/core/bono/persona-research.cjs (as built in Task 2 - the classify integration points and the injectable ctx seams)
    - tests/run-all-224.sh (the Part 8 sweep idiom: strip_comments before grep; the extended primitive list fetch(/http(s)/node:http/curl/wget)
  </read_first>
  <behavior>
    - Test 1: part8-egress-guard.classify rejects a seeded LOCAL-content breach payload (a payload string carrying fixture room content, e.g. an artifact body slice from buildFixtureRoom223) - verdict is 'block' or 'ambiguous', NEVER 'allow' (SPEC Req 5's egress acceptance, proven at the unit level here)
    - Test 2: a personaDispatchCell run with a recording classifyFn stub shows every Brain-bound payload passed through classify BEFORE dispatch, and each such payload contains only the generic handle (assert the recorded payloads carry cell.handle and do NOT contain any fixture room content string)
    - Test 3: when classifyFn returns {verdict:'block'}, the Brain leg is skipped, the reading carries a disclosed degraded marker, and NO Brain call fires (the injected brain stub records zero invocations) - fail-closed, disclosed per SEED-059
    - Test 4 (static leg): the comment-stripped grep sweep over lib/core/bono/hat-governance.cjs and lib/core/bono/persona-research.cjs finds no raw network primitive (fetch(, http(s) URL literals, require of node:http/https, curl, wget) - MISSING target file fails the leg (the run-all-224 T-224-15 idiom)
  </behavior>
  <action>
    Write tests/test-223-part8-egress.cjs as a zero-deps node assert script combining the unit legs
    (behaviors 1-3, driven through the injectable ctx seams from Task 2 - no live Brain, no live web)
    and the static sweep leg (behavior 4) in one file, since 223-VALIDATION maps the Part 8 row to
    exactly this file. Build the seeded breach payload from fixture content so the test proves the
    guard rejects REAL room bytes, not a synthetic marker the guard special-cases. For behavior 4,
    reimplement the strip-comments idiom locally (copy the sed/grep approach from run-all-224.sh)
    and iterate over an explicit target list that FAILS if a target file is missing.
  </action>
  <verify>
    <automated>node tests/test-223-part8-egress.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-223-part8-egress.cjs exits 0 with all four behaviors asserted
    - The seeded-breach leg asserts verdict !== 'allow' (not merely that classify ran)
    - The static sweep hard-fails on a missing target file (verified by the leg's own structure: an explicit existsSync check that exits 1)
    - No test leg performs live network or LLM calls (structural: the file requires no http client and sets no fetch)
  </acceptance_criteria>
  <done>Part 8 is proven at the module boundary: LOCAL content cannot ride a Brain call from the new research path, and the proof is a permanent test file Plan 05's harness aggregates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public web (SIGNAL) -> room.db (LOCAL) | untrusted web-research findings enter the room as EvidenceClaim nodes via wireAccept |
| persona modules -> Brain MCP | only generic framework/domain handles may cross; LOCAL bytes never |
| governance layer -> debate flow | machine enforcement over AI-composed arguments; over-reach would repeat the Phase 210 mistake |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-01 | Information Disclosure | persona-research Brain-bound payloads | mitigate | part8-egress-guard.classify fail-closed on every Brain leg; generic handle only in outbound queries; test-223-part8-egress.cjs seeded-breach leg |
| T-223-02 | Tampering | web findings ingested as EvidenceClaims | mitigate | injection-as-data: findings are wired as proposed nodes through wireAccept's validation, never executed or auto-confirmed; citation boundary limits blast radius per persona |
| T-223-03 | Denial of Service | unbounded persona research fan | mitigate | runCellFanout's FUTURES_FANOUT_CAP clamp + planDispatch budget are upstream and untouched; persona-research adds no second fan loop |
| T-223-04 | Elevation of Privilege | governance rules leaking into live conversation | mitigate | Phase 210 caution documented in module header; no hook/sensor wiring in this plan; Plan 05's projection/coverage gates would surface any wiring drift |
| T-223-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new dependencies (locked constraint); Plan 05's git-diff leg on package.json/package-lock.json fails the harness on any drift |
</threat_model>

<verification>
- node tests/test-223-hat-governance.cjs exits 0
- node tests/test-223-part8-egress.cjs exits 0
- Regression: bash tests/run-all-164.sh still passes 20/20 (bono base untouched - this plan only ADDS modules; no shipped file is modified)
</verification>

<success_criteria>
- Governed per-hat behavior is fixture-proven different (Black disconfirming-first; Red no-justification; White cite-or-retract)
- Heterogeneity mandate and citation boundary are mechanical checks with green legs
- Part 8 seeded breach is rejected; generic handles only; zero raw network primitives in the new modules
- buildFixtureRoom223 exists for Plans 03/04
</success_criteria>

<output>
Create `.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-01-SUMMARY.md` when done, recording the governance-map encoding choices (Claude's discretion item from CONTEXT) and the exact seam shapes composeGovernedSeams returns.
</output>
