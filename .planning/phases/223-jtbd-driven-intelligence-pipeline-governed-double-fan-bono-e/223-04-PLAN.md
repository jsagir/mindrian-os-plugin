---
phase: 223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono
plan: 04
type: execute
wave: 3
depends_on: ["223-01", "223-02", "223-03"]
files_modified:
  - lib/core/intel-pipeline.cjs
  - commands/intel-pipeline.md
  - skills/intel-pipeline/SKILL.md
  - data/connector-registry.json
  - tests/test-223-intel-pipeline.cjs
autonomous: true
requirements: ["Req 3", "Req 4", "Req 6"]
must_haves:
  truths:
    - "A --dry-run against a fixture room emits the correct phase/fan plan WITHOUT dispatching any research (zero researchFn invocations)"
    - "A real (seam-injected) run produces at least one claim node through the full calibrate -> decompose -> fan -> compute -> synthesize -> close-the-loop pipeline"
    - "The run halts at each of the 3 declared hitl_stages in order: calibrate (F.1), fan-approve (F.1), synthesize (F.5)"
    - "A quality: low research pass mid-fan forces a HALT with a structural disclosure (SEED-059), never a silent continue"
    - "jtbd-state.setCurrent is called exactly once (at the calibrate gate); findings NEVER silently re-write JTBD state - divergence is surfaced at the F.5 gate (G-2, the SPEC addendum's reinforcing-loop guardrail)"
    - "The compute step invokes the eureka measured legs (scoreMeasured / eureka-room-report path) and NEVER compute-hsi.py or any Python (D-03)"
    - "intel-pipeline terminates through writeCloseLoop - same D-01 dual write, same D-02 proposed edges as bono (Req 4, one shared contract)"
  artifacts:
    - path: "lib/core/intel-pipeline.cjs"
      provides: "The testable composition core the command drives: staged runner with injectable seams, dry-run mode, gate halts, quality-low HALT"
      exports: ["runIntelPipeline", "PIPELINE_STAGES"]
      min_lines: 150
    - path: "commands/intel-pipeline.md"
      provides: "The kind: meta surface (sibling of /mos:act): reach_id context_block, sub_mode intel-pipeline, posture hold, autonomous_safe false, hitl_stages declared"
      contains: "hitl_stages"
    - path: "skills/intel-pipeline/SKILL.md"
      provides: "Generated mirror via build-skill-mirrors.cjs"
    - path: "tests/test-223-intel-pipeline.cjs"
      provides: "Req 3 + Req 4 (intel-pipeline side) proof with injected seams - no live LLM/web"
  key_links:
    - from: "commands/intel-pipeline.md"
      to: "lib/core/intel-pipeline.cjs"
      via: "the command prose drives runIntelPipeline stage by stage, rendering the F.1/F.1/F.5 gates as cards"
      pattern: "runIntelPipeline|intel-pipeline.cjs"
    - from: "lib/core/intel-pipeline.cjs"
      to: "lib/hmi/jtbd-state.cjs"
      via: "getCurrent at calibrate; setCurrent ONCE after the calibrate gate; never again (G-2)"
      pattern: "jtbd-state"
    - from: "lib/core/intel-pipeline.cjs"
      to: "lib/core/close-loop-writer.cjs"
      via: "the close stage is ONE writeCloseLoop call with surface 'intel-pipeline' (Req 4 shared contract)"
      pattern: "writeCloseLoop"
    - from: "lib/core/intel-pipeline.cjs"
      to: "lib/core/rs-differential-scorer.cjs"
      via: "compute stage default = scoreMeasured / eureka-room-report entry (D-03)"
      pattern: "rs-differential-scorer|eureka-room-report"
---

<objective>
Ship /mos:intel-pipeline (Req 3): a new kind: meta surface (sibling of /mos:act, modeled on
act.md's verified connector block) that runs calibrate -> decompose -> fan research -> compute ->
consolidate -> synthesize -> write-to-graph against any room, oriented by that room's active JTBD.
The deterministic legs live in a NEW thin composition core (lib/core/intel-pipeline.cjs) with
injectable seams so Req 3's acceptance (dry-run plan, 3-gate halts, quality-low HALT, one claim
through the full loop) is fixture-provable - the same pattern bono uses (prose drives lib engines;
the engines are testable). Compute rides the eureka measured legs per D-03, never compute-hsi.py.

Purpose: the second born-wired surface, closing Req 3 and exercising Req 4's shared contract from
a second caller. Serialized after Plan 03 because both regenerate the mirror/registry artifacts.

Output: the composition core, the command + generated SKILL mirror, the registry's 2 new entries,
and the Req 3 test leg green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-SPEC.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-CONTEXT.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-RESEARCH.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-02-SUMMARY.md
@commands/act.md
@lib/hmi/jtbd-state.cjs
@lib/core/dispatch-optimizer.cjs
@lib/core/close-loop-writer.cjs
@tests/helpers/fixture-room-223.cjs
</context>

## Artifacts this phase produces (phase-wide contract)

| Artifact | Kind | Plan |
|----------|------|------|
| `lib/core/bono/hat-governance.cjs` (`HAT_GOVERNANCE`, `CROSS_CUTTING_RULES`, `governanceForHat`, `enforceGovernance`, `assertHeterogeneity`, `composeGovernedSeams`) | new module | 01 |
| `lib/core/bono/persona-research.cjs` (`personaDispatchCell`, `validateCitations`) | new module | 01 |
| `tests/helpers/fixture-room-223.cjs` (`buildFixtureRoom223`) | new test helper | 01 |
| `tests/test-223-hat-governance.cjs`, `tests/test-223-part8-egress.cjs` | test files | 01 |
| `lib/core/navigation/typed-open-question.cjs` (`writeOpenQuestionNode`) | new navigation submodule | 02 |
| `lib/core/navigation/typed-claim.cjs` optional `extraProps` bag | function contract extension | 02 |
| `lib/core/navigation.cjs` re-export of `writeOpenQuestionNode` | additive re-export | 02 |
| `lib/core/close-loop-writer.cjs` (`writeCloseLoop`, `findPriorConclusion`, `mintArtifactId`) | new shared-spine module | 02 |
| `lib/core/temporal/supersession.cjs::walkSupersedesChain` | function addition (net-new walker) | 02 |
| `tests/test-223-close-loop.cjs`, `tests/test-223-supersedes-chain.cjs` | test files | 02 |
| `commands/bono.md` 8-phase governed body + `hitl_stages` + `web_scope: green` | surface evolution | 03 |
| `skills/bono/SKILL.md` regenerated mirror (DESENSITIZE preserved) | generated mirror | 03 |
| bono `--version-log` (reads `walkSupersedesChain`) | command capability | 03 |
| `lib/core/intel-pipeline.cjs` (`runIntelPipeline`) | new composition module | 04 |
| `commands/intel-pipeline.md` (kind: meta connector) + `skills/intel-pipeline/SKILL.md` | new surface + generated mirror | 04 |
| `tests/test-223-intel-pipeline.cjs` | test file | 04 |
| `tests/run-all-223.sh` + `run-feynman-tests.cjs` registration | new harness + registration | 05 |
| `data/connector-registry.json`, `data/render-coverage-registry.json` regenerated | generated artifacts | 03/04/05 |
| `docs/CANON-PHASE-MAP.md` phase row + rethinking-mindrianos research entry | docs + compositing | 05 |
| Version cut via `scripts/release.sh` (navigator-gated) | release | 05 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/core/intel-pipeline.cjs - the staged composition core with injectable seams</name>
  <files>lib/core/intel-pipeline.cjs, tests/test-223-intel-pipeline.cjs</files>
  <read_first>
    - lib/hmi/jtbd-state.cjs lines 83-220 (getCurrent/setCurrent contract; the manualOverrideActive window that BLOCKS non-manual setCurrent - relevant to the G-2 leg)
    - lib/core/dispatch-optimizer.cjs lines 214-282 (planDispatch(roomDir, options) -> budget block: the fan-sizing source)
    - lib/core/research-context-extractor.cjs lines 253-283 + lib/lens-engine/source-lens-driver.cjs lines 422-719 (the research pass pipe: extractContext -> runSourceLens; identify the quality field a pass reports)
    - lib/core/findings-wirer.cjs lines 133-175 (wireAccept params - the per-pass wiring call)
    - lib/core/rs-differential-scorer.cjs lines 483-595 (scoreMeasured async signature; encoder_unavailable disclosed honestly) and scripts/eureka-room-report.cjs lines 286-340 (the per-room HSI recompute entry: --db <roomDir> --offline)
    - lib/core/close-loop-writer.cjs (Plan 02's writeCloseLoop payload contract + opts.surface)
    - lib/core/part8-egress-guard.cjs lines 223-274 (classify - gates the optional generic Brain dims in decompose)
    - 223-SPEC.md Addendum final paragraph + 223-AI-SPEC.md Section 6 G-2 (the JTBD reinforcing loop and its required guardrail)
  </read_first>
  <behavior>
    - Test 1 (dry-run): runIntelPipeline({roomDir, dryRun:true, ...injected seams}) returns {ok:true, dry_run:true, plan:{stages, jtbd, dimensions, fan:{requested,planned,budget}}} and the injected researchFn/computeFn/writeFn record ZERO invocations (SPEC Req 3 acceptance)
    - Test 2 (3-gate halts): with a recording gateFn that approves each gate, the recorded halt sequence is exactly ['calibrate','fan-approve','synthesize'] with shapes ['F.1','F.1','F.5']; with a gateFn that rejects at fan-approve, no research pass fires and the run returns a disclosed halted state
    - Test 3 (quality-low HALT): a researchFn stub returning quality:'low' on pass 2 of 3 HALTS the fan mid-flight with a structural disclosure naming the pass (SEED-059), and no further passes dispatch
    - Test 4 (full loop, Req 3 + Req 4): with approving gates and healthy stubs, the run lands at least one claim node in the fixture room.db via writeCloseLoop (proposed, pipeline extraProps 'intel-pipeline'), plus the bank .md when an opportunity is in the synthesized payload - asserted against BOTH stores
    - Test 5 (G-2, no silent JTBD re-write): a spying jtbd setCurrent records EXACTLY ONE call (at calibrate, after the gate approves); a findings set diverging from the calibrated JTBD cues produces a divergence disclosure in the synthesize gate payload, NOT a second setCurrent call
    - Test 6 (D-03 structural): the comment-stripped grep over lib/core/intel-pipeline.cjs for "compute-hsi|\.py|python|spawn.*python" returns nothing; the compute stage default references scoreMeasured / eureka-room-report by name
  </behavior>
  <action>
    Create lib/core/intel-pipeline.cjs - a thin STAGE COMPOSER, not a new loop runtime (Part 7:
    every stage delegates to a shipped engine; the only new logic is sequencing, gating, and
    disclosure). Exports PIPELINE_STAGES (frozen ordered list: calibrate, decompose, plan-fan,
    fan, compute, synthesize, close) and async runIntelPipeline(opts):

    - opts: {roomDir, db, topic?, dryRun, gateFn, onHalt, jtbdFns?, planFn?, researchFn?,
      computeFn?, writeFn?, classifyFn?, bankRollupFn?, caps?}. Every seam has a production
      default (jtbd-state getCurrent/setCurrent, dispatch-optimizer planDispatch, the
      extractContext -> runSourceLens -> wireAccept pipe, scoreMeasured + the eureka-room-report
      entry, close-loop-writer writeCloseLoop, part8-egress-guard classify, a child_process spawn
      of bash scripts/compute-opportunity-state). Tests inject all of them (VALIDATION: never live
      LLM/web).
    - calibrate: read room STATE.md/MINTO.md presence + jtbdFns.getCurrent(roomDir); HALT at the
      calibrate gate (gateFn({stage:'calibrate', shape:'F.1', jtbd, evidence})); on approve, call
      jtbdFns.setCurrent ONCE with {trigger:'intel-pipeline-calibrate', manual:false} - and NEVER
      call setCurrent again anywhere in the flow (G-2; document the reinforcing-loop rationale in
      a header comment citing the SPEC addendum). A blocked setCurrent (manual-override window) is
      disclosed and the run continues on the existing JTBD.
    - decompose: derive research dimensions from the calibrated JTBD's cues (a small local
      mapping - JTBD verbs to dimension handles); optional generic Brain dims ride ONLY through
      classifyFn verdict 'allow' with generic handles (Part 8).
    - plan-fan: planFn(roomDir, {remainingContext}) sizes N; clamp via opts.caps (default LOW -
      RESEARCH Open Question 2: cap per-persona/per-dimension fan low; the fan-approve gate is the
      navigator-facing cost control, not a hidden limit); HALT at the fan-approve gate
      (shape F.1, carrying {dimensions, N, budget}).
    - fan: dispatch the N research passes SEQUENTIALLY or via bounded Promise.all through
      researchFn(dimension, ctx) (default pipe: extractContext -> runSourceLens -> wireAccept per
      pass); each pass returns {findings, wired_sources, quality}; quality 'low' HALTS the whole
      fan with a disclosure naming the failing pass (onHalt + structural marker in the return).
    - compute: computeFn default = the D-03 eureka measured legs (scoreMeasured over
      finding/artifact pairs where vectors are available; the per-room HSI recompute via the
      eureka-room-report entry, --offline tolerated with encoder_unavailable disclosed per
      SEED-059). NEVER compute-hsi.py, NEVER any Python spawn (D-03 overrides BUILD-BRIEF
      Section 3 - RESEARCH staleness correction 2).
    - synthesize: assemble the bull/bear + ACH-skeptic payload (claims, relations, killed,
      conclusion candidate, knowns/unknowns, opportunities) + the G-2 divergence check (compare
      finding dimensions against the calibrated JTBD cues; attach {jtbd_divergence} to the gate
      payload when they disagree); HALT at the synthesize gate (shape F.5).
    - close: ONE writeFn call (writeCloseLoop with opts.surface 'intel-pipeline' - Req 4's shared
      contract, D-01 + D-02 inside the writer), then bankRollupFn(roomDir)
      (compute-opportunity-state) so the bank is aware.
    - dryRun: run calibrate-read + decompose + plan-fan PLANNING ONLY (no gate calls, no
      setCurrent, no researchFn/computeFn/writeFn) and return the emitted plan.
    - Return shape: {ok, dry_run?, halted?, halt_stage?, stages:[{stage, status, disclosure?}],
      plan, written?} - every degraded/halted stage discloses structurally (SEED-059).

    Write tests/test-223-intel-pipeline.cjs covering behaviors 1-6 against buildFixtureRoom223
    (Plan 01's helper: room.db + jtbd-state.json + MINTO.md + opportunity-bank/). All seams
    injected; Test 4 is the only leg touching room.db and the bank dir, and it spawns the real
    compute-opportunity-state for the both-stores assertion.
  </action>
  <verify>
    <automated>node tests/test-223-intel-pipeline.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-223-intel-pipeline.cjs exits 0 with behaviors 1-6 asserted
    - Source assertion: comment-stripped grep for "compute-hsi|python" over lib/core/intel-pipeline.cjs returns 0 (D-03)
    - Source assertion: grep -c "setCurrent" lib/core/intel-pipeline.cjs shows exactly one call site (G-2), with the header comment naming the JTBD reinforcing loop
    - Source assertion: grep -c "writeCloseLoop\|close-loop-writer" lib/core/intel-pipeline.cjs returns at least 1 and comment-stripped grep for "INSERT INTO" returns 0 (chokepoint discipline: the core never writes raw SQL)
    - grep -c "mindrian-designs" lib/core/intel-pipeline.cjs returns 0
  </acceptance_criteria>
  <done>The full calibrate-through-write loop is executable and fixture-proven: dry-run plan, 3 ordered gate halts, quality-low HALT, one claim through the loop into BOTH stores, JTBD never silently re-written.</done>
</task>

<task type="auto">
  <name>Task 2: commands/intel-pipeline.md + generated SKILL mirror + born-wired registry entries</name>
  <files>commands/intel-pipeline.md, skills/intel-pipeline/SKILL.md, data/connector-registry.json</files>
  <read_first>
    - commands/act.md lines 1-70 (the ENTIRE model: kind meta frontmatter, hitl_stages list format, the connector block incl. reach_id context_block / framework null / filing memory_event_only / posture hold, and the connector rationale comments)
    - scripts/build-connector-registry.cjs (how entries are derived from command + skill files; the CONN checks; how hierarchy_rank collisions are treated - pick the next free rank by grepping existing connector blocks)
    - scripts/build-skill-mirrors.cjs lines 25-52 (mirror generation; intel-pipeline's sensor_triggers is [] on BOTH files - nothing to desensitize, unlike bono)
    - scripts/check-shape-declaration.cjs lines 161-228 (the hitl_stages gate contract the new surface must satisfy)
    - lib/core/intel-pipeline.cjs (Task 1's exports and gate payload shapes the prose renders)
  </read_first>
  <action>
    Create commands/intel-pipeline.md, modeled on act.md (the verified kind: meta reference):
    - Frontmatter: name intel-pipeline; description + help_jtbd + teaching in Larry voice
      (Feynman, JTBD-oriented); kind: meta; frameworks: []; produces: null; autonomous_safe: false;
      hitl_stages in act.md's list format: calibrate (F.1, gate), fan-approve (F.1, gate),
      synthesize (F.5, gate) + hitl_why naming the three cost/trust gates; allowed-tools mirroring
      act.md's set.
    - Connector block (author, then REGENERATE - never hand-trusted): connects_to_spine: true,
      sensor_triggers: [], reach_id: context_block (existing frozen reach - NOT a new reach),
      sub_mode: intel-pipeline, framework: null, posture: hold, filing: memory_event_only (the
      legal meta-orchestrator shape per act.md's connector rationale), plan_gated: false,
      web_scope: green (the fan's research passes are SIGNAL->LOCAL web ingestion), surface: F.1,
      hierarchy_rank: the next free rank (grep existing connector blocks first). Include the
      connector rationale comment block in act.md's style: a standing meta surface offered by the
      spine, navigator confirms at F.1, no 7th reach, no second selection brain.
    - Body (drafted per Req 6's fallback from SPEC Req 3 + this plan's core - zero mindrian-designs
      references): the 7-stage flow as directive prose driving runIntelPipeline stage by stage;
      --dry-run documented (emit the plan, dispatch nothing); the three gates rendered as cards;
      quality:'low' HALT disclosed; compute = eureka measured legs (D-03); termination through
      writeCloseLoop + compute-opportunity-state (Req 4); Part 8/Part 9 constitutional callouts
      (generic handles only; everything proposed; confirmNode(byUser) is the only promotion).
      Out-of-scope named: single-room only (portfolio fan is a deferred follow-on).

    Then: node scripts/build-skill-mirrors.cjs (generates skills/intel-pipeline/SKILL.md);
    node scripts/build-connector-registry.cjs && node scripts/build-connector-registry.cjs --check.
    The registry must now carry EXACTLY 2 new entries vs Plan 03's state (the intel-pipeline
    command + its mirror), zero changed reach_ids on any existing surface, bono's tuple untouched
    (Req 5's diff criterion, finalized and evidence-recorded in Plan 05).
    Also run node scripts/check-shape-declaration.cjs (advisory) and confirm intel-pipeline is NOT
    among the violations (hitl_stages declared).
  </action>
  <verify>
    <automated>bash -c 'node scripts/build-connector-registry.cjs --check && grep -q "kind: meta" commands/intel-pipeline.md && grep -q "reach_id: context_block" commands/intel-pipeline.md && grep -q "sub_mode: intel-pipeline" commands/intel-pipeline.md && grep -q "hitl_stages" commands/intel-pipeline.md && test -f skills/intel-pipeline/SKILL.md && ! grep -rq "mindrian-designs" commands/intel-pipeline.md skills/intel-pipeline/ && echo INTEL-WIRING-OK'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints INTEL-WIRING-OK
    - build-connector-registry.cjs --check exits 0; grep -c "intel-pipeline" data/connector-registry.json shows exactly the 2 new entries (command + mirror)
    - node scripts/check-shape-declaration.cjs lists NO intel-pipeline violation (hitl_stages present with the 3 stages)
    - grep -q "autonomous_safe: false" commands/intel-pipeline.md (the spine OFFERS; the navigator confirms - elevation-of-privilege mitigation)
    - No em-dash: grep -cP "\x{2014}" commands/intel-pipeline.md returns 0
    - node tests/test-223-intel-pipeline.cjs still exits 0 (wiring did not break the core)
  </acceptance_criteria>
  <done>/mos:intel-pipeline exists, is born-wired on the frozen context_block reach with 3 declared hitl_stages, and its mirror + registry entries are machine-generated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public web (SIGNAL) -> room.db (LOCAL) | N parallel research passes ingest untrusted content as EvidenceClaims |
| meta-orchestrator -> room state (JTBD, graph, bank) | an autonomous loop writing state without human gates would defeat Parts 3/9 |
| decompose stage -> Brain MCP | only generic dimension handles may cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-14 | Elevation of Privilege | pipeline running autonomously end to end | mitigate | autonomous_safe: false + 3 blocking hitl_stages (calibrate/fan-approve/synthesize); reject at any gate stops the run, fixture-proven |
| T-223-15 | Denial of Service | unbounded research fan (cost) | mitigate | planDispatch budget + low default caps + the fan-approve gate as the explicit navigator cost control (SPEC constraint) |
| T-223-16 | Information Disclosure | LOCAL content in Brain-bound dimension queries | mitigate | classify fail-closed on the decompose Brain leg; generic handles only; Plan 05's Part 8 sweep + Plan 01's egress test cover the shared pipe |
| T-223-17 | Tampering | findings silently re-writing JTBD state (reinforcing loop) | mitigate | G-2: setCurrent exactly once at calibrate; divergence surfaced at F.5, never auto-written; fixture leg asserts the single call |
| T-223-18 | Repudiation | silent mid-fan degradation | mitigate | quality:'low' HALTs with a structural disclosure naming the pass (SEED-059); halted stages carried in the return shape |
| T-223-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new dependencies; Plan 05 git-diff leg fails on any drift; D-03 additionally removes the Python surface entirely |
</threat_model>

<verification>
- node tests/test-223-intel-pipeline.cjs exits 0
- node scripts/build-connector-registry.cjs --check exits 0 with the 2 new entries
- Regressions: node tests/test-223-close-loop.cjs, node tests/test-223-hat-governance.cjs exit 0
- Manual (deferred to phase verification per 223-VALIDATION): run /mos:intel-pipeline live against a scratch room and confirm the 3 gates fire as cards
</verification>

<success_criteria>
- Req 3's four acceptance behaviors are fixture-proven (dry-run, full loop >=1 claim, 3 ordered halts, quality-low HALT)
- Req 4 exercised from the second surface through the SAME writeCloseLoop contract (both stores asserted)
- D-03 structurally enforced (no Python, eureka legs named); G-2 guardrail live
- Born-wired: registry +2 entries, 0 changed reach_ids, hitl_stages declared
</success_criteria>

<output>
Create `.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-04-SUMMARY.md` when done, recording the fan-sizing defaults chosen (Claude's discretion item), the hierarchy_rank picked, and the runIntelPipeline seam contract Plan 05's harness relies on.
</output>
