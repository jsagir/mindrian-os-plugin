---
phase: 223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono
plan: 05
type: execute
wave: 4
depends_on: ["223-03", "223-04"]
files_modified:
  - tests/run-all-223.sh
  - lib/memory/run-feynman-tests.cjs
  - data/connector-registry.json
  - data/render-coverage-registry.json
  - docs/CANON-PHASE-MAP.md
autonomous: false
requirements: ["Req 5", "Req 6"]
must_haves:
  truths:
    - "bash tests/run-all-223.sh exits PASS with 0 FAIL, 0 SKIP - one command proves the whole phase green"
    - "build-connector-registry.cjs --check, check-shape-declaration.cjs, build-orchestration-projection.cjs --check, and check-render-coverage.cjs all exit 0 against the post-implementation tree"
    - "The connector registry's before/after diff shows exactly 2 new entries (intel-pipeline command + mirror) and 0 changed reach_ids for any existing surface"
    - "The Part 8 egress leg and the Part 9 chokepoint sweep run as PERMANENT harness tripwires over the five new lib modules"
    - "grep -r mindrian-designs commands/ skills/ lib/core/bono/ returns nothing (Req 6)"
    - "Zero new npm dependencies: the git-diff leg on package.json/package-lock.json fails on any drift"
    - "doctor.cjs --acceptance passes as a no-new-regression subset check against the documented environmental baseline"
    - "The reasoning trail is composited into the rethinking-mindrianos room (CLAUDE.md dev-research rule + BUILD-BRIEF Section 10)"
  artifacts:
    - path: "tests/run-all-223.sh"
      provides: "The aggregate phase gate: 5 test legs + constitutional tripwires + structural gates, copying run-all-224.sh's run/run_if/strip_comments shape"
      min_lines: 80
    - path: "lib/memory/run-feynman-tests.cjs"
      provides: "The five test-223-*.cjs files registered in TEST_FILES"
      contains: "test-223"
    - path: "docs/CANON-PHASE-MAP.md"
      provides: "Phase 223 row (canon_parts: 3, 7, 8, 9, 11, 12 per the BUILD-BRIEF frontmatter)"
  key_links:
    - from: "tests/run-all-223.sh"
      to: "tests/test-223-*.cjs"
      via: "run legs for all five phase test files plus regression + gate legs"
      pattern: "test-223"
    - from: "tests/run-all-223.sh"
      to: "scripts/build-connector-registry.cjs"
      via: "structural gate leg + the intel-pipeline 2-entry / bono-tuple-intact absolute assertions"
      pattern: "build-connector-registry"
---

<objective>
Close the phase (Req 5 + Req 6): one aggregate command proves everything green, the constitutional
tripwires become permanent, the born-wired evidence (exactly +2 registry entries, 0 changed
reach_ids) is recorded, the canon ledger and the rethinking-mindrianos reasoning trail are filed,
and the navigator decides the version cut via scripts/release.sh (SPEC Boundaries: wiring, gates,
release per Requirement 5 - never a hand-bumped version).

Purpose: SPEC Req 5's acceptance is that all six gate commands exit 0 against the
post-implementation tree; this plan is where that becomes provable and permanent.

Output: run-all-223.sh green (0 FAIL, 0 SKIP), feynman-tests registration, regenerated registries,
CANON-PHASE-MAP row, the compositing entry, and the navigator-gated release.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-SPEC.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-CONTEXT.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-RESEARCH.md
@.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-VALIDATION.md
@tests/run-all-224.sh
@lib/memory/run-feynman-tests.cjs
@docs/CANON-PHASE-MAP.md
</context>

## Artifacts this phase produces (phase-wide contract)

| Artifact | Kind | Plan |
|----------|------|------|
| `lib/core/bono/hat-governance.cjs` (`HAT_GOVERNANCE`, `CROSS_CUTTING_RULES`, `governanceForHat`, `enforceGovernance`, `assertHeterogeneity`, `composeGovernedSeams`) | new module | 01 |
| `lib/core/bono/persona-research.cjs` (`personaDispatchCell`, `validateCitations`) | new module | 01 |
| `tests/helpers/fixture-room-223.cjs` (`buildFixtureRoom223`) | new test helper | 01 |
| `tests/test-223-hat-governance.cjs`, `tests/test-223-part8-egress.cjs` | test files | 01 |
| `lib/core/navigation/typed-open-question.cjs` (`writeOpenQuestionNode`) | new navigation submodule | 02 |
| `lib/core/navigation/typed-claim.cjs` optional `extraProps` bag | function contract extension | 02 |
| `lib/core/navigation.cjs` re-export of `writeOpenQuestionNode` | additive re-export | 02 |
| `lib/core/close-loop-writer.cjs` (`writeCloseLoop`, `findPriorConclusion`, `mintArtifactId`) | new shared-spine module | 02 |
| `lib/core/temporal/supersession.cjs::walkSupersedesChain` | function addition (net-new walker) | 02 |
| `tests/test-223-close-loop.cjs`, `tests/test-223-supersedes-chain.cjs` | test files | 02 |
| `commands/bono.md` 8-phase governed body + `hitl_stages` + `web_scope: green` | surface evolution | 03 |
| `skills/bono/SKILL.md` regenerated mirror (DESENSITIZE preserved) | generated mirror | 03 |
| bono `--version-log` (reads `walkSupersedesChain`) | command capability | 03 |
| `lib/core/intel-pipeline.cjs` (`runIntelPipeline`) | new composition module | 04 |
| `commands/intel-pipeline.md` (kind: meta connector) + `skills/intel-pipeline/SKILL.md` | new surface + generated mirror | 04 |
| `tests/test-223-intel-pipeline.cjs` | test file | 04 |
| `tests/run-all-223.sh` + `run-feynman-tests.cjs` registration | new harness + registration | 05 |
| `data/connector-registry.json`, `data/render-coverage-registry.json` regenerated | generated artifacts | 03/04/05 |
| `docs/CANON-PHASE-MAP.md` phase row + rethinking-mindrianos research entry | docs + compositing | 05 |
| Version cut via `scripts/release.sh` (navigator-gated) | release | 05 |

<tasks>

<task type="auto">
  <name>Task 1: run-all-223.sh aggregate harness + feynman-tests registration</name>
  <files>tests/run-all-223.sh, lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - tests/run-all-224.sh (the ENTIRE aggregator to copy: set -uo pipefail, run/run_if counters, strip_comments before every grep, the git-diff zero-deps leg, the extended Part 8 primitive list, the doctor --acceptance subset-of-baseline idiom, the [ FAIL -eq 0 ] exit)
    - lib/memory/run-feynman-tests.cjs (the TEST_FILES registration idiom - append, never reorder)
    - 223-VALIDATION.md (the per-requirement test map + sampling contract this harness realizes)
    - .planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-04-SUMMARY.md (the documented doctor environmental baseline: coverage-gate + verify-release-clean-tree)
  </read_first>
  <action>
    Create tests/run-all-223.sh copying run-all-224.sh's aggregator shape exactly. Legs:

    1. Five test legs (run_if-guarded for partial-landing safety): test-223-hat-governance,
       test-223-part8-egress, test-223-close-loop, test-223-supersedes-chain,
       test-223-intel-pipeline.
    2. Regression legs: node tests/test-graph-derive-sweep.cjs (the shared derivation contract,
       4/4) and bash tests/run-all-164.sh (the bono base, 20/20).
    3. Zero-deps tripwire: the git-diff leg on package.json/package-lock.json (copy run-all-224's,
       fails on ANY drift - the SPEC's zero-new-dependency constraint made permanent).
    4. Part 8 egress sweep (permanent tripwire): the comment-stripped grep over the five new lib
       modules (lib/core/bono/hat-governance.cjs, lib/core/bono/persona-research.cjs,
       lib/core/close-loop-writer.cjs, lib/core/intel-pipeline.cjs,
       lib/core/navigation/typed-open-question.cjs) for fetch( / http(s) URL literals /
       require node:http(s) / curl / wget on executable lines; a MISSING target file FAILS the leg
       (T-224-15 idiom).
    5. Part 9 chokepoint sweep: comment-stripped grep proving close-loop-writer.cjs and
       intel-pipeline.cjs contain no raw "INSERT INTO nodes" / "INSERT INTO edges" and DO require
       navigation.cjs (mandatory-require assertion, run-all-224's Part 9 leg idiom).
    6. Req 6 leg: grep -r "mindrian-designs" commands/ skills/ lib/core/bono/ MUST return nothing
       (the harness file itself lives in tests/ so the literal in the leg does not self-trip).
    7. Structural gates: node scripts/build-connector-registry.cjs --check;
       node scripts/check-shape-declaration.cjs (advisory mode, WITHOUT --strict, exit 0);
       node scripts/build-orchestration-projection.cjs --check;
       node scripts/check-render-coverage.cjs.
    8. Registry absolute assertions: grep-count intel-pipeline entries in
       data/connector-registry.json equals exactly 2; bono's tuple intact (reach_id hats +
       sub_mode bono present; command sensor_triggers [SENS-05], mirror []).
    9. doctor leg: node scripts/doctor.cjs --acceptance as a NO-NEW-REGRESSION subset check against
       the documented environmental baseline {coverage-gate, verify-release-clean-tree} (the
       run-all-224 written-reason idiom - a NEW failure fails the leg, the two documented
       pre-existing ones do not).

    Then append the five test-223-*.cjs files to lib/memory/run-feynman-tests.cjs TEST_FILES
    (the 223-VALIDATION test-infra contract; CONTEXT integration point).
  </action>
  <verify>
    <automated>bash tests/run-all-223.sh</automated>
  </verify>
  <acceptance_criteria>
    - bash tests/run-all-223.sh exits 0 printing PASS with FAIL=0 SKIP=0
    - Tripwire-plant proof (executed once, then reverted byte-clean): planting a fetch('http://evil.example') line in lib/core/intel-pipeline.cjs flips the Part 8 leg to FAIL and the harness to exit 1
    - grep -c "test-223" lib/memory/run-feynman-tests.cjs returns 5
    - The doctor leg documents its baseline set in a comment (written-reason idiom)
    - grep -c "run_if\|strip_comments" tests/run-all-223.sh confirms the 224 idioms were copied, not reinvented
  </acceptance_criteria>
  <done>One command proves the phase green and the constitutional tripwires (zero-deps, Part 8, Part 9, Req 6) are permanent harness legs, not one-time reviews.</done>
</task>

<task type="auto">
  <name>Task 2: Final registry regeneration + born-wired diff evidence + canon ledger + compositing</name>
  <files>data/connector-registry.json, data/render-coverage-registry.json, docs/CANON-PHASE-MAP.md</files>
  <read_first>
    - docs/CANON-PHASE-MAP.md (the ledger row format; every phase declares canon_parts)
    - 223-BUILD-BRIEF.md frontmatter (canon_parts: Part 3, 7, 8, 9, 11, 12) and Section 10 (the dev-research compositing mandate)
    - CLAUDE.md "Dev-Research Compositing (Rethinking Room)" section (the two-homes rule: phase dir + ~/MindrianRooms/rethinking-mindrianos/research/<dated-entry>/, cross-linked)
  </read_first>
  <action>
    1. Regenerate the generated artifacts one final time against the fully-landed tree:
       node scripts/build-connector-registry.cjs && node scripts/build-skill-mirrors.cjs &&
       node scripts/build-orchestration-projection.cjs (if it writes a projection) and the
       render-coverage regeneration (whatever check-render-coverage.cjs's paired builder is -
       read the script header to find it). Re-run all four --check/check gates green.
    2. Record the Req 5 before/after diff evidence: diff the current data/connector-registry.json
       against the pre-phase baseline (git show of the file at the last commit before this phase's
       first change - locate it via git log -- data/connector-registry.json). Assert and record in
       the SUMMARY: exactly 2 new entries (intel-pipeline command + mirror), 0 changed reach_ids on
       any existing surface, bono's tuple unchanged except web_scope per Plan 03.
    3. Add the Phase 223 row to docs/CANON-PHASE-MAP.md (canon_parts 3, 7, 8, 9, 11, 12), naming
       the two surfaces and the shared close-the-loop spine.
    4. Dev-research compositing (CLAUDE.md rule + BUILD-BRIEF Section 10): file the durable
       reasoning entry under ~/MindrianRooms/rethinking-mindrianos/research/<dated-entry>/ -
       the hat-governance discipline sources (BUILD-BRIEF Section 5), the close-the-loop contract,
       the Req-4 write-side gap + D-01 resolution, and the two RESEARCH staleness corrections
       (sensor_triggers DESENSITIZE; compute-hsi retirement) - cross-linked back to this phase dir,
       and mirrored per the room's own conventions.
    5. Re-run bash tests/run-all-223.sh (must stay 0 FAIL after regeneration).
  </action>
  <verify>
    <automated>bash -c 'node scripts/build-connector-registry.cjs --check && node scripts/check-render-coverage.cjs && node scripts/build-orchestration-projection.cjs --check && grep -q "223" docs/CANON-PHASE-MAP.md && bash tests/run-all-223.sh && echo PHASE-GATES-OK'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints PHASE-GATES-OK
    - The SUMMARY records the registry diff evidence: +2 entries, 0 changed reach_ids (SPEC Req 5 acceptance line, satisfied with recorded proof, not assertion)
    - docs/CANON-PHASE-MAP.md carries the 223 row with canon_parts 3, 7, 8, 9, 11, 12
    - The rethinking-mindrianos entry exists and cross-links this phase directory (both homes, per CLAUDE.md)
    - node scripts/doctor.cjs --acceptance shows no NEW failure vs the documented baseline
  </acceptance_criteria>
  <done>Every Req 5 gate is green against the final tree with recorded born-wired evidence; the canon ledger and the reasoning trail are filed in both homes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Navigator gate - phase evidence review + version-cut decision</name>
  <read_first>223-05-SUMMARY draft state (Tasks 1-2 evidence: harness output, registry diff, doctor baseline), .claude/includes/release-process.md</read_first>
  <action>Present the gate evidence and the run-and-verify instructions below to the navigator and PAUSE. On resume, record the decision (approved version or explicit deferral) verbatim for Task 4 - never infer approval, never proceed without the signal.</action>
  <what-built>Both surfaces landed and gate-clean: /mos:bono evolved to the 8-phase governed debate (hat governance, per-persona research, version-cut SUPERSEDES chain with --version-log) and /mos:intel-pipeline born-wired with 3 hitl_stages; the shared close-the-loop spine writes both stores (D-01); run-all-223.sh is PASS 0 FAIL 0 SKIP; registry diff is +2/0-changed; canon ledger + compositing filed.</what-built>
  <how-to-verify>
    1. Run: bash tests/run-all-223.sh - expect PASS, FAIL=0, SKIP=0.
    2. Run: node scripts/doctor.cjs --acceptance - expect no NEW failure vs the documented baseline.
    3. Optional live feel-checks (the 223-VALIDATION manual rows, also runnable at /gsd-verify-work):
       read the new bono.md 8-phase prose against the Larry voice reference; run /mos:intel-pipeline
       --dry-run against a scratch room and confirm the plan card renders; run /mos:bono on a real
       topic and confirm the three gates fire.
    4. Decide the release: this phase's SPEC scopes a version cut via scripts/release.sh (five-gate
       lockstep, never hand-bumped). Reply with the version to cut (e.g. the next beta), or defer
       the cut to a joint release if you prefer to batch it.
  </how-to-verify>
  <acceptance_criteria>The navigator's decision is recorded verbatim (version string or deferral) before Task 4 runs; no release action fires from this task itself.</acceptance_criteria>
  <resume-signal>Reply "approved <version>" to cut, or "defer release" to close the phase without cutting (the cut then rides the next release train)</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Execute the version cut (only on navigator approval)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .claude/includes/release-process.md (the five-gate lockstep: CHANGELOG, plugin.json, package.json, git tag, marketplace.json - scripts/release.sh enforces all five; never bump by hand)
    - scripts/release.sh (flags: --prerelease exists per the Phase 221 precedent)
  </read_first>
  <action>
    ONLY if Task 3's resume signal was "approved <version>": draft the CHANGELOG.md entry (the two
    surfaces, the shared spine, the chain walker - Feynman, no em-dashes), then run
    scripts/release.sh <version> from /home/jsagi/dev/MindrianOS-Plugin (the WORKSPACE GUARD - never
    the plugin cache) and let it enforce the five-gate lockstep including the marketplace pin.
    Record the release evidence (npm dist-tag, git tag, marketplace source.ref) in the SUMMARY.

    If the signal was "defer release": record the deferral decision + rationale in the SUMMARY and
    in STATE.md's next-steps (an honest deferral, not a silent skip), and mark this task complete
    without running release.sh. The phase's other acceptance criteria are unaffected.
  </action>
  <verify>
    <automated>bash -c 'git -C /home/jsagi/dev/MindrianOS-Plugin status --porcelain | head -5; bash tests/run-all-223.sh > /dev/null && echo POST-RELEASE-SUITE-OK'</automated>
  </verify>
  <acceptance_criteria>
    - On approval: scripts/release.sh <version> exited 0 with all five gates in sync (CHANGELOG entry, plugin.json, package.json, tag, marketplace pin) and the evidence is recorded in the SUMMARY
    - On deferral: the deferral is recorded in the SUMMARY and STATE.md - never silently skipped
    - bash tests/run-all-223.sh still exits 0 after any release-side file changes
  </acceptance_criteria>
  <done>The phase ends with an executed five-gate version cut or an explicitly recorded navigator deferral - nothing hand-bumped, nothing silent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> release gate | a harness that passes vacuously (skipped legs, self-tripping greps) would green-light a broken release |
| repo -> npm/marketplace | the release publishes to users; version drift or dep drift ships silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-19 | Repudiation | vacuous gate pass (skipped/self-tripping legs) | mitigate | SKIP=0 required; missing-target-fails idiom on every sweep; tripwire-plant proof executed and reverted; strip_comments on every grep (grep-gate hygiene) |
| T-223-20 | Tampering | dependency drift shipping in the cut | mitigate | permanent git-diff zero-deps leg; release.sh five-gate lockstep; never hand-bumped |
| T-223-21 | Elevation of Privilege | release without human decision | mitigate | blocking checkpoint (Task 3); Task 4 runs ONLY on the recorded approval signal; deferral is a first-class recorded outcome |
| T-223-22 | Information Disclosure | Part 8 regression sneaking into a later edit | mitigate | the egress sweep is a PERMANENT harness leg over all five new modules, not a one-time review |
| T-223-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new dependencies enforced by the harness's own git-diff leg (this plan makes the constraint permanent) |
</threat_model>

<verification>
- bash tests/run-all-223.sh exits PASS 0 FAIL 0 SKIP
- All four structural gates + doctor --acceptance green (subset baseline)
- Registry diff evidence recorded: +2 entries, 0 changed reach_ids
- Pre-verify-work regression set (223-VALIDATION): bash tests/run-all-224.sh, bash tests/run-all-164.sh, node tests/test-graph-derive-sweep.cjs all green
</verification>

<success_criteria>
- SPEC Req 5's six gate commands all exit 0 against the post-implementation tree
- Req 6's grep returns nothing across commands/, skills/, lib/core/bono/
- Canon ledger row + rethinking-mindrianos compositing filed (both homes)
- Version cut executed via release.sh or explicitly deferred by the navigator - recorded either way
</success_criteria>

<output>
Create `.planning/phases/223-jtbd-driven-intelligence-pipeline-governed-double-fan-bono-e/223-05-SUMMARY.md` when done, recording the harness leg inventory, the registry diff evidence, the doctor baseline comparison, and the release decision + evidence.
</output>
