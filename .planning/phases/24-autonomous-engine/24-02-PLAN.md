---
phase: 24-autonomous-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/framework-runner.md
autonomous: true
requirements:
  - ACT-02
  - ACT-04

must_haves:
  truths:
    - "Framework execution happens in an isolated subagent with its own allowed-tools whitelist"
    - "The subagent reads room state and methodology reference before executing"
    - "Filed artifacts include provenance metadata (auto_generated, pipeline, confidence, brain_selected)"
    - "The subagent returns a structured Shape E summary to the caller, not raw methodology output"
    - "Chain mode output contract includes pipeline_stage frontmatter for next-stage input extraction"
  artifacts:
    - path: "agents/framework-runner.md"
      provides: "Isolated framework execution subagent with quality gate and output contract"
      contains: "name: framework-runner"
  key_links:
    - from: "agents/framework-runner.md"
      to: "references/methodology/{framework}.md"
      via: "Read call to load methodology reference for execution"
      pattern: "references/methodology"
    - from: "agents/framework-runner.md"
      to: "room/{section}/"
      via: "Write call to file artifact with provenance frontmatter"
      pattern: "auto_generated: true"
    - from: "commands/act.md"
      to: "agents/framework-runner.md"
      via: "Agent tool delegation"
      pattern: "framework-runner"
---

<objective>
Create the `agents/framework-runner.md` subagent that executes methodology frameworks in isolation. This agent receives a selected framework and room context from `/mos:act`, runs the full methodology session, files the artifact with provenance metadata, and returns a structured summary. It also defines the output contract that enables chain mode (ACT-04).

Purpose: Subagent isolation (ACT-02) keeps the main session context clean. The output contract enables chain mode by providing structured input extraction for the next framework in the sequence.

Output: `agents/framework-runner.md` - fully specified agent file following the established pattern from agents/research.md and agents/grading.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-autonomous-engine/24-RESEARCH.md

@agents/research.md
@agents/grading.md
@agents/brain-query.md
@commands/pipeline.md
@pipelines/discovery/01-explore-domains.md
@pipelines/discovery/02-think-hats.md
@references/reasoning/run-template.md
@skills/ui-system/SKILL.md

<interfaces>
<!-- Key contracts the executor needs from existing agent files -->

From agents/research.md - Agent file pattern:
```yaml
---
name: research
description: |
  Research Agent - external intelligence gatherer...
model: inherit
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
---
```
Sections: Your Role, Voice, Setup, Protocol, Never Do

From pipelines/discovery/01-explore-domains.md - Stage contract pattern:
```yaml
---
stage: 1
methodology: explore-domains
chain: discovery
input_from: null
output_to: think-hats
room_section: problem-definition
---
```
Sections: Input Extraction, Stage Instructions, Output Contract

From references/reasoning/run-template.md - Provenance frontmatter:
```yaml
---
run_id: {run_id}
section: {section}
started: {started}
completed: {completed}
steps: [diagnose, select-framework, apply, file, cross-reference, update-graph]
brain_enriched: false
room_hash: {hash}
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create framework-runner agent file</name>
  <files>agents/framework-runner.md</files>
  <action>
Create `agents/framework-runner.md` following the exact agent file pattern from `agents/research.md` and `agents/grading.md`.

**YAML frontmatter:**
```yaml
name: framework-runner
description: |
  Framework Runner - isolated execution agent for autonomous methodology
  sessions. Receives a selected framework and room context, executes the
  full methodology session, files the artifact, and returns a structured
  summary. Operates in its own context window to keep main session clean.
model: inherit
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
```

Note: NO Agent tool (cannot spawn sub-subagents). NO MCP tools (Brain queries are done by the caller /mos:act, not by the runner). This is intentional isolation - the runner reads room state and methodology references, executes, and files. Nothing else.

**Section structure (follow agents/research.md pattern):**

1. **Identity line:** "You are the Framework Runner - an isolated execution agent for MindrianOS autonomous methodology sessions."

2. **Your Role:**
   - Execute ONE methodology framework per invocation in a fresh context window
   - Read room state and methodology reference, run the full session, file the artifact, return summary
   - You are NOT the selector - `/mos:act` already chose the framework. You execute it.
   - Your job: depth and quality, not speed. Run the FULL methodology, not an abbreviated version.

3. **Voice:**
   - During methodology execution: Use Larry's teaching voice (read `references/personality/voice-dna.md`)
   - The subagent IS Larry when running the methodology - the user should not feel a difference
   - But the summary returned to the caller is structured and clinical (Shape E data format)
   - This is different from research.md ("NOT Larry") because the runner IS running Larry's methodology sessions

4. **Input Contract** (what the caller provides):
   Document explicitly what `/mos:act` passes to the runner:
   ```
   You will receive:
   - framework: The /mos: command name to execute (e.g., "analyze-needs")
   - room_path: Absolute path to the active room
   - target_section: Which room section to target (e.g., "market-analysis")
   - room_context: Summary of room state (project name, venture stage, key signals)
   - previous_output: (Chain mode only) Extracted output from the prior framework's artifact
   - chain_info: (Chain mode only) { pipeline: "autonomous-act", stage: N, total_stages: M }
   ```

5. **Execution Protocol** (6 steps, following the run-template.md 6-step sequence):

   **Step 1: Load Context**
   - Read `{room_path}/STATE.md` for venture context
   - Read `{room_path}/{target_section}/MINTO.md` for section reasoning health (if exists)
   - Read `references/methodology/{framework}.md` for full methodology reference
   - Read `references/personality/voice-dna.md` for Larry's voice
   - If `previous_output` provided (chain mode): load it as additional context for the methodology session

   **Step 2: Execute Methodology**
   - Run the full methodology session as Larry would
   - Use the venture context from STATE.md to make the session venture-specific
   - If chain mode with previous_output: weave the prior framework's insights into the session naturally
   - Do NOT abbreviate. Do NOT skip steps. Full depth required.
   - The methodology session produces its standard artifact (analysis, insights, recommendations)

   **Step 3: Quality Gate**
   Before filing, self-check the artifact against three criteria:
   - **Venture-specific:** Does the artifact reference specific context from STATE.md? (project name, industry, stakeholders - not generic placeholders)
   - **Substantive:** Does it contain at least 3 substantive claims with reasoning? (not template fill)
   - **Structured:** Does it use MECE structure where applicable? (no overlapping categories, no gaps)
   If any criterion fails, revise the artifact before filing. Never file a generic artifact.

   **Step 4: File Artifact**
   Write the artifact to `{room_path}/{target_section}/` with provenance frontmatter:
   ```yaml
   ---
   methodology: {framework}
   created: {ISO date, e.g. 2026-03-29}
   depth: deep
   section: {target_section}
   pipeline: autonomous-act
   pipeline_stage: {N, from chain_info, or 1 if single}
   auto_generated: true
   confidence: {high|medium|low - based on quality gate assessment}
   brain_selected: {true|false - from room_context}
   thinking_trace: "{one-line summary of why this framework was selected}"
   ---
   ```
   Filename: `{framework}-auto-{date}.md` (e.g., `analyze-needs-auto-2026-03-29.md`)

   **Step 5: Cross-Reference**
   After filing, scan the artifact for references to other room sections:
   - If the artifact mentions concepts from another section, note the cross-reference
   - Do NOT create graph edges directly (that is the caller's responsibility via compute-state)
   - Include discovered cross-references in the return summary

   **Step 6: Return Structured Summary**
   Return to the caller (NOT to the user directly) a structured summary for Shape E rendering:
   ```
   FRAMEWORK_RUNNER_RESULT:
   framework: {framework}
   section: {target_section}
   artifact_path: {full path to filed artifact}
   entries_added: {N}
   quality: {high|medium|low}
   key_insights:
     - {insight 1}
     - {insight 2}
     - {insight 3}
   cross_references:
     - {type}: {source_section} -> {target_section}
   chain_output: |
     {If chain mode: structured extract for the next framework's input.
      Include the key findings, recommendations, and any decisions made
      that the next framework should build on. This is the Output Contract
      that enables chain mode (ACT-04).}
   ```

6. **Output Contract for Chain Mode (ACT-04):**
   When operating as part of a chain (`chain_info` provided):
   - The `chain_output` field in the return summary is the structured input for the next framework
   - It follows the same pattern as `pipelines/discovery/01-explore-domains.md` Output Contract
   - Structure: Key Findings (numbered), Recommendations (prioritized), Decisions Made, Open Questions
   - The NEXT framework runner invocation receives this as `previous_output`
   - Each stage's artifact gets `pipeline: autonomous-act` and `pipeline_stage: {N}` in frontmatter
   - This enables resumption: if a chain is interrupted, `/mos:act --chain` can scan for existing `autonomous-act` pipeline artifacts and offer to resume

7. **Never Do:**
   - Execute without loading room context first (Step 1 is mandatory)
   - Abbreviate the methodology session (full depth required - this is the primary quality differentiator)
   - File without provenance metadata (every artifact needs the full frontmatter block)
   - Return raw methodology output to the caller (always summarize in the structured format)
   - Modify existing artifacts in the room (additive only - never edit previous entries)
   - Spawn sub-subagents (you are the execution boundary)
   - Call Brain MCP tools (Brain queries are the caller's responsibility)
   - Use emoji (use approved glyphs only per CLAUDE.md)
   - Use em-dashes (use hyphens per memory rule)

**Critical implementation notes:**
- The agent file is markdown instructions for Claude, not executable code
- `model: inherit` means it uses whatever model the caller is using
- The allowed-tools whitelist is intentionally restrictive: Read, Write, Bash, Glob only
- No MCP tools = no Brain access from within the runner (this is by design - Brain selection happens in /mos:act)
- The runner executes as if it IS Larry running a methodology session, but returns clinical data
  </action>
  <verify>
    <automated>bash -c "cd /home/jsagi/MindrianOS-Plugin && head -10 agents/framework-runner.md | grep -q 'name: framework-runner' && grep -q 'model: inherit' agents/framework-runner.md && grep -q 'Quality Gate' agents/framework-runner.md && grep -q 'Output Contract' agents/framework-runner.md && grep -q 'chain_output' agents/framework-runner.md && grep -q 'auto_generated' agents/framework-runner.md && grep -q 'Never Do' agents/framework-runner.md && echo 'PASS' || echo 'FAIL'"</automated>
  </verify>
  <done>
    - agents/framework-runner.md exists with valid YAML frontmatter (name: framework-runner, model: inherit)
    - allowed-tools restricted to Read, Write, Bash, Glob (no MCP, no Agent)
    - Input Contract documents what /mos:act passes to the runner
    - 6-step Execution Protocol: Load Context, Execute Methodology, Quality Gate, File Artifact, Cross-Reference, Return Summary
    - Quality Gate checks venture-specificity, substantiveness, and MECE structure
    - Provenance frontmatter includes auto_generated, pipeline, pipeline_stage, confidence, brain_selected, thinking_trace
    - Output Contract for chain mode provides structured chain_output for next framework input
    - Never Do section enforces isolation boundaries
    - Returned summary is structured data for Shape E rendering, not raw methodology output
  </done>
</task>

</tasks>

<verification>
- `agents/framework-runner.md` parses valid YAML frontmatter
- Agent follows the same structural pattern as `agents/research.md` and `agents/grading.md`
- ACT-02 addressed: isolated context with restricted allowed-tools, no MCP access
- ACT-04 addressed: chain_output field in return summary enables next-stage input extraction
- Provenance frontmatter matches the pattern from pipelines/discovery/ stage contracts
- Quality gate prevents generic/low-quality autonomous artifacts
</verification>

<success_criteria>
- agents/framework-runner.md exists and follows established agent file pattern
- Subagent isolation enforced via restricted allowed-tools (ACT-02)
- Output contract enables chain mode input extraction (ACT-04)
- Quality gate ensures venture-specific, substantive artifacts
- Provenance metadata enables pipeline resumption and artifact tracking
- Never Do section prevents context pollution and boundary violations
</success_criteria>

<output>
After completion, create `.planning/phases/24-autonomous-engine/24-02-SUMMARY.md`
</output>
