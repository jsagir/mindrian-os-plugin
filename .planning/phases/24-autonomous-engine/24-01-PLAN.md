---
phase: 24-autonomous-engine
plan: 01
type: execute
wave: 1
depends_on: [21-01, 21-02]
files_modified:
  - commands/act.md
  - agents/framework-runner.md
  - references/pipeline/act-output-contract.md
autonomous: true
requirements: [ACT-01, ACT-02, ACT-03, ACT-04, ACT-05]
must_haves:
  truths:
    - "/mos:act reads room/STATE.md + room/MINTO.md and selects a methodology framework via Brain query (with local fallback to problem-types.md routing table)"
    - "A thinking trace is displayed before any framework execution showing why the framework was selected"
    - "Framework execution dispatches to agents/framework-runner.md for isolated subagent context"
    - "/mos:act --chain selects 3-5 frameworks and chains them sequentially with output contracts"
    - "/mos:act --dry-run shows the full execution plan without executing"
  artifacts:
    - path: "commands/act.md"
      provides: "Autonomous framework selection and execution command"
      min_lines: 150
    - path: "agents/framework-runner.md"
      provides: "Isolated subagent for framework execution"
      min_lines: 40
    - path: "references/pipeline/act-output-contract.md"
      provides: "Output contract for chain mode inter-framework data flow"
      min_lines: 30
---

<objective>
Create the `/mos:act` autonomous engine command with framework-runner subagent and chain mode.

Purpose: Larry autonomously selects and executes methodology frameworks based on room state -- with thinking transparency, subagent isolation, and chain mode for multi-framework sequences.
Output: `commands/act.md` (new), `agents/framework-runner.md` (new), `references/pipeline/act-output-contract.md` (new)
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@commands/status.md
@commands/pipeline.md
@commands/suggest-next.md
@agents/larry-extended.md
@references/methodology/problem-types.md
@references/pipeline/chains-index.md
@references/brain/query-patterns.md
@skills/ui-system/SKILL.md
</context>

<tasks>

<task id="1" type="auto">
<name>Create framework-runner subagent and act output contract</name>
<description>
Create agents/framework-runner.md -- an isolated subagent that receives a framework name + room context and runs the methodology command in a fresh context. Also create references/pipeline/act-output-contract.md defining the structured output format for chain mode.
</description>
<done_criteria>
- agents/framework-runner.md exists with agent frontmatter and clear execution protocol
- references/pipeline/act-output-contract.md exists with structured output format for chain data flow
</done_criteria>
</task>

<task id="2" type="auto">
<name>Create /mos:act command with thinking trace, chain mode, and dry-run</name>
<description>
Create commands/act.md -- the autonomous engine command. Reads room STATE.md + MINTO.md, queries Brain (local fallback), shows thinking trace, dispatches to framework-runner subagent. Supports --chain (3-5 frameworks) and --dry-run (preview only).
</description>
<done_criteria>
- commands/act.md exists with proper command frontmatter (body_shape, ui_reference, allowed-tools)
- Thinking trace section shows framework selection reasoning before execution
- --chain mode selects and chains 3-5 frameworks with output contracts
- --dry-run mode shows plan without executing
- Brain query with local fallback to problem-types.md routing table
</done_criteria>
</task>

</tasks>

<verification>
After all tasks:
1. commands/act.md has proper YAML frontmatter with body_shape and allowed-tools
2. agents/framework-runner.md has proper agent YAML frontmatter
3. references/pipeline/act-output-contract.md defines structured output format
4. All ACT-01 through ACT-05 requirements are addressed
</verification>

<success_criteria>
Running /mos:act reads room state, queries Brain for framework, displays thinking trace, and dispatches to subagent. --chain and --dry-run modes work as specified.
</success_criteria>
