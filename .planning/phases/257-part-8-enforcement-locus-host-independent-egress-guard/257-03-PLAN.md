---
phase: 257-part-8-enforcement-locus-host-independent-egress-guard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mcp/brain-composition-census.cjs
  - docs/2026-08-20-HANDOFF-part8-guard-in-mcp-handlers.md
autonomous: true
requirements: [LOCUS-04]
canon_parts: [7, 8]
user_setup: []

must_haves:
  truths:
    - "A future reader of brain-composition-census.cjs is told the truth about Seam A: the stdio shim delegates through brain-client.cjs and the callTool belt does cover it"
    - "The 2026-08-20 handoff carries a dated correction naming H3-as-written false, with the commit-order evidence, without deleting a word of the original"
    - "Phase 254's composition census still reconciles bidirectionally against source after the comment correction"
  artifacts:
    - path: "lib/mcp/brain-composition-census.cjs"
      provides: "Corrected Seam A description, H3 restated accurately"
      contains: "Phase 257"
    - path: "docs/2026-08-20-HANDOFF-part8-guard-in-mcp-handlers.md"
      provides: "Append-only dated correction block"
      contains: "CORRECTION"
  key_links:
    - from: "lib/mcp/brain-composition-census.cjs"
      to: "docs/257-NOTE-part8-enforcement-locus-rulings.md"
      via: "the corrected comment points at the ruling document for the surface that IS open"
      pattern: "257-NOTE-part8-enforcement-locus-rulings"
---

<objective>
Correct the record (D-07). Two documents assert something measurement disproved, and both are the
kind of sentence a future session will cite rather than re-derive.

1. `lib/mcp/brain-composition-census.cjs:35-41` says of Seam A: "the callTool belt below does NOT
   [see this] (that traffic never touches brain-client.cjs at all). This is H3, and H3 IS STILL
   OPEN." The parenthetical is FALSE. `bin/mindrian-brain-mcp-client.cjs` requires
   `lib/core/brain-client.cjs` at line 62 and every one of its handlers calls through it. Phase 254
   inherited the claim from the handoff rather than re-deriving it; 254-RESEARCH.md was otherwise
   scrupulous, which is what makes this one sentence dangerous.

2. `docs/2026-08-20-HANDOFF-part8-guard-in-mcp-handlers.md` states H3 as a direct model-issued
   `mcp__...mindrian-brain__brain_*` call bypassing `brain-client.cjs` entirely. That was false when
   it was written: the Part 8 belt landed in commit `ca32b612` at 2026-08-19 09:26:51 +0300, two and
   a half hours BEFORE the handoff's own base commit `f566310c` (2026-08-19 11:55:23 +0300), and
   `git merge-base --is-ancestor ca32b612 f566310c` returns true.

The root cause of both errors is Pitfall 2 from 257-RESEARCH.md: grepping for the guard's FILENAME
to prove coverage. A filename grep cannot follow `brainClient.ask()` into `callTool()`. That is the
lesson worth writing down, because it is the same lesson the invariant test in Plan 07 encodes.

Purpose: stop the next session from paying for this twice.
Output: one corrected comment block and one append-only dated correction, cross-linked.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/257-part-8-enforcement-locus-host-independent-egress-guard/257-CONTEXT.md
@.planning/phases/257-part-8-enforcement-locus-host-independent-egress-guard/257-RESEARCH.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase-wide symbol ledger. This plan owns the rows marked OWNED.

| Symbol / artifact | Kind | Owner plan | This plan |
|---|---|---|---|
| `egress_blocked` refusal kind + `BRAIN_EGRESS_BLOCKED` status | code | 257-01 | - |
| `refusal` + `egress_disclosure` pass-through in `wrapDirective()` | code | 257-02 | - |
| corrected census parenthetical (`lib/mcp/brain-composition-census.cjs`) | comment | 257-03 | OWNED |
| dated correction block on the 2026-08-20 handoff | docs | 257-03 | OWNED |
| `docs/257-NOTE-part8-enforcement-locus-rulings.md` | docs | 257-04 | - |
| `docs/257-NOTE-theo-forward-compat-enforcement-locus.md` | docs | 257-04 | - |
| de-frozen hook-matcher literals in the two red 239 arms | test | 257-05 | - |
| `tests/test-257-refusal-egress-kind.cjs` | test | 257-01 | - |
| `tests/test-257-envelope-passthrough.cjs` | test | 257-02 | - |
| `tests/test-257-shim-honest-refusal.cjs` | test | 257-06 | - |
| `tests/test-257-brain-tool-egress-invariant.cjs` | test | 257-07 | - |
| `tests/test-257-strict-input-shapes.cjs` | test | 257-08 | - |
| `tests/run-all-257.sh` | test aggregator | 257-07 | - |
| `LOCUS-01..LOCUS-10` rows in `.planning/REQUIREMENTS.md` | planning record | 257-09 | - |

<tasks>

<task type="auto">
  <name>Task 1: Correct the Seam A description in the composition census</name>

  <read_first>
    - `lib/mcp/brain-composition-census.cjs` in full (163 lines). Docblock section (c) "THE THREE
      SEAMS, AND WHICH ENFORCEMENT SEES EACH" at lines 32-47 is the target. Read section (d) too:
      it explains that `tests/test-254-composition-census.cjs` fails the build on drift.
    - `bin/mindrian-brain-mcp-client.cjs:12-21` (the delegation-property docblock) and `:62` (the
      `require('../lib/core/brain-client.cjs')` that disproves the parenthetical).
    - `lib/core/brain-client.cjs:604-626` (the belt) and `:576-580` (the sentinel-object rationale).
    - `tests/test-254-composition-census.cjs` - understand what it reconciles, so the comment edit is
      provably not a declaration change.
    - 257-RESEARCH.md sections "Live Egress Probe" and "Which of H1 / H2 / H3 is Actually Closed" -
      the measured evidence to cite.
  </read_first>

  <files>lib/mcp/brain-composition-census.cjs</files>

  <action>
    Rewrite the Seam A bullet in docblock section (c). Delete the false parenthetical "that traffic
    never touches brain-client.cjs at all" and the flat claim "H3 IS STILL OPEN". Replace with an
    accurate statement carrying its own evidence:

    - Seam A is model-issued: the `mindrian-brain` MCP server's own tools, called by name. The host
      PreToolUse hook sees it.
    - The `callTool` belt ALSO covers it. `bin/mindrian-brain-mcp-client.cjs` is a pure stdio
      transport wrapper with zero network code; it requires `lib/core/brain-client.cjs` at line 62
      and every one of its handlers delegates through `callTool`, where the Part 8 belt sits.
    - Cite the commit-order evidence: the belt landed in `ca32b612` (2026-08-19 09:26:51 +0300),
      before the handoff's base commit `f566310c` (2026-08-19 11:55:23 +0300).
    - State what H3 actually is now, attributed to the Phase 257 correction: H3 as the handoff wrote
      it is FALSE for `mindrian-brain`. The genuinely uncovered surface is `pws-brain-mcp`,
      registered as direct HTTPS with no local plugin code in the path, which is Desktop's and
      Cowork's path by design. Point at `docs/257-NOTE-part8-enforcement-locus-rulings.md` (created
      by Plan 04) for the ruling on that surface.
    - Name the root cause in one line: a grep for the guard's FILENAME cannot see a guard reached by
      delegation. That reasoning error is what produced this sentence.

    Keep the 254-RESEARCH.md Pitfall 4 warning that follows (a summary claiming Part 8 coverage
    without naming the open surface), restated against the CORRECT open surface: `pws-brain-mcp`,
    not the shim.

    Do NOT touch the `COMPOSITION_SITES` array, `validateSites()`, or any executable line in this
    file. This is a comment-only edit and `tests/test-254-composition-census.cjs` must stay green
    unchanged.
    No em-dashes.
  </action>

  <verify>
    <automated>node tests/test-254-composition-census.cjs</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -c "never touches brain-client.cjs at all" lib/mcp/brain-composition-census.cjs` returns 0.
    - Source assertion: `grep -c "H3 IS STILL OPEN" lib/mcp/brain-composition-census.cjs` returns 0.
    - Source assertion: `grep -c "ca32b612" lib/mcp/brain-composition-census.cjs` returns at least 1, proving the commit-order evidence is carried rather than merely asserted.
    - Source assertion: `grep -c "257-NOTE-part8-enforcement-locus-rulings" lib/mcp/brain-composition-census.cjs` returns at least 1.
    - Structural (comment-only proof): `git diff -U0 lib/mcp/brain-composition-census.cjs` shows every changed line beginning with a comment marker. No line inside `COMPOSITION_SITES` or `validateSites` changes.
    - Test command: `node tests/test-254-composition-census.cjs` passes with the same arm count it passes with today (257-RESEARCH.md recorded 9 passed, 0 failed on a live run). Record the count verbatim in the summary.
  </acceptance_criteria>

  <done>
    The census file's Seam A description matches what the code actually does, carries its own
    evidence, and points at the ruling document for the surface that IS open.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append a dated correction block to the 2026-08-20 handoff</name>

  <read_first>
    - `docs/2026-08-20-HANDOFF-part8-guard-in-mcp-handlers.md` in full (149 lines). Sections 1, 2 and
      3 carry the H3 claim; section 4 is the re-verification protocol that caught it; section 5 is
      the inherited-conventions list that REMAINS VALID and must be said to remain valid; section 8
      is the handoff's own "future-ready" note.
    - 257-RESEARCH.md "Section 4 Re-Verification" and "Live Egress Probe" - every re-run command and
      its verbatim output. The correction block quotes these, it does not re-derive them.
    - `docs/2026-08-20-HANDOFF-brain-devs-gate0-diagnostic.md` - read how that file carries its
      in-place dated UPDATE, for the house style of a dated correction in this repo.
  </read_first>

  <files>docs/2026-08-20-HANDOFF-part8-guard-in-mcp-handlers.md</files>

  <action>
    APPEND a correction block. Do not delete, rewrite, or soften a single word of the original text.
    The append-only discipline is the point: the original records what was believed, the correction
    records what was measured.

    Insert exactly ONE new line immediately under the existing status blockquote at the top, saying
    that a dated correction sits at the end of this file and that H3 as stated below is disproven.
    Then add the full block at the end of the file under a heading
    `## CORRECTION 2026-09-02 (Phase 257): H3 as written is false`.

    The block states, each with its evidence:
    - What was claimed: H3, quoted verbatim from section 1 or 2.
    - What is true: `bin/mindrian-brain-mcp-client.cjs` is a pure stdio transport wrapper that
      requires `lib/core/brain-client.cjs` at line 62; all four Brain handlers delegate through
      `callTool`, which has carried the fail-closed `part8-egress-guard.classify()` belt since
      `ca32b612`.
    - The commit-order proof: `ca32b612` at 2026-08-19 09:26:51 +0300 versus the handoff's base
      `f566310c` at 2026-08-19 11:55:23 +0300, and `git merge-base --is-ancestor ca32b612 f566310c`
      returning true. The belt was already in the tree the handoff branched from.
    - The live wire proof: a canary driven through all four Brain wrappers against
      `tests/helpers/brain-capture-server.cjs` produced four blocks and ZERO captured bytes. Include
      the four-row result table from 257-RESEARCH.md's "Live Egress Probe" section.
    - The root cause, named: Pitfall 2. `git grep part8-egress-guard -- bin/` returns zero, and the
      handoff read that absence as absence of coverage. A filename grep cannot follow a delegation.
      The same error produced the census parenthetical corrected in Task 1 of this same plan.
    - What the handoff got RIGHT and this phase inherits unchanged: section 5's conventions
      (fail-CLOSED in code versus fail-OPEN in the hook, classify the raw value before sanitize and
      before interpolation per `lib/core/bono/persona-research.cjs` 208-233, the
      `check-substrate.cjs` ALLOWED_DIRECT_IMPORT trap, the egress-proof and regression-lock test
      shapes), section 4's re-verification discipline (which is what caught the error), and section
      6's instruction not to absorb D-239-05-01.
    - What is actually open, and where it is ruled on: `pws-brain-mcp` direct HTTPS, Desktop and
      Cowork. Point at `docs/257-NOTE-part8-enforcement-locus-rulings.md` (Plan 04) rather than
      restating the ruling here.
    - The status line: this handoff is CORRECTED, not retired. Its conventions still bind.

    No em-dashes anywhere in the added text. Do not edit any pre-existing line except the single
    inserted pointer line under the status blockquote.
  </action>

  <verify>
    <automated>node scripts/check-substrate.cjs --diff</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -c "CORRECTION 2026-09-02" docs/2026-08-20-HANDOFF-part8-guard-in-mcp-handlers.md` returns 1.
    - Source assertion: each of `ca32b612`, `f566310c`, `merge-base`, `brain-capture-server`, `257-NOTE-part8-enforcement-locus-rulings` and `Pitfall` appears at least once in the file (`grep -c` per token, each at least 1).
    - Append-only proof: `git diff --numstat docs/2026-08-20-HANDOFF-part8-guard-in-mcp-handlers.md` shows deletions of at most 1 (the single blockquote line replaced by the pointer insertion). If deletions exceed 1, the append-only rule was broken; revert and redo.
    - Append-only proof: `git diff -U0 docs/2026-08-20-HANDOFF-part8-guard-in-mcp-handlers.md | grep '^-' | grep -v '^---'` shows no removed line from sections 1 through 10.
    - Style: the file contains zero em-dash characters. Assert with `node -e "const s=require('fs').readFileSync('docs/2026-08-20-HANDOFF-part8-guard-in-mcp-handlers.md','utf8');if(/\u2014/.test(s))process.exit(1)"` exiting 0. Use the U+2014 escape, never a literal em-dash in the check itself.
    - Cross-link: `grep -c "2026-08-20-HANDOFF-part8-guard-in-mcp-handlers" lib/mcp/brain-composition-census.cjs` returns at least 1, or Task 1's pointer to the rulings note serves as the link. Record which link shape was used.
  </acceptance_criteria>

  <done>
    The handoff carries a dated, evidence-bearing correction; the original text is intact; the
    conventions it contributed are explicitly restated as still binding.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record -> future implementer | A false sentence in a load-bearing comment or handoff becomes the premise of a future phase. This phase exists because that already happened once. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-257-09 | Repudiation | `lib/mcp/brain-composition-census.cjs` Seam A comment | mitigate | Replace the false parenthetical with a statement carrying its own commit-order and wire evidence, and cite the ruling doc for the surface that is genuinely open. Acceptance criteria grep for the removed strings so the correction cannot be partial. |
| T-257-10 | Repudiation | the 2026-08-20 handoff | mitigate | Append-only dated correction with the evidence inline; the `git diff --numstat` deletion cap makes silent rewriting of the original detectable. |
| T-257-11 | Tampering | accidental edit to `COMPOSITION_SITES` while editing its docblock | mitigate | Comment-only proof in acceptance criteria (`git diff -U0` shows only comment-marker lines), plus `node tests/test-254-composition-census.cjs` which fails the build on any declaration drift. |
| T-257-12 | Information Disclosure | correction text quoting research output | accept | The quoted probe results contain the deliberate synthetic canary (`CANARY7F3A2B dana@acme.io`), not real user data. Do not substitute a real example. |
| T-257-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed this phase (257-RESEARCH.md metadata). This plan is comment and documentation only. |
</threat_model>

<verification>
- `node tests/test-254-composition-census.cjs` passes with an unchanged arm count.
- `node scripts/check-substrate.cjs --diff` exits 0.
- `git diff --numstat` on the handoff shows deletions of at most 1.
- No executable line changes anywhere in this plan: `git diff -U0` on both files shows only comment
  or markdown lines.
</verification>

<success_criteria>
- Neither of the two false claims survives anywhere in the tree.
- Both corrections carry their evidence inline, so a reader does not have to trust the correction any more than they had to trust the original.
- The handoff's still-valid conventions are explicitly named as still binding, so the correction does not accidentally retire a useful document.
- Phase 254's census test is green and unchanged.
</success_criteria>

<output>
Create `.planning/phases/257-part-8-enforcement-locus-host-independent-egress-guard/257-03-SUMMARY.md` when done.
Record verbatim: the `tests/test-254-composition-census.cjs` arm count, and the
`git diff --numstat` line for the handoff proving the append-only property.
</output>
