---
phase: 257-part-8-enforcement-locus-host-independent-egress-guard
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/257-NOTE-part8-enforcement-locus-rulings.md
  - docs/257-NOTE-theo-forward-compat-enforcement-locus.md
autonomous: true
requirements: [LOCUS-05, LOCUS-06, LOCUS-09]
canon_parts: [3, 7, 8]
user_setup: []

must_haves:
  truths:
    - "A reader can learn, from one document, why no far-side guard is built and why that is principled rather than merely pragmatic"
    - "A reader can learn which of the four paths to the Brain have in-code Part 8 enforcement today and which have none, without reading any source"
    - "The open high-severity RCA on the hook over-blocking pws-brain-mcp is named and cited, and is visibly NOT investigated by this phase"
    - "A future Theo cutover session finds T-1, T-2 and T-3 written down instead of rediscovering them"
  artifacts:
    - path: "docs/257-NOTE-part8-enforcement-locus-rulings.md"
      provides: "D-01 far-side ruling, D-02 direct-HTTP ruling, the four-path coverage table, D-05 G2 flag, and the four adjacent flagged-not-fixed items"
      min_lines: 90
    - path: "docs/257-NOTE-theo-forward-compat-enforcement-locus.md"
      provides: "T-1 matcher goes dark on flip day, T-2 catalog consolidation defeats name-based enforcement, T-3 the window is open now"
      min_lines: 60
  key_links:
    - from: "docs/257-NOTE-part8-enforcement-locus-rulings.md"
      to: ".planning/debug/part8-egress-guard-blocks-pws-brain-mcp-unconditionally.md"
      via: "the direct-HTTP ruling cites the open RCA by filename without investigating it"
      pattern: "part8-egress-guard-blocks-pws-brain-mcp-unconditionally"
    - from: "docs/257-NOTE-theo-forward-compat-enforcement-locus.md"
      to: "docs/257-NOTE-part8-enforcement-locus-rulings.md"
      via: "T-2 states the same convergence the rulings note reaches for MindrianOS"
      pattern: "257-NOTE-part8-enforcement-locus-rulings"
---

<objective>
Write down the three rulings this phase is required to record and is forbidden to implement.

D-01 (far-side): LOCAL-ONLY, documented. No far-side guard is built. `mcp-server-brain/` in this repo
is the DEAD Neo4j plus Pinecone service the operator was told to suspend; the real live far side is
`ProblemsWorthSolving-Brain`, an ESM repository this repo's CJS classifier cannot be required into.
And the principled half: Part 8's text is "the Brain must never RECEIVE user content." A far-side
check runs on content the process has already received over TLS, so it can prevent USE, never
RECEIPT. Only the client-side locus can satisfy the literal text.

D-02 (direct-HTTP): ACCEPT AND DOCUMENT. The `pws-brain-mcp` direct-HTTPS registration stays as-is.
Document the gap honestly: two of three product surfaces have zero in-code Part 8 enforcement today,
and the one hook standing there carries an open, unfixed, high-severity RCA.

D-08 (Theo): a written forward-compatibility note covering T-1, T-2 and T-3.

CONTAINER DECISION (Claude's Discretion, exercised and stated). CONTEXT.md leaves the container open
between `docs/MINDRIAN-CANON.md` Part 8 and a standalone phase doc. This plan chooses a STANDALONE
PHASE DOC, following the `docs/254-NOTE-*` and `docs/262-NOTE-*` precedent, and does NOT edit
MINDRIAN-CANON.md. Reason: amending the Canon in this repo is a machinery event, not a paragraph. The
existing Part 8 amendment (the orchestration-projection section) came with an Appendix D entry, a
header Version bump, and a `docs/CANON-PHASE-MAP.md` ledger row, and the map shows navigator-gated
amendments for frozen-property additions. D-01 and D-02 are RULINGS ABOUT ENFORCEMENT LOCUS, not
doctrine changes: the `LOCAL data -> BRAIN: NO` invariant is untouched by both. Invoking the
amendment machinery would inflate the phase and imply a doctrine change that did not happen. The
notes are made discoverable instead through Plan 03's corrected census comment and handoff correction
block, which both point at the rulings note by filename.

Purpose: the record states honestly what is enforced, what is not, and why the gap was accepted.
Output: two standalone notes under `docs/`, cross-linked to the RCA, the deferred item, and each
other.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/257-part-8-enforcement-locus-host-independent-egress-guard/257-CONTEXT.md
@.planning/phases/257-part-8-enforcement-locus-host-independent-egress-guard/257-RESEARCH.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase-wide symbol ledger. This plan owns the rows marked OWNED.

| Symbol / artifact | Kind | Owner plan | This plan |
|---|---|---|---|
| `egress_blocked` refusal kind + `BRAIN_EGRESS_BLOCKED` status | code | 257-01 | - |
| `refusal` + `egress_disclosure` pass-through in `wrapDirective()` | code | 257-02 | - |
| corrected census parenthetical (`lib/mcp/brain-composition-census.cjs`) | comment | 257-03 | - |
| dated correction block on the 2026-08-20 handoff | docs | 257-03 | - |
| `docs/257-NOTE-part8-enforcement-locus-rulings.md` | docs | 257-04 | OWNED |
| `docs/257-NOTE-theo-forward-compat-enforcement-locus.md` | docs | 257-04 | OWNED |
| de-frozen hook-matcher literals in the two red 239 arms | test | 257-05 | - |
| `tests/test-257-refusal-egress-kind.cjs` | test | 257-01 | - |
| `tests/test-257-envelope-passthrough.cjs` | test | 257-02 | - |
| `tests/test-257-shim-honest-refusal.cjs` | test | 257-06 | - |
| `tests/test-257-brain-tool-egress-invariant.cjs` | test | 257-07 | - |
| `tests/test-257-strict-input-shapes.cjs` | test | 257-08 | - |
| `tests/run-all-257.sh` | test aggregator | 257-07 | - |
| `LOCUS-01..LOCUS-10` rows in `.planning/REQUIREMENTS.md` | planning record | 257-09 | - |

<tasks>

<task type="auto">
  <name>Task 1: The far-side ruling and the four-path coverage table (D-01)</name>

  <read_first>
    - 257-CONTEXT.md D-01 in full. The ruling text is authoritative; this note states it, it does not
      re-decide it.
    - 257-RESEARCH.md "The Far-Side Ruling, Re-Framed" - the six independent signals that
      `mcp-server-brain/` is the dead service, the evidence that the live far side is
      `ProblemsWorthSolving-Brain` (ESM, Memgraph), and options A / B / C with their costs.
    - 257-RESEARCH.md "Architecture Patterns" - the four-path ASCII diagram (P1 through P4). The
      coverage table in this note is derived from it.
    - 257-RESEARCH.md "Assumptions Log" A2 - the note must carry A2's caveat honestly: which artifact
      provisions the live Render service is MEDIUM confidence, inferred from
      `BRAIN_HTTP_PUBLIC_HOST` plus this repo's CLAUDE.md, and the render.yaml calls itself a
      "PHASE-A DRAFT".
    - `docs/MINDRIAN-CANON.md:264-311` - Part 8's actual text, especially the `LOCAL data -> BRAIN: NO`
      invariant block and the PR gate paragraph. Quote the invariant verbatim; do not paraphrase it.
    - `docs/254-NOTE-theo-adaptation-list-additions.md` - the house format for a phase NOTE doc.
  </read_first>

  <files>docs/257-NOTE-part8-enforcement-locus-rulings.md</files>

  <action>
    Create the note. Header block naming the phase, the date 2026-09-02, the status (navigator
    rulings, ratified, not proposals), and a one-line statement that this document does NOT amend
    `docs/MINDRIAN-CANON.md` and that the `LOCAL data -> BRAIN: NO` invariant is unchanged by
    everything in it.

    Section 1, the far-side ruling (D-01). Three parts:
    - The pragmatic finding: `mcp-server-brain/` in this repo is the dead Neo4j plus Pinecone
      `mindrian-brain` service. List the signals as a table: package deps `neo4j-driver ^5.28` and
      `@pinecone-database/pinecone ^5.1`, render.yaml service name `mindrian-brain`, zero memgraph
      references anywhere in the directory, last substantive commit in the 127.x era, CLAUDE.md's
      stack table naming Memgraph with Pinecone RETIRED, and the 2026-08-11 operator runbook naming
      it confirmed dead spend and instructing suspension. A guard placed there would guard nothing.
    - The structural finding: the live far side is `ProblemsWorthSolving-Brain` (ESM,
      `"type": "module"`, `src/server.mjs`, Memgraph-backed). `lib/core/part8-egress-guard.cjs` is
      CommonJS, so it cannot simply be required across. Vendoring it would create two copies with no
      shared test, which Canon Part 7 argues against, and D-01 explicitly rejected.
    - The principled finding, and state this as the load-bearing one: Part 8's text is that the Brain
      must never RECEIVE user content. A far-side classifier inspects content already received over
      TLS by a third-party-hosted process. It can refuse to STORE or QUERY; it cannot prevent
      RECEIPT. Therefore the client-side locus is not merely the convenient locus, it is the only one
      that can satisfy the literal text. Note explicitly that whether the Canon's authors meant
      "arrive at the process" or "be stored and queried" is a Canon-interpretation question and is
      listed in section 4 as flagged, not resolved.

    Section 2, the direct-HTTP ruling (D-02). ACCEPT AND DOCUMENT. State plainly:
    - `~/.claude.json` registers `pws-brain-mcp` at project scope as `{"type":"http","url":"https://pws-brain-mcp.onrender.com/mcp"}`
      with an Authorization header. Redact the header value; never print a key.
    - There is no local plugin code in that path: no shim, no `brain-client.cjs`, no belt, no
      `classify()` call.
    - This registration is a per-machine operator entry, NOT shipped by the plugin. A code change in
      this repo cannot reach it.
    - Reproduce the four-path coverage table with the columns Path, Route, In-code belt, Host hook.
      The four rows are P1 CLI via the stdio shim (belt YES, hook yes), P2 `mindrian-os` handlers via
      `brain-router` and `sensors` (belt YES, hook no because the tool name is `suggest_next`), P3
      `pws-brain-mcp` direct HTTPS (belt NO, hook yes and possibly over-blocking), P4 Theo after the
      cutover (belt NO, hook NO). Then the one-sentence conclusion: on two of the three product
      surfaces, Canon Part 8 has no in-code enforcement at all today.
    - Name what this phase DID cover and what it deliberately did not, in those same terms. This
      exact statement is what the Plan 09 Canon Custodian checkpoint reads back.
    - Cite `.planning/debug/part8-egress-guard-blocks-pws-brain-mcp-unconditionally.md` by filename
      with its metadata (status gathering, severity high, filed 2026-08-27, three calls in a real
      room all blocked with an identical F.1 card, next_action never performed). State in the same
      breath that this phase does NOT investigate or fix it. Do not read the RCA's contents into
      this note beyond those facts.
    - State the deferred item explicitly: deprecating or rerouting the direct-HTTP registration is a
      larger separate phase, rejected for 257 by D-02, and record the two things a future phase would
      need to establish (whether the operator relies on it for admin-tier work the shim cannot do,
      and whether Desktop and Cowork can be pointed at a guarded path at all).

    Section 3, G2 flagged not fixed (D-05). `brain_query` conflates a Part 8 block with a transport
    outage: `query()` returns `null` on a block at `lib/core/brain-client.cjs:884`, before `callTool`
    runs, so `callTool`'s richer sentinel never gets a chance, and the shim maps `null` to
    `refusalResponse('unreachable', ...)`. State that `query()`'s `null`-return contract is NOT
    changed this phase: roughly 82 degradation tests key on it per the note at
    `lib/core/brain-client.cjs:640-643`, which is high blast radius. State what a future fix would
    have to choose between (a sentinel return for `query()`, or a shim-side disclosure-only
    pre-check). State that Plan 07's invariant test PINS the current conflation deliberately so it
    cannot change silently.

    Section 4, adjacent, flagged and NOT fixed. One short entry each, with a filename or a citation:
    the open RCA above; the Canon-interpretation question on the meaning of RECEIVE; D-239-05-01 at
    `.planning/phases/239-brain-access-surface/deferred-items.md` (payload design, generic handle
    versus raw domain text, and the measured interaction that ordinary methodology traffic classifies
    `ambiguous` routinely so this phase's disclosure will fire often and must stay quiet); the
    missing canonical `brain-boundary-scan` script, where the Canon at
    `docs/MINDRIAN-CANON.md:301` requires a check that no script implements under that name and the
    closest real implementation is `scripts/doctor.cjs`'s Class O `agentshield-all-surfaces-clean`
    which delegates to `classify()`; and the staleness of `mcp-server-brain/`, with the research's
    cheap suggestion of a `DEPRECATED.md` recorded as a nice-to-have this phase did not do.

    Section 5, Tri-Polar effect. One short paragraph per surface (CLI, Desktop, Cowork) stating what
    changes for it in this phase and what does not.

    Carry assumption A2's MEDIUM-confidence caveat wherever the note claims which artifact provisions
    the live Render service. Do not state it as certain.
    No em-dashes anywhere.
  </action>

  <verify>
    <automated>node scripts/check-substrate.cjs --diff</automated>
  </verify>

  <acceptance_criteria>
    - File exists at `docs/257-NOTE-part8-enforcement-locus-rulings.md` and is at least 90 lines.
    - Content assertion: each of `D-01`, `D-02`, `D-05`, `ProblemsWorthSolving-Brain`, `pws-brain-mcp`, `part8-egress-guard-blocks-pws-brain-mcp-unconditionally`, `D-239-05-01`, `brain-boundary-scan`, `agentshield-all-surfaces-clean` appears at least once (`grep -c` per token, each at least 1).
    - Content assertion: the four-path table has exactly four data rows and each row states an in-code belt value of YES or NO. Verify by reading; state the four values in the summary.
    - Content assertion: `grep -c "RECEIVE" docs/257-NOTE-part8-enforcement-locus-rulings.md` returns at least 1, proving the principled half of D-01 is present and not just the pragmatic half.
    - Negative assertion (no doctrine drift): `git status --porcelain docs/MINDRIAN-CANON.md` shows the file unmodified. This plan must not touch the Canon.
    - Negative assertion (no secret): `grep -ci "authorization: *bearer\|sk-\|Bearer ey" docs/257-NOTE-part8-enforcement-locus-rulings.md` returns 0.
    - Style: the file contains zero em-dash characters (search for the U+2014 codepoint, not the ASCII double hyphen).
  </acceptance_criteria>

  <done>
    Both navigator rulings are on the record with their evidence, the coverage gap is stated in the
    same terms the Canon Custodian checkpoint will read back, and every adjacent item is visibly
    flagged rather than silently dropped.
  </done>
</task>

<task type="auto">
  <name>Task 2: The Theo forward-compatibility note (D-08)</name>

  <read_first>
    - 257-RESEARCH.md "Theo Forward-Compatibility" in full - T-1, T-2, T-3, the verbatim quote from
      Theo's `register-content-tool.ts` GUARD-01 docblock, and the verbatim admission from Theo's own
      CLAUDE.md that an egress gate matching the real Brain's Part 8 guard is still needed.
    - `/home/jsagi/Theo/CLAUDE.md` - confirm the two quoted lines still read as research recorded
      them (the "Schema direction" admission and the line about Part 8 drawing a line INSIDE Theo's
      own catalog). Quote what is on disk today; if the text has moved, quote the current text and
      say so.
    - `/home/jsagi/Theo/src/mcp/register-content-tool.ts` - read the GUARD-01 docblock and the
      `z.strictObject(config.inputShape)` call. READ ONLY. This phase does not touch Theo's repo.
    - `hooks/hooks.json` - read the two matcher strings as they are on disk today; T-1 depends on the
      exact alternation. Do not freeze the literal into the note without also naming the file and
      line so a reader re-derives it.
    - `docs/254-NOTE-theo-adaptation-list-additions.md` and `docs/262-NOTE-theo-adaptation-list-additions.md`
      - the two precedents for a written-not-code Theo note, including how they cross-reference.
    - `docs/2026-09-01-HANDOFF-phases-272-274-275-plus-theo-flip-coordination.md` - the current Theo
      status (Phase 9 at 10/12 plans, no remote hosting story, Phase 08.4 not started). T-3's claim
      that the window is open depends on this and must cite it.
  </read_first>

  <files>docs/257-NOTE-theo-forward-compat-enforcement-locus.md</files>

  <action>
    Create the note in the `254-NOTE` / `262-NOTE` house format.

    T-1, the matcher goes dark on flip day. `hooks/hooks.json`'s PreToolUse and PostToolUse matchers
    both cover `(mindrian-brain|pws-brain-mcp)`. Theo's tools are `mcp__theo__*` and will not match.
    When `pws-brain-mcp` cuts over to Theo (Theo Phase 9, Brain-Contract Cutover), the
    defence-in-depth layer disappears with no error and no announcement. State that the matcher edit
    belongs to the cutover phase, not to 257. Name the file so a reader re-derives the matcher rather
    than trusting a literal copied into this note.

    T-2, catalog consolidation defeats name-based enforcement, and the convergence. Theo's own
    doctrine draws the Part 8 line INSIDE its catalog: content tools (generic handles only) and
    operational tools (`room_bind`, `graph_write`, which legitimately receive room content) share one
    server name. A regex over `mcp__theo__*` cannot distinguish them, so any future name-based hook
    would either block legitimate `graph_write` calls or allow content-tool leaks. The only viable
    locus is Theo's own `registerContentTool`, a single-place per-tool-class registrar. State the
    convergence plainly, because it is the most transferable thing this phase produces: enforcement
    belongs at a chokepoint the code owns, not at a name pattern a host matches. That is the same
    conclusion `docs/257-NOTE-part8-enforcement-locus-rulings.md` reaches for MindrianOS; cross-link
    it.

    T-3, the window is open now. Theo is registered in `~/.claude.json` as a local stdio server, and
    per the 2026-09-01 handoff it has no remote hosting story with its own Phase 08.4 not started. A
    client-side `classify()` at `registerContentTool` is viable today and would be structurally
    correct. When Theo goes remote, that window closes the same way it closed for `pws-brain-mcp`.
    Cite the handoff for the hosting status.

    Add a short section on what Theo has that MindrianOS does not, and what it does not have.
    GUARD-01's `z.strictObject` seal rejects UNDECLARED KEYS and Theo measured that a plain shape
    silently accepted `{framework:'x', roomSecret:'LEAK'}` and dropped the extra key with no trace.
    That is a leak class the MindrianOS belt does not close, and Plan 08 of this phase closes it here
    after confirming the same gap exists in this repo's own pins. What Theo does NOT have is any
    content classification: GUARD-01 never inspects the VALUE of a declared key, so a question-shaped
    input carrying user prose passes cleanly, and Theo's own CLAUDE.md admits an egress gate matching
    the real Brain's Part 8 guard is still needed. Quote that admission verbatim.

    Add an explicit do-not list: this phase does not touch Theo's repo code, does not edit the hook
    matchers, and does not re-derive the already-known `brain_query` Theo-shape normalization issue
    (commit `21fdd7bc` addresses it at `lib/core/brain-client.cjs` around lines 920-929; the
    2026-09-01 handoff owns that item).

    Cross-reference per CLAUDE.md's standing Theo consult rule: state which Theo file each claim came
    from, and note that Theo's per-phase `{phase}-MOS-LEARNING.md` files are where a Theo-side reader
    would record the reciprocal.
    No em-dashes.
  </action>

  <verify>
    <automated>node scripts/check-substrate.cjs --diff</automated>
  </verify>

  <acceptance_criteria>
    - File exists at `docs/257-NOTE-theo-forward-compat-enforcement-locus.md` and is at least 60 lines.
    - Content assertion: each of `T-1`, `T-2`, `T-3`, `registerContentTool`, `z.strictObject`, `mcp__theo__`, `hooks/hooks.json`, `257-NOTE-part8-enforcement-locus-rulings` appears at least once.
    - Content assertion: the note contains the verbatim Theo CLAUDE.md admission about an egress gate matching the real Brain's Part 8 guard, quoted as a quotation with its source file named.
    - Freshness: the two quoted Theo lines were re-read from `/home/jsagi/Theo` this session, not copied blind from 257-RESEARCH.md. Record in the summary whether the on-disk text matched research's transcription, and quote the current text if it moved.
    - Negative assertion (read-only across the repo boundary): `git -C /home/jsagi/Theo status --porcelain` shows no change attributable to this plan. Record the command output in the summary.
    - Negative assertion: `git status --porcelain hooks/hooks.json` shows the file unmodified.
    - Style: the file contains zero em-dash characters.
  </acceptance_criteria>

  <done>
    T-1, T-2 and T-3 are written down with their sources, the convergence with this phase's own
    conclusion is stated, and the cutover phase inherits a smaller diff instead of a rediscovery.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator machine config -> tracked documentation | `~/.claude.json` carries a live Authorization bearer for the direct-HTTP Brain registration. Documenting the registration must not document the credential. |
| this repo -> Theo repo | The Theo consult is read-only. Any write across that boundary is out of scope and unreviewed by Theo's own gates. |
| accepted gap -> future reader | An accepted risk that is not written down becomes an unknown risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-257-13 | Information Disclosure | the direct-HTTP registration documented in the rulings note | mitigate | Redact the Authorization header value; document the shape and the URL only. Acceptance criteria grep for bearer-token patterns and require 0 hits. |
| T-257-14 | Tampering | enforcement bypass via unmatched tool namespace (P3 today, P4 on flip day) | accept | Accepted by navigator ruling D-02 for this phase, and documented rather than silently carried. The rulings note states which surfaces have zero in-code enforcement and cites the open RCA on the one hook standing there. Revisiting is a named separate phase. |
| T-257-15 | Repudiation | an accepted gap silently disappearing from the record | mitigate | Section 4 of the rulings note enumerates every flagged-not-fixed item with a filename, and the Plan 09 Canon Custodian checkpoint reads the coverage statement back before the phase closes. |
| T-257-16 | Information Disclosure | undeclared-key smuggling, documented here and fixed in Plan 08 | mitigate | The Theo note records the measured leak class and points at Plan 08 as its closure in this repo. Documentation only; the fix is Plan 08's. |
| T-257-17 | Tampering | accidental write into the Theo repository | mitigate | Read-only consult. Acceptance criteria require `git -C /home/jsagi/Theo status --porcelain` to show no attributable change, recorded in the summary. |
| T-257-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed this phase (257-RESEARCH.md metadata). This plan is documentation only. |
</threat_model>

<verification>
- Both note files exist, meet their minimum line counts, and pass every content grep in the acceptance criteria.
- `git status --porcelain docs/MINDRIAN-CANON.md hooks/hooks.json` shows both files unmodified.
- `git -C /home/jsagi/Theo status --porcelain` shows no change attributable to this plan.
- `node scripts/check-substrate.cjs --diff` exits 0.
- Neither note contains an em-dash or a credential value.
</verification>

<success_criteria>
- The far-side ruling is on the record with BOTH halves: the pragmatic finding that the local far-side directory is dead, and the principled finding that a far-side guard cannot satisfy Part 8's literal text.
- The direct-HTTP gap is stated in the exact terms the Canon Custodian checkpoint reads back: which of the four paths this phase covers, and which it deliberately does not.
- Every adjacent item (the open RCA, the Canon-interpretation question, D-239-05-01, the missing brain-boundary-scan script, mcp-server-brain staleness) is flagged with a citation and visibly not fixed.
- T-1, T-2 and T-3 are written down with sources re-read from Theo this session, and nothing in Theo's repo was written.
</success_criteria>

<output>
Create `.planning/phases/257-part-8-enforcement-locus-host-independent-egress-guard/257-04-SUMMARY.md` when done.
Record verbatim: the four in-code-belt values from the coverage table, whether the two quoted Theo
lines matched research's transcription, and the `git -C /home/jsagi/Theo status --porcelain` output.
</output>
