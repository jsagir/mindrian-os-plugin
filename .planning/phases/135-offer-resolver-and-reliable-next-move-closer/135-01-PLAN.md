---
phase: 135-offer-resolver-and-reliable-next-move-closer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/memory/navigation-engine-offer.test.cjs
  - lib/memory/offer-closer.test.cjs
  - tests/test-135-resolver-no-leak.cjs
  - tests/test-135-decide-wiring-e2e.cjs
  - tests/run-all-135.sh
  - scripts/intent-classifier.cjs
autonomous: true
requirements: [SC1, SC2, SC3, SC4, SC6, SC7, SC9]
canon_parts: [Part 3, Part 9]

must_haves:
  truths:
    - "ALL resolver production inputs are present in the engine context passed to decide(): operator, roomState (db + roomDir + invocationsSinceDecision), sectionPath, jtbd, problemType -- not just operator"
    - "The decide() context carries a live room.db handle on roomState.db when room.db exists, so rankForSelector / shouldExclude / neighborhood reads run on REAL room state, never an empty fixture"
    - "Wave 0 test files exist and run (RED) so resolver + closer have a failing target"
    - "An end-to-end wiring test drives decide() with a REAL temp room.db on a DECISION_GATE turn and asserts offer_next_step is non-null with a real [[section]] reason (not [[undefined]]) and a defined scope -- the single test that catches the dark-loop class of bug"
    - "tests/run-all-135.sh aggregates the phase suites and exits non-zero on any failure"
  artifacts:
    - path: "lib/memory/navigation-engine-offer.test.cjs"
      provides: "Resolver + abstention unit tests (SC1, SC3, SC6) -- RED until 135-02"
      contains: "resolveOfferNextStep"
    - path: "lib/memory/offer-closer.test.cjs"
      provides: "F.1 closer wiring + edge persistence tests (SC7) -- RED until 135-03"
      contains: "recordSelectorDecision"
    - path: "tests/test-135-resolver-no-leak.cjs"
      provides: "fs-instrument zero-leak test (SC2, SC9) -- mirrors test-navigation-acceptance.cjs proxy idiom"
      contains: "USER.md"
    - path: "tests/test-135-decide-wiring-e2e.cjs"
      provides: "End-to-end production-wiring test (SC1/SC4/SC6) -- drives decide() through a REAL temp room.db; RED until 135-01 Task 1 wiring + 135-02 resolver land"
      contains: "offer_next_step"
    - path: "tests/run-all-135.sh"
      provides: "Phase 135 scoped runner (mirror of tests/run-all-129.sh)"
      contains: "CJS_SUITES"
  key_links:
    - from: "scripts/intent-classifier.cjs"
      to: "lib/conversation/operator.cjs"
      via: "operator.getCurrent(roomDir).current added to engine context"
      pattern: "operator"
    - from: "scripts/intent-classifier.cjs"
      to: "lib/core/navigation.cjs"
      via: "getCurrentJTBD(roomDir).jtbd + openRoomDb(roomDir) handle on roomState.db, populating the resolver's production inputs"
      pattern: "getCurrentJTBD"
---

<objective>
Lay the test substrate and the load-bearing call-site wiring the resolver needs, BEFORE the resolver body exists.

Two things ship here:
1. The Wave 0 test scaffolds (RED): the resolver/abstention unit suite, the closer integration suite, the fs-instrument zero-leak suite, the end-to-end production-wiring suite, and the run-all-135 aggregator. These give 135-02 and 135-03 a failing target to drive against (Nyquist: tests precede the code that satisfies them).
2. The production-input call-site wiring (Pitfall 3 in RESEARCH.md + checker Blocker 1): `scripts/intent-classifier.cjs:~1096` builds the engine context as `{ quadruple, brainAvailable, userPersona, intentSignal }` with NONE of the resolver's runtime inputs. The resolver reads `context.operator`, `context.roomState` (db + roomDir + invocationsSinceDecision), `context.sectionPath`, `context.jtbd`, and `context.problemType`. If only `operator` is added, `rankForSelector` runs on an empty roomState, the grounded reason becomes `[[undefined]]`, and the offer loop is dark in production while in-memory unit tests pass green. This plan wires ALL of those production inputs at the ONE call site, so the resolver computes against real room state from turn one.

Purpose: de-risk the whole phase by proving the FULL production input set reaches the engine (not just operator), and by writing the verification harness first -- including the one end-to-end wiring test that would catch the dark-loop class of bug.
Output: 4 new test files, 1 new runner, 1 edited classifier context build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/135-offer-resolver-and-reliable-next-move-closer/135-CONTEXT.md
@.planning/phases/135-offer-resolver-and-reliable-next-move-closer/135-RESEARCH.md

<interfaces>
<!-- Source-of-truth signatures the executor needs. Extracted from codebase; do NOT re-explore. -->

From lib/conversation/operator.cjs (Phase 99):
  getCurrent(roomDir) -> { current: 'JUST_TALK'|'EXPLORE_CAPTURE'|'BUILD_ROOM'|'METHODOLOGY'|'DECISION_GATE', ... }
  // never throws; falls back to { current: 'JUST_TALK' } when the file is absent or invalid.

From lib/core/navigation.cjs (Phase 109/129 -- the SQL chokepoint; ALL sync):
  getCurrentJTBD(roomDir)   -> { ok, jtbd, ... } | { ok:false, reason:'no_room_db' }   // roomDir-taking; opens room.db internally; cache fallback
  getCurrentOperator(roomDir) -> event-log authoritative + cache fallback (alternative operator source)
  getNeighborhood(db, focusNodeId, opts) -> neighborhood   // db-handle-taking, sync
  findRecentChanges(db, sinceTs, { eventType, limit }) -> Array<event>   // db-handle-taking, sync (memory_event tail)
  firstCapturedLastTouchedBySection(db, sectionSlug) -> { first_captured, last_touched, ... }   // db-handle-taking, sync (FEYNMAN temporal)
  getActiveFocus(db) -> focus   // db-handle-taking, sync
  // room.db is opened via lib/core/room-db.cjs openRoomDb(roomDir); navigation/ is the only allow-listed caller of room-db.cjs.
  // The engine NEVER opens room.db; the CALLER (intent-classifier) opens it and passes the handle on context.roomState.db (Pitfall 5).

From lib/core/folder-memory.cjs (Phase 90):
  readQuadruple(sectionPath) -> { room, state, reasoning, brain }
  // reasoning.governing_thought is the MINTO.md governing thought (parsed from MINTO.md); reasoning is the third quadruple leg.

From lib/workflow/selector-decisions.cjs (Phase 125):
  shouldExclude(command_id, roomState) -> boolean
  // reads roomState.invocationsSinceDecision[command] when present, ELSE counts f_selector_decision events via the
  // memory_event tail when roomState.db is set. roomState.db MUST be populated for the backoff loop to read persisted state.

From lib/core/navigation-engine.cjs (the consumer being prepared):
  decide(turn, context) -> decision
  // context today = { quadruple, brainAvailable, userPersona, intentSignal }  (intent-classifier.cjs:~1096)
  // ctx = context || {} at line 313; resolveOfferNextStep(ctx, brain) is called at line 475.
  // resolveOfferNextStep(_context, _brain) is still a `return null` stub at lines 280-282 -- DO NOT touch it here (135-02 fills it).

From scripts/intent-classifier.cjs (the call site):
  runNavigationEngine(roomDir, sessionId)  // builds `context` at ~1096, calls callDecideWithTimeout(navEngine.decide, turn, context, ...)
  // sectionPath already resolved at line 1023 (resolveActiveSectionPathForRoom).
  // quadruple already read at ~1036 (folderMemory.readQuadruple(sectionPath)); reasoning leg carries MINTO governing_thought.
  // userPersona.problem_type already computed at line 1057 (null when 'unknown').
  // brainAvailable lazy-require + try/catch pattern at ~1085-1094 is the template to mirror for every new lazy read.

Test idiom (mirror these EXACTLY -- do not invent a new framework):
  - Framework: direct-CJS `node:assert/strict`, files are `*.test.cjs` / `test-*.cjs`. No external runner.
  - fs-instrument proxy idiom: tests/test-navigation-acceptance.cjs (installs an fs proxy BEFORE the flow, uninstalls AFTER, asserts the only non-SQLite read is the allow-listed USER.md).
  - Real temp room.db idiom: tests/test-navigation-acceptance.cjs (creates a temp room, opens room.db via the chokepoint, seeds nodes/edges/memory_events, tears down). Mirror its setup for the E2E wiring test.
  - Runner shape: tests/run-all-129.sh (SHELL_SUITES + CJS_SUITES arrays, per-suite PASS/FAIL line, set -uo pipefail, non-zero exit on any fail).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ALL resolver production inputs into the engine context at the decide() call site</name>
  <read_first>
    - scripts/intent-classifier.cjs (the file being modified; read runNavigationEngine ~995-1140, especially: sectionPath at 1023, quadruple at ~1036, userPersona.problem_type at 1057, brainAvailable lazy-require template at ~1085-1094, the context literal at ~1096)
    - lib/conversation/operator.cjs (getCurrent signature + JUST_TALK default)
    - lib/core/navigation.cjs (getCurrentJTBD / getCurrentOperator roomDir-taking signatures lines ~111-121; openRoomDb is reached via room-db.cjs internally -- the caller opens room.db through the chokepoint, never requiring room-db.cjs directly)
    - lib/core/room-db.cjs (openRoomDb(roomDir) -> db handle; close() -- the caller owns the handle lifecycle)
    - lib/workflow/selector-decisions.cjs (shouldExclude reads roomState.db / roomState.invocationsSinceDecision lines ~82-99, 234-250)
    - 135-RESEARCH.md Pitfall 3 (operator-context) + Pitfall 5 (roomState.db must be populated by the caller, engine never opens room.db) + the checker Blocker 1 note
  </read_first>
  <action>
    In scripts/intent-classifier.cjs, inside runNavigationEngine(roomDir, sessionId), extend the `context` object literal (currently `{ quadruple, brainAvailable, userPersona, intentSignal }` at ~line 1096) so it carries the COMPLETE set of resolver production inputs. Do all new reads with the SAME lazy-require + try/catch + safe-default pattern already used for brainClient.isAvailable() at ~1085-1094, so any missing module or absent room.db degrades gracefully and never crashes the hot path. Add these fields:
      1. `operator` -- lazy-require lib/conversation/operator.cjs; `operator.getCurrent(roomDir).current`; try/catch default to the string 'JUST_TALK' (operator.cjs's own cold-start default). Cite Phase 135 SC6 (JUST_TALK silence rule) in a one-line comment.
      2. `sectionPath` -- pass the already-resolved `sectionPath` variable (line 1023) straight through. This is the scope + the [[wikilink]] target the resolver's grounded reason needs; without it the reason becomes [[undefined]] (the exact dark-loop symptom).
      3. `problemType` -- pass `userPersona && userPersona.problem_type` (already computed at 1057; null when unknown). This is a rankForSelector input.
      4. `jtbd` -- lazy-require lib/core/navigation.cjs; call `navigation.getCurrentJTBD(roomDir)`; take `.jtbd` when `.ok !== false`, else null. getCurrentJTBD is roomDir-taking and opens room.db internally through the chokepoint (sync, A3-compatible). try/catch default null. Cite SC4 (active-JTBD intent leg).
      5. `roomState` -- an object `{ db, roomDir, invocationsSinceDecision }`:
         - `roomDir`: the roomDir param.
         - `db`: a live room.db handle. Open it via the navigation chokepoint pattern (lazy-require lib/core/room-db.cjs openRoomDb(roomDir) is the chokepoint-internal door -- but per the substrate guard, the consumer path opens through the existing allow-listed pattern; reuse whatever open helper navigation/spine-events uses, do NOT add a NEW direct room-db.cjs require if one is already reachable). Guard with fs.existsSync on `<roomDir>/.mindrian/room.db`; when absent, set `db: null` (the resolver and shouldExclude both treat a null db as tier-0/empty-state and abstain gracefully -- never crash). The handle MUST be CLOSED in a try/finally after callDecideWithTimeout resolves/rejects (mirror the existing promise .then/.catch tail at ~1103-1136; close in a finally so a thrown decide() never leaks the handle).
         - `invocationsSinceDecision`: leave undefined for v1 (shouldExclude falls back to the memory_event tail via roomState.db); add a one-line comment that this is the optional fast-path counter, populated by the closer in 135-03, read by shouldExclude.
    Keep `quadruple`, `brainAvailable`, `userPersona`, `intentSignal: null` unchanged. Do NOT make decide() async and do NOT await anything new (A3 LOCKED: the resolver stays synchronous and local-only; getCurrentJTBD, openRoomDb, and all chokepoint reads are sync). Do NOT require room-db.cjs directly if the open is already reachable via a navigation/ helper -- prefer the allow-listed door (the substrate guard fails a new direct room-db.cjs require outside navigation/). No em-dashes anywhere in the edit (use hyphens).
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin &amp;&amp; node -c scripts/intent-classifier.cjs &amp;&amp; node -e "const s=require('fs').readFileSync('scripts/intent-classifier.cjs','utf8'); const idx=s.indexOf('const context = {'); const end=s.indexOf('callDecideWithTimeout'); const ctx=s.slice(idx, end>idx?end:idx+800); const need=['operator','sectionPath','problemType','jtbd','roomState']; const missing=need.filter(k=>!new RegExp('\\\\b'+k+'\\\\s*:').test(ctx)); if(missing.length){console.error('FAIL: context literal missing fields: '+missing.join(', '));process.exit(1);} if(!/getCurrentJTBD/.test(s)){console.error('FAIL: getCurrentJTBD not wired');process.exit(1);} if(/\u2014/.test(ctx)){console.error('FAIL: em-dash in context region');process.exit(1);} console.log('PASS: all 5 resolver inputs wired into context; jtbd via chokepoint; no em-dash');"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: the `context` object literal in runNavigationEngine carries ALL of `operator`, `sectionPath`, `problemType`, `jtbd`, and `roomState` -- not operator alone (the grep gate fails if any one is missing).
    - SOURCE: `operator` resolves from operator.getCurrent(roomDir).current; `jtbd` resolves from navigation.getCurrentJTBD(roomDir).jtbd; `sectionPath` is the line-1023 variable; `problemType` is userPersona.problem_type; `roomState` is `{ db, roomDir, invocationsSinceDecision }`.
    - BEHAVIOR: when room.db is absent, roomState.db is null and decide() still runs without throwing (the resolver abstains gracefully). When operator.cjs throws or roomDir is null, operator falls back to 'JUST_TALK'.
    - SOURCE: the room.db handle is opened through the allow-listed chokepoint pattern (no NEW direct room-db.cjs require outside navigation/) and closed in a try/finally after decide() settles (no handle leak).
    - SOURCE: decide() is still invoked synchronously via callDecideWithTimeout; no `await` was introduced; no `async` keyword added.
    - CLI: `node -c scripts/intent-classifier.cjs` parses clean.
    - Zero em-dashes in the edited region.
  </acceptance_criteria>
  <done>The COMPLETE resolver production input set (operator + roomState{db,roomDir,invocationsSinceDecision} + sectionPath + jtbd + problemType) flows into context, so rankForSelector / shouldExclude / the grounded reason all run against REAL room state. The loop is no longer dark, the reason can never be [[undefined]], and the handle is closed cleanly. classifier still parses, stays synchronous, and degrades gracefully when room.db is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Create the resolver + abstention RED unit suite and the closer RED integration suite</name>
  <read_first>
    - lib/memory/navigation-engine-core.test.cjs (existing engine test idiom to mirror -- setup/teardown, assert/strict)
    - lib/memory/offer-presenter.test.cjs (existing offer test idiom; presenter is the post-filter, not under test here)
    - lib/core/navigation-engine.cjs lines 280-282 (the stub the unit suite targets) and ~311 decide() entry
    - lib/workflow/selector-decisions.cjs (recordSelectorDecision / shouldExclude / DECAY_WINDOW=5 / EXCLUSION_THRESHOLD=0.1 -- the closer suite asserts these)
    - 135-RESEARCH.md "Phase Requirements -> Test Map" (SC1/SC3/SC6/SC7 rows) and "Target return shape" code example
  </read_first>
  <action>
    Create lib/memory/navigation-engine-offer.test.cjs (direct-CJS node:assert/strict). Write tests that EXERCISE resolveOfferNextStep (require it from lib/core/navigation-engine.cjs; if it is not exported, exercise it through decide() and read decision.offer_next_step). Tests, all currently RED against the `return null` stub except the explicit-null cases:
      (SC1) returns either null or an object with EXACTLY the keys {command, framework, jtbd, confidence, reason, scope}; never an array, never more than one offer.
      (SC6) returns null when context.operator === 'JUST_TALK'.
      (SC6) returns null when the top command is in rejection-backoff (seed a context.roomState whose invocationsSinceDecision[command] makes shouldExclude true with DECAY_WINDOW=5 / EXCLUSION_THRESHOLD=0.1).
      (SC6) returns an offer at context.operator === 'DECISION_GATE' when margin >= MARGIN_THRESHOLD and not in backoff.
      (SC3) does not throw across three context fixtures: mode_a (brainAvailable true + RECOMMENDED-marker trace), mode_b (brainAvailable false), tier_0 (quadruple.brain absent). Assert it returns null-or-well-formed in each, never throws.
    Mark the assertions that depend on the unbuilt resolver with a clear comment "RED until 135-02" so the executor of 135-02 knows the target. Use a tiny in-memory roomState/context fixture; do NOT open room.db in this unit suite (the resolver is pure-local sync and reads roomState passed in). NOTE: this unit suite covers the resolver's CONTRACT on in-memory fixtures; the production-wiring path (real room.db, real reason grounding) is covered separately by tests/test-135-decide-wiring-e2e.cjs (Task 4) -- the two are complementary and neither replaces the other.
    Create lib/memory/offer-closer.test.cjs (direct-CJS node:assert/strict). Write tests, RED until 135-03:
      (SC7) given a non-null offer, the closer builds an F.1 payload whose verbs array INCLUDES 'Free-Text' as the last entry (the 10th canonical verb / escape).
      (SC7) an accept/defer/reject pick calls recordSelectorDecision with {decision, command, framework, reason, roomState} and roomState.db populated; assert the returned shape is {ok:true,...} on a valid db and {ok:false, reason:'invalid_db'} when roomState.db is absent.
      (SC6/SC7 backoff persistence) a reject pick's recordSelectorDecision writes a f_selector_decision memory_event that a SUBSEQUENT shouldExclude(command, roomState) reads back as true (seed a temp room.db, record a reject, then assert shouldExclude returns true for that command on the same roomState.db) -- this proves the backoff loop closes once 135-01 Task 1's roomState.db wiring lands. Mark "RED until 135-03".
      (SC7) a Free-Text pick routes to recordSelectorMiss (memory_event only, no decision edge).
    Both suites must run under `node <file>` and print a clear PASS/FAIL summary line. No em-dashes (hyphens only). No emoji.
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin &amp;&amp; node -c lib/memory/navigation-engine-offer.test.cjs &amp;&amp; node -c lib/memory/offer-closer.test.cjs &amp;&amp; grep -LP "\x{2014}" lib/memory/navigation-engine-offer.test.cjs lib/memory/offer-closer.test.cjs &amp;&amp; echo "PASS: both suites parse, no em-dash"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: navigation-engine-offer.test.cjs asserts the six-key offer contract {command, framework, jtbd, confidence, reason, scope} and the JUST_TALK null case.
    - SOURCE: navigation-engine-offer.test.cjs has three named fixtures mode_a / mode_b / tier_0 and asserts no-throw in each.
    - SOURCE: offer-closer.test.cjs references 'Free-Text', recordSelectorDecision, recordSelectorMiss, the invalid_db path, AND a reject-then-shouldExclude backoff-persistence assertion against a temp room.db.
    - BEHAVIOR: both suites run (exit code 0 OR a clean RED with explicit assertion failures pointing at the unbuilt resolver/closer) under `node <file>`; neither crashes with a require/syntax error.
    - Zero em-dashes; zero emoji.
  </acceptance_criteria>
  <done>Two RED suites exist that pin SC1/SC3/SC6 (resolver) and SC7 + backoff-persistence (closer); they parse and run; 135-02 and 135-03 have concrete failing targets.</done>
</task>

<task type="auto">
  <name>Task 3: Create the fs-instrument zero-leak suite, the end-to-end wiring suite, and the run-all-135 aggregator</name>
  <read_first>
    - tests/test-navigation-acceptance.cjs (the fs-proxy zero-leak idiom AND the real temp-room.db setup/teardown idiom to mirror)
    - tests/run-all-129.sh (the runner shape to mirror: SHELL_SUITES + CJS_SUITES arrays, per-suite PASS/FAIL, set -uo pipefail)
    - tests/run-all-125.sh (second runner analog)
    - scripts/intent-classifier.cjs runNavigationEngine (the production path the E2E test drives; the context wiring from Task 1)
    - lib/core/navigation-engine.cjs decide() (the entry the E2E test calls)
    - 135-RESEARCH.md "Validation Architecture" SC2 + SC9 rows and "Wave 0 Gaps" list
  </read_first>
  <action>
    Create tests/test-135-resolver-no-leak.cjs by mirroring tests/test-navigation-acceptance.cjs: install the fs proxy BEFORE driving a resolver invocation (call decide() with a fixture context, or call resolveOfferNextStep directly), uninstall AFTER, and assert that the resolver path performed ZERO non-SQLite filesystem reads except the single allow-listed USER.md (intent leg). This proves SC2 (zero non-SQLite folder scans for navigation) and contributes to SC9 (the resolver is local-only sync; it issues zero Brain payloads). If decide() reaches room.db, it must do so only via the navigation.cjs chokepoint (no direct room-db.cjs require) -- assert no off-allow-list read. Mark resolver-dependent assertions "RED until 135-02". Add a prewarm() throwaway invocation before the instrumented section so lazy-require chains are cached and do not trip the proxy (the Phase 130 idiom).

    Create tests/test-135-decide-wiring-e2e.cjs -- the SINGLE end-to-end test that catches the dark-loop / [[undefined]]-reason class of bug (checker Warning 4, mandatory). Mirror tests/test-navigation-acceptance.cjs's real-temp-room setup:
      1. Build a temp room dir with a .mindrian/ + a real room.db opened through the navigation chokepoint; seed it for a DECISION_GATE turn -- enough nodes/edges/memory_events that rankForSelector can produce >= 2 ranked items with a margin >= MARGIN_THRESHOLD (0.15) and a top command NOT in backoff; write an operator_transitioned memory_event (or operator state) so getCurrentOperator/getCurrent resolves DECISION_GATE; write a jtbd_transitioned event so getCurrentJTBD resolves a real jtbd; create a section folder with a sectionPath so the grounded reason has a real [[section]] target.
      2. Drive the production path: call scripts/intent-classifier.cjs runNavigationEngine(roomDir, sessionId) (export it or require the module and invoke the function; if it is not exported, replicate the exact context build from Task 1 and call navEngine.decide(turn, context) directly -- but PREFER driving the real runNavigationEngine so the wiring itself is under test).
      3. Assert: `decision.offer_next_step` is NON-NULL; its `reason` contains a real `[[<section>]]` wikilink and does NOT contain the literal substring `[[undefined]]`; its `scope` is defined (not undefined / not null); it has exactly the six keys {command, framework, jtbd, confidence, reason, scope}.
      4. Also assert the negative control: with the SAME seeded room but operator forced to JUST_TALK, `decision.offer_next_step` is null (abstention still fires end-to-end).
      Tear down the temp room. Mark the offer-non-null assertions "RED until 135-01 Task 1 wiring + 135-02 resolver land" (the JUST_TALK-null assertion can pass earlier). This test is RED now and GREEN only when BOTH the production wiring (Task 1) and the resolver body (135-02) exist -- it is the regression guard against the entire dark-loop failure mode.

    Create tests/run-all-135.sh mirroring tests/run-all-129.sh: set -uo pipefail, SCRIPT_DIR resolution, an empty SHELL_SUITES array, and a CJS_SUITES array listing the phase-135 suites. The CJS_SUITES entries that live under lib/memory/ must be invoked via an absolute path from the repo root (these two suites are not in tests/), and the entries under tests/ resolve relative to SCRIPT_DIR. Populate CJS_SUITES with: lib/memory/navigation-engine-offer.test.cjs, lib/memory/offer-closer.test.cjs, tests/test-135-resolver-no-leak.cjs, tests/test-135-decide-wiring-e2e.cjs. Print a per-suite PASS/FAIL line and exit non-zero if any suite fails. Add a header comment block mirroring run-all-129.sh that maps each suite to its SC coverage (note the E2E suite covers the production-wiring SC1/SC4/SC6 path). No em-dashes, no emoji. chmod +x the script.
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin &amp;&amp; node -c tests/test-135-resolver-no-leak.cjs &amp;&amp; node -c tests/test-135-decide-wiring-e2e.cjs &amp;&amp; bash -n tests/run-all-135.sh &amp;&amp; test -x tests/run-all-135.sh &amp;&amp; grep -q "CJS_SUITES" tests/run-all-135.sh &amp;&amp; grep -q "test-135-decide-wiring-e2e" tests/run-all-135.sh &amp;&amp; grep -q "USER.md" tests/test-135-resolver-no-leak.cjs &amp;&amp; grep -q "offer_next_step" tests/test-135-decide-wiring-e2e.cjs &amp;&amp; ! grep -qP "\x{2014}" tests/run-all-135.sh tests/test-135-resolver-no-leak.cjs tests/test-135-decide-wiring-e2e.cjs &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: test-135-resolver-no-leak.cjs installs/uninstalls an fs proxy (mirrors test-navigation-acceptance.cjs) and asserts USER.md is the only allow-listed non-SQLite read.
    - SOURCE: test-135-decide-wiring-e2e.cjs builds a REAL temp room.db seeded for a DECISION_GATE turn, drives the production decide() path, and asserts offer_next_step is non-null with a real [[section]] reason (never [[undefined]]) and a defined scope; plus a JUST_TALK negative control asserting null.
    - SOURCE: run-all-135.sh has set -uo pipefail, a CJS_SUITES array containing ALL FOUR suites (including test-135-decide-wiring-e2e), and exits non-zero on any failure.
    - BEHAVIOR: `bash -n tests/run-all-135.sh` parses clean; the script is executable.
    - Zero em-dashes; zero emoji.
  </acceptance_criteria>
  <done>SC2/SC9 have an instrumented zero-leak test; the dark-loop failure mode has a dedicated end-to-end production-wiring guard; the phase has a single-command runner; the aggregator references all four suites (the two lib/memory suites by repo-root-relative path).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine context -> decide() | operator / jtbd / roomState are read locally (operator.cjs / navigation.cjs chokepoint); never cross to Brain |
| caller -> room.db | the consumer (intent-classifier) opens room.db via the allow-listed chokepoint pattern; closes in finally; engine never opens it (Phase 109-06 substrate guard) |
| resolver path -> Brain | NONE in v1 (A3 LOCKED: local-only sync); no outbound payload from this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-01 | Information Disclosure | operator + jtbd + roomState reads in intent-classifier | mitigate | all are local reads (operator.cjs / navigation.cjs chokepoint); the resolved enum strings + jtbd handle never enter a Brain query. fs-leak test (Task 3) instruments the path. |
| T-135-10 | Elevation/boundary erosion | caller opening room.db for roomState.db | mitigate | opened via the allow-listed chokepoint pattern, NOT a new direct room-db.cjs require; handle closed in try/finally so no leak; substrate guard verifies. |
| T-135-02 | Tampering | new test files | accept | test-only code; no production surface; runs under node:assert/strict, no network. |
| T-135-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new dependencies this plan (Phase 87 invariant). No install task exists; nothing to verify. |
</threat_model>

<verification>
- `node -c scripts/intent-classifier.cjs` parses (the full-input context wiring is syntactically valid).
- The Task 1 grep gate confirms ALL FIVE resolver inputs (operator, sectionPath, problemType, jtbd, roomState) are present in the context literal -- not operator alone.
- `bash tests/run-all-135.sh` runs and reports per-suite status (suites RED against the unbuilt resolver/closer is expected and acceptable at this wave; the E2E wiring suite is RED until 135-02 lands).
- `grep -rnP "\x{2014}"` across the six touched files returns nothing (no em-dashes).
</verification>

<success_criteria>
- The FULL resolver production input set reaches decide() (SC1/SC4/SC6 prerequisites wired; the dark-loop / [[undefined]]-reason class is closed at the source).
- Four RED suites + one runner exist (SC1/SC2/SC3/SC4/SC6/SC7/SC9 verification harness in place), including the end-to-end production-wiring guard.
- classifier still parses, stays synchronous, and degrades gracefully when room.db is absent (Tri-Polar: no surface-specific code added).
</success_criteria>

<output>
Create `.planning/phases/135-offer-resolver-and-reliable-next-move-closer/135-01-SUMMARY.md` when done
</output>
</content>
</invoke>
