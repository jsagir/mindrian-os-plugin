---
phase: 135-offer-resolver-and-reliable-next-move-closer
plan: 03
type: execute
wave: 3
depends_on: ["135-01", "135-02"]
files_modified:
  - scripts/intent-classifier.cjs
  - lib/memory/run-feynman-tests.cjs
  - tests/run-all-135.sh
files_referenced_not_modified:
  - lib/hmi/selector-dispatcher.cjs
  - lib/workflow/selector-decisions.cjs
  - lib/vault/wikilink-builder.cjs
autonomous: true
requirements: [SC5, SC7, SC8, SC9]
canon_parts: [Part 3, Part 4, Part 8]

must_haves:
  truths:
    - "When the resolver returns an offer, the F.1 Next-Move selector fires via AskUserQuestion with a Free-Text escape (the 10th canonical verb)"
    - "An accept/defer/reject pick records a typed decision edge via recordSelectorDecision (roomState.db populated by the consumer)"
    - "A Free-Text pick routes to recordSelectorMiss (memory_event only, no edge)"
    - "An accepted offer that files an artifact injects wikilinks idempotently via the Phase 76 injector"
    - "The closer renders identically on CLI / Desktop / Cowork (AskUserQuestion, zero TUI, zero third-party OSS)"
    - "Canon Part 8: brain-boundary-scan passes; no user bytes reach the Brain"
  artifacts:
    - path: "scripts/intent-classifier.cjs"
      provides: "F.1 closer wiring after presentOffer: pickShape -> aliasToCanonical -> recordSelectorDecision/Miss + wikilink-on-accept"
      contains: "recordSelectorDecision"
    - path: "lib/memory/run-feynman-tests.cjs"
      provides: "Additive Phase 135 block registering the 135 suites (prior blocks byte-unchanged)"
      contains: "135"
  key_links:
    - from: "scripts/intent-classifier.cjs"
      to: "lib/hmi/selector-dispatcher.cjs"
      via: "pickShape({requestedShape:'F.1', payload}) renders the offer + Free-Text escape"
      pattern: "pickShape"
    - from: "scripts/intent-classifier.cjs"
      to: "lib/workflow/selector-decisions.cjs"
      via: "recordSelectorDecision / recordSelectorMiss with roomState.db populated"
      pattern: "recordSelector"
    - from: "scripts/intent-classifier.cjs"
      to: "lib/vault/wikilink-builder.cjs"
      via: "injectFiledToFooter on an accepted-offer artifact filing (idempotent)"
      pattern: "injectFiledToFooter"
---

<objective>
Wire the reliable closer: connect the resolver's emitted offer (via the already-shipped offer-presenter) through the F.1 Next-Move selector to a typed decision edge, and inject wikilinks idempotently when an accepted offer files an artifact.

This is the third net-new piece: the closer wiring. It lives in the CONSUMER path (scripts/intent-classifier.cjs), NOT the engine -- the offer is DATA; render belongs outside the engine. It extends the EXISTING presentOffer block (~lines 1218-1290); it does not build a second presenter/suppression/cap layer (those shipped in Phase 91-04).

Per RESEARCH Pitfall 5: the consumer opens room.db and populates roomState.db before recordSelectorDecision (the engine never opens room.db). Per Pitfall 6: the F.1 verbs array MUST include 'Free-Text' (CANONICAL_VERBS[9]) as the escape; a Free-Text pick routes to recordSelectorMiss.

Purpose: close the loop -- exactly one calibrated offer fires through AskUserQuestion and the pick becomes graph data.
Output: closer wiring in intent-classifier.cjs, Feynman-runner registration, and run-all-135 finalized GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/135-offer-resolver-and-reliable-next-move-closer/135-CONTEXT.md
@.planning/phases/135-offer-resolver-and-reliable-next-move-closer/135-RESEARCH.md
@docs/F-SELECTOR-CONSUMER-GUIDE.md

<interfaces>
<!-- Source-of-truth signatures. Extracted from codebase; do NOT re-explore. -->

From scripts/intent-classifier.cjs (the EXISTING presentOffer block to extend, ~lines 1218-1290):
  // lazy-requires lib/core/offer-presenter.cjs; calls readOfferHistory, classifyTurnOutcome, presentOffer, recordOfferOutcome.
  // `presented.offerLine` (string) is the grounded one-line offer that survived suppression/cap.
  // roomDir, sessionId, out.decision (carries out.decision.offer_next_step) are all in scope here.
  // The closer wiring extends THIS block: when presented.offerLine is non-empty, fire the F.1 selector.

From lib/hmi/selector-dispatcher.cjs (Phase 88.2):
  pickShape({ requestedShape:'F.1', roomDir, operator, tier, payload }) -> { shape:'F.1', rendered: { contract: { alias_map }, ... } }
  aliasToCanonical(verb, aliasMap) -> canonicalVerb
  // F.1 renders via AskUserQuestion (cross-surface). ensureFreeTextLast guarantees the escape is last; still pass it explicitly.

From lib/workflow/selector-decisions.cjs (Phase 125):
  recordSelectorDecision({ decision:'accept'|'defer'|'reject', command, framework, reason?, score_at_decision?, roomState }) -> { ok, reason? }
    // roomState.db MUST be populated by the consumer, else { ok:false, reason:'invalid_db' }.
    // roomState.roomDir used to resolve byUser via navigation.resolveByUser.
  recordSelectorMiss({ top_k_offered, user_intent, roomState }) -> { miss_id }   // memory_event only, no edge; user_intent stays LOCAL

From lib/core/navigation-engine-shared.cjs:
  CANONICAL_VERBS[9] === 'Free-Text'   // the 10th canonical verb / escape

From lib/vault/wikilink-builder.cjs (Phase 76):
  injectFiledToFooter(content, { targetPath, meetingSlug }) -> content   // idempotent; dedupes via content.includes(line)

From lib/core/offer-presenter.cjs:
  recordOfferOutcome(roomDir, { outcome:'shown'|'acted'|... })   // the presenter ledger; 'acted' on accept

Test target (built RED in 135-01):
  lib/memory/offer-closer.test.cjs   // SC7 -- this plan drives it GREEN
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fire the F.1 closer after presentOffer and record the typed decision edge</name>
  <read_first>
    - scripts/intent-classifier.cjs (the presentOffer block ~1218-1290; roomDir/sessionId/out.decision scope)
    - lib/hmi/selector-dispatcher.cjs (pickShape F.1 path ~477-484; aliasToCanonical ~121; F_SUBSHAPES)
    - lib/workflow/selector-decisions.cjs (recordSelectorDecision ~90-160 incl invalid_db at ~124; recordSelectorMiss ~334+)
    - lib/core/navigation-engine-shared.cjs (CANONICAL_VERBS / 'Free-Text' lines 66-77)
    - docs/F-SELECTOR-CONSUMER-GUIDE.md (await packet before rankForSelector; roomState.db precondition lines 47-56; locked template)
    - lib/memory/offer-closer.test.cjs (the RED SC7 target from 135-01)
    - 135-RESEARCH.md Pattern 3 (F.1 closer with Free-Text escape) + Pitfalls 5 and 6
  </read_first>
  <action>
    Extend the existing presentOffer block in scripts/intent-classifier.cjs. After the point where `presented.offerLine` is confirmed non-empty (the offer survived grounding + suppression + cap), add the closer:
      1. Build the F.1 payload: verbs = [ out.decision.offer_next_step.command, ...optional sibling commands up to MAX_K=3, 'Free-Text' ]. Set recommendedVerb = (out.decision.decision_trace &amp;&amp; out.decision.decision_trace.brain_md_recommended_marker_rendered === true) ? command : null (Mode A marker rides the existing flag -- do NOT compute confidence here). header = '[NEXT MOVE]'.
      2. Lazy-require lib/hmi/selector-dispatcher.cjs and call pickShape({ requestedShape:'F.1', roomDir, operator: context.operator, tier, payload }). Wrap in try/catch; on failure, fall back to emitting only the offerLine (graceful, no crash). Do NOT write a bespoke prompt string (the tests/test-no-bespoke-brain-prompts.sh tripwire fails 'Ready to'/'Want to'/'Should I'/'Pick one'/'[GATE]').
      3. On a user pick: collapse alias -> canonical via aliasToCanonical(selection.verb, rendered.contract.alias_map). Open room.db through the navigation chokepoint (reuse the existing room.db open pattern in the file; do NOT require room-db.cjs directly) and populate roomState = { db, roomDir, investment_level?, invocationsSinceDecision? }. 
      4. Route the pick: if canonical === 'Free-Text', call selector-decisions.recordSelectorMiss({ top_k_offered: verbs, user_intent: <local user text>, roomState }) -- memory_event only, no edge; the user_intent stays LOCAL (never serialize to a Brain packet). Otherwise map the pick to decision in {'accept','defer','reject'} and call recordSelectorDecision({ decision, command: offer.command, framework: offer.framework, reason: <captured reason; for reject capture REJECTED_BECAUSE>, score_at_decision: offer.confidence, roomState }). On accept, also call presenter.recordOfferOutcome(roomDir, { outcome:'acted', ... }) consistent with the existing 'shown' record.
      5. Always close the db handle (try/finally), and ensure recordSelectorDecision returning { ok:false, reason:'invalid_db' } is handled (log to stderr, do not crash) -- this is the roomState.db precondition guard.
    Keep the whole closer inside the existing lazy-require try/catch envelope so a missing module degrades gracefully (Tri-Polar: identical AskUserQuestion render on CLI/Desktop/Cowork; no surface-specific branch). No em-dashes. Then drive lib/memory/offer-closer.test.cjs to GREEN.
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin &amp;&amp; node -c scripts/intent-classifier.cjs &amp;&amp; node lib/memory/offer-closer.test.cjs &amp;&amp; grep -q "recordSelectorDecision" scripts/intent-classifier.cjs &amp;&amp; grep -q "recordSelectorMiss" scripts/intent-classifier.cjs &amp;&amp; grep -q "Free-Text" scripts/intent-classifier.cjs &amp;&amp; grep -q "pickShape" scripts/intent-classifier.cjs &amp;&amp; bash tests/test-no-bespoke-brain-prompts.sh &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: the closer builds an F.1 payload whose verbs array ends with 'Free-Text' and calls pickShape({requestedShape:'F.1', ...}).
    - SOURCE: a non-Free-Text pick calls recordSelectorDecision with roomState.db populated; a Free-Text pick calls recordSelectorMiss.
    - SOURCE: recommendedVerb is derived ONLY from decision_trace.brain_md_recommended_marker_rendered (no confidence math in the closer).
    - SOURCE: no direct room-db.cjs require added; db opens via the existing navigation/chokepoint pattern; handle closed in finally.
    - BEHAVIOR: recordSelectorDecision { ok:false, reason:'invalid_db' } is handled without crashing.
    - TEST: `node lib/memory/offer-closer.test.cjs` exits 0 (SC7 GREEN).
    - GATE: `bash tests/test-no-bespoke-brain-prompts.sh` passes (no bespoke prompt prose).
    - CLI: `node -c scripts/intent-classifier.cjs` parses; no surface-specific branch added (SC8).
    - Zero em-dashes.
  </acceptance_criteria>
  <done>The resolver's offer fires through the F.1 selector with a Free-Text escape; the pick becomes a typed decision edge (or a miss memory_event); render is cross-surface; SC7 GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Inject wikilinks idempotently when an accepted offer files an artifact</name>
  <read_first>
    - scripts/intent-classifier.cjs (the accept branch added in Task 1; where an accepted offer may file an artifact)
    - lib/vault/wikilink-builder.cjs (injectFiledToFooter signature lines 196-218; idempotent dedupe via content.includes)
    - 135-CONTEXT.md "Wikilink awareness (reuse Phase 76)" decision
    - 135-RESEARCH.md SC5 row + Don't-Hand-Roll wikilink row (use injectFiledToFooter; do not string-concat)
  </read_first>
  <action>
    In the accept branch of the closer (scripts/intent-classifier.cjs), when an accepted offer results in an artifact being filed (or its footer touched), route the footer write through lib/vault/wikilink-builder.cjs injectFiledToFooter(content, { targetPath, meetingSlug }) rather than concatenating a wikilink string. Pass the offer.scope / sectionPath as the targetPath provenance and reuse the offer.reason's [[wikilink]] as the cited section. Because injectFiledToFooter dedupes via content.includes(line), calling it twice on the same content is a no-op -- rely on that for idempotency; do NOT add a separate dedupe. The decision edge recorded in Task 1 already carries the wikilink-bearing reason as provenance (SC5: "decision edge carries wikilink provenance") -- confirm the reason string passed to recordSelectorDecision is the resolver's [[wikilink]] reason, not a stripped variant. If no artifact is filed on accept (the offer was a methodology run with no immediate artifact), this is a no-op path -- guard it so the closer never fails when there is nothing to inject. No em-dashes.
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin &amp;&amp; node -c scripts/intent-classifier.cjs &amp;&amp; grep -q "injectFiledToFooter" scripts/intent-classifier.cjs &amp;&amp; node -e "const w=require('./lib/vault/wikilink-builder.cjs'); const c='# A\n'; const once=w.injectFiledToFooter(c,{targetPath:'sec/x',meetingSlug:'m'}); const twice=w.injectFiledToFooter(once,{targetPath:'sec/x',meetingSlug:'m'}); if(once!==twice){console.error('FAIL: not idempotent');process.exit(1);} console.log('PASS: idempotent injector reused');"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: the accept branch calls injectFiledToFooter (no manual wikilink string-concat).
    - BEHAVIOR: calling injectFiledToFooter twice on identical content yields identical output (idempotent -- proven by the inline node check).
    - SOURCE: the reason passed to recordSelectorDecision retains the resolver's [[wikilink]] (decision-edge wikilink provenance, SC5).
    - BEHAVIOR: an accept with no artifact to file is a guarded no-op (closer does not throw).
    - CLI: `node -c scripts/intent-classifier.cjs` parses.
    - Zero em-dashes.
  </acceptance_criteria>
  <done>SC5 closed: accepted-offer artifact filing injects wikilinks idempotently via the Phase 76 engine; the decision edge carries wikilink provenance.</done>
</task>

<task type="auto">
  <name>Task 3: Register the Phase 135 suites in the Feynman runner and finalize the Part 8 phase gate</name>
  <read_first>
    - lib/memory/run-feynman-tests.cjs (the additive-block registration idiom; see how Phase 129/130 blocks were appended -- prior blocks byte-unchanged)
    - tests/run-all-135.sh (built in 135-01; confirm all three suites listed)
    - scripts/check-schema-aliases.cjs (the Part 8 brain-boundary-scan PR gate)
    - 135-RESEARCH.md "Sampling Rate" + "PR gates Phase 135 must clear" + "Wave 0 Gaps" (registration item)
  </read_first>
  <action>
    Add an ADDITIVE Phase 135 block to lib/memory/run-feynman-tests.cjs registering the three Phase 135 suites (lib/memory/navigation-engine-offer.test.cjs, lib/memory/offer-closer.test.cjs, tests/test-135-resolver-no-leak.cjs), mirroring the Phase 129/130 additive-block pattern. Every prior block (including 129, 129.5, 130) MUST be byte-unchanged -- append only. If any prior em-dash exists in an out-of-scope comment line, LEAVE IT (out of scope; do not touch prior blocks) and note it in deferred-items.md if encountered. Confirm tests/run-all-135.sh lists all three suites (it should from 135-01; if 135-02/03 added a fourth suite, add it). Run the full phase gate: `bash tests/run-all-135.sh` must be GREEN; `node scripts/check-schema-aliases.cjs` (brain-boundary-scan) must pass (SC9); run the zero-regression check on the dependency suites (navigation-engine-core.test.cjs, offer-presenter.test.cjs, test-navigation-acceptance.cjs). Add a one-line header comment in the Phase 135 block citing the SC coverage. No em-dashes in the added block.
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin &amp;&amp; node -c lib/memory/run-feynman-tests.cjs &amp;&amp; grep -q "135" lib/memory/run-feynman-tests.cjs &amp;&amp; bash tests/run-all-135.sh &amp;&amp; node scripts/check-schema-aliases.cjs &amp;&amp; node lib/memory/navigation-engine-core.test.cjs &amp;&amp; node lib/memory/offer-presenter.test.cjs &amp;&amp; node tests/test-navigation-acceptance.cjs &amp;&amp; echo "PHASE 135 GATE GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: run-feynman-tests.cjs has an additive Phase 135 block registering all three (or four) phase suites; prior blocks byte-unchanged (append-only diff).
    - BEHAVIOR: `bash tests/run-all-135.sh` exits 0 (all phase suites GREEN).
    - GATE: `node scripts/check-schema-aliases.cjs` passes (Canon Part 8 brain-boundary-scan, SC9).
    - REGRESSION: navigation-engine-core.test.cjs, offer-presenter.test.cjs, test-navigation-acceptance.cjs all still GREEN (zero regression on 91/109/125).
    - Zero em-dashes in the added block.
  </acceptance_criteria>
  <done>The phase suites run in CI; the Part 8 boundary scan passes; zero regression on the dependency suites; the phase release gate is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client pick -> recordSelectorDecision | untrusted user selection; validated by the decision-edge gate (invalid_decision/command/framework/db) |
| closer -> room.db | opened by the consumer via the chokepoint pattern; never a direct room-db.cjs require |
| user_intent (Free-Text) -> graph | recordSelectorMiss writes user_intent LOCAL-only; never serialized into a Brain packet |
| closer -> Brain | NONE; AskUserQuestion + decision edges are all LOCAL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-06 | Information Disclosure | Free-text user intent leaking to Brain | mitigate | recordSelectorMiss writes user_intent LOCAL-only (selector-decisions.cjs:377); the closer never serializes it into a Brain packet. brain-boundary-scan (Task 3) gates it. |
| T-135-07 | Tampering (UX drift) | bespoke F.1 prompt bypassing the locked template | mitigate | render goes through pickShape only; tests/test-no-bespoke-brain-prompts.sh asserts no 'Ready to'/'Want to'/'Should I'/'Pick one'/'[GATE]' prose (Task 1 verify). |
| T-135-08 | Spoofing/validation | malformed pick -> recordSelectorDecision | mitigate | the decision-edge gate rejects invalid_decision/command/framework/db and never throws; the closer handles { ok:false } without crashing. |
| T-135-09 | Elevation/boundary erosion | direct room.db access in the closer | mitigate | db opened via the chokepoint pattern; no room-db.cjs require (substrate guard). |
| T-135-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new dependencies (Phase 87 invariant). No install task; nothing to verify. |
</threat_model>

<verification>
- `node lib/memory/offer-closer.test.cjs` GREEN (SC7).
- `bash tests/test-no-bespoke-brain-prompts.sh` passes (locked-template enforcement).
- `node scripts/check-schema-aliases.cjs` passes (Canon Part 8, SC9).
- injectFiledToFooter twice == once (SC5 idempotency).
- `bash tests/run-all-135.sh` GREEN; zero regression on 91/109/125 suites.
- No em-dashes across the three touched files (additive-only on run-feynman-tests.cjs).
</verification>

<success_criteria>
- SC5: accepted offer injects wikilinks idempotently; decision edge carries wikilink provenance.
- SC7: closer fires F.1 with Free-Text escape; pick records a typed decision edge (or a miss).
- SC8: cross-surface render via AskUserQuestion; zero TUI, zero third-party OSS, no surface-specific branch.
- SC9: Canon Part 8 clean; brain-boundary-scan passes; no user bytes to Brain.
</success_criteria>

<output>
Create `.planning/phases/135-offer-resolver-and-reliable-next-move-closer/135-03-SUMMARY.md` when done
</output>
