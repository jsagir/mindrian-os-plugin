---
phase: 135-offer-resolver-and-reliable-next-move-closer
plan: 02
type: tdd
wave: 2
depends_on: ["135-01"]
files_modified:
  - lib/core/navigation-engine.cjs
  - lib/core/navigation-engine-offer.cjs
autonomous: true
requirements: [SC1, SC2, SC3, SC4, SC5, SC6, SC9]
canon_parts: [Part 2, Part 3, Part 4, Part 8, Part 9]

must_haves:
  truths:
    - "resolveOfferNextStep returns a well-formed offer {command, framework, jtbd, confidence, reason, scope} or null -- never throws"
    - "Returns null in JUST_TALK; offers at DECISION_GATE / METHODOLOGY close; backs off a rejected command for N=5 turns"
    - "Margin signal = rankForSelector items[0].score - items[1].score; rank is computed FIRST, then the operator strong-signal gate uses the computed margin (no forward reference); below MARGIN_THRESHOLD=0.15 returns null"
    - "SC2: consumes the local graph neighborhood + memory_event tail (getNeighborhood / findRecentChanges via the navigation chokepoint, sync) as a relevance / confidence input -- not just a no-leak guarantee"
    - "SC4: consumes FEYNMAN.md temporal (memory_event tail via the chokepoint) + MINTO.md governing thought (quadruple.reasoning.governing_thought) + the active-JTBD intent leg (context.jtbd) into relevance / confidence -- MD-awareness is delivered, NOT deferred"
    - "Graceful across mode_a / mode_b / tier_0 (BRAIN.md absent) -- no crash; tier_0 yields the hardcoded minimal verb set or null"
    - "The reason string carries [[wikilinks]] so the presenter's section-name grounding gate passes"
    - "All room.db reads route through navigation.cjs; the resolver issues zero Brain payloads (local-only sync, A3)"
  artifacts:
    - path: "lib/core/navigation-engine-offer.cjs"
      provides: "Abstention gate + margin compute + neighborhood/memory_event + FEYNMAN/MINTO/JTBD consumption + grounded-reason builder"
      min_lines: 60
      exports: ["resolveOffer", "MARGIN_THRESHOLD"]
    - path: "lib/core/navigation-engine.cjs"
      provides: "resolveOfferNextStep no longer returns null; delegates to the offer helper"
      contains: "navigation-engine-offer"
  key_links:
    - from: "lib/core/navigation-engine.cjs"
      to: "lib/core/navigation-engine-offer.cjs"
      via: "resolveOfferNextStep delegates to the helper's resolveOffer"
      pattern: "navigation-engine-offer"
    - from: "lib/core/navigation-engine-offer.cjs"
      to: "lib/workflow/f-selector-ranker.cjs"
      via: "rankForSelector for the confidence margin (items[0].score - items[1].score)"
      pattern: "rankForSelector"
    - from: "lib/core/navigation-engine-offer.cjs"
      to: "lib/workflow/selector-decisions.cjs"
      via: "shouldExclude for rejection backoff (DECAY_WINDOW=5, EXCLUSION_THRESHOLD=0.1)"
      pattern: "shouldExclude"
    - from: "lib/core/navigation-engine-offer.cjs"
      to: "lib/core/navigation.cjs"
      via: "getNeighborhood + findRecentChanges (SC2 neighborhood + memory_event tail) and firstCapturedLastTouchedBySection (SC4 FEYNMAN temporal), all sync via the chokepoint"
      pattern: "getNeighborhood"
---

<objective>
Fill the resolver stub. Replace `return null` at lib/core/navigation-engine.cjs:280-282 with an abstention-gated resolver that returns exactly one calibrated offer or null, computed from real local memory.

This is one of the three net-new pieces the whole phase exists for: (a) the abstention triple (operator-state x confidence-margin x rejection-backoff), (b) the SQL-local + MD-aware relevance/confidence consumption (SC2 neighborhood + memory_event tail; SC4 FEYNMAN temporal + MINTO governing thought + active JTBD), and (c) the resolver body. All ship here. The resolver FEEDS the already-shipped offer-presenter.cjs (it does NOT render, suppress, or cap -- those exist); the closer wiring is 135-03.

Per A3 LOCKED: the resolver is LOCAL-ONLY and SYNCHRONOUS. No awaited buildBrainPacket inside decide(). Every memory read (neighborhood, memory_event tail, FEYNMAN temporal, MINTO governing thought, active JTBD) uses a SYNC navigation chokepoint read or the already-read quadruple -- never an awaited Brain call. Mode A's RECOMMENDED marker rides the existing decision_trace.brain_md_recommended_marker_rendered flag, not a fresh packet built here.

Purpose: turn the dark offer loop on -- a calibrated next-move offer becomes available at the right moment, grounded in the room's actual graph + temporal + reasoning context, and the loop stays silent otherwise.
Output: lib/core/navigation-engine-offer.cjs (the helper), and the one-line delegation edit in navigation-engine.cjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/135-offer-resolver-and-reliable-next-move-closer/135-CONTEXT.md
@.planning/phases/135-offer-resolver-and-reliable-next-move-closer/135-RESEARCH.md
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Source-of-truth signatures. Extracted from codebase; do NOT re-explore. -->

From lib/core/navigation-engine.cjs:
  function resolveOfferNextStep(_context, _brain)  // lines 280-282, currently `return null`
  // called at line 475: decision.offer_next_step = resolveOfferNextStep(ctx, brain);
  // The composer fallback at ~line 595 fires ONLY `if (decision.offer_next_step === null)` -- KEEP it; do not remove/duplicate.
  // decide() runs SYNCHRONOUSLY (callDecideWithTimeout / Promise.race). The resolver MUST stay sync.

From lib/workflow/f-selector-ranker.cjs (Phase 125, PURE SYNC):
  rankForSelector({ jtbd, problemType, focusNodeId, roomState, packetOptional, k=3 }) -> Array<RankedItem>   // MAX_K = 3
  RankedItem = { command:'/mos:slug', jtbd_label, jtbd_summary, teaching, framework, score:0..1, why, source, investment_level }
  // margin = items.length>=2 ? items[0].score - items[1].score : (items[0]?.score ?? 0)

From lib/workflow/selector-decisions.cjs (Phase 125):
  shouldExclude(command_id, roomState) -> boolean   // factor = 1 - exp(-(n / DECAY_WINDOW)); excludes when factor < EXCLUSION_THRESHOLD
  DECAY_WINDOW = 5;  EXCLUSION_THRESHOLD = 0.1
  // reads invocationsSinceDecision[command] from roomState, or via navigation.findRecentChanges when roomState.db set.

From lib/core/navigation.cjs (the SQL chokepoint -- ALL sync; the ONLY door to room.db):
  getNeighborhood(db, focusNodeId, opts) -> neighborhood        // SC2: local graph neighborhood
  findRecentChanges(db, sinceTs, { eventType, limit }) -> Array<event>   // SC2/SC4: memory_event tail (FEYNMAN temporal source)
  firstCapturedLastTouchedBySection(db, sectionSlug) -> { first_captured, last_touched, ... }   // SC4: FEYNMAN temporal per section
  getActiveFocus(db) -> focus                                   // resolves a focus node for getNeighborhood
  // db = context.roomState.db (opened + owned by the caller per 135-01 Task 1; NEVER opened here; engine never opens room.db).

From lib/core/navigation-engine-shared.cjs:
  CANONICAL_VERBS  // frozen 10-entry array; [9] === 'Free-Text'; tier_0 minimal set = Run Methodology / Reformulate / Free-Text
  resolveTierMode(quadruple, brainAvailable) -> 'mode_a' | 'mode_b' | 'tier_0'

From the engine context (now carrying the FULL resolver input set after 135-01 Task 1):
  context = { quadruple, brainAvailable, userPersona, intentSignal, operator, roomState, sectionPath, jtbd, problemType }
  // context.operator is the enum string ('JUST_TALK' default). context.jtbd is the active-JTBD intent leg (SC4).
  // context.roomState = { db, roomDir, invocationsSinceDecision }; db is null when room.db absent (abstain gracefully).
  // context.quadruple.reasoning.governing_thought is the MINTO.md governing thought (SC4); already read, no extra IO.
  // decision_trace.brain_md_recommended_marker_rendered is the Mode A marker (already computed elsewhere).

From lib/core/offer-presenter.cjs (the DOWNSTREAM consumer -- the reason MUST satisfy its gate):
  isReasonGrounded(reason) -> 'ok' | 'ungrounded_reason' | 'generic_reason'
  // requires reason.length >= 15 AND (a SIGNAL_KEYWORDS hit OR a section-name regex). A [[section/...]] wikilink satisfies the section-name regex for free.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the abstention gate (rank-first, gate-second) + neighborhood/memory_event + FEYNMAN/MINTO/JTBD consumption + grounded-reason helper</name>
  <read_first>
    - lib/core/navigation-engine.cjs (lines 270-312: the stub + decide() entry; the file being delegated from)
    - lib/workflow/f-selector-ranker.cjs (rankForSelector signature + RankedItem shape, lines 29-77, 359-451)
    - lib/workflow/selector-decisions.cjs (shouldExclude lines 314-323; DECAY_WINDOW=5 line 54; EXCLUSION_THRESHOLD=0.1 line 62)
    - lib/core/navigation.cjs (getNeighborhood / findRecentChanges / firstCapturedLastTouchedBySection / getActiveFocus re-exports lines 44-121 -- all sync, db-handle-taking)
    - lib/core/navigation/neighborhood.cjs (getNeighborhood(db, focusNodeId, opts) line 48) and lib/core/navigation/insights.cjs (firstCapturedLastTouchedBySection(db, sectionSlug) line 356)
    - lib/core/navigation-engine-shared.cjs (CANONICAL_VERBS lines 66-77; resolveTierMode)
    - lib/core/folder-memory.cjs (readQuadruple reasoning leg + governing_thought lines 124-166 -- the MINTO source)
    - lib/core/offer-presenter.cjs (isReasonGrounded lines 112-130; SIGNAL_KEYWORDS line 75 -- the reason must pass this)
    - lib/memory/navigation-engine-offer.test.cjs (the RED suite from 135-01 -- this is the GREEN target)
    - tests/test-135-decide-wiring-e2e.cjs (the end-to-end production guard from 135-01 -- this must go GREEN once this task + the wiring land)
    - 135-RESEARCH.md "Target return shape" + "Defensible defaults" + Pitfall 4 (sync, no awaited packet)
  </read_first>
  <behavior>
    - Test (SC6): resolveOffer(context) returns null when context.operator === 'JUST_TALK'.
    - Test (SC6/ordering): rank is computed BEFORE the operator strong-signal gate. For operator 'EXPLORE_CAPTURE' / 'BUILD_ROOM' the offer fires ONLY when the computed margin >= STRONG_SIGNAL_THRESHOLD (defined as 2 x MARGIN_THRESHOLD = 0.30); below that, null. There is no reference to margin before margin is computed.
    - Test (SC6): returns an offer at 'DECISION_GATE' (always, when margin >= MARGIN_THRESHOLD and not in backoff) and at 'METHODOLOGY' (close).
    - Test (SC6/SC1): with operator 'DECISION_GATE', two ranked items whose margin >= 0.15, and top command NOT excluded, returns {command, framework, jtbd, confidence, reason, scope} with exactly those six keys.
    - Test (SC6): margin < MARGIN_THRESHOLD (0.15) returns null (low-confidence abstain).
    - Test (SC6): shouldExclude(top.command, roomState) true -> returns null (rejection backoff).
    - Test (SC1): rankForSelector returns [] -> returns null.
    - Test (SC2): when context.roomState.db is set, the resolver consumes the local graph neighborhood + memory_event tail (getNeighborhood + findRecentChanges) as a relevance/confidence input; assert (via a spy or by observing a behavioral difference) that the read happens and feeds the decision; when db is null it abstains/degrades without crashing.
    - Test (SC4): the resolver consumes FEYNMAN temporal (firstCapturedLastTouchedBySection / memory_event tail), the MINTO governing thought (context.quadruple.reasoning.governing_thought), and the active JTBD (context.jtbd) into relevance/confidence; assert these inputs measurably affect the outcome (e.g. a stale/absent governing thought or a missing JTBD lowers confidence or shifts abstention), never just present-but-ignored.
    - Test (SC5): the reason string contains a [[wikilink]] (e.g. `[[${sectionPath}]]`) and isReasonGrounded(reason) === 'ok'.
    - Test (SC3): three fixtures mode_a / mode_b / tier_0 -- never throws; tier_0 (quadruple.brain absent) returns null or a hardcoded-minimal-verb offer, never crashes.
  </behavior>
  <action>
    Create lib/core/navigation-engine-offer.cjs (CJS, BSL-1.1 header, JSDoc, no fenced examples, no em-dashes). Export `resolveOffer(context)` and the constants `MARGIN_THRESHOLD = 0.15` and `STRONG_SIGNAL_THRESHOLD = 0.30` (= 2 x MARGIN_THRESHOLD; Claude's Discretion default per CONTEXT.md D-discretion; cite SEED-009 as the future learner in a comment; mark both as v2-tunable via .mos/config.json in the comment, do NOT build the config read). The function is SYNCHRONOUS and never throws -- wrap the body so any internal failure returns null (graceful per emptyDecision idiom).

    Implement the abstention triple in a STRICT order so there is NO forward reference (checker Warning 1 -- the operator strong-signal gate must use the margin that is computed BEFORE it, not after):
      1. HARD SILENCE: if context.operator === 'JUST_TALK' return null immediately (no ranking needed; cheapest abstention).
      2. RANK FIRST: lazy-require lib/workflow/f-selector-ranker.cjs and call rankForSelector({ jtbd: context.jtbd, problemType: context.problemType, focusNodeId: <resolved below>, roomState: context.roomState, packetOptional: context.packet || null, k: 3 }). If items.length === 0 return null. Compute margin = items.length >= 2 ? items[0].score - items[1].score : items[0].score. This is the SINGLE place margin is computed; everything after reads it.
      3. OPERATOR GATE (now uses the already-computed margin -- no circularity): if operator is 'DECISION_GATE' proceed (always eligible). If operator is 'METHODOLOGY' proceed (close-of-methodology offer). If operator is 'EXPLORE_CAPTURE' or 'BUILD_ROOM', proceed ONLY when margin >= STRONG_SIGNAL_THRESHOLD (0.30); otherwise return null (default-silent unless a strong signal, per CONTEXT D-6). For any other operator value, return null. Strong-signal is thus concretely defined as margin >= 2 x MARGIN_THRESHOLD.
      4. MARGIN FLOOR: if margin < MARGIN_THRESHOLD (0.15) return null (low-confidence abstain). (For EXPLORE_CAPTURE/BUILD_ROOM the strong-signal gate already enforced a higher bar; this floor catches the DECISION_GATE/METHODOLOGY path.)
      5. REJECTION BACKOFF: lazy-require lib/workflow/selector-decisions.cjs and if shouldExclude(items[0].command, context.roomState) return null. (Reuses DECAY_WINDOW=5 / EXCLUSION_THRESHOLD=0.1 so resolver and ranker decay agree -- N=5 per CONTEXT default; reads the f_selector_decision memory_event tail via context.roomState.db.)
      6. TIER FALLBACK: if resolveTierMode(context.quadruple, context.brainAvailable) === 'tier_0' (BRAIN.md absent), either return null or, when items exist, emit an offer whose command is constrained to the hardcoded minimal verb set (Run Methodology / Reformulate / Free-Text from CANONICAL_VERBS) -- never crash. Mode A vs Mode B does NOT change the resolver math here; the RECOMMENDED marker is the caller's decision_trace flag, not set in this function.

    SC2 + SC4 relevance/confidence consumption (the MD-aware + graph-aware inputs; ALL sync via the chokepoint, A3-compatible -- never an awaited Brain call):
      - When context.roomState && context.roomState.db is non-null, lazy-require lib/core/navigation.cjs and:
        - (SC2) resolve a focus node (getActiveFocus(db) or the section's node) and read the LOCAL GRAPH NEIGHBORHOOD via getNeighborhood(db, focusNodeId, opts); read the MEMORY_EVENT TAIL via findRecentChanges(db, 0, { limit }). Use these to compute a relevance signal -- e.g. neighborhood density / recent-activity recency -- that adjusts confidence or contributes to the abstention decision (a sparse neighborhood + stale tail should bias toward abstention; an active neighborhood toward offering). Wrap in try/catch; on any read failure degrade to the rank-only path (never crash).
        - (SC4) read the FEYNMAN temporal signal via firstCapturedLastTouchedBySection(db, <sectionSlug>) and/or the FEYNMAN-relevant memory_event tail; combine with the MINTO governing thought from context.quadruple.reasoning.governing_thought and the active JTBD from context.jtbd. These three MD-aware legs feed relevance/confidence: a present, fresh governing thought + a matching active JTBD + recent temporal activity RAISE confidence; an absent/stale governing thought or a missing JTBD LOWER it (and can tip a borderline margin under the floor into abstention). This is the SC4 quadruple-plus-FEYNMAN-plus-MINTO-plus-JTBD consumption -- delivered, not deferred.
      - When db is null (tier-0 / no room.db), skip these reads and fall to the rank-only / hardcoded-minimal path; abstain gracefully.

    EMIT: build the grounded reason FIRST via a small buildReason(top, context) helper that prepends a [[wikilink]] -- prefer `[[${context.sectionPath}]]` (or a section-scoped `[[section/...]]` form) so offer-presenter.isReasonGrounded returns 'ok' (section-name regex hit). When the active JTBD and/or governing thought are present, weave them into the reason text (e.g. cite the governing thought theme or the JTBD label) so the reason reflects the MD-aware signal, then append top.jtbd_summary; ensure total length >= 15 and the reason NEVER contains the literal `[[undefined]]` (guard: if context.sectionPath is falsy, use a defined fallback section token, never undefined). Return { command: top.command, framework: top.framework, jtbd: top.jtbd_label, confidence: <margin-and-MD-adjusted score>, reason, scope: context.sectionPath }. Exactly one offer, six keys, no extras.

    Do NOT call AskUserQuestion, do NOT read a surface flag, do NOT build/await a Brain packet, do NOT require room-db.cjs directly (route every db read through navigation.cjs only). Keep the helper a pure-local synchronous reader.
    Then run the 135-01 RED suite against the helper and iterate until GREEN; also confirm tests/test-135-decide-wiring-e2e.cjs goes GREEN (the end-to-end production guard).
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin &amp;&amp; node lib/memory/navigation-engine-offer.test.cjs &amp;&amp; node -e "const o=require('./lib/core/navigation-engine-offer.cjs'); if(typeof o.resolveOffer!=='function'){console.error('FAIL: resolveOffer not exported');process.exit(1);} if(o.MARGIN_THRESHOLD!==0.15){console.error('FAIL: MARGIN_THRESHOLD != 0.15');process.exit(1);} if(o.STRONG_SIGNAL_THRESHOLD!==0.30){console.error('FAIL: STRONG_SIGNAL_THRESHOLD != 0.30');process.exit(1);} const r=o.resolveOffer({operator:'JUST_TALK'}); if(r!==null){console.error('FAIL: JUST_TALK not silent');process.exit(1);} const src=require('fs').readFileSync('./lib/core/navigation-engine-offer.cjs','utf8'); if(!/getNeighborhood/.test(src)){console.error('FAIL: SC2 neighborhood not consumed');process.exit(1);} if(!/governing_thought/.test(src)){console.error('FAIL: SC4 MINTO governing thought not consumed');process.exit(1);} if(!/firstCapturedLastTouchedBySection|findRecentChanges/.test(src)){console.error('FAIL: SC4 FEYNMAN temporal / memory_event tail not consumed');process.exit(1);} if(/\[\[undefined\]\]/.test(src)){console.error('FAIL: reason can be [[undefined]]');process.exit(1);} console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: lib/core/navigation-engine-offer.cjs exports resolveOffer(context), MARGIN_THRESHOLD === 0.15, and STRONG_SIGNAL_THRESHOLD === 0.30.
    - SOURCE/ORDERING: margin is computed by rankForSelector BEFORE the operator strong-signal gate reads it; there is no use of `margin` above the line that defines it (no circular forward reference -- checker Warning 1).
    - SOURCE (SC2): the helper calls getNeighborhood AND findRecentChanges via lib/core/navigation.cjs and uses them as a relevance/confidence input (not just present).
    - SOURCE (SC4): the helper consumes firstCapturedLastTouchedBySection (or the FEYNMAN memory_event tail), context.quadruple.reasoning.governing_thought, and context.jtbd as relevance/confidence inputs.
    - BEHAVIOR: resolveOffer({operator:'JUST_TALK'}) === null.
    - BEHAVIOR: a DECISION_GATE context with margin >= 0.15 and no backoff returns an object with EXACTLY keys [command, framework, jtbd, confidence, reason, scope].
    - BEHAVIOR: an EXPLORE_CAPTURE context returns an offer ONLY when margin >= 0.30, null otherwise.
    - BEHAVIOR: margin < 0.15 -> null; shouldExclude(top) true -> null; rankForSelector []-> null.
    - SOURCE: the emitted reason contains `[[` and `]]`, never the literal `[[undefined]]`, and isReasonGrounded(reason) === 'ok' (assert by requiring offer-presenter and calling it in the test).
    - BEHAVIOR: no throw across mode_a / mode_b / tier_0 fixtures, and no throw when context.roomState.db is null (degrades to rank-only).
    - SOURCE: no `require(...room-db...)`, no `AskUserQuestion`, no `await buildBrainPacket`, no `async` keyword on resolveOffer.
    - TEST: `node lib/memory/navigation-engine-offer.test.cjs` exits 0; `node tests/test-135-decide-wiring-e2e.cjs` exits 0.
    - Zero em-dashes.
  </acceptance_criteria>
  <done>The abstention triple (rank-first, gate-second, no circular margin reference) + the SC2 neighborhood/memory_event consumption + the SC4 FEYNMAN/MINTO/active-JTBD consumption are a tested, synchronous, local-only helper; the 135-01 resolver suite + the end-to-end wiring suite are GREEN; the reason passes the presenter's grounding gate and is never [[undefined]].</done>
</task>

<task type="auto">
  <name>Task 2: Delegate resolveOfferNextStep to the helper; preserve composer fallback and sync contract</name>
  <read_first>
    - lib/core/navigation-engine.cjs (lines 280-282 stub; line 475 call site; lines 590-600 composer fallback)
    - lib/core/navigation-engine-offer.cjs (the helper built in Task 1)
    - 135-RESEARCH.md "Pattern 1: Resolver runs BEFORE the composer" (precedence is already wired by filling the stub)
  </read_first>
  <action>
    In lib/core/navigation-engine.cjs, replace the body of resolveOfferNextStep(_context, _brain) (lines 280-282) so it lazy-requires lib/core/navigation-engine-offer.cjs and returns its resolveOffer(context) result, wrapped in a try/catch that returns null on any failure (so the engine never throws and the composer fallback still fires). Rename the params to (context, _brain) since context is now used; keep _brain unused (A3: no Brain packet inside the resolver). Update the JSDoc above it to state the v1 behavior is now LIVE (drop the "ships null until Plan 91-04" sentence) and cite Phase 135. Do NOT touch the call site at line 475 (the precedence is automatic). Do NOT touch or duplicate the framework-chain-composer block at ~595 -- it stays as the graceful null-fallback. Confirm decide() remains synchronous (no new await). No em-dashes.
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin &amp;&amp; node -c lib/core/navigation-engine.cjs &amp;&amp; node -e "const s=require('fs').readFileSync('lib/core/navigation-engine.cjs','utf8'); const i=s.indexOf('function resolveOfferNextStep'); const body=s.slice(i,i+400); if(!/navigation-engine-offer/.test(body)){console.error('FAIL: not delegating');process.exit(1);} if(/return null;\s*}/.test(s.slice(i,i+200)) &amp;&amp; !/navigation-engine-offer/.test(s.slice(i,i+200))){console.error('FAIL: still stubbed');process.exit(1);}" &amp;&amp; grep -q "decision.offer_next_step === null" lib/core/navigation-engine.cjs &amp;&amp; echo "PASS: delegates + composer fallback intact"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: resolveOfferNextStep body lazy-requires navigation-engine-offer.cjs and returns resolveOffer(context); try/catch returns null on failure.
    - SOURCE: the composer fallback guard `if (decision.offer_next_step === null)` still exists (composer preserved).
    - SOURCE: no `async` added to decide(); the line 475 call site is unchanged.
    - CLI: `node -c lib/core/navigation-engine.cjs` parses clean.
    - BEHAVIOR: a JUST_TALK decide() context yields decision.offer_next_step === null (resolver abstains, composer may still set a fallback only if its own conditions hold).
    - Zero em-dashes.
  </acceptance_criteria>
  <done>resolveOfferNextStep is no longer a null stub; it delegates to the tested helper, has precedence over the composer (which remains as graceful fallback), and decide() stays synchronous.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolver -> room.db | reads only via navigation.cjs chokepoint (getNeighborhood / findRecentChanges / firstCapturedLastTouchedBySection / getActiveFocus); no direct room-db.cjs require (Phase 109-06 substrate guard); db handle owned by the caller |
| resolver -> Brain | NONE (A3 LOCKED: local-only sync; FEYNMAN/MINTO/neighborhood/memory_event are all LOCAL sync reads; reason + offer never form a Brain query) |
| reason string -> offer-presenter | LOCAL-only; the reason lives in the offer struct + offer-history.json + decision edge, never crosses to Brain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-03 | Information Disclosure | offer reason embedding user prose (governing thought / JTBD / section) | mitigate | reason is LOCAL-only; the resolver never builds a Brain query from it. The MINTO/FEYNMAN/neighborhood reads are local sync chokepoint reads. fs-leak test (135-01 Task 3) + brain-boundary-scan (135-03) instrument the path. |
| T-135-04 | Elevation/boundary erosion | direct room.db access | mitigate | resolver routes every db read through navigation.cjs (getNeighborhood / findRecentChanges / firstCapturedLastTouchedBySection); substrate guard fails a direct room-db.cjs require. Acceptance criteria forbid the require. |
| T-135-05 | Tampering (UX drift) | resolver emitting render concerns | mitigate | resolver returns DATA only; no AskUserQuestion / surface flag (asserted in acceptance criteria). |
| T-135-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new dependencies (Phase 87 invariant). No install task; nothing to verify. |
</threat_model>

<verification>
- `node lib/memory/navigation-engine-offer.test.cjs` GREEN (SC1/SC3/SC6 + reason-grounding SC5 + SC2 neighborhood + SC4 FEYNMAN/MINTO/JTBD consumption).
- `node tests/test-135-decide-wiring-e2e.cjs` GREEN (the end-to-end production guard; offer_next_step non-null with a real [[section]] reason).
- `node -c lib/core/navigation-engine.cjs` parses; composer fallback guard preserved.
- `node lib/core/navigation-engine-core.test.cjs` still GREEN (zero regression on Phase 91 engine).
- grep confirms no `require('...room-db...')`, no `AskUserQuestion`, no `await buildBrainPacket` in navigation-engine-offer.cjs; grep confirms getNeighborhood + governing_thought + (firstCapturedLastTouchedBySection|findRecentChanges) ARE consumed; grep confirms no `[[undefined]]`.
- No em-dashes across both files.
</verification>

<success_criteria>
- SC1: resolveOfferNextStep returns offer-or-null (six-key contract), never >1, never throws.
- SC2: resolver path is local-only sync; zero non-SQLite scans; AND it consumes the local graph neighborhood + memory_event tail as a relevance/confidence input.
- SC3: graceful across mode_a / mode_b / tier_0, and when roomState.db is null.
- SC4: MD-aware -- consumes FEYNMAN temporal + MINTO governing thought + active JTBD into relevance/confidence (delivered, not deferred).
- SC5: reason carries [[wikilinks]] (never [[undefined]]) and passes the presenter grounding gate.
- SC6: abstention triple (operator x margin x backoff) enforced, rank-first/gate-second, with MARGIN_THRESHOLD=0.15 / STRONG_SIGNAL_THRESHOLD=0.30 / N=5.
- SC9: resolver issues zero Brain payloads.
</success_criteria>

<output>
Create `.planning/phases/135-offer-resolver-and-reliable-next-move-closer/135-02-SUMMARY.md` when done
</output>
</content>
