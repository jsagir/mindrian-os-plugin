---
phase: 121-trajectory-telemetry
plan: 04
type: execute
wave: 3
depends_on: ["121-00", "121-01", "121-02", "121-03"]
files_modified:
  - docs/TELEMETRY-SCHEMA.md
  - tests/test-121-04-canon-part-8-audit.sh
  - tests/test-121-04-silent-observability.sh
  - tests/test-121-04-scaffold.sh
  - tests/run-all-121.sh
autonomous: true
requirements: [TELEMETRY-121-11, TELEMETRY-121-12]
requirements_addressed: [TELEMETRY-121-11, TELEMETRY-121-12]
canon_parts: [Part 6, Part 7, Part 8]

must_haves:
  truths:
    - "docs/TELEMETRY-SCHEMA.md documents all 15 event types with field-by-field specification"
    - "docs/TELEMETRY-SCHEMA.md references arXiv 2508.03680 for SEED-002 ingestion guidance"
    - "Canon Part 8 audit: zero network surface across ALL telemetry-touching files (writer, validator, schema, migration, capture points, hook script, helper, CLI)"
    - "Silent-observability invariant: NO user-facing surface references the telemetry corpus count"
    - "All 5 plan scaffolds (00 + 01 + 02 + 03 + 04) pass green in run-all-121.sh"
  artifacts:
    - path: "docs/TELEMETRY-SCHEMA.md"
      provides: "Frozen v1 schema spec; SEED-002 ingestion guidance; arXiv 2508.03680 reference"
      min_lines: 200
    - path: "tests/test-121-04-canon-part-8-audit.sh"
      provides: "Adversarial Part 8 grep sweep across all telemetry-touching files"
    - path: "tests/test-121-04-silent-observability.sh"
      provides: "D-12 invariant: zero user-facing telemetry corpus surface"
  key_links:
    - from: "docs/TELEMETRY-SCHEMA.md"
      to: "lib/core/telemetry/schema.cjs"
      via: "field-by-field documentation reflects ALLOWED_FIELDS exactly"
      pattern: "ALLOWED_FIELDS"
    - from: "tests/test-121-04-canon-part-8-audit.sh"
      to: "all telemetry source files"
      via: "adversarial grep for fetch/http/brain.mindrian patterns"
      pattern: "grep.*fetch|http|brain.mindrian"
    - from: "tests/test-121-04-silent-observability.sh"
      to: "commands/ + lib/hmi/ + skills/"
      via: "adversarial grep for telemetry-corpus user-facing references"
      pattern: "telemetry.*corpus|corpus.*count"
---

<objective>
Ship the closing audit + documentation plan: docs/TELEMETRY-SCHEMA.md frozen v1 spec + Canon Part 8 adversarial compliance audit (zero LOCAL-to-BRAIN egress evidence) + D-12 silent-observability invariant (zero user-facing telemetry corpus surface).

Purpose: Closing audit per D-11 enforcement evidence and D-12 silent observability. By the end of this plan, SEED-002's eventual consumption has a frozen schema spec to ingest against, Canon Part 8 has structural enforcement evidence (grep-verifiable adversarial scan), and Canon Part 10's "telemetry is lab-side" stance has invariant test coverage that prevents premature commitment to a user-facing corpus widget.

Output: 1 docs file + 3 audit/scaffold harnesses; full run-all-121.sh green across all 5 plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/121-trajectory-telemetry/121-CONTEXT.md
@.planning/phases/121-trajectory-telemetry/121-00-SUMMARY.md
@.planning/phases/121-trajectory-telemetry/121-01-SUMMARY.md
@.planning/phases/121-trajectory-telemetry/121-02-SUMMARY.md
@.planning/phases/121-trajectory-telemetry/121-03-SUMMARY.md
@lib/core/telemetry/schema.cjs
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- All 15 event types from Plan 121-00 schema.cjs -->

6 mva.* events (inherited from Phase 118):
  mva_pipeline_started, mva_agent_returned, mva_brief_rendered, mva_option_selected, mva_brief_deployed, mva_pipeline_failed

9 net-new events (Phase 121):
  selector_pick                 (D-04, Plan 121-02 Task 1)
  tension_engagement            (D-05, Plan 121-02 Task 2)
  auto_explore_decision         (D-06, Plan 121-02 Task 3)
  breakthrough_dismissed        (D-07, Plan 121-02 Task 4)
  hooked_axis_score             (D-08; schema documented; emit path unchanged in Phase 121)
  empathy_observation           (D-09, Plan 121-03 Task 1)
  room_receipt_written          (D-09, Plan 121-03 Task 2)
  command_invocation            (D-09, Plan 121-03 Task 3 broad-sweep bucket)
  nav_bypass                    (inherited from lib/core/room-db.cjs; migrated via Plan 121-01)

<!-- All telemetry-touching files (Canon Part 8 audit grep target list) -->

lib/core/telemetry/writer.cjs
lib/core/telemetry/validator.cjs
lib/core/telemetry/schema.cjs
lib/core/telemetry/writer.test.cjs
lib/core/telemetry/validator.test.cjs
lib/core/telemetry/schema.test.cjs
lib/core/mva-telemetry.cjs                          (shim; Plan 121-01)
scripts/migrate-telemetry-v1.cjs                    (Plan 121-01)
scripts/migrate-telemetry-v1.test.cjs               (Plan 121-01)
lib/hmi/selector-dispatcher.cjs                     (Plan 121-02 D-04 wire-in)
lib/memory/pending-tension-store.cjs                (Plan 121-02 D-05 wire-in)
[phase-117-auto-explore-file]                       (Plan 121-02 D-06 wire-in; recorded in 121-02-SUMMARY)
lib/core/breakthrough/scanner.cjs                   (Plan 121-02 D-07 wire-in)
scripts/empathy-observation-emit.cjs                (Plan 121-03 D-09 CLI)
lib/core/room-receipt-emit.cjs                      (Plan 121-03 D-09 helper)
[phase-119-receipt-write-file]                      (Plan 121-03 D-09 wire-in; recorded in 121-03-SUMMARY)
scripts/telemetry-command-invocation.cjs            (Plan 121-03 D-09 PostToolUse hook script)

<!-- Forbidden patterns for Canon Part 8 audit -->

grep -rE "fetch\(|require\(['\"]https?\b|require\(['\"]http\b|brain\.mindrian|tavily\.com|api\.openai\.com|api\.anthropic\.com|api\.elevenlabs\.io" [file-list]
MUST return 0 across all listed files.

<!-- Forbidden patterns for D-12 silent-observability audit -->

grep -rE "(telemetry|trajectory).*corpus|corpus.*(count|size|events)|N\s*/\s*100|SEED-002.*progress|/100 events|trajectory corpus" commands/ lib/hmi/ skills/ commands/status.md
MUST return 0. The telemetry is silent observability per D-12; no user-facing surface.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/TELEMETRY-SCHEMA.md frozen v1 specification</name>
  <read_first>
    - lib/core/telemetry/schema.cjs (THE source of truth for ALLOWED_FIELDS; docs reflect this verbatim)
    - .planning/phases/121-trajectory-telemetry/121-CONTEXT.md (D-01..D-12 inform the spec)
    - docs/MINDRIAN-CANON.md Part 8 (constitutional locality rule referenced in spec preamble)
    - .planning/seeds/SEED-002-agent-lightning-lab-loop.md (downstream consumer requirements)
    - .planning/phases/121-trajectory-telemetry/121-00-SUMMARY.md through 121-03-SUMMARY.md (file paths + LOC for cross-reference)
  </read_first>
  <action>
    Create `docs/TELEMETRY-SCHEMA.md` with the structure below. Use plain ASCII only (no em-dashes, no smart quotes; use hyphens and straight quotes throughout).

    Required sections (write the file with all of these, headings preserved verbatim):

    1. **Header block**
       - Title: `# MindrianOS Trajectory Telemetry -- Frozen v1 Schema`
       - Metadata: Version (1), Status (Active, frozen v1, additive-only changes; structural changes bump to schema_version 2), Phase (121), Milestone (v1.13.0 "The Closed Loop"), Last reviewed (date of execution).

    2. **Purpose section**
       - Single paragraph: captures structured trajectory data for SEED-002 (arXiv 2508.03680). Consolidation per Canon Part 7 (4 piecemeal writers collapsed to 1 unified writer with 1 emit-time validator).

    3. **Canon Part 8 Locality (CONSTITUTIONAL)**
       - Chokepoint chain: lib/core/telemetry/writer.cjs -> lib/core/telemetry/validator.cjs -> fs.appendFileSync(~/.mindrian/telemetry/v1.13/events-YYYY-WNN.jsonl). Zero network surface. Zero LOCAL-to-BRAIN egress. Per Canon Part 9 single-chokepoint pattern (lib/core/navigation.cjs precedent).

    4. **File Layout table**
       - Path | Purpose: events-YYYY-WNN.jsonl (unified stream), .migration-fingerprints.json (idempotence marker), .quarantine-<source>.jsonl (rejected rows), *.pre-v121.bak (renamed legacy sources).

    5. **Row Envelope table**
       - Field | Type | Description: event (String, 15-way discriminator), schema_version (Number, frozen at 1), timestamp (ISO-8601 UTC), session_id (String from CLAUDE_SESSION_ID, max 64).

    6. **EVENT_TYPES (15 total)** -- section per event with: when it fires, ALLOWED_FIELDS list, sample payload, source file. Document all 15:

       6 mva.* events (Phase 118 inheritance):
       - mva_pipeline_started, mva_agent_returned, mva_brief_rendered (note: uses `total_duration_ms` NOT `duration_ms` per Phase 118 Plan 03 WARN-2), mva_option_selected, mva_brief_deployed, mva_pipeline_failed

       9 net-new events:
       - selector_pick (D-04): ALLOWED_FIELDS sub_shape, mode, ranker_confidence, recommended_rendered, options_count, room_slug_sha256, verb_chosen; fires at lib/hmi/selector-dispatcher.cjs after pick resolution.
       - tension_engagement (D-05): ALLOWED_FIELDS tension_type, user_response, ttr_seconds, room_slug_sha256, context_hash; fires at lib/memory/pending-tension-store.cjs on user-initiated state transitions ONLY (decay excluded).
       - auto_explore_decision (D-06): ALLOWED_FIELDS finding_type, user_response, domain_match_score, room_slug_sha256; fires at the Phase 117 auto-explore file (recorded in 121-02-SUMMARY).
       - breakthrough_dismissed (D-07): ALLOWED_FIELDS detector_type, verb_chosen, ethics_tier (one of HARD_FLOOR/SOFT_BAND/NEUTRAL/GREEN per Phase 120 D-18), voice_audit_pass, room_slug_sha256; fires at lib/core/breakthrough/scanner.cjs surfaceBreakthrough() after F.7 dispatch.
       - hooked_axis_score (D-08): ALLOWED_FIELDS axis_name, score_value, room_slug_sha256, window_iso_week; emitted by scripts/hooked-rescore-117.cjs (existing path, normalized post-Plan 121-01).
       - empathy_observation (D-09): ALLOWED_FIELDS engaged_past_15m, handed_back_material, returned_within_48h, ttr_seconds, tester_id_hash; fires at scripts/empathy-observation-emit.cjs (manual trigger from tester harness).
       - room_receipt_written (D-09): ALLOWED_FIELDS room_slug_sha256, conversation_id_hash, generated_at_ts; fires at the Phase 119 receipt-write file via lib/core/room-receipt-emit.cjs helper.
       - command_invocation (D-09 broad sweep): ALLOWED_FIELDS command, outcome, duration_ms, context_hash; fires at scripts/telemetry-command-invocation.cjs (PostToolUse hook); HIGH VOLUME -- separate bucket; consumers MUST filter by event === 'command_invocation' to avoid drowning the high-signal events (type discriminator IS the drowning protection).
       - nav_bypass (inherited): ALLOWED_FIELDS op, reason, caller_hash, room_slug_sha256; fires at lib/core/room-db.cjs when navigation.cjs chokepoint is bypassed.

    7. **Forbidden Pattern Enforcement (Canon Part 8)** -- table:
       - Pattern | Regex | Rationale
       - Cypher fragment: `/\b(MATCH|RETURN|CREATE|MERGE)\s*\(/i` -- Brain query bodies
       - Email: `/[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}/` -- personal identifier
       - Phone: `/\b\d{3}[-.\s]?\d{3}[-.\s]?\d{4}\b/` -- personal identifier
       - Brain URL: `/brain\.mindrian\.ai/` -- LOCAL-to-BRAIN egress attempt
       - Absolute path with >=2 separators: `/^[\/~][^"]*\/[^"]*\//` -- file system reference
       - Raw hex >32 chars in non-sha256/hash field: `/^[0-9a-f]{33,}$/` -- suspected raw hash leak
       - Free-text prose heuristic: >120 chars + 3+ spaces + >40% lowercase -- raw artifact body

       Breach causes emit() to throw `Error{ code: 'TELEMETRY_VALIDATION' }`. Callers swallow per non-blocking pattern.

    8. **ISO-Week Rotation** -- file naming convention with examples:
       - 2026-01-01 (Thursday) -> events-2026-W01.jsonl
       - 2026-05-19 (Tuesday)  -> events-2026-W21.jsonl
       - 2026-12-31 (Thursday) -> events-2026-W53.jsonl (if applicable) or events-2027-W01.jsonl

    9. **SEED-002 Ingestion Guidance** -- per arXiv 2508.03680, consumers MUST:
       - Read all events-*.jsonl in lexicographic order (ISO week order)
       - Dispatch by event field (15-way switch + future expansion via schema_version)
       - Filter command_invocation bucket OUT for high-signal analyses (drowning protection); include only for tool-usage correlation studies
       - Group spans by session_id for within-session trajectory; by room_slug_sha256 for cross-session per-room trajectory
       - Handle schema_version dispatch (v1 today; v2 may add fields)

    10. **Schema Evolution Policy** -- additive vs structural changes; bump schema_version for structural; coexistence in same file via per-row dispatch.

    11. **D-12 Silent Observability** -- telemetry is lab-side; no /mos:status row, no SessionStart banner, no dashboard widget, no skill prose, no CHANGELOG-visible counter. SEED-002's /gsd:new-milestone trigger reads the corpus and decides when >=100-event threshold hits.

    12. **References** -- Canon (Part 7, 8, 9), 121-CONTEXT.md, SEED-002, arXiv 2508.03680, Phase 118 + Phase 88.1 Plan 16 precedents.

    Verify the docs file reflects schema.cjs EXACTLY (every event type listed, every ALLOWED_FIELDS entry documented).

    Implements: TELEMETRY-121-11.
  </action>
  <verify>
    <automated>test -f docs/TELEMETRY-SCHEMA.md && for evt in selector_pick tension_engagement auto_explore_decision breakthrough_dismissed hooked_axis_score empathy_observation room_receipt_written command_invocation nav_bypass mva_pipeline_started mva_agent_returned mva_brief_rendered mva_option_selected mva_brief_deployed mva_pipeline_failed; do grep -q "$evt" docs/TELEMETRY-SCHEMA.md || { echo "MISSING: $evt"; exit 1; }; done && echo "all 15 event types present"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/TELEMETRY-SCHEMA.md` succeeds
    - `wc -l docs/TELEMETRY-SCHEMA.md` returns >= 200
    - All 15 event types documented: `for evt in selector_pick tension_engagement auto_explore_decision breakthrough_dismissed hooked_axis_score empathy_observation room_receipt_written command_invocation nav_bypass mva_pipeline_started mva_agent_returned mva_brief_rendered mva_option_selected mva_brief_deployed mva_pipeline_failed; do grep -q "$evt" docs/TELEMETRY-SCHEMA.md || exit 1; done` exits 0
    - `grep -c "arXiv 2508.03680" docs/TELEMETRY-SCHEMA.md` returns >= 1
    - `grep -c "Canon Part 8" docs/TELEMETRY-SCHEMA.md` returns >= 1
    - `grep -c "schema_version" docs/TELEMETRY-SCHEMA.md` returns >= 2
    - `grep -c "command_invocation.*drowning\|drowning.*command_invocation\|drowning protection" docs/TELEMETRY-SCHEMA.md` returns >= 1
    - `grep -c "D-12\|silent observability\|Silent Observability" docs/TELEMETRY-SCHEMA.md` returns >= 1
    - `grep -P "\x{2014}" docs/TELEMETRY-SCHEMA.md` returns 0 (zero em-dashes)
  </acceptance_criteria>
  <done>docs/TELEMETRY-SCHEMA.md frozen v1 spec is shipped; all 15 event types + Part 8 + Part 10 + SEED-002 ingestion guidance documented.</done>
</task>

<task type="auto">
  <name>Task 2: Canon Part 8 adversarial compliance audit harness</name>
  <read_first>
    - lib/core/telemetry/writer.cjs + validator.cjs + schema.cjs (the 3 substrate files; MUST be network-free)
    - All capture-point files modified by Plans 121-02 + 121-03 (collected from those SUMMARYs)
    - 121-02-SUMMARY.md (path of Phase 117 auto-explore file recorded post-grep)
    - 121-03-SUMMARY.md (path of Phase 119 receipt-write file recorded post-grep)
    - docs/MINDRIAN-CANON.md Part 8 (constitutional reason for the audit)
  </read_first>
  <action>
    Create `tests/test-121-04-canon-part-8-audit.sh`:

    ```bash
    #!/usr/bin/env bash
    # Phase 121-04 -- Canon Part 8 adversarial compliance audit.
    # Verifies zero LOCAL-to-BRAIN egress surface across ALL telemetry-touching files.
    # Per Canon Part 8: "The boundary is not a privacy preference. It is a constitutional
    # property of the system." Single-chokepoint enforcement (Canon Part 9 precedent).
    set -euo pipefail
    cd "$(git rev-parse --show-toplevel)"

    fail() { echo "FAIL: $1" >&2; exit 1; }

    # Resolve dynamically-located files from the SUMMARY files.
    AUTO_EXPLORE_FILE=""
    RECEIPT_WRITE_FILE=""
    if [ -f .planning/phases/121-trajectory-telemetry/121-02-SUMMARY.md ]; then
      AUTO_EXPLORE_FILE=$(grep -oE "lib/core/auto-explore/[a-zA-Z0-9_.-]+\.cjs|lib/[a-zA-Z0-9_./-]*auto[a-zA-Z0-9_./-]*\.cjs" .planning/phases/121-trajectory-telemetry/121-02-SUMMARY.md | head -1 || true)
    fi
    if [ -f .planning/phases/121-trajectory-telemetry/121-03-SUMMARY.md ]; then
      RECEIPT_WRITE_FILE=$(grep -oE "lib/[a-zA-Z0-9_./-]*receipt[a-zA-Z0-9_./-]*\.cjs|lib/[a-zA-Z0-9_./-]*room[a-zA-Z0-9_./-]*receipt[a-zA-Z0-9_./-]*\.cjs" .planning/phases/121-trajectory-telemetry/121-03-SUMMARY.md | head -1 || true)
    fi

    # Static target list (always-present files)
    FILES=(
      lib/core/telemetry/writer.cjs
      lib/core/telemetry/validator.cjs
      lib/core/telemetry/schema.cjs
      lib/core/telemetry/writer.test.cjs
      lib/core/telemetry/validator.test.cjs
      lib/core/telemetry/schema.test.cjs
      lib/core/mva-telemetry.cjs
      scripts/migrate-telemetry-v1.cjs
      scripts/migrate-telemetry-v1.test.cjs
      lib/hmi/selector-dispatcher.cjs
      lib/memory/pending-tension-store.cjs
      lib/core/breakthrough/scanner.cjs
      scripts/empathy-observation-emit.cjs
      lib/core/room-receipt-emit.cjs
      scripts/telemetry-command-invocation.cjs
    )
    if [ -n "${AUTO_EXPLORE_FILE}" ] && [ -f "${AUTO_EXPLORE_FILE}" ]; then
      FILES+=("${AUTO_EXPLORE_FILE}")
    fi
    if [ -n "${RECEIPT_WRITE_FILE}" ] && [ -f "${RECEIPT_WRITE_FILE}" ]; then
      FILES+=("${RECEIPT_WRITE_FILE}")
    fi

    # Gate 1: all files exist
    for f in "${FILES[@]}"; do
      [ -f "$f" ] || fail "missing required file: $f"
    done

    # Gate 2: zero network surface (the constitutional invariant)
    FORBIDDEN='fetch\(|require\(['"'"'"]https?|require\(['"'"'"]http\b|brain\.mindrian|tavily\.com|api\.openai\.com|api\.anthropic\.com|api\.elevenlabs\.io|XMLHttpRequest|axios|node-fetch|got\.|undici'
    if grep -rE "$FORBIDDEN" "${FILES[@]}" >/dev/null 2>&1; then
      grep -rE "$FORBIDDEN" "${FILES[@]}"
      fail "network surface detected in telemetry-touching file(s) (Canon Part 8 breach)"
    fi

    # Gate 3: every capture-point source imports the unified writer (single-chokepoint)
    CAPTURE_POINTS=(
      lib/hmi/selector-dispatcher.cjs
      lib/memory/pending-tension-store.cjs
      lib/core/breakthrough/scanner.cjs
      scripts/empathy-observation-emit.cjs
      lib/core/room-receipt-emit.cjs
      scripts/telemetry-command-invocation.cjs
    )
    for f in "${CAPTURE_POINTS[@]}"; do
      grep -qE "require\(['\"][^'\"]*telemetry/writer['\"]\)|require\(['\"][^'\"]*core/telemetry/writer['\"]\)" "$f" \
        || fail "$f does not import lib/core/telemetry/writer (chokepoint bypass)"
    done

    # Gate 4: emit-time validator is the only Part 8 enforcement (no per-call validation drift)
    grep -qE "validateEventPayload" lib/core/telemetry/writer.cjs \
      || fail "writer.cjs does not call validateEventPayload (Part 8 enforcement missing)"

    # Gate 5: forbidden patterns from validator.cjs are documented in TELEMETRY-SCHEMA.md
    for pattern_label in "Cypher" "Email" "Phone" "Brain URL" "Absolute path" "Raw hex" "Free-text prose"; do
      grep -q "$pattern_label" docs/TELEMETRY-SCHEMA.md \
        || fail "TELEMETRY-SCHEMA.md missing forbidden-pattern documentation for: $pattern_label"
    done

    # Gate 6: mva-telemetry.cjs is a shim (Plan 121-01 atomic cutover invariant)
    if [ "$(wc -l < lib/core/mva-telemetry.cjs)" -gt 60 ]; then
      fail "lib/core/mva-telemetry.cjs exceeds 60 lines (shim invariant violated)"
    fi

    # Gate 7: zero em-dashes across all telemetry-touching files (project hard rule)
    if grep -lP "\x{2014}" "${FILES[@]}" docs/TELEMETRY-SCHEMA.md >/dev/null 2>&1; then
      grep -lP "\x{2014}" "${FILES[@]}" docs/TELEMETRY-SCHEMA.md
      fail "em-dashes found in telemetry source(s) -- use hyphens"
    fi

    echo "PASS: Canon Part 8 adversarial audit (7 gates green; ${#FILES[@]} files scanned)"
    ```

    `chmod +x tests/test-121-04-canon-part-8-audit.sh`.

    Implements: TELEMETRY-121-11 (Canon Part 8 enforcement evidence).
  </action>
  <verify>
    <automated>bash tests/test-121-04-canon-part-8-audit.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/test-121-04-canon-part-8-audit.sh` exits 0 with `PASS: Canon Part 8 adversarial audit (7 gates green; N files scanned)`
    - `test -x tests/test-121-04-canon-part-8-audit.sh` succeeds (executable bit set)
    - `grep -c "Canon Part 8" tests/test-121-04-canon-part-8-audit.sh` returns >= 1
    - `grep -c "brain.mindrian\|fetch\\\\(" tests/test-121-04-canon-part-8-audit.sh` returns >= 1 (the forbidden-pattern grep is present)
    - `grep -P "\x{2014}" tests/test-121-04-canon-part-8-audit.sh` returns 0
  </acceptance_criteria>
  <done>Canon Part 8 audit harness gates 7 invariants across 17+ telemetry-touching files. No network surface anywhere; single chokepoint enforced.</done>
</task>

<task type="auto">
  <name>Task 3: D-12 silent-observability invariant harness</name>
  <read_first>
    - commands/status.md (the primary user-facing surface that MUST NOT reference telemetry corpus)
    - commands/dashboard.md (if exists) -- second surface
    - lib/hmi/ directory (statusline + selector renderers; render-time surfaces)
    - skills/ directory (skill prose visible to Larry surface)
    - .planning/phases/121-trajectory-telemetry/121-CONTEXT.md (D-12 specification)
  </read_first>
  <action>
    Create `tests/test-121-04-silent-observability.sh`:

    ```bash
    #!/usr/bin/env bash
    # Phase 121-04 -- D-12 silent-observability invariant.
    # Verifies NO user-facing surface references the telemetry corpus count.
    # Per CONTEXT.md D-12: "Telemetry is lab-side concern. User doesn't need to see it."
    # SEED-002's /gsd:new-milestone trigger reads the corpus; users do not.
    set -euo pipefail
    cd "$(git rev-parse --show-toplevel)"

    fail() { echo "FAIL: $1" >&2; exit 1; }

    # User-facing surface directories.
    USER_FACING_DIRS=(
      commands
      lib/hmi
      skills
    )

    # Forbidden user-facing references:
    #   - "telemetry corpus" / "trajectory corpus" / "events corpus"
    #   - "corpus count" / "corpus size" / "corpus events"
    #   - "N / 100" or "/100 events" (the >=100 trigger threshold)
    #   - "SEED-002 progress" (premature exposure of the lab loop)
    FORBIDDEN_PATTERNS='(telemetry|trajectory|events)[[:space:]]+corpus|corpus[[:space:]]+(count|size|events)|/[[:space:]]*100[[:space:]]+events|[[:space:]]/100[[:space:]]+events|SEED-002[[:space:]]+progress|trajectory[[:space:]]+corpus|telemetry[[:space:]]+corpus'

    # Gate 1: no user-facing surface references the corpus
    HITS=$(grep -rEi "$FORBIDDEN_PATTERNS" "${USER_FACING_DIRS[@]}" 2>/dev/null || true)
    if [ -n "$HITS" ]; then
      echo "$HITS"
      fail "user-facing surface references telemetry corpus (D-12 silent-observability invariant violated)"
    fi

    # Gate 2: no /mos:status row references telemetry corpus
    if [ -f commands/status.md ]; then
      if grep -Ei "telemetry|trajectory|corpus|SEED-002" commands/status.md >/dev/null 2>&1; then
        grep -Ei "telemetry|trajectory|corpus|SEED-002" commands/status.md
        fail "commands/status.md exposes telemetry layer (D-12 violation)"
      fi
    fi

    # Gate 3: statusline broadcast scripts do not surface corpus
    if find lib/hmi scripts -name "statusline*" 2>/dev/null | head -1 | grep -q .; then
      if find lib/hmi scripts -name "statusline*" -exec grep -Ei "telemetry corpus|trajectory corpus|corpus count|/100 events" {} + 2>/dev/null | head -1 | grep -q .; then
        fail "statusline references telemetry corpus (D-12 violation)"
      fi
    fi

    # Gate 4: TELEMETRY-SCHEMA.md ITSELF references D-12 invariant (documentation evidence)
    grep -q "D-12\|silent observability\|Silent Observability" docs/TELEMETRY-SCHEMA.md \
      || fail "docs/TELEMETRY-SCHEMA.md missing D-12 silent-observability section"

    echo "PASS: D-12 silent-observability invariant (4 gates green; user-facing surfaces clean)"
    ```

    `chmod +x tests/test-121-04-silent-observability.sh`.

    Implements: TELEMETRY-121-12 (silent observability invariant).
  </action>
  <verify>
    <automated>bash tests/test-121-04-silent-observability.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/test-121-04-silent-observability.sh` exits 0 with `PASS: D-12 silent-observability invariant ...`
    - `test -x tests/test-121-04-silent-observability.sh` succeeds
    - `grep -c "D-12" tests/test-121-04-silent-observability.sh` returns >= 1
    - `grep -E "telemetry|trajectory|corpus|SEED-002" commands/status.md` returns 0 (no matches)
    - `grep -P "\x{2014}" tests/test-121-04-silent-observability.sh` returns 0
  </acceptance_criteria>
  <done>D-12 silent-observability invariant gated by 4 grep checks across commands/, lib/hmi/, skills/. User-facing surface is clean.</done>
</task>

<task type="auto">
  <name>Task 4: Phase-121 meta-scaffold + run-all aggregator finalization</name>
  <read_first>
    - tests/run-all-121.sh (the aggregator; should already invoke 00 + 01 + 02 + 03 scaffolds by now)
    - tests/test-121-00-scaffold.sh through tests/test-121-03-scaffold.sh (the per-plan scaffolds)
    - tests/test-121-04-canon-part-8-audit.sh (from Task 2)
    - tests/test-121-04-silent-observability.sh (from Task 3)
    - tests/run-all-124.sh (run-all pattern reference)
  </read_first>
  <action>
    Create `tests/test-121-04-scaffold.sh` -- the meta-harness (5 gates):

    ```bash
    #!/usr/bin/env bash
    # Phase 121-04 meta-scaffold: gates the full phase-121 contract.
    set -euo pipefail
    cd "$(git rev-parse --show-toplevel)"

    fail() { echo "FAIL: $1" >&2; exit 1; }

    # Gate 1: docs/TELEMETRY-SCHEMA.md exists and documents all 15 event types
    [ -f docs/TELEMETRY-SCHEMA.md ] || fail "docs/TELEMETRY-SCHEMA.md missing"
    for evt in selector_pick tension_engagement auto_explore_decision breakthrough_dismissed hooked_axis_score empathy_observation room_receipt_written command_invocation nav_bypass mva_pipeline_started mva_agent_returned mva_brief_rendered mva_option_selected mva_brief_deployed mva_pipeline_failed; do
      grep -q "$evt" docs/TELEMETRY-SCHEMA.md || fail "TELEMETRY-SCHEMA.md missing $evt"
    done

    # Gate 2: Canon Part 8 audit passes
    bash tests/test-121-04-canon-part-8-audit.sh >/dev/null || fail "Canon Part 8 audit failed"

    # Gate 3: D-12 silent-observability invariant passes
    bash tests/test-121-04-silent-observability.sh >/dev/null || fail "D-12 silent-observability invariant failed"

    # Gate 4: all prior plan scaffolds still green
    for scaffold in tests/test-121-00-scaffold.sh tests/test-121-01-scaffold.sh tests/test-121-02-scaffold.sh tests/test-121-03-scaffold.sh; do
      [ -f "$scaffold" ] || fail "missing prior scaffold: $scaffold"
      bash "$scaffold" >/dev/null || fail "$scaffold failed"
    done

    # Gate 5: zero em-dashes in plan files
    if grep -lP "\x{2014}" .planning/phases/121-trajectory-telemetry/121-*-PLAN.md docs/TELEMETRY-SCHEMA.md >/dev/null 2>&1; then
      grep -lP "\x{2014}" .planning/phases/121-trajectory-telemetry/121-*-PLAN.md docs/TELEMETRY-SCHEMA.md
      fail "em-dashes found in plan files or schema doc"
    fi

    echo "PASS: Phase 121 meta-scaffold (5 gates green; all 5 plans aggregate clean)"
    ```

    `chmod +x tests/test-121-04-scaffold.sh`.

    Update `tests/run-all-121.sh` -- ensure it invokes ALL 5 plan scaffolds in order:
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail
    cd "$(git rev-parse --show-toplevel)"

    for s in tests/test-121-00-scaffold.sh tests/test-121-01-scaffold.sh tests/test-121-02-scaffold.sh tests/test-121-03-scaffold.sh tests/test-121-04-scaffold.sh; do
      echo "Running $s..."
      bash "$s" || { echo "FAIL: $s"; exit 1; }
    done

    echo "PASS: all 5 Phase 121 plan scaffolds green"
    ```

    `chmod +x tests/run-all-121.sh`.

    Final verification: run `bash tests/run-all-121.sh` -- must exit 0.
  </action>
  <verify>
    <automated>bash tests/run-all-121.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/test-121-04-scaffold.sh` exits 0 with `PASS: Phase 121 meta-scaffold (5 gates green; ...)`
    - `bash tests/run-all-121.sh` exits 0 with `PASS: all 5 Phase 121 plan scaffolds green`
    - `test -x tests/test-121-04-scaffold.sh` succeeds
    - `test -x tests/run-all-121.sh` succeeds
    - `grep -c "test-121-04-scaffold.sh\|test-121-00-scaffold.sh\|test-121-01-scaffold.sh\|test-121-02-scaffold.sh\|test-121-03-scaffold.sh" tests/run-all-121.sh` returns >= 5
    - `grep -P "\x{2014}" tests/test-121-04-scaffold.sh tests/run-all-121.sh` returns 0
  </acceptance_criteria>
  <done>Meta-scaffold gates the full phase contract. run-all-121.sh aggregates all 5 plans. All gates green.</done>
</task>

</tasks>

<verification>
- `bash tests/run-all-121.sh` exits 0 (5 plans aggregate green: 00 + 01 + 02 + 03 + 04)
- docs/TELEMETRY-SCHEMA.md documents all 15 event types (verified by event-name grep)
- Canon Part 8 audit: zero network surface across 17+ telemetry-touching files (verified by adversarial grep sweep)
- D-12 silent-observability: zero user-facing telemetry corpus references in commands/ + lib/hmi/ + skills/ (verified by adversarial grep)
- mva-telemetry.cjs is a shim (<= 60 lines) -- atomic cutover from Plan 121-01 preserved
- Every capture point imports lib/core/telemetry/writer.cjs (single chokepoint enforced)
- Zero em-dashes across all telemetry files + schema doc + plan files
</verification>

<success_criteria>
- docs/TELEMETRY-SCHEMA.md frozen v1 spec is shipped with all 15 event types documented
- Canon Part 8 adversarial audit harness gates 7 invariants
- D-12 silent-observability invariant gated by 4 grep checks
- Meta-scaffold gates the full phase contract (5 gates)
- run-all-121.sh aggregates 5 plan scaffolds
- TELEMETRY-121-11 + TELEMETRY-121-12 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/121-trajectory-telemetry/121-04-SUMMARY.md` documenting:
- docs/TELEMETRY-SCHEMA.md final line count + section headings (12 sections)
- 17 telemetry-touching files audited (or N when dynamic-path resolution adds files)
- Canon Part 8 audit gate-by-gate breakdown
- D-12 silent-observability gate-by-gate breakdown
- run-all-121.sh aggregate count (5/5 scaffolds green)
- Phase 121 completion status: all 12 requirements TELEMETRY-121-01..12 satisfied
- Forward reference to SEED-002 activation (post-v1.13.0 final, when corpus >= 100 events)
</output>
