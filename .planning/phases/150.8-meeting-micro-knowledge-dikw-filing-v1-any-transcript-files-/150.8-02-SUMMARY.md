---
phase: 150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-
plan: 02
subsystem: navigation-edges / canon
tags: [edge-taxonomy, canon-amendment, dikw, frozen-set, navigator-gate]
requires:
  - "150.8-01 (writeClaimNode typed-claim DIKW substrate)"
  - "lib/core/navigation/edges.cjs ALLOWED_EDGE_TYPES frozen Set + writeEdge"
provides:
  - "REFINES / ROOT_CAUSES / INSTANTIATES edge types in the frozen closed set"
  - "canonical FLOOR test for the 3 new members + TV-01 edge round-trip"
  - "Canon v1.7 (Appendix D entry 18 + Part 4 edge-list + version bump)"
affects:
  - "lib/core/navigation/edges.cjs"
  - "tests/test-edges-refines-rootcauses-instantiates-floor.cjs"
  - "tests/claim-harness/claim-c3.cjs"
  - "tests/run-all-150.8.sh"
  - "docs/MINDRIAN-CANON.md"
  - "docs/CANON-PHASE-MAP.md"
tech-stack:
  added: []
  patterns:
    - "Additive frozen-Set extension (the 150-01 STATES/SUPPORTS/DESCRIBES idiom verbatim)"
    - "Navigator-gated canon-amendment-on-itself (Part 6 dog-fooding, entries 14/15)"
    - "Atomic lockstep wave: code + tests + canon docs in one commit so CI never goes RED"
    - "valid_from/valid_until ride the existing writeEdge properties JSON (zero signature change)"
key-files:
  created:
    - "tests/test-edges-refines-rootcauses-instantiates-floor.cjs"
  modified:
    - "lib/core/navigation/edges.cjs"
    - "tests/claim-harness/claim-c3.cjs"
    - "tests/run-all-150.8.sh"
    - "docs/MINDRIAN-CANON.md"
    - "docs/CANON-PHASE-MAP.md"
decisions:
  - "D-150.8 (navigator-LOCKED 2026-06-12): APPROVE adding REFINES + ROOT_CAUSES + INSTANTIATES to the frozen ALLOWED_EDGE_TYPES, WITH canon version bump 1.6 -> 1.7"
metrics:
  duration: "~12 min"
  completed: "2026-06-12"
  tasks: 3
  files: 6
---

# Phase 150.8 Plan 02: REFINES + ROOT_CAUSES + INSTANTIATES Edge Amendment Summary

Moved a frozen constitutional set: added the three Knowledge-rung relationship edge types (REFINES / ROOT_CAUSES / INSTANTIATES) to `ALLOWED_EDGE_TYPES` behind a navigator-gated AskUserQuestion approval, landed as ONE atomic lockstep wave (edges.cjs + FLOOR test + C3 fence + canon docs), and bumped the Canon to v1.7.

## What Was Built

- **edges.cjs additive block** -- `REFINES`, `ROOT_CAUSES`, `INSTANTIATES` appended after `DESCRIBES` in the frozen `ALLOWED_EDGE_TYPES` Set, mirroring the Phase 150-01 STATES/SUPPORTS/DESCRIBES additive idiom verbatim. The comment block carries the Canon Part 4 line, the Part 8 enum/scalar-only-props line, the lineage line, the per-edge semantics, AND the unique NAVIGATOR-GATED amendment line naming the AskUserQuestion approval (D-150.8, 2026-06-12). `writeEdge` needed ZERO change -- `valid_from`/`valid_until` ride the existing `properties` JSON param.
- **tests/test-edges-refines-rootcauses-instantiates-floor.cjs** -- the canonical FLOOR test (6 checks): all 3 new members present; the full FLOOR (18 prior members through STATES/SUPPORTS/DESCRIBES + PIVOTED/SELECTED_REACH/FEEDS_INTO/VALIDATES) preserved; frozen Set instance; the TV-01 round-trip (a REFINES edge carrying valid_from/valid_until round-trips through the properties JSON, DIKW-03 edge half); ROOT_CAUSES + INSTANTIATES accepted; a made-up type (`GENERALIZES`, a deferred type) rejected with `invalid_edge_type`. Never asserts `.size` (Pitfall 3).
- **claim-c3.cjs C3 fence extension** -- mints a REFINES edge on the real claim-harness fixture through the navigation.writeEdge chokepoint (closed-set membership exercised on the live substrate), asserting valid_from rides the properties JSON. The existing CONTRADICTS + honest-negative assertions were left byte-intact.
- **run-all-150.8.sh** -- the floor test added to the phase-gate aggregator.
- **MINDRIAN-CANON.md** -- Appendix D entry 18 (in the entry-15 voice), the Part 4 typed-edge vocabulary closed-set note listing the trio, header `Version: 1.6 -> 1.7`, and the footer v1.7 stamp.
- **CANON-PHASE-MAP.md** -- the canon-reference header (v1.6 -> v1.7), a Phase 150.8 row under the "v1.13.1 Larry Reaches connector spine + engine flip" section (canon_parts 4/7/8/9), and a v1.7 version-history entry.

## LOCKED Decision (Task 1 navigator gate, pre-resolved)

**Task 1 was a `checkpoint:decision` gate with `gate="blocking-human"`.** The navigator PRE-RESOLVED it (decision 2026-06-12, mirroring the Phase 148 D-09 procedure), so the executor did NOT pause:

> **D-150.8 (navigator-LOCKED 2026-06-12):** APPROVE the REFINES + ROOT_CAUSES + INSTANTIATES addition to `ALLOWED_EDGE_TYPES`, WITH canon version bump 1.6 -> 1.7. (Option `approve` -- the recommended option in the plan.) The 3 deferred types (GENERALIZES / CONTRADICTS_CONDITIONALLY / SUPERSEDES_v2) stay OUT (SEED-023, v1.14.0).

This is a doctrine-level change (new relationship semantics in Part 4) approved via AskUserQuestion BEFORE any edge-taxonomy or canon byte changed -- the constitutional discipline that distinguishes this from the routine additive-without-amendment idiom. Recorded for traceability per the Task-1 action contract.

## Verification

| Fence | Result |
|-------|--------|
| `node tests/test-edges-refines-rootcauses-instantiates-floor.cjs` | PASS (6/6) |
| `bash tests/claim-harness/run-all-claims.sh` | PASS (9/9, C3 REFINES extension green) |
| `bash tests/run-all-150.8.sh` | PASS (4/4) |
| `bash tests/run-all-148.sh` | PASS (18/18) |
| Canon grep (REFINES / ROOT_CAUSES in CANON, 150.8 in MAP) | PASS |
| em-dash sweep (canon docs + new code/test files) | zero em-dashes |

## Deviations from Plan

None - plan executed exactly as written. The navigator gate (Task 1) was pre-resolved per the execution contract; the atomic lockstep wave (Tasks 2 + 3) landed in one commit so CI never went RED mid-phase.

## Invariants Held

- The 5 original cascade types + STATES/SUPPORTS/DESCRIBES/INFORMS untouched (asserted by the FLOOR check).
- Frozen 148 contracts untouched: MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15 recommend gate, the 6-reach bank, the 3 postures (run-all-148.sh 18/18 green).
- Part 8: the claim/segment BODY never lands on the edge; valid_from/valid_until are scalars on the existing properties JSON; zero Brain egress delta.
- `writeEdge` signature unchanged (zero DDL, zero column migration).

## Commits

- `6148d2db`: feat(150.8-02): REFINES + ROOT_CAUSES + INSTANTIATES edge amendment (navigator-gated, canon 1.6->1.7)

## Self-Check: PASSED

- FOUND: tests/test-edges-refines-rootcauses-instantiates-floor.cjs
- FOUND: .planning/phases/150.8-.../150.8-02-SUMMARY.md
- FOUND: commit 6148d2db
- Runtime: ALLOWED_EDGE_TYPES.has(REFINES/ROOT_CAUSES/INSTANTIATES) = true; Object.isFrozen = true
