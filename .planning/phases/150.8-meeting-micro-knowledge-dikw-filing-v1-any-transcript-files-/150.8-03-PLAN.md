---
phase: 150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-
plan: 03
type: execute
wave: 3
depends_on: ["150.8-01"]
files_modified:
  - commands/file-meeting.md
  - references/meeting/knowledge-typing.md
  - scripts/check-pending-ambiguous.cjs
  - tests/test-ambiguous-queue.cjs
  - tests/test-check-pending-ambiguous.cjs
  - tests/test-trigger-invariant.cjs
  - tests/run-all-150.8.sh
autonomous: true
requirements: [DIKW-05, DIKW-06, DIKW-11]
must_haves:
  truths:
    - "file-meeting.md Step 3 is the Claimify 4-pass pipeline (selection -> disambiguation -> decomposition -> typing) calling writeClaimNode per atomic claim"
    - "An unresolvable segment mints a proposed claim with disambiguation='ambiguous' (queued, never silently dropped)"
    - "A SessionStart hook resurfaces the ambiguous queue with a mandatory Dismiss, throttles re-surfacing, and degrades to {continue:true} on empty queue or any error"
    - "Any transcript that lands fires the DIKW ladder: a fixture filing writes last-cascade.json and SENS-06 artifact_filed fires (asserted by test, not rebuilt)"
  artifacts:
    - path: "references/meeting/knowledge-typing.md"
      provides: "the 6-type knowledge taxonomy prompt (fact/causal/heuristic/anomaly_cue/mental_model/assumption) + conditions/counter_conditions extraction"
      min_lines: 40
    - path: "scripts/check-pending-ambiguous.cjs"
      provides: "SessionStart queue-then-surface resurface (mirrors check-pending-breakthrough.cjs) with mandatory Dismiss + throttle"
      min_lines: 80
  key_links:
    - from: "commands/file-meeting.md"
      to: "lib/core/navigation.cjs writeClaimNode"
      via: "Step 3 typing pass calls writeClaimNode per atomic claim"
      pattern: "writeClaimNode"
    - from: "scripts/check-pending-ambiguous.cjs"
      to: "room.db nodes (disambiguation='ambiguous')"
      via: "json_extract scan through the navigation chokepoint"
      pattern: "disambiguation"
    - from: "tests/test-trigger-invariant.cjs"
      to: "lib/core/insight-sensors.cjs dispatchSensors"
      via: "asserts SENS-06 artifact_filed fires after a fixture filing"
      pattern: "dispatchSensors"
---

<objective>
Rewrite the meeting extraction into the Claimify 4-pass pipeline (DIKW-05), wire the ambiguous queue + its SessionStart resurface (DIKW-06), and ASSERT the any-transcript DIKW trigger invariant (DIKW-11) rather than rebuilding it.

Purpose: Today file-meeting.md Step 3 is a single segment-classification prompt that writes .md artifacts only, never graph claims. This plan replaces it with selection -> disambiguation -> decomposition -> typing (LLM prose, NOT a CJS extractor - extraction is judgment, RESEARCH anti-pattern), with the typing pass calling Plan 01's writeClaimNode per atomic claim (option (a) of the GATE-0 pitfall: extraction IS the segmentation authority). Unresolvable segments mint proposed claims marked ambiguous and queued, never dropped. The trigger invariant already fires (SENS-06 fires on filed artifacts per RESEARCH finding 6 / TRIG-01) - the plan asserts it with a test.

Output: the 4-pass file-meeting.md Step 3, the new knowledge-typing.md reference, the check-pending-ambiguous.cjs SessionStart hook, and three test drivers (queue, resurface, trigger invariant).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-CONTEXT.md
@.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-RESEARCH.md
@.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-PATTERNS.md
@commands/file-meeting.md
@references/meeting/segment-classification.md
</context>

<artifacts_this_phase_produces>
- `commands/file-meeting.md` Step 3 rewritten as the Claimify 4-pass pipeline (calls navigation.writeClaimNode per atomic claim)
- `references/meeting/knowledge-typing.md` - the 6-type knowledge taxonomy prompt
- `scripts/check-pending-ambiguous.cjs` - the SessionStart ambiguous-queue resurface hook
- `tests/test-ambiguous-queue.cjs`, `tests/test-check-pending-ambiguous.cjs`, `tests/test-trigger-invariant.cjs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Claimify 4-pass extraction in file-meeting.md + knowledge-typing.md reference (DIKW-05)</name>
  <files>commands/file-meeting.md, references/meeting/knowledge-typing.md</files>
  <read_first>
    - commands/file-meeting.md:266-296 - Step 3 today (the single segment-classification block to replace); :272 shows how segment-classification.md is loaded in Setup
    - references/meeting/segment-classification.md:115-117,205-252 - the assumption-extraction embryo, the Flag-Potential-Noise rule, the below-0.5 ask-the-user threshold, the Multi-Type resolution rules (the format to mirror for knowledge-typing.md)
    - 150.8-PATTERNS.md section 7 - the exact 4-pass delta spec (selection/disambiguation/decomposition/typing) and the writeClaimNode-per-atomic-claim wiring
    - lib/core/navigation.cjs (Plan 01 re-export) - navigation.writeClaimNode signature + KNOWLEDGE_TYPES
  </read_first>
  <action>
    Replace commands/file-meeting.md Step 3 (lines 266-296) with the 4-pass Claimify pipeline, all LLM prose (NO CJS extractor):

    1. Selection - keep the 6-type segment taxonomy + priority sort verbatim (it IS the selection pass; segment-classification.md role unchanged). Filler/social talk tagged no_claim and discarded.
    2. Disambiguation - resolve pronouns/referents using speaker identity (Step 2 roster) + prior 2-3 turns; UNRESOLVABLE -> mint the claim with disambiguation='ambiguous' (queued for human review, NEVER dropped). Extend the existing below-0.5 ask-the-user rule at segment-classification.md:240 rather than inventing a new gate.
    3. Decomposition - split compound segments into atomic claims (extends the Multi-Type rules :244-252).
    4. Typing - classify each atomic claim against the NEW references/meeting/knowledge-typing.md 6-enum, extract conditions/counter_conditions + valid_from/valid_until, then call navigation.writeClaimNode per atomic claim with { text, knowledge_type, conditions, counter_conditions, valid_from, valid_until, sourceSpeaker, sourceSegment, sessionId } (and disambiguation:'ambiguous' for unresolved ones).

    Keep the "Show Classification Reasoning" transparency block, extending each line with the knowledge_type (e.g. "insight -> market-analysis | claim: fact | Confidence: 0.85"). Load knowledge-typing.md in the command Setup section exactly the way segment-classification.md is loaded today (:272). Preserve all other steps (1, 2, 4, 5, 6) byte-intact - Part 7: extend, do not rebuild.

    Create references/meeting/knowledge-typing.md mirroring segment-classification.md's format: per-type sections (fact / causal / heuristic / anomaly_cue / mental_model / assumption) each with Definition, Signal phrases, Confidence indicators; plus a conditions/counter_conditions extraction block (CTA contrastive probing: "when does this hold? / when does it break?") and a valid_from/valid_until temporal note. Answer the Part 7 reuse question in a header comment: this EXTENDS /mos:file-meeting + the segment-classification.md reference; repointing is insufficient because the 6 knowledge_type semantics are net-new (no existing reference carries them). NO em-dashes.
  </action>
  <verify>
    <automated>test -f references/meeting/knowledge-typing.md && grep -q "writeClaimNode" commands/file-meeting.md && grep -q "ambiguous" commands/file-meeting.md && grep -Eq "fact|causal|heuristic|anomaly_cue|mental_model|assumption" references/meeting/knowledge-typing.md && ! grep -P '[\x{2014}\x{2013}]' commands/file-meeting.md references/meeting/knowledge-typing.md</automated>
  </verify>
  <done>Step 3 is the 4-pass pipeline calling writeClaimNode per atomic claim; unresolvable segments are marked ambiguous; knowledge-typing.md carries the 6-type taxonomy + conditions extraction; zero em-dashes; all other file-meeting steps unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ambiguous queue + SessionStart resurface hook (DIKW-06)</name>
  <files>scripts/check-pending-ambiguous.cjs, tests/test-ambiguous-queue.cjs, tests/test-check-pending-ambiguous.cjs, tests/run-all-150.8.sh</files>
  <read_first>
    - scripts/check-pending-breakthrough.cjs (full) - the SessionStart queue-then-surface template: emitContinue / emitContinueWithContext (:41-59), room resolution (:61-88), lazy-require + scan + D-16 empty-state silence (:101-119), additionalContext payload (:159-178), uncaughtException guard (:181-185)
    - lib/memory/pending-tension-store.cjs:382-409,76-82 - the 3-strikes evaluateAndDecay throttle + the Part 8 key-denylist discipline
    - lib/hmi/selector-dispatcher.cjs:732-742 - the F.7 mandatory-Dismiss guard doctrine to mirror
    - 150.8-PATTERNS.md section 3 - the full delta: the json_extract scan for disambiguation='ambiguous', the graph-native surfacing_count throttle, the F.4/F.1 host choice (A3)
    - lib/core/room-db.cjs - openRoomDb (the chokepoint to read room.db)
  </read_first>
  <behavior>
    - Test: a claim minted with disambiguation='ambiguous' and review_status='proposed' is found by the queue scan (json_extract(properties,'$.disambiguation')='ambiguous')
    - Test: a non-ambiguous claim is NOT in the queue
    - Test: an ambiguous claim is never silently dropped (it persists as a proposed node until dismissed/resolved)
    - Test: check-pending-ambiguous.cjs against a fixture room with queued ambiguous claims emits a parseable {continue:true, hookSpecificOutput:{...additionalContext}} envelope carrying COUNTS + node ids only (no segment prose, Part 8) and a mandatory Dismiss verb
    - Test: against an empty queue the hook emits {continue:true} (D-16 silence) and exits 0
    - Test: the throttle bumps surfacing_count on the node; >= 3 flips disambiguation to 'dismissed' (or the user Dismiss flips review_status to rejected) so the same node never re-surfaces forever (dismissal-canary fix)
  </behavior>
  <action>
    Create scripts/check-pending-ambiguous.cjs by copying scripts/check-pending-breakthrough.cjs verbatim and changing only: (1) the scan - open room.db via roomDb.openRoomDb and SELECT id, properties FROM nodes WHERE type='claim' AND json_extract(properties,'$.disambiguation')='ambiguous' AND review_status='proposed' (through the navigation chokepoint pattern, never a direct room-db require outside the allow-list); (2) the Shape host - F.4 (Insight Extraction; "review ambiguous segments") or F.1 (A3 discretion; recommendation F.4); (3) the payload - COUNTS + node ids only, NEVER segment prose (Part 8 enum/scalar-only); (4) a MANDATORY Dismiss verb (mirror the F.7 :732-742 guard doctrine). Add the throttle: a surfacing_count integer inside each ambiguous claim's properties JSON, bumped by the hook each surface, with >= 3 flipping disambiguation 'ambiguous' -> 'dismissed' (mirror pending-tension-store.cjs evaluateAndDecay 3-strikes). Preserve the breakthrough template's room resolution, D-16 empty-state silence, and the uncaughtException guard verbatim (the hook NEVER blocks session start). Copy the Part 8 key-denylist discipline for anything written outside room.db.

    Register the hook in hooks/hooks.json on SessionStart alongside check-pending-breakthrough (additive entry; do NOT remove the breakthrough hook).

    Write tests/test-ambiguous-queue.cjs (the writer/queue unit: mint ambiguous + non-ambiguous claims via navigation.writeClaimNode on an in-memory db, assert the scan partitions them and never drops the ambiguous one) and tests/test-check-pending-ambiguous.cjs (the hermetic-spawn integration, mirror tests/test-mva-dror-harness.cjs:156-184: spawn the hook under a hermetic HOME + MINDRIAN_ACTIVE_ROOM_DIR pointed at a fixture room, assert the stdout envelope parses, carries the Dismiss verb + counts, and degrades to {continue:true} on empty). Add both to run-all-150.8.sh. NO em-dashes.
  </action>
  <verify>
    <automated>node tests/test-ambiguous-queue.cjs && node tests/test-check-pending-ambiguous.cjs && bash tests/run-all-150.8.sh</automated>
  </verify>
  <done>Ambiguous segments queue as proposed nodes (never dropped); the SessionStart hook resurfaces them with a mandatory Dismiss + throttle, degrades silently on empty/error; both drivers green in run-all-150.8.sh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assert the any-transcript DIKW trigger invariant (DIKW-11)</name>
  <files>tests/test-trigger-invariant.cjs, tests/run-all-150.8.sh</files>
  <read_first>
    - lib/core/insight-sensors.cjs:155-192,308-344 - deriveTurnSignals + SENS-06 artifact_filed (already fires on filed artifacts; RESEARCH finding 6 / TRIG-01)
    - .planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-GATE-0-CASCADE-VERDICT.md:60-94 - the last-cascade.json side-channel written per filed file (the seam that exists)
    - tests/run-all-150.5.sh - the sensor-firability test patterns (the production-turn-shape harness style)
    - 150.8-RESEARCH.md TRIG-01 row (line 71) + Wave-0 gap test-trigger-invariant.cjs (line 372)
  </read_first>
  <behavior>
    - Test: after a fixture filing writes last-cascade.json (the artifact_filed side-channel), dispatchSensors fed the production turn shape returns a candidate reach (SENS-06 artifact_filed fires) - ASSERTED, not rebuilt
    - Test: the invariant is honored for the any-transcript path specifically (a filed meeting artifact is an artifact_filed event like any other)
    - Test: this is an ASSERTION of shipped behavior - the test must FAIL if SENS-06 stops firing on a filed artifact (regression fence), proving the trigger is wired, not assumed
  </behavior>
  <action>
    Write tests/test-trigger-invariant.cjs as a bare node CJS driver mirroring the 150.5 sensor-firability harness: construct the production turn shape (via normalizeTurn/deriveTurnSignals from a fixture last-cascade.json that represents a filed transcript artifact), call dispatchSensors, and assert SENS-06 (artifact_filed) appears in the returned candidate reaches. The test ASSERTS the shipped trigger invariant (RESEARCH finding 6: SENS-06 already fires) - it does NOT rebuild the trigger. Include a negative guard: with NO filed-artifact signal, SENS-06 does not fire (honest negative). Add to run-all-150.8.sh. NO em-dashes.
  </action>
  <verify>
    <automated>node tests/test-trigger-invariant.cjs && bash tests/run-all-150.8.sh</automated>
  </verify>
  <done>The any-transcript trigger invariant is asserted by test: a fixture filing fires SENS-06 artifact_filed through dispatchSensors; the honest-negative arm proves the assertion has teeth; green in run-all-150.8.sh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript prose -> ambiguous-queue store / hook payload | Transcript excerpts must stay LOCAL; the queue/payload carries counts + ids only |
| SessionStart hook -> session start | The hook must never block or crash session start |
| transcript content -> file-meeting writeClaimNode params | Untrusted spoken content crosses into claim node text/props |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.8-08 | Information Disclosure | transcript excerpts leaking out of room.db via the queue/hook payload (Part 8) | mitigate | The ambiguous queue stores excerpts only inside room.db (local); the hook additionalContext payload carries COUNTS + node ids only, never segment prose; copy the pending-tension-store key-denylist discipline |
| T-150.8-09 | Denial of Service | a crashing or blocking SessionStart hook | mitigate | Copy the breakthrough hook's uncaughtException guard + D-16 empty-state silence; the hook always emits {continue:true} and exits 0 on any error |
| T-150.8-10 | Spoofing/Tampering | dismissal-canary (same item re-surfacing forever erodes trust) | mitigate | 3-strikes surfacing_count throttle + MANDATORY Dismiss verb (mirror F.7); >= 3 flips to dismissed |
| T-150.8-11 | Information Disclosure | claim prose reaching a Brain packet via the typed claim (Part 8) | mitigate | knowledge_type rides as an enum scalar; prose stays in room.db/artifacts; the packet path sha256-hashes prose by default (verified Plan 04 Part-8 sweep) |
| T-150.8-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this plan; CJS + Markdown only |
</threat_model>

<verification>
- `node tests/test-ambiguous-queue.cjs` - queue partitions ambiguous vs not; never drops
- `node tests/test-check-pending-ambiguous.cjs` - hermetic hook envelope + Dismiss + empty-degrade
- `node tests/test-trigger-invariant.cjs` - SENS-06 fires on a fixture filing (asserted)
- `bash tests/run-all-150.8.sh` - phase gate green
- grep checks confirm writeClaimNode wired in file-meeting.md, ambiguous handling present, zero em-dashes
</verification>

<success_criteria>
- file-meeting.md Step 3 is the Claimify 4-pass pipeline calling writeClaimNode per atomic claim (extraction is the segmentation authority)
- Unresolvable segments queue as proposed disambiguation='ambiguous' nodes, never dropped
- The SessionStart resurface hook has a mandatory Dismiss + 3-strikes throttle + Desktop/Cowork-safe degrade
- The any-transcript trigger invariant is ASSERTED (SENS-06 fires), not rebuilt
- Zero em-dashes; run-all-150.8.sh green
</success_criteria>

<output>
Create `.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-03-SUMMARY.md` when done.
</output>
