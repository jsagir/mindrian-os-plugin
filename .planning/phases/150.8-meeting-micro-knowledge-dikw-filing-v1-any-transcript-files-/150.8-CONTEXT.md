---
phase: 150.8
slug: meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-
inserted: 2026-06-11
milestone: v1.13.1
depends_on: [150.6]
canon_parts: [4, 7, 8, 9]
status: scoped
source_seed: SEED-023
---

# Phase 150.8 CONTEXT: Meeting Micro-Knowledge DIKW Filing v1

## Navigator directive (verbatim intent)

"Any transcript needs to trigger a proper filing system, to be a representation of Ackoff from data to wisdom." And on scoping: the meeting filing system HAS to ship on the v1.13.1 train, not wait for v1.14.0.

## What this phase is

The v1 SLICE of SEED-023 (full proposal verbatim at `.planning/research/2026-06-11-meeting-micro-knowledge-dikw-proposal.md` -- read it before planning). The slice = the proposal's priority-table items 1, 3, 4, 5 plus the minimum-viable canon amendment for item 2, plus the any-transcript trigger invariant. The heavy remainder (insight node layer, ACTA reanalyze pass, speaker expertise profiling, live CDM probes via Velma, causal FEYNMAN timeline) STAYS in SEED-023 for v1.14.0.

## In scope (the v1 slice)

1. **`knowledge_type` on the claim schema** (proposal P1, Low/High): 6-value enum (fact | causal | heuristic | anomaly_cue | mental_model | assumption) + `conditions` / `counter_conditions` fields (P4) -- ADDITIVE on the claim node, written through the navigation.cjs chokepoint. Part 9 unchanged: typed claims mint `proposed`.
2. **Claimify-style 4-pass extraction in the meeting filing path** (P3): selection -> disambiguation (speaker + prior turns; unresolvable -> `ambiguous`, queued for human review, never silently dropped) -> decomposition -> typing. Replaces the single extract-claims prompt in /mos:file-meeting.
3. **Temporal validity** (P5): `valid_from` / `valid_until` on claims and edges (additive columns).
4. **Minimum-viable edge-taxonomy amendment** (P2, REDUCED): add REFINES + ROOT_CAUSES + INSTANTIATES ONLY (GENERALIZES / CONTRADICTS_CONDITIONALLY / SUPERSEDES deferred to SEED-023). This is a Canon Part 4 + Phase 108 frozen-taxonomy amendment -- use the canon-amendment-on-itself mechanism (Appendix D entry 15 pattern): one atomic lockstep wave across ALLOWED_EDGE_TYPES (edges.cjs chokepoint), the claim-harness C3 fence (rewritten to assert the new closed set), CANON-PHASE-MAP row, MINDRIAN-CANON Appendix D entry. Navigator approval gate (AskUserQuestion) REQUIRED before the amendment lands -- a frozen constitutional set is moving.
5. **The any-transcript trigger invariant**: every transcript that lands (via /mos:file-meeting, Velma, or paste-classified-as-transcript) fires the DIKW ladder -- Data (segments) -> Information (typed claims) -> Knowledge (claims with conditions, typed edges) -> Wisdom surface (/mos:build-knowledge reads the typed graph; the existing Ackoff command becomes the render). Acceptance: filing a fixture transcript produces typed claims queryable by knowledge_type, and /mos:build-knowledge surfaces them by DIKW rung.
6. **GATE-0 consumption**: 150.6 delivers the segment-level cascade audit verdict. If segment-level firing is broken, the FIRST plan of this phase fixes the type guard before anything builds on it (the proposal's own Section 10 rule).

## Out of scope (stays in SEED-023, v1.14.0)

Insight node layer + promotion flow, ACTA probe pass in /mos:reanalyze, contrastive pair detection, speaker expertise profiling, live CDM probe injection, causal FEYNMAN timeline, graph reification beyond the additive columns, the 3 deferred edge types.

## Canon fences (from SEED-023, non-negotiable)

- Part 8: meeting content NEVER egresses; knowledge_type enums + framework handles only in Brain packets (Phase 110 wire enforces shape).
- Part 9: typed claims are truth-claim nodes -> proposed until human confirm; `ambiguous` queue is a Decision Gate surface.
- Part 7: lands on /mos:file-meeting + /mos:build-knowledge + navigation.cjs; no new command surface.
- Frozen 148 contracts untouched (MAX_K=3, 0.70/0.15, DIAL_REACH_K=6).

## Evidence and references

- Verbatim proposal + integration annotations: `.planning/research/2026-06-11-meeting-micro-knowledge-dikw-proposal.md`
- SEED-023 (now PARTIALLY PROMOTED: v1 slice -> this phase; remainder dormant for v1.14.0)
- Verified facts: cascade routes meetings/ (intelligence-cascade.cjs:196-198); ALLOWED_EDGE_TYPES frozen behind edges.cjs; knowledge_type net-new.
