---
phase: 150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-
plan: 02
type: execute
wave: 2
depends_on: ["150.8-01"]
files_modified:
  - lib/core/navigation/edges.cjs
  - tests/test-edges-refines-rootcauses-instantiates-floor.cjs
  - tests/claim-harness/claim-c3.cjs
  - docs/MINDRIAN-CANON.md
  - docs/CANON-PHASE-MAP.md
  - tests/run-all-150.8.sh
autonomous: false
requirements: [DIKW-04, DIKW-03]
must_haves:
  truths:
    - "A navigator approves the closed-set change via AskUserQuestion BEFORE any edge-taxonomy byte changes"
    - "REFINES, ROOT_CAUSES, INSTANTIATES are in ALLOWED_EDGE_TYPES; every prior FLOOR member is preserved; the Set stays frozen"
    - "writeEdge accepts the 3 new types and rejects a made-up type; valid_from/valid_until ride the existing edge properties JSON"
    - "The amendment lands as ONE atomic wave: edges.cjs + floor test + C3 fence + canon docs together, so CI never goes RED mid-phase"
  artifacts:
    - path: "tests/test-edges-refines-rootcauses-instantiates-floor.cjs"
      provides: "the canonical FLOOR test for the 3 new members + TV-01 edge round-trip"
      min_lines: 50
    - path: "docs/MINDRIAN-CANON.md"
      provides: "Appendix D entry 18 + Part 4 edge-list update + header version bump 1.6 -> 1.7"
      contains: "REFINES"
    - path: "docs/CANON-PHASE-MAP.md"
      provides: "Phase 150.8 row + Version-history entry"
      contains: "150.8"
  key_links:
    - from: "tests/test-edges-refines-rootcauses-instantiates-floor.cjs"
      to: "lib/core/navigation/edges.cjs"
      via: "imports ALLOWED_EDGE_TYPES + writeEdge"
      pattern: "ALLOWED_EDGE_TYPES"
    - from: "docs/CANON-PHASE-MAP.md"
      to: "docs/MINDRIAN-CANON.md"
      via: "version-history row references Appendix D entry 18"
      pattern: "1\\.7"
---

<objective>
Move a frozen constitutional set. Add REFINES + ROOT_CAUSES + INSTANTIATES to ALLOWED_EDGE_TYPES (DIKW-04) and confirm valid_from/valid_until ride the existing edge properties JSON (DIKW-03, edge half) - but ONLY after a navigator approves the closed-set change via a blocking AskUserQuestion gate, then land it as ONE atomic lockstep wave (edges.cjs + floor test + C3 fence + canon docs) so CI never goes RED mid-phase.

Purpose: Per CONTEXT item 4 and navigator decision 3 (pre-resolved 2026-06-11): code-wise the addition is additive and trivial (RESEARCH finding 3: every prior edge type landed additively without canon amendment). The load-bearing requirement is constitutional discipline - a frozen 148-era taxonomy is moving, so the navigator must approve BEFORE the bytes change, mirroring the Phase 148 Appendix D entry 15 procedure. This is the canon-amendment-on-itself mechanism (Part 6 dog-fooding) applied once more.

Output: the navigator-approved 3-member additive block, the canonical FLOOR test, the C3 fence extension, the canon Appendix D entry 18 + Part 4 edge list + version bump, and the CANON-PHASE-MAP row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-CONTEXT.md
@.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-RESEARCH.md
@.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-PATTERNS.md
@docs/MINDRIAN-CANON.md
@docs/CANON-PHASE-MAP.md
</context>

<artifacts_this_phase_produces>
- New edge types `REFINES`, `ROOT_CAUSES`, `INSTANTIATES` in `lib/core/navigation/edges.cjs` ALLOWED_EDGE_TYPES
- `tests/test-edges-refines-rootcauses-instantiates-floor.cjs` - the canonical FLOOR test
- Canon Appendix D entry 18 + Part 4 edge-list update + header version 1.7 (docs/MINDRIAN-CANON.md)
- Phase 150.8 row + Version-history entry (docs/CANON-PHASE-MAP.md)
- Extended `tests/claim-harness/claim-c3.cjs` (REFINES edge mints on the fixture)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: NAVIGATOR GATE - approve the frozen-set amendment (DIKW-04)</name>
  <decision>Approve adding REFINES + ROOT_CAUSES + INSTANTIATES to the frozen ALLOWED_EDGE_TYPES closed set (a Canon Part 4 + Phase 108 frozen-taxonomy amendment), and approve bumping the Canon header version 1.6 -> 1.7. The 3 deferred types (GENERALIZES / CONTRADICTS_CONDITIONALLY / SUPERSEDES) stay OUT (SEED-023 v1.14.0).</decision>
  <context>
    A frozen constitutional set is moving. Per CONTEXT item 4 and navigator decision 3, the closed-set change requires explicit navigator approval BEFORE the bytes land - this is the only constitutional discipline that distinguishes this from the routine additive-without-amendment idiom. Code-wise the change is additive and reversible (RESEARCH finding 3). The semantics: REFINES = a new claim tightens/conditions a prior claim without invalidating it (the navigator-flagged-important one: today the schema forces a wrong choice between INFORMS-too-weak and CONTRADICTS-wrong); ROOT_CAUSES = directional cause->effect; INSTANTIATES = a concrete example evidencing an abstract claim. Mirrors the Phase 148 Appendix D entry 15 amendment procedure exactly.
  </context>
  <options>
    <option id="approve">
      <name>Approve the 3-member amendment + version bump 1.6 -> 1.7</name>
      <pros>Unblocks causal + refinement structure as first-class graph; matches CONTEXT scope exactly; reversible</pros>
      <cons>Moves a frozen constitutional count (the point of the gate)</cons>
    </option>
    <option id="approve-no-bump">
      <name>Approve the 3 types but treat as non-constitutional (no version bump)</name>
      <pros>Lighter; consistent with the additive-without-amendment code idiom</pros>
      <cons>CONTEXT explicitly frames this as a frozen-set move requiring the amendment mechanism (RESEARCH A5); under-records the change</cons>
    </option>
    <option id="defer">
      <name>Defer the edge amendment to v1.14.0</name>
      <pros>No constitutional movement now</pros>
      <cons>Breaks DIKW-04; the Knowledge rung loses REFINES/ROOT_CAUSES/INSTANTIATES; ROOT_CAUSES queryability lost</cons>
    </option>
  </options>
  <action>Present the three options to the navigator via AskUserQuestion (a blocking gate). Do NOT change any edge-taxonomy or canon byte until a selection is returned. Record the selection (and any reason) in the plan SUMMARY for traceability. On "approve" or "approve-no-bump", proceed to Task 2 with the version-bump behavior set by the choice. On "defer", skip Tasks 2 and 3 and record the deferral.</action>
  <resume-signal>Select: approve, approve-no-bump, or defer. On "approve" (recommended), Task 2 lands the atomic wave. On "defer", skip Tasks 2-3 and record the deferral in the SUMMARY.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomic lockstep wave - edges.cjs block + FLOOR test + C3 fence (DIKW-04, DIKW-03)</name>
  <files>lib/core/navigation/edges.cjs, tests/test-edges-refines-rootcauses-instantiates-floor.cjs, tests/claim-harness/claim-c3.cjs, tests/run-all-150.8.sh</files>
  <read_first>
    - lib/core/navigation/edges.cjs:244-278 - the Phase 150-01 STATES/SUPPORTS/DESCRIBES additive block (the verbatim idiom to copy) ending the frozen Set at :278
    - lib/core/navigation/edges.cjs:306-321 - the writeEdge gate (invalid_edge_type) + the properties-JSON serialization (valid_from/valid_until ride here with zero change)
    - tests/test-edges-affiliated-with-floor.cjs:32-75 - the canonical 4-check FLOOR-test skeleton (membership / FLOOR preserved / frozen Set / live write); NEVER asserts .size
    - tests/claim-harness/claim-c3.cjs (full) - the contradiction fence; it does NOT enumerate the closed set today (RESEARCH Pitfall 3)
    - 150.8-PATTERNS.md section 2 - the full delta spec (comment block contents, the navigator-gate line, the TV-01 edge round-trip in check 4)
  </read_first>
  <behavior>
    - Test: ALLOWED_EDGE_TYPES.has('REFINES') && .has('ROOT_CAUSES') && .has('INSTANTIATES') all true
    - Test: every prior FLOOR member (DEFERRED, REJECTED, DERIVED_FROM, FILED_AS_DECISION, FOLLOWS_FROM, OPERATOR_TRANSITION, INFORMS, REJECTED_BECAUSE, CONTRADICTS, SUPERSEDES, plus the PIVOTED/SELECTED_REACH/FEEDS_INTO/VALIDATES/STATES/SUPPORTS/DESCRIBES additions through 150-01) is STILL present - additive, never delete; NEVER assert an exact .size
    - Test: ALLOWED_EDGE_TYPES is a frozen Set instance (Object.isFrozen true)
    - Test: writeEdge on a bare in-memory edges table accepts a REFINES edge with properties { reason:'tightens_conditions', valid_from:'2026-06-11', valid_until:'' } -> ok:true, and the properties round-trip (DIKW-03 edge half / TV-01)
    - Test: writeEdge with a made-up type returns { ok:false, reason:'invalid_edge_type' }
    - Test (C3 extension): the claim-harness fixture mints a REFINES edge through the chokepoint (closed-set membership exercised on the real fixture)
  </behavior>
  <action>
    Land ALL of the following in one wave (the atomic lockstep; do not commit a partial set):

    1. lib/core/navigation/edges.cjs: append a Phase 150.8 additive block after 'DESCRIBES' (before the closing `]));` at :278) adding 'REFINES', 'ROOT_CAUSES', 'INSTANTIATES'. The comment block MUST carry: the Canon Part 4 line (every choice is graph data), the Part 8 ENUM/scalar-only-props line (the claim/segment BODY never lands on the edge; valid_from/valid_until are scalars), the "mirrors the Phase 150-01 STATES/SUPPORTS/DESCRIBES additive idiom verbatim" lineage line, the per-edge semantics (REFINES = tightens/conditions a prior claim; ROOT_CAUSES = directional cause->effect; INSTANTIATES = concrete example evidencing an abstract claim), AND the unique navigator-gate line: "NAVIGATOR-GATED Canon Part 4 + Phase 108 amendment (AskUserQuestion approval, 2026-06-11)". writeEdge needs ZERO change - valid_from/valid_until ride the existing properties JSON param (edges.cjs:309-321 already serializes arbitrary properties).

    2. tests/test-edges-refines-rootcauses-instantiates-floor.cjs: copy the 4-check skeleton from test-edges-affiliated-with-floor.cjs. Check 1: all 3 new members present. Check 2: the FULL FLOOR (extend the FLOOR array with every member through STATES/SUPPORTS/DESCRIBES + PIVOTED/SELECTED_REACH/FEEDS_INTO/VALIDATES) preserved. Check 3: frozen Set instance. Check 4: writeEdge accepts a REFINES edge carrying valid_from/valid_until and they round-trip (covers TV-01 on edges). Add a negative check: a made-up type returns invalid_edge_type. NEVER assert .size (Pitfall 3).

    3. tests/claim-harness/claim-c3.cjs: extend (do not rewrite the existing CONTRADICTS assertions) to mint a REFINES edge on the fixture and assert ok:true - exercises the new closed-set member on the real claim-harness substrate.

    4. tests/run-all-150.8.sh: add the new floor test to the aggregator.

    NO em-dashes in any emitted file. Run claim-harness/run-all-claims.sh after to confirm the C3 extension is green.
  </action>
  <verify>
    <automated>node tests/test-edges-refines-rootcauses-instantiates-floor.cjs && bash tests/claim-harness/run-all-claims.sh && bash tests/run-all-150.8.sh</automated>
  </verify>
  <done>The 3 new edge types are in the frozen Set with all prior members preserved; the floor test + the made-up-type negative + the TV-01 edge round-trip pass; claim-c3 mints a REFINES edge; run-all-claims.sh and run-all-150.8.sh green.</done>
</task>

<task type="auto">
  <name>Task 3: Canon docs lockstep - Appendix D entry 18 + Part 4 + CANON-PHASE-MAP row (DIKW-04)</name>
  <files>docs/MINDRIAN-CANON.md, docs/CANON-PHASE-MAP.md</files>
  <read_first>
    - docs/MINDRIAN-CANON.md Appendix D entry 15 - the Phase 148 amendment entry (the exact prose template to mirror: frozen-set move, navigator-LOCKED decision, canon-amendment-on-itself mechanism)
    - docs/MINDRIAN-CANON.md header "Version: 1.6" line + Part 4 (Every Choice Is Graph Data) - where the edge-type vocabulary is referenced
    - docs/CANON-PHASE-MAP.md Part 4 section + the "v1.13.1 Larry Reaches" section (where Phase 150.5/150.6 rows live) + the Version history table tail
    - 150.8-RESEARCH.md lines 290-298 - the Phase 148 Appendix D lockstep template (the 6-step mirror)
  </read_first>
  <action>
    Mirror the Phase 148 Appendix D entry 15 procedure exactly:

    1. docs/MINDRIAN-CANON.md: add Appendix D entry 18 (next number after entry 17) in the entry-15 voice - record the REFINES/ROOT_CAUSES/INSTANTIATES amendment as a navigator-approved frozen-set move applied via the Part 6 dog-fooding canon-amendment-on-itself mechanism (mirroring entries 14/15), naming the AskUserQuestion approval from Task 1, the 3 deferred types staying out, and the lockstep surfaces moved together (edges.cjs + floor test + C3 fence). Update the Part 4 edge-type list to include the 3 new types. Bump the header "Version:" line 1.6 -> 1.7 (per the Task-1 approve decision; if the navigator chose approve-no-bump, leave at 1.6 and note it in the entry).

    2. docs/CANON-PHASE-MAP.md: add a Phase 150.8 row under the "v1.13.1 Larry Reaches connector spine + engine flip" section (alongside 150.5/150.6) with canon_parts [4, 7, 8, 9]; add a Version-history table entry recording the 1.6 -> 1.7 bump and the edge amendment, referencing Appendix D entry 18.

    NO em-dashes in any emitted text (hyphens only). Keep all existing canon entries byte-intact except the targeted Part 4 list + version line.
  </action>
  <verify>
    <automated>grep -q "REFINES" docs/MINDRIAN-CANON.md && grep -q "ROOT_CAUSES" docs/MINDRIAN-CANON.md && grep -q "150.8" docs/CANON-PHASE-MAP.md && ! grep -P '[\x{2014}\x{2013}]' docs/MINDRIAN-CANON.md docs/CANON-PHASE-MAP.md</automated>
  </verify>
  <done>Appendix D entry 18 records the amendment; Part 4 lists the 3 new edge types; header version reflects the Task-1 decision; CANON-PHASE-MAP carries the Phase 150.8 row + version-history entry; zero em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator approval -> frozen-set change | The constitutional gate; no bytes change before approval |
| edge properties JSON -> Brain (downstream) | valid_from/valid_until are scalars; the claim BODY never lands on the edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.8-05 | Tampering | edge-type vocabulary expansion without authority | mitigate | Blocking AskUserQuestion navigator gate (Task 1) BEFORE any edge bytes change; atomic lockstep so no partial unapproved state ships |
| T-150.8-06 | Tampering | writeEdge accepting an arbitrary/poisoned edge type | mitigate | ALLOWED_EDGE_TYPES frozen Set gate (invalid_edge_type); floor test asserts the closed set + a made-up-type negative |
| T-150.8-07 | Information Disclosure | claim/segment prose smuggled onto an edge property | mitigate | Part 8 enum/scalar-only edge props doctrine in the comment block; valid_from/valid_until are scalars only; no body field on edges |
| T-150.8-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this plan; docs + CJS only |
</threat_model>

<verification>
- Task 1 navigator gate cleared (blocking) BEFORE Task 2 runs
- `node tests/test-edges-refines-rootcauses-instantiates-floor.cjs` - closed set + FLOOR + frozen + TV-01 edge round-trip + negative
- `bash tests/claim-harness/run-all-claims.sh` - C3 REFINES extension green
- `bash tests/run-all-150.8.sh` - phase gate green
- Canon docs grep checks pass; zero em-dashes across canon docs
</verification>

<success_criteria>
- Navigator approved the frozen-set change via blocking AskUserQuestion before any edge bytes changed
- REFINES/ROOT_CAUSES/INSTANTIATES in ALLOWED_EDGE_TYPES; all prior members preserved; Set frozen; .size never hardcoded
- valid_from/valid_until ride the existing edge properties JSON (DIKW-03 edge half) with zero writeEdge change
- The amendment landed atomically (code + tests + canon docs in one wave); CI never RED mid-phase
- Canon version + Appendix D entry 18 + CANON-PHASE-MAP row recorded per the Phase 148 template
</success_criteria>

<output>
Create `.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-02-SUMMARY.md` when done.
</output>
