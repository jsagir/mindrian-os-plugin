---
phase: 150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-
plan: 04
type: execute
wave: 4
depends_on: ["150.8-01", "150.8-02", "150.8-03"]
files_modified:
  - commands/file-meeting.md
  - commands/build-knowledge.md
  - lib/hmi/cortex-reach-adapter.cjs
  - lib/core/navigation/room-context.cjs
  - tests/test-post-filing-selector.cjs
  - tests/test-build-knowledge-dikw-render.cjs
  - tests/test-confirm-claim-flow.cjs
  - tests/test-cortex-reach-claim-branch.cjs
  - tests/test-part8-poison-transcript.cjs
  - tests/claim-harness/claim-c5.cjs
  - tests/run-all-150.8.sh
autonomous: true
requirements: [DIKW-07, DIKW-08, DIKW-09, DIKW-10]
must_haves:
  truths:
    - "The post-filing F.1 selector renders the three ladder verbs (Review ambiguous / Confirm proposed claims / Build knowledge) with the AskUserQuestion trailer; frozen 148 constants unchanged"
    - "The 'Confirm proposed claims' verb routes per-claim APPROVE through the shipped confirmNode chokepoint with byUser resolved from USER.md (Part 9 Knowledge rung confirmable)"
    - "A fresh typed claim lifts reach scores via an additive cortex-reach-adapter claim branch; frozen DIAL_REACH_K/MAX_K/0.70/0.15 untouched"
    - "/mos:build-knowledge reads typed claims by knowledge_type and renders them by DIKW rung"
    - "A poison-transcript fixture proves no transcript prose reaches any Brain packet (Part 8 adversarial)"
  artifacts:
    - path: "commands/build-knowledge.md"
      provides: "graph reader by knowledge_type + DIKW-rung render"
      contains: "knowledge_type"
    - path: "lib/hmi/cortex-reach-adapter.cjs"
      provides: "additive 'claim' branch in CORTEX_NODE_TYPES + CONTRIBUTIONS"
      contains: "claim"
    - path: "tests/run-all-150.8.sh"
      provides: "the complete phase gate over all 4 plans"
  key_links:
    - from: "commands/build-knowledge.md"
      to: "room.db nodes (json_extract knowledge_type)"
      via: "SELECT ... json_extract(properties,'$.knowledge_type') WHERE type='claim'"
      pattern: "json_extract"
    - from: "post-filing F.1 selector"
      to: "lib/core/navigation/confirm-node.cjs confirmNode"
      via: "Confirm proposed claims verb routes APPROVE through navigation.confirmNode"
      pattern: "confirmNode"
    - from: "lib/hmi/cortex-reach-adapter.cjs"
      to: "claim node knowledge_type"
      via: "additive claim branch reading _propEnum(node,'knowledge_type')"
      pattern: "knowledge_type"
---

<objective>
Close the loop: surface the post-filing F.1 selector with the three ladder verbs (DIKW-07), wire the "Confirm proposed claims" verb through the shipped confirmNode chokepoint (DIKW-08, navigator decision 1), add the additive cortex-reach-adapter claim branch so fresh typed claims lift reach scores (DIKW-09, navigator decision 2), make /mos:build-knowledge read the typed graph by DIKW rung (DIKW-10), and prove the Part 8 boundary with a poison-transcript fixture.

Purpose: A write-only ladder with no visible next-move is the exact failure class this milestone keeps finding (RESEARCH Pitfall 5). The selector element is load-bearing. The Knowledge rung must be confirmable (navigator decision 1) and fresh claims must lift the dial (navigator decision 2) - both ride shipped chokepoints (confirmNode, the adapter's existing node-type branches) with frozen constants UNTOUCHED. The Wisdom render turns the existing Ackoff command into a graph reader. The Part 8 adversarial leg proves meeting prose never egresses.

Output: the F.1 post-filing selector wiring, the confirm-claim dispatch, the adapter claim branch, the build-knowledge graph reader, the run-all-150.8 aggregator completion with the regression fences + Part 8 sweep, and the claim-c5 extension.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-CONTEXT.md
@.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-RESEARCH.md
@.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-PATTERNS.md
@commands/build-knowledge.md
</context>

<artifacts_this_phase_produces>
- `commands/build-knowledge.md` graph-reader section (SELECT by json_extract knowledge_type -> DIKW rungs)
- Additive `claim` branch in `lib/hmi/cortex-reach-adapter.cjs` (+ `lib/core/navigation/room-context.cjs` CORTEX_NODE_TYPES mirror) with a `claim_present` weight
- The post-filing F.1 selector wiring (in commands/file-meeting.md flow via selector-dispatcher) with the 3 ladder verbs
- The "Confirm proposed claims" -> confirmNode dispatch
- `tests/test-post-filing-selector.cjs`, `tests/test-build-knowledge-dikw-render.cjs`, `tests/test-confirm-claim-flow.cjs`, `tests/test-cortex-reach-claim-branch.cjs`, `tests/test-part8-poison-transcript.cjs`
- Extended `tests/claim-harness/claim-c5.cjs` (K segments -> >= K typed claim nodes)
- `tests/run-all-150.8.sh` completed with regression fences
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Post-filing F.1 selector + Confirm-proposed-claims via confirmNode + cortex-reach claim branch (DIKW-07, DIKW-08, DIKW-09)</name>
  <files>lib/hmi/cortex-reach-adapter.cjs, lib/core/navigation/room-context.cjs, tests/test-post-filing-selector.cjs, tests/test-confirm-claim-flow.cjs, tests/test-cortex-reach-claim-branch.cjs, tests/run-all-150.8.sh</files>
  <read_first>
    - lib/hmi/selector-dispatcher.cjs:524-544,669-693,732-742,884-922 - pickShape F.1 caller payload (verbs/header), appendAskUserQuestionTrailer auto-attach, F.7 mandatory-Dismiss precedent, archetype routing
    - lib/core/navigation/confirm-node.cjs:38-44,54-71 - confirmNode(db,id,byUser,reason) + resolveByUser (USER.md identity, NAVIGATOR_DEFAULT, agent-identity coercion); re-exported at navigation.cjs:201
    - lib/hmi/cortex-reach-adapter.cjs:62-89,185-209 - CORTEX_NODE_TYPES list + CONTRIBUTIONS table + the fold loop (the additive-branch site); the 0.70-floor doctrine at :70-73 (a single signal must stay below the recommend floor)
    - lib/core/navigation/room-context.cjs CORTEX_NODE_TYPES - mirrors the adapter's list (must add 'claim' to BOTH)
    - 150.8-PATTERNS.md sections 4, 5, 6 - the confirmNode caller contract, the adapter claim-branch delta, the F.1 selector payload + trailer assertion
    - lib/hmi/dial-reach-orchestrator.cjs:56,60,61,74 - the FROZEN DIAL_REACH_K=6 / RECOMMEND_FLOOR=0.70 / MARGIN_THRESHOLD=0.15 (MUST NOT move)
  </read_first>
  <behavior>
    - Test (SEL-01): pickShape requestedShape 'F.1' with payload.verbs ['Review ambiguous segments (M)', 'Confirm proposed claims (N)', 'Build knowledge from this meeting'] renders with rendered.askuserquestion_marker === '[AskUserQuestion contract: shape=F.1 verbs=4]' (the 3 verbs + Free-Text appended last)
    - Test (SEL-01): the frozen 148 constants are unchanged - assert DIAL_REACH_K===6, RECOMMEND_FLOOR===0.70, MARGIN_THRESHOLD===0.15, MAX_K===3 (carried run-all-148.sh stays 18/18)
    - Test (DIKW-08): the Confirm-proposed-claims path routes a proposed claim node through navigation.confirmNode(db, id, resolveByUser(roomDir)) -> review_status flips proposed -> confirmed; an agent-literal byUser (e.g. 'larry') is coerced to NAVIGATOR_DEFAULT or rejected (agent_attribution_forbidden) per Part 9 role 5
    - Test (DIKW-09): a fresh claim node lifts reach scores via the adapter claim branch - a single claim_present signal stays BELOW the 0.70 recommend floor solo (doctrine); CORTEX_NODE_TYPES in both the adapter and room-context.cjs include 'claim'
    - Test (DIKW-09): the adapter reads ONLY _propEnum(node,'knowledge_type') (presence + enum), never prose (Part 8)
  </behavior>
  <action>
    Three additive wirings, all riding shipped chokepoints with frozen constants untouched:

    1. Post-filing F.1 selector (DIKW-07): in the commands/file-meeting.md post-filing flow, call selector-dispatcher.pickShape({ requestedShape:'F.1', tier, mode, roomDir, payload:{ verbs:['Review ambiguous segments (M)','Confirm proposed claims (N)','Build knowledge from this meeting'], header:'...', emitTelemetry:true } }). The dispatcher auto-attaches the trailer + archetype (:884-922). Counts M (ambiguous queue size) and N (proposed claim count) come from the room.db scans. The renderer appends Free-Text last (ensureFreeTextLast) -> 4 verbs total.

    2. Confirm-proposed-claims dispatch (DIKW-08): wire the "Confirm proposed claims" verb to route a per-claim APPROVE through navigation.confirmNode(db, claimId, navigation.resolveByUser(roomDir), reason). resolveByUser reads USER.md first; never pass a literal 'larry'/'brain'/'system'/'assistant' (promoteNodeStatus rejects agent_attribution_forbidden). Since 'claim' is already in the Part 9 truth-claim set, confirmNode works unchanged - this is a thin dispatch, not new machinery. (Closes RESEARCH Open Q1 / A6 in favor of wiring it for v1 per navigator decision 1.)

    3. Cortex-reach-adapter claim branch (DIKW-09): add 'claim' to CORTEX_NODE_TYPES in BOTH lib/hmi/cortex-reach-adapter.cjs:62-89 AND lib/core/navigation/room-context.cjs (they mirror each other per the :60 comment). Add a claim_present weight (0.10-class) to CONTRIBUTIONS that stays BELOW 0.70 solo (the :70-73 doctrine). Add one `else if (type === 'claim')` branch in the fold loop reading ONLY _propEnum(node,'knowledge_type') (presence + enum, never prose, Part 8 header :27-33), bumping an appropriate reach. The adapter never imports the orchestrator; DIAL_REACH_K/MAX_K/0.70/0.15 stay where they are - do NOT touch dial-reach-orchestrator.cjs.

    Write tests/test-post-filing-selector.cjs (assert the F.1 marker + frozen constants), tests/test-confirm-claim-flow.cjs (proposed->confirmed via confirmNode + agent-attribution rejection), tests/test-cortex-reach-claim-branch.cjs (claim lifts reach below-floor + enum-only read). Add all to run-all-150.8.sh. NO em-dashes.
  </action>
  <verify>
    <automated>node tests/test-post-filing-selector.cjs && node tests/test-confirm-claim-flow.cjs && node tests/test-cortex-reach-claim-branch.cjs && bash tests/run-all-148.sh</automated>
  </verify>
  <done>F.1 renders the 3 ladder verbs + Free-Text (marker verbs=4); Confirm routes through confirmNode with USER.md byUser and rejects agent attribution; a fresh claim lifts reach below the 0.70 floor via the enum-only adapter branch; run-all-148.sh stays 18/18 (frozen constants intact).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build-knowledge DIKW graph render + claim-c5 completeness extension (DIKW-10)</name>
  <files>commands/build-knowledge.md, tests/test-build-knowledge-dikw-render.cjs, tests/claim-harness/claim-c5.cjs, tests/run-all-150.8.sh</files>
  <read_first>
    - commands/build-knowledge.md (full) - the pure-LLM Ackoff prose today (needs a graph reader)
    - lib/core/navigation/packet.cjs:62 - the type IN (...) query idiom to compose from
    - 150.8-PATTERNS.md "No Analog Found" (lines 536-538) - the exact SELECT (json_extract knowledge_type/conditions) + the rung grouping (fact/anomaly_cue -> Data/Information; causal/heuristic/mental_model -> Knowledge; conditions present -> Wisdom-ready)
    - tests/claim-harness/claim-c5.cjs:53-80 - the K -> >= K completeness invariant + the proposed-mint assertion to extend
    - tests/claim-harness/build-fixture-room-db.cjs - applySchema + the committed fixture to add transcript claims to
  </read_first>
  <behavior>
    - Test (WIS-01): the build-knowledge graph reader returns claims grouped by knowledge_type - SELECT id, json_extract(properties,'$.knowledge_type') AS kt, json_extract(properties,'$.conditions') AS conditions, review_status FROM nodes WHERE type='claim' ORDER BY kt
    - Test (WIS-01): the render groups kt into Ackoff rungs (fact/anomaly_cue -> Data/Information; causal/heuristic/mental_model -> Knowledge; conditions present -> Wisdom-ready) and surfaces them by rung
    - Test (DIKW acceptance, EX-01): a fixture transcript decomposing into K atomic claims produces >= K type='claim' nodes queryable by knowledge_type (claim-c5 completeness idiom: report.upserted >= count)
    - Test: claims minted at proposed (Part 9) on the fixture
  </behavior>
  <action>
    Update commands/build-knowledge.md: add a graph-reader section that calls a thin read through the navigation chokepoint - SELECT id, json_extract(properties,'$.knowledge_type') AS kt, json_extract(properties,'$.conditions') AS conditions, review_status FROM nodes WHERE type='claim' ORDER BY kt - and renders the rows grouped into Ackoff DIKW rungs (fact/anomaly_cue at Data/Information, causal/heuristic/mental_model at Knowledge, claims carrying conditions as Wisdom-ready). Keep the existing Ackoff prose framing (Part 7: the existing Ackoff command BECOMES the graph reader, not a new command). Answer the Part 7 reuse question in a header comment.

    Extend tests/claim-harness/claim-c5.cjs: add fixture transcript claims to the claim-room fixture (build-fixture-room-db.cjs) so K segments -> >= K type='claim' nodes; assert the completeness idiom (report.upserted >= count) and proposed-mint. Write tests/test-build-knowledge-dikw-render.cjs: build an in-memory db with claims across the 6 knowledge_types, call the reader, assert the rung grouping. Add to run-all-150.8.sh. NO em-dashes.
  </action>
  <verify>
    <automated>node tests/test-build-knowledge-dikw-render.cjs && bash tests/claim-harness/run-all-claims.sh && grep -q "json_extract" commands/build-knowledge.md</automated>
  </verify>
  <done>build-knowledge reads typed claims by knowledge_type and renders them by DIKW rung; the claim-c5 completeness extension proves a fixture transcript yields >= K queryable typed claims at proposed; run-all-claims.sh green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Part 8 poison-transcript adversarial leg + complete the phase gate with regression fences (DIKW-07, Part 8)</name>
  <files>tests/test-part8-poison-transcript.cjs, tests/run-all-150.8.sh</files>
  <read_first>
    - lib/core/navigation/packet.cjs:99-110 - the sha256-hash-prose-by-default Part 8 enforcement (excerpts only under the unused allow_excerpts opt-in)
    - 150.8-RESEARCH.md Security Domain (lines 395-417) - the threat table; the group-b Part-8 forbidden-substring sweep idiom
    - lib/core/navigation/packet.cjs:62,150 - the packet build path the sweep exercises
    - tests/run-all-150.5.sh - the carried-fence aggregator pattern (which prior run-all-NNN.sh scripts to invoke)
    - 150.8-PATTERNS.md "Part 8 enum-only properties" shared pattern
  </read_first>
  <behavior>
    - Test (Part 8 adversarial): a poison transcript whose claim text contains a unique sentinel string (e.g. proprietary numbers, a PII-shaped token, a competitor name) is filed -> writeClaimNode mints the claim WITH the sentinel in room.db, but when a Brain packet is built for that claim, the packet carries the knowledge_type ENUM only and the sentinel substring appears NOWHERE in the packet (sha256-hashed / dropped) - assert the sentinel is absent from the serialized packet
    - Test: the packet carries the knowledge_type enum scalar (the safe handle rides)
    - Test: the forbidden-substring sweep fails loudly if any future change lets prose through (regression fence with teeth)
  </behavior>
  <action>
    Write tests/test-part8-poison-transcript.cjs: mint a claim via navigation.writeClaimNode whose text + conditions carry a unique sentinel (a proprietary-number / PII-shaped / competitor-name token). Build a Brain packet for that claim through the packet.cjs path. Assert: (1) the sentinel substring is ABSENT from the JSON-serialized packet (prose hashed/dropped by the sha256 default); (2) the knowledge_type enum scalar IS present (the safe handle). This is the adversarial Part 8 leg the quality gate requires (no transcript prose reaches any Brain packet). Mirror the group-b forbidden-substring sweep idiom.

    Complete tests/run-all-150.8.sh: ensure it runs ALL Wave-0 + later drivers from Plans 01-04, AND invokes the carried regression fences - run-all-148.sh (must be 18/18), run-all-150.sh (green), run-all-150.5.sh (green), claim-harness/run-all-claims.sh (green), and the new floor test. Add the poison-transcript test. The aggregator is the single phase-gate command. Add a final Part 8 sweep step over all new artifacts (grep that no claim prose appears in any packet-bound path). NO em-dashes.
  </action>
  <verify>
    <automated>node tests/test-part8-poison-transcript.cjs && bash tests/run-all-150.8.sh && bash tests/run-all-148.sh && bash tests/run-all-150.sh && bash tests/run-all-150.5.sh</automated>
  </verify>
  <done>The poison-transcript fixture proves no transcript prose reaches any Brain packet (sentinel absent, enum present); run-all-150.8.sh is the complete phase gate invoking all carried regression fences green (148 18/18, 150, 150.5, claim-harness all green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claim node prose -> Brain packet | The dominant Part 8 surface: meeting prose must never egress; only enums/hashes ride |
| selector verb -> confirmNode (truth promotion) | Promotion is a privileged transition; only a human byUser may confirm |
| claim knowledge_type -> reach score | The adapter reads enum presence only, never prose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.8-12 | Information Disclosure | meeting/claim prose egressing to Brain via a packet (Part 8 breach) | mitigate | packet.cjs sha256-hashes prose by default; the poison-transcript adversarial leg asserts a sentinel is absent from the serialized packet; only the knowledge_type enum rides |
| T-150.8-13 | Elevation of Privilege | a non-human confirming a truth-claim via the Confirm verb | mitigate | The Confirm dispatch routes through confirmNode with resolveByUser (USER.md); agent-literal byUser coerced to NAVIGATOR_DEFAULT or rejected (agent_attribution_forbidden); Part 9 role 5 |
| T-150.8-14 | Tampering | knowledge_type enum poisoning influencing reach scores | mitigate | The adapter reads _propEnum(node,'knowledge_type') against presence + enum only; off-enum/prose ignored; a single signal stays below the 0.70 recommend floor |
| T-150.8-15 | Tampering | frozen 148 constant drift (DIAL_REACH_K/MAX_K/0.70/0.15) | mitigate | The adapter never imports the orchestrator; carried run-all-148.sh (18/18) is a regression fence asserting the constants byte-unchanged |
| T-150.8-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this plan; CJS + Markdown only |
</threat_model>

<verification>
- `node tests/test-post-filing-selector.cjs` - F.1 marker + frozen constants
- `node tests/test-confirm-claim-flow.cjs` - proposed->confirmed + agent-attribution rejection
- `node tests/test-cortex-reach-claim-branch.cjs` - claim lifts reach below-floor, enum-only
- `node tests/test-build-knowledge-dikw-render.cjs` - DIKW-rung render
- `node tests/test-part8-poison-transcript.cjs` - sentinel absent from packet
- `bash tests/run-all-150.8.sh` - complete phase gate
- Regression fences: `bash tests/run-all-148.sh` (18/18), `bash tests/run-all-150.sh`, `bash tests/run-all-150.5.sh`, `bash tests/claim-harness/run-all-claims.sh` all green
</verification>

<success_criteria>
- The post-filing F.1 selector renders the 3 ladder verbs + Free-Text (marker verbs=4); frozen 148 constants byte-unchanged
- The Knowledge rung is confirmable: Confirm routes through the shipped confirmNode with USER.md byUser; agent attribution rejected
- Fresh typed claims lift reach scores via the additive enum-only adapter branch; DIAL_REACH_K/MAX_K/0.70/0.15 untouched
- /mos:build-knowledge reads typed claims by knowledge_type and renders by DIKW rung
- DIKW acceptance: a fixture transcript yields typed claims queryable by knowledge_type AND build-knowledge surfaces them by rung AND the F.1 footer renders the three verbs
- The Part 8 poison-transcript leg proves no transcript prose reaches any Brain packet
- run-all-150.8.sh green with all carried regression fences (148 18/18, 150, 150.5, claim-harness)
- Zero em-dashes in every emitted file
</success_criteria>

<output>
Create `.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-04-SUMMARY.md` when done.
</output>
