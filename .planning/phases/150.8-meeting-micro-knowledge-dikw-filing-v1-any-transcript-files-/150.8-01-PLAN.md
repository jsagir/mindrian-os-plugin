---
phase: 150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/typed-claim.cjs
  - lib/core/navigation.cjs
  - lib/core/intelligence-cascade.cjs
  - tests/test-typed-claim-writer.cjs
  - tests/test-temporal-validity.cjs
  - tests/test-segment-aware-filing-guard.cjs
  - tests/run-all-150.8.sh
autonomous: true
requirements: [DIKW-01, DIKW-02, DIKW-03]
must_haves:
  truths:
    - "writeClaimNode mints a type='claim' node with review_status='proposed' (never confirmed) through the navigation chokepoint"
    - "An off-enum knowledge_type is rejected with reason 'invalid_knowledge_type'; the 6 valid types are accepted"
    - "conditions, counter_conditions, valid_from, valid_until round-trip as additive JSON properties with zero DDL change"
    - "The GATE-0 segment-aware filing path is established: extraction drives per-claim writes (claim count tracks segment count, not file count)"
  artifacts:
    - path: "lib/core/navigation/typed-claim.cjs"
      provides: "writeClaimNode + KNOWLEDGE_TYPES frozen Set + CLAIM_NODE_ID id-minter"
      min_lines: 60
    - path: "lib/core/navigation.cjs"
      provides: "additive re-export of writeClaimNode / KNOWLEDGE_TYPES / CLAIM_NODE_ID"
      contains: "writeClaimNode"
    - path: "tests/run-all-150.8.sh"
      provides: "the one-command phase gate (mirror run-all-150.5.sh)"
  key_links:
    - from: "lib/core/navigation.cjs"
      to: "lib/core/navigation/typed-claim.cjs"
      via: "require + additive re-export"
      pattern: "writeClaimNode"
    - from: "tests/test-typed-claim-writer.cjs"
      to: "lib/core/navigation.cjs"
      via: "calls navigation.writeClaimNode on an in-memory db"
      pattern: "navigation\\.writeClaimNode"
---

<objective>
Mint the net-new typed-claim substrate the entire DIKW ladder stands on: `writeClaimNode` (the truth-claim node writer), the 6-value `knowledge_type` enum + `conditions`/`counter_conditions`/`valid_from`/`valid_until` additive JSON properties (DIKW-01, DIKW-03), and the GATE-0 segment-aware filing decision (DIKW-02) consuming the FILE-LEVEL-ONLY verdict. Also stand up `tests/run-all-150.8.sh`, the phase gate every later plan extends.

Purpose: There is NO `claim`-type writer in the codebase today; meeting filing writes `.md` artifacts only, never graph claims. The GATE-0 verdict (150.6-GATE-0-CASCADE-VERDICT.md, line 100) is FILE-LEVEL ONLY: the cascade fires once per file, never per segment. Per the proposal's Section 10 rule and CONTEXT item 6, this FIRST plan resolves the segmentation seam (extraction drives segmentation; option (a) of RESEARCH Pitfall 1) BEFORE anything builds the extraction on top of it. Everything downstream (extraction, edges, selector, wisdom render) consumes this writer.

Output: `lib/core/navigation/typed-claim.cjs`, its re-export on the closed navigation surface, the segment-aware filing guard in the cascade, three Wave-0 test drivers, and the phase-gate aggregator.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-CONTEXT.md
@.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-RESEARCH.md
@.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-PATTERNS.md
@.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-GATE-0-CASCADE-VERDICT.md
</context>

<artifacts_this_phase_produces>
This plan introduces these NEW symbols (downstream plans depend on them):
- `writeClaimNode(db, params)` - the truth-claim node writer (lib/core/navigation/typed-claim.cjs), re-exported as `navigation.writeClaimNode`
- `KNOWLEDGE_TYPES` - frozen Set of the 6 enum members, re-exported as `navigation.KNOWLEDGE_TYPES`
- `CLAIM_NODE_ID(sessionId, segmentKey)` - the idempotent id-minter, re-exported as `navigation.CLAIM_NODE_ID`
- `tests/test-typed-claim-writer.cjs`, `tests/test-temporal-validity.cjs`, `tests/test-segment-aware-filing-guard.cjs`
- `tests/run-all-150.8.sh` - the phase gate (extended by Plans 02/03/04)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: writeClaimNode truth-claim writer + closed-surface re-export (DIKW-01, DIKW-03)</name>
  <files>lib/core/navigation/typed-claim.cjs, lib/core/navigation.cjs, tests/test-typed-claim-writer.cjs, tests/test-temporal-validity.cjs</files>
  <read_first>
    - lib/core/navigation/evidence-claim.cjs:48,65-130 - the writeEvidenceClaim precedent to mirror (enum gate, additive JSON props, id-mint hash loop, proposed-mint UPSERT)
    - lib/core/navigation/memory-artifacts.cjs:340-388 - writeDecisionNode no-downgrade ON CONFLICT (DO UPDATE excludes review_status)
    - lib/core/navigation.cjs:122-148,191-202 - the additive re-export comment-block idiom (the Phase 150-01 block is the freshest template)
    - lib/core/migrations/phase-109-nodes-provenance.cjs:296,300-301 - properties is TEXT DEFAULT '{}' (additive keys legal); review_status is the ONLY CHECK-constrained column (knowledge_type must stay OUT of DDL)
    - tests/claim-harness/build-fixture-room-db.cjs:45-53,58-75 - module.exports.applySchema (importable in-memory schema) + the SKIP-77 node:sqlite probe
    - 150.8-PATTERNS.md section 1 - the full delta spec for typed-claim.cjs
  </read_first>
  <behavior>
    - Test: writeClaimNode with knowledge_type='fact' on an in-memory db mints a node with type='claim', created_by='system', review_status='proposed' - assert review_status === 'proposed' (NEVER 'confirmed')
    - Test: each of the 6 enum members {fact, causal, heuristic, anomaly_cue, mental_model, assumption} is accepted (ok:true)
    - Test: knowledge_type='root_cause' (a plausible-but-off-enum value) returns { ok:false, reason:'invalid_knowledge_type' } with a .slice-bounded detail
    - Test: non-object params returns { ok:false, reason:'invalid_params' }
    - Test (temporal, DIKW-03): writeClaimNode with conditions/counter_conditions/valid_from/valid_until set -> json_extract(properties,'$.conditions') etc. round-trip the exact strings; absent -> empty string default
    - Test: re-filing the same (sessionId, segment) is idempotent (same node id, ON CONFLICT updates properties, review_status NOT downgraded if already confirmed)
    - Test: KNOWLEDGE_TYPES is a frozen Set instance with exactly the 6 members; navigation.writeClaimNode === typed-claim.writeClaimNode (re-export identity)
  </behavior>
  <action>
    Create lib/core/navigation/typed-claim.cjs mirroring evidence-claim.cjs verbatim in shape. Open with 'use strict' and the header doctrine block (cite Canon Parts 8 + 9, the "NO em-dashes" line, the caller-owned-db-handle contract: this module NEVER requires node:sqlite or room-db.cjs). Define KNOWLEDGE_TYPES = Object.freeze(new Set(['fact','causal','heuristic','anomaly_cue','mental_model','assumption'])). writeClaimNode(db, params): isPlainObject guard first (return reason 'invalid_params'); validate knowledge_type is a string in KNOWLEDGE_TYPES (return reason 'invalid_knowledge_type' with detail String(knowledge_type).slice(0,40)); validate text is a non-empty string. Build props as additive JSON keys ONLY: { knowledge_type, conditions: conditions||'', counter_conditions: counter_conditions||'', valid_from: valid_from||'', valid_until: valid_until||'', source_speaker: sourceSpeaker||'', source_segment: sourceSegment||'' }; include an optional disambiguation key ONLY when params.disambiguation is a non-empty string (the AMB-01 marker consumed by Plan 03 - additive, never a column). JSON.stringify the props inside try/catch (reason 'properties_serialize_failed'). Mint the node id via a CLAIM_NODE_ID(sessionId, segmentKey) helper using the 31-multiplier hash loop from evidence-claim.cjs:110-117: 'claim:' + sid + ':' + hash.toString(16), where segmentKey = sourceSegment || text. INSERT INTO nodes (id, type, properties, source_path, created_by, confidence, review_status, created_at, last_seen_at) VALUES (?, 'claim', ?, ?, 'system', 1.0, 'proposed', ?, ?) ON CONFLICT(id) DO UPDATE SET properties = excluded.properties, last_seen_at = excluded.last_seen_at - the DO UPDATE clause MUST exclude review_status (no-downgrade; copy memory-artifacts.cjs:373-388 verbatim). Wrap the write in try/catch returning reason 'claim_write_failed' with .slice(0,80) detail. Return { ok:true, node_id, knowledge_type }. module.exports = { writeClaimNode, KNOWLEDGE_TYPES, CLAIM_NODE_ID }.

    In lib/core/navigation.cjs add an additive re-export block copying the navigation.cjs:122-148 comment-block idiom verbatim: require('./navigation/typed-claim.cjs'), re-export writeClaimNode / KNOWLEDGE_TYPES / CLAIM_NODE_ID, with the comment naming consumers (file-meeting 4-pass extraction in Plan 03) and the truth-claim sentence: "writeClaimNode is a truth-claim writer: it mints review_status=proposed (NEVER confirmed); only the confirmNode path with a human byUser promotes it (Part 9 role 5)." Do NOT open room.db in the re-export.

    Write tests/test-typed-claim-writer.cjs and tests/test-temporal-validity.cjs as bare node CJS drivers using the check/pass counter skeleton (test-edges-affiliated-with-floor.cjs:23-28) + the SKIP-77 node:sqlite probe (claim-c5.cjs:20-25). Import applySchema from tests/claim-harness/build-fixture-room-db.cjs to build an in-memory db; call navigation.writeClaimNode directly with fixture params standing in for LLM output. NO em-dashes in any emitted file.
  </action>
  <verify>
    <automated>node tests/test-typed-claim-writer.cjs && node tests/test-temporal-validity.cjs</automated>
  </verify>
  <done>Both drivers pass (or SKIP 77 if node:sqlite unavailable). navigation.writeClaimNode mints proposed claims, rejects off-enum knowledge_type, and round-trips conditions/counter_conditions/valid_from/valid_until as JSON properties with zero ALTER TABLE.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GATE-0 segment-aware filing guard + phase-gate aggregator (DIKW-02)</name>
  <files>lib/core/intelligence-cascade.cjs, tests/test-segment-aware-filing-guard.cjs, tests/run-all-150.8.sh</files>
  <read_first>
    - .planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-GATE-0-CASCADE-VERDICT.md:100,119-128 - VERDICT FILE-LEVEL ONLY + the consequence for plan 01 (extraction drives segmentation; do NOT assume a per-segment cascade exists)
    - lib/core/intelligence-cascade.cjs:185-203,219,236-294,450,467 - detectBinarySection meetings/ routing, the per-artifact (NOT per-segment) loop, runCascade signature
    - 150.8-RESEARCH.md Pitfall 1 (lines 235-239) - option (a): the 4-pass extraction drives segmentation and calls writeClaimNode per atomic claim; the cascade stays file-level
    - tests/run-all-150.5.sh:1-25 - the phase-gate aggregator shape to mirror
  </read_first>
  <behavior>
    - Test: a helper/guard exists that, given a meeting artifact, signals that segment-level claim minting is the extraction layer's responsibility (NOT the file-level cascade's) - assert the cascade does NOT silently swallow segments by treating a multi-claim transcript as one claim
    - Test: the segment-aware contract is asserted by instrumentation - filing a fixture transcript that decomposes into K atomic claims produces K writeClaimNode calls (claim count tracks segment count, not file count); the warning-sign from the verdict (claim count == file count) is the failing condition
    - Test: run-all-150.8.sh exists, is executable, and aggregates the Wave-0 drivers green
  </behavior>
  <action>
    Per the GATE-0 verdict and RESEARCH Pitfall 1 option (a), do NOT add a per-segment cascade fan-out to intelligence-cascade.cjs (that fights the LLM-driven design). Instead, formalize the segmentation-authority seam: add a small, clearly-commented guard/helper in lib/core/intelligence-cascade.cjs (near detectBinarySection:185-203) that documents and enforces the contract that meeting transcripts are segmented by the extraction layer (file-meeting.md Step 3, Plan 03), and that each atomic claim is minted via writeClaimNode per segment - the cascade remains FILE-LEVEL for cross-relationship edges. The helper exposes a testable boundary: a function (e.g. assertSegmentAuthorityForMeeting or a documented contract object) the test can call to prove the file-level cascade is NOT the claim-minting path for meeting segments. Keep the change additive and behavior-preserving for all non-meeting artifacts (run-all-150.sh must stay green).

    Write tests/test-segment-aware-filing-guard.cjs: drive a fixture transcript through the extraction-segmentation contract (using a fixture list of K atomic claims, calling navigation.writeClaimNode per claim against an in-memory db) and assert the resulting claim node COUNT equals K (the segment count), NOT 1 (the file count) - this is the instrumentation that proves the GATE-0 consequence is honored. Assert the cascade's file-level pass is untouched (no per-segment cascade events expected).

    Create tests/run-all-150.8.sh mirroring run-all-150.5.sh: a bash aggregator running the Wave-0 drivers (test-typed-claim-writer, test-temporal-validity, test-segment-aware-filing-guard) with a pass/fail tally and non-zero exit on any red; SKIP-77 tolerated. chmod +x. NO em-dashes in any emitted file. Header comment states what the gate PROVES (Canon Part 6 dog-fooding: the ladder can never go green through a file-level-only claim path).
  </action>
  <verify>
    <automated>bash tests/run-all-150.8.sh && bash tests/run-all-150.sh</automated>
  </verify>
  <done>run-all-150.8.sh is green over the Wave-0 drivers; the segment-authority test proves claim count tracks segment count (not file count); run-all-150.sh regression stays green (non-meeting cascade behavior unchanged).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript content -> claim node properties | Untrusted transcript text crosses into JSON properties of a graph node |
| caller -> room.db (via writeClaimNode) | The writer is the chokepoint; direct INSERTs are a bypass |
| claim node -> Brain packet (downstream) | knowledge_type enum may ride to Brain; prose must NOT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.8-01 | Tampering | writeClaimNode props JSON (transcript text smuggling SQL/structure) | mitigate | Parameterized prepared statement (?-bound), JSON.stringify of a fixed-key object; never string-concatenated SQL; text bound as a value never interpolated |
| T-150.8-02 | Tampering | knowledge_type enum poisoning (free text smuggled as a type) | mitigate | Validate against the frozen 6-member KNOWLEDGE_TYPES Set; off-enum -> invalid_knowledge_type with .slice-bounded detail |
| T-150.8-03 | Elevation of Privilege | agent silently confirming a truth-claim | mitigate | writeClaimNode mints review_status='proposed'; ON CONFLICT DO UPDATE excludes review_status (no auto-confirm, no downgrade); promotion only via confirmNode (Plan 04) |
| T-150.8-04 | Tampering | substrate bypass (direct room.db write outside the chokepoint) | mitigate | Module never requires node:sqlite/room-db.cjs; takes a caller-owned handle; the Phase 109 pre-commit substrate guard catches direct requires |
| T-150.8-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this plan (RESEARCH Package Legitimacy Audit: not applicable); no install surface |
</threat_model>

<verification>
- `node tests/test-typed-claim-writer.cjs` - writer + enum gate + proposed-mint
- `node tests/test-temporal-validity.cjs` - conditions/counter_conditions/valid_from/valid_until round-trip
- `node tests/test-segment-aware-filing-guard.cjs` - claim count tracks segment count (GATE-0 consequence honored)
- `bash tests/run-all-150.8.sh` - phase gate green
- `bash tests/run-all-150.sh` - regression: non-meeting cascade unchanged
- `grep -rn "[--]" lib/core/navigation/typed-claim.cjs lib/core/intelligence-cascade.cjs tests/test-typed-claim-writer.cjs tests/test-temporal-validity.cjs tests/test-segment-aware-filing-guard.cjs tests/run-all-150.8.sh | grep -v '^#'` returns zero (no em-dashes)
</verification>

<success_criteria>
- writeClaimNode mints type='claim' / review_status='proposed' through the navigation chokepoint; off-enum knowledge_type rejected
- conditions/counter_conditions/valid_from/valid_until are additive JSON properties (zero ALTER TABLE, zero new migration file)
- The GATE-0 segmentation seam is resolved: extraction is the segmentation authority, proven by the claim-count-tracks-segment-count test
- tests/run-all-150.8.sh is the green one-command phase gate
- run-all-150.sh regression stays green
</success_criteria>

<output>
Create `.planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-01-SUMMARY.md` when done.
</output>
