---
phase: 79-sqlite-migration-cleanup
plan: 04
type: execute
wave: 2
depends_on: [79-01, 79-02]
files_modified:
  - bin/mindrian-tools.cjs
  - scripts/generate-presentation.cjs
  - scripts/extract-room-intelligence.cjs
  - lib/quickview/server.cjs
  - scripts/post-write
  - scripts/pre-compact
  - scripts/on-agent-complete
  - commands/graph.md
  - commands/causal.md
  - commands/query.md
  - commands/scout.md
  - commands/scheduled-tasks.md
  - commands/dashboard.md
autonomous: true
requirements: [SQLITE-08]
must_haves:
  truths:
    - "mindrian-tools.cjs graph subcommands say SQL not Cypher, reference .mindrian not .lazygraph"
    - "All SKIP_DIRS lists say .mindrian instead of .lazygraph"
    - "Command markdown files reference SQL queries not Cypher"
    - "record-decision checks .mindrian/room.db not .lazygraph"
  artifacts:
    - path: "bin/mindrian-tools.cjs"
      provides: "CLI entry point with updated graph subcommands"
      contains: "SQL"
    - path: "commands/graph.md"
      provides: "Graph command docs referencing SQL"
  key_links:
    - from: "bin/mindrian-tools.cjs"
      to: "lib/core/graph-ops.cjs"
      via: "graphOps.buildGraphFromKuzu -> graphOps.buildGraphFromSqlite"
      pattern: "buildGraph"
---

<objective>
Update the CLI, presentation scripts, hook scripts, and command docs to remove all kuzu/.lazygraph references. These are the scattered references across the codebase.

Purpose: Eliminate remaining kuzu references outside core scripts and library files.
Output: 13 files updated, zero kuzu/.lazygraph references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mindrian-tools.cjs and presentation/hook scripts</name>
  <files>bin/mindrian-tools.cjs, scripts/generate-presentation.cjs, scripts/extract-room-intelligence.cjs, lib/quickview/server.cjs, scripts/post-write, scripts/pre-compact, scripts/on-agent-complete</files>
  <read_first>bin/mindrian-tools.cjs, scripts/generate-presentation.cjs, scripts/extract-room-intelligence.cjs, lib/quickview/server.cjs</read_first>
  <action>
**bin/mindrian-tools.cjs:**
1. USAGE string updates:
   - "graph build-kuzu" -> "graph build-sqlite" (line 33)
   - "Build graph.json from KuzuDB (primary)" -> "Build graph.json from SQLite (primary)"
   - "graph query [roomDir] \"<cypher>\"" -> "graph query [roomDir] \"<sql>\""
   - "Execute Cypher query against LazyGraph" -> "Execute SQL query against room graph"
   - "Index single artifact in LazyGraph (KuzuDB)" -> "Index single artifact in room graph"
   - "Rebuild entire LazyGraph" -> "Rebuild entire room graph"
   - "Show LazyGraph node/edge statistics" -> "Show room graph node/edge statistics"
2. graph switch-case (line 172-178):
   - `case 'build-kuzu':` -> `case 'build-sqlite':` (keep 'build-kuzu' as alias for backward compat)
   - `graphOps.buildGraphFromKuzu` -- check if graph-ops.cjs renames this function. If not, keep call name but update comment.
3. record-decision section (line 580-598):
   - Change `const lazygraphDir = require('path').join(require('path').resolve(rdRoom), '.lazygraph');` to `const graphDb = require('path').join(require('path').resolve(rdRoom), '.mindrian', 'room.db');`
   - Change `fs.existsSync(lazygraphDir)` to `fs.existsSync(graphDb)`

**scripts/generate-presentation.cjs:**
1. SKIP_DIRS: replace `.lazygraph` with `.mindrian` (line 57)
2. `collectKuzu` function (line ~317+): rename to `collectGraphData`, change `.lazygraph` dir check to `.mindrian/room.db` check
3. Replace the inline kuzu script generation (line 339 `.tmp-pres-kuzu.js`) with direct lazygraph-ops openGraph/queryGraph calls -- or if the script spawns a child process, update references.
4. Update variable name `kuzu` to `graphData` throughout (line 514, 532, 594-595).

**scripts/extract-room-intelligence.cjs:**
1. SKIP_DIRS: replace `.lazygraph` with `.mindrian` (line 28)

**lib/quickview/server.cjs:**
1. SKIP_DIRS: replace `.lazygraph` with `.mindrian` (line 36)

**scripts/post-write (bash):**
1. Update comment: "hsi-to-kuzu" -> "hsi-to-graph" (line 73)

**scripts/pre-compact (bash):**
1. Update path filter: `*/.lazygraph/*` -> `*/.mindrian/*` (line 54)

**scripts/on-agent-complete (bash):**
1. Update path filter: `*/.lazygraph/*` -> `*/.mindrian/*` (line 53)
  </action>
  <verify>
    <automated>grep -rn 'kuzu\|\.lazygraph' bin/mindrian-tools.cjs scripts/generate-presentation.cjs scripts/extract-room-intelligence.cjs lib/quickview/server.cjs scripts/post-write scripts/pre-compact scripts/on-agent-complete 2>/dev/null | grep -v 'build-kuzu.*alias\|backward' | head -20; echo "---"; grep -c 'kuzu' bin/mindrian-tools.cjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '.lazygraph' bin/mindrian-tools.cjs returns 0
    - grep -c '.lazygraph' scripts/generate-presentation.cjs returns 0
    - grep -c '.lazygraph' scripts/extract-room-intelligence.cjs returns 0
    - grep -c '.lazygraph' lib/quickview/server.cjs returns 0
    - grep -c '.lazygraph' scripts/pre-compact returns 0
    - grep -c '.lazygraph' scripts/on-agent-complete returns 0
  </acceptance_criteria>
  <done>All seven files updated, .lazygraph replaced with .mindrian, kuzu text replaced with sqlite/SQL</done>
</task>

<task type="auto">
  <name>Task 2: Update command markdown files</name>
  <files>commands/graph.md, commands/causal.md, commands/query.md, commands/scout.md, commands/scheduled-tasks.md, commands/dashboard.md</files>
  <read_first>commands/graph.md, commands/causal.md, commands/query.md</read_first>
  <action>
For each command markdown file, do a text replacement pass:

1. **commands/graph.md:** Replace "KuzuDB" with "SQLite room graph", "Cypher" with "SQL", ".lazygraph" with ".mindrian/room.db", "kuzu" with "sqlite"/"room graph". Update any example Cypher queries to SQL equivalents.

2. **commands/causal.md:** Replace "causal-to-kuzu" with "causal-to-graph", update any references to KuzuDB.

3. **commands/query.md:** Replace Cypher query examples with SQL equivalents. Change "Execute Cypher query" to "Execute SQL query". Update example queries:
   - `MATCH (a:Artifact) RETURN a.title` -> `SELECT json_extract(properties, '$.title') AS title FROM nodes WHERE type = 'Artifact'`
   - `MATCH (a)-[r:INFORMS]->(b) RETURN a.id, b.id` -> `SELECT source, target FROM edges WHERE type = 'INFORMS'`

4. **commands/scout.md:** Replace any "kuzu"/"lazygraph" text references.

5. **commands/scheduled-tasks.md:** Replace any "kuzu"/"lazygraph" text references.

6. **commands/dashboard.md:** Replace any "kuzu"/"lazygraph" text references.
  </action>
  <verify>
    <automated>grep -rn 'kuzu\|\.lazygraph\|Cypher\|cypher' commands/graph.md commands/causal.md commands/query.md commands/scout.md commands/scheduled-tasks.md commands/dashboard.md 2>/dev/null | head -20; echo "---DONE"</automated>
  </verify>
  <acceptance_criteria>
    - grep -ric 'kuzu\|\.lazygraph\|Cypher\|cypher' commands/graph.md commands/causal.md commands/query.md commands/scout.md commands/scheduled-tasks.md commands/dashboard.md returns 0 for each file
  </acceptance_criteria>
  <done>All six command docs reference SQL/SQLite, zero kuzu/Cypher mentions</done>
</task>

</tasks>

<verification>
- `grep -rn 'kuzu\|\.lazygraph\|Cypher\|cypher' bin/mindrian-tools.cjs scripts/generate-presentation.cjs scripts/extract-room-intelligence.cjs lib/quickview/server.cjs scripts/post-write scripts/pre-compact scripts/on-agent-complete commands/graph.md commands/causal.md commands/query.md commands/scout.md commands/scheduled-tasks.md commands/dashboard.md 2>/dev/null` returns empty (allowing backward-compat alias comment)
</verification>

<success_criteria>
All 13 files updated. CLI says SQL not Cypher. SKIP_DIRS say .mindrian. Command docs reference SQL. Zero kuzu/.lazygraph references outside backward-compat aliases.
</success_criteria>

<output>
After completion, create `.planning/phases/79-sqlite-migration-cleanup/79-04-SUMMARY.md`
</output>
