---
phase: 190-shape-f-declaration-mandate
plan: 04
type: execute
wave: 3
depends_on: [190-02, 190-03]
files_modified:
  - scripts/install-pre-commit.sh
  - scripts/release.sh
  - scripts/doctor.cjs
  - tests/run-all-190.sh
  - CLAUDE.md
  - docs/CIRS-RELATIONSHIP-CONTRACT.md
  - docs/CANON-PHASE-MAP.md
autonomous: true
requirements: [SFD-04, SFD-05]

must_haves:
  truths:
    - "node scripts/check-shape-declaration.cjs --check is GREEN against the real, now-backfilled commands/agents/pipelines/skills tree"
    - "The gate's OK summary reports a surface count it computed by walking commands/, agents/, pipelines/*/CHAIN.md, and skills/*/SKILL.md at run time -- never a hardcoded 116 or 125 anywhere in shipped code"
    - "A commit that stages a command/agent/pipeline/skill file without a conformant hitl_shape declaration (and, for a skill, without a connector.excluded exemption) is rejected by the pre-commit hook"
    - "scripts/release.sh aborts before any version mutation if the shape-declaration gate is not green"
    - "Any future GSD discuss-phase / plan-phase / verify session in this repo can discover the born-declared-shape mandate -- INCLUDING its skills scope -- from CLAUDE.md without re-deriving it"
  artifacts:
    - path: "tests/run-all-190.sh"
      provides: "The phase 190 verification aggregator: a check-shape-declaration gate leg (mirrors tests/run-all-188.sh)"
      contains: "check-shape-declaration"
    - path: "CLAUDE.md"
      provides: "The updated Part 11 Canon Compliance Core bullet naming the third born-clause across all four surface classes"
      contains: "hitl_shape"
  key_links:
    - from: "scripts/install-pre-commit.sh"
      to: "scripts/check-shape-declaration.cjs"
      via: "a new HOOK_TRAILER block gated on staged paths matching commands/*.md, agents/*.md, pipelines/*/CHAIN.md, OR skills/*/SKILL.md"
      pattern: "check-shape-declaration.cjs --check"
    - from: "scripts/release.sh"
      to: "scripts/check-shape-declaration.cjs"
      via: "a new ABORT block immediately after the existing render-coverage gate (release.sh:294)"
      pattern: "check-shape-declaration.cjs --check"
---

<objective>
Close the loop: verify the gate is green against the real, backfilled tree (now spanning commands, agents,
pipelines, AND qualifying skills), wire scripts/check-shape-declaration.cjs into the same three enforcement
surfaces every other CIRS gate rides (pre-commit, release.sh, doctor --acceptance), and complete the GSD
wiring bullet by making the mandate -- including its skills scope -- discoverable to every future
discuss-phase / plan-phase / verify session in this repo the same way R12 is discoverable today (via
CLAUDE.md + docs/CIRS-RELATIONSHIP-CONTRACT.md + docs/CANON-PHASE-MAP.md), per the precedent this repo
already established for R12 (no edits to the shared global ~/.claude/gsd-core scaffold; the project-local
doc-contract + gate-script + CANON-PHASE-MAP-column pattern is how this repo makes a canon rule
self-propagating).

Purpose: R9 (Part 11: "enforced, not aspirational") demands the gate actually run at merge and release, not
just exist. R12's own contract is the proof this project-local wiring pattern works without touching shared
infrastructure outside this repo.
Output: a green check-shape-declaration.cjs gate wired into pre-commit + release.sh + doctor --acceptance +
tests/run-all-190.sh, plus CLAUDE.md / CIRS-RELATIONSHIP-CONTRACT.md / CANON-PHASE-MAP.md updated to name the
skills scope and the dynamic (never-hardcoded) surface count.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/190-shape-f-declaration-mandate/190-01-PLAN.md
@.planning/phases/190-shape-f-declaration-mandate/190-02-PLAN.md
@.planning/phases/190-shape-f-declaration-mandate/190-03-PLAN.md
@docs/CIRS-RELATIONSHIP-CONTRACT.md
@docs/HITL-SHAPE-DECLARATION-CONTRACT.md
@CLAUDE.md
@scripts/install-pre-commit.sh
@scripts/release.sh
@tests/run-all-188.sh
</context>

<RULES>
- Wire the gate as HARD-FAIL (exit 1), never WARN-only. Part 11 R9 names WARN-only orphaned gates as the
  exact regression pattern (Phases 143.x/144.1) this mandate exists to prevent.
- Mirror the EXACT existing wiring idiom at each of the three sites (pre-commit HOOK_TRAILER block,
  release.sh ABORT block, doctor.cjs acceptance `gates` array entry) rather than inventing a new pattern.
- Do NOT edit any file under ~/.claude/gsd-core or ~/.claude/agents (that is shared infrastructure outside
  this repo's ownership). The GSD-wiring requirement is satisfied via this repo's own CLAUDE.md + docs +
  CANON-PHASE-MAP.md, exactly as R12 already demonstrates.
- No canon byte is written in this plan (docs/MINDRIAN-CANON.md is untouched here; that is Plan 05, gated).
- No hardcoded surface-count literal in any file this plan touches; wherever a count is cited in prose
  (CLAUDE.md, docs), phrase it as "enumerated from disk (currently N)" never as a frozen constant.
- No em-dashes anywhere.
</RULES>

<tasks>

<task type="auto">
  <name>Task 1: Verify the gate is green against the real backfilled tree (commands + agents + pipelines + skills)</name>
  <read_first>
    - scripts/check-shape-declaration.cjs (Plan 03 output)
    - data/hitl-shape-backfill.json (Plan 01 output, the ground truth Plan 02 applied)
  </read_first>
  <files>scripts/check-shape-declaration.cjs</files>
  <action>
    Run the gate's --check mode against the real tree (now backfilled by Plan 02, spanning commands, agents,
    pipelines, and 9 qualifying skills). If it reports any violation, diagnose whether the defect is in the
    backfilled frontmatter (fix via a targeted re-run of scripts/backfill-hitl-shape.cjs after correcting the
    offending data/hitl-shape-backfill.json entry) or in the gate's own parsing logic (fix
    scripts/check-shape-declaration.cjs); do not paper over a real gap by loosening the gate. Confirm the one
    seeded contradiction case (commands/act.md, the sole f-selector-ranker consumer among commands) declares
    hitl_shape F.7 or an hitl_stages entry whose first stage is F.7, so the contradiction predicate has
    nothing to flag on the real command. Separately confirm skills/larry-personality/SKILL.md's dial-reach-
    selection stage declares F.7 (its implementation_script lib/hmi/dial-reach-orchestrator.cjs is a ranker-
    consumer anchor) -- the SAME predicate must pass clean on this skill-shaped multi-stage declaration too.
    Confirm the gate's OK summary line reports counts it computed (not literals): 125 declared, 5 skill-exempt,
    130 total files scanned across the four directories.
  </action>
  <verify>
    <automated>node scripts/check-shape-declaration.cjs --check</automated>
  </verify>
  <done>The gate's --check mode exits 0 with an OK summary against the real, backfilled commands/agents/pipelines/skills tree; commands/act.md and skills/larry-personality/SKILL.md's declared shapes are consistent with their proven f-selector-ranker consumption; the printed counts (125 declared / 5 skill-exempt / 130 scanned) match enumeration, not a hardcoded literal.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the gate into pre-commit + release.sh + doctor --acceptance + tests/run-all-190.sh</name>
  <read_first>
    - scripts/install-pre-commit.sh lines 33-45 (the idempotency grep list to extend) and lines 123-141 (the
      HOOK_TRAILER_RENDER block: the exact heredoc pattern to mirror for a new HOOK_TRAILER_SHAPE block)
    - scripts/release.sh lines 280-298 (the render-coverage ABORT block immediately preceding the corpus-
      stats block: insert the new shape-declaration ABORT block right after it, same idiom)
    - scripts/doctor.cjs around line 2770-2778 (the acceptance gates array: add a new entry for the
      shape-declaration script)
    - tests/run-all-188.sh (the aggregator idiom: run_if-safe legs, PASS/FAIL/SKIP counters)
  </read_first>
  <files>scripts/install-pre-commit.sh, scripts/release.sh, scripts/doctor.cjs, tests/run-all-190.sh</files>
  <action>
    In scripts/install-pre-commit.sh: add check-shape-declaration.cjs --check to the idempotency grep list
    (the if grep -q chain near line 37) and add a new HOOK_TRAILER_SHAPE heredoc block mirroring
    HOOK_TRAILER_RENDER's structure exactly, gated on staged paths matching commands/*.md, agents/*.md,
    pipelines/*/CHAIN.md, OR skills/*/SKILL.md (the fourth class added this phase), invoking the gate's
    --check mode with the same recovery-line-then-exit-1 idiom (recovery: run node
    scripts/backfill-hitl-shape.cjs, hand-author the missing declaration per
    docs/HITL-SHAPE-DECLARATION-CONTRACT.md, or add connector.excluded:true+reason if a new skill has no
    fork). In scripts/release.sh: insert a new ABORT block immediately after the existing render-coverage
    block (after line 298), same "if not green then print ABORT and exit 1" shape. In scripts/doctor.cjs: add
    a shape-declaration entry to the acceptance gates array alongside connector/projection/render, pointing
    at scripts/check-shape-declaration.cjs. Create tests/run-all-190.sh mirroring tests/run-all-188.sh's
    structure (PASS/FAIL/SKIP counters, run_if-safe legs): register the backfill generator's test file, the
    gate's own test file, and the gate's --check mode against the real tree as required-green legs (this is
    the "check-shape-declaration gate leg" the phase's verification aggregator must carry); leave a
    placeholder run_if leg for the Plan 05 canon FLOOR test (the file does not exist yet in this wave, so it
    SKIPs cleanly until Plan 05 lands).
  </action>
  <verify>
    <automated>bash tests/run-all-190.sh</automated>
  </verify>
  <acceptance_criteria>
    <automated>grep -c "check-shape-declaration" scripts/install-pre-commit.sh scripts/release.sh scripts/doctor.cjs</automated>
  </acceptance_criteria>
  <done>The pre-commit hook, scripts/release.sh, and scripts/doctor.cjs's acceptance gates array all invoke check-shape-declaration.cjs --check with HARD-FAIL (exit 1) semantics, gated on all four surface-class path globs; tests/run-all-190.sh exists and its check-shape-declaration legs pass.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the GSD wiring (CLAUDE.md + CIRS contract cross-ref + CANON-PHASE-MAP row), naming skills + the dynamic count</name>
  <read_first>
    - CLAUDE.md "Canon Compliance Core" section (the existing Part 11 bullet naming the born-wired R1/R2 gate)
    - docs/CIRS-RELATIONSHIP-CONTRACT.md (add a "See also" cross-reference near the top)
    - docs/CANON-PHASE-MAP.md "Part 11" section (the table this task adds a Phase 190 row to)
  </read_first>
  <files>CLAUDE.md, docs/CIRS-RELATIONSHIP-CONTRACT.md, docs/CANON-PHASE-MAP.md</files>
  <action>
    Update the existing CLAUDE.md Part 11 bullet to append one clause naming the third born-clause: every
    invocable surface -- a command, an agent, a pipeline, OR a skill that reaches a genuine Decision-Gate fork
    -- is ALSO born with a declared HITL shape (hitl_shape/hitl_why or hitl_stages), enforced by
    scripts/check-shape-declaration.cjs, deep-dive docs/HITL-SHAPE-DECLARATION-CONTRACT.md. Name the skills
    carve-out explicitly: a render-only / pure-capability skill is exempt via its existing
    connector.excluded:true+reason, never via a fork it does not have. This is the mechanism by which any
    future gsd-discuss-phase, gsd-plan-phase, or gsd-verify session in this repo discovers the mandate: every
    GSD agent reads project CLAUDE.md as mandatory initial context, so a future command-or-skill-introducing
    phase's planner/discusser/verifier surfaces the hitl_shape gray area without the global gsd-core scaffold
    needing modification (mirrors exactly how the existing Part 11 CIRS bullet already makes R1/R2
    discoverable). Add a short "See also" line in docs/CIRS-RELATIONSHIP-CONTRACT.md pointing to
    docs/HITL-SHAPE-DECLARATION-CONTRACT.md, noting R12 (forward-declare in PLAN.md) and the new R16
    (born-declare on the shipped surface, across all four surface classes) are sibling rules, not the same
    rule. Add a Phase 190 row under the CANON-PHASE-MAP.md Part 11 section table, status "planned" (flips to
    "shipped" once Plan 05's canon amendment lands), referencing scripts/check-shape-declaration.cjs,
    docs/HITL-SHAPE-DECLARATION-CONTRACT.md, and noting the skills fold-in (9 qualifying of 14 skills) plus
    the dynamic surface-count principle (never a hardcoded number).
  </action>
  <verify>
    <automated>grep -q "hitl_shape" CLAUDE.md && echo claude-ok</automated>
  </verify>
  <acceptance_criteria>
    <automated>grep -q "HITL-SHAPE-DECLARATION-CONTRACT" docs/CIRS-RELATIONSHIP-CONTRACT.md && grep -q "190" docs/CANON-PHASE-MAP.md && echo gsd-wiring-ok</automated>
  </acceptance_criteria>
  <done>CLAUDE.md's Part 11 bullet names the third born-clause across all four surface classes plus the skills exemption carve-out; docs/CIRS-RELATIONSHIP-CONTRACT.md cross-references docs/HITL-SHAPE-DECLARATION-CONTRACT.md; docs/CANON-PHASE-MAP.md carries a Phase 190 row under Part 11 naming the skills fold-in and the dynamic count principle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local commit -> shipped surface | pre-commit is the last local checkpoint before a bad declaration (or a wrongly-exempted skill) enters history |
| release.sh -> published version | release is the last checkpoint before a version ships to users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-04-01 | Repudiation | a contributor bypasses pre-commit with --no-verify | accept | matches this repo's existing bypass policy (documented, not enforced; release.sh + doctor --acceptance are the backstop gates that still catch it before ship) |
| T-190-04-02 | Tampering | WARN-only wiring silently regresses to a no-op gate (the 143.x/144.1 pattern) | mitigate | every wiring point is HARD-FAIL (exit 1), mirroring the already-hardened render-coverage/connector/projection gates |
| T-190-04-03 | Tampering | a future doc or script hardcodes a stale surface-count literal as the repo grows | mitigate | Task 1's verify explicitly checks the gate's printed counts against live enumeration; Task 3's prose uses "enumerated from disk (currently N)" phrasing everywhere a count is cited |
| T-190-04-SC | Tampering | npm/pip/cargo installs | accept | zero package installs; bash + node:fs only |
</threat_model>

<verification>
- `bash tests/run-all-190.sh` is green (or SKIPs only the not-yet-landed Plan 05 FLOOR test leg).
- `grep -c check-shape-declaration scripts/install-pre-commit.sh scripts/release.sh scripts/doctor.cjs` each return at least 1.
- `bash scripts/install-pre-commit.sh` reports the hook already/newly wired with the shape-declaration guard present, gated on all four surface-class globs.
</verification>

<success_criteria>
- The gate runs HARD-FAIL at commit time, release time, and doctor --acceptance time, across all four surface classes.
- CLAUDE.md, docs/CIRS-RELATIONSHIP-CONTRACT.md, and docs/CANON-PHASE-MAP.md make the mandate -- including its skills scope and its dynamic surface count -- discoverable to any future GSD session in this repo without touching shared global infrastructure.
</success_criteria>

## Artifacts this phase produces
- Updated `scripts/install-pre-commit.sh` (new HOOK_TRAILER_SHAPE guard, gated on 4 surface-class globs)
- Updated `scripts/release.sh` (new shape-declaration ABORT block)
- Updated `scripts/doctor.cjs` (new acceptance gate entry)
- `tests/run-all-190.sh` (the phase verification aggregator)
- Updated `CLAUDE.md`, `docs/CIRS-RELATIONSHIP-CONTRACT.md`, `docs/CANON-PHASE-MAP.md`

<output>
Create `.planning/phases/190-shape-f-declaration-mandate/190-04-SUMMARY.md` when done
</output>
