---
phase: 190-shape-f-declaration-mandate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/HITL-SHAPE-DECLARATION-CONTRACT.md
  - data/hitl-shape-declaration-schema.json
  - data/hitl-shape-backfill.json
autonomous: true
requirements: [SFD-01, SFD-02]

must_haves:
  truths:
    - "A single documented contract states exactly what hitl_shape / hitl_why / hitl_stages mean and which surfaces must carry them, INCLUDING the fourth surface class skills/*/SKILL.md"
    - "The contract states the skills-specific exemption rule: a skill with NO genuine Decision-Gate fork is exempt from declaring (no hitl_shape/hitl_stages required) PROVIDED it already carries connector.excluded:true + reason (Canon Part 11 R1); this reuses the existing CIRS exclusion signal instead of minting a second closed-vocabulary 'none' path for skills"
    - "A machine-readable backfill map exists with one entry per DECLARING invocable surface file (every commands/*.md, agents/*.md, pipelines/*/CHAIN.md, and every QUALIFYING skills/*/SKILL.md -- a skill that reaches a genuine fork), so Plan 02 can apply it mechanically instead of hand-editing files"
    - "Every backfilled hitl_shape value is a member of the closed F.0-F.9 set (or 'none' with a reason for commands/agents/pipelines), never an invented shape; qualifying skills NEVER use 'none' -- a skill either declares a real shape/stages or is exempt via connector.excluded (no third state)"
    - "The surface-count assertion is NOT a hardcoded number anywhere in code: total declaring surfaces = count(commands/*.md) + count(agents/*.md) + count(pipelines/*/CHAIN.md) + count(qualifying skills/*/SKILL.md), always enumerated from disk"
  artifacts:
    - path: "docs/HITL-SHAPE-DECLARATION-CONTRACT.md"
      provides: "The frontmatter contract (mirrors docs/CIRS-RELATIONSHIP-CONTRACT.md structure) + the decision rule table + the skills exemption rule"
      contains: "hitl_shape"
    - path: "data/hitl-shape-declaration-schema.json"
      provides: "shape_vocabulary (F.0-F.9 + none) + mode_vocabulary (parallel|ordered|gate) + required-field rules"
      contains: "shape_vocabulary"
    - path: "data/hitl-shape-backfill.json"
      provides: "One entry per DECLARING surface file: 104 commands + 9 agents + 3 pipelines + 9 qualifying skills = 125 entries; {surface, hitl_shape|hitl_stages, hitl_why[, implementation_script]}"
      min_lines: 420
  key_links:
    - from: "data/hitl-shape-backfill.json"
      to: "data/hitl-shape-declaration-schema.json"
      via: "every hitl_shape / stage.shapes[] value is a member of schema _doc.shape_vocabulary"
      pattern: "shape_vocabulary"
    - from: "data/hitl-shape-backfill.json"
      to: "skills/*/SKILL.md connector.excluded"
      via: "the 5 non-qualifying skills (context-engine, room-passive, pws-methodology, ui-system, brain-connector) are DELIBERATELY ABSENT from this JSON; each already carries connector.excluded:true + reason, which is what makes their absence a conformant exemption rather than a gap"
      pattern: "excluded"
---

<objective>
Define the canonical HITL-shape declaration contract (the frontmatter fields, the closed vocabulary, the
decision rule) and author the complete backfill data set that Plan 02 will mechanically apply to every
existing invocable surface that DECLARES a shape: 104 commands + 9 agents + 3 pipeline CHAIN.md files + 9
qualifying skills = 125 entries. This plan FOLDS SKILLS INTO the declaration-mandate scope (navigator-directed
2026-07-01): every skills/*/SKILL.md that reaches a genuine Decision-Gate fork must declare a shape, exactly
like a command or agent; a pure-capability / render-only skill with no fork is EXCLUDED from the mandate --
mirroring how a render-only surface is already excluded under Canon Part 11 R1 -- and is identified by its
EXISTING `connector.excluded: true` + `reason` frontmatter (Phase 172-06), never a newly invented signal. This
is the interface-first wave: nothing here touches a command/agent/pipeline/skill file directly; it defines
the contract those files must satisfy and the data that proves the mapping was actually worked out, not
invented per-file.

Purpose: per SEED-041, "the backfill is cheap because the full command->shape map already exists" -- this
plan transcribes that already-verified map (the mindrian-f-shapes explainer's GROUPS table, the 103-command
relationship dossiers, and -- newly -- a direct read of all 14 skills/*/SKILL.md files) into one authoritative,
schema-validated JSON file, so downstream plans apply it mechanically (Part 7 reuse-before-build: one
generator run, not 125 manual edits).
Output: docs/HITL-SHAPE-DECLARATION-CONTRACT.md, data/hitl-shape-declaration-schema.json,
data/hitl-shape-backfill.json.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/MINDRIAN-CANON.md
@docs/CIRS-RELATIONSHIP-CONTRACT.md
@docs/CONNECTOR-CONTRACT.md
@data/hitl-stages-schema.json
@scripts/check-hitl-stages.cjs
@.planning/seeds/SEED-041-shape-f-declaration-mandate.md
@.planning/research/command-map/INDEX.md
</context>

<RULES>
- Frozen Part 3 scalars (MAX_K=3, DIAL_REACH_K=6, 0.70/0.15) are NOT touched anywhere in this plan; this
  wave is a DECLARATION contract, not a render/ranking change.
- Closed vocabulary ONLY: hitl_shape must be one of F.0, F.1, F.2, F.3, F.4, F.5, F.6, F.7, F.8, F.9, or the
  literal string "none" (a commands/agents/pipelines surface with no genuine fork). Never invent an eleventh
  shape.
- Skills NEVER use "none". A skill either (a) reaches a genuine fork and declares a real hitl_shape or
  hitl_stages, or (b) has no fork and is exempt by virtue of its EXISTING connector.excluded:true + reason --
  it is simply absent from data/hitl-shape-backfill.json. This is a deliberate asymmetry vs. commands/agents/
  pipelines (Task 1 documents WHY: skills already have a first-class CIRS exclusion signal on disk; reusing it
  is Part 7 reuse-before-build, and it avoids writing a redundant declaration onto 5 files that already say,
  in their own frontmatter, "this is ambient substrate with no discrete trigger").
- No em-dashes anywhere (docs or JSON comments/strings).
- CJS/JSON only; no TypeScript.
- Reuse before build (Canon Part 7): the mode vocabulary (parallel|ordered|gate) and the per-stage shape
  array shape are IDENTICAL to the already-shipped data/hitl-stages-schema.json / scripts/check-hitl-stages.cjs
  contract. Do not invent a second, incompatible stage schema.
- No hardcoded surface-count literal anywhere in the two JSON files' logic or in any future gate code (Plan 03/
  04). The docs/HITL-SHAPE-DECLARATION-CONTRACT.md prose MAY cite "125 as of this phase" for illustration but
  must frame it as "enumerated from disk", never as a frozen constant.
</RULES>

<tasks>

<task type="auto">
  <name>Task 1: Write the HITL-shape declaration contract + JSON schema (now covering 4 surface classes)</name>
  <read_first>
    - docs/CIRS-RELATIONSHIP-CONTRACT.md (the doc-contract shape to mirror: why-it-exists, the declaration
      block, the trigger, where it is recorded, the gate hook)
    - docs/CONNECTOR-CONTRACT.md section "R1: two states -- WIRED or EXCLUDED-with-reason" (the EXACT existing
      exemption mechanism this plan's skills carve-out reuses: `connector.excluded: true` + `reason`)
    - docs/MINDRIAN-CANON.md Part 11 (lines 407-573, the CIRS closed ruling set R1-R15; this contract will
      be cited by the future R16) and Part 3 (lines 139-207, the ten Shape-F sub-shapes + the decision-moment
      framing)
    - data/hitl-stages-schema.json + scripts/check-hitl-stages.cjs (the existing hitl_stages validation
      contract this plan's schema must stay compatible with)
    - skills/context-engine/SKILL.md, skills/room-passive/SKILL.md, skills/pws-methodology/SKILL.md,
      skills/ui-system/SKILL.md, skills/brain-connector/SKILL.md (the 5 already-excluded, non-forking
      skills -- confirm each carries `connector.excluded: true` + a `reason` naming an ambient/substrate/
      render/reference posture with NO discrete Decision-Gate fork of its own)
    - skills/conversation-mode/SKILL.md, skills/intelligence-orchestrator/SKILL.md,
      skills/larry-personality/SKILL.md, skills/room-proactive/SKILL.md (4 skills that ARE
      connector.excluded:true for CIRS reach-spine purposes YET reach genuine Decision-Gate forks -- proof
      that CIRS wiring status and HITL-shape-fork status are ORTHOGONAL axes, not the same test)
    - .planning/seeds/SEED-041-shape-f-declaration-mandate.md section "The decision rule" (the exact
      heuristic: ordered/dependent -> F.9/F.2; independent/any-order set -> F.8; parallel branches -> F.5;
      single move/yes-no -> F.1/F.0; depth budget -> F.3; harvest scope -> F.4; plan review/JTBD -> F.6;
      ranked capability reaches -> F.7)
  </read_first>
  <files>docs/HITL-SHAPE-DECLARATION-CONTRACT.md, data/hitl-shape-declaration-schema.json</files>
  <action>
    Write docs/HITL-SHAPE-DECLARATION-CONTRACT.md mirroring the structure of
    docs/CIRS-RELATIONSHIP-CONTRACT.md: a "Why this contract exists" section naming the THIRD Part 11
    born-clause (born WIRED-or-EXCLUDED, R1/R2; born declared-shape, the new R16) and the GIX single-select
    bug from Phase 188 as the motivating failure mode; the frontmatter contract itself (two forms -- single-
    fork surfaces carry `hitl_shape: F.x` (or `none`, commands/agents/pipelines only) + `hitl_why:` one
    Feynman-line string; multi-stage engines/pipelines/skills carry `hitl_stages:` per the existing
    data/hitl-stages-schema.json shape instead); the closed decision-rule table verbatim from SEED-041; the
    trigger (every file under commands/, agents/, pipelines/*/CHAIN.md, OR skills/*/SKILL.md -- the FOURTH
    surface class, folded in navigator-directed 2026-07-01); a dedicated "Skills: the fork test, not the
    wiring test" section stating the classification rule verbatim -- "a skill declares a shape if and only if
    it reaches a genuine Decision-Gate fork (a place the navigator picks among options), REGARDLESS of its
    Canon Part 11 R1 connector.connects_to_spine / connector.excluded status; a skill that is CIRS-excluded
    (ambient substrate, no reach-spine wiring) can still reach a fork and MUST still declare (four such skills
    ship in this phase); a skill with NO fork is exempt from THIS mandate by virtue of its EXISTING
    connector.excluded:true + reason -- it does not additionally declare hitl_shape:none, it is simply absent
    from data/hitl-shape-backfill.json, and the gate (Plan 03) treats 'absent declaration + connector.excluded
    present' as a conformant exempt state, never a gap"; a cross-reference to docs/CIRS-RELATIONSHIP-CONTRACT.md
    (R12 is the forward-declare rule for PLAN.md; this is the born-declare rule for the shipped surface itself,
    a sibling rule not the same rule); and a pointer to the gate hook scripts/check-shape-declaration.cjs (name
    it as the implementing script; Plan 03 ships it). State the dynamic surface-count principle explicitly:
    "the total count of declaring surfaces is NEVER hardcoded; it is always
    count(commands/*.md)+count(agents/*.md)+count(pipelines/*/CHAIN.md)+count(qualifying skills/*/SKILL.md),
    enumerated from disk by the gate at check time. As of this phase that total is 125 (104+9+3+9); cite this
    number for illustration only, never as a frozen constant a future gate hardcodes." Then write
    data/hitl-shape-declaration-schema.json with a `_doc` object holding `shape_vocabulary` (an array of
    exactly F.0 through F.9 plus the literal "none") and `mode_vocabulary` copied byte-identical from
    data/hitl-stages-schema.json's `_doc.mode_vocabulary` (parallel, ordered, gate) so the two schemas can
    never silently diverge on the mode enum.
  </action>
  <verify>
    <automated>node -e "const s=require('./data/hitl-shape-declaration-schema.json')._doc; console.log(s.shape_vocabulary.length===11 && s.shape_vocabulary.includes('none') ? 'schema-ok' : 'FAIL')"</automated>
  </verify>
  <done>docs/HITL-SHAPE-DECLARATION-CONTRACT.md exists and documents both frontmatter forms + the decision rule + the trigger (4 surface classes) + the skills fork-test-not-wiring-test exemption rule + the dynamic-count principle; data/hitl-shape-declaration-schema.json's shape_vocabulary is exactly the 10 closed shapes plus "none" (11 members); mode_vocabulary matches data/hitl-stages-schema.json byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Author the complete backfill data set (125 declaring surfaces, including 9 qualifying skills)</name>
  <read_first>
    - /home/jsagi/mindrian-f-shapes/index.html lines 684-870 (the `const GROUPS=[...]` array: the verified
      command/agent/framework -> shape mapping this task transcribes; row shape is
      [name, cmd, shapeIds[], multiFlag, stepsMode, chainsFlag] where shapeIds use the short form f0..f9)
    - .planning/research/command-map/INDEX.md sections 1 (CLUSTERS) and 2 (CHAINS table) -- cross-reference
      for commands the GROUPS table does not name explicitly, and for the pipeline CHAIN.md ordering
    - data/hitl-shape-declaration-schema.json (Task 1 output, the closed vocabulary this data must satisfy)
    - data/hitl-stages-schema.json (the hitl_stages shape for multi-shape rows)
    - skills/MOSDeckEngine/SKILL.md, skills/client-discovery-interview/SKILL.md,
      skills/mullins-scaffold/SKILL.md, skills/mva-pipeline/SKILL.md (each already carries
      `connector.surface: F.1` in its existing connector: block -- REUSE that value as hitl_shape rather than
      re-deriving it; the value already IS the Shape-F declaration, just under a different key)
    - skills/trending-to-absurd/SKILL.md (frontmatter `description:` explicitly names TWO judgment points:
      "trend selection, opportunity pick" -- a two-stage ordered hitl_stages, not a single hitl_shape)
    - skills/conversation-mode/SKILL.md section "Lane Picker (Shape F.1 -- the explicit traffic cop)" (an
      explicit F.1 fork despite connector.excluded:true)
    - skills/intelligence-orchestrator/SKILL.md (its own rendering mechanism for offering a dispatched reach is
      the room-proactive APPROVE/REJECT/DEFER 3-verb flow -- Canon Part 3's F.0 Mini Decision Gate -- REGARDLESS
      of which shape the dispatched reach's own connector.surface happens to be; the orchestrator's OWN fork is
      F.0, not the polymorphic F.0-or-F.1 of whichever reach it dispatches)
    - skills/room-proactive/SKILL.md (two independent decision-close moments: the proactive-finding
      APPROVE/REJECT/DEFER flow, Canon F.0; and "Close the turn with a Decision Gate F.1 Next Move selector"
      for the conversation-artifact filing offer, Canon F.1 -- a two-entry hitl_stages, mode "gate" each,
      mirroring the 190-01-original rule "a decision-close row (F.0/F.5) maps to mode 'gate'")
    - skills/larry-personality/SKILL.md (FOUR independent decision-close moments: contradiction response
      (F.0, "Decision Gate -- APPROVE / REJECT (reason) / DEFER"), deep-research plan approval (F.1, "ends at
      a Decision Gate (Run Methodology / Reformulate / Free-Text)"), conversation-artifact filing offer (F.1,
      "offers to file it via an F.1 Decision Gate"), and the ranked-reach dial (F.7, "the F.7 tri-context
      header"; implemented via lib/hmi/dial-reach-orchestrator.cjs, one of the THREE ranker-consumer allowlist
      anchors Plan 03 checks against -- set `implementation_script: "lib/hmi/dial-reach-orchestrator.cjs"` on
      this entry so the f-selector-ranker-contradiction predicate has something concrete to check and confirms
      no contradiction, since F.7 IS present in the dial-reach-selection stage)
  </read_first>
  <files>data/hitl-shape-backfill.json</files>
  <action>
    Build data/hitl-shape-backfill.json as a single JSON object keyed by relative surface path (for example
    "commands/act.md", "agents/persona-analyst.md", "pipelines/discovery/CHAIN.md",
    "skills/mullins-scaffold/SKILL.md"), covering exactly 125 DECLARING surfaces: every file under
    commands/*.md (104), agents/*.md (9), pipelines/*/CHAIN.md (3), and the 9 QUALIFYING skills/*/SKILL.md
    (MOSDeckEngine, client-discovery-interview, mullins-scaffold, mva-pipeline, trending-to-absurd,
    conversation-mode, intelligence-orchestrator, room-proactive, larry-personality). The 5 NON-qualifying
    skills (context-engine, room-passive, pws-methodology, ui-system, brain-connector) are DELIBERATELY
    ABSENT -- do not add "none" entries for them; their exemption lives in their own already-shipped
    connector.excluded:true + reason (verified in Task 1's read_first).

    For commands/agents/pipelines, apply the ORIGINAL derivation rules unchanged:
    (1) A GROUPS row with a single shapeIds entry and stepsMode 'one' becomes
        {hitl_shape: "F.n", hitl_why: one Feynman line derived from the row name + stepsMode}.
    (2) A GROUPS row with shapeIds.length greater than 1, OR a command whose GROUPS entries recur across
        multiple rows (act / act-chain / act-swarm all map to the ONE file commands/act.md), becomes
        {hitl_stages: [{stage, shapes: ["F.x"], mode}, ...]} ordered per the row sequence: stepsMode 'ind' maps
        to mode 'parallel', 'ord' maps to mode 'ordered', a decision-close row (F.0/F.5) maps to mode 'gate'.
    (3) The three pipeline rows map to pipelines/discovery/CHAIN.md, pipelines/thesis/CHAIN.md,
        pipelines/analogy/CHAIN.md respectively, each hitl_stages ordered exactly per that pipeline's own
        CHAIN.md stage list.
    (4) The 9 files under agents/*.md map to the "Agents and Synthetic Expert Creation" GROUPS section by
        role; brain-query.md and larry-extended.md have no explicit GROUPS row so derive hitl_shape from the
        decision rule directly (brain-query.md: single lookup -> F.1; larry-extended.md: persona-blend single
        response -> F.1).
    (5) Any commands/*.md file with NO matching GROUPS row gets its hitl_shape derived from the decision rule
        applied to its own frontmatter body_shape / description field; never leave a file unmapped.
    (6) hitl_why is always a single Feynman-simplified sentence, no em-dashes, explaining WHY that shape
        fires, not merely restating the shape id.

    For the 9 qualifying skills, use these EXACT entries (derived in this plan's read_first, transcribe
    verbatim, adjusting only JSON syntax):
    - "skills/MOSDeckEngine/SKILL.md": {hitl_shape: "F.1", hitl_why: "Deprecated redirect to /mos:deck;
      inherits /mos:deck's F.1 Next Move gate at the close of the Feynman pipeline stages, never introduces a
      separate fork of its own."}
    - "skills/client-discovery-interview/SKILL.md": {hitl_shape: "F.1", hitl_why: "Each of the six discovery
      movements closes with an F.1 Next Move choice (continue the movement, reframe, or move to the Discovery
      Brief), never silently auto-advancing."}
    - "skills/mullins-scaffold/SKILL.md": {hitl_shape: "F.1", hitl_why: "Each of the seven domain folders is
      offered at an F.1 Decision Gate before creation, never auto-imposed on the room."}
    - "skills/mva-pipeline/SKILL.md": {hitl_shape: "F.1", hitl_why: "The 30-second brief closes with a
      numbered option-or-free-text choice (1/2/3/free-text), the canonical F.1 Next Move shape."}
    - "skills/trending-to-absurd/SKILL.md": {hitl_stages: [{stage: "trend-selection", shapes: ["F.1"], mode:
      "ordered"}, {stage: "opportunity-pick", shapes: ["F.1"], mode: "ordered"}], hitl_why: "Two ordered Shape
      F Decision Gates per its own frontmatter description: trend selection then opportunity pick, never
      collapsed into one silent choice."}
    - "skills/conversation-mode/SKILL.md": {hitl_shape: "F.1", hitl_why: "The lane picker (Just Talk /
      Explore+Capture / Build a Room) is an explicit F.1 Decision Gate, never an inferred persona
      classification."}
    - "skills/intelligence-orchestrator/SKILL.md": {hitl_shape: "F.0", hitl_why: "Every dispatched reach is
      offered through the room-proactive APPROVE/REJECT/DEFER mini-gate (F.0) before the resolved command
      fires; the dispatched reach's own connector.surface governs only what happens after APPROVE."}
    - "skills/room-proactive/SKILL.md": {hitl_stages: [{stage: "proactive-finding-response", shapes: ["F.0"],
      mode: "gate"}, {stage: "filing-offer-close", shapes: ["F.1"], mode: "gate"}], hitl_why: "Two independent
      decision-close moments: an APPROVE/REJECT/DEFER mini-gate on a surfaced finding, and an F.1 Next Move
      gate when offering to file a conversation artifact."}
    - "skills/larry-personality/SKILL.md": {hitl_stages: [{stage: "contradiction-response", shapes: ["F.0"],
      mode: "gate"}, {stage: "deep-research-plan-approval", shapes: ["F.1"], mode: "gate"}, {stage:
      "conversation-artifact-filing-offer", shapes: ["F.1"], mode: "gate"}, {stage: "dial-reach-selection",
      shapes: ["F.7"], mode: "gate"}], hitl_why: "Four independent Decision-Gate moments: a contradiction
      mini-gate (F.0), a deep-research plan approval (F.1), a conversation-artifact filing offer (F.1), and
      the ranked-reach dial (F.7, implemented via lib/hmi/dial-reach-orchestrator.cjs).",
      implementation_script: "lib/hmi/dial-reach-orchestrator.cjs"}

    After authoring, self-verify: the top-level key count is exactly 125, every key resolves to a file that
    exists on disk, every hitl_shape / stage.shapes[] value is a member of shape_vocabulary from Task 1's
    schema, and NONE of the 5 non-qualifying skill paths (skills/context-engine/SKILL.md,
    skills/room-passive/SKILL.md, skills/pws-methodology/SKILL.md, skills/ui-system/SKILL.md,
    skills/brain-connector/SKILL.md) appear as keys, while each of those 5 files' own frontmatter still
    contains `excluded: true` (proving the exemption is real, not a forgotten gap).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const map=require('./data/hitl-shape-backfill.json');const vocab=new Set(require('./data/hitl-shape-declaration-schema.json')._doc.shape_vocabulary);const keys=Object.keys(map);let bad=[];for(const k of keys){if(!fs.existsSync(k))bad.push('missing-file:'+k);const v=map[k];if(v.hitl_shape){if(!vocab.has(v.hitl_shape))bad.push('bad-shape:'+k);if(!v.hitl_why)bad.push('no-why:'+k);}else if(Array.isArray(v.hitl_stages)){for(const st of v.hitl_stages){for(const s of st.shapes){if(!vocab.has(s))bad.push('bad-stage-shape:'+k);}}if(!v.hitl_why)bad.push('no-why:'+k);}else{bad.push('neither:'+k);}}console.log(keys.length===125&&bad.length===0?('OK-'+keys.length):('FAIL:'+bad.slice(0,8).join(',')));"</automated>
  </verify>
  <acceptance_criteria>
    <automated>node -e "const fs=require('fs');const map=require('./data/hitl-shape-backfill.json');const exempt=['skills/context-engine/SKILL.md','skills/room-passive/SKILL.md','skills/pws-methodology/SKILL.md','skills/ui-system/SKILL.md','skills/brain-connector/SKILL.md'];let bad=[];for(const s of exempt){if(map[s])bad.push('should-not-be-keyed:'+s);if(!/excluded:\s*true/.test(fs.readFileSync(s,'utf8')))bad.push('missing-excluded:'+s);}console.log(bad.length===0?'exempt-ok':'FAIL:'+bad.join(','));"</automated>
  </acceptance_criteria>
  <done>data/hitl-shape-backfill.json has exactly 125 top-level keys: 104 commands/*.md + 9 agents/*.md + 3 pipelines/*/CHAIN.md + 9 qualifying skills/*/SKILL.md; every hitl_shape (or every stage.shapes[] entry) is a member of the Task 1 shape_vocabulary; every entry carries a non-empty hitl_why; the 5 non-qualifying skills are absent from the JSON and each still carries connector.excluded:true in its own frontmatter (the exemption is real, not forgotten).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| explainer HTML -> backfill JSON | the GROUPS table in an external static HTML file is untrusted-provenance input; only structural (shape-vocabulary-membership) data crosses into the repo, no code executes from the HTML |
| skill classification -> backfill JSON | classifying a skill as "qualifying" (has a fork) vs "exempt" (no fork) is a judgment call made once during authoring; a wrong call either over-declares (harmless, just extra frontmatter) or wrongly exempts a forking skill (a real gap the R16 gate cannot catch on its own, since exemption is keyed off connector.excluded, not a semantic re-derivation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-01-01 | Tampering | data/hitl-shape-backfill.json | mitigate | Task 2's automated verify enumerates every key against the closed shape_vocabulary and against actual files on disk; a bad or invented shape id fails the task |
| T-190-01-02 | Repudiation | hitl_why left empty or generic | mitigate | verify script asserts non-empty hitl_why on every entry |
| T-190-01-03 | Spoofing | a skill wrongly marked exempt when it actually has a fork | accept | the classification is recorded in this plan's read_first with the exact quoted evidence line from each skill's own body text (Lane Picker, F.0 gate, F.7 dial, etc.); a future navigator review of docs/HITL-SHAPE-DECLARATION-CONTRACT.md's skills section is the audit trail; residual risk is named, not hidden |
| T-190-01-SC | Tampering | npm/pip/cargo installs | accept | zero package installs; CJS + JSON only, no new dependency |
</threat_model>

<verification>
- `node -e "require('./data/hitl-shape-declaration-schema.json')._doc.shape_vocabulary.length===11"` true.
- `node -e "Object.keys(require('./data/hitl-shape-backfill.json')).length===125"` true.
- Every key in data/hitl-shape-backfill.json resolves to an existing file (commands/, agents/, pipelines/*/CHAIN.md, or skills/*/SKILL.md).
- The 5 non-qualifying skills (context-engine, room-passive, pws-methodology, ui-system, brain-connector) are absent from the JSON and each still carries `connector.excluded: true` in its own frontmatter.
- docs/HITL-SHAPE-DECLARATION-CONTRACT.md exists and documents both frontmatter forms + the decision rule table + the trigger (4 surface classes) + the skills exemption rule + the dynamic-count principle.
</verification>

<success_criteria>
- The declaration contract doc + JSON schema + full 125-surface backfill data exist and are internally consistent (schema vocabulary, backfill data, and real files all agree).
- Skills are folded into the mandate on the fork test, not the CIRS-wiring test: 9 qualifying skills declare; 5 non-forking skills are exempt via their existing connector.excluded:true, never a redundant "none".
- No command/agent/pipeline/skill file has been modified yet (that is Plan 02's job); this plan only produces the contract + data other plans consume.
</success_criteria>

## Artifacts this phase produces
- `docs/HITL-SHAPE-DECLARATION-CONTRACT.md` - the frontmatter contract + decision rule table + trigger (4 surface classes) + skills exemption rule + cross-ref to CIRS-RELATIONSHIP-CONTRACT.md
- `data/hitl-shape-declaration-schema.json` - the closed shape_vocabulary (F.0-F.9 + none) + shared mode_vocabulary
- `data/hitl-shape-backfill.json` - the 125-surface backfill data set (Plan 02's generator input), spanning commands + agents + pipelines + 9 qualifying skills

<output>
Create `.planning/phases/190-shape-f-declaration-mandate/190-01-SUMMARY.md` when done
</output>
