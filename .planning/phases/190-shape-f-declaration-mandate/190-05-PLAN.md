---
phase: 190-shape-f-declaration-mandate
plan: 05
type: execute
wave: 4
depends_on: [190-04]
files_modified:
  - docs/MINDRIAN-CANON.md
  - docs/CANON-PHASE-MAP.md
  - tests/test-canon-entry-36-shape-declaration-floor.cjs
  - tests/test-canon-entry-31-two-gauge-floor.cjs
  - tests/run-all-190.sh
autonomous: false
requirements: [SFD-01]

must_haves:
  truths:
    - "The navigator explicitly APPROVES the amendment before ANY docs/MINDRIAN-CANON.md byte is written"
    - "Canon Part 11's closed CIRS ruling set gains R16 (born-declared shape, across ALL FOUR surface classes: command, agent, pipeline, and qualifying skill), moving R1-R15 to R1-R16"
    - "Appendix D gains entry 36 recording the amendment, additive (every prior entry 1-35 stays byte-identical)"
    - "Frozen Part 3 scalars (MAX_K=3, DIAL_REACH_K=6, 0.70/0.15) stay byte-identical"
    - "The ratified R16 text states the surface count is ALWAYS enumerated from disk, never a frozen constant -- it names the current count (125 declaring: 104 commands + 9 agents + 3 pipelines + 9 qualifying skills; 5 skills exempt via connector.excluded) for illustration only"
  artifacts:
    - path: "docs/MINDRIAN-CANON.md"
      provides: "R16 in the CIRS closed ruling set + Appendix D entry 36 + version bump 1.22 -> 1.23"
      contains: "R16"
    - path: "tests/test-canon-entry-36-shape-declaration-floor.cjs"
      provides: "The canonical FLOOR test for entry 36 (mirrors test-canon-entry-31-two-gauge-floor.cjs)"
  key_links:
    - from: "docs/MINDRIAN-CANON.md Appendix D entry 36"
      to: "R16 in Part 11"
      via: "the entry names R16, scripts/check-shape-declaration.cjs as its implementing gate, and the skills fold-in"
      pattern: "R16"
---

<objective>
NAVIGATOR-GATED canon amendment (mirroring Appendix D entries 31/32/33/34/35): ratify the THIRD Part 11
born-clause, now scoped across ALL FOUR invocable surface classes. Today CIRS says every invocable surface is
born WIRED or EXCLUDED (R1/R2). This plan adds R16: every invocable surface -- a command, an agent, a
pipeline, OR a skill that reaches a genuine Decision-Gate fork -- is ALSO born with a DECLARED HITL SHAPE,
enforced by scripts/check-shape-declaration.cjs (shipped and green in Plans 01-04). A pure-capability /
render-only skill with no fork is exempt via its existing connector.excluded:true+reason, never a fork it
does not have. An autonomous agent CANNOT ratify the constitution. This plan's FIRST task is a BLOCKING
checkpoint that HALTS for a navigator APPROVE before any docs/MINDRIAN-CANON.md byte is written. Only after
APPROVE does the atomic lockstep edit land.

Purpose: the code and the gate exist and are green (Plans 01-04, spanning commands/agents/pipelines/skills);
the canon must formally admit R16 as a closed-set member (R1-R15 -> R1-R16) so the mandate -- including its
skills scope -- is a real constitutional rule, not merely a script that happens to run. This is the one true
human gate in Phase 190.
Output: the ratified canon amendment (one atomic lockstep) + the frozen-scalar guards still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/190-shape-f-declaration-mandate/190-01-PLAN.md
@.planning/phases/190-shape-f-declaration-mandate/190-04-PLAN.md
@docs/MINDRIAN-CANON.md
@docs/HITL-SHAPE-DECLARATION-CONTRACT.md
@tests/test-canon-frozen-scalars-floor.cjs
@tests/test-canon-entry-31-two-gauge-floor.cjs
</context>

<RULES>
- NAVIGATOR GATE: NO byte of docs/MINDRIAN-CANON.md may be written before the navigator APPROVES at the
  Task-1 checkpoint. Drafting canon prose autonomously is a constitutional violation (mirrors Phase 188's
  D-01a / Phase 195's D-01 discipline).
- ADDITIVE house style (entries 25/27/31/32/33/34/35): mint ONE new Appendix D entry (36); leave every
  existing entry 1-35 byte-identical. R1-R15 stay byte-identical; R16 is a new bullet appended after R15.
- FROZEN SCALARS: MAX_K=3, DIAL_REACH_K=6, 0.70, 0.15 stay byte-identical. R16 mints NO reach/edge/node and
  opens NO Brain wire; it is a declaration mandate, not a render/ranking change.
- ONE ATOMIC LOCKSTEP: R16 text + Appendix D entry 36 + header/footer version bump (1.22 -> 1.23) +
  CANON-PHASE-MAP.md version-history row + the Part 11 section row flipped from "planned" to "shipped" +
  the new entry-36 FLOOR test + the entry-31 FLOOR test's version-anchor bump (Test 9: 1.22 -> 1.23) -- all in
  ONE commit-worthy edit, so the canon is never in a half-amended state.
- The planner specifies WHAT the amendment contains (below). The executor drafts the prose AFTER APPROVE.
  No em-dashes anywhere in canon prose.
- The ratified R16 text must NOT hardcode the surface count as a frozen number; it states the enumeration
  formula (commands + agents + pipelines + qualifying skills) and may cite "125 as of this phase" once, for
  illustration, clearly marked as a snapshot, never as a canon-frozen scalar (it is not in the frozen-scalars
  list and must never be treated as one).
- Confirm the version bump target (1.22 -> 1.23) WITH the navigator at the gate.
</RULES>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: NAVIGATOR GATE - approve the canon amendment (now covering skills) before any canon byte</name>
  <what-built>
    Plans 01-04 are complete and green: the declaration contract + schema + full 125-surface backfill data
    exist (Plan 01, spanning 104 commands + 9 agents + 3 pipelines + 9 qualifying skills), every declaring
    surface carries its hitl_shape/hitl_why or hitl_stages frontmatter, and the 5 non-qualifying skills remain
    untouched, exempt via their existing connector.excluded:true+reason (Plan 02),
    scripts/check-shape-declaration.cjs validates all four surface classes and is unit-tested (Plan 03), and
    the gate is wired HARD-FAIL into pre-commit + release.sh + doctor --acceptance with tests/run-all-190.sh
    green (Plan 04). NO docs/MINDRIAN-CANON.md byte has been written yet.
  </what-built>
  <amendment-spec>
    The proposed amendment (WHAT, not the drafted prose) is:
    1. ONE new bullet, R16, appended to the closed CIRS ruling set in Part 11 (after R15, the current last
       member): "R16 Born-declared shape -- every invocable surface -- a command, an agent, a pipeline, OR a
       skill that reaches a genuine Decision-Gate fork -- is ALSO born with a DECLARED HITL SHAPE:
       `hitl_shape: F.x` (or the literal `none` with a reason, commands/agents/pipelines only) + `hitl_why:`
       for a single-fork surface, or `hitl_stages:` (an ordered {stage, shapes[], mode} list) for a
       multi-stage engine/pipeline/skill, justified against the closed decision rule (ordered/dependent ->
       F.9/F.2; independent/any-order set -> F.8; parallel branches -> F.5; single move/yes-no -> F.1/F.0;
       depth budget -> F.3; harvest scope -> F.4; plan review/JTBD -> F.6; ranked capability reaches -> F.7).
       A pure-capability / render-only skill with NO fork is exempt from declaring, provided it carries the
       EXISTING connector.excluded:true + reason (Canon Part 11 R1) -- reusing that signal rather than
       minting a parallel exemption. The gate scripts/check-shape-declaration.cjs fails the build CLOSED on a
       missing or a provably-contradicting declaration (the f-selector-ranker-consumer predicate) OR on a
       skill missing BOTH a declaration and a connector.excluded exemption (a skill-gap), mirroring R2's
       born-wired hard-FAIL. The total count of declaring surfaces is NEVER a frozen scalar; it is always
       enumerated from disk as commands + agents + pipelines + qualifying skills (125 as of this phase: 104 +
       9 + 3 + 9). Frozen Part 3 scalars (MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15 gate) are UNCHANGED by R16;
       it mandates declaration, it does not touch the shapes."
    2. ONE new Appendix D entry, 36, in the additive entry-25/27/31-35 house style, naming: the THIRD born-
       clause, the GIX single-select bug (Phase 188) as the motivating failure this closes, the closed
       R1-R15 -> R1-R16 set move, the implementing gate (scripts/check-shape-declaration.cjs), the backfill
       (data/hitl-shape-backfill.json, 125 surfaces), the navigator-directed skills fold-in (2026-07-01: 9 of
       14 skills qualify on the fork test, not the CIRS-wiring test; 5 are exempt via their existing
       connector.excluded), the frozen scalars restated UNCHANGED, and the version bump target 1.22 -> 1.23.
    3. Header/footer version bump: `Version: 1.22` -> `Version: 1.23`, footer `Mindrian Canon v1.22` ->
       `Mindrian Canon v1.23`.
    4. docs/CANON-PHASE-MAP.md: a new version-history row for v1.23 (Phase 190, R16, entry 36, skills
       fold-in) and the existing Phase 190 Part 11 section row (added in Plan 04-03) flipped from status
       "planned" to "shipped".
    5. A NEW canonical FLOOR test, tests/test-canon-entry-36-shape-declaration-floor.cjs, mirroring
       tests/test-canon-entry-31-two-gauge-floor.cjs's structure (sliceByHeader on `## Part 11` and
       `## Appendix D`, assert R16 present in the Part 11 slice AND that it names skills alongside commands/
       agents/pipelines, assert entry 36 present in the Appendix D slice naming "hitl_shape", "R16", and the
       skills fold-in, assert every prior Appendix D entry 1-35 still present by number, assert the
       header/footer version bump, assert the frozen scalars unchanged, assert the entry does NOT hardcode a
       surface count as a frozen scalar).
    6. tests/test-canon-entry-31-two-gauge-floor.cjs's Test 9 version-anchor bumped from 1.22 to 1.23 (its
       byte-for-byte scalar assertions on entries 1-30 stay unweakened; only the version-tracking regex moves
       forward, per the established every-bump lockstep pattern).
    7. tests/run-all-190.sh's placeholder run_if leg (Plan 04) now finds tests/test-canon-entry-36-shape-
       declaration-floor.cjs and runs it for real instead of SKIPping.
  </amendment-spec>
  <how-to-verify>
    1. Read the amendment-spec above and the current docs/MINDRIAN-CANON.md Part 11 (R1-R15) + Appendix D
       (entries 1-35).
    2. Confirm the version bump target (1.22 -> 1.23) or direct otherwise.
    3. Confirm R16's substance matches the shipped code from Plans 01-04, INCLUDING the skills scope (9
       qualifying of 14, exempt-via-connector.excluded for the rest) and the dynamic (never-hardcoded)
       surface-count principle.
    4. Confirm the frozen scalars stay byte-identical and no reach/edge/node/Brain-wire is minted.
  </how-to-verify>
  <action>
    HALT. Do NOT write any docs/MINDRIAN-CANON.md byte. Present the amendment-spec + the how-to-verify steps
    above to the navigator and WAIT for an explicit APPROVE (blocking gate; never auto-approvable;
    workflow.auto_advance is ignored for a constitutional ratification). On APPROVE (optionally carrying a
    different version target), proceed to Task 2; on anything else, apply the requested changes to the
    amendment-spec and re-present. No canon byte is written in this task.
  </action>
  <verify>
    <human-check>Navigator has typed "approved" (blocking-human); no canon byte written before that signal.</human-check>
  </verify>
  <resume-signal>Type "approved" (optionally with a different version target) to authorize the canon edit, or describe required changes.</resume-signal>
  <done>The navigator has explicitly APPROVED the amendment-spec (including its skills scope) and confirmed the version target; no docs/MINDRIAN-CANON.md byte was written in this task.</done>
</task>

<task type="auto">
  <name>Task 2: Write the atomic canon amendment (only after APPROVE)</name>
  <read_first>
    - docs/MINDRIAN-CANON.md Part 11 (lines 407-573, the CIRS closed ruling set R1-R15 to extend with R16;
      the Appendix D entries 31-35 additive house style to mirror for entry 36; the Version: header line)
    - docs/HITL-SHAPE-DECLARATION-CONTRACT.md (the shipped contract, now covering 4 surface classes, R16
      formally ratifies)
    - tests/test-canon-entry-31-two-gauge-floor.cjs (the FLOOR test structure to mirror for the new entry-36
      FLOOR test, and the Test-9 version anchor this task bumps)
    - tests/test-canon-frozen-scalars-floor.cjs (the guard that must stay GREEN, unedited, after this amendment)
  </read_first>
  <files>docs/MINDRIAN-CANON.md, docs/CANON-PHASE-MAP.md, tests/test-canon-entry-36-shape-declaration-floor.cjs, tests/test-canon-entry-31-two-gauge-floor.cjs, tests/run-all-190.sh</files>
  <action>
    ONLY after the Task-1 APPROVE: draft and write the amendment as ONE atomic lockstep exactly per the
    approved amendment-spec: append R16 to the Part 11 CIRS list (naming all four surface classes and the
    skills exemption rule), add Appendix D entry 36 (naming the skills fold-in), bump the header/footer
    version, add the CANON-PHASE-MAP.md version-history row and flip the Phase 190 Part 11 section row to
    "shipped", author tests/test-canon-entry-36-shape-declaration-floor.cjs mirroring
    tests/test-canon-entry-31-two-gauge-floor.cjs's slice-by-header + per-number-presence technique (never
    asserting a raw Appendix D entry count, and never asserting a hardcoded surface-count scalar), bump
    tests/test-canon-entry-31-two-gauge-floor.cjs's Test 9 version-anchor regexes from 1.22 to 1.23, and flip
    tests/run-all-190.sh's placeholder leg to a real `run` invocation of the new FLOOR test file. Restate the
    frozen invariants byte-identical throughout. No em-dashes in any added prose.
  </action>
  <verify>
    <automated>node tests/test-canon-frozen-scalars-floor.cjs</automated>
  </verify>
  <acceptance_criteria>
    <automated>node tests/test-canon-entry-36-shape-declaration-floor.cjs</automated>
    <automated>node tests/test-canon-entry-31-two-gauge-floor.cjs</automated>
  </acceptance_criteria>
  <done>Canon Part 11 carries R16 naming all four surface classes; Appendix D carries entry 36 naming the skills fold-in; header/footer version bumped as approved; docs/CANON-PHASE-MAP.md carries the new version-history row and the Phase 190 row is "shipped"; the new entry-36 FLOOR test passes; the entry-31 FLOOR test's version anchor is bumped and still passes; the frozen-scalar FLOOR test is still green; no em-dashes.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full phase verification sweep</name>
  <read_first>
    - tests/run-all-190.sh (Plan 04 + Task 2 output, now with every leg real)
  </read_first>
  <files>tests/run-all-190.sh</files>
  <action>
    Run the complete phase 190 verification aggregator plus the two adjacent doctor/release gates, and
    confirm every leg is green: the backfill generator's tests, the check-shape-declaration gate's own tests
    (including the skill-exempt and skill-gap fixtures), the check-shape-declaration --check run against the
    real tree (all four surface classes), and the new entry-36 canon FLOOR test. Then run `node
    scripts/doctor.cjs --acceptance` and confirm the shape-declaration gate entry reports ok. If any leg
    fails, diagnose and fix the root cause before declaring the phase complete; do not skip or soften a
    failing leg.
  </action>
  <verify>
    <automated>bash tests/run-all-190.sh</automated>
  </verify>
  <done>tests/run-all-190.sh is fully green (PASS on every leg, zero FAIL); node scripts/doctor.cjs --acceptance reports the shape-declaration gate ok.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autonomous agent -> constitution | an agent must not ratify canon; only the navigator authorizes the edit |
| amendment -> frozen invariants | the amendment must not drift any frozen scalar or mint a reach/edge/node |
| amendment -> surface-count literal | the ratified prose must not accidentally promote "125" into a frozen-scalar-style constant a future gate hardcodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-05-01 | Elevation | an agent writes canon bytes without navigator authority | mitigate | Task 1 is a blocking-human checkpoint; auto_advance ignored; no byte before APPROVE |
| T-190-05-02 | Tampering | frozen-scalar drift or an accidental reach/edge/node mint | mitigate | frozen-scalar FLOOR test byte-for-byte guard, verified unchanged in Task 2 |
| T-190-05-03 | Tampering | a prior Appendix D entry (1-35) mutated (non-additive edit) | mitigate | entry-25/27/31-35 additive house style; the entry-36 FLOOR test asserts every prior entry still present by number |
| T-190-05-04 | Tampering | the surface count (125) gets canonized as a frozen scalar by mistake | mitigate | the entry-36 FLOOR test explicitly asserts the ratified text frames the count as enumerated-from-disk, not a frozen constant, and the frozen-scalars FLOOR test's own list is untouched |
| T-190-05-SC | Tampering | npm/pip/cargo installs | accept | zero package installs |
</threat_model>

<verification>
- Task 1 HALTS until an explicit navigator APPROVE (blocking-human; never auto-approved).
- `node tests/test-canon-frozen-scalars-floor.cjs` GREEN after the edit (scalars byte-identical).
- `node tests/test-canon-entry-36-shape-declaration-floor.cjs` GREEN (R16 + entry 36 present, prior entries preserved, version bumped, skills fold-in named).
- `node tests/test-canon-entry-31-two-gauge-floor.cjs` GREEN with its version anchor at 1.23.
- `bash tests/run-all-190.sh` fully green.
</verification>

<success_criteria>
- Navigator APPROVE precedes every canon byte.
- R16 ratified additively into the closed R1-R16 CIRS set, spanning all four surface classes; Appendix D entry 36 added; prior entries 1-35 untouched.
- Frozen scalars byte-identical; version bumped as approved; no em-dashes.
- The full phase 190 verification sweep is green end to end.
</success_criteria>

## Artifacts this phase produces
- Amended `docs/MINDRIAN-CANON.md` (Part 11 R16 + Appendix D entry 36 + version bump 1.22 -> 1.23)
- Amended `docs/CANON-PHASE-MAP.md` (version-history row + Phase 190 row flipped to "shipped")
- `tests/test-canon-entry-36-shape-declaration-floor.cjs` (the new canonical FLOOR test leg)
- Re-affirmed `tests/test-canon-entry-31-two-gauge-floor.cjs` (version anchor bumped to 1.23)
- Fully green `tests/run-all-190.sh`

<output>
Create `.planning/phases/190-shape-f-declaration-mandate/190-05-SUMMARY.md` when done
</output>
