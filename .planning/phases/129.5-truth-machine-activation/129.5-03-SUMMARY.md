---
phase: 129.5-truth-machine-activation
plan: 03
subsystem: workflow
tags: [decision-gate, selector-dispatcher, confirm-node, confirmed-facts, canon-part-9, acceptance-test, truth-machine]

# Dependency graph
requires:
  - phase: 129.5-01
    provides: Canon Part 9 v1.5 audit-node carve-out (truth-claim vs system-bookkeeping node-type sets)
  - phase: 129.5-02
    provides: confirmNode(db,id,byUser,reason?) chokepoint + resolveByUser USER.md resolver + promoteNodeStatus human-attribution guard + navigation.cjs re-exports
  - phase: 109
    provides: getRoomHomeView / getConfirmedFacts / getRiskyAssumptions room-home driver + memory_event spine + fs-instrument acceptance idiom
  - phase: 125-06
    provides: recordSelectorDecision defer/reject dispatcher (the file this plan extends with an accept branch)
provides:
  - recordSelectorDecision accept/approve branch routing the Decision Gate APPROVE to navigation.confirmNode with resolveByUser byUser
  - getConfirmedFacts widened to truth-claim types IN ('confirmed','validated') so a human-confirmed node surfaces
  - getRiskyAssumptions narrowed to needs_evidence-only (disjointness invariant preserved)
  - tests/test-129.5-truth-machine.cjs instrumented acceptance test (4 load-bearing assertions + 11 unit cases)
  - tests/run-all-129.5.sh phase aggregator (2/2)
  - Feynman runner registration of both 129.5 suites
affects: [Phase 130 lens-engine accept, Phase 116 tension resolution, any future Decision Gate APPROVE caller]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Decision Gate APPROVE delegates EVERYTHING to the navigation chokepoint (confirmNode); selector-decisions.cjs touches room.db with ZERO raw SQL"
    - "Hostile-caller passthrough: an explicit byUser is forwarded verbatim to confirmNode so the AGENT_IDENTITIES guard is exercised end-to-end; absent byUser resolves from USER.md via resolveByUser"
    - "confirmedFacts disjointness preserved by review_status partition: {confirmed,validated} truth-claim set vs needs_evidence assumption set"
    - "Instrumented acceptance with a single allow-listed non-room.db read (USER.md) filtered out of fsInstrument.calls(), mirroring the Phase 129 two-cache-file filter"

key-files:
  created:
    - tests/test-129.5-truth-machine.cjs
    - tests/fixtures/phase-129.5/sample-room/seed.sql
    - tests/run-all-129.5.sh
  modified:
    - lib/workflow/selector-decisions.cjs
    - lib/core/navigation/room-home.cjs
    - lib/memory/run-feynman-tests.cjs

key-decisions:
  - "getConfirmedFacts widened to type IN {claim,CausalClaim,assumption,decision,opportunity} AND review_status IN ('confirmed','validated'); getRiskyAssumptions narrowed to assumption + needs_evidence only. A confirmed assumption cleared human review so it belongs in confirmedFacts; the two sets stay disjoint by review_status. The acceptance test confirms a decision node (no assumption is promoted) so the 109 disjointness contract is never reopened in the load-bearing path."
  - "The accept branch is added BEFORE the defer/reject validation gate so 'accept'/'approve' is a valid decision; the defer/reject branches are byte-unchanged."

patterns-established:
  - "Decision Gate APPROVE = recordSelectorDecision({decision:'accept', nodeId, roomState:{db, roomDir}}) -> {ok:true, decision_id} where decision_id is the status_promoted eventId"
  - "Future gates (130 lens-engine accept, 116 tension resolution) reuse this accept-branch shape rather than calling confirmNode directly"

requirements-completed: [TRUTH-D01-GATE-WIRING, TRUTH-CONFIRMED-FACTS, TRUTH-ACCEPTANCE]

# Metrics
duration: 6min
completed: 2026-05-31
---

# Phase 129.5 Plan 03: Decision Gate APPROVE -> confirmNode Wiring + confirmedFacts Contract + Instrumented Acceptance Summary

**The constitutional link closed: a human APPROVE at the Decision Gate is now the only path to a confirmed truth-claim node. The selector dispatcher's accept branch promotes a proposed truth-claim node to confirmed via navigation.confirmNode with USER.md byUser; confirmedFacts returns the freshly human-confirmed node; an agent-attributed confirm is REJECTED; every promotion emits a status_promoted memory_event; audit nodes are exempt. The truth machine is a running system. PHASE 129.5 COMPLETE.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-05-31
- **Completed:** 2026-05-31
- **Tasks:** 4 (TDD RED then GREEN, then aggregator + Feynman registration)
- **Files:** 6 (3 created, 3 modified)

## Accomplishments

- `recordSelectorDecision` in `lib/workflow/selector-decisions.cjs` gains an accept/approve branch (the F.0 Decision Gate APPROVE path). It reads `db` from `roomState.db`, requires a non-empty `nodeId`, resolves `byUser` (verbatim when supplied so a hostile-caller path exercises the AGENT_IDENTITIES guard, else `navigation.resolveByUser(roomState.roomDir)` from USER.md), and calls `navigation.confirmNode(db, nodeId, byUser, reason)`. Returns `{ok:true, decision_id}` (the status_promoted eventId) on success; surfaces `agent_attribution_forbidden` / `unknown_node` / `invalid_transition` / `state_mismatch` verbatim on failure. ZERO raw SQL, ZERO direct promoteNodeStatus: the branch touches room.db only through the navigation chokepoint. The defer/reject branches are byte-unchanged.
- `getConfirmedFacts` in `lib/core/navigation/room-home.cjs` widened from the 109 `validated`-only contract to type IN the full truth-claim set `{claim, CausalClaim, assumption, decision, opportunity}` AND `review_status IN ('confirmed','validated')`, so a freshly human-confirmed decision / claim / opportunity surfaces. `getRiskyAssumptions` narrowed to `assumption` + `needs_evidence` only, preserving the load-bearing disjointness invariant: a confirmed assumption cleared human review and now belongs to confirmedFacts, while riskyAssumptions is the needs_evidence set; the two state sets do not overlap. The 109 validated back-compat is preserved (a validated node still surfaces).
- `tests/test-129.5-truth-machine.cjs` ships the instrumented acceptance test (4 load-bearing assertions: human APPROVE promotes + confirmedFacts returns it; agent confirm REJECTED + node stays proposed + absent from confirmedFacts; status_promoted memory_event emitted with the human byUser + target_node_id; audit-node exemption via setFocus) plus 11 unit cases (accept branch promotes via confirmNode, resolves USER.md byUser, the 'approve' alias, explicit agent byUser rejected, unknown_node, invalid_db, invalid_node_id, defer/reject regression, confirmedFacts returns confirmed, validated back-compat, disjointness preserved). It mirrors the Phase 109/129 fs-instrument idiom; USER.md is the single allow-listed non-room.db read for this gate.
- `tests/fixtures/phase-129.5/sample-room/seed.sql`: a 4-node room (room + section + a proposed `decision` the APPROVE promotes + a proposed `claim` the agent-confirm is rejected against).
- `tests/run-all-129.5.sh`: the phase aggregator mirroring run-all-129.sh; runs test-129.5-confirm-node.cjs (Plan 02) + test-129.5-truth-machine.cjs (Plan 03); 2/2 green.
- `lib/memory/run-feynman-tests.cjs`: an additive Phase 129.5 registration block registering both 129.5 suites; every existing entry byte-unchanged.

## Task Commits

1. **Task 1 (RED): failing truth-machine acceptance + accept-branch suite + fixture** - `88c1602d` (test). RED: 2/12 (the accept branch and getConfirmedFacts widening absent).
2. **Task 2 (GREEN): accept branch + getConfirmedFacts widening** - `9293b272` (feat). 12/12 truth-machine.
3. **Task 3 (chore): run-all-129.5 aggregator + Feynman registration** - `298f61b0` (chore). Aggregator 2/2.

_TDD plan: RED proved the suite fails (accept branch + confirmedFacts widening absent), GREEN landed the implementation and turned the suite to 12/12. No REFACTOR commit needed (implementation was clean). The defer/reject regression case in the GREEN commit gained anchor-node seeding (a test-fixture fix, not a code change)._

## Verification Results

- `node tests/test-129.5-truth-machine.cjs` -> 12/12 (4 load-bearing acceptance assertions + 8 accept-branch/regression unit cases + 3 confirmedFacts contract cases). Exit 0.
- `node tests/test-129.5-confirm-node.cjs` (Plan 02) -> 19/19. Exit 0.
- `bash tests/run-all-129.5.sh` -> 2/2 green.
- `node tests/test-navigation-acceptance.cjs` (Phase 109 release gate) -> 1/1. ZERO regression.
- `bash tests/run-all-129.sh` (Phase 129) -> green. ZERO regression.
- `node tests/test-room-home-vs-brain-derivation-regression.cjs` -> 8/8 (the disjointness + Phase 90 fence held under the widened confirmedFacts contract).
- `node lib/memory/selector-decisions.test.cjs` -> 17/17 (defer/reject branches byte-unchanged behaviorally).
- `node scripts/check-substrate.cjs --diff` -> clean on the staged selector-decisions.cjs change (the accept branch routes only through navigation.cjs; the live pre-commit substrate guard passed on every commit, no --no-verify).
- Zero em-dashes across all files created/modified by this plan.

## Decisions Made

- The plan offered two scopings for the disjointness invariant. Chose the PREFERRED one: widen getConfirmedFacts to the truth-claim type set in `('confirmed','validated')` and narrow getRiskyAssumptions to `needs_evidence` only. This keeps the two sets disjoint by review_status partition (a confirmed assumption belongs to confirmedFacts since it cleared human review; the risky set is the needs_evidence assumptions). The load-bearing acceptance assertion confirms a `decision` node, so the 109 getRiskyAssumptions contract is never reopened in the load-bearing path.
- The accept branch was placed BEFORE the `decision !== 'defer' && decision !== 'reject'` validation gate so `accept`/`approve` is a recognized decision; everything after the accept branch (defer/reject) is byte-identical.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Test fixture] defer/reject regression case lacked the edge-write anchor nodes**
- **Found during:** Task 2 (GREEN), when the truth-machine suite ran the defer/reject regression case.
- **Issue:** The defer/reject branches write a typed cascade edge `cmd:<command> -> framework:<framework>`; edges FK to `nodes(id)`, so both anchor nodes must exist before the edge write. The new regression case in test-129.5-truth-machine.cjs did not seed them, so the defer call returned `{ok:false, reason:'edge_write_failed'}` (a test-fixture omission, NOT a code regression -- the defer/reject branch code is byte-unchanged).
- **Fix:** Added a `seedAnchorNode` helper in the test and seeded `cmd:mos:explore-domains` / `framework:SWOT` / `cmd:mos:challenge-assumptions` / `framework:Porter` before the defer/reject calls, matching the existing `lib/memory/selector-decisions.test.cjs` `seedAnchorNode` pattern.
- **Files modified:** tests/test-129.5-truth-machine.cjs
- **Verification:** test_deferRejectBranches_unchanged passes; the full suite is 12/12; the production selector-decisions unit suite is 17/17 (confirming the branches are unchanged).
- **Committed in:** 9293b272 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (test fixture only; zero production-code deviation)
**Impact on plan:** None. The plan's production wiring (accept branch + confirmedFacts widening) executed exactly as written. The single fix was a test-fixture FK seeding omission in a regression-only case.

## Out-of-Scope Discoveries

- `lib/memory/run-feynman-tests.cjs` carries 2 pre-existing em-dashes in Phase 121.5 / 127 comment blocks I did not touch. Per the scope boundary (only auto-fix issues directly caused by the current task's changes), these are NOT fixed by this plan. My additive 129.5 registration block has zero em-dashes (verified by a staged-diff grep).

## Issues Encountered

- None beyond the test-fixture FK seeding documented above.

## User Setup Required

None - no external service configuration required.

## Phase 129.5 Completion

This plan COMPLETES Phase 129.5 (Truth-Machine Activation), 3/3 plans shipped:
- 129.5-01: Canon Part 9 v1.5 audit-node carve-out (the constitutional foundation).
- 129.5-02: confirmNode chokepoint + human-attribution guard + resolveByUser (the lever).
- 129.5-03: Decision Gate APPROVE wiring + confirmedFacts contract + instrumented acceptance (the live loop).

After this plan, a human APPROVE at a gate is the ONLY path to a confirmed truth-claim node; confirmedFacts queries return real human-confirmed memory; an agent-initiated confirm is rejected; every promotion emits a memory_event. The truth machine stops being a design and becomes a running system (Canon Part 9 role 5, made live).

---
*Phase: 129.5-truth-machine-activation*
*Completed: 2026-05-31*

## Self-Check: PASSED

- FOUND: tests/test-129.5-truth-machine.cjs
- FOUND: tests/fixtures/phase-129.5/sample-room/seed.sql
- FOUND: tests/run-all-129.5.sh
- FOUND: .planning/phases/129.5-truth-machine-activation/129.5-03-SUMMARY.md
- FOUND commit: 88c1602d (RED test + fixture)
- FOUND commit: 9293b272 (GREEN accept branch + confirmedFacts widening)
- FOUND commit: 298f61b0 (aggregator + Feynman registration)
