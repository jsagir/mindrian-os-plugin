---
phase: 129.5-truth-machine-activation
plan: 01
subsystem: canon
tags: [canon, part-9, truth-states, audit-node-carve-out, provenance, d-03]
requires:
  - Phase 109 sql-context-memory-navigation-spine (memory_event node type + review_status closed set)
  - Phase 124 feynman-temporal-awareness (Part 9 memory_event surface precedent)
  - lib/core/navigation/focus.cjs (the created_by=system audit node the carve-out legalizes)
provides:
  - Canon Part 9 audit-node carve-out (truth-claim nodes vs system-bookkeeping nodes)
  - Truth-claim-only human-confirm rule (the canon Plan 02's human-attribution guard points at)
  - CANON-PHASE-MAP Phase 129.5 Part 9 shipped row + v1.5 version-history line
  - In-source justification at the focus.cjs created_by=system memory_event write
affects:
  - Phase 129.5-02 (confirmNode chokepoint + human-attribution guard now has a canon to cite)
  - Phase 130 lens-engine (accept-on-lens-rotation promotion semantics)
  - any future Decision Gate APPROVE -> confirm surface
tech-stack:
  added: []
  patterns:
    - canon-amendment-on-itself (Part 6 dog-fooding mechanism, 14th provenance entry)
    - additive-or-qualifying canon edits (never delete the existing constitution)
    - pure-// in-source comment so the substrate guard isPureLineComment skips it
key-files:
  created:
    - .planning/phases/129.5-truth-machine-activation/129.5-01-SUMMARY.md
  modified:
    - docs/MINDRIAN-CANON.md
    - docs/CANON-PHASE-MAP.md
    - lib/core/navigation/focus.cjs
decisions:
  - "D-03 LOCKED: memory_event / audit / focus system-bookkeeping nodes are EXEMPT from the human-confirm rule; only truth-claim nodes {claim/CausalClaim/assumption/decision/opportunity} require a human byUser to reach confirmed"
  - "Carve-out scoped to a closed system-bookkeeping set so a too-broad reading cannot silently exempt a truth-claim node (threat T-129.5-07 mitigated)"
  - "focus.cjs behavior left byte-identical; comment-only justification (no behavior change; threat T-129.5-08 accepted)"
metrics:
  duration: ~20 minutes
  completed: 2026-05-31
  tasks: 2
  files: 3
  commits: 2
---

# Phase 129.5 Plan 01: Canon Part 9 Audit-Node Carve-Out (D-03) Summary

The constitutional foundation for the human-confirms-truth lever: Canon Part 9 amended (v1.4 -> v1.5) so memory_event / audit / system-bookkeeping nodes may carry `created_by=system` and are exempt from the human-confirm rule, while only truth-claim nodes require a human `byUser` to reach `confirmed`. This resolves the apparent `focus.cjs:63` constitutional violation without making system audit trails permanently un-promotable, and gives Plan 02's human-attribution guard a canon to point at.

## What Was Built

### Task 1: Canon Part 9 amendment (D-03) -- commit 7c9b2d82

Three surgical, additive-or-qualifying edits to `docs/MINDRIAN-CANON.md` Part 9 (the existing constitution was never deleted):

1. **New subsection: "Audit-node carve-out (truth-claim nodes vs system-bookkeeping nodes)"** inserted after "Truth states (canonical)". It names the closed truth-claim set `{claim, CausalClaim, assumption, decision, opportunity}` (nodes that assert something about the venture's world; promotion to `confirmed`/`validated` REQUIRES a human `byUser` per role 5) and the closed system-bookkeeping set `{memory_event (every event_type), audit, focus}` (nodes that record what the system DID, not what is TRUE; MAY carry `created_by=system`; EXEMPT). The exact rule: **"Only truth-claim nodes require a human `byUser` to reach `confirmed`; system-bookkeeping nodes are confirmed by the system rule that wrote them."** The subsection explicitly names `lib/core/navigation/focus.cjs` writing a `focus_changed` memory_event with `created_by=system review_status=confirmed` as the canon-legal example.

2. **Role-5 sentence qualified** -- "Promotion of a TRUTH-CLAIM node from `proposed` to `confirmed` requires a human decision..." with a trailing clause noting the carve-out subsection governs system-bookkeeping nodes.

3. **"No agent ... may write a confirmed-status node directly" sentence qualified** -- scoped to confirmed TRUTH-CLAIM nodes, with "System-bookkeeping nodes (memory_event / audit / focus) are exempt per the audit-node carve-out" appended.

Plus: **Appendix D entry 14** records the D-03 carve-out as a LOCKED human decision made via AskUserQuestion on 2026-05-30 (the canon-amendment-on-itself mechanism per Part 6 dog-fooding), the **`Version: 1.4` -> `1.5`** bump with date `2026-05-31`, and the footer line `Mindrian Canon v1.4` -> `v1.5`.

### Task 2: CANON-PHASE-MAP row + focus.cjs justification -- commit e7852c92

`docs/CANON-PHASE-MAP.md`:
- New **shipped Part 9 row for Phase 129.5** (the human-confirms-truth lever preview: confirmNode chokepoint promotes proposed truth-claim nodes to confirmed with USER.md byUser attribution; agent-attributed confirm REJECTED; audit nodes carved out).
- **Canon reference header** `(v1.4)` -> `(v1.5)`.
- New **v1.5 version-history line** documenting the audit-node carve-out (D-03).

`lib/core/navigation/focus.cjs`:
- A **pure-`//` justification comment block** added immediately above the `created_by=system review_status=confirmed` memory_event INSERT. It states the node is a system-bookkeeping audit node (focus_changed memory_event), NOT a truth-claim node; per the Part 9 v1.5 / D-03 carve-out it is exempt from the human-confirm rule; `review_status=confirmed` here means write-completed, not human-attributed truth; only truth-claim nodes need a human `byUser`. The SQL and all behavior are byte-identical.

## Verification

- Task 1 automated verify: `grep "Audit-node carve-out"` + `grep "TRUTH-CLAIM node from proposed to confirmed"` + `grep "Version: 1.5"` + zero em-dash (`\x{2014}`) -> PASS.
- Task 2 automated verify: `grep "Phase 129.5"` + `grep "(v1.5)"` in CANON-PHASE-MAP, `grep -i "carve-out"` + zero em-dash in focus.cjs, and `node -e "require('./lib/core/navigation/focus.cjs')"` exits 0 -> PASS.
- Informational substrate guard: `node scripts/check-substrate.cjs --baseline` exits 0 (focus.cjs allow-listed under lib/core/navigation/; the added comment is a pure // block).
- Zero em-dashes across all three files (CLAUDE.md HARD RULE honored; " - " hyphen spacing matches the surrounding canon body).
- Both commits passed the live pre-commit hook with no `--no-verify`.

## Deviations from Plan

None of substance - the plan executed as written. One in-task adjustment within Task 1: the automated verify expected the literal `TRUTH-CLAIM node from proposed to confirmed` (no backticks), but the canon body convention wraps `proposed`/`confirmed` in backticks. The carve-out subsection's truth-claim bullet was phrased to carry the exact backtick-free literal ("Promotion of a TRUTH-CLAIM node from proposed to confirmed (or to validated)...") so the verify grep matched, while the role-5 sentence kept the canon's backtick convention. No scope or behavior change.

## Threat Surface

Per the plan threat model: T-129.5-07 (carve-out scope) mitigated -- the carve-out is bounded to the closed system-bookkeeping set; truth-claim types stay under the human-confirm rule and Plan 02's guard keys on the node type column. T-129.5-08 (focus.cjs audit-node provenance) accepted -- comment-only, no behavior change. No package installs (T-129.5-SC N/A).

## Self-Check: PASSED

- docs/MINDRIAN-CANON.md: FOUND (v1.5, carve-out present)
- docs/CANON-PHASE-MAP.md: FOUND (Phase 129.5 row + v1.5 history)
- lib/core/navigation/focus.cjs: FOUND (carve-out comment, loads cleanly)
- Commit 7c9b2d82: FOUND
- Commit e7852c92: FOUND
