---
phase: 129.5-truth-machine-activation
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/transitions.cjs
  - lib/core/navigation/confirm-node.cjs
  - lib/core/navigation.cjs
  - docs/architecture/SUBSTRATE-CONTRACT.md
  - tests/test-129.5-confirm-node.cjs
autonomous: true
requirements: [TRUTH-D01-CHOKEPOINT, TRUTH-D02-HUMAN-ATTRIBUTION, TRUTH-D02-USERMD-IDENTITY]
must_haves:
  truths:
    - "A single confirmNode(db, id, byUser) chokepoint helper owns the APPROVE->promote call; it is the only production caller of promoteNodeStatus"
    - "confirmNode and promoteNodeStatus REJECT larry / brain / system / assistant as byUser for confirm/validate transitions of truth-claim nodes"
    - "byUser is resolvable from the active room's USER.md navigator identity, uniform across CLI/Desktop/Cowork; a fresh empty USER.md resolves to a stable non-agent default (never larry/brain/system/assistant)"
    - "A successful confirmNode promotes proposed->confirmed and emits a status_promoted memory_event via promoteNodeStatus->logEvent"
  artifacts:
    - path: "lib/core/navigation/confirm-node.cjs"
      provides: "confirmNode chokepoint + resolveByUser(roomDir) USER.md identity resolver"
      contains: "confirmNode"
    - path: "lib/core/navigation/transitions.cjs"
      provides: "promoteNodeStatus with the human-attribution REJECT guard on confirm/validate of truth-claim nodes"
      contains: "AGENT_IDENTITIES"
    - path: "tests/test-129.5-confirm-node.cjs"
      provides: "RED-first behavior suite for confirmNode + the attribution guard + resolveByUser"
      contains: "confirmNode"
  key_links:
    - from: "lib/core/navigation/confirm-node.cjs"
      to: "lib/core/navigation/transitions.cjs promoteNodeStatus"
      via: "confirmNode calls promoteNodeStatus(db, id, 'proposed', 'confirmed', byUser, reason)"
      pattern: "promoteNodeStatus"
    - from: "lib/core/navigation.cjs"
      to: "lib/core/navigation/confirm-node.cjs"
      via: "thin re-export of confirmNode + resolveByUser"
      pattern: "confirmNode"
---

<objective>
Build the `confirmNode(db, id, byUser)` chokepoint helper (D-01) that owns the APPROVE -> promote call: every gate calls this one door; none call `promoteNodeStatus` directly. Add the human-attribution guard (D-02): `confirmNode` AND `promoteNodeStatus` REJECT `larry` / `brain` / `system` / `assistant` as `byUser` for confirm/validate transitions of truth-claim nodes. Add `resolveByUser(roomDir)` that reads the active room's USER.md navigator identity (D-02), uniform across CLI/Desktop/Cowork, with a stable non-agent default when USER.md is empty. Re-export both on `navigation.cjs` and amend the SUBSTRATE-CONTRACT M11 allow-list.

Purpose: This is the dead lever made live. `promoteNodeStatus` currently has ZERO production callers and accepts an unvalidated `byUser` defaulting to 'system' -- so even once wired, larry/brain/system could "confirm" truth (the exact Canon Part 9 role-5 failure). confirmNode is the single auditable door; the attribution guard makes "no agent writes a confirmed truth-claim node" structurally true, not just documented.
Output: lib/core/navigation/confirm-node.cjs, a guarded promoteNodeStatus, navigation.cjs re-exports, the SUBSTRATE-CONTRACT amendment, and a green behavior suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129.5-truth-machine-activation/129.5-CONTEXT.md

<interfaces>
From lib/core/navigation/transitions.cjs (the function this plan guards + confirmNode calls):
```
function promoteNodeStatus(db, nodeId, fromStatus, toStatus, byUser, reason)
  -> { ok: true, eventId } | { ok: false, reason }
// TRANSITIONS closed Set includes 'proposed->confirmed', 'confirmed->validated',
//   'needs_evidence->validated' (the confirm/validate family the guard covers).
// On confirm/validate it sets nodes.confirmed_by = byUser || 'system', confirmed_at, last_seen_at,
//   then logs a status_promoted memory_event via logEvent(db, eventType, {...created_by: byUser||'system'...}).
module.exports = { promoteNodeStatus, TRANSITIONS, EVENT_FOR_TRANSITION };
```

From lib/core/user-md-ops.cjs (the USER.md reader resolveByUser wraps):
```
function readUserMd(userMdPath)
  -> null (absent) | { ...struct, user_id, canonical_role, larry_persona, parse_failed } (parse_failed:true on empty/malformed)
// user_id and canonical_role are null on a fresh template USER.md (templates/room-skeleton/USER.md.tmpl has no frontmatter).
module.exports = { emptyUser, readUserMd, writeUserMdAtomic, detectPersonaUpdate, ... };
```

From lib/core/navigation/memory-events.cjs (the truth-claim node-type set lives near getConfirmedFacts in room-home; the type column on nodes):
```
// truth-claim node types per Canon Part 9 v1.5 carve-out (Plan 01):
//   claim, CausalClaim, assumption, decision, opportunity
// system-bookkeeping (EXEMPT): memory_event, audit, focus, and other navigation-internal nodes.
```
</interfaces>

<read_first>
- lib/core/navigation/transitions.cjs (FULL): the chokepoint confirmNode wraps. Note promoteNodeStatus signature (db, nodeId, fromStatus, toStatus, byUser, reason), the TRANSITIONS Set, and line 56 `byUser || 'system'` -- the unvalidated default the guard replaces with a REJECT on agent identities for confirm/validate.
- lib/core/navigation/focus.cjs (FULL): the precedent for a navigation submodule that opens its own work; confirm-node.cjs is a sibling submodule (allow-listed under lib/core/navigation/).
- lib/core/user-md-ops.cjs lines 218-322 (readUserMd): the reader resolveByUser wraps. user_id / canonical_role are the navigator-identity fields; both null on a fresh room.
- lib/core/navigation.cjs (FULL): the re-export surface + the per-export justification-comment pattern (see logSpineRead/writeEdge blocks). Add confirmNode + resolveByUser the same way.
- docs/architecture/SUBSTRATE-CONTRACT.md lines 64-127: the M11 export allow-list + the "### Amendments" section. Add an amendment naming confirmNode + resolveByUser, the phase (129.5), and the consumer (Plan 03 selector dispatcher / future gates 130/116).
- docs/MINDRIAN-CANON.md Part 9 v1.5 audit-node carve-out (shipped by Plan 01 this wave -- read it to mirror the truth-claim node-type set exactly).
- CLAUDE.md HARD RULE: NO em-dashes. Tri-Polar: confirmNode + resolveByUser are surface-uniform (CLI/Desktop/Cowork all carry USER.md), so a single roomDir-based resolver satisfies all three.
- tests/test-129-spine-substrate.cjs (header + setup) as the direct-CJS test idiom to mirror (node:assert/strict, openRoomDb fixture, FK-seed nodes before referencing them in edges/transitions).
</read_first>
</context>

<feature>
  <name>confirmNode chokepoint + human-attribution guard + USER.md identity resolver</name>
  <files>lib/core/navigation/confirm-node.cjs, lib/core/navigation/transitions.cjs, tests/test-129.5-confirm-node.cjs</files>
  <behavior>
    RED-first suite tests/test-129.5-confirm-node.cjs (direct-CJS, node:assert/strict, openRoomDb temp-dir fixture, FK-seed every referenced node first). Cases:

    AGENT-IDENTITY GUARD (promoteNodeStatus + confirmNode both):
    - Test: AGENT_IDENTITIES is exactly the closed lowercased Set {larry, brain, system, assistant}.
    - Test: promoteNodeStatus(db, claimNode, 'proposed', 'confirmed', 'larry') returns {ok:false, reason:'agent_attribution_forbidden'} and does NOT mutate review_status (still 'proposed').
    - Test: same REJECT for 'brain', 'system', 'assistant', and case-insensitively ('SYSTEM', 'Larry').
    - Test: same REJECT applies to the validate family: 'confirmed'->'validated' and 'needs_evidence'->'validated' with an agent byUser.
    - Test: the guard does NOT fire on non-confirm/non-validate transitions (e.g. 'proposed'->'rejected', 'confirmed'->'stale') -- an agent MAY reject/stale a node. Those still succeed with byUser='system'.
    - Test: the guard does NOT fire for a SYSTEM-BOOKKEEPING node type even on a confirm transition (a memory_event/focus/audit node with byUser='system' is allowed per the Part 9 v1.5 carve-out). The guard keys on the node's `type` column: only truth-claim types {claim, CausalClaim, assumption, decision, opportunity} are guarded.
    - Test: a human byUser (e.g. 'jonathan' or 'navigator') promotes proposed->confirmed successfully; review_status becomes 'confirmed'; confirmed_by == that byUser; a status_promoted memory_event row exists with created_by == that byUser.

    confirmNode(db, id, byUser, reason?):
    - Test: confirmNode resolves the node's CURRENT review_status from the db and calls promoteNodeStatus with the correct fromStatus (so the caller need not know the from-state). proposed->confirmed for a proposed node.
    - Test: confirmNode on an agent byUser returns {ok:false, reason:'agent_attribution_forbidden'} (delegates to the guard).
    - Test: confirmNode on an unknown node id returns {ok:false, reason:'unknown_node'}.
    - Test: confirmNode on a node whose current status has no 'X->confirmed' transition (e.g. already 'rejected') returns {ok:false, reason:'invalid_transition'} (delegates to promoteNodeStatus).
    - Test: confirmNode is the ONLY new production caller of promoteNodeStatus -- a source-grep assertion in the suite reads lib/ + scripts/ + commands/ and asserts the only files matching /promoteNodeStatus/ outside comments are navigation.cjs (re-export), transitions.cjs (definition), and confirm-node.cjs (the call). (Mirror the Phase 125-07 Test-7 source-grep-audit idiom.)

    resolveByUser(roomDir):
    - Test: a roomDir whose USER.md has user_id='jonathan' in frontmatter resolves to 'jonathan'.
    - Test: a roomDir whose USER.md has user_id=null but canonical_role='founder' resolves to 'founder'.
    - Test: a roomDir with the bare template USER.md (no frontmatter / parse_failed) resolves to the stable non-agent default 'navigator'.
    - Test: an absent USER.md (no file) resolves to 'navigator'.
    - Test: resolveByUser NEVER returns a value in AGENT_IDENTITIES; if USER.md somehow carries user_id='system', resolveByUser coerces it to 'navigator' (defense-in-depth so a poisoned USER.md cannot smuggle an agent identity into a confirm).
  </behavior>
  <implementation>
    GREEN after RED.

    transitions.cjs:
    - Add `const AGENT_IDENTITIES = Object.freeze(new Set(['larry', 'brain', 'system', 'assistant']));` and export it.
    - Add a truth-claim node-type set `const TRUTH_CLAIM_TYPES = Object.freeze(new Set(['claim', 'CausalClaim', 'assumption', 'decision', 'opportunity']));` (mirror Canon Part 9 v1.5 carve-out exactly).
    - In promoteNodeStatus, after the TRANSITIONS.has(key) check and after fetching the node row (which already SELECTs id, review_status -- extend it to also SELECT `type`): if toStatus is 'confirmed' OR 'validated' AND the node's type is in TRUTH_CLAIM_TYPES AND String(byUser||'').toLowerCase() is in AGENT_IDENTITIES, return {ok:false, reason:'agent_attribution_forbidden'} BEFORE any mutation. Keep the existing 'system' default for non-guarded paths (reject/stale/supersede and system-bookkeeping nodes) byte-compatible.
    - Do NOT change the existing success behavior, the memory_event emission, or the transaction shape.

    confirm-node.cjs (NEW, under lib/core/navigation/ so it is allow-listed):
    - require('./transitions.cjs') for promoteNodeStatus; require('../user-md-ops.cjs') for readUserMd; require('node:path').
    - confirmNode(db, id, byUser, reason): SELECT review_status FROM nodes WHERE id=? ; if absent return {ok:false, reason:'unknown_node'}; call promoteNodeStatus(db, id, currentStatus, 'confirmed', byUser, reason) and return its result verbatim (so agent_attribution_forbidden / invalid_transition / state_mismatch all surface unchanged).
    - resolveByUser(roomDir): readUserMd(path.join(roomDir, 'USER.md')); pick user_id (non-empty string) else canonical_role (non-empty string) else 'navigator'; then if the picked value lowercased is in AGENT_IDENTITIES, return 'navigator'. NEVER throws.
    - module.exports = { confirmNode, resolveByUser, NAVIGATOR_DEFAULT: 'navigator' };

    navigation.cjs:
    - require('./navigation/confirm-node.cjs') and re-export confirmNode + resolveByUser with a per-export justification comment block (cite Phase 129.5 / D-01 / D-02, name the consumer = Plan 03 selector dispatcher + future gates 130 lens-engine / 116 tension, same thin-re-export pattern as writeEdge/logSpineRead).

    SUBSTRATE-CONTRACT.md:
    - Add `confirmNode` and `resolveByUser` to the "### M11 export allow-list" bullet list.
    - Add a "Phase 129.5 (date)" amendment paragraph in "### Amendments" naming the two exports, the source (lib/core/navigation/confirm-node.cjs), and the consumer (Plan 03 Decision Gate APPROVE path; future gates 130/116). State that confirmNode is the single chokepoint for proposed->confirmed promotion and that promoteNodeStatus is no longer called directly by any production code outside confirm-node.cjs.

    Author all comment strings to avoid the substrate guard m4-cypher false-positive (no line containing the bare token "MATCH" adjacent to a ${...}); confirm-node.cjs writes NO Cypher and NO raw INSERT/UPDATE/DELETE on nodes/edges (it delegates all writes to promoteNodeStatus). NO em-dashes.
  </implementation>
  <verify>
    <automated>node tests/test-129.5-confirm-node.cjs && node -e "const n=require('./lib/core/navigation.cjs'); if(typeof n.confirmNode!=='function'||typeof n.resolveByUser!=='function'){process.exit(1)}" && node scripts/check-substrate.cjs --baseline >/dev/null 2>&1 && grep -q "confirmNode" docs/architecture/SUBSTRATE-CONTRACT.md && echo PASS</automated>
  </verify>
  <done>
    tests/test-129.5-confirm-node.cjs exits 0 (all behavior cases green, including the source-grep that proves confirmNode is the only new production caller of promoteNodeStatus). navigation.cjs re-exports confirmNode + resolveByUser. promoteNodeStatus REJECTS agent byUser on confirm/validate of truth-claim nodes with reason 'agent_attribution_forbidden' and leaves review_status unmutated. resolveByUser maps USER.md identity to a non-agent byUser, defaulting to 'navigator'. SUBSTRATE-CONTRACT M11 allow-list + amendment name both new exports. Zero em-dashes.
  </done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent (larry/brain/sub-agent/hook) -> truth-claim confirmation | An automated caller attempting to write a confirmed truth-claim node is the exact Canon Part 9 role-5 failure |
| USER.md content -> byUser identity | A poisoned/misconfigured USER.md could try to smuggle an agent identity into a human-attributed confirm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129.5-01 | Elevation of Privilege | promoteNodeStatus byUser on confirm/validate | mitigate | AGENT_IDENTITIES REJECT before any mutation; tested case-insensitively for larry/brain/system/assistant on both confirm and validate transitions of truth-claim node types |
| T-129.5-02 | Spoofing | resolveByUser reading USER.md | mitigate | resolveByUser coerces any agent-identity value in USER.md (user_id/canonical_role) to the 'navigator' default; never returns a value in AGENT_IDENTITIES |
| T-129.5-03 | Tampering | confirm-node.cjs writing room.db | accept | confirm-node.cjs delegates ALL writes to promoteNodeStatus (no raw SQL); allow-listed under lib/core/navigation/; substrate guard --baseline stays clean |
| T-129.5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (node built-ins only); slopcheck N/A |
</threat_model>

<verification>
- `node tests/test-129.5-confirm-node.cjs` exits 0.
- `node scripts/check-substrate.cjs --baseline` runs clean (confirm-node.cjs is allow-listed under lib/core/navigation/; no net-new bypass).
- The source-grep test proves promoteNodeStatus has exactly one new production caller (confirm-node.cjs).
- Zero em-dashes across all modified files.
</verification>

<success_criteria>
- confirmNode is the single chokepoint owning proposed->confirmed; it is the only production caller of promoteNodeStatus.
- promoteNodeStatus + confirmNode REJECT larry/brain/system/assistant (case-insensitive) on confirm/validate of truth-claim nodes; audit nodes are exempt.
- resolveByUser maps USER.md to a non-agent byUser, defaulting to 'navigator', never an agent identity.
- navigation.cjs re-exports both; SUBSTRATE-CONTRACT M11 names both with an amendment line.
</success_criteria>

<output>
Create `.planning/phases/129.5-truth-machine-activation/129.5-02-SUMMARY.md` when done.
</output>
