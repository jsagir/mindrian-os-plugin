---
phase: 129.5-truth-machine-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/MINDRIAN-CANON.md
  - docs/CANON-PHASE-MAP.md
  - lib/core/navigation/focus.cjs
autonomous: true
requirements: [TRUTH-D03-CANON-CARVEOUT, TRUTH-D03-FOCUS-JUSTIFY]
must_haves:
  truths:
    - "Canon Part 9 explicitly exempts memory_event / audit / system-bookkeeping nodes from the human-confirm rule"
    - "Only truth-claim nodes (claims, assumptions, decisions) require a human byUser to reach confirmed, per the amended canon"
    - "focus.cjs:63 writing created_by=system review_status=confirmed audit nodes is now canon-legal (carve-out), with an in-source justification pointing at the amended Part 9"
    - "CANON-PHASE-MAP carries a Phase 129.5 row under Part 9 and a version-history line"
  artifacts:
    - path: "docs/MINDRIAN-CANON.md"
      provides: "Part 9 audit-node carve-out amendment text + truth-claim-only confirm rule"
      contains: "memory_event"
    - path: "docs/CANON-PHASE-MAP.md"
      provides: "Phase 129.5 Part 9 row + version-history line"
      contains: "129.5"
    - path: "lib/core/navigation/focus.cjs"
      provides: "in-source justification comment for the created_by=system confirmed audit node, citing the Part 9 carve-out"
      contains: "carve-out"
  key_links:
    - from: "lib/core/navigation/focus.cjs"
      to: "docs/MINDRIAN-CANON.md Part 9 carve-out"
      via: "in-source comment citing the amendment"
      pattern: "Part 9"
---

<objective>
Amend Canon Part 9 (D-03): memory_event / audit / system-bookkeeping nodes MAY carry `created_by=system` and are EXEMPT from the human-confirm rule. Only truth-claim nodes (claims, assumptions, decisions) require a human `byUser` to reach `confirmed`. This resolves the `focus.cjs:63` constitutional violation (it writes `created_by=system review_status=confirmed` audit nodes) without making system audit trails permanently un-promotable. Add the matching CANON-PHASE-MAP row and a version-history line, and add an in-source justification comment at the `focus.cjs` audit-node write.

Purpose: The constitutional foundation Plan 02's human-attribution guard rests on. Plan 02 REJECTS larry/brain/system/assistant as byUser for confirm/validate transitions of TRUTH-CLAIM nodes; that rule is only coherent once the canon carves out audit nodes. This plan ships the carve-out FIRST (Wave 1) so the guard wired in Plan 02 has a canon to point at.
Output: Amended docs/MINDRIAN-CANON.md Part 9, a CANON-PHASE-MAP Part 9 row + version line, and a justified focus.cjs audit-node write.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129.5-truth-machine-activation/129.5-CONTEXT.md

<read_first>
- docs/MINDRIAN-CANON.md Part 9 (lines ~290-360): the five-role separation, the truth-states closed set, and the two load-bearing sentences this plan amends:
    - Line 317: "5. The human confirms truth. Promotion from proposed to confirmed requires a human decision..."
    - Line 326: "No agent (Brain, Larry, sub-agents, hooks) may write a confirmed-status node directly. Every confirmed node has a human attribution in its provenance."
  These are the sentences the carve-out must qualify (NOT delete) so they apply to truth-claim nodes while exempting audit nodes.
- lib/core/navigation/focus.cjs:61-64: the INSERT that writes a memory_event node with created_by='system' AND review_status='confirmed'. This is the D-03 carve-out target. The node is an audit node (focus_changed), NOT a truth-claim node. Do NOT change its behavior; ADD a comment citing the amended Part 9 carve-out.
- docs/CANON-PHASE-MAP.md "### Part 9 - Memory Locality and Interpretation" table (shipped rows for 108/109/124) + the "## Version history" table at the bottom. Add a Part 9 row for Phase 129.5 and a version-history line.
- CLAUDE.md HARD RULE: NO em-dashes anywhere. Use hyphens. The canon body already uses " - " hyphen-spacing; match it.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend Canon Part 9 with the audit-node carve-out (D-03)</name>
  <files>docs/MINDRIAN-CANON.md</files>
  <action>
    Amend Part 9 (Memory Locality and Interpretation) to carve out audit nodes, per CONTEXT D-03. Make THREE surgical edits, all additive-or-qualifying (never delete the existing constitution):

    (1) After the "### Truth states (canonical)" paragraph (the one listing the closed review_status set), add a new subsection "### Audit-node carve-out (truth-claim nodes vs system-bookkeeping nodes)". State: truth-claim nodes are the set {claim, CausalClaim, assumption, decision, opportunity} -- the nodes that assert something about the venture's world; their promotion to confirmed or validated REQUIRES a human byUser per role 5. System-bookkeeping nodes -- memory_event (every event_type), audit, focus, and other internal navigation nodes -- record what the system DID, not what is TRUE about the venture; they MAY carry created_by=system and are EXEMPT from the human-confirm rule. A system-bookkeeping node carrying review_status=confirmed is an internal write-completed marker, not a claim of human-attributed truth. Name the exact rule: "Only truth-claim nodes require a human byUser to reach confirmed; system-bookkeeping nodes are confirmed by the system rule that wrote them."

    (2) Qualify the role-5 sentence (currently line ~317): change "Promotion from proposed to confirmed requires a human decision" to "Promotion of a TRUTH-CLAIM node from proposed to confirmed requires a human decision" and append a clause noting the audit-node carve-out subsection governs system-bookkeeping nodes.

    (3) Qualify the "No agent ... may write a confirmed-status node directly" sentence (currently line ~326): scope it to truth-claim nodes -- "No agent (Brain, Larry, sub-agents, hooks) may write a confirmed-status TRUTH-CLAIM node directly; every confirmed truth-claim node has a human attribution in its provenance. System-bookkeeping nodes (memory_event / audit / focus) are exempt per the audit-node carve-out."

    Add a provenance bullet to "Appendix D - Canonization Provenance" (entry 14, after entry 13): note that Phase 129.5 (Truth-Machine Activation) added the audit-node carve-out so the human-confirms-truth lever could be wired without mislabeling system audit trails (focus.cjs:63), and that the carve-out was a LOCKED human decision (D-03) made via AskUserQuestion 2026-05-30.

    Bump the canon version: change "Version: 1.4" near the top to "Version: 1.5" and "Date:" to the execution date. Add the v1.5 row to Appendix D Version-history is NOT in this file -- the version-history TABLE lives in CANON-PHASE-MAP.md (Task 2 owns it). NO em-dashes; use " - " hyphen spacing matching the surrounding canon body.
  </action>
  <verify>
    <automated>grep -c "Audit-node carve-out" docs/MINDRIAN-CANON.md | grep -qv '^0$' && grep -q "TRUTH-CLAIM node from proposed to confirmed" docs/MINDRIAN-CANON.md && grep -q "Version: 1.5" docs/MINDRIAN-CANON.md && ! grep -qP "\\x{2014}" docs/MINDRIAN-CANON.md && echo PASS</automated>
  </verify>
  <done>Part 9 carries the audit-node carve-out subsection; the role-5 sentence and the no-agent sentence are scoped to truth-claim nodes; canon version is 1.5; Appendix D entry 14 records the D-03 carve-out; zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: Add the CANON-PHASE-MAP Part 9 row + version-history line, and justify focus.cjs:63</name>
  <files>docs/CANON-PHASE-MAP.md, lib/core/navigation/focus.cjs</files>
  <action>
    CANON-PHASE-MAP.md: (a) In "### Part 9 - Memory Locality and Interpretation", add a row: status "shipped", phase/component "Phase 129.5 truth-machine-activation (the human-confirms-truth lever wired: confirmNode chokepoint promotes proposed truth-claim nodes to confirmed with USER.md byUser attribution; agent-attributed confirm REJECTED; audit nodes carved out per the Part 9 amendment)", reference ".planning/phases/129.5-truth-machine-activation/129.5-CONTEXT.md". (b) Update the "Canon reference:" header line near the top from "docs/MINDRIAN-CANON.md (v1.4)" to "(v1.5)". (c) Add a row to the "## Version history" table at the bottom: canon version "v1.5", commit "TBD", date (execution date), amendment "Part 9 audit-node carve-out (D-03) -- truth-claim nodes require human byUser to reach confirmed; memory_event / audit / focus system-bookkeeping nodes exempt. Phase 129.5 truth-machine-activation wires the lever (confirmNode chokepoint + USER.md attribution guard). CANON-PHASE-MAP Part 9 row flipped to shipped for Phase 129.5."

    lib/core/navigation/focus.cjs: at the INSERT that writes the memory_event node with created_by='system' and review_status='confirmed' (currently lines 61-64), ADD a comment immediately above it (do NOT change the SQL or any behavior). The comment must state: this is a system-bookkeeping audit node (focus_changed memory_event), NOT a truth-claim node; per Canon Part 9 audit-node carve-out (v1.5, Phase 129.5 / D-03) it is EXEMPT from the human-confirm rule and MAY carry created_by=system review_status=confirmed; the confirmed status here means write-completed, not human-attributed truth. Keep the comment a pure // line block so the substrate guard's isPureLineComment skips it; avoid the literal token sequence "INSERT INTO nodes" inside any non-comment context you add (the existing INSERT statement is unchanged and is allow-listed because lib/core/navigation/ is in ALLOWED_DIRECT_IMPORT). NO em-dashes.
  </action>
  <verify>
    <automated>grep -q "Phase 129.5" docs/CANON-PHASE-MAP.md && grep -q "(v1.5)" docs/CANON-PHASE-MAP.md && grep -i -q "carve-out" lib/core/navigation/focus.cjs && ! grep -qP "\\x{2014}" lib/core/navigation/focus.cjs && node -e "require('./lib/core/navigation/focus.cjs')" && echo PASS</automated>
  </verify>
  <done>CANON-PHASE-MAP has a shipped Part 9 row for Phase 129.5, a (v1.5) header, and a v1.5 version-history line; focus.cjs carries the carve-out justification comment above the audit-node INSERT and still loads (require succeeds); zero em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canon constitution -> implementing code | The Part 9 amendment governs what Plan 02's guard may exempt; a too-broad carve-out could legalize an agent-confirmed truth-claim node |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129.5-07 | Tampering | Part 9 carve-out scope | mitigate | The carve-out is scoped to the closed system-bookkeeping set (memory_event / audit / focus); truth-claim types (claim / CausalClaim / assumption / decision / opportunity) stay under the human-confirm rule. Plan 02's guard keys on the node type column, so a too-broad carve-out cannot silently exempt a truth-claim node |
| T-129.5-08 | Repudiation | focus.cjs audit-node provenance | accept | focus.cjs writes created_by=system on an audit node by design; the carve-out documents this as write-completed, not human-attributed truth. No behavior change; comment-only |
| T-129.5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (docs + one comment); slopcheck N/A |
</threat_model>

<verification>
- Canon Part 9 scopes the human-confirm rule to truth-claim nodes and exempts system-bookkeeping nodes (carve-out subsection present).
- focus.cjs behavior is byte-identical except for an added pure-comment block; `node -e "require('./lib/core/navigation/focus.cjs')"` exits 0.
- Substrate guard is not newly tripped: focus.cjs is allow-listed (lib/core/navigation/) so even the existing INSERT is exempt; the added comment is a pure // block. Confirm with: `node scripts/check-substrate.cjs --baseline >/dev/null 2>&1; echo exit=$?` (informational, exits 0).
- Zero em-dashes across all three files.
</verification>

<success_criteria>
- docs/MINDRIAN-CANON.md is v1.5 with the Part 9 audit-node carve-out and the two qualified constitution sentences.
- docs/CANON-PHASE-MAP.md has the Phase 129.5 Part 9 row + (v1.5) header + version-history line.
- lib/core/navigation/focus.cjs carries the in-source carve-out justification and loads cleanly.
- No em-dashes anywhere.
</success_criteria>

<output>
Create `.planning/phases/129.5-truth-machine-activation/129.5-01-SUMMARY.md` when done.
</output>
