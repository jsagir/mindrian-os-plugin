# Phase 188.1 - Shape-F elevation labels (quick phase under 188)

> STATUS: COMPLETE 2026-07-01. Fix landed in lib/hmi/dial-label-composer.cjs (elevation dimension on all 7 families + ELEVATION_DEFAULTS + degraded-path repoint); zero presenter change needed (dial-presenter.cjs renders composeLabel output verbatim). Gate: tests/run-all-188.sh 14/14 PASS (adds the run_if leg tests/test-188.1-elevation-labels.cjs). Existing dial tests green (drift, render-states 14/14, end-to-end 11 checks). Chose the elevation-framed-default branch over render-no-dial. Prototype wording pending the Part 12 canon amendment (205 deliverable). No em-dashes.


> Class: CODE (quick) | Priority: P1 | Parent: Phase 188 (Shape-F selector) | Registered 2026-07-01 (navigator-directed: "c add to 188 as a quick phase")
> Durable source in the phase dir (roadmap body edits do not persist on this repo's cascade; register as 188.1 via /gsd-phase --insert during the roadmap-hygiene pass).
> No em-dashes.

## Goal

Fix the Shape-F selector defect Lawrence Aronhime named: "Your Shape F does not tell me what I will get or how my thinking improves." The LIVE selector prints mechanism-blank rows ("No specific job - general thinking, talking, ranging. 50%"), contradicting the shipped doctrine that rows are Feynman-JTBD what-you-get aliases. Reframe every selector row as the ELEVATION the navigator receives and the OUTCOME to their thinking.

## The fix (elevation-as-vocabulary)

Each Shape-F row states the elevation direction + outcome (Canon Part 12 "three directions of elevation", amended 2026-07-01):
- vertical  -> "Go a level deeper on X - see what's under the surface"
- horizontal -> "Connect X and Y you're holding as separate - they may be one argument"
- lateral   -> "Bring in Z from outside your frame - a reference you wouldn't have found"

Never a mechanism-blank label. The canonical_verb still persists to the graph edge, never to the screen (existing dial-label-composer separation). Do not surface a literal "(Recommended)"; the filled-triangle glyph is the marker.

## Scope

- lib/hmi/dial-label-composer.cjs - the label composer emits elevation-framed what-you-get strings; add the elevation-direction dimension to the alias templates.
- The F.1 / F.7 render (Phase 188 selector-dispatcher) - rows carry the elevation label.
- The engine arm that currently emits "No specific job - general thinking, talking, ranging" (the tier-0 / no-fired-sensor fallback) - replace the blank fallback with an elevation-framed default OR render no dial (per the Phase 150.5 cold-turn rule). This blank string is the live symptom Lawrence saw every turn.

## Relationship to Phase 205

205 owns the elevation MODEL (canon Part 12 taxonomy + the FUSION/gear engine). 188.1 is the UI slice: make the SELECTOR speak the elevation vocabulary. 188.1 can ship ahead of 205's engine so Lawrence sees "what I get" on the next dial render (his direct ask). Depends on the Part 12 amendment (205 canon deliverable) for the taxonomy wording; can prototype against the drafted subsection now.

## Verification

A rendered Shape-F selector shows no "No specific job" / mechanism-blank rows; every row names an elevation direction + a concrete outcome. Voice-color mark intact (Part 12). No literal "(Recommended)" string.

## Canon

Part 12 (three directions of elevation + the surface obligation), Part 3 (Shape-F render, frozen scalars untouched), Part 8 (labels carry no user egress). No em-dashes.
