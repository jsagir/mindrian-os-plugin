---
phase: 89.2
plan: "08"
slug: feynman-registration-and-phase-gate
type: execute
wave: 4
depends_on:
  - "89.2-01"
  - "89.2-02"
  - "89.2-03"
  - "89.2-04"
  - "89.2-05"
  - "89.2-06"
  - "89.2-07"
files_modified:
  - lib/memory/run-feynman-tests.cjs
  - .planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md
  - .planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md
autonomous: true
canon_parts: [part-7, part-8]
milestone: v1.11.0
bundled_release: true
success_criteria_addressed: [SC-12, SC-13]

must_haves:
  truths:
    - "All 11 new Phase 89.2 test files registered in lib/memory/run-feynman-tests.cjs TEST_FILES array (single sweep, no Wave-2/Wave-3 plan-level contention)"
    - "11 NEW test files: test-rs-egress-primitives + test-rs-fetcher-academic + test-rs-fetcher-patents + test-rs-fetcher-industry + test-rs-fetcher-experts + test-rs-pinecone-bridge (NEW standalone in Plan 06 Task 1) + test-rs-preprocessor + test-rs-differential-scorer + test-rs-innovation-classifier + test-rs-breakthrough-scorer + test-rs-thesis-generator. Baseline 66 -> 77."
    - "Phase Gate transcript at .planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md mirrors 89.1-PHASE-GATE.md format: G1-G9 + E1-E5 asserts (E1-E5 are 89.2-specific: each new fetcher passes adversarial Canon Part 8 fixture sweep + per-source rate-limit graceful degradation verified)"
    - "Phase 89.2 VERIFICATION.md mirrors 89.1-VERIFICATION.md format: 13 SCs with PASS/FAIL evidence per SC"
    - "All 13 SCs from ROADMAP have direct evidence pointers in VERIFICATION.md"
    - "Phase Gate PASS gate: zero G/E asserts FAIL; full Feynman 77/77 PASS"
    - "NO release artifacts touched (CHANGELOG / plugin.json / package.json all byte-identical at HEAD vs Plan 89.2-01 base commit per bundled_release: true)"
  artifacts:
    - path: lib/memory/run-feynman-tests.cjs
      provides: "+11 TEST_FILES entries (one per new 89.2 test file); baseline 66 -> 77"
      min_lines: 511
    - path: .planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md
      provides: "Phase Gate transcript with 9 G + 5 E asserts; PASS/FAIL evidence per assert; mirrors 89.1-PHASE-GATE.md byte-for-byte structure"
      min_lines: 200
    - path: .planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md
      provides: "13 SCs from ROADMAP with PASS/FAIL evidence; mirrors 89.1-VERIFICATION.md format"
      min_lines: 150
  key_links:
    - from: ".planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md"
      to: ".planning/phases/89.1-domain-analysis-and-query-matrix/89.1-PHASE-GATE.md"
      via: "Phase Gate template inheritance; 89.2 adds E asserts but mirrors G1-G9 format"
      pattern: "G[1-9]"
    - from: ".planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md"
      to: ".planning/ROADMAP.md (Phase 89.2 Success Criteria 1-13)"
      via: "13 SC evidence pointers"
      pattern: "SC-[0-9]+"
    - from: "lib/memory/run-feynman-tests.cjs"
      to: "lib/memory/test-rs-*.cjs (11 new files from Plans 89.2-01 through 89.2-07)"
      via: "TEST_FILES registry"
      pattern: "test-rs-"
---

<objective>
Close Phase 89.2 with a single sweep that:

1. **Registers all 11 new test files** from Plans 89.2-01 through 89.2-07 into `lib/memory/run-feynman-tests.cjs` TEST_FILES array. This sweep approach avoids parallel-plan contention on the registry (Plans 89.2-02/03/04/05/06/07 ran in parallel and never touched run-feynman-tests.cjs to avoid merge conflicts on the same array). The +1 from the prior planning pass reflects Plan 06's revision: pinecone-bridge gained its own standalone test file (lib/memory/test-rs-pinecone-bridge.cjs) per checker-revision W3 Option (i), giving the bridge its own crash-safety net independent of differential-scorer test orchestration.

2. **Files Phase 89.2 Phase Gate transcript** at `.planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md` mirroring `89.1-PHASE-GATE.md` byte-for-byte structure. 9 G asserts (BSL 1.1 + em-dashes + Canon Part 8 forbidden-field audit + chokepoint exclusivity + Feynman registration + suite green + zero release artifacts + zero new deps + ExternalEgressViolation surface map) plus 5 NEW E asserts specific to 89.2 (per-fetcher adversarial fixture suite + per-source rate-limit graceful degradation + strict 1024-dim cosine via existing rs_cache.py + auditQueryObject pre-return defense across all new modules + integration smoke).

3. **Files Phase 89.2 VERIFICATION.md** at `.planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md` listing all 13 ROADMAP SCs with PASS/FAIL evidence per SC. Mirror `89.1-VERIFICATION.md` format.

NO release artifacts touched per bundled_release: true. v1.11.0 ships at Phase 91.x.

Purpose: Ships SC-12 (Feynman test suite stays green + new test files registered; baseline 65 -> 75+ ROADMAP wording, actual 66 -> 77 in this plan; the +1 difference is 89.1's actual count divergence which planner corrects in VERIFICATION.md, plus +1 from the standalone pinecone-bridge test file added during checker revision) + SC-13 (NO release gate -- bundled into v1.11.0 final ship at Phase 91.x; verified by zero release artifact diffs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89.2-external-research-fetching/89.2-CONTEXT.md
@.planning/phases/89.2-external-research-fetching/89.2-01-egress-primitives-PLAN.md
@.planning/phases/89.2-external-research-fetching/89.2-02-fetcher-academic-PLAN.md
@.planning/phases/89.2-external-research-fetching/89.2-03-fetcher-patents-PLAN.md
@.planning/phases/89.2-external-research-fetching/89.2-04-fetcher-industry-PLAN.md
@.planning/phases/89.2-external-research-fetching/89.2-05-fetcher-experts-PLAN.md
@.planning/phases/89.2-external-research-fetching/89.2-06-preprocessor-and-differential-scorer-PLAN.md
@.planning/phases/89.2-external-research-fetching/89.2-07-classifier-breakthrough-thesis-PLAN.md
@.planning/phases/89.1-domain-analysis-and-query-matrix/89.1-PHASE-GATE.md
@.planning/phases/89.1-domain-analysis-and-query-matrix/89.1-VERIFICATION.md
@lib/memory/run-feynman-tests.cjs
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- New test files to register (11 from Plans 89.2-01 through 89.2-07): -->

```javascript
// Append to TEST_FILES array in lib/memory/run-feynman-tests.cjs (after current
// final entry at lib/memory/test-rs-query-matrix.cjs from Phase 89.1-05).
// Each entry: comment block (3-5 lines explaining what the suite covers) + path.join.

// Phase 89.2-01: shared egress primitives (ExternalEgressViolation + FORBIDDEN_PATTERNS re-export + telemetry)
path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-egress-primitives.cjs'),

// Phase 89.2-02: academic fetcher (6 sources: openalex+arxiv+scopus+pubmed+ieee+nature) + per-source budget + Canon Part 8 chokepoint
path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-fetcher-academic.cjs'),

// Phase 89.2-03: patents fetcher (Google Patents + USPTO) + Canon Part 8 chokepoint
path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-fetcher-patents.cjs'),

// Phase 89.2-04: industry fetcher (Tavily orchestration with 2-layer audit, 4 adversarial scenarios) + Canon Part 8 chokepoint
path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-fetcher-industry.cjs'),

// Phase 89.2-05: experts post-processor (no egress; auditQueryObject defense in depth; 4 adversarial scenarios)
path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-fetcher-experts.cjs'),

// Phase 89.2-06 Task 1: pinecone bridge standalone (4 scenarios: API key absent, python absent, happy path, cosineSimilarity identity)
path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-pinecone-bridge.cjs'),

// Phase 89.2-06 Task 2: preprocessor (Phase 2 deterministic 5-field-per-document) + auditQueryObject pre-return
path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-preprocessor.cjs'),

// Phase 89.2-06 Task 3: differential scorer (Phase 3 dual-floor diff>0.3 AND lsa>0.2 AND bert>0.2) + per-pair LSA Python bridge (Canon Part 7 carve-out documented) + Pinecone-direct via rs_cache.py
path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-differential-scorer.cjs'),

// Phase 89.2-07: innovation classifier (3-enum deterministic on differential scorer output)
path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-innovation-classifier.cjs'),

// Phase 89.2-07: breakthrough scorer (5-dimension 0-10 deterministic rubric)
path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-breakthrough-scorer.cjs'),

// Phase 89.2-07: thesis generator (Phase 4 pure-template-fill)
path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-thesis-generator.cjs'),
```

Phase Gate transcript template (mirror 89.1-PHASE-GATE.md):
```markdown
----
phase: 89.2
slug: external-research-fetching
plan: "08"
doc_type: phase-gate-transcript
status: PASS  # or FAIL if any G/E fails
executed_at: "<ISO-8601>"
milestone: v1.11.0
bundled_release: true
canon_parts: [part-7, part-8]
----

# Phase 89.2 Phase Gate Transcript

Base commit for diff: <base hash; first commit of Plan 89.2-01>
HEAD at time of assert run: <hash; last commit before this PHASE-GATE.md is committed>

## Gate Summary
- Total asserts: 14 (9 G + 5 E)
- Passed: <N>
- Failed: <14 - N>
- Status: <PASS|FAIL>

## Assert Transcript

### G1: Zero new runtime deps in package.json (across 89.2 commit range)
### G2: BSL 1.1 headers on every new/modified .cjs (and on every embedded Python script literal)
### G3: Zero em-dashes on every new/modified file
### G4: Canon Part 8 audit (forbidden field references) on production .cjs
### G5: Chokepoint exclusivity preserved (89.1a + 89.1 + 89.2 fetchers all chokepoint single-function)
### G6: New Feynman suites registered (11 entries from 89.2)
### G7: Each new fixture suite green (direct invocation)
### G8: Full Feynman runner green
### G9: ExternalEgressViolation surfaces only in 89.1 + 89.2 modules (NOT in 89.1a substrate loader)

## New 89.2-Specific Asserts (E1-E5)

### E1: Per-fetcher adversarial Canon Part 8 fixture suites green (>=4 adversarial scenarios per egress fetcher: 02, 03, 04 = 12 total; plus 4 per defense-in-depth post-processor: 05)
### E2: Per-source rate-limit graceful degradation verified across all 3 egress fetchers
### E3: Strict 1024-dim cosine via existing rs_cache.py confirmed (Option a from CONTEXT.md, Canon Part 7 reuse)
### E4: auditQueryObject pre-return defense across all new modules (preprocessor + experts + classifier + breakthrough + thesis)
### E5: Phase 2 + Phase 3 + Phase 4 chain integration smoke (preprocess -> score -> classify -> scoreBreakthrough -> generateThesis end-to-end with mocked HTTP layer)
```

VERIFICATION.md template (mirror 89.1-VERIFICATION.md):
```markdown
# Phase 89.2 Verification

Status: PASS (13/13 SCs verified) | FAIL (X/13 SCs verified)
Executed at: <ISO-8601>

## Success Criteria Evidence

### SC-01: rs-fetcher-academic.cjs exports fetchAcademic returning deduped paper objects
Evidence: `node -e "const {fetchAcademic} = require('./lib/core/rs-fetcher-academic.cjs'); console.log(typeof fetchAcademic === 'function' ? 'PASS' : 'FAIL');"` -> PASS
File: lib/core/rs-fetcher-academic.cjs lines X-Y

### SC-02: rs-fetcher-patents.cjs exports fetchPatents
... (and so on for all 13 SCs)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 11 new test files in run-feynman-tests.cjs</name>
  <files>lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - lib/memory/run-feynman-tests.cjs (current TEST_FILES array; verify final entry at line 464 = 89.1's test-rs-query-matrix.cjs)
    - .planning/phases/89.1-domain-analysis-and-query-matrix/89.1-05-SUMMARY.md (Plan 89.1-05 Task 1 -- the equivalent registration sweep for Phase 89.1's 3 new test files)
    - .planning/phases/89.2-external-research-fetching/89.2-CONTEXT.md (Test Strategy section -- expected test file count)
  </read_first>
  <action>
    1. Read lib/memory/run-feynman-tests.cjs and locate the current final TEST_FILES entry (should be `path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-query-matrix.cjs'),` at approximately line 464).

    2. Append 11 new entries IMMEDIATELY AFTER the test-rs-query-matrix.cjs entry, BEFORE the closing `];` of the TEST_FILES array. Each entry has a 3-5 line comment block (mirror 89.1 Plan 05's comment style) followed by the path.join line. Order:
       - test-rs-egress-primitives.cjs (Plan 89.2-01)
       - test-rs-fetcher-academic.cjs (Plan 89.2-02)
       - test-rs-fetcher-patents.cjs (Plan 89.2-03)
       - test-rs-fetcher-industry.cjs (Plan 89.2-04)
       - test-rs-fetcher-experts.cjs (Plan 89.2-05)
       - test-rs-pinecone-bridge.cjs (Plan 89.2-06 Task 1; standalone NEW from W3 Option (i) revision)
       - test-rs-preprocessor.cjs (Plan 89.2-06 Task 2)
       - test-rs-differential-scorer.cjs (Plan 89.2-06 Task 3)
       - test-rs-innovation-classifier.cjs (Plan 89.2-07 Task 1)
       - test-rs-breakthrough-scorer.cjs (Plan 89.2-07 Task 2)
       - test-rs-thesis-generator.cjs (Plan 89.2-07 Task 3)

    3. Use the comment style from 89.1 Plan 05 (run-feynman-tests.cjs lines 432-464). Each comment block starts with `// Phase 89.2-NN:` and explains in 1-2 sentences what the suite covers.

    4. Verify file edit: `grep -c "test-rs-" lib/memory/run-feynman-tests.cjs` should advance by 11 from baseline (currently 4 from 89.1a/89.1: brain-substrate, brain-substrate-shape, domain-analyzer, query-matrix). Post-89.2: 15 test-rs-* entries total.

    5. Run full Feynman: `node lib/memory/run-feynman-tests.cjs > /tmp/89.2-feynman.log 2>&1; echo "exit=$?"`. EXPECT exit 0 with final line `Feynman test runner: 77/77 passed, 0 skipped, 0 failed`. NOTE: ROADMAP says baseline 65 -> 73+; the actual baseline is 66 (post-89.1) per the live run; this plan advances 66 -> 77 (delta +11; +1 from W3 Option (i) standalone pinecone-bridge test). Document the +1 baseline discrepancy and the +1 W3-revision delta in VERIFICATION.md SC-12 section. The 4 pre-existing failures from `node lib/memory/run-feynman-tests.cjs` reported during planning (62/66 passed, 4 failed) are unrelated to 89.x work; this plan does NOT fix them but DOCUMENTS them in PHASE-GATE.md G8 as "non-89.2-introduced flakes; documented separately for milestone follow-up". If those 4 failures still occur after 89.2 plans land, G8 status is CONDITIONAL PASS with documented mitigation.

    6. Commit `--no-verify`: `test(89.2-08): register 11 new 89.2 fixture suites in Feynman runner; baseline 66 -> 77`.
  </action>
  <verify>
    <automated>node lib/memory/run-feynman-tests.cjs</automated>
  </verify>
  <acceptance_criteria>
    - run-feynman-tests.cjs has 11 NEW test-rs-* entries appended: `grep -c "test-rs-" lib/memory/run-feynman-tests.cjs` returns 15 (4 baseline + 11 new)
    - Each new entry follows path.join(REPO_ROOT, 'lib', 'memory', '...') pattern: `grep -cE "REPO_ROOT, 'lib', 'memory', 'test-rs-(egress-primitives|fetcher-academic|fetcher-patents|fetcher-industry|fetcher-experts|pinecone-bridge|preprocessor|differential-scorer|innovation-classifier|breakthrough-scorer|thesis-generator)\.cjs'" lib/memory/run-feynman-tests.cjs` returns 11
    - Comment blocks present: `grep -cE "Phase 89.2-0[1-7]" lib/memory/run-feynman-tests.cjs` >= 11 (one comment per new entry)
    - Full Feynman runner exits 0 OR exits with the same 4 pre-existing failures + 0 NEW failures (if pre-existing failures are confirmed pre-89.2, document in PHASE-GATE.md): `node lib/memory/run-feynman-tests.cjs 2>&1 | tail -1 | grep -cE "(77/77 passed|73/77 passed.*4 failed)"` returns 1
    - All 11 individual new suites green when invoked directly: for each test-rs-* file added, `node lib/memory/<test>.cjs && echo PASS_<test>` exits 0
    - Atomic commit: `git log --oneline -3 | grep -c "89.2-08.*register.*11 new"` returns 1
    - No release artifacts touched: `git diff HEAD~1 -- CHANGELOG.md .claude-plugin/plugin.json package.json | wc -l` returns 0
  </acceptance_criteria>
  <done>
    All 11 new 89.2 test files registered (10 from initial planning + 1 new standalone pinecone-bridge test added during checker revision per W3 Option i). Feynman baseline advances 66 -> 77 (or documents the 4 pre-existing failures in PHASE-GATE.md G8 if they persist). Single sweep approach avoided Wave-2/Wave-3 plan-level merge conflicts.
  </done>
</task>

<task type="auto">
  <name>Task 2: File 89.2-PHASE-GATE.md transcript with 9 G + 5 E asserts</name>
  <files>.planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md</files>
  <read_first>
    - .planning/phases/89.1-domain-analysis-and-query-matrix/89.1-PHASE-GATE.md (template -- this plan mirrors structure byte-for-byte, swapping G assertions for 89.2 file set + adding 5 89.2-specific E asserts)
    - .planning/phases/89.1a-brain-methodology-substrate-loader/89.1a-04-SUMMARY.md (Phase Gate transcript template referenced in 89.1)
    - All Phase 89.2 PLAN files (read frontmatter for files_modified to populate G2-G4 file lists)
    - lib/memory/run-feynman-tests.cjs (after Task 1 sweep -- to populate G6 grep results)
    - .planning/phases/89.2-external-research-fetching/89.2-CONTEXT.md (Phase Gate G + E asserts enumeration)
  </read_first>
  <action>
    1. Identify the BASE COMMIT for diff (the commit immediately before Plan 89.2-01 RED commit lands). Use `git log --oneline | grep -E "89.1-05" | head -1` to find the last 89.1 commit; the next commit (89.2-01 RED) is the start of the 89.2 range; `git rev-parse HEAD~1-of-that` is the base.

    2. Identify HEAD COMMIT (post-Task-1 of this plan; the most recent commit before this transcript file is committed).

    3. Write `.planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md` with the structure shown in the Interfaces block. Each G + E section:
       - States expected vs observed
       - Captures the exact command used
       - Records PASS / FAIL / CONDITIONAL PASS
       - References evidence files where applicable

    4. G1 (zero new deps): `git diff <base>..HEAD -- package.json | grep -E '^\+.*"' | grep -v '"version"' | wc -l` should be 0.

    5. G2 (BSL 1.1 -- on every new file AND inside every embedded Python script literal): for each new file in Plans 89.2-01 through 89.2-07 (production + test + validator), `grep -c "BSL 1.1" <file>` returns 1+. SPECIAL CASE for Plan 06 files that contain embedded Python script literals: `grep -c "BSL 1.1" lib/core/rs-pinecone-bridge.cjs` returns >= 2 (CJS file header + embedded bridgeScript Python header); `grep -c "# BSL 1.1" lib/core/rs-differential-scorer.cjs` returns >= 1 (embedded lsaBridgeScript Python header; CJS file header counts as just "BSL 1.1" without the leading #). Build a row table mirroring 89.1-PHASE-GATE.md format. List of files (planner verifies against actual ls output during execution):
       - lib/core/rs-egress-violations.cjs
       - lib/core/rs-egress-prompts.cjs
       - lib/core/rs-egress-telemetry.cjs
       - lib/core/rs-fetcher-academic.cjs
       - lib/core/rs-fetcher-patents.cjs
       - lib/core/rs-fetcher-industry.cjs
       - lib/core/rs-fetcher-experts.cjs
       - lib/core/rs-pinecone-bridge.cjs (CJS header + embedded Python header)
       - lib/core/rs-preprocessor.cjs
       - lib/core/rs-differential-scorer.cjs (CJS header + embedded Python header)
       - lib/core/rs-innovation-classifier.cjs
       - lib/core/rs-breakthrough-scorer.cjs
       - lib/core/rs-thesis-generator.cjs
       - lib/memory/validators/external-academic-invariants.cjs
       - lib/memory/validators/external-patents-invariants.cjs
       - lib/memory/validators/external-industry-invariants.cjs
       - lib/memory/test-rs-egress-primitives.cjs
       - lib/memory/test-rs-fetcher-academic.cjs
       - lib/memory/test-rs-fetcher-patents.cjs
       - lib/memory/test-rs-fetcher-industry.cjs
       - lib/memory/test-rs-fetcher-experts.cjs
       - lib/memory/test-rs-pinecone-bridge.cjs (NEW from W3 Option (i) revision)
       - lib/memory/test-rs-preprocessor.cjs
       - lib/memory/test-rs-differential-scorer.cjs
       - lib/memory/test-rs-innovation-classifier.cjs
       - lib/memory/test-rs-breakthrough-scorer.cjs
       - lib/memory/test-rs-thesis-generator.cjs

    6. G3 (zero em-dashes): same file list as G2; `grep -cP '[\x{2013}\x{2014}]' <file>` returns 0 per file.

    7. G4 (Canon Part 8 forbidden-field audit on production .cjs): for production files only (NOT test files; tests deliberately reference forbidden field names in adversarial scenarios), `grep -cE "governing_thought[^_]|identity_text|decision_log|meeting_transcript|artifact_body" <file>` returns 0.

    8. G5 (chokepoint exclusivity): for each NEW fetcher (academic, patents, industry), `grep -c "buildAcademicQuery|buildPatentsQuery|buildIndustryQuery" lib/core/rs-fetcher-{academic,patents,industry}.cjs` returns the chokepoint function reference count >= 5 per fetcher (mirror 89.1a Plan 04 G5). Cross-check: `grep -c "auditQueryString" lib/core/rs-fetcher-{academic,patents,industry}.cjs` >= 1 per fetcher.

    9. G6 (Feynman registration): `grep -c "test-rs-" lib/memory/run-feynman-tests.cjs` returns 15.

    10. G7 (each new suite green direct invocation): for each test-rs-* file from 89.2, `node lib/memory/<file>.cjs; echo "exit: $?"` exits 0; final line shows N/N passed.

    11. G8 (full Feynman green): `node lib/memory/run-feynman-tests.cjs; echo "exit=$?"` exits 0 with `77/77 passed`. If 4 pre-existing failures persist, document as CONDITIONAL PASS with note "4 pre-existing failures predate Phase 89.2; not introduced by this phase; tracked separately".

    12. G9 (ExternalEgressViolation surface map): `grep -c "ExternalEgressViolation" lib/core/rs-fetcher-{academic,patents,industry}.cjs lib/core/rs-fetcher-experts.cjs lib/core/rs-preprocessor.cjs lib/core/rs-differential-scorer.cjs lib/core/rs-innovation-classifier.cjs lib/core/rs-breakthrough-scorer.cjs lib/core/rs-thesis-generator.cjs lib/core/rs-pinecone-bridge.cjs lib/core/rs-egress-violations.cjs lib/core/rs-egress-prompts.cjs` returns >= 1 per file (one shared class import OR throw site). Cross-check: `grep -c "class ExternalEgressViolation" lib/core/rs-fetcher-{academic,patents,industry}.cjs lib/core/rs-fetcher-experts.cjs lib/core/rs-preprocessor.cjs lib/core/rs-differential-scorer.cjs lib/core/rs-innovation-classifier.cjs lib/core/rs-breakthrough-scorer.cjs lib/core/rs-thesis-generator.cjs lib/core/rs-pinecone-bridge.cjs lib/core/rs-egress-prompts.cjs` returns 0 per file (only rs-egress-violations.cjs defines the class; everyone else imports). 89.1's per-module siblings (rs-domain-analyzer + rs-query-matrix) remain valid and continue to define their own subclasses; G9 documents the EXTRACTION pattern.

    13. E1 (per-fetcher adversarial Canon Part 8 fixture suites green): for each of test-rs-fetcher-academic + test-rs-fetcher-patents + test-rs-fetcher-industry + test-rs-fetcher-experts + test-rs-preprocessor + test-rs-differential-scorer + test-rs-innovation-classifier + test-rs-breakthrough-scorer + test-rs-thesis-generator, the suite output must show >= 4 adversarial Canon Part 8 scenarios passing (parse the suite output to confirm "adversarial" or "Canon Part 8" markers in scenario names; alternatively, total scenario count must match the per-plan must_haves spec). Combined egress-fetcher coverage = 4 + 4 + 4 = 12 across plans 02 + 03 + 04, meeting CONTEXT-required >=12.

    14. E2 (per-source rate-limit graceful degradation verified): for each of test-rs-fetcher-academic + test-rs-fetcher-patents + test-rs-fetcher-industry, the suite includes at least one of {429 graceful, timeout graceful, api-key-missing graceful, budget-exhausted graceful} scenario passing.

    15. E3 (strict 1024-dim cosine via existing rs_cache.py): `grep -c "fetch_all_from_namespace" lib/core/rs-pinecone-bridge.cjs` >= 1 (proves Option a from CONTEXT.md was chosen and uses existing primitive); `grep -c "1024" lib/core/rs-differential-scorer.cjs lib/core/rs-pinecone-bridge.cjs` >= 1 (validates dimension constant).

    16. E4 (auditQueryObject pre-return defense across new modules): `grep -c "auditQueryObject" lib/core/rs-fetcher-experts.cjs lib/core/rs-preprocessor.cjs lib/core/rs-innovation-classifier.cjs lib/core/rs-breakthrough-scorer.cjs lib/core/rs-thesis-generator.cjs` returns >= 1 per file (5 invocations total minimum).

    17. E5 (Phase 2+3+4 chain integration smoke): write a short inline node -e command that imports preprocess + score + classify + scoreBreakthrough + generateThesis; constructs a minimal mocked input; chains them end-to-end; asserts the final thesis string matches the expected template shape ('By applying X to Y...'). Document the command + observed output in the transcript. NOTE: this smoke can be either inline in the gate transcript OR a standalone test file; planner picks. Default: inline `node -e` in the transcript for simplicity (no new test file).

    18. Commit `--no-verify`: `docs(89.2-08): file Phase 89.2 Phase Gate transcript -- 9G + 5E asserts <PASS|CONDITIONAL_PASS>`.
  </action>
  <verify>
    <automated>test -f .planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md && grep -c "Status: PASS\\|Status: CONDITIONAL PASS" .planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md`
    - Frontmatter has phase + canon_parts + bundled_release: `grep -c "phase: 89.2\\|canon_parts: \\[part-7, part-8\\]\\|bundled_release: true" .planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md` >= 3
    - 9 G asserts present: `grep -cE "^### G[1-9]:" .planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md` returns 9
    - 5 E asserts present: `grep -cE "^### E[1-5]:" .planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md` returns 5
    - Status declared: `grep -cE "Status: (PASS|CONDITIONAL PASS|FAIL)" .planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md` >= 1
    - Total asserts: 14 in summary: `grep -c "Total asserts: 14" .planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md` returns 1
    - Atomic commit: `git log --oneline -3 | grep -c "89.2-08.*Phase Gate"` returns 1
  </acceptance_criteria>
  <done>
    Phase Gate transcript filed. 9 G + 5 E asserts documented with PASS/FAIL evidence. Status declared. Mirrors 89.1 Phase Gate format. v1.11.0 bundled_release: true honored (zero release artifact diffs). G2 row table includes the embedded-Python BSL 1.1 check for Plan 06 files (W4 fix from checker revision).
  </done>
</task>

<task type="auto">
  <name>Task 3: File 89.2-VERIFICATION.md with all 13 SCs</name>
  <files>.planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/89.1-domain-analysis-and-query-matrix/89.1-VERIFICATION.md (template -- this plan mirrors format)
    - .planning/ROADMAP.md Phase 89.2 entry "Success Criteria (what must be TRUE):" lines 438-451 (the 13 SCs to verify)
    - .planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md (Task 2 OUTPUT -- VERIFICATION cross-references PHASE-GATE evidence)
    - All 89.2 PLAN frontmatter `success_criteria_addressed:` (each SC mapped to which plan(s) addressed it)
  </read_first>
  <action>
    1. Write `.planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md` with frontmatter (phase, canon_parts, bundled_release, status) + 13 SC sections.

    2. For each SC (1-13 from ROADMAP entry):
       - State the SC verbatim from ROADMAP
       - Map to which plan(s) addressed it (cross-reference plan frontmatter `success_criteria_addressed`)
       - Provide PASS/FAIL evidence: a specific verification command + observed output OR a reference to the PHASE-GATE.md G/E assert that proves it
       - Status: PASS or FAIL or CONDITIONAL PASS (with condition note)

    3. SC-12 specifically: document the +2 baseline discrepancy. ROADMAP says 65 -> 73+; actual is 66 -> 77. Note: "Phase 89.1 Plan 05 actually registered 3 test files (not 2 as initially documented in 89.1's planning); the live baseline post-89.1 is 66, not 65 (+1 baseline correction). Phase 89.2 advances baseline 66 -> 77 (delta +11; +1 of the +11 is the standalone pinecone-bridge test file added during checker revision per W3 Option i, splitting Plan 06 Task 1 from the differential scorer test orchestration). Total exceeds the ROADMAP target of +8 (73 - 65)."

    4. SC-13 specifically: cross-reference PHASE-GATE.md G1 (zero new deps) + the inline diff showing CHANGELOG/plugin.json/package.json byte-identical at HEAD vs base.

    5. Final summary at top: "Status: PASS (13/13 SCs verified)" or "Status: CONDITIONAL PASS (X/13)" if any SC has CONDITIONAL note.

    6. Commit `--no-verify`: `docs(89.2-08): file Phase 89.2 Verification -- 13/13 SCs <PASS|CONDITIONAL>`.
  </action>
  <verify>
    <automated>test -f .planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md && grep -cE "^### SC-[0-9]+:" .planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md`
    - Frontmatter has phase + canon_parts + bundled_release: 3 fields verified
    - All 13 SC sections present: `grep -cE "^### SC-[0-9]+:" .planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md` returns 13
    - Status declared with count: `grep -cE "Status: (PASS|CONDITIONAL PASS|FAIL).*\\(.*/13\\)" .planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md` >= 1
    - Each SC has Evidence: subsection: `grep -cE "Evidence:" .planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md` >= 13
    - Atomic commit: `git log --oneline -3 | grep -c "89.2-08.*Verification"` returns 1
    - No release artifacts touched: `git diff HEAD~1 -- CHANGELOG.md .claude-plugin/plugin.json package.json | wc -l` returns 0
  </acceptance_criteria>
  <done>
    VERIFICATION.md filed. All 13 SCs cross-referenced to PHASE-GATE asserts + plan SUMMARYs with explicit PASS/FAIL evidence. SC-12 baseline discrepancy + W3-revision delta documented. SC-13 zero-release-artifact bundled_release evidence captured.
  </done>
</task>

</tasks>

<verification>
- 11 new test files registered in run-feynman-tests.cjs (baseline 66 -> 77; +1 from W3 Option (i) standalone pinecone-bridge test added during checker revision)
- Phase Gate transcript filed with 9 G + 5 E asserts (14 total)
- VERIFICATION.md filed with all 13 SCs cross-referenced
- Full Feynman runner exits 0 (or CONDITIONAL PASS if 4 pre-existing failures persist)
- Zero release artifacts touched (CHANGELOG/plugin.json/package.json untouched)
- Zero new runtime deps across the entire 89.2 commit range
</verification>

<success_criteria>
Plan 89.2-08 complete when:
- [ ] lib/memory/run-feynman-tests.cjs has 11 new TEST_FILES entries
- [ ] Full Feynman runner exits 0 OR documented CONDITIONAL PASS
- [ ] .planning/phases/89.2-external-research-fetching/89.2-PHASE-GATE.md filed with 9 G + 5 E asserts
- [ ] .planning/phases/89.2-external-research-fetching/89.2-VERIFICATION.md filed with 13 SCs
- [ ] All 3 atomic commits in git log
- [ ] Zero release artifacts touched
- [ ] Phase 89.2 ready to hand off to Phase 89.3 / 89.5 consumers
</success_criteria>

<output>
After completion, create `.planning/phases/89.2-external-research-fetching/89.2-08-SUMMARY.md`
</output>
