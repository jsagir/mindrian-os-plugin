---
phase: 89.2-external-research-fetching
plan: "07"
subsystem: reverse-salient-engine
tags: [classifier, breakthrough-scorer, thesis-generator, deterministic, canon-part-7, canon-part-8, tdd]
canon_parts: [part-7, part-8]
milestone: v1.11.0
bundled_release: true
success_criteria_addressed: [SC-07, SC-08, SC-09]

# Dependency graph
requires:
  - phase: 89.2-external-research-fetching
    provides: "rs-differential-scorer.cjs (Plan 06) -- {diff, lsa, bert, passes} pair output consumed as classifier input; rs-egress-violations + rs-egress-prompts (Plan 01) for shared ExternalEgressViolation + audit primitives"
provides:
  - "rs-innovation-classifier.cjs (Phase 3 deterministic 3-enum classifier; lsa>0.3 + bert>0.3 thresholds with strict > comparisons)"
  - "rs-breakthrough-scorer.cjs (Phase 3 5-dimension 0-10 rubric with frozen-order dominant_dimension tie-break)"
  - "rs-thesis-generator.cjs (Phase 4 pure-template-fill; NO runtime LLM)"
  - "Three-stage deterministic chain: scored_pair -> classified_pair -> classified_pair_with_breakthrough -> thesis string"
affects:
  - "89.4 chain wiring (PWS VP / JTBD / Causal Loop / Navigation Engine consume thesis output as upstream inputs)"
  - "89.5 engine orchestrator (rs-discovery-engine.cjs orchestrates classifier->scorer->thesis after fetcher pipeline)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "3-stage deterministic chain pattern: each stage takes pass-through prior-stage output + adds its own field; no stage rewrites prior-stage fields. Downstream consumers can read all stages from a single composite object."
    - "Frozen DIMENSIONS array as tie-break contract: dominant_dimension lookup walks DIMENSIONS in fixed order; first key matching max wins. Eliminates Object.keys insertion-order fragility under refactor."
    - "Pure template-fill (string concat, NOT template-literal interpolation): downstream grep-ability is preserved; the template string surface is mechanically inspectable."
    - "Z-fallback + bridge_concept-fallback graceful render: thesis-generator emits a coherent thesis even when downstream optional fields are missing. The error envelope is reserved for required-field shape errors only (missing query_concept / doc_concept / classification)."
    - "Canon Part 8 two-layer defense baked into every new module: Layer 1 pre-input (auditQueryString on each user-controlled string field) + Layer 2 pre-return (auditQueryObject on classifier + scorer composite output, auditQueryString on thesis). All 3 modules consume the SAME shared ExternalEgressViolation class via rs-egress-prompts -> rs-egress-violations."

key-files:
  created:
    - "lib/core/rs-innovation-classifier.cjs (194 lines; classify + computeBridgeConcept + frozen LSA_HIGH/BERT_HIGH/CLASSIFICATIONS/BRIDGE_CONCEPT_BY_CLASSIFICATION; pre-input + pre-return Canon Part 8 audits)"
    - "lib/core/rs-breakthrough-scorer.cjs (255 lines; scoreBreakthrough + computeDominant + 4 dimension functions + frozen DIMENSIONS/TIER_TO_FEASIBILITY; 5-dim 0-10 rubric; pre-input + pre-return Canon Part 8 audits)"
    - "lib/core/rs-thesis-generator.cjs (188 lines; generateThesis + frozen MECHANISM_BY_CLASSIFICATION/DOMINANT_TO_LABEL + FALLBACK_Z/FALLBACK_BRIDGE; pure string concat; pre-input + pre-return Canon Part 8 audits)"
    - "lib/memory/test-rs-innovation-classifier.cjs (301 lines; 8 scenarios + A1 sweep; 3 enum branches + boundary lsa=0.3 exact + determinism + 2 adversarial)"
    - "lib/memory/test-rs-breakthrough-scorer.cjs (375 lines; 10 scenarios + A1 sweep; full rubric all-max + all-min + tier=applied + 4 signal-count branches + determinism + 2 adversarial)"
    - "lib/memory/test-rs-thesis-generator.cjs (360 lines; 9 scenarios + A1 sweep; 3 mechanism branches + missing dominant + missing bridge + missing both + determinism + 2 adversarial)"
  modified: []

key-decisions:
  - "Classifier 3-enum rule per CONTEXT.md Specific Ideas: structural_transfer (lsa>0.3 AND bert<=0.3), semantic_implementation (bert>0.3 AND lsa<=0.3), hybrid (both>0.3), default fallback structural_transfer (most common). Strict > used so the boundary case lsa=0.3 exact falls to default fallback (Test 5 verifies)."
  - "Classification independence from passes: classifier emits a label even when scored_pair.passes=false (e.g., a high-lsa high-bert direct-intersection pair pruned by dual-floor still gets 'hybrid' label). Test 3 verifies the contract; downstream gates on passes separately. The pedagogical signal is that the underlying alignment is the load-bearing classification axis, NOT the dual-floor detection result."
  - "Breakthrough scorer 5-dimension rubric per CONTEXT.md: feasibility (substrate.tier; core=2 / applied=1.5 / theoretical=1), market (industry signal count step function 0/0.5/1.0/1.5/2.0), magnitude (diff step function 0/1.0/1.5/2.0), advantage (2*(1-density) clamped), impact (BERT step function 0/0.5/1.0/1.5/2.0). Total = simple sum, bounded in [0,10] by construction."
  - "Frozen-order tie-break for dominant_dimension: DIMENSIONS = ['feasibility','market','magnitude','advantage','impact']. computeDominant walks DIMENSIONS in order; first key whose value equals max wins. Test 1 (all-max) verifies feasibility wins the all-tied case; Test 2 (all-min, only feasibility non-zero) verifies feasibility wins as the single non-zero. Eliminates Object.keys insertion-order fragility."
  - "Thesis template assembled via pure string concat (NOT template-literal backticks) so the source surface is mechanically grep-able. By applying / achieve / because / period are all literal substrings of the assembled thesis, providing audit-friendly anchors for downstream tooling."
  - "Z-fallback + bridge_concept-fallback graceful render: thesis-generator emits 'novel insight' when breakthrough.dominant_dimension is missing/unknown, and 'domain analogy' when classified_pair.bridge_concept is null/empty. Both fallbacks are defined as named constants (FALLBACK_Z, FALLBACK_BRIDGE) so refactor drift is loud, not silent. Tests 4/5/6 verify the three fallback combinations."
  - "Error envelope reserved for required-field shape errors only (not_object, missing_concepts, invalid_classification). Optional-field absence triggers the graceful fallback, NOT an envelope. This separates 'invalid input' from 'sparse input' so downstream callers know which is recoverable."
  - "Canon Part 8 two-layer defense across all 3 modules with consistent surface tag ('innovation-classifier' / 'breakthrough-scorer' / 'thesis-generator'). Layer 1 is the strongest contract: throws BEFORE any rule application or template assembly. Tests 7-8 (classifier), 9-10 (scorer), 8-9 (thesis) verify the throw-on-input contract; the rule application / template assembly is NEVER reached when Layer 1 fires."
  - "Adversarial test 9 (thesis) accepts EITHER throw-on-input OR scrubbed-output as Canon Part 8 satisfaction. The bridge_concept field flows directly into the thesis string, so if Layer 1 catches it the thesis is never assembled (preferred path); if not, Layer 2 catches the assembled thesis containing the leaked bytes. Both branches keep the egress surface clean."
  - "Three-stage chain composability proven via end-to-end smoke test: classifier({lsa:0.5, bert:0.1}) -> structural_transfer + cross-domain isomorphism; scorer({tier:core, signals:15, density:0.3}) -> score=5.9, dominant=feasibility; thesis -> 'By applying CRISPR delivery to liposome targeting, achieve feasible novel transfer because isomorphic structure transfers under cross-domain isomorphism.' Each stage's output is a strict superset of the prior stage's output (pass-through preserved), enabling single-object reads downstream."

patterns-established:
  - "Strict > comparison in deterministic enum classifiers: any rule-based classifier with a numeric threshold MUST use strict > so the exact-threshold case is unambiguous. Test 5 (boundary at exact threshold -> default fallback) is the canonical regression fence for the classifier; mirrors Plan 89.2-06 Test 5 (exact-threshold -> passes=false) for the differential scorer."
  - "Frozen-order tie-break for max-key lookups: any module computing 'argmax over a fixed set of keys' MUST declare the keys in a frozen array AND walk the array in order for the tie-break. Object.keys insertion order is fragile under refactor; explicit frozen-order is the determinism contract."
  - "Pass-through composite shape for chained deterministic modules: each stage's output preserves prior-stage fields verbatim (NOT a transformed wrapper). Downstream consumers read all stages from a single object via direct field access, eliminating the need for nested envelope unwrapping. Reduces composition surface area significantly."
  - "Canon Part 8 surface tag discipline: every audit call carries a surface string ('innovation-classifier' / 'breakthrough-scorer' / 'thesis-generator') so audit log entries are mechanically attributable to a specific module under audit. Future Phase Gate transcripts can grep audit log by surface for per-module evidence."

requirements-completed: [SC-07, SC-08, SC-09]

# Metrics
duration: 7min
completed: 2026-04-26
---

# Phase 89.2 Plan 07: Classifier + Breakthrough Scorer + Thesis Generator Summary

**Phase 3 deterministic 3-enum innovation classifier (lsa/bert >0.3 thresholds, strict >) + Phase 3 5-dimension 0-10 breakthrough rubric (feasibility/market/magnitude/advantage/impact, frozen-order tie-break) + Phase 4 pure-template-fill thesis generator ("By applying X to Y, achieve Z because mechanism bridge_concept.") -- the three-stage deterministic chain that converts a scored pair into an actionable thesis. ZERO runtime LLM. ZERO Math.random. ZERO new runtime deps. Pre-input + pre-return Canon Part 8 audits across all 3 modules.**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-04-26T21:36:59Z
- **Completed:** 2026-04-26T21:43:59Z
- **Tasks:** 3 (TDD: 3 RED + 3 GREEN cycles)
- **Files created:** 6 (3 production CJS modules + 3 test fixture suites)
- **Lines created:** 1673 total (637 production + 1036 tests)

## Accomplishments

- **rs-innovation-classifier.cjs** ships the Phase 3 deterministic 3-enum classifier on the {diff, lsa, bert, passes} output of Plan 89.2-06's differential scorer. Strict > comparisons throughout (Test 5 verifies lsa=0.3 exactly falls to default fallback structural_transfer). Classification is independent of passes (Test 3 verifies high-lsa+high-bert+failed-dual-floor pair still gets 'hybrid' label) -- the pedagogical signal is that the underlying alignment is the load-bearing axis. Bridge concept lookup deterministic via BRIDGE_CONCEPT_BY_CLASSIFICATION frozen map.
- **rs-breakthrough-scorer.cjs** ships the Phase 3 5-dimension 0-10 rubric with frozen-order dominant_dimension tie-break. Each dimension is a pure step function over its input (no continuous interpolation; deterministic JSON.stringify). Score in [0, 10] by construction (5 dimensions * 2 max each). DIMENSIONS frozen-order tie-break verified by Test 1 (all-tied at 2.0 -> feasibility wins) and Test 2 (only feasibility non-zero -> feasibility wins).
- **rs-thesis-generator.cjs** ships the Phase 4 pure-template-fill: `'By applying ' + X + ' to ' + Y + ', achieve ' + Z + ' because ' + mechanism + ' ' + bridge_concept + '.'`. NO runtime LLM. NO template-literal interpolation (string concat preserves grep-ability of literal anchors). Z-fallback ('novel insight') and bridge_concept-fallback ('domain analogy') as frozen constants -- Tests 4/5/6 verify the three graceful-render combinations. Required-field shape errors return invalid_input envelope; optional-field absence triggers graceful fallback (NOT envelope).
- **Three-stage chain composability** proven via end-to-end smoke test: scored_pair -> classify -> scoreBreakthrough -> generateThesis emits a coherent natural-language thesis from real input. Each stage's output is a strict superset of prior-stage output (pass-through preserved); downstream consumers read all 3 stages from a single composite object via direct field access.
- **Canon Part 8 two-layer defense** baked into every module: Layer 1 pre-input (auditQueryString on every user-controlled string field) + Layer 2 pre-return (auditQueryObject on classifier + scorer; auditQueryString on thesis). All 3 modules consume the SHARED ExternalEgressViolation class via rs-egress-prompts -> rs-egress-violations (NO redefinition; Plan 89.2-01 chokepoint preserved). 6 adversarial scenarios total prove the input-gate contract; A1 sweeps clean across every captured output.
- **27 test scenarios** (8 + 10 + 9) all GREEN; A1 sweeps zero forbidden-pattern hits across all 3 suites.

## Task Commits

| Task | Module                              | RED      | GREEN    |
| ---- | ----------------------------------- | -------- | -------- |
| 1    | rs-innovation-classifier.cjs        | 614d23b  | c1aea89  |
| 2    | rs-breakthrough-scorer.cjs          | 8948cfa  | 2cc8892  |
| 3    | rs-thesis-generator.cjs             | 1c5805f  | e1d17e2  |

## Verification

| Check                                                | Status |
| ---------------------------------------------------- | ------ |
| All 6 files created with BSL 1.1 headers             | PASS   |
| Zero em-dashes across all 6 files                    | PASS   |
| CJS only (zero `import`/`export` keywords)           | PASS   |
| ExternalEgressViolation imported (NOT redefined) x 3 | PASS   |
| auditQueryObject/auditQueryString called pre-return  | PASS   |
| Classifier 8/8 scenarios green                       | PASS   |
| Breakthrough scorer 10/10 scenarios green            | PASS   |
| Thesis generator 9/9 scenarios green                 | PASS   |
| A1 sweeps zero forbidden-pattern hits across 3 suites | PASS   |
| End-to-end chain composability (smoke)               | PASS   |
| Zero new runtime deps (no package.json change)       | PASS   |
| All 6 atomic commits in git log                      | PASS   |

## End-to-End Chain Smoke Test

```
Input scored_pair: {query_concept: "CRISPR delivery", doc_concept: "liposome targeting",
                    diff: 0.4, lsa: 0.5, bert: 0.1, passes: true}

After classify():
  + classification: "structural_transfer"   (lsa=0.5 > 0.3 AND bert=0.1 <= 0.3)
  + bridge_concept: "cross-domain isomorphism"

After scoreBreakthrough({tier: "core", signals: 15, pair_density: 0.3}):
  + breakthrough.score: 5.9
  + breakthrough.breakdown: {feasibility: 2, market: 1.5, magnitude: 1, advantage: 1.4, impact: 0}
  + breakthrough.dominant_dimension: "feasibility"

After generateThesis():
  "By applying CRISPR delivery to liposome targeting, achieve feasible novel transfer
   because isomorphic structure transfers under cross-domain isomorphism."
```

The chain is composable: each stage's output is consumable by the next without any glue layer. Downstream Phase 89.4 (chain wiring) and Phase 89.5 (engine orchestrator) read all 3 stages from a single object.

## Self-Check: PASSED

- File `lib/core/rs-innovation-classifier.cjs`: FOUND
- File `lib/core/rs-breakthrough-scorer.cjs`: FOUND
- File `lib/core/rs-thesis-generator.cjs`: FOUND
- File `lib/memory/test-rs-innovation-classifier.cjs`: FOUND
- File `lib/memory/test-rs-breakthrough-scorer.cjs`: FOUND
- File `lib/memory/test-rs-thesis-generator.cjs`: FOUND
- Commit 614d23b (RED Task 1): FOUND
- Commit c1aea89 (GREEN Task 1): FOUND
- Commit 8948cfa (RED Task 2): FOUND
- Commit 2cc8892 (GREEN Task 2): FOUND
- Commit 1c5805f (RED Task 3): FOUND
- Commit e1d17e2 (GREEN Task 3): FOUND
