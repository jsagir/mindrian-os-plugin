---
phase: 89.2-external-research-fetching
plan: "06"
subsystem: reverse-salient-engine
tags: [pinecone, sklearn, lsa, bert, dual-floor, canon-part-7, canon-part-8, tdd]
canon_parts: [part-7, part-8]
milestone: v1.11.0
bundled_release: true
success_criteria_addressed: [SC-05, SC-06]

# Dependency graph
requires:
  - phase: 89.2-external-research-fetching
    provides: "Plans 02 / 03 / 04 fetcher output shapes (papers[] + patents[] + signals[]); rs-egress-violations (Plan 01); rs-egress-prompts (Plan 01)"
  - phase: 89-reverse-salient-engine
    provides: "rs_cache.py::fetch_all_from_namespace v1.10.16 (1024-dim values; consumed via Pinecone-direct bridge)"
  - phase: 89.1-domain-analysis-and-query-matrix
    provides: "domain_analysis output shape (consumed by preprocessor for concept extraction + boundary_flag heuristic)"
  - phase: 89.1a-brain-methodology-substrate-loader
    provides: "loadSubstrate (consumable for boundary_flag refinement; not consumed in v1.11.0; available for 89.4)"
provides:
  - "rs-pinecone-bridge.cjs (CJS->Python shim for strict 1024-dim Pinecone-direct vector retrieval; resolves deferred-from-89.1 strict-1024-dim item)"
  - "rs-preprocessor.cjs (Phase 2 deterministic per-document 5-field preprocessor)"
  - "rs-differential-scorer.cjs (Phase 3 dual-floor differential scorer with strict > thresholds)"
  - "Embedded Python bridgeScript + lsaBridgeScript with BSL 1.1 headers (Canon discipline marker on embedded source)"
  - "Documented Canon Part 7 carve-out for pair-wise LSA (rs_math.py corpus-level vs per-pair sklearn TfidfVectorizer + TruncatedSVD)"
affects:
  - "89.2-07 (innovation classifier consumes {diff, lsa, bert, passes} pair output)"
  - "89.2 Phase 4 thesis-generator (consumes classifier output)"
  - "89.4 chain wiring (preprocessor concepts[] feed downstream framework chains)"
  - "89.5 engine orchestrator (rs-discovery-engine.cjs orchestrates all rs-* modules)"

# Tech tracking
tech-stack:
  added:
    - "Embedded Python in CJS via spawnSync + JSON-over-stdio (CJS shim pattern for 1024-dim vector retrieval AND pair-wise LSA)"
    - "Pinecone-direct integration (NOT through brain-client MCP) via existing rs_cache.py"
  patterns:
    - "CJS->Python embedded-script bridge: BSL 1.1 header on the embedded Python literal (W4 fix; embedded source is treated as a licensable surface, not a free-form string)"
    - "Documented Canon Part 7 carve-out pattern: when a corpus-level Python primitive cannot answer a pair-wise question, embed a per-pair sklearn subset in the CJS module's bridge script + document the carve-out in 3 locations (plan must_haves, CJS file header, embedded Python comment block)"
    - "Strict > comparisons in dual-floor filter: passes IFF (diff > 0.3 AND lsa > 0.2 AND bert > 0.2). Exact-threshold cases fail by design (Test 5 verifies 0.2/0.2 -> passes=false)"
    - "Two-mock fixture pattern: spawnSync override + rs-pinecone-bridge require.cache override; both bridges mockable independently for graceful-degradation matrix coverage (Tests 6 + 7)"
    - "Canon Part 8 two-layer defense on EVERY new module: Layer 1 pre-input (auditQueryString) + Layer 2 pre-return (auditQueryObject). Adversarial fixtures verify bridges are NEVER called when Layer 1 throws (Tests 9 + 10 verify call counts == 0)"

key-files:
  created:
    - "lib/core/rs-pinecone-bridge.cjs (303 lines; CJS->Python shim wrapping rs_cache.py::fetch_all_from_namespace; queryPineconeWithVectors + cosineSimilarity exports + embedded bridgeScript with # BSL 1.1 header)"
    - "lib/core/rs-preprocessor.cjs (350 lines; deterministic 5-field-per-document processor; preprocess + 5 helper functions exposed via _test)"
    - "lib/core/rs-differential-scorer.cjs (376 lines; dual-floor differential scorer; score + computeLsaCosine + computeBertCosine + lsaBridgeScript with # BSL 1.1 + CARVE-OUT comment block)"
    - "lib/memory/test-rs-pinecone-bridge.cjs (277 lines; 4 standalone scenarios + A1 sweep)"
    - "lib/memory/test-rs-preprocessor.cjs (433 lines; 10 scenarios + A1 sweep)"
    - "lib/memory/test-rs-differential-scorer.cjs (480 lines; 10 scenarios + A1 sweep; two-mock pattern)"
  modified: []

key-decisions:
  - "Pair-wise LSA cosine via embedded sklearn TfidfVectorizer + TruncatedSVD subset is a DELIBERATE Canon Part 7 carve-out (NOT a fork of rs_math.py). Justification: rs_math.py::build_tfidf_svd is corpus-level (Sequence[str] >= 3 docs); 2-document mini-corpus produces degenerate IDF + 1-component SVD that does NOT correspond to Kwan 2023's algorithm. Documented in plan must_haves + CJS file header comment block + embedded Python lsaBridgeScript header (3 locations). Acceptance grep enforces >= 2 CARVE-OUT comment block hits in differential-scorer."
  - "Strict 1024-dim cosine via Pinecone-direct (Option a from CONTEXT.md) using EXISTING rs_cache.py::fetch_all_from_namespace (v1.10.16 contract returns 1024-dim values per its existing implementation). NO new Pinecone API surface needed; bridge is a thin JSON-over-stdio shim. NO Wave-0 prerequisite plan."
  - "BERT cosine via twin queryPineconeWithVectors calls + pure-JS cosineSimilarity helper (1024-dim dot product / norm * norm). Pinecone vectors are L2-normalized so cosine is typically in [0, 1]; helper returns 0 on degenerate zero-vectors / shape errors so caller stays in graceful-degradation territory."
  - "Strict > comparisons in dual-floor filter (NOT >=). Test 5 verifies the exact-threshold case lsa=0.2 bert=0.2 -> passes=false. The pedagogical signal is that the dual-floor is a DETECTION filter, not a borderline-acceptance filter; 0.2 is the floor below which the signal degenerates."
  - "Embedded Python script is licensed identically to surrounding CJS via leading '# BSL 1.1' header line on the embedded string literal (W4 Plan fix). The embedded source is a SOURCE SURFACE, not a free-form string. Acceptance enforces grep hit count >= 2 in pinecone-bridge AND differential-scorer."
  - "Graceful degradation matrix: BERT bridge fails -> warning='bert_unavailable' + passes=lsa>0.2 (LSA-only fallback); LSA bridge fails -> warning='lsa_unavailable' + passes=false (pessimistic because LSA is the structural anchor). Tests 6 + 7 verify both branches."
  - "Adversarial input rejected at Layer 1 (pre-input auditQueryString) BEFORE either bridge is called. Tests 9 + 10 verify lsaCalled === false AND pineconeCalled === false on FORBIDDEN_PATTERNS hit; the bridges are never invoked when Canon Part 8 fires. This is the stronger contract than 'output scrubbed' because it eliminates the egress surface entirely."
  - "Promise.all parallelism on (computeLsaCosine, computeBertCosine) is future-proofing for real Brain latency. Sequential await would be equivalent for correctness; parallel does not change the dual-floor semantics."
  - "Test 5 (industry signal) fixture refined to richer concept overlap (Rule 3 auto-fix) so the boundary_flag=false assertion the plan specified resolves correctly. Original fixture had 2/6 = 0.333 < 0.5 threshold; refined fixture has 6/6 = 1.0 overlap. One-line fixture polish, no plan behavior change."

patterns-established:
  - "Embedded Python BSL discipline: every embedded Python script literal MUST carry '# BSL 1.1' as its first line, treating the embedded source as a licensable surface. Acceptance grep enforces >= 2 hits in any CJS file containing an embedded Python script."
  - "Canon Part 7 carve-out documentation: when Reuse Before Build cannot be satisfied because the existing primitive's API contract is fundamentally incompatible with the new question, document the carve-out in 3 locations (plan must_haves + source file header + embedded language comment block). Acceptance enforces presence at all 3 locations to catch refactor drift."
  - "Two-bridge graceful-degradation matrix: any module consuming two independent external bridges MUST cover the 4-cell matrix (both succeed, A succeeds B fails, A fails B succeeds, both fail) with explicit warning fields per partial-failure mode. Tests 6 + 7 are the canonical examples."
  - "Strict > comparisons in numeric filters: any dual/triple-floor filter MUST use strict > (not >=) so the exact-threshold case is unambiguous. Test 5 (boundary at exact threshold -> passes=false) is the canonical regression fence."

requirements-completed: [SC-05, SC-06]

# Metrics
duration: 11min
completed: 2026-04-26
---

# Phase 89.2 Plan 06: Preprocessor + Differential Scorer Summary

**Phase 2 deterministic 5-field preprocessor + Phase 3 dual-floor differential scorer (passes IFF diff>0.3 AND LSA>0.2 AND BERT>0.2 strict) + CJS->Python pinecone-direct bridge for strict 1024-dim cosine via existing rs_cache.py + documented Canon Part 7 carve-out for pair-wise LSA via embedded sklearn TfidfVectorizer + TruncatedSVD.**

## Performance

- **Duration:** ~11 min
- **Started:** 2026-04-26T21:20:34Z
- **Completed:** 2026-04-26T21:31:31Z
- **Tasks:** 3 (TDD: 3 RED + 3 GREEN cycles)
- **Files created:** 6 (3 production CJS modules + 3 test fixture suites)
- **Lines created:** 2219 total (1029 production + 1190 tests)

## Accomplishments

- **rs-pinecone-bridge.cjs** ships strict 1024-dim Pinecone-direct vector retrieval via thin JSON-over-stdio CJS shim around the existing v1.10.16 `rs_cache.py::fetch_all_from_namespace` primitive. Resolves the deferred-from-89.1 strict-1024-dim item without touching the Brain MCP wire path. Embedded Python `bridgeScript` carries `# BSL 1.1` header per Canon discipline.
- **rs-preprocessor.cjs** ships deterministic 5-field-per-document preprocessing (`document_id, technical_terms[], concepts[], methods[], relevance_score, boundary_flag`). Determinism contract verified via Test 2 (5 invocations byte-identical). Source-aware text extraction (papers/patents use `abstract`; industry signals use `signal`; title fallback) flows through the same preprocessing path. Boundary flag heuristic operational.
- **rs-differential-scorer.cjs** ships the Phase 3 detection signal: dual-floor filter `passes IFF (diff > 0.3 AND lsa > 0.2 AND bert > 0.2)` with strict > comparisons throughout. BERT cosine via Pinecone-direct bridge; LSA cosine via embedded per-pair sklearn TfidfVectorizer + TruncatedSVD subset (DELIBERATE Canon Part 7 carve-out, documented in 3 locations). Graceful degradation matrix proven on both bridge-failure axes.
- **Canon Part 8 two-layer defense** baked into every new module: Layer 1 pre-input (`auditQueryString`) + Layer 2 pre-return (`auditQueryObject`). Tests 9 + 10 of differential-scorer verify both bridges are NEVER called when Layer 1 fires (`lsaCalled === false AND pineconeCalled === false`). The stronger contract than scrubbing: eliminate the egress surface entirely.
- **24 test scenarios** (4 + 10 + 10) all green; A1 sweeps clean across every captured result.

## Task Commits

1. **Task 1 (RED): pinecone bridge test suite** — `d26724b` (test)
2. **Task 1 (GREEN): rs-pinecone-bridge.cjs** — `29bdb7a` (feat)
3. **Task 2 (RED): preprocessor test suite** — `343f369` (test)
4. **Task 2 (GREEN): rs-preprocessor.cjs + Test 5 fixture refinement** — `1b42ee5` (feat)
5. **Task 3 (RED): differential scorer test suite** — `fdf278d` (test)
6. **Task 3 (GREEN): rs-differential-scorer.cjs + embedded sklearn bridge + carve-out documentation** — `b23f23f` (feat)

All 6 task commits pushed `--no-verify` per Wave 3 solo execution flag (consistency with Wave 2 parallel commits; hooks validate once after the wave).

## Files Created/Modified

- `lib/core/rs-pinecone-bridge.cjs` (303 lines) — CJS->Python shim wrapping `rs_cache.py::fetch_all_from_namespace`. Exports `queryPineconeWithVectors` (returns `{success, hits: [{id, score, values: 1024-dim, metadata}]}`) + `cosineSimilarity` (pure JS dot product / norm * norm; returns 0 on degenerate zero-vectors). Embedded `bridgeScript` with `# BSL 1.1` header. Two-layer Canon Part 8 audit (Layer 1: `auditQueryString` on namespace + queryText; Layer 2: `auditQueryObject` on parsed hits[]).
- `lib/core/rs-preprocessor.cjs` (350 lines) — Deterministic 5-field-per-document preprocessor. Exports `preprocess` + 5 helpers via `_test`. Source-aware text extraction (abstract / signal / title fallback). Composite text (title + body) flows into all extractors so adversarial title content reaches Layer 2 pre-return audit.
- `lib/core/rs-differential-scorer.cjs` (376 lines) — Phase 3 dual-floor scorer. Exports `score` + `computeLsaCosine` + `computeBertCosine` + `lsaBridgeScript` via `_test`. CJS file header AND embedded Python `lsaBridgeScript` BOTH carry `CANON PART 7 CARVE-OUT (DELIBERATE, DOCUMENTED)` comment block (3 hits total — file header + embedded comment). Frozen thresholds: `DIFF_FLOOR=0.3, LSA_FLOOR=0.2, BERT_FLOOR=0.2`. Graceful degradation: `bert_unavailable` (LSA-only fallback) + `lsa_unavailable` (pessimistic, passes=false).
- `lib/memory/test-rs-pinecone-bridge.cjs` (277 lines) — 4 standalone scenarios: api_key absent / python3 absent / happy path with 1024-dim values / cosineSimilarity identity (1.0) + anti-parallel (-1.0) + orthogonal (0.0). Mock spawnSync via require.cache override.
- `lib/memory/test-rs-preprocessor.cjs` (433 lines) — 10 scenarios covering happy path / determinism / empty docs / malformed doc / industry signal / boundary flag triggered / boundary flag not triggered / 3 adversarial Canon Part 8 (abstract / title-email / domain_analysis input).
- `lib/memory/test-rs-differential-scorer.cjs` (480 lines) — 10 scenarios with two-mock pattern (spawnSync + rs-pinecone-bridge require.cache override): 4-cell dual-floor matrix (pass / fail-diff / fail-lsa / fail-bert) + boundary at exact threshold + 2 graceful-degradation cells (bert_unavailable / lsa_unavailable) + determinism + 2 Canon Part 8 adversarial (query_concept / doc_concept; both verify bridges called == 0).

## Decisions Made

See `key-decisions` in frontmatter for the full set. Headline:

1. **Pair-wise LSA carve-out** is deliberate and documented (3 locations) because `rs_math.py::build_tfidf_svd` is corpus-level and a 2-document mini-corpus produces degenerate IDF + 1-component SVD that does NOT correspond to Kwan 2023. Future drift caught by acceptance grep `>= 2 CARVE-OUT` hits.
2. **Strict 1024-dim cosine via existing rs_cache.py** (Option a from CONTEXT.md): no new Pinecone API surface, no Wave-0 prerequisite plan. Bridge is a thin JSON-over-stdio shim.
3. **Strict > comparisons** in dual-floor filter: pedagogically the floor is a DETECTION filter, not borderline-acceptance. Test 5 (lsa=0.2 bert=0.2 -> passes=false) is the regression fence.
4. **Adversarial input rejected at Layer 1 BEFORE bridges fire** (Tests 9 + 10 verify call counts == 0). Stronger contract than scrubbing: eliminate the egress surface entirely.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking fixture polish] Test 5 industry-signal fixture refinement**
- **Found during:** Task 2 (Preprocessor GREEN execution)
- **Issue:** Test 5 originally used signal text `'CRISPR startup raised 5M dollars for cancer biomarker platform development'`. Only 2/6 da concepts overlapped (cancer + biomarker), giving relevance_score = 0.333, which is < 0.5 threshold so boundary_flag resolved to true. The plan specified `boundary_flag=false (industry signal mapped)`.
- **Fix:** Refined the test fixture's signal text to carry richer overlap (`CRISPR startup raised funding for cancer immunotherapy biomarker checkpoint inhibitor response platform development` — all 6 da concepts present), giving relevance_score = 1.0 so boundary_flag=false resolves correctly. The plan's intent (verifying signal field flows through the same preprocessing path as abstract) is preserved verbatim.
- **Files modified:** `lib/memory/test-rs-preprocessor.cjs` (1 fixture string + assertion threshold from `>=1` to `>=5` concepts)
- **Verification:** Test 5 passes. Tests 1, 2, 3, 4, 6, 7, 8, 9, 10 still pass (no regression). A1 sweep clean.
- **Committed in:** `1b42ee5` (folded into Task 2 GREEN commit so the suite is self-consistent on first green run)

---

**Total deviations:** 1 auto-fixed (Rule 3 — fixture polish, no behavior change in production code).
**Impact on plan:** Plan executed as written. The fixture polish is a one-line refinement of the test scenario's signal text so the assertion the plan specified resolves correctly. Production preprocessor behavior unchanged. No scope creep.

## Issues Encountered

None. The carve-out documentation discipline (CJS header + embedded Python header + plan must_haves) caught itself on first try via acceptance grep. Real LSA bridge sanity-checked end-to-end (sklearn 1.8.0 + TfidfVectorizer + TruncatedSVD reachable; embedded Python script computes cosine without errors). Real Pinecone bridge not exercised against a live index (test pattern fully mock-driven; live wire path validated in Phase 89.2-07 or 89.5).

## User Setup Required

None. The bridges are inert until invoked by an orchestrator (Phase 89.5). No env vars added; existing `PINECONE_API_KEY` (optional, gated path) is the only env consumed.

## Next Phase Readiness

- Wave 4 of Phase 89.2 (innovation classifier + breakthrough scorer + thesis generator) consumes `score()` output `{diff, lsa, bert, passes}` directly. Pair output shape locked.
- 89.4 chain wiring consumes `preprocess()` output `concepts[]` + `boundary_flag` for downstream Cynefin / JTBD / PWS chain routing.
- 89.5 engine orchestrator (`scripts/rs-discovery-engine.cjs`) wires `query-matrix -> fetcher-* -> preprocessor -> differential-scorer -> classifier -> breakthrough-scorer -> thesis-generator` end-to-end.
- No blockers. No user-action gates. Plan 89.2-07 (Wave 3 sequential follow-up) can proceed immediately.

## Self-Check: PASSED

Verified:
- All 6 files exist on disk: `lib/core/rs-pinecone-bridge.cjs`, `lib/core/rs-preprocessor.cjs`, `lib/core/rs-differential-scorer.cjs`, `lib/memory/test-rs-pinecone-bridge.cjs`, `lib/memory/test-rs-preprocessor.cjs`, `lib/memory/test-rs-differential-scorer.cjs`.
- All 6 commits exist in `git log --oneline`: d26724b, 29bdb7a, 343f369, 1b42ee5, fdf278d, b23f23f.
- All 3 test suites exit 0: `4/4 passed` + `10/10 passed` + `10/10 passed`.
- BSL 1.1 grep on embedded Python: 2 hits in pinecone-bridge AND 2 hits in differential-scorer (CJS file header + embedded Python literal header).
- CARVE-OUT grep on differential-scorer: 3 hits (CJS file header + 2 inside embedded Python comment block; >= 2 acceptance threshold met).
- Em-dashes: 0 across all 6 files.
- CJS only: 0 import/export statements across all 3 production files.
- Zero release artifacts touched (`git diff HEAD~6 -- package.json CHANGELOG.md .claude-plugin/plugin.json` is empty).

---
*Phase: 89.2-external-research-fetching*
*Plan: 06-preprocessor-and-differential-scorer*
*Completed: 2026-04-26*
