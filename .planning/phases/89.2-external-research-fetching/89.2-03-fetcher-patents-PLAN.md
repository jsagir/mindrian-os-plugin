---
phase: 89.2
plan: "03"
slug: fetcher-patents
type: execute
wave: 2
depends_on: ["89.2-01"]
files_modified:
  - lib/core/rs-fetcher-patents.cjs
  - lib/memory/validators/external-patents-invariants.cjs
  - lib/memory/test-rs-fetcher-patents.cjs
autonomous: true
canon_parts: [part-7, part-8]
milestone: v1.11.0
bundled_release: true
success_criteria_addressed: [SC-02, SC-10, SC-11]

must_haves:
  truths:
    - "fetchPatents(queries, opts) returns { patents[], telemetry[] } where every patent carries {patent_id, title, abstract, inventors[], assignee, filing_date, source, fetched_at}"
    - "Sources covered: google_patents (Google Patents Public Datasets API via the public BigQuery-backed search-as-html endpoint -- documented use of patents.google.com/?q={q}&oq={q}; planner verified during planning) + uspto (USPTO PEDS / open data API)"
    - "ONE chokepoint function buildPatentsQuery(query, source, opts) is the ONLY URL builder; auditQueryString runs pre-fetch"
    - "Per-source rate-limit graceful degradation: 429/503 -> log telemetry, return empty for source, continue"
    - "Dedup deterministic: patent_id is canonical (already unique per USPTO/Google); preserves first-seen order"
    - "Validator (external-patents-invariants.cjs) drop-in registered scope='global'; 6 checks mirror brain-substrate-invariants byte-for-byte"
    - ">=4 adversarial fixtures: leaked-artifact-body / leaked-venture-name / leaked-meeting-fragment / leaked-financial-figure in query -> ALL throw ExternalEgressViolation BEFORE egress"
  artifacts:
    - path: lib/core/rs-fetcher-patents.cjs
      provides: "fetchPatents + buildPatentsQuery (single chokepoint) + per-source dispatchers (fetchGooglePatents, fetchUspto) + dedup helpers"
      min_lines: 300
    - path: lib/memory/validators/external-patents-invariants.cjs
      provides: "Drop-in validator: 6 checks (telemetry shape, per-source budget, status enum, query_text-hash-not-literal, hash format, fetched_at format)"
      min_lines: 200
    - path: lib/memory/test-rs-fetcher-patents.cjs
      provides: "11 scenarios: happy 2 sources, dedup, 429 graceful, timeout graceful, malformed graceful, budget exhausted, 4 adversarial, determinism + suite-end A1 sweep + A2 parity gate"
      min_lines: 400
  key_links:
    - from: "lib/core/rs-fetcher-patents.cjs::buildPatentsQuery"
      to: "lib/core/rs-egress-prompts.cjs::auditQueryString"
      via: "pre-fetch FORBIDDEN_PATTERNS audit; throws ExternalEgressViolation"
      pattern: "auditQueryString\\("
    - from: "lib/core/rs-fetcher-patents.cjs"
      to: "lib/core/rs-egress-violations.cjs::ExternalEgressViolation"
      via: "shared throwable across all 3 new fetchers"
      pattern: "ExternalEgressViolation"
    - from: "lib/core/rs-fetcher-patents.cjs"
      to: "lib/core/rs-egress-telemetry.cjs::recordTelemetry"
      via: "per-query outcome persisted"
      pattern: "recordTelemetry\\("
---

<objective>
Ship `lib/core/rs-fetcher-patents.cjs` with `fetchPatents(queries, opts)` -- the patents egress surface for Wave 2 of Phase 89.2.

Two sources covered: **Google Patents** (Google Patents Public Datasets via the public-facing patents.google.com search-as-html endpoint OR via the Google Cloud Patents Public Datasets BigQuery API -- planner picked the no-API-key path) + **USPTO** (USPTO Open Data Portal: `https://api.uspto.gov/pep/v3/applications/search` no API key for basic search).

Single Canon Part 8 chokepoint: `buildPatentsQuery(query, source, opts)` -- the ONLY URL builder. Mirrors Plan 89.2-02 academic fetcher pattern byte-for-byte structure.

Companion drop-in validator + 11-scenario adversarial fixture suite + per-source rate-limit graceful degradation. Same Canon Part 8 5-tripwire pattern as Plan 89.2-02.

Purpose: Ships SC-02 (rs-fetcher-patents.cjs exports fetchPatents) + partial SC-10 (Canon Part 8 enforcement on patents egress) + partial SC-11 (per-source rate-limit graceful degradation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89.2-external-research-fetching/89.2-CONTEXT.md
@.planning/phases/89.2-external-research-fetching/89.2-01-egress-primitives-PLAN.md
@.planning/phases/89.2-external-research-fetching/89.2-02-fetcher-academic-PLAN.md
@.planning/phases/89.1-domain-analysis-and-query-matrix/89.1-03-SUMMARY.md
@.planning/phases/89.1a-brain-methodology-substrate-loader/89.1a-02-SUMMARY.md
@.planning/phases/88.6-python-algorithm-wiring/88.6-03-PLAN.md
@lib/core/rs-egress-violations.cjs
@lib/core/rs-egress-prompts.cjs
@lib/core/rs-egress-telemetry.cjs
@lib/memory/validators/brain-substrate-invariants.cjs
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Plan 89.2-01 outputs (CONSUME these). Same shape as Plan 89.2-02. -->

External API endpoints (planner-verified during planning):
- Google Patents (no-key path): `https://patents.google.com/?q={encodeURIComponent(query)}&oq={encodeURIComponent(query)}` returns HTML; cheerio NOT in stack so use simple regex extraction over JSON-LD <script> tags embedded in the HTML payload (Google Patents emits structured JSON-LD with patent metadata). Alternative: use the public REST API at `https://patents.google.com/xhr/query?url=...&exp=` if planner discovers it returns JSON; default to JSON-LD scrape from HTML.
- USPTO Open Data: `https://api.uspto.gov/ds-api/oa_actions/v1/records?searchText={q}` returns JSON; no API key for basic search; ~50 req/day per CONTEXT.md.

Reference implementation pattern (mirror Plan 89.2-02 byte-for-byte structure but swap fetchOpenAlex/fetchArxiv -> fetchGooglePatents/fetchUspto):
```javascript
const SOURCES = Object.freeze(['google_patents', 'uspto']);
const SOURCE_ENV_VARS = Object.freeze({});  // both sources are no-key
// All other helpers same shape as academic fetcher (fetchWithTimeout, parseSourceResponse, normalizePatent, dedupKey, dedupe).
```

Patent record shape:
```javascript
{
  patent_id: 'US10123456B2',          // canonical from USPTO/Google
  title: '...',
  abstract: '...',
  inventors: ['Last, First', ...],
  assignee: 'CompanyName Inc.',
  filing_date: '2023-04-15',          // ISO-8601 date
  source: 'google_patents'|'uspto',
  fetched_at: '2026-04-26T14:00:00Z',
}
```

Test mock pattern: same installMockFetch/restoreFetch from Plan 89.2-02; mock returns per-source JSON or HTML responses.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write rs-fetcher-patents.cjs + external-patents-invariants.cjs (RED 11+6 scenario suite first; then GREEN)</name>
  <files>lib/core/rs-fetcher-patents.cjs, lib/memory/validators/external-patents-invariants.cjs, lib/memory/test-rs-fetcher-patents.cjs</files>
  <read_first>
    - .planning/phases/89.2-external-research-fetching/89.2-CONTEXT.md (especially "Phase 1.5 4 External Fetchers" + "Per-Source Rate-Limit Strategy")
    - .planning/phases/89.2-external-research-fetching/89.2-01-egress-primitives-PLAN.md (Wave 1 dependencies you CONSUME)
    - .planning/phases/89.2-external-research-fetching/89.2-02-fetcher-academic-PLAN.md (template -- patents fetcher mirrors structure byte-for-byte; ONLY swaps SOURCES list + per-source dispatchers + parser + record shape)
    - lib/core/rs-egress-violations.cjs (CONSUME)
    - lib/core/rs-egress-prompts.cjs (CONSUME via auditQueryString)
    - lib/core/rs-egress-telemetry.cjs (CONSUME via recordTelemetry + computeRemainingBudget)
    - lib/memory/validators/brain-substrate-invariants.cjs (drop-in validator template)
    - scripts/query-semantic-scholar.cjs lines 280-460 (Phase 88.6-03 telemetry pattern)
    - lib/memory/test-rs-brain-substrate.cjs (14-scenario fixture pattern)
    - docs/MINDRIAN-CANON.md Part 7 + Part 8
  </read_first>
  <behavior>
    Test 1 (happy path 2 sources): queries=['quantum entanglement detection', 'CRISPR gene editing']; mock fetch returns 5 patents per source; result patents.length >= 8 (after dedup); each patent has {patent_id matching /^[A-Z]{2}\d+[A-Z]\d?$/, title non-empty, abstract non-empty, inventors[], assignee, filing_date ISO-8601, source in SOURCES, fetched_at ISO-8601}.

    Test 2 (dedup determinism): same queries + same mock responses invoked twice -> JSON.stringify(patents1) === JSON.stringify(patents2); patent_id is the dedup key (canonical from USPTO/Google).

    Test 3 (Google Patents 429 graceful): mock returns {ok:false, status:429}; fetchPatents does NOT throw; telemetry status='rate_limited' http_status=429; uspto still runs.

    Test 4 (USPTO timeout graceful): mock USPTO throws AbortError; fetchPatents does NOT throw; telemetry status='timeout'; google_patents still runs.

    Test 5 (Google Patents malformed JSON-LD graceful): mock returns HTML without JSON-LD <script> tag; parser returns []; fetchPatents does NOT throw; telemetry status='api_error'; uspto still runs.

    Test 6 (USPTO malformed JSON graceful): mock returns {ok:true, json: () => Promise.reject(new SyntaxError('invalid JSON'))}; fetchPatents does NOT throw; telemetry status='api_error'.

    Test 7 (per-source budget exhausted): seed telemetry with 50 google_patents entries within 24h; computeRemainingBudget('google_patents', 50) === 0; fetchPatents skips google_patents; telemetry status='budget_exhausted'; uspto runs normally.

    Test 8 (CANON PART 8 adversarial: leaked-artifact-body in query): queries=['CRISPR <<artifact: meeting with Genentech Q4 financials>>']; buildPatentsQuery throws ExternalEgressViolation BEFORE any fetch(); meta.surface === 'patents'; ZERO entries in telemetry.

    Test 9 (CANON PART 8 adversarial: leaked-venture-name in query): queries=['Lawrence said our patent strategy needs $5M']; throws.

    Test 10 (CANON PART 8 adversarial: leaked-meeting-fragment in query): queries=['biotech meeting with Pfizer about IP'] -> throws.

    Test 11 (CANON PART 8 adversarial: leaked-SSN in query): queries=['inventor 123-45-6789 patents']; throws.

    Suite-end audit A1: walk every captured outbound URL + every captured telemetry record; JSON.stringify + FORBIDDEN_PATTERNS scan; assert ZERO hits across all 11 scenarios.

    Suite-end audit A2: parity gate -- assert FORBIDDEN_PATTERNS imported via rs-egress-prompts.cjs is byte-for-byte === cross-room-aggregator.cjs FORBIDDEN_PATTERNS at every regex.source index.

    Validator tests (in same suite, scenarios V1-V6 after T11):
    - V1 (Check A): telemetry file absent -> {severity: null, violations: []}
    - V2 (Check B): per-source budget exceeded for google_patents -> warning
    - V3 (Check C): status enum violation -> warning
    - V4 (Check D): persisted record contains query_text literal -> CRITICAL canon_boundary
    - V5 (Check E): query_text_hash NOT matching /^[a-f0-9]{16}$/ -> warning
    - V6 (Check F): fetched_at malformed -> warning
  </behavior>
  <action>
    1. Write lib/memory/test-rs-fetcher-patents.cjs FIRST (RED commit). Mirror lib/memory/test-rs-fetcher-academic.cjs structure byte-for-byte, swapping:
       - test name string ("89.2-03 fetcher-patents suite: 17/17 passed")
       - mocked URLs to point at patents.google.com + api.uspto.gov
       - record shape assertions to match patent shape (patent_id + inventors + assignee + filing_date)
       - sources from ['openalex', 'arxiv', ...] to ['google_patents', 'uspto']
       Run `node lib/memory/test-rs-fetcher-patents.cjs` -> EXPECT exit 1. Commit RED `--no-verify`: `test(89.2-03): RED -- failing 17-scenario patents fetcher + validator test suite`.

    2. Write lib/core/rs-fetcher-patents.cjs (GREEN part 1):
       - BSL 1.1 header (Phase 89.2 Plan 03 -- patents external-egress fetcher; chokepoint via buildPatentsQuery)
       - Mirror lib/core/rs-fetcher-academic.cjs structure byte-for-byte, swapping:
         * SOURCES = Object.freeze(['google_patents', 'uspto'])
         * SOURCE_ENV_VARS = Object.freeze({}) (both no-key sources)
         * buildPatentsQuery(query, source, opts) replaces buildAcademicQuery; meta.surface = 'patents'
         * fetchGooglePatents + fetchUspto replace fetchOpenAlex + fetchArxiv etc.
         * parseGooglePatentsResponse extracts JSON-LD <script> tags via regex (no cheerio): regex /<script type="application\/ld\+json">([\s\S]*?)<\/script>/gi -> JSON.parse each match -> filter for @type === 'Patent' or 'Product' (Google's variant) -> normalize
         * parseUsptoResponse parses JSON.records[] from USPTO API response
         * normalizePatent({source, raw}) -> {patent_id, title, abstract, inventors, assignee, filing_date, source, fetched_at}
         * dedupKey(p) -> 'patent:' + p.patent_id (no need for title/author hash; patent_id is canonical)
       - module.exports = { fetchPatents, buildPatentsQuery, _test: { dedupe, normalizePatent, fetchWithTimeout, parseGooglePatentsResponse, parseUsptoResponse, SOURCES, SOURCE_ENV_VARS } }

    3. Write lib/memory/validators/external-patents-invariants.cjs (GREEN part 2):
       - BSL 1.1 header (Phase 89.2 Plan 03 -- external patents fetcher invariants validator)
       - Mirror lib/memory/validators/external-academic-invariants.cjs (Plan 89.2-02 OUTPUT) byte-for-byte, swapping:
         * id: 'external-patents-invariants'
         * Per-source budget check is for SOURCES_PATENTS = ['google_patents', 'uspto'] subset
         * Same 6 checks (A-F) on the SAME global telemetry file at TELEMETRY_FILE
       - module.exports = { id: 'external-patents-invariants', severity_map: SEVERITY_MAP, scope: 'global', validate }

    4. Run `node lib/memory/test-rs-fetcher-patents.cjs`. EXPECT exit 0 with "17/17 passed". Iterate implementation until green. Commit GREEN `--no-verify`: `feat(89.2-03): GREEN -- patents fetcher (Google Patents + USPTO) + Canon Part 8 chokepoint + drop-in validator`.

    5. NO Feynman registration here -- Plan 89.2-08 owns sweep registration to avoid Wave-2 contention.
  </action>
  <verify>
    <automated>node lib/memory/test-rs-fetcher-patents.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `test -f lib/core/rs-fetcher-patents.cjs && test -f lib/memory/validators/external-patents-invariants.cjs && test -f lib/memory/test-rs-fetcher-patents.cjs`
    - BSL 1.1 on all 3: `for f in lib/core/rs-fetcher-patents.cjs lib/memory/validators/external-patents-invariants.cjs lib/memory/test-rs-fetcher-patents.cjs; do grep -c "BSL 1.1" $f; done` returns 3 lines all >= 1
    - Zero em-dashes: `grep -cP '[\x{2013}\x{2014}]' lib/core/rs-fetcher-patents.cjs lib/memory/validators/external-patents-invariants.cjs lib/memory/test-rs-fetcher-patents.cjs` returns 3 lines all 0
    - CJS only: `grep -cE '^(import |export )' lib/core/rs-fetcher-patents.cjs lib/memory/validators/external-patents-invariants.cjs` returns 2 lines all 0
    - Chokepoint exclusivity: `grep -c "function buildPatentsQuery" lib/core/rs-fetcher-patents.cjs` returns 1
    - auditQueryString invoked at least once inside buildPatentsQuery: `grep -c "auditQueryString" lib/core/rs-fetcher-patents.cjs` returns >= 1
    - ExternalEgressViolation imported from shared module: `grep -c "require.*rs-egress-violations" lib/core/rs-fetcher-patents.cjs` returns >= 1; `grep -c "class ExternalEgressViolation" lib/core/rs-fetcher-patents.cjs` returns 0
    - Both 2 sources defined: `grep -c "google_patents" lib/core/rs-fetcher-patents.cjs` >= 1 AND `grep -c "uspto" lib/core/rs-fetcher-patents.cjs` >= 1
    - recordTelemetry invoked: `grep -c "recordTelemetry" lib/core/rs-fetcher-patents.cjs` returns >= 5
    - AbortController used: `grep -c "AbortController" lib/core/rs-fetcher-patents.cjs` returns >= 1
    - Validator id: `grep -c "id: 'external-patents-invariants'" lib/memory/validators/external-patents-invariants.cjs` returns 1
    - Validator scope global: `grep -c "scope.*global" lib/memory/validators/external-patents-invariants.cjs` returns >= 1
    - Test suite exits 0: `node lib/memory/test-rs-fetcher-patents.cjs && echo PASS` outputs PASS
    - Test suite final line: `node lib/memory/test-rs-fetcher-patents.cjs 2>&1 | tail -1` contains "17/17 passed"
    - Both atomic commits: `git log --oneline -10 | grep -cE "(89.2-03).*RED|89.2-03.*GREEN"` returns 2
    - No release artifacts touched: `git diff HEAD~2 -- CHANGELOG.md .claude-plugin/plugin.json package.json | wc -l` returns 0
  </acceptance_criteria>
  <done>
    Patents fetcher ships covering Google Patents + USPTO. Canon Part 8 chokepoint locked: buildPatentsQuery is the ONLY URL builder; auditQueryString runs pre-egress; ExternalEgressViolation throws BEFORE network on forbidden patterns. Per-source rate-limit graceful degradation works. Validator drop-in registered. 11+6 fixture scenarios green; A1 sweep clean; A2 parity gate confirms FORBIDDEN_PATTERNS byte-for-byte match. Wave 3 preprocessor can now consume normalized patent objects from this fetcher's output.
  </done>
</task>

</tasks>

<verification>
- Test suite exits 0 with 17/17 passed
- Both patents sources covered with native fetch
- Canon Part 8 chokepoint exclusivity preserved
- ExternalEgressViolation imported from shared module
- Drop-in validator registered scope='global'
- 4 adversarial Canon Part 8 scenarios all throw BEFORE network egress
- Per-source rate-limit graceful degradation verified
- A1 sweep clean; A2 parity gate confirms FORBIDDEN_PATTERNS match
- Zero new runtime deps; CJS-only; BSL 1.1 + zero em-dashes
</verification>

<success_criteria>
Plan 89.2-03 complete when:
- [ ] lib/core/rs-fetcher-patents.cjs ships with fetchPatents + buildPatentsQuery
- [ ] Both sources covered (google_patents + uspto, both no-key)
- [ ] lib/memory/validators/external-patents-invariants.cjs ships drop-in
- [ ] lib/memory/test-rs-fetcher-patents.cjs ships with 11+6 scenarios + A1/A2
- [ ] Test suite exits 0 with 17/17 passed
- [ ] RED + GREEN atomic commits both in git log
- [ ] Zero release artifacts; zero new runtime deps
</success_criteria>

<output>
After completion, create `.planning/phases/89.2-external-research-fetching/89.2-03-SUMMARY.md`
</output>