---
phase: 89.2
plan: "02"
slug: fetcher-academic
type: execute
wave: 2
depends_on: ["89.2-01"]
files_modified:
  - lib/core/rs-fetcher-academic.cjs
  - lib/memory/validators/external-academic-invariants.cjs
  - lib/memory/test-rs-fetcher-academic.cjs
autonomous: true
canon_parts: [part-7, part-8]
milestone: v1.11.0
bundled_release: true
success_criteria_addressed: [SC-01, SC-10, SC-11]

must_haves:
  truths:
    - "fetchAcademic(queries, opts) returns { papers[], telemetry[] } where every paper carries {id, title, abstract, authors[], institution, doi, source, fetched_at}"
    - "Sources covered: openalex, arxiv (existing v1.10.16 baseline; CJS re-implementation per kickoff §9 CJS-prefer mandate) + scopus + pubmed + ieee + nature (NEW)"
    - "ONE chokepoint function buildAcademicQuery(query, source, opts) is the ONLY path that constructs an outbound URL/payload; NO direct fetch() calls anywhere outside this builder"
    - "Every buildAcademicQuery call invokes auditQueryString(query, 'academic') BEFORE any fetch() call -- ExternalEgressViolation throws pre-egress on FORBIDDEN_PATTERNS hit"
    - "API-key gated sources (scopus, ieee, nature) auto-disable (budget=0, status='api_key_missing') if env var absent; fetcher returns gracefully empty for that source; NEVER throws"
    - "Per-source rate-limit graceful degradation: 429/503 logs telemetry status='rate_limited', returns empty for that source, continues with remaining sources"
    - "Dedup deterministic: hash of (normalized_title + first_author_lastname); preserves first-seen order so OpenAlex wins ties"
    - "Validator (external-academic-invariants.cjs) drop-in registered to lib/memory/validators/; Checks A-F mirror brain-substrate-invariants byte-for-byte structure"
    - ">=4 adversarial fixtures stress per-source path: leaked-artifact-body in query, leaked-venture-name in query, leaked-meeting-fragment in query, leaked-financial-figure in query; ALL throw ExternalEgressViolation BEFORE network egress"
  artifacts:
    - path: lib/core/rs-fetcher-academic.cjs
      provides: "fetchAcademic + buildAcademicQuery (single chokepoint) + per-source dispatchers (fetchOpenAlex, fetchArxiv, fetchScopus, fetchPubMed, fetchIeee, fetchNature) + dedup helpers"
      min_lines: 400
    - path: lib/memory/validators/external-academic-invariants.cjs
      provides: "Drop-in validator: Check A telemetry-file shape; Check B per-source budget bounds; Check C status enum membership; Check D Canon Part 8 audit on persisted query_text_hash format; Check E hash-not-literal proof; Check F rolling-window math sanity"
      min_lines: 200
    - path: lib/memory/test-rs-fetcher-academic.cjs
      provides: "12 scenarios: happy path 6 sources, dedup, API-key-missing graceful, 429 rate-limit graceful, timeout graceful, malformed response graceful, 4 adversarial Canon Part 8, determinism + suite-end A1 sweep"
      min_lines: 500
  key_links:
    - from: "lib/core/rs-fetcher-academic.cjs::buildAcademicQuery"
      to: "lib/core/rs-egress-prompts.cjs::auditQueryString"
      via: "pre-fetch FORBIDDEN_PATTERNS audit; throws ExternalEgressViolation"
      pattern: "auditQueryString\\("
    - from: "lib/core/rs-fetcher-academic.cjs"
      to: "lib/core/rs-egress-violations.cjs::ExternalEgressViolation"
      via: "shared throwable across all 3 new fetchers"
      pattern: "ExternalEgressViolation"
    - from: "lib/core/rs-fetcher-academic.cjs"
      to: "lib/core/rs-egress-telemetry.cjs::recordTelemetry"
      via: "per-query outcome persisted to ~/.mindrian/telemetry/external-papers.json"
      pattern: "recordTelemetry\\("
---

<objective>
Ship `lib/core/rs-fetcher-academic.cjs` with `fetchAcademic(queries, opts)` -- the academic-paper egress surface for Wave 2 of Phase 89.2.

Six sources covered: **OpenAlex** + **arXiv** (existing v1.10.16 baseline at lib/core/rs_corpus.py; CJS re-implementation per kickoff §9 "prefer CJS for plugin-architecture consistency" + "use native fetch") + **Scopus** + **PubMed** + **IEEE** + **Nature/Science** (all NEW).

Single Canon Part 8 chokepoint: `buildAcademicQuery(query, source, opts)` -- the ONLY function that constructs outbound URLs/payloads. Every call invokes `auditQueryString(query, 'academic')` from rs-egress-prompts.cjs (Plan 89.2-01) BEFORE any `fetch()` call. ExternalEgressViolation throws pre-egress on FORBIDDEN_PATTERNS hit.

Per-source rate-limit graceful degradation per Phase 88.6-03 pattern: 429/503 -> log telemetry status='rate_limited' + return empty for that source + continue with remaining sources. NEVER throws on rate-limit (only on Canon Part 8 violation).

Companion drop-in validator `lib/memory/validators/external-academic-invariants.cjs` mirrors brain-substrate-invariants byte-for-byte structure: 6 checks on the persisted telemetry file at ~/.mindrian/telemetry/external-papers.json.

12-scenario adversarial fixture suite proves: 6 sources reachable, dedup deterministic, API-key-missing graceful, 429 graceful, timeout graceful, malformed response graceful, 4 Canon Part 8 adversarial scenarios block egress, determinism fence + A1 forbidden-pattern sweep.

Purpose: Ships SC-01 (rs-fetcher-academic.cjs exports fetchAcademic) + partial SC-10 (Canon Part 8 enforcement on academic egress) + partial SC-11 (per-source rate-limit graceful degradation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89.2-external-research-fetching/89.2-CONTEXT.md
@.planning/phases/89.2-external-research-fetching/89.2-01-egress-primitives-PLAN.md
@.planning/phases/89.1-domain-analysis-and-query-matrix/89.1-02-SUMMARY.md
@.planning/phases/89.1-domain-analysis-and-query-matrix/89.1-03-SUMMARY.md
@.planning/phases/89.1a-brain-methodology-substrate-loader/89.1a-02-SUMMARY.md
@.planning/phases/88.6-python-algorithm-wiring/88.6-03-PLAN.md
@lib/core/rs_corpus.py
@lib/core/rs-query-matrix.cjs
@lib/core/rs-egress-violations.cjs
@lib/core/rs-egress-prompts.cjs
@lib/core/rs-egress-telemetry.cjs
@lib/memory/validators/brain-substrate-invariants.cjs
@scripts/query-semantic-scholar.cjs
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Plan 89.2-01 outputs (CONSUME these). -->

From lib/core/rs-egress-violations.cjs:
```javascript
const { ExternalEgressViolation } = require('./rs-egress-violations.cjs');
// ExternalEgressViolation extends Error; meta {surface, matched_pattern, sample}
```

From lib/core/rs-egress-prompts.cjs:
```javascript
const { auditQueryString, auditQueryObject } = require('./rs-egress-prompts.cjs');
// Throws ExternalEgressViolation on FORBIDDEN_PATTERNS hit; returns input unchanged otherwise.
```

From lib/core/rs-egress-telemetry.cjs:
```javascript
const { recordTelemetry, computeRemainingBudget, DEFAULT_BUDGETS } = require('./rs-egress-telemetry.cjs');
// recordTelemetry({source, query_text, status, http_status?, rate_limit_remaining?}) -> {success, error?}
// computeRemainingBudget('openalex', 100) -> remaining count in rolling 24h window
// DEFAULT_BUDGETS.openalex === 100, DEFAULT_BUDGETS.arxiv === 100, etc.
```

External API endpoints (planner-verified during planning):
- OpenAlex: GET https://api.openalex.org/works?search={q}&per-page=200&cursor={cursor}&select=id,title,abstract_inverted_index,publication_year,authorships,doi  (no API key; polite pool via OPENALEX_EMAIL env var; 100k/day; 10 req/s)
- arXiv: GET https://export.arxiv.org/api/query?search_query=all:{q}&start=0&max_results=200  (no API key; ~3 req/s soft limit; 0.35s spacing)
- Scopus: GET https://api.elsevier.com/content/search/scopus?query={q}&apiKey={SCOPUS_API_KEY}  (env var SCOPUS_API_KEY; 20k/week)
- PubMed: GET https://eutils.ncbi.nlm.nih.gov/entrez/eutils/esearch.fcgi?db=pubmed&term={q}&retmax=200&retmode=json  (no API key; 3 req/s)
- IEEE: GET https://ieeexploreapi.ieee.org/api/v1/search/articles?querytext={q}&apikey={IEEE_API_KEY}&max_records=200  (env var IEEE_API_KEY; 200 calls/day)
- Nature/Springer: GET https://api.springernature.com/meta/v2/json?q={q}&api_key={NATURE_API_KEY}  (env var NATURE_API_KEY; 5000/day; ~25 alloc per CONTEXT)

Test mock pattern (mirror lib/memory/test-rs-brain-substrate.cjs lines 79-100 + scripts/query-semantic-scholar.cjs telemetry test pattern):
```javascript
function installMockFetch(responses) {
  // responses: Map<urlPrefix, () => Promise<{ok, status, json, text}>>
  global.__originalFetch = global.fetch;
  global.fetch = async function (url, opts) {
    for (const [prefix, handler] of responses.entries()) {
      if (url.startsWith(prefix)) return handler(url, opts);
    }
    throw new Error('Mock fetch: no handler for ' + url);
  };
}
function restoreFetch() { global.fetch = global.__originalFetch; }
```

Reference shape from existing lib/core/rs_corpus.py (Python; CJS port mirrors these helpers):
```javascript
// invertAbstract(invertedIndex) -> string  (port from rs_corpus.py invert_abstract)
// normalizeTitle(s) -> string (port from rs_corpus.py _normalize_title)
// dedupKey(doc) -> string  (port from rs_corpus.py _dedup_key)
// dedupe(docs) -> docs preserving first-seen order
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write rs-fetcher-academic.cjs + external-academic-invariants.cjs (RED 12-scenario suite first; then GREEN)</name>
  <files>lib/core/rs-fetcher-academic.cjs, lib/memory/validators/external-academic-invariants.cjs, lib/memory/test-rs-fetcher-academic.cjs</files>
  <read_first>
    - .planning/phases/89.2-external-research-fetching/89.2-CONTEXT.md (especially "Phase 1.5 4 External Fetchers" + "Per-Source Rate-Limit Strategy" + "Test Strategy" sections)
    - .planning/phases/89.2-external-research-fetching/89.2-01-egress-primitives-PLAN.md (Wave 1 dependencies you CONSUME; do NOT modify)
    - lib/core/rs-egress-violations.cjs (Plan 89.2-01 OUTPUT; CONSUME)
    - lib/core/rs-egress-prompts.cjs (Plan 89.2-01 OUTPUT; CONSUME via auditQueryString)
    - lib/core/rs-egress-telemetry.cjs (Plan 89.2-01 OUTPUT; CONSUME via recordTelemetry + computeRemainingBudget)
    - lib/core/rs_corpus.py (existing v1.10.16 OpenAlex + arXiv + Tavily fetcher; this plan PORTS to CJS, does NOT modify the Python; the Python remains for legacy callers)
    - lib/core/rs-query-matrix.cjs (89.1 OUTPUT consumed -- the 60-query matrix; this fetcher receives queries from generateQueryMatrix output)
    - lib/memory/validators/brain-substrate-invariants.cjs (drop-in validator template; this plan mirrors structure byte-for-byte for external-academic-invariants.cjs)
    - scripts/query-semantic-scholar.cjs lines 200-460 (Phase 88.6-03 telemetry + per-query outcome ledger + 429 graceful pattern reference)
    - lib/memory/test-rs-brain-substrate.cjs (14-scenario fixture pattern + installMockBrainClient + runScenario; this plan adapts to installMockFetch for HTTP)
    - docs/MINDRIAN-CANON.md Part 7 + Part 8
  </read_first>
  <behavior>
    Test 1 (happy path 6 sources): queries=['cancer immunotherapy biomarkers', 'CRISPR off-target detection']; mock fetch returns 5 papers per source (30 total before dedup, ~25 after). Result: papers.length >= 20 AND papers[0] has all required fields {id, title, abstract, authors[], institution, doi, source, fetched_at}; sources covered include openalex+arxiv+pubmed (no-API-key sources); scopus+ieee+nature appear ONLY if env vars set, else status='api_key_missing' in telemetry.

    Test 2 (dedup determinism): same queries + same mock responses invoked twice -> JSON.stringify(papers1) === JSON.stringify(papers2); first-seen order preserved (openalex wins doi ties).

    Test 3 (API-key-missing graceful): unset SCOPUS_API_KEY + IEEE_API_KEY + NATURE_API_KEY; fetchAcademic does NOT throw; telemetry has 3 entries with status='api_key_missing' for those sources; papers[] still populated from openalex+arxiv+pubmed.

    Test 4 (429 rate-limit graceful): mock OpenAlex returns {ok:false, status:429}; fetchAcademic does NOT throw; telemetry has entry status='rate_limited' http_status=429; papers[] still populated from arxiv+pubmed.

    Test 5 (timeout graceful): mock OpenAlex throws AbortError after 10s; fetchAcademic does NOT throw; telemetry status='timeout'; remaining sources continue.

    Test 6 (malformed response graceful): mock arXiv returns {ok:true, text: () => '<<<not-xml>>>'}; fetchAcademic does NOT throw; telemetry status='api_error'; remaining sources continue.

    Test 7 (per-source budget exhausted): seed telemetry with 100 openalex entries within 24h; computeRemainingBudget('openalex', 100) returns 0; fetchAcademic skips openalex; telemetry status='budget_exhausted' for openalex; arxiv+pubmed run normally.

    Test 8 (CANON PART 8 adversarial: leaked-artifact-body in query): queries=['cancer biomarkers <<artifact: meeting with Dr Smith Q4 financials >>']; buildAcademicQuery throws ExternalEgressViolation BEFORE any fetch() call; meta.surface === 'academic'; meta.matched_pattern === '\\\\bmeeting with\\\\b'; ZERO entries in telemetry for this query.

    Test 9 (CANON PART 8 adversarial: leaked-venture-name in query): queries=['Lawrence said our valuation is $5.2M cancer treatment']; buildAcademicQuery throws ExternalEgressViolation; meta.matched_pattern matches the quoted-person pattern; ZERO telemetry entries.

    Test 10 (CANON PART 8 adversarial: leaked-meeting-fragment in query): queries=['oncology meeting with Mayo Clinic Q3'] -> matches /\\bmeeting with\\b/i -> throws.

    Test 11 (CANON PART 8 adversarial: leaked-financial-figure in query): queries=['oncology partnerships under $750K threshold']; matches currency pattern -> throws.

    Test 12 (chokepoint exclusivity): grep over rs-fetcher-academic.cjs source for 'fetch(' and assert every match is INSIDE buildAcademicQuery scope OR is the per-source dispatcher (fetchOpenAlex/fetchArxiv/etc) which itself is called ONLY by buildAcademicQuery. Use a regex/AST walk; assert no leak.

    Suite-end audit A1: walk every captured request URL + every captured telemetry record; JSON.stringify + FORBIDDEN_PATTERNS scan; assert ZERO hits across all 12 scenarios. Mirrors 89.1 Plan 02 + Plan 03 A1 sweeps.

    Suite-end audit A2: parity gate -- assert FORBIDDEN_PATTERNS imported via rs-egress-prompts.cjs is byte-for-byte === cross-room-aggregator.cjs FORBIDDEN_PATTERNS at every regex.source index.

    Validator tests (in same suite, scenarios V1-V6 after T12):
    - V1 (Check A): telemetry file absent -> {severity: null, violations: []}
    - V2 (Check B): per-source budget exceeded -> warning violation
    - V3 (Check C): status enum violation (status: 'unknown_status') -> warning
    - V4 (Check D): persisted record contains query_text literal (NOT hash) -> CRITICAL canon_boundary
    - V5 (Check E): query_text_hash NOT matching /^[a-f0-9]{16}$/ -> warning
    - V6 (Check F): fetched_at malformed ISO8601 -> warning
  </behavior>
  <action>
    1. Write lib/memory/test-rs-fetcher-academic.cjs FIRST (RED commit). Mirror lib/memory/test-rs-brain-substrate.cjs structure byte-for-byte:
       - BSL 1.1 header (Phase 89.2 Plan 02 -- academic fetcher fixture suite)
       - 'use strict';
       - require core modules (will fail to load on RED -- expected)
       - 12 scenarios + 6 validator scenarios + A1 sweep + A2 parity gate per Behavior block
       - installMockFetch / restoreFetch helpers using global.fetch override (Node 18+ pattern)
       - Per scenario, restore fetch + clean ~/.mindrian/telemetry tmpfile after assertion
       - SCENARIO_RESULTS array captures every outbound URL + every telemetry record for end-of-suite audits
       - Final line: console.log('=== 89.2-02 fetcher-academic suite: 18/18 passed ===') (12 + 6 validator)
       Run `node lib/memory/test-rs-fetcher-academic.cjs` -> EXPECT exit 1 module-not-found. Commit RED `--no-verify`: `test(89.2-02): RED -- failing 18-scenario academic fetcher + validator test suite`.

    2. Write lib/core/rs-fetcher-academic.cjs (GREEN part 1):
       - BSL 1.1 header (Phase 89.2 Plan 02 -- academic external-egress fetcher; chokepoint via buildAcademicQuery)
       - 'use strict';
       - require rs-egress-violations + rs-egress-prompts + rs-egress-telemetry
       - SOURCES = Object.freeze(['openalex','arxiv','scopus','pubmed','ieee','nature']) -- frozen iteration order (determinism)
       - SOURCE_ENV_VARS = Object.freeze({scopus:'SCOPUS_API_KEY', ieee:'IEEE_API_KEY', nature:'NATURE_API_KEY'})  // openalex/arxiv/pubmed need no key
       - DEFAULT_TIMEOUT_MS = 10000; AbortController per request
       - USER_AGENT = 'MindrianOS-Plugin/1.11.0 (https://github.com/jsagir/mindrian-os-plugin)'
       - buildAcademicQuery(query, source, opts): validate query is non-empty string; validate source in SOURCES; auditQueryString(query, 'academic') (throws ExternalEgressViolation on FORBIDDEN_PATTERNS hit BEFORE any fetch); if SOURCE_ENV_VARS[source] AND !process.env[SOURCE_ENV_VARS[source]] -> return {skip: true, reason: 'api_key_missing'}; build URL+headers per source-specific endpoint shape (see Interfaces block above); return {url, headers, method:'GET', source}
       - fetchAcademic(queries, opts): validate queries is array of non-empty strings; out = {papers: [], telemetry: []}; for each source in SOURCES: if computeRemainingBudget(source, opts.budget?.[source]) === 0 -> recordTelemetry({source, query_text: '_budget_exhausted_', status:'budget_exhausted'}); continue; for each query in queries: try { const built = buildAcademicQuery(query, source, opts); if (built.skip) { recordTelemetry({source, query_text: query, status: built.reason}); continue; } const r = await fetchWithTimeout(built); const papers = parseSourceResponse(r, source); out.papers.push(...papers); recordTelemetry({source, query_text: query, status:'ok', http_status:r.status, rate_limit_remaining: parseRateLimit(r.headers)}); } catch (e) { if (e instanceof ExternalEgressViolation) throw e; if (e.name === 'AbortError') { recordTelemetry({source, query_text: query, status:'timeout'}); continue; } if (r?.status === 429 || r?.status === 503) { recordTelemetry({source, query_text: query, status:'rate_limited', http_status:r.status}); continue; } recordTelemetry({source, query_text: query, status:'api_error', http_status: r?.status || 0}); } end-end; out.papers = dedupe(out.papers); return out
       - fetchOpenAlex(query, opts), fetchArxiv(query, opts), fetchScopus(query, opts), fetchPubMed(query, opts), fetchIeee(query, opts), fetchNature(query, opts) -- per-source dispatchers each calling buildAcademicQuery + native fetch + parseSourceResponse
       - parseSourceResponse(r, source) -- per-source JSON/XML parsing returning normalized paper objects
       - normalizePaper({source, raw}) -> {id, title, abstract, authors[], institution, doi, source, fetched_at}
       - invertAbstract(invertedIndex) -> string  (port from rs_corpus.py invert_abstract)
       - normalizeTitle(s) -> string  (port from rs_corpus.py _normalize_title)
       - dedupKey(doc) -> string  (port from rs_corpus.py _dedup_key)
       - dedupe(docs) -> first-seen-wins
       - module.exports = { fetchAcademic, buildAcademicQuery, _test: { dedupe, normalizePaper, invertAbstract, fetchWithTimeout, parseSourceResponse, SOURCES, SOURCE_ENV_VARS, DEFAULT_TIMEOUT_MS, USER_AGENT } }

    3. Write lib/memory/validators/external-academic-invariants.cjs (GREEN part 2):
       - BSL 1.1 header (Phase 89.2 Plan 02 -- external academic fetcher invariants validator)
       - 'use strict';
       - Mirror lib/memory/validators/brain-substrate-invariants.cjs structure byte-for-byte
       - require rs-egress-prompts (FORBIDDEN_PATTERNS), rs-egress-telemetry (TELEMETRY_FILE, DEFAULT_BUDGETS, WINDOW_MS), node:fs, node:path
       - SEVERITY_MAP frozen as in brain-substrate-invariants
       - CANON_PART_8_HASH_REGEX = /^[a-f0-9]{16}$/
       - validate(roomPath, ctx) -> { severity, violations[] }:
         * Check A: TELEMETRY_FILE absent -> {severity: null, violations: []}
         * Check B: per-source budget exceeded in rolling 24h window -> warning per source
         * Check C: status enum violation (status not in ['ok','rate_limited','api_error','network_error','timeout','api_key_missing','budget_exhausted']) -> warning per entry
         * Check D: query_text literal present in any persisted record -> CRITICAL canon_boundary (regex test entries[i].query_text exists -> CRITICAL)
         * Check E: query_text_hash format violation (not matching /^[a-f0-9]{16}$/) -> warning per entry
         * Check F: fetched_at not parseable as ISO-8601 -> warning per entry
       - module.exports = { id: 'external-academic-invariants', severity_map: SEVERITY_MAP, scope: 'global', validate }
       - NOTE: scope='global' (not 'room') because telemetry is global per-user at ~/.mindrian/telemetry/

    4. Run `node lib/memory/test-rs-fetcher-academic.cjs`. EXPECT exit 0 with `=== 89.2-02 fetcher-academic suite: 18/18 passed ===`. Iterate on implementation (NOT test) until green. Commit GREEN `--no-verify`: `feat(89.2-02): GREEN -- academic fetcher (6 sources) + Canon Part 8 chokepoint + drop-in validator`.

    5. NO Feynman registration here -- Plan 89.2-08 owns sweep registration to avoid Wave-2 parallel-plan contention.
  </action>
  <verify>
    <automated>node lib/memory/test-rs-fetcher-academic.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `test -f lib/core/rs-fetcher-academic.cjs && test -f lib/memory/validators/external-academic-invariants.cjs && test -f lib/memory/test-rs-fetcher-academic.cjs`
    - BSL 1.1 on all 3: `for f in lib/core/rs-fetcher-academic.cjs lib/memory/validators/external-academic-invariants.cjs lib/memory/test-rs-fetcher-academic.cjs; do grep -c "BSL 1.1" $f; done` returns 3 lines all >= 1
    - Zero em-dashes: `grep -cP '[\x{2013}\x{2014}]' lib/core/rs-fetcher-academic.cjs lib/memory/validators/external-academic-invariants.cjs lib/memory/test-rs-fetcher-academic.cjs` returns 3 lines all 0
    - CJS only: `grep -cE '^(import |export )' lib/core/rs-fetcher-academic.cjs lib/memory/validators/external-academic-invariants.cjs` returns 2 lines all 0
    - Chokepoint exclusivity: `grep -c "function buildAcademicQuery" lib/core/rs-fetcher-academic.cjs` returns 1
    - auditQueryString invoked at least once inside buildAcademicQuery: `grep -c "auditQueryString" lib/core/rs-fetcher-academic.cjs` returns >= 1
    - ExternalEgressViolation imported from shared module (not redefined): `grep -c "require.*rs-egress-violations" lib/core/rs-fetcher-academic.cjs` returns >= 1; `grep -c "class ExternalEgressViolation" lib/core/rs-fetcher-academic.cjs` returns 0 (use the shared class, do NOT redefine)
    - All 6 sources defined: `grep -c "openalex" lib/core/rs-fetcher-academic.cjs` >= 1 AND `grep -c "arxiv" lib/core/rs-fetcher-academic.cjs` >= 1 AND `grep -c "scopus" lib/core/rs-fetcher-academic.cjs` >= 1 AND `grep -c "pubmed" lib/core/rs-fetcher-academic.cjs` >= 1 AND `grep -c "ieee" lib/core/rs-fetcher-academic.cjs` >= 1 AND `grep -c "nature" lib/core/rs-fetcher-academic.cjs` >= 1
    - recordTelemetry invoked: `grep -c "recordTelemetry" lib/core/rs-fetcher-academic.cjs` returns >= 5 (one per status branch + budget_exhausted)
    - AbortController used (timeout pattern): `grep -c "AbortController" lib/core/rs-fetcher-academic.cjs` returns >= 1
    - Validator scope is global (not room): `grep -c "scope.*global" lib/memory/validators/external-academic-invariants.cjs` returns >= 1
    - Validator has 6 checks: `grep -cE "Check (A|B|C|D|E|F)" lib/memory/validators/external-academic-invariants.cjs` returns >= 6
    - Test suite exits 0: `node lib/memory/test-rs-fetcher-academic.cjs && echo PASS` outputs PASS
    - Test suite final line matches: `node lib/memory/test-rs-fetcher-academic.cjs 2>&1 | tail -1` contains "18/18 passed"
    - Both atomic commits: `git log --oneline -10 | grep -cE "(89.2-02).*RED|89.2-02.*GREEN"` returns 2
    - No release artifacts touched: `git diff HEAD~2 -- CHANGELOG.md .claude-plugin/plugin.json package.json | wc -l` returns 0
    - No new runtime deps: `git diff HEAD~2 -- package.json | grep -E '^\+.*"' | grep -v version | wc -l` returns 0
  </acceptance_criteria>
  <done>
    Academic fetcher ships covering all 6 sources (OpenAlex+arXiv+Scopus+PubMed+IEEE+Nature). Canon Part 8 chokepoint locked: buildAcademicQuery is the ONLY URL builder; auditQueryString runs pre-egress; ExternalEgressViolation throws BEFORE network when forbidden patterns appear. Per-source rate-limit graceful degradation works (429/503/timeout/budget_exhausted/api_key_missing all log telemetry, fetcher continues). Validator drop-in registered for downstream guardian discovery (scope='global' because telemetry is global). 12+6 fixture scenarios green; A1 sweep clean; A2 parity gate confirms byte-for-byte FORBIDDEN_PATTERNS match. Wave 3 preprocessor + differential scorer can now consume normalized paper objects from this fetcher's output.
  </done>
</task>

</tasks>

<verification>
- Test suite exits 0 with 18/18 passed
- All 6 academic sources covered with native fetch (NO node-fetch, NO axios)
- Canon Part 8 chokepoint exclusivity preserved (buildAcademicQuery is the single URL builder)
- ExternalEgressViolation imported from shared rs-egress-violations.cjs (NOT redefined)
- Drop-in validator registered to lib/memory/validators/ with scope='global'
- 4 adversarial Canon Part 8 scenarios all throw BEFORE network egress
- Per-source rate-limit graceful degradation verified (429, timeout, api-key-missing, budget-exhausted)
- A1 forbidden-pattern sweep clean across all 18 captured outputs
- A2 parity gate confirms FORBIDDEN_PATTERNS byte-for-byte match
- Zero new runtime deps; CJS-only; BSL 1.1 + zero em-dashes on all 3 files
</verification>

<success_criteria>
Plan 89.2-02 complete when:
- [ ] lib/core/rs-fetcher-academic.cjs ships with fetchAcademic + buildAcademicQuery chokepoint
- [ ] All 6 academic sources covered (openalex+arxiv+pubmed no-key, scopus+ieee+nature env-key-gated)
- [ ] lib/memory/validators/external-academic-invariants.cjs ships as drop-in validator (scope='global')
- [ ] lib/memory/test-rs-fetcher-academic.cjs ships with 12+6 scenarios + A1/A2 sweeps
- [ ] All assertions green; suite exits 0 with 18/18 passed
- [ ] RED + GREEN atomic commits both in git log
- [ ] Zero release artifact diffs; zero new runtime deps
</success_criteria>

<output>
After completion, create `.planning/phases/89.2-external-research-fetching/89.2-02-SUMMARY.md`
</output>