---
phase: 89.2
plan: "04"
slug: fetcher-industry
type: execute
wave: 2
depends_on: ["89.2-01"]
files_modified:
  - lib/core/rs-fetcher-industry.cjs
  - lib/memory/validators/external-industry-invariants.cjs
  - lib/memory/test-rs-fetcher-industry.cjs
autonomous: true
canon_parts: [part-7, part-8]
milestone: v1.11.0
bundled_release: true
success_criteria_addressed: [SC-03, SC-10, SC-11]

must_haves:
  truths:
    - "fetchIndustry(queries, opts) returns { signals[], telemetry[] } where every signal carries {company, signal, source, url, fetched_at}"
    - "Source: tavily orchestration only (Tavily MCP already in plugin stack per CLAUDE.md tech stack); Crunchbase + corporate R&D + startup tracker queries flow through Tavily as the orchestrator (Tavily handles the web crawl tier; we don't re-implement Crunchbase API direct)"
    - "ONE chokepoint function buildIndustryQuery(query, opts) is the ONLY outbound builder; auditQueryString runs pre-fetch"
    - "If Tavily unavailable (no TAVILY_API_KEY env var): fetchIndustry returns gracefully empty; telemetry status='api_key_missing'; NEVER throws"
    - "Per-source rate-limit graceful degradation: 429/503 -> log telemetry, return empty"
    - "Dedup deterministic: hash of (company_normalized + signal_text_first_50_chars_hash); preserves first-seen order"
    - "Validator (external-industry-invariants.cjs) drop-in registered scope='global'"
    - ">=4 adversarial fixtures: 4 Canon Part 8 scenarios -> ALL throw ExternalEgressViolation BEFORE egress (leaked-meeting / leaked-quoted-person / forbidden-via-refined-sub-query / leaked-phone-number-in-refined-sub-query). Combined across egress fetchers (02 + 03 + 04): 4+4+4 = 12 adversarial scenarios meeting CONTEXT-required >=12."
  artifacts:
    - path: lib/core/rs-fetcher-industry.cjs
      provides: "fetchIndustry + buildIndustryQuery (single chokepoint) + Tavily orchestration + dedup"
      min_lines: 250
    - path: lib/memory/validators/external-industry-invariants.cjs
      provides: "Drop-in validator: 6 checks (telemetry shape, tavily budget, status enum, query_text-hash-not-literal, hash format, fetched_at format)"
      min_lines: 200
    - path: lib/memory/test-rs-fetcher-industry.cjs
      provides: "11 scenarios: happy path, dedup, api-key-missing graceful, 429 graceful, timeout graceful, malformed graceful, budget-exhausted, 4 adversarial (leaked-meeting, leaked-quoted-person, forbidden-via-refined-sub-query, leaked-phone-number-via-refined-sub-query) + suite-end A1 sweep + A2 parity gate"
      min_lines: 420
  key_links:
    - from: "lib/core/rs-fetcher-industry.cjs::buildIndustryQuery"
      to: "lib/core/rs-egress-prompts.cjs::auditQueryString"
      via: "pre-fetch FORBIDDEN_PATTERNS audit"
      pattern: "auditQueryString\\("
    - from: "lib/core/rs-fetcher-industry.cjs"
      to: "lib/core/rs-egress-violations.cjs::ExternalEgressViolation"
      via: "shared throwable"
      pattern: "ExternalEgressViolation"
    - from: "lib/core/rs-fetcher-industry.cjs"
      to: "lib/core/rs-egress-telemetry.cjs::recordTelemetry"
      via: "per-query outcome persisted"
      pattern: "recordTelemetry\\("
---

<objective>
Ship `lib/core/rs-fetcher-industry.cjs` with `fetchIndustry(queries, opts)` -- the industry-signal egress surface for Wave 2 of Phase 89.2.

Source: **Tavily** orchestration only. Per CONTEXT.md: "Crunchbase + corporate R&D + startup trackers (Tavily-orchestrated)". The Tavily MCP is already in the plugin stack (CLAUDE.md tech stack); we don't re-implement Crunchbase direct -- we use Tavily as the search-orchestration tier with refined queries that target startup-database content (e.g. "Crunchbase site:crunchbase.com {query}", "AngelList site:angel.co {query}", etc.).

Single Canon Part 8 chokepoint: `buildIndustryQuery(query, opts)` -- the ONLY outbound builder. Mirrors Plans 89.2-02 + 89.2-03 fetcher pattern byte-for-byte.

If `TAVILY_API_KEY` env var absent: graceful degradation -- fetchIndustry returns `{signals: [], telemetry: [...]}` with telemetry status='api_key_missing'; never throws.

Companion drop-in validator + 11-scenario adversarial fixture suite (4 adversarial Canon Part 8 scenarios). Same Canon Part 8 5-tripwire pattern as Plans 89.2-02 + 89.2-03. Combined adversarial fixture count across the 3 egress fetchers (02 + 03 + 04) = 4 + 4 + 4 = 12, meeting the CONTEXT-required >=12.

Purpose: Ships SC-03 (rs-fetcher-industry.cjs exports fetchIndustry) + partial SC-10 (Canon Part 8 enforcement on industry egress) + partial SC-11 (graceful degradation when Tavily unavailable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89.2-external-research-fetching/89.2-CONTEXT.md
@.planning/phases/89.2-external-research-fetching/89.2-01-egress-primitives-PLAN.md
@.planning/phases/89.2-external-research-fetching/89.2-02-fetcher-academic-PLAN.md
@.planning/phases/89.2-external-research-fetching/89.2-03-fetcher-patents-PLAN.md
@lib/core/rs-egress-violations.cjs
@lib/core/rs-egress-prompts.cjs
@lib/core/rs-egress-telemetry.cjs
@lib/core/rs_corpus.py
@lib/memory/validators/brain-substrate-invariants.cjs
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Plan 89.2-01 outputs (CONSUME these). -->

External API endpoint:
- Tavily: POST https://api.tavily.com/search with payload {api_key, query, max_results, search_depth:'advanced'} -- existing pattern in lib/core/rs_corpus.py::fetch_tavily

Tavily query refinement strategy (planner-locked):
- For each user query, build 3 refined sub-queries targeting different startup-database content:
  1. "{query} startup OR company OR venture site:crunchbase.com OR site:pitchbook.com"
  2. "{query} corporate research lab OR R&D announcement"
  3. "{query} early stage funding OR seed round OR series A OR series B"
- Each sub-query passes through buildIndustryQuery (auditQueryString runs on EACH); throws ExternalEgressViolation if any forbidden pattern in either user query OR refined sub-query.

Industry signal record shape:
```javascript
{
  company: 'Example Corp',           // extracted from Tavily result.title or url domain
  signal: 'raised $5M Series A for cancer immunotherapy',  // from Tavily result.content first 200 chars
  source: 'tavily',
  url: 'https://example.com/article',
  fetched_at: '2026-04-26T14:00:00Z',
}
```

Default budget: tavily=30 (24h window) per CONTEXT.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write rs-fetcher-industry.cjs + external-industry-invariants.cjs (RED 11+6 scenario suite first; then GREEN)</name>
  <files>lib/core/rs-fetcher-industry.cjs, lib/memory/validators/external-industry-invariants.cjs, lib/memory/test-rs-fetcher-industry.cjs</files>
  <read_first>
    - .planning/phases/89.2-external-research-fetching/89.2-CONTEXT.md (especially "Phase 1.5" + "Per-Source Rate-Limit Strategy" + "Specific Ideas Tavily orchestration")
    - .planning/phases/89.2-external-research-fetching/89.2-01-egress-primitives-PLAN.md (Wave 1 dependencies you CONSUME)
    - .planning/phases/89.2-external-research-fetching/89.2-02-fetcher-academic-PLAN.md (template -- industry fetcher mirrors structure)
    - lib/core/rs_corpus.py lines 287-345 (existing v1.10.16 Tavily integration; the CJS port mirrors this Python pattern)
    - lib/core/rs-egress-violations.cjs (CONSUME)
    - lib/core/rs-egress-prompts.cjs (CONSUME via auditQueryString)
    - lib/core/rs-egress-telemetry.cjs (CONSUME via recordTelemetry + computeRemainingBudget)
    - lib/memory/validators/brain-substrate-invariants.cjs (drop-in validator template)
    - lib/memory/test-rs-brain-substrate.cjs (14-scenario fixture pattern)
    - docs/MINDRIAN-CANON.md Part 7 + Part 8
  </read_first>
  <behavior>
    Test 1 (happy path): queries=['cancer immunotherapy startups', 'CRISPR funding rounds 2024']; mock Tavily POST returns 5 results per refined sub-query (3 sub-queries x 5 = 15 raw results per user query); fetchIndustry returns signals.length >= 10 (after dedup); each signal has {company non-empty, signal length 1-200 chars, source='tavily', url valid URL, fetched_at ISO-8601}.

    Test 2 (dedup determinism): same queries + same mock responses twice -> JSON.stringify(signals1) === JSON.stringify(signals2); first-seen wins.

    Test 3 (TAVILY_API_KEY missing graceful): unset TAVILY_API_KEY (delete process.env.TAVILY_API_KEY); fetchIndustry returns {signals: [], telemetry: [{source:'tavily', status:'api_key_missing'}]}; does NOT throw.

    Test 4 (429 rate-limit graceful): mock Tavily returns {ok:false, status:429}; fetchIndustry does NOT throw; telemetry status='rate_limited' http_status=429; signals: [].

    Test 5 (timeout graceful): mock Tavily throws AbortError; fetchIndustry does NOT throw; telemetry status='timeout'; signals: [].

    Test 6 (malformed JSON graceful): mock Tavily returns {ok:true, json: () => Promise.reject(new SyntaxError('invalid JSON'))}; fetchIndustry does NOT throw; telemetry status='api_error'; signals: [].

    Test 7 (per-source budget exhausted): seed telemetry with 30 tavily entries within 24h; computeRemainingBudget('tavily', 30) === 0; fetchIndustry skips Tavily; telemetry status='budget_exhausted'; signals: [].

    Test 8 (CANON PART 8 adversarial #1 -- leaked-meeting / leaked-artifact-body in user query): queries=['CRISPR <<artifact: meeting with Genentech Q4 financials>>']; buildIndustryQuery throws ExternalEgressViolation BEFORE any fetch(); meta.surface === 'industry'; ZERO entries in telemetry.

    Test 9 (CANON PART 8 adversarial #2 -- leaked-quoted-person in user query): queries=['Lawrence said our competition raised funding']; throws ExternalEgressViolation BEFORE any fetch().

    Test 10 (CANON PART 8 adversarial #3 -- forbidden pattern smuggled via REFINED sub-query): user query is clean ("biotech startups"), but refined sub-query template contains forbidden pattern injected via opts (e.g. opts.refinement_template_override = 'meeting with {query}'); buildIndustryQuery audits BOTH user query AND each refined sub-query; throws ExternalEgressViolation on the refined sub-query (the auditQueryString call on each refined sub-query is the second layer of defense).

    Test 11 (CANON PART 8 adversarial #4 -- leaked phone-number pattern via refined sub-query template): user query is clean ("biotech startups"), but opts.refinement_template_override = '{query} contact 555-867-5309 sales'; the refined sub-query 'biotech startups contact 555-867-5309 sales' contains a phone-like pattern (matches FORBIDDEN_PATTERNS phone regex /\\b\\+?1?[\\s.\\-]?\\(?\\d{3}\\)?[\\s.\\-]?\\d{3}[\\s.\\-]?\\d{4}\\b/); buildIndustryQuery's second-layer auditQueryString throws ExternalEgressViolation BEFORE any fetch(); meta.matched_pattern.includes('d{3}') AND meta.surface === 'industry'; ZERO entries in telemetry. This proves the second-layer audit catches forbidden patterns ACROSS THE FULL FAMILY of FORBIDDEN_PATTERNS (Tests 8 + 9 covered meeting + quoted-person; Test 10 covered meeting via override; Test 11 covers phone-like via override -- four distinct adversarial vectors).

    Suite-end audit A1: walk every captured outbound URL + every captured telemetry record; JSON.stringify + FORBIDDEN_PATTERNS scan; assert ZERO hits across all 11 scenarios.

    Suite-end audit A2: parity gate -- assert FORBIDDEN_PATTERNS imported via rs-egress-prompts.cjs is byte-for-byte === cross-room-aggregator.cjs FORBIDDEN_PATTERNS at every regex.source index.

    Validator tests V1-V6 (after T11): same 6 checks as Plans 89.2-02/89.2-03 validators, scoped to source='tavily'.
  </behavior>
  <action>
    1. Write lib/memory/test-rs-fetcher-industry.cjs FIRST (RED commit). Mirror lib/memory/test-rs-fetcher-academic.cjs structure, swapping:
       - test name string ("89.2-04 fetcher-industry suite: 17/17 passed")
       - mocked URL to https://api.tavily.com/search
       - record shape assertions to industry signal shape (company + signal + url)
       - source list to ['tavily'] only
       - Add Test 10 + Test 11 explicitly verifying the two-layer refined-sub-query audit defense (Test 10 = meeting pattern via override; Test 11 = phone pattern via override)
       Run `node lib/memory/test-rs-fetcher-industry.cjs` -> EXPECT exit 1. Commit RED `--no-verify`: `test(89.2-04): RED -- failing 17-scenario industry fetcher + validator test suite`.

    2. Write lib/core/rs-fetcher-industry.cjs (GREEN part 1):
       - BSL 1.1 header (Phase 89.2 Plan 04 -- industry-signal external-egress fetcher; chokepoint via buildIndustryQuery)
       - Mirror lib/core/rs-fetcher-academic.cjs structure, swapping:
         * SOURCES = Object.freeze(['tavily'])
         * SOURCE_ENV_VARS = Object.freeze({tavily: 'TAVILY_API_KEY'})
         * REFINEMENT_TEMPLATES = Object.freeze([
             '{query} startup OR company OR venture site:crunchbase.com OR site:pitchbook.com',
             '{query} corporate research lab OR R&D announcement',
             '{query} early stage funding OR seed round OR series A OR series B',
           ]) -- frozen-order for determinism
         * buildIndustryQuery(query, opts):
           - validate query is non-empty string
           - auditQueryString(query, 'industry') -- first audit layer (user query)
           - if !process.env.TAVILY_API_KEY -> return {skip: true, reason: 'api_key_missing'}
           - templates = (opts.refinement_template_override ? [opts.refinement_template_override] : REFINEMENT_TEMPLATES)  // opts override allowed for testing the second-layer audit; in prod always REFINEMENT_TEMPLATES
           - for each tmpl in templates: const refined = tmpl.replace('{query}', query); auditQueryString(refined, 'industry')  // second audit layer (refined sub-query)
           - return {url: 'https://api.tavily.com/search', method: 'POST', body: JSON.stringify({api_key: process.env.TAVILY_API_KEY, query: refined, max_results: opts.maxResultsPerSubquery || 5, search_depth: 'advanced'}), refined_subqueries: templates.map(t => t.replace('{query}', query))}
         * fetchIndustry(queries, opts): for each query: try { const built = buildIndustryQuery(query, opts); if (built.skip) { recordTelemetry({source:'tavily', query_text: query, status: built.reason}); continue; } if (computeRemainingBudget('tavily', opts.budget?.tavily) === 0) { recordTelemetry({source:'tavily', query_text: query, status:'budget_exhausted'}); continue; } for each refined in built.refined_subqueries: const r = await fetchWithTimeout({url: built.url, method:'POST', body: ..., headers: {'Content-Type':'application/json'}}); const sigs = parseTavilyResponse(r); out.signals.push(...sigs); recordTelemetry({source:'tavily', query_text: refined, status:'ok', http_status: r.status}); end-end; out.signals = dedupe(out.signals); return out
         * parseTavilyResponse(r) -> array of signals from r.json().results
         * normalizeSignal({raw}) -> {company: extractCompany(raw.url, raw.title), signal: raw.content.slice(0,200), source:'tavily', url: raw.url, fetched_at: new Date().toISOString()}
         * extractCompany(url, title) -> domain segment + first noun phrase from title (deterministic; NO LLM)
         * dedupKey(s) -> 'industry:' + sha256(s.company.toLowerCase() + '|' + s.signal.slice(0,50)).slice(0,16)
         * dedupe(signals) -> first-seen-wins
       - module.exports = { fetchIndustry, buildIndustryQuery, _test: { dedupe, normalizeSignal, extractCompany, parseTavilyResponse, fetchWithTimeout, REFINEMENT_TEMPLATES, SOURCES, SOURCE_ENV_VARS } }

    3. Write lib/memory/validators/external-industry-invariants.cjs (GREEN part 2):
       - BSL 1.1 header (Phase 89.2 Plan 04 -- external industry fetcher invariants validator)
       - Mirror lib/memory/validators/external-academic-invariants.cjs byte-for-byte, swapping:
         * id: 'external-industry-invariants'
         * Per-source budget check is for SOURCES_INDUSTRY = ['tavily'] subset
       - module.exports = { id: 'external-industry-invariants', severity_map: SEVERITY_MAP, scope: 'global', validate }

    4. Run `node lib/memory/test-rs-fetcher-industry.cjs`. EXPECT exit 0 with "17/17 passed". Iterate implementation until green. Commit GREEN `--no-verify`: `feat(89.2-04): GREEN -- industry fetcher (Tavily orchestration) + Canon Part 8 chokepoint with two-layer audit (4 adversarial scenarios) + drop-in validator`.

    5. NO Feynman registration here -- Plan 89.2-08 owns sweep registration.
  </action>
  <verify>
    <automated>node lib/memory/test-rs-fetcher-industry.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `test -f lib/core/rs-fetcher-industry.cjs && test -f lib/memory/validators/external-industry-invariants.cjs && test -f lib/memory/test-rs-fetcher-industry.cjs`
    - BSL 1.1 on all 3: `for f in lib/core/rs-fetcher-industry.cjs lib/memory/validators/external-industry-invariants.cjs lib/memory/test-rs-fetcher-industry.cjs; do grep -c "BSL 1.1" $f; done` returns 3 lines all >= 1
    - Zero em-dashes: `grep -cP '[\x{2013}\x{2014}]' lib/core/rs-fetcher-industry.cjs lib/memory/validators/external-industry-invariants.cjs lib/memory/test-rs-fetcher-industry.cjs` returns 3 lines all 0
    - CJS only: `grep -cE '^(import |export )' lib/core/rs-fetcher-industry.cjs lib/memory/validators/external-industry-invariants.cjs` returns 2 lines all 0
    - Chokepoint exclusivity: `grep -c "function buildIndustryQuery" lib/core/rs-fetcher-industry.cjs` returns 1
    - Two-layer audit pattern: `grep -c "auditQueryString" lib/core/rs-fetcher-industry.cjs` returns >= 2 (one for user query, one for each refined sub-query inside the loop)
    - ExternalEgressViolation imported (NOT redefined): `grep -c "require.*rs-egress-violations" lib/core/rs-fetcher-industry.cjs` returns >= 1; `grep -c "class ExternalEgressViolation" lib/core/rs-fetcher-industry.cjs` returns 0
    - Tavily MCP integration: `grep -c "TAVILY_API_KEY" lib/core/rs-fetcher-industry.cjs` returns >= 1
    - REFINEMENT_TEMPLATES frozen + 3 templates: `grep -c "REFINEMENT_TEMPLATES" lib/core/rs-fetcher-industry.cjs` >= 2 AND `grep -c "Object.freeze" lib/core/rs-fetcher-industry.cjs` >= 1
    - recordTelemetry invoked: `grep -c "recordTelemetry" lib/core/rs-fetcher-industry.cjs` returns >= 4
    - AbortController used: `grep -c "AbortController" lib/core/rs-fetcher-industry.cjs` returns >= 1
    - Validator id + scope: `grep -c "id: 'external-industry-invariants'" lib/memory/validators/external-industry-invariants.cjs` returns 1; `grep -c "scope.*global" lib/memory/validators/external-industry-invariants.cjs` returns >= 1
    - Test suite has 4 distinct adversarial scenarios: `grep -cE "(adversarial|CANON PART 8)" lib/memory/test-rs-fetcher-industry.cjs` returns >= 4
    - Test suite exits 0: `node lib/memory/test-rs-fetcher-industry.cjs && echo PASS` outputs PASS
    - Test suite final line: `node lib/memory/test-rs-fetcher-industry.cjs 2>&1 | tail -1` contains "17/17 passed"
    - Both atomic commits: `git log --oneline -10 | grep -cE "(89.2-04).*RED|89.2-04.*GREEN"` returns 2
    - No release artifacts touched: `git diff HEAD~2 -- CHANGELOG.md .claude-plugin/plugin.json package.json | wc -l` returns 0
  </acceptance_criteria>
  <done>
    Industry fetcher ships via Tavily orchestration. Canon Part 8 two-layer audit locked: buildIndustryQuery audits BOTH user query AND each refined sub-query (catches forbidden patterns smuggled via either input vector). Graceful degradation when TAVILY_API_KEY absent (returns empty, telemetry status='api_key_missing', never throws). Validator drop-in registered. 11 fixture scenarios + 6 validator tests = 17/17 green; A1 sweep clean; A2 parity gate confirms FORBIDDEN_PATTERNS byte-for-byte match. 4 adversarial Canon Part 8 scenarios cover meeting (Test 8) + quoted-person (Test 9) + meeting-via-override (Test 10) + phone-via-override (Test 11), bringing combined egress-fetcher adversarial coverage to 4+4+4=12 across plans 02 + 03 + 04. Wave 3 preprocessor can now consume normalized industry signals.
  </done>
</task>

</tasks>

<verification>
- Test suite exits 0 with 17/17 passed
- Tavily orchestration via existing TAVILY_API_KEY env var (NO new dep)
- Canon Part 8 TWO-LAYER chokepoint: user query + refined sub-query both audited
- 4 distinct adversarial scenarios: meeting, quoted-person, meeting-via-override, phone-via-override
- ExternalEgressViolation imported from shared module
- Drop-in validator registered scope='global'
- Graceful degradation when Tavily unavailable
- Per-source rate-limit graceful degradation verified
- A1 sweep clean; A2 parity gate confirms FORBIDDEN_PATTERNS match
- Zero new runtime deps; CJS-only; BSL 1.1 + zero em-dashes
</verification>

<success_criteria>
Plan 89.2-04 complete when:
- [ ] lib/core/rs-fetcher-industry.cjs ships with fetchIndustry + buildIndustryQuery (two-layer audit)
- [ ] Tavily orchestration with 3 frozen-order refinement templates
- [ ] lib/memory/validators/external-industry-invariants.cjs ships drop-in
- [ ] lib/memory/test-rs-fetcher-industry.cjs ships with 11 scenarios + 6 validator + A1/A2 = 17 total
- [ ] 4 adversarial Canon Part 8 scenarios verified (meeting + quoted-person + meeting-via-override + phone-via-override)
- [ ] Test suite exits 0 with 17/17 passed
- [ ] RED + GREEN atomic commits both in git log
- [ ] Zero release artifacts; zero new runtime deps
</success_criteria>

<output>
After completion, create `.planning/phases/89.2-external-research-fetching/89.2-04-SUMMARY.md`
</output>
