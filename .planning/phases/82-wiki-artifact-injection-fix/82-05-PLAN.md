---
phase: "82"
plan_number: "05"
name: "CHANGELOG + version bump + 5-gate release"
status: draft
depends_on: ["82-04"]
parallel_safe: false
estimated_size: small
---

# Plan 82-05: CHANGELOG + version bump + 5-gate release

## Goal

Ship v1.10.5 by executing all five gates of the mandatory release pipeline documented in `.claude/includes/release-process.md`: (1) CHANGELOG.md entry for [1.10.5] at the top using the draft from research section 9 verbatim with the required credits and notes, (2) `.claude-plugin/plugin.json` version bumped from 1.10.4 to 1.10.5, (3) `package.json` version bumped from 1.10.4 to 1.10.5, (4) git tag `v1.10.5` created and pushed, (5) `~/mindrian-marketplace/.claude-plugin/marketplace.json` updated so the source.ref is pinned to v1.10.5 with a commit+push. Additionally update `.planning/PROJECT.md` to shift the smart-notebook milestone reference from v1.10.5 to v1.10.6, update `.planning/TODO.md` to remove the shipped wiki-fix item and promote smart-notebook, and mark `.planning/ROADMAP.md` Phase 82 as shipped. The release must pass the 5-gate consistency check at the end. The goal is testable: `git tag -l v1.10.5` returns the tag, `git log origin/main..HEAD` is empty, the marketplace repo has v1.10.5 committed and pushed, and `grep -rn "\u2014\|\u2013" CHANGELOG.md .claude-plugin/plugin.json package.json .planning/PROJECT.md .planning/TODO.md .planning/ROADMAP.md` returns zero lines.

## Scope

Files modified in MindrianOS-Plugin repo:
- `CHANGELOG.md` (+ ~60 lines at the top)
- `.claude-plugin/plugin.json` (1 line changed: `"version": "1.10.4"` -> `"version": "1.10.5"`)
- `package.json` (1 line changed: `"version": "1.10.4"` -> `"version": "1.10.5"`)
- `.planning/PROJECT.md` (1-3 lines changed in the Notion Template Gap Close section: shift smart-notebook milestone pointer from v1.10.5 to v1.10.6)
- `.planning/TODO.md` (2-5 lines changed: remove the [NEXT] v1.10.5 wiki fix entry, promote smart-notebook to [NEXT] targeting v1.10.6)
- `.planning/ROADMAP.md` (1-3 lines changed: mark Phase 82 entry shipped with 2026-04-14 date)

Files modified in mindrian-marketplace repo (cross-repo, pre-authorized per CONTEXT):
- `~/mindrian-marketplace/.claude-plugin/marketplace.json` (2 lines changed: version field and source.ref both updated from 1.10.4 to 1.10.5)

Git operations in MindrianOS-Plugin repo:
- Commit with message `release: v1.10.5 -- wiki artifact injection fix (Lawrence bug)` including the `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>` trailer
- Annotated tag `v1.10.5` created on the release commit
- Push `origin main --tags`

Git operations in mindrian-marketplace repo:
- Commit updating marketplace.json
- Push `origin master`

Files NOT modified:
- Any source code file (that is plans 82-01 through 82-04's job; this plan is release-only)
- Any other phase directory
- Any file in the plugin cache at `~/.claude/plugins/` (workspace guard)

## Out of Scope

- Any source code change
- Any test addition
- Any reorganization of `.planning/phases/` beyond marking 82 shipped
- Beta gating (CONTEXT explicitly says this is not release infrastructure, so it ships direct to stable)
- Shipping smart-notebook in this release (deferred to v1.10.6 per CONTEXT)
- Rolling back any prior release

## Task Breakdown

1. **Pre-flight workspace guard.** Run `pwd` and confirm output is `/home/jsagi/MindrianOS-Plugin`. Run `git fetch origin main` then `git status` and `git log origin/main..HEAD` to confirm the workspace is current and only contains plans 82-01 through 82-04 commits plus this plan's changes. If the workspace is in `~/.claude/plugins/*`, STOP and cd to `~/MindrianOS-Plugin`. Acceptance: `pwd` returns the canonical workspace path and `git status` shows a clean tree before starting.

2. **Write CHANGELOG.md [1.10.5] entry at the top.** Insert immediately below the existing line 6 `and this project adheres to [Semantic Versioning]` block and its comment lines, above the `## [1.10.4]` section. The entry MUST use the draft from `.planning/research/wiki-artifact-injection-bloat-analysis.md` section 9 verbatim, with these exact inclusions:
   - Header: `## [1.10.5] - 2026-04-14`
   - `onboarding: true` + `onboard_steps:` list (at least one step telling users to re-run /mos:present to pick up the fix, and one step noting that pre-81 rooms continue to work with the H1 title fallback)
   - `### Fixed` section: wiki artifact content now renders, credit Lawrence Aronhime by name with the 2026-04-13 23:23 timestamp, note the bug had been sitting since v1.9.6 across 8 subsequent releases, note that Lawrence workarounded on his own machine by injecting content into ROOM_DATA
   - `### Changed` section: SYSTEM_FILES single source of truth (SKIP_FILES alignment), section summary upgrades to MINTO governing_thought when available via the v1.10.2 Feynman-MINTO infrastructure, explicit note that this is a free leverage of v1.10.2 infra for summary upgrades
   - `### Added` section: per-artifact 20 KB cap with truncation banner, per-room 2 MB cap with stderr warning and in-wiki yellow banner, 9 new fixture-based tests in `scripts/generate-presentation.test.cjs` using node built-in assert
   - `### Notes` section: smart-notebook milestone shifts from v1.10.5 to v1.10.6 so this fix can ship same-day. Backwards compatibility for pre-81 rooms is preserved via the H1 title fallback. Upgrade path: standard two-command `/plugin marketplace update` + `claude plugin update mos@mindrian-marketplace`. Existing exported wiki.html files do not auto-regenerate; users must re-run `/mos:present` against their room.
   - `### Credit` subsection at the end: Lawrence Aronhime (Prof., Johns Hopkins Carey Business School) with the 2026-04-13 23:23 report timestamp and a thank you. Note that Lawrence has been running beta builds since v1.9.x and holds the lawrence@mindrian.ai admin Brain API key issued 2026-03-26.
   - **ZERO em-dashes, zero en-dashes, zero emoji.** Use hyphens. Grep the section before saving.

   Acceptance: `head -80 CHANGELOG.md` shows the new entry. `grep -c "Lawrence" CHANGELOG.md` is at least 2 (Lawrence credited in Fixed section and in Credit subsection). `grep -n "\u2014\|\u2013" CHANGELOG.md` returns zero lines. `grep -n "1\.10\.2" CHANGELOG.md` finds the Feynman-MINTO infrastructure note.

3. **Bump `.claude-plugin/plugin.json` version.** Change the `"version"` field from `"1.10.4"` to `"1.10.5"`. Do not touch any other field. Acceptance: `grep "\"version\"" .claude-plugin/plugin.json` shows `"version": "1.10.5"`.

4. **Bump `package.json` version.** Change the `"version"` field from `"1.10.4"` to `"1.10.5"`. Acceptance: `grep "\"version\"" package.json | head -1` shows `"version": "1.10.5"`.

5. **Update `.planning/PROJECT.md` smart-notebook milestone slot.** Grep for `smart-notebook` and `v1.10.5` in PROJECT.md. Change any reference that targets smart-notebook at v1.10.5 to v1.10.6. Update surrounding prose if it explicitly states "next milestone is v1.10.5". The Notion Template Gap Close section is the primary target per the user prompt. Acceptance: `grep -n "smart-notebook" .planning/PROJECT.md` shows the milestone now targeting v1.10.6.

6. **Update `.planning/TODO.md`.** Remove the `[NEXT]` entry for v1.10.5 wiki fix (it has shipped). Promote the smart-notebook item from its current slot to `[NEXT]` with a v1.10.6 target. If smart-notebook was already the next item, just update its version target. Acceptance: `grep -n "\[NEXT\]" .planning/TODO.md` shows smart-notebook as the next work, not the wiki fix.

7. **Update `.planning/ROADMAP.md` Phase 82 entry.** Grep for `Phase 82` or `82-wiki-artifact-injection-fix` in ROADMAP.md. Mark it as shipped with the date 2026-04-14. Typical format: change status from `[planned]` or `[in-progress]` to `[shipped 2026-04-14]` or equivalent. Acceptance: `grep -n "82" .planning/ROADMAP.md` shows the shipped marker with the date.

8. **Grep for em-dashes across all touched files.** Run: `grep -n "\u2014\|\u2013" CHANGELOG.md .claude-plugin/plugin.json package.json .planning/PROJECT.md .planning/TODO.md .planning/ROADMAP.md scripts/generate-presentation.cjs scripts/generate-presentation.test.cjs templates/presentation/wiki.html`. If any matches are found, fix them before commit. Acceptance: the grep command returns zero lines.

9. **Grep for emoji across all touched files.** The statusline carve-out from v1.10.4 does NOT apply here. Run a basic check against the CHANGELOG and the plan docs for common emoji ranges. Acceptance: no emoji in CHANGELOG [1.10.5] entry, no emoji in any source file modified by this phase.

10. **Stage and commit in MindrianOS-Plugin repo.** Run:
    ```
    git add CHANGELOG.md .claude-plugin/plugin.json package.json .planning/PROJECT.md .planning/TODO.md .planning/ROADMAP.md
    ```
    Check `git status` to confirm only the six files above are staged. Do NOT use `git add -A` or `git add .`. Commit with a heredoc:
    ```
    git commit -m "$(cat <<'EOF'
    release: v1.10.5 -- wiki artifact injection fix (Lawrence bug)
    
    Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```
    Acceptance: `git log -1 --format=%s` shows the release commit subject. `git log -1 --format=%b` shows the Co-Authored-By trailer.

11. **Create annotated tag v1.10.5.** Run `git tag -a v1.10.5 -m "v1.10.5 - Wiki artifact injection fix (Lawrence bug)"`. Acceptance: `git tag -l v1.10.5` returns `v1.10.5`.

12. **Push commit and tag to origin.** Run `git push origin main --tags`. Acceptance: `git log origin/main..HEAD` is empty.

13. **Cross-repo marketplace update.** Run:
    ```
    cd ~/mindrian-marketplace
    pwd  # confirm we are in the marketplace repo
    git fetch origin master
    git status  # must be clean
    sed -i 's/"version": "1.10.4"/"version": "1.10.5"/; s/"ref": "v1.10.4"/"ref": "v1.10.5"/' .claude-plugin/marketplace.json
    grep -n "1.10.5" .claude-plugin/marketplace.json  # confirm both lines updated
    git add .claude-plugin/marketplace.json
    git commit -m "$(cat <<'EOF'
    pin mos to v1.10.5 -- wiki artifact injection fix
    
    Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
    EOF
    )"
    git push origin master
    ```
    Then `cd` back to `/home/jsagi/MindrianOS-Plugin`. Acceptance: `cd ~/mindrian-marketplace && git log origin/master..HEAD` is empty, and `grep "1.10.5" .claude-plugin/marketplace.json` shows both the version and the ref updated.

14. **5-gate final verification.** Run these checks in order and document each one as green:
    - Gate 1: `head -15 CHANGELOG.md | grep -c "\[1.10.5\]"` is 1
    - Gate 2: `grep "\"version\"" .claude-plugin/plugin.json` is 1.10.5
    - Gate 3: `grep "\"version\"" package.json | head -1` is 1.10.5
    - Gate 4: `git tag -l v1.10.5` returns v1.10.5 AND `git log origin/main..HEAD` is empty
    - Gate 5: `cd ~/mindrian-marketplace && grep -c "v1.10.5" .claude-plugin/marketplace.json` is >= 2 AND `git log origin/master..HEAD` is empty
    
    If any gate fails, STOP and do not report the release as complete. Acceptance: all five gates green.

## Tests

No new tests in this plan. Plan 82-04 delivered the test file. This plan's verification is the 5-gate check and the em-dash grep.

## Verification

Runnable commands that must all exit 0:
- `pwd` returns `/home/jsagi/MindrianOS-Plugin`
- `head -15 CHANGELOG.md | grep -q "\[1.10.5\]"`
- `grep -q "\"version\": \"1.10.5\"" .claude-plugin/plugin.json`
- `grep -q "\"version\": \"1.10.5\"" package.json`
- `git tag -l v1.10.5 | grep -q v1.10.5`
- `test -z "$(git log origin/main..HEAD)"`
- `cd ~/mindrian-marketplace && grep -q "v1.10.5" .claude-plugin/marketplace.json && test -z "$(git log origin/master..HEAD)"`
- `grep -rn "\u2014\|\u2013" CHANGELOG.md .claude-plugin/plugin.json package.json .planning/PROJECT.md .planning/TODO.md .planning/ROADMAP.md` returns zero lines
- `node scripts/generate-presentation.test.cjs` exits 0 (regression guard that plan 82-04's tests still pass on the release commit)

## Requirements Covered

- WIKI-FIX-07 (release portion): CHANGELOG [1.10.5] entry credits Lawrence Aronhime by name with the 2026-04-13 23:23 report timestamp and notes the v1.10.2 Feynman-MINTO leverage. Plus the full 5-gate release pipeline (CHANGELOG + plugin.json 1.10.5 + package.json 1.10.5 + git tag v1.10.5 + marketplace.json source.ref pinned to v1.10.5). Plus the smart-notebook milestone slot shift from v1.10.5 to v1.10.6 in PROJECT.md, TODO.md, and ROADMAP.md (housekeeping required by the slot reassignment, not a separate requirement).

## Dependencies

Depends on plan 82-04 because the test suite must exist and pass before cutting the release commit. Transitively depends on 82-01, 82-02, and 82-03. Cannot start until 82-04's tests are green on the working tree.

## Risks

- **The marketplace repo is a separate clone the Claude session may not have fresh credentials for.** Mitigation: CONTEXT states this path is pre-authorized. If `git push origin master` on the marketplace repo fails for credentials, stop and surface the error; do NOT attempt to recreate credentials. The user will resolve and re-run the final push.
- **Em-dashes sneak into the CHANGELOG from a copy-paste of the research draft.** Mitigation: task 8 grep-checks every touched file before commit. The research doc itself was authored with the no-em-dash rule in mind, so the risk is low, but the grep is mandatory.
- **`.planning/PROJECT.md` does not actually have a "Notion Template Gap Close" section with a smart-notebook line to update.** Mitigation: task 5 greps first. If the section does not exist or the smart-notebook reference is not in it, find wherever smart-notebook is mentioned and update that location. Document the actual location in the commit message if it differs from the CONTEXT-documented section name. This is the only case where a "deviation note" might appear post-commit.
- **`.planning/TODO.md` already has a different `[NEXT]` item and the wiki fix is not explicitly listed.** Mitigation: task 6 starts by greping for `wiki` and `1.10.5`. If no match, just promote smart-notebook to [NEXT] with v1.10.6 and skip the removal step. Document in a deviation note.
- **`.planning/ROADMAP.md` does not have Phase 82 yet because Phase 82 was created from the PRD express path and not a full roadmap entry.** Mitigation: if ROADMAP.md has no Phase 82 entry, ADD one marked shipped with the 2026-04-14 date and a one-line summary. Do not skip this step silently.
- **Git commit contains the version bump but plugin.json and package.json drift from git tag.** Mitigation: the 5-gate check in task 14 catches this. If gate 2, 3, or 4 fails, roll back the commit with `git reset --soft HEAD~1`, fix, and re-commit. Never push a partial release.
- **User has uncommitted work in `~/mindrian-marketplace` that is unrelated to this release.** Mitigation: task 13 runs `git status` before touching marketplace.json. If the tree is not clean, STOP and surface the conflict to the user. Do NOT stash or rebase unrelated work.
- **Tag name collision (v1.10.5 already exists from a failed prior release attempt).** Mitigation: before task 11, run `git tag -l v1.10.5`. If it exists, STOP and surface to user. Do NOT delete and recreate without explicit permission.

## Deviation Notes

- The CONTEXT and the user prompt both say the marketplace update is cross-repo and pre-authorized. This plan treats it as Claude's responsibility to execute. If the marketplace push genuinely requires interactive authentication, this becomes a handoff step and the plan reports "4 of 5 gates green, gate 5 handed off to user" instead of claiming a complete release. The verification section explicitly checks `git log origin/master..HEAD` is empty, so a successful handoff produces a verifiable green state after the user completes the push.
- The user prompt lists some CHANGELOG requirements that go beyond the research draft (credit Lawrence by name with specific timestamp, note the 8-release latency, note the Feynman-MINTO leverage, note the smart-notebook shift). All are compatible with the draft and are additive, not contradictory. The final CHANGELOG entry will include all of them.
- Tasks 5, 6, and 7 (PROJECT.md, TODO.md, ROADMAP.md updates) may need to be adapted based on the actual file contents, which are not fully visible in the CONTEXT. Task-level risks above document the fallback behavior for each case. Adaptations are allowed as long as the end state satisfies the verification checks and no em-dashes are introduced.
