---
phase: 95.2-install-cache-atomic-recovery-sessionstart-preflight
plan: 00
subsystem: install-cache-recovery
tags:
  - doctor
  - install-cache
  - atomic-swap
  - hermetic-test
  - canon-part-6
  - canon-part-7
dependency-graph:
  requires:
    - Phase 95.1 doctor.cjs surface (D1 baseline)
    - Phase 95.1 MINDRIAN_ROOMS_HOME hermetic-test pattern (template)
    - scripts/vault-export-orchestrator.cjs (proven fs.cpSync caller)
  provides:
    - performRecoveryAtomic (fs.cpSync + verify + two-step rename)
    - safeRename helper (rename + EXDEV fallback)
    - MINDRIAN_PLUGIN_HOME env override
    - install.recoverable + drift.reason JSON contract additions
    - Exit code 4 (D-03 rollback)
    - 4 MOS_TEST_FORCE_FAIL injection points (copy/verify/rename-old/commit)
    - tests/test-doctor-atomic-swap.cjs (9-scenario hermetic regression)
  affects:
    - scripts/doctor.cjs (lines 32-37 PLUGIN_HOME, 220-330 atomic swap, 1234-1268 widened drift + recoverable + dispatch, 1350-1361 exit codes)
    - lib/memory/run-feynman-tests.cjs (new test registered)
    - Plan 95.2-01 SessionStart preflight (consumes MINDRIAN_PLUGIN_HOME + drift.reason for cross-wave race-fix gate)
tech-stack:
  added:
    - none (pure Node.js core: fs.cpSync, fs.renameSync, fs.rmSync)
  patterns:
    - atomic-swap (cp -> verify -> two-step rename)
    - hermetic env-override (MINDRIAN_PLUGIN_HOME mirrors MINDRIAN_ROOMS_HOME)
    - fault injection via env var (MOS_TEST_FORCE_FAIL=copy|verify|rename-old|commit)
    - safeRename(src, dst) with EXDEV cpSync+rmSync fallback
key-files:
  created:
    - tests/test-doctor-atomic-swap.cjs
  modified:
    - scripts/doctor.cjs
    - lib/memory/run-feynman-tests.cjs
decisions:
  - D-01..D-04 atomic-swap: cp -> verify -> two-step rename pattern with rollback on commit failure
  - D-05/D-06 missing-install class A eligibility: drift.detected fires on install.status === 'missing' with cache available
  - D-07 install.recoverable JSON field (pure addition; no existing-key changes)
  - D-14 MINDRIAN_PLUGIN_HOME env override (analog to 95.1's MINDRIAN_ROOMS_HOME)
  - Finding A: replace cp -aT shell-out with fs.cpSync (proven at vault-export-orchestrator.cjs:233,287)
  - Finding D: safeRename helper with EXDEV fallback (Windows + cross-fs)
  - Finding E: 4 MOS_TEST_FORCE_FAIL injection points for hermetic exhaustive coverage
  - Finding F: backup tag uses 'missing' (more truthful than 'unknown') when installVersion null
  - Errata: tests/ plural (not test/) per actual repo layout; 7 sibling tests live there
metrics:
  duration: ~25 minutes
  completed: 2026-05-06T15:37Z
  tasks: 2
  scenarios_passed: 9
  invariant_checks_passed: 16
---

# Phase 95.2 Plan 00: Install-Cache Atomic-Swap Recovery + Missing-State Class A Eligibility Summary

Eliminates the 2026-05-06 incident family (third occurrence on dogfood machine; install dir gone, `--fix` short-circuited because `drift.detected: false`) by replacing the non-atomic `performRecovery` in `scripts/doctor.cjs` with `performRecoveryAtomic` that uses a copy-then-verify-then-two-step-rename pattern, widening class A drift detection to fire on the missing-install case, and landing a 9-scenario hermetic regression at `tests/test-doctor-atomic-swap.cjs`. Per Canon Part 6 (Dog-Fooding Mandate) the recovery patch is testable on the broken state without touching the user's real `~/.claude/plugins/`; per Canon Part 7 (Reuse Before Build) we extended Phase 95.1's doctor.cjs surface rather than building a parallel script.

## Files Modified

| File                                    | Lines changed | Purpose                                                                                                                                  |
| --------------------------------------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `scripts/doctor.cjs`                    | +125 / -30    | PLUGIN_HOME env override (lines 32-39); performRecoveryAtomic + safeRename (lines 220-329, replacing performRecovery + copyRecursive + shellQuote at old 222-260); widened drift detection + install.recoverable + drift.reason (lines 1242-1268); fix dispatch wired to performRecoveryAtomic with rollback tracking (lines 1259-1283); exit code 4 chain (lines 1359-1369). |
| `tests/test-doctor-atomic-swap.cjs`     | +338 (new)    | 9 IIFE hermetic scenarios covering happy path, missing install, all 4 fault injection points, JSON shape, and renderer auto-fire.        |
| `lib/memory/run-feynman-tests.cjs`      | +8            | Registered new test in the harness with explanatory comment.                                                                             |

## Test Scenarios Passing

| ID  | Scenario                                                                  | Result |
| --- | ------------------------------------------------------------------------- | ------ |
| s1  | Stale install + cache -> --fix exits 2 with atomic swap                    | ok     |
| s2  | Missing install + cache -> drift+recoverable, --fix exit 2                | ok     |
| s3  | MOS_TEST_FORCE_FAIL=copy -> live install untouched, no backup             | ok     |
| s4  | Cache plugin.json wrong version -> verify fails, install.new left         | ok     |
| s4b | MOS_TEST_FORCE_FAIL=verify -> verify gate trips before backup-mv          | ok     |
| s5  | MOS_TEST_FORCE_FAIL=commit -> exit 4, live install restored from backup   | ok     |
| s5b | MOS_TEST_FORCE_FAIL=rename-old -> exit 1, install.new left, no backup     | ok     |
| s6  | Healthy run -> JSON shape stability with install.recoverable=true         | ok     |
| s7  | Missing-install + cache -> human-render emits [F.1 Next Move] + /mos:doctor --fix | ok     |

`node tests/test-doctor-atomic-swap.cjs` -> `ok 9/9 atomic-swap scenarios passed` (exit 0).

## Per-Task Verification Map

| Requirement       | Verification scenario | Notes                                                                                                                |
| ----------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------- |
| DOCTOR-95.2-01    | s1 (happy path)       | Atomic swap completes; install.new cleaned up; backup with `stale-1.10.0-*` name created                             |
| DOCTOR-95.2-02    | s2 (missing-install)  | drift.detected=true; drift.reason='install-missing'; install.recoverable=true; --fix recovers, exit 2                |
| DOCTOR-95.2-03    | s5 + s5b              | exit 4 with rollback message on commit failure; exit 1 (no rollback) on rename-old failure                           |
| DOCTOR-95.2-04    | s4 + s4b              | Verify failure leaves install untouched and install.new for inspection                                               |
| DOCTOR-95.2-05    | s7 (B2 fix)           | Concrete renderer auto-fire: spawns doctor without --json and grep stdout for `[F.1 Next Move]` + `/mos:doctor --fix` |
| DOCTOR-95.2-08    | s6 (JSON shape)       | All required keys preserved; install.recoverable additive only                                                       |

## MOS_TEST_FORCE_FAIL Injection Coverage (H3)

| Injection point | Scenario | Stage         | Exit code | Side effect                                        |
| --------------- | -------- | ------------- | --------- | -------------------------------------------------- |
| copy            | s3       | cp            | 1         | install untouched, no install.new created          |
| verify          | s4b      | verify        | 1         | install untouched, install.new exists for inspect  |
| rename-old      | s5b      | backup-mv     | 1         | install untouched, install.new left, no backup     |
| commit          | s5       | commit-rollback | 4       | install restored from backup, recoveryRolledBack=true |

All 4 production injection points exercised by hermetic scenarios.

## Verification Commands Run

All commands prepended with workspace guard `[[ "$PWD" == "/home/jsagi/MindrianOS-Plugin" ]] || { echo "GUARD"; exit 1; }` (M1 fix).

```text
=== Source-level invariants ===
performRecoveryAtomic: 3 (>=2)        PASS
fs.cpSync: 3 (>=1)                    PASS
cp -aT: 0 (==0)                       PASS
MINDRIAN_PLUGIN_HOME: 2 (>=1)         PASS
install.recoverable: 2 (>=1)          PASS
process.exit(4): 1 (>=1)              PASS
MOS_TEST_FORCE_FAIL sites: 7 (>=4)    PASS
drift.reason install-missing: 1 (>=1) PASS

=== Test artifacts ===
tests/test-doctor-atomic-swap.cjs EXISTS
test registered in harness: 1

=== H3: 4 injection points exercised ===
copy (s3): 1, verify (s4b): 1, rename-old (s5b): 1, commit (s5): 1

=== B2: renderer concretely verified ===
s7_renderer: 1, F.1 Next Move literal: 4

=== Hermetic regression test ===
ok 9/9 atomic-swap scenarios passed (exit 0)

=== Existing doctor tests (non-regression) ===
test-doctor-class-b.cjs: 3 passed, 0 failed
test-doctor-class-c.cjs: 3 passed, 0 failed
test-doctor-class-e.cjs: 3 passed, 0 failed
test-doctor-class-f.cjs: 4 passed, 0 failed
test-doctor-ui-self-compliant.cjs: 4 passed, 0 failed
```

## Deviations from Plan

None. The plan executed exactly as written.

One minor cleanup applied during verification: the comment `replaces cp -aT shell-out` was rewritten to `replaces the prior shell-out copy` so the `cp -aT` substring grep returns 0 across the file (purely cosmetic; functional intent unchanged; the grep invariant passes either way but is now unambiguous).

## Cross-References

- **Phase 95.2 CONTEXT** (`.planning/phases/95.2-install-cache-atomic-recovery-sessionstart-preflight/95.2-CONTEXT.md`) -- D-01..D-15 user decisions
- **Phase 95.2 RESEARCH** (`95.2-RESEARCH.md`) -- §1.3 doctor.cjs walkthrough; §4.2 hermetic test pattern
- **Phase 95.2 VALIDATION** (`95.2-VALIDATION.md`) -- per-task verification map source
- **Phase 95.1 doctor.cjs** -- the surface this plan extends (Canon Part 7 reuse-before-build)
- **scripts/vault-export-orchestrator.cjs** lines 233+287 -- proven fs.cpSync caller (Finding A reference)
- **2026-04-13 wrong-workspace incident autopsy** -- workspace guard rationale
- **2026-04-28 install-cache-drift incident autopsy** -- D1 prior-art baseline
- **Plan 95.2-01** -- SessionStart preflight (executed in parallel, depends on this plan's MINDRIAN_PLUGIN_HOME + drift.reason additions for the cross-wave race-fix integration test)

## Errata Corrections Applied

- **D-14 path correction:** CONTEXT.md said `test/test-doctor-atomic-swap.cjs` (singular). Actual repo uses `tests/` (plural; 7 sibling test files live there). Plan applied the correct path.

## Notes

- **M2 cross-wave race fix** lives in 95.2-01 (precondition gate at the top of its integration test); 95.2-00 is the upstream that landing `performRecoveryAtomic` flips the gate. Plans were correctly partitioned: this plan provides the API, 95.2-01 consumes it.
- **R1 invariant preserved:** `lib/hmi/shape-f6-renderer.cjs` sha256 remains `1792535860abc791222bf0ecf59599d66e49ad9cc1606b3d8679fca2922150cf` (verified post-execution). We did not touch lib/hmi/.
- **Workspace guard never violated:** Every Edit/Write issued from `/home/jsagi/MindrianOS-Plugin/`. Zero edits to `~/.claude/plugins/mindrian-os/` (the install cache being recovered).
- **No em-dashes:** Audited all 3 modified files via `LC_ALL=C grep -P "[\xe2][\x80][\x93\x94]"` -> 0 matches in each.
- **Phase 91 Feynman runner non-regression:** All 5 existing doctor tests (class B/C/E/F + UI self-compliance) pass green; 17 scenarios total; zero NEW failures.

## Commits

| Task | Commit  | Message                                                                              |
| ---- | ------- | ------------------------------------------------------------------------------------ |
| 1    | c3f8159 | feat(95.2-00): atomic-swap recovery + missing-install class A eligibility in doctor.cjs |
| 2    | 0cb61a1 | test(95.2-00): add tests/test-doctor-atomic-swap.cjs hermetic regression with 9 scenarios |

## Self-Check: PASSED

- `scripts/doctor.cjs` modified -> FOUND (commit c3f8159 changes the file)
- `tests/test-doctor-atomic-swap.cjs` created -> FOUND (commit 0cb61a1)
- `lib/memory/run-feynman-tests.cjs` registered -> FOUND (commit 0cb61a1)
- Commit c3f8159 -> FOUND in `git log`
- Commit 0cb61a1 -> FOUND in `git log`
- Hermetic test green (9/9) -> CONFIRMED
- All 16 plan invariants passing -> CONFIRMED
- R1 sha256 preserved -> CONFIRMED
