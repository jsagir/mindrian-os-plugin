---
phase: 95.2
plan: 02
verified_on: 2026-05-06
machine: jsagir-dogfood
executor: claude-code-execute-plan
synthesized: true
pre_state: ok-then-synthesized-missing
post_state: healthy
recovered_version: 1.12.5
exit_code_drift: 1
exit_code_fix: 2
exit_code_post: 0
related_artifacts:
  - docs/autopsies/2026-05-06-install-dir-missing-incident.md
  - .planning/phases/95.2-install-cache-atomic-recovery-sessionstart-preflight/95.2-02-PLAN.md
  - scripts/doctor.cjs
canon_part: 6
note: |
  This dogfood verification is an empirical complement to the 9-scenario hermetic
  test in 95.2-00 (which proves structural correctness on scratch fixtures); the
  dogfood proves the patched recovery works on the live install at this moment.
  The user explicitly approved (Option 1) re-creating the broken state by renaming
  the live install dir, then exercising the patched recovery against it. Pre-state
  was healthy (1.12.5 on disk); we synthesized the missing-install state, ran
  drift detection, ran --fix, and verified post-state restoration plus stale
  backup retention.
---

# Phase 95.2 DOGFOOD VERIFICATION

Canon Part 6 (Dog-Fooding Mandate) self-test artifact. Patched `scripts/doctor.cjs` exercised against a synthesized but real missing-install state on the dogfood machine.

## Approach

User chose Option 1 (Synthesize + recover). Rationale: the original missing-install incident from 2026-05-06 was already partially resolved (live install dir at v1.12.5 had been restored), so to capture an empirical dogfood signal we deliberately re-created the broken state by renaming the live install dir to a synthesized `.stale-1.12.5-<timestamp>` backup, then exercised the patched recovery against the actual filesystem layout (not a scratch fixture).

This complements (does not replace) the 9-scenario hermetic test in `tests/test-doctor-atomic-swap.cjs` which exercises the algorithm on isolated scratch fixtures via `MINDRIAN_PLUGIN_HOME`. Hermetic = structural correctness. Dogfood = the live install at this moment.

## Step 1 + 2: Pre-state capture (system was HEALTHY before we mutated it)

Captured at `/tmp/95.2-dogfood-pre.txt`:

```
total 684
drwxr-xr-x  25 jsagi jsagi   4096 May  6 18:37 .
drwxr-xr-x   8 jsagi jsagi   4096 May  6 18:37 ..
---
{
  "install": { "status": "ok", "version": "1.12.5", "recoverable": true },
  "cache":   { "status": "ok", "versions": ["1.11.2","1.12.0","1.12.5"], "latest": "1.12.5" },
  "drift":   { "detected": false, "compare": 0 }
}
```

Pre-mutation: install at 1.12.5, doctor exit 0, no drift. The patched code is sane on healthy state (no false-positive drift signal).

Cache contents (parsable semver versions; the doctor's version sniffer rejects four-part versions like `1.12.5.1` so it surfaces 1.12.5 as latest):

```
1.11.2  1.12.0  1.12.5  1.12.5.1
```

## Mutation: Synthesize broken state

```
TS=20260506-163240
mv ~/.claude/plugins/mindrian-os ~/.claude/plugins/mindrian-os.stale-1.12.5-${TS}
```

Post-mutation listing of `~/.claude/plugins/`:

```
mindrian-os.stale-1.10.10-20260428-095548   (original 2026-04-28 backup)
mindrian-os.stale-1.11.0-20260430-083458    (original 2026-04-30 backup)
mindrian-os.stale-1.12.5-20260506-163240    (synthesized 2026-05-06 backup; created by this dogfood test)
(no bare mindrian-os/)
```

This is the exact filesystem layout that broke the original 2026-05-06 incident: live install dir absent, multiple stale backups present, cache intact.

## Step 3: Drift detection signal (read-only doctor)

```
node scripts/doctor.cjs --json
Exit: 1
```

Output:

```json
{
  "install": {
    "status": "missing",
    "detail": "install dir does not exist: /home/jsagi/.claude/plugins/mindrian-os",
    "recoverable": true
  },
  "cache": {
    "status": "ok",
    "versions": ["1.11.2","1.12.0","1.12.5"],
    "latest": "1.12.5"
  },
  "drift": { "detected": true, "reason": "install-missing" },
  "fixRequested": false,
  "classARecovered": null
}
```

This is exactly the JSON shape the patched doctor must surface (Plan 95.2-00 D-05 + D-06 + D-07): `install.status === "missing"`, `install.recoverable === true`, `drift.detected === true`, `drift.reason === "install-missing"`. The pre-patch doctor would have surfaced `drift.detected: false` here (the bug). This output proves the missing-state widening is live in the patched code.

## Step 4: Recovery execution (--fix)

```
node scripts/doctor.cjs --fix --json
Exit: 2
```

Output:

```json
{
  "install":  { "status": "missing", "recoverable": true },
  "cache":    { "status": "ok", "latest": "1.12.5" },
  "drift":    { "detected": true, "reason": "install-missing" },
  "fixRequested": true,
  "classARecovered": {
    "status": "ok",
    "backup": null,
    "recoveredVersion": "1.12.5"
  },
  "recoveryError": null,
  "recovered": [
    { "tool": "install-cache", "status": "ok", "version": "1.12.5", "backup": null }
  ]
}
```

Exit code 2 (recovered) per the existing doctor exit-code scheme. `classARecovered.backup === null` is correct: pre-recovery there was no live install dir to back up, so the atomic-swap had nothing to displace; the recovery is a fresh `cp` from cache to `mindrian-os.new` then a one-step rename to `mindrian-os/` (the two-step backup-then-rename only fires when a live install exists pre-recovery).

## Step 5: Post-state verification

```
ls -la ~/.claude/plugins/mindrian-os
total 684
drwxr-xr-x  25 jsagi jsagi 4096 May  6 19:45 .
drwxr-xr-x   9 jsagi jsagi 4096 May  6 19:45 ..

ls ~/.claude/plugins/ | grep ^mindrian-os
mindrian-os
mindrian-os.stale-1.10.10-20260428-095548
mindrian-os.stale-1.11.0-20260430-083458
mindrian-os.stale-1.12.5-20260506-163240

cat ~/.claude/plugins/mindrian-os/.claude-plugin/plugin.json | jq -r .version
1.12.5

node scripts/doctor.cjs --json
Exit: 0
install.status: ok
install.version: 1.12.5
drift.detected: false
```

Post-recovery state:
- Live `mindrian-os/` restored at version 1.12.5 (matches cache.latest).
- All three `.stale-*` backups preserved (D-04 retention rule honored): two original 2026-04 backups plus the synthesized 2026-05-06 backup.
- No orphan `mindrian-os.new` directory (cleanup via two-step rename worked).
- Re-run doctor reports `install.status === "ok"`, `drift.detected === false`, exit 0 (system healthy).

## Step 6: Recovery shape

```json
{
  "classARecovered": {
    "status": "ok",
    "backup": null,
    "recoveredVersion": "1.12.5"
  },
  "recovered": [
    {
      "tool": "install-cache",
      "status": "ok",
      "version": "1.12.5",
      "backup": null
    }
  ],
  "recoveryError": null
}
```

JSON contract additions confirmed live: `classARecovered` populated, `recoveryError` null, `recovered` array present with the install-cache tool entry. Existing field semantics preserved.

## Conclusion

Canon Part 6 D-13 dogfood signal CAPTURED:

- Real machine (jsagir at /home/jsagi/MindrianOS-Plugin/).
- Real recovery path (`scripts/doctor.cjs --fix --json` from the live workspace; subject `~/.claude/plugins/mindrian-os/`).
- Real backup naming convention preserved (`mindrian-os.stale-<version>-<UTCTS>`).
- Real exit codes through the full sequence: 0 (pre-state healthy) -> 1 (drift detected after mutation) -> 2 (recovered with --fix) -> 0 (post-state healthy on re-run).
- Real D-04 retention: pre-existing 2026-04-28 and 2026-04-30 backups preserved, plus the synthesized 2026-05-06 backup left in place for forensic continuity.

The patched recovery is shippable per the dog-fooding mandate. The autopsy filed in Task 1 of this plan promised a recovery patch tested against the broken state; this verification artifact closes that promise.

Note on "recoveredVersion": 1.12.5 is what cache.latest computes today (the parser excludes `1.12.5.1` because it has four numeric segments). This is a separate issue tracked in the install-cache cluster, not a bug in 95.2's atomic-swap recovery; the recovery correctly chose what cache.latest reported.

Note on hermetic complement: the structural test suite in `tests/test-doctor-atomic-swap.cjs` runs 9 scenarios against scratch fixtures via `MINDRIAN_PLUGIN_HOME` (success / cp-failure / verify-failure / rollback / etc). That suite proves the algorithm. This dogfood verification proves the algorithm runs against the actual `~/.claude/plugins/` layout on the developer machine. Both signals are required for a credible recovery patch.
