---
phase: 261-enrichment-ceremony-single-admin-window
plan: 01
type: execute
wave: 1
depends_on: []
target_repo: ProblemsWorthSolving-Brain
files_modified:
  - ProblemsWorthSolving-Brain/scripts/probe-ceremony-worklist.mjs
  - ProblemsWorthSolving-Brain/docs/2026-08-21-WORKLIST-261-ceremony.md
autonomous: true
requirements: [CER-01, CER-02, CER-03, CER-04, CER-05, CER-06]

must_haves:
  truths:
    - "Every worklist this phase authors against is MEASURED live at planning-execution time, never carried from the 2026-08-13 kickoff numbers that three separate write waves have since invalidated"
    - "The Tier A set, the Cohort 1 set, the Cohort 2 set, the CER-04 set and the CER-06 set are each an explicit, named list of frameworks with a live readiness vector per row"
    - "Every already-authored, never-executed payload in the Brain repo is diffed against the live worklist BEFORE any new payload is authored, so no framework is double-authored"
    - "The delta between the 2026-08-13 baseline and today is attributed to a named writer, or recorded as unattributed, never silently absorbed"
    - "The current admin exposure is measured by listing the live tool surface, never assumed from a prior plan's record"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-ceremony-worklist.mjs"
      provides: "Read-tier probe emitting every count this ceremony's payload set is sized against"
      min_lines: 120
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-WORKLIST-261-ceremony.md"
      provides: "The measured worklist: Tier A rows, Cohort 1/2 rows, CER-04, CER-06, hygiene counts, reuse audit, attribution ledger"
      contains: "Tier A"
  key_links:
    - from: "scripts/probe-ceremony-worklist.mjs"
      to: "data/flagship-floor-set.json in MindrianOS-Plugin"
      via: "reads the ratified 28-name denominator rather than hardcoding a name list"
      pattern: "flagship-floor-set"
    - from: "docs/2026-08-21-WORKLIST-261-ceremony.md"
      to: "payloads/*.mjs already on main and never executed"
      via: "reuse audit table, one row per existing payload with a RUN / RETIRE / RETARGET disposition"
      pattern: "reuse audit"
---

<objective>
Measure the ceremony's real worklist before anything is authored. This plan writes no payload and
opens no window. It replaces every stale number this phase would otherwise inherit.

Purpose, stated as the problem it solves. Three separate write waves have hit canon since the
2026-08-13 kickoff baseline that `.planning/research/SUMMARY.md` and `.planning/research/FEATURES.md`
are written against: the untracked 2026-08-11/12 second-machine wave (Phase 258 RECON-01), the
Phase 258 RECON-02 order-collision ceremony, and a full-graph heal executed live from the Windows
operator machine on 2026-08-20 whose record only reached `main` locally on 2026-08-21
(`docs/heal-2026-08-20/`, merge `616f793`, still unpushed under the freeze). Authoring 16 payloads
against a 2026-08-13 worklist would author several of them for frameworks that already clear the
floor, and would miss the ones the heal did not touch.

The planner already ran this probe once, read-only, on 2026-08-21, and the numbers below are
recorded as the EXPECTED shape, not as the answer. Canon moves. Task 1 re-measures and the payload
plans consume Task 2's document, not this objective.

| Measured 2026-08-21 (planner, read-tier) | Value |
|---|---|
| Ratified 28: clear readiness >= 3 | 12 (4 at 4/4, 8 at 3/4) |
| Ratified 28: miss | 16 (7 at 2/4, 9 at 0/4) |
| `PEST Analysis` name matches | 0 |
| Graph-wide `:Framework` nodes | 186 |
| Graph-wide readiness 3/4 AND `pattern_type` absent (the Tier A set) | 19 |
| `ALIAS_OF` self-loops | 165 |
| Archived relabel block (demoted / also archived / inside ids 28000-29000) | 100 / 99 / 95 |

The heal run's own README claims "all 184 frameworks to 4/4 readiness". The live distribution
measured the same week is 6 at 4/4, 28 at 3/4, 27 at 2/4, 11 at 1/4, 114 at 0/4. Those two
statements cannot both be true of `orchestration_readiness`. The reconciliation is mechanical and
Task 1 must state it rather than pick a side: the heal wrote descriptions, categories,
`ADDRESSES_PROBLEM_TYPE`, `FEEDS_INTO`, `AUTHORED` and `ALIAS_OF`, and `orchestration_readiness`
scores none of those. Its four dimensions are `pattern_type`, structure
(`HAS_PHASE|HAS_STAGE|HAS_PROCESS_STEP|HAS_STEP`), techniques (`USES_TECHNIQUE|EQUIPS_WITH`) and
flow (`LEADS_TO` off a structure node) - `src/arm1-orchestrator.mjs:276-295`. A readiness claim
measured on a different definition is exactly the false-success class the standing WATCH memory
tracks, and this phase must not inherit it silently.

Output: one probe script and one worklist document, both committed locally to
`ProblemsWorthSolving-Brain` and NOT pushed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/261-enrichment-ceremony-single-admin-window/261-RESEARCH.md
@.planning/phases/260-pipeline-fixes-brain-repo-one-pass-one-push/260-05-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<cross_repo_contract>
**Read this before running any command in this plan.**

| Concern | Repo | Absolute path |
|---|---|---|
| This PLAN.md and all GSD tracking | MindrianOS-Plugin | `/home/jsagi/dev/MindrianOS-Plugin/.planning/` (gitignored, `git add -f`) |
| Every file this plan creates | ProblemsWorthSolving-Brain | `/home/jsagi/dev/ProblemsWorthSolving-Brain/` |
| The ratified floor denominator this plan READS (never edits) | MindrianOS-Plugin | `/home/jsagi/dev/MindrianOS-Plugin/data/flagship-floor-set.json` |

**PUSH FREEZE, NON-NEGOTIABLE.** `ProblemsWorthSolving-Brain` is under the freeze declared in
`docs/2026-08-20-FREEZE-push-freeze-before-261.md`. It lifts ONLY when THIS phase's admin window
closes and its `GRAPH-WRITE-LOG.md` row is appended. This plan `git commit`s locally and NEVER runs
`git push`. Plan 261-13 is the only plan in this phase permitted to push. The repo's general
"commit straight to main, always" convention is OVERRIDDEN for this phase; do not follow it here.
Local `main` is already 5 commits ahead of `origin/main` under this freeze - that is correct and
expected, not a problem to fix.

**No admin window is opened by this plan and none is required.** Every call is read-tier
`brain_query` over HTTPS with the read key from `MINDRIAN_BRAIN_KEY` or `~/.mindrian.env`.

**Subagent dispatch: SAFE.** This plan uses `node scripts/*.mjs` over HTTPS and Bash only. It
requires ZERO MCP tools. It can be dispatched to a gsd-executor subagent.

Both repos forbid em-dashes; hyphens only.
</cross_repo_contract>

<tasks>

<task type="auto">
  <name>Task 1: Build and run the ceremony worklist probe</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-ceremony-worklist.mjs (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-wave-attribution.mjs` lines 36-101: `resolveReadKey`, `call`, `q`, `initHandshake`. Import these; do NOT mint a second HTTP client (Canon Part 7, and this repo's own "no second pipeline" fence).
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/arm1-orchestrator.mjs` lines 273-299 (`orchestrationReadiness`): the EXACT four-dimension Cypher this probe must mirror. Mirror it; do not invent a readiness definition.
    - `/home/jsagi/dev/MindrianOS-Plugin/data/flagship-floor-set.json`: the ratified 28-name denominator and the strict case-sensitive `overrideNames.has(fw.name)` note in its `_comment`.
    - `/home/jsagi/dev/MindrianOS-Plugin/scripts/check-flagship-floor.cjs` header, lines 1-50: the exit-code contract (0 pass, 1 miss, 2 malformed override, 3 VOID) and the two-part floor rule (exactly-1 `normalize_framework_name` match AND readiness >= 3).
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-RCA-alias-self-loop-minting.md` sections 1 and 6: the 165 self-loop population and its label census.
  </read_first>

  <action>
Create `scripts/probe-ceremony-worklist.mjs`. It imports `resolveReadKey`, `call`, `q` and
`initHandshake` from `scripts/probe-wave-attribution.mjs` and mints no second transport. It reads
the 28 ratified names from `/home/jsagi/dev/MindrianOS-Plugin/data/flagship-floor-set.json` via an
absolute path with a `--floor-set <path>` override flag, and prints a markdown-ready table per
probe. Exit 0 on a clean run, 3 if any probe returns a transport error (a run with a failed probe
is not a measurement, per TRUST-02's VOID doctrine).

Probes, each labelled `[W-n]` in the output so the worklist document can cite them:

`[W-1]` Ratified-28 readiness vector. For each of the 28 names, case-insensitively resolve
`:Framework` nodes and compute the four dimensions with the SAME CASE expressions as
`orchestrationReadiness`. Emit `name | matches | best_score | pt | st | te | fl`. Order by
`best_score` ascending. A row with `matches = 0` is a distinct state from `best_score = 0` and both
must be visible.

`[W-2]` `normalize_framework_name` match count per ratified name. This is a SEPARATE probe from
`[W-1]` and it is not optional. `[W-1]` counts raw case-insensitive name hits; the floor gate
counts what the resolver returns, and Phase 260 proved those disagree (`Scenario Planning`
measured 2 through the resolver, see FLOOR-03's note in `.planning/REQUIREMENTS.md`). Call the
public `normalize_framework_name` tool through `call(key, 'tools/call', ...)` once per name and
record the returned canonical-match count. Any name whose `[W-1]` and `[W-2]` counts disagree is
flagged in the output, because that disagreement is the FLOOR-03 class.

`[W-3]` Graph-wide `:Framework` readiness distribution: `score | count | count missing pattern_type`.

`[W-4]` The Tier A candidate set, named not counted: every `:Framework` at readiness exactly 3 whose
`pattern_type` is null or `'unknown'`. Emit `id(f) | name | st | te | fl | category | description`
(truncate description to 160 chars). This list IS CER-01's worklist.

`[W-5]` `ALIAS_OF` self-loop population and label census: `MATCH (n)-[r:ALIAS_OF]->(n)` count, plus
the same `labels | count` census the RCA's `[S-2]` produced, so CER-05's DELETE scope is re-measured
rather than carried from 2026-08-20.

`[W-6]` The archived relabel block: `demoted`, `also_archived`, `min_id`, `max_id`, `in_28k_block`
using the exact Cypher in `261-RESEARCH.md`'s Reproduce section, plus `id | name | labels` for every
node in the block so plan 261-09 has a one-page review list rather than a count.

`[W-7]` `USES_FRAMEWORK` edge scarcity: total edges, target-is-Framework, target-not-labelled,
target-is-archived, and the count of `:MindrianCommand` nodes reaching zero frameworks.

`[W-8]` Admin exposure, MEASURED. Call `tools/list` with the read key and print whether
`brain_write` and `ingest_framework` appear. The expected answer is that both are ABSENT. Print the
result either way and never assert closure without this line. Three separate write sessions have
touched `BRAIN_HTTP_ADMIN` in the last ten days and the heal run's own README
(`docs/heal-2026-08-20/README.md`, final bullet) records an operator checklist item to return it to
deny that has no confirmation anywhere. If `brain_write` IS present, that is a live finding: report
it loudly at the top of the output and do not proceed to Task 2 without surfacing it.

`[W-9]` Node and relationship totals, plus `:Framework` count, so the worklist carries a graph
signature comparable against `docs/census-2026-08-20.md`.

Run the probe and capture its full output to the scratch directory for Task 2 to transcribe.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node scripts/probe-ceremony-worklist.mjs > /tmp/w261.txt 2>&1; echo "exit=$?"; grep -q '\[W-8\]' /tmp/w261.txt && grep -q '\[W-4\]' /tmp/w261.txt && grep -q '\[W-6\]' /tmp/w261.txt</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/probe-ceremony-worklist.mjs` exits 0 and prints all nine `[W-n]` sections.
    - `[W-1]` prints exactly 28 rows, one per ratified name, and `matches = 0` is rendered distinctly from `best_score = 0`.
    - `[W-2]` prints a resolver match count for all 28 names and explicitly flags every name where `[W-1]` and `[W-2]` disagree. If zero disagreements are found, the output says so in words rather than printing nothing.
    - `[W-4]` prints one row per Tier A candidate with an internal id, not just a total.
    - `[W-6]` prints one row per node in the archived block, not just the five aggregate counts.
    - `[W-8]` prints an explicit present/absent verdict for both `brain_write` and `ingest_framework`.
    - The script imports its transport from `scripts/probe-wave-attribution.mjs`: `grep -v '^//' scripts/probe-ceremony-worklist.mjs | grep -c "probe-wave-attribution"` returns at least 1, and `grep -v '^//' scripts/probe-ceremony-worklist.mjs | grep -c "new Headers\|await fetch("` returns 0.
    - The four readiness CASE expressions are byte-comparable to `src/arm1-orchestrator.mjs`: `grep -c "pattern_type IS NOT NULL AND f.pattern_type <> 'unknown'" scripts/probe-ceremony-worklist.mjs` returns at least 1.
    - `grep -Pn '\x{2014}' scripts/probe-ceremony-worklist.mjs` exits 1 with no output.
  </acceptance_criteria>

  <done>
    A committed, re-runnable read-tier probe emits every count and every NAMED LIST this ceremony's
    payload set will be sized against, including a measured verdict on whether the admin surface is
    currently open.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the worklist, the reuse audit, and the attribution ledger</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-WORKLIST-261-ceremony.md (new)
  </files>

  <read_first>
    - Task 1's captured probe output. Transcribe from it; do not retype from memory. The 260-05 summary records a self-caught stale-memory mistake of exactly this shape.
    - `/home/jsagi/dev/MindrianOS-Plugin/.planning/research/FEATURES.md` lines 21-70: the Cohort 1 / Cohort 2 source map, one row per framework with its source doc path and char count. This is the authoring input the payload plans consume; it is 2026-08-13 vintage and its SCORES are stale, but its SOURCE PATHS are not.
    - Every already-authored payload at the repo root: `payloads/jobs-to-be-done.mjs`, `payloads/lean-canvas.mjs`, `payloads/minto-pyramid.mjs`, `payloads/mullins-seven-domains.mjs`, `payloads/six-thinking-hats.mjs`, `payloads/reverse-salient-analysis.mjs`, `payloads/invention-disclosure.mjs`, `payloads/project-list-structure.mjs`. Read each one's header comment only (the first ~50 lines) to learn its target framework name and its declared source. All five of the first group landed in one commit, `aa15966` (2026-08-20), and none carries an execution marker.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/heal-2026-08-20/README.md` in full, and `docs/2026-08-21-DECISION-repo-hygiene-close-out.md`: the third write wave, its claimed final state, and the still-unadjudicated `alias-review.md` conflict.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/GRAPH-WRITE-LOG.md` sections 1 and 7: the ledger rule and the single existing row.
  </read_first>

  <action>
Write `docs/2026-08-21-WORKLIST-261-ceremony.md`. This document is the single input every other
plan in Phase 261 reads. Sections, in this order:

**Section 1, Instrument and honesty note.** State that every number came from
`scripts/probe-ceremony-worklist.mjs` run at a stated UTC timestamp, and that the two match-count
probes (`[W-1]` raw, `[W-2]` resolver) are reported separately because they are known to disagree.
Name the disagreement class as FLOOR-03's and record which names showed it.

**Section 2, the ratified 28.** One table, one row per name: `name | resolver matches | readiness |
pt st te fl | verdict`. Verdict is `PASS` (resolver matches exactly 1 AND readiness >= 3) or `MISS`
with the reason spelled out (`readiness 2`, `no node`, `2 matches`). Print the PASS/MISS totals.
Then state, in one sentence, how these numbers differ from the 8/28 kickoff figure and why the
kickoff figure is superseded rather than wrong.

**Section 3, the CER worklists.** Five explicitly named lists, each a table with the framework name,
its live readiness vector, and its source doc path taken from FEATURES.md's source map:
- CER-01 Tier A: the `[W-4]` set.
- CER-02 Cohort 1: the MISS rows whose FEATURES.md row is 1 through 10.
- CER-03 Cohort 2: the MISS rows whose FEATURES.md row is 11 through 17.
- CER-04: `PEST Analysis`, with its live match count.
- CER-06: `Four Lenses of Innovation`, with its live readiness and a pointer to
  `docs/2026-08-21-SOURCE-four-lenses-of-innovation.md` as its now-existing named source.
Any FEATURES.md Cohort row whose framework now measures PASS moves to a fourth list titled
`Dropped from scope by live measurement` with its old and new score side by side. Do not silently
omit it; a reader comparing this document against FEATURES.md must see why a row vanished.

**Section 4, the reuse audit.** One row per already-authored payload file: `payload file | target
framework | live readiness of that target | disposition`. Disposition is exactly one of `RUN`
(target still MISSes and this payload addresses it), `RETIRE` (target already PASSes; the payload
is redundant and must not execute), or `RETARGET` (the payload names a node that is not the node
the ratified floor name resolves to, so running it would enrich the wrong node). `RETARGET` is the
expected disposition for `minto-pyramid.mjs` if `[W-1]`'s resolution of `The Pyramid Principle`
lands on a different id than the payload's own target, which is the same node-identity dispute
`payloads/relabel-fix-260820/manifest.json` records under `contested_survivor_picks`. State the
disposition with its evidence; do not guess. This section is Canon Part 7 (reuse before build)
discharged with measurements instead of an assertion.

**Section 5, the attribution ledger.** A table of every write wave that touched canon since the
2026-08-13 baseline: `date | writer | tracked where | in GRAPH-WRITE-LOG?`. At minimum: the
2026-08-11/12 second-machine wave, the 258 RECON-02 ceremony, and the 2026-08-20 heal run. The heal
run is the one to be careful about: it IS now tracked (`docs/heal-2026-08-20/`) but it is NOT in
`GRAPH-WRITE-LOG.md`, which the ledger's own section 1 rule forbids. Record that as an open item
for plan 261-13 to close with a retrospective row, and say plainly that a retro row is a
reconciliation, not a claim the write was pre-logged.

**Section 6, the heal readiness contradiction, resolved.** State both claims (the heal README's
"all 184 frameworks to 4/4 readiness" and `[W-3]`'s live distribution), then the reconciliation:
the heal wrote description, category, `ADDRESSES_PROBLEM_TYPE`, `FEEDS_INTO`, `AUTHORED` and
`ALIAS_OF`, and `orchestration_readiness` scores none of those four things. Cite
`src/arm1-orchestrator.mjs:276-295` for the dimension definition. Conclude explicitly: the heal did
not move the flagship floor and this phase's payload worklist is not reduced by it. Do NOT edit the
heal's own README; it is another session's record and the correction belongs here.

**Section 7, hygiene counts.** The `[W-5]` self-loop population and label census (CER-05's DELETE
scope), the `[W-6]` archived block list, and the `[W-7]` `USES_FRAMEWORK` scarcity numbers.

**Section 8, admin exposure at worklist time.** Transcribe `[W-8]` verbatim.

**Section 9, the expectation reset carried to 262.** Reproduce `261-RESEARCH.md`'s single most
important line in this document's own words: relabelling the archived block will recover at most
11 edges and rescues ZERO of the zero-framework commands, so a flat command-coverage number after
the relabel is the PREDICTED outcome, not the remediation failing. State that the relabel and the
edge authoring are logged as two separate operations with a read-tier probe between them
(planner ruling on open question 1, answered YES), so that prediction stays checkable afterwards.

Commit locally. Do not push.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && D=docs/2026-08-21-WORKLIST-261-ceremony.md && for s in "Tier A" "reuse audit" "attribution ledger" "RETIRE" "Dropped from scope by live measurement" "orchestration_readiness"; do grep -qi "$s" "$D" || { echo "MISSING: $s"; exit 1; }; done && grep -Pn '\x{2014}' "$D" && exit 1 || exit 0</automated>
  </verify>

  <acceptance_criteria>
    - Section 2's table has exactly 28 rows and prints explicit PASS/MISS totals that sum to 28.
    - Every framework named in a CER worklist in section 3 carries BOTH a live readiness vector AND a source doc path. A row with a readiness but no source path is incomplete and fails this criterion, because "NO named read source = NO payload" is the template's own rule.
    - Section 3 contains a `Dropped from scope by live measurement` list. If it is empty, it says "none" in words rather than being absent.
    - Section 4 has one row per payload file found at `payloads/*.mjs` (excluding the runners `run-ingest.mjs`, `run-projection.mjs` and `verdict.mjs`), and every row carries exactly one of `RUN`, `RETIRE`, `RETARGET` with a cited reason.
    - Section 5 names at least three write waves and marks, per wave, whether a `GRAPH-WRITE-LOG.md` row exists.
    - Section 6 states both readiness claims and cites `src/arm1-orchestrator.mjs` for the dimension definition. It must NOT resolve the contradiction by adjusting either number.
    - Section 8 is a transcription of `[W-8]`, not a restatement of what a prior plan believed.
    - `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain log origin/main..HEAD --oneline` is NON-empty and `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain log HEAD..origin/main --oneline` is empty: this plan committed and did not push.
    - `grep -Pn '\x{2014}' docs/2026-08-21-WORKLIST-261-ceremony.md` exits 1 with no output.
  </acceptance_criteria>

  <done>
    A single tracked document carries the measured worklist for all six CER requirements, a
    reuse disposition for every existing payload, an attribution row for every recent write wave,
    and the resolved heal-readiness contradiction. Every downstream plan in this phase reads it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale document to live graph | every number this phase would otherwise inherit predates three write waves |
| a payload's declared target to the node a ratified name actually resolves to | a payload can enrich a real node and still miss the floor entirely |
| a prior plan's record of admin state to the current tool surface | admin exposure is a live property, not a remembered one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-261-01 | Repudiation | a worklist row carried from the 2026-08-13 kickoff and presented as current | mitigate | every row is transcribed from a timestamped probe run; the acceptance criteria require 28 rows from `[W-1]`/`[W-2]`, not a narrative |
| T-261-02 | Spoofing | `orchestration_readiness` reimplemented approximately, so the probe measures a different metric than the gate | mitigate | the probe mirrors `src/arm1-orchestrator.mjs:276-295` and an acceptance criterion greps for the exact `pattern_type` CASE expression |
| T-261-03 | Information Disclosure | the read-tier key printed into a transcript or committed | mitigate | the key is resolved by the imported `resolveReadKey`, never echoed; no probe prints its own headers |
| T-261-04 | Elevation of Privilege | proceeding on the assumption that the admin surface is closed when a third writer may have left it open | mitigate | `[W-8]` measures `tools/list` and the action halts and surfaces if `brain_write` is present |
| T-261-05 | Tampering | a redundant payload executed later against a framework that already passes, silently overwriting heal-written props | mitigate | section 4's `RETIRE` disposition is produced here, before any window, and plan 261-11's batch integrity check re-reads it |
| T-261-SC | Tampering | npm/pip/cargo installs | accept | not applicable: this plan installs nothing. No package-manager install task exists, so no legitimacy checkpoint is required |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

See `261-11-PLAN.md` for the consolidated phase-wide artifact table. New in THIS plan, both under
`/home/jsagi/dev/ProblemsWorthSolving-Brain`:

| Path | Created by |
|---|---|
| `scripts/probe-ceremony-worklist.mjs` | 261-01 |
| `docs/2026-08-21-WORKLIST-261-ceremony.md` | 261-01 |

No graph writes. No MindrianOS-Plugin file is modified by this plan;
`data/flagship-floor-set.json` is READ, never edited.
</artifacts_this_phase_produces>

<verification>
- `node scripts/probe-ceremony-worklist.mjs` re-runs cleanly and its `[W-n]` sections match the
  numbers transcribed into the worklist document.
- The worklist names, not counts, every framework in every CER list.
- Every existing payload has a RUN / RETIRE / RETARGET disposition backed by a live readiness.
- The heal contradiction is stated and reconciled without editing either source.
- Nothing was pushed.
</verification>

<success_criteria>
- No downstream plan in Phase 261 needs to re-derive a worklist; each reads section 3 of one
  document.
- The Tier A set, the two cohorts, CER-04 and CER-06 are named lists with live readiness vectors
  and source paths.
- Admin exposure is a measurement, not an assumption.
- Zero em-dashes added. Zero pushes.
</success_criteria>

<output>
Create `.planning/phases/261-enrichment-ceremony-single-admin-window/261-01-SUMMARY.md` when done.
</output>
