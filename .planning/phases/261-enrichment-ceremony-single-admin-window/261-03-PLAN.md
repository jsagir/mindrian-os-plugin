---
phase: 261-enrichment-ceremony-single-admin-window
plan: 03
type: execute
wave: 2
depends_on: [261-01]
target_repo: ProblemsWorthSolving-Brain
files_modified:
  - ProblemsWorthSolving-Brain/payloads/hsi-semantic-surprise.mjs
  - ProblemsWorthSolving-Brain/payloads/root-cause-analysis.mjs
  - ProblemsWorthSolving-Brain/payloads/domain-selection.mjs
  - ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/hsi-semantic-surprise.json
  - ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/root-cause-analysis.json
  - ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/domain-selection.json
autonomous: true
requirements: [CER-02]

must_haves:
  truths:
    - "Each of the three fixtures is authored from the SOURCE DOCUMENT before its payload exists, so the fixture can disagree with the payload"
    - "Each payload's header cites its source path, its char count, and the date it was read in full"
    - "A framework whose source asserts no sequence emits NO LEADS_TO edges, and its expected readiness is stated honestly as 3/4 rather than padded to 4/4"
    - "Every node and edge the payload emits traces to a named, quotable passage in the cited source"
    - "The payloads are never executed by this plan and the fixtures assert against the POST-window state, so they are expected to fail live until the window runs"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/root-cause-analysis.mjs"
      provides: "Root Cause Analysis ingest payload: 4 phases plus the 5 named methods as Technique nodes"
      min_lines: 90
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/root-cause-analysis.json"
      provides: "Known-answer eval fixture: exact member-set equality, flow order, readiness vector, negative controls"
      contains: "negative"
  key_links:
    - from: "payloads/root-cause-analysis.mjs"
      to: "MindrianOS-Plugin references/methodology/root-cause.md"
      via: "header SOURCE comment naming path, char count and read date"
      pattern: "references/methodology/root-cause.md"
    - from: "tests/fixtures/framework-evals/*.json"
      to: "scripts/probe-framework-evals.mjs checkFrameworkEval"
      via: "discoverFixtures picks the file up with no registration step"
      pattern: "framework-evals"
---

<objective>
Author CER-02 Cohort 1, batch A: three mechanical flagship payloads plus their known-answer eval
fixtures, fixture-first, from source documents.

The three, with their live readiness measured 2026-08-21 and their sources from
`.planning/research/FEATURES.md`'s source map:

| Framework | Live readiness | Source (MindrianOS-Plugin) | Structure the source asserts |
|---|---|---|---|
| HSI Semantic Surprise Analysis Assistant | 0/4 | `references/methodology/score-innovation.md` (~6,809 chars) | 5 turn-windowed phases |
| Root Cause Analysis | 2/4 (st 1, fl 1) | `references/methodology/root-cause.md` (~8,041 chars) | 4 phases DETECT/ANALYZE/CORRECT/EMBED plus 5 NAMED methods |
| Domain Selection | 2/4 (st 1, fl 1) | `references/methodology/explore-domains.md` (~6,567 chars) | 5 phases plus IKA Scoring |

Purpose: these are called "mechanical" because the source's own numbered structure IS the payload's
spine. That does not make them low-care. Root Cause Analysis and Domain Selection already have
structure and flow live (they measure 2/4 with `st 1 fl 1`), so their payloads are ENRICHMENTS of
existing nodes, not new-node creations, and the dedup path's `enrich` branch handles them. HSI is
at 0/4 and may need its structure built from nothing. The payloads must not assume which case they
are in: plan 261-01's worklist carries the live readiness vector per row and the payload header
must state which branch it expects and why.

Fixture-first is a hard cross-cutting rule, not a preference: "fixtures authored BEFORE payloads,
from source docs, with mutator red-proofs. A fixture citing a probe instead of a source path is a
costume" (`.planning/REQUIREMENTS.md`, Cross-Cutting Rules). Task 1 writes all three fixtures and
Task 2 writes all three payloads, in that order, and the fixture must be capable of failing the
payload.

Output: three `payloads/*.mjs` files and three `tests/fixtures/framework-evals/*.json` files,
committed locally, never executed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/261-enrichment-ceremony-single-admin-window/261-RESEARCH.md
@.planning/research/FEATURES.md
@.planning/REQUIREMENTS.md
</context>

<cross_repo_contract>
| Concern | Repo | Absolute path |
|---|---|---|
| This PLAN.md and all GSD tracking | MindrianOS-Plugin | `/home/jsagi/dev/MindrianOS-Plugin/.planning/` (gitignored, `git add -f`) |
| Every file this plan creates | ProblemsWorthSolving-Brain | `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/` and `/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/` |
| The three source documents this plan READS | MindrianOS-Plugin | `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/{score-innovation,root-cause,explore-domains}.md` |

**PUSH FREEZE, NON-NEGOTIABLE.** `ProblemsWorthSolving-Brain` is frozen by
`docs/2026-08-20-FREEZE-push-freeze-before-261.md` until THIS phase's admin window closes and its
`GRAPH-WRITE-LOG.md` row is appended. Commit locally, NEVER `git push`. Plan 261-13 is the only
plan permitted to push. This overrides the repo's general commit-straight-to-main convention for
this phase, including for payload and fixture files.

**No admin window is opened or required, and no graph call is made.** Everything this plan needs
about the live graph is already in `docs/2026-08-21-WORKLIST-261-ceremony.md`.

**Subagent dispatch: SAFE.** File authoring, `node --test`, and `git commit`. No MCP tools required.

Both repos forbid em-dashes; hyphens only.
</cross_repo_contract>

<tasks>

<task type="auto">
  <name>Task 1: Author three known-answer eval fixtures from the source documents, before any payload exists</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/hsi-semantic-surprise.json (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/root-cause-analysis.json (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/domain-selection.json (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/reverse-salient-analysis.json` and `beautiful-question.json`: the exact fixture schema, field by field. Copy the schema; do not invent fields.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-framework-evals.mjs` lines 1-60: `checkFrameworkEval`'s content-check contract. It asserts exact canonical-set equality (not "count > 0"), structure member-set equality by name, `LEADS_TO` order matching the documented sequence OR an assertion that NO chain exists for a non-linear framework, the readiness floor with the expected dimensions vector, and negative controls.
    - `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/root-cause.md` IN FULL.
    - `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/score-innovation.md` IN FULL.
    - `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/explore-domains.md` IN FULL.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-WORKLIST-261-ceremony.md` section 3, the CER-02 rows for these three names, and section 2 for their current resolver match counts.
  </read_first>

  <action>
Read each of the three source documents in full before writing anything. Then write one fixture per
framework, derived ONLY from the source document plus the worklist's live readiness row. Do not
read or write any payload in this task; the payloads do not exist yet, which is the point.

Each fixture asserts, using the schema `reverse-salient-analysis.json` already establishes:
- The canonical name the framework must resolve to, and that it resolves to EXACTLY one.
- The exact member set of structure component names, spelled as the source spells them. Member-set
  equality, not cardinality. If the source names four phases DETECT, ANALYZE, CORRECT and EMBED,
  the fixture names all four.
- The flow assertion, and it cuts both ways. If the source asserts an order, name that order. If
  the source asserts NO order, the fixture asserts that NO `LEADS_TO` chain exists. Do not omit the
  flow assertion for a non-sequential framework; assert its absence.
- The expected readiness score AND its dimension vector. State it honestly. If a source names no
  distinct technique, `techniques` is 0 and the expected score is 3, and the fixture says 3.
- At least two negative controls per fixture: one fabricated component name that must be ABSENT,
  and one framework name that must NOT be returned as a canonical match for this name.
- A `source` field carrying the source doc path, its char count, and the date read in full. A
  fixture whose `source` cites a probe or another payload rather than a document path fails this
  plan's acceptance criteria outright.

Then prove each fixture can fail. Run the existing hermetic commit-gate leg
(`node --test tests/eval-framework-structure.test.mjs`) and confirm the three new fixtures are
discovered by `discoverFixtures` with no registration step. Record the discovered-fixture count
before and after in the plan summary. If the harness needs a registration entry somewhere, find it
and add it; do not leave a fixture that the harness silently never runs, which is this repo's own
named failure mode ("work that exists but cannot be seen").

Do NOT run the live leg (`scripts/probe-framework-evals.mjs` against canon). These three fixtures
describe the POST-window state and MUST fail live today. That expected failure is the fixture doing
its job. Record the expectation in each fixture as a comment-bearing field if the schema has one,
or in the plan summary if it does not.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && for f in hsi-semantic-surprise root-cause-analysis domain-selection; do node -e "const j=require('./tests/fixtures/framework-evals/$f.json'); if(!j.source) process.exit(1)" || exit 1; done && node --test tests/eval-framework-structure.test.mjs</automated>
  </verify>

  <acceptance_criteria>
    - Three fixture files exist and each parses as JSON.
    - Each fixture's `source` field names a `references/methodology/*.md` path, a char count, and a read date. A `source` naming a probe, a payload, or another fixture fails this criterion.
    - Each fixture asserts structure by exact member-set equality with names spelled as the source spells them, not by count.
    - Each fixture carries a flow assertion. For a source that asserts no order, that assertion is the ABSENCE of a `LEADS_TO` chain, explicitly present, not omitted.
    - Each fixture states an expected readiness score AND a dimension vector, and that score is derived from what the source supports, not raised to 4 for its own sake.
    - Each fixture carries at least two negative controls: a fabricated component that must be absent, and a wrong canonical name that must not be returned.
    - `node --test tests/eval-framework-structure.test.mjs` passes, and the discovered-fixture count increased by exactly 3 from its pre-task value. Both counts are recorded in the plan summary.
    - The live leg was NOT run against canon in this task, and the plan summary states that these fixtures are expected to fail live until the window executes.
    - `grep -RPn '\x{2014}' tests/fixtures/framework-evals/` exits 1 with no output.
  </acceptance_criteria>

  <done>
    Three fixtures exist that describe what the graph must look like after the window, derived from
    source documents alone, discovered by the existing harness, and capable of failing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the three Cohort 1 payloads against the fixtures</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/hsi-semantic-surprise.mjs (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/root-cause-analysis.mjs (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/domain-selection.mjs (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/reverse-salient-analysis.mjs` IN FULL, all 165 lines. This is the proven template: the SOURCE header block, the SHAPE NOTE, the TECHNIQUE justification, the EXPECTED READINESS AFTER INGEST line, and the `from_framework` name-resolved edge convention for a framework the payload carries no id for.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/project-list-structure.mjs` lines 1-40: the minimal `framework` block pattern for an EXISTING live node ("deliberately fabricates no description / category / applicable_stages"), and why the existence-aware validator downgrades those gaps to warn.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/validator.mjs`, the `frameworkIsLive` branch: what makes a payload an enrichment rather than a creation.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ontology.mjs` line 239 and its surrounding edge list: the closed edge vocabulary. Do not emit an edge type that is not in it.
    - Task 1's three fixtures. The payload must satisfy them; the fixtures are not adjusted to match the payload.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-CARRY-fix01-live-roundtrip.md` section 1: FIX-01's additive-prop path is deployed but not yet round-tripped against canon, and the round-trip is a numbered pre-item that runs BEFORE these payloads in the window. That ordering is why these payloads may carry framework-level props at all.
  </read_first>

  <action>
Write three payloads following `reverse-salient-analysis.mjs`'s structure exactly. For each:

Header comment block, mandatory, in this order: the framework name and which cycle authored it; a
`SOURCE:` line with the absolute-from-repo-root source path, its measured char count, and
"read in full, <date>"; which `/mos:` command loads it and that command's `frameworks:` frontmatter
value; a `SHAPE NOTE:` paragraph justifying the chosen `pattern_type` and whether a `LEADS_TO`
spine is source-supported, quoting the source where it asserts sequence; a `TECHNIQUE:` paragraph
naming every distinct titled sub-component the source contains and stating explicitly that no
others exist, so a reader can tell an omission from an oversight; and an
`EXPECTED READINESS AFTER INGEST:` line with the dimension arithmetic spelled out.

Body: `framework` block, `nodes` array, `edges` array, `problem_types` array. Rules that are not
negotiable:
- If the target node is already live (per the worklist's readiness row and the `[W-1]` match), the
  `framework` block carries ONLY the name, per `project-list-structure.mjs`'s pattern. Do not
  fabricate a description, category or `applicable_stages` for a node that already has them; that
  is the prop-overwrite hazard FIX-01 exists around.
- Edges resolve the parent by `from_framework` (name-resolved), not `from` (id-resolved), because
  the payload carries no id for the framework itself.
- Emit `LEADS_TO` ONLY where the source textually asserts sequence. Root Cause Analysis's
  DETECT/ANALYZE/CORRECT/EMBED is a sequence if and only if the document says so; check, do not
  assume from the acronym's ordering on the page.
- Emit exactly as many `Technique` nodes as the source names distinct titled techniques. Root Cause
  Analysis's source is expected to name five (5 Whys, Fishbone, Fault Tree, Barrier, Change) and
  Domain Selection's to name IKA Scoring. Verify against the document; if the count differs from
  FEATURES.md's 2026-08-13 reading, the document wins and the difference is noted in the header.
- Emit NO `ADDRESSES_PROBLEM_TYPE` unless the source uses the problem-type vocabulary. Omitting it
  entirely is the honest choice when no navigator ruling names a problem_types set, per
  `reverse-salient-analysis.mjs`'s own recorded reasoning.
- Every edge type must appear in `src/ontology.mjs`'s closed list.

After writing each payload, cross-check it against its Task 1 fixture by hand and record the
comparison in the plan summary: does the payload emit exactly the structure member set the fixture
names? Does it emit the flow the fixture asserts, or the absence the fixture asserts? Does its
`EXPECTED READINESS AFTER INGEST` arithmetic equal the fixture's expected score and dimension
vector? Any disagreement is resolved by re-reading the SOURCE, and whichever of the two is wrong is
corrected. Do not resolve a disagreement by loosening the fixture.

Do NOT run `payloads/run-ingest.mjs`. It requires a live Bolt connection to canon which this
machine does not have (`docs/2026-08-20-CARRY-fix01-live-roundtrip.md` section 1, item 3), and the
dry-run for these payloads happens inside the window through the admin `ingest_framework` tool over
HTTPS. Attempting it here produces a connection error that proves nothing.

Commit locally. Do not push.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && for f in hsi-semantic-surprise root-cause-analysis domain-selection; do node -e "import('./payloads/$f.mjs').then(m=>{const p=m.payload??m.default; if(!p||!p.framework||!p.framework.name||!Array.isArray(p.nodes)||!Array.isArray(p.edges)) process.exit(1);}).catch(()=>process.exit(1))" || exit 1; grep -q 'SOURCE:' payloads/$f.mjs || exit 1; grep -q 'EXPECTED READINESS AFTER INGEST' payloads/$f.mjs || exit 1; done</automated>
  </verify>

  <acceptance_criteria>
    - All three payload modules import cleanly under `node` and export an object with `framework.name`, `nodes` and `edges`.
    - Each payload header contains a `SOURCE:` line naming a real file under `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/`, a char count, and "read in full" plus a date. Verify the path exists with `test -f`.
    - Each payload header contains a `SHAPE NOTE:` paragraph quoting the source where it does or does not assert sequence, and an `EXPECTED READINESS AFTER INGEST:` line whose arithmetic sums the four dimensions explicitly.
    - Every edge `type` emitted by every payload appears in `src/ontology.mjs`'s closed edge list. Check each type by grep and record the checks in the plan summary.
    - No payload emits `LEADS_TO` for a framework whose source does not assert sequence, and the header states which case applies and quotes the evidence.
    - The `Technique` node count per payload equals the number of distinct titled techniques the source names, and the header states that number and asserts no others exist.
    - For any framework the worklist reports as already live, the payload's `framework` block carries ONLY the name and fabricates no description, category or applicable_stages.
    - Each payload's expected readiness vector matches its Task 1 fixture's expected vector. Any mismatch was resolved by re-reading the source, and the plan summary records which of the two was corrected and why.
    - `payloads/run-ingest.mjs` was NOT run in this plan.
    - `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain log HEAD..origin/main --oneline` is empty: nothing pushed.
    - `grep -RPn '\x{2014}' payloads/hsi-semantic-surprise.mjs payloads/root-cause-analysis.mjs payloads/domain-selection.mjs` exits 1 with no output.
  </acceptance_criteria>

  <done>
    Three Cohort 1 payloads exist, each traceable line by line to a source document read in full,
    each matched against a fixture authored before it, none executed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source document to payload | content that is not in the document must not appear in the graph |
| payload to fixture | a fixture derived from the payload proves only that the payload equals itself |
| readiness score to honest shape | padding a dimension to reach 4/4 buys a number and loses the truth of the framework |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-261-12 | Spoofing | a fabricated phase, technique or LEADS_TO edge inserted to raise a readiness dimension | mitigate | fixture-first ordering plus the SHAPE NOTE and TECHNIQUE header blocks, which must quote the source; negative controls in each fixture assert a fabricated component is ABSENT |
| T-261-13 | Repudiation | a fixture written from the payload, rubber-stamping it | mitigate | Task 1 runs before Task 2 and reads only source documents; the fixture's `source` field must name a document path and is acceptance-checked |
| T-261-14 | Tampering | an enrichment payload overwriting a live node's existing description or category | mitigate | for live targets the `framework` block carries name only, per `project-list-structure.mjs`; FIX-01's additive path is additionally round-tripped as the window's first item |
| T-261-15 | Denial of Service | a fixture added but never discovered by the harness, so it silently never runs | mitigate | acceptance requires the discovered-fixture count to increase by exactly 3, recorded before and after |
| T-261-16 | Tampering | an edge type outside the closed ontology vocabulary reaching canon | mitigate | every emitted type is grep-checked against `src/ontology.mjs` and the checks are recorded |
| T-261-SC | Tampering | npm/pip/cargo installs | accept | not applicable: this plan installs nothing |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

New in THIS plan, under `/home/jsagi/dev/ProblemsWorthSolving-Brain`:
`payloads/hsi-semantic-surprise.mjs`, `payloads/root-cause-analysis.mjs`,
`payloads/domain-selection.mjs`, `tests/fixtures/framework-evals/hsi-semantic-surprise.json`,
`tests/fixtures/framework-evals/root-cause-analysis.json`,
`tests/fixtures/framework-evals/domain-selection.json`.

Graph state these payloads will create WHEN THE WINDOW RUNS THEM (not here): structure component
nodes, `Technique` nodes and their edges for the three named frameworks, plus framework-level props
through FIX-01's additive path.

No MindrianOS-Plugin file is modified; the three methodology documents are READ.
</artifacts_this_phase_produces>

<verification>
- Three fixtures parse, cite document sources, and are discovered by the existing harness.
- Three payloads import, cite sources with char counts and read dates, and emit only ontology-legal
  edge types.
- Each payload's expected readiness vector equals its fixture's.
- Nothing was ingested, nothing was pushed.
</verification>

<success_criteria>
- Three of CER-02's Cohort 1 rows are authored fixture-first and are ready for the window's dry-run.
- Not one node or edge in the three payloads lacks a source passage behind it.
- Honest ceilings are stated as ceilings, never padded.
- Zero em-dashes added. Zero pushes.
</success_criteria>

<output>
Create `.planning/phases/261-enrichment-ceremony-single-admin-window/261-03-SUMMARY.md` when done.
</output>
