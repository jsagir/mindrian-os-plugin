---
phase: 261-enrichment-ceremony-single-admin-window
plan: 04
type: execute
wave: 2
depends_on: [261-01]
target_repo: ProblemsWorthSolving-Brain
files_modified:
  - ProblemsWorthSolving-Brain/payloads/knowns-unknowns-matrix.mjs
  - ProblemsWorthSolving-Brain/payloads/dominant-design.mjs
  - ProblemsWorthSolving-Brain/payloads/systems-thinking.mjs
  - ProblemsWorthSolving-Brain/payloads/minto-pyramid.mjs
  - ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/knowns-unknowns-matrix.json
  - ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/dominant-design.json
  - ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/systems-thinking.json
  - ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/pyramid-principle.json
  - ProblemsWorthSolving-Brain/docs/2026-08-21-RULING-pyramid-principle-target.md
autonomous: true
requirements: [CER-02]

must_haves:
  truths:
    - "Systems Thinking's payload picks ONE structural spine (the four phases OR the Five Moves) and discloses the choice and the rejected alternative in its header"
    - "The already-authored minto-pyramid.mjs is verified against the node the ratified floor name actually resolves to, and RETARGETED rather than silently run if they differ"
    - "Every fixture is authored from its source document before its payload is written or amended"
    - "A framework whose source asserts no sequence emits NO LEADS_TO edges and states its honest 3/4 ceiling"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-RULING-pyramid-principle-target.md"
      provides: "The node-identity finding for The Pyramid Principle: which id the floor name resolves to, what minto-pyramid.mjs targets, and the disposition"
      contains: "The Pyramid Principle"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/systems-thinking.mjs"
      provides: "Systems Thinking payload with a single disclosed spine"
      min_lines: 90
  key_links:
    - from: "docs/2026-08-21-RULING-pyramid-principle-target.md"
      to: "payloads/relabel-fix-260820/manifest.json unresolved_residue and blocking_preconditions_NOT_MET"
      via: "the same node-identity dispute, cross-referenced so the window rules on it once"
      pattern: "relabel-fix-260820"
    - from: "payloads/systems-thinking.mjs header"
      to: "MindrianOS-Plugin references/methodology/systems-thinking.md"
      via: "SHAPE NOTE disclosing the chosen spine and naming the rejected one"
      pattern: "systems-thinking.md"
---

<objective>
Author CER-02 Cohort 1, batch B: three mechanical payloads plus a node-identity ruling for a fourth
framework whose payload already exists but may target the wrong node.

| Framework | Live readiness (2026-08-21) | Source (MindrianOS-Plugin) | Note |
|---|---|---|---|
| Knowns and Unknowns Matrix Framework | 2/4 (st 1, fl 1) | `references/methodology/map-unknowns.md` (~5,681 chars) | 4 phases plus the 2x2 matrix |
| Dominant Design | 0/4 | `references/methodology/dominant-designs.md` (~9,957 chars) | 6 phases plus Utterback-Abernathy and S-Curve concepts |
| Systems Thinking | 0/4 | `references/methodology/systems-thinking.md` (~12,573 chars) plus `causal-loop-diagrams.md` (~5,074) | TWO structure candidates; the author must pick ONE spine and disclose |
| The Pyramid Principle | 0/4 | `references/methodology/structure-argument.md` (~6,084 chars) | Payload ALREADY EXISTS as `payloads/minto-pyramid.mjs`, unexecuted, commit `aa15966` |

Two things make this batch different from batch A.

**Systems Thinking has two candidate spines and only one may win.** The source carries both four
phases and a Five Moves model (M1 through M5). Emitting both as structure produces a node whose
`HAS_PHASE` set is an incoherent union of two different decompositions, and every downstream
`discover_structure` call inherits the incoherence. Pick one, state which, state why, and name the
rejected alternative in the header so a future reader can reverse the call without re-reading
12,573 characters.

**The Pyramid Principle already has a payload, and it may point at the wrong node.** This is not a
hypothetical. `payloads/relabel-fix-260820/manifest.json` records two live disputes over exactly
this name family: `unresolved_residue.pyramid_principle_39014` (a `[Book]`-labelled node that may
be a citation source rather than a duplicate concept) and
`blocking_preconditions_NOT_MET.contested_survivor_picks` (the merged-but-unpushed
`audit/2026-08-20-brain-heal` run's `alias-review.md` collapses node 38968 INTO "The Pyramid
Principle" 30242, while `relabel-fix-260820` treats 38968 as the survivor). Running
`minto-pyramid.mjs` blind would enrich whichever node it names and leave the floor's own
`The Pyramid Principle` row possibly untouched. Task 3 produces the finding; the RULING is a
navigator decision carried on a card in plan 261-12, not made here.

Output: three payloads, four fixtures, one ruling document, and a possibly-amended
`minto-pyramid.mjs`. Committed locally, never executed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/261-enrichment-ceremony-single-admin-window/261-RESEARCH.md
@.planning/research/FEATURES.md
@.planning/REQUIREMENTS.md
</context>

<cross_repo_contract>
| Concern | Repo | Absolute path |
|---|---|---|
| This PLAN.md and all GSD tracking | MindrianOS-Plugin | `/home/jsagi/dev/MindrianOS-Plugin/.planning/` (gitignored, `git add -f`) |
| Every file this plan creates or amends | ProblemsWorthSolving-Brain | `/home/jsagi/dev/ProblemsWorthSolving-Brain/` |
| The source documents this plan READS | MindrianOS-Plugin | `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/{map-unknowns,dominant-designs,systems-thinking,causal-loop-diagrams,structure-argument}.md` |

**PUSH FREEZE, NON-NEGOTIABLE.** `ProblemsWorthSolving-Brain` is frozen by
`docs/2026-08-20-FREEZE-push-freeze-before-261.md` until THIS phase's admin window closes and its
`GRAPH-WRITE-LOG.md` row is appended. Commit locally, NEVER `git push`. Plan 261-13 is the only
plan permitted to push. This overrides the repo's general commit-straight-to-main convention.

**No admin window is opened or required, and no graph call is made.** Task 3's node-identity
finding is derived from `docs/2026-08-21-WORKLIST-261-ceremony.md` (already measured, `[W-1]`,
`[W-2]` and the section 4 reuse audit), from `payloads/relabel-fix-260820/manifest.json`, and from
`docs/heal-2026-08-20/alias-review.md`. Do not re-probe.

**Subagent dispatch: SAFE.** File authoring, `node --test`, and `git commit`. No MCP tools required.

Both repos forbid em-dashes; hyphens only.
</cross_repo_contract>

<tasks>

<task type="auto">
  <name>Task 1: Author four fixtures from source documents, before touching any payload</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/knowns-unknowns-matrix.json (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/dominant-design.json (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/systems-thinking.json (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/pyramid-principle.json (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/reverse-salient-analysis.json`: the schema, field by field.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-framework-evals.mjs` lines 1-60: the content-check contract, including that flow honesty cuts both ways.
    - `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/map-unknowns.md` IN FULL.
    - `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/dominant-designs.md` IN FULL.
    - `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/systems-thinking.md` IN FULL, and `causal-loop-diagrams.md` IN FULL.
    - `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/structure-argument.md` IN FULL. Note that this one document is the source for BOTH The Pyramid Principle and MECE (plan 261-05's row 13), so read it once and record both readings.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-WORKLIST-261-ceremony.md` sections 2, 3 and 4.
  </read_first>

  <action>
Write four fixtures using the same rules plan 261-03 Task 1 states: exact canonical-name resolution
with a count of exactly 1, structure asserted by member-set equality with source spelling, an
explicit flow assertion in either direction, an expected readiness score with its dimension vector
stated honestly, at least two negative controls each, and a `source` field naming a document path
with a char count and a read date.

Two fixture-specific instructions:

`systems-thinking.json` must assert the member set of the SPINE YOU CHOOSE and must additionally
carry a negative control asserting that a member of the REJECTED spine is ABSENT. If the four
phases win, one of M1 through M5 by name must be asserted absent. This is what makes the
one-spine decision testable rather than a comment.

`pyramid-principle.json` asserts against the canonical name `The Pyramid Principle` as spelled in
`data/flagship-floor-set.json`, because that exact string is what
`check-flagship-floor.cjs` looks up (`overrideNames.has(fw.name)`, case-sensitive). It must NOT
assert against `Minto Pyramid`. If the ratified name and the payload's target turn out to be
different nodes, that is Task 3's finding and the fixture is what will expose it.

Run `node --test tests/eval-framework-structure.test.mjs` and confirm the discovered-fixture count
rose by exactly 4. Do NOT run the live probe leg; these fixtures describe the post-window state.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && for f in knowns-unknowns-matrix dominant-design systems-thinking pyramid-principle; do node -e "const j=require('./tests/fixtures/framework-evals/$f.json'); if(!j.source) process.exit(1)" || exit 1; done && node --test tests/eval-framework-structure.test.mjs</automated>
  </verify>

  <acceptance_criteria>
    - Four fixture files exist and parse; each `source` names a `references/methodology/*.md` path with a char count and a read date.
    - Each fixture asserts structure by exact member-set equality, carries an explicit flow assertion in one direction or the other, states an expected readiness score with its dimension vector, and carries at least two negative controls.
    - `systems-thinking.json` carries a negative control naming a member of the REJECTED spine and asserting it is absent.
    - `pyramid-principle.json` asserts the canonical name exactly as `data/flagship-floor-set.json` spells it, and does not assert `Minto Pyramid`.
    - `node --test tests/eval-framework-structure.test.mjs` passes and the discovered-fixture count rose by exactly 4; both counts recorded in the plan summary.
    - The live probe leg was NOT run; the plan summary states these fixtures are expected to fail live until the window executes.
    - `grep -RPn '\x{2014}' tests/fixtures/framework-evals/` exits 1 with no output.
  </acceptance_criteria>

  <done>
    Four source-derived fixtures exist, the Systems Thinking spine decision is encoded as a testable
    negative control, and the Pyramid Principle fixture is pinned to the ratified name.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the three new Cohort 1 payloads</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/knowns-unknowns-matrix.mjs (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/dominant-design.mjs (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/systems-thinking.mjs (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/reverse-salient-analysis.mjs` IN FULL: the template, its header block ordering, and its `from_framework` edge convention.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/project-list-structure.mjs` lines 1-40: the minimal `framework` block for a node that is already live.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ontology.mjs` line 239 and its surrounding list: the closed edge vocabulary.
    - Task 1's four fixtures.
    - The worklist's readiness rows for these three names, to know which payloads are enrichments of live nodes and which build structure from nothing.
  </read_first>

  <action>
Write three payloads following the template. Every header rule plan 261-03 Task 2 states applies
here unchanged: the `SOURCE:` line with path, char count and read date; the `SHAPE NOTE:` paragraph
quoting the source on sequence; the `TECHNIQUE:` paragraph naming every distinct titled technique
and asserting no others exist; the `EXPECTED READINESS AFTER INGEST:` arithmetic; a name-only
`framework` block for live targets; `from_framework` edge resolution; `LEADS_TO` only where the
source asserts sequence; edge types only from `src/ontology.mjs`.

Systems Thinking carries one extra, mandatory header section, `SPINE DECISION:`. It must state
which of the two candidate decompositions was chosen for the structure edges, quote the source
passage that makes the chosen one the framework's actual process rather than a supplementary model,
name the rejected decomposition explicitly, and state what a future author would need to change to
reverse the decision. If `causal-loop-diagrams.md` supplies technique depth (the CLD Storytelling
Method), cite it as a SECONDARY source with its own char count and read date; do not fold two
documents into one citation.

Dominant Design's source names Utterback-Abernathy and S-Curve theory as concepts. Decide, and
state in the `TECHNIQUE:` block, whether each is a `Technique` node of this framework or a
reference to a framework that already exists in the graph. `S-Curve Analysis` is a live ratified
framework at 3/4 (see the worklist), so creating a second `S-Curve` node under Dominant Design
would be a duplicate. If a relationship is warranted, use an ontology-legal edge to the existing
node rather than a new node, and say so.

Cross-check each payload against its fixture and record the comparison in the plan summary, exactly
as plan 261-03 Task 2 requires. Resolve disagreements by re-reading the source, never by loosening
a fixture.

Do NOT run `payloads/run-ingest.mjs`; there is no Bolt connection to canon from this machine.

Commit locally. Do not push.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && for f in knowns-unknowns-matrix dominant-design systems-thinking; do node -e "import('./payloads/$f.mjs').then(m=>{const p=m.payload??m.default; if(!p||!p.framework||!p.framework.name||!Array.isArray(p.nodes)||!Array.isArray(p.edges)) process.exit(1);}).catch(()=>process.exit(1))" || exit 1; grep -q 'SOURCE:' payloads/$f.mjs || exit 1; grep -q 'EXPECTED READINESS AFTER INGEST' payloads/$f.mjs || exit 1; done && grep -q 'SPINE DECISION:' payloads/systems-thinking.mjs</automated>
  </verify>

  <acceptance_criteria>
    - All three payload modules import cleanly and export `framework.name`, `nodes` and `edges`.
    - Each header carries `SOURCE:` with a real path (verified with `test -f`), a char count and a read date; a `SHAPE NOTE:` quoting the source on sequence; a `TECHNIQUE:` paragraph asserting the complete technique inventory; and an `EXPECTED READINESS AFTER INGEST:` line with explicit dimension arithmetic.
    - `payloads/systems-thinking.mjs` contains a `SPINE DECISION:` block naming the chosen spine, quoting its justification, naming the rejected spine, and stating how to reverse the call. Its structure edges reference members of ONE spine only.
    - `payloads/dominant-design.mjs` creates no node duplicating an existing ratified framework. If it relates to `S-Curve Analysis`, it does so with an ontology-legal edge to the existing node, and the header says so.
    - Every emitted edge `type` appears in `src/ontology.mjs`'s closed list; the grep checks are recorded in the plan summary.
    - No payload emits `LEADS_TO` for a source that asserts no sequence.
    - For any live target, the `framework` block carries name only.
    - Each payload's readiness vector matches its fixture's; any correction is recorded with which side changed and why.
    - `payloads/run-ingest.mjs` was NOT run.
    - `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain log HEAD..origin/main --oneline` is empty.
    - `grep -RPn '\x{2014}' payloads/knowns-unknowns-matrix.mjs payloads/dominant-design.mjs payloads/systems-thinking.mjs` exits 1 with no output.
  </acceptance_criteria>

  <done>
    Three Cohort 1 payloads exist, the Systems Thinking spine is a documented and testable decision,
    and Dominant Design does not duplicate a framework that already exists.
  </done>
</task>

<task type="auto">
  <name>Task 3: Resolve the Pyramid Principle node identity and dispose of minto-pyramid.mjs</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-RULING-pyramid-principle-target.md (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/minto-pyramid.mjs (modify, header amendment only if RETARGET is the finding)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/minto-pyramid.mjs` IN FULL: what node it targets, by name and by any id, and what it emits.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-WORKLIST-261-ceremony.md` section 2 (`The Pyramid Principle`'s resolver match count and readiness), section 4 (its reuse-audit disposition), and `[W-1]`'s raw match count for the same name.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/relabel-fix-260820/manifest.json`: `unresolved_residue.pyramid_principle_39014` and `blocking_preconditions_NOT_MET.contested_survivor_picks` verbatim, including the specific node ids 38968, 30242 and 39014.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/relabel-fix-260820/README.md` `## Declared residue`: the `[Book]` versus framework entity-type argument in the drafter's own words.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/heal-2026-08-20/alias-review.md`: the competing survivor ruling from the already-executed heal run.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-DECISION-repo-hygiene-close-out.md`: the record that merging the heal branch did NOT adjudicate this conflict, so it is still open.
    - `/home/jsagi/dev/MindrianOS-Plugin/data/flagship-floor-set.json`: the exact ratified string `The Pyramid Principle`.
  </read_first>

  <action>
Write `docs/2026-08-21-RULING-pyramid-principle-target.md`. Despite its filename it is a FINDING
document that ENDS in a question, not a decision. The decision is a navigator card in plan 261-12.
Say that in its first paragraph so nobody mistakes it for a settled ruling.

Sections:

**1. The three nodes.** A table of every node in this name family with `id | name | labels |
readiness vector | edge summary`, drawn from the worklist and the relabel payload's manifest. At
minimum: 39014 (`[Book]`), 38968, 30242, and whatever node the ratified string `The Pyramid
Principle` actually resolves to per `[W-2]`. If those are the same node, say so plainly; if the
ratified string resolves to nothing or to more than one, that is the headline.

**2. What minto-pyramid.mjs targets.** Quote the payload's own `FW_NAME` and any id it carries,
and state whether that target is the node the ratified string resolves to. This is the whole point
of the document.

**3. The two competing survivor rulings.** State `relabel-fix-260820`'s pick and
`alias-review.md`'s pick side by side, with node ids, and note that
`docs/2026-08-21-DECISION-repo-hygiene-close-out.md` records the conflict as unadjudicated. Do not
pick a winner here.

**4. The entity-type question.** Restate the `[Book]` argument: 39014 looks like a citation or
source node, not a second copy of the concept, so `ALIAS_OF` would conflate the book with the
framework it describes, and `DESCRIBES` or `SOURCE_FOR` may be the right edge instead. Check both
candidate edge types against `src/ontology.mjs`'s closed list and STATE whether each is currently
in the vocabulary. If neither is, say so, because then the honest options collapse to "leave it
alone" or "propose a schema amendment", and inventing an edge type mid-ceremony is not one of them.

**5. Disposition of `payloads/minto-pyramid.mjs`.** Exactly one of `RUN AS IS` (its target is the
ratified node), `RETARGET` (its target is a different node and the payload must be amended before
it can close CER-02's Pyramid Principle row), or `HOLD` (the target cannot be determined until the
survivor conflict is ruled). If the disposition is `RETARGET`, amend ONLY the payload's header in
this task: add a dated `RETARGET NOTE:` block stating the finding, the ratified string, the two
candidate ids, and that the body is NOT changed until the navigator rules. Do not silently rewrite
the body to point somewhere new; that would execute a contested ruling by editing a file.

**6. The card this needs.** Write the exact question the navigator must answer, in one or two
sentences, phrased so it can be pasted into plan 261-12's checkpoint. Include the default this
document recommends and the reason.

Cross-link the ruling document from `payloads/relabel-fix-260820/README.md` by appending a single
`## Cross-reference` line pointing at it, so a reader of that payload finds this finding. That is
the only edit this task makes to the relabel payload.

Commit locally. Do not push.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && R=docs/2026-08-21-RULING-pyramid-principle-target.md && test -f "$R" && grep -qE 'RUN AS IS|RETARGET|HOLD' "$R" && grep -q '39014' "$R" && grep -q 'alias-review' "$R" && grep -q 'Cross-reference' payloads/relabel-fix-260820/README.md && ! grep -Pq '\x{2014}' "$R"</automated>
  </verify>

  <acceptance_criteria>
    - `docs/2026-08-21-RULING-pyramid-principle-target.md` exists and its opening paragraph states that it is a finding awaiting a navigator decision, not a settled ruling.
    - Section 1 tables every node in the name family with an id, labels and a readiness vector, and explicitly answers what the ratified string `The Pyramid Principle` resolves to.
    - Section 2 quotes `minto-pyramid.mjs`'s own declared target and states whether it equals the ratified node.
    - Section 3 states both competing survivor picks with node ids and cites `docs/2026-08-21-DECISION-repo-hygiene-close-out.md` as evidence the conflict is unadjudicated. Neither pick is declared the winner.
    - Section 4 checks `DESCRIBES` and `SOURCE_FOR` against `src/ontology.mjs` and states, for each, whether it is currently in the closed vocabulary. An assertion without that check fails this criterion.
    - Section 5 records exactly one of `RUN AS IS`, `RETARGET`, `HOLD`.
    - If the disposition is `RETARGET`, `payloads/minto-pyramid.mjs` gained a dated `RETARGET NOTE:` header block and its `nodes`/`edges` arrays are byte-identical to their pre-task state. Verify with `git diff` that no line outside the header comment changed.
    - Section 6 contains a paste-ready navigator question with a recommended default and a reason.
    - `payloads/relabel-fix-260820/README.md` gained exactly one `## Cross-reference` section and nothing else changed in that file.
    - `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain log HEAD..origin/main --oneline` is empty.
    - `grep -Pn '\x{2014}' docs/2026-08-21-RULING-pyramid-principle-target.md` exits 1 with no output.
  </acceptance_criteria>

  <done>
    The Pyramid Principle's node identity is a documented finding with named ids, both competing
    rulings are on the record without either being silently adopted, and the existing payload
    carries an honest disposition instead of being run blind.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a payload's declared target to the node a ratified name resolves to | enriching the wrong node produces real work and zero floor movement |
| two competing survivor rulings | adopting one by editing a file executes a contested decision without a decision |
| two candidate spines in one source | emitting both produces an incoherent structure that every downstream reader inherits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-261-17 | Tampering | `minto-pyramid.mjs` executed against a node that is not the ratified `The Pyramid Principle` | mitigate | Task 3's finding names the ids; the payload gains a `RETARGET NOTE:` and its body is proven byte-identical by `git diff` |
| T-261-18 | Repudiation | silently adopting one of the two competing survivor rulings by amending a payload body | mitigate | acceptance forbids body changes in Task 3; the ruling is a plan 261-12 checkpoint |
| T-261-19 | Spoofing | an invented edge type (`DESCRIBES`, `SOURCE_FOR`) written as if it were in the vocabulary | mitigate | section 4 must check `src/ontology.mjs` and state the result per type; a schema amendment is named as a separate path, never done inline |
| T-261-20 | Tampering | a Systems Thinking structure set that unions two decompositions | mitigate | the `SPINE DECISION:` header block plus a fixture negative control asserting a rejected-spine member is ABSENT |
| T-261-21 | Tampering | a second `S-Curve` node created under Dominant Design, duplicating a live ratified framework | mitigate | acceptance forbids creating a node duplicating an existing ratified framework and requires an ontology-legal edge instead |
| T-261-SC | Tampering | npm/pip/cargo installs | accept | not applicable: this plan installs nothing |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

New in THIS plan, under `/home/jsagi/dev/ProblemsWorthSolving-Brain`:
`payloads/knowns-unknowns-matrix.mjs`, `payloads/dominant-design.mjs`,
`payloads/systems-thinking.mjs`, `tests/fixtures/framework-evals/knowns-unknowns-matrix.json`,
`tests/fixtures/framework-evals/dominant-design.json`,
`tests/fixtures/framework-evals/systems-thinking.json`,
`tests/fixtures/framework-evals/pyramid-principle.json`,
`docs/2026-08-21-RULING-pyramid-principle-target.md`.

Modified existing files: `payloads/minto-pyramid.mjs` (header only, and only if RETARGET),
`payloads/relabel-fix-260820/README.md` (one `## Cross-reference` section).

No MindrianOS-Plugin file is modified.
</artifacts_this_phase_produces>

<verification>
- Four fixtures parse, cite document sources, and are discovered by the harness.
- Three payloads import and carry every mandated header block, including `SPINE DECISION:`.
- The Pyramid Principle finding names ids, states both competing rulings, and ends in a question.
- `minto-pyramid.mjs`'s body is unchanged.
- Nothing was ingested, nothing was pushed.
</verification>

<success_criteria>
- Four more CER-02 rows are ready for the window: three authored, one with an honest disposition.
- The Systems Thinking spine choice is testable, not just documented.
- No contested node-identity ruling was adopted by editing a file.
- Zero em-dashes added. Zero pushes.
</success_criteria>

<output>
Create `.planning/phases/261-enrichment-ceremony-single-admin-window/261-04-SUMMARY.md` when done.
</output>
