---
phase: 261-enrichment-ceremony-single-admin-window
plan: 09
type: execute
wave: 2
depends_on: [261-01]
target_repo: ProblemsWorthSolving-Brain
files_modified:
  - ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/README.md
  - ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/manifest.json
  - ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/00-review-list.md
  - ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/01-relabel-block.cypher
  - ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/90-dry-run.cypher
  - ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/91-verify.cypher
  - ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/99-undo.cypher
autonomous: true
requirements: [CER-05]

must_haves:
  truths:
    - "The relabel candidate list is a per-node, human-readable page with one row per node, because the 2026-02-05 relabel disaster was caused by a bulk automated relabel with no human-reviewed triage list"
    - "Every row carries an explicit INCLUDE or EXCLUDE with a reason, and an EXCLUDE is never a silent omission"
    - "TRIZ (id 28666) is handled as one row of this block, not as separate work, because it is not a stub and is already labelled [Archived, Method, Concept] with a real description"
    - "The payload states, in its own README, that this relabel recovers at most 11 USES_FRAMEWORK edges and rescues ZERO zero-framework commands, so a flat command-coverage number afterwards is the predicted outcome"
    - "The relabel is a SEPARATE batch from the edge authoring, with its own batch_id and its own GRAPH-WRITE-LOG row, so Phase 262's floor movement stays attributable"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/00-review-list.md"
      provides: "One page, one row per node in the 28000-29000 block: id, name, current labels, proposed labels, INCLUDE or EXCLUDE with a reason"
      contains: "EXCLUDE"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/01-relabel-block.cypher"
      provides: "Guarded UNWIND relabel: REMOVE :Archived, SET :Framework, id and name double guarded"
      contains: "UNWIND"
  key_links:
    - from: "payloads/archived-block-relabel-2026-08-21/00-review-list.md"
      to: "docs/2026-08-21-WORKLIST-261-ceremony.md [W-6]"
      via: "the per-node block listing the worklist probe emitted"
      pattern: "W-6"
    - from: "payloads/archived-block-relabel-2026-08-21/README.md"
      to: "261-RESEARCH.md 'The expectation reset this phase must carry into 262'"
      via: "the at-most-11-edges and rescues-zero-commands prediction, restated as this batch's own expected outcome"
      pattern: "262"
---

<objective>
Author the archived-block relabel: the roughly 95 core PWS methodology nodes sitting in the
contiguous id block 28000 to 29000, demoted out of `:Framework` and marked `:Archived` by a single
batch operation, restored by a human-reviewed list.

**What is in the block, and why it matters.** `261-RESEARCH.md` names the content: Six Thinking
Hats, TRIZ, Design Thinking, Lean Startup, MECE, The Pyramid Principle, The Cynefin Framework, Four
Lenses of Innovation, Jobs to Be Done, Red Teaming, Pattern Recognition, Issue Trees, Causal Loop
Diagrams, Problems Worth Solving, The PWS Value Proposition Framework, The Taxonomy of Problems, The
Opportunity Bank Framework, White Space Analysis, Human-Centered Design, Effectuation, Open
Innovation. That is the core of the methodology. Gate 0 reads `:Framework` at 186 against an
expected roughly 750, and this block is a measured, contiguous, single-batch-shaped slice of the
gap. The planner re-measured it live on 2026-08-21: 100 demoted, 99 also `:Archived`, 95 inside the
id range.

**Why this is not a bulk script.** `.planning/2026-08-20-BRIEF-complete-system-loop.md` states the
standing precedent: "The 2026-02-05 relabel disaster is the direct cautionary precedent. The
original mislabeling this whole chain traces back to was caused by a bulk automated relabel with no
human-reviewed triage list ... Any Gate 0 remediation must follow that same shape, propose, get
sign-off, then execute, not repeat the mistake it's fixing." And `261-RESEARCH.md` closes the
argument: the candidate set is roughly 95 nodes in one identifiable block, "small enough for a full
human review list and removes any argument for a bulk automated relabel." One page. Propose, sign
off, execute.

**TRIZ is a row, not a project.** The retired Phase 256 framed it as "promote TRIZ from uncurated
stub Concept to a real Framework". `261-RESEARCH.md` corrects that: TRIZ is id 28666, labelled
`[Archived, Method, Concept]`, carrying a real description. "The correct action is un-archive and
relabel TRIZ along with the other 94 in the same block, which means it is not separate work at all."
The review list must show TRIZ as one row like any other, and the README must record the correction
so nobody re-opens it as a separate task.

**The expectation reset is this payload's job to carry.** `261-RESEARCH.md`'s single most important
line: relabelling this block "will recover at most 11 edges and will rescue 0 of the 59
zero-framework commands." Zero `USES_FRAMEWORK` edges currently target an archived node. The
intuitive fix does not touch the visible product symptom. Two consequences the README must state:
do not read a flat command-coverage number after this relabel as the remediation failing, it is the
prediction; and log this relabel as its own operation with its own `batch_id` and its own
`GRAPH-WRITE-LOG` row, separate from plan 261-10's edge authoring, with a read-tier probe between
them, or Phase 262's movement becomes unattributable. That is the planner's ruling on
`261-RESEARCH.md`'s open question 1, answered YES.

**The root cause is a confirmed dead end and that is fine.** `261-RESEARCH.md`'s live update
records that neither `ProblemsWorthSolving-Brain` nor `mindrian-brain-local` git history reaches
back to 2026-02-05, so "who did this and when" will not resolve from a commit. The relabel does not
wait on an answer that does not exist. Record the dead end in the README so a future reader does not
re-run the hunt.

Output: `payloads/archived-block-relabel-2026-08-21/`, committed locally, never executed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/261-enrichment-ceremony-single-admin-window/261-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<cross_repo_contract>
| Concern | Repo | Absolute path |
|---|---|---|
| This PLAN.md and all GSD tracking | MindrianOS-Plugin | `/home/jsagi/dev/MindrianOS-Plugin/.planning/` (gitignored, `git add -f`) |
| Every file this plan creates | ProblemsWorthSolving-Brain | `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/` |
| The precedent document this plan READS | MindrianOS-Plugin | `/home/jsagi/dev/MindrianOS-Plugin/.planning/2026-08-20-BRIEF-complete-system-loop.md` |

**PUSH FREEZE, NON-NEGOTIABLE.** `ProblemsWorthSolving-Brain` is frozen by
`docs/2026-08-20-FREEZE-push-freeze-before-261.md` until THIS phase's admin window closes and its
`GRAPH-WRITE-LOG.md` row is appended. Commit locally, NEVER `git push`. Plan 261-13 is the only
plan permitted to push. This overrides the repo's general commit-straight-to-main convention.

**No admin window is opened or required, and no graph call is made.** The per-node block listing
comes from `docs/2026-08-21-WORKLIST-261-ceremony.md`'s `[W-6]`, which plan 261-01 already emitted
one row per node for. Do not re-probe here; `90-dry-run.cypher` re-measures inside the window.

**Subagent dispatch: SAFE.** File authoring plus `git commit`. No MCP tools required.

Both repos forbid em-dashes; hyphens only.
</cross_repo_contract>

<tasks>

<task type="auto">
  <name>Task 1: Build the one-page, per-node human review list</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/00-review-list.md (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-WORKLIST-261-ceremony.md` section 7 and its `[W-6]` per-node listing: id, name and labels for every node in the block.
    - `/home/jsagi/dev/MindrianOS-Plugin/.planning/phases/261-enrichment-ceremony-single-admin-window/261-RESEARCH.md`: "What is in the relabel block" (the named content), the TRIZ correction, and the expectation reset section in full.
    - `/home/jsagi/dev/MindrianOS-Plugin/.planning/2026-08-20-BRIEF-complete-system-loop.md`: the 2026-02-05 precedent paragraph, verbatim.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/SCHEMA.md` sections 1 and 2: which labels are current, which are DEPRECATED (`Stage`, `CreativeWork`, `Phrase`, `Insight`, `__Entity__`), and what a Tier 1 `:Framework` is expected to carry.
    - `/home/jsagi/dev/MindrianOS-Plugin/data/flagship-floor-set.json`: the ratified 28. Several block members share names with ratified entries, which is directly relevant to the EXCLUDE reasoning below.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-WORKLIST-261-ceremony.md` section 2: which ratified names already resolve to a live `:Framework` node.
  </read_first>

  <action>
Write `00-review-list.md`. It is a review artifact for a human to read top to bottom in one sitting,
so keep it to one table plus short prose. One row per node in the block:
`id | name | current labels | proposed labels | verdict | reason`.

`verdict` is `INCLUDE` (restore `:Framework`, drop `:Archived`) or `EXCLUDE` (leave as is). Apply
these rules mechanically and state which rule fired in the `reason` column:

- INCLUDE if the node's name matches a framework named in `261-RESEARCH.md`'s block content list
  AND the node carries a non-empty description. These are the core methodology restorations.
- EXCLUDE if restoring `:Framework` would create a SECOND `:Framework` node for a name that already
  resolves to a live one. This is the important rule and it is easy to miss: several block members
  share names with the ratified 28 (`Six Thinking Hats`, `MECE`, `The Pyramid Principle`,
  `Four Lenses of Innovation`, `Jobs to Be Done`, `Red Teaming`), and those names already resolve to
  live nodes per the worklist's section 2. Restoring the archived twin would take a name from
  exactly-1 to 2 matches and BREAK the floor gate for a framework that currently passes. Cross-check
  every row against section 2 and mark these `EXCLUDE, would create a second match for a ratified
  name` with the live node's id in the reason. This rule alone is why a bulk relabel of the block
  would have been a net loss.
- EXCLUDE if the node carries a DEPRECATED label per `SCHEMA.md` section 1 and restoring
  `:Framework` on top of it would produce a chimera. Note the deprecated label in the reason.
- EXCLUDE if the node has no description, because an undescribed `:Framework` is an ungrounded one
  and SEED-B's doctrine (an ungrounded framework yields an honest cannot-grade) means it adds a
  liability rather than a capability.
- EXCLUDE with a `CARD REQUIRED` marker for any row the rules above leave genuinely ambiguous, and
  say what the ambiguity is. Do not resolve it here.

TRIZ, id 28666, gets its own row like every other node. Immediately below the table add a short
`## TRIZ` note recording the Phase 256 correction: it is not an uncurated stub, it is
`[Archived, Method, Concept]` with a real description, and it is one row of this block rather than
separate work.

Then `## Counts`: total rows, INCLUDE, EXCLUDE, and the EXCLUDE breakdown by rule. Then
`## Why this is a list and not a script`, quoting the 2026-02-05 precedent paragraph, two sentences
of your own, and nothing more.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && R=payloads/archived-block-relabel-2026-08-21/00-review-list.md && test -f "$R" && grep -q '28666' "$R" && grep -q '## TRIZ' "$R" && grep -q '## Counts' "$R" && grep -q 'EXCLUDE' "$R" && grep -q '2026-02-05' "$R" && ! grep -Pq '\x{2014}' "$R"</automated>
  </verify>

  <acceptance_criteria>
    - `00-review-list.md` has one table row per node the worklist's `[W-6]` listed, and `## Counts` states a total that matches the table's row count.
    - Every row carries a verdict of `INCLUDE` or `EXCLUDE` and a reason naming which rule fired. A blank reason fails this criterion.
    - Every block member whose name matches a ratified floor name that ALREADY resolves to a live `:Framework` node is marked `EXCLUDE` with the live node's id in the reason. Verify this by cross-checking every ratified name from `data/flagship-floor-set.json` against the table and recording the cross-check in the plan summary.
    - Rows with a DEPRECATED label per `SCHEMA.md` are EXCLUDEd with the label named.
    - Rows without a description are EXCLUDEd with that reason.
    - Any genuinely ambiguous row is marked `CARD REQUIRED` and its ambiguity is stated; it is not resolved in this task.
    - Id 28666 appears as an ordinary row, and a `## TRIZ` note records the Phase 256 correction.
    - `## Counts` gives the EXCLUDE breakdown by rule, so a reader can see which rule did the most work.
    - `## Why this is a list and not a script` quotes the 2026-02-05 precedent.
    - `grep -Pn '\x{2014}' 00-review-list.md` exits 1 with no output.
  </acceptance_criteria>

  <done>
    A one-page, per-node review list exists with an explicit verdict and reason on every row, and
    the rule that would have made a bulk relabel actively harmful is applied and visible.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the guarded relabel payload directory</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/01-relabel-block.cypher (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/90-dry-run.cypher (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/91-verify.cypher (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/99-undo.cypher (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/manifest.json (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/README.md (new)
  </files>

  <read_first>
    - Task 1's `00-review-list.md` INCLUDE rows.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/order-collision-dishare-2026-08-20/` all nine files: the directory shape and the numbered-statement convention.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/relabel-fix-260820/01-demote-poverty.cypher`: this repo's existing label-change statement shape and its guard style. This plan's statement is the inverse operation and should mirror it.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/relabel-fix-260820/99-undo.cypher` and its comment: the honest note that a label change is NOT reversible from a `batch_id` alone.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/http/admin-tools.mjs` lines 363-380: `brain_write` takes cypher, params and dryRun, and runs through `runIngestTx`.
    - `/home/jsagi/dev/MindrianOS-Plugin/.planning/phases/261-enrichment-ceremony-single-admin-window/261-RESEARCH.md` the expectation-reset section, for the README's prediction paragraph.
  </read_first>

  <action>
Author six files in `payloads/archived-block-relabel-2026-08-21/`, `batch_id`
`pws-blockrelabel-2026-08-21`.

`01-relabel-block.cypher`. One statement, parameterised. `UNWIND $rows AS row`, then
`MATCH (n)` with a `WHERE` requiring `id(n) = row.id` AND `n.name = row.name` AND `n:Archived`,
then `REMOVE n:Archived`, `SET n:Framework`, and set `batch_id`, `created_by` and `created_at` from
params. `RETURN count(n) AS applied`. The row list travels in `params`, never inlined. Header
comment states the INCLUDE count, the batch id, and the reason the `n:Archived` precondition is in
the `WHERE`: if a node has already been un-archived by another writer between authoring and window
time, this statement must skip it rather than re-apply and re-stamp it.

`90-dry-run.cypher`, read-only, numbered:
- `[90.1]` count of INCLUDE ids resolving on id AND name AND still carrying `:Archived`. Must equal
  the INCLUDE count; a shortfall is a stale row.
- `[90.2]` count of INCLUDE ids that already carry `:Framework`. Must be 0.
- `[90.3]` THE SAFETY CHECK. For each INCLUDE row's name, the count of nodes that ALREADY carry
  `:Framework` with that name, case-insensitively. Must be 0 for every row. A non-zero result means
  restoring this node would create a second match for a name that already resolves, and that row
  must be pulled before the commit. Task 1's EXCLUDE rule should already have caught these; this
  check is what proves the rule was applied correctly against the live graph rather than against a
  document.
- `[90.4]` count of nodes already carrying `batch_id = 'pws-blockrelabel-2026-08-21'`, expected 0.
- `[90.5]` the current `:Framework` total, so the window records a before value.
- `[90.6]` the current count of `USES_FRAMEWORK` edges whose target carries `:Archived`. Per
  `261-RESEARCH.md` this is 0 today, and it is the number the expectation reset rests on. Recording
  it before the relabel is what makes the at-most-11-edges prediction checkable afterwards.

`91-verify.cypher`, read-only, numbered:
- `[91.1]` count of nodes carrying the batch id, the measured `node_count` for the
  `GraphWriteEvent`.
- `[91.2]` the `:Framework` total after, compared against `[90.5]` plus `[91.1]`.
- `[91.3]` a negative control: for every INCLUDE row's name, the count of `:Framework` nodes with
  that name must be exactly 1. A 2 means the safety check was bypassed and a passing floor row has
  just been broken.
- `[91.4]` the `USES_FRAMEWORK` edge total, to be compared against a before value, so the
  at-most-11 prediction is measured rather than assumed.
- `[91.5]` count of nodes still carrying both `:Archived` and `:Framework`, expected 0.

`99-undo.cypher`. Reverse by `batch_id`: `REMOVE n:Framework`, `SET n:Archived`, and remove the
three provenance properties, for nodes carrying this batch. Include the honest comment the existing
relabel payload's undo already carries: a label restore keyed on `batch_id` works here only because
the batch stamp is written in the same statement as the label change, so every stamped node is
exactly a node this batch relabelled. Say that, so a reader knows why this undo IS reliable when the
neighbouring payload's is not.

`manifest.json`: `batch_id: "pws-blockrelabel-2026-08-21"`, `compile_only: true`,
`review_required: true`, `not_executed: true`, a `source` block naming `00-review-list.md`, the
worklist and `261-RESEARCH.md`, `statement_counts`, `edge_vocabulary_used: []` with a note that this
batch mints no edges, an `invariants` block (`node_creation: false`, `node_deletion: false`,
`edges_touched: false`, `every_touched_node_carries_batch_id: true`, `idempotent`, `undo`), and an
`unresolved_residue` object carrying every `CARD REQUIRED` row from Task 1.

`README.md`. Four things, in this order:
1. What the batch does and its INCLUDE and EXCLUDE counts.
2. **The expectation reset**, restated as this batch's own predicted outcome: this relabel recovers
   at most 11 `USES_FRAMEWORK` edges and rescues ZERO of the zero-framework commands, because zero
   `USES_FRAMEWORK` edges currently target an archived node. A flat command-coverage number after
   this batch is the prediction holding, not the remediation failing. Cite `261-RESEARCH.md`.
3. **The separate-operation ruling**: this batch has its own `batch_id`, its own `GraphWriteEvent`
   and its own `GRAPH-WRITE-LOG` row, distinct from plan 261-10's edge authoring, and a read-tier
   probe runs between the two inside the window. State that this is the answer to
   `261-RESEARCH.md`'s open question 1, and that combining them would destroy Phase 262's ability to
   attribute floor movement.
4. **The dead-end note**: the root-cause hunt for what demoted this block is a confirmed dead end
   (neither repo's git history reaches back to 2026-02-05); an Aura console audit log is the only
   remaining possibility and may not exist. Do not re-run the hunt.
Plus empty `## Dry-run recompile`, `## Navigator approval` and `## Commit log` sections.

Commit locally. Do not push. Do not execute.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && P=payloads/archived-block-relabel-2026-08-21 && for f in 00-review-list.md 01-relabel-block.cypher 90-dry-run.cypher 91-verify.cypher 99-undo.cypher manifest.json README.md; do test -f "$P/$f" || exit 1; done && node -e "const m=require('./payloads/archived-block-relabel-2026-08-21/manifest.json'); if(m.compile_only!==true||m.not_executed!==true||m.batch_id!=='pws-blockrelabel-2026-08-21') process.exit(1)" && grep -v '^//' "$P/01-relabel-block.cypher" | grep -q 'n.name = row.name' && grep -v '^--' "$P/90-dry-run.cypher" | grep -cE '\b(SET|MERGE|CREATE|DELETE|REMOVE)\b' | grep -q '^0$' && grep -q 'at most 11' "$P/README.md"</automated>
  </verify>

  <acceptance_criteria>
    - All seven files exist in `payloads/archived-block-relabel-2026-08-21/`.
    - `01-relabel-block.cypher` is ONE parameterised statement whose `WHERE` requires `id(n) = row.id`, `n.name = row.name` AND `n:Archived`. Missing any of the three fails this criterion.
    - The statement removes `:Archived`, sets `:Framework`, stamps the three provenance properties, and inlines no ids or names.
    - `90-dry-run.cypher` contains six numbered read-only checks including `[90.3]`'s name-collision safety check and `[90.6]`'s archived-target edge count, and contains zero write clauses.
    - `91-verify.cypher` contains `[91.3]`'s exactly-1-per-name negative control and `[91.5]`'s no-chimera check.
    - `99-undo.cypher` restores by batch and explains in a comment why a batch-keyed label restore is reliable in this specific case.
    - `manifest.json` parses, is `compile_only` and `not_executed`, declares `edges_touched: false`, and carries every `CARD REQUIRED` row in `unresolved_residue`.
    - `README.md` contains all four required sections. Its expectation-reset section states the at-most-11-edges and rescues-zero-commands prediction verbatim in substance and cites `261-RESEARCH.md`.
    - `README.md` states the separate-batch ruling and names it as the answer to open question 1.
    - `README.md` records the root-cause dead end so it is not re-run.
    - `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain log HEAD..origin/main --oneline` is empty.
    - `grep -RPn '\x{2014}' payloads/archived-block-relabel-2026-08-21/` exits 1 with no output.
  </acceptance_criteria>

  <done>
    The Gate 0 block relabel exists as a reviewed per-node list plus one guarded statement, with a
    live name-collision safety check that can pull a row before the commit, an honest prediction of
    its own limited effect, and a reliable undo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an archived twin to a live canonical | restoring `:Framework` on a duplicate name breaks a floor row that currently passes |
| a document-derived EXCLUDE rule to the live graph | a rule applied against a worklist must be re-proved against canon before the write |
| a relabel to a product metric | the relabel is visible and satisfying and moves almost nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-261-44 | Denial of Service | restoring an archived twin of a ratified name, taking it from 1 match to 2 and breaking a passing floor row | mitigate | Task 1's EXCLUDE rule, `[90.3]`'s live per-name collision count expecting 0, and `[91.3]`'s exactly-1 negative control after |
| T-261-45 | Tampering | a bulk relabel of the whole block without a reviewed list, repeating the 2026-02-05 disaster | mitigate | `00-review-list.md` is a per-node artifact with a verdict and reason per row; the navigator card in plan 261-12 signs it off before any write |
| T-261-46 | Repudiation | the relabel and the edge authoring landing as one operation, making Phase 262's movement unattributable | mitigate | separate `batch_id`, separate `GraphWriteEvent`, separate `GRAPH-WRITE-LOG` row, and a read-tier probe between them inside the window |
| T-261-47 | Repudiation | a flat command-coverage number after the relabel read as the remediation failing | mitigate | the README states the at-most-11 and rescues-zero prediction before the write, and `[90.6]`/`[91.4]` measure it rather than asserting it |
| T-261-48 | Tampering | re-applying and re-stamping a node another writer already un-archived | mitigate | the `n:Archived` precondition in the statement's `WHERE`, plus `[90.2]` expecting zero already-Framework rows |
| T-261-49 | Tampering | an undescribed node restored to `:Framework`, creating an ungrounded framework | mitigate | Task 1's no-description EXCLUDE rule, grounded in SEED-B's honest-cannot-grade doctrine |
| T-261-SC | Tampering | npm/pip/cargo installs | accept | not applicable: this plan installs nothing |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

New in THIS plan, all under
`/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/archived-block-relabel-2026-08-21/`:
`00-review-list.md`, `01-relabel-block.cypher`, `90-dry-run.cypher`, `91-verify.cypher`,
`99-undo.cypher`, `manifest.json`, `README.md`.

Graph state this payload will change WHEN THE WINDOW RUNS IT (not here): the INCLUDE rows lose
`:Archived`, gain `:Framework`, and carry `batch_id` `pws-blockrelabel-2026-08-21`. No node is
created or deleted and no edge is touched.

No MindrianOS-Plugin file is modified; `data/flagship-floor-set.json` and
`.planning/2026-08-20-BRIEF-complete-system-loop.md` are READ.
</artifacts_this_phase_produces>

<verification>
- The review list has a verdict and a reason on every row and the EXCLUDE rules are applied.
- The statement carries the id, name and `:Archived` triple guard and inlines nothing.
- The dry-run's per-name collision check and the verify's exactly-1 control both exist.
- The README states the expectation reset, the separate-batch ruling and the dead end.
- Nothing was executed, nothing was pushed.
</verification>

<success_criteria>
- The Gate 0 archived block is ready to restore under exactly the propose-sign-off-execute shape the
  2026-02-05 precedent demands.
- TRIZ is closed as one row rather than left open as separate work.
- The relabel's real, limited effect is predicted in writing before it runs and measured after.
- Zero em-dashes added. Zero pushes.
</success_criteria>

<output>
Create `.planning/phases/261-enrichment-ceremony-single-admin-window/261-09-SUMMARY.md` when done.
</output>
