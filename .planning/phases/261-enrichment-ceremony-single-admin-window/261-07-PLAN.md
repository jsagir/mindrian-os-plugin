---
phase: 261-enrichment-ceremony-single-admin-window
plan: 07
type: execute
wave: 2
depends_on: [261-01]
target_repo: ProblemsWorthSolving-Brain
files_modified:
  - ProblemsWorthSolving-Brain/payloads/pest-analysis.mjs
  - ProblemsWorthSolving-Brain/payloads/four-lenses-of-innovation.mjs
  - ProblemsWorthSolving-Brain/payloads/sapphire.mjs
  - ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/pest-analysis.json
  - ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/four-lenses-of-innovation.json
  - ProblemsWorthSolving-Brain/docs/2026-08-21-CARDS-new-nodes.md
autonomous: true
requirements: [CER-04, CER-06]

must_haves:
  truths:
    - "PEST Analysis is created as a NEW :Framework node with four HAS_STEP components and NO fabricated LEADS_TO, landing an honest 3/4 that clears the floor"
    - "Four Lenses of Innovation is authored from the navigator-supplied named source that now exists, and every node and edge it emits traces to a passage in that file"
    - "SAPPhIRE is authored but explicitly GATED: it does not execute unless the navigator approves the drafted definition, because the only source held is a one-line paraphrase"
    - "No payload in this plan fabricates a sequence for a framework whose source asserts none"
    - "PEST's de-list alternative is named as a separate ratification ceremony with its own artifact, never a silent denominator edit"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/four-lenses-of-innovation.mjs"
      provides: "Four Lenses payload sourced entirely from docs/2026-08-21-SOURCE-four-lenses-of-innovation.md"
      min_lines: 80
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-CARDS-new-nodes.md"
      provides: "Three paste-ready cards: PEST shape confirmation, Four Lenses structure choice, SAPPhIRE definition approval or rejection"
      contains: "SAPPhIRE"
  key_links:
    - from: "payloads/four-lenses-of-innovation.mjs"
      to: "ProblemsWorthSolving-Brain/docs/2026-08-21-SOURCE-four-lenses-of-innovation.md"
      via: "SOURCE header naming the in-repo source file, plus per-node quote citations"
      pattern: "2026-08-21-SOURCE-four-lenses"
    - from: "payloads/sapphire.mjs"
      to: "docs/2026-08-21-CARDS-new-nodes.md SAPPhIRE section"
      via: "GATED header block stating the payload does not run without an explicit approval token"
      pattern: "GATED"
---

<objective>
Author the three net-new node payloads this phase creates: CER-04's PEST Analysis, CER-06's Four
Lenses of Innovation, and the SAPPhIRE node the roadmap absorbed from the retired Phase 256.

All three are different from every other payload in this phase because the target node does not
exist. PEST measured ZERO name matches on 2026-08-21. Four Lenses measured 0/4 with a node present
but empty. SAPPhIRE is confirmed absent by `261-RESEARCH.md`. A creation payload cannot lean on an
existing node's props, so every field it writes is a field it asserts, and the source discipline
has to be tighter, not looser.

**CER-04, PEST Analysis. The ruling is already recorded; this plan implements it.**
`.planning/research/FEATURES.md` section (c) rules INGEST, not de-list, and the requirement text
carries the shape: source `references/methodology/macro-trends.md` Phase 3, new node, four
`HAS_STEP` components (Political, Economic, Social, Technological), NO fabricated `LEADS_TO`,
honest 3/4 clears the floor. The one thing this plan must not do is quietly improve on that ruling
by adding a fifth component or a sequence to reach 4/4.

**CER-06, Four Lenses of Innovation. Un-deferred 2026-08-21 because the source now exists.**
The requirement's history matters and the plan should carry it: the original "ruling recorded at
requirements time" was investigated and found never to have existed anywhere, and the navigator
then supplied the real named source directly. It is
`ProblemsWorthSolving-Brain/docs/2026-08-21-SOURCE-four-lenses-of-innovation.md` (Rowan Gibson,
*The Four Lenses of Innovation*: the book, the core thesis, all four lenses with definitions,
applications and representative quotes, a process note, a navigator-prepared term dictionary with
PWS relevancy annotations, and a PWS-methodology relevancy mapping). That file also states plainly
what it does NOT decide: whether this becomes a Tier 1 `:Framework` with full structure or
something lighter, the exact node and edge shape, and whether the term dictionary's entries become
separate graph entities or stay descriptive text. Those are this plan's decisions to PROPOSE and
the window's card to RULE.

**SAPPhIRE. Authored but gated, and the gate is real.** The roadmap absorbed
"SAPPhIRE-creation / TRIZ-promotion" from retired Phase 256. TRIZ is not a stub and is handled by
plan 261-09's archived-block relabel. SAPPhIRE genuinely is absent. But the only source this
project holds is a single paraphrased sentence in
`/home/jsagi/dev/MindrianOS-Plugin/.planning/2026-08-20-BRIEF-complete-system-loop.md` line 141
(Chakrabarti et al. 2005, the State-Action-Parts-Phenomena-Physics-Input-oRgan-Effect causal
chain). One paraphrased sentence naming a citation is not the same as a source document read in
full, and the payload template's own rule does not bend for a framework we would like to have. So
the payload is authored to the drafted definition, marked `GATED`, and does not execute unless the
navigator approves it on a card that states exactly how thin the source is.

Output: three payloads, two fixtures, one card document. Committed locally, never executed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/261-enrichment-ceremony-single-admin-window/261-RESEARCH.md
@.planning/research/FEATURES.md
@.planning/REQUIREMENTS.md
</context>

<cross_repo_contract>
| Concern | Repo | Absolute path |
|---|---|---|
| This PLAN.md and all GSD tracking | MindrianOS-Plugin | `/home/jsagi/dev/MindrianOS-Plugin/.planning/` (gitignored, `git add -f`) |
| Every file this plan creates | ProblemsWorthSolving-Brain | `/home/jsagi/dev/ProblemsWorthSolving-Brain/` |
| The CER-06 source, already in the target repo | ProblemsWorthSolving-Brain | `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-SOURCE-four-lenses-of-innovation.md` |
| The PEST source and the SAPPhIRE draft | MindrianOS-Plugin | `references/methodology/macro-trends.md`, `.planning/2026-08-20-BRIEF-complete-system-loop.md` |

**PUSH FREEZE, NON-NEGOTIABLE.** `ProblemsWorthSolving-Brain` is frozen by
`docs/2026-08-20-FREEZE-push-freeze-before-261.md` until THIS phase's admin window closes and its
`GRAPH-WRITE-LOG.md` row is appended. Commit locally, NEVER `git push`. Note that commit `b81f3b5`
(the CER-06 source document) is ALREADY sitting unpushed under this freeze, which is correct.
Plan 261-13 is the only plan permitted to push.

**No admin window is opened or required, and no graph call is made.**

**Subagent dispatch: SAFE.** File authoring, `node --test`, `git commit`. No MCP tools required.

Both repos forbid em-dashes; hyphens only.
</cross_repo_contract>

<tasks>

<task type="auto">
  <name>Task 1: Author the PEST and Four Lenses fixtures from their sources</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/pest-analysis.json (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/four-lenses-of-innovation.json (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/reverse-salient-analysis.json`: the schema.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-framework-evals.mjs` lines 1-60: the content-check contract, and specifically that asserting NO chain exists is a supported and expected assertion for a non-linear framework.
    - `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/macro-trends.md` IN FULL, with attention to "Phase 3: PEST Systems Analysis" and its Political / Economic / Social / Technological subsections, and to whether the document asserts any order among the four.
    - `/home/jsagi/dev/MindrianOS-Plugin/commands/macro-trends.md` frontmatter: confirm `frameworks:` names `PEST Analysis` and record the `uses` count.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-SOURCE-four-lenses-of-innovation.md` IN FULL, all 113 lines: the four lenses, the process note at lines 63-69, the term dictionary at 71-92, the relevancy mapping at 94-101, and the "Explicitly NOT decided by this file" section at 103-113.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-WORKLIST-261-ceremony.md` sections 2 and 3: PEST's live match count and Four Lenses' live readiness.
    - `/home/jsagi/dev/MindrianOS-Plugin/data/flagship-floor-set.json`: the exact ratified strings `PEST Analysis` and `Four Lenses of Innovation`.
  </read_first>

  <action>
Write two fixtures using the schema and the rules from plan 261-03 Task 1.

`pest-analysis.json`. The canonical name is exactly `PEST Analysis` as `flagship-floor-set.json`
spells it. The structure member set is exactly four: Political, Economic, Social, Technological, in
the source's own spelling. The flow assertion is that NO `LEADS_TO` chain exists, asserted
explicitly. The expected readiness is 3/4 with the vector `pattern_type 1, structure 1,
techniques 0, flow 0` if the source names no distinct technique, or `techniques 1` if it does; read
the document and state which. Negative controls: one fabricated fifth component (for example
`Legal` or `Environmental`, which belong to PESTLE and not to what this source documents) asserted
ABSENT, and one wrong canonical name asserted not returned. The `Legal` control is the sharp one:
a model that knows PESTLE will want to add those letters, and this fixture is what catches it.

`four-lenses-of-innovation.json`. The canonical name is exactly `Four Lenses of Innovation`. The
structure member set is the four lenses as the source names them: Challenging Orthodoxies,
Harnessing Trends, Leveraging Resources, Understanding Needs. The flow assertion follows the source
at lines 65-67, which states Gibson frames applying the lenses TOGETHER rather than sequentially;
so the fixture asserts NO `LEADS_TO` chain. Expected readiness follows honestly from that. Negative
controls: one asserting that a member of the DIFFERENT four lenses named at
`references/methodology/systems-thinking.md` line 158 (Structure, Dynamics, Boundaries, Leverage)
is ABSENT, because that name collision is exactly why this framework was blocked for weeks; and one
asserting that a term-dictionary entry such as `Functional Fixedness` is NOT a structure member,
which pins the decision that dictionary terms do not become structure components.

Run `node --test tests/eval-framework-structure.test.mjs` and confirm the discovered-fixture count
rose by exactly 2. Do NOT run the live probe leg.

Do NOT write a SAPPhIRE fixture. A fixture asserts what the graph must look like, and whether
SAPPhIRE should exist at all is the navigator's call. Writing its fixture now would encode the
answer. Note this deliberate omission in the plan summary so a later reader does not read it as an
oversight.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && for f in pest-analysis four-lenses-of-innovation; do node -e "const j=require('./tests/fixtures/framework-evals/$f.json'); if(!j.source) process.exit(1)" || exit 1; done && test ! -f tests/fixtures/framework-evals/sapphire.json && node --test tests/eval-framework-structure.test.mjs</automated>
  </verify>

  <acceptance_criteria>
    - Two fixtures exist and parse; each `source` names a real path with a char count and a read date.
    - `pest-analysis.json` asserts exactly four structure members, asserts that NO `LEADS_TO` chain exists, and carries a negative control asserting a PESTLE-only letter (`Legal` or `Environmental`) is ABSENT.
    - `pest-analysis.json`'s canonical name is the exact string from `data/flagship-floor-set.json`.
    - `four-lenses-of-innovation.json` asserts the four Gibson lenses as its member set, asserts NO `LEADS_TO` chain citing the source's process note, and carries both named negative controls (a Systems Thinking "four lenses" member absent, and a term-dictionary entry not a structure member).
    - No `sapphire.json` fixture exists, and the plan summary states that omission is deliberate with the reason.
    - `node --test tests/eval-framework-structure.test.mjs` passes and the discovered-fixture count rose by exactly 2; both counts recorded.
    - The live probe leg was NOT run.
    - `grep -RPn '\x{2014}' tests/fixtures/framework-evals/` exits 1 with no output.
  </acceptance_criteria>

  <done>
    PEST and Four Lenses each have a source-derived fixture whose negative controls target the exact
    way each one is likely to be over-authored, and SAPPhIRE deliberately has none.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the three creation payloads, with SAPPhIRE gated</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/pest-analysis.mjs (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/four-lenses-of-innovation.mjs (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/sapphire.mjs (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/reverse-salient-analysis.mjs` IN FULL: the template.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/invention-disclosure.mjs` header and `framework` block: this repo's precedent for a payload that CREATES rather than enriches, and what a creation payload must carry that an enrichment does not.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/validator.mjs`: the new-node branch and what it requires (name, description, category and whatever else is mandatory for a creation). A creation payload that omits a required field is rejected at dry-run, which wastes a window slot.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ontology.mjs` line 239 and surroundings: the closed edge vocabulary. `HAS_STEP` must be confirmed present before PEST relies on it.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/SCHEMA.md` sections 1 and 3: the tier assignment for a `:Framework` node and the provenance dictionary.
    - `/home/jsagi/dev/MindrianOS-Plugin/.planning/2026-08-20-BRIEF-complete-system-loop.md` line 141: the SAPPhIRE drafted definition, in full and verbatim. It is one sentence. Read it and note exactly how much it contains.
    - Task 1's two fixtures.
  </read_first>

  <action>
Write three creation payloads following the template, with the standard header blocks (`SOURCE:`,
`SHAPE NOTE:`, `TECHNIQUE:`, `EXPECTED READINESS AFTER INGEST:`) plus what each one needs.

`pest-analysis.mjs`. A new `:Framework` node named exactly `PEST Analysis`, with four `HAS_STEP`
components and NO `LEADS_TO`. Its `SHAPE NOTE:` quotes `macro-trends.md` where it lays the four
lenses out without asserting order, and states that `pattern_type` is `matrix` or `parallel` on
that basis (pick one, justify it). Add a `DISCLOSURE:` block stating that PEST is documented as
Phase 3 WITHIN Macro-Changes Analysis rather than as a standalone document, which is the same
disclosure pattern MECE uses in plan 261-05. State the expected readiness as 3/4 and state, in
words, that 3/4 clears the `readiness >= 3` floor so no fifth component and no invented sequence is
needed or wanted. Do NOT add `Legal` or `Environmental`; this is PEST, not PESTLE, and the source
governs.

`four-lenses-of-innovation.mjs`. Source is
`ProblemsWorthSolving-Brain/docs/2026-08-21-SOURCE-four-lenses-of-innovation.md`, read in full,
cited with its line count. The node may already exist (live readiness 0/4 with a match), so check
the worklist and use the name-only `framework` block if it is live, or a full creation block if it
is not; state which case applies and cite the worklist row. Four structure components, one per
lens, each carrying the source's own definition text. NO `LEADS_TO`, citing lines 65-67. Techniques
only if the source names distinct titled techniques; the process note is a framing, not a technique,
so say so rather than promoting it. Add a `SHAPE PROPOSAL:` block that states, for each of the three
questions the source file's "Explicitly NOT decided" section leaves open, what this payload
proposes and why: Tier 1 `:Framework` versus lighter, `HAS_STEP` versus `HAS_PROCESS_STEP` versus a
flat four-part structure, and whether term-dictionary entries become graph entities (the proposal is
NO, they stay descriptive text, matching Task 1's fixture control). Add a `DISCLOSURE:` block
naming the different "four lenses" at `references/methodology/systems-thinking.md` line 158 and
stating explicitly that this payload is NOT about those, because that collision is the documented
reason this framework was blocked.

`sapphire.mjs`. Author it to the drafted definition, and put a `GATED:` block FIRST in the header,
before `SOURCE:`. That block must state: that the only source held is a single paraphrased sentence
at a named path and line, quoted in full; that no primary text of Chakrabarti et al. 2005 has been
read by anyone in this project; that this payload therefore does NOT execute unless the navigator
approves it explicitly on the card in Task 3; and that the alternative is to carry SAPPhIRE to
Phase 263's long-tail lane as a source-authoring task. Then author the node honestly: name, a
description drawn from and not exceeding the drafted sentence, and the causal-chain elements as
structure components ONLY if the drafted sentence enumerates them (it names State, Action, Parts,
Phenomena, Physics, Input, oRgan, Effect, so they are enumerable). Do not add applications, use
cases, or technique nodes; none of that is in the one sentence available. State the expected
readiness honestly and do not pad it.

Cross-check PEST and Four Lenses against their Task 1 fixtures and record the comparisons. SAPPhIRE
has no fixture by design; record that its correctness rests entirely on the card.

Do NOT run `payloads/run-ingest.mjs`; no Bolt connection to canon exists from this machine.

Commit locally. Do not push.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && for f in pest-analysis four-lenses-of-innovation sapphire; do node -e "import('./payloads/$f.mjs').then(m=>{const p=m.payload??m.default; if(!p||!p.framework||!p.framework.name||!Array.isArray(p.nodes)||!Array.isArray(p.edges)) process.exit(1);}).catch(()=>process.exit(1))" || exit 1; done && grep -q 'GATED:' payloads/sapphire.mjs && grep -q 'SHAPE PROPOSAL:' payloads/four-lenses-of-innovation.mjs && ! grep -qiE '\bPESTLE\b|"Legal"|"Environmental"' payloads/pest-analysis.mjs</automated>
  </verify>

  <acceptance_criteria>
    - All three payload modules import cleanly and export `framework.name`, `nodes` and `edges`.
    - `pest-analysis.mjs` emits exactly four structure components, emits zero `LEADS_TO` edges, and contains no `Legal` or `Environmental` component. Its header states the expected 3/4 and that 3/4 clears the floor.
    - `pest-analysis.mjs` carries a `DISCLOSURE:` block stating PEST is documented as a phase within Macro-Changes Analysis.
    - `four-lenses-of-innovation.mjs`'s `SOURCE:` names `docs/2026-08-21-SOURCE-four-lenses-of-innovation.md` with its line count and a read date, and every structure component's text traces to a passage in that file.
    - `four-lenses-of-innovation.mjs` carries a `SHAPE PROPOSAL:` block answering all three of the source file's explicitly-undecided questions with a proposal and a reason, and a `DISCLOSURE:` block naming the Systems Thinking name collision.
    - `four-lenses-of-innovation.mjs` emits zero `LEADS_TO` edges and creates no node per term-dictionary entry.
    - `sapphire.mjs`'s FIRST header block is `GATED:`, and it quotes the one-sentence source in full with its file path and line number, states no primary text has been read, states it does not execute without explicit navigator approval, and names the Phase 263 alternative.
    - `sapphire.mjs` emits no application, use-case or technique nodes, because none are in the available source.
    - Every emitted edge `type` across all three payloads appears in `src/ontology.mjs`'s closed list; `HAS_STEP` presence confirmed by grep and recorded.
    - PEST's and Four Lenses' readiness vectors match their fixtures'; corrections recorded with which side changed.
    - `payloads/run-ingest.mjs` was NOT run.
    - `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain log HEAD..origin/main --oneline` is empty.
    - `grep -RPn '\x{2014}' payloads/pest-analysis.mjs payloads/four-lenses-of-innovation.mjs payloads/sapphire.mjs` exits 1 with no output.
  </acceptance_criteria>

  <done>
    Three creation payloads exist. PEST implements a recorded ruling without improving on it, Four
    Lenses is grounded entirely in the source the navigator supplied, and SAPPhIRE carries its own
    gate in its first header block.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the new-node card document</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-CARDS-new-nodes.md (new)
  </files>

  <read_first>
    - The three payload headers from Task 2, in particular `SHAPE PROPOSAL:` and `GATED:`.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-CARDS-cohort2-batch-a.md`: match its section shape.
    - `/home/jsagi/dev/MindrianOS-Plugin/.planning/research/FEATURES.md` lines 139-160: the PEST ruling in full, including the de-list alternative's required artifact (an updated `flagship-floor-set.json` with 27 names, a new `ratified_at` and a reason field, PLUS re-attribution of `commands/macro-trends.md` frontmatter, never a silent denominator edit).
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-SOURCE-four-lenses-of-innovation.md` lines 103-113: the three questions this card must put.
    - `/home/jsagi/dev/MindrianOS-Plugin/.planning/REQUIREMENTS.md` CER-06's full text, including the recorded history that the original ruling never existed.
  </read_first>

  <action>
Write `docs/2026-08-21-CARDS-new-nodes.md` in the same shape as the Cohort 2 card documents: an
opening naming what these three share (each creates a node that does not exist, so approval is
approval to add something to canon rather than to change something), then one section per
framework, then `## Reply format` and `## What happens on reject`.

**PEST section, a confirmation card.** Present the recorded ruling (INGEST, not de-list), the shape
(four `HAS_STEP`, no `LEADS_TO`, honest 3/4), and ask only for confirmation. State that de-listing
remains available but is a separate ratification ceremony requiring an updated
`flagship-floor-set.json` with a new `ratified_at` and a reason field PLUS re-attribution of
`commands/macro-trends.md` frontmatter. Name it as available and expensive, not as forbidden. State
that a confirmation flips the floor row from `no node` to PASS.

**Four Lenses section, a shape card with three numbered questions**, taken verbatim from the source
file's "Explicitly NOT decided" list: Tier 1 `:Framework` with full structure or something lighter;
the exact structure edge type; and whether term-dictionary entries become separate graph entities or
stay descriptive text. Give the payload's proposal and reason per question, plus the alternatives.
Add a one-paragraph history note: CER-06 was deferred because no source existed, the search for the
"ruling recorded at requirements time" found it never existed, and the navigator then supplied the
real source. That history is why this card exists and it should be one paragraph, not a footnote.

**SAPPhIRE section, an approval card with a genuinely available no.** Quote the one-sentence source
in full with its path and line. State in the card's own words that this is the thinnest source
behind any payload in this phase, that no primary text has been read, and that the project's
standing rule is "NO named read source = NO payload". Give three options: approve the drafted
definition as written; approve with navigator-supplied corrections pasted into the reply; or reject
and carry SAPPhIRE to Phase 263 as a source-authoring task. State that the third option costs
`/mos:find-analogies` nothing it has today, because SAPPhIRE does not currently exist, so rejecting
leaves the surface exactly as it is rather than degrading it. Make the reject branch as easy to
choose as the approve branch; the whole point of a gate is that it can close.

`## Reply format` gives one line answering all three. `## What happens on reject` names the
destination per framework.

Cross-link the card from each payload header with a one-line pointer.

Commit locally. Do not push.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && C=docs/2026-08-21-CARDS-new-nodes.md && test -f "$C" && grep -q '## Reply format' "$C" && grep -q '## What happens on reject' "$C" && grep -q 'SAPPhIRE' "$C" && grep -qi 'ratified_at' "$C" && for f in pest-analysis four-lenses-of-innovation sapphire; do grep -q '2026-08-21-CARDS-new-nodes' payloads/$f.mjs || exit 1; done && ! grep -Pq '\x{2014}' "$C"</automated>
  </verify>

  <acceptance_criteria>
    - `docs/2026-08-21-CARDS-new-nodes.md` exists with three sections, a `## Reply format` line covering all three, and a `## What happens on reject` section naming a destination per framework.
    - The PEST section states the de-list alternative's full required artifact including `ratified_at`, a reason field, and command frontmatter re-attribution, and names it available rather than forbidden.
    - The Four Lenses section puts all three of the source file's undecided questions as numbered items, each with the payload's proposal, its reason, and the alternatives, plus the one-paragraph CER-06 history.
    - The SAPPhIRE section quotes the one-sentence source in full with path and line, states that no primary text has been read, offers three options including a plain reject, and states that rejecting leaves `/mos:find-analogies` exactly as it is today.
    - All three payload headers carry a one-line pointer to this card document.
    - `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain log HEAD..origin/main --oneline` is empty.
    - `grep -Pn '\x{2014}' docs/2026-08-21-CARDS-new-nodes.md` exits 1 with no output.
  </acceptance_criteria>

  <done>
    Three creation decisions are on one card with real alternatives, a costed de-list path, the
    CER-06 history recorded, and a SAPPhIRE gate that is genuinely closable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a citation to a source | naming Chakrabarti et al. 2005 is not the same as having read it |
| model knowledge to document content | PESTLE is well known; this source documents PEST, and the difference is two fabricated nodes |
| a name that appears twice in the corpus | two different "four lenses" exist and conflating them is the documented reason CER-06 stalled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-261-32 | Spoofing | `Legal` and `Environmental` added to PEST from model knowledge of PESTLE | mitigate | fixture negative control asserting a PESTLE-only letter is ABSENT, plus an acceptance grep forbidding those strings in the payload |
| T-261-33 | Spoofing | SAPPhIRE authored beyond its one-sentence source | mitigate | the `GATED:` block is first in the header and quotes the sentence in full; the payload emits no application or technique nodes; the card's reject branch is stated as costless |
| T-261-34 | Spoofing | Systems Thinking's Structure/Dynamics/Boundaries/Leverage authored as Four Lenses components | mitigate | fixture negative control asserting one of those is ABSENT, plus a mandatory `DISCLOSURE:` naming the collision and its file and line |
| T-261-35 | Tampering | a fifth PEST component or an invented sequence added to reach 4/4 | mitigate | the fixture asserts exactly four members and no chain; the payload header states 3/4 clears the floor so no lift is wanted |
| T-261-36 | Tampering | the ratified floor denominator quietly reduced to make PEST pass | mitigate | the card names de-listing as a separate ratification ceremony with a named artifact set; no plan in this phase edits `flagship-floor-set.json` |
| T-261-37 | Repudiation | a creation payload rejected at dry-run for a missing required field, burning a window slot | mitigate | `src/ingest/validator.mjs`'s new-node branch is read first and its required fields are supplied |
| T-261-SC | Tampering | npm/pip/cargo installs | accept | not applicable: this plan installs nothing |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

New in THIS plan, under `/home/jsagi/dev/ProblemsWorthSolving-Brain`:
`payloads/pest-analysis.mjs`, `payloads/four-lenses-of-innovation.mjs`, `payloads/sapphire.mjs`,
`tests/fixtures/framework-evals/pest-analysis.json`,
`tests/fixtures/framework-evals/four-lenses-of-innovation.json`,
`docs/2026-08-21-CARDS-new-nodes.md`.

Deliberately NOT created: `tests/fixtures/framework-evals/sapphire.json`, because a fixture would
encode an answer the navigator has not given.

New graph state these payloads will create WHEN THE WINDOW RUNS THEM (not here, and SAPPhIRE only
on approval): a `PEST Analysis` `:Framework` node with four `HAS_STEP` components; Four Lenses
structure components on the existing or new `Four Lenses of Innovation` node; and, gated, a
`SAPPhIRE` `:Framework` node.

No MindrianOS-Plugin file is modified; `references/methodology/macro-trends.md`,
`commands/macro-trends.md` and `.planning/2026-08-20-BRIEF-complete-system-loop.md` are READ.
`data/flagship-floor-set.json` is READ and never edited.
</artifacts_this_phase_produces>

<verification>
- Two fixtures parse and each carries the negative control targeting its framework's specific
  over-authoring risk.
- Three payloads import; PEST has four components and no chain; Four Lenses has a `SHAPE PROPOSAL:`
  answering all three open questions; SAPPhIRE leads with `GATED:`.
- The card document carries a costed de-list path, the CER-06 history, and a closable SAPPhIRE gate.
- Nothing was ingested, nothing was pushed.
</verification>

<success_criteria>
- CER-04 and CER-06 are authored to their recorded rulings and their named sources, with nothing
  added beyond what the sources support.
- The SAPPhIRE absorption is honored without inventing a framework from a citation.
- Zero em-dashes added. Zero pushes.
</success_criteria>

<output>
Create `.planning/phases/261-enrichment-ceremony-single-admin-window/261-07-SUMMARY.md` when done.
</output>
