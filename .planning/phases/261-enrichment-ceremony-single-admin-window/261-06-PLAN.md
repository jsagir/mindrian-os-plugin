---
phase: 261-enrichment-ceremony-single-admin-window
plan: 06
type: execute
wave: 2
depends_on: [261-01]
target_repo: ProblemsWorthSolving-Brain
files_modified:
  - ProblemsWorthSolving-Brain/payloads/triple-validation-compass.mjs
  - ProblemsWorthSolving-Brain/payloads/hypothesis-driven-problem-solving.mjs
  - ProblemsWorthSolving-Brain/payloads/adoption-capacity-theory.mjs
  - ProblemsWorthSolving-Brain/payloads/mullins-seven-domains.mjs
  - ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/triple-validation-compass.json
  - ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/hypothesis-driven-problem-solving.json
  - ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/adoption-capacity-theory.json
  - ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/mullins-model.json
  - ProblemsWorthSolving-Brain/docs/2026-08-21-CARDS-cohort2-batch-b.md
autonomous: true
requirements: [CER-03]

must_haves:
  truths:
    - "The Triple Validation Compass source-attribution ruling that CER-03 names by requirement text exists as a written, paste-ready card with named options and a double-attribution question"
    - "No payload in this plan asserts a framework name that appears nowhere in any source document without disclosing that fact in its header"
    - "Adoption-Capacity Theory does not claim analyze-timing.md's six phases, because those phases belong to S-Curve Analysis, which is already a live ratified framework"
    - "The already-authored mullins-seven-domains.mjs is verified against the live Mullins Model node and its ruling before it is scheduled to run"
    - "Every honest ceiling below 4/4 is stated with its missing dimension and its floor consequence"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-CARDS-cohort2-batch-b.md"
      provides: "Four paste-ready navigator cards including the named Triple Validation Compass source-attribution ruling"
      contains: "Triple Validation Compass"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/adoption-capacity-theory.mjs"
      provides: "Adoption-Capacity Theory payload that does not claim S-Curve Analysis's structure"
      min_lines: 70
  key_links:
    - from: "payloads/triple-validation-compass.mjs header"
      to: "payloads that also ground on the Three Gates"
      via: "double-attribution disclosure naming the other claimant by payload path"
      pattern: "Three Gates"
    - from: "docs/2026-08-21-CARDS-cohort2-batch-b.md"
      to: "261-12-PLAN.md task 5"
      via: "the window's Cohort 2 checkpoint pastes these cards verbatim"
      pattern: "261-12"
---

<objective>
Author CER-03 Cohort 2, batch B: three judgment payloads, one reuse verification, and the four
navigator cards they require, including the Triple Validation Compass source-attribution ruling
that CER-03's requirement text names explicitly.

| Framework | Live readiness | Source | The ruling it needs |
|---|---|---|---|
| PWS Triple Validation Compass | 2/4 (st 1, fl 1) | PARTIAL: `references/methodology/grade.md` (~8,209) plus `value-proposition.md`'s Three Gates | NO document anywhere names "Triple Validation Compass". Which doc is authoritative, and can the Three Gates ground this framework when PWS Value Proposition's own content also grounds on them? |
| Hypothesis-Driven Problem Solving | 2/4 (st 1, fl 1) | PARTIAL: `commands/research.md` (~25,520, 7 pipeline stages) | The command is a research/evidence pipeline; hypothesis-driven methodology is implicit in its stage flow and never stated as a framework. Author stages-as-structure with disclosure, or hold for a real source doc? |
| Adoption-Capacity Theory | 2/4 (st 1, fl 1) | PARTIAL: `references/methodology/analyze-timing.md` (~7,072, titled "S-Curve Analysis"), `commands/diffusion.md` (~4,663), `lib/core/sensors/sensor-diffusion-adoption.cjs` | The weakest documented framework with live commands. `analyze-timing.md`'s six phases belong to S-Curve Analysis, already ratified and live at 3/4. Thin honest payload, or write the missing reference doc first? |
| Mullins Model | 2/4 (st 1, te 1) | `references/methodology/mullins-7-domains.md` (~4,813). Payload ALREADY EXISTS as `payloads/mullins-seven-domains.mjs`, unexecuted, commit `aa15966` | 7 domains in 3 groups plus scoring and decision rules, NOT sequential phases. Confirm the existing payload matches that shape and lands 3/4 honestly with `flow` unsupported |

The one that matters most, and why. `PWS Triple Validation Compass` is a name that no document in
the repository defines. A repo-wide grep for it returns hits only in command frontmatter. This is
the exact situation the payload template's own rule exists for: "NO named read source = NO
payload". The framework is nevertheless on the ratified 28, so de-listing it would shrink the floor
denominator to dodge work, which the PEST ruling already rejected as a pattern. The honest path is
to name the two partial sources, name the double-attribution problem (the Three Gates are already
claimed as grounding for PWS Value Proposition, which now measures 3/4 live and is out of scope as
a payload), and put the authority question to the navigator with a defined reject branch.

`analyze-timing.md`'s trap, named so nobody walks into it. That file is TITLED "S-Curve Analysis"
and its six phases are S-Curve's. `S-Curve Analysis` is live at 3/4. Attaching those six phases to
Adoption-Capacity Theory would create a second framework claiming another framework's structure and
would look like progress on the floor while corrupting both.

Output: three new payloads, one verified existing payload, four fixtures, one card document.
Committed locally, never executed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/261-enrichment-ceremony-single-admin-window/261-RESEARCH.md
@.planning/research/FEATURES.md
@.planning/REQUIREMENTS.md
</context>

<cross_repo_contract>
| Concern | Repo | Absolute path |
|---|---|---|
| This PLAN.md and all GSD tracking | MindrianOS-Plugin | `/home/jsagi/dev/MindrianOS-Plugin/.planning/` (gitignored, `git add -f`) |
| Every file this plan creates or amends | ProblemsWorthSolving-Brain | `/home/jsagi/dev/ProblemsWorthSolving-Brain/` |
| The sources this plan READS | MindrianOS-Plugin | `references/methodology/{grade,value-proposition,analyze-timing,mullins-7-domains}.md`, `commands/{research,diffusion}.md`, `lib/core/sensors/sensor-diffusion-adoption.cjs` |

**PUSH FREEZE, NON-NEGOTIABLE.** `ProblemsWorthSolving-Brain` is frozen by
`docs/2026-08-20-FREEZE-push-freeze-before-261.md` until THIS phase's admin window closes and its
`GRAPH-WRITE-LOG.md` row is appended. Commit locally, NEVER `git push`. Plan 261-13 is the only
plan permitted to push. This overrides the repo's general commit-straight-to-main convention.

**No admin window is opened or required, and no graph call is made.**

**Subagent dispatch: SAFE.** File authoring, `node --test`, `grep`, `git commit`. No MCP tools.

Both repos forbid em-dashes; hyphens only.
</cross_repo_contract>

<tasks>

<task type="auto">
  <name>Task 1: Establish the source truth for all four, including the repo-wide Triple Validation Compass grep</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/triple-validation-compass.json (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/hypothesis-driven-problem-solving.json (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/adoption-capacity-theory.json (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/mullins-model.json (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/fixtures/framework-evals/reverse-salient-analysis.json`: the schema.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/probe-framework-evals.mjs` lines 1-60.
    - `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/grade.md` IN FULL (5 phases, 6 scoring components).
    - `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/value-proposition.md` IN FULL, the Three Gates section in particular (Is It Real / Can We Win / Is It Worth It).
    - `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/analyze-timing.md` IN FULL. Note its TITLE and whose six phases those are.
    - `/home/jsagi/dev/MindrianOS-Plugin/commands/diffusion.md` IN FULL and `/home/jsagi/dev/MindrianOS-Plugin/lib/core/sensors/sensor-diffusion-adoption.cjs` header: what the ACE engine actually is and whether it is documented as methodology anywhere.
    - `/home/jsagi/dev/MindrianOS-Plugin/commands/research.md` (~25,520 chars): read at least its frontmatter and its seven pipeline stages in full.
    - `/home/jsagi/dev/MindrianOS-Plugin/references/methodology/mullins-7-domains.md` IN FULL.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/mullins-seven-domains.mjs` IN FULL: what it emits and what shape it claims.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-WORKLIST-261-ceremony.md` sections 2, 3 and 4.
  </read_first>

  <action>
Before writing any fixture, run and RECORD one measurement: a repo-wide grep for the literal string
`Triple Validation Compass` across `/home/jsagi/dev/MindrianOS-Plugin/`, excluding `.git` and
`node_modules`. Record the hit count and every distinct file that matched. FEATURES.md's 2026-08-13
reading was "zero hits outside command frontmatter"; re-verify it rather than repeating it. This
count is a load-bearing input to the card in Task 3 and it goes in the plan summary verbatim.

Then write four fixtures using the schema and the rules from plan 261-03 Task 1: exact canonical
resolution counted at 1, structure by member-set equality with source spelling, an explicit flow
assertion in one direction or the other, an expected readiness score with its dimension vector, at
least two negative controls, and a `source` field naming path, char count and read date.

Fixture-specific instructions:

`triple-validation-compass.json`'s `source` field must name BOTH partial sources with their char
counts, and must carry a field or note recording the grep result (the framework's own name appears
in no source document). Its negative controls must include one asserting that a structure member
belonging to `PWS Value Proposition` is NOT returned as a member of this framework, because
double-attribution is the specific hazard.

`hypothesis-driven-problem-solving.json`'s `source` names a `commands/*.md` path, same precedent
extension as Futures Wheel in plan 261-05, and that must be visible in the fixture.

`adoption-capacity-theory.json` must carry a negative control asserting that a named phase from
`analyze-timing.md`'s six-phase S-Curve sequence is ABSENT from Adoption-Capacity Theory's
structure. This is the single most important assertion in this plan.

`mullins-model.json` asserts the seven domains as the structure member set and asserts that NO
`LEADS_TO` chain exists, because the source describes domains in groups, not a sequence. Its
expected readiness is 3/4 with `flow` at 0, stated as an honest ceiling, not a gap to be closed.

Run `node --test tests/eval-framework-structure.test.mjs` and confirm the discovered-fixture count
rose by exactly 4. Do NOT run the live probe leg.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && for f in triple-validation-compass hypothesis-driven-problem-solving adoption-capacity-theory mullins-model; do node -e "const j=require('./tests/fixtures/framework-evals/$f.json'); if(!j.source) process.exit(1)" || exit 1; done && node --test tests/eval-framework-structure.test.mjs</automated>
  </verify>

  <acceptance_criteria>
    - The repo-wide grep for `Triple Validation Compass` was run against MindrianOS-Plugin, and its hit count plus every matching file path is recorded verbatim in the plan summary.
    - Four fixtures exist and parse; each `source` names real file paths (verified with `test -f`) with char counts and read dates.
    - `triple-validation-compass.json` names both partial sources and records that the framework's own name appears in no source document.
    - `triple-validation-compass.json` carries a negative control asserting a `PWS Value Proposition` structure member is not a member of this framework.
    - `adoption-capacity-theory.json` carries a negative control naming a phase from `analyze-timing.md`'s six-phase S-Curve sequence and asserting it is ABSENT.
    - `mullins-model.json` asserts the seven domains as the member set AND asserts that no `LEADS_TO` chain exists, with an expected readiness of 3/4 and `flow` 0.
    - `hypothesis-driven-problem-solving.json`'s source path is under `commands/`.
    - `node --test tests/eval-framework-structure.test.mjs` passes and the discovered-fixture count rose by exactly 4; both counts recorded.
    - The live probe leg was NOT run.
    - `grep -RPn '\x{2014}' tests/fixtures/framework-evals/` exits 1 with no output.
  </acceptance_criteria>

  <done>
    Four fixtures exist, the Triple Validation Compass name-absence is a re-verified measurement
    rather than a carried claim, and the S-Curve structure-theft hazard is encoded as a failing
    control.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author three judgment payloads and verify the existing Mullins payload</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/triple-validation-compass.mjs (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/hypothesis-driven-problem-solving.mjs (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/adoption-capacity-theory.mjs (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/mullins-seven-domains.mjs (modify, header verification block only)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/reverse-salient-analysis.mjs` IN FULL: the template.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/mullins-seven-domains.mjs` IN FULL: its declared target, its emitted nodes and edges, and whether it emits `LEADS_TO`.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/ontology.mjs` line 239 and surroundings: the closed edge vocabulary.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/project-list-structure.mjs` lines 1-40: the name-only framework block for live nodes.
    - Task 1's four fixtures and the recorded grep result.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-WORKLIST-261-ceremony.md` section 4: the reuse disposition already recorded for `mullins-seven-domains.mjs`.
  </read_first>

  <action>
Write three payloads following the template with every header rule from plan 261-03 Task 2, plus
the two Cohort 2 blocks plan 261-05 Task 2 introduces (`RULING REQUIRED:` and `DISCLOSURE:`).

`triple-validation-compass.mjs`. Its `DISCLOSURE:` must state, in the file, that no source document
names the framework, quoting the Task 1 grep result with its hit count. Its `RULING REQUIRED:` must
put TWO questions: which of the two partial sources is authoritative, and whether the Three Gates
may ground this framework given that `value-proposition.md` also grounds `PWS Value Proposition`.
Author the payload against the option the card recommends as default, and state in the header that
the body assumes that default and what would change under each alternative. Never assert
`Triple Validation Compass` as a name the sources define; assert it as the ratified handle the
graph already carries and cite `data/flagship-floor-set.json` for it.

`hypothesis-driven-problem-solving.mjs`. Its `DISCLOSURE:` states that the source is a command body
of a research and evidence pipeline, that hypothesis-driven methodology is implicit in the stage
flow and never stated as a framework, and quotes the passage that comes closest. Its
`RULING REQUIRED:` offers stages-as-structure-with-disclosure versus hold-for-a-real-source-doc,
with the floor consequence of each.

`adoption-capacity-theory.mjs`. Its `DISCLOSURE:` must state explicitly that `analyze-timing.md` is
titled "S-Curve Analysis", that its six phases belong to `S-Curve Analysis` which is already a live
ratified framework at 3/4, and that THIS payload therefore does NOT claim them. It must then state
what the payload does claim, drawn from `commands/diffusion.md`'s ordered walk and the ACE engine
description, and state the resulting readiness honestly, including whether it clears 3. If the
honest content supports only 2, say so in the header and in the card, because that means the floor
row does not flip.

`mullins-seven-domains.mjs`. Do NOT rewrite it. Append a dated `VERIFICATION 2026-08-21:` header
block recording four things: whether its declared target equals the node the ratified string
`Mullins Model` resolves to per the worklist; whether its emitted structure member set equals the
seven domains its fixture names; whether it emits `LEADS_TO` (it must not, because the source
describes groups rather than a sequence); and its resulting expected readiness with the dimension
arithmetic. If any of the four checks fails, record the failure in the block and set the payload's
disposition to `HOLD` with the reason. Do not fix a failure by editing the body in this task; a
body change to a payload authored in another cycle is a re-authoring, and it belongs in its own
task with its own fixture check.

Cross-check each of the three new payloads against its Task 1 fixture and record the comparison in
the plan summary. Resolve disagreements by re-reading the source.

Do NOT run `payloads/run-ingest.mjs`; no Bolt connection to canon exists from this machine.

Commit locally. Do not push.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && for f in triple-validation-compass hypothesis-driven-problem-solving adoption-capacity-theory; do node -e "import('./payloads/$f.mjs').then(m=>{const p=m.payload??m.default; if(!p||!p.framework||!p.framework.name||!Array.isArray(p.nodes)||!Array.isArray(p.edges)) process.exit(1);}).catch(()=>process.exit(1))" || exit 1; grep -q 'RULING REQUIRED:' payloads/$f.mjs || exit 1; grep -q 'DISCLOSURE:' payloads/$f.mjs || exit 1; done && grep -q 'VERIFICATION 2026-08-21:' payloads/mullins-seven-domains.mjs</automated>
  </verify>

  <acceptance_criteria>
    - The three new payload modules import cleanly and export `framework.name`, `nodes` and `edges`.
    - Each new payload header carries `SOURCE:`, `SHAPE NOTE:`, `TECHNIQUE:`, `EXPECTED READINESS AFTER INGEST:`, `RULING REQUIRED:` and `DISCLOSURE:`.
    - `triple-validation-compass.mjs`'s `DISCLOSURE:` quotes the Task 1 grep hit count and states that no source document names the framework. Its `RULING REQUIRED:` puts both the authority question and the double-attribution question.
    - `adoption-capacity-theory.mjs` emits NO structure member that appears in `analyze-timing.md`'s six-phase S-Curve sequence. Verify by name comparison against the fixture's negative control and record the check.
    - `adoption-capacity-theory.mjs`'s header states its honest readiness and, if that is below 3, says in words that the floor row does not flip.
    - `payloads/mullins-seven-domains.mjs` gained a `VERIFICATION 2026-08-21:` block covering all four named checks, and `git diff` shows NO change outside the header comment.
    - If any Mullins check failed, the block records `HOLD` with the reason and the payload body is still unchanged.
    - Every emitted edge `type` in the three new payloads appears in `src/ontology.mjs`'s closed list; grep checks recorded.
    - Each new payload's readiness vector matches its fixture's; corrections recorded with which side changed.
    - `payloads/run-ingest.mjs` was NOT run.
    - `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain log HEAD..origin/main --oneline` is empty.
    - `grep -RPn '\x{2014}' payloads/triple-validation-compass.mjs payloads/hypothesis-driven-problem-solving.mjs payloads/adoption-capacity-theory.mjs payloads/mullins-seven-domains.mjs` exits 1 with no output.
  </acceptance_criteria>

  <done>
    Three judgment payloads exist with their attribution problems written into their headers, and
    the pre-existing Mullins payload carries a four-point verification instead of being trusted.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the batch B card document, including the named CER-03 Triple Validation Compass ruling</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-CARDS-cohort2-batch-b.md (new)
  </files>

  <read_first>
    - The four `RULING REQUIRED:` / `DISCLOSURE:` / `VERIFICATION` blocks from Task 2.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-CARDS-cohort2-batch-a.md` (plan 261-05 Task 3): match its section shape exactly so the window's checkpoint reads one consistent format across both batches.
    - `/home/jsagi/dev/MindrianOS-Plugin/.planning/REQUIREMENTS.md` CER-03's text: "7 judgment flagship payloads land via individual cards, including the Triple Validation Compass source-attribution ruling." That clause is why this card document is a requirement artifact and not a convenience.
    - `/home/jsagi/dev/MindrianOS-Plugin/.planning/research/FEATURES.md` lines 137-160: the PEST ruling's reasoning that de-listing a name to dodge work is not an option. The same reasoning applies to Triple Validation Compass and the card should say so.
  </read_first>

  <action>
Write `docs/2026-08-21-CARDS-cohort2-batch-b.md` in the same shape as batch A's card document: an
opening naming what these rulings share, one section per framework with options, per-option
consequences for both the payload and the floor row, a recommended default with a reason, a reply
token, a `## Reply format` line covering all four, and a `## What happens on reject` section naming
a concrete destination per framework.

The Triple Validation Compass section carries three extra things batch A's sections do not:

1. The grep evidence, quoted: the hit count for the literal name across the plugin repo and the
   files that matched. A card asking about source authority must show the measurement.
2. The double-attribution question as its OWN numbered option set, separate from the
   which-doc-is-authoritative question. They can be answered differently and collapsing them into
   one yes/no hides a real choice. Options should at least include: the Three Gates ground both
   frameworks with cross-reference edges; the Three Gates ground only `PWS Value Proposition` and
   this framework grounds on `grade.md` alone; and this framework is held for a real source doc.
3. An explicit note that de-listing the name from `data/flagship-floor-set.json` is NOT among the
   options, and why: the PEST ruling already established that shrinking the ratified denominator to
   dodge work the repo has sources for is the wrong move, and a de-list would additionally require
   re-attributing the command frontmatter that invokes it. If the navigator wants a de-list anyway,
   the card says that is a separate ratification ceremony with its own artifact (an updated
   `flagship-floor-set.json` with a new `ratified_at` and a reason field), never a silent
   denominator edit.

The Mullins section is a CONFIRMATION card, not an authority card: it presents the four verification
results from Task 2 and asks only whether an honest 3/4 with `flow` unsupported is accepted as this
framework's ceiling. Say plainly that 3/4 clears the `readiness >= 3` floor, so accepting the
ceiling costs the milestone nothing.

Cross-link the card document from each of the four payload headers with a one-line pointer.

Commit locally. Do not push.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && C=docs/2026-08-21-CARDS-cohort2-batch-b.md && test -f "$C" && grep -q '## Reply format' "$C" && grep -q '## What happens on reject' "$C" && grep -q 'Triple Validation Compass' "$C" && grep -qi 'de-list' "$C" && for f in triple-validation-compass hypothesis-driven-problem-solving adoption-capacity-theory mullins-seven-domains; do grep -q '2026-08-21-CARDS-cohort2-batch-b' payloads/$f.mjs || exit 1; done && ! grep -Pq '\x{2014}' "$C"</automated>
  </verify>

  <acceptance_criteria>
    - `docs/2026-08-21-CARDS-cohort2-batch-b.md` exists with one section per framework, matching batch A's section shape.
    - The Triple Validation Compass section quotes the grep hit count and matching files, splits the authority question and the double-attribution question into two separate numbered option sets, and states that de-listing is not an option with the reason and the alternative ceremony it would require.
    - The Mullins section presents all four Task 2 verification results and asks only about accepting the 3/4 ceiling, stating that 3/4 clears the floor.
    - A `## Reply format` section gives one line answering all four rulings.
    - A `## What happens on reject` section names a concrete destination per framework and states the floor consequence. No reject branch is undefined.
    - All four payload headers carry a one-line pointer to this card document.
    - `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain log HEAD..origin/main --oneline` is empty.
    - `grep -Pn '\x{2014}' docs/2026-08-21-CARDS-cohort2-batch-b.md` exits 1 with no output.
  </acceptance_criteria>

  <done>
    CER-03's named Triple Validation Compass source-attribution ruling exists as a measured,
    option-bearing card with a defined reject branch, alongside three more, ready for the window.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a framework's ratified handle to a framework's documented existence | a name on the floor set is not evidence that a source defines it |
| one document's structure to another framework's node | `analyze-timing.md` is titled S-Curve Analysis and its phases belong there |
| a payload authored in a prior cycle to a payload verified for this window | an unexecuted payload is not a validated payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-261-27 | Spoofing | Adoption-Capacity Theory claiming S-Curve Analysis's six phases | mitigate | fixture negative control naming an S-Curve phase and asserting absence; mandatory `DISCLOSURE:` naming the document title and the existing ratified framework; a name-comparison check recorded in the summary |
| T-261-28 | Spoofing | a payload implying that a source document defines `Triple Validation Compass` | mitigate | a re-run repo-wide grep whose hit count is quoted in the payload header and in the card; the name is asserted as a ratified handle with `data/flagship-floor-set.json` cited, never as a defined term |
| T-261-29 | Repudiation | the Three Gates silently grounding two frameworks with no record | mitigate | the double-attribution question is its own numbered option set on the card, and the payload header names the other claimant by path |
| T-261-30 | Tampering | a de-list of a ratified name performed as a quiet data edit to dodge work | mitigate | the card names de-listing as out of scope, states the PEST precedent, and specifies the separate ratification artifact it would require |
| T-261-31 | Tampering | an unexecuted prior-cycle payload run on the assumption it is correct | mitigate | a four-point `VERIFICATION 2026-08-21:` block on `mullins-seven-domains.mjs`, with `git diff` proving the body unchanged and a `HOLD` disposition on any failed check |
| T-261-SC | Tampering | npm/pip/cargo installs | accept | not applicable: this plan installs nothing |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

New in THIS plan, under `/home/jsagi/dev/ProblemsWorthSolving-Brain`:
`payloads/triple-validation-compass.mjs`, `payloads/hypothesis-driven-problem-solving.mjs`,
`payloads/adoption-capacity-theory.mjs`,
`tests/fixtures/framework-evals/triple-validation-compass.json`,
`tests/fixtures/framework-evals/hypothesis-driven-problem-solving.json`,
`tests/fixtures/framework-evals/adoption-capacity-theory.json`,
`tests/fixtures/framework-evals/mullins-model.json`,
`docs/2026-08-21-CARDS-cohort2-batch-b.md`.

Modified existing file: `payloads/mullins-seven-domains.mjs` (header verification block only).

No MindrianOS-Plugin file is modified.
</artifacts_this_phase_produces>

<verification>
- Four fixtures parse and each encodes its framework's specific hazard as a negative control.
- Three new payloads import and carry `RULING REQUIRED:` and `DISCLOSURE:`.
- `mullins-seven-domains.mjs` carries a four-point verification and an unchanged body.
- The card document carries the CER-03-named Triple Validation Compass ruling with measured
  evidence and two separate option sets.
- Nothing was ingested, nothing was pushed.
</verification>

<success_criteria>
- The remaining four CER-03 rows are authored or verified with their rulings surfaced.
- No framework claims another framework's structure.
- The one framework no document defines is handled by disclosure and a card, never by invention.
- Zero em-dashes added. Zero pushes.
</success_criteria>

<output>
Create `.planning/phases/261-enrichment-ceremony-single-admin-window/261-06-SUMMARY.md` when done.
</output>
