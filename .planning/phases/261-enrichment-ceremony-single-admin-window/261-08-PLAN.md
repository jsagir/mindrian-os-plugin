---
phase: 261-enrichment-ceremony-single-admin-window
plan: 08
type: execute
wave: 2
depends_on: [261-01]
target_repo: ProblemsWorthSolving-Brain
files_modified:
  - ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/README.md
  - ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/manifest.json
  - ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/00-evidence.md
  - ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/01-delete-self-loops.cypher
  - ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/02-entity-dedups.cypher
  - ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/90-dry-run.cypher
  - ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/91-verify.cypher
  - ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/99-undo.cypher
  - ProblemsWorthSolving-Brain/docs/2026-08-21-CARDS-alias-hygiene.md
autonomous: true
requirements: [CER-05]

must_haves:
  truths:
    - "CER-05's DELETE targets the MEASURED 165-edge ALIAS_OF self-loop population, not the single node 42214 its original requirement text names"
    - "The self-loop DELETE is scoped by the self-loop predicate itself, never by a label filter, because the measured label census spans 15 label combinations and zero of them carry :Framework"
    - "Every one of the four unresolved alias residue items reaches the window as a written card with named node ids and a recommended default: the JTBD survivor among six candidates, the Pyramid Principle edge-type call, and the two contested survivor picks"
    - "The Scenario Planning dedup is identified as the likely cause of FLOOR-03's match count of 2, and that link is stated, not assumed"
    - "No dedup merge deletes a node; alias anchors are never deleted, per the standing 2026-08-11 alias-collapse doctrine"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/01-delete-self-loops.cypher"
      provides: "The CER-05 self-loop DELETE, batch-stamped, scoped by the self-loop predicate"
      contains: "ALIAS_OF"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-CARDS-alias-hygiene.md"
      provides: "Four residue cards with node ids, both competing rulings where they exist, and recommended defaults"
      contains: "JTBD"
  key_links:
    - from: "payloads/alias-hygiene-2026-08-21/01-delete-self-loops.cypher"
      to: "docs/2026-08-20-RCA-alias-self-loop-minting.md section 6"
      via: "the RCA names the 165 population as CER-05's corrected scope and this file is that cleanup"
      pattern: "165"
    - from: "docs/2026-08-21-CARDS-alias-hygiene.md"
      to: "payloads/relabel-fix-260820/manifest.json unresolved_residue and blocking_preconditions_NOT_MET"
      via: "one card per unresolved item, quoting the manifest's own ids"
      pattern: "relabel-fix-260820"
---

<objective>
Author CER-05's graph-hygiene payload: the `ALIAS_OF` self-loop cleanup at its corrected scale, the
entity dedups the roadmap absorbed from retired Phase 256, and the four unresolved alias rulings
that must reach the navigator as cards rather than as defaults somebody picked.

**The scale correction is the point.** CER-05's requirement text says "the 42214 self-loop is
DELETEd over HTTPS", singular. Plan 260-01's live probe measured **165** self-loops
(`docs/2026-08-20-RCA-alias-self-loop-minting.md`, `[S-1]`), and the roadmap line was corrected
on 2026-08-21 to say so. The RCA's section 6 states it plainly: "It does not delete the 165 existing
self-loops measured in [S-1]. That is Phase 261 CER-05, and this doc is the corrected scope input
for it: CER-05 should target 165, not the single '42214 self-loop' its own roadmap line currently
names." This plan implements that corrected scope.

**The label census is why the DELETE cannot be label-scoped.** The RCA's `[S-2]` census spans 15
label combinations across the 165 edges, and NO row carries `:Framework`. FIX-02's guard is scoped
to the `:Framework` alias branch and covers the MINTING path; the existing population is wider. A
DELETE written as `MATCH (n:Framework)-[r:ALIAS_OF]->(n)` would report success and remove nothing.
Scope by the self-loop predicate: source and target are the same node.

**Four residue items reach this window unresolved, and they are named.** Plan 260-05's summary
hands over two. `payloads/relabel-fix-260820/manifest.json` records two more as
`blocking_preconditions_NOT_MET`. One of that pair, the FIX-02 self-loop-fixture precondition, is
already DISCHARGED by the RCA's section 7. The other, `contested_survivor_picks`, is not, and
`docs/2026-08-21-DECISION-repo-hygiene-close-out.md` confirms that merging the heal branch did not
adjudicate it. The four:

1. `Jobs To Be Done` id 45915 has SIX candidate Framework-labelled JTBD variants (32292, 28579,
   31103, 18102, 34335, 26521) and no stated survivor rule.
2. `The Pyramid Principle` id 39014 is `[Book]`-labelled; `ALIAS_OF` would conflate a book with the
   framework it describes, and `DESCRIBES` or `SOURCE_FOR` may be right instead. Plan 261-04
   Task 3's finding document is the evidence; the ruling is here.
3. `Minto Pyramid`: `relabel-fix-260820` treats 38968 as the survivor;
   `docs/heal-2026-08-20/alias-review.md` collapses 38968 INTO `The Pyramid Principle` 30242.
4. `PWS (Problems Worth Solving)`: `relabel-fix-260820` maps variant 38305 to canonical 40377;
   `alias-review.md` collapses the whole name family into `PWS Methodology` 30337.

**One dedup has a downstream consequence worth naming.** The roadmap's absorbed list includes
`Scenario Planning x3`. `Scenario Planning` is a ratified floor framework measured at 3/4 readiness,
and FLOOR-03 records that it resolves to **2** matches through `normalize_framework_name`, not the
exactly-1 the floor gate requires. A three-way `Scenario Planning` entity duplication is a plausible
cause of that resolver count. Task 1 must state whether the evidence supports that link or not,
because if it does, this dedup is what makes Phase 262's floor row pass, and if it does not, 262
needs to know that too. State the finding either way; do not assert the convenient one.

Output: `payloads/alias-hygiene-2026-08-21/` and one card document, committed locally, never
executed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/261-enrichment-ceremony-single-admin-window/261-RESEARCH.md
@.planning/phases/260-pipeline-fixes-brain-repo-one-pass-one-push/260-05-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<cross_repo_contract>
| Concern | Repo | Absolute path |
|---|---|---|
| This PLAN.md and all GSD tracking | MindrianOS-Plugin | `/home/jsagi/dev/MindrianOS-Plugin/.planning/` (gitignored, `git add -f`) |
| Every file this plan creates | ProblemsWorthSolving-Brain | `/home/jsagi/dev/ProblemsWorthSolving-Brain/` |

**PUSH FREEZE, NON-NEGOTIABLE.** `ProblemsWorthSolving-Brain` is frozen by
`docs/2026-08-20-FREEZE-push-freeze-before-261.md` until THIS phase's admin window closes and its
`GRAPH-WRITE-LOG.md` row is appended. Commit locally, NEVER `git push`. Plan 261-13 is the only
plan permitted to push. This overrides the repo's general commit-straight-to-main convention.

**No admin window is opened or required, and no graph call is made.** Every count comes from
`docs/2026-08-21-WORKLIST-261-ceremony.md` (`[W-5]`, `[W-6]`, `[W-1]`, `[W-2]`) and
`docs/2026-08-20-RCA-alias-self-loop-minting.md`. Do not re-probe; the window's own
`90-dry-run.cypher` regenerates every expectation at execution time, which is where a re-measure
belongs.

**This plan does NOT touch `payloads/relabel-fix-260820/`'s write files.** That payload stays as
committed. This plan's own `02-entity-dedups.cypher` is a SEPARATE batch with its own `batch_id`.
The relabel payload's execution disposition is a window decision, carried on this plan's cards.

**Subagent dispatch: SAFE.** File authoring plus `git commit`. No MCP tools required.

Both repos forbid em-dashes; hyphens only.
</cross_repo_contract>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the hygiene evidence, including the Scenario Planning to FLOOR-03 link</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/00-evidence.md (new)
  </files>

  <read_first>
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-RCA-alias-self-loop-minting.md` IN FULL: sections 1 (the 165 measurement), 3 (the minting path and node 42214's identity shape), 6 (the named residuals, including that the guard prevents minting but does not delete the existing population), 7 (which relabel precondition is discharged and which is not), and the `[S-2]` label census in the appendix.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-WORKLIST-261-ceremony.md` sections 2, 7 and 8, plus `[W-5]`'s re-measured self-loop count and census.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/relabel-fix-260820/manifest.json` and `README.md` IN FULL: the residue and blocking-precondition text verbatim, with every node id.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/heal-2026-08-20/alias-review.md` IN FULL: the competing survivor rulings.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-DECISION-repo-hygiene-close-out.md`: the record that the conflict is unadjudicated.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-RULING-pyramid-principle-target.md` if plan 261-04 has landed it; if it has not, note the dependency and proceed with the manifest's own evidence.
    - `/home/jsagi/dev/MindrianOS-Plugin/.planning/REQUIREMENTS.md` FLOOR-03's full text, and `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-RECORD-fix04-batched-push.md` where it records the deployed round-trip measuring `Scenario Planning` at 2.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/SCHEMA.md` line 105: `ALIAS_OF` semantics, variant to canonical, and the rule that a canonical NEVER has an outgoing `ALIAS_OF`.
  </read_first>

  <action>
Write `payloads/alias-hygiene-2026-08-21/00-evidence.md` with five sections.

**1. The self-loop population.** State the RCA's `[S-1]` 165 and the worklist's `[W-5]` re-measure
side by side with their timestamps. If they differ, say so and say which is authoritative for this
payload's expectations (the later one) and note that the window's own dry-run re-measures again
anyway. Reproduce the label census as a table. Then state the scoping rule in one sentence: the
DELETE is predicated on source and target being the same node, NOT on any label, and the census is
the evidence for why.

**2. The DELETE's safety argument.** A self-loop `(n)-[:ALIAS_OF]->(n)` carries no information: it
asserts a node is an alias of itself. Removing it cannot orphan a node, cannot break an alias chain
(a chain through a self-loop is a chain to the same node), and cannot delete a node. Say all three
explicitly, and state that the operation deletes RELATIONSHIPS only and never a node, which is what
keeps it inside the standing never-delete-alias-anchors doctrine. Then name the one real risk: a
`DELETE` without a self-loop predicate would remove legitimate `ALIAS_OF` edges, and that is what
`91-verify.cypher`'s total-`ALIAS_OF`-count check exists to catch.

**3. The entity dedups.** One row per cluster named in the roadmap's absorbed list:
`MECE x2`, `Eureka Moment x5`, `Scenario Planning x3`, `Mullins alias`. For each, record the node
ids and labels from the worklist and the relabel manifest where available, the proposed canonical
survivor, the proposed variants, and the evidence for the survivor choice. Where no evidence
supports a survivor choice, mark the row `CARD REQUIRED` and do NOT propose one. The roadmap's own
source for this list is a `claude.ai` artifact URL that is not reachable from this repository; state
that plainly, state that the ids in this table were therefore re-derived from the live worklist
rather than transcribed from that artifact, and mark any cluster the worklist does not resolve as
`EVIDENCE MISSING` rather than reconstructing it from the roadmap's one-line summary.

**4. The Scenario Planning finding, stated in both directions.** Record what
`normalize_framework_name('Scenario Planning')` returns per the worklist's `[W-2]`, what the raw
name match returns per `[W-1]`, and how many `Scenario Planning` entity duplicates the dedup row in
section 3 names. Then state ONE of two conclusions with its evidence: either the duplicate count
explains the resolver count, in which case this dedup is what makes FLOOR-03's exactly-1 assertion
true and Phase 262 should expect the row to pass after this window; or it does not, in which case
the resolver count has another cause and Phase 262 must re-rule FLOOR-03 on its own evidence. Do
not edit FLOOR-03's requirement text from here; the requirement says to re-verify at 262's planning
time and this section is an input to that, not a substitute for it.

**5. The four residue items.** One subsection each, quoting the source documents verbatim with node
ids: the JTBD six-candidate ambiguity, the Pyramid Principle `[Book]` entity-type question, and the
two contested survivor picks with BOTH rulings stated. For the Pyramid Principle item, check
`DESCRIBES` and `SOURCE_FOR` against `src/ontology.mjs`'s closed edge list and record whether each
is currently in the vocabulary; if neither is, state that the honest options are leave-it-alone or a
separate schema amendment, and that minting an unlisted edge type mid-ceremony is not one of them.
End each subsection with the recommended default and its reason. For all four, the recommended
default should be the LEAST destructive option that is defensible, because a wrong alias edge is
cheap to add later and expensive to unwind.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && E=payloads/alias-hygiene-2026-08-21/00-evidence.md && test -f "$E" && grep -q '45915' "$E" && grep -q '39014' "$E" && grep -q '38968' "$E" && grep -q '40377' "$E" && grep -qi 'Scenario Planning' "$E" && grep -qi 'FLOOR-03' "$E" && ! grep -Pq '\x{2014}' "$E"</automated>
  </verify>

  <acceptance_criteria>
    - Section 1 states both self-loop measurements with timestamps, names which is authoritative, and reproduces the label census as a table.
    - Section 1 states the scoping rule explicitly: predicated on source equals target, never on a label.
    - Section 2 states all three safety points (no orphan, no chain break, no node deleted) and names the one real risk with the verify check that catches it.
    - Section 3 has one row per named cluster with node ids, and any cluster the worklist does not resolve is marked `EVIDENCE MISSING` rather than reconstructed. The unreachable `claude.ai` artifact source is named as unreachable.
    - Section 4 reaches ONE stated conclusion about the Scenario Planning to FLOOR-03 link, with the three numbers it rests on, and does not edit FLOOR-03's requirement text.
    - Section 5 has four subsections, each quoting its source verbatim with node ids, each ending in a recommended default with a reason.
    - The Pyramid Principle subsection records, per edge type, whether `DESCRIBES` and `SOURCE_FOR` appear in `src/ontology.mjs`'s closed list.
    - Every recommended default in section 5 is the least destructive defensible option, and the text says why that bias applies.
    - `grep -Pn '\x{2014}' 00-evidence.md` exits 1 with no output.
  </acceptance_criteria>

  <done>
    Every hygiene write this payload proposes has its measurement, its safety argument and its
    residue rulings written down with node ids, and the Scenario Planning to FLOOR-03 question is
    answered in one direction with evidence rather than left implied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the hygiene payload directory</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/01-delete-self-loops.cypher (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/02-entity-dedups.cypher (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/90-dry-run.cypher (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/91-verify.cypher (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/99-undo.cypher (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/manifest.json (new)
    /home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/alias-hygiene-2026-08-21/README.md (new)
  </files>

  <read_first>
    - Task 1's `00-evidence.md`.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/order-collision-dishare-2026-08-20/` all nine files: the payload-directory shape and the numbered-statement convention.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/relabel-fix-260820/02-alias-unaliased-clusters.cypher`: the `MERGE ALIAS_OF` shape with a `batch_id` stamp, and its guard style.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/payloads/relabel-fix-260820/99-undo.cypher`: the undo-by-`batch_id` pattern and the WHEN IT ERRORS comment convention.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-20-RCA-alias-self-loop-minting.md` section 5: the guard clause chain FIX-02 deployed, and the read-only rehearsal that proved it filters a self pair to zero. Any new `MERGE ALIAS_OF` this payload emits must carry the same `id(a) <> id(canon)` guard, because the deployed fix guards the INGEST path and a raw `brain_write` statement is not on that path.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/SCHEMA.md` sections 1, 3 and 5: tiering, the provenance dictionary, and the orphan exemption rules.
  </read_first>

  <action>
Author seven files in `payloads/alias-hygiene-2026-08-21/`, `batch_id` `pws-aliashygiene-2026-08-21`.

`01-delete-self-loops.cypher`. One statement. Match the self-loop by predicate, not by label:
bind an `ALIAS_OF` relationship whose start and end node are the same node, then `DELETE` the
RELATIONSHIP only. Never `DETACH DELETE`, never delete a node. Because a deleted relationship cannot
carry a `batch_id` afterwards, the file's header comment must state the before-count from
`00-evidence.md` and point at `91-verify.cypher` as the only post-hoc evidence this ran, and
`99-undo.cypher` must be honest that this specific operation is NOT reversible from a batch stamp.
Include a `RETURN count(r)` so the call reports how many it removed, and note in the comment that
`brain_write` echoes no rows so this count is read from `91-verify.cypher`, not from the write's
return value.

`02-entity-dedups.cypher`. One numbered statement per dedup cluster that section 3 marked ready.
Each statement: `MERGE` an `ALIAS_OF` from the variant to the canonical, guarded with BOTH an
id-and-name double guard on each endpoint AND the `id(a) <> id(canon)` self-pair guard from the
RCA's section 5, and stamped with `batch_id`, `created_by` and `created_at`. Never delete a node.
Never re-point an existing edge in this file. Any cluster marked `CARD REQUIRED` or
`EVIDENCE MISSING` gets a commented-out placeholder statement with a one-line reason, so the file
shows the reader what is deliberately absent rather than silently omitting it. Do NOT include any
of the four residue items as an active statement; they are card-gated and plan 261-12 adds them
only after a ruling.

`90-dry-run.cypher`, read-only, numbered:
- `[90.1]` current self-loop count (the DELETE's expected magnitude, regenerated at window time).
- `[90.2]` current TOTAL `ALIAS_OF` count. This is the denominator for the verify's safety check.
- `[90.3]` for each active dedup statement, whether both endpoints still resolve on id AND name.
- `[90.4]` for each active dedup pair, whether an `ALIAS_OF` already exists in either direction
  (idempotency check; a pre-existing edge means the MERGE is a no-op and should be visible as one).
- `[90.5]` count of nodes or edges already carrying `batch_id = 'pws-aliashygiene-2026-08-21'`,
  expected 0.
- `[90.6]` the `normalize_framework_name`-relevant raw match count for `Scenario Planning`, so the
  window can record the before value for the FLOOR-03 finding.

`91-verify.cypher`, read-only, numbered:
- `[91.1]` self-loop count after, expected 0. This is CER-05's named acceptance ("the post-batch
  self-loop probe returns 0").
- `[91.2]` total `ALIAS_OF` count after. Compared against `[90.2]` minus `[90.1]` plus the number of
  edges `02` merged. A mismatch means the DELETE removed more than self-loops, which is the
  catastrophic case; state that arithmetic in a comment so the window can check it in one line.
- `[91.3]` count of edges carrying the batch_id, the measured `edge_count` for the
  `GraphWriteEvent`.
- `[91.4]` a negative control: count of nodes DELETED by this batch, expected 0. Implement as a
  before-and-after total node count comparison recorded by the window, since a Cypher statement
  cannot count what is gone.
- `[91.5]` the `Scenario Planning` raw match count after, for the FLOOR-03 finding's after value.

`99-undo.cypher`. Reverse `02` by `batch_id`: delete the `ALIAS_OF` edges this batch merged. Then a
prominent comment block stating that `01`'s self-loop DELETE is NOT reversible from this payload:
the edges are gone and their only record is the before-count in `00-evidence.md` and the label
census. State that restoring them would require the snapshot `brain_write` takes automatically
after the preceding commit, name that as the actual rollback path, and note that no one should want
to restore a self-loop anyway. Honesty here is the point; a `99-undo.cypher` implying full
reversibility would be worse than one that says which half is irreversible.

`manifest.json`, following the `order-collision-dishare-2026-08-20` field set:
`batch_id: "pws-aliashygiene-2026-08-21"`, `compile_only: true`, `review_required: true`,
`not_executed: true`, a `source` block naming `00-evidence.md`, the RCA and the worklist,
`statement_counts` per file, `edge_vocabulary_used: ["ALIAS_OF"]` with the note that it is already
in the closed set per the 2026-08-11 alias-collapse runbook, an `invariants` block
(`node_creation: false`, `node_deletion: false`, `no_detach_delete: true`,
`every_merged_edge_carries_batch_id: true`, `idempotent`, and an `undo` field that states the
partial reversibility honestly), and an `unresolved_residue` object with all FOUR residue items by
name and node id.

`README.md`: what the batch does, the corrected 165 scope with its citation, the safety argument in
two sentences, the four card-gated residue items listed by name, and empty `## Dry-run recompile`,
`## Navigator approval` and `## Commit log` sections.

Commit locally. Do not push. Do not execute.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && P=payloads/alias-hygiene-2026-08-21 && for f in 00-evidence.md 01-delete-self-loops.cypher 02-entity-dedups.cypher 90-dry-run.cypher 91-verify.cypher 99-undo.cypher manifest.json README.md; do test -f "$P/$f" || exit 1; done && node -e "const m=require('./payloads/alias-hygiene-2026-08-21/manifest.json'); if(m.compile_only!==true||m.not_executed!==true||!m.unresolved_residue||Object.keys(m.unresolved_residue).length<4) process.exit(1)" && ! grep -qi 'DETACH DELETE' "$P/01-delete-self-loops.cypher" && grep -v '^--' "$P/90-dry-run.cypher" | grep -cE '\b(SET|MERGE|CREATE|DELETE|REMOVE)\b' | grep -q '^0$'</automated>
  </verify>

  <acceptance_criteria>
    - All eight files exist in `payloads/alias-hygiene-2026-08-21/`.
    - `01-delete-self-loops.cypher` scopes by the self-loop predicate (start node equals end node) and contains NO label filter on the matched node. A statement matching `(n:Framework)` fails this criterion outright, because the measured census shows zero self-loops on `:Framework` nodes.
    - `01-delete-self-loops.cypher` deletes relationships only. It contains no `DETACH DELETE` and no node delete.
    - `02-entity-dedups.cypher`'s every active statement carries an id-and-name double guard on both endpoints AND an `id(a) <> id(canon)` self-pair guard, and stamps `batch_id`, `created_by`, `created_at`.
    - `02-entity-dedups.cypher` contains a commented-out placeholder with a stated reason for every cluster marked `CARD REQUIRED` or `EVIDENCE MISSING`, and contains NO active statement for any of the four residue items.
    - `90-dry-run.cypher` has six numbered read-only checks and zero write clauses.
    - `91-verify.cypher` includes `[91.1]` expecting 0 self-loops, `[91.2]` with its stated arithmetic against `[90.2]`, and `[91.4]`'s node-count negative control.
    - `99-undo.cypher` reverses `02` by batch and states in a comment block that `01` is NOT reversible from this payload, naming the automatic snapshot as the real rollback path.
    - `manifest.json` parses, is `compile_only` and `not_executed`, declares `node_deletion: false`, and lists all four residue items in `unresolved_residue`.
    - `payloads/relabel-fix-260820/`'s `.cypher` files are byte-unchanged: `git diff --stat` shows no modification to them.
    - `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain log HEAD..origin/main --oneline` is empty.
    - `grep -RPn '\x{2014}' payloads/alias-hygiene-2026-08-21/` exits 1 with no output.
  </acceptance_criteria>

  <done>
    CER-05's cleanup exists at its corrected 165-edge scope, scoped by predicate rather than label,
    deleting no nodes, with a verify that can catch an over-broad delete and an undo that admits
    which half it cannot reverse.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the four residue cards</name>

  <files>
    /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-CARDS-alias-hygiene.md (new)
  </files>

  <read_first>
    - Task 1's `00-evidence.md` section 5.
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/2026-08-21-CARDS-cohort2-batch-a.md`: match the section shape.
    - `/home/jsagi/dev/MindrianOS-Plugin/.planning/phases/258-reconcile-the-wave-hard-gates-all-writing-phases/258-06-PLAN.md` task 3's third-claimant ruling: the precedent for a card whose recommended default is LEAVE IT UNTOUCHED and record it as residue. That default was correct there for the same reason it is likely correct here, and the card should say so.
  </read_first>

  <action>
Write `docs/2026-08-21-CARDS-alias-hygiene.md` with four sections matching the established card
shape: the item, the node ids, the competing evidence, numbered options, per-option consequence,
recommended default with reason, and a reply token. Then `## Reply format` and
`## What happens on defer`.

Card 1, JTBD survivor. List all six candidate ids with their names and any distinguishing evidence
from the worklist (readiness, edge counts, which one the ratified string `Jobs to Be Done (JTBD)`
resolves to). The recommended default should be whichever id the ratified floor string resolves to,
because that is the node the floor gate and every consumer already reach; state that reasoning. Add
an option to defer the whole cluster, and state what deferring costs (nothing today; the duplication
persists and stays queued).

Card 2, Pyramid Principle entity type. Present the `[Book]` argument, the two candidate edge types,
and whether each is in the closed vocabulary per Task 1's check. If neither is, the options collapse
to leave-it-alone or open a schema amendment, and the card must say that minting an unlisted edge
type is not an option. Cross-reference plan 261-04's `docs/2026-08-21-RULING-pyramid-principle-target.md`.

Card 3, `Minto Pyramid` contested survivor. Both rulings side by side with ids 38968 and 30242, the
document each comes from, and the note that
`docs/2026-08-21-DECISION-repo-hygiene-close-out.md` records the conflict as unadjudicated.
Recommended default: defer, because both candidate rulings are already recorded and an alias edge
minted on the wrong survivor is expensive to unwind while deferring costs one queued item.

Card 4, `PWS (Problems Worth Solving)` contested survivor. Same shape, ids 38305, 40377 and 30337.

`## Reply format` gives one line answering all four. `## What happens on defer` states, per card,
that the item stays in `unresolved_residue`, stays out of `02-entity-dedups.cypher`, and carries to
the next hygiene window, and that this is a normal outcome rather than a failure. That last clause
matters: three of these four cards recommend defer, and a card set whose recommended answer is
mostly "not yet" needs to say why that is the right answer rather than looking like avoidance. The
reason is the one 258-06 already recorded: a wrong alias edge is a cheap add later and an expensive
unwind now.

Commit locally. Do not push.

No em-dashes.
  </action>

  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && C=docs/2026-08-21-CARDS-alias-hygiene.md && test -f "$C" && grep -q '## Reply format' "$C" && grep -q '## What happens on defer' "$C" && for id in 45915 39014 38968 30242 38305 40377 30337; do grep -q "$id" "$C" || exit 1; done && ! grep -Pq '\x{2014}' "$C"</automated>
  </verify>

  <acceptance_criteria>
    - `docs/2026-08-21-CARDS-alias-hygiene.md` exists with four sections in the established card shape.
    - Every node id named in this plan's objective appears in the card: 45915, the six JTBD candidates, 39014, 38968, 30242, 38305, 40377, 30337.
    - Card 1 lists all six JTBD candidates with distinguishing evidence and recommends the id the ratified floor string resolves to, with that reasoning stated.
    - Card 2 states, per candidate edge type, whether it is in `src/ontology.mjs`'s closed list, and states that minting an unlisted type is not an option.
    - Cards 3 and 4 present both competing rulings with ids and cite `docs/2026-08-21-DECISION-repo-hygiene-close-out.md` for the conflict being unadjudicated. Neither card picks a winner as its default.
    - `## What happens on defer` states the concrete consequence per card and explains why defer is the right recommendation rather than avoidance.
    - `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain log HEAD..origin/main --oneline` is empty.
    - `grep -Pn '\x{2014}' docs/2026-08-21-CARDS-alias-hygiene.md` exits 1 with no output.
  </acceptance_criteria>

  <done>
    All four unresolved alias items reach the window as cards with named ids, both competing rulings
    where they exist, and a defer path that is explained rather than implied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a requirement's original text to its measured scope | CER-05 says one edge; the graph has 165 |
| a label-scoped delete to the actual population | the measured census carries zero `:Framework` rows |
| a deployed ingest guard to a raw write statement | FIX-02 guards `ingestFramework`, not a `brain_write` call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-261-38 | Denial of Service | a `DELETE` without a self-loop predicate removing legitimate `ALIAS_OF` edges | mitigate | the statement is predicated on start equals end node; `[90.2]` records the total before and `[91.2]` checks the arithmetic after, with the expected delta stated in a comment |
| T-261-39 | Repudiation | CER-05 executed against the single node 42214 and reported complete | mitigate | the corrected 165 scope is cited from the RCA in `00-evidence.md`, the README and the manifest, and `[91.1]` requires 0 self-loops, not 164 |
| T-261-40 | Tampering | a fresh self-loop minted by this payload's own `MERGE ALIAS_OF` statements | mitigate | every dedup statement carries the RCA section 5 `id(a) <> id(canon)` guard, because the deployed FIX-02 guard covers the ingest path and not a raw write |
| T-261-41 | Tampering | a node deleted during a dedup merge | mitigate | `node_deletion: false` in the manifest, no `DETACH DELETE` anywhere, and `[91.4]`'s before-and-after node count control |
| T-261-42 | Repudiation | one of the four contested rulings adopted by writing it into a payload | mitigate | none of the four appears as an active statement; each is a card with both rulings stated and a defer default |
| T-261-43 | Spoofing | dedup ids reconstructed from the roadmap's one-line summary when the real artifact is unreachable | mitigate | `00-evidence.md` names the `claude.ai` source as unreachable and marks any unresolvable cluster `EVIDENCE MISSING` rather than filling it in |
| T-261-SC | Tampering | npm/pip/cargo installs | accept | not applicable: this plan installs nothing |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

New in THIS plan, under `/home/jsagi/dev/ProblemsWorthSolving-Brain`:
`payloads/alias-hygiene-2026-08-21/` (`00-evidence.md`, `01-delete-self-loops.cypher`,
`02-entity-dedups.cypher`, `90-dry-run.cypher`, `91-verify.cypher`, `99-undo.cypher`,
`manifest.json`, `README.md`) and `docs/2026-08-21-CARDS-alias-hygiene.md`.

Graph state this payload will change WHEN THE WINDOW RUNS IT (not here): the measured `ALIAS_OF`
self-loop population removed (relationships only, no nodes), and new `ALIAS_OF` edges merged for
the evidence-backed dedup clusters under `batch_id` `pws-aliashygiene-2026-08-21`.

`payloads/relabel-fix-260820/`'s write files are NOT modified by this plan.
No MindrianOS-Plugin file is modified.
</artifacts_this_phase_produces>

<verification>
- The DELETE is predicate-scoped, relationship-only, and its expected magnitude is the measured
  population, not one edge.
- Every merged edge carries a double guard plus the self-pair guard and a batch stamp.
- The verify can catch an over-broad delete and a node deletion.
- All four residue items are card-gated and appear nowhere as active statements.
- Nothing was executed, nothing was pushed.
</verification>

<success_criteria>
- CER-05's cleanup is authored at its true scale with a falsifiable post-condition of zero.
- The absorbed Phase 256 entity dedups are authored where evidence supports them and marked missing
  where it does not.
- The Scenario Planning to FLOOR-03 link is answered in one direction with numbers.
- Zero em-dashes added. Zero pushes.
</success_criteria>

<output>
Create `.planning/phases/261-enrichment-ceremony-single-admin-window/261-08-SUMMARY.md` when done.
</output>
