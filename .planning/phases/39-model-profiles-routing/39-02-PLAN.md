---
phase: 39-model-profiles-routing
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - commands/models.md
  - commands/act.md
  - commands/grade.md
  - commands/deep-grade.md
  - commands/research.md
  - CHANGELOG.md
  - .claude-plugin/plugin.json
autonomous: true
requirements:
  - MODEL-03
  - MODEL-05
  - MODEL-06
must_haves:
  truths:
    - "User can view current model profile and per-agent resolved models via /mos:models"
    - "User can switch profile via /mos:models set <profile>"
    - "User can override a specific agent via /mos:models override <agent> <model>"
    - "Commands that dispatch agents include model resolution before dispatch"
    - "When resolveModel returns skip, command shows stage-skip message and does not dispatch the agent"
  artifacts:
    - path: "commands/models.md"
      provides: "/mos:models command for viewing, switching, and overriding model profiles"
      contains: "model_profile"
    - path: "commands/act.md"
      provides: "Model resolution before framework-runner dispatch"
      contains: "model-profiles"
    - path: "commands/grade.md"
      provides: "Model resolution before grading dispatch"
      contains: "model-profiles"
  key_links:
    - from: "commands/models.md"
      to: "lib/core/model-profiles.cjs"
      via: "references resolveModel and formatProfileTable"
      pattern: "model-profiles"
    - from: "commands/act.md"
      to: "lib/core/model-profiles.cjs"
      via: "model resolution step before agent dispatch"
      pattern: "model-profiles"
---

<objective>
Create the /mos:models command and integrate model resolution into the 4 commands that dispatch agents (act, grade, deep-grade, research). The command lets users view, switch, and override model profiles. Agent-dispatching commands add a model resolution step before dispatch per D-03. Bump version for user-facing change per CLAUDE.md release process.

Purpose: User-facing control over model routing, plus wiring resolution into every agent dispatch path.
Output: commands/models.md + updated dispatch instructions in 4 command files + version bump
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-model-profiles-routing/39-CONTEXT.md
@.planning/phases/39-model-profiles-routing/39-RESEARCH.md
@.planning/phases/39-model-profiles-routing/39-01-SUMMARY.md

<interfaces>
From lib/core/model-profiles.cjs (created in Plan 01):
```javascript
const MODEL_PROFILES = { /* 8 agents x 3 tiers */ };
const VALID_PROFILES = ['quality', 'balanced', 'budget'];
const STAGE_HINTS = { /* 5 stages x 4 agents */ };
const CASCADE_MODELS = { /* 5 cascade steps */ };

function loadRoomConfig(roomDir) { /* returns { model_profile, model_overrides } */ }
function parseVentureStage(roomDir) { /* returns stage string or null */ }
function resolveModel(roomDir, agentType) { /* returns model alias or 'skip' or 'inherit' */ }
function resolveCascadeModel(step) { /* returns model alias or null */ }
function formatProfileTable(roomDir) { /* returns formatted display string */ }
```

From commands/act.md (dispatch pattern):
```markdown
2. If yes, dispatch to `agents/framework-runner.md` with:
```

From commands/grade.md (dispatch pattern):
```markdown
1. Read and follow `agents/grading.md`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/models.md</name>
  <files>commands/models.md</files>
  <read_first>
    - commands/act.md (reference for command frontmatter pattern: name, description, body_shape, allowed-tools)
    - commands/admin.md (reference for command that reads/writes config)
    - lib/core/model-profiles.cjs (verify exports available)
  </read_first>
  <action>
Create commands/models.md for the /mos:models command per MODEL-03.

The file must have YAML frontmatter (triple-dash delimited, matching the pattern in commands/act.md) with these fields:
- name: models
- description: View and manage model routing profiles for MindrianOS agents
- body_shape: C (Information Card)
- ui_reference: skills/ui-system/SKILL.md
- allowed-tools: [Read, Write, Bash]

After the frontmatter, write the following markdown command body:

Title: /mos:models

Opening line: You are Larry. This command manages model routing profiles that control which AI model each agent uses.

Usage section listing 6 subcommands:
- `/mos:models` -- show current profile and resolved models for all agents
- `/mos:models set <profile>` -- switch profile (quality, balanced, budget)
- `/mos:models override <agent> <model>` -- override a specific agent's model
- `/mos:models override <agent> clear` -- remove an agent override
- `/mos:models set-default <profile>` -- set global default for new rooms (writes ~/.mindrian/defaults.json)
- `/mos:models reset` -- remove room/.config.json, revert to defaults

Step 1: Resolve Room -- Find active room by walking up from $PWD looking for STATE.md. If no room found, show global defaults only.

Step 2: Parse Arguments -- Determine subcommand from user message.

Step 3: Execute Subcommand with these subsections:

View (default): Run `node lib/core/model-profiles.cjs table <roomDir>` to get formatted profile table. Display with header showing Room path, Profile name, Stage (or "none detected"), the table, Overrides (or "none"), and tip about switching profiles.

Set Profile: Validate profile is quality/balanced/budget. Read or create room/.config.json. Set model_profile. Write back with JSON.stringify(config, null, 2). Confirm change.

Override Agent: Validate agent is one of 8 MODEL_PROFILES names (larry-extended, framework-runner, grading, investor, brain-query, research, opportunity-scanner, persona-analyst). Validate model is opus/sonnet/haiku/clear. Update model_overrides in room/.config.json. Confirm.

Set Global Default: Validate profile. mkdir -p ~/.mindrian/. Write {"model_profile":"<profile>"} to ~/.mindrian/defaults.json. Confirm.

Reset: Delete room/.config.json. Confirm using quality defaults.
  </action>
  <verify>
    <automated>grep -c 'model_profile\|resolveModel\|model-profiles' commands/models.md</automated>
  </verify>
  <acceptance_criteria>
    - File commands/models.md exists
    - Frontmatter contains `name: models`
    - Frontmatter contains `body_shape: C (Information Card)`
    - File contains the string "model_profile" at least 3 times
    - File contains "model-profiles.cjs" (reference to core module)
    - File contains all 6 subcommands: view, set, override, set-default, reset
    - File contains "quality, balanced, budget" as valid profile values
    - File contains all 8 agent names: larry-extended, framework-runner, grading, investor, brain-query, research, opportunity-scanner, persona-analyst
    - File contains "~/.mindrian/defaults.json" for global defaults per D-02
  </acceptance_criteria>
  <done>/mos:models command file covers view, set profile, override agent, set global default, and reset operations with proper validation.</done>
</task>

<task type="auto">
  <name>Task 2: Add model resolution to agent-dispatching commands</name>
  <files>commands/act.md, commands/grade.md, commands/deep-grade.md, commands/research.md</files>
  <read_first>
    - commands/act.md (full file -- find exact dispatch line to add resolution before)
    - commands/grade.md (full file -- find agent dispatch)
    - commands/deep-grade.md (full file -- find agent dispatch)
    - commands/research.md (full file -- find agent dispatch)
    - lib/core/model-profiles.cjs (confirm resolveModel export and CLI interface)
  </read_first>
  <action>
Add a model resolution step to each command that dispatches an agent, per D-03. The pattern is: resolve model BEFORE dispatch, include `model: {resolved}` in dispatch instructions, handle 'skip' return.

For each command, add a "Model Resolution" section BEFORE the agent dispatch step. The section content:

    ## Model Resolution

    Before dispatching the agent, resolve its model:

        node lib/core/model-profiles.cjs resolve <roomDir> <agentType>

    - If result is "skip", tell the user: "<Agent> is not recommended at the current venture stage. Use /mos:models override <agent> sonnet to force."
    - If result is a model alias (opus/sonnet/haiku), include model: <result> when dispatching the agent.
    - If result is "inherit", do not specify a model (use session default).

Specific changes per file:

commands/act.md -- Agent type: framework-runner. Insert model resolution section BEFORE the dispatch step that reads "dispatch to agents/framework-runner.md". For --swarm mode (dispatches 3 framework-runners), resolve once and apply to all.

commands/grade.md -- Agent type: grading. Insert model resolution section BEFORE "Read and follow agents/grading.md". If skip, tell user grading is not available at this stage.

commands/deep-grade.md -- Agent type: grading. Same pattern as grade.md. Insert before agent dispatch.

commands/research.md -- Agent type: research. Insert before agent dispatch. If skip, tell user research agent is not available at this stage.

CRITICAL: In each file, the "Model Resolution" section MUST appear BEFORE the dispatch/agent invocation line. Verify insertion order -- model resolution first, then dispatch. Do not place it after the dispatch instruction.

Do NOT modify agent .md files. Agent frontmatter stays model: inherit. Resolution happens at dispatch time per D-03.
  </action>
  <verify>
    <automated>for f in commands/act.md commands/grade.md commands/deep-grade.md commands/research.md; do echo "--- $f ---"; RES=$(grep -n 'Model Resolution' "$f" | head -1 | cut -d: -f1); DISP=$(grep -n 'dispatch\|agents/framework-runner\|agents/grading\|agents/research' "$f" | tail -1 | cut -d: -f1); if [ -n "$RES" ] && [ -n "$DISP" ] && [ "$RES" -lt "$DISP" ]; then echo "PASS: resolution (line $RES) before dispatch (line $DISP)"; else echo "FAIL: resolution=$RES dispatch=$DISP"; fi; done</automated>
  </verify>
  <acceptance_criteria>
    - commands/act.md contains "Model Resolution" section BEFORE dispatch to agents/framework-runner.md
    - commands/act.md contains "framework-runner" in context of model resolution
    - commands/act.md contains "skip" handling text
    - commands/grade.md contains "Model Resolution" section BEFORE dispatch to agents/grading.md
    - commands/grade.md contains "grading" in context of model resolution
    - commands/deep-grade.md contains "Model Resolution" section BEFORE agent dispatch
    - commands/research.md contains "Model Resolution" section BEFORE agent dispatch
    - commands/research.md contains "research" in context of model resolution
    - In all 4 files, Model Resolution line number is LOWER than dispatch line number
    - No agent .md file in agents/ directory was modified (grep for model-profiles in agents/*.md returns 0 matches)
    - All 4 command files still have their original frontmatter intact (name field unchanged)
  </acceptance_criteria>
  <done>All 4 agent-dispatching commands include model resolution step before dispatch. Skip handling present. Insertion order verified. Agent .md files untouched per D-03.</done>
</task>

<task type="auto">
  <name>Task 3: Version bump and changelog</name>
  <files>CHANGELOG.md, .claude-plugin/plugin.json</files>
  <read_first>
    - CHANGELOG.md (current version and format)
    - .claude-plugin/plugin.json (current version field)
  </read_first>
  <action>
Per CLAUDE.md release process, /mos:models is a user-facing feature and requires a version bump.

1. Read current version from .claude-plugin/plugin.json
2. Bump minor version (e.g., 1.6.0 -> 1.7.0)
3. Add CHANGELOG.md entry at the top:

```markdown
## [X.Y.Z] - 2026-03-31
### Added
- /mos:models command: view, switch, and override model routing profiles
- Model resolution step in /mos:act, /mos:grade, /mos:deep-grade, /mos:research
- MODEL_PROFILES table (8 agents x 3 tiers), STAGE_HINTS (5 venture stages), CASCADE_MODELS (5 steps)
- Per-room .config.json and global ~/.mindrian/defaults.json for profile persistence
### Changed
- Agent-dispatching commands now resolve model before dispatch (override > stage-hint > profile > default)
```

4. Update version in .claude-plugin/plugin.json to match
  </action>
  <verify>
    <automated>node -e "const p=require('./.claude-plugin/plugin.json'); const c=require('fs').readFileSync('CHANGELOG.md','utf8'); const v=p.version; console.log(c.includes(v)?'PASS':'FAIL', 'version', v, 'in changelog'); console.log(c.includes('models')?'PASS':'FAIL', 'models in changelog');"</automated>
  </verify>
  <acceptance_criteria>
    - .claude-plugin/plugin.json version is bumped
    - CHANGELOG.md has new entry with matching version
    - CHANGELOG.md entry mentions /mos:models and model resolution
  </acceptance_criteria>
  <done>Version bumped and changelog updated per CLAUDE.md release process for user-facing model routing feature.</done>
</task>

</tasks>

<verification>
- `grep -l 'model-profiles' commands/*.md` returns at least models.md, act.md, grade.md, deep-grade.md, research.md
- `grep -c 'model-profiles' agents/*.md` returns 0 for all agent files (agents untouched)
- `head -5 commands/models.md` shows valid frontmatter with name: models
- `bash scripts/validate-model-profiles` still passes (Plan 01 validation)
- Model Resolution appears before dispatch in all 4 command files
- CHANGELOG.md version matches .claude-plugin/plugin.json version
</verification>

<success_criteria>
- /mos:models command covers view, set, override, set-default, reset (MODEL-03)
- All 4 agent-dispatching commands have model resolution before dispatch (MODEL-03 + MODEL-06 + D-03)
- Skip handling present in all dispatch commands (MODEL-06 end-to-end)
- Agent .md files remain untouched with model: inherit (D-03)
- Cascade model table documented and accessible (MODEL-05 wiring complete from Plan 01)
- Version bumped per CLAUDE.md release process
</success_criteria>

<output>
After completion, create `.planning/phases/39-model-profiles-routing/39-02-SUMMARY.md`
</output>
