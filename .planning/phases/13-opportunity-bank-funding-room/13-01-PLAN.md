---
phase: 13-opportunity-bank-funding-room
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/opportunity-ops.cjs
  - references/opportunities/opportunity-template.md
  - references/opportunities/funding-lifecycle.md
  - tests/fixtures/sample-room-opp/opportunity-bank/STATE.md
  - tests/fixtures/sample-room-opp/opportunity-bank/2026-03-20-nsf-sbir.md
  - tests/fixtures/sample-room-opp/funding/nsf-sbir-phase1/STATUS.md
  - tests/fixtures/sample-room-opp/funding/STATE.md
  - tests/fixtures/sample-room-opp/problem-definition/current.md
  - tests/fixtures/sample-room-opp/STATE.md
  - tests/test-phase-13.sh
autonomous: true
requirements:
  - OPP-01
  - FUND-01

must_haves:
  truths:
    - "opportunity-bank/ and funding/ are discovered by section-registry as extended sections"
    - "opportunity-ops.cjs module loads without error and exports listOpportunities + listFunding"
    - "Test fixtures provide a realistic room with opportunity-bank and funding content"
    - "Reference templates define the artifact frontmatter schema and lifecycle stages"
  artifacts:
    - path: "lib/core/opportunity-ops.cjs"
      provides: "Core operations for opportunity-bank and funding sections"
      exports: ["listOpportunities", "listFunding", "parseOpportunityFrontmatter", "parseFundingStatus"]
    - path: "references/opportunities/opportunity-template.md"
      provides: "Opportunity artifact frontmatter schema and filing instructions"
      contains: "methodology: opportunity-scan"
    - path: "references/opportunities/funding-lifecycle.md"
      provides: "4-stage lifecycle definitions and transition criteria"
      contains: "Discovered > Researched > Applying > Submitted"
    - path: "tests/fixtures/sample-room-opp/"
      provides: "Test room with opportunity-bank/ and funding/ sections populated"
    - path: "tests/test-phase-13.sh"
      provides: "Phase 13 test suite"
  key_links:
    - from: "lib/core/opportunity-ops.cjs"
      to: "lib/core/section-registry.cjs"
      via: "require('./section-registry.cjs')"
      pattern: "require.*section-registry"
    - from: "tests/test-phase-13.sh"
      to: "lib/core/opportunity-ops.cjs"
      via: "node -e require"
      pattern: "node.*opportunity-ops"
---

<objective>
Create the foundation for Opportunity Bank and Funding Room: core module, reference templates, test fixtures, and test suite.

Purpose: Establish the contracts (frontmatter schema, lifecycle stages, module API) that Plans 02 and 03 build against. Both new room sections must be discoverable by section-registry.
Output: opportunity-ops.cjs module, reference templates, test fixtures, test suite.
</objective>

<execution_context>
@/home/jsagi/.claude/get-shit-done/workflows/execute-plan.md
@/home/jsagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-opportunity-bank-funding-room/13-RESEARCH.md
@.planning/phases/13-opportunity-bank-funding-room/13-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs from existing codebase -->

From lib/core/section-registry.cjs:
```javascript
// opportunity-bank and funding already pre-assigned:
'opportunity-bank': { label: 'OPPORTUNITY BANK', color: '#8B6914' },
'funding':          { label: 'FUNDING',          color: '#1A5276' },

// Discovery API:
function discoverSections(roomDir) // returns { core, extended, all, getMeta(name) }
```

From lib/core/room-ops.cjs:
```javascript
const { discoverSections } = require('./section-registry.cjs');
const SCRIPTS_DIR = path.resolve(__dirname, '../../scripts');
function listSections(roomDir) // returns { sections, core_count, extended_count }
function analyzeRoom(roomDir) // returns raw script output
```

From lib/core/index.cjs:
```javascript
function output(data, raw, rawStr) // JSON or raw output
function error(msg) // stderr + exit(1)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reference templates + test fixtures</name>
  <files>
    references/opportunities/opportunity-template.md,
    references/opportunities/funding-lifecycle.md,
    tests/fixtures/sample-room-opp/STATE.md,
    tests/fixtures/sample-room-opp/problem-definition/current.md,
    tests/fixtures/sample-room-opp/opportunity-bank/STATE.md,
    tests/fixtures/sample-room-opp/opportunity-bank/2026-03-20-nsf-sbir.md,
    tests/fixtures/sample-room-opp/funding/STATE.md,
    tests/fixtures/sample-room-opp/funding/nsf-sbir-phase1/STATUS.md,
    tests/test-phase-13.sh
  </files>
  <action>
    1. Create `references/opportunities/opportunity-template.md` with the opportunity artifact frontmatter schema from RESEARCH.md Pattern 4 (methodology, created, source, source_url, opportunity_id, funder, program, amount_floor, amount_ceiling, deadline, eligibility, funding_category, relevance_score, relevance_reasoning, room_connections, status, rejection). Include filing instructions for Larry.

    2. Create `references/opportunities/funding-lifecycle.md` defining the 4 locked stages per CONTEXT.md:
       - **Discovered**: Opportunity identified, basic info captured. Transition: user requests deeper analysis.
       - **Researched**: Eligibility confirmed, fit assessed, requirements documented. Transition: user decides to apply.
       - **Applying**: Application in progress, narrative drafted. Transition: application submitted.
       - **Submitted**: Application sent, awaiting decision.
       - Include `outcome` attribute (awarded | rejected | withdrawn) as separate from stage per CONTEXT.md (Awarded/Rejected are outcomes, NOT stages).
       - Define STATUS.md frontmatter: stage, outcome, source_opportunity (wikilink to opportunity-bank), deadline, last_updated, transition_history.

    3. Create test fixture room `tests/fixtures/sample-room-opp/` with:
       - `STATE.md` with venture_stage, domain keywords, geography (needed for context-driven query testing)
       - `problem-definition/current.md` with a sample problem statement (provides search context)
       - `opportunity-bank/STATE.md` with section summary (1 filed opportunity)
       - `opportunity-bank/2026-03-20-nsf-sbir.md` using the template frontmatter schema, status: discovered, relevance_score: 0.85, with room_connections to problem-definition
       - `funding/STATE.md` with pipeline summary (1 entry in Researched stage)
       - `funding/nsf-sbir-phase1/STATUS.md` with stage: researched, source_opportunity wikilink `[[opportunity-bank/2026-03-20-nsf-sbir]]`, transition_history

    4. Create `tests/test-phase-13.sh` test suite script following existing pattern (set -euo pipefail, pass/fail counting). Include tests:
       - Section discovery: `node -e` verifying opportunity-bank and funding appear in discoverSections() extended list for sample-room-opp
       - Module load: `node -e "require('./lib/core/opportunity-ops.cjs')"` succeeds
       - Opportunity listing: `node -e` calling listOpportunities on sample-room-opp returns count >= 1
       - Funding listing: `node -e` calling listFunding on sample-room-opp returns count >= 1
       - Frontmatter parsing: verify sample opportunity has required fields (funder, relevance_score, status)
       - Funding status parsing: verify sample funding entry has stage and source_opportunity fields
       - Wikilink cross-reference: grep confirms `[[opportunity-bank/` exists in funding STATUS.md

    5. Register the new test in `tests/run-all.sh` (add `bash tests/test-phase-13.sh` line).
  </action>
  <verify>
    <automated>ls references/opportunities/opportunity-template.md references/opportunities/funding-lifecycle.md tests/fixtures/sample-room-opp/opportunity-bank/2026-03-20-nsf-sbir.md tests/fixtures/sample-room-opp/funding/nsf-sbir-phase1/STATUS.md tests/test-phase-13.sh && echo "All files exist"</automated>
  </verify>
  <done>Reference templates define frontmatter schema and 4-stage lifecycle. Test fixtures provide a populated sample room. Test script exists and is registered in run-all.sh.</done>
</task>

<task type="auto">
  <name>Task 2: opportunity-ops.cjs core module</name>
  <files>lib/core/opportunity-ops.cjs</files>
  <action>
    Create `lib/core/opportunity-ops.cjs` following the exact pattern of room-ops.cjs and meeting-ops.cjs:
    - 'use strict', require path/fs/section-registry
    - Pure Node.js built-ins only (zero npm deps per Phase 10 decision)

    Exports:
    1. `listOpportunities(roomDir)` - Scan `room/opportunity-bank/` for .md files (excluding STATE.md). For each, parse YAML frontmatter (split on `---` delimiters, extract key fields). Return `{ opportunities: [{filename, funder, program, deadline, relevance_score, status}], count }`.

    2. `listFunding(roomDir)` - Scan `room/funding/` for subdirectories. For each, read STATUS.md and parse frontmatter (stage, outcome, source_opportunity, deadline). Return `{ entries: [{name, stage, outcome, deadline, source_opportunity}], count }`.

    3. `parseOpportunityFrontmatter(content)` - Parse YAML frontmatter from opportunity artifact markdown string. Return object with all schema fields. Use simple regex/split parsing (no yaml library -- follow existing codebase pattern of manual frontmatter parsing).

    4. `parseFundingStatus(content)` - Parse STATUS.md frontmatter. Return { stage, outcome, source_opportunity, deadline, last_updated, transition_history }.

    5. `getOpportunityBankState(roomDir)` - Read and return opportunity-bank/STATE.md content (or null if missing).

    6. `getFundingState(roomDir)` - Read and return funding/STATE.md content (or null if missing).

    Handle missing directories gracefully (return empty results, never throw). Follow the pattern in room-ops.cjs where missing sections return empty arrays.
  </action>
  <verify>
    <automated>bash tests/test-phase-13.sh</automated>
  </verify>
  <done>opportunity-ops.cjs loads without error. listOpportunities returns 1 opportunity from fixture. listFunding returns 1 entry from fixture. Frontmatter parsing extracts funder, relevance_score, stage fields correctly. All test-phase-13.sh tests pass.</done>
</task>

</tasks>

<verification>
- `node -e "const r = require('./lib/core/section-registry.cjs'); const d = r.discoverSections('./tests/fixtures/sample-room-opp'); console.assert(d.extended.includes('opportunity-bank')); console.assert(d.extended.includes('funding')); console.log('OK')"` prints OK
- `bash tests/test-phase-13.sh` passes all tests
- `bash tests/run-all.sh` still passes (no regressions)
</verification>

<success_criteria>
- opportunity-ops.cjs exports 6 functions, loads without error
- Section registry discovers both new sections in fixture room
- Reference templates define the full frontmatter schema and lifecycle
- Test fixtures provide realistic sample data for both sections
- All test-phase-13.sh assertions pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-opportunity-bank-funding-room/13-01-SUMMARY.md`
</output>
