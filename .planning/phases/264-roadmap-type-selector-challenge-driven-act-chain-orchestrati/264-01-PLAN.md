---
phase: 264-roadmap-type-selector-challenge-driven-act-chain-orchestrati
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/roadmap-type-chains.json
  - tests/test-264-roadmap-type-chains-drift.cjs
  - tests/run-all-264.sh
autonomous: true
requirements: [R2, R5, C-01]
canon_parts: [3, 7, 8]

must_haves:
  truths:
    - "A navigator's roadmap type resolves to a real framework-name chain that composes with zero null commands on a required step"
    - "A smuggled command slug or a fake framework name in the chain table fails CI"
    - "Phase 166's B3 contract is re-proved on every run of this phase's own suite"
    - "No file this phase touches carries a U+2014 em-dash"
  artifacts:
    - path: "data/roadmap-type-chains.json"
      provides: "Six roadmap-type slugs mapped to ordered framework-NAME arrays"
      contains: "technical-roadmap"
    - path: "tests/test-264-roadmap-type-chains-drift.cjs"
      provides: "Five-arm drift validator against command-resolver framework_index"
      contains: "commandsForFramework"
    - path: "tests/run-all-264.sh"
      provides: "Phase aggregator, run-all-166 passthrough, em-dash fence, chain-executor zero-diff arm"
      contains: "run-all-166.sh"
  key_links:
    - from: "tests/test-264-roadmap-type-chains-drift.cjs"
      to: "lib/workflow/command-resolver.cjs"
      via: "commandsForFramework / composeWorkflow / validateChainAutonomy"
      pattern: "command-resolver"
    - from: "tests/run-all-264.sh"
      to: "tests/run-all-166.sh"
      via: "embedded bash passthrough regression gate"
      pattern: "run-all-166\\.sh"
    - from: "tests/run-all-264.sh"
      to: "lib/core/chain-executor.cjs"
      via: "git diff against the phase base SHA"
      pattern: "chain-executor\\.cjs"
---

<objective>
Ship the committed roadmap-type to framework-name chain table (R2), its five-arm drift
validator, and this phase's test aggregator carrying the two constitutional gates every
later plan depends on: the `tests/run-all-166.sh` regression passthrough and the
`lib/core/chain-executor.cjs` zero-diff arm (R5), plus the no-em-dash fence (C-01).

Purpose: every later plan in Phase 264 needs a table to resolve against and a harness to
run. This plan is the substrate. It also front-loads the B3 proof so a violation surfaces
on wave 1, not at phase close.

Output: `data/roadmap-type-chains.json`, `tests/test-264-roadmap-type-chains-drift.cjs`,
`tests/run-all-264.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-SPEC.md
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-CONTEXT.md
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-RESEARCH.md
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Every symbol below is CREATED by Phase 264 and does not exist in the tree before it.
A drift or source-grounding pass must treat all of these as new, never as unacknowledged
references to prior art.

**New files**
- `data/roadmap-type-chains.json`
- `lib/core/salient-governance.cjs`
- `lib/core/sensors/sensor-roadmap-type.cjs`
- `tests/test-264-roadmap-type-chains-drift.cjs`
- `tests/test-264-salient-critic.cjs`
- `tests/test-264-roadmap-type-sensor.cjs`
- `tests/test-264-flagship-ralph.cjs`
- `tests/test-264-b3-frozen.cjs`
- `tests/test-264-sensor-to-chain-resolve.cjs`
- `tests/run-all-264.sh`

**New exported functions and constants**
- `sensorRoadmapType`, `classifyRoadmapType`, `ROADMAP_TYPES`, `ROADMAP_PATTERNS`,
  `ROADMAP_PROBLEM_TYPES`, `ROADMAP_CHAIN_TABLE_PATH`, `chainForRoadmapType`
  (`lib/core/sensors/sensor-roadmap-type.cjs`, and `sensorRoadmapType` re-exported from
  `lib/core/insight-sensors.cjs`)
- `SALIENT_GOVERNANCE`, `SALIENT_PASSES`, `RS_DIRECTIONS`, `governanceForPass`,
  `findingFromResult`, `enforceSalientGovernance`, `makeSalientSelfCritiqueFn`
  (`lib/core/salient-governance.cjs`)

**New registry / enum values**
- Sensor id `SENS-18` (added to `SENSOR_REGISTRY_IDS` and to `SENS_PRIORITY` Group C)
- Reach `dispatch` handle `roadmap-type-selector`; reach `signal` value
  `roadmap_type_classified`
- Reach `evidence` keys: `roadmap_type`, `mode`, `score`, `chain_len`, `tie_broken`
- Roadmap-type enum slugs: `landscape-analysis`, `technical-roadmap`, `pipeline-analysis`,
  `opportunity-analysis`, `agenda-setting-manifesto`, `vision-paper`
- Critic pass names: `neutral`, `adversarial`
- Critic violation strings: `rs_finding_unrecognized`, `rs_no_id`, `rs_no_source_artifact`,
  `rs_no_target_artifact`, `rs_direction_missing`, `rs_diff_not_numeric`,
  `rs_body_text_empty`, `rs_self_referential`, `rs_diff_sign_mismatch`,
  `rs_diff_below_floor`, `rs_direction_unrecognized`, `rs_pass_disagreement`,
  `rs_pass_unrecognized`, `rs_step_not_governed`
- Shell env overrides: `TEST_264_PREFIX`, `TEST_264_ALLOW_MISSING`
- Shell constant `PHASE_264_BASE_SHA` in `tests/run-all-264.sh`

## Corrected paths (RESEARCH Open Question 5)

`264-CONTEXT.md`'s Canonical References carry two wrong paths. Use these instead:
- `lib/agents/reverse-salient-agent.cjs` (NOT `lib/core/reverse-salient-agent.cjs`)
- `lib/core/bono/debate-composition.cjs` (NOT bare `debate-composition.cjs`)

<tasks>

<task type="auto">
  <name>Task 1: Author data/roadmap-type-chains.json with the six live-verified chains</name>
  <files>data/roadmap-type-chains.json</files>
  <read_first>
    - data/dispatch-framework-map.json (the 18-line hand-authored precedent this file mirrors: single flat object, one `_note` key, then literal key-value pairs, no nesting, no schema version, no `generated_note`)
    - .planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-RESEARCH.md section F-06 (the six live-executed resolutions) and Pitfall 3 (the three trail chains that mis-resolve)
    - lib/workflow/command-resolver.cjs (`commandsForFramework` at :85, `composeWorkflow` at :110, `validateChainAutonomy` at :131 -- the only door from a framework name to a command)
  </read_first>
  <action>
Create `data/roadmap-type-chains.json` as a single flat JSON object. First key is `_note`,
then exactly six keys, one per roadmap-type slug. Values are ordered arrays of framework
NAMES (never command slugs, never a direct read into `data/command-registry.json`) per
D-05 and the "the resolver is the only door" doctrine.

Key naming (Claude's Discretion item 1, resolved here): kebab-case slugs, identical to the
`evidence.roadmap_type` enum values the sensor will emit in plan 264-03. Six keys exactly:

  landscape-analysis
  technical-roadmap
  pipeline-analysis
  opportunity-analysis
  agenda-setting-manifesto
  vision-paper

The six chains, taken verbatim from RESEARCH F-06 (each was executed live against
`data/command-registry.json` in the research session and re-verified during planning; do
NOT substitute the research trail's prose, three rows of which mis-resolve per Pitfall 3):

  landscape-analysis:
    ["Usher's Model of Cumulative Synthesis", "HSI Semantic Surprise Analysis Assistant"]
  technical-roadmap:
    ["Problem Definition Transformation Framework", "Reverse Salient Analysis",
     "Knowns and Unknowns Matrix Framework"]
  pipeline-analysis:
    ["Systems Thinking", "Reverse Salient Analysis", "S-Curve Analysis"]
  opportunity-analysis:
    ["PWS Value Proposition"]
  agenda-setting-manifesto:
    ["Beautiful Question Framework", "MECE (Mutually Exclusive, Collectively Exhaustive)"]
  vision-paper:
    ["Scenario Planning", "Four Lenses of Innovation"]

The `_note` string must state, in hyphens-only prose, all five of these facts:
  1. values are framework NAMES validated against `command-resolver.cjs`'s `framework_index`
     via `commandsForFramework`, never slugs and never commands (D-05);
  2. the file is hand-authored, NOT generated, because there is no frontmatter source of
     truth to generate from (D-04) -- contrast `data/command-registry.json`;
  3. it is drift-tested by `tests/test-264-roadmap-type-chains-drift.cjs`, which is wired
     explicitly into `tests/run-all-264.sh`;
  4. `agenda-setting-manifesto` deliberately ships as a 2-step chain because `/mos:present`
     has an empty `frameworks` array in the registry and therefore cannot appear in a
     framework-name chain at all (RESEARCH Open Question 3; adding a framework to it would
     mean editing command frontmatter and regenerating the registry, which is outside this
     phase's boundaries);
  5. `Futures Wheel` and `Six Thinking Hats` are the two framework_index names that are NOT
     autonomous-safe and must never be added to a chain here, because they would flip
     `validateChainAutonomy(...).runnable` to false.

House rule: hyphens only, no em-dashes, in the `_note` and everywhere else.
  </action>
  <verify>
    <automated>node -e "const r=require('./lib/workflow/command-resolver.cjs');const m=require('./data/roadmap-type-chains.json');const ks=Object.keys(m).filter(k=>k!=='_note');if(ks.length!==6)throw new Error('expected 6 keys, got '+ks.length);for(const k of ks){const wf=r.composeWorkflow(m[k]);const nulls=wf.filter(s=>s.command===null&&s.optional!==true);if(nulls.length)throw new Error(k+' has required null command');if(!r.validateChainAutonomy(wf).runnable)throw new Error(k+' not runnable');}console.log('OK 6 chains resolve');"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('./data/roadmap-type-chains.json')"` exits 0 (valid JSON).
    - `Object.keys(map).filter(k => k !== '_note').length === 6` and the six keys are exactly
      `landscape-analysis`, `technical-roadmap`, `pipeline-analysis`, `opportunity-analysis`,
      `agenda-setting-manifesto`, `vision-paper`.
    - `composeWorkflow(map['technical-roadmap']).map(s => s.command)` deep-equals
      `['/mos:diagnose', '/mos:find-bottlenecks', '/mos:map-unknowns']`.
    - For all six chains, `composeWorkflow(chain)` yields zero steps where
      `command === null && optional !== true`.
    - For all six chains, `validateChainAutonomy(composeWorkflow(chain)).runnable === true`.
    - Every value is an array whose elements are all strings; no element contains the
      substring `mos:`.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' data/roadmap-type-chains.json` reports 0.
  </acceptance_criteria>
  <done>The six roadmap types each resolve, through the resolver and only the resolver, to a runnable command chain with no required null step.</done>
</task>

<task type="auto">
  <name>Task 2: Write the five-arm chain-table drift validator</name>
  <files>tests/test-264-roadmap-type-chains-drift.cjs</files>
  <read_first>
    - tests/test-dispatch-framework-map-drift.cjs (87 lines: clone its `check(cond, msg)` plus exit-code idiom verbatim, including the `Object.keys(map).filter(k => k !== '_note')` guard, which is load-bearing -- without it the `_note` string is treated as a chain and fails every arm)
    - data/roadmap-type-chains.json (the file just authored in Task 1)
    - lib/workflow/command-resolver.cjs (exports: `commandsForFramework`, `frameworksForCommand`, `composeWorkflow`, `validateChainAutonomy`, `__reset`)
    - .planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-RESEARCH.md section F-08 (the five arms) and Pitfall 3
  </read_first>
  <action>
Create `tests/test-264-roadmap-type-chains-drift.cjs` as a plain `node` script (no test
framework, no `node:test`) using the house `check(cond, msg)` counter plus
`process.exit(failures > 0 ? 1 : 0)` idiom from `tests/test-dispatch-framework-map-drift.cjs`.

Require `data/roadmap-type-chains.json` and `lib/workflow/command-resolver.cjs` through
`path.resolve(__dirname, '..')` so the script runs from any cwd.

Filter `_note` out of the key set FIRST: `const types = Object.keys(map).filter(function (k) { return k !== '_note'; });`

Five arms (D-05 and F-08; the donor has three, this expands to five):

  Arm 1 -- dangling-name check. For every framework name in every chain,
  `commandsForFramework(name)` returns a non-empty array. Validate against
  `framework_index` through `commandsForFramework`, NEVER against
  `data/framework-names.json`: a name can pass a bare-name allowlist and still degrade to
  `{command: null, optional: true}` at resolve time (`command-resolver.cjs:110-121`), which
  is exactly the failure R3's acceptance forbids (D-05).

  Arm 2 -- no slug smuggling. No framework name starts with `mos:` or contains `/mos:`.

  Arm 3 -- shape. Exactly six keys besides `_note`; the six are exactly the slugs
  `landscape-analysis`, `technical-roadmap`, `pipeline-analysis`, `opportunity-analysis`,
  `agenda-setting-manifesto`, `vision-paper`; every value is a non-empty array of strings.

  Arm 4 -- no intra-chain duplicate framework name (Pitfall 3). The SPEC's own acceptance
  only forbids `command: null` on a required step, so a chain listing the same framework
  twice would pass the letter of the acceptance while shipping a nonsense chain
  (the research trail's Vision Paper draft did exactly this with `Scenario Planning`).
  Note that the SAME framework may legitimately appear in TWO DIFFERENT chains
  (`Reverse Salient Analysis` is in both `technical-roadmap` and `pipeline-analysis`);
  the arm is per-chain, never cross-chain.

  Arm 5 -- autonomy. `validateChainAutonomy(composeWorkflow(chain)).runnable === true` for
  all six, and zero steps where `command === null && optional !== true`.

Also assert the `_note` key exists and is a non-empty string, and that it contains the
literal substring `tests/test-264-roadmap-type-chains-drift.cjs` (so the data file always
points back at its own fence).

Footer on success: `process.stdout.write('roadmap-type-chains drift: PASS (' + types.length + ' roadmap types)\n')` then `process.exit(0)`.
Header block: a numbered "Behaviors covered" list and the closing line
`House rule: hyphens only, no em-dashes.`
  </action>
  <verify>
    <automated>node tests/test-264-roadmap-type-chains-drift.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-264-roadmap-type-chains-drift.cjs` exits 0 and its final stdout line
      matches `roadmap-type-chains drift: PASS (6 roadmap types)`.
    - The script contains the literal `filter` expression excluding `'_note'` from the key
      set (verify: `grep -c "_note" tests/test-264-roadmap-type-chains-drift.cjs` is at
      least 2).
    - The script requires `lib/workflow/command-resolver.cjs` and calls
      `commandsForFramework`; it does NOT require `data/framework-names.json`
      (verify: `grep -v '^ \*' tests/test-264-roadmap-type-chains-drift.cjs | grep -c "framework-names.json"` reports 0).
    - Negative proof, run manually once and recorded in the SUMMARY: temporarily appending a
      seventh key to a copy of the table makes Arm 3 fail and the script exit 1.
    - Negative proof, run manually once and recorded in the SUMMARY: temporarily duplicating
      `Reverse Salient Analysis` inside the `technical-roadmap` chain in a copy makes Arm 4
      fail and the script exit 1.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' tests/test-264-roadmap-type-chains-drift.cjs` reports 0.
  </acceptance_criteria>
  <done>A smuggled slug, a fake framework name, a seventh roadmap type, an intra-chain duplicate, or a non-runnable chain each fail the script with exit code 1.</done>
</task>

<task type="auto">
  <name>Task 3: Build tests/run-all-264.sh with the B3 regression, zero-diff, and em-dash gates</name>
  <files>tests/run-all-264.sh</files>
  <read_first>
    - tests/run-all-259.sh (136 lines, the structural donor: header doctrine block, `TEST_259_PREFIX` override, `run()` helper, nullglob discovery loop, the load-bearing `found -eq 0` guard, the `EMDASH_TARGETS` fence with its `rc >= 2` SCAN-BROKE arm, the PASS/FAIL/SKIP footer)
    - tests/run-all-166.sh lines 94-107 (proof that this repo's sibling suites are invoked with bare `node "$p"`, NOT `node --test`)
    - tests/run-all-158.sh lines 185-190 (the shipped precedent for embedding another run-all script as a passthrough regression gate)
    - .planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-PATTERNS.md section `tests/run-all-264.sh`
  </read_first>
  <action>
Create `tests/run-all-264.sh` (mode 0755) by cloning `tests/run-all-259.sh`'s structure with
these concrete changes.

HEADER: a "WHAT THIS PHASE HAS TO PROVE, in one sentence each" numbered list covering the
five SPEC requirements; a DISCOVERY IS BY GLOB statement; the six mandatory suites
enumerated by filename even though the glob does the discovery
(`tests/test-264-roadmap-type-chains-drift.cjs`, `tests/test-264-salient-critic.cjs`,
`tests/test-264-roadmap-type-sensor.cjs`, `tests/test-264-flagship-ralph.cjs`,
`tests/test-264-b3-frozen.cjs`, `tests/test-264-sensor-to-chain-resolve.cjs`); an explicit
statement that the `found -eq 0` guard is load-bearing and must not be softened.
House rule: hyphens only, no em-dashes, including in this header.

RUNNER: `set -uo pipefail`; `ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"; cd "$ROOT"`;
`PREFIX="${TEST_264_PREFIX:-tests/test-264-}"`; the donor's `run()` helper verbatim;
`PASS`/`FAIL`/`SKIP` counters.

DISCOVERY: nullglob loop over `"$PREFIX"*.cjs` invoked as `run "$(basename "$t")" node "$t"`.
DIVERGENCE FROM THE DONOR, deliberate (RESEARCH F-19 / A6): bare `node "$t"`, NOT
`node --test "$t"` -- every suite this phase writes is a plain script that throws or calls
`process.exit(1)`, not a `node:test` file. Do not "fix" this back toward the donor.
Keep the `found -eq 0` guard exactly as the donor has it.

EXPLICIT GATE LINES (these are pre-existing, always-present files, so list them explicitly
rather than relying on the glob), each through `run`:
  run "201 bounded-retry (ralph_verify contract)"  node tests/test-201-bounded-retry.cjs
  run "245 priority completeness (3-array lockstep)" node tests/test-245-priority-complete.cjs
  run "connector registry born-wired --check"      node scripts/build-connector-registry.cjs --check
  run "sensors routing fence (Phase 144)"          node tests/test-sensors-routing-fence.cjs
  run "sensors Canon Part 8 sweep"                 node tests/test-sensors-part8-sweep.cjs

REGRESSION PASSTHROUGH (SPEC R5, pattern from run-all-158.sh:187-188):
  run "frozen-166 passthrough (B3 / Canon Part 3 contract)" bash "$ROOT/tests/run-all-166.sh"
This suite is green at 23/23 in about 3 seconds as of the phase base commit; a red result
here means this phase broke the Phase 166 contract.

CHAIN-EXECUTOR ZERO-DIFF ARM (SPEC R5 acceptance, verbatim: "git diff for this phase touches
zero lines inside lib/core/chain-executor.cjs's gate/stop-condition functions"). Record the
phase base commit as a literal:
  PHASE_264_BASE_SHA="c7c33eea449f6f227c4cfbb86f220acaac9b5ab8"
Then, in its own labelled block: if `git cat-file -e "$PHASE_264_BASE_SHA^{commit}"` succeeds,
run `git diff --quiet "$PHASE_264_BASE_SHA" -- lib/core/chain-executor.cjs` and count PASS on
exit 0, FAIL on any non-zero exit (printing the offending `git diff --stat` output). If the
base commit is unreachable (shallow clone or a rewritten history), print a loud
`SKIPPED (base commit unreachable)` line and increment SKIP, never PASS. A silent skip is
forbidden.

NO-EM-DASH FENCE (C-01): clone the donor's fence at run-all-259.sh:96-131 including the
`rc >= 2` SCAN BROKE arm (a broken `grep -P` must FAIL, never silently pass). Replace
`EMDASH_TARGETS` with this phase's eleven files:
  data/roadmap-type-chains.json
  lib/core/salient-governance.cjs
  lib/core/sensors/sensor-roadmap-type.cjs
  lib/core/insight-sensors.cjs
  lib/core/sensors/sensor-priority.cjs
  tests/test-264-roadmap-type-chains-drift.cjs
  tests/test-264-salient-critic.cjs
  tests/test-264-roadmap-type-sensor.cjs
  tests/test-264-flagship-ralph.cjs
  tests/test-264-b3-frozen.cjs
  tests/test-264-sensor-to-chain-resolve.cjs
  tests/run-all-264.sh
DIVERGENCE FROM THE DONOR, deliberate: the donor prints "(skipped, not yet created)" for a
missing target and moves on, which would let a typo'd path skip forever. This phase counts
missing targets into an `EMDASH_MISSING` variable and FAILS the fence when
`EMDASH_MISSING > 0`, UNLESS `TEST_264_ALLOW_MISSING=1` is set in the environment. Waves 1
and 2 run with `TEST_264_ALLOW_MISSING=1`; the phase's final gate (plan 264-05) runs without
it, which is what proves all eleven files actually exist.

FOOTER: the donor's three-line banner reading `Phase 264: PASS=$PASS FAIL=$FAIL SKIP=$SKIP`
then `[ "$FAIL" -eq 0 ]`.

Finally `chmod 0755 tests/run-all-264.sh`.
  </action>
  <verify>
    <automated>TEST_264_ALLOW_MISSING=1 bash tests/run-all-264.sh</automated>
  </verify>
  <acceptance_criteria>
    - `TEST_264_ALLOW_MISSING=1 bash tests/run-all-264.sh` exits 0 and its footer prints
      `FAIL=0`.
    - The run output contains a line naming `frozen-166 passthrough` with `PASSED`, and the
      embedded 166 summary reporting `Failed: 0`.
    - The run output contains a labelled chain-executor zero-diff block reporting PASSED
      (the base commit `c7c33eea449f6f227c4cfbb86f220acaac9b5ab8` is reachable in this
      working tree).
    - `bash tests/run-all-264.sh` WITHOUT `TEST_264_ALLOW_MISSING` exits non-zero at this
      point in the phase, because nine of the eleven em-dash targets do not exist yet. This
      is the expected wave-1 state and proves the missing-target arm is live.
    - `TEST_264_PREFIX=tests/test-264-nonexistent- TEST_264_ALLOW_MISSING=1 bash tests/run-all-264.sh`
      exits non-zero, proving the `found -eq 0` guard is not softened.
    - `grep -v '^#' tests/run-all-264.sh | grep -c 'node --test'` reports 0 (the deliberate
      F-19 divergence: bare `node`, never `node --test`).
    - `test -x tests/run-all-264.sh` succeeds.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' tests/run-all-264.sh` reports 0.
  </acceptance_criteria>
  <done>The phase harness exists, discovers by glob with a provable found-eq-0 guard, re-runs Phase 166's full contract, and fails loudly on any chain-executor drift or any em-dash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed data file to resolver | `data/roadmap-type-chains.json` values are fed to `composeWorkflow`, which turns them into executable command slugs |
| test harness to constitutional contract | `tests/run-all-264.sh` is the only automated proof that Phase 166's B3 stop-condition survives this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-264-01 | Spoofing | `data/roadmap-type-chains.json` | mitigate | Drift Arm 2 rejects any value starting `mos:` or containing `/mos:`, so a command slug cannot masquerade as a framework name |
| T-264-02 | Tampering | `data/roadmap-type-chains.json` | mitigate | Drift Arms 1 and 5 pin every name to the live `framework_index` via `commandsForFramework` and require `validateChainAutonomy(...).runnable === true`, so an injected non-autonomous framework (`Futures Wheel`, `Six Thinking Hats`) reddens CI |
| T-264-03 | Elevation of Privilege | `lib/core/chain-executor.cjs` gate/stop functions | mitigate | The zero-diff arm in `run-all-264.sh` diffs the file against `PHASE_264_BASE_SHA` on every run; an unreachable base SHA counts as SKIP, never PASS |
| T-264-04 | Repudiation | no-em-dash fence | mitigate | `rc >= 2` SCAN BROKE arm treats a broken `grep -P` as FAIL, and `EMDASH_MISSING > 0` fails the fence unless explicitly allowed, so a typo'd target path cannot skip silently |
| T-264-05 | Denial of Service | `run-all-264.sh` | accept | Runtime is about 10 seconds dominated by the embedded 166 suite; no network, no unbounded loop |
| T-264-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero packages (RESEARCH Package Legitimacy Audit: not applicable, nothing to check). No install task exists in this plan. |

## Canon Part 8

Zero Brain egress. The chain table holds generic framework-name handles only; the drift test
reads local JSON and a local resolver; the aggregator makes no network call.
</threat_model>

<verification>
1. `node tests/test-264-roadmap-type-chains-drift.cjs` exits 0.
2. `TEST_264_ALLOW_MISSING=1 bash tests/run-all-264.sh` exits 0 with `FAIL=0`.
3. `bash tests/run-all-166.sh` exits 0 with `Failed: 0` (unmodified, 23/23).
4. `git diff --quiet c7c33eea449f6f227c4cfbb86f220acaac9b5ab8 -- lib/core/chain-executor.cjs` exits 0.
5. `TEST_264_PREFIX=tests/test-264-nonexistent- TEST_264_ALLOW_MISSING=1 bash tests/run-all-264.sh` exits non-zero.
</verification>

<success_criteria>
- `data/roadmap-type-chains.json` exists with exactly six roadmap-type keys plus `_note`;
  all six chains compose with zero required-null commands and `runnable: true`.
- `tests/test-264-roadmap-type-chains-drift.cjs` exits 0 and fails on a seventh key, on an
  intra-chain duplicate, on a `mos:` slug, on a dangling name, and on a non-runnable chain.
- `tests/run-all-264.sh` is executable, discovers by glob with a provable found-eq-0 guard,
  embeds the `run-all-166.sh` passthrough, carries the chain-executor zero-diff arm against
  `c7c33eea449f6f227c4cfbb86f220acaac9b5ab8`, and carries the no-em-dash fence over all
  eleven phase files with the missing-target arm live.
- Zero U+2014 em-dashes in all three files.
- Zero lines changed in `lib/core/chain-executor.cjs`.
</success_criteria>

<output>
Create `.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-01-SUMMARY.md` when done.
</output>
