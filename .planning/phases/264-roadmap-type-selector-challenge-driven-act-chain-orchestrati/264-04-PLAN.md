---
phase: 264-roadmap-type-selector-challenge-driven-act-chain-orchestrati
plan: 04
type: execute
wave: 2
depends_on: ["264-01", "264-02"]
files_modified:
  - tests/test-264-flagship-ralph.cjs
  - tests/test-264-b3-frozen.cjs
autonomous: true
requirements: [R4, R5]
canon_parts: [3, 7]

must_haves:
  truths:
    - "The Technical Roadmap chain's find-bottlenecks step runs under ralph_verify with a real adversarial critic and retries exactly once when the first candidate fails"
    - "A candidate that never passes critique forces a halt, never a silent pass"
    - "A material step with ralph_verify is never retried, so B3 and Canon Part 3 stay intact"
    - "chain-executor.cjs's gate and stop-condition functions are byte-identical to the phase base commit"
  artifacts:
    - path: "tests/test-264-flagship-ralph.cjs"
      provides: "Direct synchronous runChain proof of challenge-driven execution on find-bottlenecks"
      contains: "ralph_verify"
    - path: "tests/test-264-b3-frozen.cjs"
      provides: "Source pin over chain-executor's gate and stop-condition functions"
      contains: "_ralphSafeRetry"
  key_links:
    - from: "tests/test-264-flagship-ralph.cjs"
      to: "lib/core/chain-executor.cjs"
      via: "direct synchronous runChain call with no roomDir/journal/retries/resume/sleep"
      pattern: "runChain"
    - from: "tests/test-264-flagship-ralph.cjs"
      to: "lib/core/salient-governance.cjs"
      via: "makeSalientSelfCritiqueFn supplied as selfCritiqueFn"
      pattern: "salient-governance"
    - from: "tests/test-264-flagship-ralph.cjs"
      to: "data/roadmap-type-chains.json"
      via: "composeWorkflow over the technical-roadmap chain, then ralph_verify grafted onto the find-bottlenecks step"
      pattern: "technical-roadmap"
---

<objective>
Prove challenge-driven execution end to end (SPEC Requirement 4) and prove that doing so did
not touch the B3 / Canon Part 3 stop condition (SPEC Requirement 5). Two test files, zero
production-code changes.

Purpose: `ralph_verify` shipped in Phase 201 and has been exercised only by its own fixture.
This plan is the first time the shipped seam runs against a REAL chain composed from the
committed table with a REAL adversarial critic, and the first time the constitutional
constraint is verified rather than asserted.

Output: `tests/test-264-flagship-ralph.cjs`, `tests/test-264-b3-frozen.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-SPEC.md
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-CONTEXT.md
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-RESEARCH.md
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-PATTERNS.md
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-01-SUMMARY.md
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Every symbol below is CREATED by Phase 264 and does not exist in the tree before it.
A drift or source-grounding pass must treat all of these as new, never as unacknowledged
references to prior art.

**New files**
- `data/roadmap-type-chains.json`
- `lib/core/salient-governance.cjs`
- `lib/core/sensors/sensor-roadmap-type.cjs`
- `tests/test-264-roadmap-type-chains-drift.cjs`
- `tests/test-264-salient-critic.cjs`
- `tests/test-264-roadmap-type-sensor.cjs`
- `tests/test-264-flagship-ralph.cjs`
- `tests/test-264-b3-frozen.cjs`
- `tests/test-264-sensor-to-chain-resolve.cjs`
- `tests/run-all-264.sh`

**New exported functions and constants**
- `sensorRoadmapType`, `classifyRoadmapType`, `ROADMAP_TYPES`, `ROADMAP_PATTERNS`,
  `ROADMAP_PROBLEM_TYPES`, `ROADMAP_CHAIN_TABLE_PATH`, `chainForRoadmapType`
  (`lib/core/sensors/sensor-roadmap-type.cjs`, and `sensorRoadmapType` re-exported from
  `lib/core/insight-sensors.cjs`)
- `SALIENT_GOVERNANCE`, `SALIENT_PASSES`, `RS_DIRECTIONS`, `governanceForPass`,
  `findingFromResult`, `enforceSalientGovernance`, `makeSalientSelfCritiqueFn`
  (`lib/core/salient-governance.cjs`)

**New registry / enum values**
- Sensor id `SENS-18` (added to `SENSOR_REGISTRY_IDS` and to `SENS_PRIORITY` Group C)
- Reach `dispatch` handle `roadmap-type-selector`; reach `signal` value
  `roadmap_type_classified`
- Reach `evidence` keys: `roadmap_type`, `mode`, `score`, `chain_len`, `tie_broken`
- Roadmap-type enum slugs: `landscape-analysis`, `technical-roadmap`, `pipeline-analysis`,
  `opportunity-analysis`, `agenda-setting-manifesto`, `vision-paper`
- Critic pass names: `neutral`, `adversarial`
- Critic violation strings: `rs_finding_unrecognized`, `rs_no_id`, `rs_no_source_artifact`,
  `rs_no_target_artifact`, `rs_direction_missing`, `rs_diff_not_numeric`,
  `rs_body_text_empty`, `rs_self_referential`, `rs_diff_sign_mismatch`,
  `rs_diff_below_floor`, `rs_direction_unrecognized`, `rs_pass_disagreement`,
  `rs_pass_unrecognized`, `rs_step_not_governed`
- Shell env overrides: `TEST_264_PREFIX`, `TEST_264_ALLOW_MISSING`
- Shell constant `PHASE_264_BASE_SHA` in `tests/run-all-264.sh`

## The honest consequence, stated plainly (D-11)

`lib/mcp/tools/chain.cjs::chainRun` is INERT for `ralph_verify` today, for TWO independent
reasons, both confirmed by direct code read:
1. `chain.cjs:269` sets `roomDir` to `process.cwd()` when no roomDir is supplied, so `roomDir`
   is ALWAYS a non-empty string and is passed unconditionally at `:290`. That trips
   `runChain`'s async dispatch at `chain-executor.cjs:448-456` into `_runChainResilient`,
   where `_ralphSafeRetry` does not exist.
2. `chainRun` never passes a `selfCritiqueFn` at all; its opts object carries only
   `postureFn`, `onStep`, `maxSteps`, `roomDir`, `onHalt`.

Therefore: a navigator running the Technical Roadmap chain through the LIVE
`chain_resolve -> chain_run` flow will NOT get `ralph_verify` benefits from this phase alone.
Requirement 4's acceptance is proven via a scoped direct `runChain()` call, mirroring the
shipped precedent at `lib/core/bono/debate-composition.cjs:367-375`. This is not a workaround
hidden in a test; it is the navigator-confirmed resolution recorded in D-11, and both reasons
above MUST be restated verbatim in `tests/test-264-flagship-ralph.cjs`'s header block so a
future reader is not misled about what is live.

"Make `chain_run`'s async path honor `ralph_verify`" is a NAMED FOLLOW-ON PHASE. Do not do it
here: it would touch `chain-executor.cjs`'s core logic, which SPEC.md's Boundaries forbid.

<tasks>

<task type="auto">
  <name>Task 1: Write the flagship direct-runChain challenge-driven proof</name>
  <files>tests/test-264-flagship-ralph.cjs</files>
  <read_first>
    - tests/test-201-bounded-retry.cjs (118 lines, the assertion-for-assertion donor: the `ok(desc, fn)` counter, `safePosture`/`materialPosture`, the `makeOnStep(passAt)` attempt-counting factory, and the five cases this plan ports)
    - lib/core/chain-executor.cjs lines 285-335 (`RALPH_RETRY_CAP_DEFAULT = 2`, `_safeCritique`, `_critiqueFailed`, `_ralphSafeRetry`: the loop is synchronous and never awaits, so `onStep` and `selfCritiqueFn` must both be synchronous)
    - lib/core/chain-executor.cjs lines 440-470 and 585-610 (the sync-vs-async dispatch branch, and the `ralph_verify` opt-in preconditions at :591-592)
    - lib/core/chain-executor.cjs lines 196-240 (`IRREVERSIBLE_HINTS`, `isIrreversibleStep`, `_isMaterialStep`: a step is material if irreversible, or `step.material === true`, or `posture.posture !== 'run'`, and an ABSENT posture counts as material)
    - lib/core/bono/debate-composition.cjs lines 340-380 (the direct-`runChain` opts precedent and the injected-`postureFn` idiom; note the path is `lib/core/bono/`)
    - lib/core/salient-governance.cjs (built in plan 264-02: `makeSalientSelfCritiqueFn`)
    - data/roadmap-type-chains.json (built in plan 264-01: the `technical-roadmap` chain)
    - lib/agents/reverse-salient-agent.cjs lines 255-270 (the RS finding shape `onStep` must emit)
  </read_first>
  <action>
Create `tests/test-264-flagship-ralph.cjs` as a plain `node` script (no framework, no
`node:test`) using `node:assert/strict` and the donor's `ok(desc, fn)` counter, with the
footer `console.log('\nPASS test-264-flagship-ralph (' + n + ' assertions)');`.

HEADER BLOCK: numbered "Behaviors covered"; the D-11 honest-consequence statement copied
verbatim from this plan's section above, including BOTH reasons `chain_run` is inert and the
naming of the follow-on phase; a statement that this file changes zero production code; and
the closing line `House rule: hyphens only, no em-dashes.`

STEP COMPOSITION (RESEARCH F-10): `composeWorkflow` never emits `ralph_verify`, so graft it:
  read the `technical-roadmap` chain from `data/roadmap-type-chains.json`, call
  `composeWorkflow(chain)` from `lib/workflow/command-resolver.cjs`, then
  `.map(function (s) { return (s.command === '/mos:find-bottlenecks') ? Object.assign({}, s, { ralph_verify: true }) : s; })`.
  Assert before running that the mapped array contains exactly one step with
  `ralph_verify === true` and that its command is `/mos:find-bottlenecks`, so a future change
  to the table cannot silently turn this into a test of nothing.

CALL SHAPE, direct and synchronous (`debate-composition.cjs:367-375` precedent). The
load-bearing property is what is ABSENT: NO `roomDir`, NO `journal`, NO `retries`, NO
`resume`, NO `sleep`. Any one of those five routes the call to the async `_runChainResilient`
path where `_ralphSafeRetry` does not exist. Pass only:
  `{ onStep, postureFn, selfCritiqueFn, onHalt, ralphRetryCap }`
State this ABSENCE requirement in a comment at the call site; it is the single easiest thing
for a later editor to break by "helpfully" adding a roomDir.

FIXTURE HELPERS:
  `safePosture` -- `function () { return { command: 'c', autonomous_safe: true, posture: 'run' }; }`
  This is required, not cosmetic: `_isMaterialStep` treats an ABSENT posture as material
  (withhold-default), so a step without a resolvable `posture: 'run'` never retries. The
  three Technical Roadmap commands are all `autonomous_safe: true` in
  `data/command-registry.json`, and `/mos:find-bottlenecks` contains none of the frozen
  `IRREVERSIBLE_HINTS` (`email`, `deploy`, `publish`, `send`, `release`, `external-write`,
  `external_write`), so the opt-in branch fires.
  `makeRsOnStep(passAt)` -- the donor's attempt-counting factory, but emitting an RS-shaped
  finding on `chain_output`. It MUST be synchronous (it is called inside the sync retry loop).
  Attempts before `passAt` emit a finding that the critic FAILS on a named structural defect
  (use `source_artifact_id === target_artifact_id`, which trips `rs_self_referential` in the
  adversarial pass while the neutral pass stays clean, so the failure also exercises the
  two-pass disagreement path). Attempts at or after `passAt` emit a clean eight-field finding.
  Expose `fn.count()` exactly as the donor does.
  `selfCritiqueFn` -- `makeSalientSelfCritiqueFn({ commandFilter: '/mos:find-bottlenecks' })`
  from `lib/core/salient-governance.cjs`. The filter is what keeps the other two Technical
  Roadmap steps from being failed by an RS-shaped rule set they were never meant to satisfy.

CASES, at minimum these seven. The first five are ports of `test-201-bounded-retry.cjs`'s
cases onto the real composed chain; the last two are this phase's own.

  1. R4 "exactly one retry (cap honored), then a passing verdict". With
     `makeRsOnStep(2)` (fails once, passes on the second attempt) and the default cap:
     assert `onStep.count()` for the find-bottlenecks step is exactly 2, meaning ONE retry.
     Do NOT lower `ralphRetryCap` to 1 to force this; that would change the tested contract.
     The shipped default is `RALPH_RETRY_CAP_DEFAULT = 2`, and a first-fails/second-passes
     fixture is what produces exactly one retry.
  2. R4/R5 "or a forced LOW_QUALITY halt". With `makeRsOnStep(99)` (never passes) and
     `ralphRetryCap: 2`: assert `res.completed === false`,
     `res.haltedAt.reason === 'retry_exhausted'`, the find-bottlenecks trace entry's
     `quality === 'low'`, and `onStep.count()` for that step is exactly 3 (1 initial plus 2
     retries: cap honored, never more). This is the proof that exhaustion HALTS rather than
     silently proceeding.
  3. R5 "non-opted-in steps are byte-identical". Run the same composed chain with NO
     `ralph_verify` grafted anywhere and a never-passing `onStep`: assert the step is called
     exactly once, proving the default-off token economy is untouched.
  4. R5 B3 proof, material step. Add `material: true` to the find-bottlenecks step, supply a
     `materialPosture` returning `{ autonomous_safe: false, posture: 'halt' }`, keep
     `ralph_verify: true`: assert `res.completed === false` and `onStep.count() === 0`. The
     gate halts BEFORE `onStep`; a material step is never retried.
  5. R5 B3 proof, irreversible step. Run a one-step chain
     `[{ step: 1, command: 'deploy-thing', ralph_verify: true }]` with `safePosture`: assert
     `res.haltedAt.reason === 'forced_material'` and `onStep.count() === 0`.
  6. Budget brake is distinguishable from cap exhaustion. With `ralphRetryCap: 10` and
     `maxSteps: 2` on a never-passing single-step chain: assert
     `res.haltedAt.reason === 'budget_brake'`, NOT `'retry_exhausted'`. This pins that this
     phase did not collapse the two halt reasons.
  7. The critic is genuinely engaged, not bypassed. Wrap the `selfCritiqueFn` in a counting
     proxy and assert it was called at least twice in case 1 (once on the initial result,
     once on the retried result), and that every value it returned satisfied
     `typeof verdict.then !== 'function'`. Absence of an error proves nothing on this seam:
     a Promise verdict would make `_critiqueFailed` return false and every candidate would
     pass silently with no log. Assert the positive.

  Also assert, once, near the top: `require('lib/core/chain-executor.cjs').runChain` is a
  function and calling it with the flagship opts returns a plain object with
  `typeof res.then !== 'function'` (proving the call stayed on the SYNC path and did not fall
  through to `_runChainResilient`).
  </action>
  <verify>
    <automated>node tests/test-264-flagship-ralph.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-264-flagship-ralph.cjs` exits 0 and its final line matches
      `PASS test-264-flagship-ralph (` with at least 15 assertions.
    - POSITIVE SYNC ASSERTION (mandatory, not "no error thrown"): the `runChain` return value
      satisfies `typeof res.then !== 'function'`, proving the call stayed on the synchronous
      path where `_ralphSafeRetry` lives.
    - Case 1 asserts `onStep.count() === 2` for the find-bottlenecks step (exactly one retry)
      with `ralphRetryCap` left at its shipped default.
    - Case 2 asserts `res.haltedAt.reason === 'retry_exhausted'`, `res.completed === false`,
      and `onStep.count() === 3`.
    - Case 4 asserts `onStep.count() === 0` for a material step carrying `ralph_verify: true`.
    - Case 5 asserts `res.haltedAt.reason === 'forced_material'`.
    - Case 6 asserts `res.haltedAt.reason === 'budget_brake'`.
    - Case 7 asserts the critic was invoked at least twice and every verdict was a non-thenable.
    - The `runChain` opts object in this file contains none of `roomDir`, `journal`, `retries`,
      `resume`, `sleep` (verify:
      `grep -v '^ \*' tests/test-264-flagship-ralph.cjs | grep -cE '\b(roomDir|journal|retries|resume|sleep)\s*:'` reports 0).
    - The header restates both reasons `chain_run` is inert (verify:
      `grep -c "chain_run" tests/test-264-flagship-ralph.cjs` is at least 2).
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' tests/test-264-flagship-ralph.cjs` reports 0.
  </acceptance_criteria>
  <done>A weak candidate on the real Technical Roadmap chain triggers exactly one bounded retry then passes; a never-passing candidate forces a retry_exhausted halt; a material or irreversible step is never retried.</done>
</task>

<task type="auto">
  <name>Task 2: Pin chain-executor's gate and stop-condition functions against the phase base commit</name>
  <files>tests/test-264-b3-frozen.cjs</files>
  <read_first>
    - lib/core/chain-executor.cjs lines 1-70 (the file header carrying the B3 doctrine text this test pins verbatim)
    - lib/core/chain-executor.cjs lines 160-345 (the exact symbols to pin: `LOW_QUALITY` at :164, `_defaultPostureFn` at :181, `IRREVERSIBLE_HINTS` at :196, `isIrreversibleStep` at :206, `_isMaterialStep` at :230, `_applySelfCritique` at :255, `RALPH_RETRY_CAP_DEFAULT` at :285, `_safeCritique` at :287, `_critiqueFailed` at :291, `_ralphSafeRetry` at :295) and `makeGateFn` at :339
    - .planning/phases/166-gated-chain-executor/166-SPEC.md (B3's original ruling text: the stop condition is posture-driven, never autonomous-convergence-driven)
    - tests/test-dispatch-framework-map-drift.cjs (the `check(cond, msg)` plus exit-code idiom to reuse)
  </read_first>
  <action>
Create `tests/test-264-b3-frozen.cjs`, a plain `node` script using `node:crypto`, `node:fs`,
and `node:path`, with the `check(cond, msg)` counter and exit-code idiom from
`tests/test-dispatch-framework-map-drift.cjs`.

FIRST, BEFORE COMPUTING ANY HASH, prove the file is unchanged, so the pin cannot be
self-fulfilling. Run these two commands and record their output in the SUMMARY:
  `git diff --quiet HEAD -- lib/core/chain-executor.cjs`
  `git diff --quiet c7c33eea449f6f227c4cfbb86f220acaac9b5ab8 -- lib/core/chain-executor.cjs`
Both must exit 0. If either does not, STOP and flag it as a phase-boundary violation rather
than recording a hash of a modified file.

THE TEST ITSELF, five arms:

  Arm 1 -- SCOPED SOURCE PIN. Read `lib/core/chain-executor.cjs` as utf8. For each of these
  six symbol names, extract its source text by slicing from the line matching
  `^function <NAME>\(` (or `^const <NAME> =` for the two constants) up to and including the
  next line that is exactly `}` at column 0 (or, for the constants, the line ending that
  declaration):
    `_defaultPostureFn`, `isIrreversibleStep`, `_isMaterialStep`, `_critiqueFailed`,
    `_ralphSafeRetry`, `makeGateFn`
  Compute `crypto.createHash('sha256').update(src, 'utf8').digest('hex')` for each and compare
  against six recorded constants. Compute the six values ONCE, after the two git-clean checks
  above pass, and write them into the test as literals with a comment naming the base commit
  `c7c33eea449f6f227c4cfbb86f220acaac9b5ab8`.
  SCOPE NOTE, state it in the header: this pin covers the gate and stop-condition surface
  ONLY, deliberately NOT the whole file. A whole-file hash would redden this suite on any
  legitimate future edit elsewhere in `chain-executor.cjs` and become a maintenance landmine.
  SPEC Requirement 5's acceptance names exactly these functions.
  If an extraction returns an empty string for any symbol, that is a FAILURE, not a skip: a
  renamed or deleted function must redden this test, not silently pass it. Assert each
  extracted source is non-empty before hashing.

  Arm 2 -- DOCTRINE TEXT PIN. Assert the file still contains, verbatim, the B3 doctrine
  sentence from its own header stating that the SEED-032 / imported-harness "loop until all
  PASSING" convergence branch is REJECTED and that the stop condition is posture, quality, and
  maxSteps ONLY. Locate the exact sentence in the header before writing the assertion; do not
  paraphrase it.

  Arm 3 -- CONSTANT PIN. `RALPH_RETRY_CAP_DEFAULT` is `2` and `LOW_QUALITY` is `'low'`, both
  asserted by matching the declaration lines in the source text (they are module-private and
  not exported).

  Arm 4 -- STRUCTURAL PIN on `_ralphSafeRetry`'s bound. Assert its extracted source contains
  the literal `Math.min(cap, budgetRemaining)` and contains neither `await` nor `async`. This
  is the loop bound SPEC Requirement 5 names as untouchable.

  Arm 5 -- NO NEW CONVERGENCE SURFACE. Assert the extracted `makeGateFn` and `_isMaterialStep`
  sources contain none of the tokens `converge`, `allPassing`, `all_passing`, `loopUntil`.

  Footer on success: `process.stdout.write('264 B3 frozen pin: PASS (' + checks + ' checks)\n')`
  then `process.exit(0)`; non-zero exit with a per-arm FAIL line otherwise.
  Header block: numbered "Behaviors covered", a plain statement that Phase 264 is a CALLER of
  `chain-executor.cjs` and never an editor of it, and the closing line
  `House rule: hyphens only, no em-dashes.`
  </action>
  <verify>
    <automated>git diff --quiet c7c33eea449f6f227c4cfbb86f220acaac9b5ab8 -- lib/core/chain-executor.cjs && node tests/test-264-b3-frozen.cjs && bash tests/run-all-166.sh</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --quiet c7c33eea449f6f227c4cfbb86f220acaac9b5ab8 -- lib/core/chain-executor.cjs`
      exits 0 (zero lines changed in the whole file, which is strictly stronger than SPEC R5's
      "zero lines inside the gate/stop-condition functions").
    - `node tests/test-264-b3-frozen.cjs` exits 0 and its final line matches
      `264 B3 frozen pin: PASS (` with at least 12 checks.
    - `bash tests/run-all-166.sh` exits 0 with `Failed: 0` in its summary (23 of 23 at the
      phase base commit; the file is unmodified by this phase).
    - The test extracts and hashes all six named symbols, and each extracted source is
      asserted non-empty before hashing (verify:
      `grep -c "_ralphSafeRetry" tests/test-264-b3-frozen.cjs` is at least 2 and
      `grep -c "makeGateFn" tests/test-264-b3-frozen.cjs` is at least 2).
    - MUTATION PROOF, run manually once and recorded in the SUMMARY: temporarily adding a
      single space inside `_isMaterialStep`'s body makes this suite exit non-zero. Revert
      immediately with `git checkout -- lib/core/chain-executor.cjs` and re-run the git-clean
      check.
    - Arm 4 asserts the literal `Math.min(cap, budgetRemaining)` is present in
      `_ralphSafeRetry`'s source.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' tests/test-264-b3-frozen.cjs` reports 0.
  </acceptance_criteria>
  <done>Any edit to chain-executor's gate or stop-condition functions, and any removal of the B3 doctrine text, reddens this suite; the file is proven byte-identical to the phase base commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness to the constitutional contract | This plan is the only automated proof that Phase 264 did not weaken the Canon Part 3 halt |
| `onStep` output to the retry loop | The synthetic RS finding is the untrusted input the critic must actually reject when malformed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-264-17 | Elevation of Privilege | `_isMaterialStep` / `_ralphSafeRetry` | mitigate | Cases 4 and 5 assert `onStep.count() === 0` for a material step and a `forced_material` halt for an irreversible one, so a retry can never escalate past the Canon Part 3 gate |
| T-264-18 | Tampering | `lib/core/chain-executor.cjs` | mitigate | Task 2's scoped sha256 source pin over six named symbols, plus the whole-file `git diff --quiet` against `c7c33eea449f6f227c4cfbb86f220acaac9b5ab8`, plus the `run-all-264.sh` zero-diff arm from plan 264-01 |
| T-264-19 | Denial of Service | `_ralphSafeRetry` | mitigate | Case 2 pins the cap at exactly 3 `onStep` calls; case 6 pins `budget_brake` as a distinct, strictly-smaller bound. Arm 4 pins `Math.min(cap, budgetRemaining)` as the literal bound |
| T-264-20 | Repudiation | silent false-success | mitigate | Case 7 asserts positively that the critic was invoked at least twice and that every verdict was a non-thenable. A Promise verdict would pass every candidate with no log; absence of an error proves nothing here |
| T-264-21 | Spoofing | step composition drift | mitigate | The test asserts, before running, that exactly one composed step carries `ralph_verify: true` and that its command is `/mos:find-bottlenecks`, so a change to the chain table cannot turn this into a test of nothing |
| T-264-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero packages (RESEARCH Package Legitimacy Audit: not applicable). No install task exists in this plan. |

## Canon Part 3 (the constraint this plan exists to prove)

The chain halts at the first material step. This phase adds ZERO cross-step convergence logic.
`ralph_verify` is bounded, intra-step only, and forces a halt on exhaustion rather than a
silent pass. B3 stays rejected, correctly. No task in this plan edits
`lib/core/chain-executor.cjs`; if a task appears to need that, it is a phase-boundary
violation and must be flagged rather than executed.
</threat_model>

<verification>
1. `node tests/test-264-flagship-ralph.cjs` exits 0.
2. `node tests/test-264-b3-frozen.cjs` exits 0.
3. `bash tests/run-all-166.sh` exits 0 with `Failed: 0`.
4. `node tests/test-201-bounded-retry.cjs` exits 0 (unmodified).
5. `git diff --quiet c7c33eea449f6f227c4cfbb86f220acaac9b5ab8 -- lib/core/chain-executor.cjs` exits 0.
6. `TEST_264_ALLOW_MISSING=1 bash tests/run-all-264.sh` exits 0.
</verification>

<success_criteria>
- The real Technical Roadmap chain, composed from the committed table, runs through a direct
  synchronous `runChain` with a real adversarial critic; a weak first candidate produces
  exactly one bounded retry then a passing verdict.
- A never-passing candidate halts with `retry_exhausted` after exactly cap plus one `onStep`
  calls, never silently proceeding.
- A material step and an irreversible step with `ralph_verify: true` are never retried.
- `budget_brake` remains distinguishable from `retry_exhausted`.
- `lib/core/chain-executor.cjs` is byte-identical to `c7c33eea449f6f227c4cfbb86f220acaac9b5ab8`
  and its six gate/stop-condition symbols are sha256-pinned.
- `tests/run-all-166.sh` passes unmodified at 23 of 23.
- Zero U+2014 em-dashes in both files.
</success_criteria>

<output>
Create `.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-04-SUMMARY.md` when done.
</output>
