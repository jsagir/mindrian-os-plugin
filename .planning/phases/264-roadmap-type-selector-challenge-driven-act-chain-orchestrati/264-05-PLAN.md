---
phase: 264-roadmap-type-selector-challenge-driven-act-chain-orchestrati
plan: 05
type: execute
wave: 3
depends_on: ["264-01", "264-03", "264-04"]
files_modified:
  - tests/test-264-sensor-to-chain-resolve.cjs
  - .planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-VALIDATION.md
autonomous: true
requirements: [R3, R5]
canon_parts: [3, 7, 8]

must_haves:
  truths:
    - "One sample turn driven through dispatchSensors fires the new sensor, and its carried chain composes through chain_resolve with no null command on a required step"
    - "The whole Phase 264 suite is green with every declared file present, not skipped"
  artifacts:
    - path: "tests/test-264-sensor-to-chain-resolve.cjs"
      provides: "The Requirement 3 end-to-end wiring proof from turn text to a resolved runnable plan"
      contains: "dispatchSensors"
  key_links:
    - from: "tests/test-264-sensor-to-chain-resolve.cjs"
      to: "lib/core/insight-sensors.cjs"
      via: "dispatchSensors drives the real registry, not the sensor function directly"
      pattern: "dispatchSensors"
    - from: "tests/test-264-sensor-to-chain-resolve.cjs"
      to: "lib/mcp/tools/chain.cjs"
      via: "chainResolve, the shipped chain_resolve seam"
      pattern: "chainResolve"
---

<objective>
Close SPEC Requirement 3: prove that a navigator's stated research goal, driven through the
REAL `dispatchSensors` path, produces a reach whose carried chain composes through the shipped
`chain_resolve` seam into a plan with no `command: null` on a required step. Then run the
phase's full gate with every declared artifact present.

Purpose: Requirements 1, 2, and 4 each proved a component. This plan proves the seam between
them, and is the phase's closing gate.

Output: `tests/test-264-sensor-to-chain-resolve.cjs`, plus a green
`bash tests/run-all-264.sh` with zero skipped em-dash targets.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-SPEC.md
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-CONTEXT.md
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-RESEARCH.md
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-PATTERNS.md
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-01-SUMMARY.md
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-03-SUMMARY.md
@.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-04-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Every symbol below is CREATED by Phase 264 and does not exist in the tree before it.
A drift or source-grounding pass must treat all of these as new, never as unacknowledged
references to prior art.

**New files**
- `data/roadmap-type-chains.json`
- `lib/core/salient-governance.cjs`
- `lib/core/sensors/sensor-roadmap-type.cjs`
- `tests/test-264-roadmap-type-chains-drift.cjs`
- `tests/test-264-salient-critic.cjs`
- `tests/test-264-roadmap-type-sensor.cjs`
- `tests/test-264-flagship-ralph.cjs`
- `tests/test-264-b3-frozen.cjs`
- `tests/test-264-sensor-to-chain-resolve.cjs`
- `tests/run-all-264.sh`

**New exported functions and constants**
- `sensorRoadmapType`, `classifyRoadmapType`, `ROADMAP_TYPES`, `ROADMAP_PATTERNS`,
  `ROADMAP_PROBLEM_TYPES`, `ROADMAP_CHAIN_TABLE_PATH`, `chainForRoadmapType`
  (`lib/core/sensors/sensor-roadmap-type.cjs`, and `sensorRoadmapType` re-exported from
  `lib/core/insight-sensors.cjs`)
- `SALIENT_GOVERNANCE`, `SALIENT_PASSES`, `RS_DIRECTIONS`, `governanceForPass`,
  `findingFromResult`, `enforceSalientGovernance`, `makeSalientSelfCritiqueFn`
  (`lib/core/salient-governance.cjs`)

**New registry / enum values**
- Sensor id `SENS-18` (added to `SENSOR_REGISTRY_IDS` and to `SENS_PRIORITY` Group C)
- Reach `dispatch` handle `roadmap-type-selector`; reach `signal` value
  `roadmap_type_classified`
- Reach `evidence` keys: `roadmap_type`, `mode`, `score`, `chain_len`, `tie_broken`
- Roadmap-type enum slugs: `landscape-analysis`, `technical-roadmap`, `pipeline-analysis`,
  `opportunity-analysis`, `agenda-setting-manifesto`, `vision-paper`
- Critic pass names: `neutral`, `adversarial`
- Critic violation strings: `rs_finding_unrecognized`, `rs_no_id`, `rs_no_source_artifact`,
  `rs_no_target_artifact`, `rs_direction_missing`, `rs_diff_not_numeric`,
  `rs_body_text_empty`, `rs_self_referential`, `rs_diff_sign_mismatch`,
  `rs_diff_below_floor`, `rs_direction_unrecognized`, `rs_pass_disagreement`,
  `rs_pass_unrecognized`, `rs_step_not_governed`
- Shell env overrides: `TEST_264_PREFIX`, `TEST_264_ALLOW_MISSING`
- Shell constant `PHASE_264_BASE_SHA` in `tests/run-all-264.sh`

## Requirement 3 resolved (RESEARCH Open Question 2)

SPEC Requirement 3 names a `suggested_chain` field. D-03 already established that this cannot
be a new top-level reach key: `makeReach` freezes the struct to exactly six keys
(`reach_id`, `posture`, `dispatch`, `companions`, `signal`, `evidence`) and DROPS anything
else. The resolution, and this test proves BOTH halves of it:

- `evidence.roadmap_type` is the LOAD-BEARING carrier: a closed 6-value scalar enum from which
  the consumer resolves the chain out of `data/roadmap-type-chains.json`. This is the only
  route with a working path today.
- `companions` carries the framework-name array for observability. State plainly, in the test
  header, that NOTHING reads it yet: the sole shipped consumer
  (`lib/brain/chain-recommender.cjs::parseChainCompanions`, `:526-543`) skips any companion
  string without a `brain_framework_chain:` prefix and deliberately ignores a third
  `:<framework>` segment. Do not imply a wire that does not exist, and do not "fix" it by
  prefixing the names, which would feed the segment to the Brain chain recommender as a
  problem_type handle and produce wrong Brain queries.

"Wire a live consumer for the roadmap-type reach" is a NAMED FOLLOW-ON, exactly like D-11's
async-path fix. Do not attempt it here.

<tasks>

<task type="auto">
  <name>Task 1: Write the Requirement 3 sensor-to-chain_resolve integration proof</name>
  <files>tests/test-264-sensor-to-chain-resolve.cjs</files>
  <read_first>
    - lib/core/insight-sensors.cjs lines 880-940 (`dispatchSensors`: the sync iteration, the `REACH_IDS.indexOf(reach.reach_id) !== -1` membership check that silently drops a Promise, `isReachEligibleForTurn`, and `stampSensorId` which writes `evidence.sensor_id`)
    - lib/mcp/tools/chain.cjs lines 150-160 (`chainResolve` is a one-line `composeWorkflow` wrapper; it IS exported at :618) and lines 260-295 (why `chainRun` is not used here: `roomDir` always set, no `selfCritiqueFn`)
    - lib/workflow/command-resolver.cjs lines 105-145 (`composeWorkflow` emits `{step, framework, command, optional}` and nothing else; `validateChainAutonomy`)
    - data/roadmap-type-chains.json (built in plan 264-01)
    - lib/core/sensors/sensor-roadmap-type.cjs (built in plan 264-03)
    - tests/test-show-share-sensor.cjs lines 100-119 (the `dispatchSensors` positive-and-negative end-to-end idiom)
  </read_first>
  <action>
Create `tests/test-264-sensor-to-chain-resolve.cjs` as a plain `node` script using
`node:assert/strict`, a hand-rolled `ok(name, cond)` counter, and the footer
`console.log('\nPASS test-264-sensor-to-chain-resolve (' + pass + ' assertions)');`.
Resolve requires through `REPO_ROOT = path.resolve(__dirname, '..')`.

HEADER BLOCK: numbered "Behaviors covered"; the Requirement 3 resolution paragraph from this
plan's section above, stating which carrier is load-bearing and that `companions` has no
consumer today; and the closing line `House rule: hyphens only, no em-dashes.`

The test drives the REAL dispatcher, never the sensor function directly. Calling
`sensorRoadmapType` in isolation would skip `isReachEligibleForTurn` and `stampSensorId` and
would not prove the production path.

ASSERTIONS:

  1. Drive one sample turn:
     `dispatchSensors({ text: 'I need a technical roadmap - what has to be solved first before the rest becomes possible', signals: [], turn_count: 5 }, { problem_type: 'IDP' }, {})`
     `turn_count: 5` is required: `turnCountOf` returns `undefined` when neither
     `turn.turn_count` nor `ctx.turn_count` is finite, and an absent counter is a no-op, so a
     fixture that omits it can pass while production drops the reach.
     Assert exactly one returned reach has `dispatch === 'roadmap-type-selector'`.
  2. Assert that reach has `reach_id === 'context_block'`, `posture === 'hold'`,
     `signal === 'roadmap_type_classified'`, and `evidence.sensor_id === 'SENS-18'`.
  3. LOAD-BEARING CARRIER: `reach.evidence.roadmap_type === 'technical-roadmap'`, and that
     value is a key in `data/roadmap-type-chains.json`.
  4. Resolve the chain the way a real consumer would: read
     `data/roadmap-type-chains.json[reach.evidence.roadmap_type]` and pass it to
     `chainResolve` from `lib/mcp/tools/chain.cjs` (the shipped `chain_resolve` seam SPEC
     Requirement 3 names). Assert `typeof chainResolve === 'function'` first so a future
     export change reddens here rather than silently falling back.
  5. THE REQUIREMENT 3 ACCEPTANCE: the resolved plan has ZERO steps where
     `command === null && optional !== true`. Assert this explicitly with a filter, and
     include the offending framework name in the assertion message if it ever fires.
  6. Assert the resolved plan's commands deep-equal
     `['/mos:diagnose', '/mos:find-bottlenecks', '/mos:map-unknowns']`, and that
     `validateChainAutonomy(plan).runnable === true`.
  7. Assert every step object has exactly the keys `step`, `framework`, `command`, `optional`
     and nothing else (`composeWorkflow`'s emitted shape; this pins that no caller has started
     grafting fields onto the resolver's output).
  8. OBSERVABILITY CARRIER: `reach.companions` deep-equals the same framework-name array read
     from the table, and `reach.evidence.chain_len === reach.companions.length`. Add an
     adjacent comment restating that nothing reads `companions` today.
  9. AGREEMENT CHECK: `chainResolve(chain)` deep-equals `composeWorkflow(chain)` from
     `lib/workflow/command-resolver.cjs`, proving the MCP seam is the thin wrapper it claims
     to be and that the test is exercising the real door.
 10. ALL SIX TYPES, not just the flagship: for each of the six slugs in the table, assert
     `chainResolve(chain)` yields zero required-null commands and
     `validateChainAutonomy(...).runnable === true`. Requirement 3's acceptance names one
     sample turn, but proving the seam holds for all six costs nothing and stops a later
     table edit from breaking five of them silently.
 11. NEGATIVE: `dispatchSensors` on `{ text: 'what is the weather today', signals: [], turn_count: 5 }`
     returns NO reach with `dispatch === 'roadmap-type-selector'`.
 12. SYNC FENCE: assert the array returned by `dispatchSensors` is a real array
     (`Array.isArray(...) === true`) and that no element is a thenable. A Promise-returning
     sensor is dropped by the membership check with no throw and no log, so absence of an
     error proves nothing on this seam.
  </action>
  <verify>
    <automated>node tests/test-264-sensor-to-chain-resolve.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-264-sensor-to-chain-resolve.cjs` exits 0 and its final line matches
      `PASS test-264-sensor-to-chain-resolve (` with at least 25 assertions.
    - The suite calls `dispatchSensors`, not `sensorRoadmapType` directly, for its primary
      path (verify: `grep -c "dispatchSensors" tests/test-264-sensor-to-chain-resolve.cjs` is
      at least 2).
    - The suite calls `chainResolve` from `lib/mcp/tools/chain.cjs` (verify:
      `grep -c "chainResolve" tests/test-264-sensor-to-chain-resolve.cjs` is at least 3).
    - The resolved technical-roadmap plan's commands deep-equal
      `['/mos:diagnose', '/mos:find-bottlenecks', '/mos:map-unknowns']` with zero
      required-null steps.
    - All six chains resolve with zero required-null steps and `runnable === true`.
    - `evidence.sensor_id === 'SENS-18'` is asserted (verify:
      `grep -c "SENS-18" tests/test-264-sensor-to-chain-resolve.cjs` is at least 1).
    - Every fixture passed to `dispatchSensors` sets `turn_count` (verify:
      `grep -c "turn_count" tests/test-264-sensor-to-chain-resolve.cjs` is at least 2).
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' tests/test-264-sensor-to-chain-resolve.cjs` reports 0.
  </acceptance_criteria>
  <done>One sample turn goes from raw text through the real dispatcher to a resolved, runnable command plan with no required null step, and all six chains hold the same property.</done>
</task>

<task type="auto">
  <name>Task 2: Run the phase closing gate with every declared artifact present</name>
  <files>.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-VALIDATION.md</files>
  <read_first>
    - tests/run-all-264.sh (built in plan 264-01: confirm the eleven `EMDASH_TARGETS`, the `TEST_264_ALLOW_MISSING` arm, the `PHASE_264_BASE_SHA` zero-diff arm, and the `run-all-166.sh` passthrough)
    - .planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-SPEC.md Acceptance Criteria (the six checkboxes this gate closes)
    - .planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-VALIDATION.md (the per-task verification map and its sign-off block)
  </read_first>
  <action>
This is a verification-only task. It changes no production code and, in the normal case,
edits no file. Run the phase's closing gate and record every result in the SUMMARY.

Run these, in order, and record each command's exit code and its salient output line:

  1. `bash tests/run-all-264.sh`
     WITHOUT `TEST_264_ALLOW_MISSING`. This is the run that proves all eleven declared
     `EMDASH_TARGETS` actually exist on disk: the missing-target arm fails the fence when any
     target is absent, which is exactly how a typo'd path in the target list gets caught.
     Expect `FAIL=0` and a zero exit code.
  2. `bash tests/run-all-166.sh`
     Expect exit 0 and `Failed: 0` in its summary block (23 of 23 at the phase base commit).
     SPEC Requirement 5's acceptance is that this passes UNMODIFIED, so also run
     `git diff --quiet c7c33eea449f6f227c4cfbb86f220acaac9b5ab8 -- tests/run-all-166.sh`
     and expect exit 0.
  3. `git diff --quiet c7c33eea449f6f227c4cfbb86f220acaac9b5ab8 -- lib/core/chain-executor.cjs`
     Expect exit 0. This is SPEC Requirement 5's zero-diff acceptance, proven at the whole-file
     level, which is strictly stronger than the "zero lines inside the gate/stop-condition
     functions" the SPEC asks for.
  4. `node scripts/build-connector-registry.cjs --check`
     Expect exit 0 (Canon Part 11 R1/R2, the born-wired gate).
  5. `node scripts/doctor.cjs --acceptance`
     Expect exit 0. This is CLAUDE.md's acceptance roll-up. If it reports a pre-existing
     failure unrelated to Phase 264's files, do NOT fix it in this phase: record it in the
     SUMMARY as a pre-existing condition with the exact failing check name, and state whether
     it also fails at the phase base commit `c7c33eea449f6f227c4cfbb86f220acaac9b5ab8`.
  6. `LC_ALL=C.UTF-8 grep -rlP '\x{2014}' data/roadmap-type-chains.json lib/core/salient-governance.cjs lib/core/sensors/sensor-roadmap-type.cjs lib/core/insight-sensors.cjs lib/core/sensors/sensor-priority.cjs tests/test-264-*.cjs tests/run-all-264.sh`
     Expect NO output and exit code 1 from grep (no files matched), which is the C-01
     no-em-dash proof over every touched file.

Then update `.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-VALIDATION.md`:
set `nyquist_compliant: true` and `wave_0_complete: true` in its frontmatter, mark every row
of the Per-Task Verification Map with its real status, and check the six Validation Sign-Off
boxes. Do not invent a green status for anything you did not actually run.

If ANY of steps 1 through 4 or 6 fails, STOP and report it. Do not paper over a red gate, and
do not edit `lib/core/chain-executor.cjs` to make one pass: that is a phase-boundary violation
by construction.
  </action>
  <verify>
    <automated>bash tests/run-all-264.sh && bash tests/run-all-166.sh && git diff --quiet c7c33eea449f6f227c4cfbb86f220acaac9b5ab8 -- lib/core/chain-executor.cjs tests/run-all-166.sh && node scripts/build-connector-registry.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-264.sh` (no `TEST_264_ALLOW_MISSING`) exits 0 with `FAIL=0` in its
      footer, and its em-dash fence block prints no `(skipped, not yet created)` line, proving
      all eleven declared targets exist.
    - The `run-all-264.sh` output contains a `frozen-166 passthrough` line reporting PASSED
      and an embedded 166 summary reporting `Failed: 0`.
    - The `run-all-264.sh` output contains a chain-executor zero-diff block reporting PASSED,
      not SKIPPED.
    - `bash tests/run-all-166.sh` exits 0 with `Failed: 0`.
    - `git diff --quiet c7c33eea449f6f227c4cfbb86f220acaac9b5ab8 -- lib/core/chain-executor.cjs tests/run-all-166.sh` exits 0.
    - `node scripts/build-connector-registry.cjs --check` exits 0.
    - `node scripts/doctor.cjs --acceptance` exits 0, OR the SUMMARY records the exact failing
      check name plus a stated determination of whether it also fails at the phase base commit.
    - The em-dash sweep over all touched files produces no output.
    - `264-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`,
      and no row of its verification map is left at `pending`.
  </acceptance_criteria>
  <done>Every SPEC acceptance checkbox is closed by a command that was actually run, with its output recorded, and the Phase 166 contract plus the chain-executor source are both proven untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| turn text to resolved command plan | This is the full untrusted-input-to-executable-plan path the phase creates; it is the one place where a classification error becomes a command selection |
| phase gate to release | A green `run-all-264.sh` is what a later release gate will trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-264-22 | Tampering | turn text to command plan | mitigate | The path is text to a CLOSED 6-value enum to a COMMITTED drift-tested table to the resolver. Turn text never reaches `composeWorkflow`; only a table-sourced framework-name array does. Assertion 5 pins zero required-null commands and assertion 10 extends it to all six chains |
| T-264-23 | Spoofing | `chainResolve` seam | mitigate | Assertion 9 asserts `chainResolve(chain)` deep-equals `composeWorkflow(chain)`, so the test cannot be silently exercising a stub or a diverged wrapper |
| T-264-24 | Repudiation | skipped gate arms | mitigate | The closing gate runs WITHOUT `TEST_264_ALLOW_MISSING`, so a missing declared file FAILS instead of printing a skip line; the zero-diff arm must report PASSED, not SKIPPED |
| T-264-25 | Elevation of Privilege | autonomy of the resolved plan | mitigate | Assertion 6 and 10 require `validateChainAutonomy(...).runnable === true`, which routes through the ONE autonomy authority rather than a local `autonomous_safe` read |
| T-264-26 | Information Disclosure | reach payload | mitigate | Assertions 3 and 8 pin that the carriers are a closed enum and a table-sourced string array; no turn text rides the reach (Canon Part 8) |
| T-264-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero packages (RESEARCH Package Legitimacy Audit: not applicable). No install task exists in this plan. |

## Canon Part 8

Zero Brain egress. The integration test reads local JSON, the local resolver, and the local
sensor registry. No network call anywhere in the phase.
</threat_model>

<verification>
1. `node tests/test-264-sensor-to-chain-resolve.cjs` exits 0.
2. `bash tests/run-all-264.sh` exits 0 with `FAIL=0` and no skipped em-dash target.
3. `bash tests/run-all-166.sh` exits 0 with `Failed: 0`.
4. `git diff --quiet c7c33eea449f6f227c4cfbb86f220acaac9b5ab8 -- lib/core/chain-executor.cjs tests/run-all-166.sh` exits 0.
5. `node scripts/build-connector-registry.cjs --check` exits 0.
6. `node scripts/doctor.cjs --acceptance` exits 0, or its failure is recorded as pre-existing
   with evidence from the phase base commit.
</verification>

<success_criteria>
- One sample turn drives through `dispatchSensors`, fires SENS-18, and its carried chain
  composes through the shipped `chain_resolve` seam into a plan with zero `command: null` on
  a required step. All six chains hold the same property.
- `chainResolve` and `composeWorkflow` are proven to agree.
- The full Phase 264 suite is green with every one of the eleven declared files present and
  swept for em-dashes, no skips.
- `tests/run-all-166.sh` passes unmodified and `lib/core/chain-executor.cjs` is byte-identical
  to the phase base commit.
- `264-VALIDATION.md` reflects real, executed results with `nyquist_compliant: true`.
</success_criteria>

<output>
Create `.planning/phases/264-roadmap-type-selector-challenge-driven-act-chain-orchestrati/264-05-SUMMARY.md` when done.
</output>
