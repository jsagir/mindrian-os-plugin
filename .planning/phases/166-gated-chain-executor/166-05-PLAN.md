---
phase: 166-gated-chain-executor
plan: 05
type: execute
wave: 5
depends_on: [166-04]
files_modified:
  - commands/pipeline.md
  - lib/mcp/pipeline-state.cjs
  - tests/test-pipeline-state-shipped-behavior.cjs
  - tests/test-pipeline-on-runchain.cjs
  - tests/run-all-166.sh
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-04, B1, D-166-02, D-166-04]
canon_parts: [3, 6, 7, 8, 9]

must_haves:
  truths:
    - "the pipeline runs via lib/core/chain-executor.cjs runChain, supplying a provenanceFn that stamps each artifact's pipeline frontmatter; ~60 duplicated loop lines are de-duped (D-166-04)"
    - "resume reads position from pipeline-state.cjs ONLY (the sole truth, B1/D-166-02); the artifact-frontmatter scan is the secondary index that confirms, never competes"
    - "the isNext hard gate (Wave 1) prevents re-running an already-completed stage on resume"
    - "pipeline-state.cjs is currently UNTESTED, so its first test must capture SHIPPED behavior: a behavior-capture task records checkPosition / recordStep / chain_position round-trip as the module's FIRST coverage BEFORE reconcileResume/provenanceFn are wired, then asserts pre-vs-post identity so the new wiring cannot silently drift the shipped store"
  artifacts:
    - path: "tests/test-pipeline-state-shipped-behavior.cjs"
      provides: "the FIRST-ever pipeline-state.cjs coverage: captures the SHIPPED checkPosition / recordStep / initChain / chain_position round-trip BEFORE the reconcileResume/provenanceFn wiring lands; the pre-vs-post identity net"
      min_lines: 50
    - path: "commands/pipeline.md"
      provides: "the hand-rolled stage checklist repointed to runChain with a provenanceFn; resume sourced from pipeline-state.cjs"
      contains: "chain-executor"
    - path: "lib/mcp/pipeline-state.cjs"
      provides: "a provenanceFn helper (stage -> pipeline frontmatter) + the secondary-index reconciliation note made executable"
      contains: "provenance"
    - path: "tests/test-pipeline-on-runchain.cjs"
      provides: "runChain-driven + provenanceFn-stamps + resume-from-sole-truth + no-re-run + shipped-behavior-still-identical assertions"
      min_lines: 60
  key_links:
    - from: "commands/pipeline.md"
      to: "lib/core/chain-executor.cjs"
      via: "the pipeline calls runChain with provenanceFn supplied"
      pattern: "runChain"
    - from: "lib/mcp/pipeline-state.cjs"
      to: "tests/test-pipeline-on-runchain.cjs"
      via: "resume reads checkPosition.isNext (the hard gate) as the sole truth"
      pattern: "checkPosition"
    - from: "tests/test-pipeline-state-shipped-behavior.cjs"
      to: "lib/mcp/pipeline-state.cjs"
      via: "captures the shipped checkPosition/recordStep/chain_position round-trip as first coverage"
      pattern: "chain_position"
---

<objective>
WAVE 5 MIGRATE pipeline (the CONSUMER). commands/pipeline.md today hand-rolls a stage checklist with
its own resume mechanism (the artifact-frontmatter scan -- the block beginning "scan the Room for
existing artifacts with `pipeline: {chain}` in frontmatter" at commands/pipeline.md:78-114, and the
"Pipeline resumability" rule at commands/pipeline.md:129) competing with pipeline-state.cjs. This
wave repoints the pipeline ONTO runChain: it supplies a provenanceFn that stamps each stage
artifact's pipeline frontmatter (pipeline + pipeline_stage, the contract at framework-runner.md:220),
de-dupes the ~60 duplicated loop lines, and -- per the Wave-1 B1 reconciliation -- sources RESUME from
pipeline-state.cjs ONLY (the sole chain-state truth, D-166-02), with the frontmatter scan demoted to
a secondary confirming index. The Wave-1 isNext hard gate prevents re-running an already-completed
stage on resume.

UNTESTED STORE, CAPTURE FIRST (the gap the checker named): lib/mcp/pipeline-state.cjs is currently
UNTESTED (confirmed -- tests/ carries no pipeline-state suite). This wave ADDS new behavior
(reconcileResume + makeProvenanceFn) to a module with zero existing coverage, so the new wiring could
silently reshape the shipped store with nothing to catch it. Therefore the FIRST test for
pipeline-state.cjs must capture its SHIPPED behavior -- the checkPosition / recordStep / initChain /
chain_position round-trip -- as a baseline BEFORE reconcileResume/provenanceFn are wired, then assert
pre-vs-post identity so the additive wiring cannot drift the store. Capture shipped behavior, not only
new behavior.

Purpose: D-166-04 sequences pipeline second (after act). The pipeline is the consumer that needs
provenanceFn (act and ignite pass null); proving the callback on the pipeline validates the contract
slot. B1 must become EXECUTABLE here: before this wave the two resume memories could disagree; after
it, resume reads exactly one store and the frontmatter scan only confirms.

Reuse before build (Canon Part 7): pipeline-state.cjs ships initChain/recordStep/checkPosition/
getPreviousOutput (lib/mcp/pipeline-state.cjs:94-209) and formatPipelineContext
(pipeline-state.cjs:235-264) -- this wave ADDS a thin provenanceFn helper (stage -> pipeline
frontmatter) and wires resume through the Wave-1 isNext hard gate; it does NOT rebuild the store or
the pipeline command. The loop is runChain (Wave 2). A rewrite is rejected because the journal, the
frontmatter contract, and the loop already exist; the migration is a repoint + the provenanceFn slot.

Output: the FIRST-ever pipeline-state.cjs shipped-behavior capture suite, the pipeline repointed onto
runChain with provenanceFn, the executable B1 reconciliation, the migration regression suite, and the
Wave-5 entry in run-all-166.sh. NO em-dashes (CLAUDE.md HARD RULE). One surface per wave; this wave
touches only pipeline (no overlap with act/ignite).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-gated-chain-executor/166-SPEC.md
@.planning/phases/166-gated-chain-executor/166-CONTEXT.md
@commands/pipeline.md
@lib/mcp/pipeline-state.cjs
@lib/core/chain-executor.cjs
@agents/framework-runner.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the SHIPPED pipeline-state.cjs behavior as its FIRST coverage (before any new wiring)</name>
  <files>tests/test-pipeline-state-shipped-behavior.cjs</files>
  <action>Create tests/test-pipeline-state-shipped-behavior.cjs -- the FIRST-EVER test for lib/mcp/pipeline-state.cjs (the module ships untested). Run it against the CURRENTLY SHIPPED store (BEFORE Task 2 adds reconcileResume/makeProvenanceFn) and capture the round-trip behavior over a tmp room dir: (1) initChain(room, ['a','b','c'], 'local') then read(room) -> assert chain_position === -1 and chain === ['a','b','c'] (the initial state at pipeline-state.cjs:94-114); (2) checkPosition(room, 'a') -> assert { inPipeline:true, isNext:true, position, chain, previousOutput } shape (the shipped return at pipeline-state.cjs:171-195); (3) recordStep(room, 'a', 'p/a.md') -> assert chain_position advances to 0 and the nextExpected logic matches the shipped behavior (pipeline-state.cjs:122-159); (4) checkPosition(room, 'b') after the step -> isNext true; checkPosition(room, 'a') -> isNext false; (5) getPreviousOutput(room) round-trips the recorded output path (pipeline-state.cjs:205). These assertions are the SHIPPED contract -- they MUST stay green unchanged after Task 2's additive wiring lands (Task 2's suite re-runs them to prove pre === post). This task captures the store's truth before it is extended. Mirror the plain node assert harness of tests/test-edges-domain-taxonomy-floor.cjs. NO fenced code in this action; cite the pipeline-state.cjs line ranges. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-pipeline-state-shipped-behavior.cjs && echo PIPELINE_STATE_SHIPPED_OK</automated>
  </verify>
  <done>pipeline-state.cjs has its first-ever test: the shipped checkPosition / recordStep / initChain / chain_position / getPreviousOutput round-trip is captured as a baseline BEFORE reconcileResume/provenanceFn are wired, so the additive wiring cannot silently drift the store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: provenanceFn helper + resume from the sole truth (B1 executable, D-166-02)</name>
  <files>lib/mcp/pipeline-state.cjs, tests/test-pipeline-on-runchain.cjs</files>
  <behavior>
    - Test 1: a provenanceFn(step, result) returns the frontmatter fields { pipeline: <chain-name>, pipeline_stage: <N> } for a stage (mirroring framework-runner.md:220); a single-mode step (no chain) returns no pipeline fields.
    - Test 2: resume reads position from checkPosition (the Wave-1 isNext hard gate) as the SOLE truth; given a journal where stages 1-2 completed, checkPosition for stage 2 withholds (already run) and for stage 3 authorizes (next) -- resume starts at stage 3.
    - Test 3: the frontmatter scan is a SECONDARY index: a helper reconcileResume(roomDir) returns the pipeline-state position AND notes the frontmatter scan only as a confirming cross-check; when the two AGREE it returns the position; when they DISAGREE it trusts pipeline-state.cjs (the sole truth) and flags the frontmatter as stale (never the reverse).
    - Test 4: an already-completed stage is NOT re-runnable through the gate (no double-run on resume).
    - Test 5 (no-drift on the shipped store): after makeProvenanceFn + reconcileResume are added, the SHIPPED checkPosition / recordStep / initChain / chain_position round-trip from Task 1 is STILL byte/behavior-identical (re-run the Task-1 assertions, or require that suite and assert it exits zero) -- the additive wiring drifted nothing.
  </behavior>
  <action>Extend lib/mcp/pipeline-state.cjs with (a) a provenanceFn factory makeProvenanceFn(chainName) returning a (step, result) -> { pipeline: chainName, pipeline_stage: step.step } stamp for the pipeline migration (the framework-runner frontmatter contract at framework-runner.md:220; null/empty for single-mode), and (b) reconcileResume(roomDir) that reads the chain position from checkPosition (the Wave-1 isNext hard gate) as the SOLE source of truth and treats the commands/pipeline.md:78-114 artifact-frontmatter scan (the block "scan the Room for existing artifacts with `pipeline: {chain}` in frontmatter") as a SECONDARY confirming index ONLY -- when they agree, return the position; when they disagree, trust pipeline-state.cjs and mark the frontmatter stale (executes the B1/D-166-02 reconciliation). Keep all existing exports byte-stable (the Task-1 shipped-behavior suite is the net that proves it). Then create tests/test-pipeline-on-runchain.cjs covering the five behaviors over a tmp room journal, INCLUDING Test 5's re-assertion that the Task-1 shipped behavior is unchanged after the additive wiring. NO fenced code in this action; cite the line ranges. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-pipeline-on-runchain.cjs && echo PIPELINE_RESUME_OK</automated>
  </verify>
  <done>provenanceFn stamps each stage's pipeline frontmatter; resume reads position from pipeline-state.cjs only via the isNext hard gate; the frontmatter scan confirms but never competes; an already-completed stage cannot re-run; and the shipped store behavior captured in Task 1 stays byte/behavior-identical after the additive wiring (no drift on the untested store).</done>
</task>

<task type="auto">
  <name>Task 3: Repoint commands/pipeline.md onto runChain + register the suites</name>
  <files>commands/pipeline.md, tests/run-all-166.sh</files>
  <action>In commands/pipeline.md, repoint the hand-rolled stage walk onto lib/core/chain-executor.cjs runChain: the pipeline composes its stage chain, then calls runChain supplying provenanceFn = makeProvenanceFn(chainName) (Wave 1, this plan's Task 2), postureFn = recipe-maps.postureForCommand, the default gateFn, and an onStep that dispatches the per-stage framework-runner. Rewrite the resume section -- the artifact-frontmatter scan block beginning "scan the Room for existing artifacts with `pipeline: {chain}` in frontmatter" (commands/pipeline.md:78-114) and the "Pipeline resumability" rule (commands/pipeline.md:129) -- so resume reads from pipeline-state.cjs via reconcileResume (the SOLE truth, D-166-02) and explicitly describes the artifact-frontmatter scan as a SECONDARY confirming index, not a competing source -- this is the user-facing half of the B1 reconciliation. State that the ~60 duplicated loop lines are now the shared runChain spine (de-dup). Keep the user contract (stage-by-stage with checkpoints, resumability) identical in behavior. Do NOT introduce em-dashes. Then append BOTH suites to the CJS_SUITES array in tests/run-all-166.sh in dependency order: test-pipeline-state-shipped-behavior.cjs (the FIRST-coverage baseline, runs first) then test-pipeline-on-runchain.cjs (the migration + identity suite). There is NO pre-existing pipeline regression suite to register (confirmed: tests/ carries none) -- the shipped-behavior capture IS the regression net this wave creates. Extend the em-dash sweep to cover commands/pipeline.md and the two new suites. Runner runs to completion; exit 1 on any FAIL. NO em-dashes.</action>
  <verify>
    <automated>bash tests/run-all-166.sh && grep -q "chain-executor" commands/pipeline.md && grep -q "SECONDARY" commands/pipeline.md && echo PIPELINE_DOC_OK</automated>
  </verify>
  <done>commands/pipeline.md runs via runChain with a provenanceFn, resume reads pipeline-state.cjs as the sole truth with the frontmatter scan demoted to a secondary index, the ~60-line loop is de-duped, and run-all-166.sh registers the pipeline-state shipped-behavior + migration suites and is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resume read -> position | resume must read exactly one chain-state store (pipeline-state.cjs) |
| frontmatter scan -> resume | the scan may confirm but must never override the sole truth |
| provenanceFn -> artifact frontmatter | the stamp is generic stage metadata only (pipeline name + stage number), never user content |
| additive wiring -> shipped store | reconcileResume/provenanceFn must not drift the untested shipped store; the Task-1 capture proves it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-16 | Tampering | divergent resume memory | mitigate | reconcileResume trusts pipeline-state.cjs and flags the frontmatter stale on disagreement (B1/D-166-02 made executable); the test asserts the disagreement resolution |
| T-166-17 | Repudiation | a re-run completed stage | mitigate | the Wave-1 isNext hard gate withholds an already-completed stage; the test asserts no double-run |
| T-166-18 | Information disclosure | provenanceFn frontmatter | accept | the stamp carries only the pipeline name + stage number (generic metadata); no user content; it never crosses to Brain |
| T-166-19 | Tampering | pipeline behavior drift on migration | mitigate | pipeline-state.cjs ships untested, so Task 1 captures the shipped store behavior FIRST and Task 2 re-asserts pre === post; the pipeline command contract is preserved |
| T-166-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (repoint + helper only); no install task |
</threat_model>

<verification>
- `node tests/test-pipeline-state-shipped-behavior.cjs` captures the shipped store behavior (first-ever coverage).
- `node tests/test-pipeline-on-runchain.cjs` passes (provenanceFn + sole-truth resume + no re-run + shipped-behavior-still-identical).
- `bash tests/run-all-166.sh` is green (Waves 1-5 + the pipeline-state shipped-behavior + migration suites + sweeps).
- commands/pipeline.md names runChain + describes the frontmatter scan as SECONDARY.
- Resume reads pipeline-state.cjs only; the frontmatter scan confirms but never competes.
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
The pipeline runs on the shared runChain spine with a provenanceFn stamping each stage's frontmatter,
~60 duplicated loop lines de-duped, and resume sourced from pipeline-state.cjs as the single chain
-state truth with the frontmatter scan demoted to a secondary confirming index (EXEC-01/02/04 + B1 +
D-166-02/04). The isNext hard gate prevents stage re-runs. Because pipeline-state.cjs shipped untested,
its FIRST coverage captures the shipped behavior before the additive wiring lands and asserts
pre === post, so the new wiring drifted nothing.
</success_criteria>

<output>
Create `.planning/phases/166-gated-chain-executor/166-05-SUMMARY.md` when done.
</output>
</content>
</invoke>
