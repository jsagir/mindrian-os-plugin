---
phase: 166-gated-chain-executor
plan: 04
type: execute
wave: 4
depends_on: [166-03]
files_modified:
  - scripts/act-command.cjs
  - commands/act.md
  - tests/test-act-prebehavior-snapshot.cjs
  - tests/fixtures/act-prebehavior-baseline.json
  - tests/test-act-on-runchain.cjs
  - tests/run-all-166.sh
autonomous: true
requirements: [EXEC-01, EXEC-04, D-166-04]
canon_parts: [3, 6, 7, 8, 10]

must_haves:
  truths:
    - "scripts/act-command.cjs --chain calls lib/core/chain-executor.cjs runChain instead of its own planChainRun walk; act is the thinnest caller (D-166-04)"
    - "the gated stop render + [stop] kill switch + the autonomous-safe prefix behavior are preserved byte-for-behavior (the F.0 gate + the trace are unchanged for the user)"
    - "act has NO existing regression suite, so 'no behavior drift' is VERIFIED not asserted: a pre-migration behavior-capture task snapshots renderChainReport output + the autonomous / gated / [stop] path outcomes against a committed fixture FIRST, then the post-runChain output is asserted byte/behavior-identical to that baseline"
    - "act passes provenanceFn:null (it is not the pipeline) and supplies postureFn/gateFn/onStep/onHalt callbacks only"
  artifacts:
    - path: "tests/test-act-prebehavior-snapshot.cjs"
      provides: "the PRE-migration behavior capture: snapshots renderChainReport + autonomous/gated/[stop] outcomes from the SHIPPED act (before any refactor) and writes/asserts the committed baseline fixture"
      min_lines: 50
    - path: "tests/fixtures/act-prebehavior-baseline.json"
      provides: "the committed pre-migration baseline (renderChainReport bytes + the three path outcomes) the post-migration suite asserts identity against"
      min_lines: 10
    - path: "scripts/act-command.cjs"
      provides: "the donor loop body removed; --chain now composes the chain then delegates to runChain with act's callbacks"
      contains: "runChain"
    - path: "tests/test-act-on-runchain.cjs"
      provides: "act-delegates-to-runChain + gate-preserved + stop-preserved + POST-equals-PRE-baseline identity assertions"
      min_lines: 50
  key_links:
    - from: "scripts/act-command.cjs"
      to: "lib/core/chain-executor.cjs"
      via: "main() composes the chain (recommender + resolver) then calls runChain with act callbacks"
      pattern: "runChain"
    - from: "scripts/act-command.cjs"
      to: "lib/core/recipe-maps.cjs"
      via: "postureFn delegates to postureForCommand (the ONE posture authority)"
      pattern: "recipe-maps"
    - from: "tests/test-act-on-runchain.cjs"
      to: "tests/fixtures/act-prebehavior-baseline.json"
      via: "post-migration output asserted byte/behavior-identical to the pre-migration baseline"
      pattern: "act-prebehavior-baseline"
---

<objective>
WAVE 4 MIGRATE act (the DONOR -> the thinnest caller). scripts/act-command.cjs was the source the
runChain spine was extracted from (Wave 2); this wave repoints act ONTO the spine so the donor stops
owning a loop. act --chain keeps composing the framework chain (recommender + resolver, the existing
seed logic at act-command.cjs:316-339) but then DELEGATES the walk to lib/core/chain-executor.cjs
runChain, supplying act's callbacks (postureFn = recipe-maps.postureForCommand, the default gateFn,
an onStep that dispatches framework-runner, an onHalt that renders the existing F.0 gate, and
provenanceFn:null because act is not the pipeline). The user-visible F.0 gate render and the [stop]
kill switch are preserved.

DRIFT IS VERIFIED, NOT ASSERTED (the gap the checker named): act has NO existing regression suite
(confirmed -- tests/ carries no act --chain test). "No behavior drift" therefore cannot be claimed by
fiat. This wave captures the SHIPPED behavior FIRST: a pre-migration snapshot task records
renderChainReport's output bytes plus the three path outcomes (whole-chain-autonomous, gated-halt,
[stop] kill switch) against a committed baseline fixture BEFORE the refactor lands. The refactor task
then asserts the post-runChain output is byte/behavior-identical to that baseline. The migration is
faithful only if PRE === POST, proven by instrumentation.

Purpose: D-166-04 locks the migration cadence -- act first (it is the donor), then pipeline, then
ignite, then the larry seam, ONE surface per wave, CI green between each. This wave proves the spine
on its own origin: if act behaves identically on the shared runtime, the extraction was faithful.
The three divergent loops begin collapsing into one.

Reuse before build (Canon Part 7): act-command.cjs already ships the seed logic, the F.0 gate render
(renderChainReport at act-command.cjs:151-226), and the workflow_stage journaling
(emitWorkflowStages at act-command.cjs:245-295) -- this wave KEEPS all of that and removes only the
planChainRun walk (act-command.cjs:131-147), replacing it with a runChain call whose onHalt re-uses
the existing F.0 render. A rewrite is rejected because the gate render + the journaling are shipped
and tested; only the loop ownership moves.

Output: the pre-migration behavior baseline + capture suite, act repointed onto runChain, the
identity-asserting migration regression suite, the act.md prose updated to name the shared spine, and
the Wave-4 entry in run-all-166.sh. NO em-dashes (CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-gated-chain-executor/166-SPEC.md
@.planning/phases/166-gated-chain-executor/166-CONTEXT.md
@scripts/act-command.cjs
@commands/act.md
@lib/core/chain-executor.cjs
@lib/core/recipe-maps.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the SHIPPED act behavior as a baseline BEFORE the refactor (drift net)</name>
  <files>tests/test-act-prebehavior-snapshot.cjs, tests/fixtures/act-prebehavior-baseline.json</files>
  <action>Create tests/test-act-prebehavior-snapshot.cjs that runs against the CURRENTLY SHIPPED scripts/act-command.cjs (BEFORE Task 2's refactor) and captures its behavior as a committed baseline. Require act-command.cjs and exercise renderChainReport (act-command.cjs:151-226) and planChainRun (act-command.cjs:131-147) over three deterministic fixtures: (1) a whole-chain-autonomous_safe workflow -> capture renderChainReport's exact output string (the "Whole chain is autonomous_safe ..." render at act-command.cjs:218-224) plus plan.wouldRun; (2) a chain with a non-autonomous_safe step at position 2 -> capture the gated-halt render (the F.0 gate block, plan.stopAt + plan.stopReason 'not autonomous_safe' at act-command.cjs:142); (3) the [stop]/kill-switch path outcome (what is recorded as "filed" above the stop). Serialize these captures (the renderChainReport bytes + the three path outcome objects) to tests/fixtures/act-prebehavior-baseline.json. If the baseline fixture does not yet exist, WRITE it (this is the pre-migration record); if it exists, ASSERT the shipped behavior still matches it (so the capture is idempotent and re-runnable). This task MUST run and commit its fixture before Task 2 mutates act-command.cjs -- it is the snapshot of the donor's truth. Mirror the plain node assert harness structure of tests/test-edges-domain-taxonomy-floor.cjs. NO fenced code in this action; cite the act-command.cjs line ranges. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-act-prebehavior-snapshot.cjs && test -f tests/fixtures/act-prebehavior-baseline.json && echo ACT_BASELINE_CAPTURED</automated>
  </verify>
  <done>The pre-migration baseline fixture exists, capturing renderChainReport's output bytes + the whole-autonomous / gated-halt / [stop] path outcomes from the SHIPPED act; the snapshot suite is idempotent (writes once, asserts thereafter) so the donor's truth is recorded before any refactor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repoint act-command.cjs --chain onto runChain + assert POST equals the PRE baseline (donor -> thinnest caller)</name>
  <files>scripts/act-command.cjs, tests/test-act-on-runchain.cjs</files>
  <behavior>
    - Test 1: main() over a fixture room with a fully-autonomous_safe chain calls runChain and the chain runs end to end (no gate), output passed each hop, ONE trace emitted -- behavior matches the pre-migration "whole chain is autonomous_safe" path (act-command.cjs:218-224) AND the renderChainReport output is byte/behavior-identical to tests/fixtures/act-prebehavior-baseline.json case 1.
    - Test 2: a chain with a non-autonomous_safe step at position 2 auto-runs step 1, HALTS at step 2 via onHalt rendering the existing F.0 gate, and resumes step 3 after the approve verb -- behavior matches the pre-migration gated path AND the baseline case 2 (stopAt + stopReason identical).
    - Test 3: the [stop] verb mid-chain flushes filed artifacts and ends cleanly with a complete trace (kill switch preserved); the "filed above the stop" outcome matches baseline case 3.
    - Test 4: act supplies provenanceFn:null and its postureFn delegates to recipe-maps.postureForCommand (act does not re-implement posture); the runChain return { trace, completed, haltedAt } drives the existing renderChainReport / emitWorkflowStages.
  </behavior>
  <action>Refactor scripts/act-command.cjs main() (act-command.cjs:316-348) so that after composing the chain (keep recommendFrameworkChain + composeWorkflow + validateChainAutonomy at act-command.cjs:336-339) it DELEGATES the walk to lib/core/chain-executor.cjs runChain instead of calling the local planChainRun (act-command.cjs:340). Supply act's callbacks: postureFn = recipe-maps.postureForCommand (the ONE posture authority, Wave 1); gateFn = the default posture x quality gate from chain-executor (Wave 2); onStep = act's framework-runner dispatch (act --chain's command body dispatches the brick; the script's onStep records the would-run step the way planChainRun did); onHalt = a callback that renders the EXISTING F.0 gate (reuse renderChainReport's gate block at act-command.cjs:172-217 -- keep the [BRAIN] chip + the F.0 pickShape path) and returns the user verb; provenanceFn = null (act is not the pipeline). Keep emitWorkflowStages (act-command.cjs:245-295) driven by the runChain trace. Remove the now-dead planChainRun walk (or keep it as a thin shim that builds the runChain step list, whichever preserves behavior with less churn) -- the loop OWNERSHIP must move to runChain. Then create tests/test-act-on-runchain.cjs covering the four behaviors against a fixture room, INCLUDING the byte/behavior-identity assertion against tests/fixtures/act-prebehavior-baseline.json (PRE === POST for all three captured paths -- this is what makes "no drift" VERIFIED). NO fenced code in this action; cite the donor line ranges. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-act-on-runchain.cjs && echo ACT_MIGRATED_OK</automated>
  </verify>
  <done>act --chain composes the chain then delegates the walk to runChain; the autonomous-safe path, the F.0 gated halt, and the [stop] kill switch all behave as before AND the post-migration output is asserted byte/behavior-identical to the pre-migration baseline (no drift VERIFIED); act owns no loop and re-implements no posture.</done>
</task>

<task type="auto">
  <name>Task 3: Update commands/act.md prose to name the shared spine + register the suites</name>
  <files>commands/act.md, tests/run-all-166.sh</files>
  <action>In commands/act.md, update the Chain Mode section (commands/act.md:216-228) so the prose names lib/core/chain-executor.cjs runChain as the shared runtime act --chain now rides (replace any language implying act owns its own loop). Keep the user-facing contract identical: the helper still picks the chain (recommender), composes /mos: commands (resolver), validateChainAutonomy FIRST, then runs via runChain stopping at the first non-autonomous_safe (or command-less) step with the "needs you here" F.0 gate; [continue] resumes, [stop] halts and files what ran. State that posture now comes from recipe-maps (the ONE authority) and the trace is the single chain trace. Do NOT introduce em-dashes (the existing file uses hyphens). Then append BOTH new suites to the CJS_SUITES array in tests/run-all-166.sh in dependency order: test-act-prebehavior-snapshot.cjs (the baseline capture, runs FIRST) then test-act-on-runchain.cjs (the migration + identity suite). There is NO pre-existing act regression suite to register (confirmed: tests/ carries none) -- the prebehavior snapshot IS the regression net this wave creates. Extend the em-dash sweep to cover commands/act.md and the two new suites. Keep the runner running to completion; exit 1 on any FAIL. NO em-dashes.</action>
  <verify>
    <automated>bash tests/run-all-166.sh && grep -q "chain-executor" commands/act.md && echo ACT_DOC_OK</automated>
  </verify>
  <done>commands/act.md names runChain as the shared spine, the user contract is unchanged, run-all-166.sh registers the act baseline-capture suite + the migration/identity suite and is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| act callbacks -> runChain | act must supply callbacks only; it must not re-own the loop or re-implement posture |
| onHalt render -> user | the F.0 gate is the material-decision boundary; its render must be preserved |
| migration -> existing behavior | a faithful extraction must not drift act's user-visible behavior; PRE-baseline === POST proves it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-13 | Tampering | act behavior drift on migration | mitigate | act has no shipped regression suite, so the pre-migration snapshot (Task 1) captures the SHIPPED renderChainReport + the three path outcomes to a committed baseline FIRST; test-act-on-runchain.cjs asserts PRE === POST -- drift is VERIFIED by instrumentation, not asserted |
| T-166-14 | Elevation of privilege | act bypassing the gate after migration | mitigate | act's onHalt re-uses the shipped F.0 gate render; the gateFn is the shared default (posture x quality + forced-material); no autonomous bypass path is added |
| T-166-15 | Spoofing | act re-implementing posture from memory | mitigate | postureFn delegates to recipe-maps.postureForCommand (the ONE authority); act names no command or posture from memory (act.md:228 discipline preserved) |
| T-166-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (repoint only); no install task |
</threat_model>

<verification>
- `node tests/test-act-prebehavior-snapshot.cjs` captures/asserts the pre-migration baseline.
- `node tests/test-act-on-runchain.cjs` passes (autonomous / gated halt / stop preserved AND PRE === POST identity).
- `bash tests/run-all-166.sh` is green (Waves 1-4 + the act baseline + migration suites + sweeps).
- commands/act.md names lib/core/chain-executor.cjs runChain and keeps the user contract identical.
- act owns no loop and re-implements no posture; no behavior drift is VERIFIED against the committed baseline.
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
act is the thinnest caller of runChain: it composes the chain and supplies callbacks, the spine runs
the walk, and the autonomous-safe path + F.0 gated halt + [stop] kill switch all behave exactly as
before (EXEC-01 + EXEC-04 + D-166-04). "No drift" is VERIFIED: the SHIPPED behavior is captured to a
committed baseline BEFORE the refactor and the post-runChain output is asserted byte/behavior-identical
to it. The donor no longer owns a loop.
</success_criteria>

<output>
Create `.planning/phases/166-gated-chain-executor/166-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
