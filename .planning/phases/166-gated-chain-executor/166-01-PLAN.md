---
phase: 166-gated-chain-executor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mcp/pipeline-state.cjs
  - lib/core/recipe-maps.cjs
  - tests/test-pipeline-state-isnext-gate.cjs
  - tests/test-recipe-maps-authority.cjs
  - tests/run-all-166.sh
autonomous: true
requirements: [EXEC-01, EXEC-04, B1, B4, D-166-02, D-166-03]
canon_parts: [7, 8, 9]

must_haves:
  truths:
    - "pipeline-state.cjs is the SOLE chain-state source of truth; checkPosition exposes a hard isNext gate that callers must honor before running a step (B1, D-166-02)"
    - "the artifact-frontmatter scan is documented as a SECONDARY index only, never a competing chain-state source"
    - "one read-only helper reads exactly three maps for exactly three jobs: command-registry=posture, connector-registry=wiring, projection=ranked next-reach (B4, D-166-03)"
    - "rankedNextReach is a CONTRACT-ONLY consumer of the projection: it gives the dark 207-node projection its first NAMED reader, but the runChain loop (Wave 2) drives next-step selection from decide(), NOT from rankedNextReach -- live nav-engine consumption of the projection cache stays DEFERRED with Phase 157 (SPEC Out-of-scope: 'Live Brain consumption of the Phase 157 projection ... deferred with 157; the executor reads decide() locally until then')"
    - "recipe-maps.cjs makes ZERO Brain calls and joins posture only from the LOCAL data/command-registry.json (Part 8)"
  artifacts:
    - path: "lib/mcp/pipeline-state.cjs"
      provides: "checkPosition.isNext promoted to a hard gate (gateable boolean + reason) plus a doc-comment naming this file as the sole chain-state truth"
      contains: "isNext"
    - path: "lib/core/recipe-maps.cjs"
      provides: "postureForCommand / wiringForReach / rankedNextReach -- one job per map, three existing maps, layered not merged"
      min_lines: 80
    - path: "tests/test-pipeline-state-isnext-gate.cjs"
      provides: "the isNext-hard-gate + sole-truth assertions"
      min_lines: 40
    - path: "tests/test-recipe-maps-authority.cjs"
      provides: "three-maps-three-jobs + zero-Brain-egress assertions"
      min_lines: 40
    - path: "tests/run-all-166.sh"
      provides: "phase aggregator scaffold (Wave 1 registers the two foundation suites)"
      min_lines: 40
  key_links:
    - from: "lib/core/recipe-maps.cjs"
      to: "lib/workflow/command-resolver.cjs"
      via: "postureForCommand delegates to validateChainAutonomy / the command-registry authority"
      pattern: "command-resolver"
    - from: "lib/core/recipe-maps.cjs"
      to: "data/connector-registry.json"
      via: "wiringForReach reads the connector spine (reach -> surface)"
      pattern: "connector-registry"
    - from: "lib/mcp/pipeline-state.cjs"
      to: "tests/test-pipeline-state-isnext-gate.cjs"
      via: "require checkPosition + recordStep"
      pattern: "checkPosition"
---

<objective>
WAVE 1 FOUNDATION: the two load-bearing pre-work blockers that MUST land before any runChain loop
code exists. (A) B1 reconciliation -- standardize chain resume/position on lib/mcp/pipeline-state.cjs
as the SOLE chain-state source of truth (D-166-02), demote the commands/pipeline.md:78-114
artifact-frontmatter scan to a SECONDARY index only, and promote checkPosition.isNext (today an
advisory boolean at pipeline-state.cjs:189-195) to a HARD gate the executor honors. (B) B4 three-map
authority wiring (D-166-03) -- one new read-only helper lib/core/recipe-maps.cjs that reads the three
EXISTING recipe maps, each for exactly ONE job, layered not merged: data/command-registry.json ->
posture/autonomy authority (via the shipped command-resolver), data/connector-registry.json -> reach
-> surface wiring, data/brain-orchestration-projection.json -> ranked next-reach.

Purpose: the SPEC names B1 and B4 as pre-work blockers that resolve BEFORE the loop is built
(166-SPEC.md "Pre-work blockers (resolve BEFORE the loop is built)"). Without B1 a resumed chain has
two disagreeing memories (pipeline-state.json vs the frontmatter scan); without B4 the three maps
drift into the divergence the executor exists to kill, and the dark 207-node projection
(data/brain-orchestration-projection.json) has no named consumer. This wave gives the projection its
first NAMED CONSUMER and gives the loop (Wave 2) one posture authority, one wiring authority, and one
ranked-suggestion authority.

TRUTH-IN-LABELING (D-166-03 + SPEC Out-of-scope): rankedNextReach is a CONTRACT-ONLY consumer. It is
the projection's first named reader (closing the "dark projection has no consumer" gap), but the
runChain loop in Wave 2 re-derives the next step from decide() (navigation-engine.cjs), NOT from
rankedNextReach. Live nav-engine consumption of the projection cache for ranked next-reach is
explicitly DEFERRED with Phase 157 (166-SPEC.md Out of scope: "Live Brain consumption of the Phase 157
projection for ranked next-reach (deferred with 157; the executor reads decide() locally until then)").
This wave wires the read CONTRACT only; it opens no live nav-engine consumption path.

Reuse before build (Canon Part 7): pipeline-state.cjs already ships read/write/initChain/recordStep/
checkPosition (lib/mcp/pipeline-state.cjs:63-196) -- this wave EXTENDS checkPosition with a hard-gate
shape and a sole-truth doc-comment; it does NOT rewrite the store. recipe-maps.cjs is a thin
read-only joiner over command-resolver.cjs (validateChainAutonomy / composeWorkflow at
command-resolver.cjs:110-143) plus two existing committed JSON maps; a rewrite is rejected because
the maps and the resolver already exist and are CI-tripwired. ~85 percent repoint.

Output: the extended pipeline-state.cjs, the new recipe-maps.cjs joiner, two foundation test suites,
and the run-all-166.sh aggregator scaffold. NO em-dashes anywhere (CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-gated-chain-executor/166-SPEC.md
@.planning/phases/166-gated-chain-executor/166-CONTEXT.md
@lib/mcp/pipeline-state.cjs
@lib/workflow/command-resolver.cjs
@commands/pipeline.md
@tests/run-all-156.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote checkPosition.isNext to a hard gate + name pipeline-state.cjs the sole chain-state truth (B1)</name>
  <files>lib/mcp/pipeline-state.cjs, tests/test-pipeline-state-isnext-gate.cjs</files>
  <behavior>
    - Test 1: after initChain(room, ['a','b','c'], 'local'), checkPosition(room, 'a').isNext === true and the returned gate field (e.g. { gate: 'run' }) authorizes 'a'; checkPosition(room, 'b').isNext === false and the gate withholds 'b' with a reason naming the expected-next tool.
    - Test 2: after recordStep(room, 'a', 'p/a.md'), checkPosition(room, 'b').isNext === true (position advanced); checkPosition(room, 'a').isNext === false (already run, not re-runnable).
    - Test 3: with no active chain (no state file), checkPosition(room, 'a') returns inPipeline:false and the gate withholds with reason 'no_active_chain' (never a silent true).
    - Test 4: the module exports a stable SOLE-TRUTH marker the executor + verdict can assert on (e.g. CHAIN_STATE_SOURCE === 'pipeline-state.json') proving this file is the single source per D-166-02.
  </behavior>
  <action>Extend lib/mcp/pipeline-state.cjs WITHOUT changing the existing read/write/initChain/recordStep signatures (reuse, do not rewrite -- the store ships at lines 63-223). Specifically: (a) widen checkPosition's return object (today lib/mcp/pipeline-state.cjs:189-195 returns { inPipeline, isNext, position, chain, previousOutput }) to ALSO carry a hard-gate field -- a gate enum ('run' when isNext is true, 'withhold' otherwise) plus a withhold reason ('no_active_chain' when no chain, 'not_next' naming the expected-next tool when isNext is false). Keep isNext byte-stable for existing callers; the gate field is additive. (b) Add a module-level SOLE-TRUTH constant (e.g. CHAIN_STATE_SOURCE = 'pipeline-state.json') and export it, with a doc-comment block above the module header stating that per D-166-02 this file is the SOLE chain-state source of truth and the commands/pipeline.md frontmatter scan is a SECONDARY index only (cite commands/pipeline.md:78-114). (c) Export the gate fields and the constant. Then create tests/test-pipeline-state-isnext-gate.cjs (plain node assert harness, mirror the structure of tests/test-edges-domain-taxonomy-floor.cjs) covering the four behaviors above against a tmp room dir. NO fenced code in this action; the change lives in pipeline-state.cjs following the cited line ranges. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-pipeline-state-isnext-gate.cjs && echo ISNEXT_GATE_OK</automated>
  </verify>
  <done>checkPosition returns a hard gate ('run'/'withhold' + reason), isNext stays byte-stable for prior callers, the SOLE-TRUTH constant is exported, and the test asserts the gate withholds non-next + no-chain steps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: recipe-maps.cjs -- three existing maps, one job each, layered (B4)</name>
  <files>lib/core/recipe-maps.cjs, tests/test-recipe-maps-authority.cjs</files>
  <behavior>
    - Test 1: postureForCommand('/mos:think-hats') returns the autonomy/posture authority answer joined from data/command-registry.json (via command-resolver validateChainAutonomy / the autonomous_safe + posture fields); an unknown command degrades to a withhold-default, never a fabricated 'autonomous_safe'.
    - Test 2: wiringForReach('context_block') returns the connector-registry surface(s) for that reach (read from data/connector-registry.json connectors[] where reach_id matches); an unknown reach returns [].
    - Test 3: rankedNextReach({...}) returns the ranked next-reach list from data/brain-orchestration-projection.json (the 207-node / 51-edge projection); a missing/malformed projection degrades to [] (never throws). NOTE this is the CONTRACT-ONLY reader: it is the projection's first NAMED consumer, but the Wave-2 loop drives next-step from decide(), not from this -- live nav-engine consumption is DEFERRED with Phase 157 (SPEC Out-of-scope). The doc-comment on rankedNextReach MUST state this so a future reader does not wire it into the loop by mistake.
    - Test 4: a grep/require-surface assertion that recipe-maps.cjs makes ZERO Brain calls (no mcp__brain_*, no brain-client require, no raw fetch) -- posture is joined only from the LOCAL registry (Part 8).
  </behavior>
  <action>Create lib/core/recipe-maps.cjs as a thin READ-ONLY joiner that reads the three EXISTING maps, each for exactly ONE job, LAYERED not merged (D-166-03). Function postureForCommand(command): delegate to lib/workflow/command-resolver.cjs -- reuse validateChainAutonomy (command-resolver.cjs:131-143) as the ONE posture authority; map the 3-posture vocabulary push_forward -> auto-run, hold/pull_back -> halt per the SPEC (the posture id bank is frozen in lib/core/sensors/sensor-types.cjs POSTURE_IDS). Function wiringForReach(reachId): read data/connector-registry.json connectors[] (the shape ships: each entry carries reach_id + surface + sub_mode + posture) and return the surface(s) whose reach_id matches -- this is the onStep dispatch-target lookup. Function rankedNextReach(opts): read data/brain-orchestration-projection.json (top keys nodes/edges; 207 nodes, 51 edges) and return the ranked next-reach candidates -- the chef's suggestion. Annotate rankedNextReach with a TRUTH-IN-LABELING doc-comment: it is a CONTRACT-ONLY reader (the projection's first named consumer), the Wave-2 runChain loop selects the next step from decide() NOT from this function, and live nav-engine consumption of the projection cache is DEFERRED with Phase 157 (166-SPEC.md Out of scope) -- this read opens no live nav-engine consumption path. Each reader caches per-process and degrades to empty on missing/malformed JSON (mirror the command-resolver _load degrade idiom at command-resolver.cjs:62-78). Make ZERO Brain calls; require NO brain-client; do NOT merge or retire any map. Then create tests/test-recipe-maps-authority.cjs covering the four behaviors above plus a forbidden-token scan over recipe-maps.cjs for mcp__brain_ / brain-client / raw fetch (mirror the run-all-156.sh Part-8 sweep regexes; use grep -v '^#' style filtering so a comment cannot self-invalidate the count). NO fenced code in this action. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-recipe-maps-authority.cjs && echo RECIPE_MAPS_OK</automated>
  </verify>
  <done>recipe-maps.cjs joins posture from command-registry, wiring from connector-registry, ranked next-reach from the projection -- one job per map, zero Brain egress, graceful degrade on each missing map; rankedNextReach carries the CONTRACT-ONLY / decide()-drives-the-loop / Phase-157-deferred doc-comment; the test proves the three jobs and the Part 8 clean surface.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold tests/run-all-166.sh as the phase aggregator</name>
  <files>tests/run-all-166.sh</files>
  <action>Create tests/run-all-166.sh as the phase aggregator scaffold, mirroring tests/run-all-156.sh end to end. Define a CJS_SUITES array; Wave 1 registers test-pipeline-state-isnext-gate.cjs and test-recipe-maps-authority.cjs (later waves append their suites). The per-suite loop runs to completion (a missing file gates to a FAIL line, never a crash -- mirror run-all-156.sh:54-60). Add a placeholder Part-8 grep sweep block over the Phase-166 lib surfaces present so far (lib/core/recipe-maps.cjs) using the same BRAIN_WRITE + RAW_FETCH regexes as run-all-156.sh, with grep -v '^#' filtering. Add the em-dash sweep written via the U+2014 codepoint escape (so the runner carries no literal em-dash) over the Phase-166 lib files + the test suites + run-all-166.sh itself. Final tally + exit 1 on any failure. set -uo pipefail; bash only; no emoji. NO em-dashes.</action>
  <verify>
    <automated>bash tests/run-all-166.sh && echo PHASE_GATE_SCAFFOLD_GREEN</automated>
  </verify>
  <done>run-all-166.sh runs the two Wave-1 suites + the Part 8 sweep + the em-dash sweep to completion, prints per-suite PASS/FAIL + a final tally, and exits 0 when green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chain-state read -> executor | the executor must read position from exactly ONE store; two stores = divergent resume memory |
| recipe-map read -> posture decision | posture authority must come from the LOCAL command-registry, never the Brain |
| projection read -> ranked suggestion | the projection is a LOCAL Brain-derived cache; reading it opens no Brain wire and no live nav-engine consumption path (deferred with Phase 157) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-01 | Tampering | dual chain-state truth | mitigate | pipeline-state.cjs exports CHAIN_STATE_SOURCE + a sole-truth doc-comment; the frontmatter scan is demoted to a documented secondary index (Wave 5 enforces it on the pipeline migration) |
| T-166-02 | Spoofing | fabricated posture for an unknown command | mitigate | postureForCommand degrades to a withhold-default, never a fabricated autonomous_safe (mirrors command-resolver degrade idiom) |
| T-166-03 | Information disclosure | recipe-maps reading the projection | accept | the projection is a LOCAL committed cache (Part 8 amendment entry 19); recipe-maps makes zero Brain calls; rankedNextReach is contract-only (no live nav-engine consumption, deferred with Phase 157); the test scans for mcp__brain_ / brain-client / raw fetch |
| T-166-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (pure node built-ins + existing JSON maps); no install task |
</threat_model>

<verification>
- `node tests/test-pipeline-state-isnext-gate.cjs` passes (hard gate withholds non-next + no-chain).
- `node tests/test-recipe-maps-authority.cjs` passes (three jobs + zero Brain egress + rankedNextReach contract-only doc-comment).
- `bash tests/run-all-166.sh` is green (both Wave-1 suites + Part 8 sweep + em-dash sweep).
- pipeline-state.cjs exports CHAIN_STATE_SOURCE and a sole-truth doc-comment citing commands/pipeline.md:78-114.
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
The two pre-work blockers are resolved before any loop code lands: pipeline-state.cjs is the single
chain-state truth with a hard isNext gate (B1, D-166-02), and recipe-maps.cjs gives the loop one
posture authority + one wiring authority + one ranked-suggestion authority over the three existing
maps without merging them (B4, D-166-03). rankedNextReach is labeled CONTRACT-ONLY (decide() drives
the loop; live consumption deferred with Phase 157). All foundation suites and the phase aggregator
are green.
</success_criteria>

<output>
Create `.planning/phases/166-gated-chain-executor/166-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
