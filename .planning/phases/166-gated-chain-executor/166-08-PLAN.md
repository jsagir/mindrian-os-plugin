---
phase: 166-gated-chain-executor
plan: 08
type: execute
wave: 8
depends_on: [166-01, 166-02, 166-03, 166-04, 166-05, 166-06, 166-07]
files_modified:
  - tests/test-chain-executor-verdict.cjs
  - tests/test-chain-executor-part8-leak.cjs
  - tests/run-all-166.sh
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, EXEC-04, EXEC-05, EXEC-06, B1, B2, B3, B4, D-166-01, D-166-02, D-166-03, D-166-04, D-166-05]
canon_parts: [3, 6, 8, 9, 10]

must_haves:
  truths:
    - "the adversarial verdict returns a structured {passed, findings[]} over the SPEC's eight acceptance tests plus the canon-guard checks (harness-as-code property 6)"
    - "the verdict proves the gate halts at material steps + forces-material irreversible steps + halts on quality:low (EXEC-03), the [stop] kill switch flushes (EXEC-04), and resume does not re-run (B1/D-166-02)"
    - "the Part 8 leak scan over all Phase-166 surfaces finds zero user-content-to-Brain / raw-fetch tokens; decide()'s return shape is asserted unchanged (B2)"
    - "tests/run-all-166.sh aggregates every Phase-166 suite into ONE PASS/FAIL phase gate (Part 8 leak scan, em-dash sweep, frozen-contract checks)"
  artifacts:
    - path: "tests/test-chain-executor-verdict.cjs"
      provides: "the adversarial structured verdict over the eight acceptance tests + canon guards"
      min_lines: 100
    - path: "tests/test-chain-executor-part8-leak.cjs"
      provides: "the Part 8 leak scan over all Phase-166 lib + command + skill + agent surfaces"
      min_lines: 50
    - path: "tests/run-all-166.sh"
      provides: "the finalized phase aggregator (all 8 waves' suites + sweeps + frozen-contract checks, runs to completion)"
      min_lines: 90
  key_links:
    - from: "tests/test-chain-executor-verdict.cjs"
      to: "lib/core/chain-executor.cjs"
      via: "the verdict drives runChain through every acceptance scenario"
      pattern: "runChain"
    - from: "tests/run-all-166.sh"
      to: "tests/test-chain-executor-verdict.cjs"
      via: "CJS_SUITES registration (the final phase gate)"
      pattern: "verdict"
---

<objective>
WAVE 8 VERIFY: the adversarial structured verdict (the harness-as-code property 6 -- "adversarial
verify returns a structured verdict, not a vibe"), mirroring the Phase 163 verify wave that caught
FINDING-163-06-01 (tests/test-trending-to-absurd-verdict.cjs). The verdict returns {passed,
findings[]} over the SPEC's eight acceptance tests plus the canon-guard checks (Part 3 material-halt,
Part 8 zero-egress, Part 9 proposed-not-confirmed, B2 decide()-unchanged). Plus the finalized
tests/run-all-166.sh that is the single PASS/FAIL phase gate (every suite + the Part 8 leak scan +
the em-dash sweep + the frozen-contract checks).

Purpose: the harness-as-code 9-property mandate (and D-166-05) requires an adversarial verify that
proves by instrumentation -- not by promise -- that the foundation (Waves 1-3) and the migrations
(Waves 4-7) honor the constitution. This is the leak-net that the Phase 163 precedent proved catches
real findings.

Reuse before build (Canon Part 7): the verdict structure is the SHIPPED Phase 163 pattern
(test-trending-to-absurd-verdict.cjs's five-check {passed, findings[]} form) and the run-all
aggregator is the SHIPPED tests/run-all-156.sh structure -- this wave MIRRORS both for Phase 166. A
rewrite of the verify substrate is rejected; the proven patterns are reused verbatim in shape.

Output: the verdict suite, the Part 8 leak suite, and the finalized run-all-166.sh. NO em-dashes
(CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-gated-chain-executor/166-SPEC.md
@.planning/phases/166-gated-chain-executor/166-CONTEXT.md
@tests/test-trending-to-absurd-verdict.cjs
@tests/run-all-156.sh
@lib/core/chain-executor.cjs
@lib/core/navigation-engine.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: The adversarial structured verdict + the Part 8 leak scan</name>
  <files>tests/test-chain-executor-verdict.cjs, tests/test-chain-executor-part8-leak.cjs</files>
  <action>Create tests/test-chain-executor-verdict.cjs returning a structured {passed, findings[]} (mirror tests/test-trending-to-absurd-verdict.cjs verbatim in shape) over the SPEC's eight acceptance tests (166-SPEC.md "Acceptance / tests") plus the canon guards: (1) three autonomous_safe steps run end to end with NO gate, output passed each hop, ONE trace (EXEC-01/02/04). (2) a hold-tagged step at position 2 auto-runs step 1, HALTS at step 2 rendering the gate, resumes step 3 after approve (EXEC-03). (3) a step returning quality:low forces a halt even though autonomous_safe (EXEC-02 quality carry). (4) an irreversible step forces a halt regardless of tag (EXEC-03 forced-material). (5) [stop] mid-chain flushes filed artifacts and ends cleanly with a complete trace (EXEC-04 kill switch). (6) resume -- kill a chain at step 2, resume from pipeline-state.json, step 1 is NOT re-run (B1/D-166-02 via the isNext hard gate). (7) Part 8 sweep -- delegate to the leak-scan suite (assert it exits zero); no user bytes reach the Brain; posture joined from the local registry only (B4/D-166-03). (8) regression -- the act/pipeline/ignite migration suites stay green (assert each Wave-4..7 suite exits zero). Plus canon guards: a B2 check that decide()'s return shape is recorded UNCHANGED in the trace (deep-equal the stubbed/real decide() return to the recorded decision_trace handle); a Part 9 check that any truth-claim node written along the chain lands review_status 'proposed' not 'confirmed'; an EXEC-05 check that a transient 5xx exhaustion returns a graceful partial preserving upstream; an EXEC-06 check that maxSteps caps the run. Each check pushes a {check, passed, detail} into findings[]; exit non-zero (print findings) if ANY check failed. Then create tests/test-chain-executor-part8-leak.cjs: a grep/scan over ALL Phase-166 surfaces (lib/core/chain-executor.cjs, lib/core/chain-retry.cjs, lib/core/recipe-maps.cjs, lib/mcp/pipeline-state.cjs, scripts/act-command.cjs, commands/act.md, commands/pipeline.md, commands/ignite.md, agents/larry-extended.md, skills/larry-personality/SKILL.md) for forbidden tokens: no mcp__brain_(write|store|upsert|ingest), no writeBrain/sendToBrain/ingestToBrain, no raw fetch( egress in the lib code, no hardcoded external http(s) endpoint (mirror the run-all-156.sh BRAIN_WRITE + RAW_FETCH regexes). Use grep -v '^#' style filtering where a comment could self-invalidate the count; never a bare unfiltered == 0 gate. NO fenced code in this action; mirror the cited Phase 163 verdict + run-all-156 sweep logic. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-chain-executor-verdict.cjs && node tests/test-chain-executor-part8-leak.cjs && echo VERDICT_OK</automated>
  </verify>
  <done>The verdict returns {passed:true, findings:[...]} over the eight acceptance tests + the canon guards (B2 decide()-unchanged, Part 9 proposed, EXEC-05 graceful partial, EXEC-06 cap); the Part 8 leak scan finds zero forbidden tokens across every Phase-166 surface.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize tests/run-all-166.sh as the single phase gate</name>
  <files>tests/run-all-166.sh</files>
  <action>Finalize tests/run-all-166.sh (built up across Waves 1-7) into the complete phase aggregator, mirroring tests/run-all-156.sh structure end to end. The CJS_SUITES array MUST list every Phase-166 suite in dependency order: test-pipeline-state-isnext-gate.cjs + test-recipe-maps-authority.cjs (Wave 1), test-chain-executor-loop.cjs + test-chain-executor-gate.cjs (Wave 2), test-chain-retry-backoff.cjs + test-chain-graceful-partial.cjs (Wave 3), test-act-on-runchain.cjs (Wave 4), test-pipeline-on-runchain.cjs (Wave 5), test-ignite-on-runchain.cjs (Wave 6), test-larry-handoff-seam.cjs (Wave 7), test-chain-executor-verdict.cjs + test-chain-executor-part8-leak.cjs (Wave 8), plus any registered act/pipeline/ignite/larry regression suites. The per-suite loop runs to completion (a missing file gates to a FAIL line, never a crash). Add the Part-8 grep sweep block over all Phase-166 lib + command + skill + agent surfaces (the BRAIN_WRITE + RAW_FETCH + external-http regexes as run-all-156.sh). Add the FROZEN-CONTRACT checks: (a) grep-assert that decide()'s return shape is not mutated by chain-executor.cjs (no reassignment of the decision object's known keys -- a lightweight grep for forbidden mutation patterns, plus the verdict's B2 deep-equal carries the real proof); (b) grep-assert the three recipe maps are read each for one job (command-registry for posture, connector-registry for wiring, projection for ranked next-reach) and none is merged. Add the em-dash sweep (U+2014 codepoint escape so the runner carries no literal em-dash) over all Phase-166 lib files, the four command/skill/agent surfaces, the test suites, and run-all-166.sh itself. Final tally + exit 1 on any failure. set -uo pipefail; bash only; no emoji. NO em-dashes.</action>
  <verify>
    <automated>bash tests/run-all-166.sh && echo PHASE_GATE_GREEN</automated>
  </verify>
  <done>run-all-166.sh runs every Phase-166 suite + the Part 8 sweep + the frozen-contract checks + the em-dash sweep to completion, prints a per-suite PASS/FAIL + final tally, and exits 0 only when the entire phase is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verdict suite -> all Phase-166 surfaces | adversarial: assumes each surface is hostile until proven compliant |
| run-all-166 -> CI | the single phase gate; a RED here blocks the phase |
| decide() return -> chain-executor | the engine contract is frozen; mutation is a B2 breach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-28 | Elevation of privilege | a material step auto-running undetected | mitigate | the verdict drives the gate scenarios (hold-tagged halt, quality:low halt, irreversible forced-material) and reports a finding if any auto-runs |
| T-166-29 | Information disclosure | any Phase-166 egress path | mitigate | the Part 8 leak scan over every lib + command + skill + agent surface finds zero Brain-write / raw-fetch / external-http tokens |
| T-166-30 | Tampering | decide() return-shape mutation | mitigate | the verdict B2 check deep-equals decide()'s return to the recorded trace handle; run-all-166 grep-asserts no forbidden mutation pattern |
| T-166-31 | Repudiation | a re-run resumed step | mitigate | the verdict resume check asserts step 1 is not re-run after a step-2 kill (B1/D-166-02 isNext hard gate) |
| T-166-32 | Denial of service | a silent chain drop on synthesis failure | mitigate | the verdict EXEC-05 check asserts a transient exhaustion returns a graceful partial preserving upstream |
| T-166-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages across the entire phase (pure bash + node:test/assert); no install task |
</threat_model>

<verification>
- `node tests/test-chain-executor-verdict.cjs` returns passed over the eight acceptance tests + the canon guards.
- `node tests/test-chain-executor-part8-leak.cjs` finds zero forbidden tokens.
- `bash tests/run-all-166.sh` is green (every suite + Part 8 sweep + frozen-contract checks + em-dash sweep).
- decide()'s return shape is asserted unchanged (B2); resume does not re-run upstream (B1).
- No em-dashes anywhere in the phase.
</verification>

<success_criteria>
The adversarial structured verdict proves by instrumentation that the runChain spine honors the
constitution end to end: the gate halts at material steps and forces-material irreversible/low-quality
steps (EXEC-03), the [stop] kill switch flushes (EXEC-04), retry exhaustion returns a graceful
partial (EXEC-05), maxSteps caps the run (EXEC-06), resume does not re-run (B1/D-166-02), decide()'s
shape is unchanged (B2), the three maps stay layered (B4/D-166-03), and no Phase-166 surface egresses
to the Brain (Part 8). run-all-166.sh is the single green phase gate.
</success_criteria>

<output>
Create `.planning/phases/166-gated-chain-executor/166-08-SUMMARY.md` when done.
</output>
