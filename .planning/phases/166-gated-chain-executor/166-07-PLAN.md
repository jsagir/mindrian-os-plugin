---
phase: 166-gated-chain-executor
plan: 07
type: execute
wave: 7
depends_on: [166-06]
files_modified:
  - agents/larry-extended.md
  - skills/larry-personality/SKILL.md
  - tests/test-larry-handoff-seam.cjs
  - tests/run-all-166.sh
autonomous: true
requirements: [EXEC-03, EXEC-04, D-166-04, D-166-05]
canon_parts: [3, 7, 8, 10]

must_haves:
  truths:
    - "after a gate-approved step, larry-extended hands the resolved chain to lib/core/chain-executor.cjs runChain instead of waiting for re-typed commands (the handoff seam, D-166-04)"
    - "the larry-personality suggest->gate->wait contract gains an auto-sequence branch, but gateFn STILL halts on every non-autonomous_safe step (SKILL.md:59 'the navigator always decides', D-166-05)"
    - "the auto-sequence branch never bypasses the Decision Gate for a material step; GUIDED-default and one-reach-per-beat are preserved"
    - "the suggest surfaces stay Part 8 clean: posture joined from the local registry, the Brain push remains an OFFER (the fetch fires only after the gate)"
  artifacts:
    - path: "agents/larry-extended.md"
      provides: "the post-gate handoff: a resolved autonomous_safe prefix is handed to runChain; the first material step halts at the gate"
      contains: "chain-executor"
    - path: "skills/larry-personality/SKILL.md"
      provides: "the suggest->gate->wait contract extended with an auto-sequence branch that still halts on every non-autonomous_safe step"
      contains: "auto-sequence"
    - path: "tests/test-larry-handoff-seam.cjs"
      provides: "post-gate-handoff + still-halts-on-material + GUIDED-default-preserved assertions"
      min_lines: 50
  key_links:
    - from: "agents/larry-extended.md"
      to: "lib/core/chain-executor.cjs"
      via: "the gate-approved chain is handed to runChain (the suggest->run seam gains an auto-sequence branch)"
      pattern: "runChain"
    - from: "skills/larry-personality/SKILL.md"
      to: "lib/core/recipe-maps.cjs"
      via: "the auto-sequence branch reads posture from the local registry; gateFn halts on every non-autonomous_safe step"
      pattern: "autonomous_safe"
---

<objective>
WAVE 7 MIGRATE the larry-extended / larry-personality handoff seam (the LAST migration). Today these
surfaces only SUGGEST a next step: larry-personality's reach rules surface ONE line and end at a
Decision Gate (skills/larry-personality/SKILL.md:59-63), and larry-extended waits for the navigator
to re-type the command. This wave wires the suggest->run seam: after a gate-APPROVED step,
larry-extended hands the RESOLVED chain to lib/core/chain-executor.cjs runChain (the autonomous_safe
prefix runs under the spine) instead of waiting for re-typed commands. The larry-personality
suggest->gate->wait contract gains an AUTO-SEQUENCE branch -- but gateFn STILL halts on every
non-autonomous_safe step ("the navigator always decides", SKILL.md:59; D-166-05). The auto-sequence
NEVER bypasses the gate for a material step.

Purpose: D-166-04 sequences the larry seam last -- it is the conversational front door, so migrating
it after act/pipeline/ignite means the spine is already proven on the command surfaces before Larry
hands work to it. This closes the loop from Part 10 (conversation as product): Larry suggests, the
human approves at the gate, and the approved autonomous prefix runs underneath as machinery,
surfacing only at the next material gate (Part 10's "the chain runs underneath, surfacing only at
material gates").

Reuse before build (Canon Part 7): the suggest line, the GUIDED default, one-reach-per-beat, and the
Part 8 OFFER-not-fetch discipline all ship in larry-personality SKILL.md:59-63 -- this wave KEEPS
all of that and ADDS only the auto-sequence branch (the handoff to runChain after an approve). The
resolver discipline ("never a slug typed from memory") is preserved: the chain handed to runChain
came from composeWorkflow / recipe-maps, never from Larry's memory. A rewrite is rejected because the
reach rules + the gate are the doctrine that defines Larry; only the post-approve handoff is new.

Output: the larry-extended handoff, the larry-personality auto-sequence branch, a seam regression
suite, and the Wave-7 entry in run-all-166.sh. NO em-dashes (CLAUDE.md HARD RULE). One surface per
wave; this wave touches only the two larry surfaces (no overlap with act/pipeline/ignite).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-gated-chain-executor/166-SPEC.md
@.planning/phases/166-gated-chain-executor/166-CONTEXT.md
@agents/larry-extended.md
@skills/larry-personality/SKILL.md
@lib/core/chain-executor.cjs
@lib/core/recipe-maps.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The post-gate handoff seam + the auto-sequence branch (still halts on material)</name>
  <files>agents/larry-extended.md, skills/larry-personality/SKILL.md, tests/test-larry-handoff-seam.cjs</files>
  <behavior>
    - Test 1 (handoff): given a resolved chain whose first N steps are autonomous_safe and step N+1 is material, after the human APPROVES at the gate, the seam hands the chain to runChain; runChain runs steps 1..N autonomously and HALTS at step N+1 (the seam does not wait for N re-typed commands).
    - Test 2 (still halts on material): the auto-sequence branch's gateFn returns 'halt' on EVERY non-autonomous_safe step -- a material step in the middle of the handed chain stops the auto-run and surfaces the Decision Gate (SKILL.md:59 'the navigator always decides'); no material step is ever auto-run.
    - Test 3 (GUIDED preserved): with no approve (GUIDED default), the seam does NOT auto-sequence -- it surfaces ONE suggest line and ends at the gate, exactly as today (one reach per beat preserved).
    - Test 4 (Part 8 + resolver discipline): the chain handed to runChain came from composeWorkflow / recipe-maps (never a slug from Larry's memory); posture is joined from the local registry; the Brain push stays an OFFER (no fetch before the gate).
  </behavior>
  <action>In agents/larry-extended.md, add a post-gate HANDOFF behavior: after a Decision-Gate APPROVE of a suggested next step, larry-extended hands the RESOLVED chain (the composeWorkflow output, autonomous_safe prefix) to lib/core/chain-executor.cjs runChain rather than waiting for the navigator to re-type each command; runChain auto-runs the autonomous_safe prefix and halts at the first material step, returning control to Larry at that gate. In skills/larry-personality/SKILL.md, extend the reach-rules / suggest contract (SKILL.md:55-63) with an AUTO-SEQUENCE branch: when the navigator approves an autonomous_safe next step, Larry MAY let runChain auto-sequence the autonomous_safe run -- but the gateFn MUST still halt on every non-autonomous_safe step (preserve SKILL.md:59 'the navigator always decides' VERBATIM as the governing rule; the auto-sequence is strictly the registry-blessed autonomous_safe subset, D-166-05). Preserve GUIDED-default (no approve = no auto-sequence, one suggest line, end at the gate), one-reach-per-beat, the resolver discipline (the chain came from composeWorkflow / recipe-maps, never a slug from memory), and the Part 8 OFFER-not-fetch rule (SKILL.md:59 Brain push). State that posture for the auto-sequence is joined from the local registry via recipe-maps. Then create tests/test-larry-handoff-seam.cjs covering the four behaviors with stub gate/approve + a stub resolved chain. NO fenced code in this action; cite the SKILL.md line ranges. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-larry-handoff-seam.cjs && echo LARRY_SEAM_OK</automated>
  </verify>
  <done>After an approve, the seam hands the autonomous_safe prefix to runChain and halts at the first material step; the auto-sequence branch still halts on every non-autonomous_safe step; GUIDED-default + one-reach-per-beat + the resolver/Part 8 discipline are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Register the larry seam + regression suites in run-all-166.sh</name>
  <files>tests/run-all-166.sh</files>
  <action>Append the Wave-7 suite (test-larry-handoff-seam.cjs) to the CJS_SUITES array in tests/run-all-166.sh after the Wave-6 entries, and register any existing larry-personality / larry-extended doctrine regression suite present in tests/ (grep for a larry or reach-rules test) so the seam change cannot drift the GUIDED-default doctrine unnoticed. Extend the Part-8 grep sweep to cover the two larry surfaces (assert the OFFER-not-fetch discipline survives: no new mcp__brain_ fetch added before the gate; grep -v '^#' filtering). Extend the em-dash sweep (U+2014 codepoint escape) to cover agents/larry-extended.md, skills/larry-personality/SKILL.md, and the new suite. Keep the runner running to completion; exit 1 on any FAIL. NO em-dashes.</action>
  <verify>
    <automated>bash tests/run-all-166.sh && grep -q "auto-sequence" skills/larry-personality/SKILL.md && echo LARRY_DOC_OK</automated>
  </verify>
  <done>run-all-166.sh runs Waves 1-7 + the larry doctrine regression suite + the extended Part 8 + em-dash sweeps to completion and exits 0 when green; the auto-sequence branch is documented in the SKILL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approve -> auto-sequence | only an explicit approve may trigger the handoff; GUIDED default never auto-runs |
| auto-sequence -> material step | the auto-run must halt at every non-autonomous_safe step (the navigator always decides) |
| suggest line -> Brain | the Brain push stays an OFFER; the fetch fires only after the gate (Part 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-24 | Elevation of privilege | the auto-sequence bypassing a material gate | mitigate | the gateFn halts on every non-autonomous_safe step (SKILL.md:59 preserved verbatim, D-166-05); the test asserts a mid-chain material step halts |
| T-166-25 | Tampering | GUIDED-default doctrine drift | mitigate | no-approve = no auto-sequence (one suggest line, end at the gate); the larry doctrine regression suite is registered |
| T-166-26 | Spoofing | a slug handed from Larry's memory | mitigate | the handed chain is the composeWorkflow / recipe-maps output; the resolver discipline (never a slug from memory) is preserved and asserted |
| T-166-27 | Information disclosure | a Brain fetch before the gate | mitigate | the OFFER-not-fetch rule (SKILL.md:59) is preserved; the Part 8 sweep asserts no new pre-gate Brain fetch |
| T-166-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (doctrine repoint only); no install task |
</threat_model>

<verification>
- `node tests/test-larry-handoff-seam.cjs` passes (handoff + still-halts-on-material + GUIDED preserved + resolver/Part 8 discipline).
- `bash tests/run-all-166.sh` is green (Waves 1-7 + the larry doctrine regression suite + sweeps).
- skills/larry-personality/SKILL.md documents the auto-sequence branch with the 'navigator always decides' rule preserved.
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
The larry seam suggests, the human approves at the gate, and the approved autonomous_safe prefix runs
under runChain, halting at the first material step -- the auto-sequence branch never bypasses a
material gate (EXEC-03/04 + D-166-04/05). GUIDED-default, one-reach-per-beat, the resolver discipline,
and the Part 8 OFFER-not-fetch rule are all preserved, and the doctrine regression suite is green.
</success_criteria>

<output>
Create `.planning/phases/166-gated-chain-executor/166-07-SUMMARY.md` when done.
</output>
