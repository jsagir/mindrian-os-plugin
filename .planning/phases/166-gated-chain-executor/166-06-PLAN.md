---
phase: 166-gated-chain-executor
plan: 06
type: execute
wave: 6
depends_on: [166-05]
files_modified:
  - commands/ignite.md
  - tests/test-ignite-on-runchain.cjs
  - tests/run-all-166.sh
autonomous: true
requirements: [EXEC-03, EXEC-04, D-166-04, D-166-05]
canon_parts: [3, 6, 7, 9, 10]

must_haves:
  truths:
    - "ignite's hand-rolled B1->B2->B3 birth gates re-host on lib/core/chain-executor.cjs runChain as an ALL-MATERIAL chain (every gate halts; nothing auto-runs)"
    - "the birthRoom ordering guard holds: B3 never renders until birthRoom succeeds (room.db created, focus set, registry flipped) -- the Part 9 promotion moment completes before the first in-room gate"
    - "gateFn halts on every birth step (all forced-material); the [stop]/Defer path exits gracefully preserving the scratchpad birth answers"
    - "ignite's existing birth-gate behavior + the umbilical/express/from-brief entry paths stay green (regression)"
  artifacts:
    - path: "commands/ignite.md"
      provides: "B1/B2/B3 re-hosted on runChain as an all-material chain with the birthRoom ordering guard preserved"
      contains: "chain-executor"
    - path: "tests/test-ignite-on-runchain.cjs"
      provides: "all-material-halts + birthRoom-ordering-guard + defer-preserves-scratchpad assertions"
      min_lines: 60
  key_links:
    - from: "commands/ignite.md"
      to: "lib/core/chain-executor.cjs"
      via: "the three birth gates run as an all-material runChain (every gateFn returns halt)"
      pattern: "runChain"
    - from: "commands/ignite.md"
      to: "lib/core/navigation/room-birth.cjs"
      via: "B3 fires only after birthRoom succeeds (the ordering guard)"
      pattern: "birthRoom"
---

<objective>
WAVE 6 MIGRATE ignite (CONSUMER -> the spine). commands/ignite.md hand-rolls three birth gates in
sequence (B1 starting point at ignite.md:57-76, B2 blueprint at ignite.md:82-94, B3 first win at
ignite.md:98-102). This wave re-hosts those gates on lib/core/chain-executor.cjs runChain as an
ALL-MATERIAL chain: every birth step is forced-material so gateFn halts at each one (nothing
auto-runs -- birth is all human decisions). The load-bearing invariant preserved is the birthRoom
ordering guard: B3 (the first in-room Decision Gate) NEVER renders until birthRoom succeeds (room.db
created, focus set, registry flipped) -- the Part 9 promotion moment must complete first (the
T-155-06-01 mitigation at ignite.md:100).

Purpose: D-166-04 sequences ignite third (after pipeline). ignite is the surface where the gate is
ALWAYS material -- it exercises the forced-material side of the EXEC-03 predicate end to end (the
opposite extreme from act's autonomous-safe prefix). Migrating it proves runChain handles a chain
where the [stop]/Defer kill path and the ordering guard matter more than throughput.

Reuse before build (Canon Part 7): ignite already ships the F.1/F.0 gate renders, writeScratchpad
BirthAnswer journaling (ignite.md:76,92,94), the new-project scaffold delegation (ignite.md:84-86),
and the birthRoom promotion call (ignite.md:90, lib/core/navigation/room-birth.cjs) -- this wave KEEPS
all of that and only moves the SEQUENCING of the three gates onto runChain so the birth chain is one
trace under one runtime. A rewrite is rejected because the gates, the scaffold, and the promotion are
shipped and tested; only the loop that walks B1->B2->B3 moves to the shared spine.

Output: ignite re-hosted on runChain as an all-material chain, a migration regression suite, and the
Wave-6 entry in run-all-166.sh. NO em-dashes (CLAUDE.md HARD RULE). One surface per wave; this wave
touches only ignite (no overlap with act/pipeline).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-gated-chain-executor/166-SPEC.md
@.planning/phases/166-gated-chain-executor/166-CONTEXT.md
@commands/ignite.md
@lib/core/chain-executor.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-host B1/B2/B3 as an all-material runChain with the birthRoom ordering guard</name>
  <files>commands/ignite.md, tests/test-ignite-on-runchain.cjs</files>
  <behavior>
    - Test 1 (all-material): the three birth steps run as a runChain where gateFn returns 'halt' for every step (each step is forced-material); NONE auto-runs, mirroring "the navigator always decides" (the all-material extreme of EXEC-03).
    - Test 2 (ordering guard): B3 (first win, the in-room gate) does NOT render until birthRoom succeeds; if birthRoom returns ok:false, the chain halts after B2 and B3 never fires (the T-155-06-01 mitigation, ignite.md:100).
    - Test 3 (promotion sequencing): the trace shows B1 -> B2 -> (birthRoom promotion) -> B3 in order; B3's in-room ranker path is reachable only after the room.db-created / focus-set / registry-flipped transition.
    - Test 4 (Defer/stop): the Defer or [stop] verb at B2 exits gracefully, preserving the scratchpad birth answers (writeScratchpadBirthAnswer) for the next session (ignite.md:94); no half-promoted room is left.
  </behavior>
  <action>In commands/ignite.md, re-host the three birth gates (B1 ignite.md:57-76, B2 ignite.md:82-94, B3 ignite.md:98-102) as an ALL-MATERIAL chain on lib/core/chain-executor.cjs runChain: build a three-step birth chain and supply a gateFn that returns 'halt' for EVERY step (all forced-material -- birth is all human decisions; this is D-166-05 "gateFn MUST halt on any non-autonomous_safe step" at its extreme). onHalt renders the existing F.1/F.0 gate for each birth step and returns the user verb; onStep performs the existing per-gate side effect (writeScratchpadBirthAnswer for B1; the new-project scaffold delegation + birthRoom for B2; the closeReach/recordSelectorDecision for B3). Preserve the birthRoom ORDERING GUARD: insert the birthRoom promotion BETWEEN B2-approve and B3 so the chain only advances to B3 after birthRoom returns ok:true (room.db created, focus set, registry flipped); if birthRoom returns ok:false, the chain halts after B2 and B3 never renders (the T-155-06-01 mitigation). Keep the entry-path branches (Just Talk / --express / --from-brief / --from-opportunity at ignite.md:51-59) selecting which gates fire. The Defer/[stop] verb exits gracefully preserving the scratchpad. State the three gates now run as ONE birth trace under runChain. Then create tests/test-ignite-on-runchain.cjs covering the four behaviors with stub gate renders + a stub birthRoom (ok:true and ok:false cases). NO fenced code in this action; cite the ignite.md line ranges. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-ignite-on-runchain.cjs && echo IGNITE_MIGRATED_OK</automated>
  </verify>
  <done>The three birth gates run as an all-material runChain (every step halts), the birthRoom ordering guard holds (B3 never renders before promotion succeeds), and Defer/[stop] exits gracefully preserving the scratchpad.</done>
</task>

<task type="auto">
  <name>Task 2: Register the ignite migration + regression suites in run-all-166.sh</name>
  <files>tests/run-all-166.sh</files>
  <action>Append the Wave-6 suite (test-ignite-on-runchain.cjs) to the CJS_SUITES array in tests/run-all-166.sh after the Wave-5 entries, and register any existing ignite / birth-gate regression suite present in tests/ (grep for an ignite or birthRoom test) so the migration cannot drift the birth-gate behavior unnoticed. Extend the em-dash sweep (U+2014 codepoint escape) to cover commands/ignite.md and the new suite. Keep the runner running to completion; final tally + exit 1 on any FAIL. NO em-dashes.</action>
  <verify>
    <automated>bash tests/run-all-166.sh && echo PHASE_GATE_W6_GREEN</automated>
  </verify>
  <done>run-all-166.sh runs Waves 1-6 + the ignite migration + regression suites + the sweeps to completion and exits 0 when green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| birth step -> auto-run | no birth step may ever auto-run; all are forced-material |
| birthRoom promotion -> B3 | the in-room gate must not render before the room exists (Part 9 promotion ordering) |
| Defer/stop -> room state | a deferred birth must leave no half-promoted room |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-20 | Elevation of privilege | a birth step auto-running | mitigate | gateFn returns halt for every birth step (all forced-material, D-166-05); the test asserts none auto-runs |
| T-166-21 | Tampering | B3 rendering before promotion | mitigate | the ordering guard inserts birthRoom between B2-approve and B3; B3 never fires on birthRoom ok:false (T-155-06-01); the test asserts the ordering |
| T-166-22 | Repudiation | lost birth answers on Defer | mitigate | the Defer/[stop] path preserves writeScratchpadBirthAnswer; the test asserts the scratchpad survives |
| T-166-23 | Tampering | ignite behavior drift on migration | mitigate | the ignite regression suite is registered in run-all-166.sh; the entry-path branches are preserved |
| T-166-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (repoint only); no install task |
</threat_model>

<verification>
- `node tests/test-ignite-on-runchain.cjs` passes (all-material halts + ordering guard + Defer preserves scratchpad).
- `bash tests/run-all-166.sh` is green (Waves 1-6 + the ignite regression suite + sweeps).
- commands/ignite.md re-hosts B1/B2/B3 on runChain with the birthRoom ordering guard preserved.
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
ignite's three birth gates run as an all-material chain on the shared runChain spine: every step is
forced-material and halts, the birthRoom ordering guard holds so B3 never renders before the Part 9
promotion completes, and the Defer/[stop] kill path exits gracefully preserving the scratchpad
(EXEC-03/04 + D-166-04/05). The ignite regression suite is green.
</success_criteria>

<output>
Create `.planning/phases/166-gated-chain-executor/166-06-SUMMARY.md` when done.
</output>
