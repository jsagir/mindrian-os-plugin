---
phase: 166-gated-chain-executor
plan: 03
type: execute
wave: 3
depends_on: [166-02]
files_modified:
  - lib/core/chain-executor.cjs
  - lib/core/chain-retry.cjs
  - tests/test-chain-retry-backoff.cjs
  - tests/test-chain-graceful-partial.cjs
  - tests/run-all-166.sh
autonomous: true
requirements: [EXEC-05, D-166-01]
canon_parts: [6, 7, 8, 9]

must_haves:
  truths:
    - "a transient 5xx (500/502/503/529) on any chain step triggers bounded retry-with-backoff (EXEC-05, D-166-01)"
    - "on retry exhaustion runChain returns a GRACEFUL PARTIAL: upstream results preserved + a failure marker, never a silent drop (the AION failure mode)"
    - "a non-transient error (4xx, validation) fails fast without retry; only the named transient codes back off"
    - "partial re-run reuses the resume/journal substrate (pipeline-state.cjs, D-166-02); no new orchestration path is built"
  artifacts:
    - path: "lib/core/chain-retry.cjs"
      provides: "isTransient(err) + withBackoff(fn, opts) -- bounded retry on the four transient codes, reusable by onStep"
      min_lines: 60
    - path: "lib/core/chain-executor.cjs"
      provides: "onStep wrapped in withBackoff; exhaustion folds a failure marker into the trace + returns graceful partial"
      contains: "graceful"
    - path: "tests/test-chain-retry-backoff.cjs"
      provides: "transient-retries + non-transient-fail-fast + backoff-bound assertions"
      min_lines: 50
    - path: "tests/test-chain-graceful-partial.cjs"
      provides: "exhaustion-preserves-upstream + failure-marker + resume-from-journal assertions"
      min_lines: 50
  key_links:
    - from: "lib/core/chain-executor.cjs"
      to: "lib/core/chain-retry.cjs"
      via: "onStep dispatch wrapped in withBackoff(isTransient)"
      pattern: "withBackoff"
    - from: "lib/core/chain-executor.cjs"
      to: "lib/mcp/pipeline-state.cjs"
      via: "graceful partial preserves position via the resume/journal substrate"
      pattern: "pipeline-state"
---

<objective>
WAVE 3 RELIABILITY: EXEC-05 -- retry/backoff on transient 5xx plus graceful-partial on exhaustion,
making the executor ship reliability-complete (D-166-01 locks this as a HARD requirement in this
phase, not a fast-follow). A transient 5xx (500/502/503/529) on any chain step -- especially the
terminal synthesis step -- triggers bounded retry-with-backoff; on exhaustion runChain returns a
GRACEFUL PARTIAL (upstream results preserved + a failure marker) so the chain is never silently
dropped (the AION failure mode, SEED-028). Partial re-run reuses the resume/journal substrate from
Wave 1 (pipeline-state.cjs, D-166-02); no new orchestration path is built.

Purpose: SEED-028 (workflow synthesis retry/fallback) is folded into Phase 166 as EXEC-05 so a
transient remote hiccup on the last expensive synthesis step does not throw away everything upstream.
The failure mode this closes is silent drop -- the chain dies and the user loses the work that
already ran. Graceful partial converts that into a resumable, marked partial.

Reuse before build (Canon Part 7): the retry substrate is a thin helper (chain-retry.cjs) that the
runChain onStep dispatch wraps; the resume/journal is the SHIPPED pipeline-state.cjs
recordStep/checkPosition (lib/mcp/pipeline-state.cjs:122-196) extended in Wave 1 -- partial re-run
re-enters the loop at the journaled position, it does NOT build a parallel orchestrator. A rewrite is
rejected because the journal + the loop already exist; EXEC-05 is a wrapper + an exhaustion branch.

Output: chain-retry.cjs, the runChain graceful-partial branch, two reliability test suites, and the
Wave-3 entries in run-all-166.sh. NO em-dashes anywhere (CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-gated-chain-executor/166-SPEC.md
@.planning/phases/166-gated-chain-executor/166-CONTEXT.md
@lib/core/chain-executor.cjs
@lib/mcp/pipeline-state.cjs
@tests/run-all-156.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chain-retry.cjs -- bounded backoff on the four transient codes (EXEC-05)</name>
  <files>lib/core/chain-retry.cjs, tests/test-chain-retry-backoff.cjs</files>
  <behavior>
    - Test 1: isTransient recognizes status 500, 502, 503, 529 (and an err carrying one of those in .status / .statusCode / a parseable message) as transient -> true; a 400 / 401 / 404 / 422 validation error -> false.
    - Test 2: withBackoff(fn, {retries:3}) where fn throws a transient error twice then succeeds resolves with the success value after exactly 2 retries (assert the attempt count).
    - Test 3: withBackoff where fn throws a NON-transient error fails fast on the first attempt (no retry; assert attempt count === 1).
    - Test 4: withBackoff where fn always throws transient exhausts after `retries` attempts and rethrows / returns an exhaustion marker the caller can detect (assert attempts === retries+1 and the marker names the last transient code); backoff delays are bounded (use an injectable sleep so the test runs instantly and asserts the delays grow but are capped).
  </behavior>
  <action>Create lib/core/chain-retry.cjs exporting isTransient(err) and withBackoff(fn, opts). isTransient: true only for the four named transient codes 500/502/503/529 (read from err.status / err.statusCode / a regex over err.message); everything else (4xx, validation, type errors) is non-transient -> fail fast. withBackoff(fn, { retries, baseDelayMs, maxDelayMs, sleep }): call fn; on a transient throw, wait an exponential-with-cap delay (baseDelayMs * 2^attempt, capped at maxDelayMs) via an INJECTABLE sleep (default a real timer; tests pass a no-op recorder) and retry up to `retries` times; on a non-transient throw, rethrow immediately; on exhaustion return/throw an exhaustion marker carrying the last code so runChain can branch to graceful partial. Pure node built-ins; zero new packages. Then create tests/test-chain-retry-backoff.cjs covering the four behaviors with an injected sleep + a fault-injecting fn. NO fenced code in this action. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-chain-retry-backoff.cjs && echo RETRY_OK</automated>
  </verify>
  <done>isTransient recognizes only the four 5xx codes; withBackoff retries transient failures with bounded exponential delay, fails fast on non-transient, and returns an exhaustion marker after the bound.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: graceful-partial on exhaustion + resume from the journal (EXEC-05 / D-166-01)</name>
  <files>lib/core/chain-executor.cjs, tests/test-chain-graceful-partial.cjs</files>
  <behavior>
    - Test 1: runChain where step 3 of 4 always throws a transient error -> after retry exhaustion, runChain returns { completed:false, haltedAt: step3, partial:true } with the trace PRESERVING steps 1 and 2 (upstream results intact, never dropped) and a failure marker for step 3.
    - Test 2: the failure marker names the transient code + the step; the upstream chain_outputs are still readable in the trace (graceful, not a throw).
    - Test 3: resume -- after a graceful partial journaled to pipeline-state.cjs, re-entering runChain resumes at the journaled position (step 3), and steps 1 and 2 are NOT re-run (reuses the Wave-1 isNext hard gate + recordStep journal, D-166-02).
    - Test 4: a non-transient error at a step fails fast (no retry) and still returns a graceful partial preserving upstream (a hard failure must not silently drop the chain either).
  </behavior>
  <action>Wrap the onStep dispatch inside runChain (lib/core/chain-executor.cjs) in chain-retry.withBackoff(isTransient): each step's onStep call retries on a transient 5xx and fails fast on non-transient. On retry exhaustion (or a non-transient failure), do NOT throw away the chain: fold a { step, failure: { code, reason }, partial:true } marker into the ONE trace, journal the position via the Wave-1 pipeline-state.recordStep / the isNext hard gate (so a later runChain re-entry resumes at the failed step and does not re-run upstream -- D-166-02), and return { trace, completed:false, haltedAt: failedStep, partial:true }. The upstream chain_outputs already in the trace are preserved (the AION-failure-mode fix from SEED-028). Reuse the resume/journal substrate; build NO new orchestration path. Then create tests/test-chain-graceful-partial.cjs covering the four behaviors with a fault-injecting stub onStep over a tmp room journal. NO fenced code in this action. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-chain-graceful-partial.cjs && echo PARTIAL_OK</automated>
  </verify>
  <done>A transient exhaustion or a non-transient failure returns a graceful partial: upstream trace preserved, a failure marker recorded, the position journaled so resume re-enters at the failed step without re-running upstream.</done>
</task>

<task type="auto">
  <name>Task 3: Register the Wave-3 suites in run-all-166.sh</name>
  <files>tests/run-all-166.sh</files>
  <action>Append the two Wave-3 suites (test-chain-retry-backoff.cjs, test-chain-graceful-partial.cjs) to the CJS_SUITES array in tests/run-all-166.sh after the Wave-2 entries. Extend the Part-8 grep sweep to cover lib/core/chain-retry.cjs (same regexes; grep -v '^#' filtering -- chain-retry.cjs is a pure timing/error helper with zero network surface). Extend the em-dash sweep (U+2014 codepoint escape) to cover chain-retry.cjs and the two new suites. Keep the runner running to completion; final tally + exit 1 on any FAIL. NO em-dashes.</action>
  <verify>
    <automated>bash tests/run-all-166.sh && echo PHASE_GATE_W3_GREEN</automated>
  </verify>
  <done>run-all-166.sh now runs the Wave-1 through Wave-3 suites + the extended Part 8 + em-dash sweeps to completion and exits 0 when green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| step dispatch -> transient fault | a transient remote hiccup must not kill the whole chain |
| retry exhaustion -> chain result | exhaustion must produce a marked, resumable partial, never a silent drop |
| graceful partial -> journal | the resume position must live in the single chain-state truth (pipeline-state.cjs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-09 | Denial of service | unbounded retry on a transient code | mitigate | withBackoff caps attempts at `retries` with an exponential-with-cap delay (bounded); the test asserts the bound |
| T-166-10 | Tampering | silent chain drop on synthesis failure | mitigate | exhaustion folds a failure marker + preserves the upstream trace + journals the position (graceful partial, the AION-failure-mode fix) |
| T-166-11 | Spoofing | a non-transient error retried as transient | mitigate | isTransient is a closed allow-list of the four 5xx codes; everything else fails fast (test asserts attempt count === 1) |
| T-166-12 | Repudiation | a lost resume position | mitigate | the partial journals via pipeline-state.recordStep / the isNext hard gate (the single chain-state truth, D-166-02); resume does not re-run upstream |
| T-166-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (pure node timers + error inspection); no install task |
</threat_model>

<verification>
- `node tests/test-chain-retry-backoff.cjs` passes (transient retries, non-transient fail-fast, bounded backoff).
- `node tests/test-chain-graceful-partial.cjs` passes (upstream preserved, failure marker, resume from journal).
- `bash tests/run-all-166.sh` is green (Waves 1-3 + Part 8 + em-dash sweeps).
- chain-retry.cjs and the graceful-partial branch make zero Brain calls and build no new orchestration path.
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
The executor is reliability-complete: a transient 5xx triggers bounded retry-with-backoff, exhaustion
returns a graceful partial (upstream preserved + failure marker + journaled resume position), a
non-transient error fails fast, and partial re-run reuses the single chain-state truth without
re-running upstream (EXEC-05, D-166-01). No new orchestration path was built.
</success_criteria>

<output>
Create `.planning/phases/166-gated-chain-executor/166-03-SUMMARY.md` when done.
</output>
