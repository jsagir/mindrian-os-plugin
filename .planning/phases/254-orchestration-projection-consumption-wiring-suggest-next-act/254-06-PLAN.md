---
phase: 254-orchestration-projection-consumption-wiring-suggest-next-act
plan: 06
type: execute
wave: 4
depends_on: ["254-02", "254-03", "254-04", "254-05"]
files_modified:
  - tests/test-254-r7-structural-fence.cjs
  - docs/254-NOTE-theo-adaptation-list-additions.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [WIRE-01, WIRE-02, WIRE-03, WIRE-04, COMP-01, COMP-02]
canon_parts: [Part 8, Part 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: []
  gate_impact: "none. This plan adds one structural test, one markdown message artifact, and requirement rows to a planning document. It adds, modifies and removes zero invocable surfaces, changes no executable module, mints no reach/edge/node, and opens no wire. All three CIRS gates are unaffected."
  explanation: "Filed for completeness per the 243-02 precedent. The R12 trigger is not met. Part 11 is in canon_parts because the R7 fence this plan builds is a direct structural enforcement of CIRS R7 (local-only at decide/rank, opens no wire); Part 8 because the Theo note and the fence both concern the LOCAL-to-BRAIN boundary."

must_haves:
  truths:
    - "No module reachable from decide()'s projection path gained a Brain require in this phase, proven structurally rather than promised (R7)."
    - "lib/core/navigation-engine.cjs was not touched by any plan in this phase, proven by scanning this phase's own PLAN frontmatter as well as the file itself."
    - "This phase's new blend seam is provably unreachable from decide(): nothing on the decide() path requires lib/workflow/chain-source.cjs."
    - "lib/mcp/brain-router.cjs is added to Theo's plugin adaptation list as a written message, with zero files created or edited under /home/jsagi/Theo (D-07)."
    - "BRAIN_PROBLEM_TYPE_ALIASES is enumerated as a named plugin-side flip item with its exact incumbent-to-Theo mapping, so the future re-point is a single-line diff and not a rediscovery."
    - "WIRE-01..04 and COMP-01..02 exist as rows in .planning/REQUIREMENTS.md with the phase-local-working-ID caveat the Phase 266/269/272/274 precedent carries (D-05)."
  artifacts:
    - path: "tests/test-254-r7-structural-fence.cjs"
      provides: "The R7 fence: navigation-engine purity, projection-reader purity, chain-source unreachability from decide(), and the planner-fence scan over this phase's own PLAN files"
      min_lines: 90
    - path: "docs/254-NOTE-theo-adaptation-list-additions.md"
      provides: "The D-07 message to Theo's session: brain-router.cjs added to the adaptation list, plus BRAIN_PROBLEM_TYPE_ALIASES as a named plugin-side flip item"
      min_lines: 60
    - path: ".planning/REQUIREMENTS.md"
      provides: "The six Phase 254 requirement rows plus the updated Traceability paragraph"
      contains: "WIRE-01"
  key_links:
    - from: "tests/test-254-r7-structural-fence.cjs"
      to: "lib/core/navigation-engine.cjs"
      via: "comment-stripped source scan for forbidden require tokens"
      pattern: "navigation-engine"
    - from: "docs/254-NOTE-theo-adaptation-list-additions.md"
      to: "docs/262-NOTE-theo-adaptation-list-additions.md"
      via: "same message shape, same delivery convention, same zero-code-change discipline"
      pattern: "adaptation list"
---

<objective>
Close the phase: prove the R7 fence held, send the Theo note D-07 requires, and register the six
requirement IDs D-05 minted.

The R7 fence is the promise 254-RESEARCH.md Section 4.3 asked the planner to write into the plans:
"no plan in this phase may add a file to `lib/core/navigation-engine.cjs`'s `files_modified`, and no
plan may add a `brain-client` require to any module reachable from `decide()`." This plan turns that
promise into a test that fails if it is ever broken, shaped like
`tests/test-reader-r4-structural-184.cjs` - a structural proof, not a behavioural one.

The Theo note is a MESSAGE, not a code change. `/home/jsagi/Theo` is read-only from this repo and
nothing under it may be created or edited, exactly as Phase 262 handled the same situation. The ask
is concrete: `lib/mcp/brain-router.cjs` reads `brainResult.next_gate.options[]`, an incumbent-only
shape, and is NOT on Theo's 7-file plugin adaptation list. Post-flip its Tier 3 will silently return
null and fall through to Tier 2 with no refusal disclosure, because `isAvailable()` is still true -
the same silent-shape-mismatch class Phase 262 found in `check-flagship-floor.cjs`.

The requirement registration follows the Phase 266 / 269 / 272 / 274 precedent exactly: IDs minted
at plan time inside the phase's own decision record, promoted to `.planning/REQUIREMENTS.md` at
phase close, carrying the honest caveat that they are phase-local working IDs and not part of an
earlier milestone's scope.

Purpose: a phase that fixed a consumption path without proving it left `decide()` alone would have
closed on a promise. This closes it on a test.
Output: one structural fence, one Theo message, six requirement rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/254-orchestration-projection-consumption-wiring-suggest-next-act/254-CONTEXT.md
@.planning/phases/254-orchestration-projection-consumption-wiring-suggest-next-act/254-RESEARCH.md
@.planning/phases/254-orchestration-projection-consumption-wiring-suggest-next-act/254-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the R7 structural fence</name>
  <files>tests/test-254-r7-structural-fence.cjs</files>
  <read_first>
    - `tests/test-reader-r4-structural-184.cjs` in full - the template. Copy its four-part structure (closed require allow-list, forbidden-token scan, engine-side negative check, export-shape check), its `codeOf(file)` comment-stripping helper, and its rationale comment explaining why a header that NAMES a forbidden token in prose must not self-trip the sweep.
    - `lib/core/navigation-engine.cjs` lines 1091-1150 - `decide()`'s projection block: `projectionReader.offerProjectionCapabilities(...)` at 1102-1116 and `candidateLift.liftFiringCandidate(...)` at 1129-1143, both wrapped in try/catch that degrades to null and never throws out of `decide()`.
    - `lib/core/reader/decide-projection-reader.cjs` and `lib/core/orchestration-candidate-lift.cjs` headers - both state LOCAL-only, zero Brain read/write, zero network. The fence pins those statements structurally.
    - `lib/workflow/local-chain-recommender.cjs` header lines 22-27 - "There is no brain-client require, no fetch, no http."
    - `254-RESEARCH.md` Section 4 in full - R7 verbatim, the four independent grounds that the planned work does not touch `decide()`, and the planner fence text at the end of 4.3.
    - `lib/workflow/chain-source.cjs` (Plan 01's output) and `scripts/act-command.cjs` lines 69-90 - act's lazy `navigation-engine` require, which exists ONLY to supply `decideFn` to `runChain`. The fence must not mistake that for a decide()-path Brain require; read the comment at `act-command.cjs:76-88` which states decide() reads LOCAL context only and opens no Brain egress.
  </read_first>
  <behavior>
    All arms operate on comment-stripped source. No module is executed for the purity arms.
    - Arm 1 (engine purity): `lib/core/navigation-engine.cjs` contains ZERO executable occurrences
      of `brain-client` and ZERO of `chain-source`. `decide()` gained neither a wire nor this
      phase's blend seam.
    - Arm 2 (projection-reader purity): `lib/core/reader/decide-projection-reader.cjs`,
      `lib/core/orchestration-candidate-lift.cjs` and `lib/workflow/local-chain-recommender.cjs`
      each contain ZERO executable occurrences of `brain-client`, `fetch(`, `require('node:http`,
      `require('node:https`, `require('http')`, `require('https')`. These are the three modules
      `decide()`'s projection path actually reaches, and each is individually verifiable.
    - Arm 3 (unreachability of this phase's seam): none of the three modules in Arm 2, and not
      `navigation-engine.cjs`, contains a require of `lib/workflow/chain-source.cjs`. The blend seam
      this phase built is structurally outside `decide()`'s reach, which is the specific new claim
      this phase has to make good on.
    - Arm 4 (planner fence honoured): scan every `254-*-PLAN.md` in this phase directory and assert
      that `lib/core/navigation-engine.cjs` appears in ZERO `files_modified` blocks. The fence
      RESEARCH asked for was a planning-time constraint; this arm makes it auditable after the fact.
    - Arm 5 (decideFn seam is not a wire): assert `scripts/act-command.cjs` still requires
      `navigation-engine.cjs` lazily and still passes `decide` as `runChain`'s `decideFn`. This arm
      exists so a future reader does not "fix" the fence by deleting a legitimate, R7-clean seam.
      Phase 172-08 put it there deliberately; feeding `decide()` as `decideFn` opens no Brain egress.
    - Arm 6 (chain-source is not a firer): `lib/workflow/chain-source.cjs` contains zero executable
      occurrences of `runChain`, `chain-executor`, `act-command`, `framework-runner`, `spawnSync`,
      `execSync`, `child_process`. It proposes; it never fires. Same doctrine as READER-04.
  </behavior>
  <action>
    Write `tests/test-254-r7-structural-fence.cjs`. Copy `tests/test-reader-r4-structural-184.cjs`'s
    `codeOf(file)` helper verbatim - stripping full-line comments is what stops this file's own
    header, which necessarily NAMES `brain-client` and `fetch(` in prose, from failing its own
    sweep. This is the grep-gate-hygiene rule and it is not optional here.

    Arm 4 reads `.planning/phases/254-.../254-*-PLAN.md`. Resolve the directory relative to
    `__dirname` rather than hardcoding an absolute path, and treat "no PLAN files found" as a
    FAILURE, not a pass - a scan that discovers nothing must fail, the same doctrine
    `tests/run-all-262.sh`'s `found -eq 0` guard encodes.

    Do NOT execute `navigation-engine.cjs` or `decide()`. This is a structural proof; loading the
    engine would pull a large require graph and a `node:sqlite` warning for no added assurance, and
    the whole point of the 184 template is that structure is a stronger guarantee than a behavioural
    sample.

    Assert by IDENTITY throughout: name the offending file and the offending token in every failure
    message. No count literals anywhere.

    Hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-254-r7-structural-fence.cjs && node tests/test-reader-r4-structural-184.cjs && bash tests/run-all-254.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-254-r7-structural-fence.cjs` exits 0 with all six arms passing.
    - `node tests/test-reader-r4-structural-184.cjs` still exits 0 - the pre-existing READER-04 structural guard is unregressed.
    - Arm 4 reports the number of `254-*-PLAN.md` files it actually scanned, and that number is 6.
    - Temporarily pointing Arm 4 at an empty directory makes it FAIL rather than pass; verify once during execution and record it in the SUMMARY, then restore.
    - The suite requires only `node:assert`, `node:fs`, `node:path`. It executes no module under test.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' tests/test-254-r7-structural-fence.cjs` returns nothing.
  </acceptance_criteria>
  <done>R7 is proven structurally for this phase: the engine is untouched, the three projection modules are wire-free, and the new blend seam is unreachable from decide().</done>
</task>

<task type="auto">
  <name>Task 2: Write the D-07 Theo adaptation-list note and file the room research trail</name>
  <files>docs/254-NOTE-theo-adaptation-list-additions.md</files>
  <read_first>
    - `docs/262-NOTE-theo-adaptation-list-additions.md` in full (93 lines) - the exact template: numbered sections (`The Ask, Up Front` / `The Break, Precisely` / why an already-shipped Theo fix does not cover it / what this phase did and deliberately did not / consequences the Theo side should price in / `Delivery`), the current 7-file list quoted so the recipient sees these are ADDITIONS to a known list, and the closing paragraph stating that nothing under `/home/jsagi/Theo/` was created or edited.
    - `254-RESEARCH.md` Section 5 in full - 5.1 (Theo explicitly decided never to sync the projection, so Wave 1 is a zero-diff surface at cutover), 5.2 (Theo's own `chain-run.ts` design constraint: Theo owns the schema and wire shape, the plugin owns the behaviour, and Theo classifies NO command's autonomy), 5.3 (the 7-file list and the two Theo-side facts this phase must not be surprised by).
    - `254-CONTEXT.md` D-07 in full.
    - `.planning/phases/254-.../254-05-SUMMARY.md` - the stated `BRAIN_PROBLEM_TYPE_ALIASES` decision and its recorded rationale, which this note must carry forward verbatim rather than re-derive.
    - `/home/jsagi/Theo/src/mcp/content/orchestration-readiness.ts` and `/home/jsagi/Theo/src/mcp/content/recommend-chain.ts` - READ ONLY. Confirm the two Theo-side facts are still true before asserting them in a message to Theo's own session, and note the read date in the file.
    - `CLAUDE.md` "Dev-Research Compositing (Rethinking Room)" - the two-homes rule this task's second half discharges.
  </read_first>
  <action>
    Write `docs/254-NOTE-theo-adaptation-list-additions.md` on the 262 template.

    Section 1, the ask up front: add `lib/mcp/brain-router.cjs` to Theo's named plugin adaptation
    list. Quote the current 7-file list so the recipient sees this is an addition to a known list:
    `scripts/probe-brain-contract.cjs`, `lib/brain/chain-recommender.cjs`,
    `lib/core/enrichment-queue.cjs`, `bin/mindrian-brain-mcp-client.cjs`,
    `lib/core/resolve-brain-key.cjs`, `data/brain-surface-contract.json`, and
    `BRAIN_TOOL_MATCHER` / `hooks/hooks.json`. Note that Phase 262 already asked for two more
    (`scripts/check-flagship-floor.cjs`, `scripts/build-brain-census.cjs`), so the list the
    recipient holds may already be 9.

    Section 2, the break precisely: `brain-router.cjs`'s Tier 3 calls `brainClient.ask(question)`
    and reads `brainResult.next_gate.options[]` plus `brainResult.directive.guided.framework` to
    build its ranked chain (`brain-router.cjs:297-314`). Theo's `brain_ask` returns structured rows,
    never composed prose, with two modes and three curated ops - `next_gate` is an INCUMBENT-ONLY
    shape. Post-flip the `options` array reads as `[]` and `anchorFramework` as null, so `rawChain`
    is empty and Tier 3 falls through to Tier 2 silently. The SWEEP-01 refusal disclosure at
    `brain-router.cjs:413-421` will NOT fire, because it is conditioned on `!isAvailable()` and
    availability is still true - the failure is a shape mismatch, not an outage. This is the exact
    class Phase 262 named "the single highest-risk line": a consumer has to NOTICE that answers
    which used to be graph-grounded quietly became heuristic, with no crash and no type error.

    Section 3, what this phase did and deliberately did not: Phase 254 ratified server-side
    composition (D-01), enumerated every `mindrian-os` handler that reaches the Brain in
    `lib/mcp/brain-composition-census.cjs` (COMP-01), and closed the `ambiguous`-verdict gap in the
    `callTool` belt with disclose-and-proceed (COMP-02 / D-02 Option A). It did NOT adapt
    `brain-router.cjs`'s reader to Theo's actual response shape - that is flip-day work, and Theo is
    not deployable yet (no remote hosting story, its own Phase 08.4 not started), so it belongs on
    the list with the other files rather than in this phase.

    Section 4, the second named item: `BRAIN_PROBLEM_TYPE_ALIASES` (`lib/core/brain-client.cjs:1541`).
    Give the exact mapping table: the map projects `undefined` / `udp` -> `Undefined Problem`,
    `ill-defined` / `ill_defined` / `idp` -> `Ill-Defined Problem`, and `well-defined` /
    `well_defined` / `wdp` -> `Well-Defined Problem`. None of those three is a live Theo
    `DomainConcept` id; Theo's are `UnDefined`, `IllDefined`, `WellDefined`, `Wicked`, `Trinity`,
    `Compass`. State Phase 254's decision and its reason verbatim from 254-05's SUMMARY: NOT
    re-pointed, because no single value satisfies both populations and the standing rule is to ship
    against the CURRENT Brain; PINNED by `tests/test-254-normalize-roundtrip-probe.cjs` instead, so
    the flip-day change is a single-line diff against a test that already names the target. Also
    note the sibling map that must NOT be conflated: `chain-recommender.cjs`'s `PROBLEM_TYPE_ALIASES`
    targets the LOCAL router codes UDP/IDP/WDP and is a different concern entirely; Phase 254 added
    a structural arm proving the two maps' value sets are disjoint.

    Section 5, consequences the Theo side should price in: (a) the orchestration projection stays
    plugin-side and Theo decided in its own `09-04-PLAN.md` never to sync it, so this phase's Wave 1
    consumption work is a ZERO-diff surface at cutover - nothing to adapt there, and that is worth
    the recipient knowing so they do not budget for it; (b) Theo's own `chain-run.ts` already
    ratifies the exact composition shape this phase ratified (Theo owns the schema and wire shape,
    the plugin owns the behaviour, Theo mints no second executor and classifies no command's
    autonomy), so D-01 and Theo's design agree rather than diverge; (c) a stale node count -
    Theo's `orchestration-readiness.ts` header says 380 nodes and the artifact measures 384 today,
    a 4-node drift since Theo's 2026-08-31 re-measurement, immaterial to any decision but worth not
    repeating.

    Section 6, delivery: address it to the parallel Theo-working session, per the 262 precedent, and
    close with the explicit statement that this is a message and not a code change, naming every
    Theo-side file that was read and asserting that nothing under `/home/jsagi/Theo/` was created or
    edited.

    SECOND HALF OF THIS TASK - the room research trail (CLAUDE.md "Dev-Research Compositing"). File
    the durable reasoning trail for this phase in `~/MindrianRooms/rethinking-mindrianos/research/`
    under a dated entry, cross-linked back to `254-RESEARCH.md` and this note, and mirrored to
    `mindrianOS/research/` as source-of-record per the convention. The content is the phase's
    reasoning, not its plans: the measured one-step-chain defect and its root cause, the
    blend-vs-replace finding that a straight replace empties the two most common seeds, the D-01
    reframing from "approve vs decline" to "ratify vs remove two shipped behaviours", and the
    `ambiguous`-gap finding. If the room-write leg is BLOCKED by a stale room binding - the same
    block Phase 262 hit - record the block honestly and specifically in this plan's SUMMARY (what
    was attempted, what the binding said, what would unblock it) and DO NOT hold the phase on it.
    That is the recorded precedent and it applies here.
  </action>
  <verify>
    <automated>test -f docs/254-NOTE-theo-adaptation-list-additions.md && git -C /home/jsagi/Theo status --short && echo "theo-status-printed-above-must-be-empty"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/254-NOTE-theo-adaptation-list-additions.md` exists and carries all six sections in the 262 template's order.
    - `git -C /home/jsagi/Theo status --short` returns EMPTY output. Zero files under `/home/jsagi/Theo` were created, edited or deleted. This is the hard gate on D-07's "never a code change to Theo".
    - The note names `lib/mcp/brain-router.cjs` as the addition, quotes the current 7-file list, and cites the exact reader (`brainResult.next_gate.options[]`) and the exact silent-fallthrough mechanism (`isAvailable()` still true, so the SWEEP-01 refusal disclosure does not fire).
    - The note carries the full `BRAIN_PROBLEM_TYPE_ALIASES` mapping table, the Theo-side `DomainConcept` ids, and Phase 254's stated non-re-point decision with its reason.
    - The note's closing paragraph enumerates every Theo-side file read and states the read date.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' docs/254-NOTE-theo-adaptation-list-additions.md` returns nothing.
    - The SUMMARY records the room research-trail outcome: either the dated entry path in `~/MindrianRooms/rethinking-mindrianos/research/` and its mirror, or an honest, specific record of the room-binding block.
  </acceptance_criteria>
  <done>Theo's session has a concrete, verified ask with two named items, and nothing under /home/jsagi/Theo changed.</done>
</task>

<task type="auto">
  <name>Task 3: Register WIRE-01..04 and COMP-01..02 in .planning/REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` - read the whole file structure. Specifically: the section-heading convention at line 595 (`### Phase 274 - <title> (minted <date>)`), the row convention (`- [x] **ANCHOR-01**: <behaviour sentence>`), and the closing `## Traceability` section (the "103 active requirements" enumeration, the per-family minting-date sentence, and the caveat paragraph about phase-local working IDs promoted at phase close).
    - `254-CONTEXT.md` D-05 - the six IDs with their exact behaviour statements. Those statements are the ratified text; use them, do not paraphrase them into something weaker.
    - `.planning/phases/254-.../254-01-SUMMARY.md` through `254-05-SUMMARY.md` - the actual shipped behaviour behind each ID, so a row can be marked `[x]` only where the SUMMARY shows it landed.
  </read_first>
  <action>
    Add one new section, `### Phase 254 - Orchestration projection consumption wiring (suggest-next,
    act, server-side composition) (minted 2026-09-02)`, placed after the LAST existing phase section
    and BEFORE the `## Traceability` heading. Match the surrounding sections' heading depth and row
    formatting exactly.

    Six rows, using D-05's ratified behaviour text:
    - `WIRE-01`: `/mos:suggest-next` produces a multi-step chain sourced from the projection when
      the projection has edges for the seed.
    - `WIRE-02`: When the projection has no edge for the seed, the surface degrades to the current
      registry-composed answer with a disclosed source, never to empty.
    - `WIRE-03`: `/mos:act --chain` composes from the same source as `suggest-next`; the two cannot
      disagree.
    - `WIRE-04`: The three framework vocabularies (`KNOWN_FRAMEWORKS`, `command-registry.json`, the
      projection) can no longer silently diverge - a drift gate fails the build.
    - `COMP-01`: Every `mindrian-os`-named tool handler that reaches the Brain is enumerated in one
      place and routes through the `callTool` belt.
    - `COMP-02`: The `callTool` belt's verdict handling matches the hook's, or the divergence is a
      stated, tested decision.

    Mark each row `[x]` only if the corresponding SUMMARY shows it shipped; leave `[ ]` and say why
    in a trailing clause otherwise. Do not mark a row done on the strength of this plan's intent.

    Under the six rows add a short paragraph recording, for each ID, the artifact and the test that
    proves it - the reader should be able to go from an ID to a runnable command without opening a
    plan. Also record the two STATED DECISIONS this phase made that a future reader would otherwise
    have to rediscover: (1) `BRAIN_PROBLEM_TYPE_ALIASES` is pinned, not re-pointed, with its reason
    and its flip-day target; (2) the MCP `suggest_next` / `orchestration act*` surfaces are NOT
    wired to the new chain source, because `brain-router.recommend()` returns a chain of COMMAND
    SLUGS validated against its own `KNOWN_METHODOLOGIES` list rather than a chain of framework
    NAMES, so wiring it needs its own vocabulary reconciliation and its own regression suite; the
    divergence is instrumented by `check-framework-vocabulary-drift.cjs`'s advisory tier and named
    as a follow-up.

    Then update `## Traceability`. RE-COUNT the active requirements from the document rather than
    trusting the current header number, state the recount in this plan's SUMMARY, and update the
    enumeration to include `WIRE-01..04` and `COMP-01..02` (Phase 254). Extend the minting-date
    sentence with "and WIRE-01..04 / COMP-01..02 (minted 2026-09-02)" and add Phase 254 to the list
    of phases whose IDs were minted at plan time inside the phase's own decision record - in this
    case `254-CONTEXT.md` D-05, ratifying `254-RESEARCH.md`'s proposed family - and registered here
    at phase close. Extend the existing phase-local-working-ID caveat paragraph to cover the WIRE and
    COMP families by name, using the same wording it already applies to MCPFIX / MEMOP / GUARD /
    PYPORT / ANCHOR: the behaviour each names is real and shipped, but the ID itself did not exist
    before its phase was planned and should not be read as part of an earlier milestone's scope.

    Do not renumber, reword or re-check any existing row. `.planning/` is gitignored (`.planning/*`),
    so if this file is to be committed it needs `git add -f`; check how the surrounding phases
    handled it before deciding, and record what you did.

    Hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>grep -c 'WIRE-0\|COMP-0' .planning/REQUIREMENTS.md && grep -n 'Phase 254' .planning/REQUIREMENTS.md && bash tests/run-all-254.sh</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains a `### Phase 254 - ...` section placed after the last existing phase section and before `## Traceability`, matching the surrounding heading depth and row formatting.
    - All six IDs `WIRE-01`, `WIRE-02`, `WIRE-03`, `WIRE-04`, `COMP-01`, `COMP-02` appear as rows with D-05's ratified behaviour text, each with a stated check state that matches its plan's SUMMARY.
    - The section names, per ID, the artifact and the runnable test command that proves it.
    - The section records both stated decisions: the `BRAIN_PROBLEM_TYPE_ALIASES` pin-not-re-point call with its reason and flip-day target, and the MCP-surface non-wiring call with its measured reason plus the advisory instrumentation that tracks it.
    - `## Traceability`'s active-requirement count is RE-COUNTED from the document, the enumeration includes `WIRE-01..04` and `COMP-01..02` (Phase 254), the minting sentence names 2026-09-02 and `254-CONTEXT.md` D-05, and the phase-local-working-ID caveat paragraph names the WIRE and COMP families.
    - `git diff .planning/REQUIREMENTS.md` shows only ADDITIONS plus the Traceability edits. Zero existing rows renumbered, reworded, or re-checked.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' .planning/REQUIREMENTS.md` returns nothing.
    - `bash tests/run-all-254.sh` exits 0.
  </acceptance_criteria>
  <done>The six IDs D-05 minted exist as rows with their proving tests, and the two stated decisions are on the record where a future reader will find them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo -> `/home/jsagi/Theo` | Cross-repo, READ-ONLY. The standing rule and D-07 both forbid any write. |
| `decide()`'s reachable module set -> the Brain | The R7 boundary. The fence proves nothing in this phase crossed it. |
| planning documents -> future readers | A requirement row or a stated decision that is wrong or missing becomes a rediscovery cost, which is the exact failure this phase is correcting in others. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-254-29 | Tampering | An accidental write into `/home/jsagi/Theo` | mitigate | `git -C /home/jsagi/Theo status --short` returning EMPTY is a hard acceptance criterion on Task 2, exactly as it was for Phase 262's equivalent note. The task's action names every Theo-side file as read-only and requires the closing paragraph to enumerate them with a read date. |
| T-254-30 | Information Disclosure | A LOCAL-to-BRAIN require sneaking onto the `decide()` path | mitigate | Arms 1-3 of the fence scan `navigation-engine.cjs` and the three modules `decide()`'s projection path reaches for `brain-client`, `fetch(` and both http require forms, on comment-stripped source. Arm 3 additionally proves this phase's own blend seam is not required by any of them. This is the structural discharge of the fence 254-RESEARCH.md Section 4.3 asked the planner to write into the plans. |
| T-254-31 | Elevation of Privilege | The blend seam growing a firing path | mitigate | Arm 6 scans `chain-source.cjs` for the READER-04 firing-token set (`runChain`, `chain-executor`, `act-command`, `framework-runner`, `spawnSync`, `execSync`, `child_process`). It proposes; it never fires. |
| T-254-32 | Repudiation | A vacuous fence that passes because it scanned nothing | mitigate | Arm 4 treats "no PLAN files discovered" as a FAILURE, and its acceptance criterion requires proving that by pointing it at an empty directory once during execution. Same doctrine as `tests/run-all-262.sh`'s `found -eq 0` guard: a harness that discovers nothing must fail, not print green. |
| T-254-33 | Repudiation | A phase closing on a promise rather than a proof | mitigate | Every requirement row must name a runnable command that proves it, and rows may only be checked against what a SUMMARY shows shipped. The two stated decisions are recorded in `REQUIREMENTS.md` and in the Theo note, not only in a SUMMARY that nobody re-reads. |

No package-manager installs occur in this plan, so no `T-254-SC` supply-chain row applies.
</threat_model>

## Artifacts this phase produces (Plan 06)

| Artifact | Kind | Symbols |
|---|---|---|
| `tests/test-254-r7-structural-fence.cjs` | NEW test | R7 fence arms 1-6, including the planner-fence scan over this phase's own PLAN frontmatter |
| `docs/254-NOTE-theo-adaptation-list-additions.md` | NEW message artifact | The D-07 ask: `lib/mcp/brain-router.cjs` added to Theo's adaptation list, plus `BRAIN_PROBLEM_TYPE_ALIASES` as a named plugin-side flip item with its exact mapping |
| `~/MindrianRooms/rethinking-mindrianos/research/<dated>/` plus the `mindrianOS/research/` mirror | NEW room entry | The Dev-Research Compositing trail, or an honest recorded block |
| `.planning/REQUIREMENTS.md` | MODIFIED | `### Phase 254` section with rows `WIRE-01`, `WIRE-02`, `WIRE-03`, `WIRE-04`, `COMP-01`, `COMP-02`; updated `## Traceability` count, enumeration, minting sentence and caveat paragraph |

Explicitly NOT modified: anything under `/home/jsagi/Theo`, anything under
`/home/jsagi/dev/ProblemsWorthSolving-Brain`, `lib/core/navigation-engine.cjs`, and any executable
module in this repo.

## Full phase artifact index (all six plans)

| File | Plan | Kind |
|---|---|---|
| `lib/workflow/chain-source.cjs` | 01 | NEW module - `resolveChainSource`, `describeSource`, `MAX_HOPS` |
| `tests/run-all-254.sh` | 01 | NEW aggregator |
| `tests/test-254-projection-chain-source.cjs` | 01 | NEW test (WIRE-01) |
| `tests/test-254-degrade-floor.cjs` | 01 | NEW test (WIRE-02) |
| `scripts/suggest-next-command.cjs` | 02 | MODIFIED - chain source swapped, disclosure line added |
| `scripts/act-command.cjs` | 02 | MODIFIED - same two edits |
| `tests/test-254-one-chain-source.cjs` | 02 | NEW test (WIRE-03) |
| `scripts/check-framework-vocabulary-drift.cjs` | 03 | NEW gate - `classifyVocabularies`, `validateDeclarations`, `DECLARED_NON_PWS` |
| `tests/test-254-vocabulary-drift.cjs` | 03 | NEW test (WIRE-04) |
| `scripts/hooks/pre-commit` | 03 | MODIFIED - one gate block |
| `scripts/release.sh` | 03 | MODIFIED - one gate block |
| `scripts/doctor.cjs` | 03 | MODIFIED - one coverage-gate entry plus label |
| `lib/mcp/brain-composition-census.cjs` | 04 | NEW declaration - `COMPOSITION_SITES`, `validateSites` |
| `tests/test-254-composition-census.cjs` | 04 | NEW test (COMP-01) |
| `lib/mcp/brain-router.cjs` | 04 | MODIFIED - comments only |
| `lib/mcp/tools/sensors.cjs` | 04 | MODIFIED - comments only |
| `lib/mcp/tool-router.cjs` | 04 | MODIFIED - comments only |
| `lib/core/brain-client.cjs` | 05 | MODIFIED - `_attachEgressDisclosure`, ambiguous branch, `egress_disclosure` response field |
| `tests/test-254-ambiguous-disclosure.cjs` | 05 | NEW test (COMP-02) |
| `tests/test-254-normalize-roundtrip-probe.cjs` | 05 | NEW test (D-06 hermetic leg) |
| `tests/test-254-live-normalize-probe.sh` | 05 | NEW probe (D-06 live leg, SKIP-capable) |
| `tests/test-254-r7-structural-fence.cjs` | 06 | NEW test (R7 fence) |
| `docs/254-NOTE-theo-adaptation-list-additions.md` | 06 | NEW message artifact (D-07) |
| `.planning/REQUIREMENTS.md` | 06 | MODIFIED - six requirement rows plus Traceability |

<verification>
1. `node tests/test-254-r7-structural-fence.cjs` exits 0.
2. `node tests/test-reader-r4-structural-184.cjs` exits 0.
3. `git -C /home/jsagi/Theo status --short` returns empty.
4. `bash tests/run-all-254.sh` exits 0 - the whole phase suite, aggregated.
5. `node scripts/doctor.cjs --acceptance` exits 0.
6. `scripts/verify-release` exits 0.
7. `node scripts/build-orchestration-projection.cjs --check`, `node scripts/build-connector-registry.cjs --check`, `node scripts/check-render-coverage.cjs`, `node scripts/check-framework-vocabulary-drift.cjs --check` all exit 0.
8. `node tests/test-252-guard-census.cjs` and `node tests/test-239-query-egress-canary.cjs` exit 0.
9. The three pre-existing surface regression suites are green: `node lib/memory/suggest-next-workflow.test.cjs`, `node tests/test-act-on-runchain.cjs`, `node tests/test-act-cross-class-chain.cjs`.
</verification>

<success_criteria>
- R7 is proven, not promised: the engine and the three projection modules are wire-free, this phase's blend seam is unreachable from `decide()`, and the planner fence is auditable from this phase's own PLAN files.
- Theo's session has a concrete two-item ask, and `/home/jsagi/Theo` is byte-unchanged.
- All six requirement IDs exist as rows with the artifact and the runnable command that proves each.
- The two stated decisions (`BRAIN_PROBLEM_TYPE_ALIASES` pinned not re-pointed; the MCP surface not wired, with its measured reason and its instrumentation) are on the record in a document a future reader will actually open.
- The room research trail is filed, or its block is recorded honestly and specifically without holding the phase.
</success_criteria>

<output>
Create `.planning/phases/254-orchestration-projection-consumption-wiring-suggest-next-act/254-06-SUMMARY.md` when done.
</output>
