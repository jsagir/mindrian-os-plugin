---
phase: 254-orchestration-projection-consumption-wiring-suggest-next-act
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/run-all-254.sh
  - tests/test-254-projection-chain-source.cjs
  - tests/test-254-degrade-floor.cjs
  - lib/workflow/chain-source.cjs
autonomous: true
requirements: [WIRE-01, WIRE-02]
canon_parts: [Part 7, Part 8, Part 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: []
  gate_impact: "none. This plan adds ONE local library module (lib/workflow/chain-source.cjs) plus test files. It adds, modifies and removes zero invocable surfaces, mints no reach/edge/node, and opens no Brain wire (the module reads only committed local JSON via lib/workflow/local-chain-recommender.cjs). All three CIRS gates (build-connector-registry.cjs --check, build-orchestration-projection.cjs --check, check-render-coverage.cjs) are unaffected."
  explanation: "Filed for completeness per the 177-05 / 243-02 precedent. The R12 trigger (files under commands/|skills/|agents/, or a non-empty spine_consumed) is NOT met. Part 7 is in canon_parts because the whole point of this module is reuse-before-build: it mints no recommender, no ranker, no resolver and no posture authority, it only SELECTS between two shipped sources and reports which one won. Part 8 because the selection reads a LOCAL derived cache and opens no wire. Part 11 because R6 ranking-deferral binds: the module takes local-chain-recommender's own ordering and never re-ranks."

must_haves:
  truths:
    - "A single shared function decides the chain source for BOTH /mos:suggest-next and /mos:act --chain, so the two surfaces structurally cannot disagree (the precondition WIRE-03 is proven against in Plan 02)."
    - "When the orchestration projection has a chain edge for the governed seed, the returned chain is the projection's multi-hop path, not a one-element degenerate (WIRE-01)."
    - "When the projection has no chain edge for the seed, the returned chain is today's registry-composed answer, never empty (WIRE-02, D-03 blend-never-replace)."
    - "Every return states WHICH source produced it, so the render can disclose it and no surface degrades silently (Decision 8, D-03 step 4)."
    - "The module opens no Brain wire of its own and mints no second ranker: the surfaced ordering is local-chain-recommender's own ordering, taken as given (Canon Part 11 R6 ranking-deferral)."
    - "This plan is shippable on its own merits with zero dependency on the composition ruling, which is D-04's explicit structural requirement: Wave 1 must be able to ship even if a future session ever revisits D-01."
  artifacts:
    - path: "lib/workflow/chain-source.cjs"
      provides: "resolveChainSource(opts) projection-first blend seam with a disclosed registry floor, plus describeSource(result) - the ONE disclosure string both surfaces print"
      exports: ["resolveChainSource", "describeSource", "MAX_HOPS"]
      min_lines: 90
    - path: "tests/run-all-254.sh"
      provides: "Phase 254 glob aggregator: TEST_254_PREFIX override, found-eq-0 guard, no-em-dash fence over every file this phase touches"
      contains: "found -eq 0"
      min_lines: 100
    - path: "tests/test-254-projection-chain-source.cjs"
      provides: "WIRE-01 coverage: projection-sourced multi-hop chain, R6 no-second-ranker proof, module purity scan"
      min_lines: 70
    - path: "tests/test-254-degrade-floor.cjs"
      provides: "WIRE-02 coverage: all four problem-type paths, the two most common seeds proven non-empty, fixture-injected empty projection"
      min_lines: 70
  key_links:
    - from: "lib/workflow/chain-source.cjs"
      to: "lib/workflow/local-chain-recommender.cjs"
      via: "require + recommendMultiHopChains"
      pattern: "recommendMultiHopChains"
    - from: "lib/workflow/chain-source.cjs"
      to: "lib/brain/chain-recommender.cjs"
      via: "require + recommendFrameworkChain (the governed seed producer AND the registry floor)"
      pattern: "recommendFrameworkChain"
    - from: "tests/run-all-254.sh"
      to: "tests/test-254-projection-chain-source.cjs"
      via: "glob discovery under TEST_254_PREFIX"
      pattern: "TEST_254_PREFIX"
---

<objective>
Build the ONE shared chain-source seam this phase's wiring hangs off, and the RED tests that pin
it, per D-03 (blend, never replace - locked from research, reproduced live, not a preference).

`lib/workflow/chain-source.cjs` exports `resolveChainSource(opts)`: it asks the shipped governed
seed producer for the seed and the floor answer in one call, tries the shipped multi-hop
projection recommender from that seed, and returns whichever won WITH its provenance. It mints
nothing. It is not a second recommender, not a second ranker, not a second resolver and not a
second posture authority - every one of those has a shipped owner and a test that fails the build
if a rival appears (254-RESEARCH.md "Don't Hand-Roll", key insight: this phase's failure mode is
building a second thing that already exists).

This plan does NOT touch `scripts/suggest-next-command.cjs` or `scripts/act-command.cjs`. That is
Plan 02's job, and keeping the seam separate from its two consumers is what makes WIRE-03 ("the
two cannot disagree") provable rather than asserted.

**Wave placement, per D-04.** This plan and Plan 03 are Wave 1: entirely local, zero Brain calls,
R7-clean, Part-8-clean, and Theo-zero-diff (D-07 - Theo decided in its own `09-04-PLAN.md` never to
sync the projection, so this surface is a zero diff at cutover). D-04 requires the phase to be
structured so Wave 1 can ship even if a future session ever revisits D-01's composition ruling.
That is why this plan's `depends_on` is empty and why nothing in it requires, references or is
blocked by the composition work in Plans 04 and 05. D-06 also applies here in the negative: Wave 1
needs NO fresh measurement, because Phase 262's D-07 finding (0 `<SEP>` occurrences, 28 framework
nodes, dated 2026-09-02) covers the local artifact this plan reads and was independently
re-confirmed during research. Do not re-measure it.

Purpose: the measured defect (254-RESEARCH.md Section 2.3 - EVERY problem type collapses to a
one-element chain because `framework-chain-composer.cjs::KNOWN_FRAMEWORKS` shares 1 of 18 names
with the 28 real PWS frameworks) is a live, user-visible defect on a surface whose own body text
promises a step-numbered sequence. Cite that table by reference; do not re-derive it.
Output: one library module, one phase aggregator, two RED-then-GREEN test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/254-orchestration-projection-consumption-wiring-suggest-next-act/254-CONTEXT.md
@.planning/phases/254-orchestration-projection-consumption-wiring-suggest-next-act/254-RESEARCH.md
@.planning/phases/254-orchestration-projection-consumption-wiring-suggest-next-act/254-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the phase aggregator and the WIRE-01 / WIRE-02 RED test files</name>
  <files>tests/run-all-254.sh, tests/test-254-projection-chain-source.cjs, tests/test-254-degrade-floor.cjs</files>
  <read_first>
    - `tests/run-all-262.sh` in full (138 lines) - the exact aggregator template to copy: `set -uo pipefail`, `cd` to repo root, the `TEST_262_PREFIX` override variable, `run()` / `run_may_skip()` helpers, glob discovery over both `.cjs` and `.sh`, the self-exclusion guard that stops the runner re-running itself, the load-bearing `found -eq 0` guard, and the `grep -P '\x{2014}'` no-em-dash fence with its "skipped, not yet created" branch for missing paths.
    - `tests/test-chain-transform-composition.cjs` - the EXISTING suite that already exercises `recommendMultiHopChains`. Read it to extend the convention, not duplicate it (254-RESEARCH.md Section 7.3 names it explicitly as the model for projection-reader behaviour).
    - `lib/workflow/local-chain-recommender.cjs` in full (330 lines) - the module being wrapped. Note in particular: `recommendMultiHopChains(args)` accepts `args.from` (REQUIRED), `args.maxHops` (default 3), `args.projection` and `args.curatedChains` (the IN-MEMORY fixture seam - this is the seam the empty-projection arm uses; do NOT add an env override, the in-memory arg already exists), `args.k` (clamped at MAX_K). Its return element shape is `{ from, path, hops, confidence, transforms, kinds }` and its ordering is hops ASC then composed-confidence DESC.
    - `lib/brain/chain-recommender.cjs` lines 82-95 (`DEFAULT_SEED = 'Beautiful Question Framework'`, `MAX_CHAIN_LENGTH = 4`, `PROBLEM_TYPE_ALIASES`), lines 204-222 (`_pickSeed`, the governed seed ladder), lines 380-433 (`recommendFrameworkChain`, including the Phase 252-01 SWEEP-01 stderr disclosure at 425-430), and the export block at 771-784.
    - `tests/test-reader-r4-structural-184.cjs` lines 24-45 - the `codeOf(file)` comment-stripping helper. Reuse this exact shape for any source scan so a header that NAMES a forbidden token in prose cannot self-trip the sweep (the grep-gate-hygiene rule).
  </read_first>
  <behavior>
    `tests/test-254-projection-chain-source.cjs` (WIRE-01), arms:
    - Arm 1: `resolveChainSource({ currentFramework: 'S-Curve Analysis' })` returns
      `source === 'projection'`, `frameworks[0] === 'S-Curve Analysis'`,
      `frameworks[1] === 'Adoption-Capacity Theory'`, `frameworks.length >= 2`, `hops === 1`,
      and `Math.abs(confidence - 0.82) < 1e-9`. These are the LIVE-VERIFIED anchors from
      254-RESEARCH.md Section 1.3, re-confirmed against the committed artifact 2026-09-02.
    - Arm 2: `resolveChainSource({ problemType: 'undefined' })` returns `seed === 'Domain Selection'`,
      `source === 'projection'`, `frameworks[1] === 'Scenario Planning'`, `confidence === 0.68`.
      This proves the blend is per-seed, not a global switch.
    - Arm 3 (R6 no-second-ranker proof): for seed `'S-Curve Analysis'`, the returned `frameworks`
      array is element-for-element identical to
      `recommendMultiHopChains({ from: 'S-Curve Analysis', maxHops: 3 })[0].path`. The seam takes
      the shipped ordering as given and never re-sorts.
    - Arm 4: `describeSource(result)` for a projection result starts with the literal
      `'Chain source: projection'` and contains the composed confidence rendered as
      `String(Number(confidence.toFixed(4)))` (so 0.82 renders `0.82`, not `0.8200`).
    - Arm 5 (module purity): the comment-stripped source of `lib/workflow/chain-source.cjs`
      contains zero occurrences of `brain-client`, `fetch(`, `node:http`, `node:https`,
      `require('http`, `child_process`. The module opens no wire of its own.
    - Arm 6 (no hardcoded census): the comment-stripped source of `lib/workflow/chain-source.cjs`
      contains no numeric-count comparison matching `/\.length\s*[=!<>]==?\s*\d+/` beyond a
      zero-comparison, so Pitfall 8 (a frozen 207 / 249 / 380 / 384 node count) cannot creep in.

    `tests/test-254-degrade-floor.cjs` (WIRE-02), arms:
    - Arm 1: `resolveChainSource({ problemType: 'ill-defined' })` returns
      `seed === 'Beautiful Question Framework'`, `source === 'registry-floor'`,
      `frameworks.length >= 1`, `frameworks[0] === 'Beautiful Question Framework'`.
    - Arm 2: `resolveChainSource({})` (no problem type at all - the DEFAULT_SEED path) returns the
      same seed and `source === 'registry-floor'`. Arms 1 and 2 are the two MOST COMMON real
      invocations and a straight replace was proven live to turn both into empty; this pair is
      the regression that pins D-03.
    - Arm 3: `resolveChainSource({ problemType: 'well-defined' })` returns
      `seed === 'PWS Triple Validation Compass'`, `source === 'registry-floor'` (it has no
      outbound chain edge in the projection).
    - Arm 4 (NEVER empty): for all four inputs `{problemType:'ill-defined'}`,
      `{problemType:'undefined'}`, `{problemType:'well-defined'}`, `{}` the returned
      `frameworks` is an array of length >= 1 whose every element is a non-empty string, and
      `source` is one of the two literals `'projection'` / `'registry-floor'` (never null,
      never undefined, never a third value).
    - Arm 5 (fixture-injected empty projection): pass
      `{ currentFramework: 'S-Curve Analysis', projection: { nodes: [], edges: [] }, curatedChains: [] }`
      and assert the result flips to `source === 'registry-floor'` with a non-empty
      `frameworks`. This proves the floor fires on an EMPTY projection, not only on a seed with
      no edge.
    - Arm 6: `describeSource(result)` for a floor result starts with the literal
      `'Chain source: registry floor'` and names the seed inside double quotes.
  </behavior>
  <action>
    Write all three files now. `lib/workflow/chain-source.cjs` does not exist yet, so both `.cjs`
    suites are expected to FAIL at this point (`require` throws MODULE_NOT_FOUND). That is the RED
    baseline, matching this session's own RED-first convention (Phases 272 / 273 / 274) and
    `tests/run-all-271.sh`'s documented RED period. Guard the require so the failure is a legible
    reported arm rather than an uncaught stack: wrap it in try/catch, record a failing arm named
    `module loads`, and continue so the remaining arms report as failures too rather than the
    process dying on line 1.

    Aggregator specifics for `tests/run-all-254.sh`: copy `tests/run-all-262.sh` structurally.
    Set `PREFIX="${TEST_254_PREFIX:-tests/test-254-}"`. Keep the `run_may_skip` helper (Plan 05
    ships a `.sh` probe that prints `SKIP` when the Brain is keyless or unreachable). Populate
    `EMDASH_TARGETS` with EVERY file this phase touches across all six plans, even the ones that
    do not exist yet - the template's `(skipped, not yet created)` branch handles absence, and
    front-loading the list means no later plan has to edit this file (which keeps
    `tests/run-all-254.sh` owned by exactly one plan and keeps the waves conflict-free):
    `lib/workflow/chain-source.cjs`, `scripts/suggest-next-command.cjs`, `scripts/act-command.cjs`,
    `scripts/check-framework-vocabulary-drift.cjs`, `lib/mcp/brain-composition-census.cjs`,
    `lib/core/brain-client.cjs`, `lib/mcp/brain-router.cjs`, `lib/mcp/tools/sensors.cjs`,
    `docs/254-NOTE-theo-adaptation-list-additions.md`, `tests/run-all-254.sh`, and every
    `tests/test-254-*` file named across this phase's six plans
    (`projection-chain-source`, `degrade-floor`, `one-chain-source`, `vocabulary-drift`,
    `composition-census`, `ambiguous-disclosure`, `normalize-roundtrip-probe`,
    `r7-structural-fence`, plus `tests/test-254-live-normalize-probe.sh`).

    Header comment for the aggregator, in the 262 style, naming what this phase has to prove in
    one sentence each: (a) the chain a user sees is the projection's when the projection has an
    edge, (b) it is the registry floor when it does not, and never empty, (c) suggest-next and act
    cannot disagree, (d) the three framework vocabularies cannot silently diverge, (e) no module
    reachable from `decide()` gained a Brain require, (f) every `mindrian-os` handler reaching the
    Brain is enumerated and belted, (g) an `ambiguous` Part 8 verdict on the server-side path is
    disclosed, not silent.

    Hyphens only, no em-dashes, in all three files.
  </action>
  <verify>
    <automated>node tests/test-254-projection-chain-source.cjs; echo "exit=$? (RED expected before Task 2)"; bash tests/run-all-254.sh; echo "aggregator exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-254.sh` runs to completion and reports a non-zero PASS/FAIL summary line; it does NOT die on a missing file.
    - `TEST_254_PREFIX=tests/test-254-nonexistent- bash tests/run-all-254.sh` exits NON-ZERO and prints the `no tests/test-254-nonexistent-* files discovered` line. The found-eq-0 guard is load-bearing and must not be softened.
    - `grep -c 'found -eq 0' tests/run-all-254.sh` returns at least 1.
    - Both `.cjs` suites currently FAIL with a reported `module loads` arm (not an uncaught MODULE_NOT_FOUND stack trace), proving RED honestly.
    - The two `.cjs` files together contain at least 12 distinct assertion arms covering every bullet in `<behavior>`.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' tests/run-all-254.sh tests/test-254-projection-chain-source.cjs tests/test-254-degrade-floor.cjs` returns nothing.
  </acceptance_criteria>
  <done>The aggregator exists, self-proves its found-eq-0 guard, and both WIRE test suites fail for exactly one reason: the module Task 2 creates does not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lib/workflow/chain-source.cjs (the projection-first blend seam)</name>
  <files>lib/workflow/chain-source.cjs</files>
  <read_first>
    - `254-CONTEXT.md` D-03 in full - the four numbered steps and the sentence "This is not negotiable scope; it is a correctness requirement." Also D-04, which is why this module ships in Wave 1 with no dependency on the composition ruling.
    - `254-RESEARCH.md` Section 2.4 (the blend-vs-replace measurement table) and the "Don't Hand-Roll" table (all 8 rows).
    - `lib/workflow/local-chain-recommender.cjs` in full - already read in Task 1, re-read the `recommendMultiHopChains` docblock at lines 216-242 for the exact return shape and the R6 ranking-deferral contract.
    - `lib/brain/chain-recommender.cjs` lines 380-433 - `recommendFrameworkChain`'s full body, especially the Phase 252-01 SWEEP-01 stderr disclosure at lines 425-430 and the comment explaining why the plain `string[]` return shape is NOT reshaped (14+ callers key on it).
    - `lib/workflow/command-resolver.cjs` header - confirm `composeWorkflow` is the SOLE framework-to-command door (R4). This module must NOT call it; resolution stays at the two command surfaces.
  </read_first>
  <behavior>
    - `resolveChainSource({ currentFramework: 'S-Curve Analysis' })` -> `{ seed: 'S-Curve Analysis', frameworks: ['S-Curve Analysis','Adoption-Capacity Theory'], source: 'projection', confidence: 0.82, hops: 1, transforms: ['maturity-to-adoption'], kinds: ['feeds_into'] }`.
    - `resolveChainSource({ problemType: 'ill-defined' })` -> `{ seed: 'Beautiful Question Framework', frameworks: ['Beautiful Question Framework'], source: 'registry-floor', confidence: null, hops: null, transforms: null, kinds: null }`.
    - `describeSource` on the first -> `Chain source: projection (1-hop path, composed confidence 0.82)`.
    - `describeSource` on the second -> `Chain source: registry floor (the projection carries no chain edge for "Beautiful Question Framework")`.
  </behavior>
  <action>
    Create `lib/workflow/chain-source.cjs`. Require exactly two modules plus `node:path`:
    `lib/brain/chain-recommender.cjs` and `lib/workflow/local-chain-recommender.cjs`. Require
    NOTHING else - no `brain-client`, no `command-resolver`, no `recipe-maps`, no `fetch`, no
    `node:http`.

    `resolveChainSource(opts)` algorithm, in this exact order:
    1. `const floor = chainRecommender.recommendFrameworkChain(opts)`. This single call does two
       jobs: it yields the GOVERNED seed as `floor[0]` (never re-derive the seed - `_pickSeed` is
       private and `problem-type-router` is its one owner, R4) and it yields the registry floor
       answer for step 3. It always returns a non-empty `string[]` and never throws.
    2. `const seed = floor[0]`. Then
       `const candidates = localChainRecommender.recommendMultiHopChains({ from: seed, maxHops: MAX_HOPS, projection: opts.projection, curatedChains: opts.curatedChains })`,
       where `MAX_HOPS = 3` (matching `chain-recommender.MAX_CHAIN_LENGTH - 1`; derive it from the
       imported constant rather than writing a bare `3`, so the two cannot drift). Pass
       `projection` / `curatedChains` through ONLY when the caller supplied them (they are the
       in-memory fixture seam the tests use; production callers pass neither and the recommender
       reads the committed files).
    3. If `candidates.length > 0`: take `candidates[0]` as-is. Do NOT re-sort, do NOT re-rank, do
       NOT filter by a confidence floor - the shipped module already orders hops ASC then
       composed-confidence DESC and R6 says the final surfaced ordering is the Part-3 ranker's
       job, not this seam's. Return `source: 'projection'` with `frameworks: top.path`,
       `confidence: top.confidence`, `hops: top.hops`, `transforms: top.transforms`,
       `kinds: top.kinds`.
    4. Else return `source: 'registry-floor'` with `frameworks: floor` and the four projection-only
       fields set to `null`.
    5. Wrap the whole body so it never throws: on any internal error, degrade to
       `{ seed, frameworks: floor.length ? floor : [seed], source: 'registry-floor', ... }`. Both
       consumers are user-facing CLI helpers that exit 0 on error today; this seam must not become
       the first thing that throws at them.

    `describeSource(result)` returns exactly one of two strings, no trailing newline:
    - projection: `'Chain source: projection (' + hops + '-hop path, composed confidence ' + String(Number(result.confidence.toFixed(4))) + ')'`
    - floor: `'Chain source: registry floor (the projection carries no chain edge for "' + seed + '")'`
    Both surfaces print the SAME function's output, which is what makes WIRE-03's "cannot disagree"
    check in Plan 02 a real structural proof rather than two hand-matched strings.

    Export `resolveChainSource`, `describeSource`, and `MAX_HOPS`.

    Three things the header docblock MUST state, because they are the ways a future reader could
    misread this file:
    (a) Part 7 / R4 / R6 fence: this module mints NO recommender, NO ranker, NO framework-to-command
        resolver and NO posture authority. `composeWorkflow` (R4, the one door) and
        `recipe-maps.postureForCommand` (the ONE posture authority - Phase 237-02 measured 48/112
        disagreements and 12 material auto-runs when a second authority existed) are untouched by
        this phase and are NOT called from here.
    (b) The known stderr interaction: `recommendFrameworkChain` may emit its Phase 252-01 SWEEP-01
        stderr line ("seed-only chain served, not graph-grounded") when the Brain is unavailable
        AND its own result is seed-only. When step 3 then UPGRADES to a projection chain, that
        stderr line is stale. Do NOT suppress it and do NOT modify `recommendFrameworkChain` - 14+
        callers key on its current contract and Phase 252-01 deliberately chose stderr over a
        return-shape change. Instead, `describeSource`'s stdout line is AUTHORITATIVE about which
        source actually won, and Plan 02 wires it into both renders. Record this in the docblock so
        nobody later "fixes" it by reaching into the shared recommender.
    (c) R7 / D-04: this module is NOT on `decide()`'s path and must never be required from
        `navigation-engine.cjs` or from either projection reader. Plan 06's structural fence asserts
        exactly that, and Wave 1's independence from the composition ruling depends on this module
        staying local-only.

    Canon Part 8: the projection and `curated_chains` carry only generic machinery metadata
    (framework-name pairs and a scalar confidence). No user content is read or emitted here, and
    reading a committed local JSON file opens no wire. Hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-254-projection-chain-source.cjs && node tests/test-254-degrade-floor.cjs && node tests/test-chain-transform-composition.cjs && bash tests/run-all-254.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-254-projection-chain-source.cjs` exits 0 with every arm passing.
    - `node tests/test-254-degrade-floor.cjs` exits 0 with every arm passing.
    - `node -e "console.log(JSON.stringify(require('./lib/workflow/chain-source.cjs').resolveChainSource({currentFramework:'S-Curve Analysis'})))"` prints `"source":"projection"` and the path `["S-Curve Analysis","Adoption-Capacity Theory"]` with `"confidence":0.82`.
    - `node -e "const m=require('./lib/workflow/chain-source.cjs');console.log(m.describeSource(m.resolveChainSource({problemType:'ill-defined'})))"` prints exactly `Chain source: registry floor (the projection carries no chain edge for "Beautiful Question Framework")`.
    - `node tests/test-chain-transform-composition.cjs` still exits 0 (the pre-existing suite over `recommendMultiHopChains` is not regressed).
    - The comment-stripped source of `lib/workflow/chain-source.cjs` contains zero occurrences of `brain-client`, `fetch(`, `node:http`, `child_process`, `composeWorkflow`, `postureForCommand`.
    - `git diff --name-only` shows `lib/brain/chain-recommender.cjs` and `lib/workflow/local-chain-recommender.cjs` UNCHANGED. This plan wraps them; it does not edit them.
  </acceptance_criteria>
  <done>`resolveChainSource` returns the projection path for a seed with edges and the registry floor for a seed without, always non-empty, always with a stated source, and the two shipped modules it wraps are byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed local JSON (`data/brain-orchestration-projection.json`, `data/command-registry.json`) -> `chain-source.cjs` | A LOCAL derived-cache read. No wire is opened; the boundary is filesystem-to-process, not process-to-Brain. |
| `chain-source.cjs` -> `chain-recommender.cjs` -> `brain-client.cjs` | The seam calls a module that CAN reach the Brain. Nothing this plan adds passes a payload through that path; the seed is a generic framework-name handle only. |
| room `STATE.md` problem-type string -> the seed ladder | Caller-supplied, author-controlled text reaches `_pickSeed` via `opts.problemType`. It is normalized against a closed alias map before it is used. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-254-01 | Information Disclosure | `lib/workflow/chain-source.cjs` reaching the Brain with room content | mitigate | The module requires only `chain-recommender.cjs` and `local-chain-recommender.cjs`. Task 1 Arm 5 and the Task 2 acceptance criteria both assert the comment-stripped source carries zero `brain-client` / `fetch(` / `node:http` tokens, so this seam can never become a new egress site. The RAW seed it forwards is a generic framework-name handle, never user content - the Canon Part 8 classification of that field is already performed inside `chain-recommender.cjs` (its existing `sanitizeCypherInput` defence-in-depth at lines 394-400), and this plan neither weakens nor bypasses it. |
| T-254-02 | Elevation of Privilege | A second posture authority auto-running a material step | mitigate | `chain-source.cjs` returns framework NAMES only. It never touches `recipe-maps.postureForCommand`, never returns a posture, and never resolves a command. Posture stays where Phase 237-02's measurement put it (48/112 disagreements and 12 material auto-runs when a second authority existed). The Task 2 acceptance criteria assert `postureForCommand` and `composeWorkflow` appear nowhere in the module source. |
| T-254-03 | Repudiation | Silent degrade concealing which source produced the chain | mitigate | Every return carries an explicit `source` field with exactly two legal literals, and `describeSource` is the ONE disclosure string both surfaces print (Decision 8, D-03 step 4). A refusal or degrade is a stated value, never `null` and never an absent field. The known stale-stderr interaction with `recommendFrameworkChain`'s Phase 252-01 line is documented in the module header rather than silently papered over. |
| T-254-04 | Denial of Service | Unbounded path walk in `recommendMultiHopChains` | accept | The walk is already acyclic and hop-bounded (`maxHops`, default 3, cycle-guarded by a `visited` set) in the shipped module, over a committed 73-edge artifact. This plan passes a bound derived from `MAX_CHAIN_LENGTH` and adds no new traversal. No change to the shipped bound is in scope. |

No package-manager installs occur in this plan, so no `T-254-SC` supply-chain row applies. No
package legitimacy audit is required.
</threat_model>

## Artifacts this phase produces (Plan 01)

| Artifact | Kind | Symbols |
|---|---|---|
| `lib/workflow/chain-source.cjs` | NEW module | `resolveChainSource(opts)`, `describeSource(result)`, `MAX_HOPS` |
| `tests/run-all-254.sh` | NEW aggregator | `TEST_254_PREFIX` override, `run`, `run_may_skip`, `found -eq 0` guard, `EMDASH_TARGETS` |
| `tests/test-254-projection-chain-source.cjs` | NEW test | WIRE-01 arms 1-6 |
| `tests/test-254-degrade-floor.cjs` | NEW test | WIRE-02 arms 1-6 |
| `.planning/REQUIREMENTS.md` rows | registered in Plan 06 | `WIRE-01`, `WIRE-02` |

Explicitly NOT modified by this plan: `lib/brain/chain-recommender.cjs`,
`lib/workflow/local-chain-recommender.cjs`, `lib/workflow/command-resolver.cjs`,
`lib/core/recipe-maps.cjs`, `lib/core/navigation-engine.cjs`, `lib/core/framework-chain-composer.cjs`.

<verification>
1. `node tests/test-254-projection-chain-source.cjs` exits 0.
2. `node tests/test-254-degrade-floor.cjs` exits 0.
3. `bash tests/run-all-254.sh` exits 0.
4. `TEST_254_PREFIX=tests/test-254-nonexistent- bash tests/run-all-254.sh` exits non-zero.
5. `node tests/test-chain-transform-composition.cjs` exits 0 (pre-existing suite unregressed).
6. `node scripts/build-orchestration-projection.cjs --check` prints `orchestration-projection: OK` (pre-commit runs it regardless; this plan changes no projection input).
7. `node tests/test-252-guard-census.cjs` exits 0 - `chain-source.cjs` calls no `isAvailable(` / `ensureAvailable(`, so census.1 must not see a new unclassified guard site (Pitfall 5).
</verification>

<success_criteria>
- One shared seam exists and is the only place the projection-vs-floor decision is made.
- The seam is provably local: no Brain require, no network token, no second ranker, no second resolver, no second posture authority.
- The two most common real seeds (`Beautiful Question Framework` for ill-defined AND the no-problem-type default) return a non-empty registry-floor answer, exactly as they do today - D-03's non-negotiable correctness requirement holds.
- A seed WITH projection edges returns the multi-hop path with its earned composed confidence, which is the defect 254-RESEARCH.md Section 2.3 measured.
- D-04 holds structurally: this plan has an empty `depends_on`, references nothing from the composition wave, and would still ship unchanged if D-01 were ever revisited.
- Zero files outside this plan's `files_modified` were touched.
</success_criteria>

<output>
Create `.planning/phases/254-orchestration-projection-consumption-wiring-suggest-next-act/254-01-SUMMARY.md` when done.
</output>
