---
phase: 254-orchestration-projection-consumption-wiring-suggest-next-act
plan: 05
type: execute
wave: 3
depends_on: []
files_modified:
  - lib/core/brain-client.cjs
  - tests/test-254-ambiguous-disclosure.cjs
  - tests/test-254-normalize-roundtrip-probe.cjs
  - tests/test-254-live-normalize-probe.sh
autonomous: true
requirements: [COMP-02]
canon_parts: [Part 8, Part 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: []
  gate_impact: "none. This plan adds an ADDITIVE, non-blocking disclosure field to callTool's success returns and adds three test files. It adds, modifies and removes zero invocable surfaces, mints no reach/edge/node, changes no tool schema, and opens no new wire. The three CIRS gates are unaffected. The blocking behaviour of the Part 8 belt is byte-unchanged: block still blocks, allow still proceeds untouched, and the transport-null contract that ~82 degradation tests key on is preserved exactly."
  explanation: "Filed because this plan edits lib/core/brain-client.cjs, the single dispatch seam all 16 Brain wrappers flow through and the locus of Canon Part 8 enforcement. The R12 trigger is not met (no commands/|skills/|agents/ changes, empty spine_consumed). Part 8 is primary: this closes the ONE residual verdict-handling gap between the host PreToolUse hook and the in-process belt."

must_haves:
  truths:
    - "An `ambiguous` Part 8 verdict on the server-side path is DISCLOSED to the caller instead of passing through silently (COMP-02, D-02 Option A)."
    - "An `ambiguous` verdict still PROCEEDS - fail-closed (Option B) is explicitly not implemented, because it would change the behaviour of all 16 wrappers at once and needs its own canary suite."
    - "A `block` verdict still blocks and still returns the `egress_blocked` sentinel object, byte-unchanged."
    - "An `allow` verdict produces a byte-unchanged response with no disclosure field at all - the hot path is untouched."
    - "A transport failure still returns bare `null` and is NEVER decorated: `null` means the wire failed, a sentinel object means the constitution refused, and conflating them makes the refusal invisible."
    - "The disclosure carries no payload-derived bytes - it names the verdict class and tool, never the content that triggered it."
    - "D-06's fresh targeted probe exists: the framework names a live composed call can return are shown to survive the Phase 262 hop-depth-1 ALIAS_OF fork without the consumer guessing, and a live leg measures the current population when the Brain is reachable."
  artifacts:
    - path: "lib/core/brain-client.cjs"
      provides: "The COMP-02 ambiguous-verdict disclosure: verdict capture beside the existing block branch, _attachEgressDisclosure helper, and application at callTool's three success returns"
      contains: "egress_disclosure"
    - path: "tests/test-254-ambiguous-disclosure.cjs"
      provides: "COMP-02 live-wire coverage over the loopback capture server: proceed-and-disclose, block-unchanged, allow-unchanged, null-undecorated, and the no-laundering canary"
      min_lines: 120
    - path: "tests/test-254-normalize-roundtrip-probe.cjs"
      provides: "D-06 hermetic leg: the alias-fork non-guess proof, the retry-leg firing condition, the live-probe worklist derivation, and the BRAIN_PROBLEM_TYPE_ALIASES pin"
      min_lines: 90
    - path: "tests/test-254-live-normalize-probe.sh"
      provides: "D-06 live leg: normalize_framework_name round-trip measurement against the live population, printing SKIP when keyless or unreachable"
      min_lines: 40
  key_links:
    - from: "lib/core/brain-client.cjs"
      to: "lib/core/part8-egress-guard.cjs"
      via: "classify() at the callTool chokepoint, now reading the ambiguous verdict as well as block"
      pattern: "classify\\("
    - from: "tests/test-254-ambiguous-disclosure.cjs"
      to: "tests/helpers/brain-capture-server.cjs"
      via: "startCaptureServer + the documented env-before-require ordering contract"
      pattern: "startCaptureServer"
---

<objective>
Close COMP-02, the ONE residual gap the D-01 ratification surfaces, using D-02's locked Option A.

Measured state, from 254-RESEARCH.md Section 3.4 with both guard implementations read side by
side: `scripts/part8-egress-guard-hook.cjs` (host PreToolUse) exits 2 AND renders a Shape F.1 gate
on an `ambiguous` verdict; `lib/core/brain-client.cjs::callTool`'s belt blocks on `block` only and
lets `ambiguous` pass through SILENTLY. Server-side composed calls carry no `mcp__*brain*` token, so
the hook never sees them. What is genuinely lost on that path is not the block - the belt catches
that - it is the `ambiguous`-to-human signal.

D-02 is locked: implement Option A, disclose-and-proceed, using the additive typed-field idiom
`brain-router.cjs:414-422` already uses for `brain_refusal`. Three things are explicitly forbidden
and each is a plausible wrong turn: do NOT implement Option B (fail-closed on `ambiguous`) - it
changes the behaviour of all 16 wrappers at once, risks false blocks on the hot path, and needs its
own canary suite that is out of this phase's scope. Do NOT render a Shape F.1 gate from inside
`callTool` - a gate belongs at the MCP handler (`lib/mcp/gate-render.cjs::renderGate`, the shipped
ladder `framework_run` uses at `sensors.cjs:354`), never at the transport chokepoint, and inventing
an elicitation path inside a stateless server handler is exactly what Option A avoids. Do NOT change
what `classify()` does - it is consumed across `lib/` and the two measured laundering vectors defeat
any naive replacement.

This plan also discharges D-06. This is the composition wave and it touches a live Brain call, so
Phase 262's D-07 finding is NOT sufficient here: a composed call reads the LIVE `:Framework`
population, where the 71 `<SEP>`-corrupted names and the traced hop-depth-1 `ALIAS_OF` fork
(`normalize_framework_name({raw:'Scenario Planning'})` returning TWO "canonical" matches) still
live. The probe has two legs: a hermetic one that hard-gates the consumer's non-guessing behaviour,
and a live one that measures the current population and skips honestly when the Brain is keyless or
unreachable.

Purpose: after this plan, the belt's verdict handling either matches the hook's or the divergence is
a stated, tested decision - which is the literal text of COMP-02.
Output: one additive disclosure path in `callTool`, one live-wire suite, and D-06's two-leg probe.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/254-orchestration-projection-consumption-wiring-suggest-next-act/254-CONTEXT.md
@.planning/phases/254-orchestration-projection-consumption-wiring-suggest-next-act/254-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the COMP-02 ambiguous-disclosure RED suite</name>
  <files>tests/test-254-ambiguous-disclosure.cjs</files>
  <read_first>
    - `tests/helpers/brain-capture-server.cjs` in full (214 lines) - especially the ORDERING CONTRACT in its header at lines 16-24, which is load-bearing and NOT optional: set `process.env.MINDRIAN_BRAIN_URL` and `process.env.MINDRIAN_BRAIN_KEY` to the server's `url` BEFORE requiring `lib/core/brain-client.cjs` (because `BRAIN_URL` is captured at module load, line 24), and `delete require.cache[<resolved path>]` before that require for isolation. Also read `setToolScript` / `resetToolScript` (the Phase 259 scripted-response mechanism) and the `captured` array shape `{ name, arguments }`.
    - `tests/test-239-query-egress-canary.cjs` in full - the 7-leg canary with a live mutation leg. The 2026-08-20 handoff section 5 mandates modelling egress proofs on it; copy its leg structure and its canary-token discipline.
    - `lib/core/part8-egress-guard.cjs` lines 320-395 - `classify(payload, opts)`'s full verdict ladder. The exact ambiguous-producing shapes are: a non-object payload (`class: 'unknown'`), an unproven packet shape (`class: 'unproven_packet'`), a free-form string with no methodology vocabulary match (`class: 'freeform_unmatched'`), and the terminal `class: 'unknown'`. The allow shapes are a zero-key payload, a proven Phase 110 packet, and a generic methodology vocabulary handle. The block shapes are a forbidden-pattern hit.
    - `lib/core/brain-client.cjs` lines 495-600 - the key gate, the belt at 527-540, `_ensureSession`, and the start of the retry loop. Then the SSE parse block at roughly 680-700, which holds `callTool`'s THREE success returns (`JSON.parse(textContent.text)`, `{ text: textContent.text }`, `parsed.result || null`).
    - `lib/mcp/brain-router.cjs` lines 403-421 - the `brain_refusal` additive-field idiom D-02 names as the pattern to borrow (best-effort, additive, never replaces the existing shape, never blocks the return).
    - `lib/core/refusal-messaging.cjs` lines 275-300 and 440-460 - `refusalResponse(kind, ctx)` and the exported surface. Read `REFUSAL_KINDS` carefully: an unknown kind silently falls back to `'unreachable'`, which is why Task 2 forbids routing this disclosure through `refusalResponse`.
  </read_first>
  <behavior>
    Every arm runs against the loopback capture server. No live Brain, no network beyond localhost.

    - Arm 1 (proceed and disclose): drive a `callTool` through a wrapper whose payload classifies
      `ambiguous` (use a `freeform_unmatched` shape - a free-form string with no methodology
      vocabulary match and no forbidden pattern). Assert THREE things together: the capture server
      RECEIVED the call (it proceeded, so `captured.length` grew), the returned value is a non-null
      object, and it carries `egress_disclosure` with `verdict === 'ambiguous'`, a non-empty
      `egress_class`, `tool` equal to the tool name, and `disposition === 'proceeded'`.
    - Arm 2 (block unchanged): a payload classifying `block` (a forbidden-pattern hit). Assert the
      capture server received NOTHING new (`captured.length` unchanged - a blocked payload opens no
      socket at all, which is why the belt sits before `_ensureSession`), and the return is the
      `{ error: 'egress_blocked', tool, egress_class }` sentinel with NO `egress_disclosure` field.
    - Arm 3 (allow byte-unchanged): a payload classifying `allow` (a generic methodology vocabulary
      handle). Assert the returned object has NO own property `egress_disclosure`. Use
      `Object.prototype.hasOwnProperty` so an inherited or undefined-valued key still fails.
    - Arm 4 (null undecorated): script the capture server (via `setToolScript`) to return a 500 so
      the retry budget exhausts and `callTool` returns bare `null`, with an `ambiguous` payload.
      Assert the return is EXACTLY `null` - `null === result`, not a decorated object. Pitfall 7:
      `null` is the test-pinned transport-failure signal across ~82 degradation tests; a refusal is
      a sentinel OBJECT. Conflating them makes the refusal invisible.
    - Arm 5 (no laundering into the disclosure): send an `ambiguous` payload containing a unique
      canary token. Assert the canary DOES appear in the captured wire body (the call proceeded, so
      the canary really did egress - Option A is disclose-and-proceed, not block) AND that
      `JSON.stringify(result.egress_disclosure)` does NOT contain the canary. The disclosure must
      name the verdict, never carry the content.
    - Arm 6 (sentinel siblings undecorated): script a 403 so `callTool` returns the `tier_denied`
      sentinel, with an `ambiguous` payload. Assert no `egress_disclosure` is attached to it. A
      constitutional refusal object is not a success return and must not be decorated.
    - Arm 7 (belt still first): assert the ordering is unchanged - a `block` payload with NO key set
      still returns `null` (the key gate is before the belt, so the no-key contract is
      byte-unchanged), while a `block` payload WITH a key returns `egress_blocked`.
  </behavior>
  <action>
    Write `tests/test-254-ambiguous-disclosure.cjs` before touching `brain-client.cjs`, so Arms 1
    and 5 fail RED and prove they measure the right thing while Arms 2, 3, 4, 6 and 7 pass
    IMMEDIATELY - those five are regression pins on behaviour that must not change, and their
    passing before AND after Task 2 is the evidence that Option A did not become Option B by
    accident.

    Honour the capture server's ordering contract exactly: env vars set first, `require.cache`
    deleted for the resolved `lib/core/brain-client.cjs` path, then the require. Re-do that dance
    per arm where an arm needs different env, and call `resetCaptured()` / `resetToolScript()`
    between arms so one arm's script cannot bleed into the next.

    Pick the concrete wrapper to drive by reading which of the 16 `callTool` callers takes the
    freest-form argument, and state your choice in the SUMMARY. Whichever you pick, derive the
    payload shapes by calling `part8-egress-guard.classify()` DIRECTLY first and asserting the
    verdict is what you expect BEFORE using that payload in a wire arm - do not assume a payload
    classifies the way you intended. A wire arm built on a mis-assumed verdict silently tests
    nothing.

    Always `stopCaptureServer()` in a finally block so a failing arm cannot leave a listening
    socket behind.

    Hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-254-ambiguous-disclosure.cjs; echo "exit=$? (Arms 1 and 5 RED expected before Task 2; Arms 2,3,4,6,7 must already PASS)"</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and runs to completion; Arms 2, 3, 4, 6 and 7 PASS before Task 2 (they pin unchanged behaviour) and Arms 1 and 5 FAIL (the disclosure does not exist yet).
    - At least 7 distinct arms are present, covering every bullet in `<behavior>`.
    - Each wire arm's payload verdict is asserted against a direct `classify()` call before it is used on the wire.
    - The suite starts and stops the capture server cleanly: `node tests/test-254-ambiguous-disclosure.cjs` returns to the shell, never hangs.
    - The suite makes no request outside `127.0.0.1` / `localhost`.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' tests/test-254-ambiguous-disclosure.cjs` returns nothing.
  </acceptance_criteria>
  <done>The suite pins the four verdict outcomes and the null contract, and fails on exactly the two arms that describe behaviour Task 2 is about to add.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the ambiguous-verdict disclosure in callTool (D-02 Option A)</name>
  <files>lib/core/brain-client.cjs</files>
  <read_first>
    - `lib/core/brain-client.cjs` lines 440-600 in full, then the SSE parse block at roughly 675-705. Read the belt's placement-rationale comment at lines 502-526 in its entirety before editing: it explains why the guard sits AFTER the key gate and BEFORE `_ensureSession`, why one guard covers all 16 wrappers, why the four existing raw-field guards are NOT replaced by it, and why `egress_blocked` is a sentinel OBJECT rather than `null`. Your edit extends that comment's logic; it must not contradict it.
    - `lib/mcp/brain-router.cjs` lines 403-421 - the additive-field idiom.
    - `lib/core/refusal-messaging.cjs` `refusalResponse` and `REFUSAL_KINDS`.
    - `scripts/check-substrate.cjs` `ALLOWED_DIRECT_IMPORT` - confirm `lib/core/brain-client.cjs` is NOT on it (Pitfall 6, the substrate trap that bit 239-05). This task adds no db access, so it must not add one by accident.
  </read_first>
  <action>
    Four edits, all additive, none changing an existing branch's behaviour.

    (1) Declare `let ambiguousDisclosure = null;` immediately BEFORE the existing belt try-block at
    line 527, in `callTool`'s function scope so it survives into the retry loop and the SSE parse.

    (2) Inside the existing belt try-block, keep the `block` branch EXACTLY as it is - do not
    reorder it, do not reword its sentinel, do not change its `egress_class` default. Add an `else
    if` for `verdict === 'ambiguous'` that sets
    `ambiguousDisclosure = { verdict: 'ambiguous', egress_class: callToolEgressVerdict.class || 'unknown', reason: callToolEgressVerdict.reason || null, tool: toolName, disposition: 'proceeded' }`
    and does NOTHING else. It does not return, does not throw, does not log, does not prompt. The
    belt's existing catch stays as-is (a belt-internal error still degrades to existing behaviour).

    CRITICAL, and it is the exact wrong turn available here: do NOT build this object via
    `refusalMessaging.refusalResponse('part8_ambiguous', ...)`. `refusalResponse` silently falls
    back to `'unreachable'` for any kind not in `REFUSAL_KINDS`, so routing through it would emit a
    MISLEADING transport-failure kind for a constitutional verdict. D-02 says borrow the IDIOM
    (an additive typed field, best-effort, never blocking) that `brain-router.cjs:414-422` uses -
    it does not say reuse that function. Do not add a new kind to `REFUSAL_KINDS` either: nothing
    was refused here, the call proceeded, and widening a refusal vocabulary to describe a
    non-refusal would corrupt a shared vocabulary that `tests/test-252-guard-census.cjs` census.2
    already keys on.

    (3) Add a module-private helper near the belt:
    `function _attachEgressDisclosure(result, disclosure)`. It returns `result` UNCHANGED when
    `disclosure` is falsy, when `result` is not a non-null plain object, or when `result` has an own
    `error` property (so `tier_denied` / `invalid_key` / `rate_limited` / `egress_blocked` sentinels
    are never decorated - Arm 6). Otherwise it sets `result.egress_disclosure = disclosure` and
    returns `result`. It must never throw; wrap the assignment defensively so a frozen or exotic
    result object degrades to the undecorated value rather than crashing a Brain call.

    (4) Apply `_attachEgressDisclosure(<expr>, ambiguousDisclosure)` at `callTool`'s THREE success
    returns only - the SSE-parsed `JSON.parse(textContent.text)` return, the `{ text: textContent.text }`
    return, and the `parsed.result || null` return. Do NOT apply it to any `return null`, and do NOT
    apply it to any sentinel return. For `parsed.result || null` the helper's own non-object guard
    already handles the null case, but write it so the intent is legible at the call site rather
    than relying on the guard alone.

    Update the belt's block comment (lines 502-526) with a short added paragraph recording: Phase
    254 / COMP-02 / D-02 Option A; that `ambiguous` now DISCLOSES and still PROCEEDS; that Option B
    (fail-closed on `ambiguous`) was considered and explicitly rejected for this phase because it
    changes 16 wrappers at once and needs its own canary suite; that a Shape F.1 gate belongs at the
    MCP handler via `gate-render.cjs::renderGate` and never at this chokepoint; and that the
    disclosure names the verdict class and never carries payload bytes. Also note that the hook
    (`scripts/part8-egress-guard-hook.cjs`) still gates `ambiguous` with a real F.1 render on the
    model-issued path, so the two enforcement points now DIVERGE BY A STATED DECISION rather than by
    omission - which is COMP-02's literal wording.

    Do not touch the four existing raw-field classify sites (query :601, hatAwareRecommend :942,
    suggestValidationSteps :1056, sendPacket :1643) or the fifth belt site. Do not touch
    `BRAIN_PROBLEM_TYPE_ALIASES` or `_normalizeBrainProblemType` - see Task 3 for the stated
    decision on those. Do not add any `node:sqlite` or `roomDir`-to-db access (Pitfall 6).

    Hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-254-ambiguous-disclosure.cjs && node tests/test-239-query-egress-canary.cjs && node tests/test-brain-client-params.cjs && node tests/test-247-contract-client.cjs && node tests/test-250-transport-retry.cjs && node tests/test-252-guard-census.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-254-ambiguous-disclosure.cjs` exits 0 with all seven arms passing.
    - `node tests/test-239-query-egress-canary.cjs` exits 0 - the 7-leg egress canary including its live mutation leg is unregressed.
    - `node tests/test-brain-client-params.cjs`, `node tests/test-247-contract-client.cjs` and `node tests/test-250-transport-retry.cjs` all exit 0 - the transport, contract and retry contracts are unchanged.
    - `node tests/test-252-guard-census.cjs` exits 0.
    - `git diff lib/core/brain-client.cjs` shows only: one `let ambiguousDisclosure` declaration, one added `else if` branch inside the existing belt try, one new `_attachEgressDisclosure` helper, three success returns wrapped, and added comment text. The `block` branch, the key gate, `_ensureSession`, the retry loop, every `return null`, and every sentinel return are unchanged.
    - `grep -c 'refusalResponse' lib/core/brain-client.cjs` returns the SAME count as before this task (the disclosure does not route through it).
    - `node scripts/check-substrate.cjs --diff` passes - no direct db import was added.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' lib/core/brain-client.cjs` returns nothing.
  </acceptance_criteria>
  <done>An ambiguous Part 8 verdict on the server-side path is now visible to the caller, the call still proceeds, and every other verdict path is byte-identical to before.</done>
</task>

<task type="auto">
  <name>Task 3: Build D-06's two-leg normalize round-trip probe and pin BRAIN_PROBLEM_TYPE_ALIASES</name>
  <files>tests/test-254-normalize-roundtrip-probe.cjs, tests/test-254-live-normalize-probe.sh</files>
  <read_first>
    - `254-CONTEXT.md` D-06 in full - the sentence "for each framework name a live composed call can return, assert it round-trips clean through `normalize_framework_name` without the alias fork Phase 262 traced" and the instruction to build on `chain-recommender.cjs`'s existing `normalizeFrameworkName` retry leg.
    - `254-RESEARCH.md` Section 6 in full, especially 6.3's scope-limit quote and the table splitting Wave 1 (no fresh measurement needed) from the composition wave (fresh probe required).
    - `lib/brain/chain-recommender.cjs` lines 645-770 - `adaptChainToRunInput` in full. This is the ONE existing consumer of `brainClient.normalizeFrameworkName`. Note precisely: the retry fires ONLY when `commandResolver.commandsForFramework(rawName)` returns empty; the normalized string is read defensively across `.canonical` / `.name` / `.normalized` / a bare string; a miss after the one retry KEEPS the original name and pushes it onto `unmapped`, never invents and never drops.
    - `lib/core/brain-client.cjs` lines 1530-1575 - `BRAIN_PROBLEM_TYPE_ALIASES` and `_normalizeBrainProblemType`, including the comment at 1535-1540 explaining that this map is deliberately NOT the same map as `chain-recommender.cjs`'s `PROBLEM_TYPE_ALIASES` (which targets the LOCAL router codes UDP/IDP/WDP).
    - `docs/262-FLOOR-01-GAP-LEDGER.md` section 5 - the D-07 finding and the traced hop-depth-1 `ALIAS_OF` fork where `normalize_framework_name({raw:'Scenario Planning'})` returns TWO canonical matches.
    - `tests/run-all-262.sh` lines 54-68 and 76-81 - the `run_may_skip` helper and the `^SKIP` convention, which is why the live leg is a `.sh` file rather than a `.cjs` one (the `.cjs` glob arm uses the hard `run`, so a skippable probe must be shell).
    - `tests/helpers/brain-capture-server.cjs` `setToolScript` - the mechanism the hermetic fork arm uses to serve a two-match payload.
  </read_first>
  <behavior>
    HERMETIC leg, `tests/test-254-normalize-roundtrip-probe.cjs` (hard gate, always runs):
    - Arm 1 (retry firing condition, structural): the comment-stripped source of
      `lib/brain/chain-recommender.cjs` shows `normalizeFrameworkName` called only inside the
      `localCommands.length === 0` branch. A name the local registry already resolves triggers ZERO
      normalize calls, so the live population is only ever consulted for a local miss. This bounds
      the exposure D-06 is about.
    - Arm 2 (the alias-fork non-guess, the load-bearing arm): drive `adaptChainToRunInput` over the
      loopback capture server with `setToolScript` serving a `normalize_framework_name` response
      carrying TWO canonical matches (the Phase 262 fork shape). Assert the consumer does NOT pick
      one: the ORIGINAL framework name is preserved in `chain_input`, the name appears in
      `unmapped`, and no invented canonical string appears anywhere in the result. This is the
      guarantee that "round-trips clean without the alias fork" actually means something at the
      consumer, and it is the part this repo controls.
    - Arm 3 (worklist derivation): compute and PRINT the set of the registry's framework names for
      which `commandResolver.commandsForFramework(name)` returns empty. That set is exactly the set
      a live composed call could send to `normalize_framework_name`, and it is the live leg's target
      list. Assert only that the set is computed and printed; do not pin its size (Pitfall 8).
    - Arm 4 (`BRAIN_PROBLEM_TYPE_ALIASES` pin): assert the map's current 8 keys project onto the
      three incumbent canonical names (`Undefined Problem`, `Ill-Defined Problem`,
      `Well-Defined Problem`) and that `_normalizeBrainProblemType` passes an unknown well-shaped
      token through UNCHANGED. Carry the STATED DECISION below in the arm's comment.
    - Arm 5 (map separation preserved): assert `BRAIN_PROBLEM_TYPE_ALIASES`'s values and
      `chain-recommender.cjs`'s `PROBLEM_TYPE_ALIASES` values are disjoint sets, so the two maps
      cannot be conflated by a future edit. The existing comment says they are deliberately not
      shared; this makes that structural.

    LIVE leg, `tests/test-254-live-normalize-probe.sh` (skippable):
    - Prints `SKIP: no Brain key / Brain unreachable` and exits 0 when a key cannot be resolved or
      the endpoint does not answer. The aggregator's `run_may_skip` reports that as SKIPPED, not
      FAILED.
    - Otherwise calls `normalize_framework_name` once per name in Arm 3's worklist plus the three
      names Phase 262 named as fork-prone, and prints one line per name with the canonical-match
      count.
    - Prints a summary line `PROBE: <n> names, <k> forks` and exits 0.
  </behavior>
  <action>
    Write both files.

    THE STATED DECISION ON `BRAIN_PROBLEM_TYPE_ALIASES` (D-07 gave the planner the call; this is it,
    recorded so it is a decision and not a silent drop). The map is NOT re-pointed in this phase.
    Reason, verified during planning: Theo's live `DomainConcept` ids are `UnDefined`, `IllDefined`,
    `WellDefined`, `Wicked`, `Trinity`, `Compass`, while the incumbent Brain's are
    `Undefined Problem` / `Ill-Defined Problem` / `Well-Defined Problem`. No single value satisfies
    both, and the standing rule is to plan and ship against the CURRENT Brain because Theo is not
    deployable (no remote hosting story, its own Phase 08.4 not started). Re-pointing today would be
    a live regression traded for a future convenience. Removing the map entirely is worse: an
    unmapped `udp` would pass through as `udp` and go honest-empty on the incumbent too. So the map
    is PINNED here (Arms 4 and 5) and ENUMERATED as a named plugin-side flip item in Plan 06's Theo
    note, with the exact incumbent-to-Theo mapping written down, which turns a future rediscovery
    into a single-line diff. Write this rationale into Arm 4's comment block, not only into the
    SUMMARY.

    THE STATED DECISION ON THE LIVE LEG'S EXIT CODE. A measured fork in the live population is a
    Brain-repo defect (Phase 262 traced it; remediation is Phase 263's and the Brain repo's), not a
    plugin defect this build can fix. Failing this repo's build on another repo's data would be a
    permanent red gate nobody can clear from here. So the live leg REPORTS forks loudly and exits 0,
    and the hermetic Arm 2 - which proves the consumer never guesses when a fork is served - is the
    part that HARD-GATES. Record the measured fork count in the SUMMARY, dated, so the measurement
    is on the record rather than in a scrollback. This is the honest split between "measure the
    population" and "gate what we control"; state it explicitly in the `.sh` file's header too.

    For the live leg, resolve the key through the shipped ladder (`lib/core/resolve-brain-key.cjs`)
    rather than reading an env var directly, and reach the Brain through the shipped client rather
    than hand-rolling a fetch. If the simplest honest implementation is a small `node -e` invoked
    from the shell script, do that - the `.sh` wrapper exists for the `run_may_skip` SKIP
    convention, not because the probe logic must be bash.

    Hyphens only, no em-dashes, in both files.
  </action>
  <verify>
    <automated>node tests/test-254-normalize-roundtrip-probe.cjs && bash tests/test-254-live-normalize-probe.sh && bash tests/run-all-254.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-254-normalize-roundtrip-probe.cjs` exits 0 with all five arms passing.
    - Arm 2 demonstrably fails if the consumer's behaviour changes: temporarily serving a ONE-match payload makes the arm's original-name assertion flip, proving the arm is measuring the fork path and not passing vacuously. Verify this once during execution and record it in the SUMMARY; leave the file in its two-match form.
    - Arm 3 prints the derived worklist to stdout, and the printed set contains no name that `commandsForFramework` resolves.
    - `bash tests/test-254-live-normalize-probe.sh` exits 0 in BOTH states: with no Brain key it prints a line starting `SKIP` and exits 0; with a reachable Brain it prints one line per probed name plus a `PROBE: <n> names, <k> forks` summary and exits 0.
    - `bash tests/run-all-254.sh` exits 0 and reports the `.sh` probe as either PASSED or SKIPPED, never FAILED.
    - The SUMMARY records: the measured fork count `k` with its date and whether the Brain was reachable, the derived worklist size, and the `BRAIN_PROBLEM_TYPE_ALIASES` stated decision verbatim.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' tests/test-254-normalize-roundtrip-probe.cjs tests/test-254-live-normalize-probe.sh` returns nothing.
  </acceptance_criteria>
  <done>D-06 is discharged with both legs: the consumer provably never guesses on an alias fork, and the live population is measured and recorded when it is reachable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any of the 16 wrapper callers -> `callTool(toolName, args)` | The single dispatch seam. `args` is the RAW payload and it is classified HERE, before `_ensureSession`, so a blocked payload opens no socket at all. |
| `callTool` -> `pws-brain-mcp.onrender.com` | The only Brain wire in the process. Every composed call, every CLI-script call and every wrapper transits it. |
| `callTool` result -> the calling handler -> the navigator | The new `egress_disclosure` field rides this path. |
| host PreToolUse hook -> model-issued `mcp__*brain*` calls | Seam A. Sees `ambiguous` and gates it with a real F.1 render. Not reachable from here and not changed by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-254-21 | Information Disclosure | The disclosure field laundering the very payload the classifier flagged | mitigate | `_attachEgressDisclosure` copies ONLY `{ verdict, egress_class, reason, tool, disposition }` from the verdict object. Every ambiguous-branch `reason` in `part8-egress-guard.cjs` is a constant literal (`'non-object payload'`, `'packet shape not proven'`, `'no methodology vocabulary match'`, `'neither proven move-set nor content hit'`) and none is payload-derived - unlike the block branch's `'forbidden pattern hit: ' + scan.matched_pattern`, which this path never reaches because block returns before the ambiguous branch. Arm 5 is the canary that pins it: the token must appear on the wire and must NOT appear in the disclosure. |
| T-254-22 | Information Disclosure | RAW-field classification order regressing | mitigate | The four existing raw-field guards (query :601, hatAwareRecommend :942, suggestValidationSteps :1056, sendPacket :1643) classify the RAW field BEFORE sanitize and BEFORE interpolation, precisely because the assembled template's own word `Framework` can launder an embedded canary from `ambiguous` to `allow`, and because `sanitizeCypherInput` strips the `@` the PII pattern keys on. This plan touches none of them, adds no interpolation, and the belt stays a belt UNDERNEATH them rather than a substitute. `node tests/test-239-query-egress-canary.cjs` is a hard acceptance criterion. |
| T-254-23 | Repudiation | An `ambiguous` verdict passing through invisibly | mitigate | This is the threat COMP-02 exists to close. After this plan the verdict is a typed additive field on the response, so the navigator sees it. The divergence from the hook (which gates rather than discloses) becomes a STATED, TESTED decision recorded in the belt's own comment block, which is COMP-02's literal wording. |
| T-254-24 | Repudiation | Conflating a constitutional refusal with a transport outage | mitigate | Arm 4 pins `null` as undecorated and Arm 6 pins every `error`-carrying sentinel as undecorated. `null` remains the transport-failure signal across ~82 degradation tests; refusals remain sentinel objects (`egress_blocked`, `tier_denied`, `invalid_key`, `rate_limited`). `_attachEgressDisclosure` returns non-object and `error`-bearing results unchanged by construction, so the distinction cannot erode by edit. |
| T-254-25 | Elevation of Privilege | Option B arriving by accident and blocking the hot path | mitigate | The ambiguous branch sets a variable and does nothing else - no return, no throw. Arms 1 and 5 both assert the capture server RECEIVED the call, so any change that started blocking would fail two arms. The belt's comment records Option B as explicitly rejected for this phase with its reason (16 wrappers at once, needs its own canary suite). |
| T-254-26 | Denial of Service | The disclosure helper throwing inside the Brain path | mitigate | `_attachEgressDisclosure` is required to never throw: it guards on falsy disclosure, non-object result and own-`error` result, and wraps the assignment so a frozen or exotic result degrades to the undecorated value. A belt that can crash a Brain call is worse than the gap it closes. |
| T-254-27 | Tampering | The substrate trap (Pitfall 6) | mitigate | `lib/core/brain-client.cjs` is NOT on `check-substrate.cjs`'s `ALLOWED_DIRECT_IMPORT` list; adding a `roomDir`-to-db opener trips pre-commit. This task adds no db access at all, and `node scripts/check-substrate.cjs --diff` is a hard acceptance criterion. The sanctioned idiom if telemetry is ever wanted here is `_logEventBestEffort(options.db, ...)` with a caller-supplied handle, scalars only - explicitly NOT in scope for this plan. |
| T-254-28 | Tampering | A live-Brain alias fork causing a guessed canonical name | mitigate | Hermetic Arm 2 proves the one existing consumer keeps the ORIGINAL name and marks it `unmapped` when a fork is served, never picking one of the two arbitrarily. Theo's own `resolveFramework` treats the identical shape as `ALIAS_FORK` and refuses honestly, so this repo's defensive behaviour is aligned with the successor's design rather than diverging from it. The live leg measures the current population and records the count. |

No package-manager installs occur in this plan, so no `T-254-SC` supply-chain row applies. No new
dependency is added; every module used is already in the tree.
</threat_model>

## Artifacts this phase produces (Plan 05)

| Artifact | Kind | Symbols |
|---|---|---|
| `lib/core/brain-client.cjs` | MODIFIED | `ambiguousDisclosure` capture in `callTool`, new `else if (verdict === 'ambiguous')` branch, new module-private `_attachEgressDisclosure(result, disclosure)`, three success returns wrapped, extended belt comment block. New response field: `egress_disclosure` |
| `tests/test-254-ambiguous-disclosure.cjs` | NEW test | COMP-02 arms 1-7 over the loopback capture server |
| `tests/test-254-normalize-roundtrip-probe.cjs` | NEW test | D-06 hermetic leg, arms 1-5, including the `BRAIN_PROBLEM_TYPE_ALIASES` pin and the map-separation proof |
| `tests/test-254-live-normalize-probe.sh` | NEW probe | D-06 live leg, SKIP-capable, `PROBE: <n> names, <k> forks` summary |
| `.planning/REQUIREMENTS.md` rows | registered in Plan 06 | `COMP-02` |

Explicitly NOT modified: `lib/core/part8-egress-guard.cjs` (its `classify()` is consumed across
`lib/` and its behaviour is unchanged), `scripts/part8-egress-guard-hook.cjs`,
`lib/core/refusal-messaging.cjs` (`REFUSAL_KINDS` is NOT widened), `lib/brain/chain-recommender.cjs`,
`BRAIN_PROBLEM_TYPE_ALIASES`'s values, and all 16 wrapper bodies.

<verification>
1. `node tests/test-254-ambiguous-disclosure.cjs` exits 0.
2. `node tests/test-254-normalize-roundtrip-probe.cjs` exits 0.
3. `bash tests/test-254-live-normalize-probe.sh` exits 0 (PASSED or a `SKIP` line).
4. `node tests/test-239-query-egress-canary.cjs` exits 0 - the mandated egress canary model suite.
5. `node tests/test-brain-client-params.cjs`, `node tests/test-247-contract-client.cjs`, `node tests/test-250-transport-retry.cjs` all exit 0.
6. `node tests/test-252-guard-census.cjs` exits 0.
7. `node scripts/check-substrate.cjs --diff` passes (Pitfall 6).
8. `bash tests/run-all-254.sh` exits 0.
9. `node scripts/doctor.cjs --acceptance` passes.
</verification>

<success_criteria>
- An `ambiguous` Part 8 verdict on the server-side path is disclosed and the call proceeds - Option A, exactly as D-02 locked it.
- Option B is not implemented, no gate is rendered from the transport chokepoint, and `classify()` is unchanged.
- `block`, `allow`, the four sentinels and the bare-`null` transport contract are all byte-identical to before, proven by five regression-pin arms that passed before the change and still pass after.
- The disclosure names the verdict and never carries payload bytes, proven by a canary arm.
- D-06 is discharged with both legs, and the `BRAIN_PROBLEM_TYPE_ALIASES` call is a written decision with its reason, pinned by a test and routed into Plan 06's Theo note - not silently dropped.
</success_criteria>

<output>
Create `.planning/phases/254-orchestration-projection-consumption-wiring-suggest-next-act/254-05-SUMMARY.md` when done.
</output>
