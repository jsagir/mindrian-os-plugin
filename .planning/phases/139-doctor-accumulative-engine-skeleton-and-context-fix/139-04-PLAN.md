---
phase: 139-doctor-accumulative-engine-skeleton-and-context-fix
plan: 04
type: execute
wave: 4
depends_on: ["139-01", "139-02", "139-03"]
files_modified:
  - CHANGELOG.md
  - scripts/release.sh
  - data/doctor-modules.json
  - tests/test-139-acceptance.cjs
autonomous: false
requirements: [S4]
canon_parts: [6, 7]

must_haves:
  truths:
    - "The hotfix ships as 1.13.1-beta.4 (NOT 1.13.0.1 -- a hotfix cannot carry a LOWER semver than the current 1.13.1-beta.3; version_note LOCKED), driven by release.sh --prerelease (beta.3 -> beta.4)"
    - "release.sh registers/verifies the umbilical accumulative-engine module at Step 6.6 and the run passes doctor --acceptance"
    - "doctor --acceptance still passes with the engine skeleton + umbilical module wired (release gate intact, 5-place lockstep honored)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "1.13.1-beta.4 entry (S1-S4 summary)"
      contains: "1.13.1-beta.4"
    - path: "scripts/release.sh"
      provides: "Step 6.6 registers/verifies the umbilical module + accumulative engine"
    - path: "tests/test-139-acceptance.cjs"
      provides: "Phase-139 acceptance: resolver + selector + umbilical + Part 8 all green"
  key_links:
    - from: "scripts/release.sh Step 6.6"
      to: "scripts/doctor.cjs --acceptance"
      via: "module registration verified by the acceptance gate"
      pattern: "doctor.cjs --acceptance"
    - from: "data/doctor-modules.json umbilical.introduced_version"
      to: "1.13.1-beta.4"
      via: "introduced_version matches the shipped NEW_VERSION"
      pattern: "1.13.1-beta.4"
---

<objective>
Wire the hotfix into the release pipeline (S4): bump the version per the 5-place lockstep, register/verify the umbilical module at `release.sh` Step 6.6, and pass `doctor --acceptance`. Ship as **1.13.1-beta.4** -- NOT `1.13.0.1`. The user asked for `1.13.0.1`, but `plugin.json` is already at `1.13.1-beta.3` and a hotfix cannot carry a LOWER semver (version_note LOCKED decision). `release.sh --prerelease` advances `1.13.1-beta.3 -> 1.13.1-beta.4` on the current beta line, which is exactly the recommended continuation. Release infrastructure ALWAYS ships as a beta first (release-process.md), so beta.4 is also the correct discipline.

Purpose: register the new accumulative engine as a release-walkable surface and prove the whole phase passes the acceptance gate, so the plugin self-heals forward the same way it sells (Canon Part 6 dog-fooding).

Output: the CHANGELOG entry, the introduced_version reconciliation, the release.sh Step 6.6 registration/verification, a phase acceptance test, and a human checkpoint to actually cut the release.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-CONTEXT.md
@.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-RESEARCH.md
@.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-01-SUMMARY.md
@.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-02-SUMMARY.md
@.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-03-SUMMARY.md
@.claude/includes/release-process.md

<interfaces>
<!-- The release script surfaces this plan touches. release.sh is the ONLY release path (5-place lockstep, TWO-COMMIT form). -->

From scripts/release.sh (the registration + acceptance hook points -- do NOT duplicate verify-release; extend Step 6.6's existing acceptance call):
```
# Invocation:  bash scripts/release.sh --prerelease    # 1.13.1-beta.3 -> 1.13.1-beta.4 (semver.inc prerelease,beta)
# Step 2.5 : doctor --acceptance --pre-flight (HARD ABORT, before any mutation)
# Step 3   : bump .claude-plugin/plugin.json + package.json -> NEW_VERSION
# Step 4   : bump ~/mindrian-marketplace marketplace.json (version + source.ref == vNEW_VERSION)
# Step 6.6 : node scripts/doctor.cjs --acceptance --pre-tag   (HARD ABORT on failure)  <- module-registration verification hooks HERE
# Step 6.6b: tests/test-doctor-acceptance-self-coverage.cjs
# Step 7   : Commit A (tagged) ; Step 7.5 : Commit B (next-bump, un-track node_modules)
# Step 9.8 : full doctor --acceptance (post-publish)
# 5-place lockstep: plugin.json + package.json + marketplace.json(version) + marketplace.json(source.ref) + git tag
# Rollback idiom on Step 6.6 abort: git checkout .claude-plugin/plugin.json package.json CHANGELOG.md (+ marketplace.json in MARKETPLACE_DIR)
```

From data/doctor-modules.json (Plan 03 registered umbilical; reconcile its introduced_version to the shipped version):
```
modules: [ { id:'umbilical', introduced_version:'<reconcile to 1.13.1-beta.4>', fix_supported:true, runner:'lib/core/doctor/umbilical-module.cjs', ... } ]
```

From scripts/doctor.cjs --acceptance (Plan 02 wired runAccumulativeEngine into --acceptance):
```
# --acceptance runs the release-gate checklist; the accumulative engine surface must be present and clean.
# --pre-tag filters to pre-tag-applicable points. NO --allow override (release infra is the one gate you cannot skip).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CHANGELOG entry + introduced_version reconciliation + release.sh Step 6.6 module registration + phase acceptance test</name>
  <files>CHANGELOG.md, data/doctor-modules.json, scripts/release.sh, tests/test-139-acceptance.cjs</files>
  <action>
**CHANGELOG.md**: add a top entry `## [1.13.1-beta.4] - 2026-06-04` with Added/Fixed/Changed sections summarizing S1-S4. Fixed = doctor cwd-misfire (single resolver `lib/core/resolve-umbilical-target.cjs`; OBS-2 closed -- zero room-artifact writes from a non-room dir). Added = accumulative engine skeleton (`data/doctor-modules.json` registry + `~/.mindrian/doctor-applied.json` watermark + semver selector), Umbilical Cord as module #1 (AFFILIATED_WITH projection into room.db). Changed = `ALLOWED_EDGE_TYPES` gains AFFILIATED_WITH (additive). Feynman-plain, no em-dashes.

**data/doctor-modules.json**: reconcile the umbilical entry's `introduced_version` to `"1.13.1-beta.4"` (the exact NEW_VERSION this hotfix ships). This is the lockstep that ties the module to the version that introduced it -- the selector's window math depends on it being correct.

**scripts/release.sh**: at Step 6.6 (the existing `doctor --acceptance --pre-tag` gate), add a module-registration VERIFICATION immediately before or after the acceptance call (do NOT add a second full verify-release; extend the existing step). The verification asserts, for every entry in `data/doctor-modules.json`: the `runner` file exists on disk, `introduced_version` is valid semver, and `introduced_version <= NEW_VERSION` (a module cannot claim to be introduced in a future version). On failure, HARD ABORT with the same rollback semantics as the surrounding Step 6.6 (`git checkout` the bumped plugin.json/package.json/CHANGELOG.md + marketplace.json). Follow the existing Step 6.6 echo/abort/rollback idiom verbatim. Keep it a few lines (a small inline `node -e` check or a tiny `tests/`-style assertion script the step invokes) -- this is wiring, not a new framework.

**tests/test-139-acceptance.cjs**: a phase-level acceptance test that runs the whole S1-S4 chain green in one shot (one Phase-139 gate for release.sh + CI). Reuse the Plan 01-03 hermetic fixtures where practical:
  - resolver: `resolveUmbilicalTarget` returns null from a non-room cwd; resolves from a sentinel and from registry.active.
  - OBS-2: heal from a non-room scratch dir writes zero room artifacts (.mindrian/.heal-backup/heal-log.json all absent).
  - selector: the (applied_through, running] window + idempotency hold with a synthetic registry.
  - umbilical: a marker projects exactly one AFFILIATED_WITH edge; Part 8 audit (no egress, enum-only props, no cross-room edge).
  - registry integrity: every doctor-modules.json runner file exists; the umbilical introduced_version is valid semver and `<=` plugin.json version.
  Keep total runtime under ~60s.
  </action>
  <verify>
    <automated>node tests/test-139-acceptance.cjs && grep -q "1.13.1-beta.4" CHANGELOG.md && node -e "const r=JSON.parse(require('fs').readFileSync('data/doctor-modules.json','utf8'));const u=r.modules.find(m=>m.id==='umbilical');const s=require('semver');process.exit(u&&u.introduced_version==='1.13.1-beta.4'&&s.valid(s.coerce(u.introduced_version))?0:1)"</automated>
  </verify>
  <done>CHANGELOG has the 1.13.1-beta.4 entry; umbilical introduced_version === 1.13.1-beta.4; release.sh Step 6.6 verifies every module runner exists + introduced_version <= NEW_VERSION with HARD-ABORT rollback; phase acceptance test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The full Phase-139 hotfix is implemented and green: the single resolver + OBS-2 fix (S1), the accumulative engine skeleton (S2), Umbilical as module #1 (S3), and the release wiring + CHANGELOG + Step 6.6 module verification (S4). The version target is 1.13.1-beta.4 via `release.sh --prerelease`. Release infrastructure ships as a beta first per release-process.md; the actual cut requires the maintainer to run release.sh from the canonical workspace and push (Claude does not push releases unprompted).
  </what-built>
  <how-to-verify>
1. Confirm you are in the canonical workspace: `pwd` shows `/home/jsagi/MindrianOS-Plugin` (NOT `~/.claude/plugins/*`), then `git fetch origin main && git status`.
2. Dry-run the release to inspect the plan without mutating: `bash scripts/release.sh --prerelease --dry-run` -- confirm it reports NEW_VERSION `1.13.1-beta.4` and lists Step 6.6 (doctor --acceptance --pre-tag) + the new module-registration verification.
3. Run the pre-flight gate read-only: `node scripts/doctor.cjs --acceptance --pre-flight` -- expect pass.
4. Run the phase acceptance test: `node tests/test-139-acceptance.cjs` -- expect green.
5. From a real non-room dir (e.g. `cd ~/dev/<some-repo>`), run `node /home/jsagi/MindrianOS-Plugin/scripts/doctor.cjs --fix` and confirm NO `.mindrian/` is created in that dir (the live OBS-2 proof).
6. When satisfied, cut the release: `bash scripts/release.sh --prerelease` (this performs the 5-place lockstep, Step 6.6 gate, tag, and push per the script).
  </how-to-verify>
  <resume-signal>Type "approved" once the beta.4 release is cut (or describe any acceptance/lockstep failure to address).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workspace -> release pipeline | A release cut from the plugin cache dir silently diverges from GitHub; release.sh guards this |
| module registry -> release gate | A module with a missing runner or future introduced_version would ship a broken engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-13 | Tampering | shipping a module whose runner file is absent or whose introduced_version is in the future | mitigate | Step 6.6 verification: every runner exists + introduced_version <= NEW_VERSION, HARD ABORT (Task 1) |
| T-139-14 | Repudiation | version drift across the 5 lockstep places | mitigate | release.sh --prerelease drives all 5 places + tag; introduced_version reconciled to NEW_VERSION (Task 1) |
| T-139-15 | Elevation | cutting a release of release-infrastructure without external beta gating | mitigate | ships as 1.13.1-beta.4 (beta-first discipline); human checkpoint cuts the release (Task 2) |
| T-139-SC | Tampering | npm/pip/cargo installs | accept | no new package-manager installs in this phase (all deps are existing: semver, node:sqlite, node builtins); no Package Legitimacy Gate needed |
</threat_model>

<verification>
- `node tests/test-139-acceptance.cjs` green (full S1-S4 chain).
- `bash scripts/release.sh --prerelease --dry-run` reports NEW_VERSION 1.13.1-beta.4 and the Step 6.6 module verification.
- `node scripts/doctor.cjs --acceptance --pre-flight` passes.
- CHANGELOG.md top entry is 1.13.1-beta.4; doctor-modules.json umbilical introduced_version === 1.13.1-beta.4.
</verification>

<success_criteria>
- The hotfix ships as 1.13.1-beta.4 via the standard 5-place lockstep (never a lower 1.13.0.1).
- release.sh Step 6.6 verifies the module registry (runner exists + introduced_version <= NEW_VERSION) and passes doctor --acceptance.
- The full phase passes its acceptance test; the release is cut by the maintainer from the canonical workspace.
</success_criteria>

<output>
Create `.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-04-SUMMARY.md` when done.
</output>
