---
phase: 139-doctor-accumulative-engine-skeleton-and-context-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/resolve-umbilical-target.cjs
  - scripts/doctor.cjs
  - scripts/heal-command.cjs
  - tests/test-resolve-umbilical-target.cjs
  - tests/test-heal-obs2-regression.cjs
autonomous: true
requirements: [S1]
canon_parts: [8]

must_haves:
  truths:
    - "There is exactly ONE module that resolves doctor's target: lib/core/resolve-umbilical-target.cjs (precedence cord -> .room-root sentinel -> registry.active)"
    - "doctor.cjs::checkCascadeRoomsActive resolves its cwd-derived target through resolve-umbilical-target.cjs, not through a bare process.cwd() probe"
    - "Running heal from a non-room cwd (~/dev/<repo>) writes ZERO room artifacts (.mindrian/ + heal-log.json) to that target -- the room guards fire BEFORE any mkdir/write"
  artifacts:
    - path: "lib/core/resolve-umbilical-target.cjs"
      provides: "Single umbilical/room target resolver mirroring resolve-active-room.cjs"
      exports: ["resolveUmbilicalTarget"]
      min_lines: 60
    - path: "tests/test-resolve-umbilical-target.cjs"
      provides: "Resolver precedence + null-on-miss + no-network coverage"
    - path: "tests/test-heal-obs2-regression.cjs"
      provides: "OBS-2 regression: zero room-artifact writes from non-room cwd"
  key_links:
    - from: "scripts/doctor.cjs::checkCascadeRoomsActive"
      to: "lib/core/resolve-umbilical-target.cjs"
      via: "require + resolveUmbilicalTarget()"
      pattern: "resolve-umbilical-target"
    - from: "scripts/heal-command.cjs::runHeal"
      to: "assertIsRoom / isContainerDir guards"
      via: "guards run before safeMkdir/writeFileSync of .mindrian/heal-log.json"
      pattern: "assertIsRoom"
---

<objective>
Close the two WHERE defects (S1). (1) Stand up the SINGLE umbilical/room target resolver `lib/core/resolve-umbilical-target.cjs` (precedence: `.umbilical` cord -> `.room-root` sentinel -> registry.active) and route doctor's only cwd-derived target (`checkCascadeRoomsActive`) through it. (2) Close residual OBS-2 in `scripts/heal-command.cjs`: no `.mindrian/` or `heal-log.json` byte may land on a target that fails the room guards.

Purpose: doctor currently guesses its target from `process.cwd()` and scaffolds spurious room sections in plain code repos (the 2026-06-04 home-dir reorg incident). The codebase has been bitten TWICE by N-independent target guessers (active-plugin-root collapsed 3 copies; resolve-active-room collapsed 4). This resolver is ONE module from day one and is the foundation the engine skeleton (Plan 02) and the umbilical module (Plan 03) build on.

Output: the resolver module, the rerouted doctor cwd probe, the OBS-2 write-ordering fix, and two regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-CONTEXT.md
@.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-RESEARCH.md

<interfaces>
<!-- The resolver to MIRROR. resolve-active-room.cjs is the single-resolver precedent. -->
From lib/core/resolve-active-room.cjs (the pattern to mirror -- env-aware HOME default, null-on-miss, NEVER throws, CLI entry with 0/1 exit codes, sealed/archived honoring):
```
function resolveActiveRoom(opts) -> { slug, abs_path } | null
function resolveActiveRoomSlug() -> string | null
function resolveActiveRoomDir() -> string | null
module.exports = { resolveActiveRoom, resolveActiveRoomSlug, resolveActiveRoomDir };
// HOME default MUST be: o.home || process.env.MINDRIAN_ROOMS_HOME || path.join(process.env.HOME || process.env.USERPROFILE || os.homedir(), 'MindrianRooms')
// (FLAG-3 landmine: bare os.homedir() reads /etc/passwd on Linux and ignores process.env.HOME, breaking hermetic mkdtempSync fixtures)
```

From scripts/doctor.cjs (the cwd-misfire call site to reroute):
```
function checkCascadeRoomsActive(simulateWritePath)   // ~line 674
// ~line 686-690: probePath = simulateWritePath ? join(simulateWritePath,'__doctor_probe__') : join(process.cwd(),'__doctor_probe__')
//               writeRoomDir = findRoomRoot(probePath)   <- the bare cwd probe to replace
function readRegistry() -> { roomsHome, registry, registryPath } | null   // ~line 567
function findRoomRoot(filePath) -> roomDir | null   // bounded upward walk for .room-root sentinel
```

From scripts/heal-command.cjs (the OBS-2 write-ordering surface):
```
async function runHeal(roomDir, opts) -> heal-log envelope   // ~line 964
// Guards ALREADY run early (lines 984-1008): isContainerDir + assertIsRoom -> buildErrorEnvelope on failure.
// OBS-2 residual: confirm NO step writes .mindrian/ before those guards; the heal-log write (lines 1114-1121,
//   safeMkdir(.mindrian) + writeFileSync(heal-log.json)) already sits AFTER the guards and AFTER !dryRun.
function assertIsRoom(dir) -> { ok: bool, reason: string }   // ~line 889
function isContainerDir(dir) -> { container: bool, childRooms: string[] }   // ~line 915
function parseCliArgs(argv)   // ~line 1128: out.roomDir = registryActiveRoom || process.cwd()  <- cwd fallback
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the single umbilical/room target resolver</name>
  <files>lib/core/resolve-umbilical-target.cjs, tests/test-resolve-umbilical-target.cjs</files>
  <action>
Create `lib/core/resolve-umbilical-target.cjs` mirroring `lib/core/resolve-active-room.cjs` exactly in shape (per single_resolver LOCKED decision: ONE module, no second guesser). Export `resolveUmbilicalTarget(opts)` returning `{ slug, abs_path, source }` on hit (`source` is one of `'umbilical'` | `'sentinel'` | `'registry'`) or `null` on miss; NEVER throw (top-level try/catch returns null). Accept opts `{ cwd?, home? }` as test seams; default `cwd` to `process.cwd()` and `home` to the env-aware form `process.env.MINDRIAN_ROOMS_HOME || path.join(process.env.HOME || process.env.USERPROFILE || os.homedir(), 'MindrianRooms')` (FLAG-3 landmine -- do NOT use bare os.homedir()).

Precedence (first hit wins), per LOCKED umbilical_storage + the SCOUT-3 cord-as-doctor-map design:
  1. `.umbilical` cord: bounded upward walk from `cwd` (mirror findRoomRoot's bounded walk, max ~8 levels, stop at filesystem root or HOME). If a `.umbilical` marker file is found, parse its `room:` field (one or many; take the FIRST for target resolution), resolve that room slug against the registry to an abs_path, return `{ slug, abs_path, source: 'umbilical' }`. The marker is explicit declaration ONLY -- never guessed. If `.umbilical` exists but its `room:` does not resolve to a real registered room, fall through (do NOT fabricate a target).
  2. `.room-root` sentinel: bounded upward walk from `cwd` for a `.room-root` sentinel directory; if found, return `{ slug: basename, abs_path: roomDir, source: 'sentinel' }`.
  3. registry.active: read `<home>/.rooms/registry.json`, honor the `active` (current) / `active_room` (legacy) field and Array|Object `rooms` shape exactly as resolve-active-room.cjs does, honor sealed/archived (return null for those), final gate `fs.existsSync(abs_path)`. Return `{ slug, abs_path, source: 'registry' }`.
  4. not-found -> `null`.

Add a CLI entry (`if (require.main === module)`) that prints the JSON result and exits 0 on hit / 1 on miss, matching resolve-active-room.cjs.

Canon Part 8: read LOCAL files + env only; ZERO network surface (no fetch/http/require of brain client). Add a header comment asserting this invariant (the test greps for it).

Write `tests/test-resolve-umbilical-target.cjs` (plain `node:assert`, hermetic via mkdtempSync + MINDRIAN_ROOMS_HOME / cwd opts):
  - `.umbilical` cord present pointing at a registered room -> source==='umbilical', correct abs_path.
  - no cord, `.room-root` sentinel present above cwd -> source==='sentinel'.
  - neither cord nor sentinel, registry.active set -> source==='registry'.
  - non-room cwd, empty registry -> returns null (NOT a throw, NOT a fabricated path).
  - `.umbilical` with an unresolvable room: -> falls through (does not fabricate).
  - source-grep tripwire: read this module's own source, assert ZERO matches for /fetch\(|https?:|onrender|tavily|brain/ (Canon Part 8 no-egress floor).
  </action>
  <verify>
    <automated>node tests/test-resolve-umbilical-target.cjs && node -e "const{resolveUmbilicalTarget}=require('./lib/core/resolve-umbilical-target.cjs');console.log(typeof resolveUmbilicalTarget==='function'?'OK':'FAIL')"</automated>
  </verify>
  <done>resolveUmbilicalTarget exported; all precedence + null-on-miss + no-egress assertions pass; CLI entry prints JSON with 0/1 exit codes.</done>
</task>

<task type="auto">
  <name>Task 2: Route doctor's cwd probe through the resolver + close OBS-2 in heal-command</name>
  <files>scripts/doctor.cjs, scripts/heal-command.cjs, tests/test-heal-obs2-regression.cjs</files>
  <action>
**doctor.cjs**: at the top of the file add `const { resolveUmbilicalTarget } = require(path.join(__dirname, '..', 'lib', 'core', 'resolve-umbilical-target.cjs'));`. In `checkCascadeRoomsActive` (~line 674), replace the bare `process.cwd()` branch of the probe (~lines 686-690): when `simulateWritePath` is NOT provided, call `resolveUmbilicalTarget()` and use its `abs_path` as the directory to inspect (build `probePath = path.join(resolved.abs_path, '__doctor_probe__')` only when the resolver returns a hit; if it returns null, return `{ status: 'skip', detail: 'no umbilical/sentinel/registry target from cwd', activeRoom: ..., writeRoom: null }` -- doctor must NOT walk up from a raw cwd that is not a room). Keep the `simulateWritePath` branch unchanged (tests rely on it). Do NOT touch classes A/G/H/I (they target the install, not cwd).

**heal-command.cjs**: close residual OBS-2. The guards (isContainerDir + assertIsRoom, ~lines 984-1008) already run before the heal-log write (~lines 1114-1121). Audit the full runHeal path and the CLI path for ANY `.mindrian/` mkdir or `heal-log.json`/marker write that could fire on a target BEFORE assertIsRoom passes:
  - In `runHeal`: confirm step01..step11 are only reached AFTER the guard block; the guard block must `return buildErrorEnvelope(...)` (already does). If any step writes into `.mindrian/` and could run before the guards in any flag combination, move/gate it so guards are unconditionally first. The heal-log write at the end must stay AFTER `!dryRun` AND after the guards.
  - In `parseCliArgs` (~line 1170): the `out.roomDir = registryActiveRoom || process.cwd()` fallback is the entry point that aims heal at a non-room cwd. Leave the fallback (registry-first is correct) but ensure the resolved target still flows through runHeal's guards so a cwd fallback that is not a room is rejected with `error_not_a_room` and NO write. Per S1 the fix is the WRITE ordering, not removing the fallback.

Add NO new console writes to the rejected target. Soft-fail + additive only.

Write `tests/test-heal-obs2-regression.cjs` (hermetic, mkdtempSync): create a NON-room scratch dir simulating `~/dev/<repo>` (a `.git/` dir, a `package.json`, NO `.room-root`, NO ROOM.md). Call `runHeal(scratchDir, { silent: true })`. Assert: (a) return envelope error is `error_not_a_room` (or `error_container_dir`); (b) `fs.existsSync(path.join(scratchDir,'.mindrian'))` is FALSE; (c) `fs.existsSync(path.join(scratchDir,'.mindrian','heal-log.json'))` is FALSE; (d) `fs.existsSync(path.join(scratchDir,'.heal-backup'))` is FALSE. ZERO room-artifact writes is the OBS-2 floor.
  </action>
  <verify>
    <automated>node tests/test-heal-obs2-regression.cjs && node scripts/doctor.cjs --json 2>/dev/null | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{try{JSON.parse(s);console.log('doctor --json still parses')}catch(e){process.exit(1)}})"</automated>
  </verify>
  <done>doctor cwd probe resolves via resolve-umbilical-target.cjs (skips cleanly when no target); heal from a non-room cwd produces error_not_a_room with ZERO .mindrian/.heal-backup/heal-log.json writes; doctor --json still emits valid JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cwd -> doctor target | A non-room working directory is untrusted input; doctor must not scaffold into it |
| `.umbilical` marker -> registry | The marker's `room:` field is declared input; it must resolve to a real registered room, never fabricate one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-01 | Tampering | doctor cwd probe scaffolding into a code repo | mitigate | route cwd target through resolveUmbilicalTarget(); skip when null (Task 2) |
| T-139-02 | Tampering | heal-command writing .mindrian/heal-log.json before room validation (OBS-2) | mitigate | assertIsRoom/isContainerDir guards run before any write; regression test asserts zero artifacts (Task 2) |
| T-139-03 | Information disclosure | resolver reaching the Brain / network | accept->mitigate | resolver is LOCAL files + env only; source-grep tripwire asserts no fetch/http (Task 1); Canon Part 8 |
| T-139-04 | Spoofing | `.umbilical` room: pointing at a non-existent room to misdirect | mitigate | unresolvable room: falls through, never fabricates a target (Task 1) |
</threat_model>

<verification>
- `node tests/test-resolve-umbilical-target.cjs` green (precedence + null-on-miss + no-egress).
- `node tests/test-heal-obs2-regression.cjs` green (zero room-artifact writes from non-room cwd).
- `grep -rn "resolve-umbilical-target" scripts/doctor.cjs` shows the require + call wired.
- `node scripts/doctor.cjs --json` still emits valid JSON (no regression).
</verification>

<success_criteria>
- Exactly one resolver module exists for doctor's cwd target (no second guesser introduced).
- Running heal/doctor from a non-room dir makes ZERO room-artifact writes there (OBS-2 closed).
- The resolver has zero network surface (Canon Part 8 LOCAL-only).
</success_criteria>

<output>
Create `.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-01-SUMMARY.md` when done.
</output>
