---
phase: 139-doctor-accumulative-engine-skeleton-and-context-fix
plan: 02
type: execute
wave: 2
depends_on: ["139-01"]
files_modified:
  - data/doctor-modules.json
  - lib/core/migration-snapshot.cjs
  - scripts/doctor.cjs
  - tests/test-doctor-module-selector.cjs
autonomous: true
requirements: [S2]
canon_parts: [6, 7, 8]

must_haves:
  truths:
    - "data/doctor-modules.json is a hand-maintained module registry (mirrors data/deployment-surfaces.json): each entry carries {id, introduced_version, fix_supported}"
    - "doctor keeps its OWN persisted applied-through watermark at ~/.mindrian/doctor-applied.json (NOT ~/.mindrian-last-version, which session-start overwrites unconditionally and early)"
    - "the selector runs each registered module whose introduced_version is in (applied_through, running] -- modules predating an install are skipped; re-running is a no-op (idempotent)"
  artifacts:
    - path: "data/doctor-modules.json"
      provides: "Module registry skeleton (mirrors deployment-surfaces.json manifest pattern)"
      contains: "schema_version"
    - path: "lib/core/migration-snapshot.cjs"
      provides: "Extended with doctor-applied watermark read/write helpers"
      exports: ["readDoctorApplied", "writeDoctorApplied"]
    - path: "scripts/doctor.cjs"
      provides: "Semver module selector loop gated by introduced_version in (applied_through, running]"
    - path: "tests/test-doctor-module-selector.cjs"
      provides: "Selector window + idempotency + future-version-defer coverage"
  key_links:
    - from: "scripts/doctor.cjs selector"
      to: "data/doctor-modules.json"
      via: "require/read the module registry"
      pattern: "doctor-modules"
    - from: "scripts/doctor.cjs selector"
      to: "~/.mindrian/doctor-applied.json"
      via: "readDoctorApplied / writeDoctorApplied watermark"
      pattern: "doctor-applied"
---

<objective>
Build the accumulative engine SKELETON (S2): the version-dispatch substrate that converts doctor's frozen Phase-95 class roster into a forward-healing engine. Three pieces, generalizing the proven `install-state.cjs::migrateIfNeeded` semver dispatch + `data/deployment-surfaces.json` manifest patterns (Canon Part 7 reuse-before-build, per LOCKED accumulative_engine decision):

1. `data/doctor-modules.json` -- a hand-maintained module registry (one entry per organ module: `{id, introduced_version, fix_supported, ...}`), mirroring the deployment-surfaces manifest ("new module = one entry, no code change").
2. `~/.mindrian/doctor-applied.json` -- doctor's OWN persisted applied-through watermark, via extended `lib/core/migration-snapshot.cjs`. NOT `~/.mindrian-last-version` (session-start overwrites it unconditionally and early -- Phase 123 D-03, the watermark_not_lastversion LOCKED decision).
3. a semver selector loop inside `doctor.cjs` that runs each registered module where `introduced_version` is in `(applied_through, running]`, idempotently.

Purpose: from this release forward, every new Mindrian version registers its own health/migration module and any user on any prior version is healed forward, idempotently, any time they run doctor. This is the 2026-04-13 drift-discipline incident applied to doctor itself (Canon Part 6 dog-fooding).

Output: the registry skeleton (seeded EMPTY of organ modules -- Plan 03 adds module #1), the watermark helpers + ledger, the selector loop, and a selector test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-CONTEXT.md
@.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-RESEARCH.md
@data/deployment-surfaces.json

<interfaces>
<!-- The manifest pattern to MIRROR: data/deployment-surfaces.json is hand-maintained, has
     $schema_note + schema_version + phase + canon_parts + a surfaces[] array of {id,...} entries. -->

From lib/core/migration-snapshot.cjs (the module to EXTEND -- add the watermark helpers alongside the existing exports; do NOT rewrite the file):
```
function snapshotPath(homeDir, isoTimestamp) -> <home>/.mindrian/pre-migration-snapshots/<safe>.json
function readMigrationsLog(homeDir) -> rows[]
function appendMigrationLog(homeDir, record)
function isAlreadyMigrated(homeDir, sourceName, fingerprint) -> bool
module.exports = { fingerprintEntry, snapshotPath, readMigrationsLog, appendMigrationLog, isAlreadyMigrated, _scanForRawIdentifiers };
// HOME convention used across these helpers: homeDir is passed in; default at call sites is the env-aware form.
```

From scripts/doctor.cjs (the host for the selector; semver already a dependency):
```
const semver = require('semver');                       // ~line 39 -- already imported
function parseVersion(v)                                 // ~line 296
function checkInstallVersion() -> { status, version, parsed, ... }   // ~line 335 -- reads the ACTIVE plugin.json version (running version of record)
const { resolveActivePluginRoot } = require('.../lib/core/active-plugin-root.cjs');  // ~line 59
// main dispatch builds report.checks[...] keyed by class; selector adds report.modules (or report.checks['accumulative-engine'])
// HOME constant exists at top of file (process.env.HOME || ... ); MINDRIAN_ROOMS_HOME pattern used in readRegistry.
```

The generalization target (read for the dispatch shape, do NOT modify):
- lib/core/install-state.cjs::migrateIfNeeded -- a `===`/semver dispatch table with future-version-DEFER + additive idempotency. The selector mirrors this shape: iterate registry, run modules in the (applied_through, running] window, never run a future-version module, never re-run an already-applied one.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed data/doctor-modules.json + extend migration-snapshot with the watermark ledger</name>
  <files>data/doctor-modules.json, lib/core/migration-snapshot.cjs, tests/test-doctor-module-selector.cjs</files>
  <action>
**data/doctor-modules.json**: create the hand-maintained module registry, mirroring `data/deployment-surfaces.json` field-for-field in style. Top-level keys: `$schema_note` (explain: hand-maintained, NOT generated; "new module = one entry, no code change"; gated by introduced_version <= running so old installs are never faulted for organs they predate; document the entry shape), `schema_version: 1`, `phase: "139-doctor-accumulative-engine-skeleton-and-context-fix"`, `canon_parts: [6, 7, 8]`, and `modules: []`. SEED THE modules ARRAY EMPTY -- Plan 03 registers module #1 (umbilical). Document the per-entry contract in `$schema_note`: `{ id, introduced_version, fix_supported, runner, description }` where `runner` is a `lib/core/doctor/<id>.cjs`-style relative path resolved at run time. Do NOT invent organ modules beyond umbilical (out_of_scope LOCKED decision: the 15 SCOUT-2 modules backfill in v1.13.1+).

**lib/core/migration-snapshot.cjs**: EXTEND (do not rewrite). Add two helpers and export them alongside the existing ones:
  - `readDoctorApplied(homeDir)` -> reads `<homeDir>/.mindrian/doctor-applied.json`; returns `{ applied_through: <semver-string|null>, history: [...] }`; returns `{ applied_through: null, history: [] }` when the file is absent or malformed (soft-fail, never throw). homeDir default at the call site is the env-aware HOME form.
  - `writeDoctorApplied(homeDir, { appliedThrough, ranModuleIds })` -> atomically writes (tmp + rename) `<homeDir>/.mindrian/doctor-applied.json` advancing `applied_through` to `appliedThrough` and appending a history record `{ at: ISO, applied_through, modules: ranModuleIds }`. Additive/never-delete: never lowers `applied_through` (if the new value is < existing, keep existing -- a hotfix cannot regress the watermark, mirroring the version_note "cannot carry a LOWER semver" constraint). mkdir -p the `.mindrian` dir. Soft-fail on write error (best-effort, return false), never throw.

Add `readDoctorApplied` + `writeDoctorApplied` to `module.exports`.

Start the test file `tests/test-doctor-module-selector.cjs` (node:assert, hermetic mkdtempSync HOME). Cover the watermark helpers here: absent ledger -> `{applied_through:null}`; write then read round-trips; write with a LOWER appliedThrough does NOT regress the stored value. (The selector-window assertions land in Task 2's additions to this same file.)
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('data/doctor-modules.json','utf8'))" && node -e "const m=require('./lib/core/migration-snapshot.cjs');console.log(typeof m.readDoctorApplied==='function'&&typeof m.writeDoctorApplied==='function'?'OK':'FAIL')" && node tests/test-doctor-module-selector.cjs</automated>
  </verify>
  <done>doctor-modules.json parses with schema_version + empty modules[]; readDoctorApplied/writeDoctorApplied exported; watermark round-trips and never regresses; existing migration-snapshot exports unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add the semver module selector loop to doctor.cjs</name>
  <files>scripts/doctor.cjs, tests/test-doctor-module-selector.cjs</files>
  <action>
In `scripts/doctor.cjs`, add the accumulative-engine selector. At the top, require the registry path (`path.join(__dirname,'..','data','doctor-modules.json')`) and `const { readDoctorApplied, writeDoctorApplied } = require(path.join(__dirname,'..','lib','core','migration-snapshot.cjs'));`.

Add a function `runAccumulativeEngine(flags)`:
  1. Resolve the RUNNING version of record via `checkInstallVersion()` (returns `.version` from the active plugin.json). If it can't be resolved, return `{ status: 'skip', detail: 'running version unresolved' }` (soft-fail).
  2. Read the registry JSON (soft-fail to `{ modules: [] }` on parse error).
  3. Read the watermark: `const wm = readDoctorApplied(HOME_DIR)` -> `applied_through` (null means "first run; window is (null, running] = everything <= running").
  4. SELECT modules where `introduced_version` is in the window `(applied_through, running]`: i.e. `semver.gt(introduced, applied_through||'0.0.0') && semver.lte(introduced, running)`. Coerce versions through `semver.coerce` so pre-release labels (e.g. `1.13.1-beta.4`) compare correctly; a FUTURE-version module (`introduced > running`) is DEFERRED (never run) -- the exact future-version-DEFER shape from install-state.migrateIfNeeded.
  5. For each selected module, require its `runner` (resolved relative to the plugin root) and invoke its exported check/fix entry. In THIS plan the registry is empty, so the loop runs zero modules -- but the loop, window math, and runner-dispatch wiring MUST be complete so Plan 03 only adds a registry entry + a runner file, no engine code. Wrap each runner invocation in try/catch (one bad module must not crash doctor -- soft-fail, record an error finding).
  6. Idempotency: only ADVANCE the watermark via `writeDoctorApplied(HOME_DIR, { appliedThrough: running, ranModuleIds })` when at least one module ran OR the window was non-empty and all ran clean; re-running with the watermark already at `running` selects ZERO modules (no-op). Respect `--fix` (run module fixers) vs read-only (run module checks only). Honor `--dry-run` if present: select + report but do NOT advance the watermark and do NOT run fixers.
  7. Attach the result to `report.checks['accumulative-engine']` (or `report.modules`) so `--json` surfaces it.

Wire `runAccumulativeEngine(flags)` into main so it runs under `--all` and under `--acceptance` (and standalone is fine). Do NOT gate it behind a class A-M flag -- it is the engine, not a class.

Extend `tests/test-doctor-module-selector.cjs` with selector-window coverage using a SYNTHETIC in-test registry + watermark (hermetic mkdtempSync HOME; inject the registry via a fixture file or a test-only opts seam on runAccumulativeEngine -- prefer exporting runAccumulativeEngine and passing `{ registry, running, home }` opts so the test does not depend on the live empty registry):
  - window selection: modules with introduced in (applied_through, running] are selected; introduced <= applied_through skipped; introduced > running DEFERRED.
  - idempotency: run once advances watermark to running; immediate re-run selects ZERO (no-op).
  - pre-release compare: introduced `1.13.1-beta.4` with running `1.13.1-beta.4` is IN the window (lte), with running `1.13.1-beta.3` is DEFERRED.
  - soft-fail: a runner that throws records an error finding and does NOT crash the loop.
  </action>
  <verify>
    <automated>node tests/test-doctor-module-selector.cjs && node scripts/doctor.cjs --json 2>/dev/null | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);process.exit(j.checks&&(j.checks['accumulative-engine']||j.modules)?0:0)})" && echo "doctor --json intact"</automated>
  </verify>
  <done>runAccumulativeEngine selects modules in (applied_through, running], DEFERS future versions, is idempotent (re-run = no-op), soft-fails on a bad runner, advances the watermark only on clean non-empty runs; with an empty registry it runs zero modules and doctor --json stays valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| running-version -> selector window | A mis-read running version could run future modules against an old install |
| watermark ledger -> selector | A regressed/forged watermark could re-run or skip modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-05 | Tampering | running an organ module against an install that predates it | mitigate | window math gates every module by introduced_version <= running; future-version DEFER (Task 2) |
| T-139-06 | Elevation/Tampering | watermark depending on ~/.mindrian-last-version (overwritten early by session-start) | mitigate | doctor keeps its OWN doctor-applied.json ledger (watermark_not_lastversion); never lowers applied_through (Task 1) |
| T-139-07 | Denial of service | one bad module runner crashing the whole engine | mitigate | per-runner try/catch soft-fail; records error finding, loop continues (Task 2) |
| T-139-08 | Information disclosure | selector reaching network | accept | selector is LOCAL file reads only; runs no Brain query; Canon Part 8 (no egress added) |
</threat_model>

<verification>
- `node tests/test-doctor-module-selector.cjs` green (window + idempotency + pre-release + soft-fail).
- `data/doctor-modules.json` parses; `modules: []` (no organ modules yet).
- `grep -n "doctor-applied\|doctor-modules\|runAccumulativeEngine" scripts/doctor.cjs` shows the wiring.
- `node scripts/doctor.cjs --json` emits valid JSON with the accumulative-engine surface.
</verification>

<success_criteria>
- The engine runs modules ONLY in the (applied_through, running] window; re-running is a no-op (idempotent).
- Doctor keeps its own watermark; it never depends on ~/.mindrian-last-version for the from->to delta.
- The registry + watermark + selector are reuse of install-state.migrateIfNeeded + deployment-surfaces.json patterns, not a new framework (Canon Part 7).
</success_criteria>

<output>
Create `.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-02-SUMMARY.md` when done.
</output>
