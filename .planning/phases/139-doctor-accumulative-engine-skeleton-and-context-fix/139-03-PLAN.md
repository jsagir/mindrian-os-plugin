---
phase: 139-doctor-accumulative-engine-skeleton-and-context-fix
plan: 03
type: execute
wave: 3
depends_on: ["139-01", "139-02"]
files_modified:
  - lib/core/navigation/edges.cjs
  - lib/core/doctor/umbilical-module.cjs
  - data/doctor-modules.json
  - tests/test-edges-affiliated-with-floor.cjs
  - tests/test-umbilical-module.cjs
  - tests/test-umbilical-part8-audit.cjs
autonomous: true
requirements: [S3]
canon_parts: [4, 8]

must_haves:
  truths:
    - "AFFILIATED_WITH is a member of ALLOWED_EDGE_TYPES in lib/core/navigation/edges.cjs (additive; the floor test still asserts the prior members present)"
    - "A .umbilical marker (room:/relation:/born:/note:) in a project yields exactly one AFFILIATED_WITH edge in the corresponding room.db via edges.cjs::writeEdge"
    - "The integrity check detects a cord pointing at a non-existent room (orphan) and a removed marker; --fix may SUGGEST cords for human confirmation but NEVER auto-creates one"
    - "Canon Part 8 holds: no raw cross-room edge is written, and zero Brain egress -- the cord is projected as a LOCAL edge in the room's own room.db only"
  artifacts:
    - path: "lib/core/navigation/edges.cjs"
      provides: "AFFILIATED_WITH added to ALLOWED_EDGE_TYPES (additive)"
      contains: "AFFILIATED_WITH"
    - path: "lib/core/doctor/umbilical-module.cjs"
      provides: "Umbilical doctor module: marker read + edge projection + integrity check"
      exports: ["check", "fix"]
      min_lines: 60
    - path: "data/doctor-modules.json"
      provides: "Umbilical registered as module #1"
      contains: "umbilical"
  key_links:
    - from: "lib/core/doctor/umbilical-module.cjs"
      to: "lib/core/navigation/edges.cjs::writeEdge"
      via: "writeEdge(db, {edge_type:'AFFILIATED_WITH'})"
      pattern: "AFFILIATED_WITH"
    - from: "lib/core/doctor/umbilical-module.cjs"
      to: "lib/core/resolve-umbilical-target.cjs"
      via: "resolve the cord's room: to a real room dir"
      pattern: "resolve-umbilical-target"
    - from: "data/doctor-modules.json modules[]"
      to: "lib/core/doctor/umbilical-module.cjs"
      via: "runner path entry"
      pattern: "umbilical-module"
---

<objective>
Ship Umbilical Cord as the FIRST registered accumulative-engine module (S3), proving the Plan-02 skeleton end-to-end while doubling as the map that fixes doctor's broken context model. Minimal, per the scope_slice LOCKED decision -- marker read + edge projection + an integrity check, nothing more:

1. Add `AFFILIATED_WITH` to `ALLOWED_EDGE_TYPES` in `lib/core/navigation/edges.cjs` (additive one-line change; the floor test asserts a floor, not an exact size).
2. New `lib/core/doctor/umbilical-module.cjs`: read the `.umbilical` marker (`room:` one-or-many, `relation:` code|deck|research|deploy|doc|data, `born:`, `note:`); project an `AFFILIATED_WITH` edge into the corresponding `room.db` via `edges.cjs::writeEdge`; run an integrity check (cord points at a real room; orphan detection; removed-marker detection).
3. Register umbilical as module #1 in `data/doctor-modules.json` (the registry seeded empty in Plan 02 now gets its first entry).

Cords are authoritative at the REGISTRY layer (cross-tree) and are PROJECTED into each room.db as LOCAL `AFFILIATED_WITH` edges -- Canon Part 8 forbids raw cross-room edges and zero Brain egress (umbilical_storage LOCKED decision). `--fix` may SUGGEST candidate cords for human confirmation; it NEVER auto-creates a cord into existence (marker_format LOCKED decision: explicit declaration only, doctor never guesses).

Output: the additive edge type, the umbilical module, the registry entry, and three tests (floor, projection, Part 8 audit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-CONTEXT.md
@.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-RESEARCH.md
@.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-01-SUMMARY.md
@.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-02-SUMMARY.md

<interfaces>
<!-- The edge-write chokepoint + the resolver from Plan 01 + the registry/runner contract from Plan 02. -->

From lib/core/navigation/edges.cjs (the chokepoint; AFFILIATED_WITH gets added to the frozen Set):
```
const ALLOWED_EDGE_TYPES = Object.freeze(new Set([ 'DEFERRED','REJECTED','DERIVED_FROM',
  'FILED_AS_DECISION','FOLLOWS_FROM','OPERATOR_TRANSITION','INFORMS','REJECTED_BECAUSE',
  'CONTRADICTS','SUPERSEDES' ]));   // ADD 'AFFILIATED_WITH' additively
function writeEdge(db, params) -> { ok, edge_id, type, source, target } | { ok:false, reason }
//   params = { source_id, target_id, edge_type, properties }
//   UPSERT into edges (source,target,type,properties) ON CONFLICT(source,target,type) DO UPDATE
//   properties is JSON.stringified; ENUM-ONLY scalars per Canon Part 8 (NO freeform user content on the edge)
module.exports = { ALLOWED_EDGE_TYPES, writeEdge };
```

From lib/core/resolve-umbilical-target.cjs (Plan 01 -- resolve the cord's room: to a real room dir):
```
function resolveUmbilicalTarget(opts) -> { slug, abs_path, source } | null   // opts: { cwd?, home? }
```

From data/doctor-modules.json (Plan 02 -- the registry; modules[] currently empty; per-entry shape from $schema_note):
```
{ id, introduced_version, fix_supported, runner, description }   // runner = relative path to a lib/core/doctor/<id>.cjs module exporting check()/fix()
```

Runner contract the accumulative selector (Plan 02 runAccumulativeEngine) calls:
```
// lib/core/doctor/umbilical-module.cjs MUST export:
function check(ctx) -> { status, findings: [...], detail }      // read-only integrity check
function fix(ctx) -> { status, projected: N, suggested: [...], detail }   // projects edges; SUGGESTS (never auto-creates) cords
// ctx carries { home, running, dryRun } from the selector; module opens room.db via the project's navigation openRoomDb path.
```

room.db open path: the per-room SQLite graph lives at `<room_abs_path>/.mindrian/room.db`. Open it with the same node:sqlite handle the navigation layer uses, pass the handle to writeEdge(db, ...). Do NOT open Brain; do NOT open a SECOND room's db to write a cross-room edge.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AFFILIATED_WITH to ALLOWED_EDGE_TYPES (additive) + floor test</name>
  <files>lib/core/navigation/edges.cjs, tests/test-edges-affiliated-with-floor.cjs</files>
  <behavior>
    - ALLOWED_EDGE_TYPES.has('AFFILIATED_WITH') === true after the change.
    - The prior members (DEFERRED, REJECTED, DERIVED_FROM, INFORMS, CONTRADICTS, SUPERSEDES, ...) are ALL still present (floor assertion -- additive, never-delete).
    - writeEdge(db, {edge_type:'AFFILIATED_WITH', source_id, target_id}) returns ok:true (no longer invalid_edge_type).
  </behavior>
  <action>
Add `'AFFILIATED_WITH'` as a member of the frozen `ALLOWED_EDGE_TYPES` Set in `lib/core/navigation/edges.cjs`, following the existing additive-block comment idiom verbatim (a short comment block citing Phase 139 umbilical_storage LOCKED decision: cords projected into room.db as AFFILIATED_WITH edges; Canon Part 8 -- LOCAL edge, ENUM-ONLY properties, never crosses to Brain; mirrors the Phase 131-01 CONTRADICTS/SUPERSEDES additive idiom). Do NOT remove or reorder any existing member.

Write `tests/test-edges-affiliated-with-floor.cjs`: assert `ALLOWED_EDGE_TYPES.has('AFFILIATED_WITH')`; assert the FLOOR set (DEFERRED, REJECTED, DERIVED_FROM, FILED_AS_DECISION, FOLLOWS_FROM, OPERATOR_TRANSITION, INFORMS, REJECTED_BECAUSE, CONTRADICTS, SUPERSEDES) are all still present; assert ALLOWED_EDGE_TYPES is a frozen Set instance. (Open an in-memory/temp room.db and assert writeEdge with AFFILIATED_WITH returns ok:true only if a node:sqlite handle is trivially constructable here; otherwise leave the live-write assertion to Task 2's projection test.)
  </action>
  <verify>
    <automated>node tests/test-edges-affiliated-with-floor.cjs</automated>
  </verify>
  <done>AFFILIATED_WITH is in ALLOWED_EDGE_TYPES; all prior members preserved (floor green); Set still frozen.</done>
</task>

<task type="auto">
  <name>Task 2: Build the umbilical doctor module (marker read + edge projection + integrity) and register it</name>
  <files>lib/core/doctor/umbilical-module.cjs, data/doctor-modules.json, tests/test-umbilical-module.cjs, tests/test-umbilical-part8-audit.cjs</files>
  <action>
**lib/core/doctor/umbilical-module.cjs**: create the module exporting `check(ctx)` and `fix(ctx)` per the runner contract.

Marker format (marker_format LOCKED decision): a `.umbilical` file in the non-room project. Parse fields `room:` (one or many -- accept a scalar or a YAML-ish list), `relation:` (one of code|deck|research|deploy|doc|data -- validate against this closed set; unknown relation -> finding, skip), `born:` (ISO date), `note:` (freeform -- stays LOCAL, NEVER goes on the edge per Part 8 ENUM-ONLY). Use a small hand-rolled parser (no new dependency; mirror the existing CJS no-framework convention).

`check(ctx)` (read-only integrity check): discover `.umbilical` markers (for the skeleton, scope discovery to the markers reachable from ctx-provided candidate dirs or a bounded scan -- keep minimal). For each marker: resolve each `room:` to a real registered room via `resolveUmbilicalTarget`/registry; emit findings:
  - `orphan_cord`: a `.umbilical` room: that does not resolve to a real registered room.
  - `removed_marker`: an AFFILIATED_WITH edge exists in a room.db whose originating `.umbilical` marker is gone (cord<->marker bidirectionality broken).
  - `unprojected_cord`: a valid marker whose AFFILIATED_WITH edge is not yet in the target room.db.
  Return `{ status: 'ok'|'warn', findings, detail }`. NEVER mutate in check().

`fix(ctx)` (projection + suggestion): for each valid marker with an `unprojected_cord` finding, open the TARGET room's `<room_abs_path>/.mindrian/room.db` and project EXACTLY ONE `AFFILIATED_WITH` edge via `edges.cjs::writeEdge(db, { source_id: '<project-handle>', target_id: '<room-slug>', edge_type: 'AFFILIATED_WITH', properties: { relation: <enum>, born: <iso> } })`. properties are ENUM/scalar ONLY -- NEVER include `note:` or any freeform user content (Canon Part 8). The edge is written into the TARGET room's OWN room.db only -- never a raw cross-room edge, never a second room's db. For `orphan_cord` findings, `--fix` may add the candidate to a SUGGESTED list returned in the envelope for human confirmation -- it MUST NOT auto-create a cord/marker. Honor `ctx.dryRun`: report what would be projected, write nothing. Soft-fail per marker (one bad marker does not abort the rest). Return `{ status, projected: <count>, suggested: [...], detail }`.

Open room.db with the node:sqlite handle the navigation layer uses; close it after the write. Zero network surface.

**data/doctor-modules.json**: register umbilical as module #1 -- append to `modules[]`:
  `{ "id": "umbilical", "introduced_version": "<the version this hotfix ships -- match Plan 04's NEW_VERSION, recommend 1.13.1-beta.4>", "fix_supported": true, "runner": "lib/core/doctor/umbilical-module.cjs", "description": "Project .umbilical cords into room.db as AFFILIATED_WITH edges; integrity-check cord<->marker bidirectionality." }`. (Plan 04 confirms the final version label; if it differs, that plan updates this introduced_version in lockstep.)

**tests/test-umbilical-module.cjs** (hermetic mkdtempSync; build a fake registry with one room that has a real `.mindrian/room.db`, and a separate non-room project dir holding a `.umbilical` marker pointing at that room):
  - marker parse: valid fields parsed; unknown `relation:` produces a finding and is skipped.
  - projection: `fix` writes EXACTLY ONE AFFILIATED_WITH edge into the target room.db (query the edges table; assert count for that source/target/type === 1); re-running `fix` is idempotent (UPSERT -> still exactly one edge).
  - orphan detection: a marker whose room: does not resolve produces an `orphan_cord` finding; `fix` adds it to `suggested[]` and does NOT create a marker/cord and does NOT write an edge.
  - removed-marker detection: an AFFILIATED_WITH edge present but the marker deleted -> `check` reports `removed_marker`.
  - dryRun: `fix({dryRun:true})` writes zero edges.

**tests/test-umbilical-part8-audit.cjs** (the Canon Part 8 audit):
  - source-grep tripwire: read umbilical-module.cjs source, assert ZERO matches for /fetch\(|https?:|onrender|tavily|brain/i (no Brain egress).
  - projection audit: after `fix`, assert the projected edge's properties JSON contains ONLY enum/scalar fields (`relation`, `born`) and does NOT contain the marker's `note:` text (no freeform user content on the edge).
  - cross-room audit: assert the edge landed in the TARGET room's room.db ONLY and no edge was written into any OTHER room's db (no raw cross-room edge).
  </action>
  <verify>
    <automated>node tests/test-umbilical-module.cjs && node tests/test-umbilical-part8-audit.cjs && node -e "const r=JSON.parse(require('fs').readFileSync('data/doctor-modules.json','utf8'));process.exit(r.modules.some(m=>m.id==='umbilical')?0:1)"</automated>
  </verify>
  <done>.umbilical marker yields exactly one AFFILIATED_WITH edge in the target room.db (idempotent); orphan + removed-marker detected; --fix SUGGESTS but never auto-creates cords; properties are ENUM-only (no note: on the edge); zero Brain egress; umbilical registered as module #1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.umbilical` marker -> room.db edge | Marker fields are declared user input; only enum/scalar fields may land on the edge |
| project dir (cross-tree) -> room.db | A cord crosses trees at the REGISTRY layer but must project as a LOCAL edge only |
| room.db -> Brain | The graph boundary: no edge or query may carry user content to the Brain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-09 | Information disclosure | marker `note:` (freeform) landing on the edge / reaching Brain | mitigate | properties ENUM-only (relation, born); note: stays LOCAL; Part 8 audit asserts no note on edge + no egress grep (Task 2) |
| T-139-10 | Tampering | doctor auto-creating a cord into existence | mitigate | marker_format: explicit declaration only; --fix SUGGESTS to suggested[], never writes a marker (Task 2) |
| T-139-11 | Tampering | raw cross-room edge written into a second room's db | mitigate | edge projected into the TARGET room's own room.db only; cross-room audit asserts no other-db write (Task 2) |
| T-139-12 | Spoofing | orphan cord pointing at a non-existent room | mitigate | orphan_cord finding; resolver returns null -> no edge written (Task 2) |
</threat_model>

<verification>
- `node tests/test-edges-affiliated-with-floor.cjs` green (additive floor).
- `node tests/test-umbilical-module.cjs` green (projection + idempotency + orphan + removed-marker + dryRun).
- `node tests/test-umbilical-part8-audit.cjs` green (no egress, enum-only props, no cross-room edge).
- `data/doctor-modules.json` modules[] contains the umbilical entry.
</verification>

<success_criteria>
- A `.umbilical` marker projects exactly one AFFILIATED_WITH edge into the right room.db; removing the marker is detected.
- Canon Part 8 holds: no cross-room edge, no Brain egress, no freeform user content on the edge.
- `--fix` suggests orphan cords for confirmation but never auto-creates one.
- Umbilical is registered as accumulative-engine module #1 (proves the Plan-02 skeleton end-to-end with zero engine-code changes).
</success_criteria>

<output>
Create `.planning/phases/139-doctor-accumulative-engine-skeleton-and-context-fix/139-03-SUMMARY.md` when done.
</output>
