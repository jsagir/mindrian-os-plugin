---
phase: 246-live-verification-graph-census
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [LOOP-01]
subsystem: brain-verification
tags: [brain, loop, live-verification, hooks, beta13]
requires: []
provides:
  - loop01-live-verification-of-record
affects: [phase-247, phase-248, phase-250]
canon_parts: [8]

must_haves:
  truths:
    - "A FRESH session on plugin cache 1.16.0-beta.13 or later passes all three plugin-scope Brain calls: brain_stats, brain_search 'jobs to be done framework', brain_ask synthesis"
    - "Any failure is reported VERBATIM in-turn and decoded against the five-signature table; never summarized, never silent, never marked passed from a stale session"
    - "The verification runs on the plugin-scope tool names (mcp__plugin_mos_mindrian-brain__*), never the pws-brain-mcp project scope"
  artifacts:
    - path: ".planning/phases/246-live-verification-graph-census/246-01-SUMMARY.md"
      provides: "The verbatim three-call results, pass/fail per call, plugin version verified, and any decoded failure signature"
  key_links:
    - from: "checkpoint failure signature"
      to: ".planning/debug/<slug>.md"
      via: "RCA filing per docs/RCA-TEMPLATE.md on any NEW FAILURE (git add -f)"
    - from: "246-01-SUMMARY.md verification-of-record"
      to: "Phase 247/248 baselines"
      via: "downstream phases cite this pass instead of re-proving the hook path"
---

<objective>
Prove the beta.13 Brain path live, end to end, from a FRESH session: host -> plugin
PreToolUse egress guard -> MCP call -> PostToolUse sanitize envelope -> brain-client ->
Render edge -> Memgraph. This is LOOP-01 and the only verification that counts: the
2026-08-10 session reproduced BOTH fixed defects live purely because it ran on a stale
cache, and hooks are read only at session start.

Purpose: everything downstream in v2.0.0 (Phases 247, 248, 250) builds on a Brain path
that is PROVEN working, not assumed working. The standing rule binds: a fix is not live
until released AND picked up (memory: feedback_dev_repo_fix_not_live_until_released).
Release shipped (v1.16.0-beta.13); disk pickup verified 2026-08-10T07:43Z; SESSION pickup
requires a restart, which is why this plan is a human checkpoint by construction.

Output: a signed-off fresh-session three-call pass recorded verbatim in the plan SUMMARY
(the LOOP-01 verification of record), or a decoded failure filed as an RCA.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/246-live-verification-graph-census/246-RESEARCH.md
@docs/2026-08-09-HANDOFF-brain-envelope-and-egress-guard.md
@lib/core/resolve-brain-key.cjs
@tests/run-all-245.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-flight (cache version, key, health, envelope fence)</name>
  <files>none - read-only preflight; results go to the SUMMARY</files>
  <action>
    Run the four pre-checks that CAN be automated from the current session (they inspect
    disk, env, and network state, all session-independent - only the three calls
    themselves need the fresh session). Per the planning constraint that LOOP-01 is a
    human checkpoint, this task automates everything BEFORE the checkpoint so the
    operator's manual step is minimal.

    Pre-check 1, installed plugin cache is beta.13 or later: read
    $HOME/.claude/plugins/installed_plugins.json (via node or python3, do not hand-parse
    with grep) and extract the entry for "mos@mindrian-marketplace". Expected: version
    1.16.0-beta.13 or newer, installPath ending in /mos/<that version>. If the version is
    OLDER than 1.16.0-beta.13, STOP the plan and report: the operator must run
    "/plugin marketplace update" then "claude plugin update mos@mindrian-marketplace"
    before the checkpoint can mean anything.

    Pre-check 2, key resolves: run node -e requiring
    /home/jsagi/dev/MindrianOS-Plugin/lib/core/resolve-brain-key.cjs, call
    resolveBrainKey(), and print ONLY the fields available, source, and reason. NEVER
    print key bytes (threat T-246-05). Expected: available=true.

    Pre-check 3, deployment healthy: curl -s https://pws-brain-mcp.onrender.com/health
    and expect a JSON body containing "status":"ok" and "graph":true.

    Pre-check 4, envelope/guard invariants still fenced: run bash tests/run-all-245.sh
    from the repo root. This is the LOOP-01 adjunct fence per the research Validation
    Architecture: host-session hook behavior cannot be exercised from a script, but the
    sanitize envelope and egress-guard invariants (including the claim c/d pins on the
    exact brain_search string used in call 2) are. Expected: exit 0, FAIL=0.

    Record all four results (verbatim command output for any failure) for the SUMMARY.
    If pre-checks 1-3 all pass, proceed to Task 2. If pre-check 4 fails, stop and report
    the failing test names verbatim - do not proceed to a live checkpoint on top of a
    broken fence.
  </action>
  <verify>
    <automated>node -e "const d=require(process.env.HOME+'/.claude/plugins/installed_plugins.json');const p=d.plugins['mos@mindrian-marketplace'];console.log(JSON.stringify(p));if(!/1\.16\.0/.test(JSON.stringify(p)))process.exit(1)" &amp;&amp; node -e "const r=require('/home/jsagi/dev/MindrianOS-Plugin/lib/core/resolve-brain-key.cjs').resolveBrainKey();console.log(r.available,r.source,r.reason||'');process.exit(r.available?0:1)" &amp;&amp; curl -sf https://pws-brain-mcp.onrender.com/health | grep -q '"status":"ok"' &amp;&amp; bash tests/run-all-245.sh</automated>
  </verify>
  <done>All four pre-checks pass with output recorded; or the plan stops with the failing pre-check reported verbatim and the exact remediation named (update plugin / fix key env or 0600 perms / check Render / fix the 245 fence).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fresh-session three-call Brain test (LOOP-01)</name>
  <files>none - live host-session verification; verbatim results go to the SUMMARY</files>
  <action>
    Human-only by construction: hooks load at session start, so no script and no task in
    THIS session can exercise the live matcher + sanitize-envelope path. The operator
    runs the three-call test from a FRESH session exactly as specified in how-to-verify
    below; the executor then records the pasted verbatim outputs in the SUMMARY as the
    LOOP-01 verification of record. On a failure, the executor decodes it against the
    five-signature table below, reports it verbatim in-turn (never summarized), and for
    any NEW FAILURE (no decode row matches) files an RCA at .planning/debug/<slug>.md
    per docs/RCA-TEMPLATE.md (kind: rca, git add -f). Automation stops at Task 1's
    preflight deliberately; this checkpoint verifies AFTER automation, it does not
    replace it.
  </action>
  <what-built>
    Pre-flight is green: beta.13+ cache installed on disk, read-tier key resolves,
    Render /health OK, and the 245 envelope/guard fence passes. What no script can do is
    exercise the LIVE hook path (matcher + sanitize envelope) inside a Claude Code
    session, because hooks load at session start. You are the fresh session.
  </what-built>
  <how-to-verify>
    1. Open a NEW Claude Code session (fully new process, not /clear) in
       /home/jsagi/dev/MindrianOS-Plugin. Any session started before 2026-08-10T07:43Z
       runs the stale pre-beta.13 cache and its result is void.

    2. In that fresh session, run these three tool calls IN ORDER, using EXACTLY these
       plugin-scope tool names (never the mcp__pws-brain-mcp__* project scope - the
       plugin hooks do not match it; that scope is how the outage stayed invisible):

       | # | Tool call | Arguments | Expected |
       |---|-----------|-----------|----------|
       | 1 | mcp__plugin_mos_mindrian-brain__brain_stats | {} | JSON with "backend":"memgraph", totalRecordCount around 28,325, relationshipCount around 23,014, 9 vector indexes (1 e5-queryable). Numbers may drift; assert numeric PRESENCE, not exact equality |
       | 2 | mcp__plugin_mos_mindrian-brain__brain_search | {"query":"jobs to be done framework"} | Pinecone-shape result with hits containing methodology content. Known non-fatal issues to RECORD, not fix: flat ~0.925 scores, empty framework metadata, source_file local-path leak (CONTRACT-03, Phase 247 scope) |
       | 3 | mcp__plugin_mos_mindrian-brain__brain_ask | {"question":"which framework helps identify customer jobs and desired progress?"} | A DirectiveEnvelope with a synthesized methodology answer (server-side three-arm router). Keep the question generic methodology language, Part 8 |

    3. If ANY call fails, copy the failure VERBATIM (the exact error text, in full) and
       decode it against this table before touching anything:

       | Signature | Meaning | Action |
       |-----------|---------|--------|
       | "e.reduce is not a function" | The RUNNING session loaded a pre-beta.13 cache (non-array updatedToolOutput) | Restart the session; re-run pre-check 1. Do NOT diagnose network or server |
       | PreToolUse egress-guard BLOCK on call 2 | Old guard (pre-PR #2) in the loaded cache | Same: stale cache, restart |
       | Tier-0 sentinel (DIRECTOR_NOT_AVAILABLE shape) on ALL three calls | Key not loaded. Resolution order: MINDRIAN_BRAIN_KEY env, then ~/.mindrian.env, then CWD .env; SEC-02 rejects group/world-readable key files (needs 0600) and logs one stderr line "[mindrian-os] Brain key not loaded: <reason>" | Re-run pre-check 2; fix env or file perms |
       | brain_stats OK but a brain_query attempt returns the tier-0 sentinel | NOT an outage: HTTP 403 admin gate (the current key is READ tier; brain-client conflates 403 with unreachable). Verified live 2026-08-10 | Expected on the read key. Report as the admin-tier gate, never as "Brain down". Lane B of Plan 246-02 handles it |
       | All calls null AND key reason clean | Network or Render outage | curl https://pws-brain-mcp.onrender.com/health; check the Render dashboard |

    4. Report the outcome of all three calls VERBATIM - paste the actual outputs (or
       failures), never a summary like "it worked". Recording the three known non-fatal
       search issues from the table in step 2 is part of a PASS, not a failure.
  </how-to-verify>
  <verify>
    <automated>bash tests/run-all-245.sh</automated>
    <human-check>Operator pastes the verbatim outputs of all three plugin-scope calls from the fresh session; the executor matches them against the expected-output table and, on any failure, against the five-signature decode table. The automated command above is the scriptable adjunct fence only (envelope/guard invariants); it can never substitute for the fresh-session pass.</human-check>
  </verify>
  <done>All three plugin-scope calls passed in a fresh session with verbatim outputs on record in the SUMMARY (including the recorded non-fatal search issues); OR a decoded stale-cache signature triggered a restart-and-re-run loop with the checkpoint still open; OR a NEW FAILURE is filed as an RCA in .planning/debug/ and LOOP-01 remains explicitly NOT done.</done>
  <resume-signal>
    Type "approved" with the pasted verbatim outputs of all three calls, or paste the
    verbatim failure plus which decode-table row it matched. On approval the executor
    writes the SUMMARY with the verbatim results as the LOOP-01 verification of record.
    On a stale-cache signature: operator restarts and re-runs; the checkpoint stays open.
    On any NEW FAILURE (no decode row matches): the executor files an RCA at
    .planning/debug/<slug>.md per docs/RCA-TEMPLATE.md (kind: rca, git add -f) and the
    checkpoint stays open - LOOP-01 is NOT done.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fresh host session -> plugin hooks -> brain-client -> Render edge | The path under test; the Bearer key crosses here |
| Checkpoint reporting -> plan record | False-success reporting is the recurring bug class this repo tracks (feedback_false_success_silent_skip_gates) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-246-05 | Information disclosure | Task 1 key pre-check | mitigate | resolveBrainKey() prints available/source/reason only; key bytes never printed, never pasted into chat |
| T-246-06 | Repudiation | Checkpoint reporting | mitigate | Verbatim in-turn reporting is mandatory; decode table pins interpretation; NEW FAILURE forces an RCA file, so a failure cannot vanish into a summary |
| T-246-07 | Spoofing | Verification surface | mitigate | Plugin-scope tool names pinned in the task text; pws-brain-mcp project scope explicitly forbidden (it bypasses both hooks) |
| T-246-SC | Tampering | Package installs | accept | This plan installs no packages (read-only preflight + human checkpoint); no legitimacy gate needed |
</threat_model>

<verification>
- Pre-flight: all four Task 1 checks pass, outputs recorded.
- Checkpoint: three plugin-scope calls pass in a fresh session, results pasted verbatim.
- bash tests/run-all-245.sh stays green (FAIL=0) as the scriptable adjunct fence.
- No repo files modified by this plan; the SUMMARY is the deliverable.
</verification>

<success_criteria>
Maps to LOOP-01 (ROADMAP Phase 246 success criterion 1):
- A fresh session on beta.13+ passed brain_stats, brain_search "jobs to be done
  framework", and a synthesized brain_ask answer through the plugin-scope tool names.
- Every failure encountered along the way was reported verbatim in-turn and decoded
  against the five-signature table; nothing was summarized or silently retried.
- The pass (date, plugin version, verbatim outputs, recorded non-fatal search issues) is
  filed in 246-01-SUMMARY.md as the verification of record that Phases 247/248/250 cite.
</success_criteria>

<output>
Create .planning/phases/246-live-verification-graph-census/246-01-SUMMARY.md when done,
containing: the four pre-check results, the verbatim three-call outputs, pass/fail per
call, the plugin version verified, and the recorded non-fatal issues handed to Phase 247.
</output>
