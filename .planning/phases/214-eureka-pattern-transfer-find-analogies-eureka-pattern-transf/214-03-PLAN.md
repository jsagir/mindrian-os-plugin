---
phase: 214-eureka-pattern-transfer
plan: 03
type: execute
wave: 2
depends_on: [214-01, 214-02]
files_modified:
  - scripts/analogy-fitness-report.cjs
  - tests/fixtures/eureka/214-runner-smoke.json
  - commands/find-analogies.md
autonomous: true
requirements: [214-A, 214-C, 214-E]
must_haves:
  truths:
    - "/mos:find-analogies renders MEASURED fitness (text + band + fused, provenance-stamped) when the engine runs, and honest qualitative Surface/Behavioral/Structural/Deep labels - never fabricated decimals - when it cannot"
    - "The decorative 0.78/0.65/0.52 example decimals are GONE from the command"
    - "--external composes queries ONLY via the fenced compose-queries path, asks the navigator before any fetch, falls back Tavily -> WebSearch -> local-only"
    - "The command's F.8 hitl_shape, connector block, and firing block survive the surgery byte-intact"
    - "Validated analogies are offered a human-gated write-back through graph-refine-loop with temporal stamps - never a direct db write"
  artifacts:
    - path: "scripts/analogy-fitness-report.cjs"
      provides: "The CLI bridge between the command prompt layer and the engine: modes score / compose-queries, --stub-encoder dev flag"
      min_lines: 80
    - path: "commands/find-analogies.md"
      provides: "The SURGERY target: measured fitness wired, online fence wired, write-back offer wired; structure/modes/frontmatter preserved"
    - path: "tests/fixtures/eureka/214-runner-smoke.json"
      provides: "Committed offline smoke input for the runner"
  key_links:
    - from: "commands/find-analogies.md"
      to: "scripts/analogy-fitness-report.cjs"
      via: "Bash invocation in Steps 4.5 and External Mode"
      pattern: "analogy-fitness-report"
    - from: "scripts/analogy-fitness-report.cjs"
      to: "lib/core/eureka/analogy-fitness.cjs"
      via: "scoreAnalogyFitness / rankCandidates consumption"
      pattern: "require\\(.*analogy-fitness"
    - from: "scripts/analogy-fitness-report.cjs"
      to: "lib/core/eureka/online-pattern-query.cjs"
      via: "compose-queries mode"
      pattern: "online-pattern-query"
---

<objective>
Perform the surgery the moat audit ordered (locked conflict 1: REPLACE the decorative find-analogies stub on the ONE substrate, never a 5th pipeline). Two deliverables: (a) `scripts/analogy-fitness-report.cjs`, the thin CLI runner (eureka-room-report.cjs style template) that lets the command's prompt layer invoke the Plan-01 engine and the Plan-02 fence through Bash; (b) the `commands/find-analogies.md` rewrite that retires the fabricated fitness decimals, renders measured numbers with provenance when the engine runs, degrades to honest qualitative band labels when it cannot (the room's own Tier-0 honesty pattern), routes ALL external queries through the fenced compose-queries path with navigator confirmation (MCP-stack-awareness HARD RULE), and offers the human-gated write-back ride on graph-refine-loop + the Phase 160 temporal spine (SEED-049 D11: zero new temporal infrastructure).

Canon Part 7: zero net-new invocable surfaces - find-analogies already exists, already declares `hitl_shape: F.8` + its connector block (born WIRED); this plan preserves that declaration byte-intact (Part 11: preserve, never re-mint). The runner is a script, not a command/agent/skill, and follows the shipped eureka-room-report.cjs precedent.

Scope fence (DG-1, flagged in 214-01): this plan delivers the COMMAND-LEVEL capability plus a manual entry point. It does NOT touch SENS-13, decide(), the reach spine, or automatic empty-room routing - those are Phase 213's contract (RESEARCH.md Open Question 1: 214 ships the capability, 213 wires the routing; the seam is recorded here and must be recorded again in 213's plans). It also does NOT implement structural-hole/whitespace detection - Phase 212.5 territory (locked conflict: the modes partition cleanly; Type-3 runs on a THIN or empty room with no local-graph density floor per SEED-049 D9).

Purpose: 214-A becomes user-visible; 214-C becomes the command's actual behavior; 214-E gets its command-layer half.
Output: the runner, the smoke fixture, the rewritten command.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/214-eureka-pattern-transfer-find-analogies-eureka-pattern-transf/214-RESEARCH.md (Architecture diagram, Responsibility Map, Pitfalls 4-5)
@commands/find-analogies.md (the surgery target - read it whole before editing; frontmatter lines 1-54 are preserve-zone)
@scripts/eureka-room-report.cjs (the runner style template: provenance header, figure-guard try/catch, offline discipline)
@lib/core/eureka/analogy-fitness.cjs (Plan 01 interface: scoreAnalogyFitness/rankCandidates/toRefineProposal)
@lib/core/eureka/online-pattern-query.cjs (Plan 02 interface: composePatternQueries)
@.planning/seeds/SEED-049-mindrian-insight-engine-tri-modal-tri-source-hybrid-retrieval.md (D9 lines 324-332: chain-recommender lean; D11 lines 347-358: the six temporal integration points)
@CLAUDE.md (tri-polar rule, Part 8, Part 11, MCP-stack-awareness: ASK before web research)
</context>

## Artifacts this phase produces

This plan: `scripts/analogy-fitness-report.cjs` (CLI modes `score <input.json> [--stub-encoder] [--out <file>]` and `compose-queries <pattern.json>`); `tests/fixtures/eureka/214-runner-smoke.json`; the rewritten `commands/find-analogies.md` (new Step 4.5 Measured Fitness, rewritten Step 5 matrix with Text/Band/Fused columns + provenance line + restatement marking, rewritten External Mode, extended Step 6 write-back offer). No new env vars, no new deps.

Other plans (context): see the roster in 214-01-PLAN.md.

<tasks>

<task type="auto">
  <name>Task 1: scripts/analogy-fitness-report.cjs + offline smoke fixture</name>
  <files>scripts/analogy-fitness-report.cjs, tests/fixtures/eureka/214-runner-smoke.json</files>
  <read_first>scripts/eureka-room-report.cjs (provenance header emission, per-pair figure-guard try/catch, argv handling - clone the idioms), lib/core/eureka/analogy-fitness.cjs (the exact envelope shapes to render), lib/core/eureka/online-pattern-query.cjs (the compose-queries passthrough shape)</read_first>
  <action>
    Create scripts/analogy-fitness-report.cjs (CJS, 'use strict', process.argv switch-case router per house convention - no Commander/yargs). Mode `score <input.json>`: input is {source:{text,sapphire}, candidates:[{id,domain,text,sapphire,source_tier,source_date?}]}; for each candidate call scoreAnalogyFitness (wrap per-candidate in the figure-guard try/catch so ONE bad candidate degrades to a per-row error, never kills the run - the 211-05 nichefoods precedent), rank via rankCandidates, print a JSON report to stdout: {provenance: encoderProvenance-derived from the first success result, rows:[{rank,id,domain,text,band,fused,restatementFlag,error?}]} plus a markdown matrix block; when the encoder is unavailable print {degrade:'qualitative-only', reason} and exit 0 (the command falls back to honest labels - never a fake number, never a hard fail). `--out <file>` additionally writes the JSON. `--stub-encoder` injects the same deterministic lookup-table stub the tests use (offline dev/CI seam; document it in the header as test-only). Mode `compose-queries <pattern.json>`: parse, call composePatternQueries, print its envelope verbatim, exit 0 (a degrade envelope is a SUCCESSFUL honest answer, not an error). Malformed input/unknown mode: print {ok:false, reason:'bad_input'} and exit 1. Zero network, zero DB writes, no model-name literal. Then create tests/fixtures/eureka/214-runner-smoke.json: one source + two candidates with complete 7-field sapphire encodings (any domain-generic content; make candidate 1 share more layers with the source than candidate 2 so ranking is deterministic under the stub).
  </action>
  <verify>
    <automated>node scripts/analogy-fitness-report.cjs score tests/fixtures/eureka/214-runner-smoke.json --stub-encoder</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/analogy-fitness-report.cjs score tests/fixtures/eureka/214-runner-smoke.json --stub-encoder` exits 0 and its stdout contains the strings "provenance" and "restatementFlag"
    - `node scripts/analogy-fitness-report.cjs compose-queries /dev/stdin <<< '{"functionalKeywords":["rare-signal detection"],"trizPrinciples":["Separation"],"abstractFunction":"recover a rare signal from vast background"}'` exits 0 and stdout contains '"ok":true'
    - `node scripts/analogy-fitness-report.cjs` (no args) exits 1 and stdout contains "bad_input"
    - `grep -v "^\s*//" scripts/analogy-fitness-report.cjs | grep -cE "mdbr-leaf-ir|MiniLM|Xenova|INSERT INTO|fetch\(" ` prints 0
    - `grep -rn "$(printf '\342\200\224')" scripts/analogy-fitness-report.cjs tests/fixtures/eureka/214-runner-smoke.json; test $? -eq 1` (no em-dashes)
  </acceptance_criteria>
  <done>The prompt layer has one governed CLI door to both the fitness engine and the egress fence, offline-testable end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Surgery on commands/find-analogies.md</name>
  <files>commands/find-analogies.md</files>
  <read_first>commands/find-analogies.md (whole file - identify the preserve-zone: frontmatter lines 1-40, firing block lines 42-54, Modes, UI Format, Steps 1-3 structure), commands/whitespace.md around line 465 (the SIGNAL-only external-data discipline wording to mirror in the External Mode rewrite)</read_first>
  <action>
    Rewrite commands/find-analogies.md with these EXACT edits, touching nothing else:
    (1) PRESERVE byte-intact: the entire YAML frontmatter (hitl_shape "F.8", hitl_why, connector block, allowed-tools - already grants Bash, tavily, AskUserQuestion; add nothing, remove nothing) and the mos:firing-block. Part 11: the surface stays born-wired as declared.
    (2) Step 4 Default Mode: keep the Tier-0 LLM candidate GENERATION text, but append the honesty rule: Tier 0 without the engine labels each candidate with a QUALITATIVE band word (Surface/Behavioral/Structural/Deep, per references/methodology/sapphire-encoding.md) plus the sentence "fitness not computed - qualitative label only". It NEVER prints a numeric fitness it did not measure (retire the decoration; fusion-router never-fabricate rule).
    (3) NEW Step 4.5 "Measured Fitness (the engine)": Larry writes {source, candidates} with full SAPPhIRE encodings (extracted per the existing sapphire-encoding.md template) to room/<section>/analogies/<slug>-fitness-input.json, runs `node scripts/analogy-fitness-report.cjs score <that file>` via Bash, and uses the returned rows. On {degrade:'qualitative-only'} fall back to the Step-4 qualitative labels with the honest note. Never hand-compute a decimal.
    (4) Step 5 matrix: replace the example table (the decorative 0.78/0.65/0.52/0.41/0.35 rows) with the measured-column shape: Rank | Source Domain | Mechanism | Distance | Text | Band | Fused | Source, a provenance line under the matrix reading exactly: "fitness measured by <provenance.model> (<provenance.dim>-dim)" (values from the runner output - the command text itself names NO model, locked conflict 2), and the rule: restatement-flagged rows render a "[restatement]" marker and are ordered by the runner's band-first ranking (they can never sit at Rank 1).
    (5) External Mode rewrite: step order is now (a) run `node scripts/analogy-fitness-report.cjs compose-queries <pattern.json>` where pattern.json holds ONLY functionalKeywords/trizPrinciples/abstractFunction from Step 3; (b) if {ok:false} state the local-only degrade honestly and SKIP all fetching - never hand-compose a query (no send-anyway); (c) if ok, fire the AskUserQuestion card asking the navigator to approve the web pass and showing the query count (MCP-stack-awareness HARD RULE: ask before web research); (d) on approval fetch via mcp__tavily__tavily-search, and if Tavily is unavailable (not configured, or errors like the 2026-07-04 402) fall back to WebSearch with the SAME audited strings (navigator-approved precedent); (e) fetched results file LOCALLY only (SIGNAL -> LOCAL: YES; external data never goes to Brain - mirror the whitespace.md discipline sentence).
    (6) Step 6 additions: a write-back offer on a navigator-validated analogy - build the proposal (toRefineProposal semantics: kind analogy_transfer, valid_at from the source's date when the analogy cites a dated real-world source, else null), preview via graph-refine-loop (dryRun stays default TRUE), navigator confirms before any write (Part 9 chokepoint, never a direct db write); note that the before/after room delta rides temporal queryAsOf and a refuted analogy is superseded, never overwritten (D11). Replace any implied fixed next step with: seed lib/brain/chain-recommender.cjs recommendFrameworkChain with the Type-3 result lean (D9) - the existing four suggestion bullets stay as Larry prose.
    (7) Tri-polar table: CLI row mentions the runner; Desktop row keeps conversational delivery and falls back to qualitative honesty when Bash is unavailable; Cowork row unchanged.
    No em-dashes anywhere (the file's existing double-hyphen style stays).
  </action>
  <verify>
    <automated>bash -c "grep -c 'analogy-fitness-report' commands/find-analogies.md | grep -qx '[2-9]' && grep -q 'compose-queries' commands/find-analogies.md && ! grep -qE '0\.78|0\.65|0\.52' commands/find-analogies.md && grep -q 'qualitative' commands/find-analogies.md && grep -qc 'hitl_shape: \"F.8\"' commands/find-analogies.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "analogy-fitness-report" commands/find-analogies.md` prints >= 2 (score + compose-queries call sites)
    - `grep -cE "0\.78|0\.65|0\.52" commands/find-analogies.md` prints 0 (decoration retired)
    - `grep -c 'hitl_shape: "F.8"' commands/find-analogies.md` prints 1 and `grep -c "mos:firing-block" commands/find-analogies.md` prints 2 (open+close markers intact)
    - `grep -c "AskUserQuestion" commands/find-analogies.md` prints >= 2 (firing block + the External Mode approval ask)
    - `grep -c "WebSearch" commands/find-analogies.md` prints >= 1 (fallback documented)
    - `node scripts/build-connector-registry.cjs --check` exits 0; `node scripts/check-render-coverage.cjs` exits 0; `node scripts/check-shape-declaration.cjs --check` exits 0
    - `grep -rn "$(printf '\342\200\224')" commands/find-analogies.md; test $? -eq 1` (no em-dashes)
  </acceptance_criteria>
  <done>find-analogies is no longer decorative: measured when the engine runs, honest when it cannot, fenced when it goes online, human-gated when it writes back.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command prompt layer -> runner CLI | Larry-authored JSON crosses into deterministic code |
| audited queries -> Tavily/WebSearch | the outbound SIGNAL fetch, navigator-gated |
| fetched web content -> room files | SIGNAL -> LOCAL ingestion |
| validated analogy -> room.db | write-back, human-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-07 | Information disclosure | External Mode fetches | mitigate | queries sourced EXCLUSIVELY from compose-queries output (grep-gated); AskUserQuestion approval before any fetch; degrade path has no send-anyway |
| T-214-08 | Tampering | fetched web content written back as fact | mitigate | graph-refine-loop fact-check + human gate + dryRun default TRUE; dated claims carry valid_at for date-sync-gate |
| T-214-09 | Integrity | fabricated fitness under degrade | mitigate | qualitative-only fallback wording mandated in Steps 4/4.5; decorative decimals grep-gated to 0 |
| T-214-10 | Elevation of privilege | command gaining new tool grants | accept | allowed-tools byte-unchanged; no new grants; born-wired gates re-run in acceptance |
</threat_model>

<verification>
- Runner smoke + compose-queries acceptance commands exit 0
- All three born-wired/render gates exit 0
- `bash tests/run-all-214.sh` legs 1-2 still PASS (no regression from the surgery)
</verification>

<success_criteria>
The shipped command now delivers Phase 214's user-visible half: measured two-leg fitness with provenance, an honest qualitative floor, a fenced navigator-gated online leg with Tavily->WebSearch->local-only degrade, and a human-gated temporal-aware write-back offer - with the F.8/connector/firing-block declarations byte-intact.
</success_criteria>

<output>
Create `.planning/phases/214-eureka-pattern-transfer-find-analogies-eureka-pattern-transf/214-03-SUMMARY.md` when done.
</output>
