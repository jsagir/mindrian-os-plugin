---
phase: 214-eureka-pattern-transfer
plan: 04
type: execute
wave: 3
depends_on: [214-01, 214-03]
files_modified:
  - tests/test-214-darkmatter-gate.cjs
  - tests/fixtures/eureka/214-darkmatter.json
  - tests/fixtures/eureka/214-rank-order.json
autonomous: true
requirements: [214-D, 214-F]
must_haves:
  truths:
    - "On the archimedes-darkmatter gold case, the real bridge ranks first, the restatement distractor is flagged and never ranks first despite its higher text cosine, and the pseudoscience drift ranks last"
    - "The gate seeds ONLY the abstracted pattern from the gold card, never the whole challenge doc (whole-doc seeding leaks the answer)"
    - "Measured fitness rank-orders consistently with the qualitative Surface/Behavioral/Structural/Deep labels (gate 214-F, the SEED-049 D10 acceptance)"
    - "The full phase gate is green: run-all-214 all PASS, run-all-211 no new failures, doctor acceptance, born-wired gates, zero em-dashes, zero hardcoded model names"
    - "The phase's verdicts are mirrored to the rethinking-mindrianos room, cross-linked (Dev-Research Compositing)"
  artifacts:
    - path: "tests/test-214-darkmatter-gate.cjs"
      provides: "The 214-D + 214-F integration gate (offline, stub encoder, directional truths - 211-05 Test A pattern)"
    - path: "tests/fixtures/eureka/214-darkmatter.json"
      provides: "Gold-card fixture: abstracted seed + bridge/restatement/pseudoscience candidates with SAPPhIRE encodings"
    - path: "tests/fixtures/eureka/214-rank-order.json"
      provides: "Four labeled candidates (surface/behavioral/structural/deep) for the rank-order consistency gate"
  key_links:
    - from: "tests/fixtures/eureka/214-darkmatter.json"
      to: "evals/eureka/cases/archimedes-darkmatter.md"
      via: "seed = hypothesis_in abstracted pattern verbatim; distractor labels mirrored"
      pattern: "rare-signal-in-vast-background"
    - from: "tests/test-214-darkmatter-gate.cjs"
      to: "lib/core/eureka/analogy-fitness.cjs"
      via: "scoreAnalogyFitness / rankCandidates under injected stub encoder"
      pattern: "rankCandidates"
---

<objective>
Close Phase 214 with its acceptance gates. Gate 214-D: on the `archimedes-darkmatter` Type-3 gold case (Phase 211 gold set, manual baseline 0.95), the measured pipeline surfaces the real bridge first, flags the restatement paraphrase (the #1 false positive: it out-scores the bridge on raw text cosine yet is not a bridge), and floors the pseudoscience drift. Gate 214-F: the fused fitness rank-orders consistently with the qualitative Surface/Behavioral/Structural/Deep labels a domain-competent reasoner assigns (SEED-049 D10's stopgap acceptance - the falsifier for the DG-3 per-field-cosine choice). Then the full phase roll-up and the Dev-Research Compositing mirror.

Honest boundary, stated so nobody over-claims: full pseudoscience REFUSAL (the GuardGate zero) is Phase 212's Grounding Guard, which is planned-not-executed today (RESEARCH.md V5 - the 212.5 ROADMAP text claiming the critic "[shipped in 212]" is false on disk). What Phase 214 guarantees mechanically is the rank floor (pseudoscience can never outrank the bridge - no falsifiable layer correspondence means no band above surface) plus the prompt-layer refusal instruction already in the command, plus criticFeatures carried on every result so the 212 critic wires in as a one-line consumer later (RESEARCH.md Open Question 2, encoded in Plan 01 Test 6).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/214-eureka-pattern-transfer-find-analogies-eureka-pattern-transf/214-RESEARCH.md (Pitfalls 3 + 6, Validation Architecture, Phase Requirements table)
@evals/eureka/cases/archimedes-darkmatter.md (the gold card: hypothesis_in, destination, distractors, gold_label - the fixture's source of truth)
@evals/eureka/211-manual-baseline.md (line 78: the 0.95 hand-scored baseline this gate formalizes)
@lib/core/eureka/analogy-fitness.cjs (Plan 01 interface)
@tests/test-214-analogy-fitness.cjs (the lookup-table stub encoder to reuse)
@CLAUDE.md (Dev-Research Compositing section - the two-homes rule this plan's Task 3 satisfies)
</context>

## Artifacts this phase produces

This plan: `tests/test-214-darkmatter-gate.cjs`; `tests/fixtures/eureka/214-darkmatter.json`; `tests/fixtures/eureka/214-rank-order.json`; the room mirror entry `~/MindrianRooms/rethinking-mindrianos/research/<run-date>-phase214-find-analogies-measured-fitness/<same-slug>.md` (outside the repo, per the compositing rule). No new env vars, no new deps, no lib code.

Other plans (context): see the roster in 214-01-PLAN.md.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gold-card fixtures + tests/test-214-darkmatter-gate.cjs (214-D + 214-F)</name>
  <files>tests/test-214-darkmatter-gate.cjs, tests/fixtures/eureka/214-darkmatter.json, tests/fixtures/eureka/214-rank-order.json</files>
  <read_first>evals/eureka/cases/archimedes-darkmatter.md (frontmatter: hypothesis_in is the ONLY seed text allowed; distractors block; the lens line "seed on the ABSTRACTED PATTERN, NEVER the whole challenge doc"), tests/test-214-analogy-fitness.cjs (reuse its lookup-table stub encoder verbatim - same vectors idiom), scripts/eureka-room-report.cjs or 211-05's test notes (the "directional truths only under stub" Test A pattern)</read_first>
  <behavior>
    - Test 1 (leak guard, Pitfall 6): the fixture's seed string contains 'rare-signal-in-vast-background' and does NOT contain 'dark-matter', 'implicit-solvent', or 'background subtraction' (the answer never rides in the seed)
    - Test 2 (214-D bridge): under the stub encoder, rankCandidates places the bridge candidate at rank 1 with structural.band 'structural' or 'deep'
    - Test 3 (214-D restatement): the restatement candidate has a HIGHER stub text cosine than the bridge (engineered >= 0.95 vs the bridge's ~0.70 - the raw-differential spike is REAL in the fixture) yet restatementFlag === true, band 'surface' or 'none', and its rank is strictly worse than the bridge's
    - Test 4 (214-D pseudoscience floor): the pseudoscience candidate ranks LAST and its band is 'surface' or 'none'; the test comment names the honest boundary (full refusal = 212 critic; criticFeatures carried)
    - Test 5 (214-F rank-order consistency): for the four 214-rank-order.json candidates labeled surface/behavioral/structural/deep, the measured band EQUALS the qualitative label for each, and fused scores strictly increase with label rank (surface < behavioral < structural < deep)
    - Test 6 (aggregator flip): after this file lands, tests/run-all-214.sh leg 3 flips from SKIP to PASS
  </behavior>
  <action>
    Build tests/fixtures/eureka/214-darkmatter.json: seed = the abstracted pattern verbatim from the card's hypothesis_in ("the pattern rare-signal-in-vast-background - detecting a signal that is one part in roughly 10^9 against overwhelming noise"); three candidates each with id, domain, text, and a hand-authored 7-field SAPPhIRE encoding: (a) bridge - dark-matter direct detection, layers state_change/action/phenomenon/real_effect engineered to correspond with the seed's encoding under the stub (shared mechanism: statistical background subtraction); (b) restatement - the ppb paraphrase, overall text nearly identical to the seed (stub cosine >= 0.95) but ONLY state_change corresponding; (c) pseudoscience - the water-consciousness drift, mid text cosine, no falsifiable layer correspondence. Build tests/fixtures/eureka/214-rank-order.json: four candidates with qualitative_label fields (surface/behavioral/structural/deep) whose encodings share exactly the rubric's layer sets with the source. Write tests/test-214-darkmatter-gate.cjs (RED first where meaningful, then green): plain-Node assert, offline, injecting the SAME lookup-table stub encoder as test-214-analogy-fitness.cjs so every cosine is exact by construction (directional truths, not live-model numbers - the live-model reading happens at 215-era calibration, not in CI). Assert Tests 1-5. Commit test(214-04): then any needed fixture-tuning commit.
  </action>
  <verify>
    <automated>node tests/test-214-darkmatter-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-214-darkmatter-gate.cjs` exits 0 with all assertions green
    - `grep -c "rare-signal-in-vast-background" tests/fixtures/eureka/214-darkmatter.json` prints >= 1 and `grep -cE "dark-matter|implicit-solvent" <(node -e "console.log(JSON.stringify(require('/home/jsagi/dev/MindrianOS-Plugin/tests/fixtures/eureka/214-darkmatter.json').seed || require('/home/jsagi/dev/MindrianOS-Plugin/tests/fixtures/eureka/214-darkmatter.json').source.text))")` prints 0 (the seed field alone is leak-checked, not the candidate bodies)
    - `bash tests/run-all-214.sh` shows all three legs PASS, FAIL=0
    - `grep -rn "$(printf '\342\200\224')" tests/test-214-darkmatter-gate.cjs tests/fixtures/eureka/214-darkmatter.json tests/fixtures/eureka/214-rank-order.json; test $? -eq 1`
  </acceptance_criteria>
  <done>The gold case is a permanent executable gate: bridge wins, paraphrase flagged, drift floored, measured bands agree with qualitative labels.</done>
</task>

<task type="auto">
  <name>Task 2: Full phase gate roll-up</name>
  <files></files>
  <read_first>tests/run-all-211.sh header (the three-leg phase-gate description; the known pre-existing env-dependent rerank failure)</read_first>
  <action>
    Run the complete Phase 214 acceptance stack and record every result verbatim in the SUMMARY: (1) `bash tests/run-all-214.sh` - required FAIL=0; (2) regression `bash tests/run-all-211.sh` - required: FAIL count <= 1, and any single failure must be the pre-existing env-dependent rerank leg documented at phase start (RESEARCH.md Dependency Reality: baseline PASS=9 FAIL=1); any OTHER failing leg is a Phase 214 regression and blocks; (3) `node scripts/doctor.cjs --acceptance` exit 0; (4) `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs` all exit 0; (5) the no-hardcoded-model sweep across all three new code files; (6) the em-dash sweep across all eight phase-created/modified repo files. Do not silence, skip, or reinterpret a red result: a failure here returns to the offending plan.
  </action>
  <verify>
    <automated>bash -c "bash tests/run-all-214.sh && node scripts/doctor.cjs --acceptance && node scripts/build-connector-registry.cjs --check && node scripts/check-render-coverage.cjs"</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-214.sh` exits 0 with PASS=3 FAIL=0
    - `bash tests/run-all-211.sh; test $? -eq 0` OR its FAIL count is exactly 1 with the failing leg name matching the documented pre-existing rerank leg (record the leg name in the SUMMARY either way)
    - `node scripts/doctor.cjs --acceptance` exits 0
    - `grep -v "^\s*//" lib/core/eureka/analogy-fitness.cjs lib/core/eureka/online-pattern-query.cjs scripts/analogy-fitness-report.cjs | grep -cE "mdbr-leaf-ir|MiniLM|Xenova"` prints 0
    - `grep -rn "$(printf '\342\200\224')" lib/core/eureka/analogy-fitness.cjs lib/core/eureka/online-pattern-query.cjs scripts/analogy-fitness-report.cjs commands/find-analogies.md tests/test-214-analogy-fitness.cjs tests/test-214-online-fence.cjs tests/test-214-darkmatter-gate.cjs tests/run-all-214.sh; test $? -eq 1`
  </acceptance_criteria>
  <done>Phase 214's whole surface is green under the house acceptance stack with zero regression debt hidden.</done>
</task>

<task type="auto">
  <name>Task 3: Dev-Research Compositing mirror to the rethinking-mindrianos room</name>
  <files></files>
  <read_first>CLAUDE.md Dev-Research Compositing section (the two-homes cross-link rule), ~/MindrianRooms/rethinking-mindrianos/research/2026-07-06-phase212-blockers-fixed-node22-vec0-correction/ (a recent dated-entry naming/shape example)</read_first>
  <action>
    Create the durable room-side record: directory `~/MindrianRooms/rethinking-mindrianos/research/<today YYYY-MM-DD>-phase214-find-analogies-measured-fitness/` containing a markdown file of the same slug. Contents: (a) what shipped (the stub replaced by the two-leg measured fitness on the one substrate; the inline auditQueryString online fence; the darkmatter gate green with the exact PASS counts from Task 2); (b) the three Decision Gate calls as encoded (DG-1 decoupled from 213 at the command-level seam, DG-2 inline fence over hook extension, DG-3 per-field cosine over node2vec with 214-F as the falsifier) and whether the navigator confirmed or overrode each; (c) cross-links BOTH ways: the repo paths `.planning/phases/214-eureka-pattern-transfer-find-analogies-eureka-pattern-transf/214-RESEARCH.md` and the four PLAN files, plus a pointer back to the room's own `2026-07-05-rebuild-vs-surgery/02-moat-embedding-audit.md` (the consolidation verdict this phase executed); (d) the still-open seams for successor phases: 213 owns SENS-13 + empty-room auto-routing, 212 critic wires in via criticFeatures, the un-mirrored analogy-algorithm survey remains open for 215 (RESEARCH.md Open Question 3). Also mirror to mindrianOS/research/ if that source-of-record convention directory exists on disk; skip silently if absent and note it.
  </action>
  <verify>
    <automated>bash -c "f=$(ls ~/MindrianRooms/rethinking-mindrianos/research/*phase214-find-analogies-measured-fitness/*.md 2>/dev/null | head -1); test -n \"$f\" && grep -q '.planning/phases/214-eureka-pattern-transfer' \"$f\" && grep -q '02-moat-embedding-audit' \"$f\""</automated>
  </verify>
  <acceptance_criteria>
    - The room entry file exists under ~/MindrianRooms/rethinking-mindrianos/research/ with the phase214-find-analogies-measured-fitness slug
    - It greps positive for '.planning/phases/214-eureka-pattern-transfer' (repo cross-link) and '02-moat-embedding-audit' (room cross-link)
    - It names all three DG items and their outcomes
    - No em-dash character in the entry
  </acceptance_criteria>
  <done>The finding lives in both homes, cross-linked: the repo has the executable decision, the room has the evidence trail. Neither substitutes for the other.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gold-card fixture -> CI gate | committed synthetic content only; no real-room data enters tests |
| repo verdicts -> room mirror | LOCAL-to-LOCAL file write outside the repo; no Brain, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-11 | Integrity | gate fixtures leaking the answer into the seed | mitigate | Test 1 leak guard: seed asserted free of 'dark-matter'/'implicit-solvent' tokens (Pitfall 6, the card's own mandate) |
| T-214-12 | Integrity | over-claiming pseudoscience refusal | mitigate | honest-boundary comment required in Test 4; full refusal named as 212 critic territory; rank floor is what 214 asserts |
| T-214-13 | Repudiation | phase declared green on partial evidence | mitigate | Task 2 records every gate result verbatim in the SUMMARY; the 211 regression baseline (FAIL<=1, named leg) is explicit, never silently reinterpreted |
| T-214-SC | Tampering | npm/pip/cargo installs | accept | zero new packages this phase; any discovered need re-opens the legitimacy audit behind a checkpoint |
</threat_model>

<verification>
- `node tests/test-214-darkmatter-gate.cjs` exits 0
- `bash tests/run-all-214.sh` PASS=3 FAIL=0
- `bash tests/run-all-211.sh` no new failures vs the documented baseline
- `node scripts/doctor.cjs --acceptance` exits 0
- Room mirror entry exists with both cross-links
</verification>

<success_criteria>
Gates 214-D and 214-F are permanent executable tests anchored to the Phase 211 gold set, the whole phase is green under the house acceptance stack with the 211 substrate regression-checked, and the Dev-Research Compositing rule is satisfied with the three Decision Gate outcomes recorded durably.
</success_criteria>

<output>
Create `.planning/phases/214-eureka-pattern-transfer-find-analogies-eureka-pattern-transf/214-04-SUMMARY.md` when done.
</output>
