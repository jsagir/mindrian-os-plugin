---
phase: 127.2
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-server-brain/lib/brain-ask.cjs
  - mcp-server-brain/lib/pinecone-tools.cjs
  - docs/RCA-TEMPLATE.md
  - .planning/debug/resolved/brain-ask-contract-mismatch-rename.md
  - .planning/debug/resolved/brain-topk-uncapped-advisory.md
  - .planning/debug/resolved/stale-install-cache-audit-anti-pattern.md
  - .planning/debug/knowledge-base.md
  - tests/test-127.2-00-brain-edge-bundle.sh
autonomous: true
requirements_addressed:
  - BRAIN-EDGE-127.2-D08  (brain_ask description rewrite)
  - BRAIN-EDGE-127.2-D09  (BRAIN_MAX_TOPK cap on Pinecone forwards)
  - BRAIN-EDGE-127.2-D10  (Source-of-Truth Preamble for QA harnesses)
gap_closure: true
gap_closure_source:
  - .planning/debug/brain-ask-contract-mismatch-rename.md
  - .planning/debug/brain-topk-uncapped-advisory.md
  - .planning/debug/stale-install-cache-audit-anti-pattern.md
must_haves:
  truths:
    - "brain_ask tool description names DirectiveEnvelope return contract + GUIDED default mode + framework-chain payload (no longer reads as 'ask anything, get an answer')"
    - "BRAIN_MAX_TOPK env var enforced at both Pinecone forward sites; default cap = 100; uncapped caller input is silently truncated via Math.min"
    - "docs/RCA-TEMPLATE.md carries a 'Source-of-Truth Preamble' section requiring every audit prompt to state which source-of-truth its claims read against, and every source-code claim to re-verify against origin/main HEAD before filing"
    - "All three debug docs moved to .planning/debug/resolved/ with resolved_by: phase-127.2 in frontmatter + summary entries in knowledge-base.md"
    - "Zero behavioral change to non-Pinecone Brain code paths (brain_query, brain_schema, brain_write are not touched)"
  artifacts:
    - path: "mcp-server-brain/lib/brain-ask.cjs"
      provides: "Rewritten tool description string at line ~506 + Math.min(topK, BRAIN_MAX_TOPK) at line ~545"
      contains: "DirectiveEnvelope"
      contains_2: "BRAIN_MAX_TOPK"
    - path: "mcp-server-brain/lib/pinecone-tools.cjs"
      provides: "Math.min(topK, BRAIN_MAX_TOPK) at line ~42 mirroring brain-ask.cjs"
      contains: "BRAIN_MAX_TOPK"
    - path: "docs/RCA-TEMPLATE.md"
      provides: "New ## Source-of-Truth Preamble section + checklist row added to existing template structure"
      contains: "Source-of-Truth Preamble"
    - path: "tests/test-127.2-00-brain-edge-bundle.sh"
      provides: "Three smoke tests: (1) brain_ask description string contains 'DirectiveEnvelope'; (2) brain_ask({topK:999999}) returns <=100 rows with BRAIN_MAX_TOPK unset, <=N rows with BRAIN_MAX_TOPK=N; (3) RCA-TEMPLATE.md contains 'Source-of-Truth Preamble' header"
      contains: "BRAIN_MAX_TOPK"
  key_links:
    - from: "mcp-server-brain/lib/brain-ask.cjs"
      to: "process.env.BRAIN_MAX_TOPK"
      via: "Math.min(topK, parseInt(process.env.BRAIN_MAX_TOPK || '100', 10))"
      pattern: "BRAIN_MAX_TOPK"
    - from: "mcp-server-brain/lib/pinecone-tools.cjs"
      to: "process.env.BRAIN_MAX_TOPK"
      via: "same Math.min guard as brain-ask.cjs"
      pattern: "BRAIN_MAX_TOPK"
    - from: "docs/RCA-TEMPLATE.md"
      to: ".planning/debug/<slug>.md frontmatter"
      via: "every new RCA file MUST fill the Source-of-Truth Preamble field"
      pattern: "Source-of-Truth Preamble"
---

<objective>
Close the three Brain-edge RCAs surfaced by the 2026-05-23 Windows beta-tester deep audit as a single small change set, riding the same beta as the warmup-ping work in Plan 127.2-01. Each fix is independently small (D-08 = ~2 lines; D-09 = ~5 lines across 2 files; D-10 = ~30 lines of docs); bundling them into one plan respects "small fix => small plan" discipline and keeps the v1.13.1-beta.2 changelog narrative tight ("Brain MCP turn-1 contextualization + Brain-edge cleanups from Windows audit").

Purpose: the Brain is the moat. A moat whose tool description lies about the return contract breeds wrong mental models in every LLM that reads the tool registry. A moat whose result-set cap is INHERITED from Pinecone rather than OWNED by the Brain disappears the moment Pinecone changes its server-side cap. A moat whose audits read stale source while probing live wire produces verdicts that cannot be trusted. Close all three before Plan 127.2-01 layers the warmup ping on top, so the warmup-ping audit lands on a clean surface.

Output: three independent file edits, three debug-doc closeouts, one knowledge-base.md update, and one smoke test verifying all three landed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

### Task 1: D-08 -- brain_ask description rewrite

1. Read `mcp-server-brain/lib/brain-ask.cjs` line 506 (the tool description string).
2. Rewrite to ~2-3 lines naming: (a) returns DirectiveEnvelope, not synthesized answer; (b) default mode = GUIDED (per `feedback_larry_pedagogical_guided_first.md`); (c) payload shape = routing decision + framework-chain suggestions + optional AUTONOMOUS/HYBRID escape per explicit user invitation; (d) for free-form Cypher use brain_query; for vector-only search use brain_search.
3. No behavioral change. Single-file edit. Re-run any tool-registry serialization test that snapshots the description (if one exists; add one if not).

### Task 2: D-09 -- BRAIN_MAX_TOPK cap on Pinecone forwards

1. Add `BRAIN_MAX_TOPK` env var support to both call sites:
   - `mcp-server-brain/lib/brain-ask.cjs:545` -- the Pinecone topK forward inside the brain_ask handler.
   - `mcp-server-brain/lib/pinecone-tools.cjs:42` -- the topK forward inside brain_search.
2. Pattern: `const MAX_TOPK = parseInt(process.env.BRAIN_MAX_TOPK || '100', 10); const safeTopK = Math.min(topK, MAX_TOPK);`
3. Use `safeTopK` in the Pinecone query call.
4. No logging of the truncation (silent guard per Phase 8 minimum-surface principle); if a downstream surfacing surface needs to know "result was capped", add it in a later phase.
5. Smoke test: with `BRAIN_MAX_TOPK` unset, `brain_ask({topK: 999999})` returns at most 100 rows. With `BRAIN_MAX_TOPK=50`, same call returns at most 50.

### Task 3: D-10 -- Source-of-Truth Preamble in RCA-TEMPLATE.md

1. Read current `docs/RCA-TEMPLATE.md`.
2. Add a new top-level section `## Source-of-Truth Preamble (MANDATORY)` near the front of the template (after the YAML frontmatter spec, before the "Current Focus" section).
3. The section requires every QA / audit prompt to declare explicitly: (a) which source-of-truth this audit's CODE claims read against (origin/main HEAD? install cache? a specific tag?); (b) which source-of-truth this audit's WIRE claims probe against (deployed Brain server? local stdio shim? mock?); (c) the date/SHA pair for each.
4. Add a checklist row to the existing RCA-TEMPLATE.md checklist section: `- [ ] Source-of-Truth Preamble filled before any finding filed`.
5. Add a one-paragraph rationale citing the 2026-05-23 deep audit's false-positive pattern as the discovery moment.

### Task 4: Debug-doc closeouts

1. Move all three debug docs from `.planning/debug/` to `.planning/debug/resolved/`:
   - `brain-ask-contract-mismatch-rename.md`
   - `brain-topk-uncapped-advisory.md`
   - `stale-install-cache-audit-anti-pattern.md`
2. In each file's frontmatter, set `status: resolved`, `resolved_by: phase-127.2`, `resolved_at: <date>`.
3. Append a `## Resolution` section to each describing the fix that closed it (link to this Plan).
4. Add a 1-paragraph summary entry to `.planning/debug/knowledge-base.md` per resolved RCA so `gsd-debugger` surfaces them as known-pattern hypotheses in future investigations.

### Task 5: Smoke test

1. Create `tests/test-127.2-00-brain-edge-bundle.sh` -- 3 bash assertions:
   - `grep -q "DirectiveEnvelope" mcp-server-brain/lib/brain-ask.cjs` (description rewrite landed)
   - `grep -q "BRAIN_MAX_TOPK" mcp-server-brain/lib/brain-ask.cjs && grep -q "BRAIN_MAX_TOPK" mcp-server-brain/lib/pinecone-tools.cjs` (cap landed in both sites)
   - `grep -q "Source-of-Truth Preamble" docs/RCA-TEMPLATE.md` (preamble landed)
2. Mark executable. Add to `tests/run-all-127.2.sh` if that exists (or create it).

</tasks>

<acceptance>

- [ ] All three debug docs in `.planning/debug/resolved/` with `resolved_by: phase-127.2`.
- [ ] `tests/test-127.2-00-brain-edge-bundle.sh` exits 0.
- [ ] Smoke probe: launching the Brain MCP server and calling `brain_ask({topK: 999999})` returns at most 100 results (or `$BRAIN_MAX_TOPK` if set).
- [ ] Re-read brain_ask tool description from a fresh MCP session and confirm the new wording prepares the consumer for the DirectiveEnvelope return shape.
- [ ] `.planning/debug/knowledge-base.md` carries 3 new resolved-pattern entries.
- [ ] No behavioral change to `brain_query`, `brain_schema`, or `brain_write` (verified by re-running their existing test suites and confirming pass-rate is byte-identical to pre-127.2-00 baseline).

</acceptance>

<notes>

Plan 127.2-00 ships the THREE absorbed RCAs as a single wave-1 plan because each is individually too small to justify its own plan file, and together they form a coherent "Brain edge cleanup" change set. Plan 127.2-01 (the warmup ping) is wave 2 and depends on this plan landing first -- specifically, D-09's cap makes the warmup-ping audit gates trustable (caller can verify "warmup returns one tiny payload, not 999999 methodology rows") and D-10's Preamble makes the cross-surface audit verdicts trustable.

Per CLAUDE.md GSD discipline: this plan is `gap_closure: true` because all three task surfaces were discovered as POST-execution defects (Windows beta-tester audit), not as forward-looking design work. The gap_closure_source frontmatter cross-references the three debug docs that surfaced each finding.

</notes>
