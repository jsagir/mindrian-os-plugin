---
phase: 127.2
plan: 03
subsystem: hotfix
tags: [hotfix, engine-1-act-1, silent-failure, install-fragility, windows-tester, dog-fooding, rs-engine, find-bottlenecks, canon-part-6]
type: execute
status: complete
ship_vehicle: v1.13.0-beta.30
gap_closure: true
gap_closure_source:
  - .planning/debug/resolved/windows-tester-find-bottlenecks-silent-failure-qa-sweep.md
requirements_addressed:
  - BRAIN-EDGE-127.2-F1
  - BRAIN-EDGE-127.2-F2
  - BRAIN-EDGE-127.2-F7
requires:
  - Phase 89 reverse-salient-engine (rs-engine.py + agent layer)
  - Phase 123 install-lifecycle-harness (doctor.cjs class-flag conventions reused)
  - Phase 127.2-00 + 127.2-02 (sibling hotfix waves on the same RCA pattern family)
provides:
  - scripts/doctor.cjs --check-rs-engine (pre-flight Python deps probe; NOT a class flag)
  - lib/agents/reverse-salient-agent.cjs result.detail.diagnostic (stderr-forward contract)
  - commands/find-bottlenecks.md Empty-result UX disambiguation copy
  - tests/test-127.2-03-rs-engine-silent-failure-fixes.sh (7/7 PASS smoke test)
  - .planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md (v1.14.0 architectural stub)
affects:
  - Engine 1 Act 1 surface (5 user-facing commands: find-bottlenecks, whitespace, find-connections, find-analogies, score-innovation)
  - /mos:doctor (new sibling probe -- own dispatch path, exit-code contract, no shared state with existing class flags)
tech-stack:
  added: []
  patterns:
    - "child_process.spawnSync python -c 'import <dep>' for per-dep pre-flight (mirrors Class M pattern)"
    - "stderr last-200-chars truncation -> result.detail.diagnostic on child_process failure (graph-native error-propagation pattern, F2)"
key-files:
  created:
    - tests/test-127.2-03-rs-engine-silent-failure-fixes.sh
    - .planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md
    - .planning/phases/127.2-brain-warmup-ping-hide-mcp-cold-start-latency-inside-larry-s/127.2-03-SUMMARY.md
  modified:
    - scripts/doctor.cjs (Task 2 -- ADD-ONLY: new --check-rs-engine flag + runCheckRsEngine probe + --help text)
    - lib/agents/reverse-salient-agent.cjs (Task 1 -- F2: stderr forwarding into result.detail.diagnostic)
    - commands/find-bottlenecks.md (Task 3 -- F7: Empty-result UX disambiguation)
    - CHANGELOG.md (Task 7 -- v1.13.0-beta.30 entry)
    - .planning/ROADMAP.md (Task 6 -- Phase 134 added via gsd-sdk phase.add)
    - .planning/debug/knowledge-base.md (Task 5 -- new entry appended)
  moved:
    - .planning/debug/windows-tester-find-bottlenecks-silent-failure-qa-sweep.md -> .planning/debug/resolved/ (Task 5)
decisions:
  - "Phase number reassigned 130 -> 134 (130 already taken by lens-engine-skeleton; SDK assigned 134 as next free slot)"
  - "--check-rs-engine is NOT a class flag -- owns its own exit-code contract (0=ready, 1=missing deps). Class-flag-always-exit-0 invariant does NOT apply because this is an environment-readiness probe, not a drift detector."
  - "Probe scope: critical = requests + numpy (rs-engine.py path); umbrella = sentence_transformers + sklearn (broader Engine 1 Act 1). Critical-missing = exit 1; umbrella-missing = warn only."
  - "Python resolution: MINDRIAN_PYTHON > python3 > python (mirrors lib/agents/reverse-salient-agent.cjs:181 pattern; never crashes on either fallback)"
  - "F2 detail field shape upgraded conditionally (string when stderr empty, object {message, diagnostic} when stderr present) to preserve backward compatibility with legacy callers reading detail as a string"
  - "F7 disambiguation copy lives in commands/find-bottlenecks.md (the agent-fallback path) rather than lib/agents/reverse-salient-agent.cjs (which is the agent-success path) -- the no-finding case is rendered by Larry through the command, not by the agent directly"
metrics:
  duration_minutes: 35
  completed_date: 2026-05-23
  tasks_completed: 7
  files_touched: 9
  smoke_test_results: "7/7 PASS"
parallel_session_coordination:
  observation: "Parallel Phase 121.5 session active during execution. Last 121.5 commits on origin/main at session start: 1025a2fc (selector coverage audit + Brain-suggestion template) and 5bdb7667 (brain three-ticket reconciliation). No new 121.5 commits landed during execution (verified via git fetch immediately before commit)."
  uncommitted_121_5_work_observed: "121.5-CONTEXT.md had local working-tree edits at session start (a draft Sub-plan K absorption note from a prior session). Per orchestrator directive (ZERO edits to 121.5 surfaces), this plan did NOT modify or stage that file -- left untouched in working tree for the parallel session to own."
  shared_file_touched: "scripts/doctor.cjs -- ADD-ONLY (new --check-rs-engine flag handler; runCheckRsEngine function; --help text entry). Did NOT refactor any existing acceptance point, class-flag dispatcher, UI-compliance logic, or statusline check. Flag-parsing block extended additively (new else-if branch only)."
  forbidden_surfaces_audit: "ZERO edits to skills/ui-system/, scripts/sessionstart-*, scripts/statusline*, output-styles/, commands/operator.md, commands/doctor.md. Verified via `git diff --stat` -- modified file list contains only Plan 127.2-03 surfaces + non-121.5 .planning/ files."
---

# Phase 127.2 Plan 03: RS-Engine Silent-Failure HOTFIX (F1 + F2 + F7) Summary

The first hotfix shaped from an external tester's transcript (Aryeh's Windows machine, 2026-05-23) -- closes the worst-shape silent failure in the canon-defined product surface: `/mos:find-bottlenecks` was certifying user rooms as bottleneck-free when the Python rs-engine had crashed at module-import.

## One-Liner

Pre-flight Python deps via `/mos:doctor --check-rs-engine`, forward rs-engine stderr to the agent's `result.detail.diagnostic`, disambiguate `/mos:find-bottlenecks` empty-result UX into analyzer-down vs no-findings -- with Phase 134 scaffolded as the v1.14.0 architectural answer (CJS port via @xenova/transformers).

## Tasks Completed (7/7)

| Task | Description | Files | Status |
|------|-------------|-------|--------|
| 1 | F2 -- ReverseSalientAgent forwards stderr last 200 chars to `result.detail.diagnostic` | `lib/agents/reverse-salient-agent.cjs` | DONE |
| 2 | F1 -- `scripts/doctor.cjs --check-rs-engine` pre-flight (ADD-ONLY) | `scripts/doctor.cjs` | DONE |
| 3 | F7 -- `/mos:find-bottlenecks` empty-result UX disambiguation | `commands/find-bottlenecks.md` | DONE |
| 4 | Smoke test | `tests/test-127.2-03-rs-engine-silent-failure-fixes.sh` (7/7 PASS) | DONE |
| 5 | QA sweep doc closeout (frontmatter + Resolution + move + knowledge-base) | RCA + `.planning/debug/knowledge-base.md` | DONE |
| 6 | Phase 134 (was Phase 130) CJS-port architectural-stub scaffolding | `.planning/phases/134-.../134-CONTEXT.md` | DONE |
| 7 | CHANGELOG bullets + (ship ceremony pending atomic commit) | `CHANGELOG.md` | DONE (in-place) |

## Acceptance (All Met)

- [x] `scripts/doctor.cjs --check-rs-engine` returns exit 0 + `ok:true` on the dev machine (all 4 deps present); the exit 1 + actionable fix line path is verified by the JSON-payload contract test.
- [x] `lib/agents/reverse-salient-agent.cjs` failure path emits `result.detail.diagnostic` with stderr tail. Verified by the test's `e.stderr|stderrRaw` pattern grep.
- [x] `/mos:find-bottlenecks` empty-result surface mentions `/mos:doctor --check-rs-engine` as the disambiguation path.
- [x] Smoke test 7/7 PASS.
- [x] QA sweep doc moved to `.planning/debug/resolved/` with `resolved_by` set; knowledge-base entry appended.
- [x] Phase 134 CONTEXT.md exists with v1.14.0 milestone + CJS-port design vision; no PLAN.md (scaffolding only).
- [x] CHANGELOG entry for v1.13.0-beta.30 lists F1/F2/F7 user-readably.
- [ ] All 5 user-facing surfaces synced to v1.13.0-beta.30 post-ship -- DEFERRED to release.sh ceremony (Step F of the orchestrator instructions).
- [x] ZERO edits to skills/ui-system, scripts/sessionstart-*, scripts/statusline*, output-styles/, commands/operator.md, commands/doctor.md.

## Functional Probe Output

```
$ node scripts/doctor.cjs --check-rs-engine --json
{
  "ok": true,
  "ready": true,
  "python": "python3",
  "probes": [
    { "dep": "requests", "ok": true },
    { "dep": "numpy", "ok": true },
    { "dep": "sentence_transformers", "ok": true },
    { "dep": "sklearn", "ok": true }
  ],
  "missing": [],
  "missing_critical": [],
  "missing_umbrella": [],
  "fix": null
}
```

Exit 0 (deps present, ready). On Aryeh's machine class (Windows, no `requests` in py runtime) this would return exit 1 with `missing_critical: ["requests"]` and `fix: "Run: pip install -r requirements-hsi.txt --user ..."`.

## Self-Check: PENDING

Self-check runs after the atomic commit (see Final Report).

## Deviations from Plan

### Phase number reassignment (130 -> 134)

- **Found during:** Task 6 (Phase 130 scaffolding)
- **Issue:** Plan 127.2-03 specified "Phase 130" for the CJS-port architectural stub. Directory `130-lens-engine-skeleton/` already exists on origin/main (assigned in an earlier planning cycle).
- **Fix:** Used `gsd-sdk query phase.add "..."` which assigned 134 as the next free numbered slot. The CONTEXT.md `note_on_phase_number` frontmatter field documents the reassignment so future readers don't get confused by the Plan-vs-Phase number mismatch.
- **Rationale:** Plan 127.2-03 text references "Phase 130" but the work scope is identical to the original spec; only the slot number changes.

### Parallel session working-tree edits observed but not staged

- **Found during:** pre-commit `git status`
- **Issue:** `.planning/phases/121.5-terminal-coherence-capstone/121.5-CONTEXT.md` had uncommitted local working-tree edits at session start (~30 lines of Sub-plan K Bundle A absorption notes -- pairs with the already-pushed `1025a2fc` 121.5 commit). These pre-date this plan's execution.
- **Decision:** Per orchestrator's "ZERO edits to 121.5 surfaces" directive, this plan did NOT stage the 121.5-CONTEXT.md file. Left untouched in working tree for the parallel 121.5 session to own.
- **No code change required from this plan.**

## Known Stubs

- **Phase 134 CONTEXT.md is a scaffold stub.** It is intentional, captures the design vision for the v1.14.0 planning cycle, and explicitly states `status: scaffold` + `No PLAN.md` -- the next phase that consumes it (v1.14.0 plan-phase pass) will produce the PLAN.md.

## Threat Surface Scan

No new attack-surface introduced. The `--check-rs-engine` probe is LOCAL only (spawnSync `python -c "import <dep>"`); zero network surface; Canon Part 8 boundary preserved. F2's stderr forwarding is purely LOCAL diagnostic; never crosses to Brain. F7 copy update is documentation only.

## Cross-References

- **RCA:** `.planning/debug/resolved/windows-tester-find-bottlenecks-silent-failure-qa-sweep.md`
- **Plan:** `.planning/phases/127.2-brain-warmup-ping-hide-mcp-cold-start-latency-inside-larry-s/127.2-03-PLAN.md`
- **Architectural follow-up:** `.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md`
- **Knowledge-base entry:** `.planning/debug/knowledge-base.md` (last entry: "windows-tester-find-bottlenecks-silent-failure-qa-sweep")
- **CHANGELOG:** v1.13.0-beta.30 Unreleased section
- **Sibling hotfix waves:** Plan 127.2-00 (Brain Edge Bundle, beta.26), Plan 127.2-02 (post-ship QA-sweep closeout, beta.28)

## Dog-Fooding Milestone

Per Canon Part 6, this is the FIRST hotfix shaped from an EXTERNAL tester's transcript (not maintainer-discovered like Plans 127.2-00 + 127.2-02). The defect class -- a methodology surface certifying invisible the very problems it was built to surface -- is the worst-shape silent failure in the canon-defined product. Surfacing + closing it in the same week the transcript landed is the empirical evidence that the dog-fooding loop the QA sweep itself flagged as broken (RS-2 thesis: one-person QA is the lagging subsystem) is no longer one-person. The shipping cycle that ships THIS hotfix is itself the dog-food evidence.
