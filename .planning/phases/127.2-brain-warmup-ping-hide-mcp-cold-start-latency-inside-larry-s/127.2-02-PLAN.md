---
phase: 127.2
plan: 02
type: execute
wave: 1
depends_on: [127.2-00]
files_modified:
  - scripts/doctor.cjs
  - mindrian-website/website/src/lib/version.ts
  - .planning/debug/v1.13.0-beta.26-post-ship-qa-sweep.md  (closeout move)
  - .planning/debug/knowledge-base.md
  - tests/test-127.2-02-qa-sweep-fixes.sh
autonomous: true
requirements_addressed:
  - BRAIN-EDGE-127.2-FINDING-A  (website version resolver swap)
  - BRAIN-EDGE-127.2-FINDING-B  (doctor version-of-record uses last shipped tag)
  - BRAIN-EDGE-127.2-FINDING-C  (doctor npx-roundtrip accepts package-executed signal)
gap_closure: true
gap_closure_source:
  - .planning/debug/v1.13.0-beta.26-post-ship-qa-sweep.md
must_haves:
  truths:
    - "Website (mindrianos-jsagirs-projects.vercel.app) shows EXACTLY ONE v1.13.0-beta.* string after the rebuild -- the version users can actually install via npm @next"
    - "doctor --acceptance version-of-record-published PASSES against a Commit-B-placeholder main HEAD by checking the LAST SHIPPED TAG instead of plugin.json's current version"
    - "doctor --acceptance npx-roundtrip PASSES when the package downloads and executes, even if its inner claude invocation errors (the npm artifact's reachability is what matters, not claude's flag schema)"
    - "Phase 127.2 v1.13.0-beta.26 post-ship QA sweep doc moves from .planning/debug/ to .planning/debug/resolved/ with resolved_by: phase-127.2 Plan 127.2-02 + knowledge-base.md summary entry"
  artifacts:
    - path: "scripts/doctor.cjs"
      provides: "version-of-record-published reads last shipped tag via `git describe --tags --abbrev=0 --match='v*'` instead of plugin.json (Finding B); npx-roundtrip accepts exit code 0 OR stderr/stdout containing 'Installing the MindrianOS plugin' as success signal (Finding C)"
      contains: "git describe"
      contains_2: "Installing the MindrianOS plugin"
    - path: "tests/test-127.2-02-qa-sweep-fixes.sh"
      provides: "Smoke test: doctor harness reads tag not plugin.json; website returns single version; CHANGELOG entry exists"
  key_links:
    - from: "scripts/doctor.cjs version-of-record-published"
      to: "git describe --tags --abbrev=0"
      via: "shipped-tag resolution replaces plugin.json read"
      pattern: "git.*describe"
    - from: "scripts/doctor.cjs npx-roundtrip"
      to: "stdout/stderr text-match"
      via: "package-executed-signal pattern beats inner-claude-exit-code"
      pattern: "Installing the MindrianOS plugin"
---

<objective>
Close the 3 actionable findings from the v1.13.0-beta.26 post-ship QA sweep (`.planning/debug/v1.13.0-beta.26-post-ship-qa-sweep.md`) in one wave:

- **Finding A** (NEW low): website renders two version strings simultaneously. Fix: swap resolver priority in `mindrian-website/website/src/lib/version.ts` so npm registry (last shipped) wins over GitHub raw plugin.json (Commit B placeholder).
- **Finding B** (PROCEDURAL): `doctor --acceptance version-of-record-published` fails against Commit-B-state main HEAD because it expects the current plugin.json version to have a tag. Fix: resolve "version of record" via `git describe --tags --abbrev=0 --match='v*'` -- the last shipped tag, not the next-pre-release placeholder.
- **Finding C** (PROCEDURAL): `doctor --acceptance npx-roundtrip` reports exit 127 because the installer's inner `claude --version` invocation fails. Fix: treat "package downloaded and executed" (stdout/stderr matches "Installing the MindrianOS plugin" OR "Adding marketplace") as success regardless of inner-CLI exit code. The npm artifact's reachability is what the gate actually proves; claude's flag schema is out of scope.

This bundle ships as the v1.13.0-beta.27 → bumped to v1.13.0-beta.28 release. Findings D (install-minisite missing origin remote) and E (BRAIN_MAX_TOPK live-wire probe) stay as non-code follow-ups, not in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

### Task 1: Finding A -- website resolver priority swap

1. Edit `/home/jsagi/mindrian-website/website/src/lib/version.ts`. In `getLatestVersion()`, call `fetchFromNpm()` BEFORE `fetchFromGitHub()`. ~3 line reordering.
2. Update the priority-order comment at the top of the file to reflect the swap (npm now wins second, GitHub raw becomes third).
3. Commit + push to GitHub (auto-deploys via Vercel).

### Task 2: Finding B -- doctor version-of-record uses last shipped tag

1. Edit `scripts/doctor.cjs` `acceptance.version-of-record-published` (~lines 2349-2384):
   - Replace `const ver = pj.version;` with a `git describe --tags --abbrev=0 --match='v*'` invocation that returns the LAST SHIPPED tag (without the `v` prefix).
   - Fallback to plugin.json's version if `git describe` returns no tags (fresh repo edge case).
   - Keep all three assertions (git tag exists, marketplace source.ref pinned, npm view matches) -- they all now key off the shipped tag, not the placeholder.

### Task 3: Finding C -- doctor npx-roundtrip accepts package-executed signal

1. Edit `scripts/doctor.cjs` `acceptance.npx-roundtrip` (~lines 2386-2423):
   - In the full path, after `spawnSync`, examine `r.stdout + r.stderr` for "Installing the MindrianOS plugin" or "Adding marketplace" substring.
   - If either substring is present, return `ok: true` regardless of exit code -- the package executed, which is what the gate verifies.
   - If neither substring is present AND exit code is non-zero, return `ok: false` with the existing detail.
   - Light path (`--light-npx`) is unaffected -- `--no-install --help` still works as the cheap probe.

### Task 4: QA sweep doc closeout

1. Update `.planning/debug/v1.13.0-beta.26-post-ship-qa-sweep.md` frontmatter: `status: resolved`, `resolved: 2026-05-23`, `resolved_by: phase-127.2 Plan 127.2-02`.
2. Append a `## Resolution` section listing exactly which findings closed in code (A, B, C) and which stay as TODO (D, E).
3. Move file to `.planning/debug/resolved/`.
4. Add summary entry to `.planning/debug/knowledge-base.md`.

### Task 5: Smoke test

1. Create `tests/test-127.2-02-qa-sweep-fixes.sh`:
   - Assert `scripts/doctor.cjs` contains `git describe --tags --abbrev=0` (Finding B landed).
   - Assert `scripts/doctor.cjs` contains "Installing the MindrianOS plugin" pattern (Finding C landed).
   - Assert `.planning/debug/resolved/v1.13.0-beta.26-post-ship-qa-sweep.md` exists (sweep doc moved).
   - Assert knowledge-base.md contains entry for the QA sweep.
   - (Cross-repo website assertion is documented as out-of-scope here because tests run inside the plugin repo only; verification is `curl + grep` against the live Vercel URL post-deploy.)
2. Run + assert all PASS.

### Task 6: CHANGELOG + ship ceremony

1. Add bullet entries under `## [Unreleased] -- v1.13.0-beta.27 (in progress)` for Findings A + B + C.
2. Run `scripts/release.sh --prerelease --allow-ahead` (bumps beta.27 → beta.28 via the standard 7-place lockstep + npm publish + minisite + Commit B to beta.29).
3. If Step 9.6 minisite hard-aborts again (Finding D unresolved), recover manually: `cd ~/mindrianos-install-site && vercel --prod --yes` then push plugin + marketplace + Commit B by hand.
4. Verify all 5 user-facing surfaces serve v1.13.0-beta.28 (plugin tag, marketplace source.ref, npm @next, install minisite, mindrian website).

</tasks>

<acceptance>

- [ ] `mindrian-website` push lands on GitHub; Vercel rebuild completes; live homepage returns a SINGLE `v1.13.0-beta.*` string (the npm @next value).
- [ ] `scripts/doctor.cjs` `version-of-record-published` PASSES against current main HEAD (beta.27/beta.28 placeholder + v1.13.0-beta.27/.28 tag both present after the release commit lands).
- [ ] `scripts/doctor.cjs` `npx-roundtrip` PASSES against the published `@mindrian_os/install@<latest-beta>` package.
- [ ] `tests/test-127.2-02-qa-sweep-fixes.sh` exits 0 with all assertions PASS.
- [ ] QA sweep doc at `.planning/debug/resolved/v1.13.0-beta.26-post-ship-qa-sweep.md`, knowledge-base.md updated.
- [ ] CHANGELOG entry for v1.13.0-beta.28 lists Findings A + B + C user-readably.
- [ ] All 5 user-facing surfaces synced to v1.13.0-beta.28.

</acceptance>

<notes>

This plan is the "QA sweep ships its own remediation in the very next beta" loop -- exactly the dog-fooding discipline Canon Part 6 demands. The sweep doc applied D-10's new Source-of-Truth Preamble at file time (FIRST audit to do so); this plan closes the sweep's findings within hours of filing it.

Findings D (install-minisite no origin remote) and E (BRAIN_MAX_TOPK live wire probe) stay as non-code follow-ups, NOT as separate plans, because (D) is git config not code and (E) is observability not behavior -- per the RCA standard's classification, neither warrants a new debug session.

</notes>
