---
phase: 124-feynman-temporal-awareness
plan: "03"
type: execute
wave: 2
depends_on: ["124-00", "124-01", "124-02"]
files_modified:
  - scripts/session-start
  - commands/feynman-timeline-refresh.md
  - scripts/feynman-timeline-refresh-command.cjs
  - data/command-registry.json
autonomous: true
requirements:
  - TEMPORAL-124-03
canon_parts:
  - "Part 3 (Tri-Context Decision Gate -- the manual /mos:feynman-timeline-refresh command is a utility surface the user reaches for when the automatic session-start cascade is not enough; the F.0 Action Report shape mirrors /mos:brain-derive's Shape E)"
  - "Part 7 (Reuse Before Build -- the session-start cascade slot mirrors the cache-prune block at line ~1290 verbatim (best-effort wrap + `|| true` + `node -e` env-tunnelled subprocess; never blocks startup); the manual command mirrors the /mos:brain-derive shape (commands/brain-derive.md + scripts/brain-derive-command.cjs); the Phase 122 frontmatter contract + Phase 104 serves_jtbd are honored; the registry regen + pre-commit hook are reused verbatim)"
  - "Part 8 (Graph Boundary -- the runner reads ONLY local room.db; the manual command adds ZERO Brain surface; the registry regeneration's --check tripwire (Phase 122) ensures `frameworks: []` for this utility command does not falsely claim a Brain :Framework name)"
must_haves:
  truths:
    - "scripts/session-start gains a sibling cascade slot AFTER the `# --- END cache prune ---` marker (around line ~1302) and BEFORE the pre-commit-hook self-healing block (line ~1310); the slot runs `lib/core/feynman/timeline-runner.cjs::refreshAll(activeRoomDir)` best-effort via `node -e` with `try/catch` inside + `|| true` outside; never blocks startup"
    - "The cascade slot requires `MINDRIAN_ACTIVE_ROOM` env var (or equivalent active-room resolution from the existing session-start state) to point at the room dir; if absent or non-existent the slot is a no-op and never errors"
    - "commands/feynman-timeline-refresh.md exists with `kind: utility`, `frameworks: []`, `produces: \"room/*/FEYNMAN.md\"`, `inputs: []`, `autonomous_safe: true`, `serves_jtbd: [\"validate-idea\", \"build-knowledge\"]`, `argument-hint: \"[--all | --section <slug>]\"`, `disable-model-invocation: false`, `allowed-tools: Bash(node *)` -- the Phase 122 + Phase 104 contract"
    - "scripts/feynman-timeline-refresh-command.cjs exists; parses `--all` (default) | `--section <slug>`; delegates to `lib/core/feynman/timeline-runner.cjs::refreshAll(roomDir)` or `refreshSection(roomDir, slug)`; opens room.db for the active room; prints an F.0 / Shape E Action Report; exits 0 on success (including soft-fail paths), exit 1 only on invocation errors (no active room, invalid section slug, missing args)"
    - "data/command-registry.json is regenerated via `node scripts/build-command-registry.cjs` and contains the `/mos:feynman-timeline-refresh` entry with `kind: utility`, `frameworks: []`, `produces: room/*/FEYNMAN.md`, `autonomous_safe: true`"
    - "The Phase 122 pre-commit tripwire passes: `node scripts/build-command-registry.cjs --check` exits 0"
    - "Zero em-dashes or en-dashes in any file shipped"
  artifacts:
    - path: "scripts/session-start"
      provides: "Sibling cascade slot after cache-prune that runs the Phase 124 timeline runner best-effort; never blocks startup"
      contains: "feynman-timeline-refresh"
    - path: "commands/feynman-timeline-refresh.md"
      provides: "/mos:feynman-timeline-refresh manual command; Phase 122 + Phase 104 frontmatter; argument-hint for --all and --section <slug>; F.0 Action Report output"
      contains: "kind: utility"
    - path: "scripts/feynman-timeline-refresh-command.cjs"
      provides: "argv parser + runner delegation + Shape E Action Report rendering; mirrors scripts/brain-derive-command.cjs shape"
      min_lines: 100
    - path: "data/command-registry.json"
      provides: "Regenerated by `node scripts/build-command-registry.cjs`; carries the new `/mos:feynman-timeline-refresh` entry plus updated framework_index"
      contains: "feynman-timeline-refresh"
  key_links:
    - from: "scripts/session-start (cascade slot)"
      to: "lib/core/feynman/timeline-runner.cjs::refreshAll"
      via: "node -e 'try { require(...).refreshAll(activeRoomDir, opts); } catch (e) { stderr } || true"
      pattern: "feynman.*timeline-runner|timeline-runner.*refreshAll"
    - from: "commands/feynman-timeline-refresh.md"
      to: "scripts/feynman-timeline-refresh-command.cjs"
      via: "the Invocation block: `node ${CLAUDE_PLUGIN_ROOT}/scripts/feynman-timeline-refresh-command.cjs $ARGUMENTS`"
      pattern: "feynman-timeline-refresh-command\\.cjs"
    - from: "scripts/feynman-timeline-refresh-command.cjs"
      to: "lib/core/feynman/timeline-runner.cjs"
      via: "require('../lib/core/feynman/timeline-runner.cjs') -> refreshAll / refreshSection"
      pattern: "timeline-runner"
    - from: "data/command-registry.json"
      to: "commands/feynman-timeline-refresh.md frontmatter"
      via: "scripts/build-command-registry.cjs scans commands/*.md and re-emits the registry"
      pattern: "feynman-timeline-refresh"
---

<objective>
Wire the renderer (124-01) + runner (124-02) into the two hook surfaces locked in CONTEXT D-04: (a) the session-start cascade slot (ambient, best-effort, mirrors the cache-prune pattern) and (b) the manual `/mos:feynman-timeline-refresh` command (the explicit-redraw escape hatch, mirrors `/mos:brain-derive`). The command's frontmatter honors the Phase 122 contract (`kind`, `frameworks`, `produces`, `inputs`, `autonomous_safe`) plus the Phase 104 `serves_jtbd` convention -- which means regenerating `data/command-registry.json` is the verification gate (the Phase 122 `--check` tripwire fails the build on any drift, so a green re-check confirms the wiring is correct).

Per CONTEXT D-04 (LOCKED hybrid): session-start cascade slot AFTER cache-prune (best-effort `|| true`); manual command `/mos:feynman-timeline-refresh` mirrors `/mos:brain-derive`. Per D-12 (LOCKED manual command surface): the frontmatter keys are exactly as listed in the truths above.

This plan depends on Plan 124-02 (the runner module must exist before session-start can call it AND before the command script can delegate to it). Plan 124-04 (Canon Part 9 invariant test) depends on this plan only via the file ownership pattern (the command script + the session-start edit are tested structurally by 124-04's grep sweep, not by a dedicated test stub -- the wiring's correctness gate is the Phase 122 registry tripwire passing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-feynman-temporal-awareness/124-CONTEXT.md
@.planning/phases/124-feynman-temporal-awareness/124-02-PLAN.md
@lib/core/feynman/timeline-runner.cjs
@scripts/session-start
@commands/brain-derive.md
@scripts/brain-derive-command.cjs
@scripts/build-command-registry.cjs
@data/command-registry.json
@docs/COMMAND-FRONTMATTER.md

<interfaces>
<!-- The session-start cascade slot (insertion point: AFTER `# --- END cache prune ---` line ~1302, BEFORE the pre-commit-hook self-healing block at line ~1310):

```bash
# --- BEGIN feynman timeline refresh (Phase 124, best-effort) ---
# Phase 124-03 (TEMPORAL-124-03). Canon Part 8: LOCAL only -- the timeline-runner
# reads ONLY room.db via lib/core/navigation.cjs (D-03). Refreshes every section's
# FEYNMAN.md `## Timeline (auto)` block when the SQL is newer than the rendered
# watermark (D-09). Best-effort: a failure (`|| true`) never blocks session startup.
# Mirrors the cache-prune cascade pattern verbatim. The active room dir is provided
# by the existing session-start state (the same env vars cache-prune relies on or a
# fallback to the active-room resolution below).
if [ -n "${ROOM_DIR:-}" ] && [ -d "${ROOM_DIR}" ]; then
  env PLUGIN_ROOT_FOR_FEYNMAN="$PLUGIN_ROOT" ACTIVE_ROOM_FOR_FEYNMAN="$ROOM_DIR" node -e '
    try {
      var runner = require(process.env.PLUGIN_ROOT_FOR_FEYNMAN + "/lib/core/feynman/timeline-runner.cjs");
      var roomDir = process.env.ACTIVE_ROOM_FOR_FEYNMAN;
      // The runner opens its own db handle via the navigation chokepoint when the caller
      // does not supply one; for the cascade slot we omit opts.db and let the runner
      // resolve room.db internally. (NOTE: if the runner requires opts.db, this slot
      // falls back to a no-op log line; the manual command always supplies opts.db.)
      var result = runner.refreshAll(roomDir, {});
      if (result.refreshed && result.refreshed.length > 0) {
        process.stderr.write("feynman-timeline-refresh: " + result.refreshed.length + " section(s) refreshed\n");
      }
    } catch (e) {
      process.stderr.write("feynman-timeline-refresh failed: " + (e && e.message) + "\n");
    }
  ' 2>/dev/null || true
fi
# --- END feynman timeline refresh ---
```

The slot is conditional on `ROOM_DIR` being set + the directory existing (`ROOM_DIR` is exported by session-start's `resolve-room` block near L87; if that name has drifted since planning, the executor honors whichever name the surrounding code actually exports -- the action's JS-resolution fallback via `~/MindrianRooms/.rooms/registry.json` covers the case where no shell var is present at all). The cascade slot must work alongside (NOT replace) cache-prune.
-->

<!-- commands/feynman-timeline-refresh.md (full template; mirror commands/brain-derive.md shape):

```markdown
---
description: Refresh FEYNMAN.md ## Timeline (auto) section for one or all room sections
argument-hint: "[--all | --section <slug>]"
serves_jtbd: ["validate-idea", "build-knowledge"]
disable-model-invocation: false
allowed-tools: Bash(node *)
kind: utility
frameworks: []
produces: "room/*/FEYNMAN.md"
inputs: []
autonomous_safe: true
---

# /mos:feynman-timeline-refresh

Force-refresh the `## Timeline (auto)` sentinel-bounded section in each section's `FEYNMAN.md`. The renderer reads from `room.db` via the Phase 109 `navigation.cjs` chokepoint; the runner writes back atomically with the human-authored body byte-preserved. This is the manual knob the user reaches for when the automatic session-start cascade is not enough -- explicit redraw, ignore the watermark.

## Modes

- `/mos:feynman-timeline-refresh` -- refresh every section in the active room (default; equivalent to `--all`).
- `/mos:feynman-timeline-refresh --all` -- refresh every section in the active room.
- `/mos:feynman-timeline-refresh --section <slug>` -- refresh one section.

## Output

F.0 Action Report (Canon Part 3 UI Ruling System; same pattern as `/mos:brain-derive`'s Shape E). Three summary rows + a PER SECTION breakdown + a NEXT action footer:

- Sections refreshed: N
- Sections skipped (watermark fresh): S
- Sections failed: F

When more than 3 target sections, per-section progress streams to stderr as each completes: `[1/7] market-analysis: refreshed`. The final report renders to stdout at the end.

## Graceful degradation

- **No active room**: exit 1 with "Set one with /mos:rooms switch <slug>".
- **Invalid section slug**: exit 1 with a pointer to the active room and a hint to list sections.
- **room.db missing**: single-line "room.db not found for active room; the runner needs the Phase 109 SQL spine" message, exit 0 (soft-fail; the timeline section is enrichment, not a gate).
- **Runner exception**: the failure is logged via `feynman_timeline_refresh_failed` memory_event; the per-section row in the report names the failure; other sections still process.

## Canon references

- **Canon Part 3 (Tri-Context Decision Gate)**: output uses the F.0 / Shape E Action Report so the user sees a LOCAL snapshot of what was refreshed.
- **Canon Part 7 (Reuse Before Build)**: the command wraps `lib/core/feynman/timeline-runner.cjs` (Plan 124-02). No new render logic at the command layer.
- **Canon Part 8 (Graph Boundary)**: zero net new Brain surface. The renderer reads ONLY room.db via navigation.cjs. The runner writes ONLY FEYNMAN.md inside the sentinels.
- **Canon Part 9 (Memory Locality)**: this is the Larry-explains face of `memory_event` -- the rendered timeline section IS the human-readable view of the SQL log.

## Invocation

```bash
node ${CLAUDE_PLUGIN_ROOT}/scripts/feynman-timeline-refresh-command.cjs $ARGUMENTS
```

## Examples

```
/mos:feynman-timeline-refresh
/mos:feynman-timeline-refresh --all
/mos:feynman-timeline-refresh --section market-analysis
```

## Exit codes

| Exit | Meaning |
| --- | --- |
| 0 | Success (including soft-fail paths: no memory_event for any section, room.db missing) |
| 1 | Invocation error: no active room, invalid section slug |
```
-->

<!-- scripts/feynman-timeline-refresh-command.cjs (structural mirror of scripts/brain-derive-command.cjs lines 65-220):

  - 'use strict' + node built-ins only.
  - parseArgv(argv) -> { section: string|null, all: bool }. Defaults to { all: true, section: null } when no args.
  - resolveActiveRoom() -- reuse the same shape as scripts/brain-derive-command.cjs lines 132-164 (reads ~/MindrianRooms/.rooms/registry.json or MINDRIAN_ROOMS_ROOT override). Returns roomDir or null.
  - resolveRoomDb(roomDir) -> sqlite3 handle or null. Looks for roomDir + '/.mindrian/room.db' (the Phase 109 standard path); returns null on missing -- soft-fail.
  - main() -- parse argv; resolve active room; resolve db; call runner.refreshSection or runner.refreshAll; render F.0 Action Report; process.exit appropriately.
  - F.0 Action Report renderer: 3 lines (refreshed N / skipped S / failed F) + a PER SECTION block (one line per result) + a NEXT line ("Run again with --section <slug> to retry" if any failed; "All sections up to date" if everything skipped; "Refreshed N section(s)" on success).
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the session-start cascade slot AFTER cache-prune (best-effort `|| true`)</name>
  <files>scripts/session-start</files>
  <read_first>
    - scripts/session-start lines 1280-1310 (the cache-prune block bounded by `# --- BEGIN cache prune ...` / `# --- END cache prune ---`; the new slot mirrors this verbatim -- shell guard, `env VAR=...` env-tunnel, `node -e '...'` try/catch, `2>/dev/null || true` tail)
    - scripts/session-start (search for the variable that names the active room dir -- it might be `ACTIVE_ROOM_DIR`, `MINDRIAN_ACTIVE_ROOM`, `ROOM_DIR`, or derived from another source like the room registry; use whichever the surrounding code already exports; if no such var exists in session-start, derive the room dir by reading `~/MindrianRooms/.rooms/registry.json` via a small inline `node -e` block matching the resolveActiveRoom() pattern in scripts/brain-derive-command.cjs)
    - .planning/phases/124-feynman-temporal-awareness/124-CONTEXT.md D-04 (the hybrid trigger lock)
    - lib/core/feynman/timeline-runner.cjs (Plan 124-02 -- the `refreshAll(roomDir, opts)` signature; the runner's opts.db is OPTIONAL -- when omitted the runner internally walks the room and finds FEYNMAN.md files, but the watermark check + memory_event logging both require a db; the cascade slot can run without a db and the runner will degrade soft (no watermark check, no memory_event log -- it will simply refresh every FEYNMAN.md if the renderer call succeeds; if the runner REQUIRES a db handle, the slot must open one or the slot becomes a soft no-op))
  </read_first>
  <action>
    Open `scripts/session-start`. Find the cache-prune block (search for `# --- END cache prune ---`; this marker is at line ~1302 in the current file). Immediately AFTER that `# --- END cache prune ---` line and BEFORE the next block (the pre-commit-hook self-healing block which starts around line 1310 with `# ---------------------------------------------------------------------------`), insert the feynman-timeline cascade slot.

    Use this exact bash block (replace `ACTIVE_ROOM_VAR` with whichever variable the surrounding session-start state already exports for the active room dir; if NO such variable exists, use the inline node resolveActiveRoom fallback shown below):

    ```bash

    # --- BEGIN feynman timeline refresh (Phase 124, best-effort) ---
    # Phase 124-03 (TEMPORAL-124-03). Canon Part 8: LOCAL only -- the timeline-runner
    # reads ONLY room.db via lib/core/navigation.cjs (D-03). Refreshes every section's
    # FEYNMAN.md `## Timeline (auto)` block when the SQL is newer than the rendered
    # watermark (D-09). Best-effort: a failure (`|| true`) never blocks session startup.
    # Mirrors the cache-prune cascade pattern verbatim. The slot is a no-op when no
    # active room is resolvable.
    env PLUGIN_ROOT_FOR_FEYNMAN="$PLUGIN_ROOT" node -e '
      try {
        var fs = require("node:fs");
        var path = require("node:path");
        var os = require("node:os");
        // Resolve active room (mirror scripts/brain-derive-command.cjs resolveActiveRoom).
        var roomsRoot = process.env.MINDRIAN_ROOMS_ROOT
          || process.env.MINDRIAN_ROOMS_HOME
          || path.join(os.homedir(), "MindrianRooms");
        var regPath = path.join(roomsRoot, ".rooms", "registry.json");
        if (!fs.existsSync(regPath)) process.exit(0);
        var reg = JSON.parse(fs.readFileSync(regPath, "utf8"));
        if (!reg || !reg.active || !reg.rooms || !reg.rooms[reg.active]) process.exit(0);
        var roomDir = path.resolve(roomsRoot, reg.rooms[reg.active].path || reg.active);
        if (!fs.existsSync(roomDir)) process.exit(0);
        // Try to open room.db so the runner can do the watermark check + memory_event log.
        var db = null;
        try {
          var dbPath = path.join(roomDir, ".mindrian", "room.db");
          if (fs.existsSync(dbPath)) {
            var sqlite = require("node:sqlite");
            db = new sqlite.DatabaseSync(dbPath);
          }
        } catch (_) { db = null; }
        var runner = require(process.env.PLUGIN_ROOT_FOR_FEYNMAN + "/lib/core/feynman/timeline-runner.cjs");
        var result = runner.refreshAll(roomDir, { db: db });
        if (db) { try { db.close(); } catch (_) {} }
        if (result && result.refreshed && result.refreshed.length > 0) {
          process.stderr.write("feynman-timeline-refresh: " + result.refreshed.length + " section(s) refreshed\n");
        }
      } catch (e) {
        process.stderr.write("feynman-timeline-refresh failed: " + (e && e.message) + "\n");
      }
    ' 2>/dev/null || true
    # --- END feynman timeline refresh ---

    ```

    The block requires `PLUGIN_ROOT` to be in scope (it already is -- the cache-prune block uses the same variable). The `|| true` and the `2>/dev/null` redirect at the `node -e` line are belt-and-suspenders so the slot NEVER blocks startup. The `try/catch` inside the JS catches every internal exception; the outer shell `|| true` catches any process-level oddity.

    Do NOT touch the cache-prune block or any other line. The insertion is purely additive between the two markers.
  </action>
  <verify>
    <automated>grep -q "# --- BEGIN feynman timeline refresh" scripts/session-start && grep -q "# --- END feynman timeline refresh ---" scripts/session-start && grep -q "lib/core/feynman/timeline-runner.cjs" scripts/session-start && awk '/# --- END cache prune ---/{found=NR} /# --- BEGIN feynman timeline refresh/{if(found && NR>found) print "OK"; else print "ORDER FAIL"}' scripts/session-start | grep -q "OK" && bash -n scripts/session-start && grep -P "[\x{2014}\x{2013}]" scripts/session-start | grep -q "feynman timeline" && exit 1 || exit 0</automated>
  </verify>
  <done>
    `scripts/session-start` contains the new `# --- BEGIN feynman timeline refresh ...` / `# --- END feynman timeline refresh ---` block immediately AFTER `# --- END cache prune ---`; the block references `lib/core/feynman/timeline-runner.cjs`; `bash -n` parses the script cleanly; zero em-dashes or en-dashes added by this plan (legacy em-dashes elsewhere in session-start are out of scope).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create `commands/feynman-timeline-refresh.md` with Phase 122 + Phase 104 frontmatter</name>
  <files>commands/feynman-timeline-refresh.md</files>
  <read_first>
    - commands/brain-derive.md (the EXACT template this command mirrors -- the description, the modes block, the output block, the canon references, the invocation block, the exit codes table)
    - docs/COMMAND-FRONTMATTER.md (the Phase 122 contract: `kind`, `frameworks`, `produces`, `inputs`, `autonomous_safe` are REQUIRED; `kind: utility` means `frameworks: []` is correct -- no false Brain :Framework claim)
    - .planning/phases/124-feynman-temporal-awareness/124-CONTEXT.md D-12 (the manual command surface lock)
  </read_first>
  <action>
    Create `commands/feynman-timeline-refresh.md`. Use this exact content (no em-dashes, hyphens only; the frontmatter is the Phase 122 + Phase 104 contract):

    ```markdown
    ---
    description: Refresh FEYNMAN.md ## Timeline (auto) section for one or all room sections
    argument-hint: "[--all | --section <slug>]"
    serves_jtbd: ["validate-idea", "build-knowledge"]
    disable-model-invocation: false
    allowed-tools: Bash(node *)
    kind: utility
    frameworks: []
    produces: "room/*/FEYNMAN.md"
    inputs: []
    autonomous_safe: true
    ---

    # /mos:feynman-timeline-refresh

    Force-refresh the `## Timeline (auto)` sentinel-bounded section in each section's `FEYNMAN.md`. The renderer reads from `room.db` via the Phase 109 `navigation.cjs` chokepoint; the runner writes back atomically with the human-authored body byte-preserved. This is the manual knob the user reaches for when the automatic session-start cascade is not enough -- explicit redraw, ignore the watermark.

    ## Modes

    - `/mos:feynman-timeline-refresh` -- refresh every section in the active room (default; equivalent to `--all`).
    - `/mos:feynman-timeline-refresh --all` -- refresh every section in the active room.
    - `/mos:feynman-timeline-refresh --section <slug>` -- refresh one section.

    ## Output

    F.0 Action Report (Canon Part 3 UI Ruling System; same shape as `/mos:brain-derive`'s Shape E). Three summary rows + a PER SECTION breakdown + a NEXT action footer:

    - Sections refreshed: N
    - Sections skipped (watermark fresh): S
    - Sections failed: F

    When more than 3 target sections, per-section progress streams to stderr as each completes: `[1/7] market-analysis: refreshed`. The final report renders to stdout at the end.

    ## Graceful degradation

    - **No active room**: exit 1 with "Set one with /mos:rooms switch <slug>".
    - **Invalid section slug**: exit 1 with a pointer to the active room and a hint to list sections.
    - **room.db missing**: single-line "room.db not found for active room; the runner needs the Phase 109 SQL spine" message, exit 0 (soft-fail; the timeline section is enrichment, not a gate).
    - **Runner exception per section**: the failure is logged via `feynman_timeline_refresh_failed` memory_event; the per-section row in the report names the failure; other sections still process.

    ## Canon references

    - **Canon Part 3 (Tri-Context Decision Gate)**: output uses the F.0 / Shape E Action Report so the user sees a LOCAL snapshot of what was refreshed.
    - **Canon Part 7 (Reuse Before Build)**: the command wraps `lib/core/feynman/timeline-runner.cjs` (Plan 124-02). No new render logic at the command layer.
    - **Canon Part 8 (Graph Boundary)**: zero net new Brain surface. The renderer reads ONLY `room.db` via `navigation.cjs`. The runner writes ONLY FEYNMAN.md inside the sentinels.
    - **Canon Part 9 (Memory Locality)**: this is the Larry-explains face of `memory_event` -- the rendered timeline section IS the human-readable view of the SQL log.

    ## Invocation

    Run the dispatcher via Bash:

    ```bash
    node ${CLAUDE_PLUGIN_ROOT}/scripts/feynman-timeline-refresh-command.cjs $ARGUMENTS
    ```

    ## Examples

    ```
    /mos:feynman-timeline-refresh
    /mos:feynman-timeline-refresh --all
    /mos:feynman-timeline-refresh --section market-analysis
    ```

    ## Exit codes

    | Exit | Meaning |
    | --- | --- |
    | 0 | Success (including soft-fail paths: room.db missing, every section watermark-skipped) |
    | 1 | Invocation error: no active room, invalid section slug, malformed args |

    ## Cross-surface adaptation

    - **CLI**: full power. Slash command resolves via Claude Code's plugin hook substrate and runs the dispatcher through the Bash tool. F.0 report renders in the terminal.
    - **Desktop**: the same slash command runs when the plugin is connected. Larry may narrate the result conversationally; the underlying dispatcher is identical.
    - **Cowork**: same as CLI. Team members see the regenerated FEYNMAN.md `## Timeline (auto)` block through the shared `00_Context/` snapshot on their next session-start.

    No surface-specific code exists anywhere in the dispatcher.
    ```

    NO em-dashes anywhere (hyphens only). NO emoji. The frontmatter MUST match the Phase 122 contract exactly: `kind: utility`, `frameworks: []`, `produces: "room/*/FEYNMAN.md"`, `inputs: []`, `autonomous_safe: true` -- plus the Phase 104 `serves_jtbd: ["validate-idea", "build-knowledge"]` and the standard `description`/`argument-hint`/`allowed-tools`/`disable-model-invocation` keys.
  </action>
  <verify>
    <automated>test -f commands/feynman-timeline-refresh.md && grep -q "^kind: utility" commands/feynman-timeline-refresh.md && grep -q "^frameworks: \[\]" commands/feynman-timeline-refresh.md && grep -q 'produces: "room/\*/FEYNMAN.md"' commands/feynman-timeline-refresh.md && grep -q "^autonomous_safe: true" commands/feynman-timeline-refresh.md && grep -q 'serves_jtbd: \["validate-idea", "build-knowledge"\]' commands/feynman-timeline-refresh.md && grep -q "feynman-timeline-refresh-command.cjs" commands/feynman-timeline-refresh.md && grep -P "[\x{2014}\x{2013}]" commands/feynman-timeline-refresh.md && exit 1 || exit 0</automated>
  </verify>
  <done>
    `commands/feynman-timeline-refresh.md` exists with the full Phase 122 + Phase 104 frontmatter (`kind: utility`, `frameworks: []`, `produces: "room/*/FEYNMAN.md"`, `inputs: []`, `autonomous_safe: true`, `serves_jtbd: ["validate-idea", "build-knowledge"]`); references the dispatcher script; zero em-dashes or en-dashes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create `scripts/feynman-timeline-refresh-command.cjs` as the argv parser + runner delegate</name>
  <files>scripts/feynman-timeline-refresh-command.cjs</files>
  <read_first>
    - scripts/brain-derive-command.cjs lines 1-220 (the EXACT shape this dispatcher mirrors: header docstring, parseArgv, resolveActiveRoom, main, F.0 / Shape E Action Report rendering)
    - lib/core/feynman/timeline-runner.cjs (Plan 124-02 -- the `refreshAll(roomDir, opts)` and `refreshSection(roomDir, sectionSlug, opts)` signatures the dispatcher delegates to)
    - .planning/phases/124-feynman-temporal-awareness/124-CONTEXT.md D-12 (the manual command surface lock)
  </read_first>
  <action>
    Create `scripts/feynman-timeline-refresh-command.cjs` (CJS, `'use strict'`, node built-ins only -- NO new deps). Target size: ~120-160 lines. Structure:

    ```javascript
    #!/usr/bin/env node
    'use strict';

    /*
     * Copyright (c) 2026 Mindrian. BSL 1.1.
     *
     * Phase 124-03 -- /mos:feynman-timeline-refresh CLI dispatcher
     * ============================================================
     * Manual user command to force-refresh the `## Timeline (auto)` sentinel-bounded
     * block in each section's FEYNMAN.md. When the automatic session-start cascade
     * (Plan 124-03 cascade slot in scripts/session-start) is not enough -- the user
     * wants an explicit redraw -- this is the direct knob.
     *
     * Three invocation modes:
     *
     *   node scripts/feynman-timeline-refresh-command.cjs
     *   node scripts/feynman-timeline-refresh-command.cjs --all
     *     -- Refresh every section in the active room.
     *
     *   node scripts/feynman-timeline-refresh-command.cjs --section <slug>
     *     -- Refresh one section. Exit 1 on bad/missing slug.
     *
     * Output:
     *   F.0 Action Report (Canon Part 3 UI Ruling System; same shape as
     *   /mos:brain-derive's Shape E). Three summary rows + a PER SECTION breakdown +
     *   a NEXT action footer.
     *
     * Canon Part 8:
     *   This dispatcher adds ZERO net new Brain surface. It is a thin wrapper around
     *   lib/core/feynman/timeline-runner.cjs (Plan 124-02), which itself reads ONLY
     *   room.db via lib/core/navigation.cjs (D-03).
     *
     * Pure CJS, node built-ins only. Three-surface safe (CLI + Desktop MCP + Cowork).
     */

    const fs = require('node:fs');
    const os = require('node:os');
    const path = require('node:path');

    const REPO_ROOT = path.resolve(__dirname, '..');
    const runner = require(path.join(REPO_ROOT, 'lib', 'core', 'feynman', 'timeline-runner.cjs'));

    // ---------- argv parser ----------

    function parseArgv(argv) {
      const out = { section: null, all: false, help: false };
      if (!Array.isArray(argv)) return out;
      const startIdx = argv.length >= 2 && typeof argv[1] === 'string' && argv[1].endsWith('feynman-timeline-refresh-command.cjs') ? 2 : 0;
      for (let i = startIdx; i < argv.length; i++) {
        const a = argv[i];
        if (typeof a !== 'string') continue;
        if (a === '--all') { out.all = true; continue; }
        if (a === '--section') {
          const next = argv[i + 1];
          if (typeof next === 'string' && !next.startsWith('-')) { out.section = next; i += 1; }
          continue;
        }
        if (a === '--help' || a === '-h') { out.help = true; continue; }
        if (a.startsWith('-')) continue;
        if (out.section == null && !out.all) out.section = a;
      }
      // Default: --all when no args.
      if (!out.section && !out.all && !out.help) out.all = true;
      return out;
    }

    // ---------- Active room resolution ----------

    function safeIsFile(p) { try { return fs.statSync(p).isFile(); } catch (_) { return false; } }
    function safeIsDir(p)  { try { return fs.statSync(p).isDirectory(); } catch (_) { return false; } }

    function resolveActiveRoom() {
      const roomsRoot = process.env.MINDRIAN_ROOMS_ROOT
        || process.env.MINDRIAN_ROOMS_HOME
        || path.join(os.homedir(), 'MindrianRooms');
      const registryPath = path.join(roomsRoot, '.rooms', 'registry.json');
      if (!safeIsFile(registryPath)) return null;
      let reg;
      try { reg = JSON.parse(fs.readFileSync(registryPath, 'utf8')); } catch (_) { return null; }
      if (!reg || typeof reg !== 'object') return null;
      const activeSlug = reg.active;
      if (!activeSlug || typeof activeSlug !== 'string') return null;
      const entry = reg.rooms && reg.rooms[activeSlug];
      if (!entry || typeof entry !== 'object') return null;
      const relPath = entry.path || activeSlug;
      const absPath = path.resolve(roomsRoot, relPath);
      if (!safeIsDir(absPath)) return null;
      return absPath;
    }

    function resolveRoomDb(roomDir) {
      const dbPath = path.join(roomDir, '.mindrian', 'room.db');
      if (!safeIsFile(dbPath)) return null;
      try {
        const sqlite = require('node:sqlite');
        return new sqlite.DatabaseSync(dbPath);
      } catch (_) {
        return null;
      }
    }

    // ---------- F.0 Action Report renderer ----------

    function renderReport(result) {
      const lines = [];
      const refreshedN = (result.refreshed || []).length;
      const skippedN = (result.skipped || []).length;
      const failedN = (result.failed || []).length;
      lines.push('Phase 124 -- /mos:feynman-timeline-refresh');
      lines.push('-------------------------------------------');
      lines.push('Sections refreshed: ' + refreshedN);
      lines.push('Sections skipped (watermark fresh): ' + skippedN);
      lines.push('Sections failed: ' + failedN);
      lines.push('');
      if (refreshedN + skippedN + failedN > 0) {
        lines.push('PER SECTION:');
        for (const r of (result.refreshed || [])) {
          lines.push('  refreshed:  ' + r.slug + '  (watermark: ' + (r.watermark || '') + ')');
        }
        for (const r of (result.skipped || [])) {
          lines.push('  skipped:    ' + r.slug + '  (reason: ' + (r.reason || '') + ')');
        }
        for (const r of (result.failed || [])) {
          lines.push('  failed:     ' + r.slug + '  (reason: ' + (r.reason || '') + ')');
        }
        lines.push('');
      }
      if (failedN > 0) {
        lines.push('NEXT: Run again with --section <slug> to retry failed sections.');
      } else if (refreshedN === 0 && skippedN > 0) {
        lines.push('NEXT: All sections up to date.');
      } else {
        lines.push('NEXT: Refreshed ' + refreshedN + ' section(s).');
      }
      return lines.join('\n');
    }

    // ---------- main ----------

    function main() {
      const opts = parseArgv(process.argv);
      if (opts.help) {
        process.stdout.write('Usage: /mos:feynman-timeline-refresh [--all | --section <slug>]\n');
        process.exit(0);
      }
      const roomDir = resolveActiveRoom();
      if (!roomDir) {
        process.stderr.write('No active room. Set one with /mos:rooms switch <slug>.\n');
        process.exit(1);
      }
      // Section mode: validate slug is a directory in the room.
      if (opts.section) {
        const secDir = path.join(roomDir, opts.section);
        if (!safeIsDir(secDir)) {
          process.stderr.write('Invalid section slug: "' + opts.section + '". (Not a directory under ' + roomDir + '.) Run /mos:rooms to see available sections.\n');
          process.exit(1);
        }
      }
      const db = resolveRoomDb(roomDir);
      // room.db missing is a soft-fail: the runner can still attempt the refresh but
      // without the watermark check + memory_event log.
      if (!db) {
        process.stdout.write('room.db not found for active room; the runner needs the Phase 109 SQL spine. Skipping refresh.\n');
        process.exit(0);
      }
      let result;
      try {
        if (opts.section) {
          const r = runner.refreshSection(roomDir, opts.section, { db: db });
          if (r.status === 'refreshed') result = { refreshed: [{ slug: opts.section, watermark: r.watermark }], skipped: [], failed: [] };
          else if (r.status === 'failed') result = { refreshed: [], skipped: [], failed: [{ slug: opts.section, reason: r.reason }] };
          else result = { refreshed: [], skipped: [{ slug: opts.section, reason: r.reason || r.status }], failed: [] };
        } else {
          result = runner.refreshAll(roomDir, { db: db });
        }
      } finally {
        try { db.close(); } catch (_) {}
      }
      process.stdout.write(renderReport(result) + '\n');
      process.exit(0);
    }

    if (require.main === module) main();

    module.exports = { parseArgv, resolveActiveRoom, resolveRoomDb, renderReport };
    ```

    NO em-dashes anywhere (hyphens only). NO emoji. The dispatcher requires `node:fs`, `node:os`, `node:path`, and the project `lib/core/feynman/timeline-runner.cjs` -- nothing else. Lazy-requires `node:sqlite` inside `resolveRoomDb` so the file loads cleanly even on Node versions without it (the dispatcher then soft-fails with the "room.db not found" message).
  </action>
  <verify>
    <automated>test -f scripts/feynman-timeline-refresh-command.cjs && node -c scripts/feynman-timeline-refresh-command.cjs && node -e "const d = require('./scripts/feynman-timeline-refresh-command.cjs'); ['parseArgv','resolveActiveRoom','resolveRoomDb','renderReport'].forEach(k => { if (typeof d[k] !== 'function') { console.error('missing: ' + k); process.exit(1); } });" && node -e "const d = require('./scripts/feynman-timeline-refresh-command.cjs'); const a = d.parseArgv(['node', 'scripts/feynman-timeline-refresh-command.cjs']); if (!a.all || a.section !== null) { console.error('default not --all'); process.exit(1); } const b = d.parseArgv(['node', 'scripts/feynman-timeline-refresh-command.cjs', '--section', 'market-analysis']); if (b.section !== 'market-analysis') { console.error('section parse failed'); process.exit(1); }" && grep -P "[\x{2014}\x{2013}]" scripts/feynman-timeline-refresh-command.cjs && exit 1 || exit 0</automated>
  </verify>
  <done>
    `scripts/feynman-timeline-refresh-command.cjs` exists; parses cleanly; exports `parseArgv` / `resolveActiveRoom` / `resolveRoomDb` / `renderReport`; defaults to `--all` when no args; honors `--section <slug>`; delegates to `lib/core/feynman/timeline-runner.cjs`; zero em-dashes or en-dashes.
  </done>
</task>

<task type="auto">
  <name>Task 4: Regenerate `data/command-registry.json` via the Phase 122 generator and confirm the Phase 122 `--check` tripwire passes</name>
  <files>data/command-registry.json</files>
  <read_first>
    - scripts/build-command-registry.cjs (the Phase 122 registry generator; the `--check` flag is the drift tripwire)
    - data/command-registry.json (the current registry; the new `/mos:feynman-timeline-refresh` entry must appear here after regen)
    - docs/COMMAND-FRONTMATTER.md (the Phase 122 contract -- `kind: utility` requires `frameworks: []`; the `--check` tripwire fails the build on any drift OR an unresolvable framework)
  </read_first>
  <action>
    Run the Phase 122 registry generator to scan `commands/*.md` and re-emit `data/command-registry.json`:

    ```bash
    node scripts/build-command-registry.cjs
    ```

    This SHOULD succeed (exit 0) because the new `commands/feynman-timeline-refresh.md` declares `kind: utility` with `frameworks: []` -- which is the no-framework case and never triggers the "unresolvable framework" tripwire. The generator scans the directory, parses the new file's frontmatter, adds `/mos:feynman-timeline-refresh` to `commands[]` with the Phase 122 schema, and updates `framework_index` (which stays unchanged because the new entry has no frameworks).

    Then verify the `--check` tripwire passes (this is the gate the pre-commit hook runs):

    ```bash
    node scripts/build-command-registry.cjs --check
    ```

    Both must exit 0. If `--check` exits non-zero, inspect the stderr: most commonly the new frontmatter has a typo or missing required key, or the YAML formatting is off (e.g. a value not properly quoted). Fix the frontmatter in `commands/feynman-timeline-refresh.md` and re-run.

    Do NOT hand-edit `data/command-registry.json` directly -- it is the GENERATED artifact (per the `generated_note` field). Only the generator writes to it.
  </action>
  <verify>
    <automated>node scripts/build-command-registry.cjs && node scripts/build-command-registry.cjs --check && grep -q "/mos:feynman-timeline-refresh" data/command-registry.json && node -e "const r = require('./data/command-registry.json'); const e = r.commands.find(c => c.command === '/mos:feynman-timeline-refresh'); if (!e) { console.error('entry missing'); process.exit(1); } if (e.kind !== 'utility') { console.error('kind not utility: ' + e.kind); process.exit(1); } if (!Array.isArray(e.frameworks) || e.frameworks.length !== 0) { console.error('frameworks not empty array'); process.exit(1); } if (e.produces !== 'room/*/FEYNMAN.md') { console.error('produces wrong: ' + e.produces); process.exit(1); } if (e.autonomous_safe !== true) { console.error('autonomous_safe wrong'); process.exit(1); }"</automated>
  </verify>
  <done>
    `data/command-registry.json` regenerated; the `/mos:feynman-timeline-refresh` entry exists with `kind: utility`, `frameworks: []`, `produces: "room/*/FEYNMAN.md"`, `autonomous_safe: true`; the Phase 122 `--check` tripwire exits 0; the pre-commit hook will pass.
  </done>
</task>

</tasks>

<verification>
- `scripts/session-start` carries the new cascade slot AFTER `# --- END cache prune ---`; `bash -n scripts/session-start` parses cleanly.
- `commands/feynman-timeline-refresh.md` exists with the full Phase 122 + Phase 104 frontmatter.
- `scripts/feynman-timeline-refresh-command.cjs` exists; `node -c` passes; exports `parseArgv`, `resolveActiveRoom`, `resolveRoomDb`, `renderReport`.
- `node scripts/build-command-registry.cjs --check` exits 0 (the Phase 122 tripwire passes -- this is the verification gate for D-12).
- `data/command-registry.json` carries the new `/mos:feynman-timeline-refresh` entry.
- `bash tests/run-all-124.sh` -- still 3 of 4 (renderer + empty-state + runner); canon-part-9-invariant remains RED (filled by Plan 124-04).
- `node lib/memory/run-feynman-tests.cjs` runs to completion.
- `grep -P "[\x{2014}\x{2013}]" commands/feynman-timeline-refresh.md scripts/feynman-timeline-refresh-command.cjs` returns zero matches; the new block in scripts/session-start has zero em-dashes/en-dashes (legacy ones elsewhere in session-start are out of scope).
</verification>

<success_criteria>
Plan 124-03 (the wiring) is shippable when:
1. `scripts/session-start` runs the runner best-effort after cache-prune; never blocks startup; soft no-op when there is no active room.
2. `commands/feynman-timeline-refresh.md` exists with the Phase 122 `kind: utility` + `frameworks: []` + `produces: "room/*/FEYNMAN.md"` + `inputs: []` + `autonomous_safe: true` frontmatter, plus Phase 104 `serves_jtbd: ["validate-idea", "build-knowledge"]`, plus `argument-hint`, `description`, `allowed-tools`, `disable-model-invocation`.
3. `scripts/feynman-timeline-refresh-command.cjs` parses `--all` (default) and `--section <slug>`, delegates to the runner, renders the F.0 Action Report.
4. `data/command-registry.json` is regenerated and includes the new entry.
5. `node scripts/build-command-registry.cjs --check` exits 0.
6. Zero em-dashes or en-dashes in any new content.
</success_criteria>

<output>
After completion, create `.planning/phases/124-feynman-temporal-awareness/124-03-SUMMARY.md` recording: artifacts created (session-start slot + commands/feynman-timeline-refresh.md + scripts/feynman-timeline-refresh-command.cjs + data/command-registry.json regen), the registry tripwire confirmation (`--check` exits 0), the hybrid hook trigger now active (session-start cascade + manual command), and the 1 RED stub that remains (canon-part-9-invariant, filled by Plan 124-04). Note any deviations from the plan.
</output>
