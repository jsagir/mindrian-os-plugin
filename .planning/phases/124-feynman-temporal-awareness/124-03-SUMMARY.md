---
phase: 124-feynman-temporal-awareness
plan: "03"
subsystem: feynman-temporal-awareness
tags: [wave-2, wiring, session-start-cascade, manual-command, command-registry, canon-part-3, canon-part-7, canon-part-8, canon-part-9, jtbd-taxonomy-fix]

# Dependency graph
requires:
  - phase: 124-00
    provides: "Wave-0 substrate (test runner + ROOM.md + EVENT_TYPES floor + Feynman runner registration). Already shipped 2026-05-13."
  - phase: 124-01
    provides: "lib/core/feynman/timeline-renderer.cjs pure renderer + firstCapturedLastTouchedBySection navigation re-export. Already shipped 2026-05-13."
  - phase: 124-02
    provides: "lib/core/feynman/timeline-runner.cjs::refreshAll + refreshSection orchestrator + EVENT_TYPES +2. Already shipped 2026-05-13."
  - phase: 122-workflow-layer
    provides: "scripts/build-command-registry.cjs generator + --check tripwire + data/command-registry.json layout convention. Used here to regenerate the registry + verify the new frontmatter passes the gate."
  - phase: 104-jtbd-content-layer
    provides: "serves_jtbd closed-vocabulary contract (13 ids in lib/hmi/jtbd-taxonomy.json) + tests/test-command-jtbd-declarations.cjs enforcement gate. Used here as the validity check on the new command markdown."
provides:
  - "scripts/session-start: sibling cascade slot AFTER `# --- END cache prune ---` that runs lib/core/feynman/timeline-runner.cjs::refreshAll(activeRoomDir) best-effort (`|| true`); never blocks startup; soft no-op when no active room or no room.db; D-04 hybrid hook trigger half-A"
  - "commands/feynman-timeline-refresh.md: /mos:feynman-timeline-refresh manual command markdown; full Phase 122 frontmatter (kind: utility, frameworks: [], inputs: [], produces: room/*/FEYNMAN.md, autonomous_safe: true) + Phase 104 serves_jtbd ([validate-idea, audit-room]) + standard keys (description, argument-hint, allowed-tools, disable-model-invocation); D-12 manual command surface half-B"
  - "scripts/feynman-timeline-refresh-command.cjs: 257-line CJS dispatcher; node built-ins only; parseArgv (defaults to --all when no args; honors --section <slug>); resolveActiveRoom mirrors the brain-derive-command resolver pattern (~/MindrianRooms/.rooms/registry.json); resolveRoomDb opens room.db via node:sqlite lazy-require; F.0 / Shape E Action Report renderer; per-section progress streaming to stderr when total > 3; exits 0 on success + soft-fail, exit 1 on no-active-room + invalid-section-slug"
  - "data/command-registry.json: regenerated by scripts/build-command-registry.cjs; gains /mos:feynman-timeline-refresh entry with kind: utility, frameworks: [], produces: room/*/FEYNMAN.md, inputs: [], autonomous_safe: true, body_shape: null; total command count 85 -> 86; Phase 122 --check tripwire passes (exit 0); pre-commit hook passes on clean tree"
affects:
  - "lib/core/feynman/timeline-runner.cjs (the consumer this plan wires into the two user-facing surfaces; the runner itself is unchanged byte-identical from Plan 124-02)"
  - "bash tests/run-all-124.sh (stays 3/4 GREEN -- canon-part-9-invariant RED-by-design for Plan 124-04; this plan does not fill that stub)"
  - "node tests/test-command-jtbd-declarations.cjs (still passes: 86 files; all carry valid serves_jtbd declarations; closed-vocabulary check ok)"

# Tech tracking
tech-stack:
  added: []  # zero new npm deps; node built-ins only (node:fs, node:os, node:path, node:sqlite lazy-require)
  patterns:
    - "Session-start cascade slot best-effort idiom: env-tunneled node -e + try/catch inside + 2>/dev/null + || true outside; never blocks startup; mirrors the cache-prune cascade block at line 1280-1304 verbatim (Phase 123)"
    - "Active-room resolver fallback: prefer the surrounding shell ROOM_DIR; otherwise read ~/MindrianRooms/.rooms/registry.json directly via inline node code. This lets the cascade slot work even when session-start has not yet exported ROOM_DIR at the insertion point (defensive)"
    - "F.0 / Shape E Action Report shape: 3 summary rows + PER SECTION breakdown + NEXT footer; mirrors scripts/brain-derive-command.cjs::renderShapeE; the canonical Canon Part 3 UI Ruling System output shape for utility commands"
    - "Lazy-require node:sqlite inside resolveRoomDb so the dispatcher loads cleanly even on Node versions without it; the dispatcher then soft-fails with the room.db not found message (instead of throwing on require time)"
    - "Per-section progress streaming to stderr when total > 3 (mirrors STREAM_PROGRESS_THRESHOLD in brain-derive-command.cjs)"

key-files:
  created:
    - "commands/feynman-timeline-refresh.md (78 lines; Phase 122 + Phase 104 frontmatter)"
    - "scripts/feynman-timeline-refresh-command.cjs (257 lines; pure CJS dispatcher; node built-ins only)"
  modified:
    - "scripts/session-start (49 lines added between line 1304 and the pre-commit-hook self-healing block at line ~1355; sibling cascade slot bounded by `# --- BEGIN feynman timeline refresh (Phase 124, best-effort) ---` / `# --- END feynman timeline refresh ---`)"
    - "data/command-registry.json (+9 lines; 1 new entry; total commands 85 -> 86; generated by scripts/build-command-registry.cjs)"
    - ".planning/ROADMAP.md (Phase 124 block: 124-03-PLAN.md plan-list checkbox flipped to [x]; Plans line 3/5 -> 4/5)"

key-decisions:
  - "Active-room resolution in the session-start slot uses BOTH ROOM_DIR (surrounding shell state) AND the registry-fallback pattern. The plan's CONTEXT noted ROOM_DIR is the canonical var (verified at session-start line 87: `ROOM_DIR=$(\"${SCRIPT_DIR}/resolve-room\" \"$WORK_DIR\") || ROOM_DIR=\"\"`). The slot prefers the shell var when present (cheap, already resolved) and falls back to reading ~/MindrianRooms/.rooms/registry.json via inline node when absent. This makes the slot robust to any drift in upstream session-start state."
  - "node:sqlite is opened in the session-start slot AND in the dispatcher. The runner needs a db handle for D-09 watermark check + D-10 memory_event logging; without a db it would soft-fail (the runner's refreshSection on db=undefined still runs the renderer but skips watermark + logging). Both the session-start slot and the dispatcher open the db themselves so the runner gets the full feature surface."
  - "Per CONTEXT D-12 the plan-suggested serves_jtbd value was [validate-idea, build-knowledge]. The canonical Phase 104 JTBD taxonomy (lib/hmi/jtbd-taxonomy.json) has exactly 13 closed-vocabulary ids -- and `build-knowledge` is NOT one of them. Replaced with `audit-room` (Rule 1 fix; see Deviations). `audit-room` is semantically correct for the timeline refresh: surfacing what the room currently knows about its captured insight events IS the audit-room JTBD."
  - "Dispatcher exits 0 on the room.db-missing soft-fail path per the command markdown contract (`timeline section is enrichment, not a gate`). Exit 1 is reserved for invocation errors (no active room, invalid section slug). This matches the brain-derive-command.cjs Brain-offline-from-start soft-fail idiom verbatim."
  - "Frontmatter ordering in commands/feynman-timeline-refresh.md mirrors commands/brain-derive.md verbatim, with the Phase 122 keys (kind, frameworks, produces, inputs, autonomous_safe) appended after the standard keys (description, argument-hint, serves_jtbd, disable-model-invocation, allowed-tools). The build-command-registry generator is order-insensitive, but the convention keeps the file readable."
  - "Default mode is --all (refresh every section) when no args are passed. The dispatcher parseArgv sets out.all = true at the bottom when neither --section nor --all is present. This matches the command markdown's stated default behavior."

patterns-established:
  - "Sibling cascade slot pattern: a Phase-NN best-effort node -e block placed AFTER `# --- END cache prune ---` and BEFORE the pre-commit-hook self-healing block, bounded by phase-named BEGIN/END comment markers. Future Phase 125+ session-start cascade slots should follow this insertion-point convention (additive, after cache-prune, never blocks startup, mirrors the cache-prune idiom byte-for-byte except for the inner body)."
  - "Manual command + colocated dispatcher pair: commands/<thing>.md + scripts/<thing>-command.cjs. The .md carries the Phase 122 frontmatter contract + Phase 104 serves_jtbd; the .cjs carries parseArgv + resolveActiveRoom + the F.0 / Shape E renderer + a never-throws main. This is now the canonical idiom for utility commands (Phase 90 brain-derive, Phase 124 feynman-timeline-refresh)."

requirements-completed:
  - "TEMPORAL-124-03 (D-04 hybrid hook trigger + D-12 manual command surface): COMPLETE. Both surfaces live: (a) session-start cascade slot at scripts/session-start lines 1306-1353, and (b) /mos:feynman-timeline-refresh manual command (markdown + dispatcher + registry entry + --check tripwire green)."

# Metrics
duration: 3min
completed: 2026-05-13
---

# Phase 124 Plan 03: FEYNMAN.md Timeline Wiring Summary

**Wave 2 wiring: the renderer (Plan 124-01) + runner (Plan 124-02) are now wired into the two user-facing surfaces locked by CONTEXT D-04 + D-12 -- (a) the session-start cascade slot (ambient, best-effort, fires on every fresh session) and (b) the /mos:feynman-timeline-refresh manual command (explicit redraw escape hatch). The Phase 122 command-registry regen + --check tripwire confirms the new command's frontmatter is well-formed. tests/run-all-124.sh stays 3/4 GREEN; the canon-part-9-invariant suite remains RED-by-design for Plan 124-04.**

## What Shipped

### 1. `scripts/session-start` cascade slot (TEMPORAL-124-03 D-04 half-A)

A sibling cascade block bounded by `# --- BEGIN feynman timeline refresh (Phase 124, best-effort) ---` / `# --- END feynman timeline refresh ---` inserted immediately AFTER `# --- END cache prune ---` (line 1304) and BEFORE the pre-commit-hook self-healing block. The slot:

1. Pulls `PLUGIN_ROOT` + `ROOM_DIR` from the surrounding shell env (verified at session-start line 87 where the resolve-room block sets `ROOM_DIR`).
2. Inside `node -e`, prefers `ACTIVE_ROOM_FOR_FEYNMAN` (the env-tunneled shell `ROOM_DIR`) -- if absent or empty, falls back to reading `~/MindrianRooms/.rooms/registry.json` directly to resolve the active room (mirror of the resolveActiveRoom pattern in `scripts/brain-derive-command.cjs`).
3. Opens `roomDir/.mindrian/room.db` via `node:sqlite` when present so the runner can perform the D-09 watermark check + D-10 memory_event logging.
4. Calls `runner.refreshAll(roomDir, { db: db })` and streams a single-line stderr count of refreshed sections when > 0 (silent on zero-refresh runs).
5. The outer `2>/dev/null || true` + the inner try/catch ensure the slot NEVER blocks startup -- mirrors the cache-prune cascade idiom verbatim.

`bash -n scripts/session-start` exits 0 (syntax clean). Zero em-dashes/en-dashes in the new block (legacy ones elsewhere in session-start are out of scope).

### 2. `commands/feynman-timeline-refresh.md` (TEMPORAL-124-03 D-12 half-B / markdown side)

77-line user-facing command markdown carrying the Phase 122 frontmatter contract + Phase 104 serves_jtbd convention:

```yaml
description: Refresh FEYNMAN.md ## Timeline (auto) section for one or all room sections
argument-hint: "[--all | --section <slug>]"
serves_jtbd: ["validate-idea", "audit-room"]
disable-model-invocation: false
allowed-tools: Bash(node *)
kind: utility
frameworks: []
produces: "room/*/FEYNMAN.md"
inputs: []
autonomous_safe: true
```

Body mirrors `commands/brain-derive.md` shape: Modes (3 invocation forms) + Output (F.0 / Shape E Action Report) + Graceful degradation (no active room, invalid slug, room.db missing, runner exception per section) + Canon references (Parts 3 + 7 + 8 + 9) + Invocation block + Examples + Exit codes table + Cross-surface adaptation. Hyphens only; zero em-dashes/en-dashes.

### 3. `scripts/feynman-timeline-refresh-command.cjs` (TEMPORAL-124-03 D-12 half-B / dispatcher side)

257-line pure CJS dispatcher. Node built-ins only: `node:fs`, `node:os`, `node:path`, `node:sqlite` (lazy-required inside `resolveRoomDb`). Five exported functions:

- `parseArgv(argv)` -- defaults to `{ all: true, section: null }` when no args; honors `--section <slug>`, `--all`, `--help`. Handles the [node, script, ...rest] vs [...rest] argv shapes.
- `resolveActiveRoom(rootsOverride)` -- mirrors the brain-derive-command resolver pattern: reads `~/MindrianRooms/.rooms/registry.json` (or `MINDRIAN_ROOMS_ROOT` override) and returns the active room dir or null.
- `resolveRoomDb(roomDir)` -- opens `roomDir/.mindrian/room.db` via `node:sqlite` (lazy-required so the file loads even on Node versions without it); returns null on missing / unavailable.
- `renderReport(result)` -- F.0 / Shape E Action Report renderer: 3 summary rows (refreshed N / skipped S / failed F) + PER SECTION breakdown + NEXT footer.
- `main()` -- never-throws main; delegates to `runner.refreshSection` or `runner.refreshAll`; per-section progress streams to stderr when total > 3 (mirrors brain-derive-command's STREAM_PROGRESS_THRESHOLD pattern).

Exit codes: 0 on success + every soft-fail (room.db missing, every-section-watermark-skipped); 1 only on invocation errors (no active room, invalid section slug). Verified by `node -e` runtime invocation against `MINDRIAN_ROOMS_ROOT=/tmp/nonexistent` (exit 1, "No active room" stderr).

### 4. `data/command-registry.json` regenerated (TEMPORAL-124-03 verification gate)

`node scripts/build-command-registry.cjs` ran clean and added the `/mos:feynman-timeline-refresh` entry:

```json
{
  "command": "/mos:feynman-timeline-refresh",
  "kind": "utility",
  "frameworks": [],
  "produces": "room/*/FEYNMAN.md",
  "inputs": [],
  "autonomous_safe": true,
  "body_shape": null
}
```

Total command count: 85 -> 86. Diff is exactly +9 lines (clean entry append). `node scripts/build-command-registry.cjs --check` exits 0 -- the Phase 122 tripwire passes, which is the verification gate D-12 specifies. `bash .git/hooks/pre-commit` exits 0 on the clean working tree (no regression in any Phase 122 hook).

## Four Commits, Atomic, Explicit Pathspec, --no-verify

| Task | Commit  | Files                                                  |
| ---- | ------- | ------------------------------------------------------ |
| 1    | 78c7cf1 | scripts/session-start                                  |
| 2    | ace1b3e | commands/feynman-timeline-refresh.md                   |
| 3    | de2d59e | commands/feynman-timeline-refresh.md (Rule 1 JTBD fix) + scripts/feynman-timeline-refresh-command.cjs |
| 4    | 1510a3f | data/command-registry.json                             |

Every commit used `--no-verify` (per the parallel_execution rule -- a concurrent session is editing Phase 125 plans on `main`). Every commit used explicit pathspec `git commit ... -- <paths>` (never `git add .` / `git add -A`).

Zero Phase 125 contamination: `git show --name-only <hash>` per commit confirms only the intended files. The 6 modified `.planning/phases/125-f-selector-ranker/125-*-PLAN.md` files (plus `dashboard/graph.json`) stay unstaged across all 4 commits.

## Verification Gate Results

### Phase 124 scoped runner (this plan)

```
bash tests/run-all-124.sh
PASS test-feynman-timeline-renderer.cjs              (Plan 124-01)
PASS test-feynman-timeline-empty-state.cjs           (Plan 124-01)
PASS test-feynman-timeline-runner.cjs                (Plan 124-02)
FAIL test-feynman-timeline-canon-part-9-invariant.cjs (Plan 124-04 - RED by design)

Summary (124 scoped): Total 4, Passed 3, Failed 1
```

3 of 4 GREEN. The canon-part-9-invariant RED is BY DESIGN -- this plan does not fill that stub (it owns the Wave 2 wiring; Plan 124-04 owns Wave 3 invariant + docs). This matches the plan's stated success criterion exactly.

### Regression checks (zero regressions)

- `bash tests/run-all-110.sh` -> **4 / 4 PASSED** (Brain Context Packet Contract; no regression).
- `node tests/test-navigation-memory-events.cjs` -> **10 / 10 passed** (Phase 109 navigation memory events; includes test10_phase110BrainEventsAccepted which validates EVENT_TYPES extensibility -- still green).
- `node tests/test-navigation-acceptance.cjs` -> **1 / 1 passed** (Phase 109 acceptance test; instrumented fs-read sweep still proves zero non-SQLite reads).
- `node tests/test-feynman-timeline-renderer.cjs` -> **5 tests passed**.
- `node tests/test-feynman-timeline-runner.cjs` -> **8 tests passed**.
- `node tests/test-feynman-timeline-empty-state.cjs` -> **2 tests passed**.
- `node tests/test-command-jtbd-declarations.cjs` -> **86 files; all carry valid serves_jtbd declarations; closed-vocabulary check ok; latency 8ms** (Phase 104 enforcement gate).
- `node scripts/build-command-registry.cjs --check` -> exit 0 (Phase 122 tripwire).
- `bash .git/hooks/pre-commit` -> exit 0 on clean tree.
- `! grep -q '"ajv"' package.json` -> ajv ABSENT (zero new npm deps).
- `grep -P "[\x{2014}\x{2013}]"` on the new block in scripts/session-start + commands/feynman-timeline-refresh.md + scripts/feynman-timeline-refresh-command.cjs -> zero matches (em/en-dash clean).

### Invariants exported by this plan

- `scripts/session-start` contains `# --- BEGIN feynman timeline refresh (Phase 124, best-effort) ---` and `# --- END feynman timeline refresh ---` (verified by grep).
- The cascade slot appears AFTER `# --- END cache prune ---` (verified by awk line-number ordering: cache-prune at line 1304, feynman at line 1306).
- The cascade slot references `lib/core/feynman/timeline-runner.cjs` (verified by grep).
- `commands/feynman-timeline-refresh.md` carries `kind: utility`, `frameworks: []`, `produces: "room/*/FEYNMAN.md"`, `inputs: []`, `autonomous_safe: true`, and `serves_jtbd: ["validate-idea", "audit-room"]` (verified by grep).
- `scripts/feynman-timeline-refresh-command.cjs` exports `parseArgv`, `resolveActiveRoom`, `resolveRoomDb`, `renderReport`, `main` (verified by runtime require).
- `data/command-registry.json` contains the `/mos:feynman-timeline-refresh` entry with all 5 required Phase 122 fields correctly set (verified by `node -e` field-by-field check).

## Deviations from Plan

### Auto-fixed (Rule 1: bug fix)

**1. [Rule 1 - Bug] Plan-specified `serves_jtbd` value contains an id NOT in the canonical 13-id JTBD taxonomy**

- **Found during:** Task 3 post-create verification, running `node tests/test-command-jtbd-declarations.cjs`.
- **Issue:** The plan's CONTEXT D-12 + the `commands/feynman-timeline-refresh.md` template both specified `serves_jtbd: ["validate-idea", "build-knowledge"]`. However the canonical Phase 104 JTBD taxonomy (`lib/hmi/jtbd-taxonomy.json`) defines exactly 13 closed-vocabulary ids: `decide-pursue`, `find-problem`, `understand-market`, `find-bottleneck`, `prepare-pitch`, `validate-idea`, `compare-options`, `connect-domains`, `surface-contradiction`, `plan-execution`, `file-meeting`, `audit-room`, `explore`. The id `build-knowledge` is NOT in this taxonomy. The enforcement test (`tests/test-command-jtbd-declarations.cjs`, Phase 104-02 JTBDCONS-104-02) failed with: `commands/feynman-timeline-refresh.md:4 -> serves_jtbd[1]="build-knowledge" is not in the canonical 13-id taxonomy`.
- **Fix:** Replaced `build-knowledge` with `audit-room`. The new value `["validate-idea", "audit-room"]` is semantically correct: a timeline refresh surfaces what the room currently knows about its captured insight events -- this IS the audit-room JTBD. The validate-idea id stays because timeline events touch evidence/decisions tied to ideas under validation.
- **Files modified:** commands/feynman-timeline-refresh.md (line 4).
- **Why this is Rule 1 not Rule 4:** The plan specified a value that fails an EXISTING enforcement gate. Picking a valid id from the canonical taxonomy is a bug fix, not an architectural decision. The behavior contract (the command's role in the JTBD taxonomy) is preserved -- one of the two ids was simply replaced with the semantically-closest canonical id.
- **Commit:** de2d59e (folded into Task 3 commit, since Task 3 also touched the same file's invocation block).
- **Verification post-fix:** `node tests/test-command-jtbd-declarations.cjs` reports `ok 86 command files; all carry valid serves_jtbd declarations; closed-vocabulary check (13 canonical ids enforced)`.

### Architectural changes

None. The wiring is purely additive: a new bounded block in session-start, a new command markdown, a new dispatcher script, a regenerated registry. The runner + renderer + navigation chokepoint are unchanged byte-identical from Plans 124-01 + 124-02.

### Authentication gates

None encountered.

## Known Stubs

None introduced by this plan. The dispatcher fully wires Plan 124-01's renderer + Plan 124-02's runner into the two user-facing surfaces. The remaining RED stub in tests/run-all-124.sh (`tests/test-feynman-timeline-canon-part-9-invariant.cjs`) is Plan 124-04's responsibility -- it scans the renderer + runner source files (already shipped) for forbidden requires/calls. This plan's source files (`scripts/feynman-timeline-refresh-command.cjs`) will be additionally scanned by Plan 124-04's forbidden-substring sweep; verified pre-emptively by:

- `! grep -E "require\(.*brain-client|require\(.*node:http|require\(.*node:https" scripts/feynman-timeline-refresh-command.cjs` -> zero matches.
- `! grep -E "fetch\(|http\.request\(|https\.request\(|http\.get\(|https\.get\(" scripts/feynman-timeline-refresh-command.cjs` -> zero matches.
- `! grep -E "require\(.*child_process" scripts/feynman-timeline-refresh-command.cjs` -> zero matches.

The dispatcher is Plan-124-04 compliant pre-emptively; the Plan 124-04 forbidden-substring sweep will pass against the renderer + runner + dispatcher all three.

## Phase 124 Progress

| Plan      | Status     | Suites GREEN | Notes                                                                                          |
| --------- | ---------- | ------------ | ---------------------------------------------------------------------------------------------- |
| 124-00    | shipped    | 0/4 (substrate) | 4 RED test stubs, run-all-124.sh runner, EVENT_TYPES floor, aggregator registration            |
| 124-01    | shipped    | 2/4          | timeline-renderer.cjs + firstCapturedLastTouchedBySection navigation re-export                  |
| 124-02    | shipped    | 3/4          | timeline-runner.cjs + EVENT_TYPES +2 + runner integration suite                                 |
| **124-03**| **this plan** | **3/4**   | **session-start cascade + /mos:feynman-timeline-refresh command + dispatcher + registry regen** |
| 124-04    | pending    | 4/4          | Canon-Part-9-invariant test (the last RED stub) + docs/CANON-PHASE-MAP.md Part 9 + docs/MINDRIAN-CANON.md cross-ref |

After this plan: 4 of 5 plans executed. 3 of 4 suites green (canon-invariant RED by design for Plan 124-04). The wiring is live; a fresh Claude Code session will fire the cascade slot at startup; the user can run `/mos:feynman-timeline-refresh` manually right now.

## Issues Encountered

None unexpected. The Rule 1 JTBD-id fix was caught by the existing Phase 104 enforcement gate (test-command-jtbd-declarations.cjs) within seconds of the file landing on disk -- exactly the failure mode the gate was built to catch. The fix took one Edit + one re-run of the test.

## User Setup Required

None.

## Next Phase Readiness

**Plan 124-04 (Wave 3 Canon Part 9 invariant + docs) is unblocked**. It:

- Fills `tests/test-feynman-timeline-canon-part-9-invariant.cjs` with the 5-tripwire forbidden-substring sweep (renderer + runner + the new `scripts/feynman-timeline-refresh-command.cjs` dispatcher are all in scope; pre-emptive checks above confirm the dispatcher will pass).
- Adds the docs/CANON-PHASE-MAP.md Part 9 row for Phase 124.
- Adds the docs/MINDRIAN-CANON.md Part 9 implementing-phase cross-reference for Phase 124.

The runner + renderer + dispatcher source files are all on `main` for Plan 124-04 to scan. No new infrastructure needed.

The concurrent session editing Phase 125 plans is unaffected by this work. All 4 commits (78c7cf1, ace1b3e, de2d59e, 1510a3f) are atomic, use `--no-verify`, and have zero overlap with the 6 modified `.planning/phases/125-f-selector-ranker/125-*-PLAN.md` files (verified via `git show --name-only` per commit).

## Self-Check: PASSED

- `scripts/session-start` -> FOUND, new cascade block at lines 1306-1353 (BEGIN at 1306, END at 1353); `bash -n scripts/session-start` exits 0.
- `commands/feynman-timeline-refresh.md` -> FOUND; all 5 Phase 122 frontmatter keys + Phase 104 serves_jtbd present + valid.
- `scripts/feynman-timeline-refresh-command.cjs` -> FOUND; `node -c` passes; 5 expected exports present.
- `data/command-registry.json` -> FOUND; new entry present with all 5 required fields correctly set; `--check` exits 0.
- Commit 78c7cf1 (Task 1) -> FOUND in git log; touches only scripts/session-start.
- Commit ace1b3e (Task 2) -> FOUND in git log; touches only commands/feynman-timeline-refresh.md.
- Commit de2d59e (Task 3) -> FOUND in git log; touches commands/feynman-timeline-refresh.md (Rule 1 fix) + scripts/feynman-timeline-refresh-command.cjs.
- Commit 1510a3f (Task 4) -> FOUND in git log; touches only data/command-registry.json.
- `bash tests/run-all-124.sh` reports 3/4 (canon-invariant RED by design for Plan 124-04) -- matches the plan's stated success criterion exactly.
- `bash tests/run-all-110.sh` reports 4/4 (no regression).
- `node tests/test-command-jtbd-declarations.cjs` reports 86 files all valid (the gate this plan's Rule 1 fix unblocked).
- `node scripts/build-command-registry.cjs --check` exits 0 (Phase 122 tripwire).
- `bash .git/hooks/pre-commit` exits 0 on clean tree (no regression).
- ajv NOT in package.json (no new npm deps).
- Zero em-dashes or en-dashes in any new content shipped by this plan (verified across all 4 commits).
- Zero Phase 125 contamination across all 4 commits (`git show --name-only` per commit; no 125-* paths).

---
*Phase: 124-feynman-temporal-awareness*
*Plan: 03 (Wave 2 wiring)*
*Completed: 2026-05-13*
