---
phase: 124-feynman-temporal-awareness
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - tests/test-feynman-timeline-renderer.cjs
  - tests/test-feynman-timeline-runner.cjs
  - tests/test-feynman-timeline-empty-state.cjs
  - tests/test-feynman-timeline-canon-part-9-invariant.cjs
  - tests/run-all-124.sh
  - lib/memory/run-feynman-tests.cjs
  - lib/core/feynman/ROOM.md
autonomous: true
requirements:
  - TEMPORAL-124-01
  - TEMPORAL-124-02
  - TEMPORAL-124-03
  - TEMPORAL-124-04
  - TEMPORAL-124-05
  - TEMPORAL-124-06
  - TEMPORAL-124-07
  - TEMPORAL-124-08
  - TEMPORAL-124-09
  - TEMPORAL-124-10
canon_parts:
  - "Part 9 (Memory Locality and Interpretation -- this Wave 0 substrate registers the 10 TEMPORAL-124-XX requirement IDs that Plans 124-01 through 124-04 implement; this plan ships ZERO behavior, only the 4 RED test stubs + the scoped runner + the REQUIREMENTS/ROADMAP lock + the lib/core/feynman/ROOM.md ICM Layer 0 identity)"
  - "Part 5 (Evidence is graded by context -- the timeline section makes 'stale' a context signal alongside the existing evidence tier; the Wave 0 substrate locks the requirement IDs that ship the stale-threshold cascade)"
  - "Part 7 (Reuse Before Build -- the 4 test stubs follow the existing tests/test-*.cjs CJS pattern verified against tests/test-brain-packet-schema-check.cjs and tests/test-navigation-packet-part8-leak.cjs; tests/run-all-124.sh is a verbatim structural mirror of tests/run-all-110.sh / tests/run-all-122.sh; zero new npm dependencies)"
must_haves:
  truths:
    - "REQUIREMENTS.md gains a `## FEYNMAN.md Temporal Awareness (TEMPORAL-124)` block with 10 checkbox entries TEMPORAL-124-01 through TEMPORAL-124-10 (goal-shaped descriptions, each citing the source CONTEXT D-NN), plus 10 `| TEMPORAL-124-0N | Phase 124 | Pending |` rows appended to the bottom Status table"
    - "ROADMAP.md `### Phase 124` block: the `**Plans:** TBD - run /gsd:discuss-phase 124 then /gsd:plan-phase 124` line is replaced with `**Plans:** 5 plans across 4 waves.` plus an enumerated `Plans:` checkbox list 124-00 through 124-04; the `**Requirements**: [TEMPORAL-124-01..NN -- defined in plan-phase]` placeholder is replaced with `**Requirements**: TEMPORAL-124-01..10 (see .planning/REQUIREMENTS.md)`"
    - "4 RED test stub files exist: tests/test-feynman-timeline-renderer.cjs, tests/test-feynman-timeline-runner.cjs, tests/test-feynman-timeline-empty-state.cjs, tests/test-feynman-timeline-canon-part-9-invariant.cjs"
    - "Each test stub exits 1 with a stderr line `MISSING - Wave N must implement <deliverable> (Plan 124-NN)` so the runner records RED status without a false-positive PASS"
    - "tests/run-all-124.sh exists, is a verbatim structural mirror of tests/run-all-110.sh (bash only, no emoji, no em-dashes, SHELL_SUITES empty, CJS_SUITES = the 4 new suites, per-suite PASS/FAIL loop, exit non-zero if any failed), runs to completion, and exits 1 today (4 RED stubs)"
    - "lib/memory/run-feynman-tests.cjs TEST_FILES[] registers all 4 new test paths in a Phase 124 block with a comment header mapping each file to its owning plan"
    - "lib/core/feynman/ROOM.md exists as the ICM Layer 0 identity file for the new dir per CLAUDE.md decision #15 (D-07 location lock)"
    - "Every file this plan ships contains zero em-dashes (U+2014) and zero en-dashes (U+2013) per the CLAUDE.md hard rule"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "The 10 TEMPORAL-124-XX requirement IDs registered with Status: Pending; downstream plans (124-01..04) reference these IDs in their requirements frontmatter"
      contains: "TEMPORAL-124-01"
    - path: "tests/test-feynman-timeline-renderer.cjs"
      provides: "RED stub filled by Plan 124-01 (renderer unit test: given a fixture room.db with seeded memory_event rows across the 4 thresholds, the renderer returns the expected D-05 markdown body)"
      contains: "MISSING"
    - path: "tests/test-feynman-timeline-runner.cjs"
      provides: "RED stub filled by Plan 124-02 (runner integration: sentinel-bounded section replaced; human body byte-identical pre/post; timeline_last_rendered frontmatter set; idempotent re-run byte-identical; memory_event of type feynman_timeline_refreshed logged)"
      contains: "MISSING"
    - path: "tests/test-feynman-timeline-empty-state.cjs"
      provides: "RED stub filled by Plan 124-01 (section with zero memory_event rows renders the `*No timeline events yet.*` placeholder between the sentinels)"
      contains: "MISSING"
    - path: "tests/test-feynman-timeline-canon-part-9-invariant.cjs"
      provides: "RED stub filled by Plan 124-04 (Canon Part 9 invariant: the renderer + runner never require brain-client; no fetch/http/https near a render path; the fs-instrument allow-list catches reads outside room.db and the FEYNMAN.md being written)"
      contains: "MISSING"
    - path: "tests/run-all-124.sh"
      provides: "Scoped Phase 124 runner; CJS_SUITES = the 4 new suites; header documents RED-by-design-until-owning-plan-lands; mirrors tests/run-all-110.sh"
      contains: "Phase 124 scoped test runner"
    - path: "lib/memory/run-feynman-tests.cjs"
      provides: "Existing Feynman test registry extended with 4 new TEST_FILES entries so bash tests/run-all-124.sh and the whole-repo Feynman runner both pick them up"
      contains: "test-feynman-timeline-renderer.cjs"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 124 block: Requirements line + Plans checkbox list enumerating 124-00 through 124-04 so /gsd:progress reports plan state correctly"
      contains: "124-00-PLAN.md"
    - path: "lib/core/feynman/ROOM.md"
      provides: "ICM Layer 0 identity for the new lib/core/feynman/ dir (D-07 location lock; the renderer + runner ship here in Plans 124-01 and 124-02)"
      contains: "Phase 124"
  key_links:
    - from: "lib/memory/run-feynman-tests.cjs TEST_FILES array"
      to: "tests/test-feynman-timeline-renderer.cjs and tests/test-feynman-timeline-runner.cjs and tests/test-feynman-timeline-empty-state.cjs and tests/test-feynman-timeline-canon-part-9-invariant.cjs"
      via: "path.join(REPO_ROOT, 'tests', '<filename>') entries appended to TEST_FILES in a Phase 124 block"
      pattern: "test-feynman-timeline-renderer\\.cjs"
    - from: "tests/run-all-124.sh CJS_SUITES array"
      to: "the 4 new tests/test-feynman-timeline-*.cjs files"
      via: "CJS_SUITES=( test-feynman-timeline-renderer.cjs test-feynman-timeline-runner.cjs test-feynman-timeline-empty-state.cjs test-feynman-timeline-canon-part-9-invariant.cjs )"
      pattern: "test-feynman-timeline-renderer\\.cjs"
    - from: ".planning/REQUIREMENTS.md TEMPORAL-124-01 through TEMPORAL-124-10 rows"
      to: "Status table at the bottom of REQUIREMENTS.md"
      via: "10 rows appended to the per-phase status table mirroring the PACKET-110-01 through PACKET-110-09 pattern"
      pattern: "TEMPORAL-124-(0[1-9]|10).*Pending"
---

<objective>
Wave 0 substrate plan. Ships ZERO behavior. Ships the test infrastructure plus the requirement and roadmap lock that Waves 1 through 3 of Phase 124 require: 4 RED test stubs, the scoped `tests/run-all-124.sh` runner, the Feynman-runner registration, the 10 TEMPORAL-124-XX requirement IDs in `.planning/REQUIREMENTS.md`, the `### Phase 124` block update in `.planning/ROADMAP.md` (plan list + Requirements line), and the `lib/core/feynman/ROOM.md` ICM Layer 0 identity file for the dir where the renderer + runner will ship (D-07 location lock).

Modelled verbatim on `110-00-PLAN.md` and `109-00-PLAN.md` (the Wave-0 substrate-plan templates). The 4 test stubs each exit 1 with stderr `MISSING - Wave N must implement ... (Plan 124-NN)` so the test runner records RED status; later plans (124-01, 124-02, 124-04) replace the stub bodies with real assertions without changing the registered paths. The Wave 0 deliverables ARE the validation contract Phase 124 ships against (`124-VALIDATION.md` mirrors this).

Per CONTEXT D-11, the four suites are: `test-feynman-timeline-renderer.cjs` (covers TEMPORAL-124-02 + -04 + -06 + -08 renderer-shaped behaviors), `test-feynman-timeline-runner.cjs` (covers TEMPORAL-124-03 + -05 + -07 + -09 runner-shaped behaviors), `test-feynman-timeline-empty-state.cjs` (covers TEMPORAL-124-04 empty-state branch), `test-feynman-timeline-canon-part-9-invariant.cjs` (covers TEMPORAL-124-10 Canon Part 9 forbidden-substring sweep). The Plan 124-03 wiring is verified via grep + `bash tests/run-all-124.sh` exit codes (the wiring is structural; no separate test stub is needed because the runner test covers the runner.refreshAll invocation and the manual command delegates to the same runner).

This plan has zero dependencies and runs first. It writes nothing executable -- no renderer, no runner, no session-start cascade slot, no manual command, no commands/feynman-timeline-refresh.md. Those are Waves 1, 2, 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-feynman-temporal-awareness/124-CONTEXT.md
@.planning/phases/124-feynman-temporal-awareness/124-DISCUSSION-LOG.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@lib/memory/run-feynman-tests.cjs
@tests/run-all-110.sh
@tests/run-all-122.sh
@tests/test-brain-packet-schema-check.cjs
@lib/core/navigation.cjs
@lib/core/navigation/memory-events.cjs
@lib/core/navigation/insights.cjs

<interfaces>
<!-- Test stub pattern (verbatim from 110-00-PLAN.md Task 2 / tests/test-brain-packet-*.cjs Wave-0 stubs):

```javascript
'use strict';
// Phase 124 Wave 0 stub. Plan 124-00 ships this stub; Plan 124-NN replaces it with real assertions.
// Requirement: TEMPORAL-124-NN
// Deliverable: <one-line deliverable>
// Pattern: matches tests/test-brain-packet-schema-check.cjs CJS direct-CJS approach (no Mocha, no Jest, zero new npm dependencies).

process.stderr.write('MISSING - Wave N must implement <deliverable> (Plan 124-NN)\n');
process.exit(1);
```
-->

<!-- tests/run-all-124.sh = verbatim structural mirror of tests/run-all-110.sh:
   - shebang #!/usr/bin/env bash
   - header comment explaining the suites are RED-by-design until their owning plan lands (map each suite -> plan)
   - `set -uo pipefail`
   - SHELL_SUITES=( )  (empty)
   - CJS_SUITES=( test-feynman-timeline-renderer.cjs test-feynman-timeline-runner.cjs test-feynman-timeline-empty-state.cjs test-feynman-timeline-canon-part-9-invariant.cjs )
   - the same TOTAL/PASSED/FAILED loop, the same Summary block, exit 1 if any failed
   - bash only, no emoji, no em-dashes
-->

<!-- run-feynman-tests.cjs registry pattern (lib/memory/run-feynman-tests.cjs around line 1263 for the Phase 110 block; the Phase 122 block above and the Phase 123 block below):
const TEST_FILES = [
  path.join(REPO_ROOT, 'lib', 'memory', 'feynman-prompts.test.cjs'),
  ...
  // Phase 124-00: FEYNMAN.md Temporal Awareness Wave 0 substrate (4 stubs filled by Plans 124-01 / 124-02 / 124-04).
  //   test-feynman-timeline-renderer.cjs              -> 124-01 (TEMPORAL-124-02 + -04 + -06 + -08: renderer unit + D-05 template + thresholds)
  //   test-feynman-timeline-empty-state.cjs           -> 124-01 (TEMPORAL-124-04: empty-state placeholder)
  //   test-feynman-timeline-runner.cjs                -> 124-02 (TEMPORAL-124-03 + -05 + -07 + -09: sentinel-bounded merge + body byte-identical + watermark + idempotent + memory_event)
  //   test-feynman-timeline-canon-part-9-invariant.cjs -> 124-04 (TEMPORAL-124-10: forbidden-substring sweep + fs-instrument allow-list)
  path.join(REPO_ROOT, 'tests', 'test-feynman-timeline-renderer.cjs'),
  path.join(REPO_ROOT, 'tests', 'test-feynman-timeline-empty-state.cjs'),
  path.join(REPO_ROOT, 'tests', 'test-feynman-timeline-runner.cjs'),
  path.join(REPO_ROOT, 'tests', 'test-feynman-timeline-canon-part-9-invariant.cjs'),
];
Append the 4 entries with the Phase comment markers matching the in-file convention. The Phase 124 block goes AFTER the Phase 123 block (at the end of TEST_FILES) so the registration order tracks the chronological phase landing order.
-->

<!-- REQUIREMENTS.md format (the PACKET-110 block at lines 274-292 is the verbatim precedent; the new block sits between `## Brain Context Packet Contract (PACKET-110)` and `## Workflow Layer (WORKFLOW-122)`):
## FEYNMAN.md Temporal Awareness (TEMPORAL-124)

- [ ] **TEMPORAL-124-01**: ...one block per TEMPORAL-124-XX requirement; the Status table at the bottom gains 10 `| TEMPORAL-124-0N | Phase 124 | Pending |` rows.
-->

<!-- lib/core/feynman/ROOM.md template (ICM Layer 0 identity per CLAUDE.md decision #15; D-07 location lock):

# lib/core/feynman/

The Phase 124 (FEYNMAN.md temporal awareness) renderer + runner ship here.

Contains (after Plans 124-01 and 124-02 land):
- timeline-renderer.cjs -- pure function `(db, section_id) -> { markdown_body, summary_stats }`. Reads ONLY via lib/core/navigation.cjs.
- timeline-runner.cjs -- `refreshAll(roomDir)` + `refreshSection(roomDir, section_slug)`. Walks the room, regenerates the sentinel-bounded section in each FEYNMAN.md, byte-preserves the human body.

Owner: Phase 124 FEYNMAN.md Temporal Awareness.
Canon: Part 9 (the Larry-explains face of memory_event). Renderer reads ONLY room.db via navigation.cjs (D-03). Writes ONLY FEYNMAN.md inside the sentinels (D-02 hard invariant).
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 10 TEMPORAL-124-XX requirement IDs in REQUIREMENTS.md and update ROADMAP.md Phase 124 block</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 274-292 (the `## Brain Context Packet Contract (PACKET-110)` block plus the 9 `| PACKET-110-0N | Phase 110 | Pending |` Status-table rows -- format the 10 TEMPORAL-124-XX entries and rows identically; the new block goes immediately AFTER the PACKET-110 block, BEFORE `## Workflow Layer (WORKFLOW-122)`)
    - .planning/phases/124-feynman-temporal-awareness/124-CONTEXT.md (the LOCKED decisions D-00 through D-12 -- each TEMPORAL-124-XX description must trace to a D-NN)
    - .planning/ROADMAP.md lines 1308-1316 (the `### Phase 124` block; the Goal paragraph stands; replace the `**Requirements**: [TEMPORAL-124-01..NN -- defined in plan-phase]` placeholder; replace the `**Plans:** TBD - run /gsd:discuss-phase 124 then /gsd:plan-phase 124` line)
  </read_first>
  <action>
    STEP 1 -- REQUIREMENTS.md. Append a new section to `.planning/REQUIREMENTS.md` immediately AFTER the existing `## Brain Context Packet Contract (PACKET-110)` block (which ends around line 292) and BEFORE `## Workflow Layer (WORKFLOW-122)` (around line 294). Replace any em-dash or en-dash with a hyphen before writing -- touch ONLY the new TEMPORAL-124 section.

    Content (no em-dashes anywhere; CITE the locking decision in every entry):

    ```markdown

    ## FEYNMAN.md Temporal Awareness (TEMPORAL-124)

    - [ ] **TEMPORAL-124-01**: Sentinel contract + hard invariant. `lib/core/feynman/timeline-runner.cjs` writes a `## Timeline (auto)` section into each `FEYNMAN.md`, sentinel-bounded by `<!-- TIMELINE_AUTO_START -->` ... `<!-- TIMELINE_AUTO_END -->`. Anything outside the sentinels is human-authored and is byte-preserved across regeneration; anything inside is owned by the regenerator. On first encounter (FEYNMAN.md without the sentinel pair), the runner appends the pair plus the rendered body at end-of-file in a single atomic write. The runner test (`tests/test-feynman-timeline-runner.cjs`) asserts (a) the human body section is byte-identical pre/post-regeneration via a SHA256 hash of the outside-sentinels bytes, (b) re-running the runner against an already-rendered FEYNMAN.md produces a byte-identical file (idempotent). Per CONTEXT D-01 + D-02 (LOCKED, hard invariant).

    - [ ] **TEMPORAL-124-02**: Renderer reads ONLY room.db via navigation.cjs. `lib/core/feynman/timeline-renderer.cjs` exports a pure function `renderTimeline(db, section_id, opts) -> { markdown_body, summary_stats }`. It calls into `lib/core/navigation.cjs` (the closed chokepoint): `findRecentChanges(db, sinceEpochMs, { limit })` (Phase 109 LIVE), `findStaleDecisions(db, roomId, { stale_threshold_ms, limit })` (Phase 109 LIVE), and the new `firstCapturedLastTouchedBySection(db, sectionSlug)` -- if that primitive does not exist on navigation.cjs today, Plan 124-01 adds it as an additive re-export under the navigation/insights.cjs subpath (mirroring how Phase 110-03 re-exported `logMemoryEvent` as the 14th surface export per the navigation.cjs Phase 110-03 comment block). The renderer performs ZERO filesystem reads of its own; ZERO Brain calls; ZERO LLM calls. Per CONTEXT D-03 + Canon Part 9.

    - [ ] **TEMPORAL-124-03**: Hybrid hook trigger -- session-start cascade slot + manual command. (a) `scripts/session-start` gains a sibling cascade slot AFTER the cache-prune block (after the `# --- END cache prune ---` marker around line 1300) that runs `lib/core/feynman/timeline-runner.cjs::refreshAll(activeRoomDir)` best-effort (`try { ... } catch (e) { process.stderr.write('feynman-timeline-refresh failed: ' + ...) }` inside the `node -e` wrapper, plus a shell-level `|| true` so a failure NEVER blocks startup -- same idiom as cache-prune). The slot is conditional on the active room existing AND containing at least one FEYNMAN.md sentinel pair OR at least one section directory (it is a no-op on a fresh empty room). (b) `commands/feynman-timeline-refresh.md` plus `scripts/feynman-timeline-refresh-command.cjs` mirror the `/mos:brain-derive` shape (`commands/brain-derive.md` + `scripts/brain-derive-command.cjs`). Per CONTEXT D-04 (LOCKED, hybrid).

    - [ ] **TEMPORAL-124-04**: D-05 section format -- summary line + Recent + Stale + Health, plus empty-state placeholder. The renderer body between the sentinels matches the D-05 template literal exactly: a summary line of the form `*Last refreshed: {ISO}. {N} insight events, first captured {first_iso}, last touched {last_iso} ({last_delta_human}).*`, then a `**Recent events** (within 7 days, top 5):` list, then a `**Flagged stale** (over 30 days untouched, top 5):` list, then a `**Health:** recent={n_recent} / quiet={n_quiet} / stale={n_stale} / dormant={n_dormant}.` line. Bounded length 12 to 25 lines per section. Zero LLM strings -- every bullet renders via a templated `{iso}: {event_type} -- {one_line_explain}` format pulled from the existing `lib/core/navigation/explanation.cjs::renderExplanation` (Phase 109-05) when an explanation builder exists for the event type, else a generic `{event_type} on {target_summary}` fallback. If a section has zero `memory_event` rows scoped to it, the body between the sentinels is exactly `*No timeline events yet.*` and nothing else. Per CONTEXT D-05 (LOCKED, hybrid).

    - [ ] **TEMPORAL-124-05**: D-06 stale thresholds -- 7 / 30 / 90 day cascade as constants, overridable for tests. `lib/core/feynman/timeline-renderer.cjs` exports `THRESHOLDS = Object.freeze({ recent_ms: 7 * 24 * 60 * 60 * 1000, quiet_ms: 30 * 24 * 60 * 60 * 1000, stale_ms: 90 * 24 * 60 * 60 * 1000 })` (dormant > stale_ms). `resolveThresholds()` honors `process.env.MINDRIAN_TIMELINE_THRESHOLDS_JSON` (a JSON literal like `{"recent_ms": 1000, "quiet_ms": 5000, "stale_ms": 10000}`); invalid JSON or missing keys fall back to the locked defaults. The renderer derives the four health buckets (`n_recent`, `n_quiet`, `n_stale`, `n_dormant`) by bucketing each memory_event row's `created_at` delta from `Date.now()`. The renderer test asserts default-threshold rendering matches the D-05 template AND environment-override threshold rendering shifts the bucket counts. Per CONTEXT D-06 (LOCKED, 7/30/90).

    - [ ] **TEMPORAL-124-06**: D-07 location -- `lib/core/feynman/timeline-renderer.cjs` and `lib/core/feynman/timeline-runner.cjs` and `lib/core/feynman/ROOM.md`. The Plan 124-00 substrate creates `lib/core/feynman/ROOM.md` (ICM Layer 0 identity per CLAUDE.md decision #15). Plan 124-01 creates the renderer; Plan 124-02 creates the runner. Neither module re-exports from `lib/core/folder-memory.cjs` (the existing triple-reader) nor extends it -- the triple-reader's contract stays byte-identical, the timeline is a NEW writer next door. Per CONTEXT D-07 (LOCKED, location).

    - [ ] **TEMPORAL-124-07**: D-08 section scoping -- `memory_event.source_section` is the join key. The renderer's three navigation calls all scope by section slug: `findRecentChanges(db, sinceEpochMs, { limit })` is filtered post-fetch by `row.sourcePath === sectionSlug` OR `row.sourcePath.startsWith(sectionSlug + '/')` (sub-room aware); `findStaleDecisions(db, roomId, opts)` is filtered the same way; the new `firstCapturedLastTouchedBySection(db, sectionSlug)` joins on `nodes.source_path` (the SQL field that backs `source_section` in the lazygraph schema -- verified at `lib/core/navigation/insights.cjs` line 293 `ns.source_path AS source_section`). NO folder name parsing, NO directory walks; the slug is passed in from the runner (which DID walk the filesystem to find FEYNMAN.md, but never to derive the section identity). Per CONTEXT D-08 (LOCKED, source_section join).

    - [ ] **TEMPORAL-124-08**: D-09 watermark -- `timeline_last_rendered: <ISO>` frontmatter, second-resolution, skip-when-SQL-older. The runner reads the existing FEYNMAN.md frontmatter (the parser is the hand-rolled line-walk style already used by `scripts/frontmatter-schema-validator.cjs`; do NOT add `gray-matter`); on a successful refresh writes `timeline_last_rendered: <ISO>` (or updates the existing value) using second-resolution (`new Date().toISOString().replace(/\.\d{3}Z$/, 'Z')`). The runner skips regeneration when (a) the frontmatter `timeline_last_rendered` parses as a valid ISO AND (b) `MAX(memory_event.created_at) WHERE source_section = <sectionSlug>` is older-or-equal at second-resolution (compute the SQL value's second-resolution ISO; string-equality OR string-less comparison). On a refresh failure (the renderer threw, the FEYNMAN.md is malformed, the write atomically failed), the watermark is NOT updated AND a `feynman_timeline_refresh_failed` memory_event is logged with a redacted reason (the exception message truncated to 200 chars; never the FEYNMAN.md body). Per CONTEXT D-09 (LOCKED, watermark).

    - [ ] **TEMPORAL-124-09**: D-10 EVENT_TYPES extension -- additive +2 in `lib/core/navigation/memory-events.cjs`. The frozen `EVENT_TYPES` Set gains two new strings: `feynman_timeline_refreshed` (logged by the runner on each successful refresh with `payload = { source_path: 'feynman:' + sectionSlug, created_by: 'system' }`) and `feynman_timeline_refresh_failed` (logged with the same payload shape plus a `reason` field). The Set size grows from 35 to 37 (current count is 35; Phase 110-02 brought the count from 32 to 35 via the +3 brain_* extension; the Phase 110-02 extension comment block in memory-events.cjs is the structural template -- mirror its placement immediately AFTER the `// Phase 110-02 extension ... brain_legacy_path_used` block). Tests assert (a) `EVENT_TYPES.has('feynman_timeline_refreshed')` AND (b) `logEvent(db, 'feynman_timeline_refreshed', payload).ok === true` AND (c) a regression check that the Phase 110-02 entries are still present (defensive: the additive idiom must not regress). Per CONTEXT D-10 (LOCKED, additive +2).

    - [ ] **TEMPORAL-124-10**: D-11 test framework + Canon Part 9 invariant + D-12 manual command surface. The 4 test suites land via this Wave 0 substrate plan (124-00): `tests/test-feynman-timeline-renderer.cjs`, `tests/test-feynman-timeline-runner.cjs`, `tests/test-feynman-timeline-empty-state.cjs`, `tests/test-feynman-timeline-canon-part-9-invariant.cjs`. Framework = `node:assert/strict` + `node:child_process` (no jest/mocha/vitest/zod; CJS). All 4 registered in `lib/memory/run-feynman-tests.cjs` `TEST_FILES[]` AND in a scoped `tests/run-all-124.sh` (verbatim mirror of `tests/run-all-110.sh`; `CJS_SUITES` = the 4 suites; header documents RED-by-design-until-owning-plan-lands). The Canon Part 9 invariant test (`tests/test-feynman-timeline-canon-part-9-invariant.cjs`, filled by Plan 124-04) mirrors `lib/memory/brain-derivation.test.cjs`'s Test 13 + Test 14 forbidden-substring sweep pattern AND `tests/test-brain-packet-part8-invariant-per-job.cjs`'s adversarial seed pattern AND `tests/helpers/fs-instrument.cjs` (Phase 109-10 fs-instrument allow-list pattern): the renderer + runner source MUST NOT require `brain-client`, MUST NOT contain `fetch` / `http` / `https` near a render path, MUST NOT read any file outside the FEYNMAN.md being written and a `room.db`-shaped path (assertion via fs-instrument allow-list `[/\.mindrian\/room\.db.*$/, /FEYNMAN\.md$/]`). Manual command (D-12): `commands/feynman-timeline-refresh.md` + `scripts/feynman-timeline-refresh-command.cjs` (mirroring `/mos:brain-derive`); Phase 122 frontmatter contract (`kind: utility`, `frameworks: []`, `produces: "room/*/FEYNMAN.md"`, `inputs: []`, `autonomous_safe: true`); Phase 104 `serves_jtbd: ["validate-idea", "build-knowledge"]`; `argument-hint: "[--all | --section <slug>]"`. The command file's frontmatter passes the Phase 122 `node scripts/build-command-registry.cjs --check` tripwire; the registry rebuild is the verification gate for D-12. Per CONTEXT D-11 + D-12 (LOCKED).
    ```

    Then append these 10 rows to the Status table at the bottom of `.planning/REQUIREMENTS.md` (immediately after the last existing row in the per-phase Traceability table, identical format, no em-dashes):

    ```markdown
    | TEMPORAL-124-01 | Phase 124 | Pending |
    | TEMPORAL-124-02 | Phase 124 | Pending |
    | TEMPORAL-124-03 | Phase 124 | Pending |
    | TEMPORAL-124-04 | Phase 124 | Pending |
    | TEMPORAL-124-05 | Phase 124 | Pending |
    | TEMPORAL-124-06 | Phase 124 | Pending |
    | TEMPORAL-124-07 | Phase 124 | Pending |
    | TEMPORAL-124-08 | Phase 124 | Pending |
    | TEMPORAL-124-09 | Phase 124 | Pending |
    | TEMPORAL-124-10 | Phase 124 | Pending |
    ```

    STEP 2 -- ROADMAP.md. Open `.planning/ROADMAP.md`, find the `### Phase 124: FEYNMAN.md temporal awareness (REGISTERED 2026-05-13 - STUB)` block (around line 1308). Make two edits (use hyphens, never em-dashes, in everything you write):
      (a) Replace `**Requirements**: [TEMPORAL-124-01..NN -- defined in plan-phase]` with `**Requirements**: TEMPORAL-124-01..10 (registered 2026-05-13 -- see .planning/REQUIREMENTS.md "## FEYNMAN.md Temporal Awareness (TEMPORAL-124)")`.
      (b) Replace the line `**Plans:** TBD - run /gsd:discuss-phase 124 then /gsd:plan-phase 124` with this block:

    ```markdown
    **Plans:** 5 plans across 4 waves (planned 2026-05-13 via /gsd:plan-phase 124). Target band: v1.13.0-beta.14 (the FEYNMAN sentinel idiom is additive; no backwards-compat risk for current FEYNMAN.md consumers).

    Plans:
    - [ ] 124-00-PLAN.md - Wave 0 substrate (register TEMPORAL-124-01..10 in REQUIREMENTS.md + ROADMAP; 4 RED test stubs; tests/run-all-124.sh; Feynman-runner registration; lib/core/feynman/ROOM.md)
    - [ ] 124-01-PLAN.md - Wave 1 renderer (lib/core/feynman/timeline-renderer.cjs pure function; D-05 template literal; D-06 thresholds; D-08 section scoping via navigation.cjs; firstCapturedLastTouchedBySection primitive; fill test-feynman-timeline-renderer.cjs + test-feynman-timeline-empty-state.cjs)
    - [ ] 124-02-PLAN.md - Wave 1 runner (lib/core/feynman/timeline-runner.cjs refreshAll + refreshSection; sentinel-bounded merge; body byte-preserved; timeline_last_rendered watermark; EVENT_TYPES +2 in memory-events.cjs; fill test-feynman-timeline-runner.cjs)
    - [ ] 124-03-PLAN.md - Wave 2 wiring (session-start cascade slot after cache-prune; commands/feynman-timeline-refresh.md per Phase 122 + Phase 104 frontmatter contract; scripts/feynman-timeline-refresh-command.cjs argv parser; data/command-registry.json regenerated; pre-commit hook passes)
    - [ ] 124-04-PLAN.md - Wave 3 Canon Part 9 invariant + docs (fill test-feynman-timeline-canon-part-9-invariant.cjs; docs/CANON-PHASE-MAP.md Part 9 row shipped for Phase 124; docs/MINDRIAN-CANON.md Part 9 implementing-phase cross-reference)
    ```

    After writing both files, scan the new TEMPORAL-124 section in REQUIREMENTS.md and the modified Phase 124 block in ROADMAP.md for em-dashes and en-dashes; replace any with hyphens. Do NOT touch any pre-existing content outside those two regions (legacy em-dashes elsewhere are out of scope).
  </action>
  <verify>
    <automated>grep -c "TEMPORAL-124-0\|TEMPORAL-124-10" .planning/REQUIREMENTS.md | awk '$1 == 20 { exit 0 } { exit 1 }' && grep -q "## FEYNMAN.md Temporal Awareness (TEMPORAL-124)" .planning/REQUIREMENTS.md && grep -q "| TEMPORAL-124-10 | Phase 124 | Pending |" .planning/REQUIREMENTS.md && grep -q "124-00-PLAN.md" .planning/ROADMAP.md && grep -q "124-04-PLAN.md" .planning/ROADMAP.md && grep -q "TEMPORAL-124-01..10" .planning/ROADMAP.md && awk '/## FEYNMAN.md Temporal Awareness \(TEMPORAL-124\)/,/^## Workflow Layer/' .planning/REQUIREMENTS.md | grep -P "[\x{2014}\x{2013}]" && exit 1 || awk '/### Phase 124:/,/### Phase 125:/' .planning/ROADMAP.md | sed -n '/Plans:$/,/124-04-PLAN/p' | grep -P "[\x{2014}\x{2013}]" && exit 1 || exit 0</automated>
  </verify>
  <done>
    REQUIREMENTS.md has a `## FEYNMAN.md Temporal Awareness (TEMPORAL-124)` block with 10 goal-shaped TEMPORAL-124-01..10 entries each citing a D-NN, plus 10 `Pending` Status-table rows; ROADMAP.md `### Phase 124` block has the Requirements line and the 5-plan checkbox list; zero em-dashes or en-dashes in the new REQUIREMENTS section or the modified ROADMAP block.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the 4 RED test stubs, tests/run-all-124.sh, lib/core/feynman/ROOM.md, and register the 4 suites in the Feynman runner</name>
  <files>tests/test-feynman-timeline-renderer.cjs, tests/test-feynman-timeline-runner.cjs, tests/test-feynman-timeline-empty-state.cjs, tests/test-feynman-timeline-canon-part-9-invariant.cjs, tests/run-all-124.sh, lib/core/feynman/ROOM.md, lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - tests/test-brain-packet-schema-check.cjs (the canonical Wave-0 stub idiom this phase mirrors verbatim -- `MISSING - Wave N must implement ... (Plan NNN-NN)` exit 1)
    - tests/run-all-110.sh (the scoped-runner template -- mirror its shebang, header structure, `set -uo pipefail`, SHELL_SUITES/CJS_SUITES arrays, the TOTAL/PASSED/FAILED loop, the Summary block, the exit-1-if-any-failed tail; bash only, no emoji, no em-dashes)
    - lib/memory/run-feynman-tests.cjs lines 1263-1313 (the Phase 110 block + the Phase 123 block -- copy the `path.join(REPO_ROOT, 'tests', ...)` pattern and the Phase-comment-marker convention; the Phase 124 block goes AFTER the Phase 123 block, immediately before the closing `];` at line 1313)
    - .planning/phases/124-feynman-temporal-awareness/124-CONTEXT.md decisions D-07 + D-11 (the location lock + the test suite shape)
  </read_first>
  <action>
    STEP 1 -- create the 4 RED test stub files. Each follows this template (vary the WAVE / DELIVERABLE / TEMPORAL-ID / TARGET-PLAN per the table below; HYPHEN never em-dash):

    ```javascript
    'use strict';
    // Phase 124 Wave 0 stub. Plan 124-00 ships this stub; Plan <TARGET-PLAN> replaces it with real assertions.
    // Requirement: <TEMPORAL-ID>
    // Deliverable: <DELIVERABLE>
    // Pattern: matches tests/test-brain-packet-schema-check.cjs (CJS, node:assert, node:child_process; no Mocha, no Jest, zero new npm dependencies).

    process.stderr.write('MISSING - Wave <WAVE> must implement <DELIVERABLE> (Plan <TARGET-PLAN>)\n');
    process.exit(1);
    ```

    Assignment table:

    | File | WAVE | DELIVERABLE | TEMPORAL-ID | TARGET-PLAN |
    |---|---|---|---|---|
    | tests/test-feynman-timeline-renderer.cjs | 1 | renderer unit test (fixture room.db with seeded memory_event rows across 4 thresholds -> D-05 template body; env-override thresholds shift bucket counts) | TEMPORAL-124-02 + TEMPORAL-124-04 + TEMPORAL-124-05 + TEMPORAL-124-07 | 124-01 |
    | tests/test-feynman-timeline-empty-state.cjs | 1 | empty-state placeholder (section with zero memory_event rows -> `*No timeline events yet.*` between the sentinels and nothing else) | TEMPORAL-124-04 | 124-01 |
    | tests/test-feynman-timeline-runner.cjs | 1 | runner integration (sentinel-bounded section replaced; human body byte-identical pre/post; timeline_last_rendered frontmatter set; idempotent re-run byte-identical; feynman_timeline_refreshed memory_event logged; EVENT_TYPES +2 size invariant) | TEMPORAL-124-01 + TEMPORAL-124-03 + TEMPORAL-124-08 + TEMPORAL-124-09 | 124-02 |
    | tests/test-feynman-timeline-canon-part-9-invariant.cjs | 3 | Canon Part 9 forbidden-substring sweep (renderer + runner: no `brain-client` require, no `fetch`/`http`/`https` near render path) + fs-instrument allow-list (only room.db + the FEYNMAN.md being written are read) | TEMPORAL-124-10 | 124-04 |

    STEP 2 -- create `tests/run-all-124.sh`. Copy `tests/run-all-110.sh` verbatim as the structural template (lines 1-97), then change ONLY: the header comment block (Phase 124 framing -- "Phase 124 scoped runner -- run before each 124 task commit. Expect RED on the suites whose owning plan has not landed yet:" then a per-suite -> plan map: `test-feynman-timeline-renderer.cjs -> 124-01`, `test-feynman-timeline-empty-state.cjs -> 124-01`, `test-feynman-timeline-runner.cjs -> 124-02`, `test-feynman-timeline-canon-part-9-invariant.cjs -> 124-04`; note all 4 are RED-by-design until their owning plan lands; bash only, no emoji, no em-dashes); the title strings ("Phase 124 scoped test runner" / "Summary (124 scoped)"); and `CJS_SUITES=( test-feynman-timeline-renderer.cjs test-feynman-timeline-empty-state.cjs test-feynman-timeline-runner.cjs test-feynman-timeline-canon-part-9-invariant.cjs )` (SHELL_SUITES stays empty). Keep the `set -uo pipefail`, the loop, the Summary block, and the exit-1-if-any-failed tail byte-for-byte the same shape.

    STEP 3 -- create `lib/core/feynman/ROOM.md` (ICM Layer 0 identity for the new dir per CLAUDE.md decision #15; D-07 location lock). Content (no em-dashes anywhere):

    ```markdown
    # lib/core/feynman/

    Phase 124 (FEYNMAN.md temporal awareness) renderer + runner ship here.

    Contains (after Plans 124-01 and 124-02 land):
    - `timeline-renderer.cjs` -- pure function `renderTimeline(db, sectionSlug, opts) -> { markdown_body, summary_stats }`. Reads ONLY via `lib/core/navigation.cjs` (the Phase 109 closed chokepoint): `findRecentChanges`, `findStaleDecisions`, and the new `firstCapturedLastTouchedBySection` (added as the 15th re-export in Plan 124-01, mirroring the Phase 110-03 `logMemoryEvent` re-export idiom). ZERO filesystem reads. ZERO Brain calls. ZERO LLM calls.
    - `timeline-runner.cjs` -- `refreshAll(roomDir)` + `refreshSection(roomDir, sectionSlug)`. Walks the room's section folders, finds each `FEYNMAN.md` with the sentinel pair (creates the pair if absent on first run), reads the surrounding body, calls the renderer, writes the file back with the body byte-preserved and the sentinel-bounded section replaced, sets `timeline_last_rendered: <ISO>` frontmatter. Each refresh logs a `memory_event` of type `feynman_timeline_refreshed`. Idempotent (re-run -> byte-identical output).

    Owner: Phase 124 FEYNMAN.md Temporal Awareness.
    Canon: Part 9 (the Larry-explains face of memory_event). Renderer reads ONLY room.db via navigation.cjs (D-03). Writes ONLY FEYNMAN.md inside the sentinels (D-02 hard invariant). The sentinel pair is `<!-- TIMELINE_AUTO_START -->` ... `<!-- TIMELINE_AUTO_END -->`.

    See: `.planning/phases/124-feynman-temporal-awareness/124-CONTEXT.md`.
    ```

    STEP 4 -- register the 4 suites in `lib/memory/run-feynman-tests.cjs`. Find the `TEST_FILES` array (the closing `];` is at line 1313 in the current file). Insert, immediately BEFORE the closing `];`, a Phase 124 block with a comment header mapping each file to its owning plan, then the 4 `path.join` entries:

    ```javascript
      // Phase 124-00: FEYNMAN.md Temporal Awareness Wave 0 substrate (4 stubs filled by Plans 124-01 / 124-02 / 124-04).
      //   test-feynman-timeline-renderer.cjs              -> 124-01 (TEMPORAL-124-02 + -04 + -05 + -07: renderer unit + D-05 template + thresholds + section scoping)
      //   test-feynman-timeline-empty-state.cjs           -> 124-01 (TEMPORAL-124-04: empty-state placeholder)
      //   test-feynman-timeline-runner.cjs                -> 124-02 (TEMPORAL-124-01 + -03 + -08 + -09: sentinel-bounded merge + body byte-identical + watermark + idempotent + memory_event + EVENT_TYPES +2)
      //   test-feynman-timeline-canon-part-9-invariant.cjs -> 124-04 (TEMPORAL-124-10: forbidden-substring sweep + fs-instrument allow-list)
      path.join(REPO_ROOT, 'tests', 'test-feynman-timeline-renderer.cjs'),
      path.join(REPO_ROOT, 'tests', 'test-feynman-timeline-empty-state.cjs'),
      path.join(REPO_ROOT, 'tests', 'test-feynman-timeline-runner.cjs'),
      path.join(REPO_ROOT, 'tests', 'test-feynman-timeline-canon-part-9-invariant.cjs'),
    ```

    NO em-dashes anywhere (hyphens only). NO emoji. The 4 stubs are stubs -- they exit 1 and do nothing else. `tests/run-all-124.sh` is shippable and complete (it will exit 1 today because the 4 CJS_SUITES are RED stubs; that is correct-by-design). The `lib/core/feynman/ROOM.md` file is shippable and complete -- it documents the dir; the renderer and runner will be added next door in Plans 124-01 and 124-02.
  </action>
  <verify>
    <automated>for f in tests/test-feynman-timeline-renderer.cjs tests/test-feynman-timeline-runner.cjs tests/test-feynman-timeline-empty-state.cjs tests/test-feynman-timeline-canon-part-9-invariant.cjs tests/run-all-124.sh lib/core/feynman/ROOM.md; do test -f "$f" || { echo "missing: $f"; exit 1; }; done && for f in tests/test-feynman-timeline-renderer.cjs tests/test-feynman-timeline-runner.cjs tests/test-feynman-timeline-empty-state.cjs tests/test-feynman-timeline-canon-part-9-invariant.cjs; do node "$f" >/dev/null 2>&1 && { echo "stub did not exit 1: $f"; exit 1; }; node "$f" 2>&1 | grep -q "MISSING - Wave" || { echo "stub missing MISSING line: $f"; exit 1; }; done && grep -c "test-feynman-timeline-renderer.cjs\|test-feynman-timeline-runner.cjs\|test-feynman-timeline-empty-state.cjs\|test-feynman-timeline-canon-part-9-invariant.cjs" lib/memory/run-feynman-tests.cjs | awk '$1 >= 4 { exit 0 } { exit 1 }' && bash tests/run-all-124.sh; rc=$?; [ "$rc" -eq 1 ] || { echo "run-all-124.sh expected exit 1 with 4 RED stubs, got $rc"; exit 1; }; grep -q "CJS_SUITES" tests/run-all-124.sh && grep -q "test-feynman-timeline-renderer.cjs" tests/run-all-124.sh && grep -q "Phase 124" lib/core/feynman/ROOM.md && grep -lP "[\x{2014}\x{2013}]" tests/test-feynman-timeline-renderer.cjs tests/test-feynman-timeline-runner.cjs tests/test-feynman-timeline-empty-state.cjs tests/test-feynman-timeline-canon-part-9-invariant.cjs tests/run-all-124.sh lib/core/feynman/ROOM.md && exit 1 || exit 0</automated>
  </verify>
  <done>
    4 RED stub files exist; each exits 1 on direct invocation with a `MISSING - Wave N must implement ...` stderr line; `tests/run-all-124.sh` exists, mirrors `tests/run-all-110.sh`, runs to completion and exits 1 (4 RED stubs); `lib/core/feynman/ROOM.md` exists as the ICM Layer 0 identity; `lib/memory/run-feynman-tests.cjs` registers all 4 new test paths in a Phase 124 block; zero em-dashes or en-dashes in any file written.
  </done>
</task>

</tasks>

<verification>
- All 8 file paths in `files_modified` exist on disk.
- `node tests/test-feynman-timeline-renderer.cjs` exits 1 with stderr containing `MISSING - Wave 1 must implement` (proves the stub is RED, not silently passing); the other 3 stubs exit 1 with their respective `MISSING - Wave N ...` lines.
- `bash tests/run-all-124.sh` runs to completion and exits 1 (4 RED CJS_SUITES); the per-suite output shows 4 FAILED entries.
- `node lib/memory/run-feynman-tests.cjs` runs to completion (the 4 new entries are picked up; they fail RED, which is expected at Wave 0 -- the existing suite invocation pattern is not broken).
- `grep -c "TEMPORAL-124-0\|TEMPORAL-124-10" .planning/REQUIREMENTS.md` returns 20 (10 requirement entries + 10 Status-table rows).
- `grep -P "[\x{2014}\x{2013}]" tests/test-feynman-timeline-*.cjs tests/run-all-124.sh lib/core/feynman/ROOM.md` returns zero matches; the new `## FEYNMAN.md Temporal Awareness (TEMPORAL-124)` section of REQUIREMENTS.md and the modified `### Phase 124` block of ROADMAP.md both pass the same em-dash/en-dash check restricted to those regions.
</verification>

<success_criteria>
Wave 0 substrate is shippable when:
1. The 10 TEMPORAL-124-XX requirement IDs are registered in `.planning/REQUIREMENTS.md` with goal-shaped descriptions tracing to D-00..D-12, plus 10 `Pending` Status-table rows.
2. The `### Phase 124` block of `.planning/ROADMAP.md` lists Requirements `TEMPORAL-124-01..10`, enumerates plans 124-00 through 124-04 as a checkbox list.
3. The 4 RED test stubs exist; each exits 1 with the canonical `MISSING - Wave N must implement ... (Plan 124-NN)` message.
4. `tests/run-all-124.sh` exists, mirrors `tests/run-all-110.sh`, and runs to completion (exits 1 today because the 4 CJS_SUITES are RED stubs -- correct-by-design).
5. `lib/memory/run-feynman-tests.cjs` registers the 4 new test files in `TEST_FILES[]` in a Phase 124 block.
6. `lib/core/feynman/ROOM.md` exists as the ICM Layer 0 identity for the dir where the renderer + runner will ship.
7. Zero em-dashes or en-dashes in any file written by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/124-feynman-temporal-awareness/124-00-SUMMARY.md` recording: artifacts created, the REQUIREMENTS table state (10 Pending rows added), the ROADMAP Phase 124 block diff, the Feynman-runner registry diff, and the Wave 1 plans (124-01, 124-02) now ready to run sequentially (124-02 depends on 124-01). Note any deviations from the plan (none expected; the substrate is fully specified).
</output>
