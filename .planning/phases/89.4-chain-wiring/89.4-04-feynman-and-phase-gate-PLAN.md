---
phase: 89.4
plan: "04"
slug: feynman-and-phase-gate
type: execute
wave: 4
depends_on: ["89.4-01", "89.4-02", "89.4-03"]
files_modified:
  - lib/memory/run-feynman-tests.cjs
  - .planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md
  - .planning/phases/89.4-chain-wiring/89.4-VERIFICATION.md
autonomous: true
canon_parts: [part-3, part-7, part-8]
milestone: v1.11.0
bundled_release: true
success_criteria_addressed: [SC-07, SC-08]

must_haves:
  truths:
    - "lib/memory/run-feynman-tests.cjs TEST_FILES array EXTENDED IN PLACE with 3 new entries (NOT forked) -- test-rs-canon-violations.cjs (89.4-01) + test-rs-chain-feeder-core.cjs (89.4-02) + test-rs-chain-feeder-skill-spawn.cjs (89.4-03); each entry preceded by a Phase 89.4-NN comment header (mirror Phase 89.3-NN pattern at lines 559-568); baseline advances from 82 (post-89.3) to 85 (post-89.4) in a single registration sweep commit"
    - "Single-sweep registration commit: Plan 89.4-04 ships ALL 3 entries in ONE commit per the Phase 89.3-05 single-sweep pattern (avoids parallel-plan contention on the TEST_FILES array; Plans 89.4-01 / 02 / 03 ran in waves 1 / 2 / 3 and never touched run-feynman-tests.cjs to avoid merge conflicts on the same array)"
    - "All 3 new fixture suites still green via direct invocation: test-rs-canon-violations.cjs (8/8 passed) + test-rs-chain-feeder-core.cjs (N/N passed) + test-rs-chain-feeder-skill-spawn.cjs (11/11 passed); also verified via the runner spawnSync exit-0 path"
    - "Feynman runner reports >= 85/85 PASS for the 89.4 contract (3 new green suites added; pre-existing 3 failures from 89.3 G8 inheritance remain CONDITIONAL PASS scope; same explanation pattern as 89.3-PHASE-GATE.md G8)"
    - "89.4-PHASE-GATE.md transcript ships with 9 G asserts (mirror 89.3-PHASE-GATE.md G1-G9 invariant pattern) + 5 NEW E asserts specific to 89.4 (E1: chain-feeder rule coverage; E2: verb validation across all 10 + 1 invalid; E3: Brain query chokepoint exclusivity; E4: defense-in-depth audit count; E5: end-to-end smoke rs-output -> chain feeder -> metadata block)"
    - "89.4-VERIFICATION.md ships with all 8 ROADMAP SCs verified (SC-01 lookupUpstream + SC-02 emitChainMetadata + SC-03 validateVerb + SC-04 5-rule table + SC-05 Brain chokepoint + SC-06 Canon Part 8 + SC-07 Feynman registered + SC-08 NO release gate); each SC has disk + runtime + chain-composability evidence pointers"
    - "G1-G9 invariants from 89.3 preserved in 89.4 verbatim: G1 zero new runtime deps (across 89.4 commit range); G2 BSL 1.1 headers; G3 zero em-dashes; G4 Canon Part 8 forbidden-field audit on production .cjs; G5 chokepoint exclusivity (rs-chain-feeder.cjs in-place enhancement; module.exports stable); G6 new Feynman suites registered (3 entries); G7 each new fixture suite green; G8 full Feynman runner green (CONDITIONAL PASS for inherited 89.3 failures); G9 ExternalEgressViolation surfaces only via shared class"
    - "5 new E asserts wired into 89.4-PHASE-GATE.md: E1 (chain-feeder rule coverage = 5 positive + 1 no-match + 1 first-match-wins); E2 (verb validation = 10 valid + 1 invalid throws CanonVerbViolation); E3 (Brain query chokepoint = grep zero direct fetch in rs-chain-feeder.cjs; brainClient.query reused); E4 (defense-in-depth = auditQueryObject call count >= 3 in rs-chain-feeder.cjs); E5 (end-to-end smoke = mock pair -> chain feeder -> metadata block with non-null spawn_skill on Rule 1 input)"
    - "NO release gate -- v1.11.0 ships at Phase 91.x per milestone section 7 (bundled_release: true frontmatter; no CHANGELOG / plugin.json / package.json diffs in 89.4 commit range)"
  artifacts:
    - path: lib/memory/run-feynman-tests.cjs
      provides: "TEST_FILES array extended with 3 new 89.4 entries (test-rs-canon-violations.cjs + test-rs-chain-feeder-core.cjs + test-rs-chain-feeder-skill-spawn.cjs); baseline 82 -> 85"
      min_lines: 608
    - path: .planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md
      provides: "9 G asserts + 5 E asserts transcript with verbatim observed/expected; commit-range diffs; CONDITIONAL PASS status (89.3 G8 inheritance pattern); invariant coverage table (89.1a + 89.1 + 89.2 + 89.3 invariants preserved + 89.4 new invariants enumerated)"
      min_lines: 350
    - path: .planning/phases/89.4-chain-wiring/89.4-VERIFICATION.md
      provides: "8 SC verification report with disk + runtime + chain-composability evidence pointers; goal achievement summary; Phase 89.4's own contract closed"
      min_lines: 200
  key_links:
    - from: "lib/memory/run-feynman-tests.cjs::TEST_FILES"
      to: "3 new 89.4 test files (canon-violations + chain-feeder-core + chain-feeder-skill-spawn)"
      via: "single-sweep array extension (NOT forked)"
      pattern: "test-rs-canon-violations\\.cjs|test-rs-chain-feeder-core\\.cjs|test-rs-chain-feeder-skill-spawn\\.cjs"
    - from: ".planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md"
      to: ".planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md"
      via: "G1-G9 invariant pattern mirrored verbatim; 5 new E asserts added for 89.4 specifics"
      pattern: "G1|G2|G3|G4|G5|G6|G7|G8|G9|E1|E2|E3|E4|E5"
    - from: ".planning/phases/89.4-chain-wiring/89.4-VERIFICATION.md"
      to: ".planning/ROADMAP.md Phase 89.4 entry SC-01..SC-08"
      via: "8 SC verification with evidence pointers; Phase 89.4 contract closure"
      pattern: "SC-0[1-8]"
---

<objective>
Close Phase 89.4 -- ship the Feynman registration sweep + Phase Gate transcript + VERIFICATION report. This is Wave 4 (after Plan 89.4-01 / 02 / 03 land in Waves 1 / 2 / 3). No new production code; this plan integrates the prior 3 plans' deliverables and produces the closure artifacts.

**Three deliverables:**

1. **Feynman registration sweep** -- single commit adds 3 entries to `lib/memory/run-feynman-tests.cjs` TEST_FILES array (mirror Phase 89.3-05 single-sweep pattern):
   - `test-rs-canon-violations.cjs` (89.4-01; 8 scenarios)
   - `test-rs-chain-feeder-core.cjs` (89.4-02; ~8 scenarios + A1 sweep)
   - `test-rs-chain-feeder-skill-spawn.cjs` (89.4-03; 11 scenarios)
   - Baseline advances 82 (post-89.3) -> 85 (post-89.4)
   - Each entry preceded by `// Phase 89.4-NN: ...` comment header (mirror lines 559-568)

2. **Phase Gate transcript** at `.planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md`:
   - Mirror `89.3-PHASE-GATE.md` G1-G9 invariant structure VERBATIM
   - Add 5 NEW E asserts specific to 89.4:
     - E1: chain-feeder rule coverage (5 positive matches + 1 no-match + 1 first-match-wins)
     - E2: verb validation (10 valid pass + 1 invalid throws CanonVerbViolation)
     - E3: Brain query chokepoint exclusivity (grep zero direct fetch in rs-chain-feeder.cjs; brainClient.query reused)
     - E4: defense-in-depth (auditQueryObject call count >= 3 in rs-chain-feeder.cjs)
     - E5: end-to-end smoke (mock pair -> chain feeder -> metadata block with non-null spawn_skill on Rule 1 input)
   - Status: CONDITIONAL PASS (inherits 89.3 G8 pre-existing failures pattern)

3. **VERIFICATION report** at `.planning/phases/89.4-chain-wiring/89.4-VERIFICATION.md`:
   - Mirror `89.3-VERIFICATION.md` structure
   - 8 SCs from ROADMAP Phase 89.4 entry verified with disk + runtime + chain-composability evidence
   - Phase 89.4 contract closure statement

**Why a separate Wave 4 plan:** Per CONTEXT.md §Claude's Discretion plan recommendation. Plans 89.4-01 / 02 / 03 ran in parallel-friendly waves; touching run-feynman-tests.cjs in any of them would have caused merge conflicts on the TEST_FILES array. Wave 4 single-sweep is the same pattern Phase 89.3-05 used (per 89.3-PHASE-GATE.md G6 explanation lines 113-123).

**No production code in this plan.** All production deliverables shipped in Plans 89.4-01 / 02 / 03. Wave 4 is closure-only.

Purpose: Ships SC-07 (Feynman test suite stays green; 3 new tests registered; baseline 82 -> 85+) + SC-08 (NO release gate; bundled into v1.11.0 final ship at Phase 91.x). Closes Phase 89.4 with full verification + invariant coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89.4-chain-wiring/89.4-CONTEXT.md
@.planning/milestones/v1.11.0-KICKOFF.md
@.planning/phases/89.4-chain-wiring/89.4-01-rs-canon-violations-PLAN.md
@.planning/phases/89.4-chain-wiring/89.4-02-rs-chain-feeder-core-PLAN.md
@.planning/phases/89.4-chain-wiring/89.4-03-rs-chain-feeder-skill-spawn-PLAN.md

# Templates to mirror
@.planning/phases/89.3-mind-map-and-aura-schema/89.3-PHASE-GATE.md
@.planning/phases/89.3-mind-map-and-aura-schema/89.3-VERIFICATION.md

# Feynman runner to extend
@lib/memory/run-feynman-tests.cjs

<interfaces>
<!-- Feynman registration pattern (mirror Phase 89.3-05 lines 559-568). -->

In lib/memory/run-feynman-tests.cjs, after the Phase 89.3-05 entry at line 568:
```javascript
  path.join(REPO_ROOT, 'lib', 'memory', 'test-bridge-writer-enhanced.cjs'),
  // Phase 89.4-01: rs-canon-violations (Canon Part 3 closed-vocabulary enforcement; CanonVerbViolation + CANONICAL_VERBS + validateVerb)
  path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-canon-violations.cjs'),
  // Phase 89.4-02: rs-chain-feeder-core (lookupUpstream + emitChainMetadata; Brain FEEDS_INTO topology via existing chokepoint; Canon Part 8 audit at both entry points)
  path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-chain-feeder-core.cjs'),
  // Phase 89.4-03: rs-chain-feeder-skill-spawn (5-rule deterministic table per kickoff section 6.4; first-match-wins; recommendSkillSpawn body wired in place)
  path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-chain-feeder-skill-spawn.cjs'),
];
```

Phase Gate template (mirror 89.3-PHASE-GATE.md):
- Frontmatter (phase + slug + plan + doc_type + status + executed_at + milestone + bundled_release + canon_parts)
- Gate Summary (total / passed / conditional / failed / status)
- G1-G9 transcript (mirror 89.3 structure)
- E1-E5 transcript (NEW for 89.4)
- Invariant Coverage table (89.1a + 89.1 + 89.2 + 89.3 preserved + 89.4 new)
- Gate Signature

VERIFICATION template (mirror 89.3-VERIFICATION.md):
- Frontmatter (phase + verified + status + score + canon_parts + milestone + bundled_release)
- Goal Achievement Summary
- Success Criteria Verification (SC-01 through SC-08; each with VERIFIED status + Evidence + Source)
- Closing statement (Phase 89.4's own contract met; bundled into v1.11.0)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Single-sweep Feynman registration -- 3 new 89.4 entries; baseline 82 -> 85</name>
  <files>lib/memory/run-feynman-tests.cjs</files>
  <action>
    EDIT `lib/memory/run-feynman-tests.cjs` IN PLACE. Find line 568 (last entry: `path.join(REPO_ROOT, 'lib', 'memory', 'test-bridge-writer-enhanced.cjs'),`) and APPEND 3 new entries before the closing `];` of the TEST_FILES array (around line 569).

    Each entry preceded by a `// Phase 89.4-NN: ...` comment header (mirror lines 559-568 pattern verbatim):

    ```javascript
      // Phase 89.4-01: rs-canon-violations (Canon Part 3 closed-vocabulary enforcement; CanonVerbViolation + CANONICAL_VERBS + validateVerb)
      path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-canon-violations.cjs'),
      // Phase 89.4-02: rs-chain-feeder-core (lookupUpstream + emitChainMetadata; Brain FEEDS_INTO topology via existing chokepoint; Canon Part 8 audit at both entry points)
      path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-chain-feeder-core.cjs'),
      // Phase 89.4-03: rs-chain-feeder-skill-spawn (5-rule deterministic table per kickoff section 6.4; first-match-wins; recommendSkillSpawn body wired in place)
      path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-chain-feeder-skill-spawn.cjs'),
    ];
    ```

    Verify TEST_FILES.length advances from 82 to 85 by running:
    ```
    node -e "const m = require('./lib/memory/run-feynman-tests.cjs'); /* runner exits via process.exit so we cannot inspect TEST_FILES via require */"
    ```
    Instead, grep-verify the registration:
    ```
    grep -c "Phase 89.4-0[1-3]" lib/memory/run-feynman-tests.cjs   # expect 3
    grep -c "test-rs-canon-violations.cjs\\|test-rs-chain-feeder-core.cjs\\|test-rs-chain-feeder-skill-spawn.cjs" lib/memory/run-feynman-tests.cjs   # expect 3
    ```

    Run the FULL Feynman runner to confirm:
    ```
    node lib/memory/run-feynman-tests.cjs
    ```
    Expected output (or substring): `Feynman test runner: >=82/85 passed` (allowing for pre-existing 89.3 G8 inherited 3 failures; 89.4's 3 new suites must all PASS).

    Commit message: `test(89.4-04): register 3 new 89.4 fixture suites in Feynman runner; baseline 82 -> 85`. Per Canon Part 8: ZERO em-dashes (project hard rule). Pure CJS preserved.
  </action>
  <verify>
    <automated>grep -c "Phase 89.4-0[1-3]" lib/memory/run-feynman-tests.cjs | grep -q "^3$" && grep -c "test-rs-canon-violations.cjs\|test-rs-chain-feeder-core.cjs\|test-rs-chain-feeder-skill-spawn.cjs" lib/memory/run-feynman-tests.cjs | grep -q "^3$" && node lib/memory/test-rs-canon-violations.cjs && node lib/memory/test-rs-chain-feeder-core.cjs && node lib/memory/test-rs-chain-feeder-skill-spawn.cjs</automated>
  </verify>
  <done>3 new entries in TEST_FILES array; 3 comment headers added; 3 direct invocations exit 0; commit in git log.</done>
</task>

<task type="auto">
  <name>Task 2: Ship Phase Gate transcript (9 G + 5 E asserts; mirror 89.3-PHASE-GATE.md)</name>
  <files>.planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md</files>
  <action>
    Create `.planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md`. Mirror `89.3-PHASE-GATE.md` STRUCTURE byte-for-byte; substitute 89.4-specific values.

    Frontmatter:
    ```yaml
    ---
    phase: 89.4
    slug: chain-wiring
    plan: "04"
    doc_type: phase-gate-transcript
    status: CONDITIONAL PASS  # OR PASS depending on G8 result
    executed_at: <ISO timestamp at run time>
    milestone: v1.11.0
    bundled_release: true
    canon_parts: [part-3, part-7, part-8]
    ---
    ```

    Sections (verbatim mirror with 89.4 substitutions):

    1. **# Phase 89.4 Phase Gate Transcript** -- intro paragraph naming the 9 G + 5 E asserts; base commit (use the commit just before Plan 89.4-01 first commit lands); HEAD commit at run time.

    2. **## Gate Summary** -- total 14; passed N; conditional N; failed 0; status CONDITIONAL PASS or PASS.

    3. **## Assert Transcript** with 9 G asserts (mirror 89.3 G1-G9 verbatim with 89.4 substitutions):
       - G1: Zero new runtime deps in package.json across 89.4 commit range. Command: `git diff <89.4-base>..HEAD -- package.json | wc -l`. Expected 0.
       - G2: BSL 1.1 headers on every new/modified .cjs (rs-canon-violations.cjs + rs-chain-feeder.cjs + 3 test files). Command per file: `grep -c 'BSL 1.1' <file>`.
       - G3: Zero em-dashes on every new/modified file. Command per file: `grep -cP '[\x{2013}\x{2014}]' <file>`.
       - G4: Canon Part 8 forbidden-field audit on production .cjs (rs-canon-violations.cjs + rs-chain-feeder.cjs). Command per file: `grep -cE 'governing_thought[^_]|identity_text|decision_log|meeting_transcript|artifact_body' <file>` (test fixtures excluded per 89.1a + 89.1 + 89.2 + 89.3 scope note).
       - G5: Chokepoint exclusivity preserved (rs-chain-feeder.cjs in-place enhancement; module.exports stable across Plan 89.4-02 -> 89.4-03 swap; lookupUpstream + emitChainMetadata + recommendSkillSpawn signatures unchanged). Inline node check.
       - G6: 3 new Feynman suites registered (Phase 89.4-01/02/03). Command: `grep -cE 'Phase 89.4-0[1-3]' lib/memory/run-feynman-tests.cjs`. Expected 3.
       - G7: Each new fixture suite green via direct invocation. 3 commands; each exit 0 with final-line `=== 89.4-NN ... suite: N/N passed ===`.
       - G8: Full Feynman runner green. Command: `node lib/memory/run-feynman-tests.cjs > /tmp/full-feynman.log 2>&1; echo "exit=$?"`. Expected: 89.4's 3 new suites all PASS; baseline 82 -> 85; CONDITIONAL PASS for any inherited 89.3 G8 pre-existing failures.
       - G9: ExternalEgressViolation surfaces only via shared class. Per-file grep: `grep -c "class ExternalEgressViolation" lib/core/rs-chain-feeder.cjs` returns 0 (NOT redefined; required via rs-egress-violations.cjs). `grep -c "require.*rs-egress-violations" lib/core/rs-chain-feeder.cjs` >= 1.

    4. **## New 89.4-Specific Asserts (E1-E5)** -- 5 NEW E asserts:

       - **E1: Chain-feeder rule coverage** -- inline node smoke covering all 5 rules positive matches + 1 no-match + 1 first-match-wins. Expected: 5 distinct spawn strings + 'no rule matched' + Rule 1 wins on conflicting input.
       - **E2: Verb validation** -- inline node loop over CANONICAL_VERBS asserting validateVerb returns each verbatim; then 1 invalid 'Cancel' throws CanonVerbViolation. Expected: 10 PASS + 1 throw.
       - **E3: Brain query chokepoint exclusivity** -- grep `lib/core/rs-chain-feeder.cjs` for `fetch(` returns 0; for `brainClient.query|brain-client.cjs` returns >= 2. Expected: zero direct Brain fetch surface; only brainClient.query reused.
       - **E4: Defense-in-depth audit count** -- grep `lib/core/rs-chain-feeder.cjs` for `auditQueryObject(` returns >= 3 (lookupUpstream + emitChainMetadata + recommendSkillSpawn).
       - **E5: End-to-end smoke** -- inline node script: build mock pair {rs_type:'structural_transfer', breakthrough_score:8, active_context:{}}; call emitChainMetadata; verify returned block has spawn_skill === '/mos:find-analogies' (Rule 1) AND recommended_verb passes validateVerb AND feeds_into === 'PWS VP'. Expected: full chain composability proven.

    5. **## Invariant Coverage** table -- two sections:
       - 89.1a + 89.1 + 89.2 + 89.3 invariants preserved by 89.4 (mirror 89.3-PHASE-GATE.md verbatim; add: chokepoint substrate stays Brain-inbound-only; ExternalEgressViolation extraction verified; bridge-writer regression preserved if exercised; 89.3 EDGE_TYPES untouched by 89.4).
       - New 89.4 invariants: CanonVerbViolation sentinel separate from ExternalEgressViolation (rs-canon-violations.cjs separate module); CANONICAL_VERBS Object.freeze'd 10-string array; SKILL_SPAWN_RULES Object.freeze'd 5-entry table; first-match-wins semantics; emitChainMetadata internally calls recommendSkillSpawn (additive output shape backward-compatible); Brain query path uses existing brainClient.query chokepoint (zero new fetch surface); auditQueryObject >= 3 sites in rs-chain-feeder.cjs; recommended_verb validated through validateVerb at emitChainMetadata return.

    6. **## Gate Signature** -- executed by + executed at + base commit + HEAD commit + status.

    Per Canon Part 8: ZERO em-dashes. The transcript reports observed values verbatim (no rounding, no rewriting commands).

    Commit: `docs(89.4-04): ship Phase Gate transcript (9 G + 5 E asserts)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md && grep -cE "G[1-9]:" .planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md | grep -qE "^[9-9]|^[1-9][0-9]" && grep -cE "E[1-5]:" .planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md | grep -qE "^[5-9]|^[1-9][0-9]" && grep -cP '[\x{2013}\x{2014}]' .planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md | grep -q "^0$"</automated>
  </verify>
  <done>Phase Gate file exists; 9 G asserts + 5 E asserts present; zero em-dashes; status (CONDITIONAL PASS or PASS) recorded; commit in git log.</done>
</task>

<task type="auto">
  <name>Task 3: Ship VERIFICATION report (8 SCs verified; mirror 89.3-VERIFICATION.md)</name>
  <files>.planning/phases/89.4-chain-wiring/89.4-VERIFICATION.md</files>
  <action>
    Create `.planning/phases/89.4-chain-wiring/89.4-VERIFICATION.md`. Mirror `89.3-VERIFICATION.md` STRUCTURE byte-for-byte; substitute 89.4-specific values.

    Frontmatter:
    ```yaml
    ---
    phase: 89.4-chain-wiring
    verified: <ISO timestamp>
    status: passed
    score: 8/8 success criteria verified
    canon_parts: [part-3, part-7, part-8]
    milestone: v1.11.0
    bundled_release: true
    re_verification: false
    verifier: gsd-executor (Claude Opus 4.7 1M)
    phase_gate_status: <mirror Phase Gate Task 2 status>
    phase_gate_commit: <commit sha of Phase Gate>
    verification_head_commit: <HEAD at verification time>
    ---
    ```

    Sections (verbatim mirror with 89.4 substitutions):

    1. **# Phase 89.4: Chain Wiring (Upstream + Downstream) -- Verification Report** -- intro + Phase Goal (one paragraph mirroring ROADMAP entry); Verified date; Status; Re-verification (false; initial verification).

    2. **## Goal Achievement Summary** -- short paragraph: all 8 SC-01..SC-08 verified; Phase Gate transcript reports CONDITIONAL PASS / PASS; Phase 89.4's own contract met (3 new modules + 1 in-place enhancement of rs-chain-feeder.cjs across waves + 3 new fixture suites + Feynman registration sweep + Phase Gate + this VERIFICATION).

    3. **## Success Criteria Verification** -- 8 sections (SC-01 through SC-08); each with:
       - **SC-NN: <verbatim text from ROADMAP Phase 89.4 entry>**
       - **Status:** VERIFIED
       - **Evidence:** disk + runtime + chain-composability pointers (file paths + grep commands + smoke test outputs)
       - **Source:** Plan 89.4-NN SUMMARY (commits sha-prefixed) + Phase Gate G/E asserts

       Example SC-01:
       - SC-01: rs-chain-feeder.cjs exports lookupUpstream(problem_type, stage)
       - Status: VERIFIED
       - Evidence:
         - File exists on disk: `lib/core/rs-chain-feeder.cjs` (BSL 1.1 header; Plan 89.4-02 SUMMARY)
         - Export shape: `node -e "console.log(typeof require('./lib/core/rs-chain-feeder.cjs').lookupUpstream)"` -> `'function'` (AsyncFunction)
         - Brain unreachable graceful degradation: smoke test in Phase Gate E5 returns {state: 'ready'} when brainClient.isAvailable returns false
         - Pause-state with missing upstream: test-rs-chain-feeder-core.cjs Test 2 PASS
         - Ready-state with all upstream present: test-rs-chain-feeder-core.cjs Test 3 PASS
       - Source: Plan 89.4-02 SUMMARY (commits <sha-RED> + <sha-GREEN>); Phase Gate G7 + E5 + E3.

       Repeat for SC-02 through SC-08:
       - SC-02: emitChainMetadata exported with verb + feeds_into + spawn_skill block
       - SC-03: validateVerb throws on unknown verb (Canon Part 3 closed vocabulary)
       - SC-04: 5-rule table per kickoff section 6.4 implemented and tested
       - SC-05: Brain queries pass through existing brain-client chokepoint (zero new Brain query surface)
       - SC-06: Canon Part 8 audit; chain metadata never carries user content
       - SC-07: Feynman test suite stays green; new tests registered (82 -> 85)
       - SC-08: NO release gate; bundled into v1.11.0 final ship at Phase 91.x

    4. **## Closing Statement** -- Phase 89.4 contract met; bundled into v1.11.0; pointer to next phase (89.5 Engine Top-Level Orchestrator + Bidirectional NL-Graph Surface).

    Per Canon Part 8: ZERO em-dashes. Use real timestamps + real commit shas (not placeholders) at runtime.

    Commit: `docs(89.4-04): ship VERIFICATION report (8/8 SCs verified)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/89.4-chain-wiring/89.4-VERIFICATION.md && grep -cE "SC-0[1-8]" .planning/phases/89.4-chain-wiring/89.4-VERIFICATION.md | grep -qE "^[8-9]|^[1-9][0-9]" && grep -c "VERIFIED" .planning/phases/89.4-chain-wiring/89.4-VERIFICATION.md | grep -qE "^[8-9]|^[1-9][0-9]" && grep -cP '[\x{2013}\x{2014}]' .planning/phases/89.4-chain-wiring/89.4-VERIFICATION.md | grep -q "^0$"</automated>
  </verify>
  <done>VERIFICATION file exists; 8 SC sections present; 8 VERIFIED markers; zero em-dashes; status: passed; commit in git log.</done>
</task>

</tasks>

<verification>
- `node lib/memory/run-feynman-tests.cjs > /tmp/89.4-feynman.log 2>&1; tail -1 /tmp/89.4-feynman.log` reports `Feynman test runner: >=82/85 passed, ...` (3 new 89.4 suites green; pre-existing 89.3 G8 inherited 3 failures may persist)
- `grep -c "Phase 89.4-0[1-3]" lib/memory/run-feynman-tests.cjs` returns 3
- `node lib/memory/test-rs-canon-violations.cjs` exits 0
- `node lib/memory/test-rs-chain-feeder-core.cjs` exits 0
- `node lib/memory/test-rs-chain-feeder-skill-spawn.cjs` exits 0
- `test -f .planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md` returns success
- `test -f .planning/phases/89.4-chain-wiring/89.4-VERIFICATION.md` returns success
- `grep -cE "G[1-9]:" .planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md` returns >= 9
- `grep -cE "E[1-5]:" .planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md` returns >= 5
- `grep -cE "SC-0[1-8]" .planning/phases/89.4-chain-wiring/89.4-VERIFICATION.md` returns >= 8
- `grep -cP '[\x{2013}\x{2014}]' lib/memory/run-feynman-tests.cjs .planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md .planning/phases/89.4-chain-wiring/89.4-VERIFICATION.md` returns 0+0+0
- Bundled release verified: `git diff <89.4-base>..HEAD -- CHANGELOG.md plugin.json package.json | wc -l` returns 0 (NO release gate; bundled into v1.11.0 at Phase 91.x)
</verification>

<success_criteria>
- SC-07 (Feynman test suite stays green; new tests registered) FULLY ADDRESSED -- 3 entries added in single sweep; baseline 82 -> 85; runner exits 0 (or CONDITIONAL PASS for inherited 89.3 failures)
- SC-08 (NO release gate; bundled into v1.11.0 final ship at Phase 91.x) FULLY ADDRESSED -- bundled_release: true frontmatter on every 89.4 plan; zero release artifact diffs in 89.4 commit range
- Phase Gate transcript ships with 9 G + 5 E asserts (CONDITIONAL PASS pattern inherited from 89.3 G8)
- VERIFICATION report ships with 8/8 SCs verified
- Phase 89.4 closure complete; ready to start Phase 89.5 (Engine Top-Level Orchestrator + Bidirectional NL-Graph Surface)
- All 89.1a + 89.1 + 89.2 + 89.3 invariants preserved (chokepoint substrate stays Brain-inbound-only; ExternalEgressViolation shared class; FORBIDDEN_PATTERNS authoritative source unchanged; lazygraph-ops EDGE_TYPES untouched; bridge-writer untouched)
</success_criteria>

<output>
After completion, create `.planning/phases/89.4-chain-wiring/89.4-04-SUMMARY.md` with:
- Files touched (run-feynman-tests.cjs extended in place; 2 new docs created)
- Atomic commit list (3 commits: registration sweep + Phase Gate + VERIFICATION; sha-prefixed)
- Feynman baseline transition (82 -> 85)
- Phase Gate status (CONDITIONAL PASS or PASS)
- VERIFICATION SC count (8/8)
- Pointer to Phase 89.5 (next phase in v1.11.0 bundled milestone)
- Closure statement: Phase 89.4 contract met; chain mechanism per kickoff section 6 (USER LOAD-BEARING INTENT 7) shipped; bundled into v1.11.0
</output>
