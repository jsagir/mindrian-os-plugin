---
phase: 232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/wiki/room-home.cjs
  - lib/wiki/briefing.cjs
  - tests/test-232-room-home.cjs
  - tests/test-232-briefing.cjs
  - tests/run-all-232.sh
  - tests/fixtures/wiki-room-232/STATE.md
  - tests/fixtures/wiki-room-232/problem-definition/test-article.md
  - tests/fixtures/wiki-room-232/market-analysis/other-note.md
autonomous: true
requirements: ["SPEC Req 4"]
must_haves:
  truths:
    - "Room Home data (governing thought, venture stage, entry counts, section progress, gaps) is assembled from real STATE.md / readTriple state, never hardcoded"
    - "Larry's Briefing generates via the in-repo raw-fetch Anthropic pattern; with no key it degrades to a structured no_api_key result without throwing"
    - "The Room Home body HTML renders the governing thought, stage chip, gaps list, per-section progress bars, and a Generate Briefing control"
  artifacts:
    - path: "lib/wiki/room-home.cjs"
      provides: "STATE.md parser + Room Home data assembly + body HTML renderer (D-05)"
      exports: ["parseStateMd", "getRoomHomeData", "renderRoomHomeBody"]
      min_lines: 120
    - path: "lib/wiki/briefing.cjs"
      provides: "Larry's Briefing generator over raw-fetch Anthropic transport (D-06)"
      exports: ["generateBriefing"]
      min_lines: 60
    - path: "tests/run-all-232.sh"
      provides: "Phase 232 test aggregator (auto-discovers tests/test-232-*.cjs)"
  key_links:
    - from: "lib/wiki/room-home.cjs"
      to: "lib/core/state-ops.cjs"
      via: "require getState/computeState"
      pattern: "state-ops\\.cjs"
    - from: "lib/wiki/room-home.cjs"
      to: "lib/core/folder-memory.cjs"
      via: "readTriple(roomDir).reasoning.governing_thought"
      pattern: "readTriple"
    - from: "lib/wiki/briefing.cjs"
      to: "lib/core/mva-classifier.cjs"
      via: "require resolveAnthropicKey"
      pattern: "resolveAnthropicKey"
---

<objective>
Build the two server-side data services behind Room Home (SPEC Req 4): the deterministic
room-state assembler per D-05 (reuse lib/core/state-ops.cjs + a small newly-ported
STATE.md parser, the port target CONTEXT.md names in place of the off-disk
rooms.ts::getRoomState) and the Larry's Briefing generator per D-06 (reuse
resolveAnthropicKey() + the raw-fetch POST https://api.anthropic.com/v1/messages pattern
from lib/core/mva-classifier.cjs - the Vercel AI SDK transport was explicitly rejected in
D-06). Pure new files; wiki-server.cjs wiring happens in Plan 04.

Purpose: Room Home must be sourced from the room's real STATE.md / compute-state data,
not the graph and not fixtures (SPEC Req 4 target).
Output: lib/wiki/room-home.cjs, lib/wiki/briefing.cjs, hermetic tests, the phase test
aggregator, and the shared fixture room used by Plans 04 and 06.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-SPEC.md
@.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-CONTEXT.md
@.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-UI-SPEC.md
@lib/core/state-ops.cjs
@lib/core/mva-classifier.cjs
@scripts/compute-state
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: room-home.cjs - STATE.md parser + Room Home data + body renderer (D-05)</name>
  <files>lib/wiki/room-home.cjs, tests/test-232-room-home.cjs, tests/run-all-232.sh, tests/fixtures/wiki-room-232/STATE.md, tests/fixtures/wiki-room-232/problem-definition/test-article.md, tests/fixtures/wiki-room-232/market-analysis/other-note.md</files>
  <read_first>
    - scripts/compute-state (lines 190-260: the exact STATE.md format this parser consumes - frontmatter keys `computed`, `venture_stage`, `total_entries`; `## Sections` pipe table with columns Section|Entries|Progress|Status|Last Updated; `## Gaps` as `- section: message` bullets or the literal "No gaps detected -- all sections have entries.")
    - lib/core/state-ops.cjs (getState/computeState contract - computeState runs bash with 10s timeout and writes STATE.md; getState returns null if missing)
    - lib/core/folder-memory.cjs (readTriple(sectionPath) at line 91 - graceful all-empty shell when path/MINTO.md missing; reasoning.governing_thought is the field Room Home displays)
    - .planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-UI-SPEC.md (Component Inventory item 2 - Room Home panel list; Copywriting Contract - empty-state and CTA copy; Color section - blue progress bars, green/yellow chip rules)
    - tests/run-all-218.sh (aggregator header pattern to mirror)
  </read_first>
  <behavior>
    - Test 1: parseStateMd on a STATE.md string with frontmatter (venture_stage: Discovery, total_entries: 3), a Sections table with rows problem-definition(2 entries)/market-analysis(1 entry), and 2 gap bullets returns { ventureStage:'Discovery', totalEntries:3, sections:[{name:'problem-definition',entries:2,...},{name:'market-analysis',entries:1,...}], gaps:[{section,message},{section,message}] }
    - Test 2: parseStateMd on the "No gaps detected" variant returns gaps: []
    - Test 3: parseStateMd on null/empty/garbage input returns a safe empty shape (ventureStage:null, totalEntries:0, sections:[], gaps:[]) - never throws
    - Test 4: getRoomHomeData against tests/fixtures/wiki-room-232 (which has STATE.md but NO MINTO.md) returns governingThought null and the parsed STATE fields - the readTriple degrade path
    - Test 5: renderRoomHomeBody output contains id="room-home", the governing-thought fallback copy when null, the stage chip text, every section name, every gap message, a "Generate Briefing" control, and id="briefing-panel"
    - Test 6: renderRoomHomeBody({...}, { staticExport: true }) contains NO "Generate Briefing" control and instead the empty-state copy "No briefing yet. Generate Larry's read of the room."
  </behavior>
  <action>
    Create lib/wiki/room-home.cjs (CJS, no new dependencies; gray-matter is already a root
    dependency) exporting exactly:

    - `parseStateMd(stateText)` - pure. Frontmatter via gray-matter `matter()`; the
      `## Sections` pipe table parsed row-by-row (skip header + separator rows; columns:
      name, entries int, progress cell ignored, status, lastUpdated); `## Gaps` bullets
      parsed as `- <section>: <message>` (split on first colon; a bullet without a colon
      becomes {section:null, message:whole line}). Returns the shape in Test 1; never
      throws on malformed input (Test 3 shape).
    - `getRoomHomeData(roomDir)` - calls getState(roomDir) from lib/core/state-ops.cjs;
      if null, attempts computeState(roomDir) inside try/catch (fixture rooms without the
      bash toolchain must not crash); parses via parseStateMd. Governing thought via
      `require('../core/folder-memory.cjs').readTriple(path.resolve(roomDir)).reasoning.governing_thought`
      (null/empty -> null). Adds `suggestedNext`: the first gap's message, else null.
      Returns { governingThought, ventureStage, totalEntries, computed, sections, gaps,
      suggestedNext }. Never throws.
    - `renderRoomHomeBody(data, opts)` - returns the Room Home body HTML string per
      UI-SPEC Component Inventory item 2, all dynamic text HTML-escaped via a local
      escHtml: a `<div id="room-home">` wrapper; `h1` display treatment for the governing
      thought with fallback copy `(no MINTO yet)` when null; a venture-stage chip and a
      tabular total-entries stat; a `<section id="briefing-panel">` with the CTA button
      `Generate Briefing` (id="generate-briefing") plus an inline vanilla-JS script that
      POSTs to `/api/briefing` and renders `critical_misses` / `reframe` (left red-bar
      callout) / `next_steps` into the panel, and on { ok:false } renders the UI-SPEC
      red-bar error copy verbatim: "Briefing unavailable." / "Why: no `ANTHROPIC_API_KEY`
      set (or the call failed)." / "Fix: set `ANTHROPIC_API_KEY` and retry. Room stats
      below still work."; a suggested-next-action line from data.suggestedNext; a gaps
      list as a hairline module grid; per-section progress bars - bar width scaled to
      max section entries, fill `var(--blue)` over a `var(--rule)` track. When
      opts.staticExport is true, omit the button + script and render the empty-state copy
      "No briefing yet. Generate Larry's read of the room." (Plan 06 consumes this flag).
      No emoji, no em-dashes, hyphens only.

    Create the shared fixture room tests/fixtures/wiki-room-232/:
    - STATE.md with frontmatter `venture_stage: Discovery`, `total_entries: 3`,
      `computed: 2026-07-20T00:00:00Z`; a `## Sections` table with rows
      problem-definition (2 entries, active) and market-analysis (1 entry, active); a
      `## Gaps` section with two bullets (solution-design and business-model lines in the
      compute-state format).
    - problem-definition/test-article.md with frontmatter `title: Test Article`, body
      containing the literal wikilink `[[other-note]]`, a heading, and the unique search
      token `zx232unique`.
    - market-analysis/other-note.md with frontmatter `title: Other Note`, body containing
      `[[test-article]]`.
    No MINTO.md anywhere in the fixture (Test 4 exercises the degrade path).

    Create tests/test-232-room-home.cjs implementing Tests 1-6 (plain node asserts,
    process.exit(1) on failure, prints PASS lines - mirror the repo's test-2xx style).

    Create tests/run-all-232.sh: bash aggregator that discovers and runs every
    tests/test-232-*.cjs via a glob loop (so Plans 04/05/06 add test files WITHOUT editing
    this harness), prints per-file PASS/FAIL and a roll-up, exits nonzero on any failure.
    Header comment mirrors the run-all-218.sh style (hermetic, zero network).
  </action>
  <verify>
    <automated>node tests/test-232-room-home.cjs && bash tests/run-all-232.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const r=require('./lib/wiki/room-home.cjs'); for(const f of ['parseStateMd','getRoomHomeData','renderRoomHomeBody']) if(typeof r[f]!=='function') process.exit(1); console.log('PASS')"` prints PASS
    - `node tests/test-232-room-home.cjs` exits 0 with all 6 behaviors green
    - `bash tests/run-all-232.sh` exits 0 and its output lists test-232-room-home.cjs
    - renderRoomHomeBody output for the fixture data contains the literal strings `id="room-home"`, `id="briefing-panel"`, `Generate Briefing`, and both fixture section names; the staticExport variant contains `No briefing yet.` and not `Generate Briefing`
    - `grep -c "generateText\|@ai-sdk\|AI_GATEWAY" lib/wiki/room-home.cjs` returns 0 (D-06 rejected transport absent)
  </acceptance_criteria>
  <done>Room Home data assembles from real STATE.md + readTriple with graceful degrades, and the body renderer emits the UI-SPEC panel set; fixture room + phase aggregator exist for downstream plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: briefing.cjs - Larry's Briefing over raw-fetch Anthropic (D-06)</name>
  <files>lib/wiki/briefing.cjs, tests/test-232-briefing.cjs</files>
  <read_first>
    - lib/core/mva-classifier.cjs (resolveAnthropicKey at line 130 - 4-leg key precedence returning null keyless; the raw fetch call shape at lines 231-255 - headers x-api-key + anthropic-version '2023-06-01', AbortController; text-block extraction at 256-264; the _test.setFetch seam at the module exports, line ~386)
    - lib/wiki/wiki-server.cjs (lines 544-592 - the second in-file Anthropic transport precedent, including the model id string 'claude-sonnet-4-20250514' this plan reuses)
    - .planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-CONTEXT.md (D-06 - the explicit rejection of the ai package / AI_GATEWAY_API_KEY transport)
    - lib/wiki/room-home.cjs (getRoomHomeData - the briefing's deterministic input source, created in Task 1)
  </read_first>
  <behavior>
    - Test 1: generateBriefing with an injected resolveKey returning null resolves { ok:false, error:'no_api_key' } - no network attempted (injected fetch throws if called)
    - Test 2: generateBriefing with injected key + injected fetch returning a canned Anthropic response whose first text block is valid JSON resolves { ok:true, briefing:{ critical_misses:[...], reframe:'...', next_steps:[...] } }
    - Test 3: injected fetch returning ok:false (HTTP 500) resolves { ok:false, error:'api_error' } - never throws
    - Test 4: injected fetch returning a text block that is NOT valid JSON resolves { ok:false, error:'api_error' } - JSON.parse failure is caught
  </behavior>
  <action>
    Create lib/wiki/briefing.cjs (CJS, zero new dependencies) exporting
    `generateBriefing(roomDir, opts)` returning a Promise of
    `{ ok:true, briefing:{ critical_misses:string[], reframe:string, next_steps:string[] } }`
    or `{ ok:false, error:'no_api_key'|'api_error' }`.

    Transport per D-06, copied from the mva-classifier pattern: key via
    `resolveAnthropicKey` required from '../core/mva-classifier.cjs' (already exported);
    null key -> { ok:false, error:'no_api_key' } with no network reach. Raw fetch POST to
    'https://api.anthropic.com/v1/messages' with headers content-type application/json,
    x-api-key, anthropic-version '2023-06-01'; model 'claude-sonnet-4-20250514' (the same
    id string wiki-server.cjs line 560 already pins - do not invent a new pin); max_tokens
    1024; 30s AbortController timeout. Extract text by concatenating j.content[] blocks
    where blk.type === 'text' (the exact mva-classifier extraction shape). Do NOT add the
    'ai' package, generateText, Output.object, or AI_GATEWAY_API_KEY anywhere (D-06
    explicit rejection).

    Prompt design (ports the briefing INTENT from the off-disk prototype briefing.ts, not
    its transport): system prompt instructs Larry's read of a venture Data Room - terse,
    structural, evidence-first, never grades or compliments (Canon Part 12), no em-dashes -
    and REQUIRES a single JSON object as the entire reply:
    { "critical_misses": string[] (max 3), "reframe": string, "next_steps": string[] (max 3) }.
    User content: the room's STATE.md text plus governing thought, obtained via
    getRoomHomeData(roomDir) from './room-home.cjs' (sections/gaps/counts are already in
    the STATE text). JSON.parse the extracted text inside try/catch; strip markdown code
    fences if the model wraps the JSON; any parse/HTTP failure -> { ok:false, error:'api_error' }.

    Test seams (mirror the mva-classifier _test idiom): opts.resolveKey and opts.fetchImpl
    injectable, defaulting to the real ones. Never log or echo the key.

    Create tests/test-232-briefing.cjs implementing Tests 1-4 hermetically (injected seams
    only - no env manipulation, no network; the 4-leg key resolver must never be exercised
    by tests since dev machines may carry real keys).
  </action>
  <verify>
    <automated>node tests/test-232-briefing.cjs && bash tests/run-all-232.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-232-briefing.cjs` exits 0 with all 4 behaviors green
    - `grep -c "AI_GATEWAY\|generateText\|require('ai')\|from 'ai'" lib/wiki/briefing.cjs` returns 0
    - `grep -c "resolveAnthropicKey" lib/wiki/briefing.cjs` returns at least 1, and `grep -c "claude-sonnet-4-20250514" lib/wiki/briefing.cjs` returns 1
    - `grep -c "console.log(.*apiKey\|console.error(.*apiKey" lib/wiki/briefing.cjs` returns 0 (key never logged)
    - `bash tests/run-all-232.sh` exits 0 listing both test files
  </acceptance_criteria>
  <done>Larry's Briefing generates through the in-repo zero-dep Anthropic transport with keyless-safe and parse-safe degrades, fully covered by hermetic tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> api.anthropic.com | Room STATE text + governing thought egress to Anthropic with the user's own key (LOCAL->Anthropic; Canon Part 8 governs LOCAL->Brain, which this never touches) |
| room STATE.md content -> LLM prompt | Room content is untrusted model input (prompt injection surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-232-02-01 | Information Disclosure | briefing.cjs key handling | mitigate | Key resolved via resolveAnthropicKey only, held in local scope, never logged/returned/rendered; grep gate in acceptance criteria |
| T-232-02-02 | Tampering | prompt injection via room content | mitigate | Briefing output is data, not instructions: client renders it as escaped text via escHtml in room-home.cjs's briefing script; parse failures degrade to api_error, never eval |
| T-232-02-03 | Denial of Service | hung Anthropic call | mitigate | 30s AbortController timeout, single call per button press, graceful api_error |
| T-232-02-04 | Spoofing | LOCAL->Brain confusion | accept | No Brain reach exists in these modules (Anthropic-only); Part 8 boundary not crossed |
| T-232-SC | Tampering | package installs | accept | No package-manager installs in this plan (root gray-matter/express already vendored) |
</threat_model>

<verification>
- `bash tests/run-all-232.sh` green (both test files, hermetic, zero network)
- Neither new module appears in any require path of the MCP server or Brain code (new files only referenced by Plan 04's wiki-server wiring)
</verification>

<success_criteria>
- Room Home data assembly + body rendering exist as tested pure services (SPEC Req 4 data layer, D-05)
- Larry's Briefing service exists on the D-06 transport with no_api_key / api_error degrades proven by tests
- Fixture room + run-all-232.sh aggregator in place for Plans 04/05/06
</success_criteria>

<output>
Create `.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 02 slice)

- `lib/wiki/room-home.cjs` - functions `parseStateMd(stateText)`, `getRoomHomeData(roomDir)`, `renderRoomHomeBody(data, opts)` (opts.staticExport flag); DOM ids `room-home`, `briefing-panel`, `generate-briefing`
- `lib/wiki/briefing.cjs` - function `generateBriefing(roomDir, opts)` (opts.resolveKey, opts.fetchImpl test seams); result shape `{ ok, briefing:{critical_misses, reframe, next_steps} } | { ok:false, error:'no_api_key'|'api_error' }`
- `tests/test-232-room-home.cjs`, `tests/test-232-briefing.cjs`, `tests/run-all-232.sh` (glob-discovering aggregator)
- `tests/fixtures/wiki-room-232/` fixture room (STATE.md + 2 articles with reciprocal wikilinks, unique search token `zx232unique`, no MINTO.md)
