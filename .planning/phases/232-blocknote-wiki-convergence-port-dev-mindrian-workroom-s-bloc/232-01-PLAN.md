---
phase: 232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/ui-system/design-system/mos-design-system.css
  - skills/ui-system/design-system/M-OS-DESIGN-SYSTEM.md
  - skills/ui-system/design-system/SPEC.md
  - skills/ui-system/design-system/image-prompt-style.md
  - lib/ui/design-system.cjs
  - lib/wiki/wiki-layout.cjs
autonomous: true
requirements: ["SPEC Req 7", "SPEC Req 8"]
must_haves:
  truths:
    - "Every served wiki page carries the M:OS Canonical Design System v1.1 CSS inline (style tag with data-mos=\"v1.1\")"
    - "The wiki defaults to the light (cream) theme; toggling flips to dark and the choice survives a reload"
    - "The top nav shows the M:OS wordmark with a red colon, Home and Graph tabs, the search box, and the theme toggle"
    - "No wiki page loads fonts from fonts.googleapis.com"
  artifacts:
    - path: "skills/ui-system/design-system/mos-design-system.css"
      provides: "M:OS Canonical Design System v1.1 token bundle (landed from feat/mos-design-system-v1.1)"
      min_lines: 200
    - path: "lib/ui/design-system.cjs"
      provides: "readDesignSystemCss + mosStyleTag loader"
      exports: ["readDesignSystemCss", "mosStyleTag"]
    - path: "lib/wiki/wiki-layout.cjs"
      provides: "M:OS-retokenized wrapInLayout chrome"
      contains: "mosStyleTag"
  key_links:
    - from: "lib/wiki/wiki-layout.cjs"
      to: "lib/ui/design-system.cjs"
      via: "require + mosStyleTag() injected into <head>"
      pattern: "mosStyleTag\\(\\)"
    - from: "lib/ui/design-system.cjs"
      to: "skills/ui-system/design-system/mos-design-system.css"
      via: "readFileSync module-relative path"
      pattern: "mos-design-system\\.css"
---

<objective>
Land the M:OS Canonical Design System v1.1 files (the hard prerequisite flagged by both
232-UI-SPEC.md and 232-PATTERNS.md), then retokenize the wiki chrome
(lib/wiki/wiki-layout.cjs) to the canonical bundle, resolving divergences DV-1 through
DV-6 from 232-UI-SPEC.md. This is the visual foundation every other Phase 232 plan
renders through, and it delivers SPEC Req 8 (dark/light theme toggle, persisted, default
light) plus the SPEC Req 7 search control restyle (hard-offset dropdown, reachable from
every view via the top nav).

Purpose: unblock CSS/theming for all downstream plans; no plan may wire mosStyleTag()
until these files exist on disk (232-PATTERNS.md "Prerequisite (blocks Task 1)").
Output: canonical CSS bundle + loader on disk and committed; wiki-layout.cjs serving
M:OS-token chrome.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-SPEC.md
@.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-UI-SPEC.md
@.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-PATTERNS.md
@lib/wiki/wiki-layout.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Land the canonical design-system files from feat/mos-design-system-v1.1</name>
  <files>skills/ui-system/design-system/mos-design-system.css, skills/ui-system/design-system/M-OS-DESIGN-SYSTEM.md, skills/ui-system/design-system/SPEC.md, skills/ui-system/design-system/image-prompt-style.md, lib/ui/design-system.cjs</files>
  <read_first>
    - .planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-PATTERNS.md (section "Prerequisite (blocks Task 1)" - the exact loader contract and why this blocks everything)
    - .planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-UI-SPEC.md (section 0 "Design-Token Authority" + "Hard dependency / risk")
  </read_first>
  <action>
    Land ONLY the design-system runtime artifacts from the unmerged branch, not the full
    branch. Planner verification (2026-07-20): `git diff --name-only c0149c36..main` over
    the 13 files that branch touches returns EMPTY - main has not diverged on any of them,
    so file-level landing is conflict-free and a later full merge of
    `origin/feat/mos-design-system-v1.1` will see identical content on both sides.

    Run: `git checkout origin/feat/mos-design-system-v1.1 -- skills/ui-system/design-system lib/ui/design-system.cjs`

    This lands exactly five files: the 4 files under skills/ui-system/design-system/
    (mos-design-system.css, M-OS-DESIGN-SYSTEM.md, SPEC.md, image-prompt-style.md) and
    lib/ui/design-system.cjs (exports { readDesignSystemCss, mosStyleTag }; mosStyleTag()
    returns '<style data-mos="v1.1">...</style>' or '' if the CSS is missing, never throws).

    Do NOT merge the whole branch: its other changes (dashboard/index.html,
    dashboard/export-template.html, scripts/generate-deck.cjs, generate-hub.cjs,
    generate-lobby.cjs, generate-snapshot.cjs, skills/ui-system/SKILL.md,
    skills/ui-system/rules/design-system.md) rewire the OTHER generators and belong to the
    ui-system phase's own verification, not Phase 232.

    Commit the five landed files with message
    `feat(232-01): land M:OS design system v1.1 bundle + loader from feat/mos-design-system-v1.1 (a9e1ee88)`.
  </action>
  <verify>
    <automated>test -f skills/ui-system/design-system/mos-design-system.css && test -f lib/ui/design-system.cjs && node -e "const {mosStyleTag,readDesignSystemCss}=require('./lib/ui/design-system.cjs'); const t=mosStyleTag(); if(!t.includes('data-mos=\"v1.1\"')) {console.error('FAIL: no v1.1 marker'); process.exit(1)}; if(!readDesignSystemCss().includes('--paper:#F4F2EC')) {console.error('FAIL: canonical --paper token missing'); process.exit(1)}; console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `skills/ui-system/design-system/mos-design-system.css` exists on disk and contains the literal token declarations `--paper:#F4F2EC`, `--red:#E11D22`, `--blue:#1E52E0`, `--yellow:#FFC400`, `--green:#12A06A`
    - `lib/ui/design-system.cjs` exists; `require('./lib/ui/design-system.cjs').mosStyleTag()` returns a string containing `data-mos="v1.1"`
    - `git status --porcelain skills/ui-system/design-system lib/ui/design-system.cjs` shows no uncommitted state after the commit
    - `git diff --stat HEAD~1 HEAD` for the landing commit lists exactly 5 files, none outside skills/ui-system/design-system/ and lib/ui/
  </acceptance_criteria>
  <done>The canonical CSS bundle and its loader exist on disk on this branch, committed, without touching the branch's generator rewires.</done>
</task>

<task type="auto">
  <name>Task 2: Retokenize wiki-layout.cjs to M:OS (DV-1..DV-6)</name>
  <files>lib/wiki/wiki-layout.cjs</files>
  <read_first>
    - lib/wiki/wiki-layout.cjs (the file being modified - wrapInLayout at line 28, theme script at lines 121-132, CSS_STYLES from ~line 560, tour block at lines 323-385, chat panel markup after the layout div, exports at line 1451)
    - .planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-UI-SPEC.md (divergence table DV-1..DV-8, Component Inventory items 1/8/9, Copywriting Contract, Spacing/Typography/Color sections)
    - lib/ui/design-system.cjs (the loader being wired in)
  </read_first>
  <action>
    Rework lib/wiki/wiki-layout.cjs per 232-UI-SPEC.md, keeping the module's export surface
    IDENTICAL (wrapInLayout, renderInfobox, renderTOC, renderSidebar, renderBacklinks,
    renderSeeAlso, renderGraphLinks - wiki-server.cjs requires all of them and Plan 04
    depends on stable signatures). Changes:

    1. Inject `mosStyleTag()` from lib/ui/design-system.cjs into <head> BEFORE the local
       <style> block, via `const { mosStyleTag } = require('../ui/design-system.cjs')`.
    2. DV-1: `<html lang="en" data-theme="dark">` becomes `data-theme="light"`; the theme
       init script default flips from `localStorage.getItem('mos-wiki-theme') || 'dark'` to
       `|| 'light'`. Keep the localStorage key `mos-wiki-theme` and the toggleTheme
       mechanism exactly as-is otherwise (SPEC Req 8).
    3. DV-3: delete the fonts.googleapis.com <link> line entirely. In CSS_STYLES, replace
       every `font-family: 'Bebas Neue', sans-serif` and Inter/JetBrains Mono reference
       with `var(--grot)` (display/headings/body) or `var(--mono)` (code), which the
       canonical bundle defines. Keep the highlight.js and mermaid CDN script/link tags
       untouched - only fonts are in scope for DV-3 this phase.
    4. DV-2: retire the legacy `--ds-*`/bespoke color values in CSS_STYLES. Replace the
       wiki structural CSS's own color definitions with canonical vars. Add an alias block
       inside the wiki structural CSS so untouched route markup keeps working:
       `--text: var(--ink); --text-muted: var(--muted); --surface: var(--panel); --border: var(--rule);`
       (wiki-server.cjs inline styles use exactly these four legacy var names - verified).
       Delete the layout's own `[data-theme="dark"]`/`[data-theme="light"]` palette blocks
       where they redefine colors the canonical bundle now owns; keep only wiki-structural
       rules (grid, sidebar, cards, dropdown geometry).
    5. DV-4: the search dropdown's `box-shadow: 0 8px 24px rgba(0,0,0,0.4)` becomes a hard
       offset `8px 8px 0 var(--ink)` with a 1px `var(--edge)` border. Search-match
       highlight styling uses `var(--yellow)` background with ink text (UI-SPEC component 8).
    6. DV-5: the header brand becomes the wordmark `M:OS` with the colon wrapped in a span
       colored `var(--red)` (keep the existing outbound href). Remove the `header-pdf`
       per-page PDF link from the header (UI-SPEC component 1 slots are: wordmark, search,
       Home/Graph tabs, theme toggle; the article view's own Export PDF action supersedes
       it - the `/wiki/:section/pdf` server route itself is untouched). Active nav tab
       style becomes a `var(--blue)` underline.
    7. Retune the semantic edge classes `.link-informs/.link-contradicts/.link-converges/.link-enables/.link-invalidates`
       to canonical hex: informs #1E52E0, contradicts #E11D22 (bold), converges #FFC400,
       enables #12A06A, invalidates keeps sienna #B5602A with line-through (UI-SPEC
       "Semantic edge-color mapping").
    8. Remove the chat panel: the `<aside id="chat-panel">` markup, its `.chat-*` CSS, and
       the toggleChat script + header chat button. SPEC defers the Larry chat panel to v2
       (wiki-chat.cjs and the /api/chat routes stay untouched server-side).
    9. Remove the onboarding tour block (TOUR_KEY script, lines ~323-385) - SPEC defers the
       tour to v2 and its selectors go stale under the new chrome.
    10. Keep: the SSE auto-refresh script, the search dropdown JS (restyled only), the
        sidebar renderer, TOC, infobox, meta/OG head block, renderBacklinks/renderSeeAlso
        markup (Plan 04 relocates their placement, not their markup).

    Do not add a confirm dialog anywhere (UI-SPEC destructive-confirmation row: absence is
    intentional, per SPEC Req 2 locked decision 4).
  </action>
  <verify>
    <automated>node -e "const L=require('./lib/wiki/wiki-layout.cjs'); const h=L.wrapInLayout({title:'t',bodyHtml:'<p>x</p>',toc:[],sidebar:'',infobox:'',sectionColor:null,currentPath:'/wiki',isHomePage:true}); const must=[['data-mos=\"v1.1\"',true],['data-theme=\"light\"',true],['fonts.googleapis.com',false],['Bebas Neue',false],['chat-panel',false],['mos-wiki-theme',true],['M:OS',true]]; for (const [s,want] of must){ if(h.includes(s)!==want){console.error('FAIL '+s+' expected '+want); process.exit(1);} } console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `wrapInLayout(...)` output contains `data-mos="v1.1"` (canonical bundle inlined) and `data-theme="light"` on the html element
    - Output does NOT contain `fonts.googleapis.com`, `Bebas Neue`, `chat-panel`, or `header-pdf`
    - Output contains the localStorage key string `mos-wiki-theme` and the default-branch string `|| 'light'` appears in lib/wiki/wiki-layout.cjs source
    - `grep -v '^\s*//' lib/wiki/wiki-layout.cjs | grep -c "TOUR_KEY"` returns 0
    - The alias declarations `--text-muted: var(--muted)` and `--surface: var(--panel)` exist in lib/wiki/wiki-layout.cjs source
    - `node -e "const L=require('./lib/wiki/wiki-layout.cjs'); for (const f of ['wrapInLayout','renderInfobox','renderTOC','renderSidebar','renderBacklinks','renderSeeAlso','renderGraphLinks']) if(typeof L[f]!=='function'){console.error('FAIL missing '+f);process.exit(1)}; console.log('PASS')"` prints PASS
  </acceptance_criteria>
  <done>All served wiki pages render on M:OS canonical tokens, default light, wordmark M:OS, no external fonts, no chat panel, no tour; export surface of the module unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room markdown -> rendered HTML | Existing boundary; this plan changes chrome/CSS only, not the markdown render path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-232-01-01 | Tampering | git checkout from origin branch | mitigate | Land only the 5 named files from the pinned branch head (a9e1ee88 content); acceptance gate asserts the commit touches exactly those 5 paths |
| T-232-01-02 | Information Disclosure | inlined CSS bundle | accept | CSS contains no secrets/user data; bundle is already public on the remote branch |
| T-232-SC | Tampering | package installs | accept | No package-manager installs in this plan (git file landing + edits only) |
</threat_model>

<verification>
- `bash -n` not applicable; `node -e "require('./lib/wiki/wiki-layout.cjs')"` loads without throwing
- Task 2 smoke assertion passes (canonical marker present, legacy markers absent)
- Both tasks committed; working tree clean for the six files_modified
</verification>

<success_criteria>
- Canonical design-system CSS + loader on disk and committed (the phase-wide [BLOCKING] prerequisite is cleared)
- wiki-layout.cjs serves M:OS chrome: default light theme persisted under `mos-wiki-theme`, M:OS wordmark, Home/Graph tabs, restyled search dropdown, no Google Fonts, no chat panel, no tour
</success_criteria>

<output>
Create `.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 01 slice)

- `skills/ui-system/design-system/mos-design-system.css` (landed file - M:OS Canonical Design System v1.1 bundle)
- `skills/ui-system/design-system/M-OS-DESIGN-SYSTEM.md`, `skills/ui-system/design-system/SPEC.md`, `skills/ui-system/design-system/image-prompt-style.md` (landed docs)
- `lib/ui/design-system.cjs` (landed module) - functions `readDesignSystemCss()`, `mosStyleTag()`
- Reworked `lib/wiki/wiki-layout.cjs` - same exported function names (`wrapInLayout`, `renderInfobox`, `renderTOC`, `renderSidebar`, `renderBacklinks`, `renderSeeAlso`, `renderGraphLinks`), new internal CSS alias vars `--text`, `--text-muted`, `--surface`, `--border` mapped to canonical tokens; M:OS wordmark markup; light-default theme init
