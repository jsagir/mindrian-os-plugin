---
phase: 232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/wiki/editor-src/package.json
  - lib/wiki/editor-src/package-lock.json
  - lib/wiki/editor-src/build.cjs
  - lib/wiki/editor-src/README.md
  - lib/wiki/editor-src/src/index.jsx
  - lib/wiki/editor-src/src/mos-blocknote-theme.css
  - lib/wiki/editor-dist/wiki-editor.js
  - lib/wiki/editor-dist/wiki-editor.css
autonomous: false
requirements: ["SPEC Req 1", "SPEC Req 2"]
user_setup: []
must_haves:
  truths:
    - "A prebuilt BlockNote+React bundle exists vendored at lib/wiki/editor-dist/, built by an author-time esbuild script walled off in lib/wiki/editor-src/"
    - "The plugin's root package.json production dependencies contain no next, react, or @blocknote/* entries"
    - "The bundle self-mounts a BlockNote editor into #mos-editor-root, loads the article's markdown from GET /api/raw/:pageId, and Save POSTs markdown to /api/save/:pageId"
    - "The editor surface is themed to M:OS tokens: zero radius, no soft shadows, grotesk font, theme follows the page's data-theme attribute live"
  artifacts:
    - path: "lib/wiki/editor-src/package.json"
      provides: "Walled-off build-time dependency tree (D-02): react, @blocknote/* 0.51.4 pins, esbuild, pdfmake, docx"
      contains: "\"private\": true"
    - path: "lib/wiki/editor-src/build.cjs"
      provides: "Author-time esbuild bundling script (D-01)"
      contains: "esbuild"
    - path: "lib/wiki/editor-dist/wiki-editor.js"
      provides: "Compiled IIFE bundle served by express.static (D-03)"
      min_lines: 100
  key_links:
    - from: "lib/wiki/editor-src/build.cjs"
      to: "lib/wiki/editor-dist/wiki-editor.js"
      via: "esbuild outfile"
      pattern: "editor-dist"
    - from: "lib/wiki/editor-src/src/index.jsx"
      to: "/api/raw and /api/save endpoints"
      via: "fetch using data-page-id from #mos-editor-root"
      pattern: "api/(raw|save)"
---

<objective>
Stand up the walled-off BlockNote client bundle: legitimacy-gate the npm packages, scaffold
lib/wiki/editor-src/ with its own package.json (D-02 - React/BlockNote/esbuild never enter
the plugin's root dependency tree), build with esbuild as a manual author-time script
(D-01; Vite library mode is the only fallback and only if HMR proves needed, D-04), vendor
the compiled output to lib/wiki/editor-dist/, and implement the editor mount with direct
save-to-markdown wiring (SPEC Req 1 + client half of Req 2) themed to M:OS tokens
(UI-SPEC Section 8, re-authoring the lost blocknote-theme.ts as a token-override contract).

Purpose: this is the one place the phase deliberately adds a build step, and it must stay
sealed off from the CJS no-build-step server.
Output: committed editor-src sources + lockfile and vendored editor-dist bundle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-SPEC.md
@.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-CONTEXT.md
@.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-UI-SPEC.md
@.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-PATTERNS.md
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for the editor-src dependency tree</name>
  <files>none - approval gate only, precedes the install in Task 2</files>
  <what-built>Nothing yet - this gate precedes the only npm install of the phase. No RESEARCH.md Package Legitimacy Audit exists for Phase 232 (research was explicitly skipped this run), so per the GSD fallback policy every package below is treated as [ASSUMED] and requires human verification before install.</what-built>
  <read_first>
    - .planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-CONTEXT.md (D-01/D-02: which packages the walled bundle needs and why)
    - package.json (root - confirm none of these packages already exist in the plugin tree)
  </read_first>
  <action>
    Present the navigator with the exact package list below and the registry URLs, then
    STOP and wait for approval. Do not run npm install, do not scaffold editor-src, and do
    not proceed to Task 2 until the human types approval. This checkpoint is never
    auto-approvable regardless of workflow.auto_advance or yolo mode (package legitimacy
    gates are blocking-human by GSD contract, threat T-232-SC).
  </action>
  <how-to-verify>
    Verify each package is the legitimate, canonical package at its registry page (name
    exactly as listed, publisher plausible, weekly downloads in the millions for the
    mainstream ones, BlockNote packages published by the blocknote team):
    1. https://www.npmjs.com/package/react (^19)
    2. https://www.npmjs.com/package/react-dom (^19)
    3. https://www.npmjs.com/package/@blocknote/core (pin exactly 0.51.4 - the version D-07's byte-identical round-trip verification was performed against)
    4. https://www.npmjs.com/package/@blocknote/react (0.51.4)
    5. https://www.npmjs.com/package/@blocknote/mantine (0.51.4)
    6. https://www.npmjs.com/package/esbuild (dev-only, ^0.25)
    7. https://www.npmjs.com/package/pdfmake (^0.2 - MIT, used by Plan 05 for Export PDF)
    8. https://www.npmjs.com/package/docx (^9 - MIT, used by Plan 05 for Export Word)
    License note for context: the official @blocknote/xl-pdf-exporter and
    @blocknote/xl-docx-exporter are AGPL-3.0 and are deliberately NOT on this list - the
    plugin ships under BSL-1.1 and cannot compile AGPL code into its vendored bundle;
    pdfmake + docx (both MIT) replace them in Plan 05.
  </how-to-verify>
  <verify>Human approval message received in-session before any install command runs.</verify>
  <acceptance_criteria>
    - The full 8-package list with registry URLs was presented verbatim to the navigator
    - No npm install / package.json scaffold command executed before the approval message
    - The approval (or the rejected package name) is quoted in the SUMMARY
  </acceptance_criteria>
  <done>Human has explicitly approved the 8-package dependency tree (or named a rejection, halting the plan).</done>
  <resume-signal>Type "approved" to proceed with the install, or name any package that looks wrong</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold editor-src with its own package.json + esbuild build pipeline (D-01, D-02)</name>
  <files>lib/wiki/editor-src/package.json, lib/wiki/editor-src/package-lock.json, lib/wiki/editor-src/build.cjs, lib/wiki/editor-src/README.md</files>
  <read_first>
    - package.json (root - the dependency tree that MUST NOT change; SPEC Req 1 acceptance)
    - .planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-CONTEXT.md (D-01/D-02/D-04 - esbuild manual author-time build, walled directory, Vite-only-fallback)
    - .gitignore (confirm the global node_modules/ rule covers lib/wiki/editor-src/node_modules and that no rule ignores editor-dist)
  </read_first>
  <action>
    Create lib/wiki/editor-src/package.json: "name" "mos-wiki-editor", "private" true,
    "version" "0.1.0", dependencies exactly react ^19.0.0, react-dom ^19.0.0,
    @blocknote/core 0.51.4, @blocknote/react 0.51.4, @blocknote/mantine 0.51.4,
    pdfmake ^0.2.20, docx ^9.0.0; devDependencies esbuild ^0.25.0; scripts
    { "build": "node build.cjs" }. Pin the three @blocknote/* entries EXACT (no caret) -
    D-07's round-trip guarantee was verified against 0.51.4 source and a silent minor bump
    voids that verification.

    Run `npm install` inside lib/wiki/editor-src/ and COMMIT the generated
    package-lock.json (supply-chain pin). node_modules stays untracked via the root
    .gitignore node_modules/ rule.

    Create lib/wiki/editor-src/build.cjs (CJS, per repo convention): requires('esbuild'),
    calls build with entryPoints ['src/index.jsx'], bundle true, format 'iife', minify
    true, jsx 'automatic', loader mapping .jsx to jsx, define
    process.env.NODE_ENV '"production"', outfile '../editor-dist/wiki-editor.js',
    logLevel 'info'. CSS imported from JS (BlockNote/Mantine styles + the local theme
    file) is emitted automatically by esbuild next to the outfile as wiki-editor.css.
    Exit nonzero on build failure.

    Create lib/wiki/editor-src/README.md (Feynman/JTBD style, no em-dashes) documenting:
    this directory is the ONLY build-step island in the plugin (D-02); the bundle is
    rebuilt manually at author time via `npm run build` and the compiled output in
    lib/wiki/editor-dist/ is committed like any other static asset (D-01, same shape as
    the Cytoscape CDN pattern); React/BlockNote/esbuild must never be added to the root
    package.json (SPEC Req 1); Vite library mode is the documented fallback ONLY if
    HMR-driven widget iteration becomes necessary (D-04).
  </action>
  <verify>
    <automated>node -e "const p=require('./lib/wiki/editor-src/package.json'); if(p.private!==true) process.exit(1); if(p.dependencies['@blocknote/core']!=='0.51.4') process.exit(1); const root=require('./package.json'); for(const k of Object.keys(root.dependencies||{})) if(/^(next|react|react-dom)$/.test(k)||k.startsWith('@blocknote/')) {console.error('FAIL root dep '+k); process.exit(1)}; console.log('PASS')" && test -f lib/wiki/editor-src/package-lock.json</automated>
  </verify>
  <acceptance_criteria>
    - lib/wiki/editor-src/package.json has "private": true and exact-pinned "@blocknote/core": "0.51.4", "@blocknote/react": "0.51.4", "@blocknote/mantine": "0.51.4"
    - lib/wiki/editor-src/package-lock.json exists and is committed (git ls-files lists it)
    - Root package.json dependencies contain no `next`, `react`, `react-dom`, or `@blocknote/*` keys (SPEC Req 1 acceptance) and root package.json is byte-unchanged: `git diff --exit-code package.json` passes
    - `git check-ignore lib/wiki/editor-src/node_modules/.package-lock.json` exits 0 (node_modules ignored) and `git check-ignore lib/wiki/editor-dist/wiki-editor.js` exits 1 (dist committable)
  </acceptance_criteria>
  <done>The walled build island exists with a pinned, lockfile-committed dependency tree and a working esbuild script; the plugin's own dependency tree is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Editor mount + direct save wiring + M:OS BlockNote theme; build and vendor the bundle</name>
  <files>lib/wiki/editor-src/src/index.jsx, lib/wiki/editor-src/src/mos-blocknote-theme.css, lib/wiki/editor-dist/wiki-editor.js, lib/wiki/editor-dist/wiki-editor.css</files>
  <read_first>
    - .planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-UI-SPEC.md (Section 8 "BlockNote Theme Mapping" - the full token-override contract; Copywriting Contract - Save CTA, save-failed red-bar copy, saved success toast in --green)
    - .planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-PATTERNS.md (save handler contract - POST /api/save/:section/:page, no mtime check; the /editor static mount shape Plan 04 provides)
    - lib/wiki/editor-src/node_modules/@blocknote/react/README.md and lib/wiki/editor-src/node_modules/@blocknote/core/package.json (post-install: confirm the 0.51.4 API names used below against the installed package before writing code)
  </read_first>
  <action>
    Create src/index.jsx: an IIFE-safe entry that on DOMContentLoaded looks for
    `#mos-editor-root`; if absent, do nothing (the bundle is a no-op on non-article pages
    and in static exports). If present: read `data-page-id`, fetch
    `GET /api/raw/` + pageId, then createRoot-render an App component that:
    - creates the editor via useCreateBlockNote (from @blocknote/react) and loads initial
      content with editor.tryParseMarkdownToBlocks(markdown) (D-07: [[wikilink]] text
      survives this parse as plain text; Plan 05 adds the inline-node transforms at this
      exact boundary - leave a named seam: a module-level `loadTransform(blocks)` and
      `saveTransform(blocks)` pair that are identity functions in this plan)
    - renders a header action row (button `Save`, id "mos-save") above a BlockNoteView
      (from @blocknote/mantine) - Plan 05 appends the two export buttons to this row
    - Save: `const md = await editor.blocksToMarkdownLossy(saveTransform(editor.document))`
      then POST /api/save/ + pageId with JSON body { markdown: md }; on ok render a
      `Saved` toast in var(--green) with ink text; on failure render the UI-SPEC red-bar
      three-line copy "Save failed." / "Why: could not write {file}." / "Fix: check the
      room folder is writable, then Save again." NO confirmation dialog, NO mtime/hash
      handling anywhere (SPEC Req 2 locked decision 4 - the silent-overwrite risk is
      accepted, do not add guards)
    - theme: BlockNoteView's theme prop derives from
      document.documentElement.getAttribute('data-theme') and stays live via a
      MutationObserver on the html element's data-theme attribute (the Plan 01 toggle
      flips that attribute)
    - sets `window.__MOS_WIKI_EDITOR__ = { version: '1' }` as a mount marker.

    Create src/mos-blocknote-theme.css implementing UI-SPEC Section 8 as CSS-variable and
    class overrides scoped to .bn-container and the BlockNote menu/toolbar portals:
    editor background var(--panel) on the var(--paper) page, text var(--ink), placeholder
    and secondary var(--muted); font-family var(--grot), code var(--mono); border-radius 0
    (max 2px) on every BlockNote surface incl. slash menu, side menu, drag handle,
    formatting toolbar; kill all blur box-shadows in favor of 1px var(--edge) hairlines or
    hard offsets; ::selection background var(--blue) color #fff; :focus-visible 2px
    var(--blue) outline; honor prefers-reduced-motion by disabling BlockNote transition
    animations. Import order in index.jsx: @blocknote/mantine style.css first, then this
    override file (so the overrides win).

    Run `npm run build` in lib/wiki/editor-src/ and COMMIT the emitted
    lib/wiki/editor-dist/wiki-editor.js and wiki-editor.css (vendored static assets,
    SPEC Req 1). If any 0.51.4 API name differs from the identifiers above, resolve
    against the installed package source (read_first item 3) - do not upgrade the pin.
  </action>
  <verify>
    <automated>cd lib/wiki/editor-src && npm run build && cd ../../.. && node -e "const fs=require('fs'); const js=fs.readFileSync('lib/wiki/editor-dist/wiki-editor.js','utf8'); if(js.length<100000){console.error('FAIL: bundle suspiciously small');process.exit(1)}; for(const m of ['mos-editor-root','api/raw','api/save','__MOS_WIKI_EDITOR__']) if(!js.includes(m)){console.error('FAIL missing '+m);process.exit(1)}; if(!fs.existsSync('lib/wiki/editor-dist/wiki-editor.css')){console.error('FAIL no css');process.exit(1)}; console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` in lib/wiki/editor-src exits 0 and rewrites lib/wiki/editor-dist/wiki-editor.js + wiki-editor.css
    - The built wiki-editor.js contains the literal strings `mos-editor-root`, `api/raw`, `api/save`, `__MOS_WIKI_EDITOR__` and is at least 100 KB
    - wiki-editor.css contains a `.bn-container` selector and a `border-radius: 0` (or `border-radius:0`) declaration
    - src/index.jsx contains identity `loadTransform`/`saveTransform` seams and NO mtime/hash/conflict logic (`grep -ci "mtime\|conflict" lib/wiki/editor-src/src/index.jsx` returns 0)
    - Both editor-dist files are committed (git ls-files lists them)
  </acceptance_criteria>
  <done>Opening an article page that carries #mos-editor-root and the /editor script mounts a themed, editable BlockNote surface whose Save writes through POST /api/save (server route lands in Plan 04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> editor-src node_modules -> vendored bundle | Third-party code compiled into a committed asset served to the user's browser |
| browser bundle -> localhost API | The bundle POSTs user-authored markdown to the save endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-232-SC | Tampering | npm installs (react, react-dom, @blocknote/* 0.51.4, esbuild, pdfmake, docx) | mitigate | Task 1 blocking human legitimacy checkpoint (all packages [ASSUMED] - no RESEARCH.md audit exists); exact @blocknote pins; package-lock.json committed |
| T-232-03-01 | Tampering | vendored editor-dist drift vs source | mitigate | Bundle rebuilt only via build.cjs from committed sources; acceptance gates assert marker strings; any rebuild is a reviewable diff |
| T-232-03-02 | Elevation of Privilege | AGPL license contamination of the BSL-1.1 plugin | mitigate | @blocknote/xl-* exporters excluded by decision (recorded in Task 1); pdfmake + docx (MIT) substituted in Plan 05 |
| T-232-03-03 | Information Disclosure | markdown content in transit | accept | localhost-only HTTP to the user's own wiki server; same posture as every existing wiki route |
</threat_model>

<verification>
- Task 2 root-purity node assertion passes (SPEC Req 1 acceptance: no next/react/@blocknote in root production dependencies)
- Task 3 bundle build + marker assertion passes
- `git status --porcelain lib/wiki/editor-src lib/wiki/editor-dist` clean after commits; node_modules untracked
</verification>

<success_criteria>
- Prebuilt, vendored, M:OS-themed BlockNote bundle exists and self-mounts on #mos-editor-root (SPEC Req 1)
- Save round-trip client wiring complete against the Plan 04 API contract, with the UI-SPEC copy contracts and zero conflict-detection machinery (SPEC Req 2 client half)
- Root dependency tree untouched
</success_criteria>

<output>
Create `.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 03 slice)

- `lib/wiki/editor-src/package.json` (private walled dependency tree), `lib/wiki/editor-src/package-lock.json`, `lib/wiki/editor-src/build.cjs` (esbuild author-time build), `lib/wiki/editor-src/README.md`
- `lib/wiki/editor-src/src/index.jsx` - mount entry; exported seams `loadTransform(blocks)` / `saveTransform(blocks)` (identity here, replaced in Plan 05); DOM ids `mos-editor-root` (consumed), `mos-save`; global marker `window.__MOS_WIKI_EDITOR__`
- `lib/wiki/editor-src/src/mos-blocknote-theme.css` - BlockNote-to-M:OS token override sheet
- `lib/wiki/editor-dist/wiki-editor.js`, `lib/wiki/editor-dist/wiki-editor.css` - vendored compiled bundle (served at /editor by Plan 04)
