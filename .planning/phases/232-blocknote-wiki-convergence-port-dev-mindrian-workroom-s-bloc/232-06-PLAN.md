---
phase: 232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc
plan: 06
type: execute
wave: 3
depends_on: ["232-04", "232-05"]
files_modified:
  - lib/wiki/wiki-export.cjs
  - lib/wiki/wiki-layout.cjs
  - lib/wiki/room-home.cjs
  - scripts/serve-wiki
  - commands/wiki.md
  - tests/test-232-export.cjs
autonomous: false
requirements: ["SPEC Req 9"]
must_haves:
  truths:
    - "/mos:wiki --export produces export/wiki/ containing static HTML that renders without a live server"
    - "The exported bundle exposes no save/edit affordance: no BlockNote mount, no /editor script, no Save button, no Generate Briefing call"
    - "Exported pages carry the M:OS canonical CSS inline and wikilinks resolve as relative links between exported pages"
    - "The full phase acceptance list (all 10 SPEC checkboxes) passes a human walkthrough"
  artifacts:
    - path: "lib/wiki/wiki-export.cjs"
      provides: "Static HTML bundle generator over the read-only page-renderer path"
      exports: ["exportStaticWiki"]
      min_lines: 80
    - path: "scripts/serve-wiki"
      provides: "--export flag handling (previously documented but unimplemented)"
      contains: "--export"
  key_links:
    - from: "scripts/serve-wiki"
      to: "lib/wiki/wiki-export.cjs"
      via: "node invocation on --export before port logic"
      pattern: "wiki-export"
    - from: "lib/wiki/wiki-export.cjs"
      to: "lib/wiki/page-renderer.cjs"
      via: "scanRoom/renderPage read-only render (NOT the editor bundle)"
      pattern: "renderPage"
    - from: "lib/wiki/wiki-export.cjs"
      to: "lib/wiki/room-home.cjs"
      via: "renderRoomHomeBody with staticExport:true"
      pattern: "staticExport"
---

<objective>
Implement the static share-export and close the phase with a human walkthrough. Honest
finding from planning (2026-07-20): `--export` is DOCUMENTED in commands/wiki.md but has
never been implemented - scripts/serve-wiki treats its first argument as a port number,
so `--export` currently errors out. SPEC Req 9's "keeps working" is therefore BUILD work:
a real static generator over the read-only page-renderer path (which Plans 01-05
deliberately left untouched for exactly this purpose), producing a Room Home + articles +
wikilinks bundle in export/wiki/ with zero edit affordances, then the end-to-end
human-verify gate across all 10 SPEC acceptance criteria.

Purpose: the export is the share surface for people who should not be editing the source
room (SPEC Req 9); the walkthrough is the phase's goal-backward proof.
Output: lib/wiki/wiki-export.cjs, working --export flag, updated command doc, export
test, recorded human verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-SPEC.md
@.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-UI-SPEC.md
@lib/wiki/page-renderer.cjs
@scripts/serve-wiki
@commands/wiki.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: wiki-export.cjs static generator + serve-wiki flag + command doc truth-up</name>
  <files>lib/wiki/wiki-export.cjs, lib/wiki/wiki-layout.cjs, lib/wiki/room-home.cjs, scripts/serve-wiki, commands/wiki.md, tests/test-232-export.cjs</files>
  <read_first>
    - scripts/serve-wiki (the file being modified - $1 is currently PREFERRED_PORT; the --export branch must run BEFORE find_port)
    - lib/wiki/wiki-export.cjs does not exist yet; read lib/wiki/wiki-server.cjs (route bodies being mirrored statically: /wiki home, /wiki/:section, /wiki/:section/:page pre-Plan-04 read-only shape, /wiki/graph graphData baking) and lib/wiki/page-renderer.cjs (scanRoom/buildPageIndex/renderPage - the read-only render this export uses INSTEAD of the editor bundle)
    - lib/wiki/wiki-layout.cjs (wrapInLayout - the exportMode option is added here; search JS + SSE script blocks that must be omitted in exportMode)
    - lib/wiki/room-home.cjs (renderRoomHomeBody's staticExport option from Plan 02)
    - lib/wiki/graph-links.cjs (getBacklinks/getSeeAlso/getGraphData - baked per-page at export time, graceful [] when no .room-graph)
    - commands/wiki.md (stale claims to fix: "Read-only: edit files in your IDE", "Interactive knowledge graph view (home page)")
  </read_first>
  <action>
    1. Add an `exportMode` boolean to wrapInLayout's options in lib/wiki/wiki-layout.cjs
       (default false, nothing changes for the served wiki). When true: omit the search
       wrapper + search dropdown JS and the SSE auto-refresh script (both need a live
       server), keep the theme toggle (localStorage works offline), and rewrite the three
       chrome hrefs to relative targets supplied by a new option `hrefPrefix` (a string
       like '' or '../'): Home to `${hrefPrefix}index.html`, Graph to
       `${hrefPrefix}graph.html`, wordmark stays external.
    2. Create lib/wiki/wiki-export.cjs (CJS) exporting `exportStaticWiki(roomDir, outDir)`
       (outDir default 'export/wiki'). Flow: scanRoom + buildPageIndex; mkdir -p outDir;
       write:
       - index.html: renderRoomHomeBody(getRoomHomeData(roomDir), { staticExport:true })
         wrapped with exportMode + hrefPrefix ''
       - <section>/index.html per section (the section-card list shape from the
         /wiki/:section route), hrefPrefix '../'
       - <section>/<page>.html per article: renderPage read-only HTML (page-renderer +
         @ig3/markdown-it-wikilinks - the editor bundle is NEVER referenced), plus
         backlinks/see-also panels baked from getBacklinks/getSeeAlso at export time,
         hrefPrefix '../'
       - graph.html: the Cytoscape page with graphData JSON baked inline and the same
         unpkg cytoscape CDN script tag the served route uses (SPEC-locked Cytoscape
         reuse; DV-8 notes the inline-SVG port is a follow-up phase)
       After wrapping each page, post-process hrefs for static use: every
       href="/wiki/<section>/<page>" becomes the page-relative .html path (from root
       depth: `<section>/<page>.html`; from inside a section dir: `../<section>/<page>.html`
       or `<page>.html` for same-section), href="/wiki/<section>" likewise to the section
       index.html, href="/wiki" to index.html, href="/wiki/graph" to graph.html. Also
       rewrite src="/room-assets/..." to a copied `assets/` subtree ONLY if the room has
       image references; otherwise skip asset copying this pass (text-first bundle).
       The generator must be idempotent (re-running overwrites cleanly) and must print
       the output dir + page count.
    3. scripts/serve-wiki: before the find_port section, add a branch: if $1 equals
       --export, exec `node "${SCRIPT_DIR}/../lib/wiki/wiki-export.cjs" "$ROOM_DIR"` and
       exit with its status (no server, no browser open).
    4. commands/wiki.md truth-up (keep frontmatter/connector block byte-identical): the
       "What it does" list gains the BlockNote editing surface, Room Home landing page,
       and per-article PDF/DOCX export; replace the "Read-only: edit files in your IDE"
       note with a direct-save note (no confirmation, no conflict check - concurrent
       external edits can be overwritten, per the locked SPEC decision); replace
       "Interactive knowledge graph view (home page)" with "Room Home dashboard (landing
       page) + knowledge graph tab"; delete the onboarding-tour "First-Time Experience"
       section (tour removed in Plan 01, deferred to v2); keep the privacy note and
       deploy instructions.
    5. Create tests/test-232-export.cjs (glob-discovered by run-all-232.sh): runs
       exportStaticWiki against a scratch copy of tests/fixtures/wiki-room-232 into a
       tmpdir, then asserts: index.html exists and contains `id="room-home"`,
       `data-mos="v1.1"`, and `No briefing yet.` but NOT `Generate Briefing`,
       `mos-editor-root`, or `wiki-editor.js`; problem-definition/test-article.html
       exists, contains an `<a` wikilink href ending `other-note.html` and does NOT
       contain `mos-editor-root`, `wiki-editor.js`, or `id="mos-save"`; graph.html exists
       and contains `cytoscape`; NO file in the output tree contains the string
       `/api/save` (walk all .html files).
  </action>
  <verify>
    <automated>node tests/test-232-export.cjs && bash tests/run-all-232.sh && bash -n scripts/serve-wiki</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-232-export.cjs` exits 0 with every assertion above green
    - `bash scripts/serve-wiki --export` run with ROOM_DIR=tests/fixtures/wiki-room-232 exits 0 and creates export/wiki/index.html (clean up after)
    - `grep -c "editor-dist\|wiki-editor" lib/wiki/wiki-export.cjs` returns 0 (the export path never touches the bundle)
    - commands/wiki.md no longer contains "Read-only: edit files in your IDE" and its connector frontmatter block is byte-identical (git diff shows no changes between the `connector:` key and the closing `---`)
    - `bash tests/run-all-232.sh` exits 0 across all five phase test files
  </acceptance_criteria>
  <done>A previously-vaporware flag is real: --export emits a self-viewing, read-only, M:OS-styled static bundle with relative wikilinks and zero edit affordances.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end phase walkthrough (all 10 SPEC acceptance criteria)</name>
  <files>none - verification gate only, no file changes</files>
  <what-built>The complete Phase 232 wiki: Room Home landing, graph tab, BlockNote editing with direct save, wikilink pills, backlinks/see-also, search, theme toggle, PDF/DOCX export, static --export bundle.</what-built>
  <read_first>
    - .planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-SPEC.md (the Acceptance Criteria checklist this walkthrough proves, item by item)
  </read_first>
  <action>
    Start the wiki for the navigator (`bash scripts/serve-wiki` from a room directory),
    present the 10-step walkthrough below verbatim, then STOP and wait. Record the
    per-criterion pass/fail the navigator reports in 232-06-SUMMARY.md and in the phase
    VERIFICATION notes. Do not mark the phase complete without the approval message.
  </action>
  <how-to-verify>
    From a real room (any active Data Room, or tests/fixtures/wiki-room-232 for a minimal
    pass), run `bash scripts/serve-wiki` and walk:
    1. The browser opens http://localhost:8421/wiki showing Room Home (governing thought
       or its "(no MINTO yet)" fallback, stage chip, gaps, section progress bars) - NOT a
       graph canvas [Req 4]
    2. Click the Graph tab: the interactive Cytoscape graph renders in-wiki [Req 5]
    3. Open any article: an editable BlockNote surface loads; type a change, click Save,
       then `cat` the source .md - the change is on disk, frontmatter intact, no dialog
       appeared [Req 2, Req 1]
    4. In an article containing [[some-link]], the link renders as a blue pill; clicking
       navigates to the target article [Req 6]
    5. The target article's info rail lists the referrer under Backlinks [Req 6]
    6. Type a term unique to one article in the top-nav search: exactly that article
       returns [Req 7]
    7. Click Export PDF then Export Word: a .pdf and a .docx download and open validly
       [Req 3]
    8. Toggle the theme: flips immediately; reload: choice persists; fresh profile /
       cleared localStorage defaults to LIGHT cream [Req 8]
    9. Confirm root package.json has no next/react/@blocknote entries:
       `node -e "const d=require('./package.json').dependencies; console.log(Object.keys(d).filter(k=>/^(next|react)$/.test(k)||k.startsWith('@blocknote/')))"`
       prints [] [Req 1]
    10. Run `bash scripts/serve-wiki --export`, then open export/wiki/index.html directly
        in a browser (file://): Room Home renders styled; open an article page: readable,
        wikilinks navigate between files, and NO Save button or editor surface exists
        anywhere [Req 9]
    Also confirm Larry's Briefing: with ANTHROPIC_API_KEY set, Generate Briefing renders
    critical misses / reframe / next steps; without it, the red-bar "Briefing
    unavailable" copy shows and the stats still render [Req 4].
  </how-to-verify>
  <verify>Human approval message received; per-criterion pass/fail recorded in 232-06-SUMMARY.md.</verify>
  <acceptance_criteria>
    - All 10 walkthrough steps presented and each mapped SPEC requirement individually confirmed or failed by the navigator
    - The result table (criterion, pass/fail, note) is written into 232-06-SUMMARY.md
    - On any failure: the failing step is quoted and the plan halts for gap handling instead of self-declaring success
  </acceptance_criteria>
  <done>Human has confirmed all 10 SPEC acceptance criteria against the running wiki and the exported bundle.</done>
  <resume-signal>Type "approved" if all 10 pass, or describe which step failed and how</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export/wiki/ bundle -> anyone it is shared with | Room content intentionally leaves the machine as static HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-232-06-01 | Information Disclosure | exported bundle carries room content | accept | Export is the explicit share intent; commands/wiki.md privacy note retained ("Share only what you intend to share") |
| T-232-06-02 | Tampering | edit affordance leaking into the share surface | mitigate | Export path never references the editor bundle (grep gate); test walks every emitted .html asserting no /api/save, no mos-editor-root, no wiki-editor.js |
| T-232-06-03 | Elevation of Privilege | briefing key or API surface in static output | mitigate | staticExport render omits the Generate Briefing script entirely; no /api/* strings in the bundle (test-enforced for /api/save; briefing script omitted at render) |
| T-232-SC | Tampering | package installs | accept | No package-manager installs in this plan |
</threat_model>

<verification>
- `bash tests/run-all-232.sh` fully green (room-home, briefing, wiki-server, transforms, export)
- Human walkthrough recorded in the SUMMARY with per-criterion pass/fail
- `git diff --exit-code lib/wiki/page-renderer.cjs lib/wiki/graph-links.cjs` still passes at phase end (the two reuse-as-is modules never drifted)
</verification>

<success_criteria>
- SPEC Req 9 acceptance met: export/wiki/ static bundle renders serverless, read-only, M:OS-styled
- All 10 SPEC acceptance checkboxes verified by a human and recorded
- commands/wiki.md tells the truth about the new wiki
</success_criteria>

<output>
Create `.planning/phases/232-blocknote-wiki-convergence-port-dev-mindrian-workroom-s-bloc/232-06-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 06 slice)

- `lib/wiki/wiki-export.cjs` - function `exportStaticWiki(roomDir, outDir)`
- `wrapInLayout` gains options `exportMode` (boolean) and `hrefPrefix` (string) in lib/wiki/wiki-layout.cjs
- `renderRoomHomeBody` staticExport branch consumed (defined in Plan 02)
- `scripts/serve-wiki` gains the `--export` flag branch (first real implementation)
- Updated `commands/wiki.md` (editing + Room Home + export claims truthful; tour section removed)
- `tests/test-232-export.cjs`
