---
phase: 143-insight-sensors-the-7-row-trigger-map
plan: 02
type: execute
wave: 2
depends_on: ["143-01"]
files_modified:
  - lib/core/sensors/sensor-lagging-component.cjs
  - lib/core/sensors/sensor-methodology-decision.cjs
  - lib/core/sensors/sensor-gate-approach.cjs
  - lib/core/insight-sensors.cjs
  - tests/test-sens02-lagging-component-fires.cjs
  - tests/test-sens03-methodology-decision-fires.cjs
  - tests/test-sens07-gate-approach-fires.cjs
  - tests/run-all-143.sh
autonomous: true
requirements: [SENS-02, SENS-03, SENS-07]
canon_parts: [Part 2, Part 2 Engine 1, Part 3, Part 4, Part 8]

must_haves:
  truths:
    - "A 'the bottleneck is...' / lagging-component turn surfaces the find-bottlenecks / reverse-salient reach (dispatching the shipped Phase 89 rs-engine), proven by a loop-fires test."
    - "A methodology-decision point surfaces the brain_framework_chain reach (CHAINS_TO next framework), carrying only generic framework + problem-type handles, proven by a loop-fires test."
    - "A gate/milestone approach surfaces the breakthrough-scan (Category G) + investor-objection reach (dispatching the shipped Phase 120 scanner + agents/investor), proven by a loop-fires test."
    - "SENS-03 carries no user content to Brain; only framework names + problem-type enums (Part 8), asserted by the inherited Part-8 sweep."
    - "No detector flips routing_source (Phase 144 fence), asserted by the inherited routing fence over lib/core/sensors/."
  artifacts:
    - path: "lib/core/sensors/sensor-lagging-component.cjs"
      provides: "SENS-02 conversation-pattern detector dispatching the shipped rs-engine"
      min_lines: 30
      exports: ["sensorLaggingComponent"]
    - path: "lib/core/sensors/sensor-methodology-decision.cjs"
      provides: "SENS-03 methodology-decision detector auto-invoking brain_framework_chain (generic handles only)"
      min_lines: 30
      exports: ["sensorMethodologyDecision"]
    - path: "lib/core/sensors/sensor-gate-approach.cjs"
      provides: "SENS-07 gate-approach detector firing breakthrough scan + investor-objection surface"
      min_lines: 30
      exports: ["sensorGateApproach"]
  key_links:
    - from: "lib/core/sensors/sensor-lagging-component.cjs"
      to: "scripts/rs-engine.py"
      via: "SENS-02 surfaces the find-bottlenecks/reverse-salient reach; dispatches the shipped Phase 89 engine"
      pattern: "find-bottlenecks|rs-engine|reverse.salient"
    - from: "lib/core/sensors/sensor-methodology-decision.cjs"
      to: "references/brain/query-patterns.md"
      via: "SENS-03 auto-invokes the brain_framework_chain CHAINS_TO pattern, generic handles only"
      pattern: "brain_framework_chain|CHAINS_TO"
    - from: "lib/core/sensors/sensor-gate-approach.cjs"
      to: "lib/core/breakthrough/scanner.cjs"
      via: "SENS-07 surfaces the breakthrough-scan + investor-objection reach; dispatches the shipped Phase 120 scanner + agents/investor"
      pattern: "breakthrough|scanForBreakthroughs|investor"
---

<objective>
Build the three BUILD detectors that fire over already-shipped engines: SENS-02 (lagging-component conversation pattern -> the Phase 89 reverse-salient engine), SENS-03 (methodology-decision point -> the brain_framework_chain CHAINS_TO pattern, never auto-invoked until now), and SENS-07 (gate/milestone approach -> the Phase 120 breakthrough scanner + the investor-objection agent). Each detector is net-new detection logic; the underlying capability is shipped and reused per Canon Part 7 -- the detector surfaces the candidate reach, it does not re-implement the engine.

Purpose: closes three of the four BUILD/PARTIAL detection paths on the 7-row map. Each detector is one sensor function in its own file under lib/core/sensors/, registered into the Plan-01 dispatch chokepoint -- clean file ownership, parallelizable with Plan 03.
Output: three sensor files, three loop-fires tests, registrations into insight-sensors.cjs, three new suites appended to run-all-143.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-insight-sensors-the-7-row-trigger-map/143-CONTEXT.md
@.planning/phases/143-insight-sensors-the-7-row-trigger-map/143-01-PLAN.md
@docs/UI-UX-CONVERGENCE-2026-05-10/00c-TRIGGER-MAP.md
@docs/MINDRIAN-CANON.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. From Plan 01 + the shipped substrate. -->

From Plan 01 (lib/core/insight-sensors.cjs + lib/core/sensors/sensor-types.cjs):
  - makeReach({reach_id, posture, dispatch, companions, signal, evidence}) -> frozen candidate-reach | null. reach_id must be in REACH_IDS=['context_block','contradiction','cross_room','brain_consult','deep_research']; posture in POSTURE_IDS=['push_forward','hold','pull_back'].
  - SENSOR_REGISTRY in insight-sensors.cjs: each new detector is registered here; sensor signature is sensorFn(turn, tuple, ctx) -> candidate-reach|null where tuple = the /mos:diagnose {problem_type, complexity, stage}.
  - The Plan-01 routing fence (test-sensors-routing-fence.cjs) and Part-8 sweep (test-sensors-part8-sweep.cjs) already scan lib/core/sensors/*.cjs -- these three files are automatically covered.

SENS-02 shipped substrate (Phase 89 reverse-salient engine):
  - scripts/rs-engine.py (4-mode CLI: internal/cross-room/external/hybrid) + commands/find-bottlenecks.md. The sensor detects the lagging-component conversation pattern ("the bottleneck is...", a stated lagging component, the Appendix E trigger) and surfaces the find-bottlenecks / reverse-salient reach. Do NOT re-run rs-engine inline; surface the dispatch handle.

SENS-03 shipped substrate (references/brain/query-patterns.md Section 1):
  - brain_framework_chain: parameterized by $problem_type + $current_frameworks (CHAINS_TO traversal). EXISTS but is never auto-invoked. The sensor detects a methodology-decision moment (a methodology just completed / a decision point reached / Decision Gate verb 1 context) and surfaces the brain_consult reach whose companion is brain_framework_chain carrying ONLY generic handles (framework NAMES + problem_type ENUM) -- Part 8: never artifact bodies/identifiers.

SENS-07 shipped substrate (Phase 120 + agents/investor):
  - lib/core/breakthrough/scanner.cjs::scanForBreakthroughs(roomDir, opts) + surfaceBreakthrough; scripts/check-pending-breakthrough.cjs. agents/investor.md (the investor-objection agent). The sensor detects the gate/milestone-approach signal (Well-Defined Problem / Ready to Build stage near a commit, from the tuple.stage + STATE.md venture_stage) and surfaces the reach dispatching the breakthrough scan (Category G) + the investor-objection surface.

The candidate-reach the SENS-04/05 plan also writes into the same registry -- Plan 02 and Plan 03 own DISJOINT files under lib/core/sensors/ and both append to insight-sensors.cjs SENSOR_REGISTRY + run-all-143.sh. They are sequenced (Plan 02 wave 2, Plan 03 wave 3) ONLY because both edit insight-sensors.cjs + run-all-143.sh; the detector files themselves never collide.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SENS-02 lagging-component detector over the shipped reverse-salient engine</name>
  <files>lib/core/sensors/sensor-lagging-component.cjs, lib/core/insight-sensors.cjs, tests/test-sens02-lagging-component-fires.cjs</files>
  <read_first>
    - commands/find-bottlenecks.md (the shipped /mos:find-bottlenecks surface the reach dispatches)
    - scripts/rs-engine.py (the Phase 89 4-mode reverse-salient engine -- the dispatch target; the sensor surfaces it, does not inline it)
    - docs/MINDRIAN-CANON.md Appendix E (handoff trigger 1 "Risk surface identified" + the lagging-component / reverse-salient framing the conversation pattern keys off)
    - docs/UI-UX-CONVERGENCE-2026-05-10/00c-TRIGGER-MAP.md Section 3 ("Lagging component" pattern row: fires find-bottlenecks/rs-fetch, reads the artifact corpus + section fill levels)
    - lib/core/insight-sensors.cjs (Plan 01: SENSOR_REGISTRY + makeReach contract)
  </read_first>
  <behavior>
    - Test: a turn whose text matches the lagging-component pattern ("the bottleneck is X", "X is lagging", "X is holding us back") returns a reach with dispatch referencing find-bottlenecks / the reverse-salient engine.
    - Test: a turn with no lagging-component pattern returns null.
    - Test: the reach is surfaced as a candidate (Part 3) -- the returned struct never carries a routing_source field.
    - Test: detection is LOCAL-only (no network call) and never throws on malformed turn input.
  </behavior>
  <action>
    Create lib/core/sensors/sensor-lagging-component.cjs exporting sensorLaggingComponent(turn, tuple, ctx): a conversation-pattern detector (the "the bottleneck is..." / stated-lagging-component shape) that, on match, returns makeReach with a reach_id appropriate to the find-bottlenecks surface and a dispatch handle naming the shipped rs-engine / /mos:find-bottlenecks. The detector reads only the turn text + LOCAL section fill levels from ctx -- never re-runs rs-engine inline. Register sensorLaggingComponent in the insight-sensors.cjs SENSOR_REGISTRY.
  </action>
  <verify>
    <automated>node tests/test-sens02-lagging-component-fires.cjs</automated>
  </verify>
  <done>SENS-02 loop-fires test green: lagging-component turn surfaces the find-bottlenecks/reverse-salient reach; non-matching turn returns null; registered in the dispatch chokepoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SENS-03 methodology-decision detector auto-invoking brain_framework_chain (generic handles only)</name>
  <files>lib/core/sensors/sensor-methodology-decision.cjs, lib/core/insight-sensors.cjs, tests/test-sens03-methodology-decision-fires.cjs</files>
  <read_first>
    - references/brain/query-patterns.md Section 1 (brain_framework_chain: the $problem_type + $current_frameworks parameters; the CHAINS_TO traversal -- the EXISTING pattern this sensor finally auto-invokes)
    - docs/UI-UX-CONVERGENCE-2026-05-10/00c-TRIGGER-MAP.md Section 3 ("Decision point reached" row -> /mos:diagnose re-classify -> route to stage-appropriate command via the Decision Gate)
    - docs/MINDRIAN-CANON.md Part 8 (the generic-handles-only constraint: framework NAMES + problem_type ENUMS only -- the breach line "here is the user's model, what does Brain say" is forbidden)
    - tests/test-decide-part8-invariant.cjs (the 5-tripwire forbidden sweep this sensor must survive -- it is the Brain-touching sensor)
    - lib/core/insight-sensors.cjs (Plan 01 SENSOR_REGISTRY + makeReach)
  </read_first>
  <behavior>
    - Test: a turn carrying a methodology-decision signal (a methodology just completed / a decision point reached) + a tuple with problem_type and current_frameworks returns a reach with reach_id='brain_consult' and a companion naming brain_framework_chain with ONLY the framework-name handles + the problem_type enum.
    - Test: the returned reach carries NO user-content bytes -- a fixture turn with artifact text yields a reach whose serialized form contains none of that text (Part 8 LOCAL->Brain: NO).
    - Test: no methodology-decision signal returns null.
  </behavior>
  <action>
    Create lib/core/sensors/sensor-methodology-decision.cjs exporting sensorMethodologyDecision(turn, tuple, ctx): detect the methodology-decision moment (a RAN/CHOSE edge just landed, a decision point reached, or the Decision Gate verb-1 context) and surface a brain_consult candidate reach whose companion is the brain_framework_chain CHAINS_TO query parameterized by ONLY generic handles -- the framework names from the LOCAL methodology cache and tuple.problem_type as a bare enum. The sensor itself makes NO Brain call (it surfaces the reach for the Decision Gate per Part 3); it constructs the generic-handle payload only. Register in SENSOR_REGISTRY. The Part-8 sweep from Plan 01 covers this file.
  </action>
  <verify>
    <automated>node tests/test-sens03-methodology-decision-fires.cjs && node tests/test-sensors-part8-sweep.cjs</automated>
  </verify>
  <done>SENS-03 loop-fires test green (methodology-decision turn surfaces the brain_framework_chain reach with generic handles only); the Part-8 sweep still passes over the new file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SENS-07 gate-approach detector firing breakthrough scan + investor-objection; register in run-all-143.sh</name>
  <files>lib/core/sensors/sensor-gate-approach.cjs, lib/core/insight-sensors.cjs, tests/test-sens07-gate-approach-fires.cjs, tests/run-all-143.sh</files>
  <read_first>
    - lib/core/breakthrough/scanner.cjs (scanForBreakthroughs + surfaceBreakthrough -- the shipped Phase 120 Category-G scan the reach dispatches; the sensor surfaces it, does not re-run it inline)
    - scripts/check-pending-breakthrough.cjs (emitContinueWithContext -- the additionalContext surfacing pattern the gate-approach reach mirrors)
    - agents/investor.md (the investor-objection agent the reach co-surfaces)
    - docs/UI-UX-CONVERGENCE-2026-05-10/00c-TRIGGER-MAP.md Section 3 ("Milestone / gate approach" row: deep-grade + breakthrough scan Category G + investor-objection surface)
    - tests/run-all-142.sh (the aggregator the Plan-01 run-all-143.sh mirrors -- append the three Plan-02 suites here)
  </read_first>
  <behavior>
    - Test: a turn/tuple indicating a gate or milestone approach (tuple.stage in {Well-Defined Problem, Ready to Build} near a commit, or a STATE.md venture_stage advance toward a commit decision) returns a reach dispatching the breakthrough scan (Category G) + the investor-objection surface.
    - Test: an early-stage tuple (Pre-Opportunity / Opportunity Identified) returns null (no gate approaching).
    - Test: the reach references the shipped scanner + agents/investor; the sensor does not re-implement breakthrough scoring.
  </behavior>
  <action>
    Create lib/core/sensors/sensor-gate-approach.cjs exporting sensorGateApproach(turn, tuple, ctx): detect the gate/milestone-approach signal from tuple.stage + the LOCAL venture_stage on ctx (commit-near stages) and surface a candidate reach dispatching the shipped breakthrough scanForBreakthroughs (Category G) + the agents/investor objection surface. Register in SENSOR_REGISTRY. Append test-sens02-lagging-component-fires.cjs, test-sens03-methodology-decision-fires.cjs, and test-sens07-gate-approach-fires.cjs to the CJS_SUITES array in tests/run-all-143.sh with their requirement-ownership header comments.
  </action>
  <verify>
    <automated>node tests/test-sens07-gate-approach-fires.cjs && bash tests/run-all-143.sh</automated>
  </verify>
  <done>SENS-07 loop-fires test green (gate-approach surfaces the breakthrough + investor reach; early stage returns null); all three Plan-02 suites registered in run-all-143.sh; the full runner exits cleanly with routing fence + Part-8 sweep still passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SENS-03 -> Brain | the brain_framework_chain companion is the Brain-touching surface; generic handles only (Part 8) |
| detectors -> shipped engines | SENS-02/07 surface dispatch handles to rs-engine / breakthrough scanner; they do not execute them (Part 3 Decision Gate surfacing) |
| detectors -> routing trace | no detector flips routing_source (Phase 144 fence) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-04 | Information Disclosure | sensorMethodologyDecision brain_framework_chain payload | mitigate | companion carries ONLY framework names + problem_type enum; the SENS-03 no-leak test + the inherited Part-8 sweep assert no artifact bytes |
| T-143-05 | Tampering | the three detector files | mitigate | the inherited test-sensors-routing-fence scans lib/core/sensors/*.cjs for routing_source='engine' |
| T-143-06 | Elevation of Privilege | sensorGateApproach auto-firing breakthrough/investor | accept | the sensor SURFACES a candidate reach for the Decision Gate; the navigator approves before any engine runs (Part 3) |
| T-143-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs; pure CJS over shipped engines |
</threat_model>

<verification>
- `bash tests/run-all-143.sh` runs to completion; all SENS-02/03/07 suites + the Plan-01 gates PASS.
- `node tests/test-sensors-part8-sweep.cjs` passes over the three new detector files (SENS-03 generic-handles-only).
- `grep -rEn "routing_source\s*[:=]\s*['\"]engine['\"]" lib/core/sensors/` returns zero matches.
- No new dependency added.
</verification>

<success_criteria>
- SENS-02 surfaces the find-bottlenecks/reverse-salient reach -- ROADMAP success criterion 2.
- SENS-03 surfaces the brain_framework_chain CHAINS_TO reach with generic handles -- ROADMAP success criterion 3 (the methodology half).
- SENS-07 surfaces the breakthrough-scan + investor-objection reach -- ROADMAP success criterion 5 (the gate half).
- All three reuse shipped engines (Part 7); none re-implements; none flips routing_source (Phase 144 fence); SENS-03 carries no user content to Brain (Part 8).
</success_criteria>

<output>
Create `.planning/phases/143-insight-sensors-the-7-row-trigger-map/143-02-SUMMARY.md` when done.
</output>
