---
phase: 143-insight-sensors-the-7-row-trigger-map
plan: 03
type: execute
wave: 3
depends_on: ["143-01", "143-02"]
files_modified:
  - lib/core/sensors/sensor-external-fact.cjs
  - lib/core/sensors/hat-scoping-table.cjs
  - lib/core/sensors/sensor-jtbd-reweight.cjs
  - lib/core/insight-sensors.cjs
  - tests/test-sens04-external-fact-fires.cjs
  - tests/test-sens05-jtbd-reweight-fires.cjs
  - tests/run-all-143.sh
autonomous: true
requirements: [SENS-04, SENS-05]
canon_parts: [Part 2, Part 2 Engine 1, Part 3, Part 4, Part 8]

must_haves:
  truths:
    - "An external-fact reference (competitor / market / state-of-the-art) surfaces a hat-scoped WebSearch reach (White=data/arxiv, Green=patents+arxiv+deep-research, Black=failure-cases), proven by a loop-fires test."
    - "The external-fact reach honors the MCP-stack-ask rule: it surfaces a Decision Gate tool choice (Tavily/Firecrawl/Exa) unless the hat is pre-configured -- never a silent WebSearch."
    - "The SENS-04 web query is public SIGNAL: SIGNAL->LOCAL is allowed, SIGNAL->Brain is forbidden (Part 8), asserted by the inherited Part-8 sweep."
    - "A JTBD set/change event re-weights the selector menus + the Brain-query weighting via ADDRESSES_PROBLEM_TYPE, proven by a loop-fires test that reads the shipped Phase 104 JTBD state."
    - "Neither detector flips routing_source (Phase 144 fence), asserted by the inherited routing fence over lib/core/sensors/."
  artifacts:
    - path: "lib/core/sensors/sensor-external-fact.cjs"
      provides: "SENS-04 external-fact detector + the hat-scoped WebSearch dispatch + the MCP-stack-ask gate"
      min_lines: 40
      exports: ["sensorExternalFact"]
    - path: "lib/core/sensors/hat-scoping-table.cjs"
      provides: "The hat -> web-tool scoping table (White/Green/Black/Yellow/Red/Blue) per Canon Part 2"
      min_lines: 20
      contains: "White"
    - path: "lib/core/sensors/sensor-jtbd-reweight.cjs"
      provides: "SENS-05 JTBD set/changed re-weighting consumer over the shipped Phase 104 signal"
      min_lines: 30
      exports: ["sensorJtbdReweight"]
  key_links:
    - from: "lib/core/sensors/sensor-external-fact.cjs"
      to: "lib/core/sensors/hat-scoping-table.cjs"
      via: "the external-fact reach scopes the web tool by the active hat per Canon Part 2"
      pattern: "hat.*scop|White|Green|Black"
    - from: "lib/core/sensors/sensor-jtbd-reweight.cjs"
      to: "lib/hmi/jtbd-state.cjs"
      via: "SENS-05 reads the shipped Phase 104 JTBD signal (getCurrent/setCurrent) and re-weights on a set/change event"
      pattern: "jtbd-state|getCurrent|serves_jtbd|ADDRESSES_PROBLEM_TYPE"
---

<objective>
Build the two remaining detection paths: SENS-04 (external-fact reference -> hat-scoped WebSearch, the gate-relevant net-new web sensor) and SENS-05 (JTBD set/changed -> re-weight the selector menus + Brain-query weighting, the consumer over the shipped-but-unconsumed Phase 104 signal). SENS-04 is in the cheap-first gate subset and is the strictest Part-8 web path: its query is public SIGNAL (SIGNAL->LOCAL yes, SIGNAL->Brain no), it is hat-scoped per Canon Part 2, and it honors the MCP-stack-ask rule (no silent WebSearch). SENS-05 reuses the Phase 104 JTBD state entirely; the build is the re-weighting consumer that fires on a set/change event.

Purpose: completes SENS-01..07. After this plan, every row of the 7-row trigger map has a sensor that FIRES on its signal, with the gate-relevant SENS-04 honoring the hat-scoping + MCP-stack rules. SENS-04/05 own DISJOINT files under lib/core/sensors/; they edit insight-sensors.cjs + run-all-143.sh, hence wave 3 (after Plan 02's edits to those two shared files).
Output: sensor-external-fact.cjs, hat-scoping-table.cjs, sensor-jtbd-reweight.cjs, two loop-fires tests, registrations, two suites appended to run-all-143.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-insight-sensors-the-7-row-trigger-map/143-CONTEXT.md
@.planning/phases/143-insight-sensors-the-7-row-trigger-map/143-01-PLAN.md
@docs/UI-UX-CONVERGENCE-2026-05-10/00c-TRIGGER-MAP.md
@docs/MINDRIAN-CANON.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. From Plan 01 + the shipped substrate. -->

From Plan 01: makeReach({reach_id, posture, dispatch, companions, signal, evidence}); REACH_IDS=['context_block','contradiction','cross_room','brain_consult','deep_research']; SENSOR_REGISTRY in insight-sensors.cjs; the routing fence + Part-8 sweep already scan lib/core/sensors/*.cjs.

SENS-04 hat-scoping (Canon Part 2 "EXTERNAL WEB" affordance + 00c Section 3 external-fact row + Section 3a):
  - White Hat: Tavily + arxiv (data/research). Green Hat: patents + arxiv + deep-research (innovation lane). Black Hat: failure-case/risk searches. Yellow Hat: success-case/benefit searches. Red Hat: no external tool (intuition only). Blue Hat: synthesis across other hats' returns.
  - The MCP-stack-ask rule (CLAUDE.md memory feedback_mcp_stack_awareness + 00c Section 3a tool-choice): when the web trigger fires, surface a Decision Gate "Tavily / Firecrawl / Exa?" UNLESS pre-configured per hat. No silent WebSearch. No-ask exceptions: Context7 for library docs, Pinecone over the user's own indexes.
  - Part 8: SENS-04 web query is public SIGNAL. SIGNAL->LOCAL: YES (the sourced artifact files locally). SIGNAL->Brain: NO. The query carries the public topic, never the user's proprietary artifact bytes.

SENS-04 reach: reach_id 'deep_research' for the escalation lane (00c Section 3a) or 'context_block' for a shallow external-fact lookup. The external-fact DETECTOR + the hat-scoped dispatch table + the MCP-stack-ask gate are the net-new; /mos:research + WebSearch already exist (reuse per Part 7).

SENS-05 shipped substrate (Phase 104 JTBD, v1.12.3):
  - lib/hmi/jtbd-state.cjs: getCurrent(roomDir) -> {jtbd, confidence, entered_at, evidence} | null; setCurrent(roomDir, {jtbd, confidence, evidence, trigger, manual}); history(roomDir, n); isStale(roomDir, maxAgeHours). The state is CAPTURED but NOT consumed for re-weighting -- that consumer is the build.
  - lib/hmi/jtbd-taxonomy.json (methodology_hooks) + the serves_jtbd command frontmatter + the ADDRESSES_PROBLEM_TYPE Brain edge (query-patterns.md). SENS-05 detects a set/change event (a getCurrent transition vs the prior history row) and re-weights: the selector-menu ordering (serves_jtbd) + the Brain-query weighting (ADDRESSES_PROBLEM_TYPE, generic handles only).
  - lib/workflow/f-selector-ranker.cjs already consumes serves_jtbd -- SENS-05 surfaces the re-weight signal that ranker consumes; it does not re-rank inline.

run-all-143.sh: append test-sens04-external-fact-fires.cjs + test-sens05-jtbd-reweight-fires.cjs to CJS_SUITES with requirement-ownership header comments (Plan 02 already appended its three).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hat-scoping table + SENS-04 external-fact detector with the MCP-stack-ask gate</name>
  <files>lib/core/sensors/hat-scoping-table.cjs, lib/core/sensors/sensor-external-fact.cjs, lib/core/insight-sensors.cjs, tests/test-sens04-external-fact-fires.cjs</files>
  <read_first>
    - docs/MINDRIAN-CANON.md Part 2 (the EXTERNAL WEB affordance: White=Tavily+arxiv, Green=patents+arxiv+deep-research, Black=failure-cases, Yellow=success-cases, Red=none, Blue=synthesis) + Part 8 (SIGNAL->LOCAL: YES, SIGNAL->Brain: NO)
    - docs/UI-UX-CONVERGENCE-2026-05-10/00c-TRIGGER-MAP.md Section 3 (the external-fact row) + Section 3a (the deep-research escalation ladder local->Brain methodology->shallow web->DEEP RESEARCH; the hat affordance tool-choice; the "surface a quick Decision Gate Tavily/Firecrawl/Exa unless pre-configured" rule)
    - commands/research.md (the shipped /mos:research + WebSearch path the reach reuses -- Part 7)
    - tests/test-decide-part8-invariant.cjs (the 5-tripwire sweep -- SENS-04 is a Brain/web-touching sensor and must survive it)
    - lib/core/insight-sensors.cjs (Plan 01 SENSOR_REGISTRY + makeReach)
  </read_first>
  <behavior>
    - Test: hatScopeFor('White') -> data/arxiv tools; 'Green' -> patents+arxiv+deep-research; 'Black' -> failure-cases; 'Red' -> no external tool (returns the intuition-only marker, not a web tool).
    - Test: a turn referencing an external fact (competitor / market / state-of-the-art) with active hat 'Green' returns a reach scoped to the Green tool set, reach_id in {context_block, deep_research}.
    - Test: the reach honors the MCP-stack-ask rule -- when the hat is not pre-configured, the reach carries a Decision-Gate tool-choice flag (Tavily/Firecrawl/Exa); it does NOT mark a silent WebSearch dispatch.
    - Test: a fixture turn whose text contains the user's proprietary artifact bytes yields a reach whose web-query payload contains ONLY the public topic handle, never the artifact bytes (Part 8 SIGNAL->Brain: NO; the public query is SIGNAL).
    - Test: Red Hat returns null for a web reach (intuition only).
  </behavior>
  <action>
    Create lib/core/sensors/hat-scoping-table.cjs exporting hatScopeFor(hat) -> the hat's web-tool scope per Canon Part 2 (the six-hat table). Create lib/core/sensors/sensor-external-fact.cjs exporting sensorExternalFact(turn, tuple, ctx): detect the external-fact reference pattern (competitor/market/state-of-the-art mention), look up the active hat scope via hatScopeFor, and surface a candidate reach reusing the shipped /mos:research + WebSearch dispatch, scoped to the hat's tools. Encode the MCP-stack-ask gate: set a Decision-Gate tool-choice flag (Tavily/Firecrawl/Exa) unless the hat is pre-configured; never emit a silent-WebSearch dispatch. The web query payload carries only the public topic handle (SIGNAL), never user artifact bytes (Part 8). Register sensorExternalFact in SENSOR_REGISTRY.
  </action>
  <verify>
    <automated>node tests/test-sens04-external-fact-fires.cjs && node tests/test-sensors-part8-sweep.cjs</automated>
  </verify>
  <done>SENS-04 loop-fires test green: external-fact turn surfaces a hat-scoped web reach with the MCP-stack-ask gate and no silent WebSearch; the public-SIGNAL Part-8 assertion holds; the Part-8 sweep passes over the new files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SENS-05 JTBD set/changed re-weighting consumer; register both Plan-03 suites in run-all-143.sh</name>
  <files>lib/core/sensors/sensor-jtbd-reweight.cjs, lib/core/insight-sensors.cjs, tests/test-sens05-jtbd-reweight-fires.cjs, tests/run-all-143.sh</files>
  <read_first>
    - lib/hmi/jtbd-state.cjs (getCurrent/setCurrent/history/isStale -- the shipped Phase 104 signal SENS-05 reads to detect a set/change event; the consumer is the build, the signal is shipped)
    - lib/workflow/f-selector-ranker.cjs (the serves_jtbd consumer SENS-05 surfaces the re-weight signal to -- the ranker already exists; SENS-05 does not re-rank inline)
    - lib/hmi/jtbd-taxonomy.json (methodology_hooks + the serves_jtbd mapping the re-weight keys off)
    - docs/UI-UX-CONVERGENCE-2026-05-10/00c-TRIGGER-MAP.md Section 3 (the "JTBD set/changed" row: re-weight selector menus + re-weight Brain queries via ADDRESSES_PROBLEM_TYPE; serves_jtbd re-weighting on the next-move menu)
    - references/brain/query-patterns.md (the ADDRESSES_PROBLEM_TYPE edge the Brain-query re-weight uses, generic handles only -- Part 8)
  </read_first>
  <behavior>
    - Test: given a jtbd-state transition (getCurrent.jtbd differs from the prior history row's `to`), sensorJtbdReweight returns a reach carrying the re-weight signal (the serves_jtbd re-weight for the selector menu + the ADDRESSES_PROBLEM_TYPE handle for the Brain-query weighting).
    - Test: given no JTBD change (current == prior), sensorJtbdReweight returns null (no spurious re-weight).
    - Test: the ADDRESSES_PROBLEM_TYPE Brain-query handle carries only the generic problem-type enum, never user content (Part 8).
  </behavior>
  <action>
    Create lib/core/sensors/sensor-jtbd-reweight.cjs exporting sensorJtbdReweight(turn, tuple, ctx): read the shipped jtbd-state (getCurrent + history) for the room, detect a set/change event (the current jtbd differs from the last history transition), and surface a candidate reach whose payload carries the re-weight signal for the selector menus (serves_jtbd) and the Brain-query weighting (ADDRESSES_PROBLEM_TYPE with the generic problem-type enum only). The sensor does NOT re-rank inline -- it surfaces the signal that f-selector-ranker consumes. Register in SENSOR_REGISTRY. Append test-sens04-external-fact-fires.cjs + test-sens05-jtbd-reweight-fires.cjs to the CJS_SUITES array in tests/run-all-143.sh with their requirement-ownership header comments.
  </action>
  <verify>
    <automated>node tests/test-sens05-jtbd-reweight-fires.cjs && bash tests/run-all-143.sh</automated>
  </verify>
  <done>SENS-05 loop-fires test green (JTBD set/change surfaces the re-weight reach; no-change returns null; ADDRESSES_PROBLEM_TYPE handle is generic only); both Plan-03 suites registered; the full run-all-143.sh exits cleanly with all seven SENS suites + the routing fence + the Part-8 sweep passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SENS-04 -> external web | the web query is public SIGNAL; the payload carries only the public topic, never user artifact bytes (Part 8 SIGNAL->Brain: NO) |
| SENS-04 -> tool choice | the MCP-stack-ask gate surfaces a Decision Gate tool choice; no silent WebSearch |
| SENS-05 -> Brain query weighting | the ADDRESSES_PROBLEM_TYPE handle is generic problem-type enum only (Part 8) |
| detectors -> routing trace | no detector flips routing_source (Phase 144 fence) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-07 | Information Disclosure | sensorExternalFact web-query payload | mitigate | the query carries only the public topic handle (SIGNAL); the SENS-04 no-leak test asserts no artifact bytes in the payload; the inherited Part-8 sweep covers the file |
| T-143-08 | Spoofing | the MCP-stack tool choice | mitigate | the MCP-stack-ask gate routes tool selection through the Decision Gate (Part 3); no silent WebSearch dispatch |
| T-143-09 | Information Disclosure | sensorJtbdReweight ADDRESSES_PROBLEM_TYPE handle | mitigate | generic problem-type enum only; the SENS-05 no-leak test + the inherited Part-8 sweep assert no user content |
| T-143-10 | Tampering | the two detector files | mitigate | the inherited test-sensors-routing-fence scans lib/core/sensors/*.cjs for routing_source='engine' |
| T-143-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs; pure CJS reusing /mos:research + the Phase 104 JTBD state |
</threat_model>

<verification>
- `bash tests/run-all-143.sh` runs to completion; all SENS-01..07 suites + the routing fence + the Part-8 sweep PASS (the full Phase 143 acceptance surface, composable by Phase 146).
- `node tests/test-sensors-part8-sweep.cjs` passes over sensor-external-fact.cjs + sensor-jtbd-reweight.cjs (SENS-04 public SIGNAL, SENS-05 generic handle).
- `grep -rEn "routing_source\s*[:=]\s*['\"]engine['\"]" lib/core/sensors/` returns zero matches.
- No new dependency added.
</verification>

<success_criteria>
- SENS-04 surfaces a hat-scoped WebSearch reach honoring the MCP-stack-ask rule -- ROADMAP success criterion 3 (the external-fact half).
- SENS-05 re-weights the selector menus + Brain queries on a JTBD set/change -- ROADMAP success criterion 4 (the JTBD half).
- All seven sensors (SENS-01..07) now fire on their signals; the Brain/web scan confirms zero user-content egress (ROADMAP success criterion 5).
- SENS-04 reuses /mos:research, SENS-05 reuses the Phase 104 JTBD state (Part 7); neither flips routing_source (Phase 144 fence).
</success_criteria>

<output>
Create `.planning/phases/143-insight-sensors-the-7-row-trigger-map/143-03-SUMMARY.md` when done.
</output>
