---
phase: 143-insight-sensors-the-7-row-trigger-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/insight-sensors.cjs
  - lib/core/sensors/sensor-types.cjs
  - tests/test-sensor-spine-dispatch.cjs
  - tests/test-sens01-first-material-fires.cjs
  - tests/test-sens06-artifact-filed-fires.cjs
  - tests/test-sensors-part8-sweep.cjs
  - tests/test-sensors-routing-fence.cjs
  - tests/run-all-143.sh
autonomous: true
requirements: [SENS-01, SENS-06]
canon_parts: [Part 2, Part 2 Engine 1, Part 3, Part 4, Part 8]

must_haves:
  truths:
    - "The sensor module exposes a single pure dispatch surface that maps a turn signal + the {problem_type, complexity, stage} tuple to a candidate reach keyed by a stable reach id, or null."
    - "First material in a session surfaces the explore-domains + whitespace + brain_framework_chain($problem_type) reach (the Phase 117 auto-explore path), proven by a loop-fires test."
    - "Filing an artifact surfaces the cross-relationship cascade reach (the shipped CASC-01 side-channel), proven by a loop-fires test that reads last-cascade.json -- not a re-implementation."
    - "No sensor sets trace.routing_source='engine' or rewrites decide()'s file-presence routing (Phase 144 fence), asserted by grep."
    - "No sensor carries user-content bytes to Brain/web; only generic handles (framework names, problem-type enums, phase ids), asserted by the Part-8 5-tripwire sweep."
    - "tests/run-all-143.sh aggregates every Phase 143 loop-fires suite, mirroring run-all-142.sh, so Phase 146 can compose them."
  artifacts:
    - path: "lib/core/insight-sensors.cjs"
      provides: "The 7-row trigger map as code: the sensor registry + dispatch chokepoint + SENS-01/SENS-06 sensors"
      min_lines: 80
      exports: ["dispatchSensors", "REACH_IDS", "POSTURE_IDS", "sensorFirstMaterial", "sensorArtifactFiled"]
    - path: "lib/core/sensors/sensor-types.cjs"
      provides: "The candidate-reach struct contract + the 5 reach ids + 3 posture ids shared by every detector"
      min_lines: 30
      contains: "context_block"
    - path: "tests/run-all-143.sh"
      provides: "Phase 143 scoped loop-fires aggregator"
      contains: "CJS_SUITES"
  key_links:
    - from: "lib/core/insight-sensors.cjs"
      to: "scripts/auto-explore-fire.cjs"
      via: "SENS-01 surfaces the explore-domains/whitespace reach the shipped Phase 117 fire path produces"
      pattern: "auto-explore|explored-materials|first_material"
    - from: "lib/core/insight-sensors.cjs"
      to: "room/.mindrian/last-cascade.json"
      via: "SENS-06 reads the shipped CASC-01 side-channel; does not re-run the cascade"
      pattern: "last-cascade"
    - from: "lib/core/sensors/sensor-types.cjs"
      to: "skills/larry-personality/SKILL.md"
      via: "the 5 reach ids + 3 posture ids match the committed Phase 141 dial doctrine (LARRY-03/D-12)"
      pattern: "context_block.*contradiction.*cross_room.*brain_consult.*deep_research"
---

<objective>
Build the net-new spine of the 7-row trigger map: ONE sensor module that classifies a turn signal against the /mos:diagnose {problem_type, complexity, stage} tuple and returns a candidate reach (keyed by a stable reach id) or null. The module is pure, sync, LOCAL-first. It also lands the two SHIPPED-substrate sensors as verify/thin: SENS-01 (first-material, over Phase 117 auto-explore) and SENS-06 (artifact-filed, over the Phase 142 CASC-01 cascade). The two module-level gates -- the Phase 144 routing_source fence and the Part-8 5-tripwire sweep -- and the run-all-143.sh aggregator are established here so Plans 02 and 03 inherit them.

Purpose: every other detector (Plans 02, 03) plugs into this dispatch contract. Defining the registry + reach-struct + reach/posture id banks first prevents downstream scavenger-hunting and guarantees the Phase 144 flip + the dial-TUI (143.1) key off stable ids.
Output: lib/core/insight-sensors.cjs, lib/core/sensors/sensor-types.cjs, 5 loop-fires/gate test suites, tests/run-all-143.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-insight-sensors-the-7-row-trigger-map/143-CONTEXT.md
@docs/UI-UX-CONVERGENCE-2026-05-10/00c-TRIGGER-MAP.md
@docs/MINDRIAN-CANON.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. Do not re-discover. -->

The 5 stable reach ids + 3 posture ids (committed prose doctrine in skills/larry-personality/SKILL.md, Phase 141 LARRY-03/D-12). sensor-types.cjs makes them machine constants:
  REACH_IDS  = ['context_block', 'contradiction', 'cross_room', 'brain_consult', 'deep_research']
  POSTURE_IDS = ['push_forward', 'hold', 'pull_back']

SENS-01 shipped substrate (scripts/auto-explore-fire.cjs, Phase 117):
  - The first-material fire path runs discovery-cycle.cjs + rs-engine.py --mode hybrid in parallel, composes via lib/agents/auto-explore-agent.cjs::composeAutoExploreFinding, writes room/.mindrian/auto-explore-<material_id>.json, marks the ledger via lib/memory/explored-materials-store.cjs::markCompleted.
  - SENS-01 is PARTIAL: explore-domains + whitespace already fire via this path. The sensor's job is to (a) detect the first-material signal and surface the candidate reach with reach_id derived from this path, (b) ensure the brain_framework_chain($problem_type) COMPANION is part of the surfaced reach (generic $problem_type enum handle only -- Part 8). Do NOT re-run or fork the Phase 117 pipeline.

SENS-06 shipped substrate (the CASC-01 cascade, Phase 142):
  - scripts/post-write::write_cascade_side_channel writes <roomDir>/.mindrian/last-cascade.json atomically; proactive_intelligence.newFindings carries the cross-relationship findings.
  - skills/room-proactive/SKILL.md reads it on additionalContext prefix `^post-write: cascade complete` or `^queued MINTO regen`.
  - SENS-06 is VERIFY: the sensor reads last-cascade.json and surfaces the candidate reach when proactive_intelligence.newFindings is non-empty. It does NOT re-implement the Phase 95/142 cascade. The loop-fires test asserts the reach fires given a populated side-channel fixture.

The Phase 144 fence (HARD): no sensor may set trace.routing_source='engine'. The sensors surface candidate reaches; decide() (lib/core/navigation-engine.cjs) consumes them later (Phase 144). The fence test greps the sensor module + lib/core/sensors/ for any `routing_source` assignment to 'engine' and any rewrite of decide()'s file-presence routing.

The Part-8 5-tripwire pattern (Phase 90 / tests/test-decide-part8-invariant.cjs): forbidden-require sweep (packet, brain-client) + forbidden-token sweep (projectText/shortText/hashText/etc) + forbidden-call sweep (sha256/createHash). SENS-01/03/04 are the Brain/web-touching sensors; this module hosts SENS-01, so the sweep applies here and is inherited by Plans 02/03.

run-all-142.sh aggregator pattern (tests/run-all-142.sh): SHELL_SUITES + CJS_SUITES arrays, per-suite PASS/FAIL line, runs to completion on failure, exits non-zero if any failed, named so the Phase 146 gate composes them. Mirror exactly.

The candidate-reach struct (sensor-types.cjs): { reach_id (one of REACH_IDS), posture (one of POSTURE_IDS), dispatch (the /mos: command slug or shipped-engine handle to fire), companions (array of additional generic handles, e.g. brain_framework_chain), signal (the signal kind that fired it), evidence (LOCAL scalars only -- never user content) } or null when the sensor does not fire.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sensor-types contract + reach/posture id banks + the dispatch chokepoint shell</name>
  <files>lib/core/sensors/sensor-types.cjs, lib/core/insight-sensors.cjs, tests/test-sensor-spine-dispatch.cjs</files>
  <read_first>
    - skills/larry-personality/SKILL.md (lines ~45-92: the 5 reach id mappings + the 3 posture ids + arbitration rule 7 -- the doctrine sensor-types.cjs makes machine-readable)
    - lib/core/nav-dial.cjs (the resolveDialPosition pure-function pattern + module.exports shape to mirror for a pure, zero-I/O sensor module)
    - .planning/phases/141-local-retrieval-spine-and-capability-dial/141-CONTEXT.md (D-05 exactly-5-reaches drift contract + D-12 exactly-3-postures drift contract -- the same drift-test discipline this module inherits)
    - docs/UI-UX-CONVERGENCE-2026-05-10/00c-TRIGGER-MAP.md Section 1 (the 6 signal kinds) + Section 8 (the closed-loop cycle the dispatch chokepoint sits inside)
  </read_first>
  <behavior>
    - Test: REACH_IDS exports exactly ['context_block','contradiction','cross_room','brain_consult','deep_research'] (no more, no fewer -- drift test mirroring Phase 141 D-05).
    - Test: POSTURE_IDS exports exactly ['push_forward','hold','pull_back'] (drift test mirroring Phase 141 D-12).
    - Test: makeReach({reach_id, posture, dispatch, signal}) returns a frozen struct; an invalid reach_id or posture returns null (never throws).
    - Test: dispatchSensors(turn, tuple, ctx) returns an array of candidate reaches (possibly empty), never throws on malformed input, and runs every registered sensor in canonical order.
    - Test: dispatchSensors with an empty registry returns [] and makes zero network calls and zero non-LOCAL filesystem reads.
  </behavior>
  <action>
    Create lib/core/sensors/sensor-types.cjs exporting REACH_IDS, POSTURE_IDS, and a pure makeReach(opts) factory that validates reach_id against REACH_IDS and posture against POSTURE_IDS, returning a frozen candidate-reach struct (reach_id, posture, dispatch, companions, signal, evidence) or null. Create lib/core/insight-sensors.cjs as a pure/sync/LOCAL-first module exporting dispatchSensors(turn, tuple, ctx) -- the chokepoint that runs a SENSOR_REGISTRY array of sensor functions in canonical order and collects non-null candidate reaches. Each sensor signature is sensorFn(turn, tuple, ctx) -> candidate-reach|null where tuple is the /mos:diagnose {problem_type, complexity, stage} classification. dispatchSensors NEVER mutates routing_source and NEVER calls decide(); it only PRODUCES candidate reaches per Canon Part 3 (surfaced at the Decision Gate). Stub SENSOR_REGISTRY empty for now; Tasks 2-3 register the two sensors. No fenced code in this plan -- name identifiers and behavior only.
  </action>
  <verify>
    <automated>node tests/test-sensor-spine-dispatch.cjs</automated>
  </verify>
  <done>sensor-types.cjs exports the exactly-5 reach ids + exactly-3 posture ids with passing drift assertions; insight-sensors.cjs exposes dispatchSensors as a pure chokepoint; test-sensor-spine-dispatch.cjs green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SENS-01 first-material sensor (verify Phase 117 + brain_framework_chain companion) and SENS-06 artifact-filed sensor (verify CASC-01)</name>
  <files>lib/core/insight-sensors.cjs, tests/test-sens01-first-material-fires.cjs, tests/test-sens06-artifact-filed-fires.cjs</files>
  <read_first>
    - scripts/auto-explore-fire.cjs (the shipped Phase 117 first-material fire path: explored-materials-store ledger + auto-explore-<material_id>.json output -- SENS-01 surfaces the reach this path represents, does NOT fork it)
    - lib/memory/explored-materials-store.cjs (markCompleted/markFailed ledger -- the LOCAL signal SENS-01 reads to know first-material fired)
    - scripts/post-write (write_cascade_side_channel: the <roomDir>/.mindrian/last-cascade.json shape with proactive_intelligence.newFindings -- the LOCAL signal SENS-06 reads)
    - skills/room-proactive/SKILL.md (lines ~87-100: the side-channel read contract + the additionalContext trigger prefixes -- SENS-06 mirrors this read, never re-runs the cascade)
    - references/brain/query-patterns.md (Section 1 brain_framework_chain: the $problem_type generic-handle parameter SENS-01 attaches as a companion -- Part 8: enum only, never user content)
  </read_first>
  <behavior>
    - Test (SENS-01): given a turn carrying the first-material signal + a tuple with problem_type='undefined', sensorFirstMaterial returns a reach with reach_id='context_block', dispatch referencing the explore-domains/whitespace path, and a companions entry 'brain_framework_chain:undefined' (generic enum handle only).
    - Test (SENS-01): given NO first-material signal, sensorFirstMaterial returns null.
    - Test (SENS-01): the companion handle carries ONLY the problem_type enum string -- a fixture turn containing user artifact text does NOT appear in the returned reach (Part 8 LOCAL->Brain:NO).
    - Test (SENS-06): given a last-cascade.json fixture with proactive_intelligence.newFindings non-empty, sensorArtifactFiled returns a reach with reach_id='contradiction' (or 'cross_room' per finding edge type) and dispatch referencing the cascade surface -- LOOP FIRES.
    - Test (SENS-06): given a missing or empty side-channel, sensorArtifactFiled returns null (soft-fail, mirrors room-proactive).
  </behavior>
  <action>
    In lib/core/insight-sensors.cjs add sensorFirstMaterial(turn, tuple, ctx): detect the first-material signal (the Phase 117 explored-materials ledger entry / the auto-explore-<material_id>.json presence threaded on ctx), and surface a candidate reach (reach_id 'context_block', posture 'push_forward') whose dispatch names the shipped explore-domains + whitespace path and whose companions include the brain_framework_chain handle carrying ONLY tuple.problem_type (a generic enum per Part 8 -- never artifact bytes). Do NOT re-run the Phase 117 pipeline; the sensor surfaces the reach, the pipeline is already wired. Add sensorArtifactFiled(turn, tuple, ctx): read <roomDir>/.mindrian/last-cascade.json via the established LOCAL read, and when proactive_intelligence.newFindings is non-empty surface a candidate reach (reach_id 'contradiction'/'cross_room' chosen by the finding's edge type, posture 'pull_back' for contradictions) referencing the shipped cascade surface. Register both in SENSOR_REGISTRY. Both sensors LOCAL-only, never throw, soft-fail to null.
  </action>
  <verify>
    <automated>node tests/test-sens01-first-material-fires.cjs && node tests/test-sens06-artifact-filed-fires.cjs</automated>
  </verify>
  <done>SENS-01 loop-fires test green (reach + brain_framework_chain companion + Part-8 no-leak assertion); SENS-06 loop-fires test green (reach fires on populated side-channel, null on empty) with no re-implementation of the cascade.</done>
</task>

<task type="auto">
  <name>Task 3: Module-level gates (Phase 144 routing fence + Part-8 5-tripwire sweep) + run-all-143.sh aggregator</name>
  <files>tests/test-sensors-routing-fence.cjs, tests/test-sensors-part8-sweep.cjs, tests/run-all-143.sh</files>
  <read_first>
    - tests/test-decide-part8-invariant.cjs (the Phase 142 forbidden-require + forbidden-token + forbidden-call 5-tripwire sweep to mirror over the sensor module + lib/core/sensors/)
    - lib/core/navigation-engine.cjs (lines ~537-538: the existing "routing_source is NOT touched (Phase 144 fence)" comment -- the fence the sensor module must also honor; the fence test asserts the sensor module adds NO routing_source='engine' assignment)
    - tests/run-all-142.sh (the SHELL_SUITES/CJS_SUITES aggregator to mirror exactly as run-all-143.sh, with per-suite plan-ownership header comments)
    - .planning/phases/143-insight-sensors-the-7-row-trigger-map/143-CONTEXT.md (the hard fences section: Phase 144 fence + Part 8 fence + the cheap-first gate subset SENS-01/04/06 for Phase 146)
  </read_first>
  <acceptance_criteria>
    - test-sensors-routing-fence.cjs greps lib/core/insight-sensors.cjs + every file under lib/core/sensors/ and asserts ZERO matches for routing_source assignment to 'engine' (regex over `routing_source\s*[:=]\s*['"]engine['"]`) and ZERO rewrite of decide()'s file-presence routing; exits non-zero on any match.
    - test-sensors-part8-sweep.cjs runs the 5-tripwire forbidden-require/token/call sweep over insight-sensors.cjs + lib/core/sensors/*.cjs and exits non-zero on any forbidden Brain-egress surface.
    - tests/run-all-143.sh registers test-sensor-spine-dispatch, test-sens01-first-material-fires, test-sens06-artifact-filed-fires, test-sensors-routing-fence, test-sensors-part8-sweep in CJS_SUITES with a header comment mapping each suite to its requirement (SENS-01/SENS-06/fence/part8), runs to completion on failure, exits non-zero if any failed.
    - `bash tests/run-all-143.sh` runs to completion and reports per-suite PASS/FAIL.
  </acceptance_criteria>
  <action>
    Create test-sensors-routing-fence.cjs: a grep-style assertion suite over insight-sensors.cjs + lib/core/sensors/ enforcing the Phase 144 fence (no routing_source='engine', no decide() routing rewrite). Create test-sensors-part8-sweep.cjs mirroring the Phase 142 5-tripwire pattern (FORBIDDEN_REQUIRES packet/brain-client, FORBIDDEN_TOKENS projectText/etc, FORBIDDEN_CALLS sha256/createHash) over the sensor module + lib/core/sensors/. Create tests/run-all-143.sh by mirroring run-all-142.sh: copy the structure, replace the CJS_SUITES with the Phase 143 suites, and write the per-suite plan-ownership header so the Phase 146 loop-fires gate (ACPT-01..05) can compose them. Plans 02 and 03 will append their detector suites to this runner.
  </action>
  <verify>
    <automated>node tests/test-sensors-routing-fence.cjs && node tests/test-sensors-part8-sweep.cjs && bash tests/run-all-143.sh</automated>
  </verify>
  <done>The routing fence + Part-8 sweep both pass over the sensor module; run-all-143.sh aggregates all Plan-01 suites, mirrors run-all-142.sh, and exits cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sensor module -> Brain MCP | SENS-01 attaches a brain_framework_chain companion handle; the boundary is the generic-handle-only constraint (Part 8 LOCAL->Brain: NO) |
| sensor module -> routing trace | the sensors PRODUCE candidate reaches; they must not CONSUME the routing decision by flipping routing_source (Phase 144 fence) |
| side-channel file -> sensor | SENS-06 reads room-local last-cascade.json (LOCAL only; no network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Information Disclosure | sensorFirstMaterial brain_framework_chain companion | mitigate | companion carries ONLY tuple.problem_type enum; test-sensors-part8-sweep + the SENS-01 no-leak test assert no artifact bytes reach the handle |
| T-143-02 | Tampering | insight-sensors.cjs routing surface | mitigate | test-sensors-routing-fence asserts zero routing_source='engine' assignment (Phase 144 fence) |
| T-143-03 | Information Disclosure | sensorArtifactFiled side-channel read | accept | last-cascade.json is LOCAL by Canon Part 8; read is in-process, no network surface |
| T-143-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs in this plan; sensor module is pure CJS node built-ins + project libs |
</threat_model>

<verification>
- `bash tests/run-all-143.sh` runs to completion; every Plan-01 suite PASSES.
- `grep -rEn "routing_source\s*[:=]\s*['\"]engine['\"]" lib/core/insight-sensors.cjs lib/core/sensors/` returns zero matches (Phase 144 fence).
- `grep -rn "context_block\|brain_consult\|deep_research" lib/core/sensors/sensor-types.cjs` confirms the reach id bank matches the committed dial doctrine.
- No new dependency added (pure CJS).
</verification>

<success_criteria>
- The sensor module spine + reach/posture id banks exist and are pure/sync/LOCAL-first.
- SENS-01 surfaces the first-material reach with the brain_framework_chain($problem_type) companion (generic handle only) -- ROADMAP success criterion 1 (the surfacing half).
- SENS-06 loop-fires over the shipped CASC-01 cascade without re-implementation -- ROADMAP success criterion 4 (the artifact-filed half).
- The Phase 144 routing fence + Part-8 5-tripwire sweep gate the module -- ROADMAP success criterion 5 (zero user-content egress).
- run-all-143.sh aggregates the suites, mirroring run-all-142.sh.
</success_criteria>

<output>
Create `.planning/phases/143-insight-sensors-the-7-row-trigger-map/143-01-SUMMARY.md` when done.
</output>
