---
phase: 138-capability-radar-absorption-and-routing
plan: 03
type: execute
wave: 2
depends_on: ["138-01"]
files_modified:
  - scripts/sessionstart-coordinator.cjs
  - scripts/contribute-room-title.cjs
  - skills/whitespace-cluster/SKILL.md
  - .claude-plugin/plugin.json
  - docs/RADAR-BUCKET-C.md
  - tests/test-sessionstart-room-title.cjs
  - tests/test-radar-skill-scoping.cjs
autonomous: true
requirements: [RAD-05]
must_haves:
  truths:
    - "the single SessionStart coordinator emits hookSpecificOutput.sessionTitle sourced from the active room slug via a new room-title contributor (NOT a second SessionStart hook entry)"
    - "the coordinator returns reloadSkills: true so /reload-skills hot-swaps surfaced skills per room"
    - "at least one optional skill cluster ships defaultEnabled: false"
    - "at least one skill carries disallowed-tools in its frontmatter for per-skill tool scoping (Part 8 hardening)"
  artifacts:
    - path: "scripts/contribute-room-title.cjs"
      provides: "The room-title contributor feeding sessionTitle into the single coordinator envelope (RAD-05 #3)"
      contains: "sessionTitle"
    - path: "docs/RADAR-BUCKET-C.md"
      provides: "The Bucket C landing record (session-title, reloadSkills, defaultEnabled, disallowed-tools) with the 121.5-shipped rationale"
      min_lines: 20
  key_links:
    - from: "scripts/sessionstart-coordinator.cjs"
      to: "scripts/contribute-room-title.cjs"
      via: "the coordinator adds room-title to DEFAULT_CONTRIBUTOR_MAP and attaches sessionTitle + reloadSkills to its single emitted envelope"
      pattern: "sessionTitle|reloadSkills"
    - from: ".claude-plugin/plugin.json"
      to: "skills/whitespace-cluster"
      via: "the optional cluster ships defaultEnabled: false"
      pattern: "defaultEnabled"
---

<objective>
Land Bucket C (RAD-05): the four hook/skill capabilities that could NOT fold into Phase 121.5 because it already shipped. The SessionStart session-title reflects the active room, reloadSkills hot-swaps surfaced skills, at least one optional skill cluster ships defaultEnabled: false, and at least one skill carries disallowed-tools (Part 8 hardening). Every piece wires into the single SessionStart owner (sessionstart-coordinator.cjs) or static skill/manifest frontmatter, never a second SessionStart hook entry.

Purpose: These are the capstone items the CONTEXT routes HERE because 121.5 cannot be amended. The session-title injection point is THE reuse anchor (do not add a second SessionStart entry, Pitfall 4); the toggleable cluster and per-skill tool scoping are static frontmatter the platform reads on all three surfaces.
Output: a room-title contributor + the coordinator sessionTitle/reloadSkills wiring + one defaultEnabled: false cluster + one disallowed-tools skill + docs/RADAR-BUCKET-C.md + two CJS suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-capability-radar-absorption-and-routing/138-CONTEXT.md
@.planning/phases/138-capability-radar-absorption-and-routing/138-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Room-title contributor + coordinator sessionTitle + reloadSkills wiring (RAD-05 #3, #4)</name>
  <files>scripts/contribute-room-title.cjs, scripts/sessionstart-coordinator.cjs, tests/test-sessionstart-room-title.cjs</files>
  <read_first>
    - scripts/sessionstart-coordinator.cjs lines 42-52, 76-84, 143-145 (the DEFAULT_CONTRIBUTOR_MAP of lazy-required contribute*() fragments; runAll(opts) composing ONE additionalContext body under the 2000-char budget; the single emitted envelope hookSpecificOutput {hookEventName SessionStart, additionalContext})
    - scripts/check-onboard-statusline.cjs (an existing contributor exposing contributeSealed/contributeOnboarding -- the contributor shape to mirror)
    - lib/core/navigation/room-context.cjs (the active-room slug source the room-title contributor reads, LOCAL only)
    - 138-RESEARCH.md Pitfall 4 (do NOT add a second SessionStart hook entry; add a room-title contributor to DEFAULT_CONTRIBUTOR_MAP and have the coordinator attach sessionTitle to its single envelope), the SessionStart sessionTitle code example (sessionTitle + reloadSkills on the one envelope), and the Reuse-Surface Map sessionstart-coordinator.cjs row (THE injection point)
    - 138-CONTEXT.md ledger rows #3 (SessionStart session title, 2.1.152) and #4 (reloadSkills + /reload-skills, 2.1.152)
  </read_first>
  <behavior>
    - the coordinator's single emitted envelope carries hookSpecificOutput.sessionTitle equal to the active room slug when a room is active
    - when no room is active the sessionTitle is omitted (degrade, never a blank title)
    - the coordinator returns reloadSkills: true on its single envelope
    - no second top-level SessionStart array entry is added to hooks.json
  </behavior>
  <action>
    Create scripts/contribute-room-title.cjs exposing a contribute() that returns the active room slug (read LOCAL via lib/core/navigation/room-context.cjs), or null when no room is active. Add a room-title entry to the sessionstart-coordinator.cjs DEFAULT_CONTRIBUTOR_MAP (the lazy-require pattern) and, in runAll, attach sessionTitle (from the room-title contributor) and reloadSkills: true to the SINGLE emitted hookSpecificOutput envelope. Do NOT add a second SessionStart hook entry to hooks.json (Pitfall 4; 121.5 consolidated SessionStart into this one owner). Keep the 2000-char additionalContext budget and the D-13 precedence intact; sessionTitle and reloadSkills are envelope siblings of additionalContext, not part of the char budget. Write tests/test-sessionstart-room-title.cjs (node:assert/strict): with a room active the envelope carries sessionTitle = the slug and reloadSkills true; with no room active sessionTitle is omitted; assert hooks.json still has exactly one SessionStart entry.
  </action>
  <verify>
    <automated>node tests/test-sessionstart-room-title.cjs && grep -q sessionTitle scripts/sessionstart-coordinator.cjs && grep -q reloadSkills scripts/sessionstart-coordinator.cjs</automated>
  </verify>
  <done>contribute-room-title.cjs feeds the active room slug as sessionTitle into the single coordinator envelope (omitted when no room active); the coordinator returns reloadSkills: true; no second SessionStart hook entry is added; the test is green.</done>
</task>

<task type="auto">
  <name>Task 2: One defaultEnabled: false optional cluster + one disallowed-tools skill + the Bucket C record (RAD-05 #5, #6)</name>
  <files>skills/whitespace-cluster/SKILL.md, .claude-plugin/plugin.json, docs/RADAR-BUCKET-C.md</files>
  <read_first>
    - .claude-plugin/plugin.json (the plugin manifest; where defaultEnabled is declared for an optional cluster, enabled via /plugin)
    - skills/intelligence-orchestrator/SKILL.md (an existing SKILL.md frontmatter shape to mirror for the disallowed-tools addition; the doctrine-fence style)
    - skills/whitespace-cluster or the nearest existing optional skill cluster (pick ONE genuinely-optional cluster to ship defaultEnabled: false; whitespace is the discretion candidate per CONTEXT Claude Discretion -- confirm the exact existing skill dir at execute time and use it)
    - 138-RESEARCH.md ledger rows #5 (defaultEnabled: false for plugins, 2.1.154) and #6 (disallowed-tools in skill frontmatter, 2.1.152; per-skill tool scoping, Part 8 hardening), the Architectural Responsibility Map defaultEnabled/disallowed-tools row (static frontmatter the platform reads on all three surfaces), and Claude Discretion (which optional cluster ships defaultEnabled: false first is the planner choice, >=1 required)
    - 138-CONTEXT.md Bucket C (these four land HERE because 121.5 already shipped) and Out of scope (do NOT fold into 121.5)
  </read_first>
  <action>
    Pick ONE genuinely-optional skill cluster (whitespace-cluster is the discretion candidate; confirm the exact existing skill directory at execute time) and declare it defaultEnabled: false in the plugin manifest (.claude-plugin/plugin.json) so it ships off-by-default and is enabled via /plugin (RAD-05 #5). Add disallowed-tools to that same cluster SKILL.md frontmatter (or another skill, >=1 required) for per-skill tool scoping (RAD-05 #6, Part 8 hardening; remove a tool the skill must never use). Create docs/RADAR-BUCKET-C.md recording all four Bucket C items landing HERE with the rationale that Phase 121.5 already shipped (so they could not fold in), and the tri-polar note (defaultEnabled/disallowed-tools are static frontmatter read on CLI/Desktop/Cowork; the toggle UX is the CLI /plugin). No em-dashes. Do NOT fold anything into 121.5 (Out of scope LOCKED).
  </action>
  <verify>
    <automated>grep -q defaultEnabled .claude-plugin/plugin.json && grep -rq disallowed-tools skills/ && grep -qi defaultEnabled docs/RADAR-BUCKET-C.md</automated>
  </verify>
  <done>one optional cluster ships defaultEnabled: false in the manifest; at least one skill carries disallowed-tools; docs/RADAR-BUCKET-C.md records the four Bucket C items with the 121.5-shipped rationale; nothing folded into 121.5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room slug -> sessionTitle | The active room slug is a LOCAL string surfaced as the session title; LOCAL only, never egresses |
| skill frontmatter -> platform tool grant | disallowed-tools removes a tool the skill must never use (Part 8 hardening at the skill boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-03-01 | Elevation of Privilege | a skill retaining a tool it should not have | mitigate | disallowed-tools per-skill scoping removes the tool in frontmatter (RAD-05 #6, Part 8 hardening) |
| T-138-03-02 | Tampering | a second SessionStart hook entry colliding with the single owner | mitigate | room-title is added as a contributor to the one coordinator (Pitfall 4); the test asserts exactly one SessionStart entry |
| T-138-03-03 | Information Disclosure | room slug as sessionTitle egressing | accept | the slug is a LOCAL label surfaced in the terminal title only; no Brain/network path; LOCAL by construction |
| T-138-03-SC | Tampering | npm/pip/cargo installs | mitigate | zero installs (a contributor CJS + frontmatter/manifest edits + a doc + tests); no package surface |
</threat_model>

<verification>
- node tests/test-sessionstart-room-title.cjs (sessionTitle = active room slug; reloadSkills true; exactly one SessionStart entry)
- node tests/test-radar-skill-scoping.cjs (defaultEnabled: false on the optional cluster; disallowed-tools present on >=1 skill)
- grep confirms defaultEnabled in the manifest, disallowed-tools in skills/, and the Bucket C record
</verification>

<success_criteria>
- SessionStart sessionTitle reflects the active room via a contributor on the single coordinator, not a second hook entry (RAD-05)
- the coordinator returns reloadSkills: true (RAD-05)
- at least one optional cluster ships defaultEnabled: false (RAD-05)
- at least one skill carries disallowed-tools (RAD-05)
- docs/RADAR-BUCKET-C.md records the four items landing here because 121.5 shipped; nothing folded into 121.5
</success_criteria>

<output>
Create `.planning/phases/138-capability-radar-absorption-and-routing/138-03-SUMMARY.md` when done. Write tests/test-radar-skill-scoping.cjs (asserting defaultEnabled: false + disallowed-tools), confirm both 138-03 suites are listed in tests/run-all-138.sh, and tick the ROADMAP Phase 138 row for 138-03.
</output>
