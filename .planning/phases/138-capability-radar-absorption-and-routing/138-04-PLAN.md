---
phase: 138-capability-radar-absorption-and-routing
plan: 04
type: execute
wave: 3
depends_on: ["138-01", "138-02", "138-03"]
files_modified:
  - docs/A4-DECISION.md
  - .planning/seeds/SEED-003-claude-code-2-1-x-capability-adoption.md
  - docs/CANON-PHASE-MAP.md
  - tests/test-a4-supersede.cjs
  - tests/test-seed003-superseded.cjs
autonomous: true
requirements: [RAD-06, RAD-07]
must_haves:
  truths:
    - "the A4 adopt-vs-supersede decision is recorded as SUPERSEDE: Opus 4.8 dynamic workflows supersede SEED-003 A4; no hand-rolled fork-subagent harness ships and no CLAUDE_CODE_FORK_SUBAGENT env default is set"
    - "no FORK_SUBAGENT literal exists in lib/ scripts/ agents/ outside docs/references (the no-harness proof)"
    - "SEED-003 status is flipped to superseded with superseded-by: Phase 138 and forward-points to the capability ledger"
    - "the CANON-PHASE-MAP Phase 138 row is flipped from NOT built to shipped with the A4-superseded note"
  artifacts:
    - path: "docs/A4-DECISION.md"
      provides: "The recorded A4 SUPERSEDE decision + the BONO-on-dynamic-workflows forward-path mapping (RAD-06)"
      min_lines: 25
    - path: "tests/test-a4-supersede.cjs"
      provides: "The no-FORK_SUBAGENT-literal tripwire + the A4-decision-recorded assertion"
      contains: "FORK_SUBAGENT"
  key_links:
    - from: ".planning/seeds/SEED-003-claude-code-2-1-x-capability-adoption.md"
      to: "data/capability-ledger.json"
      via: "SEED-003 superseded-by Phase 138 forward-points to the living ledger"
      pattern: "superseded"
    - from: "docs/A4-DECISION.md"
      to: "docs/CANON-PHASE-MAP.md"
      via: "the A4 SUPERSEDE decision is reflected in the canon-phase-map Phase 138 row"
      pattern: "A4|supersede"
---

<objective>
Record the A4 adopt-vs-supersede decision as SUPERSEDE (RAD-06) and flip SEED-003 to superseded-by Phase 138 (RAD-07), then reconcile the CANON-PHASE-MAP Phase 138 row from "NOT built" to shipped. Opus 4.8 dynamic workflows are a strict superset of the manual CLAUDE_CODE_FORK_SUBAGENT harness SEED-003 A4 proposed; this plan bakes in the decision (NO fork harness, NO env flag) per the research recommendation and the CONTEXT must-honor.

Purpose: This closes the SEED-003 lifecycle (the dormant one-shot backlog this phase supersedes) and records the constitutional decision so no future phase builds the obsolete harness. The proof is a tripwire asserting no FORK_SUBAGENT literal ships and the SEED-003 status flip.
Output: docs/A4-DECISION.md (SUPERSEDE + BONO-on-dynamic-workflows forward path) + the SEED-003 status flip + the CANON-PHASE-MAP Phase 138 row reconciliation + two CJS suites (the no-harness tripwire + the SEED-003-superseded grep).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-capability-radar-absorption-and-routing/138-CONTEXT.md
@.planning/phases/138-capability-radar-absorption-and-routing/138-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the A4 SUPERSEDE decision + the no-harness tripwire (RAD-06)</name>
  <files>docs/A4-DECISION.md, tests/test-a4-supersede.cjs</files>
  <read_first>
    - 138-RESEARCH.md "A4 Decision Input + Recommendation" (the full SUPERSEDE case: dynamic workflows are a strict superset; ship NO harness, set NO CLAUDE_CODE_FORK_SUBAGENT default; map Canon Part 2 Engine 2 BONO hat-team parallelism onto the dynamic-workflows substrate as the forward path; keep the per-agent mcpServers #8 idea alive ONLY as the separate Bucket-F item), Pitfall 6 (dynamic workflows are a Claude-authored JS orchestration script run by a background runtime, NOT a config flag; record a DECISION + a doc note, do NOT ship a harness or a flag), and the Don't-Hand-Roll table (parallel subagent orchestration -> use Opus 4.8 dynamic workflows)
    - 138-CONTEXT.md "A4 decision (must resolve before any fork-subagent build)" + the a4_reeval LOCKED-decision context (this phase DECIDES adopt-vs-supersede before any fork-subagent harness is built)
    - docs/MINDRIAN-CANON.md Part 2 Engine 2 (BONO orchestration -- the parallel-team substrate the decision maps onto) for the forward-path note
  </read_first>
  <action>
    Create docs/A4-DECISION.md recording the decision as SUPERSEDE (RAD-06, per the research recommendation and the CONTEXT must-honor): Opus 4.8 dynamic workflows (2.1.154; a background JS-runtime orchestrating up to 1,000 subagents / 16 concurrent with a /workflows run browser) are a strict superset of the manual CLAUDE_CODE_FORK_SUBAGENT harness SEED-003 A4 proposed. Record explicitly: ship NO hand-rolled fork-subagent harness; set NO CLAUDE_CODE_FORK_SUBAGENT env default; mark SEED-003 A4 superseded-by Phase 138. Include the forward-path mapping of Canon Part 2 Engine 2 (BONO hat-team parallelism) onto the dynamic-workflows substrate (the mechanism is conversational + /workflows, NOT a flag; Pitfall 6), and note the per-agent mcpServers/agent-field idea survives ONLY as the separate #8 Bucket-F item, not as a reason to keep the harness. No em-dashes. Write tests/test-a4-supersede.cjs (node:assert/strict): assert docs/A4-DECISION.md contains the SUPERSEDE decision, AND run a tripwire grep asserting no FORK_SUBAGENT literal appears in lib/ scripts/ agents/ (docs/ and references/ are allowed to name it).
  </action>
  <verify>
    <automated>node tests/test-a4-supersede.cjs && grep -qi supersede docs/A4-DECISION.md</automated>
  </verify>
  <done>docs/A4-DECISION.md records SUPERSEDE (no fork harness, no FORK_SUBAGENT env default) with the BONO-on-dynamic-workflows forward path; the tripwire proves no FORK_SUBAGENT literal in lib/ scripts/ agents/; the test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Flip SEED-003 to superseded + reconcile the CANON-PHASE-MAP Phase 138 row (RAD-07)</name>
  <files>.planning/seeds/SEED-003-claude-code-2-1-x-capability-adoption.md, docs/CANON-PHASE-MAP.md, tests/test-seed003-superseded.cjs</files>
  <read_first>
    - .planning/seeds/SEED-003-claude-code-2-1-x-capability-adoption.md frontmatter (status: dormant; superseded_by: Phase 138 ... (pending its v1.14.0 ship) -- the frontmatter already anticipates the flip; needs_author_touch notes the A2/A4/A5 carry-forward + the 2.1.148-159 consolidation into Phase 138)
    - 138-RESEARCH.md Reuse-Surface Map SEED-003 row (RAD-07: flip status to superseded and add the forward-pointer to the ledger on ship; the frontmatter already anticipates this) and Phase Requirements RAD-07
    - docs/CANON-PHASE-MAP.md the Phase 138 row in the "v1.13.1 Larry Reaches + reconciliation addendum" table (currently "NOT built (scoped-backlog) | Phase 138 capability-radar-absorption | Part 2 | v1.14.0") and the Version history table (the canon-amendment-on-itself / map-row-update discipline; Part 6 dog-fooding)
    - 138-CONTEXT.md Acceptance criteria (SEED-003 status flipped to superseded-by: Phase 138 once this ships) and canon_parts Part 6 (dog-fooding -- the map names the phase that implements the canon)
  </read_first>
  <action>
    Flip the SEED-003 frontmatter status from dormant to superseded (RAD-07), keep superseded_by: Phase 138, and add a forward-pointer line in the body to data/capability-ledger.json (the living ledger that replaces the dormant one-shot backlog). Update the needs_author_touch line to reflect that A1+A3 shipped, A2 is superseded-by-architecture (Plan 02 audit), A4 is superseded by dynamic workflows (Plan 04 / docs/A4-DECISION.md), and A5 landed as the .zip beta-channel doc note (Plan 02). Reconcile the docs/CANON-PHASE-MAP.md Phase 138 row from "NOT built (scoped-backlog)" to shipped with the canon_parts already declared (Part 2 Engine 2, Part 6, Part 7, Part 8) and a one-line note: radar reader-to-router; living ledger + radar-router + radar_findings drift check; Bucket R/C retrofits; A4 superseded; SEED-003 superseded. Add a Version-history row recording the reconciliation (map rows only, no canon-text change unless a frozen count moved -- none did here). No em-dashes. Write tests/test-seed003-superseded.cjs (node:assert/strict): assert the SEED-003 frontmatter status is superseded and superseded_by names Phase 138, and assert the CANON-PHASE-MAP Phase 138 row no longer reads "NOT built".
  </action>
  <verify>
    <automated>node tests/test-seed003-superseded.cjs && grep -q "status: superseded" .planning/seeds/SEED-003-claude-code-2-1-x-capability-adoption.md</automated>
  </verify>
  <done>SEED-003 status is superseded with superseded-by Phase 138 and a forward-pointer to the ledger; the CANON-PHASE-MAP Phase 138 row is flipped to shipped with the A4-superseded note + a version-history row; the test is green.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full run-all-138.sh phase gate green (RAD-01..08 closure)</name>
  <files>tests/run-all-138.sh</files>
  <read_first>
    - tests/run-all-138.sh (Plan 01; the aggregator listing every 138 suite across all four plans + the SEED-003/FORK_SUBAGENT greps + the Part-8 + SESSION_ID grep sweep)
    - 138-RESEARCH.md "Validation Architecture" Sampling Rate (phase gate: full 138 suite green + build-capability-ledger.cjs --check clean + the Part-8 sweeps clean before verify-work)
  </read_first>
  <action>
    With all four plans landed, run tests/run-all-138.sh and confirm every suite that was previously a missing-file FAIL line is now green: the Plan 01 ledger/router/drift suites, the Plan 02 session-scoping/usage-telemetry/part8-leak suites, the Plan 03 sessionstart-title/skill-scoping suites, and the Plan 04 a4-supersede/seed003-superseded suites, plus the build-capability-ledger.cjs --check tripwire, the SEED-003-superseded grep, the no-FORK_SUBAGENT-literal grep, and the standalone Part-8 + SESSION_ID grep sweep. Fix any newly-revealed integration gap (a suite not registered, a stale ledger). Do not weaken any assertion to make it pass. This is the RAD-01..08 closure gate.
  </action>
  <verify>
    <automated>bash tests/run-all-138.sh</automated>
  </verify>
  <done>bash tests/run-all-138.sh is fully green: every 138 suite + the --check tripwire + the SEED-003/FORK_SUBAGENT greps + the Part-8 + SESSION_ID grep sweep pass; RAD-01..08 are closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| A4 decision -> shipped code | The decision must be load-bearing: no fork-subagent harness or env flag may ship (a code path is the breach, the doc is the record) |
| SEED-003 status -> downstream consumers | Flipping to superseded forward-points every reader to the living ledger; a stale dormant status reopens the rot this phase closes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-04-01 | Tampering | a fork-subagent harness or FORK_SUBAGENT env default shipping despite the SUPERSEDE decision | mitigate | the no-FORK_SUBAGENT-literal tripwire (test-a4-supersede.cjs) over lib/ scripts/ agents/; the decision is recorded in docs/A4-DECISION.md |
| T-138-04-02 | Repudiation | the A4 decision unrecorded, reopening the build-the-harness question | mitigate | docs/A4-DECISION.md is the durable record; the CANON-PHASE-MAP row + version-history entry make it auditable (Part 6 dog-fooding) |
| T-138-04-03 | Information Disclosure | dynamic-workflows adoption note leaking user content | accept | the A4 doc is a methodology/architecture note with no user content; no Brain/network path |
| T-138-04-SC | Tampering | npm/pip/cargo installs | mitigate | zero installs (docs + a seed frontmatter flip + a canon-map row + tests); no package surface |
</threat_model>

<verification>
- node tests/test-a4-supersede.cjs (SUPERSEDE recorded; no FORK_SUBAGENT literal in lib/ scripts/ agents/)
- node tests/test-seed003-superseded.cjs (SEED-003 status superseded; CANON-PHASE-MAP row no longer NOT built)
- bash tests/run-all-138.sh (the full RAD-01..08 phase gate green)
</verification>

<success_criteria>
- A4 decision recorded as SUPERSEDE; no fork-subagent harness, no FORK_SUBAGENT env default (RAD-06)
- SEED-003 flipped to superseded-by Phase 138, forward-pointing to the ledger (RAD-07)
- the CANON-PHASE-MAP Phase 138 row reconciled from NOT built to shipped with the A4-superseded note
- the full run-all-138.sh phase gate is green (RAD-01..08 closure)
</success_criteria>

<output>
Create `.planning/phases/138-capability-radar-absorption-and-routing/138-04-SUMMARY.md` when done. Update STATE.md (A4 = SUPERSEDED; SEED-003 = superseded-by Phase 138) and the ROADMAP Phase 138 Status to COMPLETE with the plan rows ticked.
</output>
