---
phase: 138-capability-radar-absorption-and-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/capability-ledger.json
  - scripts/build-capability-ledger.cjs
  - lib/workflow/radar-router.cjs
  - scripts/radar-findings-drift.cjs
  - docs/RADAR-FINDINGS.md
  - commands/radar.md
  - tests/test-capability-ledger-check.cjs
  - tests/test-radar-router.cjs
  - tests/test-radar-findings-drift.cjs
  - tests/run-all-138.sh
autonomous: true
requirements: [RAD-01, RAD-02, RAD-03]
must_haves:
  truths:
    - "data/capability-ledger.json is a generated artifact regenerated from the 138-CONTEXT ledger table; build-capability-ledger.cjs --check exits non-zero when the on-disk JSON is stale"
    - "the radar-router returns the Bucket-F findings tagged for a given phase number or slug-keyword and degrades to an empty result on a missing/malformed ledger, never fabricating"
    - "the radar_findings: frontmatter contract is documented (mirror of canon_parts:) and a PostToolUse drift check warns, advisory-only and always exit 0, when a CONTEXT.md touches a forward-mapped surface without the relevant radar_findings: row"
    - "/mos:radar --fetch appends a ledger row (router-writes-the-ledger), not only the changelog cache; the CLAUDE_CODE_SESSION_ID row is corrected to 2.1.154"
    - "tests/run-all-138.sh is the single phase-gate aggregator listing every 138 suite plus a Part-8 grep sweep, cloning the run-all-1433.sh structure"
  artifacts:
    - path: "data/capability-ledger.json"
      provides: "The generated machine-readable capability ledger (findings + forward_map), sibling of data/command-registry.json"
      contains: "forward_map"
    - path: "lib/workflow/radar-router.cjs"
      provides: "Read-only sibling of command-resolver.cjs: findingsForPhase(phaseOrSlug) with degrade-to-empty"
      contains: "findingsForPhase"
    - path: "scripts/build-capability-ledger.cjs"
      provides: "Generator + --check tripwire regenerating the ledger from the CONTEXT table"
      contains: "--check"
    - path: "scripts/radar-findings-drift.cjs"
      provides: "Advisory PostToolUse drift hook (JSONL offense log, always exit 0)"
      contains: "hookSpecificOutput"
    - path: "docs/RADAR-FINDINGS.md"
      provides: "The radar_findings: frontmatter contract spec (mirror of canon_parts:)"
      min_lines: 30
    - path: "tests/run-all-138.sh"
      provides: "The RAD-01..08 phase-gate aggregator cloning run-all-1433.sh"
      min_lines: 40
  key_links:
    - from: "lib/workflow/radar-router.cjs"
      to: "data/capability-ledger.json"
      via: "router reads the generated ledger the way command-resolver reads the registry"
      pattern: "capability-ledger"
    - from: "scripts/build-capability-ledger.cjs"
      to: "data/capability-ledger.json"
      via: "--check regenerates in memory and exits 1 on stale on-disk JSON"
      pattern: "capability-ledger"
    - from: "commands/radar.md"
      to: "data/capability-ledger.json"
      via: "--fetch appends a ledger row, not only the changelog cache"
      pattern: "capability-ledger|ledger"
---

<objective>
Build the net-new mechanism that turns /mos:radar from a READER into a ROUTER: the generated capability ledger (RAD-01), the read-only radar-router that surfaces phase-tagged Bucket-F findings at plan time (RAD-02), and the radar_findings: frontmatter contract plus its advisory drift check (RAD-03). This is the ~15% net-new of Phase 138; every piece copies an existing in-repo contract (the Phase 122 resolver, the build-command-registry --check tripwire, the frontmatter-schema-validator advisory hook). This plan also lands the run-all-138.sh aggregator that every later 138 plan registers its suites into.

Purpose: The ledger is the single parked source of truth; the router stops a future v1.14.0 consumer phase (133/134/135/136) from being planned blind; the drift check keeps the radar_findings: contract honest without blocking the navigator. Per Canon Part 7 these are repoints of the resolver/generator/validator contracts, not new dispatchers.
Output: data/capability-ledger.json + scripts/build-capability-ledger.cjs (--check) + lib/workflow/radar-router.cjs + scripts/radar-findings-drift.cjs + docs/RADAR-FINDINGS.md + the commands/radar.md ledger-append repoint + three CJS suites + tests/run-all-138.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-capability-radar-absorption-and-routing/138-CONTEXT.md
@.planning/phases/138-capability-radar-absorption-and-routing/138-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate the capability ledger and its --check tripwire (RAD-01)</name>
  <files>data/capability-ledger.json, scripts/build-capability-ledger.cjs, tests/test-capability-ledger-check.cjs</files>
  <read_first>
    - scripts/build-command-registry.cjs lines 254-366 (the --check pattern: regenerate in memory, byte-compare against the on-disk JSON, exit 1 on stale, print the one-line recovery command; the process.argv routing at line 336; the no-top-level-brain-client discipline at line 274)
    - data/command-registry.json (the generated-artifact layout convention the ledger mirrors: a top-level object with deterministic key order)
    - data/connector-registry.json (the hand-maintained-source + generated-artifact sibling precedent)
    - 138-CONTEXT.md "The capability ledger (Claude Code 2.1.148 -> 2.1.159)" table (the 14-row authoritative source the generator reads) and "The three buckets" (R/C/F membership) and "The mechanism" (the radar_findings: + router + drift roles)
    - 138-RESEARCH.md "Pattern 2: Generator + --check tripwire", "Capability Currency Check" (the #2 SESSION_ID correction to 2.1.154 with the three reaffirming versions 2.1.154/2.1.157/2.1.163), and Assumption A2 (JSON-generated is the chosen default)
  </read_first>
  <behavior>
    - --check exits 0 when data/capability-ledger.json matches the regenerated-in-memory ledger
    - --check exits 1 and prints the recovery command when the on-disk JSON is stale
    - the ledger carries every Bucket-R/C/F finding with version, leverage, destination, status, and a forward_map keyed by phase/slug for Bucket-F items
    - the SESSION_ID-to-MCP row reads 2.1.154 (NOT 2.1.153), per the research correction
  </behavior>
  <action>
    Create scripts/build-capability-ledger.cjs by cloning the scripts/build-command-registry.cjs --check pattern. The authoritative SOURCE is the 138-CONTEXT.md ledger table (the 14 rows); the generator parses that table (or carries the rows as a structured in-script source array seeded from the table) and emits data/capability-ledger.json with two top-level keys: findings[] (one object per ledger row: id, capability, version, leverage, destination, bucket in {R,C,F}, status in {dormant,adopting,shipped,superseded}) and forward_map{} (phase-number-or-slug-keyword -> [finding ids], populated from the Bucket-F destinations: #1->[133,134,135,136,research-lens]; #8->[133,134,135,bono]; #9->[136,121-telemetry]). Default the ledger format to JSON-generated (resolves research Open Question b, matching the registry/connector-registry precedent). Correct the CLAUDE_CODE_SESSION_ID row to version 2.1.154 (resolves Pitfall 2) and record the three reaffirming versions in that row's note. Wire --check exactly like build-command-registry.cjs: regenerate in memory, byte-compare against the on-disk JSON, exit 1 on drift with a one-line `node scripts/build-capability-ledger.cjs` recovery hint, exit 0 when fresh. The generator makes ZERO Brain calls and has no write-Cypher (Part 8). Run the generator once to write data/capability-ledger.json and commit it as a generated artifact (like data/command-registry.json). Then write tests/test-capability-ledger-check.cjs (node:assert/strict) asserting: the on-disk ledger is non-stale (--check exits 0), a mutated copy is detected as stale, the SESSION_ID row is 2.1.154, and every Bucket-F finding appears in forward_map.
  </action>
  <verify>
    <automated>node scripts/build-capability-ledger.cjs --check && node -e "const l=require('./data/capability-ledger.json'); const s=l.findings.find(f=>/SESSION_ID/i.test(f.id+f.capability)); if(!s||!/2\.1\.154/.test(s.version)) throw new Error('SESSION_ID not 2.1.154'); if(!l.forward_map||!Object.keys(l.forward_map).length) throw new Error('forward_map empty'); console.log('PASS ledger');" && node tests/test-capability-ledger-check.cjs</automated>
  </verify>
  <done>data/capability-ledger.json is generated from the CONTEXT table with findings[] + forward_map{}; --check passes clean and trips on staleness; the SESSION_ID row reads 2.1.154; test-capability-ledger-check.cjs green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the read-only radar-router and the radar_findings drift hook (RAD-02, RAD-03)</name>
  <files>lib/workflow/radar-router.cjs, scripts/radar-findings-drift.cjs, docs/RADAR-FINDINGS.md, tests/test-radar-router.cjs, tests/test-radar-findings-drift.cjs</files>
  <read_first>
    - lib/workflow/command-resolver.cjs (the contract to copy: EMPTY_REGISTRY frozen degrade shape, the per-process _load() cache, JSON.parse-in-try/catch-to-empty, the MINDRIAN_COMMAND_REGISTRY env override for test fixtures, zero Brain calls)
    - lib/brain/chain-recommender.cjs (the names-and-enums-only, degrade-to-[seed], never-fabricate traversal discipline the forward_map lookup mirrors)
    - scripts/frontmatter-schema-validator.cjs (the advisory PostToolUse precedent: parse YAML frontmatter, append a JSONL offense row to ${CLAUDE_PLUGIN_DATA}/schema-violations.jsonl, emit hookSpecificOutput.additionalContext one-line advisory, ALWAYS exit 0)
    - docs/CANON-PHASE-MAP.md "Forward-compatibility rule" + .planning/phases/88.2-uiux-selector-block/88.2-CONTEXT.md frontmatter (the canon_parts: declaration pattern radar_findings: mirrors)
    - 138-RESEARCH.md "Pattern 1: Read-only sibling resolver", "Pattern 3: Advisory PostToolUse drift hook", "Code Examples" (the router degrade contract + the advisory envelope), and the canon_parts drift-check precedent note (there is NO heavy canon_parts engine to inherit; the real precedent is the validator + --check tripwire)
  </read_first>
  <behavior>
    - findingsForPhase(N) returns the forward_map[N] finding objects; findingsForPhase('some-slug') matches slug-keywords; an unknown phase returns []
    - a missing or malformed data/capability-ledger.json degrades findingsForPhase to [] (never throws, never fabricates)
    - the drift hook, fed a CONTEXT.md that touches a forward-mapped surface but omits the relevant radar_findings: row, emits a one-line advisory and appends a JSONL offense row, and exits 0
    - the drift hook fed a CONTEXT.md with the matching radar_findings: row emits no advisory and exits 0
  </behavior>
  <action>
    Create lib/workflow/radar-router.cjs as a read-only sibling of command-resolver.cjs: an EMPTY_LEDGER frozen constant ({findings:[], forward_map:{}}), a per-process _load() that JSON.parse's data/capability-ledger.json in a try/catch degrading to EMPTY_LEDGER, a MINDRIAN_CAPABILITY_LEDGER env override for test fixtures, and findingsForPhase(phaseNumberOrSlug) that looks up forward_map by exact phase key first then by slug-keyword match, returning the matched finding objects or [] on miss. ZERO Brain calls; never fabricates a finding not in the ledger. Export findingsForPhase (+ a thin allFindings() helper for the drift hook). This is the RAD-02 router the /gsd:plan-phase flow consults; note in a header comment that planning is a CLI/GSD activity (no Desktop/Cowork surface per the Architectural Responsibility Map).

    Create docs/RADAR-FINDINGS.md specifying the radar_findings: CONTEXT frontmatter contract: it is a list (mirror canon_parts:), each entry is a ledger finding id the phase considered; default empty; the router fills the candidate list at plan time; the drift check warns when a phase touches a forward-mapped surface without the relevant row. Document the closed bucket vocabulary {R,C,F} and that the contract is advisory, not blocking.

    Create scripts/radar-findings-drift.cjs by cloning scripts/frontmatter-schema-validator.cjs: a PostToolUse hook firing on Write/Edit of a *CONTEXT.md; parse the YAML frontmatter; read the phase number/slug; ask radar-router.findingsForPhase for the forward-mapped findings; if any forward-mapped finding id is absent from the file's radar_findings: list, emit hookSpecificOutput.additionalContext as a one-line advisory ("Phase N touches forward-mapped surface X; consider radar_findings: [id]") AND append a JSONL offense row to ${CLAUDE_PLUGIN_DATA||~/.mindrian}/radar-drift.jsonl with scalar fields only (phase, finding_id, ISO timestamp). ALWAYS process.exit(0). It is CLI-only (hooks), advisory, never blocks.

    Write tests/test-radar-router.cjs (RAD-02: degrade-not-fabricate using the env-override fixture; phase-key and slug-keyword hits; missing-ledger -> []) and tests/test-radar-findings-drift.cjs (RAD-03: missing-row case emits advisory + JSONL row + exit 0; present-row case emits nothing + exit 0). Both node:assert/strict with PASS lines.
  </action>
  <verify>
    <automated>node tests/test-radar-router.cjs && node tests/test-radar-findings-drift.cjs && node -e "const r=require('./lib/workflow/radar-router.cjs'); if(typeof r.findingsForPhase!=='function') throw new Error('findingsForPhase missing'); if(!Array.isArray(r.findingsForPhase('does-not-exist-xyz'))) throw new Error('no degrade'); console.log('PASS router surface');" && grep -q "radar_findings" docs/RADAR-FINDINGS.md && echo "PASS router+drift+contract"</automated>
  </verify>
  <done>radar-router.findingsForPhase returns phase-tagged findings and degrades to [] on a missing/malformed ledger; docs/RADAR-FINDINGS.md specifies the radar_findings: contract; radar-findings-drift.cjs warns advisory-only (JSONL offense log, exit 0) on a missing row; both tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint /mos:radar --fetch to append the ledger and build run-all-138.sh (RAD-01, phase-gate infra)</name>
  <files>commands/radar.md, tests/run-all-138.sh</files>
  <read_first>
    - commands/radar.md (the whole 75-line command; Step 3 "With --fetch Flag" lines 39-49 writes ONLY references/capability-radar/changelog-cache.md today; the allowed-tools block line 8; the Voice Rules)
    - scripts/build-capability-ledger.cjs (Task 1; the generator the --fetch step calls to re-emit the ledger after appending a row)
    - tests/run-all-1433.sh (the full aggregator structure to clone: set -uo pipefail; the --check direct invocation in section (a); the CJS_SUITES loop in (b)/(c) that runs each suite to completion and FAILs-not-crashes on a missing file; the standalone Part-8 grep sweep in (d); the final tally + exit 1 on any failure; no emoji, no em-dashes)
    - tests/run-all-148.sh (the same clone applied to a sibling phase, with suites landing across multiple plans listed up-front)
    - 138-RESEARCH.md "Reuse-Surface Map" commands/radar.md row (RAD-01 extends Step 3 to ALSO append a ledger row -- router-writes-the-ledger) and "Validation Architecture" Wave 0 Gaps list (the exact suite filenames)
  </read_first>
  <action>
    Edit commands/radar.md Step 3 (the --fetch flag): after WebFetching the CHANGELOG and writing references/capability-radar/changelog-cache.md, ADD a step that appends any genuinely-new finding as a row to the 138-CONTEXT ledger table AND re-runs `node scripts/build-capability-ledger.cjs` to regenerate data/capability-ledger.json (router-writes-the-ledger, RAD-01). State that the first --fetch after this ships must re-fetch 2.1.160-169 and correct the CLAUDE_CODE_SESSION_ID row to 2.1.154 (cite 2.1.154/2.1.157/2.1.163). State that the ledger is the single source of truth and the changelog-cache is now a fetch byproduct, not the destination. Keep the Voice Rules and no-em-dash discipline.

    Create tests/run-all-138.sh by cloning tests/run-all-1433.sh: set -uo pipefail, no emoji, no em-dashes. Section (a): the direct CI tripwire `node scripts/build-capability-ledger.cjs --check`. Section (b)/(c): a CJS_SUITES array listing EVERY 138 suite across all four plans so the phase gate is one command -- test-capability-ledger-check, test-radar-router, test-radar-findings-drift (Plan 01); test-radar-session-scoping, test-radar-usage-telemetry, test-radar-part8-leak (Plan 02); test-sessionstart-room-title, test-radar-skill-scoping (Plan 03); test-a4-supersede, test-seed003-superseded (Plan 04) -- plus the SEED-003-superseded grep and the no-FORK_SUBAGENT-literal grep as direct invocations. A suite not yet created MUST produce a FAIL line (not a crash), so the gate flags incompleteness until every plan lands. Section (d): a standalone Part-8 grep sweep over the new 138 artifacts (data/capability-ledger.json + the new lib/scripts) for forbidden user-content-to-Brain tokens (projectText|safeNodeProjection|createHash outside an allowed path) and free-text body fields ("(summary|content|body|text|note|description)" heading toward a Brain packet), AND a CLAUDE_CODE_SESSION_ID-near-brain grep (the Part-8 tripwire: SESSION_ID must never appear next to a brain-client query or packet builder). Run to completion, print per-suite PASS/FAIL + a final tally, exit 1 on any failure.
  </action>
  <verify>
    <automated>node tests/test-capability-ledger-check.cjs && node tests/test-radar-router.cjs && node tests/test-radar-findings-drift.cjs && bash -n tests/run-all-138.sh && grep -q "build-capability-ledger.cjs --check" tests/run-all-138.sh && grep -q "CLAUDE_CODE_SESSION_ID" tests/run-all-138.sh && grep -q "capability-ledger" commands/radar.md && ! grep -P "\xE2\x80\x94" tests/run-all-138.sh && echo "PASS radar-repoint+aggregator"</automated>
  </verify>
  <done>commands/radar.md --fetch appends the ledger and regenerates the JSON (RAD-01); tests/run-all-138.sh parses, runs the --check tripwire, lists every 138 suite (missing-file FAILs not crashes), includes the SEED-003/FORK_SUBAGENT greps and the Part-8 + SESSION_ID grep sweep; no em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebFetch'd CHANGELOG -> ledger JSON | Untrusted external text crosses into a committed generated artifact at --fetch time |
| CONTEXT.md frontmatter -> drift hook | Untrusted file content parsed by the advisory PostToolUse hook |
| ledger -> radar-router -> planner context | The router injects findings into planning; must never fabricate a finding not in the ledger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-01-01 | Tampering | WebFetch'd CHANGELOG injecting malformed/hostile content into the ledger JSON | mitigate | the generator validates the row shape before writing; --check byte-compares against the deterministic regeneration; the ledger is plugin-local + committed (reviewable) |
| T-138-01-02 | Spoofing | radar-router fabricating a finding not in the ledger | mitigate | EMPTY_LEDGER degrade + names-and-enums-only lookup (copy of chain-recommender discipline); test-radar-router asserts unknown phase -> [] and never invents |
| T-138-01-03 | Denial of Service | drift hook blocking the navigator's CONTEXT.md write | mitigate | advisory-only, ALWAYS exit 0 (the frontmatter-schema-validator precedent); JSONL offense log, never a block |
| T-138-01-04 | Information Disclosure | radar-router or generator making a Brain call | mitigate | both are read-only local; zero Brain calls; the run-all-138.sh Part-8 grep sweep covers the new artifacts |
| T-138-01-SC | Tampering | npm/pip/cargo installs | mitigate | zero installs (one generated JSON + CJS modules + a markdown command edit + tests); no package surface; N/A per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- node scripts/build-capability-ledger.cjs --check (clean; trips on staleness)
- node tests/test-capability-ledger-check.cjs (ledger fresh; SESSION_ID 2.1.154; forward_map covers Bucket-F)
- node tests/test-radar-router.cjs (degrade-not-fabricate; phase + slug hits)
- node tests/test-radar-findings-drift.cjs (advisory, exit 0, JSONL offense)
- bash -n tests/run-all-138.sh + the grep assertions + no em-dashes
</verification>

<success_criteria>
- data/capability-ledger.json generated from the CONTEXT table (findings[] + forward_map{}); --check is the drift tripwire; SESSION_ID row corrected to 2.1.154 (RAD-01)
- lib/workflow/radar-router.cjs returns phase-tagged Bucket-F findings, degrades to [] on missing/malformed ledger, never fabricates (RAD-02)
- docs/RADAR-FINDINGS.md specifies the radar_findings: contract; scripts/radar-findings-drift.cjs is the advisory drift check (JSONL offense, exit 0) (RAD-03)
- commands/radar.md --fetch appends the ledger (router-writes-the-ledger), not only the cache (RAD-01)
- tests/run-all-138.sh is the phase-gate aggregator listing every 138 suite + Part-8 + SESSION_ID grep sweep; no em-dashes
</success_criteria>

<output>
Create `.planning/phases/138-capability-radar-absorption-and-routing/138-01-SUMMARY.md` when done. Update STATE.md (Decisions: ledger=JSON-generated default; SESSION_ID corrected to 2.1.154) and tick the ROADMAP Phase 138 plan row for 138-01.
</output>
