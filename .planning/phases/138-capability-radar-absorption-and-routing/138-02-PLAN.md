---
phase: 138-capability-radar-absorption-and-routing
plan: 02
type: execute
wave: 2
depends_on: ["138-01"]
files_modified:
  - lib/core/navigation/session-scope.cjs
  - lib/core/navigation.cjs
  - scripts/usage-by-category-telemetry.cjs
  - docs/RADAR-BUCKET-R.md
  - docs/A2-AUDIT.md
  - .claude/includes/release-process.md
  - tests/test-radar-session-scoping.cjs
  - tests/test-radar-usage-telemetry.cjs
  - tests/test-radar-part8-leak.cjs
autonomous: true
requirements: [RAD-04, RAD-08]
must_haves:
  truths:
    - "CLAUDE_CODE_SESSION_ID is read ONLY as a LOCAL filter key behind the navigation.cjs chokepoint; it is presence-guarded and degrades to non-scoped enumeration when absent (CLI yes, Desktop yes, Cowork confirm-at-consumer-phase)"
    - "an adversarial Part-8 test proves CLAUDE_CODE_SESSION_ID never appears in any Brain query payload or packet on any new path (mirror of test-navigation-packet-part8-leak.cjs)"
    - "per-category usage cost surfaces into a LOCAL ~/.mindrian/telemetry/usage-by-category.jsonl with scalar-only rows and zero network surface"
    - "A2 (SEED-003 hooks-as-MCP-callers collapse) is documented as superseded-by-architecture, not refactored; no hook is rewritten; Phase 89.5 fixtures stay green untouched"
    - "the Opus 4.8 model-floor note and the .zip beta-channel note are recorded in release-process docs"
  artifacts:
    - path: "lib/core/navigation/session-scope.cjs"
      provides: "The presence-guarded read-only session-id filter-key consumer behind the navigation chokepoint (RAD-04 #2, RAD-08)"
      contains: "CLAUDE_CODE_SESSION_ID"
    - path: "scripts/usage-by-category-telemetry.cjs"
      provides: "LOCAL per-category cost JSONL writer (scalar counts only, zero egress)"
      contains: "usage-by-category"
    - path: "docs/A2-AUDIT.md"
      provides: "The A2 audit-and-document record (superseded-by-architecture; no live hook target)"
      min_lines: 20
    - path: "tests/test-radar-part8-leak.cjs"
      provides: "Adversarial forbidden-substring proof that SESSION_ID never reaches a Brain payload (RAD-08)"
      contains: "CLAUDE_CODE_SESSION_ID"
  key_links:
    - from: "lib/core/navigation/session-scope.cjs"
      to: "lib/core/navigation.cjs"
      via: "the session-scope consumer reads/writes ONLY through the chokepoint, never a new SQL reader"
      pattern: "navigation"
    - from: "scripts/usage-by-category-telemetry.cjs"
      to: "~/.mindrian/telemetry/usage-by-category.jsonl"
      via: "LOCAL JSONL append of scalar cost-by-category rows (zero network)"
      pattern: "telemetry"
---

<objective>
Land the Bucket R retrofits that are genuinely behind-us (RAD-04) while holding the Part 8 boundary (RAD-08): a presence-guarded read-only CLAUDE_CODE_SESSION_ID LOCAL filter-key consumer behind the navigation.cjs chokepoint, a LOCAL per-category usage-cost JSONL writer, the A2 audit-and-document (NOT a refactor, it has no live target), and the Opus 4.8 model-floor + .zip beta-channel doc notes. The load-bearing piece is the adversarial Part-8 proof that SESSION_ID is read-only LOCAL enumeration and never egresses.

Purpose: SESSION_ID becomes a per-session LOCAL scoping key (Phase 109 chokepoint consumer), per-category cost gives the free-vs-paid tier instrumentation, and A2 is honestly closed rather than refactored against a topology that Phase 121.5 already changed. Every retrofit wires into an existing chokepoint; none adds a new Brain path.
Output: session-scope.cjs (presence-guarded, chokepoint-only) + the navigation.cjs re-export + usage-by-category-telemetry.cjs + docs/RADAR-BUCKET-R.md (model-floor note) + docs/A2-AUDIT.md + the release-process .zip beta-channel note + three CJS suites (session-scoping, usage-telemetry, the adversarial part8-leak).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-capability-radar-absorption-and-routing/138-CONTEXT.md
@.planning/phases/138-capability-radar-absorption-and-routing/138-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Presence-guarded session-id LOCAL filter-key consumer behind the chokepoint + the adversarial Part-8 proof (RAD-04 #2, RAD-08)</name>
  <files>lib/core/navigation/session-scope.cjs, lib/core/navigation.cjs, tests/test-radar-session-scoping.cjs, tests/test-radar-part8-leak.cjs</files>
  <read_first>
    - lib/core/navigation.cjs (the 13-function chokepoint + the lib/core/navigation/ sub-module pattern; how existing sub-modules like room-context.cjs / focus.cjs are required and re-exported; the "zero non-SQLite reads" acceptance invariant)
    - lib/core/navigation/room-context.cjs (the sub-module idiom: a small addition behind the chokepoint, not a new reader)
    - tests/test-navigation-packet-part8-leak.cjs (the 9-tripwire adversarial pattern to mirror: feed forbidden user strings, assert only generic handles + phase ids + enum scalars survive into the packet; the forbidden-substring sweep)
    - lib/core/rs-egress-prompts.cjs auditQueryString (the default-deny gate already used for the framework egress slot, the model for asserting a token never crosses)
    - 138-RESEARCH.md Pitfall 3 (SESSION_ID is stdio-only: gate the consumer on process.env presence; CLI yes, Desktop yes, Cowork confirm-at-plan-time; degrade gracefully), Pitfall 5 (the load-bearing Part-8 breach + the 4-part proof method), and the Security Domain Known Threat Patterns (SESSION_ID-leaked-into-Brain-payload mitigation)
  </read_first>
  <behavior>
    - sessionScopeKey() returns process.env.CLAUDE_CODE_SESSION_ID when present, and a non-scoped sentinel (falsy, meaning enumerate-all) when absent, with no throw
    - the session-scoped enumeration reads/writes ONLY through navigation.cjs (no direct room.db open); zero non-SQLite reads on the scoping path
    - the adversarial test asserts CLAUDE_CODE_SESSION_ID never appears in any Brain query payload, packet builder input, or Cypher dollar-param across the new path (forbidden-substring sweep plus a fed-forbidden-string round-trip)
  </behavior>
  <action>
    Create lib/core/navigation/session-scope.cjs as a small addition BEHIND the navigation.cjs chokepoint (do NOT add a new SQL reader; reuse the chokepoint per Phase 109). Expose sessionScopeKey() that reads process.env.CLAUDE_CODE_SESSION_ID with a presence guard (Pitfall 3): present resolves to the LOCAL filter key; absent resolves to a non-scoped enumeration sentinel; never throws. Expose enumerateScopedLocal(navApi, opts) that uses sessionScopeKey() ONLY as a LOCAL filter key passed to the existing navigation read functions, never as a Brain query parameter, never in a packet, never in a Cypher dollar-param (Pitfall 5). Re-export the new functions from lib/core/navigation.cjs the way the other sub-modules are surfaced. Add a header comment recording the tri-polar reality: CLI yes, Desktop yes (both stdio), Cowork is confirm-at-consumer-phase (resolves research Open Question a with the presence-guard default).

    Write tests/test-radar-session-scoping.cjs: assert presence resolves to the key, absence resolves to the non-scoped sentinel (delete the env var in-test), and that enumerateScopedLocal goes through the chokepoint (spy or seam: no direct room.db open). Write tests/test-radar-part8-leak.cjs by mirroring tests/test-navigation-packet-part8-leak.cjs: set CLAUDE_CODE_SESSION_ID to a sentinel value, exercise every new path, and assert the sentinel NEVER appears in any Brain payload, packet, or Cypher param; additionally run a forbidden-substring source sweep over session-scope.cjs asserting no brain-client require, no packet-builder import, and no createHash or projectText near the SESSION_ID read. Both node:assert/strict with PASS lines.
  </action>
  <verify>
    <automated>node tests/test-radar-session-scoping.cjs && node tests/test-radar-part8-leak.cjs && grep -q sessionScopeKey lib/core/navigation.cjs</automated>
  </verify>
  <done>session-scope.cjs reads SESSION_ID as a presence-guarded LOCAL filter key behind the chokepoint, degrades to non-scoped when absent, and is re-exported from navigation.cjs; the adversarial part8-leak test proves the sentinel never reaches a Brain payload/packet/Cypher param; both tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LOCAL per-category usage-cost JSONL writer (RAD-04 #10, Part 8 scalar-only)</name>
  <files>scripts/usage-by-category-telemetry.cjs, tests/test-radar-usage-telemetry.cjs</files>
  <read_first>
    - scripts/query-efficiency-telemetry.cjs (the LOCAL JSONL pattern to extend: append to ~/.mindrian/telemetry/*.jsonl; scalar integer counts plus LOCAL slug plus ISO timestamp ONLY; zero network surface; advisory PostToolUse posture)
    - 138-RESEARCH.md Reuse-Surface Map ~/.mindrian/telemetry/*.jsonl writers row (the per-category cost extends this LOCAL pattern: usage-by-category.jsonl with scalar cost-by-category rows; same Part 8 only-scalar-counts-no-egress rule) and the Security Domain per-category-cost-telemetry-egressing-user-content mitigation (scalar integer counts plus enum category labels; zero network surface, grep-asserted)
    - 138-CONTEXT.md ledger row #10 (per-category usage cost; Bucket R; telemetry + free-vs-paid tier-model instrumentation)
  </read_first>
  <behavior>
    - the writer appends one JSONL row per category with a scalar cost/count, an enum category label, a LOCAL room slug, and an ISO timestamp; nothing else
    - the row carries no free-text body, no artifact content, no name, no number that identifies user content; only scalar counts plus enum labels
    - the module has zero network surface (no fetch, no http, no brain-client require)
  </behavior>
  <action>
    Create scripts/usage-by-category-telemetry.cjs by extending the scripts/query-efficiency-telemetry.cjs LOCAL JSONL pattern: append per-category cost rows to ~/.mindrian/telemetry/usage-by-category.jsonl. Each row carries ONLY scalar fields: category (an enum label such as tokens, tools, web, cache), a scalar cost/count, the LOCAL room slug, and an ISO timestamp. NO free-text body, NO artifact content, NO network surface (Part 8: this is the per-category-cost-egressing-user-content threat; mitigate by scalar-only rows and zero egress). Mirror the advisory append-and-continue posture. Write tests/test-radar-usage-telemetry.cjs (node:assert/strict): assert a written row parses as JSON with only the scalar/enum fields, assert no free-text body field is present, and run a source grep asserting the module has no fetch/http/brain-client surface.
  </action>
  <verify>
    <automated>node tests/test-radar-usage-telemetry.cjs && grep -q usage-by-category scripts/usage-by-category-telemetry.cjs</automated>
  </verify>
  <done>usage-by-category-telemetry.cjs appends scalar-only per-category cost rows to a LOCAL JSONL with zero network surface; the test proves scalar/enum-only rows and no fetch/http/brain-client surface.</done>
</task>

<task type="auto">
  <name>Task 3: A2 audit-and-document + Opus 4.8 model-floor note + .zip beta-channel note (RAD-04 docs)</name>
  <files>docs/A2-AUDIT.md, docs/RADAR-BUCKET-R.md, .claude/includes/release-process.md</files>
  <read_first>
    - hooks/hooks.json (the on-disk proof for the A2 audit: every hook is type command routed through run-hook.cmd; ZERO entries spawn node lib/core/brain-client.cjs; type mcp_tool appears nowhere)
    - lib/core/brain-client.cjs (the 76-requirer Brain HTTP chokepoint; lib/script callers, NOT hook-spawn entries; the audit confirms there is nothing to collapse)
    - .claude/includes/release-process.md (the release doc surface for the .zip beta-channel note; the pre-release / beta discipline already documented)
    - 138-RESEARCH.md "Blocker the planner must resolve" + Pitfall 1 (A2 has no live target; re-scope from collapse-the-proxy to audit-and-document; superseded-by-architecture; Phase 89.5 fixtures stay green untouched), ledger row #13 (A5 .zip beta channel, doc-only), and the State-of-the-Art Opus model-floor note (executor_model=opus already points at newest, so the floor resolves automatically)
    - 138-CONTEXT.md "The three buckets" Bucket R (the model-floor note belongs in Bucket R) and the a4_reeval decision context
  </read_first>
  <action>
    Create docs/A2-AUDIT.md recording the A2 re-scope (RAD-04 A2 clause): SEED-003 A2 "collapse the brain-client proxy layer" has NO live hook target -- grep evidence that hooks/hooks.json references brain-client ZERO times, every hook is type command via run-hook.cmd, and type mcp_tool is used nowhere. Conclusion: A2 is superseded-by-architecture (Phase 121.5 SessionStart-coordinator consolidation changed the topology A2 was written against). Document explicitly that NO hook is rewritten and that the Phase 89.5-fixtures-green criterion degrades to "fixtures stay green because nothing changed" -- confirm, do not refactor. This is an AUDIT-AND-DOCUMENT task, NOT a refactor (research Blocker).

    Create docs/RADAR-BUCKET-R.md recording the Opus 4.8 model-floor note: the model floor resolves automatically because executor_model=opus already points at the newest model (no code change needed); record this as a Bucket R closed-by-default item with the State-of-the-Art reference. Add the .zip beta-channel note to .claude/includes/release-process.md (ledger #13 / A5, doc-only): --plugin-dir accepts .zip archives (2.1.128) as a beta side-channel, consistent with the existing beta-gating discipline. No code change; doc notes only. No em-dashes.
  </action>
  <verify>
    <automated>grep -q superseded-by-architecture docs/A2-AUDIT.md && grep -qi model-floor docs/RADAR-BUCKET-R.md && grep -qi zip .claude/includes/release-process.md</automated>
  </verify>
  <done>docs/A2-AUDIT.md records A2 as superseded-by-architecture with grep evidence (no refactor, no hook rewritten, 89.5 fixtures untouched); docs/RADAR-BUCKET-R.md records the Opus 4.8 model-floor note; the .zip beta-channel note lands in release-process.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE_CODE_SESSION_ID -> Brain payload | The load-bearing Part-8 boundary: SESSION_ID is a LOCAL filter key only, never an egress key (Pitfall 5) |
| per-category cost row -> network | Telemetry rows are LOCAL JSONL scalars only; zero network surface |
| platform env (stdio) -> consumer | SESSION_ID arrives only on stdio MCP subprocesses; the consumer must presence-guard (Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-02-01 | Information Disclosure | SESSION_ID leaked into a Brain query payload | mitigate | read-only LOCAL filter-key usage behind navigation.cjs; the adversarial forbidden-substring tripwire (test-radar-part8-leak.cjs) asserts SESSION_ID never appears in any packet/Cypher; mirrors test-navigation-packet-part8-leak.cjs |
| T-138-02-02 | Information Disclosure | per-category cost telemetry egressing user content | mitigate | LOCAL ~/.mindrian/telemetry/usage-by-category.jsonl ONLY; scalar integer counts + enum category labels; zero network surface (grep-asserted) |
| T-138-02-03 | Tampering | A2 refactor against a non-existent hook target | mitigate | A2 re-scoped to audit-and-document (no hook rewritten); the audit grep-proves hooks.json never spawns brain-client; 89.5 fixtures stay green untouched |
| T-138-02-04 | Denial of Service | session-scope consumer throwing when SESSION_ID absent | mitigate | presence-guard degrades to non-scoped enumeration; never throws (Pitfall 3); CLI yes, Desktop yes, Cowork confirm-at-consumer-phase |
| T-138-02-SC | Tampering | npm/pip/cargo installs | mitigate | zero installs (CJS modules + docs + tests only); no package surface; N/A per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- node tests/test-radar-session-scoping.cjs (presence -> key; absence -> non-scoped; chokepoint-only)
- node tests/test-radar-part8-leak.cjs (adversarial: SESSION_ID never in any Brain payload/packet/Cypher param)
- node tests/test-radar-usage-telemetry.cjs (scalar/enum-only rows; zero network surface)
- docs/A2-AUDIT.md records superseded-by-architecture; RADAR-BUCKET-R.md records the model-floor note; release-process.md carries the .zip beta-channel note
</verification>

<success_criteria>
- CLAUDE_CODE_SESSION_ID read-only LOCAL scoping behind the navigation chokepoint, presence-guarded, never egresses (RAD-04, RAD-08)
- the adversarial Part-8 test proves zero SESSION_ID egress on every new path (RAD-08)
- per-category usage cost in a LOCAL scalar-only JSONL with zero network surface (RAD-04)
- A2 audited and documented as superseded-by-architecture (no refactor; 89.5 fixtures green untouched) (RAD-04)
- Opus 4.8 model-floor note + .zip beta-channel note recorded in release docs (RAD-04)
</success_criteria>

<output>
Create `.planning/phases/138-capability-radar-absorption-and-routing/138-02-SUMMARY.md` when done. Register the three new suites in tests/run-all-138.sh (already listed by Plan 01), update STATE.md (A2 = superseded-by-architecture; Cowork SESSION_ID = confirm-at-consumer-phase), and tick the ROADMAP Phase 138 row for 138-02.
</output>
