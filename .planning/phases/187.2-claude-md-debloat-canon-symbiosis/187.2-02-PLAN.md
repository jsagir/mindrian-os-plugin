---
phase: 187.2-claude-md-debloat-canon-symbiosis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/includes/moat.md
  - .claude/includes/architecture.md
  - .claude/includes/release-process.md
  - .claude/includes/decisions.md
autonomous: true
requirements: [CMD-01, CMD-06]
must_haves:
  truths:
    - "The @-included moat.md and architecture.md no longer restate the full canon/Simon narrative; they are lean and point to the Deep-Dive canon"
    - "release-process.md is trimmed to operational essence, not a full duplicated ceremony archive"
    - "decisions.md lists all 16 Key Decisions including #16 (Obsidian Vault Nested Structure)"
    - "The four @-include files together are small enough to keep the fully-expanded CLAUDE.md under the < 4k token budget"
  artifacts:
    - path: ".claude/includes/decisions.md"
      provides: "Complete 16-row Key Decisions table"
      contains: "Obsidian Vault Nested Structure"
    - path: ".claude/includes/moat.md"
      provides: "Lean moat membrane, Deep-Dive pointer to canon"
    - path: ".claude/includes/architecture.md"
      provides: "Lean architecture membrane, Deep-Dive pointer to canon"
    - path: ".claude/includes/release-process.md"
      provides: "Lean release essence with Deep-Dive pointer"
  key_links:
    - from: "CLAUDE.md @-include block"
      to: ".claude/includes/decisions.md"
      via: "@.claude/includes/decisions.md pin"
      pattern: "includes/decisions.md"
---

<objective>
Trim the four hand-maintained @-include files so the fully-expanded CLAUDE.md fits the < 4k token budget, and fix the stale/duplicated decisions include (CMD-06c). These files are pulled into every session by the `@.claude/includes/*` pins in CLAUDE.md (L366-369), so they count fully against the expanded-load budget. They currently duplicate canon/Simon narrative that CMD-01 is moving to load-on-demand - the duplication must come off the hot path too, or demoting the canon pins alone will not reach < 4k.

This is the hand-maintained-region (durable) half of the budget cut; nothing here regenerates. This plan touches NO CLAUDE.md region and NO sentinel source.

Purpose: Get the canon-duplicating archive off the always-loaded path at its include source, and make decisions.md correct and complete.
Output: Four lean @-include files; decisions.md carrying all 16 decisions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/187.2-claude-md-debloat-canon-symbiosis/187.2-CONTEXT.md
@.claude/includes/decisions.md
@.claude/includes/moat.md
@.claude/includes/architecture.md
@.claude/includes/release-process.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete and de-duplicate decisions.md (CMD-06c)</name>
  <read_first>
    - .claude/includes/decisions.md (currently 15 rows, missing #16)
    - CLAUDE.md "## Key Decisions" section (the authoritative 16-item list; row 16 = Obsidian Vault Nested Structure v1.9.7)
  </read_first>
  <action>
    Add the missing decision #16 (Obsidian Vault Nested Structure, v1.9.7) to `.claude/includes/decisions.md` so the include matches the authoritative 16-item Key Decisions list. Condense the #16 rationale to one tight sentence: every artifact in a .mos vault sits in its own named folder (`section/artifact-name/artifact-name.md`); the folder IS the artifact, enabling graph view, per-artifact attachments, and clean wikilink resolution. Keep the existing table shape (`| # | Decision | Rationale |`). Confirm rows 1-15 are present and concise. This include becomes the single source for the Key Decisions content; the inline duplicate in CLAUDE.md is removed in Plan 03, so do not abbreviate below what the membrane needs. No em-dashes.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && grep -q 'Obsidian Vault Nested Structure' .claude/includes/decisions.md && [ "$(grep -cE '^\| *16 ' .claude/includes/decisions.md)" -ge 1 ] && [ "$(grep -cE '^\| *[0-9]+ ' .claude/includes/decisions.md)" -eq 16 ] && ! grep -qP '\x{2014}' .claude/includes/decisions.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - decisions.md contains exactly 16 numbered rows, including a row whose number is 16 and which mentions "Obsidian Vault Nested Structure".
    - Zero em-dash characters.
  </acceptance_criteria>
  <done>decisions.md is complete (16 rows) and is the single authoritative decisions source.</done>
</task>

<task type="auto">
  <name>Task 2: Trim moat.md and architecture.md to lean membranes (supports CMD-01 budget)</name>
  <read_first>
    - .claude/includes/moat.md (currently restates the full moat + MWP mandate)
    - .claude/includes/architecture.md (currently restates the ICM x Simon x Wicked narrative)
    - docs/MINDRIAN-CANON.md Part 7 (reuse) + Appendix B (ICM layers) - the Deep-Dive these should point to, NOT duplicate
  </read_first>
  <action>
    Trim `.claude/includes/moat.md` to a lean membrane: the one-paragraph moat formula (the graph that knows WHEN/WHICH/SEQUENCE is the moat; prompts can be copied) plus the MWP deepening one-liner, plus a single Deep-Dive line pointing to `docs/MOAT-MANDATE.md` and `docs/MWP-SPECIFICATION.md`. Drop the five numbered restatements of the Brain assets (they live in the canon / Brain docs). Target well under 1000 bytes.
    Trim `.claude/includes/architecture.md` to a lean membrane: a compact ICM-Layer-0-to-4 mapping (one short table or 5 bullets) plus the cross-subsystem cascade rule as 4 bullets, plus a single Deep-Dive line pointing to `docs/MINDRIAN-CANON.md` (Appendix B) and `docs/research/LIVE_DATA_ROOM_JTBD_PAPER.md`. Drop the long Simon prose and the watchmaker/perturbation table rows. Target well under 1200 bytes.
    No em-dashes. Keep both as headings + bullets + at most one small table.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && [ "$(wc -c < .claude/includes/moat.md)" -lt 1000 ] && [ "$(wc -c < .claude/includes/architecture.md)" -lt 1200 ] && grep -q 'docs/' .claude/includes/moat.md && grep -q 'docs/' .claude/includes/architecture.md && ! grep -qP '\x{2014}' .claude/includes/moat.md && ! grep -qP '\x{2014}' .claude/includes/architecture.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - moat.md < 1000 bytes and architecture.md < 1200 bytes (from ~1389 / ~1977).
    - Each retains a Deep-Dive pointer (`docs/...`) instead of duplicating the canon body.
    - Zero em-dash characters.
  </acceptance_criteria>
  <done>moat.md and architecture.md are lean membranes pointing to the Deep-Dive canon, not duplicating it.</done>
</task>

<task type="auto">
  <name>Task 3: Trim release-process.md to operational essence (supports CMD-01 budget)</name>
  <read_first>
    - .claude/includes/release-process.md (currently ~8.2KB, the single largest include - the full ceremony archive)
    - CLAUDE.md "## Release Process (MANDATORY)" inline section (the shorter duplicate, removed in Plan 03)
  </read_first>
  <action>
    Trim `.claude/includes/release-process.md` to the operational essence an agent needs every session: the workspace rule (run from `/home/jsagi/dev/MindrianOS-Plugin/`, never the plugin cache), the five-in-sync version-consistency rule (CHANGELOG / plugin.json / package.json / git tag / marketplace ref), the `scripts/release.sh <version>` entry point, and the two-command user upgrade path. Move the long-form detail (vendored node_modules rule, marketplace-pinning rationale, the two-update-channels table, pre-release/beta-gating narrative, the 2026-04-13 incident retelling) to a single Deep-Dive pointer line referencing `docs/autopsies/2026-04-13-wrong-workspace-incident.md` and the full ceremony doc. Target under 3000 bytes (from ~8.2KB). No em-dashes.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && [ "$(wc -c < .claude/includes/release-process.md)" -lt 3000 ] && grep -q 'release.sh' .claude/includes/release-process.md && ! grep -qP '\x{2014}' .claude/includes/release-process.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - release-process.md < 3000 bytes (from ~8224).
    - Retains the workspace rule, the five-surface version-consistency rule, the `release.sh` entry point, and the two-command upgrade path.
    - Zero em-dash characters.
  </acceptance_criteria>
  <done>release-process.md is a lean operational membrane; the archive detail is a Deep-Dive pointer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none added | Docs-only change to hand-maintained include files. No code path, no Brain wire. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187.2-02 | Information disclosure | @-include membranes | accept | Docs-only; Part 8 clean. No LOCAL->BRAIN egress, no secrets, no user data. |

No package-manager installs in this phase, so the package-legitimacy gate is N/A.
</threat_model>

<verification>
- Run the three `<automated>` gates; all must echo OK.
- The combined size of these four includes feeds the Plan 03 < 4k expanded-budget proof.
</verification>

<success_criteria>
- decisions.md complete (16 rows, incl #16).
- moat.md, architecture.md, release-process.md trimmed to lean membranes with Deep-Dive pointers.
- No em-dashes in any edited file.
</success_criteria>

<output>
Create `.planning/phases/187.2-claude-md-debloat-canon-symbiosis/187.2-02-SUMMARY.md` when done.
</output>
