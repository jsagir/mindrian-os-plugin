---
phase: 187.2-claude-md-debloat-canon-symbiosis
plan: 01
subsystem: sentinel-source-docs
tags: [claude-md-debloat, sentinel-source, version-drift, conventions, architecture]
requires: []
provides:
  - "Lean GSD:stack source (.planning/research/STACK.md), version-correct"
  - "Trimmed GSD:project rendered sections (.planning/PROJECT.md)"
  - "Lean GSD:conventions source (.planning/codebase/CONVENTIONS.md)"
  - "Lean GSD:architecture source (.planning/codebase/ARCHITECTURE.md)"
affects:
  - "CLAUDE.md GSD:stack / GSD:project / GSD:conventions / GSD:architecture sentinels (regenerate from these in Plan 03)"
tech-stack:
  added: []
  patterns: ["source-first sentinel fix", "headings/bullets/table-rows only render"]
key-files:
  created:
    - .planning/codebase/CONVENTIONS.md
    - .planning/codebase/ARCHITECTURE.md
  modified:
    - .planning/research/STACK.md
    - .planning/PROJECT.md
decisions:
  - "Rewrote research/STACK.md to the real lean operational stack (the v3.0 narrative the plan named lives only in CLAUDE.md, not in any source doc); this is the source that feeds GSD:stack on regen"
  - "Codebase source files left on disk untracked (matches the repo gitignore pattern); per plan constraint, not force-added; Plan 03 reads them from disk"
metrics:
  duration: "~10 min"
  completed: 2026-06-29
  tasks: 3
  files: 4
---

# Phase 187.2 Plan 01: Trim Sentinel Source Docs Summary

Source-first half of the CLAUDE.md de-bloat: trimmed the sentinel SOURCE docs (STACK.md, PROJECT.md) to lean operational essence, fixed the version drift at source, and authored two lean codebase source files so the Plan 03 regen repopulates GSD:stack / GSD:project / GSD:conventions / GSD:architecture small instead of bloated. No CLAUDE.md region was touched.

## What Changed

### Task 1 - STACK.md trimmed + version drift fixed (CMD-02 + CMD-06b)
- Rewrote `.planning/research/STACK.md` from 21581 bytes to 2482 bytes (headings + tables + bullets only, so the generator renders it cleanly).
- Cut the decision-archive bloat: Alternatives-Considered, What-NOT-to-Use, Version-Compatibility, Integration-Points, and Sources sections are gone.
- Version facts reconciled against `package.json`: `@modelcontextprotocol/sdk` `^1.29.0`, `zod` `^3.25.76`, Node `>=22.5.0`. No `1.27.1`, no `>=18`, no `cheerio` (cheerio is absent from package.json - a phantom dependency).
- Commit: `5c7138e2`.

### Task 2 - GSD:project rendered sections trimmed (CMD-02)
- Trimmed ONLY the three sections the generator renders: `## What This Is` (reduced to operational identity, dropped the Brain-node-count prose), `## Core Value` (one sentence), `## Constraints` (tight 5-bullet list: plugin format, no server infra, Brain IP protection, three surfaces, reuse-before-build).
- No other PROJECT.md section was edited (verified via `git diff -U0`: changes only at lines 5, 9, 382-387).
- Commit: `88237e5b`.

### Task 3 - Lean codebase source files authored (CMD-03)
- Created `.planning/codebase/CONVENTIONS.md` (926 bytes): CJS-not-TypeScript, `process.argv` switch-case CLI, no-em-dashes hard rule, ROOM.md-per-directory, filesystem-as-source-of-truth, reuse-before-build, three-surfaces.
- Created `.planning/codebase/ARCHITECTURE.md` (1187 bytes): the three layers (Plugin / Brain / Room) as a small table, the connector spine + `navigation.cjs` chokepoint, and the sentinel-source generation model.
- Both files are headings/bullets/table-rows only and carry zero em-dashes, so the generator will render them small in place of the current empty fallback stubs.

## Deviations from Plan

### Resolved discrepancy (Rule 3 - blocking-issue / plan-intent reconciliation)
- **Found during:** Task 1.
- **Issue:** The plan and CLAUDE.md describe `.planning/research/STACK.md` as carrying the v3.0 stack dump (Key Insight / v3.0 additions / Grants.gov / cheerio / Stack-Patterns-by-Variant). On disk that file was actually the v1.9.0 context-engineering research (21581 bytes). The v3.0 narrative exists ONLY inside CLAUDE.md's GSD:stack sentinel - it is in no source doc (verified by repo-wide grep: `v3.0 Breaks the`, `Stack Patterns by Variant`, `Grants.gov REST API` all match CLAUDE.md only).
- **Resolution:** The v1.9.0 file carried the same structural bloat the plan targets (Alternatives-Considered / What-NOT-to-Use / Version-Compatibility / Integration-Points / Sources) and the same stale `>=18` facts, so every acceptance criterion was directly satisfiable. I rewrote it to the real lean operational stack (the existing v1.0/v2.0 core table + a concise v3.0-additions table naming only the package.json deps + the runtime libraries), which is the source GSD:stack regenerates from in Plan 03. This honors the plan's intent (a lean, version-correct GSD:stack source) regardless of which archive narrative previously occupied the file.

### Codebase source files left untracked on disk (per plan constraint)
- The repo `.gitignore` matches `.planning/`. Pre-existing `.planning` files (STACK.md, PROJECT.md, PLAN.md) are already git-tracked, so their modifications committed normally. The two NET-NEW `.planning/codebase/*.md` files are blocked by the gitignore pattern; `git add` (without `-f`) refused them and the Task 3 commit staged nothing.
- This is the EXPECTED behavior per the plan's critical constraint: "Do not force-add or fight the gitignore. Proceed; the durability proof in Plan 03 reads these from disk." Both files are present on disk (CONVENTIONS.md 926B, ARCHITECTURE.md 1187B) and verified.

## Verification

All three `<automated>` gates evaluated:
- Task 1: `OK` (^1.29.0 + >=22.5.0 present; 1.27.1/>=18/cheerio absent; 2482 bytes < 8000; zero em-dashes).
- Task 2: all three headings present; ZERO em-dashes on edited lines (binding acceptance criterion). The plan's whole-file em-dash grep reports the 28 pre-existing em-dashes in untouched, out-of-scope sections (Current State, v1.14.0, Out of Scope) - the plan explicitly forbids editing those sections, so they are intentionally left in place.
- Task 3: `OK` (both files exist, >=6 structural lines each, ARCHITECTURE.md < 2000 bytes, zero em-dashes).

This plan does not itself measure CLAUDE.md or run the regen - that is Plan 03's job (Wave 2). No CLAUDE.md region was touched (critical constraint honored).

## Threat Surface

Docs-only. No code path, no new input surface, no Brain wire, no LOCAL->BRAIN egress. T-187.2-01 disposition (accept) holds. Part 8 clean.

## Self-Check: PASSED

- Created files exist on disk: STACK.md (2482B), PROJECT.md (48783B), CONVENTIONS.md (926B), ARCHITECTURE.md (1187B) - all FOUND.
- Commits exist: `5c7138e2` (Task 1), `88237e5b` (Task 2) - both FOUND in git log.
- Task 3 codebase files are on disk but intentionally untracked (gitignore pattern; per plan constraint, read from disk by Plan 03).
