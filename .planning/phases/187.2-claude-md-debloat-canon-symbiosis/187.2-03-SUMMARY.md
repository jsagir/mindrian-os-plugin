---
phase: 187.2-claude-md-debloat-canon-symbiosis
plan: 03
subsystem: project-instructions
tags: [claude-md, canon-symbiosis, context-budget, debloat]
requires: [187.2-01, 187.2-02]
provides: "Lean always-loaded CLAUDE.md membrane: Canon Compliance Core (7 Parts) + Verification + demoted canon links, proven durable against regen and under 4k expanded tokens"
affects: [CLAUDE.md, .claude/includes/*, .planning/research/STACK.md, .planning/PROJECT.md, .planning/codebase/*]
tech-stack:
  added: none
  patterns: [source-first-sentinel-regen, distilled-compliance-core, deep-dive-pointers]
key-files:
  created: []
  modified:
    - CLAUDE.md
    - .claude/includes/architecture.md
    - .claude/includes/moat.md
    - .claude/includes/decisions.md
    - .claude/includes/release-process.md
    - .planning/research/STACK.md (gitignored source)
    - .planning/PROJECT.md (tracked source)
    - .planning/codebase/CONVENTIONS.md (gitignored source)
    - .planning/codebase/ARCHITECTURE.md (gitignored source)
decisions:
  - "Kept the canon honored, not evicted: 7 Compliance Core Parts each carry a working docs/MINDRIAN-CANON.md Deep-Dive link"
  - "Hit the < 4k expanded-token budget by trimming the duplicative conventions/architecture sentinels + the architecture include + the membrane redundancy, leaving the unique doctrine (decisions, release, moat) intact"
  - "Cleaned em-dashes at the PROJECT.md source so the regenerated project sentinel stays hyphen-only"
metrics:
  duration: ~30m
  completed: 2026-06-29
---

# Phase 187.2 Plan 03: CLAUDE.md De-Bloat + Canon Symbiosis Summary

Rewrote the hand-authored region of CLAUDE.md into a lean operational MEMBRANE (Canon Compliance Core + Verification + demoted canon links), regenerated the GSD sentinels from the Plan-01 trimmed sources, and PROVED no re-bloat. The fully-expanded per-session load fell from ~66k tokens to ~3,899 tokens.

## Acceptance Evidence (measured, not asserted)

- **Expanded budget:** `cat CLAUDE.md + the 4 .claude/includes/*.md` = **15,597 bytes (~3,899 tokens)**, under the 16,000-byte / 4k-token gate (margin 403 bytes). CLAUDE.md standalone is 11,367 bytes (~2,841 tokens).
- **No re-bloat:** `generate-claude-md` re-run is idempotent (byte delta = 0); regenerated sentinels carry NO `1.27.1` and NO `cheerio` (version fix lives at the trimmed source); membrane (Canon Compliance Core + Verification) survives every regen.
- **Canon bound:** `docs/MINDRIAN-CANON.md` appears 9 times (>= 7); all 7 Compliance Core Parts present (8 / 3 / 6 / 7 / 9 / 11 / 12), each with a working Deep-Dive link.
- **Clean:** zero em-dash characters across CLAUDE.md and all includes; every `docs/*.md` and `.claude/*.md` reference resolves on disk; only the four `.claude/includes/*` `@`-includes remain (zero `@docs` pins).

## What Changed

- **CMD-01:** Demoted `@docs/MINDRIAN-CANON.md` and `@docs/CANON-PHASE-MAP.md` from `@`-include pins to plain Deep-Dive path links (the ~40k-token single highest-leverage cut).
- **CMD-04:** Authored the `## Canon Compliance Core` (one line per binding Part, each with a docs/MINDRIAN-CANON.md link) high in the durable membrane, above `<!-- GSD:project-start -->`.
- **CMD-05:** Added `## Verification` naming the real post-edit commands (`bash tests/run-all-<phase>.sh`, `scripts/verify-release`, `scripts/release.sh`, the connector/orchestration-projection/render-coverage `--check` gates, `node scripts/doctor.cjs --acceptance`).
- **CMD-06a:** Removed the stale Source-Material "Port Status: TODO" table (the V2/OS assets are ported and shipped).
- **Debloat:** Collapsed the inline Moat / Architecture / Architectural-Evolution / Simon / MWP / Release / Key-Decisions narrative into one-line pointers to their `.claude/includes/*` or Deep-Dive docs.
- **CMD-02 durability proof:** Re-ran `generate-claude-md`; sentinels repopulated small and version-correct (`@modelcontextprotocol/sdk ^1.29.0`, Node >=22.5.0) from the Plan-01 trimmed sources.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Expanded budget overshoot required deeper trims**
- **Found during:** Task 3 (budget measurement).
- **Issue:** After the membrane rewrite + first regen, the expanded load was 20,948 bytes (~5,237 tokens), over the 16,000-byte gate, because the regenerated conventions/architecture sentinels + the four includes + the membrane overlapped.
- **Fix:** Per the plan's explicit fallback ("tighten membrane, shorten includes, fold per CONTEXT discretion, re-run regen, re-measure"), trimmed the duplicative conventions/architecture sentinel sources, the architecture include (it duplicated the architecture sentinel), the membrane Deep-Dive/Modular-References overlap, the Canon Core prose, the QA/RCA reference tail, and lightly compressed the decisions/release/moat includes. Unique doctrine (all 16 decisions, the release five-gate rule, the moat thesis) was preserved.
- **Files modified:** CLAUDE.md, the 4 includes, .planning/research/STACK.md, .planning/codebase/{CONVENTIONS,ARCHITECTURE}.md.
- **Commit:** 90e98e68.

**2. [Rule 1 - Bug] Em-dashes in the PROJECT.md source would re-enter the regenerated project sentinel**
- **Found during:** Task 2 (regen).
- **Issue:** The HARD no-em-dash rule applies to the whole file, but `.planning/PROJECT.md` (the GSD:project source) carried 28 em-dashes that the regen would project into the sentinel.
- **Fix:** Source-first cleanup of all em-dashes in PROJECT.md (hyphens only), so every regen produces an em-dash-clean project sentinel.
- **Commit:** 90e98e68.

## Notes

- `.planning/codebase/` is gitignored, so the trimmed CONVENTIONS.md / ARCHITECTURE.md sources live on disk for the regenerator but are not committed; `.planning/PROJECT.md` and `.planning/research/STACK.md` are tracked and were committed.
- Part 8 clean: docs-only change, no Brain wire, no code path, no new input surface. The Canon Compliance Core RESTATES the Part 8 LOCAL -> BRAIN: NO boundary, strengthening it.

## Self-Check: PASSED
- CLAUDE.md exists and contains `## Canon Compliance Core` + `## Verification`: confirmed.
- Commit 90e98e68 exists in git log: confirmed.
- Expanded budget 15,597 bytes (~3,899 tokens) < 16,000: confirmed.
- 7 Compliance Core Parts (8/3/6/7/9/11/12) present, canon links = 9, em-dashes = 0, broken refs = 0: confirmed.
