---
phase: 187.2-claude-md-debloat-canon-symbiosis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/STACK.md
  - .planning/PROJECT.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
requirements: [CMD-02, CMD-03, CMD-06]
must_haves:
  truths:
    - "The GSD:stack sentinel source no longer carries the v3.0 stack-decision narrative, alternatives tables, or version-compat tables"
    - "STACK.md states MCP SDK ^1.29.0, Node >=22.5.0, and does not list cheerio (matches package.json)"
    - "GSD:project renders from trimmed What This Is / Core Value / Constraints sections"
    - "GSD:conventions and GSD:architecture have lean codebase source files instead of empty fallback stubs"
  artifacts:
    - path: ".planning/research/STACK.md"
      provides: "Lean operational GSD:stack source, version-correct"
      contains: "^1.29.0"
    - path: ".planning/PROJECT.md"
      provides: "Trimmed GSD:project rendered sections"
      contains: "## What This Is"
    - path: ".planning/codebase/CONVENTIONS.md"
      provides: "Lean GSD:conventions source"
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Lean GSD:architecture source"
  key_links:
    - from: ".planning/research/STACK.md"
      to: "CLAUDE.md GSD:stack sentinel"
      via: "gsd-tools generate-claude-md (run in Plan 03)"
      pattern: "GSD:stack-start"
    - from: ".planning/PROJECT.md"
      to: "CLAUDE.md GSD:project sentinel"
      via: "gsd-tools generate-claude-md (run in Plan 03)"
      pattern: "GSD:project-start"
---

<objective>
Trim the SENTINEL SOURCE docs so the regenerated CLAUDE.md sentinel sections (GSD:stack, GSD:project, GSD:conventions, GSD:architecture) repopulate lean instead of bloated. This is the source-first half of the de-bloat: editing CLAUDE.md inside these sentinels is futile because `gsd-tools generate-claude-md` (run in Plan 03) overwrites them from these sources. This plan touches NO CLAUDE.md region.

Purpose: Eliminate the v3.0 stack dump and stale version facts at their source, and give the conventions/architecture sentinels real lean content instead of empty fallback stubs, so the Plan 03 regen + measurement lands under the < 4k token budget durably.
Output: Trimmed `.planning/research/STACK.md` (version-correct), trimmed `.planning/PROJECT.md` rendered sections, and two new lean `.planning/codebase/` source files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/187.2-claude-md-debloat-canon-symbiosis/187.2-CONTEXT.md
@package.json
</context>

<mechanism_note>
The generator (`~/.claude/gsd-core/bin/lib/profile-output.cjs`) resolves each sentinel's source as follows (verified at planning time):
- GSD:stack reads `.planning/codebase/STACK.md` first, else `.planning/research/STACK.md`. Only `.planning/research/STACK.md` exists, so it is the source. It renders ONLY heading lines, table rows (`|`), and bullet lines (`- ` / `* `) - prose paragraphs between are dropped, but the v3.0 narrative is mostly tables and bullets, so it DOES render.
- GSD:project reads `.planning/PROJECT.md` and renders ONLY the H1 title plus the `What This Is`, `Core Value`, and `Constraints` sections. The other ~47KB of PROJECT.md never reaches CLAUDE.md - trim effort must target those three sections.
- GSD:conventions reads ONLY `.planning/codebase/CONVENTIONS.md` (NOT research/). Absent today -> fallback stub. It renders only sub-headings, bullets, and table rows.
- GSD:architecture reads ONLY `.planning/codebase/ARCHITECTURE.md` (NOT research/; the 37KB `.planning/research/ARCHITECTURE.md` is NEVER read by the generator). Absent today -> fallback stub. Renders sub-headings, bullets, table rows, code fences.
</mechanism_note>

<tasks>

<task type="auto">
  <name>Task 1: Trim STACK.md to lean operational essence and fix version drift (CMD-02 + CMD-06b)</name>
  <read_first>
    - .planning/research/STACK.md (the file being trimmed; the v3.0 stack source)
    - package.json (source of truth for versions: @modelcontextprotocol/sdk ^1.29.0, node >=22.5.0, NO cheerio, zod ^3.25.76)
    - ~/.claude/gsd-core/bin/lib/profile-output.cjs lines 332-367 (generateStackSection - confirms only headings/table-rows/bullets render)
  </read_first>
  <action>
    Rewrite `.planning/research/STACK.md` down to a lean operational essence. KEEP: the "Existing Stack (v1.0/v2.0)" core table (Markdown + YAML, JSON, Bash scripts, Neo4j + Brain MCP, Pinecone, Cytoscape, Velma, HSI) and a single concise "v3.0 additions" table naming only the dependencies that are ACTUALLY in package.json (`@modelcontextprotocol/sdk`, `zod`, plus the Node CJS shared-core pattern). CUT: the "Key Insight: v3.0 Breaks the No Dependencies Rule" narrative, all the per-section "Why Recommended" prose blocks, the "Alternatives Considered" table, the "What NOT to Use" table, the "Stack Patterns by Variant" bullets, the "Version Compatibility" table, the "Integration Points" table, and the "Sources" link list. These are decision-archive, not operational essence, and they are what bloat GSD:stack.
    FIX the version drift at the source (CMD-06b): every `@modelcontextprotocol/sdk` reference must read `^1.29.0` (NOT `1.27.1`); every Node engine reference must read `>=22.5.0` (NOT `>=18`); REMOVE `cheerio` entirely (it is absent from package.json - listing it is a phantom dependency). If Grants.gov / node-cron are mentioned, demote them to a one-line "future / not-installed" note or drop them, since they are not in package.json.
    Do NOT use em-dashes anywhere; use hyphens. Keep the result as headings + table rows + bullets so the generator renders it cleanly. Target the file well under 8000 bytes (from ~21.5KB).
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && grep -q '\^1\.29\.0' .planning/research/STACK.md && ! grep -q '1\.27\.1' .planning/research/STACK.md && ! grep -qi 'cheerio' .planning/research/STACK.md && ! grep -q '>=18' .planning/research/STACK.md && grep -q '>=22\.5\.0' .planning/research/STACK.md && [ "$(wc -c < .planning/research/STACK.md)" -lt 8000 ] && ! grep -qP '\x{2014}' .planning/research/STACK.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - STACK.md contains `^1.29.0` and `>=22.5.0`; contains neither `1.27.1`, `>=18`, nor `cheerio`.
    - STACK.md byte count < 8000 (from ~21581).
    - The Alternatives-Considered, What-NOT-to-Use, Version-Compatibility, Integration-Points, and Sources sections are gone.
    - Zero em-dash characters.
  </acceptance_criteria>
  <done>STACK.md is a lean, version-accurate operational stack source that will render a small GSD:stack section on regen.</done>
</task>

<task type="auto">
  <name>Task 2: Trim the three GSD:project-rendered sections in PROJECT.md (CMD-02)</name>
  <read_first>
    - .planning/PROJECT.md (only the `# ` H1, `## What This Is`, `## Core Value`, `## Constraints` sections render into CLAUDE.md)
    - ~/.claude/gsd-core/bin/lib/profile-output.cjs lines 298-330 (generateProjectSection - confirms exactly which sections render)
  </read_first>
  <action>
    In `.planning/PROJECT.md`, trim ONLY the three sections the generator renders into GSD:project: `## What This Is`, `## Core Value`, and `## Constraints`. Reduce `What This Is` to two or three sentences of operational identity (drop the long Brain-node-count prose and the curriculum boilerplate). Reduce `Core Value` to one sentence. Reduce `Constraints` to a tight bullet list (plugin format, no server infra, Brain IP protection, three surfaces, reuse-before-build) - drop the explanatory clauses. Do NOT touch any other PROJECT.md section (they do not render into CLAUDE.md and are out of scope). No em-dashes; hyphens only. Keep any corpus magnitude literals consistent with the CORPUS-STATS source (27,904 nodes / 12,485 vectors) if they remain - do not introduce a new stale set.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && grep -q '## What This Is' .planning/PROJECT.md && grep -q '## Core Value' .planning/PROJECT.md && grep -q '## Constraints' .planning/PROJECT.md && ! grep -qP '\x{2014}' .planning/PROJECT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The `What This Is`, `Core Value`, and `Constraints` headings still exist (the generator needs them) but their bodies are materially shorter.
    - No other PROJECT.md section was edited.
    - Zero em-dash characters on the edited lines.
    - The combined rendered length of these three sections is small enough that GSD:project will be a lean block (proven by the Plan 03 measurement).
  </acceptance_criteria>
  <done>PROJECT.md's three rendered sections are lean; GSD:project will regenerate small.</done>
</task>

<task type="auto">
  <name>Task 3: Author lean codebase CONVENTIONS.md and ARCHITECTURE.md sources (CMD-03)</name>
  <read_first>
    - ~/.claude/gsd-core/bin/lib/profile-output.cjs lines 369-409 (generateConventionsSection + generateArchitectureSection - confirms codebase/ path and the heading/bullet/table/fence-only render filter)
    - CLAUDE.md (the current empty "## Conventions" / "## Architecture" fallback stubs - the cleanliness defect being resolved)
    - .planning/research/ARCHITECTURE.md (reference only for distilling a lean architecture summary; the generator does NOT read this file)
  </read_first>
  <action>
    Resolve the empty GSD:conventions and GSD:architecture stubs by authoring two NEW lean source files (the durable choice: the generator always re-emits these sections, so deleting the blocks from CLAUDE.md is not durable - a real source file is).
    Create `.planning/codebase/CONVENTIONS.md`: a short bullet list of the project's actual operating conventions (CJS not TypeScript; `process.argv` switch-case CLI pattern; no em-dashes hard rule; every directory gets a ROOM.md; filesystem is the source of truth, no DB; reuse-before-build before any net-new surface). Roughly 8-15 bullet/heading lines.
    Create `.planning/codebase/ARCHITECTURE.md`: a short distillation of the three layers (Plugin / Brain / Room), the connector spine + navigation.cjs chokepoint, and the sentinel-source generation model - as headings + bullets + at most one small table. Roughly 12-20 lines.
    Keep BOTH files to headings, bullets, and table rows only (prose paragraphs are dropped by the generator). No em-dashes. Each file should be a few hundred bytes, not KB, so the regenerated sections stay tiny.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && test -f .planning/codebase/CONVENTIONS.md && test -f .planning/codebase/ARCHITECTURE.md && [ "$(grep -cE '^(#|-|\*|\|)' .planning/codebase/CONVENTIONS.md)" -ge 6 ] && [ "$(grep -cE '^(#|-|\*|\|)' .planning/codebase/ARCHITECTURE.md)" -ge 6 ] && [ "$(wc -c < .planning/codebase/ARCHITECTURE.md)" -lt 2000 ] && ! grep -qP '\x{2014}' .planning/codebase/CONVENTIONS.md && ! grep -qP '\x{2014}' .planning/codebase/ARCHITECTURE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both `.planning/codebase/CONVENTIONS.md` and `.planning/codebase/ARCHITECTURE.md` exist and contain only headings/bullets/table-rows (so the generator renders them).
    - ARCHITECTURE.md < 2000 bytes; CONVENTIONS.md similarly lean.
    - Zero em-dash characters.
    - On regen (Plan 03) the GSD:conventions / GSD:architecture sections carry these lean contents instead of the fallback stub text.
  </acceptance_criteria>
  <done>The two empty sentinel stubs now have lean, durable codebase sources; regen will populate them small.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none added | Docs-only change. No code path, no new input surface, no Brain wire. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187.2-01 | Information disclosure | Sentinel source docs | accept | Docs-only; Part 8 clean - no LOCAL->BRAIN egress, no user data, no secrets touched. Edits are planning/source markdown. |

No package-manager installs in this phase, so the package-legitimacy gate is N/A.
</threat_model>

<verification>
- Run the three `<automated>` gates above; all must echo OK.
- These sources feed the Plan 03 regen + the < 4k budget proof - this plan does not itself measure CLAUDE.md.
</verification>

<success_criteria>
- STACK.md is lean and version-accurate (^1.29.0, >=22.5.0, no cheerio).
- PROJECT.md's three rendered sections are trimmed.
- Lean codebase CONVENTIONS.md + ARCHITECTURE.md exist.
- No em-dashes in any edited file.
</success_criteria>

<output>
Create `.planning/phases/187.2-claude-md-debloat-canon-symbiosis/187.2-01-SUMMARY.md` when done.
</output>
