---
phase: 187.2-claude-md-debloat-canon-symbiosis
plan: 03
type: execute
wave: 2
depends_on: [187.2-01, 187.2-02]
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [CMD-01, CMD-04, CMD-05, CMD-06]
must_haves:
  truths:
    - "CLAUDE.md no longer @-pins the two canon docs; they are plain Deep-Dive path links"
    - "CLAUDE.md carries a Canon Compliance Core binding all 7 Parts, each with a docs/MINDRIAN-CANON.md link"
    - "CLAUDE.md carries a Verification section naming the real tests/run-all-*.sh command(s)"
    - "The stale Source-Material Port-Status-TODO table is corrected or removed"
    - "Fully-expanded CLAUDE.md (4 remaining @-includes resolved) loads under 4k tokens"
    - "Re-running gsd-tools generate-claude-md does NOT re-bloat CLAUDE.md"
    - "No broken doc references and no em-dashes in CLAUDE.md"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Operational membrane: Canon Compliance Core + Verification + lean body + demoted canon links"
      contains: "Canon Compliance Core"
  key_links:
    - from: "CLAUDE.md Canon Compliance Core"
      to: "docs/MINDRIAN-CANON.md"
      via: "per-Part Deep-Dive link"
      pattern: "docs/MINDRIAN-CANON.md"
    - from: "CLAUDE.md Verification section"
      to: "tests/run-all-*.sh"
      via: "named post-edit test command"
      pattern: "run-all"
    - from: "CLAUDE.md GSD sentinel sections"
      to: "the trimmed sentinel sources (Plan 01)"
      via: "gsd-tools generate-claude-md regen"
      pattern: "GSD:stack-start"
---

<objective>
Rewrite the hand-maintained (durable, non-sentinel) region of CLAUDE.md into a lean operational MEMBRANE, then regenerate the sentinels from the Plan-01 trimmed sources and PROVE no re-bloat. Closes CMD-01 (demote canon pins), CMD-04 (Canon Compliance Core), CMD-05 (Verification), CMD-06a (stale Source-Material table), and lands the < 4k expanded-token acceptance.

The Canon Compliance Core is the symbiosis keystone: it keeps the binding canon Parts PRESENT in the always-loaded membrane (one line + a Deep-Dive link each) even as the full canon archive moves to load-on-demand. Canon honored, not evicted.

Output: A < 4k-token fully-expanded CLAUDE.md with a working Canon Compliance Core and Verification section, proven durable against regen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/187.2-claude-md-debloat-canon-symbiosis/187.2-CONTEXT.md
@CLAUDE.md
@docs/MINDRIAN-CANON.md
</context>

<region_map>
Two region classes (verified at planning time):
- HAND-AUTHORED / DURABLE: top of file through the `## Modular References (@include)` block. Nothing regenerates this. Task 1 edits here (the stale Source-Material table, the inline Moat/Architecture/Evolution/Simon/MWP/Release/Key-Decisions narrative, the @-include block). The Canon Compliance Core + Verification MUST go here, above `<!-- GSD:project-start -->`.
- GSD SENTINEL-BOUNDED: `<!-- GSD:project-start -->` through `<!-- GSD:profile-end -->`. `generate-claude-md` overwrites these from source docs - hand-editing inside them is futile. Task 2 regenerates them from the Plan-01 sources.
</region_map>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the membrane - demote pins, debloat inline narrative, author Canon Compliance Core + Verification, fix Source-Material table (CMD-01/04/05/06a)</name>
  <read_first>
    - CLAUDE.md (hand-authored region: top through the @-include block; `## Modular References` L366-373; `### Source Material` table L149-167)
    - docs/MINDRIAN-CANON.md (Parts 3, 6, 7, 8, 9, 11, 12 - the 7 the Core binds)
    - .claude/includes/{decisions,moat,architecture,release-process}.md (the lean membranes the inline narrative is replaced by)
    - 187.2-CONTEXT.md (CMD-01/04/05/06; static-first cache-ordering note)
  </read_first>
  <action>
    Edit ONLY the hand-authored region (top through the `## Modular References (@include)` block). Touch nothing inside the GSD sentinel markers.
    (a) CMD-01 demote pins: change `@docs/MINDRIAN-CANON.md` and `@docs/CANON-PHASE-MAP.md` from `@`-includes to plain Deep-Dive PATH LINKS (a "Deep dive" bullet list naming `docs/MINDRIAN-CANON.md` and `docs/CANON-PHASE-MAP.md` without the leading `@`). Keep the four `@.claude/includes/*` pins. Only those four `@`-includes remain after.
    (b) CMD-06a fix the stale `### Source Material` "Port Status: TODO" table (Larry/25-prompts/mode-engine/grading all SHIPPED): delete the table or replace it with one line stating the V2/OS assets are ported and shipped. Remove the TODO rows.
    (c) Debloat the inline narrative (the budget mechanism): remove the inline `## The Moat`, the long `## Architecture` ASCII/plugin-structure dump (reduce to a few lines), the entire `## Architectural Evolution (from Live Data Room Paper)` Simon/ICM prose, `## MWP Moat Awareness`, the inline `## Release Process (MANDATORY)` body, and the inline `## Key Decisions` list. Replace each with a one-line pointer to its @-include or Deep-Dive doc (moat->moat.md; architecture/evolution->architecture.md + the JTBD paper; decisions->decisions.md; release->release-process.md). KEEP lean: title block, WORKSPACE GUARD (rule + incident pointer), `## What Is This?`, `## The Three Layers` table, `## Tri-Polar Design Rule` (condensed).
    (d) CMD-04 Canon Compliance Core: add `## Canon Compliance Core` high in the membrane, ABOVE `<!-- GSD:project-start -->`. One line per Part, each ending in a `docs/MINDRIAN-CANON.md` Deep-Dive link (anchors like `#part-8` are fine): Part 8 LOCAL->BRAIN: NO; Part 3 tri-context Decision Gate; Part 6 dog-fooding (honor own canon); Part 7 reuse before build (search the 25 commands first); Part 9 memory locality (SQL is the local mind, human confirms truth); Part 11 born-wired (WIRED or EXCLUDED, CIRS); Part 12 voice/pedagogy (De Stijl color mark; invisibility governs the insight).
    (e) CMD-05 Verification: add `## Verification` (above the sentinels) naming REAL post-edit commands. Inspect `ls tests/run-all-*.sh`, `scripts/verify-release`, the `--check` gates first, then name: `bash tests/run-all-<phase>.sh`, `scripts/verify-release`, the connector/orchestration-projection/render-coverage `--check` gates, `node scripts/doctor.cjs --acceptance`. State the rule: run the relevant suite after edits before declaring done.
    Apply static-first ordering where free (keep volatile dated detail low, stable membrane high); do NOT reorder sentinel sections. No em-dashes; hyphens only.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && ! grep -q '@docs/MINDRIAN-CANON.md' CLAUDE.md && ! grep -q '@docs/CANON-PHASE-MAP.md' CLAUDE.md && grep -q 'docs/MINDRIAN-CANON.md' CLAUDE.md && grep -q '## Canon Compliance Core' CLAUDE.md && [ "$(grep -c 'docs/MINDRIAN-CANON.md' CLAUDE.md)" -ge 7 ] && grep -q '## Verification' CLAUDE.md && grep -q 'run-all' CLAUDE.md && ! grep -qi 'Port Status' CLAUDE.md && ! grep -qP '\x{2014}' CLAUDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains neither `@docs/MINDRIAN-CANON.md` nor `@docs/CANON-PHASE-MAP.md`, but DOES contain a plain `docs/MINDRIAN-CANON.md` link.
    - `## Canon Compliance Core` heading present with 7 Part lines; `docs/MINDRIAN-CANON.md` appears >= 7 times.
    - `## Verification` heading present naming a `run-all` command.
    - The `Port Status` / TODO table is gone; zero em-dash characters; only the four `.claude/includes/*` `@`-includes remain.
  </acceptance_criteria>
  <done>The hand-authored region is a lean membrane: demoted canon links, Canon Compliance Core (7 Parts), Verification section, no stale TODO table.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate sentinels and prove no re-bloat (mandatory durability proof - CMD-02 source-first verification)</name>
  <read_first>
    - CLAUDE.md (post-Task-1 state; capture byte count before regen)
    - ~/.claude/gsd-core/bin/gsd-tools.cjs (`generate-claude-md`, ~L1192; flags `--auto`/`--force`)
    - .planning/research/STACK.md + .planning/PROJECT.md + .planning/codebase/CONVENTIONS.md + .planning/codebase/ARCHITECTURE.md (the Plan-01 trimmed sources the regen reads)
  </read_first>
  <action>
    From `/home/jsagi/dev/MindrianOS-Plugin`, run `node ~/.claude/gsd-core/bin/gsd-tools.cjs generate-claude-md` (add `--force` if it asks to overwrite). It rewrites the GSD sentinel sections from the Plan-01 trimmed sources and preserves the hand-authored membrane (Canon Compliance Core + Verification live above the sentinels). Capture the CLAUDE.md byte count BEFORE and AFTER. Proof: the after-count stays within +/-5% of before (no re-bloat), the sentinels repopulate small (no v3.0 dump, no empty stubs, version facts correct), and the membrane survives. If the membrane was clobbered it was placed inside a sentinel - move it above `<!-- GSD:project-start -->` and re-run.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && B=$(wc -c < CLAUDE.md) && (node ~/.claude/gsd-core/bin/gsd-tools.cjs generate-claude-md --force >/dev/null 2>&1 || node ~/.claude/gsd-core/bin/gsd-tools.cjs generate-claude-md >/dev/null 2>&1); A=$(wc -c < CLAUDE.md) && grep -q '## Canon Compliance Core' CLAUDE.md && grep -q '## Verification' CLAUDE.md && ! grep -q '1\.27\.1' CLAUDE.md && ! grep -qi 'cheerio' CLAUDE.md && awk -v b="$B" -v a="$A" 'BEGIN{d=(a>b?a-b:b-a); if(d*100<=b*5)print "OK delta="d; else{print "REBLOAT b="b" a="a; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - `generate-claude-md` runs; CLAUDE.md still contains `## Canon Compliance Core` and `## Verification` (membrane survived).
    - Regenerated sentinels carry NO `1.27.1` and NO `cheerio` (the version fix lives at the source).
    - Post-regen byte count within +/-5% of pre-regen (no re-bloat).
  </acceptance_criteria>
  <done>Source-first fix proven: regen rewrites the sentinels from trimmed sources and CLAUDE.md stays small.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the < 4k expanded budget, no broken refs, em-dash-clean (acceptance gate - CMD-01/04/05)</name>
  <read_first>
    - CLAUDE.md (post-regen final state)
    - .claude/includes/{architecture,moat,decisions,release-process}.md (the only remaining @-includes that expand)
    - 187.2-CONTEXT.md acceptance criteria
  </read_first>
  <action>
    Measure the FULLY-EXPANDED load: after CMD-01 only the four `.claude/includes/*` files expand (the canon docs are plain links now). Concatenate CLAUDE.md + those four files; measure combined bytes; token estimate = bytes/4 (audit ratio). Threshold: < 16000 bytes (~4k tokens). If over, trim further (tighten membrane, shorten includes, or fold a small include into the membrane per the CONTEXT discretion), re-run Task 2 regen, re-measure. Then verify NO broken refs: every `docs/*.md` and `.claude/*.md` path in CLAUDE.md exists on disk. Sweep for em-dashes (zero). Confirm the Core binds 7 Parts (`docs/MINDRIAN-CANON.md` >= 7). Record the measured expanded byte/token number in the SUMMARY as acceptance evidence.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && EXP=$(cat CLAUDE.md .claude/includes/architecture.md .claude/includes/moat.md .claude/includes/decisions.md .claude/includes/release-process.md | wc -c) && echo "expanded_bytes=$EXP approx_tokens=$((EXP/4))" && [ "$EXP" -lt 16000 ] && [ "$(grep -c 'docs/MINDRIAN-CANON.md' CLAUDE.md)" -ge 7 ] && ! grep -qP '\x{2014}' CLAUDE.md && MISS=0; for p in $(grep -oE '(docs|\.claude)/[A-Za-z0-9_./-]+\.md' CLAUDE.md | sort -u); do [ -f "$p" ] || { echo "BROKEN: $p"; MISS=1; }; done; [ "$MISS" -eq 0 ] && echo BUDGET_OK</automated>
  </verify>
  <acceptance_criteria>
    - Expanded byte count of CLAUDE.md + the four resolved includes < 16000 bytes (< ~4k tokens), from ~66k.
    - Every `docs/*.md` / `.claude/*.md` path in CLAUDE.md resolves (zero BROKEN lines).
    - `docs/MINDRIAN-CANON.md` appears >= 7 times; zero em-dash characters.
  </acceptance_criteria>
  <done>Acceptance proven: fully-expanded CLAUDE.md < 4k tokens, references intact, canon bound, em-dash-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none added | Docs-only: editing CLAUDE.md + running the local doc generator. No code path, no Brain wire, no new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187.2-03 | Information disclosure | CLAUDE.md membrane | accept | Part 8 clean; no LOCAL->BRAIN egress, no user data, no secrets. The Canon Compliance Core RESTATES the Part 8 boundary, strengthening it. |
| T-187.2-04 | Tampering | generate-claude-md | accept | Existing local GSD tool (no install); rewrites sentinels from local sources only; no network, no package install. |

No package-manager installs in this phase, so the package-legitimacy gate is N/A. VALIDATION.md is absent (Nyquist Dimension 8 warns) - acceptable for a docs-only phase; the acceptance gate is the byte/token measurement + grep checks, not a behavioral suite.
</threat_model>

<verification>
- Task 1: demoted pins, Canon Compliance Core (7 Parts), Verification section, no TODO table, no em-dashes.
- Task 2: regen runs, membrane survives, sentinels version-correct, no re-bloat (+/-5%).
- Task 3: expanded < 16000 bytes, no broken refs, canon bound 7x, em-dash-clean.
</verification>

<success_criteria>
- Fully-expanded CLAUDE.md < 4k tokens (from ~66k).
- Re-running generate-claude-md does not re-bloat.
- Canon Compliance Core binds all 7 Parts with working Deep-Dive links.
- Verification section names real test commands (score 1/5 -> >= 4/5).
- No broken doc references; no em-dashes; Part 8 clean.
</success_criteria>

<output>
Create `.planning/phases/187.2-claude-md-debloat-canon-symbiosis/187.2-03-SUMMARY.md` when done. Record the measured expanded byte/token count as acceptance evidence.
</output>
