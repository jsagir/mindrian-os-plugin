
## Deferred (Plan 03 execution, 2026-07-11)

- **test-hmi-compliance-e2e.cjs test #11 "hooks.json byte-identity for Phase 99/100/103 Stop entries" FAILS** -- pre-existing, out of scope. This asserts byte-identity of `hooks.json` Stop entries; Plan 03 touches only `scripts/doctor.cjs`, `data/doctor-modules.json`, and two doctor tests, never `hooks.json`. Tests 1-10 (the `--ui-compliance` doctor path this plan migrates) all PASS. Not a regression from the F/K/L/N migration.

## Deferred (Plan 07 execution, 2026-07-11)

- **Stray `.planning/phases/217/` directory (containing only `DRIFT.md`) collides with the real phase directory `217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i` in `gsd-tools`'s `findPhaseInternal` token match** -- pre-existing, out of scope (not created by this plan; `git log --diff-filter=A -- .planning/phases/217/DRIFT.md` shows it predates Plan 07). `phaseTokenMatches` matches the literal `217` directory first, so `roadmap.update-plan-progress 217` and any other `findPhaseInternal(cwd, '217')` caller reads `plans: []` / `summaries: []` off the WRONG (empty) directory and reports "No plans found" even though the real phase directory has 7 plans + 7 summaries. Routed around by editing `.planning/ROADMAP.md` directly for the Phase 217 close-out (Task 3 / final commit). NOT fixed here (scope boundary: pre-existing tooling collision unrelated to the D-04 doc audit / doc-parity test / aggregator this plan delivers). A future quick task should either rename/remove the stray `217/` dir (verify nothing else depends on it first) or teach `phaseTokenMatches` to prefer a directory whose name STARTS WITH `<num>-` over a bare numeric directory.
