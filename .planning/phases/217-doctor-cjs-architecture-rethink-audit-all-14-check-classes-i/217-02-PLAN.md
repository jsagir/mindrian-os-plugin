---
phase: 217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i
plan: 02
type: execute
wave: 1
depends_on: ["217-01"]
files_modified:
  - tests/test-doctor-module-contract-parity.cjs
  - tests/test-doctor-card-fire-health.cjs
  - lib/core/doctor/card-fire-health-module.cjs
  - data/doctor-modules.json
  - scripts/doctor.cjs
  - lib/memory/run-feynman-tests.cjs
autonomous: true
requirements: [D-03, D-05]
must_haves:
  truths:
    - "A module registered in data/doctor-modules.json that is missing an explicit boolean fix_supported, missing an explicit cadence, or whose check() returns no detail string FAILS the test suite hard (exit 1), not advisorily (D-03; Part 11 R16's advisory posture is deliberately NOT followed)"
    - "A module declaring fix_supported: true whose runner exports no fix function fails the suite; a module declaring fix_supported: false whose runner DOES export a fix function also fails (silence and contradiction are both structural failures)"
    - "doctor reports the health of scripts/check-card-fire.cjs's own diagnostic instrument: the intercept log's existence, JSONL integrity, and freshness window (D-05), without attempting to resolve the open stale-plugin-cache mystery"
  artifacts:
    - path: "tests/test-doctor-module-contract-parity.cjs"
      provides: "D-03 hard-blocking declaration-completeness gate over every doctor-modules.json entry"
      min_lines: 60
    - path: "lib/core/doctor/card-fire-health-module.cjs"
      provides: "D-05 card-fire instrument health check (check-only runner)"
      exports: ["check"]
      min_lines: 60
    - path: "tests/test-doctor-card-fire-health.cjs"
      provides: "Hermetic fixture tests: absent/valid/malformed/stale log cases"
      min_lines: 50
    - path: "data/doctor-modules.json"
      provides: "card-fire-health registry entry (cadence: always, flag: cardFireHealth, fix_supported: false)"
      contains: "card-fire-health"
  key_links:
    - from: "tests/test-doctor-module-contract-parity.cjs"
      to: "data/doctor-modules.json"
      via: "reads the registry and require()s every runner"
      pattern: "doctor-modules\\.json"
    - from: "lib/core/doctor/card-fire-health-module.cjs"
      to: "~/.mindrian/card-fire-intercepts.log"
      via: "MINDRIAN_HOME-resolved log path (mirrors check-card-fire.cjs interceptLogPath)"
      pattern: "card-fire-intercepts\\.log"
    - from: "scripts/doctor.cjs parseArgs"
      to: "card-fire-health module"
      via: "--card-fire-health flag, also set by --all"
      pattern: "cardFireHealth"
---

<objective>
Wave 0 part 2 of Phase 217: the enforcement mechanism (D-03) and the first brand-new
cadence:always module (D-05). Both land BEFORE any existing check migrates, so:
(1) every migration in Plans 03-06 is born under the hard-blocking contract gate, and
(2) the new engine path (cadence gate, flag gate, spread into report.checks) is
exercised end-to-end by a fresh module before we start moving battle-tested checks.

These are the two VALIDATION.md Wave-0 test files
(tests/test-doctor-module-contract-parity.cjs, tests/test-doctor-card-fire-health.cjs).

Purpose: D-03 (hard-blocking gate, stricter than Part 11 R16 on purpose) and D-05
(apply the 2026-07-11 printer-must-match-counter lesson to the sibling self-diagnostic
surface, check-card-fire.cjs).
Output: the contract-parity test, the card-fire-health module + its test, the registry
entry, the --card-fire-health flag.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-CONTEXT.md
@.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-RESEARCH.md
@.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-PATTERNS.md
@lib/core/doctor/umbilical-module.cjs
@data/doctor-modules.json
</context>

## Artifacts this plan produces

| Artifact | Kind | Symbols |
|----------|------|---------|
| `tests/test-doctor-module-contract-parity.cjs` | NEW file | the D-03 hard gate (exit 1 on any declaration gap) |
| `tests/test-doctor-card-fire-health.cjs` | NEW file | hermetic D-05 fixture tests |
| `lib/core/doctor/card-fire-health-module.cjs` | NEW file | exports: `check`, `STALENESS_WINDOW_MS`, `interceptLogPath` (test-only export) |
| `data/doctor-modules.json` | MODIFIED | new entry `{ id: "card-fire-health", introduced_version: "1.15.3", cadence: "always", flag: "cardFireHealth", fix_supported: false, runner: "lib/core/doctor/card-fire-health-module.cjs", description }` |
| `scripts/doctor.cjs` | MODIFIED | parseArgs: `cardFireHealth: false` init, `--card-fire-health` case, set true in the `--all` block, usageText line |
| `lib/memory/run-feynman-tests.cjs` | MODIFIED | registers both new test files |

<tasks>

<task type="auto">
  <name>Task 1: Create the D-03 hard-blocking module contract-parity test</name>
  <files>tests/test-doctor-module-contract-parity.cjs, lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - data/doctor-modules.json ($schema_note as updated by Plan 01, plus the umbilical entry)
    - lib/core/doctor/umbilical-module.cjs (the reference runner: check/fix exports, return shapes)
    - tests/test-doctor-module-selector.cjs (the hermetic harness convention: 'use strict', node:assert, mkdtempSync scratch home, ok() counter)
    - lib/memory/run-feynman-tests.cjs lines 300-330 (the existing doctor-test registration block, to append alongside)
    - .planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-RESEARCH.md (the "D-03 test design note": the real job is declaration completeness, print parity is already structural via 260711-nrd)
  </read_first>
  <action>
    Implements D-03. This test is HARD-BLOCKING: any violation enumerates to stderr and
    the process exits 1. Do NOT copy Part 11 R16's advisory-by-default posture; the
    CONTEXT explicitly rules that precedent out for doctor.cjs.

    For EVERY entry in data/doctor-modules.json `modules[]`, assert:
    1. `id` is a non-empty kebab-case string (`/^[a-z0-9]+(-[a-z0-9]+)*$/`) and unique
       across the registry.
    2. `introduced_version` is valid semver: `require('semver').valid(mod.introduced_version) !== null`
       (prerelease labels like 1.13.1-beta.4 must pass).
    3. `cadence` is EXACTLY 'always' or 'once', explicitly present. Absence fails (the
       engine's back-compat default exists only so an interrupted migration cannot brick
       doctor; the registry file itself must be explicit).
    4. The `flag` property is present and is either a string or null ('flag' in mod).
    5. `typeof mod.fix_supported === 'boolean'`. Absence or non-boolean fails - D-03:
       silence is not an acceptable no-fix declaration.
    6. `mod.runner` resolves to an existing file relative to the repo root
       (path.resolve(__dirname, '..', mod.runner)).
    7. `require(runner)` succeeds and `typeof exports.check === 'function'`.
    8. Two-way fix declaration parity: if `fix_supported === true` then
       `typeof exports.fix === 'function'`; if `fix_supported === false` then
       `typeof exports.fix === 'undefined'` (an exported fix behind a false declaration
       is an undeclared remediation - fail it).
    9. Invoke `exports.check({ home: <mkdtemp scratch>, running: '99.99.99', dryRun: true, fix: false, flags: {} })`
       inside try/catch: it must NOT throw, must return an object whose `status` is one
       of 'ok'|'warn'|'error'|'skip', and whose `detail` is a non-empty string (the
       generic renderer's row body - this is the print-line half of D-03). If
       `action_lines` is present it must be an array of strings.
       NOTE: checks may read real machine state (e.g. plugin-enabled reads
       ~/.claude/settings.json); the test asserts vocabulary + detail, never a specific
       ok/warn outcome, so it stays machine-independent.

    Harness: 'use strict', node:assert/strict, fs.mkdtempSync scratch, per-violation
    console.error lines naming module id + rule number, final PASS/FAIL counts,
    process.exit(1) on any failure. No network. No em-dashes in any string.

    Register the file in lib/memory/run-feynman-tests.cjs next to the existing
    test-doctor-* entries (also pre-register tests/test-doctor-card-fire-health.cjs,
    created in Task 2 of this same plan, so both Wave-0 files ride one registration
    edit).
  </action>
  <verify>
    <automated>node tests/test-doctor-module-contract-parity.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-doctor-module-contract-parity.cjs` exits 0 against the current registry (umbilical passes all 9 rules; after Task 3, card-fire-health passes too)
    - Mutation check: temporarily deleting `fix_supported` from a copy of the registry passed via a test-internal negative case (or a self-test sub-case using an inline bad registry object) makes the gate report a violation - the test MUST include at least one such self-test negative case using an in-memory bad entry to prove the gate actually bites
    - `grep -c "process.exit(1)" tests/test-doctor-module-contract-parity.cjs` >= 1
    - `grep -c "test-doctor-module-contract-parity" lib/memory/run-feynman-tests.cjs` == 1
  </acceptance_criteria>
  <done>The D-03 gate exists, is registered in the feynman suite, hard-fails on any declaration gap, and proves its own bite via an in-memory negative case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the D-05 card-fire-health test first (RED)</name>
  <files>tests/test-doctor-card-fire-health.cjs</files>
  <read_first>
    - scripts/check-card-fire.cjs lines 160-200 (getLogPath/interceptLogPath resolution: MINDRIAN_HOME env override, ~/.mindrian/card-fire-intercepts.log; REGISTRY_PATH at data/render-coverage-registry.json) and lines 1267-1290 (module.exports library seam + require.main guard)
    - scripts/check-card-fire.cjs: the log WRITER (grep card-fire-intercepts.log for the append call) to learn the exact JSONL entry shape and the timestamp field name written per CR-07
    - tests/test-doctor-class-b.cjs (spawn/fixture harness convention) and tests/test-doctor-module-selector.cjs (unit harness convention)
    - .planning/debug/resolved/backstop-benign-list-defeats-relevance-gate.md and .planning/debug/resolved/card-fire-block-surface.md (the sibling bug class this module guards against regressing)
  </read_first>
  <behavior>
    - Case 1 (absent log): check(ctx) with ctx.log_path pointing into an empty mkdtemp dir returns status 'ok' with a detail noting no intercepts recorded yet (a fresh install is healthy, not broken)
    - Case 2 (valid fresh JSONL): a fixture log with 2 valid JSON lines whose timestamp field is now-ish returns status 'ok', detail non-empty
    - Case 3 (malformed line): a fixture log containing one valid line and one non-JSON line returns status 'warn', detail naming the malformed-line count
    - Case 4 (stale): a fixture log whose newest entry timestamp is older than the injected ctx.staleness_ms (use 1ms to force it) returns status 'warn', detail mentioning staleness
    - Case 5 (contract): every returned status is in ok|warn|error|skip and detail is a non-empty string on every path; module exports check and does NOT export fix
    - Case 6 (library-seam parity): requiring scripts/check-card-fire.cjs from the test does not execute main() (require.main guard) and exposes classifyCardFire, gateReachingEntries, computeBackstopHit, loadRegistry as functions - the counter/classifier seams the 2026-07-11 parity fix depends on
  </behavior>
  <action>
    Create tests/test-doctor-card-fire-health.cjs implementing the six behavior cases
    against `require('../lib/core/doctor/card-fire-health-module.cjs')`. Hermetic:
    fs.mkdtempSync fixture dirs, node:assert/strict, ok() counter, exit 1 on failure,
    zero network, nothing touches the real ~/.mindrian. Commit RED:
    `test(217-02): add failing card-fire-health module test` (the module does not exist
    yet; the test run must fail at require time - that is the expected RED state).
  </action>
  <verify>
    <automated>node tests/test-doctor-card-fire-health.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists, is hermetic (grep for mkdtempSync >= 1, no network requires), and fails (non-zero exit) because lib/core/doctor/card-fire-health-module.cjs does not exist yet
    - All six behavior cases are present as named sub-tests
  </acceptance_criteria>
  <done>RED test committed pinning the full D-05 module contract before the module exists.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the card-fire-health module + registry entry + --card-fire-health flag (GREEN)</name>
  <files>lib/core/doctor/card-fire-health-module.cjs, data/doctor-modules.json, scripts/doctor.cjs</files>
  <read_first>
    - lib/core/doctor/umbilical-module.cjs (envelope: normCtx graceful defaults, soft-fail per item, check(ctx) return contract, test-only exports)
    - lib/core/doctor/shared.cjs (available helpers from Plan 01)
    - scripts/check-card-fire.cjs: interceptLogPath + the retry/session store implementation (sessionKey, SESSION_KEY_PREFIX, pruneRetryStore) to derive the exact store file path the statusline-facing session state lives in
    - scripts/doctor.cjs parseArgs (lines ~105-304: flag init block, the case chain, the --all block at ~275-285, usageText)
    - tests/test-doctor-card-fire-health.cjs (the RED contract from Task 2)
  </read_first>
  <action>
    Implements D-05. Check-only runner (fix_supported: false), umbilical envelope.

    Module `lib/core/doctor/card-fire-health-module.cjs`:
    1. `interceptLogPath()`: mirror check-card-fire.cjs exactly -
       `process.env.MINDRIAN_HOME || path.join(os.homedir(), '.mindrian')` +
       'card-fire-intercepts.log'. Test seam: ctx.log_path wins when provided.
    2. `check(ctx)` verifies, all LOCAL, zero network (Canon Part 8):
       a. Log existence: absent file -> status 'ok', detail
          'card-fire-health: no intercept log yet (no intercepts recorded)'.
       b. JSONL integrity: parse each non-empty line with per-line try/catch (the repo
          idiom); malformed count > 0 -> 'warn', detail naming the count and the path,
          action_lines suggesting inspection.
       c. Freshness: newest entry timestamp (use the exact timestamp field name the
          check-card-fire.cjs writer emits per CR-07; fall back to file mtime when the
          field is absent) older than `STALENESS_WINDOW_MS` (exported const, default 14
          days, ctx.staleness_ms test seam) -> 'warn' with a staleness detail. An empty
          log file (TTL-pruned) is 'ok'.
       d. Library-seam parity (the sibling lesson): require scripts/check-card-fire.cjs
          (safe: require.main guard) and confirm classifyCardFire, gateReachingEntries,
          computeBackstopHit, loadRegistry are exported functions; a missing seam ->
          'warn' naming it (that seam is what the printer-matches-counter fix and this
          module's future assertions depend on).
       e. Render-coverage instrument: loadRegistry() (or a direct JSON.parse of
          data/render-coverage-registry.json) parses -> else 'warn'.
       f. Session/statusline state: derive the retry/session store path from the
          check-card-fire.cjs store implementation read in read_first; when the store
          file exists it must JSON-parse -> else 'warn' (the statusline reflects
          card-fire/session state through that store; a corrupt store is instrument
          breakage).
       g. Aggregate: any warn condition -> overall 'warn' with a detail that counts the
          findings; all clean -> 'ok' with a one-line healthy detail. Every return path
          carries a non-empty detail (D-03 rule 9). Include a header comment explicitly
          citing .planning/debug/live-session-running-stale-plugin-cache-fixes-inert.md
          as the OPEN mystery this module deliberately does NOT attempt to resolve (it
          only verifies the instrument that mystery needs).
    3. Exports: `{ check, STALENESS_WINDOW_MS, interceptLogPath }` (no fix export -
       contract-parity rule 8 enforces the two-way declaration).

    Registry entry appended to data/doctor-modules.json modules[]:
    `{ "id": "card-fire-health", "introduced_version": "1.15.3", "cadence": "always", "flag": "cardFireHealth", "fix_supported": false, "runner": "lib/core/doctor/card-fire-health-module.cjs", "description": "check-card-fire.cjs instrument health: intercept log exists/valid JSONL/fresh, library seams intact, session store parseable" }`
    (introduced_version 1.15.3 is above the current 1.15.3-beta.13 prerelease and at or
    below the 211-217 release being cut; cadence:always makes check() watermark-immune
    regardless, per Plan 01.)

    parseArgs in scripts/doctor.cjs: add `cardFireHealth: false` to the flags init, an
    `else if (arg === '--card-fire-health') flags.cardFireHealth = true;` case, add
    `flags.cardFireHealth = true;` to the --all block, add the flag to usageText and to
    the classFlagsActive expression (line ~4851) so a `--card-fire-health`-only run is
    a class-flag run (exit-0 invariant, no once-heal pass). No other doctor.cjs change.

    Run the Task 2 test to GREEN, then the contract-parity gate (the new entry must
    pass all 9 rules). Commit: `feat(217-02): card-fire-health doctor module (D-05)`.
  </action>
  <verify>
    <automated>node tests/test-doctor-card-fire-health.cjs && node tests/test-doctor-module-contract-parity.cjs && node scripts/doctor.cjs --card-fire-health --json | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const j=JSON.parse(d);const c=j.checks['card-fire-health'];if(!c||!['ok','warn','error','skip'].includes(c.status)||!c.detail)process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-doctor-card-fire-health.cjs` exits 0 (all six cases GREEN)
    - `node tests/test-doctor-module-contract-parity.cjs` exits 0 with the new entry included
    - `node scripts/doctor.cjs --card-fire-health --json` emits a top-level checks['card-fire-health'] row with status in vocabulary and non-empty detail (proves the Plan 01 spread path end-to-end with a real module)
    - `node scripts/doctor.cjs --card-fire-health` (plain text) prints a visible card-fire-health row via the generic renderer (grep the output for 'card-fire-health')
    - `node scripts/doctor.cjs --all --json` includes the row (the --all block sets the flag)
    - `node tests/test-doctor-fix-renderer.cjs` still green
  </acceptance_criteria>
  <done>The first cadence:always module runs end-to-end through the new engine path: flag-gated, watermark-immune, rendered by the generic loop, guarded by the D-03 gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/.mindrian log files -> module parser | untrusted local JSONL parsed by card-fire-health |
| registry JSON -> require() | doctor-modules.json runner paths get require()d by the engine and the contract test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-04 | Repudiation/DoS | doctor-modules.json declaration gaps | mitigate | hard-blocking contract-parity test (exit 1); two-way fix declaration check |
| T-217-06 | Tampering | card-fire-intercepts.log parsing | mitigate | per-line JSON.parse try/catch; malformed input becomes a warn finding, never a throw |
| T-217-SC | Tampering | package installs | accept | zero external packages installed this phase |
</threat_model>

<verification>
- Both Wave-0 test files exist, are green, and are registered in run-feynman-tests.cjs
- `node scripts/doctor.cjs --json` bare run is unchanged except nothing (card-fire-health is flag/all-gated, not default_run)
</verification>

<success_criteria>
- D-03 gate live and biting (proven by its negative self-test case)
- D-05 module live, hermetically tested, visible in --all output
- The full Plans 03-06 migration proceeds under enforcement from this point on
</success_criteria>

<output>
Create `.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-02-SUMMARY.md` when done
</output>
