---
phase: 217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i
plan: 07
type: execute
wave: 6
depends_on: ["217-06"]
files_modified:
  - commands/doctor.md
  - tests/test-doctor-doc-parity.cjs
  - tests/run-all-217.sh
  - lib/memory/run-feynman-tests.cjs
autonomous: false
requirements: [D-03, D-04]
must_haves:
  truths:
    - "Every flag, class, --fix claim, and exit code documented in commands/doctor.md matches what is actually wired in the post-migration code - all 8 verdict lines from the RESEARCH audit table (INCOMPLETE/STALE/WRONG/MISSING) are resolved (D-04)"
    - "A doc-vs-code parity test guards the doc from regressing: an undocumented parseArgs flag or a documented-but-unparsed flag fails the suite"
    - "bash tests/run-all-217.sh aggregates the full phase test set and exits non-zero on any failure"
    - "A human has verified on the real machine that no check silently disappeared: pre/post-migration doctor --all shows the same set of visible check lines and consistent summary counts"
  artifacts:
    - path: "commands/doctor.md"
      provides: "Fully reconciled doctor documentation (flags, classes, --fix set, exit codes, module registry contract, the three carve-outs)"
      contains: "cadence"
    - path: "tests/test-doctor-doc-parity.cjs"
      provides: "D-04 regression guard: doc flags vs parseArgs flags, --fix class list vs registry fix_supported set"
      min_lines: 40
    - path: "tests/run-all-217.sh"
      provides: "Phase 217 hermetic aggregator (run-all-216.sh scaffold convention)"
  key_links:
    - from: "tests/test-doctor-doc-parity.cjs"
      to: "commands/doctor.md + scripts/doctor.cjs parseArgs + data/doctor-modules.json"
      via: "cross-parse of documented flags vs parsed flags vs fix_supported declarations"
      pattern: "doctor\\.md"
---

<objective>
Close-out plan: the D-04 full line-by-line commands/doctor.md audit (the RESEARCH.md
audit table IS the diff spec), a doc-parity regression test so the doc cannot silently
rot again, the phase test aggregator, and the VALIDATION.md manual real-room smoke as
a blocking human checkpoint.

Runs LAST because D-04 audits the POST-migration truth (the --fix set now includes the
real class B fix from Plan 04; bare-run and --all behavior now flow through the
registry).

Purpose: D-04 (two doc-vs-code mismatches were found in one sitting; the full audit
catches the rest) + the phase gate.
Output: reconciled doctor.md, doc-parity test, run-all-217.sh, human sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-RESEARCH.md
@.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-VALIDATION.md
@commands/doctor.md
@data/doctor-modules.json
</context>

## Artifacts this plan produces

| Artifact | Kind | Symbols |
|----------|------|---------|
| `commands/doctor.md` | MODIFIED | corrected argument-hint, class inventory (A-N, P, Q, R, S), bare-run behavior (A + N + engine + once-modules), --all set, the TRUE --fix set (A, B, E, G, H, I, J), exit codes 0/1/2/3/4 + classFlagsActive exit-0 invariant + reachability/disabled exit-1, the module registry + cadence contract, the three Phase 217 carve-outs, --card-fire-health |
| `tests/test-doctor-doc-parity.cjs` | NEW file | doc-vs-code parity assertions (registered in feynman suite) |
| `tests/run-all-217.sh` | NEW file | phase aggregator: contract-parity, card-fire-health, module-selector, fix-renderer, doc-parity, class b/c/e/f/g/g-fix/h/h-fix/i/j, plugin-disabled-state, ui-self-compliant |
| `lib/memory/run-feynman-tests.cjs` | MODIFIED | registers test-doctor-doc-parity.cjs |

<tasks>

<task type="auto">
  <name>Task 1: Rewrite commands/doctor.md against the post-migration code (the full D-04 audit)</name>
  <files>commands/doctor.md</files>
  <read_first>
    - .planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-RESEARCH.md section "commands/doctor.md Audit (D-04 deliverable - literal line-by-line)" - this table IS the work order; every row with verdict INCOMPLETE/STALE/WRONG/MISSING gets fixed
    - commands/doctor.md (all 189 lines, current state)
    - scripts/doctor.cjs: parseArgs + usageText (the complete flag set post-migration, including --card-fire-health), _finalizeAndExit (~5793-5852: exit codes 0/1/2/3/4, classFlagsActive forces exit 0, runtime-reachability warn exit 1, plugin-disabled exit 1)
    - data/doctor-modules.json (the 14-entry registry: ids, flags, cadence, fix_supported - the source of truth for the class and --fix documentation)
  </read_first>
  <action>
    Apply every fix the RESEARCH audit table demands, against the POST-migration code:
    1. L5 argument-hint: complete the flag list (add --eureka-smoke, --drift,
       --dogfood-acceptance, --claims, --check-rs-engine, --post-update, --bind-check,
       --simulate-write, --scan-commands, --scan-scripts, --dry-run, --pre-flight, and
       the new --card-fire-health).
    2. L42 'runs three checks': rewrite to the actual class inventory (A-N plus opt-in
       P, Q, R, S), naming the registry as the source of truth for engine-driven checks.
    3. L60 bare-run: class A + class N (plugin-enabled-state) + the accumulative engine
       (once-heals plus any flag:null always-modules).
    4. L61 --all: the actual set parseArgs activates (B, C, D, E, F, G+H, I+J, K, L, M,
       card-fire-health), explicitly noting drift/eureka stay opt-in by design.
    5. L63 --verify-surface: name the actual child runner path
       tests/test-cascade-surface-e2e.cjs.
    6. L71 --fix: the TRUE set post-migration: A, B (NEWLY REAL, Plan 04), E, G, H, I,
       J. Note that fix support is now declared per-module in data/doctor-modules.json
       (fix_supported) and enforced by tests/test-doctor-module-contract-parity.cjs.
    7. L98-105 exit codes: add 4 (rollback), the classFlagsActive-forces-exit-0
       invariant, runtime-reachability warn exit 1, plugin-disabled exit 1.
    8. Document the previously-missing surfaces: classes H, I, J, N, P, Q, R, S;
       --acceptance/--pre-tag/--pre-flight; --claims; --dogfood-acceptance;
       --check-rs-engine; --post-update; --bind-check; --card-fire-health.
    9. Add a short 'Extension architecture' section: new check = one
       data/doctor-modules.json entry (id, introduced_version, cadence always|once,
       flag, fix_supported, runner) + one lib/core/doctor/<id>-module.cjs runner
       exporting check(ctx) (and fix(ctx) when fix_supported). Name the three Phase 217
       carve-outs (class A special-cased, brain-smoke/eureka-smoke async) so the doc
       matches the code comments from Plan 06.
    House rules: no em-dashes anywhere; Feynman-simplified phrasing; preserve the
    command frontmatter contract (this is a CIRS-governed command surface - do not
    alter frontmatter keys like hitl_shape/allowed-tools, only body + argument-hint).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const doc=fs.readFileSync('/home/jsagi/dev/MindrianOS-Plugin/commands/doctor.md','utf8');for(const t of ['card-fire-health','cadence','fix_supported','exit','rollback']){if(doc.indexOf(t)<0)throw new Error('doctor.md missing: '+t)};if(/—/.test(doc))throw new Error('em-dash found');console.log('doc audit tokens ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Every INCOMPLETE/STALE/WRONG/MISSING row from the RESEARCH audit table has a corresponding corrected line in doctor.md (enumerate the mapping in the summary)
    - The --fix line names exactly A, B, E, G, H, I, J
    - Exit-code section covers 0/1/2/3/4 + the classFlagsActive exit-0 invariant
    - Zero U+2014 em-dashes in the file (verify one-liner)
    - Frontmatter keys untouched (git diff shows body-only changes)
  </acceptance_criteria>
  <done>doctor.md tells the truth about the post-migration code, line by line.</done>
</task>

<task type="auto">
  <name>Task 2: Create the doc-parity regression test + the phase aggregator</name>
  <files>tests/test-doctor-doc-parity.cjs, tests/run-all-217.sh, lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - commands/doctor.md (as rewritten by Task 1)
    - scripts/doctor.cjs parseArgs (the `else if (arg === '--...')` case chain - the machine-readable flag inventory)
    - data/doctor-modules.json (fix_supported declarations)
    - tests/run-all-216.sh (the scaffold convention: set -uo pipefail, run/run_if helpers, PASS/FAIL/SKIP counters, exit non-zero on any FAIL)
    - lib/memory/run-feynman-tests.cjs (registration block)
  </read_first>
  <action>
    1. tests/test-doctor-doc-parity.cjs (node:assert, hermetic, no spawn needed):
       a. Extract the parsed flag set from scripts/doctor.cjs source: regex the
          parseArgs case chain for `'--[a-z-]+'` literals (exclude values like
          --simulate-write=<path> by matching the flag token only).
       b. Extract the documented flag set from commands/doctor.md: every `--[a-z-]+`
          token in the body.
       c. Assert every parsed flag appears in the doc, and every documented flag
          appears in parseArgs (allowlist mechanism for prose-only tokens if needed,
          each allowlist entry commented with a reason).
       d. Assert the doc's --fix class letters match reality: parse the --fix line for
          class letters and assert the set equals {A, B, E, G, H, I, J} derived as:
          A (special-cased) + the registry entries with fix_supported true mapped to
          their class letters (cascade-rooms=B, room-md=E, statusline-visibility=G,
          install-incomplete=H, install-state=I, deployment-surfaces=J; umbilical has
          no class letter, skip it).
       e. Hard-fail exit 1 with per-violation enumeration (same posture as the D-03
          gate; doc drift is how this phase's trigger bug survived undetected).
       Register in lib/memory/run-feynman-tests.cjs.
    2. tests/run-all-217.sh on the run-all-216.sh scaffold, legs in order:
       (1) test-doctor-module-contract-parity, (2) test-doctor-module-selector,
       (3) test-doctor-fix-renderer, (4) test-doctor-card-fire-health,
       (5) test-doctor-doc-parity, (6-15) class tests b, c, e, f, g, g-fix, h, h-fix,
       i, j, (16) test-doctor-plugin-disabled-state, (17) test-doctor-ui-self-compliant.
       Do NOT include class-p/class-q (known pre-existing failures, deferred-items
       ruling from 260711-nrd; note this in the script header). Header comments state
       the phase contract + zero-network expectation. chmod +x.
  </action>
  <verify>
    <automated>node tests/test-doctor-doc-parity.cjs && bash tests/run-all-217.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-doctor-doc-parity.cjs` exits 0 against the rewritten doc, and a deliberate in-memory negative case (an invented flag string checked against the doc set) proves the gate bites
    - `bash tests/run-all-217.sh` prints PASS/FAIL counts, FAIL=0, exit 0
    - `grep -c "test-doctor-doc-parity" lib/memory/run-feynman-tests.cjs` == 1
    - `bash -n tests/run-all-217.sh` exits 0
  </acceptance_criteria>
  <done>Doc truth is regression-guarded; the whole phase is green under one aggregator command.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Real-room smoke - confirm no check silently disappeared (the VALIDATION.md manual gate)</name>
  <what-built>Plans 01-07 migrated 13 check surfaces into the accumulative-engine registry (14 entries incl. umbilical and the new card-fire-health), rewired print+fix structurally, hardened it all behind the D-03 gate, and reconciled commands/doctor.md. Hermetic tests cannot safely simulate every real room-registry edge case (cascade-rooms, cascade-rooms-active, room-md read your real ~/MindrianRooms state), so the final gate is your machine.</what-built>
  <how-to-verify>
    1. Run: node scripts/doctor.cjs --all
       Expected: one visible row per non-skip check; the Summary counts match the
       number of visible rows (the whole point of this phase); no crash; rows include
       cascade-rooms, cascade-rooms-active, room-md, ui-compliance,
       statusline-visibility, install-incomplete, stale-first-touch-copy,
       deprecated-usage, plugin-enabled-state, verify-surface (or its skip is honest),
       install-state, deployment-surfaces, card-fire-health.
    2. Run it AGAIN: node scripts/doctor.cjs --all
       Expected: the SAME rows appear (the watermark does not silence any diagnostic -
       the Pitfall-1 kill shot, verified live).
    3. Run: node scripts/doctor.cjs
       Expected: bare run shows class A + plugin-enabled-state + engine line only
       (unchanged bare-run surface).
    4. Run: bash tests/run-all-217.sh
       Expected: FAIL=0.
    5. Spot-check statuses against your known machine state (e.g. if you know a room
       is missing ROOM.md, room-md should say so, same as before the migration).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 217, or describe any missing/changed row - a described defect becomes a gap-closure item for /gsd-plan-phase 217 --gaps, not a hot patch past the gate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc claims -> user expectations | doctor.md tells users what --fix will mutate; false claims are a repudiation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-05 | Repudiation | commands/doctor.md claims | mitigate | full audit (Task 1) + hard-blocking doc-parity test (Task 2) |
| T-217-SC | Tampering | package installs | accept | zero external packages installed this phase |
</threat_model>

<verification>
- bash tests/run-all-217.sh green (FAIL=0)
- node lib/memory/run-feynman-tests.cjs green for the registered doctor set
- Human checkpoint approved verbatim (the VALIDATION.md manual-only row)
- Known pre-existing class P/Q failures unchanged and excluded from the aggregator by documented design
</verification>

<success_criteria>
- D-04 delivered in full (all audit-table verdicts resolved + regression-guarded)
- Phase gate: one aggregator command proves the whole phase; human confirms real-room parity
- Ready for the 211-217 release cut
</success_criteria>

<output>
Create `.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-07-SUMMARY.md` when done
</output>
