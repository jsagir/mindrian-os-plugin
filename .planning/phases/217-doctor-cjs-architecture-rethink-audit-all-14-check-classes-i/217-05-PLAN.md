---
phase: 217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i
plan: 05
type: execute
wave: 4
depends_on: ["217-04"]
files_modified:
  - lib/core/doctor/statusline-visibility-module.cjs
  - lib/core/doctor/install-incomplete-module.cjs
  - lib/core/doctor/verify-surface-module.cjs
  - data/doctor-modules.json
  - scripts/doctor.cjs
autonomous: true
requirements: [D-01, D-02]
must_haves:
  truths:
    - "doctor --statusline-visibility runs BOTH statusline-visibility (G) and install-incomplete (H) via the registry, exactly like today's shared-flag behavior, with their fixes routed through the engine's fix-then-recheck flow"
    - "doctor --verify-surface (D) still spawns tests/test-cascade-surface-e2e.cjs with the 30s timeout and self-skips when bash or the child test is absent (an install is never faulted for a missing dev harness)"
    - "The G and H hand-coded render branches and their generic-loop skip lines are GONE; rows + hint sub-lines come from the generic loop + action_lines"
  artifacts:
    - path: "lib/core/doctor/statusline-visibility-module.cjs"
      provides: "class G runner: check + fix (performStatuslineFix moved in)"
      exports: ["check", "fix"]
    - path: "lib/core/doctor/install-incomplete-module.cjs"
      provides: "class H runner: check + fix (performClassHFix moved in, recoverable===true guard runner-internal)"
      exports: ["check", "fix"]
    - path: "lib/core/doctor/verify-surface-module.cjs"
      provides: "class D runner: child-spawn check, fix_supported false"
      exports: ["check"]
  key_links:
    - from: "data/doctor-modules.json install-incomplete entry"
      to: "flags.statuslineVisibility"
      via: "flag: statuslineVisibility (H shares G's flag, today's behavior)"
      pattern: "statuslineVisibility"
    - from: "lib/core/doctor/verify-surface-module.cjs"
      to: "tests/test-cascade-surface-e2e.cjs"
      via: "spawnSync with 30s timeout + absent-harness self-skip"
      pattern: "test-cascade-surface-e2e"
---

<objective>
Fourth-wave migration (D-01/D-02): statusline-visibility (G, + fix), install-incomplete
(H, + fix, shares G's activation flag), and verify-surface (D, child-spawn check).
Deletes the last two hand-coded render branches (G at ~3832-3843, H at ~3844-3853) and
their generic-loop skip lines - after this plan the ONLY non-generic render path left
is class A (the sanctioned carve-out, Plan 06).

Purpose: migrate the remaining fix-carrying singles plus the one event-driven check.
Output: 3 runner files, 3 registry entries, smaller doctor.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-RESEARCH.md
@.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-PATTERNS.md
@lib/core/doctor/umbilical-module.cjs
@lib/core/doctor/shared.cjs
</context>

## Artifacts this plan produces

| Artifact | Kind | Symbols |
|----------|------|---------|
| `lib/core/doctor/statusline-visibility-module.cjs` | NEW file | `check(ctx)` (moved checkStatuslineVisibility), `fix(ctx)` (moved performStatuslineFix) |
| `lib/core/doctor/install-incomplete-module.cjs` | NEW file | `check(ctx)` (moved checkInstallIncomplete), `fix(ctx)` (moved performClassHFix with internal recoverable===true guard) |
| `lib/core/doctor/verify-surface-module.cjs` | NEW file | `check(ctx)` (moved checkSurfaceVerification, spawnSync + self-skip) |
| `data/doctor-modules.json` | MODIFIED | entries: statusline-visibility (flag statuslineVisibility, fix_supported true), install-incomplete (flag statuslineVisibility, fix_supported true), verify-surface (flag verifySurface, fix_supported false); all cadence always, introduced_version 1.15.3 |
| `scripts/doctor.cjs` | MODIFIED (shrinks) | deleted: checkStatuslineVisibility, performStatuslineFix, checkInstallIncomplete, performClassHFix, checkSurfaceVerification bodies; inline G block (~4998-5006), H block (~5008-5017), D block (~4970-4975); G fix dispatch (~5033-5048), H fix dispatch (~5050-5065); G render branch (~3832-3843), H render branch (~3844-3853) + their two generic-loop skip lines |

<tasks>

<task type="auto">
  <name>Task 1: Create the G, H, D runner files</name>
  <files>lib/core/doctor/statusline-visibility-module.cjs, lib/core/doctor/install-incomplete-module.cjs, lib/core/doctor/verify-surface-module.cjs</files>
  <read_first>
    - lib/core/doctor/umbilical-module.cjs (envelope + fix contract)
    - scripts/doctor.cjs: checkStatuslineVisibility (~1362-1525) + performStatuslineFix (~1528-1585); checkInstallIncomplete (~1587-1678) + performClassHFix (~1680-1750); checkSurfaceVerification (~958-1001: spawnSync of tests/test-cascade-surface-e2e.cjs, 30s timeout, bash-absent self-skip branch)
    - scripts/doctor.cjs: the G fix dispatch (~5033-5048: gate is status warn AND recoverable !== false, recovery pushed onto report.recovered, re-check) and the H fix dispatch (~5050-5065: gate is recoverable === true - STRICTER than the engine's generic recoverable !== false gate)
    - scripts/doctor.cjs: the G render branch (~3832-3843) and H render branch (~3844-3853, including the '-> /mos:doctor --statusline-visibility --fix re-stamps the statusLine block' hint sub-line)
    - lib/core/doctor/shared.cjs (importable helpers)
  </read_first>
  <action>
    Implements D-01 for G/H/D. Umbilical envelope, './shared.cjs' only, ids equal the
    existing report.checks keys.

    1. statusline-visibility-module.cjs: check(ctx) = moved checkStatuslineVisibility
       (preserve the recoverable field and every payload key; non-empty detail on all
       paths). fix(ctx) = moved performStatuslineFix invoked with ctx.check_result;
       return the recovery record shape main() pushed raw (tool field preserved), plus
       status/detail.
    2. install-incomplete-module.cjs: check(ctx) = moved checkInstallIncomplete. The
       deleted H render branch's hint sub-line becomes
       action_lines: ['/mos:doctor --statusline-visibility --fix re-stamps the statusLine block']
       emitted ONLY on the warn+recoverable path (mirror the branch's condition).
       fix(ctx) = moved performClassHFix with a RUNNER-INTERNAL guard: if
       ctx.check_result.recoverable !== true, return
       { status: 'skip', detail: 'install-incomplete: not recoverable, no fix attempted' }
       without touching anything - the engine's generic gate is recoverable !== false,
       which is looser than H's historical === true gate, so the runner enforces its
       own stricter contract (semantics preserved exactly).
    3. verify-surface-module.cjs: check(ctx) = moved checkSurfaceVerification verbatim:
       spawnSync of tests/test-cascade-surface-e2e.cjs with the 30s timeout, the
       bash-absent and harness-absent branches returning status 'skip' (never fault an
       install for a missing dev harness), exitCode/runner payload keys preserved.
       Re-base the child-test path for the new __dirname (lib/core/doctor -> repo root
       is three '..' hops). No fix export.
  </action>
  <verify>
    <automated>node -e "const base='/home/jsagi/dev/MindrianOS-Plugin/lib/core/doctor/';const g=require(base+'statusline-visibility-module.cjs');const h=require(base+'install-incomplete-module.cjs');const d=require(base+'verify-surface-module.cjs');if(typeof g.check!=='function'||typeof g.fix!=='function')throw new Error('G');if(typeof h.check!=='function'||typeof h.fix!=='function')throw new Error('H');if(typeof d.check!=='function'||typeof d.fix!=='undefined')throw new Error('D');const hr=h.fix({home:require('os').tmpdir(),dryRun:true,flags:{},check_result:{status:'warn',recoverable:false}});if(hr.status!=='skip')throw new Error('H fix must skip when not recoverable: '+JSON.stringify(hr));console.log('217-05 task1 smoke ok')"</automated>
  </verify>
  <acceptance_criteria>
    - G and H export check+fix; D exports check only (verify one-liner)
    - H's fix returns status 'skip' with detail when ctx.check_result.recoverable is not exactly true (pinned by the verify one-liner)
    - `grep -c "test-cascade-surface-e2e" lib/core/doctor/verify-surface-module.cjs` >= 1 and the timeout stays 30000ms (grep 30000)
    - No runner requires scripts/doctor.cjs (grep == 0 per file)
  </acceptance_criteria>
  <done>Three contract-compliant runners on disk; H's stricter fix gate is runner-internal; wiring is Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Wire G/H/D registry entries, delete inline blocks + render branches + dead bodies</name>
  <files>data/doctor-modules.json, scripts/doctor.cjs</files>
  <read_first>
    - data/doctor-modules.json (9 entries after Plan 04)
    - scripts/doctor.cjs: every block named for deletion in the artifacts table above (grep each function name for remaining call sites before deleting; checkSurfaceVerification may be referenced by --acceptance or preflight paths - if ANY non-main call site exists, keep a thin delegating stub that requires the runner module and calls its check, and record it in the summary)
    - tests/test-doctor-class-g.cjs, tests/test-doctor-class-g-fix.cjs, tests/test-doctor-class-h.cjs, tests/test-doctor-class-h-fix.cjs (the behavior pins)
    - tests/test-doctor-statusline-prefix-validator.cjs (statusline-adjacent pin that must stay green)
  </read_first>
  <action>
    1. Append three entries to data/doctor-modules.json modules[], all
       "cadence": "always", "introduced_version": "1.15.3":
       - id "statusline-visibility", flag "statuslineVisibility", fix_supported TRUE
       - id "install-incomplete", flag "statuslineVisibility", fix_supported TRUE
         (H shares G's activation flag - today's exact behavior: --statusline-visibility
         surfaces both, --all surfaces both)
       - id "verify-surface", flag "verifySurface", fix_supported false
       Registry order: statusline-visibility BEFORE install-incomplete (report row
       order matches today's render order).
    2. Delete from scripts/doctor.cjs: the G/H/D inline blocks, the G and H fix
       dispatches, the G and H hand-coded render branches, the two generic-loop skip
       lines (`if (name === 'statusline-visibility') continue;` and
       `if (name === 'install-incomplete') continue;`), and the dead function bodies
       (checkStatuslineVisibility, performStatuslineFix, checkInstallIncomplete,
       performClassHFix, checkSurfaceVerification) after the call-site grep clears.
    3. Run the regression set; update only render-label pins if any test asserts the
       old hand-coded row strings (statuses/payloads never), recording each change.
  </action>
  <verify>
    <automated>node tests/test-doctor-module-contract-parity.cjs && node tests/test-doctor-class-g.cjs && node tests/test-doctor-class-g-fix.cjs && node tests/test-doctor-class-h.cjs && node tests/test-doctor-class-h-fix.cjs && node tests/test-doctor-statusline-prefix-validator.cjs && node tests/test-doctor-fix-renderer.cjs && node tests/test-doctor-module-selector.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Contract-parity gate green with 12 registry entries
    - class-g, class-g-fix, class-h, class-h-fix, statusline-prefix-validator, fix-renderer, module-selector all exit 0
    - `node scripts/doctor.cjs --statusline-visibility --json` emits BOTH checks['statusline-visibility'] and checks['install-incomplete'] top-level rows (shared flag preserved)
    - `node scripts/doctor.cjs --verify-surface --json` emits checks['verify-surface'] with status in vocabulary (skip is legitimate when the harness is absent)
    - `grep -c "statusline-visibility'\) continue\|install-incomplete'\) continue" scripts/doctor.cjs` == 0
    - The only remaining hand-coded render section in renderHumanReport is class A (grep the function for report.checks[' literals: only 'install-cache' remains)
  </acceptance_criteria>
  <done>G/H/D registry-driven; every non-class-A render path is now the generic loop; all pins green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child process spawn | verify-surface spawns a repo-local test script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-03 | Elevation | verify-surface spawnSync | mitigate | child path is repo-root-resolved constant (never caller input); 30s timeout preserved; absent harness -> skip |
| T-217-01 | DoS (self) | G/H fix flows | mitigate | engine try/catch + runner-internal guards (H recoverable===true) preserved |
| T-217-SC | Tampering | package installs | accept | zero external packages installed this phase |
</threat_model>

<verification>
- Touched-surface regression set green (Task 2 verify chain)
- Known pre-existing class P/Q failures unchanged (2 and 6), untouched
</verification>

<success_criteria>
- G/H/D migrated with fixes structural; hand-coded render branches fully retired except class A
</success_criteria>

<output>
Create `.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-05-SUMMARY.md` when done
</output>
