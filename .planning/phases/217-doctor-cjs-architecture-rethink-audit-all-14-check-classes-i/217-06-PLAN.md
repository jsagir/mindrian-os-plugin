---
phase: 217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i
plan: 06
type: execute
wave: 5
depends_on: ["217-05"]
files_modified:
  - lib/core/doctor/install-state-module.cjs
  - lib/core/doctor/deployment-surfaces-module.cjs
  - data/doctor-modules.json
  - scripts/doctor.cjs
autonomous: true
requirements: [D-01, D-02]
must_haves:
  truths:
    - "doctor --install-state runs BOTH install-state (I) and deployment-surfaces (J) via the registry, with J reading I's same-invocation result through ctx.checks['install-state'] and self-deriving topology/activeRoot/activeVersion when absent"
    - "Class I --fix returns MULTIPLE recovery records (the classIRecoveries array semantics) and each lands on report.recovered via the engine's recoveries[] plumbing"
    - "Class A (install-cache) remains special-cased in main() with an explicit written justification comment; brain-smoke (M) and eureka-smoke (S) remain special-cased async blocks with the same treatment - these are the three sanctioned carve-outs, not silent omissions"
  artifacts:
    - path: "lib/core/doctor/install-state-module.cjs"
      provides: "class I runner: check + fix (performClassIFix moved in, returns { recoveries: [...] })"
      exports: ["check", "fix"]
    - path: "lib/core/doctor/deployment-surfaces-module.cjs"
      provides: "class J runner: check + fix (performClassJFix moved in), ctx.checks-aware with shared.cjs self-derivation fallback"
      exports: ["check", "fix"]
    - path: "scripts/doctor.cjs"
      provides: "class A carve-out justification comment block + M/S async carve-out comments"
      contains: "Phase 217 carve-out"
  key_links:
    - from: "lib/core/doctor/deployment-surfaces-module.cjs"
      to: "ctx.checks['install-state']"
      via: "same-invocation result map (engine accumulates in registry order)"
      pattern: "checks\\['install-state'\\]"
    - from: "lib/core/doctor/deployment-surfaces-module.cjs"
      to: "lib/core/doctor/shared.cjs"
      via: "resolveActivePluginRoot + readInstalledPluginsVersion fallback derivation"
      pattern: "readInstalledPluginsVersion"
---

<objective>
Final migration wave (D-01/D-02): install-state (I) and deployment-surfaces (J), the two
checks with the deepest main() interplay, plus the formal recording of the three
sanctioned carve-outs (class A install-cache, brain-smoke M, eureka-smoke S).

Carve-out justification (required by the phase quality gate, per RESEARCH Open
Question 3 which the planner hereby adopts): class A's results live OUTSIDE
report.checks (report.install / report.cache / report.drift / report.classARecovered),
its recovery is the atomic-swap path, and `_finalizeAndExit(flags, report,
classFlagsActive, cacheResult, installResult)` consumes its results POSITIONALLY - the
exit-code contract is structurally coupled to main(). Forcing it into the uniform
module shape would restructure the most-tested path days before the 211-217 release.
Its three pure constituent readers (checkInstallVersion, checkMarketplaceCache,
checkDevSourceConsistency - the D-01 list items 5, 6, 7) already left the monolith into
shared.cjs in Plan 01, which is the D-02 file-split delivery for class A this phase.
M/S stay special-cased because they are async and the engine loop is sync (RESEARCH
Pitfall 2, CONTEXT discretion clause A2: lower-risk call under release pressure).

Purpose: complete the registry migration for everything the difficulty map cleared;
make the carve-outs loud, written, and auditable instead of silent.
Output: 2 runner files, 2 registry entries, carve-out comments, smaller doctor.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-RESEARCH.md
@.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-PATTERNS.md
@lib/core/doctor/umbilical-module.cjs
@lib/core/doctor/shared.cjs
</context>

## Artifacts this plan produces

| Artifact | Kind | Symbols |
|----------|------|---------|
| `lib/core/doctor/install-state-module.cjs` | NEW file | `check(ctx)` (moved checkInstallState), `fix(ctx)` (moved performClassIFix, returns `{ status, detail, recoveries: [...] }`) |
| `lib/core/doctor/deployment-surfaces-module.cjs` | NEW file | `check(ctx)` (moved checkDeploymentSurfaces, ctx.checks-aware), `fix(ctx)` (moved performClassJFix, returns recoveries[]) |
| `data/doctor-modules.json` | MODIFIED | entries: install-state (flag installState, fix_supported true), deployment-surfaces (flag installState, fix_supported true, ordered AFTER install-state); cadence always, introduced_version 1.15.3 |
| `scripts/doctor.cjs` | MODIFIED (shrinks) | deleted: checkInstallState, performClassIFix, checkDeploymentSurfaces, performClassJFix bodies + the I/J main() block (~5066-5135); ADDED: 'Phase 217 carve-out' comment blocks at the class A section and the M/S async blocks |

<tasks>

<task type="auto">
  <name>Task 1: Create the I and J runner files (multi-recovery fix + ctx.checks dependency)</name>
  <files>lib/core/doctor/install-state-module.cjs, lib/core/doctor/deployment-surfaces-module.cjs</files>
  <read_first>
    - scripts/doctor.cjs: checkInstallState (~2002-2140) + performClassIFix (~2142-2300); checkDeploymentSurfaces (~2437-2544) + performClassJFix (~2546-2660)
    - scripts/doctor.cjs: the FULL I/J main() block (~5066-5135) - note: stateAfter re-check after fix, `for (const rec of classIRecoveries) report.recovered.push(rec);` (I's fix yields an ARRAY of recovery records), activeVersion derivation via stateAfter.record.active_version falling back to readInstalledPluginsVersion(home), J receiving { home, topology, activeRoot, activeVersion }, and J's own recoveries loop (~5121)
    - lib/core/doctor/shared.cjs (resolveActivePluginRoot, readInstalledPluginsVersion, checkInstallVersion - the self-derivation fallbacks)
    - lib/core/doctor/umbilical-module.cjs (envelope) and Plan 01's engine fix flow spec (fixRes.recoveries[] plumbing)
    - Phase 123 carve-out comments inside the class A recovery gate (~4917-4925: flags.installState suppresses class A fix - this interplay STAYS in main(), do not move it)
  </read_first>
  <action>
    Implements D-01 for I/J. Umbilical envelope, './shared.cjs' only, ids equal
    existing report.checks keys.

    1. install-state-module.cjs:
       - check(ctx) = moved checkInstallState({ home: ctx.home }); preserve topology,
         record, versions, findings, recoverable payload keys and every env-var read;
         non-empty detail on all paths.
       - fix(ctx) = moved performClassIFix invoked per its existing contract (read the
         body to confirm its input: the check result and/or home). Return
         { status, detail, recoveries } where recoveries is the exact classIRecoveries
         array main() used to push element-by-element - the Plan 01 engine glue pushes
         each recoveries[] element onto report.recovered with the fixer's own tool
         field winning. Preserve the dev-clone safety belt and dirty/unpushed refuse
         checks verbatim (D-13 semantics from Phase 123).
    2. deployment-surfaces-module.cjs:
       - check(ctx): derive inputs in this priority order: (a) ctx.checks &&
         ctx.checks['install-state'] (the same-invocation I result: topology from
         .topology, activeVersion from .record.active_version); (b) self-derive via
         shared.cjs: resolveActivePluginRoot() for topology/activeRoot,
         readInstalledPluginsVersion(ctx.home) for activeVersion. Then the moved
         checkDeploymentSurfaces body with { home, topology, activeRoot, activeVersion }.
       - fix(ctx) = moved performClassJFix; return { status, detail, recoveries } with
         J's recovery array semantics preserved.
    3. Registry ordering dependency: install-state MUST precede deployment-surfaces in
       data/doctor-modules.json (Task 2) so ctx.checks carries I's result when J runs -
       state this in both file headers.
  </action>
  <verify>
    <automated>node -e "const os=require('os');const base='/home/jsagi/dev/MindrianOS-Plugin/lib/core/doctor/';const i=require(base+'install-state-module.cjs');const j=require(base+'deployment-surfaces-module.cjs');if(typeof i.check!=='function'||typeof i.fix!=='function')throw new Error('I');if(typeof j.check!=='function'||typeof j.fix!=='function')throw new Error('J');const ir=i.check({home:os.tmpdir(),running:'99.99.99',dryRun:true,fix:false,flags:{}});if(['ok','warn','error','skip'].indexOf(ir.status)<0||!ir.detail)throw new Error('I contract: '+JSON.stringify(ir));const jr=j.check({home:os.tmpdir(),running:'99.99.99',dryRun:true,fix:false,flags:{},checks:{}});if(['ok','warn','error','skip'].indexOf(jr.status)<0||!jr.detail)throw new Error('J contract: '+JSON.stringify(jr));console.log('217-06 task1 smoke ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Both runners export check+fix and satisfy the vocabulary+detail contract against a scratch home (verify one-liner)
    - J's check works BOTH with ctx.checks['install-state'] present and with an empty ctx.checks (self-derivation fallback) - the one-liner exercises the fallback path
    - I's fix returns a recoveries array key (grep 'recoveries' in install-state-module.cjs >= 1)
    - No runner requires scripts/doctor.cjs
  </acceptance_criteria>
  <done>I and J runners on disk with multi-recovery fix contracts and the I-to-J same-invocation dependency handled; wiring is Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Wire I/J entries, delete the I/J main() block + dead bodies, write the three carve-out justifications</name>
  <files>data/doctor-modules.json, scripts/doctor.cjs</files>
  <read_first>
    - data/doctor-modules.json (12 entries after Plan 05)
    - scripts/doctor.cjs: the I/J main() block (~5066-5135) and the dead bodies (grep checkInstallState, performClassIFix, checkDeploymentSurfaces, performClassJFix for remaining call sites - checkInstallState may be referenced by the engine's running-version resolution or acceptance paths; if so, keep the reference pointing at the RUNNER module's exported check or at shared.cjs, never a duplicate body)
    - scripts/doctor.cjs: the class A section (~4880-4960) and the M/S async blocks (~5601-5626) where the carve-out comments land
    - tests/test-doctor-class-i.cjs, tests/test-doctor-class-j.cjs (behavior pins)
  </read_first>
  <action>
    1. Append two entries to data/doctor-modules.json modules[], cadence "always",
       introduced_version "1.15.3", in THIS order:
       - id "install-state", flag "installState", fix_supported TRUE
       - id "deployment-surfaces", flag "installState", fix_supported TRUE
       (J shares I's --install-state flag, exactly as today; array order carries the
       I-before-J dependency.)
    2. Delete the I/J main() block and the four dead bodies after the call-site grep
       clears. The `flags.installState` suppression inside the class A recovery gate
       STAYS untouched (it reads the flag, not the check).
    3. Write the carve-out comments (each opens with the literal marker
       'Phase 217 carve-out:'):
       a. At the class A section head: class A stays special-cased because its results
          live outside report.checks and _finalizeAndExit consumes cacheResult/
          installResult positionally; its pure readers moved to
          lib/core/doctor/shared.cjs in 217-01; full migration is future work, per
          217-RESEARCH Open Question 3.
       b. At the brain-smoke and eureka-smoke blocks: async runners vs the sync engine
          loop (217-RESEARCH Pitfall 2, CONTEXT discretion A2); migrating them requires
          an async engine pass, deliberately not taken during the 211-217 release window.
       No em-dashes in any comment (house rule).
    4. Run the regression set; record any legitimate pin updates in the summary.
  </action>
  <verify>
    <automated>node tests/test-doctor-module-contract-parity.cjs && node tests/test-doctor-class-i.cjs && node tests/test-doctor-class-j.cjs && node tests/test-doctor-module-selector.cjs && node tests/test-doctor-fix-renderer.cjs && node scripts/doctor.cjs --install-state --json | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const j=JSON.parse(d);for(const k of ['install-state','deployment-surfaces']){const c=j.checks[k];if(!c||['ok','warn','error','skip'].indexOf(c.status)<0||!c.detail)throw new Error(k)}})"</automated>
  </verify>
  <acceptance_criteria>
    - Contract-parity gate green with 14 registry entries (umbilical, card-fire-health, ui-compliance, stale-first-touch-copy, deprecated-usage, plugin-enabled-state, cascade-rooms, cascade-rooms-active, room-md, statusline-visibility, install-incomplete, verify-surface, install-state, deployment-surfaces)
    - class-i and class-j tests exit 0
    - `node scripts/doctor.cjs --install-state --json` emits BOTH top-level rows with vocabulary statuses + detail (shared flag + registry order dependency proven live)
    - `grep -c "Phase 217 carve-out" scripts/doctor.cjs` == 3 (class A, brain-smoke, eureka-smoke)
    - `grep -c "function checkInstallState\|function performClassIFix\|function checkDeploymentSurfaces\|function performClassJFix" scripts/doctor.cjs` == 0
    - `node scripts/doctor.cjs --all --json` succeeds and its checks map contains every cadence:always module id gated by --all (visual sweep for the summary)
  </acceptance_criteria>
  <done>All D-01 checks the difficulty map cleared are registry-driven; the three carve-outs are written justifications in the code; 14 entries pass the D-03 gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install ledgers -> I/J fixes | class I/J fixes mutate install topology state (backup-then-verify semantics) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-03 | Tampering | class I fix migration path | mitigate | dev-clone safety belt + dirty/unpushed refuse checks preserved verbatim (Phase 123 D-13); dryRun honored |
| T-217-01 | DoS (self) | I/J runners | mitigate | engine try/catch + soft-fail preserved |
| T-217-SC | Tampering | package installs | accept | zero external packages installed this phase |
</threat_model>

<verification>
- Touched-surface regression green (Task 2 chain)
- Known pre-existing class P/Q failures unchanged (2 and 6), untouched
- `node scripts/doctor.cjs --acceptance` still exits per its existing contract (class A + acceptance paths untouched)
</verification>

<success_criteria>
- I/J migrated with multi-recovery fix plumbing; carve-outs documented in code; registry at 14 entries, all gate-green
</success_criteria>

<output>
Create `.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-06-SUMMARY.md` when done
</output>
