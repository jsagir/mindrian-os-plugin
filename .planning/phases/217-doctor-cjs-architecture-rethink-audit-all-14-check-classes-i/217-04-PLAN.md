---
phase: 217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i
plan: 04
type: execute
wave: 3
depends_on: ["217-03"]
files_modified:
  - lib/core/doctor/cascade-rooms-module.cjs
  - lib/core/doctor/cascade-rooms-active-module.cjs
  - lib/core/doctor/room-md-module.cjs
  - data/doctor-modules.json
  - scripts/doctor.cjs
autonomous: true
requirements: [D-01, D-02, D-04]
must_haves:
  truths:
    - "doctor --cascade-rooms --fix ACTUALLY repairs missing .room-root sentinels - the class B fix that commands/doctor.md falsely claimed for months is now real and structural (closes the D-04 confirmed-wrong claim from the code side)"
    - "doctor --cascade-rooms and --room-md check paths behave identically to pre-migration (same statuses, same payload keys), driven by the registry"
    - "room-md --fix still routes through performRoomMdRecovery semantics (recovery record shape preserved, including its own tool field) via the engine's fix-then-recheck flow"
  artifacts:
    - path: "lib/core/doctor/cascade-rooms-module.cjs"
      provides: "class B runner: check + the NEWLY WIRED fix (creates missing .room-root sentinels)"
      exports: ["check", "fix"]
    - path: "lib/core/doctor/cascade-rooms-active-module.cjs"
      provides: "class C runner: positional simulateWritePath refactored to ctx opts"
      exports: ["check"]
    - path: "lib/core/doctor/room-md-module.cjs"
      provides: "class E runner: check + fix (performRoomMdRecovery moved in)"
      exports: ["check", "fix"]
  key_links:
    - from: "lib/core/doctor/cascade-rooms-module.cjs fix"
      to: "<room>/.room-root sentinel files"
      via: "fs.writeFileSync for registered rooms whose dir exists but sentinel is missing"
      pattern: "\\.room-root"
    - from: "lib/core/doctor/cascade-rooms-active-module.cjs"
      to: "ctx.flags.simulateWrite"
      via: "normCtx remap of the old positional arg"
      pattern: "simulateWrite"
    - from: "runAccumulativeEngine fix flow"
      to: "lib/core/doctor/room-md-module.cjs fix"
      via: "ctx.check_result (the just-run check result, the performRoomMdRecovery input)"
      pattern: "check_result"
---

<objective>
Third-wave migration (D-01/D-02): cascade-rooms (B), cascade-rooms-active (C), and
room-md (E). This wave carries the phase's headline payload: class B's --fix, which
commands/doctor.md has claimed since 95.1 but which was NEVER wired (confirmed RCA
.planning/debug/doctor-fix-class-b-unwired.md), gets implemented for real as the
module's fix(ctx) - making the doc claim true instead of deleting it (D-04's preferred
resolution for this line).

Purpose: kill the class B unwired-fix bug structurally; migrate the C positional
signature; move E's recovery into its runner.
Output: 3 runner files (2 with fix), 3 registry entries, smaller doctor.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-RESEARCH.md
@.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-PATTERNS.md
@.planning/debug/doctor-fix-class-b-unwired.md
@lib/core/doctor/umbilical-module.cjs
@lib/core/doctor/shared.cjs
</context>

## Artifacts this plan produces

| Artifact | Kind | Symbols |
|----------|------|---------|
| `lib/core/doctor/cascade-rooms-module.cjs` | NEW file | `check(ctx)` (moved checkCascadeRoomsSentinel), `fix(ctx)` (NEW: sentinel repair) |
| `lib/core/doctor/cascade-rooms-active-module.cjs` | NEW file | `check(ctx)` (moved checkCascadeRoomsActive, opts signature) |
| `lib/core/doctor/room-md-module.cjs` | NEW file | `check(ctx)` (moved checkRoomMd), `fix(ctx)` (moved performRoomMdRecovery) |
| `data/doctor-modules.json` | MODIFIED | entries: cascade-rooms (fix_supported true), cascade-rooms-active (fix_supported false), room-md (fix_supported true); all cadence always, flags cascadeRooms/cascadeRooms/roomMd, introduced_version 1.15.3 |
| `scripts/doctor.cjs` | MODIFIED (shrinks) | deleted: checkCascadeRoomsSentinel, checkCascadeRoomsActive, checkRoomMd, performRoomMdRecovery bodies; inline B/C block (~4962-4968), E block (~4977-4980), E fix dispatch (~5020-5030) |

<tasks>

<task type="auto">
  <name>Task 1: Create the B, C, E runner files (including the NEW class B fix)</name>
  <files>lib/core/doctor/cascade-rooms-module.cjs, lib/core/doctor/cascade-rooms-active-module.cjs, lib/core/doctor/room-md-module.cjs</files>
  <read_first>
    - lib/core/doctor/umbilical-module.cjs (envelope + the fix(ctx) contract: dryRun honored, projected/suggested/errors counters, detail on every path)
    - scripts/doctor.cjs: checkCascadeRoomsSentinel (~838-872: readRegistry, .room-root existence walk, missingSentinels), checkCascadeRoomsActive (~879-956: POSITIONAL simulateWritePath arg), checkRoomMd (~1003-1053), performRoomMdRecovery (~1055-1100: takes the check result as input, returns a recovery record with its own tool field)
    - scripts/doctor.cjs: main() blocks B/C (~4962-4968), E (~4977-4980), the E fix dispatch (~5020-5030: recovery pushed RAW onto report.recovered, then re-check)
    - The canonical .room-root writer: run `grep -rn "room-root" scripts/ lib/ --include="*.cjs" --include="*.sh" | grep -v doctor` to find what creates sentinels at room-creation time; mirror its exact content contract
    - .planning/debug/doctor-fix-class-b-unwired.md (the RCA: what the fix was supposed to do and why it was never dispatched)
    - lib/core/doctor/shared.cjs (readRegistry, findRoomRoot are exported there per Plan 01)
  </read_first>
  <action>
    Implements D-01 for B/C/E and closes the D-04 class-B false claim from the code
    side. All runners: umbilical envelope, require './shared.cjs' (readRegistry,
    findRoomRoot), never scripts/doctor.cjs. Module ids equal the existing
    report.checks keys.

    1. cascade-rooms-module.cjs:
       - check(ctx): move checkCascadeRoomsSentinel verbatim. ADD a non-empty detail on
         the ok path ('<okCount> room(s) carry .room-root') - the current ok return has
         none and D-03 rule 9 requires every path to carry one. Skip path already has
         detail.
       - fix(ctx) - THE NEW WIRING: read ctx.check_result (the engine passes the
         just-run check result per Plan 01). For each name in
         check_result.missingSentinels, resolve the room path exactly as check does
         (registry relPath against roomsHome). Two cases: (a) room dir EXISTS but
         .room-root missing: create the sentinel using the canonical content found via
         the read_first grep (mirror the room-creation writer's bytes; if the canonical
         writer writes an empty or marker file, mirror that); honor ctx.dryRun (count,
         write nothing); increment projected. (b) room dir itself MISSING: do NOT
         create directories (that is room restoration, not sentinel repair); push the
         name onto suggested with a 'room dir missing, not auto-created' note. Return
         { status, projected, suggested, errors, detail } per the umbilical fix
         contract, detail counting repaired/suggested/errors and noting dry-run.
    2. cascade-rooms-active-module.cjs: check(ctx) with the Pitfall-5 signature
       refactor: const simulateWrite = (ctx.flags && ctx.flags.simulateWrite) || ctx.simulateWrite || null;
       then the moved body of checkCascadeRoomsActive(simulateWritePath) using
       simulateWrite. Ensure a non-empty detail on every path. No fix export.
    3. room-md-module.cjs: check(ctx) = moved checkRoomMd (add an ok-path detail such
       as '<n> registered room(s) have ROOM.md' if the current ok return lacks one);
       fix(ctx) = moved performRoomMdRecovery invoked with ctx.check_result. PRESERVE
       the recovery record's existing shape verbatim, including its own tool field
       (the Plan 01 engine glue lets the fixer's tool field win over the module id) -
       the record main() used to push raw onto report.recovered must stay
       shape-identical. Add status ('ok' on success, 'error' with the caught message on
       throw) and a detail string if the record lacks them.
    4. Every moved body: re-base __dirname repo-root math for lib/core/doctor/
       (three '..' hops to repo root); preserve MINDRIAN_ROOMS_HOME env reads by exact
       name (hermetic class B/C tests depend on it).
  </action>
  <verify>
    <automated>node tests/lib-doctor-217-04-smoke.cjs 2>/dev/null || node -e "const fs=require('fs'),os=require('os'),p=require('path');const scratch=fs.mkdtempSync(p.join(os.tmpdir(),'d217-'));process.env.MINDRIAN_ROOMS_HOME=scratch;const base='/home/jsagi/dev/MindrianOS-Plugin/lib/core/doctor/';const mods=['cascade-rooms','cascade-rooms-active','room-md'];for(const m of mods){const r=require(base+m+'-module.cjs');if(typeof r.check!=='function')throw new Error(m+': no check');const res=r.check({home:scratch,running:'99.99.99',dryRun:true,fix:false,flags:{}});if(['ok','warn','error','skip'].indexOf(res.status)<0||typeof res.detail!=='string'||!res.detail)throw new Error(m+': bad contract '+JSON.stringify(res));}if(typeof require(base+'cascade-rooms-module.cjs').fix!=='function')throw new Error('class B fix missing');if(typeof require(base+'room-md-module.cjs').fix!=='function')throw new Error('class E fix missing');if(typeof require(base+'cascade-rooms-active-module.cjs').fix!=='undefined')throw new Error('class C must not export fix');console.log('217-04 task1 smoke ok')"</automated>
  </verify>
  <acceptance_criteria>
    - All three runners exist; B and E export check+fix, C exports check only (the verify one-liner asserts all of this)
    - Each check returns status-in-vocabulary + non-empty detail against a scratch MINDRIAN_ROOMS_HOME
    - `grep -c "scripts/doctor" lib/core/doctor/cascade-rooms-module.cjs lib/core/doctor/cascade-rooms-active-module.cjs lib/core/doctor/room-md-module.cjs` == 0 per file
    - cascade-rooms fix honors dryRun: a hermetic invocation with dryRun true against a fixture registry (scratch roomsHome, one room dir without .room-root) writes NO file and returns projected == 1 (assert manually via a node -e fixture or fold into Task 2's test run)
  </acceptance_criteria>
  <done>Three contract-compliant runners on disk with the class B fix implemented; wiring deferred to Task 2 so extraction and dispatch commit separately.</done>
</task>

<task type="auto">
  <name>Task 2: Wire B/C/E registry entries, delete inline blocks + dead bodies, run the class test regression set</name>
  <files>data/doctor-modules.json, scripts/doctor.cjs</files>
  <read_first>
    - data/doctor-modules.json (entries after Plan 03: umbilical, card-fire-health, ui-compliance, stale-first-touch-copy, deprecated-usage, plugin-enabled-state)
    - scripts/doctor.cjs: the exact blocks to delete - B/C inline (~4962-4968 under flags.cascadeRooms), E inline (~4977-4980 under flags.roomMd), E fix dispatch (~5020-5030) - and the now-dead function bodies checkCascadeRoomsSentinel, checkCascadeRoomsActive, checkRoomMd, performRoomMdRecovery (grep each name for remaining call sites first)
    - tests/test-doctor-class-b.cjs, tests/test-doctor-class-c.cjs, tests/test-doctor-class-e.cjs (the behavior pins: spawnSync harness, scratch MINDRIAN_ROOMS_HOME, JSON shape assertions)
  </read_first>
  <action>
    1. Append three entries to data/doctor-modules.json modules[], all
       "cadence": "always", "introduced_version": "1.15.3":
       - id "cascade-rooms", flag "cascadeRooms", fix_supported TRUE,
         runner lib/core/doctor/cascade-rooms-module.cjs
       - id "cascade-rooms-active", flag "cascadeRooms", fix_supported false,
         runner lib/core/doctor/cascade-rooms-active-module.cjs (B and C share the
         --cascade-rooms flag, exactly as today)
       - id "room-md", flag "roomMd", fix_supported TRUE,
         runner lib/core/doctor/room-md-module.cjs
    2. Delete from scripts/doctor.cjs: the B/C and E inline blocks, the E fix dispatch,
       and the four dead function bodies (zero remaining call sites confirmed by grep).
       The engine's fix-then-recheck flow (Plan 01) now owns what the E fix dispatch
       did: fix on warn, push recovery onto report.recovered, re-check.
    3. Extend tests/test-doctor-class-b.cjs with ONE new spawnSync sub-test pinning the
       NEW fix: scratch roomsHome fixture with a registered room dir lacking
       .room-root; run `node scripts/doctor.cjs --cascade-rooms --fix --json`; assert
       exit 0, checks['cascade-rooms'].status 'ok' after the fix-then-recheck, the
       .room-root file now exists on disk, and report.recovered contains an entry with
       tool 'cascade-rooms'. Also assert the dry-run variant (--fix --dry-run) writes
       nothing. (This is the regression pin for the phase's headline bug.)
    4. Run the regression set. If class b/c/e tests pin the old inline path in ways the
       migration legitimately changed (e.g. B previously had NO recovered entry under
       --fix), update only those expectations and record each in the summary.
  </action>
  <verify>
    <automated>node tests/test-doctor-module-contract-parity.cjs && node tests/test-doctor-class-b.cjs && node tests/test-doctor-class-c.cjs && node tests/test-doctor-class-e.cjs && node tests/test-doctor-module-selector.cjs && node tests/test-doctor-fix-renderer.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Contract-parity gate green with 9 registry entries
    - test-doctor-class-b (including the NEW fix sub-test), -c, -e, module-selector, fix-renderer all exit 0
    - `node scripts/doctor.cjs --cascade-rooms --json` emits top-level checks['cascade-rooms'] and checks['cascade-rooms-active'] rows with vocabulary statuses + non-empty detail
    - `grep -c "function checkCascadeRoomsSentinel\|function checkCascadeRoomsActive\|function checkRoomMd\|function performRoomMdRecovery" scripts/doctor.cjs` == 0
    - `node scripts/doctor.cjs --cascade-rooms` exits 0 (classFlagsActive exit-0 invariant intact)
  </acceptance_criteria>
  <done>B/C/E are registry-driven; the class B --fix works and is pinned by a spawnSync test; doctor.cjs is smaller; all class pins green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rooms registry JSON -> fix writes | cascade-rooms fix writes .room-root files at registry-derived paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-03 | Tampering | cascade-rooms fix path resolution | mitigate | resolve room paths exactly as check does (roomsHome join + existsSync); never create directories; dryRun honored; writes only the sentinel filename, never a caller-supplied path |
| T-217-01 | DoS (self) | new fix flow | mitigate | fix wrapped in engine try/catch; per-room try/catch inside fix (soft-fail per item) |
| T-217-SC | Tampering | package installs | accept | zero external packages installed this phase |
</threat_model>

<verification>
- Full touched-surface regression: contract-parity + class b/c/e + selector + fix-renderer green
- Known pre-existing class P/Q failures unchanged (2 and 6), untouched
</verification>

<success_criteria>
- Class B --fix real, structural, and pinned; C signature refactored; E recovery co-located with its check
- doctor.md's --fix class-B claim becomes TRUE in code (Plan 07 re-words the doc against this reality)
</success_criteria>

<output>
Create `.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/217-04-SUMMARY.md` when done
</output>
