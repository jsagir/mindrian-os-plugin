---
phase: 60-embedding-infrastructure
verified: 2026-04-07T23:59:00Z
status: gaps_resolved
score: 6/6 (gap resolved in Phase 67)
gaps:
  - truth: "Brain methodology/framework descriptions are embedded and cached as room/.mindrian/brain-baseline.json"
    status: partial
    resolved_by: 67-02
    resolution: "EMBED-02 infrastructure is complete. brain-baseline.json is correctly a runtime artifact (generated per-room). Previous milestone REQUIREMENTS.md no longer exists; v1.9.3 REQUIREMENTS.md tracks current requirements."
    reason: "brain-baseline.json is a runtime artifact (generated by running fetch-brain-baseline.py on a live room). No sample file exists in the repo and REQUIREMENTS.md still marks EMBED-02 as Pending/unchecked. The script infrastructure is complete and correct, but the cached artifact cannot be verified statically -- it only exists after the CJS+Python pipeline runs against a real room with Brain connected."
    artifacts:
      - path: "room/.mindrian/brain-baseline.json"
        issue: "Runtime-generated artifact. Does not exist in the repository. Script produces it correctly, but no sample or fixture is committed."
    missing:
      - "Update REQUIREMENTS.md to mark EMBED-02 as [x] complete now that the script infrastructure exists"
      - "Consider committing a sample brain-baseline.json fixture (empty or mock) so artifact presence can be verified without a live Brain connection"
human_verification:
  - test: "Run the full CJS-to-Python pipeline end-to-end: node scripts/fetch-brain-baseline.cjs --room ./room, then python3 scripts/fetch-brain-baseline.py --input room/.mindrian/brain-data.json --room ./room"
    expected: "room/.mindrian/brain-baseline.json created with model_name, model_dim, brain_version, and baselines array containing embedded framework vectors"
    why_human: "Requires a live room directory and either MINDRIAN_BRAIN_KEY env var or sample brain-data.json input file. Cannot verify end-to-end without the Brain connection or a fixture."
  - test: "Run python3 scripts/compute-whitespace-embeddings.py /path/to/room --verify-baseline after running both embedding pipelines"
    expected: "Output confirms room artifacts and Brain baselines have matching model_dim, cosine similarity computation succeeds, script exits 0"
    why_human: "Requires both embedding JSON files to exist, which requires running the full pipeline. Cannot verify without a populated room."
---

# Phase 60: Embedding Infrastructure Verification Report

**Phase Goal:** Room artifacts and Brain methodology descriptions exist as comparable vectors in the same 768-dim semantic space
**Verified:** 2026-04-07T23:59:00Z
**Status:** gaps_found (5/6 must-haves verified -- runtime artifact gap only)
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Running compute-whitespace-embeddings.py on a room produces a JSON file with 768-dim vectors for every artifact | ✓ VERIFIED | Script exists (459 lines), syntactically valid, all required functions present, outputs to room/.mindrian/whitespace-embeddings.json, uses BAAI/llm-embedder (768d) primary |
| 2 | When llm-embedder is unavailable, script falls back to MiniLM-L6-v2 and still produces cached embeddings | ✓ VERIFIED | load_embedding_model() tries BAAI/llm-embedder first (lines 214-236), catches all exceptions, falls back to all-MiniLM-L6-v2 -- both paths return (model, model_name, model_dim) |
| 3 | Re-running on unchanged artifacts skips computation (cache hit) | ✓ VERIFIED | check_embedding_cache() at line 154 checks content hashes AND model_name before loading model; "cache hit" message printed and exits 0 |
| 4 | Brain methodology/framework descriptions are embedded and cached as room/.mindrian/brain-baseline.json | ? PARTIAL | Script infrastructure complete and correct. fetch-brain-baseline.cjs (176 lines) queries Brain Neo4j, fetch-brain-baseline.py (355 lines) embeds and writes brain-baseline.json. Artifact cannot be verified statically -- it is runtime-generated. REQUIREMENTS.md still shows EMBED-02 as [ ] Pending. |
| 5 | Brain baseline and room artifact embeddings use the same model and dimensionality | ✓ VERIFIED | fetch-brain-baseline.py reads whitespace-embeddings.json metadata first to match model (lines 173-186). Both PRIMARY_MODEL and FALLBACK_MODEL constants match across both scripts. |
| 6 | Both embedding files can be loaded together and cosine similarity computed | ✓ VERIFIED | --verify-baseline flag added to compute-whitespace-embeddings.py (lines 252-337). Loads both JSON files, checks model_dim match (fatal), model_name match (warning), computes cosine_similarity sample, prints "cosine-comparable" summary. |

**Score:** 5/6 truths verified (1 partial -- runtime artifact)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/compute-whitespace-embeddings.py` | Room artifact embedding pipeline with llm-embedder primary, MiniLM fallback | ✓ VERIFIED | 459 lines. All 9 functions present: extract_title, extract_body, discover_artifacts, compute_content_hashes, check_embedding_cache, load_embedding_model, embed_artifacts, verify_baseline_compatibility, main. Passes syntax check and argparse check. |
| `requirements-whitespace.txt` | Python dependencies for whitespace pipeline, must contain sentence-transformers | ✓ VERIFIED | 6 lines, all 6 required dependencies present: scikit-learn>=1.3.0, numpy>=1.24.0, sentence-transformers>=2.2.0, umap-learn>=0.5.6, hdbscan>=0.8.38, scipy>=1.10.0 |
| `scripts/fetch-brain-baseline.py` | Fetches Brain framework descriptions via MCP, embeds them, caches as JSON | ✓ VERIFIED | 355 lines (exceeds 100-line min). All required functions: load_brain_data, compute_brain_hash, check_baseline_cache, load_embedding_model, embed_baselines, main. Syntactically valid. |
| `scripts/fetch-brain-baseline.cjs` | CJS CLI tool using brain-client.cjs (added as auto-fix deviation) | ✓ VERIFIED | 176 lines. Syntactically valid JS. Queries Neo4j via brain-client.cjs, graceful degradation on Brain unavailable. --help shows correct usage. |
| `room/.mindrian/brain-baseline.json` | Cached Brain methodology embeddings (generated at runtime), must contain model_name | ✗ MISSING | Runtime artifact. Not present in repo. Only produced after running fetch-brain-baseline.cjs + fetch-brain-baseline.py against a live room. |
| `tests/test_whitespace_embeddings.py` | 7 tests covering structure, skip logic, content hashing, argparse, output format | ✓ VERIFIED | 124 lines. Syntactically valid. Created as part of TDD approach in Plan 01. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| scripts/compute-whitespace-embeddings.py | room/.mindrian/whitespace-embeddings.json | JSON file write | ✓ WIRED | Pattern "whitespace-embeddings.json" found at lines 261, 352, 377. output_path.write_text() at line 450. mkdir(parents=True) at line 449. |
| scripts/compute-whitespace-embeddings.py | scripts/compute-hsi.py | Reuses discover_artifacts pattern | ✓ WIRED | discover_artifacts() at line 90 mirrors compute-hsi.py skip logic exactly. SKIP_FILES and SKIP_DIRS constants defined at lines 61-62. Plan notes "copy logic from compute-hsi.py." |
| scripts/fetch-brain-baseline.py | room/.mindrian/brain-baseline.json | JSON file write | ✓ WIRED | Pattern "brain-baseline.json" in output path at lines 257, 271. output_path.write_text() at line 345. mkdir(parents=True) at line 344. |
| scripts/fetch-brain-baseline.py | references/brain/query-patterns.md | Cypher query pattern "Framework.*description" | ✓ WIRED | fetch-brain-baseline.cjs (the CJS bridge) contains the Cypher query at lines 99-103: MATCH (f:Framework) WHERE f.description IS NOT NULL... The pattern is in the CJS bridge, not the Python file, which is correct per the CJS-to-Python design. |
| room/.mindrian/brain-baseline.json | room/.mindrian/whitespace-embeddings.json | Same model_dim, cosine-comparable vectors | ? UNVERIFIABLE | Code wiring exists: verify_baseline_compatibility() checks model_dim match between both JSON files (lines 284-305). But since brain-baseline.json is a runtime artifact that doesn't exist in the repo, the cross-file link cannot be statically confirmed. Logic is correct. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| compute-whitespace-embeddings.py | artifacts list | discover_artifacts(room_dir) walks actual room .md files | Yes -- real file system traversal with content hash | ✓ FLOWING |
| compute-whitespace-embeddings.py | vectors list | model.encode(texts) via SentenceTransformer | Yes -- actual model inference, not hardcoded | ✓ FLOWING |
| fetch-brain-baseline.py | frameworks list | load_brain_data(args.input) reads input JSON from CJS bridge | Yes -- real JSON parse with filter for descriptions >20 chars | ✓ FLOWING |
| fetch-brain-baseline.cjs | frameworks array | Brain Neo4j Cypher query via brain-client.cjs | Yes -- real database query, graceful empty on unavailability | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| compute-whitespace-embeddings.py shows correct argparse with --verify-baseline | python3 scripts/compute-whitespace-embeddings.py --help | Shows room_dir positional, --model, --output, --verify-baseline flags | ✓ PASS |
| fetch-brain-baseline.py shows correct argparse | python3 scripts/fetch-brain-baseline.py --help | Shows --input (required), --room (required), --model, --output | ✓ PASS |
| fetch-brain-baseline.cjs shows correct usage | node scripts/fetch-brain-baseline.cjs --help | Shows --room (required), --output, --help | ✓ PASS |
| compute-whitespace-embeddings.py Python syntax valid | python3 -c "import ast; ast.parse(...)" | syntax ok | ✓ PASS |
| fetch-brain-baseline.py Python syntax valid | python3 -c "import ast; ast.parse(...)" | syntax ok | ✓ PASS |
| fetch-brain-baseline.cjs JS syntax valid | node -c | syntax ok | ✓ PASS |
| All required functions present in both scripts | ast.walk() check | All functions confirmed: discover_artifacts, compute_content_hashes, load_embedding_model, embed_artifacts, main in compute script; load_brain_data, embed_baselines, main in baseline script | ✓ PASS |
| brain-client.cjs dependency exists | ls lib/core/ | brain-client.cjs (14,092 bytes) found at lib/core/brain-client.cjs | ✓ PASS |
| End-to-end pipeline run | CJS + Python pipeline | Cannot run without live room dir and Brain connection | ? SKIP |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| EMBED-01 | 60-01-PLAN.md | Room artifacts embedded using BAAI/llm-embedder (768-dim) via sentence-transformers, with MiniLM fallback for Tier 0 | ✓ SATISFIED | compute-whitespace-embeddings.py implements dual-model strategy. PRIMARY_MODEL="BAAI/llm-embedder" (768-dim), FALLBACK_MODEL="all-MiniLM-L6-v2" (384-dim). Committed e75a869 + e340443. REQUIREMENTS.md marks as [x] complete. |
| EMBED-02 | 60-02-PLAN.md | Brain methodology/framework descriptions embedded as consensus baseline ("semantic universe") -- cached locally as JSON | ✗ BLOCKED (stale status) | Script infrastructure is COMPLETE: fetch-brain-baseline.cjs + fetch-brain-baseline.py both exist, are syntactically valid, and implement the correct pipeline. However REQUIREMENTS.md still marks EMBED-02 as `[ ]` Pending (line 11) and "Pending" in the status table (line 65). The cached artifact (brain-baseline.json) only exists at runtime -- this is by design ("generated at runtime" per plan) but creates an unverifiable gap in static analysis. |

**REQUIREMENTS.md stale status flag:** EMBED-02 implementation is complete at the script level. The checkbox `[ ]` at line 11 and "Pending" at line 65 of REQUIREMENTS.md were not updated after Plan 02 completed. This is a documentation staleness issue, not an implementation gap.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | -- | No TODOs, FIXMEs, placeholders, empty returns, or hardcoded stubs detected in any of the 4 phase artifacts | -- | -- |

### Human Verification Required

#### 1. End-to-End CJS-to-Python Pipeline

**Test:** Run `node scripts/fetch-brain-baseline.cjs --room ./room` (requires MINDRIAN_BRAIN_KEY env var), then `python3 scripts/fetch-brain-baseline.py --input room/.mindrian/brain-data.json --room ./room`
**Expected:** room/.mindrian/brain-baseline.json created with model_name matching the room's whitespace-embeddings.json, model_dim matching, baselines array non-empty with float vectors
**Why human:** Requires MINDRIAN_BRAIN_KEY and live Brain Neo4j connection, or a sample brain-data.json fixture to use as --input

#### 2. Cosine Comparability Confirmation

**Test:** After running both pipelines above, run `python3 scripts/compute-whitespace-embeddings.py /path/to/room --verify-baseline`
**Expected:** Output shows matching model_dim for both files, cosine similarity printed (any numeric value between -1 and 1), script exits 0 with "cosine-comparable" summary
**Why human:** Requires both JSON cache files to exist, which requires a populated room with real artifacts

### Gaps Summary

Phase 60 has achieved its structural goal: the embedding infrastructure scripts exist, are substantive (459 + 355 + 176 lines), syntactically valid, follow compute-hsi.py conventions, and implement the correct dual-model strategy with cache invalidation, model-consistency enforcement, and cosine compatibility verification.

One gap exists: the PLAN 02 must_haves list `room/.mindrian/brain-baseline.json` as an artifact that "contains: model_name." This file is marked "generated at runtime" in the plan itself, and no sample fixture is committed to the repo. Static verification confirms the code that generates it is correct, but the artifact itself cannot be checked without running the pipeline.

A secondary documentation gap: REQUIREMENTS.md was not updated after Plan 02 completed. EMBED-02 remains `[ ]` Pending at line 11 and "Pending" at line 65, despite the implementation being complete.

**Root cause of both gaps:** The brain-baseline.json artifact requires a live Brain connection to generate, and no mechanism (mock fixture, CI seed data) was put in place to verify its existence without running the full pipeline.

---

_Verified: 2026-04-07T23:59:00Z_
_Verifier: Claude (gsd-verifier)_
