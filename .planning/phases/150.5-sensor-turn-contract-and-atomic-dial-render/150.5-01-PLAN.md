---
phase: 150.5-sensor-turn-contract-and-atomic-dial-render
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/insight-sensors.cjs
  - tests/test-150-5-sensor-firability.cjs
autonomous: true
requirements: [SENS-FIX-01, SENS-FIX-02]
canon_parts: [Part 2, Part 7, Part 8, Part 9]

must_haves:
  truths:
    - "Every one of the 8 registered sensors has a GREEN fire arm driven through the REAL dispatchSensors with the EXACT production turn shape {userText, sectionPath, sessionId} -- never a hand-shaped text/signals key on the turn object itself"
    - "The 5 structurally-dead sensors (lagging-component, external-fact, first-material, artifact-filed, methodology-decision) fire via ONE seam: dispatchSensors normalizes its input at entry (text aliased from userText; signals derived from the two CONTEXT-named shipped side-channels and merged additively with caller-supplied signals)"
    - "Every sensor has an honest-negative arm: absent fuel means no fire; a STALE side-channel (mtime outside the freshness window) does not fire"
    - "The caller's turn object is NEVER mutated by normalization (no text/signals keys appear on the original object after dispatch)"
    - "The Phase 144 fence (sensors never mutate routing_source) and the D-03a fence (tests/test-retrieval-seed.cjs) stay green; run-all-144.sh stays 5/5"
  artifacts:
    - path: "lib/core/insight-sensors.cjs"
      provides: "normalizeTurn + deriveTurnSignals + SIGNAL_FRESHNESS_MS exported; dispatchSensors normalizes at entry"
      contains: "normalizeTurn"
    - path: "tests/test-150-5-sensor-firability.cjs"
      provides: "8 sensors x (fire arm + honest-negative arm) = 16 arms driven through the real dispatchSensors with the production turn shape, plus the turn-builder drift fence and the no-mutation fence"
      min_lines: 150
  key_links:
    - from: "lib/core/insight-sensors.cjs dispatchSensors"
      to: "normalizeTurn(turn, ctx)"
      via: "entry-point call before the SENSOR_REGISTRY loop"
      pattern: "normalizeTurn"
    - from: "lib/core/insight-sensors.cjs deriveTurnSignals"
      to: "<roomDir>/.mindrian/last-cascade.json + <roomDir>/.mindrian/auto-explore-*.json"
      via: "fs existsSync + statSync mtime freshness gate + readJsonSafe"
      pattern: "last-cascade|auto-explore"
    - from: "tests/test-150-5-sensor-firability.cjs"
      to: "scripts/intent-classifier.cjs turn builder (~:1351)"
      via: "source-grep drift fence asserting the production builder still constructs exactly {userText, sectionPath, sessionId}"
      pattern: "userText"
---

<objective>
SENS-FIX-01 + SENS-FIX-02: repair the sensor turn-contract break at ONE seam and
prove per-sensor firability with a RED-first regression suite that drives the
EXACT production turn shape.

The 2026-06-09 fan-out proved 5 of 8 production sensors are STRUCTURALLY DEAD:
the hook builds turn = { userText, sectionPath, sessionId } at
scripts/intent-classifier.cjs:1351, but sensor-lagging-component and
sensor-external-fact read turn.text || turn.utterance (key mismatch), and
sensorFirstMaterial / sensorArtifactFiled / sensorMethodologyDecision need a
turn.signals field no producer populates. Net effect: fire_skill stays null,
routing_source stays legacy, the dial never renders for a real navigator.

Seam decision (planner discretion per 150.5-CONTEXT.md, ONE seam): the
DISPATCHER-side seam -- dispatchSensors (lib/core/insight-sensors.cjs:239)
normalizes its input at entry. Justification: it fixes ALL callers at once
(decide() in navigation-engine.cjs:657, the ACPT drivers, any future consumer),
keeps the hook's turn object byte-unchanged (the strongest D-03a posture: the
derived text alias and signals NEVER exist on the object that flows around the
classifier toward buildBrainPacket), and the normalization lives in the same
file as the registry it serves. The hook-side alternative would fix only one
caller and add fields to the LOCAL-lane turn object.

Signals fuel (per 150.5-CONTEXT.md fix shape -- exactly the two named shipped
side-channels): a FRESH <roomDir>/.mindrian/last-cascade.json with non-empty
proactive_intelligence.newFindings derives 'artifact_filed'; a FRESH
<roomDir>/.mindrian/auto-explore-<material_id>.json fire marker derives
'first_material'. Caller-supplied turn.signals are preserved and merged
additively (so the methodology-decision signal kinds, whose producers are the
caller layer, are guaranteed delivery through the same lane -- the structural
starvation is gone for all three signals-trio sensors). No third derivation is
invented: the CONTEXT names exactly two side-channel fuels.

Purpose: gate 4 of the 13-gate trigger chain (dispatchSensors >= 1 fired reach)
becomes passable on a real machine.
Output: the normalized seam in insight-sensors.cjs + the 16-arm firability suite,
landed RED-first (the RED commit proves the 5 dead sensors on current main).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-CONTEXT.md
@.planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-RESEARCH.md
@.planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-VALIDATION.md

# The seam file (the 8-sensor registry + dispatchSensors, the ONE edit site):
@lib/core/insight-sensors.cjs
# The production turn builder the suite fixtures from (read-only; ~:1351):
@scripts/intent-classifier.cjs
# The 5 dead + 3 live sensors (read for exact fire preconditions; NEVER edited):
@lib/core/sensors/sensor-lagging-component.cjs
@lib/core/sensors/sensor-external-fact.cjs
@lib/core/sensors/sensor-methodology-decision.cjs
@lib/core/sensors/sensor-gate-approach.cjs
@lib/core/sensors/sensor-jtbd-reweight.cjs
@lib/core/sensors/sensor-memory-cortex.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- tests/test-150-5-sensor-firability.cjs (16 arms, production turn shape, drift fence)</name>
  <files>tests/test-150-5-sensor-firability.cjs</files>
  <read_first>
    - 150.5-RESEARCH.md Finding A (the dead-sensor table + the 3 live sensors' fuel) and the "Trigger chain" section gate 4 (the gate this suite samples on both sides)
    - lib/core/insight-sensors.cjs (dispatchSensors at :239 -- the unit under test; SENSOR_REGISTRY order; hasSignal/readJsonSafe helpers; sensorArtifactFiled's own side-channel re-read at :166-202)
    - scripts/intent-classifier.cjs :1349-1355 (the production turn builder: userText: conversationSeed, sectionPath, sessionId -- the EXACT shape the suite fixtures; the drift fence greps this source)
    - lib/core/sensors/sensor-lagging-component.cjs :49-71 (LAGGING_PATTERNS + the turn.text || turn.utterance read) and lib/core/sensors/sensor-external-fact.cjs :64-71 (same read shape; fire needs an external-fact phrasing like the ACPT-02 'competitor' turn)
    - lib/core/sensors/sensor-methodology-decision.cjs :44-66 (DECISION_SIGNALS kinds; reads turn.signals only)
    - lib/core/sensors/sensor-gate-approach.cjs (fire needs tuple.stage in the commit-near set, e.g. 'ready to build')
    - lib/core/sensors/sensor-jtbd-reweight.cjs + lib/hmi/jtbd-state.cjs (fire needs a REAL jtbd transition in <roomDir>/.mindrian/jtbd-state.json: priorSlug !== currentSlug -- read jtbd-state.cjs for the exact file shape before writing the fixture)
    - lib/core/sensors/sensor-memory-cortex.cjs :22-30 (fire needs ctx.staleGoverningThought === true OR ctx.freshContradictions > 0)
    - tests/test-acpt-01-engine-fires.cjs (the ok/fail counter + process.exit suite style + the fictional-fixture rule to mirror)
  </read_first>
  <behavior>
    - The suite builds the production turn EXACTLY as the hook does: turn = { userText: fixtureText, sectionPath: null, sessionId: 'test-150-5' }. It NEVER sets turn.text, turn.utterance, or turn.signals on the production-shaped turn except in the two methodology-lane arms (below) where signals are supplied to prove the merge lane.
    - Drift fence: read scripts/intent-classifier.cjs source and assert the builder still constructs exactly the three keys -- the source must match /userText:\s*conversationSeed/ and /sectionPath:\s*sectionPath/ and /sessionId:\s*sessionId/ inside the turn-builder block, and the builder block must NOT contain /\btext:/ or /\bsignals:/ (so the fixture turn cannot silently diverge from production; if the hook ever enriches the turn, this fence flags the suite for re-derivation).
    - 8 fire arms (assert by reach.signal membership in the dispatchSensors result): lagging_component (userText carries 'the data pipeline is the bottleneck'); external_fact (userText carries the ACPT-02-style 'how does our competitor position against the market leader?'); first_material (fresh .mindrian/auto-explore-fixture.json marker in a tmp roomDir threaded via ctx.roomDir); artifact_filed (fresh .mindrian/last-cascade.json with proactive_intelligence.newFindings: [{type:'CONTRADICT', confidence:0.9}]); methodology_decision (production-shaped turn PLUS signals:['methodology_completed'] supplied on the turn -- proves the merge lane delivers caller-supplied signals through the seam untouched); gate_approach (tuple {stage:'ready to build'}); jtbd_changed (real transition written to .mindrian/jtbd-state.json per the jtbd-state.cjs shape); memory_cortex (ctx.staleGoverningThought = true).
    - 8 honest-negative arms: neutral text fires neither pattern sensor; NO side-channel (or empty newFindings) fires neither file-fuel sensor; a STALE side-channel (fs.utimesSync backdated beyond SIGNAL_FRESHNESS_MS) does NOT derive artifact_filed/first_material; no decision signal -> methodology silent; stage 'exploring' -> gate silent; no jtbd transition -> reweight silent; no cortex scalars -> cortex silent.
    - No-mutation fence: capture Object.keys(turn) + JSON.stringify(turn) before dispatch; assert both identical after dispatch (normalization must copy, never mutate -- the D-03a structural guarantee).
    - Fixtures: tmp roomDirs under os.tmpdir(), obviously-fictional content only, cleaned in finally. No em-dash / en-dash in the file (reference forbidden codepoints via String.fromCharCode if asserted).
  </behavior>
  <action>
    Create tests/test-150-5-sensor-firability.cjs as a plain node-runnable CJS suite (ok/fail counters, process.exit(failed === 0 ? 0 : 1), mirroring tests/test-acpt-01-engine-fires.cjs structure) implementing every behavior arm above. Require lib/core/insight-sensors.cjs and drive the REAL dispatchSensors -- never a reimplementation, never per-sensor direct calls for the fire arms (the dispatch chokepoint is the unit under test; per-sensor membership is asserted on the returned reach array via reach.signal). RED-by-design: on current main the 5 dead-sensor fire arms FAIL (lagging_component, external_fact, first_material, artifact_filed, and the merge-lane methodology arm passes already via passthrough -- assert it anyway as the lane regression) and the SIGNAL_FRESHNESS_MS / normalizeTurn imports are absent (guard the require so a missing export reports FAILED, not a crash). Commit this task alone as the RED commit: `test(150.5-01): RED firability suite proves the 5 dead sensors`.
  </action>
  <verify>
    <automated>node tests/test-150-5-sensor-firability.cjs; test $? -ne 0 && echo "PASS (RED-by-design on main)"</automated>
  </verify>
  <done>The suite exists, runs to completion without crashing, reports the dead-sensor fire arms as FAILED on current main, and is committed as the RED commit before Task 2 lands.</done>
  <acceptance_criteria>
    - node tests/test-150-5-sensor-firability.cjs exits non-zero on pre-seam main and its stdout lists at least 4 FAILED fire arms naming lagging_component, external_fact, first_material, artifact_filed
    - grep -c "dispatchSensors" tests/test-150-5-sensor-firability.cjs returns >= 8 (every fire arm drives the real chokepoint)
    - The drift-fence regexes /userText:\s*conversationSeed/ and the no-text/no-signals builder assertions are present verbatim in the suite source
    - grep -P '\x{2014}|\x{2013}' tests/test-150-5-sensor-firability.cjs returns no matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GREEN -- the one-seam normalization in dispatchSensors (normalizeTurn + deriveTurnSignals)</name>
  <files>lib/core/insight-sensors.cjs</files>
  <read_first>
    - tests/test-150-5-sensor-firability.cjs (Task 1 -- the contract this task turns GREEN; do not weaken any arm)
    - lib/core/insight-sensors.cjs in full (the HARD FENCES header comment at :19-27 -- the Phase 144 routing fence and Part 8 rule this edit must preserve; readJsonSafe at :65; hasSignal at :80; dispatchSensors at :239)
    - lib/core/navigation-engine.cjs :616-663 (how decide() threads sensorCtx { roomDir, lowFillSections, staleGoverningThought, freshContradictions } -- ctx.roomDir is the side-channel root deriveTurnSignals reads)
    - scripts/post-write :87-126 (the last-cascade.json atomic writer + payload shape: proactive_intelligence is the field to gate on)
    - 150.5-RESEARCH.md "Signal fuel sources that already exist" (the two named fuels; the Part 144 fence at insight-sensors.cjs:20)
  </read_first>
  <action>
    In lib/core/insight-sensors.cjs, add and export three symbols, then wire dispatchSensors:

    1. SIGNAL_FRESHNESS_MS = 30 * 60 * 1000 -- the side-channel freshness window. A derived signal fires only when the source file's mtime is within this window (prevents a days-old cascade or auto-explore marker from re-firing the engine arm on every turn of every later session -- the stale-refire failure the honest-negative arms assert).

    2. deriveTurnSignals(ctx) -> string[] -- pure-LOCAL, sync, soft-fail-to-[] derivation from EXACTLY the two CONTEXT-named shipped side-channels under ctx.roomDir: (a) 'artifact_filed' when <roomDir>/.mindrian/last-cascade.json exists, is fresh per SIGNAL_FRESHNESS_MS, parses via readJsonSafe, and proactive_intelligence.newFindings is a non-empty array (the same gate sensorArtifactFiled re-checks, so signal and sensor agree); (b) 'first_material' when any <roomDir>/.mindrian/auto-explore-*.json fire marker exists and is fresh (existsSync/readdirSync + statSync mtime; stat before parse for speed -- the whole derivation must stay trivially inside the 1200ms NAV budget). Returns signal KIND strings only -- closed enums, never file content, never prose (threat T1).

    3. normalizeTurn(turn, ctx) -> a SHALLOW ENRICHED COPY (Object.assign({}, turn) base): text = turn.text || turn.utterance || turn.userText || '' (existing text/utterance always win -- callers that already normalize are byte-stable); signals = the deduplicated union of the caller's turn.signals array (preserved verbatim, objects included) and deriveTurnSignals(ctx). NEVER mutates the input turn (the D-03a structural guarantee: the enriched fields exist only on the copy that lives inside the dispatch).

    4. In dispatchSensors(turn, tuple, ctx), normalize ONCE at entry (const normalized = normalizeTurn(turn, ctx), wrapped so a normalization fault degrades to the raw turn -- soft-fail discipline) and pass `normalized` to every sensor in the registry loop. No sensor file is edited (one seam, not eight patches, per 150.5-CONTEXT.md). Do NOT touch trace.routing_source, do NOT call decide(), do NOT alter REACH_IDS/POSTURE_IDS (the Phase 144 fence at :20 and the frozen reach bank stay byte-intact). Extend the module header comment with two lines documenting the seam and the two derived fuels. Export normalizeTurn, deriveTurnSignals, SIGNAL_FRESHNESS_MS from module.exports for the suite. No em-dashes anywhere.

    Commit as the GREEN commit: `feat(150.5-01): one-seam turn normalization revives the 5 dead sensors`.
  </action>
  <verify>
    <automated>node tests/test-150-5-sensor-firability.cjs && bash tests/run-all-144.sh && node tests/test-retrieval-seed.cjs && node tests/test-sensors-routing-fence.cjs</automated>
  </verify>
  <done>All 16 firability arms GREEN; run-all-144.sh 5/5 (the existing ACPT/NAV drivers that hand-supply signals stay green via the additive merge); the D-03a fence (test-retrieval-seed.cjs) and the 143 sensor-module routing fence stay green; the original turn object is provably unmutated.</done>
  <acceptance_criteria>
    - node tests/test-150-5-sensor-firability.cjs exits 0 (16/16 arms)
    - bash tests/run-all-144.sh exits 0 and node tests/test-retrieval-seed.cjs exits 0 and node tests/test-sensors-routing-fence.cjs exits 0
    - node -e "const s=require('./lib/core/insight-sensors.cjs'); if(typeof s.normalizeTurn!=='function'||typeof s.deriveTurnSignals!=='function'||typeof s.SIGNAL_FRESHNESS_MS!=='number')process.exit(1)" exits 0
    - node -e "const s=require('./lib/core/insight-sensors.cjs'); const t={userText:'x'}; s.dispatchSensors(t,{},{}); if(Object.keys(t).length!==1)process.exit(1)" exits 0 (no caller-turn mutation)
    - git log --oneline -2 shows the RED commit preceding the GREEN commit
    - grep -P '\x{2014}|\x{2013}' lib/core/insight-sensors.cjs returns no matches
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| side-channel files -> derived signals | LOCAL .mindrian JSON crosses into the sensor dispatch as signal-kind enums only |
| normalized turn copy -> sensors | the enriched text/signals exist ONLY on the in-dispatch copy, never on the caller's turn object (the object that flows near the buildBrainPacket lane) |
| fixture roomDirs -> disk | tmp fictional fixtures under os.tmpdir(), cleaned in finally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.5-01 | Information Disclosure | deriveTurnSignals / normalizeTurn (threat T1: user prose toward a Brain packet lane) | mitigate | derived signals are closed-enum kind strings only ('artifact_filed' / 'first_material'); normalizeTurn returns a copy and the suite's no-mutation fence proves the caller turn gains zero keys; tests/test-retrieval-seed.cjs (the D-03a fence) re-run in Task 2 verify must stay green |
| T-150.5-02 | Spoofing (false fire) | stale side-channels re-firing the engine arm forever | mitigate | SIGNAL_FRESHNESS_MS mtime gate; the stale honest-negative arms (utimesSync-backdated fixtures) assert no fire |
| T-150.5-03 | Tampering | the Phase 144 routing fence weakened by the seam edit | mitigate | dispatchSensors still never touches routing_source/decide(); tests/test-sensors-routing-fence.cjs re-run in Task 2 verify |
| T-150.5-04 | Denial of Service | per-turn fs derivation blowing the 1200ms NAV budget | mitigate | stat-before-parse, two files max, soft-fail-to-[] on any fault |
| T-150.5-SC | Tampering | npm/pip/cargo installs | accept | zero package installs (node built-ins + existing project libs only); no legitimacy checkpoint required |
</threat_model>

## Artifacts this phase plan produces

| Artifact | Kind | Symbol(s) |
|---|---|---|
| lib/core/insight-sensors.cjs | edited module | normalizeTurn(turn, ctx), deriveTurnSignals(ctx), SIGNAL_FRESHNESS_MS (all newly exported); dispatchSensors normalizes at entry |
| tests/test-150-5-sensor-firability.cjs | new test suite | 16 arms (8 fire + 8 honest-negative), turn-builder drift fence, no-mutation fence |

<verification>
- node tests/test-150-5-sensor-firability.cjs exits 0 (post-Task-2) -- 8 fire + 8 honest-negative arms through the REAL dispatchSensors with the production turn shape
- bash tests/run-all-144.sh exits 0 (the engine-flip floor unbroken)
- node tests/test-retrieval-seed.cjs exits 0 (D-03a: the conversation seed never reaches buildBrainPacket)
- node tests/test-sensors-routing-fence.cjs exits 0 (the Phase 144 sensor-module fence)
- Two commits: RED (suite proving the dead sensors) then GREEN (the seam)
- Zero em-dashes/en-dashes in both touched files
</verification>

<success_criteria>
SENS-FIX-01: all 8 sensors are reachable from the production turn shape through
the single dispatcher-side seam; the two CONTEXT-named side-channels feed the
signals lane; the merge lane delivers caller-supplied signals additively.
SENS-FIX-02: the regression suite drives the EXACT hook-built turn shape (with
a source-level drift fence on the builder), one fire arm + one honest-negative
arm per registered sensor, RED-first then GREEN. All carried fences stay green.
</success_criteria>

<output>
Create `.planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-01-SUMMARY.md` when done
</output>
