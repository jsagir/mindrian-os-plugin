---
phase: "150.5"
slug: sensor-turn-contract-and-atomic-dial-render
created: 2026-06-09
source: condensed from the 2026-06-09 8-agent prior-art fan-out (7 phase-cluster
  investigators over phases 140-150 + 1 live trigger-chain diagnostician;
  1.46M tokens, 242 tool reads) -- NOT a fresh researcher spawn; the navigator
  selected condensation over re-derivation
status: RESEARCH COMPLETE
---

# Phase 150.5 Research -- Sensor Turn-Contract Repair + Atomic Dial Render Coupling

## What I need to know to PLAN this phase well (the answer)

The phase has TWO defects and ONE decision. Both defects are seam-narrow (one
file each at the core), both have shipped test harnesses to extend rather than
new harnesses to invent, and the decision (DIAL-ATOM-03) must be made BEFORE
the render plan is written because two requirements pull on the same gate.

## Finding A -- the sensor turn-contract break (SENS-FIX-01/02)

### The production turn shape

scripts/intent-classifier.cjs:1351 builds the turn the hook passes into the
engine: `{ userText, sectionPath, sessionId }`. Phase 150-04 additionally
threads `stage`, `roomDir`, `lowFillSections` into the decide() ctx
(intent-classifier.cjs:1324-1372, per 150-VERIFICATION MEM-03/MEM-07).

### The dead sensors (lib/core/insight-sensors.cjs)

| Sensor | Reads | Defect class |
|---|---|---|
| sensor-lagging-component | `turn.text \|\| turn.utterance` | key mismatch (userText never read) |
| sensor-external-fact | `turn.text \|\| turn.utterance` | key mismatch |
| sensorFirstMaterial | `turn.signals` ('first_material') | signals never populated by hook |
| sensorArtifactFiled | `turn.signals` ('artifact_filed') | signals never populated by hook |
| sensorMethodologyDecision | `turn.signals` | signals never populated by hook |

The 3 live-firable sensors and their fuel:
- sensorGateApproach: needs `tuple.stage` (USER.md venture_stage) in the
  commit-near set ('well-defined problem', 'ready to build', 'investment',
  'commit', 'pre-commit')
- sensorJtbdReweight: needs a REAL jtbd transition (priorSlug !== currentSlug
  in .mindrian/jtbd-state.json)
- sensorMemoryCortex: needs staleGoverningThought OR freshContradictions>0
  from ctx.cortexNodes (the 150 legD projection)

### Signal fuel sources that already exist (for populating turn.signals)

- `.mindrian/last-cascade.json` `proactive_intelligence.newFindings` -- written
  by scripts/post-write on filing; THE ACPT-04 contract (146-01); currently
  observed live with `proactive_intelligence: null` even when cascade runs
- auto-explore fire markers (Phase 117 computeMaterialId +
  composeAutoExploreFinding -- the ACPT-03 first-material substrate)
- dispatchSensors is pure/sync/LOCAL-first (insight-sensors.cjs) and NEVER
  mutates trace.routing_source (the Phase 144 fence, insight-sensors.cjs:20)

### Fix-shape constraint

Normalize at ONE seam, not eight sensor patches. Two candidate seams:
(a) the hook-side turn builder (intent-classifier.cjs:1351) enriches
`{userText, text: userText, signals: deriveSignals(...)}`;
(b) dispatchSensors normalizes its input at entry. Either is acceptable;
(a) keeps sensors pure, (b) fixes all callers. The regression suite
(SENS-FIX-02) MUST require/fixture the REAL hook-built turn object, not a
hand-shaped one -- the entire bug class survived because every existing test
hand-shapes the turn.

## Finding B -- the split-render seam (DIAL-ATOM-01/02)

### What is ALREADY enforced (do not rebuild)

- intent-classifier.cjs:868-869: `if (source !== 'engine') return base;` --
  the legacy/silent arm CANNOT render triangle text from the hook path. Half
  of SEED-021 C1 is shipped. Proven by tests/test-150-render-unlock.cjs:139-150
  honest-negative arm.
- Sensors fire in ANY tier (navigation-engine.cjs:712-713 "routing_source to
  engine even with no quadruple"), so tier_0 dial TEXT is reachable today.

### The actual holes

1. **Contract discarded:** dial-presenter.cjs:261 returns
   `contract {shape:'F.1', keyboard:'askuserquestion'}` (built by
   shape-f1-renderer.cjs:150-165); the ONLY production caller appends
   `rendered.text` alone (intent-classifier.cjs:896-899). The card half is
   pure prompt doctrine (skills/larry-personality/SKILL.md:117-124).
   148-VERIFICATION.md:125-129 concedes the card is "host-driven; visual
   confirmation only."
2. **Silent catch:** intent-classifier.cjs:871 + 900-903 -- any render fault
   silently returns the base block, dropping dial AND contract with no trace
   note. Same silent-failure class Phase 140 D-03 eradicated from scout
   ("a silent scout is how this hid for weeks", 140-CONTEXT.md:25,84).
3. **Text-substring acceptance:** the 150 claim harness C2-seen
   (tests/claim-harness/claim-c2.cjs:137) passes on the substring
   'Larry can reach for:' -- TEXT presence alone. The harness stays GREEN
   through the exact split-render failure this phase fixes.
4. **Prose mimicry:** 143.2 taught the model the full triangle text format in
   doctrine (skills/ui-system/SKILL.md:247-289), so Larry can hand-paint
   interactive-looking triangles with no card on ANY arm. The 143.2 gate test
   (tests/test-143.2-doctrine-presence.cjs) is grep-presence only. A code-side
   gate cannot fully prevent prose mimicry -- the plan should add a doctrine
   line (never draw the glyphs without firing the card) and accept residual
   risk, or scope mimicry-detection out explicitly.

### The single construction door (where DIAL-ATOM-01 must build)

SEED-020 dispatch rule is codified in code, not just prose:
reach-component-map.json `_doc.dispatch_rule` + selector-dispatcher.cjs:91-93
forbid bespoke AskUserQuestion construction outside lib/hmi/selector-dispatcher.cjs.
The trailer pattern already exists: appendAskUserQuestionTrailer
(selector-dispatcher.cjs:524-544) appends the structural marker string
`[AskUserQuestion contract: shape=F.X verbs=N]`; single-site asserted by
tests/test-148-frozen-contracts.cjs:95-108. DIAL-ATOM-01 = thread the
dial-presenter contract into THIS pattern (or call the dispatcher's trailer
from renderEngineDecisionWithDial) -- never a second construction site.

NOTE a latent breach the fan-out found: the dial path calls renderShapeF1
directly (dial-presenter.cjs), bypassing the dispatcher's pickShape door --
folding the dial render through the dispatcher (or documenting the exemption)
is in-scope for DIAL-ATOM-01's design.

### The ACPT-06 slot (DIAL-ATOM-02)

Phase 146 shipped `doctor --dogfood-acceptance` (scripts/doctor.cjs:181-194,
228, 324 runDogfoodAcceptance) with exactly 5 hermetic ACPT drivers + 
tests/run-all-146.sh (which re-runs run-all-144/1441/145). The 146
investigator: "a 6th ACPT leg slot is architecturally trivial (the
checklist-point pattern at runDogfoodAcceptance + the run-all-146.sh
CJS_SUITES array)." ACPT-01 already proves the engine-fires path via the REAL
SENS-06 side-channel (last-cascade.json newFindings) -- ACPT-06 should mirror
its structure: fire arm (production-shaped turn -> sensor fires -> engine arm
-> text AND contract emitted together) + honest-negative arm (legacy -> neither).

## Finding C -- the C1/C5 gate tension (DIAL-ATOM-03, decide FIRST)

- C1 atomic coupling leans on the engine-only gate (intent-classifier.cjs:869).
- C5 (tier-0 honest cold CARD) is structurally unreachable: Phase 144's LOCKED
  honest-negative decision says a genuinely cold room "correctly stays null ->
  legacy" (144-CONTEXT.md:52,55,86) -- no engine arm, no render at all.
- Additional friction: selector-dispatcher tier0Refuse (:833-835) refuses ALL
  selectors at INSTALL-tier 0 (a different axis than brain_md tier_0); and
  143.3's orchestrator deliberately degrades to the doctrine-sim floor at
  tier_0 (intelligence-orchestrator SKILL.md:199-205) while the 144 engine arm
  fires at tier_0 -- two opposite tier-0 postures already coexist.
- Legitimate outcomes: (i) keep the engine-only gate, de-scope C5 to Phase 154
  (the cockpit binary owns a real TTY, no such gate); (ii) widen the gate with
  an explicit cold-card arm (amends the 144 LOCKED decision via the canon
  mechanism); (iii) hybrid: cold card only when a sensor fired at tier_0
  (already legal -- sensors fire any tier). Option (iii) preserves both LOCKED
  decisions and is the smallest delta. Decision goes to the navigator via
  AskUserQuestion during planning.

## Frozen contracts (byte-unchanged or the phase fails)

- MAX_K=3 / OFFERED_K=3 (dial-presenter.cjs:98-101 "hard MAX_K=3; never
  raised, AP2"; f-selector-ranker MAX_K)
- DIAL_REACH_K=6 (dial-reach-orchestrator.cjs:56, Phase 148 D-09)
- RECOMMEND_FLOOR=0.70 / MARGIN_THRESHOLD=0.15 (dial-reach-orchestrator.cjs:60-61,
  "changing either requires an INTERFACE_VERSION bump")
- The 6-reach bank + 3 postures (tests/test-reach-ids-drift.cjs,
  test-posture-ids-drift.cjs)
- Enforced by: tests/test-148-frozen-contracts.cjs + the carried fences inside
  run-all-148.sh (18/18) and run-all-150.sh (14/14) -- ALL must stay green.
- Part 8: zero egress delta. Turn normalization + signals stay LOCAL; the
  D-03a fence (the conversation seed never reaches buildBrainPacket,
  tests/test-retrieval-seed.cjs) must keep passing.
- Sensors NEVER mutate trace.routing_source (insight-sensors.cjs:20, the
  Phase 144 fence).

## Existing test harnesses to EXTEND (Part 7 -- do not invent new ones)

- tests/run-all-146.sh CJS_SUITES + scripts/doctor.cjs runDogfoodAcceptance
  (the ACPT-06 host)
- tests/test-150-render-unlock.cjs (engine-arm render assertions -- the
  atomicity assertions belong beside these)
- tests/claim-harness/ + run-all-claims.sh (C2-seen should gain a
  contract-presence assertion, not just the text substring)
- The ACPT-01 driver pattern (146-01-PLAN.md:100-102) for hermetic
  sensor-fire proof via the last-cascade.json side-channel

## Trigger chain (the full 13-gate map, for the planner's must_haves)

1. UserPromptSubmit -> run-hook.cmd intent-classifier (hooks.json:251-258, 2000ms)
2. STDIN non-empty; resolveActiveRoomDir resolves {slug, abs_path} (a
   bare-string registry silently kills the whole arm -- 144-02 fixture repair)
3. decide() inside NAV_HARD_TIMEOUT_MS=1200 (Promise.race; silent on timeout)
4. dispatchSensors >=1 fired reach  <-- THE GATE THIS PHASE FIXES
5. reachIdToSkillFamily -> CANONICAL verb (family slugs fail validateVerb --
   only the sensor path can flip the engine; brain/wicked lanes return
   non-canonical slugs)
6. routeActivation Rule 1 -> source='engine'
7. renderEngineDecisionWithDial gate (:869)  <-- THE GATE DIAL-ATOM-01 EXTENDS
8. tier_mode from decision_trace.brain_md_tier_mode (default tier_0)
9. cortex-reach-adapter reachScores (empty cortex -> registry-default 0.5
   priors -> honest-negative still renders)
10. frozen 0.70/0.15 markers (mode_a only)
11. text appended to stdout additionalContext (:1697-1702); contract DROPPED
    (:896-899)  <-- THE DEFECT
12. the card = model tool-call per doctrine (UNGATED)  <-- THE DEFECT
13. close transaction next turn via navigation.cjs into room.db (writable
    room.db required for SELECTED_REACH/PIVOTED edges)

## Validation Architecture

**Testing approach:** extend the shipped harness stack; every new assertion
rides an existing suite runner.

- **SENS-FIX validation:** a new tests/test-150-5-sensor-firability.cjs drives
  dispatchSensors with the EXACT production turn (required/fixtured from the
  intent-classifier builder, NOT hand-shaped); one fire arm + one
  honest-negative arm per registered sensor (8 sensors x 2 arms). RED first
  against current main (the 5 dead sensors fail their fire arms), GREEN after
  the seam fix.
- **DIAL-ATOM validation:** atomicity assertions added beside
  test-150-render-unlock.cjs: engine arm emits text AND contract trailer
  together; legacy arm emits neither; render-fault arm writes a trace note
  (no silent drop). claim-c2.cjs C2-seen gains the contract-presence assertion.
- **ACPT-06:** hermetic driver in the 146 pattern, registered in
  runDogfoodAcceptance + run-all-146.sh; exit-code participates in
  doctor --dogfood-acceptance.
- **Regression floor:** run-all-144.sh (5/5), run-all-146.sh, run-all-148.sh
  (18/18), run-all-150.sh (14/14) all stay green; frozen-contract byte fences
  unchanged.
- **Sampling rule (Nyquist):** every gate in the 13-gate chain that this phase
  touches (4, 7, 11, 12) gets at least one assertion on each side of the gate
  (fire/abstain, engine/legacy, emit/drop).

## Open questions for planning

1. DIAL-ATOM-03 (the C1/C5 decision) -- navigator AskUserQuestion; option (iii)
   hybrid is the smallest delta.
2. Seam choice for SENS-FIX-01 (hook-side enrich vs dispatcher-side normalize)
   -- planner's discretion, but ONE seam.
3. Whether the dial render folds through the dispatcher's pickShape door or
   documents its exemption (the latent SEED-020 breach).
4. Whether the 'preview' terminology collision (DIAL_REACH_K rank-preview vs
   AskUserQuestion preview field) gets renamed here or in Phase 152 (the
   fan-out recommends 152; this phase should only add a code comment).

## RESEARCH COMPLETE
