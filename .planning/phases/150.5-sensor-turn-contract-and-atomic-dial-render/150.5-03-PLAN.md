---
phase: 150.5-sensor-turn-contract-and-atomic-dial-render
plan: 03
type: execute
wave: 2
depends_on: ["150.5-01", "150.5-02"]
files_modified:
  - tests/test-acpt-06-dial-atomic-emission.cjs
  - scripts/doctor.cjs
  - tests/run-all-146.sh
  - tests/run-all-150.5.sh
  - skills/larry-personality/SKILL.md
  - docs/CANON-PHASE-MAP.md
  - .planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-CONTEXT.md
autonomous: true
requirements: [DIAL-ATOM-02, DIAL-ATOM-03]
canon_parts: [Part 2, Part 3, Part 6, Part 7, Part 8]

must_haves:
  truths:
    - "doctor --dogfood-acceptance runs SIX ACPT legs; ACPT-06 proves (a) a real sensor fires from the PRODUCTION-shaped turn with NO hand-shaped signals, (b) dial text + card contract emit atomically on the engine arm, (c) honest-negative: the legacy arm emits neither"
    - "bash tests/run-all-150.5.sh is ONE green command: the two new 150.5 suites + ACPT-06 + the D-03a fence + the carried run-all-144 / run-all-146 / run-all-150 aggregators (which carry the 148 frozen-contract and reach/posture drift fences)"
    - "larry-personality doctrine forbids hand-drawing the dial glyphs without firing the card in the same response (the anti-mimicry line; residual model risk accepted per RESEARCH Finding B item 4)"
    - "D-01 is recorded as EXECUTED in 150.5-CONTEXT.md with pointers to the implementing plan and the proving suites (DIAL-ATOM-03 closed)"
    - "Frozen contracts stay byte-unchanged and zero Brain egress delta (the carried fences prove it)"
  artifacts:
    - path: "tests/test-acpt-06-dial-atomic-emission.cjs"
      provides: "the hermetic ACPT-06 driver mirroring ACPT-01's pattern: production turn -> seam-derived signal -> real sensor fire -> decide() -> routeActivation engine -> atomic text+contract emission; legacy honest-negative; Part-8 sentinel sweep"
      min_lines: 90
    - path: "tests/run-all-150.5.sh"
      provides: "the phase gate: one green command carrying the new suites + the regression floor"
      min_lines: 40
    - path: "scripts/doctor.cjs"
      provides: "ACPT-06 registered as the 6th ACPT_DRIVERS row"
      contains: "ACPT-06"
  key_links:
    - from: "tests/test-acpt-06-dial-atomic-emission.cjs"
      to: "lib/core/navigation-engine.cjs decide() + the skill-activation-router routeActivation + scripts/intent-classifier.cjs renderEngineDecisionWithDial"
      via: "the production decide()->routeActivation->render boundary driven end to end with the REAL units (no stubs, MOS_NAV_TEST_FIRE_SKILL deleted)"
      pattern: "routeActivation"
    - from: "scripts/doctor.cjs ACPT_DRIVERS"
      to: "tests/test-acpt-06-dial-atomic-emission.cjs"
      via: "the 6th roster row spawned by runDogfoodAcceptance"
      pattern: "test-acpt-06-dial-atomic-emission"
    - from: "tests/run-all-146.sh CJS_SUITES"
      to: "tests/test-acpt-06-dial-atomic-emission.cjs"
      via: "appended suite entry"
      pattern: "test-acpt-06"
---

<objective>
DIAL-ATOM-02 + DIAL-ATOM-03 closeout: the 6th ACPT leg that would have caught
both defects, the one-command phase gate, the anti-mimicry doctrine line, and
the D-01 execution record.

The 146 investigation called the 6th ACPT slot "architecturally trivial": the
checklist-point pattern at scripts/doctor.cjs runDogfoodAcceptance (:3035-3092,
the ACPT_DRIVERS roster) + the tests/run-all-146.sh CJS_SUITES array. ACPT-06
mirrors ACPT-01's hermetic structure (146-01-PLAN.md: a REAL fired sensor via
the last-cascade.json side-channel, MOS_NAV_TEST_FIRE_SKILL deleted, an
adversarial honest-negative) -- but with the two upgrades this phase exists for:
the turn is the PRODUCTION shape with NO hand-shaped signals (the Plan-01 seam
derives 'artifact_filed' from the side-channel), and the assertion surface is
the ATOMIC emission (text AND contract trailer together from Plan 02's render).

DIAL-ATOM-03 was decided during planning via AskUserQuestion and is LOCKED as
D-01 in 150.5-CONTEXT.md (hybrid sensor-fired cold card; C5's always-on cold
card formally de-scoped to Phase 154). Plan 02 implemented it; this plan records
it as EXECUTED -- the requirement's deliverable is the locked decision plus its
execution record, not new code.

Purpose: the acceptance harness can never again stay green through a dead
sensor spine or a split render (Canon Part 6: the 146 harness gains the leg
that would have caught this).
Output: the ACPT-06 driver + its doctor/run-all-146 registration + the
run-all-150.5.sh phase gate + the SKILL doctrine line + the D-01 record + the
canon-map row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-CONTEXT.md
@.planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-VALIDATION.md

# The ACPT pattern to mirror (the 146 driver structure + fixture builder):
@.planning/phases/146-loop-fires-acceptance-gate/146-01-PLAN.md
@tests/test-acpt-01-engine-fires.cjs
@tests/dogfood/fixtures/synthetic-room.cjs
# The two registration hosts:
@scripts/doctor.cjs
@tests/run-all-146.sh
# The suite-runner pattern to mirror for run-all-150.5.sh:
@tests/run-all-150.sh
# The doctrine host (the Operating the Dial section):
@skills/larry-personality/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The ACPT-06 hermetic driver -- production turn, real sensor, atomic emission</name>
  <files>tests/test-acpt-06-dial-atomic-emission.cjs</files>
  <read_first>
    - tests/test-acpt-01-engine-fires.cjs (the EXACT pattern to mirror: makeSyntheticRoom contradict fixture, real decide() -> routeActivation boundary, MOS_NAV_TEST_FIRE_SKILL deleted defensively, honest-negative arm, Part-8 sentinel arm, ok/fail + process.exit shape; match its router import path)
    - tests/dogfood/fixtures/synthetic-room.cjs (makeSyntheticRoom(opts): opts.contradict writes the last-cascade.json side-channel; returns { roomDir, cleanup } -- REUSE, do not fork)
    - lib/core/insight-sensors.cjs (Plan 01: deriveTurnSignals gates on side-channel FRESHNESS -- the fixture's last-cascade.json is freshly written so the seam derives 'artifact_filed' with no signal-bag key on the turn)
    - scripts/intent-classifier.cjs renderEngineDecisionWithDial (Plan 02: the atomic emission surface; the module is require-safe and exports the function)
    - 150.5-RESEARCH.md "The ACPT-06 slot" (fire arm + honest-negative arm spec) and the 13-gate trigger chain (ACPT-06 samples gates 4, 6, 7 and 11 in one pass)
  </read_first>
  <behavior>
    - Setup: delete process.env.MOS_NAV_TEST_FIRE_SKILL defensively at the top (the proof is the SENSOR, not the stub).
    - Fire arm (the production-shape proof): build a contradict fixture room via makeSyntheticRoom({ contradict: true }); construct turn = { userText: '', sectionPath: null, sessionId: 'acpt-06' } -- the EXACT production keys, NO signal bag, NO text alias; call the REAL engine.decide(turn, { quadruple: null, brainAvailable: false, roomDir }); assert decision.fire_skill is non-null (the Plan-01 seam derived 'artifact_filed' from the fresh side-channel and sensorArtifactFiled fired); feed routeActivation(decision, ['room-passive']); assert routing.source === 'engine'.
    - Atomic-emission arm: call renderEngineDecisionWithDial(decision, routing, null, { cortexNodes: [] }); assert the block contains BOTH 'Larry can reach for:' AND '[AskUserQuestion contract:' (text and card contract emitted together -- gate 11 closed).
    - Honest-negative arm: a COLD fixture room (no side-channel); same production turn shape; decide() -> fire_skill null; routeActivation -> source 'legacy'; renderEngineDecisionWithDial returns a block containing NEITHER 'Larry can reach for:' NOR '[AskUserQuestion contract:' (a silent loop honestly reports legacy and renders no dial chrome).
    - Part-8 arm: the fixture writes an obviously-fictional proprietary sentinel string into the side-channel finding body; assert the emitted block and JSON.stringify(decision.decision_trace) do NOT contain the sentinel (generic scalars only ride the render and the trace).
    - Fixtures cleaned in finally; all content obviously fictional; no em-dash/en-dash in the file.
  </behavior>
  <action>
    Create tests/test-acpt-06-dial-atomic-emission.cjs as a single node-runnable hermetic suite implementing the arms above, mirroring test-acpt-01-engine-fires.cjs structure (require the REAL lib/core/navigation-engine.cjs decide, the REAL router routeActivation via ACPT-01's import path, and the REAL renderEngineDecisionWithDial from scripts/intent-classifier.cjs; never stub any unit under test). The driver must be self-contained for the doctor spawn contract: clear exit code 0/non-zero, completes well under 120000ms, no network, no live Brain. Commit: `test(150.5-03): ACPT-06 -- production turn fires a real sensor; text+contract emit atomically`.
  </action>
  <verify>
    <automated>node tests/test-acpt-06-dial-atomic-emission.cjs</automated>
  </verify>
  <done>ACPT-06 exits 0: the fire arm proves engine via the seam-derived signal from the production-shaped turn (no hand-shaped signal bag anywhere in the driver); the atomic arm proves text+contract together; the negative proves legacy emits neither; the Part-8 sentinel never leaks.</done>
  <acceptance_criteria>
    - node tests/test-acpt-06-dial-atomic-emission.cjs exits 0
    - grep -cE "signals\s*:" tests/test-acpt-06-dial-atomic-emission.cjs returns 0 (the driver never sets a signal-bag key on any turn object; comments use prose like "signal fuel" instead)
    - grep -c "MOS_NAV_TEST_FIRE_SKILL" tests/test-acpt-06-dial-atomic-emission.cjs returns >= 1 (the defensive delete)
    - The driver source contains both assertion substrings 'Larry can reach for:' and '[AskUserQuestion contract:'
    - grep -P '\x{2014}|\x{2013}' tests/test-acpt-06-dial-atomic-emission.cjs returns no matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Register ACPT-06 in doctor --dogfood-acceptance + run-all-146.sh</name>
  <files>scripts/doctor.cjs, tests/run-all-146.sh</files>
  <read_first>
    - scripts/doctor.cjs :3030-3092 (the ACPT_DRIVERS roster + runDogfoodAcceptance spawn loop -- the 6th row is a one-object append) and :181-194 + :323-339 (the prose that says "the 5 ACPT dogfood drivers" -- every roster-count mention must move to 6)
    - tests/run-all-146.sh (CJS_SUITES at :47-53 + the header comment enumerating the 5 drivers at :8-18 -- both gain ACPT-06)
    - .planning/phases/146-loop-fires-acceptance-gate/146-01-PLAN.md (the ACPT driver registration contract this extends)
  </read_first>
  <action>
    In scripts/doctor.cjs, append the 6th ACPT_DRIVERS row: { id: 'ACPT-06', file: 'test-acpt-06-dial-atomic-emission.cjs', label: 'dial text + card contract emit atomically on the engine arm (real sensor fired from the production turn shape); legacy emits neither' }. Update every roster-count prose mention from "the 5 ACPT" to "the 6 ACPT" (the flag comment at :181-194, the usageText loop-fires section at :323-339, and the ACPT_DRIVERS header comment driver enumeration at :3030-3034). The runDogfoodAcceptance function body needs NO change (it maps the roster).

    In tests/run-all-146.sh, append test-acpt-06-dial-atomic-emission.cjs to CJS_SUITES and add the ACPT-06 line to the header enumeration noting it is the Phase 150.5 leg (atomic dial emission; the leg that would have caught the dead-sensor spine and the split render). Update the "5 ACPT" count mentions in the header to 6.

    Commit: `feat(150.5-03): ACPT-06 registered -- doctor --dogfood-acceptance is a 6-leg gate`.
  </action>
  <verify>
    <automated>node scripts/doctor.cjs --dogfood-acceptance && bash tests/run-all-146.sh</automated>
  </verify>
  <done>doctor --dogfood-acceptance exits 0 with 6 points (ACPT-01..06 all ok); run-all-146.sh exits 0 with the appended suite; no stale "the 5 ACPT" prose remains in either file.</done>
  <acceptance_criteria>
    - node scripts/doctor.cjs --dogfood-acceptance exits 0 and its output names ACPT-06
    - bash tests/run-all-146.sh exits 0 and its run log shows test-acpt-06-dial-atomic-emission.cjs PASSED
    - grep -v '^\s*//' scripts/doctor.cjs | grep -c "ACPT-06" returns >= 1 (the roster row itself, not only comments)
    - grep -rn "the 5 ACPT" scripts/doctor.cjs tests/run-all-146.sh returns no matches
    - grep -P '\x{2014}|\x{2013}' scripts/doctor.cjs tests/run-all-146.sh returns no matches on the edited regions (both files are pre-existing; the new lines carry none)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: The phase gate (run-all-150.5.sh) + doctrine line + D-01 executed record + canon-map row</name>
  <files>tests/run-all-150.5.sh, skills/larry-personality/SKILL.md, docs/CANON-PHASE-MAP.md, .planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-CONTEXT.md</files>
  <read_first>
    - tests/run-all-150.sh (the two-group CJS_SUITES + SHELL_SUITES aggregator pattern + the PASS/FAIL loop to mirror; run-all-150.sh already carries run-all-148.sh + the claim harness, so carrying it carries the 148/150 fences and the strengthened claim-c2)
    - 150.5-VALIDATION.md (the full-suite contract: run-all-150.5.sh registers the new suites and re-runs run-all-144 + run-all-146 + the 148/150 frozen-contract fences)
    - skills/larry-personality/SKILL.md :115-128 (the "Operating the Dial (shipped)" 4-behavior numbered list the doctrine line extends, and the Mode B / Tier 0 paragraph at :124 the cold-card sentence updates)
    - 150.5-CONTEXT.md (the D-01 LOCKED paragraph the execution note points back to -- existing content preserved verbatim, append-only)
    - docs/CANON-PHASE-MAP.md "v1.13.1 Larry Reaches connector spine + engine flip" section (the row format to append; the map's how-to-read rule: a phase implementing canon parts updates the map in the same commit)
  </read_first>
  <action>
    (A) Create tests/run-all-150.5.sh mirroring the run-all-150.sh two-group pattern: CJS_SUITES = (test-150-5-sensor-firability.cjs, test-150-5-render-atomicity.cjs, test-acpt-06-dial-atomic-emission.cjs, test-retrieval-seed.cjs); SHELL_SUITES = (run-all-144.sh, run-all-146.sh, run-all-150.sh). The header documents what each constituent certifies: the firability suite is trigger-chain gate 4; the atomicity suite is gates 7 + 11 + 12; ACPT-06 is the end-to-end leg; test-retrieval-seed is the D-03a Part-8 fence; run-all-146 carries 144/1441/145; run-all-150 carries the 148 frozen-contract fences + the claim harness. run-all-144.sh is listed explicitly per 150.5-VALIDATION.md even though run-all-146.sh re-runs it (cheap, and the validation contract names it). Standard PASS/FAIL loop; exit non-zero if any constituent fails; chmod +x; bash only, no emoji, no em-dashes.

    (B) In skills/larry-personality/SKILL.md, append behavior 5 to the "Operating the Dial (shipped)" numbered list: Never hand-draw the dial glyphs -- the triangle rows and the 'Larry can reach for:' block arrive ONLY from the engine arm's rendered block, and whenever that block carries the [AskUserQuestion contract: ...] trailer Larry MUST fire the AskUserQuestion card in the same response. Text and card are atomic: no card, no picture (SEED-021 Finding 1; Phase 150.5). Also extend the Mode B / Tier 0 paragraph with one sentence: when a sensor fires on a tier-0 turn the engine arm renders the S4 'start anywhere' framing WITH the live card (the Phase 150.5 D-01 hybrid); a genuinely cold turn with no fired sensor renders no dial at all.

    (C) In 150.5-CONTEXT.md, append a short "## D-01 execution record" section at the end (append-only, never altering existing lines): D-01 EXECUTED 2026-06-09 -- implemented by Plan 150.5-02 (the trailer rides every engine arm including tier_0; the engine-only gate unchanged); proven by tests/test-150-5-render-atomicity.cjs (the tier-0 sensor-fired cold-card arm) and tests/test-acpt-06-dial-atomic-emission.cjs (the legacy honest-negative); C5's always-on cold card stays de-scoped to Phase 154. This closes DIAL-ATOM-03.

    (D) In docs/CANON-PHASE-MAP.md, append a Phase 150.5 row to the "v1.13.1 Larry Reaches connector spine + engine flip" table: status shipped, phase 150.5 sensor-turn-contract-and-atomic-dial-render, canon parts Part 2/3/6/7/8/9, reference naming the one-seam turn normalization (insight-sensors.cjs normalizeTurn + deriveTurnSignals), the atomic dial emission (the dispatcher-trailer threading + dial_render_note), ACPT-06, and tests/run-all-150.5.sh as the phase gate.

    Commit: `docs(150.5-03): phase gate run-all-150.5.sh + dial atomicity doctrine + D-01 executed + canon-map row`.
  </action>
  <verify>
    <automated>bash tests/run-all-150.5.sh && grep -q "no card, no picture" skills/larry-personality/SKILL.md && grep -q "D-01 execution record" .planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-CONTEXT.md && grep -q "sensor-turn-contract-and-atomic-dial-render" docs/CANON-PHASE-MAP.md && echo PASS</automated>
  </verify>
  <done>bash tests/run-all-150.5.sh exits 0 (every constituent green: both new suites, ACPT-06, the D-03a fence, and the three carried aggregators); the doctrine line, the D-01 execution record, and the canon-map row all exist verbatim-greppable.</done>
  <acceptance_criteria>
    - bash tests/run-all-150.5.sh exits 0
    - test -x tests/run-all-150.5.sh succeeds (executable bit set)
    - grep -q "no card, no picture" skills/larry-personality/SKILL.md exits 0 and grep -q "D-01 hybrid" skills/larry-personality/SKILL.md exits 0
    - grep -q "D-01 execution record" .planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-CONTEXT.md exits 0 and the pre-existing CONTEXT lines are byte-unchanged (git diff shows append-only)
    - grep -q "sensor-turn-contract-and-atomic-dial-render" docs/CANON-PHASE-MAP.md exits 0
    - grep -P '\x{2014}|\x{2013}' tests/run-all-150.5.sh returns no matches; the appended SKILL.md / CANON-PHASE-MAP.md / CONTEXT.md lines carry only double hyphens
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture room -> disk | tmp synthetic fixtures under os.tmpdir(), obviously fictional, cleaned in finally |
| ACPT-06 driver -> doctor gate | the driver's exit code crosses into the release-blocking dogfood gate |
| doctrine (model) -> AskUserQuestion card | the card fire stays a model action; the doctrine line is the prose-side mitigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.5-10 | Spoofing (false-green) | ACPT-06 fire arm | mitigate | the positive is a REAL fired sensor from the seam-derived signal (MOS_NAV_TEST_FIRE_SKILL deleted; no signal-bag key anywhere in the driver, grep-asserted); the honest-negative asserts legacy emits neither text nor trailer |
| T-150.5-11 | Information Disclosure | the fixture finding body riding the render/trace | mitigate | the Part-8 sentinel arm stringifies the block + decision_trace and asserts the fictional proprietary sentinel is absent |
| T-150.5-12 | Tampering | fixture data resembling real client data | mitigate | makeSyntheticRoom's obviously-fictional content rule reused (the 146 T-146-03 mitigation) |
| T-150.5-13 | Repudiation | a stale "5 ACPT" prose surface contradicting the 6-leg gate | mitigate | the no-stale-prose grep in Task 2 acceptance; the run-all-146.sh header enumerates ACPT-06 |
| T-150.5-SC | Tampering | npm/pip/cargo installs | accept | zero package installs (node built-ins + existing project libs only); no legitimacy checkpoint required |
</threat_model>

## Artifacts this phase plan produces

| Artifact | Kind | Symbol(s) |
|---|---|---|
| tests/test-acpt-06-dial-atomic-emission.cjs | new ACPT driver | the ACPT-06 hermetic suite (fire arm, atomic arm, honest-negative, Part-8 sentinel) |
| scripts/doctor.cjs | edited module | ACPT_DRIVERS 6th row { id: 'ACPT-06', file: 'test-acpt-06-dial-atomic-emission.cjs' }; roster-count prose 5 -> 6 |
| tests/run-all-146.sh | edited runner | CJS_SUITES gains test-acpt-06-dial-atomic-emission.cjs; header enumerates ACPT-06 |
| tests/run-all-150.5.sh | new suite runner | the phase gate (4 CJS suites + 3 carried shell aggregators) |
| skills/larry-personality/SKILL.md | edited doctrine | Operating-the-Dial behavior 5 (no card, no picture) + the D-01 hybrid tier-0 sentence |
| docs/CANON-PHASE-MAP.md | edited ledger | the Phase 150.5 row in the v1.13.1 connector-spine section |
| 150.5-CONTEXT.md | append-only record | the D-01 execution record (DIAL-ATOM-03 closed) |

<verification>
- node scripts/doctor.cjs --dogfood-acceptance exits 0 with 6 ACPT points
- bash tests/run-all-146.sh exits 0 (the extended milestone gate)
- bash tests/run-all-150.5.sh exits 0 (the one-command phase gate: new suites + D-03a fence + run-all-144 + run-all-146 + run-all-150 incl. the 148 frozen fences and the strengthened claim-c2)
- The doctrine line, D-01 execution record, and canon-map row grep-verifiable
- Zero em-dashes/en-dashes in every new/edited line
</verification>

<success_criteria>
DIAL-ATOM-02: doctor --dogfood-acceptance is a 6-leg gate whose new leg proves a
real sensor fires from the production turn shape and that dial text + card
contract emit atomically (never text-only), with an honest legacy negative.
DIAL-ATOM-03: D-01 (hybrid sensor-fired cold card) is recorded as EXECUTED with
its implementing plan and proving suites named. The phase closes on one green
command: bash tests/run-all-150.5.sh.
</success_criteria>

<output>
Create `.planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-03-SUMMARY.md` when done
</output>
