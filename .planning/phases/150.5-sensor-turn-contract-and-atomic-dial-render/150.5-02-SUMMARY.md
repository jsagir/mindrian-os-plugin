---
phase: 150.5-sensor-turn-contract-and-atomic-dial-render
plan: 02
subsystem: hmi-render
tags: [dial, askuserquestion, trailer, atomic-render, seed-020, d-01, larryreach]
requires:
  - scripts/intent-classifier.cjs renderEngineDecisionWithDial (Phase 150-06 render unlock)
  - lib/hmi/selector-dispatcher.cjs appendAskUserQuestionTrailer (the SEED-020 single door)
  - lib/hmi/dial-presenter.cjs renderDial contract field (Phase 150-06)
  - persistDecisionTrace (the shipped Phase-91 decision-trace surface)
provides:
  - atomic engine-arm emission -- dial text + AskUserQuestion contract trailer
    ride one emission site, never text-only (DIAL-ATOM-01)
  - D-01 sensor-fired tier-0 cold card live (S4 framing + '--' + trailer)
  - dial_render_fault decision-trace note replacing the silent render catch
  - tests/test-150-5-render-atomicity.cjs (10-arm regression fence)
  - claim-c2 C2-seen hardened with the contract-presence assertion
affects:
  - 150.5-03 (the doctrine line forbidding hand-painted glyphs without a card
    rides on this trailer being real)
  - Phase 146 acceptance harness (the 6th ACPT leg, DIAL-ATOM-02)
  - Phase 152 (the 'preview' rename; the collision comment is now in-code)
tech-stack:
  added: []
  patterns:
    - single-construction-door threading (the caller reads the scalar marker
      positionally; the dispatcher mints the trailer -- SEED-020)
    - fault-classifier note (event + error_name scalars only, never
      e.message/stack) merged into the shipped trace surface, zero new write paths
key-files:
  created:
    - tests/test-150-5-render-atomicity.cjs
  modified:
    - scripts/intent-classifier.cjs
    - lib/hmi/selector-dispatcher.cjs
    - tests/claim-harness/claim-c2.cjs
    - lib/hmi/dial-presenter.cjs
decisions:
  - "SEED-020 pickShape exemption (documented): the dial render stays on the
    shipped 143.1 renderShapeF1 resolve/format path and does NOT fold through
    pickShape -- its install-tier-0 refuse (selector-dispatcher.cjs:833-835)
    would kill the D-01 sensor-fired cold card; SEED-020's actual rule is
    honored by constructing the trailer EXCLUSIVELY via
    appendAskUserQuestionTrailer"
  - "Trace-note surface: the persisted decision trace via the shipped
    persistDecisionTrace writer (the Phase-91 surface routing_trace_notes
    already rides) -- room.db is closed by render time and Part 9's
    navigation.cjs chokepoint governs room.db writes; the note adds ZERO new
    write paths (threat T-150.5-07)"
  - "D-01 honored: the :869 engine-only gate byte-identical; the trailer rides
    every engine arm including tier_0 with NO tier_mode gating; legacy renders
    neither text nor trailer (Phase 144 honest-negative unamended)"
  - "Rule 3 deviation: appendAskUserQuestionTrailer exported top-level from
    selector-dispatcher.cjs (was _internal-only) so the planned seam is
    callable by a production caller; additive, no behavior change"
metrics:
  duration: "~8 min"
  completed: 2026-06-10
  tasks: 3
  commits: 3
---

# Phase 150.5 Plan 02: Atomic Dial Render Coupling Summary

The AskUserQuestion contract now rides the emitted additionalContext block
with the dial text through the SEED-020 dispatcher trailer (one emission
site, never text-only), a render fault writes a scalar dial_render_note into
the persisted decision trace instead of vanishing, and D-01's hybrid
sensor-fired tier-0 cold card is live -- proven RED-first by a 10-arm
atomicity suite and locked by the hardened claim-c2 contract-presence
assertion.

## What was done

### Task 1: RED -- tests/test-150-5-render-atomicity.cjs (commit 166e1826)

Created the 10-arm suite (304 lines) reusing the test-150-render-unlock
require-cache spy + loadClassifier patterns:

- Engine-arm atomicity (text AND '[AskUserQuestion contract: shape=F.1'
  together), the D-01 tier-0 cold-card arm (S4 framing + '--' + trailer; D-01
  cited in the arm name), the fault arm (renderDial forced to THROW; block
  equals the formatEngineDecisionBlock base; exactly one
  { event:'dial_render_fault', error_name } note, scalars only via
  Object.keys deepEqual), the appendAskUserQuestionTrailer dispatcher spy,
  the trailer shape fence (/\[AskUserQuestion contract: shape=F\.1
  verbs=\d+\]/), and the dial_render_note source fence
- Honest negatives + current truths green even on RED main: legacy arm
  renders neither text nor trailer; the test-148 CONSTRUCTION_MARKER regex
  matches neither edited source; frozen constants; dash sweep

RED proof on pre-fix main (verified before the GREEN commit): exit 1 with
6 FAILED arms, stdout naming the engine-arm trailer assertion first.

### Task 2: GREEN -- the threaded trailer + the trace-noted catch (commit 8957ba21)

scripts/intent-classifier.cjs, two sites:

(A) renderEngineDecisionWithDial: after renderDial, lazy-requires
lib/hmi/selector-dispatcher.cjs and calls appendAskUserQuestionTrailer
(rendered, 'F.1') -- the dispatcher mints the trailer onto the envelope's
scalar marker; the caller binds it to a local and concatenates
base + text + marker (the token askuserquestion_marker is never followed by
'=' in this file; the literal trailer string never appears outside the
dispatcher). If the marker is absent the function falls through to the base
block -- never text-only. The :869 engine-only gate is byte-identical; NO
tier_mode gating (the trailer rides tier_0 -- the D-01 cold card). The silent
catch (_e) {} became a guarded ctx.onRenderNote call emitting
{ event:'dial_render_fault', error_name: (e && e.name) || 'Error' } --
scalars only, never e.message or a stack. SEED-020 pickShape exemption
comment added above the function.

(B) The engine-emit tail: REORDERED so renderEngineDecisionWithDial runs
BEFORE persistDecisionTrace, with onRenderNote merging the note as
traceEntry.dial_render_note -- the fault lands in the SAME persisted trace
routing_trace_notes already rides. The 150-04 sensor-ctx lines and the
turn builder (~:1349-1355, Plan 01's drift-fence target) were not touched.

lib/hmi/selector-dispatcher.cjs (Rule 3 deviation, see below): added the
top-level appendAskUserQuestionTrailer export.

### Task 3: claim-c2 hardening + the preview collision comment (commit a8bbbec8)

- tests/claim-harness/claim-c2.cjs: C2-seen gains one assertion --
  block.indexOf('[AskUserQuestion contract:') !== -1 with a DIAL-ATOM-01
  message; the harness can no longer stay green through the split-render
  failure (150.5-RESEARCH.md Finding B item 3). Fixture builder, C1/C3-C7
  legs, and the carve-out SKIP lines byte-identical.
- lib/hmi/dial-presenter.cjs: 3 comment lines at the OFFERED_K block --
  'preview' here means the DIAL_REACH_K rank-preview, NOT the AskUserQuestion
  preview field; the rename rides the Phase 152 spec. git diff confirms
  comment-only (+3 insertions, zero code/constant changes).

## Verification (all green post-Task-3)

| Gate | Result |
|---|---|
| node tests/test-150-5-render-atomicity.cjs | exit 0, 10 passed / 0 failed |
| node tests/test-150-render-unlock.cjs | exit 0 (string return, spy, dash sweep) |
| node tests/claim-harness/claim-c2.cjs | exit 0 (strengthened C2-seen) |
| node tests/test-148-frozen-contracts.cjs | exit 0 (7 assertions) |
| node tests/test-reach-ids-drift.cjs + test-posture-ids-drift.cjs | exit 0 + exit 0 |
| node tests/test-150-5-sensor-firability.cjs (Plan 01 fence) | exit 0 |
| node tests/test-sensors-routing-fence.cjs (Phase 144 fence) | exit 0 |
| bash tests/run-all-144.sh / run-all-148.sh / run-all-150.sh | 0 / 0 / 0 |
| node -c scripts/intent-classifier.cjs | clean |
| grep -E 'askuserquestion_marker\s*=' scripts/intent-classifier.cjs | no matches |
| grep -F '[AskUserQuestion contract:' classifier + presenter | no matches |
| grep -P em/en-dash across all five touched files | 0 matches each |
| dispatcher suites (test-selector-dispatcher*, test-148-component-map) | all exit 0 |

## Frozen surfaces audit

MAX_K=3, DIAL_REACH_K=6, RECOMMEND_FLOOR=0.70, MARGIN_THRESHOLD=0.15, the
6-reach bank, and the 3 postures are value-asserted green (test-148 + both
drift fences + the suite's own frozen-constants arm). dial-presenter.cjs
received comment-only bytes. Zero Brain egress delta: the trailer carries
shape + verbs COUNT scalars only (threat T-150.5-05 fence); the fault note
carries event + error_name scalars only (T-150.5-06); zero new write paths
(T-150.5-07); no package installs (T-150.5-SC).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] appendAskUserQuestionTrailer was not reachable as planned**
- **Found during:** Task 2
- **Issue:** the plan's key_link calls selector-dispatcher.appendAskUserQuestionTrailer
  from the classifier, but the function was exported ONLY under the
  test-oriented `_internal` namespace (selector-dispatcher.cjs:970) -- the
  production call threw TypeError and every engine arm degraded to the base
  block
- **Fix:** added a top-level `appendAskUserQuestionTrailer` export to
  module.exports with a 150.5-02 comment; additive, zero behavior change to
  existing exports (the `_internal` copy stays). All five dispatcher suites
  re-run green
- **Files modified:** lib/hmi/selector-dispatcher.cjs (not in the plan's
  files_modified list; construction stays inside the dispatcher per SEED-020)
- **Commit:** 8957ba21

**2. [Rule 1 - Bug] First GREEN draft tripped the construction-marker fence**
- **Found during:** Task 2 (caught by the Task 1 suite before commit)
- **Issue:** the marker type-guard `typeof rendered.askuserquestion_marker
  === 'string'` matches the CONSTRUCTION_MARKER regex
  (/askuserquestion_marker\s*=/ matches the `===` comparison)
- **Fix:** bind the marker to a local (`const marker =
  rendered.askuserquestion_marker;` -- token followed by ';') and guard the
  local; the read stays read-only property access exactly as the plan demands
- **Files modified:** scripts/intent-classifier.cjs
- **Commit:** 8957ba21 (fixed pre-commit; never landed broken)

## Commits

| Commit | Type | Description |
|---|---|---|
| 166e1826 | test(150.5-02) | RED atomicity suite proves the discarded contract + silent catch |
| 8957ba21 | feat(150.5-02) | atomic dial emission -- contract threaded via dispatcher trailer, fault writes dial_render_note (D-01) |
| a8bbbec8 | test(150.5-02) | claim-c2 C2-seen gains contract-presence; preview collision comment (Phase 152 rename) |

## TDD Gate Compliance

RED commit (166e1826, exit 1 with 6 named failures on pre-fix main) precedes
the GREEN commit (8957ba21, suite exit 0). No refactor commit needed.

## Known Stubs

None. The trailer, the fault note, and the cold-card arm are fully wired and
exercised by the suite; no hardcoded empties or placeholder text introduced.

## Threat Flags

None beyond the plan's threat model. No new network endpoints, auth paths,
file-access patterns, or schema changes: the trailer is an in-process string
scalar; the fault note rides the existing persistDecisionTrace file surface.

## Self-Check: PASSED

Created files exist (tests/test-150-5-render-atomicity.cjs, this SUMMARY);
commits 166e1826 (RED), 8957ba21 (GREEN), and a8bbbec8 (Task 3) verified
present in git log; zero em/en-dashes in this file.
