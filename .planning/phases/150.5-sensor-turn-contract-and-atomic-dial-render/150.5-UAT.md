---
phase: "150.5"
slug: sensor-turn-contract-and-atomic-dial-render
status: testing
created: 2026-06-10
source: 150.5-VERIFICATION.md (status human_needed; 5/5 must-haves machine-verified)
---

# Phase 150.5 -- Human Verification (UAT)

> One item. Everything machine-checkable is already green (phase gate 7/7,
> doctor --dogfood-acceptance 6/6). This is the host-driven card render that
> 148-VERIFICATION precedent marks visual-confirmation-only.

## Current Test

### 1. The live dial card renders and accepts keyboard input

**Why manual:** the AskUserQuestion card is a host/model action; no script can
assert the host actually painted it.

**Steps:**
1. Release or install the build carrying Phase 150.5 (next beta), then fully
   restart Claude Code so the new plugin code loads.
2. Open a session whose active room is a real room (e.g. motj-ecosystem).
3. Trip a sensor deterministically: `/mos:jtbd set validate` (fires
   sensorJtbdReweight next turn), or file an artifact so last-cascade.json is
   fresh (fires artifact_filed via the new seam).
4. Send any non-empty prompt.

**Expected:**
- The NAVIGATION DECISION block shows `routing_source: engine`.
- The dial text renders WITH the `[AskUserQuestion contract: ...]` trailer
  (never text-only).
- The live card renders beneath it; arrows/numbers work; picking a row writes
  a SELECTED_REACH edge (check the newest decision trace).
- In a cold/tier-0 room with a fired sensor: S4 framing ('New room - nothing
  to rank yet. Start anywhere.') + '--' confidence + the working card (D-01).
- NO hand-painted triangles anywhere without a card in the same response
  (anti-mimicry doctrine).

**Result:** pending

## Gaps

(none recorded yet -- fill on failure with status: failed + evidence)
