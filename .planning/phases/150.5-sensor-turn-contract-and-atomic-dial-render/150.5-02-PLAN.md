---
phase: 150.5-sensor-turn-contract-and-atomic-dial-render
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/intent-classifier.cjs
  - lib/hmi/dial-presenter.cjs
  - tests/test-150-5-render-atomicity.cjs
  - tests/claim-harness/claim-c2.cjs
autonomous: true
requirements: [DIAL-ATOM-01]
canon_parts: [Part 3, Part 6, Part 7, Part 8, Part 9]

must_haves:
  truths:
    - "On the engine arm the emitted additionalContext block carries the dial TEXT and the AskUserQuestion CONTRACT TRAILER together -- one emission site, never text-only (per D-01 the engine-only gate at intent-classifier.cjs:869 STAYS)"
    - "The trailer is constructed ONLY by selector-dispatcher.appendAskUserQuestionTrailer (the SEED-020 single door); neither intent-classifier.cjs nor dial-presenter.cjs ever composes the trailer string or assigns askuserquestion_marker"
    - "A dial render fault writes a dial_render_note into the persisted decision trace (the shipped persistDecisionTrace surface) instead of vanishing; the user turn is never blocked (per D-01 cited: the Phase 140 D-03 silent-failure class is closed at this seam)"
    - "The sensor-fired tier-0 arm renders the S4 framing line + '--' confidence WITH the contract trailer (D-01 hybrid cold card); the legacy arm renders neither text nor trailer (a genuinely cold room stays silent -- Phase 144's honest-negative decision unamended per D-01)"
    - "claim-c2 C2-seen asserts contract-trailer presence, not just the 'Larry can reach for:' text substring"
    - "Frozen contracts byte-unchanged: MAX_K=3, DIAL_REACH_K=6, RECOMMEND_FLOOR=0.70, MARGIN_THRESHOLD=0.15"
  artifacts:
    - path: "tests/test-150-5-render-atomicity.cjs"
      provides: "engine-arm atomic emission, legacy-arm neither, tier-0 sensor-fired cold-card arm (D-01), fault-arm trace note, single-construction-door source fence"
      min_lines: 100
    - path: "scripts/intent-classifier.cjs"
      provides: "renderEngineDecisionWithDial threads the contract via the dispatcher trailer; fault path calls ctx.onRenderNote; caller merges dial_render_note into traceEntry before persistDecisionTrace"
      contains: "appendAskUserQuestionTrailer"
  key_links:
    - from: "scripts/intent-classifier.cjs renderEngineDecisionWithDial"
      to: "lib/hmi/selector-dispatcher.cjs appendAskUserQuestionTrailer"
      via: "lazy require + trailer call on the renderDial envelope (the SEED-020 single construction door)"
      pattern: "appendAskUserQuestionTrailer"
    - from: "scripts/intent-classifier.cjs engine-emit tail (~:1691-1702)"
      to: "persistDecisionTrace"
      via: "render call REORDERED before persist; ctx.onRenderNote merges dial_render_note into traceEntry"
      pattern: "dial_render_note"
    - from: "tests/claim-harness/claim-c2.cjs C2-seen"
      to: "the contract trailer in the rendered block"
      via: "substring assertion on '[AskUserQuestion contract:'"
      pattern: "AskUserQuestion contract"
---

<objective>
DIAL-ATOM-01: thread the dial's AskUserQuestion contract to the emitted surface
through the SEED-020 single construction door, replace the silent render catch
with a decision-trace note, and implement D-01's sensor-fired cold-card arm.

The hole (150.5-RESEARCH.md Finding B): dial-presenter.cjs:253-262 returns
contract { shape:'F.1', keyboard:'askuserquestion' } and the ONLY production
caller discards it -- renderEngineDecisionWithDial appends rendered.text alone
(intent-classifier.cjs:896-899). The card half is pure prompt doctrine, so a
navigator sees triangles that accept no keys. The catch at :900-903 drops any
render fault silently (the Phase 140 D-03 silent-failure class).

Two design decisions, each justified in one line per the phase instructions:

1. SEED-020 pickShape question -- DOCUMENTED EXEMPTION, trailer through the
   dispatcher: the dial render stays on the shipped 143.1 renderShapeF1
   resolve/format path and does NOT fold through pickShape, because pickShape's
   install-tier-0 refuse (selector-dispatcher.cjs:833-835) would kill the D-01
   sensor-fired cold card and re-architect a shipped seam mid-defect-phase;
   SEED-020's actual rule (no AskUserQuestion payload construction outside the
   dispatcher, selector-dispatcher.cjs:91-93 + reach-component-map dispatch_rule)
   is honored by constructing the trailer EXCLUSIVELY via the dispatcher's
   appendAskUserQuestionTrailer (:524-544), asserted by the same construction-
   marker regex test-148-frozen-contracts.cjs:95-108 uses.

2. Trace-note surface -- the persisted decision trace via the shipped
   persistDecisionTrace writer (intent-classifier.cjs:725, the Phase-91 surface
   where routing_trace_notes already lands): room.db is closed by render time
   and Part 9's navigation.cjs chokepoint governs room.db writes, so adding a
   graph write inside the fault handler would create new failure surface inside
   the failure path; the note adds ZERO new write paths (the strongest T3
   mitigation) and explain-decision can render it from the same trace file.

D-01 (LOCKED, 150.5-CONTEXT.md): HYBRID sensor-fired cold card. The engine-only
gate at :869 STAYS. Sensors fire any tier (navigation-engine.cjs:712-713), so a
sensor-fired tier-0 turn already reaches the engine arm; this plan ensures the
trailer rides EVERY engine arm regardless of brain_md_tier_mode -- so the S4
'New room' framing + '--' confidence renders WITH the live card contract. No
always-on cold card; legacy stays bare. Phase 144's honest-negative LOCKED
decision (144-CONTEXT.md: cold room correctly stays null -> legacy) is NOT
amended.

The 'preview' terminology collision (DIAL_REACH_K rank-preview vs AskUserQuestion
preview field) gets a code comment ONLY in this phase; the rename rides Phase 152
(per 150.5-CONTEXT.md out-of-scope).

Purpose: gates 11-12 of the 13-gate chain stop being the defect (contract
dropped; card ungated).
Output: the threaded trailer + the trace-noted catch + the D-01 cold-card arm +
the RED-first atomicity suite + the hardened claim-c2 assertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-CONTEXT.md
@.planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-RESEARCH.md

# The render seam (renderEngineDecisionWithDial :842-904; the emit tail :1691-1702):
@scripts/intent-classifier.cjs
# The single construction door (appendAskUserQuestionTrailer :524-544; pickShape tier0Refuse :833-835):
@lib/hmi/selector-dispatcher.cjs
# The contract producer (renderDial return :253-262; S4 FRAMING_TIER_0; the 'preview' comment site):
@lib/hmi/dial-presenter.cjs
# The construction-marker fence pattern to mirror (CONSTRUCTION_MARKER :95; single-site audit :97-110):
@tests/test-148-frozen-contracts.cjs
# The existing render assertions that MUST stay green (spy pattern to reuse):
@tests/test-150-render-unlock.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- tests/test-150-5-render-atomicity.cjs (atomic emission, D-01 cold-card arm, fault note, construction fence)</name>
  <files>tests/test-150-5-render-atomicity.cjs</files>
  <read_first>
    - 150.5-RESEARCH.md Finding B (the four holes) + the Validation Architecture DIAL-ATOM row (the arms this suite must carry)
    - scripts/intent-classifier.cjs :864-904 (renderEngineDecisionWithDial: the :869 engine-only gate that STAYS; the :896-899 contract discard; the :900-903 silent catch) and :1691-1702 (the persist-then-render ordering Task 2 reverses)
    - tests/test-150-render-unlock.cjs (the installPresenterSpy require-cache pattern + loadClassifier + the existing assertions that must STAY green -- this suite sits BESIDE it, never edits it)
    - lib/hmi/dial-presenter.cjs :234-262 (FRAMING_TIER_0 = 'New room - nothing to rank yet. Start anywhere.'; NO_SIGNAL = '--'; the returned contract field)
    - lib/hmi/selector-dispatcher.cjs :524-544 (the trailer format '[AskUserQuestion contract: shape=F.X verbs=N]' and the askuserquestion_marker scalar surface)
    - tests/test-148-frozen-contracts.cjs :91-110 (the CONSTRUCTION_MARKER regex and the single-site audit shape to extend to the two files this plan touches)
  </read_first>
  <behavior>
    - Engine-arm atomicity (RED today): renderEngineDecisionWithDial(decision{brain_md_tier_mode:'mode_a'}, routing{source:'engine'}, null, {cortexNodes:[...]}) returns a block containing BOTH 'Larry can reach for:' AND '[AskUserQuestion contract: shape=F.1' -- text and contract emit together.
    - Legacy-arm honest negative: routing{source:'legacy'} returns a block containing NEITHER 'Larry can reach for:' NOR '[AskUserQuestion contract:' (the :869 gate unchanged; no dial chrome on legacy).
    - D-01 sensor-fired tier-0 cold-card arm (RED today on the trailer half): decision_trace.brain_md_tier_mode='tier_0' + routing engine returns a block containing the S4 framing 'New room - nothing to rank yet. Start anywhere.' AND '--' AND '[AskUserQuestion contract:' together (cite D-01 in the arm's name string).
    - Fault arm (RED today): spy lib/hmi/dial-presenter.cjs renderDial via the require cache to THROW; call with ctx { cortexNodes:[], onRenderNote: collector }; assert the returned block equals the base block (turn never blocked) AND the collector received exactly one note object with event === 'dial_render_fault' and a string error_name -- and NO other fields carrying prose.
    - Single-construction-door fence: spy selector-dispatcher.appendAskUserQuestionTrailer via the require cache and assert it is invoked on the engine arm; THEN read scripts/intent-classifier.cjs and lib/hmi/dial-presenter.cjs sources and assert the test-148 CONSTRUCTION_MARKER regex (/askuserquestion_marker\s*=|\[AskUserQuestion contract:|new\s+AskUserQuestion|AskUserQuestion\s*\(/) matches NEITHER file (the trailer string exists only in the dispatcher).
    - Trailer shape fence (threat T2): the trailer substring in the engine-arm block matches /\[AskUserQuestion contract: shape=F\.1 verbs=\d+\]/ exactly -- scalars only, no room content beyond what the text half already carries.
    - Trace-note merge fence: assert scripts/intent-classifier.cjs source contains 'dial_render_note' (the caller-side merge Task 2 adds).
    - Frozen constants: require dial-reach-orchestrator + f-selector-ranker and assert MAX_K===3, DIAL_REACH_K===6, RECOMMEND_FLOOR===0.70, MARGIN_THRESHOLD===0.15.
    - No em-dash / en-dash in the edited sources (codepoints via String.fromCharCode).
  </behavior>
  <action>
    Create tests/test-150-5-render-atomicity.cjs as a plain node-runnable CJS suite (ok/fail counters + process.exit, reusing the installPresenterSpy + loadClassifier patterns from tests/test-150-render-unlock.cjs verbatim where applicable). RED-by-design: the engine-arm trailer assertion, the tier-0 trailer half, the onRenderNote collector, the appendAskUserQuestionTrailer spy, and the 'dial_render_note' source grep all FAIL on current main. Commit alone as the RED commit: `test(150.5-02): RED atomicity suite proves the discarded contract + silent catch`.
  </action>
  <verify>
    <automated>node tests/test-150-5-render-atomicity.cjs; test $? -ne 0 && echo "PASS (RED-by-design on main)"</automated>
  </verify>
  <done>The suite exists, runs to completion, reports the atomicity + fault-note + door arms FAILED on current main, and is committed before Task 2.</done>
  <acceptance_criteria>
    - node tests/test-150-5-render-atomicity.cjs exits non-zero on pre-fix main and stdout names the failing engine-arm trailer assertion
    - The suite source contains the exact substrings '[AskUserQuestion contract:' (assertion target), 'New room - nothing to rank yet. Start anywhere.' (D-01 arm), and 'dial_render_fault' (fault arm)
    - The legacy honest-negative and the frozen-constants checks PASS even on RED main (they assert current truth)
    - grep -P '\x{2014}|\x{2013}' tests/test-150-5-render-atomicity.cjs returns no matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GREEN -- thread the contract through the dispatcher trailer; trace-note the catch; reorder render before persist</name>
  <files>scripts/intent-classifier.cjs</files>
  <read_first>
    - tests/test-150-5-render-atomicity.cjs (Task 1 -- the contract this task turns GREEN)
    - scripts/intent-classifier.cjs :864-904 (the function body to edit) and :1660-1704 (the traceEntry composition at :1664-1690, persistDecisionTrace at :1691, the render call at :1697 -- the block to reorder)
    - lib/hmi/selector-dispatcher.cjs :524-544 (appendAskUserQuestionTrailer mutates the envelope: sets askuserquestion_marker from rendered.contract and skips zones when absent -- the renderDial envelope has contract but no zones, so the scalar marker is the surface to read)
    - tests/test-150-render-unlock.cjs + tests/claim-harness/claim-c2.cjs (existing assertions on the same function: string return type, spy invocation, 'Larry can reach for:' substring -- all must stay green)
  </read_first>
  <action>
    Edit scripts/intent-classifier.cjs in two places:

    (A) renderEngineDecisionWithDial (:864-904): after rendered = presenter.renderDial(reachList, {}), lazy-require lib/hmi/selector-dispatcher.cjs and call its appendAskUserQuestionTrailer(rendered, 'F.1') -- the SEED-020 single construction door mints the trailer onto rendered.askuserquestion_marker. Compose the return as base + '\n\n' + rendered.text + '\n' + the marker read positionally from the envelope (CRITICAL: never write a line where the token askuserquestion_marker is followed by '=' -- the construction-marker regex treats that as a construction site; read-only property access in the concatenation expression only, e.g. inline in the string-building expression). Keep the STRING return type (three shipped suites assert it). Replace the silent catch (_e) {} at :900-903: if ctx.onRenderNote is a function, invoke it (inside its own try/catch so a faulting callback cannot throw out of the fault handler) with { event: 'dial_render_fault', error_name: (e && e.name) || 'Error' } -- scalars only, never e.message or a stack (a message can carry filesystem paths; the note is a classifier, not a payload); then return base unchanged. Keep the :869 engine-only gate byte-identical (D-01: the gate STAYS). Add NO tier_mode gating on the trailer -- the trailer rides every engine arm including tier_0, which IS the D-01 sensor-fired cold card (the S4 framing + '--' already render from buildReachList's tier_0 path; cite D-01 in the comment). Add a 3-line comment block above the function recording the SEED-020 pickShape exemption decision verbatim from this plan's objective (the dial render stays on the renderShapeF1 format path; pickShape's install-tier-0 refuse would kill the D-01 cold card; the trailer construction stays dispatcher-only).

    (B) The engine-emit tail (:1691-1702): REORDER so the render runs BEFORE persistDecisionTrace -- declare a local notes holder, call renderEngineDecisionWithDial(out.decision, routing, offerLine, { cortexNodes: out.cortex_nodes, onRenderNote: function (note) { traceEntry.dial_render_note = note; } }) FIRST, then persistDecisionTrace(roomDir, sessionId, traceEntry), then the existing stdout.write of the block. The fault note thus lands in the SAME persisted decision trace routing_trace_notes already rides (the shipped Phase-91 surface; zero new write paths -- threat T3). Do NOT touch the 150-04 sensor-ctx lines (:1312-1398) or the turn builder (:1349-1355) -- Plan 01's drift fence greps that builder. No em-dashes (test-150-render-unlock asserts the whole file).

    Commit: `feat(150.5-02): atomic dial emission -- contract threaded via dispatcher trailer, fault writes dial_render_note (D-01)`.
  </action>
  <verify>
    <automated>node -c scripts/intent-classifier.cjs && node tests/test-150-5-render-atomicity.cjs && node tests/test-150-render-unlock.cjs</automated>
  </verify>
  <done>The atomicity suite is GREEN end to end; test-150-render-unlock.cjs stays green (string return, spy, no em-dash sweep); the trailer appears on every engine arm including tier_0; a forced render fault produces a dial_render_note and never blocks the turn.</done>
  <acceptance_criteria>
    - node tests/test-150-5-render-atomicity.cjs exits 0
    - node tests/test-150-render-unlock.cjs exits 0
    - grep -c "appendAskUserQuestionTrailer" scripts/intent-classifier.cjs returns >= 1
    - grep -c "dial_render_note" scripts/intent-classifier.cjs returns >= 1
    - grep -E 'askuserquestion_marker\s*=' scripts/intent-classifier.cjs returns no matches (read-only access; the construction site stays in the dispatcher)
    - grep -F '[AskUserQuestion contract:' scripts/intent-classifier.cjs returns no matches (the literal trailer string never appears outside the dispatcher)
    - node tests/test-148-frozen-contracts.cjs exits 0 (frozen constants + single-site audit untouched)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Harden claim-c2 C2-seen with the contract-presence assertion + the 'preview' collision comment</name>
  <files>tests/claim-harness/claim-c2.cjs, lib/hmi/dial-presenter.cjs</files>
  <read_first>
    - tests/claim-harness/claim-c2.cjs :115-158 (the C2-seen check that passes on the text substring alone -- the exact acceptance gap 150.5-RESEARCH.md Finding B item 3 names)
    - lib/hmi/dial-presenter.cjs :96-101 (the OFFERED_K/DIAL_REACH_K comment block where the word 'preview' collides with the AskUserQuestion preview field -- the comment site)
    - 150.5-CONTEXT.md "Explicitly OUT of scope" (the rename belongs to Phase 152; this phase adds a code comment ONLY)
  </read_first>
  <action>
    In tests/claim-harness/claim-c2.cjs, extend the C2-seen check body (after the existing 'Larry can reach for:' assertion at :137-138) with one additional assertion: block.indexOf('[AskUserQuestion contract:') !== -1 with the message naming DIAL-ATOM-01 (the harness must no longer stay green through the split-render failure -- text presence alone is the exact false-green Finding B item 3 documents). Touch nothing else in the harness (the fixture builder, C1/C3-C7, and the carve-out SKIP lines stay byte-identical).

    In lib/hmi/dial-presenter.cjs, add a 2-line comment at the OFFERED_K/DIAL_REACH_K block (:96-101): NOTE -- 'preview' in this module means the DIAL_REACH_K rank-preview (the 6-row ranked view), NOT the AskUserQuestion preview field (the host's side-panel); the rename resolving this collision rides the Phase 152 spec. Comment only; no code or constant changes anywhere in the file (the frozen-contract fences assert the values).

    Commit: `test(150.5-02): claim-c2 C2-seen gains contract-presence; preview collision comment (Phase 152 rename)`.
  </action>
  <verify>
    <automated>node tests/claim-harness/claim-c2.cjs && node tests/test-148-frozen-contracts.cjs && node tests/test-reach-ids-drift.cjs && node tests/test-posture-ids-drift.cjs</automated>
  </verify>
  <done>claim-c2 GREEN with the strengthened assertion; the 148 frozen-contract suite and both drift fences stay green; dial-presenter changed by comment bytes only.</done>
  <acceptance_criteria>
    - node tests/claim-harness/claim-c2.cjs exits 0 and its source contains the literal '[AskUserQuestion contract:' inside the C2-seen check
    - node tests/test-148-frozen-contracts.cjs exits 0 (the CONSTRUCTION_MARKER audit tolerates the assertion because claim-c2 is not in PHASE_148_FILES; the frozen constants are value-asserted and unchanged)
    - git diff for lib/hmi/dial-presenter.cjs shows comment-only lines (no executable-code or constant changes)
    - grep -P '\x{2014}|\x{2013}' tests/claim-harness/claim-c2.cjs lib/hmi/dial-presenter.cjs returns no matches
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dial contract -> additionalContext trailer | the AskUserQuestion contract crosses onto the model-visible surface as a structural marker string |
| render fault -> persisted decision trace | the fault classifier crosses into .mindrian/decision-traces/<session>.json |
| doctrine (model) -> AskUserQuestion card | the host card fire remains a model action keyed on the trailer (residual, see T-150.5-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.5-05 | Information Disclosure | the contract trailer (threat T2: echoing room content into additionalContext beyond the text half) | mitigate | the trailer is shape + verbs COUNT only; the atomicity suite asserts the trailer matches /\[AskUserQuestion contract: shape=F\.1 verbs=\d+\]/ exactly; the contract object itself is never serialized into the block |
| T-150.5-06 | Repudiation -> silent failure | the render catch (the Phase 140 D-03 class) | mitigate | ctx.onRenderNote writes { event:'dial_render_fault', error_name } -- name scalar only, never e.message/stack (paths could ride a message); fault arm asserts the note and the unblocked turn |
| T-150.5-07 | Tampering | trace-note writes bypassing the navigation.cjs chokepoint (threat T3, Part 9) | mitigate | the note rides the SHIPPED persistDecisionTrace decision-trace surface (where routing_trace_notes already lands); ZERO new write paths added; no new room.db write exists outside navigation.cjs -- room.db is closed by render time and the plan adds no db call (greppable: no new sqlite/Database require in the diff) |
| T-150.5-08 | Spoofing | prose mimicry -- the model hand-paints triangles without a card (143.2 doctrine taught the format) | accept (residual) + mitigate downstream | a code gate cannot fully prevent model prose; the doctrine line (Plan 03) forbids drawing glyphs without firing the card; residual risk accepted per 150.5-RESEARCH.md Finding B item 4 |
| T-150.5-09 | Tampering | a frozen contract drifting through the render edits | mitigate | test-148-frozen-contracts + reach/posture drift fences re-run in Task 3 verify; dial-presenter receives comment-only bytes |
| T-150.5-SC | Tampering | npm/pip/cargo installs | accept | zero package installs; no legitimacy checkpoint required |
</threat_model>

## Artifacts this phase plan produces

| Artifact | Kind | Symbol(s) |
|---|---|---|
| scripts/intent-classifier.cjs | edited module | renderEngineDecisionWithDial threads appendAskUserQuestionTrailer; ctx.onRenderNote fault callback; traceEntry.dial_render_note merge; render-before-persist reorder; SEED-020 exemption comment |
| tests/test-150-5-render-atomicity.cjs | new test suite | engine-arm atomic emission, legacy neither, D-01 tier-0 cold-card arm, fault-note arm, construction-door fence, trailer shape fence, frozen constants |
| tests/claim-harness/claim-c2.cjs | edited suite | C2-seen contract-presence assertion |
| lib/hmi/dial-presenter.cjs | comment-only edit | 'preview' terminology-collision NOTE (rename rides Phase 152) |

<verification>
- node tests/test-150-5-render-atomicity.cjs exits 0 -- atomic emission both arms, D-01 cold-card arm, fault note, single door
- node tests/test-150-render-unlock.cjs exits 0 and node tests/claim-harness/claim-c2.cjs exits 0 (the strengthened harness)
- node tests/test-148-frozen-contracts.cjs + test-reach-ids-drift + test-posture-ids-drift all exit 0 (frozen surfaces byte-unchanged)
- grep -F '[AskUserQuestion contract:' scripts/intent-classifier.cjs lib/hmi/dial-presenter.cjs returns no matches (construction stays in the dispatcher)
- Zero em-dashes/en-dashes across all four touched files
</verification>

<success_criteria>
DIAL-ATOM-01: the contract reaches the rendered surface whenever the triangle
text does (one emission site, never text-only); a render fault writes a
dial_render_note instead of vanishing; D-01's hybrid sensor-fired cold card is
live (tier-0 engine arm renders S4 + '--' + trailer; legacy renders nothing);
the C2 harness can no longer stay green through the split-render failure; every
frozen fence stays green.
</success_criteria>

<output>
Create `.planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-02-SUMMARY.md` when done
</output>
