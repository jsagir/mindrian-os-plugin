---
phase: 150.5-sensor-turn-contract-and-atomic-dial-render
plan: 03
subsystem: acceptance-harness
tags: [acpt-06, dogfood-acceptance, phase-gate, anti-mimicry, d-01, dial-atom-02, dial-atom-03]
requires:
  - lib/core/insight-sensors.cjs deriveTurnSignals (Plan 01 one-seam normalization)
  - scripts/intent-classifier.cjs renderEngineDecisionWithDial (Plan 02 atomic emission)
  - lib/hmi/selector-dispatcher.cjs appendAskUserQuestionTrailer (Plan 02 deviation export)
  - tests/dogfood/fixtures/synthetic-room.cjs makeSyntheticRoom (Phase 146 fixture builder)
  - scripts/doctor.cjs runDogfoodAcceptance ACPT_DRIVERS roster (Phase 146 Plan 04)
provides:
  - tests/test-acpt-06-dial-atomic-emission.cjs -- the hermetic 6th ACPT leg
    (production-shape turn, real sensor fire, atomic text+contract emission,
    legacy honest-negative, Part-8 sentinel sweep)
  - doctor --dogfood-acceptance as a SIX-leg gate (ACPT-01..06)
  - tests/run-all-146.sh extended with ACPT-06 (6-leg enumeration, no stale prose)
  - tests/run-all-150.5.sh -- the ONE-green-command phase gate
  - larry-personality Operating-the-Dial behavior 5 (anti-mimicry doctrine)
  - D-01 EXECUTED record in 150.5-CONTEXT.md (DIAL-ATOM-03 closed)
  - docs/CANON-PHASE-MAP.md Phase 150.5 shipped row
affects:
  - Phase 152 (archetype rollout rides a harness that now catches split renders)
  - Phase 154 (cockpit binary inherits the D-01 hybrid; C5 always-on card de-scoped there)
  - any future release gate (doctor --dogfood-acceptance non-zero blocks the milestone)
tech-stack:
  added: []
  patterns:
    - ACPT driver registration (one-object ACPT_DRIVERS append + CJS_SUITES entry;
      the Phase 146 checklist-point contract extended, not forked)
    - two-group suite-runner aggregator (run-all-150.sh pattern mirrored for the
      150.5 phase gate; PASS/FAIL/MISSING tally, missing gates to non-zero)
key-files:
  created:
    - tests/test-acpt-06-dial-atomic-emission.cjs
    - tests/run-all-150.5.sh
  modified:
    - scripts/doctor.cjs
    - tests/run-all-146.sh
    - skills/larry-personality/SKILL.md
    - docs/CANON-PHASE-MAP.md
    - .planning/phases/150.5-sensor-turn-contract-and-atomic-dial-render/150.5-CONTEXT.md
decisions:
  - "ACPT-06 stays hermetic: MOS_NAV_TEST_FIRE_SKILL deleted defensively at top;
    the positive proof is the REAL sensor fired from the seam-derived signal
    (the contradict fixture's fresh last-cascade.json side-channel); zero
    signal-bag keys anywhere in the driver (grep-asserted)"
  - "run-all-150.5.sh lists run-all-144.sh explicitly even though run-all-146.sh
    re-runs it -- cheap, and 150.5-VALIDATION.md names it in the contract"
  - "D-01 execution record is append-only in 150.5-CONTEXT.md: the LOCKED block
    byte-unchanged; the EXECUTED record points at Plan 02 and the two proving
    suites; C5 always-on cold card stays de-scoped to Phase 154"
metrics:
  duration: "~30 min (resumed run; Task 1 executed by the crashed prior instance)"
  completed: 2026-06-10
  tasks: 3
  commits: 3
---

# Phase 150.5 Plan 03: ACPT-06 + Phase Gate + Doctrine + D-01 Closeout Summary

doctor --dogfood-acceptance is now a SIX-leg gate whose new leg (ACPT-06)
fires a REAL sensor from the PRODUCTION turn shape and asserts dial text +
AskUserQuestion contract emit atomically on the engine arm with a legacy
honest-negative -- the leg that would have caught both the dead-sensor spine
and the split render -- locked by the one-command phase gate
tests/run-all-150.5.sh (7/7 green), the larry-personality anti-mimicry
doctrine line, and the D-01 EXECUTED record closing DIAL-ATOM-03.

## What was done

### Task 1: the ACPT-06 hermetic driver (commit d7264546, prior executor)

Created tests/test-acpt-06-dial-atomic-emission.cjs (274 lines) mirroring
ACPT-01's hermetic structure with the two Phase-150.5 upgrades:

- Fire arm: makeSyntheticRoom({ contradict: true }) writes the fresh
  last-cascade.json side-channel; the turn carries the EXACT production keys
  ({ userText, sectionPath, sessionId }) with NO signal bag and NO text
  alias; the REAL decide() derives artifact_filed via the Plan-01 seam,
  fire_skill goes non-null, routeActivation returns source=engine.
- Atomic arm: renderEngineDecisionWithDial emits BOTH "Larry can reach for:"
  AND "[AskUserQuestion contract:" in one block (gate 11 closed).
- Honest-negative arm: a COLD fixture (no side-channel) keeps fire_skill
  null, routes legacy, and renders NEITHER substring.
- Part-8 arm: an obviously-fictional proprietary sentinel in the side-channel
  finding body never appears in the emitted block or the stringified
  decision_trace.
- MOS_NAV_TEST_FIRE_SKILL deleted defensively; fixtures cleaned in finally.

Acceptance criteria re-verified on resume: exit 0; `grep -cE "signals\s*:"`
returns 0; 4 defensive-delete mentions; both assertion substrings present;
zero em/en-dashes.

### Task 2: ACPT-06 registration (commit 74c7ba26)

- scripts/doctor.cjs: ACPT_DRIVERS gains the 6th row ({ id: 'ACPT-06',
  file: 'test-acpt-06-dial-atomic-emission.cjs', label: 'dial text + card
  contract emit atomically...' }); every roster-count prose mention moved
  from 5 to 6 (the flag comment, the usageText loop-fires section, the
  ACPT_DRIVERS header enumeration, the main() dispatch comment).
  runDogfoodAcceptance body unchanged (it maps the roster).
- tests/run-all-146.sh: CJS_SUITES gains the suite; the header enumerates
  ACPT-06 as the Phase 150.5 leg; "5 ACPT" counts moved to 6.
- Resume note: the crashed instance left these edits in the working tree;
  verification found ONE residual stale line ("# (a) The 5 ACPT dogfood
  drivers." section comment at run-all-146.sh:79) which was fixed before
  commit. Everything else in the in-flight diff was correct.

### Task 3: phase gate + doctrine + D-01 record + canon-map row (commit 75d166ca)

- tests/run-all-150.5.sh (158 lines, executable): two-group aggregator
  mirroring run-all-150.sh. Group (a): test-150-5-sensor-firability.cjs
  (trigger-chain gate 4) + test-150-5-render-atomicity.cjs (gates 7+11+12) +
  test-acpt-06-dial-atomic-emission.cjs (end-to-end leg) +
  test-retrieval-seed.cjs (D-03a Part-8 fence). Group (b):
  run-all-144.sh + run-all-146.sh + run-all-150.sh (carrying the 148
  frozen-contract fences + the strengthened claim harness). PASS/FAIL/
  MISSING tally; missing or failed exits non-zero.
- skills/larry-personality/SKILL.md: Operating-the-Dial behavior 5 appended
  (never hand-draw the dial glyphs; trailer present means the card MUST fire
  in the same response; "no card, no picture"); the Mode B / Tier 0
  paragraph gains the D-01 hybrid sentence (sensor-fired tier-0 turn renders
  S4 framing WITH the live card; genuinely cold turn renders no dial).
- 150.5-CONTEXT.md: "## D-01 execution record" appended (append-only,
  git-diff verified 9 insertions / 0 deletions): EXECUTED 2026-06-09,
  implemented by Plan 150.5-02, proven by the atomicity suite + ACPT-06,
  C5 de-scoped to Phase 154. DIAL-ATOM-03 closed.
- docs/CANON-PHASE-MAP.md: Phase 150.5 shipped row appended to the v1.13.1
  connector-spine section (canon parts Part 2/3/6/7/8/9; names the one-seam
  normalization, the atomic emission, ACPT-06, and run-all-150.5.sh).

## Verification (all green)

| Gate | Result |
|---|---|
| node scripts/doctor.cjs --dogfood-acceptance | exit 0, 6/6 ACPT points, ACPT-06 named |
| bash tests/run-all-146.sh | exit 0, 9/9 (ACPT-06 PASSED in the run log) |
| bash tests/run-all-150.5.sh | exit 0, 7/7 (both new suites + ACPT-06 + D-03a fence + 3 carried aggregators) |
| test -x tests/run-all-150.5.sh | executable bit set |
| grep "the 5 ACPT" / "5 ACPT" in doctor.cjs + run-all-146.sh | no matches |
| grep "no card, no picture" + "D-01 hybrid" SKILL.md | both present |
| grep "D-01 execution record" 150.5-CONTEXT.md | present; pre-existing lines byte-unchanged |
| grep "sensor-turn-contract-and-atomic-dial-render" CANON-PHASE-MAP.md | present |
| em/en-dash sweep over every new/edited line | 0 matches |

## Frozen surfaces audit

MAX_K=3, DIAL_REACH_K=6, RECOMMEND_FLOOR=0.70, MARGIN_THRESHOLD=0.15, the
6-reach bank, and the 3 postures held: the carried run-all-148 fences (inside
run-all-150.sh) and the reach/posture drift suites all green inside the
phase gate. Zero Brain egress delta: this plan adds tests, shell aggregation,
and prose only; no production egress path touched (T-150.5-SC: zero package
installs, node built-ins + existing project libs only).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Residual stale "5 ACPT" section comment in run-all-146.sh**
- **Found during:** Task 2 resume verification
- **Issue:** the crashed instance's in-flight edit updated the echo line
  ("Group (a): the 6 ACPT dogfood drivers") but left the section comment one
  line above it reading "# (a) The 5 ACPT dogfood drivers." -- a stale-prose
  surface contradicting the 6-leg gate (threat T-150.5-13)
- **Fix:** moved the comment to 6 before committing Task 2
- **Files modified:** tests/run-all-146.sh
- **Commit:** 74c7ba26

No other deviations: Tasks 1 and 3 executed exactly as written.

## Execution note (crash resume)

The original executor completed Task 1 (commit d7264546) and crashed mid-Task-2
(API connection drop). This instance verified the committed Task 1 driver
against all five acceptance criteria, audited the uncommitted Task 2 working
tree edits line by line (one stale-prose fix, above), and completed Tasks 2-3.
No work was redone; no commits were amended.

## Commits

| Commit | Type | Description |
|---|---|---|
| d7264546 | test(150.5-03) | ACPT-06 -- production turn fires a real sensor; text+contract emit atomically |
| 74c7ba26 | feat(150.5-03) | ACPT-06 registered -- doctor --dogfood-acceptance is a 6-leg gate |
| 75d166ca | docs(150.5-03) | phase gate run-all-150.5.sh + dial atomicity doctrine + D-01 executed + canon-map row |

## TDD Gate Compliance

Task 1 carried tdd="true": the test commit (d7264546) precedes the
registration/feature commit (74c7ba26). The driver tests SHIPPED Plan 01/02
behavior end to end (the RED phase for that behavior was carried by Plans 01
and 02's own RED suites); no unexpected-pass occurred -- the driver exited 0
against the already-landed implementation as the plan intends for an
acceptance leg.

## Known Stubs

None. ACPT-06 drives the real decide() / routeActivation /
renderEngineDecisionWithDial units with no stubs of the units under test;
the phase gate, doctrine line, D-01 record, and canon-map row are all live
content with no placeholders.

## Threat Flags

None beyond the plan's threat model. No new network endpoints, auth paths,
file-access patterns, or trust-boundary schema changes: fixtures live under
os.tmpdir() with obviously-fictional content cleaned in finally (T-150.5-12);
the driver's exit code crossing into the release-blocking gate is the
intended boundary (T-150.5-10 mitigated by the real-sensor positive + the
honest-negative); the Part-8 sentinel arm covers T-150.5-11.

## Self-Check: PASSED

Created files exist (tests/test-acpt-06-dial-atomic-emission.cjs,
tests/run-all-150.5.sh, this SUMMARY); commits d7264546, 74c7ba26, and
75d166ca verified present in git log; bash tests/run-all-150.5.sh exit 0;
zero em/en-dashes in this file.
