---
phase: 164-bono-research-debate-engine
plan: 03
type: execute
wave: 3
depends_on: [164-01]
files_modified:
  - lib/core/issue-tree.cjs
  - commands/diagnose.md
  - tests/test-issue-tree-engine.cjs
  - tests/test-issue-tree-edge-remap.cjs
  - tests/run-all-164.sh
autonomous: true
requirements: [E2-remap, D-164-S4]
canon_parts: [2, 3, 4, 8, 9]

must_haves:
  truths:
    - "MindrianIssueTree.js is ported to lib/core/issue-tree.cjs with the navigator-LOCKED edge remap applied: INVALIDATED->INVALIDATES, RESOLVES_VIA(root-cause half)->ROOT_CAUSES, RESOLVES_VIA(opportunity half)->ENABLES, BELONGS_TO->PART_OF, INFORMS->INFORMS, and zero Math.random"
    - "the engine is a SINGLE deterministic build call (validateMECE + validateFalsifiability + renderMarkdown + toGraphEdges); it does NOT ride runChain (runChain carries the debate, not the deterministic tree build)"
    - "every edge the engine emits is a member of the frozen ALLOWED_EDGE_TYPES set (INVALIDATES / ROOT_CAUSES / ENABLES / PART_OF / INFORMS all frozen post-163 + post-168); graph emission routes through the navigation.cjs writeEdge chokepoint"
    - "/mos:diagnose carries a second sub_mode issue-tree under the SAME reach (reach_id context_block, same framework/filing/posture) with ignite's sensor pair [SENS-01, SENS-06]; it is offered as a candidate reach at a Shape F.1 gate, never auto-rendered"
    - "the hat-lens disclaimer (White/Black perspective-lens) travels in the FILED artifact, never in the tree output; MECE/falsifiability warnings surface to the navigator"
  artifacts:
    - path: "lib/core/issue-tree.cjs"
      provides: "the ported deterministic issue-tree engine with the edge remap + navigation.cjs writeEdge emission"
      min_lines: 120
    - path: "commands/diagnose.md"
      provides: "the issue-tree sub_mode connector (same reach_id, [SENS-01, SENS-06], offered at a Shape F.1 gate)"
      contains: "issue-tree"
    - path: "tests/test-issue-tree-engine.cjs"
      provides: "MECE + falsifiability + renderMarkdown + single-call + no-Math.random assertions"
      min_lines: 50
    - path: "tests/test-issue-tree-edge-remap.cjs"
      provides: "the edge remap + frozen-vocabulary assertion (every emitted edge is in ALLOWED_EDGE_TYPES)"
      min_lines: 40
  key_links:
    - from: "lib/core/issue-tree.cjs"
      to: "lib/core/navigation.cjs"
      via: "toGraphEdges output written through navigation.writeEdge (the chokepoint, never raw SQL)"
      pattern: "writeEdge"
    - from: "lib/core/issue-tree.cjs"
      to: "lib/core/navigation/edges.cjs"
      via: "every emitted edge type is a member of the frozen ALLOWED_EDGE_TYPES (asserted)"
      pattern: "ROOT_CAUSES"
    - from: "commands/diagnose.md"
      to: "lib/core/issue-tree.cjs"
      via: "the issue-tree sub_mode invokes the deterministic build"
      pattern: "issue-tree"
---

<objective>
WAVE 3: the diagnostic issue-tree engine port + the /mos:diagnose issue-tree sub_mode. This ports
reference/issue-tree/MindrianIssueTree.js to lib/core/issue-tree.cjs (164-ISSUE-TREE.md): the runnable,
zero-dependency, no-Math.random deterministic engine -- validateMECE (overlap + >=2-branch
exhaustiveness), validateFalsifiability (every leaf carries an elimination test), renderMarkdown (the
agent's only allowed output), toGraphEdges (the room-graph subgraph) -- as a SINGLE deterministic build
call. It does NOT ride runChain: runChain is the sequential gated debate loop (Wave 5); the issue-tree
is a one-shot deterministic build, so it stays a direct engine call (164-RESEARCH.md C).

THE EDGE REMAP (navigator-LOCKED 2026-06-18, 164-ISSUE-TREE.md section 5) is applied during the port,
NOT as a canon amendment -- the frozen set already covers the semantics (and Phase 168 already brought
INVALIDATES / ENABLES into the Part 9 chokepoint). The engine's two non-frozen emissions are remapped:
INVALIDATED -> INVALIDATES (frozen); RESOLVES_VIA(root-cause leaf -> governing problem) -> ROOT_CAUSES
(frozen, Phase 150.8); RESOLVES_VIA(opportunity-bank-seed half) -> ENABLES (frozen, Phase 168);
BELONGS_TO -> PART_OF (frozen, Phase 163); INFORMS stays INFORMS. So every edge the ported engine emits
is a frozen member, written through the navigation.cjs writeEdge chokepoint, with a frozen-vocabulary
assertion in the phase gate.

The issue tree adds a SECOND sub_mode to the already-shipped /mos:diagnose reach (164-ISSUE-TREE.md
section 2): sub_mode issue-tree under the SAME reach_id context_block, same framework / filing
(fileEvidenceWithReadback) / posture (push_forward), given ignite's sensor pair [SENS-01, SENS-06] so
it rides the same front door and is the natural first diagnostic reach right after ignite's B3 first-win
gate. Larry does NOT auto-render the tree; he surfaces it as a candidate reach at a Shape F.1 Decision
Gate ("This reads like a why-is-this-happening problem. Want me to map the causes?"). The connector
block is added per D-164-S4 (the surface wiring is generated/validated via the /mos:new-surface +
build-connector-registry --check discipline, not hand-invented); the issue-tree sub_mode reuses the
existing diagnose reach_id and never mints a 7th reach.

Reuse before build (Canon Part 7): MindrianIssueTree.js is RUNNABLE/portable as-is (164-ISSUE-TREE.md;
zero deps, no Math.random); the port reuses its four pure functions verbatim in logic, swapping only
the edge constants (the remap) and routing toGraphEdges through navigation.writeEdge. The tree renders
via the existing De Stijl /mos:export path (no re-styling). The sub_mode rides the SHIPPED
/mos:diagnose connector (one reach, two modes), not a new command.

Output: the ported engine, the sub_mode connector, two test suites, run-all-164.sh registration. NO
em-dashes (CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-bono-research-debate-engine/164-ISSUE-TREE.md
@.planning/phases/164-bono-research-debate-engine/reference/issue-tree/MindrianIssueTree.js
@.planning/phases/164-bono-research-debate-engine/reference/issue-tree/mos-diagnose-issue-tree.md
@commands/diagnose.md
@lib/core/navigation/edges.cjs
@lib/core/navigation.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port MindrianIssueTree.js to lib/core/issue-tree.cjs with the edge remap</name>
  <files>lib/core/issue-tree.cjs, tests/test-issue-tree-engine.cjs, tests/test-issue-tree-edge-remap.cjs</files>
  <behavior>
    - Test 1 (MECE): validateMECE flags overlapping branches and a <2-branch decomposition; a clean MECE tree returns zero warnings.
    - Test 2 (falsifiability): validateFalsifiability flags a leaf with no elimination test; a tree where every leaf carries an elimination test returns zero warnings.
    - Test 3 (render + determinism): renderMarkdown is the only output; building the SAME tree twice yields byte-identical markdown and byte-identical graphEdges (no Math.random, no Date.now in the build path).
    - Test 4 (edge remap): toGraphEdges emits ONLY frozen members -- a failed-falsification branch yields an INVALIDATES edge (not INVALIDATED); a confirmed leaf root cause yields ROOT_CAUSES (leaf -> governing problem) AND ENABLES (confirmed cause -> opportunity-bank candidate); a branch yields PART_OF (not BELONGS_TO) to the governing problem; a child cause yields INFORMS to its parent. Every emitted edge_type is asserted a member of the require()'d ALLOWED_EDGE_TYPES.
    - Test 5 (single call + chokepoint): the build is one deterministic call returning {markdown, graphEdges, warnings}; writing graphEdges routes through a navigation.writeEdge spy (no raw SQL, no runChain require).
  </behavior>
  <action>Port reference/issue-tree/MindrianIssueTree.js to lib/core/issue-tree.cjs, keeping validateMECE (:38-66), validateFalsifiability (:71-83), renderMarkdown (:86-104), and toGraphEdges (:108-132) verbatim in logic and the single build entry (:135-150) as the one deterministic call. Apply the navigator-LOCKED edge remap (164-ISSUE-TREE.md section 5) by changing ONLY the edge-type constants toGraphEdges emits: INVALIDATED -> INVALIDATES; the RESOLVES_VIA root-cause-leaf half -> ROOT_CAUSES (leaf -> governing problem); the RESOLVES_VIA opportunity-seed half -> ENABLES (confirmed cause -> opportunity-bank candidate); BELONGS_TO -> PART_OF; INFORMS unchanged. Keep zero Math.random and zero Date.now in the build path (deterministic; any timestamp is injected by the caller, mirroring the GENESIS handoff no-Date.now rule). Add a writeIssueTreeEdges(db, graphEdges) helper that routes EVERY edge through navigation.writeEdge (the chokepoint, never raw SQL); it must NOT require runChain (the tree is a deterministic build, not a debate). At the top of the module, require ALLOWED_EDGE_TYPES from lib/core/navigation/edges.cjs and add a self-check that every edge type the engine can emit is a frozen member (fail-fast at module load if a remap drifted). The hat-lens disclaimer (White/Black perspective-lens, not expert advice) is a string the FILER attaches to the artifact; it is NEVER part of renderMarkdown output (assert renderMarkdown contains no disclaimer text). Then create tests/test-issue-tree-engine.cjs (Tests 1-3, 5) and tests/test-issue-tree-edge-remap.cjs (Test 4, the frozen-vocabulary + remap assertion). NO fenced code in this action; the implementation lives in issue-tree.cjs following the cited reference lines.</action>
  <verify>
    <automated>node tests/test-issue-tree-engine.cjs && node tests/test-issue-tree-edge-remap.cjs && echo ENGINE_OK</automated>
  </verify>
  <done>lib/core/issue-tree.cjs is the ported deterministic engine with the edge remap applied; every emitted edge is a frozen member written through navigation.writeEdge; the build is a single deterministic call with no Math.random and no runChain dependency; the disclaimer is never in the tree output.</done>
</task>

<task type="auto">
  <name>Task 2: Add the issue-tree sub_mode to the /mos:diagnose connector</name>
  <files>commands/diagnose.md, tests/run-all-164.sh</files>
  <action>Add a second sub_mode issue-tree to commands/diagnose.md under the SAME reach (164-ISSUE-TREE.md section 2 + the plan-phase checklist section 8). Per D-164-S4, the surface wiring is GENERATED/validated through the /mos:new-surface + build-connector-registry --check discipline, not hand-invented: run scripts/build-new-surface.cjs (or the documented connector-registry regeneration path) to emit/validate the issue-tree sub_mode connector carrying the SAME reach_id (context_block), framework ("Problem Definition Transformation Framework"), filing (fileEvidenceWithReadback), posture (push_forward), hierarchy_rank, and the ignite sensor pair sensor_triggers [SENS-01, SENS-06] (so it rides the ignite front door and is offered post-B3). It mints NO new reach_id (one reach, two modes: problem-diagnosis classify vs issue-tree decompose-causally). In the command body, document that Larry does NOT auto-render the tree -- he surfaces it as a candidate reach at a Shape F.1 Decision Gate ("This reads like a why-is-this-happening problem. Want me to map the causes?"); on selection the engine builds + validates, MECE/falsifiability warnings surface to the navigator (never auto-suppressed), the typed edges are written through navigation.cjs, and the hat-lens disclaimer is filed with the artifact (never shown in the tree). The issue-tree sub_mode sources its key question from the active room's problem-definition/ governing problem when one exists. Then add a connector --check assertion to tests/run-all-164.sh (assert build-connector-registry.cjs --check is clean for the diagnose surface, mirroring the run-all-163/166 connector-check idiom) and register no new CJS suite (Task 1 suites already registered or appended here if not). NO em-dashes in any edited or created file.</action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check >/dev/null 2>&1 && grep -q "issue-tree" commands/diagnose.md && grep -q "SENS-01" commands/diagnose.md && bash tests/run-all-164.sh && echo SUBMODE_OK</automated>
  </verify>
  <done>commands/diagnose.md carries the issue-tree sub_mode under the same reach_id with [SENS-01, SENS-06]; the connector --check is clean (no new reach minted); the command documents the Shape F.1 offer + the disclaimer-in-artifact rule; run-all-164.sh registers the connector check and is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| issue-tree edge emission -> room.db | every emitted edge routes through navigation.writeEdge; the frozen-vocab self-check fails fast on a remap drift |
| tree output -> navigator | renderMarkdown is the only output; the hat-lens disclaimer is filed with the artifact, never in the tree |
| sub_mode -> the reach bank | the issue-tree mode rides the existing diagnose reach_id; the connector --check forbids a 7th reach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-10 | Tampering | a non-frozen edge type slipping through the remap | mitigate | the module-load self-check + test-issue-tree-edge-remap.cjs assert every emitted edge is in ALLOWED_EDGE_TYPES |
| T-164-11 | Spoofing | a 7th reach minted by the sub_mode | mitigate | the connector --check asserts the issue-tree mode rides the existing context_block reach_id; no new reach |
| T-164-12 | Information disclosure | venture content in a Brain query | accept | the issue-tree build is LOCAL deterministic; it makes no Brain call; asserted by the Wave 6 leak scan |
| T-164-13 | Tampering | non-determinism (Math.random / Date.now) | mitigate | the engine test asserts byte-identical re-builds; timestamps are caller-injected |
| T-164-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (ported zero-dep engine + doc edits); no install task |
</threat_model>

<verification>
- `node tests/test-issue-tree-engine.cjs` passes (MECE, falsifiability, determinism, single-call, chokepoint).
- `node tests/test-issue-tree-edge-remap.cjs` passes (every emitted edge is a frozen member; the remap is correct).
- `node scripts/build-connector-registry.cjs --check` is clean (issue-tree sub_mode rides the existing reach).
- commands/diagnose.md carries the issue-tree sub_mode with [SENS-01, SENS-06] and the Shape F.1 offer + disclaimer rule.
- `bash tests/run-all-164.sh` is green.
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
The deterministic issue-tree engine is ported with the navigator-LOCKED edge remap, emits only frozen
edges through the navigation chokepoint, and stays a single build call independent of runChain; the
/mos:diagnose reach gains an issue-tree sub_mode riding ignite's sensor pair, offered at a Shape F.1
gate, with the disclaimer filed in the artifact. run-all-164.sh is green.
</success_criteria>

<output>
Create `.planning/phases/164-bono-research-debate-engine/164-03-SUMMARY.md` when done.
</output>
