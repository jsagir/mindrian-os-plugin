---
phase: 164-bono-research-debate-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/transitions.cjs
  - .planning/phases/108-graph-memory-schema-reconciliation/aliases.yml
  - docs/MINDRIAN-CANON.md
  - docs/CANON-PHASE-MAP.md
  - tests/test-synthetic-expert-nodetype-floor.cjs
  - tests/run-all-164.sh
autonomous: false
requirements: [E1, D-164-S1]
canon_parts: [2, 4, 6, 8, 9]

must_haves:
  truths:
    - "SyntheticExpert is an added node type in the Phase 108 frozen taxonomy: aliases.yml node_aliases carries a SyntheticExpert entry (resolution NEW) and check-schema-aliases.cjs still passes"
    - "TRUTH_CLAIM_TYPES in transitions.cjs is extended to include SyntheticExpert so an expert is human-confirm-gated (Part 9 role 5), and every prior truth-claim type still present (FLOOR)"
    - "SyntheticExpert + its proposed->confirmed lifecycle appear in a new Appendix D entry in docs/MINDRIAN-CANON.md AND the Part 2 team-identity prose references the reusable-expert citizen"
    - "the canon version line bumps v1.12 -> v1.13 and a CANON-PHASE-MAP version-history row + a Phase 164 map row record the amendment"
    - "the floor test asserts every prior TRUTH_CLAIM_TYPES member still passes AND SyntheticExpert is a member AND promoteNodeStatus gates a SyntheticExpert (no agent-attributed confirm)"
  artifacts:
    - path: "lib/core/navigation/transitions.cjs"
      provides: "TRUTH_CLAIM_TYPES extended with SyntheticExpert via one additive member"
      contains: "SyntheticExpert"
    - path: ".planning/phases/108-graph-memory-schema-reconciliation/aliases.yml"
      provides: "node_aliases SyntheticExpert entry (resolution NEW, canon_parts Part 2/4/9)"
      contains: "SyntheticExpert"
    - path: "tests/test-synthetic-expert-nodetype-floor.cjs"
      provides: "membership + FLOOR + human-confirm-gate assertions (never .size)"
      min_lines: 40
    - path: "docs/MINDRIAN-CANON.md"
      provides: "Part 2 expert-citizen mention + Appendix D entry 24 + version bump to 1.13"
      contains: "SyntheticExpert"
    - path: "tests/run-all-164.sh"
      provides: "phase aggregator scaffold (Wave 1 registers the floor test + em-dash sweep)"
      min_lines: 30
  key_links:
    - from: "lib/core/navigation/transitions.cjs"
      to: "tests/test-synthetic-expert-nodetype-floor.cjs"
      via: "require TRUTH_CLAIM_TYPES + promoteNodeStatus"
      pattern: "TRUTH_CLAIM_TYPES"
    - from: ".planning/phases/108-graph-memory-schema-reconciliation/aliases.yml"
      to: "scripts/check-schema-aliases.cjs"
      via: "node_aliases SyntheticExpert entry parsed by the schema guard"
      pattern: "SyntheticExpert"
    - from: "docs/MINDRIAN-CANON.md"
      to: "docs/CANON-PHASE-MAP.md"
      via: "version-history row references the same Appendix D entry"
      pattern: "164"
---

<objective>
WAVE 1 FOUNDATION: the E1 SyntheticExpert node-type canon amendment, landed as ONE atomic lockstep
wave so CI never goes RED mid-phase (per D-164-S1). SyntheticExpert is NOT in the Phase 108 frozen
node taxonomy; adding it is a navigator-gated additive amendment (aliases.yml node_aliases entry +
TRUTH-STATES truth-claim membership), mirroring the 169/168/163/150.8 frozen-set moves (Appendix D
entries 23/22/21/18). The live canon today is v1.12 with Appendix D ending at entry 23 (NESTED_WITHIN,
taken by Phase 169 on 2026-06-19); this E1 amendment stacks cleanly ON TOP of 169's v1.12, bumping
v1.12 -> v1.13 and adding Appendix D entry 24. Because a SyntheticExpert is human-confirm-gated (Part 9
role 5: the navigator decides which experts are worth keeping), this wave ALSO extends TRUTH_CLAIM_TYPES in
lib/core/navigation/transitions.cjs:28 so promoteNodeStatus gates an expert's proposed->confirmed
promotion exactly as it gates {claim, CausalClaim, assumption, decision, opportunity}. Every lockstep
surface moves together: the transitions.cjs additive member, the aliases.yml node_aliases entry, the
docs/MINDRIAN-CANON.md Part 2 expert-citizen mention + a new Appendix D entry 24 + the header/footer
version bump to 1.13, the docs/CANON-PHASE-MAP.md Phase 164 row + a v1.13 version-history row, and a
canonical floor test.

E2 IS ALREADY DONE: Phase 168 (SHIPPED 2026-06-18, Appendix D entry 22) brought CONVERGES /
INVALIDATES / ENABLES into the Part 9 frozen ALLOWED_EDGE_TYPES set. The issue-tree edges now all
resolve to frozen members (INVALIDATED -> INVALIDATES frozen; RESOLVES_VIA -> ROOT_CAUSES frozen +
ENABLES now frozen; BELONGS_TO -> PART_OF frozen by Phase 163). So Wave 1 carries NO edge amendment;
it carries ONLY the E1 node-type addition. Do NOT re-touch edges.cjs.

CONFIRM THE LIVE NUMBERS BEFORE WRITING: read docs/MINDRIAN-CANON.md header + footer + the last
Appendix D entry, and docs/CANON-PHASE-MAP.md "Canon reference" line + the last version-history row,
to verify the current version is v1.12 and the last entry is 23. If the live canon has moved past
v1.12 / entry 23 (a later phase shipped between planning and execution), bump to the actual-next
version + entry instead of hard-coding 1.13 / 24, and re-point every grep below to the live target.

Purpose: D-164-S1 / E1 demand the SyntheticExpert node type added via a navigator-LOCKED canon
amendment. Node types are a frozen constitutional property (the Phase 108 frozen-taxonomy contract
froze them, Part 6 dog-fooding requires the plugin to honor its own canon), so adding one is a canon
amendment, not a per-phase edit. Wave 2's writeSyntheticExpertNode chokepoint writes these nodes; this
wave is the load-bearing constitutional prerequisite that must ship first.

Output: an extended TRUTH_CLAIM_TYPES Set, the aliases.yml node entry, the amended canon + phase map,
the floor test, and the phase test-runner scaffold. NO em-dashes anywhere (CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-bono-research-debate-engine/164-CONTEXT.md
@.planning/phases/164-bono-research-debate-engine/164-SYNTHETIC-EXPERTS.md
@.planning/phases/164-bono-research-debate-engine/164-RESEARCH.md
@lib/core/navigation/transitions.cjs
@.planning/phases/108-graph-memory-schema-reconciliation/aliases.yml
@docs/MINDRIAN-CANON.md
@docs/CANON-PHASE-MAP.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend TRUTH_CLAIM_TYPES with SyntheticExpert + ship the floor test</name>
  <files>lib/core/navigation/transitions.cjs, tests/test-synthetic-expert-nodetype-floor.cjs</files>
  <behavior>
    - Test 1: TRUTH_CLAIM_TYPES.has('SyntheticExpert') === true.
    - Test 2: every prior truth-claim type still present (FLOOR): claim, CausalClaim, assumption, decision, opportunity.
    - Test 3: TRUTH_CLAIM_TYPES is a frozen Set instance (Object.isFrozen true).
    - Test 4: on an in-memory node:sqlite db with a nodes table, a node of type 'SyntheticExpert' at review_status 'proposed' is GATED by promoteNodeStatus -- an agent-attributed confirm (byUser an AGENT_IDENTITIES member) is REJECTED, and a human byUser promotes proposed -> confirmed (mirror the existing transitions.cjs guard at lines 88-93 keyed on row.type in TRUTH_CLAIM_TYPES).
    - NEVER assert .size (additive extensions must not regress the baseline; mirror the edges.cjs FLOOR doctrine).
  </behavior>
  <action>Append SyntheticExpert as ONE additive member to the TRUTH_CLAIM_TYPES frozen Set in lib/core/navigation/transitions.cjs:28 (currently Object.freeze(new Set(['claim','CausalClaim','assumption','decision','opportunity']))). The member is SyntheticExpert. Add a comment block immediately above the Set marking the addition NAVIGATOR-GATED (per D-164-S1 / E1), stating that an expert is a truth-claim node because a human confirms which experts are kept (Part 9 role 5), and that the human-confirm-gate at the promoteNodeStatus guard (lines 88-93, keyed on row.type) now covers SyntheticExpert automatically since the guard reads TRUTH_CLAIM_TYPES.has(row.type). Do NOT change the promoteNodeStatus signature or the TRANSITIONS table; the gate already keys on the Set. Then create tests/test-synthetic-expert-nodetype-floor.cjs mirroring the structure of tests/test-edges-domain-taxonomy-floor.cjs (membership + FLOOR + frozen-Set + a behavioral negative); use node:test or a plain assert harness. For Test 4, build an in-memory better-sqlite3 (node:sqlite is acceptable if that is the project idiom -- check how transitions.cjs tests build their db) nodes table carrying id/type/review_status/confirmed_by columns, insert a SyntheticExpert at 'proposed', assert promoteNodeStatus(db, id, 'proposed', 'confirmed', <AGENT_IDENTITIES member>) returns ok:false (agent-attributed confirm rejected) and promoteNodeStatus(db, id, 'proposed', 'confirmed', 'navigator') returns ok:true. NO fenced code in this action; the implementation lives in transitions.cjs following the cited line idiom.</action>
  <verify>
    <automated>node tests/test-synthetic-expert-nodetype-floor.cjs</automated>
  </verify>
  <done>The floor test passes: SyntheticExpert is a member, every prior truth-claim type is preserved, the Set is frozen, an agent-attributed SyntheticExpert confirm is rejected, and a human byUser promotes it.</done>
</task>

<task type="auto">
  <name>Task 2: Land the aliases.yml node entry + canon amendment text + phase-map rows (the docs lockstep)</name>
  <files>.planning/phases/108-graph-memory-schema-reconciliation/aliases.yml, docs/MINDRIAN-CANON.md, docs/CANON-PHASE-MAP.md, tests/run-all-164.sh</files>
  <action>In aliases.yml: add a node_aliases entry for SyntheticExpert mirroring an existing resolution NEW entry (the `claim` entry at the node_aliases head is the template): codex_term SyntheticExpert, resolution NEW, canonical_name SyntheticExpert, source_phase 164, canon_parts [Part 2, Part 4, Part 9], notes naming it the reusable expert graph citizen promoting room/team/personas .md files to queryable nodes, human-confirm-gated (Part 9 role 5), ROOM-LOCAL this phase. Confirm scripts/check-schema-aliases.cjs still passes after the edit. In docs/MINDRIAN-CANON.md: (a) extend the Part 2 "Team member identity" prose (or the Part 2 closing) with a one-sentence mention that a high-value team member may be FILED as a reusable SyntheticExpert graph citizen and RE-INVOKED as a hat in future runs (ROOM-LOCAL this phase; cross-room reuse a deferred Part-8-gated amendment), citing E1 / D-164-S1. (b) Add Appendix D entry 24 (the next number after entry 23 = NESTED_WITHIN, taken by Phase 169 on 2026-06-19 -- CONFIRM the live last entry is 23 before writing 24) recording the SyntheticExpert node-type amendment, written in the voice of entry 21 (the four-edge precedent): name the node type + its generic-lens-metadata-only fields (Part 8: hat/name/surname/archetype/beautiful_question/method/evidence_tier/invocation_count/review_status/provenance, NEVER venture content), the TRUTH_CLAIM_TYPES extension (human-confirm-gated, Part 9 role 5), cite navigator-LOCKED D-164-S1, cite the Part 6 dog-fooding canon-amendment-on-itself mechanism mirroring entries 14/15/18/19/21/22/23, note E2 was already done by Phase 168 so this amendment carries ONLY the node type, and state the floor test as the fence. (c) Bump the header Version line (currently 1.12) to 1.13 and the footer "_Mindrian Canon v1.12_" line to v1.13. In docs/CANON-PHASE-MAP.md: (a) update the "Canon reference" line from v1.12 to v1.13. (b) Add a Phase 164 row under the "v1.14.0 execution order + phase additions" section (status "shipped (Wave 1 amendment)") with canon_parts [2,4,6,8,9] and a one-line description of the SyntheticExpert node-type amendment + that E2 was split to Phase 168. (c) Add a v1.13 version-history row dated 2026-06-19 recording the amendment, mirroring the v1.12 (Phase 169) row format. Finally create tests/run-all-164.sh as the phase aggregator scaffold, mirroring tests/run-all-163.sh structure: a CJS_SUITES array (Wave 1 registers test-synthetic-expert-nodetype-floor.cjs; later waves append their suites), a per-suite PASS/FAIL loop that runs to completion (a missing file gates to a FAIL line, never a crash), an em-dash sweep over the phase artifacts written via the U+2014 codepoint escape, and a final tally with exit 1 on any failure. Reference D-164-S1 in the amendment text for traceability. NO em-dashes in any edited or created file.</action>
  <verify>
    <automated>node scripts/check-schema-aliases.cjs --file .planning/phases/108-graph-memory-schema-reconciliation/aliases.yml >/dev/null 2>&1 || node scripts/check-schema-aliases.cjs >/dev/null 2>&1; bash tests/run-all-164.sh && grep -c "SyntheticExpert" docs/MINDRIAN-CANON.md | grep -qv '^0$' && grep -q "v1.13" docs/MINDRIAN-CANON.md && echo CANON_OK</automated>
  </verify>
  <done>run-all-164.sh runs green (floor test passes, em-dash sweep clean), aliases.yml carries the SyntheticExpert node entry and the schema guard passes, MINDRIAN-CANON.md Part 2 + Appendix D entry 24 name the SyntheticExpert node type, the version is 1.13 in header and footer, and CANON-PHASE-MAP.md carries the Phase 164 row + v1.13 version-history row + canon reference updated to v1.13.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The E1 SyntheticExpert node-type frozen-taxonomy amendment: transitions.cjs TRUTH_CLAIM_TYPES additive member, the aliases.yml node_aliases entry, the floor test, the MINDRIAN-CANON.md Part 2 mention + Appendix D entry 24 + version bump to 1.13, the CANON-PHASE-MAP.md rows, and the run-all-164.sh scaffold. NO edge amendment (E2 already shipped in Phase 168).</what-built>
  <how-to-verify>
    This is a NAVIGATOR-GATED frozen-constitutional-set move (D-164-S1 / E1); it cannot auto-advance. Confirm:
    1. `node tests/test-synthetic-expert-nodetype-floor.cjs` passes (SyntheticExpert member + all prior truth-claim types + frozen + agent-confirm-rejected + human-confirm-ok).
    2. `git diff docs/MINDRIAN-CANON.md` shows ONLY the Part 2 expert-citizen mention, Appendix D entry 24, and the two version lines (1.12 -> 1.13). No doctrine drift. The E1 entry stacks ON TOP of Phase 169's entry 23 / v1.12.
    3. `git diff lib/core/navigation/edges.cjs` is EMPTY (E2 was done by Phase 168; this wave touches no edges).
    4. The SyntheticExpert generic-lens-metadata-only field list in Appendix D entry 24 matches your intent (zero venture content; Part 8).
    5. `bash tests/run-all-164.sh` is green.
  </how-to-verify>
  <resume-signal>Type "approved" to ratify the amendment, or describe the node-semantic corrections needed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| node-write -> room.db | promoteNodeStatus gates a SyntheticExpert's proposed->confirmed via the TRUTH_CLAIM_TYPES guard before any UPDATE |
| canon doc -> code | the amendment text must match the shipped Set (drift = a Part 6 self-CONTRADICTS) |
| aliases.yml -> schema guard | check-schema-aliases.cjs must still pass with the new node entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01 | Tampering | TRUTH_CLAIM_TYPES Set | mitigate | floor test asserts FLOOR + frozen Set + the human-confirm gate; additive-only member, never a rewrite |
| T-164-02 | Elevation of privilege | agent-attributed expert confirm | mitigate | the floor test asserts an AGENT_IDENTITIES byUser confirm is rejected; only a human byUser promotes (Part 9 role 5) |
| T-164-03 | Repudiation | canon amendment | mitigate | navigator-gated blocking checkpoint; Appendix D entry 24 records D-164-S1 provenance |
| T-164-04 | Information disclosure | SyntheticExpert node fields | accept | fields are generic-lens metadata only (hat/name/surname/archetype/method/beautiful_question/evidence_tier/scalars); never venture content; never cross to Brain (asserted by the Wave 6 leak scan) |
| T-164-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages in this wave (pure Node built-ins + doc edits); no install task |
</threat_model>

<verification>
- `node tests/test-synthetic-expert-nodetype-floor.cjs` passes.
- `bash tests/run-all-164.sh` is green.
- aliases.yml carries the SyntheticExpert node_aliases entry; check-schema-aliases.cjs passes.
- MINDRIAN-CANON.md version is 1.13 (header + footer); Appendix D entry 24 names the node type + its generic-lens fields + the TRUTH_CLAIM_TYPES extension.
- CANON-PHASE-MAP.md carries the Phase 164 row + the v1.13 version-history row; canon reference updated to v1.13.
- `git diff lib/core/navigation/edges.cjs` is empty (E2 already shipped in Phase 168).
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
SyntheticExpert is an added frozen node type the schema guard accepts and promoteNodeStatus
human-confirm-gates; every prior truth-claim type is preserved; the canon and phase map record the
amendment in lockstep (v1.12 -> v1.13, Appendix D entry 24 stacking on top of Phase 169's entry 23);
the floor test and run-all-164.sh are green; the navigator has ratified the frozen-set move at the
blocking checkpoint; no edge vocabulary was touched (E2 already done in 168).
</success_criteria>

<output>
Create `.planning/phases/164-bono-research-debate-engine/164-01-SUMMARY.md` when done.
</output>
</output>
