---
phase: 164-bono-research-debate-engine
plan: 04
type: execute
wave: 4
depends_on: [164-02]
files_modified:
  - agents/persona-analyst.md
  - lib/core/bono/cell-fanout.cjs
  - tests/test-bono-cell-fanout.cjs
  - tests/test-bono-cell-selfcritique.cjs
  - tests/run-all-164.sh
autonomous: true
requirements: [D-164-S2, D-164-S3, D-164-S4]
canon_parts: [2, 3, 8, 9]

must_haves:
  truths:
    - "the cell agent (persona-analyst upgrade) gains hat-scoped web (White=data, Black=failures, Green=innovation, Yellow=success, Red=none, Blue=synthesis) + local-graph-read + Brain-generic; the connector/manifest wiring is GENERATED via the /mos:new-surface --check discipline, not hand-written (D-164-S4)"
    - "each (subdomain x hat) cell returns a structured {stance, evidence, confidence} where stance is supports|challenges|refines|neutral and confidence is a scalar"
    - "the per-(subdomain x hat) fan-out is PARALLEL (the swarm pattern, mirror act --swarm planDispatch), capped by a cost governor, and does NOT ride the sequential runChain loop (D-164-S2)"
    - "each cell self-critiques (fable-mode layer 1) its {stance, evidence, confidence} BEFORE collection, so one bad cell reading cannot propagate into the debate (D-164-S3)"
    - "each cell's web leg is research-corpus.fetchCorpus carrying a GENERIC domain handle only (Part 8 auditQueryString fail-closed), never venture body"
  artifacts:
    - path: "lib/core/bono/cell-fanout.cjs"
      provides: "the parallel (subdomain x hat) cell fan-out with cost-governor cap + per-cell self-critique, returning collected {stance, evidence, confidence}[]"
      min_lines: 100
    - path: "agents/persona-analyst.md"
      provides: "the cell-agent upgrade (hat-scoped web + local-graph-read + Brain-generic) with generated connector wiring"
      contains: "stance"
    - path: "tests/test-bono-cell-fanout.cjs"
      provides: "parallel-dispatch + cap + {stance,evidence,confidence} shape + not-runChain assertions"
      min_lines: 50
    - path: "tests/test-bono-cell-selfcritique.cjs"
      provides: "fable-mode layer 1 -- a low-quality cell reading is down-weighted/dropped pre-collection"
      min_lines: 40
  key_links:
    - from: "lib/core/bono/cell-fanout.cjs"
      to: "scripts/act-command.cjs"
      via: "the parallel swarm dispatch pattern (planDispatch / parallel agent dispatch), not runChain"
      pattern: "swarm"
    - from: "lib/core/bono/cell-fanout.cjs"
      to: "lib/core/research-corpus.cjs"
      via: "each cell's web leg is fetchCorpus with a generic domain handle (Part 8 auditQueryString)"
      pattern: "fetchCorpus"
    - from: "agents/persona-analyst.md"
      to: "lib/core/bono/cell-fanout.cjs"
      via: "the upgraded cell agent is the fan-out's per-cell dispatch target"
      pattern: "persona-analyst"
---

<objective>
WAVE 4: the cell agent + the PARALLEL (subdomain x hat) cell fan-out. This upgrades
agents/persona-analyst.md (today Read/Write/Glob only, NO Brain, inline-serial -- 164-RESEARCH.md D /
164-GENESIS-TRANSLATION.md section 7) into the BONO cell agent: it gains hat-scoped web access (Canon
Part 2 TOOL ACCESS: White=data, Black=failure-cases, Green=innovation, Yellow=success-cases,
Red=intuition/no-web, Blue=synthesis), local-graph-read, and Brain-generic (framework handles + enums
only). Per D-164-S4, the connector/manifest wiring is GENERATED via the /mos:new-surface +
build-connector-registry --check discipline, not hand-written.

It ships lib/core/bono/cell-fanout.cjs -- the genuinely net-new mechanic (164-RESEARCH.md A/D: the
shipped persona path is inline-serial, so a REAL parallel sub-agent fan-out is net-new). The fan-out
is PARALLEL, the swarm pattern (D-164-S2 / 164-RESEARCH.md A: runChain is sequential-only at
chain-executor.cjs:343-458, so the cell fan-out CANNOT be a runChain sequence): it dispatches N
independent (subdomain x hat) cells via the act --swarm planDispatch pattern (act.md:311-375 /
act-command.cjs), capped by a cost governor (mirror the FUTURES_FANOUT_CAP clamp), each cell returning
a structured {stance (supports|challenges|refines|neutral), evidence[], confidence (scalar)}. The
collected results FEED the sequential debate runChain in Wave 5 -- this wave produces the collected
cell array; it does NOT force the parallel fan-out through the sequential loop.

Fable-mode layer 1 (D-164-S3 / 164-RESEARCH.md A): each cell self-critiques its {stance, evidence,
confidence} BEFORE it folds into the collection, so one bad cell reading cannot propagate into the
ruling. This is the swarm-side half of the two-layered fable-mode (the runChain selfCritiqueFn is the
debate-side half in Wave 5).

Each cell's web leg is research-corpus.fetchCorpus (164-GENESIS-TRANSLATION.md section 7:
research-corpus is the shipped graph-aware SIGNAL pipeline; Tavily is a source adapter, not a new MCP)
carrying a GENERIC domain handle only -- research-corpus.cjs calls auditQueryString fail-closed before
any dispatch (Part 8), so the BONO research leg inherits zero-egress for free.

Reuse before build (Canon Part 7): the parallel dispatch reuses the act --swarm planDispatch pattern
(no new loop runtime); the web leg reuses research-corpus.fetchCorpus + its 30-day cache + the
auditQueryString chokepoint; the cell agent is the persona-analyst UPGRADE (not a parallel persona
system); the connector wiring is generated via /mos:new-surface (D-164-S4). No new MCP, no new fetch
path.

Output: the cell-agent upgrade, the parallel fan-out module, two test suites, run-all-164.sh
registration. NO em-dashes (CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-bono-research-debate-engine/164-GENESIS-TRANSLATION.md
@.planning/phases/164-bono-research-debate-engine/164-RESEARCH.md
@agents/persona-analyst.md
@commands/act.md
@scripts/act-command.cjs
@lib/core/research-corpus.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade persona-analyst.md to the BONO cell agent (generated connector wiring)</name>
  <files>agents/persona-analyst.md</files>
  <action>Upgrade agents/persona-analyst.md into the BONO cell agent (164-RESEARCH.md D). In the agent body, document its new dual role: (a) per-cell (subdomain x hat) research returning a structured {stance, evidence, confidence}; (b) debate consolidator (the Wave-5 onStep target). Add the hat-scoped TOOL ACCESS contract verbatim from Canon Part 2 (White=data/Tavily+arxiv, Black=failure-case/risk searches, Green=patents+arxiv+deep-research, Yellow=success-case/benefit searches, Red=no external tool/intuition only, Blue=synthesis across the other hats' returns), local-graph-read (via navigation.cjs neighborhood), and Brain-generic (brain_search/brain_query with framework names + problem-type enums ONLY, ZERO user egress -- this REVERSES the Phase 95.6 D-10 no-Brain note for the cell role; state explicitly that the Brain leg carries generic handles only, never venture content). Update the allowed-tools list to add the tools the cell role needs (the research/web tools per hat scope + the navigation read path) while keeping Read/Write/Glob. CRITICAL (D-164-S4): do NOT hand-edit the connector: frontmatter block by hand -- regenerate it via scripts/build-new-surface.cjs (or the documented connector-registry regeneration path) so the connector lands transitively across the connector-registry + harness-manifest with a clean --check; the agent rides a frozen reach_id + a sub_mode (bono-cell or the persona-hats sub_mode extended), never a 7th reach. Add the agent's beautiful-question opener per archetype (Canon Appendix E). Keep the no-emoji / no-em-dash UI rules. NO em-dashes in the file.</action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check >/dev/null 2>&1 && grep -q "stance" agents/persona-analyst.md && grep -qi "hat-scoped\|White=data\|hat scope" agents/persona-analyst.md && echo AGENT_OK</automated>
  </verify>
  <done>persona-analyst.md is the BONO cell agent with hat-scoped web + local-graph-read + Brain-generic, documents the {stance, evidence, confidence} return + the consolidator role, the connector wiring is generated (--check clean, no new reach), and the beautiful-question openers are present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The parallel (subdomain x hat) cell fan-out with cost-governor cap + fable-mode layer 1</name>
  <files>lib/core/bono/cell-fanout.cjs, tests/test-bono-cell-fanout.cjs, tests/test-bono-cell-selfcritique.cjs, tests/run-all-164.sh</files>
  <behavior>
    - Test 1 (parallel + cap): runCellFanout({subdomains, hats, opts}) plans N = subdomains x hats independent cells, clamps N to the cost-governor cap (mirror the FUTURES_FANOUT_CAP clamp), and dispatches them via the parallel swarm pattern (a Promise.all over the per-cell dispatch fn injected for testability), NOT a sequential for-loop and NOT a runChain require.
    - Test 2 (shape): each collected result is {subdomain, hat, stance in {supports,challenges,refines,neutral}, evidence:[], confidence: number in [0,1]}; a cell that errors returns a graceful neutral/low-confidence stub, never crashing the fan-out (defensive).
    - Test 3 (Part 8 web leg): each cell's web leg calls research-corpus.fetchCorpus with a GENERIC domain handle derived from the subdomain (never venture body); the test asserts the query passed to a fetchCorpus spy contains only the generic handle and that auditQueryString is the pre-dispatch gate (no raw fetch, no external http).
    - Test 4 (fable-mode layer 1): a cell whose self-critique verdict is low-quality (confidence below a floor or the critique flags the evidence) is down-weighted or dropped BEFORE collection, so it cannot fold into the debate; a clean cell passes through unchanged. The self-critique runs per cell pre-collection (the swarm-side half of two-layered fable-mode).
    - Test 5 (not-runChain): the module does NOT require lib/core/chain-executor.cjs (grep-assert in the test); the fan-out is parallel, the debate consumption is Wave 5.
  </behavior>
  <action>Create lib/core/bono/cell-fanout.cjs. runCellFanout(opts) where opts = {subdomains[], hats[], dispatchCell, selfCritique, cap, researchFn}: build the (subdomain x hat) grid, clamp the grid length to the cost-governor cap (mirror the FUTURES_FANOUT_CAP clamp idiom; default cap commented), dispatch ALL cells in PARALLEL via Promise.all over dispatchCell (injected for testability; in production it dispatches the upgraded persona-analyst cell agent). Each cell: derive a GENERIC domain handle from its subdomain (never venture body), call researchFn (default research-corpus.fetchCorpus, which runs auditQueryString fail-closed pre-dispatch, Part 8), local-graph-read via navigation, and return {subdomain, hat, stance, evidence, confidence}. Defensive: a cell error returns a neutral low-confidence stub, never crashing the fan-out. Then apply fable-mode layer 1: for each returned cell call selfCritique(cell) (injected; default a verdict fn) BEFORE collection -- a low-quality verdict (confidence below a documented floor, or the critique flags the evidence) down-weights or drops the cell so it cannot propagate (D-164-S3). The module MUST NOT require lib/core/chain-executor.cjs (the fan-out is parallel; Wave 5 consumes the collected array into runChain). Return {cells: collected[], dropped: [], plan: {requested, dispatched, capped}}. Then create tests/test-bono-cell-fanout.cjs (Tests 1-3, 5) and tests/test-bono-cell-selfcritique.cjs (Test 4) with injected spies for dispatchCell/researchFn/selfCritique. Append both Wave-4 suites to the CJS_SUITES array in tests/run-all-164.sh. NO fenced code in this action. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-bono-cell-fanout.cjs && node tests/test-bono-cell-selfcritique.cjs && bash tests/run-all-164.sh && echo WAVE4_OK</automated>
  </verify>
  <done>runCellFanout dispatches the (subdomain x hat) grid in parallel (capped, defensive, not runChain), each cell returns {stance, evidence, confidence} from a Part-8-audited generic-handle web leg, and fable-mode layer 1 drops/down-weights a bad reading before collection; run-all-164.sh registers both Wave-4 suites and is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cell web leg -> external SIGNAL | research-corpus.fetchCorpus runs auditQueryString fail-closed; only a generic domain handle crosses (Part 8) |
| cell reading -> the debate | fable-mode layer 1 self-critiques each cell pre-collection; a bad reading is dropped before it can propagate |
| fan-out -> runChain | the fan-out is parallel and must NOT require chain-executor; the sequential debate is Wave 5 |
| cell agent -> Brain | the Brain leg carries generic framework handles + enums only; zero venture content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-14 | Information disclosure | a cell web query | mitigate | the web leg is research-corpus.fetchCorpus with a generic handle; auditQueryString is the fail-closed pre-egress gate; asserted by the fan-out test + the Wave 6 leak scan |
| T-164-15 | Tampering | a garbage cell reading propagating | mitigate | fable-mode layer 1 self-critiques each cell pre-collection; the self-critique test asserts a bad reading is dropped/down-weighted |
| T-164-16 | Denial of service | unbounded fan-out | mitigate | the cost-governor cap clamps the grid (FUTURES_FANOUT_CAP idiom); the test asserts the cap bites |
| T-164-17 | Tampering | the parallel fan-out forced through the sequential loop | mitigate | the module must not require chain-executor; the test grep-asserts no runChain dependency (D-164-S2) |
| T-164-18 | Information disclosure | the cell agent Brain leg | mitigate | the agent doctrine restricts Brain to generic handles + enums; the Wave 6 leak scan asserts zero user-content egress |
| T-164-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (reuses act-swarm + research-corpus); no install task |
</threat_model>

<verification>
- `node tests/test-bono-cell-fanout.cjs` passes (parallel, capped, {stance,evidence,confidence} shape, Part 8 web leg, not-runChain).
- `node tests/test-bono-cell-selfcritique.cjs` passes (fable-mode layer 1 drops a bad reading pre-collection).
- `node scripts/build-connector-registry.cjs --check` is clean (the cell-agent connector is generated, no new reach).
- agents/persona-analyst.md carries hat-scoped web + local-graph-read + Brain-generic + the {stance, evidence, confidence} contract.
- `bash tests/run-all-164.sh` is green.
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
The cell agent gains hat-scoped web + local-graph-read + Brain-generic via generated connector wiring;
the (subdomain x hat) fan-out is parallel (the swarm pattern, capped, not runChain) returning structured
{stance, evidence, confidence}; each cell self-critiques pre-collection (fable-mode layer 1) so a bad
reading cannot propagate; the web leg carries only a generic handle (Part 8). run-all-164.sh is green.
</success_criteria>

<output>
Create `.planning/phases/164-bono-research-debate-engine/164-04-SUMMARY.md` when done.
</output>
