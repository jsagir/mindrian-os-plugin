---
phase: 164-bono-research-debate-engine
plan: 06
type: execute
wave: 6
depends_on: [164-01, 164-02, 164-03, 164-04, 164-05]
files_modified:
  - tests/test-bono-verdict.cjs
  - tests/test-bono-part8-leak.cjs
  - tests/run-all-164.sh
autonomous: true
requirements: [E1, E2-remap, D-164-S1, D-164-S2, D-164-S3, D-164-S4, D-164-S5]
canon_parts: [2, 3, 4, 8, 9]

must_haves:
  truths:
    - "the adversarial verdict returns a structured {passed, findings[]} (mirror tests/test-trending-to-absurd-verdict.cjs) over the net-new + canon guards, exiting non-zero printing findings if ANY check failed"
    - "the verdict proves by INSTRUMENTATION: the cell fan-out is parallel and NOT runChain (D-164-S2 grep-assert no chain-executor require in cell-fanout.cjs); the debate IS runChain and halts at the two material gates (D-164-S1); the debate composition RIDES the 169-shipped substrate (runDerivation + findings-wirer.wireAccept, NOT a hand-rolled loop and NOT a direct navigation.writeEdge); fable-mode is two-layered (cell-side + debate-side, D-164-S3); incremental filing journals before next and resume does not re-run (D-164-S5)"
    - "the verdict proves the SyntheticExpert is human-confirm-gated (an agent-attributed confirm rejected, Part 9 role 5) and the issue-tree emits only frozen edges (E2-remap)"
    - "the verdict asserts the canon amendment landed at the live target: MINDRIAN-CANON.md header + footer are v1.13 and Appendix D entry 24 (SyntheticExpert) exists (stacking on Phase 169's v1.12 / entry 23)"
    - "the Part 8 leak scan over ALL Phase-164 surfaces finds zero user-content-to-Brain / raw-fetch / external-http tokens; each cell + the consolidator Brain leg carries generic handles only"
    - "tests/run-all-164.sh is finalized as the single PASS/FAIL phase gate: every Wave-1..6 suite + the Part 8 leak scan + the em-dash sweep + the frozen-vocabulary check + the not-runChain-fanout negative grep + the compose-on-substrate grep + the canon-version assertion + the connector --checks, runs to completion"
  artifacts:
    - path: "tests/test-bono-verdict.cjs"
      provides: "the adversarial structured verdict over the net-new + canon guards (incl. the parallel-fanout-not-runChain negative + the compose-on-runDerivation/wireAccept assertion + the human-confirm-gate + the frozen-edge assertion + the v1.13/entry-24 canon assertion)"
      min_lines: 100
    - path: "tests/test-bono-part8-leak.cjs"
      provides: "the Part 8 leak scan over all Phase-164 lib + command + agent surfaces"
      min_lines: 50
    - path: "tests/run-all-164.sh"
      provides: "the finalized phase aggregator (all 6 waves + sweeps + frozen-vocab + not-runChain negative + compose-on-substrate + canon-version + connector checks, runs to completion)"
      min_lines: 90
  key_links:
    - from: "tests/test-bono-verdict.cjs"
      to: "lib/core/bono/debate-composition.cjs"
      via: "the verdict drives the composition through the gate-halt + seam + fable-layer-2 + resume scenarios AND asserts it rides runDerivation + wireAccept"
      pattern: "runDebate"
    - from: "tests/test-bono-verdict.cjs"
      to: "lib/core/bono/cell-fanout.cjs"
      via: "the verdict grep-asserts no chain-executor require (D-164-S2 parallel-not-sequential)"
      pattern: "cell-fanout"
    - from: "tests/test-bono-verdict.cjs"
      to: "docs/MINDRIAN-CANON.md"
      via: "the verdict asserts v1.13 + Appendix D entry 24 (the E1 amendment, stacked on Phase 169's v1.12 / entry 23)"
      pattern: "v1.13"
    - from: "tests/run-all-164.sh"
      to: "tests/test-bono-verdict.cjs"
      via: "CJS_SUITES registration (the final phase gate)"
      pattern: "verdict"
---

<objective>
WAVE 6 VERIFY: the adversarial structured verdict (the harness-as-code property 6 -- "adversarial
verify returns a structured verdict, not a vibe"), mirroring the Phase 166 verify wave
(tests/test-chain-executor-verdict.cjs) and the Phase 163 verify wave it descends from. The verdict
returns {passed, findings[]} over the Phase-164 net-new + the canon guards (Part 3 material-halt, Part
8 zero-egress, Part 9 proposed-not-confirmed + human-confirm-gate, the D-164-S2 parallel-fanout-not-
runChain negative, the compose-on-169-substrate assertion, the E2-remap frozen-edge assertion, the
v1.13 / entry 24 canon assertion). Plus the finalized tests/run-all-164.sh that is the single PASS/FAIL
phase gate (every suite + the Part 8 leak scan + the em-dash sweep + the frozen-vocabulary check + the
not-runChain-fanout negative grep + the compose-on-substrate grep + the canon-version assertion + the
connector --checks).

PROVEN BY INSTRUMENTATION (the leak-net the Phase 163/166 precedent proved catches real findings):
- D-164-S2 (the load-bearing integration shape): a DEDICATED negative check grep-asserts lib/core/bono/
  cell-fanout.cjs has NO require of lib/core/chain-executor.cjs (the parallel fan-out must NOT ride the
  sequential loop), mirroring the Phase 166 B3 convergence-stop negative grep.
- D-164-S1 (the debate IS runChain): the verdict drives runDebate and asserts the gate halts at exactly
  the hypothesis-confirm + ruling material steps (Part 3), the autonomous argument steps auto-run, and
  provenanceFn stamps each step.
- COMPOSE-ON-169-SUBSTRATE (the surgical-recalibration assertion, Part 7): the verdict reads
  lib/core/bono/debate-composition.cjs source and asserts it REQUIRES lib/core/graph-derivation.cjs
  (runDerivation) AND lib/core/findings-wirer.cjs (wireAccept) and does NOT call navigation.writeEdge
  directly (the derived relationships ride runDerivation's frozen CASCADE_SUBSET; the ruling + tension
  ride wireAccept/wireReject; no hand-rolled derivation loop, no direct edge write). grep -v '^#'
  filtered so a comment cannot self-invalidate the count; FAIL with a finding if a direct
  navigation.writeEdge call is present in the composition.
- D-164-S3 (two-layered fable-mode): the verdict asserts a bad cell reading is dropped pre-collection
  (cell-side layer 1) AND a low-quality material debate step is gated by selfCritiqueFn (debate-side
  layer 2) AND runDerivation is called with a selfCritiqueFn (the derived-candidate critique).
- D-164-S5 (incremental filing): the verdict asserts each step journals before the next and a resume
  from the pipeline-state cursor does NOT re-run a completed step (the isNext hard gate).
- E1 / Part 9 role 5: the verdict asserts a SyntheticExpert is minted proposed and an agent-attributed
  confirm is rejected (only a human byUser promotes).
- E1 / CANON: the verdict asserts the E1 amendment landed at the live target -- MINDRIAN-CANON.md header
  + footer Version line is v1.13 and Appendix D entry 24 (SyntheticExpert) exists, stacking ON TOP of
  Phase 169's v1.12 / entry 23 (NESTED_WITHIN). CONFIRM the live target before hard-coding: if a later
  phase moved the canon past v1.12 / entry 23 between planning and execution, the E1 target shifts by
  one and this assertion re-points to the actual-next version + entry.
- E2-remap: the verdict asserts the issue-tree emits ONLY frozen ALLOWED_EDGE_TYPES members.

Purpose: the harness-as-code 9-property mandate requires an adversarial verify that proves by
instrumentation -- not by promise -- that the net-new composition and the canon guards hold. This is
the leak-net that the Phase 163/166 precedent proved catches real findings.

Reuse before build (Canon Part 7): the verdict structure is the SHIPPED Phase 163/166 pattern
(test-*-verdict.cjs's {passed, findings[]} record(check, passed, detail) form) and the run-all
aggregator is the SHIPPED tests/run-all-163.sh / run-all-166.sh structure -- this wave MIRRORS both for
Phase 164. A rewrite of the verify substrate is rejected; the proven patterns are reused verbatim in
shape.

Output: the verdict suite, the Part 8 leak suite, and the finalized run-all-164.sh. NO em-dashes
(CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-bono-research-debate-engine/164-RESEARCH.md
@tests/run-all-166.sh
@lib/core/bono/cell-fanout.cjs
@lib/core/bono/debate-composition.cjs
@lib/core/issue-tree.cjs
@lib/core/navigation/synthetic-expert.cjs
@docs/MINDRIAN-CANON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The adversarial structured verdict (incl. the D-164-S2 parallel-not-runChain negative + the compose-on-substrate assertion + the v1.13 canon assertion) + the Part 8 leak scan</name>
  <files>tests/test-bono-verdict.cjs, tests/test-bono-part8-leak.cjs</files>
  <action>Create tests/test-bono-verdict.cjs returning a structured {passed, findings[]} (mirror tests/run-all-166.sh's referenced test-chain-executor-verdict.cjs and tests/test-trending-to-absurd-verdict.cjs VERBATIM in shape -- the record(check, passed, detail) accumulator that pushes {check, passed, detail} into findings[] and exits non-zero printing findings if ANY check failed) over the Phase-164 net-new + canon guards: (1) E1 human-confirm-gate -- writeSyntheticExpertNode mints proposed, an agent-attributed confirm is rejected, a human byUser promotes (Part 9 role 5). (2) E1 generic-only -- a venture-body field is rejected (forbidden_field, Part 8). (3) library-first assembly -- assembleTeam slots a confirmed expert on a hit, generates fresh on a miss, and enforces the anti-ossification guards (mandatory fresh slot + Black always re-derived + reuse cap K<N). (4) E2-remap -- issue-tree.toGraphEdges emits ONLY frozen ALLOWED_EDGE_TYPES members (INVALIDATES / ROOT_CAUSES / ENABLES / PART_OF / INFORMS), never INVALIDATED / RESOLVES_VIA / BELONGS_TO. (5) D-164-S2 PARALLEL-NOT-RUNCHAIN NEGATIVE (the load-bearing check) -- read lib/core/bono/cell-fanout.cjs source and grep-assert it has NO require of lib/core/chain-executor.cjs and NO runChain call (the fan-out is parallel; the sequential loop is the debate); use grep -v '^#' style filtering so a comment cannot self-invalidate the count; FAIL with a finding if any match, mirroring the Phase 166 B3 convergence-stop grep. (6) COMPOSE-ON-169-SUBSTRATE (the recalibration check, Part 7) -- read lib/core/bono/debate-composition.cjs source and assert it REQUIRES lib/core/graph-derivation.cjs (a runDerivation reference) AND lib/core/findings-wirer.cjs (a wireAccept reference), and assert it does NOT call navigation.writeEdge DIRECTLY (grep -v '^#' filtered; the derived relationships must ride runDerivation, the ruling/tension must ride wireAccept/wireReject; FAIL with a finding if a bare navigation.writeEdge( call appears in the composition). (7) D-164-S1 debate-IS-runChain -- drive runDebate with injected spies and assert the gate halts at exactly the hypothesis-confirm + ruling material steps, the argument steps auto-run, provenanceFn stamps each step, AND runDerivation is the path the derived relationships take (assert the injected runDerivationFn spy is called) and wireAccept is the path the ruling takes (assert the injected wireAcceptFn spy is called). (8) D-164-S3 two-layered fable-mode -- assert a bad cell reading is dropped pre-collection (cell-side) AND a low-quality material debate step is gated by selfCritiqueFn (debate-side) AND runDerivation is invoked with a selfCritiqueFn (the derived-candidate critique). (9) D-164-S5 incremental filing -- assert each step journals before the next and a resume does NOT re-run a completed step (isNext hard gate). (10) Part 3 ruling -- the ruling emits a ruling verb + a residual-tension record filed via wireAccept/wireReject with a REJECT reason as REJECTED_BECAUSE. (11) E1 CANON ASSERTION -- read docs/MINDRIAN-CANON.md and assert the header Version line + the footer line are v1.13 AND an Appendix D entry numbered 24 mentioning SyntheticExpert exists (the E1 amendment stacked on Phase 169's v1.12 / entry 23); FAIL with a finding if the version is not v1.13 or entry 24 is absent. (12) Part 8 sweep -- delegate to the leak-scan suite (assert it exits zero). Each check pushes a {check, passed, detail} into findings[]; exit non-zero (print findings) if ANY check failed. Then create tests/test-bono-part8-leak.cjs: a grep/scan over ALL Phase-164 surfaces (lib/core/bono/cell-fanout.cjs, lib/core/bono/debate-composition.cjs, lib/core/issue-tree.cjs, lib/core/navigation/synthetic-expert.cjs, lib/core/expert-library.cjs, commands/bono.md, commands/diagnose.md, agents/persona-analyst.md) for forbidden tokens: no mcp__brain_(write|store|upsert|ingest), no writeBrain/sendToBrain/ingestToBrain, no raw fetch( egress in the lib code outside the research-corpus delegation, no hardcoded external http(s) endpoint (mirror the run-all-166.sh BRAIN_WRITE + RAW_FETCH regexes). Use grep -v '^#' style filtering where a comment could self-invalidate the count; never a bare unfiltered == 0 gate. NO fenced code in this action; mirror the cited Phase 166 verdict + leak-scan logic. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-bono-verdict.cjs && node tests/test-bono-part8-leak.cjs && echo VERDICT_OK</automated>
  </verify>
  <done>The verdict returns {passed:true, findings:[...]} over the net-new + canon guards (E1 human-confirm-gate + generic-only, library-first + anti-ossification, E2-remap frozen edges, D-164-S2 parallel-not-runChain negative grep, the compose-on-runDerivation/wireAccept assertion with no direct navigation.writeEdge, D-164-S1 debate gate-halts + runDerivation/wireAccept paths, D-164-S3 two-layered fable-mode, D-164-S5 journal + resume-no-re-run, Part 3 ruling, the v1.13 / Appendix D entry 24 canon assertion); the Part 8 leak scan finds zero forbidden tokens across every Phase-164 surface.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize tests/run-all-164.sh as the single phase gate</name>
  <files>tests/run-all-164.sh</files>
  <action>Finalize tests/run-all-164.sh (built up across Waves 1-5) into the complete phase aggregator, mirroring tests/run-all-166.sh structure end to end. The CJS_SUITES array MUST list every Phase-164 suite in dependency order: test-synthetic-expert-nodetype-floor.cjs (Wave 1), test-synthetic-expert-writer.cjs + test-expert-library-assembly.cjs (Wave 2), test-issue-tree-engine.cjs + test-issue-tree-edge-remap.cjs (Wave 3), test-bono-cell-fanout.cjs + test-bono-cell-selfcritique.cjs (Wave 4), test-bono-debate-composition.cjs + test-bono-incremental-filing.cjs (Wave 5), test-bono-verdict.cjs + test-bono-part8-leak.cjs (Wave 6). The per-suite loop runs to completion (a missing file gates to a FAIL line, never a crash). Add the Part-8 grep sweep block over all Phase-164 lib + command + agent surfaces (the BRAIN_WRITE + RAW_FETCH + external-http regexes as run-all-166.sh). Add the FROZEN-CONTRACT checks: (a) the FROZEN-VOCABULARY check -- grep-assert lib/core/issue-tree.cjs emits only frozen edge types (no INVALIDATED / RESOLVES_VIA / BELONGS_TO literals as emitted edge_type values; the remap landed); (b) the D-164-S2 PARALLEL-NOT-RUNCHAIN negative grep -- assert lib/core/bono/cell-fanout.cjs has NO chain-executor require / runChain call (grep -v '^#' filtered so a comment cannot self-invalidate; the fan-out is parallel per the dispatch-optimizer swarm plan, the sequential loop is the debate); (c) the COMPOSE-ON-SUBSTRATE grep -- assert lib/core/bono/debate-composition.cjs REQUIRES graph-derivation (runDerivation) + findings-wirer (wireAccept) and has NO direct navigation.writeEdge( call (grep -v '^#' filtered); (d) the CANON-VERSION assertion -- grep-assert docs/MINDRIAN-CANON.md header + footer are v1.13 and an Appendix D entry 24 mentioning SyntheticExpert exists (the live target; re-point if the canon moved past v1.12/23 between planning and execution); (e) the connector --checks -- assert node scripts/build-connector-registry.cjs --check is clean (the diagnose issue-tree sub_mode, the persona-analyst cell agent, and /mos:bono all ride frozen reaches + sub_modes, no 7th reach). Add the em-dash sweep (U+2014 codepoint escape so the runner carries no literal em-dash) over all Phase-164 lib files, the command/agent surfaces (commands/bono.md, commands/diagnose.md, agents/persona-analyst.md), the test suites, and run-all-164.sh itself. Final tally + exit 1 on any failure. set -uo pipefail; bash only; no emoji. NO em-dashes.</action>
  <verify>
    <automated>bash tests/run-all-164.sh && echo PHASE_GATE_GREEN</automated>
  </verify>
  <done>run-all-164.sh runs every Phase-164 suite + the Part 8 sweep + the frozen-vocabulary check + the D-164-S2 parallel-not-runChain negative grep + the compose-on-substrate grep + the v1.13 canon-version assertion + the connector --checks + the em-dash sweep to completion, prints a per-suite PASS/FAIL + final tally, and exits 0 only when the entire phase is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verdict suite -> all Phase-164 surfaces | adversarial: assumes each surface is hostile until proven compliant |
| run-all-164 -> CI | the single phase gate; a RED here blocks the phase |
| cell-fanout -> chain-executor | the fan-out is parallel; a runChain require is a D-164-S2 breach |
| debate-composition -> graph writes | must ride runDerivation + wireAccept; a direct navigation.writeEdge is a compose-on-substrate breach |
| issue-tree edge emission -> frozen set | a non-frozen emitted edge is an E2-remap breach |
| canon doc -> code | the E1 amendment must read v1.13 / entry 24 (stacked on Phase 169's v1.12 / entry 23); drift is a Part 6 self-CONTRADICTS |
| any Phase-164 surface -> Brain | zero user-content egress; generic handles + enums only (Part 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-25 | Elevation of privilege | a material debate gate auto-running | mitigate | the verdict drives the gate scenarios and reports a finding if a material step auto-runs (Part 3) |
| T-164-26 | Information disclosure | any Phase-164 egress path | mitigate | the Part 8 leak scan over every lib + command + agent surface finds zero Brain-write / raw-fetch / external-http tokens |
| T-164-27 | Tampering | the parallel fan-out smuggling a runChain require | mitigate | the verdict D-164-S2 negative grep + the run-all-164 parallel-not-runChain grep assert cell-fanout.cjs has no chain-executor dependency |
| T-164-28 | Tampering | a non-frozen issue-tree edge | mitigate | the verdict E2-remap check + the run-all-164 frozen-vocabulary grep assert only frozen members are emitted |
| T-164-29 | Elevation of privilege | an agent-minted confirmed expert | mitigate | the verdict E1 check asserts an agent-attributed confirm is rejected; only a human byUser promotes (Part 9 role 5) |
| T-164-30 | Repudiation | a re-run resumed step | mitigate | the verdict D-164-S5 resume check asserts a completed step is not re-run (the isNext hard gate) |
| T-164-31 | Spoofing | a 7th reach minted by any Phase-164 surface | mitigate | the run-all-164 connector --checks assert diagnose/persona-analyst/bono all ride frozen reaches + sub_modes |
| T-164-33 | Tampering | the composition hand-rolling a derivation loop or a direct edge write | mitigate | the verdict + run-all-164 compose-on-substrate grep assert debate-composition.cjs requires runDerivation + wireAccept and never calls navigation.writeEdge directly (Part 7 reuse) |
| T-164-34 | Repudiation | a stale canon version (E1 amendment not landed at the live target) | mitigate | the verdict + run-all-164 canon-version assertion grep docs/MINDRIAN-CANON.md for v1.13 + Appendix D entry 24 (stacked on Phase 169's v1.12 / entry 23) |
| T-164-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages across the entire phase (pure bash + node:test/assert); no install task |
</threat_model>

<verification>
- `node tests/test-bono-verdict.cjs` returns passed over the net-new + canon guards (incl. the D-164-S2 parallel-not-runChain negative + the compose-on-runDerivation/wireAccept assertion + the E1 human-confirm-gate + the E2-remap frozen edges + the v1.13 / entry 24 canon assertion).
- `node tests/test-bono-part8-leak.cjs` finds zero forbidden tokens.
- `bash tests/run-all-164.sh` is green (every suite + Part 8 sweep + frozen-vocabulary check + parallel-not-runChain grep + compose-on-substrate grep + canon-version assertion + connector --checks + em-dash sweep).
- The cell fan-out has no chain-executor dependency (D-164-S2); the debate halts at the two material gates (D-164-S1); the composition rides runDerivation + wireAccept (no direct navigation.writeEdge); resume does not re-run (D-164-S5).
- The canon is v1.13 with Appendix D entry 24 (the E1 amendment, stacked on Phase 169's v1.12 / entry 23).
- No em-dashes anywhere in the phase.
</verification>

<success_criteria>
The adversarial structured verdict proves by instrumentation that the net-new BONO composition honors
the constitution end to end: the cell fan-out is parallel and not runChain (D-164-S2), the debate is
runChain halting at the two material gates (D-164-S1), the debate composition rides the 169-shipped
substrate (runDerivation for derived relationships + findings-wirer.wireAccept for the ruling/tension,
never a hand-rolled loop or a direct navigation.writeEdge), fable-mode is two-layered (D-164-S3),
incremental filing journals before next and resume never re-runs (D-164-S5), the SyntheticExpert is
human-confirm-gated (E1 / Part 9 role 5), the issue-tree emits only frozen edges (E2-remap), the canon
landed at v1.13 / Appendix D entry 24, and no Phase-164 surface egresses to the Brain (Part 8).
run-all-164.sh is the single green phase gate.
</success_criteria>

<output>
Create `.planning/phases/164-bono-research-debate-engine/164-06-SUMMARY.md` when done.
</output>
</output>
