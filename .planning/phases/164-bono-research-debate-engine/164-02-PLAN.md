---
phase: 164-bono-research-debate-engine
plan: 02
type: execute
wave: 2
depends_on: [164-01]
files_modified:
  - lib/core/navigation/synthetic-expert.cjs
  - lib/core/navigation.cjs
  - lib/core/expert-library.cjs
  - tests/test-synthetic-expert-writer.cjs
  - tests/test-expert-library-assembly.cjs
  - tests/run-all-164.sh
autonomous: true
requirements: [E1, D-164-S1]
canon_parts: [2, 4, 5, 8, 9]

must_haves:
  truths:
    - "writeSyntheticExpertNode(db, params) mints a SyntheticExpert node at review_status proposed through the navigation chokepoint, carrying generic-lens metadata only (hat/name/surname/archetype/beautiful_question/research_approach/evidence_tier/invocation_count/last_used/provenance), and rejects a params bag containing any venture-body field"
    - "an expert is promoted proposed->confirmed ONLY via the existing confirmNode(byUser) door (Part 9 role 5); an agent-attributed confirm is rejected"
    - "library-first assembly: assembleTeam queries confirmed SyntheticExpert nodes per (hat, subdomain) ranked by the lifecycle score (evidence_tier_rank + survival_rate + recency_decay - staleness_penalty), slots a hit, and returns a generate-fresh marker for a miss"
    - "anti-ossification guards hold: at least one slot per run is freshly generated, the Black hat is always re-derived (never reused), and at most K of N slots are filled from the library with a log() when the cap bites"
    - "a stale expert (subdomain claim-set hash mismatch since last validated) is flagged stale and NOT silently reused"
  artifacts:
    - path: "lib/core/navigation/synthetic-expert.cjs"
      provides: "writeSyntheticExpertNode chokepoint + SYNTHETIC_EXPERT_FIELDS allow-list (mirror typed-domain.cjs writeDomainNode + DOMAIN_NODE_TYPES idiom)"
      min_lines: 90
    - path: "lib/core/expert-library.cjs"
      provides: "library-first assembly: rank + freshness gate + anti-ossification guards (pure navigation.cjs graph walk, no Brain, no raw room.db)"
      min_lines: 90
    - path: "tests/test-synthetic-expert-writer.cjs"
      provides: "writer chokepoint + generic-only-fields + proposed-mint + confirm-gate assertions"
      min_lines: 50
    - path: "tests/test-expert-library-assembly.cjs"
      provides: "rank + match-tier + freshness + anti-ossification (fresh-lens, Black-re-derive, reuse-cap) assertions"
      min_lines: 60
  key_links:
    - from: "lib/core/navigation/synthetic-expert.cjs"
      to: "lib/core/navigation.cjs"
      via: "re-exported as navigation.writeSyntheticExpertNode (thin additive-re-export, mirror writeDomainNode)"
      pattern: "writeSyntheticExpertNode"
    - from: "lib/core/expert-library.cjs"
      to: "lib/core/navigation.cjs"
      via: "ranking reader is a pure navigation graph walk (neighborhood + STATES/SUPPORTS edge walk)"
      pattern: "navigation"
    - from: "lib/core/expert-library.cjs"
      to: "lib/core/navigation/confirm-node.cjs"
      via: "mint-at-Decision-Gate promotes via confirmNode(byUser)"
      pattern: "confirmNode"
---

<objective>
WAVE 2: the SyntheticExpert WRITER chokepoint + library-first assembly. With the node type frozen in
Wave 1, this wave ships lib/core/navigation/synthetic-expert.cjs writeSyntheticExpertNode -- the single
chokepoint that mints a SyntheticExpert node at review_status proposed, carrying GENERIC-LENS metadata
ONLY (164-SYNTHETIC-EXPERTS.md table: hat / name / surname / archetype / beautiful_question /
research_approach / evidence_tier / invocation_count / last_used / review_status / provenance). It
mirrors the typed-domain.cjs pattern VERBATIM in shape (a frozen field allow-list + a defensive
UPSERT that lands review_status proposed by the column DEFAULT, never auto-confirmed; the human
confirms via the existing confirmNode door per Part 9 role 5). It is re-exported through navigation.cjs
as a thin additive re-export, exactly like writeDomainNode.

This wave also ships lib/core/expert-library.cjs -- the library-first team assembly
(164-EXPERT-LIFECYCLE.md): for each needed (hat, subdomain) slot, query confirmed SyntheticExpert
nodes, rank by the lifecycle score (evidence_tier_rank + survival_rate + recency_decay -
staleness_penalty -- all LOCAL scalars, a pure navigation.cjs graph walk, no Brain, no raw room.db),
slot a hit, return a generate-fresh marker on a miss. survival_rate walks the expert's STATES/SUPPORTS
edges to past rulings (an expert that kept being overruled sinks). The freshness gate flags a stale
expert (subdomain claim-set hash mismatch since last validated) and refuses silent reuse. The
anti-ossification guards (164-EXPERT-LIFECYCLE.md section 3) are enforced: at least one freshly
generated slot per run, the Black hat always re-derived, and at most K of N slots from the library with
a log() when the cap bites.

Reuse before build (Canon Part 7): writeSyntheticExpertNode mirrors typed-domain.cjs writeDomainNode
(the DOMAIN_NODE_TYPES frozen Set + the defensive insertNode chokepoint + the additive JSON props
discipline); the ranking reader walks the graph via the navigation.cjs chokepoint (never raw SQL); the
mint promotes via the SHIPPED confirmNode(byUser) door (Phase 129.5). No new write primitive is
invented; the SyntheticExpert rides the existing navigation spine.

Output: the writer chokepoint, the library assembler, two test suites, run-all-164.sh registration.
NO em-dashes (CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-bono-research-debate-engine/164-SYNTHETIC-EXPERTS.md
@.planning/phases/164-bono-research-debate-engine/164-EXPERT-LIFECYCLE.md
@lib/core/navigation/typed-domain.cjs
@lib/core/navigation/transitions.cjs
@lib/core/navigation/confirm-node.cjs
@lib/core/navigation.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: writeSyntheticExpertNode chokepoint + navigation.cjs re-export</name>
  <files>lib/core/navigation/synthetic-expert.cjs, lib/core/navigation.cjs, tests/test-synthetic-expert-writer.cjs</files>
  <behavior>
    - Test 1: writeSyntheticExpertNode(db, {hat:'Black', name:'Regulatory', surname:'IND', archetype:'Domain Expert', beautifulQuestion:..., researchApproach:..., evidenceTier:'Operational', sessionId, provenance:{runId, domainNodeIds:[]}}) returns {ok:true, node_id} and lands a node of type 'SyntheticExpert' at review_status 'proposed'.
    - Test 2: a params bag carrying a venture-body field (e.g. ventureNotes / artifactBody / any non-allow-listed prose key) returns {ok:false, reason:'forbidden_field'} -- the SYNTHETIC_EXPERT_FIELDS allow-list rejects it (Part 8 generic-lens-only).
    - Test 3: the minted node is NOT auto-confirmed (review_status 'proposed'); confirmNode(db, node_id, 'navigator') promotes it to 'confirmed'; confirmNode(db, node_id, <AGENT_IDENTITIES member>) is rejected (Part 9 role 5, inherited from Wave 1's TRUTH_CLAIM_TYPES extension).
    - Test 4: an invalid hat (not a de Bono color) returns {ok:false, reason:'invalid_hat'}; missing name/surname returns a defensive failure, never a throw.
  </behavior>
  <action>Create lib/core/navigation/synthetic-expert.cjs mirroring lib/core/navigation/typed-domain.cjs:50-157 VERBATIM in shape. Define a frozen SYNTHETIC_EXPERT_FIELDS allow-list (the 164-SYNTHETIC-EXPERTS.md table keys: hat, name, surname, archetype, beautiful_question, research_approach, evidence_tier, invocation_count, last_used, provenance -- generic-lens metadata ONLY) and a frozen HAT_COLORS Set (White/Red/Black/Yellow/Green/Blue). Define SYNTHETIC_EXPERT_NODE_ID(sessionId, hat, surname) as a crypto-free 31-multiplier stable hash (mirror DOMAIN_NODE_ID at typed-domain.cjs:75-83) so re-writing the same (hat, surname, sessionId) is an UPSERT. writeSyntheticExpertNode(db, params): validate params is a plain object; validate hat in HAT_COLORS (else invalid_hat); validate name + surname non-empty strings (else defensive failure); REJECT any params key not in the allow-list union {hat,name,surname,archetype,beautifulQuestion,researchApproach,evidenceTier,invocationCount,lastUsed,provenance,sessionId} with reason 'forbidden_field' (the Part 8 generic-only gate -- no venture body ever rides the props); build an additive JSON props bag of the allow-listed fields only (provenance carries originating runId + domain node ids as scalars/ids, NEVER prose); mint via the shared insertNode chokepoint with created_by 'system' landing review_status 'proposed' by the column DEFAULT (NEVER promote here -- a SyntheticExpert is a truth-claim node, role 5; the human promotes via confirmNode). Return {ok:true, node_id, hat}. Defensive throughout: never throw on caller input; return {ok:false, reason}. Then add writeSyntheticExpertNode to lib/core/navigation.cjs as a thin additive re-export (mirror the writeDomainNode re-export line at navigation.cjs and the typed-domain require), so callers reach it through the chokepoint. Then create tests/test-synthetic-expert-writer.cjs building an in-memory db (mirror how typed-domain's tests build their nodes table) and assert the four behaviors. NO fenced code in this action; the implementation lives in synthetic-expert.cjs following the cited typed-domain.cjs lines.</action>
  <verify>
    <automated>node tests/test-synthetic-expert-writer.cjs</automated>
  </verify>
  <done>writeSyntheticExpertNode mints a proposed SyntheticExpert with generic-lens fields only, rejects a venture-body field and an invalid hat, never auto-confirms, and is reachable as navigation.writeSyntheticExpertNode; the human-confirm gate (confirmNode) promotes it and rejects an agent-attributed confirm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: library-first assembly -- rank, freshness gate, anti-ossification guards</name>
  <files>lib/core/expert-library.cjs, tests/test-expert-library-assembly.cjs, tests/run-all-164.sh</files>
  <behavior>
    - Test 1: rankExpertsForSlot(db, {hat, subdomain}) returns confirmed experts ordered by the match tiers (exact hat+subdomain > exact hat+same domain > exact hat+same archetype > none) then by score (w1*evidence_tier_rank + w2*survival_rate + w3*recency_decay(last_used) - w4*staleness_penalty); a miss returns an empty list (the caller generates fresh).
    - Test 2: survival_rate is a pure navigation graph walk over the expert's STATES/SUPPORTS edges to past rulings (an expert overruled in every past debate scores lower than one whose readings were the ruling).
    - Test 3: freshness gate -- an expert whose recorded subdomain claim-set hash differs from the current subdomain hash is flagged stale and is NOT returned for silent reuse (assembleTeam either refreshes or falls back to fresh).
    - Test 4: anti-ossification -- assembleTeam({hats, subdomains, library}) guarantees at least one freshly generated slot, ALWAYS re-derives the Black hat (never slots a library Black), caps library fills at K of N (K<N), and log()s what was reused vs generated when the cap bites.
  </behavior>
  <action>Create lib/core/expert-library.cjs with: (a) rankExpertsForSlot(db, {hat, subdomain, domain, archetype}) -- query confirmed SyntheticExpert nodes via the navigation.cjs chokepoint (neighborhood / node lookup, NEVER raw room.db), bucket by the four match tiers (164-EXPERT-LIFECYCLE.md section 1), and within a tier score by the documented formula with weights from a small DEFAULT_WEIGHTS config object (defaults tuned here: w1..w4 commented per the lifecycle doc); survival_rate computed by walking the expert's STATES/SUPPORTS edges to past ruling nodes via navigation (the RULING-vs-REJECTED_BECAUSE distinction); recency_decay an exponential over last_used; staleness_penalty from the freshness gate. (b) isExpertStale(db, expert, currentSubdomainHash) -- compare the expert's recorded governing_thought_hash / subdomain-claim-set hash (mirror the Act 1 STATE.md source-hash invalidation idiom) to the current; mismatch -> stale. (c) assembleTeam(db, {neededSlots, opts}) -- for each (hat, subdomain) slot: if hat is Black, ALWAYS mark generate-fresh (adversarial freshness, never reuse); else rankExpertsForSlot, skip stale, slot the top confirmed hit if under the reuse cap K (K = opts.reuseCap, K<N, default per the lifecycle doc), else generate-fresh; GUARANTEE at least one slot per run is generate-fresh (the Green-hat mandatory-fresh rule -- if every slot got a library hit, force the lowest-ranked slot to fresh); log() (the project log idiom) a summary of reused-vs-generated and a note when the cap bites. (d) The mint-at-Decision-Gate helper offerExpertsForFiling(runHats) -- rank the run's hats by contribution and return the Shape F gate candidate list; on APPROVE the caller promotes via confirmNode(byUser) (do NOT call AskUserQuestion here; this module returns the candidate list, the command surface renders the gate). All reads via navigation; ZERO Brain calls; ZERO raw room.db open. Then create tests/test-expert-library-assembly.cjs asserting the four behaviors against an in-memory db seeded with confirmed experts + ruling edges. Finally append both Wave-2 suites (test-synthetic-expert-writer.cjs, test-expert-library-assembly.cjs) to the CJS_SUITES array in tests/run-all-164.sh. NO fenced code in this action. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-expert-library-assembly.cjs && bash tests/run-all-164.sh && echo WAVE2_OK</automated>
  </verify>
  <done>assembleTeam ranks confirmed experts by match-tier + lifecycle score via a pure navigation graph walk, flags and skips stale experts, and enforces the three anti-ossification guards (mandatory fresh slot, Black always re-derived, reuse cap K<N with a log when it bites); run-all-164.sh registers both Wave-2 suites and is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writeSyntheticExpertNode params -> room.db | the SYNTHETIC_EXPERT_FIELDS allow-list rejects any non-generic field before any insert (Part 8) |
| expert library reader -> room.db | reads ONLY via the navigation.cjs chokepoint, never raw SQL (Part 9) |
| mint-at-gate -> confirmed | promotion rides confirmNode(byUser) only; no agent self-confirm (Part 9 role 5) |
| reused expert -> a new run | the freshness gate refuses a stale expert; the anti-ossification guards refuse a calcified roster |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-05 | Information disclosure | SyntheticExpert props bag | mitigate | the SYNTHETIC_EXPERT_FIELDS allow-list rejects forbidden_field; provenance carries ids/scalars only; asserted by the writer test + the Wave 6 leak scan |
| T-164-06 | Elevation of privilege | agent-minted confirmed expert | mitigate | writeSyntheticExpertNode lands proposed only; confirmNode(byUser) is the sole promotion door; agent confirm rejected (Wave 1 TRUTH_CLAIM_TYPES) |
| T-164-07 | Tampering | stale expert reused blind | mitigate | isExpertStale hash-gate; assembleTeam skips stale; the test asserts a hash mismatch is not silently reused |
| T-164-08 | Spoofing | echo-chamber roster | mitigate | anti-ossification guards (mandatory fresh + Black re-derive + reuse cap K<N with log); asserted by the assembly test |
| T-164-09 | Information disclosure | ranking reader path | mitigate | reads via navigation only; zero Brain calls; grep-asserted in the Wave 6 leak scan |
| T-164-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages this wave (Node built-ins + existing navigation); no install task |
</threat_model>

<verification>
- `node tests/test-synthetic-expert-writer.cjs` passes (proposed mint, generic-only fields, confirm-gate).
- `node tests/test-expert-library-assembly.cjs` passes (rank, freshness, anti-ossification).
- `bash tests/run-all-164.sh` is green (Wave 1 + Wave 2 suites).
- writeSyntheticExpertNode is reachable as navigation.writeSyntheticExpertNode.
- The library reader makes zero Brain calls and zero raw room.db opens (navigation chokepoint only).
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
The SyntheticExpert writer mints generic-lens-only proposed nodes through the navigation chokepoint and
never auto-confirms; library-first assembly ranks confirmed experts by match-tier + lifecycle score via
a pure navigation graph walk, gates on freshness, and enforces the anti-ossification guards; the mint
promotes via confirmNode(byUser) at the Decision Gate. run-all-164.sh is green.
</success_criteria>

<output>
Create `.planning/phases/164-bono-research-debate-engine/164-02-SUMMARY.md` when done.
</output>
