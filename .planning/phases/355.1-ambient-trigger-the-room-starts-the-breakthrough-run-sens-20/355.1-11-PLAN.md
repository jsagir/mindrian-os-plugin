---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 11
type: execute
wave: 5
depends_on: ["355.1-07", "355.1-08"]
files_modified:
  - tests/test-3551-surfacing.cjs
  - lib/core/sensors/sensor-eureka.cjs
  - lib/hmi/dial-presenter.cjs
  - lib/hmi/dial-label-composer.cjs
  - tests/test-dial-label-bank-drift.cjs
autonomous: true
requirements: [AMB-05, AMB-07]
canon_parts: [3, 8, 11, 12]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["lib/core/sensors/sensor-eureka.cjs SENS-13 evidence bag", "lib/hmi/dial-label-composer.cjs deep_research stamped variant"]
  gate_impact: "SENS-13's evidence gains one enum (framing) and the existing stamped reach-card variant gains a framing prefix inside the same render entry point; reach id, signal, sensor id and render entry points unchanged; check-render-coverage stays green."
  explanation: "AMB-05 and AMB-07: the ambient finding surfaces once through 355-22's SENS-13 fire-once path, framed by the room's problem type, with only strong and indirect stamps ever reaching the card."
tags: [sens-13, card, framing, tier-gate, no-decimal]

must_haves:
  truths:
    - "CONTEXT 'The split' and 'Surfacing and the Hooked lens (AMB-05, locked)': SENS-13 shows the card in the normal path, exactly one card per delta hash on CLI (355-22's decide() mark, extended not replaced, Ruling 2); reach_id deep_research and signal eureka_bridge unchanged"
    - "AMB-05 tier gate proven end to end: a composition whose findings are all unverified leaves SENS-13 silent in decide() and in the MCP pull; an indirect or strong finding produces one SENS-13 reach"
    - "CONTEXT 'Strategy, not keywords (AMB-07, locked)': SENS-13's evidence gains framing (a FRAMING_IDS member or ''), read-only from <room>/.mindrian/last-room-delta.json only when its opportunity_handle equals the side channel's; the sensor still writes nothing, stays sync and zod-free"
    - "The stamped card label with a framing is exactly FRAMING_PHRASES[framing] + ' -- ' + the 355-22 stamped line; without a framing (a command-path eureka run) it is byte-identical to 355-22's label"
    - "Canon Part 12 / AC3: no rendered label carries a D-30 decimal, a percentage, a count next to a finding word, or a praise token; planted score and count literals are caught (negative controls)"
    - "On the MCP pull path SENS-13 is a pure read and fires on every pull until the Stop-time close-out records it (Ruling 2); that close-out mark is wired in plan 355.1-13, and this plan documents the two-pull behavior as a pinned fact"
  artifacts:
    - path: "tests/test-3551-surfacing.cjs"
      provides: "tier gate, one card on CLI, framed label, MCP pull behavior, render guards with negative controls"
      min_lines: 120
    - path: "lib/core/sensors/sensor-eureka.cjs"
      provides: "evidence.framing, read-only"
      contains: "framing"
    - path: "lib/hmi/dial-label-composer.cjs"
      provides: "framed stamped variant"
      contains: "FRAMING_PHRASES"
  key_links:
    - from: "lib/core/sensors/sensor-eureka.cjs"
      to: "<room>/.mindrian/last-room-delta.json framing"
      via: "handle-matched read"
      pattern: "last-room-delta"
    - from: "lib/hmi/dial-label-composer.cjs"
      to: "lib/core/direction-convention.cjs FRAMING_PHRASES"
      via: "prefix on the stamped variant"
      pattern: "FRAMING_PHRASES"
---

<objective>
AMB-05 and AMB-07 on the card: the stamped ambient finding reaches the navigator once, through the SENS-13 path 355 built, opening with a phrase chosen by the room's problem type. The only new data SENS-13 carries is one enum; the only new text on the card is one confirmed phrase. Everything unverified stays off the card.

Output: the framing read in SENS-13, the pass-through, the framed label, the amended drift test, one surfacing test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md
@lib/core/direction-convention.cjs
@lib/core/ambient-run.cjs
@scripts/scout-cadence-guard.cjs
@tests/helpers/fixture-room-3551.cjs
@tests/helpers/fixture-room-355.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED surfacing test</name>
  <files>tests/test-3551-surfacing.cjs</files>
  <read_first>
    - tests/test-355-sens13-fire-once.cjs (355-22's two-decide() harness, fixed clock, turn_count 6 for deep_research) and tests/test-355-no-decimal.cjs (the D-30 regex, the percent check, banned-claim words)
    - lib/core/sensors/sensor-eureka.cjs (whole file as 355-19 left it: v2 gates, dedup ledger read, evidence bag)
    - lib/hmi/dial-label-composer.cjs (the 355-22 deep_research stamped variant) and the slotContext builder 355-22 extended (`grep -n "stamp_verification" lib/hmi/*.cjs`)
    - lib/mcp/tools/sensors.cjs `_internal.dispatchCandidateReaches`
    - lib/core/ambient-run.cjs runAmbientComposition (injected adapters and measureAndGuard) and scripts/scout-cadence-guard.cjs writeRoomDeltaState
  </read_first>
  <behavior>
    - setup: on a buildFilingRoom / buildDeltaRoom room, runAmbientComposition with injected adapters returning one indirect finding and an injected measureAndGuard that clears, then writeRoomDeltaState with run_state 'completed', opportunity_handle = result.card.opportunity_handle, surfaced_via 'sens13', framing 'find_the_problem'
    - CLI: two decide() calls (turn_count 6, fixed clock): the first includes exactly one reach with signal 'eureka_bridge' whose evidence.framing === 'find_the_problem' and evidence.opportunity_handle equals the node id; the second includes none (355-22's mark)
    - label: composeLabel('deep_research', <slotContext built from that reach by the same builder the presenter uses>).label === FRAMING_PHRASES.find_the_problem + ' -- ' + <the 355-22 stamped line for that stamp>; same check for pursue_or_drop and neutral by rewriting the delta state's framing
    - regression: a v2 side channel with no matching delta state (the command path) yields evidence.framing === '' and a label byte-identical to 355-22's unframed label
    - tier gate: a composition whose findings are all unverified writes no side channel; decide() and dispatchCandidateReaches both return no SENS-13 reach for that room
    - MCP pull: dispatchCandidateReaches on the indirect room returns the SENS-13 reach with evidence.framing on two consecutive pulls (pure read; the Stop-time close-out mark lands in plan 355.1-13), asserted and documented in the test
    - render guards over every label composed above and every FRAMING_PHRASES value: no D-30 decimal, no `[0-9]+%`, no `\b[0-9]+\s+(new\s+)?(finding|connection|opportunit)`, none of breakthrough / convergent / validated / proven / great / amazing / excellent / brilliant, no U+2014
    - negative controls, each reported PASS when caught: a slotContext with stamp_path 'A (0.87) B' fails the decimal check; a label with '3 new connections' fails the count check
    - static: sensor-eureka.cjs still has no zod require, no verification-stamp.cjs require, no writeFileSync / renameSync on an executable line; the sensor call writes no file (fs write spy)
  </behavior>
  <action>Write tests/test-3551-surfacing.cjs ('use strict', hygiene-355 preamble, attempts() === 0 last; exit 77 with a loud message when node:sqlite is unavailable) implementing every behavior line, copying the decide() harness from tests/test-355-sens13-fire-once.cjs. Run it; it fails (no framing in evidence or label). Commit `test(355.1-11): add failing framed one-card surfacing test (AMB-05, AMB-07)`.</action>
  <verify>
    <automated>node tests/test-3551-surfacing.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The test exists, runs and fails on the framing legs
    - `grep -c "negative control" tests/test-3551-surfacing.cjs` prints at least 2
  </acceptance_criteria>
  <done>A failing test pins the framed, tier-gated, once-only card.</done>
</task>

<task type="auto">
  <name>Task 2: framing in SENS-13's evidence, the pass-through, the framed label, the drift test</name>
  <files>lib/core/sensors/sensor-eureka.cjs, lib/hmi/dial-presenter.cjs, lib/hmi/dial-label-composer.cjs, tests/test-dial-label-bank-drift.cjs</files>
  <read_first>
    - the four files, re-read from disk now; `git status --short` on each must be empty
    - the slotContext builder 355-22 extended: if it is not lib/hmi/dial-presenter.cjs, edit the file 355-22's SUMMARY names instead and record the substitution in this plan's SUMMARY
    - tests/test-3551-surfacing.cjs (the RED legs)
  </read_first>
  <action>
sensor-eureka.cjs: add `ROOM_DELTA_STATE_RELPATH = path.join('.mindrian', 'last-room-delta.json')` (comment: replicated from sensor-room-delta.cjs DELTA_STATE_RELPATH; a sibling sensor is not required; pinned equal by tests/test-3551-surfacing.cjs) and, after every existing gate passes and only when payload.opportunity_handle is a non-empty string, a read-only helper that stats then parses that file, and returns its framing when it is schema_version 1, its opportunity_handle equals the side channel's, and framing is a member of FRAMING_IDS (from ../direction-convention.cjs, already required for DIRECTIONS); else ''. Add `framing` to the evidence bag. Comment citing Phase 355.1 AMB-07. No other change.
dial-presenter.cjs (or the builder 355-22 used): pass `framing: evidence.framing` into the slotContext exactly where 355-22 passes the stamp_* fields.
dial-label-composer.cjs: in the 355-22 stamped-finding variant, when slotContext.framing is a FRAMING_IDS member, return FRAMING_PHRASES[framing] + ' -- ' + the existing stamped line; otherwise return the existing line unchanged. Require FRAMING_IDS and FRAMING_PHRASES from ../core/direction-convention.cjs. Comment citing Phase 355.1 AMB-07 and Ruling 6.
tests/test-dial-label-bank-drift.cjs: add the framed variant for each framing with a header note "Amended Phase 355.1 AMB-07".
Run tests/test-3551-surfacing.cjs, tests/test-dial-label-bank-drift.cjs, tests/test-355-sens13-fire-once.cjs, tests/test-355-side-channel-v2.cjs, tests/test-355-no-decimal.cjs, tests/test-355-tri-polar.cjs, every tests/test-213-*.cjs, tests/test-219-harvest-sensor.cjs, tests/test-237-session-scope.cjs, tests/test-sensors-part8-sweep.cjs, tests/test-sensors-routing-fence.cjs and `node scripts/check-render-coverage.cjs`. Commit with `git commit --only` on the four files: `feat(355.1-11): SENS-13 carries the room's framing; the stamped card opens with it (AMB-05, AMB-07)`.
  </action>
  <verify>
    <automated>node tests/test-3551-surfacing.cjs && node tests/test-dial-label-bank-drift.cjs && node tests/test-355-sens13-fire-once.cjs && node tests/test-355-side-channel-v2.cjs && node tests/test-355-no-decimal.cjs && node tests/test-355-tri-polar.cjs && node tests/test-219-harvest-sensor.cjs && node scripts/check-render-coverage.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command exits 0; the surfacing test prints `FAIL: 0`
    - `grep -c "FRAMING_PHRASES" lib/hmi/dial-label-composer.cjs` prints at least 1 and `grep -c "Phase 355.1" tests/test-dial-label-bank-drift.cjs` prints at least 1
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/sensors/sensor-eureka.cjs | grep -cE "require\('zod'\)|verification-stamp\.cjs|writeFileSync|renameSync"` prints 0
    - `grep -cP '\x{2014}' lib/core/sensors/sensor-eureka.cjs lib/hmi/dial-label-composer.cjs lib/hmi/dial-presenter.cjs` prints 0 for each file
  </acceptance_criteria>
  <done>The ambient finding reaches the card once, framed by the room's problem type, and nothing unverified ever does.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| delta-state file -> SENS-13 evidence -> card | A local enum selects the card's opening phrase |
| card text -> user | The last place a number or over-claim could leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-47 | Tampering | a forged framing value on the card | mitigate | framing accepted only as a FRAMING_IDS member AND only when the delta state's handle equals the side channel's; the phrase text comes from the confirmed table, never from the file |
| T-3551-48 | Spoofing | a score or count on the card | mitigate | D-30, percent, count and praise checks over every composed label with two planted negative controls |
| T-3551-49 | Spoofing | an unverified finding carded | mitigate | Tier gate proven end to end on CLI and the MCP pull |
| T-3551-50 | Denial of service | the hook-path sensor loading heavy modules | mitigate | Only direction-convention (node:crypto) is required; static no-zod, no-stamp-module leg |
</threat_model>

## Artifacts this phase produces

This plan's share:
- `lib/core/sensors/sensor-eureka.cjs` constant `ROOM_DELTA_STATE_RELPATH`; evidence field `framing`
- slotContext key `framing` (dial-presenter builder)
- `lib/hmi/dial-label-composer.cjs` framed stamped variant (`FRAMING_PHRASES[framing] + ' -- ' + stamped line`)
- Amended `tests/test-dial-label-bank-drift.cjs`; test `tests/test-3551-surfacing.cjs`

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently. Re-read every file from disk immediately before editing; if `git status --short` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it. Commit with `git commit --only` on this plan's paths; no `state.*` writes while a peer executes.

<verification>
- `node tests/test-3551-surfacing.cjs`
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set within BASE_3551's)
</verification>

<success_criteria>
- AC3: unverified never reaches the card; strong and indirect do; no number, count or praise in any rendered line
- One framed card per delta hash on CLI; the command-path label is unchanged
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-11-SUMMARY.md` when done.
</output>
