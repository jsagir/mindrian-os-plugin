---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 01
type: execute
wave: 1
depends_on: []
execution_gate: "Phase 355 must be CLOSED before ANY task of ANY 355.1 plan runs: no `- [ ] 355-NN-PLAN.md` line in ROADMAP.md, 355-27-SUMMARY.md present (the 355-01 D-57 gate pattern reused against the 355 plan list), and SENS-21 still free in every sensor lockstep place"
files_modified:
  - .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-BASELINE.md
  - tests/run-all-3551.sh
  - tests/helpers/fixture-room-3551.cjs
  - tests/test-3551-fixture-room.cjs
autonomous: true
requirements: [AMB-09, AMB-08]
canon_parts: [6, 8, 9, 11]
tags: [phase-gate, baseline, run-all, fixture-room]

must_haves:
  truths:
    - "CONTEXT 'Execution constraints (locked)': Task 1 refuses to proceed while any 355-NN plan is unchecked in ROADMAP.md or 355-27-SUMMARY.md is absent; it reports 'Phase 355 not closed; 355.1 held' and stops the whole phase"
    - "CONTEXT 'Research corrections applied (locked)': SENS-21 is proven free at execution time in SENSOR_REGISTRY_IDS, SENS_PRIORITY, every lib/core/sensors/*.cjs SENSOR_ID constant, every commands/*.md sensor_triggers list and data/; a taken id stops the phase, because every 355.1 plan names SENS-21"
    - "CONTEXT 'Proof and close (AMB-09, locked)': the doctor --acceptance failing-point set, the exit codes of the four born-wired gates and the measured Phase 117 fire-child runtime (p50, p90, max, n) are captured ONCE at BASE_3551 and written into 355.1-BASELINE.md and the tests/run-all-3551.sh header; no later plan re-captures them"
    - "CONTEXT 'Proof and close': the aggregator is tests/run-all-3551.sh and every test is tests/test-3551-*.cjs, so run-all-355.sh's tests/test-355-*.cjs glob never runs a 355.1 test (the PRD's run-all-355.1.sh name yields to CONTEXT)"
    - "The 355 interfaces this phase consumes (355-19 writeStampedSideChannel and markEurekaReachSurfaced, 355-20 bankStatements, 355-16 main(argv, deps), 355-17 stamp-connections and stampFn, 355-18 stampRankedPairs, 355-22 decide() mark and stamped card variant, 355-13 fixture rooms, 355-21 part8 target list) are re-grepped and anchored in 355.1-BASELINE.md; a missing seam stops the phase instead of being assumed"
    - "tests/helpers/fixture-room-3551.cjs builds mkdtemp rooms seeded only through navigation writers (never raw SQL) and can plant each delta class (a)-(e) on demand, with SECRET-3551 marker strings in every text field so Part 8 legs can grep for leaks"
  artifacts:
    - path: ".planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-BASELINE.md"
      provides: "Gate result, BASE_3551, 355 interface anchors, doctor and born-wired baselines, measured child runtime"
      contains: "BASE_3551"
    - path: "tests/run-all-3551.sh"
      provides: "Phase aggregator mirroring tests/run-all-355.sh, SKIP-clean until each 355.1 artifact lands"
      contains: "doctor_acceptance_no_new_regression"
    - path: "tests/helpers/fixture-room-3551.cjs"
      provides: "buildDeltaRoom(label) with per-class planting helpers, copy355FixtureRoom(name), SKIP_EXIT_CODE"
      exports: ["buildDeltaRoom", "copy355FixtureRoom", "SKIP_EXIT_CODE", "MARKER_PREFIX"]
  key_links:
    - from: "tests/run-all-3551.sh"
      to: "tests/test-3551-*.cjs"
      via: "glob loop with run_if, exit 77 counted as SKIP"
      pattern: "test-3551-"
    - from: "tests/helpers/fixture-room-3551.cjs"
      to: "lib/core/navigation.cjs writers"
      via: "writeClaimNode, writeEdge, writeMemoryArtifactNode through the navigation write door"
      pattern: "navigation"
---

<objective>
EXECUTION GATE: nothing in Phase 355.1 runs until Phase 355 has closed (355-27 landed). Every seam this phase extends (the stamped side channel, the proposed-opportunity filing, SENS-13 fire-once, the stamped card, the five stamped producers, the fixture rooms) is built by 355 plans 13 through 23; 355.1 starts only when all of them exist. Task 1 proves the gate, proves SENS-21 is still free (Phase 345 took SENS-20; the PRD's SENS-20 reads as SENS-21), anchors the 355 interfaces, and captures the baselines the phase gate compares against.

Task 2 lays the test floor every later plan stands on: the aggregator `tests/run-all-3551.sh` and one fixture-room helper that can plant each of the five delta classes.

Purpose: every later 355.1 plan starts from a known-closed parent phase, a known doctor baseline, known 355 seams, and one fixture shape.
Output: 355.1-BASELINE.md, tests/run-all-3551.sh, tests/helpers/fixture-room-3551.cjs, tests/test-3551-fixture-room.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-RESEARCH.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-PATTERNS.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-BASELINE.md
@tests/run-all-355.sh
@tests/helpers/hygiene-355.cjs
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 355 closed gate, SENS-21 free check, 355 interface anchors, doctor and born-wired baselines, measured child runtime</name>
  <files>.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-BASELINE.md</files>
  <read_first>
    - .planning/ROADMAP.md (only the `### Phase 355` card: every `- [ ] 355-NN-PLAN.md` / `- [x]` line)
    - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-BASELINE.md (the record format to mirror: Gate, file anchors, doctor baseline headings)
    - tests/run-all-355.sh lines 180-215 (doctor_acceptance_no_new_regression: how the failing-point ids are parsed)
    - lib/core/insight-sensors.cjs (grep `SENS-2[0-9]`), lib/core/sensors/sensor-priority.cjs (grep `id: 'SENS-`)
    - 355.1-RESEARCH.md sections "Critical Findings" C1, C3, C12 and "Discretion Items Settled" item 6 (the 355 interface table)
  </read_first>
  <action>
GATE, run first and STOP the whole phase on any failure (CONTEXT 'Execution constraints (locked)'):
(a) `grep -nE '^- \[ \] 355-[0-9]+-PLAN\.md' .planning/ROADMAP.md` must print nothing, and `ls .planning/phases/355-*/355-27-SUMMARY.md` must succeed. On failure report "Phase 355 not closed; 355.1 held" and stop.
(b) SENS-21 free (CONTEXT 'Research corrections applied'): `grep -rn "SENS-21" lib data commands skills agents scripts tests` must print nothing, and SENSOR_REGISTRY_IDS must not contain it (`node -e "const s=require('./lib/core/insight-sensors.cjs');if(s.SENSOR_REGISTRY_IDS.includes('SENS-21'))process.exit(1);console.log(s.SENSOR_REGISTRY_IDS.slice(-3).join(' '))"` exits 0; record the printed tail, SENS-20 today, since plan 355.1-03 appends at the end wherever the tail is then). If a peer took SENS-21, stop and report "SENS-21 taken by <file>; every 355.1 plan names SENS-21; replan with the next free id".
(c) 355 interfaces present: run each of these greps and require at least one hit each; a zero-hit grep means a 355 seam this phase depends on did not land as planned, so stop and report the missing seam by name:
`grep -n "writeStampedSideChannel\|markEurekaReachSurfaced\|EUREKA_REACH_LEDGER_RELPATH\|runGuardGate\|probeGuard" lib/core/eureka/eureka-reach-runner.cjs`;
`grep -n "bankStatements\|stampRankedPairs\|writeOpportunityNode\|SOURCED_FROM" scripts/eureka-portfolio-report.cjs`;
`grep -n "module.exports\|--stamp" scripts/hsi-to-graph.cjs scripts/whitespace-to-graph.cjs scripts/stamp-connections.cjs`;
`grep -n "stampFn\|stampsByPairKey" lib/core/rs-engine.cjs`;
`grep -n "markEurekaReachSurfaced" lib/core/navigation-engine.cjs`;
`grep -n "unverified - novel or hallucinated" lib/hmi/dial-label-composer.cjs`;
`grep -n "opportunity_handle\|eureka-reach-ledger" lib/core/sensors/sensor-eureka.cjs`;
`grep -n "Cross-connection stamps (Phase 355)" skills/larry-personality/SKILL.md`;
`ls tests/fixtures/355-rooms tests/helpers/theo-replay-355.cjs tests/helpers/fixture-room-355.cjs tests/test-355-part8-egress.cjs tests/test-355-no-decimal.cjs`.

BASELINE RECORD: create 355.1-BASELINE.md (Feynman prose, hyphens only, no em-dash) with the headings `## Gate`, `## SENS-21 free`, `## 355 interface anchors (post-355)`, `## Shared-file status at BASE_3551`, `## Doctor acceptance baseline (BASE_3551)`, `## Born-wired gates at BASE_3551`, `## Measured Phase 117 child runtime`. Contents: `BASE_3551 = <git rev-parse --short HEAD>` and the date; the gate command outputs; the output of every grep in (c), one fenced block each, so later plans start from post-355 line numbers instead of the research's; `git status --short` for the peer-touched files (lib/core/navigation.cjs, lib/mcp/stop-gate-handler.cjs, lib/mcp/tools/sensors.cjs, lib/mcp/tools/gate.cjs, lib/core/navigation-engine.cjs, skills/larry-personality/SKILL.md, hooks/hooks.json, lib/core/insight-sensors.cjs, lib/core/sensors/sensor-priority.cjs) recorded as information only (a dirty file is a peer's work in flight; later tasks re-check before editing). Doctor: run `node scripts/doctor.cjs --acceptance` once (stdout and stderr to a file under the session scratch directory, never inside the repo), record its exit code and the sorted failing point ids parsed exactly the way run-all-355.sh's doctor_acceptance_no_new_regression parses them, and note the comparison against 355-BASELINE.md's set. Born-wired: record the exit code of `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs`, `node scripts/check-shape-declaration.cjs --check`. Child runtime (research C12): write a throwaway node script in the session scratch directory (never the repo) that reads `~/.mindrian/explored-materials/*.jsonl` read-only, pairs each fired entry's fired time with the mtime of the room's `.mindrian/auto-explore-<material_id>.json` when that file exists, and prints n, p50, p90 and max in seconds; record those four numbers (research measured n 135, p50 8.4 s, p90 11.6 s, max 166 s; record the fresh numbers, whatever they are). Commit with `git add -f` on the baseline file: `docs(355.1-01): Phase 355 closed gate, SENS-21 free, 355 anchors and baselines`.
  </action>
  <verify>
    <automated>grep -c "BASE_3551 = " .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-BASELINE.md && grep -c "## Measured Phase 117 child runtime" .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^- \[ \] 355-[0-9]+-PLAN\.md' .planning/ROADMAP.md` printed nothing when Task 1 ran (recorded under `## Gate`)
    - `grep -rn "SENS-21" lib data commands skills agents scripts tests` printed nothing when Task 1 ran (recorded under `## SENS-21 free`)
    - 355.1-BASELINE.md contains all seven headings named in the action and a 7-to-12 hex-char commit id after `BASE_3551 = `
    - Under `## Measured Phase 117 child runtime` the record holds four numbers labeled n, p50, p90, max
    - `grep -cP '\x{2014}' .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-BASELINE.md` prints 0
  </acceptance_criteria>
  <done>The phase has a proven post-355 starting point, a free sensor id, anchored 355 seams, and baselines captured once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/run-all-3551.sh skeleton and the delta fixture-room helper (test-first)</name>
  <files>tests/test-3551-fixture-room.cjs, tests/helpers/fixture-room-3551.cjs, tests/run-all-3551.sh</files>
  <read_first>
    - tests/run-all-355.sh (whole file: run / run_if, strip_comments, the glob loop at :76-90, the Part 8 sweep at :91-119, the Part 9 sweep, the package-diff leg, the structural gates, doctor_acceptance_no_new_regression, the no-regression legs, emdash_scan)
    - tests/helpers/fixture-room-219.cjs (buildFixtureRoom: seeding room.db only through navigation writers) and tests/helpers/fixture-room-354.cjs (makeScratchRoom, SKIP_EXIT_CODE)
    - tests/helpers/fixture-room-355.cjs (355-20's buildFilingRoom, the newest idiom)
    - lib/core/navigation.cjs exports (writeClaimNode, writeEdge, writeMemoryArtifactNode and the write-door open/close the analogs use)
    - lib/core/resolve-active-room.cjs lines 481-530 (resolveSessionRoom, MINDRIAN_ROOMS_HOME, the registry path) and lib/core/session-binding.cjs (the binding writer tests use)
    - tests/helpers/hygiene-355.cjs (scrubVendorKey, installNetGuard, makeChecker)
  </read_first>
  <behavior>
    - buildDeltaRoom('t') returns { root, roomsHome, roomDir, slug, cleanup } where roomDir lives under roomsHome, room.db exists and opens through navigation's read-only door, and the registry file under roomsHome lists the room
    - addClaims(5) adds exactly five nodes of type 'claim' (count read back through navigation's read-only door); each claim's text carries a 'SECRET-3551-' marker
    - addContradicts() adds two claims and exactly one CONTRADICTS edge between them through navigation.writeEdge
    - setStage('Design') rewrites STATE.md frontmatter venture_stage to 'Design'; setStage(null) writes an empty STATE.md (the unreadable case)
    - addChildRoom('child-a') adds a registry room whose parent is this room's slug, and creates its folder
    - addArtifact() adds one node of type 'memory_artifact' through navigation.writeMemoryArtifactNode
    - bindSession('sess-1') writes a session binding so resolveSessionRoom({ sessionId: 'sess-1', home: roomsHome }) resolves this room
    - copy355FixtureRoom('room-extend') returns a mkdtemp copy path when tests/fixtures/355-rooms/room-extend exists, else null
    - cleanup() removes only its own mkdtemp root
    - the helper source contains no `INSERT INTO`, no `.prepare(` and no `DatabaseSync` on an executable line
  </behavior>
  <action>
RED: write tests/test-3551-fixture-room.cjs ('use strict', hygiene-355 preamble: scrubVendorKey and installNetGuard before any repo require, attempts() === 0 as the last check; exit 77 with a loud message if node:sqlite is unavailable) asserting every behavior line. Run it; it fails (helper absent). Commit `test(355.1-01): add failing delta fixture-room helper test`.

GREEN: create tests/helpers/fixture-room-3551.cjs ('use strict', BSL header, "Phase 355.1; seeds only through navigation writers; never raw SQL; hyphens only") exporting `buildDeltaRoom(label, opts)`, `copy355FixtureRoom(name)`, `SKIP_EXIT_CODE = 77`, `MARKER_PREFIX = 'SECRET-3551-'`. buildDeltaRoom makes a mkdtemp roomsHome, a room folder with ROOM.md and STATE.md, a room.db created through the same navigation write door the 219/355 fixtures use, and a registry entry in the shape resolve-active-room.cjs reads; it returns the planting helpers named in the behavior list (addClaims, addContradicts, setStage, addChildRoom, addArtifact, bindSession, writeRoomMd(frontmatterObject), cleanup). Every text field carries MARKER_PREFIX plus a counter. Run the test green. Commit `feat(355.1-01): delta fixture-room helper`.

SKELETON: create tests/run-all-3551.sh (chmod +x) mirroring tests/run-all-355.sh: header prose naming Phase 355.1, BASE_3551 (from 355.1-BASELINE.md), the date, the baseline failing-point set as a literal list, and the measured child runtime; `set -uo pipefail`; ROOT cd; PASS/FAIL/SKIP counters; `run`, `run_if`, `strip_comments`. Legs:
(1) `for f in tests/test-3551-*.cjs; do run_if "$(basename "$f")" "$f" node "$f"; done` with exit 77 counted as SKIP;
(2) Part 8 sweep with `PART8_RE="fetch\(|https?://|require\(['\"]node:https?|\b(curl|wget)\b"` over executable lines of the target list lib/core/navigation/room-delta-facts.cjs lib/core/sensors/sensor-room-delta.cjs lib/core/ambient-framing.cjs lib/core/ambient-trigger.cjs lib/core/ambient-run.cjs scripts/ambient-stop.cjs scripts/auto-explore-fire.cjs scripts/scout-cadence-guard.cjs, each run_if-on-existence until plan 355.1-15 flips them to hard legs once every target exists;
(3) Part 9 sweep: no `node:sqlite`, `DatabaseSync` or `INSERT INTO` on executable lines of the same target list;
(4) `git diff --quiet "$BASE_3551" -- package.json package-lock.json` (zero new dependencies);
(5) `node scripts/check-floor-ledger.cjs --check`;
(6) structural gates: `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-shape-declaration.cjs --check` (no --strict), `node scripts/check-render-coverage.cjs`, and `node scripts/check-cirs-declaration.cjs --check` over every `.planning/phases/355.1-*/355.1-*-PLAN.md`;
(7) doctor_acceptance_no_new_regression copied from run-all-355.sh with THIS phase's BASE_3551 set;
(8) no-regression legs, each run_if: `bash tests/run-all-355.sh`, `node tests/test-213-sensor-eureka.cjs`, `node tests/test-219-harvest-sensor.cjs`, `node tests/test-345-lockstep.cjs`, `node tests/test-245-priority-complete.cjs`, `node tests/test-sensors-part8-sweep.cjs`, `node tests/test-sensors-routing-fence.cjs`, `node tests/test-auto-explore-fingerprint.cjs`, `node tests/test-auto-explore-fire.cjs`, `node tests/test-detection-routing-local-only.cjs`, `node tests/test-connector-tier-d-hooks.cjs`, `node tests/test-198-adapter-budget.test.cjs`, `node tests/test-355-side-channel-v2.cjs`, `node tests/test-355-filing.cjs`, `node tests/test-355-sens13-fire-once.cjs`, `node tests/test-355-no-decimal.cjs`, `node tests/test-355-tri-polar.cjs`, `node tests/test-dial-label-bank-drift.cjs`;
(9) em-dash leg: `emdash_scan` greps U+2014 over every new 355.1 file (tests/test-3551-*.cjs tests/helpers/*3551*.cjs tests/fixtures/3551 lib/core/navigation/room-delta-facts.cjs lib/core/sensors/sensor-room-delta.cjs lib/core/ambient-*.cjs scripts/ambient-stop.cjs) and over ADDED lines only of modified files (`git diff "$BASE_3551" -- lib scripts commands skills hooks data docs tests | grep '^+' | grep -v '^+++'`), zero hits required;
(10) glob-collision guard (research Pitfall 11): `ls tests/test-355-1-*.cjs` must match nothing.
End with the PASS/FAIL/SKIP summary and a non-zero exit when FAIL > 0. Run `bash tests/run-all-3551.sh` once, record its wall-clock seconds in 355.1-BASELINE.md under a new `## run-all-3551 skeleton runtime` heading. Commit `test(355.1-01): run-all-3551.sh skeleton` (git add -f the baseline file).
  </action>
  <verify>
    <automated>node tests/test-3551-fixture-room.cjs && bash -n tests/run-all-3551.sh && bash tests/run-all-3551.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-3551-fixture-room.cjs` exits 0 and prints `FAIL: 0`
    - `grep -vE '^\s*(//|\*|/\*)' tests/helpers/fixture-room-3551.cjs | grep -cE "INSERT INTO|\.prepare\(|DatabaseSync"` prints 0
    - `grep -c 'for f in tests/test-3551-\*.cjs' tests/run-all-3551.sh` prints 1
    - `grep -c "doctor_acceptance_no_new_regression" tests/run-all-3551.sh` prints at least 2
    - `grep -c "check-cirs-declaration" tests/run-all-3551.sh` prints at least 1
    - `bash tests/run-all-3551.sh` exits 0 on this tree (355.1-specific legs SKIP or pass, no-regression legs pass, doctor leg reports no new failing point)
    - `ls tests/test-355-1-*.cjs 2>/dev/null | wc -l` prints 0
    - git log shows the test commit before the feat commit for the helper
  </acceptance_criteria>
  <done>The phase has an aggregator that SKIPs cleanly until each artifact lands and one fixture helper that plants every delta class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer sessions -> shared working tree | Phases 357, 358, 361 edit shared files concurrently |
| Phase 355 closure -> Phase 355.1 start | 355.1 consumes 355 seams that must exist, not be assumed |
| doctor --acceptance -> run-all-3551 baseline | The baseline decides what counts as a new regression for the whole phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-01 | Tampering | building on an unclosed Phase 355 | mitigate | Gate (a): ROADMAP unchecked-plan grep empty and 355-27-SUMMARY.md present, else stop |
| T-3551-02 | Tampering | a duplicate sensor id (SENS-21 claimed by a peer) | mitigate | Gate (b): repo-wide grep and SENSOR_REGISTRY_IDS tail check; stop on a hit |
| T-3551-03 | Repudiation | a silently re-baselined doctor gate | mitigate | Baseline captured once at BASE_3551 with commit and date in two places; later plans only compare |
| T-3551-04 | Information disclosure | fixture text leaking through tests | mitigate | Every fixture text carries the SECRET-3551- marker so Part 8 legs can grep argv, logs and files for it |
| T-3551-SC | Tampering | npm installs | accept | No package is installed by this phase (RESEARCH Package Legitimacy Audit: none); leg (4) asserts package files unchanged |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Record `.planning/phases/355.1-.../355.1-BASELINE.md` (headings Gate, SENS-21 free, 355 interface anchors (post-355), Shared-file status at BASE_3551, Doctor acceptance baseline (BASE_3551), Born-wired gates at BASE_3551, Measured Phase 117 child runtime, run-all-3551 skeleton runtime; constant `BASE_3551`)
- Aggregator `tests/run-all-3551.sh` with shell functions `run`, `run_if`, `strip_comments`, `doctor_acceptance_no_new_regression`, `emdash_scan`, constant `BASE_3551`, variable `PART8_RE`
- Test helper `tests/helpers/fixture-room-3551.cjs` exports `buildDeltaRoom` (returning `addClaims`, `addContradicts`, `setStage`, `addChildRoom`, `addArtifact`, `bindSession`, `writeRoomMd`, `cleanup`), `copy355FixtureRoom`, `SKIP_EXIT_CODE`, `MARKER_PREFIX`
- Test `tests/test-3551-fixture-room.cjs`

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently. Commit with `git commit --only <this plan's paths>`; files under `.planning/phases/**` need `git add -f`; this plan edits no shared source file, but if any `git status --short` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it; no `state.*` writes while a peer executes.

<verification>
- `node tests/test-3551-fixture-room.cjs`
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set equals the BASE_3551 set just recorded)
</verification>

<success_criteria>
- Phase 355 proven closed and SENS-21 proven free before any 355.1 edit
- Baselines captured once with commit and date; the aggregator and fixture helper exist and pass
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-01-SUMMARY.md` when done (new phase-dir files need `git add -f`).
</output>
