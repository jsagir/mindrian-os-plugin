---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 09
type: execute
wave: 5
depends_on: ["355.1-06", "355.1-07", "355.1-08"]
files_modified:
  - tests/test-3551-child.cjs
  - lib/core/ambient-run.cjs
  - scripts/auto-explore-fire.cjs
autonomous: true
requirements: [AMB-03, AMB-06, AMB-08, AMB-04]
canon_parts: [8, 9, 11]
tags: [fire-child, ambient-mode, lifecycle, lock, part8, class-e]

must_haves:
  truths:
    - "CONTEXT 'The run underneath (AMB-03, locked)': scripts/auto-explore-fire.cjs gains an ambient mode: argv[4] === '--ambient' runs the room-level ambient run; the dead eureka call at the old :334-340 seam (runEurekaScan without a pair, then process.exit before it settles; research C3) is REPLACED by an awaited ambient run in material mode, so classes (a) to (e) all run through one child behind one lock"
    - "CONTEXT 'The run underneath': the Phase 117 material path (3 or 4 argv) is otherwise byte-identical: same pipelines, same finding file, same markCompleted and telemetry; the existing 117 tests stay green"
    - "Pitfall 7: an ambient run never emits auto_explore_fired, never calls store.markCompleted or appendMaterial for its own work, and never shares the 117 daily cap"
    - "CONTEXT 'Run ledger (AMB-04, locked)': the child acquires the 'wx' lock first; on a held lock it releases its own in-flight claim (releaseAmbientClaim) so the delta is not lost, and exits 0; on completion it records the ledger (recordAmbientRun) and rewrites last-room-delta.json (run_state completed or deps_missing, opportunity_handle, surfaced_via, framing) and always releases the lock in finally"
    - "The child verifies that argv's 'ambient-<12 hex>' id matches the delta_hash prefix in last-room-delta.json before running; a mismatch exits 0 without running (argv tamper check)"
    - "When python or the HSI dependencies are missing (scripts/check-hsi-deps), the producers that need them record deps_missing; when every producer is deps_missing the delta state becomes run_state 'deps_missing', the SENS-21 fallback input (research A3)"
    - "AMBIENT_TOTAL_BUDGET_MS = 4 minutes bounds the composition (the measured max child runtime in 355.1-BASELINE.md plus Theo stamping headroom); AMBIENT_LOCK_STALE_MS (8 minutes, plan 355.1-05) is twice it"
    - "The framing enum for the card is computed in the child through lib/core/ambient-framing.cjs framingFor(roomDir, { allowStructural: true }) and written to the delta state (CONTEXT 'Strategy, not keywords'); it never gates the run (Ruling 4)"
    - "Canon Part 8: the fire child still requires no brain-client (tests/test-detection-routing-local-only.cjs); the only wire is verification-stamp.cjs reached through ambient-run.cjs; child stderr carries enums only (no marker, no title, no path)"
    - "Exit 0 on every path (the Phase 117 discipline): the ambient run is awaited before process.exit, and the uncaughtException / unhandledRejection backstops stay"
  artifacts:
    - path: "lib/core/ambient-run.cjs"
      provides: "runAmbientInChild (the lifecycle around the composition) and checkProducerDeps"
      exports: ["runAmbientInChild", "checkProducerDeps", "AMBIENT_TOTAL_BUDGET_MS"]
    - path: "scripts/auto-explore-fire.cjs"
      provides: "--ambient mode and the awaited material-mode ambient run replacing the dead eureka seam"
      contains: "--ambient"
    - path: "tests/test-3551-child.cjs"
      provides: "process-level ambient runs, lock, tamper, deps, material mode, Part 8 stderr legs"
      min_lines: 120
  key_links:
    - from: "scripts/auto-explore-fire.cjs"
      to: "lib/core/ambient-run.cjs runAmbientInChild"
      via: "await in both modes before process.exit(0)"
      pattern: "runAmbientInChild"
    - from: "lib/core/ambient-run.cjs runAmbientInChild (material seam)"
      to: "lib/core/ambient-trigger.cjs evaluateAndClaim"
      via: "in-process evaluation and claim, no spawn (the child is already running)"
      pattern: "evaluateAndClaim"
---

<objective>
AMB-03 made real on the process level: the one existing background child learns an ambient mode and stops calling a dead seam. In --ambient mode (started by the evaluator from a Stop hook or a close-out) it runs the stamped composition for the room. In the Phase 117 material mode (started by the PostToolUse fingerprint on a filed artifact) it now awaits the same ambient run where it used to fire-and-forget a eureka scan that never finished. One lock, one ledger, one child.

This is the first plan that makes behavior live: the existing fingerprint hook already spawns this child, so after this plan class (e) runs ambiently on CLI. That is why it depends on the checkpoint (plan 355.1-08).
Output: runAmbientInChild, the child's two edits, one process-level test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-RESEARCH.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-BASELINE.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md
@lib/core/ambient-run.cjs
@lib/core/ambient-trigger.cjs
@scripts/scout-cadence-guard.cjs
@tests/helpers/fixture-room-3551.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED process-level child test</name>
  <files>tests/test-3551-child.cjs</files>
  <read_first>
    - scripts/auto-explore-fire.cjs (whole file, re-read: argv parse :160-175, pipelines, finding write, markCompleted, the dead seam at :325-340, exit backstops :340-359)
    - tests/test-auto-explore-fire.cjs, tests/test-119-02-auto-explore-fire-wiring.cjs, tests/296-blast-radius.test.cjs, tests/test-detection-routing-local-only.cjs (what they pin about the child)
    - tests/test-245-trigger-negative.cjs lines 20-45 and 140-150 (the dead-loopback MINDRIAN_BRAIN_URL='http://127.0.0.1:1' isolation idiom for child processes)
    - scripts/check-hsi-deps (its exit codes and tier output)
    - lib/memory/explored-materials-store.cjs (where the 117 ledger writes: ~/.mindrian/explored-materials, so the test must point HOME at a mkdtemp dir)
  </read_first>
  <behavior>
    - child env for every process leg: HOME and MINDRIAN_ROOMS_HOME set to mkdtemp dirs, MINDRIAN_BRAIN_URL='http://127.0.0.1:1', every vendor and Brain key variable deleted, cwd a mkdtemp dir (no .env); so stamping degrades to unverified and nothing leaves the machine
    - ambient mode: on a buildDeltaRoom room with addClaims(5), evaluateAndClaim(roomDir, { seam: 'stop_hook', now }) (in-process) then spawning `node scripts/auto-explore-fire.cjs <roomDir> '' ambient-<hex12> sess-1 --ambient` exits 0 within AMBIENT_TOTAL_BUDGET_MS plus 10 s; afterwards last-room-delta.json has run_state 'completed' (or 'deps_missing' when check-hsi-deps reports missing deps, asserted consistently), the ledger's last_delta_hash equals the claimed hash, in_flight is null, the lock file is gone, every producer outcome is in AMBIENT_PRODUCER_OUTCOMES, tier_counts.strong + tier_counts.indirect === 0 (Theo down), no .mindrian/last-eureka.json was written, surfaced_via 'none'
    - Pitfall 7: after that run, the mkdtemp HOME has no explored-materials JSONL line and room.db has no auto_explore_fired memory_event (read through navigation's read-only door)
    - Part 8: the child's captured stderr contains no 'SECRET-3551-' marker, no artifact title and no path beyond what the harness itself prints; stdout is empty
    - tamper: argv id 'ambient-000000000000' not matching the delta state's hash -> exit 0, delta state unchanged (still 'started'), no ledger completion
    - lock held: with a fresh ambient-run.lock pre-created, the ambient child exits 0 quickly, the ledger's in_flight is cleared (releaseAmbientClaim), the pre-created lock is untouched
    - material mode, in-process: runAmbientInChild(roomDir, { seam: 'material', materialId: 'ab12cd34ef56', sessionId: 's', deps: { composition: fakeComposition } }) on a room with one addArtifact() evaluates, claims (delta state material_id 'ab12cd34ef56'), runs fakeComposition once, and records the ledger; a second call on the same state does not run fakeComposition (same_hash)
    - material mode, process level: the 3-argv and 4-argv invocations keep writing the 117 finding exactly as tests/test-auto-explore-fire.cjs pins (run that test as a sub-assertion)
    - static: `grep -c "runEurekaScan" scripts/auto-explore-fire.cjs` is 0; the file contains `await` before the runAmbientInChild call in both branches; it contains no brain-client require and no ADDRESSES_PROBLEM_TYPE-style token (tests/test-detection-routing-local-only.cjs run as a sub-assertion)
  </behavior>
  <action>Write tests/test-3551-child.cjs ('use strict', hygiene-355 preamble for the in-process parts, attempts() === 0 last; exit 77 with a loud message when node:sqlite is unavailable) implementing every behavior line; spawn children with child_process.spawnSync and the isolated env; clean every mkdtemp dir. Run it; it fails (no ambient mode, no runAmbientInChild). Commit `test(355.1-09): add failing process-level ambient child test (AMB-03)`.</action>
  <verify>
    <automated>node tests/test-3551-child.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The test exists, runs and fails on the missing mode
    - `grep -c "127.0.0.1:1" tests/test-3551-child.cjs` prints at least 1 and `grep -c "auto_explore_fired" tests/test-3551-child.cjs` prints at least 1
  </acceptance_criteria>
  <done>A failing test pins the child's ambient lifecycle, its isolation and its Part 8 behavior.</done>
</task>

<task type="auto">
  <name>Task 2: runAmbientInChild and checkProducerDeps in lib/core/ambient-run.cjs</name>
  <files>lib/core/ambient-run.cjs</files>
  <read_first>
    - lib/core/ambient-run.cjs (plan 355.1-07's module, re-read)
    - lib/core/ambient-trigger.cjs evaluateAndClaim, scripts/scout-cadence-guard.cjs (acquireAmbientLock, releaseAmbientLock, releaseAmbientClaim, recordAmbientRun, readRoomDeltaState, writeRoomDeltaState)
    - lib/core/ambient-framing.cjs framingFor
    - scripts/check-hsi-deps (how to call it cheaply and read its result)
    - 355.1-BASELINE.md `## Measured Phase 117 child runtime`
  </read_first>
  <action>
Add to lib/core/ambient-run.cjs: `AMBIENT_TOTAL_BUDGET_MS = 4 * 60 * 1000` with a comment deriving it from 355.1-BASELINE.md's measured max child runtime plus Theo stamping headroom (AMBIENT_LOCK_STALE_MS in the guard is twice it). `checkProducerDeps(opts)` -> { python: boolean, hsi: boolean } by running scripts/check-hsi-deps once through child_process.spawnSync with a short timeout (stdio captured, never inherited), soft-fail to false. `async runAmbientInChild(roomDir, opts)` with opts { seam ('material' for the 117 path; omitted in ambient mode), ambientId, materialId, sessionId, deps, now }:
(1) material seam: `evaluateAndClaim(roomDir, { seam: 'material', materialId, sessionId, now })` (lazy-require ./ambient-trigger.cjs); anything but 'claimed' -> return { state: <decision> }. Ambient mode (opts.ambientId given, no materialId): readRoomDeltaState(roomDir); require run_state 'started' and `ambientIdFor(delta_hash) === opts.ambientId`, else return { state: 'tamper_or_stale' } without touching anything; the seam (stop_hook or closeout) is the delta state's own seam field.
(2) acquireAmbientLock; not ok -> releaseAmbientClaim(roomDir, delta_hash) and return { state: 'locked' }.
(3) try: writeRoomDeltaState(... run_state 'running', same other fields); deps = checkProducerDeps(); result = await `(opts.deps && opts.deps.composition) || runAmbientComposition`(roomDir, { deps: { ...opts.deps, depsAvailable: deps }, deltaState, budgetMs: AMBIENT_TOTAL_BUDGET_MS }) (adapters needing python record 'deps_missing' when deps say so; wire that through the adapters from plan 355.1-07); framing = framingFor(roomDir, { allowStructural: true }).framing; runState = every producer outcome is 'deps_missing' ? 'deps_missing' : 'completed'; recordAmbientRun(roomDir, { deltaHash, producers, tierCounts, surfacedVia, materialId, now }); writeRoomDeltaState(... run_state runState, opportunity_handle: result.card ? result.card.opportunity_handle : null, surfaced_via: result.surfaced_via, framing); finally releaseAmbientLock(roomDir).
Return { state: runState, surfaced_via }. Never throws; stderr diagnostics only as enums and only under MINDRIAN_AMBIENT_DEBUG === '1'. No emitFired, no store.markCompleted, no appendMaterial anywhere in this module. Export runAmbientInChild, checkProducerDeps, AMBIENT_TOTAL_BUDGET_MS.
Run the in-process legs of tests/test-3551-child.cjs and tests/test-3551-ambient-run.cjs. Commit with `git commit --only lib/core/ambient-run.cjs`: `feat(355.1-09): ambient run lifecycle - lock, claim, compose, record, release (AMB-03, AMB-04)`.
  </action>
  <verify>
    <automated>node tests/test-3551-ambient-run.cjs && node -e "const r=require('./lib/core/ambient-run.cjs');for(const k of ['runAmbientInChild','checkProducerDeps'])if(typeof r[k]!=='function')process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Both verify commands exit 0
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/ambient-run.cjs | grep -cE "emitFired|markCompleted|appendMaterial|auto_explore_fired"` prints 0
    - `grep -c "releaseAmbientLock" lib/core/ambient-run.cjs` prints at least 1 inside a finally block (read the function)
  </acceptance_criteria>
  <done>The ambient lifecycle exists once and is shared by both child modes.</done>
</task>

<task type="auto">
  <name>Task 3: The fire child's --ambient mode and the awaited material-mode ambient run</name>
  <files>scripts/auto-explore-fire.cjs</files>
  <read_first>
    - scripts/auto-explore-fire.cjs (whole file, re-read now; `git status --short scripts/auto-explore-fire.cjs` must be empty)
    - tests/test-3551-child.cjs (the RED legs) and the existing 117 child tests named in Task 1
  </read_first>
  <action>
Header: update the Canon Part 8 paragraph honestly: "zero outbound network in the Phase 117 pipelines; since Phase 355.1 the ambient run (lib/core/ambient-run.cjs) stamps findings through verification-stamp.cjs, the one Theo wire, with canon names only; this file itself still requires no Brain client". Add the argv line "[roomDir, '', 'ambient-<12 hex>', session_id, '--ambient'] (Phase 355.1 ambient mode, started by lib/core/ambient-trigger.cjs)". Do not add the Brain-only edge-type token or a brain-client require anywhere (tests/test-detection-routing-local-only.cjs).
main(): immediately after the argv parse, add an ambient branch taken only when argv[4] is exactly '--ambient': exit 0 unless roomDir is non-empty and the third argv element matches /^ambient-[0-9a-f]{12}$/; otherwise await runAmbientInChild(roomDir, { ambientId: <that id>, sessionId: session_id }) inside try/catch (runAmbientInChild reads the seam, stop_hook or closeout, from the delta state itself), then process.exit(0), so the 117 pipelines never run in ambient mode. Replace the dead seam (the runEurekaScan block and its comment) with a comment "Phase 355.1: the dead fire-and-forget eureka scan (research C3) is replaced by the awaited ambient run; the stamped card (SENS-13) now has a producer on this path; delta class (e)" and an awaited runAmbientInChild(roomDir, { seam: 'material', materialId: material_id, sessionId: session_id }) inside try/catch whose catch body is empty with the comment 'never regress the exit-0 discipline', followed by the existing process.exit(0). Leave every other line of the 117 path, the backstops and the helpers byte-identical.
Run tests/test-3551-child.cjs, tests/test-auto-explore-fire.cjs, tests/test-119-02-auto-explore-fire-wiring.cjs, tests/296-blast-radius.test.cjs, tests/test-237-post-write-session-stamp.cjs, tests/test-detection-routing-local-only.cjs, tests/test-connector-tier-d-hooks.cjs, tests/test-auto-explore-fingerprint.cjs, tests/test-sens01-first-material-fires.cjs, tests/test-acpt-03-first-material-explore.cjs and tests/test-auto-explore-canon-part-8.cjs. Commit with `git commit --only scripts/auto-explore-fire.cjs`: `feat(355.1-09): the Phase 117 child gains an ambient mode and awaits the stamped run (AMB-03, AMB-06)`.
  </action>
  <verify>
    <automated>node tests/test-3551-child.cjs && node tests/test-auto-explore-fire.cjs && node tests/test-119-02-auto-explore-fire-wiring.cjs && node tests/296-blast-radius.test.cjs && node tests/test-detection-routing-local-only.cjs && node tests/test-connector-tier-d-hooks.cjs && node tests/test-auto-explore-canon-part-8.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command exits 0; tests/test-3551-child.cjs prints `FAIL: 0`
    - `grep -c "runEurekaScan" scripts/auto-explore-fire.cjs` prints 0 and `grep -vE '^\s*(//|\*|/\*)' scripts/auto-explore-fire.cjs | grep -c "runAmbientInChild("` prints 2 (one call per mode)
    - `grep -cE "require\(['\"][^'\"]*brain[-_]client" scripts/auto-explore-fire.cjs` prints 0
    - `git diff HEAD~1 -- scripts/auto-explore-fire.cjs | grep '^-' | grep -v '^---' | grep -vcE '^-\s*(//|\*|$)|runEurekaScan|eurekaRunner|eslint-disable|catch \(_e\) \{ /\* ignore: additive seam'` prints 0 (only the dead seam and comments removed)
    - `grep -cP '\x{2014}' scripts/auto-explore-fire.cjs` prints 0
  </acceptance_criteria>
  <done>One child, one lock: the room-level ambient run and the filed-artifact run both end in a stamped, tier-gated result, and the dead seam is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluator -> detached child (argv) | The only input the child trusts from its parent |
| child -> room.db, ledger, side channel | Background writes outside any turn |
| child -> Theo (through verification-stamp.cjs) | The single wire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-39 | Tampering | a crafted argv starting a run on stale or foreign state | mitigate | The argv id must match the delta state's hash prefix and run_state 'started'; otherwise exit 0 untouched |
| T-3551-40 | Denial of service | two children in one room | mitigate | 'wx' lock first; a held lock releases the child's own claim and exits 0 |
| T-3551-41 | Information disclosure | room text in child stderr | mitigate | Enum-only diagnostics off by default; process-level leg greps stderr for the marker and titles |
| T-3551-42 | Denial of service | a hung producer holding the lock | mitigate | AMBIENT_TOTAL_BUDGET_MS bounds the composition; lock released in finally; stale reclaim at twice the budget |
| T-3551-43 | Tampering | the 117 path drifting | mitigate | Every other line byte-identical; the ten existing 117 tests run as verify commands; the removed-lines diff check |
</threat_model>

## Artifacts this phase produces

This plan's share:
- `lib/core/ambient-run.cjs` exports `runAmbientInChild(roomDir, opts)` (opts `seam`, `ambientId`, `materialId`, `sessionId`, `deps { composition }`, `now`; states `completed`, `deps_missing`, `locked`, `tamper_or_stale` plus evaluator decisions), `checkProducerDeps(opts)`, `AMBIENT_TOTAL_BUDGET_MS`
- `scripts/auto-explore-fire.cjs` argv flag `--ambient` (5th element) and the awaited material-mode ambient run
- Test `tests/test-3551-child.cjs`

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently. Re-read `scripts/auto-explore-fire.cjs` and `lib/core/ambient-run.cjs` from disk before editing; if `git status --short` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it. Commit with `git commit --only` on this plan's paths; no `state.*` writes while a peer executes. Released installs pick this up only after a release (the "not live until released" rule).

<verification>
- `node tests/test-3551-child.cjs`
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set within BASE_3551's)
</verification>

<success_criteria>
- The child runs the stamped composition in both modes behind one lock, exits 0 everywhere, leaks nothing
- The dead eureka seam is gone and the 117 path is otherwise unchanged
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-09-SUMMARY.md` when done.
</output>
