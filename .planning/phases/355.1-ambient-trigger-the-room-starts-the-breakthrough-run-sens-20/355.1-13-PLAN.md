---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 13
type: execute
wave: 6
depends_on: ["355.1-11"]
files_modified:
  - tests/test-3551-mcp-fire-once.cjs
  - lib/mcp/surfaced-offers.cjs
  - lib/mcp/tools/sensors.cjs
  - lib/mcp/stop-gate-handler.cjs
autonomous: true
requirements: [AMB-05, AMB-06]
canon_parts: [3, 8, 9, 11, 12]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: ["stop_gate_check close-out (lib/mcp/stop-gate-handler.cjs closeOutRoom)", "suggest_next / reach_candidates (in-process note only, still a pure read of disk and room.db)"]
  surfaces_removed: []
  spine_consumed: ["lib/mcp/tools/sensors.cjs dispatchCandidateReaches", "lib/core/eureka/eureka-reach-runner.cjs markEurekaReachSurfaced"]
  gate_impact: "SENS-13 fires once on Desktop and Cowork: the pull tools remember in process memory which stamped card won a pull, and the Stop-time close-out persists that through the 355-19 fire-once ledger; no new tool, no new reach; stop_gate_check keeps hitl_shape F.1 and the pull tools keep hitl_shape none."
  explanation: "Navigator Ruling 2: fire-once on hookless surfaces is recorded at the Stop-time close-out on every surface so suggest_next stays a pure read; 355-22's decide() mark is extended, not replaced; research C2 found the MCP loop never calls decide(), so without this the card repeats on every pull."
tags: [ruling-2, fire-once, mcp, close-out, sens-13]

must_haves:
  truths:
    - "Navigator Ruling 2 (CONTEXT, locked, approved at the checkpoint): fire-once on hookless surfaces is RECORDED at the Stop-time close-out (closeOutRoom) on every surface; suggest_next and reach_candidates stay a pure read (no disk or room.db write); 355-22's decide() mark is extended, not replaced"
    - "Research C2: the MCP loop never calls decide(), so 355-22's mark never runs on Desktop or Cowork; this plan closes that gap without a decide() ctx producer and without a write in the pull tools"
    - "Precision rule mirroring 355-22: a handle is recorded only when the SENS-13 reach WON the pull (it is reaches[0] after the Phase 222 ranking in dispatchCandidateReaches), the same 'winning reach' rule 355-22 applies in decide(); a card that lost the ranking, or was written by a child after the pull ran, is not recorded and stays available"
    - "The in-process note lives in one tiny module, lib/mcp/surfaced-offers.cjs (a bounded Map keyed by room directory, process memory only, never persisted by a pull); the close-out takes the room's noted handles and persists each with markEurekaReachSurfaced, then clears them"
    - "Stated limit (the ruling's accepted cost): chat-only turns skip stop_gate_check, so between two close-outs a winning card can be offered on more than one pull; the 30-minute side-channel freshness window bounds it; the test pins this as a documented fact"
    - "Soft-fail everywhere: a failing markEurekaReachSurfaced or a missing module never throws out of closeOutRoom or out of a pull; closeOutRoom's return shape is unchanged"
  artifacts:
    - path: "lib/mcp/surfaced-offers.cjs"
      provides: "noteSurfaced, takeSurfaced, peekSurfaced, _resetForTest; bounded in-memory map"
      exports: ["noteSurfaced", "takeSurfaced", "peekSurfaced", "MAX_ROOMS", "MAX_HANDLES_PER_ROOM", "_resetForTest"]
    - path: "lib/mcp/stop-gate-handler.cjs"
      provides: "_closeOutEurekaFireOnce inside closeOutRoom"
      contains: "_closeOutEurekaFireOnce"
    - path: "tests/test-3551-mcp-fire-once.cjs"
      provides: "one card per delta hash on the MCP path, pure-read proof, winner-only rule, soft-fail"
      min_lines: 100
  key_links:
    - from: "lib/mcp/tools/sensors.cjs dispatchCandidateReaches"
      to: "lib/mcp/surfaced-offers.cjs noteSurfaced"
      via: "winner is SENS-13 with an opportunity_handle"
      pattern: "noteSurfaced"
    - from: "lib/mcp/stop-gate-handler.cjs closeOutRoom"
      to: "lib/core/eureka/eureka-reach-runner.cjs markEurekaReachSurfaced"
      via: "takeSurfaced(roomDir) then mark each handle"
      pattern: "markEurekaReachSurfaced"
---

<objective>
Ruling 2 on the hookless surfaces: the stamped card is offered once on Desktop and Cowork, as it already is on the CLI. The pull tools stay pure reads of disk; they only remember, in the server's memory, which stamped card actually won a pull. The Stop-time close-out writes that down through the 355-19 fire-once ledger. The result is the same "winning reach is recorded" rule 355-22 applies inside decide(), moved to the one place the MCP loop is allowed to write.

Output: one small in-memory module, one note in the pull path, one close-out step, one test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-RESEARCH.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md
@tests/helpers/fixture-room-3551.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED MCP fire-once test</name>
  <files>tests/test-3551-mcp-fire-once.cjs</files>
  <read_first>
    - lib/mcp/tools/sensors.cjs (whole file, re-read: buildSensorInputs, dispatchCandidateReaches and its two return paths, the suggest_next and reach_candidates handlers, the declared-read comment, `_internal`)
    - lib/mcp/stop-gate-handler.cjs (closeOutRoom, _closeOutStateMd, the _closeOut* helper style, handleStopEvent, _resetForTest)
    - lib/core/navigation-engine.cjs (the 355-22 mark after the Phase 222 ranking: the 'winning reach' rule to mirror)
    - lib/core/eureka/eureka-reach-runner.cjs markEurekaReachSurfaced and EUREKA_REACH_LEDGER_RELPATH; lib/core/sensors/sensor-eureka.cjs (dedup ledger read)
    - tests/test-3551-surfacing.cjs (the injected composition setup that produces a filed, stamped, framed side channel)
  </read_first>
  <behavior>
    - setup as in tests/test-3551-surfacing.cjs: a room with a filed indirect finding, a v2 side channel with its opportunity_handle, a delta state with framing
    - winner noted: one dispatchCandidateReaches pull whose reaches[0] is the SENS-13 reach -> peekSurfaced(roomDir) contains that handle; a pull where another reach outranks SENS-13 (plant a higher-ranked reach through a fixture signal) -> nothing noted
    - pure read: an fs write spy (writeFileSync, renameSync, openSync with a write flag, mkdirSync) around two pulls records zero calls; no room.db write (row counts unchanged)
    - close-out: closeOutRoom(roomDir, sessionId) after the winning pull marks the handle in <room>/.mindrian/eureka-reach-ledger.json and clears the note; the next pull returns no SENS-13 reach
    - one card per delta hash on the MCP path: pull (SENS-13) -> close-out -> pull (none) -> close-out -> pull (none)
    - not shown, not marked: a side channel written after the last pull (no note) is NOT marked by the close-out and is returned by the next pull
    - stated limit pinned: two pulls with no close-out between both return SENS-13 (documented in the test output as the ruling's accepted cost, bounded by the 30-minute freshness window)
    - bounded: noting more than MAX_HANDLES_PER_ROOM handles or more than MAX_ROOMS rooms drops the oldest; takeSurfaced returns a copy and clears only that room
    - soft-fail: with <room>/.mindrian made read-only, closeOutRoom returns its normal shape and does not throw; a thrown noteSurfaced (monkeypatched) never breaks a pull
    - CLI untouched: `grep -c "markEurekaReachSurfaced" lib/core/navigation-engine.cjs` still prints 1
  </behavior>
  <action>Write tests/test-3551-mcp-fire-once.cjs ('use strict', hygiene-355 preamble, attempts() === 0 last; exit 77 with a loud message when node:sqlite is unavailable; call surfaced-offers _resetForTest and the handler's _resetForTest between legs) implementing every behavior line. Run it; it fails (module and close-out step absent). Commit `test(355.1-13): add failing MCP one-card-per-delta test (Ruling 2, AMB-05)`.</action>
  <verify>
    <automated>node tests/test-3551-mcp-fire-once.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The test exists, runs and fails on the missing module
    - `grep -c "peekSurfaced\|takeSurfaced" tests/test-3551-mcp-fire-once.cjs` prints at least 2 and `grep -c "accepted cost" tests/test-3551-mcp-fire-once.cjs` prints at least 1
  </acceptance_criteria>
  <done>A failing test pins one card per delta hash on the MCP path and the pure-read contract.</done>
</task>

<task type="auto">
  <name>Task 2: surfaced-offers.cjs, the winner note in the pull path, the close-out fire-once step</name>
  <files>lib/mcp/surfaced-offers.cjs, lib/mcp/tools/sensors.cjs, lib/mcp/stop-gate-handler.cjs</files>
  <read_first>
    - lib/mcp/tools/sensors.cjs and lib/mcp/stop-gate-handler.cjs: re-read both from disk NOW (peer-touched by 357 and 358); `git status --short` on each must be empty before editing, otherwise wait, re-read, never stage or revert a peer's diff
    - tests/test-3551-mcp-fire-once.cjs (the RED legs)
  </read_first>
  <action>
Create lib/mcp/surfaced-offers.cjs ('use strict', BSL header: "Phase 355.1 Ruling 2: which stamped SENS-13 card won a pull on this MCP server process. Process memory ONLY; a pull never persists anything (suggest_next and reach_candidates stay declared reads); the Stop-time close-out persists through the 355-19 fire-once ledger. Hyphens only."). Exports: `MAX_ROOMS = 64`, `MAX_HANDLES_PER_ROOM = 16`, `noteSurfaced(roomDir, handle)` (string guards; insertion-ordered Map of Set; oldest room or handle dropped past the bounds), `peekSurfaced(roomDir)` (array copy), `takeSurfaced(roomDir)` (array copy, then delete that room), `_resetForTest()`. Never throws.
sensors.cjs: at the single point where dispatchCandidateReaches knows the ordered winner (make both return paths flow through one small local helper so there is one call site), when reaches[0] has signal 'eureka_bridge' and a non-empty evidence.opportunity_handle, call noteSurfaced(roomDir, handle) inside try/catch; comment "Phase 355.1 Ruling 2: note the winning stamped card in process memory; the pull stays a pure read of disk and room.db; the close-out persists it". Change nothing else.
stop-gate-handler.cjs: add `_closeOutEurekaFireOnce(roomDir)` in the _closeOut* style (try/catch, lazy requires of ./surfaced-offers.cjs and ../core/eureka/eureka-reach-runner.cjs inside the try, return false on failure): for each handle in takeSurfaced(roomDir) call markEurekaReachSurfaced(roomDir, handle); then call recordAmbientCloseout(roomDir, { now: Date.now() }) from the ambient ledger module (../../scripts/scout-cadence-guard.cjs, lazy-required in the same try) so the ledger shows when the close-out last ran. Comment "Navigator Ruling 2 (Phase 355.1): fire-once on hookless surfaces is recorded at the Stop-time close-out; 355-22's decide() mark is extended, not replaced". In closeOutRoom, call `_closeOutEurekaFireOnce(roomDir);` immediately after `_closeOutStateMd(roomDir);` (plan 355.1-14 later inserts the evaluator step between the two, per CONTEXT's 'right after _closeOutStateMd'; order does not matter for correctness because only pull-noted handles are marked). Do not change closeOutRoom's return object or any other line. No stdout write.
Run tests/test-3551-mcp-fire-once.cjs, tests/test-3551-surfacing.cjs, tests/test-355-sens13-fire-once.cjs, every existing stop-gate and sensors-tool test (`ls tests/*.cjs | grep -iE "stop-gate|stop_gate|suggest|reach-candidates|mcp-sensors"`, run each), tests/test-198-adapter-budget.test.cjs, and `node scripts/build-connector-registry.cjs --check` (the tool connectors export is unchanged). Commit with `git commit --only lib/mcp/surfaced-offers.cjs lib/mcp/tools/sensors.cjs lib/mcp/stop-gate-handler.cjs`: `feat(355.1-13): SENS-13 fires once on Desktop and Cowork, recorded at the close-out (Ruling 2, AMB-05)`.
  </action>
  <verify>
    <automated>node tests/test-3551-mcp-fire-once.cjs && node tests/test-3551-surfacing.cjs && node tests/test-355-sens13-fire-once.cjs && node tests/test-198-adapter-budget.test.cjs && node scripts/build-connector-registry.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command exits 0; the fire-once test prints `FAIL: 0`
    - `grep -vE '^\s*(//|\*|/\*)' lib/mcp/tools/sensors.cjs | grep -c "noteSurfaced("` prints 1
    - `grep -c "_closeOutEurekaFireOnce(roomDir);" lib/mcp/stop-gate-handler.cjs` prints 1
    - `git diff HEAD~1 -- lib/mcp/tools/sensors.cjs lib/mcp/stop-gate-handler.cjs | grep '^-' | grep -v '^---' | grep -vc '^-\s*$'` prints 0 or only the lines folded into the single return helper (recorded in the SUMMARY)
    - `grep -vE '^\s*(//|\*|/\*)' lib/mcp/surfaced-offers.cjs | grep -cE "writeFileSync|renameSync|require\('node:fs'\)|require\('fs'\)"` prints 0
    - `grep -cP '\x{2014}' lib/mcp/surfaced-offers.cjs` prints 0
  </acceptance_criteria>
  <done>The stamped card is offered once per delta on every surface, and the pull tools still write nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP pull tools (declared reads) -> process memory | The pull remembers what won, without writing |
| MCP close-out -> fire-once ledger | The one place the MCP loop persists surfacing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-55 | Tampering | a declared-read tool gaining a write | mitigate | The note is process memory only; fs write spy and row-count legs prove zero writes per pull |
| T-3551-56 | Repudiation | a card marked surfaced that never won a pull | mitigate | Only reaches[0] with signal eureka_bridge is noted (the 355-22 winner rule); unnoted side channels are not marked |
| T-3551-57 | Denial of service | unbounded memory in a long-lived server | mitigate | MAX_ROOMS and MAX_HANDLES_PER_ROOM bounds with oldest-first eviction |
| T-3551-58 | Denial of service | a ledger write failure breaking the close-out | mitigate | Lazy require plus try/catch soft-fail; read-only .mindrian leg |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Module `lib/mcp/surfaced-offers.cjs` exports `noteSurfaced(roomDir, handle)`, `peekSurfaced(roomDir)`, `takeSurfaced(roomDir)`, `MAX_ROOMS`, `MAX_HANDLES_PER_ROOM`, `_resetForTest()`
- `lib/mcp/tools/sensors.cjs` winner note in dispatchCandidateReaches (single local return helper)
- `lib/mcp/stop-gate-handler.cjs` helper `_closeOutEurekaFireOnce(roomDir)` called in `closeOutRoom` after `_closeOutStateMd`
- Test `tests/test-3551-mcp-fire-once.cjs`

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently; `lib/mcp/tools/sensors.cjs` and `lib/mcp/stop-gate-handler.cjs` are peer-touched. Re-read each from disk immediately before editing; if `git status --short` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it; wait and re-read, or stop and report. Commit with `git commit --only` on this plan's paths; no `state.*` writes while a peer executes.

<verification>
- `node tests/test-3551-mcp-fire-once.cjs`
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set within BASE_3551's)
</verification>

<success_criteria>
- SENS-13 one card per delta hash on the MCP path, recorded at the close-out (Ruling 2)
- suggest_next and reach_candidates remain pure reads of disk and room.db
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-13-SUMMARY.md` when done.
</output>
