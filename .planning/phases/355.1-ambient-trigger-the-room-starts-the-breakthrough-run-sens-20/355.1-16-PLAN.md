---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 16
type: execute
wave: 9
depends_on: ["355.1-15"]
files_modified:
  - .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/reviews/phase-355.1-close-out.md
  - docs/OPEN-HANDOFFS.md
autonomous: true
requirements: [AMB-01, AMB-02, AMB-03, AMB-04, AMB-05, AMB-06, AMB-07, AMB-08, AMB-09]
canon_parts: [6, 8, 11]
tags: [close-out, requirements, validation, handoff, rethinking-room]

must_haves:
  truths:
    - "CONTEXT 'Proof and close (AMB-09, locked)': AMB-01..AMB-09 rows are registered in .planning/REQUIREMENTS.md at close, each flipped to `- [x]` with an indented **Measured:** line citing a command and a result from the SUMMARYs, or left `- [ ]` with a stated reason; the Traceability active count, family list and caveat list are updated; nothing is inferred"
    - "355.1-VALIDATION.md is finalized from the real runs: every Per-Task Verification Map row's status reflects the actual result, nyquist_compliant true, wave_0_complete true, approval dated; the manual-only rows (a real Desktop session; the PWS author's confirmation and the navigator's approval, both done at the checkpoint) carry their outcome or a stated pending note"
    - "docs/OPEN-HANDOFFS.md gains a dated entry at the top pointing to docs/reviews/phase-355.1-close-out.md, which records what shipped, the release-note items (a new async Stop hook entry, the fire child's ambient mode, SENS-21, /mos:auto-explore reworded as the fallback), that nothing is live for users until scripts/release.sh cuts a version and they update, and the open limits (14 percent frontmatter coverage makes the reward sparse; chat-only turns can re-offer a card between close-outs on hookless surfaces; Cowork server locality A1; missing python on user machines A3)"
    - "Dev-Research Compositing (CLAUDE.md): a dated research entry `~/MindrianRooms/rethinking-mindrianos/research/2026-09-<DD>-ambient-trigger-room-starts-the-run.md` (markdown only, the room's own flat-file convention) records the navigator's ruling, the design (detector / evaluator / card split, five delta classes, ledger, tri-polar seams), the research corrections, the Hooked audit and the checkpoint ruling, with citations back to this phase's files and commits; mirrored to ~/MindrianOS/research/; committed in the repository that tracks the room with the 'rethinking-mindrianos: file ...' message convention; no room.db or graph write"
    - "ROADMAP.md: only the Phase 355.1 card's `**Requirements**: TBD` line changes, and only if the orchestrator has not already changed it; nothing else in ROADMAP.md is touched"
    - "Theo-side analog stated in the close-out doc: Theo's projection of the plugin's sensor_index gains a SENS-21 Sensor node on its next re-emission; no Theo file is edited"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "### Phase 355.1 - Ambient trigger (AMB family) with AMB-01..AMB-09"
      contains: "AMB-09"
    - path: "docs/reviews/phase-355.1-close-out.md"
      provides: "the tracked close-out record that travels across machines"
    - path: "docs/OPEN-HANDOFFS.md"
      provides: "dated entry pointing to the close-out"
      contains: "355.1"
  key_links:
    - from: ".planning/REQUIREMENTS.md Phase 355.1 section"
      to: "the 355.1 plan SUMMARYs"
      via: "Measured lines citing the command and result"
      pattern: "Measured"
---

<objective>
Close Phase 355.1 (AMB-09): register the nine requirements with their proof, finalize the validation contract, leave a tracked close-out and a dated handoff entry so the next session on any machine knows what shipped and what is not yet live, and file the reasoning trail in the rethinking-mindrianos room as the compositing rule requires. Every claim cites a command and a result from the plan SUMMARYs; none is inferred.

Output: VALIDATION finalized, REQUIREMENTS rows, one ROADMAP line (only if still TBD), the close-out doc, the handoff entry, the room filing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-PRD.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-BASELINE.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-HOOKED-AUDIT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize VALIDATION, register AMB-01..09, the ROADMAP requirements line</name>
  <files>.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-VALIDATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - every 355.1 plan SUMMARY (`ls .planning/phases/355.1-*/355.1-*-SUMMARY.md`) for the commands and results the Measured lines cite
    - 355.1-VALIDATION.md (the Per-Task Verification Map filled at planning time)
    - .planning/REQUIREMENTS.md: the last phase section before `## Traceability` (grep `^### Phase`), the Phase 355 and 361 sections (row format and provenance paragraph wording), and the Traceability paragraph (active count, family list, mint-date list, caveat list)
    - .planning/ROADMAP.md: only the `### Phase 355.1` card
    - 355.1-PRD.md section 3 (the AMB-01..09 wording to register) and 355.1-CONTEXT.md (the corrections: SENS-21 not SENS-20; run-all-3551.sh not run-all-355.1.sh)
  </read_first>
  <action>
Run `bash tests/run-all-3551.sh` once and fill 355.1-VALIDATION.md's Status column from the real result per row (green / red / flaky), set frontmatter `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`, tick the Wave 0 list, fill the Manual-Only table's outcome column (the checkpoint row: done, with the 355.1-CHECKPOINT.md date; the real-Desktop row: its outcome if the navigator ran it, else "pending, navigator-run after release"), and replace the `**Approval:** pending` line with `**Approval:** approved <date>`.
Then `git status --short .planning/REQUIREMENTS.md .planning/ROADMAP.md` must be empty; if either is dirty a peer is editing: wait and retry, never stage or revert their change. Add `### Phase 355.1 - Ambient trigger (AMB family)` immediately after the last phase section and before `## Traceability`, opening with: "AMB-01..09 were minted in `355.1-PRD.md` (2026-09-23) as phase-local IDs, corrected by `355.1-CONTEXT.md` against `355.1-RESEARCH.md` (the detector is SENS-21, not SENS-20; the aggregator is tests/run-all-3551.sh), and are registered here at phase close by `355.1-16-PLAN.md` Task 1, per the Phase 272/274/276/275/340/355 precedent. `355.1-CONTEXT.md` is the scope contract." Then one row per ID in the house row format (about 100 columns, 6-space continuation), each ending with its plan list, followed by an indented `**Measured:** (<date>) <command> -> <result>` line: AMB-01 delta vocabulary through navigation reads (plans 02, 03, 06: delta-classes, classify, chokepoint tests); AMB-02 SENS-21 on context_block, lockstep, fallback-only fire (plan 03, 10: lockstep, sensor-fires); AMB-03 the run underneath through the one child (plans 06, 07, 09: evaluator, one-spawner, ambient-run, child); AMB-04 ledger, throttle, lock (plans 05, 06, 08: ledger test, floor rows approved); AMB-05 one card, strong and indirect only, Hooked audit (plans 07, 08, 10, 11, 13: surfacing, double-card, mcp-fire-once, hooked-audit); AMB-06 tri-polar with auto-explore as the fallback (plans 09, 12, 13, 14: tri-polar, prose); AMB-07 framing without keywords (plans 04, 08, 11, 12: framing, prose); AMB-08 boundaries (plans 06, 07, 09, 15: part8-egress, the 355-21 sweep, the four gates); AMB-09 proof and close (plans 01, 15, 16: run-all-3551, doctor vs BASE_3551). A row whose proof is missing stays `- [ ]` with the stated reason. In `## Traceability`: add 9 to the active count (read the current number first), append "plus AMB-01..09 (Phase 355.1)" to the family list with a one-sentence mint note in the house style, and add AMB to the caveat family list.
ROADMAP.md: only if the Phase 355.1 card still reads `**Requirements**: TBD`, replace that one line with `**Requirements**: AMB-01..AMB-09 (phase-local, minted in 355.1-PRD.md; registered in REQUIREMENTS.md)`; touch nothing else.
Commit immediately with `git add -f` on the VALIDATION file and `git commit --only` on the changed paths: `docs(355.1-16): register and close AMB-01..09 with measured proof; validation finalized`.
  </action>
  <verify>
    <automated>grep -c "^### Phase 355.1 - Ambient trigger (AMB family)" .planning/REQUIREMENTS.md && test "$(grep -cE '^- \[[ x]\] \*\*AMB-0[1-9]\*\*' .planning/REQUIREMENTS.md)" -eq 9 && grep -c "nyquist_compliant: true" .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The section heading appears once and exactly nine AMB rows exist
    - Every `- [x]` AMB row is followed by a `**Measured:**` line; every `- [ ]` AMB row carries a stated reason
    - `grep -c "AMB-01..09 (Phase 355.1)" .planning/REQUIREMENTS.md` prints at least 1
    - `git diff HEAD~1 -- .planning/ROADMAP.md | grep '^[-+]' | grep -v '^[-+][-+]' | wc -l` prints 0 or 2
    - `grep -c "\*\*Approval:\*\* approved" .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-VALIDATION.md` prints 1
  </acceptance_criteria>
  <done>Every AMB ID is registered with the proof that closes it, or the honest reason it stays open; the validation contract is signed.</done>
</task>

<task type="auto">
  <name>Task 2: The close-out record, the handoff entry, and the rethinking-mindrianos filing</name>
  <files>docs/reviews/phase-355.1-close-out.md, docs/OPEN-HANDOFFS.md</files>
  <read_first>
    - docs/reviews/phase-354-close-out.md (the close-out record style) and the top of docs/OPEN-HANDOFFS.md (the dated bold-entry format)
    - 355.1-HOOKED-AUDIT.md, 355.1-CHECKPOINT.md, 355.1-BASELINE.md and every 355.1 SUMMARY
    - the most recent entries under ~/MindrianRooms/rethinking-mindrianos/research/ and ~/MindrianOS/research/ (flat dated markdown files) and `git -C ~ log --oneline -5` (the 'rethinking-mindrianos: file ...' message convention)
    - /home/jsagi/Theo/notes/graph-rulebook.md lines 395-437 (READ ONLY)
  </read_first>
  <action>
Write docs/reviews/phase-355.1-close-out.md (Feynman prose, hyphens only, no room content, role names only): what the navigator ruled and why (the room, never the user, starts the breakthrough run); what shipped per requirement (one line each with the test that proves it); the six rulings and the checkpoint outcome; the release-note items for the next `scripts/release.sh` cut (new async Stop entry `scripts/ambient-stop.cjs`, the fire child's `--ambient` mode, SENS-21 registered, `/mos:auto-explore` reworded as the fallback and listing SENS-21, SENS-13's framed card); a plain statement that none of this is live for users until a release is cut and users run the two update commands; the open limits (14 percent frontmatter coverage makes the reward sparse; chat-only turns can re-offer a card between close-outs on hookless surfaces, the Ruling 2 cost; Cowork server locality A1; missing python or scikit-learn on user machines degrades to SENS-21's fallback, A3; find-connections stamps only pairs other producers surfaced, C11); the Theo-side analog (a SENS-21 Sensor node on Theo's next re-emission of the plugin's sensor_index; no Theo file edited). Add a dated bold entry at the top of docs/OPEN-HANDOFFS.md: "**<date>, Phase 355.1 CLOSED: [ambient trigger - the room starts the breakthrough run](reviews/phase-355.1-close-out.md).**" followed by two sentences (what shipped; not live until released).
Room filing (Dev-Research Compositing): bind this session to the rethinking-mindrianos room first if the write-scope check requires it (research Pitfall 14); write `~/MindrianRooms/rethinking-mindrianos/research/<YYYY-MM-DD>-ambient-trigger-room-starts-the-run.md` following the most recent entry's frontmatter and heading convention (markdown only): the navigator's ruling in substance, the design, the research corrections C1-C13 in one line each, the Hooked audit's four phases and its 14 percent disclosure, the measured floors and the checkpoint ruling, the test results, each citing the phase files and commits; mirror the same file to `~/MindrianOS/research/`; commit it in the repository that tracks the room with the message `rethinking-mindrianos: file Phase 355.1 ambient-trigger research trail`. If the write-scope check refuses and cannot be satisfied, record the drafted entry verbatim in this plan's SUMMARY with "mirror pending" (the 354 precedent), never force it. No room.db or graph write.
Commit the two docs in the dev repo with `git commit --only docs/reviews/phase-355.1-close-out.md docs/OPEN-HANDOFFS.md`: `docs(355.1-16): Phase 355.1 close-out record and handoff entry`.
  </action>
  <verify>
    <automated>test -f docs/reviews/phase-355.1-close-out.md && grep -c "phase-355.1-close-out" docs/OPEN-HANDOFFS.md && test "$(grep -cP '\x{2014}' docs/reviews/phase-355.1-close-out.md)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - docs/reviews/phase-355.1-close-out.md exists, names all nine AMB IDs, the six rulings, the release-note items and the 14 percent limit, and contains no em-dash
    - docs/OPEN-HANDOFFS.md's first dated entry references phase-355.1-close-out.md
    - A new dated file exists under ~/MindrianRooms/rethinking-mindrianos/research/ with a mirror under ~/MindrianOS/research/, committed with the 'rethinking-mindrianos: file ...' convention, OR the SUMMARY records the drafted entry verbatim with "mirror pending" and the refusal reason
    - `git -C /home/jsagi/Theo status --short` shows nothing this session created
  </acceptance_criteria>
  <done>The next session on any machine knows what shipped and what is not yet live, and the reasoning trail lives in the room.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this session -> shared tracked files (REQUIREMENTS.md, ROADMAP.md, OPEN-HANDOFFS.md) | Peers edit the same files |
| dev repo -> rethinking-mindrianos room | Research trail filed into a real room |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-69 | Tampering | clobbering a peer's REQUIREMENTS / ROADMAP edit | mitigate | Clean-file check before editing; commit immediately; only the 355.1 section, the traceability lines and one ROADMAP line change |
| T-3551-70 | Repudiation | a requirement closed without proof | mitigate | Every `- [x]` carries a Measured line with a command and a result; unproven rows stay open with a reason |
| T-3551-71 | Information disclosure | room content or a personal name in tracked docs or the room filing | mitigate | Counts, enums, file paths of this repo and role names only |
| T-3551-72 | Tampering | room graph writes during filing | mitigate | Markdown files only; no room.db or graph write; write-scope refusal recorded, never forced |
</threat_model>

## Artifacts this phase produces

This plan's share:
- `.planning/REQUIREMENTS.md` section `### Phase 355.1 - Ambient trigger (AMB family)` with rows `AMB-01`..`AMB-09`; Traceability family entry `AMB-01..09 (Phase 355.1)`
- Finalized `355.1-VALIDATION.md` (`status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`, `**Approval:**`)
- ROADMAP Phase 355.1 `**Requirements**:` line (only if still TBD)
- Record `docs/reviews/phase-355.1-close-out.md`; dated entry in `docs/OPEN-HANDOFFS.md`
- Room entry `~/MindrianRooms/rethinking-mindrianos/research/<date>-ambient-trigger-room-starts-the-run.md` and its mirror under `~/MindrianOS/research/`

Full phase inventory (every symbol Phase 355.1 creates):
- Modules: `lib/core/navigation/room-delta-facts.cjs` (`readRoomDeltaFacts`, `SQL_CLAIMS_SINCE`, `SQL_CONTRADICTS_KEYS`, `SQL_ARTIFACTS_SINCE`, `ARTIFACT_NODE_TYPES`, `keyHash`, `hasCreatedAt`, `readStageHash`, `readChildKeyHashes`); `lib/core/sensors/sensor-room-delta.cjs` (`sensorRoomDelta`, `classifyRoomDelta`, `computeDeltaHash`, `isMaterialHeldForStampedCard`, `SENSOR_ID = 'SENS-21'`, `REACH_ID`, `POSTURE`, `SIGNAL = 'room_delta'`, `DISPATCH = 'auto-explore'`, `CLAIM_FLOOR_N`, `DELTA_CLASSES`, `SEAMS`, `RUN_STATES`, `DEGRADE_STATES`, `SURFACED_VIA`, `DELTA_STATE_RELPATH`, `DELTA_STATE_SCHEMA_VERSION`, `ROOM_DELTA_FRESHNESS_MS`, `AMBIENT_LEDGER_RELPATH_REPLICA`); `lib/core/ambient-framing.cjs` (`RUNG_IDS`, `RUNG_SOURCES`, `FRAMING_BY_RUNG`, `PWS_STAGE_FRAMING`, `resolveRoomRung`, `framingFor`); `lib/core/ambient-trigger.cjs` (`evaluateRoomDelta`, `evaluateAndClaim`, `evaluateAndMaybeSpawn`, `buildAmbientArgv`, `ambientIdFor`, `DECISIONS`, `AMBIENT_EVAL_BUDGET_MS`, `FIRE_SCRIPT_RELPATH`, `_internal`); `lib/core/ambient-run.cjs` (`runAmbientComposition`, `runAmbientInChild`, `selectCardFinding`, `memoizeCallTool`, `checkProducerDeps`, `PRODUCER_COMMANDS`, `PRODUCER_ADAPTERS`, `CARD_TIERS`, `AMBIENT_TOP_N`, `AMBIENT_TOTAL_BUDGET_MS`); `lib/mcp/surfaced-offers.cjs` (`noteSurfaced`, `peekSurfaced`, `takeSurfaced`, `MAX_ROOMS`, `MAX_HANDLES_PER_ROOM`, `_resetForTest`)
- Extensions: `lib/core/direction-convention.cjs` (`FRAMING_IDS`, `FRAMING_PHRASES`, `hashFramingTable`, `framingHash`, `FRAMING_CONFIRMED`); `scripts/scout-cadence-guard.cjs` (`readAmbientLedger`, `validateAmbientLedger`, `shouldRunAmbient`, `claimAmbientRun`, `releaseAmbientClaim`, `recordAmbientRun`, `recordAmbientBaseline`, `recordAmbientCloseout`, `acquireAmbientLock`, `releaseAmbientLock`, `validateDeltaState`, `writeRoomDeltaState`, `readRoomDeltaState`, `AMBIENT_LEDGER_RELPATH`, `AMBIENT_LOCK_RELPATH`, `AMBIENT_LEDGER_SCHEMA_VERSION`, `AMBIENT_MAX_RUNS_PER_HOUR`, `AMBIENT_THROTTLE_WINDOW_MS`, `AMBIENT_LOCK_STALE_MS`, `STAMPED_MATERIALS_MAX`, `AMBIENT_PRODUCER_IDS`, `AMBIENT_PRODUCER_OUTCOMES`, `AMBIENT_POSTURES`, `SHOULD_RUN_REASONS`); `lib/core/eureka/eureka-reach-runner.cjs` (`measureAndGuardPair`); `scripts/eureka-portfolio-report.cjs` (`fileStampedOpportunity`); `lib/core/navigation.cjs` (`readRoomDeltaFacts` re-export); `lib/core/insight-sensors.cjs` (SENS-21 in five places, Ruling 5 skip); `lib/core/sensors/sensor-priority.cjs` (SENS-21 Group B record); `lib/core/sensors/sensor-eureka.cjs` (`ROOM_DELTA_STATE_RELPATH`, evidence `framing`); `lib/hmi/dial-presenter.cjs` (slotContext `framing`); `lib/hmi/dial-label-composer.cjs` (framed stamped variant); `lib/mcp/tools/sensors.cjs` (winner note); `lib/mcp/stop-gate-handler.cjs` (`_closeOutEurekaFireOnce`, `_closeOutAmbientTrigger`); `scripts/auto-explore-fire.cjs` (`--ambient`, awaited ambient run); `scripts/auto-explore-drain.cjs` (Ruling 5 skip)
- Scripts / hooks / CLI flags: `scripts/ambient-stop.cjs` (`--stop`, `--dry-run`); `hooks/hooks.json` Stop entry with `"async": true`, `"timeout": 5`; env `MINDRIAN_AMBIENT_DEBUG`
- Room files: `<room>/.mindrian/ambient-run-ledger.json` (and `.corrupt.<epoch>` quarantine), `<room>/.mindrian/last-room-delta.json`, `<room>/.mindrian/ambient-run.lock`
- Data: floor-ledger rows `sensor-room-delta.CLAIM_FLOOR_N`, `scout-cadence-guard.AMBIENT_MAX_RUNS_PER_HOUR`; `commands/auto-explore.md` `sensor_triggers: [SENS-01, SENS-21]`; regenerated `data/connector-registry.json`
- Docs: SKILL subsection `### Ambient findings (Phase 355.1)`; records `355.1-BASELINE.md`, `355.1-HOOKED-AUDIT.md`, `355.1-CHECKPOINT.md`, `docs/reviews/phase-355.1-close-out.md`
- Tests: `tests/test-3551-*.cjs` (fixture-room, delta-classes, chokepoint, classify, lockstep, sensor-fires, framing, ledger, evaluator, one-spawner, ambient-run, hooked-audit, child, double-card, surfacing, prose, mcp-fire-once, tri-polar, part8-egress); helper `tests/helpers/fixture-room-3551.cjs`; fixtures `tests/fixtures/3551/prose/*.md`; aggregator `tests/run-all-3551.sh`

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently. Re-read REQUIREMENTS.md, ROADMAP.md and OPEN-HANDOFFS.md from disk immediately before editing; if `git status --short` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it. Commit with `git commit --only` on exactly this plan's paths; `git add -f` for `.planning/phases/**`; no `state.*` writes while a peer executes.

<verification>
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set within BASE_3551's)
- nine AMB rows registered; the close-out, the handoff entry and the room filing exist
</verification>

<success_criteria>
- AC7 recorded with proof; every AMB ID closed with a Measured line or left open with a reason
- The close-out travels across machines (tracked docs); the reasoning trail lives in the room
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-16-SUMMARY.md` when done.
</output>
