---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 02
type: execute
wave: 2
depends_on: ["355.1-01"]
files_modified:
  - tests/test-3551-delta-classes.cjs
  - tests/test-3551-chokepoint.cjs
  - lib/core/navigation/room-delta-facts.cjs
  - lib/core/navigation.cjs
  - lib/core/navigation/CONTEXT.md
autonomous: true
requirements: [AMB-01]
canon_parts: [8, 9]
tags: [navigation, statement-home, delta-reader, chokepoint]

must_haves:
  truths:
    - "CONTEXT 'Trigger vocabulary and readers (AMB-01, locked)': ONE new reader module lib/core/navigation/room-delta-facts.cjs in the graph-integrity-counts.cjs statement-home style, re-exported from lib/core/navigation.cjs as readRoomDeltaFacts; it takes a caller-owned handle, opens nothing, requires nothing from node:sqlite, and never throws"
    - "CONTEXT 'Trigger vocabulary and readers': class (a) reads claims created after a watermark with a fixed SQL literal over nodes.created_at (epoch ms), NEVER goal-cadence computeStrategyCounts().claims_since (it counts the ghost node_created event)"
    - "Class (b) discretion settled (CONTEXT Claude's Discretion, smallest chokepoint change): edges carry no timestamp, so the reader returns the sorted set of truncated sha256 keys of every CONTRADICTS (source, target) pair and the ledger compares key sets; no edge column, no migration, no new memory_event"
    - "Class (c) reads STATE.md frontmatter venture_stage and returns its truncated hash; an unreadable, empty or frontmatter-less STATE.md returns null, which downstream means 'no change' (research Pitfall 5: on-stop rewrites STATE.md in parallel)"
    - "Class (d) reads the room registry and returns the sorted truncated hashes of rooms whose parent is this room's slug; class (e) counts nodes of type memory_artifact or Artifact created after a watermark"
    - "Null never zero: a legacy schema without nodes.created_at, a closed handle or an unreadable registry returns null for that class, never 0 or an empty set"
    - "Part 8 / Part 9: the module returns counts, epoch integers and truncated hashes only (no node text, no slug, no path); the only SQL in any 355.1 file lives in this module; a planted raw SQL reader elsewhere is caught by tests/test-3551-chokepoint.cjs"
  artifacts:
    - path: "lib/core/navigation/room-delta-facts.cjs"
      provides: "readRoomDeltaFacts(db, roomDir, opts) plus fixed SQL literals and the two file readers"
      exports: ["readRoomDeltaFacts", "SQL_CLAIMS_SINCE", "SQL_CONTRADICTS_KEYS", "SQL_ARTIFACTS_SINCE", "ARTIFACT_NODE_TYPES", "keyHash", "hasCreatedAt", "readStageHash", "readChildKeyHashes"]
    - path: "tests/test-3551-delta-classes.cjs"
      provides: "per-class detection legs (a)-(e) through navigation.readRoomDeltaFacts only"
      min_lines: 80
    - path: "tests/test-3551-chokepoint.cjs"
      provides: "no SQL outside the statement home in 355.1 files, with a planted negative control"
  key_links:
    - from: "lib/core/navigation.cjs"
      to: "lib/core/navigation/room-delta-facts.cjs"
      via: "require + readRoomDeltaFacts re-export"
      pattern: "readRoomDeltaFacts"
---

<objective>
AMB-01's read side: three of the five delta classes have no navigation reader today (research C6: node_created events are a ghost, CONTRADICTS edges carry no timestamp, nothing reads stage or sub-room creation). Add ONE statement-home module under lib/core/navigation/ that reads all five classes as closed, content-free facts and re-export it from the one chokepoint, so "detected only through navigation reads" is literally true and there is no second SQL door.

Purpose: every later piece (the SENS-21 classifier, the evaluator, the child) asks the room what changed through one function.
Output: the reader module, the re-export, one CONTEXT.md line, two tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-RESEARCH.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-PATTERNS.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-BASELINE.md
@lib/core/navigation/CONTEXT.md
@tests/helpers/fixture-room-3551.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for the five delta classes and the chokepoint sweep</name>
  <files>tests/test-3551-delta-classes.cjs, tests/test-3551-chokepoint.cjs</files>
  <read_first>
    - lib/core/navigation/graph-integrity-counts.cjs lines 1-110 (docblock contract rules, named SQL constants), 160-190 (schemaVariant, safeCount), 240-245 (SQL_CONTRADICTS_EDGES), 355-365 (exports)
    - lib/core/navigation/section-gate.cjs lines 135-175 (SQL_CLAIM_NODES_NO_JTBD_ANCHOR and the sinceMs epoch-ms convention)
    - lib/core/strategy/goal-cadence.cjs (grep `ARTIFACT_NODE_TYPES` for the artifact vocabulary; grep `claims_since` to see the ghost-event read this plan must NOT reuse)
    - lib/core/resolve-active-room.cjs (grep `registry` for the registry path and the `parent` field shape)
    - tests/helpers/fixture-room-3551.cjs (buildDeltaRoom and its planting helpers)
    - scripts/check-substrate.cjs (its `--diff` mode: what it flags)
  </read_first>
  <behavior>
    - on a fresh buildDeltaRoom room, readRoomDeltaFacts(db, roomDir, { since: { claims_created_at: 0, artifacts_created_at: 0 }, registryHome }) returns a frozen object with exactly the keys schema_variant, claims, contradicts_keys, stage_hash, children, artifacts
    - (a) after addClaims(5): claims.count === 5 and claims.max_created_at is an integer; with since.claims_created_at set to that max, a further addClaims(2) yields claims.count === 2
    - (b) after addContradicts(): contradicts_keys has length 1, each entry is 12 lowercase hex chars, the array is sorted, and a second call returns the identical array
    - (c) setStage('Design') then setStage('Investment') yields two different 12-hex stage_hash values; setStage(null) (empty STATE.md) and a STATE.md with no frontmatter both yield stage_hash === null
    - (d) addChildRoom('child-a') then addChildRoom('child-b') yields children of length 2, sorted, 12-hex each; an unreadable registry file yields children === null
    - (e) addArtifact() yields artifacts.count === 1; since.artifacts_created_at bounds it the same way as claims
    - a handle whose nodes table lacks created_at (a scratch db built through navigation's legacy fixture path, or a stub handle whose PRAGMA table_info(nodes) returns no created_at) yields claims === null and artifacts === null, never 0; a stub handle whose prepare throws yields null for every SQL class and the call does not throw
    - no value anywhere in the returned object contains the fixture's 'SECRET-3551-' marker, the room slug, or a path separator
    - navigation.readRoomDeltaFacts is the same function object as require('lib/core/navigation/room-delta-facts.cjs').readRoomDeltaFacts
    - chokepoint: over the 355.1 file list (lib/core/sensors/sensor-room-delta.cjs, lib/core/ambient-trigger.cjs, lib/core/ambient-run.cjs, lib/core/ambient-framing.cjs, scripts/ambient-stop.cjs, scripts/scout-cadence-guard.cjs, each only if it exists), no executable line contains `node:sqlite`, `DatabaseSync`, `.prepare(`, `.exec(` with SQL, or a `SELECT ` / `INSERT INTO` literal; lib/core/navigation/room-delta-facts.cjs contains SELECT literals but no `node:sqlite`, `DatabaseSync` or `INSERT`
    - negative control: a scratch file written to os.tmpdir() containing a `db.prepare('SELECT id FROM nodes')` line is flagged by the same scan function when its path is added to the list (reported as PASS when caught); the scratch file is removed
  </behavior>
  <action>
Write tests/test-3551-delta-classes.cjs and tests/test-3551-chokepoint.cjs ('use strict', hygiene-355 preamble in each: scrubVendorKey and installNetGuard before any repo require, attempts() === 0 as the last check; delta-classes exits 77 with a loud message when node:sqlite is unavailable). delta-classes opens each fixture room through navigation.openRoomDbReadOnlyForCaller and closes it through navigation's matching close in a finally, and calls ONLY navigation.readRoomDeltaFacts (never the module path directly, except for the same-function-object leg), implementing every behavior line. chokepoint implements a `scanForSql(files)` function (comment-stripped lines, the run-all strip_comments idiom) and the planted negative control, and also runs `node scripts/check-substrate.cjs --diff` as a sub-assertion. Run both; delta-classes fails (module absent); chokepoint passes its scan legs and its negative control (the target files do not exist yet, so the scan is vacuous and says so). Commit `test(355.1-02): add failing delta-class reader tests and the chokepoint sweep (AMB-01)`.
  </action>
  <verify>
    <automated>node tests/test-3551-chokepoint.cjs && (node tests/test-3551-delta-classes.cjs; test $? -ne 0)</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-3551-delta-classes.cjs exists, runs and fails on the missing module
    - `node tests/test-3551-chokepoint.cjs` exits 0 and its output contains `negative control` and `PASS`
    - `grep -c "readRoomDeltaFacts" tests/test-3551-delta-classes.cjs` prints at least 5
  </acceptance_criteria>
  <done>A failing test pins every class read and a green sweep pins the one-SQL-door rule.</done>
</task>

<task type="auto">
  <name>Task 2: lib/core/navigation/room-delta-facts.cjs and the navigation.cjs re-export</name>
  <files>lib/core/navigation/room-delta-facts.cjs, lib/core/navigation.cjs, lib/core/navigation/CONTEXT.md</files>
  <read_first>
    - lib/core/navigation.cjs: re-read it from disk NOW (shared with 357, 358, 361); `git status --short lib/core/navigation.cjs` must be empty before editing, otherwise a peer is editing: wait, re-read, never stage or revert their diff; then read the sectionGate require (`grep -n "sectionGate" lib/core/navigation.cjs`) and the export lines around it
    - lib/core/navigation/graph-integrity-counts.cjs (whole docblock and export shape, the style to copy)
    - lib/core/navigation/CONTEXT.md (the folder contract list of statement homes)
    - tests/test-3551-delta-classes.cjs (the RED legs)
  </read_first>
  <action>
Create lib/core/navigation/room-delta-facts.cjs ('use strict', BSL header, a docblock copying graph-integrity-counts.cjs's rules: caller-supplied open handle, requires nothing from node:sqlite, opens nothing, null never zero, fixed SQL literals with bound `?`, Part 8 local only, Part 9 caller opens through openRoomDbReadOnlyForCaller; cite Phase 355.1 AMB-01, research C6 and the class (b) key-set decision; hyphens only). Module-level constants: `SQL_CLAIMS_SINCE = "SELECT count(*) AS c, max(created_at) AS m FROM nodes WHERE type = 'claim' AND created_at > ?"`; `SQL_CONTRADICTS_KEYS = "SELECT source, target FROM edges WHERE type = 'CONTRADICTS' ORDER BY source, target"`; `SQL_ARTIFACTS_SINCE` = the same count/max shape over `type IN ('memory_artifact', 'Artifact')`; `ARTIFACT_NODE_TYPES = Object.freeze(['memory_artifact', 'Artifact'])` (with a comment citing goal-cadence.cjs's vocabulary; add a test leg asserting equality with goal-cadence's constant when that module exports it). Functions: `keyHash(s)` = first 12 hex chars of sha256 (node:crypto); `hasCreatedAt(db)` via `PRAGMA table_info(nodes)` in try/catch; `readStageHash(roomDir)` reads `<roomDir>/STATE.md`, parses only the leading `---` frontmatter block for a `venture_stage:` line, returns keyHash of the trimmed value or null on any miss; `readChildKeyHashes(roomDir, registryHome)` reads the registry file resolve-active-room.cjs reads (registryHome from opts, else process.env.MINDRIAN_ROOMS_HOME, else ~/MindrianRooms), finds this room's slug by matching the registry entry whose path equals roomDir (fallback path.basename(roomDir)), returns the sorted keyHash of every entry whose `parent` equals that slug, or null on any read or parse failure; `readRoomDeltaFacts(db, roomDir, opts)` returns Object.freeze({ schema_variant: 'migrated' | 'legacy' | 'unreadable', claims: { count, max_created_at } | null, contradicts_keys: string[] | null, stage_hash, children, artifacts }) where every SQL read is its own try/catch returning null. Never throw from any export.
navigation.cjs: add `const roomDeltaFacts = require('./navigation/room-delta-facts.cjs');` beside the sectionGate require and `readRoomDeltaFacts: roomDeltaFacts.readRoomDeltaFacts,` in module.exports beside the sectionGate re-exports, with a one-line comment "Phase 355.1 AMB-01: the room-delta statement home". Change nothing else in the file.
CONTEXT.md: add one line naming room-delta-facts.cjs as a read-only statement home for the ambient trigger's five delta classes.
Run tests/test-3551-delta-classes.cjs, tests/test-3551-chokepoint.cjs, `node scripts/check-substrate.cjs --diff`, and every test that requires navigation.cjs's export shape (`grep -ln "navigation.cjs" tests/test-*export*.cjs tests/test-*navigation*.cjs 2>/dev/null`, run each). Commit with `git commit --only lib/core/navigation/room-delta-facts.cjs lib/core/navigation.cjs lib/core/navigation/CONTEXT.md`: `feat(355.1-02): room-delta statement home re-exported from the chokepoint (AMB-01)`.
  </action>
  <verify>
    <automated>node tests/test-3551-delta-classes.cjs && node tests/test-3551-chokepoint.cjs && node scripts/check-substrate.cjs --diff</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-3551-delta-classes.cjs` exits 0 and prints `FAIL: 0`
    - `grep -c "readRoomDeltaFacts: roomDeltaFacts.readRoomDeltaFacts" lib/core/navigation.cjs` prints 1
    - `git diff HEAD~1 -- lib/core/navigation.cjs | grep '^[-+]' | grep -v '^[-+][-+]' | grep -c '^-'` prints 0 (additive only)
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/navigation/room-delta-facts.cjs | grep -cE "node:sqlite|DatabaseSync|INSERT|claims_since|computeStrategyCounts"` prints 0
    - `grep -c "room-delta-facts" lib/core/navigation/CONTEXT.md` prints at least 1
    - `grep -cP '\x{2014}' lib/core/navigation/room-delta-facts.cjs` prints 0
  </acceptance_criteria>
  <done>The room can be asked what changed, per class, through the one chokepoint, with no content leaving the read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room.db and STATE.md / registry -> delta facts | Local room state becomes the trigger's input |
| delta facts -> ledger, argv, logs | Anything the reader returns may be persisted or passed on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-05 | Tampering | a second SQL door for the trigger | mitigate | Fixed SQL literals in one statement home; tests/test-3551-chokepoint.cjs scans every 355.1 file with a planted negative control; check-substrate --diff |
| T-3551-06 | Information disclosure | room text or slugs in delta facts | mitigate | Return shape is counts, epoch integers and 12-hex truncated hashes only; the test greps every value for the SECRET-3551 marker, the slug and path separators |
| T-3551-07 | Denial of service | a throw on a legacy or locked db breaking the Stop chain | mitigate | Every read is its own try/catch returning null; null never zero; stub-handle legs prove no throw |
| T-3551-08 | Tampering | a torn STATE.md read faking a stage change | mitigate | Unreadable, empty or frontmatter-less STATE.md returns null, which the classifier treats as no change (research Pitfall 5) |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Module `lib/core/navigation/room-delta-facts.cjs` exports `readRoomDeltaFacts(db, roomDir, opts)`, `SQL_CLAIMS_SINCE`, `SQL_CONTRADICTS_KEYS`, `SQL_ARTIFACTS_SINCE`, `ARTIFACT_NODE_TYPES`, `keyHash`, `hasCreatedAt`, `readStageHash`, `readChildKeyHashes`
- Facts object keys `schema_variant`, `claims { count, max_created_at }`, `contradicts_keys`, `stage_hash`, `children`, `artifacts { count, max_created_at }`; opts keys `since { claims_created_at, artifacts_created_at }`, `registryHome`
- Re-export `readRoomDeltaFacts` on `lib/core/navigation.cjs`
- Tests `tests/test-3551-delta-classes.cjs`, `tests/test-3551-chokepoint.cjs` (function `scanForSql`)

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently; `lib/core/navigation.cjs` is peer-touched. Re-read it from disk immediately before editing; if `git status --short lib/core/navigation.cjs` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it; wait and re-read, or stop and report. Commit with `git commit --only` on this plan's paths; no `state.*` writes while a peer executes.

<verification>
- `node tests/test-3551-delta-classes.cjs` and `node tests/test-3551-chokepoint.cjs`
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set within BASE_3551's)
</verification>

<success_criteria>
- All five delta classes read through navigation.readRoomDeltaFacts, null-never-zero, content-free
- No SQL in any other 355.1 file; the planted reader is caught
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-02-SUMMARY.md` when done.
</output>
