---
phase: 355.1
slug: ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-09-23
---

# Phase 355.1 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Scaffolded by the orchestrator from the Nyquist template; the planner fills the per-task map from 355.1-RESEARCH.md "## Validation Architecture".

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | node:test / plain-assert CJS (the repo's `tests/test-*.cjs` idiom, `tests/helpers/hygiene-355.cjs` reused) |
| **Config file** | none - each test is a self-contained `node tests/test-3551-*.cjs` |
| **Quick run command** | `node tests/test-3551-<leg>.cjs` (the leg the task touched) |
| **Full suite command** | `bash tests/run-all-3551.sh` |
| **Estimated runtime** | to be measured by plan 01; the 355 aggregator runs in about 5 minutes |

---

## Sampling Rate

- **After every task commit:** Run the task's own `node tests/test-3551-*.cjs`
- **After every plan wave:** Run `bash tests/run-all-3551.sh` and `bash tests/run-all-355.sh` (no regression on the parent phase)
- **Before `/gsd-verify-work`:** Full suite must be green except the documented external reds inherited from 355 (`deferred-items.md`)
- **Max feedback latency:** 300 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (filled by the planner) | | | AMB-01..AMB-09 | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/run-all-3551.sh` - aggregator skeleton (mirrors `tests/run-all-355.sh`)
- [ ] `tests/test-3551-*.cjs` stubs per requirement, named so `run-all-355.sh`'s glob never picks them up
- [ ] Existing infrastructure (`hygiene-355.cjs`, `installNetGuard`, the fixture-room helpers) covers the rest

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| A real Desktop session sees exactly one card after a real delta | AMB-05, AMB-06 | Hookless surface, no harness | Open a live room on Claude Desktop, file five claims, end the turn, start a new turn, observe one card, end that turn, start another, observe no repeat |
| The PWS author confirms the framing phrases | AMB-07 (Ruling 6) | Human ruling | The single checkpoint |
