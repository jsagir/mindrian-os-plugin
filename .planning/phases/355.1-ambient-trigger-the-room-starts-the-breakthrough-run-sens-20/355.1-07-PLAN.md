---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 07
type: execute
wave: 4
depends_on: ["355.1-04", "355.1-05"]
files_modified:
  - tests/test-3551-ambient-run.cjs
  - lib/core/eureka/eureka-reach-runner.cjs
  - scripts/eureka-portfolio-report.cjs
  - lib/core/ambient-run.cjs
autonomous: true
requirements: [AMB-03, AMB-05, AMB-08]
canon_parts: [7, 8, 9, 11, 12]
tags: [composition, five-producers, stamp, tier-gate, proposed-filing, side-channel]

must_haves:
  truths:
    - "CONTEXT 'The run underneath (AMB-03, locked)': lib/core/ambient-run.cjs composes the five Phase 355 producers (eureka bridge, find-connections, find-bottlenecks, HSI, whitespace) through their 355 library seams so every finding carries the 355 stamp and direction phrase; it is the composition the one child runs, never a spawner"
    - "Navigator Ruling 3: /mos:eureka and /mos:scout resolve halt in the registry; the composition calls the producers' library functions directly because every write is proposed-only; no posture is reclassified; each producer's posture is JOINED from lib/core/recipe-maps.cjs postureForCommand('/mos:<cmd>') and recorded in the result, never typed by hand (CONTEXT 'The split')"
    - "CONTEXT 'Surfacing and the Hooked lens (AMB-05, locked)': only strong and indirect stamps can reach the card; selectCardFinding keeps CARD_TIERS ['strong','indirect'] and orders by tier, then producer order, then the producer's own rank, never by a score; unverified findings are counted in tier_counts and never filed, never written to the side channel"
    - "Research recommended architecture step e: the one selected finding passes the eureka measured differential and Stage A guard (one shared implementation, measureAndGuardPair, extracted from runEurekaScan), then files as a proposed opportunity through 355-20's writer (fileStampedOpportunity, extracted from bankStatements so there is still ONE writer), then writeStampedSideChannel (355-19) records it with opportunity_handle = the new node id, after COMMIT"
    - "Canon Part 9: the filed node is born proposed (review_status never passed), inside one BEGIN / COMMIT with no await between them; the first material step (confirm, qualify) stays behind gate_answer"
    - "CONTEXT 'Boundaries (AMB-08, locked)': Theo receives only canon handles through 355-06's stamping (verification-stamp.cjs is the only wire door; ambient-run.cjs never requires brain-client); one run-level memo wraps deps.callTool so a pair repeated across producers costs one call (per-run only, no cross-run cache, 355 D-12)"
    - "D-06 regulator cap honored (research item 9): the composition never runs the url-ingest crawl and caps Theo work at AMBIENT_TOP_N stamped pairs per producer (355-16's default top)"
    - "Research C11 stated limitation: find-connections is prompt-driven, so in an unattended run it stamps only the distinct pairs the other four producers surfaced"
    - "Every producer runs in its own try/catch with an outcome enum from AMBIENT_PRODUCER_OUTCOMES; one producer's failure never stops the others; a budget overrun marks the rest 'skipped'"
  artifacts:
    - path: "lib/core/ambient-run.cjs"
      provides: "the five-producer stamped composition, card selection, proposed filing and side-channel write"
      exports: ["runAmbientComposition", "selectCardFinding", "memoizeCallTool", "PRODUCER_COMMANDS", "PRODUCER_ADAPTERS", "CARD_TIERS", "AMBIENT_TOP_N"]
    - path: "lib/core/eureka/eureka-reach-runner.cjs"
      provides: "measureAndGuardPair, the extracted steps 3-4 of runEurekaScan"
      exports: ["measureAndGuardPair"]
    - path: "scripts/eureka-portfolio-report.cjs"
      provides: "fileStampedOpportunity, the extracted stamped branch of bankStatements"
      exports: ["fileStampedOpportunity"]
    - path: "tests/test-3551-ambient-run.cjs"
      provides: "selection, tier gate, filing, side channel, posture join, Part 8 Theo-args and integration legs"
      min_lines: 150
  key_links:
    - from: "lib/core/ambient-run.cjs"
      to: "scripts/eureka-portfolio-report.cjs fileStampedOpportunity"
      via: "one writer shared with bankStatements"
      pattern: "fileStampedOpportunity"
    - from: "lib/core/ambient-run.cjs"
      to: "lib/core/eureka/eureka-reach-runner.cjs measureAndGuardPair + writeStampedSideChannel"
      via: "guard then side channel after COMMIT"
      pattern: "writeStampedSideChannel"
    - from: "lib/core/ambient-run.cjs"
      to: "lib/core/recipe-maps.cjs postureForCommand"
      via: "posture joined per producer, recorded not typed"
      pattern: "postureForCommand"
---

<objective>
AMB-03's substance and AMB-05's tier gate: the composition the background child runs. It calls the five stamped Phase 355 producers, keeps only findings Theo verified strongly or indirectly, picks at most one for the card (by tier, never by score), checks it with the same differential-and-guard gate a eureka bridge passes, files it as a proposed opportunity through 355's one writer, and writes the stamped side channel SENS-13 reads. Two tiny, behavior-neutral extractions in 355 files keep "one implementation" true: measureAndGuardPair (from runEurekaScan) and fileStampedOpportunity (from bankStatements).

Nothing is wired live in this plan: the child starts calling it in plan 355.1-09, after the checkpoint.
Output: lib/core/ambient-run.cjs, two extractions, one test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-RESEARCH.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-BASELINE.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-CONTEXT.md
@lib/core/verification-stamp.cjs
@lib/core/verification-stamp-format.cjs
@scripts/scout-cadence-guard.cjs
@tests/helpers/theo-replay-355.cjs
@tests/helpers/fixture-room-355.cjs
@tests/helpers/fixture-room-3551.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Interface record and the RED composition test</name>
  <files>tests/test-3551-ambient-run.cjs</files>
  <read_first>
    - the 355 SUMMARYs for plans 16, 17, 18, 19, 20 and 22 (`ls .planning/phases/355-*/355-1[6-9]-SUMMARY.md .planning/phases/355-*/355-2[02]-SUMMARY.md`), and 355.1-BASELINE.md `## 355 interface anchors (post-355)`
    - scripts/hsi-to-graph.cjs and scripts/whitespace-to-graph.cjs (their 355-16 `main(argv, deps)` entries, --stamp, --top, hsiEndpoints / whitespaceEndpoints, what main returns)
    - lib/core/rs-engine.cjs (runModeInternal opts.stampFn, writeReverseSalientEdges(roomDir, pairs, { stampsByPairKey })) and lib/agents/reverse-salient-agent.cjs renderBottleneckFinding
    - scripts/stamp-connections.cjs (main, parsePairs, --pairs-json)
    - scripts/eureka-portfolio-report.cjs (main, stampRankedPairs, eurekaEndpoints, bankStatements and its stamped branch, the db open door main uses, the post-COMMIT writeStampedSideChannel call)
    - lib/core/eureka/eureka-reach-runner.cjs (runEurekaScan steps 3-4, probeGuard, runGuardGate, writeStampedSideChannel, FIRING_BANDS, the scoreMeasured import and resolveEurekaDiffFloor)
    - lib/core/verification-stamp.cjs (stampFindings(findings, deps), Stamp, TIERS, toNodeProps, fromNodeProps) and lib/core/recipe-maps.cjs postureForCommand (returns { command, autonomous_safe, posture })
    - tests/helpers/theo-replay-355.cjs (makeReplayCallTool, makeNullCallTool, makeCountingCallTool) and tests/helpers/fixture-room-355.cjs (buildFilingRoom)
  </read_first>
  <behavior>
    - header comment of the test holds an INTERFACE RECORD table: for each producer, the exact 355 seam called, its argument form and its return form as read from the landed code (this is the contract Task 3 binds to; no guessing)
    - selectCardFinding: given findings with stamps { unverified } only -> null; { indirect from hsi, strong from whitespace } -> the whitespace strong one; two strong from eureka and hsi -> the eureka one (producer order eureka, find-connections, find-bottlenecks, hsi, whitespace); equal tier and producer -> the lower rank; the function reads no score field (a finding with a huge score and tier indirect loses to a strong one)
    - memoizeCallTool: the same (tool, args) twice across two producer adapters reaches the underlying counting callTool once; different args twice
    - runAmbientComposition(roomDir, { deps: { adapters, callTool, measureAndGuard, now }, deltaState, budgetMs }) on a buildFilingRoom / buildDeltaRoom room with injected adapters:
      (i) only unverified findings -> result.card === null, result.surfaced_via === 'none', tier_counts.unverified equals their count, no opportunity node added, no .mindrian/last-eureka.json written
      (ii) one indirect hsi finding plus one strong whitespace finding, guard clearing -> exactly one new node type 'opportunity' with review_status 'proposed' after close and reopen; its props re-parse through fromNodeProps; formula_version 'stamp-v1'; engine_mode 'ambient'; last-eureka.json validates as v2 with opportunity_handle equal to that node id; result.card.opportunity_handle equals it; result.surfaced_via === 'sens13'
      (iii) measureAndGuard returning not cleared -> nothing filed, no side channel, the producer outcome of the selected finding is 'guard_not_cleared'
      (iv) an adapter that throws -> that producer's outcome 'error', the others still run
      (v) budgetMs exhausted after the first adapter -> every later producer 'skipped'
    - posture: result.producers[id].posture equals postureForCommand(PRODUCER_COMMANDS[id]).posture for all five ids (eureka and hsi are 'halt', whitespace / find-bottlenecks / find-connections are 'run', as measured in research C7); static: ambient-run.cjs contains no `autonomous_safe: true` and no `posture: 'run'` / `posture: 'halt'` literal
    - Part 8: with a counting callTool over a replay map, every captured Theo call's arguments are exactly the keys verification-stamp sends ({ from, to } canon names) and no captured value contains the SECRET-3551 marker or any fixture artifact title
    - static: ambient-run.cjs has no `brain-client` require, no `require('zod')`, no `fetch(`, no `INSERT INTO`, no `url-ingest`; its only side-channel writer call is writeStampedSideChannel
    - integration (exit 77 with a loud message when tests/fixtures/355-rooms/room-extend is absent or `scripts/check-hsi-deps` reports python deps missing): the REAL adapters over a copy355FixtureRoom('room-extend') with a replay callTool built from tests/fixtures/355/theo-stub-responses.json produce findings whose every stamp re-parses through Stamp and whose producers all end with an outcome in AMBIENT_PRODUCER_OUTCOMES
    - extraction regressions (these already pass before and after Task 2): tests/test-355-filing.cjs, tests/test-355-side-channel-v2.cjs, tests/test-213-sensor-eureka.cjs, tests/test-213-part8-boundary.cjs
  </behavior>
  <action>Write tests/test-3551-ambient-run.cjs ('use strict', hygiene-355 preamble, attempts() === 0 last; exit 77 with a loud message when node:sqlite or the 355 helpers are unavailable) with the interface-record header and every behavior line. Run it; it fails (module and extractions absent). Commit `test(355.1-07): add failing ambient composition test with the 355 interface record (AMB-03, AMB-05)`.</action>
  <verify>
    <automated>node tests/test-3551-ambient-run.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The test exists, runs and fails on the missing module
    - `grep -c "INTERFACE RECORD" tests/test-3551-ambient-run.cjs` prints 1 and the record names all five producers
    - `grep -c "postureForCommand" tests/test-3551-ambient-run.cjs` prints at least 1
  </acceptance_criteria>
  <done>A failing test pins the composition, and the 355 seams it binds to are written down from the landed code.</done>
</task>

<task type="auto">
  <name>Task 2: Behavior-neutral extractions - measureAndGuardPair in the runner, fileStampedOpportunity in the report</name>
  <files>lib/core/eureka/eureka-reach-runner.cjs, scripts/eureka-portfolio-report.cjs</files>
  <read_first>
    - both files, whole, re-read now; `git status --short` on each must be empty
    - the interface record in tests/test-3551-ambient-run.cjs
    - tests/test-355-filing.cjs, tests/test-355-side-channel-v2.cjs, tests/test-213-*.cjs (the regressions that prove the extraction changed nothing)
  </read_first>
  <action>
First check whether 355-19 or 355-20 already export a single function that performs runEurekaScan's steps 3-4 (measured differential with the eureka floor and firing band, then the Stage A guard) or bankStatements' stamped single-opportunity write; if one exists, use it, record that in the SUMMARY, and skip that half of this task.
Runner: extract steps (3) and (4) of runEurekaScan into `async function measureAndGuardPair(a, b, opts)` returning { ok: true, score, guard } or { ok: false, reason } with reason 'guard_unavailable' | 'below_floor' | 'guard_not_cleared' (same REASONS enum values runEurekaScan already uses); it performs its own probeGuard with opts.criticProbeFn; runEurekaScan calls it and keeps its exact return values and write behavior. Export measureAndGuardPair. Header note: "Phase 355.1: steps 3-4 extracted so the ambient composition and runEurekaScan share one implementation; behavior unchanged".
Report: extract the stamped branch of bankStatements (the writeOpportunityNode call with reason 'eureka stamped finding', formula_version 'stamp-v1', extraProps toNodeProps(stamp) plus pws_stage and engine_mode, and the two SOURCED_FROM writeEdge calls with origin 'eureka-355') into `function fileStampedOpportunity(db, { a, b, stamp, producer, roomDir, runMode, reason })` that returns the minted node id (or null) and never opens or commits a transaction itself (the caller owns BEGIN / COMMIT); bankStatements calls it with reason 'eureka stamped finding' and producer 'eureka' so its output is byte-identical; the ambient caller passes reason 'ambient stamped finding'. Never pass review_status. Export fileStampedOpportunity. Header note citing Phase 355.1 and "one writer, two callers".
Run tests/test-355-filing.cjs, tests/test-355-side-channel-v2.cjs, tests/test-355-producer-eureka.cjs, tests/test-355-sens13-fire-once.cjs, every tests/test-213-*.cjs, tests/test-215-score.cjs and `node scripts/check-substrate.cjs --diff`. Commit with `git commit --only lib/core/eureka/eureka-reach-runner.cjs scripts/eureka-portfolio-report.cjs`: `refactor(355.1-07): one guard and one filing writer shared with the ambient run (no behavior change)`.
  </action>
  <verify>
    <automated>node tests/test-355-filing.cjs && node tests/test-355-side-channel-v2.cjs && node tests/test-355-producer-eureka.cjs && node tests/test-355-sens13-fire-once.cjs && for t in tests/test-213-*.cjs; do node "$t" || exit 1; done && node scripts/check-substrate.cjs --diff</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command exits 0
    - `grep -c "measureAndGuardPair" lib/core/eureka/eureka-reach-runner.cjs` prints at least 3 (definition, runEurekaScan call, export) unless a pre-existing 355 helper was reused (then recorded in the SUMMARY)
    - `grep -c "fileStampedOpportunity" scripts/eureka-portfolio-report.cjs` prints at least 3 unless a pre-existing helper was reused
    - `git diff HEAD~1 -- scripts/eureka-portfolio-report.cjs | grep '^+' | grep -v '^+++' | grep -cE "review_status|INSERT INTO|BEGIN|COMMIT"` prints 0
  </acceptance_criteria>
  <done>The guard and the filing writer each exist once and serve both the command path and the ambient path.</done>
</task>

<task type="auto">
  <name>Task 3: lib/core/ambient-run.cjs - the stamped five-producer composition</name>
  <files>lib/core/ambient-run.cjs</files>
  <read_first>
    - tests/test-3551-ambient-run.cjs (the RED legs and the interface record)
    - lib/core/recipe-maps.cjs postureForCommand; scripts/scout-cadence-guard.cjs AMBIENT_PRODUCER_IDS, AMBIENT_PRODUCER_OUTCOMES
    - the extraction exports from Task 2 and lib/core/eureka/eureka-reach-runner.cjs writeStampedSideChannel
  </read_first>
  <action>
Create lib/core/ambient-run.cjs ('use strict', BSL header: "Phase 355.1 AMB-03 / AMB-05: the stamped five-producer composition the ONE background child runs. Not a spawner, not a sensor. Ruling 3: producers are called through their library seams (eureka and scout resolve halt; every write here is proposed-only); posture is joined from recipe-maps and recorded, never typed. Only strong and indirect stamps can reach the card; the reward is the tier, never a score. Theo is reached only through verification-stamp.cjs (Part 8). Research C11: find-connections stamps only pairs the other producers surfaced. D-06: no url-ingest crawl; Theo work capped at AMBIENT_TOP_N per producer. Hyphens only."). Requires: node:path, ./recipe-maps.cjs, ../../scripts/scout-cadence-guard.cjs (enums), ./eureka/eureka-reach-runner.cjs (measureAndGuardPair, writeStampedSideChannel), ../../scripts/eureka-portfolio-report.cjs (fileStampedOpportunity; lazy-required inside the filing step if it is heavy at load), ./navigation.cjs (the write door the report's main uses, lazy), and the producer seams (lazy, inside each adapter). No brain-client, no zod, no fetch.
Constants: `PRODUCER_COMMANDS = Object.freeze({ eureka: '/mos:eureka', 'find-connections': '/mos:find-connections', 'find-bottlenecks': '/mos:find-bottlenecks', hsi: '/mos:scout', whitespace: '/mos:whitespace' })`; `CARD_TIERS = Object.freeze(['strong', 'indirect'])`; `AMBIENT_TOP_N` = 355-16's default --top value imported from its module when exported (else the same integer with a comment citing 355-16's default and a floor-ledger check that no decimal is introduced).
`memoizeCallTool(callTool)` -> a function keyed on JSON.stringify([tool, args]) that returns the first promise for a repeated key (per call of runAmbientComposition only).
`PRODUCER_ADAPTERS` = Object.freeze({ eureka, hsi, whitespace, 'find-bottlenecks', 'find-connections' }), each `async (roomDir, ctx) => ({ outcome, findings: [{ producer, a: { handle, text }, b: { handle, text }, stamp, rank }] })` bound to the exact seams in the test's interface record: eureka uses the report's ranking plus stampRankedPairs (355-18) WITHOUT banking; hsi and whitespace use their 355-16 main(argv, deps) with --stamp and --top AMBIENT_TOP_N; find-bottlenecks uses rs-engine runModeInternal with opts.stampFn and writeReverseSalientEdges with stampsByPairKey (355-17); find-connections runs stamp-connections over the distinct endpoint pairs the other four returned (memoized, so repeats cost nothing). Every adapter threads ctx.deps.callTool (the memoized one) into the producer's deps. a.text and b.text stay in process memory for the guard only; never logged, never written.
`selectCardFinding(findings)` per the RED legs (tier order, then PRODUCER order from AMBIENT_PRODUCER_IDS, then rank; unverified dropped; no score read).
`async runAmbientComposition(roomDir, opts)`: for each id in AMBIENT_PRODUCER_IDS in order, check the budget (opts.budgetMs from the start; overrun -> 'skipped' for the rest), run `(opts.deps.adapters || PRODUCER_ADAPTERS)[id]` in try/catch ('error' on throw), record { outcome, posture: postureForCommand(PRODUCER_COMMANDS[id]).posture }; tally tier_counts over every stamp's verification; card = selectCardFinding(all findings); when card: `(opts.deps.measureAndGuard || measureAndGuardPair)(card.a, card.b, opts.deps)`; not ok -> outcome of the card's producer set to its reason when it is 'guard_not_cleared' or 'below_floor', surfaced_via 'none'; ok -> open the room db through the navigation write door, `db.exec('BEGIN')`, nodeId = fileStampedOpportunity(db, { a, b, stamp, producer, roomDir, runMode: 'ambient', reason: 'ambient stamped finding' }), `db.exec('COMMIT')` with no await in between (ROLLBACK and close on any throw), then writeStampedSideChannel(roomDir, { score, guard, a, b, stamp, opportunityHandle: nodeId }); success -> the card producer's outcome 'filed', surfaced_via 'sens13'. Return { producers, tier_counts, card: { opportunity_handle, producer, verification } or null, surfaced_via }. Never throws; every failure maps to an outcome enum.
Run tests/test-3551-ambient-run.cjs and the Task 2 regressions. Commit with `git commit --only lib/core/ambient-run.cjs`: `feat(355.1-07): stamped five-producer ambient composition, tier-gated card, proposed filing (AMB-03, AMB-05)`.
  </action>
  <verify>
    <automated>node tests/test-3551-ambient-run.cjs && node tests/test-355-filing.cjs && node tests/test-355-side-channel-v2.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-3551-ambient-run.cjs` exits 0 (or 77 only on its integration leg's documented skip condition, printed loudly) and prints `FAIL: 0`
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/ambient-run.cjs | grep -cE "brain-client|require\('zod'\)|fetch\(|INSERT INTO|url-ingest|autonomous_safe: true|posture: '(run|halt)'|review_status"` prints 0
    - `grep -c "postureForCommand" lib/core/ambient-run.cjs` prints at least 1 and `grep -c "CARD_TIERS" lib/core/ambient-run.cjs` prints at least 2
    - `grep -cP '\x{2014}' lib/core/ambient-run.cjs` prints 0
  </acceptance_criteria>
  <done>The background run can compose all five stamped producers and put at most one verified finding on the card as a proposed opportunity, with nothing unverified ever reaching it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child process -> Theo (via verification-stamp.cjs) | The only network wire in the ambient path |
| machine finding -> room.db (Part 9 chokepoint) | A background run mints a graph node |
| composition -> side channel -> SENS-13 | What the navigator will be offered next turn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-29 | Information disclosure | room text reaching Theo | mitigate | Only verification-stamp.cjs calls Theo, with resolved canon names; counting-callTool leg proves argument keys and values; static no-brain-client leg |
| T-3551-30 | Elevation of privilege | a machine finding born confirmed | mitigate | fileStampedOpportunity never passes review_status (born proposed); only gate_answer promotes (355-22) |
| T-3551-31 | Spoofing | an unverified finding shown as a card | mitigate | CARD_TIERS filter before selection; unverified-only leg asserts no filing and no side channel |
| T-3551-32 | Tampering | a second filing writer | mitigate | fileStampedOpportunity extracted from bankStatements and used by both; test-355-filing proves byte-identical output |
| T-3551-33 | Denial of service | Theo load from background runs | mitigate | AMBIENT_TOP_N cap per producer, run-level memo, the hourly throttle upstream, brain-client's null on outage |
| T-3551-34 | Denial of service | Theo awaited inside a write transaction | mitigate | All stamping and the guard complete before BEGIN; no await between BEGIN and COMMIT |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Module `lib/core/ambient-run.cjs` exports `runAmbientComposition(roomDir, opts)`, `selectCardFinding(findings)`, `memoizeCallTool(callTool)`, `PRODUCER_COMMANDS`, `PRODUCER_ADAPTERS`, `CARD_TIERS`, `AMBIENT_TOP_N`; opts keys `deps { adapters, callTool, measureAndGuard, now }`, `deltaState`, `budgetMs`; result keys `producers { <id>: { outcome, posture } }`, `tier_counts`, `card { opportunity_handle, producer, verification }`, `surfaced_via`
- `lib/core/eureka/eureka-reach-runner.cjs` export `measureAndGuardPair(a, b, opts)` (returns `{ ok, score, guard }` or `{ ok: false, reason }`)
- `scripts/eureka-portfolio-report.cjs` export `fileStampedOpportunity(db, { a, b, stamp, producer, roomDir, runMode, reason })`; opportunity props `engine_mode: 'ambient'`, reason `'ambient stamped finding'`
- Test `tests/test-3551-ambient-run.cjs`

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently. Re-read `lib/core/eureka/eureka-reach-runner.cjs` and `scripts/eureka-portfolio-report.cjs` from disk before editing; if `git status --short` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it. Commit with `git commit --only` on this plan's paths; no `state.*` writes while a peer executes. Nothing calls the composition yet; the child wiring waits for the checkpoint.

<verification>
- `node tests/test-3551-ambient-run.cjs`
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set within BASE_3551's)
</verification>

<success_criteria>
- Five stamped producers composed through their 355 seams, posture joined and recorded
- At most one strong-or-indirect finding filed proposed and put on the side channel; unverified never
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-07-SUMMARY.md` when done.
</output>
