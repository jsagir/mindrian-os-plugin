---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 06
type: execute
wave: 4
depends_on: ["355.1-02", "355.1-03", "355.1-05"]
files_modified:
  - tests/test-3551-evaluator.cjs
  - tests/test-3551-one-spawner.cjs
  - lib/core/ambient-trigger.cjs
autonomous: true
requirements: [AMB-01, AMB-03, AMB-04, AMB-08]
canon_parts: [8, 9, 11]
tags: [evaluator, machinery, detached-spawn, one-spawner, budget]

must_haves:
  truths:
    - "CONTEXT 'The split: detector, evaluator, card (locked)': lib/core/ambient-trigger.cjs is the evaluator and is NOT a sensor: it reads the room delta, computes the delta hash once, consults the ledger and lock, and spawns the existing Phase 117 fire child; it is never registered in SENSOR_REGISTRY and lives outside lib/core/sensors/"
    - "CONTEXT 'Trigger vocabulary and readers (AMB-01, locked)': the evaluator reads only through navigation.readRoomDeltaFacts over a read-only handle from navigation.openRoomDbReadOnlyForCaller (closed in finally), classifies with the SENS-21 module's pure classifyRoomDelta, and the delta hash is computed exactly once per evaluation"
    - "CONTEXT 'The run underneath (AMB-03, locked)': exactly one spawner of scripts/auto-explore-fire.cjs besides the existing fingerprint hook; no second spawner, no cadence-runner spawn, no third process type; the spawn is detached, stdio ignore, windowsHide, unref'd (research Pattern 3)"
    - "CONTEXT 'Tri-polar (AMB-06, locked)' and research C5: hook timeouts are seconds and async hooks are not bounded by the harness, so the evaluator bounds itself: AMBIENT_EVAL_BUDGET_MS is checked between steps and an over-budget evaluation spawns nothing; the evaluator never throws"
    - "CONTEXT 'Run ledger (AMB-04, locked)': the decision ladder is no change -> stop; same hash -> stop; in flight -> stop; throttled -> stop; locked -> stop; else claim (in_flight + runs_window) and write last-room-delta.json run_state 'started' BEFORE spawning; a spawn error rewrites run_state 'spawn_failed' so SENS-21 offers the honest fallback"
    - "CONTEXT 'Boundaries (AMB-08, locked)' / research Pitfall 9: argv carries only [firePath, roomDir, '', 'ambient-' + first 12 hex of the delta hash, sessionId or '', '--ambient']; no claim text, no artifact path, no slug beyond roomDir itself; stderr lines carry enums only"
    - "Pitfall 7: the ambient path never shares the Phase 117 daily cap and never emits auto_explore_fired"
  artifacts:
    - path: "lib/core/ambient-trigger.cjs"
      provides: "evaluateRoomDelta, evaluateAndClaim, evaluateAndMaybeSpawn, buildAmbientArgv, ambientIdFor, DECISIONS, AMBIENT_EVAL_BUDGET_MS"
      exports: ["evaluateRoomDelta", "evaluateAndClaim", "evaluateAndMaybeSpawn", "buildAmbientArgv", "ambientIdFor", "DECISIONS", "AMBIENT_EVAL_BUDGET_MS", "FIRE_SCRIPT_RELPATH", "_internal"]
    - path: "tests/test-3551-evaluator.cjs"
      provides: "decision ladder, hash-once, budget, argv shape, three-seams-one-claim legs"
      min_lines: 120
    - path: "tests/test-3551-one-spawner.cjs"
      provides: "pinned spawner set with a planted second-spawner negative control"
  key_links:
    - from: "lib/core/ambient-trigger.cjs"
      to: "lib/core/navigation.cjs readRoomDeltaFacts"
      via: "read-only handle, closed in finally"
      pattern: "readRoomDeltaFacts"
    - from: "lib/core/ambient-trigger.cjs"
      to: "scripts/auto-explore-fire.cjs"
      via: "detached spawn with --ambient"
      pattern: "--ambient"
    - from: "lib/core/ambient-trigger.cjs"
      to: "scripts/scout-cadence-guard.cjs shouldRunAmbient / claimAmbientRun / writeRoomDeltaState"
      via: "ledger ladder then claim then delta state"
      pattern: "claimAmbientRun"
---

<objective>
The machinery half of the split (AMB-01, AMB-03, AMB-04): one evaluator every surface calls (the CLI Stop hook and the MCP close-out, wired live in plan 355.1-14; the material-mode child in plan 355.1-09). It asks the room what changed, decides whether a run is due, claims it, and starts the one existing child in the background. It does not wire anything live: nothing calls it until the plans after the checkpoint.

Purpose: one decision ladder and one spawn site, bounded in time, content-free in argv.
Output: lib/core/ambient-trigger.cjs and two tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-RESEARCH.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-PATTERNS.md
@lib/core/navigation/room-delta-facts.cjs
@lib/core/sensors/sensor-room-delta.cjs
@scripts/scout-cadence-guard.cjs
@tests/helpers/fixture-room-3551.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED evaluator test and the one-spawner sweep</name>
  <files>tests/test-3551-evaluator.cjs, tests/test-3551-one-spawner.cjs</files>
  <read_first>
    - scripts/auto-explore-fingerprint.cjs lines 110-130 (dailyCapHit, which the ambient path must not share) and 325-350 (the existing detached spawn of the fire child)
    - lib/core/navigation.cjs (grep `openRoomDbReadOnlyForCaller` and the matching close export)
    - lib/core/room-path-containment.cjs (what it offers for validating a roomDir)
    - lib/mcp/tool-router.cjs lines 1540-1580 (the eureka-run detached spawn precedent on stdio: why stdout is never touched)
    - tests/test-267-*.cjs or any existing "pinned spawner set" grep test (`grep -rln "spawn(" tests/test-267* 2>/dev/null | head -3`) for the grep idiom research cites
    - lib/core/sensors/sensor-room-delta.cjs classifyRoomDelta and scripts/scout-cadence-guard.cjs ledger functions
  </read_first>
  <behavior>
    - with `_internal.setSpawnImpl(fakeSpawn)` capturing (cmd, args, opts) and returning a fake child with unref(): a buildDeltaRoom room with addClaims(5) -> evaluateAndMaybeSpawn(roomDir, { seam: 'stop_hook', sessionId: 's1', now }) returns decision 'spawned'; fakeSpawn saw cmd === process.execPath, args deep-equal [<abs scripts/auto-explore-fire.cjs>, roomDir, '', 'ambient-' + delta_hash.slice(0,12), 's1', '--ambient'], opts.detached === true, opts.stdio === 'ignore', opts.windowsHide === true; unref was called
    - after that spawn: last-room-delta.json validates with run_state 'started', seam 'stop_hook', classes ['a'], the same delta_hash; the ledger has in_flight.delta_hash equal to it
    - baseline: on a room with no claims and no artifacts but a readable stage, the first evaluation returns 'no_change' AND records the stage / contradicts / children baselines (recordAmbientBaseline); a later setStage('Investment') then evaluates as class c (so classes b, c and d are detectable in a room that has never run)
    - a second evaluateAndMaybeSpawn on the unchanged room returns 'in_flight' and spawns nothing; after recordAmbientRun for that hash, it returns 'same_hash'; with no claims and no artifacts it returns 'no_change'
    - three seams fired back to back on the same state ('stop_hook', 'closeout', and evaluateAndClaim with seam 'material' plus a materialId) produce exactly ONE claim and ONE spawn (fakeSpawn call count 1) (research Pitfall 6)
    - throttle: after a completed run in the same hour and a new delta, the decision is 'throttled'; a fresh lock file -> 'locked'
    - hash once: a spy on computeDeltaHash (through the sensor module's exported object, or a counting wrapper passed as opts.classifyFn) is called exactly once per evaluation
    - budget: with opts.readFacts replaced by a function that busy-waits past AMBIENT_EVAL_BUDGET_MS, the result is 'budget_exceeded', no claim is written and fakeSpawn is not called; the whole call returns within budget plus 500 ms
    - spawn failure: fakeSpawn throwing -> decision 'spawn_failed' and last-room-delta.json run_state 'spawn_failed' (the SENS-21 fallback input); sensorRoomDelta on that room now returns a SENS-21 reach
    - invalid roomDir (relative path, missing dir, '__no_room__', a path with a newline) -> decision 'no_room', nothing written
    - Part 8: no argv element and no written file contains the SECRET-3551 marker; process.stdout.write is never called during an evaluation (spy)
    - evaluateAndClaim never spawns (fakeSpawn call count stays 0) and returns { decision: 'claimed', delta_hash, classes } when due
    - one-spawner: scanning lib/, scripts/, hooks/ and bin/ for executable lines that reference `auto-explore-fire` together with spawn / fork / exec / execFile finds exactly the pinned set { scripts/auto-explore-fingerprint.cjs, lib/core/ambient-trigger.cjs }; lib/core/ambient-trigger.cjs spawns nothing else (no scout-cadence-runner, no discovery-cycle, no python)
    - one-spawner negative control: a scratch file under os.tmpdir() containing `spawn(process.execPath, [require.resolve('../scripts/auto-explore-fire.cjs')])` added to the scan list is reported as an unpinned spawner (PASS when caught); removed afterwards
  </behavior>
  <action>Write tests/test-3551-evaluator.cjs and tests/test-3551-one-spawner.cjs ('use strict', hygiene-355 preamble each, attempts() === 0 last; the evaluator test exits 77 with a loud message when node:sqlite is unavailable) implementing every behavior line. Use buildDeltaRoom rooms and injected `now` values. Run both; the evaluator test fails (module absent); the one-spawner test fails on the missing pinned file. Commit `test(355.1-06): add failing evaluator and one-spawner tests (AMB-01, AMB-03, AMB-04)`.</action>
  <verify>
    <automated>node tests/test-3551-evaluator.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Both tests exist and run; the evaluator test fails on the missing module
    - `grep -c "setSpawnImpl" tests/test-3551-evaluator.cjs` prints at least 1 and `grep -c "negative control" tests/test-3551-one-spawner.cjs` prints at least 1
  </acceptance_criteria>
  <done>Failing tests pin the ladder, the single spawn site, the budget and the argv shape.</done>
</task>

<task type="auto">
  <name>Task 2: lib/core/ambient-trigger.cjs</name>
  <files>lib/core/ambient-trigger.cjs</files>
  <read_first>
    - tests/test-3551-evaluator.cjs and tests/test-3551-one-spawner.cjs (the RED legs)
    - scripts/auto-explore-fingerprint.cjs lines 325-350 (spawn block to adapt) and lib/core/navigation.cjs read-only door exports
    - lib/core/sensors/sensor-room-delta.cjs and scripts/scout-cadence-guard.cjs (the functions this module composes)
    - 355.1-BASELINE.md (measured delta-read cost context: reads under 10 ms, navigation load about 55 ms per research item 3)
  </read_first>
  <action>
Create lib/core/ambient-trigger.cjs ('use strict', BSL header: "Phase 355.1 ambient trigger evaluator. NOT a sensor (Phase 144 fence): machinery that reads the room delta through the navigation chokepoint, consults the ambient ledger and lock, and starts the ONE existing Phase 117 fire child in --ambient mode. Never throws, never writes stdout (MCP stdio safety), bounds its own wall clock (hook timeouts are seconds and async hooks are unbounded, research C5). Canon Part 8: argv and every written file are closed-shape. Hyphens only."). Requires: node:fs, node:path, node:child_process (spawn), ./navigation.cjs, ./sensors/sensor-room-delta.cjs, ../../scripts/scout-cadence-guard.cjs, ./room-path-containment.cjs only if its API validates an absolute room dir (else validate locally). Constants: `AMBIENT_EVAL_BUDGET_MS = 1500` with a derivation comment (delta reads under 10 ms plus navigation load about 55 ms per research item 3; about 25 times headroom, far under the 5 s hook timeout); `FIRE_SCRIPT_RELPATH = path.join('scripts', 'auto-explore-fire.cjs')`; `DECISIONS = Object.freeze(['no_room', 'no_change', 'same_hash', 'in_flight', 'throttled', 'locked', 'ledger_corrupt', 'budget_exceeded', 'due', 'claimed', 'spawned', 'spawn_failed'])` ('due' is evaluateRoomDelta's verdict when a run should start; the claiming functions turn it into 'claimed' or 'spawned'). Functions:
`ambientIdFor(deltaHash)` -> 'ambient-' + deltaHash.slice(0, 12).
`buildAmbientArgv(firePath, roomDir, deltaHash, sessionId)` -> [firePath, roomDir, '', ambientIdFor(deltaHash), sessionId || '', '--ambient'].
`evaluateRoomDelta(roomDir, opts)` -> { decision, delta_hash, classes, next_watermarks }: validate roomDir (absolute, existing directory, not '__no_room__', no newline or NUL) else 'no_room'; read the ledger (readAmbientLedger); open the read-only handle, call `(opts.readFacts || navigation.readRoomDeltaFacts)(db, roomDir, { since: { claims_created_at, artifacts_created_at } from ledger.watermarks, registryHome: opts.registryHome })`, close in finally; budget check; `(opts.classifyFn || classifyRoomDelta)(facts, ledger.watermarks, { materialId: opts.materialId })` computed ONCE; unchanged -> record the b / c / d baselines through recordAmbientBaseline(roomDir, { watermarks: next_watermarks }) when the ledger still lacks them, then return 'no_change'; then shouldRunAmbient(roomDir, delta_hash, { now }) mapping its reason to the decision ('ok' -> 'due').
`evaluateAndClaim(roomDir, opts)`: evaluateRoomDelta; when due and within budget: claimAmbientRun(roomDir, { deltaHash, watermarks: next_watermarks, seam: opts.seam, now }) then writeRoomDeltaState(roomDir, { schema_version: 1, evaluated_at: ISO(now), seam, classes, delta_hash, run_state: 'started', material_id: opts.materialId || null, opportunity_handle: null, surfaced_via: null, framing: null }); returns { decision: 'claimed', delta_hash, classes }. Never spawns.
`evaluateAndMaybeSpawn(roomDir, opts)`: evaluateAndClaim; when claimed, spawn through the module-level `spawnImpl` (default child_process.spawn) with (process.execPath, buildAmbientArgv(<repo-root>/FIRE_SCRIPT_RELPATH, roomDir, delta_hash, opts.sessionId), { detached: true, stdio: 'ignore', windowsHide: true, env: process.env }) and child.unref(); a throw or a missing fire script -> writeRoomDeltaState with run_state 'spawn_failed' (same other fields) and decision 'spawn_failed'; success -> 'spawned'. Emit nothing to stdout; any diagnostic goes to stderr as `[ambient-trigger] <decision enum>` only when process.env.MINDRIAN_AMBIENT_DEBUG === '1'.
`_internal = { setSpawnImpl(fn), resetSpawnImpl() }` for in-process tests only.
Wrap every export body in try/catch returning { decision: 'no_room' } style values; never throw. Run both tests and the SENS-21 tests. Commit with `git commit --only lib/core/ambient-trigger.cjs`: `feat(355.1-06): ambient trigger evaluator - one ladder, one spawn site, self-bounded (AMB-01, AMB-03, AMB-04)`.
  </action>
  <verify>
    <automated>node tests/test-3551-evaluator.cjs && node tests/test-3551-one-spawner.cjs && node tests/test-3551-sensor-fires.cjs && node tests/test-3551-ledger.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command exits 0; the evaluator and one-spawner tests print `FAIL: 0`
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/ambient-trigger.cjs | grep -cE "process\.stdout|require\('zod'\)|fetch\(|node:sqlite|INSERT INTO|auto_explore_fired|dailyCapHit"` prints 0
    - `grep -c "'--ambient'" lib/core/ambient-trigger.cjs` prints 1 and `grep -c "detached: true" lib/core/ambient-trigger.cjs` prints 1
    - `grep -c "SENSOR_REGISTRY" lib/core/ambient-trigger.cjs` prints 0 and the file is not under lib/core/sensors/
    - `grep -cP '\x{2014}' lib/core/ambient-trigger.cjs` prints 0
  </acceptance_criteria>
  <done>One evaluator decides and starts the one child, never blocks, never leaks room bytes into argv, and is ready for the surfaces to call after the checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook / MCP host process -> detached child | A background process starts outside any turn |
| room state -> argv / stderr | What the OS process table and logs can see |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-23 | Denial of service | the Stop chain blocked by a slow evaluation | mitigate | AMBIENT_EVAL_BUDGET_MS checked between steps; over-budget spawns nothing; the CLI entry is async (plan 355.1-14) |
| T-3551-24 | Information disclosure | room content in argv (visible in ps) or stderr | mitigate | argv is roomDir, an empty string, a hash-derived id, the session id and a flag only; stderr is enum-only and off by default; marker leg over argv and files |
| T-3551-25 | Tampering | a rival spawner of the child | mitigate | tests/test-3551-one-spawner.cjs pins the spawner set with a planted negative control |
| T-3551-26 | Tampering | MCP stdio JSON-RPC corrupted by child output | mitigate | stdio 'ignore', detached, unref; stdout spy proves the evaluator never writes it |
| T-3551-27 | Denial of service | duplicate children from three seams | mitigate | Claim before spawn; in_flight and lock ladder; three-seams-one-claim leg |
| T-3551-28 | Tampering | a wrong or crafted roomDir | mitigate | Absolute existing directory only, reserved no-room sentinel refused, newline and NUL refused |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Module `lib/core/ambient-trigger.cjs` exports `evaluateRoomDelta(roomDir, opts)`, `evaluateAndClaim(roomDir, opts)`, `evaluateAndMaybeSpawn(roomDir, opts)`, `buildAmbientArgv(firePath, roomDir, deltaHash, sessionId)`, `ambientIdFor(deltaHash)`, `DECISIONS`, `AMBIENT_EVAL_BUDGET_MS`, `FIRE_SCRIPT_RELPATH`, `_internal.setSpawnImpl`, `_internal.resetSpawnImpl`
- opts keys `seam`, `sessionId`, `materialId`, `now`, `registryHome`, `readFacts`, `classifyFn`, `budgetMs`
- Decision enum values `no_room`, `no_change`, `same_hash`, `in_flight`, `throttled`, `locked`, `ledger_corrupt`, `budget_exceeded`, `due`, `claimed`, `spawned`, `spawn_failed`
- Fire-child argv contract `[firePath, roomDir, '', 'ambient-<12 hex>', sessionId, '--ambient']`; env `MINDRIAN_AMBIENT_DEBUG` (stderr enum diagnostics, off by default)
- Tests `tests/test-3551-evaluator.cjs`, `tests/test-3551-one-spawner.cjs`

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently. This plan creates one new module and edits no shared file; if any `git status --short` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it. Commit with `git commit --only` on this plan's paths; no `state.*` writes while a peer executes. Nothing calls the evaluator yet; the live wiring waits for the checkpoint (plan 355.1-08).

<verification>
- `node tests/test-3551-evaluator.cjs` and `node tests/test-3551-one-spawner.cjs`
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set within BASE_3551's)
</verification>

<success_criteria>
- One evaluator, one spawn site, a claim before every spawn, a bounded wall clock, closed-shape argv
- Three seams on one state produce one run
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-06-SUMMARY.md` when done.
</output>
