---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 08
type: execute
wave: 4
depends_on: ["355.1-03", "355.1-04", "355.1-05"]
files_modified:
  - .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-HOOKED-AUDIT.md
  - .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md
  - tests/test-3551-hooked-audit.cjs
  - lib/core/direction-convention.cjs
  - data/floor-ledger.json
  - lib/core/sensors/sensor-room-delta.cjs
  - scripts/scout-cadence-guard.cjs
autonomous: false
requirements: [AMB-05, AMB-04, AMB-07, AMB-01]
canon_parts: [3, 6, 8, 9, 12]
tags: [checkpoint, hooked-audit, floors, framing-phrases, navigator-rulings]

must_haves:
  truths:
    - "CONTEXT 'Execution constraints (locked)': this is the ONE human checkpoint of the phase: the navigator approves the Hooked audit, the two floors (N = 5 claims; 1 run per room per hour), the three framing phrases (the PWS author's confirmation, role only) and the six rulings, BEFORE any plan wires the child live (plans 355.1-09 through 355.1-14 depend on it, directly or through each other)"
    - "CONTEXT 'Surfacing and the Hooked lens (AMB-05, locked)': 355.1-HOOKED-AUDIT.md maps internal trigger (the delta), action (one card), variable reward (the stamp tier), investment (the gate answer that writes the edge the next run reads); it is a clean filed deliverable with placeholders where the navigator rules, never conversation banter"
    - "CONTEXT 'Surfacing and the Hooked lens': the audit states the honest limit plainly: only 14 percent of live artifacts (472 of 3,333) carry the framework: or methodology: frontmatter a stamp needs, so most real-room findings will stamp unverified and never reach the card until rooms carry that frontmatter; the variable reward is sparse by design"
    - "Canon Part 12: the audit and every phrase it proposes carry no praise, no grade and no count of findings for Larry's line"
    - "Navigator Ruling 4 recorded in the audit: the AI-SPEC's two trigger moments shape framing only; 'not on every turn' is satisfied by the delta plus the hourly throttle, not by gating on the moments"
    - "Navigator Ruling 1 disclosed at the checkpoint: a child started by the MCP server on Desktop or Cowork stamps through brain-client directly (a refinement of 355 D-50), and on a keyless machine that background child may perform the one-shot silent registration"
    - "Navigator Ruling 6: FRAMING_CONFIRMED becomes Object.freeze({ by: 'pws-author', at, framing_hash }) only after the PWS author's confirmation relayed at the checkpoint, and framing_hash equals framingHash() at that moment; the floor-ledger rows' gates text records the approval date"
    - "An overturned ruling is recorded and STOPS the phase for replanning of plans 355.1-09 to 355.1-14; changed floor values or phrases are applied in Task 3 directly"
  artifacts:
    - path: ".planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-HOOKED-AUDIT.md"
      provides: "The Hooked-model audit of the ambient trigger (the mandatory first-step lens)"
      contains: "14 percent"
    - path: ".planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md"
      provides: "The decision sheet and, after Task 2, the navigator's recorded ruling"
      contains: "## Navigator ruling"
    - path: "tests/test-3551-hooked-audit.cjs"
      provides: "audit structure, disclosure, no-praise and approval-record legs"
  key_links:
    - from: "lib/core/direction-convention.cjs FRAMING_CONFIRMED.framing_hash"
      to: "framingHash()"
      via: "equality leg in tests/test-3551-hooked-audit.cjs"
      pattern: "framing_hash"
---

<objective>
The single human decision of Phase 355.1 (AC6). The Hooked model is the mandatory lens for the first step of any Mindrian surface, and this phase changes the first step itself: the room, not the user, starts the breakthrough run. Task 1 files the Hooked audit and a one-page decision sheet; Task 2 is the navigator's ruling (with the PWS author's confirmation of the framing phrases relayed); Task 3 records it in code and data. Only after this plan closes may any plan wire the child live.

Output: 355.1-HOOKED-AUDIT.md, 355.1-CHECKPOINT.md, the confirmed framing record, approved floor rows, one test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-PRD.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-RESEARCH.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-BASELINE.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PHRASE-CONFIRMATION.md
@lib/core/direction-convention.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: File the Hooked audit, the decision sheet and the audit test</name>
  <files>.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-HOOKED-AUDIT.md, .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md, tests/test-3551-hooked-audit.cjs</files>
  <read_first>
    - ~/.claude/skills/hooked-model/SKILL.md (the lens: trigger, action, variable reward, investment; its audit questions)
    - 355.1-CONTEXT.md sections 'Surfacing and the Hooked lens', 'The navigator's six rulings', 'Run ledger', 'Strategy, not keywords', and `<specifics>` (the navigator's words)
    - 355.1-RESEARCH.md C9, C10, C12, items 8 and 9, Open Questions 1-6, Assumptions A1-A6
    - 355-AI-SPEC.md (grep "two moments" around line 106) and 355-PHRASE-CONFIRMATION.md (the record shape to mirror)
    - lib/core/direction-convention.cjs FRAMING_PHRASES and `node -e "console.log(require('./lib/core/direction-convention.cjs').framingHash())"`
    - data/floor-ledger.json rows sensor-room-delta.CLAIM_FLOOR_N and scout-cadence-guard.AMBIENT_MAX_RUNS_PER_HOUR
  </read_first>
  <action>
Write 355.1-HOOKED-AUDIT.md (Feynman prose, hyphens only, no em-dash, no praise words, no counts of findings in any proposed Larry line; a clean deliverable with `[NAVIGATOR RULING: ...]` placeholders wherever the navigator decides, never conversation banter). Headings in order: `## What changes for the navigator` (one paragraph: the room starts the run, one card may arrive on the next turn, nothing to type); `## Trigger` (internal trigger = a room delta: five classes, N = 5 claims, a CONTRADICTS edge surfaced, a stage change, a sub-room, a filed artifact; no external trigger: no notification outside the card, a deferred idea; 'not on every turn' held by the delta plus the hourly throttle, Ruling 4: the AI-SPEC's two moments frame, they never gate); `## Action` (one card, once per delta hash, one gate answer; Ruling 5: on a filed artifact the stamped card wins and the Phase 117 card stands down; on a surface where the run could not start, SENS-21's single fallback offer); `## Variable reward` (the stamp tier: strong or indirect, shown with its path; unverified never shown; never a score, never a count; the honest limit: only 14 percent of live artifacts (472 of 3,333) carry the framework: or methodology: frontmatter a stamp needs, so most real-room findings will stamp unverified and never reach the card until rooms carry that frontmatter; the reward is sparse by design and the audit says so); `## Investment` (the navigator's gate answer: approve confirms the proposed opportunity, reject writes a REJECTED_BECAUSE edge; either writes graph state the next run reads); `## Framing by problem type` (the three proposed phrases verbatim with their mapping; most live rooms land on neutral today); `## Surfaces` (CLI async Stop entry plus the fingerprint; Desktop and Cowork through stop_gate_check close-out; Ruling 1 disclosure including the keyless one-shot registration; Ruling 2 fire-once at close-out); `## Canon Part 12 check` (no praise, no grade, no count in Larry's line); `## Open risks` (A1-A3 from research: Cowork server locality, Desktop child sandboxing, missing python on user machines degrade to SENS-21's fallback).
Write 355.1-CHECKPOINT.md, the decision sheet: a numbered list of exactly ten items, each with the proposal, its basis in one line, and `APPROVE / CHANGE TO: ___ / OVERTURN` placeholders: (1) the Hooked audit as filed; (2) claim floor N = 5 (measured median claims per active room-hour, p50 5, across 31 rooms over 90 days); (3) throttle 1 run per room per hour (child p90 about 12 s; bounded Theo load); (4) the three framing phrases verbatim with the current framingHash(), for the PWS author's confirmation (role only, never a name); (5)-(10) Rulings 1 to 6 verbatim from CONTEXT. End with an empty `## Navigator ruling` heading for Task 3 to fill.
Write tests/test-3551-hooked-audit.cjs ('use strict', hygiene-355 preamble): the audit has the nine headings in order; contains '14 percent' and '472' and '3,333'; contains no U+2014; contains none of the words great, amazing, excellent, brilliant, breakthrough, validated, proven next to the word finding; the checkpoint sheet has items 1-10; approval legs: when the `## Navigator ruling` section is empty, print `SKIP (awaiting the 355.1 checkpoint)` for those legs and exit 77 overall only if nothing else failed; when filled, assert a dated `**Approved:**` line, that direction-convention's FRAMING_CONFIRMED is a frozen object with by 'pws-author' and framing_hash === framingHash(), that phraseHash() still equals PHRASES_CONFIRMED.phrase_hash, and that both floor rows' gates text contains 'approved'.
Commit with `git add -f` on the two .planning files and `git commit --only` on the three paths: `docs(355.1-08): Hooked audit and the single checkpoint decision sheet (AMB-05)`.
  </action>
  <verify>
    <automated>node tests/test-3551-hooked-audit.cjs; test $? -eq 0 -o $? -eq 77</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## " .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-HOOKED-AUDIT.md` prints 9
    - `grep -c "14 percent" .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-HOOKED-AUDIT.md` prints at least 1
    - `grep -cE "^[0-9]+\. " .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md` prints 10
    - `grep -cP '\x{2014}' .planning/phases/355.1-*/355.1-HOOKED-AUDIT.md .planning/phases/355.1-*/355.1-CHECKPOINT.md` prints 0 for each
    - `node tests/test-3551-hooked-audit.cjs` exits 77 (awaiting the ruling) with every structural leg passing
  </acceptance_criteria>
  <done>The navigator has one clean audit and one ten-item sheet to rule on.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: The navigator rules on the Hooked audit, the two floors, the framing phrases and the six rulings</name>
  <files>.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md</files>
  <read_first>
    - .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-HOOKED-AUDIT.md
    - .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md
  </read_first>
  <action>STOP and present the decision sheet to the navigator, one screen: the audit's path, then items 1-10 with their proposals. The navigator reads the audit and answers each item APPROVE, CHANGE TO <value or wording>, or OVERTURN, and relays the PWS author's confirmation or replacement wording for item 4 (the PWS author is named by role only in every record). Do not continue until the navigator answers. This is a decision, not a labeling sitting; nothing in the repo changes during this task.</action>
  <what-built>355.1-HOOKED-AUDIT.md and the ten-item 355.1-CHECKPOINT.md decision sheet (Task 1). Plans 01-07 are machinery only; nothing is wired live yet.</what-built>
  <how-to-verify>
    1. Read .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-HOOKED-AUDIT.md top to bottom (nine sections).
    2. For item 1, confirm the audit reads as a clean deliverable and states the 14 percent limit.
    3. For items 2 and 3, confirm or change N = 5 and 1 run per room per hour.
    4. For item 4, relay the PWS author's confirmation of the three framing phrases, or the replacement wording.
    5. For items 5-10, approve or overturn each of Rulings 1-6.
  </how-to-verify>
  <resume-signal>Type "approved" for all ten, or list per item: "N: APPROVE | CHANGE TO ... | OVERTURN"</resume-signal>
  <verify>
    <automated>test -f .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-HOOKED-AUDIT.md && test -f .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md</automated>
  </verify>
  <acceptance_criteria>
    - The navigator's answer covers all ten items (a bare "approved" covers all ten)
    - No file in the repo changed between the Task 1 commit and the navigator's answer (`git diff --stat HEAD` shows no 355.1-owned path)
  </acceptance_criteria>
  <done>The navigator has ruled on every item.</done>
</task>

<task type="auto">
  <name>Task 3: Record the ruling in code, data and the checkpoint record</name>
  <files>.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md, lib/core/direction-convention.cjs, data/floor-ledger.json, lib/core/sensors/sensor-room-delta.cjs, scripts/scout-cadence-guard.cjs</files>
  <read_first>
    - the navigator's answer from Task 2
    - lib/core/direction-convention.cjs (PHRASES_CONFIRMED block, the shape to mirror), data/floor-ledger.json (the two 355.1 rows); re-read each from disk; `git status --short` on each file this task edits must be empty
    - tests/test-3551-hooked-audit.cjs (the approval legs)
  </read_first>
  <action>
Fill `## Navigator ruling` in 355.1-CHECKPOINT.md with the date, `**Approved:** <date>`, and the answer per item verbatim in substance (role names only).
If ANY of Rulings 1-6 (items 5-10) was overturned: record it, commit the checkpoint file only, and STOP the phase with the report "Ruling <n> overturned at the 355.1 checkpoint; plans 355.1-09 to 355.1-14 need revision (/gsd-plan-phase 355.1 --reviews) before execution continues". Do not touch code in that case.
Otherwise: item 4: if the PWS author replaced any phrase, update FRAMING_PHRASES to the exact wording and re-run tests/test-3551-framing.cjs (the phrase gates must still pass; if a replacement fails a gate, record the failing gate in the checkpoint file and ask the navigator for wording that passes); then replace `FRAMING_CONFIRMED = null` with `Object.freeze({ by: 'pws-author', at: '<date>', framing_hash: '<framingHash() now>' })` plus a comment block mirroring PHRASES_CONFIRMED's (Phase 355.1-08, Ruling 6, the full record path). Items 2 and 3: if changed, update the constant (CLAIM_FLOOR_N in lib/core/sensors/sensor-room-delta.cjs, AMBIENT_MAX_RUNS_PER_HOUR in scripts/scout-cadence-guard.cjs) and the row's value together; in both rows replace 'approval recorded at the 355.1 checkpoint' in gates with 'approved at the 355.1 checkpoint <date>'. Leave the two constant files untouched when the values were approved as proposed (list them in the commit only if edited).
Run tests/test-3551-hooked-audit.cjs (now exits 0, approval legs PASS), tests/test-3551-framing.cjs, tests/test-3551-classify.cjs, tests/test-3551-ledger.cjs, tests/test-355-direction-convention.cjs, `node scripts/check-floor-ledger.cjs --check`. Commit with `git add -f` on the checkpoint file and `git commit --only` on exactly the files changed: `docs(355.1-08): navigator ruling recorded - framing confirmed, floors approved (AMB-04, AMB-05, AMB-07)`.
  </action>
  <verify>
    <automated>node tests/test-3551-hooked-audit.cjs && node tests/test-3551-framing.cjs && node scripts/check-floor-ledger.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-3551-hooked-audit.cjs` exits 0 with the approval legs passing
    - `grep -c "FRAMING_CONFIRMED = null" lib/core/direction-convention.cjs` prints 0 and `node -e "const d=require('./lib/core/direction-convention.cjs');if(!d.FRAMING_CONFIRMED||d.FRAMING_CONFIRMED.framing_hash!==d.framingHash()||d.FRAMING_CONFIRMED.by!=='pws-author')process.exit(1)"` exits 0
    - `grep -c "approved at the 355.1 checkpoint" data/floor-ledger.json` prints 2
    - `grep -c "\*\*Approved:\*\*" .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md` prints 1
  </acceptance_criteria>
  <done>The ruling lives in code and data: phrases confirmed by hash, floors approved, rulings recorded; the child may now be wired live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator ruling -> code and data | A human decision becomes a hash-pinned constant and approved ledger rows |
| audit file -> future readers | A filed deliverable others will cite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-35 | Repudiation | wiring the child live without the navigator's approval | mitigate | autonomous false; plans 09-13 depend on this plan; the audit test's approval legs gate on a dated record |
| T-3551-36 | Tampering | phrases edited after the author's confirmation | mitigate | FRAMING_CONFIRMED.framing_hash must equal framingHash(); any later edit fails tests/test-3551-hooked-audit.cjs |
| T-3551-37 | Information disclosure | a personal name in a tracked record | mitigate | Role names only ('pws-author', 'navigator') per the repo naming rule |
| T-3551-38 | Tampering | proceeding past an overturned ruling | mitigate | Task 3 records the overturn and stops the phase for replanning |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Record `.planning/phases/355.1-.../355.1-HOOKED-AUDIT.md` (headings What changes for the navigator, Trigger, Action, Variable reward, Investment, Framing by problem type, Surfaces, Canon Part 12 check, Open risks)
- Record `.planning/phases/355.1-.../355.1-CHECKPOINT.md` (ten items, `## Navigator ruling`, `**Approved:**` line)
- `lib/core/direction-convention.cjs` `FRAMING_CONFIRMED = Object.freeze({ by, at, framing_hash })`
- Floor-ledger rows `sensor-room-delta.CLAIM_FLOOR_N` and `scout-cadence-guard.AMBIENT_MAX_RUNS_PER_HOUR` gates text marked approved
- Test `tests/test-3551-hooked-audit.cjs`

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently. Re-read every file Task 3 edits from disk first; if `git status --short` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it. Commit with `git commit --only` on exactly the changed paths; `git add -f` for `.planning/phases/**`; no `state.*` writes while a peer executes.

<verification>
- `node tests/test-3551-hooked-audit.cjs`
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set within BASE_3551's)
</verification>

<success_criteria>
- AC6: the Hooked audit is filed and approved at the single checkpoint before the child is wired live
- Framing phrases confirmed by hash; both floors approved and disclosed; six rulings recorded
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-08-SUMMARY.md` when done.
</output>
