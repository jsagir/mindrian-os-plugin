---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 03
type: execute
wave: 2
depends_on: ["355.1-01"]
files_modified:
  - tests/test-3551-classify.cjs
  - tests/test-3551-lockstep.cjs
  - tests/test-3551-sensor-fires.cjs
  - lib/core/sensors/sensor-room-delta.cjs
  - lib/core/insight-sensors.cjs
  - lib/core/sensors/sensor-priority.cjs
  - data/floor-ledger.json
autonomous: true
requirements: [AMB-02, AMB-01]
canon_parts: [3, 8, 11, 12]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["lib/core/insight-sensors.cjs SENSOR_REGISTRY", "lib/core/sensors/sensor-priority.cjs SENS_PRIORITY", "lib/core/sensors/sensor-types.cjs makeReach"]
  gate_impact: "Registers one detector SENS-21 on the EXISTING context_block reach at posture hold (Group B); no seventh reach, no new command, no new MCP tool; build-connector-registry --check sensorPriorityCompletenessErrors must stay green."
  explanation: "Phase 355.1 adds the room-delta detector to the one governed sensor spine in all six gated lockstep places; place 7 (a decide() ctx producer) is deliberately absent because the sensor reads a room file, which is also what makes it fire on the MCP path."
tags: [sensor, lockstep, sens-21, delta-hash, floor-ledger]

must_haves:
  truths:
    - "CONTEXT 'Research corrections applied (locked)': the detector is SENS-21 (Phase 345 holds SENS-20 as sensorStrategyReach); the module header records the correction the way 345 recorded its own; the id is re-grepped immediately before the registration commit"
    - "CONTEXT 'The split: detector, evaluator, card (locked)': lib/core/sensors/sensor-room-delta.cjs is a pure detector: it produces a candidate reach only, never routes, never executes, never writes, is never async; it rides the EXISTING context_block reach at posture hold, SENS_PRIORITY Group B (deep_research rejected: suppressed in turns 1-2)"
    - "AMB-02 reading stated: posture 'hold' is the reach posture (POSTURE_IDS has no autonomous value); 'autonomous_safe joined from recipe-maps' applies to the run's producer steps (plan 355.1-07), never to this reach"
    - "CONTEXT 'The split': SENS-21 fires ONLY when <room>/.mindrian/last-room-delta.json is fresh, schema_version 1, and run_state is spawn_failed or deps_missing (the honest fallback offer); started, running and completed are silent because SENS-13 carries the card in the normal path"
    - "CONTEXT 'Trigger vocabulary and readers (AMB-01, locked)': classifyRoomDelta(facts, watermarks, opts) and computeDeltaHash(lines) are pure (no I/O, no clock); the delta hash is sha256 over the sorted delta-fact lines, computed once per evaluation; the same room state hashes the same way twice"
    - "CONTEXT 'Run ledger (AMB-04, locked)': the claim floor N = 5 is the named constant CLAIM_FLOOR_N, disclosed as a data/floor-ledger.json row (kind floor, status disclosed), never a bare literal; its approval is recorded at the single checkpoint (plan 355.1-08)"
    - "Pattern 1 lockstep: places 1-6 land in one commit (new file, require, SENSOR_REGISTRY append, SENSOR_REGISTRY_IDS append at the SAME index, module.exports append, SENS_PRIORITY record with explicit optimizes / watched_by / why); place 7 is deliberately absent"
    - "Pitfall 2: SENS-21 is proven to fire through the real MCP pull path (lib/mcp/tools/sensors.cjs dispatchCandidateReaches) and through decide(), never only by calling sensorRoomDelta directly"
  artifacts:
    - path: "lib/core/sensors/sensor-room-delta.cjs"
      provides: "SENS-21 detector plus the pure delta classifier and hash"
      exports: ["sensorRoomDelta", "classifyRoomDelta", "computeDeltaHash", "SENSOR_ID", "REACH_ID", "POSTURE", "SIGNAL", "DISPATCH", "CLAIM_FLOOR_N", "DELTA_CLASSES", "SEAMS", "RUN_STATES", "DEGRADE_STATES", "SURFACED_VIA", "DELTA_STATE_RELPATH", "DELTA_STATE_SCHEMA_VERSION", "ROOM_DELTA_FRESHNESS_MS"]
      contains: "SENSOR_ID = 'SENS-21'"
    - path: "tests/test-3551-lockstep.cjs"
      provides: "places 1-6 asserted, same-index check, Group B placement"
    - path: "tests/test-3551-sensor-fires.cjs"
      provides: "fallback-only fire through dispatchSensors, the MCP pull and decide()"
  key_links:
    - from: "lib/core/insight-sensors.cjs SENSOR_REGISTRY_IDS"
      to: "lib/core/sensors/sensor-room-delta.cjs SENSOR_ID"
      via: "same-index append; stampSensorId stamps by index"
      pattern: "'SENS-21'"
    - from: "data/floor-ledger.json"
      to: "lib/core/sensors/sensor-room-delta.cjs CLAIM_FLOOR_N"
      via: "row line_anchor CLAIM_FLOOR_N, check-floor-ledger stale-anchor check"
      pattern: "CLAIM_FLOOR_N"
---

<objective>
AMB-02 and the pure half of AMB-01: one detector, SENS-21, registered in every gated lockstep place, riding the existing context_block reach; plus the pure classifier and delta hash the evaluator (plan 355.1-06) and the child (plan 355.1-09) share. The detector is the honest fallback: it speaks only when a delta was seen and the background run could not start on this surface. In the normal path SENS-13 carries the card, so the navigator sees one card per delta, not two.

Purpose: the room's change is classified and hashed one way everywhere, and the sensor spine gains a detector without a seventh reach, a dark sensor, or a decide()-only producer.
Output: the sensor module, lockstep places 2-6, the claim-floor ledger row, three tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-RESEARCH.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-PATTERNS.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-BASELINE.md
@tests/helpers/fixture-room-3551.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for the pure classifier and hash, the lockstep, and fallback-only firing</name>
  <files>tests/test-3551-classify.cjs, tests/test-3551-lockstep.cjs, tests/test-3551-sensor-fires.cjs</files>
  <read_first>
    - tests/test-345-lockstep.cjs (whole file: the six-place lockstep test to mirror, same-function-object check)
    - lib/core/sensors/sensor-eureka.cjs (whole file: header shape, fail-closed REACH_ID check, readJsonSafe and isFreshFile with the WR-01 future-mtime guard, EUREKA_SIGNAL_FRESHNESS_MS, the try/catch-null body)
    - lib/mcp/tools/sensors.cjs lines 55-100 (buildSensorInputs, dispatchCandidateReaches and how `_internal` exposes it)
    - an existing test that calls decide() with a real roomDir (`grep -rln "navigation-engine" tests | head` then pick the one with the smallest ctx) for the minimal decide() context shape
    - tests/test-sensors-routing-fence.cjs and tests/test-sensors-part8-sweep.cjs (what they forbid in every lib/core/sensors/*.cjs)
    - 355.1-RESEARCH.md "Code Examples" (delta hash shape) and Pattern 2
  </read_first>
  <behavior>
    - classify: with watermarks {} (first evaluation) and facts claims { count: 5, max_created_at: 1000 }, artifacts null, contradicts_keys ['aaa...'], stage_hash 'bbb...', children ['ccc...']: classes === ['a'] (b, c, d need a prior watermark; a first evaluation sets their baseline only)
    - classify: claims.count 4 with claimFloor 5 -> no class a; claims.count 5 -> class a; line 'a:1000'
    - classify: watermarks.contradicts_keys ['k1'] and facts ['k1','k2'] -> class b with line 'b:k2' only
    - classify: watermarks.stage 's1' and facts stage_hash 's2' -> class c; facts stage_hash null -> no class c (unreadable STATE.md means no change)
    - classify: watermarks.children ['c1'] and facts ['c1','c2'] -> class d with line 'd:c2'
    - classify: artifacts.count 1 -> class e; opts.materialId 'abc123ef' adds line 'e:m:abc123ef' and class e even when artifacts is null
    - classify returns { changed, classes (sorted, unique), delta_hash (64 hex or null when unchanged), lines (sorted), next_watermarks } and next_watermarks carries every current fact (claims_created_at, artifacts_created_at, contradicts_keys, stage, children), keeping the prior value where the fact is null
    - computeDeltaHash(['b:x','a:1']) === computeDeltaHash(['a:1','b:x']); two classifications of the same facts give the same delta_hash; changing one fact changes it; the classifier source contains no `Date.now`, `new Date` or `Math.random` on an executable line
    - lockstep: sensor file exists with SENSOR_ID 'SENS-21'; insight-sensors.cjs requires it; SENSOR_REGISTRY and SENSOR_REGISTRY_IDS have equal length and SENS-21 sits at the same index as sensorRoomDelta; module.exports.sensorRoomDelta is the same function object; SENS_PRIORITY has exactly one SENS-21 record with non-empty optimizes, watched_by and why, placed in Group B immediately after SENS-13; REACH_ID is in REACH_IDS; sensorRoomDelta.constructor.name !== 'AsyncFunction'
    - fires: on a buildDeltaRoom room with a fresh last-room-delta.json { schema_version 1, run_state 'spawn_failed', classes ['a'] ... } the sensor returns a reach with reach_id 'context_block', posture 'hold', signal 'room_delta', dispatch 'auto-explore', evidence { run_state: 'spawn_failed', classes: 'a' }; same for 'deps_missing'
    - fires: run_state 'started', 'running' or 'completed' -> null; a stale file (mtime older than ROOM_DELTA_FRESHNESS_MS), a future mtime, a corrupt file, schema_version 2, or no file -> null; the sensor call writes no file (fs write spy) and never throws on a non-object ctx
    - fires through the MCP pull: `require('lib/mcp/tools/sensors.cjs')._internal.dispatchCandidateReaches(sessionId, roomDir, {})` returns a reach stamped SENS-21 for the degrade fixture and none for the completed fixture
    - fires through decide(): a decide() call with ctx.roomDir on the degrade fixture includes a SENS-21 reach among its fired sensor reaches (turn count 1: context_block is never turn-gated)
    - evidence values are enums only: no value contains the SECRET-3551 marker, a slug or a path separator
  </behavior>
  <action>Write the three tests ('use strict', hygiene-355 preamble each: scrubVendorKey and installNetGuard before any repo require, attempts() === 0 last). test-3551-classify.cjs requires only the sensor module and drives classifyRoomDelta / computeDeltaHash with literal fact objects (no db). test-3551-lockstep.cjs mirrors tests/test-345-lockstep.cjs for SENS-21 (places 1-6) plus the Group B position check. test-3551-sensor-fires.cjs builds rooms with buildDeltaRoom, writes last-room-delta.json fixtures directly with fs (the writer is plan 355.1-05's), sets mtimes with fs.utimesSync, and drives the sensor, the MCP pull and decide(). Run all three; they fail (module absent). Commit `test(355.1-03): add failing SENS-21 classifier, lockstep and fallback-fire tests (AMB-01, AMB-02)`.</action>
  <verify>
    <automated>for t in tests/test-3551-classify.cjs tests/test-3551-lockstep.cjs tests/test-3551-sensor-fires.cjs; do node "$t"; test $? -ne 0 || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - All three test files exist, run, and fail on the missing module
    - `grep -c "dispatchCandidateReaches" tests/test-3551-sensor-fires.cjs` prints at least 1 and `grep -c "decide(" tests/test-3551-sensor-fires.cjs` prints at least 1
    - `grep -c "SENS-13" tests/test-3551-lockstep.cjs` prints at least 1 (the Group B neighbor check)
  </acceptance_criteria>
  <done>Failing tests pin the classifier, the hash, the six lockstep places and the fallback-only firing on both surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: sensor-room-delta.cjs, lockstep places 2-6 in one commit, and the claim-floor ledger row</name>
  <files>lib/core/sensors/sensor-room-delta.cjs, lib/core/insight-sensors.cjs, lib/core/sensors/sensor-priority.cjs, data/floor-ledger.json</files>
  <read_first>
    - lib/core/insight-sensors.cjs and lib/core/sensors/sensor-priority.cjs: re-read both from disk NOW (peer-touched); `git status --short` on each must be empty before editing (else a peer is editing: wait, re-read, never stage or revert); then `grep -n "SENS-20\|sensorStrategyReach" lib/core/insight-sensors.cjs` for the four 345 anchors (require, SENSOR_REGISTRY, SENSOR_REGISTRY_IDS, module.exports) and `grep -n "id: 'SENS-13'" lib/core/sensors/sensor-priority.cjs`
    - lib/core/sensors/sensor-eureka.cjs (the shape to copy) and lib/core/sensors/sensor-types.cjs (makeReach, REACH_IDS, POSTURE_IDS)
    - data/floor-ledger.json (one existing row, e.g. rs-engine.DEFAULT_THRESHOLD, for the exact field order) and scripts/check-floor-ledger.cjs lines 228-310 (REQUIRED_FIELDS, VALID_KINDS, VALID_STATUSES, the stale-anchor check)
    - the three RED tests from Task 1
  </read_first>
  <action>
Re-grep the id first: `grep -rn "SENS-21" lib data commands skills agents scripts` must print nothing but this plan's own test files; if a peer took it, stop and report.
Create lib/core/sensors/sensor-room-delta.cjs ('use strict', BSL header modeled on sensor-eureka.cjs :1-53: sensor id and why it is free, the SENS-20 -> SENS-21 correction ("the PRD and ROADMAP title say SENS-20; Phase 345 holds SENS-20 as sensorStrategyReach; registered id SENS-21, Phase 355.1"), FROZEN REACH paragraph (context_block, never deep_research because deep_research is suppressed in turns 1-2), POSTURE 'hold' with the AMB-02 reading, the fallback-only firing rule, Part 8 enum-only evidence, FRESHNESS replicated not imported, "pure / sync / soft-fail", "hyphens only"). Requires only node:fs, node:path, node:crypto and ./sensor-types.cjs (never insight-sensors.cjs, circular). Constants: `SENSOR_ID = 'SENS-21'`, `REACH_ID = 'context_block'` with the fail-closed REACH_IDS check, `POSTURE = 'hold'`, `SIGNAL = 'room_delta'`, `DISPATCH = 'auto-explore'`, `CLAIM_FLOOR_N = 5`, `DELTA_CLASSES = Object.freeze(['a','b','c','d','e'])`, `SEAMS = Object.freeze(['stop_hook','closeout','material'])`, `RUN_STATES = Object.freeze(['started','running','completed','spawn_failed','deps_missing'])`, `DEGRADE_STATES = Object.freeze(['spawn_failed','deps_missing'])`, `SURFACED_VIA = Object.freeze(['sens13','none'])`, `DELTA_STATE_RELPATH = path.join('.mindrian', 'last-room-delta.json')`, `DELTA_STATE_SCHEMA_VERSION = 1`, `ROOM_DELTA_FRESHNESS_MS = 30 * 60 * 1000` (replicated from sensor-eureka's EUREKA_SIGNAL_FRESHNESS_MS with a comment). Pure functions: `computeDeltaHash(lines)` (copy, sort, join with newline, sha256 hex); `classifyRoomDelta(facts, watermarks, opts)` implementing exactly Task 1's classify behavior (opts.claimFloor defaults to CLAIM_FLOOR_N; opts.materialId accepted only when it matches /^[0-9a-f]{8,64}$/). Sensor: `sensorRoomDelta(turn, tuple, ctx)` with the sensor-eureka body shape (try/catch null, ctx.roomDir guard, stat-before-parse with the replicated readJsonSafe and isFreshFile, schema_version check, run_state in DEGRADE_STATES) returning makeReach({ reach_id: REACH_ID, posture: POSTURE, dispatch: DISPATCH, companions: [], signal: SIGNAL, evidence: { run_state, classes: <classes filtered to DELTA_CLASSES, joined with '|'> } }). Export every name in this plan's artifacts list.
insight-sensors.cjs (one hunk per place, each with the comment "Phase 355.1 detector (SENS-21 -- room delta -> honest fallback offer when the ambient run could not start):"): place 2 require after the last sensor require (the 345 require today); place 3 append `sensorRoomDelta,` at the END of SENSOR_REGISTRY (after `sensorStrategyReach,` today); place 4 append `'SENS-21',        // sensorRoomDelta` at the END of SENSOR_REGISTRY_IDS (after `'SENS-20',` today), so both arrays keep equal length and matching indices even if a peer appended a sensor since planning; place 5 append `sensorRoomDelta: sensorRoomDelta,` in module.exports. No decide() ctx producer (place 7 deliberately absent; say so in the require comment).
sensor-priority.cjs: add to the header's sensor list a line for SENS-21 and insert into Group B immediately after the SENS-13 record: Object.freeze({ id: 'SENS-21', optimizes: 'the count of room-delta fallback offers made when the ambient run could not start on this surface', watched_by: 'the count of fallback offers after which the navigator runs the fallback or moves to a surface where the run starts on its own', why: 'a fallback offer that fires on every failed start and is never acted on is noise about plumbing, not a finding' }).
data/floor-ledger.json: append one row { id: 'sensor-room-delta.CLAIM_FLOOR_N', file: 'lib/core/sensors/sensor-room-delta.cjs', line_anchor: 'CLAIM_FLOOR_N', value: 5, gates: 'the minimum count of claim nodes added since the last ambient run that counts as delta class (a) (Phase 355.1 AMB-01); proposed at the measured median claims per active room-hour (p50 5) across 31 live rooms over 90 days; approval recorded at the 355.1 checkpoint', kind: 'floor', env_override: null, env_read: 'load', status: 'disclosed', provenance: null, dependent_outputs: [] } (dependent_outputs empty: no 355 producer render discloses this floor, so tests/test-355-floor-sweep.cjs leg 4b is unaffected).
Run the three tests, `node tests/test-345-lockstep.cjs`, `node tests/test-245-priority-complete.cjs`, `node tests/test-sensors-part8-sweep.cjs`, `node tests/test-sensors-routing-fence.cjs`, `node tests/test-355-floor-sweep.cjs`, `node scripts/check-floor-ledger.cjs --check`, `node scripts/build-connector-registry.cjs --check`. Commit ALL FOUR files in ONE commit with `git commit --only lib/core/sensors/sensor-room-delta.cjs lib/core/insight-sensors.cjs lib/core/sensors/sensor-priority.cjs data/floor-ledger.json`: `feat(355.1-03): SENS-21 room-delta detector in lockstep on context_block, claim floor disclosed (AMB-01, AMB-02)`.
  </action>
  <verify>
    <automated>node tests/test-3551-classify.cjs && node tests/test-3551-lockstep.cjs && node tests/test-3551-sensor-fires.cjs && node tests/test-345-lockstep.cjs && node tests/test-245-priority-complete.cjs && node tests/test-sensors-part8-sweep.cjs && node tests/test-sensors-routing-fence.cjs && node scripts/check-floor-ledger.cjs --check && node scripts/build-connector-registry.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command exits 0; each 3551 test prints `FAIL: 0`
    - `git show --stat HEAD` lists exactly the four source/data files of this task
    - `grep -c "SENSOR_ID = 'SENS-21'" lib/core/sensors/sensor-room-delta.cjs` prints 1 and `grep -c "'SENS-21'" lib/core/insight-sensors.cjs` prints 1 and `grep -c "id: 'SENS-21'" lib/core/sensors/sensor-priority.cjs` prints 1
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/sensors/sensor-room-delta.cjs | grep -cE "writeFileSync|renameSync|child_process|require\('zod'\)|async function"` prints 0
    - `grep -c "sensor-room-delta.CLAIM_FLOOR_N" data/floor-ledger.json` prints 1
    - `grep -cP '\x{2014}' lib/core/sensors/sensor-room-delta.cjs` prints 0
  </acceptance_criteria>
  <done>SENS-21 is registered in every gated place, fires only as the honest fallback on both the CLI and MCP paths, and the classifier and hash are pure and shared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| last-room-delta.json -> hook-path sensor | A local file drives what reach is offered |
| sensor registry order -> stampSensorId | Index order decides which sensor id stamps each reach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-09 | Tampering | a forged delta file forcing an offer | mitigate | Freshness window with future-mtime guard, schema_version check, run_state closed enum, fail-closed null on any parse fault; the offer is a hold-posture suggestion that executes nothing |
| T-3551-10 | Spoofing | mis-stamped reaches from an index mismatch | mitigate | Same-index append asserted by tests/test-3551-lockstep.cjs and the registry completeness check |
| T-3551-11 | Elevation of privilege | a sensor that routes or executes | mitigate | Pure detector: no write, no spawn, not async (static legs plus test-sensors-routing-fence.cjs) |
| T-3551-12 | Information disclosure | room content in reach evidence | mitigate | Evidence holds run_state and class letters only; the marker/slug/path leg proves it; test-sensors-part8-sweep.cjs spans the new file |
| T-3551-13 | Denial of service | a dark or MCP-invisible sensor | mitigate | No decide()-only ctx producer; fire proven through the MCP pull and decide() (Pitfall 2) |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Module `lib/core/sensors/sensor-room-delta.cjs` exports `sensorRoomDelta(turn, tuple, ctx)`, `classifyRoomDelta(facts, watermarks, opts)`, `computeDeltaHash(lines)`, `SENSOR_ID = 'SENS-21'`, `REACH_ID = 'context_block'`, `POSTURE = 'hold'`, `SIGNAL = 'room_delta'`, `DISPATCH = 'auto-explore'`, `CLAIM_FLOOR_N = 5`, `DELTA_CLASSES`, `SEAMS`, `RUN_STATES`, `DEGRADE_STATES`, `SURFACED_VIA`, `DELTA_STATE_RELPATH` (`.mindrian/last-room-delta.json`), `DELTA_STATE_SCHEMA_VERSION = 1`, `ROOM_DELTA_FRESHNESS_MS`
- classifyRoomDelta return keys `changed`, `classes`, `delta_hash`, `lines`, `next_watermarks` (`claims_created_at`, `artifacts_created_at`, `contradicts_keys`, `stage`, `children`); delta-fact line prefixes `a:`, `b:`, `c:`, `d:`, `e:`, `e:m:`
- Registry entries: `sensorRoomDelta` in `SENSOR_REGISTRY`, `'SENS-21'` in `SENSOR_REGISTRY_IDS`, `sensorRoomDelta` on `insight-sensors.cjs` module.exports; SENS_PRIORITY Group B record `SENS-21`
- Floor-ledger row `sensor-room-delta.CLAIM_FLOOR_N`
- Tests `tests/test-3551-classify.cjs`, `tests/test-3551-lockstep.cjs`, `tests/test-3551-sensor-fires.cjs`

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently; `lib/core/insight-sensors.cjs` and `lib/core/sensors/sensor-priority.cjs` are peer-touched. Re-read each from disk immediately before editing; if `git status --short` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it; wait and re-read, or stop and report. Commit with `git commit --only` on this plan's paths; no `state.*` writes while a peer executes.

<verification>
- `node tests/test-3551-classify.cjs`, `node tests/test-3551-lockstep.cjs`, `node tests/test-3551-sensor-fires.cjs`
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set within BASE_3551's)
</verification>

<success_criteria>
- SENS-21 registered in six places, one commit, fires only as the fallback on CLI and MCP paths
- The delta hash is pure, order-free and stable for the same room state; the claim floor is disclosed
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-03-SUMMARY.md` when done.
</output>
