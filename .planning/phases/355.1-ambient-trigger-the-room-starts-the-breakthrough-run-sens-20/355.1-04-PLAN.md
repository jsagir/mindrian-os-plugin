---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 04
type: execute
wave: 2
depends_on: ["355.1-01"]
files_modified:
  - tests/test-3551-framing.cjs
  - lib/core/direction-convention.cjs
  - lib/core/ambient-framing.cjs
autonomous: true
requirements: [AMB-07]
canon_parts: [3, 8, 12]
tags: [framing, problem-type, strategy-not-keywords, direction-convention]

must_haves:
  truths:
    - "CONTEXT 'Strategy, not keywords (AMB-07, locked)': the card's framing follows a NON-keyword lookup chain, first hit wins: the ratified jtbd goal.rung, then ROOM.md pws_stage, then explicit STATE.md fields, then the structural MINTO classifier, then neutral; _inferRungFromQuestion (a keyword marker table) is never required, called or named on the path"
    - "CONTEXT 'Strategy, not keywords': an ill-defined room gets a find-the-problem framing, a well-defined room a pursue-or-drop framing; UnDefined and IllDefined map to find_the_problem, WellDefined to pursue_or_drop, Wicked and unknown to neutral; pws_stage ill_defined maps to find_the_problem and extend_opportunity to pursue_or_drop"
    - "CONTEXT 'Strategy, not keywords': the framing text lives in ONE place beside the direction phrases: FRAMING_IDS and FRAMING_PHRASES in lib/core/direction-convention.cjs, kept OUT of phraseHash()'s inputs so 355's confirmed direction phrases and PHRASES_CONFIRMED stay valid; framingHash() covers the framing table alone"
    - "Navigator Ruling 6: FRAMING_CONFIRMED ships null; plan 355.1-08 records the PWS author's confirmation (role only, never a name) the 355-02 way, as { by: 'pws-author', at, framing_hash }"
    - "Navigator Ruling 4: the AI-SPEC's two trigger moments (ill-defined, extend-the-opportunity) shape the framing only (pws_stage is chain link 2); nothing in this module gates a run"
    - "Every framing phrase passes 355-23's gates: no D-30 decimal, no `[0-9]+%`, no count next to a finding word, none of breakthrough / convergent / validated / proven, no praise token, no em-dash (Canon Part 12)"
    - "The neutral framing must read well, because most live rooms land on undefined or unknown today (research item 8)"
  artifacts:
    - path: "lib/core/ambient-framing.cjs"
      provides: "the non-keyword rung resolver and the rung -> framing map"
      exports: ["RUNG_IDS", "RUNG_SOURCES", "FRAMING_BY_RUNG", "PWS_STAGE_FRAMING", "resolveRoomRung", "framingFor"]
    - path: "lib/core/direction-convention.cjs"
      provides: "FRAMING_IDS, FRAMING_PHRASES, framingHash, FRAMING_CONFIRMED beside the direction phrases"
      contains: "FRAMING_PHRASES"
    - path: "tests/test-3551-framing.cjs"
      provides: "chain-order legs, phrase gates, static keyword-free legs and the planted keyword-classifier negative control"
      min_lines: 80
  key_links:
    - from: "lib/core/ambient-framing.cjs framingFor"
      to: "lib/core/direction-convention.cjs FRAMING_IDS"
      via: "returns a FRAMING_IDS member only"
      pattern: "FRAMING_IDS"
---

<objective>
AMB-07, the deferred 355 front end: the card's framing follows the room's problem type, found strategically (what the navigator ratified, what the room declares, what its structure shows), never by matching words. This plan lands the three framing phrases beside the direction phrases and the resolver chain that picks one. The PWS author confirms the phrases at the single checkpoint (plan 355.1-08); until then FRAMING_CONFIRMED is null.

Proposed phrases (checkpoint approves or replaces): find_the_problem 'This connection might help name the real problem.'; pursue_or_drop 'This connection might be worth a decision: pursue it or drop it.'; neutral 'The room changed, and this connection came out of it.'

Output: the framing constants, the resolver module, one test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-RESEARCH.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/355-PHRASE-CONFIRMATION.md
@lib/core/direction-convention.cjs
@tests/helpers/fixture-room-3551.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED framing test (chain order, phrase gates, keyword-free statics, planted keyword classifier)</name>
  <files>tests/test-3551-framing.cjs</files>
  <read_first>
    - lib/core/direction-convention.cjs (whole file: DIRECTION_MEANING, phraseHash inputs, PHRASES_CONFIRMED shape, the single node:crypto require)
    - tests/test-355-direction-convention.cjs (the static single-require leg and the PHRASES_CONFIRMED hash-equality leg that must stay green)
    - lib/hmi/jtbd-state.cjs (getGoal(roomDir) and the file it reads) and lib/core/strategy/rung-vocabulary.cjs (isTheoRung and the Theo rung ids)
    - lib/mcp/brain-router.cjs lines 298-420 (extractProblemType, _rungFromClassification, which fields are explicit; its venture_stage inference must NOT be used) and its require lines (it lazy-requires brain-client at :455)
    - lib/core/brain-derivation.cjs lines 170-210 (classifyProblemType(triple) -> UDP / IDP / WDP) and lib/core/folder-memory.cjs readTriple
    - lib/core/brain-client.cjs lines 1230-1260 (_inferRungFromQuestion: the forbidden keyword table, to know what the static leg looks for)
    - tests/test-355-no-decimal.cjs (the D-30 regex and banned-claim word list to reuse verbatim)
  </read_first>
  <behavior>
    - FRAMING_IDS deep-equals ['find_the_problem','pursue_or_drop','neutral'] and is frozen; Object.keys(FRAMING_PHRASES) deep-equals FRAMING_IDS; FRAMING_CONFIRMED === null
    - phraseHash() still equals PHRASES_CONFIRMED.phrase_hash (framing kept out of its inputs); framingHash() is 64 hex and changes when a framing phrase changes (checked on a cloned table through an exported pure hashFramingTable(table) helper or by recomputing the documented payload)
    - direction-convention.cjs still has exactly one require( on an executable line (node:crypto)
    - chain order on buildDeltaRoom rooms: jtbd goal.rung set to a Theo rung id -> source 'jtbd_goal' wins even when ROOM.md pws_stage and STATE.md fields are also set; with no goal, ROOM.md frontmatter pws_stage 'ill_defined' -> framing find_the_problem, source 'room_pws_stage'; 'extend_opportunity' -> pursue_or_drop; with neither, STATE.md frontmatter `definition_level` (an explicit field extractProblemType reads) -> source 'state_explicit'; a STATE.md holding ONLY venture_stage -> NOT state_explicit (the venture_stage inference is skipped)
    - with allowStructural true and an injected opts.structuralFn returning 'IDP' -> rung IllDefined, source 'minto_structural', framing find_the_problem; 'WDP' -> pursue_or_drop; with allowStructural false the structural step never runs (structuralFn spy not called)
    - nothing found -> { rung: 'unknown', source: 'none' } and framing 'neutral'; any thrown error inside a step moves on to the next step, never throws out
    - every FRAMING_PHRASES value passes: the D-30 regex from tests/test-355-no-decimal.cjs, no `[0-9]+%`, no digit at all, none of breakthrough / convergent / validated / proven / great / amazing / excellent / brilliant (case-insensitive, word boundary), no U+2014, length between 20 and 90 characters
    - static: lib/core/ambient-framing.cjs contains no `_inferRungFromQuestion` token, no array literal holding a quoted string with a space in it (the shape of a phrase-marker table such as 'future of'), and none of the literal marker strings of _inferRungFromQuestion's own table (the test reads that table from brain-client.cjs source and checks each string); its top-level (unindented) require( calls are only node:fs, node:path and ./direction-convention.cjs (every other dependency is required inside the step that uses it)
    - negative control: `scanForKeywordClassifier(files)` (defined in this test) flags a scratch file written to os.tmpdir() that contains `_inferRungFromQuestion(` and a second one holding `['stakeholder', 'future of', 'measure']`; both reported as PASS when caught; scratch files removed
  </behavior>
  <action>Write tests/test-3551-framing.cjs ('use strict', hygiene-355 preamble, attempts() === 0 last) implementing every behavior line. Build rooms with buildDeltaRoom; write jtbd-state.json through lib/hmi/jtbd-state.cjs's own writer (read it for the ratified-goal setter; if only a reader exists, write the file in the exact shape getGoal reads), ROOM.md via writeRoomMd, STATE.md via fs. Run it; it fails (constants and module absent). Commit `test(355.1-04): add failing non-keyword framing test (AMB-07)`.</action>
  <verify>
    <automated>node tests/test-3551-framing.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The test exists, runs, and fails on the missing exports
    - `grep -c "scanForKeywordClassifier" tests/test-3551-framing.cjs` prints at least 2 and `grep -c "negative control" tests/test-3551-framing.cjs` prints at least 2
  </acceptance_criteria>
  <done>A failing test pins the chain order, the phrase gates and the keyword ban.</done>
</task>

<task type="auto">
  <name>Task 2: FRAMING constants beside the direction phrases, and lib/core/ambient-framing.cjs</name>
  <files>lib/core/direction-convention.cjs, lib/core/ambient-framing.cjs</files>
  <read_first>
    - lib/core/direction-convention.cjs (re-read now; `git status --short lib/core/direction-convention.cjs` must be empty)
    - tests/test-3551-framing.cjs (the RED legs)
    - the four resolver dependencies named in Task 1's read_first (exports only)
  </read_first>
  <action>
direction-convention.cjs: after PHRASES_CONFIRMED add a block commented "Phase 355.1 AMB-07: the ambient card's framing phrases, beside the direction phrases (CONTEXT 'Strategy, not keywords'); deliberately NOT inputs of phraseHash() so 355's PHRASES_CONFIRMED stays valid; confirmed by the PWS author at the 355.1 checkpoint (Ruling 6)": `FRAMING_IDS = Object.freeze(['find_the_problem', 'pursue_or_drop', 'neutral'])`; `FRAMING_PHRASES = Object.freeze({ find_the_problem: 'This connection might help name the real problem.', pursue_or_drop: 'This connection might be worth a decision: pursue it or drop it.', neutral: 'The room changed, and this connection came out of it.' })`; `hashFramingTable(table)` = sha256 hex of JSON.stringify({ FRAMING_IDS, FRAMING_PHRASES: table }); `framingHash()` = hashFramingTable(FRAMING_PHRASES); `FRAMING_CONFIRMED = null` (plan 355.1-08 replaces it with the frozen record). Append the five names to module.exports. Add no require.
Create lib/core/ambient-framing.cjs ('use strict', BSL header citing Phase 355.1 AMB-07, research item 8, Rulings 4 and 6, "never a keyword match; _inferRungFromQuestion is forbidden here", hyphens only). Top-level requires: node:fs, node:path, ./direction-convention.cjs only. Constants: `RUNG_IDS = Object.freeze(['UnDefined','IllDefined','WellDefined','Wicked','unknown'])`; `RUNG_SOURCES = Object.freeze(['jtbd_goal','room_pws_stage','state_explicit','minto_structural','none'])`; `FRAMING_BY_RUNG = Object.freeze({ UnDefined: 'find_the_problem', IllDefined: 'find_the_problem', WellDefined: 'pursue_or_drop', Wicked: 'neutral', unknown: 'neutral' })`; `PWS_STAGE_FRAMING = Object.freeze({ ill_defined: 'find_the_problem', extend_opportunity: 'pursue_or_drop' })`. `resolveRoomRung(roomDir, opts)` walks the chain, each step in its own try/catch that falls through: (1) lazy-require ../hmi/jtbd-state.cjs getGoal(roomDir).rung, accepted only when ./strategy/rung-vocabulary.cjs isTheoRung says so, mapped to RUNG_IDS by that module's vocabulary; (2) room-root ROOM.md frontmatter pws_stage in PWS_STAGE_FRAMING (return { rung: null, source: 'room_pws_stage', pws_stage }); (3) lazy-require ../mcp/brain-router.cjs extractProblemType over STATE.md content, used ONLY when STATE.md frontmatter carries one of definition_level, problem_definition, complexity, problem_type, then its _rungFromClassification (reached through whatever export form brain-router offers; if it is not exported, map the explicit enum values in this module with a comment citing brain-router's table); (4) only when opts.allowStructural === true: opts.structuralFn if given, else lazy-require ./brain-derivation.cjs classifyProblemType(require('./folder-memory.cjs').readTriple(roomDir)), mapping UDP -> UnDefined, IDP -> IllDefined, WDP -> WellDefined; (5) { rung: 'unknown', source: 'none' }. `framingFor(roomDir, opts)` returns { framing (a FRAMING_IDS member), rung, source }, using PWS_STAGE_FRAMING for source room_pws_stage and FRAMING_BY_RUNG otherwise. Never throws; never writes.
Run tests/test-3551-framing.cjs, tests/test-355-direction-convention.cjs, tests/272-direction-convention.test.cjs and tests/test-355-direction-readers.cjs. Commit with `git commit --only lib/core/direction-convention.cjs lib/core/ambient-framing.cjs`: `feat(355.1-04): framing phrases beside the direction phrases and the non-keyword rung resolver (AMB-07)`.
  </action>
  <verify>
    <automated>node tests/test-3551-framing.cjs && node tests/test-355-direction-convention.cjs && node tests/272-direction-convention.test.cjs && node tests/test-355-direction-readers.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command exits 0; the framing test prints `FAIL: 0` and its output contains two caught negative controls
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/direction-convention.cjs | grep -c "require("` prints 1
    - `grep -c "_inferRungFromQuestion" lib/core/ambient-framing.cjs` prints 0 and `grep -c "FRAMING_CONFIRMED = null" lib/core/direction-convention.cjs` prints 1
    - `node -e "const d=require('./lib/core/direction-convention.cjs');if(d.phraseHash()!==d.PHRASES_CONFIRMED.phrase_hash)process.exit(1)"` exits 0
    - `grep -cP '\x{2014}' lib/core/ambient-framing.cjs lib/core/direction-convention.cjs` prints 0 for each file
  </acceptance_criteria>
  <done>The card can be framed by the room's problem type without ever reading the user's words, and the phrases sit in one place ready for the author's confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room files (jtbd-state, ROOM.md, STATE.md, MINTO triple) -> framing enum | Local room state picks a framing phrase |
| framing phrase -> user | The phrase is shown on every surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-14 | Tampering | a keyword classifier smuggled into the framing path | mitigate | Static legs ban _inferRungFromQuestion and marker tables in ambient-framing.cjs; scanForKeywordClassifier with two planted negative controls |
| T-3551-15 | Spoofing | a phrase implying a score, a count or a verdict | mitigate | Every phrase passes the D-30 regex, no digit, no banned claim word, no praise token (Canon Part 12); hedged wording |
| T-3551-16 | Repudiation | a phrase changed after the author's confirmation | mitigate | framingHash() recorded at the checkpoint in FRAMING_CONFIRMED; a later edit fails the equality leg added by plan 355.1-08 |
| T-3551-17 | Denial of service | the resolver loading brain-client on a hook path | mitigate | Only fs, path and direction-convention at top level; heavy steps lazy-required; the structural step runs only with allowStructural (child side) |
</threat_model>

## Artifacts this phase produces

This plan's share:
- `lib/core/direction-convention.cjs` new exports `FRAMING_IDS`, `FRAMING_PHRASES`, `hashFramingTable(table)`, `framingHash()`, `FRAMING_CONFIRMED` (null until plan 355.1-08)
- Module `lib/core/ambient-framing.cjs` exports `RUNG_IDS`, `RUNG_SOURCES`, `FRAMING_BY_RUNG`, `PWS_STAGE_FRAMING`, `resolveRoomRung(roomDir, opts)`, `framingFor(roomDir, opts)`; opts keys `allowStructural`, `structuralFn`
- Framing ids `find_the_problem`, `pursue_or_drop`, `neutral`; rung sources `jtbd_goal`, `room_pws_stage`, `state_explicit`, `minto_structural`, `none`
- Test `tests/test-3551-framing.cjs` (function `scanForKeywordClassifier`)

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently. Re-read `lib/core/direction-convention.cjs` from disk before editing; if `git status --short` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it. Commit with `git commit --only` on this plan's paths; no `state.*` writes while a peer executes.

<verification>
- `node tests/test-3551-framing.cjs`
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set within BASE_3551's)
</verification>

<success_criteria>
- One framing table beside the direction phrases, 355's phrase hash untouched
- A non-keyword resolver chain with a proven order and a caught planted keyword classifier
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-04-SUMMARY.md` when done.
</output>
