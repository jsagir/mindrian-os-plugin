---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 15
type: execute
wave: 8
depends_on: ["355.1-10", "355.1-12", "355.1-14"]
files_modified:
  - tests/test-3551-part8-egress.cjs
  - tests/test-355-part8-egress.cjs
  - tests/run-all-3551.sh
  - lib/memory/run-feynman-tests.cjs
autonomous: true
requirements: [AMB-08, AMB-09]
canon_parts: [8, 9, 11]
tags: [part8, egress, zod-at-edges, phase-gate, test-registry]

must_haves:
  truths:
    - "CONTEXT 'Boundaries (AMB-08, locked)': the 355-21 Part 8 egress sweep (tests/test-355-part8-egress.cjs) covers every new 355.1 file; Theo receives only framework handles and enums through 355-06's adapter; argv and logs carry no room bytes; only lib/core/verification-stamp.cjs reaches the wire on the ambient path"
    - "CONTEXT 'Boundaries': zod appears only at the two outside edges (MCP tool inputs and the Theo response parse in 355-06's Stamp); no 355.1 file requires zod: not the ledger, the delta readers, the lock, the evaluator, the guards or the composition"
    - "CONTEXT 'Boundaries' / Canon Part 9: machinery confirms nothing; no 355.1 file passes review_status, contains INSERT INTO, or mints a new memory_event type"
    - "CONTEXT 'Proof and close (AMB-09, locked)': tests/run-all-3551.sh is final: the Part 8 and Part 9 sweeps are hard legs over the final target list (a missing target FAILS), tests/test-355-part8-egress.cjs is a hard leg, the doctor leg still compares against the BASE_3551 set (never re-baselined), and external reds are documented exactly the way 355 documented its own"
    - "Every offline tests/test-3551-*.cjs is registered in lib/memory/run-feynman-tests.cjs TEST_FILES with a one-line Phase 355.1 plan / AMB comment; each exits 0 or 77 with every key unset"
    - "PRD AC5: the Part 8 leg shows zero room bytes leaving the child (argv, stderr, ledger, delta state, lock) and Theo receiving handles only; AC7: bash tests/run-all-3551.sh is green, doctor acceptance unregressed, the four born-wired gates green"
  artifacts:
    - path: "tests/test-3551-part8-egress.cjs"
      provides: "egress, zod-at-edges, Part 9 and closed-file sweeps over every 355.1 file, with negative controls"
      min_lines: 100
    - path: "tests/run-all-3551.sh"
      provides: "the final phase gate"
      contains: "test-355-part8-egress"
    - path: "lib/memory/run-feynman-tests.cjs"
      provides: "3551 tests registered for the release train"
      contains: "test-3551-"
  key_links:
    - from: "tests/test-355-part8-egress.cjs target list"
      to: "every new 355.1 source file"
      via: "list extension, Amended Phase 355.1 AMB-08"
      pattern: "355.1"
---

<objective>
AMB-08 and AMB-09's gate: prove on every new file that nothing from the room leaves the machine except canon handles through the one stamp adapter, that zod sits only at the outside edges, that machinery confirms nothing, and make the phase gate final and registered where the release train runs it.

Output: one sweep test, the 355-21 target-list extension, the final run-all-3551.sh, the registry entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-RESEARCH.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-BASELINE.md
@.planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/deferred-items.md
@tests/helpers/hygiene-355.cjs
@tests/helpers/fixture-room-3551.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The 355.1 Part 8 / Part 9 / zod-at-edges sweep and the 355-21 target-list extension</name>
  <files>tests/test-3551-part8-egress.cjs, tests/test-355-part8-egress.cjs</files>
  <read_first>
    - tests/test-355-part8-egress.cjs (whole file: its target list constant, BANNED_355, the scratch-file negative control at lib/core/__scratch_355_egress.cjs, what it asserts about verification-stamp.cjs being the only wire)
    - tests/run-all-355.sh Part 8 leg (PART8_RE) and tests/helpers/hygiene-355.cjs (nonCommentLines, isPureLineComment)
    - every new 355.1 source file: lib/core/navigation/room-delta-facts.cjs, lib/core/sensors/sensor-room-delta.cjs, lib/core/ambient-framing.cjs, lib/core/ambient-trigger.cjs, lib/core/ambient-run.cjs, lib/mcp/surfaced-offers.cjs, scripts/ambient-stop.cjs, and the 355.1 sections of scripts/scout-cadence-guard.cjs and scripts/auto-explore-fire.cjs
  </read_first>
  <behavior>
    - egress: no executable line of any 355.1 target matches PART8_RE (fetch(, http(s)://, node:http(s), curl, wget)
    - one wire: no 355.1 target requires brain-client, the mindrian-brain shim, a pws-brain client or anything under ~/Theo; lib/core/ambient-run.cjs reaches Theo only through producer seams that go through lib/core/verification-stamp.cjs (static require scan of ambient-run.cjs shows no brain-client)
    - zod at edges: no 355.1 target requires zod
    - Part 9: no 355.1 target contains INSERT INTO, DatabaseSync, node:sqlite, a review_status assignment, or a logMemoryEvent call with a new event type (the only memory_event names in 355.1 files are ones that already existed at BASE_3551)
    - closed files after a full fixture cycle (buildDeltaRoom + addClaims(5) -> evaluateAndClaim -> runAmbientInChild with injected composition producing one indirect finding -> closeOutRoom with a captured spawn): ambient-run-ledger.json, last-room-delta.json and any lock file hold no 'SECRET-3551-' marker, no slug, no '/' and no key outside their closed schemas
    - argv: buildAmbientArgv for that room returns exactly six elements matching [abs fire path, roomDir, '', /^ambient-[0-9a-f]{12}$/, session id, '--ambient'] and none holds the marker
    - Theo args: through a counting callTool over the injected composition's real stamping path (when the integration adapters are available, else the 355-06 stampFindings call with the fixture pair), every captured argument value is a canon name from data/framework-names.json or an enum
    - negative controls, each reported PASS when caught: a scratch file under os.tmpdir() with `fetch('https://example.invalid')`, one with `require('zod')`, one with `INSERT INTO nodes`, each added to the scan list
    - tests/test-355-part8-egress.cjs, extended: its target list now includes every 355.1 source file listed above, with a header note "Amended Phase 355.1 AMB-08"; it still passes, and its own negative control still bites
  </behavior>
  <action>Write tests/test-3551-part8-egress.cjs ('use strict', hygiene-355 preamble, attempts() === 0 last; exit 77 with a loud message only when node:sqlite is unavailable) implementing every behavior line. Re-read tests/test-355-part8-egress.cjs and append the 355.1 source files to its target list constant exactly in its existing style, with the header note; change nothing else. Run both. Commit with `git commit --only tests/test-3551-part8-egress.cjs tests/test-355-part8-egress.cjs`: `test(355.1-15): Part 8, Part 9 and zod-at-edges sweep over every 355.1 file; 355-21 sweep extended (AMB-08)`.</action>
  <verify>
    <automated>node tests/test-3551-part8-egress.cjs && node tests/test-355-part8-egress.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both tests exit 0 with `FAIL: 0`; the 3551 test reports three caught negative controls
    - `grep -c "ambient-trigger.cjs\|ambient-run.cjs\|sensor-room-delta.cjs\|room-delta-facts.cjs" tests/test-355-part8-egress.cjs` prints at least 4 and `grep -c "Amended Phase 355.1" tests/test-355-part8-egress.cjs` prints 1
  </acceptance_criteria>
  <done>Every new file is inside the Part 8 fence, zod stays at the edges, and machinery confirms nothing.</done>
</task>

<task type="auto">
  <name>Task 2: The final run-all-3551.sh and the TEST_FILES registration</name>
  <files>tests/run-all-3551.sh, lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - tests/run-all-3551.sh (plan 355.1-01's skeleton) and 355.1-BASELINE.md (BASE_3551, the baseline failing set)
    - lib/memory/run-feynman-tests.cjs TEST_FILES tail (the 355 entries 355-27 appended; the commented-path entry shape; missing = FAIL, 77 = SKIP)
    - .planning/phases/355-hidden-in-plain-sight-jev-through-theo-cross-connection-engi/deferred-items.md (how 355 documented its external reds)
    - `git status --short tests/run-all-3551.sh lib/memory/run-feynman-tests.cjs` (must be empty; collision rule)
  </read_first>
  <action>
run-all-3551.sh: convert the Part 8 and Part 9 legs from run_if-on-existence to hard `run` legs over the final target list (lib/core/navigation/room-delta-facts.cjs lib/core/sensors/sensor-room-delta.cjs lib/core/ambient-framing.cjs lib/core/ambient-trigger.cjs lib/core/ambient-run.cjs lib/mcp/surfaced-offers.cjs scripts/ambient-stop.cjs scripts/auto-explore-fire.cjs scripts/scout-cadence-guard.cjs), so a missing target fails; add `node tests/test-355-part8-egress.cjs` as a hard leg; add the amended tests (tests/test-dial-label-bank-drift.cjs, tests/test-355-part8-egress.cjs, tests/test-355-filing.cjs, tests/test-355-sens13-fire-once.cjs) as explicit no-regression legs if not already present; keep the doctor leg comparing against the BASE_3551 set (never re-baseline); if any leg is red for a reason outside this phase, record it in the header exactly the way 355's deferred-items.md records its external reds (leg, cause, owner phase) and do not mask it.
run-feynman-tests.cjs: append every offline tests/test-3551-*.cjs after the current tail, each with `// Phase 355.1 Plan-NN, AMB-xx: <one-line why>`; register only tests that exit 0 or 77 with TYPESAFE_API_KEY, MINDRIAN_BRAIN_KEY and every other key unset (confirm each by running it that way).
Run `bash tests/run-all-3551.sh`, `bash tests/run-all-355.sh` and `node lib/memory/run-feynman-tests.cjs`. Commit with `git commit --only tests/run-all-3551.sh lib/memory/run-feynman-tests.cjs`: `test(355.1-15): final phase gate and test registration (AMB-09)`.
  </action>
  <verify>
    <automated>bash tests/run-all-3551.sh && bash tests/run-all-355.sh && node lib/memory/run-feynman-tests.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All three commands exit 0; the run-all-3551 doctor leg reports no failing point outside the BASE_3551 set
    - `grep -c "tests', 'test-3551-" lib/memory/run-feynman-tests.cjs` equals `ls tests/test-3551-*.cjs | wc -l`
    - `grep -c 'run_if "Part 8' tests/run-all-3551.sh` prints 0 and `grep -c "test-355-part8-egress" tests/run-all-3551.sh` prints at least 1
    - `grep -cP '\x{2014}' tests/run-all-3551.sh` prints 0
  </acceptance_criteria>
  <done>The phase gate is final, green and registered where the release train will run it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| every 355.1 file -> network | The Part 8 fence |
| phase gate -> release train | What future releases re-run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-65 | Information disclosure | a later edit adding egress to a 355.1 file | mitigate | Hard Part 8 legs in run-all-3551.sh and the extended 355-21 sweep, both with negative controls |
| T-3551-66 | Tampering | zod creeping into hook-path modules | mitigate | zod-at-edges leg over every 355.1 file with a planted negative control |
| T-3551-67 | Elevation of privilege | machinery confirming a claim | mitigate | Part 9 leg: no review_status assignment, no INSERT INTO, no new memory_event type |
| T-3551-68 | Repudiation | a silently re-baselined or masked gate | mitigate | Doctor leg keeps BASE_3551; external reds recorded per leg with cause and owner, never masked |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Test `tests/test-3551-part8-egress.cjs` (scan functions for egress, zod, Part 9; closed-file and argv legs)
- `tests/test-355-part8-egress.cjs` target list extended with the 355.1 source files
- Final `tests/run-all-3551.sh` (hard Part 8 / Part 9 legs, the 355-21 sweep leg)
- `lib/memory/run-feynman-tests.cjs` TEST_FILES entries for every offline `tests/test-3551-*.cjs`

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently. Re-read every file from disk immediately before editing; if `git status --short` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it. Commit with `git commit --only` on this plan's paths; no `state.*` writes while a peer executes.

<verification>
- `node tests/test-3551-part8-egress.cjs`
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set within BASE_3551's)
</verification>

<success_criteria>
- AC5: zero room bytes leave the child; Theo sees handles only; AC7: run-all-3551 green, doctor unregressed, four gates green
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-15-SUMMARY.md` when done.
</output>
