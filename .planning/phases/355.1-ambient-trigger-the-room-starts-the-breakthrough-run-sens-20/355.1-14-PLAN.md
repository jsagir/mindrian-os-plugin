---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 14
type: execute
wave: 7
depends_on: ["355.1-06", "355.1-09", "355.1-13"]
files_modified:
  - tests/test-3551-tri-polar.cjs
  - lib/mcp/stop-gate-handler.cjs
  - scripts/ambient-stop.cjs
  - hooks/hooks.json
autonomous: true
requirements: [AMB-06, AMB-04, AMB-03, AMB-08]
canon_parts: [3, 8, 9, 11, 12]
cirs_relationship:
  surfaces_added: ["hooks/hooks.json Stop entry scripts/ambient-stop.cjs (async, HITL shape none)"]
  surfaces_modified: ["stop_gate_check close-out (lib/mcp/stop-gate-handler.cjs closeOutRoom)"]
  surfaces_removed: []
  spine_consumed: ["lib/core/ambient-trigger.cjs evaluateAndMaybeSpawn", "scripts/auto-explore-fire.cjs --ambient"]
  gate_impact: "Wires the ambient trigger live on all three surfaces: one new async Stop entry on CLI and one best-effort step in the existing stop_gate_check close-out for Desktop and Cowork; stop_gate_check keeps hitl_shape F.1; no new MCP tool, no new command, no seventh reach."
  explanation: "AMB-06 go-live after the single checkpoint: the CLI Stop hook and the MCP close-out call the same evaluator, which starts the same child; the hook entry is machinery with HITL shape none, the only human decision being the gate answer on the card; Ruling 1 lets the MCP-started child stamp through brain-client directly."
tags: [go-live, stop-hook, async, close-out, tri-polar, ac1]

must_haves:
  truths:
    - "CONTEXT 'Tri-polar (AMB-06, locked)': CLI = the existing PostToolUse fingerprint (class e) plus ONE new Stop entry in hooks/hooks.json with async: true and timeout 5 (seconds, research C5) running scripts/ambient-stop.cjs, which bounds itself; Desktop and Cowork = stop_gate_check -> closeOutRoom calling the same evaluator right after _closeOutStateMd (never context_assemble, which is declared a pure read); the evaluator step precedes plan 355.1-13's fire-once step, and the order cannot mark a just-spawned child's card because the close-out marks only handles a pull noted in memory"
    - "Navigator Ruling 1: the child started from the MCP server stamps through brain-client directly (the child, not the surface, computes the stamp); the surface only renders it"
    - "One test leg per surface: CLI (the Stop script as a real process, under its budget, spawning), Desktop (handleStopEvent close-out spawning, MCP stdout untouched), Cowork (the same handler under CLAUDE_SURFACE=cowork; the server runs as a local process in the Cowork VM, research A1, stated in the leg); a skipped surface is a stated call, never an omission"
    - "PRD AC1: a room that gains N claims with no command typed produces, within one Stop-hook cycle on CLI and one close-out on Desktop and Cowork, one background run whose finding carries the 355 stamp and surfaces once on the next turn; a second identical delta produces no second run"
    - "Research Pitfall 6: the async Stop entry, the close-out (flag-on CLI routes Stop through stop_gate_check too) and the fingerprint's material child on one room state produce exactly one run"
    - "The Stop script writes nothing to stdout outside --dry-run (an async hook's output can reach the next turn as context; this hook has nothing to say), exits 0 on every path, skips when stop_hook_active is true or no room is bound to the session, and resolves the room only through resolveSessionRoom({ sessionId, requireOwnership: true })"
    - "`/mos:auto-explore` stays the documented fallback (plan 355.1-12), never the way in; surface-detect.cjs's scripts:false capability is descriptive and gates nothing (no reader in lib/mcp), and the server-side detached spawn precedent (tool-router eureka-run, intelligence-cascade drain) runs on every surface"
  artifacts:
    - path: "scripts/ambient-stop.cjs"
      provides: "the CLI Stop entry: stdin parse, session room, evaluateAndMaybeSpawn, exit 0"
      contains: "evaluateAndMaybeSpawn"
    - path: "hooks/hooks.json"
      provides: "one async Stop entry"
      contains: "ambient-stop.cjs"
    - path: "lib/mcp/stop-gate-handler.cjs"
      provides: "_closeOutAmbientTrigger inside closeOutRoom"
      contains: "_closeOutAmbientTrigger"
    - path: "tests/test-3551-tri-polar.cjs"
      provides: "one leg per surface, AC1 end to end, three-seams-one-run"
      min_lines: 150
  key_links:
    - from: "hooks/hooks.json Stop"
      to: "scripts/ambient-stop.cjs"
      via: "async command entry, timeout 5 seconds"
      pattern: "ambient-stop.cjs"
    - from: "lib/mcp/stop-gate-handler.cjs closeOutRoom"
      to: "lib/core/ambient-trigger.cjs evaluateAndMaybeSpawn"
      via: "right after _closeOutStateMd, seam closeout"
      pattern: "evaluateAndMaybeSpawn"
---

<objective>
Go live on all three surfaces (AMB-06), after the checkpoint approved it. On the CLI one new async Stop entry evaluates the room after every turn without ever holding the turn. On Desktop and Cowork, where no hook fires, the MCP server's own Stop-time close-out runs the same evaluation right after it refreshes STATE.md, beside the fire-once step plan 355.1-13 added. One evaluator, one child, one card per delta, on every surface.

Output: scripts/ambient-stop.cjs, the hooks.json entry, one close-out step, the tri-polar test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-RESEARCH.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md
@lib/core/ambient-trigger.cjs
@lib/core/ambient-run.cjs
@lib/mcp/surfaced-offers.cjs
@tests/helpers/fixture-room-3551.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tri-polar test</name>
  <files>tests/test-3551-tri-polar.cjs</files>
  <read_first>
    - lib/mcp/stop-gate-handler.cjs (whole file, re-read: closeOutRoom with plan 355.1-13's _closeOutEurekaFireOnce, handleStopEvent, resolveSessionRoomDir, _resetForTest)
    - an existing stop-gate handler test (`ls tests/*.cjs | grep -iE "stop-gate|stop_gate"`, read the smallest) for how handleStopEvent is driven with a bound session
    - scripts/gsd-graph-derive-sweep.cjs lines 1-40 and 180-191 (the Stop-entry contract) and scripts/auto-explore-fingerprint.cjs lines 44-77 and 170-180 (readStdin, session_id, backstops)
    - hooks/hooks.json Stop array; tests that parse hooks.json (`grep -ln "hooks.json" tests/*.cjs`)
    - lib/mcp/surface-detect.cjs (CAPABILITY_MAP, CLAUDE_SURFACE)
    - tests/test-3551-child.cjs (isolated child env idiom), tests/test-3551-mcp-fire-once.cjs and tests/test-3551-surfacing.cjs (the injected composition setup)
  </read_first>
  <behavior>
    - CLI leg, dry run: `node scripts/ambient-stop.cjs --stop --dry-run` with stdin { session_id, cwd } for a session bound (bindSession) to a buildDeltaRoom room with addClaims(5), isolated env (mkdtemp HOME and MINDRIAN_ROOMS_HOME, MINDRIAN_BRAIN_URL http://127.0.0.1:1, keys deleted) exits 0 within 5 s and prints one JSON line { decision: 'spawned', argv } whose argv ends with '--ambient' and holds 'ambient-' + 12 hex; last-room-delta.json run_state 'started', seam 'stop_hook'
    - CLI leg, real: the same without --dry-run exits 0 within 5 s with EMPTY stdout; the detached child then completes (poll the ledger until in_flight is null, up to AMBIENT_TOTAL_BUDGET_MS plus 10 s; run_state becomes 'completed' or 'deps_missing')
    - CLI leg, no-ops: stdin stop_hook_active true -> exit 0, nothing written; a session with no bound room -> exit 0, nothing written; malformed stdin -> exit 0
    - hooks.json: exactly one Stop entry command references scripts/ambient-stop.cjs with '--stop', "async": true and "timeout": 5; no entry contains '--dry-run'; the PostToolUse fingerprint entry is still present; every other entry deep-equals its BASE_3551 value
    - Desktop leg: in process, with ambientTrigger._internal.setSpawnImpl(capture), handleStopEvent(sessionId, ctx) on the claims room spawns exactly once with the --ambient argv; process.stdout.write is never called during the handler; closeOutRoom's return keys are unchanged
    - Cowork leg: the same with process.env.CLAUDE_SURFACE = 'cowork' set before requiring the handler; one spawn; the leg prints "Cowork runs the same local MCP server in its VM (research A1)"
    - AC1 end to end: claims room -> closeOutRoom (capture) -> runAmbientInChild in process with the captured ambient id and injected composition deps producing one indirect finding and a clearing guard -> side channel and delta state written -> a dispatchCandidateReaches pull returns SENS-13 as the winner -> closeOutRoom marks it (plan 355.1-13) and spawns nothing new (decision no_change or same_hash: the watermarks advanced at completion) -> the next pull returns no SENS-13; re-planting the identical delta facts the ledger already consumed also spawns nothing
    - three seams: `ambient-stop.cjs --stop --dry-run` in a process, then closeOutRoom in process, then evaluateAndClaim with seam 'material' on the same state -> exactly one claim (one dry-run record, zero in-process spawns)
    - static: scripts/ambient-stop.cjs contains no spawn / child_process (the evaluator spawns), no zod, no fetch
  </behavior>
  <action>Write tests/test-3551-tri-polar.cjs ('use strict', hygiene-355 preamble for the in-process parts, attempts() === 0 last; exit 77 with a loud message only when node:sqlite is unavailable; reset the handler with _resetForTest, surfaced-offers with _resetForTest and the spawn seam with resetSpawnImpl after each leg) implementing every behavior line, printing one PASS line per surface named CLI, Desktop, Cowork. Run it; it fails (no script, no hook entry, no close-out evaluator step). Commit `test(355.1-14): add failing tri-polar go-live test (AMB-06, AC1)`.</action>
  <verify>
    <automated>node tests/test-3551-tri-polar.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The test exists, runs and fails on the missing wiring
    - `grep -c "CLAUDE_SURFACE" tests/test-3551-tri-polar.cjs` prints at least 1 and `grep -c "dry-run" tests/test-3551-tri-polar.cjs` prints at least 2
  </acceptance_criteria>
  <done>A failing test pins every surface's go-live and AC1 end to end.</done>
</task>

<task type="auto">
  <name>Task 2: The close-out evaluator step (Desktop and Cowork)</name>
  <files>lib/mcp/stop-gate-handler.cjs</files>
  <read_first>
    - lib/mcp/stop-gate-handler.cjs: re-read from disk NOW (peer-touched by 357); `git status --short lib/mcp/stop-gate-handler.cjs` must be empty before editing, otherwise wait, re-read, never stage or revert a peer's diff
    - the Desktop, Cowork and AC1 legs of tests/test-3551-tri-polar.cjs
  </read_first>
  <action>Add `_closeOutAmbientTrigger(roomDir, sessionId)` in the _closeOut* style (try/catch, lazy-require ../core/ambient-trigger.cjs inside the try, return false on failure, never throw), commented "Phase 355.1 AMB-06 (CONTEXT: right after _closeOutStateMd): Desktop and Cowork run the same ambient evaluator as the CLI Stop hook, after the STATE.md refresh so class (c) reads a fresh stage; the fire-once step that follows marks only handles a pull noted in memory, so a child started here can never be marked by this close-out; Ruling 1: the child stamps through brain-client directly", calling evaluateAndMaybeSpawn(roomDir, { seam: 'closeout', sessionId }). In closeOutRoom, call `_closeOutAmbientTrigger(roomDir, sessionId);` on the line immediately after `_closeOutStateMd(roomDir);` (so it precedes plan 355.1-13's `_closeOutEurekaFireOnce(roomDir);`). Change no other line; closeOutRoom's return object is unchanged; no stdout write. Run the Desktop, Cowork and AC1 legs, tests/test-3551-mcp-fire-once.cjs and every existing stop-gate test. Commit with `git commit --only lib/mcp/stop-gate-handler.cjs`: `feat(355.1-14): stop_gate_check close-out runs the ambient evaluator on Desktop and Cowork (AMB-06, Ruling 1)`.</action>
  <verify>
    <automated>node tests/test-3551-mcp-fire-once.cjs && for t in $(ls tests/*.cjs | grep -iE "stop-gate|stop_gate"); do node "$t" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command exits 0
    - `grep -c "_closeOutAmbientTrigger(roomDir, sessionId);" lib/mcp/stop-gate-handler.cjs` prints 1 and `grep -A1 "_closeOutStateMd(roomDir);" lib/mcp/stop-gate-handler.cjs | grep -c "_closeOutAmbientTrigger(roomDir, sessionId);"` prints 1 (it sits right after the STATE.md refresh)
    - `git diff HEAD~1 -- lib/mcp/stop-gate-handler.cjs | grep '^-' | grep -v '^---' | grep -vc '^-\s*$'` prints 0 (additive only)
  </acceptance_criteria>
  <done>Desktop and Cowork start the ambient run at every close-out.</done>
</task>

<task type="auto">
  <name>Task 3: The CLI Stop script and the async hooks.json entry</name>
  <files>scripts/ambient-stop.cjs, hooks/hooks.json</files>
  <read_first>
    - hooks/hooks.json: re-read from disk NOW (peer-touched); `git status --short hooks/hooks.json` must be empty before editing, otherwise wait, re-read, never stage or revert a peer's diff
    - scripts/gsd-graph-derive-sweep.cjs (Stop contract) and scripts/auto-explore-fingerprint.cjs lines 44-77, 170-180
    - lib/core/resolve-active-room.cjs resolveSessionRoom (lines 481-530) and lib/core/ambient-trigger.cjs (evaluateAndMaybeSpawn, _internal.setSpawnImpl, AMBIENT_EVAL_BUDGET_MS)
    - the CLI legs of tests/test-3551-tri-polar.cjs
  </read_first>
  <action>
Create scripts/ambient-stop.cjs ('use strict', BSL header: "Phase 355.1 AMB-06: the CLI Stop entry of the ambient trigger. Registered in hooks/hooks.json with async: true (hook timeouts are seconds; async hooks are not bounded by the harness, research C5), so this script bounds itself through the evaluator's budget and a hard exit timer. Writes NOTHING to stdout outside --dry-run: an async hook's output can reach the next turn as context, and this hook has nothing to say. Exit 0 on every path. HITL shape: none (machinery; the only human decision is the gate answer on the card). Canon Part 8: argv, logs and files carry no room bytes. Hyphens only."). CLI router by a process.argv switch (no Commander): requires `--stop`; accepts `--dry-run` (tests only, never registered in hooks.json). Flow: install uncaughtException and unhandledRejection handlers that exit 0; arm an unref'd timer that exits 0 after AMBIENT_EVAL_BUDGET_MS plus 1500 ms; read stdin defensively (the fingerprint's readStdin shape); stop_hook_active true or a missing session_id exits 0; resolve the room with resolveSessionRoom({ sessionId, requireOwnership: true }); no room, or the '__no_room__' sentinel, exits 0; in --dry-run, set ambientTrigger._internal.setSpawnImpl to a capture that records its args and returns a stub child with unref, run evaluateAndMaybeSpawn(roomDir, { seam: 'stop_hook', sessionId }), print one JSON line { decision, argv } and exit 0; otherwise run evaluateAndMaybeSpawn with the same opts and exit 0 printing nothing. stderr only under MINDRIAN_AMBIENT_DEBUG === '1', enums only. The script itself never spawns (the evaluator does).
hooks/hooks.json: append ONE new group to the Stop array: { "hooks": [ { "type": "command", "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/ambient-stop.cjs\" --stop", "timeout": 5, "async": true } ] }. Change no other entry; keep the file's 2-space JSON formatting.
Run tests/test-3551-tri-polar.cjs (all legs), every test that parses hooks.json (`grep -ln "hooks.json" tests/*.cjs`, run each), tests/test-connector-tier-d-hooks.cjs, tests/test-auto-explore-canon-part-8.cjs, tests/test-3551-one-spawner.cjs (pinned set unchanged) and a JSON parse of hooks/hooks.json. Commit with `git commit --only scripts/ambient-stop.cjs hooks/hooks.json`: `feat(355.1-14): async Stop entry evaluates the room delta on CLI (AMB-06)`.
  </action>
  <verify>
    <automated>node tests/test-3551-tri-polar.cjs && node tests/test-3551-one-spawner.cjs && node tests/test-connector-tier-d-hooks.cjs && node tests/test-auto-explore-canon-part-8.cjs && node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))" && for t in $(grep -ln "hooks.json" tests/*.cjs); do node "$t" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command exits 0; the tri-polar test prints `FAIL: 0` and one PASS line each for CLI, Desktop and Cowork
    - `grep -c "ambient-stop.cjs" hooks/hooks.json` prints 1 and `grep -c '"async": true' hooks/hooks.json` prints 1 and `grep -c -- "--dry-run" hooks/hooks.json` prints 0
    - `grep -vE '^\s*(//|\*|/\*)' scripts/ambient-stop.cjs | grep -cE "spawn\(|child_process|require\('zod'\)|fetch\("` prints 0
    - `grep -cP '\x{2014}' scripts/ambient-stop.cjs` prints 0
  </acceptance_criteria>
  <done>On the CLI the room is evaluated after every turn in the background and the turn is never held; all three surfaces are live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code harness -> async Stop hook (stdin JSON) | Untrusted input shape from the host |
| MCP stop_gate_check -> close-out side effects | A model-invoked tool now starts a background run |
| MCP server stdio -> JSON-RPC channel | Any stray stdout corrupts the protocol |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-59 | Denial of service | a hook holding the turn | mitigate | async: true, timeout 5 seconds, the evaluator's own budget, a hard unref'd exit timer; CLI leg asserts exit under 5 s |
| T-3551-60 | Tampering | malformed or hostile stdin | mitigate | Defensive JSON parse; missing session or stop_hook_active exits 0; room only through resolveSessionRoom with requireOwnership |
| T-3551-61 | Tampering | JSON-RPC stdout corruption on Desktop / Cowork | mitigate | No stdout in the close-out step (spy leg); the child is detached with stdio ignore |
| T-3551-62 | Information disclosure | context injection by an async hook's output | mitigate | The Stop script prints nothing outside --dry-run; --dry-run is absent from hooks.json (acceptance grep) |
| T-3551-63 | Denial of service | duplicate runs from CLI flag-on double evaluation | mitigate | Claim, in_flight and lock ladder; three-seams-one-run leg |
| T-3551-64 | Spoofing | the wrong room evaluated on Stop | mitigate | Session-bound resolution with requireOwnership; the '__no_room__' sentinel and unbound sessions are no-ops |
</threat_model>

## Artifacts this phase produces

This plan's share:
- Script `scripts/ambient-stop.cjs` (flags `--stop`, `--dry-run`; env `MINDRIAN_AMBIENT_DEBUG`)
- `hooks/hooks.json` Stop entry `node "${CLAUDE_PLUGIN_ROOT}/scripts/ambient-stop.cjs" --stop` with `"timeout": 5`, `"async": true`
- `lib/mcp/stop-gate-handler.cjs` helper `_closeOutAmbientTrigger(roomDir, sessionId)` called in `closeOutRoom` right after `_closeOutStateMd`
- Test `tests/test-3551-tri-polar.cjs`

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently; `lib/mcp/stop-gate-handler.cjs` and `hooks/hooks.json` are peer-touched. Re-read each from disk immediately before editing; if `git status --short` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it; wait and re-read, or stop and report. Commit with `git commit --only` on this plan's paths; no `state.*` writes while a peer executes. The new hook reaches user installs only after a release (the "not live until released" rule); the close-out plan's handoff entry says so.

<verification>
- `node tests/test-3551-tri-polar.cjs`
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set within BASE_3551's)
</verification>

<success_criteria>
- AC1 on every surface: N claims, no command, one background run, one card next turn, no second run for the same delta
- One test leg per surface; the turn is never held
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-14-SUMMARY.md` when done.
</output>
