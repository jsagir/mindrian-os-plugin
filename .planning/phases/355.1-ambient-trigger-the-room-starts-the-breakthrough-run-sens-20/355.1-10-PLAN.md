---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 10
type: execute
wave: 5
depends_on: ["355.1-05", "355.1-08"]
files_modified:
  - tests/test-3551-double-card.cjs
  - lib/core/sensors/sensor-room-delta.cjs
  - lib/core/insight-sensors.cjs
  - scripts/auto-explore-drain.cjs
autonomous: true
requirements: [AMB-05, AMB-06]
canon_parts: [8, 11, 12]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["lib/core/insight-sensors.cjs deriveTurnSignals first_material", "scripts/auto-explore-drain.cjs Tier D hook"]
  gate_impact: "SENS-01 and the Phase 117 drain stand down for a material whose stamped SENS-13 card won or is still being computed; no reach, sensor or command is added or removed."
  explanation: "Navigator Ruling 5 at the 355.1 checkpoint: on delta class (e) the stamped card wins and the Phase 117 card shows only when no stamped finding exists for that material id, so the navigator sees one card, not two."
tags: [ruling-5, double-card, sens-01, drain, one-card]

must_haves:
  truths:
    - "Navigator Ruling 5 (CONTEXT, locked, approved at the checkpoint): class (e) double card: SENS-13's stamped card wins; SENS-01 and the Phase 117 drain show the auto-explore card only when no stamped finding exists for that material_id"
    - "While the ambient run for a material is in flight (delta state fresh, material_id equal, run_state started or running) the 117 card for that material is DEFERRED, not dropped: the drain skips it this turn and the first_material signal does not fire for it"
    - "Once the ambient ledger's stamped_materials lists the material (surfaced_via sens13), the 117 card for it stands down permanently; when the ambient run completed with surfaced_via none, the 117 card shows as before"
    - "One helper decides, read-only: isMaterialHeldForStampedCard(roomDir, materialId, { now }) in the SENS-21 module, replicating the ledger relpath (the url-ingest sensor precedent) and never writing; the drain and the first_material scan both call it"
    - "The drain stays a conforming Tier D hook (tests/test-connector-tier-d-hooks.cjs): no new spawn, no brain-client, its fire still routes through navigation"
    - "Canon Part 12 (AMB-05): one action per delta, one card"
  artifacts:
    - path: "lib/core/sensors/sensor-room-delta.cjs"
      provides: "isMaterialHeldForStampedCard, the read-only Ruling 5 helper"
      exports: ["isMaterialHeldForStampedCard", "AMBIENT_LEDGER_RELPATH_REPLICA"]
    - path: "tests/test-3551-double-card.cjs"
      provides: "defer while running, stand down after a stamped win, show when none"
      min_lines: 80
  key_links:
    - from: "scripts/auto-explore-drain.cjs readFindingFiles"
      to: "lib/core/sensors/sensor-room-delta.cjs isMaterialHeldForStampedCard"
      via: "skip held findings"
      pattern: "isMaterialHeldForStampedCard"
    - from: "lib/core/insight-sensors.cjs deriveTurnSignals (first_material scan)"
      to: "lib/core/sensors/sensor-room-delta.cjs isMaterialHeldForStampedCard"
      via: "skip held markers"
      pattern: "isMaterialHeldForStampedCard"
---

<objective>
Navigator Ruling 5, so a filed artifact produces one card, not two. After plan 355.1-09 the Phase 117 child writes its old auto-explore finding AND runs the stamped ambient composition for the same material. Without this plan the navigator could see the unstamped 117 card (through the UserPromptSubmit drain and SENS-01) and then the stamped SENS-13 card for the same material. One read-only helper tells both 117 surfaces to wait while the stamped run is in flight and to stand down for good once the stamped card won.

Output: the helper, two small read-side edits, one test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md
@lib/core/sensors/sensor-room-delta.cjs
@scripts/scout-cadence-guard.cjs
@tests/helpers/fixture-room-3551.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED double-card test</name>
  <files>tests/test-3551-double-card.cjs</files>
  <read_first>
    - scripts/auto-explore-drain.cjs (whole file: readFindingFiles :80-105, the one-finding-per-turn cap, how a finding is marked surfaced, the navigation fire)
    - lib/core/insight-sensors.cjs lines 330-380 (deriveTurnSignals, the (b) first_material scan over auto-explore-*.json markers, isFreshFile, isMarkerOwnedByCaller)
    - lib/core/sensors/sensor-url-ingest.cjs lines 85-95 and 180-215 (the read-only replicated-relpath ledger precedent, fail-closed on corrupt)
    - scripts/scout-cadence-guard.cjs (writeRoomDeltaState, recordAmbientRun with stamped_materials, AMBIENT_LEDGER_RELPATH)
    - tests/test-sens01-first-material-fires.cjs and tests/test-connector-tier-d-hooks.cjs (what must stay green)
  </read_first>
  <behavior>
    - helper: isMaterialHeldForStampedCard(room, 'ab12cd34ef56') is false with no ledger and no delta state; true when a fresh delta state has material_id 'ab12cd34ef56' and run_state 'started' or 'running'; false when that state is 'completed' with surfaced_via 'none'; true when the ledger's stamped_materials contains the id (regardless of the delta state's age); false for a different material id; false for a malformed id (non-hex); a corrupt ledger fails closed to false for the stamped-list branch (the 117 card shows; never hide a card on a bad file) while a fresh running delta state still returns true
    - the helper writes nothing (fs write spy) and is sync
    - AMBIENT_LEDGER_RELPATH_REPLICA equals scout-cadence-guard's AMBIENT_LEDGER_RELPATH (drift pin)
    - drain: with a fresh owned auto-explore-ab12cd34ef56.json finding and a running delta state for that id, running scripts/auto-explore-drain.cjs with the hook stdin shape produces no auto-explore directive for it; after the state becomes completed with surfaced_via 'none', the same run produces the directive exactly as before; after recordAmbientRun lists the id in stamped_materials, it never produces it again
    - SENS-01: deriveTurnSignals for that room does not include 'first_material' while the material is held, and does include it once the run completed with surfaced_via 'none' (the existing sensorFirstMaterial then fires as before)
    - a different material's finding is unaffected in every case
  </behavior>
  <action>Write tests/test-3551-double-card.cjs ('use strict', hygiene-355 preamble; buildDeltaRoom rooms; write the 117 finding file in the exact shape auto-explore-fire.cjs writes; write delta states and ledgers through scout-cadence-guard's writers; run the drain with child_process.spawnSync and an isolated HOME) implementing every behavior line. Run it; it fails (helper absent). Commit `test(355.1-10): add failing one-card-per-material test (Ruling 5, AMB-05)`.</action>
  <verify>
    <automated>node tests/test-3551-double-card.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The test exists, runs and fails on the missing helper
    - `grep -c "stamped_materials" tests/test-3551-double-card.cjs` prints at least 1 and `grep -c "first_material" tests/test-3551-double-card.cjs` prints at least 1
  </acceptance_criteria>
  <done>A failing test pins Ruling 5 on both 117 surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: The read-only helper and the two stand-down edits</name>
  <files>lib/core/sensors/sensor-room-delta.cjs, lib/core/insight-sensors.cjs, scripts/auto-explore-drain.cjs</files>
  <read_first>
    - lib/core/sensors/sensor-room-delta.cjs, lib/core/insight-sensors.cjs and scripts/auto-explore-drain.cjs: re-read each from disk NOW (insight-sensors.cjs is peer-touched); `git status --short` on each must be empty before editing, otherwise a peer is editing: wait, re-read, never stage or revert
    - tests/test-3551-double-card.cjs (the RED legs)
  </read_first>
  <action>
sensor-room-delta.cjs: add `AMBIENT_LEDGER_RELPATH_REPLICA = path.join('.mindrian', 'ambient-run-ledger.json')` (comment: replicated from scripts/scout-cadence-guard.cjs, pinned equal by tests/test-3551-double-card.cjs; the sensor module never requires scripts/) and `isMaterialHeldForStampedCard(roomDir, materialId, opts)`: returns false unless materialId matches /^[0-9a-f]{8,64}$/; true when the fresh delta state (the module's own replicated read + freshness helpers) has that material_id and run_state 'started' or 'running'; true when the ledger file parses and its stamped_materials array includes materialId; false on every other path including parse failures; sync, read-only, never throws. Export both. Comment: "Navigator Ruling 5 (Phase 355.1 checkpoint): the stamped card wins on delta class (e)".
insight-sensors.cjs: in deriveTurnSignals's (b) first_material loop, after the ownership check, skip a candidate whose payload.material_id (or the id parsed from its `auto-explore-<id>.json` file name when the payload lacks it) is held per isMaterialHeldForStampedCard(roomDir, id); add the require of isMaterialHeldForStampedCard to the existing place-2 require line of the sensor module (no new require line elsewhere); one comment citing Ruling 5. Change nothing else.
auto-explore-drain.cjs: in readFindingFiles (or at the point the drain picks its one finding), skip a finding whose material_id is held; require the helper from ../lib/core/sensors/sensor-room-delta.cjs; one comment citing Ruling 5; keep the navigation fire, the cap and every other line as is.
Run tests/test-3551-double-card.cjs, tests/test-sens01-first-material-fires.cjs, tests/test-acpt-03-first-material-explore.cjs, tests/test-connector-tier-d-hooks.cjs, tests/test-detection-routing-local-only.cjs, tests/test-3551-lockstep.cjs, tests/test-3551-sensor-fires.cjs, tests/test-sensors-part8-sweep.cjs, tests/test-sensors-routing-fence.cjs and every test requiring auto-explore-drain.cjs (`grep -ln "auto-explore-drain" tests/*.cjs`). Commit with `git commit --only lib/core/sensors/sensor-room-delta.cjs lib/core/insight-sensors.cjs scripts/auto-explore-drain.cjs`: `feat(355.1-10): Phase 117 card stands down when the stamped card wins (Ruling 5, AMB-05)`.
  </action>
  <verify>
    <automated>node tests/test-3551-double-card.cjs && node tests/test-sens01-first-material-fires.cjs && node tests/test-connector-tier-d-hooks.cjs && node tests/test-detection-routing-local-only.cjs && node tests/test-3551-lockstep.cjs && node tests/test-sensors-routing-fence.cjs && node tests/test-sensors-part8-sweep.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command exits 0; the double-card test prints `FAIL: 0`
    - `grep -c "isMaterialHeldForStampedCard" lib/core/insight-sensors.cjs` prints at least 2 and `grep -c "isMaterialHeldForStampedCard" scripts/auto-explore-drain.cjs` prints at least 2
    - `git diff HEAD~1 -- lib/core/insight-sensors.cjs scripts/auto-explore-drain.cjs | grep '^-' | grep -v '^---' | grep -vc '^-\s*$'` prints 0 or only the single edited require line
    - `grep -vE '^\s*(//|\*|/\*)' lib/core/sensors/sensor-room-delta.cjs | grep -cE "writeFileSync|renameSync|child_process"` prints 0
  </acceptance_criteria>
  <done>A filed artifact yields one card: the stamped one when it wins, the Phase 117 one otherwise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger / delta-state files -> 117 surfacing | Local files decide whether an existing card shows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-44 | Denial of service | a bad ledger hiding every 117 card | mitigate | Corrupt ledger fails OPEN for the stamped-list branch (the card shows); only a fresh running state or a parsed list entry holds a card |
| T-3551-45 | Repudiation | a double card nagging the navigator | mitigate | Defer while in flight, stand down after a stamped win; drain and SENS-01 legs |
| T-3551-46 | Tampering | the Tier D drain losing its chokepoint routing | mitigate | Only a skip added; tests/test-connector-tier-d-hooks.cjs and the removed-lines diff check |
</threat_model>

## Artifacts this phase produces

This plan's share:
- `lib/core/sensors/sensor-room-delta.cjs` exports `isMaterialHeldForStampedCard(roomDir, materialId, opts)`, `AMBIENT_LEDGER_RELPATH_REPLICA`
- Skip rule in `lib/core/insight-sensors.cjs` deriveTurnSignals (first_material) and in `scripts/auto-explore-drain.cjs` readFindingFiles
- Test `tests/test-3551-double-card.cjs`

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently; `lib/core/insight-sensors.cjs` is peer-touched. Re-read every file from disk immediately before editing; if `git status --short` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it; wait and re-read, or stop and report. Commit with `git commit --only` on this plan's paths; no `state.*` writes while a peer executes.

<verification>
- `node tests/test-3551-double-card.cjs`
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set within BASE_3551's)
</verification>

<success_criteria>
- Ruling 5 holds on both Phase 117 surfaces; unrelated materials unaffected
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-10-SUMMARY.md` when done.
</output>
