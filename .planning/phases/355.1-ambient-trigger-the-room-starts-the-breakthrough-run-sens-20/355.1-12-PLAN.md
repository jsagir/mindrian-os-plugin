---
phase: 355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20
plan: 12
type: execute
wave: 6
depends_on: ["355.1-08", "355.1-11"]
files_modified:
  - tests/test-3551-prose.cjs
  - skills/larry-personality/SKILL.md
  - tests/fixtures/3551/prose/desktop-find-the-problem.md
  - tests/fixtures/3551/prose/desktop-pursue-or-drop.md
  - tests/fixtures/3551/prose/cowork-neutral.md
  - tests/fixtures/3551/prose/desktop-room-delta-fallback.md
  - commands/auto-explore.md
  - data/connector-registry.json
  - data/connector-coverage-ledger.json
  - data/brain-orchestration-projection.json
autonomous: true
requirements: [AMB-06, AMB-07, AMB-05, AMB-08]
canon_parts: [3, 7, 8, 11, 12]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: ["/mos:auto-explore", "skills/larry-personality"]
  surfaces_removed: []
  spine_consumed: ["data/connector-registry.json", "scripts/build-connector-registry.cjs", "commands/auto-explore.md connector.sensor_triggers"]
  gate_impact: "/mos:auto-explore is re-described as the fallback, never the way in, and lists SENS-21 in sensor_triggers beside SENS-01 (regenerated registry, hitl_shape F.3 kept); Larry's SKILL gains one ambient-findings rule; no new command, no new MCP tool, no seventh reach."
  explanation: "AC8 and AMB-07's tri-polar half: every user-facing text says the breakthrough card arrives on its own, the typed command is only the fallback, and Larry narrates the same framing phrase and stamp on Desktop and Cowork that the CLI card shows."
tags: [larry-skill, prose-fixtures, auto-explore-fallback, tri-polar, ac8]

must_haves:
  truths:
    - "CONTEXT 'Tri-polar (AMB-06, locked)' / PRD AC8: /mos:auto-explore is documented as a fallback, never as the way in; no user-facing text in the five producer commands, auto-explore.md or Larry's SKILL presents a typed command as the way to get a breakthrough"
    - "CONTEXT 'Strategy, not keywords (AMB-07, locked)': on Desktop and Cowork Larry opens an ambient SENS-13 narration with FRAMING_PHRASES[evidence.framing] verbatim, then the 355 Desktop stamp sentences verbatim; the same phrase and the same stamp path appear on the CLI card (tri-polar), proven against recorded prose fixtures"
    - "Navigator Ruling 1: ambient findings on Desktop and Cowork carry a stamp the background run computed on that machine, so Larry narrates them like any stored stamp; the 355-23 'Not yet checked; run the CLI to verify.' line stays for findings with no stored stamp"
    - "SENS-21 narration: Larry says plainly, once, that the background run could not start on this surface and names /mos:auto-explore as the fallback; never framed as the way to a breakthrough"
    - "Canon Part 12: no praise, no grade, no count of findings in any Larry line the SKILL rule prescribes or any fixture records"
    - "Canon Part 11: commands/auto-explore.md keeps hitl_shape F.3; connector.sensor_triggers becomes [SENS-01, SENS-21]; the regenerated registry files come from the builders' own write modes, never hand edits; all four born-wired gates green. Theo-side analog stated: Theo's projection of the plugin's sensor_index gains a SENS-21 Sensor node on its next re-emission; no Theo code is written"
  artifacts:
    - path: "skills/larry-personality/SKILL.md"
      provides: "### Ambient findings (Phase 355.1) under the 355 stamp section"
      contains: "Ambient findings (Phase 355.1)"
    - path: "commands/auto-explore.md"
      provides: "fallback wording and SENS-21 in sensor_triggers"
      contains: "fallback"
    - path: "tests/test-3551-prose.cjs"
      provides: "tri-polar same-phrase proof, render guards over prose, AC8 checks"
      min_lines: 90
    - path: "tests/fixtures/3551/prose/desktop-find-the-problem.md"
      provides: "recorded Desktop narration for a framed ambient finding"
  key_links:
    - from: "tests/fixtures/3551/prose/*.md"
      to: "lib/core/direction-convention.cjs FRAMING_PHRASES + verification-stamp-format formatStampLines(stamp, 'desktop')"
      via: "body contains both verbatim"
      pattern: "framing"
    - from: "commands/auto-explore.md connector.sensor_triggers"
      to: "lib/core/sensors/sensor-room-delta.cjs SENSOR_ID"
      via: "SENS-21 listed; registry regenerated"
      pattern: "SENS-21"
---

<objective>
AC8 and the prose half of AMB-06 / AMB-07: the words users and Larry see. The auto-explore command stops being described as how to get the run and becomes the fallback it is. Larry learns one rule for ambient findings on hookless surfaces: open with the confirmed framing phrase, reproduce the stored stamp sentences, never add a number, and when the run could not start, say so once and name the fallback. Recorded prose fixtures and one test prove the Desktop and Cowork narration match the CLI card word for word.

Output: the SKILL rule, four prose fixtures, the auto-explore.md edit, regenerated registry data, one test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CONTEXT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-CHECKPOINT.md
@.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-HOOKED-AUDIT.md
@lib/core/direction-convention.cjs
@lib/core/verification-stamp-format.cjs
@tests/helpers/hygiene-355.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED prose test</name>
  <files>tests/test-3551-prose.cjs</files>
  <read_first>
    - tests/test-355-tri-polar.cjs and tests/fixtures/355/prose/*.md (355-23's fixture frontmatter shape: surface, stamp, finding; how bodies are checked against formatStampLines(stamp, 'desktop'))
    - skills/larry-personality/SKILL.md section `## Cross-connection stamps (Phase 355)`
    - commands/auto-explore.md (whole file) and commands/eureka.md, commands/scout.md, commands/find-connections.md, commands/find-bottlenecks.md, commands/whitespace.md (the files the AC8 leg scans)
    - lib/hmi/dial-label-composer.cjs framed variant (plan 355.1-11) and lib/core/verification-stamp-format.cjs formatStampLines, formatUncheckedDesktop
  </read_first>
  <behavior>
    - SKILL: skills/larry-personality/SKILL.md contains `### Ambient findings (Phase 355.1)` once, placed inside the `## Cross-connection stamps (Phase 355)` section; the subsection contains every FRAMING_PHRASES value verbatim, the word 'fallback' on every line that names /mos:auto-explore, and none of: a digit next to finding / connection / opportunity, great / amazing / excellent / brilliant, U+2014 (the banned-claim words breakthrough / validated / proven are checked on the fixtures and labels, not on the SKILL text, because the rule itself must name them to forbid them)
    - fixtures: exactly four files under tests/fixtures/3551/prose/; each has frontmatter keys surface (desktop | cowork), signal (eureka_bridge | room_delta), framing (a FRAMING_IDS member or empty for room_delta), stamp (a stamp object for eureka_bridge fixtures, null for room_delta); each body opens with one De Stijl glyph from the Larry SKILL list
    - tri-polar: for each eureka_bridge fixture, the body contains FRAMING_PHRASES[framing] verbatim and every sentence of formatStampLines(stamp, 'desktop') verbatim; composeLabel('deep_research', { signal: 'eureka_bridge', stamp_verification, stamp_path, framing }) for the same stamp contains the same framing phrase and the same formatted path text
    - fallback fixture: the room_delta body names /mos:auto-explore on a line that contains 'fallback' and says the run could not start on this surface
    - render guards over every fixture body: no D-30 decimal, no `[0-9]+%`, no count next to a finding word, no praise token, no U+2014; one planted negative control (a body copy with '0.91' inserted) is caught
    - AC8: commands/auto-explore.md's description and body both contain 'fallback'; its connector.sensor_triggers lists SENS-01 and SENS-21; its hitl_shape is still "F.3"; no line in commands/eureka.md, commands/auto-explore.md, commands/scout.md, commands/find-connections.md, commands/find-bottlenecks.md, commands/whitespace.md or the new SKILL subsection contains both a `/mos:` command and the word breakthrough
  </behavior>
  <action>Write tests/test-3551-prose.cjs ('use strict', hygiene-355 preamble, attempts() === 0 last; parse frontmatter with the same local helper 355-23's tri-polar test uses) implementing every behavior line. Run it; it fails (SKILL subsection, fixtures and the auto-explore edits absent). Commit `test(355.1-12): add failing ambient prose and fallback-wording test (AMB-06, AMB-07)`.</action>
  <verify>
    <automated>node tests/test-3551-prose.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The test exists, runs and fails on the missing SKILL subsection and fixtures
    - `grep -c "sensor_triggers" tests/test-3551-prose.cjs` prints at least 1 and `grep -c "negative control" tests/test-3551-prose.cjs` prints at least 1
  </acceptance_criteria>
  <done>A failing test pins the fallback wording and the same-words-everywhere rule.</done>
</task>

<task type="auto">
  <name>Task 2: Larry's ambient rule, the prose fixtures, the auto-explore fallback wording, the regenerated registry</name>
  <files>skills/larry-personality/SKILL.md, tests/fixtures/3551/prose/desktop-find-the-problem.md, tests/fixtures/3551/prose/desktop-pursue-or-drop.md, tests/fixtures/3551/prose/cowork-neutral.md, tests/fixtures/3551/prose/desktop-room-delta-fallback.md, commands/auto-explore.md, data/connector-registry.json, data/connector-coverage-ledger.json, data/brain-orchestration-projection.json</files>
  <read_first>
    - skills/larry-personality/SKILL.md and commands/auto-explore.md: re-read from disk NOW (SKILL.md is peer-touched by 357 and 358); `git status --short` on each must be empty before editing, otherwise wait, re-read, never stage or revert a peer's diff
    - tests/test-3551-prose.cjs (the RED legs)
    - scripts/build-connector-registry.cjs and scripts/build-orchestration-projection.cjs headers (their write modes versus --check)
    - /home/jsagi/Theo/notes/graph-rulebook.md lines 395-437 (READ ONLY: how Theo projects Sensor nodes from sensor_index; never edit a Theo file)
  </read_first>
  <action>
SKILL.md: inside `## Cross-connection stamps (Phase 355)`, append `### Ambient findings (Phase 355.1)` (hyphens only, Feynman prose, no praise, no counts): (1) an ambient finding arrives on its own when the room changed; never tell the navigator to type a command to get one; (2) when a SENS-13 reach carries evidence.framing, open with the De Stijl glyph, then the phrase for that framing verbatim (quote all three FRAMING_PHRASES values), then the 355 Desktop stamp sentences verbatim, then the gate question from the 355 rule; (3) ambient findings on Desktop and Cowork carry a stamp the background run computed on this machine (Ruling 1), so narrate them like any stored stamp; the 'Not yet checked; run the CLI to verify.' line stays for findings with no stored stamp; (4) never count findings, never praise, never grade, never call a finding a breakthrough (Canon Part 12); (5) when a context_block reach carries signal room_delta (SENS-21), say once and plainly that the room changed but the background run could not start on this surface, and name /mos:auto-explore as the fallback.
Fixtures: write the four files with the frontmatter keys from the RED test; stamps built the way 355-23's fixtures build theirs (a verified strong stamp for the find-the-problem fixture, a verified indirect stamp for pursue-or-drop and for the Cowork neutral fixture); each body a short Larry turn written to the new rule, formatter sentences verbatim inside hedged prose; the fallback fixture has signal room_delta, no stamp and no framing.
auto-explore.md: description becomes a fallback statement, for example "Fallback: run auto-explore on one file by hand when the ambient run could not start on this surface (the room starts it on its own; Phase 355.1)"; keep help_jtbd, layer, hitl_shape "F.3", hitl_why and the rest; connector.sensor_triggers becomes [SENS-01, SENS-21]; retitle the body heading to "/mos:auto-explore -- Fallback (the room normally starts this on its own)", add a short paragraph that on CLI the PostToolUse fingerprint and the Stop hook start the run, and on Desktop and Cowork the stop_gate_check close-out does, so this command is the fallback, never the way in; update the Tri-polar surfaces table's Desktop row from NONE to the close-out path with this command as the recovery path; teaching line says fallback.
Regenerate: run `node scripts/build-connector-registry.cjs` in its write mode (read its header for the exact invocation; never hand-edit data files), then `node scripts/build-orchestration-projection.cjs` in its write mode only if its --check reports drift; `git status --short data/` must show only files this plan lists; if data/framework-names.json changed as a side effect of this plan's own regeneration run, restore it with `git checkout -- data/framework-names.json` (it is this run's own diff, not a peer's) and record that.
Run tests/test-3551-prose.cjs, tests/test-355-tri-polar.cjs, tests/test-355-no-decimal.cjs, the four born-wired gates, `node scripts/check-cirs-declaration.cjs --check .planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-12-PLAN.md`, and every test that reads auto-explore.md (`grep -ln "auto-explore.md" tests/*.cjs`). Commit with `git commit --only` on exactly the changed paths (fixtures are new tracked files under tests/): `docs(355.1-12): auto-explore is the fallback; Larry narrates ambient findings with the confirmed framing (AMB-06, AMB-07)`.
  </action>
  <verify>
    <automated>node tests/test-3551-prose.cjs && node tests/test-355-tri-polar.cjs && node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check && node scripts/check-shape-declaration.cjs --check && node scripts/check-render-coverage.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command exits 0; the prose test prints `FAIL: 0`
    - `grep -c "### Ambient findings (Phase 355.1)" skills/larry-personality/SKILL.md` prints 1
    - `grep -c "fallback" commands/auto-explore.md` prints at least 3 and `grep -c "SENS-21" commands/auto-explore.md` prints 1 and `grep -c 'hitl_shape: "F.3"' commands/auto-explore.md` prints 1
    - `grep -c "SENS-21" data/connector-registry.json` prints at least 1
    - `ls tests/fixtures/3551/prose/*.md | wc -l` prints 4
    - `git diff HEAD~1 -- skills/larry-personality/SKILL.md commands/auto-explore.md | grep '^+' | grep -v '^+++' | grep -cP '\x{2014}'` prints 0
    - `git -C /home/jsagi/Theo status --short` shows nothing this session created
  </acceptance_criteria>
  <done>Every user-facing word says the card arrives on its own; the typed command is the fallback; Larry says the same thing on every surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host-generated Larry prose -> user | Plugin code cannot filter it at runtime; the SKILL rule and fixtures are the control |
| command frontmatter -> connector registry -> Theo projection | A frontmatter edit changes generated data and Theo's next re-emission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3551-51 | Spoofing | Larry counting, praising or scoring an ambient finding | mitigate | SKILL rule forbids it; recorded fixtures checked offline with a planted negative control (stated limit: runtime prose is host-generated) |
| T-3551-52 | Repudiation | a typed command presented as the way in | mitigate | AC8 legs over the five producer commands, auto-explore.md and the SKILL subsection |
| T-3551-53 | Tampering | hand-edited generated registry data | mitigate | Builders' write modes only; --check gates green; data/ status limited to the listed files |
| T-3551-54 | Tampering | a Theo-side edit | mitigate | Theo rulebook read only; acceptance checks ~/Theo has no session-created change |
</threat_model>

## Artifacts this phase produces

This plan's share:
- SKILL subsection `### Ambient findings (Phase 355.1)` in `skills/larry-personality/SKILL.md`
- Prose fixtures `tests/fixtures/3551/prose/desktop-find-the-problem.md`, `desktop-pursue-or-drop.md`, `cowork-neutral.md`, `desktop-room-delta-fallback.md` (frontmatter keys `surface`, `signal`, `framing`, `stamp`)
- `commands/auto-explore.md` fallback description and body; `connector.sensor_triggers: [SENS-01, SENS-21]`
- Regenerated `data/connector-registry.json` (and the coverage ledger / orchestration projection only if their builders report drift)
- Test `tests/test-3551-prose.cjs`

Execution notes (shared tree): Phases 357, 358 and 361 execute in this working tree concurrently; `skills/larry-personality/SKILL.md` is peer-touched. Re-read every file from disk immediately before editing; if `git status --short` shows a diff this session did not make, it is a peer's work in flight: never stage, revert or overwrite it; wait and re-read, or stop and report. Commit with `git commit --only` on exactly this plan's changed paths; no `state.*` writes while a peer executes.

<verification>
- `node tests/test-3551-prose.cjs`
- `bash tests/run-all-3551.sh`
- `bash tests/run-all-355.sh` (no regression on the parent phase)
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`
- `node scripts/check-shape-declaration.cjs --check`
- `node scripts/doctor.cjs --acceptance` (failing-point set within BASE_3551's)
</verification>

<success_criteria>
- AC8: auto-explore docs say fallback; no user-facing text presents a typed command as the way to a breakthrough
- Desktop, Cowork and CLI show the same framing phrase and the same stamp for the same finding
</success_criteria>

<output>
Create `.planning/phases/355.1-ambient-trigger-the-room-starts-the-breakthrough-run-sens-20/355.1-12-SUMMARY.md` when done.
</output>
