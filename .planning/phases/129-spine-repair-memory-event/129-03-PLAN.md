---
phase: 129-spine-repair-memory-event
plan: 03
type: execute
wave: 2
depends_on: ["129-01"]
files_modified:
  - scripts/jtbd-command.cjs
  - scripts/operator-command.cjs
  - lib/conversation/operator.cjs
  - lib/core/navigation/edges.cjs
  - tests/test-129-state-transition-events.cjs
autonomous: true
requirements: [SPINE-EVENTS, STATE-AUTHORITY, FOLLOWS-FROM]
canon_parts:
  - Part 4 (Every Choice Is Graph Data -- JTBD + operator transitions ARE choices)
  - Part 9 (Memory Locality -- transitions journaled via navigation.cjs)

must_haves:
  truths:
    - "/mos:jtbd emits a jtbd_transitioned event (kind=set|override|clear) on every transition; the jtbd-state.json cache still updates"
    - "/mos:operator emits an operator_transitioned event on every mode change"
    - "operator.cjs no longer opens room.db directly -- the OPERATOR_TRANSITION edge write routes through navigation.cjs"
    - "the memory_event log is the authoritative record; the JSON cache files remain as fast-read caches (no deprecation)"
  artifacts:
    - path: "scripts/jtbd-command.cjs"
      provides: "jtbd_transitioned emission on set/clear via navigation.logJtbdTransition"
      contains: "logJtbdTransition"
    - path: "scripts/operator-command.cjs"
      provides: "operator_transitioned emission on set/reset via navigation.logOperatorTransition"
      contains: "logOperatorTransition"
    - path: "lib/conversation/operator.cjs"
      provides: "the legacy node:sqlite OPERATOR_TRANSITION edge bypass retired in favor of navigation.cjs"
      contains: "navigation"
    - path: "tests/test-129-state-transition-events.cjs"
      provides: "behavior test for jtbd + operator transition emission + the operator.cjs bypass retirement"
      min_lines: 80
  key_links:
    - from: "scripts/jtbd-command.cjs"
      to: "lib/core/navigation.cjs"
      via: "navigation.logJtbdTransition(roomDir, payload) after mod.setCurrent/clear"
      pattern: "logJtbdTransition"
    - from: "scripts/operator-command.cjs"
      to: "lib/core/navigation.cjs"
      via: "navigation.logOperatorTransition(roomDir, payload) after transition()"
      pattern: "logOperatorTransition"
---

<objective>
Refactor the 2 STATE-TRANSITION spine scripts (jtbd-command, operator-command) to emit
memory_event on every transition, and retire the pre-existing direct-sqlite OPERATOR_TRANSITION
edge bypass in lib/conversation/operator.cjs (a baselined Phase 128 violation) by routing that
edge write through navigation.writeEdge.

Purpose: these two scripts write per-room state (jtbd-state.json, conversation-operator.json)
but never journal the transition to the canonical event log. operator.cjs ALSO opens room.db
directly via require('node:sqlite') -- exactly the bypass Phase 128 forbids net-new. This plan
makes the event log authoritative (cache stays as fast-read fallback per the 129-CONTEXT
decision) and closes the operator.cjs bypass.

Output: 2 refactored scripts + operator.cjs edge-write rerouted through navigation.cjs +
a behavior test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-spine-repair-memory-event/129-CONTEXT.md
@.planning/phases/129-spine-repair-memory-event/129-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- From Plan 01 (Wave-1 substrate). Use directly. -->
From lib/core/navigation.cjs (Plan 129-01 additions):
  logJtbdTransition(roomDir, payload) -> { ok, eventId, deduped? } | { ok:false, reason:'no_room_db' }
    payload SHOULD carry: kind ('set'|'override'|'clear'), from, to, confidence, trigger.
  logOperatorTransition(roomDir, payload) -> { ok, eventId } | { ok:false, reason:'no_room_db' }
    payload SHOULD carry: from, to, trigger.
  writeEdge(db, { source_id, target_id, edge_type, properties }) -> { ok, edge_id } | { ok:false, reason }
    edge_type must be in ALLOWED_EDGE_TYPES. NOTE: OPERATOR_TRANSITION is NOT in that Set today.

From scripts/jtbd-command.cjs:
  resolveActiveRoom() -> { slug, abs_path } | null (via lib/core/resolve-active-room.cjs chokepoint)
  mod = lib/hmi/jtbd-state.cjs ; mod.getCurrent(abs_path), mod.setCurrent(abs_path,{jtbd,confidence,evidence,trigger,manual}), mod.clear(abs_path)
  set/clear subcommands write the cache via mod, then re-read to confirm.
From scripts/operator-command.cjs:
  resolveActiveRoom() -> { slug, abs_path } | null
  op = lib/conversation/operator.cjs ; op.getCurrent(abs_path), op.transition(abs_path, to, trigger, contextDelta) -> { success, current, previous, violations? }
  set + reset subcommands call op.transition.
From lib/conversation/operator.cjs (lines 182-214 -- the bypass to retire):
  writeOperatorTransitionEdge(roomDir, from, to, trigger, contextDelta) opens <roomDir>/.room-graph/room.db
  via require('node:sqlite') DatabaseSync and runs raw INSERT OR IGNORE into nodes + edges (OPERATOR_TRANSITION).
  Called inside transition() after a successful state write. This is a baselined Phase 128 violation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit jtbd_transitioned from /mos:jtbd on set + clear (cache file still updates)</name>
  <read_first>
    - scripts/jtbd-command.cjs (the set subcommand at lines 621-741 + the clear subcommand at lines 585-619; resolveActiveRoom + mod.setCurrent/clear)
    - lib/core/navigation.cjs (the Plan 01 logJtbdTransition re-export)
    - .planning/phases/129-spine-repair-memory-event/129-CONTEXT.md (state-files-stay-as-cache decision -- the JSON cache is NOT deprecated)
  </read_first>
  <behavior>
    - Test: /mos:jtbd set <id> on a seeded temp room writes jtbd-state.json (unchanged behavior) AND emits ONE jtbd_transitioned memory_event with properties.kind='set', from=<prior>, to=<id>, confidence, trigger='manual_set'.
    - Test: /mos:jtbd clear emits jtbd_transitioned with kind='clear', to=null.
    - Test: a manual override (set when a sticky override is already active) emits kind='override' (or kind='set' with an override marker -- pick one and assert it consistently).
    - Test: emission only fires when the cache write actually landed (the script re-reads to confirm; no event on a rejected/no-op transition).
    - Test (substrate): scripts/jtbd-command.cjs scanFiles returns [] (zero direct room.db access).
  </behavior>
  <action>
    In scripts/jtbd-command.cjs set subcommand: after mod.setCurrent succeeds AND the re-read confirms newState.jtbd === arg (the existing success branch around line 693), call navigation.logJtbdTransition(room.abs_path, { kind: <'override' when the prior state had an active expires_at else 'set'>, from: <prior state.jtbd or null>, to: newState.jtbd, confidence: newState.confidence, trigger: 'manual_set' }). In the clear subcommand: after mod.clear succeeds, call navigation.logJtbdTransition(room.abs_path, { kind: 'clear', from: <prior jtbd or null>, to: null, trigger: 'manual_clear' }). Lazy-require navigation.cjs with a graceful try/catch (mirror loadStateModule's lazy pattern). Wrap each emission in try/catch -- best-effort telemetry, never blocks the cache write or the user render. Emit AFTER the cache write + confirmation, BEFORE the final stdout render (so a later /mos:status sees the event).

    Per 129-CONTEXT: the jtbd-state.json cache STILL updates (no deprecation). The memory_event log becomes the authoritative record; the cache is the fast-read fallback that navigation.getCurrentJTBD already handles. Do NOT remove or weaken the mod.setCurrent / mod.clear cache writes. Do NOT open room.db here; route through navigation.cjs. NO em-dashes.
  </action>
  <verify>
    <automated>node tests/test-129-state-transition-events.cjs</automated>
    <automated>node -e "const g=require('./scripts/check-substrate.cjs'); const fs=require('fs'); const v=g.scanFiles(['scripts/jtbd-command.cjs'],p=>fs.readFileSync(p,'utf8')); if(v.length)throw new Error('substrate violation: '+JSON.stringify(v)); console.log('clean')"</automated>
  </verify>
  <done>/mos:jtbd set + clear emit jtbd_transitioned with the right kind enum + from/to, the cache file still updates, emission fires only on confirmed transitions, and the script has zero direct room.db access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit operator_transitioned from /mos:operator + retire the operator.cjs direct-sqlite OPERATOR_TRANSITION bypass through navigation.cjs</name>
  <read_first>
    - scripts/operator-command.cjs (set subcommand lines 414-542 + reset subcommand lines 544-650; op.transition call sites)
    - lib/conversation/operator.cjs (writeOperatorTransitionEdge lines 182-214 -- the require('node:sqlite') bypass; transition() that calls it)
    - lib/core/navigation/edges.cjs (ALLOWED_EDGE_TYPES -- note OPERATOR_TRANSITION is NOT yet allowed; you will add it)
    - lib/core/navigation.cjs (logOperatorTransition + writeEdge re-exports)
  </read_first>
  <behavior>
    - Test: /mos:operator set <op> on a seeded temp room performs the state transition (unchanged) AND emits ONE operator_transitioned memory_event with from + to + trigger='manual_set'.
    - Test: /mos:operator reset --confirm emits operator_transitioned with to='JUST_TALK', trigger='manual_reset'.
    - Test: no event on a rejected transition (op.transition returns success:false) and no event on an already-in-target no-op.
    - Test (bypass retirement): lib/conversation/operator.cjs scanFiles returns [] -- the require('node:sqlite') + raw INSERT INTO nodes/edges in writeOperatorTransitionEdge is gone; the OPERATOR_TRANSITION edge now writes via navigation.writeEdge.
    - Test: the OPERATOR_TRANSITION edge still lands in room.db after a successful transition (regression: the edge that the old bypass wrote is preserved, now via the chokepoint).
    - Test (substrate): scripts/operator-command.cjs scanFiles returns [].
  </behavior>
  <action>
    Add OPERATOR_TRANSITION to ALLOWED_EDGE_TYPES in lib/core/navigation/edges.cjs (additive-comment idiom; Phase 129; consumer = the operator.cjs transition path being routed through the chokepoint). This is the legal home for the edge the bypass used to write.

    In lib/conversation/operator.cjs: REPLACE the body of writeOperatorTransitionEdge so it no longer requires node:sqlite and no longer runs raw INSERT. Instead require lib/core/navigation.cjs and call navigation.writeEdge with a navigation-opened handle. Since writeEdge takes a db handle (not a roomDir), and operator.cjs must NOT open room.db itself, route through a navigation helper that opens internally: call navigation.logOperatorTransition(roomDir, { from, to, trigger, follows_from: <optional prior event id> }) which (per Plan 01) opens room.db, logs the event, and -- when you pass an edge intent -- writes the typed edge. If logOperatorTransition does not itself write OPERATOR_TRANSITION, add a thin navigation helper (or extend logOperatorTransition in Plan 01's spine-events to also emit the OPERATOR_TRANSITION edge between from/to nodes when payload.write_transition_edge is true). Keep the existing graceful-skip behavior (no room.db -> silent skip, no throw). The .room-graph vs .mindrian path divergence is resolved by routing through navigation.cjs which uses the canonical .mindrian/room.db opener.

    In scripts/operator-command.cjs: after op.transition returns success:true (the set success branch ~line 519 and the reset success branch ~line 627), call navigation.logOperatorTransition(room.abs_path, { from: <state.current before>, to: result.current, trigger: <'manual_set'|'manual_reset'> }). Lazy-require navigation.cjs with a graceful try/catch (mirror loadOperatorModule). Best-effort try/catch around emission. NOTE: if operator.cjs transition() already emits operator_transitioned via the routed edge helper, do NOT double-emit -- emit the memory_event from EXACTLY ONE site (prefer the script layer for the manual commands; have operator.cjs transition() emit for non-CLI callers). Document which layer owns emission in a header comment so there is one emission per transition.

    Do NOT open room.db in either script. NO em-dashes. The operator.cjs change is the load-bearing bypass retirement -- verify the substrate guard goes clean on it.
  </action>
  <verify>
    <automated>node tests/test-129-state-transition-events.cjs</automated>
    <automated>node -e "const g=require('./scripts/check-substrate.cjs'); const fs=require('fs'); const v=g.scanFiles(['scripts/operator-command.cjs','lib/conversation/operator.cjs'],p=>fs.readFileSync(p,'utf8')); if(v.length)throw new Error('substrate violation: '+JSON.stringify(v)); console.log('clean')"</automated>
    <automated>node -e "const e=require('./lib/core/navigation/edges.cjs'); if(!e.ALLOWED_EDGE_TYPES.has('OPERATOR_TRANSITION'))throw new Error('OPERATOR_TRANSITION not allowed'); console.log('OK')"</automated>
  </verify>
  <done>/mos:operator set + reset emit operator_transitioned exactly once per transition; the operator.cjs node:sqlite bypass is gone and the OPERATOR_TRANSITION edge writes through navigation.cjs; both files pass the substrate guard; no double-emit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spine transition script -> navigation.cjs | scripts pass roomDir + scalar from/to; the chokepoint owns room.db |
| operator.cjs -> room.db (BEFORE) | the live bypass: direct node:sqlite handle to .room-graph/room.db |
| operator.cjs -> navigation.cjs (AFTER) | retired bypass; edge + event route through the chokepoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-03-01 | Tampering | operator.cjs writing an un-provenanced row via the node:sqlite bypass | mitigate | retire writeOperatorTransitionEdge's raw INSERT; route OPERATOR_TRANSITION + the event through navigation.cjs (provenance + truth-state preserved) |
| T-129-03-02 | Tampering | jtbd/operator transition skipping the event log (the bug this phase fixes) | mitigate | every confirmed transition calls navigation.log*Transition; event log becomes authoritative, cache stays as fallback |
| T-129-03-03 | Repudiation | a double-emitted operator_transitioned masking the true transition count | mitigate | single emission site per transition, documented in a header comment; the acceptance test asserts exact count |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | NONE -- zero new dependencies |
</threat_model>

<verification>
- node tests/test-129-state-transition-events.cjs exits 0.
- scripts/check-substrate.cjs scanFiles over jtbd-command.cjs, operator-command.cjs, AND lib/conversation/operator.cjs returns [] (the bypass is retired -- this is the load-bearing assertion).
- OPERATOR_TRANSITION is in ALLOWED_EDGE_TYPES; the edge still lands after a transition.
- Existing jtbd-state.json / conversation-operator.json cache writes are byte-stable (no deprecation).
- Zero em-dashes in authored files.
</verification>

<success_criteria>
- jtbd + operator transitions emit their canonical events exactly once each.
- operator.cjs no longer opens room.db directly (substrate guard clean -- baselined violation closed).
- Cache files still update; event log is authoritative via navigation.getCurrent*.
</success_criteria>

<output>
Create `.planning/phases/129-spine-repair-memory-event/129-03-SUMMARY.md` when done.
</output>
