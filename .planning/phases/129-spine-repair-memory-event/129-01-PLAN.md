---
phase: 129-spine-repair-memory-event
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/memory-events.cjs
  - lib/core/navigation/edges.cjs
  - lib/core/navigation/spine-events.cjs
  - lib/core/navigation.cjs
  - docs/architecture/SUBSTRATE-CONTRACT.md
  - tests/test-129-spine-substrate.cjs
autonomous: true
requirements: [SPINE-EVENTS, FOLLOWS-FROM, EVENT-CAP-5, STATE-AUTHORITY, IDEMPOTENCY-60S]
canon_parts:
  - Part 4 (Every Choice Is Graph Data -- spine transitions are choices)
  - Part 8 (Graph Boundary -- spine helpers are LOCAL-only)
  - Part 9 (Memory Locality -- navigation.cjs is the only door to room.db)

must_haves:
  truths:
    - "The canonical EVENT_TYPES Set carries exactly 5 net-new spine event types (no more)"
    - "FOLLOWS_FROM is the 8th allowed cascade edge type and writeEdge accepts it"
    - "A second identical spine event within 60 seconds is deduped (not written)"
    - "navigation.cjs exposes per-event spine helpers that open room.db internally (callers pass roomDir, never a db handle)"
    - "getCurrentJTBD/getCurrentOperator read the memory_event log first, falling back to the cache file"
    - "The substrate guard passes on every file this plan ships (spine-events.cjs is in navigation/ allow-list)"
  artifacts:
    - path: "lib/core/navigation/spine-events.cjs"
      provides: "The 6 roomDir-taking spine helper functions + getCurrentJTBD/getCurrentOperator + 60s dedup"
      min_lines: 120
    - path: "lib/core/navigation/memory-events.cjs"
      provides: "5 net-new spine event-type strings + 60s-TTL dedup in logEvent"
      contains: "spine_read"
    - path: "lib/core/navigation/edges.cjs"
      provides: "FOLLOWS_FROM in ALLOWED_EDGE_TYPES"
      contains: "FOLLOWS_FROM"
    - path: "tests/test-129-spine-substrate.cjs"
      provides: "RED-first behavior test for the 5 types, dedup, FOLLOWS_FROM, getCurrent* fallback"
      min_lines: 80
  key_links:
    - from: "lib/core/navigation.cjs"
      to: "lib/core/navigation/spine-events.cjs"
      via: "module re-export of the 6 helpers + getCurrentJTBD + getCurrentOperator"
      pattern: "spine-events"
    - from: "lib/core/navigation/spine-events.cjs"
      to: "lib/core/navigation/memory-events.cjs"
      via: "logEvent call through the internal helper"
      pattern: "logEvent"
---

<objective>
Ship the shared substrate that all 6 spine-script refactors (Plans 02/03/04) build on:
the 5 net-new canonical spine event types, the FOLLOWS_FROM cascade edge (8th type),
60-second idempotency in the event log, and the per-event spine helper API on
navigation.cjs that opens room.db internally so the (non-allowlisted) spine scripts
never touch room.db directly.

Purpose: this is the Phase 128 substrate contract made usable by the spine. The spine
scripts are NOT in the substrate-guard allow-list, so they must reach room.db ONLY through
navigation.cjs. This plan adds the helpers that make that possible. It is Wave 1 because
all three Wave 2 script-refactor plans depend on these helpers existing.

Output: lib/core/navigation/spine-events.cjs (new navigation submodule, allow-listed),
5 new EVENT_TYPES strings + dedup in memory-events.cjs, FOLLOWS_FROM in edges.cjs,
re-exports on navigation.cjs, a SUBSTRATE-CONTRACT.md amendment line per the M11 rule,
and a RED-first behavior test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-spine-repair-memory-event/129-CONTEXT.md
@docs/MINDRIAN-CANON.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase; do NOT re-explore. -->

From lib/core/navigation/memory-events.cjs (the file you extend):
  EVENT_TYPES: a frozen Set of event-type strings. logEvent rejects any type not IN it.
  logEvent(db, eventType, payload) -> { ok: true, eventId } | { ok: false, reason }
    INSERTs a row into nodes with type='memory_event', properties=JSON(payload + {event_type}),
    review_status='confirmed'. Reads payload.source_path / payload.created_by for provenance columns.
  findRecentChanges(db, sinceEpochMs, opts) -> Array<{ id, eventType, targetNodeId, createdAt, sourcePath, properties }>
    opts.eventType filters by a single event_type; opts.limit caps rows (default 100).
  module.exports = { EVENT_TYPES, logEvent, findRecentChanges }

From lib/core/navigation/edges.cjs (the file you extend):
  ALLOWED_EDGE_TYPES: frozen Set; currently DEFERRED, REJECTED, DERIVED_FROM, FILED_AS_DECISION.
  writeEdge(db, { source_id, target_id, edge_type, properties }) -> { ok, edge_id, ... } | { ok:false, reason }
    Rejects edge_type not in ALLOWED_EDGE_TYPES. UPSERTs into edges(source,target,type,properties).
  module.exports = { ALLOWED_EDGE_TYPES, writeEdge }

From lib/core/navigation/dashboard-helpers.cjs (the EXACT pattern to mirror for room.db access):
  const roomDbMod = require('../room-db.cjs');   // legal here -- navigation/ is allow-listed
  db = roomDbMod.openRoomDb(roomDir);            // opens <roomDir>/.mindrian/room.db
  ... db.prepare(...).get/run/all ...
  finally { roomDbMod.closeRoomDb(db); }
  detectActiveRoom() -> { roomDir, hasRoomDb } | null  (resolves the active room via the registry)

From lib/core/room-db.cjs:
  openRoomDb(roomDir) -> DatabaseSync handle (SYNCHRONOUS). closeRoomDb(db).
  Canonical room.db path is <roomDir>/.mindrian/room.db.

From lib/hmi/jtbd-state.cjs:
  getCurrent(roomDir) -> { jtbd, confidence, evidence, entered_at, expires_at, ... } | null-ish
  HISTORY_MAX, history(roomDir, n). (This is the cache file <roomDir>/.mindrian/jtbd-state.json.)

From lib/conversation/operator.cjs:
  getCurrent(roomDir) -> { current, previous, entered_at, context, history }
  OPERATORS (array of valid operator names). (Cache file <roomDir>/.mindrian/conversation-operator.json.)

From docs/architecture/SUBSTRATE-CONTRACT.md M11 export allow-list + amendment rule:
  Every addition to navigation.cjs module.exports requires an amendment LINE in the ADR
  naming the export + phase + consumer. spine-events.cjs sits under lib/core/navigation/
  which is already in scripts/check-substrate.cjs ALLOWED_DIRECT_IMPORT (regex /^lib\/core\/navigation\//).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the canonical enums -- 5 net-new spine event types + FOLLOWS_FROM cascade edge + 60s dedup</name>
  <read_first>
    - lib/core/navigation/memory-events.cjs (EVENT_TYPES Set + logEvent + the additive-block comment idiom at lines 26-237)
    - lib/core/navigation/edges.cjs (ALLOWED_EDGE_TYPES Set + writeEdge + the additive-comment idiom at lines 32-71)
    - .planning/phases/129-spine-repair-memory-event/129-CONTEXT.md (the "Pre-resolved open decisions" + event-cap <=5 mandate)
  </read_first>
  <behavior>
    - Test: EVENT_TYPES contains exactly these 5 net-new strings and no other net-new spine strings: spine_read, jtbd_transitioned, operator_transitioned, workflow_stage, suggestion_surfaced.
    - Test: EVENT_TYPES.size grew by exactly 5 versus the pre-129 baseline (assert a named-membership FLOOR + a delta of 5; do not hardcode a brittle absolute size).
    - Test: writeEdge(db, { ..., edge_type: 'FOLLOWS_FROM' }) returns ok:true and the row lands; ALLOWED_EDGE_TYPES.has('FOLLOWS_FROM') is true.
    - Test: writeEdge with edge_type 'ASSOCIATION_LENS' or 'TRANSITION_LENS' returns ok:false reason 'invalid_edge_type' (the rejected dual-graph taxonomy stays rejected).
    - Test: logEvent(db, 'spine_read', payload) twice within 60s with the same dedupe key writes ONE row; the second returns ok:true deduped:true (or ok:true with a deduped marker) and findRecentChanges shows a single spine_read.
    - Test: logEvent(db, 'spine_read', payload) twice with the dedupe TTL elapsed (inject a clock seam) writes TWO rows.
  </behavior>
  <action>
    Add the 5 net-new event-type strings to the EVENT_TYPES Set in memory-events.cjs, following the existing additive-block comment idiom (a dated Phase 129 comment block citing the v1.13.1 risk-3 event-cap-5 lock, then the strings). Justify the consolidation in the comment: spine_read merges the old status_rendered + memory_inspected (both are read-surface renders, disambiguated by a payload.surface enum 'status'|'memory'); jtbd_transitioned merges set/override/clear (payload.kind enum 'set'|'override'|'clear'); workflow_stage merges act dispatch/completion AND pipeline stage entered/completed (payload.surface enum 'act'|'pipeline', payload.phase enum 'entered'|'completed'); operator_transitioned and suggestion_surfaced stand alone. This is exactly 5 net-new types -- honor the v1.13.1 risk-3 cap (per 129-CONTEXT Pre-resolved decisions).

    Add a 60-second idempotency layer INSIDE logEvent (per 129-CONTEXT idempotency decision: dedupe repeated no-op emissions with a 60s TTL). Compute a dedupe key as sha256 of (eventType + payload.dedupe_key) when the caller supplies a payload.dedupe_key string; when absent, do NOT dedupe (back-compat -- every existing caller keeps writing every event). Maintain an in-DB lookup: before INSERT, when a dedupe_key is present, run findRecentChanges-style query for a memory_event of the same eventType whose properties.dedupe_key matches AND whose created_at is within 60000ms of now; if found, return { ok: true, deduped: true, eventId: <existing id> } without inserting. Use a clock seam (an optional opts arg or module-level _now override hook, mirror the test-seam idiom used elsewhere) so the test can advance time deterministically. Keep logEvent's existing signature working unchanged for callers that pass no dedupe_key.

    Add FOLLOWS_FROM to ALLOWED_EDGE_TYPES in edges.cjs following the existing additive-comment idiom (a dated Phase 129 comment block: 8th canonical cascade type extending INFORMS/CONTRADICTS/CONVERGES/INVALIDATES/ENABLES/REJECTED/DEFERRED per the 2026-05-16 dual-graph review; enum-only properties per Canon Part 8; explicitly note the lens-class taxonomy ASSOCIATION_LENS/TRANSITION_LENS is REJECTED). No change to writeEdge logic is needed beyond the Set membership.

    NO em-dashes in any authored line (CLAUDE.md HARD RULE). Use hyphens.
  </action>
  <verify>
    <automated>node tests/test-129-spine-substrate.cjs</automated>
    <automated>node -e "const m=require('./lib/core/navigation/memory-events.cjs'); ['spine_read','jtbd_transitioned','operator_transitioned','workflow_stage','suggestion_surfaced'].forEach(t=>{if(!m.EVENT_TYPES.has(t))throw new Error('missing '+t)}); const e=require('./lib/core/navigation/edges.cjs'); if(!e.ALLOWED_EDGE_TYPES.has('FOLLOWS_FROM'))throw new Error('FOLLOWS_FROM missing'); console.log('OK')"</automated>
  </verify>
  <done>EVENT_TYPES has the 5 named strings (delta exactly 5), FOLLOWS_FROM is allowed and lens-class edges are rejected, and logEvent dedupes same-key events inside a 60s window while leaving no-dedupe-key callers byte-unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship spine-events.cjs helper API (roomDir-taking, room.db opened internally) + getCurrentJTBD/getCurrentOperator + navigation.cjs re-exports</name>
  <read_first>
    - lib/core/navigation/dashboard-helpers.cjs (the EXACT roomDir -> openRoomDb -> work -> closeRoomDb pattern to mirror; this file is the precedent for a navigation submodule that opens room.db)
    - lib/core/navigation.cjs (the module.exports re-export surface + the per-export justification-comment idiom at lines 73-101)
    - lib/hmi/jtbd-state.cjs (getCurrent cache-file reader -- the fallback source)
    - lib/conversation/operator.cjs (getCurrent cache-file reader -- the fallback source)
    - docs/architecture/SUBSTRATE-CONTRACT.md (M11 export allow-list + the amendment-line rule)
  </read_first>
  <behavior>
    - Test: spine-events exports logSpineRead(roomDir, payload), logJtbdTransition(roomDir, payload), logOperatorTransition(roomDir, payload), logWorkflowStage(roomDir, payload), logSuggestionSurfaced(roomDir, payload), getCurrentJTBD(roomDir), getCurrentOperator(roomDir).
    - Test: each log* helper opens room.db via roomDbMod.openRoomDb(roomDir), writes the right event_type via logEvent, and closes the handle; callers pass roomDir only (never a db handle).
    - Test: when roomDir has no room.db, each log* helper returns { ok:false, reason:'no_room_db' } and does NOT throw (graceful -- mirrors dashboard-helpers).
    - Test: getCurrentJTBD(roomDir) returns the jtbd from the most-recent jtbd_transitioned memory_event when one exists (event log is authoritative); when no such event exists it falls back to jtbd-state.cjs getCurrent (cache file). Same contract for getCurrentOperator over operator_transitioned + operator.cjs getCurrent.
    - Test: navigation.cjs re-exports all 7 names and calling navigation.logSpineRead / navigation.getCurrentJTBD works end-to-end against a seeded temp room.
    - Test (FOLLOWS_FROM emission seam): a log* helper accepts an optional payload.follows_from = <prior memory_event id>; when present the helper also writes a FOLLOWS_FROM edge (source = new event id, target = prior event id) via writeEdge. Assert the edge lands.
  </behavior>
  <action>
    Create lib/core/navigation/spine-events.cjs. Mirror dashboard-helpers.cjs structure exactly: require('../room-db.cjs') (legal -- navigation/ is allow-listed by scripts/check-substrate.cjs regex), require('./memory-events.cjs') for logEvent, require('./edges.cjs') for writeEdge. Each log* helper signature is (roomDir, payload): resolve <roomDir>/.mindrian/room.db existence (return { ok:false, reason:'no_room_db' } when absent, like dashboard-helpers), openRoomDb(roomDir) in a try, call logEvent(db, '<the type>', payload), and ALWAYS closeRoomDb in finally. logSpineRead -> 'spine_read'; logJtbdTransition -> 'jtbd_transitioned'; logOperatorTransition -> 'operator_transitioned'; logWorkflowStage -> 'workflow_stage'; logSuggestionSurfaced -> 'suggestion_surfaced'. Each helper sets a deterministic payload.dedupe_key when the caller does not supply one (so 10x /mos:status with no state change dedupes per the 60s TTL): for spine_read use surface + section + jtbd + operator scalars; for the transition helpers the natural key is the from->to pair. Do NOT invent state-reduction language ("v1", "static for now"); these helpers carry the full payload the spine scripts will pass.

    FOLLOWS_FROM emission: after a successful logEvent, when payload.follows_from is a non-empty string, call writeEdge(db, { source_id: <new event id>, target_id: payload.follows_from, edge_type: 'FOLLOWS_FROM', properties: { surface: payload.surface || null } }). Enum-only properties per Canon Part 8 (no freeform user content). Tolerate writeEdge failure (return the log result regardless; FOLLOWS_FROM is additive, never load-bearing for the log write).

    getCurrentJTBD(roomDir): open room.db, query the most-recent jtbd_transitioned memory_event (findRecentChanges with eventType filter, limit 1); when found, return { jtbd: <properties.jtbd>, source: 'event_log', ... }. When none found, close db and return jtbdState.getCurrent(roomDir) shaped result with source:'cache_fallback'. getCurrentOperator(roomDir): identical pattern over operator_transitioned + operator.cjs getCurrent. Per 129-CONTEXT decision: the event log becomes authoritative; the cache file is the fallback; NO deprecation of the cache file this phase.

    Wire navigation.cjs: require('./navigation/spine-events.cjs') and re-export the 7 names in module.exports, each with a per-export justification comment in the established idiom (Phase 129; consumer = the 6 spine scripts; same thin-re-export pattern as logMemoryEvent / writeEdge / detectActiveRoom).

    Amend docs/architecture/SUBSTRATE-CONTRACT.md: add 7 lines to the M11 export allow-list (one per new export) AND one amendment line per the amendment rule naming the exports, Phase 129, and the consumer (the 6 spine scripts). This is mandatory -- adding an export without the amendment line is itself a contract violation.

    NO em-dashes in any authored line.
  </action>
  <verify>
    <automated>node tests/test-129-spine-substrate.cjs</automated>
    <automated>node -e "const n=require('./lib/core/navigation.cjs'); ['logSpineRead','logJtbdTransition','logOperatorTransition','logWorkflowStage','logSuggestionSurfaced','getCurrentJTBD','getCurrentOperator'].forEach(k=>{if(typeof n[k]!=='function')throw new Error('missing export '+k)}); console.log('OK')"</automated>
    <automated>node scripts/check-substrate.cjs --baseline >/dev/null 2>&1; node -e "const g=require('./scripts/check-substrate.cjs'); const v=g.scanFiles(['lib/core/navigation/spine-events.cjs'],p=>require('fs').readFileSync(p,'utf8')); if(v.length)throw new Error('spine-events.cjs violates substrate guard: '+JSON.stringify(v)); console.log('substrate clean')"</automated>
  </verify>
  <done>All 7 helpers exist and re-export through navigation.cjs; log* helpers open/close room.db internally and never expose a db handle to callers; getCurrentJTBD/getCurrentOperator are event-log-authoritative with cache fallback; FOLLOWS_FROM emits when follows_from is supplied; spine-events.cjs passes the substrate guard; the ADR carries the M11 amendment lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spine script -> navigation.cjs | The spine scripts are untrusted callers of the chokepoint; they pass roomDir + payload only |
| navigation.cjs -> room.db | The only legitimate room.db write path; provenance + dedup enforced here |
| memory_event payload -> Brain | Payloads must stay LOCAL; FOLLOWS_FROM properties are enum-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01 | Tampering | spine scripts bypassing navigation.cjs to write room.db | mitigate | spine-events.cjs is the only door; scripts pass roomDir, never a db handle; substrate guard blocks net-new direct access in non-allowlisted spine scripts |
| T-129-02 | Information Disclosure | user content leaking into memory_event or FOLLOWS_FROM edge properties | mitigate | FOLLOWS_FROM properties are enum-only (surface scalar); payloads carry scalars/enums per the Phase 90 sanitize precedent; no Brain egress in any spine helper |
| T-129-03 | Denial of Service | telemetry noise from repeated no-op emissions (10x /mos:status) | mitigate | 60s-TTL dedupe in logEvent keyed on a deterministic dedupe_key |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | NONE -- this plan adds zero dependencies (node built-ins only); no install tasks |
</threat_model>

<verification>
- node tests/test-129-spine-substrate.cjs exits 0 (the RED-first behavior suite goes GREEN).
- node scripts/check-substrate.cjs --baseline shows spine-events.cjs carries ZERO violations (it is allow-listed under navigation/).
- Existing Phase 109 navigation tests still pass: node tests/test-navigation-acceptance.cjs exits 0.
- grep confirms zero em-dashes in the four authored files.
</verification>

<success_criteria>
- 5 net-new event types in EVENT_TYPES (delta exactly 5); FOLLOWS_FROM is the 8th cascade edge.
- 60s idempotency live in logEvent; no-dedupe-key callers unchanged.
- navigation.cjs exposes 7 spine helpers; spine scripts will pass roomDir only.
- getCurrentJTBD/getCurrentOperator event-log-authoritative with cache fallback.
- SUBSTRATE-CONTRACT.md amended per the M11 rule.
- Phase 109 acceptance test still green (zero regression).
</success_criteria>

<output>
Create `.planning/phases/129-spine-repair-memory-event/129-01-SUMMARY.md` when done.
</output>
