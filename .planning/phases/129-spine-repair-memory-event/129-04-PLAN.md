---
phase: 129-spine-repair-memory-event
plan: 04
type: execute
wave: 2
depends_on: ["129-01"]
files_modified:
  - scripts/act-command.cjs
  - scripts/pipeline-command.cjs
  - tests/test-129-workflow-stage-events.cjs
autonomous: true
requirements: [SPINE-EVENTS, FOLLOWS-FROM]
canon_parts:
  - Part 3 (Tri-Context Decision Gate -- act dispatch + pipeline stage are gate transitions)
  - Part 9 (Memory Locality -- stages journaled via navigation.cjs)

must_haves:
  truths:
    - "/mos:act emits workflow_stage (surface=act, phase=entered) on dispatch and workflow_stage (surface=act, phase=completed) on return"
    - "/mos:pipeline emits workflow_stage (surface=pipeline) on stage entered + stage completed per stage"
    - "consecutive workflow_stage events in one loop carry a FOLLOWS_FROM edge to the prior spine event when a prior event id is available"
    - "neither script carries any direct room.db access"
  artifacts:
    - path: "scripts/act-command.cjs"
      provides: "workflow_stage emission on act dispatch + completion via navigation.logWorkflowStage"
      contains: "logWorkflowStage"
    - path: "scripts/pipeline-command.cjs"
      provides: "workflow_stage emission per pipeline stage via navigation.logWorkflowStage"
      contains: "logWorkflowStage"
    - path: "tests/test-129-workflow-stage-events.cjs"
      provides: "behavior test for act + pipeline stage emission + FOLLOWS_FROM linkage"
      min_lines: 70
  key_links:
    - from: "scripts/act-command.cjs"
      to: "lib/core/navigation.cjs"
      via: "navigation.logWorkflowStage(roomDir, {surface:'act', phase})"
      pattern: "logWorkflowStage"
    - from: "scripts/pipeline-command.cjs"
      to: "lib/core/navigation.cjs"
      via: "navigation.logWorkflowStage(roomDir, {surface:'pipeline', stage, phase})"
      pattern: "logWorkflowStage"
---

<objective>
Refactor the 2 WORKFLOW-EXECUTION spine scripts (act-command, pipeline-command) to emit
workflow_stage memory_event on dispatch + completion, and demonstrate the FOLLOWS_FROM
cascade edge by linking consecutive spine events in the proactive loop.

Purpose: closes the backward arc for the two execution surfaces. /mos:act dispatches a
methodology but never records that it ran; /mos:pipeline chains stages but never journals them.
After this plan, every act dispatch + completion and every pipeline stage is a workflow_stage
event, and FOLLOWS_FROM connects them temporally so the next /mos:status can render "what
changed since last look" as a real chain.

Output: 2 refactored scripts + a behavior test that exercises the FOLLOWS_FROM linkage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-spine-repair-memory-event/129-CONTEXT.md
@.planning/phases/129-spine-repair-memory-event/129-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- From Plan 01 (Wave-1 substrate). Use directly. -->
From lib/core/navigation.cjs (Plan 129-01 additions):
  logWorkflowStage(roomDir, payload) -> { ok, eventId, deduped? } | { ok:false, reason:'no_room_db' }
    payload SHOULD carry: surface ('act'|'pipeline'), phase ('entered'|'completed'),
    stage (the stage/methodology name), autonomy (for act), framework (for act).
    When payload.follows_from = <prior memory_event id>, the helper ALSO writes a FOLLOWS_FROM edge
    (source = new event id, target = follows_from) with enum-only properties.

From scripts/act-command.cjs:
  parseArgs(argv) -> { chain, problemType, fromFramework, roomDir }
  resolveRoomDir(args.roomDir), readProblemType(roomDir)
  --chain mode: recommender.recommendFrameworkChain -> resolver.composeWorkflow -> validateChainAutonomy(workflow) ->
    walks steps; stops at the first non-autonomous_safe step (the "needs you here" gate).
From scripts/pipeline-command.cjs:
  parseArgs(argv) -> { fromProblemType, fromFramework, roomDir, pipelineName }
  --from-problem-type / --from-framework: recommender -> resolver.composeWorkflow -> prints the run order (one /mos: per stage).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit workflow_stage from /mos:act on dispatch + completion (with autonomy + framework in payload)</name>
  <read_first>
    - scripts/act-command.cjs (the --chain walk: composeWorkflow + validateChainAutonomy + the per-step loop; resolveRoomDir)
    - lib/core/navigation.cjs (the Plan 01 logWorkflowStage re-export)
    - .planning/phases/129-spine-repair-memory-event/129-CONTEXT.md (act_dispatched/act_completed consolidated INTO workflow_stage per the event-cap-5 decision)
  </read_first>
  <behavior>
    - Test: /mos:act --chain against a seeded temp room emits a workflow_stage with surface='act', phase='entered' carrying the dispatched methodology + autonomy mode at dispatch, and a workflow_stage with phase='completed' on return.
    - Test: the entered event payload carries framework (the dispatched methodology) + autonomy (the autonomy mode) scalars.
    - Test: when the chain stops at a non-autonomous_safe gate, the phase='entered' event still fires for the greenlit steps and NO phase='completed' fires for the gated step (the gate event reflects the stop).
    - Test: when there is no room.db, no throw and no write; the gate render still prints.
    - Test (substrate): scripts/act-command.cjs scanFiles returns [].
  </behavior>
  <action>
    In scripts/act-command.cjs --chain path: at dispatch (just before the command body would run a greenlit step, or at the point this helper greenlights it), call navigation.logWorkflowStage(roomDir, { surface: 'act', phase: 'entered', stage: <command/methodology name>, framework: <framework name>, autonomy: <the autonomy mode from validateChainAutonomy> }). On return/completion of the greenlit run (or at the natural end of the helper's greenlit walk), call navigation.logWorkflowStage(roomDir, { surface: 'act', phase: 'completed', stage: <name>, framework: <framework>, follows_from: <the eventId returned by the matching 'entered' emission> }). Capture the eventId from the entered emission's return ({ ok, eventId }) and pass it as follows_from on the completed emission so the FOLLOWS_FROM edge links them.

    resolveRoomDir(args.roomDir) (or navigation.detectActiveRoom().roomDir fallback) for the roomDir. Lazy-require navigation.cjs with a graceful try/catch (mirror the top-of-file require idiom). Best-effort try/catch around each emission. Emit at the dispatch/completion boundaries, not before stdout-critical gate rendering. autonomy + framework come from the EXISTING validateChainAutonomy / composeWorkflow output (do not fabricate). Do NOT open room.db. NO em-dashes.
  </action>
  <verify>
    <automated>node tests/test-129-workflow-stage-events.cjs</automated>
    <automated>node -e "const g=require('./scripts/check-substrate.cjs'); const fs=require('fs'); const v=g.scanFiles(['scripts/act-command.cjs'],p=>fs.readFileSync(p,'utf8')); if(v.length)throw new Error('substrate violation: '+JSON.stringify(v)); console.log('clean')"</automated>
  </verify>
  <done>/mos:act emits workflow_stage entered + completed with framework + autonomy scalars, links them with FOLLOWS_FROM, gates correctly, and carries zero direct room.db access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit workflow_stage per pipeline stage from /mos:pipeline + FOLLOWS_FROM chaining across stages</name>
  <read_first>
    - scripts/pipeline-command.cjs (the --from-problem-type / --from-framework path: composeWorkflow + the printed run order)
    - lib/core/navigation.cjs (logWorkflowStage re-export + the follows_from edge behavior)
  </read_first>
  <behavior>
    - Test: /mos:pipeline --from-problem-type <x> against a seeded temp room emits one workflow_stage (surface='pipeline', phase='entered') AND one (phase='completed') per resolved stage, with properties.stage naming the stage.
    - Test: consecutive stages are linked: stage N+1's entered event carries follows_from = stage N's completed eventId (a FOLLOWS_FROM edge lands between them).
    - Test: a stage whose framework has no /mos: command (the "run it manually; continuing" skip path) still emits its entered/completed pair OR is explicitly skipped from emission -- pick one and assert it; do not throw.
    - Test (substrate): scripts/pipeline-command.cjs scanFiles returns [].
  </behavior>
  <action>
    In scripts/pipeline-command.cjs --from-problem-type / --from-framework path: as the resolved run order is walked (the composeWorkflow stage list the helper prints), for each stage call navigation.logWorkflowStage(roomDir, { surface: 'pipeline', phase: 'entered', stage: <stage/framework name>, follows_from: <prior stage's completed eventId, when available> }) then navigation.logWorkflowStage(roomDir, { surface: 'pipeline', phase: 'completed', stage: <name>, follows_from: <this stage's entered eventId> }). Thread the eventIds so each stage's entered FOLLOWS_FROM the previous stage's completed, and each completed FOLLOWS_FROM its own entered. This is the FOLLOWS_FROM "one memory_event clearly follows another in the proactive loop" demonstration from 129-CONTEXT.

    roomDir via the script's existing resolution (or navigation.detectActiveRoom() fallback). Lazy-require navigation.cjs gracefully. Best-effort try/catch per emission. The stage names come from composeWorkflow / the registry (never fabricated). Do NOT open room.db. NO em-dashes.
  </action>
  <verify>
    <automated>node tests/test-129-workflow-stage-events.cjs</automated>
    <automated>node -e "const g=require('./scripts/check-substrate.cjs'); const fs=require('fs'); const v=g.scanFiles(['scripts/pipeline-command.cjs'],p=>fs.readFileSync(p,'utf8')); if(v.length)throw new Error('substrate violation: '+JSON.stringify(v)); console.log('clean')"</automated>
  </verify>
  <done>/mos:pipeline emits a workflow_stage entered/completed pair per stage with FOLLOWS_FROM chaining across stages, stage names sourced from the resolver, zero direct room.db access.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| act/pipeline script -> navigation.cjs | scripts pass roomDir + scalar stage/phase; the chokepoint owns room.db |
| stage chain -> FOLLOWS_FROM edge | only memory_event ids + a surface enum cross into the edge properties |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-04-01 | Tampering | act/pipeline opening room.db directly | mitigate | emission routes through navigation.logWorkflowStage; substrate guard scanFiles asserts zero direct access |
| T-129-04-02 | Information Disclosure | user content in workflow_stage payload or FOLLOWS_FROM properties | mitigate | payload carries framework names + enum scalars + numeric autonomy mode only; FOLLOWS_FROM properties are enum-only (surface) per Canon Part 8 |
| T-129-04-03 | Availability | emission delaying the act gate render or pipeline run order | mitigate | best-effort try/catch per emission; emission at stage boundaries, never blocking the gate stdout |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | NONE -- zero new dependencies |
</threat_model>

<verification>
- node tests/test-129-workflow-stage-events.cjs exits 0.
- scripts/check-substrate.cjs scanFiles over both scripts returns [].
- FOLLOWS_FROM edges land between consecutive workflow_stage events (asserted in the test).
- Zero em-dashes in authored files.
</verification>

<success_criteria>
- /mos:act emits entered + completed workflow_stage with framework + autonomy.
- /mos:pipeline emits entered + completed per stage with FOLLOWS_FROM chaining.
- Both scripts carry zero direct room.db access.
</success_criteria>

<output>
Create `.planning/phases/129-spine-repair-memory-event/129-04-SUMMARY.md` when done.
</output>
