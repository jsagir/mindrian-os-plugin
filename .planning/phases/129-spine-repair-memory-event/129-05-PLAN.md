---
phase: 129-spine-repair-memory-event
plan: 05
type: execute
wave: 3
depends_on: ["129-02", "129-03", "129-04"]
files_modified:
  - tests/test-129-spine-acceptance.cjs
  - tests/run-all-129.sh
  - lib/memory/run-feynman-tests.cjs
  - tests/fixtures/phase-129/sample-room/seed.sql
autonomous: true
requirements: [SPINE-ACCEPTANCE, EVENT-CAP-5, NO-REGRESSION]
canon_parts:
  - Part 9 (Memory Locality -- proven by instrumentation, zero non-SQLite reads)

must_haves:
  truths:
    - "The instrumented acceptance test runs the full proactive loop (status -> suggest-next -> act -> completion) and asserts zero non-SQLite filesystem reads outside the cache files"
    - "The test asserts the EXACT memory_event count + sequence per loop pass"
    - "The test asserts the next /mos:status render reads the just-emitted events"
    - "The Phase 129 aggregator runs all 5 phase-129 suites and the Feynman runner registers them"
    - "Phase 109 + Phase 122 test suites still pass (zero regression)"
  artifacts:
    - path: "tests/test-129-spine-acceptance.cjs"
      provides: "the load-bearing instrumented acceptance test mirroring tests/test-navigation-acceptance.cjs"
      min_lines: 150
    - path: "tests/run-all-129.sh"
      provides: "Phase 129 scoped aggregator (mirrors run-all-128.sh)"
      min_lines: 40
    - path: "tests/fixtures/phase-129/sample-room/seed.sql"
      provides: "a seeded room fixture for the proactive-loop acceptance test"
      min_lines: 10
  key_links:
    - from: "tests/test-129-spine-acceptance.cjs"
      to: "tests/helpers/fs-instrument.cjs"
      via: "fsInstrument.install/calls/uninstall around the loop (the Phase 109 zero-reads gate)"
      pattern: "fs-instrument"
    - from: "lib/memory/run-feynman-tests.cjs"
      to: "tests/test-129-spine-acceptance.cjs"
      via: "Phase 129 registration block"
      pattern: "test-129"
---

<objective>
Ship the load-bearing instrumented acceptance test that proves Phase 129 closed the backward
arc: the full proactive loop produces the expected memory_event count + sequence, with zero
non-SQLite filesystem reads outside the named cache files, and the next status render reads the
just-emitted events. Plus the Phase 129 aggregator + Feynman-runner registration so the suite
runs in CI.

Purpose: this is the phase's release gate. It mirrors the Phase 109 instrumented test
(tests/test-navigation-acceptance.cjs) -- proving by instrumentation, not by promise, that SQL
is the local mind and the spine writes the event log via navigation.cjs.

Output: tests/test-129-spine-acceptance.cjs + a fixture seed + tests/run-all-129.sh +
a Phase 129 registration block in the Feynman runner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-spine-repair-memory-event/129-CONTEXT.md
@.planning/phases/129-spine-repair-memory-event/129-01-PLAN.md
@.planning/phases/129-spine-repair-memory-event/129-02-PLAN.md
@.planning/phases/129-spine-repair-memory-event/129-03-PLAN.md
@.planning/phases/129-spine-repair-memory-event/129-04-PLAN.md
@CLAUDE.md

<interfaces>
<!-- The exact pattern to mirror. Read these; do NOT invent a new harness. -->
From tests/test-navigation-acceptance.cjs (THE pattern to copy):
  Uses node:assert/strict (ok, equal); direct-CJS, no Mocha/Jest, zero new npm deps.
  setupRoom(): fs.mkdtempSync a temp dir; openRoomDb(tmp); db.exec(readFileSync(SEED_SQL)); set focus BEFORE the proxy installs.
  fsInstrument.install({ throwOnViolation:false }) -> run the flow -> fsInstrument.uninstall(); fsInstrument.calls() must be [].
  cleanup(tmp): fs.rmSync recursive.
  run(): iterate test fns, print PASS/FAIL, process.exit(fail===0?0:1).

From tests/helpers/fs-instrument.cjs:
  install({ throwOnViolation }) wraps fs reads; calls() returns the non-SQLite read leaks; uninstall() restores.
  ALLOW-LIST seam: reads of the named cache files (jtbd-state.json, conversation-operator.json) are EXPECTED
    by the spine getCurrent* fallback -- the test must allow those two filenames and assert zero OTHER reads.

From lib/core/navigation.cjs (Plan 01-04 surface the test drives):
  logSpineRead / logSuggestionSurfaced / logWorkflowStage / logJtbdTransition / logOperatorTransition (roomDir, payload)
  getCurrentJTBD(roomDir) / getCurrentOperator(roomDir)
  findRecentChanges(db, sinceEpochMs, opts) -> the events the test counts.

From tests/run-all-128.sh (the aggregator shape to mirror):
  SHELL_SUITES=() ; CJS_SUITES=( test-substrate-contract.cjs ) ; per-suite PASS/FAIL; exit non-zero on any fail.

From lib/memory/run-feynman-tests.cjs:
  Per-phase registration blocks of path.join(...) test entries (Phase 124 block, Phase 125 block, etc.).
  Add a Phase 129 block registering the 4 behavior tests + the acceptance test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Phase 129 fixture seed + the instrumented proactive-loop acceptance test</name>
  <read_first>
    - tests/test-navigation-acceptance.cjs (copy its structure verbatim: setupRoom, the fs proxy install/uninstall, the zero-reads LOAD-BEARING assertion, run())
    - tests/helpers/fs-instrument.cjs (the install/calls/uninstall API + how to allow-list the two cache filenames)
    - tests/fixtures/phase-109/sample-room/seed.sql (the seed shape to base the phase-129 fixture on)
    - lib/core/navigation.cjs (the 7 Plan-01 helpers the loop drives)
    - .planning/phases/129-spine-repair-memory-event/129-CONTEXT.md (the acceptance criteria: zero non-SQLite reads outside cache files; exact event count per loop; next status render reads the just-emitted events)
  </read_first>
  <action>
    Create tests/fixtures/phase-129/sample-room/seed.sql: a minimal seeded room with a .mindrian/room.db worth of nodes (a decision node + a couple of section/artifact nodes) sufficient for the loop to resolve. Base it on the phase-109 seed but trim to the minimum the spine loop needs. Also seed (or have setupRoom write) the two cache files <roomDir>/.mindrian/jtbd-state.json + conversation-operator.json so the getCurrent* fallback path has something to read (the test asserts those two reads are ALLOWED and all other non-SQLite reads are zero).

    Create tests/test-129-spine-acceptance.cjs mirroring tests/test-navigation-acceptance.cjs structure exactly (node:assert/strict, direct-CJS, zero new npm deps, setupRoom/cleanup/run). The flow exercised is the full proactive loop in order:
      1. /mos:status surface: navigation.logSpineRead(roomDir, {surface:'status', section, jtbd, operator})
      2. /mos:suggest-next surface: navigation.logSuggestionSurfaced(roomDir, {commands:[{command,score}], top_score})
      3. /mos:act surface: navigation.logWorkflowStage(roomDir, {surface:'act', phase:'entered', framework, autonomy}) then phase:'completed' with follows_from
      4. next /mos:status: navigation.logSpineRead again (second pass)

    Three LOAD-BEARING assertions (per 129-CONTEXT + the Phase 109 precedent):
      (a) Install fsInstrument BEFORE the loop and uninstall AFTER; assert fsInstrument.calls() filtered to EXCLUDE the two allow-listed cache filenames (jtbd-state.json, conversation-operator.json) is empty -- zero non-SQLite filesystem reads outside the cache files during the loop.
      (b) Assert the EXACT memory_event count for one loop pass via findRecentChanges: the precise number of rows (status spine_read + suggestion_surfaced + act entered + act completed = the count your loop emits; with the 60s dedupe, the SECOND status spine_read with an unchanged dedupe_key dedupes, so a repeated status emits 0 new rows -- assert that too). Document the exact expected count in a comment and assert it numerically.
      (c) Assert the next status render READS the just-emitted events: after the loop, call findRecentChanges (or navigation.getCurrentJTBD/getCurrentOperator which read the event log first) and assert it returns the events the loop just wrote (the backward arc is closed -- the next look sees what just happened).

    Also assert a FOLLOWS_FROM edge landed between the act entered + completed events (query the edges table for type='FOLLOWS_FROM').

    NO em-dashes in the authored test. Use the fixture seed via readFileSync BEFORE the proxy installs (the phase-109 precedent: schema bootstrap + seed happen pre-proxy).
  </action>
  <verify>
    <automated>node tests/test-129-spine-acceptance.cjs</automated>
  </verify>
  <done>The acceptance test runs the full proactive loop, asserts zero non-SQLite reads outside the two cache files, asserts the exact memory_event count + the dedupe behavior, asserts the next status render reads the just-emitted events, and asserts the FOLLOWS_FROM edge landed. Exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the Phase 129 aggregator + register the 5 suites in the Feynman runner + zero-regression gate</name>
  <read_first>
    - tests/run-all-128.sh (the aggregator shape: SHELL_SUITES/CJS_SUITES arrays, per-suite PASS/FAIL, exit non-zero on fail, no emoji, no em-dashes)
    - lib/memory/run-feynman-tests.cjs (the per-phase registration block idiom -- add a Phase 129 block)
  </read_first>
  <action>
    Create tests/run-all-129.sh mirroring tests/run-all-128.sh exactly (set -uo pipefail, SCRIPT_DIR resolution, SHELL_SUITES=(), CJS_SUITES with the 5 phase-129 suites: test-129-spine-substrate.cjs, test-129-read-surface-events.cjs, test-129-state-transition-events.cjs, test-129-workflow-stage-events.cjs, test-129-spine-acceptance.cjs). Per-suite PASS/FAIL line; exit non-zero if any suite failed. No emoji. No em-dashes. Make it executable (chmod +x equivalent via the file mode; note in the SUMMARY).

    Add a Phase 129 registration block to lib/memory/run-feynman-tests.cjs: path.join(...) entries for the 5 phase-129 test files, placed after the most recent phase block, with a header comment mapping each test to its plan (mirror the Phase 125 / Phase 124 block comment idiom). Preserve every existing entry byte-identical.

    Zero-regression gate: the SUMMARY must record that node tests/test-navigation-acceptance.cjs (Phase 109) still exits 0 and the Phase 122 workflow suites are unchanged. Run them and capture the result.

    NO em-dashes in any authored file.
  </action>
  <verify>
    <automated>bash tests/run-all-129.sh</automated>
    <automated>node tests/test-navigation-acceptance.cjs</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('lib/memory/run-feynman-tests.cjs','utf8'); if(!s.includes('test-129-spine-acceptance'))throw new Error('Phase 129 not registered in Feynman runner'); console.log('registered')"</automated>
  </verify>
  <done>tests/run-all-129.sh runs all 5 phase-129 suites GREEN; the Feynman runner registers them; Phase 109 acceptance test still exits 0 (zero regression); no em-dashes in authored files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> room.db | the acceptance test is allow-listed (tests/ in the substrate guard); it opens room.db directly to assert state, which is legal for tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-05-01 | Tampering | a spine script leaking a non-SQLite filesystem read into the proactive loop (folder scan instead of graph read) | mitigate | the fs-instrument zero-reads gate is the structural proof; any leak fails the LOAD-BEARING assertion |
| T-129-05-02 | Repudiation | the event count drifting silently (a transition not journaled, or double-journaled) | mitigate | the exact-count assertion + the dedupe assertion pin the loop's event production |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | NONE -- zero new dependencies; node built-ins + bash only |
</threat_model>

<verification>
- bash tests/run-all-129.sh exits 0 (all 5 suites GREEN).
- node tests/test-129-spine-acceptance.cjs exits 0 with the three LOAD-BEARING assertions passing.
- node tests/test-navigation-acceptance.cjs exits 0 (Phase 109 zero regression).
- The Feynman runner contains a Phase 129 block registering all 5 suites.
- Zero em-dashes in authored files.
</verification>

<success_criteria>
- The instrumented acceptance test proves zero non-SQLite reads outside the cache files, the exact event count + sequence, and the next-render-reads-the-events backward arc.
- FOLLOWS_FROM edge linkage asserted.
- Phase 129 aggregator + Feynman registration shipped.
- Zero regression on Phase 109 + Phase 122.
</success_criteria>

<output>
Create `.planning/phases/129-spine-repair-memory-event/129-05-SUMMARY.md` when done.
</output>
