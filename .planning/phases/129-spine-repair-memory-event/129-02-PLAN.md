---
phase: 129-spine-repair-memory-event
plan: 02
type: execute
wave: 2
depends_on: ["129-01"]
files_modified:
  - scripts/mos-status.cjs
  - scripts/memory-command.cjs
  - scripts/suggest-next-command.cjs
  - tests/test-129-read-surface-events.cjs
autonomous: true
requirements: [SPINE-EVENTS, STATE-AUTHORITY]
canon_parts:
  - Part 3 (Tri-Context Decision Gate -- the read surfaces are LOCAL context)
  - Part 9 (Memory Locality -- reads + writes via navigation.cjs)

must_haves:
  truths:
    - "/mos:status emits a spine_read event (surface=status) carrying section + JTBD + operator snapshot on each non-deduped render"
    - "/mos:memory emits a spine_read event (surface=memory) carrying the inspected layer"
    - "/mos:suggest-next emits a suggestion_surfaced event with the suggested commands + their confidence scores"
    - "None of the 3 scripts carry any direct room.db access -- all emission routes through navigation.cjs"
  artifacts:
    - path: "scripts/mos-status.cjs"
      provides: "spine_read emission via navigation.logSpineRead after render"
      contains: "logSpineRead"
    - path: "scripts/memory-command.cjs"
      provides: "spine_read emission via navigation.logSpineRead after inspect"
      contains: "logSpineRead"
    - path: "scripts/suggest-next-command.cjs"
      provides: "suggestion_surfaced emission via navigation.logSuggestionSurfaced"
      contains: "logSuggestionSurfaced"
    - path: "tests/test-129-read-surface-events.cjs"
      provides: "behavior test that the 3 scripts emit the right events with the right payload shape"
      min_lines: 70
  key_links:
    - from: "scripts/mos-status.cjs"
      to: "lib/core/navigation.cjs"
      via: "navigation.logSpineRead(roomDir, payload)"
      pattern: "navigation.*logSpineRead"
    - from: "scripts/suggest-next-command.cjs"
      to: "lib/core/navigation.cjs"
      via: "navigation.logSuggestionSurfaced(roomDir, payload)"
      pattern: "logSuggestionSurfaced"
---

<objective>
Refactor the 3 READ-surface spine scripts (mos-status, memory-command, suggest-next-command)
to emit memory_event through the Plan 01 navigation helpers on every render. These three are
grouped because they share an event shape: status + memory are both spine_read (a render of
existing state), and suggest-next surfaces suggestions. None currently writes anything.

Purpose: closes the backward arc for the three read surfaces. After this plan, every
/mos:status, /mos:memory, and /mos:suggest-next a user runs is journaled and visible to the
NEXT render via navigation.cjs.

Output: 3 refactored scripts + a behavior test. Zero direct room.db access in any of them
(all emission goes through navigation.cjs per the Phase 128 substrate guard).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-spine-repair-memory-event/129-CONTEXT.md
@.planning/phases/129-spine-repair-memory-event/129-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- From Plan 01 (the Wave-1 substrate this plan consumes). Use directly; do NOT re-explore navigation internals. -->
From lib/core/navigation.cjs (Plan 129-01 additions):
  logSpineRead(roomDir, payload) -> { ok, eventId, deduped? } | { ok:false, reason:'no_room_db' }
    payload SHOULD carry: surface ('status'|'memory'), section, jtbd, operator, layer (for memory). Opens/closes room.db internally.
  logSuggestionSurfaced(roomDir, payload) -> { ok, eventId } | { ok:false, reason:'no_room_db' }
    payload SHOULD carry: commands (array of {command, score}), problem_type, top_score.
  getCurrentJTBD(roomDir) -> { jtbd, source } ; getCurrentOperator(roomDir) -> { current, source }
  detectActiveRoom() -> { roomDir, hasRoomDb } | null  (resolve the active room when the script has no --room arg)

From scripts/mos-status.cjs:
  resolveRoomRoot(cwd) -> absolute room root path | null (walks up looking for ROOM.md layout)
  Renders per-section MINTO via folder-memory.readTriple; main() assembles + writes stdout.
From scripts/memory-command.cjs:
  Subcommand dispatcher (overview/query/cross-room/resume/park/complete/--opt-out); resolves room via the registry.
From scripts/suggest-next-command.cjs:
  resolveRoomDir(argRoomDir), readRoomState(roomDir) -> {problemType,...}, recommender.recommendFrameworkChain(opts),
  resolver.composeWorkflow(frameworkChain), ranker scored items[] with numeric .score per item.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit spine_read from /mos:status and /mos:memory</name>
  <read_first>
    - scripts/mos-status.cjs (resolveRoomRoot + main() render path; the lazy-require + graceful-fallback idiom at lines 65-77)
    - scripts/memory-command.cjs (the subcommand dispatcher + registry-based room resolution)
    - lib/core/navigation.cjs (the Plan 01 logSpineRead / getCurrentJTBD / getCurrentOperator / detectActiveRoom re-exports)
    - .planning/phases/129-spine-repair-memory-event/129-CONTEXT.md (state-files-stay-as-cache + idempotency-60s decisions)
  </read_first>
  <behavior>
    - Test: invoking mos-status main() against a seeded temp room (with .mindrian/room.db) writes exactly ONE spine_read memory_event with properties.surface='status' and a section + jtbd + operator snapshot in the payload.
    - Test: invoking it twice in a row with no state change writes ONE spine_read total (the Plan 01 60s dedupe fires off the deterministic dedupe_key).
    - Test: invoking memory-command on the overview subcommand writes ONE spine_read with properties.surface='memory' and properties.layer naming the inspected layer.
    - Test: when the active room has no room.db, neither script throws and neither writes (logSpineRead returns no_room_db; the render still prints).
    - Test (substrate): grep-style assertion that scripts/mos-status.cjs and scripts/memory-command.cjs contain zero direct sqlite require, zero openGraph, zero raw INSERT INTO nodes|edges (the substrate guard scanFiles returns []).
  </behavior>
  <action>
    In scripts/mos-status.cjs: after the render is assembled (at the END of main(), AFTER stdout is written so emission never delays the user-visible frame), resolve the active room dir (reuse resolveRoomRoot(process.cwd()); when null, fall back to navigation.detectActiveRoom().roomDir) and call navigation.logSpineRead(roomDir, { surface: 'status', section: <the rendered section or 'all'>, jtbd: navigation.getCurrentJTBD(roomDir).jtbd, operator: navigation.getCurrentOperator(roomDir).current }). Lazy-require navigation.cjs with the SAME graceful try/catch idiom already used for statuslineCache + folderMemory (lines 65-77) so a navigation load failure degrades to a no-op render, never a crash. Wrap the whole emission in try/catch -- emission is best-effort telemetry, never load-bearing for the render.

    In scripts/memory-command.cjs: after each subcommand renders its output, call navigation.logSpineRead(roomDir, { surface: 'memory', layer: <subcommand name: 'overview'|'query'|'cross-room'|'resume'|'park'|'complete'> }). Resolve roomDir via the script's existing registry resolution (or navigation.detectActiveRoom() fallback). Same lazy-require + try/catch graceful idiom. Do NOT emit on the --opt-out subcommand (the user is opting out of memory; emitting would contradict that).

    Do NOT open room.db in either script. Do NOT require room-db.cjs / lazygraph-ops.cjs / node:sqlite. All graph traffic goes through navigation.cjs (per the Phase 128 substrate contract; these scripts are NOT allow-listed). NO em-dashes in authored lines.
  </action>
  <verify>
    <automated>node tests/test-129-read-surface-events.cjs</automated>
    <automated>node -e "const g=require('./scripts/check-substrate.cjs'); const fs=require('fs'); const v=g.scanFiles(['scripts/mos-status.cjs','scripts/memory-command.cjs'],p=>fs.readFileSync(p,'utf8')); if(v.length)throw new Error('substrate violation: '+JSON.stringify(v)); console.log('clean')"</automated>
  </verify>
  <done>Both scripts emit spine_read with the right surface enum + snapshot, dedupe on repeat, degrade gracefully with no room.db, and carry zero direct room.db access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit suggestion_surfaced from /mos:suggest-next with the suggested commands + confidence scores</name>
  <read_first>
    - scripts/suggest-next-command.cjs (resolveRoomDir + readRoomState + recommender.recommendFrameworkChain + resolver.composeWorkflow + the ranker scored items at lines 171-242)
    - lib/core/navigation.cjs (the Plan 01 logSuggestionSurfaced re-export)
  </read_first>
  <behavior>
    - Test: invoking suggest-next main() against a seeded temp room writes ONE suggestion_surfaced memory_event whose properties.commands is an array of { command, score } and properties.top_score is the max score.
    - Test: the commands in the event are exactly the commands the script printed (sourced from composeWorkflow / the registry, never fabricated) -- assert the event command list equals the rendered command list.
    - Test: when there are zero recommendable frameworks, the script still renders its degrade message and writes a suggestion_surfaced with an empty commands array (or skips emission -- pick one and assert it; do not throw).
    - Test (substrate): scripts/suggest-next-command.cjs scanFiles returns [] (zero direct room.db access).
  </behavior>
  <action>
    In scripts/suggest-next-command.cjs: after the framework chain is composed into the command sequence (the resolver.composeWorkflow result + the ranker scored items[]), build a commands array of { command, score } from the resolved + ranked output (use the SAME numeric .score the renderer reads at line 171/191 so the event matches the printed surface exactly), then call navigation.logSuggestionSurfaced(resolveRoomDir(args.roomDir) or navigation.detectActiveRoom().roomDir, { commands, problem_type: roomState.problemType || null, top_score: <max score> }). Emit AFTER stdout so the user-visible suggestion is never delayed. Lazy-require navigation.cjs with a graceful try/catch (mirror the existing top-of-file require block; degrade to no-op on load failure). Wrap emission in try/catch -- best-effort telemetry.

    The commands MUST come from composeWorkflow / the generated registry (Larry never names a /mos: command from memory -- the existing invariant). Do NOT invent scores; use the ranker's numeric .score. Do NOT open room.db; route through navigation.cjs only. NO em-dashes.
  </action>
  <verify>
    <automated>node tests/test-129-read-surface-events.cjs</automated>
    <automated>node -e "const g=require('./scripts/check-substrate.cjs'); const fs=require('fs'); const v=g.scanFiles(['scripts/suggest-next-command.cjs'],p=>fs.readFileSync(p,'utf8')); if(v.length)throw new Error('substrate violation: '+JSON.stringify(v)); console.log('clean')"</automated>
  </verify>
  <done>/mos:suggest-next emits suggestion_surfaced with commands + confidence scores matching the rendered surface, sourced from the resolver (never fabricated), with zero direct room.db access.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spine read script -> navigation.cjs | scripts pass roomDir + scalar payload; the chokepoint owns room.db |
| rendered surface -> memory_event payload | only the printed scalars (section, jtbd, operator, command list, scores) become event data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-02-01 | Tampering | a read script opening room.db directly (the bug this phase fixes) | mitigate | emission routes ONLY through navigation.logSpineRead / logSuggestionSurfaced; substrate guard scanFiles asserts zero direct access |
| T-129-02-02 | Information Disclosure | user content (governing_thought prose, artifact bodies) leaking into the event payload | mitigate | payloads carry scalars + enums + command names + numeric scores only; no raw MINTO body, no artifact text |
| T-129-02-03 | Availability | emission delaying or crashing the user-visible render | accept->mitigate | emission is best-effort: it runs AFTER stdout, wrapped in try/catch, degrades to no-op on any failure |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | NONE -- zero new dependencies; node built-ins + existing requires only |
</threat_model>

<verification>
- node tests/test-129-read-surface-events.cjs exits 0.
- scripts/check-substrate.cjs scanFiles over all 3 scripts returns [] (zero net-new bypass).
- grep -v '^#' the 3 scripts | grep -c emdash == 0 (no em-dashes).
- Existing render output for each command is byte-stable on the happy path (emission is additive, post-stdout).
</verification>

<success_criteria>
- /mos:status + /mos:memory emit spine_read with the right surface enum + snapshot.
- /mos:suggest-next emits suggestion_surfaced with command list + scores from the resolver.
- All 3 scripts carry zero direct room.db access (substrate guard clean).
- Dedup fires on repeated no-op /mos:status.
</success_criteria>

<output>
Create `.planning/phases/129-spine-repair-memory-event/129-02-SUMMARY.md` when done.
</output>
