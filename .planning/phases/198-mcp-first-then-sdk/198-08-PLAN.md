---
phase: 198-mcp-first-then-sdk
plan: 08
type: execute
wave: 4
depends_on: ["198-03", "198-06"]
files_modified:
  - scripts/statusline-mos-dispatch
  - scripts/sessionstart-coordinator.cjs
  - hooks/hooks.json
  - lib/mcp/adapter-client.cjs
  - tests/test-198-adapter-budget.test.cjs
autonomous: true
requirements: [SPEC-5]
must_haves:
  truths:
    - "The statusline and SessionStart hooks wake and query mindrian-core; their business logic (segment composition, session reconcile) runs server-side"
    - "The migrated hook scripts import NO lib/core business modules (import audit passes)"
    - "The migrated hook scripts stay under a measured line-count budget"
    - "The statusline segment (incl. spend/cap) comes from status_read; SessionStart binding comes from the daemon"
  artifacts:
    - path: "lib/mcp/adapter-client.cjs"
      provides: "the thin HTTP query helper hooks use to reach the daemon"
      exports: ["queryDaemon", "wakeDaemon"]
    - path: "tests/test-198-adapter-budget.test.cjs"
      provides: "D-06 import audit + line-count budget over migrated hooks"
      contains: "lib/core"
    - path: "scripts/sessionstart-coordinator.cjs"
      provides: "thin SessionStart adapter (wake + query, no business logic)"
  key_links:
    - from: "scripts/statusline-mos-dispatch"
      to: "lib/mcp/adapter-client.cjs"
      via: "wake daemon + query status_read for the segment"
      pattern: "queryDaemon\\|status_read"
    - from: "tests/test-198-adapter-budget.test.cjs"
      to: "scripts/sessionstart-coordinator.cjs"
      via: "import audit asserts zero lib/core business requires"
      pattern: "lib/core"
---

<objective>
Begin the thin-plugin adapter refactor (SPEC-5) with the LOW-RISK, HIGH-payoff surfaces FIRST (D-05): the statusline and SessionStart hooks become wake-up + render adapters that query mindrian-core; their business logic (segment composition, session reconcile) moves server-side. Enforce "adapter-only" as a MEASURED budget (D-06): an import audit (no lib/core business modules from hook scripts) plus a line-count budget, checked in CI.

Purpose: The statusline segment (including spend/cap) already comes from status_read (Plan 06); the SessionStart binding comes from the daemon (Plan 02/03). Moving these two first proves the thin-adapter shape without touching the risky Stop-gate (Plan 09, LAST). Runs in parallel with chain_run (Plan 07) - disjoint files.
Output: lib/mcp/adapter-client.cjs, thin statusline + SessionStart adapters, the D-06 adapter-budget test.

D-05 discipline: statusline + SessionStart ONLY in this plan. Stop-gate migration is Plan 09 and must NOT be touched here (moving it early re-creates the card-misfire regression class - Pitfall 7). D-06: hooks may wake, query, render; never import a lib/core business module.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-mcp-first-then-sdk/198-SPEC.md
@.planning/phases/198-mcp-first-then-sdk/198-CONTEXT.md
@.planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md
@.planning/phases/198-mcp-first-then-sdk/198-PATTERNS.md
@scripts/statusline-mos-dispatch
@scripts/sessionstart-coordinator.cjs
@hooks/hooks.json
@.planning/phases/198-mcp-first-then-sdk/198-03-SUMMARY.md
@.planning/phases/198-mcp-first-then-sdk/198-06-SUMMARY.md
</context>

## Artifacts this phase produces (full inventory)

Full inventory in 198-01/02-PLAN.md. **This plan (198-08) delivers:** lib/mcp/adapter-client.cjs (the thin daemon-query helper), the thin statusline + SessionStart adapters, hooks.json migration markers, and the D-06 adapter-budget test.

<tasks>

<task type="auto">
  <name>Task 1: adapter-client helper + thin statusline + thin SessionStart</name>
  <read_first>
    - scripts/statusline-mos-dispatch (the shipped "zero logic by design" shim form to generalize - 49 lines, exec-based fallback)
    - scripts/sessionstart-coordinator.cjs (171 lines; currently imports lib/core - the business logic to move server-side)
    - lib/mcp/daemon-lifecycle.cjs (Plan 03 - ensureDaemon/discoverPort for wake)
    - lib/mcp/tools/status.cjs (Plan 06 - status_read the statusline queries)
    - .planning/phases/198-mcp-first-then-sdk/198-PATTERNS.md (Pattern 4 thin adapter; the hooks.json SessionStart matcher shape to keep)
    - .planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md (SPEC-5 target: hooks wake + query; business logic server-side; D-05 order; D-06 budget)
  </read_first>
  <action>
    Create `lib/mcp/adapter-client.cjs` exporting `wakeDaemon()` (calls ensureDaemon from Plan 03) and `queryDaemon(tool, args)` (a thin HTTP call to the durable daemon over 127.0.0.1 at the discovered port, returning the tool result). This helper is the ONLY server-reach a hook needs; it carries no business logic. It IS allowed to require lib/mcp/daemon-lifecycle.cjs (that is adapter plumbing, not a lib/core business module) - the D-06 audit forbids lib/core/lib/workflow/lib/memory business imports FROM HOOK SCRIPTS, not the adapter-client's own daemon plumbing.
    Migrate `scripts/statusline-mos-dispatch` to the thin shape (behind the flag): wake the daemon, `queryDaemon('status_read', ...)` for the segment (including spend/cap), render it, and on ANY trouble emit nothing so Claude Code's default statusline renders (keep the shipped forgiving contract). Flag OFF: exec the existing statusline-mos exactly as today (byte-identical legacy).
    Migrate `scripts/sessionstart-coordinator.cjs` to the thin shape (behind the flag): wake the daemon and query it for the session binding + start segments; move its lib/core business calls (the reconcile/coordination logic) into a server-side handler the daemon runs. Flag OFF: run the existing coordinator logic unchanged. Remove the lib/core business `require`s from the flag-ON adapter path so the D-06 audit passes.
    Keep the hooks/hooks.json SessionStart matcher shape (startup|clear|compact, node command, timeout/async); only the target script goes thin. Add a migration marker comment in hooks.json for the two migrated surfaces so test-198-adapter-budget can enumerate them. No em-dashes.
  </action>
  <acceptance_criteria>
    - `node -e "const a=require('./lib/mcp/adapter-client.cjs'); if(typeof a.queryDaemon!=='function'||typeof a.wakeDaemon!=='function') throw 0; console.log('ok')"` prints `ok`.
    - `grep -c "require(.*lib/core\|require(.*lib/workflow\|require(.*lib/memory" scripts/sessionstart-coordinator.cjs` returns 0 on the flag-ON adapter path (business imports moved server-side).
    - `grep -n "queryDaemon\|status_read\|wakeDaemon" scripts/statusline-mos-dispatch` matches (statusline queries the daemon).
    - `bash scripts/statusline-mos-dispatch < /dev/null; echo "exit=$?"` exits 0 and never throws (forgiving contract preserved).
    - `MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs` exits 0 (flag-OFF plugin smoke: statusline + SessionStart byte-identical legacy).
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('./lib/mcp/adapter-client.cjs')" && MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs; echo "exit=$?"</automated>
  </verify>
  <done>statusline + SessionStart wake and query mindrian-core; their business logic runs server-side; the flag-OFF path is byte-identical legacy; the adapter-client is the single thin reach.</done>
</task>

<task type="auto">
  <name>Task 2: D-06 adapter-budget import audit + line-count budget test</name>
  <read_first>
    - tests/test-198-adapter-budget.test.cjs (Plan 01 stub to fill)
    - scripts/sessionstart-coordinator.cjs + scripts/statusline-mos-dispatch (Task 1 outputs - the migrated surfaces the audit enumerates)
    - hooks/hooks.json (the migration markers Task 1 added)
    - .planning/phases/198-mcp-first-then-sdk/198-CONTEXT.md (D-06: import audit + line-count budget enforce adapter-only in CI)
    - .planning/phases/198-mcp-first-then-sdk/198-PATTERNS.md ("No Analog Found" row for adapter-budget - author fresh)
  </read_first>
  <action>
    Fill `tests/test-198-adapter-budget.test.cjs` (Plan 01 stub, net-new check per D-06). Two assertions over the MIGRATED hook surfaces (enumerated from the hooks.json migration markers Task 1 added - do NOT audit un-migrated scripts, which Plan 09 handles): (1) IMPORT AUDIT - each migrated hook script contains ZERO `require(...lib/core...)`, `require(...lib/workflow...)`, `require(...lib/memory...)` business imports (adapter-client's daemon plumbing is allowed; the audit targets hook scripts, not lib/mcp/adapter-client.cjs). Use a comment-stripped grep (`grep -v '^#'` / strip `//` lines) so a header comment naming lib/core does not self-invalidate the gate. (2) LINE-COUNT BUDGET - each migrated hook script stays under a measured budget (set the budget from the thin post-migration line counts with a small margin; record the exact numbers in the test and the SUMMARY). The test PASSES today over the two migrated surfaces and is the CI guard against re-fattening.
    No em-dashes. CJS only.
  </action>
  <acceptance_criteria>
    - `node tests/test-198-adapter-budget.test.cjs` exits 0.
    - The test enumerates ONLY the migrated surfaces (statusline-mos-dispatch, sessionstart-coordinator.cjs) from the hooks.json markers, not the Stop-gate scripts (Plan 09).
    - The import audit uses a comment-stripped match (grep -v of comment lines) before counting lib/core imports (no self-invalidating header prose).
    - Introducing a `require('../lib/core/navigation.cjs')` into scripts/sessionstart-coordinator.cjs makes the test FAIL (guard is live) - verify by temporary injection then revert.
    - `bash tests/run-all-198.sh` exits 0 with the adapter-budget leg promoted from SKIP to PASS.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-198-adapter-budget.test.cjs && bash tests/run-all-198.sh; echo "exit=$?"</automated>
  </verify>
  <done>The D-06 adapter-only budget is enforced by CI: migrated hooks carry zero lib/core business imports and stay under the line budget; the guard fails on a re-fattening import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook script -> daemon (127.0.0.1) | The thin adapter queries the daemon; a compromised query path could inject a segment or a false binding |
| adapter-client -> daemon HTTP | Loopback-only reach; must not expose a remote listener |
| migrated hook -> lib/core | The D-06 boundary: business logic must not creep back into hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-06 | Information Disclosure | adapter-client daemon reach | mitigate | queryDaemon connects 127.0.0.1 only via daemon-lifecycle's discovered port; no remote host; grep asserts no 0.0.0.0 |
| T-198-13 | Tampering | business logic re-fattening the plugin | mitigate | test-198-adapter-budget import audit + line-count budget fail closed on any lib/core business import into a migrated hook |
| T-198-05 | Information Disclosure | statusline segment payload | mitigate | status_read segment is navigator data (spend/cap); the hook renders it, never sends room content to Brain; test-198-local-only covers adapter-client |
| T-198-07 | Elevation of Privilege | Stop-gate migrated too early | mitigate | D-05 order enforced: this plan touches ONLY statusline + SessionStart; the adapter-budget test enumerates only migrated surfaces; Stop-gate is Plan 09 after server-side gate dedup |
| T-198-SC | Tampering | npm installs | accept | No new installs; N/A per RESEARCH audit |
</threat_model>

<verification>
- `node tests/test-198-adapter-budget.test.cjs` green (import audit + line budget - D-06).
- `bash scripts/statusline-mos-dispatch < /dev/null` exits 0 and never throws (forgiving contract).
- `MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs` green (flag-OFF plugin smoke).
- `bash tests/run-all-198.sh` exits 0 with the adapter-budget leg PASS.
- Only statusline + SessionStart migrated; Stop-gate untouched (D-05).
</verification>

<success_criteria>
The statusline and SessionStart hooks are thin adapters that wake and query mindrian-core; their business logic runs server-side; a CI import-audit + line-budget enforces adapter-only. Stop-gate is deliberately untouched. Flag OFF is byte-identical legacy.
</success_criteria>

<output>
Create `.planning/phases/198-mcp-first-then-sdk/198-08-SUMMARY.md` when done.
</output>
