---
phase: 198-mcp-first-then-sdk
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/run-all-198.sh
  - tests/test-198-concurrency-mcp.test.cjs
  - tests/test-198-contract-schema.test.cjs
  - tests/test-198-chokepoint-guard.test.cjs
  - tests/test-198-chain-run-halt.test.cjs
  - tests/test-198-gate-renderers.test.cjs
  - tests/test-198-adapter-budget.test.cjs
  - tests/test-198-flag-off-parity.test.cjs
  - tests/test-198-local-only.test.cjs
  - tests/parity-198.sh
  - scripts/198-plurai-gate-check.cjs
  - evals/plurai/198-baseline.json
autonomous: true
requirements: [SPEC-1, SPEC-2, SPEC-3, SPEC-4, SPEC-5, SPEC-6, SPEC-7, SPEC-8]
must_haves:
  truths:
    - "bash tests/run-all-198.sh exits 0 with every module leg SKIPPED (no phase modules exist yet)"
    - "node scripts/198-plurai-gate-check.cjs runs and reports a baseline_deferred degrade or a clean load"
    - "tests/test-198-local-only.test.cjs enforces the Part 8 source-grep floor and passes"
  artifacts:
    - path: "tests/run-all-198.sh"
      provides: "SKIP-safe phase aggregator cloned from run-all-194.sh"
      contains: "run_if"
    - path: "scripts/198-plurai-gate-check.cjs"
      provides: "Plurai parity gate cloned from 189-plurai-gate-check.cjs"
      contains: "PLURAI_GATE_OK"
    - path: "evals/plurai/198-baseline.json"
      provides: "198 invocation-parity baseline fixture"
    - path: "tests/test-198-concurrency-mcp.test.cjs"
      provides: "SPEC-1 two-session concurrency guard (stub until 198-02 lands session-registry)"
  key_links:
    - from: "tests/run-all-198.sh"
      to: "tests/test-198-*.test.cjs"
      via: "run_if gated on the net-new module each test targets"
      pattern: "run_if"
    - from: "scripts/198-plurai-gate-check.cjs"
      to: "evals/plurai/198-baseline.json"
      via: "JSON.parse of BASELINE_PATH"
      pattern: "198-baseline\\.json"
---

<objective>
Author the Wave 0 test scaffold for Phase 198 BEFORE any substrate module lands: a SKIP-safe aggregator, nine `run_if`-gated test stubs (one per SPEC requirement's flagship behavior), the Part 8 local-only source-grep floor, a parity-transcript scaffold, and the Plurai invocation-parity gate cloned from the shipped Phase 189 pattern.

Purpose: Nyquist-compliant feedback. Every later plan's `<verify>` names a test that exists here as a SKIP-safe stub, then flips to a real assertion when its module lands. Nothing FAILs for a not-yet-built module; only real defects fail.
Output: `tests/run-all-198.sh`, 9 test files, `tests/parity-198.sh`, `scripts/198-plurai-gate-check.cjs`, `evals/plurai/198-baseline.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-mcp-first-then-sdk/198-SPEC.md
@.planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md
@.planning/phases/198-mcp-first-then-sdk/198-PATTERNS.md
@tests/run-all-194.sh
@tests/test-194-concurrency-integration.test.cjs
@tests/test-194-local-only.test.cjs
@scripts/189-plurai-gate-check.cjs
</context>

## Artifacts this phase produces (full inventory - authoritative for the verifier)

**New tools (20):** room_bind, room_list, room_state, room_search, graph_query, graph_write, memory_event, chain_resolve, chain_run, framework_run, gate_render, gate_answer, suggest_next, reach_candidates, contradiction_check, whitespace_scan, artifact_file, view_compile, status_read, contract_version.
**New modules:** lib/mcp/mcp-first-flag.cjs, lib/mcp/session-registry.cjs, lib/mcp/daemon-lifecycle.cjs, lib/mcp/sse-event-bus.cjs, lib/mcp/contract-version.cjs, lib/mcp/register-core-tools.cjs, lib/mcp/tools/{room,graph,gate,chain,sensors,views,status}.cjs, lib/mcp/gate-render.cjs, lib/mcp/gate-dedup.cjs, bin/mindrian-mcp-shim.cjs.
**New data/config:** data/mcp-tool-connectors.json (MCP-tool connector source manifest), evals/plurai/198-baseline.json.
**New scripts/tests:** scripts/198-plurai-gate-check.cjs, scripts/198-rollback-rehearsal.cjs, tests/run-all-198.sh, tests/parity-198.sh, tests/test-198-*.test.cjs (9 files below).
**New env flag:** MINDRIAN_MCP_FIRST (per-surface list: e.g. `cli`, `cli,desktop`, `all`; unset/empty = byte-identical legacy).
**New connector-registry entries:** one per new tool, generated from data/mcp-tool-connectors.json by the extended scripts/build-connector-registry.cjs.
**Modified:** bin/mindrian-mcp-server.cjs, lib/mcp/tool-router.cjs, hooks/hooks.json, scripts/sessionstart-coordinator.cjs, scripts/statusline-mos-dispatch, scripts/build-connector-registry.cjs.

*This plan (198-01) produces only the test scaffold + Plurai gate + baseline listed in `files_modified`.*

<tasks>

<task type="auto">
  <name>Task 1: SKIP-safe aggregator + nine run_if-gated test stubs + Part 8 local-only floor</name>
  <read_first>
    - tests/run-all-194.sh (the SKIP-safe run/run_if helpers to clone byte-identical, lines 34-57)
    - tests/test-194-concurrency-integration.test.cjs (the SPEC-1 concurrency shape to clone into test-198-concurrency-mcp)
    - tests/test-194-local-only.test.cjs (the Part 8 source-grep floor to clone)
    - .planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md (the "Phase Requirements -> Test Map" and "Wave 0 Gaps" tables - the exact 9 test filenames + which module each run_if gates on)
  </read_first>
  <action>
    Create `tests/run-all-198.sh` by cloning `tests/run-all-194.sh`: copy the `run` and `run_if` helper functions verbatim, the SKIP/PASS/FAIL counters, and the exit-code roll-up. Retarget the body to the nine SPEC test files. Every leg targeting a not-yet-built module uses `run_if "<label>" "<module-path-it-needs>" node tests/<file>` so a missing module SKIPs, never FAILs. Two legs are HARD floors that must PASS in Wave 0: `tests/test-198-local-only.test.cjs` (always runs) and the aggregator's own self-check.
    Create the nine test files as SKIP-safe stubs, each keyed to its SPEC requirement per the RESEARCH test map: test-198-concurrency-mcp (SPEC-1, clone test-194-concurrency-integration structure, guard on lib/mcp/session-registry.cjs presence), test-198-contract-schema (SPEC-2, guard on lib/mcp/contract-version.cjs), test-198-chokepoint-guard (SPEC-2, guard on lib/mcp/tools/graph.cjs), test-198-chain-run-halt (SPEC-3, guard on lib/mcp/tools/chain.cjs), test-198-gate-renderers (SPEC-4, guard on lib/mcp/gate-render.cjs), test-198-adapter-budget (SPEC-5, guard on hooks/hooks.json migration marker), test-198-flag-off-parity (SPEC-7, guard on lib/mcp/mcp-first-flag.cjs), test-198-local-only (SPEC Part 8 floor - clone test-194-local-only and point its source-grep at lib/mcp/*.cjs). Each stub: if its guarded module is absent, print a SKIP line and `process.exit(0)`; the real assertions are filled by the owning plan. `test-198-local-only.test.cjs` is NOT a stub - it greps lib/mcp source for Brain/network tokens and asserts none, passing today over the existing lib/mcp files.
    Create `tests/parity-198.sh` as a scaffold: a documented transcript sequence (bind room, reach card via gate_render, chain_run with halt, gate_answer approve, gated write via navigation.cjs, artifact_file) with each step `run_if`-gated and a header comment naming the two hosts (Claude Code CLI + one elicitation-capable MCP host). It SKIPs cleanly today.
    No em-dashes in any authored file. CJS only for the .cjs test files.
  </action>
  <acceptance_criteria>
    - `bash tests/run-all-198.sh` exits 0.
    - Its output contains `SKIPPED` for every leg whose target module does not yet exist, and `PASS` for `test-198-local-only`.
    - `node tests/test-198-local-only.test.cjs` exits 0 and its source asserts zero Brain/network tokens across `lib/mcp/*.cjs`.
    - `grep -c "run_if" tests/run-all-198.sh` returns a count >= 8 (one gated leg per SPEC test).
    - `grep -rIl "\xe2\x80\x94" tests/run-all-198.sh tests/test-198-*.test.cjs tests/parity-198.sh` returns no matches (no em-dashes; use the literal em-dash byte check).
    - `bash tests/parity-198.sh` exits 0 with all steps SKIPPED.
  </acceptance_criteria>
  <verify>
    <automated>bash tests/run-all-198.sh; echo "exit=$?"</automated>
  </verify>
  <done>run-all-198.sh green with all module legs SKIPPED; the Part 8 local-only floor and the parity scaffold both pass; nine stub files present and run_if-wired.</done>
</task>

<task type="auto">
  <name>Task 2: Plurai invocation-parity gate + 198 baseline (clone 189)</name>
  <read_first>
    - scripts/189-plurai-gate-check.cjs (the 88-line gate contract to clone: BASELINE_PATH load, runGateCheck, PLURAI_GATE_OK/FAIL stdout + exit codes)
    - evals/plurai/189-baseline.json (the baseline JSON shape: method + verdict fields)
    - evals/plurai/196-baseline.json (the baseline_deferred degrade precedent from the roadmap GATE lineage)
    - .planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md ("Plurai gate pattern to clone for SPEC-8" code block)
  </read_first>
  <action>
    Clone `scripts/189-plurai-gate-check.cjs` to `scripts/198-plurai-gate-check.cjs`. Retarget `BASELINE_PATH` to `evals/plurai/198-baseline.json`. Keep the exact contract: `runGateCheck()` loads the baseline, and on load-failure returns `{ ok:false, reason:'baseline_load_failed' }`; on success returns `{ ok:true, method }`; the CLI main prints `PLURAI_GATE_OK method=<m>` and exits 0 on pass, `PLURAI_GATE_FAIL <json>` and exits 1 on mismatch. `module.exports = { runGateCheck }`.
    Create `evals/plurai/198-baseline.json` scoped to one-path invocation parity across CLI + one MCP host against the transcript corpus (SPEC-8). Seed it on the `baseline_deferred` degrade path (the shipped precedent for phases where the interactive Plurai optimize run is navigator-gated), matching the 196/200/201 baseline shape: a `method` field, a `verdict` field, and a note that the real parity corpus is filled once the parity transcript (SPEC-6) runs on two hosts. The gate must LOAD and PASS on this deferred baseline so run-all-198 stays green; Plan 10 replaces the deferred verdict with the measured two-host parity result.
    No em-dashes. CJS only.
  </action>
  <acceptance_criteria>
    - `node scripts/198-plurai-gate-check.cjs` prints a line beginning `PLURAI_GATE_OK` and exits 0.
    - `evals/plurai/198-baseline.json` parses as valid JSON and contains a `method` key.
    - `scripts/198-plurai-gate-check.cjs` requires `evals/plurai/198-baseline.json` (grep confirms the retargeted path) and does NOT reference `189-baseline.json`.
    - `node -e "require('./scripts/198-plurai-gate-check.cjs').runGateCheck()"` returns an object with `ok:true`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/198-plurai-gate-check.cjs; echo "exit=$?"</automated>
  </verify>
  <done>The 198 Plurai gate loads its own baseline and exits 0 on the deferred verdict; wired into run-all-198.sh as a SKIP-safe leg that passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> lib/mcp source files | The local-only floor greps source for forbidden Brain/network tokens; a false-negative grep would let egress slip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-05 | Information Disclosure | test-198-local-only source grep over lib/mcp/*.cjs | mitigate | Clone the Phase 194 local-only floor verbatim; assert zero Brain/network tokens; this floor is the Wave 0 Part 8 tripwire every later lib/mcp module must pass |
| T-198-07 | Elevation of Privilege | run_if gating (a real test silently SKIPping forever) | mitigate | Each run_if is keyed to a specific module path the owning plan creates; the owning plan flips the stub to a real assertion, and run-all-198 counts SKIPs so a stuck-SKIP is visible in the roll-up |
| T-198-SC | Tampering | npm/pip/cargo installs | accept | No new installs this phase - all deps vendored (RESEARCH Package Legitimacy Audit: N/A). No slopcheck needed; row retained per template |
</threat_model>

<verification>
- `bash tests/run-all-198.sh` exits 0 (all module legs SKIP, local-only + parity scaffold PASS).
- `node scripts/198-plurai-gate-check.cjs` exits 0.
- No em-dashes in any authored file.
</verification>

<success_criteria>
Nine SKIP-safe test stubs + aggregator + parity scaffold + Plurai gate + baseline exist and are green with zero phase modules present. Every downstream plan can name a test that already exists.
</success_criteria>

<output>
Create `.planning/phases/198-mcp-first-then-sdk/198-01-SUMMARY.md` when done.
</output>
