---
phase: 198-mcp-first-then-sdk
plan: 04
type: execute
wave: 2
depends_on: ["198-02"]
files_modified:
  - lib/mcp/contract-version.cjs
  - lib/mcp/register-core-tools.cjs
  - lib/mcp/tools/room.cjs
  - lib/mcp/tools/graph.cjs
  - lib/mcp/tool-router.cjs
  - scripts/build-connector-registry.cjs
  - data/mcp-tool-connectors.json
  - tests/test-198-contract-schema.test.cjs
  - tests/test-198-chokepoint-guard.test.cjs
autonomous: true
requirements: [SPEC-2]
must_haves:
  truths:
    - "A contract_version tool returns a semver string for the mindrian-core contract"
    - "room_list/room_state/room_search and graph_query/graph_write/memory_event are callable MCP tools with per-tool zod schemas that validate"
    - "Every graph write routes through navigation.cjs; a write attempted around the chokepoint is rejected under test"
    - "Every new MCP tool carries a connector + HITL-shape declaration (exported from its module); build-connector-registry.cjs discovers them and regenerates data/mcp-tool-connectors.json + the connector registry clean"
  artifacts:
    - path: "lib/mcp/contract-version.cjs"
      provides: "contract_version tool returning the mindrian-core contract semver"
      exports: ["CONTRACT_VERSION", "registerContractVersion"]
    - path: "lib/mcp/register-core-tools.cjs"
      provides: "auto-discovery seam registering lib/mcp/tools/*.cjs modules"
      exports: ["registerCoreTools"]
    - path: "lib/mcp/tools/graph.cjs"
      provides: "graph_query/graph_write/memory_event wrapping navigation.cjs chokepoint"
      contains: "navigation"
    - path: "scripts/build-connector-registry.cjs"
      provides: "extended to discover MCP-tool `connectors` exports and generate data/mcp-tool-connectors.json"
      contains: "mcp-tool-connectors"
  key_links:
    - from: "lib/mcp/tools/graph.cjs"
      to: "lib/core/navigation.cjs"
      via: "graph_write/memory_event route through the single SQL chokepoint"
      pattern: "navigation"
    - from: "scripts/build-connector-registry.cjs"
      to: "lib/mcp/tools/*.cjs + tool-router MCP_TOOL_CONNECTORS"
      via: "discovers each module's exported connectors descriptor and generates the MCP-tool registry"
      pattern: "connectors"
    - from: "lib/mcp/register-core-tools.cjs"
      to: "lib/mcp/tools/*.cjs"
      via: "directory auto-discovery + register(server, ctx)"
      pattern: "tools"
---

<objective>
Stand up the versioned mindrian-core contract's core (SPEC-2): a `contract_version` tool returning a semver, a tool-module registration seam (`lib/mcp/tools/*.cjs` auto-discovered by `register-core-tools.cjs`) so later plans add disjoint tool files, the room and graph tool groups (room_list/state/search, graph_query/graph_write/memory_event), the navigation.cjs chokepoint guard, AND - the load-bearing gap - a born-wired mechanism that makes MCP tools first-class in the connector registry.

Purpose: Today `build-connector-registry.cjs` and `check-shape-declaration.cjs` scan ONLY commands/skills/agents; MCP tools (existing and new) are invisible to the Part 11 born-wired gate. Every new tool this phase ships (room_bind, graph_*, gate_*, chain_*, status_read, ...) must carry a connector + HITL-shape declaration and `--check` must regenerate clean. This plan teaches the build script to DISCOVER each tool module's exported `connectors` descriptor (the source of truth) and GENERATE `data/mcp-tool-connectors.json` (the MCP-tool projection, "do not edit by hand" like `data/connector-registry.json`).
Output: contract_version, the tools/ seam, room+graph tools, the chokepoint guard, and the MCP-tool born-wired generation.

Reuse, never rebuild: graph tools WRAP navigation.cjs (Part 9); do NOT mint a second graph writer. room tools wrap the shipped room registry reads.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-mcp-first-then-sdk/198-SPEC.md
@.planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md
@.planning/phases/198-mcp-first-then-sdk/198-PATTERNS.md
@lib/mcp/tool-router.cjs
@lib/core/navigation.cjs
@scripts/build-connector-registry.cjs
@data/connector-registry.json
@.planning/phases/198-mcp-first-then-sdk/198-02-SUMMARY.md
</context>

## Artifacts this phase produces (full inventory)

Full inventory in 198-01/02-PLAN.md. **Born-wired model:** each tool module exports a `connectors` array (the SOURCE of truth); `data/mcp-tool-connectors.json` is GENERATED by build-connector-registry.cjs, never hand-edited. **This plan (198-04) delivers:** the `contract_version`, `room_list`, `room_state`, `room_search`, `graph_query`, `graph_write`, `memory_event` tools; lib/mcp/register-core-tools.cjs (the tools/ seam); lib/mcp/tools/{room,graph}.cjs; the extension to scripts/build-connector-registry.cjs that discovers MCP-tool connector exports and generates data/mcp-tool-connectors.json; the first generation of data/mcp-tool-connectors.json (including room_bind from Plan 02).

<tasks>

<task type="auto">
  <name>Task 1: Born-wired MCP-tool generation + tools/ registration seam + contract_version</name>
  <read_first>
    - scripts/build-connector-registry.cjs (lines 248-290 source-file discovery over commands/skills/agents; the connector object shape lines 340-360; how --check regenerates + exits 1 on drift; the "GENERATED - do not edit by hand" header)
    - scripts/check-shape-declaration.cjs (lines 651-672 the source dirs it enumerates - MCP tools are absent; the hitl_shape validation rules :166-215)
    - data/connector-registry.json (the generated connector entry shape to mirror for MCP tools)
    - lib/mcp/tool-router.cjs (Plan 02 - the exported MCP_TOOL_CONNECTORS array carrying room_bind; :383 registerRouterTools where registerCoreTools hooks in; the :102-125 server.tool pattern)
    - .planning/phases/198-mcp-first-then-sdk/198-PATTERNS.md (Born-wired tool registration; the "new tools" per-tool wiring block)
  </read_first>
  <action>
    Create `lib/mcp/register-core-tools.cjs` exporting `registerCoreTools(server, ctx)` where `ctx` carries the sessionId accessor, fallbackRoomDir, and pluginRoot. It reads `lib/mcp/tools/` at load, requires each `*.cjs` module, and calls each module's exported `register(server, ctx)`. Each tool module ALSO exports a `connectors` array (its connector + hitl_shape descriptors) - the born-wired SOURCE of truth. Hook `registerCoreTools` into `registerRouterTools` in `lib/mcp/tool-router.cjs` so the server gains the new tools without editing bin (keeps this plan file-disjoint from Plan 03). Guard the new-tool registration behind `isMcpFirst` where a tool changes behavior; read-only tools (room_list/state/search, contract_version) may register unconditionally since they add surface without altering legacy paths.
    Create `lib/mcp/contract-version.cjs` exporting `CONTRACT_VERSION` (a semver string, e.g. read from package.json or a pinned contract semver), `registerContractVersion(server)` that registers the `contract_version` tool returning `{ version: CONTRACT_VERSION, trust_boundaries: [...] }`, and a `connectors` descriptor for the tool. This is the SPEC-2 version field.
    Extend `scripts/build-connector-registry.cjs`: add a fourth connector source that DISCOVERS MCP-tool descriptors by requiring each `lib/mcp/tools/*.cjs` module + the tool-router `MCP_TOOL_CONNECTORS` export and reading their `connectors` arrays (surface = the tool name prefixed `mcp:`, source = `mcp_tool`, connects_to_spine, hitl_shape, hitl_why, filing, decision_surface). GENERATE `data/mcp-tool-connectors.json` from these discovered descriptors (write the "GENERATED - do not edit by hand" header) AND fold them into the generated `data/connector-registry.json` alongside the command/skill/agent connectors. Keep `--check` semantics: regenerate in memory, exit 1 on drift. Mirror the same MCP source into `scripts/check-shape-declaration.cjs`'s enumeration so a declared MCP tool with a missing/invalid hitl_shape is caught (advisory WARN, matching the Phase 210 posture).
    No em-dashes. CJS only.
  </action>
  <acceptance_criteria>
    - `node -e "const c=require('./lib/mcp/contract-version.cjs'); if(!/^[0-9]+\.[0-9]+\.[0-9]+/.test(c.CONTRACT_VERSION)) throw new Error('not semver'); console.log(c.CONTRACT_VERSION)"` prints a semver.
    - `node scripts/build-connector-registry.cjs --check` exits 0 (registry regenerates clean including the MCP tools).
    - `grep -n "mcp-tool-connectors" scripts/build-connector-registry.cjs` matches (generation wired); `grep -n "GENERATED" data/mcp-tool-connectors.json` matches (generated header present).
    - `node -e "const r=require('./data/connector-registry.json'); const has=r.connectors.some(c=>String(c.surface).indexOf('room_bind')>=0||c.source==='mcp_tool'); if(!has) throw new Error('mcp tools not in registry'); console.log('ok')"` prints `ok` (room_bind from Plan 02 discovered via its exported descriptor).
    - `node scripts/check-shape-declaration.cjs; echo shape-exit=$?` runs (advisory; MCP tools enumerated).
    - `node -e "require('./lib/mcp/register-core-tools.cjs')"` loads without error.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check && node -e "require('./lib/mcp/register-core-tools.cjs');require('./lib/mcp/contract-version.cjs')" && MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs; echo "exit=$?"</automated>
  </verify>
  <done>MCP tools are first-class in the born-wired gate via module-exported connectors; contract_version returns a semver; the tools/ seam auto-discovers modules so later plans add disjoint files; data/mcp-tool-connectors.json is generated and --check regenerates clean.</done>
</task>

<task type="auto">
  <name>Task 2: room tool group (room_list/room_state/room_search)</name>
  <read_first>
    - lib/mcp/tool-router.cjs (the existing grouped room tools + safeResolveSection/SECTION_RE guards to reuse; the server.tool shape)
    - lib/core/resolve-active-room.cjs (resolveActiveRoom for read-scope room listing; resolveWriteRoom already consumed in Plan 02)
    - lib/mcp/register-core-tools.cjs (Task 1 - the auto-discovery seam + the register(server, ctx)+connectors module contract)
    - .planning/phases/198-mcp-first-then-sdk/198-PATTERNS.md (room tool role-match)
  </read_first>
  <action>
    Create `lib/mcp/tools/room.cjs` exporting `register(server, ctx)` and a `connectors` array (the born-wired source). Register `room_list` (enumerate rooms under $MINDRIAN_ROOMS_HOME, read-only), `room_state` (return a room's ROOM.md/registry state for the session's bound room via ctx.sessionId, read-only), and `room_search` (search entries within the bound room, read-only). Each tool: per-tool zod input schema; reuse SECTION_RE/safeResolveSection for any path input (ASVS V5); resolve the room from the session binding (ctx.sessionId -> resolveWriteRoom/resolveActiveRoom read leg) not a frozen roomDir. These three are read-only so they register unconditionally. Declare each in the module's `connectors` export with `hitl_shape: "none"` and a hitl_why naming them as pure reads (the render-only exemption, not a fork they lack). Do NOT hand-edit data/mcp-tool-connectors.json - build-connector-registry.cjs regenerates it from this export.
    No em-dashes. CJS only. Zero Brain/network tokens.
  </action>
  <acceptance_criteria>
    - `node -e "const m=require('./lib/mcp/tools/room.cjs'); if(typeof m.register!=='function') throw 0; if(!Array.isArray(m.connectors)||m.connectors.length<3) throw new Error('need 3 room tool connectors'); const n=m.connectors.map(c=>String(c.surface||c.tool)); ['room_list','room_state','room_search'].forEach(x=>{if(!n.some(y=>y.indexOf(x)>=0)) throw new Error('missing '+x)}); console.log('ok')"` prints `ok`.
    - Each room connector descriptor carries an `hitl_shape` key.
    - `node scripts/build-connector-registry.cjs --check` exits 0 (new tools regenerate clean into the generated files).
    - `node tests/test-198-local-only.test.cjs` exits 0.
    - `grep -n "safeResolveSection\|SECTION_RE" lib/mcp/tools/room.cjs` matches (guards reused).
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('./lib/mcp/tools/room.cjs')" && node scripts/build-connector-registry.cjs --check && node tests/test-198-local-only.test.cjs && MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs; echo "exit=$?"</automated>
  </verify>
  <done>room_list/state/search are read-only session-scoped tools declared born-wired via their module export; the connector registry regenerates clean.</done>
</task>

<task type="auto">
  <name>Task 3: graph tool group (graph_query/graph_write/memory_event) + chokepoint guard</name>
  <read_first>
    - lib/core/navigation.cjs (the single SQL chokepoint; the write/memory_event entry surface; checkLostUpdate/last_modified_at CAS token shipped in Phase 194)
    - lib/mcp/tools/room.cjs (Task 2 - the register(server, ctx) + connectors shape to mirror)
    - tests/test-198-chokepoint-guard.test.cjs (the Plan 01 stub to fill)
    - .planning/phases/198-mcp-first-then-sdk/198-PATTERNS.md (Graph write chokepoint shared pattern; SPEC-2 chokepoint-guard test)
    - .planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md (Part 9 constraint; graph_write wraps navigation, nothing bypasses)
  </read_first>
  <action>
    Create `lib/mcp/tools/graph.cjs` exporting `register(server, ctx)` and `connectors`. `graph_query` (read-only graph reads via navigation.cjs). `graph_write` and `memory_event` (WRITES) route EXCLUSIVELY through navigation.cjs - do NOT open room.db directly, do NOT mint a second writer (Part 9, R4). Writes resolve the target room from ctx.sessionId (Plan 02 resolveWriteRoom), stamp through navigation's checkLostUpdate CAS path (reuse the Phase 194 reconcile guard), and gate behind `isMcpFirst`. Declare graph_write/memory_event in the module `connectors` export with a material hitl_shape (they mint typed edges - e.g. `hitl_shape: "F.1"`), graph_query as `hitl_shape: "none"`. Do NOT hand-edit data/mcp-tool-connectors.json.
    Fill `tests/test-198-chokepoint-guard.test.cjs` (Plan 01 stub): assert graph_write/memory_event go through navigation.cjs and that a direct room.db write attempted around the chokepoint is REJECTED. The test verifies the SPEC-2 acceptance "graph writes attempted around navigation.cjs are rejected by test" - e.g. source-grep tools/graph.cjs proving the only write path is a navigation.cjs require, plus a runtime assertion that a bypass helper is absent/guarded.
    No em-dashes. CJS only.
  </action>
  <acceptance_criteria>
    - `node tests/test-198-chokepoint-guard.test.cjs` exits 0 (graph writes only via navigation; bypass rejected).
    - `grep -n "navigation" lib/mcp/tools/graph.cjs` matches; `grep -cE "sqlite|room\.db|new Database" lib/mcp/tools/graph.cjs` returns 0 (no direct DB open - writes go through navigation.cjs).
    - `node -e "const m=require('./lib/mcp/tools/graph.cjs'); const n=m.connectors.map(c=>String(c.surface||c.tool)); ['graph_query','graph_write','memory_event'].forEach(x=>{if(!n.some(y=>y.indexOf(x)>=0)) throw new Error('missing '+x)}); console.log('ok')"` prints `ok`.
    - `node scripts/build-connector-registry.cjs --check` exits 0.
    - `node tests/test-198-local-only.test.cjs` exits 0.
    - `bash tests/run-all-198.sh` exits 0 with the contract-schema + chokepoint-guard legs promoted from SKIP to PASS.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-198-chokepoint-guard.test.cjs && node scripts/build-connector-registry.cjs --check && bash tests/run-all-198.sh && MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs; echo "exit=$?"</automated>
  </verify>
  <done>graph_query/graph_write/memory_event wrap the navigation.cjs chokepoint; out-of-band writes are rejected under test; the SPEC-2 chokepoint guard is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool call -> navigation.cjs | The only sanctioned graph-write path; a tool writing around it is a Part 9 breach |
| tool schema -> Brain proxy | A new tool schema accepting room content would breach Part 8 |
| connector-registry generation -> born-wired gate | An undeclared tool reachable without a connector entry is an ungoverned surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-04 | Tampering / Repudiation | graph_write / memory_event | mitigate | Writes route through navigation.cjs only; test-198-chokepoint-guard rejects a bypass; the Phase 194 checkLostUpdate CAS token guards lost-update |
| T-198-07 | Elevation of Privilege | new MCP tools ungoverned | mitigate | Every tool exports a `connectors` descriptor with connector + hitl_shape; build-connector-registry.cjs discovers them, generates data/mcp-tool-connectors.json, and --check fails closed on drift; check-shape-declaration enumerates MCP tools |
| T-198-05 | Information Disclosure | new tool schemas + tool modules | mitigate | tool schemas accept generic handles/room-local params only, no Brain egress; test-198-local-only source-greps tools/*.cjs for Brain/network tokens |
| T-198-03 | Tampering | room/section path input in room tools | mitigate | Reuse SECTION_RE + safeResolveSection on every path param |
| T-198-SC | Tampering | npm installs | accept | No new installs; N/A per RESEARCH audit |
</threat_model>

<verification>
- `node scripts/build-connector-registry.cjs --check` exits 0 (MCP tools born-wired via module exports, registry clean).
- `node tests/test-198-chokepoint-guard.test.cjs` green (Part 9 enforced).
- `node tests/test-198-contract-schema.test.cjs` green (contract_version semver + tool schemas valid).
- `MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs` green (flag-OFF plugin smoke).
- `bash tests/run-all-198.sh` exits 0 with contract + chokepoint legs promoted to PASS.
</verification>

<success_criteria>
The mindrian-core contract has a version tool and a born-wired tool-module seam; room + graph tools ship with per-tool zod and module-exported connector/HITL declarations; graph writes are chokepoint-only and proven so; --check discovers the exports and regenerates data/mcp-tool-connectors.json + the registry clean.
</success_criteria>

<output>
Create `.planning/phases/198-mcp-first-then-sdk/198-04-SUMMARY.md` when done.
</output>
