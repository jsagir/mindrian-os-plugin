---
phase: 198-mcp-first-then-sdk
plan: 07
type: execute
wave: 4
depends_on: ["198-05"]
files_modified:
  - lib/mcp/tools/chain.cjs
  - tests/test-198-chain-run-halt.test.cjs
autonomous: true
requirements: [SPEC-3]
must_haves:
  truths:
    - "chain_resolve composes a framework chain server-side via composeWorkflow"
    - "chain_run executes the autonomous_safe prefix and HALTS at the first material step, returning a gate"
    - "the material step executes ONLY after gate_answer returns an approve verdict"
    - "postures are joined from the connector registry, not re-derived"
  artifacts:
    - path: "lib/mcp/tools/chain.cjs"
      provides: "chain_resolve + chain_run wrapping runChain server-side (with exported connectors)"
      contains: "runChain"
    - path: "tests/test-198-chain-run-halt.test.cjs"
      provides: "2 autonomous_safe + 1 material halt-then-approve proof"
      contains: "gate_answer"
  key_links:
    - from: "lib/mcp/tools/chain.cjs"
      to: "lib/core/chain-executor.cjs"
      via: "chain_run wraps runChain (Phase 166 halt-at-material)"
      pattern: "runChain"
    - from: "lib/mcp/tools/chain.cjs"
      to: "lib/mcp/gate-render.cjs"
      via: "a material step returns a gate_render payload instead of executing"
      pattern: "gate"
---

<objective>
Wrap the shipped Phase 166 `runChain` as server-side chain execution (SPEC-3): `chain_resolve` composes a framework chain via `composeWorkflow`, and `chain_run` executes the `autonomous_safe` prefix and HALTS at the first material step, returning a gate (a `gate_render` payload) instead of executing it. The material step executes ONLY after `gate_answer` returns an approve verdict. Postures are joined from the connector registry, not re-derived.

Purpose: Execution unifies on the substrate - the halt-at-material posture semantics that exist in-process on the CLI surface (Phase 166) now run server-side for every host (Canon Part 3, Part 11 one governed path). Runs in parallel with the thin-adapter plan (Plan 08) - disjoint files. Depends on the gate ladder (Plan 05) for the halt payload.
Output: lib/mcp/tools/chain.cjs, the halt-then-approve proof.

Reuse: chain_run WRAPS `lib/core/chain-executor.runChain` - do NOT mint a second executor or a second posture engine (R4, RESEARCH Don't-Hand-Roll). The halt-at-material behavior already exists; expose it, do not re-implement it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-mcp-first-then-sdk/198-SPEC.md
@.planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md
@.planning/phases/198-mcp-first-then-sdk/198-PATTERNS.md
@lib/core/chain-executor.cjs
@lib/workflow/command-resolver.cjs
@lib/mcp/gate-render.cjs
@.planning/phases/198-mcp-first-then-sdk/198-05-SUMMARY.md
</context>

## Artifacts this phase produces (full inventory)

Full inventory in 198-01/02-PLAN.md. **Born-wired model:** chain tools declare their connectors via a module export; data/mcp-tool-connectors.json is GENERATED by build-connector-registry.cjs (never hand-edited). **This plan (198-07) delivers:** the `chain_resolve` + `chain_run` tools (with exported connectors) and the halt-then-approve proof.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chain_resolve + chain_run server-side (halt-at-material -> gate)</name>
  <read_first>
    - lib/core/chain-executor.cjs (runChain at :432 - the six-callback contract; the material-step classification :218-236; the forced-material EXEC-03 rule; module.exports :899)
    - lib/workflow/command-resolver.cjs (composeWorkflow at :110 - chain_resolve wraps this)
    - lib/mcp/gate-render.cjs (Plan 05 - the gate payload a material halt returns)
    - lib/mcp/tools/graph.cjs (Plan 04 - the register(server, ctx)+connectors module shape)
    - data/connector-registry.json (the posture field per surface - chain_run JOINS postures from here, does not re-derive)
    - .planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md (SPEC-3 target: chain_run wraps runChain; postures joined from the connector registry; material steps return a gate_render payload)
  </read_first>
  <behavior>
    - chain_resolve(frameworkChain) returns the composed step list from composeWorkflow
    - chain_run over a chain of 2 autonomous_safe + 1 material step: runs the 2 safe steps, HALTS at the material step, returns a gate (gate_render payload), and does NOT execute the material step
    - after gate_answer with an approve verdict, chain_run resumes and executes the material step
    - after gate_answer with a reject/defer verdict, the material step does NOT execute
    - postures are read from the connector registry (joined), never re-derived inside chain.cjs
  </behavior>
  <action>
    Create `lib/mcp/tools/chain.cjs` exporting `register(server, ctx)` and `connectors` (the born-wired source). `chain_resolve` wraps `composeWorkflow` (server-side composition). `chain_run` wraps `runChain` (Phase 166) with the six-callback contract: join each step's posture from the connector registry (`data/connector-registry.json`), pass them into runChain's classification, and on the first material step return a `gate_render` payload (Plan 05) instead of executing - the halt. Resume execution of the material step ONLY when a subsequent `gate_answer` approve verdict arrives (thread the gate answer back into runChain's continuation). Any material write the resumed step performs routes through navigation.cjs (Part 9). Gate the tool registration behind `isMcpFirst`. Declare chain_resolve/chain_run in the module `connectors` export with a hitl_shape (chain_run's material halt IS a gate fork). Do NOT hand-edit data/mcp-tool-connectors.json - build-connector-registry.cjs regenerates it from this export.
    Fill `tests/test-198-chain-run-halt.test.cjs` (Plan 01 stub): a chain of 2 autonomous_safe + 1 material step; assert the prefix runs, the run halts and returns a gate at the material step, the material step does NOT execute before gate_answer, and DOES execute after an approve verdict. This is the SPEC-3 acceptance.
    No em-dashes. CJS only.
  </action>
  <acceptance_criteria>
    - `node tests/test-198-chain-run-halt.test.cjs` exits 0: 2 safe + 1 material chain runs the prefix, halts, returns the gate, executes the material step only after an approve verdict.
    - `grep -n "runChain" lib/mcp/tools/chain.cjs` matches; `grep -c "function runChain\|_runChainResilient =" lib/mcp/tools/chain.cjs` returns 0 (wrapped, not reimplemented).
    - `grep -n "composeWorkflow" lib/mcp/tools/chain.cjs` matches (chain_resolve wraps the shipped resolver).
    - `grep -n "connector-registry\|posture" lib/mcp/tools/chain.cjs` matches (postures joined, not re-derived).
    - `node -e "const m=require('./lib/mcp/tools/chain.cjs'); const n=m.connectors.map(c=>String(c.surface||c.tool)); if(!n.some(x=>x.indexOf('chain_resolve')>=0)||!n.some(x=>x.indexOf('chain_run')>=0)) throw new Error('chain connectors missing'); console.log('ok')"` prints `ok`.
    - `node scripts/build-connector-registry.cjs --check` exits 0 (chain tools regenerate clean).
    - `bash tests/run-all-198.sh` exits 0 with the chain-run-halt leg promoted from SKIP to PASS.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-198-chain-run-halt.test.cjs && node scripts/build-connector-registry.cjs --check && bash tests/run-all-198.sh && MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs; echo "exit=$?"</automated>
  </verify>
  <done>chain_run wraps runChain server-side, honors postures from the connector registry, halts at the first material step returning a gate, and resumes only on an approve verdict; no second executor; connectors declared via module export.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chain_run -> material step execution | A material step must not execute without a gate approve; an unattended run is a governance breach |
| resumed material step -> navigation.cjs write | The write must route through the chokepoint |
| posture join -> connector registry | A wrong posture could mark a material step autonomous_safe and skip the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-11 | Elevation of Privilege | chain_run auto-executing a material step | mitigate | runChain's shipped halt-at-material classification is honored; material steps return a gate and execute only after a gate_answer approve; test-198-chain-run-halt is the guard |
| T-198-04 | Tampering | resumed material step write | mitigate | The write routes through navigation.cjs (Part 9); no direct DB write in chain.cjs |
| T-198-12 | Spoofing | forged approve verdict resuming a chain | mitigate | The resume consumes a gate_answer tied to the gate_id chain_run minted (Plan 05 gate_id binding); a verdict for an unknown gate does not resume |
| T-198-07 | Elevation of Privilege | chain tools ungoverned | mitigate | chain_resolve/chain_run declared via module `connectors` export with hitl_shape; --check fails closed |
| T-198-SC | Tampering | npm installs | accept | No new installs; N/A per RESEARCH audit |
</threat_model>

<verification>
- `node tests/test-198-chain-run-halt.test.cjs` green (halt-then-approve - SPEC-3 acceptance).
- `node scripts/build-connector-registry.cjs --check` exits 0 (chain tools born-wired via module export).
- `MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs` green (flag-OFF plugin smoke).
- `bash tests/run-all-198.sh` exits 0 with the chain-run-halt leg PASS.
</verification>

<success_criteria>
chain_run runs the autonomous_safe prefix server-side, halts at the first material step returning a gate, and executes the material step only after an approve verdict - wrapping the shipped runChain with no second executor and postures joined from the registry.
</success_criteria>

<output>
Create `.planning/phases/198-mcp-first-then-sdk/198-07-SUMMARY.md` when done.
</output>
