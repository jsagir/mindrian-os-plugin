---
phase: 198-mcp-first-then-sdk
plan: 06
type: execute
wave: 3
depends_on: ["198-04"]
files_modified:
  - lib/mcp/tools/sensors.cjs
  - lib/mcp/tools/views.cjs
  - lib/mcp/tools/status.cjs
  - tests/test-198-contract-schema.test.cjs
autonomous: true
requirements: [SPEC-2]
must_haves:
  truths:
    - "suggest_next/reach_candidates/contradiction_check/whitespace_scan are callable pull sensors with zod schemas"
    - "framework_run exposes the 107 commands as parameters (not surfaces) through the one governed reach path"
    - "artifact_file and view_compile expose filing + the view registry as tools"
    - "status_read carries a spend/cap segment from day one (the Warp lesson)"
  artifacts:
    - path: "lib/mcp/tools/sensors.cjs"
      provides: "suggest_next/reach_candidates/contradiction_check/whitespace_scan + framework_run"
      contains: "reach_candidates"
    - path: "lib/mcp/tools/status.cjs"
      provides: "status_read with spend/cap segment"
      contains: "status_read"
    - path: "lib/mcp/tools/views.cjs"
      provides: "artifact_file + view_compile"
      contains: "view_compile"
  key_links:
    - from: "lib/mcp/tools/sensors.cjs"
      to: "lib/workflow/command-resolver.cjs"
      via: "framework_run wraps composeWorkflow / the one governed reach path"
      pattern: "composeWorkflow"
    - from: "lib/mcp/tools/status.cjs"
      to: "lib/mcp/sse-event-bus.cjs"
      via: "status_read publishes the spend/cap status-segment"
      pattern: "status-segment"
---

<objective>
Complete the SPEC-2 tool inventory with the pull-sensor, view, and status tool groups: `suggest_next`, `reach_candidates`, `contradiction_check`, `whitespace_scan` (sensors as pull), `framework_run` (the 107 commands become parameters, not surfaces), `artifact_file` + `view_compile` (filing + view registry), and `status_read` carrying the spend/cap segment from day one (the Warp lesson from the room teardown).

Purpose: SPEC-2 acceptance requires EVERY listed tool callable via MCP Inspector with schema validation passing. This plan lands the read-mostly remainder so the full contract is complete and the statusline thin-adapter (Plan 08) has status_read to query. Runs in parallel with the gate ladder (Plan 05) - disjoint tool files.
Output: lib/mcp/tools/{sensors,views,status}.cjs, their module-exported connector/HITL declarations, the completed contract-schema test.

Reuse: sensors pull through the shipped `dispatchSensors`/`decide()` reach path; framework_run wraps `composeWorkflow` - do NOT mint a second selection brain (R4). artifact_file/view_compile wrap the shipped view registry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-mcp-first-then-sdk/198-SPEC.md
@.planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md
@.planning/phases/198-mcp-first-then-sdk/198-PATTERNS.md
@lib/workflow/command-resolver.cjs
@lib/mcp/tool-router.cjs
@.planning/phases/198-mcp-first-then-sdk/198-04-SUMMARY.md
</context>

## Artifacts this phase produces (full inventory)

Full inventory in 198-01/02-PLAN.md. **Born-wired model:** each tool module exports its `connectors` descriptors; data/mcp-tool-connectors.json is GENERATED by build-connector-registry.cjs (never hand-edited). **This plan (198-06) delivers:** suggest_next, reach_candidates, contradiction_check, whitespace_scan, framework_run, artifact_file, view_compile, status_read, plus their module-exported connector/HITL declarations.

<tasks>

<task type="auto">
  <name>Task 1: pull-sensor tools + framework_run</name>
  <read_first>
    - lib/workflow/command-resolver.cjs (composeWorkflow at :110 - framework_run wraps this; the one governed reach path)
    - lib/mcp/tools/graph.cjs (Plan 04 - the register(server, ctx)+connectors module shape)
    - lib/mcp/register-core-tools.cjs (Plan 04 - the auto-discovery seam these modules drop into)
    - .planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md (Architectural Responsibility Map: sensors as pull; R4 no second selection brain)
    - .planning/phases/198-mcp-first-then-sdk/198-PATTERNS.md (sensors role-match; framework_run wraps composeWorkflow)
  </read_first>
  <action>
    Create `lib/mcp/tools/sensors.cjs` exporting `register(server, ctx)` and `connectors` (the born-wired source). Register the four pull sensors reading through the shipped reach path (dispatchSensors -> decide), each read-only with a zod schema: `suggest_next` (next-action suggestion for the bound room), `reach_candidates` (the reach candidate set feeding a reach card), `contradiction_check` (CONTRADICTS scan), `whitespace_scan` (gap scan). Register `framework_run`: it wraps `composeWorkflow` so the 107 methodology commands become PARAMETERS (a framework name + args) not new tool surfaces - do NOT mint a second resolver. framework_run resolves the room from ctx.sessionId; a material step in a composed framework returns a gate (delegating to gate_render, Plan 05), it does not execute unattended. Declare each in the module `connectors` export: the four sensors are pull reads (`hitl_shape: "none"`); framework_run declares the material fork it can reach. Do NOT hand-edit data/mcp-tool-connectors.json - build-connector-registry.cjs regenerates it from this export.
    No em-dashes. CJS only. Zero Brain/network tokens.
  </action>
  <acceptance_criteria>
    - `node -e "const m=require('./lib/mcp/tools/sensors.cjs'); if(typeof m.register!=='function') throw 0; const names=m.connectors.map(c=>String(c.surface||c.tool||'')); ['suggest_next','reach_candidates','contradiction_check','whitespace_scan','framework_run'].forEach(n=>{if(!names.some(x=>x.indexOf(n)>=0)) throw new Error('missing '+n)}); console.log('ok')"` prints `ok`.
    - `grep -n "composeWorkflow" lib/mcp/tools/sensors.cjs` matches (framework_run wraps the shipped resolver); `grep -c "function composeWorkflow" lib/mcp/tools/sensors.cjs` returns 0.
    - Each of the five connector descriptors carries an `hitl_shape` key.
    - `node scripts/build-connector-registry.cjs --check` exits 0.
    - `node tests/test-198-local-only.test.cjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('./lib/mcp/tools/sensors.cjs')" && node scripts/build-connector-registry.cjs --check && node tests/test-198-local-only.test.cjs && MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs; echo "exit=$?"</automated>
  </verify>
  <done>Four pull sensors + framework_run are born-wired tools (via module export) wrapping the one governed reach path; no second selection brain.</done>
</task>

<task type="auto">
  <name>Task 2: artifact_file + view_compile + status_read (spend/cap segment)</name>
  <read_first>
    - lib/mcp/tool-router.cjs (the existing filing/opportunity write path artifact_file wraps; the safeResolveSection guard)
    - lib/mcp/sse-event-bus.cjs (Plan 03 - status_read publishes the status-segment; EVENT_KINDS)
    - lib/mcp/tools/sensors.cjs (Task 1 - the module shape to mirror)
    - tests/test-198-contract-schema.test.cjs (Plan 01 stub to complete - every listed tool schema-valid)
    - .planning/phases/198-mcp-first-then-sdk/198-CONTEXT.md (specifics: spend/cap visibility as a first-class statusline segment; status_read carries it from day one)
    - .planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md (Architectural Responsibility Map: status_read spend/cap segment)
  </read_first>
  <action>
    Create `lib/mcp/tools/views.cjs` exporting `register(server, ctx)` and `connectors`: `artifact_file` (file an artifact/entry into the session's bound room via the shipped filing path + navigation.cjs where it mints edges; reuse safeResolveSection), `view_compile` (compile a registered view for the bound room, read-mostly). Create `lib/mcp/tools/status.cjs` exporting the same shape: `status_read` returns navigator segments INCLUDING a spend/cap segment from day one, and publishes it to the SSE bus as a `status-segment` event (Plan 03 EVENT_KINDS). Declare in the module `connectors` exports: status_read is read-only (`hitl_shape: "none"`); artifact_file declares its filing fork. Do NOT hand-edit data/mcp-tool-connectors.json.
    Complete `tests/test-198-contract-schema.test.cjs` (Plan 01 stub): assert contract_version returns a semver AND every tool discovered in the generated data/mcp-tool-connectors.json has a registered zod schema that validates a sample input. This closes the SPEC-2 "every listed tool schema-valid" acceptance.
    No em-dashes. CJS only.
  </action>
  <acceptance_criteria>
    - `node -e "const s=require('./lib/mcp/tools/status.cjs'); if(typeof s.register!=='function') throw 0; if(!s.connectors.some(c=>String(c.surface||c.tool).indexOf('status_read')>=0)) throw new Error('status_read missing'); console.log('ok')"` prints `ok`.
    - `grep -n "status-segment\|spend\|cap" lib/mcp/tools/status.cjs` matches (spend/cap segment present, published to the bus).
    - `node -e "const v=require('./lib/mcp/tools/views.cjs'); const n=v.connectors.map(c=>String(c.surface||c.tool)); if(!n.some(x=>x.indexOf('artifact_file')>=0)||!n.some(x=>x.indexOf('view_compile')>=0)) throw 0; console.log('ok')"` prints `ok`.
    - `node tests/test-198-contract-schema.test.cjs` exits 0 (contract_version semver + all listed tools schema-valid).
    - `node scripts/build-connector-registry.cjs --check` exits 0 (artifact_file/view_compile/status_read regenerate clean).
    - `bash tests/run-all-198.sh` exits 0 with the contract-schema leg PASS.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-198-contract-schema.test.cjs && node scripts/build-connector-registry.cjs --check && bash tests/run-all-198.sh && MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs; echo "exit=$?"</automated>
  </verify>
  <done>artifact_file/view_compile/status_read complete the contract; status_read carries spend/cap and publishes to the SSE bus; the full tool inventory is schema-valid (SPEC-2 acceptance); connectors declared via module export.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| framework_run -> composed material step | A composed framework must halt at material steps, not execute unattended |
| artifact_file -> room filing / navigation.cjs | Filing must land in the session's bound room and mint edges through the chokepoint |
| status_read -> SSE bus segment | The spend/cap segment leaves the server over loopback; must carry no room content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-04 | Tampering | artifact_file filing writes | mitigate | Filing routes edges through navigation.cjs; reuse safeResolveSection for path inputs; resolve room from ctx.sessionId not a frozen roomDir |
| T-198-11 | Elevation of Privilege | framework_run auto-executing a material step | mitigate | framework_run halts at material steps returning a gate (delegates to gate_render); it wraps composeWorkflow's governed path, no second resolver |
| T-198-05 | Information Disclosure | status_read segment payloads | mitigate | Spend/cap + navigator segments only; test-198-local-only greps the status/views/sensors modules for Brain/network tokens; Part 8 by construction |
| T-198-07 | Elevation of Privilege | new tools ungoverned | mitigate | All eight tools declared via module `connectors` exports with hitl_shape; --check fails closed |
| T-198-SC | Tampering | npm installs | accept | No new installs; N/A per RESEARCH audit |
</threat_model>

<verification>
- `node tests/test-198-contract-schema.test.cjs` green (full tool inventory schema-valid - SPEC-2 acceptance).
- `node scripts/build-connector-registry.cjs --check` exits 0 (all tools born-wired via module export).
- `MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs` green (flag-OFF plugin smoke).
- `bash tests/run-all-198.sh` exits 0.
</verification>

<success_criteria>
The SPEC-2 tool inventory is complete: pull sensors, framework_run, artifact_file, view_compile, and status_read (with spend/cap) all callable, schema-valid, and born-wired via module export; sensors and framework_run wrap the shipped reach path with no second selection brain.
</success_criteria>

<output>
Create `.planning/phases/198-mcp-first-then-sdk/198-06-SUMMARY.md` when done.
</output>
