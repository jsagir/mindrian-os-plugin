---
phase: 198-mcp-first-then-sdk
plan: 10
type: execute
wave: 6
depends_on: ["198-07", "198-08", "198-09"]
files_modified:
  - scripts/198-rollback-rehearsal.cjs
  - tests/parity-198.sh
  - tests/test-198-flag-off-parity.test.cjs
  - evals/plurai/198-baseline.json
autonomous: false
requirements: [SPEC-6, SPEC-7, SPEC-8]
must_haves:
  truths:
    - "The same governed transcript (bind room, reach card, chain run with halt, gated write, artifact filed) produces identical graph writes + gate sequence on Claude Code CLI and one non-Anthropic MCP host"
    - "The rollback rehearsal passes: flip the flag OFF after cutover, restore one room from snapshot, re-run the legacy parity transcript green"
    - "The 198 Plurai baseline passes on the measured two-host parity result (the phase cannot close without it)"
    - "Born-wired, projection, and render-coverage gates regenerate clean; doctor --acceptance is green"
  artifacts:
    - path: "scripts/198-rollback-rehearsal.cjs"
      provides: "rehearsed reversal: flag-off + snapshot restore + legacy parity green"
      contains: "migration-snapshot"
    - path: "tests/parity-198.sh"
      provides: "the two-host parity transcript (CLI leg automated, MCP-host leg checkpoint-driven)"
      contains: "gate_answer"
    - path: "evals/plurai/198-baseline.json"
      provides: "the 198 invocation-parity baseline updated with the measured two-host result"
  key_links:
    - from: "scripts/198-rollback-rehearsal.cjs"
      to: "lib/core/migration-snapshot.cjs"
      via: "pre-migration snapshot + restore"
      pattern: "migration-snapshot"
    - from: "tests/parity-198.sh"
      to: "lib/mcp/tools/graph.cjs"
      via: "node/edge diff of the two-host graph writes must be empty"
      pattern: "graph"
    - from: "scripts/198-plurai-gate-check.cjs"
      to: "evals/plurai/198-baseline.json"
      via: "the measured two-host parity verdict"
      pattern: "198-baseline"
---

<objective>
Close the phase: prove surface parity on two hosts (SPEC-6), rehearse the rollback (SPEC-7), and pass the 198 Plurai invocation-parity gate on the measured result (SPEC-8) - then confirm the born-wired/projection/render gates and doctor --acceptance are all green. The same governed transcript (bind room, reach card via gate_render, chain_run with halt, gated write through navigation.cjs, artifact_file) must produce identical typed graph writes (empty node/edge diff) and an identical gate sequence on Claude Code CLI and one elicitation-capable non-Anthropic MCP host (VS Code). The rollback rehearsal is the contract test: a reversal that was never rehearsed is a hope, not a contract.

Purpose: The phase's completion gate. Every prior plan kept main green per commit; this plan proves the substrate holds across hosts, reverses cleanly, and passes the roadmap-mandated Plurai judge.
Output: scripts/198-rollback-rehearsal.cjs, the filled parity transcript, the updated Plurai baseline, the phase-close confirmation.

This plan has a checkpoint: the non-Anthropic MCP host (VS Code, elicitation-capable) runs on the navigator's own install (RESEARCH: test-execution dependency the navigator supplies). The CLI leg is automated; the VS Code leg is a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-mcp-first-then-sdk/198-SPEC.md
@.planning/phases/198-mcp-first-then-sdk/198-SPEC-DRAFT.md
@.planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md
@lib/core/migration-snapshot.cjs
@scripts/198-plurai-gate-check.cjs
@scripts/doctor.cjs
@.planning/phases/198-mcp-first-then-sdk/198-07-SUMMARY.md
@.planning/phases/198-mcp-first-then-sdk/198-09-SUMMARY.md
</context>

## Artifacts this phase produces (full inventory)

Full inventory in 198-01/02-PLAN.md. **This plan (198-10) delivers:** scripts/198-rollback-rehearsal.cjs, the filled two-host parity transcript, the measured Plurai baseline, and the phase-close gate confirmation. No new tools.

<tasks>

<task type="auto">
  <name>Task 1: SPEC-7 reversibility mechanics + rehearsed rollback</name>
  <read_first>
    - lib/core/migration-snapshot.cjs (the shipped snapshot/restore ledger precedent SPEC-7 names)
    - scripts/heal-command.cjs (the backup + dry-run precedent the reversibility mechanics compose from)
    - lib/mcp/mcp-first-flag.cjs (Plan 02 - the flag the rehearsal flips OFF)
    - tests/test-198-flag-off-parity.test.cjs (Plan 02 - the byte-identical-legacy assertion)
    - .planning/phases/198-mcp-first-then-sdk/198-SPEC-DRAFT.md (the reversibility contract clauses 1-5: kill switch, git tag, expand-contract data, release door, test-of-the-rollback)
    - .planning/phases/198-mcp-first-then-sdk/198-SPEC.md (SPEC-7 acceptance: flip flag OFF after cutover, restore one room from snapshot, re-run legacy parity green)
  </read_first>
  <action>
    Create `scripts/198-rollback-rehearsal.cjs`. It rehearses the full reversal (SPEC-7 clause 5): (1) confirm a last-known-good git tag exists on the pre-phase baseline commit (identify the commit immediately before Plan 01's first phase commit via git log; if no `last-known-good`-style tag points at it, cut one - the beta tag line is the anchor); (2) take a pre-migration room.db snapshot via `migration-snapshot.cjs` (reuse, do not reimplement); (3) with a room cut over (flag ON), flip `MINDRIAN_MCP_FIRST` OFF, restore the room from the snapshot, and re-run the legacy parity transcript asserting it passes green (the byte-identical legacy path still works after reversal). Print a stable `ROLLBACK_REHEARSAL_OK` token + exit 0 on success. Confirm room.db changes this phase were expand-only (additive tables/columns; no destructive contract step) - assert no DROP/DELETE-column migration shipped.
    Re-run `tests/test-198-flag-off-parity.test.cjs` as part of the rehearsal to prove flag-OFF byte-identical legacy end to end.
    No em-dashes. CJS only.
  </action>
  <acceptance_criteria>
    - `node scripts/198-rollback-rehearsal.cjs` prints `ROLLBACK_REHEARSAL_OK` and exits 0.
    - `grep -n "migration-snapshot" scripts/198-rollback-rehearsal.cjs` matches (snapshot reused); `grep -c "function snapshot\|function restoreSnapshot" scripts/198-rollback-rehearsal.cjs` returns 0 (not reimplemented).
    - `node tests/test-198-flag-off-parity.test.cjs` exits 0 (flag-OFF byte-identical legacy).
    - `git tag -l | grep -E "last-known-good|lkg|1.15.3-beta"` shows an anchor tag the rehearsal restores to.
    - The rehearsal asserts no destructive room.db contract step shipped this phase (expand-only).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/198-rollback-rehearsal.cjs && node tests/test-198-flag-off-parity.test.cjs; echo "exit=$?"</automated>
  </verify>
  <done>The rollback is rehearsed and green: flag-off + snapshot restore + legacy parity; room.db changes were expand-only; the reversibility contract is a test, not a hope.</done>
</task>

<task type="auto">
  <name>Task 2: SPEC-6 parity transcript (CLI leg) + SPEC-8 Plurai measured baseline + final gates</name>
  <read_first>
    - tests/parity-198.sh (Plan 01 scaffold - fill the CLI leg of the transcript)
    - lib/mcp/tools/graph.cjs (Plan 04 - the graph writes whose node/edge diff must be empty across hosts)
    - lib/mcp/tools/gate.cjs + lib/mcp/tools/chain.cjs (Plans 05/07 - the gate sequence + chain halt in the transcript)
    - scripts/198-plurai-gate-check.cjs + evals/plurai/198-baseline.json (Plan 01 - replace the deferred verdict with the measured two-host parity result)
    - scripts/doctor.cjs (the --acceptance roll-up)
    - .planning/phases/198-mcp-first-then-sdk/198-SPEC.md (SPEC-6 acceptance: identical typed graph writes + identical gate sequence; SPEC-8: 198 Plurai baseline passes)
  </read_first>
  <action>
    Fill the CLI leg of `tests/parity-198.sh`: run the full governed transcript (room_bind, reach card via gate_render, chain_run with a material halt, gate_answer approve, gated write through navigation.cjs, artifact_file) on the Claude Code CLI surface with the flag ON for `cli`, and capture the resulting typed graph writes (node/edge set) + the gate sequence into a comparable artifact for the two-host diff. Structure the script so the VS Code leg (Task 3 checkpoint) drops its captured artifact beside the CLI one and the script asserts an EMPTY node/edge diff + identical gate sequence.
    Update `evals/plurai/198-baseline.json` to carry the measured invocation-parity verdict (replacing the Plan 01 baseline_deferred seed) once the two-host transcript is captured, and confirm `node scripts/198-plurai-gate-check.cjs` passes on it (SPEC-8: the phase cannot close without the Plurai baseline passing - roadmap GATE clause).
    Run the final phase gates and confirm green: `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs`, `node scripts/doctor.cjs --acceptance`. No em-dashes.
  </action>
  <acceptance_criteria>
    - `bash tests/parity-198.sh` runs the CLI leg green and captures the graph-write + gate-sequence artifact for the two-host diff.
    - `node scripts/198-plurai-gate-check.cjs` prints `PLURAI_GATE_OK` and exits 0 on the measured baseline.
    - `node scripts/build-connector-registry.cjs --check` exits 0.
    - `node scripts/build-orchestration-projection.cjs --check` exits 0.
    - `node scripts/check-render-coverage.cjs` exits 0.
    - `node scripts/doctor.cjs --acceptance` exits 0 (acceptance roll-up green).
    - `bash tests/run-all-198.sh` exits 0 with every leg PASS (no SKIPs remaining for shipped modules).
  </acceptance_criteria>
  <verify>
    <automated>bash tests/parity-198.sh && node scripts/198-plurai-gate-check.cjs && node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check && node scripts/check-render-coverage.cjs && node scripts/doctor.cjs --acceptance; echo "exit=$?"</automated>
  </verify>
  <done>The CLI parity leg captures the transcript; the Plurai gate passes on the measured baseline; every born-wired/projection/render/acceptance gate is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Two-host parity verification (VS Code / elicitation-capable host)</name>
  <read_first>
    - tests/parity-198.sh (Task 2 - the CLI-leg artifact the VS Code leg diffs against)
    - .planning/phases/198-mcp-first-then-sdk/198-SPEC.md (SPEC-6 acceptance: both runs produce the same typed graph writes + gate sequence)
    - .planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md (Environment Availability: VS Code v1.102+ elicitation-capable is the navigator-supplied test host; MCP Inspector is an acceptable substitute)
  </read_first>
  <action>
    Pause for the navigator to run the identical governed transcript on the second host (VS Code v1.102+ or MCP Inspector, elicitation-capable) connected to the durable daemon over 127.0.0.1, then diff its captured typed graph writes + gate sequence against the CLI-leg artifact from Task 2. Claude automates nothing further here; the navigator supplies the non-Anthropic MCP host per the RESEARCH environment dependency and confirms the two-host parity diff is empty and the gate sequence identical.
  </action>
  <what-built>
    The mindrian-core durable daemon serves the full governed contract; the CLI parity leg (Task 2) captured its typed graph writes + gate sequence. The parity script is ready for the second host's run to diff against.
  </what-built>
  <how-to-verify>
    1. Start the durable daemon (it is already running from the CLI leg, or `MINDRIAN_MCP_FIRST=all` launches it).
    2. Connect the elicitation-capable MCP host - VS Code v1.102+ (or MCP Inspector) - to the daemon over 127.0.0.1 at the discovered port (see the pidfile under $MINDRIAN_ROOMS_HOME/.rooms/daemon/).
    3. Run the same transcript from that host: bind a room, answer the reach card (rendered via elicitation on this host), run the chain and approve the material halt, perform the gated write, file the artifact.
    4. Run `bash tests/parity-198.sh --host-leg vscode` (or drop the captured artifact beside the CLI one) so the script diffs the two hosts.
    5. Confirm the reported node/edge diff is EMPTY and the gate sequence is identical across the two hosts.
  </how-to-verify>
  <acceptance_criteria>
    - The two-host node/edge diff is empty (identical typed graph writes).
    - The gate sequence is identical across CLI and the elicitation-capable host.
    - `bash tests/parity-198.sh` (full, both legs present) exits 0.
    - The navigator confirms the same governed session behaved identically on both hosts.
  </acceptance_criteria>
  <resume-signal>Type "approved" if the two-host parity diff is empty and the gate sequence matches, or describe the divergence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-Anthropic MCP host -> durable daemon | An external host runs the same transcript; the daemon must serve it identically without exposing a remote listener |
| rollback rehearsal -> room.db snapshot/restore | A botched restore could corrupt a live room |
| Plurai gate -> baseline fixture | A tampered baseline could pass a non-parity result |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-06 | Information Disclosure / EoP | daemon serving the external parity host | mitigate | The daemon binds 127.0.0.1 only; the VS Code host connects over loopback; no remote interface is opened for the parity run |
| T-198-15 | Tampering | room.db snapshot restore | mitigate | Restore uses the shipped migration-snapshot.cjs ledger (atomic, dry-run precedent); the rehearsal asserts expand-only changes so no destructive contract step can corrupt on restore |
| T-198-02 | Tampering | two-host write divergence hiding a stale-room defect | mitigate | The parity diff asserts an EMPTY node/edge diff; any divergence (including a stale-room write) fails the gate and blocks phase close |
| T-198-16 | Repudiation | Plurai gate passing a non-parity result | mitigate | The baseline carries the measured two-host verdict; 198-plurai-gate-check reconstructs the fixture and asserts membership == baseline; a mismatch exits 1 and the phase cannot close |
| T-198-SC | Tampering | npm installs | accept | No new installs; N/A per RESEARCH audit |
</threat_model>

<verification>
- `node scripts/198-rollback-rehearsal.cjs` prints ROLLBACK_REHEARSAL_OK (SPEC-7 rehearsed reversal).
- `bash tests/parity-198.sh` green on both legs; empty node/edge diff + identical gate sequence (SPEC-6).
- `node scripts/198-plurai-gate-check.cjs` PLURAI_GATE_OK on the measured baseline (SPEC-8, roadmap GATE).
- `node scripts/build-connector-registry.cjs --check`, `build-orchestration-projection.cjs --check`, `check-render-coverage.cjs`, `doctor.cjs --acceptance` all exit 0.
- `bash tests/run-all-198.sh` green with all legs PASS.
- The human-verify checkpoint confirms two-host parity.
</verification>

<success_criteria>
Surface parity proven on CLI + one non-Anthropic MCP host (empty graph diff, identical gate sequence); the rollback is rehearsed and green; the 198 Plurai baseline passes on the measured result; every born-wired/projection/render/acceptance gate regenerates clean. The phase closes.
</success_criteria>

<output>
Create `.planning/phases/198-mcp-first-then-sdk/198-10-SUMMARY.md` when done.
</output>
