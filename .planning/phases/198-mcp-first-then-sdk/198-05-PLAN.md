---
phase: 198-mcp-first-then-sdk
plan: 05
type: execute
wave: 3
depends_on: ["198-04"]
files_modified:
  - lib/mcp/gate-render.cjs
  - lib/mcp/tools/gate.cjs
  - tests/test-198-gate-renderers.test.cjs
autonomous: true
requirements: [SPEC-4]
must_haves:
  truths:
    - "gate_render emits ONE Mindrian superset card schema (options + per-option descriptions + ranks + previews + single/multi-select)"
    - "The same gate renders through all three rungs: (a) MCP elicitation, (b) AskUserQuestion inside Claude Code, (c) structured text for headless"
    - "All three renderers return an identical gate_answer payload for the same user choice"
    - "The F.8 binding card fires ONCE per session on genuine ambiguity only (no per-turn card noise)"
  artifacts:
    - path: "lib/mcp/gate-render.cjs"
      provides: "superset card schema + 3-renderer ladder wrapping shape-f8/f9 renderers"
      exports: ["renderGate", "SUPERSET_SCHEMA", "pickRenderer"]
    - path: "lib/mcp/tools/gate.cjs"
      provides: "gate_render + gate_answer tools (with exported connectors)"
      contains: "gate_render"
    - path: "tests/test-198-gate-renderers.test.cjs"
      provides: "identical-answer-payload assertion across the three rungs"
      contains: "gate_answer"
  key_links:
    - from: "lib/mcp/gate-render.cjs"
      to: "lib/hmi/selector-dispatcher.cjs"
      via: "superset composition reuses shape-f8/f9 renderers"
      pattern: "shape-f"
    - from: "lib/mcp/tools/gate.cjs"
      to: "lib/core/navigation.cjs"
      via: "gate_answer ratify routes the material write through the chokepoint"
      pattern: "navigation"
---

<objective>
Ship the gate superset schema + renderer ladder (SPEC-4). 307 files today assume the Claude Code AskUserQuestion card; no schema exists independent of the host. This plan defines ONE Mindrian superset card (options + per-option descriptions + ranks + previews + single/multi-select) emitted by `gate_render`, and a three-rung renderer ladder: (a) MCP elicitation where the client declares the capability (lossy: titles only - descriptions travel app-side), (b) canUseTool/AskUserQuestion via the thin adapter inside Claude Code, (c) structured-text fallback for headless clients. All three must return an IDENTICAL gate_answer payload.

Purpose: The card-primitive collapse. Every gate (reach card, F.8 binding, F.9 reconcile) renders on any MCP host, and the binding-card noise the navigator logged all week is retired by design (D-04: the F.8 card fires ONCE per session, only on genuine ambiguity).
Output: lib/mcp/gate-render.cjs (superset + ladder), gate_render/gate_answer tools, the three-renderer identical-payload proof.

Reuse: compose from the shipped `lib/hmi/selector-dispatcher.cjs` + `shape-f8-renderer.cjs` (binding) + `shape-f9-renderer.cjs` (reconcile). Do NOT mint new card renderers. F.8 IS SEED-039's "F.7" (naming drift - do not double-build).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-mcp-first-then-sdk/198-SPEC.md
@.planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md
@.planning/phases/198-mcp-first-then-sdk/198-PATTERNS.md
@lib/hmi/shape-f8-renderer.cjs
@lib/hmi/shape-f9-renderer.cjs
@lib/hmi/selector-dispatcher.cjs
@.planning/phases/198-mcp-first-then-sdk/198-04-SUMMARY.md
</context>

## Artifacts this phase produces (full inventory)

Full inventory in 198-01/02-PLAN.md. **Born-wired model:** gate tools declare their connectors via a module export; data/mcp-tool-connectors.json is GENERATED by build-connector-registry.cjs (never hand-edited). **This plan (198-05) delivers:** lib/mcp/gate-render.cjs, the `gate_render` + `gate_answer` tools (with exported connectors), and the three-rung renderer ladder with identical-payload proof.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Superset card schema + three-renderer ladder</name>
  <read_first>
    - lib/hmi/shape-f8-renderer.cjs (renderShapeF8 at :69 - the binding card the ladder wraps)
    - lib/hmi/shape-f9-renderer.cjs (renderShapeF9 at :107 - the reconcile card)
    - lib/hmi/selector-dispatcher.cjs (the gate composition entry the superset reuses)
    - .planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md (Pattern 3 renderer ladder; Pitfall 4 elicitation is lossy - enum+enumNames only, no per-option descriptions; A2 elicitation client capability re-check at plan time)
    - .planning/phases/198-mcp-first-then-sdk/198-PATTERNS.md (gate-render role-match; the ladder block; F.8 fires once per session)
  </read_first>
  <behavior>
    - SUPERSET_SCHEMA carries: options[], per-option description, per-option rank, per-option preview, and a select mode (single|multi)
    - pickRenderer(clientCapabilities): returns 'elicitation' when the client declares elicitation, 'askuserquestion' inside Claude Code, 'text' otherwise
    - renderGate(card, ctx) via rung (a) elicitation produces a gate_answer with the SAME chosen option id as rung (b) and rung (c) for the same simulated user choice
    - the elicitation rung loses per-option descriptions on the WIRE but the returned gate_answer payload is still identical (descriptions are app-side metadata, not part of the answer)
    - an unambiguous binding context does NOT fire the F.8 card (fires once per session, only on genuine ambiguity)
  </behavior>
  <action>
    Create `lib/mcp/gate-render.cjs`. Define `SUPERSET_SCHEMA` - Mindrian's own card superset (options + per-option descriptions + ranks + previews + single/multi-select). Export `pickRenderer(capabilities)` implementing the ladder detection (elicitation-declared > insideClaudeCode > headless-text), and `renderGate(card, ctx)` dispatching to one of three renderers: (a) `renderViaElicitation` builds an elicitation `requestedSchema` (enum + enumNames titles only) and calls the server's elicitInput seam - descriptions/ranks/previews stay app-side; (b) `renderViaAskUserQuestion` composes the full superset card through the shipped shape-f8/f9 renderers / selector-dispatcher for the Claude Code thin adapter; (c) `renderViaText` emits a structured-text card + next-message fallback. All three MUST normalize the user's choice into ONE canonical `gate_answer` payload shape `{ gate_id, chosen: [optionId...], verdict }` so the answer is renderer-independent. Re-check the elicitation client-capability status at build time (RESEARCH A2, issue #2799) and record the finding in the SUMMARY; keep all three rungs regardless (headless needs (c)).
    F.8 binding-card discipline (D-04): renderGate fires the binding card ONCE per session on genuine ambiguity only; a resolved/unambiguous context returns no card. No em-dashes. CJS only. Zero Brain/network tokens.
  </action>
  <acceptance_criteria>
    - `node -e "const g=require('./lib/mcp/gate-render.cjs'); if(!g.SUPERSET_SCHEMA) throw new Error('no schema'); if(g.pickRenderer({elicitation:true})!=='elicitation') throw 0; if(g.pickRenderer({claudeCode:true})!=='askuserquestion') throw 0; if(g.pickRenderer({})!=='text') throw new Error('headless must pick text'); console.log('ok')"` prints `ok`.
    - `grep -n "shape-f8\|shape-f9\|selector-dispatcher" lib/mcp/gate-render.cjs` matches (renderers reused, not reinvented); `grep -c "function renderShapeF8\|function renderShapeF9" lib/mcp/gate-render.cjs` returns 0.
    - `node tests/test-198-local-only.test.cjs` exits 0.
    - `grep -n "gate_answer\|verdict" lib/mcp/gate-render.cjs` shows the canonical answer normalization.
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('./lib/mcp/gate-render.cjs')" && node tests/test-198-local-only.test.cjs && MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs; echo "exit=$?"</automated>
  </verify>
  <done>One superset schema; a three-rung ladder that reuses the shipped shape renderers; every rung normalizes to one canonical gate_answer; the F.8 card fires once per session on ambiguity only.</done>
</task>

<task type="auto">
  <name>Task 2: gate_render + gate_answer tools + identical-payload proof</name>
  <read_first>
    - lib/mcp/gate-render.cjs (Task 1 - renderGate/pickRenderer/SUPERSET_SCHEMA)
    - lib/mcp/tools/graph.cjs (Plan 04 - the register(server, ctx)+connectors module shape; the navigation.cjs write path gate_answer ratifies through)
    - lib/core/navigation.cjs (the chokepoint gate_answer's ratified write routes through)
    - tests/test-198-gate-renderers.test.cjs (Plan 01 stub to fill)
    - .planning/phases/198-mcp-first-then-sdk/198-RESEARCH.md (SPEC-4 acceptance: same gate through 3 renderers, identical answer payloads)
  </read_first>
  <action>
    Create `lib/mcp/tools/gate.cjs` exporting `register(server, ctx)` and `connectors` (the born-wired source). `gate_render` (input: a gate spec; output: the superset card rendered via the ladder for the calling client's capabilities). `gate_answer` (input: the canonical `{ gate_id, chosen, verdict }`; on an approve verdict it ratifies - routing any resulting material write through navigation.cjs, Part 9). Declare gate_render/gate_answer in the module `connectors` export with a hitl_shape (these ARE the HITL surface - e.g. `hitl_shape: "F.1"` for the general card, with hitl_why naming the renderer ladder). Do NOT hand-edit data/mcp-tool-connectors.json - build-connector-registry.cjs regenerates it from this export.
    Fill `tests/test-198-gate-renderers.test.cjs` (Plan 01 stub): construct ONE gate spec, render it through all three rungs (elicitation, askuserquestion, text) simulating a fixed user choice, and assert the three `gate_answer` payloads are byte-identical (`{ gate_id, chosen, verdict }` equal). This is the SPEC-4 acceptance.
    No em-dashes. CJS only.
  </action>
  <acceptance_criteria>
    - `node tests/test-198-gate-renderers.test.cjs` exits 0: one gate, three renderers, three identical gate_answer payloads.
    - `node -e "const m=require('./lib/mcp/tools/gate.cjs'); if(typeof m.register!=='function'||!Array.isArray(m.connectors)) throw 0; const n=m.connectors.map(c=>String(c.surface||c.tool)); if(!n.some(x=>x.indexOf('gate_render')>=0)||!n.some(x=>x.indexOf('gate_answer')>=0)) throw new Error('gate connectors missing'); console.log('ok')"` prints `ok`.
    - Each gate connector descriptor carries an `hitl_shape` key.
    - `grep -n "navigation" lib/mcp/tools/gate.cjs` matches (gate_answer ratify goes through the chokepoint).
    - `node scripts/build-connector-registry.cjs --check` exits 0 (gate tools regenerate clean into the generated files).
    - `bash tests/run-all-198.sh` exits 0 with the gate-renderers leg promoted from SKIP to PASS.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-198-gate-renderers.test.cjs && node scripts/build-connector-registry.cjs --check && bash tests/run-all-198.sh && MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs; echo "exit=$?"</automated>
  </verify>
  <done>gate_render emits the superset card via the ladder; gate_answer ratifies through navigation.cjs; the same gate through all three renderers yields identical answers (SPEC-4 acceptance green); gate connectors declared via module export.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate_answer -> navigation.cjs write | A ratified answer triggers a material write; it must route through the chokepoint |
| gate spec / answer schema -> tool surface | Card content and answer payloads cross the MCP boundary; must carry no room content bound for Brain |
| renderer ladder -> external MCP host | Elicitation payloads leave the server to a non-Anthropic host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-04 | Tampering | gate_answer ratified write | mitigate | The ratified material write routes through navigation.cjs only (Part 9); no direct DB write in tools/gate.cjs |
| T-198-05 | Information Disclosure | gate card + elicitation payload | mitigate | The superset card carries local option metadata only; test-198-local-only greps gate modules for Brain/network tokens; no room content in the schema (Part 8) |
| T-198-07 | Elevation of Privilege | gate_render/gate_answer ungoverned | mitigate | Both declared via the module `connectors` export with hitl_shape; --check fails closed on drift |
| T-198-10 | Spoofing | forged gate_answer for a gate the user never saw | mitigate | gate_answer carries the gate_id minted by gate_render; ratify verifies the answer references a live rendered gate before acting |
| T-198-SC | Tampering | npm installs | accept | No new installs; N/A per RESEARCH audit |
</threat_model>

<verification>
- `node tests/test-198-gate-renderers.test.cjs` green (three renderers, identical payloads - SPEC-4 acceptance).
- `node scripts/build-connector-registry.cjs --check` exits 0 (gate tools born-wired via module export).
- `MINDRIAN_MCP_FIRST= node scripts/coherence-smoke-test.cjs` green (flag-OFF plugin smoke).
- `bash tests/run-all-198.sh` exits 0 with the gate-renderers leg promoted to PASS.
</verification>

<success_criteria>
One superset card schema, three renderers, identical answers. Gate composition reuses the shipped shape renderers; gate_answer ratifies through the chokepoint; the F.8 binding card fires once per session on ambiguity only; gate connectors are born-wired via module export.
</success_criteria>

<output>
Create `.planning/phases/198-mcp-first-then-sdk/198-05-SUMMARY.md` when done.
</output>
