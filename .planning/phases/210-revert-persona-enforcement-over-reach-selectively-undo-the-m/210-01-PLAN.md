---
phase: 210-revert-persona-enforcement-over-reach
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-card-fire-relevance-gate.cjs
  - tests/test-210-trailer-relevance.cjs
  - tests/test-shape-declaration-advisory.cjs
  - tests/test-voice-glyph-advisory.cjs
  - tests/test-voice-contract-signal.cjs
  - tests/test-elevation-quorum-advisory.cjs
  - tests/run-all-210.sh
autonomous: true
requirements: [210-A, 210-B, 210-C, 210-D, 210-E]

must_haves:
  truths:
    - "Every softened gate in this phase has a two-directional test on disk BEFORE any implementation task runs (Nyquist Wave 0)"
    - "tests/run-all-210.sh exists with ALL phase legs pre-declared, so no later-wave plan ever edits the runner"
    - "The already-answered-plain-text-yes live incident from 2026-07-02 is encoded as an automated replay fixture"
  artifacts:
    - path: "tests/test-card-fire-relevance-gate.cjs"
      provides: "Two-directional item-E Stop-hook test: already-answered passes, irrelevant passes, genuine fork still blocks"
    - path: "tests/test-210-trailer-relevance.cjs"
      provides: "Item E-2 trailer test: frozen marker byte-identical, imperative softened"
    - path: "tests/test-shape-declaration-advisory.cjs"
      provides: "Item A advisory-exit-code test stub"
    - path: "tests/test-voice-glyph-advisory.cjs"
      provides: "Item B glyph-preference test stub"
    - path: "tests/test-voice-contract-signal.cjs"
      provides: "Item C score-not-veto test stub"
    - path: "tests/test-elevation-quorum-advisory.cjs"
      provides: "Item D suggest-not-force test stub"
    - path: "tests/run-all-210.sh"
      provides: "Phase aggregator, run()/run_if() idiom, all legs pre-declared"
  key_links:
    - from: "tests/test-card-fire-relevance-gate.cjs"
      to: "scripts/check-card-fire.cjs"
      via: "require + exported deriveTurnSignals/classifyCardFire/loadRegistry"
      pattern: "classifyCardFire"
    - from: "tests/run-all-210.sh"
      to: "tests/test-*.cjs legs"
      via: "run()/run_if() bash idiom copied verbatim from tests/run-all-209.sh"
      pattern: "run_if"
---

<objective>
Wave 0 for Phase 210 (Nyquist rule): create the six regression-test files named in 210-VALIDATION.md's Wave 0 Requirements plus the trailer test from 210-RESEARCH.md, and the tests/run-all-210.sh aggregator. Every test encodes BOTH directions of the CONTEXT.md constraint: (1) a genuine/relevant/unanswered case still fires the gate; (2) an irrelevant/already-answered case no longer force-blocks. The item-E tests are expected RED at the end of this plan (the relevance gate does not exist yet); the A/B/C/D stubs are expected RED for their softened-direction legs. Later waves make them green WITHOUT rewriting the assertions.

Serves all five requirement items: 210-A, 210-B, 210-C, 210-D, 210-E.

Purpose: implementation plans in wave 2 must have their acceptance tests waiting on disk, per the phase's own VALIDATION contract (per CONTEXT.md constraint "every softened gate needs a regression test proving BOTH directions").
Output: 6 new test files + 1 aggregator, committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-CONTEXT.md
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-VALIDATION.md
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols created across Phase 210 (this plan creates the test-side entries; plans 02-05 create the code-side entries):

- `lib/core/gate-relevance.cjs` (plan 05) - exports `extractOptionLabels(text)`, `gateAlreadyAnswered(precedingUserText, gateLabels)`, `gateTopicallyRelevant(precedingUserText, gateText)`
- `scripts/check-card-fire.cjs` - new `preceding_user_text` field on the `readTranscriptTurn` return object (plan 05)
- `scripts/check-shape-declaration.cjs` - new `--strict` CLI flag; advisory (WARN, exit 0) becomes the `--check` default (plan 02)
- `lib/core/fusion-router.cjs::sessionEndQuorum` - new `suggested: true` field replacing `forced: true` semantics (plan 04)
- `lab/apo/apo-loop.cjs` - `voiceFlagged` candidate field replacing the `disqualified` veto filter (plan 04)
- `scripts/stamp-firing-block.cjs` - `STAMP_MARKER` bumped to `<!-- mos:firing-block v2 -->` + v1-block strip-and-replace path in `runStamp` (plan 05)
- New test files (this plan): `tests/test-card-fire-relevance-gate.cjs`, `tests/test-210-trailer-relevance.cjs`, `tests/test-shape-declaration-advisory.cjs`, `tests/test-voice-glyph-advisory.cjs`, `tests/test-voice-contract-signal.cjs`, `tests/test-elevation-quorum-advisory.cjs`, `tests/run-all-210.sh`
- `docs/MINDRIAN-CANON.md` Appendix D entry 37 + Part 11 R16 advisory amendment, canon version 1.24 (plan 06, navigator-gated)

<tasks>

<task type="auto">
  <name>Task 1: Create the two item-E tests (Stop-hook relevance gate + trailer)</name>
  <files>tests/test-card-fire-relevance-gate.cjs, tests/test-210-trailer-relevance.cjs</files>
  <read_first>
    tests/test-209-incident-replay.cjs (the transcript-fixture harness to copy verbatim: JSONL transcript to tmp dir at lines 81-116, MINDRIAN_HOME re-point at 101-116, CARD_FIRE_SIDECHANNEL_PATH tmp isolation at 36-37 per T-209-25),
    tests/test-reach-gate-stale-turn-input.cjs (the three-legged bidirectional skeleton at lines 59-85),
    scripts/check-card-fire.cjs (exports deriveTurnSignals/classifyCardFire/loadRegistry; gateSignature label normalization at 308-343; classifyCardFire at 420-475),
    lib/hmi/selector-dispatcher.cjs (appendAskUserQuestionTrailer at 551-580; frozen askuserquestion_marker at 557-560; export sites at 1115 and 1131),
    .planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md (sections "CREATE: tests/test-210-relevance-gate.cjs" and "CREATE: tests/test-210-trailer-relevance.cjs")
  </read_first>
  <action>
    Per item 210-E. Create tests/test-card-fire-relevance-gate.cjs copying the test-209-incident-replay.cjs harness idiom (writeFileSync JSONL transcript, env with hook_event_name Stop + transcript_path, MINDRIAN_HOME re-point, side-channel tmp isolation, try/finally restore). Three legs mapped per 210-PATTERNS.md: (1) ALREADY-ANSWERED - a user record whose content plainly answers the gate's option labels (the live-incident shape: plain-text "yes" to the immediately preceding question), followed by an assistant record with gate-shaped ASCII-box text and NO AskUserQuestion tool_use; assert classifyCardFire returns intercept:false with reason 'gate-already-answered'. (2) IRRELEVANT/STALE - a user turn about a topic with zero token overlap with the gate's option-label set; assert intercept:false with reason 'gate-irrelevant-to-turn'. (3) PRESERVE FLOOR - a genuine, relevant, unanswered fork (reuse the exact fixture shape from test-209-incident-replay.cjs assertion set) still returns intercept:true. Legs 1-2 are RED until plan 05 lands; leg 3 must be green NOW and stay green. Do NOT modify tests/test-209-incident-replay.cjs.

    Create tests/test-210-trailer-relevance.cjs calling appendAskUserQuestionTrailer directly via the module export (lib/hmi/selector-dispatcher.cjs:1115 export site). Two directions: (a) a genuine-fork envelope still carries a firing trailer naming the AskUserQuestion tool and the SEED-021 no-reproduce clause; (b) the frozen '[AskUserQuestion contract:' marker (line 557, "do NOT alter shape" contract) stays byte-identical in all cases; (c) the trailer imperative is conditional, not unconditional-BINDING - assert the trailer text does NOT open with the literal '[BINDING:' and instead opens with the new stable prefix '[FIRE-IF-FORK:' once plan 05 lands (this assertion is RED now). Reverse-regression floor note for this stub: the floor is the classifyCardFire VERDICT behavior (legs b/c/d of test-209-incident-replay.cjs), not the literal string - plan 05 re-points the wording-only leg (a) in test-209-incident-replay.cjs plus three sibling tests (test-209-engine-arm-contract.cjs, test-209-room-pick-sensor.cjs, test-gate-native-fire-w1.cjs) from '[BINDING:' to '[FIRE-IF-FORK:' in the same commit; that is the ONE intentional re-point, not a floor violation. House test idiom: node:assert, pass counter, exit non-zero on failure, no em-dashes.
  </action>
  <verify>
    <automated>node -c tests/test-card-fire-relevance-gate.cjs && node -c tests/test-210-trailer-relevance.cjs && node tests/test-209-incident-replay.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both files parse (node -c exit 0)
    - node tests/test-card-fire-relevance-gate.cjs currently FAILS on legs 1-2 (expected RED) and its leg-3 fixture, run standalone against current classifyCardFire, yields intercept:true
    - node tests/test-209-incident-replay.cjs exits 0 (untouched, byte-identical per git diff)
    - grep -c "CARD_FIRE_SIDECHANNEL_PATH" tests/test-card-fire-relevance-gate.cjs returns >= 1 (hermetic isolation present)
    - No em-dashes in either file (grep for the em-dash character returns nothing)
  </acceptance_criteria>
  <done>Item-E tests exist on disk, preserve-floor leg green, softened-direction legs RED awaiting plan 05.</done>
</task>

<task type="auto">
  <name>Task 2: Create the four A/B/C/D test stubs</name>
  <files>tests/test-shape-declaration-advisory.cjs, tests/test-voice-glyph-advisory.cjs, tests/test-voice-contract-signal.cjs, tests/test-elevation-quorum-advisory.cjs</files>
  <read_first>
    scripts/check-shape-declaration.cjs (main() --check branch at 770-790; exported check()/checkTree() predicates),
    scripts/check-shape-declaration.test.cjs (fixture-driven check()/checkAll() idiom),
    lib/core/stance-state.cjs (FORCED_VOICE_COLORS at 52-55, forcedVoiceColorForStance at 134-138),
    lib/statusline/cockpit-renderer.cjs (forced-color override consumption near line 336),
    lab/apo/apo-loop.cjs (disqualifier at 206-235, selectBest at 141-151),
    lib/core/fusion-router.cjs (sessionEndQuorum at 476-517),
    tests/test-205-fusion-router.cjs (Tests 11-12 at 248-281),
    .planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-VALIDATION.md (Wave 0 Requirements list)
  </read_first>
  <action>
    Create four standalone node:assert scripts, one per item, each encoding both directions against the TO-BE contract (softened-direction legs RED until wave 2; preserve-floor legs green now where the current behavior already satisfies them):

    test-shape-declaration-advisory.cjs (item 210-A): spawnSync 'node scripts/check-shape-declaration.cjs --check' and assert exit 0 (advisory default - RED today if the live tree were violating; today's tree is conformant so ALSO assert the advisory contract on a synthetic violation: call the exported checkTree()/check() predicates against a fixture surface missing hitl_shape and assert the violation is still DETECTED and ENUMERATED in the report - the lint signal survives). Assert spawnSync with '--check --strict' on a synthetic violating fixture exits 1 once plan 02 lands (skip-guard this leg on flag absence today: if '--strict' is not yet recognized, the leg fails RED, which is correct). Predicate-detection legs must pass both before and after plan 02 (predicates stay intact per CONTEXT addendum decision 1).

    test-voice-glyph-advisory.cjs (item 210-B): require lib/core/stance-state.cjs and lib/statusline/cockpit-renderer.cjs; assert (1) stance-state still maps redteam->red and tell-act->blue as the DEFAULT (capability preserved), (2) the renderer treats the stance color as a preference, not an override: when natural voice detection yields a confident different color, natural detection wins (RED until plan 03 flips precedence at cockpit-renderer.cjs:336), (3) null stance still degrades safely (green now, per cockpit-signals.cjs:249-265 try/catch degrade).

    test-voice-contract-signal.cjs (item 210-C): require lab/apo/apo-loop.cjs exports; feed a candidate set where a violating candidate has decisively higher quality than a compliant one; assert (1) the violating candidate is NOT filtered out of selection (RED until plan 04 removes the .filter at apo-loop.cjs:232/:235), (2) voice violations still appear on the candidate record as a visible signal (voiceFlagged/voiceViolations fields), (3) checkVoiceContract itself (lab/apo/voice-contract-gate.cjs) still returns pass:false with violations for an em-dash output (detector untouched, green now).

    test-elevation-quorum-advisory.cjs (item 210-D): require lib/core/fusion-router.cjs; build the 2-open-frames fixture from tests/test-205-fusion-router.cjs Test 11; assert (1) sessionEndQuorum returns suggested:true and forced:false (RED until plan 04), (2) hypothesis still present with offered:true, committed:false, hedged:true (the T-205-07-E preserve floor, green now except the forced field), (3) the Test-12 zero-force case (horizontalFiredThisSession:true) still returns no force AND no suggestion pressure beyond the existing reason field.

    House rules: CJS, node built-ins only, no em-dashes, no hardcoded surface counts (RETRO-07c).
  </action>
  <verify>
    <automated>node -c tests/test-shape-declaration-advisory.cjs && node -c tests/test-voice-glyph-advisory.cjs && node -c tests/test-voice-contract-signal.cjs && node -c tests/test-elevation-quorum-advisory.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All four files parse (node -c exit 0)
    - Each file contains BOTH a softened-direction assertion and a preserve-floor assertion (visually verifiable: each file has >= 3 assert legs)
    - Running each file today produces at least one RED leg (softened direction) and no crash-before-assert (harness errors vs assertion failures distinguished)
    - No em-dashes in any of the four files
  </acceptance_criteria>
  <done>Four A/B/C/D stubs exist, both directions encoded, preserve-floor legs green against HEAD.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/run-all-210.sh with all legs pre-declared</name>
  <files>tests/run-all-210.sh</files>
  <read_first>
    tests/run-all-209.sh (the verbatim idiom to copy: set -uo pipefail, ROOT cd, PASS/FAIL/SKIP counters, run() and run_if() at lines 25-41, final exit on FAIL -eq 0, all legs pre-declared up front),
    .planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md (section "CREATE: tests/run-all-210.sh")
  </read_first>
  <action>
    Copy the run()/run_if() idiom from tests/run-all-209.sh verbatim (Pattern 2 in 210-RESEARCH.md, explicit CONTEXT.md requirement). Pre-declare ALL Phase 210 legs up front, per the 209 convention, so no later-wave plan ever edits this runner:
    (1) node tests/test-card-fire-relevance-gate.cjs, (2) node tests/test-210-trailer-relevance.cjs, (3) node tests/test-shape-declaration-advisory.cjs, (4) node tests/test-voice-glyph-advisory.cjs, (5) node tests/test-voice-contract-signal.cjs, (6) node tests/test-elevation-quorum-advisory.cjs, (7) node scripts/check-shape-declaration.test.cjs, (8) node tests/test-stance-voice-glyph-override.cjs, (9) node tests/test-202-apo-loop.cjs, (10) node tests/test-202-voice-contract-gate.cjs, (11) node tests/test-205-fusion-router.cjs, (12) node tests/test-209-incident-replay.cjs (the item-E preserve-floor leg), (13) node tests/test-209-stamp-firing-block.cjs, (14) node scripts/stamp-firing-block.cjs --check.
    Use run_if for every leg (SKIP on file absence). chmod +x. Expected state at end of this plan: several legs FAIL (RED, awaiting waves 2-3); the runner itself must execute end to end without a bash error.
  </action>
  <verify>
    <automated>bash -n tests/run-all-210.sh && test -x tests/run-all-210.sh</automated>
  </verify>
  <acceptance_criteria>
    - bash -n exits 0 (syntax valid) and the file is executable
    - grep -c "run_if" tests/run-all-210.sh returns >= 14
    - bash tests/run-all-210.sh runs to completion (non-zero exit is EXPECTED now, but it must print the PASS/FAIL/SKIP summary line, proving no bash abort)
    - grep -cE "test-209-incident-replay" tests/run-all-210.sh returns 1 (preserve-floor leg declared)
  </acceptance_criteria>
  <done>Aggregator on disk, all 14 legs pre-declared, executes end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures -> real user state | Tests synthesize transcripts and side-channel files; they must never touch the real ~/.mindrian path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-210-01 | Tampering | tests writing outside tmp isolation | mitigate | Copy the T-209-25 idiom exactly: MINDRIAN_HOME re-point + CARD_FIRE_SIDECHANNEL_PATH tmp isolation in every transcript-fixture test; try/finally env restore |
| T-210-02 | Repudiation | RED legs mistaken for infra failures | mitigate | Each stub distinguishes harness errors from assertion failures (assert messages name the direction being tested); run-all-210.sh prints per-leg labels |
| T-210-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (Package Legitimacy Audit: N/A per 210-RESEARCH.md) |
</threat_model>

<verification>
- bash tests/run-all-210.sh executes end to end and prints the summary (FAIL count > 0 expected at this wave)
- node tests/test-209-incident-replay.cjs exits 0 (byte-untouched)
- git status shows exactly the 7 new files
</verification>

<success_criteria>
- All 6 test files + aggregator on disk, syntax-valid, both directions encoded per softened gate
- Preserve-floor legs green against HEAD; softened-direction legs RED (correct Wave 0 state)
- No existing test file modified
</success_criteria>

<output>
Create `.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-01-SUMMARY.md` when done
</output>
