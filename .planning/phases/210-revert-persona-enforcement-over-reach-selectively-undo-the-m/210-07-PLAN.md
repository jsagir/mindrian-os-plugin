---
phase: 210-revert-persona-enforcement-over-reach
plan: 07
type: execute
wave: 4
depends_on: [210-01, 210-02, 210-03, 210-04, 210-05, 210-06]
files_modified:
  - CHANGELOG.md
  - .claude-plugin/plugin.json
  - package.json
  - "~/mindrian-marketplace/.claude-plugin/marketplace.json (via release.sh)"
autonomous: true
requirements: [210-A, 210-B, 210-C, 210-D, 210-E]

must_haves:
  truths:
    - "bash tests/run-all-210.sh reports FAIL=0 (all 14 pre-declared legs green or legitimately SKIP)"
    - "The five softened phases' own aggregators (run-all-190/192/202/205/209) are green except only the assertions this phase intentionally re-pointed"
    - "node scripts/doctor.cjs --acceptance is fully green"
    - "A new, previously-unpublished version ships via scripts/release.sh with a REAL npm publish to @mindrian_os/cli - the five-gate lockstep, never a hand bump"
    - "Phase 194/196/200 data-boundary gates and the 19 standalone v1.15 bug fixes are untouched (regression proof via the full suite run)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v1.15.3-beta.1 entry at the top describing the five softenings"
  key_links:
    - from: "scripts/release.sh"
      to: "npm registry @mindrian_os/cli"
      via: "Step 9.5 npm publish (real, not dry-run)"
      pattern: "npm publish"
---

<objective>
Phase gate and ship: prove the whole phase green (tests/run-all-210.sh, the five softened phases' own aggregators, doctor --acceptance), then cut the release through scripts/release.sh with a real npm publish - the explicit CONTEXT.md constraint and the mandatory end-of-phase step.

Version decision (verified against the live registry 2026-07-03): npm has published @mindrian_os/cli through 1.15.2 only; package.json and plugin.json already carry the committed pre-release bump 1.15.3-beta.1, which is NOT on the registry and NOT a reuse of any published version - it is the release target. If the registry state changed by execution time and 1.15.3-beta.1 is published, fall forward to 1.15.3-beta.2.

Serves all five items (210-A/B/C/D/E) as their shipping vehicle.

Purpose: the navigator's "less like Larry" fix reaches installed users only through the 5-gate lockstep.
Output: green phase suite + published release.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-CONTEXT.md
@.claude/includes/release-process.md
@CHANGELOG.md
</context>

## Artifacts this phase produces

See the full phase artifact list in 210-01-PLAN.md. This plan's contributions:

- CHANGELOG.md v1.15.3-beta.1 entry; version lockstep across plugin.json / package.json / git tag v1.15.3-beta.1 / marketplace.json source.ref (all via release.sh, never by hand)
- Published @mindrian_os/cli@1.15.3-beta.1 on npm

<tasks>

<task type="auto">
  <name>Task 1: Full phase regression gate</name>
  <files>(none modified - verification only; fix-forward commits allowed if a leg is red)</files>
  <read_first>
    tests/run-all-210.sh (the 14 pre-declared legs),
    .planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-VALIDATION.md (sampling contract: full suite + doctor before verify-work),
    .planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-CONTEXT.md (constraints section: the exact pre-done gate list and the preserve list)
  </read_first>
  <action>
    Run, in order, and fix-forward any red leg before proceeding (a fix must trace root cause per the house RCA discipline, never a surface patch): (1) bash tests/run-all-210.sh - expect FAIL=0; (2) the five softened phases' own aggregators: bash tests/run-all-190.sh, run-all-192.sh, run-all-202.sh, run-all-205.sh, run-all-209.sh - each must be green; any red leg must be one of the assertions plans 02-06 intentionally re-pointed (cross-check the plan SUMMARYs) and updated there, NOT silenced here; (3) node scripts/doctor.cjs --acceptance - fully green; (4) spot-check the preserve list is untouched: git log --oneline against the phase branch shows zero commits touching lib/core/navigation.cjs write paths, the Phase 194 session-room-binding gate, the Phase 196 PreToolUse Brain-egress guardrail, or the Phase 200 semantic-floor gate (grep the phase's changed-file list from git diff --name-only against those paths).
  </action>
  <verify>
    <automated>bash tests/run-all-210.sh && bash tests/run-all-209.sh && node tests/test-gate-native-fire-w1.cjs && bash tests/run-all-205.sh && bash tests/run-all-202.sh && bash tests/run-all-192.sh && bash tests/run-all-190.sh && node scripts/doctor.cjs --acceptance</automated>
  </verify>
  <acceptance_criteria>
    - bash tests/run-all-210.sh prints FAIL=0
    - All five prior-phase aggregators print FAIL=0
    - node tests/test-gate-native-fire-w1.cjs exits 0 explicitly run here (it is not a run-all-209.sh/run-all-210.sh leg; a fix-forward commit anywhere in this phase could otherwise regress it unseen at the final gate)
    - node scripts/doctor.cjs --acceptance exits 0
    - git diff --name-only <phase-start>..HEAD contains no path matching the Phase 194/196/200 gate files (preserve-list proof, listed in the SUMMARY)
    - run-all-209.sh's re-pointed tests (test-209-incident-replay.cjs, test-209-engine-arm-contract.cjs, test-209-room-pick-sensor.cjs, test-gate-native-fire-w1.cjs) each show a diff scoped ONLY to the '[BINDING:' -> '[FIRE-IF-FORK:' literal (or, for test-gate-native-fire-w1.cjs, the P3 doctrine-check body per plan 05 Task 4) per plan 05's line-scoped git-diff checks; no classifyCardFire verdict assertion, intercept flag, or retry/session-cap counter in any of the four differs from the pre-phase baseline
    - grep -rn "\[BINDING:" agents/ skills/ lib/ scripts/ tests/ commands/ | grep -v "CHANGELOG" returns empty (the two auto-loaded doctrine surfaces from plan 05 Task 4 are clean too, not just the code path)
  </acceptance_criteria>
  <done>Every phase behavior proven in both directions; no preserve-list drift; acceptance roll-up green.</done>
</task>

<task type="auto">
  <name>Task 2: Cut and publish the release via the five-gate lockstep</name>
  <files>CHANGELOG.md, .claude-plugin/plugin.json, package.json</files>
  <read_first>
    .claude/includes/release-process.md (the five-gate version-consistency rule; workspace guard),
    scripts/release.sh (full read before running - plan 02 edited its Step 2; confirm the edited block is the advisory shape-declaration invocation and the render-coverage/corpus-stats gates still HARD-ABORT),
    CHANGELOG.md (top-entry format of the last two releases)
  </read_first>
  <action>
    From /home/jsagi/dev/MindrianOS-Plugin ONLY (workspace guard - never the plugin cache). (1) Confirm the target version is unpublished: npm view @mindrian_os/cli versions --json must NOT contain 1.15.3-beta.1 (verified absent 2026-07-03); if present, use 1.15.3-beta.2 everywhere below. (2) Add the CHANGELOG.md top entry for 1.15.3-beta.1: the five softenings (A: shape-declaration gate advisory with --strict; B: glyph mapping default-not-locked + relevance-conditional footer; C: APO voice contract scores-not-vetoes; D: elevation quorum suggests-not-forces; E: relevance-gated force-fire + v2 firing block), the canon v1.24 amendment, and the preserve guarantees - Feynman-simplified, JTBD-oriented, no em-dashes. (3) Run scripts/release.sh 1.15.3-beta.1 - the full five-gate lockstep including the REAL npm publish to @mindrian_os/cli (CONTEXT.md constraint: never bump versions by hand; the script owns plugin.json/package.json/tag/marketplace sync). (4) Post-publish verification: npm view @mindrian_os/cli version (or versions) shows the new version; git tag v1.15.3-beta.1 points at the release commit; marketplace.json source.ref pinned to the tag. If npm auth blocks the publish, surface it and pause for the navigator to authenticate, then re-run - do not fake or skip the publish step.
  </action>
  <verify>
    <automated>npm view @mindrian_os/cli versions --json | grep -c "1.15.3-beta" && git tag --list "v1.15.3-beta*" && node -e "const p=require('./package.json'),j=require('./.claude-plugin/plugin.json');if(p.version!==j.version)process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - npm view @mindrian_os/cli versions --json contains the released version (real publish confirmed against the registry, not a local artifact)
    - git tag v1.15.3-beta.1 (or the fallback) exists and points at the release commit
    - CHANGELOG.md top entry, plugin.json, package.json, tag, and marketplace.json source.ref all carry the SAME version string (the five-place consistency rule)
    - scripts/release.sh exited 0 having run all its gates (release log shows the advisory shape-declaration WARN path and the still-hard render-coverage/corpus-stats gates)
  </acceptance_criteria>
  <done>New version live on npm via the full lockstep; all five version surfaces in sync.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local release -> public npm registry | The one real egress in this phase: publishing the plugin package (no user data, plugin source only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-210-20 | Tampering (shipping a regression) | release cut | mitigate | Task 1's full bidirectional suite + five prior-phase aggregators + doctor --acceptance are a hard precondition of Task 2; release.sh's remaining HARD gates (render-coverage, corpus-stats, CIRS) still abort |
| T-210-21 | Spoofing (version collision) | npm publish | mitigate | Pre-publish registry check that the target version is unpublished; fall-forward rule to beta.2; never reuse a published version (CONTEXT constraint) |
| T-210-22 | Repudiation (silent skip of publish) | release.sh Step 9.5 | mitigate | Post-publish registry verification is an acceptance criterion; auth failure pauses for the navigator, never skips |
| T-210-SC | Tampering | npm/pip/cargo installs | accept | No package installs; the phase only PUBLISHES, and only from the guarded dev workspace |
</threat_model>

<verification>
- Full suite + doctor green before the cut; registry, tag, and five version surfaces agree after the cut
</verification>

<success_criteria>
- Phase 210 proven green in both directions and shipped as a new npm version through the mandatory five-gate lockstep
</success_criteria>

<output>
Create `.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-07-SUMMARY.md` when done
</output>
