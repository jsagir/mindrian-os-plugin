---
phase: 210-revert-persona-enforcement-over-reach
plan: 04
type: execute
wave: 2
depends_on: [210-01]
files_modified:
  - lab/apo/apo-loop.cjs
  - lib/core/fusion-router.cjs
  - tests/test-202-apo-loop.cjs
  - tests/test-202-voice-contract-gate.cjs
  - tests/test-205-fusion-router.cjs
  - tests/test-voice-contract-signal.cjs
  - tests/test-elevation-quorum-advisory.cjs
autonomous: true
requirements: [210-C, 210-D]

must_haves:
  truths:
    - "The APO voice-contract check scores and flags, but can no longer veto a candidate out of selection (210-C: signal, not gate)"
    - "sessionEndQuorum offers its cross-frame hypothesis as a suggestion (suggested:true, forced:false); nothing downstream treats it as a mandatory pick (210-D)"
    - "checkVoiceContract stays an untouched detector; the elevation taxonomy and persona-taxonomy.cjs elevation-lean bias are byte-untouched (already soft, out of scope per Pitfall 5)"
    - "The hypothesis floor holds: offered:true, committed:false, hedged:true, never auto-committed, no edge written (T-205-07-E)"
  artifacts:
    - path: "lab/apo/apo-loop.cjs"
      provides: "Veto filter removed at :232/:235; violations folded as a negative score term; voiceFlagged visible on the span record"
    - path: "lib/core/fusion-router.cjs"
      provides: "sessionEndQuorum suggestion semantics (suggested:true, forced:false)"
      contains: "suggested"
  key_links:
    - from: "lab/apo/apo-loop.cjs"
      to: "lab/apo/voice-contract-gate.cjs"
      via: "checkVoiceContract(output, opts) -> { pass, violations }"
      pattern: "checkVoiceContract"
    - from: "lib/core/fusion-router.cjs consumers"
      to: "sessionEndQuorum return shape"
      via: "grep sessionEndQuorum( across lib/ and scripts/ - every consumer of .forced re-pointed"
      pattern: "sessionEndQuorum"
---

<objective>
Two small independent softenings in one plan (sized per the CONTEXT.md addendum: item C is deliberately small, item D is a single-function change):

Item 210-C (Phase 202, offline lab tool only - zero live-conversation blast radius per addendum decision 2): strip the voice-contract disqualifier's veto power in lab/apo/apo-loop.cjs; keep it as a visible score/signal. checkVoiceContract itself is untouched.

Item 210-D (Phase 205, per addendum decision 3: the confirmed target is lib/core/fusion-router.cjs::sessionEndQuorum, NOT persona-taxonomy.cjs which is already soft): relax the force-pick of exactly one hypothesis to a suggestion/default. The taxonomy stays a lens; the quorum stops being a mechanical must-follow.

Purpose: remove the last two judgment-call-to-mechanical-rule conversions outside the item-E fire path.
Output: veto-free APO selection + suggestion-semantics quorum + re-pointed 202/205 suites + green Wave 0 stubs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-CONTEXT.md
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-RESEARCH.md
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md
</context>

## Artifacts this phase produces

See the full phase artifact list in 210-01-PLAN.md. This plan's contributions:

- `voiceFlagged` candidate/span field in `lab/apo/apo-loop.cjs` (replaces the `disqualified` veto flag; violations fold into `score` as a negative term)
- `suggested: true` field on the `sessionEndQuorum` return object in `lib/core/fusion-router.cjs` (replaces `forced: true` semantics; `forced` stays present as `false` so no consumer crashes on a missing key)

<tasks>

<task type="auto">
  <name>Task 1: Item C - APO disqualifier becomes a score term, not a veto</name>
  <files>lab/apo/apo-loop.cjs, tests/test-202-apo-loop.cjs, tests/test-202-voice-contract-gate.cjs, tests/test-voice-contract-signal.cjs</files>
  <read_first>
    lab/apo/apo-loop.cjs (disqualifier at 206-235: checkVoiceContract call at 210-214, the two .filter((c) => !c.disqualified) veto sites at :232 and :235; selectBest at 141-151, quality-lexicographic with score tiebreak; span record at 245-253),
    lab/apo/voice-contract-gate.cjs lines 88-128 (checkVoiceContract returns { pass, violations } - DO NOT TOUCH this file),
    tests/test-202-apo-loop.cjs (the assertion proving a quality-0.95 em-dash candidate loses to a quality-0.80 compliant one - this is the assertion this task intentionally re-points),
    tests/test-voice-contract-signal.cjs (the Wave 0 stub this task makes green),
    .planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md (section "Item C")
  </read_first>
  <action>
    Per item 210-C (RESEARCH-recommended mechanical fix, addendum decision 2). In lab/apo/apo-loop.cjs: drop the .filter((c) => !c.disqualified) at :232 and :235 so selectBest sees every candidate; fold voice.violations.length as a negative term into the candidate's score field (signal-only - deliberately dent score, NOT quality, so quality primacy in selectBest stays structural; document the coefficient choice in a comment). Rename the disqualified flag to voiceFlagged (boolean) and keep voiceViolations on the span record at 245-253 so a human running the loop still SEES the signal. checkVoiceContract and lab/apo/voice-contract-gate.cjs are byte-untouched (the detector survives; only its veto power in the loop goes).

    Re-point tests/test-202-apo-loop.cjs: the quality-0.95-em-dash-loses assertion flips (it now WINS on quality since the veto is gone) - rewrite that assertion to prove the new contract (violating candidate selectable, score dented, voiceFlagged visible) and note the intentional flip in the SUMMARY. tests/test-202-voice-contract-gate.cjs should pass unchanged (detector untouched); extend it only if it asserts loop-level veto behavior. Make tests/test-voice-contract-signal.cjs green.
  </action>
  <verify>
    <automated>node -c lab/apo/apo-loop.cjs && node tests/test-202-apo-loop.cjs && node tests/test-202-voice-contract-gate.cjs && node tests/test-voice-contract-signal.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-voice-contract-signal.cjs exits 0 (all legs: violating candidate not filtered; signal visible; detector still detects)
    - node tests/test-202-apo-loop.cjs and node tests/test-202-voice-contract-gate.cjs exit 0
    - git diff shows zero changes to lab/apo/voice-contract-gate.cjs
    - grep -c "disqualified" lab/apo/apo-loop.cjs returns 0; grep -c "voiceFlagged" lab/apo/apo-loop.cjs returns >= 1
  </acceptance_criteria>
  <done>APO selection is score-informed, never vetoed; signal fully visible to the human operator.</done>
</task>

<task type="auto">
  <name>Task 2: Item D - sessionEndQuorum suggests instead of forces</name>
  <files>lib/core/fusion-router.cjs, tests/test-205-fusion-router.cjs, tests/test-elevation-quorum-advisory.cjs</files>
  <read_first>
    lib/core/fusion-router.cjs (sessionEndQuorum at 476-517: the early-return at 481-489, the force return at 510-516 with forced:true and count:1 D-Q1; the hypothesis fields offered:true/committed:false/hedged:true at 499-509 which are the T-205-07-E preserve floor),
    tests/test-205-fusion-router.cjs lines 248-281 (Test 11's forced===true assertion at 259-263 is the one this task re-points; Test 12 at 267-281 is KEEP-AS-IS),
    tests/test-elevation-quorum-advisory.cjs (the Wave 0 stub this task makes green),
    .planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md (section "Item D")
  </read_first>
  <action>
    Per item 210-D (addendum decision 3, navigator-confirmed target). In lib/core/fusion-router.cjs::sessionEndQuorum, downgrade the force to a suggestion (the self-contained option from 210-PATTERNS.md, chosen here to keep this plan independent of plan 05's gate-relevance module): the 510-516 return keeps count:1 and the full hypothesis object but returns forced:false plus a new suggested:true field, with the reason field naming the downgrade (e.g. 'cross-frame-suggestion'). Keep forced as an explicit false (never delete the key - consumers may read it). Preserve byte-exactly: hypothesis.offered:true, committed:false, hedged:true (T-205-07-E - never auto-committed, no edge written), and the 481-489 early-return conditions including the alreadyFired suppression.

    Then grep 'sessionEndQuorum(' across lib/ and scripts/ and re-point EVERY consumer of the .forced field so the hypothesis still SURFACES as an offer when suggested:true (capability preserved: the suggestion must not silently disappear from whatever rendered the forced pick before), while nothing downstream treats it as mandatory/blocking. List every touched consumer in the SUMMARY.

    Re-point tests/test-205-fusion-router.cjs Test 11 (259-263): forced===false, suggested===true, count===1, hypothesis.offered===true, hypothesis.committed===false (last two unchanged). Test 12 (267-281) stays byte-identical. Do NOT touch lib/core/persona-taxonomy.cjs (already soft, Pitfall 5). Make tests/test-elevation-quorum-advisory.cjs green.
  </action>
  <verify>
    <automated>node -c lib/core/fusion-router.cjs && node tests/test-205-fusion-router.cjs && node tests/test-elevation-quorum-advisory.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-elevation-quorum-advisory.cjs exits 0 (suggested:true/forced:false; hypothesis floor intact; Test-12 zero-force case intact)
    - node tests/test-205-fusion-router.cjs exits 0 with Test 12 assertions byte-unchanged (git diff scope check on lines 267-281)
    - git diff shows zero changes to lib/core/persona-taxonomy.cjs
    - Every consumer of sessionEndQuorum().forced found by grep is either re-pointed or listed in the SUMMARY as verified-unaffected; the hypothesis still renders as an offer somewhere (no capability loss)
  </acceptance_criteria>
  <done>Quorum suggests, never forces; hypothesis still offered and hedged; taxonomy untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline lab tool -> commands/act.md | APO remains recommend-then-ratify; nothing auto-writes prompts (unchanged) |
| session-end engine -> navigator-facing offer | The quorum output remains an offer, never a committed decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-210-09 | Tampering (signal loss) | apo-loop span record | mitigate | voiceFlagged + voiceViolations kept visible on every candidate/span; test asserts signal presence; detector file byte-untouched |
| T-210-10 | Tampering (capability loss) | sessionEndQuorum consumers | mitigate | Consumer grep + re-point requirement with SUMMARY listing; acceptance criterion that the suggestion still surfaces as an offer (the Phase 205 capability survives as a lens) |
| T-210-11 | Elevation of Privilege | a low-quality violating prompt variant winning an APO run | accept | Offline, human-ratified tool (addendum decision 2); the human sees voiceFlagged before ratifying; quality primacy in selectBest is structural |
| T-210-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- node tests/test-202-apo-loop.cjs, tests/test-202-voice-contract-gate.cjs, tests/test-205-fusion-router.cjs, tests/test-voice-contract-signal.cjs, tests/test-elevation-quorum-advisory.cjs all exit 0
- git diff scope: lab/apo/voice-contract-gate.cjs and lib/core/persona-taxonomy.cjs byte-untouched
</verification>

<success_criteria>
- Voice contract = signal, quorum = suggestion; both capabilities preserved; both 202/205 phase suites green with only the intentionally re-pointed assertions changed
</success_criteria>

<output>
Create `.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-04-SUMMARY.md` when done
</output>
