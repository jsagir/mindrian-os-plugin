---
phase: 210-revert-persona-enforcement-over-reach
plan: 03
type: execute
wave: 2
depends_on: [210-01]
files_modified:
  - lib/core/stance-state.cjs
  - lib/statusline/cockpit-renderer.cjs
  - skills/larry-personality/SKILL.md
  - tests/test-stance-voice-glyph-override.cjs
  - tests/test-voice-glyph-advisory.cjs
autonomous: true
requirements: [210-B]

must_haves:
  truths:
    - "The stance-to-voice-color mapping (redteam->red, tell-act->blue) survives as the DEFAULT/RECOMMENDED choice, no longer an unconditional override of natural detection"
    - "The stance-toggle footer offer is conditional on genuine relevance (navigator mid-decision about conversational mode), not near-universal every turn"
    - "Null/absent stance still degrades safely (cockpit-signals try/catch floor intact)"
  artifacts:
    - path: "lib/core/stance-state.cjs"
      provides: "Default (not forced) voice-color semantics for the two mapped stances"
    - path: "lib/statusline/cockpit-renderer.cjs"
      provides: "Preference-not-override precedence at the forced-color consumption seam (~line 336)"
    - path: "skills/larry-personality/SKILL.md"
      provides: "Footer SHOULD-clause tightened to genuine relevance; LOCKED wording moved to default/recommended"
  key_links:
    - from: "lib/statusline/cockpit-renderer.cjs"
      to: "lib/core/stance-state.cjs"
      via: "cockpit-signals.cjs readStanceState -> forced_voice_color field"
      pattern: "forced_voice_color"
    - from: "tests/test-stance-voice-glyph-override.cjs"
      to: "skills/larry-personality/SKILL.md"
      via: "doctrine-presence text-grep assertions at lines 70-72"
      pattern: "mos:stance"
---

<objective>
Item 210-B: soften the two Phase 192 mechanisms, which are DIFFERENT things needing separate edits (210-RESEARCH.md Pitfall 4): (1) the code-level LOCKED voice-glyph mapping in lib/core/stance-state.cjs consumed as an override by lib/statusline/cockpit-renderer.cjs, and (2) the prose-level near-universal stance-toggle footer SHOULD-clause in skills/larry-personality/SKILL.md:134 plus its line-136 LOCKED wording. Target state per CONTEXT.md item B: glyph stays the default/recommended choice but is not enforced as a hard contract violation when a turn's shape genuinely does not fit; footer becomes conditional on genuine relevance, not universal.

The glyph vocabulary itself, the 5-color De Stijl mark, and the stance system all survive untouched (capability preserved, only the no-override property softened).

Purpose: Larry's voice-color and footer become his judgment again, guided by the stance system instead of overridden by it.
Output: preference-semantics code + tightened prose + re-pointed doctrine tests + green tests/test-voice-glyph-advisory.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-CONTEXT.md
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-RESEARCH.md
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md
</context>

## Artifacts this phase produces

See the full phase artifact list in 210-01-PLAN.md. This plan's contributions:

- Preference-not-override precedence seam in `lib/statusline/cockpit-renderer.cjs` (natural detection wins on confident disagreement; stance color fills the default)
- Default/recommended wording in `lib/core/stance-state.cjs` doc-comments (export names preserved for consumers)
- Rewritten SKILL.md footer SHOULD-clause + default-mapping prose
- Re-pointed doctrine-presence assertions in `tests/test-stance-voice-glyph-override.cjs`

<tasks>

<task type="auto">
  <name>Task 1: Flip glyph precedence from override to preference (code)</name>
  <files>lib/core/stance-state.cjs, lib/statusline/cockpit-renderer.cjs, tests/test-voice-glyph-advisory.cjs</files>
  <read_first>
    lib/core/stance-state.cjs (FULL, 147 lines: FORCED_VOICE_COLORS at 52-55, forcedVoiceColorForStance at 134-138, module.exports map),
    lib/statusline/cockpit-signals.cjs lines 249-265 (readStanceState degrade floor - do not break),
    lib/statusline/cockpit-renderer.cjs (the forced-color consumption seam near line 336, where a non-null forced color currently OVERRIDES natural detection - read the surrounding natural-detection logic in full before editing),
    tests/test-stance-state.cjs and tests/test-192-statusline-stance-chip.cjs (existing suites that must stay green),
    tests/test-voice-glyph-advisory.cjs (the Wave 0 stub this task makes green),
    .planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md (section "Item B", Mechanism 1)
  </read_first>
  <action>
    Per item 210-B, using the cleanest seam named in 210-PATTERNS.md: change the CONSUMER precedence, keep the producer's export surface stable. In lib/statusline/cockpit-renderer.cjs at the ~line-336 seam: the stance color becomes the DEFAULT - it applies when natural voice detection yields no color (null/low-confidence); when natural detection confidently yields a DIFFERENT color, natural detection wins. Document the precedence rule in a comment citing Phase 210 item B. In lib/core/stance-state.cjs: keep the exported function name forcedVoiceColorForStance (cockpit-signals.cjs:249-265 consumes it; renaming breaks the degrade floor) but rewrite the constant name and doc-comments from FORCED/LOCKED language to DEFAULT/RECOMMENDED language (e.g. DEFAULT_VOICE_COLORS with a same-file alias if the constant is exported; check module.exports before renaming anything exported). Both consumers already degrade safely on null (verified in 210-PATTERNS.md) so no new failure mode is introduced.

    Finalize tests/test-voice-glyph-advisory.cjs against the shipped seam: leg (2) (natural detection wins on confident disagreement) goes green; legs (1) and (3) (default mapping preserved; null degrades) must have stayed green throughout.
  </action>
  <verify>
    <automated>node -c lib/core/stance-state.cjs && node -c lib/statusline/cockpit-renderer.cjs && node tests/test-stance-state.cjs && node tests/test-192-statusline-stance-chip.cjs && node tests/test-voice-glyph-advisory.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-voice-glyph-advisory.cjs exits 0 (all three legs)
    - node tests/test-stance-state.cjs exits 0 AND node tests/test-192-statusline-stance-chip.cjs exits 0 (existing floors; if a 192 assertion tests the OLD override precedence specifically, re-point that single assertion and note it in the SUMMARY as an intentional Phase 210 change - do not weaken any null-degrade or chip-render assertion)
    - forcedVoiceColorForStance('redteam') still returns 'red' and ('tell-act') still returns 'blue' (capability preserved)
    - grep -ci "locked" lib/core/stance-state.cjs returns 0 in code comments describing the mapping
  </acceptance_criteria>
  <done>Stance color is a preference at the renderer, default mapping intact, degrade floor intact.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the footer SHOULD-clause and LOCKED prose (doctrine)</name>
  <files>skills/larry-personality/SKILL.md, tests/test-stance-voice-glyph-override.cjs</files>
  <read_first>
    skills/larry-personality/SKILL.md (the stance section around lines 125-140: line 134 footer SHOULD-clause with the near-universal "when it is not already the most recent line" condition, line 136 "The two forced mappings are LOCKED" prose, line 125 "changes nothing until the navigator sets it" scoping pattern to mirror),
    tests/test-stance-voice-glyph-override.cjs (FULL: check() idiom at 29-36, the three assertions at 70-72 that grep the exact clauses being changed),
    .planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md (section "Item B" Mechanism 2 and "No Analog Found" row - this is the first relevance-conditioned prose clause in the skill; write fresh per RESEARCH Pitfall 4 guidance)
  </read_first>
  <action>
    Per item 210-B. Two prose edits in skills/larry-personality/SKILL.md, then re-point the doctrine test:

    (1) Line 134: replace the near-universal SHOULD-condition ("when it is not already the most recent line") with a genuine-relevance condition: Larry names the /mos:stance affordance when the navigator is actively mid-decision about conversational mode (stance friction visible, mode question raised, or a stance flip would change the very next move), NOT as a default every-turn closing line. Keep the "never forces the flip" sentence.

    (2) Line 136: move the "two forced mappings are LOCKED" wording to default/recommended language in lockstep with Task 1's code change (the mapping is the recommended color for those stances; natural detection may win when the turn's shape genuinely does not fit).

    (3) tests/test-stance-voice-glyph-override.cjs:70-72: the assertion regexes grep for the exact old clauses - re-point them to the new prose: keep the never-forced assertion, replace the every-turn footer-offer assertion with a genuine-relevance-condition assertion, keep the /mos:stance affordance-named assertion. Do not delete assertions; re-point them. No em-dashes anywhere.
  </action>
  <verify>
    <automated>node tests/test-stance-voice-glyph-override.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-stance-voice-glyph-override.cjs exits 0 with the same assertion COUNT as before (re-pointed, not removed)
    - grep -c "mos:stance" skills/larry-personality/SKILL.md returns >= 1 (affordance still named)
    - grep -ci "locked" skills/larry-personality/SKILL.md returns 0 in the stance-mapping prose
    - grep -c "never forces" skills/larry-personality/SKILL.md returns >= 1 (the anti-coercion floor survives)
    - No em-dashes in the edited section
  </acceptance_criteria>
  <done>Footer conditional on genuine relevance, mapping prose reads default/recommended, doctrine suite green at full assertion count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stance state file -> statusline render | Local JSON read; already try/catch degraded, unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-210-07 | Tampering (capability loss) | cockpit-renderer precedence flip | mitigate | Two-directional test: default mapping still applies when natural detection is silent (capability floor), natural detection wins only on confident disagreement; test-192-statusline-stance-chip stays green |
| T-210-08 | Denial of Service | renderer throw on new precedence path | mitigate | Both consumers already degrade to null on error (cockpit-signals.cjs:249-265); no new I/O introduced; node -c + existing chip test |
| T-210-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- node tests/test-voice-glyph-advisory.cjs, tests/test-stance-state.cjs, tests/test-192-statusline-stance-chip.cjs, tests/test-stance-voice-glyph-override.cjs all exit 0
</verification>

<success_criteria>
- Glyph mapping = default/recommended, not override; footer = relevance-conditional; capability and degrade floors intact
</success_criteria>

<output>
Create `.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-03-SUMMARY.md` when done
</output>
