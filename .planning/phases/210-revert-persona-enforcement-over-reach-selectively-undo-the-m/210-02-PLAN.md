---
phase: 210-revert-persona-enforcement-over-reach
plan: 02
type: execute
wave: 2
depends_on: [210-01]
files_modified:
  - scripts/check-shape-declaration.cjs
  - scripts/check-shape-declaration.test.cjs
  - scripts/release.sh
  - scripts/doctor.cjs
  - CLAUDE.md
  - docs/HITL-SHAPE-DECLARATION-CONTRACT.md
  - tests/test-shape-declaration-advisory.cjs
autonomous: true
requirements: [210-A]

must_haves:
  truths:
    - "check-shape-declaration.cjs --check WARNS and exits 0 on a violation (advisory default); --strict restores exit 1"
    - "Every violation is still DETECTED and ENUMERATED in the output - the lint signal survives, only the block is removed (per 210-A: soften all four checks, 190 base + three 209-03 predicates)"
    - "release.sh Step 2 and doctor --acceptance no longer HARD-ABORT on a shape-declaration violation, but the WARN output is visible in both"
    - "The adjacent render-coverage and corpus-stats gates in release.sh Step 2 are byte-untouched (preserve list: Phase 178 R15)"
  artifacts:
    - path: "scripts/check-shape-declaration.cjs"
      provides: "Advisory-default --check with --strict opt-in; all four predicates intact"
      contains: "--strict"
    - path: "scripts/release.sh"
      provides: "Step-2 shape-declaration block converted from ABORT to advisory WARN"
    - path: "scripts/doctor.cjs"
      provides: "coverage-gate comment/label rewritten to reflect advisory shape-declaration"
    - path: "CLAUDE.md"
      provides: "Part 11 bullet no longer claims HARD-FAIL enforcement for shape declaration"
  key_links:
    - from: "scripts/release.sh"
      to: "scripts/check-shape-declaration.cjs"
      via: "Step 2 invocation, lines 314-318"
      pattern: "check-shape-declaration\\.cjs"
    - from: "scripts/doctor.cjs"
      to: "scripts/check-shape-declaration.cjs"
      via: "coverage-gate gates array, spawnSync --check, ok test r.status === 0"
      pattern: "shape-declaration"
---

<objective>
Item 210-A: soften scripts/check-shape-declaration.cjs from HARD-FAIL to advisory at both wiring sites (release.sh Step 2, doctor --acceptance), per the CONTEXT.md addendum decision 1: ALL FOUR checks go advisory together - the Phase 190 base declaration-exists check (predicate 5, lines 182-195) AND the three Phase 209-03 predicates wired-body (248-260), tool-grant (263-271), declared-matches-body (276-284). The predicates and checkTree() stay fully intact (the lint signal survives); only the exit-1 block is removed by default. A --strict flag preserves the old behavior as opt-in.

This plan touches project doc shadows (CLAUDE.md Part 11 bullet, docs/HITL-SHAPE-DECLARATION-CONTRACT.md) but does NOT touch docs/MINDRIAN-CANON.md - the canon amendment is plan 06's navigator-gated checkpoint, per the house rule that no canon byte is written without navigator approval (Phase 190/192/195/205 precedent).

Purpose: restore "declared shape" to a visible lint signal instead of a build blocker, per the navigator's locked decision.
Output: advisory gate + rewired release/doctor call sites + honest doc shadows + green tests/test-shape-declaration-advisory.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-CONTEXT.md
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-RESEARCH.md
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md
</context>

## Artifacts this phase produces

See the full phase artifact list in 210-01-PLAN.md. This plan's contributions:

- `scripts/check-shape-declaration.cjs` `--strict` CLI flag (new symbol); advisory WARN-and-exit-0 as the `--check` default
- Rewritten release.sh Step-2 shape-declaration block (advisory invocation, ABORT removed)
- Rewritten doctor.cjs coverage-gate comment block (2783-2788) and label (2767)
- Extended `scripts/check-shape-declaration.test.cjs` (advisory exit-code assertions)

<tasks>

<task type="auto">
  <name>Task 1: Advisory default + --strict opt-in in check-shape-declaration.cjs</name>
  <files>scripts/check-shape-declaration.cjs, scripts/check-shape-declaration.test.cjs, tests/test-shape-declaration-advisory.cjs</files>
  <read_first>
    scripts/check-shape-declaration.cjs (FULL current state - Pitfall 1 in 210-RESEARCH.md: predicate 5 at 182-195, wired-body at 248-260, tool-grant at 263-271, declared-matches-body at 276-284, main() --check exit-1 branch at 770-782, STAMP_MARKER import at line 45),
    scripts/doctor.cjs lines 3162-3177 (the canonical WARN-not-fail output idiom to mirror),
    scripts/check-shape-declaration.test.cjs (existing fixture-driven suite that must stay green),
    tests/test-shape-declaration-advisory.cjs (the Wave 0 stub this task makes green),
    .planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md (section "Item A")
  </read_first>
  <action>
    Per item 210-A (CONTEXT addendum decision 1: all four checks together). In main()'s --check branch (scripts/check-shape-declaration.cjs:770-782): keep checkTree() and every predicate byte-identical; change the failure path so that WITHOUT --strict it prints a WARN block (prefix each violation line with 'WARN: ' mirroring the doctor.cjs:3172-3177 idiom, keep the existing recovery hint line) and exits 0; WITH a new --strict argv flag it keeps the current SHAPE DECLARATION VIOLATION output and process.exit(1). Do NOT change the STAMP_MARKER import at line 45 (shared constant with stamp-firing-block.cjs, T-209-10; plan 05 bumps the marker value in its home file - this import must keep working unmodified).

    Extend scripts/check-shape-declaration.test.cjs with advisory exit-code assertions (spawnSync the script --check vs --check --strict against a violating fixture; the pure-predicate fixtures already there should pass unchanged since predicates are untouched). If the script has no fixture-root seam for spawn-level tests, add a scan-root override (env var or --root flag) as a test seam, defaulting to the plugin root - document the seam in the file header. Finalize tests/test-shape-declaration-advisory.cjs against the shipped seam (Wave 0 stub owner: this plan may refine the stub's seam usage, not its two-directional contract).
  </action>
  <verify>
    <automated>node -c scripts/check-shape-declaration.cjs && node scripts/check-shape-declaration.test.cjs && node tests/test-shape-declaration-advisory.cjs && node scripts/check-shape-declaration.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - node scripts/check-shape-declaration.cjs --check exits 0 on the live (conformant) tree
    - On a synthetic violating fixture: --check exits 0 AND stdout/stderr contains 'WARN:' AND the violation text still names the offending surface (signal enumerated); --check --strict exits 1 on the same fixture
    - node scripts/check-shape-declaration.test.cjs exits 0 (existing predicate fixtures unchanged and green)
    - node tests/test-shape-declaration-advisory.cjs exits 0 (Wave 0 stub green, both directions)
    - git diff shows zero changes to the four predicate bodies (lines 182-195, 248-260, 263-271, 276-284 logic intact; only main()/reporting changed)
  </acceptance_criteria>
  <done>Advisory default shipped, --strict preserved, all predicates intact, both test suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire release.sh Step 2 + doctor.cjs + doc shadows</name>
  <files>scripts/release.sh, scripts/doctor.cjs, CLAUDE.md, docs/HITL-SHAPE-DECLARATION-CONTRACT.md</files>
  <read_first>
    scripts/release.sh lines 285-325 (the FULL Step-2 block - Anti-pattern warning in 210-RESEARCH.md: check-render-coverage.cjs at line 294 and build-corpus-stats.cjs at line 303 share this region and are on the Phase 178/preserve list, DO NOT TOUCH; only the shape-declaration block at 308-318 is in scope),
    scripts/doctor.cjs lines 2766-2812 (coverage-gate entry: label at 2767, the "HARD-FAIL, never WARN (R9)" comment at 2783-2788, ok test r.status === 0 at ~2801),
    CLAUDE.md (Part 11 Canon Compliance bullet: "enforced HARD-FAIL by scripts/check-shape-declaration.cjs at commit + release + doctor --acceptance (R16...)"),
    docs/HITL-SHAPE-DECLARATION-CONTRACT.md line 151 (the "then hard-FAILs once the baseline is conformant" enforcement paragraph),
    tests/test-canon-entry-36-shape-declaration-floor.cjs (run only - confirms the canon floor test does not grep the files touched here)
  </read_first>
  <action>
    Per item 210-A. Three edits, each surgical:

    (1) scripts/release.sh:308-318 ONLY - replace the if-!-ABORT-exit-1 block around check-shape-declaration.cjs with a direct advisory invocation: run the script --check (its WARN output now prints inline in the release log), follow with an echo noting the gate is advisory as of Phase 210 (navigator decision, CONTEXT addendum 1). Rewrite the preceding comment block (the "HARD-FAIL, never WARN (R9)" language at ~308-313) to state the advisory downgrade and cite Phase 210. The render-coverage block (line ~294) and corpus-stats block (line ~303) must be byte-identical before/after (verify with git diff).

    (2) scripts/doctor.cjs coverage-gate entry - the ok test (r.status === 0) auto-softens since the script now exits 0 (the cheapest correct path per 210-PATTERNS.md); required edits are honesty-only: rewrite the 2783-2788 comment (remove "HARD-FAIL, never WARN (R9)" claim for shape-declaration, cite Phase 210 advisory), and adjust the label at 2767 so it does not claim the shape-declaration leg blocks (e.g. note shape-declaration is advisory). Do not restructure the gates array; the other three gate entries (connector, projection, render) keep their blocker semantics untouched.

    (3) Doc shadows (project docs, NOT canon): update the CLAUDE.md Part 11 bullet to say the shape-declaration check is advisory (WARN at release + doctor, --strict opt-in) as of Phase 210, keeping the R16 declaration mandate language itself intact; update docs/HITL-SHAPE-DECLARATION-CONTRACT.md's enforcement paragraph (line ~151) the same way. Do NOT touch docs/MINDRIAN-CANON.md (plan 06, navigator-gated). No em-dashes in any edited prose.
  </action>
  <verify>
    <automated>bash -n scripts/release.sh && node -c scripts/doctor.cjs && node tests/test-canon-entry-36-shape-declaration-floor.cjs && node scripts/check-render-coverage.cjs</automated>
  </verify>
  <acceptance_criteria>
    - bash -n scripts/release.sh exits 0; git diff scripts/release.sh touches ONLY the shape-declaration block and its comment (render-coverage and corpus-stats blocks byte-identical)
    - grep -c "ABORT: shape-declaration gate failed" scripts/release.sh returns 0; grep -c "check-shape-declaration.cjs" scripts/release.sh returns >= 1 (still invoked, advisory)
    - node scripts/doctor.cjs --acceptance: coverage-gate entry passes on the live tree; grep -n "HARD-FAIL, never WARN" scripts/doctor.cjs no longer matches inside the shape-declaration gate comment (2783-2788 region)
    - grep -c "advisory" CLAUDE.md returns >= 1 in the Part 11 bullet region; grep -c "HARD-FAIL by scripts/check-shape-declaration" CLAUDE.md returns 0
    - node tests/test-canon-entry-36-shape-declaration-floor.cjs exits 0 (canon untouched, floor intact)
  </acceptance_criteria>
  <done>Both wiring sites advisory, adjacent preserve-list gates byte-untouched, doc shadows honest, canon floor green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release pipeline -> npm publish | Softening this gate changes what "block" means at release-cut time |

## STRIDE Threat Register

Orchestrator question 1 answered here: can softening item A let a genuinely broken/unrendered Shape-F declaration ship silently past release.sh?

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-210-03 | Tampering (gate guarantee) | release.sh Step 2 | mitigate | NOT silent: the script still runs at Step 2 and its WARN block prints inline in the release log (every violation enumerated); --strict opt-in preserved for any future re-hardening; two-directional test asserts detection survives (test-shape-declaration-advisory.cjs) |
| T-210-04 | Tampering | release.sh adjacent gates (render-coverage, corpus-stats) | mitigate | Explicit byte-identical acceptance criterion + git diff scope check; these are Phase 178/preserve-list gates and remain HARD ABORT |
| T-210-05 | Repudiation | doctor --acceptance reporting | mitigate | Label/comment rewritten so the acceptance report does not falsely claim a blocker that no longer blocks; the other three coverage gates keep blocker severity |
| T-210-06 | Elevation of Privilege | residual risk: an unrendered declaration ships in a release | accept | Navigator-directed downgrade (CONTEXT addendum decision 1); the runtime backstop for an ACTUALLY unfired card is item E's Stop hook, which stays armed (plan 05 preserve floor); declaration drift is a lint concern, not a runtime failure |
| T-210-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- node tests/test-shape-declaration-advisory.cjs exits 0
- node scripts/check-shape-declaration.test.cjs exits 0
- node scripts/doctor.cjs --acceptance passes coverage-gate on the live tree
- git diff confirms render-coverage + corpus-stats release.sh blocks byte-identical
</verification>

<success_criteria>
- All four shape checks advisory by default, detectable-and-enumerated, --strict opt-in
- Release and doctor wiring honest; no preserve-list gate touched; canon proper untouched (deferred to plan 06)
</success_criteria>

<output>
Create `.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-02-SUMMARY.md` when done
</output>
