---
phase: 210-revert-persona-enforcement-over-reach
plan: 05
type: execute
wave: 2
depends_on: [210-01]
files_modified:
  - lib/core/gate-relevance.cjs
  - scripts/check-card-fire.cjs
  - lib/hmi/selector-dispatcher.cjs
  - scripts/stamp-firing-block.cjs
  - commands/*.md (80 body-stamped, enumerated from disk at run time)
  - tests/test-209-stamp-firing-block.cjs
  - tests/test-card-fire-relevance-gate.cjs
  - tests/test-210-trailer-relevance.cjs
  - tests/test-209-incident-replay.cjs (wording-only re-point: leg (a) literal, never legs b/c/d)
  - tests/test-209-engine-arm-contract.cjs (wording-only re-point, same constraint)
  - tests/test-209-room-pick-sensor.cjs (wording-only re-point, same constraint)
  - tests/test-gate-native-fire-w1.cjs (wording-only re-point, same constraint)
autonomous: true
requirements: [210-E]

must_haves:
  truths:
    - "The Stop-hook backstop no longer force-blocks when the preceding user turn already plainly answered the gate, or when the gate's subject matter has zero connection to the current conversation"
    - "A genuine, relevant, unanswered fork STILL force-fires the card - tests/test-209-incident-replay.cjs's intercept-VERDICT legs (b/c/d: classifyCardFire outcome, retry/session-cap counters) stay byte-unmodified and green (the Phase 209 guarantee, the reverse-regression floor); the ONE intentional re-point is leg (a)'s trailer-literal wording assertion, moved from '[BINDING:' to the new stable prefix '[FIRE-IF-FORK:' in the same commit as Task 2, together with the three sibling tests that assert the same literal (test-209-engine-arm-contract.cjs, test-209-room-pick-sensor.cjs, test-gate-native-fire-w1.cjs)"
    - "MAX_FORCE_RETRIES=3 and MAX_SESSION_INTERCEPTS=12 are byte-untouched (T-179-01 DoS floor); the CR-03 invariant holds: preceding user text never enters gateSignature/turnContextHash"
    - "The trailer imperative and the command-body firing block instruct firing on a genuine unanswered fork, not unconditionally-BINDING; the frozen [AskUserQuestion contract: marker is byte-identical"
    - "The stamp sweep is enumerated from disk, never a hardcoded count (RETRO-07c); no command carries both v1 and v2 markers"
  artifacts:
    - path: "lib/core/gate-relevance.cjs"
      provides: "Shared relevance predicate: extractOptionLabels, gateAlreadyAnswered, gateTopicallyRelevant; pure, deterministic, never throws"
      min_lines: 40
    - path: "scripts/check-card-fire.cjs"
      provides: "preceding_user_text captured in readTranscriptTurn; relevance gate inserted before the final intercept:true"
    - path: "lib/hmi/selector-dispatcher.cjs"
      provides: "Conditional (fire-if-genuine-fork) trailer wording replacing unconditional [BINDING:"
    - path: "scripts/stamp-firing-block.cjs"
      provides: "Softened CANONICAL_FIRING_BLOCK, marker bumped to v2, v1 strip-and-replace sweep path"
  key_links:
    - from: "scripts/check-card-fire.cjs"
      to: "lib/core/gate-relevance.cjs"
      via: "require in classifyCardFire path"
      pattern: "gate-relevance"
    - from: "scripts/check-shape-declaration.cjs:45"
      to: "scripts/stamp-firing-block.cjs STAMP_MARKER"
      via: "shared constant import (T-209-10) - marker bump must keep this import green with zero edits to the importer"
      pattern: "STAMP_MARKER"
---

<objective>
Item 210-E, the big one: put a relevance/confidence gate in front of Phase 209's force-fire machinery at all three layers named in 210-RESEARCH.md - (E-1) the Stop-hook backstop in scripts/check-card-fire.cjs, (E-2) the unconditional [BINDING:] trailer minted by lib/hmi/selector-dispatcher.cjs::appendAskUserQuestionTrailer, (E-3) the mos:firing-block v1 stamp carried by 80 body-stamped commands. The backstop should NOT block when (a) the preceding user turn already plainly answered the question, or (b) the gate's subject matter has zero connection to the current conversation (the stale-artifact pattern). Bare removal is FORBIDDEN (Pitfall 3: it would re-create Phase 209's original bug in reverse); the default on uncertainty is intercept.

Builds ONE shared predicate module, lib/core/gate-relevance.cjs, per the RESEARCH primary recommendation, reusing gateSignature's existing label normalization instead of writing a second detection heuristic.

Purpose: the engine can finally distinguish "a genuine unanswered fork" from "a follow-up already answered" (the live 2026-07-02 incident).
Output: gate-relevance module + relevance-gated backstop + conditional trailer + v2 stamp sweep, with the 209 incident-replay floor green throughout.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-CONTEXT.md
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-RESEARCH.md
@.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md
@.planning/debug/resolved/reach-gate-stale-turn-input.md
</context>

## Artifacts this phase produces

See the full phase artifact list in 210-01-PLAN.md. This plan's contributions:

- `lib/core/gate-relevance.cjs` - NEW module exporting `extractOptionLabels(text)` (the label extraction moved out of gateSignature so there is exactly one implementation), `gateAlreadyAnswered(precedingUserText, gateLabels)`, `gateTopicallyRelevant(precedingUserText, gateText)`
- `preceding_user_text` field on the `readTranscriptTurn` return object in `scripts/check-card-fire.cjs`
- Two new classifyCardFire pass reasons: `'gate-already-answered'`, `'gate-irrelevant-to-turn'`
- `<!-- mos:firing-block v2 -->` marker + strip-and-replace path in `scripts/stamp-firing-block.cjs::runStamp`
- Conditional trailer wording in `lib/hmi/selector-dispatcher.cjs::appendAskUserQuestionTrailer`

<tasks>

<task type="auto">
  <name>Task 1: E-1 - gate-relevance module + relevance-gated Stop-hook backstop</name>
  <files>lib/core/gate-relevance.cjs, scripts/check-card-fire.cjs, tests/test-card-fire-relevance-gate.cjs</files>
  <read_first>
    scripts/check-card-fire.cjs (classifyCardFire header contract at 395-419 [pure, deterministic, no clock/random/network] and body at 420-475; gateSignature label normalization at 308-343, the OPTION_LABEL_RE extraction at 329-338 to MOVE not copy; the CR-03 comment block at 358-368 [retry-key invariant]; readTranscriptTurn at 700-774, the role:user window-reset at 743-749, the return object at 769-773; buildEnforcementEnvelope at 487-504 [unchanged]; MAX_FORCE_RETRIES/MAX_SESSION_INTERCEPTS at 169/189 [byte-untouched]),
    lib/core/stance-state.cjs (the module skeleton to copy: 'use strict', node built-ins, never-throws pure functions, frozen constants, flat module.exports),
    tests/test-card-fire-relevance-gate.cjs (the Wave 0 test this task makes green),
    tests/test-209-incident-replay.cjs (the preserve floor - run, never edit),
    .planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md (sections "Item E-1" and "CREATE: lib/core/gate-relevance.cjs")
  </read_first>
  <action>
    Per item 210-E. Three coordinated edits:

    (1) CREATE lib/core/gate-relevance.cjs on the stance-state.cjs module skeleton: pure, deterministic, node built-ins only, never throws (input-guard every function), zero network/Brain (Canon Part 8 header note). Exports: extractOptionLabels(text) - the OPTION_LABEL_RE extraction + lowercase/alnum-collapse normalization MOVED from check-card-fire.cjs:329-338 (single implementation; per RESEARCH Don't-Hand-Roll row 1); gateAlreadyAnswered(precedingUserText, gateLabels) - true when the normalized preceding user text matches one of the gate's normalized option labels or is an unambiguous plain-text affirmation/negation of a 2-option gate (the live-incident "yes" shape); gateTopicallyRelevant(precedingUserText, gateText) - false only on ZERO token overlap between the user turn and the gate's subject tokens (the stale-artifact pattern); on empty/absent user text return the CONSERVATIVE verdict (answered=false, relevant=true) so uncertainty defaults to intercept.

    (2) scripts/check-card-fire.cjs::readTranscriptTurn - capture the LAST role:user record's text into a new preceding_user_text field on the 769-773 return object. The 743-749 window-reset behavior is unchanged; per the CR-03 invariant (358-368) the user text feeds ONLY the relevance verdict, NEVER gateSignature or turnContextHash. Re-point gateSignature's label extraction to require lib/core/gate-relevance.cjs's extractOptionLabels so its output stays byte-equivalent (same regex, same normalization - retry keys must not change across this edit).

    (3) scripts/check-card-fire.cjs::classifyCardFire - insert the relevance gate at the exact insertion point named in 210-PATTERNS.md: between the retry-ceiling degrade (line ~465) and the final intercept:true (line ~467). If gateAlreadyAnswered -> return { intercept:false, reason:'gate-already-answered', degrade:false }; else if !gateTopicallyRelevant -> return { intercept:false, reason:'gate-irrelevant-to-turn', degrade:false }; else fall through to the existing intercept:true. NEVER delete the intercept path; MAX_FORCE_RETRIES/MAX_SESSION_INTERCEPTS byte-untouched (T-179-01).
  </action>
  <verify>
    <automated>node -c lib/core/gate-relevance.cjs && node -c scripts/check-card-fire.cjs && node tests/test-card-fire-relevance-gate.cjs && node tests/test-209-incident-replay.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-card-fire-relevance-gate.cjs exits 0 (all three legs: already-answered passes, irrelevant passes, genuine fork still intercepts)
    - node tests/test-209-incident-replay.cjs exits 0 with git diff showing the test file byte-UNMODIFIED (the reverse-regression floor)
    - grep -n "MAX_FORCE_RETRIES = 3" scripts/check-card-fire.cjs and grep -n "MAX_SESSION_INTERCEPTS = 12" scripts/check-card-fire.cjs both still match (adjust pattern to the file's exact spacing; values unchanged)
    - preceding_user_text does NOT appear inside gateSignature or turnContextHash function bodies (CR-03; grep-verify)
    - lib/core/gate-relevance.cjs contains zero require of node:https/node:http/child_process (pure local)
  </acceptance_criteria>
  <done>Backstop relevance-gated in both directions; 209 guarantee and DoS caps intact; one shared predicate module.</done>
</task>

<task type="auto">
  <name>Task 2: E-2 - conditional trailer wording in appendAskUserQuestionTrailer</name>
  <files>lib/hmi/selector-dispatcher.cjs, tests/test-210-trailer-relevance.cjs, tests/test-209-incident-replay.cjs, tests/test-209-engine-arm-contract.cjs, tests/test-209-room-pick-sensor.cjs, tests/test-gate-native-fire-w1.cjs</files>
  <read_first>
    lib/hmi/selector-dispatcher.cjs (appendAskUserQuestionTrailer at 551-580: the byte-frozen askuserquestion_marker at 557-560 ["do NOT alter shape"], the [BINDING: imperative at 565-567, the footer copy at 569-578; call site at 1050; export sites at 1115 and 1131),
    tests/test-210-trailer-relevance.cjs (the Wave 0 test this task makes green; encodes the re-scoped floor: verdict legs frozen, wording assertion is the one intentional re-point),
    tests/test-209-incident-replay.cjs (leg (a) at line 61 is the ONE literal-wording assertion to re-point from '[BINDING:' to '[FIRE-IF-FORK:'; legs (b)/(c)/(d) - the classifyCardFire verdict, intercept flag, retry-file absence - are the frozen floor and MUST NOT be touched),
    tests/test-209-engine-arm-contract.cjs (grep 'BINDING' to find its equivalent literal-wording assertion; re-point wording only, verdict assertions untouched),
    tests/test-209-room-pick-sensor.cjs (same constraint),
    tests/test-gate-native-fire-w1.cjs (same constraint),
    scripts/check-card-fire.cjs (grep 'BINDING' first - if the backstop keys on the '[BINDING:' literal anywhere outside the four tests above, every remaining detection site must also be re-pointed in this same task),
    .planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md (section "Item E-2")
  </read_first>
  <action>
    Per item 210-E. First grep -rn "BINDING" across scripts/ lib/ tests/ commands/ to enumerate every consumer of the literal before changing it; re-point each in this same task.

    In appendAskUserQuestionTrailer (565-578): keep the frozen '[AskUserQuestion contract:' marker (557-560) byte-identical; replace the unconditional '[BINDING: call the AskUserQuestion tool in THIS response...' imperative with conditional fire-if-genuine-fork wording using the new stable prefix '[FIRE-IF-FORK:' (machine-greppable, same bracket-prefix shape as the literal it replaces) that (a) still instructs calling the AskUserQuestion tool with the N options when the fork is genuinely unanswered, (b) explicitly licenses NOT firing when the navigator already answered in plain text or the gate is unrelated to the current conversation (acknowledge and proceed instead), (c) keeps the SEED-021 do-not-reproduce-as-text clause verbatim (render hygiene is not over-reach). Keep the rendered.askuserquestion_binding field name (consumers read it); only its contents change. Both call/export sites (1050, 1115, 1131) need no signature change.

    In the same commit, re-point ONLY the trailer-literal wording assertion in each of tests/test-209-incident-replay.cjs (line 61), tests/test-209-engine-arm-contract.cjs, tests/test-209-room-pick-sensor.cjs, and tests/test-gate-native-fire-w1.cjs from '[BINDING:' to '[FIRE-IF-FORK:'. Every other assertion in these four files (classifyCardFire verdict outcomes, intercept flags, retry/session-cap counters, the CR-03 retry-key invariant) MUST remain byte-identical. Prove this per-file with a line-scoped diff check, e.g. `git diff <file> | grep -E '^[+-]' | grep -v 'BINDING\|FIRE-IF-FORK'` returns empty (no other line changed).
  </action>
  <verify>
    <automated>node -c lib/hmi/selector-dispatcher.cjs && node tests/test-210-trailer-relevance.cjs && node tests/test-209-incident-replay.cjs && node tests/test-209-engine-arm-contract.cjs && node tests/test-209-room-pick-sensor.cjs && node tests/test-gate-native-fire-w1.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-210-trailer-relevance.cjs exits 0 (trailer present on genuine fork; frozen marker byte-identical; no '[BINDING:' prefix; new '[FIRE-IF-FORK:' prefix present)
    - grep -c "\[AskUserQuestion contract:" in a rendered envelope fixture returns 1 with unchanged bytes (the 557-560 frozen contract)
    - grep -rn "\[BINDING:" lib/ scripts/ tests/ commands/ returns 0 after re-pointing (or every remaining match is a deliberate historical comment, listed in the SUMMARY)
    - SEED-021 clause still present in the trailer text (grep 'SEED-021' lib/hmi/selector-dispatcher.cjs returns >= 1)
    - node tests/test-209-incident-replay.cjs, tests/test-209-engine-arm-contract.cjs, tests/test-209-room-pick-sensor.cjs, tests/test-gate-native-fire-w1.cjs all still exit 0
    - for each of the four re-pointed test files: `git diff <file> | grep -E '^[+-]' | grep -v 'BINDING\|FIRE-IF-FORK'` returns empty (proves only the literal-wording lines changed, no verdict-assertion drift)
  </acceptance_criteria>
  <done>Trailer instructs judgment-gated firing using the new [FIRE-IF-FORK: prefix; frozen marker and SEED-021 hygiene intact; the four sibling tests are re-pointed on wording only, their verdict assertions (and the reverse-regression floor) untouched.</done>
</task>

<task type="auto">
  <name>Task 3: E-3 - soften the canonical firing block and sweep all stamped commands to v2</name>
  <files>scripts/stamp-firing-block.cjs, commands/*.md (enumerated from disk), tests/test-209-stamp-firing-block.cjs</files>
  <read_first>
    scripts/stamp-firing-block.cjs (STAMP_MARKER at :46, INJECT_BLOCK/STAMP_END assembly at :66, runStamp enumeration at :191-237, --check at :244-264),
    scripts/check-shape-declaration.cjs line 45 (imports STAMP_MARKER - the shared-constant coupling T-209-10; the wired-body predicate keys on the marker),
    tests/test-209-stamp-firing-block.cjs (may assert the v1 literal - re-point if so),
    commands/mos.md and commands/help.md (two live examples of the v1 stamped block, per CONTEXT item E),
    .planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-PATTERNS.md (section "Item E-3")
  </read_first>
  <action>
    Per item 210-E and RETRO-07c (never hand-edit the 80 files; the stamp script is the single mutation surface). In scripts/stamp-firing-block.cjs: (1) soften CANONICAL_FIRING_BLOCK in this ONE place - same conditional fire-if-genuine-fork semantics as Task 2's trailer (fire the card on a genuine unanswered fork; do not fire when the navigator already answered in plain text or the gate is off-topic; keep the never-reproduce-the-selector-as-text hygiene line); (2) bump STAMP_MARKER to '<!-- mos:firing-block v2 -->' (block bytes changed, so the marker version must move per 210-PATTERNS.md); (3) extend runStamp with a strip-and-replace path: detect any prior v1 marker..STAMP_END span and replace it with the v2 block (idempotent: a second run is a no-op), so the sweep cannot double-inject. Then run the sweep across commands/*.md (live enumeration; ~80 body-stamped expected but NEVER hardcode the count - call the script's own enumeration).

    check-shape-declaration.cjs:45 imports the STAMP_MARKER constant, so the wired-body predicate follows the bump automatically with ZERO edits to the importer - verify, do not edit that file (it belongs to plan 02 this wave). Re-point tests/test-209-stamp-firing-block.cjs if it asserts the v1 literal or v1 block wording.
  </action>
  <verify>
    <automated>node scripts/stamp-firing-block.cjs --check && node tests/test-209-stamp-firing-block.cjs && node scripts/check-shape-declaration.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - node scripts/stamp-firing-block.cjs --check exits 0 (sweep clean, idempotent: a second apply run reports changedCount 0)
    - grep -rl "mos:firing-block v1" commands/ | wc -l returns 0 AND grep -rl "mos:firing-block v2" commands/ | wc -l returns the same count runStamp enumerates (no hardcoded literal in any assertion)
    - No command file carries both markers (grep -rlE "firing-block v1" on the v2 set returns 0)
    - node scripts/check-shape-declaration.cjs --check exits 0 with git diff showing scripts/check-shape-declaration.cjs byte-UNMODIFIED by this plan
    - node tests/test-209-stamp-firing-block.cjs exits 0
  </acceptance_criteria>
  <done>All stamped commands carry the softened v2 block via one scripted sweep; shared-constant coupling green untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript text -> Stop-hook verdict | Locally trusted per the existing Stop-hook contract; the new predicate adds no external input surface (ASVS V5: N/A per RESEARCH Security Domain) |

## STRIDE Threat Register

Orchestrator question 2 answered here: can softening the force-fire let a genuinely unanswered Decision Gate get skipped, re-creating Phase 209's ORIGINAL bug in reverse?

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-210-12 | Tampering (gate guarantee, the reverse-regression) | classifyCardFire relevance gate | mitigate | tests/test-209-incident-replay.cjs's verdict legs (b/c/d) must pass byte-unmodified (acceptance criterion with line-scoped git-diff proof); the only intentional change is leg (a)'s literal-wording re-point in Task 2 (same constraint applied to the three sibling tests); relevance pass verdicts are two NARROW named reasons only; empty/uncertain user text defaults to intercept (conservative verdict encoded in gate-relevance.cjs); bidirectional test test-card-fire-relevance-gate.cjs proves both directions |
| T-210-13 | Denial of Service | Stop-hook loop (navigator-trap, T-179-01) | mitigate | MAX_FORCE_RETRIES=3 / MAX_SESSION_INTERCEPTS=12 byte-untouched (grep acceptance criterion); relevance gate only ADDS pass paths, never new intercept paths |
| T-210-14 | Tampering (retry-key drift) | gateSignature/turnContextHash | mitigate | CR-03 invariant: preceding_user_text grep-verified absent from both hash inputs; extractOptionLabels moved (not rewritten) so signatures stay byte-equivalent |
| T-210-15 | Tampering (detection loss) | '[BINDING:' literal consumers | mitigate | Mandatory pre-change grep enumeration of every BINDING consumer; re-point in the same commit; frozen [AskUserQuestion contract: marker asserted byte-identical |
| T-210-16 | Tampering (sweep corruption) | 80-command v2 re-stamp | mitigate | Single scripted mutation surface, strip-then-inject idempotence asserted (second run = no-op); no-dual-marker acceptance criterion; count enumerated from disk (RETRO-07c) |
| T-210-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- node tests/test-card-fire-relevance-gate.cjs, tests/test-210-trailer-relevance.cjs, tests/test-209-incident-replay.cjs (verdict legs unmodified, wording leg re-pointed), tests/test-209-engine-arm-contract.cjs, tests/test-209-room-pick-sensor.cjs, tests/test-gate-native-fire-w1.cjs, tests/test-209-stamp-firing-block.cjs all exit 0
- node scripts/stamp-firing-block.cjs --check and node scripts/check-shape-declaration.cjs --check both exit 0
</verification>

<success_criteria>
- Force-fire is relevance-gated at all three layers; genuine forks still fire; already-answered and stale gates pass through; DoS caps and CR-03 invariant untouched
</success_criteria>

<output>
Create `.planning/phases/210-revert-persona-enforcement-over-reach-selectively-undo-the-m/210-05-SUMMARY.md` when done
</output>
