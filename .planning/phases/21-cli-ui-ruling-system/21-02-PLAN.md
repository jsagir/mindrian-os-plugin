---
phase: 21-cli-ui-ruling-system
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - commands/status.md
  - commands/room.md
  - commands/help.md
autonomous: true
requirements:
  - UI-02
  - UI-03

must_haves:
  truths:
    - "/mos:status output follows Body Shape A (Mondrian Board) with 4-zone anatomy"
    - "/mos:room overview follows Body Shape B (Semantic Tree) and /mos:room [section] follows Body Shape C (Room Card)"
    - "/mos:help follows tldr-style grouped format per D-25"
    - "All retrofitted commands include Zone 4 (Action Footer) with 2-3 next steps"
    - "No command invents its own format -- all reference the ui-system SKILL.md shapes"
  artifacts:
    - path: "commands/status.md"
      provides: "Status command using Body Shape A (Mondrian Board)"
      contains: "Mondrian Board"
    - path: "commands/room.md"
      provides: "Room command using Body Shape B (Semantic Tree) and C (Room Card)"
      contains: "Semantic Tree"
    - path: "commands/help.md"
      provides: "Help command using tldr-style grouped format"
      contains: "tldr"
  key_links:
    - from: "commands/status.md"
      to: "skills/ui-system/SKILL.md"
      via: "references Body Shape A for output format"
      pattern: "Shape A|Mondrian Board|ui-system"
    - from: "commands/room.md"
      to: "skills/ui-system/SKILL.md"
      via: "references Body Shape B and C for output format"
      pattern: "Shape B|Shape C|Semantic Tree|Room Card"
    - from: "commands/help.md"
      to: "skills/ui-system/SKILL.md"
      via: "references help format rules"
      pattern: "tldr|grouped"
    - from: "commands/status.md"
      to: "references/templates/MINTO.md"
      via: "reads MINTO health for Reasoning column"
      pattern: "MINTO|Reasoning"
---

<objective>
Retrofit existing commands (status, room, help) to follow the 4-zone anatomy and declare their body shapes per the UI ruling system.

Purpose: The SKILL.md defines the rules; the commands must follow them. Without retrofit, the ruling system is theory. With it, every command output is consistent.

Output: Updated `commands/status.md`, `commands/room.md`, `commands/help.md` -- each declaring its body shape and following 4-zone anatomy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-cli-ui-ruling-system/21-CONTEXT.md
@.planning/phases/21-cli-ui-ruling-system/21-01-SUMMARY.md

# The ruling system this plan implements
@skills/ui-system/SKILL.md

# Current commands to retrofit
@commands/status.md
@commands/room.md
@commands/help.md

# MINTO template for dual context
@references/templates/MINTO.md

<interfaces>
<!-- From the ruling system (Plan 01 output) -->
The SKILL.md defines:
- 4-zone anatomy: Header Panel, Content Body, Intelligence Strip, Action Footer
- Body Shape A: Mondrian Board (progress bars, 10-char wide, filled with glyph)
- Body Shape B: Semantic Tree (folder tree with meaning symbols)
- Body Shape C: Room Card (wiki-style with graph relationships)
- Symbol vocabulary: 12 glyphs only, no emoji
- Color contract: Green/Cyan/Yellow/Red/Gray + Default
- Zone 4 NEVER omitted
- Density adaptation: >30 lines compresses Zone 1, limits Zone 3
- Error format: 3-line pattern
- Dual context: read STATE.md + MINTO.md before routing

From commands/status.md (current structure):
- Step 1: Check for room
- Step 2: Compute or read state
- Step 3: Display status (venture stage, room overview table, gaps, team, suggested action, integrations)
- Voice rules at end

From commands/room.md (current structure):
- Subcommands: view, overview (default), add, export
- overview: reads STATE.md, displays section detail
- Voice rules at end

From commands/help.md (current structure):
- Step 1: Determine venture stage
- Step 2: Load references
- Step 3: Default behavior (recommendations by state)
- Step 4: --all flag (full command list by stage)
- Voice rules at end
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit commands/status.md to Body Shape A (Mondrian Board)</name>
  <files>commands/status.md</files>
  <action>
Rewrite `commands/status.md` to follow the 4-zone anatomy using Body Shape A (Mondrian Board). Keep ALL existing functionality but restructure the output format.

**Changes to make:**

1. **Add a "UI Format" section** near the top (after the YAML frontmatter) that declares:
   - Body Shape: A (Mondrian Board)
   - References: `skills/ui-system/SKILL.md`
   - This tells the executor which rendering template to follow

2. **Restructure Step 3 (Display Status)** into 4-zone format:

   **Zone 1 -- Header Panel:**
   Show room name + venture stage in a compact card:
   ```
   ╭─ [Room Name] ── [Venture Stage] ─╮
   │ [N] sections · [N] entries · [date]│
   ╰────────────────────────────────────╯
   ```

   **Zone 2 -- Content Body (Shape A: Mondrian Board):**
   Replace the current "Room Overview" table with progress bars per section:
   ```
   problem-definition  ■■■■■■□□□□  3 entries · ✓ Reasoning
   market-analysis     ■■□□□□□□□□  1 entry   · ⚠ No MINTO
   solution-design     □□□□□□□□□□  Empty
   business-model      ■■■□□□□□□□  2 entries · • Draft MINTO
   competitive-analysis□□□□□□□□□□  Empty
   team-execution      ■■■■■■■□□□  4 entries · ✓ Reasoning
   legal-ip            □□□□□□□□□□  Empty
   financial-model     □□□□□□□□□□  Empty
   ```
   Each bar is 10 chars wide. Fill uses `■`, empty uses `□`. After the bar: entry count + MINTO health indicator (per D-21: `✓ Reasoning` = healthy MINTO, `⚠ No MINTO` = missing, `• Draft MINTO` = exists but incomplete).

   Keep venture stage, meetings mention, and integration status but format them compactly below the board.

   **Zone 3 -- Intelligence Strip:**
   Read room-proactive signals. If HIGH/MEDIUM signals exist, show max 3:
   ```
   ⚡ "aging infrastructure" converges across 3 sections
   ⬜ competitive-analysis has no entries
   ```
   If no signals, omit Zone 3 entirely.

   **Zone 4 -- Action Footer (NEVER omit per D-04):**
   ```
   ▶ /mos:explore-domains    Map your innovation landscape
   ▷ /mos:room overview      See section details
   ▷ /mos:diagnose           Classify your problem type
   ```
   Exactly 2-3 grounded next steps. Primary action uses `▶`, alternatives use `▷`.
   Derive the suggested actions from the current room state (same logic as existing "Suggested Next Action" but formatted as footer).

3. **Update the "no room" case** to follow error format (per D-24):
   ```
   ✗ No project found
     Why: No room/ directory in workspace
     Fix: /mos:new-project
   ```

4. **Add dual context reading (per D-20, D-21):**
   In Step 2, after reading STATE.md, also check for MINTO.md in each section. The MINTO health feeds the "Reasoning" column in the Mondrian Board.

5. **Keep existing voice rules** but add reference to D-23 banned phrases.

6. **Do NOT remove any existing functionality** (integration detection, team intelligence, etc.). Restructure it into the zones.
  </action>
  <verify>
    <automated>grep -c "Zone\|Shape A\|Mondrian\|■\|▶\|▷" commands/status.md | awk '{if ($1 >= 5) print "PASS"; else print "FAIL: only "$1" zone/shape references"}'</automated>
  </verify>
  <done>
- commands/status.md declares Body Shape A (Mondrian Board)
- Output follows 4-zone anatomy (Header, Mondrian Board, Intelligence Strip, Action Footer)
- Progress bars use 10-char glyph fill per section
- MINTO health shown per section (Reasoning column)
- Zone 4 always present with 2-3 next steps
- No room case uses 3-line error format
- All existing functionality preserved (integrations, team, meetings)
  </done>
</task>

<task type="auto">
  <name>Task 2: Retrofit commands/room.md and commands/help.md</name>
  <files>commands/room.md, commands/help.md</files>
  <action>
**Part A: Retrofit commands/room.md**

Restructure `commands/room.md` to follow the 4-zone anatomy with Body Shapes B and C.

1. **Add UI Format declaration** at top:
   - overview subcommand: Body Shape B (Semantic Tree)
   - [section] subcommand: Body Shape C (Room Card)

2. **Retrofit overview subcommand to Shape B (Semantic Tree):**

   **Zone 1 -- Header Panel:**
   ```
   ╭─ [Room Name] ── Data Room ─╮
   │ [N] sections · [N] entries   │
   ╰──────────────────────────────╯
   ```

   **Zone 2 -- Content Body (Shape B: Semantic Tree):**
   Replace the current section listing with a meaningful tree:
   ```
   ▼ problem-definition/        3 entries · ✓
   │ ├─ initial-exploration.md   2026-03-20
   │ └─ market-barriers.md       2026-03-21
   ▶ market-analysis/            1 entry
   ▷ solution-design/            Empty
   ▶ business-model/             2 entries
   ▷ competitive-analysis/       Empty
   ▼ team-execution/             4 entries · ✓
   │ ├─ core-team.md             2026-03-20
   │ └─ advisors.md              2026-03-21
   ▷ legal-ip/                   Empty
   ▷ financial-model/            Empty
   ```
   Symbols: `▼` = expanded (has entries, show children), `▶` = collapsed with content, `▷` = collapsed and empty. `├─` for non-last, `└─` for last child.

   Show the 2-3 most populated sections expanded, rest collapsed.

   **Zone 3 -- Intelligence Strip** (if signals exist, max 3)
   **Zone 4 -- Action Footer** (always, 2-3 next steps)

3. **Retrofit [section] subcommand to Shape C (Room Card):**

   **Zone 2 -- Content Body (Shape C: Room Card):**
   Wiki-style card for a single section:
   ```
   ╭─ problem-definition ─────────────────╮
   │ Define the core problem your venture  │
   │ addresses.                            │
   │                                       │
   │ Entries: 3 · Stage: Discovery         │
   │ MINTO: ✓ Governing thought defined    │
   │                                       │
   │ Graph Relationships:                  │
   │  → INFORMS market-analysis (2 edges)  │
   │  → CONTRADICTS financial-model        │
   ╰───────────────────────────────────────╯
   ```
   Include graph relationships (INFORMS, CONTRADICTS, CONVERGES, FILED_FROM) if LazyGraph data exists. If not, show "No graph connections yet."

   Floating signal badge if proactive intelligence exists for this section:
   ```
   ⚠ Single-lens gap: all entries from explore-domains
   ```

4. **Keep all existing subcommands** (view, add, export) functional. Only restructure the output format.

5. **Update error cases** to use 3-line error format (per D-24).

**Part B: Retrofit commands/help.md**

Restructure `commands/help.md` to follow tldr-style format (per D-25).

1. **Add UI Format declaration:** No specific body shape -- help uses a custom grouped list format.

2. **Restructure default behavior (Step 3):**
   Group commands by FLOW, not by venture stage:
   ```
   Getting Started
     /mos:new-project     Start a venture project
     /mos:diagnose        Classify your problem type
     /mos:beautiful-question  Reframe your challenge

   Working
     /mos:explore-domains Map your innovation landscape
     /mos:analyze-needs   Jobs to Be Done analysis
     /mos:structure-argument  Build a Minto Pyramid
     ...

   Reviewing
     /mos:status          See where you stand
     /mos:grade           Honest feedback on your thinking
     /mos:room            Manage your Data Room

   Brain (requires /mos:setup brain)
     /mos:suggest-next    Graph-informed recommendations
     /mos:find-connections  Cross-domain discovery
     /mos:deep-grade      Calibrated assessment

   Admin (hidden)
     [not shown to non-admin users]
   ```
   Keep the existing stage-aware recommendations but present them in flow groups.

3. **Restructure per-command help (`/mos:help <command>`):**
   Exactly 4 lines + up to 3 examples:
   ```
   /mos:explore-domains
   Map innovation domains around your venture idea

     /mos:explore-domains                    # interactive exploration
     /mos:explore-domains --deep             # comprehensive analysis
     /mos:explore-domains "renewable energy" # focused domain
   ```
   This is tldr-style. No walls of text. No man-page.

4. **Add Zone 4 (Action Footer)** to help output:
   ```
   ▶ /mos:diagnose    Not sure where to start? Let me classify your problem
   ▷ /mos:status      See your current project state
   ```

5. **Keep Brain enhancement** (optional brain query for personalized recommendations) but format output to match the grouped style.

6. **Update voice rules** to reference D-23 banned phrases.
  </action>
  <verify>
    <automated>grep -c "Shape B\|Shape C\|Semantic Tree\|Room Card\|Zone\|▶\|▷\|tldr" commands/room.md commands/help.md | awk -F: '{sum+=$2} END {if (sum >= 8) print "PASS"; else print "FAIL: only "sum" references"}'</automated>
  </verify>
  <done>
- commands/room.md declares Body Shape B (Semantic Tree) for overview and Shape C (Room Card) for section view
- Room overview uses tree symbols (expanded/collapsed/empty sections)
- Room section view uses wiki-style card with graph relationships
- commands/help.md uses tldr-style grouped format (Getting Started, Working, Reviewing, Brain, Admin)
- Per-command help is 4 lines + 3 examples max
- Both commands include Zone 4 (Action Footer) with next steps
- Error cases use 3-line format
- All existing functionality preserved
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `commands/status.md` declares Shape A and follows 4-zone anatomy
2. `commands/room.md` declares Shape B (overview) and Shape C (section) with 4-zone anatomy
3. `commands/help.md` uses tldr-style grouped format with Zone 4 footer
4. All three commands reference `skills/ui-system/SKILL.md` as their rendering authority
5. No command invents output format outside the ruling system
6. MINTO health appears in status output per section
7. No emoji in any retrofitted command
</verification>

<success_criteria>
- Running /mos:status produces Mondrian Board output with progress bars, MINTO health, and action footer
- Running /mos:room shows Semantic Tree with meaningful symbols (expanded/collapsed/empty)
- Running /mos:room [section] shows Room Card with graph relationships
- Running /mos:help shows grouped commands by flow, not a wall of text
- All three commands follow the same 4-zone structure defined in skills/ui-system/SKILL.md
</success_criteria>

<output>
After completion, create `.planning/phases/21-cli-ui-ruling-system/21-02-SUMMARY.md`
</output>
