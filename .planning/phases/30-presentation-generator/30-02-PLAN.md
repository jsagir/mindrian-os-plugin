---
phase: 30-presentation-generator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/presentation/wiki.html
  - templates/presentation/deck.html
autonomous: true
requirements: [PRES-03, PRES-04]

must_haves:
  truths:
    - "Wiki view has 3-panel layout: collapsible sidebar, article content, infobox"
    - "Wiki sidebar shows all sections with collapsible artifact lists"
    - "Wiki search filters sidebar items and highlights matches"
    - "Wiki supports [[wikilinks]] that navigate between artifacts"
    - "Deck auto-generates slides from MINTO.md sections + key artifact summaries + stats"
    - "Deck has fullscreen slides with keyboard navigation (left/right/escape)"
  artifacts:
    - path: "templates/presentation/wiki.html"
      provides: "3-panel wiki browser with sidebar, content, infobox, search, wikilinks"
      contains: "ROOM_DATA_PLACEHOLDER"
    - path: "templates/presentation/deck.html"
      provides: "Fullscreen slide deck with keyboard nav, auto-generated from MINTO"
      contains: "ROOM_DATA_PLACEHOLDER"
  key_links:
    - from: "templates/presentation/wiki.html"
      to: "ROOM_DATA.sections"
      via: "JavaScript buildSidebar() rendering section tree"
      pattern: "ROOM_DATA\\.sections"
    - from: "templates/presentation/deck.html"
      to: "ROOM_DATA.minto"
      via: "JavaScript generateSlides() creating slide DOM from MINTO sections"
      pattern: "ROOM_DATA\\.minto"
---

<objective>
Create the Wiki and Deck presentation templates. Wiki is the most content-rich view (3-panel browser with navigation). Deck is the most visual (fullscreen slides auto-generated from room content). Both are self-contained HTML with De Stijl dark theme default.

Purpose: These two views represent the primary reading experiences -- wiki for exploration, deck for presentation. They are the most complex templates and benefit from parallel development with Plan 01.
Output: templates/presentation/wiki.html, templates/presentation/deck.html
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- ROOM_DATA shape injected by generate-presentation.cjs (Plan 30-01) -->
ROOM_DATA = {
  roomName: string,        // venture name from STATE.md
  stage: string,           // venture stage
  generatedDate: string,   // ISO date
  theme: 'dark' | 'light', // selected theme
  sections: [{
    id: string,            // e.g. 'problem-definition'
    label: string,         // e.g. 'Problem Definition'
    color: string,         // hex color from SECTION_COLORS
    lightText: boolean,    // whether to use light text on this color
    entryCount: number,
    summary: string,
    artifacts: [{          // added by generate-presentation.cjs
      filename: string,
      title: string,
      content: string,     // full markdown content
      frontmatter: object,
      wordCount: number
    }]
  }],
  intelligence: { gaps: [], convergence: [], contradictions: [] },
  graph: { elements: { nodes: [], edges: [] } },
  stats: { artifacts, meetings, speakers, sections, empty },
  kuzu: { available, artifacts, sections, edges },
  minto: {                 // parsed from MINTO.md
    governing_thought: string,
    sections: [{
      title: string,
      content: string,
      key_points: string[]
    }]
  },
  opportunities: [{ title, status, section, content }],
  assets: [{ filename, section, type, size }],
  team: [{ name, role, section }],
  videoUrl: string | null
}

<!-- From rooms/align-x-milken/wiki.html -- 454 lines, proven De Stijl dark wiki -->
- 3-panel layout: sidebar (280px) + article + infobox (260px)
- Sidebar: collapsible sections with arrow toggle, item count badges
- Search: filters sidebar items, highlights text
- Article: H1 title, section tag, word count, markdown rendered via marked.js
- Infobox: key-value pairs from frontmatter, related articles list
- [[wikilinks]]: regex replacement linking to other artifacts
- Section colors on sidebar items and article tags
- Fonts: Bebas Neue (headings), Inter (body), JetBrains Mono (code/tags)

<!-- From rooms/align-x-milken/deck.html -- 1,158 lines, proven dark deck -->
- Fullscreen slides: 100vw x 100vh, one visible at a time
- Keyboard nav: ArrowRight/ArrowLeft/Space/Escape + click nav dots
- Slide types: title-slide, content-slide, stats-slide, quote-slide
- Progress bar at bottom
- Slide counter "3 / 12" in corner
- Auto-generated from room content: title slide, per-section slides, stats slide, closing slide

<!-- Branding contract (PRES-09) -- same as Plan 01 -->
- Meta generator tag
- System instruction comment
- Header with MindrianOS logo SVG
- Footer: "Built with MindrianOS" + Mondrian color bar
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wiki.html template</name>
  <files>templates/presentation/wiki.html</files>
  <action>
Create `templates/presentation/wiki.html` ported from the Align wiki.html (454 lines) but templatized with ROOM_DATA injection.

**Layout (3-panel):**
- Header bar: room name (Bebas Neue), search box, stage tag, "Back to Dashboard" link
- Sidebar (280px, collapsible): section groups with arrow toggles, artifact items with section-color left borders
- Article area (flex:1): article title, section tag with color, word count, rendered markdown content
- Infobox (260px): frontmatter key-value pairs, related artifacts list (from graph edges), section badge

**Sidebar behavior:**
- Each section is collapsible (click header to toggle)
- Section header shows: arrow icon, section name (uppercase), artifact count badge
- Artifact items show title, clicking loads that artifact in the article area
- Active item gets highlighted with section-color left border
- First section auto-opens on load, first artifact auto-loads

**Search:**
- Search box in header filters sidebar items by title match (case-insensitive)
- Matching items shown, non-matching hidden
- If search is empty, all items shown with section collapse state preserved

**Article rendering:**
- Title: Bebas Neue, 36px
- Meta: section tag (colored), word count (JetBrains Mono)
- Content: rendered via `marked.parse()` from CDN (`https://cdn.jsdelivr.net/npm/marked/marked.min.js`)
- [[wikilinks]]: before rendering, replace `[[artifact-title]]` with clickable links that navigate to that artifact in sidebar

**Infobox:**
- If the artifact has frontmatter fields (framework, confidence, artifact_id, etc.), display as key-value table
- Related artifacts: query ROOM_DATA.graph.elements.edges for edges touching this artifact, list connected artifacts as clickable links
- Section badge with color

**TOC (Table of Contents):**
- Parse H2/H3 headings from markdown content
- Render as clickable TOC in infobox below frontmatter
- Scroll article to heading on click

**Section colors:**
- Each section uses its SECTION_COLORS hex as accent (sidebar borders, article tags, infobox badge)
- CSS custom property `--sc` set per-item for easy color application

**Theme support:**
- Default De Stijl dark (--bg:#0D0D0D, --surface:#1A1A1A, etc.)
- `[data-theme="light"]` CSS override block with light colors
- Theme set by `{{THEME}}` placeholder in `<html data-theme="{{THEME}}">`
- No toggle button in wiki (theme is set at generation time by --theme flag)

**Branding (PRES-09):**
- MindrianOS meta tags in head
- System instruction comment in head
- Header: small MindrianOS logo SVG (inline) left of room name
- Footer: fixed bottom bar with "Built with MindrianOS" text + Mondrian 4-color bar

**Data injection:**
- `<script>const ROOM_DATA = /*ROOM_DATA_PLACEHOLDER*/{};</script>` in body
- JavaScript reads ROOM_DATA.sections to build sidebar and article content
  </action>
  <verify>
    <automated>ls /home/jsagi/MindrianOS-Plugin/templates/presentation/wiki.html && grep -q "ROOM_DATA_PLACEHOLDER" /home/jsagi/MindrianOS-Plugin/templates/presentation/wiki.html && grep -q "wikilink" /home/jsagi/MindrianOS-Plugin/templates/presentation/wiki.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - wiki.html exists in templates/presentation/
    - Has 3-panel layout (sidebar + article + infobox)
    - Sidebar renders sections with collapsible artifact lists
    - Search box filters sidebar items
    - [[wikilinks]] replaced with navigable links
    - TOC generated from headings
    - Infobox shows frontmatter + related artifacts from graph
    - Branding header + footer present
    - ROOM_DATA_PLACEHOLDER for data injection
  </done>
</task>

<task type="auto">
  <name>Task 2: Create deck.html template</name>
  <files>templates/presentation/deck.html</files>
  <action>
Create `templates/presentation/deck.html` ported from the Align deck.html (1,158 lines) but templatized for any room.

**Slide generation logic (JavaScript, from ROOM_DATA):**
1. Title slide: room name (Bebas Neue, large), stage tag, generation date, MindrianOS branding
2. Governing thought slide (if ROOM_DATA.minto.governing_thought exists): large quote-style text
3. Per-section slides: for each section with artifacts, one slide per section showing:
   - Section name as heading (section-colored accent bar)
   - Key points (bullet list from first artifact or minto section)
   - Artifact count badge
   - If section has >3 artifacts, create an additional "deep dive" slide with more detail
4. Stats slide: animated counters for artifacts, meetings, speakers, sections, edges
5. Intelligence slide (if intelligence data exists): gaps, convergence themes, contradictions
6. Opportunities slide (if opportunities exist): cards for each opportunity
7. Closing slide: room name, "Explore more" links to other views, MindrianOS branding

**Slide types (CSS classes):**
- `.slide-title` -- large centered text, dark background
- `.slide-content` -- heading + body content with section color accent
- `.slide-stats` -- grid of stat counters with animated number increment
- `.slide-quote` -- large italic text, centered (for governing thought)
- `.slide-closing` -- final slide with links

**Navigation:**
- Keyboard: ArrowRight/Space = next, ArrowLeft = prev, Escape = show overview, Home = first, End = last
- Click: left half = prev, right half = next
- Nav dots at bottom center (small circles, active one filled)
- Slide counter in top-right: "3 / 12"
- Progress bar at very bottom (thin colored line, width = currentSlide/totalSlides)

**Transitions:**
- CSS transform: slides positioned off-screen left/right, active slide at translateX(0)
- Transition: transform 400ms ease

**Theme support:**
- De Stijl dark default: dark bg, cream text, section-colored accents
- `[data-theme="light"]` override: light bg, dark text (same accent colors work)
- `{{THEME}}` placeholder in html tag

**Branding (PRES-09):**
- MindrianOS meta tags
- System instruction comment
- Small logo in top-left of every slide (subtle, 16px height)
- Bottom Mondrian color bar visible on all slides (fixed position)

**Data injection:**
- `<script>const ROOM_DATA = /*ROOM_DATA_PLACEHOLDER*/{};</script>` in body
- JavaScript generates all slide DOM from ROOM_DATA on page load
- No external dependencies except marked.min.js CDN for markdown rendering and Google Fonts CDN
  </action>
  <verify>
    <automated>ls /home/jsagi/MindrianOS-Plugin/templates/presentation/deck.html && grep -q "ROOM_DATA_PLACEHOLDER" /home/jsagi/MindrianOS-Plugin/templates/presentation/deck.html && grep -q "ArrowRight\|keydown" /home/jsagi/MindrianOS-Plugin/templates/presentation/deck.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - deck.html exists in templates/presentation/
    - Slides auto-generated from ROOM_DATA (title, sections, stats, closing)
    - Keyboard navigation works (arrow keys, space, escape)
    - Nav dots and progress bar present
    - Slide counter shows current/total
    - Governing thought slide renders from MINTO.md
    - Stats slide has animated counters
    - Branding on every slide + Mondrian color bar
    - ROOM_DATA_PLACEHOLDER for data injection
  </done>
</task>

</tasks>

<verification>
1. `ls templates/presentation/wiki.html templates/presentation/deck.html` -- both exist
2. Both contain `ROOM_DATA_PLACEHOLDER` for data injection
3. wiki.html contains sidebar, search, wikilink, infobox patterns
4. deck.html contains keyboard navigation (keydown event listener)
5. Both have MindrianOS branding elements (meta generator, footer, color bar)
</verification>

<success_criteria>
- Wiki template provides full 3-panel browsing experience with search, TOC, wikilinks, infobox
- Deck template auto-generates slides from any room's MINTO + sections + stats
- Both templates are self-contained HTML (inline CSS, CDN fonts + marked.js only)
- Branding contract met in both templates
</success_criteria>

<output>
After completion, create `.planning/phases/30-presentation-generator/30-02-SUMMARY.md`
</output>
