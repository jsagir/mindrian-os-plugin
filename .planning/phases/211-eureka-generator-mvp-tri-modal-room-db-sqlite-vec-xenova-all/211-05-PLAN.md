---
phase: 211-eureka-generator-mvp
plan: 05
type: execute
wave: 3
depends_on: [211-02, 211-03, 211-04]
files_modified:
  - scripts/eureka-room-report.cjs
  - tests/test-211-judge-gate.cjs
  - tests/run-all-211.sh
  - evals/plurai/211-baseline.json
  - evals/eureka/211-room-report.md
autonomous: false
requirements: [R211-REALROOM-GATE]
must_haves:
  truths:
    - "The full pipeline ran on a REAL room.db (room/.mindrian/room.db, 261 nodes) and produced a ranked eureka-candidate report with fire-rate statistics"
    - "The phase gate is executable as one command: bash tests/run-all-211.sh aggregates every 211 test leg with PASS/FAIL/SKIP"
    - "The deployed Cross-Topic Connection judge scored the gold-card connection texts (or an honest baseline_deferred record exists when offline) - never real-room content"
    - "A human spot-checked the real-room eureka candidates and the verdict (meaningful or not, fire-rate too hot/cold) is recorded - the MVP's empirical de-risk of embedding quality + fire-rate"
  artifacts:
    - path: "scripts/eureka-room-report.cjs"
      provides: "The vertical-slice runner: index -> embed once per node -> full-matrix signed differential -> top-50 report"
      min_lines: 120
    - path: "tests/run-all-211.sh"
      provides: "The phase aggregator (run_if guarded, modeled on run-all-200.sh)"
    - path: "evals/eureka/211-room-report.md"
      provides: "The real-room run output: top candidates + provenance + fire-rate table + spot-check verdict"
  key_links:
    - from: "scripts/eureka-room-report.cjs"
      to: "lib/core/rs-differential-scorer.cjs"
      via: "scoreMeasured with opts.vectors (embed once, score many)"
      pattern: "scoreMeasured"
    - from: "scripts/eureka-room-report.cjs"
      to: "lib/core/room-db.cjs"
      via: "openRoomDb(roomDir, {allowExtension:true}) - the single door"
      pattern: "openRoomDb"
    - from: "tests/test-211-judge-gate.cjs"
      to: "lab/eval/report-from-transcript.cjs"
      via: "endpointUrl + JUDGES reuse for the cross-topic-connection endpoint"
      pattern: "report-from-transcript"
---

<objective>
Prove the thesis on real data and close the phase gate the ROADMAP names: "run-all-211 + real-room eureka spot-check + the deployed Cross-Topic Connection judge". Build the vertical-slice runner that executes the whole measured pipeline (tri-modal index -> one embedding per node -> full-matrix signed differential -> ranked top-50 per SEED-049 PART 4) against the live dogfood room.db, aggregate every 211 test into run-all-211.sh, wire the deployed `cross-topic-connection` Plurai judge on the pseudonymous gold-card texts, and put a human in front of the real-room output.

Purpose: SEED-049's residual risk is explicit - "the open questions are calibrated small-local-embedding QUALITY (D3) and FIRE-RATE - both answered by the MVP, not by more design." This plan is that answer.
Output: the runner script, the judge-gate test, the phase aggregator, the real-room report with a human verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/seeds/SEED-049-mindrian-insight-engine-tri-modal-tri-source-hybrid-retrieval.md (PART 4 full-matrix shape; the residual-risk section)
@.planning/phases/211-eureka-generator-mvp-tri-modal-room-db-sqlite-vec-xenova-all/211-02-PLAN.md and 211-03-PLAN.md (the interface contracts consumed here)
@tests/run-all-200.sh (the aggregator template: run/run_if, PASS/FAIL/SKIP, file-guarded legs)
@lab/eval/report-from-transcript.cjs (endpointUrl, JUDGES.elevation slug cross-topic-connection, PLURAI_API_KEY resolution, offline fallback discipline)
@evals/plurai/196-baseline.json or 201-baseline.json (the baseline_deferred house shape)
</context>

## Egress rule for this plan (Part 8 discipline, stated once, enforced everywhere)

Real-room content NEVER reaches the Plurai endpoint. The judge leg scores ONLY the pseudonymous gold-card texts from 211-04 (synthetic-by-construction content, the run-all-200.sh precedent: "Plurai is BUILD/CI only, synthetic data only, never on the runtime path"). The real room.db output is verified by the HUMAN spot-check, not by network judges. scripts/eureka-room-report.cjs makes zero network calls of any kind (the encoder model is already cached from 211-01 Task 3; if not cached, the one model download carries zero room bytes).

## Artifacts this phase produces

This plan: `scripts/eureka-room-report.cjs` (flags: `--db <roomDir>` default `room`, `--offline` stub-encoder mode, `--top <n>` default 50, `--out <path>` default `evals/eureka/211-room-report.md`); `tests/test-211-judge-gate.cjs`; `tests/run-all-211.sh`; `evals/plurai/211-baseline.json`; `evals/eureka/211-room-report.md`.

Other plans (context): 211-01 embedding-spine + deps; 211-02 tri-modal-index + hybrid-retrieve + room-db door; 211-03 scoreMeasured + lexical-overlap; 211-04 gold cards + manual baseline. Together: the complete Phase 211 artifact set.

<tasks>

<task type="auto">
  <name>Task 1: scripts/eureka-room-report.cjs (the vertical-slice runner)</name>
  <files>scripts/eureka-room-report.cjs</files>
  <read_first>lib/core/eureka/tri-modal-index.cjs (openIndex/indexNodes/nodeText contracts as built), lib/core/rs-differential-scorer.cjs (the scoreMeasured opts.vectors reuse path), lib/core/room-db.cjs (openRoomDb(roomDir, {allowExtension:true}) + closeRoomDb), scripts/build-corpus-stats.cjs (the house script shape: REPO_ROOT constants, pure exported functions, require.main === module main(), argv switch-case per the gsd-tools pattern - no Commander)</read_first>
  <action>
    Create `scripts/eureka-room-report.cjs`. Flow: (1) parse argv (`--db`, `--offline`, `--top`, `--out` per the artifact spec; switch-case router, no dep); (2) open the room via openRoomDb(roomDir, {allowExtension:true}), openIndex, indexNodes (offline mode injects the deterministic stub encodeFn; live mode uses the real encoder); (3) load every indexed node's text + vector ONCE into memory (261 nodes at 384-dim is trivial); (4) build the cross-boundary pair set: pairs where the two nodes differ in top-level domain ancestor (walk properties.parentId to the root domain) OR differ in node type - never same-domain same-type pairs (the differential is a CROSS-domain signal); (5) for each pair call scoreMeasured(textA, textB, {vectors:[vecA, vecB]}) - embed once, score many; (6) sort by abs_diff descending, keep --top; (7) write the markdown report to --out: header with encoderProvenance + EUREKA_RRF_K/EUREKA_DIFF_FLOOR in effect + run date + node/pair counts; a FIRE-RATE table (pair counts passing |diff| > 0.1 / 0.3 / 0.4 / 0.5 - the calibration evidence 202-APO needs, and the honest answer to "does 0.3 ever fire with real embeddings" given the s11 finding that real bridges measured 0.16-0.25); the top-N candidate table (RS-001.. numbering per SEED-049 PART 4: node ids, truncated texts, semantic, lexical, signed_diff, direction, band); and a mandatory caveat block: "high differential is NECESSARY not SUFFICIENT - the top of this list may be restatements; the Grounding Guard (Phase 212) is the filter" plus "bands UNCALIBRATED". (8) finally closeRoomDb. Zero network calls; zero writes to nodes/edges/memory_event (report file + derived index tables only); read the room.db in a try/finally so the handle always closes. Exit 0 on success, 1 with a named error otherwise.
  </action>
  <verify>
    <automated>node scripts/eureka-room-report.cjs --db room --offline --top 10 --out /tmp/claude-1000/-home-jsagi/476b6d64-c732-49cc-8ab3-ef92ad87a500/scratchpad/211-offline-report.md && grep -c "FIRE-RATE\|fire-rate" /tmp/claude-1000/-home-jsagi/476b6d64-c732-49cc-8ab3-ef92ad87a500/scratchpad/211-offline-report.md</automated>
  </verify>
  <acceptance_criteria>
    - The offline smoke against the REAL room/.mindrian/room.db exits 0 and the report contains the fire-rate table, the provenance header, and the caveat block
    - `grep -v "^\s*//" scripts/eureka-room-report.cjs | grep -c "scoreMeasured"` returns >= 1 and `grep -v "^\s*//" scripts/eureka-room-report.cjs | grep -cE "https?://"` returns 0 (zero network)
    - The script never writes to the nodes or edges tables (read the final code and confirm; the only db writes are the eureka_* index tables via indexNodes)
    - No em-dash character in the script or its report output
  </acceptance_criteria>
  <done>One command turns a real room.db into a ranked, provenance-stamped, honestly-caveated eureka-candidate report.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test-211-judge-gate.cjs + tests/run-all-211.sh (the executable phase gate)</name>
  <files>tests/test-211-judge-gate.cjs, tests/run-all-211.sh, evals/plurai/211-baseline.json</files>
  <read_first>lab/eval/report-from-transcript.cjs (endpointUrl(slug) + the JUDGES map with elevation slug 'cross-topic-connection' labels No Connection | Hedged | Confident + the PLURAI_API_KEY / ~/.config/evals/credentials.json resolution + the offline-fallback discipline), evals/plurai/201-baseline.json (the baseline_deferred:true shape to mirror), tests/run-all-200.sh (the aggregator template), evals/eureka/cases/*.md (the gold-card texts this test scores)</read_first>
  <behavior>
    - Test A (offline, always runs): scoreMeasured over gold-card pairs with the stub encoder asserts DIRECTIONAL truths only, never magnitudes: (1) for archimedes-darkmatter, the abstracted-pattern text vs the destination text yields a valid signed result with full provenance; (2) the restatement distractor pair yields lexical LOWER than a verbatim self-pair (the paraphrase trap is representable); (3) an unrelated cross-card pair (nichefoods hypothesis vs lovelace destination) produces a well-formed result - no NaN, no throw
    - Test B (network, key-gated): when a Plurai key resolves, POST the darkmatter transferable connection statement and one unrelated pairing statement to the cross-topic-connection endpoint; assert the transferable one is judged Hedged or Confident and the unrelated one No Connection; on missing key, SKIP and write/refresh evals/plurai/211-baseline.json with {baseline_deferred:true, reason:'no_plurai_key', date, legs:['cross-topic-connection']} per the house pattern
  </behavior>
  <action>
    Create `tests/test-211-judge-gate.cjs` implementing behaviors A and B: reuse endpointUrl + the key resolution by requiring lab/eval/report-from-transcript.cjs exports (it exports endpointUrl and JUDGES - confirm the exact export list at the file bottom before wiring); ONLY gold-card frontmatter/body texts ever go over the wire (assert this structurally: the test never opens room/.mindrian/room.db - grep-able absence). Then create `tests/run-all-211.sh` modeled line-for-line on run-all-200.sh (set -uo pipefail, run/run_if helpers, PASS/FAIL/SKIP counters, exit [ FAIL -eq 0 ]) with these run_if-guarded legs: (1) test-211-embedding-spine.cjs, (2) test-211-tri-modal.cjs, (3) test-211-measured-differential.cjs, (4) test-211-case-cards.cjs, (5) test-211-judge-gate.cjs, (6) the runner offline smoke: `node scripts/eureka-room-report.cjs --db room --offline --top 5 --out /tmp/211-gate-smoke.md` guarded on scripts/eureka-room-report.cjs. Header comment names the phase gate: run-all-211 + real-room spot-check + Cross-Topic Connection judge, and restates the egress rule (synthetic/gold-card text only to Plurai, never room content). Commit evals/plurai/211-baseline.json in its deferred state when no key is present at build time.
  </action>
  <verify>
    <automated>bash tests/run-all-211.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-211.sh` prints PASS/FAIL/SKIP with FAIL=0 (judge leg may SKIP offline; everything else PASSES)
    - `grep -v "^#" tests/run-all-211.sh | grep -c "run_if"` returns >= 6
    - `grep -v "^\s*//" tests/test-211-judge-gate.cjs | grep -c "room.db"` returns 0 (real-room content structurally cannot reach the judge)
    - evals/plurai/211-baseline.json exists and is valid JSON (either live results or baseline_deferred:true)
    - No em-dash character in touched files
  </acceptance_criteria>
  <done>The phase gate is one command; the deployed judge is wired with the Part 8 egress rule enforced structurally.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live real-room run + eureka spot-check (the MVP de-risk verdict)</name>
  <read_first>evals/eureka/211-room-report.md (generated in step 1 below before presenting)</read_first>
  <what-built>Before pausing, Claude runs the LIVE pipeline (real encoder, real room.db): `node scripts/eureka-room-report.cjs --db room --top 50 --out evals/eureka/211-room-report.md` (first run downloads the ~25MB q8 model if 211-01 Task 3 deferred it). The report now holds the first MEASURED eureka candidates ever produced from a real MindrianOS room: 261 nodes, cross-domain pairs, signed differentials with provenance, and the fire-rate table at the 0.1/0.3/0.4/0.5 bands.</what-built>
  <how-to-verify>
    1. Open evals/eureka/211-room-report.md
    2. Read the top 10 candidates: for each, is the surfaced pair a MEANINGFUL cross-domain connection (something worth a hedged Larry offer), a restatement/paraphrase, or noise? SEED-049's live validation bar: "confirm the eurekas are meaningful"
    3. Read the fire-rate table: at the 0.3 floor, is the engine too hot (noise fountain), too cold (silent - plausible given s11 measured real bridges at 0.16-0.25), or reasonable? This is the FIRE-RATE de-risk answer 202-APO will tune from
    4. Give a verdict per candidate reviewed (meaningful / restatement / noise) and an overall fire-rate verdict (hot / cold / ok)
  </how-to-verify>
  <acceptance_criteria>After resume, the executor appends a "## Spot-check verdict (navigator, dated)" section to evals/eureka/211-room-report.md recording the per-candidate labels and the fire-rate verdict verbatim, and mirrors the conclusion in the phase SUMMARY (including, if the verdict is "cold at 0.3", the honest note that EUREKA_DIFF_FLOOR needs 202-APO calibration - a finding, not a failure). `bash tests/run-all-211.sh` re-run after the append still exits FAIL=0.</acceptance_criteria>
  <resume-signal>Provide the per-candidate verdicts and the fire-rate verdict (or describe issues found)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> Plurai REST endpoint | judge scoring crosses the network |
| room.db content -> report file | local room content lands in a tracked report |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-211-11 | Information disclosure | Plurai judge leg | mitigate | structural rule: judge test never opens room.db (grep-gated acceptance); only pseudonymous gold-card text egresses; offline default = baseline_deferred |
| T-211-12 | Information disclosure | 211-room-report.md in the tracked repo | accept | the dogfood room is repo-internal command-research content, already tracked in spirit; texts are truncated in the report; no user room is touched |
| T-211-13 | Repudiation | spot-check verdict | mitigate | verdict recorded verbatim, dated, attributed to the navigator inside the report artifact itself |
</threat_model>

<verification>
- `bash tests/run-all-211.sh` FAIL=0 (the phase gate)
- evals/eureka/211-room-report.md exists with fire-rate table, provenance header, and the spot-check verdict section
- evals/plurai/211-baseline.json valid (live or deferred)
- `bash tests/run-all-200.sh` PASS=6 FAIL=0 (no regression across the whole phase)
</verification>

<success_criteria>
All three ROADMAP gate legs closed: run-all-211 green, the deployed Cross-Topic Connection judge wired (or honestly deferred) on synthetic-only text, and a human verdict on real-room eureka quality + fire-rate recorded - the empirical answer to SEED-049's two open MVP questions (small-embedding quality, fire-rate).
</success_criteria>

<output>
Create `.planning/phases/211-eureka-generator-mvp-tri-modal-room-db-sqlite-vec-xenova-all/211-05-SUMMARY.md` when done.
</output>
