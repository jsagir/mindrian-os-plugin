---
phase: 211-eureka-generator-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - lib/core/eureka/embedding-spine.cjs
  - tests/test-211-embedding-spine.cjs
autonomous: false
requirements: [R211-MEASURED-DIFF]
must_haves:
  truths:
    - "A CJS module embeds two texts locally (no Python, no GPU, no Brain) and returns 384-dim normalized vectors"
    - "When @huggingface/transformers is absent or the model cannot load, the module degrades with encoder_unavailable instead of throwing"
    - "Every embedding result carries provenance (model id, dtype, dim) so no score is ever a bare unattributed number"
  artifacts:
    - path: "lib/core/eureka/embedding-spine.cjs"
      provides: "getEncoder / embedTexts / encoderProvenance - the D-200-1 encoder swap target"
      exports: ["getEncoder", "embedTexts", "encoderProvenance", "cosineSimilarity", "_test"]
      min_lines: 80
    - path: "tests/test-211-embedding-spine.cjs"
      provides: "Offline deterministic contract tests (stub encoder, no network)"
  key_links:
    - from: "lib/core/eureka/embedding-spine.cjs"
      to: "lib/core/rs-pinecone-bridge.cjs"
      via: "cosineSimilarity reuse (Canon Part 7)"
      pattern: "require\\('.*rs-pinecone-bridge"
    - from: "package.json"
      to: "@huggingface/transformers"
      via: "dependencies entry"
      pattern: "huggingface/transformers"
---

<objective>
Land the D-200-1 encoder swap foundation: install the two validated packages (@huggingface/transformers, sqlite-vec) behind a package-legitimacy checkpoint, then build `lib/core/eureka/embedding-spine.cjs` - the ONE local encoder every Phase 211 consumer (tri-modal index, hybrid retrieve, measured differential) calls. Model per SEED-049 WebSearch validation 2026-07-04: `Xenova/all-MiniLM-L6-v2`, 384-dim, `dtype: q8`, with `Xenova/bge-small-en-v1.5` as the documented stronger alternative (SEED-049 D3).

Purpose: Phase 200 explicitly deferred the transformers.js encoder swap to "the embedding-spine decision D-200-1" (200-01-SUMMARY.md line 121). This plan closes that decision. Everything downstream in Phase 211 rides this module.
Output: installed deps, the embedding-spine module with graceful degradation, offline tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 211 section, line ~2886)
@.planning/seeds/SEED-049-mindrian-insight-engine-tri-modal-tri-source-hybrid-retrieval.md
@lib/core/rs-pinecone-bridge.cjs (cosineSimilarity to reuse)
@CLAUDE.md (CJS-only, no em-dashes, Part 8 Brain boundary, reuse-before-build)
</context>

## Interface contract (consumed by plans 211-02 and 211-03)

```
// lib/core/eureka/embedding-spine.cjs
getEncoder(opts) -> Promise<{success:true, encoder} | {success:false, error:'encoder_unavailable', detail}>
embedTexts(texts /* string[] */, opts) -> Promise<
    {success:true, vectors: number[][] /* 384-dim, L2-normalized */, provenance: {model, dtype, dim}}
  | {success:false, error:'encoder_unavailable'|'embed_failed', detail}>
  // opts.encodeFn (texts -> number[][]) injection bypasses the model entirely - the test seam
encoderProvenance() -> {model, dtype, dim, method:'transformers.js-feature-extraction'}
cosineSimilarity(a, b) -> number  // re-export of rs-pinecone-bridge.cosineSimilarity, Part 7 reuse
```

Env tunables (RS_SEMANTIC_FLOOR precedent from Phase 200): `MINDRIAN_EMBED_MODEL` (default `Xenova/all-MiniLM-L6-v2`), `MINDRIAN_EMBED_DTYPE` (default `q8`), `MINDRIAN_MODEL_CACHE` (optional; when set, assign transformers.js `env.cacheDir` before pipeline creation).

## Artifacts this phase produces

This plan: `lib/core/eureka/embedding-spine.cjs` exporting `getEncoder`, `embedTexts`, `encoderProvenance`, `cosineSimilarity`, `_test`; env vars `MINDRIAN_EMBED_MODEL`, `MINDRIAN_EMBED_DTYPE`, `MINDRIAN_MODEL_CACHE`; deps `@huggingface/transformers` + `sqlite-vec` in package.json; `tests/test-211-embedding-spine.cjs`.

Other plans (context): 211-02 `lib/core/eureka/tri-modal-index.cjs` (`openIndex`, `indexNodes`, `lexicalSearch`, `vectorSearch`) + `lib/core/eureka/hybrid-retrieve.cjs` (`rrfFuse`, `hybridRetrieve`, `rerank`) + `openRoomDb(roomDir, opts)` allowExtension passthrough. 211-03 `lib/core/eureka/lexical-overlap.cjs` (`lexicalOverlap`) + `rs-differential-scorer.cjs` `scoreMeasured`. 211-04 `evals/eureka/cases/*.md` (6 gold cards) + `evals/eureka/README.md` + `evals/eureka/211-manual-baseline.md`. 211-05 `scripts/eureka-room-report.cjs`, `tests/run-all-211.sh`, `tests/test-211-judge-gate.cjs`, `evals/plurai/211-baseline.json`, `evals/eureka/211-room-report.md`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification for @huggingface/transformers and sqlite-vec</name>
  <read_first>.planning/seeds/SEED-049-mindrian-insight-engine-tri-modal-tri-source-hybrid-retrieval.md (Verification log + Standalone research validation sections - the packages were runtime-verified but no Package Legitimacy Audit table exists, so both are [ASSUMED])</read_first>
  <what-built>Nothing yet - this checkpoint gates the first npm install of two new packages. SEED-049 research validated both technically (sqlite-vec production-safe at 100K vectors/384-dim under 100ms; transformers.js production-ready in plain Node) but did not run a package legitimacy audit, so per policy both are [ASSUMED] and require human verification before install.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@huggingface/transformers - confirm publisher is the huggingface org, weekly downloads are in the hundreds of thousands, and the repository link points to github.com/huggingface/transformers.js
    2. Open https://www.npmjs.com/package/sqlite-vec - confirm the author is Alex Garcia (asg017), the repository link points to github.com/asg017/sqlite-vec, and downloads are consistent with a widely used extension
    3. Confirm neither package name is a typosquat (exact names: `@huggingface/transformers`, `sqlite-vec`)
  </how-to-verify>
  <acceptance_criteria>Navigator explicitly approves both packages by name. This checkpoint is never auto-approvable (package legitimacy gate; workflow.auto_advance ignored).</acceptance_criteria>
  <resume-signal>Type "approved" to proceed with install, or name the package that failed verification</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install deps and build lib/core/eureka/embedding-spine.cjs</name>
  <files>package.json, package-lock.json, lib/core/eureka/embedding-spine.cjs, tests/test-211-embedding-spine.cjs</files>
  <read_first>lib/core/rs-pinecone-bridge.cjs (locate the exported cosineSimilarity to re-export - Part 7 reuse, do not reimplement), lib/core/rs-differential-scorer.cjs lines 119-126 (the RS_SEMANTIC_FLOOR env-resolution pattern to copy for the three new env vars), tests/test-200-corpus-quality.cjs (the deterministic stub-encoder offline test house pattern)</read_first>
  <behavior>
    - Test 1: embedTexts(['a','b'], {encodeFn: stub}) returns success:true with vectors from the stub and provenance.model === 'stub' - no model load attempted
    - Test 2: with MINDRIAN_EMBED_MODEL unset, encoderProvenance().model === 'Xenova/all-MiniLM-L6-v2' and dtype === 'q8'; with MINDRIAN_EMBED_MODEL=Xenova/bge-small-en-v1.5 set, provenance reflects it
    - Test 3: when require('@huggingface/transformers') throws (simulate via opts._forceUnavailable test hook), embedTexts returns {success:false, error:'encoder_unavailable'} and NEVER throws
    - Test 4: cosineSimilarity is reference-equal to require('./rs-pinecone-bridge.cjs').cosineSimilarity (no fork)
    - Test 5: vectors returned by the stub path are passed through unchanged (no re-normalization of injected vectors)
  </behavior>
  <action>
    Run `npm install @huggingface/transformers sqlite-vec` from the repo root (records both under dependencies; record the exact installed versions in the SUMMARY). Then create `lib/core/eureka/embedding-spine.cjs` implementing the interface contract above (SEED-049 D3 as sharpened by the 2026-07-04 WebSearch validation: MiniLM default, q8 dtype). Implementation requirements: 'use strict'; CJS only; LAZY require of @huggingface/transformers INSIDE getEncoder (never at module top - the module must load on machines where the dep install failed, per the Tier-0 graceful-degradation decision #8); singleton-cache the pipeline promise (one model load per process); call the pipeline with {pooling:'mean', normalize:true} so vectors are L2-normalized 384-dim; convert Tensor output to plain number[][]; when MINDRIAN_MODEL_CACHE is set, assign the transformers.js `env.cacheDir` before creating the pipeline. Copyright header matches the house BSL 1.1 block (see rs-differential-scorer.cjs lines 1-3). Part 8 note in the header: the model DOWNLOAD fetches generic public model weights by model id only - zero user bytes egress; embedding computation is fully local. Do not add any audit/egress call here (the scorer keeps its own Part 8 layers). Write tests/test-211-embedding-spine.cjs FIRST (RED), offline-only: no network, no model download, stub encodeFn injection + the _forceUnavailable hook; then implement (GREEN). Commit RED then GREEN per house TDD convention (`test(211-01): ...` / `feat(211-01): ...`).
  </action>
  <verify>
    <automated>node tests/test-211-embedding-spine.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-211-embedding-spine.cjs` exits 0 with all 5 behaviors asserted
    - `node -e "require('/home/jsagi/dev/MindrianOS-Plugin/lib/core/eureka/embedding-spine.cjs')"` exits 0 (module loads without the heavy dep being imported)
    - `grep -c "huggingface/transformers" package.json` returns 1 and `grep -c "sqlite-vec" package.json` returns 1
    - `grep -v "^\s*//" lib/core/eureka/embedding-spine.cjs | grep -c "require('@huggingface/transformers')"` returns >= 1 and the require sits inside a function body (lazy), verified by reading the file
    - No em-dash character anywhere in the new files
  </acceptance_criteria>
  <done>embedding-spine.cjs exists with the exact exported interface, offline tests green, both deps installed and recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Live encoder smoke (one real model load) and provenance record</name>
  <files>lib/core/eureka/embedding-spine.cjs</files>
  <read_first>lib/core/eureka/embedding-spine.cjs (just built - confirm the getEncoder path before exercising it live)</read_first>
  <action>
    Run one LIVE smoke of the real model path (network required once for the ~25MB q8 model download): `node -e` script that calls embedTexts on the SEED-049 canonical eureka pair ("circadian rhythm optimization in sleep science", "manufacturing shift scheduling around worker fatigue") plus one unrelated control ("tax form filing deadline"), prints the three pairwise cosines via the module's cosineSimilarity, and prints encoderProvenance(). Expected shape (directional, not exact): cosine(circadian, manufacturing) > cosine(circadian, tax) - the semantic leg detects the cross-domain meaning link the vocabulary hides. If the download fails (offline environment), record the failure honestly in the SUMMARY as a deferred smoke and do NOT fake numbers - the offline tests remain the gate. Record the measured cosines + model version + dtype in the plan SUMMARY (this is the first MEASURED semantic-leg number in the repo, the D-200-1 receipt).
  </action>
  <verify>
    <automated>node -e "const s=require('/home/jsagi/dev/MindrianOS-Plugin/lib/core/eureka/embedding-spine.cjs'); s.embedTexts(['circadian rhythm optimization in sleep science','manufacturing shift scheduling around worker fatigue','tax form filing deadline']).then(r=>{ if(!r.success){console.log('DEFERRED:',r.error);process.exit(0);} const c=s.cosineSimilarity; const ab=c(r.vectors[0],r.vectors[1]), ac=c(r.vectors[0],r.vectors[2]); console.log('bridge',ab.toFixed(3),'control',ac.toFixed(3),JSON.stringify(r.provenance)); process.exit(ab>ac?0:1); })"</automated>
  </verify>
  <acceptance_criteria>
    - The smoke command exits 0 (bridge cosine strictly greater than control cosine, OR an honest DEFERRED print when offline)
    - SUMMARY records the three cosines, the model id, dtype, and installed package versions
  </acceptance_criteria>
  <done>The real encoder ran at least once locally (or a deferred record exists), proving the no-Python semantic leg end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> node_modules | supply-chain install of two new packages |
| HuggingFace hub -> local model cache | one-time generic model weight download (no user bytes outbound) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-211-SC | Tampering | npm install of @huggingface/transformers + sqlite-vec | mitigate | Task 1 blocking human legitimacy checkpoint ([ASSUMED] - no audit table in SEED-049); exact-name install; versions recorded in SUMMARY |
| T-211-01 | Tampering | HF model download | mitigate | model ids pinned via env defaults; provenance (model, dtype) stamped on every result; cache dir local |
| T-211-02 | Information disclosure | embedTexts inputs | accept | computation is fully local; no network call carries text; Part 8 audit layers stay in the scorer (211-03) |
</threat_model>

<verification>
- `node tests/test-211-embedding-spine.cjs` exits 0 (offline, no model)
- Live smoke exits 0 or records an honest deferral
- `bash tests/run-all-200.sh` still PASS=6 FAIL=0 (no regression from the dependency install)
</verification>

<success_criteria>
The repo has a working, provenance-stamped, gracefully-degrading local encoder (D-200-1 closed), both new packages installed behind an approved legitimacy gate, and offline tests that never touch the network.
</success_criteria>

<output>
Create `.planning/phases/211-eureka-generator-mvp-tri-modal-room-db-sqlite-vec-xenova-all/211-01-SUMMARY.md` when done.
</output>
