---
phase: 211-eureka-generator-mvp
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - evals/eureka/README.md
  - evals/eureka/cases/archimedes-uq.md
  - evals/eureka/cases/archimedes-sterling.md
  - evals/eureka/cases/archimedes-darkmatter.md
  - evals/eureka/cases/davinci-salient.md
  - evals/eureka/cases/nichefoods-null.md
  - evals/eureka/cases/lovelace-lean.md
  - evals/eureka/211-manual-baseline.md
  - tests/test-211-case-cards.cjs
autonomous: false
requirements: [R211-GOLDSET]
must_haves:
  truths:
    - "Six gold case cards exist, schema-valid, pseudonymous (no real names), each with a human-validated destination and human_baseline_effort"
    - "The COMPRESSION formula (Score = CompressionDelta x GuardGate x StatusQuoGate, Lured negative) is documented where the cards live"
    - "Larry has been run manually on the cases and hand-scored - the first COMPRESSION baseline exists on disk"
    - "The niche-foods null-control encodes arrival-WITHOUT-compression scoring ~0, so the judge can never conflate confirmation with compression"
  artifacts:
    - path: "evals/eureka/README.md"
      provides: "Card schema + COMPRESSION formula + two-gate validation rule"
      min_lines: 40
    - path: "evals/eureka/cases/archimedes-darkmatter.md"
      provides: "The Type-3 find-analogies gold with restatement distractor"
      contains: "rare-signal-in-vast-background"
    - path: "evals/eureka/211-manual-baseline.md"
      provides: "Hand-scored first baseline (filled at the checkpoint)"
    - path: "tests/test-211-case-cards.cjs"
      provides: "Schema validation via gray-matter"
  key_links:
    - from: "tests/test-211-case-cards.cjs"
      to: "evals/eureka/cases/"
      via: "gray-matter frontmatter parse of all 6 cards"
      pattern: "gray-matter"
---

<objective>
Front-load SEED-050's critic gold-set exactly as the ROADMAP goal orders: write the 6 case cards (archimedes-uq, archimedes-sterling, archimedes-darkmatter Type-3 gold, davinci-salient, niche-foods null-control, 1 math case), run Larry manually on all, hand-score with the COMPRESSION formula as the first baseline. This gives Phase 211 its acceptance yardstick (does the measured engine beat the model-judgment baseline) and Phase 212's Grounding Guard its gold labels.

Purpose: generator (049) + critic (050) = the two-in-a-box; neither ships trustworthy alone. The cards are the critic's first ground truth.
Output: evals/eureka/ directory with 6 schema-valid cards, README (schema + formula + two-gate rule), schema test, and the hand-scored manual baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/seeds/SEED-050-eureka-eval-salient-verifier-judge-synthetic-trust.md
@.planning/research/2026-07-02-eureka-eval-real-user-corpus-and-synthesis.md (sections 3, 5, 10, 11 - the card contents live here)
@CLAUDE.md (no real names in repo - HARD RULE; no em-dashes)
</context>

## Naming and content sources (load-bearing)

- `archimedes-uq`: research file section 5 yaml (clean positive; gold_label arrival Full / salient transferable / compressed yes).
- `archimedes-sterling`: the Lean-checkable forced_context control. Its CONTENT is the research file's `archimedes-formal` yaml (section 5, lines ~137-145); the CANONICAL NAME is `archimedes-sterling` per SEED-050 and the ROADMAP goal. Note the rename in the card body ("content source: archimedes-formal in the research file").
- `archimedes-darkmatter`: section 10's yaml card verbatim (Type-3 find-analogies gold), PLUS one added distractor from section 11 Finding 2: `restatement: "the ppb-challenge paraphrase - same problem restated in swapped vocabulary; out-scores the real bridge on raw differential yet is not one"` with gold label `salient: restatement`. Preserve the note: seed find-analogies on the ABSTRACTED PATTERN (a part), never the whole challenge doc.
- `davinci-salient`: section 5 yaml (validated: candidate - Gate B human validation required, critic_available none).
- `nichefoods-null`: the $12M->$30M session as `posture: solve` NULL-CONTROL - arrived at the team's answer after two years of prior thought = arrival WITHOUT compression = score ~0. "niche-foods" is already the pseudonym; no company name, no person name.
- `lovelace-lean`: the 6th card (the "1 math case"), AUTHORED here using the `lovelace` persona name reserved by the research file for formal/computational cases. Requirements: `posture: solve` or `hypothesis-based-innovation`, `critic_available: lean_checkable` (Gate A objective calibration), a destination checkable by a proof assistant (pick a small real formalization insight, e.g. an informal induction argument whose destination is its rigorous strong-induction restatement), `human_baseline_effort` estimated honestly, distractors including one seductive-but-wrong formalization. Mark `validated: candidate` until the navigator confirms at the checkpoint.

## Card schema (frozen in README.md)

Markdown file, YAML frontmatter parsed by gray-matter (house stack), prose body = the scenario script used to run Larry manually. Frontmatter required keys: `case` (matches filename), `persona`, `posture`, `hypothesis_in`, `destination`, `human_baseline_effort`, `distractors` (list of {label: text}), `dials` ({stamina, status_quo_pressure, retrieval_gap, critic_available}), `gold_label`, `validated` (true | candidate), optional `lens`.

## Artifacts this phase produces

This plan: `evals/eureka/README.md` (schema + COMPRESSION formula + two-gate rule); 6 case cards under `evals/eureka/cases/`; `evals/eureka/211-manual-baseline.md` (hand-scored baseline); `tests/test-211-case-cards.cjs`.

Other plans (context): 211-01 embedding-spine; 211-02 tri-modal index + hybrid retrieve; 211-03 scoreMeasured + lexicalOverlap; 211-05 room report + run-all-211.sh + judge gate (the judge gate scores THESE cards' text pairs - pseudonymous content is the only text that ever reaches the Plurai endpoint).

<tasks>

<task type="auto">
  <name>Task 1: Write evals/eureka/README.md + the 6 case cards</name>
  <files>evals/eureka/README.md, evals/eureka/cases/archimedes-uq.md, evals/eureka/cases/archimedes-sterling.md, evals/eureka/cases/archimedes-darkmatter.md, evals/eureka/cases/davinci-salient.md, evals/eureka/cases/nichefoods-null.md, evals/eureka/cases/lovelace-lean.md</files>
  <read_first>.planning/research/2026-07-02-eureka-eval-real-user-corpus-and-synthesis.md sections 3, 5, 10, 11 (card yamls + persona dials + the null-control rationale + the restatement trap), .planning/seeds/SEED-050-eureka-eval-salient-verifier-judge-synthetic-trust.md (the judge design + COMPRESSION composite + two-gate rule + no-real-names rule)</read_first>
  <action>
    Create `evals/eureka/README.md`: the card schema (exact frontmatter keys above), the COMPRESSION formula verbatim from SEED-050 - `Score = CompressionDelta(hypothesis_in -> destination) x GuardGate x StatusQoGate` is WRONG spelling, write it correctly as `Score = CompressionDelta(hypothesis_in -> destination) x GuardGate x StatusQuoGate`, GuardGate/StatusQuoGate = 0 if any scoring-path turn is pseudoscience/status_quo_stuck, Lured scores NEGATIVE, arrival without compression scores ~0 - plus the hand-scoring rubric (CompressionDelta = human_baseline_effort vs observed turns-to-destination, scored 0..1), the two-gate validation rule (Gate A lean_checkable objective / Gate B human confirms destination + distractors + label; only validated:true enters any upload_data batch), the salient-verifier label set (transferable / general_shallow / pseudoscience / restatement - restatement is the #1 false positive per the s11 paraphrase trap), and the no-real-names rule restated. Then write the 6 cards per the "Naming and content sources" section of this plan - transcribe the research-file yamls faithfully into frontmatter, author the lovelace-lean card fresh, and give every card a short prose body: the scenario script a human follows to run Larry on it (opening message as the persona, what the persona pushes back on per its dials, when to stop). Every card body ends with a "Scoring notes" line naming its gold labels. No real names anywhere; no em-dashes anywhere.
  </action>
  <verify>
    <automated>ls evals/eureka/cases/ | wc -l | grep -q 6 && grep -L "human_baseline_effort" evals/eureka/cases/*.md | wc -l | grep -q 0</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 6 files in evals/eureka/cases/ with the exact filenames listed in files_modified
    - `grep -c "rare-signal-in-vast-background" evals/eureka/cases/archimedes-darkmatter.md` returns >= 1 and the card carries a restatement distractor (`grep -c "restatement" evals/eureka/cases/archimedes-darkmatter.md` >= 1)
    - `grep -c "StatusQuoGate" evals/eureka/README.md` returns >= 1 and `grep -c "two-gate\|Gate A\|Gate B" evals/eureka/README.md` returns >= 2
    - `grep -rn "sterling" evals/eureka/cases/archimedes-sterling.md | head -1` shows the canonical name and the body notes the archimedes-formal content source
    - nichefoods-null.md frontmatter has `posture: solve` and gold label encoding compressed: no (arrival without compression)
    - No em-dash character in any file under evals/eureka/
  </acceptance_criteria>
  <done>Six pseudonymous, schema-complete gold cards + the documented formula and gates exist on disk.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test-211-case-cards.cjs schema validation</name>
  <files>tests/test-211-case-cards.cjs</files>
  <read_first>evals/eureka/README.md (the frozen schema just written), one card file to confirm frontmatter shape, package.json (gray-matter is already a dependency - reuse, do not add a YAML parser)</read_first>
  <behavior>
    - Test 1: all 6 expected filenames exist and gray-matter parses each without error
    - Test 2: every card has all required frontmatter keys (case, persona, posture, hypothesis_in, destination, human_baseline_effort, distractors, dials, gold_label, validated) and `case` matches its filename stem
    - Test 3: exactly one card has posture 'solve' with a null-control gold label (nichefoods) and exactly two cards have critic_available 'lean_checkable' (sterling + lovelace)
    - Test 4: no card file content matches a frozen deny-list of real-name tokens (assert the no-real-names rule mechanically; source the deny-list as sha256 hashes in the test so the names themselves never enter the repo - compare sha256(lowercased token) per word against the hash list)
  </behavior>
  <action>
    Create tests/test-211-case-cards.cjs: pure node built-ins + require('gray-matter'). Read every evals/eureka/cases/*.md, assert Tests 1-4. For Test 4, hash-compare per-token so the test file itself never contains a real name (the navigator supplies zero hashes initially - ship the mechanism with an empty list and a comment telling the navigator how to add hashes; the mechanical rule still catches future regressions once seeded). Exit non-zero on any failure with a named assertion message.
  </action>
  <verify>
    <automated>node tests/test-211-case-cards.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-211-case-cards.cjs` exits 0 with all assertions named in output
    - The test fails loudly (verified once by temporarily renaming a required key in a scratch copy, not the real card) - document the RED proof in the SUMMARY
  </acceptance_criteria>
  <done>Card schema drift is mechanically caught from this commit forward.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run Larry manually on all 6 cards + hand-score the first COMPRESSION baseline</name>
  <read_first>evals/eureka/README.md (the scoring rubric), all 6 card bodies (the scenario scripts)</read_first>
  <action>Present the run-and-score instructions below to the navigator and PAUSE. On resume, write evals/eureka/211-manual-baseline.md from the pasted per-card results (or record an honest deferral) and flip validated flags exactly as the acceptance criteria specify.</action>
  <what-built>Six gold case cards with scenario scripts and a documented COMPRESSION formula + rubric. Claude cannot do this step by design: the ROADMAP goal orders "run Larry manually on all, hand-score with the COMPRESSION formula as the first baseline" - the hand-scores ARE the human gold labels the critic will later be validated against, so a model self-scoring here would contaminate the baseline (the same reason SEED-050 forbids synthesizing upload_data records).</what-built>
  <how-to-verify>
    1. For each of the 6 cards, open a fresh Larry session and play the persona per the card's scenario script (dials included)
    2. Record per card: turns-to-destination (or Missed/Lured), any pseudoscience or status_quo_stuck turn, and the arrival grade (Full / Partial / Missed / Lured)
    3. Hand-score each with the README rubric: Score = CompressionDelta x GuardGate x StatusQuoGate
    4. Expected shape: archimedes-uq scores positive; nichefoods-null scores ~0 (arrival without compression); any Lured outcome scores negative
    5. Dictate or paste the per-card results back into this session
  </how-to-verify>
  <acceptance_criteria>After resume, the executor writes `evals/eureka/211-manual-baseline.md`: a table (case, arrival, guard events, status-quo events, turns observed, human_baseline_effort, CompressionDelta, final Score) + a dated provenance line ("hand-scored by the navigator, first baseline, N=6 - a DEMO not a validation set per SEED-050"). Cards the navigator confirmed flip `validated: candidate` to `validated: true`; unconfirmed ones stay candidate. `node tests/test-211-case-cards.cjs` still exits 0 after the flips.</acceptance_criteria>
  <resume-signal>Paste the per-card scores (or "defer baseline" to ship cards now and score in a follow-up session - the deferral is then recorded honestly in 211-manual-baseline.md and the phase SUMMARY)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real-user transcripts -> tracked repo | identity leakage risk in card content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-211-09 | Information disclosure | case cards in the tracked repo | mitigate | pseudonymous personas only (no-real-names HARD RULE); Test 4 hash-based deny-list mechanism; role descriptors not names |
| T-211-10 | Spoofing | model-generated baseline masquerading as human gold | mitigate | Task 3 is a blocking human checkpoint; the baseline file records who scored and when; deferral recorded honestly rather than auto-filled |
</threat_model>

<verification>
- `node tests/test-211-case-cards.cjs` exits 0
- 211-manual-baseline.md exists with either 6 scored rows or an honest deferral record
</verification>

<success_criteria>
SEED-050's smallest-experiment steps 2-3 are done: six schema-valid pseudonymous gold cards, the COMPRESSION formula documented, and a human-scored (or honestly deferred) first baseline that Phase 212's Grounding Guard and Phase 202's APO can calibrate against.
</success_criteria>

<output>
Create `.planning/phases/211-eureka-generator-mvp-tri-modal-room-db-sqlite-vec-xenova-all/211-04-SUMMARY.md` when done.
</output>
