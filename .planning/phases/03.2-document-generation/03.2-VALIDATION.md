---
phase: 3.2
slug: document-generation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-22
---

# Phase 3.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | bash + python3 -c (script validation + PDF generation test) |
| **Config file** | none — file existence, content verification, Python import checks |
| **Quick run command** | `bash -c 'test -f scripts/render-pdf && test -f templates/destijl-base.css && test -f commands/export.md && echo PASS'` |
| **Full suite command** | `bash -c 'cd /home/jsagi/MindrianOS-Plugin && test -x scripts/render-pdf && ls templates/*.html 2>/dev/null | wc -l && python3 -c "import weasyprint, markdown2, jinja2, yaml; print(\"DEPS_OK\")" && echo ALL_PASS'` |
| **Estimated runtime** | ~2 seconds |

---

## Sampling Rate

- **After every task commit:** Run quick run command
- **After every plan wave:** Run full suite command
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 3 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 03.2-01-01 | 01 | 1 | DOCS-02 | file+content | `test -f templates/destijl-base.css && grep -q "#0D0D0D" templates/destijl-base.css && grep -q "Bebas" templates/destijl-base.css` | ❌ W0 | ⬜ pending |
| 03.2-01-02 | 01 | 1 | DOCS-02, DOCS-03 | file+content | `ls templates/*.html | wc -l` (expect >= 4) | ❌ W0 | ⬜ pending |
| 03.2-02-01 | 02 | 2 | DOCS-01, DOCS-04 | file+exec | `test -x scripts/render-pdf && grep -q "weasyprint" scripts/render-pdf` | ❌ W0 | ⬜ pending |
| 03.2-02-02 | 02 | 2 | DOCS-01 | file+content | `test -f commands/export.md && grep -q "thesis" commands/export.md` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements — WeasyPrint, markdown2, Jinja2, PyMuPDF, PyYAML all installed. Only font files (~700KB Bebas Neue + Inter) need bundling.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| PDF renders with correct De Stijl styling | DOCS-02 | Visual rendering check | Run `/mindrian-os:export thesis`, open PDF, verify dark bg, fonts, colors |
| Running headers/footers and page numbers | DOCS-04 | Visual check in multi-page PDF | Generate thesis PDF, verify page numbers and headers on pages 2+ |
| PWS Profile populates from methodology outputs | DOCS-05 | Requires room with methodology artifacts | Run Bono + JTBD first, then export profile, verify data populated |
| PDF opens automatically after generation | DOCS-01 | Requires OS interaction | Run export, verify PDF viewer launches |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 3s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
