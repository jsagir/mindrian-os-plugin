---
phase: 296-seed-030-rs-pipeline-spine-wiring-expert-graph-reconciliatio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/run-all-296.sh
  - tests/fixtures/296/room-fixture.cjs
  - tests/296-no-pinecone-internal.test.cjs
  - tests/296-dim-invariant.sh
autonomous: true
requirements: [RSFENCE-01, RSLOCAL-04]
canon_parts: [7, 8, 9]

must_haves:
  truths:
    - "A phase-296 aggregator discovers every tests/296-*.test.cjs and tests/296-*.sh by glob and hard-fails on zero discovery"
    - "A shared room.db fixture helper can build a temp room and seed its vector store on either backend"
    - "The already-true finding F-3 (rs-engine.py internal and cross-room modes call zero Pinecone) is locked by an executable test, not by prose"
    - "The 384-dim / 1024-dim non-mixing invariant has a source gate that SKIPs honestly before the repoint lands and asserts after it"
  artifacts:
    - path: "tests/run-all-296.sh"
      provides: "Glob-discovery aggregator with a found-eq-0 guard, a Part 8 source sweep and a no-em-dash fence"
      min_lines: 110
      contains: "TEST_296_PREFIX"
    - path: "tests/fixtures/296/room-fixture.cjs"
      provides: "makeRoom / seedVectors / cleanup helpers over openRoomDb + vector-store"
      min_lines: 55
      contains: "allowExtension"
    - path: "tests/296-no-pinecone-internal.test.cjs"
      provides: "F-3 regression fence: internal and cross-room modes reach no rs_cache and no PINECONE_API_KEY"
      min_lines: 70
      contains: "run_mode_cross_room"
    - path: "tests/296-dim-invariant.sh"
      provides: "RSLOCAL-04 source gate, SKIP-tolerant until the repoint lands"
      min_lines: 55
      contains: "1024"
  key_links:
    - from: "tests/fixtures/296/room-fixture.cjs"
      to: "lib/core/eureka/vector-store.cjs"
      via: "require of ensureStore and insertVector against a caller-owned db handle"
      pattern: "vector-store"
    - from: "tests/296-no-pinecone-internal.test.cjs"
      to: "scripts/rs-engine.py"
      via: "source slice of run_mode_internal and run_mode_cross_room function bodies"
      pattern: "run_mode_internal"
---

<objective>
Build this phase's test spine: the glob-discovery aggregator, the shared room.db fixture helper
every later plan reuses, and the two fences that lock findings which are ALREADY TRUE today.

This matters because 296-RESEARCH.md's single highest-risk finding (Pitfall 1) is a verification
hazard, not a code bug: `sqlite-vec` is a hard `package.json` dependency installed on every real
user machine but absent from this checkout, so a test written casually goes green here and is
broken for every user. The fixture helper built in Task 2 is what makes the both-backends proof
in plan 296-03 possible at all.

The second reason: F-3 and F-9 found that two of SEED-030's acceptance items are already
satisfied. Prose saying so rots. An executable fence does not.

Purpose: give plans 296-02 through 296-06 a runner they never have to edit and a room fixture they
never have to re-derive.
Output: `tests/run-all-296.sh`, `tests/fixtures/296/room-fixture.cjs`,
`tests/296-no-pinecone-internal.test.cjs`, `tests/296-dim-invariant.sh`.

Unlike Phase 272's Wave 0, this plan is GREEN BY DESIGN. Every assertion it writes is about a
fact that holds today. The RED-until-implemented arms belong to plans 296-03 and 296-04 and land
with their implementations, per this phase's quality gate ("write the test files as part of the
relevant task, don't defer them").
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/296-seed-030-rs-pipeline-spine-wiring-expert-graph-reconciliatio/296-VALIDATION.md

Do NOT read `.planning/STATE.md` in full. It is 1.4MB and will consume the whole context window.
Grep it for a specific literal if you need it.

Do NOT read `296-RESEARCH.md` in full (1082 lines). Read only the sections named in each task's
`read_first`.
</context>

<artifacts_this_phase_produces>

This plan (296-01) produces:

| Artifact | Kind | Path |
|---|---|---|
| `tests/run-all-296.sh` | new test aggregator (glob discovery) | `tests/` |
| `TEST_296_PREFIX` | new env-var override proving the found-eq-0 guard | `tests/run-all-296.sh` |
| `TEST_296_ALLOW_MISSING` | new env-var escape for the no-em-dash fence | `tests/run-all-296.sh` |
| `tests/fixtures/296/room-fixture.cjs` | new test fixture module | `tests/fixtures/296/` |
| `makeRoom(prefix)` | new exported fixture function | `tests/fixtures/296/room-fixture.cjs` |
| `seedVectors(db, dim, pairs)` | new exported fixture function | `tests/fixtures/296/room-fixture.cjs` |
| `cleanup(handle)` | new exported fixture function | `tests/fixtures/296/room-fixture.cjs` |
| `tests/296-no-pinecone-internal.test.cjs` | new test file | `tests/` |
| `tests/296-dim-invariant.sh` | new test file (SKIP-tolerant gate) | `tests/` |

Symbols produced by LATER plans in this phase, listed once so the whole phase surface is visible
in one place. A plan-review source-grounding pass must treat every row below as newly created by
this phase, not as drift against an existing symbol:

| Symbol | Kind | Home | Plan |
|---|---|---|---|
| `resolveExpertTier(topic, opts)` | new exported function | `scripts/rs-experts-command.cjs` | 296-02 |
| `AURA_TRANSPORT_ABSENT` | new `refusal_code` string value | `scripts/rs-experts-command.cjs` | 296-02 |
| `tests/296-rs-experts-degrade.test.cjs` | new test file | `tests/` | 296-02 |
| `scripts/rs-vector-bridge.cjs` | new CJS stdio bridge entry point | `scripts/` | 296-03 |
| `embed` / `knn` / `meta` | new argv ops on the bridge router | `scripts/rs-vector-bridge.cjs` | 296-03 |
| `tests/296-vector-read-both-backends.test.cjs` | new test file | `tests/` | 296-03 |
| `cache_dir(room_dir, topic)` | new exported Python function | `lib/core/rs_cache.py` | 296-04 |
| `_embed_via_bridge(texts)` | new module-private Python function | `lib/core/rs_cache.py` | 296-04 |
| `_resolve_room(room_dir)` | new module-private Python function | `lib/core/rs_cache.py` | 296-04 |
| `CACHE_DIRNAME` / `VECTORS_FILENAME` / `MANIFEST_FILENAME` / `SCHEMA_VERSION` / `BRIDGE_TIMEOUT_SEC` / `EMBED_BATCH` | new module constants | `lib/core/rs_cache.py` | 296-04 |
| `MINDRIAN_RS_ROOM` | new env var (room scope for the signal cache) | `lib/core/rs_cache.py` | 296-04 |
| `MINDRIAN_RS_BRIDGE` | new env var (bridge script path test seam) | `lib/core/rs_cache.py` | 296-04 |
| `MINDRIAN_NODE` | new env var (node binary override) | `lib/core/rs_cache.py` | 296-04 |
| `<room>/research/<slug>/.rs-signal-cache/vectors.jsonl` | new on-disk artifact | room sidecar | 296-04 |
| `<room>/research/<slug>/.rs-signal-cache/manifest.json` | new on-disk artifact | room sidecar | 296-04 |
| `tests/296-signal-corpus-local.test.cjs` | new test file | `tests/` | 296-04 |
| `tests/fixtures/296/stub-embed-bridge.cjs` | new test fixture (deterministic embedder) | `tests/fixtures/296/` | 296-04 |
| `_signal_cache_available()` | renamed from `_pinecone_path_available()` | `scripts/rs-engine.py` | 296-05 |
| `_gate_records_cached()` | renamed from `_gate_records_pinecone()` | `scripts/rs-engine.py` | 296-05 |
| `_cached_values` | renamed artifact key from `_pinecone_values` | `scripts/rs-engine.py` | 296-05 |
| `cache_id` | renamed corpus-dict key from `pinecone_id` | `lib/core/rs_hybrid.py` | 296-05 |
| `roomDir` 4th parameter | new parameter on `queryPineconeWithVectors` | `lib/core/rs-pinecone-bridge.cjs` | 296-05 |
| `roomDir` 4th parameter | new parameter on `computeBertCosine` | `lib/core/rs-differential-scorer.cjs` | 296-05 |
| `tests/296-blast-radius.test.cjs` | new test file | `tests/` | 296-05 |
| `tests/296-pinecone-residue.sh` | new test file | `tests/` | 296-06 |

</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: tests/run-all-296.sh, the glob-discovery aggregator</name>

  <files>tests/run-all-296.sh</files>

  <read_first>
    - `tests/run-all-272.sh` IN FULL. This is the nearest precedent (same subsystem, same phase family). Copy verbatim: `set -uo pipefail` plus the `ROOT=`/`cd "$ROOT"` preamble, the `run()` and `run_may_skip()` helpers, the `shopt -s nullglob` discovery loop, the `found -eq 0` guard, the PART8 sweep loop shape, the em-dash fence loop shape, and the `PASS=$PASS FAIL=$FAIL SKIP=$SKIP` tail followed by `[ "$FAIL" -eq 0 ]`.
    - `296-VALIDATION.md` sections "Test Infrastructure", "Sampling Rate" and "Wave 0 Requirements" for the six file names this runner must eventually discover.
    - `296-RESEARCH.md` section "Common Pitfalls" (Pitfall 1 and Pitfall 4 only) so the header block states what a green run does and does not prove.
  </read_first>

  <action>
    Create `tests/run-all-296.sh`, executable via `chmod +x`, modeled on `tests/run-all-272.sh`.

    Discovery prefix differs from 272 in ONE way that must not be copied wrong. Set
    `PREFIX="${TEST_296_PREFIX:-tests/296-}"` and glob `"$PREFIX"*.test.cjs` for the node arm and
    `"$PREFIX"*.sh` for the bash arm. The `.test.cjs` suffix scoping is deliberate and load-bearing:
    this phase puts shared helpers under `tests/fixtures/296/`, and a bare `"$PREFIX"*.cjs` glob
    would try to execute a helper as a test. Skip the runner itself by basename comparison in the
    `.sh` loop, exactly as 272 does.

    Run node files through `run` and bash files through `run_may_skip`, so a gate that prints a
    leading `SKIP` line counts as SKIPPED instead of PASSED. `tests/296-dim-invariant.sh` depends
    on that behavior.

    Found-eq-0 guard: if the discovered count is 0, print a failure line naming the prefix and
    exit non-zero. Add a comment stating that a harness printing green over zero discovery is the
    false-success class this repo has already paid for, and that `TEST_296_PREFIX` exists purely so
    an operator can prove the guard without editing this file.

    Header block, one sentence per requirement: RSFENCE-01, RSLOCAL-01, RSLOCAL-02, RSLOCAL-03,
    RSLOCAL-04, RSEXP-01, RSEXP-02, each naming the plan that owns it (296-01 through 296-06).
    State explicitly that this phase's own scope is the `rs_cache.py` external/hybrid signal corpus
    and the `rs-experts` degrade, that internal and cross-room modes were ALREADY Pinecone-free
    before this phase (296-RESEARCH.md F-3), and that `lib/core/pinecone-inference.cjs`,
    `scripts/compute-hsi.py` Tier 2 and `PINECONE_API_KEY` itself are deliberately OUT of scope and
    must NOT be swept for removal.

    Part 8 source sweep: `PART8_TARGETS` is `scripts/rs-vector-bridge.cjs`, `lib/core/rs_cache.py`,
    `scripts/rs-experts-command.cjs`. `PART8_FORBIDDEN` is the extended regex
    `brain-client|brainClient|brain_query|pws-brain|api\.pinecone\.io|import pinecone|from pinecone`.
    Comment-strip before counting so a header sentence naming a forbidden token cannot fail the
    file it documents: pipe through `grep -v '^[[:space:]]*\(//\|\*\|/\*\|#\)'` and only then
    `grep -Ec`. A target that does not exist yet counts into `PART8_MISSING` and does NOT fail the
    leg (these files land in waves 2 and 3), matching 272's deliberate asymmetry.

    No-em-dash fence: `LC_ALL=C.UTF-8 grep -lP '\x{2014}'` over `PART8_TARGETS` plus this runner
    plus every discovered test file. Treat a `grep -P` return code of 2 or higher as a broken scan
    and fail. A missing target counts into `EMDASH_MISSING` and fails unless
    `TEST_296_ALLOW_MISSING=1`. Never write a literal em-dash glyph into this file; express the
    fence only as the `\x{2014}` escape.

    Tail: print `Phase 296: PASS=$PASS FAIL=$FAIL SKIP=$SKIP` between two separator lines and exit
    `[ "$FAIL" -eq 0 ]`.
  </action>

  <verify>
    <automated>bash -n tests/run-all-296.sh && TEST_296_PREFIX=tests/296-nonexistent-prefix- bash tests/run-all-296.sh; test $? -ne 0</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n tests/run-all-296.sh` exits 0.
    - `TEST_296_PREFIX=tests/296-nonexistent-prefix- bash tests/run-all-296.sh` exits non-zero and its output contains a zero-discovery failure line naming the prefix.
    - `grep -c 'TEST_296_PREFIX' tests/run-all-296.sh` is at least 2.
    - `grep -c '\*\.test\.cjs' tests/run-all-296.sh` is at least 1 (proves the suffix-scoped glob, not a bare `*.cjs`).
    - `grep -c 'run_may_skip' tests/run-all-296.sh` is at least 2.
    - `grep -c 'pinecone-inference' tests/run-all-296.sh` is at least 1 (the out-of-scope statement is present).
    - `LC_ALL=C.UTF-8 grep -P '\x{2014}' tests/run-all-296.sh` returns no match (exit 1).
    - `test -x tests/run-all-296.sh` succeeds.
  </acceptance_criteria>

  <done>The aggregator exists, is executable, parses, discovers by glob with a `.test.cjs`-scoped node arm, and its found-eq-0 guard is proven by the prefix override rather than asserted in a comment.</done>
</task>

<task type="auto">
  <name>Task 2: tests/fixtures/296/room-fixture.cjs, the shared both-backends room helper</name>

  <files>tests/fixtures/296/room-fixture.cjs</files>

  <read_first>
    - `lib/memory/selector-miss.test.cjs` lines 1-60. Its `freshDb()` helper (`fs.mkdtempSync(path.join(os.tmpdir(), '<prefix>'))` then `openRoomDb(dir)`) is the exact idiom to lift; `296-VALIDATION.md`'s Wave 0 list names it as the precedent by name.
    - `lib/core/eureka/vector-store.cjs` lines 1-60 (the header contract) and lines 90-130 (`createVecTable` and the `MINDRIAN_FORCE_NO_VEC0` probe seam) and its `Exports:` block. The caller-owned-handle rule and the `allowExtension: true` requirement for the vec0 leg are both stated there.
    - `lib/core/room-db.cjs` around the `openRoomDb` and `closeRoomDb` definitions, for the exact signature and the `allowExtension` option name.
    - `296-RESEARCH.md` finding F-2 (the two vector tables and the dev/prod divergence) and Architecture Pattern 3 (process-latched capability probe with a forced test seam).
  </read_first>

  <action>
    Create `tests/fixtures/296/room-fixture.cjs`. It lives under `tests/fixtures/296/`, NOT under
    `tests/`, so the Task 1 aggregator glob can never execute it as a test. Header comment must say
    exactly that.

    Export three functions:

    `makeRoom(prefix)` returns `{ dir, db }`. It builds `dir` with
    `fs.mkdtempSync(path.join(os.tmpdir(), prefix))` and opens the handle with
    `openRoomDb(dir, { allowExtension: true })` from `lib/core/room-db.cjs`. `allowExtension: true`
    is required, not optional: on a plain handle `vector-store.cjs` silently degrades to the CJS
    cosine fallback, which would make a both-backends test pass while only ever exercising one
    backend. Say that in a comment.

    `seedVectors(db, dim, pairs)` where `pairs` is an array of `{ id, vec }`. It calls
    `ensureStore(db, dim)` from `lib/core/eureka/vector-store.cjs`, captures the returned
    `{ backend }`, then calls `insertVector(db, id, vec)` for every pair. It returns the resolved
    backend string (`'sqlite-vec'` or `'cjs-fallback'`) so a caller can assert WHICH backend it
    actually exercised instead of assuming.

    `cleanup(handle)` calls `closeRoomDb(handle.db)` inside a try/catch and then
    `fs.rmSync(handle.dir, { recursive: true, force: true })`. Never throw from cleanup.

    Also export `_test` carrying `FORCE_NO_VEC0_ENV: 'MINDRIAN_FORCE_NO_VEC0'` so consuming tests
    name the seam through the fixture rather than hard-coding the string in three places.

    Never call `new DatabaseSync` directly and never require `sqlite-vec`. `openRoomDb` owns WAL,
    `synchronous`, the `timeout: 5000` constructor option and the migration chain; `ensureStore`
    owns backend selection. This fixture only composes them.
  </action>

  <verify>
    <automated>node -e "const f=require('./tests/fixtures/296/room-fixture.cjs');const h=f.makeRoom('p296-fix-');const b=f.seedVectors(h.db,4,[{id:'a',vec:[0.1,-0.25,0.5,0.75]},{id:'b',vec:[1,0,0,0]}]);if(!['sqlite-vec','cjs-fallback'].includes(b))throw new Error('bad backend: '+b);f.cleanup(h);console.log('backend='+b)"</automated>
  </verify>

  <acceptance_criteria>
    - The verify command exits 0 and prints a line matching `backend=(sqlite-vec|cjs-fallback)`.
    - `node -e "const f=require('./tests/fixtures/296/room-fixture.cjs');['makeRoom','seedVectors','cleanup','_test'].forEach(k=>{if(!(k in f))throw new Error('missing '+k)})"` exits 0.
    - `grep -c 'allowExtension' tests/fixtures/296/room-fixture.cjs` is at least 1.
    - `grep -c 'DatabaseSync' tests/fixtures/296/room-fixture.cjs` equals 0.
    - `grep -c "require('sqlite-vec')" tests/fixtures/296/room-fixture.cjs` equals 0.
    - Running the verify command twice in a row leaves no `/tmp/p296-fix-*` directory behind (`ls -d /tmp/p296-fix-* 2>/dev/null | wc -l` is 0).
    - `LC_ALL=C.UTF-8 grep -P '\x{2014}' tests/fixtures/296/room-fixture.cjs` returns no match.
  </acceptance_criteria>

  <done>A temp room can be created, seeded on whichever vector backend this machine resolves, and torn down cleanly, with the resolved backend name returned to the caller rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 3: the two already-true fences, tests/296-no-pinecone-internal.test.cjs and tests/296-dim-invariant.sh</name>

  <files>tests/296-no-pinecone-internal.test.cjs, tests/296-dim-invariant.sh</files>

  <read_first>
    - `scripts/rs-engine.py` lines 110-130 (the guarded `from lib.core.rs_cache import ...` block and the `_RS_CACHE_AVAILABLE` flag), lines 268-300 (`compute_embeddings` and the `_embed_via_pinecone_inference` `NotImplementedError` stub), lines 975-1006 (`_pinecone_path_available` and `_build_sem_matrix_from_records`). Locate `def run_mode_internal(` and `def run_mode_cross_room(` and read each function body in full.
    - `lib/core/rs-engine.cjs` lines 1-60 (the header, including the documented 384-vs-1024 dimensional-invariant warning at lines 20-35 and the "ONLY local encoder call site" rule at lines 31-35).
    - `296-RESEARCH.md` finding F-3 in full, and the "Anti-Patterns to Avoid" list (the 384/1024 mixing entry).
  </read_first>

  <action>
    Create `tests/296-no-pinecone-internal.test.cjs` using `node:test` and `node:assert/strict`,
    pure source-assertion, no subprocess, no ML model load.

    Add a `sliceFunction(source, defLine)` helper that returns the body of a top-level Python
    function: find the line starting with `defLine`, then take every line up to but excluding the
    next line matching `/^def /`. Add a `stripPyComments(text)` helper that drops lines whose
    trimmed form starts with `#`. Every count below runs on the comment-stripped slice, because
    `rs-engine.py` documents Pinecone in prose inside these functions and a raw count would be
    self-invalidating.

    Test 1: the comment-stripped body of `run_mode_internal` contains zero occurrences of
    `_rs_cache_`, zero of `PINECONE`, zero of `rs_cache`.
    Test 2: same three counts are zero for `run_mode_cross_room`.
    Test 3: `_embed_via_pinecone_inference` still raises `NotImplementedError` (assert the
    comment-stripped source of that function contains `NotImplementedError`). This locks F-3's
    second half: if someone later implements that stub, the internal path gains a Pinecone
    reachability edge and this fence must go red.
    Test 4: the comment-stripped source of `lib/core/rs-engine.cjs` contains zero occurrences of
    `pinecone` (case-insensitive). Strip lines whose trimmed form starts with `//`, `*` or `/*`.
    Test 5: all four `rs-*` surfaces (`rs-experts`, `rs-explain`, `rs-fetch`, `rs-thesis`) appear
    in `data/connector-registry.json` in both the `/mos:` and `skill:` families, which is
    SEED-030 acceptance item 1 held as a regression fence (296-RESEARCH.md F-9).

    Every assertion message must name the file, the function and the token it counted, so a red run
    is actionable without reopening the test.

    Create `tests/296-dim-invariant.sh`, a bash gate, SKIP-tolerant by design.

    First it probes whether the repoint has landed: if the comment-stripped
    `lib/core/rs_cache.py` still contains `import pinecone` or `from pinecone`, print a single
    leading line `SKIP: rs_cache.py still on the Pinecone path (repoint lands in plan 296-04)` and
    exit 0. The Task 1 runner's `run_may_skip` arm turns that into a SKIPPED count, so
    `bash tests/run-all-296.sh` stays green through waves 1 to 3 instead of carrying a red arm that
    trains everyone to ignore it.

    Once the probe passes, assert, all on comment-stripped input
    (`grep -v '^[[:space:]]*#'` for Python and shell, `grep -v '^[[:space:]]*\(//\|\*\|/\*\)'` for
    CJS):
      1. `lib/core/rs_cache.py` contains zero occurrences of the literal `1024`.
      2. `lib/core/rs_cache.py` contains zero occurrences of `multilingual-e5-large`.
      3. `scripts/rs-engine.py` still contains the guard line comparing every vector length against
         `dim` inside `_build_sem_matrix_from_records` (grep for `len(v) != dim`). This is the
         runtime backstop that turns a dimensional mix into a clean `None` and a local re-embed
         instead of a silent wrong cosine.
      4. `lib/core/rs-engine.cjs` still documents the invariant (grep for `1024` inside its header
         comment is EXPECTED there and must not be swept; assert only that the file exists, so this
         gate cannot be read as demanding the warning's removal).

    Print `PASS` or a failure line naming the assertion number and the file. Exit non-zero on any
    failure. Never write a literal em-dash.
  </action>

  <verify>
    <automated>node tests/296-no-pinecone-internal.test.cjs && bash tests/296-dim-invariant.sh</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/296-no-pinecone-internal.test.cjs` exits 0 with five passing tests today, before any production file in this phase is touched.
    - `bash tests/296-dim-invariant.sh` exits 0 and its first output line begins with `SKIP:` today, because `lib/core/rs_cache.py` still imports pinecone at this point in the phase.
    - `bash tests/run-all-296.sh` exits 0 and reports `SKIP=1` or greater.
    - `grep -c 'run_mode_cross_room' tests/296-no-pinecone-internal.test.cjs` is at least 1.
    - `grep -c 'NotImplementedError' tests/296-no-pinecone-internal.test.cjs` is at least 1.
    - Both new files pass the no-em-dash fence: `LC_ALL=C.UTF-8 grep -P '\x{2014}' tests/296-no-pinecone-internal.test.cjs tests/296-dim-invariant.sh` returns no match.
    - Negative control: temporarily setting `TEST_296_PREFIX=tests/296-` and deleting nothing, then running `bash tests/296-dim-invariant.sh` after manually stubbing a copy of `rs_cache.py` without the pinecone import in a scratch dir, is NOT required; instead assert by reading that the SKIP branch and the assert branch are both present in the source (`grep -c 'SKIP:' tests/296-dim-invariant.sh` is at least 1 and `grep -c 'len(v) != dim' tests/296-dim-invariant.sh` is at least 1).
  </acceptance_criteria>

  <done>Two fences exist. One is green today and locks findings F-3 and F-9 so they cannot silently regress. The other skips honestly until plan 296-04 lands the repoint and then enforces RSLOCAL-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process to room.db | The fixture opens a real `node:sqlite` handle with `allowExtension: true`, which is the handle class permitted to load a native extension |
| test process to filesystem | `fs.mkdtempSync` under the OS temp dir, and `fs.rmSync(..., {recursive:true})` on teardown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-296-01 | Tampering | `tests/fixtures/296/room-fixture.cjs` vector-backend selection | mitigate | Never require `sqlite-vec` directly and never accept an extension path; `ensureStore` owns the probe and honors the `T-211-03` `getLoadablePath()`-only rule. The fixture returns the resolved backend so a consumer asserts rather than assumes. |
| T-296-08 | Denial of Service | `cleanup()` on a temp dir path | mitigate | `cleanup` only ever removes the exact `dir` returned by its own `fs.mkdtempSync` call in the same process. It takes no caller-supplied path and never accepts a relative path. |
| T-296-09 | Repudiation | `tests/run-all-296.sh` reporting green over zero discovery | mitigate | The `found -eq 0` guard exits non-zero, and `TEST_296_PREFIX` makes the guard provable without editing the runner. |
| T-296-10 | Tampering | grep gates counting their own header prose | mitigate | Every count in this plan comment-strips first (`grep -v` on the language's comment prefix) before `grep -c`. A bare count against an unfiltered file is forbidden. |
| T-296-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero packages. `296-RESEARCH.md`'s Package Legitimacy Audit is N/A on the no-new-packages branch; every module consumed is in-repo or an already-declared dependency that predates this phase. No install task exists, so no blocking human checkpoint is required. |
</threat_model>

<verification>
Run after every task:

    node tests/296-no-pinecone-internal.test.cjs
    bash tests/296-dim-invariant.sh

Run at plan close:

    bash tests/run-all-296.sh
    bash tests/run-all-272.sh
    TEST_296_PREFIX=tests/296-nonexistent-prefix- bash tests/run-all-296.sh; test $? -ne 0

`bash tests/run-all-272.sh` is the adjacent-subsystem regression fence named in
`296-VALIDATION.md`'s Sampling Rate. This plan touches no Phase 272 file, so it must stay exactly
as green (or as red) as it was before this plan started. Record its PASS/FAIL/SKIP counts in the
SUMMARY so plans 296-03 through 296-06 have a baseline to compare against.
</verification>

<success_criteria>
- `tests/run-all-296.sh` exists, is executable, discovers by glob with a `.test.cjs`-scoped node arm, and hard-fails on zero discovery.
- `tests/fixtures/296/room-fixture.cjs` builds and tears down a temp room and reports which vector backend it resolved.
- `node tests/296-no-pinecone-internal.test.cjs` is green today, locking findings F-3 and F-9.
- `bash tests/296-dim-invariant.sh` SKIPs honestly today and carries the real RSLOCAL-04 assertions for after plan 296-04.
- `bash tests/run-all-296.sh` exits 0.
- No file under `lib/`, `scripts/`, `commands/`, `skills/` or `dist/` is modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/296-seed-030-rs-pipeline-spine-wiring-expert-graph-reconciliatio/296-01-SUMMARY.md` when done.
Record the `bash tests/run-all-272.sh` baseline counts in it.
</output>
