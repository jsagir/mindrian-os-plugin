---
phase: 158-bounded-rejection-penalty-seed-009-minimal
plan: 02
type: execute
wave: 2
depends_on: ["158-01"]
files_modified:
  - lib/workflow/reject-penalty.cjs
  - tests/test-rejection-penalty.cjs
autonomous: true
requirements: [RJP-01, RJP-02, RJP-03, RJP-06, RJP-07]
canon_parts: [Part 4, Part 7, Part 8, Part 9]
user_setup: []

must_haves:
  truths:
    - "A REJECT-only count-within-window reader exists - it filters decision==='reject' / edge_semantic==='REJECTED' itself and does NOT reuse _invocationsSinceDecision (which counts defer+reject)"
    - "The count is REJECTs within the trailing window W in PRESENTATION-units (D-10 Q1), not lifetime cumulative"
    - "countPenalty is a bounded discount: adjusted = base * recencyFactor * (1 - countPenalty), countPenalty <= CAP"
    - "Two otherwise-identical candidates differing only in reject count: the rejected one gets a strictly lower adjustedScore via the existing _applyDecay rail"
    - "Zero rejections yields countPenalty=0 -> (1-0)=1 -> byte-identical output (RJP-02)"
    - "Below the threshold the combined recency*(1-countPenalty) is clamped >= base*FLOOR so it never lands at exactly 0 by accident (D-02a)"
    - "DEFER and PIVOT do NOT contribute to countPenalty (D-03)"
    - "All reads go through navigation.cjs; the penalty reads the outcome ENUM only, never the reason string"
  artifacts:
    - path: "lib/workflow/reject-penalty.cjs"
      provides: "rejectCountInWindow + countPenalty + the combined-suppression floor helper, layered on applyDecayWeight; REJECT-only, window W in presentation-units; with roomState.rejectCountInWindow test-injection seam"
      exports: ["rejectCountInWindow", "countPenalty", "applyRejectPenalty"]
    - path: "tests/test-rejection-penalty.cjs"
      provides: "deterministic suite covering RJP-01/02/03 + D-02a double-crush floor + D-03 reject-only + Part 8/Part 9 source asserts (extended by Plan 03 for suppression/parole)"
  key_links:
    - from: "lib/workflow/reject-penalty.cjs"
      to: "lib/core/navigation.cjs"
      via: "findRecentChanges read of f_selector_decision rows, enum-filtered"
      pattern: "navigation\\.findRecentChanges"
    - from: "lib/workflow/reject-penalty.cjs"
      to: "lib/workflow/selector-decisions.cjs"
      via: "layers (1-countPenalty) on the applyDecayWeight recency factor without rewriting its core"
      pattern: "applyDecayWeight|recencyFactor"
---

<objective>
Build the reject-count-in-window reader (D-03/D-04/D-08a) and the bounded count penalty on the shipped _applyDecay IoC seam (D-02/D-02a) - RJP-01/02/03. This LAYERS a new persistent count-penalty factor on top of the shipped transient recency factor; it does NOT rewrite applyDecayWeight's core.

Shape (D-02): adjusted = base * recencyFactor * (1 - countPenalty), where recencyFactor = 1 - exp(-(n/DECAY_WINDOW)) is the EXISTING transient signal and countPenalty is the NEW persistent bounded discount that grows with the REJECT count within window W. Below the hard-suppression threshold the combined suppression is clamped by FLOOR (D-02a) so a heavily-rejected-but-recovering command never hits exactly 0 by accident (0 is reserved for the Plan 03 hard-suppress path).

The named constants N/M/W/P/CAP/FLOOR are introduced in Plan 03 (the suppression plan that owns RJP-05); this plan imports CAP/FLOOR/W from the constants Plan 03 defines, OR defines the constants module here and Plan 03 consumes it - the executor wires whichever keeps the constants in ONE named-constant module (see Task 1 note). countPenalty in THIS plan needs CAP, FLOOR, W; the suppression fences (N, M, P) land in Plan 03.

Purpose: close the open rejection->ranking loop on the existing rail (Part 7 reuse) - a 5x-rejected command stops re-surfacing at the top on turn 6.
Output: lib/workflow/reject-penalty.cjs (reader + bounded penalty + floor) and the deterministic test suite tests/test-rejection-penalty.cjs (RJP-01/02/03 + D-02a + D-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-SPEC.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-CONTEXT.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-RESEARCH.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-VALIDATION.md
@CLAUDE.md
@lib/workflow/selector-decisions.cjs
@lib/workflow/dial-close-reach.cjs
@lib/core/navigation.cjs
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rejectCountInWindow - REJECT-only count within window W in presentation-units (D-03/D-04/D-08a)</name>
  <read_first>
    - lib/workflow/selector-decisions.cjs (_invocationsSinceDecision :276-310 - DO NOT reuse: it counts ANY f_selector_decision row, defer+reject, with NO decision-enum filter, per D-08a + RESEARCH Seam Landmine 1; mirror only its injection-seam idiom :278-282 and its findRecentChanges read :290)
    - lib/workflow/selector-decisions.cjs (recordSelectorDecision :175,:211-222 - writes decision:'defer'|'reject' AND edge_semantic:'DEFERRED'|'REJECTED' into the payload; the enum IS readable without touching reason)
    - lib/core/navigation.cjs (findRecentChanges - the ONLY read door; returns properties parsed)
    - lib/workflow/reach-presentation-counter.cjs (presentationsCount from Plan 01 - the presentation timeline that defines window W in presentation-units, D-10 Q1)
  </read_first>
  <behavior>
    - rejectCountInWindow(db, command, roomState): returns the injected roomState.rejectCountInWindow[command] when it is a number (test seam, mirroring _invocationsSinceDecision :278-282).
    - else reads f_selector_decision rows via navigation.findRecentChanges and counts ONLY rows where properties.command===command AND properties.decision==='reject' (equivalently edge_semantic==='REJECTED') - DEFER and PIVOT are excluded (D-03).
    - window W in PRESENTATION-units (D-10 Q1): only rejects whose presentation-rank falls within the last W presentations of that command count toward the total (use the Plan 01 presentation timeline; a reject older than W presentations ages out).
    - returns 0 when no db and no injected counter -> drives byte-stable-at-zero downstream.
    - NEVER reads properties.reason (Part 8).
  </behavior>
  <action>
    Create lib/workflow/reject-penalty.cjs (CJS, no new dependency). Implement rejectCountInWindow(db, command, roomState) as a NEW reader (do NOT extend _invocationsSinceDecision - it conflates defer+reject, D-08a). Filter on the outcome ENUM properties.decision==='reject' (or edge_semantic==='REJECTED') only; NEVER touch properties.reason (Part 8). Read exclusively via navigation.findRecentChanges (Part 9). Honor the roomState.rejectCountInWindow[command] injection seam first (mirror _invocationsSinceDecision :278-282 verbatim). Apply window W in presentation-units (D-10 Q1) using the Plan 01 presentationsCount timeline: a reject only counts if it occurred within the last W presentations of the command. Define W as a named constant in this module's constants block (see Task 2) - no magic literal. Defensive type guards on every input (ASVS V5; mirror applyDecayWeight :331-333).
  </action>
  <verify>
    <automated>node tests/test-rejection-penalty.cjs</automated>
  </verify>
  <done>rejectCountInWindow counts REJECT-only rows (DEFER/PIVOT excluded) within the trailing W presentations, via the chokepoint, never reading reason, honoring the injection seam.</done>
  <acceptance_criteria>
    - node tests/test-rejection-penalty.cjs exits 0 (PASS) for the reject-only + window cases.
    - grep over reject-penalty.cjs finds no 'properties.reason' / '.reason' read (Part 8) and no 'node:sqlite'/'better-sqlite3'/fs read (Part 9).
    - The test proves a DEFER row and a PIVOT row do NOT increment the count (D-03); only decision==='reject' does.
    - The test proves a reject older than W presentations ages out of the count (window in presentation-units).
    - rejectCountInWindow honors roomState.rejectCountInWindow[command] before any db read.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bounded countPenalty + applyRejectPenalty on the _applyDecay rail with the combined-suppression floor (D-02/D-02a/D-09 constants, RJP-01/02/03)</name>
  <read_first>
    - lib/workflow/selector-decisions.cjs (applyDecayWeight :330-339 - the transient recency factor to LAYER on, byte-unchanged; DECAY_WINDOW=5 :61 + EXCLUSION_THRESHOLD=0.1 :69 - the named-constant idiom to mirror)
    - lib/workflow/f-selector-ranker.cjs (_applyDecay seam :205-213; the IoC injection point opts._applyDecayWeight :378-379; call site :420-422 - Pattern 1(a)/(b) wiring, RESEARCH Architecture Patterns)
    - lib/workflow/dial-close-reach.cjs (_pivotPenalty bounded-clamp idiom :79-94 + PIVOT_PENALTY_FLOOR=0.2 :79 - the shipped investment-scaled bounded-penalty shape to mirror for CAP rationale)
    - .planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-RESEARCH.md (Constant Values table: CAP=0.6, FLOOR=0.05, W=8 with one-line rationales)
  </read_first>
  <behavior>
    - countPenalty(db, command, roomState): returns 0 when rejectCountInWindow===0 (byte-stable-at-zero, RJP-02). Else returns a BOUNDED discount min(CAP, n/(N+1)) (or a gentler bounded curve - Claude's discretion per CONTEXT, must be <= CAP). CAP=0.6 (D-09).
    - applyRejectPenalty(base, command, roomState): composes onto the recency rail per D-02: factor = recencyFactor * (1 - countPenalty); adjusted = base * factor. recencyFactor is the shipped applyDecayWeight signal (call applyDecayWeight(1, command, roomState) to recover the bare factor, OR compose so _applyDecay runs first and the penalty multiplies its result - RESEARCH Pattern 1(a)/(b)).
    - combined-suppression floor (D-02a): when NOT hard-suppressed, adjusted = max(adjusted, base * FLOOR) so a heavily-rejected-but-recovering command never lands at exactly 0 by accident (0 reserved for the Plan 03 hard-suppress path). FLOOR=0.05 (D-09). (The hard-suppress predicate itself lands in Plan 03; this plan applies the floor unconditionally-below-threshold and Plan 03 wires the drop.)
    - applyDecayWeight's core formula stays byte-unchanged (D-02).
    - strictly-lower invariant (RJP-01): for two identical candidates differing only in reject count, the one with rejects gets a strictly lower adjustedScore.
  </behavior>
  <action>
    In lib/workflow/reject-penalty.cjs add a named-constants block (REJECT_WINDOW=8 (W), COUNT_PENALTY_CAP=0.6 (CAP), COMBINED_SUPPRESS_FLOOR=0.05 (FLOOR)) mirroring the DECAY_WINDOW named-constant idiom (selector-decisions.cjs:61) with a documented low-data rationale comment citing ~4 users / <100 edges and the shipped reference points (DECAY_WINDOW=5, PIVOT_PENALTY_FLOOR=0.2). NOTE on constants ownership: N/M/P (the suppression fences) are owned by Plan 03 (RJP-05); to keep ALL six constants in ONE module per RJP-05's "no magic literal" rule, define all six named constants in THIS module's constants block now (N=3, M=2, W=8, P=5, CAP=0.6, FLOOR=0.05 per D-09) and export them so Plan 03 imports N/M/P from here - do NOT duplicate any literal in Plan 03. Implement countPenalty (bounded by CAP, 0 at zero rejects) and applyRejectPenalty (layers (1-countPenalty) on the applyDecayWeight recency factor per D-02; applies the FLOOR clamp per D-02a). Do NOT touch applyDecayWeight's core (:330-339) and do NOT touch the 0.40/0.30/0.30 literals (f-selector-ranker.cjs:287-290, RJP-08). Defensive guards throughout.
  </action>
  <verify>
    <automated>node tests/test-rejection-penalty.cjs</automated>
  </verify>
  <done>countPenalty is bounded by CAP and is 0 at zero rejects; applyRejectPenalty layers the discount on the recency rail without rewriting applyDecayWeight; the combined floor keeps below-threshold scores off exactly-0; all six named constants live in one block.</done>
  <acceptance_criteria>
    - node tests/test-rejection-penalty.cjs exits 0 (PASS).
    - The test proves two identical candidates differing only in reject count: rejected one has strictly lower adjustedScore (RJP-01).
    - The test proves zero rejections -> countPenalty===0 -> adjusted byte-identical to applyDecayWeight-only output (RJP-02).
    - The test proves countPenalty never exceeds COUNT_PENALTY_CAP for any reject count (RJP-03 bounded).
    - The test proves a heavily-rejected-but-below-threshold command's combined factor is clamped >= COMBINED_SUPPRESS_FLOOR, never exactly 0 (D-02a double-crush).
    - grep confirms N/M/W/P/CAP/FLOOR are all named module constants in reject-penalty.cjs; applyDecayWeight core (selector-decisions.cjs:330-339) and the 0.40/0.30/0.30 literals are unmodified.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- node tests/test-rejection-penalty.cjs exits 0 (the RJP-01/02/03 + D-02a + D-03 cases; Plan 03 extends this same file with suppression/parole cases).
- grep over lib/workflow/reject-penalty.cjs: no reason read (Part 8); no direct sqlite/fs read (Part 9); all six constants named.
- applyDecayWeight core and the 0.40/0.30/0.30 ensemble literals are byte-unchanged (RJP-08 prelim).
</verification>

<success_criteria>
- REJECT-only count-within-W reader exists (DEFER/PIVOT excluded, D-03), via the chokepoint, enum-only.
- Bounded countPenalty layered on the shipped recency rail (D-02); byte-stable at zero rejects (RJP-02); bounded by CAP (RJP-03).
- Combined-suppression floor keeps below-threshold scores off accidental 0 (D-02a).
- All six named constants (N/M/W/P/CAP/FLOOR) defined in one module with documented low-data rationale (sets up RJP-05).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| f_selector_decision rows -> rejectCountInWindow | local read of REJECT outcome enums; must read the enum, never the reason string |
| room.db -> penalty math | local read; per-room scope only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-02-01 | Information Disclosure | rejectCountInWindow reading a decision row | mitigate | filter on properties.decision==='reject' / edge_semantic==='REJECTED' enum ONLY; NEVER read properties.reason (RJP-06; Task 1 acceptance grep + Plan 04 sweep) |
| T-158-02-02 | Tampering | direct sqlite/fs bypass | mitigate | reads only via navigation.findRecentChanges (RJP-07; Plan 04 Part 9 sweep) |
| T-158-02-03 | Tampering | cross-room reject bias | mitigate | read only the active room.db; per-room scope (Part 8) |
| T-158-02-04 | Elevation of Privilege | countPenalty unbounded over-suppression | mitigate | bounded by COUNT_PENALTY_CAP; combined-suppression FLOOR keeps below-threshold scores off accidental 0 (D-02a) |
| T-158-SC | Tampering | npm/pip/cargo installs | accept | zero external packages; no install task |
</threat_model>

<output>
Create `.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-02-SUMMARY.md` when done. Record the exported constant names + the IoC wiring choice (Pattern 1(a) extend-the-injected-fn vs 1(b) sibling-on-the-rail) so Plan 03 wires the hard-drop consistently.
</output>
