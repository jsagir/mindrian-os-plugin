---
phase: 158
slug: bounded-rejection-penalty-seed-009-minimal
plan: 02
type: execute
wave: 2
depends_on: ["158-01"]
files_modified:
  - lib/core/navigation/memory-events.cjs
  - lib/workflow/reach-reject-reader.cjs
  - scripts/intent-classifier.cjs
  - tests/test-158-reach-presentation-counter.cjs
  - tests/test-158-reach-reject-only.cjs
autonomous: true
requirements: [RJP-06, RJP-07]
canon_parts: [Part 4, Part 7, Part 8, Part 9]
user_setup: []

must_haves:
  truths:
    - "An additive reach_presented EVENT_TYPES entry exists (floor-not-size contract preserved)"
    - "One reach_presented memory_event lands per offered top-3 reach_id, fired on the LIVE engine arm where roomDb is open (intent-classifier.cjs runNavigationEngine), keyed by reach_id"
    - "presentationsCount(reach_id) reads reach_presented events back via navigation.findRecentChanges filtered by properties.reach_id"
    - "rejectCountInWindow(reach_id) counts REJECT-only f_selector_decision rows (decision==='reject' / edge_semantic==='REJECTED') for that reach_id within the trailing W presentation-units; DEFER/PIVOT never contribute (D-03)"
    - "Both readers prefer the roomState.rejectCountInWindow[reach_id] / roomState.presentationsCount[reach_id] test-injection seam so unit tests run db-free; the dial orchestrator stays PURE (no db threaded in)"
    - "The reach_presented write is a side-channel memory_event, never a ranker input -> byte-stable at zero (RJP-02 foundation)"
  artifacts:
    - path: "lib/core/navigation/memory-events.cjs"
      provides: "additive 'reach_presented' EVENT_TYPES entry"
      contains: "reach_presented"
    - path: "lib/workflow/reach-reject-reader.cjs"
      provides: "rejectCountInWindow(db, reach_id, roomState) + presentationsCount(db, reach_id, roomState), REJECT-only + reach_id-filtered, via the navigation.cjs chokepoint, with the test-injection seam"
      exports: ["rejectCountInWindow", "presentationsCount"]
    - path: "scripts/intent-classifier.cjs"
      provides: "reach_presented fired per offered top-3 reach_id on the live engine arm (roomDb open)"
      contains: "reach_presented"
  key_links:
    - from: "scripts/intent-classifier.cjs (live arm, roomDb open :1446-1488)"
      to: "navigation.logMemoryEvent(roomDb, 'reach_presented', ...)"
      via: "one event per offered top-3 reach_id"
      pattern: "reach_presented"
    - from: "lib/workflow/reach-reject-reader.cjs"
      to: "navigation.findRecentChanges"
      via: "reach_id-filtered scan of f_selector_decision + reach_presented"
      pattern: "findRecentChanges"
---

<objective>
Build the two signals the fences consume. (1) Add an additive reach_presented EVENT_TYPES entry and fire one reach_presented memory_event per offered top-3 reach_id on the LIVE engine arm inside runNavigationEngine (where roomDb is open at intent-classifier.cjs:1341, closed at :1488) -- NOT at the pure render seam (db is already closed there, :1757). (2) Create lib/workflow/reach-reject-reader.cjs with two pure readers keyed by reach_id: rejectCountInWindow (REJECT-only, within the trailing W presentation-units) and presentationsCount, both reading ONLY via navigation.findRecentChanges (Part 9), both honoring the roomState test-injection seam so the fence tests in Plan 03 run db-free.

Purpose: nothing today records which reach was OFFERED (the render path is pure; selector_presentation is command-anonymous). The M-floor, the W-window, and parole all count reach_presented events per reach_id. This plan ships that counter plus the reject-count reader, keeping the dial orchestrator PURE (SC-07: db never threaded in; reads happen upstream on the live arm).

Output: the EVENT_TYPES entry, the live-arm reach_presented write, the reach-reject-reader module, and two deterministic test suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-CONTEXT.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-RESEARCH.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the additive reach_presented EVENT_TYPES entry</name>
  <read_first>
    - lib/core/navigation/memory-events.cjs:10-60 (the EVENT_TYPES frozen Set + the floor-not-size contract comment at :51-53; the tail at :60-80 where prior phases append additively)
    - lib/core/navigation/memory-events.cjs:418 (the closing ])); of the Set literal -- add the entry before it)
    - 158-RESEARCH.md Priority 4 (the single net-new write substrate) + the EVENT_TYPES additive-floor idiom note
  </read_first>
  <acceptance_criteria>
    'reach_presented' is a member of the EVENT_TYPES frozen Set, added additively with a Phase-158 comment mirroring the prior additive blocks (110-02 / 124-02 idiom). logEvent accepts it; no existing entry removed; the floor-not-size contract is intact (no test asserts an exact .size).
  </acceptance_criteria>
  <action>
    In lib/core/navigation/memory-events.cjs, append 'reach_presented' to the EVENT_TYPES Object.freeze(new Set([...])) literal, before the closing ])); at line ~418, with a Phase 158 comment block mirroring the Phase 124-02 two-string additive idiom verbatim (cite: additive extension only; logEvent rejects event_type outside EVENT_TYPES so this is accepted only because it is now IN the Set; the Set grows by 1; the floor-not-size contract makes the addition safe). Optionally add 'reach_suppressed' / 'reach_paroled' ONLY if trivially cheap (D-10 Q3 / Open Question 4 -- observability is in-scope-minimal-if-cheap, enum/scalar only); if added they are additive entries too. No em-dashes.
  </action>
  <verify>
    <automated>node -e "const m=require('./lib/core/navigation/memory-events.cjs'); const ok=m.EVENT_TYPES.has('reach_presented'); console.log(ok?'OK reach_presented in EVENT_TYPES':'FAIL'); process.exit(ok?0:1)"</automated>
  </verify>
  <done>reach_presented is in the frozen Set; logEvent will accept it; no entry removed.</done>
</task>

<task type="auto">
  <name>Task 2: Create lib/workflow/reach-reject-reader.cjs (rejectCountInWindow + presentationsCount, reach_id-keyed, REJECT-only, chokepoint reads, injection seam)</name>
  <read_first>
    - lib/workflow/selector-decisions.cjs:276-310 (_invocationsSinceDecision: the pre-computed-counter-wins test seam at :278-282; the findRecentChanges scan idiom at :290-301; the command-filter at :296 -- the reach_id filter mirrors this)
    - lib/core/navigation/memory-events.cjs:484-512 (findRecentChanges returns parsed properties; the eventType filter)
    - lib/hmi/cortex-reach-adapter.cjs:51-58 (the flat frozen REACH_IDS const idiom -- mirror it; do NOT import the orchestrator)
    - 158-RESEARCH.md Priority 5 (W unit = presentation-units of THIS reach_id) + the SUPERSEDED Code Examples (the bounded-clamp shape; re-key command->reach_id)
    - 158-RESEARCH.md Common Pitfalls 2 (REJECT/DEFER conflation -- filter the decision enum) + Anti-Patterns (do NOT reuse _invocationsSinceDecision; it counts ALL rows and keys by command)
  </read_first>
  <acceptance_criteria>
    A pure module exports rejectCountInWindow(db, reach_id, roomState) and presentationsCount(db, reach_id, roomState). Each prefers the roomState injection seam (roomState.rejectCountInWindow[reach_id] / roomState.presentationsCount[reach_id]) when present (returns the injected number, no db touch); else reads via navigation.findRecentChanges and returns 0 when db is null. rejectCountInWindow counts ONLY f_selector_decision rows with properties.reach_id === reach_id AND properties.decision === 'reject' (or edge_semantic === 'REJECTED'), within the trailing W presentation-units for that reach_id. presentationsCount counts reach_presented rows with properties.reach_id === reach_id. NO read of properties.reason anywhere (Part 8 / RJP-06). NO direct sqlite / fs read; reads ONLY via navigation.cjs (Part 9 / RJP-07). NO Brain call.
  </acceptance_criteria>
  <action>
    Create lib/workflow/reach-reject-reader.cjs ('use strict'; BSL 1.1 header; no em-dashes). require('../core/navigation.cjs') as navigation. Declare a local frozen REACH_IDS const (the six strings, mirroring cortex-reach-adapter.cjs:51-58). presentationsCount(db, reach_id, roomState): if roomState.presentationsCount[reach_id] is a number, return it (test seam, mirroring _invocationsSinceDecision:278-282); if !db return 0; else navigation.findRecentChanges(db, 0, {eventType:'reach_presented', limit:200}) and count rows where row.properties && row.properties.reach_id === reach_id. rejectCountInWindow(db, reach_id, roomState): if roomState.rejectCountInWindow[reach_id] is a number, return it (test seam); if !db return 0; else read navigation.findRecentChanges(db, 0, {eventType:'reach_presented', limit:200}) to get this reach_id's presentation timeline, take the W-th-most-recent presentation's createdAt as the window floor (W is the REJECT_WINDOW value). Then read navigation.findRecentChanges(db, windowFloorTs, {eventType:'f_selector_decision', limit:200}) and count rows where properties.reach_id === reach_id AND (properties.decision === 'reject' || properties.edge_semantic === 'REJECTED'). Read enums/scalars only -- NEVER properties.reason. Defensive guards on every read (non-throwing on bad input, mirror applyDecayWeight type guards). The window MUST be honored, not ignored. Keep the module self-contained before Plan 03 lands the shared named constants: define a module-local REJECT_WINDOW_DEFAULT = 8 (with a comment that Plan 03 reconciles this to the shared named constant REJECT_WINDOW), OR expose W as a function parameter that Plan 03's caller passes the named constant into -- executor picks the cleaner seam, but document the reconciliation so Plan 03 has one source of truth for W. No em-dashes.
  </action>
  <verify>
    <automated>node -e "const r=require('./lib/workflow/reach-reject-reader.cjs'); const a=r.rejectCountInWindow(null,'deep_research',{rejectCountInWindow:{deep_research:2}}); const b=r.presentationsCount(null,'deep_research',{presentationsCount:{deep_research:5}}); const ok=a===2&&b===5; console.log(ok?'OK injection seam works db-free':'FAIL got '+a+','+b); process.exit(ok?0:1)"</automated>
  </verify>
  <done>The reader module exports both functions; the injection seam returns injected counts db-free; reads go only through navigation.cjs; REJECT-only + reach_id-filtered.</done>
</task>

<task type="auto">
  <name>Task 3: Fire reach_presented per offered top-3 reach_id on the live engine arm</name>
  <read_first>
    - scripts/intent-classifier.cjs:1339-1345 (roomDb opened), :1446-1485 (the decision .then where cortexNodes is extracted), :1488 (roomDb closed in finally)
    - scripts/intent-classifier.cjs:869-899 (renderEngineDecisionWithDial -- how reachScores is built from cortexNodes via cortex-reach-adapter and buildReachList; mirror this to compute the would-be-offered top-3 on the live arm)
    - lib/hmi/dial-reach-orchestrator.cjs:211-255 (buildReachList returns reaches sorted desc + offered_count = min(total, OFFERED_CAP=3))
    - lib/core/navigation.cjs (logMemoryEvent re-export at :89)
    - 158-RESEARCH.md Priority 4 item 3 (the exact firing point + the one-event-per-offered-top-3 rule)
  </read_first>
  <acceptance_criteria>
    On the live engine arm, after decide() settles and cortexNodes is extracted but BEFORE roomDb is closed (inside the .then block at :1446-1485, while roomDb is the open handle), the code computes the would-be-offered top-3 reach_ids (build reachScores via cortex-reach-adapter.buildReachScoresFromCortex(cortexNodes), run buildReachList on the same reachScores + tierMode, take reaches.slice(0, offered_count)) and fires ONE navigation.logMemoryEvent(roomDb, 'reach_presented', {reach_id, source_path:'dial:presented:'+reach_id, created_by:'system'}) per offered reach_id. The payload is enum/scalar only (reach_id + source_path + created_by). Best-effort: any fault is swallowed (mirrors the dial-render fault handling) and NEVER blocks the turn or leaks the db handle. This fires only on the engine arm (the same arm that renders the dial), so legacy/silent turns write nothing.
  </acceptance_criteria>
  <action>
    In scripts/intent-classifier.cjs runNavigationEngine, inside the .then(function (decision) {...}) block where cortexNodes is computed (:1481-1485) and roomDb is still open (closed only in the trailing finally at :1488): add a best-effort reach_presented emit. Lazy-require cortex-reach-adapter.cjs + dial-reach-orchestrator.cjs (mirror the require pattern in renderEngineDecisionWithDial:877-885). Derive tierMode from decision.decision_trace.brain_md_tier_mode (default 'tier_0', mirror :892-894). Compute reachScores = adapter.buildReachScoresFromCortex(cortexNodes); reachList = orchestrator.buildReachList({tierMode, reachScores}); offered = reachList.reaches.slice(0, reachList.offered_count). For each offered reach, call navigation.logMemoryEvent(roomDb, 'reach_presented', {reach_id: offered[i].reach_id, source_path: 'dial:presented:' + offered[i].reach_id, created_by: 'system'}). Wrap the whole emit in try/catch so a fault is swallowed (never throws out of the .then, never blocks resolve, never prevents the finally closeRoomDbHandle). Do NOT thread roomDb into buildReachList (it stays pure -- buildReachList receives only reachScores + tierMode here, same as the render seam). Do NOT move db close earlier. No em-dashes. Guard: only emit when roomDb is a truthy open handle and cortexNodes is a non-empty array.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('scripts/intent-classifier.cjs','utf8'); const hasEmit=/logMemoryEvent\([^)]*['\"]reach_presented['\"]/.test(src); const offending=src.split('\n').filter(function(l){return l.indexOf('reach_presented')>-1 && l.indexOf(String.fromCharCode(0x2014))>-1;}).length; const ok=hasEmit&&offending===0; console.log(ok?'OK reach_presented emit present, no em-dash':'FAIL emit='+hasEmit+' emDashLines='+offending); process.exit(ok?0:1)"</automated>
  </verify>
  <done>reach_presented fires per offered top-3 reach_id on the live arm (db open), enum/scalar payload, best-effort, no db leak, orchestrator stays pure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Deterministic counter + REJECT-only test suites</name>
  <read_first>
    - tests/test-148-frozen-contracts.cjs (the check()/PASS-line/non-zero-exit idiom)
    - lib/memory/selector-decisions.test.cjs:236-285 (temp room.db setup for chokepoint-backed reads)
    - 158-VALIDATION.md rows: "presentation counter (SC-06)" and "REJECT-only (D-03)"
    - lib/workflow/reach-reject-reader.cjs (the module under test, from Task 2)
  </read_first>
  <acceptance_criteria>
    Two deterministic suites (no RNG, no live Brain). test-158-reach-presentation-counter.cjs: with the injection seam, presentationsCount returns the injected count; with a temp db, after writing N reach_presented events for a reach_id via the chokepoint, presentationsCount(db, reach_id) === N and a different reach_id reads its own count independently. test-158-reach-reject-only.cjs: with a temp db, after writing a mix of reject + defer f_selector_decision rows (via recordSelectorDecision with reach_id from Plan 01) for one reach_id, rejectCountInWindow counts ONLY the reject rows (DEFER excluded), and never reads properties.reason.
  </acceptance_criteria>
  <behavior>
    - presentation Test 1: injection seam -> presentationsCount returns injected number db-free.
    - presentation Test 2: write 3 reach_presented for 'deep_research' + 1 for 'hats' -> presentationsCount(db,'deep_research')===3, ('hats')===1.
    - reject-only Test 1: write 2 reject + 1 defer for 'deep_research' -> rejectCountInWindow counts 2 (defer excluded).
    - reject-only Test 2: a row carrying reason:'SECRETREASON123' never has its reason read by the reader (assert the count is correct; the reason-leak source grep is enforced in Plan 04's run-all-158.sh).
  </behavior>
  <action>
    Create tests/test-158-reach-presentation-counter.cjs and tests/test-158-reach-reject-only.cjs using node:assert/strict + the check()/PASS-line idiom. Open a temp room.db through the shipped navigation chokepoint (mirror selector-decisions.test.cjs:236-285). For presentations, write reach_presented events via navigation.logMemoryEvent(db, 'reach_presented', {reach_id, source_path, created_by:'system'}); for rejects, write via selectorDecisions.recordSelectorDecision({decision:'reject'|'defer', command, framework, reach_id, roomState:{db}}) (the Plan 01 reach_id keying). When the test needs to exercise the W window deterministically, write enough reach_presented events for the reach_id so the window floor is well-defined, or assert with all rejects inside W. Assert the behaviors above against reach-reject-reader. Print the PASS line per suite; exit non-zero on failure. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-158-reach-presentation-counter.cjs && node tests/test-158-reach-reject-only.cjs</automated>
  </verify>
  <done>Both suites green: presentationsCount is reach_id-isolated; rejectCountInWindow is REJECT-only and never reads reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live engine arm -> room.db (reach_presented write) | a presentation is graph data (Part 4); written via the chokepoint while db is open |
| room.db -> reach-reject-reader (count reads) | the only read path is navigation.findRecentChanges (Part 9 chokepoint) |
| f_selector_decision payload -> reader | only enum/scalar (decision, edge_semantic, reach_id) is read; reason is never touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-02-01 | Information Disclosure | rejectCountInWindow reading payloads | mitigate | reads decision/edge_semantic/reach_id enums ONLY; never properties.reason (RJP-06); proven by the reject-only suite + the Plan 04 source grep. |
| T-158-02-02 | Tampering | direct DB / fs read bypassing the chokepoint | mitigate | the reader reads ONLY via navigation.findRecentChanges; no require('node:sqlite')/better-sqlite3/fs read (RJP-07); Plan 04 grep enforces. |
| T-158-02-03 | Tampering | db threaded into the pure orchestrator | mitigate | reach_presented is computed + written on the live arm; buildReachList receives only reachScores + tierMode (SC-07 purity); db never enters dial-reach-orchestrator. |
| T-158-02-04 | Denial of Service | a faulting emit blocking the turn or leaking the db handle | mitigate | the emit is wrapped best-effort (try/catch), runs before the trailing finally closeRoomDbHandle, never throws out of the .then. |
| T-158-02-05 | Tampering | cross-room rejection bias | mitigate | reads only the active room's room.db (per-room scope, Canon Part 8); reach_presented + f_selector_decision rows are room-local by construction. |
| T-158-02-SC | Tampering | npm/pip/cargo installs | accept | zero new packages (pure CJS edits + a new CJS module); no install task; package-legitimacy gate N/A. |
</threat_model>

<verification>
- node tests/test-158-reach-presentation-counter.cjs green.
- node tests/test-158-reach-reject-only.cjs green.
- reach_presented is in EVENT_TYPES (Task 1 verify).
- The reader honors the injection seam db-free (Task 2 verify).
- The live-arm emit is present and em-dash-free (Task 3 verify).
- No em-dashes: `grep -rn "[\xe2][\x80][\x94]" lib/workflow/reach-reject-reader.cjs tests/test-158-reach-presentation-counter.cjs tests/test-158-reach-reject-only.cjs` returns nothing.
</verification>

<success_criteria>
- reach_presented EVENT_TYPES entry added additively; floor-not-size contract intact.
- reach_presented fires per offered top-3 reach_id on the live arm; orchestrator stays pure.
- rejectCountInWindow is REJECT-only + reach_id-filtered + window-honored; presentationsCount is reach_id-isolated.
- Both readers honor the test-injection seam and read only via navigation.cjs.
- Both deterministic suites green; no em-dashes.
</success_criteria>

<output>
Create `.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-02-SUMMARY.md` when done. Because .planning is gitignored, commit with `git add -f` per CLAUDE.md.
</output>
