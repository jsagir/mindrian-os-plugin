---
phase: 158
slug: bounded-rejection-penalty-seed-009-minimal
plan: 04
type: execute
wave: 4
depends_on: ["158-03"]
files_modified:
  - tests/run-all-158.sh
  - tests/test-158-reach-byte-stable.cjs
  - tests/test-158-reach-orchestrator-pure.cjs
  - tests/test-158-reach-part8-no-reason.cjs
  - tests/test-158-reach-part9-chokepoint.cjs
autonomous: true
requirements: [RJP-02, RJP-06, RJP-07, RJP-08]
canon_parts: [Part 4, Part 7, Part 8, Part 9]
user_setup: []

must_haves:
  truths:
    - "tests/run-all-158.sh is a one-command PASS/FAIL phase gate that runs every test-158-*.cjs suite, a Part 8 grep sweep, a Part 9 grep sweep, and a frozen-148 passthrough (bash tests/run-all-148.sh), and exits non-zero if anything fails"
    - "A byte-stable-at-zero snapshot proves a zero-reject room produces a reach list byte-identical to the captured pre-phase baseline (RJP-02)"
    - "A Part 8 source sweep proves the new reach-penalty + counter code reads decision/edge_semantic/reach_id ENUMS only, never properties.reason; a seeded SECRETREASON123 never appears in any value the path reads/emits (RJP-06)"
    - "A Part 9 source sweep proves the penalty/reader path reads only via navigation.cjs (no node:sqlite / better-sqlite3 / fs read) and the orchestrator stays pure (no db) (RJP-07)"
    - "frozen-148 passthrough stays green inside the 158 gate (RJP-08 / SC-05)"
  artifacts:
    - path: "tests/run-all-158.sh"
      provides: "the one-command phase gate (CJS suites loop + Part 8 sweep + Part 9 sweep + run-all-148.sh passthrough), mirroring run-all-148.sh"
      contains: "run-all-148.sh"
    - path: "tests/test-158-reach-byte-stable.cjs"
      provides: "byte-identical zero-reject buildReachList snapshot vs captured baseline (RJP-02)"
    - path: "tests/test-158-reach-orchestrator-pure.cjs"
      provides: "grep tripwire: buildReachList / _resolveReachScore make zero db/fs/Brain calls"
    - path: "tests/test-158-reach-part8-no-reason.cjs"
      provides: "adversarial forbidden-substring scan: no reason read in the penalty/counter path"
    - path: "tests/test-158-reach-part9-chokepoint.cjs"
      provides: "grep tripwire: reads only via navigation.cjs; no direct sqlite/fs"
  key_links:
    - from: "tests/run-all-158.sh"
      to: "every test-158-*.cjs + run-all-148.sh"
      via: "the bash PASS/FAIL loop + carried frozen-148 gate"
      pattern: "run-all-148.sh"
---

<objective>
Land the one-command phase gate and the boundary proofs. tests/run-all-158.sh mirrors tests/run-all-148.sh: a bash PASS/FAIL loop running every test-158-*.cjs suite (from Plans 01-03 + this plan), a standalone Part 8 grep sweep over the new artifacts, a Part 9 chokepoint grep sweep, and a frozen-148 passthrough (bash tests/run-all-148.sh) so the frozen constitution stays green inside the 158 gate. Plus four boundary suites: the byte-stable-at-zero snapshot (RJP-02), the orchestrator-purity tripwire, the Part 8 no-reason scan, and the Part 9 chokepoint scan.

Purpose: this is the phase capstone -- one command proves the whole reach-surface build is correct AND that frozen-148 + Part 8 + Part 9 + byte-stable-at-zero all hold together. The byte-stable snapshot is the load-bearing RJP-02 proof on the reach surface (zero rejections -> reachScores unchanged + empty suppressedReachIds -> identical reach list).

Output: tests/run-all-158.sh + four deterministic boundary suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-CONTEXT.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-RESEARCH.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Byte-stable-at-zero + orchestrator-purity boundary suites</name>
  <read_first>
    - tests/test-drift-baseline.cjs (the byte-stable snapshot idiom -- capture then byte-compare)
    - lib/hmi/dial-reach-orchestrator.cjs:211-255 (buildReachList; the purity invariant at :28-30, :209)
    - tests/test-148-frozen-contracts.cjs (the check()/PASS-line idiom)
    - 158-VALIDATION.md rows: "byte-stable-at-zero (RJP-02)" + "orchestrator purity (Part 9)"
  </read_first>
  <acceptance_criteria>
    test-158-reach-byte-stable.cjs: capture a baseline buildReachList output for a fixed reachScores input with NO suppressedReachIds and NO penalty signal; assert that buildReachList for the SAME input (and for an input where every reach_id has zero rejects via empty discountedScores + empty suppressedReachIds) is byte-identical (JSON.stringify equal / deep-equal) to the baseline. test-158-reach-orchestrator-pure.cjs: read lib/hmi/dial-reach-orchestrator.cjs source and assert it contains NO db/fs/Brain tokens in the live code (no require('node:sqlite'), no better-sqlite3, no fs read, no fetch/http/await/brain-client require) -- mirroring the 148 zero-egress sweep; comments are excluded from the count (filter lines starting with optional whitespace + // or * before grepping, so header prose describing the purity invariant does not self-invalidate the gate).
  </acceptance_criteria>
  <behavior>
    - byte-stable Test 1: buildReachList(fixedScores) === captured baseline (byte-identical).
    - byte-stable Test 2: buildReachList(fixedScores, suppressedReachIds:[]) === baseline (empty set is a no-op).
    - purity Test 1: dial-reach-orchestrator.cjs non-comment source contains zero of {require('node:sqlite'), better-sqlite3, fs.read, fetch(, http, brain-client, await}.
  </behavior>
  <action>
    Create tests/test-158-reach-byte-stable.cjs: build a fixed reachScores object (e.g. {context_block:0.72, contradiction:0.4, deep_research:0.9, hats:0.6}); capture baseline = JSON.stringify(buildReachList({tierMode:'mode_a', reachScores})); assert JSON.stringify(buildReachList(same input)) === baseline and JSON.stringify(buildReachList({...same, suppressedReachIds:[]})) === baseline. Create tests/test-158-reach-orchestrator-pure.cjs: read the orchestrator source, strip comment lines (lines matching /^\s*(\/\/|\*|\/\*)/), then assert the stripped source matches none of the forbidden tokens (require\(['\"]node:sqlite, better-sqlite3, \bfs\.(read|write), fetch\(, https?://, brain-client, \bawait\b). Both suites: check()/PASS-line/non-zero-exit idiom; no em-dashes; deterministic.
  </action>
  <verify>
    <automated>node tests/test-158-reach-byte-stable.cjs && node tests/test-158-reach-orchestrator-pure.cjs</automated>
  </verify>
  <done>Byte-stable-at-zero proven on the reach surface; orchestrator purity proven by source tripwire.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Part 8 no-reason + Part 9 chokepoint boundary suites</name>
  <read_first>
    - tests/test-navigation-packet-part8-leak.cjs:69-89 (the adversarial seeded-secret forbidden-substring idiom -- seed a secret, assert ZERO in the output)
    - tests/run-all-148.sh:124-175 (the standalone Part 8 grep-sweep step d idiom)
    - lib/workflow/reach-reject-reader.cjs (the reader/penalty path under scan)
    - 158-VALIDATION.md rows: "Part 8 (RJP-06)" + "Part 9 (RJP-07)"
  </read_first>
  <acceptance_criteria>
    test-158-reach-part8-no-reason.cjs: a behavioral + source proof. Behavioral: write a REJECTED f_selector_decision row carrying reason:'SECRETREASON123' (via recordSelectorDecision with reach_id) into a temp db, run rejectCountInWindow + countPenalty + computeReachPenalties, and assert the string 'SECRETREASON123' never appears in any returned value (JSON.stringify of all outputs) AND the count is still correct. Source: assert reach-reject-reader.cjs non-comment source never references properties.reason / .reason in a read position. test-158-reach-part9-chokepoint.cjs: assert reach-reject-reader.cjs non-comment source reads ONLY via navigation (require('../core/navigation.cjs') present; navigation.findRecentChanges used) and contains none of {require('node:sqlite'), better-sqlite3, fs.read, direct DatabaseSync}. Comment lines are stripped before grepping so header prose does not self-invalidate.
  </acceptance_criteria>
  <behavior>
    - Part 8 behavioral: SECRETREASON123 seeded in a reject row -> never in computeReachPenalties / countPenalty / rejectCountInWindow output; count is correct.
    - Part 8 source: no .reason read in the reader's non-comment source.
    - Part 9 source: reads only via navigation.cjs; no node:sqlite / better-sqlite3 / fs read in non-comment source.
  </behavior>
  <action>
    Create tests/test-158-reach-part8-no-reason.cjs: open a temp room.db; recordSelectorDecision({decision:'reject', command:'cmd_x', framework:'fw_x', reach_id:'deep_research', reason:'SECRETREASON123', roomState:{db}}); call rejectCountInWindow/countPenalty/computeReachPenalties and assert JSON.stringify(every output).indexOf('SECRETREASON123') === -1 AND rejectCountInWindow is correct; then strip comment lines from reach-reject-reader.cjs source and assert no /\.reason\b/ read. Create tests/test-158-reach-part9-chokepoint.cjs: strip comment lines from reach-reject-reader.cjs source; assert it requires ../core/navigation.cjs and uses findRecentChanges and contains none of the forbidden direct-db/fs tokens. Both: check()/PASS-line/non-zero-exit; no em-dashes; deterministic. Use the filtered-grep idiom (strip comment lines) so header prose mentioning "reason" or "sqlite" in a comment never trips the gate.
  </action>
  <verify>
    <automated>node tests/test-158-reach-part8-no-reason.cjs && node tests/test-158-reach-part9-chokepoint.cjs</automated>
  </verify>
  <done>Part 8 (no reason read, seeded-secret never leaks) + Part 9 (chokepoint-only reads) proven behaviorally and by source tripwire.</done>
</task>

<task type="auto">
  <name>Task 3: tests/run-all-158.sh -- the one-command phase gate</name>
  <read_first>
    - tests/run-all-148.sh:1-45 (the header + SCRIPT_DIR/REPO_ROOT + CJS_SUITES array idiom) and :120-180 (the standalone Part 8 grep-sweep step d + the final tally + non-zero exit)
    - all the test-158-*.cjs suites created across Plans 01-04 (the full list to enumerate)
    - 158-RESEARCH.md "## Validation Architecture" Wave 0 Gaps (run-all-158.sh requirements) + the carried-fences list
  </read_first>
  <acceptance_criteria>
    tests/run-all-158.sh (bash, set -uo pipefail, no emoji, no em-dashes) runs to completion even when a suite fails, prints a per-suite PASS/FAIL line + a final tally, and exits 1 if any failed. It: (a) loops every test-158-*.cjs suite (test-158-reach-id-keying, test-158-reach-presentation-counter, test-158-reach-reject-only, test-158-reach-discount, test-158-reach-hard-suppress, test-158-reach-fences, test-158-reach-frozen-148-guard, test-158-reach-byte-stable, test-158-reach-orchestrator-pure, test-158-reach-part8-no-reason, test-158-reach-part9-chokepoint); a missing file gates to a FAIL line (never a crash); (b) runs a standalone Part 8 grep sweep over the new artifacts (reach-reject-reader.cjs + the intent-classifier edits) for forbidden reason-read tokens, using the strip-comments idiom (grep -v of comment lines before counting) so header prose never self-invalidates; (c) runs a Part 9 grep sweep asserting reach-reject-reader.cjs has no node:sqlite/better-sqlite3/fs read and the orchestrator has no db; (d) runs bash tests/run-all-148.sh as a carried passthrough so frozen-148 stays green inside the 158 gate.
  </acceptance_criteria>
  <action>
    Create tests/run-all-158.sh mirroring tests/run-all-148.sh structure verbatim where possible. Header comment block (no em-dashes) explaining the four parts. set -uo pipefail; SCRIPT_DIR/REPO_ROOT resolution; a CJS_SUITES array listing all eleven test-158-*.cjs files; a loop that runs each via node, prints "--- Running: <suite> ---" + ">>> <suite>: PASSED/FAILED", increments counters, and continues on failure. Part 8 sweep step: for the SWEEP_TARGETS (lib/workflow/reach-reject-reader.cjs + scripts/intent-classifier.cjs reach-penalty lines), grep for a forbidden reason-read pattern (e.g. '\.reason\b' used in a read) AFTER stripping comment lines (grep -vE '^\s*(//|\*|/\*)' first) so a comment mentioning reason does not trip it; FAIL the sweep if found. Part 9 sweep step: grep reach-reject-reader.cjs (comments stripped) for require\(['\"]node:sqlite / better-sqlite3 / '\bfs\.(read|write)' and FAIL if any present; assert it requires navigation.cjs. Carried passthrough: run "bash \"$REPO_ROOT/tests/run-all-148.sh\"" and fold its exit code into the tally (PASS only if 0). Final tally + exit 1 if any FAILED. chmod +x semantics noted (the file is run via bash). No emoji, no em-dashes.
  </action>
  <verify>
    <automated>bash tests/run-all-158.sh</automated>
  </verify>
  <done>One command (bash tests/run-all-158.sh) runs all 158 suites + Part 8 sweep + Part 9 sweep + frozen-148 passthrough, PASS overall, exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded secret (reason text) -> penalty path outputs | the Part 8 suite proves the secret never crosses into any returned value |
| new source files -> the boundary grep sweeps | the gate scans the new artifacts for direct-db / reason-read regressions |
| frozen-148 constitution -> the 158 gate | run-all-148.sh runs inside run-all-158.sh so a frozen-148 break fails the phase gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-04-01 | Information Disclosure | a future edit reading reason in the penalty path | mitigate | the Part 8 behavioral seeded-secret suite + the run-all-158.sh comment-stripped reason-read grep catch any regression (RJP-06). |
| T-158-04-02 | Tampering | a future edit bypassing the chokepoint with direct sqlite/fs | mitigate | the Part 9 source tripwire + the run-all-158.sh Part 9 sweep (comment-stripped) fail on any direct-db/fs token (RJP-07). |
| T-158-04-03 | Tampering | a frozen-148 break slipping past the phase gate | mitigate | run-all-148.sh is a carried passthrough inside run-all-158.sh; a broken constant fails the 158 gate (RJP-08 / SC-05). |
| T-158-04-04 | Repudiation | a grep gate self-invalidating on header prose (comments mentioning reason/sqlite) | mitigate | every grep sweep strips comment lines (grep -vE of comment patterns) before counting, per the grep-gate-hygiene rule -- so a comment cannot trip or mask the gate. |
| T-158-04-05 | Tampering | byte-stability silently broken | mitigate | the byte-stable snapshot suite (captured baseline byte-compare) is in the gate; a non-identical zero-reject render fails it (RJP-02). |
| T-158-04-SC | Tampering | npm/pip/cargo installs | accept | zero new packages (bash + CJS test files only); no install task; package-legitimacy gate N/A. |
</threat_model>

<verification>
- bash tests/run-all-158.sh exits 0 (all suites + Part 8 sweep + Part 9 sweep + frozen-148 passthrough PASS).
- node tests/test-158-reach-byte-stable.cjs green (RJP-02).
- node tests/test-158-reach-orchestrator-pure.cjs green.
- node tests/test-158-reach-part8-no-reason.cjs green (RJP-06).
- node tests/test-158-reach-part9-chokepoint.cjs green (RJP-07).
- bash tests/run-all-148.sh green standalone (RJP-08 / SC-05).
- No em-dashes: `grep -rln "[\xe2][\x80][\x94]" tests/run-all-158.sh tests/test-158-reach-byte-stable.cjs tests/test-158-reach-orchestrator-pure.cjs tests/test-158-reach-part8-no-reason.cjs tests/test-158-reach-part9-chokepoint.cjs` returns nothing.
</verification>

<success_criteria>
- run-all-158.sh is the one-command phase gate: all 158 suites + Part 8 sweep + Part 9 sweep + run-all-148.sh passthrough, exit 0.
- Byte-stable-at-zero proven on the reach surface (RJP-02).
- Part 8 (no reason read; seeded secret never leaks) + Part 9 (chokepoint-only) proven behaviorally and by source tripwire (RJP-06/07).
- frozen-148 stays green inside the gate (RJP-08 / SC-05).
- Every grep sweep strips comments first (grep-gate hygiene); no self-invalidating gate.
</success_criteria>

<output>
Create `.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-04-SUMMARY.md` when done. Because .planning is gitignored, commit with `git add -f` per CLAUDE.md.
</output>
