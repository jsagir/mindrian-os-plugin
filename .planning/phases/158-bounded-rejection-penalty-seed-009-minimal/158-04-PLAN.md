---
phase: 158-bounded-rejection-penalty-seed-009-minimal
plan: 04
type: execute
wave: 4
depends_on: ["158-01", "158-02", "158-03"]
files_modified:
  - tests/run-all-158.sh
  - tests/test-158-byte-stable.cjs
  - tests/test-rejection-penalty.cjs
autonomous: true
requirements: [RJP-02, RJP-06, RJP-07, RJP-08]
canon_parts: [Part 6, Part 7, Part 8, Part 9]
user_setup: []

must_haves:
  truths:
    - "bash tests/run-all-158.sh is the one-command phase gate: it runs the 158 CJS suites, a Part 8 grep sweep, a Part 9 grep sweep, and passes through bash tests/run-all-148.sh"
    - "The frozen-148 invariant stays green (18/18): MAX_K=3, DIAL_REACH_K=6, 0.70/0.15 gate, 6-reach bank, 3 postures unchanged (RJP-08)"
    - "A byte-stable-at-zero snapshot test proves a zero-reject room produces ranked output byte-identical to a captured pre-phase baseline (RJP-02)"
    - "The Part 8 grep sweep finds zero rejection-reason / freeform-body reads in the new 158 code (RJP-06)"
    - "The Part 9 grep sweep finds no direct sqlite / better-sqlite3 / fs read in the penalty + counter path (RJP-07)"
    - "No ranker_weights table DDL is added anywhere (RJP-08)"
  artifacts:
    - path: "tests/run-all-158.sh"
      provides: "the phase gate mirroring run-all-148.sh structure: CJS suite loop + Part 8 grep sweep + Part 9 grep sweep + run-all-148.sh passthrough + the byte-stable snapshot"
    - path: "tests/test-158-byte-stable.cjs"
      provides: "byte-stable-at-zero snapshot test mirroring test-drift-baseline.cjs (captured zero-reject rankForSelector output, asserted byte-identical)"
  key_links:
    - from: "tests/run-all-158.sh"
      to: "tests/run-all-148.sh"
      via: "passthrough invocation so the frozen-6 invariant rides the 158 gate"
      pattern: "run-all-148\\.sh"
    - from: "tests/run-all-158.sh"
      to: "lib/workflow/reject-penalty.cjs + reach-presentation-counter.cjs"
      via: "Part 8 + Part 9 grep sweeps over the new artifacts"
      pattern: "reject-penalty|reach-presentation-counter"
---

<objective>
Build the phase gate (RJP-06/07/08 + RJP-02 snapshot) - the one-command proof the phase is safe. tests/run-all-158.sh mirrors the shipped tests/run-all-148.sh structure: a CJS-suite loop, a Part 8 forbidden-substring grep sweep over the new 158 artifacts, a Part 9 chokepoint-only grep sweep, the byte-stable-at-zero snapshot, AND a passthrough of bash tests/run-all-148.sh so the frozen-148 invariant (the frozen-6 contracts) stays green.

Purpose: prove counts/enums only (Part 8, RJP-06), reads via navigation.cjs only (Part 9, RJP-07), no ranker_weights table + 0.40/0.30/0.30 + the frozen-6 unchanged (RJP-08), and byte-identical output at zero rejections (RJP-02). This is the Part 6 dog-fooding gate - the harness that would have caught a Part 8 leak or a frozen-contract regression.
Output: tests/run-all-158.sh (the gate), tests/test-158-byte-stable.cjs (the snapshot), and the final consolidation of tests/test-rejection-penalty.cjs into the gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-SPEC.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-CONTEXT.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-RESEARCH.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-VALIDATION.md
@CLAUDE.md
@tests/run-all-148.sh
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Byte-stable-at-zero snapshot test (RJP-02)</name>
  <read_first>
    - tests/test-drift-baseline.cjs (the shipped snapshot idiom - capture output, assert byte-identity)
    - lib/memory/f-selector-ranker.test.cjs (_setRegistry :81 + idempotence deepEqual :385-403 - the deterministic fixture idiom)
    - lib/workflow/f-selector-ranker.cjs (rankForSelector - the surface to snapshot)
    - lib/workflow/reject-penalty.cjs (the penalty layered in Plan 02/03 - must contribute ZERO at zero rejects)
  </read_first>
  <behavior>
    - Capture rankForSelector output on a zero-reject _setRegistry fixture (no reach_presented events, no f_selector_decision rows, empty injection seams) WITH the Plan 02/03 penalty wired in.
    - Assert it is byte-identical (JSON.stringify deepEqual) to the same fixture's output computed without any rejection input - i.e. the penalty path is a true no-op at zero rejects (RJP-02).
    - Deterministic: no live Brain, no RNG, no clock dependence (fixture-injected roomState).
  </behavior>
  <action>
    Create tests/test-158-byte-stable.cjs (plain CJS + node:assert/strict, PASS-line + non-zero-exit convention). Using _setRegistry fixtures and empty injection seams (no rejectCountInWindow, no presentationsCount, no db rows), capture rankForSelector output with the Plan 02/03 reject penalty + suppression wired in, and assert it deepEquals the pre-phase baseline output (the same ranker output with the penalty contributing 0). Mirror tests/test-drift-baseline.cjs verbatim for the snapshot mechanics. This proves countPenalty===0 and isHardSuppressed===false on a cold room -> identical ranked array (RJP-02 + RESEARCH Pitfall 4).
  </action>
  <verify>
    <automated>node tests/test-158-byte-stable.cjs</automated>
  </verify>
  <done>A zero-reject fixture produces ranked output byte-identical to the pre-phase baseline; the penalty + suppression contribute nothing on a cold room.</done>
  <acceptance_criteria>
    - node tests/test-158-byte-stable.cjs exits 0 (PASS).
    - The test injects a zero-reject fixture and asserts JSON.stringify(withPenalty) === JSON.stringify(baseline) (byte-identical).
    - No live Brain, no Math.random, no Date.now-dependent branch in the test.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: tests/run-all-158.sh - the phase gate (CJS loop + Part 8 sweep + Part 9 sweep + frozen-148 passthrough)</name>
  <read_first>
    - tests/run-all-148.sh (the gate structure to mirror: TOTAL/PASSED/FAILED counters, the CJS-suite loop, step (d) the Part-8 grep sweep using grep -nE :130-176, the failed-tests roster + non-zero exit)
    - tests/test-navigation-packet-part8-leak.cjs (:69-89 adversarial seeding idiom - the in-suite Part 8 adversarial test that complements the grep sweep)
    - lib/workflow/reject-penalty.cjs + lib/workflow/reach-presentation-counter.cjs (the new artifacts the sweeps target)
    - .planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-03-SUMMARY.md (the exact new-file list + ranker insertion lines so the sweeps target the right surfaces)
  </read_first>
  <action>
    Create tests/run-all-158.sh mirroring tests/run-all-148.sh structure (resolve REPO_ROOT, TOTAL/PASSED/FAILED counters, FAILED_TESTS roster, non-zero exit on any failure). Steps:
    (a) Loop the 158 CJS suites: tests/test-rejection-penalty.cjs, tests/test-158-presentation-counter.cjs, tests/test-158-byte-stable.cjs - each must exit 0.
    (b) Part 8 grep sweep over the NEW 158 artifacts (lib/workflow/reject-penalty.cjs, lib/workflow/reach-presentation-counter.cjs, and the f-selector-ranker.cjs additions): use grep -nE (NOT grep -c, to avoid the comment-counting pitfall) to assert ZERO reads of a rejection-reason / freeform-body token - forbidden pattern includes properties.reason / \.reason\b / the free-text body field set (summary|content|body|text|note|description) being READ from a decision row. Mirror run-all-148.sh step (d) :130-176. Fail the sweep if any forbidden token appears in the new code.
    (c) Part 9 grep sweep: assert the new penalty + counter files contain NO require of 'node:sqlite' / 'better-sqlite3' and NO fs read (fs.readFile / readFileSync / fs.read) - all reads/writes must go through navigation.cjs. Use grep -nE; fail if any direct-DB / fs token appears.
    (d) RJP-08 sweep: assert no 'ranker_weights' table DDL token (CREATE TABLE ranker_weights) anywhere in the new code, and assert the 0.40/0.30/0.30 literals at f-selector-ranker.cjs:287-290 are present-and-unchanged (grep the exact weight literals).
    (e) Frozen-148 passthrough: run bash tests/run-all-148.sh and require it green (the frozen-6 invariant - MAX_K=3, DIAL_REACH_K=6, 0.70/0.15, 6 reaches, 3 postures - must stay 18/18). Count its result into the gate.
    Make the script executable (chmod +x via the file mode in the write, or note it in SUMMARY for the executor to chmod).
  </action>
  <verify>
    <automated>bash tests/run-all-158.sh</automated>
  </verify>
  <done>bash tests/run-all-158.sh runs all 158 CJS suites, the Part 8 + Part 9 + RJP-08 grep sweeps, the byte-stable snapshot, and the frozen-148 passthrough; it exits 0 only when every step passes and run-all-148.sh is still 18/18.</done>
  <acceptance_criteria>
    - bash tests/run-all-158.sh exits 0 with all 158 suites green AND run-all-148.sh green (18/18).
    - The Part 8 sweep uses grep -nE (not grep -c) and fails if 'reason' or a free-text body field is read from a decision row in the new code (RJP-06).
    - The Part 9 sweep fails if 'node:sqlite' / 'better-sqlite3' / fs read appears in reject-penalty.cjs or reach-presentation-counter.cjs (RJP-07).
    - The RJP-08 sweep fails if a 'ranker_weights' CREATE TABLE token appears, and confirms the 0.40/0.30/0.30 literals at f-selector-ranker.cjs:287-290 are intact.
    - The script invokes bash tests/run-all-148.sh and counts its pass/fail into the gate (frozen-148 passthrough).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Consolidate the Part 8 adversarial assertion into the suite + flip nyquist_compliant (RJP-06)</name>
  <read_first>
    - tests/test-rejection-penalty.cjs (the suite from Plans 02/03 - the home for the in-code Part 8 adversarial test)
    - tests/test-navigation-packet-part8-leak.cjs (:69-89 - seed a distinctive SECRETREASON token and assert ZERO in any value the penalty path reads/emits)
    - .planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-VALIDATION.md (frontmatter nyquist_compliant: false -> flip to true once the map is complete)
  </read_first>
  <action>
    Append to tests/test-rejection-penalty.cjs an adversarial Part 8 case mirroring test-navigation-packet-part8-leak.cjs:69-89: seed an f_selector_decision REJECTED row carrying a distinctive reason:'SECRETREASON123' (plus an email + a long string), drive rejectCountInWindow / countPenalty / isHardSuppressed / applyRejectPenalty over it, and assert 'SECRETREASON123' NEVER appears in any value those functions read or return (JSON.stringify of every return value is clean). This proves the penalty path consumes the enum only and never the reason string (RJP-06), complementing the run-all-158.sh static grep sweep with a runtime adversarial proof. Then flip nyquist_compliant: false -> true in 158-VALIDATION.md frontmatter (the per-task verification map is now complete and every RJP maps to a deterministic automated test).
  </action>
  <verify>
    <automated>node tests/test-rejection-penalty.cjs</automated>
  </verify>
  <done>The suite includes a runtime Part 8 adversarial assertion (SECRETREASON123 never surfaces from the penalty path); nyquist_compliant flipped true.</done>
  <acceptance_criteria>
    - node tests/test-rejection-penalty.cjs exits 0 (PASS) including the SECRETREASON123 adversarial case.
    - The adversarial case asserts JSON.stringify of every penalty-path return value does NOT contain 'SECRETREASON123' (Part 8 runtime proof).
    - 158-VALIDATION.md frontmatter reads nyquist_compliant: true.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- bash tests/run-all-158.sh exits 0: all 158 CJS suites green, Part 8 + Part 9 + RJP-08 grep sweeps clean, byte-stable snapshot green, run-all-148.sh 18/18 passthrough green.
- node tests/test-158-byte-stable.cjs exits 0 (RJP-02).
- node tests/test-rejection-penalty.cjs exits 0 including the SECRETREASON123 adversarial case (RJP-06 runtime).
- nyquist_compliant: true in 158-VALIDATION.md.
</verification>

<success_criteria>
- One-command phase gate (run-all-158.sh) covers the 158 suites + Part 8 sweep + Part 9 sweep + RJP-08 sweep + frozen-148 passthrough.
- Byte-stable at zero rejections proven against a captured baseline (RJP-02).
- Part 8 clean: no reason / freeform-body reads (static sweep + runtime adversarial) - RJP-06.
- Part 9 clean: reads only via navigation.cjs (RJP-07).
- No ranker_weights table; 0.40/0.30/0.30 intact; frozen-6 invariant 18/18 (RJP-08).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new 158 code -> static grep sweeps | the gate inspects the new artifacts for Part 8 (reason/body reads) + Part 9 (direct DB/fs) violations |
| frozen-148 contracts -> passthrough | the gate proves the frozen-6 invariant is untouched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-04-01 | Information Disclosure | a Part 8 reason/body leak escaping review | mitigate | run-all-158.sh Part 8 grep sweep (grep -nE, not grep -c) over the new artifacts + the runtime SECRETREASON123 adversarial test (RJP-06) |
| T-158-04-02 | Tampering | a direct DB/fs read sneaking into the penalty path | mitigate | run-all-158.sh Part 9 grep sweep fails on node:sqlite/better-sqlite3/fs read (RJP-07) |
| T-158-04-03 | Tampering | a ranker_weights table or a 0.40/0.30/0.30 edit drifting the frozen contracts | mitigate | RJP-08 sweep (no CREATE TABLE ranker_weights; the weight literals intact) + run-all-148.sh passthrough (18/18 frozen-6) |
| T-158-04-04 | Repudiation | a regression making zero-reject output drift | mitigate | byte-stable-at-zero snapshot test against the captured baseline (RJP-02) |
| T-158-SC | Tampering | npm/pip/cargo installs | accept | zero external packages; tests are plain CJS + node:assert/strict; no install task |
</threat_model>

<output>
Create `.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-04-SUMMARY.md` when done. Record the final run-all-158.sh step roster + the run-all-148.sh pass count so /gsd-verify-work has the gate command and expected result.
</output>
