---
phase: 158
slug: bounded-rejection-penalty-seed-009-minimal
plan: 03
type: execute
wave: 3
depends_on: ["158-02"]
files_modified:
  - lib/workflow/reach-reject-reader.cjs
  - lib/hmi/dial-reach-orchestrator.cjs
  - scripts/intent-classifier.cjs
  - tests/test-158-reach-discount.cjs
  - tests/test-158-reach-hard-suppress.cjs
  - tests/test-158-reach-fences.cjs
  - tests/test-158-reach-frozen-148-guard.cjs
autonomous: true
requirements: [RJP-01, RJP-02, RJP-03, RJP-04, RJP-05, RJP-08]
canon_parts: [Part 3, Part 4, Part 7, Part 8, Part 9]
user_setup: []

must_haves:
  truths:
    - "Named module constants N=3, M=2, W=8, P=5, CAP=0.6, FLOOR=0.05 exist with a documented low-data rationale; no magic literal gates the suppression check (RJP-05)"
    - "countPenalty(reach_id) is a BOUNDED discount (min(CAP, n/(N+1))) returning exactly 0 when reject-count is 0 (byte-stable at zero, RJP-02/03)"
    - "A chronically-rejected reach_id (reject-count >= N within W, all four fences pass) is ABSENT from buildReachList's rendered top-K; at N-1 it is PRESENT and discounted (RJP-04)"
    - "All four fences hold: M min-presentations floor, W aging window, deterministic parole every Pth presentation (counter % P, never Math.random per D-06), per-room scope only"
    - "The discount is folded UPSTREAM into roomState.reachScores on the live arm; a suppressedReachIds set is passed into the PURE buildReachList which DROPS suppressed reach_ids BEFORE the .sort (:241) and _applyFrozenGate (:244)"
    - "buildReachList / _resolveReachScore make zero db/fs/Brain calls after the change (SC-07 purity); db is never threaded into the orchestrator"
    - "Frozen-148 stays green: DIAL_REACH_K=6, MAX_K=3, RECOMMEND_FLOOR=0.70, MARGIN_THRESHOLD=0.15, REACH_IDS length 6 all unchanged (SC-05 / RJP-08)"
  artifacts:
    - path: "lib/workflow/reach-reject-reader.cjs"
      provides: "named constants N/M/W/P/CAP/FLOOR + countPenalty(reach_id) + isHardSuppressed(reach_id) (the four fences) + computeReachPenalties(db, roomState) returning {discountedScores, suppressedReachIds}"
      exports: ["countPenalty", "isHardSuppressed", "computeReachPenalties", "REJECT_SUPPRESS_THRESHOLD", "MIN_PRESENTATIONS", "REJECT_WINDOW", "PAROLE_PERIOD", "COUNT_PENALTY_CAP", "COMBINED_SUPPRESS_FLOOR"]
    - path: "lib/hmi/dial-reach-orchestrator.cjs"
      provides: "buildReachList DROPS roomState.suppressedReachIds before sort + gate; stays pure"
      contains: "suppressedReachIds"
    - path: "scripts/intent-classifier.cjs"
      provides: "the upstream fold: discount reachScores + pass suppressedReachIds into buildReachList on the live/render path"
      contains: "suppressedReachIds"
  key_links:
    - from: "scripts/intent-classifier.cjs (live arm, db open)"
      to: "reach-reject-reader.computeReachPenalties -> discounted reachScores + suppressedReachIds"
      via: "fold upstream, pass scalars/sets into the pure orchestrator"
      pattern: "computeReachPenalties|suppressedReachIds"
    - from: "lib/hmi/dial-reach-orchestrator.cjs buildReachList"
      to: "the rendered reaches array (post-drop)"
      via: "reaches.filter(r => !suppressed.has(r.reach_id)) BEFORE .sort + _applyFrozenGate"
      pattern: "suppressedReachIds"
---

<objective>
Close the loop on the reach surface. Add the named constants + the bounded penalty + the four bias fences + the upstream fold + the drop-before-sort/gate in the pure orchestrator. A reach_id rejected >= N times within the trailing W presentation-units (with M presentations, not on a parole turn, room-local) is hard-suppressed: DROPPED from buildReachList's reaches BEFORE the desc-sort (:241) and BEFORE _applyFrozenGate (:244). Below N it is discounted (score * (1 - countPenalty), bounded by CAP, floored by FLOOR). Zero rejections leaves reachScores untouched and suppressedReachIds empty -> byte-identical reach list (RJP-02).

Purpose: this is the SEED-009-MINIMAL behavior the phase exists to deliver. The penalty is the SOLE multiplier on the reach surface (no recency factor exists here, SC-03). The orchestrator stays PURE: the discount is folded into roomState.reachScores upstream on the live arm and a suppressedReachIds set is passed in; buildReachList only drops the set (SC-07). The frozen-148 constants are untouched (SC-05): the bank stays 6, we filter the RUNTIME reaches array.

Output: the constants + penalty + fences + computeReachPenalties in reach-reject-reader.cjs, the upstream fold in intent-classifier.cjs, the drop in dial-reach-orchestrator.cjs, and four deterministic suites including the carried frozen-148 guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-CONTEXT.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-RESEARCH.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-VALIDATION.md
@CLAUDE.md
@lib/hmi/dial-reach-orchestrator.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Named constants + bounded penalty + four fences + computeReachPenalties in reach-reject-reader.cjs</name>
  <read_first>
    - lib/workflow/reach-reject-reader.cjs (the Plan 02 module: rejectCountInWindow + presentationsCount + the REJECT_WINDOW reconciliation note)
    - lib/workflow/selector-decisions.cjs:61 (the DECAY_WINDOW named-constant idiom to mirror)
    - lib/workflow/dial-close-reach.cjs:79-94 (the PIVOT_PENALTY_FLOOR bounded-clamp idiom)
    - 158-RESEARCH.md "## Constant Values" (the grounded N=3/M=2/W=8/P=5/CAP=0.6/FLOOR=0.05 with per-line rationale) + the SUPERSEDED Code Examples (the countPenalty / isHardSuppressed shape; re-key command->reach_id, DROP the recencyFactor term)
    - 158-RESEARCH.md Priority 7 (constants re-confirmed for 6 reaches) + Pattern 2 (deterministic parole counter %P, D-06)
    - 158-RESEARCH.md Priority 4 item 2 (computeReachPenalties runs upstream; folds discount into reachScores; returns suppressedReachIds)
  </read_first>
  <acceptance_criteria>
    reach-reject-reader.cjs exports the six named constants (REJECT_SUPPRESS_THRESHOLD=3, MIN_PRESENTATIONS=2, REJECT_WINDOW=8, PAROLE_PERIOD=5, COUNT_PENALTY_CAP=0.6, COMBINED_SUPPRESS_FLOOR=0.05) with a documented low-data (~4 user, <100 edge) rationale comment block. countPenalty(db, reach_id, roomState): returns 0 when rejectCountInWindow===0 (byte-stable); returns 0 when presentationsCount < MIN_PRESENTATIONS (M fence); else min(COUNT_PENALTY_CAP, n/(REJECT_SUPPRESS_THRESHOLD+1)). isHardSuppressed(db, reach_id, roomState): false when presentationsCount < MIN_PRESENTATIONS (M); false when presentationsCount>0 AND presentationsCount % PAROLE_PERIOD === 0 (deterministic parole P, D-06); else rejectCountInWindow >= REJECT_SUPPRESS_THRESHOLD (N within W). computeReachPenalties(db, roomState): for each of the 6 reach_ids, compute the discounted score (baseReachScore * (1 - countPenalty), floored to max(adjusted, base * COMBINED_SUPPRESS_FLOOR) when NOT hard-suppressed -- D-02a) and whether it is hard-suppressed; returns {discountedScores: {reach_id: score}, suppressedReachIds: Set/array}. The W in rejectCountInWindow now uses the named REJECT_WINDOW (reconcile the Plan 02 default). NO Math.random anywhere. Per-room scope: reads only the passed db (Part 8).
  </acceptance_criteria>
  <action>
    Extend lib/workflow/reach-reject-reader.cjs. Add the six named module constants with a documented low-data rationale comment (mirror selector-decisions.cjs:61 DECAY_WINDOW idiom; cite 158-RESEARCH Constant Values rationale per constant; flag tunable-later). Reconcile the Plan 02 window: rejectCountInWindow now uses REJECT_WINDOW (the named constant) as W. Implement countPenalty(db, reach_id, roomState) and isHardSuppressed(db, reach_id, roomState) exactly per the acceptance criteria (re-key the SUPERSEDED Code Example shape to reach_id; DROP the recencyFactor term -- the reach surface has no recency multiplier, SC-03; the penalty is the sole multiplier). Parole is deterministic: presentationsCount % PAROLE_PERIOD === 0 (NEVER Math.random, D-06). Implement computeReachPenalties(db, roomState): iterate the frozen 6 reach_ids; baseScore = (roomState.reachScores && typeof roomState.reachScores[reach_id]==='number') ? roomState.reachScores[reach_id] : undefined (only fold a discount where a base prior exists -- leave reaches with no supplied prior to the orchestrator's existing default path so byte-stability holds); discounted = baseScore * (1 - countPenalty(...)); when NOT isHardSuppressed, discounted = Math.max(discounted, baseScore * COMBINED_SUPPRESS_FLOOR) (D-02a, never accidental 0); collect hard-suppressed reach_ids into a set. Return {discountedScores, suppressedReachIds}. When roomState provides no reachScores and no reject signal, discountedScores is empty and suppressedReachIds is empty (byte-stable foundation). Read enums/scalars only; no reason; reads only via navigation.cjs; no Brain. No em-dashes.
  </action>
  <verify>
    <automated>node -e "const r=require('./lib/workflow/reach-reject-reader.cjs'); const ok=r.REJECT_SUPPRESS_THRESHOLD===3&&r.MIN_PRESENTATIONS===2&&r.REJECT_WINDOW===8&&r.PAROLE_PERIOD===5&&r.COUNT_PENALTY_CAP===0.6&&r.COMBINED_SUPPRESS_FLOOR===0.05&&typeof r.computeReachPenalties==='function'; const cp0=r.countPenalty(null,'deep_research',{rejectCountInWindow:{deep_research:0},presentationsCount:{deep_research:5}}); const fs=require('fs'); const noRng=!/Math\.random/.test(fs.readFileSync('lib/workflow/reach-reject-reader.cjs','utf8')); console.log(ok&&cp0===0&&noRng?'OK constants+zero-penalty+no-RNG':'FAIL ok='+ok+' cp0='+cp0+' noRng='+noRng); process.exit(ok&&cp0===0&&noRng?0:1)"</automated>
  </verify>
  <done>Named constants exported; countPenalty is bounded + zero-at-zero; isHardSuppressed honors all four fences; parole is deterministic; computeReachPenalties returns discountedScores + suppressedReachIds.</done>
</task>

<task type="auto">
  <name>Task 2: Drop suppressedReachIds in the PURE buildReachList before sort + gate</name>
  <read_first>
    - lib/hmi/dial-reach-orchestrator.cjs:211-255 (buildReachList: the REACH_DEFS.map at :219-238, the .sort at :241, _applyFrozenGate at :244, total_count/offered_count at :246-247)
    - lib/hmi/dial-reach-orchestrator.cjs:28-30, :209 (the purity invariant: no db, no fs, no Brain -- the change must preserve this)
    - 158-RESEARCH.md Priority 2 (the drop goes immediately after the .map and before the .sort; why before the gate not after) + Priority 3 (the frozen-148 guard holds; total_count shrinks honestly)
  </read_first>
  <acceptance_criteria>
    buildReachList reads roomState.suppressedReachIds (a Set or array of reach_id strings; absent/empty = no drop). Immediately after the REACH_DEFS.map (:238) and BEFORE the .sort (:241), the reaches array is filtered to drop any reach whose reach_id is in suppressedReachIds. The .sort, _applyFrozenGate, total_count, and offered_count all run on the post-drop survivors. The function stays PURE (no db/fs/Brain). DIAL_REACH_K / REACH_IDS / REACH_DEFS / the frozen constants are byte-unchanged (the bank is still defined as 6; only the runtime array is filtered). When suppressedReachIds is absent or empty, the output is byte-identical to today.
  </acceptance_criteria>
  <action>
    In lib/hmi/dial-reach-orchestrator.cjs buildReachList: read const suppressed = a normalized Set from state.suppressedReachIds (accept a Set or an array; empty Set when absent -- defensive, never throw). After const reaches = REACH_DEFS.map(...) (ends :238) and BEFORE reaches.sort(...) (:241), insert: const survivors = reaches.filter(function (r) { return !suppressed.has(r.reach_id); }); and use survivors for the sort + gate + counts (or reassign reaches = reaches.filter(...) before the existing sort line). Leave the .sort, _applyFrozenGate(reaches, tierMode), total_count = reaches.length, offered_count = Math.min(total_count, OFFERED_CAP) exactly as they are so total_count honestly reflects the post-drop bank for this turn (the "top-3 of N" footer shows the reduced N). Do NOT touch DIAL_REACH_K, RECOMMEND_FLOOR, MARGIN_THRESHOLD, REACH_DEFS, REACH_IDS, or the module.exports. Do NOT add db/fs/Brain. No em-dashes. The drop is the ONLY change to this file.
  </action>
  <verify>
    <automated>node -e "const o=require('./lib/hmi/dial-reach-orchestrator.cjs'); const base=o.buildReachList({tierMode:'mode_a', reachScores:{deep_research:0.9, hats:0.8, context_block:0.7}}); const dropped=o.buildReachList({tierMode:'mode_a', reachScores:{deep_research:0.9, hats:0.8, context_block:0.7}, suppressedReachIds:['deep_research']}); const present=base.reaches.some(function(r){return r.reach_id==='deep_research';}); const absent=!dropped.reaches.some(function(r){return r.reach_id==='deep_research';}); const frozen=o.DIAL_REACH_K===6&&o.RECOMMEND_FLOOR===0.70&&o.MARGIN_THRESHOLD===0.15&&o.REACH_IDS.length===6; const ok=present&&absent&&frozen&&dropped.total_count===base.total_count-1; console.log(ok?'OK drop works, frozen constants intact':'FAIL present='+present+' absent='+absent+' frozen='+frozen+' tc='+dropped.total_count); process.exit(ok?0:1)"</automated>
  </verify>
  <done>buildReachList drops suppressed reach_ids before sort + gate; total_count shrinks honestly; frozen constants byte-unchanged; pure; byte-identical when no suppression.</done>
</task>

<task type="auto">
  <name>Task 3: Fold the discount + suppressedReachIds upstream into the dial render path</name>
  <read_first>
    - scripts/intent-classifier.cjs:869-899 (renderEngineDecisionWithDial builds reachScores via cortex-reach-adapter then calls buildReachList({tierMode, reachScores}) at :896 -- the fold target)
    - scripts/intent-classifier.cjs:1446-1488 (the live arm where db is open; the Plan 02 reach_presented emit lands here; computeReachPenalties must run here while db is open, then the result threads out to the render seam)
    - scripts/intent-classifier.cjs:1481-1485, :1757-1762 (cortex_nodes threaded out; renderEngineDecisionWithDial called with ctx.cortexNodes after db closed)
    - lib/workflow/reach-reject-reader.cjs computeReachPenalties (Task 1)
    - 158-RESEARCH.md Priority 4 item 2 (read reject-counts upstream; fold into reachScores; pass suppressedReachIds; db NEVER threaded into the orchestrator)
  </read_first>
  <acceptance_criteria>
    On the live engine arm (db open), computeReachPenalties(roomDb, {reachScores, db:roomDb, roomDir}) is called with the cortex-derived reachScores; the resulting discountedScores + suppressedReachIds are threaded OUT alongside cortex_nodes (e.g. on the resolved object). At the render seam, renderEngineDecisionWithDial applies the discount (merge discountedScores over the cortex reachScores) and passes suppressedReachIds into buildReachList({tierMode, reachScores, suppressedReachIds}). buildReachList stays pure (it receives only scalars/sets; no db). When there are zero rejections, discountedScores is empty + suppressedReachIds is empty -> reachScores unchanged -> byte-identical render (RJP-02). Best-effort: any fault degrades to the un-discounted render (never blocks the turn).
  </acceptance_criteria>
  <action>
    In scripts/intent-classifier.cjs: (1) On the live arm inside the decision .then (where cortexNodes + the Plan 02 reach_presented emit already are, db open at :1446-1485): lazy-require reach-reject-reader.cjs + cortex-reach-adapter.cjs; build reachScores = adapter.buildReachScoresFromCortex(cortexNodes); call const pen = reachReader.computeReachPenalties(roomDb, {reachScores: reachScores, db: roomDb, roomDir: roomDir}) inside a try/catch (degrade to {discountedScores:{}, suppressedReachIds:[]} on fault). Thread pen out on the resolved object (e.g. resolve({decision, elapsed_ms, cortex_nodes, reach_penalties: pen})). (2) In renderEngineDecisionWithDial: accept the threaded reach_penalties via ctx; after building reachScores from cortexNodes (:888), merge ctx.reach_penalties.discountedScores over reachScores (Object.assign(reachScores, discountedScores)); pass suppressedReachIds into buildReachList({tierMode, reachScores, suppressedReachIds: ctx.reach_penalties && ctx.reach_penalties.suppressedReachIds}). (3) At the render call site (:1757-1762), thread reach_penalties onto the ctx object passed to renderEngineDecisionWithDial (alongside cortexNodes + onRenderNote). Do NOT thread roomDb into buildReachList (orchestrator stays pure -- SC-07). Do NOT change the db lifecycle. All folds best-effort; a fault degrades to the un-discounted render. No em-dashes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('scripts/intent-classifier.cjs','utf8'); const hasCompute=/computeReachPenalties/.test(src); const hasSuppressArg=/suppressedReachIds/.test(src); const offending=src.split('\n').filter(function(l){return (l.indexOf('computeReachPenalties')>-1||l.indexOf('suppressedReachIds')>-1) && l.indexOf(String.fromCharCode(0x2014))>-1;}).length; const ok=hasCompute&&hasSuppressArg&&offending===0; console.log(ok?'OK upstream fold wired, no em-dash':'FAIL compute='+hasCompute+' suppress='+hasSuppressArg+' emDash='+offending); process.exit(ok?0:1)"</automated>
  </verify>
  <done>computeReachPenalties runs on the live arm; discountedScores + suppressedReachIds thread to the render seam and into buildReachList; orchestrator stays pure; zero-reject path is byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Deterministic discount + hard-suppress + four-fence + byte-stable + frozen-148 suites</name>
  <read_first>
    - tests/test-148-frozen-contracts.cjs:53-110 (the exact frozen assertions to mirror as the carried 158 guard)
    - tests/test-reach-ids-drift.cjs:24-72 (the doctrine 6-bank assertion)
    - tests/test-drift-baseline.cjs (the byte-stable snapshot idiom)
    - 158-VALIDATION.md the full reach-surface test map (discount, hard-suppress, M floor, W aging, parole, floor, byte-stable)
    - lib/workflow/reach-reject-reader.cjs + lib/hmi/dial-reach-orchestrator.cjs (the units under test)
  </read_first>
  <acceptance_criteria>
    Four deterministic suites (no RNG, no live Brain; inject roomState.reachScores + roomState.rejectCountInWindow[reach_id] + roomState.presentationsCount[reach_id]). test-158-reach-discount.cjs: a reach_id with rejects (below N, M met) gets a strictly LOWER countPenalty-discounted score than its zero-reject self; the discount never exceeds CAP; byte-stable-at-zero (zero rejects -> countPenalty 0 -> buildReachList output identical to the no-penalty baseline). test-158-reach-hard-suppress.cjs: at reject-count>=N within W (fences pass), computeReachPenalties marks the reach_id suppressed AND buildReachList(roomState with that suppressedReachIds) has it ABSENT; at N-1 it is PRESENT and discounted. test-158-reach-fences.cjs: (M) presentationsCount<M -> never suppressed even at >=N; (W) rejects outside the trailing W window do not count toward N -> re-surfaces; (P) presentationsCount % P === 0 -> a would-be-suppressed reach is force-re-admitted (deterministic); (floor/D-02a) a heavily-rejected-below-N reach never lands at exactly 0. test-158-reach-frozen-148-guard.cjs: mirrors test-148-frozen-contracts.cjs assertions (DIAL_REACH_K===6, MAX_K===3, RECOMMEND_FLOOR===0.70, MARGIN_THRESHOLD===0.15, REACH_IDS.length===6) AFTER a discount + drop, proving SC-05 holds.
  </acceptance_criteria>
  <behavior>
    - discount: countPenalty(n=2, pres=5) in (0, CAP]; score strictly lower than base; n=0 -> penalty 0 -> identical list.
    - byte-stable: buildReachList(fixed reachScores, no suppressedReachIds) === captured baseline output (deep-equal / JSON byte compare).
    - hard-suppress: n>=N, pres>=M, not on parole turn -> suppressed set contains reach_id -> absent from buildReachList; n=N-1 -> present + discounted.
    - M fence: pres<M, n>=N -> NOT suppressed (present).
    - W aging: rejects only outside W -> rejectCountInWindow < N -> not suppressed (re-surfaces).
    - parole: pres % P === 0 with n>=N -> isHardSuppressed false (force re-admit), deterministic.
    - floor: n high but < N, pres>=M -> discounted score >= base * FLOOR, never exactly 0.
    - frozen-148: all five constants unchanged after discount + drop.
  </behavior>
  <action>
    Create the four suites with node:assert/strict + the check()/PASS-line/non-zero-exit idiom. Drive every fence via the roomState injection seam (roomState.rejectCountInWindow[reach_id], roomState.presentationsCount[reach_id], roomState.reachScores[reach_id]) so all tests run db-free + deterministic. For byte-stable, capture a baseline buildReachList output for a fixed reachScores with NO suppression/penalty and assert byte-identity (mirror test-drift-baseline.cjs). For the frozen-148 guard, mirror test-148-frozen-contracts.cjs:53-75 assertions and run them after exercising a discount + a drop. Print a PASS line per suite; exit non-zero on any failure. No em-dashes, no Math.random, no clock-dependence in assertions.
  </action>
  <verify>
    <automated>node tests/test-158-reach-discount.cjs && node tests/test-158-reach-hard-suppress.cjs && node tests/test-158-reach-fences.cjs && node tests/test-158-reach-frozen-148-guard.cjs && bash tests/run-all-148.sh</automated>
  </verify>
  <done>All four suites green AND run-all-148.sh stays green (frozen-148 intact after discount + drop).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live arm (db open) -> computeReachPenalties -> render seam | reject-counts read upstream; only discounted scalars + a reach_id set cross into the pure orchestrator |
| suppressedReachIds set -> buildReachList | the orchestrator drops by enum membership only; no db, no prose |
| frozen-148 constants | the discount + drop must not move DIAL_REACH_K / MAX_K / 0.70 / 0.15 / the 6-bank |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-03-01 | Tampering | db threaded into the pure orchestrator | mitigate | computeReachPenalties runs on the live arm; buildReachList receives only reachScores + suppressedReachIds (SC-07); the orchestrator-purity assertion (Plan 04 grep) + the Task 2 verify prove no db/fs/Brain. |
| T-158-03-02 | Tampering | a discount/drop silently moving a frozen-148 constant | mitigate | the carried test-158-reach-frozen-148-guard.cjs + run-all-148.sh assert the five constants unchanged (SC-05 / RJP-08); REACH_DEFS/REACH_IDS exports byte-unchanged. |
| T-158-03-03 | Repudiation | non-deterministic ranking from RNG parole | mitigate | parole is presentationsCount % P (D-06); the Task 1 verify greps for Math.random absence; the parole fence test is deterministic. |
| T-158-03-04 | Denial of Service | over-suppression hiding a useful reach (noise / confirmation-bias loop) | mitigate | the four fences (M floor, W aging, deterministic parole, per-room scope) + the bounded CAP + the D-02a floor; each fence has a deterministic test. |
| T-158-03-05 | Information Disclosure | a discount/penalty reading reason text or crossing rooms | mitigate | countPenalty reads only counts/enums via the Plan 02 reader (no reason, Part 8); per-room scope reads only the passed db (Part 8). |
| T-158-03-06 | Tampering | byte-stability broken by an always-on penalty | mitigate | countPenalty returns exactly 0 at zero rejects; computeReachPenalties returns empty discountedScores + empty suppressedReachIds with no reject signal; the byte-stable suite proves identical output (RJP-02). |
| T-158-03-SC | Tampering | npm/pip/cargo installs | accept | zero new packages (pure CJS edits + new test files); no install task; package-legitimacy gate N/A. |
</threat_model>

<verification>
- node tests/test-158-reach-discount.cjs green.
- node tests/test-158-reach-hard-suppress.cjs green.
- node tests/test-158-reach-fences.cjs green.
- node tests/test-158-reach-frozen-148-guard.cjs green.
- bash tests/run-all-148.sh green (frozen-148 intact).
- Constants exported + zero-penalty-at-zero + no RNG (Task 1 verify).
- Drop works + frozen constants intact (Task 2 verify).
- Upstream fold wired, no em-dash (Task 3 verify).
- No em-dashes: `grep -rln "[\xe2][\x80][\x94]" lib/workflow/reach-reject-reader.cjs lib/hmi/dial-reach-orchestrator.cjs scripts/intent-classifier.cjs tests/test-158-reach-discount.cjs tests/test-158-reach-hard-suppress.cjs tests/test-158-reach-fences.cjs tests/test-158-reach-frozen-148-guard.cjs` returns nothing.
</verification>

<success_criteria>
- Named constants N/M/W/P/CAP/FLOOR with documented low-data rationale; no magic literal at the suppression check (RJP-05).
- countPenalty is bounded (<= CAP), zero at zero rejects (RJP-02/03).
- A chronically-rejected reach_id is ABSENT from buildReachList's top-K at N within W (all fences pass); PRESENT + discounted at N-1 (RJP-04).
- All four fences hold; parole is deterministic (D-06).
- The discount is folded upstream; suppressedReachIds is dropped in the pure orchestrator before sort + gate (SC-07).
- Frozen-148 stays green (SC-05 / RJP-08); byte-stable at zero (RJP-02).
</success_criteria>

<output>
Create `.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-03-SUMMARY.md` when done. Because .planning is gitignored, commit with `git add -f` per CLAUDE.md.
</output>
