---
phase: 158-bounded-rejection-penalty-seed-009-minimal
plan: 03
type: execute
wave: 3
depends_on: ["158-01", "158-02"]
files_modified:
  - lib/workflow/reject-penalty.cjs
  - lib/workflow/f-selector-ranker.cjs
  - tests/test-rejection-penalty.cjs
autonomous: true
requirements: [RJP-04, RJP-05]
canon_parts: [Part 3, Part 4, Part 7, Part 8, Part 9]
user_setup: []

must_haves:
  truths:
    - "At reject-count-in-window >= N AND presentations >= M (fences pass), the candidate is DROPPED from the candidate set BEFORE scored.sort and BEFORE slice(0,k) - it is ABSENT from the returned top-K"
    - "At N-1 the same candidate is PRESENT (discounted per Plan 02), proving the threshold boundary"
    - "1..M-1 presentations never suppress (M min-presentations floor defuses the noise attack)"
    - "Only rejects inside the trailing window W count toward N (W aging expires a stale streak -> suppression lifts)"
    - "Every Pth presentation a suppressed candidate is force-resurfaced via a DETERMINISTIC counter (presentations % P === 0), NOT Math.random (D-06)"
    - "Suppression state is per-room only - read only the active room.db (Part 8 constitutional fence)"
    - "N=3, M=2, W=8, P=5, CAP=0.6, FLOOR=0.05 are NAMED module constants; no magic literal gates the suppression check (RJP-05)"
    - "A reach with no command yields no suppression (D-01a degrade-to-zero)"
    - "Byte-stable at zero rejections preserved: a zero-reject room triggers no parole and no drop"
  artifacts:
    - path: "lib/workflow/reject-penalty.cjs"
      provides: "isHardSuppressed predicate gated by all four fences (N within W, M floor, deterministic parole every Pth, per-room scope); named constants block (RJP-05) already defined in Plan 02, consumed here"
      exports: ["isHardSuppressed"]
    - path: "lib/workflow/f-selector-ranker.cjs"
      provides: "hard-drop wired into the scoring loop BEFORE scored.sort (:451) and slice(0,k) (:465) - skip pushing a hard-suppressed candidate to scored; the layered penalty wired on the _applyDecay rail"
      contains: "isHardSuppressed"
  key_links:
    - from: "lib/workflow/f-selector-ranker.cjs"
      to: "lib/workflow/reject-penalty.cjs"
      via: "isHardSuppressed check inside the for-loop before scored.push, subject to the parole fence"
      pattern: "isHardSuppressed"
    - from: "lib/workflow/reject-penalty.cjs (isHardSuppressed)"
      to: "presentation + reject counters"
      via: "presentationsCount % P parole + rejectCountInWindow >= N within W"
      pattern: "% PAROLE_PERIOD|REJECT_SUPPRESS_THRESHOLD"
---

<objective>
Build hard-suppression-with-parole (D-05/D-06/D-08b) gated by ALL FOUR bias-control fences, plus the named-constant enforcement (D-09, RJP-05). This is the synthesis of the navigator's hard-drop choice (158-SPEC Req 4) and the four low-data bias fences.

The four fences (D-05): (M) min-presentations floor - suppression-eligible only after presented >= M times (defuses the noise attack); (W) recency-aging window - only rejects inside the trailing W presentations count toward N, so suppression EXPIRES (the primary confirmation-bias fix); periodic parole - every Pth presentation a suppressed reach is force-resurfaced via a DETERMINISTIC counter (presentations % P === 0, NOT Math.random - D-06); per-room scope only - read only the active room.db (Part 8 constitutional fence).

The hard-drop MUST remove the candidate INSIDE the scoring loop BEFORE scored.sort (f-selector-ranker.cjs:451) and slice(0,k) (:465), per D-08b + RESEARCH Seam Landmine 5 - a true drop-from-top-K, distinct from the Plan 02 D-02a floor (which deliberately keeps below-threshold scores off 0).

Purpose: a >= N-rejected-within-W command stays out of the top-K (subject to parole) instead of re-surfacing at the top on turn 6 - closing the deaf-ranker loop while the four fences prevent the low-data over-suppression and confirmation-bias self-lock.
Output: the isHardSuppressed predicate (all four fences) in reject-penalty.cjs, the hard-drop + layered-penalty wiring in f-selector-ranker.cjs, and the bias-fence test cases appended to tests/test-rejection-penalty.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-SPEC.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-CONTEXT.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-RESEARCH.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-VALIDATION.md
@CLAUDE.md
@lib/workflow/f-selector-ranker.cjs
@lib/workflow/reject-penalty.cjs
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: isHardSuppressed - all four fences with deterministic parole (D-05/D-06/D-09, RJP-05)</name>
  <read_first>
    - lib/workflow/reject-penalty.cjs (Plan 02: rejectCountInWindow + countPenalty + the named-constants block N=3/M=2/W=8/P=5/CAP=0.6/FLOOR=0.05 - all six constants already defined here per Plan 02 Task 2; consume N/M/P here, do NOT redefine)
    - lib/workflow/reach-presentation-counter.cjs (Plan 01: presentationsCount - the presentation timeline the M floor and the parole counter both read)
    - lib/workflow/selector-decisions.cjs (shouldExclude :348 - the DEFER/recency-scoped precedent for "drop below a factor threshold"; do NOT overload it, write the REJECT-specific predicate per RESEARCH State-of-the-Art note)
    - .planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-RESEARCH.md (Code Examples isHardSuppressed shape :264-270; Pattern 2 deterministic parole :157-159)
  </read_first>
  <behavior>
    - isHardSuppressed(db, command, roomState): returns false (no suppression) when presentationsCount(command) < MIN_PRESENTATIONS (M fence - noise attack defuse).
    - returns false (parole - force-resurface) when presentationsCount(command) > 0 AND presentationsCount(command) % PAROLE_PERIOD === 0 (deterministic, D-06; NEVER Math.random).
    - else returns rejectCountInWindow(command) >= REJECT_SUPPRESS_THRESHOLD (N gate, where the count is already window-W-scoped from Plan 02 rejectCountInWindow - the W fence).
    - per-room scope: reads ONLY the active room.db passed in (Part 8); never aggregates across rooms.
    - no-command guard (D-01a): a missing/empty command yields false (no suppression), never throws.
    - byte-stable: a zero-reject command is never suppressed and never paroled (parole only acts on already-suppressed reaches, which require >= N rejects, which require presentations).
  </behavior>
  <action>
    In lib/workflow/reject-penalty.cjs add isHardSuppressed(db, command, roomState) consuming the named constants N (REJECT_SUPPRESS_THRESHOLD), M (MIN_PRESENTATIONS), P (PAROLE_PERIOD) already defined in the Plan 02 constants block (do NOT redefine any literal - RJP-05 forbids magic literals at the suppression check). Order the fences exactly: M floor first (false if presentations < M), then deterministic parole (false if presentations % P === 0, presentations > 0; modular arithmetic, NOT Math.random - D-06), then the N gate on the window-W-scoped rejectCountInWindow (W fence is already inside rejectCountInWindow from Plan 02). Read only the active room.db (per-room scope, Part 8). Implement the D-01a no-command degrade-to-false guard. Export isHardSuppressed. Mirror the RESEARCH Code Examples shape (:264-270) and Pattern 2 (:157-159).
  </action>
  <verify>
    <automated>node tests/test-rejection-penalty.cjs</automated>
  </verify>
  <done>isHardSuppressed returns true only when reject-count-in-W >= N AND presentations >= M AND it is not a parole turn; parole is deterministic (presentations % P); a no-command reach and a zero-reject reach are never suppressed.</done>
  <acceptance_criteria>
    - node tests/test-rejection-penalty.cjs exits 0 (PASS) for the fence cases.
    - grep confirms the suppression check uses named constants REJECT_SUPPRESS_THRESHOLD / MIN_PRESENTATIONS / PAROLE_PERIOD - no bare numeric literal gates suppression (RJP-05).
    - grep confirms NO Math.random in reject-penalty.cjs (D-06 deterministic parole).
    - A documented low-data-rationale comment cites ~4 users / <100 edges and the shipped reference points (already present from Plan 02; verify it covers N/M/P).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire the hard-drop BEFORE sort/slice + the layered penalty on the _applyDecay rail (D-08b, RJP-04)</name>
  <read_first>
    - lib/workflow/f-selector-ranker.cjs (the for (const cmd of commands) loop :400-448; _applyDecay call site :420-422; the IoC injection opts._applyDecayWeight :378-379; scored.sort :451; scored.slice(0,k) :465; the 0.40/0.30/0.30 literals :287-290 to leave UNTOUCHED)
    - lib/workflow/reject-penalty.cjs (isHardSuppressed + applyRejectPenalty from Plan 02/Task 1)
    - .planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-02-SUMMARY.md (the Plan 02 IoC wiring choice - Pattern 1(a) extend-the-injected-fn vs 1(b) sibling-on-the-rail - wire the penalty consistently with it)
    - .planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-RESEARCH.md (Seam Landmine 5 :427 - filter inside the for-loop, skip pushing a hard-suppressed candidate to scored, subject to parole)
  </read_first>
  <action>
    In f-selector-ranker.cjs rankForSelector, inside the for (const cmd of commands) loop (:400-448): before scored.push (:435), check isHardSuppressed(roomState.db, cmd.command, effectiveRoomState) - when true, skip pushing this candidate to scored (a true drop-from-top-K BEFORE scored.sort :451 and slice(0,k) :465, per D-08b). The parole fence inside isHardSuppressed re-admits the candidate every Pth presentation, so a paroled candidate IS pushed. Wire the Plan 02 reject penalty onto the existing _applyDecay rail consistently with the Plan 02 SUMMARY's wiring choice (Pattern 1(a): compose applyRejectPenalty into the injected _applyDecayWeight fn at the consumer; or 1(b): multiply applyRejectPenalty after _applyDecay runs). Do NOT edit the 0.40/0.30/0.30 literals (:287-290) and do NOT edit applyDecayWeight's core (RJP-08). Keep the no-command guard path safe (D-01a: cmd with no command never suppresses - rankForSelector already skips non-string commands at :401, but a reach-mapped command with empty string must degrade to no penalty / no suppression, not throw). Preserve rankForSelector's pure-synchronous contract (no Promise, no await, no db write - the reads are synchronous chokepoint reads via roomState.db).
  </action>
  <verify>
    <automated>node tests/test-rejection-penalty.cjs</automated>
  </verify>
  <done>A >= N-rejected-within-W candidate (fences pass) is ABSENT from rankForSelector output; the same candidate at N-1 is PRESENT and discounted; a paroled candidate (every Pth presentation) re-appears; the 0.40/0.30/0.30 literals and applyDecayWeight core are unchanged.</done>
  <acceptance_criteria>
    - node tests/test-rejection-penalty.cjs exits 0 (PASS).
    - The test uses _setRegistry fixtures + the roomState injection seams (no live Brain, no RNG) to prove: candidate ABSENT at count>=N (fences pass) and PRESENT at N-1 (RJP-04 boundary).
    - The test proves the drop happens before truncation (top-K does not return a hole or a suppressed item riding a slot - RESEARCH Pitfall 3).
    - The test proves a suppressed candidate RE-SURFACES on the Pth presentation (deterministic parole) and via W-aging once rejects fall outside the window (RESEARCH Pitfall 5, double-fenced confirmation-bias).
    - grep confirms f-selector-ranker.cjs:287-290 (0.40/0.30/0.30) is byte-unchanged and applyDecayWeight core (selector-decisions.cjs:330-339) is byte-unchanged (RJP-08 prelim; the frozen-148 gate in Plan 04 is the hard proof).
    - rankForSelector remains synchronous (no async/await/Promise added).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- node tests/test-rejection-penalty.cjs exits 0 (RJP-04 hard-suppress + N-1-present + M-floor + W-aging + parole; plus the Plan 02 RJP-01/02/03 + D-02a + D-03 cases still green).
- grep: no Math.random in reject-penalty.cjs (D-06); no magic literal at the suppression check (RJP-05); 0.40/0.30/0.30 + applyDecayWeight core unchanged (RJP-08 prelim).
- The drop fires before scored.sort/slice (D-08b).
</verification>

<success_criteria>
- Hard-suppression-with-parole gated by all four fences (M floor, W aging, deterministic parole every Pth, per-room scope) - RJP-04 + D-05/D-06.
- The candidate is DROPPED before sort/slice (true drop-from-top-K, D-08b), ABSENT at N / PRESENT at N-1.
- All six constants named, no magic literal at the suppression check (RJP-05).
- Byte-stable at zero rejections preserved; the 0.40/0.30/0.30 ensemble and applyDecayWeight core untouched.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reject + presentation counts -> isHardSuppressed | local read; the drop decision must be deterministic + per-room |
| isHardSuppressed -> rankForSelector top-K | the drop removes a candidate before sort/slice; must not leak across rooms or introduce non-determinism |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-03-01 | Repudiation/Testability | non-deterministic ranking from RNG parole | mitigate | deterministic parole (presentationsCount % P), NOT Math.random (D-06); Task 1 acceptance grep asserts no Math.random |
| T-158-03-02 | Denial of Service | confirmation-bias self-lock (suppressed forever) | mitigate | double-fenced: W aging expires the streak + periodic parole force-resurfaces every Pth presentation (RESEARCH Pitfall 5; Task 2 test) |
| T-158-03-03 | Denial of Service | noise attack (1-2 off-turn rejects hide a useful reach) | mitigate | M min-presentations floor + W aging (D-05) |
| T-158-03-04 | Tampering | cross-room suppression leakage | mitigate | per-room scope only; read only the active room.db (Part 8 constitutional fence) |
| T-158-03-05 | Information Disclosure | suppression reading reason text | mitigate | isHardSuppressed consumes counts/enums only via the Plan 02 reader; no reason read (Part 8) |
| T-158-SC | Tampering | npm/pip/cargo installs | accept | zero external packages; no install task |
</threat_model>

<output>
Create `.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-03-SUMMARY.md` when done. Record the exact f-selector-ranker.cjs insertion line(s) for the hard-drop and the final IoC wiring so Plan 04's frozen-148 passthrough and Part 8/Part 9 sweeps target the right surfaces.
</output>
