---
phase: 158-bounded-rejection-penalty-seed-009-minimal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/memory-events.cjs
  - lib/workflow/reach-presentation-counter.cjs
  - tests/test-158-presentation-counter.cjs
autonomous: false
requirements: [RJP-04, RJP-05, RJP-06, RJP-07]
canon_parts: [Part 4, Part 7, Part 8, Part 9]
user_setup: []

must_haves:
  truths:
    - "A reach_presented memory_event lands in room.db per offered cmd:<command> at the dial-render consumer seam"
    - "presentationsCount(cmd) reads the reach_presented events back via navigation.findRecentChanges"
    - "The dial orchestrator (dial-reach-orchestrator.cjs) and presenter (dial-presenter.cjs) stay PURE - no db threaded in"
    - "The reach_presented payload is enum/scalar only - no reason string, no label prose"
    - "Zero rejections / zero presentations leaves all ranked output byte-identical (the counter is a side-channel, never a ranker input)"
  artifacts:
    - path: "lib/core/navigation/memory-events.cjs"
      provides: "additive 'reach_presented' EVENT_TYPES entry (and optional 'reach_suppressed'/'reach_paroled' if cheap)"
      contains: "reach_presented"
    - path: "lib/workflow/reach-presentation-counter.cjs"
      provides: "writeReachPresented (fires per offered cmd) + presentationsCount reader, both via navigation.cjs chokepoint, with the roomState.presentationsCount test-injection seam"
      exports: ["writeReachPresented", "presentationsCount"]
    - path: "tests/test-158-presentation-counter.cjs"
      provides: "deterministic suite: event lands, count reads back, payload enum/scalar only, no-command guard"
  key_links:
    - from: "lib/workflow/reach-presentation-counter.cjs"
      to: "lib/core/navigation.cjs"
      via: "logMemoryEvent (write) + findRecentChanges (read)"
      pattern: "navigation\\.(logMemoryEvent|findRecentChanges)"
    - from: "the dial consumer (located in Task 1)"
      to: "lib/workflow/reach-presentation-counter.cjs"
      via: "writeReachPresented call after renderDial, on the surface that already holds roomState.db"
      pattern: "writeReachPresented"
---

<objective>
Build the presentation counter (D-07) - the enabler the four bias fences require. Nothing in the shipped code records that a specific reach/command was OFFERED (the dial path is pure; selector_presentation is command-anonymous; suggestion_surfaced / f_selector_miss fire on other paths - 158-RESEARCH Pitfall 1). This plan adds ONE additive reach_presented memory_event, fired at the dial-render CONSUMER seam (the surface that already holds roomState.db, like closeReach does), keyed to cmd:<command>, Part 8 enum/scalar-only, written via the Part 9 navigation.cjs chokepoint.

Purpose: M-floor + periodic-parole + count-within-window (D-04/D-05) all read presentation-units; without this counter the fences cannot be built. This is the single most important plan input (158-RESEARCH §Summary finding 1).

Output: an additive EVENT_TYPES entry, a reach-presentation-counter module (writer + reader + test-injection seam), the counter wired at the located consumer seam, and the deterministic counter test.

This plan is autonomous: false because Task 1 must LOCATE the dial consumer that owns roomState.db at render time (158-RESEARCH Open Question 2 / D-10 Q2 - the one scope-risk in the phase). If no single consumer owns the db handle at render time, the executor must surface the purity-exception decision to the navigator before threading db anywhere.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-SPEC.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-CONTEXT.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-RESEARCH.md
@.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-VALIDATION.md
@CLAUDE.md
@lib/core/navigation/memory-events.cjs
@lib/workflow/dial-close-reach.cjs
@lib/hmi/dial-reach-orchestrator.cjs
@lib/hmi/dial-presenter.cjs
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Locate the dial consumer that owns roomState.db at render time (D-10 Q2 / RESEARCH Open Question 2)</name>
  <read_first>
    - lib/workflow/dial-close-reach.cjs (closeReach reads roomState.db at :226 - the caller-populates-db consumer pattern this phase mirrors)
    - lib/hmi/dial-reach-orchestrator.cjs (buildReachList :211 is PURE - "No Brain call, no fs write, no db write" :209; do NOT thread db here)
    - lib/hmi/dial-presenter.cjs (renderDial :233 PURE - "reads no room.db" :51; OFFERED_K=3 :125)
    - lib/hmi/selector-dispatcher.cjs (the dispatch flow; emitPresentationTelemetry :490 shows where a presentation side-effect already fires)
    - lib/workflow/offer-closer.cjs (closeOffer - consumer pattern with caller-populated roomState.db, named in RESEARCH Sources)
    - lib/core/navigation-engine.cjs (decide() - the engine-side consumer of the sensor spine)
  </read_first>
  <action>
    Grep for the surface(s) that call buildReachList and/or renderDial AND later route to closeReach in the same turn - that surface is the dial CONSUMER and already owns roomState.db (the same way closeReach reads roomState.db at dial-close-reach.cjs:226). Confirm which file+function is the single seam where (a) the offered reaches are known and (b) roomState.db is in hand. Per 158-RESEARCH Pitfall 1 and D-10 Q2, the counter write MUST fire at this consumer seam AFTER renderDial returns - do NOT thread db into the pure orchestrator (buildReachList) or pure presenter (renderDial). Present to the navigator: (option-a) a single consumer owns roomState.db at render time -> fire writeReachPresented there [strongly preferred per RESEARCH Assumption A1]; (option-b) no such consumer exists -> document the purity exception and thread a thin db-only counter call, flagged. The decision determines exactly where Task 4 wires the call.
  </action>
  <decision>Where does the reach_presented counter fire - at an existing dial consumer that owns roomState.db (option-a, preferred), or via a flagged purity exception threading db into the orchestrator (option-b)?</decision>
  <context>RESEARCH Assumption A1 rates option-a LOW-risk (closeReach's caller-populates-db pattern is strong evidence the consumer exists). Option-b is a scope bump that breaks the orchestrator/presenter purity invariant (Part 9: "this module just ranks what is on the table") and must be navigator-approved.</context>
  <options>
    <option id="option-a">
      <name>Fire at the located dial consumer (owns roomState.db)</name>
      <pros>Preserves orchestrator/presenter purity; mirrors the shipped closeReach consumer pattern; no scope bump.</pros>
      <cons>Requires confirming the consumer exists and owns db at render time.</cons>
    </option>
    <option id="option-b">
      <name>Thread db into a thin counter call (purity exception)</name>
      <pros>Works even if no single consumer owns db at render time.</pros>
      <cons>Breaks the pure-renderer invariant; scope bump; must be documented + navigator-approved.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b (with the exact file:function seam named)</resume-signal>
  <acceptance_criteria>
    - The exact file + function that owns roomState.db at dial-render time is named in writing (e.g. "lib/.../xyz.cjs::fn at line N").
    - The decision (option-a vs option-b) is recorded so Task 4 knows precisely where to wire writeReachPresented.
    - buildReachList and renderDial remain unmodified by this task (location only, no edits).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the additive reach_presented EVENT_TYPES entry (D-07)</name>
  <read_first>
    - lib/core/navigation/memory-events.cjs (EVENT_TYPES frozen Set :10-105; the floor-not-size additive contract documented :51-53 and :80-83; logEvent rejects event_type values outside the Set)
  </read_first>
  <action>
    Append 'reach_presented' to the EVENT_TYPES frozen Set in memory-events.cjs, mirroring the Phase 124-02 / 150-07 additive idiom verbatim (a dated comment block naming Phase 158, then the string). The Set grows ADDITIVELY - tests assert a FLOOR + named membership, never an absolute size, so this cannot regress baseline. Per D-10 Q3 and RESEARCH Open Question 3, you MAY additionally add 'reach_suppressed' and 'reach_paroled' in the SAME additive block ONLY if trivially cheap (they are observability-only, not load-bearing for the fences); otherwise add only 'reach_presented' and leave the other two for BOG-07/Phase 157. The comment must cite Canon Part 8 (payloads carry cmd:<command> + reach_id enum/scalar only; no reason text, no label prose) and Part 9 (written via the navigation chokepoint).
  </action>
  <verify>
    <automated>node tests/test-158-presentation-counter.cjs</automated>
  </verify>
  <done>'reach_presented' is a member of EVENT_TYPES; logEvent accepts it; the existing memory-events floor/membership tests stay green.</done>
  <acceptance_criteria>
    - grep finds 'reach_presented' as a quoted member inside the EVENT_TYPES Object.freeze(new Set([...])) block in memory-events.cjs.
    - The additive comment block names Phase 158 and cites Part 8 (enum/scalar only) + Part 9 (chokepoint).
    - No EVENT_TYPES entry is removed or reordered; the addition is purely additive (floor-not-size contract honored).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create reach-presentation-counter.cjs (writer + reader + injection seam) keyed to cmd:&lt;command&gt; (D-01/D-01a/D-07)</name>
  <read_first>
    - lib/core/navigation.cjs (closed surface: logMemoryEvent for the write, findRecentChanges for the read - the ONLY sanctioned door, Part 9)
    - lib/workflow/selector-decisions.cjs (_invocationsSinceDecision :276-310 - the test-injection seam idiom: roomState.invocationsSinceDecision[command] wins before any db read; mirror this exact shape for roomState.presentationsCount)
    - lib/workflow/dial-close-reach.cjs (closeReach :236-249 keys recordSelectorDecision by reach.command - the cmd:<command> collapse this counter must match so presentation count + reject count key the SAME node, per D-01)
  </read_first>
  <behavior>
    - writeReachPresented(db, reach): writes one reach_presented memory_event with payload {command:'cmd:'+reach.command, reach_id:reach.reach_id, source_path:'dial:presented:'+reach.command, created_by:'system'} via navigation.logMemoryEvent. Enum/scalar only.
    - no-command guard (D-01a): when reach.command is missing/empty (render-only family), writeReachPresented is a no-op returning {ok:false, reason:'no_command'} - NEVER throws.
    - presentationsCount(db, command, roomState): returns the injected roomState.presentationsCount[command] when it is a number (test seam, mirroring _invocationsSinceDecision :278-282); else counts reach_presented events for that command via navigation.findRecentChanges({eventType:'reach_presented'}); returns 0 when no db and no injected counter.
    - byte-stable: presentationsCount returns 0 for a never-presented command; the writer is a side-channel that never feeds the ranked array (Pitfall 4).
    - the count keys on the SAME cmd:<command> string closeReach uses, so presentation + reject counts align (D-01).
  </behavior>
  <action>
    Create lib/workflow/reach-presentation-counter.cjs (CJS, no new dependency). Export writeReachPresented(db, reach) and presentationsCount(db, command, roomState). The writer fires via navigation.logMemoryEvent only (Part 9) with the enum/scalar payload above - NEVER any reason/label/prose field (Part 8). The reader mirrors the _invocationsSinceDecision injection-seam idiom verbatim (roomState.presentationsCount[command] wins, then db read via findRecentChanges, then 0). Implement the D-01a no-command guard as a non-throwing no-op. The cmd:<command> key MUST match closeReach's recordSelectorDecision key so presentation-count and reject-count co-key the same node. Defensive type guards on every input (ASVS V5; mirror applyDecayWeight :331-333).
  </action>
  <verify>
    <automated>node tests/test-158-presentation-counter.cjs</automated>
  </verify>
  <done>writeReachPresented writes a single enum/scalar reach_presented event per offered cmd; presentationsCount reads it back (and honors the injection seam); a reach with no command yields a non-throwing no-op; all reads/writes go through navigation.cjs.</done>
  <acceptance_criteria>
    - node tests/test-158-presentation-counter.cjs exits 0 (PASS).
    - The module references navigation.logMemoryEvent and navigation.findRecentChanges and contains NO require of 'node:sqlite' / 'better-sqlite3' and NO fs read (Part 9 grep clean).
    - The reach_presented payload contains command/reach_id/source_path/created_by only; grep finds no 'reason' read or write in this file (Part 8).
    - presentationsCount honors roomState.presentationsCount[command] before any db read (injection seam present).
    - A reach object with missing reach.command returns {ok:false} from writeReachPresented and does not throw.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Wire writeReachPresented at the consumer seam located in Task 1 + write the deterministic counter test</name>
  <read_first>
    - The exact consumer file:function recorded in Task 1's decision (the surface owning roomState.db at render time)
    - lib/hmi/dial-presenter.cjs (OFFERED_K=3 :125 - fire one event per offered reach, bounded by OFFERED_K)
    - lib/memory/f-selector-ranker.test.cjs (_setRegistry :81 + idempotence :385-403 - fixture idioms to mirror)
    - tests/test-navigation-packet-part8-leak.cjs (:69-89 adversarial forbidden-substring seeding - mirror for the Part 8 assertion)
  </read_first>
  <action>
    At the consumer seam from Task 1 (option-a: the located consumer; option-b: the approved purity-exception call), after renderDial returns, call writeReachPresented(roomState.db, reach) for each of the (<= OFFERED_K=3) offered reaches. Wrap in try/catch so a counter write NEVER fails Larry's turn (mirror emitPresentationTelemetry's graceful-degradation idiom at selector-dispatcher.cjs:490-510). Then create tests/test-158-presentation-counter.cjs (plain CJS + node:assert/strict, PASS-line + non-zero-exit convention; mirror test-148-frozen-contracts.cjs structure) asserting: (1) a reach_presented event lands per offered cmd and presentationsCount reads the right count back; (2) the payload is enum/scalar only - seed a reach carrying a distinctive reason:'SECRETREASON123' and assert it NEVER appears in the written event's properties (mirror test-navigation-packet-part8-leak.cjs:69-89); (3) the no-command guard returns a non-throwing no-op; (4) presentationsCount honors the roomState.presentationsCount injection seam; (5) reach_presented is a member of EVENT_TYPES. Use an in-memory room.db fixture via the navigation chokepoint (no live Brain, no RNG).
  </action>
  <verify>
    <automated>node tests/test-158-presentation-counter.cjs</automated>
  </verify>
  <done>The counter fires per offered reach at the located consumer seam (try/catch-guarded); the deterministic test proves event-lands + count-reads-back + Part 8 payload cleanliness + no-command guard + injection seam, all without live Brain or RNG.</done>
  <acceptance_criteria>
    - node tests/test-158-presentation-counter.cjs exits 0 (PASS).
    - The write call is wrapped in try/catch at the consumer seam; buildReachList and renderDial remain unmodified (grep diff confirms no db threaded into the pure modules under option-a).
    - The test seeds reason:'SECRETREASON123' on a reach and asserts JSON.stringify of the written event properties does NOT contain 'SECRETREASON123' (Part 8 adversarial).
    - The test asserts presentationsCount returns the injected value when roomState.presentationsCount[command] is set.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- node tests/test-158-presentation-counter.cjs exits 0.
- grep over lib/workflow/reach-presentation-counter.cjs: no 'node:sqlite' / 'better-sqlite3' / fs read (Part 9); no 'reason' read/write (Part 8).
- reach_presented is in EVENT_TYPES; no EVENT_TYPES entry removed (additive floor honored).
- buildReachList / renderDial unmodified under option-a (purity preserved).
</verification>

<success_criteria>
- The dial consumer seam is located and the counter fires there (or via the navigator-approved purity exception).
- A reach_presented event lands per offered cmd:<command>, enum/scalar only, via the chokepoint.
- presentationsCount reads it back and honors the test-injection seam.
- The no-command guard (D-01a) degrades to a non-throwing no-op.
- Byte-stability is preserved (the counter is a side-channel, never a ranker input).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dial-render consumer -> room.db | the reach_presented write crosses into local memory; payload must be enum/scalar only |
| room.db -> presentationsCount reader | local read; must stay per-room (no cross-room aggregation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-01-01 | Information Disclosure | reach_presented payload (writeReachPresented) | mitigate | enum/scalar only (command/reach_id/source_path/created_by); Task 4 adversarial test seeds reason:'SECRETREASON123' and asserts it never lands in the event (Part 8) |
| T-158-01-02 | Tampering | presentationsCount cross-room read | mitigate | read only the active room.db via navigation.findRecentChanges; per-room scope (Part 8 constitutional fence) |
| T-158-01-03 | Tampering | direct sqlite/fs bypass of the chokepoint | mitigate | all reads/writes via navigation.cjs only; Plan 04 Part 9 grep sweep fails on node:sqlite/better-sqlite3/fs read |
| T-158-01-04 | Denial of Service | counter write throwing on Larry's turn | mitigate | try/catch-guarded write (mirror emitPresentationTelemetry graceful-degradation); no-command guard returns no-op (D-01a) |
| T-158-SC | Tampering | npm/pip/cargo installs | accept | this plan installs ZERO external packages (CJS + shipped modules only); no install task exists |
</threat_model>

<output>
Create `.planning/phases/158-bounded-rejection-penalty-seed-009-minimal/158-01-SUMMARY.md` when done. Record the located consumer seam (file:function:line) and the option-a/option-b decision so Plans 02 and 03 know exactly where the presentation count is sourced.
</output>
