---
phase: 175-deck-command-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/deck.md
  - data/deck-styles.json
autonomous: true
requirements: [R1, R2, R3, R4, R5, R7, R8]
canon_parts: [1, 3, 7, 8, 10, 11]
cirs_relationship:
  surfaces_added: ["/mos:deck"]
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: ["build-connector-registry", "command-resolver", "chain-executor"]
  gate_impact: "Adds one new WIRED command surface (commands/deck.md carries a connector: block); the born-wired gate (build-connector-registry.cjs --check) and the projection gate (build-orchestration-projection.cjs --check) must regenerate clean in Plan 175-03."
  explanation: "This plan authors the /mos:deck command and its connector: block so the surface is born WIRED (CIRS R1/R2). The command USES CIRS at runtime: it resolves the chosen deck style through command-resolver and hands the resolved chain to runChain (R4 one governed path). The style sub-selector and per-section gates render through the Part 3 F.1 Decision Gate; no second selection brain is introduced."
must_haves:
  truths:
    - "/mos:deck is an invokable command with a connector: block (born WIRED)"
    - "The build offers a Feynman / HEART / mesh style sub-selector via one AskUserQuestion (F.1) with an explicit Other free-text option"
    - "Each of the 3 styles routes to a distinct named generation path; mesh composes both Feynman and HEART"
    - "The HEART style emits a 5-section H/E/A/R/T structure filled from LOCAL room content; the HEART methodology text is fetched generic from Brain (Part 8: no venture content sent to Brain)"
    - "The Feynman style runs the deterministic 6-stage pipeline producing the same section structure on repeat"
    - "The build surfaces an F.1 accept/reshape/skip gate per major section"
    - "Default render binds the MindrianOS Design System + a Mindrian logo linking to https://mindrian-os.com; user brand overrides when present"
  artifacts:
    - path: "commands/deck.md"
      provides: "The /mos:deck consolidated command: style sub-selector, Feynman + HEART + mesh paths, F.1 per-section build flow, deck-design ruleset doctrine, connector: + cirs_relationship: frontmatter"
      contains: "connector:"
      min_lines: 120
    - path: "data/deck-styles.json"
      provides: "The 3 deck styles + the HEART 5-section schema (H/E/A/R/T section ids) + the Feynman 6-stage ids as a data map the command and the test read as the single source of truth"
      contains: "heart"
  key_links:
    - from: "commands/deck.md"
      to: "lib/workflow/command-resolver.cjs"
      via: "resolve the chosen style's chain through the registry door"
      pattern: "command-resolver"
    - from: "commands/deck.md"
      to: "lib/core/chain-executor.cjs"
      via: "hand the resolved chain to runChain"
      pattern: "runChain"
    - from: "commands/deck.md"
      to: "https://mindrian-os.com"
      via: "default Mindrian logo link target"
      pattern: "mindrian-os\\.com"
---

<objective>
Author `commands/deck.md`, the consolidated `/mos:deck` command, plus `data/deck-styles.json` (the style + section-schema data map). This is the single invokable surface that MOSDeckEngine and the feynman-engine merge into: a methodological, F.1-guided deck-build flow with a Feynman / HEART / mesh style sub-selector and a deck-design ruleset.

Purpose: Turn two separate conversational skills into ONE governed, born-wired command (Canon Part 10 commands-are-internals; Part 11 born-wired). The command reuses the shipped MOSDeckEngine 6-stage Feynman pipeline and the live MindrianOS Design System; it does not rebuild a deck renderer (Canon Part 7).

Output: `commands/deck.md` (the command, born WIRED via a connector: block, carrying a cirs_relationship: block) and `data/deck-styles.json` (the style + section-schema source of truth). Aliasing the old skills, the make-land repoint, the ruleset --check script, and the regression suite are Plans 175-02 and 175-03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The contract + decisions
@.planning/phases/175-deck-command-consolidation/175-SPEC.md
@.planning/phases/175-deck-command-consolidation/175-CONTEXT.md

# The engines to consolidate (REUSE, do not rebuild - Canon Part 7)
@skills/MOSDeckEngine/SKILL.md

# The born-wired command pattern to mirror (connector: block + Larry voice doctrine)
@commands/discover.md
@commands/show.md

# The one governed path the command resolves through
@lib/workflow/command-resolver.cjs
@lib/core/chain-executor.cjs

# The default Brand-Kit (logo -> mindrian-os.com; palette tokens)
@references/visual/palette.json

# The CIRS declaration contract this plan's frontmatter obeys
@docs/CIRS-RELATIONSHIP-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data/deck-styles.json (the style + section-schema source of truth)</name>
  <read_first>
    - skills/MOSDeckEngine/SKILL.md (the 6 Feynman stage names: reduce-to-essence, translate, expose-confusion, build-mental-models, simplify-until-breaks, teach-it-back)
    - data/publish-needs.json (the flat data-map idiom + the _note header convention to mirror)
    - .planning/phases/175-deck-command-consolidation/175-SPEC.md (R2 the 3 styles; R3 the HEART H/E/A/R/T 5 sections; R4 the Feynman pipeline)
  </read_first>
  <action>
    Create `data/deck-styles.json` as a flat data map mirroring the `data/publish-needs.json` idiom (a leading `_note` string documenting provenance + the no-em-dash house rule, then a top-level `version: 1`). It holds three keys. `styles`: an array of exactly three style objects `{ id, label, spine, routes_to, composes }` where the ids are `feynman` (spine "comprehension", routes_to "feynman-pipeline"), `heart` (spine "persuasion", routes_to "heart-structure"), and `mesh` (spine "composite", routes_to "mesh-compose", composes ["feynman","heart"]). `heart_sections`: an ordered array of exactly five section objects `{ id, letter, name, fill_source }` with ids/letters/names hypothesis/H/Hypothesis, enormous-stakes/E/Enormous stakes, alternatives-inadequate/A/Alternatives inadequate, radically-different/R/Radically different, team/T/Team; every `fill_source` is the literal string "local-room-content" (Canon Part 8: only LOCAL room content fills the structure; the methodology text is fetched generic from Brain at runtime, never the reverse). `feynman_stages`: an ordered array of exactly six stage objects `{ id, name }` matching the MOSDeckEngine 6 stages verbatim (reduce-to-essence, translate, expose-confusion, build-mental-models, simplify-until-breaks, teach-it-back). Use hyphens only; no em-dashes anywhere. This JSON is the SINGLE source of truth the command doctrine and the Plan 175-03 tests both read, so the three-route claim and the HEART-5-sections claim are data-asserted, not prose-asserted.
  </action>
  <verify>
    <automated>node -e "const d=require('./data/deck-styles.json'); const ids=d.styles.map(s=>s.id).sort().join(','); if(ids!=='feynman,heart,mesh')throw new Error('styles='+ids); if(d.heart_sections.length!==5)throw new Error('heart='+d.heart_sections.length); if(d.heart_sections.map(s=>s.letter).join('')!=='HEART')throw new Error('HEART order'); if(d.feynman_stages.length!==6)throw new Error('feynman='+d.feynman_stages.length); if(!d.heart_sections.every(s=>s.fill_source==='local-room-content'))throw new Error('fill_source not local'); const m=d.styles.find(s=>s.id==='mesh'); if(JSON.stringify(m.composes.sort())!==JSON.stringify(['feynman','heart']))throw new Error('mesh composes'); console.log('OK deck-styles');"</automated>
  </verify>
  <done>data/deck-styles.json exists, parses, declares exactly the 3 styles (feynman/heart/mesh), the 5 ordered HEART sections spelling H/E/A/R/T with fill_source local-room-content, the 6 Feynman stages, and mesh composes feynman+heart.</done>
</task>

<task type="auto">
  <name>Task 2: Author commands/deck.md (born-wired /mos:deck with the F.1 style selector, the 3 style paths, the per-section build flow, and the deck-design ruleset doctrine)</name>
  <read_first>
    - commands/discover.md (the connector: frontmatter block shape + the Larry voice-rules doctrine to mirror; reach_id/sub_mode/posture/hierarchy_rank/filing/surface keys)
    - commands/show.md (the F.1 AskUserQuestion + Other-option doctrine; the resolve-through-command-resolver-then-runChain idiom verbatim)
    - skills/MOSDeckEngine/SKILL.md (the 6-stage Feynman pipeline + the slide architecture to reference, not duplicate)
    - data/deck-styles.json (the styles + section schema created in Task 1)
    - references/visual/palette.json (the default Brand-Kit tokens; the logo -> mindrian-os.com link target)
    - docs/CIRS-RELATIONSHIP-CONTRACT.md (the cirs_relationship: five-field block + the canon_parts-11 auto-derivation rule)
    - docs/MINDRIAN-CANON.md Part 8 (HEART/Feynman methodology stays generic in Brain; zero venture-content egress)
  </read_first>
  <action>
    Create `commands/deck.md`. The YAML frontmatter mirrors commands/discover.md and commands/show.md exactly: `name: deck`; a `description` and `help_jtbd`; `body_shape: F.1`; `serves_jtbd: ["prepare-pitch"]`; a `teaching` line; the Phase 122 block (`kind: methodology`, `frameworks: []`, `allowed-tools: [Read, Write, Bash, Glob]`); and a `connector:` block with `connects_to_spine: true`, `sensor_triggers: []`, `reach_id: context_block` (reuse the existing reach per D-04; mint NO 7th reach), `sub_mode: deck-build`, `framework: null`, `posture: hold`, a `hierarchy_rank` integer not colliding with an existing rank (use 15; verify it is unused in data/connector-registry.json), `filing: memory_event_only`, `plan_gated: false`, `web_scope: null`, `surface: F.1`. Add the `cirs_relationship:` block verbatim from this plan's own frontmatter (surfaces_added ["/mos:deck"], spine_consumed, gate_impact, explanation) so the command file self-declares its CIRS relationship per the R12 contract. Include `canon_parts: [1, 3, 7, 8, 10, 11]` in the frontmatter (the cirs_relationship block auto-implies 11).

    The body is Larry-voiced (mirror the discover.md LOCKED voice rules: conversational, no emoji, no em-dashes, no "I"-led sentences, no fenced code blocks in the action prose). It establishes that `/mos:deck` is the consolidated deck command that MOSDeckEngine and the feynman-engine resolve to (deprecate-not-delete; the two prior skill handles are aliases routed here - state this so the alias intent is documented in the command itself). State Canon Part 7 reuse plainly: the command does NOT rebuild a deck renderer; it reuses the shipped MOSDeckEngine 6-stage pipeline (cite skills/MOSDeckEngine/SKILL.md) and the live MindrianOS Design System (cite references/visual/palette.json + assets/logo.svg + templates/destijl-base.css).

    Setup section: read references/personality/voice-dna.md for voice; read data/deck-styles.json as the single source of truth for the styles and the section schemas; resolve the active room.

    The style sub-selector (R2): render ONE AskUserQuestion (Shape F.1) offering the three style labels from data/deck-styles.json (Feynman, HEART, mesh) PLUS the mandatory "something else" Other free-text option (navigator standing preference). Each selection routes to a distinct NAMED path: Feynman -> the feynman-pipeline path, HEART -> the heart-structure path, mesh -> the mesh-compose path that invokes BOTH structures. When the navigator picks Other, interpret the free text and route to the closest style or ask one disambiguating question.

    The Feynman path (R4): run the deterministic 6-stage pipeline by its stage ids from data/deck-styles.json in order (reduce-to-essence, translate, expose-confusion, build-mental-models, simplify-until-breaks, teach-it-back), each producing a structured section. State explicitly that the same room yields the same section structure on repeat (deterministic): the stage list is fixed data, not free prose.

    The HEART path (R3): walk the five H/E/A/R/T sections by their ids from data/deck-styles.json (hypothesis, enormous-stakes, alternatives-inadequate, radically-different, team). Fetch the HEART methodology text GENERIC from the Brain (a methodology handle only - "HEART pitch model" - never venture content), and fill each section from LOCAL room content. State the Part 8 boundary in the doctrine: the methodology flows Brain -> local; the room content NEVER flows local -> Brain.

    The mesh path (R2): compose both - run the Feynman comprehension pass to clarify each idea, then arrange the clarified material into the HEART persuasion structure (cite the composes ["feynman","heart"] data).

    The methodological per-section build flow (R5): for EACH major section of the chosen structure, surface an F.1 Decision Gate offering accept / reshape / skip, filling the accepted section from room content before moving on. The gate honors the navigator's choice per section (accept keeps it, reshape re-runs that section, skip omits it).

    The deck-design ruleset doctrine (R6/R7/R8, WARN-first): state the four rules the render binds by default and that a separate `--check` (Plan 175-02) WARNs (does not FAIL) on a miss this phase (CIRS deferred-enforcement, D-04d). (1) Source linking: every sourced claim or figure carries a resolvable hyperlink to its source artifact or citation. (2) Brand + default design system: the render binds the MindrianOS Design System BY DEFAULT (Brand-Kit model - tokens/fonts/logo applied automatically) with the Mindrian logo linking to https://mindrian-os.com; user brand assets (logo, colors) override when present; the default is user-overridable. (3) Visuals + image provenance: SVG/diagram/visual-metaphor first, one main idea per slide; when an AI-generated image is used, stamp a provenance footer at a consistent location (bottom-right, 8-10pt, "AI: <tool>, <year>"). State that render reuses scripts/generate-deck.cjs / generate-presentation.cjs and the palette.json/logo.svg/destijl-base.css design system rather than emitting ad-hoc HTML.

    Resolving + running: on style selection, resolve the chosen path's chain through `lib/workflow/command-resolver.cjs` (never name a command from memory - D-03) and hand the resolved chain to `runChain` in `lib/core/chain-executor.cjs` (Canon Part 11 R4: one governed path; Canon Part 3: halt at material steps at the Decision Gate).

    Rules section: one governed path (command-resolver then runChain, no second selection brain); reuse the existing context_block reach (no 7th reach); Part 8 boundary (methodology generic from Brain, only local content fills the deck); honor all three surfaces (CLI renders the AskUserQuestion, Desktop renders it conversationally, Cowork shares the resolved deck across the room); no em-dashes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('commands/deck.md','utf8'); if(/[\u2014\u2013]/.test(t))throw new Error('em-dash present'); for(const k of ['connector:','connects_to_spine: true','reach_id: context_block','cirs_relationship:','sub_mode: deck-build']) if(!t.includes(k))throw new Error('missing '+k); for(const s of ['feynman','HEART','mesh','something else']) if(!t.toLowerCase().includes(s.toLowerCase()))throw new Error('missing style token '+s); for(const r of ['mindrian-os.com','command-resolver','runChain','bottom-right','accept','reshape','skip']) if(!t.includes(r))throw new Error('missing ruleset/flow token '+r); const cp=t.match(/canon_parts:\s*\[([^\]]*)\]/); if(!cp||!cp[1].split(',').map(x=>x.trim()).includes('11'))throw new Error('canon_parts missing 11'); console.log('OK deck.md');"</automated>
  </verify>
  <done>commands/deck.md exists with a born-wired connector: block (connects_to_spine true, reach_id context_block, sub_mode deck-build), a cirs_relationship: block, canon_parts containing 11, the F.1 style selector with Feynman/HEART/mesh + Other, the deterministic Feynman pipeline + the HEART 5-section + the mesh compose paths, the per-section accept/reshape/skip gate, the WARN-first deck-design ruleset doctrine (source links, brand auto-bind + logo->mindrian-os.com, AI-image provenance bottom-right), the resolve-through-command-resolver-then-runChain idiom, and zero em-dashes.</done>
</task>

</tasks>

<verification>
- `node -e "require('./data/deck-styles.json')"` parses and asserts 3 styles / 5 HEART sections / 6 Feynman stages.
- `commands/deck.md` exists, carries a connector: block + cirs_relationship: block + canon_parts containing 11, references all three styles + Other, the per-section accept/reshape/skip gate, the deck-design ruleset (source link, brand+logo->mindrian-os.com, AI-image provenance), and resolves through command-resolver + runChain.
- No em-dashes anywhere in commands/deck.md or data/deck-styles.json.
- The connector layer regeneration + the orchestration projection regeneration + the run-all-172/173 regression are validated in Plan 175-03 (this plan creates the surface; 175-03 wires the registries and proves the gates green).
</verification>

<success_criteria>
- `/mos:deck` exists as commands/deck.md, born WIRED (connector: block), self-declaring its CIRS relationship.
- The style sub-selector offers Feynman/HEART/mesh + Other, each a distinct route (mesh composes both), asserted against data/deck-styles.json.
- HEART emits 5 H/E/A/R/T sections filled from LOCAL content with the methodology generic from Brain (Part 8 stated in doctrine).
- Feynman is the deterministic 6-stage pipeline (fixed stage-id list).
- The build surfaces an F.1 accept/reshape/skip gate per section.
- Default render binds the MindrianOS Design System + logo -> https://mindrian-os.com; user brand overrides.
- The deck-design ruleset doctrine states source-link + AI-image-provenance + brand rules as WARN-first (deferred-enforcement).
</success_criteria>

<output>
Create `.planning/phases/175-deck-command-consolidation/175-01-SUMMARY.md` when done.
</output>
