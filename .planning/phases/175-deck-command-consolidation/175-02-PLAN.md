---
phase: 175-deck-command-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/deck-design-rules.cjs
  - scripts/check-deck-design.cjs
  - tests/test-deck-design-check.cjs
autonomous: true
requirements: [R6, R7, R8]
canon_parts: [3, 7, 8, 11]
must_haves:
  truths:
    - "A deck-design ruleset --check runs in WARN mode: it reports a sourced claim without a resolvable hyperlink without failing the build"
    - "The --check WARNs on an AI-generated image with no provenance footer (bottom-right, 8-10pt, tool + year format)"
    - "The --check confirms the default render binds the MindrianOS Design System + a Mindrian logo linking to https://mindrian-os.com"
    - "WARN-first: the --check exits 0 even when warnings are present (CIRS deferred-enforcement; hard-gate later)"
  artifacts:
    - path: "lib/core/deck-design-rules.cjs"
      provides: "The pure rule helpers: the design-system + logo-link constants, the source-link checker, the AI-image-provenance checker, and the brand-binding checker, each returning structured findings"
      exports: ["checkSourceLinks", "checkImageProvenance", "checkBrandBinding", "DESIGN_SYSTEM", "PROVENANCE_FORMAT"]
    - path: "scripts/check-deck-design.cjs"
      provides: "The CLI --check that runs the three rule helpers over a generated deck HTML file and prints WARN lines, exiting 0 (WARN-first)"
    - path: "tests/test-deck-design-check.cjs"
      provides: "The proof the --check WARNs (not fails) on a missing source link AND a missing AI-image provenance footer, and confirms the logo links to mindrian-os.com"
  key_links:
    - from: "scripts/check-deck-design.cjs"
      to: "lib/core/deck-design-rules.cjs"
      via: "require the three rule helpers"
      pattern: "deck-design-rules"
    - from: "lib/core/deck-design-rules.cjs"
      to: "references/visual/palette.json"
      via: "read the design-system tokens for the brand-binding check"
      pattern: "palette"
---

<objective>
Build the deck-design ruleset enforcement as a WARN-first `--check`: a pure rule library (`lib/core/deck-design-rules.cjs`), the CLI wrapper (`scripts/check-deck-design.cjs`), and the test proving WARN-not-FAIL behavior (`tests/test-deck-design-check.cjs`).

Purpose: The deck-design ruleset (mandatory source hyperlinks per Hebbia ISD; brand auto-binding via the Brand-Kit model + logo -> mindrian-os.com; AI-image provenance footers) is a real value lever, but it starts WARN this phase and hard-gates later (D-04d, CIRS deferred-enforcement, mirroring R6/R11). This plan ships the warning surface, not a hard gate.

Output: A `--check` that runs over a generated deck HTML file, prints WARN lines for missing source links and missing AI-image provenance, confirms the default MindrianOS Design System + logo-to-mindrian-os.com binding, and exits 0 even with warnings. This plan is independent of Plan 175-01 (no file overlap); Plan 175-03 wires the --check into the test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/175-deck-command-consolidation/175-SPEC.md
@.planning/phases/175-deck-command-consolidation/175-CONTEXT.md

# The --check exit-code discipline to mirror (WARN-first variant of this pattern)
@scripts/check-publish-needs.cjs
@scripts/build-connector-registry.cjs

# The default Brand-Kit the brand-binding check validates against (logo -> mindrian-os.com)
@references/visual/palette.json

# The shipped renderer whose output shape the checks parse (the deck HTML structure)
@scripts/generate-deck.cjs

# Best-implementation research: in-line source citation (Hebbia ISD), AI-image provenance footer format
@.planning/phases/173-publish-jtbd-need-selector/173-RESEARCH-tavily.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/core/deck-design-rules.cjs (the pure rule helpers + the design-system constants)</name>
  <read_first>
    - scripts/generate-deck.cjs (the generated deck HTML shape: the logo anchor, the slide structure, where a source link or an img would sit)
    - references/visual/palette.json (the design-system tokens the brand-binding check confirms; the base mondrian_* hex values)
    - .planning/phases/173-publish-jtbd-need-selector/173-RESEARCH-tavily.md section 3 (the AI-image provenance footer format: bottom-right, 8-10pt, "AI: <tool>, <year>"; the in-line source citation value lever)
    - .planning/phases/175-deck-command-consolidation/175-SPEC.md R6/R7/R8 (the three rules)
  </read_first>
  <behavior>
    - Test 1: checkSourceLinks(html) on a deck whose sourced claim (a slide element marked data-sourced or a claim element with a citation marker) has NO accompanying resolvable href returns a finding array with one entry of severity "warn" naming the unlinked claim.
    - Test 2: checkSourceLinks(html) on a deck where every sourced claim carries an href returns an empty findings array.
    - Test 3: checkImageProvenance(html) on a deck containing an AI-generated image (an img flagged data-ai or a generated-image marker) with NO provenance footer matching the bottom-right 8-10pt "AI: <tool>, <year>" format returns one "warn" finding.
    - Test 4: checkImageProvenance(html) on a deck whose AI image carries a conformant provenance footer returns an empty findings array.
    - Test 5: checkBrandBinding(html) on a deck whose logo anchor href is NOT https://mindrian-os.com returns one "warn" finding; on a deck binding the MindrianOS Design System with the logo linking to https://mindrian-os.com returns empty.
    - Test 6: DESIGN_SYSTEM names "MindrianOS Design System" and DESIGN_SYSTEM.logo_link === "https://mindrian-os.com"; PROVENANCE_FORMAT encodes the bottom-right + 8-10pt + tool+year contract.
  </behavior>
  <action>
    Create `lib/core/deck-design-rules.cjs` as a pure, zero-dependency CJS module (Node built-ins only; no Brain, no network - Canon Part 8). Export `DESIGN_SYSTEM` (an object naming "MindrianOS Design System", carrying `logo_link: "https://mindrian-os.com"`, and the palette token source `references/visual/palette.json`) and `PROVENANCE_FORMAT` (an object encoding the bottom-right position, the 8-10pt size band, and the "AI: <tool>, <year>" template per the Tavily research). Export three pure checkers, each taking the deck HTML string and returning an array of `{ rule, severity, message }` findings where severity is always "warn" this phase (WARN-first; the hard-FAIL flip is deferred per D-04d):
    `checkSourceLinks(html)` - scan for sourced-claim markers (a slide element bearing a sourced marker such as a `data-sourced` attribute or a citation token) that lack an accompanying resolvable hyperlink (an `href` to a source artifact or citation); each unlinked sourced claim yields one warn finding. A deck with no sourced claims, or where every sourced claim carries an href, yields an empty array.
    `checkImageProvenance(html)` - scan for AI-generated-image markers (an `img` flagged as AI-generated, e.g. a `data-ai` attribute or a generated-image class/marker) and, for each, require a provenance footer matching the bottom-right 8-10pt "AI: <tool>, <year>" format; a missing or malformed footer yields one warn finding. A deck with no AI images, or where every AI image has a conformant footer, yields an empty array.
    `checkBrandBinding(html)` - confirm the deck binds the MindrianOS Design System by default: a logo anchor whose href is `https://mindrian-os.com`, and the design-system tokens present; a logo link that is not mindrian-os.com (and no user-brand override marker) yields one warn finding. A user-brand override marker present means the brand check passes (user brand overrides the default per R7). Keep every checker a pure function of the HTML string; do not read the filesystem inside the checkers except `DESIGN_SYSTEM` may reference the palette path as a constant string. Hyphens only; no em-dashes; no fenced code in comments that would trip a grep gate.
  </action>
  <verify>
    <automated>node tests/test-deck-design-check.cjs</automated>
  </verify>
  <done>lib/core/deck-design-rules.cjs exports DESIGN_SYSTEM (logo_link https://mindrian-os.com), PROVENANCE_FORMAT (bottom-right, 8-10pt, tool+year), and the three pure WARN-severity checkers checkSourceLinks/checkImageProvenance/checkBrandBinding; all six behavior tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/check-deck-design.cjs (the WARN-first CLI) and tests/test-deck-design-check.cjs (the WARN-not-FAIL proof)</name>
  <read_first>
    - scripts/check-publish-needs.cjs (the LOCAL read-only validator + exit-code discipline + the stderr one-line recovery idiom to mirror, inverted to WARN-first)
    - lib/core/deck-design-rules.cjs (the three checkers + the constants from Task 1)
    - scripts/build-connector-registry.cjs (the WARN-via-stderr-without-exit-1 idiom: methodologyCommandsMissingConnector prints to stderr and does NOT exit 1)
  </read_first>
  <action>
    Create `scripts/check-deck-design.cjs`: a CLI taking a deck HTML file path (`node scripts/check-deck-design.cjs <deck.html>`). Read the file LOCALLY (Canon Part 8: zero Brain, zero network), run checkSourceLinks + checkImageProvenance + checkBrandBinding from lib/core/deck-design-rules.cjs, print each finding as a `[deck-design WARN]` line to stderr (mirroring the build-connector-registry.cjs methodologyCommandsMissingConnector stderr-warn idiom), print a one-line summary count, and EXIT 0 even when warnings exist (WARN-first; D-04d CIRS deferred-enforcement). Add a `--strict` flag that is RESERVED for the future hard-gate flip (when passed, document in a header comment that it will exit 1 once enforcement hard-gates; for THIS phase --strict still exits 0 so no surface depends on a hard gate that is not yet law). A missing file argument or unreadable file prints a usage line to stderr and exits 2 (operator error, distinct from a ruleset warning).

    Create `tests/test-deck-design-check.cjs`: a Node test (mirror the assert-based, no-framework idiom of the existing tests/) that (a) exercises the six Task-1 behaviors directly against the three checkers with inline HTML fixtures (a deck with a missing source link; a deck with all source links; a deck with an AI image lacking provenance; a deck with a conformant provenance footer; a deck whose logo links to mindrian-os.com; a deck whose logo does not), and (b) spawns `scripts/check-deck-design.cjs` over a fixture HTML with a missing source link AND a missing AI-image provenance footer and asserts the process exits 0 (WARN-first) while emitting the WARN lines on stderr. Use Node built-ins (assert, child_process). Hyphens only; no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-deck-design-check.cjs && node scripts/check-deck-design.cjs /dev/null; echo "exit=$?"</automated>
  </verify>
  <done>scripts/check-deck-design.cjs runs the three checkers over a deck HTML file, prints [deck-design WARN] lines to stderr, and exits 0 even with warnings (WARN-first); tests/test-deck-design-check.cjs passes, proving the --check WARNs (not fails) on a missing source link and a missing AI-image provenance footer and confirms the logo-to-mindrian-os.com binding.</done>
</task>

</tasks>

<verification>
- `node tests/test-deck-design-check.cjs` passes: all six checker behaviors plus the spawned-process WARN-not-FAIL assertion.
- `scripts/check-deck-design.cjs <deck.html>` prints `[deck-design WARN]` lines for a missing source link and a missing AI-image provenance footer, prints a summary, and exits 0.
- The checkers are pure (no Brain, no network); DESIGN_SYSTEM.logo_link === "https://mindrian-os.com".
- No em-dashes in any of the three files.
</verification>

<success_criteria>
- The deck-design ruleset --check runs in WARN mode (exits 0 with warnings present) - D-04d deferred-enforcement.
- It WARNs on a sourced claim without a resolvable hyperlink (R6).
- It WARNs on an AI-generated image without a bottom-right 8-10pt tool+year provenance footer (R8).
- It confirms the default MindrianOS Design System binding + the Mindrian logo linking to https://mindrian-os.com (R7).
</success_criteria>

<output>
Create `.planning/phases/175-deck-command-consolidation/175-02-SUMMARY.md` when done.
</output>
