---
phase: 175-deck-command-consolidation
plan: 03
type: execute
wave: 2
depends_on: ["175-01", "175-02"]
files_modified:
  - skills/MOSDeckEngine/SKILL.md
  - data/deck-aliases.json
  - data/publish-needs.json
  - commands/show.md
  - data/connector-registry.json
  - data/connector-coverage-ledger.json
  - data/orchestration-projection.json
  - data/orchestration-command-ledger.json
  - tests/test-deck-consolidation.cjs
  - tests/run-all-175.sh
autonomous: true
requirements: [R1, R2, R3, R4, R6, R7, R8, R9]
canon_parts: [3, 7, 8, 10, 11]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: ["skill:MOSDeckEngine", "/mos:show"]
  surfaces_removed: []
  spine_consumed: ["build-connector-registry", "build-orchestration-projection", "command-resolver"]
  gate_impact: "Regenerates data/connector-registry.json + data/connector-coverage-ledger.json + the orchestration projection + command ledger so the new /mos:deck surface (Plan 175-01) is WIRED at the connector layer and ranked at the projection layer; both --check gates must exit 0 with /mos:deck present. Modifies skill:MOSDeckEngine into a deprecation-redirect alias whose connector stays WIRED (deprecate-not-delete)."
  explanation: "This plan completes the born-wiring started in 175-01: it consumes the connector-registry and orchestration-projection generators to register /mos:deck, repoints the /mos:show make-land lane from the MOSDeckEngine skill handle to /mos:deck (modifying the /mos:show invocation behavior, R10 lockstep), and turns the MOSDeckEngine skill into an alias that resolves to /mos:deck. The CIRS born-wired gate is the proof surface: build-connector-registry.cjs --check exits 0 with /mos:deck WIRED."
must_haves:
  truths:
    - "build-connector-registry.cjs --check exits 0 with /mos:deck WIRED at the connector layer"
    - "build-orchestration-projection.cjs --check exits 0 with /mos:deck ranked at the projection layer"
    - "The MOSDeckEngine and feynman-engine handles both resolve to /mos:deck (alias map; deprecate-not-delete, the SKILL.md files are not deleted)"
    - "The /mos:show make-land jobs in data/publish-needs.json resolve to /mos:deck (not the MOSDeckEngine skill handle)"
    - "REACH_IDS stays length 6; run-all-172.sh and run-all-173.sh stay green"
    - "A test proves: the 3 styles route distinctly, HEART emits 5 sections, Feynman is deterministic, the ruleset --check WARNs (not fails), and the make-land jobs resolve to /mos:deck"
  artifacts:
    - path: "data/deck-aliases.json"
      provides: "The alias map: both prior skill handles (MOSDeckEngine, feynman-engine) -> /mos:deck (deprecate-not-delete, back-compat)"
      contains: "/mos:deck"
    - path: "tests/run-all-175.sh"
      provides: "The single PASS/FAIL phase gate composing the 175 behavior suite + the carried 172/173 frozen-bank regression fences"
    - path: "tests/test-deck-consolidation.cjs"
      provides: "The behavior proof: 3 distinct routes, HEART 5 sections, Feynman determinism, ruleset --check WARN-not-FAIL, make-land repoint to /mos:deck, alias resolution"
  key_links:
    - from: "data/publish-needs.json"
      to: "/mos:deck"
      via: "make-land jobs resolves_to repoint"
      pattern: "/mos:deck"
    - from: "data/connector-registry.json"
      to: "/mos:deck"
      via: "the WIRED connector entry for the new command surface"
      pattern: "/mos:deck"
    - from: "skills/MOSDeckEngine/SKILL.md"
      to: "/mos:deck"
      via: "the deprecation-redirect alias body"
      pattern: "/mos:deck"
---

<objective>
Complete the consolidation: turn MOSDeckEngine + feynman-engine into ALIASES that resolve to `/mos:deck` (deprecate-not-delete), repoint the `/mos:show` make-land lane from the skill handle to `/mos:deck`, regenerate the connector-registry and orchestration-projection registries + ledgers so `/mos:deck` is born WIRED and ranked, and ship the behavior + regression test suite.

Purpose: This is the wiring + proof plan. It makes the new surface (Plan 175-01) governed end-to-end (CIRS born-wired gate exits 0 with /mos:deck WIRED), repoints the Phase 173 make-land seam, and proves the whole phase: 3 distinct style routes, HEART 5 sections, Feynman determinism, the ruleset --check WARNs-not-fails, the make-land jobs resolve to /mos:deck, and the 172/173 frozen-bank regression fences stay green.

Output: the alias map + the MOSDeckEngine deprecation-redirect, the repointed publish-needs.json + show.md note, the regenerated four registry/ledger files, tests/test-deck-consolidation.cjs, and tests/run-all-175.sh. Depends on Plans 175-01 (commands/deck.md must exist to register) and 175-02 (the --check must exist to test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/175-deck-command-consolidation/175-SPEC.md
@.planning/phases/175-deck-command-consolidation/175-CONTEXT.md

# The surface created in 175-01 + the --check created in 175-02 (this plan registers + tests them)
@commands/deck.md
@scripts/check-deck-design.cjs
@data/deck-styles.json

# The make-land seam to repoint (the MOSDeckEngine resolves_to -> /mos:deck)
@data/publish-needs.json
@commands/show.md
@scripts/check-publish-needs.cjs

# The skill to alias (deprecate-not-delete; mirror the deprecation-redirect, keep the connector WIRED)
@skills/MOSDeckEngine/SKILL.md

# The two born-wired gates this plan regenerates + must keep green
@scripts/build-connector-registry.cjs
@scripts/build-orchestration-projection.cjs

# The carried regression fences (must stay green; REACH_IDS length 6)
@tests/run-all-172.sh
@tests/run-all-173.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alias MOSDeckEngine + feynman-engine to /mos:deck (deprecate-not-delete) and repoint the make-land lane</name>
  <read_first>
    - skills/MOSDeckEngine/SKILL.md (the existing skill body + its WIRED connector: block - keep the connector, rewrite the body to a deprecation redirect)
    - data/publish-needs.json (the make-land jobs whose resolves_to is "MOSDeckEngine"; the _note that documents the D-01 interim "route to the EXISTING deck skill until Phase 175")
    - commands/show.md (the doctrine line that routes resolves_to: MOSDeckEngine to the EXISTING skill - update it to route to /mos:deck)
    - scripts/check-publish-needs.cjs (the validator that asserts every resolves_to is a real command or skill - the /mos:deck repoint must keep it green)
    - data/deck-styles.json (the styles, to keep the alias map consistent)
  </read_first>
  <action>
    Create `data/deck-aliases.json`: a small flat data map (leading `_note` + `version: 1`) declaring the alias table `{ "MOSDeckEngine": "/mos:deck", "feynman-engine": "/mos:deck" }` (deprecate-not-delete, back-compat per D-04b). Document in the _note that both prior conversational-skill handles now resolve to the consolidated `/mos:deck` command and that the SKILL.md files are RETAINED (not deleted) as deprecation redirects. Hyphens only; no em-dashes.

    Rewrite the BODY of `skills/MOSDeckEngine/SKILL.md` into a deprecation redirect: keep the YAML frontmatter (the `name`, the `description`, and CRITICALLY the existing `connector:` block - the surface stays WIRED so the born-wired gate does not see a regressed surface), but replace the pipeline prose with a short redirect stating this engine is now consolidated into the `/mos:deck` command (the Feynman style), that the skill is retained for back-compat, and that new work should invoke `/mos:deck` and pick the Feynman style. Do NOT delete the file. Do NOT remove or alter the connector: block keys (changing connects_to_spine or reach_id would trip the connector --check). Keep the description text so the skill still triggers and routes the navigator to /mos:deck. No em-dashes.

    Repoint the make-land lane in `data/publish-needs.json`: change every job whose `resolves_to` is `"MOSDeckEngine"` to `"/mos:deck"` (the make-land lane now points at the consolidated command per R9 + D-04). Update the file's `_note` to record that the make-land lane resolves to the `/mos:deck` command (replacing the D-01 interim "route to the EXISTING deck skill until Phase 175"). Keep every other job row byte-unchanged. The repoint MUST keep scripts/check-publish-needs.cjs green: `/mos:deck` is a real command keyed in data/command-registry.json after the registry regeneration in Task 2, so order Task 2's command-registry regeneration is not required for THIS file edit, but the publish-needs --check in Task 3 runs AFTER the registry is regenerated.

    Update `commands/show.md`: change the doctrine line that routes the make-land `resolves_to: MOSDeckEngine` to the EXISTING MOSDeckEngine skill so it now states the make-land lane resolves to `/mos:deck` (the consolidated command shipped in Phase 175), routed through command-resolver + runChain like every other job. Keep the rest of show.md byte-unchanged. No em-dashes.
  </action>
  <verify>
    <automated>node -e "const a=require('./data/deck-aliases.json'); if(a['MOSDeckEngine']!=='/mos:deck'||a['feynman-engine']!=='/mos:deck')throw new Error('alias map'); const fs=require('fs'); if(!fs.existsSync('skills/MOSDeckEngine/SKILL.md'))throw new Error('skill deleted'); const s=fs.readFileSync('skills/MOSDeckEngine/SKILL.md','utf8'); if(!s.includes('connector:'))throw new Error('connector block removed'); if(!s.includes('/mos:deck'))throw new Error('redirect missing'); const p=require('./data/publish-needs.json'); if(p.jobs.some(j=>j.resolves_to==='MOSDeckEngine'))throw new Error('MOSDeckEngine resolves_to remains'); if(!p.jobs.some(j=>j.resolves_to==='/mos:deck'))throw new Error('no /mos:deck resolves_to'); const sh=fs.readFileSync('commands/show.md','utf8'); if(/[\u2014\u2013]/.test(sh))throw new Error('em-dash in show.md'); console.log('OK alias+repoint');"</automated>
  </verify>
  <done>data/deck-aliases.json maps both prior handles to /mos:deck; skills/MOSDeckEngine/SKILL.md is retained as a deprecation redirect with its connector: block intact; data/publish-needs.json make-land jobs resolve to /mos:deck (no MOSDeckEngine resolves_to remains); commands/show.md routes the make-land lane to /mos:deck; no em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the connector-registry + orchestration-projection registries and ledgers so /mos:deck is WIRED and ranked</name>
  <read_first>
    - scripts/build-connector-registry.cjs (the generator: default run writes data/connector-registry.json + data/connector-coverage-ledger.json; --check byte-compares)
    - scripts/build-orchestration-projection.cjs (the generator: writes data/orchestration-projection.json + data/orchestration-command-ledger.json; classifies /mos:deck as ranked because its connector declares a reach_id)
    - data/connector-registry.json (the on-disk registry the regeneration updates; the skill:MOSDeckEngine entry that must remain)
    - data/orchestration-command-ledger.json (the command-grained ledger; total goes 102 -> 103 with /mos:deck ranked)
    - commands/deck.md (the new surface whose connector: block drives the registration)
  </read_first>
  <action>
    Run the two generators in their default (write) mode to pick up the new commands/deck.md connector: block and emit the updated registries + ledgers: `node scripts/build-connector-registry.cjs` (writes data/connector-registry.json + data/connector-coverage-ledger.json) then `node scripts/build-orchestration-projection.cjs` (writes data/orchestration-projection.json + data/orchestration-command-ledger.json). Both must succeed (exit 0). After regeneration, confirm `/mos:deck` appears in data/connector-registry.json connectors[] as a WIRED surface (source "command", reach_id "context_block", sub_mode "deck-build") and in data/connector-coverage-ledger.json with state "wired"; confirm `/mos:deck` appears in data/orchestration-command-ledger.json with state "ranked" (it carries a reach_id, so classifyCommandNode returns ranked, NOT gap and NOT excluded - /mos:deck is a thinking/deck surface, never a utility exclude). Do NOT hand-edit these four generated files; let the generators write them. If a generator refuses (an unresolvable framework or a missing reason), the cause is upstream frontmatter (commands/deck.md framework must be null, not a fabricated name); fix the source, never the generated file. The hierarchy_rank chosen in commands/deck.md (15) must not collide in a way that trips the (sensor,reach,sub_mode) tuple validation - deck declares no sensor_triggers, so it cannot collide on a tuple; verify the connector --check passes.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs && node scripts/build-orchestration-projection.cjs && node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check && node -e "const r=require('./data/connector-registry.json'); if(!r.connectors.some(c=>c.surface==='/mos:deck'&&c.reach_id==='context_block'))throw new Error('deck not WIRED in registry'); const cl=require('./data/connector-coverage-ledger.json'); if(!cl.surfaces.some(s=>s.surface==='/mos:deck'&&s.state==='wired'))throw new Error('deck not wired in coverage ledger'); const ol=require('./data/orchestration-command-ledger.json'); if(!ol.commands.some(c=>c.command==='/mos:deck'&&c.state==='ranked'))throw new Error('deck not ranked in command ledger'); console.log('OK /mos:deck WIRED + ranked');"</automated>
  </verify>
  <done>Both generators ran clean; data/connector-registry.json + data/connector-coverage-ledger.json carry /mos:deck as WIRED (context_block / deck-build); data/orchestration-command-ledger.json carries /mos:deck as ranked; build-connector-registry.cjs --check and build-orchestration-projection.cjs --check both exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Ship the behavior + regression suite (tests/test-deck-consolidation.cjs + tests/run-all-175.sh)</name>
  <read_first>
    - tests/run-all-173.sh (the bash PASS/FAIL aggregator skeleton + the CARRIED frozen-bank drift fences list to mirror: test-reach-ids-drift.cjs, test-posture-ids-drift.cjs)
    - tests/run-all-172.sh (the pattern of invoking both --check gates directly as constituents of the phase gate)
    - data/deck-styles.json + data/deck-aliases.json + data/publish-needs.json (the data the behavior test asserts against)
    - scripts/check-deck-design.cjs (the WARN-first --check the test exercises for WARN-not-FAIL)
    - tests/test-deck-design-check.cjs (the 175-02 test the run-all-175 aggregator composes)
  </read_first>
  <action>
    Create `tests/test-deck-consolidation.cjs` (assert-based, Node built-ins, no framework, mirroring the existing tests/ idiom) proving every phase behavior from data + process exit codes (never subjective): (1) THREE DISTINCT ROUTES - data/deck-styles.json declares exactly feynman/heart/mesh, each with a distinct routes_to, and mesh.composes === ["feynman","heart"]. (2) HEART 5 SECTIONS - heart_sections has length 5, letters spell H/E/A/R/T in order, and every fill_source === "local-room-content" (Part 8: only local content fills the structure). (3) FEYNMAN DETERMINISM - feynman_stages is a fixed ordered list of the 6 stage ids (the same list on every read; the test reads it twice and deep-equals). (4) RULESET --check WARNs NOT FAILS - spawn scripts/check-deck-design.cjs over an inline fixture HTML with a missing source link AND a missing AI-image provenance footer and assert exit code 0 with WARN lines on stderr. (5) MAKE-LAND REPOINT - data/publish-needs.json has zero jobs with resolves_to "MOSDeckEngine" and at least one make-land job with resolves_to "/mos:deck". (6) ALIAS RESOLUTION - data/deck-aliases.json maps both MOSDeckEngine and feynman-engine to "/mos:deck", and skills/MOSDeckEngine/SKILL.md still exists (deprecate-not-delete) and references /mos:deck. (7) PART 8 SWEEP - commands/deck.md states the methodology-generic-from-Brain / local-content-only boundary (assert the doctrine mentions the Brain-to-local methodology direction and that room content does not egress). (8) NO EM-DASHES - assert commands/deck.md, data/deck-styles.json, data/deck-aliases.json contain no em-dash or en-dash character.

    Create `tests/run-all-175.sh` mirroring tests/run-all-173.sh exactly (bash PASS/FAIL loop, set -uo pipefail, no emoji, no em-dashes): it composes (a) the 175 suites - test-deck-design-check.cjs (175-02), test-deck-consolidation.cjs (this task); (b) BOTH born-wired gates as direct invocations - `node scripts/build-connector-registry.cjs --check` and `node scripts/build-orchestration-projection.cjs --check`; (c) the CARRIED frozen-bank regression fences proving 175 is ADDITIVE (no frozen-set move) - test-reach-ids-drift.cjs (REACH_IDS length 6, no 7th reach), test-posture-ids-drift.cjs; and (d) the cross-phase regression call-throughs - invoke `bash tests/run-all-172.sh` and `bash tests/run-all-173.sh` and fail if either is non-zero (the SPEC regression bar: run-all-172 + run-all-173 stay green). Make tests/run-all-175.sh executable.
  </action>
  <verify>
    <automated>chmod +x tests/run-all-175.sh && bash tests/run-all-175.sh</automated>
  </verify>
  <done>tests/test-deck-consolidation.cjs proves the 8 behaviors (3 distinct routes, HEART 5 sections, Feynman determinism, ruleset --check WARN-not-FAIL, make-land repoint to /mos:deck, alias resolution, Part 8 boundary doctrine, no em-dashes); tests/run-all-175.sh composes the 175 suites + both --check gates + the carried 172/173 regression and exits 0.</done>
</task>

</tasks>

<verification>
- `bash tests/run-all-175.sh` exits 0: the 175 behavior suites + both born-wired --check gates + the carried reach/posture drift fences + run-all-172.sh + run-all-173.sh all green.
- `node scripts/build-connector-registry.cjs --check` exits 0 with /mos:deck WIRED.
- `node scripts/build-orchestration-projection.cjs --check` exits 0 with /mos:deck ranked.
- `node scripts/check-publish-needs.cjs` exits 0 (the make-land repoint to /mos:deck keeps the validator green).
- REACH_IDS stays length 6 (test-reach-ids-drift.cjs green); skills/MOSDeckEngine/SKILL.md not deleted.
- No em-dashes anywhere in the touched files.
</verification>

<success_criteria>
- MOSDeckEngine + feynman-engine resolve to /mos:deck (alias map; SKILL.md retained, deprecate-not-delete) - R1.
- The /mos:show make-land jobs resolve to /mos:deck - R9.
- build-connector-registry.cjs --check exits 0 with /mos:deck WIRED; build-orchestration-projection.cjs --check exits 0 with /mos:deck ranked - R9.
- A test proves the 3 styles route distinctly, HEART emits 5 sections, Feynman is deterministic, the ruleset --check WARNs (not fails), the make-land jobs resolve to /mos:deck - R2/R3/R4/R6/R7/R8.
- REACH_IDS stays length 6; run-all-172.sh + run-all-173.sh stay green (regression bar).
</success_criteria>

<output>
Create `.planning/phases/175-deck-command-consolidation/175-03-SUMMARY.md` when done.
</output>
