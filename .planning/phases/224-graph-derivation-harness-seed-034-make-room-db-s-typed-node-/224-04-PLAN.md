---
phase: 224-graph-derivation-harness-seed-034
plan: 04
type: execute
wave: 3
depends_on: ["224-02", "224-03"]
files_modified:
  - tests/run-all-224.sh
  - lib/memory/run-feynman-tests.cjs
  - docs/ENV-TUNING.md
autonomous: true
requirements: ["Req 5", "Req 7"]
must_haves:
  truths:
    - "bash tests/run-all-224.sh exits PASS with 0 FAIL, 0 SKIP on the fully-landed phase (the SPEC phase-level gate)"
    - "A comment-stripped Part 8 sweep over every new/modified phase-224 surface finds zero network-call primitives (Req 5)"
    - "A comment-stripped Part 9 sweep proves the classifier holds no direct-db tokens and every phase-224 edge write rides navigation.cjs"
    - "package.json and package-lock.json are unchanged - zero new dependencies (locked constraint)"
    - "build-connector-registry --check, check-shape-declaration, and doctor --acceptance all pass post-implementation (Req 7)"
    - "The eight test-224 legs are registered in lib/memory/run-feynman-tests.cjs TEST_FILES (224-VALIDATION test-infrastructure contract)"
  artifacts:
    - path: "tests/run-all-224.sh"
      provides: "One-command PASS/FAIL/SKIP phase gate mirroring run-all-222.sh's run/run_if + strip_comments shape"
      min_lines: 80
    - path: "lib/memory/run-feynman-tests.cjs"
      provides: "TEST_FILES entries for the eight tests/test-224-*.cjs legs"
      contains: "test-224"
    - path: "docs/ENV-TUNING.md"
      provides: "DERIVE_CONVERGES_FLOOR / DERIVE_INFORMS_FLOOR entries with fixture-calibrated defaults"
      contains: "DERIVE_CONVERGES_FLOOR"
  key_links:
    - from: "tests/run-all-224.sh"
      to: "tests/test-224-*.cjs"
      via: "run_if-guarded node legs, partial-landing safe"
      pattern: "run_if"
    - from: "tests/run-all-224.sh"
      to: "scripts/build-connector-registry.cjs"
      via: "Req 7 structural-gate legs"
      pattern: "build-connector-registry"
---

<objective>
Ship the phase gate: tests/run-all-224.sh mirroring run-all-222.sh's aggregator shape (run/run_if
counters, comment-stripped grep sweeps so header prose never trips or masks a gate), wiring the
eight phase legs plus the permanent constitutional tripwires - Part 8 egress sweep (Req 5), Part 9
chokepoint sweep, the zero-new-deps git-diff leg, and the Req 7 structural gates
(build-connector-registry --check, check-shape-declaration.cjs, doctor --acceptance). Register the
new tests in lib/memory/run-feynman-tests.cjs per 224-VALIDATION's test-infrastructure contract and
document the D-01 floor tunables in docs/ENV-TUNING.md.

Purpose: Req 5 and Req 7 are gate requirements by nature; this plan makes them permanent,
re-runnable tripwires rather than one-time reviews, and closes the phase with the SPEC's
one-command acceptance.

Output: the harness, test registration, ENV-TUNING entries, and a full green run including the
222/218 no-regression legs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-SPEC.md
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-VALIDATION.md
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-02-SUMMARY.md
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-03-SUMMARY.md
@tests/run-all-222.sh
@lib/memory/run-feynman-tests.cjs
@docs/ENV-TUNING.md
</context>

## Artifacts this phase produces (phase-wide contract)

See the full phase-wide artifact table in 224-01-PLAN.md. This plan delivers: `tests/run-all-224.sh`
(harness + Part 8/Part 9/zero-deps/structural-gate legs), the eight `tests/test-224-*.cjs` entries in
`lib/memory/run-feynman-tests.cjs` TEST_FILES, and the `DERIVE_CONVERGES_FLOOR` /
`DERIVE_INFORMS_FLOOR` entries in `docs/ENV-TUNING.md`.

<tasks>

<task type="auto">
  <name>Task 1: tests/run-all-224.sh - phase legs + Part 8/Part 9/zero-deps/structural-gate tripwires</name>
  <files>tests/run-all-224.sh</files>
  <read_first>
    - tests/run-all-222.sh (the EXACT shape to mirror per the locked constraint: set -uo pipefail, PASS/FAIL/SKIP counters, run() and run_if() helpers, strip_comments() stripping lines starting with optional whitespace then // or * or /*, the Part 8 / Part 9 sweep structure, the git-diff zero-deps leg, the final FAIL -eq 0 exit)
    - 224-SPEC.md Requirement 5 and Requirement 7 acceptance wording (the grep targets and the three structural-gate commands)
    - lib/core/graph-derive-classifier.cjs, lib/core/migrations/phase-224-edge-review-status.cjs, scripts/gsd-graph-derive-drain.cjs, scripts/gsd-graph-derive-sweep.cjs, lib/core/graph-backfill.cjs (the sweep targets this harness pins - confirm each file's actual require lines before writing the sweep patterns so the gates assert reality, not intention)
  </read_first>
  <action>
    Create tests/run-all-224.sh cloning run-all-222.sh's structure with these legs:

    Node proof legs (run_if-guarded on each file, partial-landing safe): 224-01 migration
    (tests/test-224-migration.cjs), 224-01 classifier (tests/test-224-classifier.cjs), 224-02
    encoder-skip (tests/test-224-encoder-skip.cjs), 224-02 per-write-derive
    (tests/test-224-per-write-derive.cjs), 224-02 cost-bound (tests/test-224-cost-bound.cjs), 224-03
    backfill-idempotent (tests/test-224-backfill-idempotent.cjs), 224-03 resolver-fallback
    (tests/test-224-resolver-fallback.cjs), 224-03 proposed-only (tests/test-224-proposed-only.cjs).
    Label each leg with its Req number, matching the 222 labeling idiom.

    Part 8 sweep (Req 5): over the sweep-target list lib/core/graph-derive-classifier.cjs,
    lib/core/migrations/phase-224-edge-review-status.cjs, scripts/gsd-graph-derive-drain.cjs,
    scripts/gsd-graph-derive-sweep.cjs, and lib/core/graph-backfill.cjs, run the comment-stripped
    grep with the 222 pattern extended per the SPEC: fetch open-paren, http or https URL scheme,
    require of node:http or node:https, plus network-capable child_process invocations (curl, wget)
    - a MISSING target or any hit on an executable line fails the leg. Never use a bare grep -c on
    an unfiltered file (grep-gate hygiene: strip comments first, exactly like run-all-222's
    strip_comments idiom).

    Part 9 sweep: (a) lib/core/graph-derive-classifier.cjs must contain NO node:sqlite / DatabaseSync
    / raw INSERT INTO edges token on executable lines (it is pure classification + fs enumeration);
    (b) scripts/gsd-graph-derive-drain.cjs and lib/core/graph-backfill.cjs must contain NO raw INSERT
    INTO edges token (edge writes ride graph-derivation -> navigation.writeEdge only); (c)
    lib/core/graph-derivation.cjs must still require navigation.cjs (the chokepoint assertion,
    mirroring 222's mandatory-require leg).

    Zero-deps leg: git diff --quiet package.json package-lock.json, failing with the 222-style
    message naming the locked zero-new-deps constraint.

    Req 7 structural-gate legs (each its own run leg so a failure names the gate): node
    scripts/build-connector-registry.cjs --check, node scripts/check-shape-declaration.cjs, node
    scripts/doctor.cjs --acceptance. Per Phase 210 / CLAUDE.md Part 11 note, check-shape-declaration
    is advisory-WARN by default; run it WITHOUT --strict and treat its exit code per its shipped
    contract.

    No-regression legs (224-VALIDATION pre-verify contract): run_if legs for bash
    tests/run-all-222.sh and node tests/test-218-write-safety.cjs and node
    tests/test-graph-derive-sweep.cjs (the shared room.db write path and the Phase-169 queue
    contract).

    Close with the PASS/FAIL/SKIP banner and exit [ FAIL -eq 0 ]. bash only, no network, no emoji,
    no em-dashes, hyphens only.
  </action>
  <verify>
    <automated>bash tests/run-all-224.sh</automated>
  </verify>
  <acceptance_criteria>
    - bash tests/run-all-224.sh prints the Phase 224 PASS/FAIL/SKIP banner and exits 0 with FAIL=0 and SKIP=0 on the fully-landed phase (the SPEC gate requires 0 SKIP at phase close)
    - Deliberately planting the string fetch( on an executable line of lib/core/graph-derive-classifier.cjs and re-running flips the Part 8 leg to FAILED (tripwire proof; revert the plant immediately)
    - The zero-deps leg passes: git diff --quiet package.json package-lock.json exits 0
    - All three Req 7 structural-gate legs pass
    - shellcheck-clean enough to run under bash with set -uo pipefail (no unset-var crashes on the SKIP paths)
  </acceptance_criteria>
  <done>One command proves the whole phase: eight requirement legs, four constitutional tripwires, three structural gates, three no-regression legs, all green.</done>
</task>

<task type="auto">
  <name>Task 2: Test registration + ENV-TUNING floor docs + full phase gate run</name>
  <files>lib/memory/run-feynman-tests.cjs, docs/ENV-TUNING.md</files>
  <read_first>
    - lib/memory/run-feynman-tests.cjs (the TEST_FILES array: entries are path.join(REPO_ROOT, 'tests', '...') lines with a phase-comment above each group - append, never reorder; note the runner spawns each file in a child process so a slow leg cannot short-circuit others)
    - docs/ENV-TUNING.md (the existing entry format for env tunables like EUREKA_DIFF_FLOOR / RS_SEMANTIC_FLOOR - match its table/prose idiom exactly)
    - lib/core/graph-derive-classifier.cjs header (the calibrated default floor values and the fixture evidence recorded in Plan 01 - the ENV-TUNING entry documents THOSE numbers, not re-derived ones)
  </read_first>
  <action>
    Append the eight phase legs to TEST_FILES in lib/memory/run-feynman-tests.cjs under a single
    Phase 224 comment block (test-224-migration, test-224-classifier, test-224-encoder-skip,
    test-224-per-write-derive, test-224-cost-bound, test-224-backfill-idempotent,
    test-224-resolver-fallback, test-224-proposed-only), each as path.join(REPO_ROOT, 'tests', name)
    matching the existing entry idiom. This satisfies 224-VALIDATION's test-infrastructure row (new
    tests register in run-feynman-tests.cjs) alongside the run-all-224.sh aggregator.

    Add a Phase 224 section to docs/ENV-TUNING.md documenting DERIVE_CONVERGES_FLOOR and
    DERIVE_INFORMS_FLOOR: what each floor gates (the D-01 high band mapping to CONVERGES, the
    moderate band to INFORMS with older-INFORMS-newer direction), the fixture-calibrated default
    values copied from the classifier module header (with the calibration evidence reference), the
    precision-over-recall bias rationale (raising a floor silences the graph, lowering it risks the
    false-proposal flood CONTEXT.md designed against), and the D-04 note that no floor makes the
    system guess without the encoder - unavailability is a disclosed skip, never a lexical fallback.
    Match the file's existing entry format; no em-dashes, hyphens only, Feynman-plain wording.

    Then run the full close-out sequence and fix anything red before finishing: bash
    tests/run-all-224.sh (must end PASS with 0 FAIL 0 SKIP), MINTO_FROZEN_DATE unset plain node
    lib/memory/run-feynman-tests.cjs is NOT required to run in full here (it is a long suite) - only
    verify the eight new entries resolve to existing files so the runner cannot break on a missing
    path.
  </action>
  <verify>
    <automated>bash tests/run-all-224.sh && node -e "const p=require('node:path');const s=require('node:fs').readFileSync('lib/memory/run-feynman-tests.cjs','utf8');const names=['migration','classifier','encoder-skip','per-write-derive','cost-bound','backfill-idempotent','resolver-fallback','proposed-only'];for(const n of names){const f='test-224-'+n+'.cjs';if(!s.includes(f))throw new Error('unregistered: '+f);if(!require('node:fs').existsSync(p.join('tests',f)))throw new Error('missing file: '+f)}console.log('all 8 registered + present')"</automated>
    <human-check>Live-room backfill sanity (224-VALIDATION manual-only): run /mos:graph --derive against a copy of a real room (b2-journey shape); eyeball that the proposed CONVERGES/INFORMS edges are plausible relationships, not noise - the precision-over-recall promise holding on real prose.</human-check>
    <human-check>Foreground latency feel (224-VALIDATION manual-only): write a room artifact in a live session; confirm no perceptible post-write stall (under the 300ms foreground target) now that Step 2b enqueues + spawns instead of scoring inline.</human-check>
  </verify>
  <acceptance_criteria>
    - bash tests/run-all-224.sh exits 0 with FAIL=0 and SKIP=0
    - The node verification one-liner prints: all 8 registered + present
    - docs/ENV-TUNING.md contains DERIVE_CONVERGES_FLOOR and DERIVE_INFORMS_FLOOR entries whose default values byte-match the classifier module header's calibrated values
    - git diff --quiet package.json package-lock.json exits 0 (zero new deps held through the whole phase)
    - No em-dash character appears in any file this phase created or modified (grep sweep over the files_modified union of plans 01-04)
  </acceptance_criteria>
  <done>The phase closes with a one-command green gate, registered tests, and documented tunables; the two manual sanity checks are queued for the navigator at verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness greps -> constitutional guarantees | the Part 8/Part 9 sweeps ARE the permanent enforcement of Req 5; a self-invalidating grep silently voids the guarantee |
| dependency manifest -> supply chain | any package.json drift is an unreviewed supply-chain change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-224-15 | Repudiation | grep gates | mitigate | comment-stripped sweeps (the run-all-158/222 hygiene idiom); MISSING sweep target fails the leg rather than skipping; tripwire-plant proof in acceptance criteria |
| T-224-16 | Information disclosure | new code egressing LOCAL bytes | mitigate | Part 8 sweep over all five phase surfaces on every harness run (Req 5, permanent) |
| T-224-17 | Elevation of privilege | bypassing the navigation chokepoint | mitigate | Part 9 sweep: no direct-db tokens in the classifier, no raw edge INSERT in drain/backfill, mandatory navigation require in graph-derivation |
| T-224-SC | Tampering | npm/pip/cargo installs | mitigate | zero new dependencies (locked); git-diff leg on package.json/package-lock.json fails the harness on any drift; no install task exists anywhere in this phase |
</threat_model>

<verification>
- bash tests/run-all-224.sh exits 0 with FAIL=0 SKIP=0
- bash tests/run-all-222.sh and node tests/test-218-write-safety.cjs green (no-regression contract from 224-VALIDATION)
- node scripts/doctor.cjs --acceptance passes
</verification>

<success_criteria>
- The SPEC's phase-level acceptance line - bash tests/run-all-224.sh exits PASS with 0 FAIL, 0 SKIP - is literally true
- Req 5 and Req 7 are permanent re-runnable gates, not one-time reviews
- Eight tests registered; floor tunables documented with calibration provenance
</success_criteria>

<output>
Create `.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-04-SUMMARY.md` when done.
</output>
