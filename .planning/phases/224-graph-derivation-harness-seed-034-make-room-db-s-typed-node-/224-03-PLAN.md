---
phase: 224-graph-derivation-harness-seed-034
plan: 03
type: execute
wave: 2
depends_on: ["224-01"]
files_modified:
  - lib/core/graph-backfill.cjs
  - commands/graph.md
  - scripts/gsd-artifact-graph-hook.cjs
  - tests/test-224-backfill-idempotent.cjs
  - tests/test-224-resolver-fallback.cjs
  - tests/test-224-proposed-only.cjs
autonomous: true
requirements: ["Req 2", "Req 3", "Req 4"]
must_haves:
  truths:
    - "Running the existing /mos:graph --derive backfill against a b2-journey-shaped fixture (0 typed edges pre-run) moves the typed-edge count from 0 to N greater than 0 (Req 2)"
    - "A second backfill run on the same unchanged room leaves the typed-edge count unchanged - the Ralph invariant (Req 2)"
    - "The backfill's default deriver is the score-based producer, not the keyword-cue regex that emitted zero edges on normal prose (D-03 amended: swap the deriver, do NOT rebuild the harness)"
    - "With no .room-root sentinel on the write path, gsd-artifact-graph-hook resolves to the same room resolveWriteRoom independently returns; its duplicated registry-read logic is gone (Req 3)"
    - "Every edge this phase's derivation writes carries review_status 'proposed'; no code path this phase adds calls a confirm/promote function without an explicit byUser argument (Req 4)"
  artifacts:
    - path: "lib/core/graph-backfill.cjs"
      provides: "runDeriveBackfill driving runDerivation with buildAllPairs + scoreBasedDeriveFn default + D-04 skip/disclose"
      contains: "graph-derive-classifier"
    - path: "scripts/gsd-artifact-graph-hook.cjs"
      provides: "resolveRoomDir fallback via resolveWriteRoom (Phase-194 canonical resolver)"
      contains: "resolveWriteRoom"
    - path: "commands/graph.md"
      provides: "--derive doc reflecting the score-based default deriver and the encoder-unavailable disclosure"
    - path: "tests/test-224-backfill-idempotent.cjs"
      provides: "Req 2 zero-to-N + idempotent-re-run proof on the b2-journey fixture"
    - path: "tests/test-224-resolver-fallback.cjs"
      provides: "Req 3 same-resolution + no-duplicated-registry-read proof"
    - path: "tests/test-224-proposed-only.cjs"
      provides: "Req 4 proposed-only edge/node proof + no-confirm-without-byUser sweep"
  key_links:
    - from: "lib/core/graph-backfill.cjs"
      to: "lib/core/graph-derive-classifier.cjs"
      via: "lazy require inside runDeriveBackfill supplies scoreBasedDeriveFn + buildAllPairs (avoids a require cycle)"
      pattern: "graph-derive-classifier"
    - from: "scripts/gsd-artifact-graph-hook.cjs"
      to: "lib/core/resolve-active-room.cjs"
      via: "resolveRoomDir fallback extracts abs_path from resolveWriteRoom({filePath})"
      pattern: "resolveWriteRoom"
    - from: "lib/core/graph-backfill.cjs"
      to: "lib/core/graph-derivation.cjs"
      via: "runDerivation({roomDir, deriveFn, artifactPairs}) - the untouched Phase-169 composer"
      pattern: "runDerivation"
---

<objective>
Close Req 2 and Req 3 as the two surgical deltas RESEARCH proved they are. Req 2: the
/mos:graph --derive backfill ALREADY EXISTS (Phase 169: graph-backfill.cjs::runDeriveBackfill, the
command flag, the heal-first sequence) - D-03 as amended says this phase swaps its default
deriveFn from the keyword-cue regex (the mechanical root cause of the 0-edge gap: normal prose
never contains the literal cascade verbs) to Plan 01's score-based producer, driving the
composer with explicit full-pairwise artifactPairs. Req 3: replace the duplicated registry-read
fallback in gsd-artifact-graph-hook.cjs::resolveRoomDir with the Phase-194 canonical
resolveWriteRoom. Req 4's phase-level acceptance (proposed-only, no confirm without byUser) is
proven here against the full derivation surface.

Purpose: the backfill is what retroactively wires b2-journey and david-innovation-studio - the two
independently reconfirmed incident rooms - and the resolver fix ends the last resolution-path
disagreement SEED-034 documented.

Output: score-based backfill with D-04 disclosure, canonical resolver fallback, updated --derive
doc, three green test legs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-SPEC.md
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-CONTEXT.md
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-RESEARCH.md
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-01-SUMMARY.md
@lib/core/graph-backfill.cjs
@scripts/gsd-artifact-graph-hook.cjs
@lib/core/resolve-active-room.cjs
@commands/graph.md
</context>

## Artifacts this phase produces (phase-wide contract)

See the full phase-wide artifact table in 224-01-PLAN.md. This plan delivers: the score-based
`runDeriveBackfill` default (D-03), the `resolveWriteRoom`-backed `resolveRoomDir` fallback
(Req 3), the updated `commands/graph.md` --derive section, and the test files
`tests/test-224-backfill-idempotent.cjs`, `tests/test-224-resolver-fallback.cjs`,
`tests/test-224-proposed-only.cjs`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backfill deriver swap (D-03) + pairwise drive + D-04 disclosure + doc update</name>
  <files>lib/core/graph-backfill.cjs, commands/graph.md, tests/test-224-backfill-idempotent.cjs</files>
  <read_first>
    - lib/core/graph-backfill.cjs (full file: runDeriveBackfill's heal-first STEP 0-4 sequence which must stay byte-equivalent in ORDER; the deriveFn default at line 212; the runDerivation call at line 279 currently passed NO artifactPairs; _localCueDeriveFn and _roomArtifacts which stay in the tree as the deterministic fallback per RESEARCH State-of-the-Art)
    - lib/core/graph-derive-classifier.cjs (Plan 01 output: scoreBasedDeriveFn(step, injectOpts) and buildAllPairs(roomDir) contracts; note the classifier deliberately does NOT require graph-backfill, so the backfill may lazy-require the classifier inside the function body without a cycle)
    - lib/core/graph-derivation.cjs lines 154-200 (runDerivation accepts artifactPairs and drives deriveFn once per pair; the GDH-07 pre-propose guard that makes re-runs mint nothing)
    - commands/graph.md lines 162-204 (the shipped --derive doc section; frontmatter must NOT change - a body-only edit avoids the skill-mirror regen, verify per Req 7 note below)
    - tests/helpers/fixture-room-224.cjs (buildFixtureRoom224 default 21-artifact b2-journey shape with relatedPair/unrelatedPair)
  </read_first>
  <behavior>
    - Test 1: buildFixtureRoom224 (default b2 shape, 0 typed edges), then runDeriveBackfill({roomDir, approvedBy:'test-navigator', deriveFn: deterministic wrapper injecting a high-cosine encodeFn for the related pair}) reports typedEdgesBefore 0 and typedEdgesAfter N greater than 0, and every cascade-family edge row in room.db has review_status 'proposed' (Req 2 + Req 4)
    - Test 2 (Ralph invariant): a second identical runDeriveBackfill on the unchanged room reports typedEdgesAfter equal to the first run's count - the edge count does NOT grow (writeEdge ON CONFLICT no-op + GDH-07 node guard, both untouched)
    - Test 3: runDeriveBackfill with an injected probe forcing encoder-unavailable performs zero scoring, leaves the typed-edge count unchanged, sets a skipped field naming encoder_unavailable on its result, and logs one derivation_skipped memory event (D-04 applies to the backfill pass too)
    - Test 4: with NO deriveFn injected, the default resolves to the score-based producer: a source-level assertion that runDeriveBackfill's default path requires graph-derive-classifier, plus a behavioral assertion that a room whose prose contains zero cascade cue words still derives edges when the injected encodeFn marks a pair related (the exact keyword-regex failure mode, inverted)
  </behavior>
  <action>
    Modify lib/core/graph-backfill.cjs::runDeriveBackfill minimally (Part 7 - producer swap, not a
    rebuild; the heal-first STEP 0-4 order, the target-set assembly, and the before/after counting
    stay exactly as they are): (1) change the deriveFn default from _localCueDeriveFn to a lazy
    require of lib/core/graph-derive-classifier.cjs inside the function body, binding
    scoreBasedDeriveFn; keep _localCueDeriveFn exported and in the tree (RESEARCH State-of-the-Art:
    it remains a deterministic fallback callers can still inject). (2) in STEP 3, for each target
    room build artifactPairs with the classifier's buildAllPairs(target) and pass them into
    runDerivation({roomDir: target, deriveFn, selfCritiqueFn, artifactPairs}); process pairs in
    bounded chunks with a stderr progress line per chunk (chunk size is Claude's discretion per
    CONTEXT.md; pick a value like 50 pairs and document it). (3) D-04: before STEP 3, probe encoder
    availability once via scoreMeasured over two short literal strings, threading an opts.probeOpts
    test seam; on semantic null / warning encoder_unavailable, skip ALL derivation for this run, set
    result.skipped = 'encoder_unavailable', log one derivation_skipped memory event through
    navigation.logMemoryEvent with scalar-only payload and dedupe_key 'derivation_skipped:' +
    resolved roomDir (the EVENT_TYPES member ships in Plan 02, same wave - coordinate: if this task
    lands first, the logEvent call simply returns invalid_event_type and the soft-fail try/catch
    swallows it until Plan 02 merges; the test for behavior 3 asserts the marker only when the
    member exists, using a tolerant assertion that requires EITHER the event row OR the
    invalid_event_type envelope, and the Plan 04 harness re-asserts the strict version). The probe
    must NOT run when the caller injects deriveFn (test-seam paths stay deterministic and
    encoder-free). Also skip probing when there are zero pairs.

    Update commands/graph.md's --derive body section ONLY (frontmatter untouched): state that as of
    Phase 224 (D-03 amended) the default deriver is the score-based producer
    (graph-derive-classifier.cjs consuming rs-differential-scorer scoreMeasured, CONVERGES + INFORMS
    only per D-01), that derived edges carry review_status 'proposed' as a literal edges-table column
    (D-05) pending human confirmation at the gate, and that when the local encoder is unavailable the
    backfill SKIPS and reports encoder_unavailable with a derivation_skipped disclosure marker instead
    of guessing (D-04). Keep the heal-first sequence text as-is. After editing, run git diff on
    commands/graph.md and confirm no frontmatter line changed; if the frontmatter DID change, run
    node scripts/build-skill-mirrors.cjs (Req 7 note from RESEARCH Runtime State Inventory).

    Write tests/test-224-backfill-idempotent.cjs covering behaviors 1-4 with deterministic injected
    encodeFn wrappers (no real encoder needed in CI).
  </action>
  <verify>
    <automated>node tests/test-224-backfill-idempotent.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-224-backfill-idempotent.cjs exits 0: 0 to N, second-run count unchanged, encoder-skip leaves counts unchanged, proposed-only column values asserted
    - Source assertion: comment-stripped grep of lib/core/graph-backfill.cjs finds graph-derive-classifier on an executable line, and the deriveFn default line no longer names _localCueDeriveFn
    - _localCueDeriveFn remains exported from lib/core/graph-backfill.cjs (grep module.exports line)
    - git diff commands/graph.md shows body-only changes (no line inside the leading frontmatter block changed), or build-skill-mirrors was run and its output committed
    - bash tests/run-all-169.sh reports 0 FAIL (the Phase-169 backfill/derivation aggregate survives the producer swap)
  </acceptance_criteria>
  <done>The universal-net backfill derives real edges from normal prose, proves 0 to N and idempotent re-run on the b2-journey fixture, and discloses instead of guessing when the encoder is missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolver-fallback fix (Req 3) + phase-wide proposed-only proof (Req 4)</name>
  <files>scripts/gsd-artifact-graph-hook.cjs, tests/test-224-resolver-fallback.cjs, tests/test-224-proposed-only.cjs</files>
  <read_first>
    - scripts/gsd-artifact-graph-hook.cjs lines 86-125 (resolveRoomDir: leg 1 file-rooted resolveRoomRoot to KEEP; the env-var leg CLAUDE_ROOM_DIR/CLAUDE_ACTIVE_ROOM/ROOM_DIR to KEEP - it is a hook-contract convention shared with scripts/memory-artifact-graph-hook.cjs and scripts/memory-completion-detector.cjs, NOT registry duplication; the registry.json direct read at lines 107-119 which IS the duplication Req 3 removes)
    - lib/core/resolve-active-room.cjs lines 201-243 (resolveWriteRoom({filePath, sessionId, home}) returning {slug, abs_path, source} or null; leg order room-root, session.primary, reg.active - the Phase-194 canonical precedence)
    - lib/core/graph-derivation.cjs (runDerivation - driven directly in the Req 4 test with a stub deriveFn)
    - lib/core/navigation/edges.cjs (writeEdge post-Plan-01: the review_status param and validation)
    - 224-SPEC.md Requirement 3 and Requirement 4 (the exact acceptance wording these tests pin)
  </read_first>
  <behavior>
    - Test 1 (resolver-fallback): with a tmp MINDRIAN_ROOMS_ROOT registry fixture, a write path carrying NO .room-root sentinel and no room env vars resolves via the hook's resolveRoomDir to exactly resolveWriteRoom({filePath}).abs_path (byte-equal paths after path.resolve)
    - Test 2 (resolver-fallback): a write path WITH a .room-root sentinel still resolves file-rooted first (leg 1 unchanged - the Phase 169 GDH-01 contract survives)
    - Test 3 (resolver-fallback, source assertion): the post-fix resolveRoomDir contains no registry.json read - comment-stripped grep for registry.json and reg.rooms inside scripts/gsd-artifact-graph-hook.cjs returns nothing on executable lines (the SPEC Req 3 grep acceptance, verbatim)
    - Test 4 (proposed-only): runDerivation driven with a stub deriveFn emitting one CONVERGES and one INFORMS candidate against a fixture db lands edges rows whose review_status column reads 'proposed' AND claim nodes whose review_status reads 'proposed' (Req 4 acceptance, both layers)
    - Test 5 (proposed-only, no-confirm sweep): a comment-stripped grep across every phase-224 module (graph-derive-classifier.cjs, phase-224-edge-review-status.cjs, the modified graph-backfill.cjs, gsd-graph-derive-drain.cjs, gsd-graph-derive-sweep.cjs, the Step 2b region of intelligence-cascade.cjs) finds ZERO occurrences of confirmNode or a review_status promotion to 'confirmed' - this phase's code never confirms, with or without byUser (strictly stronger than the SPEC's no-confirm-without-byUser wording)
  </behavior>
  <action>
    Fix scripts/gsd-artifact-graph-hook.cjs::resolveRoomDir per SPEC Req 3: keep leg 1 (file-rooted
    resolveRoomRoot) and the env-var leg byte-identical (they are hook-contract conventions, not the
    duplication), then REPLACE the entire registry.json direct-read block (the try block reading
    MINDRIAN_ROOMS_ROOT/.rooms/registry.json, parsing reg.active and reg.rooms) with a call to
    resolveWriteRoom({filePath}) required from lib/core/resolve-active-room.cjs via the same
    path.resolve(__dirname, '..', 'lib', 'core', ...) idiom the file already uses for room-root.cjs;
    return the result's abs_path when it exists on disk, else the empty string (preserving the
    existing empty-string degrade contract). Update the function's header comment: the fallback now
    rides the Phase-194 canonical resolver (leg order room-root, session.primary, reg.active), which
    SUBSUMES the removed registry read and adds session awareness for free (RESEARCH Req 3 drop-in
    note). The module export shape (isPlanningMarkdown, resolveRoomDir) and the always-exit-0 CLI
    contract are untouched.

    Write tests/test-224-resolver-fallback.cjs covering behaviors 1-3: build a tmp rooms root with
    .rooms/registry.json naming an active room, point MINDRIAN_ROOMS_ROOT at it, clear
    CLAUDE_ROOM_DIR/CLAUDE_ACTIVE_ROOM/ROOM_DIR in the test's child env or process.env scope, and
    compare the hook module's resolveRoomDir(filePath) against resolveWriteRoom({filePath}).abs_path
    for a sentinel-less path; then create a .room-root sentinel case for behavior 2.

    Write tests/test-224-proposed-only.cjs covering behaviors 4-5: for behavior 4 build a small
    fixture room, call runDerivation({roomDir, deriveFn: stub, artifactPairs: [one pair]}) where the
    stub returns the two candidates, then SELECT the edges rows and claim nodes and assert both
    review_status readings; for behavior 5 implement the comment-stripped no-confirm sweep in-node
    (read each file, strip comment lines, regex for confirmNode and for a quoted confirmed literal
    adjacent to review_status in WRITE position - excluding the migration header docs and excluding
    writeEdge's own validation enum in edges.cjs which legitimately names 'confirmed').
  </action>
  <verify>
    <automated>node tests/test-224-resolver-fallback.cjs && node tests/test-224-proposed-only.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-224-resolver-fallback.cjs exits 0: sentinel-less resolution equals resolveWriteRoom's abs_path; sentinel-present stays file-rooted; no registry.json token on executable lines of the hook
    - node tests/test-224-proposed-only.cjs exits 0: edges AND claim nodes read 'proposed'; the no-confirm sweep over all phase-224 modules finds zero confirm/promote sites
    - grep -c "resolveWriteRoom" scripts/gsd-artifact-graph-hook.cjs returns at least 2 (require + call)
    - node scripts/gsd-artifact-graph-hook.cjs with an empty stdin still exits 0 (the PostToolUse soft-fail contract)
    - Existing planning-artifact hook behavior unaffected: echo of a .planning-path payload through the CLI still exits 0
  </acceptance_criteria>
  <done>Every resolution path (sentinel-present and sentinel-absent) agrees with the Phase-194 canonical resolver, and the phase's entire derivation surface is proven proposal-only end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook stdin -> room resolution | attacker-influenceable file_path drives which room.db the hook opens (T-149-11 lineage) |
| backfill -> whole-room pairwise scan | an explicit navigator-triggered O(n^2) pass over every artifact body, LOCAL only |
| derived proposals -> human trust surface | machine claims must stay proposals until a human confirms (Part 9) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-224-10 | Elevation of privilege | resolveRoomDir fallback | mitigate | resolveWriteRoom is the single audited resolver (Phase 194); existence check on abs_path before returning; strict .planning-md gate upstream unchanged |
| T-224-11 | Spoofing | registry fixture / env in tests | accept | test-scoped tmp roots; production precedence unchanged and pinned by test 2 |
| T-224-12 | Elevation of privilege | auto-confirmation of derived claims | mitigate | proposed-only column + node status asserted; no-confirm source sweep across every phase-224 module (Req 4) |
| T-224-13 | Denial of service | O(n^2) backfill on a large room | mitigate | chunked pair processing with progress lines; navigator-triggered only (never on the write path, Req 6 boundary) |
| T-224-14 | Information disclosure | backfill reading artifact bodies | mitigate | LOCAL scoring only via scoreMeasured (Part 8 audited); Plan 04 grep sweep over the modified files (Req 5) |
| T-224-SC | Tampering | npm/pip/cargo installs | mitigate | zero new dependencies; Plan 04 git-diff leg on package.json/package-lock.json |
</threat_model>

<verification>
- node tests/test-224-backfill-idempotent.cjs, node tests/test-224-resolver-fallback.cjs, node tests/test-224-proposed-only.cjs all exit 0
- Regression: bash tests/run-all-169.sh reports 0 FAIL
</verification>

<success_criteria>
- b2-journey-shaped fixture: typed edges 0 to N on first backfill, unchanged on second (Ralph invariant)
- Score-based producer is the default; keyword-cue regex retired from the default path but still exported
- Hook fallback resolution is canonically shared with Phase 194; duplicated registry read gone per the SPEC grep
- Proposed-only proven at both the edge column and the claim node, with a zero-confirm source sweep
</success_criteria>

<output>
Create `.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-03-SUMMARY.md` when done.
</output>
