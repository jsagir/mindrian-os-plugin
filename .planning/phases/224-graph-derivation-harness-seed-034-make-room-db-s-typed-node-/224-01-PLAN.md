---
phase: 224-graph-derivation-harness-seed-034
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/migrations/phase-224-edge-review-status.cjs
  - lib/core/room-db.cjs
  - lib/core/navigation/edges.cjs
  - lib/core/graph-derivation.cjs
  - lib/core/graph-derive-classifier.cjs
  - tests/helpers/fixture-room-224.cjs
  - tests/test-224-migration.cjs
  - tests/test-224-classifier.cjs
autonomous: true
requirements: ["Req 1", "Req 4"]
must_haves:
  truths:
    - "A fresh room.db opened via openRoomDb has a review_status column on the edges table (D-05)"
    - "Re-running the phase-224 migration on the same db is a no-op (sentinel-idempotent)"
    - "A pre-existing edge row keeps review_status NULL after the migration; it is never retroactively demoted to a proposal"
    - "writeEdge called with review_status 'proposed' lands a row whose review_status column reads 'proposed'; called without it, the column stays NULL"
    - "The classifier maps a scoreMeasured result to CONVERGES, INFORMS, or nothing; it can never emit CONTRADICTS, INVALIDATES, REFINES, or ROOT_CAUSES (D-01)"
    - "Threshold floors are derived from observed fixture scores, not hardcoded from intuition (D-01)"
  artifacts:
    - path: "lib/core/migrations/phase-224-edge-review-status.cjs"
      provides: "Sentinel-idempotent, transaction-wrapped ALTER TABLE edges ADD COLUMN review_status migration (D-05)"
      exports: ["runMigration", "SENTINEL_KEY"]
    - path: "lib/core/graph-derive-classifier.cjs"
      provides: "Score-band to edge-type classification layer + scoreBasedDeriveFn producer + pair builders (D-01)"
      exports: ["classifyScore", "scoreBasedDeriveFn", "buildNewArtifactPairs", "buildAllPairs"]
    - path: "tests/helpers/fixture-room-224.cjs"
      provides: "b2-journey-shaped fixture room builder (N md artifacts, structural nodes, 0 typed edges)"
      exports: ["buildFixtureRoom224"]
    - path: "tests/test-224-migration.cjs"
      provides: "Migration idempotency + backward-safety + writeEdge review_status proof"
    - path: "tests/test-224-classifier.cjs"
      provides: "D-01 band mapping proof + CONVERGES/INFORMS-only proof + calibration mode"
  key_links:
    - from: "lib/core/room-db.cjs"
      to: "lib/core/migrations/phase-224-edge-review-status.cjs"
      via: "runMigration chained in openRoomDb after runPhase222RankerWeights"
      pattern: "phase-224-edge-review-status"
    - from: "lib/core/graph-derivation.cjs"
      to: "lib/core/navigation/edges.cjs"
      via: "runDerivation passes review_status 'proposed' into navigation.writeEdge"
      pattern: "review_status.*proposed"
    - from: "lib/core/graph-derive-classifier.cjs"
      to: "lib/core/rs-differential-scorer.cjs"
      via: "scoreBasedDeriveFn consumes scoreMeasured AS-IS (never modifies the scorer)"
      pattern: "rs-differential-scorer"
---

<objective>
Lay the two foundations every other Phase 224 plan builds on: (1) the D-05 edges-table
review_status column via a phase-222-pattern migration plus the additive writeEdge extension so
derived edges literally carry review_status 'proposed' as a column (Req 4, navigator ruling
overriding the researcher's node-status recommendation), and (2) the D-01 score-based
classification layer - a NEW thin module consuming rs-differential-scorer.cjs::scoreMeasured()
AS-IS and mapping semantic bands to CONVERGES and INFORMS ONLY, with thresholds derived against
the b2-journey-shaped fixture, never from intuition.

Purpose: Req 1's per-write trigger (Plan 02) and Req 2's backfill swap (Plan 03) both inject the
scoreBasedDeriveFn this plan creates and both write edges through the review_status contract this
plan establishes. Without this wave, neither can land.

Output: migration file chained into openRoomDb, extended writeEdge, extended runDerivation edge
write, the classifier module, the shared fixture builder, and two green test legs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-SPEC.md
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-CONTEXT.md
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-RESEARCH.md
@lib/core/migrations/phase-222-ranker-weights.cjs
@lib/core/room-db.cjs
@lib/core/navigation/edges.cjs
@lib/core/graph-derivation.cjs
@lib/core/rs-differential-scorer.cjs
@tests/helpers/fixture-room-219.cjs
</context>

## Artifacts this phase produces (phase-wide contract)

| Artifact | Kind | Plan |
|----------|------|------|
| `lib/core/migrations/phase-224-edge-review-status.cjs` (`runMigration`, `SENTINEL_KEY`) | new migration module | 01 |
| `edges.review_status` column (TEXT, NULL default) in every room.db | schema change | 01 |
| `lib/core/navigation/edges.cjs::writeEdge` optional `review_status` param | function contract extension | 01 |
| `lib/core/graph-derivation.cjs::runDerivation` writes edges with review_status 'proposed' | behavior change | 01 |
| `lib/core/graph-derive-classifier.cjs` (`classifyScore`, `scoreBasedDeriveFn`, `buildNewArtifactPairs`, `buildAllPairs`, `_test`) | new module | 01 |
| `tests/helpers/fixture-room-224.cjs` (`buildFixtureRoom224`) | new test helper | 01 |
| `derivation_skipped` member in `lib/core/navigation/memory-events.cjs::EVENT_TYPES` | enum extension (D-04) | 02 |
| `scripts/gsd-graph-derive-sweep.cjs::enqueueDerive(roomDir, opts)` optional `filePath` entry field | function contract extension | 02 |
| `scripts/gsd-graph-derive-drain.cjs::drainDerive` pair-building + score-based default + D-04 skip/disclose | behavior change | 02 |
| Step 2b (derive enqueue + detached spawn) in `lib/core/intelligence-cascade.cjs::_runCascadeSteps` | behavior change (D-02) | 02 |
| `lib/core/graph-backfill.cjs::runDeriveBackfill` score-based default deriveFn + pairwise drive | behavior change (D-03) | 03 |
| `scripts/gsd-artifact-graph-hook.cjs::resolveRoomDir` fallback via `resolveWriteRoom` | behavior change (Req 3) | 03 |
| `commands/graph.md` --derive section documents the score-based deriver + encoder skip | doc change | 03 |
| `tests/test-224-migration.cjs`, `tests/test-224-classifier.cjs` | test files | 01 |
| `tests/test-224-encoder-skip.cjs`, `tests/test-224-per-write-derive.cjs`, `tests/test-224-cost-bound.cjs` | test files | 02 |
| `tests/test-224-backfill-idempotent.cjs`, `tests/test-224-resolver-fallback.cjs`, `tests/test-224-proposed-only.cjs` | test files | 03 |
| `tests/run-all-224.sh` aggregate harness + Part 8/Part 9/zero-deps/structural-gate legs | new harness | 04 |
| Test registration in `lib/memory/run-feynman-tests.cjs` TEST_FILES + `docs/ENV-TUNING.md` floor entries | registration + docs | 04 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-05 edges review_status migration + writeEdge extension + runDerivation proposed edges</name>
  <files>lib/core/migrations/phase-224-edge-review-status.cjs, lib/core/room-db.cjs, lib/core/navigation/edges.cjs, lib/core/graph-derivation.cjs, tests/test-224-migration.cjs</files>
  <read_first>
    - lib/core/migrations/phase-222-ranker-weights.cjs (the exact pattern to copy: SENTINEL_KEY, sentinelPresent via identity table, BEGIN/COMMIT/ROLLBACK)
    - lib/core/room-db.cjs (the migration chain in openRoomDb, lines 128-142; the new call appends after runPhase222RankerWeights)
    - lib/core/navigation/edges.cjs (writeEdge at lines 694-725: param validation idiom, the INSERT with ON CONFLICT(source,target,type) DO UPDATE SET properties = excluded.properties)
    - lib/core/graph-derivation.cjs (runDerivation edge write at lines 269-276; header comments state the old node-only review_status convention that D-05 now extends)
    - lib/core/lazygraph-ops.cjs lines 40-62 (the edges CREATE TABLE IF NOT EXISTS baseline schema the migration alters)
  </read_first>
  <behavior>
    - Test 1: after openRoomDb(tmpRoom), PRAGMA table_info(edges) lists a review_status column (the chain ran)
    - Test 2: calling runMigration(db) a second time returns applied false and changes nothing (sentinel idempotency, double-run proof per 224-VALIDATION Wave 0)
    - Test 3: a legacy db built with the pre-224 4-column edges schema plus one inserted row, then migrated, keeps that row with review_status NULL (backward-safe, never demoted)
    - Test 4: writeEdge with review_status 'proposed' produces a row whose review_status column is 'proposed'; writeEdge without the param leaves NULL; writeEdge with review_status 'bogus' returns ok false, reason invalid_review_status
    - Test 5: an existing edge row manually UPDATEd to review_status 'confirmed', then re-written via writeEdge with review_status 'proposed', still reads 'confirmed' (upsert never touches review_status on conflict)
    - Test 6: runDerivation driven with a stub deriveFn emitting one CONVERGES candidate lands an edges row with review_status 'proposed' AND a claim node with review_status 'proposed'
  </behavior>
  <action>
    Create lib/core/migrations/phase-224-edge-review-status.cjs copying the phase-222-ranker-weights.cjs
    shape exactly (per D-05): SENTINEL_KEY 'phase_224_edge_review_status_v1', sentinelPresent(db) probing
    the identity table, runMigration(db) returning {applied:false} when the sentinel exists, else BEGIN,
    a defensive PRAGMA table_info(edges) probe that only runs ALTER TABLE edges ADD COLUMN review_status
    TEXT DEFAULT NULL when the column is absent (protects a partially-migrated db), sentinel INSERT OR
    REPLACE into identity, COMMIT, ROLLBACK-and-rethrow on error. Document the existing-rows default
    semantics IN the file header (Claude's discretion item, D-05): review_status NULL means a pre-224 or
    non-derivation edge and is treated as NOT-a-proposal by every consumer; NULL rows are never
    retroactively demoted, and NULL is chosen over a 'confirmed' backfill so legacy edges are not
    silently promoted to human-ratified either.

    Chain it in lib/core/room-db.cjs::openRoomDb: require runMigration as runPhase224EdgeReviewStatus
    next to the phase-222 require, and call it immediately after runPhase222RankerWeights(db) with a
    one-line comment naming Phase 224-01 / D-05.

    Extend lib/core/navigation/edges.cjs::writeEdge additively: params gains an OPTIONAL review_status.
    Validation follows the existing idiom: when present it must be exactly 'proposed' or 'confirmed',
    else return {ok:false, reason:'invalid_review_status'}; when absent, bind NULL. The INSERT column
    list becomes (source, target, type, properties, review_status); the ON CONFLICT clause stays
    DO UPDATE SET properties = excluded.properties ONLY - review_status is set at first insert and
    NEVER changed by an upsert, which is what makes re-derivation idempotent (the Ralph invariant),
    never downgrades a confirmed edge, and never demotes a NULL legacy edge (Req 4). Every existing
    caller omits the param and is byte-compatible.

    Extend lib/core/graph-derivation.cjs: in candidateToFinding keep edge properties enum/scalar-only
    exactly as-is, and in runDerivation's navigation.writeEdge call add review_status: 'proposed'
    (D-05 amends the Part 9 Pitfall-1 header comment: update that comment to say review_status now
    lives on BOTH the claim node and, as of Phase 224 D-05, the edge row's own column). Do not touch
    the GDH-07 node pre-propose guard or any other logic.

    Write tests/test-224-migration.cjs as a zero-deps node assert script (repo convention, mirror
    tests/test-222-weight-state.cjs style) covering the six behaviors above, building throwaway dbs
    under fs.mkdtempSync(os.tmpdir()). For Test 3 construct the legacy db with a raw DatabaseSync
    handle and the pre-224 CREATE TABLE edges statement copied from lazygraph-ops.cjs, insert one row,
    then call runMigration directly.
  </action>
  <verify>
    <automated>node tests/test-224-migration.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-224-migration.cjs exits 0 with all six behaviors asserted
    - grep -c "phase-224-edge-review-status" lib/core/room-db.cjs returns at least 2 (require + call)
    - grep -n "invalid_review_status" lib/core/navigation/edges.cjs returns exactly one validation site inside writeEdge
    - grep -n "review_status" lib/core/graph-derivation.cjs shows the writeEdge call passing 'proposed'
    - The ON CONFLICT clause in writeEdge does NOT reference review_status (source assertion: grep "excluded.review_status" lib/core/navigation/edges.cjs returns nothing)
    - node tests/test-222-weight-state.cjs and node tests/test-graph-derive-sweep.cjs still exit 0 (no regression on the shared migration chain and writeEdge callers)
  </acceptance_criteria>
  <done>Every room.db opened through openRoomDb carries edges.review_status; derived edges land 'proposed' as a literal column value; legacy rows stay NULL; re-runs and confirmed rows are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-01 classifier module + pair builders + b2-journey fixture + fixture-derived thresholds</name>
  <files>lib/core/graph-derive-classifier.cjs, tests/helpers/fixture-room-224.cjs, tests/test-224-classifier.cjs</files>
  <read_first>
    - lib/core/rs-differential-scorer.cjs lines 483-616 (scoreMeasured signature: (a, b, opts) with opts.vectors cheap path, opts.encodeFn test seam, opts._forceUnavailable; output shape {semantic, lexical, band, warning}; semantic null when encoder unavailable)
    - lib/core/graph-derivation.cjs lines 154-200 (the deriveFn producer contract runDerivation drives: deriveFn({roomDir, artifactPair, llm}) returning an array of {source, target, edge_type, reason} tuples; CASCADE_SUBSET guard)
    - lib/core/graph-backfill.cjs lines 53-151 (_roomArtifacts enumeration + the 'artifact:' + basename id convention + _localCueDeriveFn, the keyword producer being replaced as default; note its docx/html awareness is OUT of 224 scope)
    - lib/core/graph-ops.cjs and lib/core/lazygraph-ops.cjs (confirm the artifact node id convention indexArtifact mints, so pair ids attach to real indexed nodes; fall back to the backfill's 'artifact:' + basename convention when node lookup misses)
    - tests/helpers/fixture-room-219.cjs (the fixture-builder idiom to mirror: writeFileUnder, openRoomDb-created .mindrian, returned ids map)
    - 224-CONTEXT.md decisions block (D-01 full text: CONVERGES high band, INFORMS moderate band with older-INFORMS-newer direction, stance types excluded, precision over recall)
  </read_first>
  <behavior>
    - Test 1: classifyScore over a sweep of semantic values 0.0 to 1.0 in 0.05 steps only ever returns 'CONVERGES', 'INFORMS', or null; a membership assertion proves CONTRADICTS, INVALIDATES, REFINES, ROOT_CAUSES can never be emitted (D-01)
    - Test 2: scoreBasedDeriveFn with an injected deterministic scoreFn returning semantic above the CONVERGES floor emits exactly one CONVERGES candidate for the pair; a mid-band score emits exactly one INFORMS candidate whose source is the OLDER artifact and target the NEWER (mtime-directed); a below-floor score emits []
    - Test 3: scoreBasedDeriveFn returns [] when the injected scoreFn reports semantic null / warning encoder_unavailable (the D-04 upstream contract: silence here, the disclosure marker is the caller's job)
    - Test 4: buildNewArtifactPairs(roomDir, filePath) on a fixture with N existing artifacts plus the new file returns exactly N pairs, each pairing the new artifact with one existing artifact, never the new artifact with itself; buildAllPairs returns exactly N-choose-2 pairs with no duplicates
    - Test 5 (real-encoder leg, self-skipping): when the local encoder loads, the fixture's related pair classifies CONVERGES or INFORMS and the unrelated pair classifies null; when the encoder is unavailable the leg prints SKIP and exits 0
  </behavior>
  <action>
    Create lib/core/graph-derive-classifier.cjs (module naming per Claude's discretion in CONTEXT.md),
    pure CJS, zero new deps, requiring ONLY node built-ins plus lib/core/rs-differential-scorer.cjs.
    It must NOT require node:sqlite, DatabaseSync, navigation.cjs, or graph-backfill.cjs (keeps the
    Part 9 sweep clean and avoids a require cycle with graph-backfill). Exports:

    1. classifyScore(scoreResult, meta) - a pure function reading scoreResult.semantic (the topical
       cosine, per RESEARCH note A1 - NOT abs_diff) and returning {edge_type:'CONVERGES'} when
       semantic >= CONVERGES_FLOOR, {edge_type:'INFORMS'} when semantic >= INFORMS_FLOOR, else null.
       Floors resolve from env DERIVE_CONVERGES_FLOOR / DERIVE_INFORMS_FLOOR (matching the
       RS_SEMANTIC_FLOOR naming convention) with module defaults DERIVED FROM THE FIXTURE per D-01,
       see calibration below.
    2. scoreBasedDeriveFn(step, injectOpts) - matches the runDerivation producer contract on its
       first arg ({roomDir, artifactPair, llm}); injectOpts is a test seam carrying an optional
       scoreFn (default rs-differential-scorer scoreMeasured) and scoreOpts merged into the
       scoreMeasured opts (so tests inject encodeFn or _forceUnavailable deterministically).
       Returns [] on a missing/degenerate pair, [] when the score result has semantic null (D-04
       upstream silence), one candidate tuple {source, target, edge_type, reason} otherwise, where
       reason is a short scalar naming the band ('high semantic similarity' / 'moderate semantic
       similarity'). CONVERGES keeps pair order; INFORMS directs source = older artifact, target =
       newer artifact using pair mtimeMs metadata, falling back to given pair order when mtimes are
       missing or equal (D-01 older-INFORMS-newer). When both pair members carry a vector field, pass
       opts.vectors to skip re-embedding (RESEARCH Pitfall 4); absence must not break anything.
    3. buildNewArtifactPairs(roomDir, newFilePath) - the Req 6 O(n) pair builder: enumerate the
       room's markdown artifacts with a local recursive walker that skips dot-directories, skips any
       subdirectory carrying its own .room-root sentinel (sub-room sweep is deferred, SEED-034 pipe 2),
       and skips the structural scaffold basenames ROOM.md, STATE.md, MINTO.md, FEYNMAN.md, BRAIN.md
       (precision over recall: scaffold-to-content edges are noise). md-only by design in this phase
       (non-md readability deferred, pipe 3). Each artifact object carries {id, path, text, mtimeMs}
       where id follows the indexed artifact node id convention discovered in graph-ops/lazygraph-ops
       (fallback: 'artifact:' + basename, matching graph-backfill). Returns [{a: newArtifact,
       b: existing_i}] for every existing artifact except the new file itself: exactly N pairs.
    4. buildAllPairs(roomDir) - the backfill's full pairwise builder over the same enumeration:
       N-choose-2 unique unordered pairs, each ordered by mtimeMs ascending (a = older) so the
       INFORMS direction rule composes.
    Also export _test with the resolved floor values and the enumerator for white-box assertions.

    Create tests/helpers/fixture-room-224.cjs exporting buildFixtureRoom224(tmpDir, opts) that mirrors
    fixture-room-219's idiom: build tmpDir/room with a .room-root sentinel file, ROOM.md, and by
    default 21 markdown artifacts echoing the b2-journey shape (SPEC Req 2 fixture), of which two are
    a RELATED pair (same venture topic paraphrased with distinct wording, at least 200 words each so
    the encoder has signal) and the rest span clearly unrelated domains; open room.db via openRoomDb
    and index every artifact through graph-ops indexArtifact so structural nodes + BELONGS_TO exist
    and typed-edge count starts at 0. Return {roomDir, dbPath, relatedPair, unrelatedPair, allFiles}.
    Support opts.artifactCount for small fast variants used by the Plan 02 cost-bound test.

    CALIBRATION (D-01 hard requirement - thresholds from fixture, not intuition): give
    tests/test-224-classifier.cjs a --calibrate mode that, with the REAL encoder, runs scoreMeasured
    over the fixture's related pair, unrelated pair, and a sample of cross-domain pairs, printing each
    observed semantic value. Run it once during this task; set CONVERGES_FLOOR above the highest
    observed unrelated-pair semantic with clear margin and INFORMS_FLOOR between, biased upward
    (precision over recall, false-CONTRADICTS-class noise is the designed-against failure mode per
    CONTEXT.md specifics). Record the observed values and the chosen floors in the classifier module
    header as the calibration evidence. If the encoder cannot load in this environment, STOP and
    surface the blocker to the user rather than shipping intuition numbers.

    Write tests/test-224-classifier.cjs covering behaviors 1-5; behaviors 1-4 run with injected
    deterministic scoreFn/encodeFn (CI-safe, no model download); behavior 5 probes the encoder with a
    tiny scoreMeasured call first and prints SKIP + exits 0 when semantic is null.
  </action>
  <verify>
    <automated>node tests/test-224-classifier.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-224-classifier.cjs exits 0; behaviors 1-4 always run; behavior 5 runs or prints SKIP
    - Source assertion: grep -E "CONTRADICTS|INVALIDATES|REFINES|ROOT_CAUSES" lib/core/graph-derive-classifier.cjs matches only comment lines (the D-01 exclusion is structural, not accidental); verify with the comment-stripped idiom from tests/run-all-222.sh
    - Source assertion: grep -E "node:sqlite|DatabaseSync|graph-backfill" lib/core/graph-derive-classifier.cjs on executable lines returns nothing
    - The classifier module header contains a calibration table with observed semantic values from the fixture and the derived DERIVE_CONVERGES_FLOOR / DERIVE_INFORMS_FLOOR defaults
    - buildNewArtifactPairs on a 5-artifact fixture plus 1 new file returns exactly 5 pairs (asserted in test); buildAllPairs on 5 artifacts returns exactly 10 pairs
    - The fixture room reports 0 rows for SELECT COUNT(*) FROM edges WHERE type IN the cascade family after build (asserted in test)
  </acceptance_criteria>
  <done>The classification layer exists as a thin, fixture-calibrated, CONVERGES/INFORMS-only producer matching runDerivation's deriveFn contract, plus the shared pair builders and the b2-journey fixture every later plan reuses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration -> room.db edges table | schema change on a core table every room carries; a botched ALTER corrupts every consumer |
| derivation writer -> edges rows | machine-derived claims enter the human-trust surface; mislabeling defeats Part 9 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-224-01 | Tampering | phase-224 migration | mitigate | sentinel-idempotent + BEGIN/COMMIT/ROLLBACK + defensive PRAGMA column probe; double-run test leg proves no drift |
| T-224-02 | Elevation of privilege | writeEdge review_status | mitigate | enum validation ('proposed'/'confirmed' only); upsert never mutates review_status, so no writer can promote or demote an existing row; confirm stays a separate byUser path (Req 4) |
| T-224-03 | Repudiation | legacy NULL edges | accept | NULL documented as not-a-proposal in the migration header; no retroactive relabeling, low risk by design |
| T-224-04 | Information disclosure | classifier module reading artifact text | mitigate | LOCAL fs reads only; zero network primitives, enforced by Plan 04's Part 8 comment-stripped grep sweep (Req 5) |
| T-224-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new dependencies this phase (locked constraint); Plan 04 git-diff leg on package.json/package-lock.json fails the harness on any drift |
</threat_model>

<verification>
- node tests/test-224-migration.cjs exits 0
- node tests/test-224-classifier.cjs exits 0
- Regressions: node tests/test-graph-derive-sweep.cjs, node tests/test-222-weight-state.cjs, node tests/test-218-write-safety.cjs all exit 0 (shared room.db open path + migration chain)
</verification>

<success_criteria>
- edges.review_status exists on fresh and migrated dbs; NULL-legacy, proposed-insert, confirmed-preserve semantics all proven by test
- classifyScore is structurally incapable of emitting a stance-requiring edge type (D-01)
- Threshold floors are calibration-derived with printed fixture evidence recorded in the module header
- Pair builders deliver exactly N (per-write) and N-choose-2 (backfill) pairs with sub-rooms and scaffolds excluded
</success_criteria>

<output>
Create `.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-01-SUMMARY.md` when done, recording the calibrated floor values and the documented NULL semantics decision.
</output>
