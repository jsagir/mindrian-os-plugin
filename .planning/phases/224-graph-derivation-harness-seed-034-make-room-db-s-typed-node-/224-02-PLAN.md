---
phase: 224-graph-derivation-harness-seed-034
plan: 02
type: execute
wave: 2
depends_on: ["224-01"]
files_modified:
  - lib/core/navigation/memory-events.cjs
  - scripts/gsd-graph-derive-sweep.cjs
  - scripts/gsd-graph-derive-drain.cjs
  - lib/core/intelligence-cascade.cjs
  - tests/test-224-encoder-skip.cjs
  - tests/test-224-per-write-derive.cjs
  - tests/test-224-cost-bound.cjs
autonomous: true
requirements: ["Req 1", "Req 6"]
must_haves:
  truths:
    - "A markdown write through the shared cascade enqueues a derive request and spawns a detached background worker; scoring NEVER runs inline in the foreground cascade (D-02 refined)"
    - "Draining a per-write queue entry over a fixture with a related pair lands at least one non-BELONGS_TO typed edge with review_status 'proposed'; an unrelated pair lands zero (Req 1)"
    - "A drain over N existing artifacts plus 1 new write makes exactly N scoreMeasured calls, not N squared and not N-choose-2 (Req 6)"
    - "When the encoder is unavailable the derivation pass skips entirely and writes a derivation_skipped disclosure marker; never a silent no-op, never a lexical-only fallback, never a blocked write (D-04)"
    - "The Phase-169 Stop sweep and SessionStart drain stay registered and now ride the same score-based deriveFn (RESEARCH OQ2/OQ3)"
  artifacts:
    - path: "lib/core/intelligence-cascade.cjs"
      provides: "Step 2b derive-enqueue + detached-spawn inside _runCascadeSteps per-artifact loop"
      contains: "enqueueDerive"
    - path: "scripts/gsd-graph-derive-drain.cjs"
      provides: "Pair-building drain with score-based default deriveFn + D-04 probe/skip/disclose"
      contains: "graph-derive-classifier"
    - path: "scripts/gsd-graph-derive-sweep.cjs"
      provides: "enqueueDerive with optional filePath-scoped entries, deduped"
    - path: "lib/core/navigation/memory-events.cjs"
      provides: "derivation_skipped EVENT_TYPES member (SEED-059 disclosure home)"
      contains: "derivation_skipped"
    - path: "tests/test-224-per-write-derive.cjs"
      provides: "Req 1 related/unrelated + proposed-status + cascade-wiring proof"
    - path: "tests/test-224-cost-bound.cjs"
      provides: "Req 6 exact-N scorer call-count proof"
    - path: "tests/test-224-encoder-skip.cjs"
      provides: "D-04 skip + disclosure-marker proof via the _forceUnavailable seam"
  key_links:
    - from: "lib/core/intelligence-cascade.cjs"
      to: "scripts/gsd-graph-derive-sweep.cjs"
      via: "Step 2b calls enqueueDerive(roomDir, {filePath}) then spawns the drain detached"
      pattern: "enqueueDerive"
    - from: "scripts/gsd-graph-derive-drain.cjs"
      to: "lib/core/graph-derive-classifier.cjs"
      via: "drainDerive injects scoreBasedDeriveFn + buildNewArtifactPairs/buildAllPairs into runDerivation"
      pattern: "scoreBasedDeriveFn|graph-derive-classifier"
    - from: "scripts/gsd-graph-derive-drain.cjs"
      to: "lib/core/navigation.cjs"
      via: "logMemoryEvent(db, 'derivation_skipped', payload) on encoder-unavailable"
      pattern: "derivation_skipped"
---

<objective>
Wire Req 1's per-write trigger: a new Step 2b in the shared intelligence cascade (D-02 - the ONE
body both the CLI post-write hook and the MCP tool-router call, so all three surfaces get
derivation, tri-polar) that ENQUEUES to the existing graph-derive-queue.json and spawns a DETACHED
worker (D-02 refined: spawn detached true plus unref, the async-artifact-auto-commit pattern),
never running scoreMeasured on the write-lock. The drain becomes the worker: it builds O(n)
new-artifact-vs-existing pairs (Req 6), injects Plan 01's scoreBasedDeriveFn into the existing
Phase-169 runDerivation composer (Part 7: producer swap, not a new engine, per D-03 amended), and
implements D-04: encoder unavailable means skip everything and write a derivation_skipped
disclosure marker - soft-fail, advisory, never blocking (Phase 210 caution).

Purpose: this is the phase's headline behavior - the twice-reconfirmed 0-typed-edge gap closes on
every normal conversational write, on every surface.

Output: cascade Step 2b, filePath-aware queue entries, the pair-building score-based drain, the
derivation_skipped event type, and three green test legs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-SPEC.md
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-CONTEXT.md
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-RESEARCH.md
@.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-01-SUMMARY.md
@lib/core/intelligence-cascade.cjs
@scripts/gsd-graph-derive-sweep.cjs
@scripts/gsd-graph-derive-drain.cjs
@lib/core/graph-derive-classifier.cjs
@scripts/async-artifact-auto-commit.cjs
</context>

## Artifacts this phase produces (phase-wide contract)

See the full phase-wide artifact table in 224-01-PLAN.md. This plan delivers: the
`derivation_skipped` EVENT_TYPES member, `enqueueDerive(roomDir, opts)` filePath entries, the
pair-building score-based `drainDerive`, cascade Step 2b (enqueue + detached spawn) in
`_runCascadeSteps`, and the test files `tests/test-224-encoder-skip.cjs`,
`tests/test-224-per-write-derive.cjs`, `tests/test-224-cost-bound.cjs`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: filePath-aware queue + score-based pair-building drain + D-04 skip/disclose</name>
  <files>lib/core/navigation/memory-events.cjs, scripts/gsd-graph-derive-sweep.cjs, scripts/gsd-graph-derive-drain.cjs, tests/test-224-encoder-skip.cjs</files>
  <read_first>
    - scripts/gsd-graph-derive-sweep.cjs (enqueueDerive at lines 111-129: the {entries:[{roomDir, enqueued_at}]} queue shape and the resolved-roomDir dedupe to extend, not replace)
    - scripts/gsd-graph-derive-drain.cjs (drainDerive at lines 68-119: the entry loop, the opts.deriveRunner injection seam the Phase-169 round-trip test depends on, the clear-by-rewrite idiom)
    - tests/test-graph-derive-sweep.cjs lines 40-78 (the FOUR existing assertions that must stay green: enqueue, idempotent re-enqueue, drain-calls-runDerivation-with-roomDir, empty-queue no-op; the spy only reads argObj.roomDir so additive args are safe, but the D-04 probe must NOT fire when a deriveRunner is injected or the spy path breaks)
    - lib/core/navigation/memory-events.cjs (EVENT_TYPES set; the reach_weight_state_unavailable entry near line 638 is the exact SEED-059 additive-disclosure precedent to copy, comment style included; logEvent(db, eventType, payload, opts) signature with the payload.dedupe_key 60s idempotency window)
    - lib/core/graph-derive-classifier.cjs (Plan 01 output: scoreBasedDeriveFn(step, injectOpts), buildNewArtifactPairs, buildAllPairs contracts)
    - lib/core/rs-differential-scorer.cjs lines 483-520 (opts._forceUnavailable test seam and the semantic-null encoder-unavailable envelope)
    - .planning/seeds/SEED-059-fallback-disclosure-convention.md (the disclosure convention D-04 follows: mark at point of occurrence, additive, non-blocking, checkable)
  </read_first>
  <behavior>
    - Test 1 (in test-224-encoder-skip.cjs): drainDerive over a fixture room queue entry with probe opts forcing encoder-unavailable performs ZERO scoring, writes ZERO typed edges, logs exactly one memory_event node with event_type derivation_skipped and reason encoder_unavailable, and still returns ok true with the queue cleared (soft-fail, never throws, never blocks)
    - Test 2: the derivation_skipped payload carries scalar fields only - reason, room-relative trigger basename, pair count - never artifact body text (Part 8)
    - Test 3: a second forced-unavailable drain within the dedupe window does not write a second marker (payload.dedupe_key rides the existing 60s logEvent idempotency)
    - Test 4: the four existing assertions in tests/test-graph-derive-sweep.cjs still pass unchanged (regression fence for the Phase-169 contract)
  </behavior>
  <action>
    Extend scripts/gsd-graph-derive-sweep.cjs::enqueueDerive additively to enqueueDerive(roomDir, opts)
    where opts.filePath is optional: entries become {roomDir, filePath?, enqueued_at}, deduped by the
    (resolved roomDir, resolved filePath) tuple with an absent filePath treated as the room-scoped
    entry it is today. The zero-arg Stop-hook main() path is untouched (RESEARCH OQ2: the sweep and
    drain stay registered as a harmless second net).

    Rework scripts/gsd-graph-derive-drain.cjs::drainDerive to be the real derivation worker while
    preserving its existing seams: (1) keep opts.deriveRunner exactly as-is and, when it IS injected,
    keep the legacy call shape runDerivation({roomDir: target}) plus SKIP the encoder probe entirely -
    this preserves tests/test-graph-derive-sweep.cjs byte-for-byte semantics; (2) on the DEFAULT path,
    per entry: when entry.filePath exists build artifactPairs via buildNewArtifactPairs(target,
    entry.filePath) (Req 6, O(n)), else via buildAllPairs(target) (the room-scoped Stop-sweep entry);
    then call runDerivation({roomDir: target, deriveFn: scoreBasedDeriveFn, artifactPairs}) - the
    producer swap that RESEARCH names as the actual fix, honoring Part 7 by reusing the Phase-169
    composer untouched. Thread an opts.probeOpts test seam merged into the probe call.

    D-04 in the drain's default path: BEFORE processing entries, probe encoder availability with one
    scoreMeasured call over two short literal probe strings (merging opts.probeOpts so tests pass
    _forceUnavailable true). When the result has semantic null or warning encoder_unavailable: process
    NO pairs, open the room db via room-db.cjs openRoomDb, call navigation.logMemoryEvent(db,
    'derivation_skipped', payload) with scalar-only fields {reason:'encoder_unavailable', trigger:
    basename of entry.filePath or 'room-sweep', pairs_skipped: pair count, dedupe_key:
    'derivation_skipped:' + resolved roomDir}, closeRoomDb, clear the queue entries as drained, and
    return ok true. NO lexical-only degrade path exists anywhere (D-04: lsa-only scoring cannot
    honestly distinguish edge types). Every failure path stays silent-exit-0 (Phase 210 caution:
    advisory, never a new hard-fail; the SessionStart hook contract at the bottom of the file is
    unchanged).

    Add 'derivation_skipped' to EVENT_TYPES in lib/core/navigation/memory-events.cjs as a purely
    additive member, copying the reach_weight_state_unavailable entry's comment discipline: name Phase
    224 D-04, the SEED-059 convention, the emitter (gsd-graph-derive-drain.cjs and, per Plan 03,
    graph-backfill.cjs), and the Part 8 scalar-only payload rule.

    Write tests/test-224-encoder-skip.cjs covering behaviors 1-3 with buildFixtureRoom224 (small
    artifactCount variant), enqueueDerive with filePath, and drainDerive with probeOpts
    _forceUnavailable true; assert the marker row via a direct SELECT over nodes WHERE type =
    'memory_event' AND json_extract(properties, '$.event_type') = 'derivation_skipped' (reads in
    tests are exempt from the chokepoint rule per repo convention).
  </action>
  <verify>
    <automated>node tests/test-224-encoder-skip.cjs && node tests/test-graph-derive-sweep.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-224-encoder-skip.cjs exits 0 with behaviors 1-3 asserted
    - node tests/test-graph-derive-sweep.cjs still prints PASS (4/4) unchanged
    - grep -c "derivation_skipped" lib/core/navigation/memory-events.cjs returns at least 1 inside the EVENT_TYPES set
    - Source assertion: scripts/gsd-graph-derive-drain.cjs contains a require of graph-derive-classifier and NO require of graph-candidate-producer on its default path (comment-stripped grep)
    - Source assertion: grep -E "lexical|lsa" on executable lines of scripts/gsd-graph-derive-drain.cjs returns nothing (no lexical-only degrade path exists, D-04)
    - The drain still exits 0 on every path when run as a CLI with a nonexistent room (behavior check: node scripts/gsd-graph-derive-drain.cjs --room /nonexistent; echo $? prints 0)
  </acceptance_criteria>
  <done>The drain is the score-based derivation worker: pair-scoped, encoder-aware, disclosure-marking, contract-compatible with every Phase-169 caller and test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cascade Step 2b (enqueue + detached spawn) + Req 1 and Req 6 proof legs</name>
  <files>lib/core/intelligence-cascade.cjs, tests/test-224-per-write-derive.cjs, tests/test-224-cost-bound.cjs</files>
  <read_first>
    - lib/core/intelligence-cascade.cjs lines 286-362 (_runCascadeSteps per-artifact loop: the isMd branch, Step 2 graph-index try/catch at lines 330-337 that Step 2b follows, the {status:'ok'|'error'} error-envelope entry pattern to imitate, SCRIPTS_DIR constant)
    - scripts/async-artifact-auto-commit.cjs lines 370-396 (spawnDetachedWorker: spawn(process.execPath, [script, args], {detached:true, stdio:'ignore', env:process.env}) then proc.unref() - the exact D-02-refined pattern to copy)
    - scripts/post-write lines 240-268 (the CLI surface runs the cascade FOREGROUND and captures CASCADE_OUTPUT: this is WHY Step 2b must only enqueue + spawn, never score; also the additive-JSON side-channel that receives the new deriveEnqueue field)
    - lib/mcp/tool-router.cjs lines 495-503 (fireCascade awaits runCascade: the MCP surface is foreground too - tri-polar confirmation for D-02)
    - lib/core/graph-derive-classifier.cjs and tests/helpers/fixture-room-224.cjs (Plan 01 contracts consumed by the tests)
  </read_first>
  <behavior>
    - Test 1 (per-write-derive): after building the fixture and simulating a write by calling enqueueDerive + drainDerive with a deterministic injected deriveFn wrapper (scoreBasedDeriveFn with an injected encodeFn mapping the related pair to high cosine and everything else low), room.db contains at least one edge whose type is in the cascade family and whose review_status column reads 'proposed' (Req 1 + Req 4 wiring); the same flow over the unrelated pair produces zero cascade-family edges (the no-false-positive floor)
    - Test 2 (per-write-derive): running runCascade on the fixture room for a newly written markdown file puts a {roomDir, filePath} entry into graph-derive-queue.json (the foreground effect) and the perFile entry carries a deriveEnqueue field with status ok; the test then drains synchronously in-process and asserts edges - no reliance on detached-process timing
    - Test 3 (per-write-derive, source assertions): _runCascadeSteps contains the enqueueDerive call and a detached unref spawn of the drain script, and contains NO scoreMeasured call and NO await of the drain (D-02 refined: never inline, never serialized on the write-lock)
    - Test 4 (cost-bound): a fixture with N existing artifacts plus 1 new write, drained with an injected counting scoreFn, records EXACTLY N scoring calls - the assertion fails on N squared and on N-choose-2 (Req 6)
  </behavior>
  <action>
    Add Step 2b to lib/core/intelligence-cascade.cjs::_runCascadeSteps inside the isMd branch,
    immediately after the Step 2 graph-index try/catch: in its own try/catch (the error-envelope
    pattern), require scripts/gsd-graph-derive-sweep.cjs via path.join(SCRIPTS_DIR,
    'gsd-graph-derive-sweep.cjs'), call enqueueDerive(roomDir, {filePath: fp}), then spawn the drain
    detached exactly per the async-artifact-auto-commit spawnDetachedWorker pattern: spawn
    process.execPath with [path.join(SCRIPTS_DIR, 'gsd-graph-derive-drain.cjs'), '--room', roomDir],
    options detached true, stdio ignore, env process.env, followed by unref. Record entry.deriveEnqueue
    = {status:'ok', queued: <enqueue result queued flag>} or {status:'error', message} - additive to
    the perFile envelope, so post-write's CASCADE_OUTPUT side-channel and the MCP router are untouched
    consumers. Foreground cost is one small JSON write plus one unref'd spawn (SPEC latency contract:
    under 300ms foreground target, 3000ms hook ceiling). The debounce IS the queue dedupe: rapid
    repeat writes to the same file collapse to one entry, and extra spawned drains no-op on the
    already-cleared queue (the enqueue-then-drain idiom the module comments should state, citing D-02).
    Add a comment on Step 2b naming Phase 224 Req 1, D-02, and the tri-polar rationale (this ONE
    insertion covers CLI post-write, Desktop/MCP tool-router, and Cowork).

    Write tests/test-224-per-write-derive.cjs covering behaviors 1-3. For behavior 2 call runCascade
    from lib/core/intelligence-cascade.cjs directly against the fixture room (trigger 'test'); assert
    the queue entry and deriveEnqueue envelope; then require the drain module and run drainDerive
    in-process with the deterministic injected deriveFn so edge assertions are race-free. For behavior
    3 use the comment-stripped source-grep idiom from tests/run-all-222.sh (strip lines starting with
    optional whitespace then // or * or /*, then grep) against lib/core/intelligence-cascade.cjs:
    assert presence of enqueueDerive and unref, absence of scoreMeasured and of any await on the
    spawned drain.

    Write tests/test-224-cost-bound.cjs for behavior 4: buildFixtureRoom224 with a small
    artifactCount (5 existing), write one extra markdown file, enqueueDerive with that filePath, then
    drainDerive with a deriveFn wrapper that delegates to scoreBasedDeriveFn(step, {scoreFn:
    countingStub}) where countingStub increments a counter and returns a below-floor semantic result;
    assert counter === 5 exactly, and assert it does NOT equal 25 or 10 with an explanatory message
    naming Req 6.
  </action>
  <verify>
    <automated>node tests/test-224-per-write-derive.cjs && node tests/test-224-cost-bound.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-224-per-write-derive.cjs exits 0: related pair yields at least 1 cascade-family edge with review_status 'proposed', unrelated pair yields 0, queue entry + deriveEnqueue envelope present after runCascade, source assertions hold
    - node tests/test-224-cost-bound.cjs exits 0 with the exact-N assertion (5 calls for 5 existing artifacts)
    - Comment-stripped grep of lib/core/intelligence-cascade.cjs finds enqueueDerive and unref on executable lines, and finds NO scoreMeasured token
    - node tests/test-graph-derive-sweep.cjs still exits 0 (Phase-169 round-trip regression)
    - bash tests/run-all-169.sh reports 0 FAIL (the derivation aggregate still green after the producer swap wiring)
    - Manual latency sanity is deferred to Plan 04's phase-gate human check (VALIDATION manual-only table); no foreground timing assertion is required here beyond the no-inline-scoring source proof
  </acceptance_criteria>
  <done>Every markdown write through the shared cascade (all three surfaces) enqueues and background-derives; Req 1 and Req 6 are proven by deterministic tests with zero detached-process races.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user write path -> cascade Step 2b | attacker-influenceable file paths flow from tool input into enqueue + spawn argv |
| detached worker <-> room.db | background scoring process and the live conversation both hold write intent on the same WAL file |
| hook contract -> user experience | any new failure in the write path must never block the user's write (Phase 210 caution) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-224-05 | Denial of service | foreground cascade | mitigate | Step 2b does only a JSON write + unref'd spawn; source-grep test asserts no inline scoreMeasured and no await of the drain (SPEC latency contract) |
| T-224-06 | Denial of service | worker vs live-write WAL contention | mitigate | Phase 218 D-05 write-safety already global in openRoomDb (busy_timeout 5000, synchronous NORMAL); the drain inherits it; duplicate drains no-op on the cleared queue |
| T-224-07 | Tampering | spawn argv from file paths | mitigate | spawn uses an argv ARRAY (no shell), path resolved via path.resolve; drain validates roomDir existence before any work |
| T-224-08 | Information disclosure | derivation_skipped payload | mitigate | scalar-only fields (reason enum, basename, counts); test asserts no artifact body text in the payload (Part 8) |
| T-224-09 | Denial of service | hook/worker crash blocking writes | mitigate | every path try/catch + exit 0 (existing drain discipline preserved); the skip is advisory, never a hard-fail (D-04, Phase 210) |
| T-224-SC | Tampering | npm/pip/cargo installs | mitigate | zero new dependencies; Plan 04 git-diff leg on package.json/package-lock.json |
</threat_model>

<verification>
- node tests/test-224-encoder-skip.cjs, node tests/test-224-per-write-derive.cjs, node tests/test-224-cost-bound.cjs all exit 0
- Regressions: node tests/test-graph-derive-sweep.cjs and bash tests/run-all-169.sh green
</verification>

<success_criteria>
- Per-write derivation fires from the ONE shared cascade body (tri-polar), backgrounded, debounced via queue dedupe
- Related-pair fixture yields proposed CONVERGES/INFORMS edges; unrelated yields zero; exactly N scorer calls per write
- Encoder-unavailable is a disclosed, deduped, non-blocking skip with no lexical fallback
</success_criteria>

<output>
Create `.planning/phases/224-graph-derivation-harness-seed-034-make-room-db-s-typed-node-/224-02-SUMMARY.md` when done.
</output>
