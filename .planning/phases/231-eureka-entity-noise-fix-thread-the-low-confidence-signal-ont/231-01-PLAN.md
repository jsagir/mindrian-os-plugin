---
phase: 231-eureka-entity-noise-fix-thread-the-low-confidence-signal-ont
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/entity-extract.cjs
  - scripts/eureka-portfolio-report.cjs
  - lib/core/mva-classifier.cjs
  - lib/core/eureka/entity-classifier.cjs
  - tests/test-218-low-trust-exclusion.cjs
autonomous: true
requirements: [EEN-01, EEN-02, EEN-03, EEN-04, EEN-06]
tags: [eureka, entity-extraction, provenance, fail-open, rca]

must_haves:
  truths:
    - "A no-LLM embedding best-guess WHAT entity is written with evidenceTier 'low_confidence'; a confident embedding/model verdict stays unstamped and persists as 'None'"
    - "A keyless + encoder-absent degrade stamps WHAT entities evidenceTier 'fallback' for honest provenance but they are NOT excluded from pairing"
    - "A candidate pair with either endpoint stamped 'low_confidence' is excluded from eureka ranking, counted as low_trust_pairs_excluded, and surfaced in the provenance table - never silently dropped"
    - "A pure Tier-0 room (zero verified entities) still ranks non-empty: the exclusion is guarded on hasVerifiedEntity (Decision 8)"
    - "resolveAnthropicKey() resolves the repo .env regardless of process.cwd() via a module-relative 4th leg, and still returns null (graceful degrade) in an installed plugin cache with no .env"
    - "A garbled or missing per-term model answer drops the term as 'noise'; a whole-response failure still fails OPEN to every-name-'what' source:'fallback' (T-T2-01 preserved)"
  artifacts:
    - path: "scripts/entity-extract.cjs"
      provides: "WHAT-side evidenceTier stamping (routeLabel low-confidence branch, runDegradeHzx fallback stamp, write-time persist)"
      contains: "e.evidenceTier = 'low_confidence'"
    - path: "scripts/eureka-portfolio-report.cjs"
      provides: "step-4b low-trust pair exclusion + Decision-8 guard + provenance counter"
      contains: "LOW_TRUST_EVIDENCE_TIERS"
    - path: "lib/core/mva-classifier.cjs"
      provides: "cwd-independent module-relative .env key-resolution leg (4th leg of resolveAnthropicKey)"
      contains: "path.resolve(__dirname, '..', '..', '.env')"
    - path: "lib/core/eureka/entity-classifier.cjs"
      provides: "_coerceLabels drop-on-garbage narrowing; _fallback T-T2-01 contract untouched"
      contains: "? v : 'noise'"
    - path: "tests/test-218-low-trust-exclusion.cjs"
      provides: "3-leg regression test: stamping, mixed-room exclusion, Tier-0 guard"
      min_lines: 100
  key_links:
    - from: "scripts/entity-extract.cjs"
      to: "lib/core/navigation/typed-entity.cjs"
      via: "writeEntityNode evidenceTier param (defaults 'None' when undefined)"
      pattern: "evidenceTier: e\\.evidenceTier"
    - from: "scripts/eureka-portfolio-report.cjs step-4b loop"
      to: "isLowTrustEntity / LOW_TRUST_EVIDENCE_TIERS"
      via: "hasVerifiedEntity-guarded either-endpoint continue"
      pattern: "hasVerifiedEntity && \\(aLowTrust \\|\\| bLowTrust\\)"
    - from: "lib/core/mva-classifier.cjs resolveAnthropicKey"
      to: "repo-root .env"
      via: "__dirname walk-up, never process.cwd()"
      pattern: "path\\.resolve\\(__dirname, '\\.\\.', '\\.\\.', '\\.env'\\)"
---

<objective>
Verify and accept the ALREADY-APPLIED Phase 231 implementation (uncommitted in the working tree) covering the original phase scope: FIX A (WHAT-side evidenceTier provenance stamping in `scripts/entity-extract.cjs` + low-trust pairing exclusion in `scripts/eureka-portfolio-report.cjs`), FIX B (cwd-independent module-relative `.env` key leg in `lib/core/mva-classifier.cjs`), and FIX C (`_coerceLabels` drop-on-garbage narrowing in `lib/core/eureka/entity-classifier.cjs`).

**This is a verify-not-build plan.** The implementation was written and tested in a `/gsd-debug` session (RCA `handoff-eureka-entity-noise-2026-07-19`) before this plan existed; the navigator then required the full GSD process retroactively (`/goal do full gsd propper proces !`). Every task below asserts the CURRENT file contents and test results against literal acceptance criteria. If an assertion fails, the working tree has drifted since CONTEXT.md was written (5 concurrent sessions touched this tree) - STOP and report the exact failing assertion rather than "fixing" code that another session may own.

Purpose: junk entities ("Windows", "CSFs") ranked top-2 in a live eureka run because tier-2b (the LLM NOISE filter) has never fired on this machine (`tier2_model: 0` in every real status.json) - everything resolves through the weaker embedding tier as low-confidence WHAT best-guesses. The fix threads the existing low-confidence signal onto WHAT entities, excludes them from pairing (with a Decision-8 guard), and unblocks tier-2b's key resolution.

Output: verified acceptance of FIX A/B/C against source assertions + hermetic tests. The commit happens in plan 231-02 (after CR-01 verification and the human checkpoint), NOT here.

### Decision map (CONTEXT.md carries prose decisions; D-NN ids minted here for traceability)

| ID | Decision (source: 231-CONTEXT.md `<decisions>`) |
|----|--------------------------------------------------|
| D-01 | Thread low-confidence onto WHAT entities as node prop `evidenceTier`; exclude ONLY `'low_confidence'` from pairing (NOT `'fallback'`), guarded by `hasVerifiedEntity`; wiring point is `scripts/eureka-portfolio-report.cjs` step-4b, NOT `room-native-substrate.cjs` (PATTERNS.md location correction) |
| D-02 | Add a cwd-independent module-relative `.env` leg to `resolveAnthropicKey()` (walk up from `__dirname`, cross-platform, keyless-safe null degrade); ships in THIS phase, not a fast-follow |
| D-03 | `_coerceLabels()` narrowed: unrecognized/missing per-term model answer coerces to `'noise'` (dropped), NOT `'what'`; `_fallback()` T-T2-01 whole-response fail-open contract UNTOUCHED |
| D-05 | `tests/test-218-what-why-classifier.cjs` assertions must not weaken: no-key -> `source:'fallback'`, embedding-degrade -> `classifier_source:'embedding'` never silently `'fallback'` |

(D-04, the CR-01 duplicate-name reconciliation, is plan 231-02's scope.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/231-eureka-entity-noise-fix-thread-the-low-confidence-signal-ont/231-CONTEXT.md
@.planning/phases/231-eureka-entity-noise-fix-thread-the-low-confidence-signal-ont/231-PATTERNS.md
@.planning/debug/handoff-eureka-entity-noise-2026-07-19.md
</context>

## Artifacts this phase produces

Full phase-level symbol table (plans 01 + 02 together). Column 3 says which plan verifies each.

| Artifact / symbol | Where | Verified in |
|---|---|---|
| `e.evidenceTier = 'low_confidence'` WHAT-branch stamp in `routeLabel()` | `scripts/entity-extract.cjs` (~line 420) | 231-01 Task 1 |
| `lowConfidence=true` origin call site in `runTier2()` no-LLM branch | `scripts/entity-extract.cjs` (~lines 580-587 pre-CR-01 numbering) | 231-01 Task 1 |
| `evidenceTier = 'fallback'` provenance-only stamp in `runDegradeHzx()` | `scripts/entity-extract.cjs` (~line 471) | 231-01 Task 1 |
| `evidenceTier: e.evidenceTier` write-time persist via `writeEntityNode` | `scripts/entity-extract.cjs` (~line 839) | 231-01 Task 1 |
| `LOW_TRUST_EVIDENCE_TIERS` frozen set (`'low_confidence'` ONLY) | `scripts/eureka-portfolio-report.cjs` (line 1507) | 231-01 Task 2 |
| `isLowTrustEntity(row)` predicate | `scripts/eureka-portfolio-report.cjs` (~lines 1511-1522) | 231-01 Task 2 |
| `hasVerifiedEntity` Decision-8 guard | `scripts/eureka-portfolio-report.cjs` (~lines 1058-1061) | 231-01 Task 2 |
| Either-endpoint exclusion branch in the 4b loop | `scripts/eureka-portfolio-report.cjs` (~line 1093) | 231-01 Task 2 |
| `low_trust_pairs_excluded` provenance field + report row | `scripts/eureka-portfolio-report.cjs` (lines 471, 1308, 1910) | 231-01 Task 2 |
| 4th (module-relative) `.env` leg in `resolveAnthropicKey()` | `lib/core/mva-classifier.cjs` (~lines 152-171) | 231-01 Task 3 |
| `_coerceLabels` drop-on-garbage (`? v : 'noise'`) | `lib/core/eureka/entity-classifier.cjs` (line 141) | 231-01 Task 3 |
| `tests/test-218-low-trust-exclusion.cjs` (NEW, 3 legs) | wired at `tests/run-all-218.sh` lines 160-161 | 231-01 Tasks 1-2 |
| `reconcileEvidenceTierAcrossDuplicateNames()` (CR-01) | `scripts/entity-extract.cjs` (~lines 778-797) | 231-02 Task 1 |
| `tests/test-218-duplicate-entity-reconciliation.cjs` (NEW) | wired at `tests/run-all-218.sh` lines 170-171 | 231-02 Task 1 |

<tasks>

<task type="auto">
  <name>Task 1: Verify FIX A stamping side - WHAT-side evidenceTier provenance in entity-extract.cjs (D-01)</name>
  <files>scripts/entity-extract.cjs, tests/test-218-low-trust-exclusion.cjs</files>
  <read_first>
    - scripts/entity-extract.cjs (routeLabel ~lines 400-425; runDegradeHzx ~lines 455-475; runTier2 no-LLM else-branch ~lines 575-590; write loop ~lines 830-845)
    - .planning/phases/231-eureka-entity-noise-fix-thread-the-low-confidence-signal-ont/231-PATTERNS.md (section "scripts/entity-extract.cjs" - the WHY-side self-analog this mirrors)
    - tests/test-218-low-trust-exclusion.cjs (all 3 legs)
    - lib/core/navigation/typed-entity.cjs line 126 (the 'None' default the stamp rides on)
  </read_first>
  <action>
    Verify (do NOT re-implement) that the applied FIX A stamping matches D-01 exactly. Assert each of the following against current file contents:
    1. `routeLabel(e, label, whatEntities, whyTerms, lowConfidence)` stamps the WHAT branch: `if (lowConfidence === true) e.evidenceTier = 'low_confidence';` before `whatEntities.push(e)`. The WHY branch's existing `entry.lowConfidence = true` stamping is unchanged (the self-analog per PATTERNS.md).
    2. The ONLY call site passing `lowConfidence=true` is the `runTier2()` no-LLM else-branch (`routeLabel(it.e, it.bestGuess, whatEntities, whyTerms, true)`); every other `routeLabel` call leaves the param unset (confident entities stay unstamped).
    3. `runDegradeHzx()` stamps `if (label === 'what' && result.source !== 'model') e.evidenceTier = 'fallback';` - the provenance-only degrade stamp, deliberately DISTINCT from 'low_confidence' (fallback is NOT excluded from pairing; see D-01 rationale in CONTEXT.md).
    4. The write loop passes `evidenceTier: e.evidenceTier` into `navigation.writeEntityNode`, and `lib/core/navigation/typed-entity.cjs:126` defaults a non-string to `'None'`, so a confident (unstamped) entity's write is byte-identical to the pre-fix write.
    Then run the regression test. If any assertion fails, STOP and report the failing assertion + current file state (working-tree drift protocol in the objective).
  </action>
  <verify>
    <automated>node tests/test-218-low-trust-exclusion.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "e.evidenceTier = 'low_confidence'" scripts/entity-extract.cjs` returns exactly 1
    - `grep -c "result.source !== 'model') e.evidenceTier = 'fallback'" scripts/entity-extract.cjs` returns exactly 1
    - `grep -c "evidenceTier: e.evidenceTier" scripts/entity-extract.cjs` returns exactly 1
    - `grep -c "whatEntities, whyTerms, true)" scripts/entity-extract.cjs` returns exactly 1 (single lowConfidence=true call site)
    - `grep -c "typeof evidenceTier === 'string' ? evidenceTier : 'None'" lib/core/navigation/typed-entity.cjs` returns exactly 1
    - `node tests/test-218-low-trust-exclusion.cjs` exits 0 (3/3 legs: stamping, mixed-room exclusion, Tier-0 guard) - verified passing 2026-07-19 during planning
  </acceptance_criteria>
  <done>All five source assertions hold on the current working tree and the low-trust regression test exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Verify FIX A exclusion side - step-4b low-trust pair exclusion + Decision-8 guard in eureka-portfolio-report.cjs (D-01)</name>
  <files>scripts/eureka-portfolio-report.cjs</files>
  <read_first>
    - scripts/eureka-portfolio-report.cjs (provenance table row ~line 471; 4b loop ~lines 1037-1104; provenance object ~lines 1300-1310; predicate block ~lines 1485-1525; counter init ~line 1910)
    - .planning/phases/231-eureka-entity-noise-fix-thread-the-low-confidence-signal-ont/231-PATTERNS.md (the "CONTEXT.md location correction" note: the wiring point is THIS file's step-4b, NOT room-native-substrate.cjs)
    - tests/test-218-scaffold-pair-filter.cjs (the direct-seed idiom the new exclusion test mirrors)
  </read_first>
  <action>
    Verify the applied exclusion matches D-01's deliberate narrowing. Assert:
    1. `LOW_TRUST_EVIDENCE_TIERS = Object.freeze(new Set(['low_confidence']))` - the set contains ONLY 'low_confidence'. 'fallback' is deliberately EXCLUDED from the set (excluding it would empty a pure Tier-0 room, breaking Decision 8). This narrowing is a locked decision, not a gap to "fix".
    2. `isLowTrustEntity(row)` parses `row.properties` defensively (try/catch -> false), gates on `ENTITY_NODE_TYPES.has(entityType)`, and returns `LOW_TRUST_EVIDENCE_TIERS.has(tier)`.
    3. The 4b loop computes `hasVerifiedEntity` (at least one entity node with `!n.lowTrust`) BEFORE the pair loop, and the exclusion branch is `if (hasVerifiedEntity && (aLowTrust || bLowTrust)) { lowTrustPairsExcluded += 1; continue; }` - placed as a third additive branch alongside the existing container and scaffold exclusions (same insertion point, no parallel filter pass, per PATTERNS.md "additive-branch-at-a-shared-insertion-point").
    4. The count is surfaced honestly: `low_trust_pairs_excluded` in the provenance object AND a markdown provenance-table row ("Low-trust entity pairs excluded..."). Never a silent drop.
    5. Note in the SUMMARY that room-native-substrate.cjs was NOT touched for the exclusion - CONTEXT.md's original wiring guess was corrected by PATTERNS.md and the RCA reconciliation (pairing enumerates `indexed` SELECT nodes here, not techMap).
    Then run the two behavioral gates (REQ-5 non-empty contract + scaffold-filter no-regression).
  </action>
  <verify>
    <automated>node tests/test-218-noise-reduction.cjs && node tests/test-218-scaffold-pair-filter.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Object.freeze(new Set(\['low_confidence'\]))" scripts/eureka-portfolio-report.cjs` returns exactly 1
    - `grep -c "'fallback'" <(grep "LOW_TRUST_EVIDENCE_TIERS = " scripts/eureka-portfolio-report.cjs)` returns 0 (fallback NOT in the exclusion set)
    - `grep -c "hasVerifiedEntity && (aLowTrust || bLowTrust)" scripts/eureka-portfolio-report.cjs` returns exactly 1
    - `grep -c "low_trust_pairs_excluded" scripts/eureka-portfolio-report.cjs` returns >= 3 (provenance object, table row, counter init)
    - `grep -c "function isLowTrustEntity" scripts/eureka-portfolio-report.cjs` returns exactly 1
    - `node tests/test-218-noise-reduction.cjs` exits 0 (REQ-5: post-extraction ranked list stays NON-EMPTY - proves the Tier-0 guard)
    - `node tests/test-218-scaffold-pair-filter.cjs` exits 0 (existing scaffold exclusion unregressed)
  </acceptance_criteria>
  <done>Exclusion set is low_confidence-only, guard + counter + provenance row all present, REQ-5 and scaffold-filter tests exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Verify FIX B key-resolution leg + FIX C _coerceLabels narrowing, and the D-05 test contract (D-02, D-03, D-05)</name>
  <files>lib/core/mva-classifier.cjs, lib/core/eureka/entity-classifier.cjs</files>
  <read_first>
    - lib/core/mva-classifier.cjs (resolveAnthropicKey, lines ~110-175 - all four legs)
    - lib/core/eureka/entity-classifier.cjs (VALID_LABELS ~line 71, _fallback lines 113-117, _coerceLabels lines 134-143, the T-T2-01 header contract)
    - tests/test-218-what-why-classifier.cjs (FULL read - the must-not-break contract: fail-open legs 234-289, embedding-degrade leg vii 690-716, degrade-writes-Larry leg ii ~497-498)
  </read_first>
  <action>
    Verify FIX B and FIX C match D-02/D-03 and that the D-05 contract holds:
    1. FIX B (D-02): `resolveAnthropicKey()` has a 4th leg resolving `path.resolve(__dirname, '..', '..', '.env')` (lib/core/ -> plugin root), wrapped in try/catch-swallow, feeding `_parseAnthropicKey`, falling through to `return null`. Cross-platform (path.resolve segments, no process.cwd(), no string concat). Keyless-safe: an installed plugin cache has no .env (gitignored + absent from package.json files[]) so the leg returns null and degrade is preserved. Canon Part 8 holds: the key goes to Anthropic (LOCAL boundary), never Brain.
    2. FIX C (D-03): `_coerceLabels` line is `labels[n] = (v && VALID_LABELS.has(v)) ? v : 'noise';` and the `? v : 'what'` coercion no longer exists ANYWHERE in the file. `_fallback()` (lines 113-117) is byte-identical to the shipped contract: every name -> 'what', `source: 'fallback'`, never throws. The narrowing only fires when the model DID run and returned a parseable-but-garbled per-term answer; the whole-response fail-open path (no key / throw / non-JSON / non-2xx) is untouched, so T-T2-01 DoS protection stands.
    3. D-05: run the full classifier contract test - all 22 checks must pass, including leg (ii) asserting a degrade-mode entity ("Larry") IS still written as a node (the fallback stamp is provenance-only, never a write-blocker), and leg (vii) asserting embedding-degrade discloses `classifier_source:'embedding'`, never silently 'fallback'.
    4. Run the two MVA harness tests to prove the new key leg did not disturb the classifier's callers.
    Known limitation to carry into the SUMMARY, not to fix here: 231-REVIEW.md WR-01 (truncation-tail coerced to noise), WR-02 (no-key test not hermetic against a real .env), WR-04 (leg's two-levels-up depth assumption) are logged non-blocking warnings - candidates for a fast-follow, out of this verify-and-accept plan's scope.
  </action>
  <verify>
    <automated>node tests/test-218-what-why-classifier.cjs && node tests/test-mva-from-brief.cjs && node tests/test-mva-dror-harness.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "path.resolve(__dirname, '..', '..', '.env')" lib/core/mva-classifier.cjs` returns exactly 1
    - `grep -c "process.cwd" <(sed -n '152,175p' lib/core/mva-classifier.cjs)` returns 0 (the new leg never touches cwd)
    - `grep -c "? v : 'noise';" lib/core/eureka/entity-classifier.cjs` returns exactly 1
    - `grep -c "? v : 'what'" lib/core/eureka/entity-classifier.cjs` returns 0 (old fail-open coercion gone)
    - `sed -n '113,117p' lib/core/eureka/entity-classifier.cjs` shows _fallback returning `labels[n] = 'what'` for every name and `source: 'fallback'` (contract byte-intact)
    - `node tests/test-218-what-why-classifier.cjs` exits 0 and prints "22/22 tier-2 classifier checks passed" - verified passing 2026-07-19 during planning
    - `node tests/test-mva-from-brief.cjs` and `node tests/test-mva-dror-harness.cjs` both exit 0
  </acceptance_criteria>
  <done>4th key leg present and cwd-free, coercion narrowed with _fallback intact, 22/22 contract green, MVA harnesses green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room documents -> extractor | untrusted user prose enters the entity pipeline (tier-1 regex candidates) |
| extractor -> Anthropic API | LOCAL boundary; repo/.env or ~/.mindrian.env key crosses to api.anthropic.com only (never Brain - Canon Part 8) |
| filesystem .env -> resolveAnthropicKey | secret material read from disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-231-01 | Information disclosure | resolveAnthropicKey 4th leg | mitigate | key value never logged/printed (presence-only checks); .env gitignored + absent from package.json files[] so never shipped; leg fails to null on any error |
| T-231-02 | Denial of service | entity-classifier _coerceLabels | mitigate | adversarial doc inducing FULL classifier failure still fails OPEN via untouched _fallback (T-T2-01); only per-term non-answers inside a valid response drop - verified by 22/22 contract test |
| T-231-03 | Tampering (ranking manipulation) | eureka pairing via low-confidence junk | mitigate | low_confidence endpoints excluded from ranking (LOW_TRUST_EVIDENCE_TIERS), counted in low_trust_pairs_excluded provenance, hasVerifiedEntity guard prevents empty-room DoS |
| T-231-SC | Tampering | package installs | accept | NO npm/pip/cargo installs in this phase (verify-only, zero new dependencies) - package legitimacy gate not triggered |
</threat_model>

<verification>
- All three tasks' source assertions hold against the current working tree (no drift from the 5-concurrent-session window).
- `node tests/test-218-low-trust-exclusion.cjs`, `node tests/test-218-noise-reduction.cjs`, `node tests/test-218-scaffold-pair-filter.cjs`, `node tests/test-218-what-why-classifier.cjs` (22/22), both MVA harnesses: all exit 0.
- Nothing in this plan commits: the atomic commit is 231-02 Task 3, gated on the human checkpoint.
</verification>

<success_criteria>
- FIX A stamping, FIX A exclusion, FIX B key leg, FIX C narrowing each verified against literal source assertions (exact grep counts above).
- The D-05 test contract proven unweakened: 22/22 with all 7 must-not-break assertions intact.
- The deliberate refinements documented in the SUMMARY as locked decisions, not deviations: (a) fallback tier NOT excluded (Decision 8), (b) wiring point is eureka-portfolio-report.cjs step-4b not room-native-substrate.cjs, (c) 231-REVIEW.md WR-01/WR-02/WR-04 logged as non-blocking fast-follow candidates.
</success_criteria>

<output>
Create `.planning/phases/231-eureka-entity-noise-fix-thread-the-low-confidence-signal-ont/231-01-SUMMARY.md` when done.
</output>
