---
phase: 340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d
plan: 02
type: execute
wave: 2
depends_on: [340-01]
files_modified:
  - docs/MINDRIAN-CANON.md
  - docs/CANON-PHASE-MAP.md
  - agents/larry-extended.md
  - tests/test-canon-entry-38-sourced-claims-floor.cjs
  - tests/test-canon-entry-31-two-gauge-floor.cjs
  - tests/test-canon-entry-36-shape-declaration-floor.cjs
  - .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-02-DRAFT-PROSE.md
autonomous: false
requirements: [CANON-01]
canon_parts: [4, 6, 8, 9, 12]

must_haves:
  truths:
    - "The Canon distinguishes a hedged OPINION (Canon-legal elevation) from a hedged FABRICATION (an invented number or claim wrapped in a disclaimer word), and states that a hedge word never exempts an unsourced claim."
    - "Larry's shipped runtime persona file carries the same rule, so the doctrine binds behavior and not only the constitution."
    - "The navigator APPROVED the exact prose at a blocking checkpoint before any byte reached docs/MINDRIAN-CANON.md, exactly as all 37 prior Appendix D entries did."
    - "Appendix D entries 1-37 are byte-preserved and the frozen Part 3 scalars are unweakened after the wave lands."
  artifacts:
    - path: "docs/MINDRIAN-CANON.md"
      provides: "Part 12 Sourced Claims Doctrine subsection + Appendix D entry 38 + version 1.25"
      contains: "Sourced Claims Doctrine"
    - path: "agents/larry-extended.md"
      provides: "The behavioral mirror of the doctrine in Larry's shipped system prompt"
      contains: "A hedge word is not a source."
    - path: "tests/test-canon-entry-38-sourced-claims-floor.cjs"
      provides: "Byte-presence/absence floor guard for entry 38 across the Canon and the persona file"
      exports: []
  key_links:
    - from: "docs/MINDRIAN-CANON.md Part 12"
      to: "agents/larry-extended.md"
      via: "mirrored doctrine clause, asserted in both files by the same floor test"
      pattern: "A hedge word is not a source\\."
    - from: "docs/MINDRIAN-CANON.md Appendix D entry 38"
      to: "docs/CANON-PHASE-MAP.md v1.25 row"
      via: "version-history lockstep"
      pattern: "v1\\.25"
---

<objective>
Amendment Wave A: land the SOURCED CLAIMS DOCTRINE into Canon Part 12 and mirror it into
`agents/larry-extended.md`, as Appendix D entry 38, bumping the Canon from v1.24 to v1.25.

Purpose: Part 12's existing "Elevation tone (HARD requirement)" governs CONFIDENCE framing
("hedged, cautious, evidence-backed, NEVER confident") but says nothing about SOURCE existence.
The gap SEED-086 names, and the real Aronhime fabrication incident demonstrated, is a number or
claim that has NO source at all, wrapped in a hedge word ("illustrative", "e.g.", "roughly") that
a later reader then treats as pre-cleared rather than unsourced. 340-RESEARCH.md verified the gap
live: zero occurrences of "sourced", "hedge" or "fabricat" anywhere in Part 12, and
`agents/larry-extended.md` grepped clean of any number-sourcing language.

Output: Part 12 gains a new subsection; `agents/larry-extended.md` gains the behavioral mirror;
Appendix D gains entry 38; header/footer Version moves 1.24 -> 1.25; `docs/CANON-PHASE-MAP.md`
gains its v1.25 row; a new floor test guards all of it; the two carried-forward entry floor tests
move their version anchors 1.24 -> 1.25.

This is D-03's Sourced Claims front, landing as its own entry per the Claude's-Discretion call
340-CONTEXT.md left to planning time.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-CONTEXT.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-RESEARCH.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-PATTERNS.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: BLOCKING navigator gate - draft the exact prose, halt for APPROVE before any canon byte</name>

  <files>.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-02-DRAFT-PROSE.md</files>

  <read_first>
    - `docs/MINDRIAN-CANON.md` Part 12 IN FULL (lines 603 to 700, header `## Part 12 - The Pedagogy Constitution (Invisibility)` through the `## Appendix A` header) - you are inserting a subsection into this Part and must place it against the existing "Elevation tone (HARD requirement)" paragraph and "The cardinal sins" paragraph, both of which the new doctrine extends
    - `docs/MINDRIAN-CANON.md` Appendix D entries 30 through 37 (lines 795 to 811) - the provenance-paragraph voice every new entry must match: what changed, why (traced to a real incident, never an invented justification), who approved and when, what stayed frozen, and the explicit "mints NO reach/edge/node, opens NO Brain wire" framing
    - `agents/larry-extended.md` IN FULL (181 lines) - the insertion point for the mirror sits alongside the existing "## Elevation (Part 12 ...)" section and the "## Never Do" section; find the real seam, do not append at the end
    - `.planning/seeds/SEED-086-fabrication-hedge-laundering-prose-output-not-covered-by-tool-honesty-detector.md` - the traced source of this doctrine; entry 38's provenance paragraph must cite it accurately, not approximately
    - `.planning/phases/340-.../340-LIVE-VERIFICATION.md` - the Wave A section, which states whether `SOURCED_FROM` has a confirmed runtime writer. If it does NOT, the drafted prose must not cite it.
    - `.planning/phases/340-.../340-PATTERNS.md` section "`docs/MINDRIAN-CANON.md` (config, transform) - the Canon Amendment Lifecycle"
  </read_first>

  <action>
Write a DRAFT ONLY to `340-02-DRAFT-PROSE.md`. Write nothing to `docs/MINDRIAN-CANON.md`,
`agents/larry-extended.md` or `docs/CANON-PHASE-MAP.md` in this task. Then present the draft and
HALT for the navigator's verdict.

Draft three pieces of prose:

(1) A new Part 12 subsection, heading exactly `### The Sourced Claims Doctrine (HARD requirement)`,
placed immediately AFTER the paragraph beginning `**Elevation tone (HARD requirement).**` and
BEFORE the paragraph beginning `**Surface obligation.**`, so it reads as the source-plane sibling
of the confidence-plane rule it extends. It must make these points and contain these exact anchor
strings so a floor test can pin them:
  - `sourced or absent` - the rule itself: a claim either carries a source Larry can name, or
    Larry states plainly that it has none;
  - `A hedge word is not a source.` - the laundering move being forbidden: "illustrative", "e.g.",
    "roughly", "on the order of" and their kin describe CONFIDENCE, not PROVENANCE, and never
    convert an invented number into a cleared one;
  - an explicit statement that this EXTENDS and does not weaken the existing Elevation tone
    requirement: hedging stays mandatory on every elevation; hedging a FABRICATION is a different
    act from hedging an OPINION, and only the second is Canon-legal;
  - the failure mode named concretely: a downstream reader treating a hedged unsourced figure as
    pre-cleared because the hedge word read like a disclaimer;
  - IF AND ONLY IF `340-LIVE-VERIFICATION.md` confirmed a real runtime writer, a pointer to
    `SOURCED_FROM` as the graph-native provenance edge (Part 4 vocabulary) with its writer file
    named. If no writer was confirmed, omit this entirely and say so at the gate rather than
    citing aspirational code.

(2) The `agents/larry-extended.md` mirror: a short behavioral clause in that file's own voice
(second person, imperative, the register the rest of that file uses), carrying the SAME two anchor
strings `sourced or absent` and `A hedge word is not a source.` so one floor test can assert both
files. Name the concrete behavior: never recite a number Larry cannot source; when a number is
wanted and no source exists, say that no source exists rather than producing a plausible one; a
disclaimer word does not license the number.

(3) Appendix D entry 38, opening exactly `38. **`, in the voice of entries 30 through 37: what
changed, why (SEED-086 and the Aronhime fabrication incident, cited accurately), that the
navigator APPROVED at a blocking checkpoint BEFORE any canon byte was written with the version
target v1.24 -> v1.25 confirmed, that entry 31's self-binding clause is RELEASED for this
amendment by the Part-10 navigator-authority override (entry 20) recorded truthfully with no
two-gauge reading taken or fabricated, that the amendment MINTS NO reach/edge/node and opens NO
Brain wire, and that the frozen Part 3 scalars are byte-identical
(`MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15 gate`). State the full lockstep manifest for this wave
the way entries 34 through 37 do.

House rules for all three: hyphens only, never an em-dash; Feynman-plain prose; no invented
citation; every claim traceable to `340-LIVE-VERIFICATION.md`, SEED-086, or the Canon's own text.

After writing the draft, present it and HALT. Do not proceed to Task 2 on silence, on a thumbs-up,
or on any inference. Record the navigator's literal reply in the plan SUMMARY.
  </action>

  <what-built>
A draft-only prose file, `340-02-DRAFT-PROSE.md`, containing the three pieces of text exactly as
they would land: the new Part 12 subsection, the `agents/larry-extended.md` mirror clause, and
Appendix D entry 38. Zero constitutional bytes have moved.
  </what-built>

  <how-to-verify>
1. Read `.planning/phases/340-.../340-02-DRAFT-PROSE.md` in full - it contains the three pieces of
   prose verbatim, exactly as they would land.
2. Confirm the Part 12 subsection wording says what you want the constitution to say. This is the
   only moment the wording is negotiable; after this it is canon.
3. Confirm the `agents/larry-extended.md` mirror wording - this is the text that actually changes
   Larry's behavior at runtime; the Canon text alone does not.
4. Confirm the version target: canon v1.24 -> v1.25.
5. Confirm the anchor strings `sourced or absent` and `A hedge word is not a source.` are the ones
   you want pinned by a floor test forever, or name replacements.
6. If `340-LIVE-VERIFICATION.md` could not confirm a `SOURCED_FROM` runtime writer, the draft omits
   that citation; confirm you are content with the omission rather than a speculative cite.

Reply with one of:
  - `APPROVE` - the exact drafted prose lands as-is.
  - `REJECT: <reason>` - nothing lands; the draft is revised and this gate re-runs.
  - `APPROVE WITH EDITS: <the edits>` - the executor applies the named edits to the draft, updates
    the anchor-string list to match the edited text, and re-presents before writing.

This checkpoint is NOT auto-approvable. `workflow.auto_advance` is false in `.planning/config.json`,
and even if it were true this gate would remain blocking: every one of the 37 prior Appendix D
entries required a navigator APPROVE on the literal text before any canon byte landed, and
340-RESEARCH.md Pitfall 4 names "a plan that schedules a canon-file Edit call before a
checkpoint:human-verify task in the same wave" as this phase's signature failure mode. Research
quality is not a substitute for this sign-off.
  </how-to-verify>

  <resume-signal>Type "APPROVE", "REJECT: reason", or "APPROVE WITH EDITS: edits"</resume-signal>

  <verify>
    <automated>test -f .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-02-DRAFT-PROSE.md &amp;&amp; grep -q "sourced or absent" .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-02-DRAFT-PROSE.md &amp;&amp; grep -q "A hedge word is not a source." .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-02-DRAFT-PROSE.md &amp;&amp; test "$(git diff --name-only docs/MINDRIAN-CANON.md agents/larry-extended.md | wc -l)" -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `340-02-DRAFT-PROSE.md` exists and contains three clearly labelled sections: the Part 12 subsection, the `agents/larry-extended.md` mirror, and Appendix D entry 38.
    - The draft contains the exact strings `### The Sourced Claims Doctrine (HARD requirement)`, `sourced or absent`, and `A hedge word is not a source.`
    - The draft's entry-38 text opens with the exact prefix `38. **` and contains the exact substring `MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15 gate`.
    - The draft contains zero U+2014 em-dashes: `grep -c "$(printf '\xe2\x80\x94')" <draft>` returns 0.
    - `git diff --name-only docs/MINDRIAN-CANON.md agents/larry-extended.md docs/CANON-PHASE-MAP.md` returns empty at the moment this task halts - not one constitutional byte has moved.
    - Execution HALTS here and does not proceed to Task 2 until the navigator's literal reply is recorded in the plan SUMMARY.
  </acceptance_criteria>

  <done>The navigator has replied APPROVE (or APPROVE WITH EDITS, with the edits applied and re-presented), the reply is recorded verbatim in the plan SUMMARY with its date, and the Canon is still byte-identical to its pre-task state.</done>
</task>

<task type="auto">
  <name>Task 2: Land the atomic lockstep - Part 12 subsection, persona mirror, entry 38, version 1.25, map row</name>

  <files>docs/MINDRIAN-CANON.md, agents/larry-extended.md, docs/CANON-PHASE-MAP.md, tests/test-canon-entry-31-two-gauge-floor.cjs, tests/test-canon-entry-36-shape-declaration-floor.cjs</files>

  <read_first>
    - `.planning/phases/340-.../340-02-DRAFT-PROSE.md` - the APPROVED text. Write exactly this, byte-for-byte. Do not improve it, re-word it, or expand it while writing.
    - `docs/MINDRIAN-CANON.md` lines 1-8 (the `Version: 1.24` header block) and lines 863-867 (the `_Mindrian Canon v1.24 - MindrianOS Plugin_` footer) - the two version tokens that move together
    - `docs/CANON-PHASE-MAP.md` line 6 (`Canon reference: docs/MINDRIAN-CANON.md (v1.24)`) and the version-history rows for v1.22, v1.23 and v1.24 (near lines 474-476) - the row format your v1.25 row must match, including the trailing `Map + canon text change.` sentence and the `Fences:` clause naming every test kept green
    - `tests/test-canon-entry-31-two-gauge-floor.cjs` - version anchors in the Test 9 doc-comment and the two `1.24` assertions
    - `tests/test-canon-entry-36-shape-declaration-floor.cjs` - version anchors in the Test 8 and Test 9 doc-comments and the three `1.24` assertions including the CANON-PHASE-MAP token check
  </read_first>

  <action>
Land every edit below in ONE commit so CI never goes RED mid-wave (the "ONE atomic lockstep wave"
discipline every entry from 31 onward states in its own manifest).

1. `docs/MINDRIAN-CANON.md` Part 12: insert the APPROVED subsection immediately after the paragraph
   beginning `**Elevation tone (HARD requirement).**` and immediately before the paragraph
   beginning `**Surface obligation.**`. Additive only: do not reword, reorder or delete any
   existing Part 12 sentence.
2. `agents/larry-extended.md`: insert the APPROVED mirror clause at the seam identified during
   Task 1 (alongside the existing `## Elevation (Part 12 ...)` section). Additive only: the YAML
   frontmatter, `connector.excluded`, `hitl_shape: "F.1"` and `hitl_why` are untouched, so
   `node scripts/check-shape-declaration.cjs --check` sees no new violation.
3. `docs/MINDRIAN-CANON.md` Appendix D: append the APPROVED entry 38 immediately after entry 37
   (which ends at the line beginning `37. **Part 11 R16 enforcement downgraded`) and before the
   line beginning `Conversation transcript reference: session 2026-04-20`. Entries 1 through 37
   are byte-preserved: do not touch one character of them. This is the strictly-additive rule
   340-RESEARCH.md's Anti-Patterns section names.
4. `docs/MINDRIAN-CANON.md` version: header line 3 `Version: 1.24` becomes `Version: 1.25`; the
   footer `_Mindrian Canon v1.24 - MindrianOS Plugin_` becomes
   `_Mindrian Canon v1.25 - MindrianOS Plugin_`. Exactly two version tokens move.
5. `docs/CANON-PHASE-MAP.md`: reference line 6 `(v1.24)` becomes `(v1.25)`; append a
   `| v1.25 | TBD | <execution date> | ...` row to the version-history table in the same shape as
   the v1.24 row. The row names: the phase SLUG
   `340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d` (never the phase number
   alone - CANON-PHASE-MAP.md documents phase-number collision as a known fragility), entry 38,
   requirement id CANON-01, `NAVIGATOR-GATED` with the approval date, the
   `agents/larry-extended.md` mirror, the frozen-scalars-byte-identical clause, and a `Fences:`
   clause naming `tests/test-canon-entry-38-sourced-claims-floor.cjs`,
   `tests/test-canon-frozen-scalars-floor.cjs`, the two version-anchor-bumped tests, and
   `bash tests/run-all-340.sh exit 0`.
6. `tests/test-canon-entry-31-two-gauge-floor.cjs`: move its version anchor 1.24 -> 1.25 in every
   place it appears (the Test 9 doc-comment description and the two assertions
   `/^Version: 1\.24$/m` and `/_Mindrian Canon v1\.24 - MindrianOS Plugin_/`). Its byte-for-byte
   scalar assertions on MAX_K=3 / DIAL_REACH_K=6 / 0.70 / 0.15 and its prior-entry loop
   (`n = 1..30`) are UNWEAKENED - change nothing else in the file.
7. `tests/test-canon-entry-36-shape-declaration-floor.cjs`: same version-anchor move 1.24 -> 1.25,
   including its CANON-PHASE-MAP token assertion `/v1\.24/` which becomes `/v1\.25/`. Its
   prior-entry loop (`n = 1..35`) and frozen-scalar assertions are UNWEAKENED.

Do NOT touch the frozen scalar VALUES anywhere. Restating `MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15
gate` byte-identical inside entry 38 is expected and correct; changing any of those numbers is the
exact tamper `tests/test-canon-frozen-scalars-floor.cjs` exists to catch.

Do NOT edit `CLAUDE.md` in this wave - its stale figures are wave C's scope (plan 340-04) and
splitting them here would break wave C's own same-commit lockstep requirement.
  </action>

  <verify>
    <automated>node tests/test-canon-frozen-scalars-floor.cjs &amp;&amp; node tests/test-canon-entry-31-two-gauge-floor.cjs &amp;&amp; node tests/test-canon-entry-36-shape-declaration-floor.cjs &amp;&amp; node tests/test-195-canon-7-kind-floor.cjs &amp;&amp; grep -c "^Version: 1.25$" docs/MINDRIAN-CANON.md | grep -qx 1 &amp;&amp; grep -c "_Mindrian Canon v1.25 - MindrianOS Plugin_" docs/MINDRIAN-CANON.md | grep -qx 1 &amp;&amp; grep -c "A hedge word is not a source." docs/MINDRIAN-CANON.md | grep -qx 1 &amp;&amp; grep -c "A hedge word is not a source." agents/larry-extended.md | grep -qx 1</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^Version: 1.25$" docs/MINDRIAN-CANON.md` equals 1 and `grep -c "^Version: 1.24$" docs/MINDRIAN-CANON.md` equals 0.
    - `grep -c "_Mindrian Canon v1.25 - MindrianOS Plugin_" docs/MINDRIAN-CANON.md` equals 1.
    - `grep -c "### The Sourced Claims Doctrine (HARD requirement)" docs/MINDRIAN-CANON.md` equals 1.
    - `grep -c "A hedge word is not a source." docs/MINDRIAN-CANON.md` equals 1 AND the same grep against `agents/larry-extended.md` equals 1 (the mirror landed, not just the Canon).
    - `grep -c "sourced or absent" docs/MINDRIAN-CANON.md` is at least 1 and the same grep against `agents/larry-extended.md` is at least 1.
    - `grep -c "^38\. \*\*" docs/MINDRIAN-CANON.md` equals 1 (entry 38 exists exactly once).
    - For every n in 1..37, `grep -c "^${n}\. \*\*" docs/MINDRIAN-CANON.md` is at least 1 (prior entries preserved).
    - The only deletions in the Canon this wave are the two version tokens: `git diff docs/MINDRIAN-CANON.md | grep "^-" | grep -v "^---" | wc -l` equals 2, and both removed lines contain a `1.24` version token.
    - `node tests/test-canon-frozen-scalars-floor.cjs` exits 0 (frozen scalars unweakened).
    - `node tests/test-canon-entry-31-two-gauge-floor.cjs` and `node tests/test-canon-entry-36-shape-declaration-floor.cjs` both exit 0 at the new v1.25 anchor.
    - `grep -c "v1.25" docs/CANON-PHASE-MAP.md` is at least 2 (the reference line plus the new row) and `grep -c "340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d" docs/CANON-PHASE-MAP.md` is at least 1.
    - `node scripts/check-shape-declaration.cjs --check` exit code and violation count are unchanged from the baseline recorded in `340-LIVE-VERIFICATION.md` (the persona-file edit introduced no new declaration violation).
    - `grep -c "$(printf '\xe2\x80\x94')" docs/MINDRIAN-CANON.md agents/larry-extended.md docs/CANON-PHASE-MAP.md` returns 0 for all three files.
    - All edits land in ONE git commit.
  </acceptance_criteria>

  <done>The Canon reads v1.25, Part 12 carries the Sourced Claims Doctrine, Larry's shipped persona carries the same rule in his own voice, Appendix D carries entry 38 with entries 1-37 untouched, the map carries its v1.25 row, and every carried-forward floor test is green at the new anchor.</done>
</task>

<task type="auto">
  <name>Task 3: Write the entry-38 floor test and flip its aggregator leg from SKIPPED to PASSED</name>

  <files>tests/test-canon-entry-38-sourced-claims-floor.cjs</files>

  <read_first>
    - `tests/test-canon-entry-36-shape-declaration-floor.cjs` IN FULL - the template for a heavier doctrine-amendment floor test: the `'use strict'` + `node:assert`/`node:path`/`node:fs` imports, the `REPO_ROOT`/`CANON_PATH`/`MAP_PATH` constants, the `ok(name, cond)` helper, the `sliceByHeader` and `collapse` helpers, the `sliceEntry` entry-body isolation helper, the prior-entry loop over the Appendix D slice only, the frozen-scalar assertions, the version-anchor assertions, the map-row check, and the `PASS ... assertions` / `>>> <file>: PASSED` banner. Copy the helpers VERBATIM; PATTERNS.md states these files are deliberately standalone and duplication across them is the convention, not a DRY violation.
    - `tests/test-canon-entry-31-two-gauge-floor.cjs` - the multi-location placement-proof precedent
    - `.planning/phases/340-.../340-02-DRAFT-PROSE.md` - the APPROVED anchor strings this test pins. If the navigator used APPROVE WITH EDITS, pin the EDITED strings.
    - `.planning/phases/340-.../340-PATTERNS.md` section "THE PRIMARY NEW-FILE PATTERN" including its "Assertion style rules (hard constraints)"
    - `tests/run-all-340.sh` - confirm the `run_if` leg already names this exact filename
  </read_first>

  <action>
Create `tests/test-canon-entry-38-sourced-claims-floor.cjs`. Copy the imports, the `ok` helper,
`sliceByHeader`, `collapse`, `sliceEntry` and the output banner verbatim from
`tests/test-canon-entry-36-shape-declaration-floor.cjs`. Add a third file read for
`agents/larry-extended.md` alongside the existing canon and map reads.

Doc-comment header, house style: the phase slug and plan (`340-02`), the entry number (38), the
requirement id (CANON-01), the navigator-approval date from Task 1, a numbered list of what each
Test asserts and why, and the two mandatory house statements "NEVER asserts a raw count of
Appendix D entries" and "House rule: hyphens only, no em-dashes. Canon Part 8: zero Brain /
network."

Assertions, in this order:

Test 1 - Part 12 placement proof. Slice the Canon with `sliceByHeader(canon, /^## Part 12 - /)` and
assert the subsection heading `### The Sourced Claims Doctrine (HARD requirement)` and both anchor
strings `sourced or absent` and `A hedge word is not a source.` are present INSIDE that slice, not
merely somewhere in the file.

Test 2 - the pre-existing Elevation tone rule is unweakened. Assert the Part 12 slice still
contains `hedged, cautious, evidence-backed, NEVER confident` byte-identical, proving the new
doctrine extended rather than replaced it.

Test 3 - persona mirror proof. Read `agents/larry-extended.md` and assert both anchor strings
`sourced or absent` and `A hedge word is not a source.` are present. This is the assertion that
makes the mirror structurally required rather than aspirational.

Test 4 - entry 38 body isolation. Slice `## Appendix D`, collapse it, isolate entry 38 with
`sliceEntry(collapsedAppendixD, 38)`, and assert that slice contains `CANON-01`, a reference to
SEED-086, the navigator-approval language, and the byte-identical frozen-scalar restatement
`MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15 gate`.

Test 5 - prior entries preserved. Loop `n = 1..37` over the Appendix D slice ONLY (never the whole
canon, to avoid a low number leaking a false match from an unrelated Part) and assert `^n\.\s` is
present per-number. Never assert sequential ordering (entries 17/18 are intentionally out of
numeric order in the real document) and NEVER assert a raw count of entries, so a future entry 39
or 40 cannot false-fail this test.

Test 6 - frozen scalars byte-present. Copy entry-36's Test 7 pattern verbatim: assert `/MAX_K=3/`,
`/DIAL_REACH_K=6/` and `/0\.70\/0\.15/` are byte-present in the canon.

Test 7 - version bump. Assert `/^Version: 1\.25$/m` and
`/_Mindrian Canon v1\.25 - MindrianOS Plugin_/` against the canon.

Test 8 - map row, light. Assert `docs/CANON-PHASE-MAP.md` carries a `v1.25` token, an `entry 38`
reference, and the phase slug `340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d`.

Assertion style, hard constraints from PATTERNS.md: byte-presence and byte-absence only, via
`.includes()` or `.test()`. Never `.length`, never `.size`, never a count of Appendix D entries.

`tests/run-all-340.sh` already carries the `run_if` leg naming this exact filename, so no edit to
the aggregator is needed - creating the file is what flips that leg from SKIPPED to PASSED.
Confirm that is what happened rather than assuming it.
  </action>

  <verify>
    <automated>node tests/test-canon-entry-38-sourced-claims-floor.cjs &amp;&amp; bash tests/run-all-340.sh &amp;&amp; test "$(bash tests/run-all-340.sh 2>/dev/null | grep -c 'SKIPPED (file not present')" -eq 2 &amp;&amp; test "$(bash tests/run-all-340.sh 2>/dev/null | grep -c ': PASSED')" -eq 5</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-canon-entry-38-sourced-claims-floor.cjs` exits 0 and prints a `>>> test-canon-entry-38-sourced-claims-floor.cjs: PASSED` banner.
    - `bash tests/run-all-340.sh` exits 0 with exactly 5 `: PASSED` lines, 2 `SKIPPED (file not present` lines, and 0 `: FAILED` lines.
    - No count-based assertion: `grep -v '^ \*' tests/test-canon-entry-38-sourced-claims-floor.cjs | grep -v '^//' | grep -c "\.size\|appendixD.length"` equals 0.
    - `grep -c "larry-extended.md" tests/test-canon-entry-38-sourced-claims-floor.cjs` is at least 1 (the persona mirror is genuinely asserted, not assumed).
    - `grep -c "for (let n = 1; n <= 37" tests/test-canon-entry-38-sourced-claims-floor.cjs` equals 1 (prior-entry loop present and correctly bounded).
    - A deliberate negative proof, run and then reverted: temporarily remove the mirror clause from `agents/larry-extended.md`, confirm `node tests/test-canon-entry-38-sourced-claims-floor.cjs` exits NON-ZERO, then restore the file and confirm it exits 0 again. Record both exit codes in the SUMMARY. A floor test that cannot go red does not guard anything.
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-canon-entry-38-sourced-claims-floor.cjs` returns 0.
  </acceptance_criteria>

  <done>Wave A's amendment is guarded by a floor test that provably goes red when the doctrine or its persona mirror is removed, and the phase aggregator carries it as a real green leg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft prose -> constitutional text | The only crossing point is the navigator's APPROVE at Task 1. Task 2 may not run until that verdict is recorded |
| Canon -> Larry runtime behavior | `agents/larry-extended.md` is the enforced system prompt; the Canon alone is aspirational. The doctrine crosses this boundary only because Task 2 writes both and Task 3 asserts both |
| local -> Brain | None. Every edit is a local markdown or `.cjs` file write; no Brain call, no network call, no user-room bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-340-04 | Elevation of privilege | `docs/MINDRIAN-CANON.md` | mitigate | An executor writing canon bytes without the navigator's sign-off would seize constitutional authority it does not hold. Task 1 is a `gate="blocking-human"` checkpoint whose acceptance criteria assert `git diff --name-only` on the constitutional files is empty at halt time |
| T-340-05 | Tampering | Appendix D entries 1-37 | mitigate | Task 2's acceptance criteria assert every prior entry marker is still present and cap total Canon line deletions at exactly 2 (the version tokens). Task 3's Test 5 pins entries 1-37 permanently |
| T-340-06 | Tampering | Part 3 frozen scalars | mitigate | `tests/test-canon-frozen-scalars-floor.cjs` runs in Task 2's verify and as a required-green `run` leg of `tests/run-all-340.sh`; entry 38 restates the scalars byte-identical rather than re-deriving them |
| T-340-07 | Spoofing | floor-test coverage | mitigate | Task 3's acceptance criteria require a deliberate red-proof (break the mirror, confirm non-zero exit, restore) so a vacuously-passing test cannot ship |
| T-340-08 | Information disclosure | Brain wire | accept | The amendment mints no reach, no edge, no node and opens no Brain wire; entry 38 states this explicitly per the entries 34-37 precedent |
| T-340-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable: zero packages installed in this phase (340-RESEARCH.md "Package Legitimacy Audit: Not applicable"). The new floor test uses only `node:assert`, `node:fs`, `node:path` |
</threat_model>

<verification>
- `bash tests/run-all-340.sh` exits 0: 5 PASS, 0 FAIL, 2 SKIP.
- `node tests/test-canon-frozen-scalars-floor.cjs` exits 0.
- The Canon reads `Version: 1.25` in the header and `_Mindrian Canon v1.25 - MindrianOS Plugin_` in the footer.
- `agents/larry-extended.md` and `docs/MINDRIAN-CANON.md` both contain `A hedge word is not a source.` exactly once each.
- The navigator's literal APPROVE reply and its date are recorded in `340-02-SUMMARY.md`.
</verification>

<success_criteria>
- Canon v1.25 carries the Sourced Claims Doctrine in Part 12 and its provenance in Appendix D entry 38.
- Larry's shipped persona carries the behavioral mirror, so the doctrine binds runtime output and not only the constitution.
- No frozen scalar moved, no prior Appendix D entry moved, and the whole wave landed as one commit.
- The floor test guarding it has been proven capable of going red.
</success_criteria>

## Artifacts this phase produces

Newly created by THIS plan (exclude from drift and source-grounding checks - these symbols do not
exist on `main` before this plan runs):

| Artifact | Kind | Created by |
|----------|------|-----------|
| `tests/test-canon-entry-38-sourced-claims-floor.cjs` | new floor test | 340-02 Task 3 |
| `.planning/phases/340-.../340-02-DRAFT-PROSE.md` | new phase record (draft prose, gate input) | 340-02 Task 1 |
| Canon Part 12 subsection `### The Sourced Claims Doctrine (HARD requirement)` | new constitutional text | 340-02 Task 2 |
| Canon Appendix D entry 38 | new constitutional text | 340-02 Task 2 |
| Canon version token `1.25` (header, footer, map reference line, map version-history row) | new version token | 340-02 Task 2 |
| The `agents/larry-extended.md` Sourced Claims mirror clause | new persona text | 340-02 Task 2 |
| Requirement ID `CANON-01` | new requirement id | minted at plan time; registered in `.planning/REQUIREMENTS.md` by 340-05 |

Pre-existing and only MODIFIED (version anchors moved 1.24 -> 1.25, all other assertions
unweakened): `tests/test-canon-entry-31-two-gauge-floor.cjs`,
`tests/test-canon-entry-36-shape-declaration-floor.cjs`, `docs/CANON-PHASE-MAP.md`.

<output>
Create `.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-02-SUMMARY.md` when done. Record the navigator's literal checkpoint reply and its date.
</output>
