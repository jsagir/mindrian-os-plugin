---
phase: 340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d
plan: 04
type: execute
wave: 4
depends_on: [340-03]
files_modified:
  - docs/MINDRIAN-CANON.md
  - docs/CANON-PHASE-MAP.md
  - CLAUDE.md
  - tests/test-canon-entry-40-corpus-figures-floor.cjs
  - tests/test-canon-entry-31-two-gauge-floor.cjs
  - tests/test-canon-entry-36-shape-declaration-floor.cjs
  - .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-04-DRAFT-PROSE.md
autonomous: false
requirements: [CANON-05, CANON-06, CANON-07, CANON-08, CANON-09]
canon_parts: [2, 6, 7, 8, 11]

must_haves:
  truths:
    - "The Canon's Glossary names the Brain's live origin theo-mcp.onrender.com; the retired pws-brain-mcp.onrender.com origin no longer appears in any live Part."
    - "Part 2's Engine 1 no longer describes a retired vector backend; it names the shipped local e5 embedding path."
    - "Part 7's reuse-before-build instruction no longer sends a builder to search a command surface roughly a quarter the real size, and the obligation itself is not softened."
    - "CLAUDE.md's parallel stale figures are corrected in the SAME commit as the Canon prose, so the drift this phase closes does not survive one file over."
    - "The navigator APPROVED the exact prose at a blocking checkpoint before any byte reached docs/MINDRIAN-CANON.md, and separately ruled three named judgment calls."
    - "Appendix D entries 1-39 are byte-preserved, including entries 11, 13, 16 and 36 which carry the historical Pinecone and 126-surface figures."
  artifacts:
    - path: "docs/MINDRIAN-CANON.md"
      provides: "Appendix C Theo origin + Part 2 e5-local backend + Part 7 enumerated surface + Part 11 refreshed snapshot + Appendix D entry 40 + version 1.27"
      contains: "theo-mcp.onrender.com"
    - path: "CLAUDE.md"
      provides: "Same-commit correction of the parallel stale figures and the dead docu-optimizer skill row"
      contains: "enumerated from disk"
    - path: "tests/test-canon-entry-40-corpus-figures-floor.cjs"
      provides: "Slice-scoped presence AND absence guard across four Canon sections plus CLAUDE.md"
      exports: []
  key_links:
    - from: "docs/MINDRIAN-CANON.md Appendix C"
      to: "lib/core/brain-client.cjs getBrainUrl()"
      via: "corrected live origin citation"
      pattern: "theo-mcp\\.onrender\\.com"
    - from: "docs/MINDRIAN-CANON.md Part 7"
      to: "CLAUDE.md Canon Compliance Core Part 7 line"
      via: "same-commit lockstep figure correction"
      pattern: "enumerated from disk"
---

<objective>
Amendment Wave C: correct every stale figure and retired-backend reference the part-by-part audit
found, in the Canon AND in CLAUDE.md in the same commit, as Appendix D entry 40 in the
"corpus figures corrected" voice of entries 13 and 16. Canon v1.26 -> v1.27.

Four corrections land as one entry because they are one act - the Canon describing a stack that
moved underneath it - and because entries 13 and 16 set the precedent for a single factual entry
normalizing several number surfaces at once:

1. **Appendix C Glossary, the Brain origin (CANON-05).** The Glossary still defines Brain as
   `pws-brain-mcp.onrender.com`, the retired Memgraph incumbent. Phase 339 flipped the live default
   to `theo-mcp.onrender.com` in production. Per Phase 339 D-09 the fix is NOT to rename "Brain" to
   "Theo" throughout: the constitutional role name stays "Brain" (Part 8's boundary term) and Theo
   is named as the current implementation. Zero mentions of Theo exist in the Canon today.

2. **Part 2 Engine 1, the vector backend (CANON-06).** Part 2 states cross-domain match runs
   "against Pinecone embeddings (12,401 methodology nodes in Brain's semantic index)". CLAUDE.md's
   own stack table states plainly that Pinecone is RETIRED and that e5 (multilingual-e5-large,
   1024-dim) embeds LOCALLY with no network egress. The command surface Part 2 cites is unaffected;
   only the backend description is stale.

3. **Part 7 Reuse Before Build, the command count (CANON-07).** "Search the 25 methodology commands
   first" is a PRESCRIPTIVE instruction whose correctness depends on its number, and the live
   `commands/` directory holds roughly 4.5 times that. The same figure is repeated in CLAUDE.md.

4. **Part 11 CIRS, the declared-surface snapshot (CANON-08).** Part 11's own doctrine already says
   the count is "NEVER a frozen scalar... an illustrative snapshot, not a canon-frozen constant a
   future gate may hardcode", so the DOCTRINE needs no change - only the illustrative parenthetical
   in Part 11's body and in CLAUDE.md's Part 11 paragraph are stale.

Plus the CLAUDE.md sibling sweep (CANON-09), which is not optional: 340-RESEARCH.md Pitfall 3 names
"amending the Canon without the parallel CLAUDE.md fix in the same wave" as how this exact drift
class re-appears one file over. Included is the dead `.claude/skills/docu-optimizer/SKILL.md` row in
CLAUDE.md's Project Skills table, which the planner's pre-check found does not exist on disk - the
resolution of 340-RESEARCH.md Open Question 3.

This is D-03's widened scope closing out: findings 2, 3 and 4 came from the part-by-part sweep the
navigator asked for, not from the original four-front floor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-CONTEXT.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-RESEARCH.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-PATTERNS.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-01-SUMMARY.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: BLOCKING navigator gate - draft the corrections, surface three rulings, halt for APPROVE</name>

  <files>.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-04-DRAFT-PROSE.md</files>

  <read_first>
    - `docs/MINDRIAN-CANON.md` Appendix C IN FULL - especially the `- Brain - the remote methodology repository (pws-brain-mcp.onrender.com).` line
    - `docs/MINDRIAN-CANON.md` Part 2 lines 25-60 - the Engine 1 three-layer block (the `CROSS-DOMAIN MATCH` line) and the `Code references:` bullet list
    - `docs/MINDRIAN-CANON.md` Part 7 IN FULL - both figure-bearing sentences: "search the 25 methodology commands first" and "which of the 25 does this replace or extend"
    - `docs/MINDRIAN-CANON.md` Part 11 lines 547-558 - the R16 paragraph whose parenthetical carries `(126 as of this phase: 105 + 9 + 3 + 9, plus 5 skills exempt via connector.excluded - an illustrative snapshot, not a canon-frozen constant a future gate may hardcode)`. The self-disclaiming doctrine language around it must SURVIVE.
    - `docs/MINDRIAN-CANON.md` Appendix D entries 11, 13, 16 and 36 - these name Pinecone and the historical corpus/surface figures. HISTORICAL RECORD, OFF LIMITS. Read them for the "corpus figures corrected" voice entry 40 must match, and so you do not mistake them for live claims.
    - `docs/MINDRIAN-CANON.md` Appendix D entries 38 and 39 - this phase's own prior entries, so entry 40 reads as their sibling
    - `CLAUDE.md` lines 66-80 (Canon Compliance Core), line 109 (Core Value), line 159 (Conventions reuse bullet), the Technology Stack rows for Theo and e5, and the `## Project Skills` table
    - `lib/core/brain-client.cjs` - `BRAIN_URL`, `getBrainUrl()`, `THEO_ORIGINS`
    - `docs/CORPUS-STATS.generated.md` - read before citing any corpus figure. CLAUDE.md states it describes the PRIOR Memgraph-backed incumbent's corpus and has NOT been regenerated against Theo; a figure from it carries that caveat or is not carried at all.
    - `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-CONTEXT.md` - D-09 ("Brain names a role, Theo is the current implementation") and D-12 (`getBrainUrl()` is the single origin resolver)
    - `.planning/phases/340-.../340-LIVE-VERIFICATION.md` - the Wave C section: today's real four-glob counts, the declaring-versus-exempt split, the `commands/*.md` count, and the docu-optimizer existence finding. Use THESE numbers, not 340-RESEARCH.md's.
  </read_first>

  <action>
Write a DRAFT ONLY to `340-04-DRAFT-PROSE.md`. Write nothing to `docs/MINDRIAN-CANON.md`,
`CLAUDE.md` or `docs/CANON-PHASE-MAP.md` in this task. Then present the draft and HALT for the
navigator's verdict plus three named rulings.

Draft six pieces:

(1) **Appendix C Glossary Brain line (CANON-05).** Rewrite the single `- Brain - ...` bullet so it
names the live origin `theo-mcp.onrender.com`, keeps "Brain" as the constitutional ROLE name per
Phase 339 D-09, and adds a short parenthetical naming Theo as the current implementation and
`getBrainUrl()` in `lib/core/brain-client.cjs` as the single origin resolver every runtime site
reads. Required: the Appendix C slice must contain `theo-mcp.onrender.com` and must NOT contain
`pws-brain-mcp.onrender.com`. The existing sentence "Strategic thinking tools only. Never a store
for user data." stays byte-identical - it is Part 8's boundary restated and a URL fix must not
weaken it.

(2) **Part 2 Engine 1 backend (CANON-06).** Rewrite the `CROSS-DOMAIN MATCH` backend clause and the
`- Pinecone 12,401 embeddings ...` code-reference bullet to describe the shipped path: e5 /
multilingual-e5-large, 1024-dim, embedded LOCALLY with no network egress, Pinecone retired.
Required: the Part 2 slice must contain `multilingual-e5-large` and `embedded LOCALLY` (matching
CLAUDE.md's own stack-table wording so the two surfaces agree) and must NOT contain `Pinecone`. Do
NOT carry the `12,401` figure forward as a live number - `docs/CORPUS-STATS.generated.md` describes
the retired incumbent and has not been regenerated against Theo. Draft BOTH options (omit the
corpus size entirely; or state it with the caveat and a pointer to the generated doc) and let the
navigator pick. The five command wrappers (`/mos:whitespace`, `/mos:find-bottlenecks`,
`/mos:find-connections`, `/mos:find-analogies`, `/mos:score-innovation`) are confirmed still present
and stay byte-identical.

(3) **Part 7 Reuse Before Build (CANON-07).** Rewrite both figure-bearing sentences so the
instruction is correct at any size. Required: the Part 7 slice must contain `enumerated from disk`
(or the navigator-approved equivalent) and must NOT contain `25 methodology commands`. The rewrite
must (a) keep the reuse-before-build OBLIGATION exactly as strong as today - this is a binding Part,
not a nicety; (b) name the enumeration source the schema itself defines, `commands/*.md` (with
`agents/*.md`, `pipelines/*/CHAIN.md`, `skills/*/SKILL.md` where the sentence spans surface
classes); (c) state today's live figure only as an explicitly illustrative snapshot, never as the
thing to search; (d) rewrite "which of the 25 does this replace or extend" so it no longer binds to
a number. Pitfall 2 governs: a number in a PRESCRIPTIVE sentence gets de-frozen, not merely updated
to a new literal that goes stale the same way.

(4) **Part 11 R16 snapshot (CANON-08).** Replace ONLY the stale parenthetical in Part 11's body with
today's verified snapshot from `340-LIVE-VERIFICATION.md`, attributed to THIS amendment (entry 40)
rather than to Phase 190. The surrounding self-disclaiming sentences stay byte-identical:
`NEVER a frozen scalar`, `ALWAYS enumerated from disk at run time`, and `not a canon-frozen constant
a future gate may hardcode`. Required: the Part 11 slice must NOT contain `126 as of this phase`.
Appendix D entry 36's own historical figure is a PRIOR ENTRY and stays byte-identical - entry 40
supersedes it in place by recording the newer read, the way entry 16 superseded entry 13 without
editing it.

(5) **CLAUDE.md siblings (CANON-09).** Draft each parallel edit as its own line item with current
text and proposed text: the Canon Compliance Core Part 7 bullet; the Conventions reuse-before-build
bullet; the Core Value sentence (a DISAMBIGUATION - see ruling A below, do not rewrite
unilaterally); the Canon Compliance Core Part 11 `(currently 126 declared + 5 skill-exempt)`
parenthetical; and the `## Project Skills` table row naming `.claude/skills/docu-optimizer/SKILL.md`
(draft both the recommended removal and the alternative, and let the navigator rule). Do NOT touch
CLAUDE.md's Technology Stack rows for Theo or e5 - live verification confirmed both current.

(6) **Appendix D entry 40**, opening exactly `40. **`, in the LIGHT "corpus figures corrected" voice
of entries 13 and 16 rather than the heavy doctrine-amendment voice of entries 31/34/36: what was
corrected, on what live read, with the OLD value named alongside the new one exactly as entry 16 did
("was ..."), the explicit statement that this is a factual and stack-currency correction with NO
doctrine change in Parts 7, 8 or 11, the navigator-APPROVED-at-a-blocking-checkpoint line with the
version target v1.26 -> v1.27, the entry-31 self-binding-clause release recorded truthfully per the
entry-20 navigator-authority override, the frozen-scalar clause
`MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15 gate`, the "mints NO reach/edge/node, opens NO Brain wire"
clause (true for this entry), and the same-commit CLAUDE.md lockstep named explicitly so a future
reader sees the two files moved together. List every file this wave touches in the manifest.

House rules: hyphens only, never an em-dash; no invented citation; every figure traceable to
`340-LIVE-VERIFICATION.md` or to a file this task read.

After writing the draft, present it and HALT. Do not proceed to Task 2 on silence or inference.
Record the navigator's literal reply, including all three rulings, in the plan SUMMARY.
  </action>

  <what-built>
A draft-only prose file, `340-04-DRAFT-PROSE.md`, containing six pieces of text exactly as they
would land: the Appendix C Brain bullet, the Part 2 backend clause (both corpus-figure options),
the Part 7 rewrite, the Part 11 parenthetical, the itemized CLAUDE.md sibling list (with both
docu-optimizer options), and Appendix D entry 40. Zero constitutional bytes have moved.
  </what-built>

  <how-to-verify>
1. Read `.planning/phases/340-.../340-04-DRAFT-PROSE.md` in full.
2. **Ruling A - the "25 methodology bots" disambiguation.** CLAUDE.md line 109 says "25 methodology
   bots", and CLAUDE.md's own header says `/home/jsagi/MindrianV2/ (V2 production: 25 bot prompts,
   ...)`. So "25" may be a historically ACCURATE count of the ported V2 bot prompts, a different
   thing from the count of `commands/*.md` files. The draft states what the live check found. Rule
   one of: `BOTS` (historically accurate, leave it; only the two "25 methodology commands"
   instruction sentences change), `COMMANDS` (it means the command surface and is stale; rewrite it
   too), or `REPHRASE` (drop the number from the Core Value sentence entirely).
3. **Ruling B - docu-optimizer.** The Project Skills table points at a file that does not exist.
   Rule one of: `REMOVE ROW` (recommended), `RESTORE SKILL` (out of this phase's scope; would be
   registered as a follow-up), or `LEAVE AND FLAG`.
4. **Ruling C - the Part 2 corpus figure.** Omit a corpus size entirely, or state it with the
   "describes the retired incumbent, not regenerated against Theo" caveat. Both are drafted.
5. Confirm Part 7's rewrite keeps the reuse-before-build obligation as strong as today. A de-frozen
   number must not become a softened rule.
6. Confirm Part 11's self-disclaiming doctrine sentences survive byte-identical and only the
   parenthetical moved.
7. Confirm the version target: canon v1.26 -> v1.27.
8. Confirm the anchor strings a floor test will pin forever, or name replacements.

Reply with the verdict AND all three rulings, for example:
`APPROVE. BOTS. REMOVE ROW. Omit the corpus size.`
Or `REJECT: <reason>` (nothing lands; the draft is revised and this gate re-runs), or
`APPROVE WITH EDITS: <the edits>` (the executor applies them, updates the anchor list to match, and
re-presents before writing).

This checkpoint is NOT auto-approvable, regardless of `workflow.auto_advance`. 340-RESEARCH.md
Pitfall 4 names skipping it as this phase's signature failure mode, and the three rulings above are
judgment calls this plan deliberately did not make on the navigator's behalf.
  </how-to-verify>

  <resume-signal>Type "APPROVE" plus all three rulings (BOTS/COMMANDS/REPHRASE, REMOVE ROW/RESTORE SKILL/LEAVE AND FLAG, and the Part 2 corpus-figure choice), or "REJECT: reason", or "APPROVE WITH EDITS: edits"</resume-signal>

  <verify>
    <automated>test -f .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-04-DRAFT-PROSE.md &amp;&amp; grep -q "theo-mcp.onrender.com" .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-04-DRAFT-PROSE.md &amp;&amp; grep -q "multilingual-e5-large" .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-04-DRAFT-PROSE.md &amp;&amp; grep -q "docu-optimizer" .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-04-DRAFT-PROSE.md &amp;&amp; test "$(git diff --name-only docs/MINDRIAN-CANON.md CLAUDE.md | wc -l)" -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `340-04-DRAFT-PROSE.md` exists with six labelled sections: Appendix C, Part 2, Part 7, Part 11, the CLAUDE.md sibling list, and Appendix D entry 40.
    - The draft contains the exact strings `theo-mcp.onrender.com`, `getBrainUrl()`, `multilingual-e5-large`, `embedded LOCALLY`, `enumerated from disk`.
    - The draft's CLAUDE.md section enumerates every edit as a separate line item with current text and proposed text, including the `docu-optimizer` row with both options drafted.
    - The draft's entry-40 text opens with the exact prefix `40. **`, names each old value alongside its new one, and contains the exact substring `MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15 gate`.
    - The draft explicitly states that Appendix D entries 11, 13, 16 and 36 are historical record and stay byte-identical.
    - The draft presents all three rulings as options rather than making a unilateral choice.
    - `grep -c "$(printf '\xe2\x80\x94')" <draft>` returns 0 (no em-dash).
    - `git diff --name-only docs/MINDRIAN-CANON.md CLAUDE.md docs/CANON-PHASE-MAP.md` returns empty at halt time.
    - Execution HALTS and does not proceed to Task 2 until the navigator's literal reply, including all three rulings, is recorded.
  </acceptance_criteria>

  <done>The navigator has replied APPROVE with all three rulings named (or APPROVE WITH EDITS, applied and re-presented), the reply is recorded verbatim with its date in the plan SUMMARY, and neither the Canon nor CLAUDE.md has moved a byte.</done>
</task>

<task type="auto">
  <name>Task 2: Land the atomic lockstep - Appendix C, Part 2, Part 7, Part 11, entry 40, version 1.27, CLAUDE.md siblings, map row, ONE commit</name>

  <files>docs/MINDRIAN-CANON.md, CLAUDE.md, docs/CANON-PHASE-MAP.md, tests/test-canon-entry-31-two-gauge-floor.cjs, tests/test-canon-entry-36-shape-declaration-floor.cjs</files>

  <read_first>
    - `.planning/phases/340-.../340-04-DRAFT-PROSE.md` - the APPROVED text plus the three recorded rulings. Write exactly this, byte-for-byte. Apply the rulings literally; do not re-open a decision the navigator already made.
    - `docs/MINDRIAN-CANON.md` line 3 (`Version: 1.26`) and the footer line (`_Mindrian Canon v1.26 - MindrianOS Plugin_`)
    - `docs/CANON-PHASE-MAP.md` line 6 and the v1.26 row plan 340-03 appended - the row shape your v1.27 row must match
    - `tests/test-canon-entry-31-two-gauge-floor.cjs` and `tests/test-canon-entry-36-shape-declaration-floor.cjs` - their 1.26 version anchors, which now move to 1.27
  </read_first>

  <action>
Land every edit below in ONE commit. The single-commit requirement is load-bearing, not stylistic:
340-RESEARCH.md Pitfall 3 states that fixing the Canon while leaving CLAUDE.md stale re-introduces
the exact drift class this phase exists to close.

1. Canon Appendix C: replace the `- Brain - ...` bullet with the APPROVED text. Every other Glossary
   bullet stays byte-identical.
2. Canon Part 2: replace the `CROSS-DOMAIN MATCH` backend clause and the
   `- Pinecone 12,401 embeddings ...` code-reference bullet with the APPROVED text. The
   DECOMPOSITION and WHITESPACE MAP lines, the five `/mos:` wrappers, the Engine 2 BONO block and
   everything else in Part 2 stay byte-identical.
3. Canon Part 7: replace both figure-bearing sentences with the APPROVED text. The closing "Reuse
   compounds the moat. Net-new surface area dilutes it." stays byte-identical.
4. Canon Part 11: replace ONLY the stale parenthetical inside the R16 paragraph with the APPROVED
   refreshed snapshot. The three self-disclaiming doctrine sentences stay byte-identical and no
   other R1-R16 text is touched.
5. Canon Appendix D: append the APPROVED entry 40 immediately after entry 39 and before the line
   beginning `Conversation transcript reference: session 2026-04-20`. Entries 1 through 39 are
   byte-preserved. Entries 11, 13, 16 and 36 carry the historical Pinecone and 126-surface figures
   and MUST NOT be edited - entry 40 supersedes them in place by recording the newer read, exactly
   as entry 16 superseded entry 13 without touching it.
6. Canon version: header `Version: 1.26` becomes `Version: 1.27`; footer
   `_Mindrian Canon v1.26 - MindrianOS Plugin_` becomes `_Mindrian Canon v1.27 - MindrianOS Plugin_`.
7. `CLAUDE.md`: apply each APPROVED sibling edit exactly as ruled - the Canon Compliance Core Part 7
   bullet, the Conventions reuse-before-build bullet, the Part 11 parenthetical, the Core Value
   sentence per ruling A, and the Project Skills docu-optimizer row per ruling B. Touch nothing
   else: the WORKSPACE GUARD, the handoff table, the Technology Stack table, the Dev-Research
   Compositing section and the grounding-sources section are all out of scope for this wave.
8. `docs/CANON-PHASE-MAP.md`: reference line 6 `(v1.26)` becomes `(v1.27)`; append a
   `| v1.27 | TBD | <execution date> | ...` version-history row matching the v1.26 row's shape. The
   row names: the phase SLUG `340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d`
   (never the phase number alone), entry 40, requirement ids CANON-05 through CANON-09,
   `NAVIGATOR-GATED` with the approval date and the three rulings, the four corrected surfaces with
   old and new values, the explicit CLAUDE.md same-commit lockstep, the "factual/stack-currency
   correction, no doctrine change" clause, the frozen-scalars-byte-identical clause, and a `Fences:`
   clause naming `tests/test-canon-entry-40-corpus-figures-floor.cjs`,
   `tests/test-canon-frozen-scalars-floor.cjs`, the two version-anchor-bumped tests, this phase's
   entry-38 and entry-39 floor tests, and `bash tests/run-all-340.sh exit 0`.
9. `tests/test-canon-entry-31-two-gauge-floor.cjs` and
   `tests/test-canon-entry-36-shape-declaration-floor.cjs`: move each version anchor 1.26 -> 1.27
   everywhere it appears, including entry-36's CANON-PHASE-MAP `/v1\.26/` token assertion. Both
   files' prior-entry loops and frozen-scalar assertions are UNWEAKENED - change nothing else.

Hard prohibitions: do not change any frozen scalar VALUE; do not remove or reword any Appendix D
entry 1-39; do not rewrite Part 11's self-disclaiming doctrine sentences; do not "correct" the
historical figures in entries 11, 13, 16 or 36; do not edit `lib/core/brain-client.cjs` or any other
implementation file - this phase cites them and never modifies them.
  </action>

  <verify>
    <automated>node tests/test-canon-frozen-scalars-floor.cjs &amp;&amp; node tests/test-canon-entry-31-two-gauge-floor.cjs &amp;&amp; node tests/test-canon-entry-36-shape-declaration-floor.cjs &amp;&amp; node tests/test-canon-entry-38-sourced-claims-floor.cjs &amp;&amp; node tests/test-canon-entry-39-graph-substrate-floor.cjs &amp;&amp; node tests/test-195-canon-7-kind-floor.cjs &amp;&amp; grep -c "^Version: 1.27$" docs/MINDRIAN-CANON.md | grep -qx 1 &amp;&amp; test "$(grep -c '25 methodology commands' docs/MINDRIAN-CANON.md CLAUDE.md | grep -c ':0$')" -eq 2</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^Version: 1.27$" docs/MINDRIAN-CANON.md` equals 1 and `grep -c "^Version: 1.26$" docs/MINDRIAN-CANON.md` equals 0.
    - `grep -c "_Mindrian Canon v1.27 - MindrianOS Plugin_" docs/MINDRIAN-CANON.md` equals 1.
    - `grep -c "theo-mcp.onrender.com" docs/MINDRIAN-CANON.md` is at least 1.
    - The retired origin is gone from the live Glossary: the Appendix C section (from `## Appendix C` to the next `## `) contains zero occurrences of `pws-brain-mcp.onrender.com`. If the approved entry-40 text names it as the superseded value, occurrences are permitted ONLY inside the Appendix D section; Task 3's slice assertions prove this.
    - `grep -c "25 methodology commands" docs/MINDRIAN-CANON.md` equals 0 AND `grep -c "25 methodology commands" CLAUDE.md` equals 0 - the prescriptive form is gone from both files in the same commit.
    - `grep -c "126 declared + 5 skill-exempt" CLAUDE.md` equals 0 and `grep -c "126 as of this phase" docs/MINDRIAN-CANON.md` equals 0.
    - Part 11's self-disclaiming sentences survive: `grep -c "NEVER a frozen scalar" docs/MINDRIAN-CANON.md` is at least 1, `grep -c "ALWAYS enumerated from disk at run time" docs/MINDRIAN-CANON.md` is at least 1, and `grep -c "not a canon-frozen constant a future gate may hardcode" docs/MINDRIAN-CANON.md` is at least 1.
    - `grep -c "^40\. \*\*" docs/MINDRIAN-CANON.md` equals 1, and for every n in 1..39, `grep -c "^${n}\. \*\*" docs/MINDRIAN-CANON.md` is at least 1.
    - The historical entries are byte-identical: `git diff docs/MINDRIAN-CANON.md` shows no removed line containing `Corpus figures corrected (2026-05-20)`, `Corpus figures corrected (2026-06-11)`, `User correction 10`, or `Part 11 R16 (Born-Declared Shape) minted`.
    - Ruling B was applied literally: if `REMOVE ROW`, `grep -c "docu-optimizer" CLAUDE.md` equals 0; if `RESTORE SKILL` or `LEAVE AND FLAG`, the SUMMARY records which, why, and any follow-up registered.
    - `node tests/test-canon-entry-38-sourced-claims-floor.cjs` and `node tests/test-canon-entry-39-graph-substrate-floor.cjs` both exit 0 - waves A and B survived wave C untouched.
    - `node tests/test-canon-frozen-scalars-floor.cjs` exits 0.
    - No em-dash introduced: `grep -c "$(printf '\xe2\x80\x94')" docs/MINDRIAN-CANON.md CLAUDE.md docs/CANON-PHASE-MAP.md` returns 0 for all three files.
    - `git status --porcelain lib/ scripts/` returns empty: no implementation file was modified.
    - Every edit lands in ONE git commit; `git show --stat HEAD` lists `docs/MINDRIAN-CANON.md`, `CLAUDE.md`, `docs/CANON-PHASE-MAP.md` and the two anchor-bumped test files together.
  </acceptance_criteria>

  <done>The Canon reads v1.27 and describes the stack that actually ships; CLAUDE.md carries the identical corrections in the same commit; entry 40 records the provenance in the entries-13/16 voice; entries 1-39 including the historical Pinecone entries are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Write the entry-40 slice-scoped presence-and-absence floor test and flip the last aggregator leg</name>

  <files>tests/test-canon-entry-40-corpus-figures-floor.cjs</files>

  <read_first>
    - `tests/test-canon-entry-36-shape-declaration-floor.cjs` IN FULL - the `sliceByHeader` / `collapse` / `sliceEntry` helpers and the version/map assertions to copy verbatim, plus its Test 5 pattern for asserting FRAMING language rather than a hardcoded number when a doctrine self-disclaims its own count
    - `tests/test-canon-entry-38-sourced-claims-floor.cjs` and `tests/test-canon-entry-39-graph-substrate-floor.cjs` - this phase's two sibling floor tests; match their doc-comment shape and naming
    - `.planning/phases/340-.../340-04-DRAFT-PROSE.md` - the APPROVED anchor strings and the three recorded rulings. Pin the approved text, not the drafted text, and pin only what the rulings actually landed.
    - `CLAUDE.md` - the post-edit state, so the CLAUDE.md assertions match what actually landed
    - `.planning/phases/340-.../340-PATTERNS.md` sections "THE PRIMARY NEW-FILE PATTERN" and the light-versus-heavy entry-style note (this entry is the LIGHT style; size the assertion count to the entry's weight, do not pad it)
  </read_first>

  <action>
Create `tests/test-canon-entry-40-corpus-figures-floor.cjs`. Copy the imports, the `ok` helper,
`sliceByHeader`, `collapse`, `sliceEntry` and the output banner verbatim from
`tests/test-canon-entry-36-shape-declaration-floor.cjs`. Add a third file read for `CLAUDE.md`
alongside the canon and map reads.

Doc-comment header, house style: phase slug and plan (`340-04`), entry number (40), requirement ids
(CANON-05 through CANON-09), the navigator-approval date and the three rulings from Task 1, a
numbered list of what each Test asserts and why, and the two mandatory house statements "NEVER
asserts a raw count of Appendix D entries" and "House rule: hyphens only, no em-dashes. Canon Part
8: zero Brain / network." Add one statement specific to this test: "Absence assertions are
SLICE-SCOPED, never whole-file - the retired names must survive in the Appendix D historical record
and only disappear from the live Parts."

Slices to build: `part2 = sliceByHeader(canon, /^## Part 2 - /)`,
`part7 = sliceByHeader(canon, /^## Part 7 - /)`, `part11 = sliceByHeader(canon, /^## Part 11 - /)`,
`appendixC = sliceByHeader(canon, /^## Appendix C/)`,
`appendixD = sliceByHeader(canon, /^## Appendix D/)`.

Assertions, in this order:

Test 1 - Appendix C, present and absent (CANON-05). The `appendixC` slice CONTAINS
`theo-mcp.onrender.com` and DOES NOT CONTAIN `pws-brain-mcp.onrender.com`. The boundary sentence
`Never a store for user data.` is still present in that slice.

Test 2 - Part 2, present and absent (CANON-06). The `part2` slice CONTAINS `multilingual-e5-large`
and `embedded LOCALLY`, and DOES NOT CONTAIN `Pinecone`. The five command wrappers are still
present in that slice by name.

Test 3 - Part 7, present and absent (CANON-07). The `part7` slice CONTAINS the approved non-frozen
framing anchor (`enumerated from disk` or its approved replacement) and DOES NOT CONTAIN
`25 methodology commands`. The obligation survives: `Reuse compounds the moat.` is still
byte-present in that slice.

Test 4 - Part 11 doctrine unweakened (CANON-08). The `part11` slice STILL CONTAINS all three
self-disclaiming sentences byte-identical: `NEVER a frozen scalar`,
`ALWAYS enumerated from disk at run time`, and `not a canon-frozen constant a future gate may
hardcode`. It DOES NOT CONTAIN `126 as of this phase`. Do NOT assert today's new number - pin the
FRAMING, mirroring entry-36's Test 5 precedent, so a future refresh does not false-fail.

Test 5 - historical record preserved. The `appendixD` slice STILL CONTAINS `Pinecone` and the
entry-16 phrase `Corpus figures corrected (2026-06-11)`, proving the absence assertions above were
slice-scoped and the history was not scrubbed.

Test 6 - CLAUDE.md same-commit lockstep (CANON-09). Read `CLAUDE.md` and assert it DOES NOT CONTAIN
`25 methodology commands` and DOES NOT CONTAIN `126 declared + 5 skill-exempt`. If ruling B was
`REMOVE ROW`, also assert it does not contain `docu-optimizer`. Assert it STILL CONTAINS
`theo-mcp.onrender.com` and `multilingual-e5-large` (its stack table was already current and must
stay so). This test is what makes Pitfall 3 structurally impossible to repeat.

Test 7 - entry 40 body isolation. Isolate entry 40 via `sliceEntry(collapse(appendixD), 40)` and
assert that slice contains `CANON-05`, `CANON-07`, the navigator-approval language, and the
byte-identical frozen-scalar restatement `MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15 gate`.

Test 8 - prior entries preserved. Loop `n = 1..39` over the `appendixD` slice ONLY and assert
`^n\.\s` is present per-number. Never assert ordering, never assert a raw entry count.

Test 9 - frozen scalars byte-present: `/MAX_K=3/`, `/DIAL_REACH_K=6/`, `/0\.70\/0\.15/`.

Test 10 - version bump: `/^Version: 1\.27$/m` and `/_Mindrian Canon v1\.27 - MindrianOS Plugin_/`.

Test 11 - map row, light: `docs/CANON-PHASE-MAP.md` carries a `v1.27` token, an `entry 40`
reference, and the phase slug.

Assertion style, hard constraints: byte-presence and byte-absence only via `.includes()` or
`.test()`. Never `.length`, never `.size`, never a count of Appendix D entries, and never a
hardcoded surface count or corpus count anywhere in this file - pinning a number here would recreate
the exact disease this entry cures.

`tests/run-all-340.sh` already carries the `run_if` leg naming this exact filename; creating the
file flips the LAST SKIP to a PASS. Confirm that rather than assuming it.
  </action>

  <verify>
    <automated>node tests/test-canon-entry-40-corpus-figures-floor.cjs &amp;&amp; bash tests/run-all-340.sh &amp;&amp; test "$(bash tests/run-all-340.sh 2>/dev/null | grep -c 'SKIPPED (file not present')" -eq 0 &amp;&amp; test "$(bash tests/run-all-340.sh 2>/dev/null | grep -c ': PASSED')" -eq 7 &amp;&amp; test "$(bash tests/run-all-340.sh 2>/dev/null | grep -c ': FAILED')" -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-canon-entry-40-corpus-figures-floor.cjs` exits 0 and prints its `>>> ...: PASSED` banner.
    - `bash tests/run-all-340.sh` exits 0 with exactly 7 `: PASSED` lines, 0 `SKIPPED (file not present` lines, and 0 `: FAILED` lines - every leg real, none skipped, matching the bar entry 36 set for `run-all-190.sh`.
    - `grep -c "sliceByHeader" tests/test-canon-entry-40-corpus-figures-floor.cjs` is at least 6 (five slices plus the helper definition), proving the absence assertions are slice-scoped and not whole-file.
    - `grep -c "CLAUDE.md" tests/test-canon-entry-40-corpus-figures-floor.cjs` is at least 1 - the same-commit lockstep is genuinely asserted.
    - `grep -c "for (let n = 1; n <= 39" tests/test-canon-entry-40-corpus-figures-floor.cjs` equals 1.
    - No hardcoded surface or corpus count: `grep -v '^ \*' tests/test-canon-entry-40-corpus-figures-floor.cjs | grep -v '^//' | grep -cE "[0-9]{3,}"` returns 0. Any exception is listed in the SUMMARY with its justification.
    - Four deliberate negative proofs, each run then reverted, with both exit codes recorded in the SUMMARY: (a) re-insert `pws-brain-mcp.onrender.com` into the Appendix C bullet and confirm non-zero exit; (b) re-insert `Pinecone` into the Part 2 slice and confirm non-zero exit; (c) re-insert `25 methodology commands` into `CLAUDE.md` and confirm non-zero exit; (d) delete the entry-16 line `Corpus figures corrected (2026-06-11)` from Appendix D and confirm non-zero exit (proving the history-preservation assertion is live). Restore after each and confirm exit 0.
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-canon-entry-40-corpus-figures-floor.cjs` returns 0.
  </acceptance_criteria>

  <done>Wave C's corrections are guarded by a floor test that provably goes red if any retired name returns to a live Part, if CLAUDE.md drifts back out of lockstep, or if the Appendix D history is scrubbed - and the phase aggregator now runs seven real green legs with zero skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft prose -> constitutional text and project instructions | The only crossing point is the navigator's APPROVE plus three rulings at Task 1; Task 2 may not run before they are recorded |
| live Parts -> historical Appendix D record | Retired names (`pws-brain-mcp.onrender.com`, `Pinecone`, the old figures) must DISAPPEAR from live Parts and SURVIVE in the historical record. Every absence assertion in this wave is slice-scoped for exactly this reason |
| Canon -> CLAUDE.md | Two independent instruction surfaces carrying the same figures. The boundary is closed by the single-commit requirement and by Test 6 of the floor test |
| local -> Brain | None. Every edit is a local markdown or `.cjs` write. Correcting a URL string in a glossary is a documentation-accuracy fix to Part 8's description, not a change to its enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-340-15 | Elevation of privilege | `docs/MINDRIAN-CANON.md`, `CLAUDE.md` | mitigate | Task 1 is a `gate="blocking-human"` checkpoint whose acceptance criteria assert `git diff --name-only` on both instruction surfaces is empty at halt time |
| T-340-16 | Tampering | Appendix D entries 11, 13, 16, 36 (historical figures) | mitigate | Task 2 asserts no removed line matches their opening phrases; Task 3's Test 5 asserts `Pinecone` and the entry-16 phrase are STILL present in the Appendix D slice, so a history scrub fails loudly |
| T-340-17 | Tampering | Part 11 self-disclaiming doctrine | mitigate | Task 2 and Task 3 both assert the three doctrine sentences byte-present; only the parenthetical moves |
| T-340-18 | Repudiation | Part 7 obligation strength | mitigate | De-freezing a number must not soften a binding Part. Task 3's Test 3 asserts `Reuse compounds the moat.` survives byte-identical alongside the new framing |
| T-340-19 | Tampering | `CLAUDE.md` drift re-entry | mitigate | Single-commit requirement in Task 2 plus Test 6 of the floor test, which fails if the stale figure returns to CLAUDE.md even while the Canon stays clean |
| T-340-20 | Information disclosure | Part 8 boundary description | accept | Correcting the Brain's cited origin is a documentation-accuracy fix; the PR gate, the schema caps and the pre-commit `check-brain-boundary.cjs` hook are untouched. No new attack surface is introduced by a glossary URL |
| T-340-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable: zero packages installed in this phase (340-RESEARCH.md "Package Legitimacy Audit: Not applicable"). The new floor test uses only `node:assert`, `node:fs`, `node:path` |
</threat_model>

<verification>
- `bash tests/run-all-340.sh` exits 0: 7 PASS, 0 FAIL, 0 SKIP - every leg real, none skipped.
- The Canon reads `Version: 1.27` in header and footer.
- `grep -c "25 methodology commands" docs/MINDRIAN-CANON.md CLAUDE.md` returns 0 for both files.
- The Appendix C slice contains zero occurrences of `pws-brain-mcp.onrender.com`.
- Appendix D still contains `Pinecone` and `Corpus figures corrected (2026-06-11)` - history preserved.
- `git show --stat HEAD` lists the Canon, CLAUDE.md and the map in ONE commit.
- The navigator's literal APPROVE reply, its date, and all three rulings are recorded in `340-04-SUMMARY.md`.
</verification>

<success_criteria>
- The Canon describes the stack that actually ships: Theo as the Brain's current implementation, e5-local as the vector path, a command surface enumerated from disk, and a surface-count snapshot matching today's read.
- CLAUDE.md carries the identical corrections in the same commit, so the drift class this phase closes cannot survive one file over.
- The historical Appendix D record is untouched, including every prior entry naming a now-retired figure.
- No frozen scalar moved, no binding obligation softened, and the phase aggregator runs seven real green legs with zero skips.
</success_criteria>

## Artifacts this phase produces

Newly created by THIS plan (exclude from drift and source-grounding checks):

| Artifact | Kind | Created by |
|----------|------|-----------|
| `tests/test-canon-entry-40-corpus-figures-floor.cjs` | new floor test | 340-04 Task 3 |
| `.planning/phases/340-.../340-04-DRAFT-PROSE.md` | new phase record (draft prose, gate input) | 340-04 Task 1 |
| Canon Appendix D entry 40 | new constitutional text | 340-04 Task 2 |
| Canon version token `1.27` (header, footer, map reference line, map version-history row) | new version token | 340-04 Task 2 |
| Requirement IDs `CANON-05`, `CANON-06`, `CANON-07`, `CANON-08`, `CANON-09` | new requirement ids | minted at plan time; registered in `.planning/REQUIREMENTS.md` by 340-05 |

Pre-existing and MODIFIED IN PLACE (corrections, not new symbols): the Canon's Appendix C Brain
bullet, its Part 2 Engine 1 backend clause and code-reference bullet, its Part 7 two figure-bearing
sentences, its Part 11 R16 parenthetical; `CLAUDE.md`'s Canon Compliance Core Part 7 bullet, its
Conventions reuse bullet, its Part 11 parenthetical, its Core Value sentence (per ruling A) and its
Project Skills docu-optimizer row (per ruling B); `docs/CANON-PHASE-MAP.md`;
`tests/test-canon-entry-31-two-gauge-floor.cjs` and
`tests/test-canon-entry-36-shape-declaration-floor.cjs` (version anchors 1.26 -> 1.27 only).

Pre-existing and only CITED, never modified: `lib/core/brain-client.cjs`,
`docs/CORPUS-STATS.generated.md`, `data/hitl-shape-declaration-schema.json`,
`scripts/check-shape-declaration.cjs`.

<output>
Create `.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-04-SUMMARY.md` when done. Record the navigator's literal checkpoint reply, its date, all three rulings, and the four red-proof exit codes.
</output>
