---
phase: 340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d
plan: 03
type: execute
wave: 3
depends_on: [340-02]
files_modified:
  - docs/MINDRIAN-CANON.md
  - docs/CANON-PHASE-MAP.md
  - tests/test-canon-entry-39-graph-substrate-floor.cjs
  - tests/test-canon-entry-31-two-gauge-floor.cjs
  - tests/test-canon-entry-36-shape-declaration-floor.cjs
  - .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-03-DRAFT-PROSE.md
autonomous: false
requirements: [CANON-02, CANON-03, CANON-04]
canon_parts: [4, 6, 8, 9]

must_haves:
  truths:
    - "Part 9 names TWO separate constitutional chokepoints - lib/core/navigation.cjs for navigate/read and lib/core/node-insert.cjs for write - as two different constitutional properties, not one citation folded into existing navigation.cjs prose (D-01)."
    - "Part 4's closed typed-edge vocabulary prose names every edge type that lib/core/navigation/edges.cjs actually ships, and states that the authoritative list is enumerated from that module at run time rather than frozen as a literal count."
    - "Appendix B's ICM Layer table carries real file citations to the shipped L1/L2/L3 mechanisms, matching how the rest of the Canon cites concrete files (D-02)."
    - "The navigator APPROVED the exact prose at a blocking checkpoint before any byte reached docs/MINDRIAN-CANON.md."
    - "Appendix D entries 1-38 are byte-preserved and the frozen Part 3 scalars are unweakened after the wave lands."
  artifacts:
    - path: "docs/MINDRIAN-CANON.md"
      provides: "Part 9 two-chokepoint split + Part 4 edge reconciliation + Appendix B citations + Appendix D entry 39 + version 1.26"
      contains: "lib/core/node-insert.cjs"
    - path: "tests/test-canon-entry-39-graph-substrate-floor.cjs"
      provides: "Multi-Part placement-proof floor guard for entry 39 across Part 4, Part 9 and Appendix B"
      exports: []
  key_links:
    - from: "docs/MINDRIAN-CANON.md Part 9"
      to: "lib/core/node-insert.cjs"
      via: "named write chokepoint citation"
      pattern: "lib/core/node-insert\\.cjs"
    - from: "docs/MINDRIAN-CANON.md Part 4"
      to: "lib/core/navigation/edges.cjs"
      via: "named source of truth for the closed ALLOWED_EDGE_TYPES set"
      pattern: "lib/core/navigation/edges\\.cjs"
    - from: "docs/MINDRIAN-CANON.md Appendix B"
      to: "lib/core/room-skeleton-scaffold.cjs"
      via: "ICM L1/L2/L3 implementing-file citation"
      pattern: "lib/core/room-skeleton-scaffold\\.cjs"
---

<objective>
Amendment Wave B: bring the Canon's description of its own graph and room substrate up to what
actually shipped, as Appendix D entry 39, bumping the Canon from v1.25 to v1.26.

Three currency gaps land together because they are one doctrinal act - the Canon's account of its
own local substrate - and because entry 31 set the precedent for one entry whose doctrine spans
several Parts with a single floor test proving placement in each:

1. **Part 9, two constitutional chokepoints (D-01, LOCKED).** Part 9 today names only
   `lib/core/navigation.cjs`, the read/navigate chokepoint Phase 109 shipped. The WRITE chokepoint
   `lib/core/node-insert.cjs` shipped in Phase 276's TOOLHON work with fail-closed `epistemic_type`
   validation and is still actively accreting callers, and the Canon has never named it. The
   navigator locked a DEEPER doctrinal split, not a light citation: navigate and write are
   DIFFERENT constitutional properties and must read as two named things.

2. **Part 4, typed-edge vocabulary reconciliation.** 340-RESEARCH.md's largest single finding: the
   live frozen `ALLOWED_EDGE_TYPES` set carries 44 members and Part 4's closed-vocabulary prose
   names only 29. The drift accumulated across multiple phases that each shipped one additive edge
   type without a canon-amendment wave - the exact Part-6 self-CONTRADICTS pattern entries 18, 21
   and 23 were each written to close for earlier such gaps.

3. **Appendix B, ICM code citations (D-02, LOCKED).** Appendix B is a five-row concept-to-Part
   table with ZERO file citations, unusual for a Canon that cites concrete files everywhere else.
   Phase 275 shipped the L1 `STATEMENT` field, the L2 per-section `CONTEXT.md` writer, and the L3
   `references/` factory directory; the table should point at them.

This is D-03's widened scope in practice: findings 2 and 3 came out of the part-by-part sweep the
navigator asked for, not from the original four-front floor.

Output: Part 9, Part 4 and Appendix B body edits; Appendix D entry 39; version 1.25 -> 1.26; the
`docs/CANON-PHASE-MAP.md` v1.26 row; a multi-Part placement-proof floor test; the two
carried-forward entry floor tests moved to the 1.26 anchor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-CONTEXT.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-RESEARCH.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-PATTERNS.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-01-SUMMARY.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: BLOCKING navigator gate - draft the exact prose for all three Parts, halt for APPROVE before any canon byte</name>

  <files>.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-03-DRAFT-PROSE.md</files>

  <read_first>
    - `docs/MINDRIAN-CANON.md` Part 9 IN FULL (lines 326 to 375) - especially role 2 ("SQL (`room.db`) remembers and navigates"), the "### What this means architecturally" subsection, and the "### Implementing phase" subsection which currently carries the sole `lib/core/navigation.cjs` citation
    - `docs/MINDRIAN-CANON.md` Part 4 IN FULL (lines 209 to 221) - the single long paragraph under `### The typed-edge vocabulary (closed set)` that lists 29 types across five named categories, plus its existing "added by navigator-gated amendment (Appendix D entry 18 / 21 / 23)" clauses, which are the exact idiom this reconciliation must extend
    - `docs/MINDRIAN-CANON.md` Appendix B IN FULL (lines 709 to 719) - the five-row table
    - `docs/MINDRIAN-CANON.md` Appendix D entries 18, 21 and 23 - the prior edge-vocabulary amendment idiom (a type is named, then defined in one clause explaining what it expresses)
    - `docs/MINDRIAN-CANON.md` Appendix D entries 30 through 38 - the provenance-paragraph voice entry 39 must match
    - `lib/core/node-insert.cjs` header comment and the `insertNode` function through its `ALLOWED_EPISTEMIC_TYPES` validation - so the Part 9 prose describes what the file actually does, including its two documented coverage exclusions
    - `lib/core/navigation/edges.cjs` - the live `ALLOWED_EDGE_TYPES` set and the inline comments naming which phase added which type
    - `lib/core/room-skeleton-scaffold.cjs` and `lib/core/section-registry.cjs` - the L1 `STATEMENT` substitution, the L2 `CONTEXT.md` writer, the L3 `references/` factory directory
    - `.claude/includes/architecture.md` - the sibling ICM Layers 0-4 text; D-02 requires the new Appendix B citations to stay consistent with this file's framing
    - `.planning/phases/340-.../340-LIVE-VERIFICATION.md` - the Wave B section: today's real edge-type list, today's real `insertNode` call-site counts and scope, today's real line numbers for the three Phase 275 mechanisms. Use THESE numbers, not 340-RESEARCH.md's.
  </read_first>

  <action>
Write a DRAFT ONLY to `340-03-DRAFT-PROSE.md`. Write nothing to `docs/MINDRIAN-CANON.md` or
`docs/CANON-PHASE-MAP.md` in this task. Then present the draft and HALT for the navigator's verdict.

Draft four pieces of prose:

(1) **Part 9 two-chokepoint split (D-01).** Amend the `### What this means architecturally`
subsection, and/or add a short new subsection immediately after it, so the Canon names TWO
chokepoints as two distinct constitutional properties. Required anchor strings a floor test will
pin: `two constitutional chokepoints`; `lib/core/navigation.cjs` (the NAVIGATE/READ chokepoint,
already cited in the Implementing-phase subsection, which the new prose must also name in the
architectural subsection so both chokepoints appear side by side rather than one being a
footnote); `lib/core/node-insert.cjs` (the WRITE chokepoint, newly named); `epistemic_type` (the
required argument the write chokepoint validates against a closed enum BEFORE any other work);
`fail-closed` (the enforcement property); and `lib/core/navigation/memory-events.cjs` plus
`lib/core/rs-sqlite-mirror.cjs` (the two coverage exclusions the file's own header documents,
named honestly as exclusions BY DESIGN rather than silently omitted). The prose must state WHY
they are different constitutional properties: navigating asks "what matters around this focus
node" and returns a ranked explained neighborhood; writing asserts a new fact into the local mind
and therefore must declare what KIND of knowledge it is before it lands. Do NOT state a call-site
count as a frozen figure; if a count is used at all, frame it as enumerated from disk at run time
(Pitfall 2: a number in a prescriptive sentence must not be allowed to go stale the way Part 7's
did).

(2) **Part 4 edge-vocabulary reconciliation.** Extend the closed-vocabulary paragraph so every edge
type the live module ships is named in the Canon prose, using the existing category idiom and the
existing "added by navigator-gated amendment" sentence pattern. The candidate list from
340-RESEARCH.md, to be re-checked against `340-LIVE-VERIFICATION.md` and pruned of any type that
turned out to be cited already: `ATTRIBUTED_TO`, `AUTHORED_BY`, `COMPETES_WITH`, `CONCERNS`,
`DISCOVERED`, `ELEVATES_TO`, `MAPS_TO_SECTION`, `NOT_REMEMBERED_BECAUSE`, `REMEMBERED_AS`,
`SHARES_JOB`, `SOURCED_FROM`, `SUPPLIES_TO`, `UMBILICAL_TO`, `USES_COMPONENT`, `USES_FRAMEWORK`.
Each named type gets one short clause saying what it expresses, in the same voice as the existing
definitions of REFINES / ROOT_CAUSES / INSTANTIATES. Two further required properties: the anchor
string `lib/core/navigation/edges.cjs` must appear in the Part 4 slice, naming the module as the
authoritative source of the closed set; and the anchor string `enumerated from disk` (or the
navigator-approved equivalent) must appear, stating that MEMBERSHIP is authoritative in that module
and the Canon's prose is the ratified list, so a future additive type is a canon amendment and the
count is never a frozen literal a gate may hardcode. Do NOT write a total member count as a frozen
number. Record the reconciliation honestly: several of these types shipped in phases that added
them additively without a canon wave, and entry 39's provenance paragraph names that truthfully
rather than implying they were always ratified. Additive only: every one of the 29 already-named
types stays byte-identical, including the existing entry-18 / entry-21 / entry-23 clauses.

(3) **Appendix B ICM code citations (D-02).** Add citations to the Layer table, either as a new
"Implementing mechanism" column or as a short citation block directly beneath the table - the
navigator picks at this gate, so draft BOTH forms. Required anchor strings:
`lib/core/room-skeleton-scaffold.cjs`, `STATEMENT`, `CONTEXT.md`, `references/`, and
`lib/core/section-registry.cjs`. Map them as: L1 Routing/Identity to the `STATEMENT` field
substitution; L2 Contracts to the per-section `CONTEXT.md` writer; L3 Reference to the
`references/` factory directory registered as a core section. Keep the existing five
Layer-to-Part mappings byte-identical - this adds citations, it does not re-map layers. Do NOT
cite a line number as a frozen figure; cite the file and the named function or constant, since
line numbers drift and file-plus-symbol does not.

(4) **Appendix D entry 39**, opening exactly `39. **`, in the voice of entries 30 through 38: what
changed across all three Parts, why (the live-verified drift, traced to the phases that shipped
each mechanism - Phase 276 for the write chokepoint, the additive edge-type phases for Part 4,
Phase 275 for the ICM mechanisms), that the navigator APPROVED at a blocking checkpoint BEFORE any
canon byte was written with the version target v1.25 -> v1.26 confirmed, that entry 31's
self-binding clause is RELEASED for this amendment by the Part-10 navigator-authority override
(entry 20) recorded truthfully with no two-gauge reading taken or fabricated, that the amendment
RATIFIES already-shipped code rather than minting new mechanisms (so canon and code are consistent
the instant it lands, mirroring entry 35's framing), that it mints NO new reach and NO new node
type and opens NO Brain wire, and that the frozen Part 3 scalars are byte-identical
(`MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15 gate`). Note carefully: this entry DOES ratify edge types
into the closed Part 4 vocabulary, so unlike entries 34 through 37 it may NOT claim "mints no edge
type" - it must state truthfully that it RECONCILES already-shipped edge types into the canon prose
that should have carried them. The lockstep manifest must list every file this wave touches.

House rules: hyphens only, never an em-dash; no invented citation; every figure traceable to
`340-LIVE-VERIFICATION.md`.

After writing the draft, present it and HALT. Do not proceed to Task 2 on silence or inference.
Record the navigator's literal reply in the plan SUMMARY.
  </action>

  <what-built>
A draft-only prose file, `340-03-DRAFT-PROSE.md`, containing four pieces of text exactly as they
would land: the Part 9 two-chokepoint prose, the Part 4 edge-vocabulary reconciliation, the
Appendix B citations (both candidate forms), and Appendix D entry 39. Zero constitutional bytes
have moved.
  </what-built>

  <how-to-verify>
1. Read `.planning/phases/340-.../340-03-DRAFT-PROSE.md` in full.
2. Part 9: confirm the two chokepoints read as TWO NAMED THINGS with different constitutional
   properties, which is what D-01 locked, and not as one citation folded into existing
   `navigation.cjs` prose. If it reads as a footnote, REJECT.
3. Part 4: confirm the final list of edge types to ratify (the draft states which of the 15
   candidates survived re-verification and which were dropped as already-cited), and confirm each
   one-clause definition is accurate rather than plausible.
4. Appendix B: pick the citation FORM - a new table column, or a citation block beneath the table.
   Both are drafted; name which you want.
5. Confirm the version target: canon v1.25 -> v1.26.
6. Confirm the anchor strings that a floor test will pin forever, or name replacements.
7. Confirm the entry-39 honesty clause: it ratifies shipped code and explicitly does NOT claim to
   mint nothing, because it does ratify edge types into a closed constitutional set.

Reply with one of:
  - `APPROVE` - the exact drafted prose lands as-is.
  - `REJECT: <reason>` - nothing lands; the draft is revised and this gate re-runs.
  - `APPROVE WITH EDITS: <the edits>` - the executor applies the named edits, updates the
    anchor-string list to match the edited text, and re-presents before writing.

This checkpoint is NOT auto-approvable, regardless of `workflow.auto_advance`. Every prior Appendix
D entry required a navigator APPROVE on the literal text before any canon byte landed;
340-RESEARCH.md Pitfall 4 names skipping it as this phase's signature failure mode.
  </how-to-verify>

  <resume-signal>Type "APPROVE", "REJECT: reason", or "APPROVE WITH EDITS: edits"</resume-signal>

  <verify>
    <automated>test -f .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-03-DRAFT-PROSE.md &amp;&amp; grep -q "two constitutional chokepoints" .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-03-DRAFT-PROSE.md &amp;&amp; grep -q "lib/core/node-insert.cjs" .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-03-DRAFT-PROSE.md &amp;&amp; grep -q "lib/core/room-skeleton-scaffold.cjs" .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-03-DRAFT-PROSE.md &amp;&amp; test "$(git diff --name-only docs/MINDRIAN-CANON.md | wc -l)" -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `340-03-DRAFT-PROSE.md` exists with four clearly labelled sections: Part 9, Part 4, Appendix B, Appendix D entry 39.
    - The draft contains the exact strings `two constitutional chokepoints`, `lib/core/node-insert.cjs`, `lib/core/navigation.cjs`, `epistemic_type`, `fail-closed`, `lib/core/navigation/edges.cjs`, `lib/core/room-skeleton-scaffold.cjs`, `lib/core/section-registry.cjs`.
    - The draft explicitly lists which of the 15 candidate edge types survived re-verification against `340-LIVE-VERIFICATION.md` and which were dropped as already-cited.
    - The draft's entry-39 text opens with the exact prefix `39. **` and contains the exact substring `MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15 gate`.
    - The draft's entry-39 text does NOT contain the string `mints NO new edge type` (that claim would be false for this entry) and instead states it reconciles already-shipped edge types.
    - The draft contains no frozen numeric total of edge types and no frozen `insertNode` call-site count inside a prescriptive sentence.
    - `grep -c "$(printf '\xe2\x80\x94')" <draft>` returns 0.
    - `git diff --name-only docs/MINDRIAN-CANON.md docs/CANON-PHASE-MAP.md` returns empty at halt time.
    - Execution HALTS and does not proceed to Task 2 until the navigator's literal reply is recorded.
  </acceptance_criteria>

  <done>The navigator has replied APPROVE (or APPROVE WITH EDITS, applied and re-presented), the reply is recorded verbatim with its date in the plan SUMMARY, and the Canon is still byte-identical to its pre-task state.</done>
</task>

<task type="auto">
  <name>Task 2: Land the atomic lockstep - Part 9, Part 4, Appendix B, entry 39, version 1.26, map row</name>

  <files>docs/MINDRIAN-CANON.md, docs/CANON-PHASE-MAP.md, tests/test-canon-entry-31-two-gauge-floor.cjs, tests/test-canon-entry-36-shape-declaration-floor.cjs</files>

  <read_first>
    - `.planning/phases/340-.../340-03-DRAFT-PROSE.md` - the APPROVED text. Write exactly this, byte-for-byte. Do not improve, re-word or expand it while writing.
    - `docs/MINDRIAN-CANON.md` line 3 (`Version: 1.25`) and the footer line (`_Mindrian Canon v1.25 - MindrianOS Plugin_`) - the two version tokens
    - `docs/CANON-PHASE-MAP.md` line 6 and the v1.25 row plan 340-02 appended - the row format your v1.26 row must match
    - `tests/test-canon-entry-31-two-gauge-floor.cjs` and `tests/test-canon-entry-36-shape-declaration-floor.cjs` - their 1.25 version anchors (moved there by plan 340-02) which now move to 1.26
  </read_first>

  <action>
Land every edit below in ONE commit so CI never goes RED mid-wave.

1. Canon Part 9: apply the APPROVED two-chokepoint prose into the `### What this means
   architecturally` subsection and/or the approved new subsection position. Additive: the five-role
   block, the truth-states subsection, the audit-node carve-out and the `### Implementing phase`
   subsection all keep their existing sentences byte-identical.
2. Canon Part 4: apply the APPROVED reconciliation into the paragraph under `### The typed-edge
   vocabulary (closed set)`. Every one of the 29 already-named types and every existing entry-18 /
   21 / 23 definition clause stays byte-identical; the new types and the
   `lib/core/navigation/edges.cjs` source-of-truth sentence are additions.
3. Canon Appendix B: apply the APPROVED citation form (new column, or citation block beneath the
   table - whichever the navigator picked). The five existing Layer-to-Part mappings stay
   byte-identical.
4. Canon Appendix D: append the APPROVED entry 39 immediately after entry 38 and before the line
   beginning `Conversation transcript reference: session 2026-04-20`. Entries 1 through 38 are
   byte-preserved: do not touch one character of them.
5. Canon version: header `Version: 1.25` becomes `Version: 1.26`; footer
   `_Mindrian Canon v1.25 - MindrianOS Plugin_` becomes `_Mindrian Canon v1.26 - MindrianOS Plugin_`.
6. `docs/CANON-PHASE-MAP.md`: reference line 6 `(v1.25)` becomes `(v1.26)`; append a
   `| v1.26 | TBD | <execution date> | ...` version-history row matching the v1.25 row's shape. The
   row names: the phase SLUG `340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d`
   (never the phase number alone), entry 39, requirement ids CANON-02 / CANON-03 / CANON-04,
   `NAVIGATOR-GATED` with the approval date, all three amended Parts, the honest statement that the
   entry ratifies already-shipped edge types rather than minting none, the
   frozen-scalars-byte-identical clause, and a `Fences:` clause naming
   `tests/test-canon-entry-39-graph-substrate-floor.cjs`,
   `tests/test-canon-frozen-scalars-floor.cjs`, the two version-anchor-bumped tests,
   `tests/test-195-canon-7-kind-floor.cjs` (which guards the Part 9 seven-kind complement this wave
   edits around), and `bash tests/run-all-340.sh exit 0`.
7. `tests/test-canon-entry-31-two-gauge-floor.cjs` and
   `tests/test-canon-entry-36-shape-declaration-floor.cjs`: move each version anchor 1.25 -> 1.26 in
   every place it appears, including entry-36's CANON-PHASE-MAP `/v1\.25/` token assertion. Both
   files' prior-entry loops (`n = 1..30` and `n = 1..35`) and their frozen-scalar assertions are
   UNWEAKENED - change nothing else.

Hard prohibitions for this task: do not change any frozen scalar VALUE; do not remove or reword any
Appendix D entry 1-38; do not edit `CLAUDE.md` (wave C's scope); do not edit
`lib/core/node-insert.cjs`, `lib/core/navigation.cjs`, `lib/core/navigation/edges.cjs`,
`lib/core/room-skeleton-scaffold.cjs` or `lib/core/section-registry.cjs` - this phase CITES those
files, it does not modify them.
  </action>

  <verify>
    <automated>node tests/test-canon-frozen-scalars-floor.cjs &amp;&amp; node tests/test-canon-entry-31-two-gauge-floor.cjs &amp;&amp; node tests/test-canon-entry-36-shape-declaration-floor.cjs &amp;&amp; node tests/test-195-canon-7-kind-floor.cjs &amp;&amp; node tests/test-canon-entry-38-sourced-claims-floor.cjs &amp;&amp; grep -c "^Version: 1.26$" docs/MINDRIAN-CANON.md | grep -qx 1 &amp;&amp; grep -q "lib/core/node-insert.cjs" docs/MINDRIAN-CANON.md &amp;&amp; grep -q "lib/core/room-skeleton-scaffold.cjs" docs/MINDRIAN-CANON.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^Version: 1.26$" docs/MINDRIAN-CANON.md` equals 1 and `grep -c "^Version: 1.25$" docs/MINDRIAN-CANON.md` equals 0.
    - `grep -c "_Mindrian Canon v1.26 - MindrianOS Plugin_" docs/MINDRIAN-CANON.md` equals 1.
    - `grep -c "two constitutional chokepoints" docs/MINDRIAN-CANON.md` is at least 1 and `grep -c "lib/core/node-insert.cjs" docs/MINDRIAN-CANON.md` is at least 1.
    - `grep -c "lib/core/navigation/edges.cjs" docs/MINDRIAN-CANON.md` is at least 1.
    - `grep -c "lib/core/room-skeleton-scaffold.cjs" docs/MINDRIAN-CANON.md` is at least 1 and `grep -c "lib/core/section-registry.cjs" docs/MINDRIAN-CANON.md` is at least 1.
    - For every edge type on the APPROVED ratification list, `grep -c "<TYPE>" docs/MINDRIAN-CANON.md` is at least 1.
    - `grep -c "^39\. \*\*" docs/MINDRIAN-CANON.md` equals 1, and for every n in 1..38, `grep -c "^${n}\. \*\*" docs/MINDRIAN-CANON.md` is at least 1.
    - Deletions are bounded and accounted for: `git diff docs/MINDRIAN-CANON.md | grep "^-" | grep -v "^---" | wc -l` is 12 or fewer, and every deleted line is listed in the SUMMARY with its replacement.
    - `node tests/test-canon-frozen-scalars-floor.cjs` exits 0.
    - `node tests/test-canon-entry-38-sourced-claims-floor.cjs` exits 0 - wave A's guarantees survived wave B untouched.
    - `node tests/test-195-canon-7-kind-floor.cjs` exits 0 - the Part 9 seven-kind memory complement is unharmed by the Part 9 edit.
    - `grep -c "v1.26" docs/CANON-PHASE-MAP.md` is at least 2 and the new row contains the phase slug.
    - `grep -c "$(printf '\xe2\x80\x94')" docs/MINDRIAN-CANON.md docs/CANON-PHASE-MAP.md` returns 0 for both files.
    - `git status --porcelain lib/` returns empty: no cited implementation file was modified.
    - All edits land in ONE git commit.
  </acceptance_criteria>

  <done>The Canon reads v1.26, Part 9 names two chokepoints, Part 4's closed vocabulary matches what ships, Appendix B carries real file citations, entry 39 records the provenance, entries 1-38 are untouched, and every carried-forward floor test is green at the new anchor.</done>
</task>

<task type="auto">
  <name>Task 3: Write the entry-39 multi-Part placement-proof floor test and flip its aggregator leg</name>

  <files>tests/test-canon-entry-39-graph-substrate-floor.cjs</files>

  <read_first>
    - `tests/test-canon-entry-31-two-gauge-floor.cjs` IN FULL - the multi-Part placement-proof precedent: one doctrine, several `## Part N` slices, each asserted independently so a citation that landed only in Appendix D fails the test
    - `tests/test-canon-entry-36-shape-declaration-floor.cjs` IN FULL - the `sliceByHeader` / `collapse` / `sliceEntry` helpers and the version/map assertions to copy verbatim
    - `tests/test-canon-entry-38-sourced-claims-floor.cjs` - the sibling written one wave earlier in this same phase; match its doc-comment shape and naming so the family reads consistently
    - `.planning/phases/340-.../340-03-DRAFT-PROSE.md` - the APPROVED anchor strings and the FINAL edge-type ratification list. Pin the approved text, not the originally drafted text.
    - `.planning/phases/340-.../340-PATTERNS.md` sections "THE PRIMARY NEW-FILE PATTERN" and "The section-slicing + collapse helper pair"
  </read_first>

  <action>
Create `tests/test-canon-entry-39-graph-substrate-floor.cjs`. Copy the imports, the `ok` helper,
`sliceByHeader`, `collapse`, `sliceEntry` and the output banner verbatim from
`tests/test-canon-entry-36-shape-declaration-floor.cjs`.

Doc-comment header, house style: phase slug and plan (`340-03`), entry number (39), requirement ids
(CANON-02, CANON-03, CANON-04), the navigator-approval date from Task 1, a numbered list of what
each Test asserts and why, and the two mandatory house statements "NEVER asserts a raw count of
Appendix D entries" and "House rule: hyphens only, no em-dashes. Canon Part 8: zero Brain /
network." Add one more statement specific to this test: "NEVER asserts a total count of edge types -
membership is authoritative in lib/core/navigation/edges.cjs and an additive type must not
false-fail this test."

Slices to build: `part4 = sliceByHeader(canon, /^## Part 4 - /)`,
`part9 = sliceByHeader(canon, /^## Part 9 - /)`,
`appendixB = sliceByHeader(canon, /^## Appendix B/)`,
`appendixD = sliceByHeader(canon, /^## Appendix D/)`.

Assertions, in this order:

Test 1 - Part 9 placement proof (CANON-02). The `part9` slice contains
`two constitutional chokepoints`, `lib/core/navigation.cjs`, `lib/core/node-insert.cjs`,
`epistemic_type` and `fail-closed`. All five must be in the PART 9 SLICE, not merely in the file -
this is the assertion that makes D-01's "two named things, not a footnote" structurally required.

Test 2 - Part 9 non-regression. The `part9` slice still contains the five-role invariant lines
`**SQL remembers and navigates.**` and `**The human confirms truth.**` byte-identical, and still
contains `readSextuple` and the seven-kind complement language that entry 35 ratified, proving this
wave extended Part 9 rather than rewriting it.

Test 3 - Part 4 placement proof (CANON-03). The `part4` slice contains
`lib/core/navigation/edges.cjs`, the approved non-frozen framing anchor (`enumerated from disk` or
its approved replacement), and EACH edge type on the APPROVED ratification list, asserted
individually by name so a partial landing fails loudly. Do NOT assert a total count.

Test 4 - Part 4 non-regression. The `part4` slice still contains a representative sample of the
pre-existing 29 types asserted individually - at minimum `INFORMS`, `CONTRADICTS`, `REFINES`,
`ROOT_CAUSES`, `INSTANTIATES`, `NESTED_WITHIN`, `DECOMPOSED_INTO` - plus the existing amendment
provenance clauses `Appendix D entry 18`, `Appendix D entry 21` and `Appendix D entry 23`.

Test 5 - Appendix B placement proof (CANON-04). The `appendixB` slice contains
`lib/core/room-skeleton-scaffold.cjs`, `STATEMENT`, `CONTEXT.md`, `references/` and
`lib/core/section-registry.cjs`, AND still contains all five original Layer-to-Part mappings
(assert the substrings `Layer 0 - Identity`, `Layer 1 - Routing`, `Layer 2 - Contracts`,
`Layer 3 - Reference`, `Layer 4 - Artifacts` individually).

Test 6 - entry 39 body isolation. Isolate entry 39 via `sliceEntry(collapse(appendixD), 39)` and
assert that slice contains `CANON-02`, `CANON-03`, `CANON-04`, the navigator-approval language, and
the byte-identical frozen-scalar restatement `MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15 gate`. Also
assert the honesty clause: the entry-39 slice must NOT contain the false phrase
`mints NO new edge type`.

Test 7 - prior entries preserved. Loop `n = 1..38` over the `appendixD` slice ONLY and assert
`^n\.\s` is present per-number. Never assert ordering, never assert a raw entry count.

Test 8 - frozen scalars byte-present: `/MAX_K=3/`, `/DIAL_REACH_K=6/`, `/0\.70\/0\.15/`.

Test 9 - version bump: `/^Version: 1\.26$/m` and `/_Mindrian Canon v1\.26 - MindrianOS Plugin_/`.

Test 10 - map row, light: `docs/CANON-PHASE-MAP.md` carries a `v1.26` token, an `entry 39`
reference, and the phase slug.

Assertion style, hard constraints: byte-presence and byte-absence only via `.includes()` or
`.test()`. Never `.length`, never `.size`, never a count of Appendix D entries or edge types.

`tests/run-all-340.sh` already carries the `run_if` leg naming this exact filename; creating the
file flips it from SKIPPED to PASSED. Confirm that rather than assuming it.
  </action>

  <verify>
    <automated>node tests/test-canon-entry-39-graph-substrate-floor.cjs &amp;&amp; bash tests/run-all-340.sh &amp;&amp; test "$(bash tests/run-all-340.sh 2>/dev/null | grep -c 'SKIPPED (file not present')" -eq 1 &amp;&amp; test "$(bash tests/run-all-340.sh 2>/dev/null | grep -c ': PASSED')" -eq 6</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-canon-entry-39-graph-substrate-floor.cjs` exits 0 and prints its `>>> ...: PASSED` banner.
    - `bash tests/run-all-340.sh` exits 0 with exactly 6 `: PASSED` lines, 1 `SKIPPED (file not present` line, and 0 `: FAILED` lines.
    - `grep -c "sliceByHeader" tests/test-canon-entry-39-graph-substrate-floor.cjs` is at least 5 (four slices plus the helper definition), proving placement proof is per-slice and not whole-file.
    - `grep -c "for (let n = 1; n <= 38" tests/test-canon-entry-39-graph-substrate-floor.cjs` equals 1.
    - `grep -v '^ \*' tests/test-canon-entry-39-graph-substrate-floor.cjs | grep -v '^//' | grep -c "\.size\|ALLOWED_EDGE_TYPES.length"` equals 0 - no count-based assertion that an additive edge type could false-fail.
    - Three deliberate negative proofs, each run then reverted, with both exit codes recorded in the SUMMARY: (a) temporarily delete `lib/core/node-insert.cjs` from the Part 9 slice text and confirm non-zero exit; (b) temporarily delete one ratified edge type name from the Part 4 paragraph and confirm non-zero exit; (c) temporarily delete `lib/core/room-skeleton-scaffold.cjs` from Appendix B and confirm non-zero exit. Restore after each and confirm exit 0.
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-canon-entry-39-graph-substrate-floor.cjs` returns 0.
  </acceptance_criteria>

  <done>Wave B's three-Part amendment is guarded by one floor test that provably goes red when any one of the three Parts loses its citation, and the phase aggregator carries it as a real green leg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft prose -> constitutional text | The only crossing point is the navigator's APPROVE at Task 1; Task 2 may not run before the verdict is recorded |
| Canon prose -> shipped code it cites | The Canon cites `lib/core/*.cjs` but must never modify it. This wave is read-only against `lib/`, enforced by an acceptance criterion asserting `git status --porcelain lib/` is empty |
| closed constitutional set -> per-phase edit | Part 4's edge vocabulary and Part 9's chokepoint doctrine are closed sets; the Canon's own text says a change to a closed set is an amendment, never a per-phase edit. This wave IS that amendment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-340-09 | Elevation of privilege | `docs/MINDRIAN-CANON.md` closed sets | mitigate | Task 1 is a `gate="blocking-human"` checkpoint; its acceptance criteria assert `git diff --name-only` on the constitutional files is empty at halt time |
| T-340-10 | Tampering | Appendix D entries 1-38 | mitigate | Task 2 asserts every prior entry marker still present and caps deleted lines; Task 3's Test 7 pins entries 1-38 permanently |
| T-340-11 | Tampering | Part 3 frozen scalars | mitigate | `tests/test-canon-frozen-scalars-floor.cjs` runs in Task 2's verify and as a required-green aggregator leg; entry 39 restates the scalars byte-identical |
| T-340-12 | Repudiation | edge-type ratification honesty | mitigate | Entry 39 is explicitly forbidden from claiming "mints NO new edge type"; Task 3's Test 6 asserts that false phrase is ABSENT from the entry body |
| T-340-13 | Tampering | cited implementation files | mitigate | Acceptance criterion asserts `git status --porcelain lib/` is empty; the phase cites `node-insert.cjs` / `edges.cjs` / `room-skeleton-scaffold.cjs` and never edits them |
| T-340-14 | Spoofing | floor-test coverage | mitigate | Task 3 requires three separate red-proofs, one per amended Part, so a test that passes vacuously for any one Part cannot ship |
| T-340-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable: zero packages installed in this phase. The new floor test uses only `node:assert`, `node:fs`, `node:path` |
</threat_model>

<verification>
- `bash tests/run-all-340.sh` exits 0: 6 PASS, 0 FAIL, 1 SKIP.
- The Canon reads `Version: 1.26` in header and footer.
- Part 9 slice, Part 4 slice and Appendix B slice each independently carry their required citations.
- `node tests/test-canon-entry-38-sourced-claims-floor.cjs` still exits 0 (wave A untouched).
- `git status --porcelain lib/` is empty.
- The navigator's literal APPROVE reply and its date are recorded in `340-03-SUMMARY.md`.
</verification>

<success_criteria>
- The Canon's account of its own local substrate matches what ships: two named chokepoints, a complete typed-edge vocabulary, and an ICM table that cites real files.
- D-01 landed as the deeper doctrinal split the navigator locked, not as a citation folded into existing prose.
- D-02 landed as real code citations consistent with `.claude/includes/architecture.md`'s framing.
- No frozen scalar moved, no prior Appendix D entry moved, no cited implementation file was edited, and the whole wave landed as one commit.
</success_criteria>

## Artifacts this phase produces

Newly created by THIS plan (exclude from drift and source-grounding checks):

| Artifact | Kind | Created by |
|----------|------|-----------|
| `tests/test-canon-entry-39-graph-substrate-floor.cjs` | new floor test | 340-03 Task 3 |
| `.planning/phases/340-.../340-03-DRAFT-PROSE.md` | new phase record (draft prose, gate input) | 340-03 Task 1 |
| Canon Part 9 two-chokepoint prose | new constitutional text | 340-03 Task 2 |
| Canon Part 4 ratified edge-type clauses (the approved subset of `ATTRIBUTED_TO`, `AUTHORED_BY`, `COMPETES_WITH`, `CONCERNS`, `DISCOVERED`, `ELEVATES_TO`, `MAPS_TO_SECTION`, `NOT_REMEMBERED_BECAUSE`, `REMEMBERED_AS`, `SHARES_JOB`, `SOURCED_FROM`, `SUPPLIES_TO`, `UMBILICAL_TO`, `USES_COMPONENT`, `USES_FRAMEWORK`) | new constitutional text | 340-03 Task 2 |
| Canon Appendix B ICM implementation citations | new constitutional text | 340-03 Task 2 |
| Canon Appendix D entry 39 | new constitutional text | 340-03 Task 2 |
| Canon version token `1.26` (header, footer, map reference line, map version-history row) | new version token | 340-03 Task 2 |
| Requirement IDs `CANON-02`, `CANON-03`, `CANON-04` | new requirement ids | minted at plan time; registered in `.planning/REQUIREMENTS.md` by 340-05 |

Pre-existing and only MODIFIED (version anchors moved 1.25 -> 1.26, all other assertions
unweakened): `tests/test-canon-entry-31-two-gauge-floor.cjs`,
`tests/test-canon-entry-36-shape-declaration-floor.cjs`, `docs/CANON-PHASE-MAP.md`.

Pre-existing and only CITED, never modified: `lib/core/navigation.cjs`,
`lib/core/node-insert.cjs`, `lib/core/navigation/edges.cjs`, `lib/core/room-skeleton-scaffold.cjs`,
`lib/core/section-registry.cjs`, `lib/core/navigation/memory-events.cjs`,
`lib/core/rs-sqlite-mirror.cjs`.

<output>
Create `.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-03-SUMMARY.md` when done. Record the navigator's literal checkpoint reply, its date, the final edge-type ratification list, and the three red-proof exit codes.
</output>
