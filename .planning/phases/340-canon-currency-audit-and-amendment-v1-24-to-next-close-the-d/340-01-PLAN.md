---
phase: 340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/run-all-340.sh
  - .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-LIVE-VERIFICATION.md
autonomous: true
requirements: [CANON-10]
canon_parts: [6]

must_haves:
  truths:
    - "A single command (bash tests/run-all-340.sh) runs every canon floor test this phase carries and exits 0 today, with the three not-yet-written entry floor tests SKIPPING cleanly rather than FAILING."
    - "Every number and file citation the three amendment waves will write into the Canon has a dated, live-command-produced record in the phase directory, produced today, not carried from 340-RESEARCH.md."
    - "The two canon tests that are ALREADY red on main (test-canon-crossref-completeness.cjs, test-canon-part-9-ratification.cjs) are named in writing with their root cause, so no later wave mistakes a pre-existing red for a regression it caused."
  artifacts:
    - path: "tests/run-all-340.sh"
      provides: "Phase 340 PASS/FAIL/SKIP aggregator; the per-wave merge gate"
      contains: "run_if"
    - path: ".planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-LIVE-VERIFICATION.md"
      provides: "Dated live-command record of every figure and citation waves A/B/C consume"
      contains: "ALLOWED_EDGE_TYPES"
  key_links:
    - from: "tests/run-all-340.sh"
      to: "tests/test-canon-frozen-scalars-floor.cjs"
      via: "run leg (required green every wave)"
      pattern: "test-canon-frozen-scalars-floor"
    - from: "tests/run-all-340.sh"
      to: "tests/test-canon-entry-38-sourced-claims-floor.cjs"
      via: "run_if leg (SKIPs until wave A lands)"
      pattern: "test-canon-entry-38-sourced-claims-floor"
---

<objective>
Build the Wave 0 test scaffold and refresh every live citation this phase will write into the
Canon, BEFORE any amendment wave drafts prose.

Purpose: 340-RESEARCH.md states its own findings go stale fast ("re-verify surface counts,
call-site counts, and file existence at execution time regardless") and this repo ships
canon-adjacent code multiple times per week. A Canon amendment that cites a number nobody
re-checked today is exactly the failure class this whole phase exists to close. This plan produces
the checked numbers and the gate that keeps every later wave honest.

Output: `tests/run-all-340.sh` (the phase aggregator) and `340-LIVE-VERIFICATION.md` (the dated
figures-and-citations record waves A, B and C read instead of trusting 340-RESEARCH.md's numbers).

This plan writes ZERO bytes to `docs/MINDRIAN-CANON.md`, `docs/CANON-PHASE-MAP.md`, `CLAUDE.md`,
or `agents/larry-extended.md`. It is fully autonomous by construction. It serves D-03's widened
part-by-part scope by re-grounding every figure the sweep produced.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-RESEARCH.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-PATTERNS.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the live re-verification battery and file 340-LIVE-VERIFICATION.md</name>

  <files>.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-LIVE-VERIFICATION.md</files>

  <read_first>
    - `.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-RESEARCH.md` (sections "Part-by-Part Currency Findings", "Code Examples", "Open Questions", "Assumptions Log") - these are the claims being re-checked; read them so the record can state CONFIRMED / MOVED / CONTRADICTED per claim rather than restating them
    - `.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-VALIDATION.md` (the Wave 0 Requirements section)
  </read_first>

  <action>
Run every command below from `/home/jsagi/dev/MindrianOS-Plugin` (WORKSPACE GUARD: confirm with
`pwd` first; never `~/.claude/plugins/mindrian-os/`). Record each command's LITERAL output in the
record file next to the 340-RESEARCH.md claim it re-checks, tagged CONFIRMED (same as research),
MOVED (different value, record both), or CONTRADICTED (research claim was wrong).

Battery, one section per amendment wave that consumes it:

WAVE B inputs (Part 4, Part 9, Appendix B):
1. `node -e "const e=require('./lib/core/navigation/edges.cjs'); const a=[...e.ALLOWED_EDGE_TYPES].sort(); console.log(a.length); console.log(a.join('\n'))"` - the full sorted `ALLOWED_EDGE_TYPES` list and its length. Research recorded 44 members with 15 absent from Part 4's prose.
2. For each of the 15 types research named absent (`ATTRIBUTED_TO`, `AUTHORED_BY`, `COMPETES_WITH`, `CONCERNS`, `DISCOVERED`, `ELEVATES_TO`, `MAPS_TO_SECTION`, `NOT_REMEMBERED_BECAUSE`, `REMEMBERED_AS`, `SHARES_JOB`, `SOURCED_FROM`, `SUPPLIES_TO`, `UMBILICAL_TO`, `USES_COMPONENT`, `USES_FRAMEWORK`), run `grep -c "<TYPE>" docs/MINDRIAN-CANON.md` and record the count. Any type whose count is already non-zero is NOT a gap and must be dropped from wave B's list. Also diff the live 44-member list against Part 4's prose to catch any type added since research ran.
3. `grep -rn "insertNode(" lib/ | wc -l` and `grep -rl "insertNode(" lib/ | wc -l` - the live call-site and file counts for the write chokepoint. Research recorded 80 sites / 26 files scanning `lib/core/` only; re-run over all of `lib/` AND over `lib/core/` and record BOTH scopes explicitly so the Canon prose can state which scope its number describes.
4. `grep -n "ALLOWED_EPISTEMIC_TYPES" lib/core/node-insert.cjs` then read that array - record the exact member list and its length (research recorded 10 members).
5. `grep -n "memory-events.cjs\|rs-sqlite-mirror.cjs" lib/core/node-insert.cjs` - confirm the two named coverage exclusions are still documented in the file's own header.
6. `grep -n "STATEMENT" lib/core/room-skeleton-scaffold.cjs`, `grep -n "CONTEXT.md" lib/core/room-skeleton-scaffold.cjs`, `grep -n "references" lib/core/section-registry.cjs` - record the CURRENT line numbers for the L1 `STATEMENT` token substitution, the L2 per-section `CONTEXT.md` writer, and the L3 `references/` factory directory. Research cited lines 578-579, 316-356 and 385-399; line numbers drift, so the record must carry today's.

WAVE A inputs (Part 12 Sourced Claims):
7. `grep -rn "SOURCED_FROM" lib/ scripts/` - this closes 340-RESEARCH.md Open Question 2. Record whether `SOURCED_FROM` has a REAL runtime writer (the planner's pre-check found `lib/core/navigation/reasoning-write.cjs` writing `edge_type: 'SOURCED_FROM'`, consumed by `lib/mcp/tools/gate.cjs`'s approve branch and `lib/mcp/tools/views.cjs`'s `artifact_file`). If the writer is confirmed, wave A may cite it; if it is NOT confirmed, wave A must NOT cite it and the record must say so.
8. `grep -c -i "sourced\|hedge\|fabricat" docs/MINDRIAN-CANON.md` and `grep -c -i "sourced\|hedge\|fabricat" agents/larry-extended.md` - confirm the doctrine gap is still open in both files.

WAVE C inputs (Appendix C, Part 2, Part 7, Part 11, CLAUDE.md):
9. `grep -n "BRAIN_URL\|THEO_ORIGINS\|getBrainUrl" lib/core/brain-client.cjs` - the live default Brain origin and its single resolver.
10. `grep -n "pws-brain-mcp.onrender.com" docs/MINDRIAN-CANON.md` and `grep -c -i "theo" docs/MINDRIAN-CANON.md`.
11. `grep -n "25 methodology" docs/MINDRIAN-CANON.md CLAUDE.md` and `grep -n "Pinecone" docs/MINDRIAN-CANON.md` - record every line number and classify each hit PRESCRIPTIVE (an instruction whose correctness depends on the number) or DESCRIPTIVE/HISTORICAL (an illustrative snapshot, or an Appendix D historical entry that must NOT be touched). Appendix D entries 13 and 16 name Pinecone historically and are OFF LIMITS.
12. The four-glob surface count the schema itself defines (`data/hitl-shape-declaration-schema.json`, `surface_count_principle`): `ls commands/*.md | wc -l`, `ls agents/*.md | wc -l`, `ls pipelines/*/CHAIN.md | wc -l`, `ls skills/*/SKILL.md | wc -l`.
13. The declaring-versus-exempt split per class. For each of the four globs, count files matching `^hitl_shape:|^hitl_stages:` (declaring) and files matching `^\s*excluded:\s*true` (CIRS-excluded), and record total/declaring/excluded per class. Also run `node scripts/check-shape-declaration.cjs --check` and record its violation count and exit code (advisory since Phase 210, so a non-zero violation count with exit 0 is expected, not a failure).
14. `ls .claude/skills/` - CLAUDE.md's Project Skills table names `.claude/skills/docu-optimizer/SKILL.md`; the planner's pre-check found that path DOES NOT EXIST. Confirm or refute, and run `find . -name "SKILL.md" -path "*docu*"` to confirm it is nowhere else in the tree. This closes 340-RESEARCH.md Open Question 3 and is wave C's input.

PRE-EXISTING RED baseline (so no later wave misreads it):
15. Run each of `node tests/test-canon-frozen-scalars-floor.cjs`, `node tests/test-canon-entry-31-two-gauge-floor.cjs`, `node tests/test-canon-entry-36-shape-declaration-floor.cjs`, `node tests/test-195-canon-7-kind-floor.cjs`, `node tests/test-canon-crossref-completeness.cjs`, `node tests/test-canon-part-9-ratification.cjs` and record PASS/FAIL plus, for each failure, the failing assertion text. The planner's pre-check found `test-canon-crossref-completeness.cjs` and `test-canon-part-9-ratification.cjs` ALREADY RED on `main` before this phase: both are Phase-108/109-era tests pinned to canon v1.4 and to a pre-Part-9-ratification document state. Record that root cause in writing. These two are OUT OF SCOPE for this phase (fixing them would rewrite historical test anchors, a separate concern) and MUST NOT be registered as required-green legs in the aggregator.

Write the record to `340-LIVE-VERIFICATION.md` with a `**Verified:** <YYYY-MM-DD>` line at the top,
one section per wave, and a closing "Deltas against 340-RESEARCH.md" table listing every MOVED or
CONTRADICTED claim. House rule: hyphens only, no em-dashes. Do not paraphrase a command's output;
record it literally.
  </action>

  <verify>
    <automated>test -f .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-LIVE-VERIFICATION.md &amp;&amp; grep -q "ALLOWED_EDGE_TYPES" .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-LIVE-VERIFICATION.md &amp;&amp; grep -q "SOURCED_FROM" .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-LIVE-VERIFICATION.md &amp;&amp; grep -q "docu-optimizer" .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-LIVE-VERIFICATION.md &amp;&amp; grep -q "test-canon-part-9-ratification" .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-LIVE-VERIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - `340-LIVE-VERIFICATION.md` exists in the phase directory and its first 5 lines carry a `**Verified:**` date equal to the execution date.
    - The record contains a section for each of Wave A, Wave B and Wave C inputs, and a "Deltas against 340-RESEARCH.md" table.
    - For each of the 15 candidate edge types, the record carries an explicit `grep -c "<TYPE>" docs/MINDRIAN-CANON.md` result; any type with a non-zero count is marked "DROP - already cited".
    - The record states, in one unambiguous sentence, whether `SOURCED_FROM` has a confirmed runtime writer and names the file plus symbol if yes.
    - The record states, in one unambiguous sentence, whether `.claude/skills/docu-optimizer/SKILL.md` exists.
    - The record carries the four-glob surface counts AND a per-class total/declaring/excluded split, each as a literal number.
    - The record names `tests/test-canon-crossref-completeness.cjs` and `tests/test-canon-part-9-ratification.cjs` as PRE-EXISTING RED with their root cause (version anchors pinned to canon v1.4 and a pre-Phase-109 document state) and marks them out of scope.
    - The record contains no U+2014 em-dash: `grep -c "$(printf '\xe2\x80\x94')" <record>` returns 0.
    - Zero bytes written to `docs/MINDRIAN-CANON.md`, `docs/CANON-PHASE-MAP.md`, `CLAUDE.md`, `agents/larry-extended.md`: `git status --porcelain` lists none of those four paths.
  </acceptance_criteria>

  <done>A dated, literal-output verification record exists that either confirms or corrects every figure and citation the three amendment waves will write into the Canon, and the two pre-existing red canon tests are documented as out of scope.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/run-all-340.sh, the phase aggregator</name>

  <files>tests/run-all-340.sh</files>

  <read_first>
    - `tests/run-all-190.sh` - the verbatim source of the `run` / `run_if` bash helpers, the `set -uo pipefail` + `ROOT` preamble, the PASS/FAIL/SKIP tally, the summary banner, and the final `[ "$FAIL" -eq 0 ]` exit. Copy these byte-for-byte; do not invent a new aggregator shape.
    - `.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-PATTERNS.md` (section "`tests/run-all-340.sh` (test/aggregator, batch)") - the registration pattern and why `run_if` (not `run`) is correct for a floor test that has not landed yet.
    - `.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-LIVE-VERIFICATION.md` (written by Task 1) - the pre-existing-red baseline, so the two red tests are correctly excluded.
  </read_first>

  <action>
Create `tests/run-all-340.sh`, executable (`chmod +x`), modeled byte-for-byte on
`tests/run-all-190.sh`'s helper block. Substitute the phase-specific legs.

Header doc-comment must state: the phase slug
`340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d`; that this phase lands three
navigator-gated Canon amendments as Appendix D entries 38, 39 and 40 (canon v1.24 to v1.25 to
v1.26 to v1.27); that the three entry floor tests are registered with `run_if` so they SKIP cleanly
until their wave lands; and that `tests/test-canon-crossref-completeness.cjs` and
`tests/test-canon-part-9-ratification.cjs` are DELIBERATELY NOT registered because they were already
RED on `main` before this phase (version anchors pinned to canon v1.4 and to a pre-Phase-109
document state) and fixing historical test anchors is out of this phase's scope. House rule: bash
only, no emoji, hyphens only, no em-dashes.

Legs, in this order.

`run_if` legs (SKIP until their wave lands), one per new floor test:
- label "CANON-01 entry-38 Sourced Claims FLOOR", file `tests/test-canon-entry-38-sourced-claims-floor.cjs`, command `node tests/test-canon-entry-38-sourced-claims-floor.cjs`
- label "CANON-02/03/04 entry-39 graph-substrate FLOOR", file `tests/test-canon-entry-39-graph-substrate-floor.cjs`, command `node tests/test-canon-entry-39-graph-substrate-floor.cjs`
- label "CANON-05/06/07/08 entry-40 corpus-figures FLOOR", file `tests/test-canon-entry-40-corpus-figures-floor.cjs`, command `node tests/test-canon-entry-40-corpus-figures-floor.cjs`

`run` legs (required GREEN on every wave, all four confirmed passing today):
- label "frozen-scalars regression (must stay GREEN every wave)", command `node tests/test-canon-frozen-scalars-floor.cjs`
- label "entry-31 two-gauge regression (version anchor moves each wave)", command `node tests/test-canon-entry-31-two-gauge-floor.cjs`
- label "entry-36 shape-declaration regression (version anchor moves each wave)", command `node tests/test-canon-entry-36-shape-declaration-floor.cjs`
- label "195 seven-kind regression (Part 9 memory complement, guards wave B's Part 9 edit)", command `node tests/test-195-canon-7-kind-floor.cjs`

Close with the same `========================================` summary banner as run-all-190.sh,
reading `Summary (340 verification)`, and the same `[ "$FAIL" -eq 0 ]` final exit expression.

Do NOT add a leg for a script that does not exist, and do NOT convert a `run_if` leg to `run` in
this plan - each amendment wave flips its own leg from SKIPPED to PASSED simply by creating the file
the `run_if` already names.
  </action>

  <verify>
    <automated>chmod +x tests/run-all-340.sh &amp;&amp; bash tests/run-all-340.sh; test $? -eq 0 &amp;&amp; test "$(bash tests/run-all-340.sh 2>/dev/null | grep -c 'SKIPPED (file not present')" -eq 3 &amp;&amp; test "$(bash tests/run-all-340.sh 2>/dev/null | grep -c ': PASSED')" -eq 4</automated>
  </verify>

  <acceptance_criteria>
    - `tests/run-all-340.sh` exists and is executable (`test -x tests/run-all-340.sh`).
    - `bash tests/run-all-340.sh` exits 0.
    - Its output contains exactly 3 lines matching `SKIPPED (file not present` (the three unwritten entry floor tests).
    - Its output contains exactly 4 lines matching `: PASSED` (frozen-scalars, entry-31, entry-36, 195 seven-kind).
    - Its output contains 0 lines matching `: FAILED`.
    - `grep -c "run_if()" tests/run-all-340.sh` equals 1 and `grep -c "^run() {" tests/run-all-340.sh` equals 1 (the helpers were copied, not re-invented).
    - The two pre-existing red tests appear only inside comments: `grep -v '^#' tests/run-all-340.sh | grep -c "test-canon-crossref-completeness\|test-canon-part-9-ratification"` equals 0.
    - `grep -c "test-canon-entry-38-sourced-claims-floor.cjs" tests/run-all-340.sh` is at least 2 (the `run_if` file argument and the `node` command argument).
    - The file contains no U+2014 em-dash: `grep -c "$(printf '\xe2\x80\x94')" tests/run-all-340.sh` returns 0.
  </acceptance_criteria>

  <done>One command, `bash tests/run-all-340.sh`, is the phase's merge gate: green today with 4 PASS and 3 SKIP, and each amendment wave turns one SKIP into a PASS by landing its floor test file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree -> constitutional text | This plan reads the Canon but writes zero bytes to it; the boundary is enforced by the plan's own file list and by an acceptance criterion asserting `git status --porcelain` shows no canon-file change |
| local shell -> network | None. Every command in this plan is a local file read, a `grep`, an `ls`, or a `node -e` against a local module. Canon Part 8: zero Brain wire, zero egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-340-01 | Tampering | `docs/MINDRIAN-CANON.md` | mitigate | This plan declares no canon file in `files_modified`; Task 1's acceptance criteria assert `git status --porcelain` lists none of the four constitutional files |
| T-340-02 | Repudiation | `340-LIVE-VERIFICATION.md` | mitigate | Every figure is recorded as the LITERAL output of a named command plus the execution date, so a later wave can re-run the same command and reproduce it |
| T-340-03 | Information disclosure | Brain wire | accept | No Brain call, no network call, no user-room bytes touched anywhere in this plan; nothing to disclose |
| T-340-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable: this phase installs zero packages (340-RESEARCH.md "Package Legitimacy Audit: Not applicable"). Every new file uses only Node.js built-ins (`node:assert`, `node:fs`, `node:path`) and bash. No install task exists in any plan of this phase, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `bash tests/run-all-340.sh` exits 0 with 4 PASS / 0 FAIL / 3 SKIP.
- `340-LIVE-VERIFICATION.md` exists and answers 340-RESEARCH.md Open Questions 2 and 3 explicitly.
- `git diff --name-only` contains neither `docs/MINDRIAN-CANON.md` nor `docs/CANON-PHASE-MAP.md` nor `CLAUDE.md` nor `agents/larry-extended.md`.
</verification>

<success_criteria>
- The phase has a single green merge gate command before any amendment drafts prose.
- Every number waves A/B/C will write into the Canon has a dated, reproducible, live-command provenance in the phase directory.
- The two pre-existing red canon tests are documented, root-caused and explicitly scoped out, so no later wave misattributes them.
</success_criteria>

## Artifacts this phase produces

Newly created by THIS plan (exclude from any drift or source-grounding check - these symbols do not
exist on `main` before this plan runs):

| Artifact | Kind | Created by |
|----------|------|-----------|
| `tests/run-all-340.sh` | new bash aggregator script | 340-01 Task 2 |
| `.planning/phases/340-.../340-LIVE-VERIFICATION.md` | new phase record | 340-01 Task 1 |

Created LATER in this phase (named here only so this plan's aggregator can reference them as
`run_if` legs; they do not exist yet and their absence is expected):

| Artifact | Kind | Created by |
|----------|------|-----------|
| `tests/test-canon-entry-38-sourced-claims-floor.cjs` | new floor test | 340-02 |
| `tests/test-canon-entry-39-graph-substrate-floor.cjs` | new floor test | 340-03 |
| `tests/test-canon-entry-40-corpus-figures-floor.cjs` | new floor test | 340-04 |
| Appendix D entries 38, 39, 40 | new constitutional text | 340-02 / 340-03 / 340-04 |
| Canon versions 1.25, 1.26, 1.27 | new version tokens | 340-02 / 340-03 / 340-04 |
| Requirement IDs CANON-01 .. CANON-10 | new requirement family | minted at plan time, registered in `.planning/REQUIREMENTS.md` by 340-05 |

<output>
Create `.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-01-SUMMARY.md` when done.
</output>
