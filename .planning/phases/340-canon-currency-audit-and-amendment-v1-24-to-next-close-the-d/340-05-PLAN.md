---
phase: 340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d
plan: 05
type: execute
wave: 5
depends_on: [340-04]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-CLOSE-OUT-SWEEP.md
autonomous: true
requirements: [CANON-10]
canon_parts: [6]

must_haves:
  truths:
    - "One command proves the whole phase: bash tests/run-all-340.sh exits 0 with every leg real and none skipped."
    - "No stale figure or retired backend name survives in any live Canon Part or in CLAUDE.md, proven by a re-run of the same live commands the audit used, not by assertion."
    - "The Canon's own claims about the codebase are re-checked against the codebase one final time AFTER all three amendments landed, so the phase does not close on figures that were true three waves ago."
    - "The phase's findings live in both homes the Dev-Research Compositing mandate requires: the phase directory and the rethinking-mindrianos research room, mirrored and cross-linked."
    - "The CANON-01 through CANON-10 requirement family is registered in .planning/REQUIREMENTS.md, per the PYPORT / CHOKE / ANCHOR / TOOLHON / ICML phase-close precedent."
  artifacts:
    - path: ".planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-CLOSE-OUT-SWEEP.md"
      provides: "Post-amendment verification record: suite output, drift sweep, and the delta against 340-LIVE-VERIFICATION.md"
      contains: "run-all-340"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Registered CANON-01 .. CANON-10 requirement family with per-ID status"
      contains: "CANON-01"
  key_links:
    - from: ".planning/phases/340-.../340-CLOSE-OUT-SWEEP.md"
      to: "~/MindrianRooms/rethinking-mindrianos/research/"
      via: "Dev-Research Compositing filing, mirrored to ~/MindrianOS/research/ and cross-linked back"
      pattern: "rethinking-mindrianos"
---

<objective>
Close the phase: prove the three amendments hold together, prove no drift survived them, register
the requirement family, and file the durable reasoning trail in the two homes CLAUDE.md's
Dev-Research Compositing mandate requires.

Purpose: this phase exists because a constitution drifted from the codebase it governs while
every individual change looked reasonable. Closing it with an assertion rather than a re-check
would repeat the disease. The final act is a live re-run of the same commands the audit opened
with, now against the amended Canon, plus a written record of the delta.

Output: `340-CLOSE-OUT-SWEEP.md` (the post-amendment verification record), the CANON-01 through
CANON-10 registration in `.planning/REQUIREMENTS.md`, and a dated research-trail entry in
`~/MindrianRooms/rethinking-mindrianos/research/` mirrored to `~/MindrianOS/research/`.

This plan writes ZERO bytes to `docs/MINDRIAN-CANON.md`, `docs/CANON-PHASE-MAP.md`, `CLAUDE.md`,
`agents/larry-extended.md` or any `tests/` file. It is fully autonomous by construction.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-CONTEXT.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-RESEARCH.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-02-SUMMARY.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-03-SUMMARY.md
@.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Post-amendment verification sweep and drift re-check</name>

  <files>.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-CLOSE-OUT-SWEEP.md</files>

  <read_first>
    - `.planning/phases/340-.../340-LIVE-VERIFICATION.md` - the pre-amendment baseline this sweep is measured against, including the two documented pre-existing red tests
    - `.planning/phases/340-.../340-02-SUMMARY.md`, `340-03-SUMMARY.md`, `340-04-SUMMARY.md` - the three navigator verdicts, the recorded rulings, and each wave's own claimed lockstep manifest. This sweep verifies those claims independently rather than trusting them.
    - `tests/run-all-340.sh` - the aggregator whose legs this sweep runs
    - `docs/MINDRIAN-CANON.md` header and footer version lines - to confirm the final version
  </read_first>

  <action>
Run from `/home/jsagi/dev/MindrianOS-Plugin` (confirm with `pwd` first). Record every command's
LITERAL output in `340-CLOSE-OUT-SWEEP.md`, with a `**Swept:** <YYYY-MM-DD>` line at the top.

Section 1 - Suite:
1. `bash tests/run-all-340.sh` - record the full output and the exit code. Every leg must be real
   (7 PASS, 0 FAIL, 0 SKIP).
2. `node tests/test-canon-frozen-scalars-floor.cjs`, `node tests/test-canon-entry-31-two-gauge-floor.cjs`,
   `node tests/test-canon-entry-36-shape-declaration-floor.cjs`, `node tests/test-195-canon-7-kind-floor.cjs`,
   and the three new entry floor tests individually - record each exit code.
3. `node tests/test-canon-crossref-completeness.cjs` and `node tests/test-canon-part-9-ratification.cjs` -
   these were PRE-EXISTING RED before the phase. Re-run them and record whether their failure set
   is IDENTICAL to the baseline in `340-LIVE-VERIFICATION.md`. If a NEW assertion in either test
   now fails that was passing before, that is a regression this phase caused and must be reported,
   not absorbed. If the failure set is unchanged, record that explicitly so the SUMMARY can say so
   honestly rather than staying silent about two red tests.

Section 2 - Drift sweep (the assertions that prove the phase did what it claims):
4. `grep -c "25 methodology commands" docs/MINDRIAN-CANON.md CLAUDE.md` - expect 0 for both.
5. `grep -c "126 declared + 5 skill-exempt" CLAUDE.md` and `grep -c "126 as of this phase" docs/MINDRIAN-CANON.md` - expect 0 for both.
6. Slice the Canon's Appendix C section (from `## Appendix C` to the next `## `) and grep it for
   `pws-brain-mcp.onrender.com` - expect 0. Then grep the Appendix D section for `Pinecone` -
   expect NON-zero (history preserved, not scrubbed).
7. Slice Part 2 and grep for `Pinecone` - expect 0. Slice Part 7 and grep for `25 methodology commands` - expect 0.
8. `grep -c "A hedge word is not a source." docs/MINDRIAN-CANON.md agents/larry-extended.md` - expect 1 for both.
9. `grep -c "lib/core/node-insert.cjs" docs/MINDRIAN-CANON.md` and `grep -c "lib/core/room-skeleton-scaffold.cjs" docs/MINDRIAN-CANON.md` - expect non-zero for both.
10. For every n in 1..40, `grep -c "^${n}\. \*\*" docs/MINDRIAN-CANON.md` - expect at least 1 for
    every n. Record any n that returns 0 as a hard failure.
11. `grep -c "^Version: 1.27$" docs/MINDRIAN-CANON.md` and `grep -c "_Mindrian Canon v1.27 - MindrianOS Plugin_" docs/MINDRIAN-CANON.md` - expect 1 for both. `grep -c "(v1.27)" docs/CANON-PHASE-MAP.md` - expect non-zero.
12. Em-dash sweep across every file this phase touched: `grep -c "$(printf '\xe2\x80\x94')" docs/MINDRIAN-CANON.md docs/CANON-PHASE-MAP.md CLAUDE.md agents/larry-extended.md tests/run-all-340.sh tests/test-canon-entry-38-sourced-claims-floor.cjs tests/test-canon-entry-39-graph-substrate-floor.cjs tests/test-canon-entry-40-corpus-figures-floor.cjs` - expect 0 for every file.

Section 3 - Re-check the Canon's own claims against the codebase, one final time AFTER the
amendments landed (the point of the phase, applied to the phase's own output):
13. Re-run the edge-type enumeration `node -e "const e=require('./lib/core/navigation/edges.cjs'); console.log([...e.ALLOWED_EDGE_TYPES].sort().join('\n'))"` and, for EACH member, `grep -c "<TYPE>" docs/MINDRIAN-CANON.md`. Record any type with count 0 - that is a residual Part 4 gap the phase did not close, and it must be reported openly with a recommendation (register a follow-up), never silently absorbed.
14. Re-run the four-glob surface count and compare it to the number wave C wrote into Part 11. If
    the codebase moved between wave C and this sweep, record the delta and state plainly that the
    Canon's snapshot is illustrative and non-frozen by its own doctrine, so a small delta is
    expected and correct rather than a defect.
15. `node scripts/build-connector-registry.cjs --check` and `node scripts/check-shape-declaration.cjs --check` - record exit codes and compare the violation count to the baseline in `340-LIVE-VERIFICATION.md`. Neither should have moved; this phase touched no surface declaration.
16. `git log --oneline -8` and `git show --stat` for each of the three amendment commits - confirm
    each wave landed as ONE commit and that wave C's commit contains `docs/MINDRIAN-CANON.md` and
    `CLAUDE.md` together (the Pitfall 3 lockstep).

Close the record with a "Residual gaps and recommended follow-ups" section. If there are none,
say so explicitly. Do not pad it, and do not omit a real gap to make the phase look clean - that
would be the exact hedged-fabrication move Appendix D entry 38 was written to forbid.

House rule: hyphens only, no em-dashes. Record literal command output, not paraphrase.
  </action>

  <verify>
    <automated>bash tests/run-all-340.sh &amp;&amp; test -f .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-CLOSE-OUT-SWEEP.md &amp;&amp; grep -q "run-all-340" .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-CLOSE-OUT-SWEEP.md &amp;&amp; grep -q "Residual gaps" .planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-CLOSE-OUT-SWEEP.md &amp;&amp; test "$(bash tests/run-all-340.sh 2>/dev/null | grep -c ': FAILED')" -eq 0 &amp;&amp; test "$(bash tests/run-all-340.sh 2>/dev/null | grep -c 'SKIPPED (file not present')" -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-all-340.sh` exits 0 with 7 `: PASSED`, 0 `: FAILED`, 0 `SKIPPED (file not present` lines, and that output is recorded literally in the sweep file.
    - `340-CLOSE-OUT-SWEEP.md` exists with a `**Swept:**` date equal to the execution date and three labelled sections plus a "Residual gaps and recommended follow-ups" section.
    - The sweep records `grep -c "25 methodology commands" docs/MINDRIAN-CANON.md CLAUDE.md` returning 0 for both files.
    - The sweep records the Appendix C slice containing zero `pws-brain-mcp.onrender.com` AND the Appendix D slice containing at least one `Pinecone` - both, so the reader can see the absence was scoped and the history was not scrubbed.
    - The sweep records `grep -c "A hedge word is not a source." docs/MINDRIAN-CANON.md agents/larry-extended.md` returning 1 for each file.
    - The sweep records a per-number result for n = 1..40 of `grep -c "^${n}\. \*\*" docs/MINDRIAN-CANON.md` with no zero.
    - The sweep records the em-dash count as 0 for all eight named files.
    - The sweep records, for every member of the live `ALLOWED_EDGE_TYPES` set, whether it appears in the Canon, and names any residual zero explicitly rather than omitting it.
    - The sweep states whether the two pre-existing red canon tests have an IDENTICAL failure set to the `340-LIVE-VERIFICATION.md` baseline, and flags any new failure as a regression.
    - The sweep records `git show --stat` for wave C's commit showing `docs/MINDRIAN-CANON.md` and `CLAUDE.md` in the same commit.
    - `git status --porcelain docs/ CLAUDE.md agents/ tests/ lib/` returns empty: this task changed nothing outside `.planning/`.
  </acceptance_criteria>

  <done>A dated, literal-output record proves the three amendments hold together, no stale figure survived in any live Part or in CLAUDE.md, the historical record is intact, and any residual gap is named openly rather than absorbed.</done>
</task>

<task type="auto">
  <name>Task 2: Register CANON-01 .. CANON-10 in REQUIREMENTS.md and file the Dev-Research Compositing trail</name>

  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` - the existing structure: the milestone header, the "Cross-Cutting Rules" block, and the `### Phase family` sections with `- [ ] **ID**: description` checkbox items. Match this shape exactly; do not invent a new format.
    - `.planning/phases/340-.../340-CLOSE-OUT-SWEEP.md` (written by Task 1) - the evidence each requirement's status claim rests on
    - `.planning/phases/340-.../340-02-SUMMARY.md`, `340-03-SUMMARY.md`, `340-04-SUMMARY.md` - the three navigator verdicts and rulings, which the research-trail entry must record verbatim
    - `~/MindrianRooms/rethinking-mindrianos/research/2026-09-03-same-disease-consolidation/` (or the most recent sibling entry) - the shape of an existing research-room entry: its directory naming, its files, and how it cross-references the phase or seed it informs
    - `CLAUDE.md` section "Dev-Research Compositing (Rethinking Room)" - the mandate this task satisfies, including the explicit rule that a room entry which reaches a verdict and never lands in a phase, or a phase that cites the room without the citation, both count as INCOMPLETE
  </read_first>

  <action>
Two deliverables.

**A. Register the requirement family.** Append a `### Phase 340 - Canon currency (CANON-)` section
to `.planning/REQUIREMENTS.md` in the file's existing checkbox format, one line per ID, each
marked `[x]` only where `340-CLOSE-OUT-SWEEP.md` carries the evidence, and `[ ]` with a named
reason otherwise. The ten IDs and their scope:
  - CANON-01: Part 12 Sourced Claims Doctrine plus its `agents/larry-extended.md` mirror (entry 38, canon v1.25)
  - CANON-02: Part 9 two-chokepoint doctrinal split, `navigation.cjs` navigate and `node-insert.cjs` write (entry 39, canon v1.26)
  - CANON-03: Part 4 typed-edge-vocabulary reconciliation plus the non-frozen enumeration framing (entry 39)
  - CANON-04: Appendix B ICM L1-L3 code citations (entry 39)
  - CANON-05: Appendix C Glossary Brain origin corrected to `theo-mcp.onrender.com`, Brain kept as the constitutional role name per Phase 339 D-09 (entry 40, canon v1.27)
  - CANON-06: Part 2 Engine 1 backend currency, Pinecone retired, e5-local named (entry 40)
  - CANON-07: Part 7 methodology-command surface de-frozen in the Canon and in CLAUDE.md (entry 40)
  - CANON-08: Part 11 declared-surface snapshot refreshed, doctrine unchanged (entry 40)
  - CANON-09: CLAUDE.md project-instruction siblings landed in the SAME commit as their Canon counterparts, including the docu-optimizer Project Skills row per the navigator's ruling (entry 40)
  - CANON-10: `tests/run-all-340.sh` exists and runs seven real green legs; frozen scalars unweakened and Appendix D entries 1-37 preserved across all three waves; findings filed to the rethinking-mindrianos research room and mirrored
Each line cites the Appendix D entry number and the canon version that satisfies it, so the
requirement family is traceable to constitutional text and not only to a plan.

**B. File the Dev-Research Compositing trail.** Create
`~/MindrianRooms/rethinking-mindrianos/research/<YYYY-MM-DD>-canon-currency-audit-340/` following
the naming and file shape of the most recent existing sibling entry read above. It carries the
durable REASONING, distinct from the phase directory's executable decisions:
  - the part-by-part audit method and what it found, including the four findings that were NOT in
    the original four-front floor (Part 4's edge drift, Part 7's prescriptive stale number, Part
    2's retired backend, Part 11's stale snapshot) and WHY a part-by-part sweep surfaced them when
    a four-front scope would not have;
  - the generalizable lesson, stated so a future phase can use it: a number inside a PRESCRIPTIVE
    sentence is a different defect class from a number inside a DESCRIPTIVE one, and only the
    first needs de-freezing rather than updating (340-RESEARCH.md Pitfall 2);
  - the second generalizable lesson: an additive change to a closed constitutional set that skips
    its amendment wave is invisible until someone diffs the set against the prose, which is how
    Part 4 drifted 15 types behind across multiple phases;
  - the three navigator verdicts and all recorded rulings, verbatim with their dates;
  - the two open items this phase deliberately did NOT take: the harness-as-code prose-fabrication
    detector (deferred to phase 2 per 340-CONTEXT.md, dependent on SEED-032/SEED-062) and the two
    pre-existing red canon tests pinned to canon v1.4;
  - the langtalks grounding relayed in 340-RESEARCH.md, carried forward with its honest caveat
    intact: the two arxiv papers' corpus linkage is confirmed real, their CONTENT relevance is an
    untested hypothesis and must not be restated as a finding;
  - a cross-reference back to `.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/`.
Mirror the same directory to `~/MindrianOS/research/<same-name>/` as source-of-record.

The mandate is explicit that neither home substitutes for the other: a room entry that reaches a
verdict and never lands in a phase, and a phase that says "per the room's research" without the
citation, both count as incomplete. Cross-link both directions.

Do not write to `docs/`, `CLAUDE.md`, `agents/`, `tests/` or `lib/` in this task.
  </action>

  <verify>
    <automated>grep -c "CANON-01" .planning/REQUIREMENTS.md | grep -qv "^0$" &amp;&amp; grep -c "CANON-10" .planning/REQUIREMENTS.md | grep -qv "^0$" &amp;&amp; ls -d ~/MindrianRooms/rethinking-mindrianos/research/*canon-currency-audit-340* &amp;&amp; ls -d ~/MindrianOS/research/*canon-currency-audit-340* &amp;&amp; grep -rq "340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d" ~/MindrianRooms/rethinking-mindrianos/research/*canon-currency-audit-340*/ &amp;&amp; test "$(git status --porcelain docs/ CLAUDE.md agents/ tests/ lib/ | wc -l)" -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains a Phase 340 section with exactly ten lines matching `CANON-0[1-9]` or `CANON-10`, each in the file's existing `- [ ] **ID**: description` checkbox format.
    - Every CANON-NN line names the Appendix D entry number and the canon version that satisfies it.
    - Every line marked `[x]` has corresponding evidence in `340-CLOSE-OUT-SWEEP.md`; every line marked `[ ]` carries a named reason in the same line.
    - A directory matching `~/MindrianRooms/rethinking-mindrianos/research/*canon-currency-audit-340*` exists and contains at least one markdown file.
    - The identical directory exists at `~/MindrianOS/research/*canon-currency-audit-340*` (the mirror), and a byte-comparison of the two directories' markdown files reports no differences.
    - The room entry contains the literal phase slug `340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d` (the cross-reference back to the phase).
    - The room entry records all three navigator verdicts verbatim with their dates, and names the two deliberately-untaken items (the harness-as-code detector, the two pre-existing red canon tests).
    - The room entry restates the langtalks grounding WITH its caveat: `grep -c "not verified\|untested hypothesis\|CONTENT relevance" <entry>` is at least 1. A restatement that presents the two arxiv papers' content as a finding fails this criterion, because that is the exact hedged-fabrication move entry 38 forbids.
    - `grep -c "$(printf '\xe2\x80\x94')" .planning/REQUIREMENTS.md` returns 0 and the same check returns 0 for every markdown file in the new research entry.
    - `git status --porcelain docs/ CLAUDE.md agents/ tests/ lib/` returns empty.
  </acceptance_criteria>

  <done>The CANON family is registered with per-ID evidence, and the phase's reasoning lives in both required homes, cross-linked in both directions, with every caveat carried forward intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification claim -> evidence | A close-out sweep that asserts green without recording the command output is a claim without a source, which is exactly what entry 38 forbids. Every acceptance criterion here requires literal recorded output |
| phase directory -> research room | The Dev-Research Compositing mandate requires the finding in both homes, cross-linked. Neither substitutes for the other |
| local -> Brain | None. Every action is a local read, a local grep, or a local file write. Canon Part 8: the room entry contains MindrianOS's own dev reasoning and never crosses a Brain wire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-340-21 | Repudiation | close-out verification claim | mitigate | Task 1 requires literal command output recorded for every check, including the two pre-existing red tests, so the phase cannot close on an unevidenced "all green" |
| T-340-22 | Tampering | residual-gap suppression | mitigate | Task 1's acceptance criteria require every residual zero in the edge-type re-check to be named openly and a "Residual gaps" section to exist even when empty; omitting a real gap to make the phase look clean is called out in the action text as the exact move entry 38 forbids |
| T-340-23 | Spoofing | relayed langtalks grounding | mitigate | The room entry must carry the caveat that the two arxiv papers' CONTENT relevance is an untested hypothesis; an acceptance criterion greps for that caveat |
| T-340-24 | Tampering | constitutional files after the waves | mitigate | Both tasks assert `git status --porcelain docs/ CLAUDE.md agents/ tests/ lib/` is empty; this plan is read-only against everything the amendments touched |
| T-340-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable: zero packages installed in this phase. This plan runs only existing local scripts and Node built-ins |
</threat_model>

<verification>
- `bash tests/run-all-340.sh` exits 0 with 7 PASS, 0 FAIL, 0 SKIP.
- `340-CLOSE-OUT-SWEEP.md` records literal output for every check, including a per-number Appendix D presence result for n = 1..40 and an explicit residual-gap section.
- `.planning/REQUIREMENTS.md` carries CANON-01 through CANON-10 with per-ID evidence.
- A dated research entry exists in `~/MindrianRooms/rethinking-mindrianos/research/`, mirrored byte-identically to `~/MindrianOS/research/`, cross-referencing the phase slug.
- `git status --porcelain docs/ CLAUDE.md agents/ tests/ lib/` is empty.
</verification>

<success_criteria>
- The phase closes on evidence, not assertion: every claim in the close-out record has a recorded command behind it.
- Any residual gap is named openly with a recommended follow-up rather than absorbed.
- The requirement family is registered and traceable to constitutional text (entry number plus canon version), not only to a plan.
- The reasoning trail lives in both homes the Dev-Research Compositing mandate requires, cross-linked in both directions, with every caveat intact.
</success_criteria>

## Artifacts this phase produces

Newly created by THIS plan (exclude from drift and source-grounding checks):

| Artifact | Kind | Created by |
|----------|------|-----------|
| `.planning/phases/340-.../340-CLOSE-OUT-SWEEP.md` | new phase record | 340-05 Task 1 |
| `.planning/REQUIREMENTS.md` Phase 340 `CANON-` section (CANON-01 .. CANON-10) | new requirement registration | 340-05 Task 2 |
| `~/MindrianRooms/rethinking-mindrianos/research/<date>-canon-currency-audit-340/` | new research-room entry | 340-05 Task 2 |
| `~/MindrianOS/research/<date>-canon-currency-audit-340/` | new mirrored research entry | 340-05 Task 2 |

Created EARLIER in this phase and only READ here (never modified by this plan):
`tests/run-all-340.sh`, `tests/test-canon-entry-38-sourced-claims-floor.cjs`,
`tests/test-canon-entry-39-graph-substrate-floor.cjs`,
`tests/test-canon-entry-40-corpus-figures-floor.cjs`, Canon Appendix D entries 38/39/40, Canon
versions 1.25/1.26/1.27, the `agents/larry-extended.md` Sourced Claims mirror,
`.planning/phases/340-.../340-LIVE-VERIFICATION.md` and the three `340-0N-DRAFT-PROSE.md` records.

<output>
Create `.planning/phases/340-canon-currency-audit-and-amendment-v1-24-to-next-close-the-d/340-05-SUMMARY.md` when done.
</output>
