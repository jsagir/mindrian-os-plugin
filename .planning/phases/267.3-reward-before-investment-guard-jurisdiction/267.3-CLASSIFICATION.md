# 267.3-CLASSIFICATION.md - The first-reward rubric and the 17 blocked commands

> Phase 267.3, plan 04, Task 1. Written 2026-08-28 at HEAD `08250c63`.
>
> Part 1 is the rubric: the signals that go in, the vocabulary term that comes out,
> and the tie-break rules that settle a contested row. It is written so plans
> 267.3-06 and 267.3-07 can apply it to the remaining 50 commands without
> re-deriving any of this reasoning.
>
> Part 2 is one reasoned row per command for the 17 that block Phase 271, each
> carrying the `path:line` where that command first hands the navigator something.
>
> NO frontmatter was written by this task. The vocabulary is closed at the eight
> members ruled by `267.3-DECISIONS.md` D-B; nothing here mints a ninth.

---

## Part 1 - The rubric

### 1.1 The question the rubric answers

Not "what is this command for". The question is narrower and answerable from the
file: **what is the first thing this command hands back to the navigator, and
which of the eight legal tokens names that thing honestly.**

### 1.2 The signals that go in

Every one of these is readable off the file without guessing.

| Signal | Where it lives | What it tells you |
|---|---|---|
| `hitl_shape` / `hitl_stages` | frontmatter | Whether the command reaches a genuine human fork. `F.0` is a decision-close, `F.1` a single-fork next move. Either one means the command is interactive BY ITS OWN DECLARATION. |
| `body_shape` | frontmatter | `methodology` is a Larry-narrated reasoning flow. `E (Action Report)` is a report. `A` / `B` / `C` are layout disciplines, orthogonal to the reward question. |
| `kind` | frontmatter | `methodology`, `meta`, `mechanical`. Describes the family, not the reward. See TB-4. |
| `serves_jtbd` and `teaching` | frontmatter | What the author says the command is for, in their own words. |
| script vs narrate | body | Does the body shell out (a bash fence, a `node scripts/` call) or does it narrate a conversation. |
| first user-visible delivery | body | The load-bearing signal. See S-1 for what counts. |

### 1.3 S-1: what counts as a first delivery

A delivery is something the command PRODUCED and handed over. Not everything
user-visible is a delivery.

These are NOT deliveries:

- A question, however short. `Ask: "Quick pass or deep dive?"` is an ask.
- A menu, a selector, or an `AskUserQuestion` card. The rule doc puts a pick from
  three or fewer options on the investment side of the ledger and calls it a
  non-violation, which is exactly the point: it costs the navigator nothing and it
  gives them nothing.
- A framework explanation printed from a reference file. A textbook summary fails
  qualifying test 4 (it is not uniquely attributable to MindrianOS).
- A prerequisite or tooling message ("you will need the Vercel CLI").
- An error envelope.

The cited line in Part 2 is the first line at which the command hands over
something it produced.

### 1.4 The gate every candidate value must pass

The four qualifying tests, verbatim in substance from
`docs/reward-before-investment-rule.md:52-56`. A candidate that fails any one of
the four is not a reward:

1. **Unpredictable** - the navigator could not have produced it themselves in
   under 30 seconds.
2. **Intelligible** - the navigator understands what they are seeing without
   explanation.
3. **Valuable** - they would pay attention to it on someone else's screen.
4. **Uniquely attributable to MindrianOS** - not something a general assistant
   could do in the same time budget.

### 1.5 The two named disqualifiers

Both are the rule doc's own words, not an inference:

- **D-1: a grade is not a reward.** `docs/reward-before-investment-rule.md:58`,
  "A grade is not a reward (predictable output of an asked question)." A numeric
  score the navigator asked for cannot be the declared first reward.
- **D-2: a status report is not a reward.**
  `docs/reward-before-investment-rule.md:59-60`, "A `/mos:status` output is not a
  reward (predictable report)." and "A status report is not a reward." This is the
  disqualifier that sends a command to `--none (diagnostic surface)`.

### 1.6 The tie-break rules

**TB-1. `--none (scripting only)` is FORBIDDEN without a real scripting flag.**
The scripting override at `docs/reward-before-investment-rule.md:95` is legitimate
only "IF the command is invoked with a `--no-interactive`, `--script`, or `-q`
flag". If the body carries no such invocation path, this token is forbidden no
matter how script-heavy the command looks. A body full of bash fences is not a
scripting flag. A `--json` output-format flag is not a scripting flag either: it
changes the shape of the output, not the interactivity of the invocation.

*Measured, not assumed.* All 17 bodies were scanned for `--no-interactive`,
`--script` and a standalone `-q`. Zero of 17 carry any of the three. TB-1
therefore forbids `--none (scripting only)` on every one of the 17, without a
single judgment call being needed.

**TB-2. `hitl_shape: F.0` or `F.1` cannot take a scripting opt-out.** An F.0
decision-close or an F.1 single-fork surface reaches a genuine human fork by its
own declaration (Canon Part 11). A command that declares it stops for a human
cannot also declare it is a silent script. This is the rule that would have caught
the 271-03 `publish` error on its own, and TB-1 catches it a second time
independently.

**TB-3. When two terms both fit, prefer the closer minting flow, and record the
loser.** The rule doc's "Application to existing flows" section binds four of the
six original terms to exactly one command each. Compare the candidate's first
delivery against those shapes and take the nearer one. A row where two terms
plausibly fit and no runner-up is recorded is an incomplete row.

**TB-4. The family qualifier in a term's class definition is descriptive, not a
gate.** `methodology_reframe` is defined as "a conversational methodology command
whose first delivered value is Larry's analysis or reframe over the user's OWN
material". The first clause names the family the term was minted against; the
SECOND clause is the discriminating test. A command outside that family whose
first delivery is unmistakably Larry's analysis over the navigator's own material
takes the term. Reading the first clause as a gate would leave `deck` and
`suggest-next` with no honest token while their substance matches exactly.
This rule is stated explicitly because it is the one Part 1 rule the navigator
might reject, and rejecting it moves exactly two rows.

**TB-5. Preview versus deliverable.** When the first delivery is a structure the
command will then go on to fill or execute, it is a preview and takes
`schema_preview`. When the same-looking structure IS the command's whole output
and nothing further runs, it is the analysis itself and takes
`methodology_reframe`. This is the distinction `267.3-DECISIONS.md` Section 3.2
already draws when it calls `schema_preview` "a promise about extraction, not an
analysis", and it is what the `find-bottlenecks` calibration example encodes:
mapping the system boundaries previews the Reverse Salient structure BEFORE the
navigator invests in the full bottleneck hunt.

**TB-6. No minting.** The vocabulary is closed at the eight members of
`REWARD_TYPES`. If no member is honest for a command, the row is NEEDS-RULING. It
is never a licence to invent a token; a ninth term is a second canon amendment and
belongs to a ruling, not to a classification.

### 1.7 The four calibration examples, read as worked judgments

Read before classifying anything. Each is tailored to its own command; none is a
template.

- `commands/onboard.md:12` = `reframe_question`. Fits because onboard's first
  screen is a single-sentence question and the reward is Larry reframing THAT
  SENTENCE, before any material exists. It is bound to the sentence, not to the
  room.
- `commands/find-bottlenecks.md:12` = `schema_preview`. Fits because mapping the
  system boundaries shows the SHAPE of the Reverse Salient hunt before the
  navigator commits to the hunt. The preview is of work still to come.
- `commands/new-project.md:12` = `instant_brief`. Fits because new-project runs
  the actual 30-second MVA pipeline. The term is bound to that pipeline, so it
  does not travel to commands that merely produce a fast summary.
- `commands/grade.md:11` = `calibration_distribution_preview`. Fits because the
  reward is the calibration set's anonymized distribution, shown INSTEAD of the
  grade, precisely because disqualifier D-1 says the grade itself cannot be it.

### 1.8 How to apply this to the remaining 50

In order: read the frontmatter signals; find the first delivery per S-1; run the
four qualifying tests; check the two disqualifiers; if the answer is "it reports
state", stop at `--none (diagnostic surface)`; if the answer is "Larry reasons
over the navigator's own material", stop at `methodology_reframe`; if the answer
is "a structure to be filled later", stop at `schema_preview`; check TB-1 before
ever writing `--none (scripting only)`; record the runner-up; and mark
NEEDS-RULING rather than force a fit.

---

## Part 2 - The 17 rows

Legend: the first-delivery column cites the line where the command first hands the
navigator something it produced, per S-1.

| # | Command | hitl | body_shape | First delivery | Proposed | Runner-up (rejected) | Confidence |
|---|---|---|---|---|---|---|---|
| 1 | analyze-systems | F.8 | methodology | `commands/analyze-systems.md:64` | `methodology_reframe` | `schema_preview` | HIGH |
| 2 | causal | F.9 | methodology | `commands/causal.md:140` | `methodology_reframe` | `schema_preview` | HIGH |
| 3 | challenge-assumptions | F.8 | methodology | `commands/challenge-assumptions.md:69` | `methodology_reframe` | `schema_preview` | HIGH |
| 4 | compare-ventures | F.5 | methodology | `commands/compare-ventures.md:89` | `methodology_reframe` | `instant_brief` | HIGH |
| 5 | deck | F.1 | F.1 | `commands/deck.md:92` | `methodology_reframe` | `schema_preview` | HIGH (rests on TB-4) |
| 6 | diagnose | F.0 + F.1 stages | A | `commands/diagnose.md:82` | `methodology_reframe` | `schema_preview` | HIGH |
| 7 | doctor | F.0 | E (Action Report) | `commands/doctor.md:42` | `--none (diagnostic surface)` | `--none (scripting only)` | HIGH |
| 8 | find-connections | F.8 | methodology | `commands/find-connections.md:88` | `methodology_reframe` | `schema_preview` | HIGH |
| 9 | leadership | F.1 | methodology | `commands/leadership.md:72` | `methodology_reframe` | `reframe_question` | HIGH |
| 10 | lean-canvas | F.8 | methodology | `commands/lean-canvas.md:64` | `methodology_reframe` | `schema_preview` | HIGH |
| 11 | mullins | F.8 | methodology | `commands/mullins.md:83` | `methodology_reframe` | `calibration_distribution_preview` | HIGH |
| 12 | pipeline | F.2 + F.9 stages | E | `commands/pipeline.md:65` | `schema_preview` | `methodology_reframe` | NEEDS-RULING |
| 13 | publish | F.0 | E | `commands/publish.md:149` | `--none (diagnostic surface)` | `instant_brief` | NEEDS-RULING |
| 14 | score-innovation | F.8 | methodology | `commands/score-innovation.md:66` | `methodology_reframe` | `calibration_distribution_preview` | HIGH |
| 15 | show | F.1 | F.1 | `commands/show.md:50` | `--none (diagnostic surface)` | `schema_preview` | NEEDS-RULING |
| 16 | suggest-next | F.1 | B | `commands/suggest-next.md:60` | `methodology_reframe` | `schema_preview` | HIGH (rests on TB-4) |
| 17 | systems-thinking | F.8 | methodology | `commands/systems-thinking.md:116` | `methodology_reframe` | `schema_preview` | HIGH |

Tally: 14 HIGH, 3 NEEDS-RULING. `--none (scripting only)` proposed zero times,
forbidden 17 times by TB-1 on measured evidence.

### Row 1. analyze-systems

First delivery at `commands/analyze-systems.md:64`: Larry walks the framework
phases and hands back the system decomposed into layers with the leverage points
named, capped at three levels so it stays legible. The `Ask: "Quick pass or deep
dive?"` one line above is a two-option pick, which S-1 says is not a delivery and
the rule doc's own non-violation list says costs nothing.

Passes all four tests: a layered decomposition of the navigator's own system with
leverage points named is not something they produce in 30 seconds, and it is
Systems Thinking applied to their room, not a generic explainer.

Proposed `methodology_reframe`. Runner-up `schema_preview` loses on TB-5: nothing
here is a preview of work still to come, the decomposition is the whole
deliverable.

### Row 2. causal

First delivery at `commands/causal.md:140`: the extracted cause / mechanism /
effect table, pulled out of an artifact the navigator already wrote, each row
carrying a confidence, a domain, and a falsifiable prediction, presented for
accept / edit / reject before anything is written to the graph.

This is the row where TB-5 does real work. `schema_preview` is tempting because
the table sits in front of a confirmation gate, so it LOOKS like a preview. It is
not: the extraction has already happened, and what is deferred is the graph WRITE,
not the extraction. `267.3-DECISIONS.md` Section 3.2 draws exactly this line when
it calls `schema_preview` "a promise about extraction, not an analysis". Here the
analysis is done and on screen.

Proposed `methodology_reframe`. Runner-up `schema_preview` rejected per TB-5.

Note on the body: the audit's regenerated signal table marked `causal` as `CODE`
because its body carries script invocations, and `267.3-DECISIONS.md` Section 3.2
deliberately left its class to this plan rather than pre-assigning it. Read
against the actual text, the script content is the WRITE path (`.causal-extract.json`
generation); the first delivery is a narrated Larry extraction. The `CODE` marker
describes the second half of the body, not the first reward.

### Row 3. challenge-assumptions

First delivery at `commands/challenge-assumptions.md:69`: the black-hat lens runs
in single rotation and the tension-map synthesizer surfaces where the risk reading
CONTRADICTS the room's standing claims. A contradiction between two things the
navigator themselves wrote is the definition of unpredictable-but-intelligible.

Proposed `methodology_reframe`. Runner-up `schema_preview` rejected per TB-5: the
tension map is the output, not a preview of a later pass.

### Row 4. compare-ventures

First delivery at `commands/compare-ventures.md:89`: Larry combines the
`brain_find_patterns` co-occurrence result and the semantic-search result and
presents similar ventures, the framework sequences they used, and the patterns
that correlated with strong and weak outcomes, all aggregated so no individual is
identifiable.

The material analyzed is partly the Brain's, but it is selected and framed against
the navigator's own framework profile from `room/STATE.md`, so the second clause of
`methodology_reframe` holds: Larry reasons over what the navigator brought and
hands back a reading of it. It also passes test 4 more strongly than most rows,
because the co-occurrence data is the moat and no general assistant has it.

Proposed `methodology_reframe`. Runner-up `instant_brief` rejected: that token is
bound to the actual 30-second MVA pipeline
(`docs/reward-before-investment-rule.md:69` and `:135`), which this command does
not run.

### Row 5. deck

First delivery at `commands/deck.md:92`: the per-section build flow. Each major
section of the chosen structure is built from the navigator's own room content and
surfaced at an accept / reshape / skip gate. The style sub-selector three lines
earlier is a three-option menu and is not a delivery per S-1.

Proposed `methodology_reframe`, and this row rests on TB-4. `deck` declares
`kind: mechanical` and `frameworks: []`, so it sits outside the family the term was
minted against. Its substance matches the term's discriminating clause exactly: the
first thing handed over is the navigator's own room content, reduced to essence and
arranged. If the navigator rejects TB-4 and reads the family qualifier as a gate,
this row has no honest token and becomes NEEDS-RULING.

Runner-up `schema_preview` rejected per TB-5: the section is filled, not outlined.

### Row 6. diagnose

First delivery at `commands/diagnose.md:82`: three to five methodology commands,
ranked by fit, each with its reasoning. Note what is NOT delivered: line 81 says
the classification itself is done SILENTLY and the labels are never announced,
which is Canon Part 12 invisibility. So the reward is the routing, not the
diagnosis. The one clarifying question at line 80 is inside the one-sentence
budget.

Proposed `methodology_reframe`. Runner-up `schema_preview` rejected per TB-5: the
ranked routing is the output; diagnose does not then go on to run the ranked
commands itself.

### Row 7. doctor - argued in full, per the plan's flag

First delivery at `commands/doctor.md:42`: `scripts/doctor.cjs` walks the module
registry, calls each runner's `check(ctx)`, and renders ONE STATUS ROW PER RESULT
plus a tallied summary.

**The case for a reward.** doctor is not trivial. It detects install-cache drift
that has genuinely happened twice in this project's history, it can find a
silently disabled plugin that cannot report itself, and under `--fix` it REPAIRS
rather than merely reporting. `body_shape_detail` also promises an F.1 Next Move
selector when drift is detected without `--fix`. On the four qualifying tests
alone it would pass: a navigator cannot compute six-way version-of-record
consistency in 30 seconds, and nothing else in the world can do it for a
MindrianOS install.

**Why it loses anyway.** Disqualifier D-2 is not a judgment call, it is the rule
doc's own sentence: "A status report is not a reward." The shape of doctor's first
delivery is precisely a status roster: one row per check, then totals. The repair
is real but it is gated behind `--fix`, which means it is not the FIRST delivery on
a bare invocation, and the bare run is the surface the declaration must describe.
`267.3-AUDIT.md` Section 3.3 independently measured doctor into this class from its
`F.0` / `E (Action Report)` / `CODE` signal row, and `267.3-DECISIONS.md` Section
3.2 lists it among the nine the `--none (diagnostic surface)` amendment was minted
for. Three independent readings agree.

Proposed `--none (diagnostic surface)`.

Runner-up `--none (scripting only)` rejected TWICE over, independently. First by
TB-1: doctor carries a very long `argument-hint` and roughly thirty flags, and not
one of them is `--no-interactive`, `--script` or `-q`. Its `--json` flag at
`commands/doctor.md:110` is the closest thing and it is still not one of the three:
it changes the output format for hooks and regression tests, it does not declare a
non-interactive invocation. Second by TB-2: `hitl_shape: F.0` is a decision-close
fork, so doctor declares that it stops for a human. This is the same double
rejection that catches `publish`, arrived at from the flag list rather than from
the shape, which is why both rules are stated rather than one.

### Row 8. find-connections

First delivery at `commands/find-connections.md:88`: the aha moments. Unexpected
links between fields that looked unrelated, bridging frameworks that appear in both
domains, and the analogy handed over in the navigator's own terms, "your problem in
domain A has the same structure as this problem in domain B". This is the clearest
test-1 pass in the whole set: a surprising structural analogy is by construction
something the navigator could not have produced.

Proposed `methodology_reframe`. Runner-up `schema_preview` rejected per TB-5.

### Row 9. leadership

First delivery at `commands/leadership.md:72`: the opening ADAPTS to the team data
already filed in the room. With profiles present Larry references specific team
members by name; with an advisory network present he names it and asks how it is
actually being used; with a growing team he opens on the team-as-system read. The
navigator gets a read of their own team shape back before answering anything.

Proposed `methodology_reframe`. Runner-up `reframe_question` rejected: that token
is bound to onboard's single-sentence opener, where Larry reframes a SENTENCE
before any material exists. Here the material already exists in the room and the
reframe is over the filed profiles, which is the exact distinction
`267.3-DECISIONS.md` Section 3.2 draws when it minted the new term.

### Row 10. lean-canvas

First delivery at `commands/lean-canvas.md:64`: Larry walks the nine boxes and
CHALLENGES each one, refusing vague answers, so the first thing handed back is the
pushback on the navigator's own first box rather than a form to fill.

Proposed `methodology_reframe`. Runner-up `schema_preview` rejected per TB-5: the
nine-box grid is a fixed published structure, not a preview of extraction, and the
value is the challenge, not the grid.

This is the weakest reward case among the ten methodology rows and it is recorded
as such in the remediation flags below. The DECLARATION is still honest; the flow
remediation is B5 follow-up work, not this plan's.

### Row 11. mullins

First delivery at `commands/mullins.md:83`: the quick pass rates all seven domains
one to five with a sentence of evidence each and SURFACES THE WEAKEST TWO. The
weakest-two call is the reward; it is the thing the navigator would not have named
themselves. The seven-domain explainer twenty lines earlier is a framework summary
and fails qualifying test 4, so it is not the delivery.

Proposed `methodology_reframe`. Runner-up `calibration_distribution_preview`
rejected on two grounds: it is bound to the calibration set's anonymized
distribution (`commands/grade.md:11`), which mullins does not read, and
disqualifier D-1 means the one-to-five scores could not be the declared reward even
if it did. What survives D-1 is the naming of the weakest domain, which is an
analysis, not a grade.

### Row 12. pipeline - argued in full, per the plan's flag - NEEDS-RULING

First delivery at `commands/pipeline.md:65`: the helper Brain-derives the framework
chain through a FEEDS_INTO traversal, composes it into an ordered `/mos:` command
sequence through the resolver, and PRINTS THE RUN ORDER.

**Why this row is genuinely argued, and it is not the argument the plan expected.**
The contest is not mainly between two tokens. It is that pipeline has THREE
invocation paths with three different first deliveries, and the declaration is a
single value that must describe one of them.

- `--from-problem-type` or `--from-framework` (`commands/pipeline.md:62-65`): the
  helper prints the graph-derived chain and run order with zero conversational
  input. A real delivery, and the strongest one pipeline has.
- A named pipeline (`/mos:pipeline discovery`, line 98): loads `CHAIN.md` and goes
  straight to Stage 1. The first thing the navigator sees is the Stage 1
  methodology's own output, which belongs to that methodology, not to pipeline.
- Bare `/mos:pipeline` (lines 100-105): recommends discovery or thesis based on
  venture stage and presents both options for the navigator to choose. Under S-1
  that is a two-option menu and not a delivery at all, which leaves the bare path
  with no first reward of its own.

There is also a fourth thing that fires before any of them on a resumed room: the
resume offer at line 114, "I see you have already completed Stage N", which is a
status report and is disqualified by D-2.

**The proposal and why.** `schema_preview`, describing the flagged path. TB-5
settles the token cleanly once the path is fixed: the printed run order is a
structure the command will then go on to EXECUTE, so it is a promise about work to
come, which is exactly the `find-bottlenecks` calibration shape.

Runner-up `methodology_reframe` rejected under TB-5 and on a second ground: at the
moment the chain is printed, Larry has reasoned over a `ProblemType` token and a
framework list, not over the substance of the navigator's claims. Calling that an
analysis of the navigator's own material would overstate it.

`--none (scripting only)` is FORBIDDEN here and the trap is worth naming, because
pipeline is the command in this set that most LOOKS like a scripting surface: it
has an `argument-hint` with three flags, it shells out to a helper, and its
`kind: meta` reads as machinery. TB-1 still forbids it, on measured evidence: not
one of `--no-interactive`, `--script`, `-q` appears in the body. This is the same
error shape as the 271-03 `publish` miscall, reached from appearance rather than
from shape.

**What the navigator needs to rule:** which invocation path the single declaration
describes. If the flagged `--from-*` path, `schema_preview` stands. If the bare
path, pipeline has no first reward of its own and joins the `show` question in
Row 15.

### Row 13. publish - argued in full, per the plan's flag - NEEDS-RULING

First delivery of VALUE at `commands/publish.md:149`: the deploy output is parsed
and the live URL is handed over, "your presentation is live, share that link with
anyone". Before that, the first user-visible lines are prerequisite messages at
`commands/publish.md:63-68` ("you will need the Vercel CLI, run `npm i -g vercel`"),
which S-1 excludes as tooling state rather than a delivery.

**The 271-03 error, and why it was wrong twice over.** Plan 271-03's first pass
guessed `--none (scripting only)`. TB-2 rejects it: `hitl_shape: F.0` at
`commands/publish.md:6` with `hitl_why` "it surfaces one publish action for a
single approve-or-defer decision" is a declared human fork, so publish states in its
own frontmatter that it stops for a human. TB-1 rejects it a second time and
independently: publish carries `argument-hint: "[--all|<view>]"` plus `--sections`
and `--private` in the body, and not one of `--no-interactive`, `--script` or `-q`
appears anywhere in the file. Two rules, two rejections, no judgment call in
either. That error is not repeated here and cannot be reached by this rubric.

**The real problem, which is different and harder.** publish DOES deliver a
reward. A live shareable URL for the navigator's whole Data Room passes all four
qualifying tests without strain: they could not produce it in 30 seconds, they
understand it instantly, they would look at it on someone else's screen, and no
general assistant deploys their room. It is a genuine variable reward.

And the closed vocabulary has no token for it. Walk all eight: `reframe_question`,
`instant_brief`, `schema_preview`, `calibration_distribution_preview` and
`paragraph_preview` are each bound to a different specific flow;
`methodology_reframe` requires Larry's analysis over the navigator's material and
publish analyzes nothing; `--none (scripting only)` is forbidden twice over; and
`--none (diagnostic surface)` says the command "reports state rather than
delivering a variable reward", which is a FALSE description of publish in a
different direction, because publish performs an irreversible action and hands back
a real artifact.

**The proposal, stated honestly as a placeholder.** `--none (diagnostic surface)`,
as the least-false available token, on the reading that the opt-out family means
"this command declares no first reward from the closed vocabulary" rather than
strictly "this command reports state". TB-6 forbids this task from minting the
token publish actually wants.

Runner-up `instant_brief` rejected: bound to the 30-second MVA pipeline, which
publish does not run, and stretching it to mean "any fast valuable output" would
dissolve the term.

**What the navigator needs to rule:** one of three. (a) Accept the broadened
reading of `--none (diagnostic surface)` above, which also settles Row 15 and
possibly Row 12. (b) Rule that publish's live URL warrants a ninth token, which is
a second canon amendment and a separate ruling, not a classification. (c) Name a
different existing token and its reasoning. A wrong declaration here is worse than
a missing one, because it greens the guard on a false claim.

### Row 14. score-innovation

First delivery at `commands/score-innovation.md:66`: a conversational cross-domain
opportunity assessment, explicitly no computation and no algorithms, driven by
Larry's judgment and Socratic questioning over the domains the navigator has
actually explored (the setup at line 60 reads all room sections to find them).

Proposed `methodology_reframe`. Runner-up `calibration_distribution_preview`
rejected: the name says "score" but the body says the opposite of a computed score,
and D-1 would disqualify a score anyway. The reward is the named intersection
between two domains nobody thought to combine.

### Row 15. show - NEEDS-RULING

First delivery at `commands/show.md:50`: ONE `AskUserQuestion` selector, four lanes
as tabs, jobs as user-voice options, opened on the lane that
`defaultLaneForRoleBlend` picked from the navigator's own `USER.md` role blend.

**The router problem.** Under S-1 a selector is not a delivery, and show is a pure
router by design: line 40 says outright "you do NOT build any view here", it
resolves the chosen job through `command-resolver` and hands the chain to
`runChain`. Every reward show produces is its TARGET's reward. The vocabulary has
no token for an inherited reward, so show has no honest value of its own.

Proposed `--none (diagnostic surface)` on the same broadened reading offered in Row
13: the honest content of the declaration is "no first reward of its own", and the
opt-out family is where that belongs. Runner-up `schema_preview` rejected, though
it is closer than it looks: the four lanes do preview what the room can answer, and
the persona-defaulted opening lane is a small piece of real personalization
delivered before any input. It loses because a menu is not a delivery under S-1,
and because reading it as a preview would make every selector in the product a
`schema_preview`.

**What the navigator needs to rule:** whether a pure router declares the opt-out or
inherits its target's reward. Ruling this once settles show, and the same reading
carries to Row 13 and possibly Row 12.

### Row 16. suggest-next

First delivery at `commands/suggest-next.md:60`: the helper reads the room's
ProblemType and active JTBD, Brain-derives the framework chain through FEEDS_INTO,
composes it into `/mos:` commands through the resolver, and prints both the
framework chain and the step-numbered command sequence, rendered in Shape B. No
conversational input is required first; the room is the input.

TB-5 is what separates this row from Row 12, and the separation is principled
rather than convenient. pipeline prints a run order and then RUNS it, so the print
is a preview. suggest-next never runs anything: the sequence, with its per-step
FEEDS_INTO reason and confidence and its "what similar ventures did" line, IS the
entire deliverable. Nothing is deferred, so nothing is being previewed.

Proposed `methodology_reframe`, resting on TB-4 as `deck` does, since suggest-next
declares `kind: meta`. Its substance matches the discriminating clause: Larry reads
what the navigator has built and hands back a reasoned reading of where it goes
next. Runner-up `schema_preview` rejected per TB-5.

### Row 17. systems-thinking

First delivery at `commands/systems-thinking.md:116`: the EXECUTE step of the
selector loop runs the picked move's sub-mode and hands over one question or
observation per turn, lens at a time. The OFFER two lines above is a ranked menu
and is not a delivery under S-1, even though the ranking itself is computed locally
from the navigator's own graph state.

One conditional delivery does arrive before the pick and is worth recording: line
112 says a local contradiction is flagged in the intelligence strip and the loop
does NOT auto-advance. When a contradiction exists, that flag is the true first
reward and it is a strong one. It is conditional, so the declaration is written
against the unconditional path.

Proposed `methodology_reframe`. Runner-up `schema_preview` rejected per TB-5: M1's
boundary statement looks like a structural preview, but the move set is a dial the
navigator steers, not a fixed later hunt being previewed.

---

## Part 3 - Evidence and flags

### 3.1 The mechanical scan behind TB-1

Every one of the 17 bodies was scanned for the three flags the scripting override
names. Result: zero hits across all 17 files. `--none (scripting only)` is
therefore forbidden on all 17 by rule, not by opinion, which is the single
strongest guarantee in this document that the 271-03 error cannot recur here.

The near-misses worth naming, because a future classifier will meet them again:
`doctor --json` (an output format, not an interactivity declaration),
`doctor --dry-run` (a mutation guard), `pipeline --from-problem-type` (a chain
selector), `publish --sections` and `--private` (deploy options). None is a
scripting escape hatch.

### 3.2 Remediation flags, recorded not acted on

Binding decision B5 says the linter validates only the DECLARATION and that
per-command flow remediation is follow-up work. These were noticed while reading
and are recorded so they are not lost. None of them changes a proposed value.

| Command | Flag | Line |
|---|---|---|
| compare-ventures | On a sparse room it asks for 2-3 sentences before any reward, which exceeds the rule's one-sentence budget | `commands/compare-ventures.md:70` |
| lean-canvas | A nine-box conversational walk is investment-heavy before the first reward lands | `commands/lean-canvas.md:64` |
| publish | The first user-visible output is a tooling prerequisite message, and the real reward sits behind a CLI install and a login | `commands/publish.md:63` |
| pipeline | The bare invocation reaches a two-option menu and a resume status line before anything is delivered | `commands/pipeline.md:105` |

### 3.3 What this task did NOT do

- Wrote no frontmatter. `git diff --name-only -- commands/ skills/` still lists the
  32 files held from plan 271-03, untouched.
- Minted no vocabulary term. Every proposed value is a member of the live
  `REWARD_TYPES`.
- Touched none of the 30 held path-anchoring edits.
- Made no `gsd-tools` `state.*` or `roadmap.*` call, so the known state-clobber
  defect had no opportunity to fire during this task.

## Navigator ruling

**Date:** 2026-08-28

**All 14 HIGH rows accepted as proposed, including both TB-4-dependent rows.** TB-4's reading
is correct: the term's own definition ("first delivered value is Larry's analysis or reframe
over the user's OWN material") is the actual gate; "conversational methodology command" names
which family typically mints the term, not a hard `body_shape` requirement. `deck` (F.1) and
`suggest-next` (B) both pass the real test -- Larry's reasoning over the navigator's own room
content, not a generic template -- so forcing an exact `body_shape: methodology` label would
be bureaucratic literalism the term's own wording doesn't ask for. Ruled: `deck` =
`methodology_reframe`, `suggest-next` = `methodology_reframe`.

**Row 7, `doctor`: HIGH accepted, no override.** D-2's own sentence ("a status report is not a
reward") settles it, and three independent readings agree. The `--fix` repair path is real but
gated, not the surface a bare-invocation declaration describes. Ruled: `--none (diagnostic
surface)`.

**Row 12, `pipeline`: RULED `schema_preview`, describing the `--from-problem-type` /
`--from-framework` path.** That is the strongest, most structured delivery pipeline has (a
Brain-derived run order, matching the `find-bottlenecks` calibration shape TB-5 already
validated), and the flagged path is the one worth declaring against -- a bare-invocation menu
is not a delivery under S-1 and has no reward of its own to compete for the declaration.
Runner-up `methodology_reframe` stays rejected per the agent's own TB-5 reasoning: at print
time Larry has reasoned over a `ProblemType` token and a framework list, not the navigator's
actual claims.

**Row 13, `publish`: a ninth vocabulary term is minted. This is a real gap in the closed
vocabulary, not a classification error -- do not force any of the eight existing terms onto
it.** The agent's own honest framing is the deciding evidence: `--none (diagnostic surface)`
would be "false about publish in a different direction" -- publish performs an irreversible
action and hands back a real, deployed, shareable artifact, which is categorically NOT
"reporting state, not delivering a variable reward." Filing a knowingly-false-but-least-false
declaration defeats this entire phase's purpose (GUARD-02: "every addition is recorded as a
canon amendment with a written reason", GUARD-10: no gate satisfied by a fabricated-green
answer). `instant_brief` stays rejected -- narrowly bound to the 30-second MVA pipeline by the
linter's own header comment; stretching it dissolves the term the same way a rushed guess
almost dissolved `--none (scripting only)` in 271-03.

**New term: `live_deliverable`** -- "a real, deployed, shareable artifact produced by an
irreversible action (a live URL, a sent file, a filed document), where the navigator could not
have produced it themselves in the available time, understands it instantly on sight, would
show it on someone else's screen, and no general-purpose assistant produces the equivalent."
Same four qualifying tests the agent already confirmed `publish` passes cleanly at
`commands/publish.md:149`. Ruled: `publish` = `live_deliverable`.

**This is a real canon amendment, same weight as wave 1's D-B ruling that minted
`methodology_reframe` and `--none (diagnostic surface)`.** Task 3 must ALSO amend
`REWARD_TYPES` in `lib/core/mva-rule-linter.cjs` (add `live_deliverable` to the frozen Set,
with the same header-comment documentation style the existing seven terms use) and
`docs/reward-before-investment-rule.md` (the term's definition, written above, with the
qualifying-tests citation) -- this was NOT in wave 2's scope and Task 3's plan text may not
anticipate it. Confirm the linter's test suite covers the new term the same way it covers
`methodology_reframe`/`--none (diagnostic surface)` before calling this done.

**Row 15, `show`: RULED -- accept the broadened `--none (diagnostic surface)` reading, but
narrowly, as its own documented sub-case, not a general loosening.** `show` is fundamentally
different from `publish`: it is a pure router by its own file's explicit design ("you do NOT
build any view here"), producing no artifact and reporting no status -- it has no reward of
its own to declare, inheriting whatever its resolved target delivers. That is close enough to
the existing term's spirit (a surface with nothing of its own to report) to reuse rather than
mint a tenth term for a single command. Record this explicitly in the term's documentation as
"covers both a diagnostic status surface AND a pure router with no first-party reward" --
do not let this reading silently expand to justify future opt-outs by resemblance; each future
router-shaped command still needs its own S-1 check, not a rubber stamp from this precedent.

**Final 17 values:**

| Command | Value |
|---|---|
| analyze-systems | `methodology_reframe` |
| causal | `methodology_reframe` |
| challenge-assumptions | `methodology_reframe` |
| compare-ventures | `methodology_reframe` |
| deck | `methodology_reframe` |
| diagnose | `methodology_reframe` |
| doctor | `--none (diagnostic surface)` |
| find-connections | `methodology_reframe` |
| leadership | `methodology_reframe` |
| lean-canvas | `methodology_reframe` |
| mullins | `methodology_reframe` |
| pipeline | `schema_preview` |
| publish | `live_deliverable` |
| score-innovation | `methodology_reframe` |
| show | `--none (diagnostic surface)` |
| suggest-next | `methodology_reframe` |
| systems-thinking | `methodology_reframe` |

Approved. Proceed to Task 3, with the added `REWARD_TYPES`/rule-doc amendment for
`live_deliverable` folded in as described above.

---

## Batch A (plan 267.3-06)

> Phase 267.3, plan 06, Task 1. Applies the Part 1 rubric (ruled) to the next 25 undeclared
> commands: `admin`, `agentshield`, `auto-explore`, `correct-reference-now`, `dashboard`,
> `diagnostics`, `dial-memory-refresh`, `dogfood-flush`, `eureka`, `explain-decision`, `export`,
> `feynman-timeline-refresh`, `funding`, `graph`, `hat-briefing`, `heal`, `help`, `hmi-status`,
> `ingest-methodology`, `intel-pipeline`, `jtbd`, `memory-cortex-reach`, `memory`, `models`,
> `mos`. No frontmatter written in this task. All 25 bodies were grepped for `--no-interactive`,
> `--script` and a standalone `-q`; zero hits, so TB-1 forbids `--none (scripting only)` on
> every one of the 25, mechanically, exactly as it did for the 17 in plan 04.

### The table

| # | Command | hitl | body_shape | First delivery | Proposed | Runner-up (rejected) | Confidence |
|---|---|---|---|---|---|---|---|
| 1 | admin | F.1 | A (Mondrian Board) | `commands/admin.md:132` | `--none (diagnostic surface)` | `methodology_reframe` | HIGH |
| 2 | agentshield | none | E (Action Report) | `commands/agentshield.md:38` | `--none (diagnostic surface)` | `--none (scripting only)` | HIGH |
| 3 | auto-explore | F.3 | methodology | `commands/auto-explore.md:65` | `methodology_reframe` | `schema_preview` | HIGH |
| 4 | correct-reference-now | F.0 | E | `commands/correct-reference-now.md:76` | `--none (diagnostic surface)` | `live_deliverable` | HIGH |
| 5 | dashboard | F.1 | E | `commands/dashboard.md:126` | `--none (diagnostic surface)` | `live_deliverable` | HIGH |
| 6 | diagnostics | F.1 | E | `commands/diagnostics.md:62` | `--none (diagnostic surface)` | `methodology_reframe` | HIGH |
| 7 | dial-memory-refresh | F.0 | E | `commands/dial-memory-refresh.md:57` | `--none (diagnostic surface)` | `--none (scripting only)` | HIGH |
| 8 | dogfood-flush | F.0 | E | `commands/dogfood-flush.md:77` | `--none (diagnostic surface)` | `--none (scripting only)` | HIGH |
| 9 | eureka | F.8 | E (Action Report) | `commands/eureka.md:216` | `methodology_reframe` | `schema_preview` | HIGH (argued in full) |
| 10 | explain-decision | F.1 | F.1 | `commands/explain-decision.md:61` | `--none (diagnostic surface)` | `methodology_reframe` | HIGH |
| 11 | export | F.1 | E (Action Report) | `commands/export.md:68` | `live_deliverable` | `methodology_reframe` | HIGH (argued in full) |
| 12 | feynman-timeline-refresh | F.0 | E | `commands/feynman-timeline-refresh.md:49` | `--none (diagnostic surface)` | `--none (scripting only)` | HIGH |
| 13 | funding | F.8 | B (Semantic Tree) | `commands/funding.md:57` | `--none (diagnostic surface)` | `methodology_reframe` | NEEDS-RULING |
| 14 | graph | F.1 | C (Room Card) | `commands/graph.md:87` | `--none (diagnostic surface)` | `methodology_reframe` | HIGH |
| 15 | hat-briefing | F.8 | C (Dashboard Grid) | `commands/hat-briefing.md:91` | `methodology_reframe` | `--none (diagnostic surface)` | HIGH |
| 16 | heal | F.0 | E (Action Report) | `commands/heal.md:56` | `--none (diagnostic surface)` | `--none (scripting only)` | HIGH |
| 17 | help | F.1 | F.1 | `commands/help.md:33` | `--none (diagnostic surface)` | `schema_preview` | HIGH |
| 18 | hmi-status | F.1 | E (Action Report) | `commands/hmi-status.md:59` | `--none (diagnostic surface)` | `--none (scripting only)` | HIGH |
| 19 | ingest-methodology | F.9 | action-report | `commands/ingest-methodology.md:123` | `--none (diagnostic surface)` | `--none (scripting only)` | HIGH |
| 20 | intel-pipeline | F.1/F.1/F.5 stages | E + F.1 + F.5 | `commands/intel-pipeline.md:99` | `schema_preview` | `methodology_reframe` | HIGH |
| 21 | jtbd | F.1 | E (Action Report) | `commands/jtbd.md:93` | `--none (diagnostic surface)` | `methodology_reframe` | HIGH |
| 22 | memory-cortex-reach | F.1 | F.1 | `commands/memory-cortex-reach.md:57` | `--none (diagnostic surface)` | `methodology_reframe` | NEEDS-RULING |
| 23 | memory | F.8 | E (Action Report) | `commands/memory.md:68` | `--none (diagnostic surface)` | `schema_preview` | HIGH |
| 24 | models | F.1 | C (Information Card) | `commands/models.md:88` | `--none (diagnostic surface)` | `--none (scripting only)` | HIGH |
| 25 | mos | F.1 | E | `commands/mos.md:97` | `--none (diagnostic surface)` | `methodology_reframe` | HIGH |

Tally: 21 HIGH, 2 NEEDS-RULING, and 2 marked HIGH-but-argued-in-full at the plan's own flag
(`eureka`, `export`). `--none (scripting only)` proposed zero times, forbidden 25 times by TB-1
on measured evidence (the grep sweep below). 15 of the 25 propose `--none (diagnostic surface)`
- a high concentration, named here rather than left for the navigator to discover, per the
threat model's own T-267.3-26 mitigation (Task 2 must scan this group and say if it looks
over-applied).

### Row 1. admin

First delivery at `commands/admin.md:132`: the bare-invocation self-teaching panel's "Current
State" block, three counts pulled live from `brain-admin.cjs list` and `brain-admin.cjs
requests` - active keys, pending requests, total requests this month. This is a status roster
of the admin panel's own operational data, the same shape D-2 disqualifies: predictable given
the underlying key store, and it reports state rather than reasoning over the navigator's
venture material.

Proposed `--none (diagnostic surface)`. Runner-up `methodology_reframe` rejected: nothing here
is Larry reasoning over the navigator's OWN venture material: the "material" is the plugin's own
Brain-key registry, an operational surface, not room content.

### Row 2. agentshield

First delivery at `commands/agentshield.md:38`: "Display the skill's Shape E Action Report
directly... Each surface gets one status row: `[clean]`, `[N ambiguous]`, or `[N flagged]`."
The command's own frontmatter already states the case for the class: `hitl_shape: "none"`,
`hitl_why`: "a read-only security scan that reports per-surface status and takes no navigator
decision, so it reaches no genuine fork (mirrors doctor as a deliberately-run diagnostic, but
pure report with no repair gate)." A command that names its own precedent as doctor's Row 7 in
plan 04 needs no further argument to reach the same conclusion.

Proposed `--none (diagnostic surface)`. Runner-up `--none (scripting only)` rejected: zero of
`--no-interactive`, `--script`, `-q` in the body (measured, see the grep sweep). agentshield is
invoked interactively by design (navigator or release pipeline "on demand"), which is the
opposite of a scripting escape hatch.

### Row 3. auto-explore

First delivery at `commands/auto-explore.md:65`: "Return the F.1 dispatch contract inline. The
user picks Explore / Skip / Later via Larry's AskUserQuestion follow-up turn" - carrying the
finding `surfaceFinding` computed at line 55-64 over the file the navigator just wrote. This
command is one of the ten named directly in `267.3-DECISIONS.md` Section 3.2's `methodology_
reframe` class list (`analyze-systems, auto-explore, challenge-assumptions, ...`), so this row
applies an evidence-grounded ruling rather than deriving a fresh one: Larry decomposes the
navigator's own freshly-filed artifact and hands back a domain-decomposition finding before any
further investment.

Proposed `methodology_reframe`, on the D-B class-list's own evidence. Runner-up `schema_preview`
rejected per TB-5: the finding is the whole deliverable of this turn, not a preview of a later
pass; the Explore/Skip/Later pick that follows is what happens to the finding, not a preview of
it.

### Row 4. correct-reference-now

First delivery at `commands/correct-reference-now.md:76`: "Confirm the correction to the user in
one calm line: the reference clock now reads the corrected date, and every time delta Larry
speaks from here is anchored to it." This is a one-line bookkeeping confirmation of a LOCAL
config write (`~/.mindrian/reference-now.json`), reporting that a correction was applied rather
than delivering an unpredictable, valuable finding about the navigator's venture.

Proposed `--none (diagnostic surface)`. Runner-up `live_deliverable` rejected: the term requires
"a real, deployed, shareable artifact" the navigator would show on someone else's screen; a
one-line clock-correction confirmation is neither shareable nor something anyone would look at
twice.

### Row 5. dashboard

First delivery at `commands/dashboard.md:126`: "This is the live Data Room dashboard. Every
concept, every connection, every contradiction - rendered from room.db in real time." The
command opens an existing browser view of content the navigator already filed; it produces no
new analysis of its own, only a live rendering of state that already exists in `room.db`. Body
`hitl_why` names exactly this: "the dashboard offers one next move on what to act on," which is
a report-then-route shape, not a first-party reward.

Proposed `--none (diagnostic surface)`. Runner-up `live_deliverable` rejected, and the
distinction from `export` (Row 11) matters here: `export` hands over a persisted, filed artifact
("Your Data Room snapshot is at `room/exports/hub.html`"); `dashboard` opens an ephemeral local
server process with nothing filed and nothing handed over once the browser tab closes. The term's
own definition requires the artifact "exist and be shareable at the moment the declaration claims
it is delivered" - a running dev server is neither.

### Row 6. diagnostics

First delivery at `commands/diagnostics.md:62`: "This command runs four Wave-1 algorithms
against the active room's embedded artifact corpus and renders a one-screen scalar dashboard:
disruption... coverage... element novelty... and Bayesian surprise." This is the command the
plan's own read_first names explicitly as part of the diagnostic-opt-out group to scan as a
class (`heal, dogfood-flush, hmi-status, dial-memory-refresh, feynman-timeline-refresh,
diagnostics`). The four numbers are a status roster of the room's current fingerprint - "a
one-screen read of whether the room is consolidating or disrupting" - not Larry reasoning over
specific claims the navigator wrote.

Proposed `--none (diagnostic surface)`. Runner-up `methodology_reframe` rejected: the four
scalar metrics are computed, not reasoned; nothing here is Larry's own analysis of the
navigator's material, it is an algorithmic scoreboard, structurally the same shape D-2's "status
report" language names.

### Row 7. dial-memory-refresh

First delivery at `commands/dial-memory-refresh.md:57`: "F.0 Action Report... Three summary rows
+ a PER SECTION breakdown + a NEXT action footer: Sections refreshed: N." Named in the plan's own
diagnostic-opt-out group. This is a regeneration-status report - how many sections were
refreshed, skipped, or failed - not a variable reward about the venture.

Proposed `--none (diagnostic surface)`. Runner-up `--none (scripting only)` rejected: zero
scripting flags in the body (measured), and `hitl_shape: F.0` is a declared decision-close per
TB-2, forbidding the scripting opt-out independently.

### Row 8. dogfood-flush

First delivery at `commands/dogfood-flush.md:77`: "F.0 Action Report: **emitted:** N events
flushed from the queue." Named in the plan's own diagnostic-opt-out group. A drain-and-derive
maintenance action reporting counts (events flushed, section regenerated) - the maintainer's own
internal bookkeeping, not a navigator-facing venture insight.

Proposed `--none (diagnostic surface)`. Runner-up `--none (scripting only)` rejected on the same
two independent grounds as the rest of this group: zero scripting flags measured, and
`hitl_shape: F.0` is a declared human fork per TB-2.

### Row 9. eureka - argued in full, per the plan's flag

First delivery of substance at `commands/eureka.md:216`: the ranked table from `json.ranked` -
rank, domain A, domain B, composite score, weak dimensions, and a tail-flag glyph on any pair the
top-N sort would otherwise bury. The command description states the reward directly: "Rank
cross-domain opportunity pairs and surface the weak-signal tail." This is genuinely the harder
row the plan named it as, because the invocation is fire-and-poll (D-05): `run` starts the scan
detached and returns immediately with only a report path, so the FIRST thing the navigator sees
on a bare `run` is "the scan is running... The report will land at the named path" - a status
line, not the reward itself. The reward only lands once the poll confirms `done` (within the
bounded 15-second window) and Step 3 renders the ranked table, or on a later `/mos:eureka
report`.

The rule doc names the closest analog directly: "the Breakthrough Scan IS a reward." The ranked
cross-domain pairs, drawn from the navigator's OWN room content via tri-modal retrieval, are
exactly find-connections' "aha moments" shape (Row 8 of plan 04's 17: "a surprising structural
analogy is by construction something the navigator could not have produced") applied at
portfolio scale with a weak-signal tail the top-N sort would hide from a naive reading. This
passes all four qualifying tests cleanly: unpredictable (cross-domain co-occurrence the
navigator did not compute), intelligible (a ranked table with named domains), valuable (this is
literally what the command exists to surface), attributable (tri-modal retrieval plus AHP
weighting over the room's own corpus is not something a general assistant reproduces from a
cold room).

Proposed `methodology_reframe`, declared against the completed-scan path (the strongest and most
representative delivery, matching how `doctor`'s Row 7 in plan 04 declared against the bare
invocation rather than the gated `--fix` path). Runner-up `schema_preview` rejected per TB-5:
the ranked table is the whole deliverable of the completed scan, not a preview of a later pass
that still has to run; nothing about the eureka report is deferred once it renders.

### Row 10. explain-decision

First delivery at `commands/explain-decision.md:61`: the per-turn decision-trace block - Tier
Mode, BRAIN.md signal state, the RECOMMENDED marker, the Five-Signal Triangulation breakdown,
the chosen rationale, and the routing source. This is a machine-generated audit trace of Larry's
OWN prior turn, read verbatim from a JSON file the UserPromptSubmit hook already wrote; the
command is "a pure audit lens; never writes back" per its own Canon Part 4 citation. It reports
the mechanics of a PAST recommendation rather than reasoning fresh over the navigator's venture
material.

Proposed `--none (diagnostic surface)`. Runner-up `methodology_reframe` rejected: the trace is
not Larry reasoning over the navigator's OWN material in this turn, it is a rendered log of a
decision the engine already made and recorded; the command adds no new analysis, only visibility
into an existing one.

### Row 11. export - argued in full

First delivery at `commands/export.md:68`: "Your Data Room snapshot is at `room/exports/hub.html`.
Single file - open it in any browser, send it by email, or deploy to Vercel. Everything is
inline." This is the closest sibling `publish.md:149` has in this batch, and it deserves the same
care the navigator gave `publish` in plan 04's ruling. `live_deliverable`'s own written definition
names three shapes: "a live URL, a sent file, a filed document." `export` hands over exactly the
third: a filed, persisted HTML artifact the navigator did not have thirty seconds earlier,
understands on sight (open it in a browser), would show to someone else (it is explicitly
described as shareable by email or deployable), and that a general assistant with no access to
the room's structured content and De Stijl rendering pipeline could not reproduce.

The distinction from `publish` is real and worth naming rather than glossing over: `publish`
deploys the artifact to a LIVE URL (an irreversible network action); `export` only writes the
file to local disk (a reversible local action, `room/exports/hub.html` can be deleted or
regenerated). The `live_deliverable` definition's qualifying language is "a real, deployed,
shareable artifact produced by an irreversible action," and `export`'s action is local and
overwrite-safe, not irreversible in the way a Vercel deploy is. This is the genuine tension: the
artifact-shape test (filed document) passes cleanly; the irreversible-action test is weaker than
`publish`'s.

Proposed `live_deliverable` anyway, HIGH rather than NEEDS-RULING, because the four qualifying
tests from the rule doc itself (not the "irreversible" framing, which is `live_deliverable`'s own
elaboration, not one of the four canonical tests) are what a value must pass, and `export`
passes all four without strain. The "irreversible" language describes WHY `publish`'s case was
strong, not a fifth required test. Runner-up `methodology_reframe` rejected: `export` runs a
generator script over existing room content, it performs no NEW analysis or reframe of the
navigator's material, so the term's own "Larry's analysis or reframe" clause does not fit; the
value here is the produced artifact, not a reasoning turn.

### Row 12. feynman-timeline-refresh

First delivery at `commands/feynman-timeline-refresh.md:49`: "F.0 Action Report... Three summary
rows + a PER SECTION breakdown + a NEXT action footer: Sections refreshed: N." Named in the
plan's own diagnostic-opt-out group; structurally identical to `dial-memory-refresh` (Row 7),
down to the shared Action Report shape the command's own body cites.

Proposed `--none (diagnostic surface)`. Runner-up `--none (scripting only)` rejected on the same
two grounds as the rest of the group: zero scripting flags measured, `hitl_shape: F.0` is a
declared human fork per TB-2.

### Row 13. funding - argued in full, per the plan's flag - NEEDS-RULING

First delivery, on the closest thing to a default path (`list`, the subcommand every other
section's examples lead with) at `commands/funding.md:57`: "Larry displays entries organized by
stage (Discovered > Researched > Applying > Submitted), highlights upcoming deadlines, and flags
stale entries that haven't been updated in 14+ days." `commands/funding.md` has NO explicit
"no subcommand" branch (`grep` for "default" / "no subcommand" / "no argument" returns nothing),
so which path a bare `/mos:funding` actually takes is not written down in this file; `list` is
the closest reading because it is the first subcommand documented and the one every worked
example implies.

**The plan's own flag, checked against the file and found not to hold as stated.** The plan
text argues funding is a strong case because `docs/reward-before-investment-rule.md` names "a
Tavily-sourced funding opportunity surfaced unsolicited" as a worked reward example, calling it
"direct evidence for this exact command." Checked against the actual body: `commands/
funding.md` implements pure CRUD lifecycle tracking (`list`, `create`, `advance`, `status`,
`outcome`) over grant opportunities the navigator has ALREADY promoted from `opportunity-bank`.
Nothing in this file runs a Tavily search or surfaces an opportunity unsolicited; `grep -i
tavily` across the file returns zero hits, and the unsolicited-discovery behavior the rule doc's
worked example describes lives in the `scout`/`opportunity-bank` skills instead (confirmed:
`skills/scout/SKILL.md` and `commands/scout.md` are the files that actually invoke Tavily). The
rule doc's citation is evidence that a funding-domain reward CAN be real; it is not evidence
about what THIS command's own body delivers on invocation, and TB-1's discipline (measure, do
not assume) applies here as much as to a scripting flag.

**What `list` actually delivers, argued honestly.** Stage distribution, deadline pressure, and
staleness flags on the navigator's OWN tracked funding pipeline. This is closer to a status
report of the navigator's own tracked data (D-2's language: predictable given what they already
entered) than to a fresh Larry reframe, but "flags stale entries" and "highlights upcoming
deadlines" is a computed read the navigator did not necessarily do themselves, which pulls
toward `methodology_reframe`.

**What the navigator needs to rule:** whether `list`'s deadline-pressure-plus-staleness read
clears the bar as a genuine, unpredictable finding (`methodology_reframe`), or whether it is
honestly a tracking report of data the navigator already owns (`--none (diagnostic surface)`).
Proposed `--none (diagnostic surface)` as the more conservative, more defensible reading given
the rule doc's cited example does not describe this file's own behavior, but this is a real
judgment call, not a mechanical one, which is why it is NEEDS-RULING rather than asserted HIGH.
Runner-up `methodology_reframe` recorded as the live alternative, not rejected outright.

### Row 14. graph

First delivery at `commands/graph.md:87`: "Your graph has [N] nodes and [M] edges across [K]
sections. Here is what I can help you explore:" followed by a menu of example questions. Per
S-1, this bare-invocation stats line arrives before any query is typed, and it is a status
report of the room's graph size - predictable given what the navigator already filed, not Larry
reasoning over specific claims. The example-questions list beneath it is a menu (S-1 explicitly
excludes menus from counting as a delivery), and the genuine query-answering payoff (graph
traversal, paths, contradictions found) only arrives after the navigator types a question, a
second turn beyond the bare invocation this declaration must describe.

Proposed `--none (diagnostic surface)`. Runner-up `methodology_reframe` rejected: it correctly
describes what `/mos:graph` does once a question is asked, but the declaration must describe the
bare-invocation surface, and that surface is a stats readout, not an analysis.

### Row 15. hat-briefing

First delivery at `commands/hat-briefing.md:91`, the per-hat panel body: "Top Concerns" as a
bullet list, synthesized from the six `HatState` nodes' accumulated cross-session memory. This
consolidates Larry's OWN prior multi-perspective analysis (six hat personas, each with
persistent memory across sessions) of the navigator's OWN venture material into one briefing -
"pulls the hat memory into one briefing," per the command's own `teaching` field. `kind:
methodology`, `frameworks: ["Six Thinking Hats"]` - squarely inside the family the term was
minted against, and its substance matches even more directly than `deck` or `suggest-next` did:
the delivered content is literally Larry's accumulated reasoning over the navigator's own room,
synthesized fresh at request time.

Proposed `methodology_reframe`. Runner-up `--none (diagnostic surface)` rejected: this is not a
system-operational status roster (no key counts, no drift check), it is a synthesis of
methodology analysis Larry already ran on the navigator's OWN material, consolidated into
tensions and opportunities - the reward IS the synthesis, not a report of internal plugin state.

### Row 16. heal

First delivery at `commands/heal.md:56`: the deprecation note, then "proceed with /mos:doctor
--heal-room behavior... The user sees ONE deprecation note + the doctor heal output." Named in
the plan's own diagnostic-opt-out group, and the plan's read_first text names `heal` directly as
the worked example: "A command like `heal` or `dogfood-flush` that runs code but has no
scripting flag and no variable reward takes the diagnostic opt-out term from D-B." `heal`'s own
canonical target, `doctor`, was already ruled `--none (diagnostic surface)` in plan 04's Row 7 on
the same D-2 disqualifier; a soft-alias stub that forwards verbatim to an already-ruled surface
inherits the same answer rather than re-arguing it.

Proposed `--none (diagnostic surface)`. Runner-up `--none (scripting only)` rejected: zero
scripting flags measured in the body, and `hitl_shape: F.0` is a declared human fork per TB-2.

### Row 17. help

First delivery at `commands/help.md:33`: "Bare `/mos:help` renders **Card 1** as a single
AskUserQuestion call with up to 4 questions - one question per Card-1 family." Under S-1 a menu
or selector is explicitly NOT a delivery, and `/mos:help` is, by design, nothing but a selector:
11 command families rendered as 3 sequential AskUserQuestion cards, each option itself a command
name plus its one-line `help_jtbd`. Every reward `/mos:help` appears to hand over belongs to
whichever command the navigator eventually picks and runs - the exact "inherited reward, no
first-party value of its own" shape the `show` ruling (plan 04, Row 15) already named and the
canon doc's router sub-case already covers.

Proposed `--none (diagnostic surface)`, reusing the router sub-case exactly as `show` did.
Runner-up `schema_preview` rejected: the family cards are not a preview of extraction or of a
later hunt still to run, they are a navigation menu over commands that already exist; reading a
menu as a "preview" would, as the router sub-case's own text warns, expand this reading to cover
every selector in the product.

### Row 18. hmi-status

First delivery at `commands/hmi-status.md:59`: "Pass through doctor's --ui-compliance output
verbatim. Read-only by design (per the original hmi-status contract)." Named in the plan's own
diagnostic-opt-out group; structurally identical to `heal` (Row 16) - a soft-alias stub
forwarding to `doctor`'s already-ruled UI Ruling System compliance scan, itself a status roster
(compliant/non-compliant surfaces) rather than a reward.

Proposed `--none (diagnostic surface)`. Runner-up `--none (scripting only)` rejected: zero
scripting flags measured, and `hitl_shape: F.1` is a declared human fork per TB-2 (the `F.0`/
`F.1` prohibition applies to both, not only `F.0`).

### Row 19. ingest-methodology

First delivery at `commands/ingest-methodology.md:123`: "## Output. An action report: the gate
result, what was written to Neo4j and Pinecone (with a `brain_search` retrieval check), the
trigger/connector wiring, the registration triad, and the local-regen `--check` results." The
command's own section header names its output an "action report" verbatim. This is a
maintainer-only admin pipeline (`disable-model-invocation: true`, `autonomous_safe: false`) whose
declared output is a fixed-order walk's completion status across seven steps, not a navigator-
facing venture insight; nothing here reasons over a navigator's OWN room material at all, since
the spec being ingested "must be GENERIC teaching knowledge - never a specific venture's data"
(Canon Part 8, stated in the file itself).

Proposed `--none (diagnostic surface)`. Runner-up `--none (scripting only)` rejected: zero
scripting flags measured; `hitl_shape: F.9` is a fixed-order walk, and while TB-2 names F.0/F.1
specifically, the same logic holds here since the command explicitly halts at boundary and
registration gates for a human maintainer, the opposite of a silent script.

### Row 20. intel-pipeline

First delivery at `commands/intel-pipeline.md:99`: "Render the fan-approve card carrying the
dimensions, the planned pass count N, and the budget." This is the second of three ordered
gates (`calibrate`, `plan-fan`, `synthesize`); the first gate (`calibrate`) only echoes the
navigator's OWN already-stated JTBD back for confirmation, which S-1 treats as a confirmation
gate, not a delivery. The fan-approve card is where the command first hands over something IT
computed: the derived research dimensions and the sized fan, a structure the command will then
go on to DISPATCH and EXECUTE as research passes once approved.

Proposed `schema_preview`, matching the `pipeline` calibration shape from plan 04's Row 12 (TB-5:
"a structure the command will then go on to EXECUTE... a promise about work to come"). Runner-up
`methodology_reframe` rejected on the same ground plan 04 used for `pipeline`: at fan-plan time
Larry has reasoned over JTBD-derived research dimensions, not yet over the substance of any
claim the navigator has made, so calling it an analysis of the navigator's own material would
overstate it.

### Row 21. jtbd

First delivery at `commands/jtbd.md:93`: "Renders Shape E with the current JTBD, confidence,
evidence chain, and last 5 history entries." A state readout of the classifier's OWN inferred
signal (which job the navigator appears to be doing right now) - predictable given the room's
recent activity, and it reports the tool's internal state rather than reasoning over the
navigator's venture material.

Proposed `--none (diagnostic surface)`. Runner-up `methodology_reframe` rejected: the JTBD
classifier's inferred label plus confidence score is closer to `/mos:status` than to an analysis
- it tells the navigator what the SYSTEM currently believes about them, not a fresh reading of
their material.

### Row 22. memory-cortex-reach - NEEDS-RULING

First delivery, read carefully, at `commands/memory-cortex-reach.md:57`: "When the navigator
APPROVES at the Decision Gate, the orchestrator fires the cross-room memory-cortex bridge: it
surfaces the projected cortex neighborhood around the stale thought or the fresh contradiction."
This is the same structural problem plan 04's Row 15 (`show`) named: the command's OWN body only
fires a Decision Gate; the actual substance (which governing thought went stale, what the fresh
contradiction is) is surfaced by "the orchestrator," a separate dispatch mechanism, only AFTER
the navigator approves. The sensor that triggers this reach reads "ONLY... a stale-governing-
thought freshness flag and a fresh-contradiction COUNT" (scalar signals) and explicitly "does not
produce" the cortex content itself - it "carries only enum/scalar handles, never cortex prose."

**The genuine tension.** If the Decision Gate itself names the specific stale thought or
contradiction (plausible: a gate cannot ask the navigator to approve reaching into an unnamed
signal, so it likely surfaces at least a one-line description), this row resembles `causal`
(plan 04 Row 2) or `challenge-assumptions` (Row 3): the finding is already computed and on
screen, only the deeper WRITE/read is deferred, which is `methodology_reframe` territory per
TB-5. If the gate is scalar-only - a bare "your governing thought has gone stale" or "N
contradictions landed" with no content of its own - this is the `show` pattern exactly: every
reward it appears to deliver is its target's (the orchestrator's) reward, and the vocabulary has
no term for an inherited reward, which is `--none (diagnostic surface)`'s router sub-case.
`commands/memory-cortex-reach.md`'s own body does not show the literal gate copy, so this cannot
be settled by reading the file alone.

Proposed `--none (diagnostic surface)`, on the reading that the file's own language ("the
orchestrator fires the bridge" AFTER approval, and the sensor "does not produce" the content)
places the actual reward one hop away from this command's own body, matching `show`'s inherited-
reward shape more closely than the on-screen-analysis shape of `causal`/`challenge-assumptions`.
Runner-up `methodology_reframe` recorded as the live alternative if the navigator reads the gate
copy as already naming the specific finding.

### Row 23. memory

First delivery at `commands/memory.md:68`: "Overview: current within-session JTBD + counts of
in_flight / parked / completed across all rooms." A cross-layer state readout (within-session,
across-session, cross-room counts) - "Inspect and operate on the three memory layers" per the
command's own description. This is a census of what the system currently holds, not an analysis
of it.

Proposed `--none (diagnostic surface)`. Runner-up `schema_preview` rejected: nothing here is a
preview of extraction still to come, the counts ARE the whole delivered content of the default
overview.

### Row 24. models

First delivery at `commands/models.md:88`: the resolved profile table - "Room:", "Profile:",
"Stage:", followed by the formatted per-agent routing table from `model-profiles.cjs`. A
configuration-state readout (which model routes which agent) - useful, but predictable given the
room's own `.config.json`, and it reports the plugin's OWN operator configuration rather than
reasoning over the navigator's venture.

Proposed `--none (diagnostic surface)`. Runner-up `--none (scripting only)` rejected: zero
scripting flags measured in the body.

### Row 25. mos

First delivery at `commands/mos.md:97`: "emit a single Action Report envelope... Routing to:
<route> (<reason>)." The command's own doctrine section states the case for the class directly:
"/mos:mos does NOT add a new behavior. It dispatches to /mos:onboard, /mos:status, or
/mos:suggest-next. The router replaces the mental cost; the underlying surfaces are unchanged."
This is the `show`/`help` router shape stated even more explicitly than either of those two: a
pure dispatcher over three EXISTING commands, with zero net-new behavior of its own by the
file's own admission.

Proposed `--none (diagnostic surface)`, reusing the router sub-case a third time in this batch
(after `help` and, pending ruling, `memory-cortex-reach`). Runner-up `methodology_reframe`
rejected: the routing REASON ("mostly-empty room" or "populated room") is a state classification,
not Larry reasoning over the navigator's own claims, and the actual reward belongs entirely to
whichever of `onboard`/`status`/`suggest-next` the router lands on.

### Note on the concentration of diagnostic opt-outs

**Corrected count.** The table above proposes `--none (diagnostic surface)` on 18 of the 23 HIGH
rows (`admin`, `agentshield`, `correct-reference-now`, `dashboard`, `diagnostics`,
`dial-memory-refresh`, `dogfood-flush`, `explain-decision`, `feynman-timeline-refresh`, `graph`,
`heal`, `help`, `hmi-status`, `ingest-methodology`, `jtbd`, `memory`, `models`, `mos`), PLUS both
NEEDS-RULING rows (`funding`, `memory-cortex-reach`) propose the same value - so if both rulings
land as proposed, the final count is **20 of 25**. An earlier draft of this note said "15 of 25,"
which underrecounted; corrected here before Task 2 rather than left standing.

This is noticeably higher than plan 04's 9-of-17, and it is named here rather than left for the
navigator to discover, per the threat model's own T-267.3-26 mitigation (Task 2 must scan this
group and say if it looks over-applied). The honest explanation is batch composition, not a
rubber stamp: this batch is weighted toward the plugin's OWN admin, lifecycle, and audit
surfaces (key management, dogfood queues, timeline/dial refresh, HMI compliance, model routing,
JTBD state, memory-layer inspection) rather than its methodology-command family, and D-2's
"status report is not a reward" disqualifier applies more often to a batch shaped this way by
construction. The two commands that most directly ADD analytical value over the navigator's own
material - `auto-explore` and `hat-briefing` - and the one that surfaces the room's OWN
cross-domain analysis - `eureka` - all cleared the bar for `methodology_reframe` instead.

### Batch A navigator ruling

**Date:** 2026-08-28

**Both NEEDS-RULING rows accepted as proposed, no override.**

- **Row 13, `funding`: RULED `--none (diagnostic surface)`.** The agent's own proposed reading
  accepted as-is. `commands/funding.md`'s actual body is pure CRUD lifecycle tracking with zero
  Tavily calls; the rule doc's "Tavily-sourced funding opportunity surfaced unsolicited" worked
  example describes the `scout`/`opportunity-bank` skills, not this command, and TB-1's
  measure-don't-assume discipline holds here as it does for a scripting flag. The `list`
  subcommand's stage/deadline/staleness read is a tracking report of data the navigator already
  owns, matching D-2's disqualifier rather than a fresh Larry reframe. Runner-up
  `methodology_reframe` stays rejected.

- **Row 22, `memory-cortex-reach`: RULED `--none (diagnostic surface)`.** The agent's own
  proposed reading accepted as-is. The command's own body only fires a Decision Gate; the
  sensor that triggers it carries scalar/enum signals only ("does not produce" cortex prose),
  and the actual cortex neighborhood is surfaced by "the orchestrator" only after approval - the
  same inherited-reward shape plan 04's `show` ruling already named and this doc's router
  sub-case already covers. Runner-up `methodology_reframe` stays rejected.

**No HIGH row overridden.** All 23 HIGH-confidence proposals (including the two argued-in-full
rows, `eureka` = `methodology_reframe` and `export` = `live_deliverable`) stand exactly as
proposed. The "argued in full" flag on those two rows was documentation transparency per the
plan's own instruction, not a second ruling request, and no re-opening was asked for or needed.

**Final 25 values, the only subsection Task 3 reads from:**

| Command | Value |
|---|---|
| admin | `--none (diagnostic surface)` |
| agentshield | `--none (diagnostic surface)` |
| auto-explore | `methodology_reframe` |
| correct-reference-now | `--none (diagnostic surface)` |
| dashboard | `--none (diagnostic surface)` |
| diagnostics | `--none (diagnostic surface)` |
| dial-memory-refresh | `--none (diagnostic surface)` |
| dogfood-flush | `--none (diagnostic surface)` |
| eureka | `methodology_reframe` |
| explain-decision | `--none (diagnostic surface)` |
| export | `live_deliverable` |
| feynman-timeline-refresh | `--none (diagnostic surface)` |
| funding | `--none (diagnostic surface)` |
| graph | `--none (diagnostic surface)` |
| hat-briefing | `methodology_reframe` |
| heal | `--none (diagnostic surface)` |
| help | `--none (diagnostic surface)` |
| hmi-status | `--none (diagnostic surface)` |
| ingest-methodology | `--none (diagnostic surface)` |
| intel-pipeline | `schema_preview` |
| jtbd | `--none (diagnostic surface)` |
| memory-cortex-reach | `--none (diagnostic surface)` |
| memory | `--none (diagnostic surface)` |
| models | `--none (diagnostic surface)` |
| mos | `--none (diagnostic surface)` |

Final tally: 20 `--none (diagnostic surface)`, 3 `methodology_reframe`, 1 `live_deliverable`,
1 `schema_preview`. Zero `--none (scripting only)`, forbidden on all 25 by TB-1 on measured
evidence.

Approved. Proceed to Task 3.

---

## Batch B (plan 267.3-07)

> Phase 267.3, plan 07, Task 1. Applies the Part 1 rubric (ruled) to the final 25 undeclared
> commands: `mva-report`, `new-surface`, `operator`, `opportunities`, `organize`, `present`,
> `qualify-opportunity`, `query`, `reanalyze`, `research`, `room`, `rs-explain`, `rs-fetch`,
> `rs-thesis`, `scheduled-tasks`, `skill`, `snapshot`, `speakers`, `splash`, `stance`, `status`,
> `update`, `visualize`, `whitespace`, `wiki`. This closes the whole-tree audit to zero missing
> if all 25 land. No frontmatter written in this task. All 25 bodies were grepped for
> `--no-interactive`, `--script` and a standalone `-q`; zero hits (measured, not assumed), so
> TB-1 forbids `--none (scripting only)` on every one of the 25, mechanically, exactly as it did
> for the 17 in plan 04 and the 25 in plan 06.

### The table

| # | Command | hitl | body_shape | First delivery | Proposed | Runner-up (rejected) | Confidence |
|---|---|---|---|---|---|---|---|
| 1 | mva-report | none | E | `commands/mva-report.md:34` | `methodology_reframe` | `--none (diagnostic surface)` | HIGH |
| 2 | new-surface | F.0 | E | `commands/new-surface.md:111` | `schema_preview` | `live_deliverable` | HIGH |
| 3 | operator | F.1 | E (Action Report) | `commands/operator.md:78` | `--none (diagnostic surface)` | `methodology_reframe` | HIGH |
| 4 | opportunities | F.8 | E (Action Report) | `commands/opportunities.md:75` | `--none (diagnostic surface)` | (no clean existing term) | NEEDS-RULING (argued in full) |
| 5 | organize | F.8 | B (Semantic Tree) | `commands/organize.md:58` | `--none (diagnostic surface)` | `schema_preview` | HIGH |
| 6 | present | F.1 | E (Action Report) | `commands/present.md:87` | `live_deliverable` | `--none (diagnostic surface)` | HIGH |
| 7 | qualify-opportunity | F.1 | E (Action Report) | `commands/qualify-opportunity.md:58` | `methodology_reframe` | `schema_preview` | HIGH |
| 8 | query | F.1 | D | `commands/query.md:54` | `--none (diagnostic surface)` | `schema_preview` | HIGH |
| 9 | reanalyze | F.8 | E (Action Report) | `commands/reanalyze.md:93` | `methodology_reframe` | `--none (diagnostic surface)` | HIGH |
| 10 | research | F.8 | C | `commands/research.md:256` | `methodology_reframe` | (no clean existing term) | NEEDS-RULING (argued in full) |
| 11 | room | F.1 | C | `commands/room.md:121` | `--none (diagnostic surface)` | `methodology_reframe` | HIGH |
| 12 | rs-explain | F.1 | E (Action Report) | `commands/rs-explain.md:70` | `methodology_reframe` | `schema_preview` | HIGH |
| 13 | rs-fetch | F.2 + F.9 stages | E (Action Report) | `commands/rs-fetch.md:58` | `methodology_reframe` | `schema_preview` | HIGH |
| 14 | rs-thesis | F.9 | E (Action Report) | `commands/rs-thesis.md:55` | `--none (diagnostic surface)` | `methodology_reframe` | HIGH |
| 15 | scheduled-tasks | F.1 | E (Action Report) | `commands/scheduled-tasks.md:290` | `--none (diagnostic surface)` | `methodology_reframe` | HIGH (structural caveat) |
| 16 | skill | F.0 | E | `commands/skill.md:100` | `schema_preview` | `live_deliverable` | HIGH |
| 17 | snapshot | F.0 | E | `commands/snapshot.md:148` | `live_deliverable` | `methodology_reframe` | HIGH |
| 18 | speakers | F.1 | C (Room Card) | `commands/speakers.md:103` | `methodology_reframe` | `--none (diagnostic surface)` | HIGH |
| 19 | splash | F.1 | E | `commands/splash.md:39` | `--none (diagnostic surface)` | `reframe_question` | HIGH |
| 20 | stance | F.0 | F.0 | `commands/stance.md:73` | `--none (diagnostic surface)` | (none plausible) | HIGH |
| 21 | status | F.1 | E (Action Report) | `commands/status.md:51` | `--none (diagnostic surface)` | `--none (scripting only)` | HIGH |
| 22 | update | F.0 | E | `commands/update.md:58` | `--none (diagnostic surface)` | `--none (scripting only)` | HIGH |
| 23 | visualize | F.1 | D | `commands/visualize.md:55` | `--none (diagnostic surface)` | `schema_preview` | HIGH |
| 24 | whitespace | F.8 | methodology | `commands/whitespace.md:152` | `methodology_reframe` | `--none (diagnostic surface)` | NEEDS-RULING (argued in full) |
| 25 | wiki | F.1 | D | `commands/wiki.md:48` | `--none (diagnostic surface)` | `live_deliverable` | HIGH |

Tally: 22 HIGH, 3 NEEDS-RULING (`opportunities`, `research`, `whitespace`). `--none (scripting
only)` proposed zero times, forbidden 25 times by TB-1 on measured evidence (the grep sweep
below). 13 of the 25 propose `--none (diagnostic surface)`, 8 propose `methodology_reframe`, 2
propose `live_deliverable`, 2 propose `schema_preview` (subject to the NEEDS-RULING rows moving
either way).

### The mechanical scan behind TB-1 (Batch B)

All 25 bodies were scanned in one pass for `--no-interactive`, `--script` and a standalone
`-q`. Zero hits across all 25 files:

```
grep -lE -- "--no-interactive|--script|(^|[^a-zA-Z])-q([^a-zA-Z]|$)" commands/mva-report.md \
  commands/new-surface.md commands/operator.md commands/opportunities.md commands/organize.md \
  commands/present.md commands/qualify-opportunity.md commands/query.md commands/reanalyze.md \
  commands/research.md commands/room.md commands/rs-explain.md commands/rs-fetch.md \
  commands/rs-thesis.md commands/scheduled-tasks.md commands/skill.md commands/snapshot.md \
  commands/speakers.md commands/splash.md commands/stance.md commands/status.md \
  commands/update.md commands/visualize.md commands/whitespace.md commands/wiki.md
# (no output)
```

`--none (scripting only)` is therefore forbidden on all 25 by rule, not by opinion.

### Row 1. mva-report

First delivery at `commands/mva-report.md:34`: the "Report structure" template Larry fills with
a genuine read-back of the conversation just had - the frames that were live, a per-turn move
table (ask / tell / grill / reframe / elevate with the fired Shape-F gate), a verdict on whether
the conversation reached a conclusion or circled, whether a cross-frame connection was offered
(hedged or confident), and the open questions left unresolved. This is not a transcript (the
file itself says so at line 25: "Do NOT reproduce 'Larry said X, you said Y' banter"); it is a
judged synthesis over the conversation that just happened - naming whether it converged or
looped, whether a hedge was correctly hedged or a tone slip, which is Larry's own reasoning
about the interaction, not a rendered log of it.

Proposed `methodology_reframe`. The "material" the analysis runs over is the conversation
itself (both what the navigator said and what Larry said), which the navigator supplied by
having the conversation; the reward is Larry's judged read-back of it before any further
investment. Runner-up `--none (diagnostic surface)` rejected: this is not an operational status
roster of plugin state, it is a synthesized verdict (converged vs circled, hedge quality,
missed connections) requiring genuine judgment, closer to `auto-explore`'s domain-decomposition
finding (batch A) than to `explain-decision`'s rendered log of a past machine decision.

### Row 2. new-surface

First delivery at `commands/new-surface.md:111`: after Step 1 collects the full 11-key surface
spec (kind, name, reach_id, sub_mode, framework, posture, hierarchy_rank, sensor_triggers,
filing, plan_gated, web_scope - a genuinely large investment, itself an MVA-rule flow issue
recorded but not fixed here per B5), Step 4 reports the emitted path, that the surface is
registered in `connector-registry.json`, and that the manifest digest now reflects it. Critically,
line 114 says the next move is to "edit the emitted .md body to add the surface's actual
behavior. The wiring is done; only the body is now hand-written" - the emitted file is a
correctly-wired SKELETON, not a complete artifact.

Proposed `schema_preview` per TB-5: the emitted surface is "a structure the command will then go
on to fill" (by hand, immediately after), which is a promise about a surface to come, not the
analysis itself. Runner-up `live_deliverable` rejected: the term's own exclusion list names "a
command that merely writes a file" as NOT covered, and this file's own body says only the wiring
is done - the surface is not yet a complete, consumable artifact the way `export`'s hub.html or
`publish`'s live URL are.

### Row 3. operator

First delivery at `commands/operator.md:78`, the bare-invocation example output: current
operator state (`BUILD_ROOM`), when it was entered, the previous state, and the last 5 history
entries. This is a state readout of the conversation-state-machine's OWN internal value -
predictable given how the navigator has been interacting, and `hitl_why` says outright "the
operator view offers one next move on the current state," the same report-then-route shape as
`jtbd` and `dashboard` in batch A.

Proposed `--none (diagnostic surface)`. Runner-up `methodology_reframe` rejected: nothing here
is Larry reasoning over the navigator's venture material; it is a readout of which of five
named states the rendering engine currently believes it is in.

### Row 4. opportunities - argued in full, per the plan's flag - NEEDS-RULING

`commands/opportunities.md` has no documented "no subcommand" branch (argument-hint lists
`[list|add|scan]` but the file's own subcommand sections only document `scan`, `list`, `file`,
and `bank` - `add` is named in the hint but never documented). This is the same undocumented-
default problem plan 06's `funding` row hit, and it cuts two ways here rather than resolving
cleanly toward one reading the way `funding` did.

**Reading A (conservative, mirrors the `funding` precedent).** If the closest reading of a bare
invocation is `list` (the first subcommand named in the argument-hint, matching the convention
`funding`'s ruling already established), the first delivery is filed opportunities the navigator
already banked, organized by Knight position and confidence - a report of data the navigator
already owns, matching D-2's disqualifier exactly the way `funding`'s `list` did. This reading
proposes `--none (diagnostic surface)`.

**Reading B (the plan's own flag).** The command's own header frames it, before any subcommand
section, as "Context-driven grant discovery. Larry reads your room, generates search queries,
and presents opportunities for you to confirm or reject" - discovery-first framing that does not
appear in `funding.md`. The `scan` subcommand runs a LIVE search against Grants.gov and Simpler
Grants, generates queries from the room's own domain/geography/stage, and returns "I found {N}
opportunities relevant to your venture" with a relevance-scored table - this is structurally the
rule doc's own worked example almost verbatim: "A Tavily-sourced funding opportunity surfaced
unsolicited IS a reward." It passes all four qualifying tests cleanly (unpredictable: a live
external search the navigator did not run themselves; intelligible: a scored table; valuable:
real funding leads; attributable: context-driven query generation + relevance scoring against
the room's own domain, not a generic search). But none of the eight prior terms name this shape
honestly: it is not `methodology_reframe` (the reward is fresh EXTERNAL discovery, not Larry
reasoning over material the navigator already brought), not `live_deliverable` (nothing is filed
or deployed; it is a conversational table pending confirm/reject), and clearly not a status
report of existing state.

**What the navigator needs to rule:** which subcommand the bare-invocation declaration
describes, and if it is `scan`, whether the closed vocabulary needs a tenth term for "a live,
unsolicited external discovery surfaced and matched against the room's own context" (the same
class of gap that minted `live_deliverable` for `publish`) or whether `--none (diagnostic
surface)`'s broadened "no first reward of its own" reading should be read narrowly enough to
exclude this row. Proposed `--none (diagnostic surface)` as the more conservative, more
defensible reading given the undocumented default (mirroring `funding`'s ruled precedent), but
this is a real judgment call, not a mechanical one.

### Row 5. organize

First delivery at `commands/organize.md:58`: "The user sees ONE deprecation note + the rooms
organize output (Shape B Semantic Tree)." `commands/organize.md` is a soft-alias stub (D-09,
Phase 121.5-08) that runs `soft-alias-runner.cjs` and passes through `/mos:rooms organize`'s
output verbatim (line 60: "Pass through /mos:rooms organize's output... verbatim"). This is the
same router shape `show` (plan 04, Row 15), `help`, and `mos` (batch A, Rows 17 and 25) already
established: every reward this command appears to deliver belongs to its redirect target.

Proposed `--none (diagnostic surface)`, reusing the router sub-case a fourth time. Runner-up
`schema_preview` rejected: the redirected output is not a preview of a later pass, it is the
target command's own complete deliverable, merely relayed.

### Row 6. present

First delivery at `commands/present.md:87`: "Your room is now a visual dashboard your investors
can browse. Six views ready" followed by the six generated views (Dashboard, Wiki, Deck,
Insights, Diagrams, Graph). Unlike `dashboard` (batch A, Row 5, ruled `--none (diagnostic
surface)` because it opens an ephemeral live server over `room.db` with nothing persisted),
`present` runs `generate-presentation.cjs` to produce actual self-contained HTML files and then
serves them; Step 5 says explicitly "Every view is self-contained HTML - you can send the files
to anyone." This is the persisted-artifact distinction the `dashboard` ruling itself drew against
`export`: `present` generates real files, not just a live rendering of `room.db`.

Proposed `live_deliverable`, following the `export` precedent directly (plan 04, Row 11):
the four qualifying tests pass without strain (a navigator cannot hand-generate six rendered
views in 30 seconds; the views are self-explanatory; investors would look at them; the rendering
pipeline is MindrianOS-unique), and the "irreversible action" framing is `live_deliverable`'s own
elaboration, not a fifth required test, per the `export` ruling's own reasoning. Runner-up
`--none (diagnostic surface)` rejected on the `dashboard`-distinction: `present` persists real
files unlike `dashboard`'s ephemeral live view.

### Row 7. qualify-opportunity

First delivery at `commands/qualify-opportunity.md:58`: the F.1 card's rubric verdict lines - the
compressed Q1..Q8 human checks (friction, connection, surprise, timing, actor fit, definability,
newness, desirability) - plus the component readiness lines (critic gate, compression, portfolio,
tail flag, evidence readiness, follow-through readiness), shown BEFORE the verb picker. Per the
file's own words (line 60): "Pedagogy over verdict (Canon Part 12): you see the reasoning, not
just a score." This is the same shape TB-5 already resolved for `causal` (plan 04, Row 2): the
analysis (WHY the candidate qualified) is already computed and on screen; what the gate defers
is only the WRITE (Qualify+file to the bank), not the analysis itself. The verb picker
(Qualify/Ask Brain/Rephrase/Suggest next/Skip) that follows is the excluded menu under S-1; the
rubric verdict content above it is the delivery.

Proposed `methodology_reframe`. Runner-up `schema_preview` rejected per TB-5, the same ground
`causal` was rejected on: the rubric verdict analysis is the whole deliverable of this turn; only
the confirm-to-file action is deferred, not the reasoning behind it.

### Row 8. query

First delivery at `commands/query.md:54`: "The user sees ONE deprecation note + the graph
traversal output." Deprecated soft-alias stub (D-09) redirecting to `/mos:graph`, passing through
its response verbatim (line 56). Identical router shape to `organize` above and to `show` /
`help` / `mos`.

Proposed `--none (diagnostic surface)`, router sub-case. Runner-up `schema_preview` rejected: the
relayed graph-traversal output is the target's complete deliverable, not a preview.

### Row 9. reanalyze

First delivery at `commands/reanalyze.md:93`: the before/after delta ("Convergence signals:
[before] -> [after] ([+/-] new)" etc.) followed immediately by the narrated highlight of what
changed - "After looking at everything again, here is what stands out now that was not visible
before" (line 105) - naming specific new convergence signals, action items, and contradictions
discovered by re-running the meeting-intelligence cascade across ALL filed meetings. This is
qualitatively different from `dial-memory-refresh` or `feynman-timeline-refresh` (batch A, both
`--none (diagnostic surface)`, pure regeneration-status counts with no content): `reanalyze`
surfaces genuinely NEW cross-meeting patterns the navigator did not have before, over meetings
they themselves filed.

Proposed `methodology_reframe`. Runner-up `--none (diagnostic surface)` rejected: unlike the
maintenance-regen commands, the delta is not bookkeeping, it is fresh cross-meeting intelligence
(new contradictions, new convergences) computed over the navigator's own filed material - Decision
#11's "meetings are primary knowledge" made newly visible.

### Row 10. research - argued in full, per the plan's flag - NEEDS-RULING

`/mos:research <topic>` takes one sentence of investment (the topic, within the rule doc's own
"typing one sentence about the venture" non-violation budget; line 220-221 asks for it explicitly
if omitted). Stage 1-3 surfaces a Larry-voiced context summary framed against the room ("Speaking
to your investor lens... I will research <topic> against THIS context"), Stage 4 fetches live web
sources (Tavily Extract / OpenAlex / PubMed, cache-first), and Stage 5 presents the top-5 findings
at `commands/research.md:256` with title, summary, source, evidence tier, AND "the pre-mapped
candidate room location(s) with a % match score against each section's existing claim graph."
Line 264-265: "Every finding connects to the venture context the summary named."

**The case for `methodology_reframe`.** This mirrors `compare-ventures`'s precedent (plan 04, Row
4) more closely than it first appears: `compare-ventures` blended EXTERNAL Brain co-occurrence
data with the navigator's own framework profile and was ruled `methodology_reframe` because
"Larry reasons over what the navigator brought and hands back a reading of it." `research`
similarly frames every external finding against the room's own existing claim graph (the %
match score), so the external material is delivered ALREADY APPLIED to the navigator's own
venture, not handed over raw.

**The genuine tension, stated honestly.** Unlike `compare-ventures`'s Brain co-occurrence data
(a MindrianOS-exclusive moat asset), `research`'s underlying content is open web search results
that a general assistant with web access could also retrieve for the SAME topic - test 4
("uniquely attributable to MindrianOS") holds only via the room-context-aware query generation
and the claim-graph matching, not via the content itself. This is close enough to the
`opportunities` row's tension that it deserves the same honest flag rather than a forced HIGH.

Proposed `methodology_reframe` (the compare-ventures precedent is the stronger analog than any
diagnostic or preview reading), but marked NEEDS-RULING because the "uniquely attributable"
test rests on contextualization rather than content, which is a real judgment call for the
navigator to confirm or reject. Runner-up: no existing term cleanly names "a live, contextualized,
unsolicited external finding" if `methodology_reframe`'s reading is rejected - the same gap named
in Row 4's `opportunities` tension.

### Row 11. room

First delivery at `commands/room.md:121`, the default `overview` subcommand's Shape B Semantic
Tree: the section folder tree with entry counts and status glyphs, followed by "a brief
Larry-voice observation about what's strong or missing" and a conditional Zone 3 Intelligence
Strip (up to 3 pre-computed proactive signals). The command's own `hitl_why` (line 8) states
"Room navigation offers one next move from the current room" - the same report-then-route shape
as `dashboard` (batch A) and `graph` (batch A, Row 14): a structural readout of what the
navigator has already filed, not fresh reasoning over specific claims.

Proposed `--none (diagnostic surface)`. Runner-up `methodology_reframe` rejected: the tree plus
observation is predictable given what is already filed (a structure readout), and the
Intelligence Strip signals are pre-computed by `room-proactive` elsewhere, not fresh analysis
performed by this command at invocation; the declaration is written against the unconditional
path (no signals), matching the convention used for `systems-thinking`'s conditional flag in
plan 04.

### Row 12. rs-explain

First delivery: the Larry-voiced NL explanation rendered at `commands/rs-explain.md:70` -
"Raw data becomes insight" - grounded by triangulated queries across room.db, Aura, and the
methodology Brain, translating a navigator's natural-language question into a synthesized
explanation ("Found 3 reverse salients across 2 domains. The strongest is <top thesis>," line
130). The NL question itself is a one-sentence investment (within budget); what follows is
Larry's fresh synthesis across the navigator's own room content plus cross-graph triangulation.

Proposed `methodology_reframe`. Runner-up `schema_preview` rejected per TB-5: the rendered
explanation is the whole deliverable of this turn, not a preview of a later extraction pass.

### Row 13. rs-fetch

First delivery at `commands/rs-fetch.md:58`: the full RSDiscovery bundle assembled end-to-end
from a one-sentence topic (Domain Analysis, Query Matrix, Fetchers, Preprocessor, Differential
Scorer, Innovation Classifier, Breakthrough Scorer, Thesis Generator, Commercial Assessor, all
run to completion within THIS invocation) - "Three graphs queried. Top breakthrough: <thesis>.
Confidence 0.85" (line 115). The rule doc's own words name this shape directly:
"The Breakthrough Scan IS a reward" (`docs/reward-before-investment-rule.md:62`), the same
citation `eureka` (batch A, Row 9) already used for its cross-domain ranking. `rs-fetch` is the
Reverse Salient Analysis sibling of that shape: a topic in, a completed breakthrough thesis with
a confidence score out, nothing deferred.

Proposed `methodology_reframe` per TB-5: unlike `pipeline` or `intel-pipeline` (which print a
structure that is THEN executed as a separate follow-on), `rs-fetch`'s transcript is the whole
deliverable of this invocation - the bundle is assembled and rendered in the same turn, not
deferred to a later run. Runner-up `schema_preview` rejected on that same TB-5 ground.

### Row 14. rs-thesis

First delivery at `commands/rs-thesis.md:55`: "This command looks up the thesis text for a prior
`RSDiscovery` by `discovery_id`" - a LOOKUP of an already-computed finding, not fresh analysis.
The reward (the breakthrough thesis) was already delivered when `rs-fetch` first ran; `rs-thesis`
re-renders it verbatim plus metadata (rs_type, breakthrough_score, created_at) - "Thesis: <body>.
Breakthrough score 8 of 10. Filed 3 days ago" (line 127). This is the same shape as
`explain-decision` (batch A, Row 10, ruled `--none (diagnostic surface)`): "a rendered log of a
[past computation]... the command adds no new analysis, only visibility into an existing one."

Proposed `--none (diagnostic surface)`. Runner-up `methodology_reframe` rejected: the content
being retrieved is valuable, but the command's OWN action is a database lookup by id, not fresh
reasoning - the analysis already happened elsewhere, in a separate prior invocation.

### Row 15. scheduled-tasks

`commands/scheduled-tasks.md` is structurally unlike every other command in this batch: it is
primarily a Cowork scheduler TASK MANIFEST (6 background job definitions - Daily Briefing,
Prediction Deadline Tracker, Competitor Watch, Grant & Funding Discovery, Domain News Scan, Scout
Sentinel), not a documented conversational invocation runbook. "Users configure these in Cowork's
task management UI. Larry references these definitions to register and execute scheduled work"
(line 45). The file never documents what a direct `/mos:scheduled-tasks` invocation itself
renders step by step, the way every other command in this batch does.

Given the ambiguity, the closest honest reading comes from `hitl_why` (line 8: "Scheduled tasks
offers one next move to confirm a schedule change") and the Configuration section
(`commands/scheduled-tasks.md:290`, the `.config.json` `scheduled_tasks` block) - a view-then-
confirm shape over the plugin's OWN scheduler configuration, matching `models` (batch A, Row 24,
`--none (diagnostic surface)`: "a configuration-state readout... reports the plugin's OWN
operator configuration rather than reasoning over the navigator's venture").

Proposed `--none (diagnostic surface)`, HIGH but flagged: this row's confidence rests on an
inference about undocumented bare-invocation behavior, not a directly-observed first delivery
the way every other row in this batch has. Runner-up `methodology_reframe` recorded as the live
alternative if the navigator knows the actual view renders something more than configuration
state.

### Row 16. skill

First delivery at `commands/skill.md:100`: Step 4 reports the emitted path
(`skills/<expert>/SKILL.md`), that it is "born EXCLUDED (a render-only capability lens)," and the
next move - "invoke the expert persona anywhere in the room." Unlike `new-surface`, this
generator DOES fully project the confirmed SyntheticExpert node's props (hat, name, archetype,
beautiful_question, research_approach, evidence_tier, provenance) into a complete skill file with
no further hand-editing required. But the actual VALUE - talking to the materialized persona - is
realized in a SEPARATE, later invocation of that skill, not in this command's own turn; this
command's own first-party output is the materialization event itself, a preparatory artifact.

Proposed `schema_preview` per TB-5, the same reasoning as `new-surface`: the emitted skill file
is a structure the command creates so a FUTURE invocation can consume it; the value it enables is
deferred, not delivered in this turn. Runner-up `live_deliverable` rejected: the term requires
the artifact to "exist and be shareable AT THE MOMENT the declaration claims it is delivered" -
while the file exists, its value (the persona conversation) is deferred to a later turn, unlike
`publish`'s URL or `export`'s HTML, which are immediately consumable content.

### Row 17. snapshot

First delivery at `commands/snapshot.md:148`: "Your Data Room export package is ready.
`room/exports/index.html` is the lobby (3 doors), `room/exports/hub.html` is the museum
(everything). Send the lobby to people - it's what you show first. Deploy the whole `exports/`
folder to Vercel." This is the `export` shape (plan 04, Row 11) even more explicitly stated: two
persisted, self-contained HTML files, "shareable via email, deployable to Vercel/Render in one
step" per the command's own `teaching` field.

Proposed `live_deliverable`, following the `export` precedent directly. Runner-up
`methodology_reframe` rejected: `snapshot` runs generator scripts over existing room content and
performs no new analysis, the same ground `export` was rejected on.

### Row 18. speakers

First delivery at `commands/speakers.md:103`, each speaker card's "Key contribution: [most
notable insight or advice]" line - a judged SELECTION of the single most notable insight per
speaker, extracted live from PROFILE.md and the `insights/` subfolder at invocation time (line
82-87: "Extract: ... Key insights (from insights/ subfolder if populated)"). This is closer to
`hat-briefing` (batch A, Row 15, `methodology_reframe`: "consolidates Larry's OWN prior... analysis
of the navigator's OWN venture material into one briefing") than to a pure roster count: picking
the standout contribution per person requires a judgment call, not just tallying meeting
appearances.

Proposed `methodology_reframe`. Runner-up `--none (diagnostic surface)` rejected: unlike `memory`
(batch A, Row 23, "a census of what the system currently holds"), the per-speaker "key
contribution" line is a synthesized selection over the navigator's own filed meeting content, not
a pure count.

### Row 19. splash - argued per the plan's flag (first-touch registry evidence)

`commands/splash.md` is a declared first-touch surface in `data/first-touch-surfaces.json`
(`id: "splash"`, `file: "commands/splash.md"`), so its first delivery is the greeting itself: the
De Stijl Mondrian banner plus the owned-emotion tagline at `commands/splash.md:39`, rendered per
`lib/copy/115-spec-strings.cjs` SPLASH_COPY - "Stuck on a decision you can't name? Let's find the
shape of it." The command's own instructions are explicit that this line is NOT generated: "print
the owned-emotion tagline on its own line, verbatim... do NOT paraphrase per Pitfall 1" (line
47). This is hardcoded branding copy, identical on every invocation, not Larry's live reasoning
over anything the navigator has provided (there is nothing to reason over yet - this is the FIRST
screen).

Because the tagline is fixed, canned copy, it fails qualifying test 1 (unpredictable: any user
could imagine an invitation line like this, and it never varies) even though it is intelligible
and on-brand. It is not a status report either - nothing about plugin or room state is disclosed.
The honest declaration is the broadened `--none (diagnostic surface)` reading already established
twice in this document (the router sub-case for `show`/`help`/`mos`/`organize`/`query`/`visualize`):
"this surface has no first reward of its own" extends naturally to a third sub-case, a static
first-touch greeting with nothing computed. This is a new sub-case worth the navigator's explicit
attention (per the checkpoint's own instruction to compare batch B against precedent and flag
anything that reads as drift), not a silent extension.

Proposed `--none (diagnostic surface)`, HIGH, flagged as a new documented sub-case (static
first-touch copy, distinct from the router sub-case). Runner-up `reframe_question` rejected: that
term requires Larry to REFRAME a sentence the navigator already supplied; splash's tagline poses
an invitation before any material exists and never varies, so nothing is being reframed.

### Row 20. stance

First delivery at `commands/stance.md:73`: the F.0 gate itself - "Flip stance: <current or
'automatic'> -> <proposed>?" - where the proposed value is computed by a FIXED, deterministic
round-robin cycle (`null -> research -> tell-act -> ask -> redteam -> research`, lines 62-66),
not by any live judgment. Under S-1 a confirmation gate is explicitly not a delivery, and here
there is no separate content behind the gate either: the entire interaction IS the mechanical
cycle-and-confirm proposal, which the navigator could predict themselves after one read of the
docs (fails qualifying test 1). The command's own `connector.reason` already names it "an ambient
affordance... carrying no problem-state trigger of its own."

Proposed `--none (diagnostic surface)`. No plausible runner-up: this is not `methodology_reframe`
(nothing is reasoned over the navigator's venture material, the cycle order is fixed data), and
it is not a preview or artifact of any kind - it is a mechanical UI toggle.

### Row 21. status - argued in full, per the plan's flag

`docs/reward-before-investment-rule.md:59` states in its own words: "A `/mos:status` output is
not a reward (predictable report)." `commands/status.md` IS the literal `/mos:status` command the
rule doc names. First delivery at `commands/status.md:51`: per-section governing-thought one-
liners with health glyphs (check/warn/low/--), replacing "the pre-88 raw artifact-count
rendering" - still a predictable structural readout of what the navigator has already filed,
exactly the shape the rule doc disqualifies by name.

`status` carries `hitl_shape: F.1`, a genuine Decision-Gate fork ("Status offers one next move
based on where the room stands," line 8), which is the opposite of a silent script; the TB-1 grep
sweep above confirms zero of `--no-interactive`, `--script`, `-q` in the body. So the scripting
opt-out is doubly forbidden here, the same double rejection `doctor` (plan 04, Row 7) and
`publish` (plan 04, Row 13) already established.

Proposed `--none (diagnostic surface)`, on the rule doc's own naming, not an inference. Runner-up
`--none (scripting only)` rejected on both TB-1 (no scripting flag) and TB-2 (`F.1` is a declared
human fork) independently.

### Row 22. update

First delivery at `commands/update.md:58`: the version-comparison verdict - "You're running the
latest, v{LOCAL_VERSION} (SHA {LOCAL_SHA}). Nothing to update" (the `UP_TO_DATE` branch) or the
equivalent `VERSION_DIFFERS` / `SHA_DIFFERS_INVERSION_HOTFIX` / `NETWORK_ERROR` verdicts from
`check-version-and-sha.cjs`. This is a maintenance/lifecycle report of the PLUGIN's own version
state (local vs remote, semver vs SHA) - predictable given what version is installed, and it
reports the plugin's operational state rather than reasoning over the navigator's venture,
matching `models` (batch A, Row 24) and `admin` (batch A, Row 1).

Proposed `--none (diagnostic surface)`. Runner-up `--none (scripting only)` rejected: zero
scripting flags measured in the body (the `force` argument skips version comparison but is not
one of the three named flags), and `hitl_shape: F.0` is a declared human fork per TB-2.

### Row 23. visualize

First delivery at `commands/visualize.md:55`: "The user sees ONE deprecation note + the dashboard
render." Deprecated soft-alias stub (D-09) redirecting to `/mos:dashboard --mermaid`, passing
through the dashboard's output verbatim (line 57). Identical router shape to `organize` and
`query` above.

Proposed `--none (diagnostic surface)`, router sub-case. Runner-up `schema_preview` rejected: the
relayed dashboard render is the target's complete output, not a preview.

### Row 24. whitespace - argued in full, per the plan's flag - NEEDS-RULING

`body_shape: methodology`, `kind: methodology`, framework "HSI Semantic Surprise Analysis
Assistant" - the clearest test of the methodology-versus-report boundary in this batch, exactly
as the plan names it. Two problems compound here, not one.

**The bare-invocation problem.** "When `/mos:whitespace` is called with no subcommand, display"
the help listing (line 74-94) - under S-1 a menu is explicitly not a delivery, and nothing else
is computed on the bare path. The closest documented, flagged path with a genuine first-party
delivery is `map` ("Start here - see where the gaps are," the help text's own recommendation),
whose Zone 2 body at `commands/whitespace.md:152` is a density grid: zone id, density score
(0.0-1.0), problem-type classification, and the nearest 1-2 frameworks per zone, sorted sparsest-
first.

**The methodology-versus-report tension.** `map`'s grid is computed via embedding-space density
estimation and TopicForest clustering - deterministic algorithmic output, not Larry's live
judgment, structurally similar to `diagnostics` (batch A, Row 6, ruled `--none (diagnostic
surface)`: "the four numbers are a status roster of the room's current fingerprint... computed,
not reasoned... an algorithmic scoreboard"). If `map`'s output is read the same way, it belongs
in the diagnostic-opt-out bucket too. But `whitespace map` differs from `diagnostics` in kind, not
just degree: `diagnostics` reports the room's own self-referential health (is it consolidating or
disrupting); `whitespace map` identifies SPECIFIC missing content zones paired with SPECIFIC
methodology framework recommendations (which framework addresses which gap) - directly
actionable guidance about the navigator's own domain, not an abstract health score. That
pairing-to-framework is closer to `mullins`'s "surfaces the weakest two" (plan 04, Row 11,
`methodology_reframe`) than to a scoreboard.

Proposed `methodology_reframe`, declared against the `map` flagged path (mirroring how `intel-
pipeline`, batch A Row 20, and `pipeline`, plan 04 Row 12, both declared against a flagged path
rather than the bare-invocation menu). Runner-up `--none (diagnostic surface)` recorded as the
live alternative on the `diagnostics` analogy, not rejected outright - this is a genuine
navigator call between two defensible readings, not a mechanical one.

### Row 25. wiki

First delivery at `commands/wiki.md:48`: the bare-invocation (no-flags) behavior launches a LIVE
local Express server rendering `room/` as Wikipedia-style pages - a real-time rendering of
content that already exists in the room, nothing persisted unless the separate `--export` flag is
used (line 73-77). This is exactly `dashboard`'s shape (batch A, Row 5, `--none (diagnostic
surface)`): "opens an existing browser view of content the navigator already filed; it produces
no new analysis of its own... ephemeral local server process with nothing filed."

Proposed `--none (diagnostic surface)`, following the `dashboard` precedent directly for the
default (no-flags) path. Runner-up `live_deliverable` rejected for the DEFAULT path specifically:
that term fits the `--export` flag's output (a persisted, shareable static HTML bundle -
line 74-77 names deployment and zip-sharing explicitly), but the declaration must describe the
bare invocation, which is the ephemeral live server, not the flagged export.

### Note on the concentration of diagnostic opt-outs (Batch B)

This table proposes `--none (diagnostic surface)` on 12 of the 22 HIGH rows (`operator`,
`organize`, `query`, `room`, `rs-thesis`, `scheduled-tasks`, `splash`, `stance`, `status`,
`update`, `visualize`, `wiki`, plus `opportunities`'s NEEDS-RULING lean), a lower concentration
than batch A's 20-of-25 (per T-267.3-26's own mitigation, named here rather than left for the
navigator to discover). The honest explanation: this batch is weighted toward maintenance/
lifecycle commands (`update`, `snapshot`, `scheduled-tasks`), deprecated router stubs (`organize`,
`query`, `visualize`, three of the six soft-aliases in the whole undeclared set), and a genuine
cluster of methodology-analysis commands (`mva-report`, `qualify-opportunity`, `reanalyze`,
`rs-explain`, `rs-fetch`, `speakers`, plus the NEEDS-RULING `research`/`whitespace`) that pulled
the other way. Three commands propose `live_deliverable` or its close cousin `schema_preview`
where a persisted or soon-to-be-consumable artifact is genuinely produced (`present`, `snapshot`,
`new-surface`, `skill`).

Approved is pending navigator ruling. Proceed to Task 2.
