---
phase: 267.3-reward-before-investment-guard-jurisdiction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-AUDIT.md
  - .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md
autonomous: false
requirements: [GUARD-09, GUARD-02, GUARD-01, GUARD-08]
canon_parts: ["Part 6", "Part 7", "Part 11"]

must_haves:
  truths:
    - "A reader can see, in one table, every surface the reward-before-investment guard CAN reach and every surface it CANNOT, with a file:line citation for each claim."
    - "A reader can see why no member of the current six-value REWARD_TYPES vocabulary honestly describes a conversational methodology command or a diagnostic/report command, quoted against the rule doc's own text."
    - "The navigator has ruled, in writing, on three linked questions: the out-of-frontmatter declaration mechanism, the vocabulary amendment, and the lint scope."
    - "Every rejected alternative is recorded with the reason it was rejected, not silently dropped."
  artifacts:
    - path: ".planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-AUDIT.md"
      provides: "The measured jurisdiction map: reachable surfaces, unreachable surfaces, vocabulary-fit finding"
      min_lines: 90
      contains: "scripts/session-start"
    - path: ".planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md"
      provides: "The navigator ruling D-A / D-B / D-C plus the ten minted GUARD-NN requirement IDs"
      min_lines: 60
      contains: "GUARD-01"
  key_links:
    - from: "267.3-AUDIT.md"
      to: "lib/core/mva-rule-linter.cjs"
      via: "file:line citation of the scanCommands directory-only scope"
      pattern: "mva-rule-linter\\.cjs:"
    - from: "267.3-DECISIONS.md"
      to: "267.3-AUDIT.md"
      via: "each ruling cites the audit row that motivated it"
      pattern: "267\\.3-AUDIT\\.md"
---

<objective>
Rule the jurisdiction question before writing any mechanism.

This phase exists because the reward-before-investment guard has a real enforcement
mechanism that cannot see the surface that needs it most. Before extending anything, the
phase needs two things it does not have: a MEASURED map of what the guard reaches and what
it does not, and a navigator RULING on three linked design questions the ROADMAP explicitly
left open.

Purpose: a mechanism built before the ruling is a guess. Plan 271-03 already paid for one
guess (`publish` classified `--none (scripting only)` when it is `hitl_shape: F.0`
interactive). This plan buys the ruling first.

Output: `267.3-AUDIT.md` (the measured map) and `267.3-DECISIONS.md` (the ruling plus the
minted requirement IDs every later plan cites).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/reward-before-investment-rule.md
@lib/core/mva-rule-linter.cjs
@scripts/check-reward-before-investment.cjs
@data/first-touch-surfaces.json
@data/hitl-shape-declaration-schema.json
@.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/deferred-items.md
</context>

## Artifacts this phase produces

Named here once so every downstream plan can cite them. Only the first two are produced by
THIS plan; the rest are listed so the phase's whole output surface is visible in one place.

| Artifact | Kind | Produced by | What it is |
|---|---|---|---|
| `267.3-AUDIT.md` | phase doc | 267.3-01 | The measured jurisdiction map (reachable / unreachable / vocabulary-fit) |
| `267.3-DECISIONS.md` | phase doc | 267.3-01 | The D-A / D-B / D-C navigator ruling plus the GUARD-NN requirement IDs |
| `data/first-reward-surfaces.json` | new registry | 267.3-02 | The out-of-frontmatter declaration contract, registry-is-the-table |
| `REWARD_TYPES` amendment | vocabulary | 267.3-02 | New closed-vocabulary members, each with a written canon-amendment reason |
| `scanDeclaredSurfaces()` | new linter export | 267.3-02 | The linter entry point that reads the registry instead of frontmatter |
| `--surfaces` CLI mode | new CLI mode | 267.3-02 | `check-reward-before-investment.cjs --surfaces`, the registry audit |
| `tests/run-all-267.3.sh` | test aggregator | 267.3-02 | Phase test runner, fails loudly on zero discovered files |
| Registry entries for `scripts/session-start` | declarations | 267.3-03 | FIRST_INSTALL, UPDATE, MODE_ROUTING first-reward contracts |
| `verify-release` surfaces gate | release gate | 267.3-03 | Fail-closed gate over the registry |
| `267.3-CLASSIFICATION.md` | phase doc | 267.3-04 | The per-command classification rubric plus every command's ruled value with its cited first-reward moment |
| 67 `interactive_first_reward` declarations | frontmatter | 267.3-04/06/07 | One honest per-command declaration for every previously-undeclared command |
| `verify-release` full-audit gate | release gate | 267.3-08 | Whole-tree commands audit, fail-closed, promoted only after the backfill lands |

<tasks>

<task type="auto">
  <name>Task 1: Write the measured jurisdiction map (267.3-AUDIT.md)</name>

  <read_first>
    - `lib/core/mva-rule-linter.cjs` (whole file, 252 lines) -- the header block lines 6-27, `scanFiles` lines 170-204, `scanCommands` lines 224-243, `REWARD_TYPES` lines 49-56
    - `scripts/check-reward-before-investment.cjs` (whole file, 181 lines) -- the two modes, the `^commands/[^/]+\.md$` staged filter at line 82, the default target at line 127
    - `docs/reward-before-investment-rule.md` -- the rule statement, "What counts as first variable reward" (the four qualifying tests), the explicit non-rewards ("A grade is not a reward", "A `/mos:status` output is not a reward"), and "The scripting override"
    - `scripts/hooks/pre-commit-room-minto-guard.sh` lines 285-315 -- the `mva-rule-linter` arm and its `^commands/.+\.md$` matcher at line 300
    - `scripts/session-start` lines 640-716 -- `ONBOARD_STATUS`/`ONBOARD_TYPE` at 647-648, `COLD_START_MENU` at 657, the FIRST_INSTALL `context=` at 666, the UPDATE `context=` at 680, `MODE_MENU` at 704, the MODE_ROUTING `context=` at 706 and 714
    - `data/first-touch-surfaces.json` (whole file) -- the existing registry-is-the-table precedent and its six surface records
    - `data/hitl-shape-declaration-schema.json` (the `_doc` block) -- the `surface_count_principle` and `registry-is-the-table` language this phase reuses
    - `.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/deferred-items.md` -- DEFERRED-271-D1 and DEFERRED-271-D2
  </read_first>

  <files>.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-AUDIT.md</files>

  <action>
Write `267.3-AUDIT.md` with exactly five numbered sections. Every factual claim carries a
`path:line` citation. Do not estimate a number that can be measured; run the command and
paste its real output.

**Section 1 -- What the guard reaches today (measured, not asserted).**
Run `node scripts/check-reward-before-investment.cjs` and paste the head and tail of its
real output. Record the three live numbers as a table: compliant, missing, invalid, and the
denominator `ls -1 commands/*.md | wc -l`. As of 2026-08-27 the expected reading is
46 compliant / 67 missing / 0 invalid over 113 command files; if the live numbers differ,
record the LIVE numbers and note the delta, never adjust the instrument to the expectation.
Then state the three independent proofs of the guard's scope with citations:
`lib/core/mva-rule-linter.cjs` header lines 6-9 (`Scans commands/*.md frontmatter`),
`scanCommands` reading `commandsDir` only (lines 224-243), and
`scripts/check-reward-before-investment.cjs:127` defaulting to
`path.join(__dirname, '..', 'commands')`. Add the fourth, hook-level proof:
`scripts/hooks/pre-commit-room-minto-guard.sh:300` gates on `^commands/.+\.md$`, so the arm
does not even fire unless a command file is staged.

**Section 2 -- What the guard cannot reach (the unreachable surface inventory).**
A table with columns: surface id, file, kind, why unreachable, evidence path:line.
Rows to include at minimum, each verified by reading the file rather than assumed:
`session-start:FIRST_INSTALL` (`scripts/session-start:666`, bash heredoc-free string
assignment, no frontmatter exists in a bash script);
`session-start:UPDATE` (`scripts/session-start:680`);
`session-start:MODE_ROUTING` (`scripts/session-start:706` plus the `MODE_MENU` at 704 and
the mode-behavior append at 714);
`session-start:COLD_START_MENU` (`scripts/session-start:657`, the shared menu all three
branches append).
Also enumerate every `SessionStart` entry in `hooks/hooks.json` and state, per entry,
whether it emits user-facing first-touch prose or not, so the inventory is derived from the
hook manifest rather than from memory. State plainly which of these carry an
`interactive_first_reward` declaration today: none of them do.

**Section 3 -- The vocabulary-fit finding (the reason a backfill cannot start yet).**
This is the section that changes what the phase can honestly build. Establish three claims
with evidence:

(a) `REWARD_TYPES` (`lib/core/mva-rule-linter.cjs:49-56`) has six members, and five of them
are named after ONE specific command's reward each: `reframe_question` (onboard),
`instant_brief` (new-project), `calibration_distribution_preview` (grade),
`paragraph_preview` (file-meeting), `schema_preview`. Cite
`docs/reward-before-investment-rule.md`'s "Application to existing flows" section, which
mints each term against exactly one flow.

(b) A conversational methodology command has no term. Reproduce the frontmatter-signal
table below (regenerate it with the one-liner in the verify block rather than copying these
numbers on faith) and point at the rows where `body_shape` is `methodology` and the command
is conversational: `analyze-systems`, `challenge-assumptions`, `find-connections`,
`lean-canvas`, `mullins`, `score-innovation`, `systems-thinking`. Their first delivered
value is a Larry analysis or reframe over the user's own material, which is not
`reframe_question` (that term is bound to onboard's single-sentence opener) and is
certainly not `--none (scripting only)`.

(c) A diagnostic / operator / report command has no term either, and `--none (scripting
only)` MISLABELS it. Quote `docs/reward-before-investment-rule.md`'s scripting-override
section verbatim: the override is legitimate only when "the command is invoked with a
`--no-interactive`, `--script`, or `-q` flag". `commands/status.md` is invoked
interactively and the same rule doc says in its own words that "A `/mos:status` output is
not a reward". So `status` can honestly declare neither a reward type nor the scripting
opt-out. Name the other members of this class from the signal table: `doctor`, `heal`,
`dogfood-flush`, `hmi-status`, `dial-memory-refresh`, `feynman-timeline-refresh`,
`diagnostics`, `update`.

Close the section with the consequence stated flatly: **an honest backfill of the 67 is
blocked on a vocabulary amendment.** Cite the linter's own header line 15-17, which already
says future additions "are canon amendments, not command-level inventions". This is a
missing-information constraint, not a difficulty judgment.

**Section 4 -- The blocked work this phase unblocks.**
Reproduce the current blocked state as measured, not remembered:
run `git status --short -- commands/ skills/` and paste the real output;
run `node scripts/check-plugin-path-anchoring.cjs --check; echo "exit=$?"` and paste the
violation total. Expected at plan time: 16 modified `commands/*.md` plus 16 modified
`skills/*/SKILL.md` held uncommitted, and gate 10c red. Name the 17 blocked files (the 16
held plus `commands/doctor.md`). Cite DEFERRED-271-D1 for the proof that the missing
declarations are pre-existing (`git log --all -S` returns zero commits ever touching
`interactive_first_reward` in those files).

**Section 5 -- The three open questions this phase must rule, stated as questions.**
Do NOT answer them here. State each with its candidate options and the evidence bearing on
each, so Task 2's checkpoint is a decision and not a research exercise:
D-A the out-of-frontmatter declaration mechanism (candidates: a new sibling registry
modeled on `data/first-touch-surfaces.json`; extending that same file; a parsed comment
convention inside `scripts/session-start`; an extension of the born-wired connector
registry);
D-B the vocabulary amendment (how many new terms, and their exact spellings);
D-C the lint scope (keep `--staged` only, versus adding a whole-tree audit to a release
gate so the debt cannot hide until an unrelated sweep stages the files).

House rules: hyphens only, no em-dashes, no emoji. Feynman-simplified prose.
  </action>

  <verify>
    <automated>test -f .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-AUDIT.md && test "$(wc -l < .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-AUDIT.md)" -ge 90 && grep -q 'scripts/session-start:666' .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-AUDIT.md && grep -q 'mva-rule-linter.cjs:' .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-AUDIT.md && ! grep -q '—' .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-AUDIT.md && echo AUDIT_OK</automated>
    <automated>node -e "const fs=require('fs'),path=require('path');const {scanCommands}=require('./lib/core/mva-rule-linter.cjs');const r=scanCommands('commands');console.log('compliant',r.compliant.length,'missing',r.missing.length,'invalid',r.invalid.length);for(const m of r.missing){const t=fs.readFileSync(m.path,'utf8');const fm=/^---\r?\n([\s\S]*?)\r?\n---/.exec(t);const b=fm?fm[1]:'';const g=k=>{const mm=new RegExp('^'+k+':\\\\s*(.*)\$','m').exec(b);return mm?mm[1].trim().replace(/^[\"']|[\"']\$/g,'').slice(0,30):''};console.log([path.basename(m.path,'.md'),g('hitl_shape'),g('body_shape'),/\`\`\`bash|Bash\(|node scripts\//.test(t)?'CODE':'conv'].join('|'))}"</automated>
  </verify>

  <acceptance_criteria>
    - `267.3-AUDIT.md` exists, is at least 90 lines, and contains all five numbered sections.
    - Section 1 contains the LIVE compliant/missing/invalid counts pasted from a real run of `node scripts/check-reward-before-investment.cjs`, plus all four scope proofs with `path:line` citations.
    - Section 2's unreachable table has at least 4 rows and every row carries a `scripts/session-start:<line>` or `hooks/hooks.json` citation.
    - Section 3 quotes `docs/reward-before-investment-rule.md`'s scripting-override sentence verbatim and names at least 7 commands in the conversational-methodology class and at least 7 in the diagnostic/operator class.
    - Section 4 contains pasted real output of `git status --short -- commands/ skills/` and of `node scripts/check-plugin-path-anchoring.cjs --check`.
    - Section 5 states D-A, D-B and D-C as unanswered questions with their candidate options.
    - The file contains zero em-dash characters (U+2014).
  </acceptance_criteria>

  <done>A navigator can read one file and know exactly what the guard reaches, what it does not, why the vocabulary blocks a backfill, and what three questions are outstanding, with a citation for every claim.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: The jurisdiction ruling (D-A mechanism, D-B vocabulary, D-C lint scope)</name>

  <read_first>
    - `.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-AUDIT.md` (produced by Task 1, all five sections)
  </read_first>

  <decision>Three linked rulings that together define who must declare a first-reward contract, on what surface, in what words, and what enforces it. They are presented as one package because a mechanism without a vocabulary cannot be filled in, and a vocabulary without a scope decision cannot be enforced.</decision>

  <context>
The ROADMAP left D-A explicitly open ("Open design question, not decided here"). Task 1's
audit added D-B, which the ROADMAP did not anticipate: the closed six-value vocabulary
cannot honestly describe 67 of 113 shipped commands, so a backfill cannot start until the
vocabulary is amended. D-C is the ROADMAP's own scope-widening recommendation.

Phase 271 is blocked on this ruling right now: 16 command files plus 16 regenerated skill
mirrors sit uncommitted in a SHARED working tree, and `scripts/verify-release` gate 10c is
red, so no release can be cut until it lands.
  </context>

  <options>
    <option id="option-a">
      <name>RECOMMENDED: sibling registry + two new vocabulary terms + release-gated whole-tree audit</name>
      <pros>
D-A: a new `data/first-reward-surfaces.json`, modeled byte-for-byte on the record shape of
the existing `data/first-touch-surfaces.json` (id / file / kind / plus this registry's own
`interactive_first_reward` and `why` fields). This is the house pattern, already used twice
in this repo (`first-touch-surfaces.json`, `hitl-shape-declaration-schema.json`), already
has a scanner precedent (`lib/core/stale-copy-scanner.cjs` reads the first-touch registry),
and satisfies Canon Part 7 reuse-before-build by copying an existing contract shape rather
than minting a new one. A SIBLING file rather than an extension of `first-touch-surfaces.json`
because that file's consumers (`lib/core/stale-copy-scanner.cjs`,
`lib/core/first-touch-version-stamper.cjs`, `scripts/doctor.cjs`,
`lib/core/doctor/stale-first-touch-copy-module.cjs`) all key off version-stamping semantics,
and overloading it with reward semantics would make one file answer to two gates.

D-B: amend `REWARD_TYPES` with exactly two new members, each with a written reason recorded
in `docs/reward-before-investment-rule.md` as a canon amendment:
`methodology_reframe` for a conversational methodology command whose first delivered value
is Larry's analysis or reframe over the user's own material, and
`--none (diagnostic surface)` for a command that reports state rather than delivering a
variable reward, which the rule doc itself already names as a non-reward
("A `/mos:status` output is not a reward"). The second term is deliberately spelled in the
same `--none (...)` family as the existing scripting opt-out so a reader sees at a glance
that it is an opt-out and not a reward.

D-C: keep `--staged` as the commit gate exactly as Phase 245-02 built it, and ADD the
already-existing full audit to `scripts/verify-release` as a fail-closed gate, promoted
only at phase close after the backfill lands (the 271-05 discipline: wire the gate, do not
relax it, and do not turn it on before the work that makes it green is done).
      </pros>
      <cons>Mints a new data file and two new vocabulary terms; both are permanent surface area. The `--none (diagnostic surface)` term formalizes a category the original rule doc did not name.</cons>
    </option>
    <option id="option-b">
      <name>Parsed comment convention inside the bash script</name>
      <pros>No new data file. The declaration sits three lines above the prose it describes, so a reader editing `scripts/session-start` cannot miss it.</pros>
      <cons>Requires the linter to grow a bash parser, which is a second parsing surface next to the existing YAML-ish frontmatter parser and a place for the two to drift. It also does not generalize: a future non-bash injected-prose surface would need a third parser. No existing precedent in this repo.</cons>
    </option>
    <option id="option-c">
      <name>Extend the born-wired connector registry (Canon Part 11 machinery)</name>
      <pros>Part 11 is already "every invocable surface is born declared", and `data/connector-registry.json` already enumerates surfaces. One registry instead of two.</pros>
      <cons>The connector registry is generated by `scripts/build-connector-registry.cjs` and is about INVOCATION wiring, not first-touch reward contracts. `scripts/session-start` is a hook, not an invocable surface, so it does not belong there. Adding a reward field would make a generated file carry hand-authored content.</cons>
    </option>
    <option id="option-d">
      <name>Option A, but narrow the phase to the 17 Phase-271-blocking commands and register the other 50 as a follow-up phase</name>
      <pros>Smaller phase, faster unblock of the release gate.</pros>
      <cons>Leaves 50 commands undeclared behind a gate that only fires when someone stages them, which is the exact debt-ratchet failure DEFERRED-271-D1 documented. The next unrelated sweep hits the same wall. The ROADMAP's own scope-widening paragraph recommends against this.</cons>
    </option>
  </options>

  <action>
Present the four options with the Task 1 audit's Section 3 and Section 5 as the evidence
base. State plainly that option-a is the planner's recommendation and why. Record the
ruling in `267.3-DECISIONS.md` with:

1. The ruled option id, verbatim navigator reasoning if any is given, and the date.
2. The three rulings written out separately as D-A, D-B, D-C, each citing the
   `267.3-AUDIT.md` section that motivated it.
3. Every rejected option with the reason it was rejected. A rejected option is never
   deleted.
4. The exact final spelling of any new `REWARD_TYPES` members, since plan 267.3-02 codes
   against these strings literally and plans 04/06/07 classify against them.
5. The ten minted requirement IDs for this phase, matching the Phase 270 precedent of
   minting IDs at plan time in a `-DECISIONS.md` and registering them in
   `.planning/REQUIREMENTS.md` at phase close:
   GUARD-01 a machine-readable declaration contract exists for surfaces with no frontmatter;
   GUARD-02 the reward vocabulary honestly covers every shipped command class, and every
   addition is recorded as a canon amendment with a written reason;
   GUARD-03 the linter can read a first-reward declaration for `scripts/session-start`'s
   FIRST_INSTALL branch;
   GUARD-04 every injected-prose first-touch surface carries a declaration;
   GUARD-05 the 17 commands blocking Phase 271 carry honest per-command-reasoned
   declarations, each citing the command's actual first-reward moment;
   GUARD-06 Phase 271's held work is committed through the full pre-commit hook with no
   bypass, and anchoring gate 10c reads zero violations;
   GUARD-07 every `commands/*.md` carries a declaration, and the full audit reads zero
   missing and zero invalid;
   GUARD-08 the debt is visible without waiting for an unrelated commit, via a whole-tree
   audit wired fail-closed into `scripts/verify-release`;
   GUARD-09 the ruling is recorded with its reasoning and its rejected alternatives;
   GUARD-10 no gate was relaxed, allowlisted, edited, or bypassed to make the board green.

If the navigator amends a leg (for example accepts D-A and D-C but changes D-B's term
spellings), record the amendment as the ruling and do NOT re-plan; plan 267.3-02 reads its
literals from this file.
  </action>

  <resume-signal>Select: option-a, option-b, option-c, option-d, or option-a with amendments (name the amended leg and the change).</resume-signal>

  <verify>
    <automated>test -f .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md && grep -q 'D-A' .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md && grep -q 'D-B' .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md && grep -q 'D-C' .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md && grep -q 'GUARD-10' .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md && grep -qi 'rejected' .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md && ! grep -q '—' .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md && echo DECISIONS_OK</automated>
  </verify>

  <acceptance_criteria>
    - `267.3-DECISIONS.md` exists and records a ruled option id with a date.
    - D-A, D-B and D-C each appear as their own ruling with a citation to a `267.3-AUDIT.md` section.
    - Every non-ruled option appears with a written rejection reason.
    - The final `REWARD_TYPES` member strings are written out literally and unambiguously, quoted, so a later plan can copy them character for character.
    - All ten GUARD-01 through GUARD-10 IDs appear with their one-line statements.
    - The file contains zero em-dash characters (U+2014).
  </acceptance_criteria>

  <done>The three open design questions are answered in writing, with rejected alternatives preserved, and every later plan in this phase has literal strings and requirement IDs to build against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner -> navigator | The ruling is a human judgment; a plan that pre-answers it is a boundary violation |
| audit doc -> later plans | Plans 02 through 08 code against literals recorded here; a wrong literal propagates silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267.3-01 | Tampering | `267.3-AUDIT.md` numbers | mitigate | Every count is pasted from a real command run, never estimated; Task 1's second `<automated>` verify regenerates the signal table so a stale number is visible |
| T-267.3-02 | Repudiation | the ruling | mitigate | `267.3-DECISIONS.md` records the ruled option, the date, and every rejected option with its reason; nothing is deleted |
| T-267.3-03 | Information disclosure | none | accept | Local repo files only; zero network, zero Brain calls, zero user-content egress (Canon Part 8) |
| T-267.3-04 | Elevation of privilege | vocabulary invention | mitigate | New `REWARD_TYPES` members are minted only by this navigator ruling and recorded as a canon amendment in `docs/reward-before-investment-rule.md` (plan 02), never invented at command level |
| T-267.3-SC | Tampering | package installs | accept | This plan installs zero packages; no npm/pip/cargo invocation anywhere in phase 267.3 |
</threat_model>

<verification>
- `267.3-AUDIT.md` and `267.3-DECISIONS.md` both exist in the phase directory.
- Both files are free of em-dashes (project hard rule).
- `node scripts/check-reward-before-investment.cjs` still runs and still exits 1 (nothing in this plan changes the linter; a changed exit code means a file was edited that should not have been).
- `git status --short -- commands/ skills/` still shows the 16 held command files and 16 held mirrors untouched by this plan.
</verification>

<success_criteria>
- A navigator can read `267.3-AUDIT.md` and see, with citations, every surface the guard reaches, every surface it does not, and why the vocabulary blocks a backfill.
- `267.3-DECISIONS.md` carries a ruled D-A, D-B and D-C, the literal new vocabulary strings, all ten GUARD-NN IDs, and every rejected alternative with its reason.
- Zero source files under `lib/`, `scripts/`, `commands/`, `skills/` or `data/` are modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-01-SUMMARY.md` when done.
</output>
