---
phase: 267.3-reward-before-investment-guard-jurisdiction
plan: 08
subsystem: reward-before-investment-guard
tags: [mva-rule-linter, verify-release, release-gate, phase-close, guard-08, guard-09, guard-10, no-relaxation-audit]

requires:
  - phase: 267.3-07
    provides: "the whole-tree audit reading 113 compliant / 0 missing / 0 invalid, the precondition this plan's fail-closed promotion needed"
provides:
  - "commit 8394073a: scripts/verify-release gate 10e (the whole-tree commands/*.md reward audit, fail-closed, proven against a stripped-fixture A/B test) plus the corrected docs/reward-before-investment-rule.md enforcement-surface description (two declaration sources, three CLI modes, one commit gate, two release gates)"
  - "commit ca5a42ed: all ten GUARD-01..10 requirement IDs registered in .planning/REQUIREMENTS.md, ROADMAP.md's Phase 267.3 section marked 8/8 plans complete with a measured phase-outcome paragraph, a CHANGELOG entry, and a knowledge-base.md debt-ratchet lesson"
  - "commit 8b0696fa: the full Dev-Research compositing trail drafted and staged (not landed -- see Known Stubs) plus an honest ROADMAP note naming the pending navigator action"
  - "phase 267.3 CLOSED: all 8 plans across 7 waves executed, all 10 GUARD requirements satisfied, navigator approval recorded below"
affects:
  - "any future release cut (scripts/verify-release now carries two reward-before-investment gates, 10d and 10e)"
  - "any future gate design in this repo (the knowledge-base.md debt-ratchet lesson is a standing pattern warning)"
  - "whoever next has room-write access to ~/MindrianRooms (must land the staged compositing trail; see Known Stubs)"

tech-stack-added: []
tech-stack-patterns:
  - "The 271-05 A/B-fixture-proof discipline applies to promoting an EXISTING gate mode to fail-closed, not just to a brand-new gate: prove the stripped case fails and the real case passes before wiring, even when the underlying check (the full-audit CLI mode) has existed unchanged since the tool was written."
  - "A worktree-isolated executor's Write tool enforces its sandbox boundary before any repo-level guard (scripts/write-scope-check) gets a chance to run -- two independent, differently-scoped blocks can both apply to the same write, and reporting only the anticipated one would understate the actual constraint."
  - "A gate narrowed to staged/changed-file scope for a legitimate reason can still let its target invariant reaccumulate invisibly; the standing fix is wiring a whole-tree audit at the SAME TIME as the narrowing (even at a slower cadence), not as a follow-up phase once the debt has had time to hide -- filed to knowledge-base.md as reward-guard-staged-scope-debt-ratchet."

key-files-created:
  - .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-08-SUMMARY.md
  - .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-08-COMPOSITING-TRAIL-STAGED.md
key-files-modified:
  - scripts/verify-release (gate 10e added)
  - docs/reward-before-investment-rule.md (enforcement-surface description corrected)
  - .planning/REQUIREMENTS.md (GUARD-01..10 registered, Traceability updated to 80 active requirements)
  - .planning/ROADMAP.md (Phase 267.3 section: 8/8 plans, phase-outcome paragraph, compositing-trail status note)
  - CHANGELOG.md ([Unreleased] entry)
  - .planning/debug/knowledge-base.md (reward-guard-staged-scope-debt-ratchet entry)

key-decisions:
  - "Gate 10e sited immediately after gate 10d in scripts/verify-release, modeled line-for-line on gate 10c's capture-output/capture-code pattern, invoking the CLI's full-audit default mode (no flag) against the real commands/ directory."
  - "docs/reward-before-investment-rule.md's 'Two scopes' table rewritten as 'Three CLI modes, one commit gate, two release gates' with each CLI invocation's exact consumer named, and a 'why both scopes exist' paragraph citing DEFERRED-271-D1 verbatim."
  - "GUARD-10 (no relaxation) verified twice, independently: once mechanically at Task 1 (git diff grep for removed anchor/surfaces/mirror lines = 0) and once at Task 3 via a full phase-scoped git log walk of every commit touching a gate/hook file, each individually verdicted against its actual diff, not its commit message."
  - "The Dev-Research compositing trail could not be landed at its intended room path from this worktree-isolated execution context (the Write tool refuses paths outside the worktree root, independent of and prior to scripts/write-scope-check's own room-scope refusal). Per the plan's explicit instruction, neither guard was bypassed: the trail was drafted in full and staged in-repo instead, with the block state recorded honestly in both this summary and ROADMAP.md, naming the exact pending navigator action."
  - "Navigator independently re-verified the roll-up (re-ran the full reward audit, the full release gate, and the gate-10e diff directly against the worktree) before approving phase close, rather than accepting the executor's report on trust."

requirements-completed: [GUARD-01, GUARD-02, GUARD-03, GUARD-04, GUARD-05, GUARD-06, GUARD-07, GUARD-08, GUARD-09, GUARD-10]

duration: "~50min from first read to navigator approval (three commits landing 02:40-02:51 local +0300, plus reading/audit/checkpoint-verification time not separately tracked); checkpoint pause between Task 3's action and navigator sign-off not counted against active work"
completed: 2026-08-28
---

# Phase 267.3 Plan 08: Fail-Closed Whole-Tree Gate, Requirement Registration, and Phase Close Summary

The whole-tree `commands/*.md` reward-before-investment audit is now a fail-closed release
gate (proven against a stripped-fixture A/B test), the rule doc's enforcement description is
corrected to state the live truth (two declaration sources, three CLI modes, one commit gate,
two release gates), all ten GUARD-01..10 requirements are registered with their measured
results, and a phase-wide no-relaxation audit confirms every gate-touching commit across all
eight plans was a pure addition. The navigator independently re-verified the roll-up and
approved phase close. The Dev-Research compositing trail is drafted in full and staged
in-repo but not yet landed in the room -- an honest, reported block, not a silent skip.

## Performance

- **Duration:** ~50 min (three landing commits 02:40-02:51 local +0300; reading, live
  verification, and the no-relaxation audit not separately timed)
- **Tasks:** 3/3 completed (Task 1 auto, Task 2 auto, Task 3 checkpoint -- paused, navigator
  independently re-verified, approved)
- **Commits:** 3 (one per task's real work) plus this SUMMARY.md commit

## Accomplishments

- Added `scripts/verify-release` gate 10e: the CLI's full-audit default mode (no flag, whole
  `commands/` tree) is now fail-closed at release time, sited alongside gate 10d's registry
  audit. Proven against a stripped-fixture A/B test before being wired: a `commands/` copy
  with one declaration stripped exits 1, the real tree exits 0. Live run confirmed: all 113
  files pass, gate reads green.
- Corrected `docs/reward-before-investment-rule.md`'s "Detection mechanism" and "Two scopes"
  sections, which were materially out of date: now states two declaration sources, three CLI
  modes with each mode's real consumer named, one commit gate (still staged-scoped, unchanged),
  and two release gates, with a "why both scopes exist" paragraph citing `DEFERRED-271-D1`.
- Registered all ten `GUARD-01` through `GUARD-10` requirement IDs in `.planning/REQUIREMENTS.md`,
  matching the Phase 270 block's shape, each row citing its measured result.
- Updated `.planning/ROADMAP.md`'s Phase 267.3 section: marked 8/8 plans complete, added a
  phase-outcome paragraph with measured numbers (4 declared surfaces, 6->9 vocabulary members,
  67 commands declared across 3 waves, 113/0/0 final audit, both gates A/B-proven), and
  recorded the compositing-trail staged-not-landed state with the pending navigator action.
- Added a `CHANGELOG.md` entry and a `.planning/debug/knowledge-base.md` lesson
  (`reward-guard-staged-scope-debt-ratchet`) generalizing the phase's core finding past this
  one rule: a gate narrowed to staged/changed-file scope for a legitimate reason still needs a
  whole-tree audit wired at the same time, or its target debt reaccumulates invisibly.
- Ran the full Task 3 roll-up live: 4 gates green (full audit, surfaces, anchoring, mirrors),
  2 of 4 test suites green (267.3, 271) and 2 with pre-existing, already-logged, unrelated
  failures (118, 267.1 -- see Pre-existing Test Debt), `scripts/verify-release` exits 0 with
  35 passed / 0 failed / 2 warnings, CLEAR TO RELEASE.
- Ran the phase-wide no-relaxation audit: 5 commits touched gate/hook files across the whole
  phase (`1967e28d`, `9a1b9fda`, `4b35f2c1`, `ef1704d4`, `8394073a`); every one individually
  verified as a pure addition by reading the actual diff, not trusting the commit message.
- Drafted the full Dev-Research compositing trail and attempted to land it at
  `~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-reward-guard-jurisdiction/`;
  blocked by the worktree's own Write-tool sandbox (confirmed on two independent attempts,
  including a scratchpad path outside the worktree), a stronger block than the
  `scripts/write-scope-check` room-scope refusal the plan anticipated (also independently
  confirmed live: the room registry's active room is `launchpad-02`, not
  `rethinking-mindrianos`). Neither guard was bypassed; the trail was staged in-repo instead.
- Paused at the plan's own `checkpoint:human-verify gate="blocking"` with the full roll-up
  table and no-relaxation verdict list. The navigator independently re-ran the full reward
  audit, the full release gate, and the gate-10e diff directly against the worktree before
  approving -- not a rubber-stamped "approved" on the executor's report alone.

## Task Commits

Each task's real work was committed atomically:

1. **Task 1: Promote the whole-tree audit to a fail-closed release gate, correct the rule
   doc** - `8394073a` (feat)
2. **Task 2: Register GUARD-01..10, update ROADMAP/CHANGELOG/knowledge-base** - `ca5a42ed`
   (docs)
3. **Task 3: Draft + stage the compositing trail, run the roll-up and no-relaxation audit**
   (checkpoint action, pre-approval) - `8b0696fa` (docs)

No separate plan-metadata commit for `STATE.md`: this executor ran in worktree isolation
mode, where `STATE.md` updates are the orchestrator's responsibility after this wave merges.
`ROADMAP.md` and `REQUIREMENTS.md` ARE in this plan's own `files_modified` scope per its
frontmatter (this plan closes the whole phase's tracking, not just one plan's checkbox) and
were edited directly, per explicit dispatch instruction.

## Files Created/Modified

- `scripts/verify-release` - gate 10e added (whole-tree `commands/*.md` reward audit,
  fail-closed)
- `docs/reward-before-investment-rule.md` - "Detection mechanism" and "Two scopes" sections
  rewritten to state the live enforcement surface accurately
- `.planning/REQUIREMENTS.md` - `### Phase 267.3` section added (10 GUARD IDs), Traceability
  section updated (70 -> 80 active requirements)
- `.planning/ROADMAP.md` - Phase 267.3 section: plan 267.3-08 marked `[x]`, `7/8` -> `8/8`,
  phase-outcome paragraph added, compositing-trail status note added
- `CHANGELOG.md` - `[Unreleased]` entry naming gate 10e, the registry, the field, the 67
  declarations
- `.planning/debug/knowledge-base.md` - `reward-guard-staged-scope-debt-ratchet` entry
- `.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-08-COMPOSITING-TRAIL-STAGED.md`
  (new) - the full compositing trail content, staged pending a room-context switch

## Decisions Made

See `key-decisions` in the frontmatter above for the five load-bearing calls. The most
consequential: when the room write was blocked by a guard the plan did not fully anticipate
(worktree-Write-tool sandboxing, stronger than the anticipated `write-scope-check` room-scope
guard), the correct response -- per this plan's own explicit instruction and the standing
`destructive_git_prohibition`/authorization discipline -- was to stage the work honestly
rather than find a workaround, and to report the actual block encountered rather than only
the one the plan text predicted.

## Deviations from Plan

**None requiring a Rule 1-4 invocation.** One process note worth naming: the plan's
`<action>` for Task 3 anticipated `scripts/write-scope-check` as the specific blocker on the
room write ("if `scripts/write-scope-check` blocks the write, stage the file..."). The actual
first block encountered was the worktree's own Write-tool sandbox refusing any path outside
the worktree root -- confirmed on the room path itself and, separately, on a scratchpad path
also outside the worktree, before `write-scope-check` (a PreToolUse hook that only fires
inside a live Claude Code session, not from this tool-level sandbox check) ever had the
chance to run. This is not a contradiction of the plan's instruction, since the plan's
governing principle ("do not bypass the guard, stage the file, report the block plainly")
applies identically regardless of which specific mechanism produced the block; it is recorded
here because the plan text named a narrower mechanism than what was actually observed, and an
honest report names the mechanism that actually fired. The `write-scope-check` room-scope
guard was independently confirmed to ALSO apply (`~/MindrianRooms/.rooms/registry.json` reads
`"active": "launchpad-02"`), so both guards would have blocked the write; neither was tested
by bypassing the other, since the worktree sandbox refused the attempt before any room-scope
logic could run.

## Issues Encountered

None blocking. The room-write block (see above) was anticipated in spirit by the plan and
handled exactly as instructed: staged, reported, not bypassed.

## Pre-existing Test Debt (confirmed unaffected, not fixed here)

Both re-confirmed live during this plan's Task 3 roll-up, both already logged before this
plan started (first noted in `267.3-06-SUMMARY.md` and `267.3-07-SUMMARY.md`), both unrelated
to any file this plan touched (`scripts/verify-release`, `docs/reward-before-investment-rule.md`,
`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `CHANGELOG.md`,
`.planning/debug/knowledge-base.md`):

- **DEFERRED-267.3-D1** - `bash tests/run-all-118.sh` exits 1, 3 of 16 suites fail
  (`lib/agents/mva/test-all-six-agents.cjs`, `lib/core/mva-deck-builder.test.cjs`,
  `lib/core/mva-option-router.test.cjs`). None reference `mva-rule-linter`,
  `first-reward-surfaces`, or `check-reward-before-investment` (the linter's own suite is
  green). Pre-dates this plan.
- **DEFERRED-267.3-D3** - `bash tests/run-all-267.1.sh` exits 1, 1 of 2 discovered suites
  fails (`test-267-1-first-install-hooked-audit.cjs`, a GAP I-1 pinning assertion now finding
  4 `~/.mindrian-user.md` references instead of 1, all 3 extra hits in
  `lib/mcp/tools/identity.cjs`, a Phase 270 file this plan never touched). Pre-dates this plan.

Both are already assigned to their correct owners (a `/gsd-quick --validate` pass, or Phase
267.2/270 respectively) in the deferred-items log; no new entry was needed here.

## Known Stubs

**The Dev-Research compositing trail is staged, not landed.** Its full content lives at
`.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-08-COMPOSITING-TRAIL-STAGED.md`
(committed `8b0696fa`). Per the coordinator's own instruction on this checkpoint's approval,
the orchestrator will handle the room-trail copy into `~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-reward-guard-jurisdiction/`
after merge (a room-context switch is outside this worktree's authority). This is not a code
stub affecting the phase's functional goal (all ten GUARD requirements are genuinely
satisfied and independently navigator-verified); it is a documentation-delivery step
explicitly deferred to a context with room-write authority, tracked so it is not silently
lost.

## User Setup Required

None - no external service configuration required. The one pending manual step (the room
write) is explicitly the orchestrator's, per the coordinator's own message on this
checkpoint's approval, not a user action.

## Next Phase Readiness

**Phase 267.3 is CLOSED.** All 8 plans across 7 waves executed; all ten GUARD-01..10
requirements registered and satisfied; the navigator independently re-verified the full gate
roll-up and the no-relaxation audit before approving. No blockers carry forward from this
phase's own scope. Two items explicitly NOT absorbed by this phase and named so a later
reader does not assume otherwise: `DEFERRED-271-D2` (a Canon Part 11 SHAPE-declaration
question, `agents/larry-extended.md` + 52 `skills/*/SKILL.md` advisory violations) stays OPEN
with no owner; Phase 267.2 still owns repairing the FIRST_INSTALL Reward and Investment legs
of the onboarding flow itself (this phase declared the enforcement contract, did not change
the flow). One post-merge action remains, owned by the orchestrator per this checkpoint's
resolution: land the staged compositing trail in `~/MindrianRooms/rethinking-mindrianos/research/`
and mirror it to `~/MindrianOS/research/`.

## Navigator Approval

**Approved 2026-08-28.** The coordinator relayed the navigator's sign-off: "Navigator
independently re-verified the roll-up before approving (re-ran the full reward audit, the
full release gate, and the gate-10e diff against your worktree directly) -- all confirmed as
reported. Phase close approved as-is, no changes needed." The room-trail landing is
explicitly retained as the orchestrator's post-merge action, not delegated back to this
worktree.

---
*Phase: 267.3-reward-before-investment-guard-jurisdiction*
*Completed: 2026-08-28*
